# 取证执行日志（层 1 完成 / 层 2-3 进行中）

配套计划：`00_计划_历史溯源与控制包取证.md`（已批准）

## 1. 负责人批准记录（2026-09-14 会话内问答）

| 事项 | 裁决 |
|---|---|
| 计划整体 | **批准，按默认参数执行**（分片权重、重叠 10 条、包逐个详析） |
| 前史 13 个独立根谱系 | **与主仓同等深度逐条分析** |
| 仅存 Git 历史的控制包 | **全部复原并逐个详析** |
| 入库方式 | **完成后提交，不登记到 AGENTS.md；提交时只在本目录内提交与推送，不连带其他目录的工作** |

## 2. 层 1 已固化的证据底座（全部由脚本生成，可重跑）

脚本目录 `_tools/`（按序执行即可复现）：

| 脚本 | 作用 | 产物 |
|---|---|---|
| `01_build_commit_evidence.py` | 逐提交机械固定证据：消息原文、numstat、增删改名、hunk 函数上下文、声明/类型/测试签名、include、CMake 目标、阈值与并行度赋值、台账 CSV 行变化、JSON/schema 键、文档标题增删、内容摘录；噪声区（run/ build/ testdata/ third_party/ GaiaDR3* BASS DR3* worktrees/ 等）只计数不抽取 | `_evidence/commits/index.jsonl`（1988 行）、`cards/NNNN-sha8.md`（1988 张全卡，33MB）、`commits_table.csv`、`cross_dir_month.csv`、`cross_id_month.csv` |
| `03_build_slices.py` | 紧凑卡 + 分片（固定窗口 + 相邻重叠） | `compact/`（1988）、`slices/S01..S37/{manifest.csv,BRIEF.md}`、`slices/index.csv` |
| `02b_build_pack_inventory_v2.py` | 控制包清点：worktree 解包件（不剪枝，允许嵌套与容器识别）、逐个 zip、影子树副本登记、仅存历史包整树复原（绑定来源 rev 与删除提交）、历史 zip 从 blob 复原、包内 SHA256SUMS 实测核对、台账 CSV 解析（表头/行数/状态字面量分布/任务号全集） | `_evidence/packs/{pack_inventory.csv,pack_instances.json,pack_files.jsonl,digest/*.md,history/**,history_zip/**,recovered.json}` |
| `05_pack_dispatch_table.py` | 身份归一后的代际谱系底稿 | `pack_lineage_table.md`、`dispatch_groups.csv` |
| `06_pack_groups.py` | 49 身份 → 14 派工组（+G99 残余） | `dispatch_groups.json` |
| `07_pack_commit_link.py` | 包台账任务号 × 提交消息编号 的机械对接 | `pack_commit_link.csv`、`pack_commit_link.md` |
| `04_check_slice_coverage.py` | 分片报告对 manifest 主责条目的覆盖率核对 | `reports/history/coverage.csv` |

### 2.1 提交侧机械事实
- 覆盖 **1988** 条提交（`--all` 全 ref：main 1981 + ci-fix 等额外 ref；含 13 个独立根、25 条 merge）。
- 分级：小 1170 / 中 596 / 大 191（+ merge）。巨型提交（36 条 >10k 行）走"结构模式"，只出文件集合与目录直方图。
- 噪声区计数已写入每卡（"噪声区 N"），影子树 `run/**` 不进入内容抽取。
- 分片：37 片，每片 55 条主责 + 10 条与上一片重叠（S01 无重叠），S37 为尾片。

### 2.2 控制包侧机械事实
- **49 个包身份 / 74 个存形态实例 / 32 个仅存历史的包目录整树复原 / 6 个历史 zip 从 blob 复原**。
- 身份来源形态：worktree 解包件、`工程控制/_control_packs/*.zip`、`engineering/control/archive/…` 归档件、`run/**` 影子树副本、`history/<rev>/…` 复原件、`history_zip/*.zip`。
- 已识别的容器型目录（内含子包）与父子收纳关系写入 `parent_pack` / `nested_packs`（例：2026-09-02 legacy 归档容器收录了 V4/V5/V6/ACR 等 8 个子包）。
- 对接表亮点（客观数字，解释留给阶段报告）：
  - `AstroCS_MAIN_RELEASE_CONTROL_V5…`、`AstroCS_V6_SYSTEM_REFACTOR…`、`prerelease_v5/AUDIT_REVIEW` 的台账任务号在提交消息中的命中率 **100%**；
  - `REAUDIT_V4/v4_reaudit`（98 号）命中率 **0%**，与 `b12305ed` 提交消息自述"自造包、非用户物料、已从当前树移除"相互印证（两条独立证据，不做因果判断）；
  - legacy 归档容器 199 号命中 190（95.5%）、V7 191 号命中 167（87.4%）。

## 3. 方法局限（后续引用者必读）
1. 编号对接只用提交消息文本中被正则捕获的编号，**不能**用来判断任务是否完成，只用来说明"包与提交在编号口径上是否对得上"。
2. 全卡有容量上限（小 ≤5.2KB / 中 ≤13KB / 大 ≤9KB），超限按固定顺序丢弃区块，丢弃会在卡内标注；完整文件清单在 `index.jsonl`。
3. 历史复原每个目录上限 400 文件（关键文件优先），超限写入 `_warnings.txt`；`engineering_v1.3`（401 文件）、legacy 容器（855 文件）等触顶。
4. zip 只读取清单与抽样内容，未解压进仓库；zip 与其解包件的逐文件差异核对留给层 2 报告。
5. 卡片里的"结构要点"是正则抽取，可能漏（例如未匹配到的宏定义、非 C++ 语言）也可能误（把注释当声明）；分析员被要求以 compact/card 为索引、必要时回读 `git show`，报告只写可核对内容。

## 4. 层 2 / 层 3 完成情况（两侧均已收口）

### 任务一（历史）`reports/history/`
- 37 份分片报告（每片 55 主责 + 10 重叠复核，正文条目组 1568）→ 8 份大阶段报告 → `00_OVERVIEW.md`。
- 覆盖率：主责提交 **1988/1988 = 100%**（`coverage.csv`）；重叠区 360/360 处理，一致 350 / 口径差 8 / 不一致 2（`overlap_recheck.md` 逐条裁定并保留双方原文）。
- 独立抽查 30 条：误读 0/30；部分相符 3（落点目录前缀、组条目漏最大落点），最严上界 6.7%（`spotcheck.md`）。

### 任务二（控制包）`reports/packs/`
- 16 份派工组报告（G01..G16，G99 并入 G13）→ 7 份代际阶段报告 → `00_OVERVIEW.md`。
- 覆盖：49 包身份 **49/49**；74 存形态实例 0 个完全未引用（3 处标"引用-缩写"并给判定依据）；3 个非包形控制件（`包规范.md`、`ACTIVITY_STATE.md`、`问题扫描/` RQS-2026-01）单列 coverage.md C 节 → **分析对象 52/52**。
- 组报告登记的证据缺口合计 **225 条**；`pack_events.md` 给出各包的"引入 / 台账最后落账 / 废止归档"三时刻；`digest_verify.md` 抽 6 代际 11 实例 37 指标全部一致（另记 3 类口径差异：_PROVENANCE.json 计数、CRLF↔LF 造成的 zip 与解包件自校验结论相反、CHECKLIST 大小写漂移）。
- P-G13 订正了 2 处 digest 与本体不一致：宪章对齐包首末提交方向倒置（真序 e9547c02 首 / f5f944a5 末）、G99 所带 98 号台账经核为 V5 系件（与 `prerelease_v5/AUDIT_REVIEW` 台账逐字节同）。

### 前台（我）对两侧结论的独立核验
见 `reports/_前台独立核验.md`：指针 4479 次对账异常 0、无幻觉路径、无代码夹带、9 代台账实测数字、V7 包内 SHA256SUMS 唯一不符项即其 TASK_LEDGER.csv、包↔提交对接表的方法局限。

## 5. 提交范围说明
本目录约 100MB。入库范围为"报告 + 脚本 + 索引表 + `index.jsonl` + 包 digest"，另有 `设计大纲/.gitignore` 排除 90MB 可重跑证据底座（cards 18M / compact 8M / history 复原 40M / history_zip 22M / pack_files.jsonl）；重建命令见 `_evidence/README.md`。**若负责人要全量入库，删除 `设计大纲/.gitignore` 再提交即可**（此选择为前台默认，负责人当时未答复）。
提交严格限定在本目录：仓库其他位置（含开工前既有的 60 余处脏文件，例如 `artifacts/prerelease_v5/ISA-00x`、`docs/TRACEABILITY.csv`、`evidence/v6_1_rework/CHK-001` 等，mtime 09-09~09-14，非本任务产物）一律不纳入本次提交；按负责人指令不登记 AGENTS.md。
