ADD|宪章期的槽位清单与路由链。|序列收束：START_HERE → ENTRY＋PROMPT → ＋README_INSTALLATION → AGENT_START 双套 → READ_FIRST＋PROMPT（V3 定标）→ V4 断档 → V5 恢复 → V6–V8.1 沿用 → V1 去 PROMPT → 救援回归 → 包规范只定 READ_FIRST → RQS 00_README。失效点：C237｜a78f5430 删体、V3 定标改名、宪章期改由槽位清单裁定。
ADD|（ACTIVITY_STATE.md L23，P-G15 §五）。|序列收束：根 18 份规格 → docs＋contracts 分层 → contracts 全 schema 化 → 顶层 9 份 → 15 份加 schemas 独立 → 19 份加 fixtures → 99 文件 24 规格 → 56 文件 16 规格加 baseline 内嵌 → 根 14 加 tasks 57 的 JSON 图。失效点：schemas 目录在 V8.1 取消、完整性担保在宪章期改由 git 与登记面承担。
ADD|包内数值死于 V8.1。|序列收束：CPU≥150% 绝对值 → 0.80×min 相对带宽 → 同式加快速失败 → p50/mean 分位数 → 阈值入验收合同 → 均值 80% 加 10s<50% → 包内零数值 → 宪章 §10.5/§18.2 与代码 0.85/0.60 现行；真机口径的载体由包内规格迁到仓库代码与登记件。
ADD|而不在包内（P-G07 §一 1.2）。|序列收束：无版本串 → 日期加特性长串 → 两行 → 语义化 1.3.0 → alpha 正则入校验器 → MANIFEST target_version → 包根 VERSION 同值 product_version → VERSION 语义改包版本 → 包内无版本件。审核包命名同步演变：每 Gate 一包、任务胶囊、单包带 UTC 戳、宪章期 hash 式、救援期取 sha256 前十二位（P-G05 §四、P-G07 §四、P-G10 §四、P-G14 §四）。
ADD|PASSED 亦不在其七值集内（口径注，P-G13 §四 4.3）。|序列收束：三值 → 四值 → 五值 → 六值 → 四态加结论词 → 五值 → 七值 → 六值 → 六值（REVIEW_PENDING 判死）→ 三套并存 → 删列外置 → 列回归全 NOT_STARTED → PASSED → 无台账 → fix_state；载体在包内清单、包外 JSON、仓库工具与登记件间四次迁移。
ADD|MAIN_ONLY_GIT_PROTOCOL，00_OVERVIEW §2.6）。|序列收束：十步提交加每 Gate 一包（≤25MiB/5MiB）→ 仅最终一次加胶囊 → 胶囊索引 → 单一 audit zip 加四类提交相等 → 出口校验器十九类 → verdict 四值加自斥容量门 → 三件套对账加五步提交门 → 宪章六步冻结 → 终审计自产物加基线只减不增 → 常驻修复账本。失效点：胶囊死于 V6、包内顺序条款自此让位宪章 §14.5。
ADD|不入库成常态"方向相反。|序列收束：单代理总提示词 → 一句话提示词 → 安装器 → 启动动作定型（校验非零即停）→ 连续执行加异步回执 → 执行态外置加哈希绑定 → 队列加租约派发 → cprun 空白靠前台手册 → 模板与规范件定八字段派发 → 明文不依赖队列的前台交接 → 负责人直令加自建 git 工作区。
DEL|；同型事实在宪章期是|互证）
DEL|同一份清单在两形态给出两组数是登记面的常态|两记。
DEL|、CP0 与 v3_cp0 同源却分两身份|（coverage.md B 节）
DEL|、V8.0 包体全仓零命中|（P-G12 缺口 1）
DEL|、实例数漏登影子副本与内嵌副本|P-G13 缺口 3）
DEL|、G-CI 至 G-REL 无判定记录|（P-G12 缺口 15）
DEL|、source_line_state 越列污染|（P-G16 缺口 5）
DEL|、审核包容量两个口径|（P-G10 缺口 3）
DEL|；合计五十二个分析对象|覆盖闭合
DEL|；类别协议内 9 类|（缺口 3）
