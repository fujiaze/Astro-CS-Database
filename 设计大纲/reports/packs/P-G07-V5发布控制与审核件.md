# P-G07 · V5 单 CLI/AMD64 发布控制与审核件（工程控制包取证报告）

取证基准：`设计大纲/_evidence/packs/dispatch_groups.json` 的 G07 条目（标题「V5 单 CLI/AMD64 发布控制包与 V5 时代审核件」，7 个身份）→ 逐个 digest → 按 digest 真实路径读本体；关键数字全部以 `git log / git cat-file / python3 zipfile / sha256sum` 在仓库现状上复算，不复用表格结论。zip 一律只读（未解压进仓库，校验实验在 /tmp 与 `git cat-file` 管道内完成）。

## 一、组内包清单与身份归并

### 1.1 身份—实例清单（实测）

| 身份 | 存形态实例 | 路径 | 文件数 | 摘要 / 字节 | 首次出现提交 | 最后出现提交 | 删除情况 |
|---|---|---|---|---|---|---|---|
| `AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828` | 仅历史复原 | `设计大纲/_evidence/packs/history/f99e80d8/工程控制/RELEASE_V5/AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828/` | 38（37 包件 + `_PROVENANCE.json` 1,039 B） | 100,484 B；`recovered_from_rev=f99e80d8574d…` | `f99e80d8` 2026-08-28T20:58:26+08:00（原路径 `工程控制/RELEASE_V5/…`） | `3ca7f974` 2026-08-30T17:07:02（该路径最后一次原地内容提交；共 107 个提交触及） | 未被删；`b7b2dea7` 2026-09-02T22:15:41 以 R100 移入 `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/` |
| 同上 | 归档解包件 | `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/RELEASE_V5/AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828/` | 37 | 99,444 B | `b7b2dea7` 2026-09-02T22:15:41（该路径唯一提交） | 同左 | 无 |
| 同上 | 影子树（gitignore） | `run/**/engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/RELEASE_V5/AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828/` | 37 × 72 份 | 72 份台账 sha 前缀均为 `907137567225` | 无 git 事件（`.gitignore:19 run/*`） | 无 | 无 |
| `AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_ALPHA_20260828` | zip（未跟踪） | `工程控制/_control_packs/AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_ALPHA_20260828.zip` | zip 内 37 条目 | zip 本体 sha256 `42a03c0b09168f3e865b05aa65578701beb74c268f0d52000cf3e4e82eecb7d8`，61,746 B；未压缩 100,077 B；内容聚合摘要 `4b4414645d8dab2a…aa5fca` | 无（`git ls-files` 0，`git check-ignore` 未命中 → 未跟踪且未忽略） | 无 | 无 |
| `REAUDIT_V4/v4_reaudit` | 仅历史复原 | `设计大纲/_evidence/packs/history/b12305ed/工程控制/REAUDIT_V4/v4_reaudit/` | 24（23 包件 + `_PROVENANCE.json` 425 B） | 72,610 B；`recovered_files=23`，`recovered_from_rev=b12305ed…` | `020cdc99` 2026-08-28T20:36:03（21 文件） | `ae643541` 2026-08-28T20:38:04（23 文件） | 已被删：`b12305ed` 2026-08-28T20:54:30（该路径 name-status 为 23 D + 2 R） |
| `工程控制/REAUDIT_V4` | 仅历史复原（同一棵树的外层容器） | `设计大纲/_evidence/packs/history/b12305ed/工程控制/REAUDIT_V4/` | 实测 25（23 包件 + 内层 425 B + 外层 `_PROVENANCE.json` 600 B） | 73,210 B；外层无任何自有包文件 | 同上 | 同上 | 同上（同一删除提交） |
| `prerelease_v5/AUDIT_REVIEW` | 解包件（已入库） | `artifacts/prerelease_v5/AUDIT_REVIEW/` | 34 | 163.1 KB（digest） | `ef0858c5` 2026-08-30T13:54:10 | `fd4e11ed` 2026-08-30T17:53:51（该路径共 6 个提交） | 未被删（当前树活跃）；另有 70 份 `run/` 影子副本 |
| `prerelease_v5/audit_src` | 解包件（未跟踪） | `artifacts/prerelease_v5/audit_src/` | 33 | 159.7 KB（digest） | 无（`git ls-files` 0） | 无 | 无 |
| `AUDIT_PACKAGE_587fe0e341a7` | zip（未跟踪） | `artifacts/prerelease_v5/AUDIT_PACKAGE_587fe0e341a7.zip` | 1,699 条目 | sha256 `81b7e39ed80c273b41057babb9f7aed3262aa097f95537d214e37906a298c14b`；4,918,009 B 压缩 / 15,518,698 B 未压缩 | 无 | 无 | 无 |

组内 7 身份实际收敛为 4 组物理对象：V5 控制包（37 文件，4 种形态）、REAUDIT_V4 包（23 文件，2 个身份指向同一棵树）、V5 时代审核件（34/33 文件目录 + 1 个大 zip）。

### 1.2 同名多形态逐文件比对（V5 三形态 + ALPHA zip）

- 文件集合：ZIP、历史复原件、归档解包件三者相对路径集合完全一致（37 项，无任何仅存在于单一形态的路径）。
- 唯一内容差异文件为 `02_TASK_LEDGER.csv`，四个实测摘要各不同：
  - ALPHA zip：`22e3d8919cf96248…`，10,712 B，98 行全 `NOT_STARTED`
  - 历史复原件（= `f99e80d8` 提交态，实测 `git cat-file` 同 sha 前缀 `12f7beb6c234`）：10,705 B，`BASE-001=PASS` + 97 `NOT_STARTED`
  - `52e9541d` 提交态（中间态）：`1b5ccf5c59b4`
  - 归档解包件（= `3ca7f974` 提交态，实测同 sha 前缀 `907137567225`）：10,079 B，88 `PASS` / 1 `BLOCKED` / 1 `REVIEW_PENDING` / 1 `IN_PROGRESS` / 7 `NOT_STARTED`
- 归档件相对 ZIP 另有两处 `depends_on` 差异：`PAR-007` 去掉 `PAR-002`；`WIN-009` 由 `WIN-008|VER-001` 改为 `VER-001`。`52e9541d` 提交消息对此的原文为「(审核包 item#9) 机器一致性校对: 纠正依赖边(PASS依赖非PASS=0)」。
- 其余 36 个文件在 ZIP / 复原件 / 归档件三形态逐字节相同（例：`00_READ_FIRST.md` 三处均为 `475c0bcc85773236336854e050c038617672c6126bea8864dbc42bcdc095d96f`）。
- 包内 `SHA256SUMS` 出厂记录即 ZIP 态台账：`SHA256SUMS` 第 3 行 `22e3d8919cf96248221b57892f0ce53258bced669c68de7db5ce69976a1b9965  02_TASK_LEDGER.csv`；实测 ZIP 内 36/36 条通过，两个解包件均 35 通过 / 1 不一致（不一致项恰为该台账）。
- ALPHA 差异（目录名 vs zip 名）：`..._AMD64_20260828.zip` 与 `..._AMD64_ALPHA_20260828.zip` 并非两个不同包——ALPHA 只出现在 zip 文件名上，zip 内顶层目录名与包内 37 路径均无 ALPHA；两身份的包内容只差在台账状态位。
- REAUDIT_V4 两个身份：23 个包件文件一一对应、内容一致；差异仅在 `_PROVENANCE.json` 份数与字节（600 vs 425）。digest 记录外层 24 文件 / 71.1 KB、内层 24 文件 / 70.9 KB（差 175 B = 600 − 425），实测外层整树为 25 文件 / 73,210 B（见第八节第 6 条）。

## 二、包内文件地图

### 2.1 V5 控制包（37 文件）

15 个 `NN_*.md`（无 `02` 号 md，`02` 位为台账）+ `PACKAGE_MANIFEST.md` + `START_PROMPT.txt` + `SHA256SUMS` + `02_TASK_LEDGER.csv` + `scripts/` 3 + `schemas/` 2 + `templates/` 13。各文件自述标题与职责：

| 文件 | 自述标题（首行） | 职责要点（引包内首段/章节） |
|---|---|---|
| `00_READ_FIRST.md` | `# AstroCS V5 预发布控制包：单一 CLI / amd64 CPU 自适应` | §0 唯一目标「在当前 main 上收敛出可发布候选」；§1 12 条硬约束；§2 首 30 分钟 6 步；§3 连续执行状态机；§4 唯一允许停止条件（4 条）；§5 最终 PASS 含义 |
| `01_PRODUCT_ARCHITECTURE.md` | `# 产品与执行架构冻结` | §1 用户可见产品；§2 单一 CLI 的模块边界；§3 Phase3 规则；§4 配置分层；§5 不进入本轮的范围 |
| `02_TASK_LEDGER.csv` | —（表头） | `PACKAGE_MANIFEST.md` 自述「98 个有序原子 Task 的唯一状态源」 |
| `03_TASK_DETAILS.md` | `# 逐 Task 施工规范` | §A 每 Task 固定 8 步；§B–§G 按 BASE/DOC/SCI/ALG、ARCH/API/CLI/Phase3、CPU Backend/Benchmark、Monitor、Synthetic、Linux/Windows/Release 分节给输入-动作-验证-产物-PASS；§H 禁止 Agent 的替代解释（8 条） |
| `04_CLI_COMMAND_AND_PROTOCOL_CONTRACT.md` | `# CLI 命令、协议与兼容性合同` | §1 命令清单；§2 退出码冻结；§3 stdout/stderr；§4 JSONL 事件 v1；§5 取消与崩溃；§6 机器化接口一致性 |
| `05_CPU_BACKEND_ABI_AND_PACKAGING.md` | `# amd64 CPU Backend ABI、ISA 与安全加载` | §1「先由 profile 找到热点…再为有收益的 kernel 实现变体」；§2 编译隔离；§3 CPU 与 OS 状态检测（6 项）；§4 稳定 C ABI v1；§5 Kernel 注册粒度；§6 失败与回退；§7 发布检查 |
| `06_BENCHMARK_AND_PROFILE_SPEC.md` | `# Benchmark、自动调优与 cpu_profile 合同` | §1 原则「速度永远不能使错误路径获胜」；§2 硬件画像；§3 候选空间；§4 计时与选择；§5 profile 生命周期；§6 未 benchmark 的行为；§7 命令模式 |
| `07_RESOURCE_MONITOR_AND_UTILIZATION_GATE.md` | `# 内置资源监控与“禁止低利用率”门禁` | §1 强制启用；§2 必采指标；§3 机器门禁；§4 快速失败避免浪费 32R；§5 内存泄漏/增长；§6 自动诊断输出 |
| `08_SCIENCE_SYNTHETIC_AND_EXTERNAL_REVIEW.md` | `# 科学文档、合成 Oracle 与独立审核合同` | §1 六层单向推导；§2 科学文档最低内容；§3 算法与工程文档；§4 合成验证目录合同；§5 禁止历史束缚；§6 外部科学审核 |
| `09_LINUX_WINDOWS_BUILD_RELEASE.md` | `# Linux 控制节点、Windows 重计算节点与发布` | §1 vm-bj 职责；§2 Fatduck 九步顺序；§3 接缝回归专项；§4 双平台一致性；§5 发布物命名 |
| `10_GIT_REVIEW_CAPSULE_AUDIT_PACKAGE.md` | `# main-only Git、审阅胶囊与审核包` | §1 原子提交；§2 每 commit 的 review capsule；§3 审阅节奏；§4 最终审核包白名单；§5 最终 verdict |
| `11_AGENTS_MD_REQUIRED_BLOCK.md` | `# 必须合并到根 AGENTS.md 的短块` | 内嵌一段 md 短块（`## AstroCS 开发与节点`），要求并入根 `AGENTS.md` |
| `12_V3_V4_MIGRATION.md` | `# V3/V4 继承、失效与当前已知风险` | §1 允许继承；§2 明确失效 8 项；§3 必须进入 FINDINGS 的已知风险 R1–R6 |
| `13_ALPHA_VERSION_AND_PHASE3.md` | `# Alpha 版本合同与 Phase3 科学施工边界` | §1 版本格式；§2 Phase3 冻结定义；§3 Agent 不得自行决定的科学项（12 项）；§4 Alpha 最小实现候选；§5 Phase3 独立 Oracle |
| `14_V4_COVERAGE_MATRIX.md` | `# V4 内容继承覆盖矩阵` | 首句「V5 必须包含 V4 全部有效要求；本表防止架构改版遗漏。」；21 行 V4 主题→V5 承接位置→结论 + 「V5 新增」清单 |
| `15_CONTINUOUS_CHECKPOINTS.md` | `# 连续检查点与控制包/审核包工作流` | C0–C9 检查点表（所需 Task / 逐项机器检查 / 失败动作）+ §控制包与审核包循环（5 条）+ §Checkpoint 记录规则 |
| `PACKAGE_MANIFEST.md` | `# 控制包内容清单` | 入口与施工 / 专项合同 / 机器资产三段清单 |
| `START_PROMPT.txt` | （无标题，单段提示词） | 「在当前仓库外解压 V5 控制包，先运行 scripts/validate_control.py，再严格执行 00_READ_FIRST.md 与 02_TASK_LEDGER.csv。」 |
| `SHA256SUMS` | — | 36 条，覆盖除自身外全部包件 |

目录命名口径：`tasks/` 目录不存在（逐号规格集中在 `03_TASK_DETAILS.md` 的 A–H 分节，非 per-task 文件）；`scripts/` 3 个（`validate_control.py` 144 行、`package_final.py` 71 行、`validate_final_package.py` 149 行）；`schemas/` 2 个（`cli_event.schema.json` 965 B，title `AstroCS CLI JSONL event v1`，required 10 字段；`cpu_profile.schema.json` 3,050 B，title `AstroCS CPU profile v1`，required 7 字段）；`templates/` 13 个（12 个 CSV 各仅 1 行表头 + `SUMMARY.json` 15 行）。

### 2.2 REAUDIT_V4（23 文件）

9 个 `NN_*.md`（00–08）+ `EVIDENCE_C0.md` + `02_TASK_LEDGER.csv` + `scripts/` 4 + `templates/` 9。无 `SHA256SUMS`、无 `START_PROMPT.txt`、无 `PACKAGE_MANIFEST.md`、无 `schemas/`、无 `tasks/`。

| 文件 | 自述标题 | 职责 |
|---|---|---|
| `00_READ_FIRST.md` | `# AstroCS MAIN 预发布重审控制 V4` | §唯一目标（5 条，含「单一 astrocs CLI」「强制资源门禁」）；§不可变规则 1–12；§执行入口 1–5；§当前证据起点（继承） |
| `01_WORKFLOW_AND_GATES.md` | 检查点链（C0–C9，连续执行，不得跳过） | §检查点链表；§连续执行纪律；§资源门禁（判定权在机器指标）；§历史代码使用 |
| `03_TASK_SPECIFICATIONS.md` | 按 C0–C9 分节（行 6/38/88/138/200/274/408/494/532/582） | 98 号逐号规格 |
| `04_CHECKPOINT_CHECKLISTS.md` | C0 控制与起点 … C9 32R 与发布候选 | 每检查点勾选清单；C9 行含「输出 AWAITING_EXTERNAL_RELEASE_REVIEW（C9-005，唯一允许终态）」 |
| `05_NUMERICAL_AND_PERFORMANCE_GATES.md` | 数值门禁 / 资源门禁 / 性能基准 | 数值与资源阈值表 |
| `06_GIT_MAIN_ONLY.md` | 每个 Task 的固定流程 / 禁止 / 历史锚 | 提交纪律与 message 格式 |
| `07_AUDIT_PACKAGE_SPEC.md` | 体积 / 必须包含 / 禁止包含 / 大产物引用 / 固定打包命令 / 一致性 | 审核包规范 |
| `08_V2_INHERITED_FINDINGS.md` | （含 §额外 V3 审核问题） | V2 期 P0/P1/P2/P3 继承基线表 |
| `EVIDENCE_C0.md` | C0-001 控制包自校验 / C0-002 起点 SHA 冻结 / C0-003 工具链与主机盘点 / C0-004 V3 继承证据与债务登记 / C0-005 账本初始化与策略核验 | 包内唯一的 C0 阶段留痕文件 |
| `scripts/` | `validate_control.py`(95 行)、`validate_audit_package.py`(171)、`package_audit.py`(90)、`make_capsule.sh`(19) | 三个校验/打包门 + 胶囊脚本 |
| `templates/` 9 | `BUILD_RESULTS.csv`、`CHECKPOINT_RESULTS.csv`、`COMMITS.csv`(8 行)、`FINDINGS.csv`、`LARGE_ARTIFACT_MANIFEST.csv`、`PERF_RESULTS.csv`、`SUMMARY.schema.json`(27 行)、`TEST_RESULTS.csv`、`TRACEABILITY.csv` | 8 个仅表头；`COMMITS.csv` 出厂即含 7 行 V3 期真实数据（CON-006..CON-009） |

### 2.3 V5 时代审核件

- `AUDIT_REVIEW` 34 文件 = 根 18（`00_READ_FIRST.md`、`TASK_LEDGER.csv`、`FINAL_REPORT.md`、`SUMMARY.json`、`MANIFEST.json`、`SHA256SUMS` + 12 张表：`BUILD_RESULTS/CHECKPOINTS/COMMITS/CPU_PROFILE_RESULTS/FINDINGS/LARGE_ARTIFACTS/RELEASE_ARTIFACTS/RESOURCE_RESULTS/REVIEW_CAPSULE_INDEX/SCIENCE_CLAIMS/TEST_RESULTS/TRACEABILITY`）+ `control/` 7 份 V5 规格（01、03、04、05、09、10、13）+ `reports/evidence/` 9 份 md。
- `audit_src` 33 文件 = 上述减去 `SHA256SUMS`；`tools/assemble_audit.py:5-6` 自述前者为「包_final 的 SOURCE(白名单文件就位)」、后者为「package_final 输出(审核包)」。
- `AUDIT_PACKAGE_587fe0e341a7.zip` 1,699 条目 = 根 17（含 `00_README.md` 697 B、`MANIFEST.json` 386,944 B、`SHA256SUMS` 194,984 B）+ `code/` 1,071（lib 738、docs 129、tools 102、tests 87、cli 10、launch 2、schemas 2、include 1）+ `evidence/` 611（`reports` 565、`audit_review` 34、`tables` 12）；其中 `reports` 二级分布 REAUDIT_V3 348、evidence 49、v19r3 35、v19r2 31、v19r7_quality 29、v19r8_quality 24 等。
- 模板/表命名口径差异：V5 用 `CHECKPOINTS.csv`、`LARGE_ARTIFACTS.csv`；REAUDIT_V4 用 `CHECKPOINT_RESULTS.csv`、`LARGE_ARTIFACT_MANIFEST.csv`、`PERF_RESULTS.csv`、`SUMMARY.schema.json`；V5 审核件把 `02_TASK_LEDGER.csv` 改名 `TASK_LEDGER.csv`、把包内 `templates/` 换成已填值的根级同名表。

## 三、任务结构

| 项 | V5 控制包（三形态） | REAUDIT_V4 | AUDIT_REVIEW / audit_src / AUDIT_PACKAGE |
|---|---|---|---|
| 任务数 | 98 行 | 98 行 | 98 行（同一号族） |
| 台账表头 | `task_id,phase,depends_on,scope,required_commit,required_push,preferred_host,status` | `task_id,gate,depends_on,scope,required_commit,required_push,checkpoint,status` | 与 V5 相同 8 列 |
| 号族（族:数） | 23 族：ABI3 ALG7 API5 ARCH5 BASE1 BENCH5 CLI8 DOC1 DOCCHK2 GOV1 ISA5 ISO1 LNX5 MON4 P34 PAR7 REL4 REV3 SCI7 SYN9 TRACE1 VER1 WIN9 | 10 族：C0:5 C1:8 C2:8 C3:10 C4:12 C5:22 C6:14 C7:6 C8:8 C9:5 | 同 V5 |
| 状态字面量 | 校验器集合 6：`NOT_STARTED/IN_PROGRESS/PASS/FAIL/BLOCKED/REVIEW_PENDING`（`scripts/validate_control.py:11`） | 校验器集合 5：无 `REVIEW_PENDING`（`scripts/validate_control.py:12`；`validate_audit_package.py:15` 同） | 同 V5 |
| 状态分布 | ZIP 98 NOT_STARTED；复原件 1 PASS/97 NOT_STARTED；归档件 88 PASS/1 BLOCKED/1 REVIEW_PENDING/1 IN_PROGRESS/7 NOT_STARTED | 020cdc99 态 98 NOT_STARTED；ae643541 态（=复原件）93 NOT_STARTED/5 PASS | 与归档件同（台账 sha `907137567225`） |
| 其他列分布 | phase 17 值（CODE12 TEST9 WINDOWS9 CPU8 PAR8 SCI7 ALG7 ARCH5 API5 BENCH5 LINUX5 MON4 RELEASE4 DOC3 REVIEW3 BASE2 CHECK2）；host Linux 86 / Windows 12；`required_commit` yes90/no8、`required_push` yes90/no8 | `gate` 与 `checkpoint` 两列取值逐行相同；`required_commit`、`required_push` 全部 yes（98/98） | — |
| 依赖声明 | `depends_on` 以 `\|` 分隔多父，47 行含多依赖；`scripts/validate_control.py:80` 拒前向依赖、`:84` 缺族报错；顺序另由 `15_CONTINUOUS_CHECKPOINTS.md` C0–C9 表与 `03 §A.1` 承担 | 98 行中 97 行有依赖、0 行含 `\|`（全部单亲）；`validate_control.py:67-68` 硬要求行数恰为 98、`:78` 要求 checkpoint 匹配 `C[0-9]` | `COMMITS.csv` 90 行（90 个号有提交记录，8 个号无）；`REVIEW_CAPSULE_INDEX.csv` 88 行；`CHECKPOINTS.csv` 仅 C0、C1 两行；`TRACEABILITY.csv` 66 行；`TEST_RESULTS` 5、`BUILD_RESULTS` 2、`RELEASE_ARTIFACTS` 2、`LARGE_ARTIFACTS` 2；`FINDINGS/SCIENCE_CLAIMS/RESOURCE_RESULTS/CPU_PROFILE_RESULTS` 均 0 行（仅表头） |

## 四、门禁与验收要求（全部标出处）

### 4.1 V5 控制包

机器门清单：
1. `scripts/validate_control.py` → 输出 `CONTROL_PASS tasks=<n> files=<n> sha256=<聚合>` / `CONTROL_FAIL`。检查项：REQUIRED 35 文件存在且非空、台账 8 字段（`EXPECTED_FIELDS`）、`:61` 任务数下限 85、号族前缀 17（`MANDATORY_PREFIXES`）、`:80` `forward dependency`、`:84` `missing task family`、`:96-102` 11 个必需短语（含 `AVX-512`、`cpu_profile.json`、`32R`、`AWAITING_EXTERNAL_RELEASE_REVIEW`）、`:107` 禁用台账号 `forbidden_ledger = ["ACR-001","RUN-003","RUN-004","CP0"]`、`:109` 包内出现分支创建命令即失败、`:123/127` `SHA256SUMS` 文件集合与逐条 hash、`:139` 输出聚合摘要。
2. `scripts/package_final.py` → `PACKAGE_SELECTION_PASS`；白名单 `ROOT_FILES` + `ALLOWED_DIRS{control,docs,source_review,reports,metrics,screenshots,capsule_index}` + `ALLOWED_SUFFIXES` + 单文件 5 MiB（`:18`）。
3. `scripts/validate_final_package.py` → `FINAL_PACKAGE_PASS files=<n> bytes=<n>`（`:144`）或失败清单。检查项：18 必需表；单文件 5 MiB / 总包 25 MiB（`:11-12,50,56`）；禁目录与禁后缀（`:20-21,52,54`，含 `.zip/.dll/.fits/.testdata`）；台账无未完号（`:72`）；`COMMITS.csv` 每行 `branch=main` 且 `push_status=PASS`（`:76`）；BUILD `ignored_errors=0`（`:82`）；TEST 无 FAIL（`:85`）；RESOURCE 每行 `verdict=PASS`（`:88`）；CHECKPOINTS 每行 PASS（`:91`）；RELEASE_ARTIFACTS 必须同时含 Linux 与 Windows、`arch=amd64`、version 匹配 alpha 正则（`:95-98`）；`SUMMARY.json` 的 `verdict` 必须为 `AWAITING_EXTERNAL_RELEASE_REVIEW`、version 为 alpha、`windows_32r_run_id` 非空（`:104-109`）；SUMMARY 各类计数与表一致（`:115`）。
4. 检查点机器门：`15_CONTINUOUS_CHECKPOINTS.md` C0–C9 表（每行「所需 Task / 逐项机器检查 / 失败动作」），并规定「检查点是机器核对，不是让 Agent 等人批准」；§Checkpoint 记录规则要求 `required_tasks` 展开、状态由 checker 计算而非手填。

完成顺序：`00 §2`（首 30 分钟 6 步，含「依次执行，不得先改代码」）；`00 §2.6`「同一时间只能有一个 Task 为 IN_PROGRESS」；`03 §A.1-8`（依赖全 PASS → 改 IN_PROGRESS → fetch 核对三 SHA → 抄规格 → 只改 scope 文件 → 跑指定测试 → `(<task_id>) <单一目的>` 提交并 push → 生成 review capsule）；`15` C0–C9；`09 §2` Fatduck 九步严格顺序；`10 §1`（一 Task 一原子 commit，push 后记 `COMMITS.csv`）。

状态机口径：`00 §3` 六态流转 + 各态定义 + 「禁止 DEFERRED 用来绕过预发布必需项」，与 `scripts/validate_control.py:11` 集合同值；`00 §1.12`「Task 状态只能来自 02_TASK_LEDGER.csv」。

waiver / 豁免：本包无豁免通道，四处明文：`00 §3`（禁 `DEFERRED`）、`03 §A.8`「任何 PARTIAL、waiver、忽略错误均不是 PASS」、`12 §2` 将「CON-010 一类失败后 waiver」列为明确失效、`15` C4 行失败动作「不得 waiver」。资源门禁数值：`07 §3` `avg_equivalent_cores >= 0.80 * min(selected_workers, available_cpus)`；`07 §4` 前 10 秒低于阈值即快速失败并返回退出码 10；`07 §5` 内存增长判据。仓库根的 waiver 工具链（`ci/checks.json`）不在本包内、本包文件未引用。

审核包与提交要求：`10 §2` 文件名 `capsules/<task_id>_<commit12>.zip`，必含 `CAPSULE.json`/`commit.patch`/`changed_files.txt`/变更文件完整最新版/相关测试与资源摘要/findings/`SHA256SUMS`，SCI/ALG/ARCH/API 提交另需带该层级全部正式文档与 traceability 子集；`10 §3` 抽样 core 100%、other 20%；`10 §4` 白名单 + 禁止 `.git`/历史包/testdata/FITS-XISF-HISS/build/exe-dll-so/单文件 >5MiB/总包 >25MiB，大型证据以 `LARGE_ARTIFACTS.csv` 路径+hash 引用；`10 §5` 六条件全成立才可生成终态；`PACKAGE_MANIFEST.md` 规定 `SHA256SUMS` 覆盖除自身外全部文件；`04 §2` CLI 退出码冻结为 0,2,3,4,5,6,7,8,9,10,70；`08 §4` 规定 `tools/validation/<group>/` 五文件合同；`13 §1` 版本格式 `MAJOR.MINOR.PATCH-alpha.N`。

### 4.2 REAUDIT_V4

机器门清单：`scripts/validate_control.py`（REQUIRED 20 项 = 9 规格 + 8 模板 + 2 脚本 + 台账；`:55-61` 表头 8 字段；`:67-68` 行数必须等于 98；`:70` 状态白名单 5 值；`:78` `checkpoint` 必须匹配 `C[0-9]`；`:90` 输出 `CONTROL_PASS files=<n> sha256=<聚合>`）。`scripts/validate_audit_package.py`（`:13-14` 5 MiB / 25 MiB；`:16` 必需清单；`:22-23` 禁目录/禁后缀；`:67-71` 疑似密钥扫描；`:87-88` 各表 status 白名单；`:94` `SUMMARY.verdict` 只接受 `{"AWAITING_EXTERNAL_REVIEW","FAIL","BLOCKED"}`，否则报「SUMMARY verdict lets agent self-accept」；`:104` SUMMARY 与表计数一致；`:115-127` `SHA256SUMS` 行格式/去重/集合/逐条 hash；`:135-152` `MANIFEST.json` 文件集合、file_count、total_bytes、逐条 size/hash）。`scripts/package_audit.py:15,78` 输出 zip ≤25 MiB。`scripts/make_capsule.sh` 固定输出 `run/reaudit_v4/capsules/capsule_<TASK>_<UTC>.zip`，roots 为 `工程控制/REAUDIT_V4/v4_reaudit` + `reports/REAUDIT_V4`。

完成顺序与状态机：`00 §执行入口` 1–5（读 00→01；`validate_control.py` 非 `CONTROL_PASS` 立即停止；「逐行执行 02_TASK_LEDGER.csv（98 行，不得跳号）」；C0–C9 按 `04` 打包核验）；`00 §不可变规则 7` 唯一状态词 5 值；`01 §检查点链` C0–C9 连续不得跳过。

豁免：`00 §不可变规则 6`「Agent 无权改变任务、阈值、状态词、豁免条件与执行顺序」；规则 12「历史 PASS/waiver/PARTIAL/忽略错误/视觉判断均不能替代当前证据」；规则 8/9（并行与门禁前置）「任何生产重计算阶段持续 >1s 不得单线程；累计串行计算时间不得超过总计算时间 1%」「资源门禁与 C6 测试未通过前，禁止 32R 全量、历史全量或任何 >60s 科学运行」；规则 10 32R 仅当前候选一次。数值与资源阈值见 `05 §数值门禁/§资源门禁/§性能基准`，资源门禁公式另见 `01 §资源门禁`（均值核心利用率 ≥ min(affinity 核数×50%, 已配置并行度)）。

审核包要求：`07 §体积`（≤25 MiB）、`§必须包含`、`§禁止包含`、`§大产物引用`、`§固定打包命令`、`§一致性`；提交格式 `06 §每个 Task 的固定流程` 要求 commit message 为 `<task_id>: <摘要>`；终态 `00 §执行入口 5` 与 `03 C9-005` 为唯一输出 `AWAITING_EXTERNAL_RELEASE_REVIEW`。

### 4.3 审核件实测门禁结果（复算）

- `python3 <V5 归档件>/scripts/validate_final_package.py artifacts/prerelease_v5/AUDIT_REVIEW` → `FINAL_PACKAGE_FAIL`，四条：`non-PASS tasks: ['PAR-002','REV-002','WIN-006','WIN-007','WIN-008','REV-003','REL-001','REL-002','REL-003','REL-004']`、`invalid commit row: PAR-002`、`invalid commit row: REV-002`、`invalid final verdict`。
- `AUDIT_REVIEW/SUMMARY.json` 的 `verdict` 实为 `RELEASE_NOT_READY_BLOCKED`（非 `AWAITING_EXTERNAL_RELEASE_REVIEW`），`final_commit` `59649edc0480…`，`windows_32r_run_id` `4f885b8b8fd4`；`FINAL_REPORT.md` 结论段原文「当前候选**未达发布门槛**(09 §5 / 10 §5)」。
- `AUDIT_REVIEW/SHA256SUMS` 33 条全部通过；`AUDIT_PACKAGE_587fe0e341a7.zip` 内 `SHA256SUMS` 1,698 条全部通过，无 >5 MiB 单文件、无内嵌 zip。
- V5 三形态各自跑 `validate_control.py`：ALPHA zip（/tmp 解包）`CONTROL_PASS tasks=98 files=37 sha256=4b4414645d8d…`；归档解包件 `CONTROL_FAIL`（仅 `hash mismatch: 02_TASK_LEDGER.csv`）；历史复原件 `CONTROL_FAIL`（另加 `SHA256SUMS file set mismatch`，因目录内多出 `_PROVENANCE.json`）。

## 五、设计意图（转述）与负责人/用户指令痕迹

包自述目标与禁止项（转述）：V5 要「在当前 main 上收敛出可发布候选」（`00 §0`），把 V3/V4 期的结构性问题改为单一 CLI + 纯 CPU 多后端 + 强制资源门禁；禁止项集中在 `00 §1`（禁分支、禁非 amd64、禁 ACR/GPU、禁硬编码、禁 `-march=native`、禁低利用率、禁历史输出冒充）、`03 §H`（8 条禁止替代解释）、`13 §3`（12 项 Agent 不得自行决定的科学项，缺项只能标 `UNRESOLVED-SCIENCE`）、`08 §5`（禁止历史束缚）、`05 §1`（「AVX-512 不是天然更快…没有实测收益就不选，甚至不发布」）。

对前代的继承与废止声明：`12 §1`「只继承实现，不继承 PASS」；`12 §2` 明确失效 8 项（历史 A/B/C 对比、重跑旧 32R、用历史 baseline 判当前、ACR/GPU 接线、CON-010 类 waiver、CP0/CP1 频繁外部停工、局部 checker PASS 当科学一致、把大日志/testdata/build 打进审核包）；`12 §3` R1–R6 六项风险要求入 FINDINGS；`14` 21 行矩阵声明 V5 必须覆盖 V4 全部有效要求。REAUDIT_V4 的对应声明在 `00 §当前证据起点（继承）`：以 `run/reaudit_v3/AstroCS_REAUDIT_V3_REVIEWPACK_20260828T1126Z.zip`（sha256 `76a43bdd…4949089`）为唯一继承证据，并把 TSan 5634 条竞争、mosaic 层缺口、串行架构、stage1 版本漂移、BLD-002/003 五项债务分别映射到 C1-007/C5-017/C6-013/C8/C9；`08` 另立 V2 期 P0–P3 基线表。

负责人裁决 / 用户指令痕迹（实测）：
1. `artifacts/prerelease_v5/BASE-001/BASELINE.json` 的 `user_directives_registered` 第 1 条：`"删除前会话自造的'V4重审控制包', 使用用户上传的 AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828 为唯一 V4 对照基准"`，并记 `executed_in_commit = b12305ed0df13318f9b0369…`。
2. 删除提交 `b12305ed` 2026-08-28T20:54:30 消息原文首句「chore(v5): 按用户指示移除自造V4重审控制包并归位用户上传的V4_CPU_ADAPTIVE原包」。
3. `artifacts/prerelease_v5/BASE-001/V3_V4_RISK_REGISTER.csv` 第 2 行 R0 的 `source` 字段为「BASE-001用户裁定」，描述前会话自造包已被移除、V4 语义以用户上传包（54 任务）为准，`status=CLOSED`，证据指回 `BASELINE.json`。
4. 审核件 `CHECKPOINTS.csv` C0 行记录 checker `python3 tools/check_agents_gov.py` exit 0，锚定 commit `15720f5efb2a93aaf855f348ae49223e509c70a6`（GOV-001），C1 行链式列出 6 个 checker 命令；对应 `11_AGENTS_MD_REQUIRED_BLOCK.md`（要求把短块并入根 `AGENTS.md`，`00 §2.5` 为第 5 步）。
5. `52e9541d`、`fcb4056f`、`fd4e11ed` 三条提交消息均为审核包一致性/重打记录，含「validate 保持 FINAL_PACKAGE_FAIL(诚实 RELEASE_NOT_READY…)」「validate=RELEASE_NOT_READY_BLOCKED(诚实)」字样。
6. 负责人裁决专用文件在本组不存在：对 `OWNER_BINDINGS` / `04_OWNER_DECISIONS` / `07_FRONT_DESK_RULINGS` / `*RULING*` 全仓检索命中的是 `工程控制/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909/`、`工程控制/AstroCS_RELEASE_RESCUE_CONTROL_V3_20260912/`、`问题扫描/40_OWNER_DECISIONS.md` 及其 `run/` 影子，均属其他组，不在本组 7 身份内。

## 六、代际差异表

### 6.1 V5 单 CLI 包 vs `AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828`（G06 末包，同归档容器 `CONTROL_V4/`，实测 26 文件）

| 维度 | V4_CPU_ADAPTIVE | V5 | 核对指针 |
|---|---|---|---|
| 规格文件 | 12 个 `NN_*.md`（00、01、03–12）+ 台账，共 13 份 NN 件；无 PACKAGE_MANIFEST/START_PROMPT/SHA256SUMS/schemas | 15 个 `NN_*.md`（00–15，无 02）+ PACKAGE_MANIFEST + START_PROMPT + SHA256SUMS + 2 schemas | zip 实测 `has SHA256SUMS: False`、`has START_PROMPT: False`、`has schemas: False` |
| 文件总数 | 26 | 37 | find 实测 |
| templates | 10（无 `CHECKPOINTS.csv`、`RELEASE_ARTIFACTS.csv`、`SUMMARY.json`） | 13 | `ls templates/` |
| scripts | 3（同名 `validate_control.py` / `package_final.py` / `validate_final_package.py`） | 3 同名 | — |
| 任务数 / 号族 | 54 / 14 族（ALG5 BASE2 CPU4 DOC2 GOV1 LNX4 MON2 PAR7 QA3 REL2 REV4 SCI6 SYN6 WIN6） | 98 / 23 族 | 台账实测 |
| 台账表头 | 与 V5 逐列相同（含 `phase`、`preferred_host`） | 同 | 两文件首行对比 |
| 多依赖 | 9 行含 `\|` | 47 行含 `\|` | 实测 |
| 状态字面量 | 校验器 7 值，含 `DEFERRED`、`REVIEW_PENDING`（`validate_control.py:10`） | 6 值，`DEFERRED` 从集合移除并在 `00 §3` 明文禁止 | 两处源码 |
| 校验器新增项 | 无 required_phrases、无分支命令正则、无 SHA256SUMS 闭包校验；forbidden ledger 为 `RUN-003..006, ACR-001..003` 7 项（`:63`） | 11 个必需短语、分支创建命令正则、`SHA256SUMS` 集合+逐条校验、forbidden ledger 4 项（`ACR-001/RUN-003/RUN-004/CP0`）、任务数下限 85 | `validate_control.py:96,107,109,123,61` |
| 资源门禁阈值 | `05 §4`「平均使用核数应达到 `0.80 × min(selected_workers, available_cpus)`」，且对 io 类「低CPU允许」 | `07 §3` 同式（写作 `avg_equivalent_cores >= …`），另加 `§4` 前 10 秒快速失败返回 10 | 两处原文 |
| 发布终态与 alpha | 同样要求 `AWAITING_EXTERNAL_RELEASE_REVIEW`（`validate_final_package.py:81`）；无 alpha 版本正则、无 `windows_32r_run_id` 检查 | 增 alpha 版本正则（`:97`）、双平台 amd64 alpha 产物（`:95`）、`windows_32r_run_id`（`:108`） | 两处源码 |
| 失效清单 | `11_V3_INHERITANCE_AND_INVALIDATION.md` 逐号点名 9 项（CON-006/008/009、CON-010 豁免、CHK-006、SCI-001/004、WIN-002、RUN-001..006、历史接缝阈值/Oracle） | `12 §2` 8 项，按主题命名，不再逐号点名（仅保留 CON-010 类 waiver），新增「重跑旧 32R」「将大日志、pixel CSV、testdata 或 build 打进审核包」 | 两文件对读 |
| 新增文件 | — | `13_ALPHA_VERSION_AND_PHASE3.md`、`14_V4_COVERAGE_MATRIX.md`、`15_CONTINUOUS_CHECKPOINTS.md`、`04`/`06` 独立合同 | — |

### 6.2 REAUDIT_V4 vs REAUDIT_V3（`v3_reaudit`，实测 22 包件）

| 维度 | REAUDIT_V3 | REAUDIT_V4 | 指针 |
|---|---|---|---|
| 文件数 | 22（00–09 十规格 + 台账 + 3 脚本 + 9 模板） | 23（00–08 九规格 + `EVIDENCE_C0.md` + 台账 + 4 脚本 + 9 模板） | `find -type f ! -name _PROVENANCE.json` |
| 规格增删 | 有 `09_CONTINUOUS_EXECUTION_ORDER.md` | 删除该文件，新增 `EVIDENCE_C0.md`、`scripts/make_capsule.sh` | 两处文件列表 |
| 校验器 | `known = ids ∪ {CP0..CP8}`，checkpoint 正则 `CP[0-8]`，无行数硬约束 | 改为 `C[0-9]` / `{C0..C9}`，并新增「task ledger must have exactly 98 rows」（`:67-68`） | `diff` 两脚本仅 3 处差异（含文档字符串） |
| REQUIRED 集合 | 20 项 | 与 V3 逐字节一致（`diff` 无输出） | 两脚本 `REQUIRED` 段 |
| 任务数 | 62 | 98 | 台账行数 |
| 检查点分布 | CP4:14 CP3:10 CP5:9 CP7:8 CP2:7 CP6:5 CP0:3 CP1:3 CP8:3 | C5:22 C6:14 C4:12 C3:10 C1/C2/C8:8 C7:6 C0/C9:5 | `checkpoint` 列实测 |
| 00 编号行 | 17 条 | 22 条 | `grep -cE '^[0-9]+. '` |
| 模板 | 9 个同名 | 9 个同名（`COMMITS.csv` 出厂含 7 行 V3 期真实数据） | `wc -l` |

### 6.3 V5 vs 同期 REAUDIT_V4（同为 98 任务的并存两包）

台账第 2 列 `phase` ↔ `gate`、第 7 列 `preferred_host` ↔ `checkpoint`；状态字面量 6 ↔ 5（`REVIEW_PENDING` 之差）；多依赖 47 行 ↔ 0 行；`required_commit/push` yes90/no8 ↔ 全 yes；capsule 命名 `capsules/<task_id>_<commit12>.zip`（`10 §2`）↔ `capsule_<TASK>_<UTC>.zip`（`make_capsule.sh`）；提交消息 `(<task_id>) <单一目的>`（`03 §A.7`）↔ `<task_id>: <摘要>`（`06`）；资源阈值 0.80×min(workers, cpus)（`07 §3`）↔ min(affinity 核数×50%, 已配置并行度)（`01 §资源门禁`）；V5 有 `SHA256SUMS` 闭包门与 REQUIRED 35 项，REAUDIT_V4 无 `SHA256SUMS` 文件、REQUIRED 20 项；V5 校验器终态字面量与规格一致，REAUDIT_V4 校验器白名单为 `AWAITING_EXTERNAL_REVIEW`（`validate_audit_package.py:94`）与其规格要求的 `AWAITING_EXTERNAL_RELEASE_REVIEW`（`00 §执行入口 5`）不同。

## 七、执行留痕三方核对

### 7.1 V5 控制包 / AUDIT_REVIEW / audit_src（同一 98 号族）

- `设计大纲/_evidence/packs/pack_commit_link.csv`：V5、AUDIT_REVIEW、audit_src 三行均记 `98 号 / 98 出现 / 100.0 / 379 提交`。
- 抽查①（`设计大纲/_evidence/commits/index.jsonl` 1988 条的 `ids` 字段）：98/98 命中，涉及 377 个提交。
- 抽查②（自行 `git log --all` 2045 条 subject+body 字面包含，main 上 2043 条）：98/98 命中，main 上涉及 382 个提交。→ 命中率 100% 成立，提交数因口径不同为 377 / 379 / 382 三值。
- 三方分类（账 = 归档件台账 98 号；据 = `artifacts/prerelease_v5/<号>/` 60 个目录 ∪ `artifacts/prerelease_v5/capsules/` 91 个号前缀 ∪ `reports/evidence/` 可映射 31 号；提交 = 抽查①）：
  - **一致（有账、有提交、有留痕）92**：88 个 `PASS` 全部有 capsule（无一缺失），加 `PAR-002`(BLOCKED)、`REV-002`(REVIEW_PENDING)、`WIN-006`(IN_PROGRESS，仅有 reports/evidence 件，无 capsule 无任务目录)、`WIN-007`(NOT_STARTED，仅有 `WIN007_32r_manifest.json` 与 `WIN007_real_yinxin_32r_chain.md`)。
  - **有账有提交但无任何留痕 6**：`REL-001 REL-002 REL-003 REL-004 REV-003 WIN-008`（状态均 `NOT_STARTED`；`WIN-007` 例外，有 `reports/evidence/WIN007_real_yinxin_32r_chain.md` 与 `WIN007_32r_manifest.json`）。
  - **有账（PASS）无 artifacts 任务目录 28**（均有 capsule）；**有账（PASS）无 reports/evidence 对应件 61**。
  - **有据无账 1**：`artifacts/prerelease_v5/capsules/HOUSEKEEPING-V4-CORRECTION_1c580eb9f5a4.zip` 与 `…_38951423450d.zip`，`CAPSULE.json` 的 `task_id` 为 `HOUSEKEEPING-V4-CORRECTION`，不在 98 号台账内。
  - **盘上有胶囊但未登记进 `REVIEW_CAPSULE_INDEX.csv` 2**：`BASE-001`、`GOV-001`（索引 88 行、盘上 91 个号前缀、台账 98 号）。
  - `COMMITS.csv` 未覆盖的 8 个号：`WIN-006`(IN_PROGRESS)、`WIN-007`、`WIN-008`、`REV-003`、`REL-001..004`；索引中不存在的幽灵号 0 个；`artifacts/prerelease_v5/` 下 60 个任务目录全部属于台账。
- 审核件自证与机器门一致：`SUMMARY.json` verdict、`FINAL_REPORT.md` 的 88/98 自述、复算 `FINAL_PACKAGE_FAIL` 三者互相一致（均指向未达终态）。
- 影子留痕：`run/` 下 AUDIT_REVIEW 影子 70 份、V5 包影子 72 份；抽查 8 份影子台账 sha 均为 `907137567225`（LF 版），与入库 HEAD blob 同，与工作树 CRLF 版不同。

### 7.2 REAUDIT_V4（两个身份，同一 98 号族 `C0-001..C9-005`）

- `pack_commit_link.csv`：`REAUDIT_V4/v4_reaudit` 与 `工程控制/REAUDIT_V4` 两行均 `98 / 0 / 0.0 / 0 提交`。
- 复核①：`index.jsonl`（1988 条）`ids` 字段命中 0/98；且引入提交 `020cdc99`、修改提交 `ae643541` 的 `ids` 均为空数组；全库 `ids` 中不存在任何 `C<0-9>-<三位>` 形态 token → 0% 的一部分是抽取器口径未覆盖该号族。
- 复核②（最宽口径：`git log --all` 2045 条 subject+body 字面包含）：命中 3/98 —— `C0-001`@`020cdc99`、`C0-002`@`ae643541`（消息原文「feat(v4): C0-002..005 — 起点 SHA 冻结(020cdc9)+双平台工具链盘点…」）、`C3-002`@`56fe14c1`(2026-09-12，上下文标识为 `F-C3-002`，属另一命名空间的子串命中)。→ 有效命中 2/98，且全部落在该包自身的引入/修改提交上。
- 留痕侧：仓库内不存在 `evidence/REAUDIT_V4`、`artifacts/REAUDIT_V4`、`reports/REAUDIT_V4`（后者恰是 `make_capsule.sh` 声明的 roots 之一）；盘上唯一外部痕迹为未入库的 `run/reaudit_v4/capsules/capsule_C0_20260828T1237Z.zip`（33,407 B，23 条目，内容为本包自身文件快照，无 `CAPSULE.json`/`SHA256SUMS`；内嵌台账 sha `e2c901696525` 与 `ae643541` 提交态逐字节一致）。
- 分类：**有账无据 98/98**（其中 5 号 `C0-001..005` 为 `PASS`，证据仅包内 `EVIDENCE_C0.md` 自述 + 上述未入库胶囊；其余 93 号 `NOT_STARTED`，无留痕属预期）；**有据无账 0**；按 V5 口径（提交消息含号 + 包外留痕）**一致 0**。
- 台账演进：`020cdc99` 态 98 全 NOT_STARTED（sha `91da262a8f65`）→ `ae643541` 态 5 PASS（sha `e2c901696525`）；两提交相隔 2 分 1 秒，包在 18 分钟后被 `b12305ed` 整体删除。

## 八、缺口与不确定

1. 组定义与 digest 索引不同步：`dispatch_groups.json` G07 的 `digests` 数组 7 项中第 1、2 项均为 `…/AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828.md`，ALPHA 的 digest 文件在盘（44 行）却未被列出。
2. 同一 zip 的两个 sha 口径并存：`BASELINE.json` 的 `controller_package.zip_sha256 = 4b4414645d8d…aa5fca` 等于 `validate_control.py` 对解包内容的聚合摘要（`:139`），不等于 zip 文件本体 `42a03c0b09168f3e…eecb7d8`。
3. 现存两形态过不了自家门：归档件 `CONTROL_FAIL: hash mismatch: 02_TASK_LEDGER.csv`；复原件另加 `SHA256SUMS file set mismatch`（多出 `_PROVENANCE.json`）。出厂态（ZIP）与执行态无法同时满足 `SHA256SUMS` 闭包（`PACKAGE_MANIFEST.md` 要求覆盖除自身外全部文件）。
4. REAUDIT_V4 自述摘要不可在提交态复现：`EVIDENCE_C0.md` C0-001 记 `CONTROL_PASS files=21 sha256=bf5b51b3…d631`；实测 `git archive 020cdc99`（21 文件，台账 LF）复算为 `b63926b63147498e…`；仅把台账行尾还原为 CRLF 才复现 `bf5b51b38a8f374536875f7c9edbeff216a4945608bc7f71dab5d1fdf5b4d631`。同文件 C0-005 段自述「账本行尾归一 LF」。23 文件态（`ae643541`）复算为 `711d60d73cc164c2…`。
5. REAUDIT_V4 规格与校验器字面量不一致：`00 §执行入口 5`、`03 C9-005`、`04 C9`、`EVIDENCE_C0.md:44` 均要求终态 `AWAITING_EXTERNAL_RELEASE_REVIEW`，而 `scripts/validate_audit_package.py:94` 白名单为 `AWAITING_EXTERNAL_REVIEW`（少 `_RELEASE`）。
6. `工程控制/REAUDIT_V4` 外层身份的 provenance/digest 计数与 git 实测对不上：`delete_commits` 外层重复列 `b12305ed` 6 次、内层 2 次，实测该提交对该路径为 23 D + 2 R；digest 文件数 24 / 实测外层 25；`adds_n`（6 / 2）无法对齐到 `020cdc99`(21 A) 或 `ae643541`(2 A)。
7. digest 的 `first/last` 语义为 git log 新→旧（例：V5 复原件 first=`3ca7f974` 2026-08-30、last=`f99e80d8` 2026-08-28；REAUDIT_V4 first=`b12305ed` 删除提交、last=`020cdc99` 引入提交），与字面「首次/最后」相反；`pack_inventory.csv` 首次/最后提交列同口径。本报告时间线以实测 `git log` 为准。
8. `AUDIT_PACKAGE_587fe0e341a7.zip` 内 `MANIFEST.json` 的 `total_bytes=14,936,073` ≠ 1,697 条 `size` 之和 `14,936,770`（差 697 = `00_README.md` 字节数；生成脚本 `tools/pack_audit_package.py:96-100` 在写 MANIFEST 前未把该文件计入 total）。按 REAUDIT_V4 校验器（`:142`）口径会判 `MANIFEST total_bytes mismatch`；V5 的 `validate_final_package.py` 不检查 MANIFEST 内部。
9. 该 zip 的 `code/` 快照 1069/1071 与提交 `587fe0e3` 逐字节一致；2 处例外：`code/docs/architecture/execution_inventory.csv`（20,181 B，15 处 CRLF，行尾归一后与提交态一致，与工作树当前文件同）、`code/tests/cli/test_bench_cli.py`（5,240 B / 113 行；`587fe0e3`、`59649edc`、`b842899e` 三态均为 4,608 B / 99 行，工作树 12,937 B；该 blob 不在 `git rev-list --objects --all` 全史对象集中）。审核包内因此含无 git 出处的文件版本。
10. 该 zip 的 `evidence/` 中 `control/` 目录为 0 条目：生成脚本 `tools/pack_audit_package.py:16,52` 声明纳入 `工程控制/RELEASE_V5/<包>/` 并改名 `evidence/control/`，实测包内无该前缀、亦无任何含 `工程控制`/`RELEASE_V5` 的路径；原因未证实。
11. `AUDIT_REVIEW` 入库 blob 与工作树字节不同：`TASK_LEDGER.csv` HEAD 版 LF `90713756…`(10,079 B)、工作树 CRLF `914fbd83…`(10,178 B)，`.gitattributes:2 * text=auto eol=lf` 使 `git status` 不报改动；zip 内 `evidence/audit_review/` 34 文件与工作树逐字节一致（含 CRLF 台账）→ zip 快照取自工作树而非 HEAD。
12. `audit_src`（33 文件）与 `AUDIT_REVIEW`（34 文件）：唯一缺文件 `SHA256SUMS`；9 个 CSV 仅行尾差异（src LF / REVIEW CRLF，归一后逐字节相等），其余 24 文件字节一致；`audit_src` 无 git 事件（全史该路径 0 文件）、mtime 与两个 zip 同为 2026-09-04T18:51 → 两者先后与生成批次未证实。
13. 规格覆盖缺口：`03_TASK_DETAILS.md` §D 对 ISA 族写作区段「ISA-001..004」，`ISA-002/003/004` 无独立条目（REAUDIT_V4 `03` 对 98 号逐号展开）；`12 §3` 要求六项风险进 FINDINGS，审核件 `FINDINGS.csv` 为 0 行，风险改记于 `BASE-001/V3_V4_RISK_REGISTER.csv`（R0–R6，8 行）。
14. 资源/科学门禁留痕为空：`AUDIT_REVIEW/RESOURCE_RESULTS.csv`、`CPU_PROFILE_RESULTS.csv`、`SCIENCE_CLAIMS.csv`、`FINDINGS.csv` 均仅表头（0 行），`FINAL_REPORT.md` 自述「无 32R 资源门禁记录(RESOURCE_RESULTS 为空)」；`CHECKPOINTS.csv` 只 2 行（C0、C1），C2–C9 无记录行。
15. 表内容与磁盘不同源：`BUILD_RESULTS.csv`(2 行)、`TEST_RESULTS.csv`(5 行)、`RESOURCE_RESULTS`/`CPU_PROFILE_RESULTS`(空) 由 `tools/assemble_audit.py:95-124` 以脚本内字面量写入；`artifacts/prerelease_v5/tables/` 与 `AUDIT_REVIEW/` 共有 12 表中 7 表相同、5 表内容不同（`BUILD_RESULTS/COMMITS/CPU_PROFILE_RESULTS/RESOURCE_RESULTS/TEST_RESULTS`，其中 tables 版 BUILD、TEST 仅表头）。
16. 三个提交号并存于同一审核件：`MANIFEST.json`/`SUMMARY.json` 记 `59649edc0480…`、包目录名与 `code/` 快照记 `587fe0e341a7…`、`RELEASE_ARTIFACTS.csv` 两行的 commit 字段为 `ef0858c55a42`（Windows）与 `ec7a8cdc1566`（Linux）；未证实三者间的唯一对应关系。
17. digest 与本体不一致项：`digest/AUDIT_PACKAGE_587fe0e341a7.md` 的「包内 SHA256SUMS 核对」小节实际贴的是 zip 内 `MANIFEST.json` 的 6 段 JSON 片段并被截断（无核对结论），且无「00_READ_FIRST 标题」小节（实测该 zip 的 `SHA256SUMS` 1,698 条全部通过、zip 内唯一 `00_*` 文件是 `00_README.md` 而非 `00_READ_FIRST.md`）。
18. 影子形态无提交时间：`run/` 下 72 + 70 份副本受 `.gitignore:19` 屏蔽，无 git 事件，只有文件系统时间；无法据 zip/影子判定其生成时刻。
19. 未归组身份：`dispatch_groups.json` G99 含 1 个未归组身份 `AstroCS_AUDIT_REVIEWPACK_20260909T133841Z`（时间晚于本组审核件，本组包内无对该身份的引用），其与本组的归属关系在本组证据内未证实。
20. REAUDIT_V4 的生命周期只有 18 分钟（20:36:03 引入 → 20:38:04 修改 → 20:54:30 删除），删除与「用户上传 V4 包归位」在同一提交（`b12305ed`：24 A / 21 D / 1 R099 / 1 R061），包内 5 个 PASS 的 C0 证据未随包保留到仓库跟踪区；动机与责任链未证实。

## 与上一组的衔接

G06 的最后一个包是 `工程控制/CONTROL_V4/AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828`（当前位于 `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/CONTROL_V4/`，实测 26 文件、54 任务、12 份 `NN_*.md` 规格（00、01、03–12）+ 台账、7 个状态字面量含 `DEFERRED`、无 SHA256SUMS/START_PROMPT/schemas）。G07 的第一个包 `AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828` 与之并列在同一归档容器（`CONTROL_V4/` 与 `RELEASE_V5/` 同级），并由同一提交 `b7b2dea7`（2026-09-02T22:15:41，GOV-002 归档）一起移档，两者在归档路径下的首次且唯一提交相同。V5 对 V3/V4 的继承与废止声明集中在 `12_V3_V4_MIGRATION.md`（§1「只继承实现，不继承 PASS」；§2 八项明确失效；§3 R1–R6 风险须入 FINDINGS）与 `14_V4_COVERAGE_MATRIX.md`（21 行「V4 主题 → V5 承接位置」矩阵，首句声明 V5 必须包含 V4 全部有效要求）；与 V4 包自身的 `11_V3_INHERITANCE_AND_INVALIDATION.md`（9 项逐号点名失效）对读可见：CON-010 类豁免禁令被保留并改写为主题条目，V4 期逐号点名的 CON/CHK/SCI/WIN 号在 V5 中不再出现，V5 新增「重跑旧 32R」「将大日志、pixel CSV、testdata 或 build 打进审核包」两条废止。可核对的代际差为任务 54→98、模板 10→13、状态字面量 7→6（取消 `DEFERRED`）、schemas 0→2、SHA256SUMS/START_PROMPT/PACKAGE_MANIFEST 从无到有、资源门禁 0.80×min(workers, cpus) 两代同式。另需说明：G07 组内的 `REAUDIT_V4` 两身份并非 V4 用户包的后代——它由 `020cdc99`/`ae643541` 引入、由 `b12305ed` 删除，而 `b12305ed` 正是把用户上传的 V4_CPU_ADAPTIVE 包解包件归位到 `工程控制/CONTROL_V4/` 的同一提交（该提交内 V4 重审包的 `templates/LARGE_ARTIFACT_MANIFEST.csv` 与 `templates/TRACEABILITY.csv` 被识别为 R099/R061 改名为 CONTROL_V4 的 `templates/LARGE_ARTIFACTS.csv` 与 `templates/TRACEABILITY.csv`）。

## 重叠身份

按 `dispatch_groups.json` 全组遍历，本组 7 个身份只出现在 G07，无跨组重名，因此不存在跨组 digest 一致性可核项。组内存在四处内容重叠：(1) `..._AMD64_20260828` 与 `..._AMD64_ALPHA_20260828` 两身份指向同一 37 文件集合，唯一差异是 `02_TASK_LEDGER.csv`（zip `22e3d8919cf9` / 复原件 `12f7beb6c234` / 归档件 `907137567225`），digest 分别记 97.7 KB 与 98.1/97.1 KB，两个身份的摘要互不可核（zip 本体 `42a03c0b…` 只在 ALPHA 名下，非 ALPHA 名下无 zip 形态）；(2) `工程控制/REAUDIT_V4` 与 `REAUDIT_V4__v4_reaudit` 是同一删除提交的容器与内包，23 个包件逐文件一致，两份 digest 字节差 175 B 恰为两个 `_PROVENANCE.json`（600 − 425）之差，`delete_commits` 指向同一 SHA `b12305ed`；(3) `prerelease_v5/AUDIT_REVIEW` 与 `prerelease_v5/audit_src` 重叠 33 文件，`MANIFEST.json` 同为 `package=AstroCS-audit-59649edc0480`、`commit=59649edc…`（digest 一致），9 个 CSV 只差行尾；(4) `AUDIT_PACKAGE_587fe0e341a7.zip` 与本组另两身份重叠——其 `evidence/audit_review/` 34 文件与 `AUDIT_REVIEW` 工作树逐字节一致、`evidence/tables/` 12 文件与 `artifacts/prerelease_v5/tables/` 逐字节一致、内嵌台账与 V5 归档态台账归一后同为 `90713756…`，而其本体（`code/` 1,071 + `evidence/reports/` 565）在本组无对应身份；`evidence/reports/` 中 348 个 REAUDIT_V3 期文件与 G05/G06 的 REAUDIT_V3 系留痕构成跨组内容重叠。跨组引用方向为出：G08 的 `AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830` 在 `02_BASELINE_AUDIT.md` 第 5、7 行以本组 zip 为输入包并引用 `587fe0e341a780da726917f40ed77f610de0c73f`，但未记录 zip 的 sha256，故与本组 digest 的 `81b7e39e…c14b` 无法核对（未证实）。
