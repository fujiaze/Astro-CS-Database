# P-G09 · V6.1 返工控制包 取证报告

- 组：G09（V6.1 返工控制包）；身份：`AstroCS_V6_1_REWORK_CONTROL_20260831`
- 身份内标识：`control_id = AstroCS-V6.1-REWORK`、`target_version = 0.10.0-alpha.2`、`created_utc = 2026-08-31T03:21:20Z`（`工程控制/AstroCS_V6_1_REWORK_CONTROL_20260831/MANIFEST.json` 顶层键，下称 包MANIFEST）
- 方法：digest → 包内文件本体（zip 用 python zipfile 只读；归档件/影子树用文件系统只读；提交用只读 git）；所有 sha256/字节/计数为本组实测，实测命令与结果在正文标注。
- 口径：引用不超过单行；`文件+条款/行号` 为证据指针；提交一律给 8 位 SHA。

---

## 一、组内包清单与身份归并

| 存形态 | 路径 | 文件数 | 字节（实测） | zip sha256 | git 首次 | git 末次 | 删除/移档 |
|---|---|---|---|---|---|---|---|
| zip | `工程控制/_control_packs/AstroCS_V6_1_REWORK_CONTROL_20260831.zip` | 47 | 214,426（包内条目求和） | `903018212bfb584b1aaf0dd05b318d8171d1a5b3af43014ab020834e9359ede6`（实测磁盘重算，与 digest 与包MANIFEST 记载一致） | 无 git 事件（该 zip 全史未入库：`git log --all -- 该路径` 空；`pack_events.md` §一.5 同口径） | 同左 | 无 |
| 解包件（worktree） | `工程控制/AstroCS_V6_1_REWORK_CONTROL_20260831/` | 46 | 209,075（`du -sb`；`du -sh` = 332K） | — | `8c71f7ab` 2026-09-02T02:12:13+08:00（adds_n=1） | `8c71f7ab`（mod_n=0） | `b7b2dea7` 2026-09-02T22:15:41+08:00 记 R100（唯一入库文件 `02_FINDINGS.csv` 移入归档容器） |
| 归档 | `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/AstroCS_V6_1_REWORK_CONTROL_20260831/` | 1（仅 `02_FINDINGS.csv`） | 与 `8c71f7ab` 同 blob `f3fe50e5` | — | `b7b2dea7`（随 GOV-002 移档入库） | `b7b2dea7`（HEAD 仍在，`git ls-files` 命中 1） | 未删除 |
| 影子树 | `run/*/**/engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/AstroCS_V6_1_REWORK_CONTROL_20260831/` | 72 份目录副本 × 1 文件 | 72 份 `02_FINDINGS.csv`，sha256 全为 `0f0c6573cffeb818…`（与归档件同一 hash） | — | 不入库（`.gitignore:19` `run/*`） | — | — |
| 仅历史复原 | 无 | 0 | — | — | — | — | — |
| 交付审核包（非本包形态，但同身份绑定的产物） | `dist/audit/AstroCS_V6_1_AUDIT_20260902T042239Z_faad602da555.zip` | 1309 条目 | 4,243,274 | `98216e5fef43e104163aee1a97fd4f249c03c9ab1264a88b1a8fe586c3bdefaa`（git blob 实测） | `24c7eec7`（A，2026-09-02） | `24c7eec7` | `789c5b6c`（D，2026-09-09，题述"旧 audit zip 出库"） |

- digest 与 计划文档 记为"2 个实例"（`设计大纲/_evidence/packs/digest/AstroCS_V6_1_REWORK_CONTROL_20260831.md`:3；`pack_lineage_table.md:39` 形态列 `work,zip`），未计归档件与影子树；上表两列为本组实测补记。
- **同名两形态的文件集合差异（实测）**：zip 47 条 vs 解包件 46 条；
  - 仅 zip 有：`02_FINDINGS.csv`（7,357 B）。
  - 仅解包件有：无。
  - 共有 46 条中 45 条逐字节相同；唯一不同者为 `scripts/validate_return_audit.py`：zip 16,177 B / sha256 `82384d0eded3…` ↔ 解包件 18,183 B / sha256 `12b34b7fa07d…`（差异 4 个 hunk、277→302 行，逐处见 §八 第 6 条）。
  - 差异不推测原因；可核对的时间事实是：该 4 处改动的内容指向 zip 内文件与规范条文（见 §四）。
- **自带 SHA256SUMS 实测（digest 记 ok 43 / mismatch 1 / missing 1）**：
  - 解包件：SHA256SUMS 共 45 条；实测 ok=43、mismatch=1（`scripts/validate_return_audit.py`）、missing=1（`02_FINDINGS.csv`）→ **与 digest 一致**。
  - zip 内同一份 SHA256SUMS 对 zip 内文件核对：45 条全部命中，mismatch 0、missing 0 → mismatch/missing 仅存在于解包件形态。
  - 未被 SHA256SUMS 列出的仅 `MANIFEST.json`、`SHA256SUMS` 自身（与 `validate_control.py:100` 的 `content_names` 定义一致）。
- 包MANIFEST（45 项）与 SHA256SUMS（45 项）文件集与 hash 完全一致；包MANIFEST 与 zip 内容零不一致；与解包件不一致 1 项（同一脚本）。
- 是否被删除：本包目录整体从未入库（`git ls-files 工程控制/AstroCS_V6_1_REWORK_CONTROL_20260831` = 0 条；`git log --all -- 该路径` 仅 2 提交 `8c71f7ab`、`b7b2dea7`）；`工程控制/` 与 zip 路径均未被 `.gitignore` 覆盖（实测 `git check-ignore` 返回未忽略），属"未跟踪但可跟踪"状态。

---

## 二、包内文件地图

规格文件（NN_XXX.md，编号取自文件名；职责引包内自述标题或首段要点）：

| 文件 | 自述标题 / 首段要点（转述） |
|---|---|
| `00_READ_FIRST.md` | "AstroCS V6.1 Alpha 重构打回控制包"；§1 结论与目标、§2 最高优先级、§3 启动动作、§4 不得自由发挥、§5 每任务闭环、§6 当前必须消除的事实、§7 完成出口 |
| `01_ASTROCS_ENGINEERING_CONSTRAINTS.md` | "AstroCS 预发布工程约束"，16 节；§1 文档效力、§5 并行和资源利用硬约束（第 3 条：无法并行须量化证据+负责人批准，不得 Agent 自行豁免）、§11 机器一致性检查 13 项、§13 控制包与审核包、§16 最终判定原则 |
| `01_INDEPENDENT_AUDIT_CONCLUSION.md` | 对 `astrocs_audit_20260830T183323Z.tar.gz` 的独立审核结论；§10 决定 7 条（当前 commit 只作待修起点、不回退历史、按台账从身份与已知失败复现起） |
| `04_TASK_SPECIFICATIONS.md` | "V6.1 逐任务执行规格"；§A 通用任务模板（10 步），随后 G0–G10 逐任务小节，67 个 `### <TASK-ID>` 小节与台账 67 号一一对应（实测：集合差 0/0） |
| `05_GATE_CHECKLISTS.md` | "V6.1 Gate 自动检查清单"；自述为台账的人工可读投影、非第二套状态源；11 个 Gate 共 96 个复选项 |
| `06_ARCHITECTURE_ACCEPTANCE.md` | "目标架构与验收合同"；首段限定为可验收的职责迁移、迁移顺序合同→适配器→生产接线→删除旧路径；7 节 |
| `07_SCIENCE_AND_TEST_ACCEPTANCE.md` | "科学合同与数值测试验收"；8 节（科学真相链、Oracle 独立性、Phase1/2/3 专项、平台等价、抽查机制） |
| `08_CPU_RESOURCE_ACCEPTANCE.md` | "纯 CPU Provider、Benchmark 与资源利用合同"；8 节；§6 三份必需采样文件、§7 门禁阈值、§8 必需负面测试 |
| `09_DOCUMENTATION_AND_MACHINE_CHECKS.md` | "文档体系与机器一致性检查"；§1 四层文档、§3 六类检查器（3.1–3.6）、§8 检查器质量门 |
| `10_LINUX_WINDOWS_RELEASE_FLOW.md` | "Linux→GitHub main→Fatduck→发布连续流程"；7 节；§5 最终 32R、§6 冻结键、§7 允许停止的唯一情形 |
| `11_GIT_MAIN_ONLY.md` | "Git main-only 原子提交规范"；6 节；§1 硬规则、§3 任务结果与 commit 绑定 6 条验证、§6 最终审核 |
| `12_AUDIT_PACKAGE_SPEC.md` | "V6.1 返工审核包白名单规范"；7 节；§2 根目录必含 33 行白名单、§3 严禁、§4 SUMMARY 状态、§7 打包器 9 步 |
| `13_OWNER_REVIEW.md` | "项目负责人最终审查说明"；8 个入口文件、六张必须视图、负责人只需回答 6 项 |
| `14_PRIMARY_REFERENCES.md` | "一手科学与工程参考"；11 条文献（Drizzle/HEALPix/HiPS/FITS-WCS/FITS4.0/Montage/RCR/CFITSIO 多线程/OpenMP5.2/AMD64+Intel SDM/编译器文档）+ 引用验收 4 条 |
| `AGENTS_PATCH.md` | "AstroCS AGENTS.md（建议替换正文）"，13 行（当前仓库根 `AGENTS.md` 是否被其替换：见 §八 第 9 条） |
| `02_FINDINGS.csv`（仅 zip 形态在包内） | 表头 10 列（zip 版）；40 条 F-001..F-040 |
| `START_PROMPT.txt` / `MANIFEST.json` / `SHA256SUMS` | 单行启动指令（487 字符）/ 清单（45 项）/ 校验和（45 条） |

子目录数量与命名口径（实测，解包件＝zip 同一集合）：

- `scripts/` 6：`validate_control.py`、`validate_task_graph.py`、`validate_return_audit.py`、`known_failure_scan.py`、`package_audit.py`、`selftest.py`。命名口径为动词式 `validate_*/` + 名词式扫描/打包/自检脚本各一。
- `schemas/` 7：`audit_summary`、`cpu_profile`、`data_artifact`、`module_descriptor`、`pipeline_ir`、`resource_summary`、`task_result`，均 `.schema.json` 后缀；`$schema` 全部为 draft/2020-12，`$id` 前缀 `https://astrocs.local/schemas/…-v2.json`（实测 7/7）。
- `templates/` 14：CSV 7（`COMMITS`、`FINDINGS`、`SOURCE_INDEX`、`TEST_SUMMARY`、`RESOURCE_SUMMARY`、`LARGE_ARTIFACT_MANIFEST`、`PREVIEW_MANIFEST`）、MD 4（`SCI_CONTRACT`、`ALG_CONTRACT`、`MODULE_README`、`OWNER_REVIEW_CHECKLIST`）、JSON 3（`SUMMARY.template.json`、`TASK_RESULT.template.json`、`SOURCE_IDENTITY.template.json`）。
- **`tasks/` 目录：0（本包不含 tasks/ 单任务文件）**；`fixtures/` 目录：0（V6 有 3 个，见 §六）。

---

## 三、任务结构

- 台账文件：`03_REWORK_TASK_LEDGER.csv`；67 行任务（`validate_task_graph.py` 实测 `TASK_GRAPH_PASS tasks=67`，`ledger_sha256 = c6f21304d1835ca0…`，与包MANIFEST 的 `task_ledger_sha256` 同值）。
- 表头 11 列（与 `validate_task_graph.py:15-18` 的 `REQUIRED_COLUMNS` 集合相等）：`task_id, gate, title, depends_on, platform, change_class, heavy_compute, commit_required, scope, acceptance, status`。
- 任务号族 15 族 / 67 号：`R0`(4)、`GOV`(2)、`VER`(1)、`CHK`(3)、`RT`(9)、`CPU`(4)、`MON`(3)、`P1`(4)、`P2`(7)、`P3`(6)、`DOC`(6)、`QA`(3)、`LNX`(5)、`WIN`(6)、`REL`(4)；编号正则 `^[A-Z0-9]+-[0-9]{3}$`（`validate_task_graph.py:20`）。
- Gate 分布 11 个（G0–G10，实测按 gate 计数）：G0 4 / G1 6 / G2 9 / G3 7 / G4 4 / G5 7 / G6 6 / G7 9 / G8 5 / G9 6 / G10 4；合计 67。
- 列值分布：`platform` Linux 51 / Linux+Windows 9 / Windows 6 / Owner 1；`heavy_compute` yes 37 / no 30；`commit_required` yes 66 / no 1（唯一 `no` 为 REL-004）；`change_class` architecture 15、validation 15、quality 7、science 7、documentation 6、performance 6、release 5、governance 4、audit 2。
- 依赖与顺序声明：`depends_on` 以 `;` 分隔，共 111 条边；仅 `R0-001` 无前驱；最大入度 4（`DOC-002`、`REL-001`）；跨 Gate 依赖 43 条。拓扑序由脚本给出，末位固定 `REL-004`（`validate_control.py:71` 断言 `task_count == 67` 且拓扑末位为 REL-004）。
- 状态字面量（三处口径并存，实测）：
  1. 包内 `scripts/validate_task_graph.py:19`：`NOT_STARTED / IN_PROGRESS / PASS / FAIL / WAITING_WINDOWS`（5）；
  2. 包内 `04_TASK_SPECIFICATIONS.md:47`（R0-003 小节）要求仓库侧 `tools/quality/validate_task_ledger.py` 允许 `NOT_STARTED/IN_PROGRESS/PASS/FAIL/BLOCKED/WAITING_WINDOWS/WAITING_DATA`（7）；该文件现存于仓库（`tools/quality/validate_task_ledger.py:42-43` 同 7 值，注释第 5 条：`BLOCKED` 须填 `BLOCKED_REASON`）；
  3. schema 层：`schemas/task_result.schema.json` 的 `status` 枚举 = `PASS/FAIL/WAITING_WINDOWS`（3）；`schemas/audit_summary.schema.json` 的 `gate_status` 值枚举 = 5 值（同 1）；`verdict` 枚举 = `NOT_READY / READY_FOR_OWNER_REVIEW`。
- 台账密封态：67/67 = `NOT_STARTED`（`validate_control.py:73-74` 断言密封台账必须整表 NOT_STARTED）。

---

## 四、门禁与验收要求

机器门清单（可执行脚本 → 判据 → 出处）：

1. **包自校验门 `CONTROL_PASS`**：`scripts/validate_control.py`；REQUIRED 47 项齐（`:17-32`）、`01_` 约束 hash 等于硬编码 `dc47fbb…`（`:16,54`）、START_PROMPT 单行 ≤1000 字符且含 `main/ACR/32R/READY_FOR_OWNER_REVIEW`（`:57-61`）、schemas 声明 draft/2020-12（`:63-66`）、台账 67 号且末位 REL-004 且全 NOT_STARTED（`:70-74`）、F-001..040 恰一次且全 OPEN（`:76-80`）、禁 symlink/禁止路径段/后缀/单文件 >5 MiB/总 >25 MiB（`:82-98`）、MANIFEST 与 SHA256SUMS 文件集与 hash 双校（`:100-125`）。实测（`/tmp` 内解包，未写入仓库）：zip 形态输出 `CONTROL_PASS files=47 bytes=214426 tasks=67 findings=40 prompt_chars=487`；解包件形态输出 `CONTROL_FAIL: missing required files: ['02_FINDINGS.csv']`；解包件补回该 CSV 后输出 `CONTROL_FAIL: manifest mismatch: scripts/validate_return_audit.py`。
2. **任务图门 `TASK_GRAPH_PASS`**：`scripts/validate_task_graph.py`；规则＝ID/gate 格式与唯一、状态枚举、`heavy_compute/commit_required` 只 yes/no、`WAITING_WINDOWS` 仅限 Windows 任务（`:64-65`）、依赖存在且**不得依赖后置 Gate**（`:80-82`）、无环（`:96`）、至多 1 个 IN_PROGRESS（`:99-100`）、PASS/IN_PROGRESS 的依赖必须全 PASS（`:101-106`）、REL-004 不得被 Agent 置 PASS（`:107-108`）；Gate 状态由成员状态聚合（全 PASS→PASS，含 FAIL→FAIL，含 IN_PROGRESS→IN_PROGRESS，含 WAITING_WINDOWS→WAITING_WINDOWS，否则 NOT_STARTED，`:110-124`）。实测两形态均 PASS。
3. **已知失败扫描**：`scripts/known_failure_scan.py`，19 个 `Probe()`、覆盖 19 个 F- 号（40 项中的一部分），输出 `KNOWN_FAILURE_SCAN_PASS probes=19` 或 `KNOWN_FAILURES_PRESENT count=N`（`:108-121`）；`00_READ_FIRST.md:34` 要求起点预期失败并存 `KNOWN_FAILURES_BASELINE.json`。
4. **自检门 `SELFTEST_PASS`**：`scripts/selftest.py`；负例含非法状态（用 `REVIEW_PENDING` 构造）、依赖未过即 PASS、环等，实测输出 `SELFTEST_PASS ledger_negative=4 known_failure=2 audit_negative=1 schemas=PASS`。
5. **审核包终局门 `AUDIT_PACKAGE_PASS`**：`scripts/validate_return_audit.py`；REQUIRED 19 类根文件（含 `SUMMARY.json`、`TASK_LEDGER.csv`、`COMMITS.csv`、`FINDINGS.csv`、`TRACEABILITY_MATRIX.csv`、`source/AGENTS.md`、`source/AstroCS_ENGINEERING_CONSTRAINTS.md`、`scripts/validate_audit.py`），`BANNED_PARTS` 含 `.git/build/testdata/third_party/__pycache__/.pytest_cache/archive/history/cache`、`BANNED_SUFFIXES` 含 `.zip/.tar/.gz/.dll/.so` 等（实测摘录）；CLOSED finding 须有 SHA40 `resolution_commit` + SHA64 证据（`:170-175`）；COMMITS 列 `task_id,commit,parent,subject,pushed,origin_main_ancestor,task_result_sha256`（`:180`）且集合须等于"commit_required=yes 且 status=PASS"的任务集（`:181-182`）；`READY_FOR_OWNER_REVIEW` 时终局测试必须全 PASS 且同 source commit（`:194`）；PREVIEW 必须逐条 hash 校验（`:224-230`）。
6. **仓库侧台账门**：`tools/quality/validate_task_ledger.py`（`04_TASK_SPECIFICATIONS.md:47` 指名实现，8 条规则，含 BLOCKED 需 `BLOCKED_REASON`、BLOCKED 非终态）。

完成顺序要求：

- `00_READ_FIRST.md:35`：从 `R0-001` 连续执行，不设 CP0/普通外部审核/人工等待点；`:29-34` 启动 7 步顺序。
- `START_PROMPT.txt`：只在 main 从 R0-001 连续执行到 REL-003。
- `05_GATE_CHECKLISTS.md:3`：Gate 通过后立即进入下一可执行任务，不等待人工回复；`10_LINUX_WINDOWS_RELEASE_FLOW.md:53-58` 列出允许停止的 6 种情形，末句"普通 Gate、checker PASS、文档审阅、CP0 均不是人工停止点"。
- `11_GIT_MAIN_ONLY.md:7,9`：`commit_required=yes` 任务恰对应一个（或一组显式登记的）原子 commit，消息 `<TASK-ID>: <中文、单一目的>`，通过后立即 push 并 fetch 核验。
- `00_READ_FIRST.md:53-62`（每任务闭环）：`NOT_STARTED → IN_PROGRESS → 修改/验证 → TASK_RESULT → 原子 commit → push → PASS`；PASS 的 6 项同时满足条件（含"没有未登记 P0/P1"）。
- `04_TASK_SPECIFICATIONS.md:48`：依赖不全 PASS 时当前任务不得 PASS，唯一例外为 Owner 任务保持 NOT_STARTED。

状态机口径：任务级 5 值（包内脚本）／7 值（包内规格＋仓库工具）两套并存；`WAITING_WINDOWS` 仅限 Windows 平台任务；`REVIEW_PENDING` 在两代包脚本中均非合法状态（`01_INDEPENDENT_AUDIT_CONCLUSION.md:22` 把它列为 V6 打回项之一）；`schemas/audit_summary.schema.json` `verdict` 只允许 `NOT_READY/READY_FOR_OWNER_REVIEW`；`12_AUDIT_PACKAGE_SPEC.md:70-75` 给出 READY 的机器条件（除 REL-004=NOT_STARTED 外全 PASS、P0/P1 全 CLOSED、WIN-001..006 与 32R PASS、source==main==origin/main、blockers/not_verified 为空、owner_review=PENDING、不得输出 `ALPHA_RELEASE_APPROVED`）。

waiver／豁免机制：**本包内无 waiver／`ci/checks.json` 类豁免登记机制**（全包 grep `waiver`、`豁免`、`exempt` 命中仅 `01_ASTROCS_ENGINEERING_CONSTRAINTS.md:79`（无法并行须负责人批准、不得 Agent 自行豁免）、`08_CPU_RESOURCE_ACCEPTANCE.md:74`（memory-bound 分类不豁免 worker 与内存增长检查）、`04_TASK_SPECIFICATIONS.md:70`（版本扫描"测试 fixture 可登记豁免"）与解包件被改脚本内的注释"豁免"字样）。允许的分类豁免仅两处口径：`08_CPU_RESOURCE_ACCEPTANCE.md:74` 与 `04_TASK_SPECIFICATIONS.md:88-93`（CHK-003 allowlist 仅限 <5 s 或总耗时 <5% 的 I/O/metadata，须含 owner/reason/expiry/test）。

数值／资源门禁阈值：`08_CPU_RESOURCE_ACCEPTANCE.md:62-74`（active window ≥10 s 且 available≥2：active worker p50≥2、normalized CPU p50≥90%、mean≥85%、无单 worker 独占、无未解释锁/I/O 等待、无不可解释正斜率、无 oversubscription/swap/OOM；memory-bound 须带宽 ≥ 本机 benchmark 80%）；配套清单条目 `05_GATE_CHECKLISTS.md:49-51`（含 ACR 状态 `DORMANT_NOT_IN_PRODUCTION`）。

审核包与提交要求：文件名与时限 `12_AUDIT_PACKAGE_SPEC.md:5`（`AstroCS_V6_1_AUDIT_<UTC>_<main12>.zip`，只能由 `scripts/package_audit.py` 从白名单生成）；根目录必含 `12 §2` 33 行；严禁 `12 §3` 9 条（单文件 >5 MiB 或总 ZIP >25 MiB 必须失败，Agent 无权放宽，`:62`）；四张表字段 `12 §5`；预览须 `source_kind=WINDOWS_FINAL_32R`（`:86`）；打包器 9 步 `12 §7`，末句要求验证器同时随控制包提供。提交面要求另见 `11 §2/§3/§5/§6`（起点三 SHA 一致、`COMMITS.csv` 由 Git 现场生成、Windows 缺陷须回 Linux/main 修再 push、`SOURCE_IDENTITY.json` 三值相等、审核包不含 `.git`）。**注意口径分裂**：`12 §5` 与包内两份模板／终局验证器用 `commit/parent/subject/pushed/origin_main_ancestor`，而 `04_TASK_SPECIFICATIONS.md:60`（GOV-001）用 `parent_commit/result_commit/commit_subject/pushed_origin_main` 8 列，仓库执行留痕采用后者（`evidence/v6_1_rework/COMMITS.csv` 表头），交付审核包内 `COMMITS.csv` 采用前者（实测表头 7 列、57 行）。

---

## 五、设计意图（转述，不评价）

- **目标**：`00_READ_FIRST.md:12` 自述"上轮结果正式打回。不得继承'81 PASS、Linux 侧完成、G11 PASS'等状态"，并要求在当前 main 完成生产架构、纯 CPU 并行、科学 Oracle、文档—代码闭环、双平台发布验证；`:39` 限定只有全部非 Owner 任务完成 + Windows 最终验证 + 审核包自验证通过后才能输出 `READY_FOR_OWNER_REVIEW`，且"Agent 永远无权输出 ALPHA_RELEASE_APPROVED"。
- **优先级序位**：`00_READ_FIRST.md:20-25` 6 级冲突服从序（仓库根约束 > 本文件与台账 > 专项合同 > 已核实 SCI/ALG/DATA > 当前源码仅作待审对象 > 历史报告/自报 PASS/旧日志仅作线索）。
- **禁止项**（`00_READ_FIRST.md:39-47`）：不改任务 ID/依赖/验收/严重度、只追加；禁分支与 merge/rebase/cherry-pick/reset/force-push；不运行历史版本全量对比；ACR 保留但须 `DORMANT_NOT_IN_PRODUCTION`；可用 CPU≥2 时持续 ≥10 s 的 heavy 节点不得单线程；关键词 grep、注释、`CHECK(true)`、伪造资源指标、空 trace、旧 V5 表、人工填 PASS 均不得作验收；Windows 离线标 `WAITING_WINDOWS` 继续其他工作。另见 `11 §1`、`12 §3`、`06:3`（不授权大爆炸重写或改科学公式）、`07 §3`（Oracle 独立性）。
- **对前代的继承／废止声明**：`01_INDEPENDENT_AUDIT_CONCLUSION.md:125-136`（"本轮可保留的成果"6 项 + "不是已通过的发布能力"）、`:138-147`（决定 7 条：当前 commit 不作已验收基线、不回退历史、不做旧版全量差分）；`MANIFEST.json` 以 `prior_control_sha256 = fdad8e40…` 绑定上一代 zip（实测 `工程控制/_control_packs/AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830.zip` sha256 前缀即 `fdad8e40123a757c`），并以 `reviewed_audit_sha256 = 137e3ed1…` + `reviewed_audit_reported_commit = b16d422a`（`00_READ_FIRST.md:6-7` 注明"尚未被接受为基线"）绑定被审对象；`01_ASTROCS_ENGINEERING_CONSTRAINTS.md` 与上一代同名文件逐字节同 hash（`dc47fbb…`，实测相等）。
- **负责人／用户指令痕迹**（本包无 `OWNER_BINDINGS`、`04_OWNER_DECISIONS`、`07_FRONT_DESK_RULINGS` 一类文件；Owner 相关内容集中于）：`13_OWNER_REVIEW.md`（负责人只需回答 6 项、`APPROVE ALPHA` 或 `REJECT + 指定问题`、"Agent 无权代填批准"）；`templates/OWNER_REVIEW_CHECKLIST.md`（标题 `# Owner Review`，末段留"负责人："签名位）；`templates/SCI_CONTRACT.md` 末节"待负责人核实的问题"；`01_ASTROCS_ENGINEERING_CONSTRAINTS.md:79`（无法并行的量化证据须负责人批准）；`05_GATE_CHECKLISTS.md:132`（仅负责人可批准发布）；`04_TASK_SPECIFICATIONS.md:382`（REL-004"Agent不得修改为PASS、不得代签"）；`10_LINUX_WINDOWS_RELEASE_FLOW.md:53-58`（唯一人工停点清单）。包内未见负责人署名、批复文本或裁决编号。
- 包内亦无 CP0／外部审核停点（`00_READ_FIRST.md:35` 明示"不设 CP0"）。

---

## 六、代际差异表（对照 G08 的 `CONTROL_V6`／`AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830`，归档副本 `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/CONTROL_V6/…`）

| 项 | V6（20260830） | V6.1（20260831） | 可核对差异 |
|---|---|---|---|
| MANIFEST schema | `astrocs.control-manifest/v1`，键 `control_version=V6.0`、`baseline_commit=587fe0e3…` | `astrocs.control-manifest/v2`，键 `control_id`、`reviewed_audit_sha256`、`reviewed_audit_reported_commit`、`prior_control_sha256`、`task_ledger_sha256` | 新增 4 个绑定字段（审核包 hash／自报 commit／上代包 hash／台账 hash）；`baseline_commit` 取消 |
| 目标版本 | `0.10.0-alpha.1` | `0.10.0-alpha.2` | 递增（`04 §VER-001`） |
| 文件数 | 46（zip 与归档件同集，实测） | zip 47 / 解包件 46 | V6.1 多 `02_FINDINGS.csv`（zip 形态） |
| 规格文件删除 | — | — | V6 有 `02_BASELINE_AUDIT.md`、`03_TARGET_ARCHITECTURE.md`、`04_MIGRATION_AND_GATES.md`、`06_TASK_SPECIFICATIONS.md`、`07_SCIENCE_AND_TEST_MATRIX.md`、`08_CPU_PARALLEL_BACKEND.md`、`10_LINUX_WINDOWS_EXECUTION.md`、`13_RELEASE_ACCEPTANCE.md`、`15_GATE_CHECKLISTS.md`、`16_ACCEPTANCE_MATRIX.csv`、`17_RISK_REGISTER.csv`、`18_CURRENT_TO_TARGET_MAPPING.csv`、`AGENTS_REPLACEMENT.md`（共 13 项在本代不存在） |
| 规格文件新增 | — | — | `01_INDEPENDENT_AUDIT_CONCLUSION.md`、`03_REWORK_TASK_LEDGER.csv`、`04_TASK_SPECIFICATIONS.md`、`05_GATE_CHECKLISTS.md`、`06_ARCHITECTURE_ACCEPTANCE.md`、`07_SCIENCE_AND_TEST_ACCEPTANCE.md`、`08_CPU_RESOURCE_ACCEPTANCE.md`、`10_LINUX_WINDOWS_RELEASE_FLOW.md`、`13_OWNER_REVIEW.md`、`AGENTS_PATCH.md`（10 项）；两代同名顶层文件共 9 个（`00_READ_FIRST.md`、`01_ASTROCS_ENGINEERING_CONSTRAINTS.md`、`09_/11_/12_/14_` 四份、`MANIFEST.json`、`SHA256SUMS`、`START_PROMPT.txt`），实测其中**仅 `01_ASTROCS_ENGINEERING_CONSTRAINTS.md` 逐字节相同**，其余 8 个 hash 全部不同 |
| 编号口径 | 规格与台账分散于 02/05/06/15/16/17/18 | 前缀 `NN_` 出现两个 `01_`（约束与审核结论），台账占 `03_` | 同名前缀重复、且 `02_` 为 CSV 而非 MD |
| 台账 | `05_TASK_LEDGER.csv`，88 行，Gate G0–G11（12 门），族 21 个（含 BAS/CORE/IO/LEG/SCI/DATA/ARCH/API/TEST/BLD/CLI） | `03_REWORK_TASK_LEDGER.csv`，67 行，Gate G0–G10（11 门），族 15 个 | 任务 −21、Gate −1（取消 G11）、族 −6；两代任务号有 **45 个完全同名**（含 CPU-001..004、DOC-001..005、LNX-001..005、P1/P2/P3 全系列、QA-001..003、REL-001..004、VER-001、WIN-001..006） |
| Gate 清单 | `15_GATE_CHECKLISTS.md`：12 Gate / 105 复选项 | `05_GATE_CHECKLISTS.md`：11 Gate / 96 复选项 | −1 门、−9 项 |
| 任务规格覆盖 | `06_TASK_SPECIFICATIONS.md`：51 个 `###` 小节 / 88 号 | `04_TASK_SPECIFICATIONS.md`：67 个 `###` 小节 / 67 号 | 由"合并叙述"改为逐号一节 |
| scripts | 5：`package_audit`、`selftest`、`validate_audit`、`validate_control`、`validate_task_graph` | 6：`package_audit`、`selftest`、`validate_control`、`validate_task_graph`、**`known_failure_scan`（新）**、**`validate_return_audit`（替代 `validate_audit`）** | 净 +1；审核包验证器改名并在 v2 内保留 REQUIRED 白名单 |
| schemas | 6 | 7（新增 `resource_summary.schema.json`） | +1 |
| templates | 9 | 14（新增 `COMMITS.csv`、`FINDINGS.csv`、`PREVIEW_MANIFEST.csv`、`RESOURCE_SUMMARY.csv`、`SOURCE_INDEX.csv`、`SOURCE_IDENTITY.template.json`、`SUMMARY.template.json`、`TASK_RESULT.template.json`；删除 `CHANGE_REVIEW.md`、`OWNER_REVIEW.md`、`TASK_REPORT.md`） | 净 +5 |
| fixtures 目录 | 3 个 JSON（`valid_pipeline`、`valid_module_descriptor`、`invalid_pipeline_serial_heavy`） | 无 `fixtures/` | 负例改由脚本内联构造（`selftest.py`） |
| 状态字面量（包内脚本） | 6：`NOT_STARTED/IN_PROGRESS/PASS/FAIL/BLOCKED/WAITING_WINDOWS`（`CONTROL_V6/…/scripts/validate_task_graph.py`） | 5：去掉 `BLOCKED`（`scripts/validate_task_graph.py:19`） | 脚本枚举收紧；但本代规格文本 `04:47` 反而扩到 7（加 `BLOCKED`、`WAITING_DATA`），并把该 7 值集落到仓库工具 |
| START_PROMPT | 47 字符 | 487 字符（`validate_control.py:58` 上限 1000） | 显著加长；新增须含 `main/ACR/32R/READY_FOR_OWNER_REVIEW` 四 token 的自校验（`:60-61`） |
| 资源阈值口径 | V6 包内 `08_CPU_PARALLEL_BACKEND.md` | `08_CPU_RESOURCE_ACCEPTANCE.md §7` p50≥90 / mean≥85 / worker p50≥2 + 7 项禁止式；`§8` 7 条必需负面测试 | 阈值与负例被写入验收合同正文并同步进 `05:49-50` |
| 交付物 | V6 交付 `astrocs_audit_20260830T183323Z.tar.gz`（被本包 §1 记载为 569 文件、568/568 hash 正确） | 交付 zip 审核包（实测 `AstroCS_V6_1_AUDIT_20260902T042239Z_faad602da555.zip`，1309 条目） | 容器由 tar.gz 改为 zip，命名口径写入 `12 §1` |
| 包内自带校验和结果 | 44/44 ok（`pack_inventory.csv` 该行 ok=44, mismatch=0, missing=0） | zip 45/45 ok；解包件 43 ok / 1 mismatch / 1 missing | 差异只出现在解包件形态 |

---

## 七、执行留痕三方核对

留痕位置：包内台账 `03_REWORK_TASK_LEDGER.csv` ↔ 仓库执行台账与证据 `evidence/v6_1_rework/`（347 文件；`TASK_LEDGER.csv` 67 行、`COMMITS.csv` 62 行、`tasks/` 68 目录、`gates/` 仅 G0/G1/G2 三份 CHECKLIST.md）↔ git 提交消息。本包在 `工程控制/` 下没有 `evidence/<包>/` 目录，执行留痕一律在包外 `evidence/v6_1_rework/`（`00_READ_FIRST.md:33` 要求建该目录并复制台账）。

提交面判据（本报告的两种口径，两者都列出以免单一口径失真）：

- **判据 A（日期窗）**：subject 以 `<TASK-ID>:` 或 `<T-A>/<T-B>:` 开头，且提交日期 ≥ 2026-08-31（包 `created_utc` 之日）。
- **判据 B（基线之后，更严）**：同上前缀，且该提交**不是** `b16d422a`（被审审核包自报 commit）的祖先 —— 用于排除两代同名号（45 个同名号中的 V6 侧提交，如 `b840ed64` 2026-08-31 01:59 `REL-003: 白名单审核包打包+验证`、`97f6ee8e` 01:58 `REL-002: 最终追溯校验 + 旧入口扫描`、`0f78c111`/`ecc805d4` 2026-08-30 三条均系 `b16d422a` 祖先，实测）。
- 两判据差异只在 `REL-004` 一号：A 命中 62/67（92.5%），B 命中 **61/67 = 91.0%**（B 缺 `REL-004` + `WIN-002..006`）。

先修正跨组表的口径（自行验证）：

- `设计大纲/_evidence/packs/pack_commit_link.csv` **无本身份行**（实测该 CSV 全部 16 行中不含 `AstroCS_V6_1_REWORK_CONTROL_20260831`）；`pack_lineage_table.md:39` 因此记 `台账行 0 / 任务号族 空 / 提交命中率 0`。
- 机制核对：`设计大纲/_tools/02b_build_pack_inventory_v2.py:25` 的 `LEDGER_RE = (^|/)([0-9]{2}_)?TASK_LEDGER[^/]*\.csv$` 只能匹配 `NN_TASK_LEDGER*.csv`；本包台账名为 `03_REWORK_TASK_LEDGER.csv`（`REWORK_` 插在中间），故不被识别；上一代 `05_TASK_LEDGER.csv` 可识别（`pack_commit_link.csv` 该身份行：88 号／88 命中／100.0%）。本包 `pack_inventory.csv` 两行的"台账行数"与"状态分布"列为空/`{}` 与此一致。→ 0 命中是**登记口径缺口，非执行缺口**。
- 包外台账事件计数（自证 `pack_events.md` §一.3 的"60 条 LEDGER，seq1361–1458"）：`git log -- evidence/v6_1_rework/TASK_LEDGER.csv` = 60 次（最旧 `a3d16d85` 2026-08-31 = seq1361，最新 `faad602d` 2026-09-02 = seq1458）；`设计大纲/_evidence/commits/structural_events.csv` 中 seq1356–1465 的 LEDGER 事件恰 60 条、detail 均为"改台账 TASK_LEDGER.csv"。两处 60 一致。

自行计算的三方结果（提交面按上文判据 A；判据 B 的差额只影响 REL-004）：

| 类别 | 数量 | 清单 |
|---|---|---|
| 一致（包内台账 ∧ `tasks/<ID>/` 留痕 ∧ 判据 A 或 B 命中的前缀提交） | **60** | 除下列三组外的全部 67 号（60 = 67 − 2 − 5） |
| 有账无据（包内在账，无 `tasks/` 留痕目录） | **2** | `REL-002`、`REL-004`（`REL-002` 有前缀提交 `c99babad`、`8c71f7ab` 与合并提交 `d5a34ea9`；`REL-004` 窗口内无任何前缀提交） |
| 有账无窗口内前缀提交（在账、有留痕、无 `<ID>:` 开头提交） | **5** | `WIN-002`、`WIN-003`、`WIN-004`、`WIN-005`、`WIN-006`（其留痕为单提交 `e1045969`，subject 为范围式 `WIN-001..006: Fatduck 离线 WAITING_WINDOWS 登记 …`；`WIN-001` 另有 15 条窗口内 `WIN-001:` 前缀提交，如 `c1696156`） |
| 有据无账（有留痕/在 `COMMITS.csv`，不在包内台账） | **3** | `CPU-006`、`CPU-007`、`CPU-008`（三者 `tasks/<ID>/TASK_RESULT.json` 状态均 PASS，但 `control_sha256` 分别为 `47076fac…`、`70a9bde9…`、`56dcb191…`，与本包 zip `903018212bfb…` 不同；入库提交为 `5d4ab05d`、`410165f1`/`5d9dc556`、`aceca50c`/`f9930eb0`，日期 2026-09-09，subject 记"CORE 回填"字样；这三个号存在于上一代 V6 台账 CPU-001..008 中） |
| 账—据状态不一致 | 1 | `REL-003`：执行台账 `NOT_STARTED` vs `tasks/REL-003/TASK_RESULT.json` `status=FAIL`（limitations 首行自述"NOT_READY 诚实状态: Windows/32R 门未过"），该任务判据 B 下前缀提交 3 条（`24c7eec7`、`faad602d`、`d5a34ea9`，均 2026-09-02）；另有 `b840ed64`（2026-08-31 01:59）系 V6 侧同号提交 |
| 台账状态终态 | — | `PASS 57 / WAITING_WINDOWS 7 / NOT_STARTED 3`（`evidence/v6_1_rework/TASK_LEDGER.csv` 工作副本；HEAD 版本行内容相同、仅行尾为 LF）；留痕 `TASK_RESULT.json` 68 份分布 `PASS 60 / WAITING_WINDOWS 7 / FAIL 1` |
| 执行台账的历史状态字面量 | — | 60 次落账中出现过的值仅 `NOT_STARTED/PASS/WAITING_WINDOWS/WAITING_DATA`；`WAITING_DATA` 只在 `edcc8688`（LNX-005）短暂出现，`faad602d` 改为 PASS；从未出现 `FAIL/BLOCKED/IN_PROGRESS` |
| 提交号数 | — | 全史 2,045 条提交中，subject 前缀命中 V6.1 号集者 145 条：2026-08-30 2 条（`0f78c111` DOC-001、`ecc805d4` VER-001，早于包 created_utc，属 V6 同号族）、08-31 52 条、09-01 62 条、09-02 29 条；其中前 4 条中 `0f78c111`、`ecc805d4`、`b840ed64`、`97f6ee8e` 实测为 `b16d422a` 祖先（V6 侧同号），判据 A 命中号数 62/67 = 92.5%，判据 B 命中号数 **61/67 = 91.0%**（自行计算，替代 `pack_commit_link.csv` 的缺失行） |
| 命名空间风险 | — | V6.1 67 号与 V6 88 号同名者 45 个 → 单靠提交消息无法定代，本报告一律用"号 + 日期窗 + 台账文件"三条件联合判定 |

`COMMITS.csv`（执行留痕，24,273 B，62 行）与 git 的逐行复核（实测 62/62）：

- 60 行 `result_commit` 为可解析 SHA40；`MON-001`、`MON-002` 两行 `result_commit = PENDING-FRONTEND-FILL` 且 `pushed_origin_main=false`、日期 2026-09-09（对应提交 `6c69848a`/`16abea46` 另在同表出现，故该两号为重复行）。
- 全部 62 行 `parent_commit` 均为合法 SHA40；`CPU-007`、`CPU-008` 两行登记的 parent（`ec7dd747…`）与该 commit 实际 parent（`07e50ff8…`、`8aca39b6…`）不符。
- 7 行 `commit_subject` 不以 `<ID>:` 开头：`CPU-001/CPU-002:`（同一提交服务两号）、`CPU-006`（subject 空）、`feat(benchmark): CPU-007 …`、`feat(scheduler): CPU-008 …`、`MON-001 reverify-2: …`、`test(resource): MON-002 …`；另有双号前缀提交共 3 条（含 `REL-002/REL-003: …` `d5a34ea9`）。
- 交付审核包内 `COMMITS.csv` 为 57 行、表头 `task_id,commit,parent,subject,pushed,origin_main_ancestor,task_result_sha256`（与 `12 §5`／终局验证器口径相同，与执行留痕口径不同）；`FINDINGS.csv` 40 行（CLOSED 36 / OPEN 4）、`SOURCE_INDEX.csv` 1,245 行、`TEST_SUMMARY.csv` 254 行（PASS 246 / NOT_RUN 1 / WAITING 7）、`RESOURCE_SUMMARY.csv` 3 行（gate 全 PASS）、`PREVIEW_MANIFEST.csv` 1 行（`source_kind=synthetic`）。
- FINDINGS 闭环对账：归档副本 `02_FINDINGS.csv`（12 列，含 `resolution_commit`、`resolution_evidence_sha256`）36 条 CLOSED 的 `resolution_commit` **全部可在 git 解析**（实测 36/36），12 条等于 `COMMITS.csv` 的 result_commit，24 条指向其他修复提交；4 条 OPEN 为 `F-032/F-033/F-036`（required_task=REL-003）与 `F-037`（required_task=WIN-006），均 P1。zip 内密封版为 10 列、40 条全 OPEN（`validate_control.py:76-80` 要求）。
- 交付审核包 SUMMARY 实测（git blob）：`verdict=NOT_READY`、`control_sha256=903018212bfb…`（本包 zip 的 sha256）、`ledger_sha256=a71f287757…`（当前工作副本台账 hash，非密封台账 `c6f21304…`）、`source_commit=main=origin_main=faad602da555…`、`gate_status` G0–G8 PASS / G9、G10 WAITING_WINDOWS、`final_32r.status=NOT_RUN`、`not_verified` = WIN-001..006 + REL-001/002/003 括号项、`owner_review=PENDING`。
- 终局门实测（`/tmp` 内解包，未写仓库）：zip 内密封版 `validate_return_audit.py` → `AUDIT_PACKAGE_FAIL: banned path/type: evidence/build/summary/lnx001.md`；解包件被改版 → `AUDIT_PACKAGE_PASS files=1309 bytes=13571160 tasks=67 findings=40 verdict=NOT_READY`。
- 包内 00/G0 断言抽查：`evidence/v6_1_rework/gates/G0/CHECKLIST.md` 第 1 项记 `CONTROL_PASS files=47`（与本报告对 zip 的实测一致）、第 8 项记 `KNOWN_FAILURES_BASELINE.json sha256=dbe859b8…`（与 `tasks/R0-004/KNOWN_FAILURES_BASELINE.json` 实测 hash 一致，40 条 finding 全部 status=REPRODUCED）；该 CHECKLIST 仅存在于 G0/G1/G2，G3–G10 无对应文件（`05_GATE_CHECKLISTS.md:3` 要求每个 Gate 完成时复制入 `evidence/v6_1_rework/gates/Gx/`）。
- 台账 hash 冻结链：`tasks/R0-003/ledger_state.json` 记 `task_count=67`、`{NOT_STARTED:65, PASS:2}`（R0-001/R0-002 完成时点快照），65/68 份 `TASK_RESULT.json` 的 `ledger_sha256 = c6f21304…`（等于密封台账），3 份（CPU-006/007/008）指向其他 hash。

---

## 八、缺口与不确定

1. **解包件形态不可自校验**：`工程控制/AstroCS_V6_1_REWORK_CONTROL_20260831/` 缺 `02_FINDINGS.csv`（实测 `CONTROL_FAIL: missing required files`），且 `scripts/validate_return_audit.py` 与包MANIFEST/SHA256SUMS 不一致（补文件后转 `CONTROL_FAIL: manifest mismatch`）。证据指针同上 §一、§四.1。
2. **digest 文本截断**：`设计大纲/_evidence/packs/digest/AstroCS_V6_1_REWORK_CONTROL_20260831.md` 全文 59 行，第 24–58 行的 `00_READ_FIRST 开头` 摘录止于第 5 步"复制任务台账；不得"（未含第 6、7 步与 §4–§7）；已用包内本体补全。
3. **跨组表三项数字需替换**：`pack_lineage_table.md:39` 的"台账行 0、任务号族空、提交命中率空"与 `pack_commit_link.csv` 缺行，成因为 `02b_build_pack_inventory_v2.py:25` 的台账文件名正则不含 `(NN_)?REWORK_` 形态；实测该台账 67 行、15 族、窗口命中率 92.5%。
4. **实例计数不全**：digest/lineage 记 2 实例，实测另有归档容器内 1 文件目录（`b7b2dea7` R100）与 `run/` 下 72 份同一单文件副本（`.gitignore:19`，全为同一 hash `0f0c6573…`）；"归档容器内仅见 02_FINDINGS.csv"的 计划文档 说法（`设计大纲/00_计划_历史溯源与控制包取证.md:71`）**核实成立**，且该目录仅此 1 文件。同处 46 / 332KB 为 `du -sh` 块口径，与 digest 的 204.2 KB（字节口径）不冲突（实测两值同得）。
5. **zip 与审核包 zip 无 git 引入事件**：`工程控制/_control_packs/*.zip` 全史 0 入库（`pack_events.md` §一.5），但本代交付审核包曾以 `dist/audit/*.zip` 入库又在 `789c5b6c` 出库（blob 现存于 `24c7eec7`）；"zip 一律不入库"的表述对该路径不成立。
6. **被改验证器的 4 处改动与包内条文互相指向**（逐项实测）：hunk1-2 `zip:102-107`（CMake 字面源引用扫描）↔ 解包件 `102-121` 新增跳过注释/`"`/`${`/`$<`/`find_*`、`/third_party/`、`_generated.*`；hunk3 `zip:125` ↔ 解包件 `139-144` 把 `build`、`history` 从禁止段豁免（注释自述"规范要求 evidence/build/summary/* 证据目录"，指 `12_AUDIT_PACKAGE_SPEC.md:22-40` 白名单第 `evidence/build/summary/*` 条目）；hunk4 `zip:186` ↔ 解包件 `205-211` 接受双号前缀 subject（注释自述"V6.1 已知合法模式"，指 `CPU-001/CPU-002`）。改动后的脚本无任何 git 版本（`git log --all -- "*validate_return_audit*"` 空）。
7. **包内自相矛盾条文（可核对，不评价）**：密封版 `validate_return_audit.py` 的 `BANNED_PARTS` 含 `build`，而 `12_AUDIT_PACKAGE_SPEC.md:22-40` 要求交付含 `evidence/build/summary/*`；实测密封版对交付包报 `banned path/type`。另：包内 `validate_task_graph.py:19` 的 5 值状态集与 `04_TASK_SPECIFICATIONS.md:47` 的 7 值集不同，`WAITING_DATA` 会被前者判为非法。
8. **`COMMITS.csv` 列名双口径**（`12 §5` + 模板 + 验证器 vs `04 §GOV-001` + 执行留痕），已实测三处采前者、两处采后者；交付包与留痕因此需各自转换（`d5a34ea9` subject 自述"列转 validator 格式"）。
9. **`AGENTS_PATCH.md` 落地与否**：仓库根 `AGENTS.md` 在 2026-08-30..09-03 仅一次改动（`065c81de` 2026-09-02），`git log --all -S"未来 GUI 只驱动 CLI" -- AGENTS.md` 无命中，`git show faad602d:AGENTS.md` 中 `WAITING_WINDOWS`/`DORMANT_NOT_IN_PRODUCTION` 计数为 0；本包是否曾以其他文件形式落地：**未证实**。
10. **仓库根约束文件的时序**：`00_READ_FIRST.md:20` 与 `validate_control.py:16,54` 以 `AstroCS_ENGINEERING_CONSTRAINTS.md`（`dc47fbb…`）为最高优先级；该路径首次入库为 `065c81de`（2026-09-02，7,`7f139bb4…`），在 `b16d422a`/`8be6eb4e`/`8c71f7ab` 时不存在（`git show <ref>:该路径` 报 NOFILE），HEAD 现为 `e6547a0b…`（`d8c821db` 2026-09-10 宪章降级后）。包内 `01_` 副本 hash 与之均不同。
11. **无 `history/<rev>/` 复原件**：`设计大纲/_evidence/packs/history/` 13 个复原目录中无本包路径（实测 find 空），故本包不存在"仅历史复原"形态；亦无法用 `_PROVENANCE.json` 交叉验证本包。
12. **CPU-006/007/008 归属未证实**：其留痕的 `control_sha256`（`47076fac…`/`70a9bde9…`/`56dcb191…`）与 `工程控制/_control_packs/` 现存 11 个 zip 的 sha256 均不相等（实测比对），亦不等于本包；其号在 V6 台账存在、在本包台账不存在。
13. **REL-004 的窗口内证据缺失**：包内要求 REL-004 恒为 NOT_STARTED（`04:382`、`05:131`），实测其无 `tasks/REL-004/` 留痕、无窗口内前缀提交，唯一的 `REL-004:` 前缀提交是 V6 期的 `a7faa502`（2026-08-31 02:01:18+08:00，subject 记 REVIEW_PENDING）。
14. **Gate 留痕不全**：`evidence/v6_1_rework/gates/` 仅 G0、G1、G2 三目录，G3–G10 缺（交付包内 `evidence/*` 计 10 条摘要条目，未含各门 CHECKLIST）。
15. **读不到/无对象**：`reviewed_audit_sha256 = 137e3ed14ef85142…`（被审审核包）在仓库现存文件与 11 个 zip 中无任何命中（实测 hash 扫描），tar.gz 原件不在仓库；其对 569/568 的计数只有 `01_INDEPENDENT_AUDIT_CONCLUSION.md:9` 单点来源，**无法二次核验**。

---

## 尾一、与上一组（G08）的衔接

- 本组唯一包 `AstroCS_V6_1_REWORK_CONTROL_20260831` 与上一组末包 `CONTROL_V6`／内包 `AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830` 的关系为**废止其状态、继承其冻结约束**，依据：
  - 废止声明：`00_READ_FIRST.md:12`（"上轮结果正式打回。不得继承'81 PASS、Linux 侧完成、G11 PASS'等状态"）；`01_INDEPENDENT_AUDIT_CONCLUSION.md:5`（禁止把包内 81 项 PASS 继续作为真相源）、`:138-141`（当前 commit 只作待修起点、不回退历史）。
  - 被点名状态的实物：V6 终态执行台账 `evidence/refactor/TASK_LEDGER.csv`（最完版本提交 `8be6eb4e`，2026-08-31）实测 88 行 = **PASS 81 / WAITING_WINDOWS 6（WIN-001..006） / REVIEW_PENDING 1（REL-004，G11）**；V6 期文档 `docs/review/RELEASE_STATUS.md`（提交 `b16d422a`）第 3、6、7、9 行分别写"V6 重构 Linux 侧完成""已过门: G0/…/G9/G11(Linux 侧)""88 任务: 81 PASS + 6 WAITING_WINDOWS + 1 REVIEW_PENDING"——即 00_READ_FIRST 三项点名（81 PASS、Linux 侧完成、G11 PASS）的出处；`REVIEW_PENDING` 字面量另见提交 `a7faa502`（2026-08-31）。
  - 依赖侧点名：`01_INDEPENDENT_AUDIT_CONCLUSION.md:23`（V6 台账 `WIN-006=WAITING_WINDOWS` 而其依赖者 `REL-001=PASS`）；实测 V6 台账该行 `REL-001` gate=G11、depends_on=`LNX-005;WIN-006;QA-005`、status=PASS，成立。
  - 新包对上述两点的直接堵法：`scripts/validate_task_graph.py:64-65,101-108` 与 `scripts/selftest.py`（用 `REVIEW_PENDING` 作非法状态负例、用"依赖未过即 PASS"作负例）；`04_TASK_SPECIFICATIONS.md:64-66`（GOV-002"修复上轮 REVIEW_PENDING 和依赖未过但 release PASS 漏洞"）。
  - 继承实物：`MANIFEST.json` 的 `prior_control_sha256 = fdad8e40…` = 上一代 zip 的实测 sha256；`01_ASTROCS_ENGINEERING_CONSTRAINTS.md` 与上一代同名文件同 hash（`dc47fbb…`）。
  - 归档同轨：两代包目录的同一次移动均发生在 `b7b2dea7`（GOV-002 归档），V6 系整包入容器、V6.1 仅其 `02_FINDINGS.csv` 入容器（实测 name-status R100）。

## 尾二、重叠身份

- 按 `设计大纲/_evidence/packs/dispatch_groups.json`，`AstroCS_V6_1_REWORK_CONTROL_20260831` **只登记于 G09**，无跨组重复登记，故本组无"同一身份出现在别组"的重叠身份。
- 目录级重叠（标注用，不视为身份重叠）：本包 1 个文件（`02_FINDINGS.csv`）物理位于 G11 身份 `archive__2026-09-02_legacy_工程控制_v1.3-to-v6.1` 的容器内（`engineering/control/archive/…/AstroCS_V6_1_REWORK_CONTROL_20260831/02_FINDINGS.csv`）。G11 的 digest（`digest/archive__2026-09-02_legacy_工程控制_v1.3-to-v6.1.md`）登记容器"含子包 9 个"（实测标记文件所在子目录：`archive/ACR_*`×4、`CONTROL_V4/…`、`CONTROL_V6/…`、`REAUDIT_V3/v3_reaudit`、`RELEASE_V5/…`、`tasks/acr`），**未把本包目录列为子包**（其内无 `00_READ_FIRST.md`/`MANIFEST.json`/`START_PROMPT.txt`），因此与本组 digest 的"2 实例"记载不冲突但口径不一致：本组 digest 不计该形态，G11 亦不计该目录为包，而 计划文档 §1.2 称之为"AstroCS_V6_1_REWORK（部分）"。
- 与 G08 的文件级重叠：`01_ASTROCS_ENGINEERING_CONSTRAINTS.md`（同 hash `dc47fbb…`）在两代包各存 1 份；`14_PRIMARY_REFERENCES.md`、`12_AUDIT_PACKAGE_SPEC.md`、`11_GIT_MAIN_ONLY.md`、`09_DOCUMENTATION_AND_MACHINE_CHECKS.md` 同名不同 hash（实测 00_READ_FIRST 亦同名不同 hash：V6 `ef821216…` vs V6.1 `f08e7c5a…`）。
