# P-G14 发布救援控制包 V3 取证报告

- 身份：`AstroCS_RELEASE_RESCUE_CONTROL_V3_20260912`（组 G14，本组唯一身份）
- 取证时间口径：2026-09-15（工作区 HEAD `b67c32fa`，2026-09-15 02:50 +0800）
- digest：`设计大纲/_evidence/packs/digest/AstroCS_RELEASE_RESCUE_CONTROL_V3_20260912.md`（57 行，记"共 2 个实例"）
- 包内文件路径缩写：`<PKG>` = `工程控制/AstroCS_RELEASE_RESCUE_CONTROL_V3_20260912/`；行号为包内文件实读行号；提交号 8 位。

## 一、组内包清单与身份归并

| 身份 | 存形态实例 | 文件数 | zip sha256 | 首/末提交 | 删除 |
|---|---|---|---|---|---|
| AstroCS_RELEASE_RESCUE_CONTROL_V3_20260912 | 2：worktree 解包件 ×1 + zip 原件 ×1；归档/影子树/仅历史复原均 ×0 | 各 24 | `14ee6592f0e1e6b556bdaeb156ba33e1898d2998cf3e97ab7d09dcb401f6fdf5` | 无任何提交出现（两形态均未入库） | 未被删除（全史无路径事件） |

- 实例清单与 `pack_inventory.csv` L41–42（两行：worktree 24 文件/33161 字节无自带 sha；zip 24/33161、sha256 同上行）及 `pack_lineage_table.md` L48（实例 2、形态 work,zip、最大文件 24、子包 0、台账行 0）一致。
- 两形态文件集合与内容**逐字节一致**：本次用 python3 zipfile 对 zip 内 24 条目与解包件同名文件做 sha256 比对，zip-only=∅、wt-only=∅、byte-diff=∅。
- zip sha256 本次独立复算 = digest 所记值 = inventory 所记值（三处一致）。
- git 状态（本次实测，回应 G05 前提"_control_packs 四 zip 全部 untracked"）：
  - 解包件：`git ls-files <PKG>` = 0；`git status --porcelain` 显示 `??`；`git check-ignore` 判"未被忽略"。
  - zip：`git ls-files 工程控制/_control_packs/AstroCS_RELEASE_RESCUE_CONTROL_V3_20260912.zip` = 空；`??`；未被忽略。**本包 zip 与 G05 实测同为未跟踪**，且解包件同样未跟踪（对照：同目录宪章对齐包全部被跟踪，`git ls-files 工程控制/ | wc -l` = 85，对齐包引入提交 `e9547c02`，2026-09-10）。
  - `git log --all -- <PKG> <zip>` 输出为空（0 条路径事件），与 digest 两实例 git 事件全 None 一致。
- 时间锚（非 git，因无提交）：zip 内条目时间戳 2026-09-12 08:19:18–09:50:26；zip 文件 mtime 2026-09-12 22:38；解包件目录 mtime 2026-09-13 13:06:24。
- 影子树/归档/历史复原核查：`engineering/control/` 下无 RESCUE 命中；`设计大纲/_evidence/packs/history/` 无 rescue 目录；`run/release-rescue/*/clean/` 下无本包副本目录。

## 二、包内文件地图

根级 6 件（无 NN_ 编号体系）：

| 文件 | 自述职责（标题/首段要点） |
|---|---|
| `00_READ_FIRST.md`（39 行） | 标题"AstroCS 科学重审与全流程恢复工作包 V3"；L3 自述不依赖 CPRun/CP 插件/任务图解析器；§唯一目标 5 项；§执行原则 10 条；§前台启动 4 步 |
| `01_SCIENCE_AUTHORITY_BASELINE.md`（53 行） | L1 自述"审计输入，不是对现有代码的背书"；§1 权威层级 5 条、§2 固定一手来源 7 域（FITS/FITS-WCS/投影/HiPS/MOC/HEALPix/Drizzle）、§3 坐标词汇 `i/u/p/e` 四型、§4 项目定义域最低要求、§5 证据链 7 环 |
| `02_E2E_ACCEPTANCE.md`（40 行） | "端到端与 CI 验收口径"，分节 A 最早闭环、B 科学闭环、C Linux CI 真启用、D testdata 全量解析、E 两套马赛克、F 冻结与 Windows 边界 |
| `FRONT_DESK_RUNBOOK.md`（41 行） | "前台运行规则"：角色六件事、并行与写入、每次派发、每次验收 5 条、失败处理、跨会话 |
| `TASK_LIST.md`（30 行） | "任务清单与依赖"：阶段 0–11 表格 + L26 并行/失效规则 + L28 写入范围 |
| `START_PROMPT.txt`（1 行 141 字节/54 字） | 启动提示词；满足 00 L37 自设"不超过 100 字"上限 |

`tasks/` 18 件：AUD-CI-LIVE、AUD-COORD、AUD-E2E、AUD-P1、AUD-P2P3、CI-LINUX-EXACT、DOC-CLOSE、FINAL-AUDIT、FIX-CI、FIX-E2E、FIX-SCIENCE、LINUX-FREEZE、REAL-MOSAIC-E2E、REAL-PLATESOLVE-ALL、RESCUE-RULING、RESCUE-SNAPSHOT、REVIEW-SCIENCE、WIN-PORT-VERIFY（与 digest 所记 18 项同名同集合）。命名口径：全大写语义 ID + 连字符，无数字任务号、无 `NN_` 前缀。`templates/`=0、`schemas/`=0、`scripts/`=0（包内无这三类目录）；亦无 control-pack.json、OWNER_BINDINGS、CHANGELOG、validators/。

## 三、任务结构

- 任务数：18。任务号族：`RESCUE-*`（2）、`AUD-*`（5）、`FIX-*`（3）、`REVIEW-*`（1）、`CI-*`（1，CI-LINUX-EXACT）、`REAL-*`（2）、`LINUX-FREEZE`、`WIN-PORT-VERIFY`、`DOC-CLOSE`、`FINAL-AUDIT` 各 1。
- **台账不存在**：包内无任何 `TASK_LEDGER*.csv`（24 文件清单实证）。`00_READ_FIRST.md` L37 自述"任务状态可在当前会话中维护；若必须跨会话，在 `run/release-rescue/HANDOFF.md` 写一页简短交接，不建立复杂台账"。**无台账表头字段可录**。仓库实测 `run/release-rescue/HANDOFF.md` 不存在，实际承担记录职能的是 `run/release-rescue/FRONT_DESK_VERIFY.md`（L3 引用包路径 `<PKG>`，L5-L7 记录基线 `f5f944a5`→`5a250999`）。
- 状态字面量（包内文本出现，非台账分布——分布因无台账不可统计）：
  - 包级结论 4 值：`READY_FOR_OWNER_REVIEW`/`NOT_READY`/`BLOCKED_EXTERNAL`/`PACKAGE_INVALID`（`00_READ_FIRST.md` L39；FINAL-AUDIT.md L7）。
  - 验收级：`NOT_VERIFIED`、`BLOCKED_EXTERNAL`（FRONT_DESK_RUNBOOK.md L28）；`PARTIAL`/`NOT_VERIFIED`（01 §5 L52）。
  - 科学分级集：`CONFORMANT/PROJECT_DEFINED/PARTIAL/NON_CONFORMANT`（AUD-P2P3.md L3）。
  - 产品状态 `complete` 及负例非零退出（02 §A L5）；冻结撤销标记 `LINUX_BASELINE_INVALIDATED`（WIN-PORT-VERIFY.md L7）；视觉审核 `PASS/FAIL`（REAL-PLATESOLVE-ALL.md L24）。
- 依赖与顺序声明（包内）：`TASK_LIST.md` L7-L24 阶段 0→11 表逐行给出"可并行/开始条件/完成标志"；L26 规定 6A/6B 只能同一 clean Linux SHA 并行、任一需改科学源码则两方证据全部作废、LINUX-FREEZE 后 Windows 科学问题须回 Linux 解冻重验；FRONT_DESK_RUNBOOK.md L9-L14 规定 RESCUE-SNAPSHOT 先行、五个 AUD-* 并行、`FIX-E2E/FIX-SCIENCE/FIX-CI/DOC-CLOSE` 严格串行、任一时刻最多一个 SubAgent 改 tracked 源码。

## 四、门禁与验收要求（全部标出处）

机器门清单：
1. 合成 E2E 门：三 Phase 各自正式 CLI、rc=0、状态 complete、artifact 非空且 hash/manifest 可验、负例必须非零退出不得写 complete（`02_E2E_ACCEPTANCE.md` §A L5）。
2. 外部交叉验证门：CFITSIO + Astropy/WCSLIB 独立读取、绝对已知 sky↔pixel fixtures、HiPS/MOC/HEALPix 独立库交叉（§B L9-L11）；每条科学结论 7 环证据链 `AUTH/SCI→ALG→DATA/API→production symbol→independent test→executed evidence→product/manifest`，任一断裂只能标 `PARTIAL/NOT_VERIFIED`，且须有自测之外独立实现 + 故障注入必败证明（`01_…` §5 L52）。
3. CI 真启用门："workflow 文件存在不算启用"，须 Actions 允许、state=active、`workflow_dispatch` 精确 SHA、conclusion=success、artifact 内部 SHA 一致；禁用旧 run/其他 SHA/本地 `ci/run.py`/cancelled/skipped 替代（§C L16）。
4. 全量解析闭合门：`count_enumerated = count_solved + count_failed_with_reason + count_explicitly_unavailable`，零静默丢弃，不硬编码"906"（§D L20；REAL-PLATESOLVE-ALL.md L8 同式）。
5. 双马赛克门：M42 与银心两组都必须完成，"只跑其中一组不得通过"（§E L30；REAL-MOSAIC-E2E.md L13 同判）。
6. 基线冻结门：`LINUX_SCIENCE_BASELINE.json` 字段清单（source SHA/工具链/输入 hash/容差/统计/产物/资源/视觉索引）（§F L36；LINUX-FREEZE.md L5 扩展为"任何关键门未执行或有开放 P0/P1 均不得冻结"）。
7. 故障注入复核门：REVIEW-SCIENCE.md L3 列 7 种 mutation，L5"任何 mutation survived 均失败并退回 FIX-SCIENCE"。
8. 超时纪律：一切外部进程/网络/CI/硬件等待设总超时（00 L26；RUNBOOK L37）；量化口径：AUD-CI-LIVE.md L3 网络 60s、轮询上限 20min；CI-LINUX-EXACT.md L3 单次 API 60s、间隔 30–60s、总上限 6h。
9. 审核包门：FINAL-AUDIT.md L5 生成 `<10MB` 审核包（只含结论/manifest/必要 diff/矩阵/CI 摘要/ledger/预览/findings/复现命令/SHA256；禁塞源码树、真实 FITS、数据库、巨型日志），校验 zip 可解、清单 hash 正确。

完成顺序要求：TASK_LIST.md L7-L24 阶段序 + L26 SHA 一致性规则；RESCUE-RULING.md L5 把源码动作冻结为不超过三批（①合成 E2E ②科学正确性 ③CI 真启用）；02 §C L16 末句与 §F、WIN-PORT-VERIFY.md L3 规定 Windows 一切真实运行后置于 Linux 冻结之后；00 L39 最终四态报告限定 + "发布决定仍由项目负责人作出"。

状态机口径：无任务级状态机与台账（与宪章对齐包不同，见第六节）；仅 4 值包级结论字面量（00 L39）与 02 §F L40 的 `READY_FOR_OWNER_REVIEW` 达成条件（Linux 冻结 + Windows CI/Fatduck 复验 + 零开放 P0/P1 + 文档追踪一致）。

waiver/豁免机制：**包内只设禁止，不设通道**——00 L25"不靠 waiver、skip、扩大 known-failures、放宽容差或把 partial 写成 complete 获得绿灯"；RUNBOOK L35"不增加 waiver、skip 或 known-failures 掩盖 P0/P1"；FIX-CI.md L3"不通过 skip、waivable、known_failures 增长、软失败或缩减 profile 转绿"；FIX-E2E.md L7"新测试同步进入现有 CI 注册表，但不得扩 known failures/waiver"；FIX-CI.md L5 权限不足时标 `BLOCKED_EXTERNAL`、"不得伪造启用"。仓库现实对照见第七节末段（`ci/checks.json` 的 `waivable` 字段与 `ci/known_failures.json` 是仓库既有机制，包内未定义与其衔接的登记流程）。

提交要求：RUNBOOK L14 SubAgent 禁全部 git 写操作、前台验收后精确暂存；L30 单目的 commit、push `main`、fetch 核对 `HEAD == main == origin/main`、长 CI 期不密集 push；00 L22 不新建分支/worktree/仓库、不 reset/stash/clean/rebase、不覆盖未提交内容。

## 五、设计意图（转述，不评价）

- 目标（00 §唯一目标 L9-L15 转述）：在不回退既有重构成果前提下，依外部权威标准与独立 Oracle 重审科学定义与代码，恢复五件事：可构建；三 Phase 真实 CLI 分别运行并经持久化产品交换；Linux CI 与全量真实数据终验成功并冻结 Linux 科学基线；Windows 只在冻结算法上做编译/ABI/安装树/数值对拍随后 Fatduck 验证；文档、合同、实现、测试与证据在最终 SHA 一致。01 L1 明确基线文件"不是对现有代码的背书"。
- 禁止项（00 §执行原则 L19-L28）：旧会话/旧控制包继续派工；并行写 tracked 源码；Worker git 写；新建分支/worktree/reset/stash/clean/rebase；checkout 或运行 900 余提交时的旧可运行版本（仅作 git archaeology）；waiver 换绿灯；产品中新增隐式三阶段连跑；Linux 冻结后在 Windows 阶段改科学公式/默认参数/容差/数据语义/测试真值。01 §1 L9 禁止现有文档/测试/代码互相循环证明；01 §3 L36 禁止用两个共享同一错误的正反变换证明绝对原点。
- 对前代的继承/废止声明：包内唯一的前代排斥性表述为 00 L19"先收敛为一个前台和一份活动控制包；旧会话、旧控制包不得继续派工"与 00 L3 不依赖 CPRun/CP 插件——**未点名任何前代包身份，未引用宪章条款号**（全包 `§` 出现 0 次；"宪章"仅 3 处泛指：01 §1 第 4 条、RESCUE-SNAPSHOT.md L7、RESCUE-RULING.md L3）。
- 负责人裁决/用户指令痕迹：包内无 OWNER_BINDINGS/04_OWNER_DECISIONS/07_FRONT_DESK_RULINGS 类文件（24 件清单实证）。外部登记痕迹：`工程控制/ACTIVITY_STATE.md` §2 状态表首行登记本包 ACTIVE 并引 2026-09-13 负责人指令原文「使用该工程包替代以前的工程包，作为权威并执行」；该登记由提交 `5a250999`（docs(GOV/rev6.3): 登记 RESCUE-V3 为唯一 ACTIVE 控制包——对齐包 V1 转 ARCHIVED_SUPERSEDED，2026-09-13 13:15 +0800，改 2 文件 +6/-5）写入；`docs/DOCUMENT_INDEX.yaml` L600/L604 两处 replacement 字段指向本包目录。
- 版本字面规定：**包内 0 处版本号**（无"0.11"、无 `VERSION` 字样命中）。仓库现实：输入状态 SHA `f5f944a5` 时点根 `VERSION` = `0.11.0-alpha.2`，HEAD 时同值（`git show f5f944a5:VERSION`）。
- Fatduck 字面规定：Fatduck 出现在 00 L14、TASK_LIST L22、WIN-PORT-VERIFY.md L5（不 checkout/编译源码，只验证同一 candidate 的 version/doctor/modules、安装树、合成三 Phase 与代表真实烟测）及其 L7、FINAL-AUDIT.md L3、02 L40；共 6 处，语义与宪章 §15.3 角色口径一致，但包内未引用条款号。
- CI 状态字面规定：`state=active`、`workflow_dispatch`、`conclusion=success`、"未启用/未触发/排队被替换/环境失败/产品测试失败/artifact 缺失"六分类（02 §C L16；AUD-CI-LIVE.md L5；FIX-CI.md L5 要求解决 concurrency 导致精确 SHA 取不到结果的问题）。

## 六、代际差异表（对照上一代 ACTIVE 包 AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909；代际依据 = ACTIVITY_STATE.md §2 两行 + 提交 `5a250999`）

| 维度 | 宪章对齐包 V1（20260909） | 发布救援包 V3（20260912） | 可核对差异 |
|---|---|---|---|
| 文件数 | 74（find 实测） | 24 | −50 |
| 编号规格文件 | 00–08 共 9 件 | 00–02 共 3 件 | 03–08 号规格文件不存续；新增无编号根件 `FRONT_DESK_RUNBOOK.md`/`TASK_LIST.md`/`START_PROMPT.txt` |
| tasks/ | 57 件（命名如 `CI-001.md`、`AIO-001.md`） | 18 件（命名如 `FIX-CI.md`，语义段前缀 `AUD-/FIX-/REAL-`） | 任务数 57→18 |
| 台账 | `TASK_LEDGER.csv` 57 行，表头 10 列 `task_id,title,owner_id,lane,depends_on,spec_ref,write_scope,timeout_sec,domain,status`；状态分布 PASSED 29 / NOT_STARTED 23 / IN_PROGRESS 5；另有 `TASK_LEDGER_V1_ARCHIVE.csv` | 无台账文件；00 L37 明文"不建立复杂台账" | 状态字面量体系由 PASSED/NOT_STARTED/IN_PROGRESS 变为包级 4 值 + 分级集（见第三、四节） |
| 机器件 | `control-pack.json`、`schemas/evidence.schema.json`、`validators/selftest.py`、`validators/validate_control.py`、`OWNER_BINDINGS.yaml`、`CHANGELOG.md` | 全部不存在 | 删除 6 类文件；templates/ 两代均无 |
| 宪章引用 | `00_READ_FIRST.md` 含 `§` 2 处 | 全包 `§` 0 处 | 引用口径由条款号改为泛指"冻结宪章" |
| git 存形态 | tracked（`e9547c02`，09-10 入库） | 两形态均 untracked（实测 `??`，非忽略） | 入库策略相反 |
| zip 原件 | `_control_packs/` 无本包 zip（ls 实证无 CONSTITUTION 名件） | `_control_packs/` 有 zip 原件且未入库 | 新增 zip 派发形态 |
| 调度框架 | 使用 CP 框架（台账含 owner_id/lane 列；其 08 号交接记载负责人停用 CP 指令） | 00 L3 明文不依赖 CPRun/CP 插件 | 继承 08 号交接的停用口径（文本平行，见尾段） |

门禁松紧仅列可核对差异：救援包取消台账/validators/机器合同件，新增包内自述的机器门条款（第四节，02 §A–F、REVIEW-SCIENCE mutation、FINAL-AUDIT 审核包）；对齐包台账有 `timeout_sec` 列，救援包无台账、超时以任务书文字规定（AUD-CI-LIVE L3、CI-LINUX-EXACT L3）。两代门禁条文语义孰严孰宽超出可核对范围，不写。

## 七、执行留痕三方核对

口径先行：本包**无 `TASK_LEDGER*.csv`**（24 件清单实证），故 `pack_commit_link.csv`（17 行含表头，即 16 行数据）无本组行——原因＝①包内不存在可被 LEDGER_RE（只认 `NN_TASK_LEDGER*.csv`）匹配的台账文件；注意该正则同样匹配不到宪章对齐包的 `TASK_LEDGER.csv`（无 `NN_` 前缀），属 G09/G12 已证实采集盲区在本组的叠加情形；②两形态均未入库，路径扫描无从发现。`pack_events.md` L70/L109/L140 同结论（"CSV 未捕获（zip + 工作副本均未入库，无台账路径事件）""A+B 类""台账 0 行"）。以下以 `TASK_LIST.md` 的 18 任务号为"账"，自行三方计算。

**账侧**：18 任务号（第三节清单）。

**据侧（run/release-rescue/，顶层 65 条目）**：16 个条目映射到 14 个任务号——`snapshot/`（REPORT.md + STARTUP_SNAPSHOT.json 44626B，09-13 13:11）、`audits/aud-{coord,p1,p2p3,e2e,ci-live}/REPORT.md`（5 审计件齐）、`ruling/REPAIR_PLAN.md`、`fix-e2e/REPORT.md`、`fix-science{,-2,-3}/REPORT*.md`、`review-science/REPORT.md`、`fix-ci{,-2}/REPORT.md`、`ci-linux-exact/`（run.json/jobs.json/candidate_sha.txt/linux-ci-artifact.zip 等，**无任务书规定的 REPORT.md**，见 CI-LINUX-EXACT.md L5）、`real-platesolve-all{,-r2,-r4}/REPORT.md`（-r3 无 REPORT，仅 ATTEMPTS.jsonl 等）、`real-mosaic-e2e/`（PREP_REPORT.md、SUMMARY.md、prep_audit.json）。

**码侧（git 提交消息，main）**：含 `RESCUE-` 字样提交 17 枚（并集去重），首枚 `5a250999`（09-13 13:15，登记 ACTIVE）、末枚 `e60edcec`（09-14 03:06，fix(SCI/FD-05)…（RESCUE-FD-05））；其中 `RESCUE-V3` 10 枚（3848fc61 Batch 1 → 9a3b5a7d P1 终批，09-13 16:30–21:43）、`RESCUE-FD` 10 枚（c21eec50 FD-01 → e60edcec FD-05，与 RESCUE-V3 集重叠 3 枚）；另有 FD 系后续 `fa5581a0`（09-14 10:27，FD-14）、`23a91351`（FD-08b）、`48ceee59`（09-14 04:59，FD-08）。任务号字面在提交消息出现计数：AUD-COORD 4、AUD-P1 4、AUD-P2P3 3、AUD-CI-LIVE 2、REVIEW-SCIENCE 2、DOC-CLOSE 2（均为消息正文引用 finding/去向）；其余 12 个任务号字面 0 命中。

分类结果：
- **一致（有账有据，14 项）**：RESCUE-SNAPSHOT、AUD-COORD、AUD-P1、AUD-P2P3、AUD-E2E、AUD-CI-LIVE、RESCUE-RULING、FIX-E2E、FIX-SCIENCE、REVIEW-SCIENCE、FIX-CI、CI-LINUX-EXACT、REAL-PLATESOLVE-ALL、REAL-MOSAIC-E2E。前台独立复跑记录在 `run/release-rescue/FRONT_DESK_VERIFY.md`（节标题 L7–L474；含"阶段 4 B2-A7…A10 首次验收未通过退回"、"阶段 6B REAL-PLATESOLVE-ALL 判定 PLATESOLVE_ALL_FAIL（发现 P0 平台阻断 RESCUE-FD-05）"等条目；L152-L285 各节直接落提交号 a6449610/01eaba36/1012fa41/344e0c9c/bc059562/66906609/b32246c4/9a3b5a7d）。
- **有账无据（4 项）**：LINUX-FREEZE（全仓无 `LINUX_SCIENCE_BASELINE*` 文件、无 `artifacts/release-rescue/` 目录）、WIN-PORT-VERIFY（无目录）、DOC-CLOSE（无专设证据目录；2 处提交命中系正文引用）、FINAL-AUDIT（无本包 `<10MB` 审核包 zip 产物）。此 4 项对应 TASK_LIST 阶段 8–11，取证时点未见完成留痕；后续是否仍计划执行：未证实（无 HANDOFF.md）。
- **有据无账（49 条目）**：`fd-*` 验证树 29 个（fd-p2…p21 系列、fd-b1/b7/backend/f13/fd08b/hdrcheck/rcf-check/utcli 等，对应前台登记 finding `RESCUE-FD-01..08`，编号文本首见 `ruling/REPAIR_PLAN.md` L180"登记 RESCUE-FD-01（前台新发现…）"、L283 FD-02）；`clean-check*` ×4、`fix-backend`、`fix-platform`、`fix-utcli`、`frame-async`、`perf-probe`、`perf-study`、`real-chain-fix`、`real-platesolve-all-r2_smoke`、`routing-fix`、`rqs-fix`、`science-phot`、`drizzle-study`、日志 3 件、`FRONT_DESK_VERIFY.md`。其中 RQS 审计线（07eb229b…b67c32fa，09-14 13:29 起）提交主题为 `docs(audit/RQS)`，不在包 18 任务号之列，归属（本包延伸或另立工作轴）在本包文本内**未证实**。
- 包内字面 vs 仓库现实：`ci/checks.json` 存在（HEAD 实测 132 checks 全部含 `waivable` 字段，waivable=true 7 / false 125）——FIX-CI L3 点名禁止"waivable"滥用、FIX-E2E L7 要求新测试进注册表；注册表提交实证（`91024e7f` 注册 CTEST-PHASE2-GATES、`1fdfb0b5` 注册 CTEST-DRIZZLE-PRECISION-DEFAULT、`73684105` 登记 13 个 ctest 目标；09-12 以来 checks.json 共 16 枚提交）。`ci/known_failures.json` 存在（schema `astrocs.known-failures-baseline/v2`，purpose 字段引用 05_FINDINGS_REGISTER §STD-F9 即对齐包产物；机器化提交 `778fe98e` 09-12 13:42 早于本包到达）——救援期内基线只减不增：`f5f944a5` 时点 failures=2/removals=0 → HEAD 时点 failures=1/removals=1，期间唯一变更提交为删除（`ccc7a933` 09-14 12:31"removal_condition 已满足 → 只减不增"），与 FIX-E2E L7 / 00 L25 禁止条款同向。`evidence/` 下无本包目录（现仅 ciqa、refactor、v6_1_rework、v8_1_ci_control 四子目录）——与包自述写入范围一致（TASK_LIST L30：审计只写 `run/release-rescue/`，真实数据只写 `run/` 与 `artifacts/`；LINUX-FREEZE L5 规定基线归 `artifacts/release-rescue/linux-baseline/`，该路径取证时点未建立）。

## 八、缺口与不确定

1. 两形态均无 git 事件 → "首次/最后出现提交、删除提交"为空；时间口径退化为 zip 内部时间戳（2026-09-12 08:19–09:50）、zip mtime（09-12 22:38）、解包件目录 mtime（09-13 13:06）。（依据：`git log --all -- <两路径>` 空；`pack_inventory.csv` L41–42 提交列空。）
2. 包内与 zip 均无 SHA256SUMS/manifest → 解包件完整性无包内自证，只能依赖本报告第一节所述 zip↔worktree 逐字节比对（结果 24/24 一致）。
3. digest 引用的 00_READ_FIRST 文本在"4. 前台不亲自实施源码任务；只负"处截断（digest 文件第 57 行为末行）；包本体该行完整（00 L35-L36）。判定为 digest 采集截断，非内容不一致。
4. `pack_commit_link.csv` 无行成因无法区分"LEDGER_RE 采集盲区"与"无台账"——本组两者同时成立（无台账文件 + 未入库；且该正则亦不认无前缀 `TASK_LEDGER.csv`），口径见第七节。
5. "V3"编号来源：全仓（lineage/inventory/_control_packs/history）无任何 RELEASE_RESCUE V1/V2 实例或提及；包体标题含"工作包 V3"但未说明 V1/V2 所指——未证实。
6. `RESCUE-FD` 的 FD 语义包内与 REPAIR_PLAN 均未定义（仅"前台新发现"注记，REPAIR_PLAN.md L180）——缩写展开未证实。
7. `run/release-rescue/HANDOFF.md`（00 L37 指定的跨会话载体）不存在；替代件 FRONT_DESK_VERIFY.md 非包内规定文件——包规定交接机制与现场留痕形态不一致；留痕截止时点后的执行计划未证实。
8. CI-LINUX-EXACT、REAL-PLATESOLVE-ALL(-r3) 任务书规定的 `REPORT.md` 未在对应目录发现（存在 run.json/artifact/ATTEMPTS.jsonl 等原始证据）；REAL-MOSAIC-E2E 只有 PREP_REPORT/SUMMARY（其 SUMMARY.md 头部：生成 2026-09-14T18:32:21Z、run_id correct-nside-subset2），无最终 REPORT 与 `M42_LEDGER`/`GALACTIC_CENTER_LEDGER` 定稿（REAL-MOSAIC-E2E.md L13 规定件名）——是否在他处补写：未证实（全仓 find 未见）。
9. 09-14 之后 RQS 审计线、`b67c32fa` 等 HEAD 邻近提交与本包任务图的归属关系：包内无对应任务号，ACTIVITY_STATE.md 亦未增补——未证实，不作归并。
10. 两形态 untracked 为取证时点事实；本报告不对采集后工作区状态负责。

## 尾段一：与上一组（G13）的衔接

- 上一组最后身份（本体系口径）为 `AstroCS_AUDIT_REVIEWPACK_20260909T133841Z`（G99 并入 G13；存形态仅 1 个 zip：`artifacts/AstroCS_AUDIT_REVIEWPACK_20260909T133841Z.zip`，14556 文件，内含 V5 预发布包台账 98 行，见其 digest）。时间先后：审核包戳 2026-09-09T13:38:41Z，早于本包 zip 生成（zip 内部时间戳 09-12 08:19–09:50）与落盘（09-12 22:38）；本包是本报告体系内最新一代 zip 派发件。
- "审核包→救援包"文本痕迹：本包内"审核"字样 5 处，全部指向 FINAL-AUDIT 自己的输出物或人工视觉审核（TASK_LIST L24、FINAL-AUDIT L1/L5、02 L26、REAL-MOSAIC-E2E L11），**未出现"响应某审核失败结果"的自述**——该叙事未证实。本包自述动机指向"重构后恢复构建/E2E/CI 并科学重审"（00 §唯一目标 L9-L15）。
- 实际衔接对象是 G13 的宪章对齐包而非审核包，三重证据：①本包 00 L5"输入状态提示：`f5f944a5…`"——该提交即对齐包 08 号交接件的入库提交（message：docs(GOV/rev6.2): 新增 08 号前台交接说明，2026-09-12 18:52 +0800；本包 zip 落盘于其后约 3 小时 46 分）；②本包 00 L3"不依赖 CPRun、CP 插件"与 `08_FRONTDESK_HANDOVER_20260912.md` §1 负责人指令「首先，禁用调 CP 框架…前台负责合并」文本平行；③废止方向登记在 `工程控制/ACTIVITY_STATE.md` §2（对齐包行注"2026-09-13 被 RESCUE-V3 取代；…本包不得继续派工或据此续作"，登记提交 `5a250999`），反向 grep 对齐包全目录 0 处提及 RESCUE/救援（对齐包早于本包）。另 `f5f944a5..HEAD` 共 196 枚提交构成两包之间的执行带。

## 尾段二：重叠身份

本身份在 `dispatch_groups.json` 全部组表（G01–G99）中仅命中 G14 一组，无 G05/G06 式双记情形；`pack_inventory.csv`/`pack_lineage_table.md` 中亦无同名跨形态多余实例（仅 worktree+zip 两件）。与本包名含相同 `V3` 字样的 `AstroCS_MAIN_PRERELEASE_REAUDIT_CONTROL_V3_20260827`（G05/G06，REAUDIT 系）系不同身份、不同日期、不同 sha，不构成重叠。本包两形态间无 digest 分歧（digest 记 2 实例，与实测一致；唯一差异为其 00_READ_FIRST 引文截断，见第八节第 3 条）。
