# P-G06 · V4 CPU_ADAPTIVE 预发布控制包 取证报告

- 组：G06（V4 CPU_ADAPTIVE 预发布控制包）
- 覆盖身份：AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828、AstroCS_MAIN_PRERELEASE_REAUDIT_CONTROL_V3_20260827、CONTROL_V4
- 依据 digest：`设计大纲/_evidence/packs/digest/AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828.md`（下称 V4-digest）、`…/AstroCS_MAIN_PRERELEASE_REAUDIT_CONTROL_V3_20260827.md`（V3-digest）、`…/CONTROL_V4.md`（C4-digest）
- 取证日期方法：digest + 包内本体文件 + git log/show + python zipfile 只读；zip 一律未解压入仓库。所有哈希为本次实测（sha256）。

---

## 一、组内包清单与身份归并

### 1.1 AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828（3 个存形态实例）

| 形态 | 路径 | 文件数 | zip sha256 | 首次出现 | 最后出现 | 删除 |
|---|---|---|---|---|---|---|
| zip | `工程控制/_control_packs/AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828.zip`（下称 V4.zip） | 26（未压缩 47,997 B） | `f41aacec3d85cf5ac205b78472e8657a0bbbb8b1e41cc511c904311da139ce0a`（实测与 V4-digest、`pack_inventory.csv` 第 3 数据行一致） | 无 git 事件（zip 未入库：`git ls-files 工程控制/_control_packs/` 空输出） | 同左 | 不适用 |
| 解包件（git 史） | `工程控制/CONTROL_V4/AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828/`（当前树已不存在） | 26 | — | `b12305ed` 2026-08-28T20:54（提交消息自述"用户V4包(zip sha256 f41aacec3d85…)解包为对照文档"） | `b12305ed` | 被删除：`b7b2dea7` 2026-09-02T22:15（"GOV-002 归档非当前工程文档"，`git log --diff-filter=D -- 工程控制/CONTROL_V4/*` 命中） |
| 归档（worktree） | `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/CONTROL_V4/AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828/`（下称 V4.arc） | 26（47,997 B） | — | `b7b2dea7` 2026-09-02T22:15 | 同左（`b7b2dea7`） | 未见删除提交（现存） |
| 仅历史复原 | `设计大纲/_evidence/packs/history/b12305ed/工程控制/CONTROL_V4/AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828/`（下称 V4.hist） | 27（48,409 B＝26 本体＋内层 `_PROVENANCE.json` 412 B） | — | 复原件不在 git；`_PROVENANCE.json` 记 first=last=`b12305ed0df13318f9b0369d1d87e8c2e63edd28`、recovered_from_rev 同、delete_commits=[] | 同左 | 不适用 |

**形态间文件集合一致性（实测逐文件 sha256 对比）**：V4.zip、V4.arc、V4.hist 三形态 26 个本体文件集合完全一致、内容零差异（zip-vs-arc diff=NONE；arc-vs-hist diff=NONE）；V4.hist 仅多 1 个复原件附加的 `_PROVENANCE.json`。台账三形态均 54 行、状态全 `NOT_STARTED`（V4-digest 三实例条目一致，实测吻合）。

### 1.2 AstroCS_MAIN_PRERELEASE_REAUDIT_CONTROL_V3_20260827（1 个存形态实例）

| 形态 | 路径 | 文件数 | sha256 | git | 删除 |
|---|---|---|---|---|---|
| zip | `工程控制/_control_packs/AstroCS_MAIN_PRERELEASE_REAUDIT_CONTROL_V3_20260827.zip`（V3.zip） | 21（未压缩 51,229 B） | `fcd96d697a58e13c16bc00045185a94ee9b3ee76b045de744100e3c8fa6e50e2`（实测与 V3-digest、`pack_inventory.csv` 第 4 数据行一致） | 无 git 事件（未入库） | 不适用 |

仅历史复原形态：无（V3-digest 实例数=1）。同谱系解包件以别的身份存在（`工程控制/REAUDIT_V3` 三影子树，归 G05），其规格文本与 V3.zip 相同而台账填写不同，见 §七 与 §八。

### 1.3 CONTROL_V4（1 个形态实例，归档容器目录——本组重点核对项）

| 形态 | 路径 | digest 文件数/字节 | 实测 | 首次=最后 | 删除 |
|---|---|---|---|---|---|
| 仅历史复原（外层目录） | `设计大纲/_evidence/packs/history/b12305ed/工程控制/CONTROL_V4/`（C4.hist） | 27 文件 / 48,306 B（C4-digest 与 `pack_inventory.csv` 第 5 数据行） | 递归 find 实测 28 个文件 = 26 本体 + 内层 `_PROVENANCE.json`(412 B) + 外层 `_PROVENANCE.json`(309 B)；48,306 = 47,997 + 309，即外层计数口径为"26 本体＋外层 provenance、以内层身份实例另记" | `_PROVENANCE.json` 记 first=last=`b12305ed` | 原 `工程控制/CONTROL_V4` 路径被 `b7b2dea7` 删除/移入归档容器（同 §1.1） |

**CONTROL_V4 与 V4 zip/解包件的形态对应（结论）**：CONTROL_V4 不是一份独立内容的包，而是 V4 包在 git 历史中的**外层容器目录名**（`工程控制/CONTROL_V4/<包名>/`）。证据链：① C4.hist 目录树实测只含 V4 包目录与两个 provenance（find 结果）；② 其台账路径即 `AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828/02_TASK_LEDGER.csv`（C4-digest 第 6 行）；③ `pack_commit_link.csv` 中 `CONTROL_V4` 行与 `AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828` 行数值完全相同（54/52/96.3/232，未命中同为 BASE-002、REV-004）；④ 26 个本体文件与 V4.zip 逐文件 sha256 一致（§1.1）。归一脚本把"外层容器目录"与"内层包目录"拆成了两个身份；两者存形态均为"仅历史复原＋归档"，无独立 zip 原件。

**是否被删除**：V4 包目录以 `工程控制/CONTROL_V4` 名义在 `b7b2dea7`（2026-09-02）从原位置删除并移入归档容器；归档容器副本（V4.arc）与复原件（V4.hist）现存；zip 原件因从未入库而无删除提交可考。

---

## 二、包内文件地图

### V4 包（26 文件：12 规格 md + 1 台账 csv + 3 scripts + 10 templates）

规格文件（自述标题，逐一直引包内 `# ` 行；路径基准 = V4.arc）：
- `00_READ_FIRST.md`：「# AstroCS V4：CPU 自适应预发布控制」——目标、本包优先级（替代 V3）、硬规则 12 条、启动顺序、审核责任、唯一允许状态。
- `01_SCOPE_AND_WORKFLOW.md`：「# 范围与连续工作流」——取消清单/保留清单/8 步连续流程/异步外部审核。
- `02_TASK_LEDGER.csv`：54 任务台账（见 §三）。
- `03_TASK_DETAILS.md`：「# Task 固定执行要求」——按 BASE/SCI/ALG/DOC/REV/QA/CPU/MON/PAR/SYN/LINUX/WINDOWS/RELEASE 分组逐任务规定。
- `04_CPU_AUTOTUNE_SPEC.md`：「# CPU 拓扑、指令集与自动 benchmark 规范」——§1 不得硬编码、§2 可用 CPU 数、§3 ISA 安全注册、§4 每 kernel benchmark、§5 内存基准、§6 缓存、§7 启动开销。
- `05_RESOURCE_MONITOR_SPEC.md`：「# 重计算资源监控规范」——§1 统一入口 `tools/perf/run_profile.py`、§2 采样（默认 500 ms）、§3 stage 标记、§4 自动判定、§5 内存健康、§6 结果。
- `06_SYNTHETIC_VALIDATION_MATRIX.md`：「# 文档驱动的合成验证矩阵」——固定目录 `tools/validation/<group>/{contract.json,generate.py,oracle.py,run.py,README.md}`、SYN-001..006 内容、PASS 六条件。
- `07_DOC_CODE_CONSISTENCY.md`：「# 科学、算法、架构、接口、代码一致性」——单一链、机器真相源（`docs/contracts/*.json` 四件）、必查不一致 8 条。
- `08_LINUX_WINDOWS_POLICY.md`：「# Linux 控制节点与 Fatduck Windows 节点」——两节点职责/限制/在线策略（探测 timeout 30 秒、每 30 分钟最多一次）。
- `09_GIT_AND_AUDIT_PACKAGE.md`：「# Git 与最终审核包」——Git 5 条＋审核包必含/禁含清单。
- `10_AGENTS_MD_REQUIRED_BLOCK.md`：「# AGENTS.md 必须加入的精简块」——给出须并入根 AGENTS.md 的固定短文。
- `11_V3_INHERITANCE_AND_INVALIDATION.md`：「# V3 成果继承与失效表」——允许继承代码不继承 PASS（6 类）/直接失效（9 类，点名 CON/CHK/SCI/WIN/RUN 号）/V4 发布阻塞项（8 类）。
- `12_REVIEW_CAPSULE_AND_EXTERNAL_AUDIT.md`：「# 每 commit 审查胶囊与外部科学审核」——§1 胶囊目录与组成、§2 最新源码提交、§3 REV-001 科学审查包与 SCIENCE_CLAIMS 字段、§4 代码抽查（100%/确定性抽样 20%）、§5 不阻塞原则。

scripts/（3 件）：`validate_control.py`（包完整性机器校验）、`validate_final_package.py`（最终审核包校验）、`package_final.py`（打包器，MANIFEST schema `AstroCS-V4-final-audit`）。templates/（10 件，全部 *.csv、单行表头）：BUILD_RESULTS、COMMITS、CPU_PROFILE_RESULTS、FINDINGS、LARGE_ARTIFACTS、RESOURCE_RESULTS、REVIEW_CAPSULE_INDEX、SCIENCE_CLAIMS、TEST_RESULTS、TRACEABILITY。命名口径＝"记录类型全大写 .csv"；无 schemas/、无 scripts 外的 .py/.sh 目录层。

### V3 包（21 文件：9 规格 md + 1 台账 csv + 3 scripts + 9 templates）

- `00_READ_FIRST.md`：「# AstroCS MAIN 预发布重审控制 V3」——唯一目标/不可变规则 12 条/执行入口 5 步/当前证据起点（V2 审核时 main=`535e738796…`、历史接缝锚 A=`b38b446e6`、B=`83471979a`、C/D）/立即停止条件 10 条。
- `01_WORKFLOW_AND_GATES.md`：「# 工作流与强制关卡」——G0..G8 关卡表（每 Gate 是停止点）＋串并行统一规则 6 条。
- `02_TASK_LEDGER.csv`：62 任务台账（见 §三）。
- `03_TASK_SPECIFICATIONS.md`：「# Task 详细规范」——A..G 七组逐任务（ID/CON/SCI/ALG/ORA/ARCH/API/DOC/CHK/BLD/TST/WIN/ACR/RUN/SEAM/HIPS/REL/PKG）。
- `04_CHECKPOINT_CHECKLISTS.md`：「# Checkpoint 清单」——CP0..CP8 勾选块（前言行："没有证据的勾选等于 FAIL"）。
- `05_NUMERICAL_AND_PERFORMANCE_GATES.md`：「# 数值与性能固定门禁」——§1 容差管理、§2 并行性能、§3 停止规则。
- `06_GIT_MAIN_ONLY.md`：「# Git：仅 main、原子提交、立即 push」——每 Task 固定 10 步流程、禁止 7 条、历史锚。
- `07_AUDIT_PACKAGE_SPEC.md`：「# 审核包白名单规范」——体积/必含/禁含/大产物引用/固定打包命令/一致性。
- `08_V2_INHERITED_FINDINGS.md`：「# V2 已确认问题基线」——P0-01..P3-03 表 29 行映射到 V3 任务号＋额外 V3 审核问题 6 条。

scripts/（3 件）：`validate_control.py`、`validate_audit_package.py`、`package_audit.py`。templates/（9 件）：BUILD_RESULTS.csv、CHECKPOINT_RESULTS.csv、COMMITS.csv、FINDINGS.csv、LARGE_ARTIFACT_MANIFEST.csv、PERF_RESULTS.csv、TEST_RESULTS.csv、TRACEABILITY.csv + `SUMMARY.schema.json`（唯一 JSON schema，混置于 templates/，两代包均无独立 schemas/ 目录）。

---

## 三、任务结构

### V4（`02_TASK_LEDGER.csv` 实测全 54 行）
- 表头：`task_id,phase,depends_on,scope,required_commit,required_push,preferred_host,status`（与 V4-digest zip 条目一致）。
- 任务号族 14 个：BASE 2、GOV 1、SCI 6、ALG 5、DOC 2、REV 4、QA 3、CPU 4、MON 2、PAR 7、SYN 6、LNX 4、WIN 6、REL 2（合计 54）。
- phase 值 13 种：BASE/SCI/ALG/DOC/REVIEW/QA/CPU/MON/PAR/SYN/LINUX/WINDOWS/RELEASE（REV→REVIEW、LNX→LINUX、WIN→WINDOWS）。
- 状态字面量分布：`NOT_STARTED` 54/54（zip、V4.arc、V4.hist 三形态一致）。
- 依赖：`depends_on` 支持 `|` 多前置（如 PAR-001 依赖 `CPU-004|MON-002`、REL-001 依赖 `WIN-006|REV-001|REV-002|REV-003|REV-004`）；`scripts/validate_control.py` 强制"forward dependency"禁止——依赖必须指向台账更靠前的行（行序即拓扑序）。
- 顺序声明：`00_READ_FIRST.md` 启动顺序第 3 条"执行 02_TASK_LEDGER.csv，依赖满足后连续推进"；`01_SCOPE_AND_WORKFLOW.md` 连续流程 8 步。
- 提交要求列：`required_commit/required_push` 成对，yes,yes 40 行、no,no 14 行；`preferred_host`＝Linux 48、Windows 6（仅 WIN-001..006）。

### V3（V3.zip `02_TASK_LEDGER.csv` 实测全 62 行）
- 表头：`task_id,gate,depends_on,scope,required_commit,required_push,checkpoint,status`（与 V3-digest 一致）。
- 任务号族 18 个：ID 3、CON 10、SCI 5、ALG 4、ORA 1、ARCH 2、API 5、DOC 1、CHK 6、BLD 4、TST 5、WIN 2、ACR 3、RUN 6、SEAM 1、HIPS 1、REL 2、PKG 1（合计 62）。
- 状态（zip 干净版）：`NOT_STARTED` 62/62；gate 列 G0..G8；checkpoint 列分布 CP0 3、CP1 3、CP2 7、CP3 10、CP4 14、CP5 9、CP6 5、CP7 8、CP8 3。
- 依赖：`depends_on` 可指向任务号或 CP 门名（V3 `validate_control.py`：known = 任务号 ∪ CP0..CP8；单前置，未见 `|` 用法，无 V4 的前向依赖检查）。
- 顺序声明：`00_READ_FIRST.md` 执行入口第 3 条"逐行执行 02_TASK_LEDGER.csv，不得跳号"。

---

## 四、门禁与验收要求

### V4
**机器门（包内自带脚本）**
1. `scripts/validate_control.py`：REQUIRED 清单 24 项（12 md+台账+全部 templates+全部 scripts）存在且非空；台账字段 8 元组精确匹配；task_id 去重；状态 ∈ 7 词集；commit/push 标志 ∈ {yes,no}；依赖必须存在且**不得前向引用**；被取消任务号（RUN-003..006、ACR-001..003）出现在台账即报错；通过时输出整包 sha256 摘要（`CONTROL_PASS tasks=… files=… sha256=…`）。启动前置：`00_READ_FIRST.md` 启动顺序第 1 条。
2. `scripts/validate_final_package.py`：审核包 REQUIRED 15 件、单文件 ≤5 MiB（MAX_FILE）、总量 ≤25 MiB（MAX_TOTAL）、FORBIDDEN_DIRS 7 项（.git/build/cache/tmp/hips 等）、FORBIDDEN_SUFFIXES 17 种（.fits/.dll/.exe/.pdb/.tar…）。
3. `scripts/package_final.py`：固定打包器，生成 MANIFEST.json（schema `AstroCS-V4-final-audit`）与 SHA256SUMS，zip 上限 25 MiB。
**完成顺序与状态机**：7 状态 `NOT_STARTED/IN_PROGRESS/PASS/FAIL/BLOCKED/DEFERRED/REVIEW_PENDING`（`00_READ_FIRST.md` "唯一允许状态"节）；DEFERRED 仅用于被排除的 ACR GPU/Mixed 与历史任务；`01_SCOPE_AND_WORKFLOW.md` "中间任务通过机器门禁后自动继续"、Windows 离线置 BLOCKED 继续 Linux、每 30 分钟最多探测一次；末态输出 `AWAITING_EXTERNAL_RELEASE_REVIEW`（`00_READ_FIRST.md` 启动顺序第 5 条、`03_TASK_DETAILS.md` REL-002）。
**waiver/豁免**：包内**无任何豁免通道**——`00_READ_FIRST.md` 启动顺序第 4 条"不得用豁免把 FAIL 改 PASS"；`01_SCOPE_AND_WORKFLOW.md` 取消节"用『环境性失败』『影响不大』把 FAIL 改 PASS"列入取消；`11_V3_INHERITANCE_AND_INVALIDATION.md` 失效节点名"CON-010豁免：利用率门禁失败不能豁免"。
**性能/资源硬门**：`05_RESOURCE_MONITOR_SPEC.md` §4——compute 段 >5 s 时 `max_threads>=2`、平均使用核数 ≥ `0.80 × min(selected_workers, available_cpus)`、1-worker 对 autotuned 必须有实测吞吐提升、CPU 低且非 I/O/带宽瓶颈直接 FAIL；§5 内存趋势回归单调增长必须 FAIL。`03_TASK_DETAILS.md` QA-003——9 类 mutation 全部非零退出，任何漏报即 FAIL。`06_SYNTHETIC_VALIDATION_MATRIX.md` "PASS"节——Oracle 独立、图层齐全、无事后容差修改等 6 条件。
**审核包与提交**：`09_GIT_AND_AUDIT_PACKAGE.md`——审核包仅最终生成一次、目标 10 MiB/硬上限 25 MiB/单文件 5 MiB、必含 8 组/禁含 6 组清单；每个 required_commit=yes 任务恰一个 commit、push 后验证 `HEAD==origin/main`。`12_REVIEW_CAPSULE_AND_EXTERNAL_AUDIT.md`——每 commit 生成胶囊（`run/review_capsules/<task_id>_<commit12>/`，目标 5 MiB 上限 10 MiB）；REV-001 科学审查包字段与引用要求；外部审核人给 `ACCEPT/CORRECT/USER_DECISION_REQUIRED`，"Agent 不得替外部审核人填写 review_status"（§3 末行）；§4 确定性抽样 20%、抽样发现系统性问题扩大到同类 100%。

### V3
**机器门**：`scripts/validate_control.py`——REQUIRED 20 项、台账字段 8 元组、task_id 去重、状态 ∈ 5 词集、`checkpoint` 必须匹配正则 CP[0-8]、依赖 ∈ 任务号∪CP0..8；`scripts/validate_audit_package.py` + `scripts/package_audit.py`——`07_AUDIT_PACKAGE_SPEC.md` "固定打包命令"节指定为唯一打包通道（"禁止用通配符 zip -r 或人工裁剪替代"）。
**完成顺序（状态机口径）**：`01_WORKFLOW_AND_GATES.md` G0..G8 关卡表＋表后行"每个 Gate 都是停止点。Agent 只能把结果打包并报告 AWAITING_EXTERNAL_REVIEW，不得自行进入下一 Gate"；`00_READ_FIRST.md` 执行入口第 3 条"逐行执行，不得跳号"；状态 5 词（`00_READ_FIRST.md` 不可变规则 7）。
**waiver/豁免**：`00_READ_FIRST.md` 不可变规则 6——"Agent 无权改变任务、阈值、状态词、豁免条件和执行顺序"（豁免条件存在但 Agent 不可触碰）；`08_V2_INHERITED_FINDINGS.md` 前言——V2 问题"不得因 V3 未复现自动关闭"、初始状态一律 FAIL。
**性能门**：`05_NUMERICAL_AND_PERFORMANCE_GATES.md` §2——Linux 2 核 `max_threads>=2`、CPU≥150%、`wall_1T/wall_2T>=1.50`、采样 ≤200 ms、单串行段 <1 s 且累计 <1%；GPU/Mixed 必须真实激活（`fallback_reason` 为空等）；§3 停止规则——首次不达标即停，禁止失败状态启动 32R。
**审核包与提交**：`06_GIT_MAIN_ONLY.md`——每 Task 固定 10 步（fetch→验证 main==origin/main→限定授权文件→测试→`git diff --check`→commit message 格式 `<task_id>: <单一目的中文摘要>`→验证单任务→push→验证→写 COMMITS.csv）；`07_AUDIT_PACKAGE_SPEC.md`——≤25 MiB/≤5 MiB、必含/禁含、大产物十字段 manifest、SUMMARY.json 计数必须由 CSV 机器重算；`04_CHECKPOINT_CHECKLISTS.md`——CP8 末项"Agent 只声明 AWAITING_EXTERNAL_REVIEW"。

---

## 五、设计意图（转述，不评价）

**V4 自述目标**：`00_READ_FIRST.md` 目标节——以当前 origin/main 为唯一开发对象，完成科学定义/算法/架构 API/代码/测试正向统一，纯 CPU 自适应并行，最终只运行一次当前候选 32R 并生成 HiPS 审核产品。`01_SCOPE_AND_WORKFLOW.md` 取消节声明作废：历史 A/B/C 构建与 2R/32R 差分、旧图像作数值 Oracle、ACR GPU/Mixed 接入、CP0–CP8 逐次等待人工批准、Linux 32R 与大规模重复基准。禁止项＝`00_READ_FIRST.md` 硬规则 12 条（main-only、不跑历史、ACR dormant、禁硬编码核心数/ISA、标量参考路径、benchmark 缓存+失效完备、>5 s 必过监控包装器、禁单线程/长期低利用率、Linux/Fatduck 分工、离线不闲等、连续执行、审核包体积）。
**对前代的继承/废止**：`00_READ_FIRST.md` 本包优先级节"本包完全替代 V3"；`11_V3_INHERITANCE_AND_INVALIDATION.md` 允许继承 6 类代码但须在 V4 对应 Task 复验、直接失效 9 类（点名 CON-006/008/009 PASS、CON-010 豁免、CHK-006 PASS、SCI-001/SCI-004 完成声明、WIN-002 PASS、RUN-001..006 及 A/B/C 全部历史运行、历史接缝阈值与历史输出 Oracle——全部对应 V3 台账任务号）。
**负责人/用户裁决痕迹**：包内 = `12_REVIEW_CAPSULE_AND_EXTERNAL_AUDIT.md` §5"只有科学定义存在两个合理但互斥选择时才请求用户"及 `USER_DECISION_REQUIRED` 裁决出口、`00_READ_FIRST.md` 硬规则 11 停止条件。包外 = commit `b12305ed`（2026-08-28）消息："按用户指示移除自造V4重审控制包并归位用户上传的V4_CPU_ADAPTIVE原包…用户V4包(zip sha256 f41aacec3d85…)解包为对照文档,执行账本仍为V5 98任务"——该消息同时记录自造包由前会话 `020cdc99`/`ae643541`（2026-08-28T20:36/20:38）引入、98 任务、"非用户物料"。本组两包内均无 OWNER_BINDINGS、04_OWNER_DECISIONS、07_FRONT_DESK_RULINGS 类文件（全仓 find 检索，仅 V6/V6.1 包与 `问题扫描/40_OWNER_DECISIONS.md` 存在同类文件；后者标题行中的"V4 转达"指问题扫描轮次编号，与本预发布 V4 包无对应物，未证实相关）。

**V3 自述目标**：`00_READ_FIRST.md` 唯一目标节——把 main 收敛到可预发布，使 SCI→ALG→ARCH/API→SRC→TEST 逐项可追溯且一致，消除生产重计算单线程、接缝回归和虚假质量门禁。禁止项＝不可变规则 12 条＋立即停止条件 10 条。`08_V2_INHERITED_FINDINGS.md` 为对 V2 的继承声明（29 项 P0..P3 基线逐条映射 V3 任务号）；V3 自身后来被 V4 的 11_ 文件部分废止（见上）。裁决痕迹＝不可变规则 12"Agent 只能报告证据，最终 PASS/REJECT 由外部审核人裁决"、G0..G8 逐 Gate 外审。

---

## 六、代际差异表（V3.zip → V4.zip，全部为可核对差异）

| 维度 | V3（zip，21 文件） | V4（zip，26 文件） | 差异 |
|---|---|---|---|
| 规格文件 | 9 md | 12 md | 改序改名：01_WORKFLOW_AND_GATES→01_SCOPE_AND_WORKFLOW；03_TASK_SPECIFICATIONS→03_TASK_DETAILS；06_GIT_MAIN_ONLY+07_AUDIT_PACKAGE_SPEC 合并为 09_GIT_AND_AUDIT_PACKAGE；08_V2_INHERITED_FINDINGS→11_V3_INHERITANCE_AND_INVALIDATION（对象代际前移）。删除：04_CHECKPOINT_CHECKLISTS、05_NUMERICAL_AND_PERFORMANCE_GATES。新增：04_CPU_AUTOTUNE_SPEC、05_RESOURCE_MONITOR_SPEC、06_SYNTHETIC_VALIDATION_MATRIX、07_DOC_CODE_CONSISTENCY、08_LINUX_WINDOWS_POLICY、10_AGENTS_MD_REQUIRED_BLOCK、12_REVIEW_CAPSULE_AND_EXTERNAL_AUDIT |
| 任务数/号族 | 62 任务、18 族（含 CON/CHK/ORA/BLD/TST/ACR/RUN/SEAM/HIPS/PKG） | 54 任务、14 族（新增 QA/CPU/MON/PAR/SYN/LNX 独立族） | CON/CHK/ORA/BLD/TST/ACR/RUN/SEAM/HIPS/PKG 族整体消失；`validate_control.py` 明令 RUN-003..006、ACR-001..003 不得再入台账 |
| 台账列 | gate + checkpoint + 无 host | phase + 无 checkpoint + preferred_host | 门（G0-G8）与检查点（CP0-CP8）两列被删除，改为单一 phase 列＋主机列 |
| 依赖语义 | 可引用 CP 门名、单前置 | 仅任务号、`|` 多前置、禁前向引用 | V4 新增 forward-dependency 机器检查 |
| 状态字面量 | 5：NOT_STARTED/IN_PROGRESS/PASS/FAIL/BLOCKED | 7：追加 DEFERRED、REVIEW_PENDING | V3 00_ 规则 7 vs V4 00_ "唯一允许状态"节 |
| 完成顺序 | 每 Gate 停止、等外部 PASS、不得跳号、不得自行跨关 | 依赖满足后连续推进、机器门通过自动继续、异步胶囊 | 逐检查点停工被取消（V4 01_ "取消"节明文） |
| GPU/ACR | CP6 要求 CPU/GPU/Mixed 三路真实激活（04_ CP6 节、05_ §2） | 取消并 dormant；QA-003 mutation "ACR 被重新接入生产"须报错、PAR-007 移除生产路由 | 方向反转 |
| 利用率门 | CPU≥150%、1T/2T≥1.50、串行<1%（05_ §2） | 平均核数≥0.80×min(selected_workers,available_cpus) 等（05_RESOURCE_MONITOR_SPEC §4），串行仍分算 | 阈值表达改为按选定 worker 数的相对带宽 |
| 审核包 | 每 Checkpoint 打包（执行入口 5、CP 清单） | 仅最终一次，目标 10 MiB/上限 25 MiB（09_）；新增每 commit 胶囊（12_） | 频次收敛＋新增胶囊层 |
| scripts | validate_control / validate_audit_package / package_audit | validate_control / validate_final_package / package_final | 打包器换名；包内自校验强化（前向依赖、取消号、整包 sha256 输出） |
| templates | 9 件，含 CHECKPOINT_RESULTS.csv、PERF_RESULTS.csv、LARGE_ARTIFACT_MANIFEST.csv、SUMMARY.schema.json | 10 件，新增 CPU_PROFILE_RESULTS、RESOURCE_RESULTS、REVIEW_CAPSULE_INDEX、SCIENCE_CLAIMS；CHECKPOINT_RESULTS 与 SUMMARY.schema.json 消失；LARGE_ARTIFACT_MANIFEST→LARGE_ARTIFACTS；PERF→RESOURCE+CPU_PROFILE 拆分 | 两代均无 schemas/ 目录；V3 的 JSON schema 混置于 templates/ |
| 体积 | 未压缩 51,229 B | 未压缩 47,997 B | — |

---

## 七、执行留痕三方核对

### V4 身份（ CONTROL_V4 与其台账同源，合并核对）
1. **包内台账**：54 号（`02_TASK_LEDGER.csv`，三形态全部 NOT_STARTED）。
2. **evidence/artifacts 留痕**：`evidence/` 现存仅 ciqa、refactor、v6_1_rework、v8_1_ci_control 四目录，`artifacts/` 仅 AUDIT_REVIEWPACK/ci/graph/KNOWN_FAILURES_BASELINE.json/prerelease_v5——**均无 V4 身份子目录或任务留痕**（实测 ls）。
3. **git 提交消息**：`pack_commit_link.csv` 第 3/5 行——54 号中 52 号在提交消息出现，命中率 96.3%，232 个提交提及，未命中 BASE-002、REV-004。本次抽查复核：`git log --all --grep` 实测 BASE-002=0、REV-004=0（与 CSV 一致）；抽查的 11 个"命中"号逐一看归属，**全部无法归属于 V4 执行**——15720f5e（GOV-001，2026-08-28，消息自述"AGENTS合并V5必需短块"）、e7034892/77d96c35（PAR-002）、af69a9c4（MON-002）、53530408（WIN-005）、962d18c9（REV-002）、8c21173b（LNX-004）均为 2026-08-28/29 的 V5 期提交；af6cb259（CPU-001）、1c7da8de（REL-001）、6a7bdb49（QA-003）为 2026-08-31 提交。原因可核对：V5 台账（`engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/RELEASE_V5/AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828/02_TASK_LEDGER.csv`，98 行）复用 BASE/GOV/DOC/SCI/ALG/REV/MON/PAR/SYN/LNX/WIN/REL 同名号族（awk 族计数：WIN9/SYN9/SCI7/PAR7/ALG7/LNX5/MON4/REL4/REV3/GOV1/DOC1/BASE1），GOV-001 更在宪章期 d8c821db（2026-09-10）复用。96.3% 属同号文本命中，不构成 V4 执行证据；且 `b12305ed` 提交消息明文"执行账本仍为V5 98任务"。
4. **分类**：一致 0；**有账无据 54/54**（台账在、无 evidence 留痕、无归属 V4 的提交；52 号仅有同号歧义命中）；有据无账 0（无"提及号不在 54 号集且属 V4 语境"的提交）。

### V3 身份
1. 台账（zip 干净版）62 号全 NOT_STARTED。
2. `pack_commit_link.csv` **无该 zip 身份行**（grep 实测）——命中率核算由同谱系解包身份行承载：REAUDIT_V3/v3_audit、v3_cp0、v3_reaudit、工程控制/REAUDIT_V3 四行同为 62/55、88.7%、194 提交，未命中 ACR-002、ACR-003、CHK-004、ID-002、ID-003、PKG-001、RUN-006。
3. 解包件留痕（归 G05 管辖，本组仅登记差异）：`V4.arc 同容器/REAUDIT_V3/v3_reaudit` 台账已填写执行状态（PASS 47、FAIL 1、IN_PROGRESS 3、BLOCKED 4、NOT_STARTED 7；CON-010=FAIL），`COMMITS.csv` 含 CON-006 行（commit `da037926…` 前 12 位实测）；`设计大纲/_evidence/packs/history/3703650d/工程控制/REAUDIT_V3/v3_reaudit` 为更早进度（PASS 10、IN_PROGRESS 1、NOT_STARTED 51）。
4. **zip 与解包件文件集合对比（实测逐文件 sha256）**：21 文件中 6 件字节一致（08_V2_INHERITED_FINDINGS.md、scripts/ 3 件、templates/SUMMARY.schema.json）；14 件仅差 zip 侧 1 个尾部空行（00/01/03/04/05/06 md 与 7 件模板，diff 实测各 1 行、内容为空行）；1 件实质差异＝`02_TASK_LEDGER.csv`（zip 62×NOT_STARTED vs 解包填写版，台账 sha256 三方各异：zip `3e14c578…`、history/3703650d `0693c1b0…`、archive `33693b75…`）。规格文本一致，差异全部集中在执行填写状态列——不猜原因，留 §八 第 6 条。

---

## 八、缺口与不确定

1. **digest 摘录截断**：V4-digest 第 69 行（00_READ_FIRST 摘录）止于"但不中断独立任"（实测该行原文 225 字节即断）；V3-digest 末行止于"B：`83471979a`（预定"。本体两文件均完整（zip 内直读核对），截断为 digest 生成侧行为，机制未在证据文件内说明。
2. **CONTROL_V4 计数口径**：C4-digest/inventory 报"27 文件/48,306 B"，实测该目录树含 28 个文件；48,306=47,997+外层 provenance(309 B)，即外层口径以内层实例另记（412 B 内层 provenance 不计入外层）。双记/漏记风险仅系口径，无内容缺失。证据：`设计大纲/_evidence/packs/history/b12305ed/工程控制/CONTROL_V4/_PROVENANCE.json` 与目录 find 实测。
3. **digest git 事件字段口径不明**：V4-digest 恢复实例条 adds_n=2、CONTROL_V4 条 adds_n=6，而 `b12305ed` 对 `工程控制/CONTROL_V4` 实际一次性加入 26 个包文件（`git show --name-status b12305ed`：全提交 A24/D21/R061×1/R099×1）；REAUDIT_V4 的 provenance delete_commits 把同一提交 `b12305ed` 重复列 6 次。adds_n/mod_n 语义未证实。
4. **V3 zip 身份无 commit_link 行**：`pack_commit_link.csv` 不含 `AstroCS_MAIN_PRERELEASE_REAUDIT_CONTROL_V3_20260827`；其提交对接需借道 REAUDIT_V3/* 影子树行，zip↔影子树的身份桥接由台账同构（62 行/同表头）支撑，脚本侧无显式映射记录。
5. **zip 原件均不在 git**：两个 zip 所在 `工程控制/_control_packs/` 无任何 `git ls-files` 记录（实测），digest git 事件 None 与之一致；zip 的"首次/最后出现提交"无解，sha256 为工作区实测值（与 inventory 自述一致）。
6. **V3 台账三形态填写进度并存**：zip 空白版、history/3703650d 早期版（PASS 10）、archive 晚期版（PASS 47）三份台账哈希各异、行数与结构相同；哪份为"权威执行账"包内与 digest 均未标注——未证实。
7. **V4/V3 无 evidence 留痕目录**：`evidence/`、`artifacts/` 实测无对应包目录（§七），无法做"有账无据"之外的第二方核对；V4 的 96.3% 命中率因号族跨代复用（V5 98 任务、V6、宪章期）不能解读为本包执行率——歧义无法在现有证据内消解。
8. **G05 重叠身份的解包形态**（`工程控制/REAUDIT_V3` 及其 v3_audit/v3_cp0/v3_reaudit 影子树）本组未展开逐文件审计，仅做与 zip 的一致性核对；细节以 G05 报告为准。
9. **`问题扫描/40_OWNER_DECISIONS.md` 的"V4 转达"条目**（A-41/A-42/A-43 标题行）与本预发布 V4 包的关系未证实（措辞指问题扫描轮次，非包编号）。

---

## 与上一组（G05）的衔接

本组第一个包 `AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828` 与 G05 最后包 `工程控制/REAUDIT_V3`（G05 digest `工程控制__REAUDIT_V3.md`：恢复形态、62 任务台账、v3_reaudit 状态 PASS 10/IN_PROGRESS 1/NOT_STARTED 51@3703650d）之间是**明文继承＋部分废止**关系，非无直接关系：V4 `00_READ_FIRST.md` "本包优先级"节声明"本包完全替代 V3"；V4 `11_V3_INHERITANCE_AND_INVALIDATION.md` 逐号点名废止 REAUDIT_V3 台账中的 CON-006/008/009 PASS、CON-010 豁免、CHK-006 PASS、SCI-001/SCI-004 完成声明、WIN-002 PASS、RUN-001..006 及历史接缝阈值，同时允许继承其构建入口、worker options、有界队列、checker/Oracle 框架、Fatduck 脚本等 6 类代码并要求在 V4 任务内复验；V4 `01_SCOPE_AND_WORKFLOW.md` "取消"节废止 CP0–CP8 逐次外审制。时间上 V4.zip 出现（b12305ed，2026-08-28T20:54）晚于 REAUDIT_V3 最后触及提交（G05 digest 记 first `6a947f51` 2026-08-28T19:29），谱系方向成立。另一衔接事实：同提交 `b12305ed` 删除自造 REAUDIT_V4（98 任务，`设计大纲/_evidence/packs/history/b12305ed/工程控制/REAUDIT_V4/_PROVENANCE.json` delete_commits 记 b12305ed）并把 V4 定位为"对照文档"、执行账本交 V5——即 V4 在 G05→G06→G07 之间是"上传而未执行"的一代。依据文件：V4 包 00/01/11 三件；G05 digest `工程控制__REAUDIT_V3.md`；commit `b12305ed`、`020cdc99`。

## 重叠身份标注

`AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828` 与 `AstroCS_MAIN_PRERELEASE_REAUDIT_CONTROL_V3_20260827` 两身份同时列入 G05 与本组（`设计大纲/_evidence/packs/dispatch_groups.json` G05 identities 第 2、3 项 vs G06 identities 第 1、2 项）。两组的 digests 数组指向**同一对 digest 文件**（G05 条目第 90-91 行与 G06 条目第 109-110 行为相同路径），故本组 digest 与 G05 天然一致（同一文件，逐字节同源）。本组实测亦未发现与该二 digest 相悖之处（V4 三实例文件数/台账 54 行全 NOT_STARTED、V3 单 zip 实例 21 文件均复核吻合）；唯一的组间分工差异是解包形态（REAUDIT_V3 三影子树、工程控制__REAUDIT_V3）只在 G05 消化，本组仅在 §一/§七/§八 做 zip↔解包件一致性登记。
