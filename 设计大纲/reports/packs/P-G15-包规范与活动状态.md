# P-G15 包规范与活动状态（元控制层）取证报告

- 组别：G15（包规范与活动状态，元控制层），前台追加派工。
- 本组身份 2 项：`工程控制/包规范.md`、`工程控制/ACTIVITY_STATE.md`。两者为非包形控制件，不在 pack_inventory.csv / pack_lineage_table.md / pack_commit_link.csv / digest 体系内（对 `设计大纲/_evidence/packs/pack_commit_link.csv` grep 本组关键词命中 0 行），无组内 digest；断言以本体文件与 git/文件系统实测为准。
- 报告内行号均指对应文件当前工作区版本（`包规范.md` 332 行；`ACTIVITY_STATE.md` 112 行）。提交号一律 8 位。
- 模板本体（cprun-v4-pack-template）全量分析归 G13；救援包（RESCUE-V3）归 G14。本组只做「规范件与模板的一致性核对」（2.3 表、第六节）。

## 一、组内包清单与身份归并

| 身份 | 存形态实例数 | 文件数 | zip sha256 | 首次/最后出现提交 | 是否被删除 |
|---|---|---|---|---|---|
| 工程控制/包规范.md | 仅工作区本体 1（zip 0 / 解包件 0 / 归档 0 / 影子树 0 / 仅历史复原 0） | 1（4453 B） | 无 | 无（git 历史 0 条：`git ls-files --error-unmatch` 无匹配；`git log --all --diff-filter=A -- '*包规范*'` 为空，即从未以任何路径入库） | 否，现存 |
| 工程控制/ACTIVITY_STATE.md | 工作区 tracked 本体 1 + 影子树副本 70（`find run -name ACTIVITY_STATE.md` = 70，全部在 `run/**` gitignore 影子树内） | 1（9572 B） | 无 | 首：`8e03d7da`（2026-09-10 02:22:41 +0800，+95 行新建）；中：`45695873`（2026-09-11 05:05:26，+21/−5）；末：`5a250999`（2026-09-13 13:15:19，+4/−3） | 否，现存且工作区与 HEAD 零 diff |

身份归并与形态说明：
1. `包规范.md`：权限 0644，mtime 2026-09-04 18:51:23，未跟踪也未被忽略（`git check-ignore` 无规则命中）；`git status --porcelain` 显示 `??`。唯一 git 痕迹是提交消息文本：`19ef3722`（2026-09-09 17:52:48）消息称「11控制包zip+审核证据请求+包规范.md→工程控制/_control_packs/与工程控制/」，但该提交对该文件的 diffstat 为空——文件随根目录归位被搬入 `工程控制/` 而始终未入库。其首次/最后出现提交按台账口径记「无」。
2. `ACTIVITY_STATE.md`：git 记录模式 100644（blob `671a747f`），工作区权限实测 0600（`stat`：-rw-------）；权限位收窄未随包入库，属工作区状态，如实记录。文件自称「建立日期：2026-09-09」（L3），git 首次入库 09-10 `8e03d7da`，相差一天（第八节缺口 1）。
3. 同名多形态一致性：ACTIVITY_STATE 的 70 份影子树副本未逐一比对内容（各影子树版本取决于创建时点；第八节缺口 6）；本体与 HEAD 一致已证。包规范无第二形态，不存在形态间差异问题；影子树中 0 份 `包规范.md` 副本为实测观察（其 untracked 身份与之相容，因果未单独证实）。
4. 组内目录背景事实：`工程控制/` 下 tracked 文件共 85 个 = ACTIVITY_STATE.md(1) + 宪章对齐包(74) + cprun-v4 模板(10)（`git ls-files 工程控制` 统计）；V7/V8.1/V6.1/RESCUE 解包件本体均不跟踪，`_control_packs/` 无 tracked 文件。

## 二、文件地图（两规范件内部结构 + 模板槽位一致性核对）

本组身份是单文件规范件，无包内 `NN_XXX.md`/tasks 子结构（tasks/templates/schemas/scripts 计数：两文件均为 0 子目录）。

### 2.1 `包规范.md`（L1 标题「控制包 / Worker 返回包 / 审核包通用格式」；L3 版本 1.0；L4 原则「简单、可读、可机器检查；项目自己的规范可覆盖本文默认项」）

| 节 | 行 | 职责（自述要点） |
|---|---|---|
| §1 控制包 | L8–107 | 回答「做什么、谁做、依赖谁、怎么验收、最后交什么」（L12）；推荐 7 槽位树（L16–25）；「必要内容」逐槽位字段规定（L27–106） |
| §2 Worker 派发包 | L110–137 | 派发 8 字段（L114–123）；`DISPATCH.json` 建议（L125–127）；Worker 六禁令（L131–136） |
| §3 Worker 返回包 | L140–184 | 返回 6 项（L145–151）；TASK_RESULT.json 10 最低字段+5 推荐（L156–177）；PASS 语义=申请前台验收（L179–183） |
| §4 前台集成记录 | L187–212 | 至少存 3 件（L191–195）；六段追溯链（L199–207）；一任务一正式 commit、main 串行（L209–211） |
| §5 审核包 | L215–281 | REVIEW_PACK 推荐树（L224–234）；身份/任务/证据必含组（L238–255）；禁装 7 类（L261–269）；大文件 4 字段（L273–278） |
| §6 最终结论 | L284–299 | 4 枚举（L288–293）；禁自宣 RELEASED（L295–299） |
| §7 三方职责 | L303–319 | 发包者/Worker/前台 Executor/审核者 边界（L305–319） |
| 最小原则 | L323–332 | 六问合格判据（L327–332） |

### 2.2 `ACTIVITY_STATE.md`（L1 自称「工程控制 — 控制包活动状态总表（唯一登记源）」）

| 节 | 行 | 职责 |
|---|---|---|
| §1 状态枚举 | L10–15 | ACTIVE / ARCHIVED_SUPERSEDED / ARCHIVED_DISARMED / REFERENCE_TEMPLATE 四态语义 |
| §2 状态表 | L17–29 | 5 列 × 9 行（列头 L19：包/条目、包 ID、状态、位置、说明） |
| §3 V7 线中断态残留登记 | L31–40 | 4 未跟踪半成品 + 根 CMakeLists 追加块；「只登记、不清理、不提交、不收编」（L33–34） |
| §4 V7 未修候选与挂账去向 | L42–50 | DISP-WCS-007/008、iter_trans_solve 空对 UB、DISP-PSF-007、p1hips make_tmp_dir；挂账 10 项指针 |
| §5 旧台账冻结参照 | L52–66 | 9 文件 SHA-256 冻结指针、BASE-001 复跑核对、F4 口径差 |
| §6 域外工具影响 | L68–91 | finding F1/F4/F2/F3 |
| §7 一致性自检 | L93–111 | 内嵌 python 机器门（ACTIVE 唯一 + 路径存在性，「只读，随验收复跑」L95） |

### 2.3 包规范规定槽位 vs cprun-v4 模板实际槽位（逐条）

模板实测 10 文件（README.md、control-pack.json、TEMPLATE.control-pack.jsonc、evidence-template.json + `tasks/` 6 件），与 G13 口径 digest（`设计大纲/_evidence/packs/digest/cprun-v4-pack-template.md` L6–8：文件 10、tasks/ 共 6）一致。

| 包规范槽位（条款行） | 模板提供 | 判定 |
|---|---|---|
| `00_READ_FIRST.md`（L18、L29–40） | 无同名件；README.md 自述「复制本目录→改 id/workspace→按任务改 tasks/→注册」 | 不一致（入口职责由 README.md+control-pack.json 头部承担） |
| `TASK_LEDGER.csv` 5 最低字段（L19、L42–52） | 无 CSV；任务为 control-pack.json `tasks[]` 数组（digest L9：5 任务含 id/title/spec/lane/review/retry） | 不一致（台账改 JSON 口径，无 commit_subject/wave 列语义） |
| `OWNER_BINDINGS.yaml`（L20、L63–72，L72 允许省略） | 无 owner 文件；lanes 容量与 review 归属内嵌 control-pack.json（digest L9） | 不一致（槽位不存在；规范允许省略，不构成违规） |
| `tasks/`（L21、L74–84） | `tasks/` 6 件：AUDIT-001、CI-TEST、DOC-001、FIX-001、RELEASE-001、_TEMPLATE | 一致（唯一同名命中槽位；`_TEMPLATE.md` 为命名模板） |
| `schemas/`+`validators/`（L22–23、L86–88「推荐提供，但不强制」） | 两目录均无；evidence-template.json 单文件承担证据格式 | 不一致（非强制项） |
| `AUDIT_PACKAGE_SPEC.md`（L24、L97–106） | 无；gates（SOURCE-READY/CI-GATE，digest L9）以 JSON 表达验收 | 不一致 |
| 机器检查四对象 task state/dispatch/task result/audit package（L90–95） | evidence-template.json（result 口径）+ control-pack.json gates | 部分一致（state/dispatch 走框架而非包内文件） |

可核对事实：7 个默认槽位仅 `tasks/` 同名命中；规范 L4 是覆盖条款，模板 README 未援引它声明覆盖关系（未证实）。

## 三、任务结构

1. 包规范的台账结构：最低字段 5（L46–52：task_id / owner_id / depends_on / spec_ref / acceptance），推荐字段 4（L56–61：wave / mutex_lock / resource_class / commit_subject）；派发结构 8 字段（L114–123）；返回结构 10 字段（L156–169）。依赖声明=`depends_on`；顺序声明=六段追溯链（L199–207）与「一任务一正式 commit，main 串行集成」（L211）。两规范件自身任务数均为 0（纯格式/登记件）。
2. ACTIVITY_STATE 的结构：状态字面量集合 4（L12–15）；§2 表 9 行分布 ACTIVE 1 / ARCHIVED_SUPERSEDED 4 / ARCHIVED_DISARMED 1 / REFERENCE_TEMPLATE 3；表头 5 列（L19）。实测复跑其 §7 自检脚本（提取 L97–111 原样执行）输出「ACTIVITY_STATE 自检 PASS：9 行，唯一 ACTIVE = AstroCS 科学重审与全流程恢复工作包 V3」，rc=0。
3. 文内引用任务号族：BASE-001（§5/§6）、GOV-002（§3–§6）、V81-ADOPT-001（§5，提交 `a4fdee3f` 2026-09-05）、DISP-WCS-007/008、DISP-PSF-007、P1-NOISE-IMPL（§3/§4）、「140 任务队列终止于 42/140（41 submit + NOISE-IMPL 沿旧包收尾）」（L24）。
4. 交叉事实：被点名的 V7 `TASK_LEDGER.csv`（解包件）191 行、表头 11 列（task_id/wave/owner_id/kind/depends_on/mutex_lock/resource_class/commit_subject/spec_ref/acceptance/alias_of），无 status 列——42/140 无法由包内台账复算（第七节 7.2#2）。

## 四、门禁与验收要求（硬性规定逐条 + 活动状态件自身门禁）

### 4.1 控制包必备（包规范）
- `00_READ_FIRST.md` 8 项：包 ID/版本、项目名称、起始 baseline SHA、正式分支、本轮目标、冻结约束、启动顺序、最终完成定义（L29–40）。
- `TASK_LEDGER.csv` 最低 5 字段（L42–52）+推荐 4（L54–61）。
- `OWNER_BINDINGS.yaml` 4 项（L63–70），L72「如果项目不需要固定 owner，可以省略」。
- `tasks/` 每任务 7 项：目标/非目标/输入/修改范围/必须执行的动作/验收方法/返回物（L74–84）。
- `schemas/ + validators/` L86–88「推荐提供，但不强制」；机器检查目标四对象 task state / dispatch / task result / audit package（L90–95，措辞为「至少最好能」，建议级）。
- `AUDIT_PACKAGE_SPEC.md` 6 项：审核包目录/必含/排除/源码快照范围/大文件处理/最终结论枚举（L97–106）。

### 4.2 派发、回报与完成顺序
- 派发 8 字段（L112–123），建议 `DISPATCH.json`（L125–127）。
- Worker 六禁令（L129–136）：只改自己范围、不 commit、不 push、不修改总台账、不修改其他 Worker 文件、未执行的测试不能写 PASS。
- 返回包 6 项目录（L144–152）；审计/只读任务可无 change.patch（L154）；TASK_RESULT.json 10 最低字段（L156–169）、推荐含 patch SHA-256 等 5 项（L171–177）。
- Worker PASS 只表示「申请前台验收」，不代表任务正式完成（L179–183）。
- 前台至少保存 TASK_STATE.json / commit_ledger.jsonl / evidence_index（L189–195）；每正式任务可追溯六段链（L197–207）；完成顺序=一任务一正式 commit、main 串行集成（L209–211）。
- 审核包必须包含：身份 5（项目版本/最终 main SHA/baseline SHA/生成时间/控制包 ID，L238–245）、任务 2（最终状态快照、task→commit 映射，L246–249）、证据 3（测试构建验证索引、Gate 或最终验收结果、未解决 findings，L251–255）；禁装 7 类（.git、build/cache、大型原始数据、历史审核包、无关日志、临时文件、凭据，L261–269）；大文件只记 4 字段（L271–280）。
- 审核包提交要求：最终结论枚举 4（READY_FOR_OWNER_REVIEW / NOT_READY / BLOCKED_EXTERNAL / PACKAGE_INVALID，L286–293）；Executor/Worker 不应自行声明 RELEASED，发布裁定归 Owner/外部审核（L295–299）。
- 三方职责边界（L303–319），审核者只依据最终 SHA、任务状态和证据独立判断（L319）。
- 状态机口径：包规范只定义 Worker PASS 语义与结论 4 枚举，无任务级状态机字面量定义（全文核对）。
- waiver/豁免：包规范无 waiver 条款；仅有的放宽通道 = L4 覆盖条款、L72 owner 绑定可省、L88 schemas/validators 非强制。

### 4.3 ACTIVITY_STATE 自身门禁
- §7 机器门（L95「只读，随验收复跑」）：状态表可解析、ACTIVE 恰 1、位置列引用的 `工程控制/…` `engineering/…` 路径逐一 exists()；实测 PASS（9 行 rc=0）。
- §5 冻结参照门（BASE-001 哈希核对，实测见 7.1#5–9）；§6 finding 登记制（无 waiver 机制，域外问题「登记，不顺手修」L68 标题句）。
- 处置禁令：§3 残留「只登记、不清理、不提交、不收编」（L33–34）；§4「本任务不改科学裁决」（L48）。

## 五、设计意图（转述，不评价）

1. 包规范自述目标：为控制包/返回包/审核包提供通用最小格式（L1），原则「简单、可读、可机器检查」并把最终裁量留给项目（L4）；合格判据=六问（L325–332：基线/任务总表/归属/验收/commit 映射/证据位置）。禁止性表述=Worker 六禁令（L131–136）+ 禁自宣 RELEASED（L295–299）。对前代无任何继承/废止声明（文内未提包名）。
2. ACTIVITY_STATE 自述目标：`工程控制/` 与 `engineering/control/` 全部控制包活动状态的「唯一登记源」（L6–8），ACTIVE/ARCHIVED 判定以本表为准；上位约束为冻结宪章（L4–5，引 §1.1 权威分层第 8 条）；与 `docs/DOCUMENT_INDEX.yaml`、`memory.md` 互相引用（L8）。
3. 继承/废止痕迹（本件内）：L3 自署 GOV-002、ASTROCS-CONSTITUTION-ALIGNMENT-V1 期建立；L22 对齐包 09-13 被 RESCUE-V3 取代；L23 V8.1 治理前提被宪章 supersession 替代、其 `01_FROZEN_CONSTRAINTS.md` 上级来源降级 ARCHIVED_NON_NORMATIVE；L24 V7 线 42/140 disarm、旧 cp 运行 `Rmtuajdhv73d430` 已 force 替换废弃；L25 V6.1 交付面由后续轮次与对齐包接管；L26 legacy 归档=GOV-002（SA-GOV-01，2026-09-02）。
4. 负责人裁决/用户指令痕迹：
- L21 原样引 2026-09-13 负责人指令「使用该工程包替代以前的工程包，作为权威并执行」；L27 模板为「owner 指定模板」（入库提交 `4d3feaf4`）。
- 对齐包侧：`04_OWNER_DECISIONS_20260910.md` 标题「负责人裁决与真实数据验收指令」，裁决 3 条（WCS-002 F1 选 B / F-P1001-005 选 A / F-AIO-002 选 A，L6/L17/L23）+ 真实数据验收指令 3 条（L40/L48/L57）+ 管理意见（L28）；文首「Agent 不得修改裁决内容，只能执行或在 BLOCKED_EXTERNAL 中上报」（L4）。
- `07_FRONT_DESK_RULINGS_20260912.md`：自称「前台裁决唯一登记源」，登记原则引负责人指令「能依宪章裁决的一律裁决并留档……才登记为 BLOCKED_EXTERNAL」（L4–6）；`##` 级条目实测 7 条。
- `OWNER_BINDINGS.yaml`（对齐包）：FG-000（前台调度、机器验收、Git与打包）、OWNER（仅项目负责人可执行的冻结/科学裁决）、SA-GOV 等，规则键 no_commit / no_push / respect_write_scope / report_unrun_as_not_verified——与包规范 §1 owner 槽位及 §2 六禁令同构（可核对）。
- L22「历史裁决 R-01..R-31」实际检索于 `05_FINDINGS_REGISTER_20260911.md` 与 `08_FRONTDESK_HANDOVER_20260912.md`（grep R-31 命中）；05 文首登记负责人指令「按国际标准冻结 + 验收已完成部分，P0/1/2 问题记录在册，下一轮控制包一并解决」。
- 救援包侧：`tasks/RESCUE-RULING.md` 要求「合并审计并冻结最小修复序列」「冻结不超过三批源码动作」；`00_READ_FIRST.md` L3 自述不依赖 CPRun/CP 插件/任务图解析器。

## 六、包规范口径 vs 实测历代包结构的符合度抽样

抽样：V7 FINAL3（99 文件）、V8.1（56 文件）、宪章对齐 V1（74 文件）、救援 V3（24 文件）；判据=规范 §1 必要内容（L27–106）+ 派工点名三文件（00_READ_FIRST / MANIFEST / START_PROMPT）。

| 槽位（条款） | V7 FINAL3 | V8.1 | 对齐 V1 | 救援 V3 |
|---|---|---|---|---|
| `00_READ_FIRST.md`（L18/L29） | 有 | 有 | 有 | 有 |
| `TASK_LEDGER.csv`（L19/L42） | 有（同名，191 行无 status 列） | 变体 `CONTROL_TASK_LEDGER.csv` | 有（同名，另 TASK_LEDGER_V1_ARCHIVE.csv） | 变体 `TASK_LIST.md`（无 CSV 台账） |
| `OWNER_BINDINGS.yaml`（L20/L63，可省 L72） | 变体 `05_FIXED_SUBAGENT_BINDINGS.yaml` | 同左变体名 | 有（同名） | 无 |
| `tasks/`（L21/L74） | 有（13 件） | 有（4 件） | 有（57 件） | 有（18 件） |
| `schemas/`+`validators/`（L22/L86 非强制） | 有（9+18 件） | validators 4 件、schemas 无 | 有（1+2 件） | 均无 |
| `AUDIT_PACKAGE_SPEC.md`（L24/L97） | 变体 `18_AUDIT_PACKAGE_SPEC.md` | 变体 `11_EVIDENCE_AND_AUDIT_PACKAGE.md` | 变体 `03_AUDIT_PACKAGE_SPEC.md` | 无 |
| MANIFEST（规范控制包槽位清单无此项） | `CONTROL_MANIFEST.json`+`LARGE_ARTIFACT_MANIFEST.csv` | `CONTROL_MANIFEST.json` | 无 | 无 |
| `START_PROMPT.txt`（规范无此项） | 有 | 有 | 无 | 有 |
| SHA256SUMS（规范仅审核包 L227） | 有 | 有 | 无 | 无 |

可核对差异（MANIFEST/START_PROMPT/SHA256SUMS 非规范控制包槽位，其有无不构成违规项，仅列事实）：
- 必要内容全项命中度：V7=00✓ 台账同名✓ bindings 变名 tasks✓ 审核规格变名；V8.1=00✓ 台账变名 bindings 变名 tasks✓ 审核规格变名 schemas 缺（非强制）；对齐 V1=七槽位全同名的唯一一例；救援 V3=仅 00✓/tasks✓ 两槽命中，台账改 `TASK_LIST.md`，bindings/审核规格缺失（bindings 有 L72 省略条款，`AUDIT_PACKAGE_SPEC` 属必要内容条款，缺失为可核对差异）。
- 命名与路径口径：ledger 名 TASK_LEDGER.csv（V7、对齐）→ CONTROL_TASK_LEDGER.csv（V8.1）→ TASK_LIST.md（救援）；bindings 名 05_FIXED_SUBAGENT_BINDINGS.yaml（V7/V8.1）→ OWNER_BINDINGS.yaml（对齐）→ 无（救援）；NN_ 编号规格件制（V7 24 件、V8.1 16 件、V6.1 15 件）→ 编号+JSON 混合（对齐 9 件 NN_ 前缀 + control-pack.json）→ 无编号 3 件 + TASK_LIST（救援）。
- 状态字面量变化：包规范无任务态定义；V7 线登记口径「未完成任务保留 NOT_STARTED 作历史」（L24）；救援包 TASK_LIST.md 为任务清单非状态台账（本组未展开，G14 口径）。
- 模板（REFERENCE_TEMPLATE，非控制包）：7 槽位仅 tasks/ 同名（2.3 表）。

## 七、三方核对：ACTIVITY_STATE 自述状态 vs 证据现实（本节按派工代换执行留痕口径）

口径：pack_commit_link.csv 无本组行（grep 0），三路核对为手工：(a) 文内点名路径/提交/哈希逐一实测；(b) 框架与仓库证据域（`/workspace/.dsh/control-pack-runs/<run>/`、`run/cp_runs/`、`evidence/`、`engineering/control/`）；(c) git 提交消息中对应编号。digest 体系 LEDGER_RE 只认 `NN_TASK_LEDGER*.csv`：本组涉及的 `TASK_LEDGER.csv`（无 NN 前缀）、`03_REWORK_TASK_LEDGER.csv`（NN 后夹 REWORK）、`CONTROL_TASK_LEDGER.csv` 均落采集盲区（V6.1/V8.1 同病已证），故本节不采用 digest 采集结果、直接文件枚举。

### 7.1 一致（登记↔证据可核，16 项）
1. 唯一 ACTIVE=RESCUE-V3：§7 自检脚本原样复跑 PASS（9 行、唯一 ACTIVE、rc=0）。
2. 基线 `f5f944a5` 存在（09-12 18:52:52，docs(GOV/rev6.2)）；`run/release-rescue/snapshot/STARTUP_SNAPSHOT.json` 的 independent_remote_check refs/heads/main=f5f944a51201… 与 git_describe …gf5f944a5 双源吻合。
3. 阶段 0 RESCUE-SNAPSHOT 冻结：STARTUP_SNAPSHOT.json 存在（schema=astrocs.release-rescue.startup-snapshot/v1，generated_by 注明 RESCUE-SNAPSHOT SubAgent）。
4. RESCUE 解压件目录与 `工程控制/_control_packs/AstroCS_RELEASE_RESCUE_CONTROL_V3_20260912.zip`（29799 B，09-12 22:38）双路径存在。
5. §5「共 9 文件」=框架域 `Rmtucy2cqced995/evidence/BASE-001/freeze_snapshot_r1.json` 的 ledgers 字段实测 9 项，8 位前缀逐吻合：49dab5d1（对齐本包）/ c6f21304（V6.1）/ 32e3e414（V7）/ ee6a5d7e（CONTROL）/ b1ebbf5c（CHECKPOINT）/ 524ab719（baseline）× 解压件组 + 旧 active 镜像组。
6. 「双遍一致」：freeze_snapshot_r2.json ledgers 与 r1 全等（比较 True）。
7. ee6a5d7e 四方一致：解压件工作区、镜像工作区、镜像 git blob、zip 成员（python zipfile 只读）。
8. b1ebbf5c 四方一致（同上方法，含镜像 blob）。
9. 524ab719 = 解压件工作区 = 镜像工作区 = zip 成员；镜像 git blob = 32e3e414（git show HEAD 与 a4fdee3f 旧 active 路径 blob 同值）——F4 两条参照链复现；`tr -d CR` 后 524ab719→32e3e414，`git ls-files --eol` 示 attr=text=auto eol=lf，口径差（工作区 CRLF vs git blob LF）完整闭合。
10. §6 F1：`ci/reconcile_state.py` L37 V71_LEDGER 现值指向 archive 镜像路径；修复提交 `d1ac4dfc`（09-10 02:23:54 fix(GOV-002-F1)）存在；实跑该只读检查器 `--current-first --strict` rc=0、checks_total=9、9/9 PASS；负向注入证据 `…/GOV-002/reconcile_v71_finding.json` 10 checks 中恰 v71_ledger_readable / v71_coverage / v71_no_extra_tasks 三项 fail（与 L73–74 三检查名单吻合，checks 计数注入态 10/正常态 9 的差异未在登记面说明，见第八节缺口）。
11. §6 F3：`evidence/v8_1_ci_control/` tracked 实测 232 = 登记值。
12. §3 残留：`lib/snr_estimator/CMakeLists.txt`、`lib/snr_estimator/include/`、`lib/snr_estimator/src/`、`tests/unit/p1_noise/` 现存且 `??` untracked；freeze_snapshot_r1 tracked_modified n=8 含根 `CMakeLists.txt`，与「预存 8 个 tracked modified 之一」快照时点吻合。
13. §4：`run/local/HANDOFF_CONTROL_PACK_20260909.md` 存在（gitignore 仅现场可读，与 L49–50 自我限定一致）。
14. §2 V8.1 行：`8e03d7da` rename 检测 R100 实测 56 文件；镜像 tracked 57 = 56 移入 + README_ARCHIVED.md。
15. §2 模板行：`4d3feaf4`（09-09 21:27:22）收录、实测文件 10 = digest 10。
16. 点名提交全部存在且为 HEAD 祖先：`f5f944a5` `d1ac4dfc` `45f80776` `a4fdee3f` `8e03d7da` `009ee419`（F2 关联，09-09 20:30:56）逐一 merge-base --is-ancestor 通过；本文件三提交 `8e03d7da`/`45695873`/`5a250999` diffstat +95 / +21−5 / +4−3 与登记动作描述吻合。

### 7.2 有账无据（登记在、仓库内不可独立复核，4 项）
1. L55 `evidence/BASE-001/freeze_snapshot_r1.json` 字面仓库路径不存在（repo `evidence/` 仅 ciqa/refactor/v6_1_rework/v8_1_ci_control）；实体在框架证据域 `/workspace/.dsh/control-pack-runs/Rmtucy2cqced995/evidence/BASE-001/`（`HANDOVER.md` L120 记证据目录约定）。内容可核（7.1#5）但仓库内引用断链——路径口径不一致。
2. V7「140 任务队列终止于 42/140（41 submit + NOISE-IMPL）」：队列 TASK_STATE/CSV/COMMIT_LEDGER 主体不在仓库；框架 `Rmtuajdhv73d430/state.json` nodes=25 但 state 字段为 None 无法复算；包内台账无 status 列。仅有旁证：memory.md item 9 同数、item 8 早前时点 38/140、提交消息复述。数字不可独立复算。
3. L24「旧 cp 运行 `Rmtuajdhv73d430` 已 force 替换废弃」归因：该 run 目录实存（paused=True；repo `run/cp_runs/Rmtuajdhv73d430/BASE-001/staged_evidence/old_ledgers_inventory.json` frozen_at 09-10 00:19+08、base_sha 789c5b6c，8 台账清单与 cy2 冻结 9 项集合不同），但其 package-ref.json 指向对齐包目录而非 V7 包；「V7 线的旧 cp 运行」归因与框架记录不一致，force 替换动作无直接登记。未证实。
4. L22「R-01..R-31」：登记面未注明所在文件；实测检索于 05/08 两文件，04 用「裁决 1/2/3」编号制。可核但指针粒度不足。

### 7.3 有据无账（现实在、登记未载/过时，5 项）
1. `包规范.md` 自身：状态表 L29 登记它为 REFERENCE_TEMPLATE，但其未入库、未列入取证台账体系（inventory/lineage/commit_link/pack_events 全 0 命中）——台账体系对它有据无账。
2. 根 `CMakeLists.txt`：§3 称追加块未提交；实测 09-14 `35c85f53`（science(P8)）/ `57c04256`（perf(P10)）已提交该文件，当前不再 dirty——登记滞后（本件 mtime 09-13 早于两提交）。
3. 对齐包 `TASK_LEDGER.csv` 现值 0ec2d0f5 ≠ §5 登记冻结值 49dab5d1：冻结后台账继续演进，现值变化未回登（§5 语义为「冻结参照」故非错账，但变化无记载面）。
4. 镜像 baseline CSV 工作区现值 524ab719（w/mixed、status clean）：§5/F4 仅写 blob 口径 32e3e414 与解压件口径，未登记镜像工作区现值这一实测态。
5. 救援包 `00_READ_FIRST.md` L5 限定「f5f944a5 为输入状态提示……执行时必须以当前 origin/main 为准」；ACTIVITY_STATE L21 记作「基线 SHA f5f944a5」，未带出包内限定语。

分类计数：一致 16 / 有账无据 4 / 有据无账 5。

## 八、缺口与不确定
1. 建立日期差：ACTIVITY_STATE L3 自称 09-09 建立，git 首入库 09-10 `8e03d7da`（+95 新建）；09-09 是否存在未入库草稿——未证实。
2. 包规范.md 溯源断链：无提交、无 zip、无 digest；仅 mtime 09-04 与 `19ef3722` 消息提及归位两线索；正文版本「1.0」（L3）外修订史不可核；作者未证实。
3. 框架证据域指针：L55 路径按仓库字面不可读（7.2#1）。
4. Rmtuajdhv73d430 归因矛盾：登记为 V7 线运行 vs 框架 package-ref 指对齐包；两 run state.json 均无显式 status/replaced 键（采样键集 version/run_id/revision/paused/agent_id/session_id/nodes/children/reservations/externals/processed_events/event_seq）——未证实（7.2#3）。
5. 42/140 不可复算：队列主体不在仓库；memory item 8（38/140）与 item 9（42/140）为两时点数，本件采 item 9 口径（L24 交接锚自指 item 9）。
6. 70 份影子树副本与本体的一致性未逐一验证。
7. 权限 0600 成因：git mode 100644、工作区 0600；收窄主体/时点无登记——未证实。
8. reconcile 检查数在正常态实测 9、注入态证据文件为 10；两种状态检查项集合差异未在登记面说明（L74「其余检查 PASS」与 L77「9/9」并存，注入文件 checks_passed=7/failed=3/total=10）。
9. 包规范.md 的派发/回报文件族（DISPATCH.json、returns/<TASK_ID>/、TASK_RESULT.json）在仓库文件层面未见按规范实例化的目录留痕（框架证据域以 1.json 形态存在，schema=cprun/evidence-v1，与规范字段集不同构）——规范被历史包实际执行的程度：未证实。
10. digest 与本体：本组无 digest（非包形）；交叉模板 digest 与本体两点实测吻合（10 文件、tasks/ 6、`4d3feaf4`），无不一致项。

## 片尾一：与上一组（G14 发布救援 V3）的衔接
本组第一个身份 `包规范.md`（mtime 2026-09-04，未入库）早于 G14 救援包 zip（09-12 22:38），两者无 git 关系。实质衔接在本组第二身份：`ACTIVITY_STATE.md` 在 `5a250999`（2026-09-13 13:15:19）将 G14 的 RESCUE-V3 登记为唯一 ACTIVE 并把对齐包转 ARCHIVED_SUPERSEDED（依据：ACTIVITY_STATE §2 L21–22；该提交对此文件 +4/−3）。时间线按 git 实录为交叠非线性：包规范 09-04（工作区实物）→ `19ef3722` 09-09 消息提及归位 → ACTIVITY_STATE 建立 09-09/入库 09-10（`8e03d7da`）→ 复跑登记 09-11（`45695873`）→ 08 号交接提交 `f5f944a5` 09-12 18:52 / 救援 zip 09-12 22:38 → 本状态件终版 09-13（`5a250999`，mtime 13:14:24）。救援包自述不依赖 CPRun 框架（其 00_READ_FIRST L3），与包规范 L4 覆盖条款的关系仅记录为文本事实。

## 片尾二：重叠身份
本组两身份非包形、无 zip、无 digest，不存在与本组 digest 的比对。跨组重叠两处：(1) cprun-v4 模板（G13）：ACTIVITY_STATE L27 一行登记与 G13 digest L5–8（worktree 实例、文件 10、first/last=`4d3feaf4`、无删除）逐点一致。(2) 救援 V3 / V8.1 / V7 / V6.1 / 对齐包（G12–G14）：在本组状态表各占一行（L21–25），其行内断言已按第七节三路复核；本组与他们的差异仅为口径——ACTIVITY_STATE 是登记面文件，不属 pack_events.md 的 49 身份三时刻体系（grep 0 命中已核）。

（报告完 — G15）
