# P-G10 取证报告：V7 模块化重筑基控制包（FINAL / FINAL3）

- 组：G10；身份：`AstroCS_ALPHA3_MODULAR_REFOUNDATION_CONTROL_V7_1_20260902_FINAL3`（下称 **ALPHA3/V7.1**）、`AstroCS_V7_MODULAR_REFOUNDATION_CONTROL_20260902_FINAL3`（下称 **V7 解包件**）
- 输入：`设计大纲/_evidence/packs/digest/AstroCS_ALPHA3_MODULAR_REFOUNDATION_CONTROL_V7_1_20260902_FINAL3.md`、`.../digest/AstroCS_V7_MODULAR_REFOUNDATION_CONTROL_20260902_FINAL3.md`；包本体 `工程控制/AstroCS_V7_MODULAR_REFOUNDATION_CONTROL_20260902_FINAL3/`（解包件）与四处同名 zip
- 方法：`python3 zipfile` 只读；`git log/show/cat-file/ls-files/status` 只读；台账与门禁 CSV 用 csv 模块解析；包内 validators 以 `--root` 指向目标目录、`PYTHONDONTWRITEBYTECODE=1`、不带 `--json-out` 运行（zip 副本先解到 `/tmp`，未解压进仓库）
- 全部写入仅限本文件；未修改任何既有文件、未做 git 写操作

---

## 一、组内包清单与身份归并

| 身份 | 存形态实例（digest） | 实测存形态分布 | 文件数 | 字节 | zip sha256（前 8） | 首次出现提交 | 最后出现提交 | 是否删除 |
|---|---|---|---|---|---|---|---|---|
| ALPHA3/V7.1 | 4（3 zip + 1 history_zip 复原） | zip 全库 **77** 份：digest 记 4 份（见下）+ `设计大纲/_evidence/packs/history/a4fdee3f/engineering/control/active/…/baseline/…FINAL3.zip` 复原件 1 份 + `run/` 影子树 72 份；72 份影子副本 sha 与 4 份一致 | 99 | zip 209,978 B（包内成员未压缩合计 398,022 B = 388.7 KiB） | `005448fb`（全 64 位见 digest 首实例行；77 份实测同值） | `a4fdee3f` 2026-09-05（作为 `engineering/control/active/AstroCS_ALPHA0.11.0_EXISTING_WORKSPACE_CI_CONTROL_V8_1_20260905/baseline/` 内文件 A 入档，该提交 61 文件） | `8e03d7da` 2026-09-10（R100 移入 `engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/baseline/`） | 未被删除：全史仅 1 条 A + 1 条 R100 移动，无 `--diff-filter=D` 命中（`git log --all --name-status` 实测） |
| V7 解包件 | 1（worktree） | 1（`工程控制/AstroCS_V7_MODULAR_REFOUNDATION_CONTROL_20260902_FINAL3/`；`find` 实测 `run/` 内无第二份同名目录） | 99 | 397,833 B（388.5 KiB） | 无 zip | **从未入库**：`git log --all -- <目录>` 0 条；`git status --porcelain` = `??`（未跟踪） | 同左（无） | 不存在删除提交 |

ALPHA3/V7.1 的 digest 四实例路径（逐条实测存在且同 sha256 `005448fb`）：
1. `engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/baseline/…FINAL3.zip`（唯一被 git 跟踪的实例，跟踪状态见上）
2. `工程控制/AstroCS_ALPHA0.11.0_EXISTING_WORKSPACE_CI_CONTROL_V8_1_20260905/baseline/…FINAL3.zip`（`git ls-files` 0 → 未跟踪）
3. `工程控制/_control_packs/…FINAL3.zip`（`git ls-files "工程控制/_control_packs/"` = 0 条，整个目录 status `??` → 未跟踪）
4. `设计大纲/_evidence/packs/history_zip/…FINAL3.zip`，其 `.provenance.json` 记 `orig_path_in_repo` = 实例 1 的 **移动前路径**（active 路径）、`recovered_from_rev` = `8e03d7da9feacd9a…`、`sha256` = `005448fb…`、`bytes` = 209978

**两身份 = 同一包内容的两种命名/存形态**。同名多形态文件集合比对（实例 1–4 的 zip 成员名集合 vs 解包件相对路径集合）：
- 四个 zip 之间：双向差 0（`ZIPDIFF onlyA=0 onlyB=0`，实测脚本口径）
- 任一 zip vs 解包件：路径名集合双向差为空（`onlyZip=[]`、`wt-only=[]`）→ **99 个路径完全一致**
- 逐字节比对：**98/99 完全相同**，唯一差异文件 `TASK_LEDGER.csv`：zip 内 40,677 B（含 189 个 CR）、`sha256=524ab719b840…`；解包件内 40,488 B（纯 LF）、`sha256=32e3e414bf7c…`；差 189 B 恰等于 CR 计数；去除 CR 后 unified diff 为 0 行（193 物理行含表头）→ 差异口径 = **行尾符（CRLF vs LF）单文件差异，内容字段无变化**
- 字节差解释链（可核）：`398022 − 397833 = 189`，即 digest 中 zip「388.7 KB」与解包件「388.5 KB」之差全部来自该文件的 189 个 CR
- 关联实证：解包件自带 `SHA256SUMS`（98 条）与 `CONTROL_MANIFEST.json`（97 条）对 `TASK_LEDGER.csv` 均记 `524ab719…`（= zip 形态值），故包内自校验在解包件上失败、在 zip 解包上通过（见 §四）

---

## 二、包内文件地图

99 文件的目录分布（实测 `os.walk`）：根 37、`validators/` 18、`tasks/` 13、`schemas/` 9、`templates/` 9、`checklists/` 6、`package_policy/` 4、`science/` 3。

**编号规格 00–23 共 24 份（00–04、06–24 为 md，05 为 yaml），编号无缺口**。各自自述标题/首段要点（一句话转述，出处 = 该文件首行标题或其 §1）：

| 文件 | 自述职责（标题要点） |
|---|---|
| `00_READ_FIRST.md` | 「先读我」：包 ID、冻结提交 `c169615…`、起点版本 `0.10.0-alpha.2`→目标 `0.11.0-alpha.1`、本轮唯一目标 10 条、执行者第一小时 9 步、权限模型、状态机、结论词义、交付 7 项 |
| `01_OWNER_FROZEN_CONSTRAINTS.md` | 「项目负责人冻结约束」，自称本包最高优先级规范，A–H 八组（产品与阶段 / 平台与发布形态 / ACR 与 CPU / 并行与资源 / 科学与迁移 / 模块接口文档 / Git 执行交付 / 判定权） |
| `02_CURRENT_BASELINE_AUDIT.md` | 「AstroCS V6.1 架构审核与下一阶段设计方向」，状态行自标「设计审查稿（不是控制包）」；§19 最终判定（49,050 B，为包内最大 md） |
| `03_TARGET_PRODUCT_AND_ARCHITECTURE.md` | §1「目标不是大爆炸重写」，strangler 迁移与 DLL 目录架构 |
| `04_FOREGROUND_AGENT_RUNBOOK.md` | 前台 Agent `FG-000` 执行手册，§1–§10（职责/工作区/启动/基线前进/派发/串行集成 13 步/退回/Windows 离线/Gate/收尾） |
| `05_FIXED_SUBAGENT_BINDINGS.yaml` | `policy: fixed_owner_detached_worktree_foreground_integrates`；`foreground` + `owners` 33 项及各自 `write` 路径 glob（自称路径权限事实源，见 `SUBAGENT_DISPATCH_PLAN.md` 首段） |
| `06_TASK_GRAPH_AND_WAVES.md` | 任务图/波次 W0–W8/§2 八条调度规则/§4 依赖链/§5 九条排序 |
| `07_CHECKPOINTS_AND_GATES.md` | G0–G8 检查点与「机器化 Gate 任务集合」表、自动判定词义、waiver 限制 |
| `08_GIT_MAIN_ONLY_INTEGRATION.md` | main-only 原子提交规范（标题格式、集成前检查、push 后三 SHA、禁止动作） |
| `09_WINDOWS_TOOLCHAIN_LOCK.md` | 「Fatduck 的唯一编译依据」，工具链版本冻结（10,103 B） |
| `10_LINUX_CONTROL_NODE.md` | Linux 常在线控制节点定位（约 2c / 2–4 GiB，仅控制+静态+轻编译+小合成） |
| `11_MODULE_SOURCE_TEST_STANDARD.md` | 模块为最小可拥有单元；源码/README/测试/目录标准 |
| `12_DLL_ABI_AND_LOADER_STANDARD.md` | §1「唯一导出入口」；DLL C ABI、注册、所有权与加载 |
| `13_DATA_PIPELINE_AND_ARTIFACT_STANDARD.md` | §1「数据对象而不是路径字符串」（`ArtifactHandle`） |
| `14_RUNTIME_SCHEDULER_AND_TRACE_STANDARD.md` | §1 职责边界：CLI → config compiler → typed DAG |
| `15_CPU_PROVIDER_AND_RESOURCE_STANDARD.md` | §1 Provider 层级（kernel interface → provider table → baseline/avx2/avx512） |
| `16_SCIENCE_DOCUMENT_AND_TRACEABILITY_STANDARD.md` | §1 权威顺序（SCI 为顶）与文档-代码追溯 |
| `17_INDEPENDENT_REVIEWER.md` | 独立审计 `SA-AUD-99` 规范：固定检查组 IR-010…IR-180（18 项，含失败级别）、审计流程 6 步、Finding JSON 格式、结论规则 |
| `18_AUDIT_PACKAGE_SPEC.md` | 审核包与源码快照规范：包名式、固定目录、白名单、排除、证据字段、容量门 v2、归档路径 |
| `19_MACHINE_CHECKS.md` | 机器化一致性检查清单（13 脚本表 + 5 类静态扫描 + 脚本质量 5 条） |
| `20_RELEASE_GATES.md` | 发布门禁：P0–P3 定义、G0–G8 门表、强制科学/性能门、最终 verdict 四值 |
| `21_PRIMARY_REFERENCES.md` | 科学与工程参考（WCS/FITS 两篇 2002、HEALPix 2005、HiPS IVOA、fitsverify、VS/CMake/MSVC 文档链接） |
| `22_FAILURE_ESCALATION.md` | 失败 8 类处置、重试 5 条、升级格式 12 字段 |
| `23_GRAPH_AND_DOC_TOOL_POLICY.md` | §1 静态图与真实运行图必须分开 |

**`tasks/`（13 文件）**：`00_TASK_EXECUTION_CONTRACT.md`（共同执行合同 A–E）+ `01_W0`…`10_AUDIT_PACKAGING` 十份分波次任务规格（命名口径 `NN_<域>_TASKS.md`）+ `MODULE_MIGRATION_TEMPLATE.md`（四任务展开模板）+ `MODULE_MIGRATION_MATRIX.csv`（表头 `prefix,owner_id,phase,module_id,target_dll,legacy_paths,depends_on_int,science_specific_acceptance`，**19 个数据行**）。台账 191 行的 `spec_ref` 全部落在 `tasks/`：01=12、02=15、03=17、04=11、05=7、06=14、07=2、08=14、09=14、10=9、`MODULE_MIGRATION_TEMPLATE.md`=76。

**`templates/` 9**：`AGENTS.md`、`L0_OWNER_REVIEW.md`、`MODULE_README.md`、`REVIEWER_SIGNOFF.md`、`DISPATCH.json`、`EVIDENCE_RECORD.json`、`INDEPENDENT_FINDING.json`、`TASK_RESULT.json`、`TASK_STATE.json`（md/json 两轨，命名全大写）。
**`schemas/` 9**：均为 `<对象>.schema.json`（artifact_manifest、audit_summary、dispatch、evidence_record、finding、gate_result、module_manifest、task_result、task_state）。
**`checklists/` 6**：`00_FOREGROUND_AGENT`…`05_INDEPENDENT_REVIEW`（`NN_<角色>_CHECKLIST.md`）。
**`validators/` 18**：13 个 `validate_*.py` + `selftest.py` + `run_control_checks.py` + `build_control_manifest.py` + `common.py` + `control_plane.py`。
**`package_policy/` 4**：`PACKAGE_POLICY.json`、`SOURCE_ALLOWLIST.txt`、`AUDIT_ALLOWLIST.txt`（6 行根名）、`ARCHIVE_INDEX.csv`（表头 + 2 条模板行，reason 字段写 `superseded by V7`）。
**根级其他**：`CONTROL_MANIFEST.json`（`package_id`=`ASTROCS-ALPHA3-MODULAR-REFOUNDATION-V7.1`、`product_version`=`0.11.0-alpha.1`、`source_reference`=`c1696156583c…`、`files` 97 条）、`SHA256SUMS`（98 条）、`GATE_REQUIREMENTS.csv`、`TASK_LEDGER.csv`、`TASK_LEDGER_README.md`、`START_PROMPT.txt`（187 B，validate_control 强制 ≤100 字符，见 §八）、`VERSION`（`0.11.0-alpha.1`）、`README.md`、`ACTIVE_DOC_INDEX.md`（L0–L3 分层与机器入口）、`SUBAGENT_DISPATCH_PLAN.md`（owner→职责→波次→典型 task 映射表 22 行）、`LUNA_OPTIMIZATION_REPORT.md`（7,386 B，包内唯一未在任何规格/索引中被点名的正文文件）、`toolchain.lock.json`、`LARGE_ARTIFACT_MANIFEST.csv`（3 条逻辑名，required_tasks 引 `WIN-010/P2-REAL-032R` 等）。

---

## 三、任务结构

- **任务数**：`TASK_LEDGER.csv` 数据行 **191**（物理 192 行含表头；zip 与解包件同口径，实测 `csv.DictReader` = 191）
- **台账表头 11 列**：`task_id, wave, owner_id, kind, depends_on, mutex_lock, resource_class, commit_subject, spec_ref, acceptance, alias_of`
- **无 status 列**（digest 记「状态列 None」经实测复现：表头内不含任何 `status` 字段）。口径出处 = `TASK_LEDGER_README.md` 首段：台账是「静态任务定义单源；它不保存动态状态」，状态运行时复制到工作根 `TASK_STATE.json` 并按 `schemas/task_state.schema.json` 校验
- **状态字面量集合（11 值，三处一致）**：`00_READ_FIRST.md` §4（主链 `NOT_STARTED → READY → DISPATCHED → RETURNED → INTEGRATED → CLOSED`，异常 `FAIL/BLOCKED/WAITING_RESOURCE/REBASE_REQUIRED/SUPERSEDED`）= `schemas/task_state.schema.json` 的 `status.enum` = `validators/validate_tasks.py` 的 `STATES`；同时 §4 明令不得创建 `IMPLEMENTED/VERIFYING/ACCEPTED/PUSHED/REWORK_*/BLOCKED_CANDIDATE` 第二套状态。台账内状态分布 = 空（无该列）
- **实际状态分布**（唯一可查的运行态副本，见 §七）：`evidence/v8_1_ci_control/TASK_STATE.json` 对 191 号给 CLOSED 105 / NOT_STARTED 84 / FATDUCK_PENDING 2（`WIN-001`、`WIN-002`），其 `status_enum` 为 6 值（含 `FATDUCK_PENDING`，不属本包 11 值枚举）
- **任务号族 27 个**（按首段前缀计）：P1 43、P2 38、P3 14、WIN 14、CPU 8、QA 8、RT 7、ABI 6、LNX 6、AUD 5、GOV 5、BLD 4、CAT 4、CLI 4、DATA 4、ID 3、IO 3、LOG 3、PKG 3、MON 2，ACR/ARC/CTL/DOC/FG/GATE/TST 各 1。重点号族实测均存在：ABI-001..006、ACR-001、ARC-001、AUD-001..005、BLD-001..004、CAT-GAIA-{DOC,TEST,IMPL,INT}、CLI-001..004、CPU-001..008、CTL-001、DATA-001..004；另含非数字后缀号（`P1-CAL-DOC` 型 84 个模块号 + `P1-REAL-SAMPLE`、`P2-REJ-ACCEPT`、`P2-PERF-DESIGN`、`FG-VERIFY-001`、`GATE-OWNER-001` 等）。任务号无重复（`validate_tasks.py` 唯一性检查亦 PASS）
- **波次分布**：W0 5、W1 35、W2 31、W3 77、W4 2、W5 15、W6 19、W7 1、W8 6
- **kind 分布（9 值）**：implementation 57、evidence 38、module-doc/module-test/module-impl/module-integration 各 21、audit 6、gate 5、control 1
- **owner**：33 个（台账 `owner_id` 去重 = `05_FIXED_SUBAGENT_BINDINGS.yaml` 中 `id:` 去重，双向差均为空集）；单 owner 最大任务量 SA-WIN-30 18、SA-QA-29 13、SA-P1-D18 9
- **resource_class**：LINUX_LIGHT 167、WINDOWS_HEAVY 16、STATIC_READ 8（三值恰为 `validate_tasks.py` 的合法集）
- **依赖与顺序声明（包内确有）**：`depends_on` 191 行全部非空、`;` 分隔，扇出度分布 1→59、2→65、3→16、4→49、5→2（例：`WIN-014:WIN-009;WIN-010;WIN-011;WIN-012;WIN-013`）；`mutex_lock` 191 行全部非空、92 个不同锁名；机器校验 `validate_tasks.py` 含 wave 单调性、DAG 环检测、模块四任务对 `-DOC` 的传递依赖、禁止依赖 G0–G8；文本排序声明见 `06_TASK_GRAPH_AND_WAVES.md` §2（8 条）、§4（依赖链）、§5（9 条「主 Agent 不得自行改变的排序」）
- **`alias_of` 非空 3 行**：`P1-REAL-SAMPLE→WIN-009`、`P2-REAL-032R→WIN-010`、`P3-REAL-SAMPLE→WIN-011`（与 `07_CHECKPOINTS_AND_GATES.md` 末段「真实 32R 只由 WIN-010 产生」互指）
- **`commit_subject`**：`no-commit` 12、`evidence-only` 38、其余 141 条为 `type(TASK-ID): 完成TASK-ID任务：<英文目的>` 模板串（distinct 143）；`validate_tasks.py` 要求非 no-commit/evidence-only 者含中文字符（实测 PASS）
- **模块展开**：21 个模块组（`validators/validate_tasks.py` 的 `MODULE_PREFIXES` = 21，含 `P1-SESSION`/`P2-SESSION`）× `DOC/TEST/IMPL/INT` = 84，与台账 `kind=module-*` 84 行一致；`tasks/MODULE_MIGRATION_MATRIX.csv` 仅 19 行（不含两个 SESSION 组）

---

## 四、门禁与验收要求

**机器门清单**
- `19_MACHINE_CHECKS.md`「必须实现的检查」表列 **13 个脚本**（validate_control/tasks/docs/module_contracts/traceability/abi_manifest/dag_phase/thread_budget/cpu_providers/source_scope/audit_package/results + selftest）与通过词（`CONTROL_PASS`、`TASK_LEDGER_PASS`、`DOCS_PASS`、`TRACEABILITY_PASS`、`AUDIT_PACKAGE_PASS` 等）；另列静态扫描最低规则 5 类（C/C++ 线程与旧适配器符号、构建第二入口与全局 `/arch`、文档旧版本号/绝对路径/`--phases`、ABI 越界类型、资源观测序列不得用配置值代替）、脚本质量要求 5 条（`--root/--json-out/--strict/--help`、崩溃判 `TOOLING_FAILURE/P0`、不得「grep 一个关键词作为科学等价证明」）
- 实测套件口径不一致：`validators/run_control_checks.py` 的 `checks` 列表只有 **12** 项 —— 缺 `validate_source_scope.py`、`validate_audit_package.py`，多 `validate_templates.py`（三向集合差实测）
- 启动门：`00_READ_FIRST.md` §2 步 2–3 要求跑 `validators/validate_control.py --root` 并必须出现 `CONTROL_PASS`，否则「仅允许修复传输损坏，不得开始开发」。实测：对 zip 解包（`/tmp`）→ `CONTROL_PASS files=99`；对仓库内解包件 → `CONTROL_FAIL`（`ERROR manifest size mismatch: TASK_LEDGER.csv` / `manifest SHA mismatch` / `SHA256SUMS mismatch`，共 3 条，全部指向同一文件）；`validate_tasks.py` 两种形态均 `TASK_LEDGER_PASS tasks=191`
- 完整性哈希：`SHA256SUMS`（98 条）实测 ok 97 / mismatch 1（`TASK_LEDGER.csv`），`CONTROL_MANIFEST.json`（97 条）实测同样仅该文件 mismatch —— 与 digest 记「ok 97, mismatch 1」一致

**完成顺序要求**
- 状态机口径：`00_READ_FIRST.md` §4 + `TASK_LEDGER_README.md`（迁移图 + 「`INTEGRATED` 必须已有只含该 task 目的的 main commit；`CLOSED` 必须已有当前 SHA 的测试和 evidence」+「状态不能靠 Agent 自报」）
- `04_FOREGROUND_AGENT_RUNBOOK.md` §6 集成 13 步（回包 schema→changed-files 双向比对→允许路径/二进制/凭据→base 落后机器判定→`git apply --check`→主线复跑 acceptance→横向 guards→`SA-AUD-99` 抽检→失败回退→`git diff --check`→提交标题格式→push 后三 SHA→记录并解锁），并规定「任何时刻只允许一个 task 处于 `INTEGRATING`」
- `04` §10 收尾 8 条（含「全 task 无 NOT_STARTED/READY/DISPATCHED/RETURNED」「clean main、fetch、三 SHA 相等」「最终 push 后再生成源码快照」「三种模式复验」）
- `06` §5 九条排序（ABI 先于 loader、ThreadBudget 先于模块迁移、模块 TEST→IMPL→INT、接管先于删除、baseline provider 先于高级 ISA、最终 push 先于源码快照等）
- `08_GIT_MAIN_ONLY_INTEGRATION.md` §2「一 task 一目的」、标题式 `type(scope): TASK-ID 中文目的`、§4 push 后 `HEAD=main=origin/main` 且记 `COMMIT_LEDGER.jsonl`、§5 禁止动作清单
- `tasks/00_TASK_EXECUTION_CONTRACT.md` §E 完成条件（返回完整→scope clean→主线验收→独立抽检无阻断→原子 commit→push→三 SHA 相同），并声明「代码写完/本地能编译/文档已更新/看起来正常」均不是完成状态

**Gate 与验收**
- `07_CHECKPOINTS_AND_GATES.md`：G0–G8 每节给「必需/失败/证据」三段；「Gate evaluator 只读取下表和 TASK_STATE/evidence/findings，不接受手写 counts 或 status」「任何 task 禁止依赖 G0–G8」
- `GATE_REQUIREMENTS.csv`（表头 `gate_id,required_tasks`，9 行）实测各门任务数：G0 5、G1 12、G2 13、G3 **91**、G4 2、G5 6、G6 12、G7 9、G8 6；并集 156 个唯一号，全部存在于台账（差集为空）；台账中 **35 号不在任何 Gate 集合**（`ABI-006, ACR-001, BLD-004, CLI-003, CLI-004, CPU-001..008, DATA-003, DATA-004, IO-001..003, LNX-001, LOG-002, LOG-003, MON-001, MON-002, P1-REAL-SAMPLE, P2-PERF-001, P2-PERF-DESIGN, P2-REAL-032R, P2-SEAM-001, P3-REAL-SAMPLE, RT-005, RT-006, RT-007, WIN-001, WIN-002, WIN-008`）
- 自动判定四值（`07` 末段与 `20_RELEASE_GATES.md`「最终 verdict」一致）：`PACKAGE_INVALID / NOT_READY / BLOCKED_EXTERNAL / READY_FOR_OWNER_REVIEW`；`17_INDEPENDENT_REVIEWER.md`「结论规则」同口径并加「源码…发生任何变化，全部审计证据失效」
- 性能/资源量化门（`01_OWNER_FROZEN_CONSTRAINTS.md` §D.6–D.7 与 `20_RELEASE_GATES.md`「强制科学/性能门」同值）：heavy 区间平均归一化 CPU ≥80%、≥70% 采样点 ≥75%、有工作时连续 10s 不得低于 50%、2/1 worker 加速比 ≥1.60、`resource_class=cpu_heavy` 且 `work_units>1`、无 profile 只能 baseline

**waiver / 豁免机制**
- `07_CHECKPOINTS_AND_GATES.md` 末段：P2/P3 不能由 Agent 自行 waiver，需负责人选择时列 `WAIVER_REQUIRED`
- `20_RELEASE_GATES.md` 末段：不得用 waiver 掩盖 P0/P1；不得自行把 `READY_FOR_OWNER_REVIEW` 改成 `RELEASED`
- `22_FAILURE_ESCALATION.md` 「升级」段：仅科学定义冲突、正式平台/数据不可用、需改冻结合同、需 P2/P3 waiver 四种情形可发给负责人
- `17_INDEPENDENT_REVIEWER.md` Finding 格式含 `"waiver": null` 字段（`schemas/finding.schema.json` 同族）
- 包内 **无** waiver/豁免登记表或清单文件（实测 99 文件名单，无 waiver 命名字文件）

**审核包与提交要求（出处）**
- `18_AUDIT_PACKAGE_SPEC.md` §1 包名式 `AstroCS_ALPHA0.11.0_REVIEW_<UTC>_<MAIN_SHA12>.zip`、根目录固定 `AstroCS_REVIEW/`、压缩前后三层校验；§2 固定目录树；§3 源码白名单（由 `package_policy/SOURCE_ALLOWLIST.txt` 精确列举）；§4 排除项（原始 FITS/testdata/HiPS tile/build/.git/单文件 >5 MiB 等）；§5 证据 13 字段；§6 容量与完整性门 v2（总大小目标 ≤20 MiB、无嵌套包、无绝对本机路径、`SHA256SUMS` 解包后退出码必须 0）；§7 归档目录与 `ARCHIVE_INDEX.csv`
- `package_policy/PACKAGE_POLICY.json`：`policy_version 2`、`max_file_bytes 5242880`（5 MiB，与 18 §4 同）、`source_fixture_max_bytes 262144`、`module_fixture_total_max_bytes 2097152`、`forbidden_names` 与 `forbidden_path_patterns`（含 `^[A-Za-z]:[/\\]`、`/home/`、credential/token/secret）、`nested_review_package_forbidden true`、`symlink_escape_forbidden true`
- `00_READ_FIRST.md` §6 交付 7 项 + 「执行 Agent 最多写 `READY_FOR_OWNER_REVIEW`」；`01` §H 判定权保留给项目负责人

---

## 五、设计意图（转述，不评价）

- **目标**（`00_READ_FIRST.md` §1）：把「能工作但架构、文档和执行真实性仍不可靠」的现状收敛为可预发布的纯 CPU 模块化产品，同时科学定义不因架构迁移漂移；「必须同时实现」10 条 = 三 Phase 分跑分配置分验收不得同进程串联、Windows 单入口 `astrocs.exe` + DLL 化、DAG 节点一对一绑真实模块操作、单一共享 executor + 真实 ThreadBudget、ACR 保留但不进生产构建/加载/benchmark/发布包、ISA 只在热点 kernel 且无 profile 一律 baseline、SCI→ALG→DATA/API/ARCH→符号/manifest→TEST→当前提交证据全链闭合、每模块独立源码+README+manifest+五类可复用测试、Phase3 独立完成 TAN/SIN/ZEA/CAR/AIT + `nearest`/`healpix_interp4` 流式 FITS、最终仅一次 Windows 候选提交真实数据/32R/接缝/资源验证
- **禁止项（包内自述，逐处）**：`01` §A.4 禁同进程 `--phases 1,2,3`；§C.1 生产构建/发布包不得依赖或包含 ACR/CUDA；§D.3–D.4 禁私有永久线程池、禁 `ThreadLease::make(n)` 伪造授权；§E.1 迁移不得同时改公式/权重/ivar/SNR/排异/归约顺序/精度/默认容差；§F.3 禁跨 DLL 传 STL/异常/RTTI；§F.8 活跃文档禁以 V18/V19/V4/V5/V6、旧 commit、旧路径冒充现状；§G.3 禁 force push/reset --hard/破坏历史/覆盖用户未提交工作；`00` §3「前台 Agent 不可以」7 条（含不得用历史 PASS 或旧 Windows 二进制替代当前提交证据、不得每过一个 Gate 索要批准）、「SubAgent 不可以」4 条；`04` §1 前台最多改不超过约 20 行且纯机械；`08` §5 禁止动作；`22` 重试 5 条含「不能重复跑直到碰巧 PASS」「Windows 离线不构造虚假日志」
- **对前代的继承/废止声明**：`00_READ_FIRST.md` 抬头状态行「任何历史 V4/V5/V6/V18/V19 控制文件均不得覆盖本包」；§4 点名禁止 `REWORK_*` 等状态字面量；`01` 首段「历史工程包、仓库旧文档、Agent 习惯或实现便利均不能覆盖它」；`02` §19 判上一轮快照「可以作为下一轮重构输入基线，但不能作为发布候选基线」并写「下一轮的目标不应写成『继续 V6.1 剩余任务』」；`ACTIVE_DOC_INDEX.md` 规定历史审核包/旧报告/旧源码只能进 `control/archive/` 或 `audit/archive/`，不得作当前证据；`package_policy/ARCHIVE_INDEX.csv` 两条模板行 reason = `superseded by V7`
- **负责人裁决/用户指令痕迹**：本包 **无** `04_OWNER_DECISIONS.md`/`07_FRONT_DESK_RULINGS.md`/`OWNER_BINDINGS.*` 同名文件（实测文件名清单），负责人裁决以两类文件承载 ——
  - `01_OWNER_FROZEN_CONSTRAINTS.md`：整篇为「项目负责人冻结约束」，A–H 八组逐条（如 §D.6 三项利用率阈值、§B.2 Win10 22H2 build 19045 下限、§C.8 AVX-512 不因「更高级」自动优先）
  - `02_CURRENT_BASELINE_AUDIT.md`：抬头「审查原则：以项目负责人本轮修正为最高约束」；§2 标题「本轮必须覆盖旧文档的负责人决定」并逐条 15 项（main-only、三 Phase 独立、Windows amd64 正式平台、Linux 控制节点、单入口 exe + DLL、HiPS Browser 不入 CLI 注册表、ACR 保留但 dormant、纯 CPU、重计算不得单线程、前台只拆解派发、模块自持六件套等）；§17 记录两项负责人选择（Windows 兼容下限与 Phase3 REQUIRED 投影/TAN…AIT + `nearest`、`healpix_interp4`）；§16 A 段「将本轮负责人决定合并进正式 `AstroCS_ENGINEERING_CONSTRAINTS.md` 并提交 main」
  - `05_FIXED_SUBAGENT_BINDINGS.yaml`：`foreground.may_edit_source: false` + `duties`/`forbidden` 列表 + 33 个 owner 的写路径白名单（对应 `SUBAGENT_DISPATCH_PLAN.md` 首段「`05…yaml` 是路径权限事实源，`TASK_LEDGER.csv` 是任务/依赖事实源」）
  - 仓库侧负责人指令痕迹（非包内）：`memory.md` item 9「旧控制包作废归档（2026-09-09 owner 指令）…V7 …（140 任务队列）disarm 终止于 42/140（41 submit + NOISE-IMPL 沿用旧包收尾）」（入库提交 `32a252f8` 2026-09-09）；`工程控制/ACTIVITY_STATE.md` §2 V7 行同口径 + §3「只登记、不清理、不提交、不收编」（入库提交 `8e03d7da` 2026-09-10）

---

## 六、代际差异表（上一代 = G09 的 `AstroCS_V6_1_REWORK_CONTROL_20260831`；规模参照 `AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830`）

| 项 | V6.1 REWORK（20260831） | V7（ALPHA3/V7.1 FINAL3，20260902） | 差异性质 |
|---|---|---|---|
| 文件数 | zip 47 / 解包件 46（`工程控制/AstroCS_V6_1_REWORK_CONTROL_20260831`） | zip 99 / 解包件 99 | +52（解包件） |
| 编号规格文件 | 00–14 共 15 份（`01_` 前缀重复两份；另有 `AGENTS_PATCH.md`） | 00–23 共 24 份（05 为 yaml） | 规格文件增 9；V6.1 的 `01_INDEPENDENT_AUDIT_CONCLUSION.md`/`13_OWNER_REVIEW.md`/`05_GATE_CHECKLISTS.md` 在 V7 无同名对应；V7 新增 `12_DLL_ABI…`/`14_RUNTIME…`/`15_CPU…`/`17_INDEPENDENT_REVIEWER.md`/`19…23` 等 |
| 台账文件 | `03_REWORK_TASK_LEDGER.csv`，数据行 67 | `TASK_LEDGER.csv`，数据行 191 | +124 行 |
| 台账列（11 列同数） | `task_id,gate,title,depends_on,platform,change_class,heavy_compute,commit_required,scope,acceptance,status` | `task_id,wave,owner_id,kind,depends_on,mutex_lock,resource_class,commit_subject,spec_ref,acceptance,alias_of` | 新增 `wave/owner_id/kind/mutex_lock/resource_class/commit_subject/spec_ref/alias_of`；删除 `gate/title/platform/change_class/heavy_compute/commit_required/scope/status` |
| 状态列 | **有**（67 行全部 `NOT_STARTED`） | **无**（状态外置 `TASK_STATE.json`，`TASK_LEDGER_README.md`） | 台账由「带状态的单源」改为「静态定义单源」 |
| 状态字面量 | `00_READ_FIRST`/`START_PROMPT` 用 `WAITING_WINDOWS`（Fatduck 离线） | `00_READ_FIRST` §4 用 `WAITING_RESOURCE`，并禁 `REWORK_*`/`IMPLEMENTED`/`VERIFYING`/`ACCEPTED`/`PUSHED`/`BLOCKED_CANDIDATE` | 字面量替换 + 显式封禁第二套状态 |
| 任务号族 | `R0-001..R0-004, GOV, VER, CHK … REL-003`（START_PROMPT：「从 R0-001 连续执行到 REL-003」） | 27 族（P1/P2/P3/WIN/CPU/QA/RT/ABI/LNX/AUD/GOV/BLD/CAT/CLI/DATA/ID/IO/LOG/PKG/MON/ACR/ARC/CTL/DOC/FG/GATE/TST），含 84 个 `<前缀>-{DOC,TEST,IMPL,INT}` 模块号 | 号族体系重构；实测 V7 191 号中有 **51 号**与 V5(98)/V6(88)/V6.1(67) 台账同号 |
| 门禁脚本 | `scripts/` 6 个（`validate_control.py`、`validate_task_graph.py`、`validate_return_audit.py`、`package_audit.py`、`known_failure_scan.py`、`selftest.py`） | `validators/` 18 个（13 个 `validate_*.py` + selftest + 4 个辅助），`19_MACHINE_CHECKS.md` 表列 13 | 脚本 +12；V6.1 出口门 `validate_return_audit.py` 无 V7 同名对应（V7 对应能力拆为 `validate_audit_package.py`+`validate_results.py`） |
| Gate 体系 | 台账以 `gate` 列内联（G0…），`05_GATE_CHECKLISTS.md` | `GATE_REQUIREMENTS.csv` 独立文件（9 门，G3 91 任务）+ `07` 叙述 + 「任何 task 禁止依赖 G0–G8」 | 门禁从列内联改为独立机器文件 |
| schemas / templates | 7 / 14（templates 含 `COMMITS.csv`、`FINDINGS.csv`、`TEST_SUMMARY.csv` 等 csv 模板） | 9 / 9 + `checklists/` 6 + `science/` 3 + `package_policy/` 4 | templates 收敛、新增三类专目录 |
| 清单文件字段 | `MANIFEST.json`：`control_id`、`target_version`、`created_utc`、**`prior_control_sha256`**、**`reviewed_audit_sha256`**、`reviewed_audit_reported_commit`、`engineering_constraints_sha256`、`task_ledger_sha256`、`files[45]` | `CONTROL_MANIFEST.json`：`package_id`、`product_version`、`source_reference`（源码提交 SHA）、`manifest_version`、`generated_by`、`files[97]`；**无 prior_control_sha256 / reviewed_audit_sha256 / task_ledger_sha256 字段** | 链代方式由「上包 SHA-256」改为「源码提交 SHA + 包内审计稿」 |
| 目标版本 | `0.10.0-alpha.2`（MANIFEST `target_version`） | `0.11.0-alpha.1`（`VERSION` 与 `CONTROL_MANIFEST.product_version` 一致） | 版本推进 |
| 规模参照 V6 | `…/CONTROL_V6/AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830/05_TASK_LEDGER.csv`：数据行 88、21 号族、含 `status` 列（全 `NOT_STARTED`）、列结构同 V6.1 | 191 行 | V6→V6.1 缩 21 行、V6.1→V7 扩 124 行；V7 = V6 的 2.17 倍 |
| 包自校验 | `SHA256SUMS` 实测 ok 43 / mismatch 1 / missing 1（`scripts/validate_return_audit.py`，见 G09 digest） | `SHA256SUMS` 实测 ok 97 / mismatch 1（`TASK_LEDGER.csv`）；zip 解包形态 ok 98 / mismatch 0 | 均非零差异，但成因文件不同 |
| 路径命名 | 解包目录名 = zip 名 = 身份名 | zip 名（`ALPHA3…V7_1…`）≠ 解包目录名（`V7_MODULAR…`）≠ 包内 ID（`…V7`）≠ manifest ID（`…V7.1`） | 一物四名（见 §八 第 7 条） |

---

## 七、执行留痕三方核对（台账 ↔ evidence/artifacts ↔ 提交消息）

**evidence/ 实测**：`evidence/` 仅 `ciqa(1)`、`refactor(108)`、`v6_1_rework(283)`、`v8_1_ci_control(232)` 四个 tracked 目录，**无 v7/V7/refoundation 目录**；`工程控制/`、`run/` 亦无 V7 自产证据目录。V7 号的留痕全部落在 V8.1 线文件里：`evidence/v8_1_ci_control/{TASK_STATE.json, STATE_RECONCILIATION.csv, COMMIT_LEDGER.jsonl}` 三件套对 191 个台账号覆盖 **191/191**（严格全号 token 匹配实测）。该文件自身的 `sources.v71_static_ledger` 记台账 sha `524ab719…`、`task_count: 191`；`taskStatePath` 亦为 `evidence/v8_1_ci_control/TASK_STATE.json`（cp 队列文件内字段，见下）。

**提交消息命中率（自行复算 vs 引用表）**
- 引用值：`设计大纲/_evidence/packs/pack_commit_link.csv` 行 `AstroCS_V7_MODULAR_REFOUNDATION_CONTROL_20260902_FINAL3,191,167,87.4,317`（未命中样例含 `AUD-002..005, CAT-GAIA-DOC/IMPL/INT/TEST, CTL-001, FG-VERIFY-001, GATE-OWNER-001, ID-002`）
- 我的复算（语料 = `git log --all` 2045 条，subject+body，严格全号边界匹配）：命中 **119/191 = 62.3%**，涉及 390 条提交；限定日期窗 2026-09-02..09-09 命中 107/191 = 56.0%，涉及 192 条提交（同期全量提交 330 条）
- 用同一语料源文件 `设计大纲/_evidence/commits/index.jsonl`（1988 行）做严格匹配亦得 119/191 = 62.3% → 87.4% 与 62.3% 之差**源于抽取规则而非语料**：该 CSV 以每行预算的 `ids` 字段比对，而 `ids` 抽取会把多段号拆解。抽查证据：提交 `7c3d66e2`（2026-09-07）主题即 `docs(CAT-GAIA-DOC)`，其 `ids` 字段被抽成 `['AST-001','DESIGN-001','GAIA-001']`；`925fb26e`（`docs(P1-CAL-DOC)`）被抽成 `['CAL-001','DESIGN-001']`。因此 CSV 把 `CAT-GAIA-*`/`P1-*-DOC` 列为「未命中」与提交事实不符；而 `AUD-002..005`、`CTL-001`、`ID-002`、`FG-VERIFY-001`、`GATE-OWNER-001`、`PKG-001..003` 实测在全部 2045 条提交消息中 **0 命中**（与 CSV 一致）
- 归因限制：191 号中 51 号与 V5/V6/V6.1 台账同号（如 `CPU-001..008`、`WIN-001..006`、`QA-001..005`、`LNX-001..005`、`GOV-001..002`、`RT-001..007`），这类命中不能单独归因于 V7 线；对 140 个「V7 独有号」计算严格命中 = 68/140 = 48.6%（窗口内 64），窗口内提及 V7 独有号的提交 115 条

**分类清单与数量**
| 类别 | 判据（实测口径） | 数量 | 清单/样例 |
|---|---|---|---|
| 一致 | `TASK_STATE` 状态 CLOSED ∧ `closed_commit` 非空 ∧ 窗口内提交消息出现该号 | **89** | 例 `CAT-GAIA-DOC`（`7c3d66e2`/`fa261e84`）、`P1-CAL-DOC`（`925fb26e`/`b1b07b36`）、`ABI-001`（`76b85d45`）；89 个 `closed_commit` 全部可被 `git cat-file -e` 解析，日期分布 09-02 10 / 09-03 25 / 09-04 6 / 09-05 2 / 09-07 8 / 09-08 12 / 09-09 26，无一早于冻结日 2026-09-02 |
| 有账无据 A：状态 CLOSED 但全库提交消息无该号 | 台账有号 + CLOSED + 0 提交提及 | **3** | `CTL-001`、`ID-002`、`ID-003`（`TASK_STATE` 记 `source_of_status: review_claim_no_commit_task`，`STATE_RECONCILIATION.csv` 第 2 行原文口径「claimed:no-commit」） |
| 有账无据 B：CLOSED 且窗口内无该号提交提及（含 A） | CLOSED + 窗口内 0 提及 | **7** | 上 3 项 + `ID-001`、`AUD-001`（其唯一提及在 `e9547c0` 2026-09-10）、`QA-004`、`QA-005` |
| 有账无据 C：CLOSED 但台账无关闭提交号字段 | CLOSED + `closed_commit` 空 | **16**（其中 9 个号有窗口内提交提及） | 9 项为 `ABI-006, QA-001, QA-002, QA-003, LNX-001, LNX-002, LNX-003, LNX-004, LNX-005` |
| 有账无据 D：台账有号、零执行痕迹 | 状态 NOT_STARTED / FATDUCK_PENDING | **86** | NOT_STARTED 84；FATDUCK_PENDING 2 = `WIN-001`、`WIN-002` |
| 有据无账 E：提交消息/台账外出现但不在 V7 台账 | 2026-09-02..09-09 主题号 ∉ 台账 | 前缀计数：V8 83、V81 14、UT 13、DISP 12、REL 8、FIX 8、SCI 6、CON 4、KI 4、SA 4、DOC 3（Top 项 `V8-CI-012` 32 次、`V8-CI-010` 13 次） | 属 V8.1 线与 finding/契约号，非 V7 台账；`REL-002/003` 属 V6.1 台账 |
| 有据无账 F：状态非 CLOSED 却出现该号提交 | ≠CLOSED ∧ 有提交提及 | **17**（窗口内 9） | `P1-NOISE-IMPL`（`f74fc20` 2026-09-11）、`P2-UPM-IMPL`、`P3-FITS-IMPL/TEST`、`P3-PROJ-IMPL`、`P3-RSMP-IMPL/INT`、`QA-006`、`WIN-001..009`（其中 `WIN-001..006` 与 V6/V6.1 同号，归因未证实） |
| 无痕迹缺口 | 既无提交提及也无 evidence 记录 | **0** | 191/191 在 `TASK_STATE.json` 中有条目（含状态与 `source_of_status`），故按该三件套口径无「零留痕」号 |

**完成度口径并存（同一线三个互不一致数字，均实测在案，不判断谁对）**
1. 38/140：`run/local/HANDOFF_CONTROL_PACK_20260909.md` §1（「idx = 39（下一条未注入任务：40 P1-HIPS-IMPL，已在飞被中断）」「已闭环 38/140」）；对应 cp 队列文件 `/workspace/.dsh/control-runs/session-ccad3cec-df44-40f6-aa85-a9bdfc83ca2a.json`（实测 `tasks len=140`、`idx=39`、`finished=false`、`doneStatuses=['CLOSED','PASS','DONE','COMPLETED']`、`updatedAt 2026-09-09T03:51:12Z`）
2. 42/140：`memory.md` item 9（提交 `32a252f8`）、`工程控制/ACTIVITY_STATE.md` §2 V7 行、`REVIEW.md` 第 70 行、`HANDOVER.md` 第 117 行
3. 105/191 CLOSED：`evidence/v8_1_ci_control/TASK_STATE.json`（`counts` 字段，`updated_utc 2026-09-09T11:23:00Z`，`main_sha 80f2727d`，`reconciled_by.task_id = MAIN23-W3-RECONCILE`）
- 队列与台账关系（实测）：140 个队列任务号 **100% ⊂ 191 台账**（差集为空）；未入队的 51 号分布 W0 5 / W1 14 / W2 30 / W5 2，恰为 W0–W2 治理与宿主基础设施任务；队列 wave 分布 W1 21 / W2 1 / W3 77 / W4 2 / W5 13 / W6 19 / W7 1 / W8 6
- 另需登记（不入分类）：`02_CURRENT_BASELINE_AUDIT.md` 与 `00_READ_FIRST.md` 声明的冻结起始提交 `c1696156`（2026-09-02，主题以 `WIN-001:` 起头）确为 HEAD 祖先，该提交处 `VERSION` = `0.10.0-alpha.2`，与包内自述一致

---

## 八、缺口与不确定

1. **解包件形态不通过包内自校验**：`validators/validate_control.py --root <解包件>` → `CONTROL_FAIL`（3 条错误全在 `TASK_LEDGER.csv`）；zip 解包 → `CONTROL_PASS files=99`。唯一可观测差异为行尾符；行尾差异成因未证实。指针：`TASK_LEDGER.csv`（`524ab719…` vs `32e3e414…`）、`SHA256SUMS` 第 98 条量级、`CONTROL_MANIFEST.json` `files[]`
2. **机器门清单三处不齐**：`19_MACHINE_CHECKS.md` 表 13 项；`validators/run_control_checks.py` 实串 12 项（缺 `validate_source_scope.py`、`validate_audit_package.py`，多 `validate_templates.py`）；`validators/` 目录 18 个 .py。缺项脚本文件本身存在（`validate_source_scope.py`、`validate_audit_package.py` 均在），只是未入套件；是否有意未入，未证实
3. **审核包容量门两个口径**：`18_AUDIT_PACKAGE_SPEC.md` §6「包总大小目标 ≤20 MiB」vs `package_policy/PACKAGE_POLICY.json` `max_total_bytes: 52428800`（50 MiB）；`validators/validate_control.py` 对控制包自身硬门为 20 MiB（`total>20*1024*1024`）。取何值执行，未证实
4. **G3 规模三种表述**：`07_CHECKPOINTS_AND_GATES.md` G3 行写「全部模块 DOC/TEST/IMPL/INT」（无计数）；`20_RELEASE_GATES.md` G3 行写「19 组模块」；`GATE_REQUIREMENTS.csv` G3 实列 91 个号；台账 `kind=module-*` 为 21 组 84 任务，`validators/validate_tasks.py` `MODULE_PREFIXES` = 21，`tasks/MODULE_MIGRATION_MATRIX.csv` = 19 行（缺 `P1-SESSION`、`P2-SESSION`）。可核对结论：19 = matrix 行数，21 = 台账/校验器口径
5. **包内悬空引用 2 处**：`tasks/05_PHASE1_TASKS.md` §8 引用 `validate_no_callers.py`（包内无此脚本）；`tasks/00_TASK_EXECUTION_CONTRACT.md` §A.5 引用 `24_FAILURE_ESCALATION.md`（包内实为 `22_FAILURE_ESCALATION.md`，无 24_ 文件）
6. **包内规格引用 `19_MACHINE_CHECKS.md` 与 `04_FOREGROUND_AGENT_RUNBOOK.md` 无编号缺口，但 `START_PROMPT.txt` 长度需按字符口径核对**：该文件 187 字节 / 79 字符，`validators/validate_control.py` 的门限是「字符数 > 100 即 error」，实测 79 < 100 通过 → 非缺口，登记以备后续形态比对使用。另：`17_INDEPENDENT_REVIEWER.md` 审计流程第 1 步只点名 `validate_control.py`、`validate_tasks.py`、`validate_traceability.py` 三脚本，与第 2 条的 13/12/18 三处清单差并存
7. **同一包四套身份命名口径**：`00_READ_FIRST.md` 抬头 ID = `ASTROCS-ALPHA3-MODULAR-REFOUNDATION-V7`；`CONTROL_MANIFEST.package_id` = `…-V7.1`；zip 文件名 = `…V7_1_20260902_FINAL3`；解包目录名 = `AstroCS_V7_MODULAR_REFOUNDATION_CONTROL_20260902_FINAL3`（无 ALPHA3、无 7_1）。脚本归一后成为两个身份（`设计大纲/_evidence/packs/dispatch_groups.json` G10 两条）
8. **规格覆盖缺口**：191 号中 61 号在 `tasks/` 全部文件中无字面任务号出现（模块号由 `MODULE_MIGRATION_TEMPLATE.md` 前缀展开覆盖，如 `P1-CAL-TEST`、`P2-UPM-IMPL`、`P3-RSMP-TEST`、`P2-SEAM-001`），即字面规格覆盖 130/191
9. **digest 未记的存形态**：ALPHA3 zip 实测全库 77 份（digest 4 实例），新增 `设计大纲/_evidence/packs/history/a4fdee3f/…` 复原件 1 份 + `run/` 影子树 72 份（同 sha256 `005448fb`）；`pack_lineage_table.md` 第 40 行的「4 / zip,zip_」与此一致（不含影子树），`pack_inventory.csv` 亦只 4 行
10. **引用表两处与本体/提交不符**：(a) `pack_commit_link.csv` 命中率 87.4% 不可用严格全号匹配复现（62.3%），且未命中样例 `CAT-GAIA-DOC/IMPL/INT/TEST` 实际存在 2026-09-07 / 09-09 提交（`7c3d66e2`、`fa261e84`、`a030a3e7`、`babe752d`、`054af3a3`、`94074a32`），成因指向 `index.jsonl` `ids` 抽取拆解多段号；(b) `pack_lineage_table.md` 第 40 行 ALPHA3 行记「首次 2026-09-10 / 末次 2026-09-10」，实测首现为 `a4fdee3f` 2026-09-05（旧 active 路径），09-10 是 `8e03d7da` 的 R100 移动；`pack_events.md` 第 63 行另记「zip 随 V8.1 baseline 于 seq1521｜a4fdee3f｜2026-09-05 入库」，与我的实测一致、与 lineage 行不一致
11. **登记与现状不一致**：`工程控制/ACTIVITY_STATE.md` §3 与 `memory.md` item 10 记「根 `CMakeLists.txt` 追加块未提交（预存 8 个 tracked modified 之一）」；实测现工作树 `git diff -- CMakeLists.txt` 为空、`git status --porcelain -- CMakeLists.txt` 无输出。未跟踪残留则仍在（`?? lib/snr_estimator/CMakeLists.txt`、`?? lib/snr_estimator/include/`、`?? lib/snr_estimator/src/`、`?? tests/unit/p1_noise/`）；CMakeLists 追加块后续被提交吸收还是被清理，未证实
12. **V7 运行态用了包外状态字面量**：`evidence/v8_1_ci_control/TASK_STATE.json` 的 `status_enum` 含 `FATDUCK_PENDING`（用于 `WIN-001`、`WIN-002`），不在本包 `00_READ_FIRST.md` §4 / `schemas/task_state.schema.json` 的 11 值集合内；该文件属 V8.1 线产物，故与本包枚举不同源
13. **完成度三口径未归一**：38/140、42/140、105/191（见 §七），互相之间无法用现存文件推得换算；「140」与「191 − 51（与上代同号的碰撞号）」数值相同但实测两个 51 集合并不相等（各自有 23 项不在对方集合），故未证实同一口径
14. **前代进度包不可实测**：`02_CURRENT_BASELINE_AUDIT.md` 抬头记审查对象 `AstroCS_V6_1_PROGRESS_PACK_20260902T064333Z.zip` 及其 SHA-256 `65b2c21431a31e64…`；`find` 全库 0 命中该文件，`工程控制/_control_packs/` 只有 `AstroCS_V6_1_REWORK_CONTROL_20260831.zip`（`903018212bfb…`）→ 该 SHA 无法核对（未证实）
15. **无 V7 专属 evidence/artifacts 目录**：`evidence/` 实测 4 子目录无 v7；`artifacts/`、`reports/` 下亦未见以 V7 命名的留痕目录（`run/local/` 两份交接文档为 gitignore 现场文件）→ V7 执行痕迹只能经 V8.1 线对账文件与提交消息间接读取
16. **读不到/未核**：`evidence/v8_1_ci_control/TASK_STATE.json` 的 `sources` 中 `REVIEW_CLAIMED_TASK_STATE.csv` 路径以 `…/baseline/` 省略号书写（包内文本原样），该文件是否即 `engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/baseline/REVIEW_CLAIMED_TASK_STATE.csv` 未逐一核对内容；`tasks/10_AUDIT_PACKAGING_TASKS.md` 的 9 个号未逐号读正文；02 号族（ACR/CAT 等）`acceptance` 文本仅抽样

---

## 尾段一：与上一组（G09）的衔接

- **关系定性：替代性重启（继承冻结起点 + 废止上一轮状态口径），非台账继承。** 依据：V7 包内 `02_CURRENT_BASELINE_AUDIT.md` 抬头（审查对象为 V6.1 进度包、源码提交 `c1696156`、版本 `0.10.0-alpha.2`）、§19「可以作为下一轮重构输入基线，但不能作为发布候选基线」「下一轮的目标不应写成『继续 V6.1 剩余任务』」；`00_READ_FIRST.md` 抬头状态行废止「历史 V4/V5/V6/V18/V19 控制文件」覆盖本包，§4 又点名禁建 `REWORK_*` 状态（V6.1 期状态口径）。
- **无 SHA 级上包锚**：G09 的 `AstroCS_V6_1_REWORK_CONTROL_20260831` 在 `MANIFEST.json` 中携带 `prior_control_sha256: fdad8e40…` 与 `reviewed_audit_sha256: 137e3ed1…`；V7 的 `CONTROL_MANIFEST.json` **无 `prior_control_sha256` 字段**（实测键清单：files / generated_by / manifest_version / package_id / product_version / source_reference），两包之间唯一的机器可核链接是源码提交 `c1696156583c…`（V7 `00_READ_FIRST.md` 与 `CONTROL_MANIFEST.source_reference`）。V7 包内文本亦无 `AstroCS_V6_1_REWORK` 字符串命中（实测 grep 仅 `02_CURRENT_BASELINE_AUDIT.md` 三处提及「V6.1」与进度包名）。
- **字面量衔接点**：V6.1 用 `WAITING_WINDOWS` 表 Fatduck 离线（其 `START_PROMPT.txt` 口径），V7 改为 `WAITING_RESOURCE` 并禁第二套状态（`00_READ_FIRST.md` §4）。
- **仓库侧收口**：`工程控制/ACTIVITY_STATE.md` §2 把 V6.1 判 `ARCHIVED_SUPERSEDED`、V7 判 `ARCHIVED_DISARMED`（提交 `8e03d7da`），`memory.md` item 9 记 owner 指令「将重新修订工程包（新控制包）」（提交 `32a252f8`）→ 本组两身份 = 该链条中被 V8.1/G12 线接管的中间代际。

## 尾段二：重叠身份

- **组归属**：在 `设计大纲/_evidence/packs/dispatch_groups.json`（G01–G14、G99）中，两身份 **仅登记于 G10**，无跨组同名登记 → 名称层面无重叠身份（实测按 identities 精确匹配）。
- **内容层面跨组重叠（属 G12「V8 与 V8.1 CI 控制包」）**：
  1. ALPHA3/V7.1 zip 以「V8.1 baseline 内嵌文件」形态存在（`engineering/control/{active→archive}/…V8_1_20260905/baseline/AstroCS_ALPHA3_…FINAL3.zip`，`a4fdee3f` 引入、`8e03d7da` R100 移动），即同一文件同时是 G10 身份实体与 G12 身份包内成员。
  2. V7 台账以另一身份成员形态再现：`engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/baseline/V7_1_STATIC_TASK_LEDGER.csv` 与 `工程控制/AstroCS_ALPHA0.11.0_EXISTING_WORKSPACE_CI_CONTROL_V8_1_20260905/baseline/V7_1_STATIC_TASK_LEDGER.csv`，工作副本 sha 实测 `524ab719…`（192 物理行 = 191 数据行），git blob 实测 `32e3e414…`（`git cat-file -p :…` = 40,488 B）；两者与本组 digest 记的 11 列表头/191 行/「无 status 列」完全一致（本组 digest = 一致）。
  3. 该 blob/工作副本口径差已由 G12/G13 线自行登记：`工程控制/ACTIVITY_STATE.md` §5、§6 F4（`32e3e414…` 自 `a4fdee3f` 入库 vs BASE-001 快照工作区口径 `524ab719…`），与本报告的行尾差异测量互相印证、无冲突。
- **与本组 digest 的一致性小结**：两份 digest 记的文件数（99）、zip sha256（`005448fb`）、台账行（191）与表头、「SHA256SUMS ok 97 / mismatch 1 (TASK_LEDGER.csv)」、命中率 87.4% 中除命中率外全部实测复现（`设计大纲/_evidence/packs/digest/…` 两文件；命中率一项不吻合，见 §八 第 10 条）。

---

*报告结束。所有断言均带相对路径 + 条款/任务号或 8 位提交 SHA；未评价优劣、未提建议、未推测动机。*
