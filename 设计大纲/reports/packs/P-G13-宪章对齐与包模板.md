# P-G13 宪章对齐控制包 V1 与控制包模板（并入 G99 残余项）

> 取证范围：组 G13（`AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909`、`cprun-v4-pack-template`）+ 前台指令并入的 G99（`AstroCS_AUDIT_REVIEWPACK_20260909T133841Z`），共 3 个身份。
> 取证件：`设计大纲/_evidence/packs/digest/`（3 份）、`pack_inventory.csv`、`pack_commit_link.csv`、`pack_lineage_table.md`、`设计大纲/reports/packs/pack_events.md`。
> 复核面：工作树包目录、git（只读）、`artifacts/*.zip`（python3 zipfile 只读）、`run/`（不入库工作区）。
> SHA 一律 8 位短写；日期为提交者日期（+08:00）。所有结论均为可重算的计数/枚举，不含优劣判断。

---

## 一、组内包清单与身份归并

### 1.1 身份逐行清单

| 身份 | 存形态与实例数 | 文件数 | zip sha256 | 首次提交（git 实测） | 最后出现提交 | 是否被删除及删除提交 |
|---|---|---|---|---|---|---|
| `AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909` | 解包件 1（`工程控制/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909/`）；zip 原件 0；归档镜像 0；仅历史复原 0；影子树 70 份（`run/**`，本组自测、取证件未登记） | 74（294654 字节） | 无 zip 形态：`工程控制/_control_packs/` 12 件内无本包；包内亦无 `SHA256SUMS`/`MANIFEST.json` | `e9547c02` 2026-09-10 15:14:01 | `f5f944a5` 2026-09-12 18:52:52（该目录全史 11 次提交：A=74 / M=30 / D=0） | 无删除提交（`git log --all --diff-filter=D` 对本目录为空）；但包级状态已转 `ARCHIVED_SUPERSEDED`（`工程控制/ACTIVITY_STATE.md:22`，登记提交 `5a250999` 2026-09-13 13:15:19），目录原位保留 |
| `cprun-v4-pack-template` | 解包件 1（`工程控制/cprun-v4-pack-template/`）；zip 0；归档 0；历史复原 0；影子树 70 份（本组自测）；另在 G99 zip 内嵌 2 份（本组自测） | 10（14182 字节） | 无自身 zip；G99 zip 内 2 份内嵌副本各 10 条目，20/20 条目 CRC32 与工作树一致 | `4d3feaf4` 2026-09-09 21:27:22 | 同一次提交（全史仅 1 次，A=10） | 无删除；`工程控制/ACTIVITY_STATE.md:27` 标 `REFERENCE_TEMPLATE` |
| `AstroCS_AUDIT_REVIEWPACK_20260909T133841Z`（G99 并入） | zip 1（`artifacts/AstroCS_AUDIT_REVIEWPACK_20260909T133841Z.zip`）；解包件 0；归档 0；影子树 0；历史复原 0 | zip 条目 14556（唯一路径 14554，见第八节第 14 条）；解压合计 205347794 字节；压缩实体 58795448 字节 | `3b5d9c230473e4210bac0e1e636cfef317df524307e908ce0f1f17940c16ab2c`（本组自算，与 `pack_inventory.csv` 登记前缀 `3b5d9c230473e421` 一致） | 无 git 事件（`git ls-files --error-unmatch` 失败、`git log --all -- <该路径>` 空、`git status --porcelain artifacts/` 显示 `??`、`git check-ignore` rc=1 即未被忽略） | 无 | 无（不在版本控制内，无法定义删除提交）；zip 内条目时间戳区间 2026-08-21 21:43:26 → 2026-09-09 21:38:42（=13:38:41Z，与身份名同值） |

### 1.2 同名多形态之间的文件集合一致性

- V1 包（工作树 74 文件 vs `run/**` 70 份影子副本）：66 份在换行归一（CRLF→LF）后逐文件 sha256 与工作树相同，原始字节差异仅落在 `TASK_LEDGER.csv`、`TASK_LEDGER_V1_ARCHIVE.csv` 两文件的行尾（例：`run/perf-fix/P1-gaia/verify-clean/…/TASK_LEDGER.csv` 8133 字节 / 0 处 CRLF，工作树 8191 字节 / 58 处 CRLF，换行归一后差异 0 行）。4 份实质不一致，均为较早版本快照，可核对差异清单如下：
  - `run/arch_audit_p1/shadow/…`：42 文件，相对工作树缺 32（根文件 5：`05`/`06`/`07`/`08` + `TASK_LEDGER_V1_ARCHIVE.csv`；per-task 27），另有 14 文件内容不同（`00_READ_FIRST.md`、`CHANGELOG.md`、`TASK_LEDGER.csv`、`control-pack.json` + 10 个 per-task），台账 30 行且状态含 `IN_FLIGHT`、`PASSED_FINDING_OPEN`；
  - `run/rt001/base_shadow/…`：42 文件，缺 32，改动集与上一条同型；
  - `run/scif2001/shadow/…`：43 文件，缺 31（根文件 4：`06`/`07`/`08` + `TASK_LEDGER_V1_ARCHIVE.csv`；per-task 27），改动 15 件（含 `00_READ_FIRST.md`、`05`、`CHANGELOG.md`、`TASK_LEDGER.csv`、`control-pack.json`）；
  - `run/arch_audit_p1/shadow_base/…`：73 文件，缺 1（`08_FRONTDESK_HANDOVER_20260912.md`），`05`、`07` 两文件内容为较早版本。
  本组不推断成因，只登记缺失与差异集合。
- 模板（工作树 10 文件 vs 70 份影子 vs zip 内 2 份）：影子 70/70 逐字节相同；zip 内 `工程控制/cprun-v4-pack-template/`（10 条）与 `repo/工程控制/cprun-v4-pack-template/`（10 条）各 10/10 条目 CRC32 与工作树一致。
- G99：单一 zip 形态，无跨形态对象；zip 内 `memory.md`、`AGENTS.md` 各出现 2 条同名条目（size 与 CRC 相同），故 14556 条目对应 14554 唯一路径。

### 1.3 取证件与工作树/ git 的冲突（登记）

1. `digest/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909.md` 与 `pack_inventory.csv` 记该身份"首次提交 `f5f944a5` / 2026-09-12，最后提交 `e9547c02` / 2026-09-10"，与 `git log --reverse` 实测时序相反（实测首 `e9547c02`、末 `f5f944a5`）；同一对 SHA 的标签互换。`pack_events.md:68` 记"引入 `e9547c02`"与实测一致，即三份件内部亦不自洽。
2. digest 记该实例 `adds_n=6 / mod_n=39`；实测该目录全史 A=74 / M=30 / D=0。模板 digest 记 `adds_n=6`，实测 `4d3feaf4` 内该目录 A=10。
3. digest 对 V1 与模板各记"共 1 个实例"，未含 §1.1 表中 70 份 `run/**` 影子副本与模板的 2 份 zip 内嵌副本（`run/*` 由 `.gitignore:19` 排除，采集面不扫）。
4. `pack_lineage_table.md:47` 与 `pack_commit_link.csv` 对模板与 G99 均无提交对接行（原因见 §7.5）。
5. G99 的 digest 把其"00_READ_FIRST 开头"呈现为 V5 预发布控制包文本（`digest/AstroCS_AUDIT_REVIEWPACK….md:12-16`），实为 zip 内嵌 `artifacts/prerelease_v5/AUDIT_REVIEW/00_READ_FIRST.md`（sha256 前缀 `475c0bcc8577`，与 zip 内 V5 归档件同值）；G99 自身唯一入口件是根 `AUDIT_PACK_README.md`（659 字节）。

### 1.4 G99 并入本组的理由与判定依据

1. 指令面：`设计大纲/_evidence/packs/dispatch_groups.json` 中 G99 仅此一身份，前台指令并入本组；G07 报告已声明其未归组（`设计大纲/reports/packs/P-G07-V5发布控制与审核件.md:236`）。
2. 同一声明基线：G99 根 `AUDIT_PACK_README.md` 写"基线：git HEAD `789c5b6cec5e7b25f6c13e41c55a0a2e8e905cd9`（main，2026-09-09T21:36:43+08:00）"；本组 V1 包 `00_READ_FIRST.md:6` 声明同一 SHA 并注"HEAD/main/origin/main 一致"，`01_BASELINE_AUDIT.md:3` 同。
3. 同一时间窗口：`4d3feaf4`（模板收录）21:27:22 → `789c5b6c` 21:36:43 → G99 打包 21:38:41 → V1 包自述生成 2026-09-09T15:34:42Z（`00_READ_FIRST.md:4`）→ `d8c821db`（GOV-001 宪章冻结）2026-09-10 01:58:38 → `8e03d7da`（GOV-002 归档 + 建 `ACTIVITY_STATE.md`）02:22:41 → V1 首次入库 `e9547c02` 15:14:01。
4. 台账归属判定：G99 被采集的唯一台账 `artifacts/prerelease_v5/AUDIT_REVIEW/TASK_LEDGER.csv`（98 行）经核属 V5 系，而非本组任何一代：与工作树同名文件逐字节相同（sha256 前缀 `914fbd838ec80752`、10178 字节）；与 zip 内 V5 归档 `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/RELEASE_V5/AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828/02_TASK_LEDGER.csv` 逐格内容相同（98/98 号同、状态分布同为 `PASS 88/NOT_STARTED 7/BLOCKED 1/REVIEW_PENDING 1/IN_PROGRESS 1`），仅行尾不同（前者 CRLF 10178 字节、后者 LF 10079 字节，首个差异在第 82 字节）。与本包 57 号仅 7 号同字面（`BASE-001`、`CLI-001`、`DOC-001`、`GOV-001`、`P3-001`、`P3-002`、`WIN-001`）。
5. 内容互斥：zip 内 `AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909` 路径条目 0 条；zip 内亦无 `ASTROCS_PROJECT_CONSTITUTION.md`（宪章首次入库 `d8c821db` 2026-09-10 01:58:38，晚于 zip）。
6. G08 口径交叉验证（前台要求）：本组只读枚举得 `CONTROL_V6` 路径条目恰 46 条，全部位于 `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/CONTROL_V6/AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260820/…20260830/` 前缀（容器外 0 条），其中 `05_TASK_LEDGER.csv` 88 行状态全 `NOT_STARTED` → 与 G08 记的 46 份 V6 文件一致（`P-G08-V6系统重构.md:131`），且这 46 条不在本组 V1 包内。
7. 判定：G99 是本组 V1 包所依据基线（`789c5b6c`）的整仓审核快照件（自述"git archive HEAD 全量 + 工作树未提交增量"），不是控制包；归并属"同窗口同基线证据件挂靠"，不改变 3 个身份各自的独立判定。

---

## 二、包内文件地图

### 2.1 V1 包（74 文件 = 根 14 + `tasks/` 57 + `schemas/` 1 + `validators/` 2）

规格件 `NN_XXX.md` 共 9 件（行数为实测）与其职责（按各件自述标题）：

| 文件 | 行数 | 自述职责 |
|---|---:|---|
| `00_READ_FIRST.md` | 95 | `# 00｜唯一执行入口`：§1 本轮目标、§2 执行前硬门（G-GOV）、§3 基线保护、§4 启动顺序、§5 完成定义、§6 修订记录、§7 rev5 V2 重编与启动快照制、§8 rev6 重编 |
| `01_BASELINE_AUDIT.md` | 38 | `# 01｜基线审核结论`：P0（6 项）/P1/P2-P3/审核原则 |
| `02_GATES_AND_EXECUTION.md` | 23 | `# 02｜任务图、门禁与执行纪律`：门禁（6 门）/执行（6 条）/证据最小字段（12 项） |
| `03_AUDIT_PACKAGE_SPEC.md` | 15 | `# 03｜审核包规格`：结论枚举/白名单 9 类/禁止/必检 |
| `04_OWNER_DECISIONS_20260910.md` | 84 | 负责人三裁决（`:6`/`:17`/`:23`）+ 管理意见（`:28`）+ 真实数据验收指令 1–3（`:40`/`:48`/`:57`）+ 已知数据缺口（`:65`）+ 验收底线（`:75`） |
| `05_FINDINGS_REGISTER_20260911.md` | 620 | Findings 登记册：P0（`:14`）0 项、P1（`:19`）3 项、P2（`:52`）3 项、补 P1（`:84`）、附录 A（`:141`）/B（`:322`）/C（`:381`，C.1–C.9）/D（`:496`，D.0–D.7）；三级条目标题共 52 个 |
| `06_V2_PACK_DESIGN_20260911.md` | 75 | V2 设计总纲：三层 CI（`:9`）、双线与并行度（`:20`）、宪章符合性硬约束（`:32`）、V2 谱系草案 46 任务（`:41`）、门禁链（`:58`）、轮报义务（`:70`） |
| `07_FRONT_DESK_RULINGS_20260912.md` | 268 | 前台裁决留档：`:4` 自称"前台裁决唯一登记源"；R-01…R-14（`:22`–`:148`）、§2 不代裁项（`:157`）、§3 追认清单（`:170`）、R-15…R-17（`:189`/`:201`/`:216`）、R-18…R-20（`:235`/`:245`/`:255`） |
| `08_FRONTDESK_HANDOVER_20260912.md` | 88 | 前台交接：§1 现行执行模式（`:6`）、§2 本轮转绿门与提交对照（`:22`）、§3 在跑任务（`:44`）、§4 下一批（`:59`）、§5 并发会话风险（`:70`）、§6 项目最高约束（`:85`） |

其余槽位与数量口径：`control-pack.json`（1637 行，顶层键仅 `schema,id,title,workspace,max_parallel,lanes,gates,tasks`）、`TASK_LEDGER.csv`（58 行 = 表头 + 57）、`TASK_LEDGER_V1_ARCHIVE.csv`（31 行 = 表头 + 30）、`OWNER_BINDINGS.yaml`（55 行、18 个 owner）、`CHANGELOG.md`（119 行、rev1…rev6）、`tasks/` 57 个（文件名 = 任务号 `.md`，非 `NN_` 前缀，行数 31–73）、`schemas/evidence.schema.json`（89 行）、`validators/validate_control.py`（47 行）、`validators/selftest.py`（13 行）。
命名口径差异点（实测）：本包无 `templates/`、无 `scripts/`、无 `README.md`、无 `START_PROMPT.txt`、无 `SHA256SUMS`/`MANIFEST.json`、无 `baseline/`；台账一份为 `TASK_LEDGER.csv`（可被 `LEDGER_RE` 采集）、一份为带后缀的 `TASK_LEDGER_V1_ARCHIVE.csv`（同被采集）。

### 2.2 模板（10 文件）

`README.md`（57 行：CP 使用流程 + 字段速查 + 前台规则）、`control-pack.json`（142 行，`id: my-project-remediation`）、`TEMPLATE.control-pack.jsonc`（55 行注释骨架）、`evidence-template.json`（17 行，`schema: cprun/evidence-v1`）、`tasks/_TEMPLATE.md`（53 行，六节骨架：目标/输入/要做的事/产出/验收条件/证据报告/禁止事项）、`tasks/{AUDIT-001,CI-TEST,DOC-001,FIX-001,RELEASE-001}.md` 5 个示例任务。
无编号规格件（`NN_XXX.md` 0）、无台账 CSV、无 `validators/`、无 `schemas/`、无 `OWNER_BINDINGS.yaml`、无 `00_READ_FIRST.md`（实测目录清单）。

### 2.3 G99（zip，14556 条目）

顶层 25 个目录 + 22 条根文件条目；条目数前列：`run` 7581、`evidence` 2785、`lib` 1156、`engineering` 911、`reports` 571、`artifacts` 455、`tests` 278、`docs` 225、`工程控制` 212、`tools` 157、`ci` 41、`runtime` 31、`include` 22、`contracts` 20、`cli` 20、`packaging` 17、`modules` 16、`providers` 10、`repo` 10、`cmake` 7、`.github` 4、`scripts` 2、`logs` 1、`third_party` 1、`testdata` 1。
`工程控制/` 内嵌：V6.1（46 条）、V7.1（99 条）、V8.1（55 条）、cprun 模板（10 条）、`包规范.md`（1 条，4453 字节，与工作树逐字节相同、CRC32 `fbc58697` 相同）、`_control_packs/`（1 条 `.md`）。另有 `engineering/control/archive/2026-09-02_legacy_…` 855 条、`engineering/control/active/…V8_1…` 56 条（与 `工程控制/` 版条目名差 1：缺 `baseline/AstroCS_ALPHA3_MODULAR_REFOUNDATION_CONTROL_V7_1_20260902_FINAL3.zip`）、`repo/` 子树仅 10 条（全为 cprun 模板）。
zip 内 `TASK_LEDGER*.csv` 共 15 份（行数 98、98、88、88、67、67、62、62、62、54、36、36、191、191、191），其中仅 `artifacts/prerelease_v5/AUDIT_REVIEW/TASK_LEDGER.csv` 与 `工程控制/AstroCS_V7_…/TASK_LEDGER.csv` 形态符合采集正则（见 §7.5）。

---

## 三、任务结构

### 3.1 V1 包

- 任务数 57；任务号族 25 个（按 `task_id` 首段实测）：`CI` 11、`WCS` 4、`GOV`/`MOD`/`RT`/`SCI` 各 3、`AIO`/`ARCH`/`BASE`/`CLI`/`DOC`/`P1`/`P2`/`P3`/`REAL`/`STD`/`WIN` 各 2、`AUD`/`CON`/`CORE`/`DATA`/`PACK`/`PSF`/`UT`/`VIS` 各 1（合计 57）。子号形态两类：字母后缀（`RT-001A`、`RT-001B`、`MOD-001A`、`MOD-001B`、`CLI-001B`、`CI-001B`）与多段号（`ARCH-AUDIT-P1`、`BASE-UTIL-001`、`P1-HIPS-DIGEST-001`、`STD-F1-ADJ`、`SCI-F2-001`、`SCI-F3-001`、`SCI-ANCHOR-001`、`GOV-AGENTS-001`、`CON-COMMENT-001`、`DOC-CONV-001`、`UT-CLI-MAINT`、`WCS-PATH-001`、`CI-DATA-REG-001`、`CI-VER-CHK-001`、`CI-BACKEND-001`、`CI-BASELINE-001`、`CI-REPAIR-001`、`CI-REPAIR-002`、`CI-WIN-001`）。
- 台账表头（`TASK_LEDGER.csv:1`，10 列）：`task_id,title,owner_id,lane,depends_on,spec_ref,write_scope,timeout_sec,domain,status`；归档台账表头与之完全相同（实测），其 30 号全部为主台账子集（交集 30、归档独有 0）。
- 状态字面量集合与分布：主台账 `{PASSED:29, IN_PROGRESS:5, NOT_STARTED:23}`；归档台账 `{PASSED:9, PASSED_FINDING_OPEN:1, IN_FLIGHT:1, NOT_STARTED:19}`（后两个值在主台账 0 次出现）。
- 证据层另一套字面量：`schemas/evidence.schema.json` 的 `status` 枚举 = `PASS_REQUESTED|FAIL|BLOCKED_EXTERNAL`，`checks[].status` = `pass|fail|not_run`，`required` = 10 项（`task_id,owner_id,base_sha,status,changed_files,checks,commands,scope_clean,scientific_change,known_limits`，`final_sha` 不在 required）。
- 依赖与顺序声明：`depends_on` 非空 29/57（台账以 `|` 连接、`control-pack.json` 以数组表达，实测逐号 0 处不一致）；`requires` 仅 1 处（`CI-002 requires G-CI-FIX`）；机器校验依赖存在性与无环（`validators/validate_control.py:19-29`）；优先级区间 50–250，repo-write 串行队列在 `00_READ_FIRST.md:88-90`（`GOV-AGENTS-001` 250 → `CI-DATA-REG-001` 245 → `CI-VER-CHK-001` 240 → `WCS-PATH-001` 235 → `ARCH-TB-001` 230 → `CORE-RACE-001` 228 → `CON-COMMENT-001` 225 → `CI-WIN-001` 220 → `CI-BACKEND-001` 215 → `CI-REPAIR-002` 210）；`timeout_sec` 区间 600–259200；尾部依赖链：`CI-002`（12 前置）→ `REAL-001` → `VIS-001` → `DOC-001` → `AUD-001` → `PACK-001`，Windows 支链 `CI-002` → `WIN-000` → `WIN-001` → `DOC-001`。
- lane 分布（11 道）：`repo-write` 18、`rw-mod` 9、`rw-ci` 8、`read-only` 5（容量 4）、`rw-docs` 4、`rw-lib` 3、`rw-tools` 3、`rw-tests` 2、`owner` 2、`windows` 2、`realdata` 1；容量除 `read-only` 外均为 1；`max_parallel: 12`；`write_scope` 声明 50/57。
- owner 分布（18 个，实测）：`SA-CI` 9、`SA-DOC` 6、`SA-WCS` 5、`SA-RT` 5、`SA-AIO` 4、`FG-000`/`SA-P2`/`SA-CLI` 各 3、`OWNER`/`SA-GOV`/`SA-P1`/`SA-P3`/`SA-REAL`/`SA-WIN`/`SA-AUD` 各 2、`SA-PSF`/`SA-VIS` 各 1。
- domain 分布（12 值）：`science` 11、`ci` 11、`architecture` 11、`governance` 4、`performance` 4、`docs` 4、`testing` 3、`realdata` 3、`windows` 2、`audit` 2、`baseline` 1、`release` 1。
- per-task 规格件标题集共 6 种（实测）：42 份为 `目标/依赖/写入白名单/非目标与禁令/必须动作/验收/返回证据`；10 份与 2 份在其上加 `裁决依据（前台 rev6，见 07_FRONT_DESK_RULINGS_20260912.md）`（后者再加 `关联 finding`）；`tasks/WCS-003.md` 用 `背景（owner 裁决 2026-09-09）/目标/写入白名单/硬性要求/纪律/验收/返回证据`；`tasks/REAL-000.md` 无 `依赖` 节。

### 3.2 模板

5 个示例任务（`AUDIT-001`、`FIX-001`、`CI-TEST`、`DOC-001`、`RELEASE-001`）+ 1 骨架；3 lane（`repo-write` 1 / `read-only` 4 / `windows` 1）、`max_parallel 4`、2 门（`SOURCE-READY`、`CI-GATE`）、1 个 external（`ci/windows`，states `success|failure|pending`）。无台账 → 任务数/状态分布口径不适用（`pack_inventory.csv` 该行台账列为空，实测一致）。

### 3.3 G99（98 号，挂靠登记，非本组任务结构）

`artifacts/prerelease_v5/AUDIT_REVIEW/TASK_LEDGER.csv`：表头 8 列 `task_id,phase,depends_on,scope,required_commit,required_push,preferred_host,status`；状态 `{PASS:88, NOT_STARTED:7, BLOCKED:1, REVIEW_PENDING:1, IN_PROGRESS:1}`；`phase` 17 值（CODE 12、TEST 9、WINDOWS 9、PAR 8、CPU 8、SCI 7、ALG 7、ARCH 5、API 5、ISA 5、BENCH 5、LINUX 5、MON 4、RELEASE 4、DOC 3、REVIEW 3、CHECK 2、BASE 2）；`depends_on` 非空 97/98；`required_commit=yes` 90/98；`preferred_host` Linux 86 / Windows 12；23 个号族。其中 `REVIEW_PENDING` 在现行仓库校验器状态集合中不存在（`tools/quality/validate_task_ledger.py:42-43`），且 `AGENTS.md` 状态机映射行注明其为 V5 遗留、当前非法。

---

## 四、门禁与验收要求（标出处）

### 4.1 机器门清单

| 机器门 | 载体与条款 | 判据（实测枚举） |
|---|---|---|
| 包自洽校验 | `validators/validate_control.py`（47 行）| `:4`/`:9` 8 个必需文件（`00`–`03`、`TASK_LEDGER.csv`、`OWNER_BINDINGS.yaml`、`control-pack.json`、`schemas/evidence.schema.json`）；`:11` 两个 JSON 可解析；`:13` `schema=='cprun/v4'` 且 `workspace=='../..'`；`:15` 台账含 8 个必备列；`:18` 任务集去重且等于内建 CRIT 的 57 号；`:21` 依赖号存在；`:22-29` DFS 无环；`:30-36` owner 在 `OWNER_BINDINGS`、lane 在 `cp.lanes`、`timeout_sec>0`、`spec_ref` 文件存在、`repo-write` 任务必须有 `write_scope`；`:38` `control-pack` 与台账任务集相等；`:39-41` 逐号 `depends_on` 相等；`:42` `repo-write capacity==1`；`:43-45` `00`+`01`+`02` 合文必须含 `DRAFT_FOR_OWNER_REVIEW`、`FROZEN`、`P0/P1`、`Fatduck`、`call_count=1`、`NOT_READY_FOR_RELEASE` 六个字面量 |
| 校验器负例自测 | `validators/selftest.py`（13 行）| 变异负例（实测通过） |
| 启动前置 | `00_READ_FIRST.md:29` | 启动顺序第 1 步"运行两个 validators" |
| CI 面机器门 | 包外仓库工具链（`ci/checks.json`、`ci/known_failures.json`、`tools/quality/*`）| 由 11 个 CI 承接任务与 `07:82-88`（R-05）、`07:141-147`（R-13）、`07:189-200`（R-15 单写者）约束；包内无 CI 配置副本 |

当前工作树实测输出：`CONTROL_PASS tasks=57 files=74`、`SELFTEST_PASS`。
历史版本实测（`git archive` 解至 `/tmp` 后运行包内同版本校验器，未写入仓库）：`e9547c02` → `CONTROL_FAIL: control/ledger mismatch`（39 文件；台账 25 行 vs 图 29 任务，缺 `WCS-003`、`BASE-UTIL-001`、`P1-HIPS-DIGEST-001`、`ARCH-AUDIT-P1`）；`85a5fc60` → tasks=30 files=42；`e6b1ea87`、`178222a7` → tasks=57 files=73；`f5f944a5` → tasks=57 files=74。

### 4.2 门禁链与完成顺序

- 文档口径 6 门：`02_GATES_AND_EXECUTION.md:5-10` = `G-GOV`（GOV-001、BASE-001、GOV-002 通过；宪章 FROZEN、四项有明确值、supersession 唯一、dirty 全登记）、`G-SCI`、`G-CODE`、`G-CI`、`G-REAL`、`G-RELEASE`；`00_READ_FIRST.md:29` 启动顺序第 5–6 步为 `G-CODE→G-CI→G-REAL→G-RELEASE` + 独审/审核包/Owner 裁定。
- 机器口径 5 门（`control-pack.json` 实测）：`G-CI-FIX`（9 原子）、`G-SCI`（3）、`G-CODE`（4）、`G-CI-L1`（1）、`G-RELEASE`（2），共 19 个原子，原子形态一律 `{task, state:"passed"}`，无 `any` 嵌套、无 `external`/`evidence` 原子。
- 第三套门名（下一轮）：`06_V2_PACK_DESIGN_20260911.md:60-68` 写 `G-CI-L1/G-STD/G-SCI/G-CODE → G-CI-FINAL → G-REAL-L2 → G-FAT-L3 → G-RELEASE`。
- 门名一致性核对：`G-GOV`、`G-CI`、`G-REAL` 不在 `control-pack.json`；`G-CI-FIX`、`G-CI-L1` 不在 `02_GATES_AND_EXECUTION.md`；`00_READ_FIRST.md:29` 引用的 `G-CI`、`G-REAL` 在 JSON 中不存在；4 个门（`G-SCI`、`G-CODE`、`G-CI-L1`、`G-RELEASE`）无任何任务以 `requires` 引用（唯一 `requires` 是 `CI-002 → G-CI-FIX`）。包内自代际演变（实测）：`85a5fc60`（rev3）为 6 门 + 30 任务 + 5 lane + `max_parallel 4`；`178222a7`（rev6.1）起为 5 门 + 57 任务 + 11 lane + `max_parallel 12`。
- 宪章 §14.5 完成顺序在本包中的角色：宪章 `ASTROCS_PROJECT_CONSTITUTION.md:558`（§14.5）的 `:567` 规定六步顺序（任务提交全部完成 → GitHub CI → Linux 最终 SHA 真实数据流终验 → Agent 图像初审 → Windows/Fatduck 复验 → 汇总和打包）并含"不得在真实数据终验前宣布控制包完成"。全包实测：该六步句的任何片段（`任务提交全部完成`、`真实数据流终验`、`汇总和打包`）在包内 0 命中；替代实现是三处结构 —— `00_READ_FIRST.md:29` 启动顺序第 5–6 步、`02_GATES_AND_EXECUTION.md:8-10` 的 `G-CI/G-REAL/G-RELEASE` 语义、以及尾部依赖链（`CI-002 → REAL-001 → VIS-001 → DOC-001 → AUD-001 → PACK-001`，`control-pack.json` 实测）。对 §14.5 的逐条引用集中在同节另三条：`07:25-26`（R-01 tracked 写串行、保留双写 lane 即违反 §14.5）、`07:194`（R-15）、`07:205`（R-16 lane 与被约束对象不等价）、`07:220`（R-17 不设频繁人工 checkpoint）、`07:265`（R-20）、`00:81`、`CHANGELOG.md:37`、`tasks/CI-REPAIR-002.md:25`、`tasks/GOV-AGENTS-001.md:10`/`:18`（把 `状态机` 要素映射到 §14.5 控制包完成顺序）、`05:334`、`05:375`。
- 条款引用密度（全包实测）：`§17` 33 次、`§14.4` 17 次、`§14.5` 12 次、`§1.1` 10 次、`§10.5` 10 次、`§12.3` 8 次、`§1.2` 5 次、`§7.3` 4 次；引用文件名 `ASTROCS_PROJECT_CONSTITUTION.md` 的文件共 7 个（`00`、`06`、`07`、`08`、`TASK_LEDGER_V1_ARCHIVE.csv`、`tasks/GOV-001.md`、`tasks/GOV-AGENTS-001.md`）。

### 4.3 状态机口径（四套并存）

1. 台账层：`PASSED/IN_PROGRESS/NOT_STARTED`（主）＋ `PASSED_FINDING_OPEN/IN_FLIGHT`（归档）。
2. 证据层：`PASS_REQUESTED/FAIL/BLOCKED_EXTERNAL` + check 级 `pass/fail/not_run`（`schemas/evidence.schema.json`）。
3. 结论层：`READY_FOR_OWNER_REVIEW|NOT_READY|BLOCKED_EXTERNAL|PACKAGE_INVALID`（`03_AUDIT_PACKAGE_SPEC.md:3`、`00_READ_FIRST.md:11`）。
4. 包级活动层：`ACTIVE/ARCHIVED_SUPERSEDED/ARCHIVED_DISARMED/REFERENCE_TEMPLATE`（`工程控制/ACTIVITY_STATE.md:15`）。
另有 Worker 语义限定：`02_GATES_AND_EXECUTION.md:23` 末句"Worker PASS 仅表示申请前台验收"；`tasks/GOV-AGENTS-001.md:18` 记 V5 期任务级状态链（含 `REVIEW_PENDING`），该字面量在现行 `tools/quality/validate_task_ledger.py:42-43` 的 `STATUSES` 中不存在。
与仓库现行机器门的关系（实测）：V1 两本台账与 G99 的 98 行台账均不满足 `tools/quality/validate_task_ledger.py:37-40` 的 `REQUIRED_COLUMNS`，三条分别返回 `TASK_LEDGER_FAIL: ledger columns mismatch` → 现行台账机器门不覆盖这三本台账中的任何一本。

### 4.4 waiver / 豁免

- 不可 waiver 清单：`02_GATES_AND_EXECUTION.md:18`（P0/P1、Windows build/test/package、manifest/hash、真实数据完整性）；`01_BASELINE_AUDIT.md:36-38`（历史 PASS、旧 SHA、Worker 完成消息都不是当前候选 PASS；P0/P1 不得 waiver）；`04_OWNER_DECISIONS_20260910.md:21` 裁决 2 末句"不得仅凭「BASE 预存失败」进入审核包豁免"。
- 放行登记约束：`07:82-88` R-05（known-failures 基线不得扩容，只减不增）、`07:141-147` R-13（CI 镜像依赖与工具链 PATH 属配置缺陷，不是数值门禁放宽；不得把 `waivable` 改回 true）、`07:189-200` R-15（`ci/checks.json` 单写者=前台）。
- 反面登记：`05:86-103` STD-F7（全量 C++ 测试矩阵 7 项检查全部 `waivable=true`）、`05:104-110` STD-F8（windows-main 三门已不可豁免）、`07:78` R-04 表内 `UT-CPU-AVX512 = SKIPPED(waivable)`。
- 包内无 waiver 登记文件（无 waiver 专用 CSV/JSON，实测 grep）；科学决策任务的例外通道是 `BLOCKED_EXTERNAL`（`02_GATES_AND_EXECUTION.md:19`、`07:157-169`）。

### 4.5 审核包与提交要求

- 审核包：`03_AUDIT_PACKAGE_SPEC.md:7` 白名单 9 类（宪章与活动 L0/L1、tracked source manifest、task/state/commit ledger、追踪链、AST/API/ABI/导出符号与 DAG/trace/call-count、双平台 CI_RESULT 与 build provenance、Linux real-final 与 Fatduck 脱敏摘要与资源汇总、candidate product manifest/SBOM/licenses/SHA256、findings 与最小失败复现）；`:11` 禁止（`.git`、旧控制包集合、build/cache/worktrees、raw testdata/Gaia/FITS/完整 HiPS tiles、大索引、core、凭据、完整重复日志、用户绝对路径、重复源码；大件只记 logical ID/size/sha256/生成命令/保留位置）；`:15` 必检（空 staging 目录按白名单复制、拒绝 symlink/special file/path traversal、生成 manifest 与 `SHA256SUMS`、fresh extract 后复验、全部证据绑定 final SHA + candidate digest + 配置 hash + 输入数据清单 hash）。
- 提交面：`02_GATES_AND_EXECUTION.md:15`（一 task 一个可验证 commit；科学/架构/性能/文档不混提）、`:16`（前台复跑验收、检查 `write_scope` 与 diff 纯度、精确 add、commit/push、fetch 后核对 HEAD/main/origin/main 三 SHA）、`:17`（所有命令设 timeout 并记录 cwd/argv/start/end/rc/stdout/stderr/SHA；heavy 同 run ID 资源监控）、`00_READ_FIRST.md:25`（SubAgent 不 commit/push/改总台账）、`08_FRONTDESK_HANDOVER_20260912.md:22-42`（门 → 提交对照表 10 行：`AGENTS-GOV`=`b40c8a49`、`DATA-ARTIFACTS`=`2fdd6626`、`CON-COMMENTS`+`CON-FULL-INTEGRATION`=`1dc5447d`、`UT-VERSION`=`19bdf1ab`、`CTEST-P1WCS-ASTROPY-CROSS`=`af56020c`、`CTEST-P1001-REAL-NODES`=`43f0c429`、`THREAD-BUDGET`+`UT-ARCH`=`da74dcad`、`WIN-BUILD-RELEASE`/`WIN-PACKAGE-CANDIDATE`=`27909ddf`、`CTEST-P1WCS-STD-F1-BRIDGE`=`8069d85d`、`UT-GAIA-ZLIB`=`0c9130d6`）。
- 证据最小字段：`02_GATES_AND_EXECUTION.md:21-23` 12 项，与证据 schema 的 10 个 required 字段一致（另含可选 `final_sha`、`known_limits` 等）。
- Owner 侧规则：`OWNER_BINDINGS.yaml` 18/18 owner 携带同一组规则 `[no_commit, no_push, respect_write_scope, report_unrun_as_not_verified]`（实测）。

---

## 五、设计意图转述（包自述与负责人裁决痕迹）

- 目标（`00_READ_FIRST.md:11`）：以负责人提交的 `ASTROCS_PROJECT_CONSTITUTION.md` 为目标上位规范，先消除治理状态冲突，再按原子任务修复科学正确性、真实模块化、AIO、并行资源、CLI、CI、真实数据与发布差距；最终只产出 `READY_FOR_OWNER_REVIEW` 或诚实的 `NOT_READY/BLOCKED_EXTERNAL/PACKAGE_INVALID`，"任何 Agent 不得宣布发布"。
- 包自述禁止项：`00:23-25`（不得 reset/stash/clean、不得覆盖或顺手收编预存修改、不得新建 branch/worktree/clone、SubAgent 不 commit/push/改总台账）；`02:19`（科学决策任务不得选择方便方案）；`03:11`（审核包禁止内容）；`01:36-38`（历史 PASS/旧 SHA/Worker 完成消息不作为当前候选 PASS）；`07:13-14`（裁决不得修改、放宽或重新解释宪章，与宪章冲突条目即行作废）；`00:17`（"Agent 不得代裁"）；`07:216-229` R-17（门禁不得成为真实修复的阻塞项，但"不得为过门改判据"）。
- 宪章在本包中的角色（首批性证据）：`00:13-17§2 把"宪章状态 FROZEN + 四项裁决值 + supersession 明确 + 根白名单登记"设为一切写任务前置硬门（`GOV-001` 未通过时其余写任务全部等待）；`07:3`、`08:85-88` 重申宪章为上位约束；`validators/validate_control.py:44` 把 `FROZEN`、`DRAFT_FOR_OWNER_REVIEW` 列为包内文本必须出现的字面量（即宪章状态被写进机器门）。首批性核对（实测）：上一代三包目录（V8.1、V6.1、V7.1）内 `ASTROCS_PROJECT_CONSTITUTION` 0 处引用，本包 7 个文件引用；唯一在包外的前代引用点是 V8.1 归档件 `engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/README_ARCHIVED.md`，而该件由本包 GOV-002 的提交 `8e03d7da` 创建。
- 基线自述与实测对照：`00:15` 记录生成当时宪章 sha256 为 `37fefdcc64…`（第 4、651–660 行仍为 `DRAFT_FOR_OWNER_REVIEW`）、约束文件 sha256 为 `b3c66eb542…` 仍声明 `ACTIVE_NORMATIVE`。本组复算：`git show 789c5b6c:AstroCS_ENGINEERING_CONSTRAINTS.md` 的 sha256 = `b3c66eb542a1f03dd82e26279299fe8210c68c488cc2c77b4c4cf9510d4e7089`（与登记值相同）；`ASTROCS_PROJECT_CONSTITUTION.md` 在 `789c5b6c` 中不存在，首次入库为 `d8c821db`（2026-09-10 01:58:38）且该 blob 与 `f5f944a5` 版、当前工作树版同为 sha256 `0f25544e…`（即仓库内从未出现 `37fefdcc…` 版）→ 该指纹与 `00:21-22` 的 dirty 指纹 `e271debb…`、index 指纹 `eb01dce9…` 均不可复现，判"未证实"。
- 负责人裁决痕迹：`OWNER_BINDINGS.yaml`（FG-000 前台调度/机器验收/Git 与打包；OWNER 仅项目负责人可执行的冻结与科学裁决；SA-GOV/WCS/PSF/AIO/P1/P2/P3/RT/MOD/CLI/CI/CORE/DATA/REAL/VIS/WIN/DOC/AUD 分工）；`04_OWNER_DECISIONS_20260910.md` 三裁决 + 三条真实数据验收指令 + 验收底线；`06:1-7` 负责人 V2 设计指令（含"更严格设计工程包；ASTROCS_PROJECT_CONSTITUTION.md 必须完全符合"）与 `:7`"V1 剩余任务并入 V2 谱系执行"；`07_front…` R-01…R-20（R-02 STD-F1 采方案 b、R-05 基线不得扩容、R-07 AGENTS.md 采映射式恢复、R-13 不放宽数值门、R-15 checks.json 单写者、R-16 按实际写域并行、R-17 门禁不得阻塞、R-18 停用 cprun/CP 框架、R-19 共享工作树 + 前台统一合并、R-20 热点文件三重处置）；`08:6-21` 现行执行模式（负责人指令已写入 agent persona）、`08:39-40` 写"裁决 R-01 … R-31"；`05` 内联裁决 R-22（`05:459`）、R-24（`05:465`）、R-26（`05:449`/`:580`）、R-28-b（`05:434`/`:573`）、R-29（`05:489`）、R-31（`05:589`/`:597`）。
- 执行面留痕（不入库）：`run/pack_state/WORK_LEDGER.json` 的 `mode` = "direct-subagent（CP 框架已按 R-18 停用）"、`base_sha = 09ca8aec`、`updated_utc = 2026-09-12T09:51:21Z`，`completed` 6 项（`GOV-AGENTS-001`→`b40c8a49`、`CI-DATA-REG-001`→`2fdd6626`、`CON-COMMENT-001`→`1dc5447d`、`CI-VER-CHK-001`→`19bdf1ab`、`WCS-PATH-001`→`af56020c`、`BASE-UTIL-001`→commit 为 null 并注"只读归因，零 tracked 改动；产出附录 C.1/C.2"），`running` 10 项（含 `CORE-RACE-001`、`CI-WIN-001`、`ARCH-TB-001`、`STD-F1-ADJ`、`SCI-F3-001`、`ARCH-AUDIT-P1`、`MON-FIX-001`、`UT-BACKEND-FIX-001`、"CI 平台域合并"、"孤儿 worker SCI-F3-001"）。
- 模板件自述意图：`README.md:1-3`（配套"控制包执行模式（CP）"preset；前台=控制面只操作 `cp` 工具；控制器=唯一调度权威；Worker SubAgent 干活）、`README.md:5-7`（复制本目录 → 改 `id`/`workspace` → 按任务改 `tasks/` → 注册）、`README.md:19-20`（前台仅派发与简审，不得亲自执行）。收录提交消息（`4d3feaf4`）自述"owner 指定审核包随附件"，源为仓库外目录 `/workspace/Local search/cprun-v4-pack-template`。
- 模板槽位 → V1 包实例化对应（逐项实测）：
  - 已实例化：`schema`（同为 `cprun/v4`）、`id`（`my-project-remediation` → `astrocs-constitution-alignment-v1`）、`title`、`workspace`（`../my-project` → `../..`）、`max_parallel`（4 → 12）、`lanes`（3 → 11；新增 `owner`、`realdata` 与 6 条 `rw-*`，拆分自 `repo-write`，保留 `read-only` 容量 4 与 `windows` 容量 1）、`gates`（2 → 5）、`tasks`（5 → 57，全部带 `spec` 指向 `tasks/<id>.md`）、`retry.max_attempts/on_exhausted`（V1 统一为 `2/hold`，模板为混用值）、`evidence.checks`（模板混用 `build/unit-tests` 等 → V1 统一为 `scope/acceptance/provenance` 57/57）、`write_scope`（50/57 实例化，`validators/validate_control.py:36` 只对 `repo-write` 强制）、`requires`（模板 2 处 → V1 仅 1 处）。
  - 未实例化（模板有、V1 无）：顶层 `externals` 槽（模板 `ci/windows`）→ V1 的 `control-pack.json` 无该键，故 `CI-GATE` 型 external 原子用法未落地；`gates` 中的 `any` 嵌套原子未使用；`TEMPLATE.control-pack.jsonc` 注释骨架件未使用；`evidence-template.json`（`cprun/evidence-v1`，字段 `run_id/node_id/attempt/dispatch_token/result/summary/checks/artifacts/changed_paths`）未被沿用，V1 改立自有 `schemas/evidence.schema.json`（required 10 项，无 `run_id`/`node_id`/`attempt`/`dispatch_token`，状态枚举改为 `PASS_REQUESTED|FAIL|BLOCKED_EXTERNAL`）；`tasks/_TEMPLATE.md` 的六节骨架（输入/要做的事/产出/验收条件/证据报告/禁止事项）被 V1 的七节结构替换（仅"目标"与"验收"两节同名）。
  - V1 新增而模板无对应槽位：两本台账 CSV、`validators/`（含负例自测）、`CHANGELOG.md`、`01`–`08` 八类规格与裁决/登记件、18-owner 绑定文件。
- G99 自述意图（`AUDIT_PACK_README.md` 逐条）：基线 `789c5b6c…`（main，2026-09-09T21:36:43+08:00）；构成为 git archive HEAD 全量 + 工作树未提交增量，并点名残留为 NOISE-IMPL（`lib/snr_estimator` 模块面 + `tests/unit/p1_noise` + 根 CMakeLists 追加块，"未验收未提交，状态如实保留"）；排除大体积观测数据、编译产物、运行索引、影子树快照、`.git`；证据面为 `evidence/`（含 `TASK_STATE`、`STATE_RECONCILIATION.csv`、`COMMIT_LEDGER.jsonl`）、`reports/`、`artifacts/`、`工程控制/`（含 `_control_packs` zip 原件）、`run/local/**`、`docs/`；并登记"旧控制包状态：V7 MODULAR REFOUNDATION 线 disarm 终止于 42/140（memory.md item 9）"。该件无目标/禁止项/门禁条款，非控制包。另 zip 内 `artifacts/prerelease_v5/AUDIT_REVIEW/MANIFEST.json` 记 `package AstroCS-audit-59649edc0480`、`version 0.9.0-alpha.1`、`commit 59649edc0480a4e663ed37bd3e56215ba729526a`（`59649edc` 实测为 2026-08-30 17:50:55 的 WIN-007 冻结 manifest 提交）→ 同一 zip 内并存两个时点的"被审对象"声明（8-30 的 V5 候选与 9-09 的 main 基线）。

---

## 六、代际差异表（V1 包 vs 上一代 V8.1 / 模板；逐项可核对）

| 维度 | 上一代 `AstroCS_ALPHA0.11.0_EXISTING_WORKSPACE_CI_CONTROL_V8_1_20260905` | 本组 V1 包 | 模板 |
|---|---|---|---|
| 存形态 | 4 形态同身份：zip 原件（`工程控制/_control_packs/…zip`，56 文件，sha256 前缀 `77a4b03a622c63cc`）+ 工作树解包件 56 + `engineering/control/active/…` 镜像 + `history/a4fdee3f/…` 复原 57；另有 `engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/` 归档 56 | 解包件 1（74 文件）+ 影子树 70；无 zip、无归档、无历史复原 | 解包件 1（10 文件）+ 影子树 70 + zip 内嵌 2 份 |
| 编号规格件 | 15 件（`00`–`15`，无 `05`）+ `README.md`（9 行）+ `00_READ_FIRST.md`（52 行） | 9 件（`00`–`08`）+ `CHANGELOG.md`；无根 `README.md` | 0 件（仅 `README.md` 57 行） |
| 任务承载 | `tasks/` 4 个批次文件（`01_WORKSPACE_ADOPTION_TASKS.md` … `04_FATDUCK_AND_RELEASE_TASKS.md`） | `tasks/` 57 个 per-task 文件（6 种标题集） | `tasks/` 5 示例 + `_TEMPLATE.md` |
| 台账 | `CONTROL_TASK_LEDGER.csv` 36 行 / 12 列（`task_id,wave,owner,mode,depends_on,goal,allowed_paths,acceptance_command,timeout_s,heavy,gate,status`），状态 36/36 `NOT_STARTED` | `TASK_LEDGER.csv` 57 行 / 10 列 + `TASK_LEDGER_V1_ARCHIVE.csv` 30 行；状态 `PASSED 29/IN_PROGRESS 5/NOT_STARTED 23`，归档另含 `PASSED_FINDING_OPEN`、`IN_FLIGHT` | 无台账 |
| 号族命名 | 前缀式（`V8-CI-*`、`V81-ADOPT-*`、`V8-RT-*`…，与包版本号绑定） | 域前缀式（`CI/WCS/MOD/RT/SCI/STD/ARCH/…`）+ 字母子号 | 通用示例号 |
| 门禁载体 | `GATE_REQUIREMENTS.csv`（列 `gate,required_tasks,automatic_result,blocks,manual_owner_action`；5 门 `G-ADOPT/G-CI/G-FIX/G-FAT/G-REL`，required_tasks 以 `|` 连接）+ `06_TASK_GRAPH_AND_GATES.md` | `control-pack.json` 的 `gates`（5 门 19 原子）+ `02` 文档 6 门 + `06` 另一套 7 门名（三套不同集） | `control-pack.json` 2 门（含 `any` 嵌套与 `external` 原子） |
| 机器门 | `validators/` 4 件（`validate_control.py`、`selftest.py`、`build_manifest.py`、`next_tasks.py`）+ `ci/` 8 件种子配置（`checks.schema.json`、`checks.seed.json`、`ci_result.schema.json`、`host_layout.json`、`impact_map.seed.json`、`publish_policy.json`、`toolchain.policy.json`、`actions.lock.template.json`） | `validators/` 2 件 + `schemas/evidence.schema.json`；无 `ci/` 种子件 | 无 validators；`evidence-template.json` 1 件 |
| 完整性与自描述 | `SHA256SUMS` + `CONTROL_MANIFEST.json`（逐文件 path/size/sha256）+ `CURRENT_CHECKPOINT.json` + `START_PROMPT.txt` + `VERSION` + `baseline/` 8 件（含 V7.1 zip 原件与 `V7_1_STATIC_TASK_LEDGER.csv` 镜像）+ `templates/` 7 件 + `LUNA_INDEPENDENT_REVIEW.md` | 上述全缺（无 SHA256SUMS/MANIFEST/START_PROMPT/VERSION/baseline/templates/LUNA 件） | 上述全缺（另无 `00_READ_FIRST`） |
| owner 绑定 | `05_FIXED_SUBAGENT_BINDINGS.yaml` 13 个 id | `OWNER_BINDINGS.yaml` 18 个 id，rules 四项 18/18 相同 | 无 |
| 上位规范引用 | 包内 `01_FROZEN_CONSTRAINTS.md`（约束在包内自带）；`15_SUPERSESSION_NOTICE.md` 只作废 V8.0 | 直接引用仓库根冻结宪章（7 文件引用、`§14.5` 等条款级引用 12 类计数见 §4.2）；无包内 supersession 件 | 无宪章引用 |
| 图规模与执行参数 | 无 lane 图、无 `max_parallel` 字段（走 manifest + checkpoint 口径） | 57 任务 / 11 lane / `max_parallel 12` / `retry {2, hold}` 57/57 / `review: foreground` 57/57 | 5 任务 / 3 lane / `max_parallel 4` / review 混用 `foreground|machine` / retry 混用 |
| 自述生成 → 首次入库间隔 | 自述 2026-09-05；入库 `a4fdee3f` 2026-09-05（同日入库，57 文件复原件） | 自述 2026-09-09T15:34:42Z；入库 `e9547c02` 2026-09-10 15:14:01 → 约 23.7 小时空窗（期间内容不在任何提交中，G99 zip 内 0 条本包条目可反证） | 自述无时间戳；入库 `4d3feaf4` 2026-09-09 21:27:22，此后 0 修改 |
| 台账能否过现行仓库机器门 | 12 列表头不满足 `validate_task_ledger.py:37-40`（FAIL） | 10 列表头同样 FAIL（两本均 FAIL） | 无台账，不适用 |

---

## 七、执行留痕三方核对

### 7.1 口径（因 `pack_commit_link.csv` 对本组只有 1 行，另两身份自算）

1. 账：V1 = `TASK_LEDGER.csv` 57 号；模板 = 无台账（0 号）；G99 = zip 内 `artifacts/prerelease_v5/AUDIT_REVIEW/TASK_LEDGER.csv` 98 号（挂靠）。
2. 据（git）：`git log --all` 全部 refs 的 subject+body（实测 2047 条，`git log --all --oneline | wc -l`；取证件索引 `设计大纲/_evidence/commits/index.jsonl` 为 1988 条），对任务号做整号字面匹配（前后不接字母数字），另按本包在库窗口（提交者日期 ≥ 2026-09-10，实测 262 条）二次限定；并区分"交付型 subject"（subject 头部 `type(scope):` 的 scope 段含该号）。
3. 据（留痕）：`evidence/`、`artifacts/`、`reports/`（tracked）+ `run/`（不入库工作区）+ `run/pack_state/WORK_LEDGER.json`。
4. 重号处理：V1 ∩ V5 = 7、∩ V6 = 9、∩ V6.1 = 10，凡 tracked 留痕路径归属前代目录（`prerelease_v5`、`refactor`、`v6_1_rework`、`v8_1_ci_control`）者不计本包。

### 7.2 V1 包分类结果（实测计数）

| 分类 | 数量 | 清单 |
|---|---|---|
| 一致（完成态 + 交付型 subject）| 31 | `PASSED` 26 号（例：`WCS-003`、`CI-001B`=`a67bbc83`、`STD-REG-001`、`CI-REPAIR-001`=`830b38c0` 等）+ `IN_PROGRESS` 5 号全部（`STD-F1-ADJ`=`8069d85d`、`SCI-F2-001`=`54287b48`、`SCI-F3-001`=`e89ce10e`、`GOV-AGENTS-001`=`b40c8a49`、`ARCH-TB-001`=`da74dcad`） |
| 一致（完成态，交付面为只读归因/登记面）| 2 | `ARCH-AUDIT-P1`（整号出现在 `59fdeab3`、`eb315b20`、`e9547c02` 的 subject，号不在 scope 段）、`BASE-UTIL-001`（`WORK_LEDGER.json` 记 commit=null、只读归因） |
| 有账无据 | 1 | `BASE-001`（台账 `PASSED`；窗口内唯一 subject 提及为登记件提交 `45695873`；其被引用证据 `evidence/BASE-001/freeze_snapshot_r1.json`（`工程控制/ACTIVITY_STATE.md:54-55`）在工作树与 git 全史均不存在；同型 staged 证据仅存于不入库的 `run/cp_runs/Rmtuajdhv73d430/BASE-001/staged_evidence/`（9 件），且该 run 在 `ACTIVITY_STATE.md` 中被记为 V7 包所属，与本包引用的 run `Rmtucy2cqced995` 不同） |
| 有交付提交、台账未记（NOT_STARTED）| 7 | `SCI-ANCHOR-001`=`3fd9e5e2`(18:47:08)、`CI-DATA-REG-001`=`2fdd6626`(16:58:19)、`CI-VER-CHK-001`=`19bdf1ab`(17:39:54)、`WCS-PATH-001`=`af56020c`(17:45:55)、`CORE-RACE-001`=`43f0c429`(18:22:31)、`CON-COMMENT-001`=`1dc5447d`(17:39:54)、`CI-WIN-001`=`27909ddf`(18:01:08)（日期均 2026-09-12；台账末次改动提交为 `a628bc21` 16:46:22，`control-pack.json` 末次改动 `178222a7` 16:35:58，7 条交付全部晚于两者）；其中 6 号是 `G-CI-FIX` 的原子成员 |
| 有据无账（规格点名 + 提交或目录留痕，台账无该号）| 8 | `08_FRONTDESK_HANDOVER_20260912.md:44-58`（§3 在跑）与 `:59-68`（§4 下一批）：`MON-FIX-001`（`run/mon_fix_001` 在案 + 提交 `09ca8aec` 提及 + `WORK_LEDGER.running`）、`UT-BACKEND-FIX-001`（`run/ut_backend_fix_001` + `WORK_LEDGER.running`，整号提交 0）、`P3-PROJ-FAILFAST-001`（提交 `56fe14c1` 提及，无同名 run 目录）、`CLI-INPUT-GATE-001`（`run/cli_input_gate_001`，整号提交 0）、`TEST-DISCRIM-001`、`CLI-COMPLETE-GATE-001`、`CLI-DEFECT-001`、`TEST-MUTATION-GATE-001`（后 4 号既无整号提交也无同名目录） |
| 零命中且台账未开始（自洽）| 8 | `RT-001A`、`RT-001B`、`MOD-001A`、`MOD-001B`、`CLI-001B`、`WIN-000`、`DOC-001`（其 6 条整号命中全部落在 2026-08-28→09-03，属前代同号）、`PACK-001` |

留痕面总览（实测）：本包无 `evidence/<包>/` 目录（`evidence/` 仅 `ciqa`、`refactor`、`v6_1_rework`、`v8_1_ci_control` 四项，全部属前代），`artifacts/` 下无本包目录；27/57 号在 `run/` 有同名或号变形目录（mtime 区间 2026-09-10 → 2026-09-15，不入库）；另有 `run/cprun/CI-VER-CHK-001`、`run/pack_state/`（`WORK_LEDGER.json`、`DISPATCH_LOG_rev6.json`、`ROUND_REPORT` 类件）、`run/ci_repair/round1..4`、`run/Rmtvuwx3i0c5cb8/P1-001`。包目录自身 11 次提交的内容分工：`e9547c02`（首入）、`85a5fc60`（rev3）、`f8778bbb`（05 并入 round1）、`e6b1ea87`（rev6 重编）、`178222a7`（rev6.1 拆道）、`57f7bd80`/`a628bc21`/`09ca8aec`/`56fe14c1`（05/07 续写）、`f9e4d921`（07 追加 R-18…R-20）、`f5f944a5`（新增 08）。

台账状态随提交序列的变化（实测逐提交重算）：`e9547c02` 25 行全 `NOT_STARTED` → `85a5fc60`/`f8778bbb` 30 行（9/1/19/1，含 `PASSED_FINDING_OPEN` 与 `IN_FLIGHT`）→ `e6b1ea87` 57 行（29 PASSED / 27 NOT_STARTED / 1 IN_PROGRESS）→ `178222a7` 57 行（30 / 26 / 1）→ `a628bc21` 起至 `f5f944a5` 57 行（29 / 23 / 5）。即末次台账改动之后新增的 7 条交付型提交未回写 `status` 列。

### 7.3 57.9% 命中率的复核（前台指定核查项）

1. 工具口径复现成功：`pack_commit_link.csv` 记 V1"57 / 33 / 57.9% / 125 提交"，用 `设计大纲/_tools/07_pack_commit_link.py` 的取号与变体逻辑重跑，得同一 `33/57 = 57.9%`。
2. 整号口径实测：全史 50/57 = 87.7%；限本包在库窗口 49/57 = 86.0%。
3. 两套命中集合的实测对称差为 27 号（工具命中 33、整号命中 50、共同命中 28；净差 17 = 漏 22 − 误计 5）：
   - 工具未命中而整号命中 22 号（假阴性）：`ARCH-AUDIT-P1`、`ARCH-TB-001`、`BASE-UTIL-001`、`CI-BACKEND-001`、`CI-BASELINE-001`、`CI-DATA-REG-001`、`CI-REG-002`、`CI-REPAIR-001`、`CI-REPAIR-002`、`CI-VER-CHK-001`、`CI-WIN-001`、`CON-COMMENT-001`、`CORE-RACE-001`、`DOC-CONV-001`、`GOV-AGENTS-001`、`SCI-ANCHOR-001`、`SCI-F2-001`、`SCI-F3-001`、`STD-F1-ADJ`、`STD-REG-001`、`UT-CLI-MAINT`、`WCS-PATH-001`；
   - 两侧均未命中 2 号：`PACK-001`、`WIN-000`。
   成因可核对：`设计大纲/_tools/01_build_commit_evidence.py:71` 的 `RE_TASKID` 只识别 `[A-Z]{2,6}-\d{2,4}` 一类单段号，多段号从未进入 `index.jsonl`（1988 条）的 `ids` 字段（`CI-BACKEND-001` 的字母段 7 字符超限；`ARCH-AUDIT-P1` 尾段非纯数字），`07_pack_commit_link.py:12-13` 直接以该字段建索引 → 这类号在本组包中恒为未命中。
4. 反向误计 5 号（假阳性）可核对：`07_pack_commit_link.py:22-26` 的 `variants()` 把子号折叠到母号，`RT-001A`/`RT-001B`→`RT-001`、`MOD-001A`/`MOD-001B`→`MOD-001`、`CLI-001B`→`CLI-001`；这 5 号整号在全部提交消息中实测 0 命中（`git log --all --grep` 与各号整号正则均为空）却被计为命中，且台账状态全为 `NOT_STARTED`。同型折叠另有 `P1-001→P-001`、`P2-001→P-002`、`P3-001→P-003`、`P1-HIPS-DIGEST-001→P-001`、`CI-001B→CI-001`（`CI-001B` 本身另有整号命中 `a67bbc83`）。
5. 关于"未命中样例 ARCH-AUDIT-P1、CI-BACKEND-001 是否子号/回填"的核查结论：两号都在 57 号台账内且各有独立 per-task 规格件（`tasks/ARCH-AUDIT-P1.md`、`tasks/CI-BACKEND-001.md`），非子号（无母号同名条目）、亦非回填号（分别首次出现在 `e9547c02`/`e6b1ea87` 台账中，`git show` 实测）；其"未命中"完全是采集正则所致（第 3 条）。
6. 完成率的两组事实并存：按 `validators/validate_control.py:5` 的 CRIT 集，57 号中 29 号 `PASSED`；按门原子回灌，`G-CI-FIX` 9 原子为 8 `NOT_STARTED` + 1 `IN_PROGRESS`、`G-SCI` 3/3 `IN_PROGRESS`、`G-CODE` 4/4 `NOT_STARTED`、`G-CI-L1` 1/1 `NOT_STARTED`、`G-RELEASE` 2/2 `NOT_STARTED` → 台账口径下 5 门无一达成；而 `08:22-42` 记 10 项 CI 门转绿并给出提交号，两套载体的状态未同步（见 §8-7）。

### 7.4 采集口径盲区（本组另须写明）

- `设计大纲/_tools/02b_build_pack_inventory_v2.py:25` 的 `LEDGER_RE = (^|/)([0-9]{2}_)?TASK_LEDGER[^/]*\.csv$`：可匹配本包 `TASK_LEDGER.csv` 与 `TASK_LEDGER_V1_ARCHIVE.csv`、G99 内 `…/AUDIT_REVIEW/TASK_LEDGER.csv` 与 V7.1 版；不可匹配 V8.1 的 `CONTROL_TASK_LEDGER.csv`、V6.1 的 `03_REWORK_TASK_LEDGER.csv`、V5 的 `02_TASK_LEDGER.csv`（zip 内 15 份台账中仅 2 份被采）。
- `设计大纲/_tools/07_pack_commit_link.py:20-21` 对 zip 台账分支为空实现（`z = (it.get("zip") or {}).get("ledger")` 之后仅 `pass`），zip 形态台账不参与提交对接 → 本组 G99 与模板在 `pack_commit_link.csv` 无行（模板另有原因：无台账）。

### 7.5 另两身份的三方核对

- `cprun-v4-pack-template`：账 0（无台账）；据（git）唯一事件 `4d3feaf4` 2026-09-09 21:27:22 收录（`pack_events.md:67` seq1784 PACKNEW，另记源为 `/workspace/Local search/…`）；据（工作区）70 份 `run/**` 影子副本 + G99 zip 内 20 条内嵌条目，均与工作树逐字节/逐 CRC 一致；该身份 5 个示例号中 `DOC-001`（6 条）与 `FIX-001`（1 条）的整号命中全部落在 2026-08-28→09-03，属其他包同号，不计本模板；`AUDIT-001`、`CI-TEST`、`RELEASE-001` 整号命中 0。
- `AstroCS_AUDIT_REVIEWPACK_20260909T133841Z`：账 98（V5 系台账，随快照嵌入）；整号命中 98/98 = 100.0%，但 98 号的命中提交日期区间实测为 2026-08-06 → 2026-08-31，全部早于该 zip 的打包时点（2026-09-09 21:38:41）与 V1 包窗口 → 该 100% 属于被嵌入的 V5 任务（同值亦见于 `pack_commit_link.csv` 的 `prerelease_v5/AUDIT_REVIEW` 行与 `pack_lineage_table.md:38`，本组重算一致），不是 G99 自身的执行留痕；G99 自身 0 条 git 路径事件、0 条提交消息引用该身份名（实测 grep 0 命中）→ 按"有据无账 + 有件无提交"登记。

---

## 八、缺口与不确定

1. 取证件时序反转：`digest/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909.md` 与 `pack_inventory.csv` 把 `f5f944a5` 记为"首次提交"、`e9547c02` 记为"最后提交"；与 `git log --reverse` 及 `pack_events.md:68` 冲突。
2. digest 的 `adds_n/mod_n`（V1 6/39；模板 6/0）与 git 实测（74A/30M；10A）不符，采集脚本的计数口径未在 `设计大纲/_tools/` 中说明为"首提交数"或"全史数"。
3. 实例数口径缺口：V1 与模板各记 1 实例，实测另有 70+70 份 `run/**` 影子副本与模板 2 份 zip 内嵌副本未登记；影子中 4 份文件集合与工作树不同（缺 32/32/31/1 文件）。
4. V1 包首次入库版本自身机器门为红：`e9547c02` 处 `CONTROL_FAIL: control/ledger mismatch`（台账 25 vs 图 29，缺 4 号）。
5. 裁决登记面不完整：`07:4` 自称唯一登记源但只有 R-01…R-20；R-22/R-24/R-26/R-28-b/R-29/R-31 只在 `05` 内联，R-21/R-23/R-25/R-27/R-30 全包 0 处出现；`08:39-40` 却写"R-01 … R-31"。
6. 门名三套互不一致（`02:5-10` 6 门；`control-pack.json` 5 门；`06:58-68` 7 门），且 `00:29` 引用 JSON 中不存在的 `G-CI`、`G-REAL`；4 个门无 `requires` 消费方。
7. 门禁判定面与台账面未同步：5 门原子在台账口径下无一 `PASSED`（`G-CI-FIX` 8 NS + 1 IP 等），而 `08:22-42` 记 10 项 CI 门转绿。
8. 7 号有交付型提交而台账仍 `NOT_STARTED`（`3fd9e5e2`、`2fdd6626`、`19bdf1ab`、`af56020c`、`43f0c429`、`1dc5447d`、`27909ddf`），且均晚于台账末次改动 `a628bc21`。
9. `BASE-001` 标 `PASSED` 但其被引用证据 `evidence/BASE-001/freeze_snapshot_r1.json` 不存在（工作树与 git 全史）；引用它的 `ACTIVITY_STATE.md:54-55` 同时把同 run 号 `Rmtuajdhv73d430` 记于 V7 名下，与本包自述 run `Rmtucy2cqced995` 的关系未证实。
10. 本包在 tracked 证据面无任何留痕目录，执行痕迹集中在 `.gitignore` 排除的 `run/`（27/57 号有同名目录）→ 提交侧与包侧都无法独立复核执行完成度。
11. 包自述生成（2026-09-09T15:34:42Z）到首次入库（`e9547c02`）约 23.7 小时空窗，期间内容不在任何提交中；G99 zip（同窗口快照）内 0 条本包条目为反证。
12. `00:15`/`:21`/`:22` 四个 sha256 中仅约束文件 `b3c66eb5…` 可在 `789c5b6c` 复现；宪章 DRAFT 指纹 `37fefdcc…` 与 dirty/index 指纹不可复现 → 未证实。
13. V1 包无 `SHA256SUMS`/`MANIFEST.json`/`START_PROMPT.txt`/`templates/`/`baseline/`、无 zip 原件 → 包级完整性仅覆盖于包内校验器的文件存在性与任务集校验；与上一代（逐文件 sha256 清单）不可直接互证。
14. G99 zip 有 2 个重复条目名（`memory.md`、`AGENTS.md`，size/CRC 相同）→ 14556 条目对应 14554 唯一路径；成因未证实（本组不猜）。
15. G99 的 digest"包自述"取自其内嵌 V5 件、台账亦为 V5 系 98 行 → 直读 digest 会把 V5 的结构与 100% 命中率归到 G99 名下（本组按 §7.5 已剥离）。
16. G99 内 `工程控制/` 仅 212 条、`_control_packs/` 仅 1 个 `.md`，而当前工作树 `_control_packs/` 有 12 件（含 V7.1、V8.1、V5、V4、RESCUE-V3 等 zip 原件；无 V1、无模板）→ 同名目录跨窗口内容不同，代际对比不可直接互推。
17. 门禁原子状态值 `passed`（JSON 小写）与台账字面量 `PASSED`（CSV 大写）之间的映射由包外插件承担，包内无映射定义；`07:235` R-18 已停用该框架、`WORK_LEDGER.mode` 记 direct-subagent → 该门在当前执行形态下是否仍被评估，未证实。
18. 8 号被 `08` 点名在跑/待办（含 `MON-FIX-001`、`P3-PROJ-FAILFAST-001` 各见 1 条提交、3 号有同名 run 目录）却不在 57 行台账，也不在 `control-pack.json`；同时 `05:449` 裁决 R-26 撤销了 `CI-BACKEND-001` 两条必修动作而台账仍保留该号 `NOT_STARTED` → 任务集与实际执行清单不一致（未证实后续是否并入 G14 的 RESCUE-V3 台账）。
19. 模板与 `工程控制/包规范.md`（`ACTIVITY_STATE.md:29` 标 `REFERENCE_TEMPLATE`、"控制包/Worker 返回包/审核包通用格式（1.0）"）的规范—模板对应关系未在本组取证（前台分工留 G15）；本组仅记录 G99 zip 内 `包规范.md` 与工作树逐字节相同（4453 字节，CRC32 `fbc58697`）。
20. 三身份之间不存在可比的 zip sha256（V1 与模板无 zip 形态、G99 无解包件形态），跨形态一致性只能靠条目级 CRC/字节比较（本报告 §1.2、§1.4 已给出）；同名多形态在本组内没有"digest 值是否一致"的可判对象。

---

## 片尾一 · 与上一组（G12）的衔接

1. G12 的最后包是 `2026-09-09_superseded_V8.1_CI_CONTROL_20260905`（`pack_lineage_table.md:43`；最后提交 `8e03d7da`；`pack_events.md:70` 记 seq1787 RENAME50）。先后关系实测：`d8c821db`（2026-09-10 01:58:38，GOV-001 宪章首次入库即 FROZEN，blob sha256 `0f25544e…`）→ 9 个提交 → `8e03d7da`（2026-09-10 02:22:41，GOV-002 把 V8.1 tracked 镜像 56 文件原样移入归档目录并新建 `工程控制/ACTIVITY_STATE.md`）→ 9 个提交 → `e9547c02`（2026-09-10 15:14:01，本组 V1 包首次入库）。`8e03d7da` 是 `e9547c02` 的祖先（`git merge-base --is-ancestor` 实测通过），且 `工程控制/ACTIVITY_STATE.md` 首次提交即 `8e03d7da`（`git log --format…` 实测末条）。
2. 归档与替代的因果指向同一包：归档动作的提交标题为 `chore(GOV-002)`，而 `GOV-002` 是本包 57 号台账内的任务（`TASK_LEDGER.csv:4`，状态 `PASSED`，lane `repo-write`，owner `SA-GOV`）；`ACTIVITY_STATE.md:3` 亦自述"建立日期 2026-09-09（GOV-002，ASTROCS-CONSTITUTION-ALIGNMENT-V1）"、`:22-23` 把 ACTIVE 交给本包、把 V8.1 记 `ARCHIVED_SUPERSEDED` → 上一组最后包的归档是由本组 V1 包的任务编号执行并登记的。
3. 替代声明的方向核对：归档件自述在 `engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/README_ARCHIVED.md`（"56 个 tracked 文件原样移动，逐文件 SHA-256 零差异；归档日期 2026-09-09（GOV-002）"，并引用宪章）；而 V8.1 自身的 `15_SUPERSESSION_NOTICE.md:1-3` 只作废 V8.0、未提及本包 → 包与包之间的替代关系仅由归档侧（归档件 + `ACTIVITY_STATE`）确立，被替代包内无反向声明。
4. 与 `ASTROCS_AUDIT_REVIEWPACK` 的同时点关系：G99 打包（2026-09-09 21:38:41+08）早于 `8e03d7da` 归档约 4 小时 44 分、早于本包首次入库约 17 小时 36 分，三者同锚基线 `789c5b6c`（G99 的 `AUDIT_PACK_README.md` 与本包 `00_READ_FIRST.md:6` 声明同一 SHA）。可核对的一点是：G99 内并无 `2026-09-09_superseded_V8.1` 归档目录（其 `engineering/control/archive/` 下只有 `2026-09-02_legacy_…` 855 条），V8.1 在 zip 内仍以 `工程控制/`（55 条）与 `engineering/control/active/`（56 条）未归档形态存在 → 归档发生在审核包快照之后。
5. 本组 V1 包现亦已被取代：`ACTIVITY_STATE.md:22` 标 `ARCHIVED_SUPERSEDED`（登记提交 `5a250999` 2026-09-13 13:15:19，标题为"登记 RESCUE-V3 为唯一 ACTIVE 控制包——对齐包 V1 转 ARCHIVED_SUPERSEDED"），ACTIVE 交 G14 的 `AstroCS_RELEASE_RESCUE_CONTROL_V3_20260912`（`:21`，其 zip 原件在 `工程控制/_control_packs/`）；V1 目录原位保留，登记理由为 `05` 号登记册被仓库侧脚本引用（实测引用者 5 处：`docs/standards/checks/check_standards_registry.py`、`docs/standards/STANDARDS_REGISTRY.md`、`tools/quality/check_ctest_registration.py`、`tools/quality/known_failures_baseline.py`、`ci/known_failures.json`）。

## 片尾二 · 重叠身份

1. 模板身份的第二、第三存形态在 G99 内：`工程控制/cprun-v4-pack-template/`（10 条）与 `repo/工程控制/cprun-v4-pack-template/`（10 条），20/20 条目 CRC32 与工作树一致；与本组 digest"1 实例"记录不冲突（digest 只登记 worktree 形态），但该形态未在任何取证件中出现；zip 内 `repo/` 子树除该模板外无其他条目（10 条全部为该模板）。
2. V1 包的重叠形态是影子树而非 zip：`run/**` 70 份目录副本（66 份换行归一后与工作树逐文件相同、4 份为早期版本快照），因 `run/*` 被 `.gitignore:19` 排除而不在 `pack_inventory.csv` 中 → 与本组 digest 的实例计数无 sha 可比对（影子副本无 zip 摘要，工作树包亦无 `SHA256SUMS`，故"digest 是否一致"在本身份上无共同对象可判；可判的是逐文件 sha256 比较结果，见 §1.2）。
3. G99 与 G07/G08/G11 的重叠计数：其 98 行台账 = G07 的 `prerelease_v5/AUDIT_REVIEW` 身份台账（逐字节相同）；其 46 条 `CONTROL_V6` 条目 = G08/G11 记的同一批 V6 文件（本组实测 46 条一致）；其 V8.1 条目（55/56 条）与 G12 身份重叠、两份副本条目数差 1（缺 `baseline/` 内 V7.1 zip）→ 均属"容器视角重复计数"，不构成本组新内容。与本组两个包身份的内容交集为 0（V1 条目 0 条；模板条目 2 份副本属重叠计数，已在第 1 条登记）。
4. 三身份 digest 一致性小结：V1（74 文件、57 行台账、无 zip）与 G99（14556 条目、98 行 V5 台账、无 worktree）无共同可比摘要字段；模板与 G99 之间可比对的只有模板的 20 条内嵌条目 CRC（全部一致）；因此本组不存在"同名多形态 digest 是否一致"的可判对象，仅存在跨身份的容器/嵌入重叠。
5. 号面重叠（跨代复用号，非同一任务实例）：V1 57 ∩ V5 98 = 7、∩ V6 88 = 9、∩ V6.1 67 = 10；V1 归档 30 号 ⊂ V1 主 57 号（交集 30、独有 0）。表头与状态字面量三本各不相同（10 列 / 8 列 / 12 列；`PASSED` / `PASS` / `NOT_STARTED`），故归组统计时不可合并计数。
