# P-G08 · V6 系统重构控制包（含 CONTROL_V6 外层容器）取证报告

身份：`AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830`、`CONTROL_V6`；组 G08；全部结论出自包体本体与仓库自测，指针为「相对路径 + 条款/任务号（+ 行号/8 位提交号）」。

## 一、组内包清单与身份归并

**身份 1：AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830**（digest 记 3 实例）

| 存形态 | 路径 | 文件数 | 字节 | zip/自带 sha256 | 首次=最后提交 | 删除 |
|---|---|---|---|---|---|---|
| zip | `工程控制/_control_packs/AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830.zip` | 46 成员（+5 目录项） | 200775（未压）/99253（压后） | `fdad8e40123a757cfcdd51429261ca2e9c915f640e69adba8eb449ee2850f689`（自测=digest） | 无 git 事件（`git ls-files` 无匹配；`git check-ignore` rc=1 即非忽略，纯未跟踪） | 不在库，无删除提交 |
| 解包件（归档 worktree） | `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/CONTROL_V6/AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830` | 46 | 200775 | 台账 `7d1ff6f8…`；`01` 文件 `dc47fbbb…`（自测） | `b7b2dea7` 2026-09-02T22:15:41+08:00（该路径下 46 个 `A`） | 无 |
| 仅历史复原 | `设计大纲/_evidence/packs/history/4b1b948e/工程控制/CONTROL_V6/AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830` | 47（46 原生 + `_PROVENANCE.json` 398B） | 201173 | 同上 46 文件 | `4b1b948e` 2026-08-30T23:20:14+08:00 | `_PROVENANCE.json` 的 `delete_commits` 为空 |
| 影子树（自测补录） | `run/*/shadow_base/engineering/control/archive/…/CONTROL_V6/AstroCS_V6_…` | 72 份目录，各 46 文件 | 200775/份 | 内容树哈希 `13cd8aa1084b3a0a…` | 不在库（`.gitignore:19` `run/*`） | 无 |

形态一致性（实测，不猜原因）：zip 46 成员与解包件 46 文件逐文件 SHA-256 相同（46/46 命中，0 差异）；72 影子树与解包件内容树哈希相同；复原件树哈希 `21f7f3d71024074c…` 与上述不同，唯一差异是多出脚本注入的 `_PROVENANCE.json`。即三形态的**原生文件集合完全一致，无内容漂移**。MANIFEST 只列 44 项 = 46 文件去掉 `MANIFEST.json` 与 `SHA256SUMS`（排除规则见 `scripts/validate_control.py:110,117`）。zip 成员时间戳 2026-08-30 23:43:54 晚于入库提交 23:20:14、晚于 `MANIFEST.json` 的 `generated_utc: 2026-08-30T15:06:37Z`。

**身份 2：CONTROL_V6**（digest 记 1 实例，`pack_inventory.csv:54`：47 文件 / 201084 B，首末提交同为 `4b1b948e`，删除提交列空）

容器性质已核验：`git ls-tree -r 4b1b948e 工程控制/CONTROL_V6` = 46 文件，全部位于其下一层 `AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830/` 内，目录级 `git ls-tree` 只返回 1 个 tree 条目 → **CONTROL_V6 自身零文件，是 V6 包的历史外层容器**；同提交内该旧路径于 `b7b2dea7` 以 46 条 `R100` 改名迁入归档容器（限旧路径统计显示 46 `D`）。与 CONTROL_V4 同形：`engineering/control/archive/…/CONTROL_V4/` 也只含 `AstroCS_MAIN_PRERELEASE_CONTROL_V4_CPU_ADAPTIVE_20260828/` 一个子包（引入 seq1039｜`b12305ed`，见 `设计大纲/reports/packs/pack_events.md` §关键对照）。

归并判定：身份 2 与身份 1 的复原形态是同一批 46 文件的「容器视角」，无文件差异可列；两身份的 46 原生文件在复原中被**重复计数**（各加自己的 `_PROVENANCE.json`）。

## 二、包内文件地图

根 23 个文件 + `schemas/` 6 + `scripts/` 5 + `templates/` 9 + `fixtures/` 3 = 46；**无 `tasks/` 目录**（逐任务规格集中写在 06），无 `evidence/`。命名口径：规格 `NN_大写下划线.md` / 表 `NN_大写下划线.csv`；schema 文件 `xxx.schema.json`（draft 2020-12，`$id` 为 `astrocs.*`）；脚本为动词式 `validate_*.py`/`package_audit.py`/`selftest.py`；模板全大写（.md 6 个、.csv 3 个）；fixture 为 `valid_*` / `invalid_*` 前缀。

- `00_READ_FIRST.md` 执行入口（7 节）：§1 目标链，§2 文档优先级（01 最高、历史 PASS 只作线索），§3 执行前必做（含把台账复制为 `evidence/refactor/TASK_LEDGER.csv`），§4 连续执行（6 类真实阻塞；Windows 离线只标 `WAITING_WINDOWS`），§5 修改纪律，§6 绝对禁止项 10 条，§7 完成定义。
- `01_ASTROCS_ENGINEERING_CONSTRAINTS.md` 工程约束 16 节，首段标「状态：已冻结 / 日期：2026-08-29」；§5 并行资源 8 条，§10 L0–L3 文档体系，§11 机器一致性 13 项，§13 控制包与审核包白名单，§14 每轮十问，§16 最终判定原则。
- `02_BASELINE_AUDIT.md` 基线审计（8 节）：§1 输入包与可信边界，§2 总体判定，§3 `P0-001..007`，§4 `P1-001..008`，§5 文档治理漂移 6 条，§6 保留并迁移清单，§7 Phase3 判定，§8 重构边界（自述为静态审计）。
- `03_TARGET_ARCHITECTURE.md` 13 节：架构结论、目标布局、边界与 CLI 命令集合、Pipeline IR、DataArtifact、ModuleDescriptor、RunContext、调度（§8.3 重计算并行硬门）、CPU Backend ABI、I/O 并发、presets、机器文档、ACR 隔离。
- `04_MIGRATION_AND_GATES.md` 9 节：strangler 原则、自动 Gate 表、状态机、每任务 11 步模板、回退、科学变更隔离、性能隔离、最终一次真实运行、任务图变更规则。
- `05_TASK_LEDGER.csv` 台账（11 列 88 行）。
- `06_TASK_SPECIFICATIONS.md` 任务规格：12 个 `## G0..G11` 节 + 51 个 `###` 标题（36 个为单号，其余为并写号组如 `CPU-001..005`）；字面覆盖 62/88 号。
- `07_SCIENCE_AND_TEST_MATRIX.md` 8 节：验收逻辑 8 步序、通用误差规则（FP64 `rtol=1e-12`；FP32 `rtol=5e-6`；归约 `gamma_n` 模型且 `C` 测试前冻结、通常 ≤4）、Phase1/2/3 矩阵、后端与并行等价、真实数据策略、参考依据。
- `08_CPU_PARALLEL_BACKEND.md` 10 节：发布范围、Provider 构建、能力探测、benchmark 子命令与防错选、profile 字段、Runtime 线程合同、分块均衡、资源监测、§9 资源门、ACR 兼容。
- `09_DOCUMENTATION_AND_MACHINE_CHECKS.md` 9 节：单一真相表、L0/L1/L2、注释规则、6 个检查器（`check_contract_graph`、`extract_api_ast`、`check_module_registry`、`check_pipeline_trace`、`check_serial_heavy`、`impact_analysis`，09:83-120）、负面测试要求、运行图、文档发布门。
- `10_LINUX_WINDOWS_EXECUTION.md` 8 节：节点角色、Linux 流程与 timeout 表、Fatduck 数据获取、Windows 在线探测状态机（`UNKNOWN → probe → WAITING_WINDOWS / BLOCKED_WINDOWS / ready`）、Windows 流程、32R 唯一运行合同、HiPS 视觉、减少等待。
- `11_GIT_MAIN_ONLY.md` 6 节：main-only、8 类提交不可混合、提交信息模板（`<TASK-ID>: …` + `Evidence:` 行）、push 前检查、并发协作、错误提交 revert。
- `12_AUDIT_PACKAGE_SPEC.md` 8 节：包名、必含 13 文件、禁止内容、大产物清单字段、`SUMMARY.json` 单一状态、FINDINGS、测试资源摘要、8 步二次验证。
- `13_RELEASE_ACCEPTANCE.md` 6 节：可发布定义、25 条 `R-*` 硬门、接缝专项门、发布包布局、已实现/未实现声明、最终抽查 7 项。
- `14_PRIMARY_REFERENCES.md` 4 组：科学（Drizzle/HEALPix/FITS WCS I-II）、FITS 与并发、并行与 ISA、引用验收。
- `15_GATE_CHECKLISTS.md` G0–G11 共 12 节、105 个复选框（`grep -c "^- \[ \]"` = 105）。
- `16_ACCEPTANCE_MATRIX.csv` 26 行（11 类 category；`automatic` yes 24 / partial 1 / no 1）。
- `17_RISK_REGISTER.csv` 18 行（P0 7、P1 10、P2 1，status 全 OPEN）。
- `18_CURRENT_TO_TARGET_MAPPING.csv` 11 行（6 列映射现状组件→目标任务）。
- `AGENTS_REPLACEMENT.md`（3 行，整块替换根 AGENTS.md）；`SHA256SUMS`（44 条）；`START_PROMPT.txt`（47 字符单行）；`MANIFEST.json`（`astrocs.control-manifest/v1`，`control_version V6.0`，`target_version 0.10.0-alpha.1`，`baseline_commit 587fe0e341a7…`，`engineering_constraints_sha256 dc47fbbb…`，`generated_utc 2026-08-30T15:06:37Z`，44 项合计 189227 B）。

## 三、任务结构

- 规模：88 行、`task_id` 全唯一；21 个号族：BAS4、VER1、DOC5、SCI3、DATA1、ARCH1、API1、TEST1、BLD1、CORE8、IO4、CPU8、P1-9、P2-8、P3-6、CLI3、LEG4、QA5、LNX5、WIN6、REL4（合计 88）。
- 表头 11 列：`task_id,gate,title,depends_on,platform,change_class,heavy_compute,commit_required,scope,acceptance,status`（实测；digest 三形态同值）。
- 状态字面量：包内台账只出现 1 种 `NOT_STARTED`×88；机器允许集 6 种（`scripts/validate_task_graph.py:19`：NOT_STARTED/IN_PROGRESS/PASS/FAIL/BLOCKED/WAITING_WINDOWS）；证据 schema 枚举只 4 种（`schemas/task_result.schema.json`：PASS/FAIL/BLOCKED/WAITING_WINDOWS）；`04 §3` 叙述上述迁移路径另加 `BLOCKED` 语义。
- Gate 分布：G0 4 / G1 9 / G2 9 / G3 12 / G4 9 / G5 8 / G6 6 / G7 7 / G8 9 / G9 5 / G10 6 / G11 4；行序 Gate 单调不减；每 Gate 号族固定（G0=BAS、G1=SCI/DOC/DATA/ARCH/API/TEST/VER、G2=BLD/CORE、G3=IO/CPU、G4=P1、G5=P2、G6=P3、G7=CLI/LEG、G8=DOC/QA、G9=LNX、G10=WIN、G11=REL）。
- 依赖与顺序声明：87/88 行有 `depends_on`（仅 BAS-001 空），`;` 分隔（存在 `; ` 变体），最大入度 6；无「依赖后继 Gate 任务」的行（实测 0 例）；拓扑排序唯一终点 `REL-004`。其余维度：`platform` Linux 64 / Linux+Windows 17 / Windows 6 / Owner 1；`heavy_compute` yes 38 / no 50（yes 全落在 G2–G10）；`commit_required` yes 87 / no 1（REL-004）；`change_class` 12 类（architecture 42、validation 11、performance 6、release 4、test 4、quality 4、evidence 4、tooling 3、documentation 3、science 3、contract 2、bugfix 2）。

## 四、门禁与验收要求

- 包自校验（实测输出，在 `/tmp` 副本上以 `python3 -B` 运行）：`scripts/validate_control.py` → `CONTROL_PASS files=46 tasks=88 bytes=200775 prompt_chars=47`；`scripts/validate_task_graph.py` → `TASK_GRAPH_PASS tasks=88`；`scripts/selftest.py` → `SELFTEST_PASS`。要求面：`validate_control.py:15-27` 齐 19 规格文件+6 schema+5 script、`:14`+`:50` 约束文件 hash 必须等于硬编码 `dc47fbbb…`、`:93` `baseline_commit` 必须等于 `587fe0e341a780…`、`:63-66` `task_count>=80` 且拓扑终点必须为 `REL-004`、`:28-29`+`:73` 禁 symlink/禁目录/禁二进制扩展名、5 MiB 上限、`:133-135` START_PROMPT 单行 ≤100 字符、`:105-124` MANIFEST/SHA256SUMS 与实际文件逐项 size+sha 一致。
- 台账不可变与 Gate 计算：`04 §2` 表尾要求 Gate 状态只能由台账计算、不得删 dependency 变绿；`04 §9` 规定 `05` 的 ID/依赖/验收语义执行期不可改，出错须生成 `CONTROL_ERRATA.md` + 版本 V6.0→V6.1 + 负责人批准 + 新旧 hash 复验；`validate_task_graph.py` 提供 `--baseline-sha256` 冻结比对、依赖未 PASS 不得自证 PASS、Gate 汇总优先级 PASS>FAIL>BLOCKED>IN_PROGRESS>WAITING_WINDOWS>NOT_STARTED。
- 完成顺序：`00 §3`（先校验包/读 01–05/对齐三 SHA/复制台账为包外执行态）→ `04 §4` 每任务 11 步（第 8 步生成 `evidence/refactor/tasks/<TASK_ID>/TASK_RESULT.json`，第 10 步 `<TASK_ID>:` 前缀提交并 push，第 11 步记录 HEAD 一致）→ `15` 每 Gate 复选框逐项填 PASS/FAIL+证据+SHA（15 前言：空框不能通过）→ `00 §7`+`13 §1`+`12 §5`（只有 13 全门通过且 `validate_audit.py` PASS 才可 `READY_FOR_OWNER_REVIEW`；只有负责人 HiPS 视觉审核批准才可 `ALPHA_RELEASE_APPROVED`）→ `04 §8` 最终一次 32R 真实运行。
- 状态机语义：`04 §3` 允许 `NOT_STARTED→IN_PROGRESS→PASS`、`IN_PROGRESS→FAIL→IN_PROGRESS→PASS`、`NOT_STARTED/IN_PROGRESS→WAITING_WINDOWS→IN_PROGRESS`，`BLOCKED` 只用于真实阻塞，禁止 FAIL 直接改 PASS；`10 §4` 另有 `BLOCKED_WINDOWS`（仅探测分支，未进任何枚举）。
- 资源/科学/文档硬门：`08 §9` 与 `03 §8.3` 同阈值（可用 CPU≥2 时 active worker p50≥2；normalized CPU p50≥90%、mean≥85%；短窗只记录），低 CPU 必分类为 `LOCK_BOUND_FAIL / IO_BOUND_INVESTIGATE / IMBALANCE_FAIL / MEMORY_BOUND_EVIDENCED / SERIAL_PATH_FAIL / LOW_UTILIZATION_UNEXPLAINED_FAIL`，并规定「任何 FAIL 不得通过改报告阈值消失」（08:152）；`07 §2` 容差与归约误差模型 + 容差变更须独立 SCI/TEST commit；`09 §6/§7` 六检查器 + 负面测试；`13 §2` 25 条 `R-*`、`16` 26 行矩阵（16 比 13 多 1 行 `R-OWNER-01`，且是唯一 `automatic=no` 项）；`17` RISK-001..018 逐条挂 `mitigation_task`。
- 审核包与提交要求：`12 §1` 包名 `AstroCS_V6_AUDIT_<UTC>_<12-char-main-sha>.zip` 且只能由 `scripts/package_audit.py` 生成（禁止手工整体压缩）；`12 §2`+`validate_audit.py:17-21` 必含 13 文件（含 `SOURCE_IDENTITY.json`、`TRACEABILITY_MATRIX.csv`、`PREVIEW_MANIFEST.csv`、`MANIFEST.json`、`SHA256SUMS`）；`12 §3` 禁止 git 历史/旧包/原始 testdata/构建产物/dump/凭据/>5MiB 未登记文件/超 25MiB（须先生成 size report，不得 Agent 自行放宽）；`12 §5` `verdict` 三值、`owner_review` `PENDING|APPROVED|REJECTED`、`task_counts` 必须由 ledger 计算、除 REL-004 外不得有非 PASS、四类 commit 必须相等；`12 §8` 8 步二次验证。提交侧 `11 §1-§4`。
- 豁免：包内**无 waiver 机制**（包全库 `grep -i waiver` 零命中）；最接近的三处是 `01 §5.3:79`「不能由 Agent 自行豁免」（须量化证据+负责人批准）、`03 §8.3:204`「这不是自动豁免」（`MEMORY_BOUND_EVIDENCED`）、`09 §6:113-118` 的 allowlist 须带 reason/owner/expiry/test；另 `12 §5` 只给 REL-004 一个例外位。

## 五、设计意图（按包内自述转述）

- 自述目标：`00 §1` 把「多套编排器 + 临时 CLI 路径 + 旧工具」迁移为统一 CLI→Pipeline Runtime→类型化数据管道→模块注册表→科学模块→纯 CPU 后端→I/O/Artifact Store，并声明「本轮不是增加功能」；`03 §1` 只要一个唯一生产执行平面，旧路径按序退出生产；`01 §16` 目标是长期可持续工程机制而非把当前代码跑通。
- 自述禁止项：`00 §6` 十条（单线程重计算、模块自设线程数/绕过租约、硬编码 AVX、ACR 进生产、I/O 层承担编排、CLI 直连内部函数、共享 `fitsfile*`、以关键词/旧报告宣称科学正确、反复跑历史 32R、改依赖隐藏 blocker）；`04 §6:77` 禁止「顺手修公式后用重构误差解释」；`12 §1:7` 禁止手工压缩整仓。
- 对前代的继承/覆盖：`01:12` 历史控制包与本文冲突以本文为准且仅负责人可改；`02 §1` 把上一轮审核包降格为「起点与线索」，源码起点只认 `587fe0e`，并要求今后四类 commit 必须一致；`02 §6` 列出 7 类「必须保留并迁移」的既有设计；`02 §7` 将 Phase3 现状判为 `PROTOTYPE_NOT_PRODUCTION`；`04 §9` 预告控制包版本升至 V6.1。**包内未见任何「取代/废止 V5 控制包」的显式条款**（全包检索 取代/废止/V5/V4 仅命中 `00 §4:49` 不得像旧 CP0 停工、`03:256` 一处表述）→ 该点记为未证实。
- 负责人裁决与用户指令痕迹：包内**不存在** `OWNER_BINDINGS*`/`04_OWNER_DECISIONS*`/`07_FRONT_DESK_RULINGS*` 文件（该类文件只出现在 `工程控制/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909/`，非本组）。包内 owner 痕迹为：`01 §14`（负责人只审顶层科学结论/架构边界/重大风险/发布状态）、`templates/OWNER_REVIEW.md`、`12 §5` `owner_review` 枚举、`13 §1`/`§6`、`16` `R-OWNER-01`、`05` 的 REL-004 行（acceptance 写「负责人明确APPROVE或REJECT; Agent不得代签」，`platform=Owner`、`commit_required=no`）、`04 §9` 负责人批准。包外同期痕迹：提交 `a7faa502`（08-31）「REL-004: 负责人视觉验收视图就绪 (REVIEW_PENDING)」、`6affe300`（09-03）「docs(owner): GOV-004 建立负责人审查入口」、`evidence/refactor/gates/G10/STATUS.md` 末节「待负责人: 视觉审核 + G10 Windows 恢复」。

## 六、代际差异表（对照上一代 `RELEASE_V5/AstroCS_MAIN_RELEASE_CONTROL_V5_SINGLE_CLI_AMD64_20260828`，同一归档容器）

| 维度 | V5 | V6 | 备注 |
|---|---|---|---|
| 文件数 | 37 | 46 | 实测目录与 zip 一致 |
| 规格清单 | `01_PRODUCT_ARCHITECTURE`、`03_TASK_DETAILS`、`04_CLI_COMMAND_AND_PROTOCOL_CONTRACT`、`05_CPU_BACKEND_ABI_AND_PACKAGING`、`06_BENCHMARK_AND_PROFILE_SPEC`、`07_RESOURCE_MONITOR_AND_UTILIZATION_GATE`、`08_SCIENCE_SYNTHETIC_AND_EXTERNAL_REVIEW`、`09_LINUX_WINDOWS_BUILD_RELEASE`、`10_GIT_REVIEW_CAPSULE_AUDIT_PACKAGE`、`11_AGENTS_MD_REQUIRED_BLOCK`、`12_V3_V4_MIGRATION`、`13_ALPHA_VERSION_AND_PHASE3`、`14_V4_COVERAGE_MATRIX`、`15_CONTINUOUS_CHECKPOINTS`、`PACKAGE_MANIFEST.md` | 新增 `02_BASELINE_AUDIT`、`03_TARGET_ARCHITECTURE`、`04_MIGRATION_AND_GATES`、`06_TASK_SPECIFICATIONS`、`07_SCIENCE_AND_TEST_MATRIX`、`08_CPU_PARALLEL_BACKEND`、`09_DOCUMENTATION_AND_MACHINE_CHECKS`、`10_LINUX_WINDOWS_EXECUTION`、`11_GIT_MAIN_ONLY`、`12_AUDIT_PACKAGE_SPEC`、`13_RELEASE_ACCEPTANCE`、`14_PRIMARY_REFERENCES`、`15_GATE_CHECKLISTS`、`16/17/18` 三张 CSV、`MANIFEST.json`、`AGENTS_REPLACEMENT.md` | 左列 15 项在 V6 全部不存在；V6 另新增 `fixtures/`（1 负例 + 2 正例） |
| 机读清单 | `PACKAGE_MANIFEST.md`（无 hash 绑定校验器） | `MANIFEST.json` + `SHA256SUMS` 双清单，`validate_control.py:105-124` 逐文件复算 | 收紧 |
| 台账 | `02_TASK_LEDGER.csv`，8 列、98 行 | `05_TASK_LEDGER.csv`，11 列、88 行 | 新增 `gate/title/change_class/heavy_compute/commit_required/acceptance`，去掉 `required_commit/required_push/preferred_host`（改 `platform`） |
| 号族 | 23 族（BASE/GOV/VER/TRACE/DOC/SCI/ALG/REV/ARCH/API/CLI/P3/ABI/ISA/BENCH/MON/ISO/PAR/SYN/DOCCHK/LNX/WIN/REL） | 21 族（BAS/VER/DOC/SCI/DATA/ARCH/API/TEST/BLD/CORE/IO/CPU/P1/P2/P3/CLI/LEG/QA/LNX/WIN/REL） | 任务数 98→88 |
| 状态字面量 | `scripts/validate_control.py:8` 含 `REVIEW_PENDING`，无 `WAITING_WINDOWS` | `validate_task_graph.py:19` 含 `WAITING_WINDOWS`，**去掉 `REVIEW_PENDING`** | 一收一放；见 §八-11 |
| 校验器 | 3 脚本（`package_final`、`validate_control`、`validate_final_package`）；`task_count>=85`；要求 prompt 含若干概念短语；禁 obsolete token | 5 脚本（新增 `validate_task_graph`、`selftest`）；`task_count>=80` 且拓扑终点必须 REL-004；约束文件 hash 与 baseline_commit 硬绑；包内负例自检 | V6 增加「包自证」层 |
| 审核包 | `10` 的 review capsule：`capsules/<task_id>_<commit12>.zip` + `templates/REVIEW_CAPSULE_INDEX.csv` | `12 §1` 单一 `AstroCS_V6_AUDIT_<UTC>_<12-char>.zip`，13 必含文件 + 8 步复验，模板改 `LARGE_ARTIFACT_MANIFEST.csv` | capsule 概念在 V6 消失 |
| 模板/schemas | 13 模板（多为 CSV 工作证据表 + `SUMMARY.json`）/ 2 schema | 9 模板（新增合同类 `ALG_CONTRACT`/`SCI_CONTRACT`/`MODULE_README` 与 `CHANGE_REVIEW`/`OWNER_REVIEW`）/ 6 schema | — |
| 与上一代关系声明 | `00 §0` 明写「本包取代 V3/V4 的未完成门禁」 | 无同类条款 | 差异事实 |
| 外层容器 | `CONTROL_V4/`（零文件） | `CONTROL_V6/`（零文件） | 同形 |
| 包内台账是否回写执行态 | 是：归档件状态分布 PASS 88/BLOCKED 1/REVIEW_PENDING 1/IN_PROGRESS 1/NOT_STARTED 7，其 `SHA256SUMS` 实测 35 对 1 失配（失配文件即 `02_TASK_LEDGER.csv`） | 否：三形态均 88×NOT_STARTED，`SHA256SUMS` 实测 44/44 全对；执行态外置（`00 §3` 明文要求） | 路径/口径变化 |

## 七、执行留痕三方核对

两本账先分开：包内 `05_TASK_LEDGER.csv`（88 行，状态 100% `NOT_STARTED`，sha256 `7d1ff6f8…`）与包外执行态 `evidence/refactor/TASK_LEDGER.csv`（88 行；表头及**非状态列与包内逐字段相同**，实测；状态 PASS 81 / WAITING_WINDOWS 6（WIN-001..006）/ REVIEW_PENDING 1（REL-004）；sha256 `e0285bb6…`；最后改动 `8be6eb4e` 2026-08-31T02:29:08+08:00，subject「G10-WIN: WIN-001..006 WAITING_WINDOWS (修复 LEDGER 完整性)」；80 个提交改过该文件）。`设计大纲/_evidence/commits/structural_events.csv` 的 seq1261–1355 窗内 LEDGER 事件 80 条（1 条同时点 `05_TASK_LEDGER.csv`），与 `设计大纲/reports/packs/pack_events.md:134` 的窗口登记一致。

1. **号→提交**：`pack_commit_link.csv:9,10` 记两身份同为「88 号 / 命中 88 / 100.0% / 337 提交」。自测：main 全史 2043 条提交的标题+正文中 88/88 号出现（100% 成立），提及提交 **341** 条；按该表口径取最早 1988 条复算得 **339**（差 2 条未证实，见 §八-6）。
2. **严格同号提交**（subject 以 `<ID>:` 起首，即 `11 §3` 要求的格式）：160 条提交、覆盖 83 号，日期分布 08-30 14 / 08-31 64 / 09-01 54 / 09-02 28；09-01 之后的 82 条其改动文件路径多数落在 `evidence/v6_1_rework/*` 而非 `evidence/refactor/*`（例 `01803fd7`、`065aa340`）→ 按号归本包会串代。
3. **号→留痕目录**：`evidence/refactor/tasks/` 共 83 目录（各含 `TASK_RESULT.json`），其中 82 个目录名与台账号一致。
4. **分类**
   - **一致（82）**：包内有账 + 包外有 `evidence/refactor/tasks/<ID>/TASK_RESULT.json` + 有 `<ID>:` 前缀提交。按「提交落在台账落账窗 08-30–08-31」的严口径为 **77**（另有 LNX-001..005 五号留痕目录齐、但其 `<ID>:` 前缀提交全在 09-01/09-02）。抽样核对：BAS-001→`4b1b948e`；DOC-002→`8e7b2c95`；LNX-001..005 目录→`38f62f2a`（08-31 合并提交）；REL-003→`b840ed64`、`24c7eec7`；REL-004→`a7faa502`；QA-001..005 复验→`3da0670b` 系；CPU-006..008/LNX-003..005 复验→`4f6e2b65`（09-07）。
   - **有账无据（6）**：`WIN-001`–`WIN-006` 无留痕目录（与包外台账状态 `WAITING_WINDOWS` 及 `evidence/refactor/gates/G10/STATUS.md` 的离线登记自洽）。细分两种：WIN-001 有 15 条 `<ID>:` 前缀提交（全在 09-02，例 `c1696156`、`8d1a957c`）但无留痕目录；WIN-002/003/004/005/006 连同号前缀提交都没有，其中 WIN-002/003/005 的**全部**提及提交早于本包引入日（如 08-29 的 `b098ef4`、`cdadcc2`、`68e5fd3`，为 V5 期 capsule 注册）。
   - **有据无账（1）**：`evidence/refactor/tasks/REL-CONSISTENCY/TASK_RESULT.json`（status PASS，但内部 `task_id` 字段写的是 `G11-CONSISTENCY`，两本台账均无该号），对应提交 `457dcf6b` 2026-08-31「G11-CONSISTENCY: 发布一致性校验 (VERSION/SBOM/checksums)」；其 `control_sha256` 为本包 zip hash，说明是 V6 执行期产物。
5. **跨包同号规模（削弱命中率解释力）**：V6 88 号中 29 号与 V5 台账号重合、45 号与 V6.1 执行台账（`evidence/v6_1_rework/TASK_LEDGER.csv` 67 行）重合。
6. **留痕与本包的绑定**：83 份 `TASK_RESULT.json` 的 `control_sha256` 全部等于 `fdad8e40…`（= zip 原件 sha256），既不等于 MANIFEST 内任何值也不等于台账 hash → 执行留痕锚定的是 zip 形态。
7. **字面量冲突（实测）**：以包内 `validate_task_graph.py` 跑包外执行态台账 → `TASK_GRAPH_FAIL: REL-004: invalid status 'REVIEW_PENDING'`（V5 遗留字面量）；11 份 `TASK_RESULT.json` 带 `reverify` 字段，而 `schemas/task_result.schema.json` 为 `additionalProperties: false` 且无该属性。
8. **Gate 留痕**：`evidence/refactor/gates/` 有 G0–G11 共 12 目录；11 个含 `CHECKLIST.md`，G10 只有 `STATUS.md`，G8 另有 `QA_SUPPLEMENT.md`；这些清单为表格式（`- [ ]` 计数 0），与 `15` 前言「复制对应段…逐项填 PASS/FAIL + evidence path + SHA」及 105 个复选框的形式不同口径。

## 八、缺口与不确定

1. V6 期审核包实物不在树中：`evidence/refactor/gates/G10/STATUS.md` 末节记 `dist/audit/astrocs_audit_20260830T183323Z.tar.gz`（568 files，SHA `137e3ed1…`），`evidence/refactor/tasks/REL-003/TASK_RESULT.json` 记 `tarball_sha 668a8820…`；`git ls-files dist` 为空，`789c5b6c`（09-09）曾把 `dist/audit/AstroCS_V6_1_AUDIT_20260902T042239Z_faad602da555.zip` 出库 → 无法按 `12 §8` 做 8 步复验。
2. 审核包命名与格式偏离规范：实物名为小写 `.tar.gz`，而 `12 §1` 规定 `AstroCS_V6_AUDIT_<UTC>_<12-char-main-sha>.zip`（且 `validate_audit.py:24` 把 `.tar/.gz` 列为禁止扩展名）；`REL-003/TASK_RESULT.json` 的 tests 记「522 files」与 G10 记 568 files 为两次不同产物（差 46）。
3. 影子树 72 份未进 digest/inventory（`find run -type d -name AstroCS_V6_SYSTEM_REFACTOR_ALPHA_CONTROL_20260830` = 72）。
4. CONTROL_V6 计数口径：`pack_inventory.csv:54` 记 47 文件 / 201084 B，实测该复原目录树为 48 文件 / 201482 B（差额 = 内层 `_PROVENANCE.json` 398 B）。
5. digest 的 `adds_n`/`mod_n` 不是文件计数：复原件记 `adds_n: 4`、容器记 `adds_n: 6`、解包件记 `mod_n: 4`，而实测 `4b1b948e` 为该路径新增 46 文件、`b7b2dea7` 为 46 条 `R100`；上述三值与 `structural_events.csv` seq1261（3 PACKNEW+1 LEDGER=4；再加 2 NEWDIR=6）与 seq1480（LEDGER count=4）逐项吻合 → 引用该表须换算。
6. 提交数不闭合：`pack_commit_link.csv:9,10` 的 337 vs 自测 341（main 全史 2043 条）vs 同表 1988 条口径复算 339；差 2 条的具体筛选条件未在表内声明 → 未证实。
7. zip 形态在 `pack_inventory.csv:51` 的「状态分布」为 `{}`、「自带SHA256核对」为空，而 zip 内台账实为 88×NOT_STARTED（与解包件逐文件同 hash）→ 该形态未被脚本采数。
8. 100% 命中率含跨包同号借用（§七-5）：WIN-002/003/005 的命中全部来自 V5 期提交 → 不能单独据命中率判定本包已执行。
9. 逐号规格缺口：`06` 中无精确号字面的有 28 个（CLI-002/003、CPU-002..005、LEG-002..004、LNX-001..005、P1-008/009、P2-007/008、QA-002..005、WIN-001..006）；其中 **LNX-001..005 与 WIN-001..006 在 06 内既无 `###` 标题也无号字面**，仅由 `06:330-346` 的 G9/G10 节点级要点与 `15` 的 G9/G10 清单间接覆盖。
10. Gate 表口径自相矛盾：`04 §2` 只列到 G10（11 行，且 G8=Linux 收敛、G9=Windows 收敛、G10=发布审计），而 `05` 台账与 `15` 为 12 Gate（G8=文档与质量、G9=Linux、G10=Windows、G11=发布）。
11. 状态枚举三处不闭合：`04 §3`（4 态 + BLOCKED）、`validate_task_graph.py:19`（6 态）、`schemas/task_result.schema.json`（4 态），另 `10 §4` 引入未登记的 `BLOCKED_WINDOWS`；执行留痕实际用的 `REVIEW_PENDING` 与 `reverify` 字段都不在包内允许集（§七-7）。
12. 13/16 计数差 1：`13 §2` 25 条 `R-*`，`16_ACCEPTANCE_MATRIX.csv` 26 行，多出的是 `R-OWNER-01`（`automatic=no`），13 侧无对应条目、仅在 `13 §6` 以「抽查建议」表述。
13. 约束文件的外部漂移：包内 `01` sha `dc47fbbb…`（被 `validate_control.py:14` 硬绑），仓库根 `AstroCS_ENGINEERING_CONSTRAINTS.md` 首次入库于 `065c81de`（2026-09-02，当时内容 sha `7f139bb4…`），HEAD 现为 `e6547a0b…`（标题已标 `ARCHIVED_NON_NORMATIVE`）；`4b1b948e` 当时根文件不存在（`git show 4b1b948e:<根路径>` 报无此对象）→ 包内 pin 与仓库根权威不同源、包外无法按同一 hash 复核。
14. `CONTROL_ERRATA.md`（`04 §9` 要求物）在包内与 git 全库均无命中 → 任务图是否曾变更无包内记录可核。
15. 留痕形式不全：G10 无 `CHECKLIST.md`；`evidence/refactor/` 顶层只有 1 个文件（`TASK_LEDGER.csv`），无 12 号规定的 `FINDINGS.csv`/`TEST_SUMMARY.csv`/`PREVIEW_MANIFEST.csv` 等（这些仅存在于未入库的 dist 产物中）。

## 与上一组的衔接

G07 末包 `AUDIT_PACKAGE_587fe0e341a7.zip`（zip 实例 1699 成员、解压 15,518,698 B、根 `SHA256SUMS` 1698 条实测**全部校验通过**、zip sha256 `81b7e39e…`、不在库）被 V6 直接指认为基线：`00:5`、`MANIFEST.json:baseline_commit`、`validate_control.py:93` 三处硬绑 `587fe0e341a780da726917f40ed77f610de0c73f`，`02_BASELINE_AUDIT.md:5` 以「输入包」名义点名该 zip 并复述其声明版本 `0.9.0-alpha.1`（V6 目标推进为 `0.10.0-alpha.1`）。打回痕迹可核：`02 §1:10` 指控包内证据非单一快照并列出混入提交 `59649ed`/`b8ce366`/`b842899`/`77fc48e`——实测该 zip 的 `evidence/audit_review/SUMMARY.json` 中 `commit = 59649edc0480a4e663ed37bd3e56215ba729526a`、`verdict = RELEASE_NOT_READY_BLOCKED`、`task_counts = PASS 88 / BLOCKED 1 / IN_PROGRESS 1 / REVIEW_PENDING 1 / NOT_STARTED 7`，指控成立；`02 §2` 给出 `ARCHITECTURE_REFACTOR_REQUIRED，不具备 Alpha 发布条件`，`02 §5.5` 与 `17_RISK_REGISTER.csv:18`（RISK-017「task dependencies previously changed to pass」，缓解任务 REL-003）构成对上一轮治理的点名。V6 的对应堵法：`12 §5` 四类 commit 必须相等 + `validate_task_graph.py` 的 `--baseline-sha256` 台账冻结 + `00:35`「将任务台账复制为仓库内 `evidence/refactor/TASK_LEDGER.csv`」（把执行态移出包体，令包内 `SHA256SUMS` 可长期复算，实测 44/44 全对；而 V5 归档件因回写台账致 1 处失配）。基线提交 `587fe0e3`（2026-08-30T17:59:46+08:00）的 subject 为统一 SHA/Windows 重建记录，本包 20 分钟后由 `4b1b948e`（BAS-001，23:20:14）入库；包内无对 V5 控制包文本的任何引用。下游方向：`工程控制/AstroCS_V6_1_REWORK_CONTROL_20260831/MANIFEST.json` 以 `prior_control_sha256 = fdad8e40…`（实测等于本包 zip sha256）并共享同一 `engineering_constraints_sha256 dc47fbbb…`，与本包 `04 §9` 自述的「V6.0 增至 V6.1」路径一致（该 V6.1 清单为 `control-manifest/v2`，且不伴随任何 `CONTROL_ERRATA.md`）；G09 侧的「打回」条文属该组范围，此处只登记 hash 绑定事实。

## 重叠身份

按 `设计大纲/_evidence/packs/dispatch_groups.json`，两个身份都只登记在 G08（G09 另为 `AstroCS_V6_1_REWORK_CONTROL_20260831`），无同名身份跨组重复。存在三类重叠需标注：① **路径级**——本组 46 文件同时是 G11 身份 `archive__2026-09-02_legacy_工程控制_v1.3-to-v6.1` 容器子树，G11 digest 第 10 行登记同一台账（`CONTROL_V6/AstroCS_V6_…/05_TASK_LEDGER.csv` 行 88、分布 `{"NOT_STARTED": 88}`、号样例 BAS-001..CORE-002）与本组 digest **一致**，属同一批文件的父包视角重复计数；② **嵌入级**——G99 的 `artifacts/AstroCS_AUDIT_REVIEWPACK_20260909T133841Z.zip` 内含同一 46 文件于归档路径（zip 只读枚举 46 条 `CONTROL_V6` 路径条目），即本包内容被再次封入他人审核包；③ **号级**——V6 的 88 号中 29 号与 G07 的 V5 台账重号、45 号与 G09 的 V6.1 执行台账重号（各 digest 对台账行数/分布的记载彼此不冲突），跨组引用任务号时必须带时间窗与路径，否则 §七 类核对会串代。另 `02 §1` 指向 G07 包属跨组内容引用，不构成身份重叠。
