# 00 阅读入口：最终内容只有两份

**最终交付（读完这两份即可）**
1. `大报告_项目历史.md` —— 15 节通读型叙述：1988 条提交、13 根谱系、8 个大阶段、6 条主线；829 条 `C‹seq›｜‹sha8›` 提交锚，与 `_evidence/commits/index.jsonl` 全量对账零异常（独立覆盖 559 个 seq，全库的 28.1%）
2. `大报告_历代控制包.md` —— 16 节通读型叙述：49 个包身份、74 个存形态实例、7 个代际、7 条口径演进主线；89 条提交锚零异常，另有节引/条款/路径指针共 652 处

两份均为纯段落叙述（**代码围栏 0、列表行 0**），只描述不评价。字数一类的易变计数不在此声明，需要时用 `python3 _tools/12_check_final_reports.py` 现读。

**以下全部是过程件与证据底座，不是最终内容**（按负责人要求保留、不删，逐条正文与原始记录均已下沉到证据层）：

| 位置 | 角色 | 说明 |
|---|---|---|
| `reports/history/` | 过程件 | 37 份分片报告（现只剩每片三段：主题归纳 / 重叠区核对 / 遗留缺口）、8 份阶段报告、总览（判据表与指针索引）、coverage/spotcheck/overlap_recheck/structural_events |
| `reports/packs/` | 过程件 | 16 份派工组报告（八段结构）、7 份代际报告、总览、coverage / pack_events / digest_verify |
| `reports/_前台独立核验.md` | 过程件 | 前台不依赖子代理汇报的独立实测（覆盖率、指针与路径审计、9 代台账、SHA256SUMS 不符项） |
| `_evidence/commits/逐条详析/` | 证据层 | 37 份分片的**逐条正文**（**1502 个条目组**，覆盖全部 1988 条主责提交）；1568 = 1502 + 报告树 66 个三段小标题，逐片对账基线 `a3a343a4` 相等，零丢失（校验 `_tools/15_verify_split_lossless.py`） |
| `_evidence/commits/{cards,compact,index.jsonl,slices}` | 证据层 | 1988 张全卡 / 1988 张紧凑卡 / 结构化全量记录 / 37 分片清单与 BRIEF |
| `_evidence/packs/` | 证据层 | 清点表、逐身份 digest、32 个仅存历史包整树复原、6 个历史 zip 复原、包↔提交对接表 |
| `_tools/` | 工具 | 取证脚本（可重跑复现底座）与核验脚本 |
| `00_计划…` `01_取证执行日志…` `02_交付索引…` | 过程件 | 计划与批准记录、执行日志、详版索引 |

**引用规则**：两份大报告中的每个论断都带 `C<seq>｜<sha8>` 或"文件+条款/任务号"指针。要复核任意一句：
- 提交类：`_evidence/commits/index.jsonl` 查 seq→SHA → `_evidence/commits/cards/<seq>-<sha8>.md` 看卡 → `git show <sha8> -- <路径>` 看本体
- 包类：`_evidence/packs/pack_inventory.csv` 与 `digest/<身份>.md` → 包本体路径（现存的在 `工程控制/`、`engineering/control/archive/`；仅存历史的在 `_evidence/packs/history/<rev8>/…`，含 `_PROVENANCE.json` 双指针）
- 机器核对：`python3 _tools/12_check_final_reports.py`（字数/代码围栏/指针真实性/节长均衡）、`_tools/_ref_audit.py`、`_tools/_path_audit3.py`、`_tools/04_check_slice_coverage.py`
