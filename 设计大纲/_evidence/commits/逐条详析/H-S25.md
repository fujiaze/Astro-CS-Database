# 逐条详析 （证据层过程件）

> 本文件是 设计大纲/reports/history/slices/H-S25.md 的逐条正文；最终交付只保留两份大报告，逐条正文下沉到证据层（条目文本零改动，仅移动）。
> 由 _tools/11_split_slice_reports.py 从入库基线 a3a343a4 重切，日期 2026-09-15；条目组 45 个；报告树保留段（主题归纳/重叠区核对/遗留缺口）见原文件。

## 主责条目（55 条 seq，合并为 53 组；同编号连续提交成组并列全 seq 与 sha8）

### seq 1321 | c9a22b5d | 2026-08-31 | S（P3-005）
- 做了什么：Phase3 FITS 输出的重开验证修复——在 `lib/phase3_session/p3_output.cpp` 的 `p3_output_write_atomic` 写完后调用 `p3_output_verify` 置 `reopen_ok`，并在 verify 内用 `astrocs::crypto::sha256_file` 重算 checksum 填 `result->sha256`；新增 `tests/unit/p3_output_test.cpp` 四组测试（卡片测试 id：T-ATOMIC-WRITE、T-REOPEN、T-ORACLE、T-PROVENANCE），并在 `tests/unit/CMakeLists.txt` 注册 p3_output 测试。
- 落点：`lib/phase3_session/p3_output.cpp`（+11/-1）、`tests/unit/p3_output_test.cpp`（新增 +86）、`tests/unit/CMakeLists.txt`（+6）、`evidence/refactor/tasks/P3-005/TASK_RESULT.json`（新增 +62）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：生产代码 + 测试 + 控制包或台账。
- 意图证据：消息自述"Change: p3_output.cpp(write 后 verify 置 reopen_ok + verify 重算 sha256)"、"Contracts: P3-005; ALG-P3-004"；变更面证据与之吻合——lib/phase3_session 仅 1 文件 +11/-1（小面修复），hunk 头恰为 `p3_output_write_atomic` 与 `p3_output_verify` 两个函数上下文，TASK_RESULT 内 metrics 键 fix1/fix2 与 findings 行自述"发现并修复两缺陷"。台账 P3-005 行状态字面量 IN_PROGRESS→PASS，并把 P3-006 行 NOT_STARTED→IN_PROGRESS。
- 关联：父即重叠区末尾 seq 1320（P3-004 coverage 语义）；其修复的"重开 + checksum"语义被 seq 1322（P3-006 全链组装）当作末段环节复用；P3-006 由本条打开、由 1322 关闭。
- 不确定点：两处缺陷的发现路径（测试失败驱动还是审查驱动）未证实；卡片"消息中的编号"含 P3-004，经比对系从 `ALG-P3-004` 合同号误提取，非本条第二任务号。

### seq 1322 | bfb07282 | 2026-08-31 | S（P3-006）
- 做了什么：Phase3 全链组装验证——新增 `tests/unit/p3_assembly_test.cpp`，覆盖 mini HiPS 生成 → sampler open → 采样确定性 → FITS 重开验证四段，并在 tests/unit/CMakeLists.txt 注册。
- 落点：`tests/unit/p3_assembly_test.cpp`（新增 +119）、`tests/unit/CMakeLists.txt`、`evidence/refactor/tasks/P3-006/TASK_RESULT.json`、`evidence/refactor/TASK_LEDGER.csv`（+1/-1）。
- 内容性质：测试 + 控制包或台账。
- 意图证据：消息自述"Change: p3_assembly_test(mini HiPS 生成/sampler open/采样确定性/FITS 重开验证)"、"Contracts: P3-006; ALG-P3-003/004"、"Tests: P3-006 TESTS PASS; 全链"；变更面证据：代码面 2 文件全在 tests 下，**无任何 lib 改动**，与"纯组装验证"自述一致；TASK_RESULT 的 status 为 PASS 且 start_commit=source_commit=c9a22b5d…（即 seq1321 的完整 SHA）。
- 关联：闭合 seq 1321 打开的 P3-006；是 seq 1323（G6-GATE）判定的直接前置；台账依赖字段为 P3-003;P3-004;P3-005;CPU-008，跨接到 seq 1318–1321。
- 不确定点：卡片"消息中的编号"含 P3-003，系从 `ALG-P3-003` 误提取；"全链"是否含真实数据（非 synthetic）未证实，卡片仅示 mini HiPS 合成构造。

### seq 1323 | d3f0b37a | 2026-08-31 | S（G6-GATE，无任务号）
- 做了什么：登记 G6 Phase3 门禁 8/8 PASS，新增 `evidence/refactor/gates/G6/CHECKLIST.md`。
- 落点：单文件（紧凑卡计 +25，全卡"每文件变更行数"计 +21，属计数口径差）。
- 内容性质：控制包或台账。
- 意图证据：纯登记提交——代码面 0、包与证据面 1；卡片新增标题区块给出 "# G6 Phase3 Gate Checklist"、"## 验证命令 (全部 exit 0)"、"## Gate 判定"；消息自述 "Tests: 6 项 P3 测试全 PASS"。
- 关联：本片第一个门编号收束点，收束 P3-001..006（seq 1317–1322）；与上一片 seq 1316（G5-GATE）构成同一登记节拍；其"HEAD 与 origin/main 一致"句式被 seq 1331/1336/1343/1346 沿用为模板。
- 不确定点：标题计数 8/8 与 Tests 行"6 项 P3 测试"不同源（前者为 checklist 条目数、后者为测试数），CHECKLIST 八项原文未逐项展开——登记为证据缺口。

### seq 1324 | bb2d9fcd | 2026-08-31 | S（CLI-001）
- 做了什么：确立 CLI 稳定 command 层规格与机器门——新增 `docs/refactor/CLI_COMMAND_LAYER.md`（记录 `cli/main.cpp` 1521 行、以 `CmdRule kRules[]` 表驱动命令分发、统一 `cmd_*` 签名、15 条路径、未知路径拒）与 `tools/check_cli_command_layer.py`。
- 落点：`docs/refactor/CLI_COMMAND_LAYER.md`（+17）、`tools/check_cli_command_layer.py`（+44）、`evidence/refactor/tasks/CLI-001/TASK_RESULT.json`（+65）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：文档或规格 + CI 或机器门 + 控制包或台账。
- 意图证据：消息自述 "Change: docs/refactor/CLI_COMMAND_LAYER.md + tools/check_cli_command_layer.py(7 cmd_*/15 路径)"、"Tests: check_cli_command_layer PASS; --version/--help 实测"；变更面证据：**代码面唯一文件是 tools 下的检查器，本条未改 `cli/main.cpp`**——即"先立规约与门、后改实现"。
- 关联：CLI 族首条（1324→1325→1326）；三条共用编号 CLI-005；被 seq 1331（G7 条目 1）以短码 bb2d9fc 回指；文档所记 `--version → 0.10.0-alpha.1+g<commit>` 在 seq 1367 被改写为 alpha.2。
- 不确定点："7 cmd_*/15 路径"的逐项清单未证实（卡片仅列 cmd_config_init / cmd_config_validate / cmd_show_effective / cmd_stub / cmd_run_pipeline 五个）。

### seq 1325 | e5b84f30 | 2026-08-31 | S（CLI-002）
- 做了什么：CLI run 的 preset/IR 驱动规格 + Artifact 哈希链机器门——新增 `docs/refactor/CLI_RUN_PRESET.md` 与 `tools/check_cli_run_preset.py`。
- 落点：`docs/refactor/CLI_RUN_PRESET.md`（+18）、`tools/check_cli_run_preset.py`（+39）、`evidence/refactor/tasks/CLI-002/TASK_RESULT.json`（+61）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：文档或规格 + CI 或机器门 + 控制包或台账。
- 意图证据：消息自述 "Tests: check_cli_run_preset PASS; hash-mismatch→8"（退出码 8 为可核对判据）；变更面证据同 seq 1324——代码面仅 tools 检查器，无 CLI 实现改动。
- 关联：CLI 族第二条；seq 1331（G7 条目 2）以短码 e5b84f3 为证据并复述"run manifest 哈希链 + resume hash-mismatch→8"；与 1324/1326 共用 CLI-005。
- 不确定点：哈希链在实现层的落点本条未改代码；退出码常量（ARGS/8）的定义处未在卡片出现，未证实。

### seq 1326 | d93054c0 | 2026-08-31 | S（CLI-003）
- 做了什么：把 `astrocs test synthetic` 从占位接通到真实合成门——在 `cli/main.cpp` 新增 `cmd_test_synthetic`（卡片含前向声明与实现两处），建立 group→测试二进制的 `kMap[]` 映射（7 组），从 `ASTROCS_TEST_BIN_DIR` 或 `<cwd>/build/root-cmake/tests/unit` 定位已编译测试，无测试二进制（非开发构建）时返回可诊断错误而非静默；注入 `ASTROCS_REPO`。
- 落点：`cli/main.cpp`（+60/-0）、`evidence/refactor/tasks/CLI-003/TASK_RESULT.json`（+64）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：生产代码 + 控制包或台账。
- 意图证据：消息自述 "Change: cli/main.cpp cmd_test_synthetic(7 组映射/ASTROCS_REPO 注入/失败明确码)"；变更面证据吻合——全卡内容摘录首行即"无测试二进制 (非开发构建) → 明确错误 (可诊断, 非静默)"，紧凑卡"点名与变更面交集 = cli/main.cpp"。
- 关联：CLI 族第三条、本族唯一真实改生产码者；同一函数在 seq 1341 被二次修补（跨 15 个 seq 的 CLI-003 两笔）；被 seq 1331（G7 条目 3）与 seq 1343（G9 条目 3）引用。
- 不确定点：本条只负责"运行已编译二进制"，构建本身不在面内；7 组映射的组名清单在卡片中被截断，未证实。

### seq 1327 | 317566c0 | 2026-08-31 | S（LEG-001）
- 做了什么：CLI drizzle 命令降级为测试 wrapper，并去掉 nside 的 2048 硬编码——`cli/main.cpp` 中删除带 "2048" 字面量的 `int nside = std::atoi(...)` 一行，改为"必须 --nside 显式或 config.drizzle.nside"，否则打印引导错误并返回 `astrocs::ARGS`；`nside` 初值改为 0。
- 落点：`cli/main.cpp`（+19/-1）、`evidence/refactor/tasks/LEG-001/TASK_RESULT.json`（+60）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：生产代码 + 控制包或台账。
- 意图证据：消息自述 "cmd_drizzle wrapper 标记 + nside 无硬编码(显式/config 派生)"、"Tests: wrapper 引导错误; 生产入口=phase2 run preset"；变更面证据：唯一删除行正是含 "2048" 的那一行，新增注释写明"生产路径 = phase2 run preset / phase1 run 生成校准帧 → phase2 组装"。
- 关联：LEG 族（1327–1330）首条，把 CLI 族与"旧路径退出"衔接；seq 1331（G7 条目 4）以短码 317566c 为"direct drizzle 退出"证据；仍携 CLI-005（依赖面）。
- 不确定点：wrapper 是否仍被任何 CI/脚本当生产入口调用未证实；本条未新增测试，验收依赖引导错误路径的人工判定。

### seq 1328 | fb0d081d | 2026-08-31 | S（LEG-002）
- 做了什么：旧 Orchestrator 退出生产——新增 `tools/check_legacy_exit.py`（任务粒度检查器），并订正 `docs/contracts/PUBLIC_API.md` 中 `orchestrator.exe <stage1.json>` 条目为"V5 遗留, LEG-002 已退出生产；当前生产入口 = `astrocs phase1 run` / `astrocs run --phases` preset"。
- 落点：`tools/check_legacy_exit.py`（+47）、`docs/contracts/PUBLIC_API.md`（+1/-1）、`evidence/refactor/tasks/LEG-002/TASK_RESULT.json`（+62）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：CI 或机器门 + 文档或规格 + 控制包或台账。
- 意图证据：消息自述 "PUBLIC_API orchestrator.exe 标 LEG-002 退出 + tools/check_legacy_exit.py(任务粒度)"、"Tests: check_legacy_exit PASS; 生产符号=0"；变更面证据：合同文档一行确实成对改写；**无任何 lib 或 CMake 删除面**。
- 关联：LEG 族第二条；其创建的检查器是 seq 1329、1330 的承载文件（同工具三次增量）；seq 1331（G7 条目 5）引用短码 fb0d081 并复述"生产符号=0, CMake 零引用, PUBLIC_API 标退出"。
- 不确定点：**"退出"未以代码删除形态出现**（本条删除文件数为 0）；"生产符号=0 / CMake 零引用"仅为自述，卡片不含符号扫描输出——证据缺口（与上一片对 seq 1317 的观察同构）。

### seq 1329 | 94512274 | 2026-08-31 | S（LEG-003）
- 做了什么：AIO PipelineEngine 调度职责退出——在 `tools/check_legacy_exit.py` 追加 LEG-003 的"engine run caller"检查（+14），不改生产代码。
- 落点：`tools/check_legacy_exit.py`（+14/-0）、`evidence/refactor/tasks/LEG-003/TASK_RESULT.json`（+61）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息自述 "Change: tools/check_legacy_exit.py 加 LEG-003 engine run caller 检查"、"Tests: check_legacy_exit PASS; run caller=0"；变更面证据：唯一代码面文件即该检查器，无被检查对象的删除。
- 关联：LEG 族第三条；seq 1331（G7 条目 6）以短码 9451227 为证据并注明"frame API 保留"——即只退调度职责、不退 I/O frame 能力；台账行依赖字段为 CLI-002;IO-001。
- 不确定点：`aio_pipeline_engine` 5-stage scheduler 的实际调用点在何处清零未证实；该检查器的扫描目录集合未在卡片给出。

### seq 1330 | 6a66acab | 2026-08-31 | S（LEG-004）
- 做了什么：旧 Stage2 退出 + ACR dormant 隔离——`tools/check_legacy_exit.py` 再增 LEG-004 三项检查（stage2 工具、ACR link map、module list；+20/-1）。
- 落点：`tools/check_legacy_exit.py`（+20/-1）、`evidence/refactor/tasks/LEG-004/TASK_RESULT.json`（+61）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息自述 "加 LEG-004(stage2 工具/ACR link map/module list)"、"Tests: check_legacy_exit PASS; 生产 acr=0"；变更面证据：纯检查器增量，无删除面。
- 关联：LEG 族末条；seq 1331（G7）条目 7 与条目 8 同时引用本条（stage2 与 ACR 两项，含 `ASTROCS_ENABLE_ACR=OFF` 与 nm acr=0）；**编号 LEG-004 在本片被三次使用**——本条立门、seq 1338 订正 PUBLIC_API 的 stage2 条目、seq 1349 真正改构建面。
- 不确定点：ACR 的真实剔出发生在 seq 1349，本条仅为扫描门，须与 1349 合读才构成完整证据；"stage2 工具不随根构建产出"的构建面证据不在本条。

### seq 1331 | b891eda3 | 2026-08-31 | S（G7-GATE，无任务号）
- 做了什么：登记 G7"唯一生产路径"门禁 8/8 PASS，新增 `evidence/refactor/gates/G7/CHECKLIST.md`。
- 落点：单文件（紧凑卡 +24，全卡逐文件 +20）。
- 内容性质：控制包或台账。
- 意图证据：纯登记；八项条目逐条以**前序提交短码**为证据（bb2d9fc / e5b84f3 / d93054c / 317566c / fb0d081 / 9451227 / 6a66aca），构成 seq 1324–1330 的回指闭环；标题行含"HEAD=6a66aca, 与 origin/main 一致"。
- 关联：收束 CLI + LEG 两族共 7 条；本片"门条目 → 具体 commit"映射最完整的一条，可作交叉验证锚点。
- 不确定点：条目 1 判 "CLI 不 include 科学内部实现 = PASS"，与 seq 1368（CHK-001）PLAN 自述的真实基线相反（后者列出 CLI 直连 session/drizzle 违规）——跨证据期矛盾，详见片尾遗留问题 3，本片不裁断。

### seq 1332–1333 | 8e7b2c95, b63958b2 | 2026-08-31 | M×2（DOC-002, DOC-003）
- 做了什么：L0／L2 文档体系落地。seq 1332（DOC-002）新增根 `REVIEW.md` 与 `docs/review/{PIPELINE_OVERVIEW,ARCHITECTURE_OVERVIEW,RELEASE_STATUS,CHANGE_REVIEW}.md` 四篇（docs/review 合计 +97），配套机器门 `tools/check_l0_docs.py`；seq 1333（DOC-003）为 5 个模块新增 L2 README——`lib/phase1/{stars,wcs,photometry,noise}/README.md` 与 `lib/phase3_session/README.md`（每篇 +8~+12），配套 `tools/check_module_readmes.py`。
- 落点：根 `REVIEW.md`（+24）、`docs/review/`（4 文件）、`lib/phase1/`（4 文件）、`lib/phase3_session/`（1 文件）、两个 tools 检查器、`evidence/refactor/tasks/DOC-00{2,3}/TASK_RESULT.json`、`TASK_LEDGER.csv`。
- 内容性质：文档或规格 + CI 或机器门 + 控制包或台账。
- 意图证据：消息自述分别点名 "REVIEW.md + docs/review/{…} + tools/check_l0_docs.py" 与 "5 模块 README + tools/check_module_readmes.py"；变更面证据吻合——seq 1332 的点名交集为 REVIEW.md 与 check_l0_docs.py，两条均"新增 7 文件、删除 0"，属铺面而非整理。
- 关联：DOC 族（1332–1335）前两条，由 seq 1336（G8）收束；`docs/review/RELEASE_STATUS.md` 在本片创建后即成为 seq 1356／1357／1359 三次改写的对象，并被 seq 1367 的版本行改写触及；台账 DOC-003 依赖字段为 P1-008;P2-007;P3-006。
- 不确定点：README 是否含合同链接以外的技术断言未逐项核对（每篇仅 8–12 行，按文件集合归纳）；seq 1333 自述"13 引用"的计数无法由卡片复算。

### seq 1334 | cda2898b | 2026-08-31 | S（DOC-004，卡含 TRACE-001）
- 做了什么：新增 `tools/check_ast_api.py`——用 Clang AST dump 与公共头声明比对 public 符号的参数、cv 限定、`noexcept`、visibility。
- 落点：`tools/check_ast_api.py`（紧凑卡 +71／全卡 +65）、`evidence/refactor/tasks/DOC-004/TASK_RESULT.json`（+55）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息自述 "Change: tools/check_ast_api.py (AST dump vs 头声明参数/cv/noexcept/visibility)"、"Tests: check_ast_api PASS (3 头); TRACE-001 矩阵可运行"；变更面证据：唯一代码面为检查器，被比对的头文件本条未改。
- 关联：DOC 族第三条；台账行显示 DOC-004 依赖 CORE-003;DOC-001;DOC-003（跨接上一片），同批把 DOC-005 与 REL-001 行推进；被 seq 1347 的 LNX-002 依赖字段引用（LNX-002 depends DOC-004）。
- 不确定点："3 头"是哪三个头未证实；TRACE-001 只出现在消息 Tests 行、变更面无对应新增，是否独立任务号未证实。

### seq 1335 | 6608b166 | 2026-08-31 | S（DOC-005）
- 做了什么：新增 `tools/check_pipeline_trace.py`——比对 3 个 session 的静态节点与 manifest stages 的一致性（静态 Pipeline 图 vs 运行 trace）。
- 落点：`tools/check_pipeline_trace.py`（+54／+60 口径差）、`evidence/refactor/tasks/DOC-005/TASK_RESULT.json`（+60）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息自述 "Change: tools/check_pipeline_trace.py (3 session 静态节点 vs manifest stages)"；变更面证据：台账 DOC-005 行 IN_PROGRESS→PASS，同批改写 REL-001 行——说明本条同时把发布族的依赖就绪状态推进。
- 关联：DOC 族末条；与 seq 1325（CLI-002 哈希链）、seq 1331（G7 条目 2）构成 trace/artifact 一致性链；与 seq 1369（V6.1 期 CHK-002 的 `check_pipeline_graph.py`）为**同一命题的两期实现**，间隔 34 个 seq。
- 不确定点："contract graph 可运行"无输出摘录；两个检查器（tools/ 与 tools/quality/）是否并存或替代，卡片不足以判定。

### seq 1336 | 8989c7f8 | 2026-08-31 | S（G8-GATE，卡含 DOC-002）
- 做了什么：登记 G8 文档与质量门禁 9/9 PASS，新增 `evidence/refactor/gates/G8/CHECKLIST.md`。
- 落点：单文件（+22／+26 口径差）。
- 内容性质：控制包或台账。
- 意图证据：消息自述 "gates/G8/CHECKLIST.md(9 项证据)"、"Tests: DOC-002..005 检查器全 PASS"；变更面证据：代码面 0。
- 关联：收束 DOC 族 1332–1335；注意门号 G8 与 seq 1353 的 "G8-QA"（`gates/G8/QA_SUPPLEMENT.md`）同名分册——G8 被拆成"文档质量"与"QA 机器门"两次登记，相隔 17 个 seq。
- 不确定点：9 项条目原文未展开；"9/9"与 DOC-002..005 四条任务的映射关系未证实。

### seq 1337 | 1c7da8de | 2026-08-31 | S（REL-001）
- 做了什么：新增 `tools/check_release_layout.py`——校验发布目录布局（单一用户入口、必含文件、checksum、禁止夹带 build/testdata）。
- 落点：`tools/check_release_layout.py`（+41／+45）、`evidence/refactor/tasks/REL-001/TASK_RESULT.json`（+65）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息自述 "Change: tools/check_release_layout.py(单一入口/必含文件/checksum/无 build-testdata)"、"Tests: check_release_layout PASS; 发布产物不入 VCS(dist ignore)"；台账 REL-001→PASS、REL-002→IN_PROGRESS。
- 关联：REL 族（1337–1340）首条；被 seq 1346（G11 条目 1/2）作为唯一布局证据引用；其"发布产物不入 VCS"口径可解释 seq 1345 消息点名的 dist 产物未入库。
- 不确定点："干净 smoke"（标题自述）的执行输出未在卡片登记；台账 REL-001 行的版本字面量仍为 0.10.0-alpha.1（早于 seq 1367 的 alpha.2，属时序两期事实，非矛盾）。

### seq 1338 | 97f6ee8e | 2026-08-31 | S（REL-002，卡含 LEG-004）
- 做了什么：最终追溯校验 + 旧入口扫描——新增 `tools/check_final_traceability.py`，并再次订正 `docs/contracts/PUBLIC_API.md`（stage2 条目标 LEG-004 退出，+1/-1）。
- 落点：`tools/check_final_traceability.py`（+45／+49）、`docs/contracts/PUBLIC_API.md`（+1/-1）、`evidence/refactor/tasks/REL-002/TASK_RESULT.json`（+67）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2）。
- 内容性质：CI 或机器门 + 文档或规格 + 控制包或台账。
- 意图证据：消息自述 "Change: PUBLIC_API stage2 标 LEG-004 退出 + tools/check_final_traceability.py"、"Tests: traceability 66 claims PASS; RELEASE_STATUS 诚实"；变更面证据：合同文档一行确实改写。
- 关联：LEG-004 的第二段收口（seq 1330 立门 → 本条订正文档 → seq 1349 改构建）；REL 族第二条；本条的检查器被 seq 1357 当作 PASS 判据引用、被 seq 1367 因版本推进二次改写——**同一工具在本片四次出现在不同任务号下**。
- 不确定点："66 claims" 的来源文件（`docs/TRACEABILITY.csv`）不在本条变更面，计数无法核对。

### seq 1339 | b840ed64 | 2026-08-31 | S（REL-003）
- 做了什么：白名单审核包打包与复验——新增 `scripts/package_audit.py`（白名单 522 files）与 `scripts/validate_audit.py`（解包重验 SHA）。
- 落点：`scripts/package_audit.py`（+62／+67）、`scripts/validate_audit.py`（+31／+33）、`evidence/refactor/tasks/REL-003/TASK_RESULT.json`（+62）、`evidence/refactor/TASK_LEDGER.csv`（+2/-2：REL-003→PASS、REL-004→IN_PROGRESS）。
- 内容性质：CI 或机器门（工具脚本）+ 控制包或台账。
- 意图证据：消息自述 "package_audit.py(白名单 522 files) + validate_audit.py(解包重验 SHA)"、"Tests: VALIDATE_OK; 无 .git/build/testdata"；变更面证据：两个新脚本落 `scripts/`，zip 产物本身不入本条。
- 关联：REL 族第三条；seq 1346（G11 条目 6/7）以短码 b840ed6 与"522 files"为证据；该计数在 seq 1360 被"568 files"取代，是本片唯一可核对的跨条目计数变化。
- 不确定点：522→568 的差额 46 未证实（卡片无 diff 级证据）。

### seq 1340 | a7faa502 | 2026-08-31 | S（REL-004）
- 做了什么：负责人视觉验收视图就绪——新增 `tools/gen_visual_views.py`（6 视图）与 `evidence/refactor/tasks/REL-004/VIEWS.md`；台账与 TASK_RESULT 的状态字面量取 `REVIEW_PENDING`。
- 落点：`tools/gen_visual_views.py`（+49／+59）、`evidence/refactor/tasks/REL-004/VIEWS.md`（+12）、`evidence/refactor/tasks/REL-004/TASK_RESULT.json`（+56）、`evidence/refactor/TASK_LEDGER.csv`（+1/-1）。
- 内容性质：CI 或机器门（生成器）+ 数据或产物（VIEWS.md）+ 控制包或台账。
- 意图证据：消息自述 "REL-004: 负责人视觉验收视图就绪 (REVIEW_PENDING)"、"Tests: 6 视图生成; Agent 无权宣布发布"；变更面证据：台账 REL-004 行 IN_PROGRESS→REVIEW_PENDING，TASK_RESULT 的 status 字段同为 REVIEW_PENDING——**消息字面量与账目字面量完全一致，是本片最干净的自述／变更面吻合例**。
- 关联：REL 族末条；REL-004 依赖 REL-003（seq 1339）；这一唯一未关闭任务被 seq 1356 的"1 REVIEW_PENDING"计数、seq 1358 的 memory 段、以及 seq 1366（GOV-002 的 F-035 非法状态负例）三度引用。
- 不确定点：6 视图产物是否入库未证实（卡片仅见 VIEWS.md）；`REVIEW_PENDING` 在 V6.1 期被 seq 1366 的验证器判为非法状态，两期状态机口径冲突——登记事实，不作评价。

### seq 1341 | 16643bc1 | 2026-08-31 | S（G9-TIMEOUT，卡含 CLI-003）
- 做了什么：给 `cli/main.cpp` 的 `cmd_test_synthetic` 外部命令加 timeout 前缀（环境变量 `ASTROCS_TEST_TIMEOUT_S`，默认 600s）。
- 落点：`cli/main.cpp`（+4/-1，唯一文件；代码面 1、包与证据面 0）。
- 内容性质：生产代码。
- 意图证据：消息自述 "Change: cli/main.cpp cmd_test_synthetic 加 timeout 前缀(ASTROCS_TEST_TIMEOUT_S 默认 600s)"、"Evidence: G9 gate 条目 6"；变更面证据：**本片仅有的两条"有代码面无证据挂账"提交之一**，4 行量级与"前缀注入"吻合，hunk 头正是 seq 1326 引入的同一函数。
- 关联：与 seq 1326 构成 CLI-003 的前后两笔；被 seq 1343（G9 条目 6）以短码 16643bc 追认；被 seq 1358 的 memory 结论隐含覆盖。
- 不确定点：无任务级 TASK_RESULT 挂账，验收只能靠事后门登记；标题含"log 纪律"但变更面只见 timeout 前缀，日志留存是否同批落地未证实。

### seq 1342 | 43de4ac2 | 2026-08-31 | S（G9-CLANG，无任务号）
- 做了什么：OpenMP 可选化以支撑 clang Debug 构建——在 5 个生产源文件为 `omp.h` 包含与 `omp_get*` 调用加 `#ifdef _OPENMP` 宏保护（`lib/calibration/src/ac_api.cpp`、`calibrator.cpp`、`cosmetic_corrector.cpp`、`lib/healpix_db/healpix_drizzle/drizzle_engine.cpp`、`snr_evaluator.cpp`），无 OpenMP 时降级串行并注释"线程数由 backend 管理"；`tests/unit/CMakeLists.txt` 把 OpenMP 相关测试条件化。
- 落点：lib/calibration 3 文件（+6/+2/+2）、lib/healpix_db 2 文件（+10/+2）、tests/unit 1 文件（+11/-5）；**包与证据面 0**。
- 内容性质：生产代码 + CI 或机器门（构建条件化）。
- 意图证据：消息自述 "calibrator/cosmetic/ac_api/drizzle_engine/snr_evaluator omp.h+omp_get 宏保护; tests/unit OpenMP 条件化"、"Science: unchanged (串行退化不改变结果)"；变更面证据：五个文件的 hunk 头逐一对应消息点名的函数（`ac_set_num_threads`、`DrizzleEngine::drizzleTiledImpl`），卡片新增规格行 "# G9: OpenMP 可选 (clang Debug 无 libomp 时降级串行; P2_ENABLE_OPENMP 默认 OFF)"。
- 关联：为 seq 1343（G9 条目 2 Clang Debug）铺路，短码 43de4ac 被 G9 CHECKLIST 直接引用；亦被 seq 1350（QA-003 ASan 构建成功）与 seq 1358 的 memory 结论"omp.h 宏保护"复述。
- 不确定点：**"串行退化不改变结果"未在变更面出现任何等价性测试**，属自述未证实；卡片"点名与变更面交集=无交集"仅因点名对象是宏与头名而非路径，不构成矛盾。

### seq 1343 | 0940febb | 2026-08-31 | S（G9-GATE，无任务号）
- 做了什么：登记 G9 Linux 平台门禁 6/6 PASS，新增 `evidence/refactor/gates/G9/CHECKLIST.md`。
- 落点：单文件（+21／+26）。
- 内容性质：控制包或台账。
- 意图证据：纯登记；六项判据原文可读（GCC Release clean build、clang++ Debug + 短码 43de4ac、"GCC: test synthetic all 7 组 PASS; clang: calibration/UPM PASS"、"taskset -c 0,1 p2_workers_test PASS; SIGINT cancel 正常"、"doctor selftest ok; --version 0.10.0-alpha.1+g<commit>"、短码 16643bc）。
- 关联：收束 seq 1341＋1342；与 seq 1347（LNX-001..005）同属 G9 段且**门先过、任务台账后补**（G9 在 1343 已过，LNX 行在 1347 才置 PASS）；被 seq 1346（G11 的 Windows 继承说明）与 seq 1344 形成平台两面。
- 不确定点：条目 4 的 2 核利用率门只给测试名与命令行，未给利用率数值。

### seq 1344 | 42d5dd78 | 2026-08-31 | S（G10-GATE，无任务号）
- 做了什么：登记 Windows 侧离线——新增 `evidence/refactor/gates/G10/STATUS.md`，7 项标 `WAITING_WINDOWS`，记录 Fatduck DNS 失败。
- 落点：单文件（+14／+17）。
- 内容性质：控制包或台账。
- 意图证据：消息自述 "Change: gates/G10/STATUS.md(7 项 WAITING, Fatduck DNS 失败)"、"Contracts: G10 gate; AGENTS.md Fatduck 离线不阻塞"、"Tests: ssh fatduck 探测失败(登记)"；变更面证据：只有 STATUS.md，**AGENTS.md 未被改动**（消息点名属规范依据引用而非落点）。
- 关联：与 seq 1354／1355（G10-WIN 台账登记）、seq 1360（G10 审核包记录）同族——G10 在本片被拆成四点（1344／1354／1355／1360），是最分散的编号族；seq 1346（G11）标题限定"Linux 侧"即依赖本条登记。
- 不确定点：DNS 失败的主机与命令输出未登记；"7 项"与 seq 1354 的 6 项 WIN 任务数目不同，口径未证实。

### seq 1345 | 457dcf6b | 2026-08-31 | S（G11-CONSISTENCY，无任务号）
- 做了什么：新增 `tools/check_release_consistency.py`——校验 VERSION／SBOM／checksums 三者一致。
- 落点：`tools/check_release_consistency.py`（+47／+51）、`evidence/refactor/tasks/REL-CONSISTENCY/TASK_RESULT.json`（+67）。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息自述 "Change: tools/check_release_consistency.py + dist/astrocs-alpha/SBOM.json"、"Tests: REL_CONSISTENCY_PASS; P0/P1=0"；变更面证据：TASK_RESULT 的 task_id 为 "G11-CONSISTENCY" 而 change_class 为 "release"；**dist 产物未入库**（与 seq 1337 自述"发布产物不入 VCS"一致）。
- 关联：为 seq 1346（G11 条目 3）提供唯一证据（短码 457dcf6 + "SBOM 8 组件"）；本检查器在 seq 1367 因版本推进被再次改写。
- 不确定点：SBOM 内容不在仓库证据面，8 组件计数未证实；任务目录名 REL-CONSISTENCY 与 task_id G11-CONSISTENCY 不一致，命名口径未证实。

### seq 1346 | 04a00e11 | 2026-08-31 | S（G11-GATE，无任务号）
- 做了什么：登记 G11 发布门禁 Linux 侧 7/7 PASS，新增 `evidence/refactor/gates/G11/CHECKLIST.md`。
- 落点：单文件（+16／+20）。
- 内容性质：控制包或台账。
- 意图证据：纯登记；七项逐一回指前序证据（457dcf6、97f6ee8、b840ed6、REL-001），并在"待负责人"段复述 G10 的 WAITING_WINDOWS 登记。
- 关联：收束 seq 1337–1340 ＋ 1345；是 G5／G6／G7／G8／G9 之后的第五道 V6 门（本片门登记共 9 条）。
- 不确定点：条目 5 的"FINDINGS.csv 空 → P0/P1=0"——该 CSV 不在本片任何变更面，未证实；"Linux 可验 7/7"的限定不得被读作发布门全过。

### seq 1347 | 38f62f2a | 2026-08-31 | S（LNX-001..005；结构模式：批量证据登记）
- 做了什么：一次性登记 Linux 平台五任务全 PASS——新增 `evidence/refactor/tasks/LNX-00{1..5}/TASK_RESULT.json`（每份 +34，五份同构），并把台账 LNX-001..005 五行由 NOT_STARTED→PASS，同时把 QA-001 行推进到 IN_PROGRESS。
- 落点：全部在 `evidence/refactor/`（6 文件 +176/-6）；**代码面 0、测试面 0、文档面 0**。
- 内容性质：控制包或台账（批量登记，结构模式）。
- 意图证据：消息自述 "LNX-001(GCC Release)/002(clang Debug)/003(合成 all)/004(2核+取消)/005(doctor smoke) 证据"、"Tests: 全 rc=0 (GCC/clang 构建, 7 组合成, 2 核 P2-002, doctor)"；变更面证据：五份 TASK_RESULT 行数完全相同（+34），构成"一任务一提交"纪律的一处批处理例外。
- 关联：兑现 seq 1343 所过 G9 门的任务级账；QA-001 行的推进为 seq 1348 的前置；台账 LNX-003 的依赖字段指向 QA-003（要到 seq 1350 才 PASS），呈"后补前置"的账目拓扑。
- 不确定点：五份 TASK_RESULT 的 commands/exit_code 未逐项展开；卡片"消息中的编号"含 LNX-00（标题 "LNX-001..005" 的截断误提取）与 P2-002（条目 4 引用的跨族任务号），均非本条真实任务号。

### seq 1348–1352 | 41943d42, f8001e29, 6a7bdb49, 1ee07955, 60493ea3 | 2026-08-31 | S×5（QA-001…QA-005）
- 做了什么：QA 五任务连续施工。
  - seq 1348（QA-001）：`cli/CMakeLists.txt` 移除 P2/P3 生产源的 -w 警告抑制（删除两处 `set_property(SOURCE … PROPERTY COMPILE_OPTIONS ACS_WARN_SUPPRESS)`，`sampler.cpp` 的 MSVC 选项由 "/EHa;ACS_WARN_SUPPRESS" 改为 "/EHa"），新增 `tools/check_warning_suppression.py`。
  - seq 1349（QA-002）：同文件把 cfitsio 由 `file(GLOB …)` 改为 60 个源显式清单（buffers.c／cfileio.c／checksum.c／drvrfile.c／drvrmem.c／drvrnet.c／editcol.c …），P2、P3 源亦改显式清单，并把 ACR 4 文件剔出生产构建；新增 `tools/check_serial_hardcode.py`。
  - seq 1350（QA-003）：仅登记 ASan 验证证据（TASK_LEDGER +2/-2、`tasks/QA-003/TASK_RESULT.json` +67），无代码面。
  - seq 1351（QA-004）：新增 `tools/check_duplication.py` ＋ `evidence/refactor/tasks/QA-004/DUPLICATION_REVIEW.md`（逐区域 KEEP/MIGRATE/DELETE 判定，含 `lib/phase1/wcs/wcs_tan.{h,cpp}`；结论 WCS/weight/config KEEP、scheduler DELETE(LEG)）。
  - seq 1352（QA-005）：新增根 `DEPENDENCIES.md`（工具链锁定：GCC 14.2.0／Clang 18／CMake 3.31.6／Python 3.13／vendored cfitsio 60 源显式清单）＋ `tools/check_reproducible_build.py`。
- 落点：`cli/CMakeLists.txt`（seq 1348、1349 两次）、四个新 tools 检查器、`DEPENDENCIES.md`、`evidence/refactor/tasks/QA-00{1..5}/`、`TASK_LEDGER.csv`。
- 内容性质：生产代码（构建面，1348/1349）＋ CI 或机器门 ＋ 文档或规格（1352）＋ 控制包或台账；seq 1350 为纯证据登记。
- 意图证据：各条 Change 行与变更面逐项对应（如 seq 1349 的"cfitsio 60 显式/P2/P3 显式; ACR 4 文件剔出"对应 GLOB 删除行与显式 set 清单新增行）；卡片为 seq 1349 抽出三条新增构建注释标题（"QA-002: cfitsio 60 源显式清单 (禁 GLOB)"、"…ACR 文件 (acr_kernels/kernel_registry/device_executor/cuda_bridge_stub)"、"不编入生产 (LEG-004 ACR dormant, 生产仅纯 CPU)"），属面内显式意图声明。
- 关联：编号 BLD-001 贯穿 seq 1348/1349（承上一片 seq 1275 的构建目标图）；LEG-004 在 seq 1349 第三次兑现；QA-004 台账行依赖 LEG-002;LEG-003;LEG-004（回接 seq 1328–1330）；五条由 seq 1353 统一收束。
- 不确定点：seq 1350 **完全无代码/测试面**，ASan 与"G3 cfitsio 受限解除"仅有 TASK_RESULT 自述与 log_sha256 挂账；seq 1349 的"nm acr=0"同为自述；seq 1352 的"重构建 build id 一致"无输出摘录。

### seq 1353 | 04a5d7cc | 2026-08-31 | S（G8-QA，卡含 QA-001）
- 做了什么：G8 门的 QA 分册登记，新增 `evidence/refactor/gates/G8/QA_SUPPLEMENT.md`（5/5 PASS）。
- 落点：单文件（+14／+18）。
- 内容性质：控制包或台账。
- 意图证据：纯登记；文件内表格逐行引用被收束任务的短码（41943d4／f8001e2／6a7bdb4／1ee0795／60493ea）并写明"HEAD=60493ea"，同时登记"LSan ptrace 受限"这一受限项与"验证命令（四个 python 检查器 + ASan 7 组合成）"。
- 关联：与 seq 1336（G8 CHECKLIST）同门分册，二者共同构成 G8；本片"门登记类单文件提交"共 9 条（1323/1331/1336/1343/1344/1346/1353/1360/1371）。
- 不确定点："4 检查器 PASS + ASan 合成 PASS"与实际 QA 工具数（本区 4 个新脚本）一致，但 seq 1345 的 release 一致性检查器未计入该门，口径未证实。

### seq 1354–1355 | 196b2e8e, 8be6eb4e | 2026-08-31 | S×2（WIN-001；同编号连续，合并成组）
- 做了什么：Windows 六任务登记为 `WAITING_WINDOWS`，并在第二条修复第一条造成的台账完整性破损。seq 1354 对 `evidence/refactor/TASK_LEDGER.csv` 做 +0/-10（把 WIN-001..006 与 REL-001..004 共 10 行整体删除，另 `gates/G10/STATUS.md` +4/-0）；seq 1355 对同一 CSV 做 +10/-0（十行原样恢复），使台账回到 88 行。
- 落点：`evidence/refactor/TASK_LEDGER.csv`（seq 1354 −10 行／seq 1355 +10 行）、`evidence/refactor/gates/G10/STATUS.md`（+4）；两条代码面均为 0。
- 内容性质：控制包或台账（含一次自修）。
- 意图证据：消息自述差异即关键证据——seq 1354 仅称"LEDGER WIN-001..006 → WAITING_WINDOWS (Fatduck 离线登记)"；seq 1355 明确写"LEDGER 恢复 88 任务完整 (上提交异常截断修复); WIN-001..006 → WAITING_WINDOWS"并给出账目"88 任务 = 81 PASS + 6 WAITING + 1 REVIEW_PENDING"；变更面证据：seq 1354 的删除行集合与 seq 1355 的新增行集合**完全同一**，构成可核对的"误删—回补"对。
- 关联：与 seq 1344／1360 同属 G10 族；被删的 REL-001..004 正是 seq 1337–1340 改写的四行，故本组误删波及整个 REL 族的收口状态；seq 1355 的 88/81/6/1 账目被 seq 1356 原样写入 RELEASE_STATUS.md，并在 seq 1366（GOV-002）被反向规则化（F-034 提前 PASS／任务数不符负例）。
- 不确定点：seq 1355 恢复的 WIN 行其状态列在卡片摘录中被截断，**WAITING_WINDOWS 字面量是否最终写入这 6 行未证实**（仅由标题与 seq 1356 的计数间接支撑）；seq 1354 截断的技术成因未证实。

### seq 1356–1357 | 32be7024, ae140e5d | 2026-08-31 | S×2（REL-002 措辞链；同编号连续，合并成组）
- 做了什么：订正负责人审查文档 `docs/review/RELEASE_STATUS.md` 的完成度表述。seq 1356（+19/-14）把阶段表述从"ALPHA 重构执行中 (G0..G11 任务 DAG)／当前 G8"改写为"ALPHA 重构 Linux 侧完成, 待负责人最终审核"，小节结构改为"未完成/等待项（诚实标注, 无“基本完成”）"、"已验证 (PASS)"、"环境限制 (登记)"，删除原"发布判据 (未过硬门不得称完成)"与"已知环境限制 (移交)"两节，并写入"已过门 G0/G1/…/G9 + G11(Linux 侧)"与"88 任务 = 81 PASS + 6 WAITING_WINDOWS + 1 REVIEW_PENDING"、"QA-001..005 5/5 PASS; LNX-001..005 5/5 PASS"。seq 1357（+1/-1）仅把该小节标题再改为"未完成/等待项 (诚实标注; 未过项一律 NOT VERIFIED/FAIL, 无模糊完成措辞)"。
- 落点：`docs/review/RELEASE_STATUS.md`（两条唯一文件；代码面 0、包与证据面 0）。
- 内容性质：文档或规格。
- 意图证据：消息自述分别为"诚实标注 81 PASS/6 WAITING/1 REVIEW"与"措辞修正 (无模糊完成措辞, checker PASS)"；变更面证据：卡片"文档规格标题 新增/删除"区块逐条列出小节改名（seq 1356 为新增 3 节／删除 2 节，seq 1357 为 1 换 1），是"为过机器门而改标题字面量"的直接形态证据。
- 关联：改写对象由 seq 1332（DOC-002）创建；计数口径来自 seq 1355；判据检查器为 seq 1338 的 `check_final_traceability.py`；与 seq 1359 构成同一文档的三连措辞链（1356→1357→1359），中间夹 seq 1358。
- 不确定点：seq 1356 删除"发布判据"节的原因未在任何证据中说明，未证实（不作推测）；seq 1357 是否即为 checker 不通过的唯一变动点，卡片示 1 换 1 标题但未配门输出。

### seq 1358 | 00032018 | 2026-08-31 | S（MEM，无任务号）
- 做了什么：在根 `memory.md` 追加 V6 重构终态五条结论——88 任务账目（81 PASS／6 WAITING_WINDOWS／1 REVIEW_PENDING，指明为 REL-004 视觉审核）、门覆盖（G0-G9 + G11(Linux 侧) 全 PASS、G8 QA 5/5、G10 Windows 登记离线）、关键质量成果（P2/P3/cfitsio GLOB→显式清单 ＋ ACR 4 文件剔出生产，nm acr=0；ASan 构建成功使 G3 受限解除，靠 omp.h 宏保护；14/14 检查器 PASS）、状态字面量 `AWAITING_EXTERNAL_RELEASE_REVIEW`（并写明"Agent 无权宣布发布；待负责人视觉审核 ＋ Fatduck 恢复后 G10"）。
- 落点：`memory.md`（+5/-0，唯一文件；代码面 0、包与证据面 0）。
- 内容性质：文档或规格（记忆／稳定结论）。
- 意图证据：消息自述 "Change: memory.md 终态结论"、"Contracts: 工程管理 (稳定结论)"、"Tests: n/a"；变更面证据：五行全为新增，卡片 hunk 头出现 "SHA256 e2130977…" 上下文行，说明追加位置在既有哈希记录段之后。
- 关联：是全片对 1321–1357 的自述性总结；所记 HEAD 为 ae140e5（=seq 1357），即本条以 seq 1357 为快照，其后 seq 1359 又改文档，故 memory 的 HEAD 记录在落库后即滞后一条。
- 不确定点：`AWAITING_EXTERNAL_RELEASE_REVIEW` 的定义处不在本片证据面（卡片未含该字面量来源）；"14/14 检查器"的清单未证实。

### seq 1359 | b16d422a | 2026-08-31 | S（REL-002 第三笔）
- 做了什么：`docs/review/RELEASE_STATUS.md` 小节标题再次改写——由"诚实标注; 未过项一律 NOT VERIFIED/FAIL, 无模糊完成措辞"改为"owner review 范围; 未过项一律标 NOT VERIFIED/FAIL, 无模糊完成措辞"（+1/-1）。
- 落点：`docs/review/RELEASE_STATUS.md`（唯一文件）。
- 内容性质：文档或规格。
- 意图证据：消息自述 "Change: RELEASE_STATUS.md 未过项标注 owner review 范围"、"Tests: 14/14 检查器全 PASS"；变更面证据：单行标题替换。
- 关联：与 seq 1356／1357 同族同文件三连；**本条 sha8 b16d422 成为全片最重要的下游锚点**——seq 1360 记录"最终审核包 HEAD b16d422"，seq 1361 把 `b16d422_is_ancestor` 与 merge-base 校验写入 SOURCE_IDENTITY，seq 1364 的 G0 条目 3 复述该祖先判定。
- 不确定点：为何在 seq 1357 之后仍需再改一次措辞（checker 具体不通过项）未证实。

### seq 1360 | 30a3516a | 2026-08-31 | S（REL-003；G10-STATUS）
- 做了什么：在 `evidence/refactor/gates/G10/STATUS.md` 追加"最终审核包 (2026-08-31, HEAD b16d422)"小节，记录审核包位置／SHA、568 files、VALIDATE_OK。
- 落点：`evidence/refactor/gates/G10/STATUS.md`（+5／+6，唯一文件）。
- 内容性质：控制包或台账 + 数据或产物（外部包 SHA 记录）。
- 意图证据：消息自述 "Change: gates/G10/STATUS.md 记录最终审核包位置/SHA"、"Tests: VALIDATE_OK (568 files)"；变更面证据：仅新增小节，本条无重打包动作。
- 关联：任务号归 REL-003（工具在 seq 1339）、落点归 G10（登记在 seq 1344/1354/1355）——本片唯一"任务号与门号分属两族"的记录提交；作为 V6 段的收尾（其后即 575 分钟空窗与 V6.1 段）。
- 不确定点：568 files 与 seq 1339 的 522 files 差额未证实；审核包 zip 的实际路径未在卡片摘录范围。

### seq 1361 | a3d16d85 | 2026-08-31 | M（R0-001；V6.1 段起点）
- 做了什么：V6.1 返工控制包的起点——新建 `evidence/v6_1_rework/` 证据根并写入：新 `TASK_LEDGER.csv`（+68：表头 ＋ R0-001..004／GOV-001..002／VER-001／CHK-001..003／RT-001..009／CPU-001..004／MON-001..003／P1-001..003 等任务，gate 列取 G0..G4）、`tasks/R0-001/PLAN.md`、`SOURCE_IDENTITY.json`（键含 head_commit／main_commit／origin_main_commit／branch／`b16d422_is_ancestor`／ancestor_check_command／ancestor_check_exit／reviewed_audit_commit／control_zip_sha256／ledger_sha256／branch_list／remotes_redacted／submodules／dirty_entries／dirty_classification）、`WORKTREE_CLASSIFICATION.md`（小节：三 SHA 身份结论／已跟踪修改 5 项／未跟踪项 200 项分类／冻结要求核对／结论）、`TASK_RESULT.json`、`logs/identity.log`。
- 落点：全部在 `evidence/v6_1_rework/`（6 文件 +303/-0）；代码面 0。
- 内容性质：控制包或台账（新证据根目录建立，属结构建立）。
- 意图证据：消息**无正文**（正文长度 0），自述仅标题"冻结当前 main 身份 (三 SHA 相同, b16d422 为祖先, 工作树 205 项全分类, CONTROL_PASS)"；变更面证据：SOURCE_IDENTITY 的键集与 WORKTREE_CLASSIFICATION 的五级小节支撑标题三项断言；seq 1364 的 G0 条目 2/3/4 分别回指本条 identity.log、"git merge-base --is-ancestor b16d422… HEAD exit=0"、"205 项全分类，0 未知"。
- 关联：与 seq 1360 之间存在本片唯一 >30 分钟的时段分界（575 分钟）；开启 V6.1 台账族——seq 1362–1375 全部改写在 `evidence/v6_1_rework/TASK_LEDGER.csv` 上；同时**终结** V6 台账 `evidence/refactor/TASK_LEDGER.csv`（该文件自本条后不再出现于任何变更面）。
- 不确定点：控制包 `AstroCS_V6_1_REWORK_CONTROL_20260831` 的解压内容不在本片核对范围（其"CONTROL_PASS files=47 tasks=67"仅由 seq 1364 的 G0 条目 1 文本转述）；67 任务与本条实加 68 行（含表头）自洽，但未独立点票。

### seq 1362 | 31b10cab | 2026-08-31 | L（R0-002；结构模式——批量清单产出，按文件集合与目录直方图归纳）
- 做了什么：生成完整自有源码清单并配双工具——新增 `tools/quality/gen_source_index_v61.py`（+270）与 `tools/quality/check_source_index_v61.py`（+82）；产出 `SOURCE_INDEX.csv`（+1121 行，列 path,kind,owner,target,sha256,size_bytes,generated,required_for_build）、`TARGET_SOURCE_GRAPH.json/.dot`、`third_party_sources.csv`（+240）、PLAN、TASK_RESULT、logs，台账 R0-002 行 NOT_STARTED→PASS。
- 落点（目录直方图，不逐文件展开）：`evidence/v6_1_rework/` 8 文件 +1848/-1（清单与图产物，占本条约 82% 行数）；`tools/quality/` 2 文件 +402/-0——**该工具子目录首次出现**。
- 内容性质：CI 或机器门（生成器 ＋ 校验器）＋ 数据或产物（清单／图）＋ 控制包或台账。
- 意图证据：消息（无正文）标题自述"58 targets 1120 文件, 生产源全部证明存在, 负例缺源 FAIL"；变更面证据：PLAN "## 需求" 明列七列 schema、第三方单列、"故意删除一个 CMake source 的 fixture 必须让 checker 非零退出"；"## 现状证据"记"根 CMakeLists 声明 19 个生产 target（astrocs CLI + 18 库）、tests/unit 39 个测试 executable"；"## 风险"自承"CMake 解析用正则近似；不解析变量展开"；"## 验收命令"给出三条命令与负例（隐藏 `lib/io/src/io_adapter.cpp` → SOURCE_INDEX_FAIL missing target sources）。
- 关联：`tools/quality/` 自本条起为 seq 1363/1364/1365/1366/1368/1369/1370 共用（本片 8/55 条改该目录），构成 V6.1 工具族根；依赖 R0-001（seq 1361）；TARGET_SOURCE_GRAPH 为 seq 1368（CHK-001 可达图）的前置口径；其清单 hash 被 seq 1364 的 G0 条目 5/6 回指。
- 不确定点：1120 文件／58 targets／239 vendored 三个计数以 PLAN 与 seq 1364 的 G0 条目自述为准（卡片仅示 SOURCE_INDEX 行数级）；正则近似对生成源的处理是否完整未证实。

### seq 1363 | d7b202ec | 2026-08-31 | M（R0-003）
- 做了什么：建立 V6.1 证据台账与状态机验证器——新增 `tools/quality/validate_task_ledger.py`（+236）与 `tools/quality/check_task_result_schema.py`（+121）；产出 `tasks/R0-003/ledger_state.json`（+90，台账 hash 冻结状态）、PLAN、TASK_RESULT（+138）、logs/c01.log；台账 R0-003 行推进。
- 落点：`tools/quality/` 2 文件 +406、`evidence/v6_1_rework/` 5 文件 +272/-1。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息（无正文）标题"10 负例 PASS, 冻结台账 hash 未变, TASK_RESULT schema 一致"；变更面证据：`ledger_state.json` 独立存在支撑"hash 冻结"；`validate_task_ledger.py` 在 seq 1366 被二次改写（+42/-18），且其 PLAN 摘录回述"R0-003 的 validate_task_ledger.py 已支持 7 状态 + 依赖检查（10 负例）"，构成交演证据。
- 关联：R0-003 依赖 R0-002（seq 1362）；本条工具在 seq 1366 强化为 V6.1 台账主门、在 seq 1371（G1 条目 2）作为 PASS 证据；V6.1 的 TASK_RESULT 键口径（parent_commit／ledger_sha256／changed_paths／argv_redacted）与本条起的新 schema 一致，区别于 V6 期的 start_commit／source_commit／files_changed。
- 不确定点：10 个负例清单未展开；"7 状态"的字面量集合未证实（与 AGENTS.md 所述 V5/V6 状态字面量的映射关系不在本片证据面）。

### seq 1364 | 50673825 | 2026-08-31 | M（R0-004）
- 做了什么：复现并冻结 40 项已知问题 ＋ G0 门达成——新增 `tools/quality/known_failures_baseline.py`（+369／+426 口径）与产物 `KNOWN_FAILURES_BASELINE.json`（+423，schema 为 astrocs.known-failures-baseline/v1，键 finding_count=40、reproduced_count=40、source_commit=d7b202ec…、repo 为工作目录路径）、`KNOWN_FAILURES_SCAN_RAW.json`（+174）、`gates/G0/CHECKLIST.md`（+15，8 项全 PASS，每项带 evidence path ＋ SHA-256），台账 R0-004 行→PASS。
- 落点：`tools/quality/` 1 文件、`evidence/v6_1_rework/gates/G0/`（**G0 门首次出现**）、`evidence/v6_1_rework/tasks/R0-004/`（5 文件 ＋ logs ×2）、台账。
- 内容性质：CI 或机器门 ＋ 数据或产物 ＋ 控制包或台账。
- 意图证据：消息（无正文）标题"40/40 REPRODUCED + G0 门清单 8/8 PASS"；变更面证据：G0 CHECKLIST 八项文本在卡片完整可读（控制包 CONTROL_PASS files=47 tasks=67、三 SHA 一致且 remote 已脱敏、merge-base exit=0、205 项 0 未知、SOURCE_INDEX sha256=7283473d…、TARGET_SOURCE_GRAPH 58 targets missing=0 ＋ 239 vendored 单列、冻结台账 sha256=c6f21304…、KNOWN_FAILURES_BASELINE sha256=dbe859b8…），是**本片六条门登记中唯一把八项判据逐项展开到 hash 粒度的一条**。
- 关联：G0 是 V6.1 门序列首站（→G1 于 seq 1371）；TASK_RESULT 的 source_commit 恰为 seq 1363 的完整 SHA，形成机器级父子链；F-001..F-040 基线成为 seq 1368／1370 两条 PLAN"现状证据"的引用来源（分别引 F-001/F-006 与 F-003/004/005/006）。
- 不确定点：40 项 finding 的逐项标题与判据未展开（JSON 仅计数与 hash），故"40 项的内容"未证实；JSON 的 repo 字段指向另一主机路径名（/home/lighthouse/…），登记为环境事实。

### seq 1365 | a52dcce1 | 2026-08-31 | M（GOV-001）
- 做了什么：把任务证据与 commit 绑定——新增 `tools/quality/gen_commits_csv.py`（+118）与 `tools/quality/check_commits_csv.py`（+181），生成 `evidence/v6_1_rework/COMMITS.csv`（+5：表头 task_id,parent_commit,result_commit,commit_subject,committed_utc,pushed_origin_main,changed_paths_sha256,task_result_sha256 ＋ R0-001..R0-004 四行，parent/result 均为 40 位完整 SHA 且首尾相接）；同时**修复前轮结果文件 schema**：改写 `tasks/R0-001/TASK_RESULT.json`（+12/-3）与 `tasks/R0-002/TASK_RESULT.json`（+10/-4）；台账 GOV-001 行→PASS。
- 落点：`tools/quality/` 2 文件 +354、`evidence/v6_1_rework/` 7 文件 +203/-8。
- 内容性质：CI 或机器门 ＋ 控制包或台账 ＋ 结构整理（旧证据 schema 订正）。
- 意图证据：消息（无正文）标题"COMMITS.csv 连续链, 篡改/交换/缺push 负例全抓, 旧结果 schema 修复"；变更面证据：本条是全片唯一"既新增自身证据、又回改历史两条任务结果文件"的提交，其 -8 行正对应"旧结果 schema 修复"；PLAN 标题 "## 需求 (04_TASK_SPECIFICATIONS.md GOV-001)" 显示规格外置于控制包文件。
- 关联：GOV 族（1365→1366）；COMMITS.csv 成为 seq 1371（G1 条目 1）的证据；"parent/result commit 不混"直接针对 V6 期 TASK_RESULT 的"start_commit=source_commit=上一提交完整 SHA"写法（见 seq 1321/1322/1337/1339 摘录），构成两期证据口径的显式对照；这也与 seq 1361 起"消息无正文、改以机器账目承载"的形态变化同向。
- 不确定点：push 缺失负例依赖 pushed_origin_main 列的取值口径，卡片未示判定实现；对 R0-001/R0-002 历史结果的回改是否改变其原结论语义未证实。

### seq 1366 | c1585123 | 2026-08-31 | S（GOV-002）
- 做了什么：修复 gate 与依赖状态机——增强 `tools/quality/validate_task_ledger.py`（+42/-18；函数 `load_ledger`、`validate_rows(rows, *, enforce_states=True)`、`SELFTEST_CASES`、`run_selftest`、`main(argv)`；新增任务数校验），实现"全部错误聚合"而非只报首个；台账 GOV-002 行→PASS。
- 落点：`tools/quality/validate_task_ledger.py`（1 文件）、`evidence/v6_1_rework/tasks/GOV-002/{PLAN.md,TASK_RESULT.json,logs/c01.log}`（+151/-1）、台账。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息（无正文）标题"12 负例全抓, 全部错误聚合, 任务数校验, F-034/035 闭环"；变更面证据：PLAN 逐条列出七类负面 fixture（非法状态、两个 IN_PROGRESS、PASS 依赖 WAITING、环、缺任务、REL 提前 PASS、task count 不符）并写明"不允许改 dependency 来使 ledger 通过"；"## 现状证据"点名 F-034（REL-001 PASS 而 WIN-006 WAITING_WINDOWS）与 F-035（REVIEW_PENDING 非法状态）。
- 关联：F-034／F-035 的当事行恰与本片 seq 1337–1340／1354／1355 造成的账目状态有关，故本条是对本片前段账目异常的事后规则化；被 seq 1371（G1 条目 2）直接引用；与 seq 1365 构成 GOV 族两连。
- 不确定点：12 负例与 PLAN 所列 7 类 fixture 的计数关系未证实；`enforce_states` 默认 True 的实际生效范围（是否覆盖 V6 期旧台账）未证实。

### seq 1367 | 99a79200 | 2026-08-31 | L（VER-001；结构模式——跨面版本改写，按文件集合与目录直方图归纳）
- 做了什么：版本推进到 0.10.0-alpha.2 并落实"VERSION 单源"。根 `VERSION` +1/-1（0.10.0-alpha.1→0.10.0-alpha.2）；`docs/VERSIONING.md`、`docs/refactor/CLI_COMMAND_LAYER.md`（--version 行改 alpha.2）、`docs/review/` 5 篇（ARCHITECTURE／CHANGE／PIPELINE／RELEASE_STATUS／SCIENCE OVERVIEW 的"目标版本"行）同步；生产侧 `lib/phase3_session/p3_session.cpp`（+7/-4，P3 provenance 改派生）；测试侧 tests/unit 3 文件、tests/backend 2 文件、`tests/version/test_version_consistency.py`（+5/-3）；工具侧 `check_version_consistency.py`（+14/-6）与 check_final_traceability／check_release_consistency／check_reproducible_build／make_linux_release／make_windows_release 各 2 行级；根 `CMakeLists.txt`（+2/-1，增二进制目录 include 以派生 version_generated.h）；evidence 6 文件（PLAN、TASK_RESULT、logs ×3）。
- 落点（目录直方图，不逐文件展开）：`evidence/v6_1_rework/` 6 文件 +226/-1 为最大块；生产／测试／工具／文档四面共 22 文件、每面行数均为个位级（±1~±7）——"一次性全库替换旧版本字面量"的典型形态；合计 28 文件 +278/-39。
- 内容性质：生产代码 ＋ 测试 ＋ CI 或机器门 ＋ 文档或规格 ＋ 控制包或台账（五面混合）。
- 意图证据：消息（无正文）标题"版本推进 0.10.0-alpha.2 (VERSION 单源, 生产源码 0.1.0/alpha.1 清零, P3 provenance 派生, checker PASS)"；变更面证据：0.10.0-alpha.1 字面量在 10 处被删除；`check_version_consistency.py` 同批被强化（"清零"由检查器守护）；根 CMake 的 include 面变化支撑"派生"。
- 关联：把 seq 1324 立的 --version alpha.1 规格、seq 1337／1345／1352 的发布与可复现工具、seq 1356／1359 的 RELEASE_STATUS 版本行一并回改；台账依赖字段为 GOV-002（seq 1366）；VER-001 任务号与上一片重叠区 seq 1265 的 VER-001（建立 0.10.0-alpha.1）同号同题，构成 V6／V6.1 两期"版本任务号复用"。
- 不确定点：卡片"消息中的编号"仅 VER-001；"生产源码 0.1.0 清零"中的裸 0.1.0 字面量未在本条 diff 摘录中单独显现，未证实；"checker PASS"仅有 c01/c03.log 挂账。

### seq 1368 | abb75893 | 2026-08-31 | M（CHK-001；本片最大证据体积条目）
- 做了什么：建立生产可达调用图检查器——新增 `tools/quality/check_prod_reachability.py`（+201／+229），基于 `compile_commands.json` ＋ `nm` 建可达图，输出 `PROD_REACHABILITY.json`（+894）与 `PROD_REACHABILITY.dot`（+385）。
- 落点：`evidence/v6_1_rework/tasks/CHK-001/` 7 文件 +1451/-1（其中 1279 行为图产物）；`tools/quality/` 1 文件。
- 内容性质：CI 或机器门 ＋ 数据或产物 ＋ 控制包或台账。
- 意图证据：消息（无正文）标题"compile_commands+nm, 负例抓 session/drizzle 直连, JSON/DOT 图, ACR 零可达"；变更面证据：PLAN 明列五条断言（CLI 只可调 public Runtime／Benchmark／Test／Verify API；session／科学内核／I-O 内部 symbol 从 CLI 不可达；生产只有一个 scheduler owner；ACR 从默认产品不可达；legacy wrapper 仅经 test registry/preset），并要求 fixture 能抓"CLI 新增一次 `hp_drizzle_run_hips` 直连"与 dead Runtime；"## 现状证据"自述 `cli/main.cpp` 当前直接 include p1/p2/p3_session.h、hp_drizzle_api.h、aio_fits.h、fitsio.h 并直接调用 `p*_session_*`、`hp_drizzle_run_hips`、`spawn_frame_from_fits`、`fits_read_*`（F-001…），且"V6 astrocs_core Runtime 在 lib/ 与 cli/ 生产代码中无调用（F-006 死代码基线）"；"## 风险"明确"当前真实二进制 FAIL 是**预期基线**（正是 RT-008 要修复的状态）"，验收为 `--selftest` → SELFTEST_PASS ＋ 真实扫描 REACH_FAIL 列违规。
- 关联：CHK 族首条（1368→1369→1370）；产物被 seq 1371（G1 条目 4）引用；其"CLI 直连 session"基线与 seq 1331（G7 条目 1）的 PASS 判定**直接对立**（见片尾遗留问题 3）；修复承诺由 seq 1372 的合同与 seq 1361 台账中的 RT-008 承载。
- 不确定点：真实扫描违规项数（seq 1371 摘录作"列出 12 项"）在本条卡片未给计数；`spawn_frame_from_fits` 等的定义模块未证实。

### seq 1369 | 1490bae5 | 2026-08-31 | M（CHK-002）
- 做了什么：建立非空静态图／运行图检查器——新增 `tools/quality/check_pipeline_graph.py`（+247／+283）：`--ir`／`--module-index`／`--trace` 三参数必填，任一缺失或 trace 零节点直接 FAIL；逐项比较 node ID、module ID/version、edge、port、DATA schema、unit、coordinate、artifact ID/hash、producer/consumer、resource class、provider、workers、status、start/end，并输出完整 diff；7 个负例 fixture。
- 落点：`tools/quality/check_pipeline_graph.py`（1 文件）、`evidence/v6_1_rework/tasks/CHK-002/{PLAN.md,TASK_RESULT.json,logs/c01.log}`、台账 CHK-002 行→PASS。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息（无正文）标题"--ir/--module-index/--trace 必填, 7 负例全抓, 空 trace FAIL"；变更面证据：PLAN 逐字列出比较字段与"negative fixtures 覆盖少节点、换 artifact、换单位、隐藏节点、空 trace"。
- 关联：与 seq 1335（V6 期 DOC-005 的 `check_pipeline_trace.py`）为同一命题的两期实现；依赖 CHK-001（seq 1368）；被 seq 1371（G1 条目 5/6）引用。
- 不确定点：本条**无真实图产物入库**（区别于 seq 1368），其"已跑通"仅有 logs/c01.log +8 行挂账；7 负例与 G1 摘录中 empty_trace／hidden_node／fewer_nodes／swapped_artifact 四项的对应关系未完全展开。

### seq 1370 | 80a00b1e | 2026-08-31 | M（CHK-003）
- 做了什么：建立 serial-heavy 与资源接线检查器——新增 `tools/quality/check_serial_heavy.py`（+135／+161）：从生产调用图定位每个 `cpu_heavy` 节点的实现与编译 target，核对并行轴、lease 调用、compile definition、OpenMP／`std::thread` 实现、MSVC／Linux 实际路径、resource gate caller；扫描固定 workers=1、`hardware_concurrency` 模块自取、裸 thread pool、serial pixel/sample loop、全局锁；allowlist 仅允许 <5s I/O/metadata（须含 owner/reason/expiry/test）。
- 落点：`tools/quality/check_serial_heavy.py`（1 文件）、`evidence/v6_1_rework/tasks/CHK-003/{PLAN.md,TASK_RESULT.json,logs/c01.log,logs/c02.log}`、台账 CHK-003 行→PASS。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息（无正文）标题"P2 宏关/P3 双循环/无 gate caller 负例全抓, allowlist 登记"；变更面证据：PLAN 小节标题即为"## 现状证据（真实扫描基线，F-003/004/005/006 复现）"，把本检查器直接挂到 seq 1364 的 finding 基线；验收要求"负面 fixture 必须抓：Phase2 宏关闭、P3 双循环、无 gate caller"。
- 关联：CHK 族末条；与 seq 1342（omp.h 宏保护）构成正反对照——本检查器把"P2 宏关闭"当缺陷抓，而 seq 1342 正是为 clang 构建引入该宏分支；与 seq 1349（V6 期 QA-002 的 `check_serial_hardcode.py`）为同一命题的两期实现。
- 不确定点：G1 摘录称"真实扫描 8 项基线"，本条卡片未展开该 8 项；`logs/c01.log +1/-0` 的行数量级异常小，其内容完整性未证实。

### seq 1371 | 545bd4d4 | 2026-08-31 | S（G1-STATUS，V6.1 门）
- 做了什么：登记 V6.1 的 G1 门 7/7 PASS，新增 `evidence/v6_1_rework/gates/G1/CHECKLIST.md`（+14／+19）。
- 落点：单文件，代码面 0。
- 内容性质：控制包或台账。
- 意图证据：消息（无正文）标题"G1 门清单 7/7 PASS (治理/版本/检查器)"；变更面证据：七项判据原文可读，逐项回指 COMMITS.csv（seq 1365）、validate_task_ledger 12 负例（seq 1366）、VERSION=0.10.0-alpha.2 ＋ check_version_consistency（seq 1367）、check_prod_reachability（seq 1368）、check_pipeline_graph 与其负例（seq 1369）、check_serial_heavy（seq 1370）；结论段自述"任务—commit 绑定、状态机、版本单源、生产可达性、图/trace 一致性、serial-heavy/资源接线检查器均已建立并带负例"。
- 关联：收束 GOV／VER／CHK 三族共 6 个 seq（1365–1370）；与 seq 1364（G0）构成 V6.1 门序列前两站。
- 不确定点：条目 4 的"真实扫描列出 12 项"与 seq 1368 的违规计数是否同源未证实；V6.1 的 G1 未像 V6 期 G8 那样拆分册，口径差异未证实。

### seq 1372–1375 | ef9e34db, 30c83b45, a03077ec, 90cf35a8 | 2026-08-31 | M×4（RT-001…RT-004）
- 做了什么：V6.1 的 Runtime 与模块公共合同冻结及其前四项实现。
  - seq 1372（RT-001）：新增 `include/astrocs/core/runtime.h`（+83）并大幅扩写 `include/astrocs/core/context.h`（+69/-2）与 `lib/core/src/runtime.cpp`（+56）、`lib/core/src/context.cpp`（+32）；类型 `class ThreadBudget`、`class IModule`、`class Runtime`、`class RuntimeImpl`、`struct ModulePlan`；新增声明 `acquire(min, max) noexcept`、`release() noexcept`、`make(size)`、`available()/budget()/reset_available()`、`validate_config(config_json)`、`plan(node_id, …)`、`execute(RunContext&)`、`inspect()`、`load_pipeline(ir_json, …)`、`run(ctx)`、`cancel()`；删除旧 `ThreadLease(uint32_t size)` 构造（改为携带 release 回调）；新增 `docs/contracts/RT-001.md`（+38）与 `tests/unit/rt001_abi_test.cpp`（+100，含 test_error_domain_layout／test_scalar_unit_layout／test_artifact_layout／test_port_layout／test_module_descriptor_fields／test_budget_factory_signature／test_runtime_header_includes）；根 `CMakeLists.txt` 把 `lib/core/src/runtime.cpp` 纳入 `astrocs_core`。
  - seq 1373（RT-002）：`acquire` 增第三参数（策略）并新增 `_make_lease(got) noexcept`、`enum class`（策略枚举）、RAII 归还；`tests/unit/rt002_budget_test.cpp`（+152）含 test_two_heavy_nodes_no_oversubscribe／test_nested_lease_no_double_count／test_exception_restores_available／test_cancel_restores_available／test_policies／test_single_worker_reference；引入 <mutex> <condition_variable>。
  - seq 1374（RT-003）：`context.h`（+22/-12）删除 `add_metric`／`record_tick`／`mark_checkpoint` 的无锁内联实现，改为带锁版本；新增快照式访问 `get_artifact(id, DataArtifactDescriptor* out) const`（替代返回裸指针的形式，支撑"唯一 producer"）；`lib/core/src/scheduler.cpp`（+4/-2）为 budget 竞争修复点；新增 `tests/unit/rt003_context_test.cpp`（+109，含 test_concurrent_nodes／test_duplicate_conflict／test_concurrent_cancel）并改写 `tests/unit/core_context_test.cpp`（+5/-3）。
  - seq 1375（RT-004）：`include/astrocs/core/pipeline.h`（+23/-14）新增 `enum class IrError : uint8_t`、`struct IrIssue`、`class PipelineIRParser`（`parse`／`validate`）；`lib/core/src/pipeline.cpp`（+233/-176）**删除手写 JSON 字符串解析函数** `json_trim`／`top_key_value`／`obj_key_str`／`obj_key_obj`，改 include `nlohmann/json.hpp`（新增 include 另含 `astrocs/core/module.h`、<sstream>）；根 `CMakeLists.txt` 把 third_party 加入 `astrocs_core` 的 PUBLIC include；`tests/unit/core_pipeline_test.cpp`（+202/-12）覆盖错误种类（test_parse_bad_schema／test_parse_serial_heavy_rejected／test_validate_unknown_module／test_validate_missing_port／test_validate_unit_mismatch／test_validate_coordinate_mismatch／test_validate_data_mismatch／test_validate_duplicate_producer／test_validate_cycle／test_control_package_fixtures）。
- 落点：`include/astrocs/core/`、`lib/core/src/`、`tests/unit/`、`docs/contracts/RT-001.md`、根与 tests 的两级 CMakeLists、`evidence/v6_1_rework/tasks/RT-00{1..4}/`、台账 RT 行逐条→PASS。
- 内容性质：生产代码 ＋ 测试 ＋ 文档或规格（合同冻结）＋ 控制包或台账（四条混合；本片生产代码密度最高的一段）。
- 意图证据：四条消息均**无正文**，但变更面证据极强——每条均为"生产实现 ＋ 专属测试文件 ＋ 台账一行推进"的对称结构；seq 1375 的"删除手写解析函数 ＋ 新增 nlohmann include ＋ +233/-176"三项同现，与其 PLAN 自述"使用真正 JSON parser+schema，不写脆弱字符串解析"直接对应；seq 1372 的 `docs/contracts/RT-001.md` 与 PLAN 双文档（合同文本含"CLI→Runtime→Registry→Module→CPU/I-O 唯一调用链；禁止 CLI 直连 session/科学内核；禁止第二套调度器；ACR 不注册不链接"）显示：该合同的边界规则正是对 seq 1368 抓出的可达性基线的反向冻结。
- 关联：依赖链严格接续（RT-001←CHK-003(seq 1370)、RT-002←RT-001、RT-003←RT-002、RT-004←RT-001），四者为 seq 1361 新台账 G2 段的前四项（后续 RT-005..009 仍在 NOT_STARTED）；`lib/core` 与 `include/astrocs/core` 因本段重新成为生产代码面（与上一片 1276–1283 的 CORE-001..008 构成同一模块的第二轮实现）。
- 不确定点：seq 1372 标题"ABI layout 测试 9/9 PASS"与卡片可见的 7 个 test 函数名数目不符（其余 2 项未证实）；seq 1373 的"三策略"枚举名未在卡片出现（仅见 `enum class`），"8x PASS"为自述；seq 1374 的"TSan 4 测试 PASS"仅有 TASK_RESULT 与 logs 挂账，无 TSan 输出摘录；seq 1375 的"9 错误种类全 fixture／CLI 重链接通过"无链接日志摘录，`IrError` 的完整枚举成员未证实。
