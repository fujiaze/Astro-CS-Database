# 逐条详析 （证据层过程件）

> 本文件是 设计大纲/reports/history/slices/H-S13.md 的逐条正文；最终交付只保留两份大报告，逐条正文下沉到证据层（条目文本零改动，仅移动）。
> 由 _tools/11_split_slice_reports.py 从入库基线 a3a343a4 重切，日期 2026-09-15；条目组 55 个；报告树保留段（主题归纳/重叠区核对/遗留缺口）见原文件。

## 一、主责条目正文（seq 661..715，55 条）

### seq 661 | 03d5d961 | 2026-08-14 | L
- 做了什么：phase2-v17 遗留清除（legacy removal）：healpix_stack 整体迁入 archive/legacy（37 文件以改名形态移动，+0/-0 纯搬家）；orchestrator 删除遗留 Stage2 接线（不再 DLL 加载、删除对应 handlers，5 文件净 -289 行）；新增两个配套脚本 tools/migrate_stage2_config.py（stage2 配置迁移）与 tools/no_legacy_production_reference.py（"生产面零遗留引用"机器门）。
- 落点：lib/healpix_db（37 改名）、lib/orchestrator（5）、tools/ 两脚本。分级 L，按目录直方图归纳，不逐文件展开。
- 内容性质：结构整理（归档搬家）+ 生产代码（orchestrator 删线）+ CI 或机器门（no_legacy_production_reference 门）。
- 意图证据：消息自述"legacy removal / no DLL load / gate PASS"；变更面证据一致——healpix_db 37 文件 0 增 0 删的改名形态 + orchestrator 大量净删除 + 门脚本新增，与自述逐项吻合。
- 关联：承接 seq 660（ed7a544d）的 v17 集成契约工作；其删除的直接后果由 seq 665（2d3b31fc）修复。
- 不确定点：消息中的 "config migration tool" 指 migrate_stage2_config.py，其运行效果证据不在本片（未证实）。

### seq 662 | efdbe7e2 | 2026-08-14 | M
- 做了什么：建立"受控干净数据剔除真值"（controlled clean rejection truth）测量面：新增 lib/phase2/tools/controlled_rejection_metrics.py（零 outlier 合成 20 帧上的 FPR / Siril 1.4.3 冻结同源对照 / 科学保留 / outlier 召回指标，文件头自述 NON_PRODUCTION_TOOL_ONLY）与 controlled_rejection_truth.py；rejection_cli.cpp 遵从 normalization 与 WBPP Light 类型化默认；satellite_gate_real_metrics.py 把 real16 指标改名为 observed_rejection_rate。
- 落点：lib/phase2/tools/ 四文件（+626/-7）。
- 内容性质：测试/测量工具（Python，非生产）+ 生产代码（rejection_cli.cpp）。
- 意图证据：消息自述与变更面一致（两新脚本 + 两改名/修默认的行为文件）；"避免 AstroCS 自身过拒而不自知"是卡内脚本注释自述的动机。
- 关联：与 seq 663（c7cb2e69）同为 phase2-v17 剔除线；改名 observed_rejection_rate 与 seq 664（85851c2f）文档订正同一命名主题。
- 不确定点：FPR/召回等指标数值结论未随卡摘录，未证实。

### seq 663 | c7cb2e69 | 2026-08-14 | M
- 做了什么：新特性 astrocs.large_scale_rejection.v1：在逐帧 low/high 剔除掩膜上做连通域生长（min structure size、low/high 独立半径），stage2 增加两遍 CPU 路径；同步 schema/template/config 一致性；附 5 个单元测试；自述 74/74 门 PASS。
- 落点：lib/phase2（6 文件：rejection.h/.cpp、stage2_common.h/.cpp、stage2.cpp、synthetic_gate.cpp）、tools/config_consistency_check.py、工程控制/configs/stage2.template.json、工程控制/schemas/stage2.schema.json。
- 内容性质：生产代码 + 测试 + 控制包或台账（schema/template）。
- 意图证据：消息自述与变更面吻合（代码 6 + 契约 2 + 一致性工具 1）；"74/74 PASS"为自述，无独立测试输出面佐证于卡内（未证实）。
- 关联：延续 seq 662 的剔除测量线；seq 664 的 rejection_semantics 文档订正引用其成果。
- 不确定点：连通域生长的具体阈值语义以文档为准，卡内无。

### seq 664 | 85851c2f | 2026-08-14 | M
- 做了什么：文档/API/config 机器一致性收口：PUBLIC_API、SCIENCE_FREEZE、rejection_semantics、CONFIG_SCHEMA 四类契约文档订正到 V17；wbpp_2_9_1 定为 canonical；统一 observed rejection 命名；新增 api_doc_consistency.py 机器门（PASS）。
- 落点：docs/contracts、docs/development、docs/validation 各 1 文件；reports/ 8 份（api_inventory、final_status、full_e2e、performance、rejection_normalization 等）；tools/api_doc_consistency.py（新增）。
- 内容性质：文档或规格 + CI 或机器门。
- 意图证据：消息自述与变更面（14 文件中 10 个包与证据面文档 + 1 门脚本）一致。
- 关联：承接 seq 662/663 的命名与语义改动；为 seq 669 freeze 的文档前置。
- 不确定点：无重大。

### seq 665 | 2d3b31fc | 2026-08-14 | S
- 做了什么：修复 seq 661 legacy 移除后的 orchestrator 编译面：恢复 load_module 花括号、从 stage_name_v2/run_stage2 去掉 GRADIENT_SPHERE/STACK 引用；同时新增 Phase1 端到端基准运行器 tools/phase1_e2e_bench.py（cold/warm platesolve hint）。
- 落点：lib/orchestrator（dll_loader.cpp、orchestrator.cpp）、tools/phase1_e2e_bench.py（新）。
- 内容性质：生产代码（修复）+ 工具（bench runner）。
- 意图证据：消息自述"orchestrator rebuild after legacy removal"与变更面吻合；bench runner 属夹带新增，自述已列。
- 关联：直接后续于 seq 661（同一修复因果）；其 bench 工具被 seq 686（44a23e52）增量增强。
- 不确定点：无重大。

### seq 666 | a46e7028 | 2026-08-14 | M
- 做了什么：V17 报告与自评批次：Phase1 源码审计（单调用图、无隐藏路径结论）、Phase1/Phase2 性能报告更新、V17 语义路径清单（semantic_path_inventory.csv）、self-review round0-5 更新。
- 落点：reports/phase1_source_audit.md（新）、reports/phase1_performance.md、reports/phase2_performance.md、reports/semantic_path_inventory.csv、self_review/round0..round3 四件（共 11 文件，0 代码面）。
- 内容性质：控制包或台账（自评/报告证据面）。
- 意图证据：消息自述与变更面完全在报告/自评目录一致。
- 关联：与 seq 667/668/669/670 构成 v17 收尾链；round 系列与 seq 682（c9d05625）的 v18r2 round0-6 同一方法学。
- 不确定点："no hidden path" 的审计结论细节在报告内，卡未摘录，未证实。

### seq 667 | 88293174 | 2026-08-14 | M
- 做了什么：V17 审核工具：新增仓库源清单生成器 tools/gen_repo_source_manifest.py（消息自述对 4197 文件出 SHA256）与审核包组装器 tools/assemble_v17_review_pkg.py；更新 round6 清洁树验证草稿。
- 落点：tools/ 两新脚本、self_review/round6_final_verification.md。
- 内容性质：CI 或机器门（工具）+ 控制包或台账。
- 意图证据：消息自述与两新工具脚本吻合；"4197 files"为自述数字（卡无独立佐证，未证实）。
- 关联：assemble 工具由 seq 670（3547b108）继续增强。
- 不确定点：manifest 是否入库不可从卡判定（变更面无对应 csv）。

### seq 668 | d00b9f42 | 2026-08-14 | M
- 做了什么：V17 六份必交交付报告全部新建：integration_contract、wbpp_final、large_scale（rejection）、controlled_rejection_truth、legacy_removal、docs_consistency。
- 落点：reports/ 六文件全新增（+298/-0）。
- 内容性质：控制包或台账（交付报告）。
- 意图证据：消息自述与六个新报告文件名一一对应（6/6）。
- 关联：报告主题分别回收 seq 660（集成契约）、661（legacy 移除）、662（受控剔除真值）、663（large_scale）、664（文档一致性）。
- 不确定点：无重大。

### seq 669 | c9b8dfc3 | 2026-08-14 | M
- 做了什么：V17 冻结收口（"v17-final: freeze PASS"）：清洁树验证（自述 74/74 清洁构建、真实 16 帧 warm E2E、3 次性能采样）；lib/phase2/CMakeLists.txt 增加 CUDA bridge DLL 在清洁构建下的复制步骤；SCIENCE_FREEZE、final_status、round4、round6 文档定稿；api_doc_consistency.py 微调。
- 落点：docs/validation、lib/phase2/CMakeLists.txt、reports/final_status.md、reports/phase1_performance.md、self_review/round4、round6、tools/api_doc_consistency.py。
- 内容性质：生产代码（构建脚本）+ 控制包或台账（冻结文档）。
- 意图证据：消息自述与变更面（1 个构建面 + 4 份定稿 + 门微调）一致；74/74、E2E 数字为自述。
- 关联：是 seq 666-668 报告链的收口；V17 阶段的边界点。
- 不确定点：freeze 判定人/流程未见（未证实）。

### seq 670 | 3547b108 | 2026-08-14 | S
- 做了什么：审核包组装器增强：SHA256SUMS 生成、before/after 性能 JSON 打入、"honest B1 interruption note"（对中断采样如实注记）、zip 后校验。
- 落点：tools/assemble_v17_review_pkg.py（单文件 +107/-11）。
- 内容性质：CI 或机器门（打包工具）。
- 意图证据：消息自述与单文件变更一致。
- 关联：直接续 seq 667 的同一脚本。
- 不确定点：B1 事件背景仅见于消息，未证实。

### seq 671 | df5cbb5d | 2026-08-15 | L
- 做了什么：V18 起始性能工程：为 Drizzle/HiPS 加粗粒度全墙钟计时（orchestrator 外相位、drizzle 内部、sink、hips writer 分段 clock）与门控细粒度逐像素 profiler（环境变量 ASTROCS_DRIZZLE_FINE_PROFILE，默认关）；引入 DrizzleRunContext 常量结构；adaptive-edge acos→dot 阈值；healpix_core 与 snr_evaluator 从已归档 healpix_stack 提升为 drizzle 模块私有实现（修复 V17 归档后 build 残留引用），同名第三方 nanoflann.hpp（3652 行）随迁入模块。
- 落点：lib/healpix_db/healpix_drizzle 12 文件（+5548/-42，其中 nanoflann.hpp +3652、healpix_core +694、snr_evaluator +415 为搬家/vendored 大头）、lib/orchestrator 2、lib/astro_image_io/src/hips/aio_hips_writer.cpp 1。分级 L，按文件集合归纳。
- 内容性质：生产代码（profiling 插桩 + 热路径微优化 + 模块内化搬家）。
- 意图证据：消息自述详尽；变更面证据强一致（新增 5 文件中 3 个是提升的搬家文件、Makefile 删 ../healpix_stack 引用改模块内 DEPS）。
- 关联：承接 seq 669 冻结后的 V18 周期；其插桩为 seq 672/676 的优化提供测量基座；候选 oracle 9003 为自述门。
- 不确定点：nanoflann 版本与上游出处卡内未记录（未证实）。

### seq 672 | ce071948 | 2026-08-15 | S
- 做了什么：V18 热路径四项修复/优化，编号 PERF-002/003/007/010（另有 009）：SNR 模型 RAII 化修泄漏（PERF-010）、候选/drop 几何 thread-local scratch 复用（PERF-002）、边界四角固定数组（PERF-003）、HiPS dtype scratch + NESTED hierarchy 缓存（PERF-007/009），并修 clip_normals 复用未清空 bug。
- 落点：lib/healpix_db 4 文件（aio_hips_writer 在 astro_image_io 计 1）、lib/astro_image_io 1。
- 内容性质：生产代码。
- 意图证据：消息自述带 PERF 编号（与紧凑卡编号字段一致）；变更面为纯 C++ 源文件修改；"FP32-level science equivalence confirmed" 为自述。
- 关联：同编号族 perf(phase1-v18) 连续于 seq 671；其效果由 seq 673 记录。
- 不确定点：PERF-001/004/005 的实现在哪条提交未在卡中显式（seq 671 消息含 DrizzleRunContext 对应 005，未完全证实）。

### seq 673 | 137f20d5 | 2026-08-15 | S
- 做了什么：Drizzle 优化记录报告：PERF-001..005/010 已实现清单、oracle 等价性证据（candidate 9003、edge/overlap 于 -O2 PASS、-O3 环境漂移如实记录）、同编译器 Drizzle -9.2%。
- 落点：reports/drizzle_optimization.md（新增单文件 +47/-0）。
- 内容性质：控制包或台账（性能报告）。
- 意图证据：消息自述与单报告新增一致。
- 关联：为 seq 671/672 优化的书面收口；"-O3 env-drift documented" 体现如实记录纪律。
- 不确定点：无重大。

### seq 674 | f5e24335 | 2026-08-15 | S
- 做了什么：Gaia cone-search 极面剪枝修复大内存/慢查询：AzimuthalEquidistant 投影 RA-wrap bbox 退化导致每次查询 16.3GB/13s → 35MB/0.03s，节点 164k→1.2k，星集 899/899 逐位一致（以 stashed before/after DLL 对比自证）。
- 落点：lib/gaia_xpsd_client/src/gaia_client.c（+94/-3）。
- 内容性质：生产代码。
- 意图证据：消息自述含前后量化数字；变更面单文件吻合。899/899 一致为自述。
- 关联：与 seq 675 同函数族连续提交（perf(phase1-v18r2) gaia 系列 1/2）；其保守性缺陷由 seq 684 修正（"可证明保守"）。
- 不确定点：无重大。

### seq 675 | 4147f9ce | 2026-08-15 | S
- 做了什么：Gaia spectrum/photometry 查询套用同一极面剪枝：PHOTOMETRIC 17.8s/36GB → <1s，整帧墙钟 131.7s→65.7s，RSS 峰值 37.5GB→1.2GB，进程退出延迟 40s→0.7s。
- 落点：lib/gaia_xpsd_client/src/gaia_client.c（+9/-1）。
- 内容性质：生产代码。
- 意图证据：消息自述量化对比；单文件小改与"same pruning"叙述一致。
- 关联：seq 674 的直接延续；数字与 seq 677 报告互印。
- 不确定点：无重大。

### seq 676 | 77fc48eb | 2026-08-15 | S
- 做了什么：Drizzle 行级 Vec3 顶点缓存（共享角点，免逐像素 sin/cos）+ 安全裕量 dot 预过滤 quick-reject（lim+1e-9 rad，声明为原拒绝集严格子集）；候选 oracle 9003、overlap 76/76、edge PASS 自述。
- 落点：lib/healpix_db/healpix_drizzle 3 文件（drizzle_engine.h/.cpp、spherical_overlap.cpp）。
- 内容性质：生产代码。
- 意图证据：消息自述"strict subset"语义断言 + 三组门；变更面吻合。
- 关联：perf(phase1-v18r2) drizzle 线，前接 674/675（gaia 线）后接 677 收口报告。
- 不确定点：1e-9 rad 裕量的证明细节未证实。

### seq 677 | c8c40855 | 2026-08-15 | S
- 做了什么：V18R2 性能收口三份报告：资源时间线前后对比（RSS 37.5GB→1.2GB、exit 40s→0.7s）、热点复杂度 CSV、重复劳动审计；最终全 16 帧中位 67.35s（相对冻结基线 126.65s -47%），全部 rc=0。
- 落点：reports/resource_timeline.md、reports/hotpath_complexity.csv、reports/repeated_work.md（三新）。
- 内容性质：控制包或台账。
- 意图证据：消息自述与三份新报告一致；数字为报告自述。
- 关联：收口 seq 671-676 的 v18/v18r2 性能线。
- 不确定点：无重大。

### seq 678 | 578b4814 | 2026-08-15 | S
- 做了什么：SHA-256 统一重构（phase1 侧，编号 SHA-256/SHA 族）：删除 orchestrator 内 sha256_impl 重复实现，改用 lib/common/crypto；config SHA 前后同为 603879c6912b 验证。
- 落点：lib/orchestrator 3 文件（Makefile、cli_command.cpp、json_config.cpp，+9/-69）。
- 内容性质：生产代码（去重重构）。
- 意图证据：消息自述 + 净删除 60 行的变更面形态一致；哈希一致为自述。
- 关联：与 seq 679 为同一任务（SHA 族）连续两提交（phase1 侧与 ACR 侧）。
- 不确定点：无重大。

### seq 679 | 53ff1143 | 2026-08-15 | S
- 做了什么：SHA-256 统一（ACR 侧）：删除 profile_reader/profile_generator 中两份重复 SHA256 struct，收敛到 lib/common/crypto 单实现（自述"compiles"）。
- 落点：lib/acr 4 文件（两 CMakeLists + profile_reader.cpp + profile_generator.cpp，+11/-176）。
- 内容性质：生产代码（去重重构）。
- 意图证据：消息自述与 -176 行净删吻合。
- 关联：seq 678 的同任务续集；两者合计构成 CODE-001 去重主题的前半（后半见 seq 680）。
- 不确定点：仅"compiles"未过完整门的表述值得注意——门禁补全是否在别处，未证实。

### seq 680 | 25a6479b | 2026-08-15 | L
- 做了什么：删除重复模块 lib/data_pipeline（编号 CODE-001）：canonical 定为 astro_image_io PipelineFrame（消息自述其为超集 299 vs 258 行、有导出 API，且无构建系统引用 data_pipeline）。
- 落点：lib/data_pipeline 8 文件整体删除（+0/-2627，含 README、api_spec、memory.md、aio_pipeline*）。分级 L，纯删除，直方图即全貌。
- 内容性质：结构整理（死码清除）。
- 意图证据：消息自述 + 变更面 100% 为删除，形态完全一致；"superset/no build ref"两个前置断言为自述。
- 关联：与 seq 678/679 同属 v18r2 重复消除线；seq 683 的 duplicate_cleanup.md 报告收口此线。
- 不确定点：删除后是否有引用残漏未由卡验证（构建门自述在 682/683）。

### seq 681 | f4f5138f | 2026-08-15 | M
- 做了什么：OpenMP 全局副作用消除：omp_set_num_threads 改为 parallel 子句 num_threads（drizzle_engine.cpp、orchestrator.cpp 两文件小改）；orchestrator logger 默认路径清理；HANDOVER.md 重写为当前 V18R2 状态（-509/+71 大幅瘦身）。
- 落点：lib/healpix_db 1、lib/orchestrator 1、HANDOVER.md 1。
- 内容性质：生产代码 + 文档或规格。
- 意图证据：消息自述与三面一致；线程改法有宪章 §10.4（统一线程预算）背景但卡内不引用条款，动机按消息记。
- 关联：与 seq 680 同为 v18r2 重构收束；文档重写与 seq 683 报告同期。
- 不确定点：logger 路径清理细节卡未展开。

### seq 682 | c9d05625 | 2026-08-15 | M
- 做了什么：v18r2 自审：Round0-6 资源驱动审计重写，消息自述 15 条红队假设关闭并附 oracle/科学/性能证据。
- 落点：self_review/round0..round6 八件（+175/-407，净删旧账）。
- 内容性质：控制包或台账。
- 意图证据：变更面全部位于 self_review/，与自述一致。
- 关联：方法学沿袭 seq 666 的 v17 round 系列；为 v18r2 收口（seq 683）的前置。
- 不确定点：15 条假设清单在文件内，未证实。

### seq 683 | f12d39a1 | 2026-08-15 | M
- 做了什么：v18r2 收口报告七件：final_status、bottleneck_classification、hips_io_optimization、phase1_final_performance、phase2_regression、duplicate_cleanup、code_quality 更新/新建。
- 落点：reports/ 7 文件（5 新 2 改，+49/-92）。
- 内容性质：控制包或台账。
- 意图证据：消息主题词与七个报告名一一对应。
- 关联：收口 seq 671-682；duplicate_cleanup.md 对应 seq 678/679/680 去重三件套。
- 不确定点：无重大。

### seq 684 | 348ca1e8 | 2026-08-15 | M
- 做了什么：Gaia 客户端正确性/健壮性大修（v18r3）：polar_plane_intersects 用 AE 投影双-Lipschitz 常数（C45=pi/(2sqrt2)、global=pi/2）建立**可证明保守**判据，修复 center-outside-cap 直拒导致 cone 跨 ±45 漏选（含 RA wrap/极点/多半径覆盖，photometry 查询补极区剪枝）；查询缓存键从量化舍入改为精确语义（ra/dec/radius/mag_low/mag_high + db_type/file_count + GAIA_CACHE_VERSION 2）；缓存替换事务式；全部公开出口 checked allocation + 稳定错误码；trace 从 process-global 计数器改 per-query 栈上下文（GaiaTraceCtx，OpenMP atomic 仅更新本查询）；新增 GAIA_ALLOC_TEST / GAIA_POLAR_PRUNE_DISABLED 测试钩子；Makefile 补 -Wextra -Wpedantic。
- 落点：lib/gaia_xpsd_client/src/gaia_client.c（+384/-149）+ Makefile。
- 内容性质：生产代码。
- 意图证据：消息正文极详尽（684 字符）且全卡 hunk 摘录与自述逐条对上（GAIA_CACHE_VERSION 1→2、GaiaTraceCtx 结构、round_to 删除等）。
- 关联：修正 seq 674/675 剪枝的保守性缺口；其闭包由 seq 687 记录；同提交内警告固化衔接 seq 685。
- 不确定点：无重大；"cache ON/OFF 结果等价"为自述门。

### seq 685 | 871092f8 | 2026-08-15 | M
- 做了什么：v18r3 全仓一方件零警告（-Wall -Wextra -Wpedantic）首批：AIO 删死变量/计数、%p 显式转换、HISS_DLOG 改变参宏；drizzle 删重复 thread_local 计数与辅助、strncpy→snprintf、WCS_LOG/POLY_LOG 加 -Wno-c++20-extensions；plate_solve GetProcAddress FARPROC 两步转换消 cast-function-type；star_detector/snr/photometric/calibration/dynamic_psf 未用变量清理与 narrowing 显式转换；orchestrator 同款 GetProcAddress 修复并删死函数指针（含 master path 推导）；phase2 删 leaf_shift/sigma_floor/support_power/mask 未用项；两 Makefile 固化警告旗标。
- 落点：lib/astro_image_io 6、healpix_db 5、plate_solve 5、photometric_calib 2、star_detector 2、calibration/dynamic_psf/orchestrator 各 1（25 文件，+67/-178）。M 级但仍属多目录清理，按目录直方图归纳。
- 内容性质：生产代码（警告卫生）。
- 意图证据：消息正文逐模块列举，与目录直方图对应。
- 关联：为 seq 692（v19-round2 全仓版）的前次尝试；与 seq 684 同属 v18r3。
- 不确定点：685 未覆盖全仓（692 再次处理），两轮间残余项差异未证实。

### seq 686 | 44a23e52 | 2026-08-15 | S
- 做了什么：phase1 基准工具增量持久化：每帧完成即写 log/partial.json，超时/中断不丢已完成帧。正文自述这是 V18R2 基准期间使用的未记录修改，应审核要求补提交以恢复可追溯性。
- 落点：tools/phase1_e2e_bench.py（+18/-1）。
- 内容性质：CI 或机器门（基准工具）+ 可追溯性补记。
- 意图证据：单文件小改与自述一致；"补交未记录修改"是少见的自述流程事件，无独立佐证（未证实，但与 seq 670 的 honest B1 note 同风格）。
- 关联：增强 seq 665 引入的同一脚本。
- 不确定点：原始使用时的提交为何缺失，无证据。

### seq 687 | 87173cf7 | 2026-08-15 | S
- 做了什么：Gaia 模块 memory 更新：V18R3 极面剪枝/缓存/内存安全/trace 四项闭包记录。
- 落点：lib/gaia_xpsd_client/memory.md（+21）。
- 内容性质：文档或规格（模块记忆）。
- 意图证据：单文件与自述一致。
- 关联：seq 684 的书面收口。
- 不确定点：无重大。

### seq 688 | f6727773 | 2026-08-15 | M
- 做了什么：V19 科学重构起点（SNR-001 族）：在 lib/snr_estimator 落地三层噪声模型——PhotometricCalibrationQuality（dex/mag/rel 单位正确的定标质量，fit_status 0=ok/1=degenerate/2=invalid input，禁作逐像素 ivar）、PsfFitQuality（q_psf=A/residual_scale，弃用误称 MAD、给出 0.731673 换算 robust_residual_sigma）、NoiseWeightModelV1（source-masked blank-sky 稳健方差→ivar，σ=1.4826022185×MAD 型稳健尺度、patch 网格 + IDW 平滑、gain/read-noise 交叉验证与经验 fallback）；附模块级科学测试 32/32（SNR-001..010/013/014 矩阵）；旧 snr_extract_model_* 降级为诊断/迁移专用。
- 落点：lib/snr_estimator/cpp：snr_estimator.h +150、src/noise_model.cpp 新 +422、test/noise_model_science_test.cpp 新 +453、Makefile。
- 内容性质：生产代码 + 测试。
- 意图证据：消息正文列五要点；卡内头文件注释自引 SNR_SCIENCE_DERIVATION.md / SNR_REDESIGN_CONTRACT.md 为规格源（变更面证据强）。
- 关联：seq 689（drizzle 侧传播）与 seq 690（phase2 侧消费）构成 V19 三联链首环。
- 不确定点：32/32 矩阵通过为自述。

### seq 689 | 05cccb4e | 2026-08-15 | M
- 做了什么：Drizzle 方差传播与计数（DRZ-014/016、P1-003、SNR-011/012）：累加 sumVarNum += v_j·w_jp²、variance_p = sumVarNum/sumArea²；操作计数器族（source/candidates/overlaps/quick-rejects/pix2radec/boundary/geometry/sh/tile-lookups/heap-alloc）进 DrizzleStats 并导出 operation_counts.json；AIO HiPS 新增 variance/ivar 子产品（AIO_HIPS_PRODUCT_VARIANCE=8/IVAR=16、ALL_V19=31，hierarchy 归约同式）；PipelineFrame variance 块经 hp_drizzle_run_hips 贯通；科学断言：MC 4000 实现 p50=1.001 p95=[0.962,1.042]、协方差 mean|rho|=0.186 max=0.571、DRZ-016 信号对方差输入不变。
- 落点：lib/healpix_db 6 文件（含新 variance_propagation_test.cpp +305）、lib/astro_image_io 2（aio_hips.h、aio_hips_writer.cpp）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息五条与两目录变更面（写侧 sink/engine + 产品侧 writer/reader 头）完全对应；错误码语义（-1/-2/-3/-4）见卡内摘录佐证。
- 关联：上承 seq 688（模型提供方），下启 seq 690（消费方）；消息点名 operation_counts.json 属产物文件名而非在库文件（自述与变更面无交集的机器判定即因此，非矛盾）。
- 不确定点：MC 数值独立复现未证实。

### seq 690 | eb48cefe | 2026-08-15 | M
- 做了什么：Phase2 ivar 科学权重模型上线，退役 raw-star-SNR（SNR-015）：UPM 权重 w = quality × support^p × ivar（obs.ivar>0 否则 1/unc²），legacy snr²/(1+snr²) 仅 use_ivar_weight=0 消融路径；stage2 weight_mode 默认 ivar(2)，support 仅做有效性防双重计数，产品缺失→support fallback；ACR kernel mode2 = support×ivar、mode0 legacy；sampler 在 control 叶读 frame ivar 积入 obs.ivar；AIO 读取侧新增 AIO_HIPS_RD_VARIANCE=3/IVAR=4；orchestrator SNR stage 接 NoiseWeightModelV1 写 variance/ivar 块 + KV 元数据（NOISE_MODEL_STATUS 字面量 SKIPPED_NO_DATA/SKIPPED_API_MISSING），旧 snr_model 标 DIAGNOSTIC_ONLY；phase2 合成门 74/74 自述。
- 落点：lib/phase2 7 文件、lib/astro_image_io 2、lib/orchestrator/cpp/src/orchestrator.cpp +173/-1。
- 内容性质：生产代码。
- 意图证据：消息七要点与三目录变更面逐条对上（含状态字面量与枚举值，卡内摘录佐证）。
- 关联：V19 三联链第三环（688→689→690）；其权重无偏性由 seq 693 的 G5 门验证；后被 seq 710 升级为 control-variance 权重。
- 不确定点："support fallback" 与 seq 710 的"缺 ivar 硬错误"之间的策略收紧发生在 v19r3，本条当时口径以消息为准。

### seq 691 | 1d3a34c2 | 2026-08-15 | M
- 做了什么：V19 预发布文档 + 诊断工具：新增 tools/astrocs_diagnose.py（symptom→stage→evidence→cause→fix 报告，ERROR_TAXONOMY 模式、有界正则、400 文件上限、JSON 输出）；新建/更新 18 份文档（ERROR_TAXONOMY、DIAGNOSTICS、TROUBLESHOOTING、SNR_NOISE_MODEL、DRIZZLE、PHASE2、ARCHITECTURE、MODULE_INDEX、DATA_CONTRACTS、CONFIG_REFERENCE、DEVELOPER_GUIDE、SCIENTIFIC_ALGORITHMS、TESTING_AND_ACCEPTANCE、PERFORMANCE、BUILD_RELEASE、KNOWN_LIMITATIONS + CHANGELOG + README）。
- 落点：docs/ 16 新 + README/CHANGELOG 2 改 + tools/astrocs_diagnose.py（19 文件 +1001/-4）。
- 内容性质：文档或规格 + 工具。
- 意图证据：消息列举与文件集合对应。
- 关联：V19 科学链（688-690）的用户面包装；seq 695 的机器一致性检查对象即这批文档。
- 不确定点：无重大。

### seq 692 | 5c2a98ca | 2026-08-15 | M
- 做了什么：v19-round2 全仓一方件零警告：AIO 以 aio_safe_copy 统一替代 strncpy、删死码 dense_checksum_of，vendored CFITSIO 以 -w 编译并登记 third_party 例外（144 条上游警告如实计数）；plate_solve 删死 polygon_match_single_stage + sip_basis；star_detector 删 LM/trimmed-mad 死 helper（sdet_gauss_solve、sdet_lm_solve、sdet_compute_trimmed_mad 等）；orchestrator 删 write_hips_from_hiss + format_exposure_2f；全仓 -B 重建 0 警告 0 错误（自述）。
- 落点：lib/astro_image_io 6、plate_solve 2、orchestrator 1、star_detector 1（10 文件 +69/-609，净删 540）。
- 内容性质：生产代码（死码清除 + 卫生）。
- 意图证据：消息逐模块列举与变更面对应；净删为主符合"死码清除"形态。
- 关联：与 seq 685 同一警告卫生主题的 v19 全仓版（685 遗留残余在此收掉）；为 seq 704（v19r2-s4）的 CMake 级固化铺路。
- 不确定点：与 685 的残余映射未逐条证实。

### seq 693 | 04ffaa0f | 2026-08-15 | S
- 做了什么：G5WeightTruthGate 测试：UPM 校准 ivar 无偏断言（bias=-0.0008、var=0.0359 vs 最优点 0.036，SNR-015），gate 中记录 V19 默认 weight_mode=ivar，保留 legacy support_x_snr2 消融行。
- 落点：lib/phase2/tests/synthetic_gate.cpp（+27/-2）。
- 内容性质：测试。
- 意图证据：消息数值断言 + 单测试文件变更一致。
- 关联：为 seq 690 的 ivar 权重提供真值门。
- 不确定点：无重大。

### seq 694 | 729f150c | 2026-08-15 | M
- 做了什么：v19-round5 红队收口（17/17 自述）：修 HiPS sink 方差 tile 全零/无数据（rc=-5/-2）时跳 tile 而非中止整产品（H5 稳健性发现）；新增 tools/redteam_v19.py（17 假设覆盖 SNR/ivar/ACR/docs/legacy-consumer）与 tools/gen_v19_evidence.py（shipping/warnings/deps/API/findings 证据生成）。
- 落点：lib/healpix_db/healpix_drizzle/astro_sphere_sink.cpp（+24/-5）、tools/ 两新脚本。
- 内容性质：生产代码（缺陷修复）+ CI 或机器门（红队/证据工具）。
- 意图证据：三要点与三文件一一对应。
- 关联：round 系列沿 v17/v18r2 方法学；其证据工具输出供 seq 695/696 收口引用。
- 不确定点：17 假设明细在脚本内，未证实。

### seq 695 | c0753e33 | 2026-08-15 | S
- 做了什么：v19-round6 文档机器一致性 PASS：新增 tools/docs_machine_consistency.py（6 检查：config/error/stage/SNR/product/drizzle）；CONFIG_REFERENCE 定 canonical wbpp_2_9_1（删别名）；DRIZZLE.md 方差公式记号与引擎对齐（variance_p/ivar_p）。
- 落点：tools/docs_machine_consistency.py（新 +104）、docs/CONFIG_REFERENCE.md、docs/DRIZZLE.md。
- 内容性质：CI 或机器门 + 文档或规格。
- 意图证据：消息与三文件对应。
- 关联：检查器被 seq 706 复用到新 L0-L5 路径结构；收口 seq 691 的文档面。
- 不确定点：无重大。

### seq 696 | dab83fa5 | 2026-08-15 | S
- 做了什么：V19 预发布闭包记录：五模块 memory.md（astro_image_io/healpix_db/orchestrator/phase2/snr_estimator）+ 根 memory.md 各追加进度条目。
- 落点：6 个 memory.md（+61/-0）。
- 内容性质：文档或规格（记忆台账）。
- 意图证据：变更面与自述逐文件一致。
- 关联：V19 主链（688-695）的书面终点标记；与 seq 659/654（上片 v16/v15 memory 条目）同一惯例。
- 不确定点：无重大。

### seq 697 | 9c415bc1 | 2026-08-15 | M
- 做了什么：UPM 帧绑定与持久化强化：upm.cpp 重建（+200/-131）——绑定只由稳定 frame_id 决定、与容器遍历/排序/插入顺序无关（canonical parameter_rows[index] 与 frame_id_by_index[index] 同长无重复，frame_index_by_id 由列表重建，禁有序 map 遍历推断参数序）；synthetic_gate.cpp 新增 +548 覆盖 TEST-PR-UPM-001..006（乱序 ID 20/10/15、N=4 全排列 0 漂移、100 确定性种子、稀疏/稠密持久化、save/open 链 M0==M1==M2==M3）；首建 docs/TRACEABILITY.csv 并登记 SCI-UPM-PERSIST-001 / ALG-UPM-FRAME-BIND-001 / DATA-UPM-MODEL-001 行（BIND-001/MODEL-001 编号族源头）；phase2 gate 82/82 自述；向后兼容采用 safe migration (A)。
- 落点：lib/phase2/src/upm.cpp、lib/phase2/tests/synthetic_gate.cpp、docs/TRACEABILITY.csv（新）。
- 内容性质：生产代码 + 测试 + 控制包或台账（traceability 行）。
- 意图证据：卡内 TRACEABILITY 行摘录与消息编号完全互证（变更面证据强）。
- 关联：TRACEABILITY.csv 由 seq 705（v19r2-s5）扩到 30 行；此条是 v19r2 计划的事实前奏。
- 不确定点："safe migration (A)" 的 A 方案定义文档未定位于卡内（未证实）。

### seq 698 | 729bc3b9 | 2026-08-15 | L
- 做了什么：v19r2-s0 全仓盘点：source_manifest.csv（自述 2062 tracked / 713 一方件活跃、逐文件 sha256）、file_audit_inventory.csv（25 字段 S8 schema，PENDING 播种）、shipping_units.csv（280 单元 = 243 V19 基线 + 37 新生产文件：browser core、ACR tools、drizzle、orchestrator、phase2 tools）；排除 build/vendor（acr build2/_deps、cfitsio third_party）与数据目录（BASS DR3/testdata）；reconciliation 0 deleted；配套新增 tools/quality/check_source_inventory.py。
- 落点：reports/v19r2/ 三 csv + tools/quality/ 一脚本（4 文件 +3276/-0）。分级 L，按文件集合归纳。
- 内容性质：控制包或台账 + CI 或机器门（盘点校验器）。
- 意图证据：消息五条与四文件对应。
- 关联：v19r2 计划（s0→s10）的 s0；基线数字被 seq 715 的最终盘点引用（final_source_manifest.csv）。
- 不确定点：无重大。

### seq 699 | 5c7032db | 2026-08-15 | S
- 做了什么：v19r2-s1 契约冻结：contract_inventory.csv 登记 44 个权威科学/算法 ID（SNR-001..015、DRZ-001/014-016、UPM-001..010、RJ-001..008、INT/CAL/AST/PHOT/PSF、UPM-PERSIST/FRAME-BIND/MODEL），缺口（API-/DATA-HIPS-/ENG-/ERR-/METRIC- 族）显式留给 S2。
- 落点：reports/v19r2/contract_inventory.csv + s1_contract_freeze.md（两新）。
- 内容性质：文档或规格（契约台账）。
- 意图证据：消息列 ID 族与 s1 文件名一致；紧凑卡编号字段给出 DRZ-001/RJ-001/SNR-001/UPM-001 样例。
- 关联：其"缺口留 S2"由 seq 700 兑现（44→64 行）。
- 不确定点：44 这个数字未独立清点（卡内不可判）。

### seq 700 | 8ab2efbd | 2026-08-15 | L
- 做了什么：v19r2-s2 权威文档层级 L0-L5 建立 + 契约扩到 64 行：L1 docs/science/（11，定义/方程/单位/域/ID）、L2 docs/algorithms/（12，I/O/前置条件/不变式/oracle）、L3 docs/architecture/（11）+ docs/diagnostics/、L4 docs/standards/（13）、L5 docs/modules/（13）；L0 更新 README/RELEASE_STATUS/KNOWN_LIMITATIONS/DEVELOPER_GUIDE；V19 扁平文档 13 文件改名迁移到 docs/history/v19/（新增 history README 2 件）；contract_inventory.csv 补 API-AIO/DATA-HIPS/ENG/ERR/METRIC 族；附 tools/gen_v19_source_snapshot.py。
- 落点：docs/ 六目录 83 文件（新增 66、改名 13、修改 4）。分级 L，按目录直方图归纳，不逐文件展开。
- 内容性质：文档或规格 + 结构整理（历史迁移）。
- 意图证据：消息逐层计数（11/12/11/13/13）与新文件列表目录分布吻合（新增文件清单含 algorithms 12、architecture 11、standards 13、modules 13 等）。
- 关联：s1 缺口（seq 699）在此闭合；seq 706 将机器一致性检查迁移到新路径。
- 不确定点：64 行契约的完整清单未展开验证（未证实）。

### seq 701 | c8a0a7ba | 2026-08-15 | L
- 做了什么：v19r2-s3 注释卫生：把轮次/审计史标（Vxx/Rxx/MICROFIX/控制包/审计 markers）从生产注释迁出；新增 tools/quality/check_comment_hygiene.py 与 strip_version_comments.py；消息自述触及 354 个一方件 C/C++ 文件、434 扫描文件中违例 1240→0、语义/why/invariant 注释保留、全量 -Wall/-Wextra/-Wpedantic 构建 PASS 且 phase2 82/82 + SNR 32/32 + drizzle/variance/oracle 套系 PASS。
- 落点：lib/acr 135、lib/healpix_db 65、lib/astro_image_io 42、lib/plate_solve 30、lib/orchestrator 20、lib/phase2 19、photometric_calib 11、calibration 9 等（353 文件，+6136/-5916，逐行对称改写形态）。分级 L，直方图归纳。
- 内容性质：结构整理（注释级）+ CI 或机器门（hygiene 检查器）。
- 意图证据：变更面 +N/-N 近乎逐文件对称（典型注释替换形态），与"仅注释改动"自述形态一致。
- 关联：seq 707（s3 updater）属同步骤配套；seq 713 是 v19r3 的第二轮卫生。
- 不确定点：文件计数 354（自述）vs 卡 353 文件（含 2 新脚本，代码面 353-2+…口径差 1，未证实成因，可能计数口径不同）。

### seq 702 | 4265b603 | 2026-08-15 | S
- 做了什么：v19r2-s6 测试修正 F-V19R2-DRZ-001：SIP 细分面积断言改按科学容差——tiny pixel（~3.2e-8 sr）处两种采样密度面积差落在 float64 ~1e-17（rel ~3e-10），严格单调（err8<err1）在该尺度不可判（实测 err8=1.25e-17 > err1=8.08e-18），改为两采样密度共同满足绝对 1e-9 sr 容差；77/77 PASS 自述。
- 落点：lib/healpix_db/healpix_drizzle/test_spherical_overlap.cpp（+74/-69）。
- 内容性质：测试。
- 意图证据：消息含实测数字与判据改法，单测试文件变更一致。
- 关联：与 seq 703 同步骤 s6 连续；对应编号族 DRZ。
- 不确定点：无重大。

### seq 703 | e82ca0f2 | 2026-08-15 | M
- 做了什么：v19r2-s6 两修（编号 IO-001、UPM-002）：F-V19R2-UPM-002（P1）——p2_upm_calibrate_block 对未知 frame_id 原本返回 rc=0 并套用 frame 0 参数，改为硬失败 rc=1 输出不动，p2_upm_evaluate_c 未知帧返 NaN（稠密路径本就拒绝），新增 UpmUnknownFrameRejected 测试；F-V19R2-IO-001（P2）——aio_upm_write_sparse 改临时文件 + 原子 rename，防部分写伪装有效模型；phase2 gate 83/83 自述。
- 落点：lib/phase2/src/upm.cpp、tests/synthetic_gate.cpp、lib/astro_image_io/src/aio_upm.cpp。
- 内容性质：生产代码 + 测试。
- 意图证据：两缺陷编号、函数名、状态字面量（rc=0/1、NaN）均见消息且与三文件对应。
- 关联：seq 697 帧绑定工作的缺陷回收；与 seq 702 同步骤。
- 不确定点：P1/P2 优先级定级来源未证实。

### seq 704 | 7538cbf0 | 2026-08-15 | S
- 做了什么：v19r2-s4 一方件零警告收尾（编号 BLD-001、COV-001、CUDA-001、REJ-001）：phase2 CMake 启用高警告旗标（F-V19R2-BLD-001）；rejection.cpp 删死 ls_fit_line（REJ-001）；coverage.cpp strncpy→snprintf 保证 NUL（COV-001）；cuda_bridge_loader.cpp 对 Win32 FARPROC 转换加文档化 pragma（CUDA-001）；synthetic_gate.cpp 使用悬空计数器并 brace dangling-else；gate 83/83、warnings 0 自述。
- 落点：lib/phase2 4 文件 + lib/acr/cuda_bridge_loader.cpp。
- 内容性质：生产代码 + CI 或机器门（构建旗标）。
- 意图证据：五要点四编号与 5 文件对应。
- 关联：延续 seq 685/692 警告线到构建系统级固化；s4 编号排在 s6 之后提交，属计划外重排（见声明段）。
- 不确定点：无重大。

### seq 705 | 1727a0cd | 2026-08-15 | S
- 做了什么：v19r2-s5 科学/算法追溯：docs/TRACEABILITY.csv 扩 30 行（noise/drizzle/UPM/rejection/integration/cal/ast/phot/psf + DATA-HIPS/ENG 族）；reports/v19r2/science_code_mapping.csv 登记 12 科学模块→生产符号；tools/quality/check_traceability.py 双向校验（code→contract→test 与 contract→code→test，broken=0，抽样符号 13/13）。
- 落点：docs/TRACEABILITY.csv（+17）、reports/v19r2/ 两件、tools/quality/check_traceability.py。
- 内容性质：文档或规格（追溯台账）+ CI 或机器门。
- 意图证据：消息与四文件对应；seq 697 首创的 TRACEABILITY.csv 在此成体系。
- 关联：上承 seq 697 的行播种；为宪章"科学=算法=接口=代码=测试"同链的可验证化（此句为背景，非卡内证据）。
- 不确定点：无重大。

### seq 706 | feb12ee0 | 2026-08-15 | L
- 做了什么：v19r2-s7..s9 汇总提交：docs_machine_consistency 迁移到新 L0-L5 路径并 6/6 PASS；DATA_SEMANTICS 补 variance/ivar 产品文档（F-V19R2-DOCS-001）；ERROR_MODEL 落 AstroCsExitCode 表；reports/v19r2 下 docs_quality/diagnostics_review/duplicate_cleanup/traceability_summary/science_regression/full_repo_audit/final_status 等收口；self_review round0-6 全套更新（findings.csv、sanitizer_coverage.json、static_analysis.json、warnings.json 等）。
- 落点：reports/v19r2 25 文件（+1216/-713）、docs/architecture 1、docs/contracts 1、tools/docs_machine_consistency.py。分级 L，直方图归纳。
- 内容性质：控制包或台账（审计/自评）+ 文档或规格。
- 意图证据：消息与文件集合对应（编号 DOCS-001 与 DATA_SEMANTICS 条目一致）。
- 关联：s7-s9 三步骤合并为一提交（是否有意合并未证实）；收口 seq 700/705。
- 不确定点：多步骤合并提交的边界未在证据中说明。

### seq 707 | 2871a517 | 2026-08-15 | S
- 做了什么：v19r2-s3 配套小工具：tools/quality/update_audit_status.py（审计状态更新器，179 行新）。
- 落点：tools/quality/update_audit_status.py。
- 内容性质：CI 或机器门（台账工具）。
- 意图证据：变更面即自述全部内容（无正文）。
- 关联：归 s3（seq 701 同步骤）但晚于 s7-s9 提交，属步骤重排样本；服务于 file_audit_inventory.csv 的 PENDING→状态流转（此用途由文件名与 seq 698 的 schema 推断，未证实）。
- 不确定点：工具操作的具体台账文件未在卡内点名（未证实）。

### seq 708 | 2885bea5 | 2026-08-15 | S
- 做了什么：CHANGELOG 登记 v19r2 条目："traceable foundation closure"（可追溯基座收口）。
- 落点：CHANGELOG.md（+26/-0）。
- 内容性质：文档或规格。
- 意图证据：单文件与自述一致。
- 关联：为 s0-s9 主链（698-706）的对外里程碑标记。
- 不确定点：无重大。

### seq 709 | edfdd1d1 | 2026-08-15 | S
- 做了什么：v19r2-s10 审核包构建器：tools/quality/build_v19r2_package.py（183 行新）。
- 落点：tools/quality/build_v19r2_package.py。
- 内容性质：CI 或机器门（打包工具）。
- 意图证据：变更面即自述。
- 关联：延续 seq 667/670 的审核包传统（v17→v19r2）；s10 为 v19r2 编号收尾。
- 不确定点：包是否产出入库，卡无产物文件（未证实）。

### seq 710 | 1752e8ca | 2026-08-16 | M
- 做了什么：v19r3-s1 UPM control-variance 科学权重 + integration/ACR 契约（编号 UPMW-001..007）：P2ControlObservation 增 control_variance/control_ivar，SAMPLER 计算 patch median 方差 Var = k_corr·(π/2)·σ²/N_retained（N_retained 取 clipping 后样本；k_corr 由 Drizzle pixfrac=0.8 synthetic MC 2000 实现校准 =1.3883，冻结保守值 1.4）；p2_upm_raw_weight 生产路径只含 quality×control_ivar（SCI-UPM-WEIGHT-001），缺 control ivar 显式 rc=2 禁止静默回退，legacy snr²/(1+snr²) 仅 use_ivar_weight=0 消融；stage2 显式透传 use_ivar_weight=1，ivar 产品整体缺失默认硬科学错误、legacy_allow_weight_fallback=true 才降级 support 并标红；integration 零权重合同（zero 合法不贡献、NaN/Inf/负 INVALID）并删 P2PixelStack 未用 weight_mode（policy/reducer 分离）；ACR wmode=2 与 CPU 逐像素 ivar 不等价→生产 weight_mode=ivar 强制 CPU canonical path、kernel 禁用 wmode=2；新增 UPMW-001..004/006/007 gate 与 lib/healpix_db/healpix_drizzle/tests/control_median_mc_test.cpp（UPMW-005）；phase2 gate 89/89 自述。
- 落点：lib/phase2 11 文件（upm/sampler/integrate/stage2_common/acr_kernels 及头、synthetic_gate.cpp、tools/stage2.cpp）+ control_median_mc_test.cpp 新（+207）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息正文 806 字符极详尽，卡内新增测试名（Phase2Weight.UPMW001SnrInvariance 等 6 门）与声明摘录为强变更面证据。
- 关联：对 seq 690 权重模型的收紧（fallback→硬错误）；其 k_corr 由 seq 711 验证不变、seq 712 文档化。
- 不确定点：k_corr=1.3883 与冻结值 1.4 的余量选择理由未记载（未证实）。

### seq 711 | ce8ec527 | 2026-08-16 | M
- 做了什么：v19r3-s3 drizzle 几何缓存（编号挂 UPMW-005 验证线）：TargetGeomCache 有界 LRU（默认 8192，线程私有）缓存 target 叶 center+boundary4，消除逐候选重复 pix2radec/radec_to_vec/boundary4 构建；新增 compute_overlap_area_g_ctx_cached 与 _g_ctx 科学等价（同一 impl）；quick-reject 保持在边界构建前（保序防穷举 oracle 退化 ~1000x）；run generation 语义——每次 drizzleTiled 递增代际、线程 cache 随新 run 清空防跨 run NSIDE 污染；新计数 target_boundary_builds/target_geometry_builds/geometry_cache_hit/miss 进 DrizzleStats 与 [ops] 行；验证：candidate oracle 9003/0 false_negative、freeze 42/42、UPMW-005 k_corr=1.3883 不变（缓存科学中性）、小图 3463 候选仅 288 次几何构建（hit≈91.7%）。
- 落点：lib/healpix_db/healpix_drizzle 4 文件（drizzle_engine.h/.cpp、spherical_overlap.h/.cpp）。
- 内容性质：生产代码。
- 意图证据：消息六要点与四文件对应；计数名与 seq 689 的 op counters 体系连续。
- 关联：把 seq 671-676 的 v18 热路径方法学延伸到 v19r3；为 seq 712 文档对象之一（cache 生命周期）。
- 不确定点：无重大。

### seq 712 | a3506d64 | 2026-08-16 | M
- 做了什么：v19r3-s4 权威文档同步（编号 WEIGHT-001）：ERROR_MODEL 退出码修正为 orchestrator.h 真实枚举（GENERIC_ERROR/BLOCK_MISSING/CALIBRATE_FAILED/PLATESOLVE_FAILED/DRIZZLE_FAILED/TIMEOUT/CANCELLED + 20-29 模块码），消除审计 §12 假 PASS；PHASE2_UPM/UPM_SOLVER 落 SCI-UPM-WEIGHT-001 + control_variance/control_ivar 公式与 k_corr 冻结及 UPMW 硬门；PHASE2_SAMPLER 记 SE(patch median)/N_retained/control_k_corr=1.4；INTEGRATION_ALGORITHMS 记零权重合同 + policy/reducer 分离；DRIZZLE_GEOMETRY 按审计 §14 深度 checklist 补球面推导/候选包围圆/RA 跨 0 极区/cache 生命周期/操作计数模型/参考-快路径符号映射；UNCERTAINTY_AND_COVARIANCE 与 modules 文档同步。
- 落点：docs/algorithms 4、docs/modules 2、docs/science 2、docs/architecture 1（9 文件 +225/-25）。
- 内容性质：文档或规格。
- 意图证据：消息点名文档集与目录分布吻合；"消除假 PASS"属纠错性质。
- 关联：seq 710/711 的文档化收口；引用 seq 700 建立的 L1-L5 层级。
- 不确定点：无重大。

### seq 713 | fb59df96 | 2026-08-16 | L
- 做了什么：v19r3-s10 注释卫生第二轮：新增 tools/quality/v19r3_strip_comments.py 机械剥离轮次前缀（Vxx/Rxx/MICROFIX/V18R2(CODE-00x)/F-V19R2-XXX-00N）、P0x-00x 任务号、聚焦版/控制包/审计轮次/Full Freeze/后续 Task 历史标记（仅注释文本，测试文件保留 F-V19R2-* 稳定测试 ID）；手工修正陈旧语义注释（orchestrator 骨架实现→生产实现说明、dll_loader 后续 Task、abstract_view、resource_monitor 移植说明——审计 §13 点名项）；新增 tools/quality/v19r3_audit.py（inventory + F01-F12 新鲜审计 + 字符串字面量跳过的扩展 comment scanner）；验证：全仓 toolchain build、phase2 gate 89/89、comment_hygiene_fail=0（786/786 inventory verified 自述）。
- 落点：lib/acr 45、lib/phase2 13、lib/healpix_db 12、plate_solve 9、orchestrator 8、photometric_calib 5、dynamic_psf 2、tools/quality 2（99 文件 +754/-367）；**另有 1 个误提交运行产物 lib/healpix_db/healpix_drizzle/healpix_drizzle.dll.bak_v18r2**。分级 L，直方图归纳。
- 内容性质：结构整理 + CI 或机器门（审计器）+（附带）运行产物误入库。
- 意图证据：消息要点与目录分布对应；dll.bak 不在消息中（影子树/备份产物混入）。
- 关联：第二轮卫生（第一轮 seq 701）；其误提交的 dll.bak 由下一 seq 714 清理。
- 不确定点：dll.bak 混入路径未说明（由 714 消息反推为 stray，未证实成因）。

### seq 714 | c4131430 | 2026-08-16 | S
- 做了什么：取消跟踪游离的 v18r2 DLL 备份文件（healpix_drizzle.dll.bak_v18r2 从库中删除，0 行增删）。
- 落点：lib/healpix_db/healpix_drizzle/（1 文件删除）。
- 内容性质：结构整理（登记说明：该文件本体属**运行产物/影子树副本**，不构成工作内容证据；本提交的价值恰是把这种产物从版本面移除）。
- 意图证据：消息即自述全部；变更面 0/0 删除二进制备份，一致。
- 关联：直接纠正 seq 713 的误提交（两提交构成引入-清理对）。
- 不确定点：无。

### seq 715 | 23c11c19 | 2026-08-16 | L
- 做了什么：v19r3-s7 直接静态分析 100% 覆盖 + 发现修复：clang --analyze 22.1.4 直析 163 个编译单元 + 144 headers via TU（311 shipping units 全覆盖）；CUDA .cu/host 4 个工具例外（CUDA_RUNTIME_DEP/NEEDS_NVCC，替代路径 nvcc build PASS + 人工）；修复真实发现——acr_kernels.cpp 缺 <stdexcept>（clang 严格）、hiss_writer.cpp 3 处 move 后读 size（moved-from 使用，P2）、fits_reader.cpp fread 后补 ferror/errno 检查（P3）、ahpx/dpsf_image/sdet_image dead-store 清理；剩余 19 项全 P3 逐条 triage 入报告；P0=0 P1=0；全仓 toolchain build 通过（均自述）。
- 落点：生产码 6 文件小修（lib/astro_image_io 2、dynamic_psf 1、healpix_db 1、phase2 1、star_detector 1）+ tools/quality/v19r3_static.py 新（+257）+ reports/v19r3/ 证据 8 件（static_analysis.md +229、analyzer_coverage.csv +312、shipping_units.csv +312、final_source_manifest.csv +787、static_analysis.json +2867、file_audit_inventory.csv +787 等，+5371 为数据产物大头）。分级 L：报告侧按数据产物归纳。
- 内容性质：生产代码（缺陷修复）+ CI 或机器门（分析器）+ 数据或产物（证据包）。
- 意图证据：消息要点与三类落点对应；其中 reports/v19r3 的 csv/json 属生成证据，不作逐文件展开。
- 关联：收口 v19r3 质量线（710-713），引用 seq 698 基线口径（shipping_units/final_source_manifest 为 s0 清单的终版）。
- 不确定点：100% 覆盖口径以 shipping units 计，例外 4 项见自述；无法从卡复核。
