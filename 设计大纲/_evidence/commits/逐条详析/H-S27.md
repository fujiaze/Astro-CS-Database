# 逐条详析 （证据层过程件）

> 本文件是 设计大纲/reports/history/slices/H-S27.md 的逐条正文；最终交付只保留两份大报告，逐条正文下沉到证据层（条目文本零改动，仅移动）。
> 由 _tools/11_split_slice_reports.py 从入库基线 a3a343a4 重切，日期 2026-09-15；条目组 41 个；报告树保留段（主题归纳/重叠区核对/遗留缺口）见原文件。

## 二、主责条目正文（55 条提交，归并为 41 组；组内 seq 与 sha8 全部列明）

### 组 1 | seq 1431, 1432 | 50d9759f, 5ecc60df | 2026-09-01 | M, S（DOC-003）
- 做了什么：重建"全公共 API 的 AST 语义检查"并登记其结果；同时在 Phase3 重采样/会话侧落地 max_tiles 守卫：新增声明 `p3_sampler_set_max_tiles(P3Sampler*, int max_tiles)`，改动 `p3_resample_check_mode`、`p3_sampler_open_ex`、`p3_session_run` 与 `struct TileCache` 所在文件；同步 `docs/api/CLI_PROTOCOL_V1.md` 一行。1432 为该任务的 COMMITS.csv 绑定行（"绑定 46 任务"）。
- 落点：`lib/phase3_session/`（p3_resample.cpp +7/-1、p3_resample.h +3/-0、p3_session.cpp +16/-0）；`docs/api/CLI_PROTOCOL_V1.md`（+1/-1）；`evidence/v6_1_rework/`（TASK_LEDGER.csv、tasks/DOC-003/PLAN.md +33、TASK_RESULT.json +119、logs/c01_api_docs.log、c02_abi.log、c03_ast.log、c04_semantics.log +17）；1432 仅 COMMITS.csv +1。
- 内容性质：生产代码（守卫实现）+ 文档或规格（CLI 协议行、PLAN.md）+ 控制包或台账（TASK_LEDGER/TASK_RESULT/COMMITS/logs）。
- 意图证据：消息自述给出验收结论（"API docs/ABI/AST 全 PASS; max_tiles 守卫实现; CLI doc 同步"）；变更面证据支持其中三项——代码样本含新增声明 `p3_sampler_set_max_tiles`，docs/api 有 1 行改动，evidence 有 4 个 c0x 检查日志（c04 最长 +17 行）；TASK_LEDGER 的 DOC-003 行由 NOT_STARTED 改写为 PASS 形态（卡载"台账行新增/删除"成对出现）。
- 关联：承接重叠区 seq 1429/1430（DOC-002 追溯矩阵）；DOC-003 台账行的依赖列引用 DOC-001；其代码落点与重叠区 seq 1423（P3-005 p3_output）同属 lib/phase3_session 模块族。
- 不确定点：c01/c02/c03 三个日志各仅 +1 行，其"全 PASS"的具体判定文本未证实（卡片未展开日志内容）。

### 组 2 | seq 1433, 1434 | 5eda0bc9, 47d59806 | 2026-09-01 | L, S（DOC-004）
- 做了什么：批量补齐 registry 模块文档：一次生成 22 个模块文档文件（phase1 系 8 个、phase2 系 8 个、phase3 系 6 个，命名形如 `astrocs.phase1.calibration.md`、`astrocs.phase2.upm-fit.md`），并新增生成器 `tools/quality/gen_module_readmes.py`（+148），以模块 descriptor 为唯一源。1434 为 COMMITS.csv 绑定行（47 任务）。
- 落点（按目录直方图归纳，不逐文件展开）：`docs/modules/registry/` 22 文件 +1237/-0；`tools/quality/` 1 文件 +148/-0；`evidence/v6_1_rework/` 4 文件 +137/-1（TASK_LEDGER.csv、PLAN.md、TASK_RESULT.json、logs/c01_module_readmes.log）。
- 内容性质：文档或规格（批量生成）+ CI 或机器门（生成器脚本）+ 控制包或台账。
- 意图证据：消息自述"22 个 registry 模块 README, 生成器 gen_module_readmes 以 descriptor 为源"；变更面证据与数量一致（22 个新增文档 + 1 个生成器）。客观登记一处措辞差异：消息称"README"，实际新增文件为 `docs/modules/registry/<module-id>.md` 形态，每个约 56-57 行（同构模板特征）。
- 关联：与组 1（DOC-003）、组 3（DOC-005）、组 4（DOC-006）构成 DOC-003..006 连续文档重建族，每条内容提交后紧跟一条 COMMITS.csv 绑定提交（1432/1434/1436/1438）。
- 不确定点：生成器是否已接入 CI 检查项（除 c01 日志外未见 ci/checks.json 变更）——未证实。

### 组 3 | seq 1435, 1436 | 8a3dec6f, 54caf70e | 2026-09-01 | S, S（DOC-005）
- 做了什么：重建 L0 负责人文档：改写根 `REVIEW.md`（+29/-11），加入 V6.1 进度与组件状态表；1436 为 COMMITS.csv 绑定行（48 任务）。
- 落点：`REVIEW.md`（1 文件 +29/-11）；`evidence/v6_1_rework/`（5 文件 +136/-1：TASK_LEDGER.csv、PLAN.md、TASK_RESULT.json、logs/c01_l0_docs.log、c02_final_trace.log）。
- 内容性质：文档或规格 + 控制包或台账。
- 意图证据：消息自述"5 L0 链接完整; 追溯诚实"；变更面证据仅显示 REVIEW.md 有实际改写、两个检查日志各 1 次落盘，"5 个 L0 链接"的具体清单未在卡片中展开。
- 关联：c02_final_trace.log 与重叠区 seq 1429（DOC-002 追溯矩阵，其日志同名）同名复用，属同一追溯链的两次重建；REVIEW.md 在组 41（seq 1482 GOV-003）再次被改（+2/-1）。
- 不确定点：REVIEW.md 中组件状态表的具体状态字面量未证实（卡片未摘录）。

### 组 4 | seq 1437, 1438 | 8ba2f265, c0927374 | 2026-09-01 | M, S（DOC-006）
- 做了什么：统一注释命名与可读性清理：在 15 个代码文件中把任务号注释清零并保留合同 ID（改动量对称，如 `lib/phase2/src/upm.cpp` +13/-13、`sampler.cpp` +6/-6、`noise_model.h` +1/-1、`wcs_tan.h` +1/-1、`hp_drizzle_api.cpp` +1/-1、`p3_output.cpp/.h`、`p3_resample.cpp/.h`、`p2_session.cpp`）；1438 为 COMMITS.csv 绑定行（49 任务）。
- 落点：`lib/phase3_session/`（7 文件 +12/-12）、`lib/phase2/`（4 文件 +25/-25）、`lib/phase1/`（2 文件 +2/-2）、`lib/healpix_db/`（1 文件）、`lib/phase2_session/`（1 文件）、`evidence/v6_1_rework/`（5 文件 +155/-1，含 logs/c01_hygiene.log、c02_taskid_scan.log）。
- 内容性质：结构整理（注释层清理，增删行数对称）+ 控制包或台账。
- 意图证据：消息自述"任务号注释 0 残留, 保留合同 ID; hygiene 0 违规"；变更面证据支持"注释级整理"判断——15 个代码文件全部为等量增删（+n/-n），无新增声明、无新文件。
- 关联：紧接组 1..3 的 DOC 族；其清理对象包含组 1（seq 1431）刚改过的 `p3_resample.cpp/.h`；后续 seq 1455（REL-002）改动的 `tools/quality/check_doc_symbols.py` 属同一注释/符号一致性链。
- 不确定点：c02_taskid_scan.log 仅 +2 行，扫描范围（是否覆盖全部 lib/）未证实。

### 组 5 | seq 1439 | e25f3f0c | 2026-09-01 | L（QA-001 第一轮）
- 做了什么：零警告与静态分析首轮：在根 `CMakeLists.txt` 新增 39 行严格编译 flags（GCC/Clang），并跨 42 个文件消除警告与静态分析 P0/P1 项；触及 `cli/`（commands.cpp +3/-10、monitor.h +16/-14、resource_events.h、resource_recorder.h、runtime_client.cpp +7/-7）、`lib/core/`（4 文件 +71/-25）、`lib/backend_host/`（4 文件，含 baseline_kernels_impl.inc、bench_harness.cpp、hardware_inspect.cpp）、`lib/phase2/`（3 文件 +28/-46）、`lib/healpix_db/`（2 文件 +31/-31）、`lib/astro_image_io/`（3 文件）、`lib/phase1/`、`lib/phase1_session/`、`lib/common/`、`include/astrocs/core/scheduler.h`（-2）、`tests/unit/`（8 文件 +15/-21）。
- 落点（按目录直方图归纳）：tests/unit 8、lib/backend_host 4、lib/core 4、lib/astro_image_io 3、lib/phase1 3、lib/phase2 3、lib/common 2、lib/healpix_db 2、lib/phase3_session 2、cli 5、include/astrocs 1、lib/phase1_session 1、CMakeLists.txt 1、evidence/v6_1_rework 3（TASK_LEDGER.csv、tasks/QA-001/PLAN.md +74、TASK_RESULT.json +248）。
- 内容性质：CI 或机器门（编译 flags）+ 生产代码（警告修复）+ 测试（tests/unit 适配）+ 控制包或台账。
- 意图证据：消息自述"GCC/Clang 严格 flags 0 warning; clang-tidy/cppcheck P0/P1 清零"；变更面证据支持"flags 新增 + 全仓小改动"结构（CMakeLists 纯新增 39 行；其余文件增删量小且多为对称）。本条提交未附 c0x 日志（日志在组 8 的 seq 1444 补交）。
- 关联：与组 11（seq 1447）同为 QA-001，构成"首轮 + 收尾"两段式；其 flags 是组 8（seq 1443/1444 LNX-001 干净构建）的前提；台账行 QA-001 的依赖列引用 DOC-006（组 4）。
- 不确定点：clang-tidy/cppcheck 的调用配置位置未在卡片变更面中单独出现（可能内联于 CMakeLists 或 tools/），未证实。

### 组 6 | seq 1440 | 8003297f | 2026-09-01 | S（QA-002）
- 做了什么：接入 sanitizer 并修两处并发/内存缺陷：根 `CMakeLists.txt` 新增 sanitizer 接线（+19/-1，卡载注释标题显示 ASan+UBSan+LSan 与 TSan 互斥单选、生产默认 OFF）；`tests/unit/cpu_abi_test.cpp`（+3）与 `tests/unit/p1_resource_test.cpp`（+16/-11，出现 `worker(out_a, 0, n/2)` 形态的线程参数改动）；新增 `tools/quality/tsan/cfitsio.supp`（+9）抑制项。
- 落点：`CMakeLists.txt`、`tests/unit/`（2 文件）、`tools/quality/tsan/`、`evidence/v6_1_rework/`（TASK_LEDGER.csv、tasks/QA-002/TASK_RESULT.json +146）。
- 内容性质：CI 或机器门（sanitizer 选项与 TSan 抑制文件）+ 测试 + 控制包或台账。
- 意图证据：消息自述"ASan/UBSan/LSan 56/56, TSan 定向 28/28, 修复 cpu_abi leak 与 p1_resource race"；变更面证据与两个被点名测试文件一一对应，且台账 QA-002 行由 NOT_STARTED 改为 PASS（卡载成对增删行）。
- 关联：依赖列为 QA-001;RT-003（组 5 与更早的 RT 族）；其 56/56 与 28/28 计数在组 10（seq 1446 LNX-003）现场复验。
- 不确定点：`cpu_abi_test.cpp` 仅 +3 行，泄漏修复是否落在生产侧 `lib/core`（同族的 `astrocs_host_services_default_v1` 出现在 hunk 头）未证实。

### 组 7 | seq 1441, 1442 | 46e8dbb0, 591f541f | 2026-09-01 | M, S（QA-003）
- 做了什么：生成依赖许可与 SBOM 证据：新增 `DEPENDENCY_LOCK.md`、`SBOM.json`（CycloneDX 1.5，锁定 nlohmann/cfitsio/zlib 与编译链 metadata）；1442 为 QA-003 的 TASK_RESULT.json 补 `commands` 字段以过 schema 的 minItems=1 校验（+29/-1）。
- 落点：`evidence/v6_1_rework/`（1441：TASK_LEDGER.csv、DEPENDENCY_LOCK.md、SBOM.json、TASK_RESULT.json 共 +288/-1；1442：tasks/QA-003/TASK_RESULT.json）。
- 内容性质：数据或产物（SBOM/锁定表）+ 控制包或台账。
- 意图证据：消息自述给出格式与依赖清单；变更面证据为两个新文件 + 台账行；1442 的消息自述（"schema minItems=1 校验"）与单文件 +29/-1 的补丁形态一致。
- 关联：1442 是对 1441 的即时补正（同编号连续提交）；其 parent_commit 字段在组 20（seq 1458 REL-003）被再次修正。
- 不确定点：SBOM 是否被 CI 消费（无 ci/ 面变更）——未证实。

### 组 8 | seq 1443, 1444 | b8879dae, 84a4ae9d | 2026-09-01 | S, L（LNX-001）
- 做了什么：登记 Linux 干净构建结果（GCC Release + Clang Debug 全新目录、0 warning、56/56 PASS、compile_commands/link map/time-v…），随后把 QA-001/QA-002/QA-003/LNX-001 四个任务的现场日志一次性补交入库。
- 落点（1444 按目录直方图归纳）：1443 → `evidence/v6_1_rework/` 2 文件 +181/-1（TASK_LEDGER.csv、TASK_RESULT.json）；1444 → `evidence/v6_1_rework/tasks/{LNX-001,QA-001,QA-002}/logs/` 共 21 个新增日志文件 +44853/-0，其中体量最大者为 `QA-001/logs/c04_clang_tidy.log`（+28207）与 `LNX-001/logs/c07_link_map.map`（+11895）、`c08_compile_commands_gcc.json`（+1190）。
- 内容性质：数据或产物（构建日志/链接映射/compile_commands）+ 控制包或台账。
- 意图证据：消息自述"gitignore logs/ 例外, 强制纳入版本库"；变更面证据显示 1444 的 21 个文件全在 evidence 区且全为新增，**但该提交未包含 `.gitignore` 变更面**（本片 `.gitignore` 唯一改动在 seq 1480）。登记为自述与变更面的可核对差异，不推断原因。
- 关联：1444 是组 5/6/7/8（QA-001..003、LNX-001）证据的集中落盘；其日志名（c01_gcc_build、c04_clang_tidy、c05_cppcheck、c06_scan_build）与组 5 声称的静态分析工具集对应。
- 不确定点：卡片 truncated=True（文件清单为前 21 项，与 nfile=21 一致）；"time-v…"（消息首行被截断）所指内容未证实。

### 组 9 | seq 1445 | 25dc4562 | 2026-09-02 | S（LNX-002）
- 做了什么：登记 Linux 全合成科学测试结果：`astrocs test synthetic all` 7/7 PASS、模块 56/56，并落一份机器可读摘要。
- 落点：`evidence/v6_1_rework/` 3 文件 +209/-1（TASK_LEDGER.csv、tasks/LNX-002/MACHINE_SUMMARY.json、TASK_RESULT.json）。
- 内容性质：控制包或台账 + 数据或产物（MACHINE_SUMMARY.json）。
- 意图证据：消息自述给出计数；变更面证据为新增 MACHINE_SUMMARY.json（机器摘要）与台账行改写，无代码面。
- 关联：紧随组 8（LNX-001 干净构建）之后，是"构建通过 → 科学测试通过"的顺序推进；MACHINE_SUMMARY.json 形态与 seq 1449 的 c0x_*_summary.json 同族。
- 不确定点：7 个合成用例的具体名称未证实（卡片未展开）。

### 组 10 | seq 1446 | db5c3bd1 | 2026-09-02 | S（LNX-003）
- 做了什么：在新 build 目录现场复验 sanitizer（ASan/LSan 56/56、TSan 28/28）并把结果与 commit 绑定登记。
- 落点：`evidence/v6_1_rework/` 2 文件 +162/-1（TASK_LEDGER.csv、tasks/LNX-003/TASK_RESULT.json）。
- 内容性质：控制包或台账。
- 意图证据：消息自述"绑定 commit"；变更面证据为单任务结果文件新增，无代码/测试面。
- 关联：复验对象即组 6（QA-002）建立的 sanitizer 接线；台账行依赖列含 LNX-002;QA-002。
- 不确定点：TASK_RESULT 中绑定的 commit 值未证实（卡片未摘录字段值）。

### 组 11 | seq 1447 | c0c5f46c | 2026-09-02 | L（QA-001 第二轮收尾）
- 做了什么：QA-001 收尾：清零 sign-conversion 类警告 93 处并做 Clang 严格重建。改动 46 个文件、全部为修改（无新增/删除），增删几乎完全对称（+160/-159）。
- 落点（按目录直方图归纳）：`lib/backend_host/` 11 文件（+13/-13，含 avx2_backend.cpp、avx512_backend.cpp）、`lib/acr/` 8 文件（+22/-22，含 acr_cuda_bridge_host.cpp、cuda_bridge_api.hpp、cuda_bridge_loader.cpp、kernel_registry.hpp、focused_operations.cpp、device_executor.hpp、dispatcher.cpp/.hpp）、`lib/healpix_db/` 7 文件（+44/-44）、`lib/astro_image_io/` 6 文件（+48/-48，含 aio_upm.h、aio_cfitsio_mutex.h、aio_fits.cpp、aio_xisf.cpp、aio_hips_writer.cpp、hiss_writer.cpp）、`lib/phase2/` 6 文件、`tests/unit/` 5 文件、`lib/orchestrator/` 1、`lib/phase1/` 1、`lib/phase3_session/` 1。
- 内容性质：生产代码（警告清零）+ 测试（tests/unit 同步）。
- 意图证据：消息自述点名四个家族（spherical_overlap/reverse_drizzle/hp_drizzle_api/fits_reader）与"93 处"计数；变更面证据支持"等量替换式修复"（对称增删、无新文件、无台账面）。
- 关联：与组 5 同编号（QA-001）的第二段；本条**不含任何 evidence 面**，即结果证据延后到组 12/组 8 的日志族登记。
- 不确定点：本条触及 `lib/acr/`（ACR/GPU 桥接面）；该目录代码在本期是否生产可达、是否受 ACR 关闭策略约束，卡片未给出证据——未证实（仅登记事实：改动落在 lib/acr 的 CUDA 桥接与调度器文件）。

### 组 12 | seq 1448, 1449 | 2baf4ecd, 243c12af | 2026-09-02 | M, M（LNX-004）
- 做了什么：2c2g 资源与恢复验证并修一处取消接线缺陷：`cli/runtime_client.cpp`（+28/-6）新增 `cancel_watch` 闭包（含 `sleep_for(std::chrono::milliseconds(50))`）并新增 include `cancel_token.h`、`atomic`、`chrono`、`thread`；1449 补交 9 个证据文件。
- 落点：`cli/runtime_client.cpp`；`evidence/v6_1_rework/tasks/LNX-004/`（TASK_RESULT.json +216；1449 的 c01_p1_heavy_summary.json、c02_p2_heavy_summary.json、c03_p3_heavy_summary.json、c04_cancel_manifest.json、c05_cancel_run.log、c06_resume_run.log、c07_worker_balance_p2.csv、c08_worker_balance_p3.csv、c09_cancel_fix_build.log，共 +551/-0）。
- 内容性质：生产代码（取消接线）+ 数据或产物（资源摘要/worker 均衡/取消清单）+ 控制包或台账。
- 意图证据：消息自述"修复 cancel 接线"与"P1/P2/P3 heavy 门禁 PASS, cancel/resume/低内存 backpressure"；变更面证据同时支持两点——代码侧新增 cancel_watch 与 token/atomic 依赖，证据侧三阶段 heavy 摘要 + cancel/resume 日志 + 两份 worker_balance CSV。
- 关联：runtime_client.cpp 亦是重叠区 seq 1425（P3-006）与组 5（QA-001）的落点，属同一 CLI↔Runtime 客户端链；worker_balance CSV 命名与 AGENTS.md 登记的 CI 资源产物名一致（是否由 CI 消费未证实）。
- 不确定点：低内存 backpressure 的实现在本提交变更面中无对应文件（仅日志 c05/c06 与摘要），未证实。

### 组 13 | seq 1450, 1451 | c7dee933, 73282a1f | 2026-09-02 | S, S（LNX-005 首次登记）
- 做了什么：真实数据冒烟尝试的外部依赖等待登记：Fatduck 离线，任务记为 WAITING_WINDOWS / NOT_RUN_EXTERNAL_OFFLINE，并声明 WIN-004 仍为强制真实数据门；1451 补交离线探测日志。
- 落点：`evidence/v6_1_rework/`（1450：TASK_LEDGER.csv、tasks/LNX-005/TASK_RESULT.json +67/-1；1451：logs/c01_fatduck_offline_probe.log 与 fatduck_probe_offline.log，共 +15/-0）。
- 内容性质：控制包或台账 + 数据或产物（探测日志）。
- 意图证据：消息自述给出两个状态字面量；变更面证据为台账 + 结果文件 + 两份探测日志，无任何代码面。
- 关联：本条台账状态在组 18（seq 1456）被改为 WAITING_DATA，并在组 20（seq 1458）改写为 PASS 诚实登记，构成同一任务的三次状态订正链。
- 不确定点：两份探测日志内容差异（c01_ 前缀与裸名并存）未证实。

### 组 14 | seq 1452 | e1045969 | 2026-09-02 | M（WIN-001..006 批量登记）
- 做了什么：为 WIN-001 至 WIN-006 六个 Windows 任务各建一份 TASK_RESULT.json 并把台账 6 行改写为等待状态，明确"非伪造 PASS，下一在线窗口执行"。
- 落点：`evidence/v6_1_rework/TASK_LEDGER.csv`（+6/-6）与 `evidence/v6_1_rework/tasks/WIN-00{1..6}/TASK_RESULT.json`（6 个新增，各 +63）。
- 内容性质：控制包或台账。
- 意图证据：消息自述与变更面完全对应（6 个同构结果文件 + 台账 6 行等量改写）。
- 关联：与组 13 同一现场（Fatduck 离线）；其 WIN-001 在组 22..30（seq 1460..1474）被真实执行，WIN-004 在组 20（seq 1458）与 REL-003 一致性修正中再次出现，WIN-006 与组 16（seq 1454）的 F-037 遗留绑定。
- 不确定点：六个结果文件的 status 字面量是否全为 WAITING_WINDOWS（消息如此自述）未逐条展开证实。

### 组 15 | seq 1453 | 294c2b76 | 2026-09-02 | S（REL-001）
- 做了什么：登记 Linux 侧 alpha.2 发布包完成、Windows 侧待 Fatduck（WAITING_WINDOWS）。
- 落点：`evidence/v6_1_rework/` 2 文件 +95/-1（TASK_LEDGER.csv、tasks/REL-001/TASK_RESULT.json）。
- 内容性质：控制包或台账。
- 意图证据：消息自述；变更面为单任务结果 + 台账行，无代码/构建面（发布包本体不在本提交变更面内）。
- 关联：与组 21（seq 1459 审核包 ZIP）同属 REL 交付族；alpha.2 版本号与组 38（seq 1482 GOV-003）推进到 0.11.0-alpha.1 形成版本线对照（重叠区 seq 1367 亦为版本推进，见片尾）。
- 不确定点：发布包产物路径未在本提交变更面出现，未证实。

### 组 16 | seq 1454 | 8c71f7ab | 2026-09-02 | S（REL-002 FINDINGS）
- 做了什么：在 V6.1 返工控制包目录内新增 FINDINGS 台账，登记 P0/P1 关闭 36/40（含 resolution_commit 与 evidence 绑定），点名遗留 F-032/033/036（归 REL-003）与 F-037（归 WIN-006）。
- 落点：`工程控制/AstroCS_V6_1_REWORK_CONTROL_20260831/02_FINDINGS.csv`（新增 +41/-0，单文件提交）。
- 内容性质：控制包或台账。
- 意图证据：消息自述给出计数与遗留编号；变更面证据为唯一一份 FINDINGS CSV 的新建（41 行）。
- 关联：该文件在组 36（seq 1480 GOV-002）被整体改名为 `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/AstroCS_V6_1_REWORK_CONTROL_20260831/02_FINDINGS.csv`（卡载改名行直接可证），构成"新建 → 归档"闭环。
- 不确定点：41 行中 36 条关闭项的 resolution_commit 值未证实（卡片未展开 CSV 行）。

### 组 17 | seq 1455 | c99babad | 2026-09-02 | M（REL-002 校验套件适配）
- 做了什么：把三个契约校验器适配到 V6.1 现状：`tools/quality/check_serial_heavy.py`、`check_doc_symbols.py`、`check_execution_contracts.py`（合计 +135/-72）改为识别 lease + std::thread 形态，并把 CPU_BACKEND_ARCH 数学合同头指向 `baseline_kernels.h`；同时重生成两份可达性产物（PROD_REACHABILITY.dot/.json，净删 1201 行）与注释卫生报告。
- 落点：`tools/quality/`（3 文件）、`evidence/v6_1_rework/`（2 文件 +144/-1201）、`reports/v19r2/`（2 文件 +2/-2）、`docs/architecture/`（1 文件 +1/-1）。
- 内容性质：CI 或机器门（校验器）+ 数据或产物（重生成图/报告）+ 文档或规格（1 行架构文档指向）。
- 意图证据：消息自述点名三个校验器与合同头指向；变更面证据与点名一致（三脚本 + 合同相关文档 1 行 + 产物重生成）。
- 关联：校验器族与组 4（DOC-006 hygiene）同源；其产物重生成解释了 -1276 的大删除量（非人工删内容）；与组 19（seq 1457 gen_commits_csv）同属 tools/quality 工具链。
- 不确定点：`docs/architecture/` 被改的 1 行具体文件未证实（卡片仅给目录聚合）。

### 组 18 | seq 1456 | edcc8688 | 2026-09-02 | S（LNX-005 状态订正）
- 做了什么：把 LNX-005 台账状态由等待 Windows 改为 WAITING_DATA，理由为"真实数据待取非 Windows 编译窗口"以满足 validator 规则 4，同时保持 TASK_RESULT 的 WAITING_WINDOWS 以过 schema 枚举。
- 落点：`evidence/v6_1_rework/TASK_LEDGER.csv`（+1/-1，单行改写）。
- 内容性质：控制包或台账。
- 意图证据：消息自述明确区分两个状态字面量的不同约束来源（validator 规则 4 与 schema 枚举）；变更面证据为台账单行，未改 TASK_RESULT，与自述一致。
- 关联：紧接组 13（seq 1450）与组 14（seq 1452）的等待登记；组 20（seq 1458）再次订正同一任务。
- 不确定点：validator 规则 4 的条文位置（tools/quality/validate_task_ledger.py 或 ci/）未在本提交变更面出现，未证实。

### 组 19 | seq 1457 | d5a34ea9 | 2026-09-02 | S（REL-002/REL-003 生成器）
- 做了什么：修改 `tools/quality/gen_commits_csv.py`（+5/-0）使其只输出 PASS 任务，并据此生成 COMMITS.csv（56 任务，列格式转 validator 口径）。
- 落点：`tools/quality/gen_commits_csv.py`、`evidence/v6_1_rework/COMMITS.csv`（+7/-0）。
- 内容性质：CI 或机器门（生成器）+ 控制包或台账。
- 意图证据：消息自述"只输出 PASS 任务"；变更面证据为生成器 5 行新增 + COMMITS 追加，与组 1..4 的"绑定 N 任务"式单行追加形成口径切换（由逐任务绑定改为按 PASS 过滤生成）。
- 关联：与组 20（seq 1458，COMMITS 57 任务）连续，任务计数 56→57 可核对。
- 不确定点：过滤规则是否同时排除 WAITING_* 状态未证实。

### 组 20 | seq 1458 | faad602d | 2026-09-02 | S（REL-003 一致性订正）
- 做了什么：审核包内部一致性修正：把 LNX-001 的 parent_commit 改为真实的 QA-003 SHA、把 LNX-005 改为 PASS 诚实登记、COMMITS.csv 扩为 57 任务并含 LNX-005。
- 落点：`evidence/v6_1_rework/` 4 文件 +10/-9（COMMITS.csv、TASK_LEDGER.csv、两份 TASK_RESULT.json）。
- 内容性质：控制包或台账。
- 意图证据：消息自述点名三处修正；变更面证据为等量小改（+10/-9），符合"字段订正"而非新增内容。
- 关联：直接订正组 8（LNX-001）、组 7（QA-003 SHA 引用）、组 13/18（LNX-005 状态第三次改写）与组 19（COMMITS 计数）。
- 不确定点：LNX-005 由 WAITING_DATA 改 PASS 所依据的真实数据是否已在 09-02 取得（组 13 曾记 NOT_RUN_EXTERNAL_OFFLINE），卡片未给出证据——未证实。

### 组 21 | seq 1459 | 24c7eec7 | 2026-09-02 | S（REL-003 Linux 侧交付）
- 做了什么：审核包 Linux 侧交付登记：1309 文件 validator 双 PASS、状态 NOT_READY、生成 dist/audit 下 ZIP、FINDINGS 36/40、COMMITS 57 任务；Windows/32R 待 Fatduck。
- 落点：`evidence/v6_1_rework/COMMITS.csv`（+1/-1）、`evidence/v6_1_rework/tasks/REL-003/TASK_RESULT.json`（+89）、`dist/audit/AstroCS_V6_1_AUDIT_20260902T042239Z_faad602da555.zip`（二进制，噪声区 1）。
- 内容性质：控制包或台账 + 数据或产物（ZIP 封装）。
- 意图证据：消息自述给出文件数与状态字面量 NOT_READY；变更面证据为结果文件 + ZIP 落盘。
- 关联：ZIP 文件名内嵌 sha8 faad602d（即组 20 的提交），构成"绑定 SHA 的封装"证据；与组 15（REL-001）同为 REL 族。
- 不确定点：`dist/` 目录在本片仅此一次出现，其是否长期入库或后续被归档未证实（本片内无后续处置证据）。**该 ZIP 本身登记为运行产物/封装件，不构成工作内容证据**（本提交其余两文件为台账面，故整条提交不属"全在噪声区"）。

### 组 22 | seq 1460 | e37d9147 | 2026-09-02 | S（WIN-001 第 1 环）
- 做了什么：MSVC 兼容首修：`lib/core/logging.cpp` 对 `gmtime_r` 加条件编译改用 `gmtime_s`（+4/-0）。
- 落点：`lib/core/logging.cpp`。
- 内容性质：生产代码。
- 意图证据：消息自述点名函数与替换方向；变更面为单文件 4 行新增，符合条件编译补丁形态。
- 关联：与组 24（seq 1462 的 runtime.cpp gmtime_s）同族；是组 14（seq 1452 WAITING_WINDOWS 登记）之后 WIN-001 的真实开工点。
- 不确定点：无（单点修复，证据自洽）。

### 组 23 | seq 1461 | bf61fa7d | 2026-09-02 | S（WIN-001 第 2 环）
- 做了什么：为 MSVC 添加全局 `/utf-8` 编译选项，消息点名根因为中文注释乱码引发级联语法错误、表现为 contracts.h 的 C2144。
- 落点：根 `CMakeLists.txt`（+4/-0）。
- 内容性质：CI 或机器门（构建选项）。
- 意图证据：消息自述给出错误码与根因；变更面证据为构建面 4 行新增，未触及 contracts.h 本体（与"根因在他处"的自述一致）。
- 关联：与组 22、组 24 同为 MSVC 构建打通的前三环。
- 不确定点：C2144 的现场日志未入库（本条无 evidence 面），未证实。

### 组 24 | seq 1462 | fc676dd6 | 2026-09-02 | S（WIN-001 第 3 环）
- 做了什么：MSVC 构建依赖适配：cfitsio 的 pthread/zlib 经 MSYS2 + `ACS_ZLIB_ROOT` 接入、cpu 目标补 nlohmann include、`lib/core/runtime.cpp` 改 `gmtime_s`。
- 落点：根 `CMakeLists.txt`（+23/-3）、`lib/core/runtime.cpp`（+4/-0）。
- 内容性质：CI 或机器门（依赖接线）+ 生产代码。
- 意图证据：消息自述点名四项；变更面证据支持其中三项（构建面 + runtime.cpp）。
- 关联：组 22 的同类时间函数修复在 runtime.cpp 复现；组 25（seq 1463）紧接处理 pthread shim。
- 不确定点：`ACS_ZLIB_ROOT` 的取值来源（Fatduck 机器路径）在组 25 卡片注释标题中出现（zlib-msvc 安装路径），本条未证实。

### 组 25 | seq 1463, 1464, 1465, 1466 | fed64294, a8cfb8c7, 1713b758, 8c03ddf1 | 2026-09-02 | S×4（WIN-001 pthread shim 迭代）
- 做了什么：为 cfitsio 在 MSVC 下新建并四次迭代 `cmake/win32_pthread_shim/pthread.h`：1463 首版以 SRWLOCK 映射实现 `pthread_mutex_init/lock/unlock`、`pthread_mutexattr_init/settype` 并全局定义 NOMINMAX；1464 去除 windows.h include 改手动声明 SRWLock；1465 加 SRWLock guard 以兼容 windows.h、补 `PTHREAD_MUTEX_RECURSIVE`、新增 `unistd.h` shim 与 `_USE_MATH_DEFINES`；1466 最终改为纯原子自旋锁（净 -6 行，声明零 windows.h/SDK 依赖）。
- 落点：`cmake/win32_pthread_shim/`（pthread.h 四连改：+42、+28/-12、+23/-11、+19/-25；unistd.h 新增）、根 `CMakeLists.txt`（1463 +7/-5、1464 +1/-0、1465 +2/-1）。
- 内容性质：生产代码（第三方兼容 shim，构建支撑面）。
- 意图证据：四条消息自述各自点名本轮问题（宏污染、winnt.h 冲突、递归互斥、SRWLOCK 重定义）；变更面证据与自述一致——同一文件的连续小改且末轮为净删减（+19/-25），符合"换实现"而非"加功能"。
- 关联：四条为同一 shim 的连续收敛链（间隔 13:33→13:37，4 分钟内 3 次改），并共同服务组 24 引入的 cfitsio MSVC 编译。
- 不确定点：shim 是否通过 MSVC 实机编译（本片 1460..1474 均无 evidence 面日志），未证实。

### 组 26 | seq 1467 | 3b304df7 | 2026-09-02 | S（WIN-001 第 4 环）
- 做了什么：zlib 链接库名改为 `libz.lib`，并为 astro_image_io 目标补 third_party include（nlohmann）。
- 落点：根 `CMakeLists.txt`（+4/-3）。
- 内容性质：CI 或机器门（链接与 include 配置）。
- 意图证据：消息自述两项；变更面为构建面单文件小改。
- 关联：承接组 24 的 zlib 接入与组 25 的 shim 链。
- 不确定点：无。

### 组 27 | seq 1468 | 95958371 | 2026-09-02 | S（WIN-001 第 5 环）
- 做了什么：`lib/astro_image_io` 的 `hiss_format.h` 中 undef `OPTIONAL`/`REQUIRED`，以解除 Windows 宏对 `SubblockFlags` 枚举值的污染。
- 落点：`hiss_format.h`（+8/-0）。
- 内容性质：生产代码（平台宏防御）。
- 意图证据：消息自述点名枚举与宏名；变更面为单头文件 8 行新增。
- 关联：与组 28（seq 1469）同属"Windows 宏污染"清理族。
- 不确定点：无。

### 组 28 | seq 1469 | d80d26d9 | 2026-09-02 | S（WIN-001 第 6 环）
- 做了什么：undef Windows 噪音宏（含 `ERROR`）：`include/astrocs/core/context.h` 自清理（+6/-0），`cli/monitor.h` 统一在 windows.h 引入点清理（+31/-0）。
- 落点：`cli/monitor.h`、`include/astrocs/core/context.h`。
- 内容性质：生产代码（公共头与 CLI 头的宏卫生）。
- 意图证据：消息自述"统一清理 windows.h 引入点"；变更面证据为两处纯新增，符合"加 undef 块"形态。
- 关联：monitor.h 亦是组 5（QA-001）与重叠区 seq 1425 的落点，属同一文件的多轮改写。
- 不确定点：被 undef 的宏完整清单未证实。

### 组 29 | seq 1470, 1471, 1472, 1473 | 46acc590, 2e6bf329, ce8a8455, 8d1a957c | 2026-09-02 | S×4（WIN-001 strtok/链接迭代）
- 做了什么：`m` 链接加 UNIX guard 并在 shim 中补 `strtok_r`（1470，因 cfitsio 定义 `_REENTRANT`）；修复 CMakeLists 中 m/z 链接块冗余 `endif`（1471，纯删 1 行）；把 `strtok_r` 改为手写实现以避开 `strtok_s` 的 UCRT 冲突 C2040（1472，+34/-3）；把 shim 内 `nullptr` 改 `NULL`（1473，+4/-4，因 cfitsio 为 C 文件）。
- 落点：根 `CMakeLists.txt`（1470 +7/-5、1471 +0/-1）、`cmake/win32_pthread_shim/pthread.h`（1470 +6/-0、1472 +34/-3、1473 +4/-4）。
- 内容性质：生产代码（shim）+ CI 或机器门（链接块结构）。
- 意图证据：四条消息自述分别点名错误码/冲突原因；变更面证据与自述一致（1471 为纯结构删除，1473 为等量替换）。
- 关联：与组 25 同文件（pthread.h）延续，构成 WIN-001 的第二段收敛链（13:49→13:52）。
- 不确定点：`_REENTRANT` 的定义位置（cfitsio 侧还是本项目宏）未证实。

### 组 30 | seq 1474 | c1696156 | 2026-09-02 | S（WIN-001 收尾）
- 做了什么：测试面 MSVC 兼容：`tests/unit/CMakeLists.txt` 为 `io_reentrant_test` 加 z 链接分支（+5/-1），`tests/unit/p3_output_test.cpp` 以 `<filesystem>` 替代 `popen`（+8/-5）。
- 落点：`tests/unit/`（2 文件）。
- 内容性质：测试 + CI 或机器门（测试构建配置）。
- 意图证据：消息自述点名两个测试目标与替换方向；变更面证据一致（新增 include filesystem）。
- 关联：WIN-001 链的末条（13:55:44），其后即组 31 的 435 分钟断点。
- 不确定点：Windows 侧测试是否实际跑通（无 evidence 面），未证实。

### 组 31 | seq 1475 | 065c81de | 2026-09-02 | M（GOV-001）
- 做了什么：冻结 Alpha 工程约束：新建根 `AstroCS_ENGINEERING_CONSTRAINTS.md`（负责人冻结整理版，含"0. 来源与修订关系（机器可读）"与 A..H 分类节 + 机器索引），把 `AGENTS.md` 由长文规则改写为入口/路由式精简版（+28/-60，删除"基本规则/连续执行状态机/验证/工程管理/Linux环境/Windows验证/工程包执行"等节，新增"节点与角色/执行纪律/目录规范"式引用），并新增机器门 `tools/doccheck/check_engineering_constraints.py`（+183）。
- 落点：`AGENTS.md`、`AstroCS_ENGINEERING_CONSTRAINTS.md`、`tools/doccheck/`。
- 内容性质：文档或规格 + CI 或机器门（约束一致性检查器）。
- 意图证据：消息正文（本片首批含正文的提交之一）自述 requirements GOV-001-R1..R8、tests 判定字面量 `CONSTRAINTS_PASS (6/6)`、`scientific_change: NO`、paths 三项；变更面证据与 paths 完全一致（三文件，点名与变更面交集含 AGENTS.md 与约束文件）。
- 关联：与组 36（GOV-002）、组 38（GOV-003）构成 GOV 族；其约束文件在仓库当前状态中已被宪章取代（AGENTS.md 现登记 ARCHIVED_NON_NORMATIVE），属后续片段的演进，本片不展开。
- 不确定点：检查器 6/6 判定的具体断言项未证实。

### 组 32 | seq 1476 | bbe2e59e | 2026-09-02 | M（LOG-001）
- 做了什么：冻结统一结构化日志合同：新增 `runtime/logging/log_event_v1.schema.json`（15 顶层字段 + seq/ts/run/task/node/module/phase/commit/host/level/event/units/elapsed/diagnostic，error 为唯一允许嵌套）、参考实现 `runtime/logging/log_event.py`（`class LogEvent`、`class SeqAllocator`、`MAX_LINE_BYTES=4096`、`redact()`、`summary()`）、合同文档 `docs/architecture/observability/STRUCTURED_LOGGING_CONTRACT.md`（状态 ACTIVE_NORMATIVE）、检查器 `tools/monitoring/check_log_contract.py` 与测试 `tests/monitoring/test_log_contract.py`（TestSchemaContract/TestDualOutput/TestRedact/TestSequenceConcurrency/TestLineSizeLimit/TestCheckerCli/TestNoRawTestdata）。
- 落点：`runtime/logging/`（3 文件 +424）、`tests/monitoring/`（2 文件 +352）、`docs/architecture/observability/`（+135）、`tools/monitoring/`（+360）。
- 内容性质：文档或规格（合同）+ 生产代码（参考实现，Python）+ 测试 + CI 或机器门（检查器）。
- 意图证据：消息正文自述 requirements LOG-001-A1..A7、tests 判定 `check_log_contract.py PASS` 与 "pytest 24 passed"、`scientific_change: NO`；变更面证据显示四目录全新增、零删除，与"只冻结合同 + schema + 小型验证，不实现生产 logger、不接监控"的边界自述一致（合同文档 §1 明确 LOG-002 才接生产 Runtime）。
- 关联：组 36（seq 1480）的编号字段同时含 LOG-001，因归档时补登本任务文档；本条与组 22..30 的 logging.cpp/monitor.h 改动同属日志面但不同编号族。
- 不确定点：卡载自动比对字段显示"点名与变更面交集：无交集"，原因是消息以目录形式（带尾斜杠）点名而比对器按文件名匹配；实际变更目录与 paths 一致（登记为比对器口径问题，非内容矛盾）。

### 组 33 | seq 1477 | 1c3d1dc9 | 2026-09-02 | M（ARC-001）
- 做了什么：冻结 DLL 产品边界为机器可校验合同：新增 `contracts/config/module_dll_contract.schema.json`（contract_id `CONTRACT-ARC001-DLL-BOUNDARY-V1`、doc_status ACTIVE_NORMATIVE、target_version `0.11.0-alpha.1`、entrypoint_abi `astrocs_module_query_v1`，含 dll_units、forbidden_dependencies、dependency_matrix（cli_to_runtime/cli_to_io/runtime_to_io/module_to_runtime/module_to_catalog/catalog_to_io/provider_to_runtime）、install_tree、aggregation_policy）。
- 落点：`contracts/config/`（1 文件 +267/-0）。
- 内容性质：文档或规格（合同 schema，JSON Schema 2020-12）。
- 意图证据：消息正文自述 requirements ARC-001 验收 1/2、tests "validate_dll_contract.py 6 项全 PASS"、note 说明 ARCH 文档正文交付在 returns/ARC-001/docs/（允许路径不含 docs/architecture），正式收编属 GOV/QA；变更面证据为**仅一个 schema 文件**，与 note 自述一致（文档正文未入库）。
- 关联：target_version 0.11.0-alpha.1 与组 38（GOV-003 的 VERSION）同值；本条是 contracts/ 目录在本片的首次出现，后续组 35/40 续用。
- 不确定点：`validate_dll_contract.py` 未出现在变更面（可能位于控制包 returns/ 区），其入库位置未证实。

### 组 34 | seq 1478 | f260b804 | 2026-09-02 | M（BLD-001）
- 做了什么：冻结 Windows 发布工具链：新增 `cmake/toolchain/`（+351，含 verify_toolchain.py）、`CMakePresets.json` 新增 114 行 preset（VS17 2022 x64、v143 14.44.35207、SDK 26100、CMake 3.31.12，另含 linux light preset）、`packaging/windows/`（2 文件 +46，含 .vsconfig 与 README）、`packaging/schemas/preset-contract.json`（+64）、`DEPENDENCIES.md`（+41/-7）。
- 落点：`cmake/toolchain/`、`CMakePresets.json`、`packaging/windows/`、`packaging/schemas/`、`DEPENDENCIES.md`。
- 内容性质：CI 或机器门（工具链校验器 + preset 合同）+ 文档或规格（DEPENDENCIES）。
- 意图证据：消息正文自述 requirements 版本号清单与 tests 判定 `verify_toolchain.py verdict=PASS`、"无 latest/MinGW/Ninja 正式路径"；变更面证据为工具链与 preset 面全新增。
- 关联：与组 22..30 的 MSVC 手工修复链形成"先打通、后冻结"的顺序；与组 41（BLD-002 唯一根构建图）同族。
- 不确定点：preset 与组 41 的根 CMake 图之间是否有校验绑定，未证实。

### 组 35 | seq 1479 | 2df77afe | 2026-09-02 | M（DATA-001）
- 做了什么：冻结类型化产物合同：新增 `contracts/data/artifact_manifest.schema.json`（+149）、`artifact_types.registry.json`（+42）、示例 `examples/frame_hips_manifest.example.json`（+25）、C ABI 头 `include/astrocs/contracts/artifact_abi_v1.h`（+91）与其 C 实现 `runtime/artifact_store/artifact_abi_v1.c`（+633）、校验器 `runtime/artifact_store/artifact_manifest_validator.py`（+286）、负例测试 `tests/artifact/test_manifest_schema_negative.py`（+151）。
- 落点：`contracts/data/`（3 文件）、`runtime/artifact_store/`（2 文件 +919）、`include/astrocs/contracts/`、`tests/artifact/`。
- 内容性质：文档或规格（schema）+ 生产代码（C 实现与 Python 校验器）+ 测试。
- 意图证据：消息正文自述 requirements（严格拒绝缺字段/NaN/重复 producer/未知 type；C ABI handle 无路径字符串）与 tests "14 PASS"；变更面证据为全新增 7 文件、零删除，schema+实现+负例三件套齐备。
- 关联：与组 40（DATA-002 三阶段产品交换合同）同族递进；其 `artifact_abi_v1.h` 与组 37（ABI-001 的 artifact_api_v1.h）为相邻但不同的 ABI 面。
- 不确定点：DATA-001 的 C 实现是否已接入生产构建目标（本条未改 CMakeLists），未证实。

### 组 36 | seq 1480 | b7b2dea7 | 2026-09-02 | L（GOV-002，结构模式：批量归档搬家）
- 做了什么：归档非当前工程文档：881 个文件改名搬家 + 3 个新增 + 1 个修改。控制文档侧把 `工程控制/**` 整体移入 `engineering/control/archive/2026-09-02_legacy_工程控制_v1.3-to-v6.1/`（854 项，含 V4/V5/V6/REAUDIT_V3/ACR_FOCUSED 各包与 evidence/P09..P13 任务证据树）；技术文档侧把 `docs/{release,history,refactor,review,phase2}` 等 27 项移入 `docs/archive/`；新增归档说明头（`README_ARCHIVED.md` 等，engineering/control 面 +35 行）、`docs/DOCUMENT_INDEX.yaml`（+414，唯一分类事实源）与机器门 `tools/doccheck/check_doc_index.py`（+184）；`.gitignore` +3/-1。
- 落点（按目录直方图归纳，不逐文件展开）：engineering/control 855 文件 +35/-0；docs/archive 27 文件 +0/-0；docs/DOCUMENT_INDEX.yaml 1 文件 +414/-0；tools/doccheck 1 文件 +184/-0；.gitignore 1 文件 +3/-1。操作构成：新增 3、修改 1、改名 881、删除 0、二进制 0、噪声区 0。
- 内容性质：结构整理（归档搬家）+ 文档或规格（索引与 ARCHIVED 头）+ CI 或机器门（doc_index 校验器）+ 控制包或台账（被移动的历史控制包）。
- 意图证据：消息正文自述 requirements（DOCUMENT_INDEX.yaml 唯一分类；历史控制文档→engineering/control/archive；技术文档→docs/archive；archive 带 ARCHIVED 头 + 替代链接；移动由 Git 追踪）与 tests 判定 `DOC_INDEX_PASS 10/10`；变更面证据完全支持——改名方向逐条可查（例：`工程控制/AUDIT_PACK.md` → `engineering/control/archive/2026-09-02_legacy_…/AUDIT_PACK.md`；`docs/history/v19/ARCHITECTURE.md` → `docs/archive/history/v19/ARCHITECTURE.md`），且 881 个改名全部为 0 行变更（纯移动）。
- 关联：直接吞并组 16（seq 1454 新建的 02_FINDINGS.csv，卡载改名行可证）；编号字段同时含 LOG-001，对应自述"补登 LOG-001 文档与 .gitignore 例外集成修正"；本片 `evidence/v6_1_rework/` 未被移动（dirhist 无 evidence 面）。
- 不确定点：卡片 truncated=True（885 文件仅记录前 885 项中的部分，nfile=885 与记录数一致但清单为截断集），故"854 + 27"的精确分桶以消息自述与 dirhist 聚合互证，逐文件清单未证实。

### 组 37 | seq 1481 | 76b85d45 | 2026-09-02 | M（ABI-001）
- 做了什么：定义模块 C ABI v1：新增 4 个纯 C 头 `include/astrocs/abi/` 下的 `status_codes.h`、`host_api_v1.h`、`module_api_v1.h`、`artifact_api_v1.h`（合计 +575/-0），要求 C11 与 C++17 双可编译、struct_size/abi_version、opaque handles、UTF-8 spans、禁 STL/模板/异常跨头。
- 落点：`include/astrocs/`（4 文件，全新增）。
- 内容性质：生产代码（接口头）+ 文档或规格（头内合同注释）。
- 意图证据：消息正文自述 requirements/tests（gcc -std=c11 + g++ -std=c++17 四头全过、`acs_status` 类 static_assert、禁带项扫描干净）与 note"前台补 3 处 C11 前向 typedef 机械修正"；变更面证据为四头全新增、无实现文件。
- 关联：与组 35（DATA-001 的 artifact_abi_v1.h/.c）同处 ABI 面但分属 contracts/ 与 abi/ 两个目录；`acs_handle`、`acs_span_u8` 形态在重叠区 seq 1431 的 `p3_session_run(acs_handle, acs_span_u8)` 已出现，属既有句柄风格延续。
- 不确定点：卡载比对显示"点名与变更面交集：无交集"（消息以目录形式点名 include/astrocs/abi/），实际四文件确在 include/astrocs 聚合下；其子目录是否严格为 abi/ 未逐条证实。

### 组 38 | seq 1482 | 39e77317 | 2026-09-02 | M（GOV-003）
- 做了什么：统一 Alpha 版本事实源：`VERSION` 改为 `0.11.0-alpha.1`（+1/-1），新增 `docs/governance/VERSION_NAMESPACES.md`（+99，5 个版本命名空间），新增机器门 `tools/doccheck/check_version_namespaces.py`（+318），`REVIEW.md` 同步（+2/-1）。
- 落点：`VERSION`、`REVIEW.md`、`docs/governance/`、`tools/doccheck/`。
- 内容性质：CI 或机器门（版本命名空间校验器）+ 文档或规格 + 数据或产物（VERSION 单值）。
- 意图证据：消息正文自述 requirements（VERSION 单源、5 命名空间、生成链唯一源、禁手抄）与 tests 判定 `VERSION_NAMESPACES_PASS`；变更面证据为 VERSION 单行改写 + 校验器新增。
- 关联：与组 33（ARC-001 schema 内 target_version 同值）互证；与组 15（REL-001 的 alpha.2 发布包）构成版本线切换点（V6.1 期 alpha.2 → V7 期 0.11.0-alpha.1）。
- 不确定点：5 个命名空间的完整清单未证实（卡片未展开文档标题）。

### 组 39 | seq 1483 | 5d38ebd7 | 2026-09-02 | M（RT-001）
- 做了什么：实现类型化运行图：新增 `runtime/pipeline/typed_dag.py`（+624）、`typed_dag.schema.json`（+65）、`typed_dag_contract.h`（+122）、`module_ports.registry.json`（+314）、fixture `fixtures/phase2_typed_dag.json`（+139），以及测试 `tests/pipeline/test_typed_dag_plan.py`（+171）与 `tests/runtime/test_typed_dag_negative.py`（+356）。
- 落点：`runtime/pipeline/`（5 文件 +1264）、`tests/pipeline/`（2 文件 +171）、`tests/runtime/`（2 文件 +356）。
- 内容性质：生产代码（Python 编译器 + C 合同头）+ 测试 + 数据或产物（fixture/registry）。
- 意图证据：消息正文自述 requirements（typed node id/module/operation/ports/config/resource；校验 phase scope/type/unit/shape/必需端口/无环/单 operation）与 tests "pytest 24/24"（负例点名 7 节点同 session、隐式路径、跨 Phase、循环、重复 producer、type-unit mismatch）；变更面证据为全新增 9 文件、正例与负例测试分置两目录。
- 关联：与组 35（DATA-001 产物合同）互为上下游；与重叠区 seq 1425（P3-006 的 IR 5 节点链 properties→wcs→resample2→writer→verify）构成"生产 pipeline 已跑通 → 类型化 DAG 合同化"的推进关系。
- 不确定点：typed_dag 是否已接入 CLI 入口（本条未改 cli/），未证实。

### 组 40 | seq 1484 | c1ee791a | 2026-09-02 | M（DATA-002）
- 做了什么：定义三阶段产品交换合同：新增 `contracts/data/phase_product_exchange.schema.json` 与 `phase_product_exchange_matrix.json`、4 份示例（external_fixture_hips、phase1_product_v1、phase2_mosaic_v1、phase3_planar_fits_v1）、规格文档 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`（+195）、校验器 `runtime/artifact_store/phase_product_exchange_validator.py`（+303）、测试 `tests/artifact/test_phase_product_exchange.py`（+306）。
- 落点：`contracts/data/`（6 文件 +560）、`docs/interfaces/`（1 文件）、`runtime/artifact_store/`（1 文件）、`tests/artifact/`（1 文件）。
- 内容性质：文档或规格（合同 + 接口文档）+ 生产代码（Python 校验器）+ 测试 + 数据或产物（示例）。
- 意图证据：消息正文自述 requirements（跨 Phase 仅磁盘交换；缺 manifest/hash/schema/units 拒绝；无隐式 artifact name binding）与 tests "pytest 32/32"；变更面证据为 9 文件全新增。
- 关联：与组 35（DATA-001）同目录同族递进；与组 39（RT-001）共同构成 V7 期的合同 + 运行图两翼。
- 不确定点：矩阵中三阶段兼容判定的具体取值未证实。

### 组 41 | seq 1485 | 3e7f7581 | 2026-09-03 | S（BLD-002）
- 做了什么：建立唯一根 CMake 构建图：根 `CMakeLists.txt`（+9/-1）确立唯一 `project(astrocs)` 与唯一 `add_executable(astrocs)` 显式源列表、无 GLOB/install；`cli/CMakeLists.txt`（+10/-2）与 `lib/phase2/CMakeLists.txt`（+15/-0）降级为 compatibility 声明并把 phase2 工具置为 EXCLUDE_FROM_ALL。
- 落点：`CMakeLists.txt`、`cli/CMakeLists.txt`、`lib/phase2/CMakeLists.txt`。
- 内容性质：CI 或机器门（构建图结构）。
- 意图证据：消息正文自述 requirements（含依据 tasks/02_ABI_BUILD_CLI_TASKS.md）与 tests "cmake configure root/cli/phase2 三处 EXIT=0（logs/02,03,04）"；变更面证据为三处构建文件小改，无日志入库（logs/ 目录未出现在变更面）。
- 关联：与组 34（BLD-001 工具链冻结）同族；是本片末条，也是 V7 族中唯一直接改根构建面的提交。
- 不确定点：自述点名的 `tasks/02_ABI_BUILD_CLI_TASKS.md` 与 logs/02..04 均不在变更面（应在控制包 returns/ 区），未证实。
