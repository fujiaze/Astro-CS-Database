# 逐条详析 （证据层过程件）

> 本文件是 设计大纲/reports/history/slices/H-S31.md 的逐条正文；最终交付只保留两份大报告，逐条正文下沉到证据层（条目文本零改动，仅移动）。
> 由 _tools/11_split_slice_reports.py 从入库基线 a3a343a4 重切，日期 2026-09-15；条目组 44 个；报告树保留段（主题归纳/重叠区核对/遗留缺口）见原文件。

## 主责条目（55 条，合并为 44 组，列全 seq 与 sha8）


### seq 1651 | 71e1c6aa | 2026-09-08 | S（无编号；chore(state)）

- 做了什么：把 `evidence/v8_1_ci_control/COMMIT_LEDGER.jsonl` 一次追加 2 行，补登 seq 1649（d3235bdd）与 seq 1650（d277e0ec）两条提交自身，并按消息自述"含 HEAD 预登"以终结 1641–1650 的"提交一次→补登一次"追赶循环。
- 落点：evidence/v8_1_ci_control/COMMIT_LEDGER.jsonl（唯一文件，+2 -0）。
- 内容性质：控制包或台账。
- 意图证据：消息自述"破除豁免追赶循环"；变更面证据吻合——纯追加、零删除，代码面 0 / 包与证据面 1（紧凑卡字段）。
- 关联：直接承接重叠区 seq 1641–1650 同族补登链（见片尾二）；与 seq 1655（d8832fc9）、1656（5bd9ed95）为同族三段；机制层面的终局处理在 seq 1668（b05288b8）。
- 不确定点：预登是否被机器门接受，本片内无复验证据（规则换轨发生在 seq 1668）——未证实。

### seq 1652–1653 | 3cbd5fd7, 0dbaa354 | 2026-09-08 | M + S（WCS-001 / P1-WCS-DOC）

- 做了什么：1652 冻结 plate_solve 模块合同——新增 `docs/algorithms/PLATESOLVE.md`（+117）、`lib/plate_solve/module.yaml`（+148，本提交唯一新增文件）、`lib/plate_solve/README.md`（+137/-174 重写）、`lib/plate_solve/memory.md`（+26），改 `docs/contracts/DATA_SEMANTICS.md`（+69）、`docs/contracts/PUBLIC_API.md`（+106）、`docs/contracts/INDEX.yaml`（+16）、`docs/modules/registry/astrocs.phase1.wcs-platesolve.md`（+45/-14）、`docs/modules/plate_solve.md`（+28/-10）、trace 两文件（CSV 1 行、JSON 14 行）、DOCUMENT_INDEX（+6/-2）、ledger（+1）。1653 把该任务在台账上改判 CLOSED。
- 落点（聚类）：docs/contracts（3）、docs/algorithms（1）、docs/modules（2）、docs/traceability（2）、docs/DOCUMENT_INDEX.yaml（1）、lib/plate_solve（3）、evidence/v8_1_ci_control（1652 计 COMMIT_LEDGER.jsonl +1；1653 计 COMMIT_LEDGER.jsonl +1 / STATE_RECONCILIATION.csv +1/-1 / TASK_STATE.json +8/-8）。
- 内容性质：文档或规格（合同冻结）+ 控制包或台账（状态同步）。
- 意图证据：消息自述点名四份合同 ID（SCI-WCS-001 / ALG-WCS-001 / DATA-P1-WCS / API-WCS-001）与控制包序号"11/140, wave W1"；变更面证据一一对应（算法面 +129/-0 与 +117 的新文档、API 面 +106、数据语义 +69）。台账侧给出机器可核三处（1653）：STATE_RECONCILIATION.csv 行由 `NOT_STARTED / not_dispatched_no_evidence` 改为带 `commit:3cbd5fd7` 的证据串；TASK_STATE.json 中 status 由 NOT_STARTED 改 CLOSED、source_of_status 改 `fg_verified_commit`、closed_commit 写 3cbd5fd7；聚合计数 CLOSED 61→62、NOT_STARTED 115→114。
- 关联：1653 是 1652 的成对同步；1652 为本片"控制包 11/140 起的 W1 文档冻结波"起点（后见 12/140=1659、13/140=1665、14/140=1676、15/140=1684、16/140=1692、17/140=1698、18/140=1704）；1652 的 trace 行改写与 seq 1679（b80404eb）第 5 根因（DATA-COV-001 漏登记）同族。
- 不确定点：TASK_STATE 的 note 字段自述"SCI-WCS-001 = 共享 ASTROMETRY.md 零改动""DISP-WCS-001..006（核心 = 001 CD 退化静默坍缩）""astrocs_p1_wcs.dll / CMake 集成归 P1-WCS-IMPL"，本片内无对应实现或整改提交——DISP-WCS 系列的处置状态未证实。

### seq 1654–1656 | 9276f383, d8832fc9, 5bd9ed95 | 2026-09-08 | S ×3（CI 工具修复 + 台账 rebase 漂移恢复）

- 做了什么：1654 修 `tools/quality/ci_windows_driver.py` 的 dumpbin 导出行解析正则，补 hint 列使按 ordinal / hint / RVA / name 四段（自述"三列"指 ordinal·RVA·name 主体）切分，消 exported_abi 误判 0 符号；1655 在 ledger 补登该修复，一次 +1/-15——清出 15 条旧行；1656 再做全量对齐 +18/-1，补回 18 行并删除 sha 为 43b259ae 的坏行。
- 落点：tools/quality（1 文件 +4/-1）；evidence/v8_1_ci_control/COMMIT_LEDGER.jsonl（1655：+1/-15；1656：+18/-1）。
- 内容性质：CI 或机器门（1654）+ 控制包或台账（1655、1656）。
- 意图证据：1654 消息自述与变更面双向吻合（唯一改动文件即 Windows CI 驱动）；"rebase 残留坏 SHA"有直接变更面证据——同一 subject（dumpbin 正则修复）在 1655 挂 sha 43b259ae、在 1656 改挂真实 sha 9276f383，即历史映射被重导出而非新增工作；1655 删 15 行同时把 date 由整点值改为真实时刻，与 1669 的"date 对齐 committer date"是同一问题的两次局部处理。
- 关联：1654 与 seq 1679 / 1680（b80404eb、b95f147a）同属 Windows 机器门误报处置族；1655–1656 与 seq 1651（71e1c6aa）、1668（b05288b8）、1669（4e91ca2e）构成"ledger 债务"五连。
- 不确定点：dumpbin 修复后的实际符号计数与 CI 复跑结果本片内无证据——未证实。

### seq 1657 | 5d1fca08 | 2026-09-08 | S（CPU-003）

- 做了什么：把 `lib/backend_host/profile_gen_v2.cpp` 中 profile_gen_v2 的 oracle 相对容差常量 kOracleRelTol 由 2e-3 收紧为 2e-4，按自述对齐 CPU_003 §7 冻结值并收敛为单一出处防漂移；同提 ledger +1。
- 落点：lib/backend_host（1 文件；全卡每文件 +5/-1）、evidence/v8_1_ci_control（COMMIT_LEDGER.jsonl +1）。
- 内容性质：生产代码（容差常量）+ 控制包或台账。
- 意图证据：消息自述点名任务号 CPU-003、条款 §7、轮次"bug 狩猎 R4"并自报"CPU-003 测试复验 PASS"；变更面证据：代码改动仅 5 行、单常量层面，与"容差对齐 + 单一出处"规模吻合。卡片无测试文件同提，"复验 PASS"只有消息自述支撑。
- 关联：与 seq 1658（f1cb487c，R3）相邻但不同编号族；容差收紧的后果由 seq 1681（dd89e530）显式引用（"容差收紧后真实 FAIL 显形"）。
- 不确定点：CPU_003 §7 的冻结条款原文不在本片变更面，字面一致性未证实。

### seq 1658 | f1cb487c | 2026-09-08 | L（无编号；按文件集合与目录直方图归纳，不逐文件展开）

- 做了什么：一轮 P0 外部输入面加固四连（自述）：SIP order 限定 [0,5] 硬校验、XISF geometry 与 64MB XML 长度上限、灰度分支 size_t 索引、AIO 导出面 25 个 AIO_EXPORT 入口加异常屏障与分配上限。规模 20 文件 / +1942 / -170 / 新增 5 / 二进制 0 / 噪声区 0。
- 落点（目录直方图）：lib/astro_image_io 4 文件 +626/-91（aio_api.cpp +263/-83、aio_xisf.cpp +64/-6、aio_fits.cpp +42/-1、新增 tests/test_p0_io_hardening.cpp +231）；lib/healpix_db 3 文件 +272/-4（hp_drizzle_api.cpp +72/-4、新增 tests/mini_sip1000.cpp +32、新增 tests/p0_sip_order_guard_test.cpp +152）；lib/phase2 3 文件 +327/-6（README/memory/module.yaml，module.yaml 为新增）；docs/contracts 3 文件 +253/-0；docs/algorithms 1 文件 +348/-0（新增 PHASE2_COVERAGE.md +311）；docs/modules 2 文件 +91/-49；docs/traceability 2 文件 +16/-16；docs/DOCUMENT_INDEX.yaml +8/-4；evidence/v8_1_ci_control +1。
- 内容性质：生产代码 + 测试 + 文档或规格（同一提交内三类并存）。
- 意图证据：消息自述"恶意 fixture 30 例 + ASAN 对比验证 / bug 狩猎 R3"；变更面证据两条——(a) 3 个新增测试文件与加固代码同落点，属"带验证的加固"；(b) 全卡 hunk 头显示被触及的是 aio_read / aio_read_fits / aio_read_xisf / aio_read_header_only / aio_read_metadata / aio_write_fits / aio_get_* 族与 aio_is_fits / aio_is_xisf 等导出入口，与"25 入口异常屏障"自述对应。另有越权夹带的变更面证据：PHASE2_COVERAGE.md（+311 新增）、DATA_SEMANTICS（+85 新增节）、PUBLIC_API（+112 新增节）、registry/astrocs.phase2.coverage.md（+67/-30）、trace 两文件与 lib/phase2 三件套全在本提交内出现，属另一任务族（P2-COV-DOC）的文档面，seq 1659 的消息自述明确指其为"staged 泄漏"。
- 关联：与 1659（22fe677a）是同一被夹带任务的两阶段（初版产物 → 验收完成态）；与 seq 1661（b9d63505，SIP 漏改面补修）、seq 1688（1415df04，AIO 加固二）构成输入面加固链。
- 不确定点：一个 L 级提交同时含 IO 安全加固与 COV 合同文档，消息自述未提及后者，两类内容无声明级关联；"30 例恶意 fixture""ASAN 对比"仅见于消息自述，卡片无通过证据；lib/phase2 面 +327 属模块页三件套而非源码。

### seq 1659–1660 | 22fe677a, d14c1404 | 2026-09-08 | M + S（COV-001 / P2-COV-DOC）

- 做了什么：1659（8 文件 +145/-128，全为修改、零新增）把 phase2 coverage 子模块合同改到验收完成态——`docs/algorithms/PHASE2_COVERAGE.md` +96/-85、`docs/contracts/DATA_SEMANTICS.md` +12/-11、`docs/contracts/PUBLIC_API.md` +21/-19、`docs/modules/registry/astrocs.phase2.coverage.md` +4/-4、`lib/phase2` 三件套（README +9/-7 / memory +1/-1 / module.yaml +1/-1）、ledger +1。1660（3 文件 +10/-9）同步 CLOSED。
- 落点：docs/algorithms（1）、docs/contracts（2）、docs/modules（1）、lib/phase2（3）、evidence/v8_1_ci_control（1659 计 1、1660 计 3：COMMIT_LEDGER.jsonl / STATE_RECONCILIATION.csv / TASK_STATE.json）。
- 内容性质：文档或规格 + 控制包或台账。
- 意图证据：消息自述罕见地给出过程事实——"f1cb487c 曾夹带初版产物（staged 泄漏，处置 a：保持现状台账注明），本 commit 为验收完成态"；变更面证据与之吻合：8 文件全为修改、净 +145/-128 的收敛型改动（有删除），正是"就地校正"而非"新建"。1660 台账证据：CLOSED 62→63、NOT_STARTED 114→113，source_of_status = fg_verified_commit，closed_commit = 22fe677a。
- 关联：1658 → 1659 → 1660 同任务三段；seq 1679（b80404eb）第 1、5 根因再次处理 COV 文档引入的注释违规与 DATA-COV-001 登记，属同族滞后清账。
- 不确定点："处置 a：保持现状"是决策口径，其授权来源（负责人裁决或控制包条款）在本片变更面不可见——未证实。

### seq 1661 | b9d63505 | 2026-09-08 | M（p1-batchC）

- 做了什么：SIP 漏改面补修三连——`wcs_transform.cpp/.h`（+15/-0、+4/-0）加 [0,5] 硬校验；`pc_api.cpp`（+62/-0）为 6 个入口加异常屏障并约定返回码 -4；`lib/backend_host/backend_loader.cpp`（+17/-1）把 required_features_bits 不匹配改为硬失败拒载；新增两枚测试 test_p0_pc_api_exception_barrier.cpp（+143）、test_p0_sip_order_guard.cpp（+123），改 `tests/backend/test_abi_loader.py`（+37）与 photometric_calib.h（+8/-4）；ledger +1。
- 落点：lib/photometric_calib（6 文件 +384/-4）、lib/backend_host（1）、tests/backend（1）、evidence/v8_1_ci_control（1）。
- 内容性质：生产代码 + 测试 + 控制包或台账。
- 意图证据：消息自述"bug 狩猎 R5 / ASAN 新旧对照实锤 / 恶意 fixture 13 例"；变更面证据吻合——两枚新增测试文件体量（+143 / +123）与"入口级负例"形态一致，loader 侧唯一改动文件与"拒载"自述对应，且 Python ABI 测试同步扩 (+37) 支持 loader 行为改变。
- 关联：与 1658 是同一加固主题的补修；与 seq 1662 相邻（batchC 先于 batchB 落地），与 seq 1667（batchA）同为 bug 狩猎产物——批次字母按发现轮次而非落地顺序，属结构模式；photometric_calib 侧在本片另见 seq 1690（photometry_apply）。
- 不确定点：返回码 -4 是否与已冻结的 PC/PHOT 错误码表一致，本片无合同面变更证据——未证实。

### seq 1662 | e4576ed6 | 2026-09-08 | M（p1-batchB）

- 做了什么：orchestrator 三处 P1（自述）——HiPS 输出目录树改 remove_all 原子清理、SIGINT handler 内只做 atomic store 以消死锁、JSONL 事件写出口统一转义。落点 `lib/orchestrator/cpp` 下 7 文件（orchestrator.cpp +47/-11、cli_command.cpp +17/-9、main.cpp +15/-5、cli_command.h +9/-0、orchestrator.h +9/-5、Makefile +31/-2）与新增测试 test_p1_batchB_fixes.cpp（+200）；ledger +1。
- 落点：lib/orchestrator（7 文件 +361/-33）、evidence/v8_1_ci_control（1）。
- 内容性质：生产代码 + 测试 + 控制包或台账。
- 意图证据：消息自述"18 项单测 + stash 基线对照"；变更面证据：新增测试单文件 +200 与被改的 4 个源文件一一对应，Makefile +31/-2 属测试注册面，佐证新测试确进构建。
- 关联：与 seq 1678（be23e65d）、1690（1efef060）同模块（orchestrator.cpp 在本片被三次触及）；与 1661 为相邻批次对。
- 不确定点："stash 基线对照"属工作区操作，仓库面无痕——未证实。

### seq 1663 | d699d8ca | 2026-09-08 | S（quality-gates）

- 做了什么：P1 验收工具链"真实化"四项——contract_graph 双向断言（`tools/check_contract_graph.py` +9/-2）、API 签名真实比对（`check_api_contracts.py` +51/-8，自述 422 符号）、注释全量扫描（`check_comments.py` +5/-4，自述 507 处）、聚合门豁免条件化（`check_full_integration.py` +30/-5）；ledger +1。
- 落点：tools/quality（3 文件 +87/-17）、tools/check_contract_graph.py（1）、evidence/v8_1_ci_control（1）。
- 内容性质：CI 或机器门 + 控制包或台账。
- 意图证据：消息自述的关键约束是"修复后暴露的真实违反清单已交 DOC 批次，未放宽未代写"；变更面证据与该约束一致——5 文件全部是检查器自身，无任何被检文档随提；符号/注释量级（422 / 507）在 seq 1679 正文中以同一口径复现（"422 rows 比对"），构成跨条一致性证据。
- 关联：本条是 seq 1679 / 1680 / 1687（b80404eb、b95f147a、dc391772）大量"检查器缺陷"处置的上游；与 seq 1701（6c5f5fc9）构成质量门收口线两端。
- 不确定点：被"暴露的真实违反清单"无落点文件（本片无报告或台账同提），交 DOC 批次的书面证据不可见——未证实。

### seq 1664 | cf5f44e5 | 2026-09-08 | M（p1-batchD）

- 做了什么：`lib/phase1_session/p1_session.cpp`（+112/-14）三处修复——四阶段 manifest 的引用写穿（消除"恒 running"）、cosmetic 的 number|bool 类型甄别、C 边界异常屏障；新增 `test_p1_session_manifest.cpp`（+198）并改 `tests/unit/CMakeLists.txt`（+10）、`p1_ir_facade_test.cpp`（+3/-1）；ledger +1。
- 落点：lib/phase1_session（2 文件 +342/-14）、tests/unit（2）、evidence/v8_1_ci_control（1）。
- 内容性质：生产代码 + 测试 + 控制包或台账。
- 意图证据：消息自述给出可核症状"红复现 rc=134 → 绿 23 checks + e2e rc=0"（轮次 R6）；变更面证据：测试 +198 与生产 +112/-14 同提，规模匹配。
- 关联：新增的 test_p1_session_manifest.cpp 在 seq 1682（5c43dc04）、1686（4b6eb26d）被连续两轮 Windows 化改造，是本片最长的跨条测试演进线。
- 不确定点：manifest 状态口径改变是否触及 contracts/schemas 下的已冻结 schema——本片该目录零变更，未证实。

### seq 1665–1666 | c0b464cf, a3d5f2dc | 2026-09-08 | M + S（HIPS-001 / P2-HIPS-DOC）

- 做了什么：1665（13 文件 +1396/-50，新增 5）冻结 hips_writer 模块合同——新增 `docs/algorithms/PHASE2_MOSAIC_WRITE.md`（+469）、`docs/modules/hips_p2.md`（+56）、`lib/hips_p2` 三件套（README +159 / memory +72 / module.yaml +113），改 DATA_SEMANTICS（+145）、contracts/INDEX.yaml（+61/-13）、PUBLIC_API（+99）、registry/astrocs.phase2.write.md（+93/-19）、trace 两文件（+16/-16）、DOCUMENT_INDEX（+8/-2）、ledger +1；自述含"批次 E 暴露 13 条半边反向边补全，追加式"。1666（3 文件 +10/-9）同步 CLOSED。
- 落点：docs/contracts（3）、docs/algorithms（1）、docs/modules（2）、docs/traceability（2）、DOCUMENT_INDEX、lib/hips_p2（3）、evidence（1665 计 1、1666 计 3）。
- 内容性质：文档或规格 + 控制包或台账。
- 意图证据：消息自述点名 ALG-P2-HIPS-001..004 / DATA-P2-HIPS / API-P2-HIPS-001 与控制包"13/140"；变更面证据吻合——合同三件齐备且算法面为纯新增（+503/-0 级），与"冻结/追加式"表述一致。1666 台账证据：CLOSED 63→64、NOT_STARTED 113→112、closed_commit = c0b464cf。
- 关联：1665 的"13 条反向边"与 seq 1663（双向断言门）互为因果——门先收紧、文档再补边；1666 与本组同型（八对"冻结 + 同步"）。
- 不确定点：卡片未逐条列出 13 条反向边的具体端点 ID，无法在本片复核其是否全部补全——未证实。

### seq 1667 | 8d656bbe | 2026-09-08 | M（p1-batchA，并发句柄域 P1×5）

- 做了什么：五个并发/所有权缺陷修复并各配新测试——dpsf NaN-safe 全序（`dpsf_psf.cpp` +35/-5 + 新增 test_dpsf_nan_sort.cpp +125）；gaia block_cache 以 BcLock 串行化与 spec_collector realloc 单一所有权（`gaia_client.c` +96/-28 + 新增 test_gaia_race.c +226、test_spec_collector_ownership.c +135）；ipv 句柄互斥与 last_inliers_ 在 6 入口 / 15 失败路径重置（`ipv_entry.cpp` +26/-2、`ipv_solver.cpp` +30 + 新增 test_last_inlier_reset.cpp +136）；ledger +1。
- 落点：lib/gaia_xpsd_client（3 文件 +507/-29）、lib/plate_solve（3 文件 +211/-2）、lib/dynamic_psf（2 文件 +172/-5）、evidence/v8_1_ci_control（1）。
- 内容性质：生产代码 + 测试（4 枚新增）+ 控制包或台账。
- 意图证据：消息自述给出量化前后态"TSAN 4 race→0""ASAN double-free→净"（轮次 R4）；变更面证据：三簇生产改动各自配一枚专用并发/所有权测试（新增 4 文件、测试合计 +623 行），与该量化形态吻合。
- 关联：与 seq 1678（be23e65d）同处 dpsf→ipv 数据路径（1667 改句柄与排序，1678 改坐标契约）；与 1662 同属并发/资源类批次；1667 落在 1665/1666 的 HIPS 文档冻结之后，说明 DOC 波与安全波交错推进（时间上仅相隔 3 分钟，见 1666 07:50:03 / 1667 07:52:56）。
- 不确定点：TSAN/ASAN 复跑日志不在仓库面（本任务禁读 run/，且无产物文件同提），"→0 / →净"仅消息自述——未证实。

### seq 1668 | b05288b8 | 2026-09-08 | S（governance；本片结构性断点）

- 做了什么：把 COMMIT_LEDGER 机器门检查从"missing 追赶 + HEAD 自参照豁免"改为三条非递归不变量——条目 sha 必须存在于 git log、四字段 schema（sha/subject/task_id/date）齐全、subject 必须与 git log 中该 sha 的实际 subject 一致；同提交修正 8 条历史错位 ledger 行（全卡摘录显示 7 条为 subject 内含 "|" 致分隔工具截断、1 条错登，按 git 重导出补全）。
- 落点：`ci/reconcile_state.py`（+41/-30）、`evidence/v8_1_ci_control/COMMIT_LEDGER.jsonl`（+8/-8）。
- 内容性质：CI 或机器门（治理规则）+ 控制包或台账（数据重导出）。
- 意图证据：消息自述"覆盖事实 = git log，ledger 降级任务映射登记（三不变量：存在性/schema/防改史）；负例自证 5/5"；变更面证据强——全卡摘录可见旧判定式（missing / missing_head_only / rev-list --count main / n_main）与新判定式（git_history[sha] = subj、bad_subject、aligned_with_git_history）成对替换，属**语义换轨**而非修补；ledger 8 行同改为 subject 补全形态，与"重导出"自述吻合。
- 关联：本条终止了 seq 1641–1651 / 1655–1656 的"补登潮"——此后本片再无"单文件 +1 行的台账补登"提交（唯一同型的 seq 1655/1656 都在本条之前）；与 seq 1669（4e91ca2e）为治理面一对（先换规则、再洗数据）；本条之后的 CLOSED 同步提交形态也随之收窄（见 seq 1705）。
- 不确定点："负例自证 5/5"无对应测试文件同提（本提交仅改 1 个 py + 1 个 jsonl）——未证实。

### seq 1669 | 4e91ca2e | 2026-09-08 | S（V8-CI-010，governance 数据 repair）

- 做了什么：一次性修台账数据质量——`COMMIT_LEDGER.jsonl` +52/-53（净减 1 行以去重 9ae1f1d4；其余为整行 date 重写）与 `TASK_STATE.json` +1/-1。卡片可核的重写形态包括：sha 6555e98e / 8ba7a609 / fd350e5f / 871092f8 / 62226ec7 / 8c0762ca / 8cbeef6d 等条目的 date 由整点或错误时刻改为 committer 实时刻；并把两条 merge 提交（fe4ca383 "Merge branch 'main'"、58cbabf3 "Merge remote-tracking branch 'origin/main'"）纳入登记。
- 落点：evidence/v8_1_ci_control（2 文件 +53/-54）。
- 内容性质：控制包或台账（纯数据质量修复）。
- 意图证据：消息自述点名三类处置（9ae1f1d4 去重 / V8-CI-010 坏 SHA 修正，closed_commit 互证统一 / date 全量对齐 committer date，52 行含 19 未来时间）与轮次 R7-D，并声明"登记规范四字段 git 单一来源固化"；变更面证据与"date 对齐"直接吻合（同一 sha 仅 date 变化的成对 -/+ 行），与"去重"吻合（+52/-53 净减 1 行）。
- 关联：与 seq 1668 同为治理面；与 seq 1655/1656 同题（坏 SHA / rebase 残留）但口径升级——1655/1656 手工补删、1669 声明单一来源规则；被登记的 merge 提交（fe4ca383、58cbabf3）不在本片区段内，属对上游合并漂移的补记。
- 不确定点：V8-CI-010 的 closed_commit 互证具体对应哪两个值卡片未展开——未证实。

### seq 1670 | 0ef34a5e | 2026-09-08 | S（CI dirty 豁免显式登记）

- 做了什么：把三类运行产物从"工作区漂移"改判为"运行产物"并按检查项显式登记——`ci/checks.json` +14/-2（CLI cancel 合同的 write_run_manifest 输出/仓库根、资源监控三件套、benchmark MEASUREMENTS.csv）、`ci/checks.schema.json` +12/-0（新增键 dirty_ignore_exact / dirty_ignore_prefixes）、`ci/run.py` +7/-0（消费新键）、`tests/cli/test_monitor_events.py` +1/-1 与 `tests/cli/test_phase3_inprocess.py` +3/-3。
- 落点：ci/（3 文件）、tests/cli（2 文件）。代码面 5 个文件、包与证据面 0。
- 内容性质：CI 或机器门 + 测试。
- 意图证据：消息自述"UT-CLI/UT-BACKEND 按 R7 日志逐项登记""严格 schema 校验通过"；变更面证据吻合——schema 新增键与消费方 run.py 同提，属真门改造而非注水豁免（无删除既有条目）。
- 关联：与 AGENTS.md 目录规范中"CI 资源产物落 run/resource/ 并由 ci/checks.json 的 dirty_ignore 引用"的现行口径一致，本条即该口径的建立点；与 seq 1671（MON-002 事件接线）互为前后件。
- 不确定点：豁免是否仅覆盖运行产物、有无被用于掩盖真实脏工作区——本片无 R7 日志面证据，未证实。

### seq 1671 | d3eb86fd | 2026-09-08 | S（CLI-002 / CLI-007 / MON-002）

- 做了什么：CLI 事件与产物接线收尾——`emit_resource_summary` / `emit_backend_event` 接入三个 phase 的 run 路径（自述 CLI-002 漏接）；phase3 artifacts 改按 manifest 内容收集（旧按 hips 节点过滤恒空）并补 phase3_output role（CLI-007 冻结口径）；phase2 session summary 改按内容扫描 n_obs 节点 manifest（旧按 res 过滤恒 0）；resume 预检恢复并按 config_path 收窄防跨 run 误判；`ResourceRecorder::record_count` 供 raw_n。
- 落点：`cli/commands.cpp`（+100/-9）、`cli/resource_recorder.h`（+6/-0）。
- 内容性质：生产代码。
- 意图证据：消息自述逐条列出四处缺陷与三个任务号；变更面证据：改动集中在单一大文件（+100/-9）且伴随头文件新增访问器，与"接线收尾"的形态吻合，但**无测试同提**，验收只能靠后续 CI 轮次间接佐证。
- 关联：与 seq 1670（dirty 豁免）同域（资源监控产物面，MON-002 一线）；与 seq 1672、1673、1675 同为 CLI-002 / 测试可运行性收尾群（1670→1675 五条集中在 08:40 前后 1 分钟内落盘，属同一轮清理）。
- 不确定点："恒空 / 恒 0"的旧缺陷是否有历史测试基线记录，本片无证据——未证实。

### seq 1672 | a6c39cc1 | 2026-09-08 | S（CLI-002）

- 做了什么：`cli/parser.cpp`（+7/-1）把"未知命令"判定前移——非 dash token 拼接终结（EOF / dash）后仍不命中规则表即报 unknown command（返回码 2），不再落入 flag 循环被 unknown flag 抢先；自述点名 CLI-002 移除的 graph / run 与 --preset / --phases 组合，并声明 1-token 可续拼（如 config）不受影响。
- 落点：cli/parser.cpp（1 文件）。
- 内容性质：生产代码（CLI 判定顺序）。
- 意图证据：消息自述给出精确契约（返回码 2 与不受影响面）；变更面证据：7 行改动与"判定前移"规模吻合，无配套测试。
- 关联：与 seq 1671、1673 同编号族 CLI-002；其回归验收在 seq 1673/1675（include 补齐）之后才在 CI 中可见（1673 自述"掩盖 CLI-002 漏检面"）。
- 不确定点：返回码 2 是否与已冻结 CLI 退出码合同一致，本片无合同面变更证据——未证实。

### seq 1673 | 88328f9b | 2026-09-08 | S（CLI-002 测试面）

- 做了什么：`tests/cli/test_phase123_pipeline.py` 与 `tests/cli/test_phase2_inprocess.py` 的 fixture 编译命令补 -Ilib/common/healpix（2 文件 +4/-2）；自述"include 缺失致 setUpClass ERROR 全套全空转，掩盖 CLI-002 漏检面"。
- 落点：tests/cli（2 文件）。
- 内容性质：测试（可运行性修复）。
- 意图证据：消息自述直指症状与后果链；变更面证据吻合——每文件各 2 行、纯编译包含路径，无断言改动。
- 关联：与 seq 1675（14a84cc5）同根因同修法（跨 2 条复现），与 1672 为同一验收链。
- 不确定点：受影响套件的实跑结果无仓库面产物——未证实。

### seq 1674 | 4b3fa3dc | 2026-09-08 | S（P3-001）

- 做了什么：`tests/cli/test_phase3_inprocess.py`（+2/-2）把 phase3 test_03 的拒绝域断言对齐 P3-001 冻结科学：旧用例用 dec=2.0（属已废止的 |dec|<5° 显式拒），改为 dec=88.0（在 abs(dec)<=85° 单条件下仍为近极拒），并把 max_tiles 64→16 以适配 CI 4 核预算。
- 落点：tests/cli（1 文件）。
- 内容性质：测试（科学口径对齐 + 资源预算适配）。
- 意图证据：消息自述给出谱系证据——"|dec|<5° 显式拒（bcc6cb7b）已被 9e8ba103 冻结为 abs(dec)<=85° 单条件"，即引用两个历史 sha 作冻结依据；变更面证据吻合：仅 2 行参数变更、自述"保持近极拒验收语义不变"。注：bcc6cb7b 的完整 subject 亦在 seq 1668（b05288b8）的 ledger 重导出中被补全，两者可交叉印证（CLI-006 phase3 run 显式拒清单）。
- 关联：与 1672/1673 同属 CLI 测试面收口；与 seq 1670（同文件 test_phase3_inprocess.py 被改）为同一测试文件的连续两次触及。
- 不确定点：9e8ba103 的冻结文本不在本片区段，条款原文一致性未证实（该片断属上游 S29/S30 区段）。

### seq 1675 | 14a84cc5 | 2026-09-08 | S（测试构建）

- 做了什么：`tests/cli/test_monitor_events.py` 的 fixture 编译补 -Ilib/common/healpix（1 文件 +2/-1）；自述与 seq 1673 同根因（R5/R6/R7 三条 setUpClass ERROR）。
- 落点：tests/cli（1 文件）。
- 内容性质：测试。
- 意图证据：消息自述"同根因"；变更面证据：单文件 2 行 include 路径，与 1673 完全同型。
- 关联：1673 的续作；与本条之后 seq 1679 第 6 根因（UT-BACKEND 缺 libgsl-dev 致 setUpClass ERROR）同属"套件空转"根因族。
- 不确定点：无。

### seq 1676–1677 | 84c6715a, 813f3004 | 2026-09-08 | M + S（INT-001 / P2-INT-DOC）

- 做了什么：1676（13 文件 +976/-34，新增 5）冻结 integration 模块合同——新增 `docs/algorithms/PHASE2_INTEGRATION.md`（+279）、`docs/modules/phase2_int.md`（+59）、`lib/phase2_int` 三件套（README +73 / memory +80 / module.yaml +91），改 DATA_SEMANTICS（+87/-2）、contracts/INDEX.yaml（+34/-2）、PUBLIC_API（+64）、registry/astrocs.phase2.integrate.md（+84/-14）、trace 两文件（+13/-13）、DOCUMENT_INDEX（+9/-3）；本条 ledger 特例为 +9（一次登记 9 行）。1677（3 文件 +10/-9）同步 CLOSED。
- 落点：docs/contracts（3）、docs/algorithms（1）、docs/modules（2）、docs/traceability（2）、DOCUMENT_INDEX、lib/phase2_int（3）、evidence（1676 计 1、1677 计 3）。
- 内容性质：文档或规格 + 控制包或台账。
- 意图证据：消息自述"控制包 14/140, wave W1；矩阵 13 行原位融合，零权重边界 + 并行 bitwise 容差冻结"；变更面证据吻合——trace CSV 恰 +1/-1（单行原位改写）对应"原位融合"，算法面 +319/-0 纯增对应"冻结"。1677 台账证据：CLOSED 64→65、NOT_STARTED 112→111。
- 关联：与 1665/1666、1659/1660 等同型（八对）；1676 引入的注释违规在 seq 1679（b80404eb）第 1 根因中被点名并补 trace 引用行；1676 的 ledger +9 是本片唯一"批量补登 + 工作内容"同提实例。
- 不确定点："并行 bitwise 容差"的实现与测试证据不在本片（无 lib 源码或测试同提）——未证实。

### seq 1678 | be23e65d | 2026-09-08 | M（p1-batchH，星坐标契约）

- 做了什么：修 star_measurements 坐标契约的 0.5px 系统错位——dpsf 与 sdet 两条分支统一经单一桥 `astro_coord_to_unified` 归一化（自述以 DATA_SEMANTICS §17.2 / §18.1 作合同仲裁），消除 PSF 星进 ipv 时的方向性偏移。落点 `lib/orchestrator/cpp`：orchestrator.cpp +97/-58、orchestrator.h +28/-0、Makefile +30/-1、新增 test_p1_batchH_star_coord.cpp +179；ledger +1。
- 落点：lib/orchestrator（4 文件 +360/-59）、evidence/v8_1_ci_control（1）。
- 内容性质：生产代码 + 测试 + 控制包或台账。
- 意图证据：消息自述给出缺陷量级（0.5px 系统性）、仲裁依据（两个合同条款号）与验收（"20 项单测 + 基线对照零新增失败"，轮次 R8-A）；变更面证据吻合：新接口出现在头文件（+28 全新增），双分支改写集中在 .cpp（+97/-58，含删除），新测试 +179 与"20 项"规模相称。
- 关联：与 seq 1667 同为 dpsf→ipv 数据路径（句柄域 → 坐标域）；与 seq 1662/1690 同模块（orchestrator 三次触及）；其科学口径与 seq 1689（healpix 天区查询）、1674（dec 冻结域）共同构成天区/坐标语义收口带。
- 不确定点：§17.2 / §18.1 是否确为 0.5px 口径的权威源，本片未复核合同正文——未证实。

### seq 1679 | b80404eb | 2026-09-08 | M（CI R9 双平台收敛；本片唯一 1774 字长正文）

- 做了什么：逐条处置 R8 在 Linux/Windows 各 7 项失败（正文八节）：CON-COMMENTS 4 处补 trace 引用行（ALG-P2-REJ-001 / SCI-DRZ-001 / ALG-DRZ-001，引用既有冻结 ID）并对 F-V19R2-DRZ-001 加"冻结验证记录"豁免；CON-API-CONTRACTS 改按 (symbol, header) 保留全部 + CSV header 列精确匹配（消 ast_syms dict 后写覆盖前写），并校 `docs/contracts/API_CONTRACTS.csv` 4 行抄录失真；CON-DOC-SYMBOLS 修检查器三缺陷（多行反引号 token 走 os.stat 致 Errno 36、模块内相对路径缺 lib/healpix_db 前缀探测、枚举常量 AIO_HIPS_RD_IVAR / SNR 与占位符路径 <out_hips>/diagnostics.json 放行）；CON-FULL-INTEGRATION 为聚合门（上游修好即 PASS）；DATA-ARTIFACTS 按 §19.2 补登记 DATA-COV-001 行；UT-BACKEND 把 libgsl-dev 从 linux-deep 移入 linux-main deps 并加头文件存在性断言；p1004 fixture 取消 mon001_cfg 的 max_tiles=64（与 _ensure_mon001_fixture 语义对齐）；UT-ARCH 重生成 `PRODUCTION_EXECUTION_INVENTORY.csv`。
- 落点：tools/quality（2 文件 +52/-3）、lib/healpix_db（3 文件 +5/-1）、docs/contracts（2 文件 +5/-4）、.github/workflows/ci-linux.yml（+7/-1）、ci/prepare_linux_fixtures.py（+5/-1）、docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv（+10/-7）、lib/phase2/include/astro/phase2/rejection.h（+1）。包与证据面 0。
- 内容性质：CI 或机器门（主导）+ 测试 fixture + 数据或产物（生成台账）+ 生产代码（仅注释行）。
- 意图证据：正文属"逐根因 + 处置 + 红线声明"三段式，红线自述为"docs/contracts 仅补登记行或对齐头文件实测签名（零语义改写）；lib 头仅注释行；检查器修复为消除误报，未放宽任何真实比对"；**变更面证据与红线严格吻合**——被点名的 rejection.h（+1）、spherical_overlap.h（+2）、candidate_oracle_test.cpp（+2）、test_spherical_overlap.cpp（+1/-1）均为注释量级，DATA_ARTIFACTS.md +1/-0（补登记一行），实质逻辑改动只落在两个检查器（+52/-3）。
- 关联：上游是 seq 1663（质量门真实化）与三批 DOC 冻结（1665 / 1676 / 1684）；下游是 seq 1680（R10 残余 1 条）；与 seq 1700（PRODUCTION_EXECUTION_INVENTORY 重生成）为同型重复（该片台账非幂等漂移两次处置）。
- 不确定点：正文自报的本地复跑结论（两检查 PASS、422 rows 比对、DATA_ARTIFACTS_PASS schemas=20、5/5 OK）无仓库面产物；正文自述"UT-CLI 4F 与 WIN-TEST-UNIT(8)/WIN-PACKAGE(5) 为 R6/R7 延续结构项，不在本轮域，另行报告"——即本片存在明示未闭合的红项，其归属报告未在本片区段出现。

### seq 1680 | b95f147a | 2026-09-08 | S（CI R10 平台差）

- 做了什么：`tools/quality/contracts/check_doc_symbols.py`（+3/-1）把 known_files 后缀匹配统一正斜杠（对 key 做 backslash→slash 归一后再 endswith 匹配），修 R9 在 Windows 残留的唯一 1 条 CON-DOC-SYMBOLS（healpix_drizzle/astro_sphere_sink.cpp file not found）。
- 落点：tools/quality（1 文件）。
- 内容性质：CI 或机器门。
- 意图证据：正文自述给出根因链（rglob 相对路径在 Windows 为反斜杠，故 endswith("/" + token) 永不匹配；Linux 同检查 PASS），并带 CI run 号 34178712916 作外部锚点；变更面证据：单点 1 行逻辑替换，与"平台差归一"规模吻合。全卡"自述与变更面比对"显示消息点名的 cpp 文件未被改动（属被检查对象，非改动对象），无夹带。
- 关联：1679 的残余续作；与本片 Windows 线（1682 / 1683 / 1686 / 1694–1697）同族；其正斜杠归一思路在 seq 1701 的 warning_suppression 空格路径处置中再次出现（同一类平台差根因）。
- 不确定点：无。

### seq 1681 | dd89e530 | 2026-09-08 | M（p1-batchG：CLI 进程 API + benchmark verdict）

- 做了什么：两处 std::system 字符串拼接改跨平台进程 API——新建 `cli/process.cpp`（+193）与 `cli/process.h`（+38），argv 零 shell 解析 + timeout 消毒 + exit 检查；benchmark 输出加顶层 verdict 字段且 FAIL 时 exit=4；改 `cli/commands.cpp`（+87/-32）、`cli/cli_common.h`（+16）、根 `CMakeLists.txt`（+7）、`cli/CMakeLists.txt`（+1/-1）；新增 `tests/unit/cli_process_test.cpp`（+145）、`cli_bench_verdict_test.cpp`（+81）、`tests/cli/test_bench_cli.py`（+41）、tests/unit/CMakeLists（+14）；ledger +3。
- 落点：cli/（4 文件）、tests/unit（3 文件 +260）、tests/cli（1）、构建面（根 + cli 两处）、evidence（1）。
- 内容性质：生产代码 + 测试 + 构建 + 控制包或台账。
- 意图证据：消息自述点名轮次 R8-B 与动机"容差收紧后真实 FAIL 显形，不再被恒 PASS 掩盖"；变更面证据吻合：新增测试 260 行且被测对象即本条新增的 process.cpp/.h；test_bench_cli.py +41 支持 verdict / exit=4 契约被验收。
- 关联：本条新建的 cli/process.h 直接引发 seq 1683（fa60113b）的 Windows 编译失败（同名遮蔽 CRT process.h），属"引入即埋雷、三条内引爆并修"的因果闭环；与 seq 1671/1672 同属 CLI 面收口；verdict 语义与 seq 1657 容差收紧构成因果链。
- 不确定点：紧凑卡目录聚合（process.cpp +209 / process.h +45）与全卡每文件（+193 / +38）不一致，属统计口径差异——以全卡为准，差异原因未证实。

### seq 1682 | 5c43dc04 | 2026-09-08 | S（BLD-003；CI R10 Windows 收敛）

- 做了什么：消两类 Windows 失败根因——(1) WIN-TEST-UNIT ctest exit 8：`test_p1_session_manifest.cpp`（+19/-3）弃 POSIX 命令（rm -rf / mkdir -p）与 TMPDIR 默认 /tmp，改 C++17 std::filesystem（remove_all / create_directories）加临时根回退链 TMPDIR→TEMP→TMP→.，自述"断言语义零变更"；(2) WIN-PACKAGE-CANDIDATE exit 5：新增 `cmake/astrocs.product.windows.json.in`（+14，windows-amd64 产品 manifest：astrocs.exe / astrocs_runtime.dll / astrocs_io.dll / modules/astrocs_noop.dll / providers/astrocs_cpu_baseline.dll，status 字面量 IMPLEMENTED，@ASTROCS_BASE_VERSION@ 与 @ASTROCS_GIT_COMMIT@ 占位填充），并把 `cmake/install_layout.cmake`（+17/-2）改为 WIN32 分支 configure 期生成、Linux 分支维持 BLD-003 骨架。
- 落点：cmake/（2 文件，含 1 新增）、lib/phase1_session（1 文件）。
- 内容性质：构建或打包面（生产）+ 测试。
- 意图证据：正文给出四轮复现史（R6/R7/R8/R9–R10）与逐条根因，并自报"Linux 侧 5 合同检查 + DATA-ARTIFACTS 全 PASS、ctest p1_session_manifest PASS、reconcile --strict rc=0"；变更面证据吻合——新增文件正是此前不存在的 Windows manifest 模板（此前无条件装 Linux 骨架，故 Windows 安装树 manifest 与实际产物全错位）。
- 关联：与 seq 1683（同 Windows build 收敛）、1686（同一测试文件第二轮 Windows 化）成链；自述点名 BLD-003 为既有 Linux 骨架任务号（其建立点在上片区段）。
- 不确定点：Windows 端实跑只在正文自述且本条未给 run 号；Fatduck 复验状态本片无证据——未证实。

### seq 1683 | fa60113b | 2026-09-08 | S（CI R11；本片唯一改名提交）

- 做了什么：`cli/process.h` 改名 `cli/astrocs_process.h`（操作构成：改名 1 + 修改 3，总计 +4/-4），消 MSVC 下 <thread> 内部 <process.h> 被同名本地头遮蔽致 _beginthreadex 未声明（error C2039/C3861）；同步三处 include（commands.cpp、process.cpp、cli_process_test.cpp）。
- 落点：cli/（3 文件含改名）、tests/unit（1 文件）。
- 内容性质：结构整理（改名）+ 生产代码（include 面）。
- 意图证据：正文给出完整根因链（cli/ 下同名头 + -I cli/ 使尖括号搜索按 -I 序命中；R10 时 cli/ 尚无该文件故 PASS；Linux 不炸因 libstdc++ 的 <thread> 不引 <process.h>），并带 run 号 34182250472 与"语义零变更"声明；变更面证据极强：4 文件各 1–2 行、无新增文件、无逻辑行变化，形态即纯改名 + include 更新。
- 关联：直接后继 seq 1681（dd89e530 新建该头），前引 seq 1682 的 Windows 收敛线；与 seq 1694（include 传递性）同属"MSVC 与 libstdc++ 头解析差异"族。
- 不确定点：无。

### seq 1684–1685 | 6f668c87, bd33a8a5 | 2026-09-08 | M + S（REJ-001 / P2-REJ-DOC）

- 做了什么：1684（13 文件 +1665/-33，新增 5）冻结 rejection 模块合同——新增 `docs/algorithms/PHASE2_REJECTION.md`（+627）、`docs/modules/phase2_rej.md`（+154）、`lib/phase2_rej` 三件套（README +108 / memory +85 / module.yaml +102），改 DATA_SEMANTICS（+154，§22）、contracts/INDEX.yaml（+34/-2）、PUBLIC_API（+124）、registry/astrocs.phase2.reject.md（+99/-16）、trace 两文件（+13/-13）、DOCUMENT_INDEX（+5/-2）、ledger +3；自述冻结对象为"四拒绝统计定义 / NaN 传播 / 确定性判定"，并含"descriptor 实测双输入端口修正"。1685（3 文件 +10/-9）同步 CLOSED。
- 落点：docs/contracts（3）、docs/algorithms（1）、docs/modules（2）、docs/traceability（2）、DOCUMENT_INDEX、lib/phase2_rej（3）、evidence（1684 计 1、1685 计 3）。
- 内容性质：文档或规格 + 控制包或台账。
- 意图证据：消息自述点名 ALG-P2-REJ-001 / DATA-P2-REJ(§22) / API-P2-REJ-001 与控制包"15/140"；变更面证据吻合——算法面 701 行纯新增、契约三文件净增（+350/-2 级），符合"冻结"语义；registry 页 -16 与"descriptor 实测修正"对应。1685 台账证据：CLOSED 65→66、NOT_STARTED 111→110。
- 关联：1684 未把 PHASE2_REJECTION.md 登记进 DOCUMENT_INDEX，直接造成 seq 1687（dc391772）的 R13 双红（该条正文点明"6f668c87 R1 冻结文档在 R12 push 后 landing"）；其注释违规在 seq 1679 第 1 根因中补 trace；rejection.h 在 1679 中 +1 行。
- 不确定点：四个拒绝态的确切状态字面量卡片未展开；外部审核状态本片无证据——未证实。

### seq 1686 | 4b6eb26d | 2026-09-08 | S（CI R12）

- 做了什么：`test_p1_session_manifest.cpp`（+6/-1）把 fixture 路径统一 std::filesystem::path::generic_string() 化，消 Windows TEMP 反斜杠直接拼进 config JSON 形成非法转义（"\U" / "\T"）致 p1_session_validate 拒绝。
- 落点：lib/phase1_session（1 文件）。
- 内容性质：测试（跨平台路径口径）。
- 意图证据：正文给出精确失败面（R12 run 34185034180 唯一失败用例 T1 validate/run）、上一轮修复生效判定（"fixture open 已消"）与新失败面判因，自述"断言语义零变更"；变更面证据：6 行全在测试文件，与被点名文件命中（全卡交集成立）。
- 关联：seq 1682 的续作（同一测试文件第二轮 Windows 化）；其 generic_string 模式在 seq 1696（56b69072）被复用到 p3 测试。
- 不确定点：无。

### seq 1687 | dc391772 | 2026-09-08 | S（CI R13 双红）

- 做了什么：(1) `docs/DOCUMENT_INDEX.yaml` +3/-0 按 ACTIVE_NORMATIVE 条目格式登记 docs/algorithms/PHASE2_REJECTION.md（含 Siril 1.4.3 / RCR 2.4.7 的 ORACLE ONLY 声明）修 DOC-INDEX 的 docs_fully_covered FAIL；(2) `tools/check_version_consistency.py` +5/-2 在 EXEMPT 关键词族增补 harness（与既有 siril / rcr / astropy 同族），修 UT-VERSION test_04 因"上一行尾词 Siril 跨行断开 + 行首字面量 1.4.3"造成的产品版本误判。
- 落点：docs/DOCUMENT_INDEX.yaml（1）、tools/check_version_consistency.py（1）。
- 内容性质：文档或规格（索引登记）+ CI 或机器门（豁免口径）。
- 意图证据：正文自述"内容型，非 Windows 特有；Linux R13 同因将同样失败"并声明"文档本体（冻结科学内容）零改动"；变更面证据吻合——文档面只动索引 YAML 3 行，PHASE2_REJECTION.md 未被触碰；豁免侧仅关键词族扩充 5 行。
- 关联：直接后继 seq 1684（漏登记）；与 seq 1700（SAMP 模块页索引）为同型第二例，即"DOC 冻结波未同步索引"在本片重复出现两次；与本条同分钟的 seq 1688（12:37:59）为并行工作流（不同域）产物。
- 不确定点：正文称"仓库内 harness 一词仅出现于外部 oracle 上下文"，该全库唯一性断言本片无扫描证据支撑——未证实。

### seq 1688 | 1415df04 | 2026-09-08 | L（p1-batchI：AIO 加固二；按目录直方图归纳，不逐文件展开）

- 做了什么：输入面二次加固一轮（自述七类处置）——cfitsio calloc 返回 NULL 处置、FP64 写硬失败（禁静默降精度）、HiPS reader 读侧 tile_width == 512 硬校验、异常屏障由 27 处扩到 114 处 try（自述 119 入口全覆盖）、HCSD 上限与错位读 fail-closed、XISF 64 位 seek、MOC fail-closed。规模 12 文件 / +3938 / -2414 / 新增 1 / 噪声区 0。
- 落点（目录直方图）：lib/astro_image_io 10 文件 +3894/-2414——其中 src/healpix/aio_healpix_io.cpp +1498/-1180、src/aio_pipeline.cpp +695/-429、src/hips/aio_hips_writer.cpp +506/-416、src/aio_pipeline_engine.cpp +279/-183、src/hips/aio_hips_reader.cpp +259/-104、src/aio_compressor.cpp +76/-38、src/aio_fits.cpp +77/-4、src/ahpx/aio_ahpx_api.cpp +46/-0、src/aio_xisf.cpp +13/-2、新增 tests/test_p1_io_hardening.cpp +281；lib/healpix_db 1 文件 +41/-0（hp_drizzle_api.cpp）；evidence/v8_1_ci_control +3/-0。
- 内容性质：生产代码 + 测试 + 控制包或台账。
- 意图证据：消息自述"bug 狩猎 R9-A，恶意 fixture 11/11"；变更面有两条支撑"加固而非重写"——(a) 全卡"新增声明"与"删除声明"两组符号集合近乎同名成对（aio_alloc / aio_realloc / aio_free / aio_pipeline_frame_create / aio_frame_* / aio_hips_open / aio_hips_read_tile_f32 / f64 / aio_hips_finalize / aio_hips_abort 等同时出现在增删两侧），且 struct TileGroup 亦成对增删，形态为**同函数体整体重写（加 try 包裹）**，与"异常屏障 27→114"自述吻合，而非删除功能；(b) include 侧新增 exception、stdexcept、cstdint、zstd.h 等，符合屏障改造形态。
- 关联：与 seq 1658（f1cb487c，加固一）构成输入面两阶段（同模块、同族函数、同名测试前缀 test_p0_ → test_p1_）；新测试 test_p1_io_hardening.cpp 在 seq 1701（6c5f5fc9）才被注册进 ctest；本条与 seq 1687 相隔 5 秒落盘（12:37:54 / 12:37:59），属同一工作时段的两条不同域提交。
- 不确定点：单提交 +3938/-2414 混合 7 类独立处置，卡片未提供分块信息，无法把具体文件切给具体处置——逐项归因未证实；删除量与新增量近乎相当（-2414），无法从变更面区分"整函数包 try 的重排"与"夹杂格式化"，未证实。

### seq 1689 | ce93f9ac | 2026-09-08 | M（p1-batchJ：healpix 邻域与圆形查询）

- 做了什么：`lib/common/healpix/healpix_core.cpp`（+145/-121）与 `healpix_core.h`（+29/-4）——neighbors 的 corner 分支按官方表驱动重写、query_disc 的 NESTED 改四叉树直译、非法 nside 由静默取整改为抛异常；新增 `lib/common/healpix/tests/test_healpix_neighbors.cpp`（+228）并在 `tests/unit/CMakeLists.txt`（+5）注册；ledger +1。
- 落点：lib/common/healpix（3 文件 +431/-125）、tests/unit（1）、evidence（1）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述给出三重量化验收（oracle 1020 例 0 mismatch / query_disc 265 case bad=0 / 官方钉值 18 例）并声明"R5-B 疑点终判闭环"（轮次 R9-B）；变更面证据吻合：新测试单文件 +228 与被重写的 corner 分支体量相称，require_valid_nside 相关改动落在头文件（+29/-4）与"改抛异常"一致。
- 关联：本条只加 <stdexcept> 未加 <string> 直接造成 seq 1694（9f85b422）的 MSVC C2039（该条正文点名 ce93f9ac 为根因）；与本片区段内 seq 1673/1675 补的 -Ilib/common/healpix 为同一目录的包含面；"R5-B 疑点"跨 5 轮才终判，与 seq 1661/1662（R5）构成疑点→闭环链。
- 不确定点："官方表"与"官方钉值"的权威来源版本卡片未给——未证实。

### seq 1690 | 1efef060 | 2026-09-08 | M（p1-batchK：ACR CUDA 桥 + 回归恢复）

- 做了什么：ACR 桥三处 P1 与一处回归恢复——`acr_cuda_bridge_host.cpp`（+138/-20）加 31 导出的异常屏障、reduce grid 同源化（消显存越界）、shutdown 排空闸门（消 UAF），配套改 `acr_cuda_bridge_kernels.cu`（+10/-10）与 `lib/acr/core/runtime.cpp`（+30/-1）；`lib/calibration/src/photometry_apply.cpp`（+6/-1）恢复 photscal <= 0 的拒绝分支（自述 37ed737d 经 merge 93ce026c 意外丢失）并改 `test_photometry_apply.cpp`（+16/-8）；`orchestrator.cpp`（+9/-2）mkdir 改 fs API；ledger +1。
- 落点：lib/acr（3 文件 +179/-31）、lib/calibration（2 文件 +23/-9）、lib/orchestrator（1）、evidence（1）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述"bug 狩猎 R10-B，26/26 + 15/15 + 14/14 + ASAN 对照"并声明"考古链完整"；变更面证据吻合：photometry_apply 仅 +6/-1 且其测试同提（+16/-8），属"恢复一条被丢失的拒绝分支"的最小修补形态，而非新增能力。
- 关联：与 seq 1662 / 1678 同模块（orchestrator 在本片被三次触及：1662、1678、1690）；被恢复的 37ed737d 属上游区段提交，本条是其回归落点；ACR 面按宪章属"生产构建默认关闭"域，本条为桥侧健壮性——变更面内无路由/开关改动。
- 不确定点：所称"merge 93ce026c 意外丢失"的考古链证据不在本片变更面——未证实；三组通过数（26/15/14）对应哪些测试目标卡片未映射——未证实。

### seq 1691 | 659b5513 | 2026-09-08 | M（p2-batchL：phase3 完整性锚）

- 做了什么：`lib/phase3_session/p3_output.cpp`（+76/-7）与 `p3_output.h`（+6/-4）把落盘时序对齐 IO_003 合同（flush→close→fsync→rename），并把 sha256 静默失败改为整体输出失败（禁假哈希进 provenance）；新增 LD_PRELOAD 事件序探针两件——`tests/backend/p3_output_fsync_interposer.cpp`（+99）、`tests/backend/p3_output_fsync_probe.cpp`（+74），并扩 `tests/backend/test_p3_output.py`（+170/-1）；ledger +1。
- 落点：lib/phase3_session（2）、tests/backend（3，含 2 新增）、evidence/v8_1_ci_control（1）。共 6 文件 +457/-12。
- 内容性质：生产代码 + 测试（含注入式探针）+ 控制包或台账。
- 意图证据：消息自述"11 passed + ctest 5/5 + 预存 3 失败 HEAD 基线对照非回归"（轮次 R10-C）；变更面证据吻合：新增 interposer / probe 两个专用 C++ 文件用于事件顺序可观测，与"fsync 时序对齐"自述匹配，属可重放验证设计而非口头声明。
- 关联：本条引入的 close→open(O_RDONLY)→fsync→rename 链在 Windows 必败，由 seq 1697（09a69fc4）以 _WIN32 下 O_RDWR 修正（该条正文点名 659b5513 为引入点）；与 seq 1695 / 1696 同为 p3_output / p3_assembly 收敛线；批号由 fix(p1-batch*) 转入 fix(p2-batch*)（batchK→batchL→batchO）。
- 不确定点：IO_003 合同正文不在本片区段，时序条款字面一致性未证实；"预存 3 失败"的归属清单卡片未列——未证实。

### seq 1692–1693 | e654d4c2, dc1ed210 | 2026-09-08 | M + S（SMP-001 / P2-SAMP-DOC；含台账改判潮）

- 做了什么：1692（11 文件 +1423/-107，新增 4）冻结 sampling 模块合同——新增 `docs/modules/phase2_samp.md`（+163）与 `lib/phase2_samp` 三件套（README +112 / memory +87 / module.yaml +108），改 `docs/algorithms/PHASE2_SAMPLER.md`（+388/-34）、DATA_SEMANTICS（+142，§23）、PUBLIC_API（+113）、registry/astrocs.phase2.sample.md（+118/-15）；**同提交一次性把 13 条 READY 任务改判 CLOSED**（`STATE_RECONCILIATION.csv` +13/-13，逐行含 CPU-006 / CPU-007 / CPU-008 / MON-001 / MON-002 / QA-001..005 / LNX-003 / LNX-004 / LNX-005，source_of_status 全为 fg_verified_commit；`TASK_STATE.json` +43/-43）。1693（3 文件 +10/-9）为 SAMP-DOC 自身的 CLOSED 同步。
- 落点：docs/algorithms（1）、docs/contracts（2）、docs/modules（2）、lib/phase2_samp（3）、evidence/v8_1_ci_control（1692 计 3 文件 +57/-56；1693 计 3 文件 +10/-9）。
- 内容性质：文档或规格 + 控制包或台账（两类各半，同提交混合）。
- 意图证据：消息自述"ALG-P2-SMP-001（108 锚实测）/ DATA-P2-SMP(§23) / API-P2-SMP-001 / 三件套（控制包 16/140, wave W1）"，并声明改判依据"R13 审计 13/13 证据链齐：reverify 4f6e2b65 / f0bc039a / ab84dc16 / 3da0670b 全在 HEAD 祖先，跨代口径注登记，READY 归零"；**变更面证据与"改判"完全对应**——CSV 13 行、JSON 13 任务块，且卡片可见旧值列为 READY 与 DISPATCHED/NOT_STARTED 混合，属"补记已达状态"而非"新完成工作"。
- 关联：1693 的 CLOSED 聚合计数起点为 79（而 seq 1685 之后为 66），差值 13 正由本条改判潮造成（CLOSED 66→79→80），是"用台账计数反查改判规模"的第一手证据；本条的 phase2_samp.md 未登记 DOCUMENT_INDEX，直接构成 seq 1695 正文点名的"DOC-INDEX 未覆盖，留待 R1 收敛"，并在 seq 1698（补登）与 seq 1700（同步 origin/main 索引）收尾。
- 不确定点：四条 reverify 提交（4f6e2b65 / f0bc039a / ab84dc16 / 3da0670b）不在本片区段，其证据文件本片不可见——未证实；"跨代口径注"的具体口径卡片未展开——未证实。

### seq 1694 | 9f85b422 | 2026-09-08 | S（CI R15 编译面）

- 做了什么：`lib/common/healpix/healpix_core.h` +1/-0 增补 string 头包含，修 MSVC 下 require_valid_nside 使用 std::to_string 报 C2039（libstdc++ 经 stdexcept 传递引入故 Linux build 过）。
- 落点：lib/common/healpix（1 文件）。
- 内容性质：生产代码（机械 include 增补）。
- 意图证据：正文点名根因提交 ce93f9ac（R9-B batchJ）与 run 号 34189230606，自述"机械 include 增补，零语义变更；本地全量 build+ctest rc=0"；变更面证据严格吻合——1 行新增、无任何逻辑行变更。
- 关联：seq 1689 的补偿性修补；与 seq 1683 同属"平台头文件解析语义差"族；与其前一条 seq 1693（同分钟级落盘）属不同域。
- 不确定点：无。

### seq 1695 | 804462a8 | 2026-09-08 | S（CI R16 测试面）

- 做了什么：4 个 unit 测试的临时目录改跨平台回退链——`tests/unit/cpu_fallback_test.cpp`、`p1_hips_writer_test.cpp`、`p3_output_test.cpp` 各 +8/-1，`p3_assembly_test.cpp` +21/-3（并把其 fixture 改 std::filesystem）。
- 落点：tests/unit（4 文件 +45/-6）。
- 内容性质：测试。
- 意图证据：正文给出精确症状（TMPDIR 缺省时 /tmp 落当前盘根不存在目录 → p3_output_write_atomic 返回值不等 P3_OUT_OK；p3_assembly 另有 rm -rf / mkdir -p 在 cmd.exe 报语法错）与先例引用（同 io_adapter_test 既有模式、R11 filesystem 先例），带 run 号 34190515939；变更面证据：4 文件全在 tests/unit、生产代码零改动，与"断言语义零变更"吻合。
- 关联：延续 seq 1682 / 1686 的跨平台化模式；正文对 phase2_samp.md 的"留待 R1 收敛"书面处置，是 1692→1698→1700 索引缺口的中间证据点。
- 不确定点：自报"本地全量 build+ctest(61/61) rc=0"无仓库面产物——未证实。

### seq 1696 | 56b69072 | 2026-09-08 | S（CI R17 测试面）

- 做了什么：`tests/unit/p3_assembly_test.cpp`（+10/-3）与 `p3_output_test.cpp`（+14/-5）把测试目录统一 generic_string() 化（Windows TEMP 反斜杠路径进 hips_product_validate 的 path_is_safe 被判"含反斜杠"拒收 → P3_RS_PARAM 连锁 6 CHECK），并加 p3_sampler_open / write_atomic 失败时打印 status + err / errno 作 CI 诊断；正文声明"lib 层 path_is_safe 安全语义不动"。
- 落点：tests/unit（2 文件 +24/-8）。
- 内容性质：测试（含诊断输出面）。
- 意图证据：正文引用 R17 run 34191327121 的诊断串（"[diag] sampler_open status=1 err=path contains backslash"），并明确 path_is_safe 拒反斜杠是"防 Windows 路径注入的设计语义"；变更面证据：改动只在测试侧、与被点名文件命中（全卡交集成立），未触及校验实现，与红线自述一致。
- 关联：seq 1686 的 generic_string 模式复用；为 seq 1697 留下诊断回传口（1697 正文即引用本条新增诊断输出所得 status=2 errno=9）。
- 不确定点：无。

### seq 1697 | 09a69fc4 | 2026-09-08 | S（CI R18 生产面）

- 做了什么：`lib/phase3_session/p3_output.cpp` +7/-0 在 _WIN32 下把 fsync 前的只读 open 改为 O_RDWR（Windows fsync 即 _commit / FlushFileBuffers，要求可写句柄，只读 fd 必报 EBADF），POSIX 路径不变。
- 落点：lib/phase3_session（1 文件）。
- 内容性质：生产代码（平台分支）。
- 意图证据：正文自述诊断回传（status=2 / errno=9，即 P3_OUT_IO + EBADF）、点名引入提交 659b5513 的 close→open(O_RDONLY)→fsync→rename 链，并声明"不改内容，fsync/_commit 语义与 POSIX 一致；本地全量 build+ctest rc=0"，带 run 号 34201181796；变更面证据：7 行全为平台分支新增，无既有用例逻辑改写。
- 关联：闭合 1691（引入）→ 1695 / 1696（测试侧残余）→ 1697（生产侧根因）的 Windows p3 输出线；其后 3 分钟落盘的 seq 1698 转入另一域（DOC 冻结）。
- 不确定点：以 O_RDWR 打开是否扩大写权限面（安全语义），本片无相关证据——未证实。

### seq 1698–1699 | 722b5737, 01842b32 | 2026-09-08 | M + S（DISP-001 / P2-SESSION-DOC）

- 做了什么：1698（12 文件 +1109/-3，新增 5）冻结装配层（phase2 session）合同——新增 `docs/algorithms/PHASE2_SESSION.md`（+320）、`docs/modules/registry/astrocs.phase2.session.md`（+62）、`lib/phase2_session` 三件套（README +104 / memory +77 / module.yaml +109），改 DATA_SEMANTICS（+150，§24）、contracts/INDEX.yaml（+35/-3）、PUBLIC_API（+105）、trace（CSV +1/-0、JSON +26/-0）、DOCUMENT_INDEX（+9/-0，含对 1692 漏项的补登）；自述登记"NODE-CALL 唯一性 + IMPL-COMPLETE 全链 artifact 目标合同，partial facade 现状差距 DISP-001..008 如实登记"。1699（3 文件 +10/-9）同步 CLOSED。
- 落点：docs/contracts（3）、docs/algorithms（1）、docs/modules（1）、docs/traceability（2）、docs/DOCUMENT_INDEX.yaml、lib/phase2_session（3）、evidence/v8_1_ci_control（1698 计 1、1699 计 3）。
- 内容性质：文档或规格 + 控制包或台账。
- 意图证据：消息自述点名 ALG-P2-SESSION-001 / DATA-P2-SESSION(§24) / API-P2-SESSION-001 / 三件套 / "trace 30 行"（控制包 17/140, wave W1）；变更面证据吻合——trace 侧 +27 行全为追加（CSV 1 + JSON 26，删除 0），整个提交删除量仅 -3（集中在 contracts/INDEX.yaml），符合"如实登记现状差距而非改写"。1699 台账证据：CLOSED 80→81、NOT_STARTED 109→108、closed_commit = 722b5737、source_of_status = fg_verified_commit。
- 关联：DISP-001..008 与本模块的实现类缺陷（seq 1691 / 1697 的 p3/phase2 装配完整性）同域；DOCUMENT_INDEX 的 +9 补齐 seq 1692 的漏登，与 seq 1695 / 1700 共同闭环"冻结波滞后登记"缺口。
- 不确定点：DISP-001..008 中哪些已被本片实现类提交消化，卡片未给映射——未证实；DISP 是本片编号族之一但只以登记形式出现（无独立 DISP 提交）。

### seq 1700 | 69ec459f | 2026-09-08 | S（CI R19 Linux 残余 3 项）

- 做了什么：(1) `.github/workflows/ci-linux.yml` +4/-0——构建步把 linux-control 树生成的 version_generated.h 拷入 build/，修 oracle fixture 以 -IREPO/build 找不到该头（TestPhase3ReprojOracle setUpClass）；(2) `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv` +5/-4——重生成以消化行号漂移（自述 orchestrator.h 345→375 等 4 处 + aio 新增）；(3) 自述同步 origin/main 版 DOCUMENT_INDEX.yaml（SAMP 模块页，与 R14 的 PHASE2_REJECTION 行共存）。
- 落点：.github/workflows（1）、docs/architecture（1）。紧凑卡字段：代码面文件 0、包与证据面文件 0。
- 内容性质：CI 或机器门 + 数据或产物（生成台账）。
- 意图证据：正文自述 R19 run 34204130361 Linux fail=4、"可动 3 项"，并把 UT-CLI 4F 归为"既有 R1 域已知清单"、TestP2003SeamOracle 的 phase2 run exit10 归为"R1 域另案"；变更面证据：仅 2 文件、其中 1 个为生成物 CSV，属最小可动集处置。
- 关联：收口 seq 1692 遗留的索引缺口；与 seq 1679 第 8 根因（同一 CSV 重生成）构成本片两次同型"台账非幂等漂移"，说明生成器与源行号间存在结构性漂移（结构模式，非单次事故）。
- 不确定点：**自述第 3 项（索引同步）在本条变更面为 0 文件**——可能已由 seq 1698 覆盖或同步后无差异，本片不可判，未证实。

### seq 1701 | 6c5f5fc9 | 2026-09-08 | M（p2-batchO：tools 质量面 R13）

- 做了什么：工具面加固与检查器失效族处置——strip 族改原子写（`strip_version_comments.py` +36/-6、`v19r3_strip_comments.py` +36/-6、`v19r4_strip_comments.py` +39/-6，自述 SIGKILL 验证 SHA256 不变）、zip-slip 预验证（自述 20 恶意 zip 拒绝）、`check_serial_heavy.py`（+7/-4）UnboundLocal 修、检查器失效族 8 项（`check_warning_suppression.py` +50/-18 空格路径、`api_doc_consistency.py` +81/-22 候选链、`validate_cpu_profile.py` +47/-24 重导入、`check_release_consistency.py` +15/-8 语义化版本、`check_unit_closure.py` +4/-2 字符类真根因、`check_module_readmes.py` +11/-7 真值、`check_pipeline_trace.py` +65/-31 跨 session、`config_consistency_check.py` +50/-2 候选链、`doccheck/check_engineering_constraints.py` +29/-7），并在 `tests/unit/CMakeLists.txt`（+60）补注册 cal_photometry_apply 与 p1_io_hardening；ledger +2。
- 落点：tools/（9 文件）、tools/quality（4 文件）、tools/doccheck（1 文件）、tests/unit（1）、evidence/v8_1_ci_control（1）。共 16 文件 +585/-147，代码面 15 / 包与证据面 1。
- 内容性质：CI 或机器门（检查器群，主导）+ 构建注册（测试面）+ 控制包或台账。
- 意图证据：消息自述给出三验标准"非恒 PASS / 非恒 FAIL / 非崩溃"；变更面证据吻合：改动全部落在检查器与被检工具写路径，无 lib 生产代码被触碰；ctest 注册 +60 行说明 seq 1690 / 1688 的新测试在本条才真正进 ctest，属"暴露面补建"。
- 关联：与 seq 1663（质量门真实化）为同族的两端；与 1688 / 1690 的测试注册闭环；批号 batchO 与本片已见的 batchA..batchL 之间存在字母跳号（E / F / M / N 未见于本片区段，其中 E 仅在 seq 1665 自述中被回指）。
- 不确定点：zip-slip 预验证的具体落点文件按体量推断（`astro_toolkit.py` +26/-2），卡片未逐处置映射——未证实；跳号批次落在本片区段外还是从未存在，本片无证据。

### seq 1702–1703 | 21ce603a, 1f002575 | 2026-09-08 | S + S（Fatduck 运维通道，成组）

- 做了什么：1702 新建 `.github/workflows/fatduck-admin.yml`（+22，workflow_dispatch 带 cmd 输入、runs-on 标签 self-hosted / fatduck-realdata、timeout-minutes 30、permissions contents:read），作为"SSH exec 通道不可用时的节点级带外管理入口"，注释含"仅限节点运维用途，禁止跑业务负载"约束，并登记 ledger +1；1703 把该 workflow 唯一 step 的 shell 由 pwsh 改 cmd（+1/-1），自述根因"runner 服务账户 PATH 无用户级 pwsh（自托管首曝）"。
- 落点：.github/workflows/fatduck-admin.yml（1702 新增、1703 修改）、evidence/v8_1_ci_control（1702 计 COMMIT_LEDGER.jsonl +1）。
- 内容性质：CI 或机器门（运维通道）。
- 意图证据：消息自述说明用途与背景（DefaultShell 切 bash 后 ssh exec 被注入 /c 死锁；复用于 harness 部署）；变更面证据吻合——1702 为纯新增文件（+22/-0），1703 为单行 shell 关键字替换，两者均无生产代码面（1702 卡片字段：代码面 0）。
- 关联：AGENTS.md 现行条目"节点级运维走 .github/workflows/fatduck-admin.yml 的 workflow_dispatch 带外通道；pwsh 禁止作为开发环境默认 shell、仅节点运维通道可用"与本组两条互为因果，本片区段即该路由的建立点；与本片的 Windows / 自托管残余族（1682–1697）同背景。
- 不确定点：1703 只改 step 的 shell、未订正 1702 新增文件内"以 pwsh 执行"的注释（全卡每文件 +1/-1），注释与执行口径是否已一致——未证实。

### seq 1704–1705 | f506ef7f, 69582af7 | 2026-09-08 | M + S（IMPL-001 / UPM-001 / P2-UPM-DOC）

- 做了什么：1704（15 文件 +1896/-68，新增 5）冻结 UPM 域合同——对 `docs/algorithms/UPM_SOLVER.md` 做 ALG-UPM-001 **原位修订**（+14/-12，自述 std::thread 实测锚）、新增 `docs/algorithms/PHASE2_UPM_IMPL.md`（+401，实现级合同 ALG-P2-UPM-IMPL-001）、`docs/modules/phase2_upm.md`（+222）、`registry/astrocs.phase2.upm-fit.md`（+108/-17）与 `registry/astrocs.phase2.upm-apply.md`（+109/-17）、`lib/phase2_upm` 三件套（README +171 / memory +122 / module.yaml +134），改 DATA_SEMANTICS（+228，§25 UPM / §26 COR）、contracts/INDEX.yaml（+50/-2）、PUBLIC_API（+151，16 符号）、trace（CSV +2/-2、JSON +14/-14）、DOCUMENT_INDEX（+7/-4）、ledger +2。1705 为该任务 CLOSED 同步，**只改 1 文件 `TASK_STATE.json`（+7/-7）**。
- 落点：docs/contracts（3）、docs/modules（3）、docs/algorithms（2）、docs/traceability（2）、docs/DOCUMENT_INDEX.yaml、lib/phase2_upm（3）、evidence/v8_1_ci_control（1704 计 COMMIT_LEDGER.jsonl +2；1705 计 TASK_STATE.json +7/-7）。
- 内容性质：文档或规格 + 控制包或台账。
- 意图证据：消息自述点名"ALG-UPM-001 原位修订 + ALG-P2-UPM-IMPL-001 实现级合同 + DATA-P2-UPM/§25 + DATA-P2-COR/§26 + API-P2-UPM-001 16 符号 + INDEX+6 双向回写 + 三件套（CONTRACT_READY，DISP-P2UPM-001..004）+ upm-fit/apply registry 手写页 + 矩阵两行（SCI-UPM-001 VERIFIED / SRC 与执行测试显式 MISSING）+ CSV rows=30 parity + 单位口径按 SCI 冻结 ADU 纠控制包笔误（控制包 18/140, wave W1）+ 五门复跑：矩阵 30 / pytest 9 / 合同 80 / docindex / selfcheck ALL PASS"；变更面证据吻合——本片唯一"原位修订"痕迹即 UPM_SOLVER.md 的 +14/-12（其余七条 DOC 冻结的算法面均为纯新增），trace CSV 恰 +2/-2 对应"矩阵两行"，删除量集中在两个 registry 页（各 -17）与 DOCUMENT_INDEX（-4），与"双向回写 + 手写页改写"一致。1705 的台账证据（卡片 JSON 键变化）：status NOT_STARTED→CLOSED、closed_commit = f506ef7f、source_of_status = fg_machine_verified、review_claim NOT_STARTED→docs_complete、evidence_path 写 run/local/agent_p2_upm_doc/。
- 关联：1705 的形态差异（只改 1 文件、无 COMMIT_LEDGER 与 STATE_RECONCILIATION 变更、source_of_status 用 fg_machine_verified 而非前七对的 fg_verified_commit）是本片治理规则换轨（seq 1668）之后的首个实例；std::thread 实测锚与 seq 1667 / 1662 的并发处置同域；UPM 与 seq 1692（SMP）、1698（SESSION）、1684（REJ）同属 phase2 装配链，本条即 W1 冻结波在本片的最末一对。
- 不确定点：1705 未追加 ledger 行，究竟"由 git log 覆盖事实接管"还是"漏登"，本片无机器门输出证据——未证实；其 evidence_path 指向 run/local/，属本任务禁读区，内容存在性未核；"纠控制包笔误"所指的 ADU 口径差异对象未在变更面中定位——未证实。
