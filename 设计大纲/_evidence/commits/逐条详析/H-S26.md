# 逐条详析 （证据层过程件）

> 本文件是 设计大纲/reports/history/slices/H-S26.md 的逐条正文；最终交付只保留两份大报告，逐条正文下沉到证据层（条目文本零改动，仅移动）。
> 由 _tools/11_split_slice_reports.py 从入库基线 a3a343a4 重切，日期 2026-09-15；条目组 36 个；报告树保留段（主题归纳/重叠区核对/遗留缺口）见原文件。

## 主责条目

### seq 1376 | c764c2be | 2026-08-31 | 分级 M | RT-005
- 做了什么：把 ModuleRegistry 从静态验证推进到可执行——新增模块适配器层与 Phase1/2/3 真实工厂注册入口，配 `rt005_registry_test` 单测；台账 RT-005 状态由 `NOT_STARTED` 变 `PASS`。
- 落点：`include/astrocs/core/module.h`、新增 `include/astrocs/core/module_adapters.h`、`lib/core/src/module.cpp`、新增 `lib/core/src/module_adapters.cpp`、根与 `tests/unit/CMakeLists.txt`、新增 `tests/unit/rt005_registry_test.cpp`；包面 `evidence/v6_1_rework/tasks/RT-005/` 下 `PLAN.md`、`TASK_RESULT.json`、`logs/c01.log`、`logs/c02.log` 与 `TASK_LEDGER.csv` 行（12 文件 +873 -13）。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：消息自述"可执行 ModuleRegistry（真实 Phase1/2/3 工厂，合同校验/重复端口/heavy+serial/ACR 拒，nlohmann index）"；变更面证据为全卡新增声明区块中的 `register_factory`、`has_factory`、`register_phase_modules`，以及 `IModule` 的 `validate_config`、`plan`、`execute`、`inspect` 纯虚接口，测试文件与 CMake 登记同提交出现，二者一致。
- 关联：直接承接重叠区 seq 1375（90cf35a8，RT-004 PipelineIR 解析与静态验证）；为 seq 1377（d0114cd4，RT-006）、seq 1393（b0ad63c3，P1-001）、seq 1411（b27600d6，P2-006）、seq 1425（3617b6c4，P3-006）复用同一 `module_adapters.cpp` 落点。
- 不确定点：`ACR 拒`、`heavy+serial` 的拒收在卡片中只见函数名与测试文件名，未见具体判定分支或状态字面量，拒收实现细节未证实。

### seq 1377 | d0114cd4 | 2026-08-31 | 分级 M | RT-006
- 做了什么：实现唯一 Scheduler＋Runtime 的 DAG 调度，含节点级失败传播、内存回压、以 node-ID 索引状态，并修 `add_node` 的 move 问题；台账 RT-006 转 `PASS`。
- 落点：`include/astrocs/core/scheduler.h`、`lib/core/src/scheduler.cpp`、`lib/core/src/runtime.cpp`、`tests/unit/CMakeLists.txt`、新增 `tests/unit/rt006_scheduler_test.cpp`（+239）、既有 `tests/unit/core_scheduler_test.cpp`；包面 `tasks/RT-006/` 下 `PLAN`、`TASK_RESULT`、`logs/c01..c03.log`（12 文件 +685 -51）。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：消息自述四项能力均有变更面对应：`Scheduler` 构造形参 `available_cpu`、`budget`、`memory_limit`，方法 `set_registry`、`run`（形参 `RunContext&`）；测试侧用例名 `test_diamond_concurrency`、`test_memory_backpressure`（以 `memory_limit` 设为 1000 构造）。"TSan PASS"属消息自述，卡片仅登记日志路径，未见 sanitizer 输出字段。
- 关联：依赖 seq 1376（c764c2be）的 registry（`set_registry`）与重叠区 seq 1373/1374（30c83b45/a03077ec）的 ThreadBudget 与 RunContext；与 seq 1374 同为 `scheduler.cpp` 的连续改动。
- 不确定点：失败传播所用的状态字面量未在证据中出现，未证实；`add_node` 的 move 修复具体位置卡片未标注。

### seq 1378 | d5f5143a | 2026-08-31 | 分级 M | RT-007
- 做了什么：新增类型化 ArtifactStore，以 role 绑定 P1→P2→P3 artifact 链，提供 JSON 序列化回读与唯一 producer 语义；台账 RT-007 转 `PASS`。
- 落点：新增 `include/astrocs/core/artifact_store.h`（+108）、新增 `lib/core/src/artifact_store.cpp`（+177）、`CMakeLists.txt`、`tests/unit/CMakeLists.txt`、新增 `tests/unit/rt007_artifact_store_test.cpp`（+174）；包面 `tasks/RT-007/`（10 文件 +694 -1）。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面证据——新增声明 `ArtifactDescriptor::validate`、`to_json`、`from_json`、`store`、`get`、`bind_as_input`（形参含 `expected_role`）、`bind_p1_to_p2`（形参含 `cal_frame_id`）、`bind_p2_to_p3`（形参含 `hips_id`），与消息自述的 role 绑定／P1→P2→P3／JSON roundtrip 逐项吻合；"篡改检测"仅出现在台账验收列的 `artifact ID/hash/schema/unit/provenance` 字段名，未见对应函数名。
- 关联：为 seq 1409（afb0cf3e，P2-005 台账依赖列含 `RT-007`）、seq 1417（3227578a，P3-002 依赖列含 `RT-007`）、seq 1423（d6af18a1，P3-005 依赖列含 `RT-007`）提供跨阶段契约底座；其 `artifact_store.cpp` 在 seq 1379（84191084）的可达图中首次登记为生产可达节点。
- 不确定点：无。

### seq 1379 | 84191084 | 2026-08-31 | 分级 L（结构模式：CLI 拆分搬家）| RT-008（消息另带 CHK-001 号）
- 做了什么：把 CLI 接入唯一 Runtime——`cli/main.cpp` 由 1538 行削为入口壳，命令实现／参数解析／Runtime 客户端拆成三个独立编译单元；`astro_image_io` 侧加 cfitsio 互斥串行化；同提交刷新 CHK-001 生产可达图产物。按目录直方图归纳，不逐文件展开。
- 落点（34 文件 +2479 -1612，目录直方图）：`evidence/v6_1_rework` 14 文件 +687/-25（含 `tasks/CHK-001/PROD_REACHABILITY.dot` +31/-14 与同名 `.json` +51/-10、`tasks/RT-008/` 下 PLAN＋TASK_RESULT＋`logs/c01..c09`、台账行）；`cli/` 5 文件（新增 `commands.cpp`、`parser.cpp`、`runtime_client.cpp`、`runtime_client.h`；`main.cpp` 净 -1533；`cli/CMakeLists.txt`）；`lib/astro_image_io/src` 3 文件 +33（新增 `aio_cfitsio_mutex.h`、改 `aio_fits.cpp`、`aio_hips_reader.cpp`）；`lib/core` 3、`include/astrocs/core` 2（`runtime.h` 净 -27）、`tests/unit` 2（新增 `rt008_runtime_client_test.cpp`）、`tools/quality` 1、`lib/phase3_session` 1、根 `CMakeLists.txt` 1。
- 内容性质：生产代码 ＋ 测试 ＋ CI 或机器门（可达图产物重算）＋ 控制包或台账 ＋ 结构整理（main 拆分）。
- 意图证据：变更面证据强于消息自述——`commands.cpp` 文件头注释自述"RT-008 拆分自 main.cpp""本文件允许 include 科学内部头（CHK-001 只扫描 cli/main.cpp）"；`CMakeLists.txt` 新增静态库 `astrocs_cli_runtime`（链接 `astrocs_core`、`astrocs_module_adapters`）；`runtime.h` 的删除声明中 `IModule`、`ModulePlan`、`validate_config`、`plan`、`execute`、`inspect` 移出公共头。消息中的"CHK-001 REACH_PASS"与 `PROD_REACHABILITY.json` 新增节点 `cli/parser.cpp`、`cli/commands.cpp`、`cli/runtime_client.cpp`、`lib/core/src/runtime.cpp`、`lib/core/src/artifact_store.cpp`、`lib/core/src/module_adapters.cpp` 互证，同提交删除 `linked_symbol_count` 键。
- 关联：消费 seq 1377（d0114cd4）与 seq 1378（d5f5143a）的 Scheduler 与 ArtifactStore；其 `dispatch`、`cmd_drizzle`、`cfitsio_io_mutex` 的搬迁为 seq 1395（c626780f，P1-003 路径清理验证）准备前提；被 seq 1380（90cad495，RT-009）在同一 `cli/` 面上继续扩展。
- 不确定点：卡片同时把 `cmd_drizzle`、`spawn_frame_from_fits`、`read_double_key`、`read_str_key` 列在删除声明、又把 `cmd_drizzle` 列在新增声明，说明这是"搬家"而非"移除"；hp_drizzle 直连究竟在哪一条被真正切断，本片证据不足以断定（见 seq 1395 条目的同一缺口），未证实。

### seq 1380 | 90cad495 | 2026-08-31 | 分级 M | RT-009
- 做了什么：产出真实静态图与 observed 运行图（NodeTrace 采集、static／observed／L0 图与 sidecar、`graph` 子命令、运行图路径脱敏），并补录 seq 1379 拆分时未纳入构建清单的 `cli_common.h`；同提交落 G2 门清单。台账 RT-009 由 `NOT_STARTED` 变 `PASS`。
- 落点（24 文件 +1397 -5）：新增 `cli/cli_common.h`、`tools/quality/gen_run_graphs.py`（+276）、`tests/unit/rt009_node_trace_test.cpp`（+69）；改 `cli/commands.cpp`（+198）、`cli/parser.cpp`（+52/-1）、`cli/runtime_client.cpp`、`cli/runtime_client.h`、`include/astrocs/core/runtime.h`（`struct NodeTrace`）、`lib/core/src/runtime.cpp`、`tests/cli/test_phase123_pipeline.py`（+59/-1）；包面新增 `evidence/v6_1_rework/gates/G2/CHECKLIST.md`（+20）与 `tasks/RT-009/` 下 PLAN＋TASK_RESULT＋`logs/c01..c08`。
- 内容性质：生产代码 ＋ 测试 ＋ CI 或机器门（图生成器）＋ 文档或规格（G2 门清单）＋ 控制包或台账。
- 意图证据：消息自述五项均见变更面——`struct NodeTrace`、`collect_node_trace`（形参为 NodeTrace 向量）、`cmd_graph`、`write_run_graphs`、`sanitize_path`／`sanitize`；"路径脱敏"对应摘录注释自述"绝对路径 → 根占位＋末两组件；相对路径原样"；"sidecar source_commit"对应注释自述"当前 git HEAD 短 SHA（sidecar source_commit；无 git 环境返回 nullopt）"；输出落点为运行目录下的 `graph/static_graph.json`。
- 关联：台账 RT-009 依赖列 `RT-008;CHK-002`，即承接 seq 1379（84191084）与重叠区 seq 1369（1490bae5）；`gates/G2/CHECKLIST.md`（标题自述"G2 Gate Checklist — 唯一 Runtime 与类型化数据链 (V6.1)"）是本片唯一的门清单收口文件，标志 G2 段结束、G3（CPU/MON）段自 seq 1381 起。
- 不确定点：卡片显示本条亦改 `PROD_REACHABILITY.json`，但未见 `.dot` 同步更新，二者是否一致未证实。

### seq 1381 | 8df1a86d | 2026-08-31 | 分级 M | CPU-001 ＋ CPU-002（双任务合并提交）
- 做了什么：搭出 baseline／AVX2／AVX512 三个独立 provider 构建目标及其 manifest 生成器、self-test 与负例，并加 ISA 泄漏检查器；同提交实现 CPU 与有效配额探测（cgroup v1＋quota_signature）。台账 CPU-001、CPU-002 两行同时 `NOT_STARTED`→`PASS`。
- 落点（22 文件 +1467 -3）：根 `CMakeLists.txt`（+22，新增 `astrocs_cpu_avx2`、`astrocs_cpu_avx512` 两个 target 及其 PRIVATE 编译旗标）；`lib/backend_host/` 的 `avx2_backend.cpp`、`avx512_backend.cpp`、`backend_table.inc`、`hardware_inspect.cpp` 共 +35/-1；`tools/gen_provider_manifests.py`（新增 186）、`tools/quality/check_isa_leak.py`（新增 172）；`tests/unit/` 的 `cpu001_provider_selftest.cpp`、`cpu001_negative_test.cpp`、`CMakeLists.txt`；包面 `tasks/CPU-001/`（含 `logs/cpu001_manifest.json`）与 `tasks/CPU-002/`（含 `logs/c01.json`）。
- 内容性质：CI 或机器门（两个新检查器）＋ 生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面证据——对 `astrocs_cpu_avx2` target 以 PRIVATE 方式施加 AVX2／FMA 旗标、对 avx512 target 施加 F／DQ／BW／VL 四组旗标；CMake 注释自述"编译隔离合同: 高级旗标仅作用于对应 provider target(PRIVATE)"与"两个变体 target 均不得被 astrocs(主 CLI) 链接 — 由 check_isa_leak 的 link map 验证把关"；PLAN 摘录自述检测面位 `ACS_FEAT_AVX512F`（DQ/BW/VL 子集声明在 manifest）。台账 CPU-002 验收列 `CPUID/XGETBV; affinity/cgroup/Job Object`。消息中的"cgroup v1"、"quota_signature"属自述，卡片未给出对应代码位置。
- 关联：CPU-002 台账依赖列 `CPU-001`，两任务合并进一条提交，该形态由 seq 1383（065aa340）对绑定生成／验证器的改造正式支持；本条是编号族从 RT 切到 CPU 的第一条（G3 段起点）。
- 不确定点：`backend_table.inc` 是新增还是修改（卡片"新增文件"清单未列，但变更面计入 `lib/backend_host`），未证实。

### seq 1382 | d3cd1a6c | 2026-09-01 | 分级 L（结构模式：v2 profile 换代）| CPU-003
- 做了什么：建立 v2 benchmark profile 生成与 `verify-profile` 复读——新增 profile 生成实现、CLI 子命令、schema 改版与既有 benchmark 测试面同步。按目录直方图归纳，不逐文件展开。
- 落点（23 文件 +2354 -104）：`lib/backend_host` 2 文件 +725/-3（新增 `profile_gen_v2.cpp` 634 行、`profile_gen.h` +52/-3）；`schemas/hardware_inspect.schema.json` +141/-34；`cli/commands.cpp` +84/-8（`cmd_verify_profile`）、`cli/parser.cpp` +31/-6、`cli/CMakeLists.txt`、根 `CMakeLists.txt`；`tests/backend` 6 文件 +29/-27（`bench_candidates_main.cpp`、`bench_harness_main.cpp`、`kernel_bench_main.cpp`、`kernel_oracle_main.cpp`、`test_cpu_profile.py`、`test_hardware_inspect.py`）、`tests/cli` 3 文件 +105/-24（`test_bench_cli.py`、`test_cli_build.py`、`test_cli_protocol.py`）、`tests/unit` 2 文件 +135（新增 `cpu003_profile_v2_test.cpp`）；包面 `tasks/CPU-003/`（含 `logs/c01_full_benchmark.jsonl` +865、`logs/c02_verify_profile.log`）。
- 内容性质：生产代码 ＋ 测试 ＋ CI 或机器门（schema 与 profile 校验）＋ 数据或产物（865 行 benchmark JSONL 入证据包）＋ 控制包或台账。
- 意图证据：变更面证据——`verify_profile_v2`（形参含 profile 文本与 `expected_commit`）、`raw_candidates_sha256`、`cmd_verify_profile`、`verify profile` 分派分支；`ASTROCS_COMMIT_SHA` 作为机器一致性输入；台账 CPU-003 验收列 `correctness first; memory; kernel/thread/block` 与"3 warmup＋7 measure"。消息自述的"12 kernel Oracle PASS"、"3 provider DSO 实测"、"864 raw candidates"、"AVX512<3% 保守规则"中，仅"864"可由 `c01_full_benchmark.jsonl` 的 +865 行间接同向印证（表头／末行差异未澄清），其余三项无可核对的变更面证据。
- 关联：依赖 seq 1381（8df1a86d）的 provider 与探测（台账依赖列 `CPU-001;CPU-002`）；其 v2 profile 语义直接触发 seq 1390（0a24e857）的既有 CLI 回归改写，并被 seq 1384（37e06ea8，CPU-004 路由）消费。
- 不确定点：**本条全卡标注"diff 读取截断"**，内容摘录区块不完整；"12 kernel"、"AVX512<3% 保守规则"、"3 provider DSO"均为消息自述，未证实。

### seq 1383 | 065aa340 | 2026-09-01 | 分级 S | CPU-003 ＋ RT-001（台账全量绑定与绑定校验器改造）
- 做了什么：把 `COMMITS.csv` 全量补录到 22 个任务，订正 `RT-001` 记录的 parent 完整 SHA，并把 commit 绑定生成器／验证器改造为支持双任务合并提交。
- 落点（4 文件 +49 -6）：`evidence/v6_1_rework/COMMITS.csv` +18、`tasks/RT-001/TASK_RESULT.json` +2/-2、`tools/quality/check_commits_csv.py` +13/-4、`tools/quality/gen_commits_csv.py` +16。
- 内容性质：控制包或台账 ＋ CI 或机器门（绑定验证器规则变更）。
- 意图证据：变更面证据明确——`check_commits_csv.py` 删除"同一 result_commit 不得被两个任务共享"的报错分支，`seen_commits` 由 set 结构改为以 commit 为键的列表结构，并新增注释自述"同一 commit 可由双任务合并提交共享，但必须 subject 声明两者"；`RT-001` 的 `parent_commit` 字段值由 `545bd4d47c13b8ed…` 改为 `545bd4d47c13c024…`（前 12 位相同、后段不同），同条 `sha256` 亦随之更换；18 条新增绑定行覆盖 `CHK-001..003`、`CPU-001..003`、`GOV-001/002`、`RT-001..009`、`VER-001`。
- 关联：是全片台账连续链的锚点，把重叠区（S25 主责的 seq 1366..1375）与本片主责前段（seq 1376..1382）一并绑定；与 seq 1385（f10edacc）构成 `gen_commits_csv.py` 的连续两步改造。
- 不确定点：原 `parent_commit` 错误值从何而来（生成器缺陷抑或人工写入）未证实；对 `commit_subject` 前缀的强校验删除后改为何种判定，卡片只给删除行不给新写法，未证实。

### seq 1384 | 37e06ea8 | 2026-09-01 | 分级 M | CPU-004
- 做了什么：实现逐 kernel 自适应路由——v2 profile 的机器一致性校验、provider 不支持时回 baseline 但保持多线程、输出 `kernel_routes`；台账 CPU-004 `NOT_STARTED`→`PASS`。
- 落点（14 文件 +570 -14）：新增 `lib/backend_host/cpu_routing.h`、`cpu_routing.cpp`（合计 +190）、新增 `tests/unit/cpu004_routing_test.cpp`（+155）、`cli/commands.cpp` +22、`cli/parser.cpp` +10/-12、`CMakeLists.txt`、`cli/CMakeLists.txt`、`tests/unit/CMakeLists.txt`；包面 `tasks/CPU-004/`（含 `logs/c01_routing.json`、`logs/c02_stale.log`、`logs/c03_unit_test.log`）。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面与消息自述一一对应——`validate_profile_v2_for_machine`、`route_kernel_from_profile`、`conservative_route`（形参 `kernel_id`、`available_cpus`）、`provider_supported`、`logical_available_from_hw`、`detected_features`；输出常量 `kernel_routes`、`fallback_reason`、`self_test_sha256`；日志名 `c02_stale.log` 与台账"stale／篡改 profile 被拒"的验收列同向。
- 关联：台账依赖列 `CPU-003;RT-006`（seq 1382、1377）；其 profile 校验语义变更立刻引发 seq 1390（0a24e857）的既有 CLI 回归改写，并被 seq 1419（c62daea4，P3-003 台账依赖含 `CPU-004`）复用。
- 不确定点："unsupported 回 baseline"所用的原因字面量未出现在证据中，未证实。

### seq 1385 | f10edacc | 2026-09-01 | 分级 S | CPU-004（台账绑定）
- 做了什么：把 CPU-004 绑定进 `COMMITS.csv`（累计 23 任务），并把生成器由"任务号→单条 commit 记录"改为"任务号→多条候选＋parent 匹配优先"。
- 落点（2 文件 +16 -11）：`evidence/v6_1_rework/COMMITS.csv` +1、`tools/quality/gen_commits_csv.py` +15/-11。
- 内容性质：控制包或台账 ＋ CI 或机器门（生成器）。
- 意图证据：变更面证据——`commit_of` 映射的值类型由单条记录改为记录列表、以 setdefault 方式追加候选、parent 回填循环改为对每条候选逐一取父；新增绑定行以 seq 1383 的结果提交为 parent、以 seq 1384 的 `37e06ea8` 为 result。消息自述"parent 匹配优先, 双任务合并提交支持"与 seq 1383 的改造方向一致。
- 关联：与 seq 1383（065aa340）构成生成器两步改造；自此各任务实现提交之后固定跟随一条单行绑定提交（seq 1387、1389、1392…直至 1430），本片绑定计数 23→45 连续无缺。
- 不确定点："parent 匹配优先"的选择逻辑新写法未摘录到（卡片仅给删除行），未证实。

### seq 1386 | 5b13e214 | 2026-09-01 | 分级 M | MON-001
- 做了什么：把资源监测接入每个 heavy node，运行自动生成 `resource_samples.csv`、`resource_summary.json`、`worker_balance.csv`，按 init／active／flush 分段并给 p50／p95 统计。
- 落点（13 文件 +582 -2）：新增 `cli/resource_recorder.h`（+250）、`cli/commands.cpp` +19/-1、`cli/monitor.h` +3、新增 `tests/unit/mon001_recorder_test.cpp`（+104）、`tests/unit/CMakeLists.txt`；包面 `tasks/MON-001/`（含 `logs/c01_run_events.jsonl`、`logs/c02_unit.log`、`logs/resource_samples.csv`、`logs/resource_summary.json`、`logs/worker_balance.csv`）。
- 内容性质：生产代码 ＋ 测试 ＋ 数据或产物（三份资源样本快照入证据包）＋ 控制包或台账。
- 意图证据：变更面证据——`write_all`（形参含 `out_dir`、`wall_total`、`sample_overhead_ms`）、`resource_samples.csv` 的输出拼接与表头字段 `elapsed_seconds`、`stage`、`cpu_pct`、`system_cpu_pct`、`active_workers`、`runnable_workers`、`rss_bytes`、`pss_bytes`、`commit_bytes`、`page_faults`、`read_bytes`、`write_bytes`、队列深度项；`worker_balance.csv` 表头含 `utilization_pct`；样本行的 `stage` 字段取 `active`；摘录注释自述"MON-001: 自动生成（无需操作者额外脚本; 开销<2% 由 summary.sample_overhead_ms 度量）"。
- 关联：台账依赖列 `RT-006;CPU-002`（seq 1377、1381）；`resource_recorder.h` 与 `commands.cpp` 的接点在 seq 1399（a0a8df2b，P1-004）被再改；产物名与根 `AGENTS.md` 登记的 CI 资源产物同名，但本片落点在 `evidence/…/logs/`（小样本快照），非 `run/resource/`。
- 不确定点：p50／p95 的计算实现仅见于消息自述与台账列，卡片摘录只给 `workers_p50` 一处，统计口径未证实。

### seq 1387 | 6c69848a | 2026-09-01 | 分级 S | MON-001（台账绑定）
- 做了什么：`COMMITS.csv` 追加 MON-001 绑定行（累计 24 任务）。
- 落点（1 文件 +1 -0）：`evidence/v6_1_rework/COMMITS.csv`。
- 内容性质：控制包或台账。
- 意图证据：仅消息自述（"MON-001: COMMITS.csv 绑定 24 任务"）；变更面证据为单行追加，与 seq 1386（5b13e214）构成 parent／result 对。
- 关联：紧接 seq 1386；与 seq 1389、1392 等共同构成本片固定成对模式。
- 不确定点：无。

### seq 1388 | 575f68e8 | 2026-09-01 | 分级 M | MON-002
- 做了什么：把资源门禁接入生产控制流——首 10 秒低 CPU 快速失败，结束时 `evaluate_gate` 失败即以 `RESOURCE`（退出码 10）加 diagnosis 退出。
- 落点（8 文件 +345 -1）：`cli/commands.cpp` +57、新增 `tests/unit/mon002_gate_test.cpp`（+126）、`tests/unit/CMakeLists.txt`；包面 `tasks/MON-002/`（含 `logs/c01_gate_ok.jsonl`、`logs/c02_unit.log`），台账 MON-002 `NOT_STARTED`→`PASS`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面证据充分——终态事件使用退出码常量 `RESOURCE` 与原因字面量 `resource_gate_failed`，并以 `diagnosis` 字段携带诊断文本；正常路径亦以 `RESOURCE` 作为返回值；`first-10s` 分支先发射 gate／error 事件再协作取消；注释自述"MON-002: first-10s 快速失败 — 首 10s 内若低 CPU 且非 IO 则取消（exit 10）"与"失败 → RESOURCE(10) + diagnosis"；PLAN 摘录引用规格出处 `04_TASK_SPECIFICATIONS.md` MON-002 与 `08_CPU_RESOURCE_ACCEPTANCE.md` §8，并记 manifest 标记为 incomplete。
- 关联：台账依赖 `MON-001`（seq 1386）；其门禁行为被 seq 1391（1c4b2802，MON-003）实测、被 seq 1399／1413／1425 的联合门复用；本条新增 include `resource_gate.h` 而非新增该头文件，说明门禁头早于本片已存在（本片区间内未见其创建）。
- 不确定点：门禁阈值（85%／60% 一类）未出现在本条证据字段中，未证实。

### seq 1389 | 16abea46 | 2026-09-01 | 分级 S | MON-002（台账绑定）
- 做了什么：`COMMITS.csv` 追加 MON-002 绑定行（累计 25 任务）。
- 落点（1 文件 +1 -0）：`evidence/v6_1_rework/COMMITS.csv`。
- 内容性质：控制包或台账。
- 意图证据：仅消息自述；变更面为单行追加。
- 关联：紧接 seq 1388（575f68e8），parent／result 成对。
- 不确定点：无。

### seq 1390 | 0a24e857 | 2026-09-01 | 分级 S | TEST（消息内指向 CPU-004）
- 做了什么：把 CLI 回归 `test_06`（类 `TestManifestVerify`）改写为适配 CPU-004 的 v2 profile 校验语义——先经 `benchmark cpu --quick` 生成合法 profile，再把 `build.source_commit` 改写为全零值构造 stale，断言退出码 5 与 stderr 含 `cpu profile invalid`。
- 落点（1 文件 +17 -14）：`tests/cli/test_cli_protocol.py`。
- 内容性质：测试。
- 意图证据：变更面与消息自述完全对齐——新增以 benchmark 快捷模式产出 profile 的调用、以篡改 commit 字段构造 stale 的分支、"stale profile 应返回 5（机器一致性失败）"的断言与 stderr 文案断言；同提交删除旧的本地 `cpu_signature` 比对与从 stderr 取回本机签名的断言。"35/35 PASS"属消息自述。
- 关联：是对 seq 1384（37e06ea8）的回归跟进，插在 MON-002（seq 1388／1389）与 MON-003（seq 1391）之间；亦回收 seq 1382（d3cd1a6c）对同一文件的改动。
- 不确定点：本条消息首行前缀为 `TEST` 而非任务号（卡片"消息中的编号"解析为 CPU-004），因此**没有对应的 `COMMITS.csv` 绑定行**（绑定计数在 25→26 之间跳过本条）；其是否有意不入绑定台账未证实。

### seq 1391 | 1c4b2802 | 2026-09-01 | 分级 M | MON-003
- 做了什么：在 2 核环境用合成 workload 验证资源门禁——5 个 kernel 各 ≥10s，多核生产路径通过、单核负 fixture 被拒；并把验证机环境干扰如实写入 PLAN 而不放宽阈值。
- 落点（7 文件 +453 -1）：新增 `tests/backend/mon003_synthetic_main.cpp`、新增 `tests/backend/test_mon003_synthetic.py`（两者合计 +278）、`tests/backend/CMakeLists.txt`；包面 `tasks/MON-003/`（含 `logs/c01_synthetic.log`、`logs/c02_gate_unit.log`），台账 MON-003 `NOT_STARTED`→`PASS`。
- 内容性质：测试 ＋ 控制包或台账（含环境约束记录）。
- 意图证据：变更面证据——新增声明 `run_kernel`、`lcg_f`、`struct SynKernel`、`class TestMon003Synthetic`，新增 include `baseline_kernels.h`、`monitor.h`、`resource_gate.h`；PLAN 摘录自述"单核负 fixture 会失败｜workers=1 → 5/5 门判为 single_threaded 拒绝"、"实跑：5 kernel 各 13-18s，多核 avg_cores 0.65-0.83／cpu% 65-83，单核 avg_cores 0.43-0.49"、小节标题"环境约束（如实记录，不放宽阈值）"及"2c2g 验证机有 DSH harness 常驻 node 进程（约 1.5GB RSS，D 状态，load 1.4-2.1，25% iowait）"。
- 关联：台账依赖 `MON-002;CPU-004`（seq 1388、1384）；其结论被 seq 1399（P1-004）、seq 1413（P2-007）、seq 1425（P3-006）以 `MON-003` 依赖形式复用，是 Phase 三段联合门的共同前置。
- 不确定点：PLAN 实记 cpu% 65-83 与台账验收列"归一化 CPU p50≥90、mean≥85"的关系（是如实记录未达标，还是另有归一化口径）在证据中未闭合，未证实；PLAN 引用的 `test_resource_gate.py`（V6 MON-003 fixture）不属本条变更面。

### seq 1392 | 0b58d3f4 | 2026-09-01 | 分级 S | MON-003（台账绑定）
- 做了什么：`COMMITS.csv` 追加 MON-003 绑定行（累计 26 任务）。
- 落点（1 文件 +1 -0）：`evidence/v6_1_rework/COMMITS.csv`。
- 内容性质：控制包或台账。
- 意图证据：仅消息自述；变更面为单行追加。
- 关联：紧接 seq 1391（1c4b2802）；G3 段（CPU／MON）到此收尾，seq 1393 起进入 G4（P1）。
- 不确定点：无。

### seq 1393–1394 | b0ad63c3, 21df35cf | 2026-09-01 | 分级 M, S | P1-001（实现 ＋ 绑定 27 任务）
- 做了什么：把 Phase1 的 8 类模块注册进 Runtime（descriptor＋端口＋执行类），执行委托给 Runtime，并同步调整 RT-005／RT-008 两个既有单测；随后追加台账绑定行。
- 落点：1393（10 文件 +411 -4）`lib/core/src/module_adapters.cpp`（+154）、新增 `tests/unit/p1001_modules_test.cpp`、改 `tests/unit/rt005_registry_test.cpp`、`tests/unit/rt008_runtime_client_test.cpp`、`tests/unit/CMakeLists.txt`；包面 `tasks/P1-001/`（含 `logs/c01_modules_test.log`、`logs/c02_rt005.log`），台账 P1-001 `NOT_STARTED`→`PASS`。1394（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面证据——新增声明列出 7 个 descriptor 名 `p1_cosmetic_descriptor`、`p1_star_psf_descriptor`、`p1_wcs_descriptor`、`p1_photometry_descriptor`、`p1_noise_snr_descriptor`、`p1_drizzle_descriptor`、`p1_writer_descriptor`，新增 include `astrocs/core/module.h`、`astrocs/core/module_adapters.h`；PLAN 摘录引用规格原文"注册 calibration、cosmetic、star/PSF、WCS/platesolve、photometry、noise/SNR、…"。
- 关联：台账依赖 `RT-008;CPU-004`（seq 1379、1384）；是编号族切到 P1（G4 段）的第一条；被 seq 1395（P1-003 依赖 `P1-001`）与 seq 1397（P1-002 依赖 `P1-001`）作前置引用。
- 不确定点：**消息自述"8 类"而卡片新增声明只抽到 7 个 p1 descriptor**，差额（calibration 是否沿用既有 descriptor 或未被 hunk 抽取）未证实。

### seq 1395–1396 | c626780f, 56da7faa | 2026-09-01 | 分级 S, S | P1-003（验证 ＋ 绑定 28 任务）
- 做了什么：以 CLI 层负例测试证明 Drizzle 路径清理达成（生产 callgraph 无 `hp_drizzle_run_hips`／`spawn_frame_from_fits` 直连、`cmd_drizzle` 仅 preset 入口、可达性检查 REACH_PASS）；随后追加绑定行。
- 落点：1395（5 文件 +196 -1）新增 `tests/cli/test_p1003_drizzle_path.py`（+58）、包面 `tasks/P1-003/`（含 `logs/c01_drizzle_path.log`），台账 P1-003 `NOT_STARTED`→`PASS`。1396（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：测试 ＋ 控制包或台账（**本组无生产代码文件**）。
- 意图证据：变更面证据为测试字符串常量 `hp_drizzle_run_hips`、`spawn_frame_from_fits`、`drizzle`、`preset`、`tools/quality`、`python3`（即以子进程调检查器）；PLAN 摘录引用规格"删除 CLI 的 `spawn_frame_from_fits`、直接 CFITSIO header 解析和 `hp_drizzle_run_hips` production call"。消息自述的"清理"动作本身不在本条变更面内。
- 关联：与 seq 1379（84191084）构成一对（后者搬迁 `cmd_drizzle` 并把 `spawn_frame_from_fits`、`read_double_key`、`read_str_key` 列于删除声明），本条只做证明；台账依赖 `P1-001`（seq 1393）；REACH_PASS 口径沿用重叠区 seq 1368（abb75893，CHK-001）。
- 不确定点：**"移除"这一动作的落点提交在本片区段内未被直接观测到**——P1-003 自身不含 `cli/` 生产文件改动，只能确认它在 seq ≤1395 前已成立（否则本条负例不成立），属推断链，未证实。

### seq 1397–1398 | 70c2701b, 14baed5d | 2026-09-01 | 分级 M, S | P1-002（实现 ＋ 绑定 29 任务）
- 做了什么：补齐 Phase1 生产路径 Oracle（stars／PSF 五场景含 completeness／FP／FWHM，WCS 扰动与无解，photometry 饱和拒绝），并同步 ABI 相关遗留测试期望；随后追加绑定行。注：本组编号 P1-002 在实现顺序上排在 P1-003 之后。
- 落点：1397（15 文件 +841 -12）`tests/backend/` 7 文件 +591/-11（新增 `test_p1002_gaps.py` +529；小改 `test_wcs_psf_oracle.py`、`test_drizzle_oracle.py`、`test_phase1_hotspot.py`、`test_drizzle_parallel.py`、`test_phase3_reproject_oracle.py`、`p3_session_probe.cpp`）；包面 `tasks/P1-002/`（含 `logs/c01_gaps_oracle.log` 与四份 `c02_test_*_oracle.log`），台账 P1-002 `NOT_STARTED`→`PASS`。1398（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：测试 ＋ 控制包或台账（本组无 `lib/`、`include/` 生产代码文件）。
- 意图证据：变更面证据——新增测试类 `TestStarsPsfOracle`、`TestWcsOracle`、`TestPhotometryOracle`，独立参考实现侧辅助函数 `render`（形参含图像缓冲、`W`／`H`、`Star`、背景值）与 `struct Star`、`phot`、固定种子 `rng`，include `star_detector.h`；台账验收列"测试调用 production module／CLI；独立 reference；容差预冻结"。
- 关联：P1 段内 seq 1393（P1-001）为前置；本条是 P1 段唯一触碰 Phase3 测试面（`p3_session_probe.cpp`、`test_phase3_reproject_oracle.py`）的提交；seq 1399（a0a8df2b，P1-004）以 `P1-002;P1-003;MON-003` 为依赖收口本段。
- 不确定点："五场景／饱和拒绝"的具体判据与容差数值未出现在证据字段，未证实。

### seq 1399–1400 | a0a8df2b, 157a1d06 | 2026-09-01 | 分级 M, S | P1-004（实现 ＋ 绑定 30 任务）
- 做了什么：建立 Phase1 数值与资源联合门——`resource_summary` 写入 run_id 并与 science manifest 互相引用，6 个 Oracle 与资源门在同一 commit 下全绿；随后追加绑定行。
- 落点：1399（9 文件 +310 -5）`cli/commands.cpp`（+3/-1）、`cli/resource_recorder.h`（+6/-3）、新增 `tests/backend/test_p1004_joint_gate.py`（+100）；包面 `tasks/P1-004/`（含 `logs/c01_run.jsonl`、`logs/c02_resource_summary.json`、`logs/c03_joint_gate.log`），台账 P1-004 `NOT_STARTED`→`PASS`。1400（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码（小面接线）＋ 测试 ＋ 数据或产物（run 事件与 resource_summary 快照入包）＋ 控制包或台账。
- 意图证据：变更面证据为 `write_all` 签名扩展（新增输出目录与开销形参）与新增测试类 `TestP1004JointGate`；台账验收列"科学 Oracle＋资源门同跑同 commit；2 核利用达标"。"6 Oracle 同 commit 全绿"属消息自述，卡片只登记日志文件名。
- 关联：把 seq 1386（resource_recorder）、seq 1397（P1-002 Oracle）、seq 1391（MON-003）三线合流，是 G4 段收口条；P2 段首条（seq 1401）台账依赖列为 `RT-008;MON-003` 而**不含 P1-004**，说明 Phase 之间按控制包依赖而非线性串接。
- 不确定点：science manifest 的文件名／字段未出现在证据中，未证实。

### seq 1401–1402 | 10d1c318, e8a07e02 | 2026-09-01 | 分级 M, S | P2-001（实现 ＋ 绑定 31 任务）
- 做了什么：并行化生产 Coverage 与 Sampler——改用 `std::thread`＋Runtime lease 出多 worker，移除 `P2_ENABLE_OPENMP` 默认关闭与 `_MSC_VER` 排除造成的生产串行；随后追加绑定行。
- 落点：1401（8 文件 +336 -26）`lib/phase2/sampler.h`、`sampler.cpp`（+26/-23）、`lib/phase2_session/p2_session.cpp`（+3/-2）、新增 `tests/backend/test_p2001_parallel_sampler.py`（+151）；包面 `tasks/P2-001/`（含 `logs/c01_parallel_sampler.log`），台账 P2-001 `NOT_STARTED`→`PASS`。1402（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面与自述双证——PLAN 摘录逐条给出验收对照：`sampler.cpp` 移除 OpenMP 条件改 `std::thread`、并行不再被 `_MSC_VER` 排除、`p2_session` 把 `budget.max_workers` 传给 `cfg.cpu_workers` 再到 sampler workers、每 worker 独立 reader `SamplerReader.init_own`、以 `std::atomic` 计数做动态分块、"生产无 `hardware_concurrency` 调用（仅注释提及）"；台账验收列 `worker-local readers; deterministic chunks`。
- 关联：编号族切到 P2（G5 段）的第一条；为 seq 1403（P2-002 依赖 `P2-001`）与 seq 1409（P2-005 依赖 `P2-001;RT-007`）开路；"lease 为唯一线程来源"口径沿用重叠区 seq 1373（RT-002）与 seq 1377（RT-006）。
- 不确定点：无。

### seq 1403–1404 | f37d4a09, eb116a25 | 2026-09-01 | 分级 M, S | P2-002（实现 ＋ 绑定 32 任务）
- 做了什么：并行化生产 UPM 求解——per-obs／per-control／per-frame 的 CG 走 Runtime lease，worker-local 规约＋定序合并，N==1 persist 结果一致；随后追加绑定行。
- 落点：1403（7 文件 +561 -134）`lib/phase2/upm.h`、`upm.cpp`（+271/-133）、新增 `tests/backend/test_p2002_parallel_upm.py`（+146）；包面 `tasks/P2-002/`（含 `logs/c01_parallel_upm.log`），台账 P2-002 `NOT_STARTED`→`PASS`。1404（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：PLAN 摘录自述逐条对应——"4 处并行（compute_raw／w／M／C）均以 std::thread 与 lease 的 worker 数执行"、"upm.cpp 生产无 hardware_concurrency／omp_get 调用"、"compute_raw 的 per-thread 局部和＋按 tid 升序合并（determinism class D1）"、"每线程局部数组约 KB 级，注释说明 2c2g 安全"、"取消点位于迭代／块边界"；删除行数（-133）与替换既有串行／共享数组实现的意图同向。
- 关联：与 seq 1401（10d1c318）同模块（`lib/phase2/`）连续推进，并被 seq 1405（P2-003 依赖 `P2-002`）、seq 1407（P2-004 依赖 `P2-002`）复用；台账验收列 `control weights; IRLS; per-frame solve; reductions`。
- 不确定点：`determinism class D1` 的定义位置（文档抑或代码注释）未在卡片证据中定位，未证实。

### seq 1405–1406 | 2211d0a9, 65668e4e | 2026-09-01 | 分级 M, S | P2-003（实现 ＋ 绑定 33 任务）
- 做了什么：建立调用生产 UPM 的三块 mini HiPS 接缝 Oracle（phase2 run 端到端；M 吸收背景差、C 校空间残差，before→after RMS 下降且源不被拟合）；随后追加绑定行。
- 落点：1405（6 文件 +402 -2）`tests/backend/phase2_fixture_main.cpp`（+62/-1，新增 `write_seam_frame`，形参含 `mode`、`offset`）、新增 `tests/backend/test_p2003_seam_oracle.py`（+171）；包面 `tasks/P2-003/`（含 `logs/c01_seam_oracle.log`），台账 P2-003 `NOT_STARTED`→`PASS`。1406（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：测试（含测试夹具 main）＋ 控制包或台账。
- 意图证据：变更面证据——新增容差常量 `AFTER_RATIO_TOL`（after／before RMS 比上限 0.35）；夹具函数 `write_seam_frame` 的 `mode`／`offset` 两形参对应背景差与空间残差两路注入；台账验收列 `mini HiPS; masks; stars; gradients; disconnected graph`。
- 关联：`phase2_fixture_main.cpp` 为本片跨条复用夹具，本条首次扩展，随后 seq 1413（fa677b5a，P2-007 再 +10）与 seq 1421（267b3ed0，P3-004 再 +36/-1）继续复用；seq 1411（P2-006）以 `P2-003;P2-004;P2-005` 为依赖收口。
- 不确定点：无。

### seq 1407–1408 | bb78ff6b, 01b8d8da | 2026-09-01 | 分级 M, S | P2-004（实现 ＋ 绑定 34 任务）
- 做了什么：重建排异／积分生产 Oracle（cosmic、低样本、NaN-ivar、AUTO 解析、integration weighted mean、all-rejected、frame identity）；随后追加绑定行。
- 落点：1407（5 文件 +325 -1）新增 `tests/backend/test_p2004_reject_integrate.py`（+176，内含探针入口与固定种子 777，include `astro/phase2/rejection.h`、`astro/phase2/integrate.h`）；包面 `tasks/P2-004/`（含 `logs/c01_reject_integrate.log`），台账 P2-004 `NOT_STARTED`→`PASS`。1408（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：测试 ＋ 控制包或台账（无 `lib/` 生产代码文件）。
- 意图证据：变更面证据为测试 include 生产头 `rejection.h`、`integrate.h`（即直调 production API）；台账验收列 `all methods; cosmic/hot/streak/stars; ivar` 与消息自述场景清单一致；固定种子支持可重复 fixture。
- 关联：与 seq 1405（P2-003）、seq 1409（P2-005）同期铺开 Phase2 科学／内存面，共同成为 seq 1411（P2-006 canonical pipeline）前置；台账依赖 `P2-002`（seq 1403）。
- 不确定点：AUTO 解析对应的接口名／参数名未出现在证据中，未证实。

### seq 1409–1410 | afb0cf3e, 2b44f9a8 | 2026-09-01 | 分级 M, S | P2-005（实现 ＋ 绑定 35 任务）
- 做了什么：验证 Block／IO 内存计划——block plan 有界、禁 35GB 稠密 cache、峰值 RSS ≤ 容差、覆盖完整；随后追加绑定行。
- 落点：1409（5 文件 +267 -1）新增 `tests/backend/test_p2005_block_io.py`（+131，探针含 `vm_hwm_kb` 读取 `proc/self/status`、include `astro/phase2/block.h`）；包面 `tasks/P2-005/`（含 `logs/c01_block_io.log`），台账 P2-005 `NOT_STARTED`→`PASS`。1410（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：测试（自读系统状态文件的峰值内存实测）＋ 控制包或台账。
- 意图证据：变更面证据为 `vm_hwm_kb` 与 `proc/self/status` 的取数方式、`astro/phase2/block.h` 的生产 include；台账验收列 `bounded cache; block edges; worker IO` 与"峰值 RAM 受 plan 约束；小块／full reference 等价"。"35GB"这个数字来自消息自述，卡片未给出比较表达式。
- 关联：台账依赖 `P2-001;RT-007`（seq 1401、1378），把 ArtifactStore 契约与 block plan 绑定；与 seq 1411（P2-006）的 write 节点语义衔接。
- 不确定点：`block.h` 本体是否同条改动——变更面不含 `lib/phase2/`，故本条为纯验证；"禁 35GB"的实际实现落点未证实。

### seq 1411–1412 | b27600d6, 2cb60258 | 2026-09-01 | 分级 M, S | P2-006（实现 ＋ 绑定 36 任务）
- 做了什么：组装 Canonical Phase2 Pipeline，注册 7 个 P2 模块 descriptor 成 IR 7 节点链（coverage..write），observed trace 逐节点匹配 static graph；随后追加绑定行。
- 落点：1411（9 文件 +459 -19）`lib/core/src/module_adapters.cpp`（+157）、`cli/runtime_client.cpp`（+35/-13）、新增 `tests/backend/test_p2006_canonical_pipeline.py`（+96）、改 `tests/unit/rt005_registry_test.cpp`、`tests/unit/rt008_runtime_client_test.cpp`（+7/-5）；包面 `tasks/P2-006/`（含 `logs/c01_canonical_pipeline.log`），台账 P2-006 `NOT_STARTED`→`PASS`。1412（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面证据精确对齐链长——7 个 descriptor 名 `p2_coverage_descriptor`、`p2_sample_descriptor`、`p2_upm_fit_descriptor`、`p2_upm_apply_descriptor`、`p2_reject_descriptor`、`p2_integrate_descriptor`、`p2_write_descriptor`；端口／artifact 常量成对出现（`coverage`→`calibrated`、`sample`→`samples`、`upm_fit`→`upm_model`、`upm_apply`→`corrected`、`reject`→`accepted_mask`、`integrate`→`integrated`）。
- 关联：与 seq 1376（c764c2be，RT-005）共用 `module_adapters.cpp`，与 seq 1380（90cad495，RT-009）共用 `runtime_client.cpp` 与 trace 采集面；台账依赖 `P2-003;P2-004;P2-005`；seq 1413（P2-007）是同段门收口，seq 1425（P3-006）是其 Phase3 同形态版本。
- 不确定点：无。

### seq 1413–1414 | fa677b5a, 9d03a5b7 | 2026-09-01 | 分级 M, S | P2-007（实现 ＋ 绑定 37 任务）
- 做了什么：Phase2 接缝与资源联合门——6 块 seam workload ≥10s，workers≥2 与 CPU≥90% 达门，科学证据与资源证据同次运行取得；随后追加绑定行。
- 落点：1413（6 文件 +282 -1）`tests/backend/phase2_fixture_main.cpp`（+10）、新增 `tests/backend/test_p2007_joint_gate.py`（+101）；包面 `tasks/P2-007/`（含 `logs/c01_joint_gate.log`），台账 P2-007 `NOT_STARTED`→`PASS`。1414（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：测试 ＋ 控制包或台账。
- 意图证据：变更面证据为对既有夹具 `phase2_fixture_main.cpp` 的复用扩展与新测试类 `TestP2007JointGate`；台账验收列 `production synthetic >=10s`、判定列"seam metrics pass and CPU/resource pass in same run"。阈值的两处数值（worker 数与 CPU 占比）来自消息自述，卡片未摘录断言行。
- 关联：复用 seq 1405（2211d0a9）的夹具与 seq 1411（b27600d6）的 canonical pipeline；台账依赖 `P2-006;MON-003`（seq 1411、1391）；与 seq 1399（a0a8df2b，P1-004）同形态。
- 不确定点：本片区间内只见 `gates/G1`（seq 1371）与 `gates/G2`（seq 1380）两份门清单文件，**未见 G3／G4／G5 的 CHECKLIST**，G5 是否另有收口文件未证实。

### seq 1415–1416 | 9e8ba103, f3fc3bbf | 2026-09-01 | 分级 M, S | P3-001（实现 ＋ 绑定 38 任务）
- 做了什么：Phase3 科学与算法冻结——把极区条件统一为单一"dec 绝对值 ≤85°"，同时订正 SCI 文档、API 文档、session 与 WCS 代码，附冻结项审计；随后追加绑定行。
- 落点：1415（11 文件 +259 -13）`docs/science/PHASE3_HIPS_TO_FITS.md`（+2/-2）、`docs/api/PHASE3_API_V1.md`（+2/-2）、`lib/phase3_session/p3_session.cpp` 与 `p3_session.h`（各 +1/-1）、`p3_wcs.cpp` 与 `p3_wcs.h`（各 +3/-3）、新增 `tests/backend/test_p3001_science_freeze.py`（+58）；包面 `tasks/P3-001/`（含 `logs/c01_science_freeze.log`），台账 P3-001 `NOT_STARTED`→`PASS`。1416（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：文档或规格 ＋ 生产代码（极小面）＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面证据强——一条提交内 docs 两处各 2 行、代码四处 1~3 行同步，属"多处一致"型改动；PLAN 摘录自述视场约束"dec 绝对值 ≤85°（距极点 ≥5°；TAN 极点退化显式拒，单一条件，SCI／API／session 三处一致）"、输出宽高区间 [1,20000]、`s_out`>0、"输出四角与中心同半球"，错误映射"越界→ `ACS_ERR_PARAM`"；hunk 头出现 `p3_wcs_make`、`p3_wcs_world2pix`、`p3_wcs_pix2world`、`enum P3WcsStatus`、`parse_request`。台账 P3-001 变更类别列为 `science`，PLAN 变更类别自述为 "documentation + validation"。
- 关联：编号族切到 P3（G6 段）的第一条；台账依赖列为 `CHK-003`（重叠区 seq 1370）；与同段 seq 1417／1419／1421／1423／1425 构成 P3 链，极区条件是 seq 1421（独立球面 Oracle）与 seq 1423（FITS 输出）的口径前置。
- 不确定点：代码实际只改 8 行（4 文件），"统一"是否已覆盖全部判定点（卡片未见其它条件残留证据）未证实。

### seq 1417–1418 | 3227578a, 01803fd7 | 2026-09-01 | 分级 M, S | P3-002（实现 ＋ 绑定 39 任务）
- 做了什么：properties／order／unit——`order_sel` 上限改取输入实际 order、reader／resampler 真正使用该 order、BUNIT 合同缺省 ADU 并移除 Jy／beam 硬编码；随后追加绑定行。
- 落点：1417（12 文件 +272 -10）`lib/phase3_session/hips_properties.cpp`（+2）、`hips_properties.h`（+1）、`p3_resample.cpp`（+9/-1）、`p3_resample.h`（+5）、`p3_session.cpp`（+9/-5）；`tests/backend/p3_output_probe_main.cpp`（+1/-1）、新增 `tests/backend/test_p3002_properties_order_unit.py`（+53）、`test_p3_output.py`（+3/-1）；包面 `tasks/P3-002/`（含 `logs/c01_properties_order_unit.log`），台账 P3-002 `NOT_STARTED`→`PASS`。1418（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面证据——新增声明 `p3_sampler_open_ex`（形参含 `product_dir`、`P3Sampler* out`，暴露 order）、新增 `max_order` 取输入 order 与 20 的下限并限幅；PLAN 摘录逐条自述"HipsProperties 增加 bunit 字段（解析 BUNIT 键，可缺）"、"session 的 max_order 取输入 order 与 20 的小者"、"leaf_nside 按 512 乘 2 的 K 次"、"缺省 ADU；移除硬编码 Jy／beam"。
- 关联：台账依赖 `P3-001;RT-007`（seq 1415、1378）；为 seq 1419（c62daea4，P3-003 依赖 `P3-002`）与 seq 1423（d6af18a1，P3-005 的 BUNIT／BITPIX 校验）提供前置；`p3_output_probe_main.cpp` 为跨条复用探针（亦见 seq 1417、1423）。
- 不确定点：BUNIT 缺省 ADU 的实际字面量位置未摘录到，未证实。

### seq 1419–1420 | c62daea4, 7d773cbb | 2026-09-01 | 分级 M, S | P3-003（实现 ＋ 绑定 40 任务）
- 做了什么：Runtime 并行 tile resampler——按 row-band 生成 work units，每 worker 独立 sampler 与 bounded tile cache，输出 buffer 不重叠，写 FITS 由单 writer 有序提交，禁用 hardware_concurrency；随后追加绑定行。
- 落点：1419（6 文件 +330 -16）`lib/phase3_session/p3_session.cpp`（+52/-15）、新增 `tests/backend/test_p3003_parallel_resampler.py`（+101）；包面 `tasks/P3-003/`（含 `logs/c01_parallel_resampler.log`），台账 P3-003 `NOT_STARTED`→`PASS`。1420（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面集中于单文件 `p3_session.cpp`（改动量与"删除串行全图双循环"自述同向）；PLAN 摘录逐条自述"rows_per_worker 行带划分"、"worker 内使用 `p3_sampler_open_ex`（其自含 bounded tile cache）"、"worker 数取 `host` 的 `budget.max_workers`（禁 hardware_concurrency）"、"采样并行、写 FITS 单线程（`p3_output_write_atomic`）"、"并行（budget=2）与单线程输出 FITS 逐字节一致"；台账验收列 `output tiles; per-worker cache; ordered writer`。
- 关联：台账依赖 `P3-002;RT-006;CPU-004`（seq 1417、1377、1384）；其并行 resampler 与 seq 1401／1403 的 Phase2 并行化同口径；被 seq 1421（P3-004 依赖 `P3-003`）与 seq 1425（P3-006 组装 `resample2` 节点）复用；`p3_output_write_atomic` 与 seq 1423 构成同一函数的连续演进。
- 不确定点：PLAN 引用规格中的"小图可串行阈值由 benchmark profile 确定且 <5s"，本条未见 profile 判定实现，未证实。

### seq 1421–1422 | 267b3ed0, 052f44c3 | 2026-09-01 | 分级 M, S | P3-004（实现 ＋ 绑定 41 任务）
- 做了什么：独立球面重采样 Oracle——常数场与解析场（cos²dec）对独立 TAN reference 比对、RA wrap、旋转 TAN、无人工接缝检查；随后追加绑定行。
- 落点：1421（7 文件 +410 -3）`tests/backend/phase2_fixture_main.cpp`（+36/-1，新增 `write_analytic_frame`）、新增 `tests/backend/test_p3004_spherical_oracle.py`（+186）、小改 `tests/backend/test_p2003_seam_oracle.py`（+2/-1）；包面 `tasks/P3-004/`（含 `logs/c01_spherical_oracle.log`），台账 P3-004 `NOT_STARTED`→`PASS`。1422（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：测试（含解析场夹具）＋ 控制包或台账（**本组无生产代码文件**）。
- 意图证据：变更面证据——新增 `write_analytic_frame`、`class TestP3004SphericalOracle`、include `healpix_core.h`，接缝检查辅助 `max_jump`（取最大跳变并做阈值比较）；台账验收列 `nearest/bilinear; cross-tile; RA wrap; boundaries`，判定列"Oracle 不调用 production lookup／WCS"。
- 关联：复用 seq 1405 引入的 `phase2_fixture_main.cpp`（跨 Phase 借用），并回头小改 seq 1405 的 `test_p2003_seam_oracle.py`；台账依赖 `P3-003`（seq 1419）；为 seq 1425（P3-006 联合门）供科学证据面。
- 不确定点：本条属 Phase3 任务却改动 Phase2 夹具文件，卡片未提供独立说明其复用理由，未证实。

### seq 1423–1424 | d6af18a1, 9a96b2c4 | 2026-09-01 | 分级 M, S | P3-005（实现 ＋ 绑定 42 任务）
- 做了什么：FITS 输出校验与 provenance——bitpix −32／−64 真实决定 buffer（原硬编码 −32）、补齐关键字／coverage／provenance；随后追加绑定行。
- 落点：1423（11 文件 +263 -6）`lib/phase3_session/p3_output.cpp`（+6/-1）、`p3_output.h`（+1）、`p3_session.cpp`（+3/-1）；`tests/backend/test_p3005_fits_output.py`（新增 +79）、`p3_output_probe_main.cpp`（+2/-1）；`tests/unit/p3_assembly_test.cpp`、`p3_output_test.cpp`（各 +1/-1）；包面 `tasks/P3-005/`（含 `logs/c01_fits_output.log`），台账 P3-005 `NOT_STARTED`→`PASS`。1424（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面与自述双证——PLAN 摘录自述"`p3_output.h`／`p3_output.cpp`：`p3_output_write_atomic` 增加 bitpix 参数（原硬编码 −32）"；验收对照行给出 FITS 关键字齐备（以独立 astropy 读取）、BUNIT 取 ADU（非 Jy／beam）并含 BSCALE／BZERO、coverage 扩展 HDU 存在、provenance 关键字 `HIPSID`、`ORDERSEL`、`SAMPLER`、`SWVER`、`RUNID`、"f64 精度保留覆盖区正值"；台账验收列 `BITPIX/BUNIT/WCS/checksum/atomicity/version/run/missing`。
- 关联：`p3_output_write_atomic` 在 seq 1419（P3-003）作为"单 writer"被引用，本条改其签名，两者构成同一函数的连续演进；`p3_assembly_test.cpp`、`p3_output_test.cpp` 的 1 行同步与重叠区 seq 1367（99a79200，VER-001 曾改同名文件）同属遗留测试面；台账依赖 `P3-001;RT-007`。
- 不确定点：checksum verify、mismatch hard fail 的实现在卡片摘录中未出现（只见于台账验收列），未证实。

### seq 1425–1426 | 3617b6c4, 26304309 | 2026-09-01 | 分级 M, S | P3-006（实现 ＋ 绑定 43 任务）
- 做了什么：组装 Phase3 生产 Pipeline 与资源门——IR 5 节点链 properties→wcs→resample2→writer→verify，≥10s，科学／资源／trace 三项同次通过；随后追加绑定行。
- 落点：1425（10 文件 +415 -17）`lib/core/src/module_adapters.cpp`（+113）、`cli/runtime_client.cpp`（+32/-13）、新增 `tests/backend/test_p3006_production_pipeline.py`（+101）、改 `tests/unit/rt005_registry_test.cpp`、`rt008_runtime_client_test.cpp`（+2/-2）、`tests/cli/test_phase3_inprocess.py`（+2/-1）；包面 `tasks/P3-006/`（含 `logs/c01_production_pipeline.log`），台账 P3-006 `NOT_STARTED`→`PASS`。1426（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：生产代码 ＋ 测试 ＋ 控制包或台账。
- 意图证据：变更面证据与链长精确吻合——5 个 descriptor 名 `p3_properties_descriptor`、`p3_wcs_descriptor`、`p3_resample2_descriptor`、`p3_writer_descriptor`、`p3_verify_descriptor`；节点／端口常量成对（`properties`／`hips`／`props`、`wcs`／`wcs_plan`、`resample2`／`resampled`、`writer`／`fits`、`verify`／`verified`），并含 IR 字段常量 `node_id`、`module_id`、`module_api`、`config`、`inputs`、`outputs`、`resources`、`class`、执行类字面量 `cpu_heavy`；台账验收列 `registry/IR/session removal; full synthetic`、判定列"SCI／ALG／API／CODE／TEST 闭合"。
- 关联：与 seq 1411（b27600d6，P2-006）为同形态跨 Phase 对应（同样改 `module_adapters.cpp`、`runtime_client.cpp` 并同步 rt005／rt008 两测）；依赖 `P3-004;P3-005;MON-003`；本条之后紧接 DOC 段（G7），是 Phase 实现的收尾。
- 不确定点：台账验收列的 session removal 在本条变更面中没有对应的 session 侧删除证据（`lib/phase3_session/` 未被触碰），该删除动作的落点在本片区段内未观测到，未证实。

### seq 1427–1428 | b67a9167, 635481e1 | 2026-09-01 | 分级 M, S | DOC-001（复验 ＋ 绑定 44 任务）
- 做了什么：DOC-001 的合同 front matter 与完整链复验并归档证据——36 合同图、生产可达、源索引、注释卫生四项检查全 PASS；随后追加绑定行。
- 落点：1427（7 文件 +188 -1，**全部在 `evidence/v6_1_rework`，代码面文件 0 个**）`tasks/DOC-001/` 下 `PLAN.md`、`TASK_RESULT.json` 与四份检查日志（`c01_contract_graph.log`、`c02_reachability.log`、`c03_source_index.log`、`c04_comment_hygiene.log`）及 `TASK_LEDGER.csv` 行（`NOT_STARTED`→`PASS`）。1428（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：控制包或台账 ＋ 数据或产物（检查器日志）。
- 意图证据：变更面证据——PLAN 摘录列出四项检查与机器门结论字面量：合同图检查给出 `CONTRACT_GRAPH_PASS`、contracts=36；可达性检查给出 `REACH_PASS`、18 entries、acr=0；源索引检查给出 `SOURCE_INDEX_PASS`、targets=79、missing=0（并记 vendored=239）；注释卫生检查记 501 扫描、0 违规。PLAN 另自述"## 说明 — 文档基础在 R0 系列已建立；本任务复验当前 SHA（含 P3-001..006 全部 G6 变更）的一致性"。**故本条的变更面性质是"复验与留证"，不是"重建"**：消息自述用"重建"，变更面只给检查结果与 PLAN。
- 关联：台账依赖 `P1-004;P2-007;P3-006`（seq 1399、1413、1425），即三个 Phase 全部收口后的文档门；被 seq 1429（e70f7d94，DOC-002）作为依赖引用；检查器本体（重叠区 seq 1368 的 `check_prod_reachability.py` 等）在此仅被调用。
- 不确定点：docs/contracts 下各合同的 front matter 实际是否齐备，本条无该目录改动证据（PLAN 以既有事实引用）；"重建"一词对应的历史改动点未证实。

### seq 1429–1430 | e70f7d94, 0756ecdf | 2026-09-01 | 分级 S, S | DOC-002（复验 ＋ 绑定 45 任务）
- 做了什么：当前版本追溯矩阵复验并归档证据——63 行追溯、66 claims 最终追溯、18 个 DATA artifact 全 PASS；随后追加绑定行（本片最后一条）。
- 落点：1429（6 文件 +160 -1，全部在 `evidence/v6_1_rework`，代码面文件 0 个）`tasks/DOC-002/` 下 `PLAN.md`、`TASK_RESULT.json` 与三份检查日志（`c01_traceability.log`、`c02_final_trace.log`、`c03_data_artifacts.log`）及 `TASK_LEDGER.csv` 行（`NOT_STARTED`→`PASS`）。1430（1 文件 +1 -0）`COMMITS.csv`。
- 内容性质：控制包或台账 ＋ 数据或产物。
- 意图证据：变更面证据为 hunk 头连续推进（`TASK_LEDGER.csv` 中 DOC-001 行被作为上下文触及）与三条日志文件名（名称自述检查种类：追溯、最终追溯、数据 artifact）；PLAN 标题结构为"目标／验收项与实现对照／检查命令／测试结果／说明"，与 DOC-001 同模板。台账验收列 `current source only; symbols/tests/evidence`、判定列"无 V5 默认表／硬编码计数；每行路径符号真实"。
- 关联：依赖 `RT-009;P1-004;P2-007;P3-006`（seq 1380、1399、1413、1425）；追溯检查器本体在重叠区 seq 1367（99a79200）曾改动（`tools/check_final_traceability.py`），本条为调用方；seq 1430 是 S26 末条，其后为 S27（seq 1431 起）。
- 不确定点：63 行／66 claims／18 artifact 三个数字仅见于消息自述与 PLAN 文本，卡片未给机器输出行，未证实。

---
