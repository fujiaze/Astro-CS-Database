# 逐条详析 （证据层过程件）

> 本文件是 设计大纲/reports/history/slices/H-S33.md 的逐条正文；最终交付只保留两份大报告，逐条正文下沉到证据层（条目文本零改动，仅移动）。
> 由 _tools/11_split_slice_reports.py 从入库基线 a3a343a4 重切，日期 2026-09-15；条目组 48 个；报告树保留段（主题归纳/重叠区核对/遗留缺口）见原文件。

## 主责条目（55 条，合并为 48 组，列全 seq 与 sha8）

### seq 1761 | fd9adf19 | 2026-09-09 | S
- 做了什么：为 P1-CAL-INT（calibration 模块集成，实现提交在重叠区 1760 | 58d20223）做 V8.1 台账收口：`evidence/v8_1_ci_control/STATE_RECONCILIATION.csv` 该行由 `NOT_STARTED / not_dispatched_no_evidence` 改写为 `CLOSED`（review_claim 取 `integration_complete`，摘要记"113断言+循环5次全绿+descriptor/typed_ports/真实trace"），`TASK_STATE.json` 同步 status/closed_commit/evidence_path/source_of_status/review_claim/current_evidence 六键（closed_commit=58d20223，evidence_path=run/local/agent_p1cal_int/）。
- 落点：evidence/v8_1_ci_control/（2 文件 +7/-7）。
- 内容性质：控制包或台账。
- 意图证据：消息自述"台账 CLOSED 同步（fg_machine_verified, commit 58d20223）"；变更面证据吻合——仅两本台账文件、纯 7 增 7 删的行级改写，无代码面文件。
- 关联：与 1760（实现/集成面）构成"实现提交 → 台账 CLOSED"成对模式；与同族 1763、1765、1767、1774、1777、1780 同型。
- 不确定点：无。

### seq 1762–1763 | 948dfcba, d01f919f | 2026-09-09 | L + S
- 做了什么：1762 | 948dfcba 把 cosmetic（装饰校正）域迁为独立 SHARED 模块 `astrocs_p1_cosmetic`：新增 `lib/cosmetic/src/module_entry.cpp`（C ABI v1 九操作 vtable：纯标量迷你 JSON、inline base64 平面、两阶段 strbuf、事务性输出、九回调统一异常屏障 `ACS_ERR_EXCEPTION` detail130）、`include/astrocs/cosmetic/types.h`（2 个 op 词表 / 8 个 config 键）、导出面收敛件 `astrocs_p1_cosmetic.def` + `module_exports.map`（AC_API 本地化 4 个 legacy 符号，nm 唯一导出 `astrocs_module_query_v1`）、`lib/cosmetic/CMakeLists.txt` 与模块 `module.yaml`；测试面新增 `tests/unit/cosmetic_adapter_test.cpp`（合 +759/-0）。1763 | d01f919f 为同一任务的台账 CLOSED 同步（同 1761 模式，closed_commit=948dfcba，摘要"cosmetic 3/3+全量107/107+BITWISE三通道+导出面唯一"）。
- 落点（逐文件可核）：lib/cosmetic/（6 文件 +1475/-14，其中 module_entry.cpp +1256、CMakeLists.txt +119、types.h +74、.def +3、.map +6 为新增，module.yaml +17/-14 为修改）；tests/unit/（cosmetic_adapter_test.cpp +734 新增、CMakeLists.txt +25）；根 CMakeLists.txt（+9/-1）；reports/P1-COS-IMPL-module-impl-report.md（新增 +100）；evidence/v8_1_ci_control/COMMIT_LEDGER.jsonl（1762 内 +2）；1763 为台账 2 文件 +7/-7。
- 内容性质：生产代码 + 测试 + 文档或规格（交付报告）+ 控制包或台账（1763）。分级 L 按文件集合归纳：实现面 6 文件、测试面 3 文件、构建面 1、报告 1、台账 1。
- 意图证据：消息自述逐项给出九操作、导出面唯一性双重实证（nm + dlsym 全 NULL）、"生产闭包实证修正：ac_api.cpp 为 calibration 全域 C API 转发层不可拆 → 4TU 全集编译"、DISP-003/002/004/011 处置；变更面证据吻合——7 新增 4 修改、零删除，报告文件与测试文件同提；"全量 ctest 107/107"仅见于消息自述（卡面无产物文件）。
- 关联：与 1758/1760（calibration 的 IMPL/INT，重叠区）同构（消息自述"CAL-IMPL 同款"闭包口径）；下接 1764–1765（同模块的集成 descriptor 面）。1763 的台账行同时是 1762 的验收回执。
- 不确定点：BITWISE 三通道与 107/107 的原始日志在 run/ 区（本片禁读且卡面不计入变更面），实测口径未证实。

### seq 1764–1765 | 5e7d7f46, dc2f5f3e | 2026-09-09 | M + S
- 做了什么：1764 | 5e7d7f46 给 cosmetic 模块补集成面：新增 `lib/cosmetic/integration/astrocs_p1_cosmetic.integration.json`（schema v1、contracts 五元组、dll 段"缺 DLL 必败"、typed_ports 4 端口含 DISP-COS-009 可选 NULL 恒等通道、plan 10 键冻结、trace 锚 RT-006、call_count per_node_sequence 六调用、`global_dag_registration=delegated`）+ module.yaml 端口注册同步；测试面新增 `tests/unit/cosmetic_integration_test.cpp` 7 段（三方一致/config 负面 7 例/两节点链调用计数恰一/真实 plan work_units==80 独立算术交叉/dll_sha256 标准 64 项 K 表三方交叉/重放无 repeated-call/负面 ABI 三态/manifest 端口负面 4）。1765 | dc2f5f3e 为台账 CLOSED 同步（closed_commit=5e7d7f46）。
- 落点：lib/cosmetic/（2 文件 +160/-0：integration/astrocs_p1_cosmetic.integration.json 新增 +152、module.yaml +8）；tests/unit/（2 文件 +1106/-0：cosmetic_integration_test.cpp 新增 +1072、CMakeLists.txt +34）；evidence/v8_1_ci_control/COMMIT_LEDGER.jsonl（+2）；1765 为台账 2 文件 +8/-8。
- 内容性质：生产代码（descriptor/端口合同）+ 测试 + 控制包或台账（1765）。
- 意图证据：消息自述显式登记四处偏差与一处**前任缺陷**——"CAL-INT/GAIA-INT 测试内 SHA-256 K 表仅 61 项错排 2 项（非 FIPS 标准，建议后续统一修 oracle）"；变更面证据吻合——本提交只新增 cosmetic 两文件与测试两文件，未触碰 CAL/GAIA 域，与该"只登记不改前任"的自述一致。
- 关联：紧接 1762；与重叠区 1752（CAT-GAIA-INT）、1760（P1-CAL-INT）为同一"descriptor+typed ports+调用计数"三部曲的第三次实现，消息自述点名的 K 表缺陷正落在这两个前任身上。
- 不确定点：卡面"消息中的编号"提取为 RT-006/RT-05（RT-05 系提取器对 RT-006 与文中序号的误拆），不作双任务号处理。K 表缺陷是否被后续片修复，本片无证据。

### seq 1766–1767 | c19b4a59, f83dd8f0 | 2026-09-09 | L + S
- 做了什么：1766 | c19b4a59 建立 HIPS 域可复用验证面 `p1hips`：在 `lib/astro_image_io/tests/p1hips/` 新增 9 个测试文件（fixtures / oracle / test_main / tests_{main,negative,oracle,perf,properties,selfcheck,units}.cpp）+ 该子目录 CMakeLists.txt（合 +2322/-0），含固定 seed F1/F3/F5/F6 的 f64/f32 双精度 fixture、独立 oracle（FITS 读回、MOC UNIQ 独立 3·2^k 式、SNR TSV、Fliegel-Van Flandern MJD、local↔FITS 索引映射，均不经被测函数）、六组 CTest 注册与 fork/execve 双注入点必败自检、hierarchy NESTED 局部序与行主序反查对拍。1767 | f83dd8f0 为台账 CLOSED 同步（closed_commit=c19b4a59，摘要"ctest p1hips_ 6/6+双注入必败+独立oracle"）。
- 落点：lib/astro_image_io/tests/p1hips/（11 文件 +2322/-0 全新增：CMakeLists +83、fixtures.hpp +203、oracle.hpp +344、test_main.hpp +201，tests_{main +25, negative +318, oracle +236, perf +105, properties +330, selfcheck +123, units +354}.cpp）；evidence/v8_1_ci_control/COMMIT_LEDGER.jsonl（+2）；1767 为台账 2 文件 +7/-7。
- 内容性质：测试（验证面建在模块自带 tests 子树内，非 tests/unit/）+ 控制包或台账（1767）。分级 L 按文件集合归纳：11 个文件全部新增，oracle/fixtures/六组测试同批。
- 意图证据：消息自述"独立 oracle 不经被测函数""selfcheck 递归 fork 缺陷修复（argv 子进程模式 + registry 初始化）"；变更面证据吻合——纯新增测试文件、零生产源修改、零删除。perf 三哨兵数值（0.51x/0.26x）与"6/6 PASS"仅见于消息自述。
- 关联：为后续 1770（hips_writer 迁独立模块）与 1781（tree_digest 归一化修复）、1803（DIGEST-001 多 HDU 修复）提供被测面；1770 的自述"p1hips 六组影子 5/5 主树 3/5，2 轮 flaky 定性 tree_digest"正是对本验证面的实测反哺。
- 不确定点：p1hips 落位在 `lib/astro_image_io/tests/` 而 p1wcs 落位在 `tests/unit/`（1779），两种放法的取舍依据在证据中未出现——未证实；消息自述"p1hips 9 文件"与卡面 11 文件（含 CMakeLists 与 test_main.hpp）存在计数口径差，本报告以卡面逐文件清单为准。

### seq 1768 | 858efc98 | 2026-09-09 | S
- 做了什么：`memory.md` 追加两条（+2/-0）：第 7 条沉淀本周期控制包执行模式（cp_run 串行单飞 DOC→TEST→IMPL 链依赖、修复批次线按文件域互斥并行、前台=调度员+验收官、completion≠PASS 需五门机器复跑 + diff 纯度抽验 + 禁区复核、SubAgent 零 git）；第 8 条为 2026-09-09 会话交接指针，登记"控制包 disarm 收尾，38/140 闭环、6 TEST + HIPS-IMPL 中断待重派、fatduck 真实数据拉取进行中（GaiaDR3/DR3SP/testdata 132G）"，正文指向 `run/local/HANDOFF_CONTROL_PACK_20260909.md`。
- 落点：memory.md（1 文件 +2/-0）。
- 内容性质：文档或规格（记忆/台账类）。
- 意图证据：消息自述"控制包执行模式指针 + 会话交接指针"与变更面证据完全一致（单文件两行新增，被指向的 HANDOFF 文件在 run/ 区不入库，故未出现在变更面）。
- 关联：与 1774（把 1768 提到的 6 个中断任务补成 CLOSED）、1783（把 disarm 结论订正为 42/140）构成同一条"记忆锚—台账锚"链。
- 不确定点：38/140 与 1783 的 42/140 口径差（见 1783 条目）在本条内无法判定谁是终值。

### seq 1769 | e54cd740 | 2026-09-09 | M
- 做了什么：建立 noise/snr 域可复用验证面 `p1noise`：`lib/snr_estimator/tests/p1noise/` 新增 8 文件（fixtures / oracle / test_main / tests_core / tests_main / tests_perf / tests_selfcheck + CMakeLists，合 +1843/-0），固定 seed 用 splitmix64 + Box-Muller 生成 f64/f32 噪声场，独立 oracle 覆盖 median·MAD·3x3 正规方程 LS·fill 解析式·gain·scale_law 全部不经被测函数（oracle.hpp 仅依赖 API 头 + std），fork+execve 必败自检，六组 + scale_law + fill 共 8 条 CTest。
- 落点：lib/snr_estimator/tests/p1noise/（8 文件 +1843/-0）；tests/unit/CMakeLists.txt（+16/-0）。
- 内容性质：测试。
- 意图证据：消息自述给出 8 条注册项、5 轮全绿用时（4.52–0.44s）与 asan+ubsan 树；变更面证据吻合——生产源零修改（lib/snr_estimator 的改动全部落在 tests 子目录）、16 行注册落在 tests/unit/CMakeLists.txt。
- 关联：与同批 PSF/SESSION/STAR/PHOT/WCS 验证面（1771、1772、1773、1776、1779）同构；台账收口不在本条而在批量收口件 1774。该域本身是 V7 残留态（`lib/snr_estimator` 未跟踪），后续 1787 自述登记"残留态如实登记不收编"、1814 才把根 CMakeLists 的断链引用摘除。
- 不确定点：消息自述"8 条 CTest 5 轮全绿"与 1774 台账行的"ctest p1noise_ 8/8重编后"一致；但 1793/1795 等处反复出现的 `p1_noise_adapter` 预存失败（F-AIO-001）与本验证面的边界（adapter 面 vs 域内面）在卡片层面未逐条对齐——部分未证实。

### seq 1770 | 1959dc89 | 2026-09-09 | L
- 做了什么：把 hips_writer 域迁为独立 SHARED 模块 `astrocs_p1_hips_writer`：新增 `lib/hips/src/module_entry.cpp`（生产闭包 2TU + vendored cfitsio 源 PIC 零改动重编译 + 九操作 C ABI v1 adapter）、`astrocs_p1_hips_writer.def` 与 `module_exports.map`（唯一导出 `astrocs_module_query_v1`）、`include/astrocs/hips/types.h`、`lib/hips/CMakeLists.txt`；测试面新增 `tests/unit/p1_hips/adapter_entry_impl.cpp` + `adapter_test.c`（direct-vs-plugin 产物树对拍、manifest base64 通道）；根 CMakeLists.txt 追加 add_subdirectory 与 RPATH；交付报告 `reports/P1-HIPS-IMPL-module-impl-report.md`（+80）。
- 落点：lib/hips/（7 文件 +1442/-9）；tests/unit/p1_hips/（3 文件 +979/-0）；根 CMakeLists.txt（+10/-1）；reports/（1 文件 +80）。
- 内容性质：生产代码 + 测试 + 文档或规格。分级 L 按目录直方图归纳（4 个目录、8 新增 4 修改）。
- 意图证据：消息自述给出导出面双定格口径（nm -D 唯一 T + dlsym 九 legacy `aio_hips_*` 全 NULL）、direct-vs-plugin 产物"文件集合 + 逐文件 size 相等、数据位级零差异，仅 CHECKSUM 注释秒级时间戳 4 字节"、以及"前任产物修正"清单（adapter_entry_impl 相对 include 少一级、module.yaml 交付态、导出面证明九符号实名）与"生产源零改动 git diff 空"；变更面证据吻合——lib/hips 的 -9 全部落在既有非科学文件（含 module.yaml / memory.md / CMakeLists），核心实现为新增文件。
- 关联：使用 1766 建立的 p1hips 验证面；自述把两组 flaky（tree_digest 含 CHECKSUM 秒级时间戳、perf parity4 贴 0.25 哨兵线）定性并"移交 TEST 域"，直接对应 1781 的修复与 1803 的多 HDU 二次修复。台账收口落在批量件 1774。
- 不确定点：卡面"代码样本"列出的 memory.md 为 lib/hips 模块内 memory 还是根 memory 无法从聚合面区分——未证实。

### seq 1771 | 2763386e | 2026-09-09 | M
- 做了什么：建立 dynamic_psf 域可复用验证面 `p1psf`（`lib/dynamic_psf/tests/p1psf/` 8 文件 1787 行）：固定 seed FIX-PSF-A..E（splitmix64 + Box-Muller，f64/f32/u16 同源）、独立 oracle 用 Moffat4 旋转主轴投影直接公式 + 解析积分 flux=2πA·sx·sy/3 + FWHM 因子 + M 矩阵特征轴谱与生产二次型对照 + LM 零共享代码；七组 CTest 注册（units·properties·oracle·negative·boundary·performance·selfcheck）、`ASTROCS_P1PSF_FAULT` 双向必败注入、前任 9 项测试自缺陷修正（U3 截断 vs 舍入、N1N4 返回码、2 main 拆分、B2 拟合中心、N8 全窗 NaN 必败等）。
- 落点：lib/dynamic_psf/tests/p1psf/（8 文件 +1787/-0）；tests/unit/CMakeLists.txt（+16/-0）。
- 内容性质：测试。
- 意图证据：消息自述关键在**生产缺陷候选登记但不改生产源**："DISP-PSF-007 候选 dpsf_fit_batch 无 w/h≤0 校验致 size 下溢 abort，不改生产源 fork 隔离断言移交 IMPL"；变更面证据吻合——本提交 8 文件全新增于 tests 子树、lib/dynamic_psf 生产源零修改。
- 关联：其登记的 DISP-PSF-007 在 1792 | ef1ccfcc（fix(PSF-001)）中被明确按编号整改；与 1769/1772/1773/1776/1779 同批，台账在 1774 批量收口。
- 不确定点：无。

### seq 1772 | ab2f6ade | 2026-09-09 | M
- 做了什么：建立 phase1_session 域可复用验证面 `p1sess`（`lib/phase1_session/tests/p1sess/` 9 文件）：固定 seed FIX-SESS-A..F（splitmix64）、独立 oracle 用手写大端 FITS 解码（不经 `aio_read`）+ ALG-CAL-001 冻结公式代数重算；核心断言面为"each node once"（manifest stages 调用账本 4 个声明节点各恰 1 次 + canonical 序 + cosmetic gated-off 不计数）与 failure propagation（缺文件·垃圾 master·尺寸不匹配·截断 FITS·取消·错误 ABI 六路径全断言不写完成 manifest、后续节点 0 次、无伪产物）；5 组 CTest + asan + ubsan + `ASTROCS_P1SESS_FAULT` 双向注入；本会话内修 3 处测试自缺陷（注入名单解析提取、selfcheck argv 子进程模式对齐 c19b4a59、p_cancelled 空指针守卫）。
- 落点：lib/phase1_session/tests/p1sess/（10 文件 +1713/-0）；tests/unit/CMakeLists.txt（+13/-0）。
- 内容性质：测试。
- 意图证据：消息自述点名的三项调用账本与失败传播语义即可直接映射到 1795（P1-001）"complete 门"与 1798（P2-001）/1808（P3-002）"call_count=1 + fail-fast 下游零调用"的口径——三处使用同一断言族；变更面证据吻合——纯新增测试文件，selfcheck 修正自述"对齐 c19b4a59"与 1766 实际引入的 argv 模式一致。
- 关联：上承 1766 的 selfcheck 框架口径，下为 1795 的 Phase1 真实节点链提供验收面（1795 变更面中 lib/phase1_session 8 文件含本域测试的 perf/properties 两处更新）。
- 不确定点：无。

### seq 1773 | 8e5dfab6 | 2026-09-09 | M
- 做了什么：建立 star_detector 域可复用验证面 `p1star`（`lib/star_detector/tests/p1star/` 9 文件 1683 行）：固定 seed FIX-STAR-A..G（mt19937_64 + splitmix）、独立 oracle 用 erf 解析盒内积分 + Simpson + FWHM 解析 + 全序比较器，零调用 `sdet_*`；flux 口径按 DOC README §5 冻结为"振幅 A"并以 1e-3 强断言（消息自述称修正前任 box 积分口径错误，mean_rel 0.93）；六组 CTest 4 轮 + asan 5/6 零 sanitizer 发现（negative 组因 malloc interposer 与 ASan LD_PRELOAD 先序架构性冲突标不适用）+ OOM 注入 N=3309..3314 六数组事务化 checkpoint rc=-1（配套 `sdet_oom_interposer.c`）；修前任 6 处框架缺陷与 GSL 域内 find_path/find_library 自探。
- 落点：lib/star_detector/tests/p1star/（9 文件 +1683/-0）；tests/unit/CMakeLists.txt（+12/-0）。
- 内容性质：测试。
- 意图证据：消息自述的"flux 口径 = 振幅 A（README §5 冻结）"是科学口径声明，变更面证据仅显示测试文件新增与该子树 CMakeLists，未见 README 修改，故该口径为沿用既有文档的解释而非本提交新立；"asan 5/6 不适用"作为显式例外写在消息自述中。
- 关联：与 1771/1772/1776/1779 同批成面；为 1795（P1-001）的 sdet 检测链提供被测面（1795 新增 astrocs_p1_sdet STATIC 并要求 PSF-001 边界修复先行）。
- 不确定点：negative 组 sanitizer 例外是否已在机器门/waiver 面登记，卡片无证据——未证实。

### seq 1774 | 5e45e65e | 2026-09-09 | S
- 做了什么：把 1769/1770/1771/1772/1773 五条的台账一次性收口：`STATE_RECONCILIATION.csv` 中 P1-HIPS-IMPL、P1-NOISE-TEST、P1-PSF-TEST、P1-SESSION-TEST、P1-STAR-TEST 五行由 NOT_STARTED 改 CLOSED（各带 review_claim、fg_machine_verified 与对应 sha），`TASK_STATE.json` 同步 5 组状态键，`COMMIT_LEDGER.jsonl` 补录 5 行（含 ae147df3、30296a9f 的后续行位）；合计 +41/-36。
- 落点：evidence/v8_1_ci_control/（COMMIT_LEDGER.jsonl、STATE_RECONCILIATION.csv、TASK_STATE.json，3 文件）。
- 内容性质：控制包或台账。
- 意图证据：消息自述"台账 CLOSED 同步（fg_machine_verified, commits 1959dc89/e54cd740/2763386e/ab2f6ade/8e5dfab6）+ COMMIT_LEDGER 补录 5 行"；变更面证据吻合——三本台账纯改写、零代码面文件；卡内"台账或CSV 行 新增/删除"配对恰为 5 组，与自述五条 sha 一一对应。
- 关联：本片的批量收口件；与逐条收口模式（1761/1763/1765/1767/1777/1780）互为替代，标志"五条测试面同批交付、一次性验收"。
- 不确定点：卡面"台账或CSV 行 新增"的 P1-NOISE-TEST 行文案含引号拼接痕迹（`tests_complete",ctest p1noise_ ...`），系机械抽取而非台账原样，不据此判定台账格式缺陷。

### seq 1775 | 19ef3722 | 2026-09-09 | S
- 做了什么：根目录整洁基线第一批：消息自述登记约 170 项散落项归位（100+ `astrocs_run_*.json` → `run/cli_runs/`、11 控制包 zip + 审核证据请求 + 包规范 → `工程控制/_control_packs/` 与 `工程控制/`、`Testing/` → `run/Testing_archive/`）；实际入库改动是两处分则文件——`.gitignore` 补 `astrocs_run_*.json`、`/Testing/`、`/out/`、`/AstroCS.wiki/` 与 CI 三产物；`AGENTS.md` "目录规范"节升级为强制基线（根目录固定条目清单 + 工作域落位规则 + 用户资料区禁改清单 + CI 固定路径 + 违规处理）。
- 落点：.gitignore（+9/-0）；AGENTS.md（+29/-4）。
- 内容性质：结构整理（规则面）+ 文档或规格。
- 意图证据：变更面证据显示**归位动作本身不产生入库 diff**（被移动的项全部落在 gitignore 区或未跟踪区），入库的只有忽略规则与规范文本；消息自述与变更面的差集即为"移动发生在版本库之外"的客观说明。
- 关联：与 1782（第二批归纳，实际产生 tracked 改名）、1785（gitignore 收紧并让 368 个已跟踪数据文件出库）、1784（AGENTS.md 目录规范同步）构成同一"根目录治理"事件簇；AGENTS.md 本节即现行目录规范的来源。
- 不确定点：170 项的实际去向仅有消息自述，无版本库证据可核——未证实。

### seq 1776–1777 | ae147df3, d342b3fa | 2026-09-09 | L + S
- 做了什么：1776 | ae147df3 建立 photometric_calib 域可复用验证面 `p1phot`（`lib/photometric_calib/tests/p1phot/` 14 文件 +3185/-0，本片最大测试面）：固定 seed FIX-PHOT-A..F（SplitMix64）、独立 oracle 覆盖 IRLS-Tukey·TAN+SIP·Akima·Simpson·XPSD 闭式·暴力配对·孔径，全部纯 stdlib 零被测头；新增 `p1phot_gaia_stub.cpp/.hpp`（gaia_client C ABI 桩，互斥链接）与 `p1phot_field_stub.hpp`；六组主树 3 轮 + asan + ubsan 全绿 + 双注入点 fork+execve 必败 + `fail_mode` env 运行时注入桩。1777 | d342b3fa 为台账 CLOSED 同步（commit=ae147df3）并补 COMMIT_LEDGER 行。
- 落点：lib/photometric_calib/tests/p1phot/（14 文件）；tests/unit/CMakeLists.txt（+40/-0）；evidence/v8_1_ci_control/（1777，3 文件 +9/-8）。
- 内容性质：测试 + 控制包或台账（1777）。分级 L 按文件集合归纳：14 个测试文件一次入库，全部新增，生产源零改动。
- 意图证据：消息自述在过程性修正上给了具体数值（gaia 桩 flux_min·flux_mul 漏写堆残留、MAD 偶数样本口径对齐、P4 全样本 IRLS 停机复刻、U7 SIP 量级对齐真实 Gaia·HSC），变更面证据吻合——这些修正均落在同批新增的桩与 oracle 文件内。
- 关联：其 SIP 量级对齐为 1779（P1-WCS-TEST）与 1791（WCS-002）的 AP/BP 口径铺垫；桩面与 1752（CAT-GAIA-INT）的 gaia 模块接口互为镜像。
- 不确定点：无。

### seq 1778 | 2e018dd0 | 2026-09-09 | S
- 做了什么：`TASK_STATE.json` 顶层 counts 字段重算同步：CLOSED 81→104、NOT_STARTED 108→85（total 191 不变）、`updated_utc` 由 2026-09-06T14:49:59Z 刷新为 2026-09-09T11:12:44Z；消息自述定性为"修复 counts 字段 12 周期未随闭环刷新的滞后"。
- 落点：evidence/v8_1_ci_control/TASK_STATE.json（1 文件 +3/-3）。
- 内容性质：控制包或台账。
- 意图证据：变更面证据与自述完全吻合——三行数值改写；"12 周期"这一自述给出的滞后量是本片唯一关于台账滞后的量化证据。
- 关联：紧随 1777（P1-PHOT-TEST 收口），并为 1780（P1-WCS-TEST 收口 + counts 104→105/85→84）建立新基线；与 1774 的批量收口构成"逐条收口不改顶层计数、批量补算一次"的模式。
- 不确定点：CLOSED 81→104 的 23 项增量与本片逐条收口任务数（1761/1763/1765/1767/1774/1777 共 11 项）不等，差额来源可能落在前片（seq ≤1760）累计未刷项——卡片无法逐项核对，未证实。

### seq 1779–1780 | 30296a9f, 9690c1da | 2026-09-09 | L + S
- 做了什么：1779 | 30296a9f 建立 plate_solve 域可复用验证面 `p1wcs`（**落位在 `tests/unit/p1wcs/`** 11 文件 +2172/-14）：6 个独立推导器（gnomonic 闭式·Cramer·inv(M)q·TAN 前向·固定点逆·AP-BP 语义逆）期望值全不经被测函数，fixture 真值直映 CD；六组主树 3 轮 + 影子 loop2·3 + asan/ubsan 5 组零报告 + selfcheck 四阶段双注入；重大发现登记为 DISP-WCS-007 候选（`WcsTan.pix2sky` deg 未转 rad 进球面公式，与 sky2pix 同族单位错成对抵消，第三方交叉差 3.2°）与 DISP-WCS-008 候选（AP/BP 网格拟合逆 42px 对冻结门 1e-4px 不可达，差 5 个量级）+ `iter_trans_solve` 空对 UB 状态依赖，均"锚锁现状未改码移交 IMPL"；前任 FIX-WCS-D 平移场构造不成立改注入。1780 | 9690c1da 为台账 CLOSED 同步 + counts 重算（104→105 / 85→84）。
- 落点：tests/unit/p1wcs/（11 文件）；tests/unit/CMakeLists.txt（+14 改写）；evidence/v8_1_ci_control/（1780，3 文件 +12/-11）。
- 内容性质：测试 + 控制包或台账（1780）。分级 L 按文件集合归纳。
- 意图证据：变更面证据显示**零生产源修改**（lib/plate_solve 未出现在本提交），与自述"锚锁现状未改码移交 IMPL"吻合；两条 DISP 候选的量化数值（3.2°、42px vs 1e-4px）出现在消息自述中。
- 关联：直接催生后续三件——1789（WCS-001 oracle 量纲收口）、1791（WCS-002 整改 AP/BP 网格与空对边界）、1805（WCS-003 布局扩展达冻结门），1796 更把 WCS-003 写成控制包任务；1779 也是 1781 的 tree_digest 修复与 1803 的 DIGEST 修复的平行先例（同样"测试面发现→后续修复"）。
- 不确定点：p1wcs 落位 tests/unit 而其余五域落位模块自带 tests 子树，规则未见于证据——未证实。

### seq 1781 | 64c1e988 | 2026-09-09 | M
- 做了什么：修复 p1hips 的 `tree_digest` 时间戳假败：`lib/astro_image_io/tests/p1hips/p1hips_tests_units.cpp` 的 `fnv_file` 对 .fits/.fts 头区（文件首至 END 卡、80 对齐逐卡、上限 360）扫描，把 CHECKSUM/DATASUM 卡第 11..80 列（value + 注释区）清零后再哈希；并同批登记两条杂项收编——7 个合同 schema 从根 `schemas/` 改名为 `contracts/schemas_tmp/`、`launch/` 2 文件改名为 `packaging/launch/`。
- 落点：lib/astro_image_io/tests/p1hips/（1 文件 +110/-9）；改名 9 项（schemas→contracts/schemas_tmp 7 项、launch→packaging/launch 2 项，均 0 行变化）。
- 内容性质：测试（验证面归一化内核）+ 结构整理（改名落位）。
- 意图证据：消息自述给出归一化范围选择的机理理由——"仅清注释 31..80 不彻底：CHECKSUM value 本身是含 DATASUM 时间戳注释的头区补码和 ASCII 编码（ffcsum/ffesum），跨秒时 value 区 8 字节必变"，并声明"数据区仍全字节哈希、科学敏感性无损失、解析失败退回全字节并留日志"、6 轮 ctest 含 sleep 2 强制跨秒 6/6 零 flaky；变更面证据吻合——改动只在测试文件、生产源零 diff。
- 关联：兑现 1770 移交的 flaky 定性；被 1803 | 45f80776 指认存在新缺陷（只扫第一个 END，多 HDU 产品第二头区漏归一化），是"两轮修复同一内核"的第一轮。其 schemas_tmp 落位是 1782 终态 `contracts/schemas/` 的中间态。
- 不确定点：卡面"消息中的编号：无"，与消息中提及的 vendored cfitsio 4.6.4 版本锚无编号可挂；schemas_tmp 中间态存在的原因（是否因与 1782 分两步提交）证据未说明——未证实。

### seq 1782 | 009ee419 | 2026-09-09 | M
- 做了什么：根目录归纳第二批的入库面：把 1781 暂存于 `contracts/schemas_tmp/` 的 7 个合同 schema 定名为 `contracts/schemas/`（R100 零内容差异），并更新 9 处引用（tests×5：backend/cli/traceability/version + tools×4：check_api_docs.py·check_version_consistency.py·pack_audit_package.py·quality/traceability/validate_cpu_profile.py + ci/impact_map.json 的 `schemas/**`→`contracts/schemas/**` 段，后者 +660/-36 为影响面重算）；消息自述另含 `launch/`→`packaging/launch/`、`graph/` 孤儿图产物→`artifacts/graph/`、CI 资源三 csv→`run/resource/`，以及版本一致性 SCAN_ROOTS 与 pack_audit CODE_TOPS 同步收敛。
- 落点：contracts/schemas（7 改名）；ci/impact_map.json；tests/backend(2)、tests/cli、tests/traceability、tests/version；tools/ 6 文件。
- 内容性质：结构整理（改名 + 引用改指）+ CI 或机器门（impact_map 与版本一致性扫描根）。
- 意图证据：变更面证据吻合"改名 + 引用改指"——19 文件、+671/-47，其中实质文本改动仅集中在 impact_map.json 的重算段与若干 1 行路径替换；"归并影响面复跑 45 passed + TRACEABILITY_PASS + VERSION_PASS + gen_source_index rc=0"为消息自述。
- 关联：与 1775、1785、1784 同簇；1782 的 `contracts/schemas/` 终态在 1814 的 `check_task_result_schema.py` 默认路径改指中被再次消费；1812 的 ci/tests 预存失败（test_impact_map schemas/**）登记为 memory F2，正源于本批 impact_map 重算。
- 不确定点：`graph/`→`artifacts/graph/` 与 CI 资源 csv 落位在变更面中不可见（`graph/` 属噪声前缀、`run/` gitignore），仅消息自述——未证实。

### seq 1783 | 32a252f8 | 2026-09-09 | S
- 做了什么：`memory.md` 追加 1 条（item 9）：登记旧 V7 MODULAR REFOUNDATION 控制包线（140 任务队列）disarm 终止于 42/140（41 submit + NOISE-IMPL 沿用旧包收尾）、根目录归纳两批锚点、新工程包交接锚、DISP 候选清单。
- 落点：memory.md（1 文件 +1/-0）。
- 内容性质：文档或规格（控制包/台账类记忆锚）。
- 意图证据：单文件单行新增与自述四项登记主题一致；被登记对象（控制包台账、DISP 清单）本身不入库。
- 关联：与 1768 同为会话交接锚；把 1768 的"38/140"订正为"42/140"，二者构成同一事件的两版口径。
- 不确定点：38→42 的差 4 项未列名，卡片无产物——未证实。

### seq 1784 | 4d3feaf4 | 2026-09-09 | M
- 做了什么：收录 cprun-v4 控制包模板到 `工程控制/cprun-v4-pack-template/`（10 文件 +428/-0：README.md、TEMPLATE.control-pack.jsonc 全字段注释版、control-pack.json 示例、evidence-template.json、tasks/ 下 AUDIT-001·CI-TEST·DOC-001·FIX-001·RELEASE-001·_TEMPLATE 六份任务模板）；AGENTS.md 目录规范节同步第二批归纳结果（graph→artifacts/graph、schemas→contracts/schemas、launch→packaging/launch、CI 资源 csv→run/resource/，+3/-3）。
- 落点：工程控制/cprun-v4-pack-template/（10 新增）；AGENTS.md。
- 内容性质：控制包或台账（模板）+ 文档或规格。
- 意图证据：消息自述标注来源为工作区外部路径（`/workspace/Local search/cprun-v4-pack-template`）且为"owner 指定审核包随附件"；变更面证据吻合——10 文件全新增、AGENTS.md 三行改指与 1782 落位一致。
- 关联：是 1796 新控制包目录结构（00_READ_FIRST/tasks/validators/TASK_LEDGER/control-pack.json）的模板上游；1802 的环境变更节亦提及"模板收录"。
- 不确定点：模板与 cprun-v4 工具版本的关系仅由目录名自证，无其他证据——未证实。

### seq 1785 | 789c5b6c | 2026-09-09 | L
- 做了什么：git 跟踪面瘦身：`.gitignore` 收紧使 `BASS DR3/` 用户数据区全目录不再跟踪，并让 `dist/audit` 旧 audit zip 出库；入库面为 368 个文件的删除登记（直方图：BASS DR3/dates 353、BASS DR3/tools 8、BASS DR3 6、dist/audit 1、根 1）与 .gitignore +4/-2；磁盘原地保留、禁改禁删不变。
- 落点：.gitignore（唯一非噪声面）；被摘除跟踪的 BASS DR3/ 与 dist/audit（噪声区 368、二进制 3）。
- 内容性质：结构整理（跟踪面收缩）；就噪声区的 368 文件而言属"数据区/运行产物不再入库"，不构成工作内容证据。
- 意图证据：消息自述给出动机（"审核包瘦身：git 跟踪面不再携带数据二进制"）与登记号 V81-ADOPT-002（zip 来源未知、冻结保留）；变更面证据吻合——唯一实质改动是 .gitignore，+4/-2961 的净删除全部来自噪声区清单。
- 关联：延续 1775 的"忽略规则先行"路线，把 1775 未收编的用户数据区一次摘除；AGENTS.md 目录规范的"用户/资料区原地保留、禁改禁删，gitignore"条款即为此类处置的现行依据。
- 不确定点：3 个二进制项的具体路径在卡面未逐条列出（完整清单在 index.jsonl.files），逐项内容不可核——未证实。

### seq 1786 | d8c821db | 2026-09-10 | M（GOV-001）
- 做了什么：宪章冻结与权威分层换轨：`ASTROCS_PROJECT_CONSTITUTION.md` 新增入库（+674/-0，状态 DRAFT_FOR_OWNER_REVIEW→FROZEN），§18 落定四项负责人裁决（① Phase3 首批投影冻结 TAN+SIN+CAR+AIT ② CPU 门禁采 §10.5 冻结值 平均≥85%/连续10秒<60%/单活跃线程即失败，替代旧 D.6 的 80%/75%/50% ③ Alpha 允许 Phase3 未验收仅发布 Phase1/2 且 manifest 明确 unavailable ④ 禁止第三方模块目录、只加载签名清单官方模块）+ supersession 条款与三处双值冲突消解；`AstroCS_ENGINEERING_CONSTRAINTS.md`（+21/-7）降级 ARCHIVED_NON_NORMATIVE 原文保留；`AGENTS.md`（+7/-6）把宪章升为唯一最高约束并登记根白名单；`docs/DOCUMENT_INDEX.yaml`（+11/-2）活动规范唯一收敛。
- 落点：根三份权威文件 + docs/DOCUMENT_INDEX.yaml。
- 内容性质：文档或规格（治理权威变更）。
- 意图证据：消息自述列出四项裁决与"官方检查器 DOC_INDEX_PASS + CONSTRAINTS_PASS + 特化断言 19/19"、证据 run `Rmtucy2cqced995/GOV-001/1.json`；变更面证据吻合——宪章为整文件新增、其余三处为口径改指与小节改写，代码面文件 0。
- 关联：本片后续条目凡引"宪章 §7.1/§7.2/§8.1/§8.2/§8.4/§10.4/§10.5/§18.1/§18.2/§18.4"均以上述冻结文本为源（见 1793、1795、1806–1812）；其裁决 2 的阈值直接成为 1809 的 `evaluate_frozen_gate()` 常量与 1812–1813 的争议对象，裁决 1 成为 1806 的 registry 冻结表内容。1796 起新控制包取代 1787 归档的旧包。
- 不确定点：AGENTS.md 的 10 项治理要素映射表是否在本提交成形的细节，卡片未展开——未证实（仅能确认本提交改了 AGENTS.md 7 行）。

### seq 1787 | 8e03d7da | 2026-09-10 | L（GOV-002）
- 做了什么：控制包归档与唯一活动源建立：V8.1 CI 控制包的 tracked 镜像 56 文件原样 mv 入 `engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/`（自述 SHA-256 零差异）并新增 README_ARCHIVED.md；新建 `工程控制/ACTIVITY_STATE.md`（+95/-0）作为唯一活动源（ACTIVE=ASTROCS-CONSTITUTION-ALIGNMENT-V1；V6.1/V8.1=ARCHIVED_SUPERSEDED；V7=ARCHIVED_DISARMED 42/140；V7 残留态如实登记不收编——`lib/snr_estimator` 与 `tests/unit/p1_noise` 未跟踪 + 根 CMakeLists 追加块；含唯一性/存在性自检与负向注入拦截）；DOCUMENT_INDEX 补归档条目并修断链（233→234）、memory item 10、REVIEW §3 活动包收敛（+13/-7）。
- 落点：engineering/control（57 文件，其中 56 项为改名/移动：active 镜像 25 + baseline 8 + ci 8 + templates 7 + tasks 4 + validators 4）；工程控制/ACTIVITY_STATE.md（新增）；REVIEW.md、docs/DOCUMENT_INDEX.yaml、memory.md。
- 内容性质：控制包或台账 + 结构整理（归档搬家）。
- 意图证据：变更面证据显示"零内容差异的移动"——56 项改名占 61 文件中的绝大多数、文本净增仅 +179/-17；消息自述显式留下 F1 待办（`ci/reconcile_state.py` 的 V71_LEDGER 常量待改指归档路径），该待办在 1788 兑现。
- 关联：是 1786 权威换轨的物理动作面；与 1788、1804 构成 GOV-002 三件套（归档→修断链→复跑验收）；其登记面更新在 1804 完成闭环。REVIEW.md 的"活动包收敛"与本条同批。
- 不确定点：消息中的 "SHA-256" 系文本提取器把哈希口径当作编号，非新任务号；被归档的 56 文件与本区其他片的关系需 S32 报告核对（本片无该报告）。

### seq 1788 | d1ac4dfc | 2026-09-10 | S（GOV-002-F1）
- 做了什么：把 `ci/reconcile_state.py` 的 `V71_LEDGER` 常量由旧 active 路径改指 `engineering/control/archive/2026-09-09_superseded_V8.1_CI_CONTROL_20260905/…`（+2/-2），修复 1787 物理归档后的断链：v71 三检查 fail、`--strict` 复跑 rc=1→0、checks_failed=0、verdict PASS。
- 落点：ci/reconcile_state.py（1 文件）。
- 内容性质：CI 或机器门（路径常量修正）。
- 意图证据：变更面证据（2 行常量）与自述的"改指归档路径"精确对应；rc 变化与 verdict 为消息自述，未入库。
- 关联：1787 自述 F1 的兑现件；1804 又把本条列为"已闭环"并记录负向注入可复现修复前 rc=1。
- 不确定点：无。

### seq 1789 | 86af39d8 | 2026-09-10 | S（WCS-001）
- 做了什么：p1wcs 独立 oracle 的量纲收口：对 oracle-1..6 逐一量纲审查（`oracle_gnomonic_inv` 漏 S 归一；`oracle_wcs_forward` 的 SIP 前向 px/度混淆已随 30296a9f 修复）；新增 `oracle_gnomonic_vec`（单位向量 + 切平面正交基的第二推导路径，不调生产、零第三方依赖）与 `o1_gnomonic_cross` 双路径交叉断言（512 点闭式 vs 向量法 <1e-9deg、跨路径逆 roundtrip <1e-10deg，用于把"单位错成对抵消"的概率压到≈0）；把死代码 `oracle_wcs_reverse_apbp` 接入 `o1_apbp_reverse_oracle` 三重一致断言（<1e-6px，AP 期望含生产 -1 线性归一约定）；两新注入名注册 FaultRegistry 并实测 env 注入必败 rc=1。DISP-WCS-007/008 生产缺陷候选保持域外禁改锚锁定、移交 IMPL。
- 落点：tests/unit/（4 文件 +198/-2：CMakeLists.txt、p1wcs_oracle.hpp、p1wcs_test_main.hpp、p1wcs_tests_oracle.cpp）。
- 内容性质：测试（oracle 面）。
- 意图证据：变更面证据与"只动 oracle 不动生产"一致（lib/plate_solve 未出现）；"临时探针实测 `oracle_wcs_reverse_apbp` 不动点=一步语义=真值 0.000px、压缩映射论证成立"为消息自述（探针未入库）；证据 run `Rmtucy2cqced995/WCS-001/1.json`。
- 关联：1779 登记的三处发现之一在此收口，另两处分别在 1791（WCS-002 空对/AP-BP 拟合）与 1805（WCS-003 布局扩展）兑现；1805 的"输出口径 u=x−crpix（WCS-001 冻结自洽约定）"即以本条冻结的口径为锚。
- 不确定点：卡面"编号: WCS-001, WCS-007"中的 WCS-007 与 1779 的 DISP-WCS-007 候选是否同一编号体系，证据未说明——未证实。

### seq 1790 | 99713034 | 2026-09-10 | M（DATA-001）
- 做了什么：冻结 Phase2/Phase3 不确定度产品合同 DATA-UNC-001（自述标为"目标态，实现不得先行"），新增 §30 四节：① Phase2 variance/ivar 产品（ivar_mosaic=wsum、variance=1/W、weight_mode=2 无 fallback，weight_mode≠2 或 fallback 不写 + manifest `uncertainty_available=false` fail-closed，invalid=NaN 同态对齐 §12.4 writer 通道）② rejection 产品（nused/nrej int32 子产品、逐帧 reason 级显式非目标、AIO 位 NREJ=32/NUSED=64 冻结、exchange planes 枚举零改动）③ provenance 五键（INPUT_MANIFEST_HASH/MODEL_HASH/UNCERTAINTY_AVAILABLE/WEIGHT_MODE/REJECT_PROFILE 双写）④ Phase3 传播（variance 优先/ivar 导出/皆无显式 unavailable 禁静默；nearest var_out=u_in，bilinear var_out=Σc_k²·u_k 冻结并加"禁 Σc_k=1 误归一"负向锚；负/Inf 损坏显式错误、NaN 传播、C=0→NaN 同态；VARIANCE/IVAR 扩展 HDU）；另加 ⑤ TEST-P2/P3-UNC-DESIGN-001 验证门；配套五处"现状节"目标态注记（DISP-P2HIPS-001/002 整改去向不变）、DATA_ARTIFACTS 5 schema 行、INDEX 95→100、科学公式两节（马赛克方差=SCI-DRZ-014 特例、重采样传播含 Σc_k²≠1 物理边界）、PHASE3 头部 supersession 注记。
- 落点：docs/contracts（3 文件 +300/-8）；docs/science（2 文件 +61/-1）。
- 内容性质：文档或规格（科学/数据合同冻结）。
- 意图证据：消息自述给出 owner_deferred=0 与验证口径（RED→GREEN + 负向注入 4/4 + 静态追踪 10 项 rc=0）与 F-UNC-001/002/003 预存域外登记；变更面证据吻合——五文件全为文档，代码面 0，印证"合同先行、实现不得先行"。公式以引用形式出现在合同文本中（本报告不作为源码引用）。
- 关联：是 1798（P2-001 integrate 落 §30.1）、1800（P2-002 落 §30.2/§30.3）、1807（P3-002 落 §30.4）三件实现的上位规格；与 1786 裁决 3（Alpha 期允许 Phase3 未验收、manifest 明确 unavailable）在同一 fail-closed 语义上互锁。
- 不确定点：`DESIGN-001`、`DRZ-014` 两个提取编号在变更面中无对应文件线索（疑为正文引用的 SCI-DRZ-014 与 DESIGN 文档锚），不作独立任务号处理。

### seq 1791 | 04556761 | 2026-09-10 | S（WCS-002）
- 做了什么：SIP 逆模型与空对边界整改：① `lib/plate_solve/src/ipv_wcs.cpp` 的 `extract_wcs_sip` 5.3 段 AP/BP 网格反变换 7×7→41×41、拟合阶→布局上限 5、各向归一化正规方程、AP[6]-=1/BP[1]-=1 约定、奇异 warn + 仅前向语义、Y-down 翻转、§5c 确定性顺序归约保持——消费方一步语义 roundtrip 42px→7.6438px（center90 密网格与独立 numpy 最优投影 7.644 一致）、对 10.88px 星点采样约 5.5×；② `iter_trans_solve` 空 guard 生产路径确定化（rc=success=0 + 确定 warn + 先检查索引后解引用，零 UB），negative 侧把 fork 隔离现状锚翻为三空对组合的确定性断言（全空 / U·W 空 pairs 非空 / U·W 非空 pairs 空，UB 崩溃即红），注入名 `n1_zero_pairs_anchor` 保持 env 必败 rc=1；③ F1 裁决移交：自述论证冻结门 1e-4px 在冻结 fixture 边缘畸变 ~117px 下数学不可达（5 阶 LSQ 最优投影迭代重线性化零增益，阶 15 仅 0.26px、收敛率 0.73 由逆映射最近奇点决定，畸变×0.1 即达标），锚保持 <50px 未放宽未伪造 PASS，并列出三条裁决路径（fixture 量级 / AP-BP 布局扩展+迭代语义 / 冻结门复核）。
- 落点：lib/plate_solve（1 文件 +41/-24）；tests/unit/（4 文件 +69/-24：CMakeLists、p1wcs_test_main.hpp、p1wcs_tests_negative.cpp、p1wcs_tests_oracle.cpp）。
- 内容性质：生产代码（解算器边界与拟合段）+ 测试。
- 意图证据：变更面证据与"改生产 + 同步翻锚"一致（生产 1 文件、测试 4 文件同提）；数学不可达性论证与 numpy 交叉值只出现在消息自述；证据 run `Rmtucy2cqced995/WCS-002/1.json`。
- 关联：本条的"一步语义 7.6438px"与"1e-4px 不可达"论证是 1796 负责人裁决 1（选 B：AP/BP 布局扩展 + 消费方迭代反演）与 1805（WCS-003 实施）的直接上游；1805 自述实测 7.601px 与本条 7.6438px 同量级零回归即指此。DISP-WCS-007（F3）本条未触碰。
- 不确定点：分级 S 而变更面 5 文件 +110/-48 且含生产代码，与卡面分级口径的关系（按权重 1 计）不反映改动实质规模——登记该口径差异，不作推断。

### seq 1792 | ef1ccfcc | 2026-09-10 | M（PSF-001）
- 做了什么：`dpsf_fit_batch` 批量 ABI 尺寸边界修复（DISP-PSF-007 整改，即 1771 登记的候选）：新增 `static dpsf_batch_dims_check`（`dpsf_psf.cpp:514-527`）拒绝 w/h≤0 并设 w*h>INT_MAX 的 int 索引寻址上界，五个批入口（batch / batch_f / batch_d / batch_f32 / batch_f64）统一接入，超界整体返 -1、零整图分配、零 sentinel 触碰；uint16 整图副本加 bad_alloc/length_error 的 try/catch 屏障（稳定 -1 不外抛）；逐星 patch 分配失败做星级隔离（INVALID_PARAMS + memset0/NaN 占位，不外抛跨 OpenMP/C ABI）；f32/f64 的 NaN 初始化规模 N*9 改按 int64 计并加入口 n>INT_MAX/9 拒绝；测试先红后绿（N4b fork 现状锚翻为 5 入口×{0,-1,INT_MIN} 双维 30 条确定性断言、N4c w*h>INT_MAX、新注入名 batch_boundary 必败 rc=1）；README r2 + module.yaml 行号锚复测 + source_symbols 补 2 符号。
- 落点：lib/dynamic_psf（6 文件 +364/-181：src/dpsf_psf.cpp 与 README.md、module.yaml 三个非测试文件 + tests/p1psf 的三个测试文件 p1psf_test_main.hpp、p1psf_tests_core.cpp、p1psf_tests_selfcheck.cpp）。
- 内容性质：生产代码 + 测试 + 模块文档同步。
- 意图证据：变更面证据与自述吻合（实现与断言同提、文档行号锚同步）；缺陷实证三形态（伪成功 rc=0、size_t 下溢 length_error→SIGABRT、h=INT_MIN 时 y1-y0 int 回绕→巨量分配→SIGABRT）与"主树 core 全组 rc=0 + perf 3.93x + asan 零报告"均为消息自述；证据 run `Rmtucy2cqced995/PSF-001/1.json`。自述声明 ABI 签名/科学公式/默认容差/状态码语义/OpenMP 结构零改动。
- 关联：1771 登记→1792 整改；1795 的"PSF-001 边界修复后入口"显式以本条为前置。域外登记：单星 `dpsf_fit` 与 `moffat4_fit_tmpl` 的 rect 巨大 patch 分配失败仍可抛（非 batch 域）。
- 不确定点：无。

### seq 1793 | 00b73fc1 | 2026-09-10 | M（AIO-001）
- 做了什么：建立唯一 AIO C ABI v1 与内容哈希复核。新增合同头 `include/astrocs/io/aio_abi_v1.h`（+167：aio_status 17 码数值冻结，0..7==acs_status 与 0..13==acs_fio_status 编译期 + 运行期双断言，14 DECL_INVALID / 15 HASH_MISMATCH / 70 INTERNAL 为 AIO 扩展；`aio_abi_info_v1` 严格 struct_size + abi_version handshake；4 个公共符号全 POD、零跨边界分配）、唯一事实源 `contracts/data/aio_abi_contract_v1.json`（+170：版本规则/状态码表/符号表/对齐矩阵/oracle 引用/scope_limits）、唯一实现 `lib/astro_image_io/src/aio_abi.cpp`（+210：SHA-256 复用 lib/common 单一实现、异常屏障家族口径、内容哈希复核收口——先验声明格式→流式重算比对→HASH_MISMATCH/TRUNCATED/DECL_INVALID 稳定码）；测试面新增 `tests/unit/aio_abi_tests.cpp`、`aio_abi_test_main.hpp`、`aio_abi_omp.hpp`、`aio_abi_selfcheck.cpp`（合 +835：FaultRegistry + fork 注入自检；oracle = hashlib 预生成 FIPS 向量 + lib/common Sha256 交叉对拍）。
- 落点：include/astrocs/io（1）；contracts/data（1）；lib/astro_image_io/src（1）；tests/unit（5）。
- 内容性质：生产代码（合同头 + 唯一实现）+ 合同数据（contracts/data）+ 测试。
- 意图证据：变更面证据为"头/合同/实现/测试"四件套同批、7 新增 1 修改，与"建立唯一"自述吻合；U4 确定性 100 次、U5 OpenMP 1/4/16 位级一致、3 注入名必败、主树 aio_abi 3/3 与全量 157 中 156 PASS 仅为消息自述；证据 run `Rmtucy2cqced995/AIO-001/1.json`。
- 关联：自述把本条锚到"基线审核 P2/P3 finding（内容哈希复核）"；三条域外登记在此立账——F-AIO-001（p1_noise_adapter 预存失败，V7 残留组合态）→ 1814 门卫化、F-AIO-002（tree_digest 只扫第一个 END 的多 HDU 缺口）→ 1803 修复、F-AIO-003（生产接线归 MOD-001）→ 1810 闭环。下接 1794（AIO-002 复用其状态码全域一致性做 _Static_assert 对齐）。
- 不确定点：卡面提取编号含 AIO-002/AIO-003/DATA-003/MOD-001/SHA-256，均为正文引用的后续归属而非本提交任务号，逐项无变更面对应。

### seq 1794 | 36f2edfa | 2026-09-10 | M（AIO-002）
- 做了什么：HiPS 原子发布与临时目录清理。新增合同头 `lib/hips/include/astrocs/hips/publish.h`（4 原语 `stage_create`/`stage_discard`/`tree_fsync`/`promote`；`aio_publish_status_v1` 数值 0..15/70/71 与 AIO-001 的 aio_status 全域一致并以 `_Static_assert` 给出对齐证明；5 个注入注册名，env=`ASTROCS_HIPS_PUBLISH_FAULT`；DLL 内隐藏符号、唯一导出面不变）、唯一实现 `lib/hips/src/aio_publish.cpp`（兄弟 staging 目录 `<parent>/.<base>.hips_staging.tmp`、同名残留自愈、kill 残留下次事务确定性清除、discard 递归幂等、fsync 后序"文件+目录"且 ENOSPC→DISKFULL=13 收敛、promote=整树 rename 原子 + 目标非空 STATE 拒绝）、`module_entry.cpp` 的 `write_product` 事务化（写 staging→finalize→fsync 树→原子 promote→out_dir 全有或全无；fail/cancel→abort+discard→无 partial），并顺带实证修复 finalize 成功路径的 abort double-free 隐患；测试面 `tests/unit/p1_hips/publish_atomic_test.c`（TEST-P1-HIPS-PUBLISH-001）+ adapter 幂等零态化 + CMake 注册 + module.yaml/README §9/双 memory.md 同步。
- 落点：lib/hips（8 文件 +761/-25）；lib/hips_p2（1 文件 +15）；tests/unit（3 文件 +750/-1）。
- 内容性质：生产代码 + 测试 + 模块文档（README/module.yaml/域内 memory）同步。
- 意图证据：变更面证据与"合同头+实现+测试+模块文档"四件套一致；自述把残留自愈动机锚到 V7 `make_tmp_dir` 20G tmpfs 事故（该风险最早由 1781 登记）；"生产源零 diff、scientific_change=false""全量 158/159 唯一 FAIL=p1_noise_adapter（F-AIO-001 预存）""F-AIO-002 维持移交未触碰""kill 用例改轮询等待（asan -O0 慢 10x，纯测试面修正）"均为消息自述；证据 run `Rmtucy2cqced995/AIO-002/1.json`。
- 关联：AIO-001（1793）→AIO-002（本条）→ 1795 的 writer 域真实化 → 1807 的 Phase3 扩展 HDU → 1810 的安装加载，是本片贯穿的 IO/发布链；1808 的 writer 节点委托 `p3_output_write_atomic_ex` 复用同一原子发布语义。
- 不确定点：`lib/hips/memory.md` 与 `lib/hips_p2` 的 15 行改动内容卡片未展开（疑为交付说明与构建注册）——未证实。

### seq 1795 | 9e09941a | 2026-09-10 | L（P1-001）
- 做了什么：Phase1 真实节点与 complete 门三域真实化。① star-psf 域：`lib/star_detector` sdet 检测 → star_det v1 [N,6]（mag=-2.5log10(flux) 真实换算）→ `dpsf_fit_batch_f64` Moffat4 FP64 批量拟合（float32 检测帧升 double 不降级，入口以 1792 的 PSF-001 边界修复为前置），DATA-P1-PSF 携 psf_params FLOAT64[N,9]，N>0 而全拟合失败即 DATA 拒绝；② wcs-platesolve 域：`sdet_create(maxStars=2000, fitRadius=0)` + `gaia_client_create(gaia_data_dir)` 句柄注入 → `ipv_solve_from_memory_with_callback_d`（FP64）→ IpvWcsResult → WcsTan roundtrip<1e-6px 自检；缺 ra0/dec0/focal_length_mm/pixel_size_um/gaia_data_dir 任一即 DATA 拒绝、禁 silent default；销毁顺序 ipv→sdet→gaia；Linux 侧 ipv 为源内 stub → fail-closed 如实报平台限制（节点代码真实调用非占位），Windows 侧同节点真实求解；③ writer 域：消费上游 p1_stack.hiss → `aio_hiss_inspect`/`read_tile_*` → NESTED 同构聚合（hiss tile 16×16 展开标准 leaf ipix → 512×512 tile，id=leaf>>18）→ AstroSphereTileView（flux_sum=HISS signal 通道，covered_area=support>0?A_cell:0）→ `aio_hips_product_begin`/`write_signal_support_tile`/`finalize` → IVOA 1.4 标准 HiPS（signal/support/properties/MOC）；nside>=512 为 IVOA 合同 fail-closed；DATA-P1-HIPS 登记 `covered_area_model=support_x_A_cell`。测试面新增 `tests/unit/p1001_real_nodes_test.cpp`（六组：主链改 7 节点 + 口径⑨下游零调用用例（drz FAILED→wr call_count==0 且无 p1_final.json）+ writer HiPS 断言 + wcs 平台合同断言 + complete 门/负向/确定性）；新增三 STATIC `astrocs_p1_dpsf`/`astrocs_p1_ipv`/`astrocs_p1_sdet`（GSL + gaia_client.c C11 + AIO_ENABLE_HEALPIX 门卫）。
- 落点：lib/core/src/module_adapters.cpp（+1144/-19，本条最大单文件）；lib/phase1_session（8 文件 +161/-46：p1_session.cpp +17/-1、README +49/-21、memory +58/-6、module.yaml +25/-11，另 tests/p1sess 三文件与 tests/test_p1_session_manifest.cpp 各 1–9 行小改）；tests/unit（3 文件 +714/-14：新增 p1001_real_nodes_test.cpp +682，rt005_registry_test.cpp、rt009_node_trace_test.cpp 同步）；根 CMakeLists.txt（+72/-3）；cli/（4 文件：CMakeLists.txt +20/-2、commands.cpp +4、parser.cpp +3、runtime_client.cpp +30/-13）；docs/contracts/PUBLIC_API.md（+24/-8）。
- 内容性质：生产代码（节点委托 adapters 与构建注册为主体）+ 测试 + CLI 面 + 文档或规格（合同）。分级 L 按目录直方图归纳：9 个目录 18 文件（17 修改 + 1 新增）。
- 意图证据：变更面证据与"三域真实化"一致（session/adapters/cli/contracts 同批），且**自证越界**——消息自述"白名单外 3 处越界登记于证据 `out_of_scope_entries`（根 CMakeLists / cli CMakeLists / PUBLIC_API.md）"，变更面确含根与 cli 构建文件，二者互证；"全量 ctest 159/160 唯一 FAIL=p1_noise_adapter=F-AIO-001 BASE 预存豁免""pytest 791 passed/13 failed 均为 BASE 预存集零新增""CLI phase1 端到端 rc=0""根 ninja 全量 0 error"仅为消息自述；结论绑定 BASE=36f2edfa；证据 run `Rmtucy2cqced995/P1-001/1.json`。
- 关联：前置为 1792（PSF-001）、1793/1794（AIO ABI 与原子发布）、1772（p1sess 调用账本口径）；本条遗漏 `tests/unit/CMakeLists.txt` 的 p1001_real_nodes 注册块 → 1799 补录；本条引入的 `lib/snr_estimator` 根 CMakeLists 引用（V7 残留 untracked）→ 1814 摘除；1796 的 ARCH-AUDIT-P1 与 1802 的交接快照均以本条为抽验对象；1808 自述"P1-001/P2-001 先例同构"引用本条模式。
- 不确定点：`lib/core` +1144 与 `lib/phase1_session` 的内部结构（是否全为节点委托薄层）卡片仅给 hunk 头清单，未逐函数核对——细节未证实。

### seq 1796 | e9547c02 | 2026-09-10 | L（控制包：负责人三裁决落地）
- 做了什么：把负责人三项裁决写成新控制包 ASTROCS-CONSTITUTION-ALIGNMENT-V1 的任务面并整体入库（39 文件 +2220/-0，全部新增）：新增 `tasks/WCS-003.md`（裁决 1 选 B：AP/BP 布局扩展 + 消费方迭代反演恢复冻结 1e-4px，拒绝缩 fixture 与改门，G-SCI 加挂 WCS-003 依赖、达门前不开）、`tasks/BASE-UTIL-001.md`（裁决 2 选 A：`utilization_p75_low` 独立归因、不得凭 BASE 预存豁免、四类归因含"门禁不适用"，挂 RT-001 依赖）、`tasks/P1-HIPS-DIGEST-001.md`（裁决 3 选 A：多 HDU 归一化，10 轮 × asan × 敏感/稳定双向矩阵，挂 AUD-001 依赖）、`tasks/ARCH-AUDIT-P1.md`（对 1795 的独立架构抽验五项：真实 PlateSolve / 真实 PSF 拟合 / 标准 HiPS / adapters 薄层 / 不完整链禁 complete，挂 AUD-001 依赖）；同步更新 RT-001/AUD-001 依赖链与 G-SCI 门。包骨架含 00_READ_FIRST、01_BASELINE_AUDIT、02_GATES_AND_EXECUTION、03_AUDIT_PACKAGE_SPEC、OWNER_BINDINGS.yaml、control-pack.json、TASK_LEDGER.csv、schemas/evidence.schema.json、validators/validate_control.py + selftest.py，以及既有 25 张任务卡（AIO-001/002、AUD-001、BASE-001、CI-001/002、CLI-001、DATA-001、DOC-001、GOV-001/002、MOD-001、P1-001、P2-001/002、P3-001/002、PACK-001、PSF-001、REAL-001、RT-001、VIS-001、WCS-001/002/003、WIN-001）。
- 落点：工程控制/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909/（tasks 29 文件、包根 7、validators 2、schemas 1）。
- 内容性质：控制包或台账（新控制包整体入库）。分级 L 按文件集合与目录直方图归纳，不逐文件展开。
- 意图证据：变更面证据为"整包新增、零修改零删除"，与"落地裁决 + 建立活动任务图"自述一致；任务状态口径（WCS-002=整改完成但科学 finding OPEN、P1-001 passed≠Phase1 产品可用）仅见于消息自述。
- 关联：structural_events 记本条为该目录 NEWDIR + PACKNEW（control-pack.json 首现）+ LEDGER（TASK_LEDGER.csv 首现）三重结构事件；是本片后段一切实现的派发上游（1798/1800/1803/1805/1806–1812 的编号全部可见于其 tasks/ 清单）；与 1787 的 ACTIVITY_STATE（ACTIVE=ASTROCS-CONSTITUTION-ALIGNMENT-V1）互为账实。
- 不确定点：本条无 owner 决策原文文件（04_OWNER_DECISIONS 在 1797 才入库），"三裁决"的原始载体在包外（负责人指令 / run 区，禁读），卡面无副本——未证实。

### seq 1797 | 85a5fc60 | 2026-09-10 | L（控制包 rev3）
- 做了什么：控制包 rev3 入库并写入负责人真实数据验收指令：新增 `04_OWNER_DECISIONS_20260910.md`、`CHANGELOG.md`、`tasks/REAL-000.md`（真实数据审计 + testdata/index.json v1.1→v1.2 + 确定性匹配工具与逐文件 sha256 清单 + 缺口量化：T2/T3 300s dark 缩放 53+94 帧、T2 Lum 平场缺失 15 帧、T1 显式空集）；扩写 `tasks/REAL-001.md`（三层验收 A 全量校准解算 553 帧 / B M42+银心完整 Phase1 353 帧单帧 HiPS 与 Phase2 4+5 滤镜马赛克 GaiaDR3+GaiaDR3SP 双分支 / C 机器证据与资源门，断点续跑 timeout 259200s）、`tasks/VIS-001.md`（固定拉伸参数预览 + 初审清单）、`tasks/WIN-001.md`（同候选真实数据复验子集 manifest 绑定）；`TASK_LEDGER.csv` 扩至 30 任务（PASSED×9、PASSED_FINDING_OPEN×1、IN_FLIGHT×1）、REAL-001 依赖 CI-002+REAL-000、VIS-001 转 repo-write、validate_control 的 CRIT 集扩至 30。
- 落点：同一控制包目录（包根 5、tasks 4、validators 1），10 文件 +1240/-923（新增 3、修改 7）。
- 内容性质：控制包或台账。分级 L 按文件集合归纳。
- 意图证据：变更面证据（删除 923 行集中于被改写的任务卡与台账、新增集中于 REAL/VIS/WIN 与 owner 决策）与"rev 扩写"一致；复跑口径 `validate_control --root` → CONTROL_PASS tasks=30 files=42 为消息自述。
- 关联：1796 同一控制包的第二版；其 REAL-000 指令由 1815 兑现（自述"04_OWNER_DECISIONS rev3 指令落地"、BASE=f74fc20f）；REAL-001/VIS-001/WIN-001 本片内均未开工，1802 交接快照登记"19 NOT_STARTED 与恢复派发顺序（REAL 线 owner 指令）"。
- 不确定点：923 行删除所替换的 rev2 原文未在卡片展开，逐条差异不可核——未证实。

### seq 1798 | 439f9f20 | 2026-09-10 | L（P2-001）
- 做了什么：Phase2 七个算法节点全部改为唯一真实 operation 委托（自述为 ARCH-P0-001 的 Phase2 侧整改：原工厂委托 `P2Api` session adapter 会让每个子节点调用完整 `p2_session_run`，7 节点链重复执行全链 7 次）。七项映射：①coverage→`p2_coverage_build`（两阶段容量协议）②sample→`p2_frame_id`（DATA-FRAME-ID-001）+ `p2_sample_controls_cached` 查询/回填 + `input_manifest_hash`（§20.3 同公式）③upm-fit→`p2_upm_build_geo` + `p2_upm_save`（production `use_ivar_weight=1` 按 SCI-UPM-WEIGHT-001 冻结，control_ivar 缺失显式拒绝）④upm-apply→`p2_upm_open` + `p2_upm_calibrate_block`（帧 HiPS signal 真读，fits→NESTED LUT 单一权威，support<=0 像素不进校正）⑤reject→`p2_reject_plan_resolve`（AUTO 仅 planning 层 wbpp_current group-level）+ `p2_collect_candidate_stack`（生产 strided 单一路径）+ `p2_reject_stack_ex`（reason 权威，UNDERDETERMINED 全接受并记录）⑥integrate→`p2_validate_candidate_weights` + `p2_integrate_pixel`（自述为 DATA-UNC-001 §30.1 目标态合同落地：weight_mode=2 逐样本 ivar 逆方差无 fallback、fail-closed，ivar==0 为合法零权，非有限/负 hard fail；`legacy_allow_weight_fallback=true` 是唯一显式等权降级出口并置 `uncertainty_available=false`；ivar_mosaic=Σivar_i 按帧索引序，variance=1/W；weight_mode=0 作为非科学方差面显式拒绝）⑦write→`aio_hips_product_begin`/`write_signal_support_tile`/`write_variance_tile`/`finalize` 四产品链（内建 1794 的原子发布；§30.1 `var_num_sum=cov²/W` 经 writer 通道归约；n_used=0→cov=0→NaN/NaN 同态；nside=2^(target_order+9)≥512 恒满足 IVOA；HIPS_VERIFY 回读 tile 数一致）。typed artifact 全链 p2_coverage.json→p2_samples.json→p2_upm_model.{bin,json}→p2_corrected.{json,bin}→p2_rejection.{json,bin}→p2_integrated.{json,bin}→mosaic + p2_final.json，output_dir 节点幂等创建；`p2_session` complete 门 fail-closed（partial + availability 7 域，P1 先例同构）；trace `call_count=1` + fail-fast 下游 `call_count=0`。测试面新增 `tests/unit/p2001_real_nodes_test.cpp` 7 组。
- 落点：lib/core/src/module_adapters.cpp（+1505/-10，主体）；lib/phase2_session/src/p2_session.cpp（+17/-1）；tests/unit（2 文件 +959/-0）。
- 内容性质：生产代码 + 测试。分级 L 按文件集合归纳（4 文件、1 新增 3 修改，但单文件 1505 行）。
- 意图证据：变更面证据显示"委托层厚化、session 侧仅 17 行"，与"唯一真实 operation 替换 session 工厂"的自述结构一致；测试面 959 行含 §30 数值 oracle（自述 ivar=2.5/variance=0.4/NaN 角落 4096）、UPM 帧间校正残差<5、1/4 worker parity、负向注入与 P1→P2 衔接面；自述特别登记"tests/unit/CMakeLists.txt 只 stage 本任务 hunk（P1-001 遗留 dirty 11 行保留工作区不混提）"——该 11 行在 1799 补交；"全量 ctest 唯一 FAIL=p1_noise_adapter（F-AIO-001 BASE 预存豁免）""p2001/p2002/p2006 pytest 6 failed 4 passed 与 BASE 通过集一致"为自述并给出失败归因（fixture 无 ivar 产品属 Phase1/DATA-001 域；p2006 t02 的 resource gate 事件缺失属 CLI 域），均移交 finding；结论绑定 BASE=9e09941a；证据 run `Rmtucy2cqced995/P2-001/1.json`。
- 关联：上位规格为 1790（DATA-UNC-001 §30.1）与 1794（原子发布）；同构先例为 1795（P1-001），同构后续为 1808（P3-002 五子节点）；其未闭环面（§30.2 nused/nrej 的 AIO 位 32/64 writer 无实现、§30.3 provenance 通道仅 pixfrac/scale）由 1800（P2-002）以合同 + 诊断面承载并三度移交。
- 不确定点：1505 行内部的节点分派结构卡片仅给 hunk 头清单，未逐一核对——细节未证实。`P0-001`、`ID-001`、`WEIGHT-001`、`UNC-001` 等提取编号系正文引用（ARCH-P0-001/DATA-FRAME-ID-001/SCI-UPM-WEIGHT-001/DATA-UNC-001）而非本提交任务号。

### seq 1799 | 8545d70a | 2026-09-10 | S（P1-001-F4）
- 做了什么：补录 1795 遗漏的测试注册块——`tests/unit/CMakeLists.txt` +11/-0（add_executable + 三条 include 路径 + link `astrocs_module_adapters`·`astrocs_core`·`astrocs_phase1_session` + add_test）。自述说明缺陷机理："工作树测试一直跑绿但 HEAD 构建产物缺该目标"，且该 11 行是由 P2-001（1798）刻意留在工作区不混提的 dirty 块。
- 落点：tests/unit/CMakeLists.txt（1 文件 +11）。
- 内容性质：测试（构建注册）+ 结构整理（工作区 dirty 收编）。
- 意图证据：变更面证据恰为 11 行新增，与自述"11 行"完全对得上；"工作树绿而 HEAD 缺目标"属构建态描述，卡面无独立产物。
- 关联：直接修补 1795；与 1798 的"只 stage 本任务 hunk"声明互为因果，是本片"漏-补"模式的一例。
- 不确定点：无。

### seq 1800 | 9e0fa3a8 | 2026-09-10 | M（P2-002）
- 做了什么：Phase2 不确定度排异与 provenance 产品面。新增唯一事实源 `contracts/data/phase2_uncertainty_rejection_provenance_v1.json`（+104：§30.2 AIO 子产品位分配 NREJ=32/NUSED=64 冻结登记、§30.3 provenance 五键冻结表 INPUT_MANIFEST_HASH/MODEL_HASH/UNCERTAINTY_AVAILABLE/WEIGHT_MODE/REJECT_PROFILE、诊断平面不入 science planes 的 F-UNC-003 零断链、pending AIO 通道与 findings 登记面）；新增 `tests/unit/p2002_unc_rej_prov_test.cpp`（合 +826：§30.2 kernel 语义直调正确 gather 契约 + 集成投影对拍 + §30.3 五键真实值对拍 + unavailable 显式登记 + F-UNC-003 只读断言 + 2 帧链确定性/1v4 parity + 故障注入 `ASTROCS_P2002_FAULT=proj|prov` 双模式 rc=1 必败）；`lib/phase2` 的 CMake 注册 16 行与 phase2 memory 登记。
- 落点：contracts/data（1 新增）；tests/unit（2 文件 +826）；lib/phase2（1 文件 +23）。
- 内容性质：合同数据 + 测试 + 构建注册。
- 意图证据：变更面证据显示**生产源零修改**（自述 scientific_change=false），实现只落合同/测试/CMake；"全量 161/162 唯一 FAIL=p1_noise_adapter（F-AIO-001 预存豁免）""pytest 6F4P 与 BASE 通过集一致"为自述；BASE=8545d70a；证据 run `Rmtucy2cqced995/P2-002/1.json`。
- 关联：承接 1798 遗留的 §30.2/§30.3 未落地面并向上位合同 1790 对齐；本条自述移交三 finding——F-P2-002-01（P0，`lib/core` 的 `p2_op_reject` gather `value_stride=sizeof(double)` 违反契约 + 3 元素缓冲堆越界读，ASAN 实证 rejection bins 失真非确定，depth=2 kernel 永不触发故 P2-001 未暴露）→ 由 1801 当场修复；F-P2-002-02（P1，integrate 不剔除 kernel 拒绝样本，§30.2 n_ineligible 恒等式部分拒绝场景不成立）与 F-P2-002-03（AIO 域 writer int32 通道与 `ASTROCS_*` 键通道未实现）本片内未修。
- 不确定点：卡面"UNC-003"与自述"F-UNC-003"是否同一登记线，卡片未给出编号权威表——未证实。

### seq 1801 | 991e3e2e | 2026-09-10 | M（P0 / F-P2-002-01）
- 做了什么：`p2_op_reject` gather 契约三重错位紧急整改。①`gin.value_stride` 由 `sizeof(double)` 改为 `tile_span`（每帧元素跨度），自述以三处权威为证（rejection.cpp:1185 索引 `s*stride+pixel`，stage2.cpp:1086 与 acr_kernels.cpp:121 同证）；②`values` 的 3 元素逐像素缓冲改为每 tile 各帧 `tile_span` 元素连续拼接、depth×tile_span frame-major 平面整体传入（scratch 复用 assign 语义，内存与原 tiles_v 同量级，零堆越界）；③修复中新确认的第三处同面错位——`gin{}` 零初始化使 `value_dtype=0`（fp32）而 corrected bin 实为 fp64，kernel 以 float 宽度解释 double 位模式（ASAN READ of size 4 同证），改为显式 `value_dtype=1`；自述给出量化对照"只修①② mismatch 122414→58215，三处全修 mismatch=0"。测试面 `test_f_p2002_01_rejection_parity` 等 RED→GREEN 四组断言（生产 bins==kernel 重放、void 角落 nrej==1→0、depth=3 双跑 bitwise、1v4 parity），Fixture2 模板化与 KernelRun eligible_count 仅测试面。
- 落点：lib/core/src/module_adapters.cpp（+32/-16）；tests/unit/p2002_unc_rej_prov_test.cpp（+213/-4）。
- 内容性质：生产代码（缺陷整改）+ 测试。
- 意图证据：变更面证据是两处小面积修改（生产 48 行、测试 217 行），与"契约错位三处 + 断言四组"自述相称；自述声明科学公式/容差/状态码语义/kernel/plan 零改动；"主树 ctest 161/161 + asan 树 p2002 修复前 heap-buffer-overflow abort@rejection.cpp:1185 → 修复后 PASS 零报告 + p2001 PASS"为消息自述；证据落 `run/p2003_fix/`（red/green 日志、asan 前后对比、baseline 快照 sha256），该片不入库；BASE=9e0fa3a8。
- 关联：是 1800 移交 F-P2-002-01 的当场兑现；与 1802（紧随其后的交接快照）构成"P0 已修 + 剩余 F 清单"的交接口径；F-P2-002-02/03 本条明示未触碰、仍在账。
- 不确定点：1815 之前未见对 F-P2-002-02/03 的处置证据——本片内未证实。

### seq 1802 | eb315b20 | 2026-09-11 | S（docs handover）
- 做了什么：`HANDOVER.md` 更新为 ASTROCS-CONSTITUTION-ALIGNMENT-V1 的执行快照（HEAD=991e3e2e）：12 任务闭环提交链、WCS-002 finding OPEN 口径、19 NOT_STARTED 与恢复派发顺序（REAL 线 owner 指令）、P3-001 中断遗留面甄别指引、P0 F-P2-002-01 已修、F 全清单、WCS-003/BASE-UTIL-001/P1-HIPS-DIGEST-001/ARCH-AUDIT-P1 四新节点、环境变更（/tmp bind 磁盘 + GC cron + 模板收录）、V7 残留与预存 dirty 锁死清单、审核包重打指引。
- 落点：HANDOVER.md（1 文件 +113/-89）。
- 内容性质：文档或规格（交接台账）。
- 意图证据：变更面证据为单文件改写（+113/-89 与"快照刷新"相称）；所有清单细节仅存在于文档正文与消息自述，卡面不展开文档内容。
- 关联：把 1796/1797 控制包面与 1798–1801 实现面缝合成可续作的派发序；其"P3-001 中断遗留面甄别指引"被 1806 自述认领（"HANDOVER §4 P3-001 中断遗留工作面正式续作认领"）；"19 NOT_STARTED"与 1797 的 30 任务台账互为账实。
- 不确定点：环境变更项（/tmp bind、GC cron）属主机侧动作，仓库内无证据——未证实。

### seq 1803 | 45f80776 | 2026-09-11 | M（P1-HIPS-DIGEST-001）
- 做了什么：p1hips tree_digest 的多 HDU 归一化修复（即 1793 登记的 F-AIO-002、1796 裁决 3 选 A 的任务）：`p1hips_tests_units.cpp` 的归一化内核由 `fits_header_end` 单 END 扫描改为 `normalize_fits` 全文件 80 对齐卡位扫描，覆盖全部 HDU 头区；自述给出缺陷机理（64c1e988 只扫第一个 END，Moc.fits 等多 HDU 产品第二头区的 CHECKSUM/DATASUM 卡漏归一化，秒级 UTC 时间戳入哈希致 asan 树 p1hips_properties nt=4 稳定假败）与实测布局依据（cfitsio 4.6.4 主头 pad 2880、BINTABLE 扩展头 15 卡不 pad 直接落数据、CHECKSUM@3840 DATASUM@3920 均在第一个 END@480 之后、文件尾零 pad，故逐 HDU 公式推进会漏位，改取全文件卡位扫描）；并写明取舍与危害方向（误清零仅当数据区恰含 80 对齐的 CHECKSUM/DATASUM 卡字样，splitmix64 浮点 fixture 不可能生成；漏扫危害远大于误扫；白名单外字节一律不动）。新增专项验收 harness `p1hips_digest_verify.cpp`（EXCLUDE_FROM_ALL、不入 CTest 常规矩阵，跨秒慢测不进 CI）+ CMakeLists 注册：自述 37 断言覆盖单/多 HDU 归一化清零、payload 不动、非白名单卡不动、sleep 2 强制跨秒 tree_digest 位级一致、第二头区注释翻转 digest 不变（稳定回归锚）、Moc.fits/tile 数据区翻转 digest 必变（敏感性双向）。
- 落点：lib/astro_image_io（3 文件 +349/-40：tests/p1hips/p1hips_tests_units.cpp、新增 p1hips_digest_verify.cpp、该子树 CMakeLists.txt）。
- 内容性质：测试（含独立验收 harness 与构建注册）。
- 意图证据：变更面证据显示"生产源零改动、写入面全在 tests/p1hips/ 3 文件"，与自述"零越界"一致；10 轮主树 6/6 + asan 树 6/6 + harness 37 断言 ×10 轮 + asan 1 轮、以及"缺陷对照 git show 64c1e988 老版同 harness 实测 4 项关键断言 FAIL 证明矩阵可区分缺陷/修复"为消息自述；证据 run `Rmtvuwx3i0c5cb8/P1-HIPS-DIGEST-001/1.json` + `run/p1hips_digest_001/`（32 文件）。
- 关联：修复 1781 引入的第二层缺陷（同一内核的两轮修复：1781 只解决单 HDU 时间戳、1803 解决多 HDU 覆盖）；兑现 1796 裁决 3 与 1793 的 F-AIO-002 移交；harness 采用 EXCLUDE_FROM_ALL 的取舍与 1766/1770 的 perf 哨兵线问题同族。
- 不确定点：harness 的 37 断言与"缺陷可区分"的实测证据在 run/ 区，禁读且未入库——未证实。

### seq 1804 | 45695873 | 2026-09-11 | S（GOV-002 复跑验收）
- 做了什么：`工程控制/ACTIVITY_STATE.md` §5 补记 BASE-001 冻结参照核对实况并更新 §6 F1 状态（+21/-5），`memory.md` item 10 同步两处（+1/-1）。核对结论（自述）：V8.1 baseline/V7_1_STATIC_TASK_LEDGER.csv 镜像 blob `32e3e414` 自 a4fdee3f 入库即现行，与 BASE-001 freeze_snapshot 的工作区口径 `524ab719`（zip 原件/解压件）内容不一致，属**历史口径差、非本次归档动作引起**；以 `git R100 56/56` 证明 8e03d7da 的归档移动零内容差异；三份冻结台账的另两份 `ee6a5d7e`/`b1ebbf5c` 一致；§6 F1 标记为已由 d1ac4dfc 闭环（`reconcile_state --current-first --strict` rc=0、9/9，负向注入可复现修复前 rc=1）。
- 落点：工程控制/ACTIVITY_STATE.md；memory.md。
- 内容性质：控制包或台账（活动状态登记）。
- 意图证据：变更面证据为两文件登记面改写（零代码、零包文件移动）；所有 blob 哈希、rc 与轮次均为消息自述 + 登记文本，卡面无产物。
- 关联：为 1787（归档）+ 1788（修断链）的验收回执件；其 BASE-001 冻结参照口径与 1796 包内 tasks/BASE-001.md 对应；自述"主提交 8e03d7da + d1ac4dfc 已在 HEAD 祖先（BASE=45f80776）"。
- 不确定点：`32e3e414`/`524ab719` 两个 blob 口径差的根因（zip 打包环节 vs 镜像生成环节）证据未指明——未证实。

### seq 1805 | b8d9a69c | 2026-09-11 | M（WCS-003）
- 做了什么：兑现负责人裁决 1 选 B 的 AP/BP 布局扩展与消费方迭代式反演，使实现能力达冻结门 1e-4px。①`SIPCoeffs` 布局扩展：追加扩展逆向布局 `APx/BPx[100]`（索引 i*10+j，i+j<=apx_order<=9）+ `apx_order` 字段，A/B/AP/BP 36 项兼容层偏移零变（自述 C ABI `IpvWcsResult` 为独立 POD、orchestrator 仅走 C API、wcs_transform 36 契约零影响）；②`ipv_wcs.cpp` 5.4 段扩展逆向拟合：网格 41×41→81×81、拟合阶 5→7（布局容量 9 内）、各向归一化正规方程、`APx[10]-=1/BPx[1]-=1` 约定，5.3 段 36 项兼容层代码零改动；③新增 `wcs_sky_to_pixel_iterative` 迭代式反演（消费方迭代语义的生产参考实现，替代一步直加）：TAN 正投影 + CD⁻¹ + APx 一步初值 + 牛顿迭代（J=I+∂A/∂u 的 2×2 解析导数，与数值微分逐分量对拍一致，并修初版 ∂/∂v 漏 i=0 项缺陷），残差判据 |F(u)-UV|∞<tol + 阻尼回退（残差未降步长减半、至多 20 次/步），确定性拒绝三路径（reject_code 1 背面/非有限、2 发散超限、3 J 或 CD 奇异）零 UB；输出口径 u=x−crpix（自述为 WCS-001 冻结自洽约定）；④p1wcs 测试面新增 apbp 组六类验收（三档畸变 fixture FIX-WCS-F low 0.1×/mid 0.5×/high 1.0×，冻结 FIX-WCS-B 原样不动，high 边缘畸变实测 183.6px 超冻结 117px 量级；四区域 roundtrip max≤2.9e-9px；确定性拒绝四路径两次调用逐字段 bitwise；1/4 worker parity；3 轮全指标 bitwise）；⑤新增 Astropy 7.0.1 第三方交叉验证脚本 `tests/unit/p1wcs/p1wcs_astropy_cross.py`（只入测试面、零生产依赖）；⑥登记重大 finding **WCS-003-F1** 并移交：生产自洽口径 u=x−crpix 与标准 FITS/astropy/消费方 wcs_transform 的 dx=x−(crpix−1) 存在常量 1px 原点平移（交叉验证初跑差 √2px，crpix+1 桥接后前向达机器精度级一致），修复涉 F3 CRPIX 冻结锚与既有对拍口径，归 owner 裁决、域外不改。
- 落点：lib/plate_solve（3 文件 +384/-5：ipv_types.h、ipv_wcs.h、ipv_wcs.cpp）；tests/unit/p1wcs（7 文件 +829/-3，含新增 p1wcs_tests_apbp.cpp 与 p1wcs_astropy_cross.py）。
- 内容性质：生产代码 + 测试（含第三方交叉验证脚本）。
- 意图证据：变更面证据与"布局扩展 + 新增迭代反演 + 新测试组 + 第三方脚本"一致，且扩展以新增字段/新增文件为主（仅 8 修改 2 新增，零删除）；自述声明"科学公式/容差/F2 冻结门 1e-4px 定义零改动，本任务使实现能力达冻结口径"，并给出布局扩展证据（APx 一步直加 low 3.6e-7/mid 3.3e-2/high 3.46px vs 36 层兼容层 8.8e-5/0.28/7.60px）与"主树 ctest 165/166 唯一 FAIL=p1_noise_adapter（F-AIO-001 预存豁免）、asan 双 sanitizer 8/8 零报告"；BASE=45695873。
- 关联：任务规格出自 1796 的 `tasks/WCS-003.md`（裁决 1 选 B）；数学不可达性论证出自 1791（WCS-002 F1 裁决移交三路径之一即"AP-BP 布局扩展 + 迭代语义"）；口径约定回溯 1789（WCS-001）；其 F1 finding 由 1802 交接快照列入"四新节点/F 全清单"。
- 不确定点：WCS-003-F1 的 1px 原点平移归属 owner 裁决在本片后续 seq 内未见裁决记录（1806 之后无相关提交）——未证实。

### seq 1806 | 9953f103 | 2026-09-11 | M（P3-001）
- 做了什么：建立投影 registry 与宪章 §18.1 四投影实现。新增 `lib/phase3_proj/p3_projection.h/.cpp`（namespace `astrocs::phase3proj`）：registry v1 冻结表（`kP3ProjectionRegistryVersion=1`，恰 TAN/SIN/CAR/AIT 四行，函数指针 dispatch 不散落 CLI switch，fail-closed 的 table/find/find_id/selfcheck）+ 统一操作面 make/pix2world/world2pix/fits_keywords；TAN 逐式沿用 `lib/phase3_session/p3_wcs.cpp` 的冻结生产事实（bitwise 对拍零漂移），SIN/CAR/AIT 为负责人裁决新增，claim 公式=FITS WCS Paper II 标准定义；ALG 唯一权威落位 `docs/algorithms/PHASE3_PROJ_IMPL.md` §15（六要素声明/共享旋转核/守卫域/测试设计逐式冻结），SCI 层零改动；测试 `tests/unit/p3_projection_test.cpp`（G1–G9：registry 完整性、3D 向量独立往返 Oracle<1e-6px、TAN 生产 bitwise 对拍、独立解析解、CD 精确断言、负面 fail-closed、CTYPE 面、确定性 bitwise、1-2-4-8 worker parity）+ `tests/backend/test_p3_projection_oracle.py`（numpy 完全独立第一性对拍 + 跨进程 sha256 确定性）+ CMake 注册 p3_projection_units/p3_projection_fault（直编生产源）。
- 落点：lib/phase3_proj/（5 文件 +608/-8，其中 p3_projection.h/.cpp 为新增）；tests/unit（2 文件 +663）；tests/backend（1 文件 +311）；docs/algorithms（1 文件 +162）。
- 内容性质：生产代码 + 测试（C++ 与 Python 双 oracle）+ 文档或规格（ALG 权威）。
- 意图证据：变更面证据显示本提交仅新增 `p3_projection.h/.cpp` 两枚实现件（README/memory/module.yaml 三件的 A 记录在更早的 seq 1722，本提交为 M），与自述"HANDOVER §4 P3-001 中断遗留工作面正式续作认领…归属厘清入库"精确吻合；自述"根 CMakeLists/lib/phase3_session/docs/contracts 零改动"且变更面确实无这些路径；自述明确"registry 现状为测试直编面非 DLL 入口（module.yaml CONTRACT_READY/entrypoint=MISSING 不冒认），生产挂载归 P3-002"；验证面（ctest 2/2、oracle pytest 3/3、故障注入 5 模式必败、受影响回归 p3_wcs/p3_interp/p3_coverage/p3_output/p3_assembly 5/5、test_p3001_science_freeze 4/4、DOC_INDEX_PASS）为消息自述；BASE=b8d9a69c。
- 关联：认领 1802 交接快照中的"P3-001 中断遗留工作面甄别指引"（自述"HANDOVER §4 P3-001 中断遗留工作面正式续作认领…预存 dirty 其余项零覆盖"）；投影集合直接来自 1786 裁决 1；下接 1807（P3-002 科学面）与 1808（P3-002 节点委托）。
- 不确定点：`lib/phase3_proj` 的 README.md +30/-7、memory.md +49、module.yaml +25/-1 三处修改是 1722 遗留骨架的交付态更新，其内容与 P3-001 中断时（1802 交接快照所指"预存 dirty"）的差集卡片不展开——未证实；另 structural_events 未把本条记为 NEWDIR（该目录已于 seq 1722 入库），目录归属链需与 S31 报告核对（该片报告亦缺位）。

### seq 1807–1808 | 9662afa8, 1a56ffb7 | 2026-09-11 | M + M（P3-002 两面）
- 做了什么：同一任务号的科学面与节点面两提。1807 | 9662afa8（科学面，BASE=9953f103）：`p3_resample.h/.cpp` 新增 uncertainty 消费面——`p3_uncertainty_open`（variance 优先/次 ivar，properties 键集严格解析 + order 一致性；皆无=`P3_UNC_NONE` 显式 unavailable；存在但非法=`P3_RS_PARAM` 产品损坏拒绝）、`p3_sample_nearest_ex`/`p3_sample_bilinear_ex`（暴露 G4 冻结权重与四角 leaf、Σc_k=1）、`p3_uncertainty_propagate`（nearest var_out=u_in，bilinear var_out=Σc_k²·u_k 以 FP64 计并禁 Σc_k=1 误归一；三态 `P3_U_OK`/`NAN`/`MISSING` 对应 §30.4 invalid policy；u<0/Inf=`P3_RS_PARAM` 显式拒绝；ivar==0→u 无效 NaN 传播）；`p3_resample_check_mode` 按 §30.4-4 把 variance/ivar 从 UNSUPPORTED 移除（→PARAM，weight/flux-per-pixel 不变，`test_p3_resample.py` test_02 同步更新并注明授权锚）；`p3_output.h/.cpp` 新增 `p3_output_write_atomic_ex`/`p3_output_verify_ex`（VARIANCE/IVAR 扩展 HDU：EXTNAME/BUNIT=`<BUNIT>^2` 与 `1/(<BUNIT>^2)`、DATASUM 逐 HDU、同一原子发布序；verify 双向防"available 静默缺 HDU / unavailable 静默占位"；单边 NULL=`P3_OUT_PARAM`；旧签名转发 EX 零行为变化；`P3Provenance` 尾部追加 uncertainty_source/uncertainty_missing_pixels）；`p3_session.cpp` run 面接线（每 worker 独立 uncertainty sampler、corrupt→`ACS_ERR_PARAM` 拒绝不落盘、inspect result 新增 uncertainty_available/source/missing_pixels）；新增 `tests/unit/p3002_uncertainty_test.cpp`。1808 | 1a56ffb7（节点面，BASE=9953f103，科学面 commit=9662afa8）：`lib/core/src/module_adapters.cpp` 的 Phase3 子节点工厂由 `make_session_module<P3Api>`（每子节点跑完整 `p3_session_run`，5 节点链重复执行全链 5 次）替换为 `P3NodeModule` 唯一真实 operation 委托——properties→`p3_sampler_open_ex`+`p3_uncertainty_open`（ALG-P3-001）/wcs→`p3_wcs_make`+`p3_wcs_fits_keywords`（ALG-P3-002）/resample→`p3_order_select`+`p3_sample_{nearest,bilinear}_ex`+`p3_uncertainty_propagate`（ALG-P3-003，lease cap 权威 worker 池行带并行）/writer→`p3_output_write_atomic_ex`（ALG-P3-004）/verify→`p3_output_verify_ex`；operation/entry 与 `runtime/pipeline/module_ports.registry.json` 冻结绑定表逐一一致；typed artifact 链 p3_props.json→p3_wcs.json→p3_resampled.{json,bin}→output_phase3.fits→p3_verify.json，上游缺失 fail-closed DATA 拒绝；Runtime 全链每节点 call_count=1 + trace_violations 空；新增 `tests/unit/p3002_real_nodes_test.cpp` 并注册 p3002_real_nodes/p3002_uncertainty 两目标。
- 落点：1807 = lib/phase3_session（5 文件 +439/-20）+ tests/unit（1 文件 +821）+ tests/backend（1 文件 +8/-2）；1808 = lib/core/src/module_adapters.cpp（+779/-7）+ tests/unit（2 文件 +466）。
- 内容性质：生产代码（两提）+ 测试；1808 另含构建注册。
- 意图证据：变更面证据显示两提分工清晰——1807 只在 phase3_session 科学面与测试、1808 只在 core 委托层与测试，互不重叠，印证自述的"科学面/委托面分离"；两提各自声明先红后绿并留档（RED 27 失败→GREEN；red_real_nodes.log 42 失败→GREEN）、故障注入三模式 rc=1（`ASTROCS_P3002_FAULT=zero_var|norm_sum|skip_hdu`）、受影响回归 6/6 与 backend pytest 37/37、全量 ctest 168 项唯一 FAIL=p1_noise_adapter（F-AIO-001）；"顶层占位 descriptor `astrocs.phase3.resample` 未触碰（归 P3-RSMP-INT）"为自述。
- 关联：科学面直接落地 1790 的 §30.4（DATA-P3-UNC-001）与 1786 裁决 3 的 unavailable 显式语义；委托面与 1795（P1-001）/1798（P2-001）同构，并把 1806 的 registry 从测试直编面转为生产挂载路径；1809 的 resample 行带改造以本两提为 BASE。
- 不确定点：1808 的编号字段抽出 P3-003/P3-004 与 1806 抽出的 P3-002 均系正文引用（后续任务与 ALG 锚），非本提交任务号；P3-RSMP-INT 的后续兑现不在本片。

### seq 1809 | 91440c16 | 2026-09-11 | M（RT-001）
- 做了什么：接入唯一 Executor 与实测资源门（宪章 §10.4 work-unit 租约 + §10.5/§18.2 冻结阈值）。①唯一 executor 生产接入：自述先做基线实证（`lib/core/src/executor.cpp` 的 RT-004 唯一池未编入任何生产 target、生产零使用），再整改四项——(a) `worker_loop` 每任务租约由 `acquire(1,budget)` 改 `acquire(1,1)` 恰租 1 槽（旧语义首 worker 抓走整份预算致严格串行，自述 RED 实测峰值=1；Σactive≤budget 合同不变）；(b) 新增 `lib/core/src/executor_runtime.h` 与 `rt::shared_work_executor` 注册点（按 ThreadBudget 实例绑定的进程唯一池，同预算同池/异预算异池/null→串行降级不伪造授权，析构 join 无 detach/UAF）；(c) `executor.cpp` 经 `module_adapters.cpp` 的 include 编入 `astrocs_module_adapters`（自述属根 CMakeLists 白名单外，编译归属临时迁移登记 F-RT-001-04）；(d) P3 resample 节点行带自建 `std::thread` 池删除、改为行带 work unit 提交唯一 executor（每带独立 sampler 行为一致→输出 bitwise 不变），节点租约 cap 观测后显式 release 防池任务自等待死锁，`band_executed`≠提交数→节点 CANCELLED 拒绝不落伪产物（fail-closed），manifest 新增实测 work_units/band_executed/band_active_peak。②实测资源门：`tools/monitoring/run_monitored.py` 新增 `evaluate_frozen_gate()`（有效 CPU≥2 且区间>10s 严格成立时判 均值≥85% 已分配容量 + 任何连续 10s<60% + 单活跃线程→fail；监控缺失/中部断流/输入非法 fail-closed 绝不 pass；≤10s 或 <2 CPU→显式 `not_applicable` 非豁免，采 BASE-UTIL-001 口径）+ CLI opt-in（`--gate-workers` 等，fail→exit 10，不给旗标零行为变化）。③测试：新增 `tests/unit/rt001_unique_executor_test.cpp` 5 用例（峰值并行/池身份/节点经 executor + 实测 manifest/串行 vs 4-worker bitwise parity/drop-band 注入必败）与 `tests/monitoring/test_frozen_gate.py` 14 用例（G1–G9）。
- 落点：lib/core（3 文件 +168/-15：executor.cpp、executor_runtime.h 新增、module_adapters.cpp）；tools/monitoring/run_monitored.py（+225/-9）；tests/unit（2 文件 +372）；tests/monitoring/test_frozen_gate.py（+220 新增）。
- 内容性质：生产代码 + CI 或机器门（监控门求值器）+ 测试。
- 意图证据：变更面证据与"执行器 + 门求值 + 双侧测试"一致；关键因果链（executor.cpp 此前未入生产 target、旧租约语义致串行）为消息自述的基线实证，卡面无法直接复核；"rt001 2/2 + frozen_gate 14/14 + 受影响回归 10/10 + rt004 harness 8/8 + pytest monitoring 74/74 + cli 17/17 + ci 15/15 + 全量 ctest 169 唯一 FAIL=p1_noise_adapter + ASAN/TSAN 零报告 + p3006 ≥10s 锚 git archive 影子树 A/B 实证 BASE=4.08s vs HEAD=4.125s 无回归"均为自述；BASE=1a56ffb7。
- 关联：阈值 85/60/单线程即失败源自 1786 裁决 2；`not_applicable` 分类口径出自 1796 的 BASE-UTIL-001；行带并行改造以 1807/1808 的 resample 节点为对象；其域外登记 F-RT-001-02（`cli/resource_gate.h` 的 MON-001 75/50/70 与冻结 85/60 分叉）在 1812 被再次维持移交。
- 不确定点：F-RT-001-01..08 的逐项内容仅见于自述，登记载体在证据区（禁读）——未证实；`cli/resource_gate.h` 阈值分叉在本片内未修（1812/1813 只动 CI 侧旗标）。

### seq 1810 | 59fdeab3 | 2026-09-11 | M（MOD-001）
- 做了什么：完成科学 DLL 安装加载验证与产品清单（宪章 §8.4 安装面 + §8.1 产品清单 + §18.4 签名清单加载）。①F-AIO-003 闭环：把 1793 的 `lib/astro_image_io/src/aio_abi.cpp`（唯一 AIO C ABI v1 实现）编入生产 target `astrocs_aio`（此前仅测试面直编），自述以 nm 符号 + 直链生产库握手探针实证 abi=1/status_count=71；②安装面 `cmake/install_layout.cmake`：6 个科学模块 SHARED（`astrocs_catalog_gaia` + `astrocs_p1_{drizzle,calibration,cosmetic,hips_writer,noise}`）入安装树 `modules/` + `$ORIGIN` RPATH；③产品清单 `packaging/astrocs.product.json` units 5→11（module_id 与 module.yaml/types.h 逐一核对；自述声明 sha256 维持 null 不伪造、PLATFORM-RUNTIME/IO 维持 SKELETON 不冒认）+ `install-tree.contract.json` required 6→12 + `cmake/astrocs.product.windows.json.in` 同步 11 units 并修正预存断链（`astrocs.noop`→`astrocs.conformance.noop`）；④加载验证暴露并修复两个真实缺陷——缺陷 A：6 个科学模块 describe 拒绝空 ID，与 ABI-003 安全 loader 既成合同（empty 调用，noop 先例）冲突，致安装树内被自家 loader 必拒 `DESCRIPTOR_MISMATCH`，改 5 个 `module_entry.cpp` + gaia `module_entry.c` 对齐 noop 语义（空 ID 返回描述、错 ID 仍 MISMATCH，既有 adapter 测试锚不变）；缺陷 B：gaia DLL 依赖闭包缺 libm（GNU `sincos`，RTLD_NOW 下独立进程 dlopen 必败 OS_LOAD），在 gaia CMakeLists 的 UNIX 分支链 `m`；⑤新增 `tests/abi/mod001_install_load_check.py` 机器验收编排（自述 72 断言、RED 31 失败留档→GREEN 72/72×3 轮、6 类故障注入全必败：HASH_MISMATCH/MODULE_ID_MISMATCH/PATH_ESCAPE/FILE_MISSING/删 DLL verify 必败/selftest 未登记必败）。
- 落点：cmake/（install_layout.cmake、astrocs.product.windows.json.in）；packaging/（README.md +10/-2、astrocs.product.json +11/-5、install-tree.contract.json +6，3 文件）；cmake/（install_layout.cmake +26/-1、astrocs.product.windows.json.in +8/-2）；根 CMakeLists.txt（+9/-1）；五个模块侧 module_entry 小改（lib/calibration +9/-2、lib/drizzle +9/-2、lib/cosmetic +8/-2、lib/hips +8/-2、lib/gaia_xpsd_client/src/module_entry.c +8/-2）+ lib/gaia_xpsd_client/CMakeLists.txt（+16，缺陷 B 链 m）；tests/abi/mod001_install_load_check.py（新增 +376）。
- 内容性质：生产代码（构建与安装面 + 六模块 describe 语义）+ CI 或机器门（安装加载机器验收）+ 数据或产物（产品清单 JSON 属合同/清单面）。
- 意图证据：变更面证据（13 文件、12 修改 1 新增、每模块 8–24 行的小改动）与"清单/安装面 + 缺陷 A 的六处语义对齐"完全相称；"CLI 安装树实测 modules list/verify PASS units=11 + selftest 6 模块装配 PASS + 主树 ctest -j1 唯一 FAIL=p1_noise_adapter + asan 受影响域 38/38 + secure_loader 36/36 + registry 28/28 + 静态检查 7 项 PASS"为自述；BASE=91440c16。
- 关联：闭环 1793 移交的 F-AIO-003；被 1811 指为断裂点（"MOD-001 后 compatibility CLI 独立构建断裂"由本提交引入并修）；其清单中的 `astrocs_p1_noise` 行在 1814 摘除生产断链后（下一片 1817）才同步收缩——本片内该模块仍按 V7 残留态留在清单里。
- 不确定点：域外 finding F-MOD-001-01..04（含 -j4 抖动 = ARCH-AUDIT-P1 F1 预存竞态、gaia LSan 预存泄漏 bisect 同败）仅见于自述；sha256 仍为 null 的实际签核链未在本片出现。

### seq 1811 | 026717fd | 2026-09-11 | M（CLI-001）
- 做了什么：完成薄 CLI 的 validate/plan/inspect 命令面（宪章 §8.1）。①`cli/parser.cpp`：`kRules`/`kHelp` 正式登记 phase1/2/3 × validate|plan|inspect 共 9 条命令，旗标白名单 `--config`/`--json`/`--output`；②`cli/commands.cpp`（+243）：`cmd_phase_validate`（session_mode 全量校验 + PipelineIR 静态构建，零 Runtime、零科学执行、零 I/O 产物，深层拒绝与 run 同面）、`cmd_phase_plan`（确定性 plan 文档 kind=`astrocs_plan`，含 config.sha256/budget/typed DAG 节点/work_units{total,parallel,io}/artifact_refs，无时间戳→同 config 逐字节一致，`--output` 原子落盘同一文档）、`cmd_phase_inspect`（只读扫描 output_dir 的 `astrocs_run_*.json`，按 phases 过滤呈现 + products 去重，malformed 呈现不中断，零写入，output_dir 缺→退出码 3）；③`cli/CMakeLists.txt`：修复 1810 之后 compatibility CLI 独立构建断裂（自述 module_adapters.cpp 直读 astro/phase2 头与 lib/common，HEAD 基线 git archive 影子构建同败实证），补 2 条 include 路径；④测试：新增 `tests/cli/test_cli001_vpi.py` 15 用例（自述先 RED 2 失败→GREEN 15/15×2 轮，含 4 类故障注入：schema 篡改→2/未知键→3/缺 phase3→2/删 manifest 同步变化）+ `test_cli_protocol.py` HELP_LINES golden 同步 9 行 + `test_cli003_semantics.py` test_16 按宪章上位命令面演进（supersede 旧冻结面）。
- 落点：cli/（parser.cpp、commands.cpp、CMakeLists.txt）；tests/cli/（3 文件 +371/-6）。
- 内容性质：生产代码（CLI 命令面）+ CI 或机器门（golden/协议测试）+ 测试。
- 意图证据：变更面证据与"命令面注册 + 三命令实现 + golden 同步"一致；"vpi 15/15 + cli_v7_surface/api_cli_protocol/doc_machine_check/check_cli_command_layer/check_cli_run_preset/check_api_docs 全 rc=0 + 根构建面 ctest 168/169 唯一 FAIL=p1_noise_adapter 零回归 + 根面 astrocs 重建新命令实测 OK"为自述；基线 59fdeab3。
- 关联：命令面直接实现 1786 的 §8.1 单入口与三 Phase 隔离口径；其 plan/inspect 的只读与确定性口径与 1809 新增的实测 manifest 字段（work_units/band_executed）相互衔接；1814 的门卫化与本条共同构成"CLI/构建面收口"。
- 不确定点：域外 finding F-CLI-001-01..03（UT-CLI 17 项预存过时断言、docs 命令树同步、plan/inspect schema 化）与 `lib/infrastructure/cli` 物理迁移归属仅见于自述——未证实。

### seq 1812–1813 | e9b82ea4, ca948367 | 2026-09-11 | M + S（CI-001 及其 finding 回调）
- 做了什么：1812 | e9b82ea4（feat(CI-001)，基线 026717fd）收紧 CI 注册表与资源 fail-closed：①不可 waiver——`ci/checks.json` 的 WIN-BUILD-RELEASE/WIN-TEST-UNIT/WIN-PACKAGE-CANDIDATE 由 `waivable true` 改 `false`，platform/prerequisite 不再判 SKIPPED 而判 FAIL(prerequisite)（自述负向实测 run.py rc=1）；②监控必须调用 evaluate——`tools/monitoring/run_monitored.py` 新增 `--gate-required`（与 `--gate-workers` 互斥，容量取 `host_probe.effective_cpu_cores`，fail→exit 10，`not_applicable` 显式分类非豁免，host_probe 缺失 fail-closed），checks.json 全部 8 条监控包装命令（BUILD-GCC-RELEASE + DEEP-*×5 + WIN-*×3）加 `--gate-required`，`ci/run.py` 新增 `FAIL(monitor_gate_missing)` 入 `HARD_FAILURE_VERDICTS`；③候选缺失必须失败——`.github/workflows` 的 fatduck.yml select-candidate rc=3 由 notice+success 改 exit 3 job fail，ci-windows.yml 注释同步；④域内预存断链修复——`ci/validate_registry.py` R2 白名单补 `dirty_ignore_*`（对齐 checks.schema.json，strict 2 错→PASS）与 UT-QUALITY heavy/requires_monitor 登记 conform 修正；测试新增 `ci/tests/test_ci001_failclosed.py` 19 用例（自述 RED 14 留档）。1813 | ca948367（fix(CI/F-CI-001-01)，owner 裁决回退）：把 WIN-BUILD-RELEASE/WIN-TEST-UNIT/WIN-PACKAGE-CANDIDATE 三条命令移除 `--gate-required`，`waivable=false` 维持不动，Linux 重计算面（BUILD-GCC-RELEASE + DEEP-*×5）旗标保持；测试面把门禁断言限定为非 WIN 的 requires_monitor 检查并新增 `test_win_checks_excluded_from_gate_by_owner_ruling`，`test_windows_ci.py` 的 WIN 合同断言改 `assertNotIn`。
- 落点：1812 = ci/checks.json、ci/run.py、ci/validate_registry.py、ci/tests/test_ci001_failclosed.py（新增）、.github/workflows（fatduck.yml、ci-windows.yml）、tools/monitoring/run_monitored.py；1813 = ci/checks.json（-3）、ci/tests（2 文件 +39/-11）。
- 内容性质：CI 或机器门（两提均是）+ 测试。
- 意图证据：变更面证据为"注册表旗标 + 判定语义 + 工作流 exit 码 + 断言同步"四件齐动，与自述一致；1812 显式留下重大移交 **F-CI-001-01**（监控栈为 Linux /proc 专用，WIN-* 加 `--gate-required` 后将因无有效 CPU 采样判 monitoring_missing→exit 10→Windows CI 预期红线），并声明"本任务不放宽冻结门禁"——1813 即以 owner 裁决（2026-09-11：构建/打包/单测为非重计算面，§10.5/§18.2 资源门冻结语义针对重计算区间）回调该旗标，两提构成"收紧→裁决回调"的完整闭环；复验（validate_registry strict PASS(80)、failclosed 19/19、tests/monitoring 74/74 零回归、WIN-BUILD-RELEASE 跨平台负向 rc=1 不受影响）为自述。
- 关联：1812 的 `--gate-required` 阈值语义承接 1809 的 `evaluate_frozen_gate()` 与 1786 裁决 2；两提均引用 1782 的 impact_map 预存失败（test_impact_map schemas/** 记为 memory F2）；1813 维持 1812 的 `waivable=false`，说明回退范围仅限旗标；后续 F-CI-002 系列由 1814 处理。
- 不确定点：1812 自述 ci/tests 312/314 的两项失败（test_impact_map、TestDriverStages=driver 零 diff 绝对路径）为预存、本片未修——其修复不在本片主责区。

### seq 1814 | f74fc20f | 2026-09-11 | S（CI / F-CI-002-01+02+03）
- 做了什么：Linux 构建断链与 TASK-RESULT-SCHEMA 口径修复（自述为 owner 裁决执行）。①根 CMakeLists.txt 移除 `add_subdirectory(lib/snr_estimator)` 断链引用——自述归因"由 P1-001（9e09941a）越界提交引入，引用从未入库的 V7 残留 untracked 文件"，以 git archive 影子树实证干净树 cmake configure rc=1→rc=0，并同步从 RPATH foreach 摘除 `astrocs_p1_noise`，`lib/snr_estimator/` 保持 untracked 锁死不收编、零触碰；②`tests/unit/CMakeLists.txt` 两处门卫化——p1noise 子图 add_subdirectory 包 `if(EXISTS)`、P1-NOISE-IMPL adapter 对拍块包 `if(TARGET...)`，自述"V7 残留收编后自动重新激活"；③`tools/quality/check_task_result_schema.py`（+27/-3）——默认 schema 路径由根 `schemas/` 改指唯一事实源 `contracts/schemas/task_result.schema.json`（F-CI-002-02），并新增 `ARCHIVED_EVIDENCE_PREFIXES=(evidence/v6_1_rework/,)` 排除清单（F-CI-002-03 owner 裁决：V6.1 线 ARCHIVED_SUPERSEDED 归档证据不受现行 schema 复验、改写历史证据被禁止，排除逐条显式输出 `SCHEMA_CHECK_ARCHIVED_SKIP` + `archived_skipped=N` 计数而非静默，ACTIVITY_STATE 为归档集合权威）。
- 落点：根 CMakeLists.txt；tests/unit/CMakeLists.txt；tools/quality/check_task_result_schema.py。
- 内容性质：CI 或机器门（schema 校验器口径）+ 生产构建面（断链摘除与门卫化）。
- 意图证据：变更面证据（3 文件、+46/-14）与"摘引用 + 包门卫 + 改默认路径与排除清单"三处小改动相称；影子树终验（cmake configure rc=0、schema rc=0、traceability 67/67、pipeline_graph/check_build_graph/known_failures_baseline 全 rc=0）与"known_failures_baseline 无 p1_noise 依赖"为消息自述。
- 关联：与 1787（V7 残留态如实登记不收编）、1793（F-AIO-001 预存失败）、1795（越界引入根 CMakeLists 引用）三点合流，是 V7 残留问题的口径收口件；1811 曾自述同类影子树实证法；其口径延续到下一片（seq 1817 继续处理 F-CI-002-05/07 与 p1_noise 清单安装面对齐）。
- 不确定点：`lib/snr_estimator` 与 `tests/unit/p1_noise` 的实际文件内容不在版本库内（untracked），本片无法判断其去留终局——未证实。

### seq 1815 | 9f6b72b5 | 2026-09-11 | L（REAL-000）
- 做了什么：真实数据审计索引 v1.2 与确定性匹配计划（落地 1797 的 04_OWNER_DECISIONS rev3 指令）。①盘点对账：testdata 944 文件逐文件 sha256 全覆盖（自述 file_manifest.csv 944 行=文件数，10 条抽验 sha256sum 独立复核一致）、v1.1 索引 7 数据集/710 帧/27 母版声明与磁盘一致、M42 磁盘实测 196 帧（T2 73/T3 123）与指令逐面板计数吻合、GaiaDR3 16 个 .xpsd（db_type=GaiaDR3 32B）与 GaiaDR3SP 20 个 .xpsd（db_type=GaiaDR3SP 384B）注册摘要 gaia_registry.json（DATA-GAIA-001）；②`testdata/index.json` 升 v1.2：新增 M42_T2T3_mosaic_Flying_dutchman multi-telescope 条目（telescopes=[T2,T3] + 逐面板逐曝光逐滤镜实测计数树 + Astrodon Ha 3nm），schema 向后兼容扩展 telescopes/multi_telescope/pixel_size_um（自述 T2/T3/T4 素材未记载像元尺寸→写 null + `OWNER_CONFIRM` 不臆造），statistics 同步 8 数据集/906 帧/27 母版；③确定性匹配工具 `tools/realdata/match_plan.py`：906 帧 enumerated==classified 零静默丢弃，MATCHED 783（精确 636 + dark 缩放 147）、UNMATCHED 123 全部 `NO_LUM_FLAT` 机器可读 reason_code；缺口复核（T2 300s×53 / T3 300s×94→600s dark 缩放 K=0.5 SCALE_OPTIMAL、OPTIMAL fallback EXPOSURE_RATIO、dark_optimization=true 逐帧记 K，冻结锚 CALIBRATION.md:57,90 + CALIBRATION_ALGORITHMS.md:158,292；NGC247 Lum×15→UNAVAILABLE(NO_LUM_FLAT)；T1 显式空集 empty(PASS)）；四类负例（错滤镜/错曝光超 K 域 (0,10]/错传感器 4096↔4500/错望远镜目录）全确定性拒绝；④测试 `tests/realdata/`（`test_index_v12.py`、`test_match_plan.py`、`__init__.py`）42 用例 + tests/monitoring 74 用例全过 + phase_config 模板 9 份；⑤审计发现如实移交 owner：LDN43 Lum×10 + Victory Lum×98 同因缺 Lum master flat（合计 123 帧 NO_LUM_FLAT）+ M42 素材提及 HDR 180s 曝光而磁盘无对应帧（写入索引 notes）。
- 落点：tools/realdata/（新增目录：match_plan.py + README.md，含目录首现 NEWDIR）；tests/realdata/（新增目录 3 文件）；testdata/index.json（噪声区 1 文件，+860/-120，属数据产物）。
- 内容性质：生产代码（工具）+ 测试 + 数据或产物（索引 v1.2 落 testdata/ 噪声区）。分级 L 按文件集合归纳：6 文件、5 新增 1 修改。
- 意图证据：变更面证据与"新建工具与测试 + 索引改写"一致，其中索引本体落在噪声区故卡面不计内容（本片不据其判定工作细节，但工具与测试为可核工作证据）；"未跑 Phase1/2 链路（属 REAL-001 范围）、未改任何数据文件与冻结文档"与变更面（无 testdata 数据文件与 docs 改动）吻合；BASE=f74fc20f。
- 关联：承接 1797 的 tasks/REAL-000.md 与 1802 的"REAL 线 owner 指令"派发序；为 1816+ 的 REAL-001 三层验收提供输入；其 Gaia 注册摘要与重叠区 1752（CAT-GAIA-INT）的 gaia 模块接口同域。
- 不确定点：`testdata/index.json` 的 v1.2 具体键值、file_manifest.csv 与 gaia_registry.json 产物是否在库内均不可核（噪声区/未列名）——未证实；自述"27.9GB"等规模数字无版本库证据。

## 重叠复核条目（seq 1751–1760，共 10 条）

上一片报告文件名应为 `设计大纲/reports/history/slices/H-S32.md`。经 `**/H-S*.md` 全局 glob 与目录列举双重核实：现存报告为 H-S01..H-S24、H-S26、H-S27、H-S28（止于 seq 1486–1540），**H-S32.md 确实不存在**；缺位片为 H-S25（seq 1321–1375）与 H-S29..H-S32（seq 1541–1760，其中 H-S32 主责即 1706–1760，正是本片重叠区的直接上位片）。故本片 10 条重叠复核一律标"重叠待核"，不得给出一致/不一致判定。为便于后续回补，此处仅登记每条的**本片区可核事实**（卡面证据 + 与本片区主责条目的链上关系），不作结论比对。

| seq | sha8 | 日期 | 分级 | 卡面要点（消息首行） | 变更面（卡面聚合） | 与本片区主责条目的链上关系 | 复核结论 |
|---|---|---|---|---|---|---|---|
| 1751 | c042f0c4 | 2026-09-09 | S | chore(state): P1-DRZ-TEST 台账 CLOSED 同步（commit 902d2e20） | evidence/v8_1_ci_control（2 文件 +7/-7） | 与本片 1761/1763/1765/1767 同型；902d2e20=seq 1750（本片区外） | **重叠待核**（H-S32.md 不存在） |
| 1752 | 054af3a3 | 2026-09-09 | M | feat(CAT-GAIA-INT): gaia 模块集成 descriptor+typed ports+调用计数（编号 ABI-004/RT-006/RT-05） | lib/gaia_xpsd_client（2 文件 +132/-2）；tests/unit（2 文件 +861）；evidence 1 | 其"descriptor+typed ports+调用计数"三部曲与本片 1764（P1-COS-INT）同构；1764 自述指认本条的测试内 SHA-256 K 表缺陷 | **重叠待核**（H-S32.md 不存在） |
| 1753 | 94074a32 | 2026-09-09 | S | chore(state): CAT-GAIA-INT 台账 CLOSED 同步（commit 054af3a3） | evidence/v8_1_ci_control（2 文件） | 成对收口件；94074a32 又被 1755 作为 closed_commit 引用 | **重叠待核**（H-S32.md 不存在） |
| 1754 | acceab68 | 2026-09-09 | M | test(resource): MON-002 增加内存增长检测（memory_report.h 双探针 glibc mallinfo2 + /proc smaps_rollup、Theil-Sen 全曲线斜率、reclaim 三分类、validate_alloc_report 复读重算） | cli/memory_report.h（新增 +487，structural_events 记 NEWDIR）；cli/resource_gate.h（+47/-1）；cli/commands.cpp（+48/-2）；tests/unit（2 文件 +337）；evidence/v6_1_rework（2 文件 +85） | 与本片 1809（RT-001 实测资源门）同域；1809 自述 F-RT-001-02 指 `cli/resource_gate.h` 阈值与本文件口径分叉 | **重叠待核**（H-S32.md 不存在） |
| 1755 | a5e4da42 | 2026-09-09 | S | chore(evidence): MON-002 COMMITS.csv 回填 + 台账 CLOSED（commit 94074a32） | evidence/v8_1_ci_control（2 文件 +5/-5） | 与 1778 同为"台账滞后回填"型 | **重叠待核**（H-S32.md 不存在） |
| 1756 | 2c065ace | 2026-09-09 | L | feat(P1-DRZ-IMPL): drizzle 迁移独立模块——lib/drizzle 九操作 C ABI v1 adapter（编号 DRZ-001） | lib/drizzle（5 文件 +1588）；tests/unit（2 文件 +520）；根 CMakeLists（+8/-1）；evidence 1 | 模块迁移三部曲之首，本片 1762（COS-IMPL）与 1770（HIPS-IMPL）为其后续同构件；drizzle 节点在 1795 的"口径⑨下游零调用"用例中被引用 | **重叠待核**（H-S32.md 不存在） |
| 1757 | 4f150bfc | 2026-09-09 | S | chore(state): P1-DRZ-IMPL 台账 CLOSED 同步（commit 2c065ace） | evidence/v8_1_ci_control（2 文件） | 与 1763 同型 | **重叠待核**（H-S32.md 不存在） |
| 1758 | adf820ac | 2026-09-09 | L | feat(P1-CAL-IMPL): calibration 迁移独立模块——astrocs_p1_calibration SHARED（PIC/C++17/OpenMP）legacy 4TU + module_entry.cpp 1490 行九操作（编号 CAL-004） | lib/calibration（7 文件 +1733/-7）；tests/unit（2 文件 +819）；根 CMakeLists；evidence 1 | 导出面净化与 nm/dlsym 双实证口径与本片 1762/1770 同一族；1762 自述"CAL-IMPL 同款 4TU 闭包修正"指向本条 | **重叠待核**（H-S32.md 不存在） |
| 1759 | 0fa802dc | 2026-09-09 | S | chore(state): P1-CAL-IMPL 台账 CLOSED 同步（commit adf820ac） | evidence/v8_1_ci_control（2 文件） | 与 1761/1763 同型 | **重叠待核**（H-S32.md 不存在） |
| 1760 | 58d20223 | 2026-09-09 | M | feat(P1-CAL-INT): calibration 模块集成 descriptor+typed ports+调用计数（10-op 词表 / typed_ports 5 端口 / DEV-2 可选 NULL 恒等通道，编号 RT-006/RT-05） | lib/calibration（2 文件 +181）；tests/unit（2 文件 +999）；evidence 1 | 本片首个主责条目 1761 正是为其做台账收口；1764 自述登记本条测试内 K 表缺陷 | **重叠待核**（H-S32.md 不存在） |

可交叉的结构事实（非一致性结论）：本区 10 条的父指针逐条等于前一 seq 的 sha8（1751 父 902d2e20=seq 1750；1752 父 c042f0c4=1751；…；1761 父 58d20223=1760），与 S32 的 BRIEF 声明"主责 55 条（seq 1706..1760）"完全对齐——即这 10 条在派发关系上属 S32 主责范围，报告缺位系成片积压而非证据缺失；若补齐 H-S32，上表 10 条可按"同链单亲"直接复核。
