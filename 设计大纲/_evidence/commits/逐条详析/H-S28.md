# 逐条详析 （证据层过程件）

> 本文件是 设计大纲/reports/history/slices/H-S28.md 的逐条正文；最终交付只保留两份大报告，逐条正文下沉到证据层（条目文本零改动，仅移动）。
> 由 _tools/11_split_slice_reports.py 从入库基线 a3a343a4 重切，日期 2026-09-15；条目组 48 个；报告树保留段（主题归纳/重叠区核对/遗留缺口）见原文件。

## 主责条目（55 条，合并为 48 组，列全 seq 与 sha8）

### seq 1486 | caee3e67 | 2026-09-03 | L（IO-001）
- 做了什么：新建流式 FITS 读写核心：`runtime/io/fits_core.c`（+1702 单文件）实现 header 解析、plane-chunk 读写、字节序（标题自述修复 F32 字节序）、checksum/datasum、错误码与 trace hooks；`modules/services/io` 侧新增对外头 `fits_stream_v1.h` 与 C 自检 `fits_core_selftest.c`（合 +789）；接口文档 IO_001_FITS_STREAM_INTERFACE.md 与 Python 契约测试同提。分级 L，按文件集合归纳（生产 C 核心 1、模块头+selftest 2、文档 1、测试 1，共 5 文件 +3071/-0 全新增）。
- 落点：runtime/io/（1）；modules/services/io/（2）；docs/interfaces/io/（1）；tests/io/（1）。
- 内容性质：生产代码 + 测试 + 文档或规格。
- 意图证据：消息自述 requirements 行逐项列出 FITS 流式接口能力与"astropy 交叉 oracle"；变更面证据吻合——四目录全部为新增文件、零删除，tests 行自述"契约 pytest 9/9 + fits_core_selftest ALL PASS"；scientific_change 自述 NO。
- 关联：与 1490（IO-002 HiPS 输入）、1510（IO-003 原子发布）构成 IO 族三级递进；fits_core 的 fitsverify 同判定在 1510 复验中再引用。
- 不确定点：F32 字节序"修复"无对应旧代码删除（全新增文件），修复对象是否为本提交自身初版，卡片无法判定——未证实。

### seq 1487 | 5de08133 | 2026-09-03 | M（ABI-002）
- 做了什么：以合同+头文件+探针+pytest 四件套冻结模块 ABI 生命周期语义：状态机、并发规则、稳定错误域、版本协商（major 拒绝 + struct_size 尾部扩展）、诊断缓冲；新增 `contracts/config/module_lifecycle_contract.schema.json`、`include/astrocs/abi/lifecycle_v1.h`、`tests/abi/abi002_lifecycle_probe.c`、`tests/abi/test_abi002_lifecycle.py`。
- 落点：tests/abi（2 文件 +650）；contracts/config（1）；include/astrocs（1，+299）。
- 内容性质：测试（test 型提交）+ 生产代码（ABI 头）+ 合同 schema。
- 意图证据：消息自述 requirements/tests 行（pytest 18/18、probe C11/C++17 ALL_OK、schema-头 6 组一致性）；变更面证据：4 文件全新增，schema 与头文件配对落码支持"冻结"语义。
- 关联：承接重叠区 1481（ABI-001 模块 C ABI v1 四头文件），下启 1497（ABI-003 加载器）、1504（ABI-004 注册表）、1508（ABI-005 echo 探针）。
- 不确定点：无。

### seq 1488 | 56ab85ba | 2026-09-03 | M（RT-002）
- 做了什么：隔离三阶段运行时生命周期：per-phase 独立 Runtime/Store/RunContext 与各自 registry，跨 phase 仅磁盘交换；新增 `runtime/core/phase_lifecycle.py` + README + `tests/runtime/test_phase_lifecycle.py`。
- 落点：runtime/core（2 文件 +329）；tests/runtime（1 文件 +272）。
- 内容性质：生产代码 + 测试 + 附说明文档。
- 意图证据：消息自述 requirements（RT-002 括注 per-phase CLI 独立/仅磁盘交换）与 tests（170/170 含 22 新用例 + 92 回归 + 56 core）；变更面证据吻合：3 文件全新增零修改。
- 关联：RT 族链首（RT-001 类型化运行图在重叠区 1483）；后续 RT-003（1498）→RT-007（1517）沿 runtime 递进。"跨 phase 仅磁盘交换"是宪章 §3.2 三 Phase 隔离的运行时面落实。
- 不确定点：无。

### seq 1489 | 6affe300 | 2026-09-03 | M（GOV-004）
- 做了什么：建立负责人审查入口：L0 `REVIEW.md`（+87/-39 重写）+ `docs/owner/` 五份概览（SCIENCE/PIPELINE/ARCHITECTURE/RELEASE_STATUS/CHANGE_REVIEW，合 +564）+ DOCUMENT_INDEX 补登。
- 落点：docs/owner（5 文件新增）；REVIEW.md；docs/DOCUMENT_INDEX.yaml。
- 内容性质：文档或规格（代码面文件 0）。
- 意图证据：消息自述 requirements（GOV-004 L0+5 份清单）与 tests 行（DOC_INDEX_PASS(161)/CONSTRAINTS_PASS/version PASS 三个机器门）；变更面证据吻合：纯文档 7 文件。
- 关联：与 1491（GOV-005 清陈旧）同日连续治理动作；1489 建入口、1491 清历史；RELEASE_STATUS.md 承载状态字面量入口（AGENTS.md 映射表所引文档在此诞生）。
- 不确定点：文档内状态字面量具体值未展开核对——以卡片标题清单为准。

### seq 1490 | 316d4175 | 2026-09-03 | L（IO-002）
- 做了什么：建立 HiPS 输入合同：properties 合同、tile width-order、NESTED 寻址、FITS-only 科学平面、partial tree、MOC optional、全拒绝路径；新增 `runtime/io/hips_core.c`（+956）、模块侧 `hips_input_v1.h` + `hips_core_selftest.c`、接口文档、fixture 生成器 `make_hips_fixture.py` 与 `test_hips_input_contract.py`。按文件集合归纳（6 文件 +2316/-0 全新增）。
- 落点：runtime/io（1）；modules/services/io（2）；tests/io（2）；docs/interfaces（1）。
- 内容性质：生产代码 + 测试 + 文档或规格。
- 意图证据：消息自述 requirements 逐项 + tests 行（hips_core_selftest ALL PASS + pytest 17/17）；变更面证据吻合。
- 关联：延续 1486 fits_core 的同构模式（C 核心 + 模块头 + selftest + 契约 pytest）；hips-bulk 后来成为 1511（CPU-004）AVX512 唯一迁移热点，输入面与后端面在此咬合。
- 不确定点：无。

### seq 1491 | 9bc3d0fd | 2026-09-03 | L（GOV-005）
- 做了什么：清理陈旧现状与记忆：`memory.md` 大收敛（+89/**-2256**）、README/HANDOVER/CHANGELOG 订正（CHANGELOG 新增 current alpha 节 +50）、陈旧叙述移入 docs/archive（2 文件 +2275，含 memory_V18R2-V19_operational_log 归档件）、DOCUMENT_INDEX 同步。按文件集合归纳（7 文件，净 +181）。
- 落点：memory.md、README.md、HANDOVER.md、CHANGELOG.md、docs/archive/history/、docs/DOCUMENT_INDEX.yaml。
- 内容性质：文档或规格 + 结构整理（归档搬家成分）。
- 意图证据：消息自述 requirements（stale claims→history、validate_docs 无 stale-active）+ tests 行（DOC_INDEX_PASS 由 1489 的 161 升至 189）；变更面证据吻合：-2256 全落在 memory.md，与"收敛"自述一致。代码面文件 0。
- 关联：与 1489 同日治理收尾，二者构成 V7 治理文档层的一次性收敛。
- 不确定点：归档两文件的具体覆盖范围未逐篇核对——未证实。

### seq 1492 | 46b0d8e1 | 2026-09-03 | M（BLD-003）
- 做了什么：建立模块化安装骨架：唯一 install 规则源 `cmake/install_layout.cmake`（+95）、SHARED 模块 DLL 骨架（runtime/io/noop/cpu_baseline，`modules/conformance/noop/` 含 noop_module.c/module.yaml/握手测试）、白名单安装树 contract（install-tree.contract.json）、RPATH $ORIGIN、licenses（CFITSIO_LICENSE 等 3 文件）、根 CMakeLists 接线（+99/-3）。
- 落点：modules/conformance（6）；packaging/（licenses 3、schemas 2、README、astrocs.product.json、install-tree.contract.json 共 7）；CMakeLists.txt；cmake/install_layout.cmake。共 18 文件 +968/-4。
- 内容性质：生产代码（C+CMake）+ CI 或机器门（contract 校验）+ 打包规格。
- 意图证据：消息自述 requirements（删 noop→verify FAIL 负测、无静态副本）+ tests 行（install 11 units verify PASS；ctest 56/57，1 项 legacy core_pipeline V6 fixture 缺失标注 pre-existing）；变更面证据：16 新增 2 修改吻合"骨架"性质。
- 关联：BLD 族沿 1485（BLD-002 唯一根构建图，重叠区）→1492→1493（BLD-004 依赖锁定）递进；noop 模块被 1497（ABI-003）用作真实加载正测对象。
- 不确定点：ctest 56/57 的 1 项遗留失败归属仅凭消息自述"pre-existing"，未见对照基线证据文件——未证实。

### seq 1493 | 64a2f996 | 2026-09-03 | M（BLD-004）
- 做了什么：锁定依赖与许可证边界：`packaging/dependency-lock.json`（+149，生产依赖 vs test-only oracle 分离，版本/来源/hash/license）、`gen_sbom_input.py`（+168，SBOM 输入生成）、`dependency-lock.schema.json`、DEPENDENCIES.md 同步、cli/CMakeLists 小改（+17/-10）。
- 落点：packaging/（4 文件）；DEPENDENCIES.md；cli/CMakeLists.txt。
- 内容性质：生产代码（工具脚本）+ 数据或产物（lock）+ 文档。
- 意图证据：消息自述 requirements（机器绝对路径 FORBIDDEN、vcpkg NOT_USED）+ tests 行（gen_sbom_input PASS：lock↔DEPENDENCIES.md 一致、12 SBOM rows、0 机器路径）；变更面证据吻合。
- 关联：1492 安装骨架的许可证/来源侧配套；同日落码。
- 不确定点：无。

### seq 1494 | c1e02339 | 2026-09-03 | M（DATA-003）
- 做了什么：接入生产 ArtifactStore：`runtime/artifact_store/production_store.py`（+607）实现"临时对象→校验→hash→原子 publish"，模块 execute 只拿已校验 handle，cancel/fail 路径不产生成功对象，spy 证明每读写经 Store，manifest hash 可重算；同提接口文档与 30 用例测试。
- 落点：runtime/artifact_store（1）；tests/artifact（1，+475）；docs/interfaces（1）。
- 内容性质：生产代码 + 测试 + 文档或规格。
- 意图证据：消息自述 requirements/tests（30/30；全量 Python 回归 131 passed；SPY_IO_AND_HASH_RECOMPUTE PASS）；变更面证据吻合（3 文件全新增）。
- 关联：DATA 族承接 1479/1484（重叠区合同冻结），下启 1499（DATA-004 溯源）；1507 为该任务补漏。
- 不确定点：无。

### seq 1495 | 0d32c07d | 2026-09-03 | M（CPU-001）
- 做了什么：ISA 与 OS 安全探测：`providers/cpu/common/capability_detect.c` 等 4 文件（合 +743）实现 CPUID 叶、AVX/AVX2/FMA/AVX512F/CD/BW/DQ/VL、BMI2、OSXSAVE/XCR0 探测，区分"硬件支持 vs OS 可安全执行"；`cpu_capability.schema.json` + feature matrix 模拟负测 + `run_cpu_capability_checks.py` runner + 文档。
- 落点：providers/cpu（4）；tests/cpu（3，+458）；docs/architecture（1）。
- 内容性质：生产代码 + 测试/机器门 + 文档。
- 意图证据：消息自述 requirements（不读硬编码核心数；同 schema 跨平台）与 tests（R1-R8 ALL PASS + gcc/clang 双编译 live probe）；变更面证据吻合。
- 关联：CPU 族（CPU-002→003→004→005，seq 1501/1503/1511/1513）的能力探测地基；宪章 §10.3 benchmark/profile 选 ISA 的前置件。
- 不确定点：无。

### seq 1496 | 9cc5cb51 | 2026-09-03 | M（ACR-001，附带 ACK-001）
- 做了什么：固化 ACR 首发休眠边界：改写 `lib/acr/CMakeLists.txt` + README，新增 `lib/acr/ci/ACR_DORMANT_GUARD.md` 与 `check_acr_dormant.py`（机器 guard）：默认/Release/install/product manifest 不构建不链接不加载 ACR/CUDA；显式 ON 仅隔离实验 target，不被 release preset 接收。
- 落点：lib/acr（4 文件 +454/-3）。
- 内容性质：CI 或机器门 + 生产构建代码 + 文档。
- 意图证据：消息自述 requirements + tests 行（默认/ON graph ACR count=0；add_subdirectory→FATAL 负测；ACK-001..007 guard 通过 + selftest 检出 4 违规）；变更面证据吻合（2 新增 2 修改，全在 lib/acr）。
- 关联：直接落实宪章 §10.1"ACR 暂不接入/默认关闭"；与 1519 迁移的 `test_iso_acr_gpu_isolation.py`（CLI-002 恢复顶层键拒绝面）前后呼应。编号字段中 ACK-001 为 guard 断言组号，非双任务。
- 不确定点：ACK-001..007 的逐项内容未展开——未证实。

### seq 1497 | da5ca108 | 2026-09-03 | M（ABI-003）
- 做了什么：实现受限 DLL 加载：`runtime/module_loader/secure_loader.{c,h}`（合 +953）——canonical 绝对路径受控 dlopen、加载前后校验路径/hash/module ID/ABI/build ID，劫持/symlink/hash mismatch/缺 symbol/wrong arch 全拒绝，日志不泄凭据；配套 `abi003_loader_probe.c`、`test_secure_loader.py`（36 checks）、架构文档。
- 落点：runtime/module_loader（3）；tests/abi（2，+659）；docs/architecture（1）。
- 内容性质：生产代码 + 测试 + 文档。
- 意图证据：消息自述 requirements/tests（含 noop 真实模块 LOAD_OK/DESCRIBE_OK/RELEASE_OK 正测）；变更面证据吻合。
- 关联：ABI-002（1487）生命周期与 ABI-004（1504）注册表之间的加载链；1508 echo 模块再扩展其正/负语义。
- 不确定点：无。

### seq 1498 | a0e118ed | 2026-09-03 | M（RT-003）
- 做了什么：接入原子线程预算：`lib/core` 的 context/scheduler/module_adapters 与 `include/astrocs/core/{context,scheduler}.h` 小改（生产面仅 +52/-7），ThreadBudget acquire/release 串到 scheduler→RunContext→module host API，lease RAII 异常/取消回收，上限由资源+profile 计算；新增接线测试 +314。
- 落点：lib/core（3 修改）；include/astrocs（2 修改）；tests/runtime（1 新增）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述 requirements（"不用 ThreadLease::make"）与 tests（3 tests OK：竞争/嵌套/超额/取消/budget=1 负测 + 伪授权静态扫描；cmake 全链构建 100%）；变更面证据吻合（1 新增 5 修改，测试面占 86% 行数）。
- 关联：宪章 §10.4 统一线程预算落地；1502 为其旧测试适配尾巴，1506/1512/1517 沿同一批 core 头文件递进。
- 不确定点：无。

### seq 1499 | 9256587d | 2026-09-03 | M（DATA-004）
- 做了什么：完善产物溯源：`runtime/artifact_store/provenance.py`（+769）记录 product/module/ABI/data schema 与 doc revision/history 区分、source commit/config/provider/worker/input hashes 与 science IDs，同输入同 digest，旧版本不静默接收，privacy scan 无泄露；配套文档与测试 61/61。
- 落点：runtime/artifact_store（1）；tests/artifact（1，+672）；docs/interfaces（1）；DOCUMENT_INDEX（补登 ABI-003/DATA-003/CPU-001 文档）。
- 内容性质：生产代码 + 测试 + 文档。
- 意图证据：消息自述 requirements/tests（91 passed；artifact 全回归 137）；变更面证据吻合。
- 关联：直接建在 1494 production_store 之上；卡面编号字段并列 ABI-003/CPU-001/DATA-003 系文档补登引用，非本提交实现对象。1507 补其集成漏交。
- 不确定点：无。

### seq 1500 | 2a8676bf | 2026-09-03 | M（DOC-001）
- 做了什么：建立机器追溯合同：`docs/traceability/` 矩阵（SCI→ALG→DATA/API/ARCH→MOD/SRC→TEST→EVIDENCE 每层必填、每模块空缺显式 MISSING）、`schemas/traceability_matrix.schema.json`、`tools/traceability/`（check_traceability_matrix.py + gen_traceability_csv.py）、`tests/traceability/` 7 文件含 6 个负例 fixture（bad_id_format/chain_break/dangling_ref/duplicate_module/empty_cell/missing_column）。
- 落点：tests/traceability（7）；docs/traceability（4，+866）；tools/traceability（2，+510）；schemas/（1）；CHANGELOG/DOCUMENT_INDEX。16 文件 +1795/-0。
- 内容性质：CI 或机器门 + 测试 + 文档或规格。
- 意图证据：消息自述 requirements + tests 行（pytest 9 passed；TRACEABILITY_MATRIX_PASS 25 modules 0 errors）；变更面证据吻合（14 新增）。
- 关联：为前述 IO/ABI/RT/CPU/DATA 各任务提供追溯层；GOV 族治理工具链（1482 check_version_namespaces 在重叠区）同族延续。
- 不确定点：25 modules 清单以矩阵文件为准未展开——未证实。

### seq 1501 | c20a94c9 | 2026-09-03 | M（CPU-002）
- 做了什么：提供保守基线后端：`providers/cpu/baseline/`（baseline_provider_v1.h + baseline_provider.cpp，合 +768）——无 /arch:AVX*、SSE2 默认、12 kernel 可靠实现、provider query/self_test/export/ABI 完整、能力不足→baseline、无全局 SIMD 静态初始化；tests/cpu 侧 5 文件（+969）oracle/handshake/so_load 门。
- 落点：providers/cpu（2）；tests/cpu（5）。
- 内容性质：生产代码 + 测试/机器门。
- 意图证据：消息自述 requirements + tests 行（12 kernels f64 2e-4 + determinism budget1vs4 bitwise + worker 1/N）；变更面证据吻合。
- 关联：CPU 族第二级（1495 探测→1501 基线→1503 AVX2→1511 AVX512→1513 路由）；"baseline 兜底"语义在 1511/1513 反复引用。
- 不确定点：无。

### seq 1502 | a37aeb58 | 2026-09-03 | S（RT-003 适配，编号关联 1498）
- 做了什么：旧 C++ 单测 `tests/unit/core_context_test.cpp` 适配 RT-003 新预算语义（+11/-3）：原 `set_thread_budget` 仅计数且期望 cap 不计数，改为 `create_thread_budget` 注入 + `acquired()` 断言 + RAII 归还后可再租。
- 落点：tests/unit（1 文件）。
- 内容性质：测试。
- 意图证据：消息正文（非 requirements 模板）直述适配动机与旧语义缺陷；变更面证据：单文件微改，与"适配"自述完全吻合。
- 关联：1498 的滞后清扫，二者同编号 RT-003 但不连续（中间隔 1499–1501），故独立成条不并组。卡面编号附带 CORE-005 系所改测试文件自述的 TESTS PASS 组号。
- 不确定点：无。

### seq 1503 | d823895f | 2026-09-03 | M（CPU-003）
- 做了什么：增加 AVX2 热点后端：`providers/cpu/avx2/`（avx2_provider.{h,cpp} 合 +511，单独 -mavx2 -mfma target）仅迁移 profile 指定热点 kernel，其余回落 baseline，非支持 CPU 不加载，FMA 归约顺序变化记录为验收项；tests/cpu/avx2 5 文件 +1051（gate 负测/oracle 对照/objdump 编译隔离验证）。
- 落点：providers/cpu（2）；tests/cpu（5）。
- 内容性质：生产代码 + 测试/机器门。
- 意图证据：消息自述 requirements（"不复制科学模块""target 单独"）与 tests（2 hotspot oracle rel 2e-4 + bitwise determinism）；变更面证据吻合。
- 关联：CPU 族第三级；1526（V81-ADOPT-006）的文档标题抽取显示 CPU_003 架构文档在此后仍被再整理（该文档标题增删出现在 1526 卡面，属版本行同步附带）。
- 不确定点：1526 卡面出现"CPU AVX2 provider (CPU-003)"标题新增的原因（疑为文档改版）未核——未证实。

### seq 1504 | 40b5687b | 2026-09-03 | M（ABI-004）
- 做了什么：接入动态模块注册：`runtime/registry/module_registry.{c,h}`（+1165）——运行时 registry 只来自 product manifest + DLL query，检测重复 ID/版本冲突/未登记 DLL/descriptor 不一致，module.yaml/embedded descriptor/release manifest 三方比对，static factory 不作正式路由；tests/abi 探针 + 28/28 pytest。
- 落点：runtime/registry（2）；tests/abi（2，+545）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述 requirements/tests（selftest SHA 向量/越界硬失败/mask 位测试）；变更面证据吻合。
- 关联：ABI-003（1497）加载链下游；1508 echo 模块用其"删除 kind=13/替换 kind=12 无 fallback"负测复用本注册表语义。
- 不确定点：无。

### seq 1505 | b09e4b2c | 2026-09-03 | M（TST-001）
- 做了什么：冻结模块验证结构：`tests/testkit/` 11 文件（oracle_sum/property_invariant/fixture_hash/negative_bad_input/perf_linear/check_constant 等 kit 件 + test_metadata.schema.json + testkit.spec.md + registry 样例）与 `tools/testkit/check_testkit.py`（+340，--strict 门、故障注入 harness 必败、module:<id> 选择）。
- 落点：tests/testkit（11）；tools/testkit（1）。
- 内容性质：测试基建 + CI 或机器门 + 规格。
- 意图证据：消息自述 requirements（"生产函数禁生成期望"）与 tests 行（TESTKIT_PASS errors=0；c01–c04 四场景）；变更面证据吻合（12 文件全新增）。
- 关联：验证方法学层，服务 IO/ABI/CPU 各族 oracle/负测范式（1501/1503 的 oracle+gate 结构与其同型）。
- 不确定点：无。

### seq 1506 | deef06d8 | 2026-09-03 | M（RT-004）
- 做了什么：建立共享执行器：`include/astrocs/core/executor.h` + `lib/core/src/executor.cpp`（合 +412）——CPU heavy executor 与有界 I/O executor 唯一共享、模块不得 std::thread 私建永久池、lease 注入不超预算、worker cv 不空转、取消唤醒、join 生命周期无 UAF；`test_rt004_executor.py`（+451，8/8 + 静态扫描无 detach/async/私池）。
- 落点：include/astrocs（1）；lib/core（1）；tests/runtime（1）。
- 内容性质：生产代码 + 测试/机器门（静态扫描）。
- 意图证据：消息自述 requirements/tests；变更面证据吻合（3 全新增）。
- 关联：直接消费 RT-003（1498）预算对象；1512 trace 回填其 worker/provider 观测。宪章 §10.4"一个进程一个调度器"的构件化。
- 不确定点：无。

### seq 1507 | 1df9a7ee | 2026-09-03 | M（DATA-004 补漏）
- 做了什么：单文件补交 `runtime/artifact_store/production_store.py`（+288/-11）：把 DATA-004 已验收变更集中的 sidecar 原子发布接 provenance（cap digest 校验/history 链/发布恢复加载）接线补上。
- 落点：runtime/artifact_store（1）。
- 内容性质：生产代码（补漏）。
- 意图证据：消息正文明确自述"FG 首轮集成漏提交该文件，仅入 provenance.py+测试+文档；补漏提交使主库与 DATA-004 验收一致"，附 review_notes；变更面证据：单文件修改与该叙述吻合。
- 关联：1499（DATA-004 本体）的集成尾款；与 1516（LOG-002 补登索引）、1528 台账"补登 V8-CI-001（前次集成遗漏）"构成同一"漏-补"模式的三例。
- 不确定点：无。

### seq 1508 | d9c39803 | 2026-09-03 | L（ABI-005；卡面附带 ABI-03 系 owner 号 SA-ABI-03 误提取）
- 做了什么：新增 echo conformance 探针模块：`modules/conformance/echo/`（echo_module.c 等 6 文件 +1742）覆盖 artifact 读写、host allocator、logger、metrics、cancel、executor、error 全部 host callback 正/负语义；编译零告警、唯一导出 `astrocs_module_query_v1`、删除/替换 DLL 证明无静态 fallback（registry kind=13/12 负测）；`test_abi005_echo.py` 29 checks。按文件集合归纳。
- 落点：modules/conformance/echo（6）；tests/abi（1）。
- 内容性质：测试（conformance 模块本体是验证探针资产）+ 生产代码边界待议——卡面归代码面 7 文件。
- 意图证据：消息自述 requirements/tests 行详尽（loader 正/篡改负 + 三方一致 + 源码无静态副本）；变更面证据吻合。
- 关联：ABI 族（001→002→003→004→005）收口件；复用 1492 noop、1497 loader、1504 registry 三件做全链验证。
- 不确定点：无。

### seq 1509 | 4bb34b68 | 2026-09-03 | M（RT-005）
- 做了什么：完善模块资源计划：`include/astrocs/core/plan_estimator.h` + `lib/core/src/plan_estimator.cpp`（合 +719）——从 input metadata 计算 work_units/axis/min-max useful workers/peak memory/读写 bytes/kernel IDs/serial sections；不同图像尺寸/order/输出 projection 改变计划；估算与运行峰值在冻结误差范围内；work_units=1 的 heavy 任务拒绝或标 tiny；测试 5/5 + C++ driver 独立 checks=108。
- 落点：include/astrocs（1）；lib/core（1）；tests/runtime（1，+525）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述 requirements/tests（oracle sweep 168 cases ok=150 rejected=18）；变更面证据吻合。附带自述"constraints FAIL 属基线 c169615 冻结对照，与本次无关"（将前区基线的既有红项与本提交切割）。
- 关联：为 1513 CPU-005 的 benchmark 选择与 1512 trace 的 work units 观测提供计划面；卡面编号附带 RT-05 系 owner 号 SA-RT-05。
- 不确定点：c169615 基线对照细节未展开——未证实。

### seq 1510 | 442958df | 2026-09-03 | M（IO-003）
- 做了什么：实现原子产物发布：`runtime/io/` 新增 hips_output_store/hips_output_fixture/fits_verify（合 +1119）——唯一用户路径或 run ID 目录、临时写→关闭→fitsverify→SHA256→原子 rename→完成 manifest；默认不覆盖、显式 overwrite 才可；并发不同 run 不互覆；中断后无完成标记；路径穿越拒绝；tree hash 可重算；17/17 + io 全套 43 passed + artifact 回归 137。
- 落点：runtime/io（2）；tests/io（2，+522）；docs/interfaces（IO_003 文档）；DOCUMENT_INDEX。
- 内容性质：生产代码 + 测试 + 文档。
- 意图证据：消息自述 requirements + 验证摘要（独立只读复核镜像重验 16 线程并发/中断/权限/符号链/tree-hash/astropy oracle 同判定）；变更面证据吻合。
- 关联：IO 族收口（001 读→002 输入合同→003 输出发布）；与 DATA-003（1494）的"原子 publish"在产物层与文件层两处独立实现，属同语义双层。
- 不确定点：无。

### seq 1511 | b99fcd80 | 2026-09-03 | M（CPU-004）
- 做了什么：增加 AVX512 热点后端：`providers/cpu/avx512/`（+511）只迁移实测获益 kernel——表内恰 1 热点 hips-bulk，单独 -mavx512f/cd/bw/dq/vl target；缺子集/OS ZMM 不可用拒绝、非法指令保护（`check_avx512_illegal_instr.py` 新增）；baseline/AVX2/AVX512 三路 oracle rel≤2e-4、determinism 逐位；tests/cpu 6 文件 +1368。
- 落点：providers/cpu（2）；tests/cpu（6）。
- 内容性质：生产代码 + 测试/机器门。
- 意图证据：消息自述 requirements/tests + 验证摘要"stale-base 判定通过（中间 4 commit 零触碰 cpu 路径）"；变更面证据吻合。
- 关联：CPU 族第四级；1513 的路由表以本后端为最上层选项。1519 基线预存 FAIL 提及 protocol test_06 属 CPU-004 域（跨族红项归属注记）。
- 不确定点：无。

### seq 1512 | b2e3b0af | 2026-09-04 | M（RT-006）
- 做了什么：trace 记录真实运行事实：改 6 个 core 头 + 5 个 lib/core 源（context/contracts/executor/runtime/scheduler/trace + module_adapters），新增 `runtime/pipeline/trace_replay.py` 与 `test_rt006_trace.py`（+636）——trace 记实际 module/DLL/hash/build/entry/call count/worker/provider/artifact/timing/error/checkpoint，executor/provider/monitor 填写、禁 config 冒充，7 个 P2 节点各一次，隐藏 session/重复调用检测（双实现抓出），JSONL 可重放。
- 落点：include/astrocs（6 修改）；lib/core（5 修改）；runtime/pipeline（1）；tests/runtime（1）。13 文件 +1557/-4。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述 requirements + tests（driver checks=98 failures=0；module_calls==7/call_count==1；provider avx2 观测）；变更面证据吻合（10 修改 3 新增，触碰面宽但删除仅 -4）。自述附"include/astrocs/core 头 scope 按 RT-003/004/005 同 owner core 契约先例裁决放行"。
- 关联：卡面编号附带 RT-003/RT-05 系回归引用与 owner 号；观测面（本件）→ 呈现面（1518 LOG-003 渲染）→ 校验面（1515 LOG-002 CSV）构成观察三角。
- 不确定点：无。

### seq 1513 | 9096d08d | 2026-09-04 | M（CPU-005）
- 做了什么：固化逐内核后端选择：新增 `lib/backend_host/cpu_routing.{cpp,h}`（合 +618，-43 为既有文件改写）——kernel_id 粒度路由、禁全局 preferred_isa；query→self_test→eligible→benchmark→select 管线；伪造 profile/类型篡改/build·CPU·OS·benchmark·self-test hash 变化/self_test 失败/缺 benchmark/oracle-fail/NaN·Inf/低收益全退回 baseline；路由表 JSON per-kernel provider；10/10 T1–T10。
- 落点：lib/backend_host（2）；tests/cpu/dispatch（2，+523）。
- 内容性质：生产代码 + 测试/机器门。
- 意图证据：消息自述 requirements/tests（grep preferred_isa 仅注释）；变更面证据吻合。自述"CPU-006 现场 benchmark 未合入、门限 0.03 同源冻结"——预告后续未在本片出现。
- 关联：CPU 族顶格件（001 探测→002 baseline→003 AVX2→004 AVX512→005 路由）；宪章 §10.3"逐内核 benchmark 选择、禁硬编码"的实现件。
- 不确定点：CPU-006 的去向在本片之后的 seq 才能核对——本片未证实。

### seq 1514 | 10a59723 | 2026-09-04 | M（CLI-001）
- 做了什么：V7 统一命令面骨架：`cli/commands.cpp`（+409）统一命令分派、`cli/parser.cpp` 解析小改，新增契约 `contracts/config/cli_modules_list.schema.json`、`cli_selftest.schema.json`，测试 `test_cli_v7_surface.py`（19）/`test_cli_build.py`（6）/`test_cli_protocol.py` golden 0.11.0-alpha.1。
- 落点：cli（2 修改）；tests/cli（3）；contracts/config（2）。
- 内容性质：生产代码 + 测试 + 合同 schema。
- 意图证据：消息自述 requirements（BLD-002 兼容 target 禁止正式 install）与 tests 行（25 passed 复验 + assertNotIn install(TARGETS astrocs)）；变更面证据吻合。附"5 基线预存 FAIL（clean b99fcd8 同失败，非本任务回归）"注记。
- 关联：CLI 族三件套（1514 骨架→1519 隔离→1520 语义固化）；引用重叠区 BLD-002（1485）。
- 不确定点：基线预存 FAIL 五项清单在返回包 logs，卡片未展开——未证实。

### seq 1515–1516 | 4ca0d0e9, 3b6fc397 | 2026-09-04 | L+S（LOG-002，同编号连续成组）
- 做了什么：1515（4ca0d0e9）资源监控 CSV 落盘与机器验证：`runtime/monitoring/` 6 件（monitor/runner/trace_feed/linux_procfs/windows_pdh_etw/__init__，合 +1358）——前后端分离、init/io/active phase 分离 CSV、seed+链式指纹防伪造/篡改、run_id/seq/ts 单调、windows 后端显式未实现、无 monitor 重任务抛 MonitorRequired、观察面只读 trace observer 永不读 config；`tools/monitoring/verify_monitor_csv.py` 机器验证；测试 20/20。1516（3b6fc397）为该任务补登 DOCUMENT_INDEX（+3）：RESOURCE_MONITORING_CONTRACT.md 漏登致 check_doc_index FAIL→PASS。分级 L 部分按目录直方图归纳（9 文件 +2042 全新增）。
- 落点：runtime/monitoring（6）；tools/monitoring（1）；tests/monitoring（1，+436）；docs/architecture/observability（1）；1516 仅 docs/DOCUMENT_INDEX.yaml。
- 内容性质：生产代码 + CI 或机器门 + 文档（1516 纯索引）。
- 意图证据：1515 消息自述 requirements/tests（tamper/forged 篡改 FAIL 语义与返回包一致）；1516 消息自述 requirements（"GOV-002 覆盖约束 + 按 STRUCTURED_LOGGING 先例补登 ACTIVE_NORMATIVE"）——两卡变更面与自述逐一吻合；1516 单文件 +3 的 S 体量支持"补漏"定性。
- 关联：观察面与 1512（RT-006 trace）互证；与 1518（LOG-003）同族递进；1516 与 1507 属同一"集成漏登→单文件补"模式。
- 不确定点：windows_pdh_etw.py"显式未实现"的占位形态未展开——未证实。

### seq 1517 | e78dac6a | 2026-09-04 | M（RT-007）
- 做了什么：固化取消、失败与恢复语义：改 `lib/core/src/{checkpoint,scheduler}.cpp` 与两对应头（生产面 +219/-26），新增 `test_rt007_cancel_resume.py`（+436）——Scheduler::cancel() 桥到 RunContext CancellationToken 统一取消 token；节点 fn 抛 C++ 异常→INTERNAL 失败传播不杀 worker；取消 latch 后无 false-success（修复竞态：真实失败根保留原域不被取消掩盖）；checkpoint phase scope 隔离（begin(run_id,phase_scope)，不同 scope 互不可见/中途切换拒绝/半成品拒绝）；resume 门只接受 hash/schema 匹配的已发布 artifact；run 边界清陈旧取消。
- 落点：include/astrocs（2 修改）；lib/core（2 修改）；tests/runtime（1 新增）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述 requirements + 验证摘要（"孤儿 WIP 接管补齐 A+B+C+D"与 30 tests 全绿清单）；变更面证据吻合。
- 关联：RT 族收口（002→003→004→005→006→007），依赖 1498 预算与 1506 executor；自述"DOC_INDEX 经 LOG-002 补登后 PASS"直接把 1516 编入自身验证链。
- 不确定点："孤儿 WIP 接管"的前身（哪个会话/工作区产物）卡片无证据——未证实。

### seq 1518 | 0b0d2694 | 2026-09-04 | M（LOG-003）
- 做了什么：运行图渲染工具：`tools/graph/render_run_graph.py`（+942）从 plan/trace 生成 DOT/SVG/JSON，真实入口/调用计数/并行轴/workers/provider/耗时/DLL hash/artifact hash 全从 trace 观测取（禁 config 冒充）；机器验证图与 trace 一致（GRAPH_CONSISTENT diffs=0, 7 nodes；tamper 负样例 exit 1）；零第三方依赖（无 graphviz 也产 DOT+最小 SVG）；附 RUN_GRAPH_CONTRACT.md + 索引补登。
- 落点：tools/graph（1）；tests/monitoring（1，+373）；docs/architecture/observability（1）；DOCUMENT_INDEX（+3）。
- 内容性质：生产代码（工具）+ 测试 + 文档。
- 意图证据：消息自述 requirements + 验证行（16/16 + LOG-002/001 回归 44/44 + trace 9/9）；变更面证据吻合。
- 关联：LOG 族（001 在重叠区 1476→002 在 1515/1516→003 本件）呈现层收口；消费 1512 trace 面。
- 不确定点：无。

### seq 1519 | de2d6d7f | 2026-09-05 | M（CLI-002）
- 做了什么：隔离三个阶段入口（删除式重构，净 -294 行）：`cli/parser.cpp` 删 kValueFlags 的 --phases/--preset、kRules 删 run/graph 行、kHelp 删 run --phases——run/graph 顶层命令现 parse_fail unknown command（exit 2）；`cli/commands.cpp` 全删 cmd_run_pipeline（--phases 连续调度 P1/P2/P3 + ArtifactStore）与 cmd_graph（-328 行）；cmd_phaseN_run 补 validate_config_full（unknown key→3）恢复 ACR/GPU backend 顶层键拒绝面；6 个测试文件同步迁移（golden 删 run --phases、negative 断言 exit 2、run --phases N→phaseN run 逐相独立 session/manifest/run ID）。
- 落点：cli/commands.cpp、cli/parser.cpp；tests/cli 6 文件（test_cli_build/test_cli_protocol/test_iso_acr_gpu_isolation/test_monitor_events/test_phase123_pipeline/test_phase3_inprocess）。
- 内容性质：生产代码（删除为主）+ 测试迁移。
- 意图证据：消息正文（非模板行）逐文件列出删除项与恢复的拒绝面；变更面证据强吻合：+104/-398、8 文件全为修改、commands.cpp -331。自述附基线预存 FAIL（protocol test_06 v2 profile 属 CPU-004 域）与环境性 ERROR（2c2g fixture）注记。
- 关联：宪章 §3.2"严禁隐式串接三 Phase 为一次运行"的命令面执行件；承接 1514 CLI-001；1520 固化其语义。CLI 族编号卡面附带 CLI-04 系 owner 号。
- 不确定点：无。

### seq 1520 | 257ae1f4 | 2026-09-05 | M（CLI-003）
- 做了什么：新增 `tests/cli/test_cli003_semantics.py`（+435，16 项 golden，生产二进制实测冻结）：validate 浅校验零科学执行（unknown key 精确键名→3、schema_version 缺失→3/≠1→2、深垃圾不递归、I/O spy 零产物零 mtime、300 项 manifest <10s）；verify 不重算（手工 complete manifest 闭环 rc0、sha 篡改→8）；phaseN run 语义（新 run_id journal 不覆盖先前 manifest、incomplete 永不可 verify→8、失败 run 事件面无 resource/backend、--force/--overwrite/--resume 非命令面旗标→2）。test-only 无代码改动。
- 落点：tests/cli（1 文件）。
- 内容性质：测试。
- 意图证据：消息自述详尽（16 项断言逐类枚举 + baseline: 无）；变更面证据吻合（单文件纯新增）。
- 关联：为 1519 的删除结果出具行为冻结；退出码字面量 2/3/8 在本条首次成体系固化为 CLI 合同。
- 不确定点：无。

### seq 1521 | a4fdee3f | 2026-09-05 | L（V81-ADOPT-001）
- 做了什么：注册控制包 V8.1 并冻结工作区身份：整包落位 `engineering/control/active/AstroCS_ALPHA0.11.0_EXISTING_WORKSPACE_CI_CONTROL_V8_1_20260905/`——56 文件 +2660：00_READ_FIRST 至 15_SUPERSESSION_NOTICE 十五份规程（唯一执行入口/冻结约束/当前状态审计/VM 与 CI 架构/前台运行手册/固定 SubAgent 绑定 yaml/任务图与门禁/CI 机器合同/main-only git 协议/Windows 与 Fatduck 验证/安全与 runner/证据审核包/失败政策/主要参照/token 节约执行/supersession 通知）+ CONTROL_MANIFEST.json（276 行）+ CONTROL_TASK_LEDGER.csv + GATE_REQUIREMENTS.csv + SHA256SUMS + 版本文件；另 `evidence/v8_1_ci_control/` 5 文件（TASK_STATE/WRITE_LEASE 等控制面初始化）。分级 L 按目录直方图归纳，不逐文件展开。
- 落点：engineering/control（56，含 1 个二进制文件，卡面未列其名）；evidence/v8_1_ci_control（5）。
- 内容性质：控制包或台账（纯治理注册，代码面 0）。
- 意图证据：消息无正文，仅标题自述"注册控制包 V8.1 并冻结工作区身份"；变更面证据：全新增、SHA256SUMS/CONTROL_MANIFEST 等完整性构件与 WRITE_LEASE/TASK_STATE 初始化同提，支持"注册+冻结"定性；机械抽取的文档标题集合（00｜唯一执行入口…12｜失败政策等）佐证包体结构。
- 关联：V8.1 波次的起点件，1522–1526（ADOPT-002..006）为其接管序列，1527 起 V8-CI 系列在其任务图下运行；本提交后所有任务提交流入 `evidence/v8_1_ci_control/` 台账面。
- 不确定点：那 1 个二进制文件（疑为包原件 zip）的路径与散列未在卡面呈现——未证实。

### seq 1522 | af9ecdc5 | 2026-09-05 | M（V81-ADOPT-002）
- 做了什么：冻结预存状态与远端关系：`ci/verify_workspace_adoption.py`（+239，机器校验器）+ evidence 面 22 文件——PREEXISTING_CHANGES.md 把 1 项 tracked 修改（dist/audit zip 工作区删除）与 90 项 untracked 逐类归类（根部历史控制包 11 项、artifacts/prerelease_v5 27 项、根 astrocs_run_*.json 44 项等，处置一律保留原样不删不改）；REMOTE_RELATION.json 记录 fetch --prune 证据；findings.csv 登记 F-001..006（含 AstroCS.wiki gitlink LOW、41 条 worktree 记录 LOW）。
- 落点：evidence/v8_1_ci_control/adoption/（22）；ci/（1 代码）。
- 内容性质：控制包或台账 + CI 或机器门。
- 意图证据：标题自述"冻结预存状态与远端关系"；变更面证据：证据文件集合（identity_before/after_fetch、diff_stat、index_gitlink_check、remote_v_raw/sanitized 等）即取证产物本身，与"冻结记录"吻合。
- 关联：ADOPT-001 注册包的取证续件；其"不删除不修改"处置基线被 1523 对账与 1526 版本收敛引用。
- 不确定点：41 条 worktree 记录与 main-only 规则的张力仅作 LOW finding 登记、未处置——后续处置在本片范围外。

### seq 1523 | 98c2354f | 2026-09-05 | L（V81-ADOPT-003）
- 做了什么：任务证据对账重建 TASK_STATE 191 项：`ci/reconcile_state.py`（+230）+ STATE_RECONCILIATION.csv（卡面样例 60+ 行：task_id/v71_status/review_claim/current_evidence/final_status/source_of_status/commit）——对 V7.1 静态台账与 review claim 逐项对账（如 GOV-001→065c81de、ABI-001→76b85d45 均以 git_commit+artifact_verified 复核 CLOSED；CLI-002 在评审快照 MISSING→现证据 CLOSED；CLI-004 NOT_STARTED not_dispatched_no_evidence）；COMMIT_LEDGER.jsonl；TASK_STATE.json 重建。分级 L 按文件集合归纳（12 文件，evidence 面 +4387 行主体为机器生成台账）。
- 落点：evidence/v8_1_ci_control/（11）；ci/（1）。
- 内容性质：控制包或台账（对账重建）。
- 意图证据：标题自述"重建 TASK_STATE 191 项"；变更面证据：reconcile_state.py 为可复核对账器，CSV/JSONL 为输出物；抽读的 CSV 行含本片全部主责任务号的 commit 指针（如 DOC-001→2a8676bf、TST-001→b09e4b2c），与本区各条 sha8 一致。
- 关联：为 V8-CI 系列提供任务态真相源。
- 不确定点：191 项与卡面样例 60 项的差额清单未展开核对——未证实。

### seq 1524 | becc85a0 | 2026-09-05 | M（V81-ADOPT-004）
- 做了什么：Agent 主机工具链盘点与锁存：`ci/toolchain.lock.json`（138 行：cmake/gcc/g++/clang/clang++/ninja/make 逐项 present/path/version/source_cmd）+ `ci/toolchain.policy.json`（39 行：agent_host require_exact_hosted_versions=false/host_reprovisioning=false/acr=false）+ `ci/verify_toolchain.py`（+208）+ `docs/operations/TOOLCHAIN_AGENT_HOST.md` 快照；findings 实测：python3 3.13.5、git 2.47.3、zstd/xz 在位；**Build drivers（cmake/编译器/ninja/make）全部缺失 high、pytest 缺失 medium、ccache 缺失 medium**；仓库既有约束只读取证（CMakePresets 3.31.0/CMakeLists 3.24/C++17）。
- 落点：ci/（3）；docs/operations（1）；evidence/v8_1_ci_control（8）。
- 内容性质：CI 或机器门 + 数据或产物（lock 快照）+ 台账。
- 意图证据：标题自述"盘点与锁存"；变更面证据：三件锁存/政策/校验器与文档四件套成组出现，吻合。
- 关联：ADOPT-005（1525）为其索引补登，V8-CI-007（1538）的 `bootstrap.py --policy` 直接消费本 policy 文件。控制节点工具链缺编译器的记录是后文 deep/windows 检查走 hosted 真跑 + 本地 SKIPPED(waivable) 设计的证据前件。
- 不确定点：无。

### seq 1525 | 1ecbf10e | 2026-09-05 | M（V81-ADOPT-005）
- 做了什么：补登工具链快照恢复活动索引：`docs/DOCUMENT_INDEX.yaml` +3 登记 TOOLCHAIN_AGENT_HOST.md（check_doc_index --strict 缺登 FAIL 的补漏）；evidence 面 8 文件台账推进；findings F-004 记录派发包 acceptance_command 路径笔误（tools/quality/check_doc_index.py 不存在，实际 tools/doccheck/）按实处理，F-002/F-003 对 AGENTS.md/memory.md 的核查结论为 NO-OP。
- 落点：docs/DOCUMENT_INDEX.yaml（1）；evidence/v8_1_ci_control（8）。
- 内容性质：文档或规格（索引）+ 控制包或台账。代码面 0。
- 意图证据：标题自述"补登…活动索引"；变更面证据：+3 行文档索引单文件，吻合最小改动定性。
- 关联：1524 的收尾件；与 1516、1507 属同一"漏登→补登"三模式第三例。
- 不确定点：无。

### seq 1526 | b4f923cc | 2026-09-05 | L（V81-ADOPT-006）
- 做了什么：统一活动版本源至 0.11.0-alpha.2 并同步冻结约束版本行：根 VERSION 0.11.0-alpha.1→alpha.2；`CMakeLists.txt` project(astrocs VERSION 0.10.0→0.11.0（修 BLD-002 登记的版本漂移，findings 自述；alpha 后缀由根 VERSION 唯一承载、生成串 ASTROCS_VERSION_STRING=VERSION+g<commit>）；AstroCS_ENGINEERING_CONSTRAINTS.md/HANDOVER/README/REVIEW/docs/governance/VERSION_NAMESPACES 等版本行同步；docs/owner 5 文件版本引用订正；新增 `ci/check_version.py` 一致性快速门 + `test_adopt006_version_gate.py`。分级 L（26 文件）按"版本行多点同步 + 门 + 台账"集合归纳。
- 落点：根 VERSION/CMakeLists/README/REVIEW/HANDOVER/CONSTRAINTS（8 修改）；docs/owner（5 微改）；ci/ + tests（新门 2 件）；evidence（8）。
- 内容性质：结构整理（版本行同步）+ CI 或机器门 + 台账。
- 意图证据：标题自述完整（含目标版本号字面量）；变更面证据：hunk 头抽取显示各文件仅版本相关行变动，findings.csv 逐项 drift_fixed 登记（文件→动作）吻合。
- 关联：GOV-003（1482，重叠区）版本事实源统一的 V8.1 再执行；1514 CLI golden 0.11.0-alpha.1 的协议面在此后进入 alpha.2 轨道（后续片核对）。
- 不确定点：卡面出现的 CPU_003 文档标题新增（见 1503 条）与本任务的关系未自明——未证实。

### seq 1527 | 0acfb514 | 2026-09-05 | L（V8-CI-001）
- 做了什么：检查器与测试资产盘点注册 70 项：`ci/checks.json`（+1815，唯一检查注册表初版 70 项）、`ci/INVENTORY_REPORT.md`（+79 盘点报告）、`ci/impact_map.json`（+20 影响映射占位）、`ci/validate_registry.py`（+178 注册表校验器）、`tests/test_index.csv`（+23）；evidence 台账 8 文件。分级 L 按文件集合归纳。
- 落点：ci/（4 新）；tests/（1）；evidence/v8_1_ci_control（8）。
- 内容性质：CI 或机器门 + 台账。
- 意图证据：无消息正文；标题自述"盘点注册 70 项"；变更面证据：checks.json 体量（+1815）与 validate_registry.py 同提，注册表性质由校验器存在佐证。70 计数在 1528 消息中自述"--plan-only fast selected 57"（70 中 fast 档 57）可交叉印证。
- 关联：V8-CI 系列基座；1528 台账行自述"补登 V8-CI-001 条目（前次集成遗漏）"——本提交的 TASK_STATE 登记者实为后一提交。
- 不确定点：impact_map.json 的 20 行内容与 1532 的 25 规则版本差异（初版规模）未核对——未证实。

### seq 1528 | 459da629 | 2026-09-06 | L（V8-CI-002）
- 做了什么：统一确定性 CI 执行器：`ci/run.py`（+1071）——唯一 CI 入口，--profile/--check/--focus/--changed-from/--plan-only 等 12 参数，argv 数组执行杜绝 shell 注入，mutates_workspace 前后 git status 对比，known_failures 基线 KNOWN_FAIL 计数分离，退出码 0=PASS/FATDUCK_PENDING、1=FAIL、2=runner 错误，CI_RESULT 写前按 ci_result.schema.json 自校验 + 原子覆盖；`ci/checks.schema.json`/`ci/ci_result.schema.json` 控制包副本在位（sha256 一致）；`ci/tests/` 29 用例覆盖 13 类场景（选择/plan-only/registry 非法/PASS/FAIL/TIMEOUT/SIGNAL/dirty 对照/空集不 PASS/fatduck 例外/changed-from/focus/known_failures 全分支，tempfile fixture 仓库内运行）。台账面补登 V8-CI-001、WRITE_LEASE 释放并转 V8-CI-003。分级 L（18 文件）按执行器+schema+测试+证据四集合归纳。
- 落点：ci/（run.py + 2 schema + tests/5 件）；evidence/v8_1_ci_control（10，+1892）。
- 内容性质：CI 或机器门 + 测试。
- 意图证据：无标准正文但消息要点清单极详（12 参数、退出码、验收 29 tests OK、--plan-only fast selected 57）；变更面证据吻合。
- 关联：消费 1527 注册表；其 run.py 在 1534 被加 probe_prerequisite 扩展（+7/-1 微改）。
- 不确定点：编号字段含 V8-CI-001/002/003 三号为台账连带（补登 001、释租转 003），本提交实现对象即 002。

### seq 1529–1530 | c5924da5, df01ea7b | 2026-09-06 | S+L（V8-CI-003，同编号连续成组：分发租约→实现）
- 做了什么：1529（c5924da5）纯租约提交：WRITE_LEASE.json 的 base_sha 从 0acfb514 对齐到 459da629 并更新 acquired_utc；dispatch/V8-CI-003.json 的 base_sha/allowed_paths（ci/**, tools/monitoring/**）/prohibited（commit/push/branch/worktree/clone/history_rewrite/scope_expansion/modify_control_package/modify_TASK_STATE/modify_WRITE_LEASE）展开重写——2 文件 +20/-6，S。1530（df01ea7b）实现动态资源探测与统一监控 wrapper：`tools/monitoring/resource_probe.py`（affinity/cgroup v2+v1 quota/meminfo，effective=min(affinity,quota) 动态并发，全字段 source 注记，零硬编码核数、缩容 affinity 注入测试证明跟随）+ `run_monitored.py`（argv 数组永不 shell=True、start_new_session+killpg 超时击杀、进程树 CPU%/RSS/PSS/IO/threads 采样、PROGRESS 双途径、evaluate() fail-closed 阈值判定）+ ci/tests 36 用例（busy-loop>50%/sleep 违规/64MB 泄漏/多线程≥3/timeout 2s 击杀/双进度途径）+ discover 65/65 + host_probe.json 本机快照（16C/8P quota=None）。分级 L 部分按证据集合归纳。
- 落点：1529 仅 evidence/v8_1_ci_control（2）；1530 tools/monitoring（2，+888）、ci/tests（2，+601）、evidence（9，+1007）。
- 内容性质：CI 或机器门 + 测试 + 控制包或台账。
- 意图证据：1529 标题自述"分发与写租约 base 对齐 459da629"，变更面证据（仅 WRITE_LEASE/dispatch 两 JSON 的 base_sha/acquired_utc/allowed_paths/prohibited 键变化）完全吻合；1530 消息自述与 allowed_paths 域（ci/**, tools/monitoring/**）逐文件吻合——写租约约束被遵守是其直接变更面证据。
- 关联：宪章 §10.5 重计算门禁与 §10.4 禁硬编码的 CI 侧执行件；1534/1536 各 heavy 检查 command 自含本 wrapper（"heavy 项 command 自含统一监控 wrapper"两度引用）。1529 与 1531/1533/1535/1537/1539 构成 V8-CI 的"chore 分发 + feat 实现"成对模式首对。
- 不确定点：无。

### seq 1531–1532 | b2d31bfe, c583e59d | 2026-09-06 | S+L（V8-CI-004，成组）
- 做了什么：1531（b2d31bfe）分发与写租约：evidence 面 WRITE_LEASE.json + 新增 dispatch/V8-CI-004.json，2 文件 +30/-4。1532（c583e59d）changed path→checks 映射与 fast 实测：`ci/impact_map.json` 重写为 25 规则全路径域（每规则 13 项 BASE、规格九类必含显式落位、fallback 最小 always-recheck 核心）+ `test_impact_map.py` +13（注册表一致性 8 + fixture 行为 5，含改 VERSION 仅选版本类；discover 78/78）+ fast 全量实测：**57 项 41 PASS/16 FAIL、wall 24.1s（<60s 目标）、最慢 UT-MONITORING 7.07s；16 项均主仓库固有红项，按失败政策登记 findings 移交 G-FIX 波**；changed-scope 演示 --changed-from HEAD~1 plan-only selected=32<57。
- 落点：1531 仅 evidence/v8_1_ci_control（2）；1532 ci/impact_map.json（+58/-15）、ci/tests（1，+297）、evidence（11，+2883，含 CI_RESULT_fast_full.json、20260905_V8-CI-004_fast_failures.md 与各阶段 log）。
- 内容性质：CI 或机器门 + 测试 + 数据或产物（实测结果 JSON/log）+ 台账。
- 意图证据：1531 标题自述与 2 文件证据面吻合；1532 消息要点与变更面吻合（impact_map.json -15 行重写对应"25 规则"扩张）。
- 关联：把 1527 注册表与 1528 执行器的 --changed-from 接成增量选择链；16 项固有红项移交 G-FIX 的落账文件 fast_failures.md 在此诞生；1534 自述"impact_map 不动：每规则 13 BASE 不变量与 heavy deep 检查不相容"延续本条不变量。
- 不确定点：16 项固有红项的逐项目录（checks.json id 级）未展开——未证实。

### seq 1533–1534 | d41f4516, e1fde093 | 2026-09-06 | S+L（V8-CI-005，成组）
- 做了什么：1533（d41f4516）分发与写租约（2 文件 +31/-3）。1534（e1fde093）linux-deep 检查注册与 deep 构建驱动链：`ci/checks.json` 70→77（BUILD-GCC-RELEASE 入 linux-main+deep；新增 DEEP-CLANG-BUILD/DEEP-SAN-ASAN/DEEP-SAN-TSAN/DEEP-COV-CPP/DEEP-COV-PY/DEEP-COMPLEXITY 六项 linux-deep；heavy 项 command 自含 1530 wrapper；prerequisite_tools 缺工具→SKIPPED(waivable)、hosted 真跑）；run.py/checks.schema.json/validate_registry.py 加 probe_prerequisite；`tools/quality/deep_ci_driver.py`（5 子命令 cmake/ctest argv 数组、逐步超时）、`ci_coverage_runner.py`（pytest-cov、仓库外 output-dir 受控 exit 2）、`check_complexity.py`（复杂度基线测量、threshold=null 不虚构）；`CMakeLists.txt` 接 QA-006 ccov/qa-sanitize/qa-sanitize-tsan（默认 OFF、复用 QA-002、互斥单选）+ `cmake/qa_coverage_report.sh`（llvm-profdata/llvm-cov）；docs/quality coverage/complexity 基线文档 v1（阈值 null 待 hosted 首跑冻结）；单测 96/96（test_deep_profiles +18）；plan-only fast=57/linux-main=71/linux-deep=7；本地 deep 实跑 PASS（6 SKIPPED(waivable)+DEEP-COMPLEXITY PASS）。分级 L（28 文件）按注册表/驱动/构建/文档/证据五集合归纳。
- 落点：ci/（4 改）；tools/quality（3 新）；docs/quality（2）；CMakeLists.txt + cmake/；evidence（15，+2975）。
- 内容性质：CI 或机器门 + 生产构建代码 + 文档 + 台账。
- 意图证据：1534 消息要点清单极详且与文件集合逐项对应（checks 77 计数、5 子命令、96/96、SKIPPED(waivable) 语义）；卡面编号附带 QA-002/QA-006 系 CMake 接线对旧质量检查构件的复用引用。
- 关联：waivable 语义沿用 `ci/checks.json` waiver 机制（AGENTS.md 所述 R-05/R-13 不掩盖红灯口径）；1536 的 WIN-* 三项沿用本条"heavy 自含 wrapper + waivable"模板。
- 不确定点：hosted 首跑冻结阈值的兑现时间在本片外——未证实。

### seq 1535–1536 | f5d579bc, 847a7a6b | 2026-09-06 | S+L（V8-CI-006，成组）
- 做了什么：1535（f5d579bc）分发与写租约（2 文件 +46/-3）。1536（847a7a6b）Windows 构建驱动与 candidate 打包链：`ci/checks.json` 77→80（WIN-BUILD-RELEASE（configure+build）/WIN-TEST-UNIT（ctest）/WIN-PACKAGE-CANDIDATE（install+package），platform=windows、heavy 自含 wrapper、waivable=true：Linux 上 SKIPPED(waivable)、hosted windows-2022 真跑）；`tools/quality/ci_windows_driver.py`（+755，五阶段驱动、argv 数组/shell=False/逐阶段超时/JSON summary；candidate 三清单 BUILD_PROVENANCE/SOURCE_MANIFEST/SHA256SUMS；排除源码/测试数据/build cache；verify_candidate 落 hosted-only 登记——dumpbin 导出 ABI/加载注册/CLI 入口/selftest Oracle，无伪造 PASS）；test_windows_ci.py +17（共 113 OK）+ test_deep_profiles 注册表计数断言 77→80 连带；plan-only windows-main 61。
- 落点：ci/checks.json、ci/tests（2）；tools/quality（1）；evidence（14，+5490，主体为各 plan 阶段 log 与 TASK_RESULT）。
- 内容性质：CI 或机器门 + 测试 + 数据或产物。
- 意图证据：消息要点与变更面逐项吻合（80 计数、五阶段、hosted-only 语义）；+5490 证据行集中于 logs 支持"驱动链实跑留痕"定性。
- 关联：宪章 §15.3 Fatduck/Windows 验证节点的 CI 化前半；1538 的 ci-windows.yml 与 1540 的 fatduck.yml 分别消费本驱动与 candidate 清单。
- 不确定点：无。

### seq 1537–1538 | a90dbb90, a032c0cc | 2026-09-06 | S+L（V8-CI-007，成组）
- 做了什么：1537（a90dbb90）分发与写租约（2 文件 +47/-3）。1538（a032c0cc）双平台 workflow、action 锁与 CI 辅助链：`.github/workflows/ci-linux.yml`（ubuntu-24.04；push main+dispatch+schedule）与 `ci-windows.yml`（windows-2022；push main+dispatch）——permissions contents:read、persist-credentials:false、全 action 锁完整 SHA、无算法命令；`ci/actions.lock.json`（checkout v7.0.1/upload-artifact v5.0.0(+v4.6.2 参考)）+ `verify_actions_lock.py`（在线 api.github.com 查询生成、机器复验、offline 结构校验模式）；`ci/bootstrap.py`（--policy 1524 toolchain.policy.json 逐项校验，缺失 exit 2 结构化 repair 报告）；`ci/select_profile.py`（push→linux-main/schedule→linux-deep/dispatch 白名单）；`ci/validate_candidate.py`（candidate zip 五类校验：三清单/哈希双向完备/成员排除/溯源必填）+ ci_windows_driver --zip 确定性打包连带；test_workflow_lock.py +38（共 151 OK）；自述"toolchain.policy.json 误覆盖已恢复(0 diff)"。
- 落点：.github/workflows（2）；ci/（bootstrap.py/select_profile.py/validate_candidate.py/actions.lock.json/verify_actions_lock.py/checks.json 微改/ci_windows_driver 连带/tests 1 件）；evidence（9）。
- 内容性质：CI 或机器门 + 生产代码（工具链脚本）。
- 意图证据：消息要点极详且与 19 文件集合吻合；编号字段 V8-CI-006 为对前任务的台账 CLOSED 补登引用。
- 关联：把 1536 的 Windows 驱动接入托管 workflow；1540 的 fatduck.yml 以 workflow_run 挂接本条 ci-windows.yml；actions.lock 三件（lock/verifier/test）是 supply-chain 门的成组证据。
- 不确定点："误覆盖已恢复"所指误覆盖事件的责任方未见于证据——未证实。

### seq 1539–1540 | 16441987, 9b3fe93e | 2026-09-06 | S+M（V8-CI-008，成组）
- 做了什么：1539（16441987）分发与写租约（2 文件 +45/-3）。1540（9b3fe93e）Fatduck 三 job workflow 与候选选择器：`.github/workflows/fatduck.yml`（+190）三 job——select-candidate（hosted：API 查询 Windows CI 成功 run + 同 SHA Linux PASS + candidate artifact）/fatduck-validate（[self-hosted, fatduck-realdata]：零 checkout、零 repo script、固定 harness 入口 run_validation.ps1）/notify-owner（仅此 job issues:write，单一固定 Owner Review Issue，变量缺失结构化跳过不伪造）；触发仅 workflow_run（AstroCS Windows CI completed）+ schedule 每 6h，无手动触发，concurrency 不取消 running；actions/download-artifact v8.0.1 在线锁定（3e5f45b2）追加 actions.lock.json（4 entries 复验 PASS）；select-candidate 显式双重校验 head_branch==main + conclusion==success；`ci/select_candidate.py`（+403，纯 stdlib + GITHUB_TOKEN 环境变量，no-candidate exit 3 结构化）+29 离线单测（共 180 OK）；plan-only 四档回归不变（57/71/7/61）。
- 落点：.github/workflows（1）；ci/（select_candidate.py、actions.lock.json、tests/1 件）；evidence（10，+454）。
- 内容性质：CI 或机器门 + 生产代码 + 台账。
- 意图证据：消息要点与 14 文件集合吻合；证据面 download_artifact_tag_query.jsonl/verify_actions_lock_online.jsonl 为在线锁定留痕。
- 关联：V8-CI 系列收口件；宪章 §15.3 Fatduck"不 checkout 源码、不编译、不运行仓库任意脚本"以 fatduck-validate job 设计字面落实；ADOPT 波（1521–1526）→ CI 波（1527–1540）在此闭合。
- 不确定点：Fatduck 节点上 run_validation.ps1 的实际内容不在本提交（harness 入口固定引用），其真跑结果在本片外——未证实。

## 重叠复核条目（seq 1476–1485）

上一片报告 `设计大纲/reports/history/slices/H-S27.md` 经目录列举核实**不存在**（该目录现仅存 H-S01..H-S21 与 H-S24，其最新者 H-S24 覆盖 seq 1266–1320，与本片重叠区无交集；H-S22/23/25/26/27 均缺）。按读法规则与任务指令，以下 10 条一律标**重叠待核**，不写一致性结论：

| seq | sha8 | 日期 | 编号 | 消息首行（摘） | 重叠区结论 |
|---|---|---|---|---|---|
| 1476 | bbe2e59e | 2026-09-02 | LOG-001 | feat(logging): 统一结构化日志接口 | 重叠待核（H-S27.md 不存在） |
| 1477 | 1c3d1dc9 | 2026-09-02 | ARC-001 | docs(architecture): 冻结DLL产品边界 | 重叠待核（同上） |
| 1478 | f260b804 | 2026-09-02 | BLD-001 | build(msvc): 冻结Windows发布工具链 | 重叠待核（同上） |
| 1479 | 2df77afe | 2026-09-02 | DATA-001 | feat(data): 冻结类型化产物合同 | 重叠待核（同上） |
| 1480 | b7b2dea7 | 2026-09-02 | GOV-002（附带 LOG-001） | docs(governance): 归档非当前工程文档（L；885 文件、改名 881、engineering/control 855 文件） | 重叠待核（同上） |
| 1481 | 76b85d45 | 2026-09-02 | ABI-001 | feat(abi): 定义模块C ABI v1 | 重叠待核（同上） |
| 1482 | 39e77317 | 2026-09-02 | GOV-003 | build(version): 统一Alpha版本事实源 | 重叠待核（同上） |
| 1483 | 5d38ebd7 | 2026-09-02 | RT-001 | feat(runtime): 实现类型化运行图 | 重叠待核（同上） |
| 1484 | c1ee791a | 2026-09-02 | DATA-002 | feat(data): 定义三阶段产品交换合同 | 重叠待核（同上） |
| 1485 | 3e7f7581 | 2026-09-03 | BLD-002 | feat(build): 建立唯一根 CMake 构建图（S；3 文件 +34/-3） | 重叠待核（同上） |

可交叉的结构事实（非一致性结论）：1476 的父为 065c81de，恰等于紧凑目录中 seq 1475（GOV-001）的 sha8；1486 的父恰为 1485 的 3e7f7581——重叠区与本片区、与前一区皆为同一条线性链；S27 的 manifest.csv 尾部确含 1483/1484/1485 行，即该 10 条在派发关系上属 S27 主责范围，其报告缺位系流程积压而非提交证据缺失。若 H-S27 补齐，上述 10 条可按"同链单亲"直接复核。
