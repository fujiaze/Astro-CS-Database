# 逐条详析 （证据层过程件）

> 本文件是 设计大纲/reports/history/slices/H-S01.md 的逐条正文；最终交付只保留两份大报告，逐条正文下沉到证据层（条目文本零改动，仅移动）。
> 由 _tools/11_split_slice_reports.py 从入库基线 a3a343a4 重切，日期 2026-09-15；条目组 41 个；报告树保留段（主题归纳/重叠区核对/遗留缺口）见原文件。

## 正文条目（55 条主责，合并为 41 组）

### seq 1 | 4a2750bd | 2026-05-24 | M
- 做了什么：Gaia DR3 XPSD C 客户端仓库的初始提交，`lib/gaia_xpsd_client` 整目录 6 文件新增 +974 行：.gitignore、Makefile、README.md、demo.c、gaia_client.c、gaia_client.h（seq 1 | 4a2750bd）。
- 落点：`lib/gaia_xpsd_client/`（新建目录）。
- 内容性质：生产代码（含 README、Makefile 的初始导入）。
- 意图证据：消息自述"Initial commit: Gaia DR3 XPSD Client"；变更面证据为 6 文件全新增、0 修改 0 删除，落在单一新模块目录，与自述一致（seq 1 | 4a2750bd）。
- 关联：`lib/gaia_xpsd_client` 谱系根；本片该链后续 seq 4,5,10,11,16,19,21,22,27,34,53 全部在此之上演进。
- 不确定点：XPSD 格式为第三方（Siril XPSD）还是自定义，卡与消息均未说明，未证实。

### seq 2 | 802becc2 | 2026-05-24 | L（初始导入）
- 做了什么：Astro Image IO 纯 Python 库初始导入：`lib/astro_image_io` 18 文件全新增 +2148，含 fits_reader.py、image_data.py、image_metadata.py、keywords.py、observation_metadata.py、_xisf_py.py、_xisf_pcl.py、_types.py、errors.py、__init__.py 及 README/.gitignore（seq 2 | 802becc2）。
- 落点：`lib/astro_image_io/`（新建目录，L 级按文件集合归纳：一个 FITS/XISF 读写 Python 包骨架）。
- 内容性质：生产代码（初始导入）。
- 意图证据：消息自述"Initial commit: Astro Image IO"；变更面证据：18 文件全新增、0 修改，与自述一致（seq 2 | 802becc2）。
- 关联：`lib/astro_image_io` 谱系根；seq 20（48396682）在同目录引入 C++ 原生实现，seq 38 起该目录成为管线载体。
- 不确定点：无正文说明是否派生自外部项目（如 _xisf_pcl.py 的来源），未证实。

### seq 3 | 850973a7 | 2026-05-24 | L（初始导入）
- 做了什么：Star Detector 恒星检测模块初始导入：`lib/star_detector` 12 文件全新增 +2126，C++ 源（sdet_api.cpp、sdet_detector.cpp/h、sdet_image.cpp/h、sdet_log.cpp/h）、C 头 star_detector.h、Python 绑定 star_detector.py、Makefile、README、.gitignore（seq 3 | 850973a7）。
- 落点：`lib/star_detector/`（新建目录）。
- 内容性质：生产代码（初始导入）。
- 意图证据：消息自述"astronomical star detector with PSF fitting filter, FWHM clipping and roundness filter"；变更面为全新增单模块目录，与自述一致（seq 3 | 850973a7）。
- 关联：`lib/star_detector` 谱系根；该链后续 seq 6,7,8,9,12,13,14,15,17 在本片内完成第一轮功能演进。
- 不确定点：无。

### seq 4-5 | cebab976, 1ddbe246 | 2026-05-24 | S,S（同模块连续组）
- 做了什么：gaia_xpsd_client 的 README 两次微修：添加百度网盘下载链接（seq 4 | cebab976，+5/-1）；删除多余文字（seq 5 | 1ddbe246，+1/-1）。
- 落点：`lib/gaia_xpsd_client/README.md`。
- 内容性质：文档或规格。
- 意图证据：消息自述与变更面一致——两笔均只改 README.md 一个文件（seq 4 | cebab976，属 seq 1 | 4a2750bd 谱系链，变更面证据；前台订正原指针笔误）。
- 关联：紧接 seq 1 初始 README；1ddbe246 同时是 seq 11 merge（1153cd95）的父提交之一。
- 不确定点：网盘链接指向的数据集内容未入库，未证实。

### seq 6 | 2ce5598e | 2026-05-26 | M
- 做了什么：star_detector 功能升级：新增 SExtractor 风格背景估计（新文件 sdet_background.cpp/h）并修复 Moffat4 坐标计算；11 文件 +846/-24，含新增 test_detection.py，修改 Makefile、README、star_detector.h/py、sdet_api/detector/image（seq 6 | 2ce5598e）。
- 落点：`lib/star_detector/`（背景估计新源文件 + API/检测器/测试聚类）。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述"基于SExtractor背景估计 + 修复Moffat4坐标计算"（正文 222 字符）；变更面证据：sdet_background.* 为新增、test_detection.py 为新增，与自述对应（seq 6 | 2ce5598e）。
- 关联：seq 3（850973a7）初始版后的第一个功能提交；其后 seq 7-9 为同模块文档注释收尾。
- 不确定点：无。

### seq 7-9 | b33f88da, 25d71815, 23f3ee5f | 2026-05-26 | S,S,S（同模块同日组）
- 做了什么：star_detector 同日三连收尾：README 添加检测效果示例图像（seq 7 | b33f88da，+7/-1，README 单文件）；为 sdet_api.cpp 与 sdet_background.cpp 添加注释说明核心算法原理（seq 8 | 25d71815，+106/-0，纯增）；README 一行微修（seq 9 | 23f3ee5f，+1/-1，消息仅"Update README.md"）。
- 落点：`lib/star_detector/README.md`、`sdet_api.cpp`、`sdet_background.cpp`。
- 内容性质：文档或规格（seq 8 为源码内注释性文档，计入代码面）。
- 意图证据：消息自述明确（7、8）；变更面证据：seq 8 两个源文件 +106/-0 与"只加注释"自述吻合；seq 9 消息零信息，仅能从变更面知道改了一行 README（seq 9 | 23f3ee5f）。
- 关联：直接收尾 seq 6（2ce5598e）的功能提交。
- 不确定点：seq 9 具体改动内容卡片不足，未证实。

### seq 10 | 08a8766a | 2026-05-27 | M
- 做了什么：gaia_xpsd_client 增加多数据库支持（GaiaDR3 与 GaiaDR3SP 双库）：6 文件 +425/-12，新增 test_multi_db.py、verify_dr3.py 两个验证脚本，修改 gaia_client.c/h、Makefile、README（seq 10 | 08a8766a）。
- 落点：`lib/gaia_xpsd_client/`。
- 内容性质：生产代码 + 测试/验证脚本。
- 意图证据：消息自述"Add multi-database support (GaiaDR3 and GaiaDR3SP)"（正文 389 字符）；变更面证据：双库验证脚本新增，与自述一致（seq 10 | 08a8766a）。
- 关联：seq 1 链上功能提交；seq 11（1153cd95）merge 的父之一即本提交。
- 不确定点：无。

### seq 11 | 1153cd95 | 2026-05-27 | S（merge）
- 做了什么：合并提交"Merge branch 'master' of github.com:fujiaze/Gaia-DR3SP-C-Client"，父为 08a8766a（seq 10）与 1ddbe246（seq 5）；卡面显示变更文件 0、+0/-0，并明确标注"本提交改动全部落在排除区（影子树/构建产物/数据）"；消息点名文件 github.c 与变更面交集为空（seq 11 | 1153cd95）。
- 落点：排除区（代码面无落点）。
- 内容性质：运行产物/影子树副本，不构成工作内容证据。
- 意图证据：仅消息自述（远端同步合并）；变更面证据缺失（全在排除区）。
- 关联：把远端线与 seq 5 本地线并轨的形式性 merge。
- 不确定点：合并是否实际带入过代码——卡面无代码面证据，未证实。

### seq 12 | 8cbeef6d | 2026-05-27 | M
- 做了什么：star_detector 新增过饱和星（saturated star）检测与调试图像输出：5 文件 +644/-7，新增 test_saturated.py，修改 README、star_detector.h/py、sdet_api.cpp（seq 12 | 8cbeef6d）。
- 落点：`lib/star_detector/`。
- 内容性质：生产代码 + 测试。
- 意图证据：消息自述（正文 578 字符）；变更面证据：API 层 sdet_api.cpp 修改 + 饱和星专项测试新增，与自述一致（seq 12 | 8cbeef6d）。
- 关联：饱和星主题的前置版本，seq 14（1013feb6）以 half-threshold 方案大幅重做同一主题。
- 不确定点：无。

### seq 13 | 7eb45fce | 2026-05-27 | S
- 做了什么：把 README 的 example2 示例图压缩为 JPEG 以便更快的 git push：1 文件 +3/-2，变更面仅 README.md（seq 13 | 7eb45fce）。
- 落点：`lib/star_detector/README.md`（及其引用图像资产）。
- 内容性质：数据或产物（资产瘦身）+ 文档。
- 意图证据：消息自述"compress example2 image to JPEG for faster push"；变更面证据：仅 README 行级变化，与替换图片引用一致（seq 13 | 7eb45fce）。
- 关联：seq 7（b33f88da）添加示例图像后的资产优化。
- 不确定点：图像文件本身的变更未出现在卡文件清单（二进制计数为 0），未证实。

### seq 14 | 1013feb6 | 2026-06-03 | L
- 做了什么：star_detector 检测管线重写级升级：半阈值（half-threshold）饱和星检测 + 候选预过滤 + OpenMP 并行化；9 文件 +1591/-974，全部为修改（0 新增），覆盖 Makefile、README、star_detector.h/py、sdet_api.cpp、sdet_detector.cpp/h、sdet_image.cpp/h（seq 14 | 1013feb6）。L 级按目录直方图归纳：改动集中于 lib/star_detector 单一目录的既有文件，删除量达新增量的 61%，属既有核心源文件（检测器/图像层）成重写。
- 落点：`lib/star_detector/`（9 文件聚类）。
- 内容性质：生产代码。
- 意图证据：消息自述（正文 530 字符，三项特性）；变更面证据：sdet_detector/image 核心重写与"预过滤+并行化"自述相符（seq 14 | 1013feb6）。
- 关联：延续并取代 seq 12（8cbeef6d）的饱和星方案；后续 seq 15、17 在本模块继续。
- 不确定点：分级 L 的具体判据（行数）卡未说明，未证实。

### seq 15 | 787ab234 | 2026-06-04 | M
- 做了什么：star_detector 自适应 fitRadius：按连通域大小自动估算 FWHM；7 文件 +1031/-20，新增 4 文件——memory.md（模块记忆首现）、analyze_fit_performance.py、test_auto_fitradius.py、test_dynamic_background.py，修改 README、star_detector.py、sdet_api.cpp（seq 15 | 787ab234）。
- 落点：`lib/star_detector/`。
- 内容性质：生产代码 + 测试 + 性能分析脚本；memory.md 具台账属性。
- 意图证据：消息自述（正文 155 字符）；变更面证据：两个针对性新测试与一个性能分析脚本，与"自适应+估算"自述一致（seq 15 | 787ab234）。
- 关联：紧接 seq 14 的并行化重写；该模块 memory.md 在 seq 17 被删除（见下）。
- 不确定点：test_dynamic_background.py 与 seq 6 背景估计的关系未在卡中说明，未证实。

### seq 16 | 22e727ff | 2026-06-06 | M
- 做了什么：gaia_xpsd_client 增加两级缓存：L1 查询缓存（60s TTL）与 L2 解压块缓存；2 文件 +547/-30（gaia_client.c、README）（seq 16 | 22e727ff）。
- 落点：`lib/gaia_xpsd_client/gaia_client.c`。
- 内容性质：生产代码（性能路径）。
- 意图证据：消息自述含具体参数（"60s TTL"、"L1/L2"）；变更面证据：全部落在客户端单文件，与缓存实现一致（seq 16 | 22e727ff）。
- 关联：seq 1 链上、seq 10 多库支持之后的查询侧深化。
- 不确定点：无。

### seq 17 | 8c0762ca | 2026-07-07 | L
- 做了什么：star_detector V5.0 代际切换：Gaussian PSF 替代 Moffat4；GSL trust-region LM 替代手写 LM（新增 include gsl/gsl_multifit_nlinear.h 等，新增 sdet_gaussian_residual/df、sdet_lm_fit、reject_star 等函数，删除 sdet_moffat4_residual 家族）；halfA 边界搜索提供稳健拟合初值；候选 mag_est 降序排序与 reject_star 自适应 FWHM 上限；移除源代码中第三方项目引用元素；删除死代码（旧手写 LM）；Makefile 新增 lto/pch 目标；新增 CHANGELOG.md 与 test_ngc6302_v417.py；删除 memory.md；11 文件 +1739/-639（seq 17 | 8c0762ca）。L 级归纳：改动全部集中于 lib/star_detector 既有文件集。
- 落点：`lib/star_detector/`（api/image/h + Makefile + CHANGELOG/README/memory）。
- 内容性质：生产代码 + 测试 + 文档（CHANGELOG）+ 结构整理（memory.md 删除）。
- 意图证据：消息自述含量化效果（"3帧抽样全部达标: 60/59/59""790帧全帧对比: 前60中位 98.3%, 前100中位 98.0%"）；变更面证据：全卡符号抽取的新增/删除函数与 Moffat4→Gaussian、手写 LM→GSL 的切换一一对应（seq 17 | 8c0762ca）。
- 关联：seq 3 链本模块在本片的收束点；同目录 memory.md（seq 15 新增）在此被删。注意 dynamic_psf 仍保留 Moffat4（seq 28 修其 flux 计算），两模块算法代际自此分叉。
- 不确定点："与 Siril 1.4.3 效果对比"为 CHANGELOG 标题所载，对比方法细节卡内截断，未证实。

### seq 18 | 12c55ed6 | 2026-05-24 | M
- 做了什么：Dynamic PSF 初始导入：`lib/dynamic_psf` 11 文件全新增 +1556——Moffat4 PSF 拟合引擎 C++ 源（dpsf_psf.cpp/h、dpsf_image.cpp/h、dpsf_log.cpp/h）、dynamic_psf.h、Python 绑定 dynamic_psf.py、Makefile、README、.gitignore；支持 16bit 输入与 batch 模式（seq 18 | 12c55ed6）。
- 落点：`lib/dynamic_psf/`（新建目录）。
- 内容性质：生产代码（初始导入）。
- 意图证据：消息自述"Moffat4 PSF fitting engine with 16bit input and batch mode"；变更面为全新增单目录，一致（seq 18 | 12c55ed6）。
- 关联：独立谱系根；该链后续 seq 28,33,47,50。日期（05-24）早于其 seq 前驱 seq 17（07-07）——再证 seq 为谱系分组序而非时间序。
- 不确定点：无。

### seq 19 | 941b82a2 | 2026-06-07 | M
- 做了什么：修复 search_recursive 在 AzimuthalEquidistant 投影下 bbox 计算未包含投影中心点的问题；3 文件 +305/-176，新增 verify_global_coverage.py 验证脚本，修改 gaia_client.c、README（seq 19 | 941b82a2）。
- 落点：`lib/gaia_xpsd_client/`。
- 内容性质：生产代码（缺陷修复）+ 验证脚本。
- 意图证据：消息自述点名函数 search_recursive 与投影类型（正文 654 字符）；变更面证据：新增"全球覆盖验证"脚本与修复漏检的动机互相印证（seq 19 | 941b82a2）。
- 关联：seq 1 链查询侧修正，在 seq 16 缓存之后。
- 不确定点：无。

### seq 20 | 48396682 | 2026-05-24 | L（重写导入）
- 做了什么：astro_image_io 的 C++ 原生实现：FITS/XISF 读写 + Python ctypes 绑定；12 文件 +2313/-126，新增 10 文件——aio_api.cpp、aio_fits.cpp/h、aio_xisf.cpp/h、aio_log.cpp/h、astro_image_io.h、astro_image_io.py（绑定层）、Makefile、.gitignore，修改 README 等 2 文件（seq 20 | 48396682）。L 级归纳：为 lib/astro_image_io 引入 src/include C/C++ 子结构，Python 与 C++ 双实现自此并存。
- 落点：`lib/astro_image_io/`（C++ 子树新建）。
- 内容性质：生产代码（原生重写/绑定引入）。
- 意图证据：消息自述"C++ native implementation - FITS/XISF read/write with Python ctypes binding"；变更面证据：aio_* C++ 文件簇全新增，-126 落在既有 Python/README，一致（seq 20 | 48396682）。
- 关联：seq 2（802becc2）谱系根链上；该目录此后由 seq 23,26,32,38,40,41,43,48,51 延续。
- 不确定点：seq 2（Python 导入）与 seq 20（C++ 实现）同为 2026-05-24，同日内的先后与 Python 版去留比例卡面无法判断，未证实。

### seq 21-22 | 7691d768, 51adc4d7 | 2026-07-10 | M,M（同链连续组）
- 做了什么：gaia_xpsd_client 光谱接口两步走。seq 21：暴露 DR3SP 光谱接口（BP/RP 光谱 + magBP/magRP），4 文件 +925/-3，新增 verify_spectrum.py，修改 gaia_client.c/h、README；seq 22：按关注点拆分光谱与测光 API，4 文件 +397/-79，全部为修改（同一文件集 README、verify_spectrum.py、gaia_client.c/h）（seq 21 | 7691d768；seq 22 | 51adc4d7）。
- 落点：`lib/gaia_xpsd_client/`。
- 内容性质：生产代码 + 验证脚本。
- 意图证据：两笔消息自述均具体（正文 555/493 字符）；变更面证据：seq 21 大增小删（新接口）、seq 22 有删有增（重构拆分），与"先加后拆"顺序吻合（seq 21-22）。
- 关联：21 → 22 连续父子；seq 27（c4b5333b）在此光谱 API 上继续批量化。
- 不确定点：无。

### seq 23 | b58ff404 | 2026-07-10 | S
- 做了什么：修复 FITS 关键字写出：write_card 引号逻辑 + BZERO/BSCALE 关键字过滤；2 文件 +38/-12（aio_fits.cpp、README）（seq 23 | b58ff404）。
- 落点：`lib/astro_image_io/`（C++ FITS 写路径）。
- 内容性质：生产代码（缺陷修复）。
- 意图证据：消息自述点名函数 write_card 与关键字 BZERO/BSCALE（正文 280 字符）；变更面证据一致（seq 23 | b58ff404）。
- 关联：seq 20 C++ 实现链的修正；是 seq 26（0107e3d7）的父提交。此修复主题与 seq 43 的 FITS 导出修复（SIMPLE/EXTEND 逻辑型不加引号）同族。
- 不确定点：无。

### seq 24-25 | a88d9789, ce62842f | 2026-07-10 | M,M（同谱系连续组）
- 做了什么：Astro Calibration C++ 模块（`lib/calibration`）第一谱系。seq 24（独立谱系根）：8 文件 +1481 全新增——include/astro_calibration.h、python/astro_calibration.py、src/ac_api.cpp、src/calibrator.cpp、src/cosmetic_corrector.cpp、src/master_generator.cpp、build.ps1、.gitignore；seq 25（父 24）：4 文件 +565/-0，新增 README.md、batch_config.json、batch_process.py，修改 astro_calibration.py——批处理入口 + 文档 + UTF-8 路径 + FITS 写优化（seq 24 | a88d9789；seq 25 | ce62842f）。
- 落点：`lib/calibration/`（include/src/python 三层结构）。
- 内容性质：生产代码 + 配置数据（batch_config.json）。
- 意图证据：seq 24 消息自述"Astro Calibration C++ module"（正文 359 字符）与全新增 C++ 文件簇一致；seq 25 自述四项、变更面主体是批量脚本与 README，"UTF-8/FITS 优化"两项在本目录卡面未见对应源文件（见不确定点）（seq 25 | ce62842f）。
- 关联：24 → 25 父子；seq 30（4c7f5df9）merge 把本谱系并入 seq 29 谱系。
- 不确定点：seq 25 消息与 seq 26（0107e3d7，astro_image_io 目录）自述存在"UTF-8 path support + FITS write optimization"重复字样，是否同源动机未证实。

### seq 26 | 0107e3d7 | 2026-07-10 | S
- 做了什么：astro_image_io 的 UTF-8 路径支持 + FITS 写优化：4 文件 +70/-13，新增 aio_util.h，修改 aio_fits.cpp、aio_xisf.cpp、README（seq 26 | 0107e3d7）。
- 落点：`lib/astro_image_io/`。
- 内容性质：生产代码。
- 意图证据：消息自述（正文 452 字符）；变更面证据：新增工具头 + 读写两侧文件，与自述对应（seq 26 | 0107e3d7）。
- 关联：父 b58ff404（seq 23）；子 9d0da974（seq 32）。与 seq 25 的自述重叠见上条。
- 不确定点：无。

### seq 27 | c4b5333b | 2026-07-11 | M
- 做了什么：gaia_xpsd_client 新增批量坐标光谱查询 API（OpenMP 并行）：4 文件 +397/-0，新增 vector_match_v2.py，修改 gaia_client.c/h、.gitignore（seq 27 | c4b5333b）。
- 落点：`lib/gaia_xpsd_client/`。
- 内容性质：生产代码 + 辅助脚本。
- 意图证据：消息自述点名 OpenMP parallel（正文 310 字符）；变更面证据：C 层 API 纯新增，与"新增批量 API"一致（seq 27 | c4b5333b）。
- 关联：seq 22（51adc4d7）的直接子提交，光谱 API 拆分后的批量化；其并行 pragma 中的 num_threads(16) 硬编码在 seq 34 被删除。
- 不确定点：vector_match_v2.py 命名暗示存在 v1，v1 不在本片任何卡面，未证实。

### seq 28 | 0349b723 | 2026-07-11 | S
- 做了什么：dynamic_psf 的 Moffat4 PSF flux 计算由窗口累加改为解析积分：单文件 dpsf_psf.cpp，+2/-4（seq 28 | 0349b723）。
- 落点：`lib/dynamic_psf/src/dpsf_psf.cpp`。
- 内容性质：生产代码（算法口径修正）。
- 意图证据：消息自述明确；变更面证据：单文件小 diff 与"以解析式替代累加"一致（seq 28 | 0349b723）。
- 关联：seq 18（12c55ed6）谱系（父即 18）；对照 seq 17——star_detector 已弃 Moffat4，dynamic_psf 仍在修 Moffat4 精度。
- 不确定点：无。

### seq 29-30 | 3b39d126, 4c7f5df9 | 2026-07-11 | L,L（同消息连续组 + merge）
- 做了什么：`lib/calibration` 第二谱系导入与双线并轨。seq 29（独立谱系根）：18 文件 +4314 全新增，Python+C++ 混合骨架——cosmetic_corrector.cpp/h、Makefile、CALIBRATION_PROCESS.md、memory.md、calibrate_fits.py、calibration_pipeline.py、calibrator.py、cosmetic_corrector.py、diagnose_cc.py、__init__.py 等；seq 30（merge，父 3b39d126 与 ce62842f）：把 seq 24-25 的"C++ module"线（README.md、batch_config.json、batch_process.py、build.ps1、include/astro_calibration.h、python/astro_calibration.py、src/ac_api.cpp、src/calibrator.cpp、src/cosmetic_corrector.cpp、src/master_generator.cpp 共 10 个新增落位）并入本线，11 文件 +2043/-8，非代码面的实质变化为 .gitignore 英文注释化并登记 *.o/*.obj/*.dll/*.exp/*.lib 构建产物排除（seq 29 | 3b39d126；seq 30 | 4c7f5df9）。L 级归纳：两笔均为单目录（lib/calibration）整体导入/并集，不逐文件展开。
- 落点：`lib/calibration/`。
- 内容性质：生产代码（初始导入 + merge 并集）+ 文档（CALIBRATION_PROCESS.md、README）+ 台账（memory.md）。
- 意图证据：seq 29 消息自述"calibration module with C++ OpenMP cosmetic corrector (C++)"与新增 cosmetic_corrector.cpp/h 对应；seq 30 与 29 同消息（merge 特性），变更面证据即 seq 24/25 文件集落入 lib/calibration，构成双谱系并集的直接证据（seq 30 | 4c7f5df9，父字段 3b39d126,ce62842f）。
- 关联：24,25 线 + 29 线 → 30 并轨；此链后续 seq 44（ddf17ca3，父 4c7f5df9）、seq 52。
- 不确定点：两条线各有一个 cosmetic_corrector.cpp（29 的顶层 vs 24 的 src/），merge 后同名文件如何并存在卡面未完整呈现，未证实。

### seq 31 | 0c08aeb5 | 2026-07-11 | L（初始导入）
- 做了什么：HEALpix Database"球面存储与可视化系统"整仓初始导入：`lib/healpix_db` 51 文件 +14892 全新增，内含四个子模块加文档：ahpx_io/（.ahpx 单帧格式读写：ahpx_api、ahpx_reader/writer、compressor（zstd/lz4）、ahpx_io.py Python 绑定、Makefile、tests/）、healpix_stack/（稀疏 HEALpix 堆栈：ahps_format、ahps_reader/writer、healpix_core、stack_db、stack_engine、hp_stack_api 等）、healpix_lod/（LOD 金字塔：hp_lod_api、lod_downsample、lod_manager）、healpix_browser/（球面可视化 Python GUI：sphere_renderer、sphere_view、stf、export_dialog、main_window、requirements.txt）、docs/healpix_drizzle_overview.md（seq 31 | 0c08aeb5）。L 级归纳：一个四子模块 + 文档的单目录多模块布局；卡标注"diff 读取截断，另有 11 项未列"。
- 落点：`lib/healpix_db/`（新建目录，子目录聚类如上）。
- 内容性质：生产代码 + 文档 + 测试（端到端集成测试）（初始导入）。
- 意图证据：消息自述（正文 246 字符）逐条列举 ahpx_io/healpix_stack/healpix_lod/healpix_browser/docs/tests 五块；变更面证据：新增子目录名与自述逐项对应（seq 31 | 0c08aeb5）。
- 关联：独立谱系根；seq 39 将其中 28 个文件拆出；seq 55 补文档。作者邮箱为 fujiaze@outlook.com（全片多数提交为 users.noreply），系客观差异，原因未记录。
- 不确定点：完整 51 文件清单需查 index.jsonl，卡面截断；对归纳无实质影响。

### seq 32-34 | 9d0da974, 07795923, e53c8865 | 2026-07-12 | S,S,S（同批次跨模块组）
- 做了什么：三笔同批小修（卡头时间戳 10:01:25/10:01:28/10:01:30，5 秒内三连，各属不同模块）：seq 32——astro_image_io 的 _load_dll 对 PATH 追加去重，注释点名"避免重复追加PATH导致环境变量超长（Windows 32767字符限制）"，1 文件 +4/-1；seq 33——dynamic_psf 的 Makefile 移除 -fopenmp 与 -static-libgcc/-static-libstdc++，dpsf_psf.cpp 的 `#pragma omp parallel for` 移除 num_threads(16) 硬编码保留 schedule(dynamic)，2 文件 +3/-3；seq 34——gaia_client.c 四处 `#pragma omp parallel for schedule(dynamic)` 移除 num_threads(16)，1 文件 +4/-4（seq 32 | 9d0da974；seq 33 | 07795923；seq 34 | e53c8865）。
- 落点：`lib/astro_image_io/python/astro_image_io.py`、`lib/dynamic_psf/{Makefile,src/dpsf_psf.cpp}`、`lib/gaia_xpsd_client/src/gaia_client.c`。
- 内容性质：生产代码（微修）。
- 意图证据：消息自述仅"update <文件名>"，零信息；变更面证据（卡内逐行摘录）显示 33/34 同为"删除硬编码线程数"、32 为环境修复——三笔共同构成跨模块的并行参数与 DLL 加载修正批次，但无作者自述支撑动机。
- 关联：33 移除 -fopenmp 使 OpenMP 失效，seq 47（312dd8ad）重新加回；34 承接 seq 27 批量 API 的 pragma。三者分别位于各自模块主链中段。
- 不确定点：33 同时移除 -fopenmp 与静态链接是有意还是误删，消息与卡均未解释，未证实（seq 47 自述"add -fopenmp to enable OpenMP 16-thread parallel fitting"证实当时并行确已失效）。

### seq 35 | f75ae3f0 | 2026-07-12 | L | 结构模式（巨型导入）
- 做了什么：photometric_calib"鲁棒流量校准 Python 开发原型 v1.0"整仓导入：34 文件 +64269 全新增，块级结构：spectrum_integrator/（integrator.py、gaia_spectrum_client.py、curve_loader.py、run_integrator.py、synthetic_photometry.py、f_syn_galactic_center.json、memory.md）、gradient_estimator/（estimator.py、gradient_fitter.py、star_matcher.py、image_corrector.py、wcs_transform.py、run_estimator.py、analyze_gradient.py、fsyn_loader.py、test_synthetic.py、IMPLEMENTATION.md、memory.md）、python/（sed_builder.py、pc_logger.py、synthetic_photometry.py）、shared/python/（data_types.py、pc_logger.py）、data/response_curves/（filters.json、qe_curves.json）、docs/（algorithm.md、architecture.md）+ 根 README/memory/.gitignore（seq 35 | f75ae3f0）。
- 落点：`lib/photometric_calib/`（新建目录；结构模式，按块归纳不逐文件展开）。
- 内容性质：生产代码 + 文档或规格（算法/架构文档）+ 数据或产物（响应曲线 JSON、F_syn 样例 JSON）+ 台账（多处 memory.md）。
- 意图证据：消息自述详实（光度定标全链路、双程序解耦架构、JSON 文件通信、合成数据端到端 3/3、公式 F_syn=integral(S*T*Q*lambda*d_lambda)、r=log10(F_instr/F_syn)、I_cal=(I-S)/M）；变更面证据：全新增与"原型整档导入"自述一致（seq 35 | f75ae3f0）。
- 关联：独立谱系根；该链后续 seq 36,37,42,46,49。
- 不确定点：+64269 行中数据 JSON（filters/qe_curves/f_syn）占比多大，卡未列每文件行数（结构模式不抽符号），未证实。

### seq 36 | 787679bb | 2026-07-12 | M
- 做了什么：photometric_calib 内部重命名与策略封存：目录 gradient_estimator/ → flux_calibrator/（11 个文件改名）；删除 analyze_gradient.py（消息自述归档至 archive/old_gradient_tools/）；封存天光校正（v1.1 决策）——S_map 加性梯度拟合以注释禁用，校正公式由 I_cal=(I-S)/M 改为 I_cal = I / M_map，理由（自述与 docs/algorithm.md 新增封存说明框一致）为"S_map 经验多项式无法区分缓变天光与缓变星云信号，银河区域会误减目标信号"，天光一致性留给后续马赛克背景匹配模块；docs/algorithm.md、memory.md 记录决策；14 文件 +37/-260（seq 36 | 787679bb）。
- 落点：`lib/photometric_calib/`（改名面 flux_calibrator/*，修订面 docs、memory、wcs_transform.py +322/-213）。
- 内容性质：结构整理（目录改名）+ 生产代码（校正公式与禁用）+ 文档或规格（封存说明）+ 控制包或台账（memory.md 决策记录）。
- 意图证据：消息自述（305 字符正文）与变更面证据高度一致：改名 11、删除 1、algorithm.md 新增 [已封存 2026-07-12] 说明块，符号抽取显示 fit_additive/GradientSurface 等以注释方式保留（seq 36 | 787679bb）。
- 关联：seq 35 的直接子提交；37 接续 README 定名；42（bc894de2）进一步删除梯度拟合主文件。
- 不确定点：无。

### seq 37 | ebc8197a | 2026-07-12 | S
- 做了什么：README 更名为 Flux Calibration 口径并添加 45 帧性能数据：单文件 +80/-20（seq 37 | ebc8197a）。
- 落点：`lib/photometric_calib/README.md`。
- 内容性质：文档或规格。
- 意图证据：消息自述（正文 230 字符）；变更面单文件一致（seq 37 | ebc8197a）。
- 关联：seq 36 改名（flux_calibrator）后的文档对齐；是 seq 42 的父提交。
- 不确定点：无。

### seq 38 | 51b8c7b6 | 2026-07-12 | L
- 做了什么：把 HEALpix 侧的 ahpx 能力迁入 astro_image_io 并立起管线框架首版：13 文件 +3047/-1，新增 10 文件——include/aio_ahpx_format.h、include/aio_pipeline.h、src/ahpx/（aio_ahpx_api.cpp、aio_ahpx_reader/writer.cpp/h）、src/aio_compressor.cpp/h、src/aio_pipeline.cpp，修改 Makefile、astro_image_io.h/py。自述五项迁移内容：compressor 入 aio:: 命名空间（aio_compress/aio_decompress/aio_compress_bound C API）、ahpx format/writer/reader 入 aio::ahpx::（aio_ahpx_write/read_header/read_pixels/read_snr）、PipelineFrame 数据结构（frame_create/destroy/alloc_pixels/snr/weight/healpix、free、memory_usage）、XML 调试导出 aio_pipeline_export_xml（base64，无外部依赖）、ahpx_io.py 并入 astro_image_io.py（seq 38 | 51b8c7b6）。
- 落点：`lib/astro_image_io/`（ahpx/ 子目录与 pipeline 文件簇）。
- 内容性质：生产代码（跨模块迁移落地 + 新框架）。
- 意图证据：消息自述（495 字符）与全卡符号抽取逐项对应；变更面证据：aio_ahpx_* / aio_pipeline_* 新增符号命名模式（aio_ 前缀 + 31 导入件同名主体）与"从 ahpx_io 迁移"的自述吻合（seq 38 | 51b8c7b6）。
- 关联：与 seq 39（healpix_db 拆出，17:52:41）构成配对搬家——本笔 17:47:00 先入；父 9d0da974（seq 32）。
- 不确定点：机械改名对应（rename detection）未在卡中给出，迁移对应关系靠消息自述加符号对照，未证实。

### seq 39 | 55d69dd0 | 2026-07-12 | L
- 做了什么：healpix_db 拆分后的瘦身：33 文件 +115/-7149，删除 28 个文件——ahpx_io/ 全部 12 件（Makefile、ahpx_api.cpp/h、ahpx_format.h、ahpx_io.py、ahpx_reader/writer.cpp/h、compressor.cpp/h、tests/test_ahpx_io.py）与 healpix_stack/ 全部 16 件（ahps_*、healpix_core、stack_db、stack_engine、hp_stack_api、healpix_stack.py、tests）；修改 5 文件（.gitignore、README、Makefile、lod 侧）——自述：ahpx_io 迁往 astro_image_io（Astro-Image-IO-C）、stack 独立成 Healpix-Mosaic、drizzle 独立成 Healpix-Drizzle、healpix_lod 改为依赖 astro_image_io.dll（AIO C API）、.gitignore 排除拆出模块（本地保留供 lod 构建）、README 增加跨仓依赖文档（seq 39 | 55d69dd0）。
- 落点：`lib/healpix_db/`（ahpx_io/、healpix_stack/ 两个子目录被清空删除）。
- 内容性质：结构整理（仓库拆分）+ 生产代码（lod 依赖切换）。
- 意图证据：消息自述（336 字符）列出四处迁出去向；变更面证据：删除文件清单与 seq 31 新增清单逐一对应（-7149 ≈ seq 31 中 ahpx_io+stack 部分的体量），与拆出自述一致（seq 39 | 55d69dd0）。
- 关联：父 0c08aeb5（seq 31）；与 seq 38 构成"一入一出"；保留的 healpix_lod/healpix_browser 在本片后续仅 seq 55 文档触及。
- 不确定点：Healpix-Mosaic / Healpix-Drizzle 独立仓的去向不在本仓历史内，卡面无从证实。作者邮箱 outlook（同 seq 31）。

### seq 40 | e3f0f782 | 2026-07-12 | M
- 做了什么：astro_image_io 新增管线编排引擎：6 文件 +1057/-7，新增 aio_pipeline_engine.h/cpp（stage handler 注册 + OpenMP 并行批量执行），修改 Makefile、README、astro_image_io.h/py（seq 40 | e3f0f782）。
- 落点：`lib/astro_image_io/`（pipeline engine 文件簇）。
- 内容性质：生产代码。
- 意图证据：消息自述（正文 511 字符）"pipeline orchestration engine with OpenMP parallel batch execution"；变更面证据：引擎成对新增文件，一致（seq 40 | e3f0f782）。
- 关联：seq 38（框架首版 PipelineFrame）→ 40（引擎）→ 41（帧模型重构）三段式生长。
- 不确定点：无。

### seq 41 | c0073c9f | 2026-07-12 | L
- 做了什么：PipelineFrame 迁移为带 KV 存储/缓存/导出能力的命名块（named-block）容器模型：6 文件 +2179/-625，修改 aio_pipeline.h/cpp、aio_pipeline_engine.h/cpp、astro_image_io.py，新增测试 test_pipeline_blocks.py（seq 41 | c0073c9f）。L 级归纳：改动集中在 astro_image_io 的 4 个 pipeline 文件 + 绑定 + 测试，重写量大（删 625）。
- 落点：`lib/astro_image_io/`（pipeline 文件簇）。
- 内容性质：生产代码（模型级重构）+ 测试。
- 意图证据：消息自述一行（无正文）；变更面证据：+2179/-625 全部落在 pipeline 相关文件与新增块测试，构成模型重构的直接证据（seq 41 | c0073c9f）。
- 关联：40 → 41 → 43（orchestrator 基于该容器）→ 45（同一模型外迁成 lib/data_pipeline）。
- 不确定点：无正文，重构动因（为 seq 43/45 的编排与外迁铺路）系变更面推断，未证实。

### seq 42 | bc894de2 | 2026-07-12 | L
- 做了什么：photometric_calib 重写为 C++ DLL 并简化算法：17 文件 +2500/-1246——新增 cpp/ 全套 11 件（photometric_calib.h、pc_api.cpp、star_matcher.cpp/h、wcs_transform.cpp/h、image_corrector.cpp/h、Makefile、build.ps1、test/test_photometric_calib.py）、flux_calibrator/python 新增 pipeline_adapter.py 与 test_pipeline_adapter.py、python/ 新增 photometric_calib.py；删除 estimator.py、gradient_fitter.py（梯度拟合路线弃用，卡示"archived"）。算法简化：scale=median(F_syn/F_instr)，I_cal=I*scale；TAN+SIP WCS 投影（逆 SIP 用 Newton 迭代）；MAD 离群清洗（sigma=MAD/0.6745）；OpenMP 并行（自述 16 线程）；自述 4/4 测试通过、697KB DLL 静态链接（seq 42 | bc894de2）。
- 落点：`lib/photometric_calib/`（cpp/ 新建 + flux_calibrator/ 收缩）。
- 内容性质：生产代码（Python→C++ 重写）+ 测试。
- 意图证据：消息自述（390 字符正文）与变更面证据一致：14 新增 2 删除对应"新增 DLL、删除梯度拟合 Python"；每文件行数显示 estimator.py/gradient_fitter.py 整档移除（seq 42 | bc894de2）。
- 关联：seq 35 原型 → 36 封存天光 → 42 简化为纯乘性定标；seq 46 清残余、49 文档定版 v2.0。时间戳 22:30:02，与 seq 43（22:30:27）、seq 44（22:30:49）为同晚三连。
- 不确定点：自述"OpenMP 16-thread"与 seq 33/34 删除 num_threads(16) 硬编码的关系（是否改由 profile/环境决定）卡面未说明，未证实。

### seq 43 | 9fcd406b | 2026-07-12 | M
- 做了什么：astro_image_io"C++ 迁移完成"收尾：修复 aio_frame_export_block_fits——SIMPLE/EXTEND 卡按逻辑型 T 写出（不加引号）、新增 FLOAT32/FLOAT64/INT32/INT64 大端字节序转换；新增 python/orchestrator.py（类 Orchestrator、_HandlerExtractor，基于命名块容器 PipelineFrame 的编排层）与三个测试（test_orchestrator_e2e.py 15 项校验、test_export_fits_fix.py、test_psf_fit_handler.py）；6 文件 +1546/-5；自述 15/15 E2E 通过、无临时 FITS 文件（seq 43 | 9fcd406b）。
- 落点：`lib/astro_image_io/`（aio_pipeline.cpp +17/-4 修复面；orchestrator 与测试纯新增）。
- 内容性质：生产代码（修复 + 编排层）+ 测试。
- 意图证据：消息自述（373 字符）与变更面证据逐项对应（修复在 C++ 导出函数、编排在新增 py 层）（seq 43 | 9fcd406b）。
- 关联：父 c0073c9f（seq 41）；FITS 导出修复与 seq 23 的关键字修复同族；seq 45 把该 pipeline 栈独立成库。
- 不确定点：无。

### seq 44 | ddf17ca3 | 2026-07-12 | M
- 做了什么：calibration 管线数据路径切换到 C++ DLL：5 文件 +448/-16——新增 pipeline_adapter.py、test_calibrator_dll.py、test_pipeline_adapter.py，修改 calibrator.py 与 memory.md（seq 44 | ddf17ca3）。消息首行带 UTF-8 BOM 前缀（卡原文显示为"\ufefffeat: switch calibrate_data to C++ DLL path"），属格式痕迹。
- 落点：`lib/calibration/`。
- 内容性质：生产代码（路径切换）+ 测试 + 台账（memory.md 同步）。
- 意图证据：消息自述（正文 344 字符）与变更面证据一致：适配器与 DLL 专项测试新增即"switch to C++ DLL path"的直接落点（seq 44 | ddf17ca3）。
- 关联：父 4c7f5df9（seq 30 双线并轨 merge）——并轨后立即切换到 24-25 线带来的 C++ 构建面；seq 52 文档化该状态。
- 不确定点：BOM 成因为工具痕迹，未证实。

### seq 45 | e27ff13d | 2026-07-12 | L（新目录出生）
- 做了什么：Astro-Data-Pipeline（消息原文拼写"Pipline"）初始导入：`lib/data_pipeline` 7 文件 +2524 全新增——include/aio_pipeline.h、include/aio_pipeline_engine.h、src/aio_pipeline.cpp、src/aio_pipeline_engine.cpp、docs/api_spec.md、README.md、.gitignore。内容（自述+符号抽取）：PipelineFrame 命名块容器 + KV 存储、PipelineEngine stage handler 注册与批量执行、C API（aio_frame_* / aio_engine_* 家族，含 aio_frame_add_block(_move)/kv_set/get_double/save_cache/load_cache/export_block_fits/export_block_xml 等）、OpenMP 并行批处理、api_spec.md 完整接口规格（seq 45 | e27ff13d）。
- 落点：`lib/data_pipeline/`（新建目录）。
- 内容性质：生产代码（拆分/初始导入）+ 文档或规格（api_spec.md）。
- 意图证据：消息自述（244 字符）；变更面证据：文件名与 astro_image_io 侧 seq 38/40/41 的 pipeline 文件同名同源（aio_pipeline/aio_pipeline_engine），系同一套模型从宿主库中独立成库（seq 45 | e27ff13d）。
- 关联：seq 41/43 建成的模型在当晚（22:49）外迁为独立谱系根；seq 54 补文档。依赖 include ../astro_image_io.h（全卡 include 抽取可见）。
- 不确定点：搬运为文件复制还是 git rename，卡未列改名对应，未证实。

### seq 46 | 2767c66b | 2026-07-12 | M
- 做了什么：photometric_calib 残余 Python 实现归档清除：4 文件纯删除 -1470——image_corrector.py、run_estimator.py、star_matcher.py、wcs_transform.py（flux_calibrator/python 侧被 C++ 替代的最后一批），0 新增（seq 46 | 2767c66b）。
- 落点：`lib/photometric_calib/flux_calibrator/python/`。
- 内容性质：结构整理（迁移后残留清理）。
- 意图证据：消息自述（正文 559 字符）"archive residual Python photometric files"；变更面证据：+0/-1470 纯删除，与"清理残余"一致（seq 46 | 2767c66b）。
- 关联：父 bc894de2（seq 42）；时间 22:51:35 与 seq 47 同秒——两模块各一笔的同批收尾。
- 不确定点：无。

### seq 47 | 312dd8ad | 2026-07-12 | S
- 做了什么：dynamic_psf 性能回归修复（自述 9.26s → 0.26s，-97.1%）：dpsf_log.cpp 默认日志级 LOG_INFO → LOG_WARN、移除 stderr+file 双 fflush、仅 WARN 级以上写文件；Makefile 加回 -fopenmp。自述根因：日志级逻辑默认发 DEBUG + 364MB 日志文件；效果：15/15 E2E 通过、管线总耗时 27.90s → 20.90s（-25.1%）；2 文件 +12/-8（seq 47 | 312dd8ad）。
- 落点：`lib/dynamic_psf/`（Makefile、src/dpsf_log.cpp）。
- 内容性质：生产代码（性能修复）。
- 意图证据：消息自述（363 字符）与卡内逐行摘录完全对应（日志级、fflush、-fopenmp 三处）；变更面证据并证 seq 33 移除 -fopenmp 曾致并行失效（seq 47 | 312dd8ad）。
- 关联：父 07795923（seq 33），修复其留下的编译开关缺口；与 seq 46 同秒批。
- 不确定点：无。

### seq 48 | 9d6b200a | 2026-07-12 | S
- 做了什么：.ahpx 头 JSON 读取改为动态缓冲：3 文件 +40/-13——aio_ahpx_reader.cpp、aio_ahpx_api.cpp、astro_image_io.py（seq 48 | 9d6b200a）。
- 落点：`lib/astro_image_io/`（ahpx 读取面）。
- 内容性质：生产代码（缺陷修复）。
- 意图证据：消息自述（正文 350 字符）"dynamic buffer for .ahpx header JSON reading"；变更面证据与自述一致（seq 48 | 9d6b200a）。
- 关联：父 9fcd406b（seq 43）；是对 seq 38 迁入的 ahpx 读取路径的修复，22:52 当晚三连（46/47/48）之一。
- 不确定点：无。

### seq 49-55 | 9de3fa88, 9e366aed, 675d351d, 5c2a5e7f, 39498757, 65a52de3, 75c6ab30 | 2026-07-13 | M,S,S,S,S,S,S（文档定稿批组）
- 做了什么：连续 7 笔模块文档定稿，逐模块刷新 README.md + memory.md，全部只触这两类文件：seq 49 photometric_calib——"update memory.md and README to v2.0 C++ DLL version"（+221/-145，README 与 memory 双修改）；seq 50 dynamic_psf——新增模块 memory.md + README 记录性能修复（+39/-2）；seq 51 astro_image_io——新增 memory.md + README 补 GitHub 仓库信息（+39/-0）；seq 52 calibration——README/memory 更新 C++ DLL 切换状态（+49/-4）；seq 53 gaia_xpsd_client——新增 memory.md + README 补 GitHub 仓库信息（+35/-0）；seq 54 data_pipeline——新增 memory.md + README 使用示例（+123/-25）；seq 55 healpix_db——新增 memory.md + README 补 GitHub 仓库信息（+41/-0）（seq 49 | 9de3fa88；seq 50 | 9e366aed；seq 51 | 675d351d；seq 52 | 5c2a5e7f；seq 53 | 39498757；seq 54 | 65a52de3；seq 55 | 75c6ab30）。
- 落点：`lib/{photometric_calib, dynamic_psf, astro_image_io, calibration, gaia_xpsd_client, data_pipeline, healpix_db}/` 的 README.md 与 memory.md。
- 内容性质：文档或规格 + 控制包或台账（模块级 memory.md 集中建档/刷新）。
- 意图证据：消息自述逐条明确（且 51/53/55 消息完全同文，呈模板化批量特征）；变更面证据：每笔 1 新增 1 修改或 2 修改、行数小，与文档批一致（seq 49-55）。
- 关联：49 对应 seq 42/44/46 的 C++ DLL 定版（"v2.0"）；52 对应 seq 44 的切换状态；50 对应 seq 47；54 对应 seq 45；覆盖本片全部 8 条模块链的收束端（star_detector 无此批文档提交，其文档面在 seq 17 已由 CHANGELOG 承担）。
- 不确定点：49 的"v2.0"版本号首现于文档提交而代码提交（seq 42）未标版本，版本命名起点仅此一例证据，未证实为全局约定。

---
