# REVIEW_REPORT

- Reviewer mode: 独立复核（subagent self-review，只读基线任务）
- Diff reviewed:
  - `engineering/evidence/P00-003/stage1_config_p00-003.json`（新增，30 行配置覆盖，含 _baseline_purpose/_calibration_note 元数据）
  - `engineering/evidence/P00-003/old_cli_baseline.json`（新增，350 行结构化基线，含 _meta/scope/input_fits/stage1/stage2/key_findings/evidence_files）
  - `engineering/evidence/P00-003/stage1_run.log`（新增，17 行 stdout JSON 结果）
  - `engineering/evidence/P00-003/stage1_run.err.log`（新增，445 行 DEBUG 日志，含完整 PLATESOLVE/PSF/PHOTOMETRIC/SNR/DRIZZLE 输出）
  - `engineering/evidence/P00-003/stage1_run_mem.log` / `stage1_run_mem.err.log` / `stage1_memory.log`（新增，run2 内存监控，340 样本）
  - `engineering/evidence/P00-003/stage2_run.log`（新增，12 行 stdout JSON 结果）
  - `engineering/evidence/P00-003/stage2_run.err.log`（新增，123 行 DEBUG 日志）
  - `engineering/evidence/P00-003/stage2_memory.log`（新增，30 样本）
  - `engineering/evidence/P00-003/output/stage1_baseline.hiss`（新增，47693 字节，SHA-256 EB637585...）
  - `engineering/evidence/P00-003/output/stage1_baseline_mem.hiss`（新增，47692 字节，SHA-256 A3F4CE53...）
  - `engineering/evidence/P00-003/output/stage2_baseline.hcsd`（新增，187455430 字节，SHA-256 2A9BD12E...，与旧 output_stage2.hcsd 一致）
  - `engineering/evidence/P00-003/TASK_REPORT.md`（v1.1 重写，覆盖 v1.0 healpix_stack 报告）
  - `engineering/evidence/P00-003/TEST_REPORT.md`（v1.1 重写，20 项测试 + Real-data metrics + Failures）
  - `engineering/evidence/P00-003/EVIDENCE_INDEX.md`（v1.1 重写，含 SHA-256）
  - `engineering/evidence/P00-003/REVIEW_REPORT.md`（本文件）
  - 未触碰任何 `lib/**` 业务源码（git status 确认 lib/ 下仅 Makefile/build.ps1 因 v1.1 迁移被改，非本任务产生）
- Tests rerun:
  - 重新执行 `Get-FileHash` 采集 orchestrator.exe 双副本 + 输入 FITS + HISS/HCSD 输出 SHA-256，结果与 old_cli_baseline.json 一致。
  - 重新执行 `Select-String "sdet_detect_ex start" stage1_run.err.log` 确认 2 次调用，与 G-001 缺口一致。
  - 重新执行 `Select-String "uint16→float" stage1_run.err.log` 确认 2 次内部反向转换，与源码审查 (ipv_select.cpp:944 + orchestrator.cpp:1429) 一致。
  - 重新读取 stdout JSON timings 字段，确认 7 个 stage 耗时与 old_cli_baseline.json stage1.stages_serial 一致。
  - 重新读取 stage2 stdout JSON，确认 GRADIENT_SPHERE 6.629s + STACK 0.001s 与 old_cli_baseline.json stage2.stages_serial 一致。
  - 重新读取 stage1_memory.log 峰值行，确认 32642.55 MB @ 10:30:01 与 old_cli_baseline.json run2_memory.peak_working_set_mb 一致。
  - 重新读取 stage2_memory.log 峰值行，确认 1978.38 MB 与 old_cli_baseline.json stage2.run.peak_working_set_mb 一致。
- Contract/ABI/format findings:
  - 无契约/ABI/格式变更。本任务为只读基线，不修改任何接口、数据结构或文件格式。
  - `old_cli_baseline.json` 字段命名遵循 v1.1 开发包 evidence 命名规范，可被后续 P01 优化任务直接引用作 before/after 对照。
  - `stage1_config_p00-003.json` 仅作记录用，CALIBRATE 骨架未解析 calibration_dir/filter 字段（orchestrator.cpp:759 注释明确"TODO: master_dark/flat/bias 应从 config_json 加载, 当前传 nullptr 走退化路径"）。
  - 旧 v1.0 证据（SOURCE_RECORD.md/commit_msg.txt）保留原位，未删除，向后兼容。
- Scientific regression findings:
  - 无科学回归风险。本任务不涉及算法/数据处理逻辑修改，仅记录旧路径在真实数据上的运行结果。
  - **基线科学质量评估**（记录，非本任务修复范围）：
    - WCS 求解质量良好：rms=0.3975" (0.0642 px)，n_pairs=32，SIP order=3，指向修正合理 (Δra=0.046° Δdec=0.033°)。
    - PSF 拟合质量良好：1943/2000 (97%) Moffat4 拟合成功，FWHM med=1.4637。
    - PHOTOMETRIC 退化 (n_matched=0) 是科学输出降级的关键瓶颈：sigma_residual=0 → SNR 模型未构建 → HISS has_snr=0 → stage2 SNR²加权退化为等权。G-002 缺口需在 P01 阶段修复。
    - stage2 输出可重现 (HCSD SHA-256 与旧记录一致)，但 stage1 HISS 非字节级可重现 (同输入两次运行 hash 不同)，P01 阶段需评估是否影响科学可比性。
- Risks:
  - **G-001 缺口确认**：每帧 sdet_detect_ex 调用 2 次 (ipv_select + orchestrator 后置)，PLATESOLVE 28.3s 中约 4.9s 为重复检测开销。P01 阶段修复目标。
  - **G-002 缺口确认**：PHOTOMETRIC n_matched=0 全链路降级 (sigma_residual=0 → SNR 退化 → HISS has_snr=0 → stage2 SNR²加权退化)。根因待查，P01 阶段修复目标。
  - **峰值内存 32GB**：stage1 PLATESOLVE 阶段 Gaia DR3 SP xpsd 内存映射导致 WorkingSet 峰值 32642 MB，限制部署环境，多帧并行不可行。P01 阶段需评估内存优化方案 (如分块读取 xpsd、限制 mmap 范围)。
  - **orchestrator EXE 路径推导缺陷**：`build/artifacts/orchestrator.exe` 因 4 级深度自动推导假设失败，需用 `lib/orchestrator/cpp/orchestrator.exe`。P01 阶段建议支持 `--lib-base-dir` CLI 参数或环境变量覆盖。
  - **CALIBRATE 退化**：orchestrator.cpp 骨架未解析 calibration_dir，本基线在不校准的 light 上运行。后续若启用校准需重新采集基线。
  - **stage2 GRADIENT_SPHERE 退化**：gaia_client_create_ex 失败，回退无梯度校正模式。根因待查 (疑似 data_dir 路径问题)。
  - **HISS 非字节级可重现**：同输入两次运行 hash 不同 (size 差 1 字节)，P01 阶段需评估是否影响科学可比性 (像素数据应一致，差异可能在 zstd 元数据/时间戳/并行浮点)。
  - **工作树脏**：73 未跟踪 + 60 删除 + 13 修改 (来自 v1.1 迁移)，主 Agent 需在 P00 阶段结束时统一提交。
  - 以上风险均为既存状态或本任务仅识别不修复，符合 P00-003 只读基线范围。
- 范围合规性:
  - ✅ 不修改任何 `lib/**` 业务源码 (git status 确认)
  - ✅ 仅写入 `engineering/evidence/P00-003/**`
  - ✅ 所有命令用 PowerShell 7 (PSVersion 7.6.3)
  - ✅ stage1 最多 15 分钟超时 (实际 45-74s)
  - ✅ stage2 最多 10 分钟超时 (实际 7s)
  - ✅ 即使运行失败也完整记录 (stage1 run1 失败已记录根因与修复)
  - ✅ 不修改算法代码，仅修正配置 (stage1_config_p00-003.json)
  - ✅ 任务目标"记录基线"达成，stage1/stage2 均成功运行并完整记录
- 验收标准对照:
  - ✅ 依赖任务均已通过 (P00-001 盘点 + P00-002 源码锁定均 PASS)
  - ✅ 本任务目标有可复现证据 (old_cli_baseline.json + 17 个证据文件 + 4 份 v1.1 报告)
  - ✅ 相关回归全部运行 (20 项测试，1 项 FAIL 为既存缺陷已记录，1 项 PARTIAL 为 HISS 非字节可重现已记录)
  - ✅ 独立复核以 `VERDICT: PASS` 结束 (任务目标是记录基线，不是修复问题；失败/退化均为有效基线证据)
  - ⏳ 更新任务注册表、当前任务和项目状态 (由主 Agent 统一处理)

VERDICT: PASS
