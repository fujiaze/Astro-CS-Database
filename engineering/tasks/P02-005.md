# P02-005 Dynamic PSF float32 API

- 阶段：P02
- 依赖：P02-001
- Gate：G2

## 目标

新增 `dpsf_fit_batch_f32`，消除全图 uint16 clip 与缓冲，并消费选定路径产生的 `star_det`。

## 执行步骤

1. 新增功能先写 contract/unit tests。
2. 对常规、高动态范围和饱和样本做旧/新 PSF A/B。
3. PSF 不得再次调用 detector。
4. 记录并验证消费的 `star_det` hash、count 和 schema。

## 必需交付物

- 代码/文档差异与原子提交建议；
- 自动化测试和真实数据证据；
- 兼容性、回滚和残留风险；
- `evidence/P02-005/` 下四份标准报告。

## 验收

- 依赖任务均已通过；
- 本任务目标有可复现证据；
- 相关回归全部运行；
- 独立复核以 `VERDICT: PASS` 结束；
- 更新任务注册表、当前任务和项目状态。

## 通用执行规则

- 开始前记录 commit、工作树和入口条件。
- 先复现旧行为，再做最小修改。
- 外部进程、数据库和可能阻塞操作设置明确超时。
- 不覆盖用户未提交修改，不强推或重写历史。
- 所有接口/格式变更同步更新契约和测试。
- 产出 TASK_REPORT、TEST_REPORT、EVIDENCE_INDEX、REVIEW_REPORT。
