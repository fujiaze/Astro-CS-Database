# P02-004 实施选定 PlateSolve 单次检测路径

- 阶段：P02
- 依赖：P02-003
- Gate：G2

## 目标

根据 P02-003 决策实施生产路径，同时消除 Orchestrator 的第二次检测。

## 执行步骤

1. 若选择路径 A：正式接入独立 STAR_DETECT 和 `ipv_solve_from_detections_v1`。
2. 若选择路径 B：保持原 `ipv_solve_from_memory` 数据路径，只增加内部 detection sink 导出同次检测结果。
3. 两种路径都写入 `star_det v1`、producer、hash 和 detection-path provenance。
4. 生产运行禁止 API 缺失时静默切换到另一条路径。

## 必需交付物

- 代码/文档差异与原子提交建议；
- 自动化测试和真实数据证据；
- 兼容性、回滚和残留风险；
- `evidence/P02-004/` 下四份标准报告。

## 验收

- 依赖任务均已通过；
- 本任务目标有可复现证据；
- 相关回归全部运行；
- 独立复核以 `VERDICT: PASS` 结束；
- 更新任务注册表、当前任务和项目状态。

## 通用执行规则

- 开始前记录 commit、工作树和入口条件。
- 先复现旧行为，再做最小修改。
- 外部进程、数据库和可能阻塞操作设置明确超时。
- 不覆盖用户未提交修改，不强推或重写历史。
- 所有接口/格式变更同步更新契约和测试。
- 产出 TASK_REPORT、TEST_REPORT、EVIDENCE_INDEX、REVIEW_REPORT。
