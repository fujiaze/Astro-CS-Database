# P11-004 — 修正WCS验证Gate并完成生产端裁决

## 阶段 / Gate

- 阶段：P11
- Gate：G11
- 依赖：P11-003
- 参考：`docs/23_P11_004_REVIEW_DECISION.md`、`docs/24_WCS_VALIDATION_V2_SPEC.md`、`docs/25_AUTHORITATIVE_MATCH_PAIR_CONTRACT.md`

## 目标

用求解器最终 inlier 对作为固定对应关系，通过最终序列化 WCS/SIP 独立回投，判断 WCS 生产端是否真的需要修复。

## 执行要求

1. 保留 P11-004 既有 review bundle 和全部失败证据；
2. 导出权威 inlier pairs，禁止诊断工具重新 kd-tree 配对；
3. 在原16帧运行两层闭环；
4. 通过则以 `NO_CODE_CHANGE_REQUIRED` 关闭本任务；失败才允许最小修改唯一 WCS 生产端；
5. blind catalog rematch 仅作诊断，不作为本任务硬 Gate。

## 预期交付

- authoritative pair schema与导出实现；
- 16帧 Gate v2 报告；
- `P11_004_DECISION.md`（NO_CODE_CHANGE_REQUIRED 或 WCS_PRODUCTION_FIX_REQUIRED）；
- 代码/diff（允许合法 no-op）；
- 测试、证据、回滚说明。

## 通过条件

满足 WCS Validation v2 的 B层全部门限；独立复核最后一行 `VERDICT: PASS`。

## 禁止捷径

不得以全星表 kd-tree p68 作为唯一Gate；不得只用内部RMS；不得在Photometric局部补偿；不得在权威星对闭环通过时仍修改CRPIX/CD/SIP。
