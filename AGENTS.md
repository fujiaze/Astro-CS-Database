# AstroCS AGENTS.md

## 基本规则

中文分析、开发、汇报。

任务开始前读取：
- 根目录 memory.md
- 相关模块 memory.md
- 对应 docs 文档

保持：
科学定义 = 算法 = 接口 = 代码 = 测试。

---

## 开发

修改前明确：
需求、影响范围、风险、验收标准。

遵循最小修改：
禁止无关重构、格式化和扩大修改范围。

模块必须：
职责明确、接口明确、数据含义明确。

核心算法修改必须同步更新：
科学文档、软件文档、测试。

---

## 质量

关注：
- 正确性
- 数值安全
- 内存生命周期
- 错误处理
- 并发安全
- 性能

性能优化先分析：
CPU、内存、IO、等待、重复计算和算法复杂度。

禁止无意义重复跑完整基准。

---

## 验证

修改后必须测试。

报告：
修改内容、测试结果、限制、遗留问题。

未经验证不得声明完成。

---

## 工程管理

memory.md记录稳定结论。

logs记录过程。

commit保持单一目的。

禁止破坏性 Git 操作。

执行命令必须设置 timeout，并保存日志。

---

## Linux环境

开发环境：
vm-bj Linux。

默认：
bash + git + Linux工具链。

所有长时间任务必须：
- 有超时；
- 有日志；
- 可恢复。

---

## Windows验证

Windows仅作为远程验证节点。

需要时通过 SSH 调用 Windows PowerShell 进行：
- Windows编译；
- MSVC测试；
- GUI验证。

Windows不可用时记录等待，不阻塞Linux开发。

---

## 工程包执行

收到工程包：

读取规范和 memory；
分析依赖；
执行任务；
完成测试；
提交报告。

不要自行扩大任务范围。
