import os, sys
sys.stdout.reconfigure(encoding='utf-8')

HERE = os.path.dirname(os.path.abspath(__file__))          # 独立审计/复算件
ROOT = os.path.dirname(HERE)                                # 产出
INV = os.path.join(ROOT, "inventory")
DISP = os.path.join(ROOT, "dispatch")
PRE = os.path.join(ROOT, "DISPATCH-CODE-READ-PREAMBLE.md")
OUTDIR = os.path.join(ROOT, "raw")

CORE = """你是 ACSD 仓库的只读代码通读审计节点。被审基线 HEAD = c8f64e9a，仓库路径：F:\\Astro dev\\Astro CS Normalization Database（Windows + Git Bash）。

第一步（必须、不得跳读）：完整读 {PRE}，按它 §0–§5 全条执行。

批次：CR-{B}
你的独占范围（不得越出，也不得漏任一项）：
{FILES}
输出文件（绝对路径）：{OUT}\\通读-CR-{B}.md

六条不可协商项（前言里已写，这里再钉一次）：
1. **第一个动作**是 Write 上面那个骨架文件：文件头＋每项一行"已读：否"＋`<!-- PROGRESS: 0/{N} -->`；骨架未落盘不许开始读。
2. **每读完一项立刻 append** 该文件的 §2.5 逐数值处置表（全部数值项，五档之一，缺项＝该文件未完成）＋该文件名下 findings（没有就写"无可辩护指控"并附你实际跑过的检索式），然后 bump PROGRESS、把该行改"已读：是（表 N 项）"。禁止攒到最后一次写。
3. 不写普查脚本、不做数量统计（统计不作交付）；单条 finding 取证命令硬上限 8 条。
4. 判 S1 的硬门槛：能沿生产调用路径逐跳写出 `文件:行`；写不出就定 S2/S3 并写明缺哪一跳。
5. 凡涉及判据，先把**不等式方向**钉死（偏绿／偏红／无方向）再下结论；方向没定就不许写"会误绿"。
6. 核过为合规的项也要写"不报的核过项＋检索式"，防止重复立案；**不得照抄派单或邻批给的任何先验结论**，一律独立取证。

只读纪律：零 git 写、零 `git config` 写；不跑构建／ctest／run_checks.py／任何 `eng/**`；不 import 仓库 Python；不写仓库内任何文件；开工与收工各跑一次 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 并把原文贴进成稿。
环境：`rg` 不可用→用 `git -C <repo> grep`；自写 Python 加 `sys.stdout.reconfigure(encoding='utf-8')`；CJK 路径不要写进 bash 内联，落成 .py 再跑（脚本落 `独立审计/复算件/CR-{B}/`）。
收尾：成稿末尾追加"本批最高危三条（只挑不复述）""我读不动或需实跑才能定的""派单与清单差异"三节。"""

FOCUS = {
 35: "本批重点（创新点④的面积算核与反向加钻）：① 多边形裁剪是球面还是平面近似？裁剪后面积是否保证总和不被重分配（通量守恒）？退化多边形／共线／零面积如何处理；② 反向加钻的权重与步长常数出处；③ 信噪比评估器里"未测／不可估计"用什么数值表示（0／1／非数伪装即缺陷）。",
 36: "本批重点（球面面积交叠＝创新点④的几何正本，最严口径）：① 精确性主张逐条核：像元边界是大圆弧还是平面近似、极点与经度环绕如何处理、跨边界与极冠的分解是否完备；② 归一化：交叠面积之和是否恒等于球面立体角（有无静默归一或除以近似值）；③ 每条容差／阶数／细分阈给出处档位，并核"求和型闭合检不出逐格错配"这一已知缺口在本批是否有承载；④ 该门的注册与能红性。",
 37: "本批重点（加钻族期望值的独立性）：① 独立期望实现与被测实现是否同构复制（若同构，写清它能证伪什么、不能证伪什么）；② 故障注入是否单向，"零条断言被执行"与"未知组名"各返回什么码（自己数，别预设）；③ 性能组判据在缺并行支持时是否恒真；④ 夹具里每个常数的出处。",
 38: "本批重点（探针与参照件）：① 这些探针是不是证据载体——若是，查其结论是否只落在 run/ 面（按"三留一不留"口径判不合格）、跟踪面有无对应报告数据；② 参照实现是被调用的还是死件，其算法与被测是否同源（同源即无独立性）；③ 线程探针结论与"不硬编码线程"纪律是否一致；④ 注册状态：这些件是否进构建／进基线。",
 39: "本批重点（反向加钻与方差传播的测试面）：① 通量守恒与方差守恒是否各有真判据（还是只判通量）；② 分辨率因子与分块参数的取值域与条文是否一致；③ 期望值是否由被测实现产出（自证循环）；④ 那个 verify 件的性质：是验收件还是基准件，是否冒充门。",
 40: "本批重点（v6 双轨）：① 同一算法存在两版实现时生产走哪一条、两版常数是否已不一致（逐常数对比并列位点）；② 球面交叠在 v6 侧的精确性是否弱于另一版（若弱，产品可达性决定定级）；③ 多项式像元坐标变换的阶数与系数换算是否与一手论文式一致（须给一手依据或标 UNPROVEN，不得凭记忆）。",
 41: "本批重点（对外类型与句柄声明面）：① 声明面里每个数值语义（枚举值域、单位、缺省、对齐尺寸）与两份合同、实现三方是否同一套数；② 有无"合同声明不存在、代码仍静默读"形态的键；③ 未接线声明（声明了但全仓无消费者）逐条列出。",
 42: "本批重点（板解算类型与求解器接口）：① 单位口径（像元／角秒／度）在类型注释、字段名、实现三处是否一致——邻批已就"畸变向量单位两说"立案，本批独立取证、不照抄；② 结构体首两字段的版本约定在哪些结构上缺失；③ 鲁棒精化的收敛阈／剔除阈常数逐个给档位。",
 43: "本批重点（星点选择与匹配，单文件大段，分段落盘）：① 匹配半径与门限的出处（含是否绑到不存在的依据编号）；② 角分离是否含赤纬余弦因子（跨段结论须写明"与 CR-24 合并定案"，不得单方下结论）；③ 循环角度量的折半口径在本文件内是否自相一致；④ 退化路径返回值形态；⑤ 每读完约 250 行 append 一次处置表。",
 44: "本批重点（求解与三角／多项式变换主实现）：① 多个生产位点共用的那个匹配半径在本批是否有第二来源（副本数与漂移）；② 三角模式索引与哈希常数的出处；③ 像元坐标变换的阶数上界与归一化是否与条文一致；④ 由公式导出却被写死且存多份副本的配额常数，发现就列全位点。",
}

os.makedirs(DISP, exist_ok=True)
made = []
for b in sorted(FOCUS):
    p = os.path.join(INV, f"CR-{b}.txt")
    if not os.path.exists(p):
        print("missing inventory", p); continue
    lines = [l.strip() for l in open(p, encoding="utf-8")
             if l.strip() and not l.strip().startswith("#")]
    prompt = (CORE.replace("{PRE}", PRE).replace("{OUT}", OUTDIR).replace("{B}", str(b))
              .replace("{N}", str(len(lines)))
              .replace("{FILES}", "\n".join(lines) + "\n\n" + FOCUS[b]))
    out = os.path.join(DISP, f"CR-{b}.prompt.txt")
    open(out, "w", encoding="utf-8", newline="\n").write(prompt)
    made.append((b, len(lines)))
print("prompts written:", made)
