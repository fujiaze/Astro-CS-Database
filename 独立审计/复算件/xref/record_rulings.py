import sys
import re
from pathlib import Path

sys.stdout.reconfigure(encoding='utf-8')
BASE = Path(r'产出/')
UN = BASE / '独立审计包' / '07_未决' / 'UNRESOLVED清单.md'
t = UN.read_text(encoding='utf-8')

RULINGS = [
    ('**裁-1 算法模块的交付形态**',
     '**裁-1 算法模块的交付形态【已裁：独立动态库作数】**\n'
     '> **裁决**：认可"每个算法模块是独立 DLL/SO"这一侧为正本，不把全部能力编译进单个可执行。\n'
     '> **因此现状被定性为实现缺陷**（不是设计现状）：装出去的共享库无一在产品的链接行上、也无一份落在活加载器会扫的目录里，'
     '动态 ISA 选择在产品上恒不生效——这条必须补齐。\n'
     '> **派工影响**：① 新工单＝补齐整条加载链（安装布局把库放进加载器真会扫的目录 ＋ 加载器要的清单入仓 ＋ '
     '加一条"装载即必被加载"的门）；② 最高设计里"或在可执行程序内部实现"那半句按正本删除，走变更流程；'
     '③ 文档整治可据"独立动态库"给模块页排序。\n'),
    ('**裁-2 阶段调度器的身份**',
     '**裁-2 阶段调度器的身份【已裁：各阶段各自设计调度器，定 P0】**\n'
     '> **裁决**：取字面读法——每阶段由该阶段的调度器唯一执行；负责人定级为**严重 P0 缺陷**。\n'
     '> **依据（负责人原话要旨）**：各阶段运行特征不同，就需要各自按本阶段特征优化的调度逻辑，'
     '不是一个通用调度器代跑三阶段。\n'
     '> **派工影响**：① 标准化与叠加两个阶段的专用调度器必须成为各自阶段的唯一执行者，工单规模按"重排运行期装配"算（比另一侧大一个量级）；'
     '② 需在设计里回答那个通用调度器退役到哪一步；③ 三阶段各自的调度优化点要进任务书作验收项，不能只"接上"。\n'),
    ('**裁-3 回压账本是否计入调度器常驻线程**',
     '**裁-3 回压账本是否计入调度器常驻线程【已裁：不计入；附一条待核事实】**\n'
     '> **裁决**：不计入。理由（负责人）：调度器本身不占用计算资源。\n'
     '> **待核（不推翻裁决，只标一处前提）**：实测翻倍的对象是**调度器起的 worker 数量**，不是它的协调开销；'
     '而公共头 `lib/include/astrocs/core/context.h:110-111` 自己写着"sum(active)<=budget 全局不超卖"与'
     '"Scheduler 自身 worker 与节点内部 work 共用同一预算"，`lib/include/astrocs/common_abi_v1.h:99` 写"backend 禁自建线程池"，'
     '`lib/include/astrocs/abi/host_api_v1.h:61` 的 acquire 语义是"租借 n 个 worker，预算不足即失败"。'
     '若那些 worker 是执行帧工作的实体，则"不计入"的净效果是稳态并发与内存可达预算两倍（本机 swap 永久禁用、物理 23 GiB）。\n'
     '> **处置**：该事实由第②层批 `复核-V401-B`（AUD401-008）复算。结论回来后二选一：'
     '① 按裁决执行——把"共用同一预算、总和不超预算"那句声明改掉，并写明两套预算如何隔离；'
     '② 若 worker 确为执行体，则请重裁是否计入。\n'),
    ('**裁-4 通量折算条款的签署**',
     '**裁-4 通量折算条款的签署【已裁：按一手论文追认，且定性改变】**\n'
     '> **裁决**：以原始论文为准——归一化保证守恒与像素收缩无关，折算因子恒为一。条款签署结案。\n'
     '> **定性订正（负责人指出）**：这类**科学性问题应依据论文核实**，不该挂成"待负责人签"；'
     '它之所以成为待裁项，来源是**开发节点的执行纪律**（未据一手文献核实就自行选边并登记为负责人裁决）。\n'
     '> **派工影响**：① 撤掉该条款的 `PENDING_OWNER_SIGNOFF` 状态；② 恒为一的那个溯源字段保留并注明"仅溯源、不参与折算"；'
     '③ 另立一条执行纪律整改：科学口径分歧一律先回到一手文献/官方实现核实，核不出才上呈，且上呈件要写"试过哪三条"。\n'),
    ('**裁-17 测光正本的定量证据已全部不在跟踪面（论文与对外口径怎么收）**',
     '**裁-17 测光正本的定量证据【已裁：在 `实验/` 单元内重跑；入库口径按"三留一不留"】**\n'
     '> **裁决（负责人原话口径）**：① **运行日志落进 `实验/` 对应单元**；② **数据仍在 `run/` 里跑**（运行位置不变）；'
     '③ 合成数据一类要求留下**可复现代码**等；④ 基于真实数据的实验要求留下**代码与证据**；'
     '⑤ **所有运行产物一律不留，只留报告数据**。\n'
     '> 这条同时给整个"证据不在跟踪面"的合案定了处置口径：**不是把大产物搬进仓库**，而是把'
     '**可复现代码 ＋ 报告数值 ＋ 运行日志 ＋（真实数据实验的）证据**留在 `实验/` 单元内并入库，产物仍落 `run/` 可回收。\n'
     '> **派工影响**：① 新工单＝测光三组证据在 `实验/photometric-magnitude` 内按上述口径重跑并留档；'
     '② 各实验单元的 `README`/报告模板要新增"运行日志落位"与"报告数据"两栏，使正文引用的每个数都能指到单元内一件入库物；'
     '③ 正文里凡证据指针现指 `run/**` 的，改指单元内入库后的报告数据或日志；产物类引用（大图、产品树）如实注明"可再生、不入库"；'
     '④ 同类合案（其它落在 `run/` 的关键读数）按同一口径批量处理，不逐条挂账。\n'
     '> **本节点（只读）能先做的**：对已归档的 code＋results 做纯 Python 独立复算、并对漂亮数字做换源退化自测；'
     '重跑与留档由开发节点执行。\n'),
]

for head, repl in RULINGS:
    if head not in t:
        print('!! 未找到条目标题：%s' % head[:40])
        continue
    i = t.index(head)
    j = t.find('\n**裁-', i + 1)
    if j < 0:
        j = t.find('\n## ', i + 1)
    t = t[:i] + repl + '\n' + t[j + 1:]
    print('已替换 %s（原区间 %d 字符）' % (head[:26], j - i))

t = t.replace('## 1 需要你裁的（结构性或产品方向，读码不能定案）',
              '## 1 需要你裁的（结构性或产品方向，读码不能定案）\n\n'
              '本轮已有 5 条落裁（裁-1、裁-2、裁-3、裁-4、裁-17），原文就地标注【已裁】并把裁决与派工影响写在条目里，'
              '不再作为待裁项上呈；裁-3 另留一条待核事实，结论回来可能需要重裁。')
UN.write_text(t, encoding='utf-8')
L = t.splitlines()
print('行数 %d；【已裁】标记数 %d；剩余待裁条目标题：' % (len(L), t.count('【已裁')))
for i, l in enumerate(L, 1):
    if re.match(r'^\*\*裁-\d+', l):
        print('   %4d %s' % (i, l.strip()[:66]))
