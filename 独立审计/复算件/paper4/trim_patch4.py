# -*- coding: utf-8 -*-
"""AUDIT-06 论文4 终稿收尾压缩（第三轮）：只删冗词，不动任何主张、边界与数字。"""
import io
import re
import sys

sys.stdout.reconfigure(encoding="utf-8")
P = r"独立审计/03_小论文\论文4_HEALPix面积交叠.md"
t = io.open(P, encoding="utf-8").read()

REPS = [
    ("等面积基数与 NESTED 编号出自 HEALPix 定义文献 [1]；\"叶边界不是大圆弧\"以 HEALPix 自然球面投影的专文 [2] 为准——其摘要逐字主张这层映射等面积且属一族未见文献的投影。定义文献被指名的小节全文本轮不可得，其内容不作依据。",
     "等面积基数与 NESTED 编号出自 HEALPix 定义文献 [1]；\"叶边界不是大圆弧\"以其自然球面投影专文 [2] 为准（摘要逐字主张该映射等面积且属一族未见文献的投影）。定义文献被指名的小节全文本轮不可得，其内容不作依据。"),
    ("物理仿真成像与真实数据端到端两类**未做**（受只读面与阶段裁决限制），本链路因此未达三方一致。",
     "物理仿真成像与真实数据端到端两类**未做**，本链路因此未达三方一致。"),
    ("叶侧是直线段围成的精确方格、零近似，近似只在 drop 侧。求积须先平移到包围盒左下角再累加：卡坐标约 0.5 处的鞋带项约 0.25，而单叶面积可小到 `1e-12`，`1e-16` 的相消噪声会放大成 `5.4e-5` 相对误差——纯数值坑。逐叶面积一律按绝对立体角累加，不以无量纲覆盖分数作中间口径。",
     "叶侧是直线段围成的精确方格、零近似，近似只在 drop 侧。求积须先平移到包围盒左下角再累加：卡坐标约 0.5 处的鞋带项约 0.25 而单叶面积可小到 `1e-12`，`1e-16` 相消噪声会放大成 `5.4e-5` 相对误差。逐叶面积一律按绝对立体角累加，不以无量纲覆盖分数作中间口径。"),
    ("| 两支 `\\|J\\|` | 解析恒等 `= π/3`；逐点差分全天 44652 点，最坏偏离 `5.14e-4` 恰在南极点本身；本机两支各 4000 点避开极点后 `≤1.3e-8` | 推导＋数值 |",
     "| 两支 `\\|J\\|` | 解析恒等 `= π/3`；全天逐点差分 44652 点，最坏偏离 `5.14e-4` 在南极点本身 | 推导＋数值 |"),
    ("三类数据未齐意味着本链路证据等级低于前三个创新点",
     "三类数据未齐使本链路证据等级低于前三个创新点"),
    ("帧信号校准到统一测光坐标系后，绝对信噪比要落到具体像素上才能定权叠加",
     "信号校准到统一测光坐标系后，绝对信噪比要落到具体像素上才能定权叠加"),
]
    ("它是支撑度、面亮度归一分母与逐叶通量闭合的共同分母，且必须在极点、角点、面缝上同样成立。方法是把 12 个基面各自的等面积坐标卡当坐标域：每面是单位正方形、雅可比恒为 π/3、叶在卡内轴对齐，交叠遂化为轴对齐裁剪加鞋带求积，面积按绝对立体角累加。",
     "它是支撑度、权重分母与逐叶通量闭合的共同分母，且必须在极点、角点、面缝上同样成立。方法是把 12 个基面的等面积坐标卡当坐标域：每面单位正方形、雅可比恒为 π/3、叶在卡内轴对齐，交叠化为轴对齐裁剪加鞋带求积，面积按绝对立体角累加。"),
    ("贡献四条：**一**，把卡的前提钉到可复核判据并给出极点的解析落点；**二**，用保总量错配注入为求和型守恒与逐叶正确性划判别力的界；**三**，把精度地板定位到叶的弦表示并按天区分档给出它与冻结预算的差距；**四**，登记两种写入形态在低支撑处的不等价及非数被静默计入的位置。",
     "贡献四条：**一**，把卡的前提钉到可复核判据并给出极点的解析落点；**二**，用保总量错配注入划出求和型守恒与逐叶正确性的判别力界；**三**，把精度地板定位到叶的弦表示并分档给出它与冻结预算的差距；**四**，登记两种写入形态在低支撑处的不等价及非数被静默计入的位置。"),
    ("drizzle 的输出是以累计权重为分母的加权均值，其式(4)(5) 含面积分数、输入权重与像素面积比三因子，\"加权均值\"不含它们的幂次约定 [3]；迁到球面等面积网格属本项目自定义。drop 由任意 WCS（含 SIP）给出：线性核是中间坐标的一次变换 [4]，切平面投影取 TAN、`R_θ = (180°/π)·cot θ` [5]，故直边成球面大圆弧——drop 侧因此可测地化。面积用球面三角形立体角闭式 [6] 作扇形剖分；裁剪原文只处理平面多边形，球面逐边裁剪是其推广 [7]；层级面积按叶计数取 `4π/(12·4^order)` [8][9]。",
     "drizzle 的输出是以累计权重为分母的加权均值，式(4)(5) 含面积分数、输入权重与像素面积比三因子，\"加权均值\"不含它们的幂次约定 [3]；球面实施属本项目自定义。drop 由任意 WCS（含 SIP）给出：线性核是中间坐标的一次变换 [4]，切平面投影取 TAN、`R_θ = (180°/π)·cot θ` [5]，直边因此成球面大圆弧。面积用球面三角形立体角闭式 [6] 作扇形剖分；裁剪原文只处理平面多边形，其球面逐边形式是推广 [7]；层级面积按叶计数取 `4π/(12·4^order)` [8][9]。"),
    ("常规成像软件在切平面规则网格上重采样，本仓未取到可核证据表明主流实现在球面等面积层级网格上定义逐叶面积交换，故不以其作逐叶正确性的对照。",
     "主流成像软件在切平面规则网格上重采样，本仓未取到可核证据显示它们在球面等面积层级网格上定义逐叶面积交换，故不以其作逐叶正确性对照。"),
    ("**判据分两族且必须分列。**求和型（`Σ_p a_jp` 对 `A_drop`、全天叶面积对 `4π`、通量对 `Σ_j x_j`、层级面积对叶计数）约束完备性与全局标度；",
     "**判据分两族且必须分列。**求和型（`Σ_p a_jp` 对 `A_drop`、全天叶面积对 `4π`、通量对 `Σ_j x_j`、层级面积对叶计数）只管完备性与全局标度；"),
    ("**负例**：一叶 ×2 其余按比例吸收、索引整体错位、相邻两叶互换（三型保总量，只有逐叶型能抓）；漏一个候选叶（两族都能抓）；关掉 drop 侧细分；改错 `π/3`。**对象边界**：卡路径只存在于设计条款与随仓库归档的只读原型，生产面积算法命名唯一；本文主张的是设计前提、原型事实与生产叶表示，不是产品字节。",
     "**负例**：一叶 ×2 其余按比例吸收、索引整体错位、相邻两叶互换（保总量，只有逐叶型能抓）；漏一个候选叶（两族都能抓）；关掉 drop 细分；改错 `π/3`。**对象边界**：卡路径只在设计条款与归档只读原型中，生产面积算法命名唯一；本文主张设计前提、原型事实与生产叶表示，不是产品字节。"),
    ("不依赖基面归属的球面精确构造给出：跨缝位形的逐叶误差对 drop 侧细分段数不敏感——`N=2048` 从 1 段到 64 段为 `3.23e-8 → 4.61e-8`，`N=8192` 为 `2.80e-7 → 2.70e-7`，\"近缝不跨缝\"对照同量级——**缝上没有平台**。与细分无关的是叶表示本身：弦四边形对真曲线面积的相对偏差在缝上 `−1.17e-4`（`N=2048`）/`−2.93e-5`（`N=8192`），在极点 `−9.95e-2`，赤道仅 `+3.87e-9…+3.87e-8`。生产范围与此一致：`N ≥ 256` 与 `9 ≤ N < 256` 都只取四角，故生产 `N` 域 `[16, 2²²]` 内没有真曲线边界路径；真曲线细分件在仓但被调用点旁路，其自述\"对所有 `N` 统一启用\"与调用点相反。",
     "不依赖基面归属的球面精确构造显示：跨缝位形的逐叶误差对细分段数不敏感（`N=2048` 从 1 段到 64 段为 `3.23e-8 → 4.61e-8`，`N=8192` 为 `2.80e-7 → 2.70e-7`，\"近缝不跨缝\"对照同量级）——**缝上没有平台**。与细分无关的是叶表示：弦四边形对真曲线面积的相对偏差，缝上 `−1.17e-4`（`N=2048`）/`−2.93e-5`（`N=8192`），极点 `−9.95e-2`，赤道 `+3.87e-9…+3.87e-8`。`N ≥ 256` 与 `9 ≤ N < 256` 都只取四角，故生产 `N` 域 `[16, 2²²]` 内无真曲线边界路径；真曲线细分件在仓却被调用点旁路，其自述与调用点相反。"),
    ("511 例扫描（全 256 档加全中点）：`|r|` 最大 `0.500`、`S > 0.05` 档内最大 `3.57e-2`；量化步长比 32 位存储噪声约大 `4.1e3` 倍，是表示问题而非浮点问题。两形态连\"是否有效\"都分叉，溯源中却无\"因量化丢覆盖\"的计数，过覆盖钳制计数只在未量化一侧触发。一直写形态**不存在** `pixfrac` 偏移：`k` 与分母精确相消、发布信号 `= S_p`——代数事实，可结案。",
     "全 256 档加全中点共 511 例：`|r|` 最大 `0.500`，`S > 0.05` 档内 `3.57e-2`；量化步长比 32 位存储噪声约大 `4.1e3` 倍——表示问题而非浮点问题。两形态连\"是否有效\"都分叉，而溯源无\"因量化丢覆盖\"的计数、过覆盖钳制计数只在未量化一侧触发。一直写形态**不存在** `pixfrac` 偏移：`k` 与分母精确相消、发布信号 `= S_p`，可结案。"),
    ("该闭合（`0.0` 至 `1e-15`）**不构成逐格分配正确**的证据：它是恒等式——弦叶铺满球面、面积算子可加，`Σ_p a_jp ≡ (π/3)·|D∩face|`、`Σ_p A_leaf ≡ 4π`——残差是浮点末位而非几何信息；保总量错配只让它动 `≤4.4e-16`，同一注入的逐叶度量却是 `1e0…3e2`。而且原型里逐格叶面积算完即弃、只累加裁剪格计数，\"构造闭合\"于是拿同一个面积算子与自身相比，属恒真门。它的正当资格范围是**完备性与全局标度**：抓得住漏候选、量级错、雅可比错，抓不住\"面积落在哪一叶\"。",
     "该闭合（`0.0` 至 `1e-15`）**不构成逐格分配正确**的证据：它是恒等式——弦叶铺满球面、面积算子可加，`Σ_p a_jp ≡ (π/3)·|D∩face|`、`Σ_p A_leaf ≡ 4π`——残差是浮点末位而非几何信息；保总量错配只让它动 `≤4.4e-16`，同一注入的逐叶度量却是 `1e0…3e2`。原型里逐格叶面积更算完即弃、只累加裁剪格计数，\"构造闭合\"于是拿同一个面积算子与自身相比，属恒真门。它的正当资格是**完备性与全局标度**：抓得住漏候选、量级错、雅可比错，抓不住\"面积落在哪一叶\"。"),
    ("能抓逐叶分配的独立可执行件在仓内**存在两件**——异源蒙特卡洛参照（不复用被测面积函数）与点源质心级完成度检验（质心对错分敏感）——都被**明文排除于测试注册之外**：前者一次实跑登记 3 条断言为红、容差留待裁决，后者的理由是\"不把无关历史红灯带进基线\"。那三条红灯的原始产物不在仓库内、当前无法重算，故本文只引这一登记事实、**不引其数字**。现行唯一的逐叶面积断言取样域是低 `N` 的**单个赤道中心像素**、容差 `1%` 与 `0.01%`，极区像素只断言顶点数——最坏位形（极点邻叶 −9.9%）恰在其盲区。容差档位还三值并存：文档冻结的 `1e-6` 属**求和型**通量闭合，现行**逐叶**门是同一实现两种精度的 `1e-5` 自洽，另一条门取 `1e-7`、个别段仍写 `1e-6`。",
     "能抓逐叶分配的独立可执行件在仓内**存在两件**——异源蒙特卡洛参照（不复用被测面积函数）与点源质心级完成度检验（质心对错分敏感）——都被**明文排除于测试注册之外**：前者一次实跑登记 3 条断言为红、容差待裁，后者为免把历史红灯带进基线。三条红灯的原始产物不在仓库内、无法重算，故本文只引这一登记事实、**不引其数字**。唯一的现行逐叶面积断言只取低 `N` 的**单个赤道中心像素**、容差 `1%` 与 `0.01%`，极区像素只断言顶点数——最坏位形（极点邻叶 −9.9%）恰在盲区。容差还三值并存：文档冻结的 `1e-6` 属**求和型**通量闭合，现行**逐叶**门只是同一实现两种精度的 `1e-5` 自洽，另一条门取 `1e-7`、个别段仍写 `1e-6`。"),
    ("跨缝位形既不随细分段数变化、也没有平台，把误差量级归因于缝上折线并不成立；拿逐叶量级的数去比 `1e-6` 那条**求和型**门，更是度量与门的类型错配。真正的地板是叶的弦表示：矢高实测超那条弧弦容差 2–5 个数量级且全天空皆然。已知限制条目把同族现象记为\"与 `1e-6` 同阶\"（其量在 `N=512` 为 `1.9e-6`、极点邻叶达 `1e-1`），属低估。这条地板直接进入产品 `support`：分子按弦口径积分、分母按真叶面积取，两者不同源。",
     "跨缝位形既不随细分段数变化、也无平台，把误差量级归因于缝上折线并不成立；拿逐叶量级的数去比 `1e-6` 那条**求和型**门，更是度量与门的类型错配。真正的地板是叶的弦表示：矢高超那条弧弦容差 2–5 个数量级且全天空皆然；已知限制条目把同族现象记为\"与 `1e-6` 同阶\"（`N=512` 为 `1.9e-6`、极点邻叶达 `1e-1`），属低估。地板直接进入产品 `support`：分子按弦口径积分、分母按真叶面积取，两者不同源。"),
    ("量化偏差在常量面亮度场上产生与天区位置相关的 `±0.1–0.5%` 散斑（毫星等量级），与\"无接缝\"目标冲突；`S<1/510` 的像素按未覆盖发布、信号为非数。下游两处接缝度量把非数填 0 后直接进度量且不计数（创新点三的门正走这个读数），一处至少以中位数替代并如实标记；消费侧采样器逐项跳过非有限值，属 fail-closed。一条真实帧旁证记 `min 0.35948、mean 0.99811、median 1.0、n_zero 0`，未落入非数档，对应单像素偏差上界 `0.5/92 = 5.4e-3`；是否触及真实测光取决于产品端是否只用高支撑像素，而因溯源无丢覆盖计数，这类像素在当前证据面上不可见。",
     "量化偏差在常量面亮度场上产生与位置相关的 `±0.1–0.5%` 散斑（毫星等量级），与\"无接缝\"目标冲突；`S<1/510` 的像素按未覆盖发布、信号为非数。下游两处接缝度量把非数填 0 后直接进度量且不计数（创新点三的门正走这个读数），一处至少以中位数替代并如实标记；消费侧采样器逐项跳过非有限值，属 fail-closed。一条真实帧旁证记 `min 0.35948、n_zero 0`，未落入非数档，对应上界 `0.5/92 = 5.4e-3`；是否触及真实测光取决于产品端是否只用高支撑像素，而溯源无丢覆盖计数，这类像素在证据面上不可见。"),
    ("面积交叠的叶侧在卡内不再需要近似：两支各自 `|J| = π/3` 解析成立，叶为轴对齐方格，方格与 NESTED 像素双射、不重不漏，面积按绝对立体角累加，层级面积与覆盖重数守恒成立，全天求和残差 `0.0`。极点是卡的不可逆点（`φ ∝ 1/s` 的方向折叠）而非面积口径失效，回退路径据此存在。",
     "面积交叠的叶侧在卡内不再需要近似：两支各自 `|J| = π/3` 解析成立，叶为轴对齐方格，方格与 NESTED 像素双射、不重不漏，面积按绝对立体角累加，层级面积与覆盖重数守恒成立，全天求和残差 `0.0`。极点是卡的不可逆点而非面积口径失效，回退路径据此存在。"),
    ("边界与主张同等明确：求和型闭合只证完备性与全局标度，不是逐格正确的证据；逐格正确性有异源可复算的对照手段，但两件对照件未注册、参照件有三条红灯待裁、容差三值并存，主张停在\"总量闭合与完备性已验证\"。精度地板在叶的弦表示而非缝上的分段数，并经支撑度分子分母不同源进入产品。",
     "边界与主张同等明确：求和型闭合只证完备性与全局标度，不是逐格正确的证据；逐格正确性有异源对照手段，但两件对照件未注册、参照件有三条红灯待裁、容差三值并存，主张停在\"总量闭合与完备性已验证\"。精度地板在叶的弦表示而非缝上的分段数，并经支撑度分子分母不同源进入产品。"),
]

miss = []
for o, n in REPS:
    if o in t:
        t = t.replace(o, n)
    else:
        miss.append(o[:36])
io.open(P, "w", encoding="utf-8").write(t)
print("applied:", len(REPS) - len(miss), "misses:", len(miss))
for m in miss:
    print("  MISS:", m)
nb = re.sub(r"```.*?```", "", t.split("## 参考文献")[0], flags=re.S)
print("正文 CJK:", len(re.findall(r"[一-鿿]", nb)))
print("含参考文献 CJK:", len(re.findall(r"[一-鿿]", t)))
