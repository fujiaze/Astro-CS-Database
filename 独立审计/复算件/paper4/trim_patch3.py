# -*- coding: utf-8 -*-
"""AUDIT-06 论文4 终稿篇幅修订：只压措辞，不动结论、边界与数字。"""
import io
import re
import sys

sys.stdout.reconfigure(encoding="utf-8")
P = r"独立审计/03_小论文\论文4_HEALPix面积交叠.md"
t = io.open(P, encoding="utf-8").read()

REPS = [
    ("它是支撑度、权重分母与逐叶通量闭合的共同分母，且必须在极点、角点、面缝上同样成立。方法是把 12 个基面的等面积坐标卡当坐标域：每面单位正方形、雅可比恒为 π/3、叶在卡内轴对齐，交叠化为轴对齐裁剪加鞋带求积，面积按绝对立体角累加。",
     "它是支撑度、权重分母与逐叶通量闭合的共同分母，且在极点、角点、面缝上同样成立。方法是把 12 个基面的等面积坐标卡当坐标域：每面单位正方形、雅可比恒为 π/3、叶在卡内轴对齐，交叠化为轴对齐裁剪加鞋带求积，面积按绝对立体角累加。"),
    ("资格只到完备性与全局标度——保总量的错分只让它动 `1e-16`，同一注入的逐叶度量却是 `1e0…3e2`。精度地板在叶的弦表示：逐叶偏 −9.9%…+0.6%，全天求和却恰好闭合。极点是卡的不可逆点而非面积失效点——回退路径据此存在。",
     "资格只到完备性与全局标度——保总量错分只让它动 `1e-16`，同一注入的逐叶度量却是 `1e0…3e2`；精度地板在叶的弦表示（逐叶偏 −9.9%…+0.6%，全天求和却恰好闭合）；极点是卡的不可逆点而非面积失效点——回退路径据此存在。"),
    ("等面积基数与 NESTED 编号出自 HEALPix 定义文献 [1]；\"叶边界不是大圆弧\"以 HEALPix 自然球面投影的专文 [2] 为准——其摘要逐字主张这层映射等面积且属一族未见文献的投影。定义文献被指名的小节全文本轮不可得，其内容不作依据。",
     "等面积基数与 NESTED 编号出自 HEALPix 定义文献 [1]；\"叶边界不是大圆弧\"以其自然球面投影专文 [2] 为准（摘要逐字主张该映射等面积且属一族未见文献的投影）。定义文献被指名的小节全文本轮不可得，其内容不作依据。"),
    ("drizzle 的输出是以累计权重为分母的加权均值，其式(4)(5) 含面积分数、输入权重与像素面积比三因子，\"加权均值\"不含它们的幂次约定 [3]；迁到球面等面积网格属本项目自定义。drop 由任意 WCS（含 SIP）给出：线性核是中间坐标的一次变换 [4]，切平面投影取 TAN、`R_θ = (180°/π)·cot θ` [5]，故直边成球面大圆弧——drop 侧因此可测地化。",
     "drizzle 的输出是以累计权重为分母的加权均值，式(4)(5) 含面积分数、输入权重与像素面积比三因子，\"加权均值\"不含它们的幂次约定 [3]；迁到球面等面积网格属本项目自定义。drop 由任意 WCS（含 SIP）给出：线性核是中间坐标的一次变换 [4]，切平面投影取 TAN、`R_θ = (180°/π)·cot θ` [5]，直边因此成球面大圆弧。"),
    ("对照只两路可核：第三方 HEALPix 实现 [10] 与不依赖基面归属的异源球面构造。常规成像软件在切平面规则网格上重采样，本仓未取到可核证据表明主流实现在球面等面积层级网格上定义逐叶面积交换，故不以其作逐叶正确性对照。",
     "可核对照只有两路：第三方 HEALPix 实现 [10] 与不依赖基面归属的异源球面构造。主流成像软件在切平面规则网格上重采样，本仓未取到可核证据显示它们在球面等面积层级网格上定义逐叶面积交换，故不以其作逐叶正确性对照。"),
    ("物理仿真成像与真实数据端到端两类**未做**（受只读面与阶段裁决限制），本链路因此未达三方一致。",
     "物理仿真成像与真实数据端到端两类**未做**，本链路因此未达三方一致。"),
    ("的 oracle 必须异源（真曲线边界加外推，或第三方库亚像素偏移），不得与被检路径共用同一折线或弦。幂次类与面积口径类判据也不可互替：把叶表示换成弦，幂次比值不变、照样判绿。",
     "的 oracle 必须异源（真曲线边界加外推或第三方库亚像素偏移），不得与被检路径共用同一折线或弦。幂次类与面积口径类判据也不可互替：叶表示换成弦，幂次比值不变照样判绿。"),
    ("**负例**：一叶 ×2 其余按比例吸收、索引整体错位、相邻两叶互换（保总量，只有逐叶型能抓）；漏一个候选叶（两族都能抓）；关掉 drop 细分；改错 `π/3`。**对象边界**：卡路径只在设计条款与只读原型中，生产面积算法命名唯一；本文主张的是设计前提、原型事实与生产叶表示，不是产品字节。",
     "**负例**：一叶 ×2 其余按比例吸收、索引错位、相邻两叶互换（保总量，只有逐叶型能抓）；漏候选叶（两族都能抓）；关掉 drop 细分；改错 `π/3`。**对象边界**：卡路径只在设计条款与只读原型中，生产面积算法命名唯一；本文主张设计前提、原型事实与生产叶表示，不是产品字节。"),
    ("| 两支 `\\|J\\|` | 解析恒等 `= π/3`；逐点差分全天 44652 点，最坏偏离 `5.14e-4` 恰在南极点本身 |",
     "| 两支 `\\|J\\|` | 解析恒等 `= π/3`；全天逐点差分 44652 点，最坏偏离 `5.14e-4` 落在南极点本身 |"),
    ("| 方格 ↔ 像素 | `N=16`：3072 格 ↔ 3072 互异像素、格内 9 点同格、跨格 0；`N=64`：49152/49152、跨格 0 |",
     "| 方格 ↔ 像素 | `N=16`：3072 格 ↔ 3072 互异像素、格内 9 点同格、跨格 0；`N=64` 同样 |"),
    ("`N ≥ 256` 与 `9 ≤ N < 256` 都只取四角，故生产 `N` 域 `[16, 2²²]` 内没有真曲线边界路径；真曲线细分件在仓但被调用点旁路。",
     "`N ≥ 256` 与 `9 ≤ N < 256` 都只取四角，故生产 `N` 域 `[16, 2²²]` 内没有真曲线边界路径；真曲线细分件在仓却被调用点旁路，其自述与调用点相反。"),
    ("两形态连\"是否有效\"都分叉，溯源中却无\"因量化丢覆盖\"的计数，过覆盖钳制计数只在未量化一侧触发。一直写形态**不存在** `pixfrac` 偏移：`k` 与分母精确相消、发布信号 `= S_p`——代数事实，可结案。",
     "两形态连\"是否有效\"都分叉，而溯源无\"因量化丢覆盖\"的计数、钳制计数只在未量化一侧触发。一直写形态**不存在** `pixfrac` 偏移：`k` 与分母精确相消、发布信号 `= S_p`，可结案。"),
    ("能抓逐叶分配的独立可执行件在仓内**存在两件**——异源蒙特卡洛参照（不复用被测面积函数）与点源质心级完成度检验（质心对错分敏感）——都被**明文排除于测试注册之外**：前者一次实跑登记 3 条断言为红、容差待裁，后者的理由是不把无关历史红灯带进基线。那三条红灯的原始产物不在仓库内、当前无法重算，故本文只引这一登记事实、**不引其数字**。现行唯一的逐叶面积断言取样域是低 `N` 的**单个赤道中心像素**、容差 `1%` 与 `0.01%`，极区像素只断言顶点数——最坏位形（极点邻叶 −9.9%）恰在盲区。容差档位还三值并存：文档冻结的 `1e-6` 属**求和型**通量闭合，现行**逐叶**门是同一实现两种精度的 `1e-5` 自洽，另一条门取 `1e-7`、个别段仍写 `1e-6`。",
     "能抓逐叶分配的独立可执行件在仓内**存在两件**——异源蒙特卡洛参照与点源质心级完成度检验（质心对错分敏感）——都被**明文排除于测试注册之外**：前者一次实跑登记 3 条断言为红、容差待裁，后者的理由是不把无关历史红灯带进基线。三条红灯的原始产物不在仓库内、无法重算，故本文只引这一登记事实、**不引其数字**。现行唯一的逐叶面积断言取样域是低 `N` 的**单个赤道中心像素**、容差 `1%` 与 `0.01%`，极区像素只断言顶点数——最坏位形（极点邻叶 −9.9%）恰在盲区。容差还三值并存：文档冻结的 `1e-6` 属**求和型**通量闭合，现行**逐叶**门只是同一实现两种精度的 `1e-5` 自洽，另一条门取 `1e-7`、个别段仍写 `1e-6`。"),
    ("一条真实帧旁证记 `min 0.35948、mean 0.99811、n_zero 0`，未落入非数档，对应单像素偏差上界 `0.5/92 = 5.4e-3`；是否触及真实测光取决于产品端是否只用高支撑像素，而溯源无丢覆盖计数，这类像素在当前证据面上不可见。",
     "一条真实帧旁证记 `min 0.35948、n_zero 0`，未落入非数档，对应上界 `0.5/92 = 5.4e-3`；是否触及真实测光取决于产品端是否只用高支撑像素，而溯源无丢覆盖计数，这类像素在证据面上不可见。"),
    ("面积交叠的叶侧在卡内不再需要近似：两支各自 `|J| = π/3` 解析成立，叶为轴对齐方格，方格与 NESTED 像素双射、不重不漏，面积按绝对立体角累加，层级面积与覆盖重数守恒成立，全天求和残差 `0.0`。极点是卡的不可逆点（`φ ∝ 1/s` 的方向折叠）而非面积口径失效，回退路径据此存在。",
     "面积交叠的叶侧在卡内不再需要近似：两支各自 `|J| = π/3` 解析成立，叶为轴对齐方格，方格与 NESTED 像素双射、不重不漏，面积按绝对立体角累加，层级面积与覆盖重数守恒成立，全天求和残差 `0.0`。极点是卡的不可逆点而非面积口径失效，回退路径据此存在。"),
    ("边界与主张同等明确：求和型闭合只证完备性与全局标度，不是逐格正确的证据；逐格正确性有异源可复算的对照手段，但两件对照件未注册、参照件有三条红灯待裁、容差三值并存，主张停在\"总量闭合与完备性已验证\"。",
     "边界与主张同等明确：求和型闭合只证完备性与全局标度，不是逐格正确的证据；逐格正确性有异源对照手段，但两件对照件未注册、参照件有三条红灯待裁、容差三值并存，主张停在\"总量闭合与完备性已验证\"。"),
]

miss = []
for item in REPS:
    if isinstance(item, str):
        print("SKIP malformed entry:", item[:30])
        continue
    o, n = item
    if o in t:
        t = t.replace(o, n)
    else:
        miss.append(o[:40])
io.open(P, "w", encoding="utf-8").write(t)
print("misses:", len(miss))
for m in miss:
    print("  MISS:", m)

nb = re.sub(r"```.*?```", "", t.split("## 参考文献")[0], flags=re.S)
print("正文 CJK:", len(re.findall(r"[\u4e00-\u9fff]", nb)))
for s in re.split(r"\n(?=### |## )", nb):
    n = len(re.findall(r"[\u4e00-\u9fff]", s))
    if n:
        print("  %4d  %s" % (n, s.split("\n")[0][:30]))
