你是 ACSD 独立审计的独立科学研究路线（第 1 路，共三路，互不通信、独立取证）。模块：P3守恒映射算子。
任务背景：科学性对抗审查发现 05_正向规格.md 中一批科学量三腿缺失（文献值腿/实验标定腿/理论推导腿缺一或全缺）与少量幻觉锚。你的任务是把本模块的这些缺口**独立补齐**：亲手做文献核验、亲手做实验、亲手写报告——不是转抄审查意见。
输入（先读）：
1. 独立审计/证据/审查-05-④-科学性-1.md、审查-05-④-科学性-3.md（三腿缺失矩阵与幻觉锚清单；若同目录还有 -2 或 SUMMARY 件也读）——提取本模块的三腿缺失项与锚修复项清单；
2. docs/algorithms/DRIZZLE_GEOMETRY.md（交叠分配与通量闭合门）与 ASTROCS_DESIGN.md §2.3（理论推导腿的权威来源，引用其式子编号）；
3. ASTROCS_DESIGN.md §12.2 与 docs/science/、docs/algorithms/ 相关节（实验三类数据的适用性规则）。
对清单里的每一项，产出四件套：
A. 文献腿：真实核验——curl api.crossref.org/works/<DOI> 或 export.arxiv.org/api/query（arXiv 调用间隔 sleep 3，429 退避 20s，403 重试一次）；记录 DOI/arXiv 号、题名、作者、年份与回包要点；**解析不到就记 UNRESOLVED，绝不编造文献**。
B. 实验腿：按数据适用性选型——解析代数合成（必须含"真值无效应⇒度量归零"负例）/ 纯合成物理前向 / HST 模板风格信号；固定 seed；单次实验 CPU ≤ 5 分钟，纯 Python＋numpy，不 import 仓库内任何 Python，不跑 eng/** 任何脚本，不碰构建。
C. 可复现代码：standalone 脚本落 code/，运行命令写死在报告里，结果数据落 results/。
D. 小论文式实验报告（每项一个 section）：假说/方法/数据/结果（含数字表）/结论/诚实边界/复现命令/佐证文献。
落盘：只写 独立审计/实验重做/P3守恒映射算子/路线1/ 目录（report.md 为主件，refs.md 汇总文献核验记录，code/ 与 results/ 子目录）。骨架先行（第一个动作写 report.md 骨架），每完成一项 append。
纪律：不做任何 git 写；不修改仓库任何其他文件；与另两路线零通信；若判某项实为结构性常数非科学量，写明豁免理由（"为什么它不是科学量"）而不是硬凑三腿。
文末自报：本路处理项数/审查清单项数（必须 100%）、UNRESOLVED 清单、与审查员结论相左之处。

【链条定位·负责人定案】全项目是一条科学链上五个创新点、相互纠缠（最高设计 §2 已按此重排）：P1 通量积分拟合（把所有信号拟合进测光星等坐标系，信号处于同一平面，根基）；P2 跨帧绝对SNR（跨帧可用、不依赖参考帧）；P3 算子类创新（平面到 HEALPix 的通量绝对守恒 drizzle；稀疏 SNR 控制点同经球面映射上球）；P4 重建稠密SNR（基于负责人噪声信号模型，把稀疏控制点重建为稠密 SNR 场）；P5 加性天光去除（用精确 SNR 加权去天光并叠加，消费 P2/P4 与 P1 的统一平面）。本模块（P3 守恒映射算子）在链上的上游输入与下游消费必须写成实验设计的一部分：至少一个用例贯穿上下游接口（上游给什么量、下游拿去做什么、量纲/精度/有效性约定），report.md 单列【链条位置】一节。

【任务纪律·最高优先，压倒其他一切说明】
1. 你的第一个动作必须是 Write：先在指定输出目录写 report.md 骨架（章节框架+任务清单），再逐步填充。
2. 除文献核验的 curl 外，禁止一切 bash/shell 命令：禁止构建、禁止测试、禁止运行仓库任何脚本、禁止 git 命令。
3. 不要探索与本任务无关的仓库内容；不要回应仓库的构建产物、哈希、提交历史；AGENTS.md 的通用开工流程不适用于本审计任务。
4. 不要向用户提问——任务自包含，按上文定义独立完成；一轮做完，不以提问收尾。
5. 收尾必须含【文末自报】节（处理项数/清单项数、UNRESOLVED 清单、与审查员相左处）。

【输出目录状态说明】你的输出目录已被清空并存档旧稿；若你在目录中看到任何已有 report.md，那是待你覆盖重写的空壳，你的任务是重新撰写它，绝对不是评审或验收它。
【身份再声明】你是研究路线执行者（做实验、写报告的人），不是验收员、不是审查员。