# Qoder 配额恢复后一键续跑

```bash
cd "/workspace/Astro CS Database/独立审计/运行时" && AUDIT_MAX_CONC=8 python3 -u audit-monitor.py run
```

- 队列已就绪：15 路全量（queue.tsv）；提示词在 prompts/；状态已清零。
- 验收环：`python3 -u audit-acceptor.py run`；哨兵：`python3 audit-sentinel.py`。
- 已完成的路线会被自动跳过（按 report.md 文末自报判据）。