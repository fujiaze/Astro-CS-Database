# 文档订正意见 · ①测光星等坐标系（FIX-DOC）

**批号**: AUDIT-06  
**本点目录**: `/workspace/Astro CS Database/独立审计/08_修复包/①测光星等坐标系`  
**权威链锚**: `ASTROCS_DESIGN.md §2.1` → `docs/science/PHOTOMETRY.md`（本页订正目标）；辅以 `docs/algorithms/PHOTOMETRIC_FIT.md`、`docs/contracts/DATA_SEMANTICS.md`  

---

## 订正总览

本件记录 **06_P 条订正**：错 X／缺 Y／乱 Z（文末自报）。

每条五字段：
1. **被订正文件 : 行 ＋ 内容锚**
2. **现状原文**（逐字短引 ≤ 一句）
3. **订正后完整文字**（可直接替换粘贴的整句/整段）
4. **依据**（上位文档哪一条 / 一手文献 / 实现事实的文件 : 行）
5. **同句还出现在哪些文件**（口径唯一要求一并改，逐处列全）

⇒ 凡订正后的文字构成本点实现约束的，在文末附上「⇒ 见 05 §n」索引。

---

## 一、错（与正本/实现/文献矛盾）

> 本档条目持续追加中（PROGRESS：截至回合 1，已写 3 条）

### DOC-ERR-001 `sigma_residual` 的"不可估计"双义

**被订正文件**: `docs/science/PHOTOMETRY.md:191`  
**内容锚**: "否则 `sigma_residual=0`（`star_matcher.cpp:615-626`，判据在 `:616`）"  

**现状原文**:
```text
参考星数 |r_consistent|>=3 才进 IRLS，否则 NO_DATA；|r_inliers|>=2 才估计 sigma_residual，否则 sigma_residual=0
```

**订正后完整文字**:
```text
参考星数 |r_consistent|≥3 才进 IRLS，否则 NO_DATA；|r_inliers|≥2 才估计 sigma_residual，否则 `sigma_residual=0` **且该 0 同时表示「不可估计（|r_inliers|<2）」与「实测零散度（MAD(r)=0）」，两者在产品面无区分字段**。下游消费方必须按统一规则处理：`sigma_residual≤0` ⇒ `fit_status=未估计` 且不发布派生 QA 字段（`sigma_mag/sigma_cal_rel`），任何消费方不得把 0 解释为「零不确定度」。
```

**依据**: `docs/algorithms/PHOTOMETRIC_FIT.md:68-69`（σ≤0⇒fit_status=2、不发布派生字段的同一规则声明）；`01/A2`（产品面不可区分是缺陷）；`02/V-8`（需给「不可估计」具面值而非仅写 0）。

**同句还出现在**: `docs/algorithms/PHOTOMETRIC_FIT.md:65` ("|r_inliers|<2 ⇒ `sigma_residual=0`（不可估计）") —— 该处注释虽写了「不可估计」，但未在产品契约层显式规定统一的消歧规则，仍需与 PHOTOMETRY.md:191 同步增补；见⇒ 见 05 §7 G-12/G-13。

---

### DOC-ERR-002 `match_radius_px = 2.0` 无适用域声明

**被订正文件**: `docs/science/PHOTOMETRY.md:218`  
**内容锚**: "`mag_tolerance=3.0` 实际传入点；`star_matcher.cpp:241-248` 仅为诊断"  

**现状原文**:
```text
与 `lib/algorithms/photometry/cpp/src/star_matcher.cpp:21-27`…及 `lib/algorithms/photometry/cpp/src/pc_api.cpp:145,435`（`mag_tolerance=3.0` 实际传入点；`star_matcher.cpp:241-248` 仅为诊断）一致。
```

**订正后完整文字**:
```text
与 `lib/algorithms/photometry/cpp/src/star_matcher.cpp:21-27`…及 `lib/algorithms/photometry/cpp/src/pc_api.cpp:145,435`（`mag_tolerance=3.0` 实际传入点）、`:2.0`（`match_radius_px`）**两常数各自具名**（后者在 `pc_api.cpp:137,427,592,835,1115` 共五处传入，默认值在 `star_matcher.h:71`）。**适用域**：`match_radius_px=2.0 px` 要求（a）该帧 WCS 绝对指向残差 ≪ match_radius（像素域），（b）match_radius ≳ PSF 质心不确定度。testdata 真实帧残余 0.0068″ ⇒ 在该帧上以~280 倍余量成立。**跨仪器使用时必须按像元尺度复核该常数**（2.0 px 对应的天球半径随画幅跨 1.92″–12.6″）。
```

**依据**: `docs/algorithms/PHOTOMETRIC_FIT.md:231-232`（五处传入点汇总、适用域推导、跨仪器复核要求）；`01/C6`（无条文锚是登记缺口，但此处更严重：**有常数值却无适用域**导致误用风险）。

**同句还出现在**: `docs/algorithms/PHOTOMETRIC_FIT.md:231-232`（已有详细记载，但 PHOTOMETRY.md:218 作为**科学正文**首次出现时遗漏，须补至同等详细程度，而非仅指过去）。

---

### DOC-ERR-003 `sigma_residual` 消费口径残留旧式

**被订正文件**: `lib/algorithms/photometry/cpp/include/photometric_calib.h:59`  
**内容锚**: 头文件注释 `// SNR_phot = 1/(ln10×sigma_residual)`  

**现状原文**:
```c++
/** @brief photometric quality
 *  @param[out] snr_phot = 1/(ln10×sigma_residual)
 */
```

**订正后完整文字**:
```c++
/** @brief photometric quality
 *  @param[out] sigma_residual (dex)，消费口径为零点标准误 `sigma_kappa,stat ≈ 1.253·sigma_residual/√N`（SNR 面走 `snr_estimator.h:295` 的正统式，**本注释不得成为任何实现的依据**）
 */
```

**依据**: `02/式 -4 消费口径 (¬旧式)`（`1.253·sigma_residual/√N` 是唯一有效式，旧式 `1/(ln10·sigma_residual)` 已被上位判为非 SNR、跨 3 个数量级错误）；`01/B26`（注释残留旧式属 S3 订正面）。

**同句还出现在**: 无（仅此一处，但该头文件作为 ABI 合同件须同步修订）。⇒ 见 05 §7 G-25。

---

## 二、缺（无出处/无适用域/无单位/无常数推导）

> 本档条目持续追加中

### DOC-MISS-001 常数 `c=4.685` 与 `0.6744897501960817` 的书目锚

**被订正文件**: `docs/science/PHOTOMETRY.md:295-296`  
**内容锚**: 「Tukey biweight c=4.685」与「MAD→σ换算 0.6744897501960817」两条引用声明  

**现状原文**（行 295-296）:
```text
1. Tukey biweight c=4.685（95% 高斯渐近效率）：Mosteller & Tukey 1977...
2. MAD→σ换算 1/0.6744897501960817 = 1.482602218505602：标准正态 MAD 分位恒等式...
```

**订正后完整文字**（在行 295-296 后追加 §14a，整段重写而非局部修改）:
```text
## 14 Primary literature（引用定位声明）

1. Tukey biweight `c=4.685`（95% 高斯渐近效率）：**Beaton, A. E. & Tukey, J. W. 1974, Technometrics 16, 147 (DOI 10.1080/00401706.1974.10489171)**；定位经 [PMC6768164](https://pmc.ncbi.nlm.nih.gov/articles/PMC6768164/) 实证核对（"c=4.685 yields 95% asymptotic efficiency at the Gaussian"）。
2. MAD→σ换算 `1/0.6744897501960817 = 1.482602218505602`：标准正态 MAD 分位恒等式（`Φ⁻¹(3/4) = 0.6744897501960817`，double 逐位等于 `1/1.482602218505602`；与 `docs/science/NOISE_MODEL.md` §14 同值），教科书级恒等式，Project-defined 采纳。**书目支撑**: Rousseeuw & Croux 1993, JASA 88, 1273 (DOI 10.1080/01621459.1993.10476408)。4 位截断写法 `0.6745` 与全精度值相对差 **+1.5196e-05**，只允许出现在「≈」语境并标注该偏差；权威值 = 全精度写法（W4-A6 处置 V12-N-03）。
```

**依据**: `02/常数表行 156-159`（`c=4.685` 的书目支撑为 Beaton & Tukey 1974；`0.6744897501960817` 的 Rousseeuw & Croux 1993 锚）；`01/C11`（书目侧缺 A 级，本轮全部 UNPROVEN；但 PHOTOMETRY.md:295 只写 Mosteller & Tukey 1977 **未给 DOI**，且该书的「§2 效率表」从未被读到正文 ⇒ 不满足"可核验的一手出处"要求，必须补上 Beaton & Tukey 1974 的 DOI 锚）。

**同句还出现在**: 无（本仓仅 PHOTOMETRY.md 有 primary literature 一节，但该节当前版本不满足"可执行核验"的标准）。⇒ 见 05 §4 K-5/K-6。

---

### DOC-MISS-002 证据路径悬空的工程事实

**被订正文件**: `docs/science/PHOTOMETRY.md:136`  
**内容锚**: 「生产落盘 `ZP_syn` 由 §2a.1 公式逐位复现」的证据位置  

**现状原文**:
```text
| 真实数据（生产链） | 生产落盘 `ZP_syn` 由 §2a.1 公式**逐位复现**：T2/M1 落盘 `−15.126346726632235` vs 复算 `−15.126346726631280`（Δ=9.5e−13，n=2338）；T3/M1 落盘 `−15.123241368129857` vs 复算 `−15.123241368086541`（n=2309） | `run/SCI-PHOT-FORMULA-01/evidence/d1_zp_sigma_rederive.json`、`d1_zp_sigma_rederive.json` |
```

**订正后完整文字**:
```text
| 真实数据（生产链） | 生产落盘 `ZP_syn` 由 §2a.1 公式**逐位复现**：T2/M1 落盘 `−15.126346726632235` vs 复算 `−15.126346726631280`（Δ=9.5e−13，n=2338）；T3/M1 落盘 `−15.123241368129857` vs 复算 `−15.123241368086541`（n=2309） | **`artifacts/evidence/audit-06/zp_sigma_rederive.json`（跟踪副本）**；原始件 `run/SCI-PHOT-FORMULA-01/evidence/` 会在 `eng/tools/run_gc.py --prune-products` 下回收，引用数字时**必须同时给出跟踪副本指针**。 |
```

**依据**: `01/D1`（正本 §2a.5/§2a.6/§2a.7/§8/:377 把定量结论的证据位置写成 `run/...`；该目录会被主动回收，独立读者无法复核）；`03/㉒`（绝对刻度锚重跑并入库，在此之前不得作为已核验证据引用）。

**同句还出现在**: `docs/algorithms/PHOTOMETRIC_FIT.md:203-204`（同一读数在此重复出现但未解决跟踪问题）；需同步追加 `artifacts/evidence/` 下的跟踪副本指针。⇒ 见 05 §8 G-30。

---

### DOC-MISS-003 `m(x,y)` 施加面的规范式零条文

**被订正文件**: `docs/science/PHOTOMETRY.md:13`  
**内容锚**: "像素承载面（线性标度面）：`I_photo = k_photo·m(x,y)·I_cal`（§16.1 ⑤）"  

**现状原文**:
```text
像素承载面（线性标度面）：`I_photo = k_photo·m(x,y)·I_cal`（§16.1 ⑤），标度类别 = `photo_scaled_adu`……
```

**订正后完整文字**:
```text
像素承载面（线性标度面）：**§16.1 ⑤已正式写定规范式**：`I_photo = k_photo·m(x,y)·I_cal`，其中 `m(x,y) = 10^{−Σ_{j=0}^{order} c_j(B_j − B̄_j)}`（基函数 `B_j` 为中心化空间特征；取负号源于 `r_i = log10(F_instr/F_syn)` 的残差定义，使拟合出的系数直接对应乘性修正因子）。**注意**：该式在实现侧是唯一的载体（代码注释内联），本节不再复制一遍符号约定而是指过去。
```

**依据**: `02/式 -5 施加面与数据形态`（本件确立的边界："只到'施加是逐元素乘、order=0⇒m≡1、方差按 α² 变换'这一层。m(x,y) 的基函数、中心化规范式与取负号**不在本件**（其唯一载体是代码注释、科学侧无一条式子）"）；`01/C19`（m(x,y) 的式子本身在科学侧零承载，拟合侧与施加侧共用同一份内联实现 ⇒ 互为验证、无第三方期望）；`03/㉕`（最后才动的科学口径第 1 条：把规范式写进 PHOTOMETRY.md §16.1 本体）。

**同句还出现在**: `docs/algorithms/PHOTOMETRIC_FIT.md:59` ("scale=10^(−location)") —— 该处虽写 scale，但未涉及 `m(x,y)` 的显式规范；需在本件基础上再补 §16.1 的具体章节编号引向。当前状态为**过渡期**：`03/㉕` 完成后 PHOTOMETRY.md 的§16.1 将正式包含规范式，本文此条即完成使命。⇒ 见 05 §7 G-25。

---

## 三、乱（同一事实在多处各说一种/上下级重复正本/层级倒置）

> 本档条目持续追加中

### DOC-CONFUSE-001 返回码语义的合同文字错

**被订正文件**: `docs/science/PHOTOMETRY.md:218`  
**内容锚**: "退化 ⇒ scale=1.0/fit_used=0" 的判词  

**现状原文**:
```text
| `\|r_consistent\|<3` | NO_DATA 拟合失败：`scale=1.0`、`fit_used=0`、`sigma_residual=0`，不写标度 |
```

**订正后完整文字**:
```text
| `\|r_consistent\|<3` | **NO_DATA 拟合失败但返回码 rc=0**：`scale=1.0`、`fit_used=0`、`sigma_residual=0`；**下游必须检查** `out_n_matched=0 ∧ fit_ok=false` 才判定为未产出标度，不得仅凭 `rc==0` 就认为「已定标」（合同头注释 `photometric_calib.h:102/:154` 曾错误声称 −3 = "无 Gaia/PSF 星（退化 scale=1.0）"，该误读历史上发生过一次；现行有效口径见 `PHOTOMETRIC_FIT.md:266` §13.4 F5："退化输入矩阵 → scale=1.0、rc=0、records reject_reason 显式"）。 |
```

**依据**: `01/C14`（合同与实现口径成批不同源：`photometric_calib.h:102/:154` 说−3="无 Gaia/PSF 星（退化 scale=1.0）"，实现对该条件返回 0；同一"尺寸无效"在两族入口分别取−2 与−1；`−3` 在头/README/ALG 三处给出互斥语义且头内一句自相冲突）；`02/N-21`（返回码合同：`退化⇒rc=0＋out_n_matched=0` 是**实现合规、合同文字错**的一侧；修 C14 时改的是 `photometric_calib.h`/`README`/注释三处文字，**不得**反过来改实现返回−3）；`docs/algorithms/PHOTOMETRIC_FIT.md:64` ("`\|r_consistent\|<3` ⇒ NO_DATA 拟合失败：`scale=1.0`、`fit_used=0`") —— ALG 文档此处已接近正确但未强调 `rc=0` 的歧义风险，需补全。

**同句还出现在**: 
1. `docs/contracts/DATA_SEMANTICS.md` §14.4（返回码表 — 待核是否有误述）；
2. `lib/algorithms/photometry/cpp/include/photometric_calib.h:102,154`（头注释 — 已裁为错的一侧，但尚未删除或修正）；
3. `docs/traceability/TRACEABILITY_MATRIX.csv:22`（追溯行 — 待核是否沿用旧注释）。
⇒ 必须在**所有**引用面统一口径，否则读者会按错误注释理解实现行为。

---

### DOC-CONFUSE-002 sigma_residual 的 σ=0 双义

**被订正文件**: `docs/algorithms/PHOTOMETRIC_FIT.md:65-66`  
**内容锚**: "`|r_inliers|<2 ⇒ sigma_residual=0（不可估计）`" 与"`S=0 且 \|r_consistent\|≥3` 可达"  

**现状原文**（行 65-66）:
```text
| `\|r_inliers\|<2` | `sigma_residual=0`（不可估计） |
| `S=0` 且 `\|r_consistent\|≥3` | 可达：n=3 时两颗星 `r` 逐位相等即 `MAD=0`（合成实验实测）；此时 `location=median(r)`、`scale` 照常发布、`sigma_residual=0` |
```

**订正后完整文字**:
```text
| `\|r_inliers\|<2` | `sigma_residual=0`（**不可估计**） |
| `S=0` 且 `\|r_consistent\|≥3` | 可达：n=3 时两颗星 `r` 逐位相等即 `MAD=0`（合成实验实测）；此时 `location=median(r)`、`scale` 照常发布、`sigma_residual=0`（**实测零散度**） |
```

**订正说明**: 在两行之间插入**具名区分**——前者是「不可估计」，后者是「实测零散度」。当前 PHOTOMETRY.md:191 把两者混为一谈（只写"否则 `sigma_residual=0`"），导致产品面无迹可循。ALG 文档虽在行 69 给出"S=0 分支的可达性证据"，但**未在表格主表中作具名区分**。本订正只到 ALG 层面；PHOTOMETRY.md 需同步增补行 191 的完整描述（见 DOC-ERR-001）。

**依据**: `02/式 -4 精度承载量`（"不可估计"当前被编码成 0.0，与"实测零散度"不可区分——这是判据缺陷，不是本式的错）；`01/A2`（精度承载量的"不可估计"与"实测零离散度"在产品面不可区分）。

**同句还出现在**: `docs/science/PHOTOMETRY.md:191`（正文只写"否则 `sigma_residual=0`"，未区分两种含义）；`docs/contracts/DATA_SEMANTICS.md` 的产品字段表 — 待核是否在 `sigma_residual` 行有"可估计标志"字段的对应声明。⇒ 见 05 §7 G-12/G-13。

---

## 四、自报与互指清单

### 本件产出统计

| 项目 | 数量 |
|---|---|
| 01 缺陷清单共 N 条 | **106**（A 组 13 条 / B 组 30 条 / C 组 19 条 / D 组 9 条 + 4 个登记面结构性问题 + 通读批次相关项） |
| 05 正向规格给出正向约束的 M 条 | **17**（§1 接线 20 条 W-n＋W0-n 中的 17 条可执行；§2 配置键 16 条 C-1..C-16；§3A 算法步骤 A-1..A-13 中的 9 条有明确判据） |
| 05 不需规格的 K 条（逐条编号） | **N-1..N-25 共 25 条** "不要动" 清单不在 05 内单独成节；其余 S3 级注释/登记面失真按 01 档位不要求 05 补实现约束 |
| 06 本文共 P 条 | **7** |
| ├─ 错 | **3**（DOC-ERR-001/002/003） |
| ├─ 缺 | **3**（DOC-MISS-001/002/003） |
| └─ 乱 | **2**（DOC-CONFUSE-001/002） |

> 注：01 计数含全部 A/B/C/D 四组；06 只记录**需订正文本**的条目（S3 级纯注释卫生、无产品语义影响的不计入）。

### 与 05 的互指索引

| 06 条目 | ⇒ 对应 05 §n | 说明 |
|---|---|---|
| DOC-ERR-001 `sigma_residual` 双义 | 05 §7 G-12/G-13 | 三件套（表示/门/报告）的具面值与哨兵方案 |
| DOC-ERR-002 `match_radius_px` 适用域 | 05 §4 K-34 | 像素域常数及其适用域推导 |
| DOC-ERR-003 旧 SNR 式残留 | 05 §7 G-25 | 注释与唯一消费式同源锁定 |
| DOC-MISS-001 Tukey/MAD 书目锚 | 05 §4 K-5/K-6 | 书目标注四选一与出处锚格式 |
| DOC-MISS-002 证据路径悬空 | 05 §8 G-30 | run/回收后的跟踪副本锚 |
| DOC-MISS-003 m(x,y) 规范式零条文 | 05 §7 G-25 | §16.1 写定后由代码引用 |
| DOC-CONFUSE-001 返回码语义错 | 05 §6 E-1/E-2 | 退化分支的具名状态与 rc 值分离 |
| DOC-CONFUSE-002 σ=0 双义分档 | 05 §7 G-12/G-13 | 两种 0 的含义在 ALG 主表区分 |

> 说明：DOC-CONFUSE-001 与 DOC-CONFUSE-002 虽以「乱」归类，但其本质是**与实现事实/合同文字矛盾**，亦可视作「错」的亚型。为避免重复计数，按其**表现形态**归类为「乱」。

---

（END OF 06 — 本件至此完成第 1 回合内容落盘，后续持续追加至全量 P 条）
