# 缺陷清单 · ①测光星等坐标系

档位口径：`S1` = 生产入口到产品字节的逐跳全部写到且有原文实据；`S2` = 事实成立但缺某一跳（缺哪一跳在末列写明）；
`S3` = 后果限于诊断、登记面或注释，不改产品字节。每条末列是取证成稿锚（`通读-CR-<N>` 的 finding 编号，
`通读-CR-01 §2.5 :276 行` 一形式指逐数值处置表行；`测光 DEV-<NN>` 指 `02_科学/测光链路核验报告.md` §5 的偏差条目）。

触发路径一律写到产品字节；写不到就用「缺：…」明写缺的那一跳，不假装闭合。

---

## A. 判据不读真实对象（这类不修，后面每一条都无法自证）

| 编号 | 事实 | 档 | 触发路径（到产品字节逐跳） | 成稿锚 |
|---|---|---|---|---|
| A1 | 「声明的通带名 ↔ `FILTER` 关键字解析出的库键」对账门在生产配置下恒不触发：两个操作数取自同一个键。`module_adapters.cpp:5526-5527` 在未写 `photometry.fit.filter` 时把 `filter_passband` 同时喂给 `req.filter_name` 与 `req.declared_filter_passband`；`filter_key = map_filter_name(req.filter_name)`，而 45 个库键逐键复算全部原样返回自身 ⇒ 判据式退化为 `X != X` | S1 | `normalize` → `module_adapters.cpp:5526-5527` → `:5698`（`freq.filter_name`）＋`:5704`（`freq.declared_filter_passband`）→ `frame_photometry_fit.cpp:92`（`map_filter_name`）→ `:102-103`（恒假 ⇒ 永不 `mark_frame_fail`）→ 该帧的 `k_photo`/`zero_point_mag`/`sigma_residual` 按未经对账的通带入 `p1_phot.json` 与 `photoapplied_*` 像素 | 通读-CR-01 CR-01-07 |
| A2 | 精度承载量 `sigma_residual` 的"不可估计"与"实测零离散度"在产品面不可区分，且唯一消费它的门只卡上界与有限性：`MAD(r)=0 ⇒ S=0 ⇒` 无条件全收为内点 `⇒ mad_in=0 ⇒ sigma_residual=0 ∧ fit_ok=true`。可达路径是**多数票严格相等**，不是样本不够；报告级 `status` 只看 `fit_used>0` | S1 | 合成最小构型 `{0.30, 0.30, 8.00}`：`star_matcher.cpp:549-552`（`S<=0` 跳 IRLS）→ `:600-608`（`S<=0.0 ||` 短路 ⇒ 2 dex 离群星进内点）→ `:616-626`（`MAD(r_inliers)=0`）→ `module_adapters.cpp:5747-5748` 的 `P1_PHOT_MAX_SIGMA_DEX=1.0` 上界门放行 → `:6240` 落盘 `sigma_residual=0.0` → 下游 `snr_estimator.cpp:148,:322,:596` 把该帧整幅 SNR 填 `1.0`（一个看起来合法的假 SNR）→ 进阶段二 | 测光 DEV-07／DEV-08；通读-CR-02 CR-02-16；通读-CR-04 CR-04-29 |
| A3 | 冻结门 ②（`|r_inliers|≥2` 才估 `sigma_residual`）在实现里对 `n≥3` 数学上不可触发（接受阈 `|r−loc| < 6.945·MAD`，MAD 的半数性质给出 `inliers ≥ ⌈n/2⌉ ≥ 2`），而声称锁它的测试用例实际在门 ① 前就被星等预过滤截住：把门 ② 删成"无条件估计"，该测试文件 12 条断言逐条不变 | S2 | 缺：门 ② 无可达输入，因此写不出"当前生产输入下产物已错值"的跳；成立的是 `eng/ci/checks.json:3701,3905-3923` 采集的 `p1phot_fixgates` 绿灯被当作"两条冻结门都有锁"的验收证据 | 通读-CR-02 CR-02-9／CR-02-10；打回报告 B §5.5.3 |
| A4 | 恒真断言占据 PASS 名额：`diag.rejected_quality >= 0`（实现三处赋值全为非负和，无可达反例）却标称为"nullptr 不做质量过滤"的证据；`static_assert(&pc_abi_probe_simple != nullptr)` 取的是变量地址、`reinterpret_cast<void*>(&fn)` 对任意形参表都合法 ⇒ 该 TU 声称的"6 个 C 导出不得新增形参"在编译期没有任何表达 | S3 | 不进产品字节；失真是 `p1phot_fixgates` 的 12 条 PASS 里 2 条无证据资格（形参锁实际由 `CHK-API-CONTRACTS` + `docs/contracts/API_CONTRACTS.csv:288` 承担） | 通读-CR-02 CR-02-11／CR-02-13 |
| A5 | 登记为 SCI-PHOT-001「VERIFIED」证据的 TEST-SPEC-001 宿主 `test_spectrum_integrator.cpp` 全文零断言（只 `printf` 出 JSON 并 `return 0`），且它调用的 `compute_f_syn`/`compute_f_syn_cached` 被模块自己的头标为「非生产通道」（含 `10^(−0.4·magG)` 历史乘子）；真断言宿主在 `p1phot_tests_units.cpp:361,383`（配 `p1phot_oracle.hpp:308`）却没回写登记面 | S2 | 缺：该文件不在构建图，不进产品；成立的是 `docs/TRACEABILITY.csv:22` 与 `docs/contracts/API_CONTRACTS.csv:284-289` 把该科学条款记成已验证，ACCEPTANCE L1/L2 按登记面会当已覆盖跳过 | 通读-CR-07 CR-07-12 |
| A6 | 「akima n=2 边界回归」唯一实际驱动者路径已失效且失败被吞：该测试不在任何 `*.txt`/`*.cmake`（零引用）、`eng/ci/checks.json` 对 `spectrum_integrator` 零命中，`eng/tools/quality/v19r3_sanitizer.sh:144-146` 用的是改名前的 `photometric_calib/cpp/…` 路径并以 `|| {…}` 吞掉编译失败；Python 侧 `_EXE_PATH` 带 `.exe` 后缀 ⇒ 在 Linux 节点恒 `FileNotFoundError` 且无采集面记红 | S2 | 缺：构建图外，不产错值；成立的是全部小样本分支（n=2、奇区间末 3 用 3/8、n==1 退梯形）在机器面无锁 ⇒ 与 C1 的 Simpson 缺陷长期不可见直接相关 | 通读-CR-07 CR-07-13 |
| A7 | 性能哨兵方向相反：`P1PHOT_CHECK(cs, paritym >= 0.25, "perf_trend_maxw")`，而 `paritym = T(max线程)/T(1线程)`，`>1` 才是负加速 ⇒ 任何变慢恒绿，并行越好（ratio<0.25）越容易判红；同文件把 `rc`、匹配星数 `nm`、`PhotometricDiag` 全部丢弃，"三个 perf 哨兵"可由一次根本没做校正的运行满足 | S2 | 缺：非生产数据路径；成立的是 `ctest → p1phot_performance`（注册 `CMakeLists.txt:137`）对外主张的"无负加速趋势"不成立 | 通读-CR-04 CR-04-1／CR-04-2 |
| A8 | 该测试族自称的"注入已知错必红"证据不成立：自检把"子进程非零退出"当作"注入使其失败"的充分条件，`fork` 失败／`execve("/proc/self/exe")` 失败／被信号杀（`-1`）／未知组名全部返回 127 或 `-1` 也满足判据；故障注入分支还短路掉了被测断言的求值；未知或拼错的组名一律静默 `rc=0` | S2 | 缺：不进产品；成立的是 `p1phot_selfcheck` 这一"门非恒真"的证据件本身 fail-open，本包 §03 里所有以它为模板的"能红"证据都要按新判据重做 | 通读-CR-04 CR-04-9；通读-CR-03 CR-03-2-01／CR-03-2-02 |
| A9 | 鲁棒性对外主张被测成了与被测无关的量：注入 20% 离群后"位置估计变化 < 0.1 dex"被实现为"oracle 自身 location 的幅值 < 0.1"（给定幅值自带约十倍余量，生产算什么都不会红）；同一组里 `sip_order` 恒为 0，`oracle_f_syn_mag` 与 SIP 求值在本批零调用 | S2 | 缺：oracle 不在生产调用路径；成立的是 SCI §11 那条鲁棒性主张当前无判据承载 | 通读-CR-03 CR-03-6-01／CR-03-6-03；打回报告 B §5.6.3 |
| A10 | 断言用组内共享且从不复位的 `nm/sc/sg`，前一个用例写进的恒等值可被后一个用例当期望满足；被比较量为退化值（`scale` 或 `sigma` 为 0）时断言恒真；"尺寸不等即静默跳过像素比较"的分支同样可恒绿 | S2 | 缺：不进产品；成立的是"实现不再写回退化恒等值"这类回归不可红 | 通读-CR-03 CR-03-5-01／CR-03-3-02／CR-03-3-04 |
| A11 | 号称"独立重写"的参照件与被测同源：`oracle_simpson`、`fsyn_astrocs.py` 与生产实现三处同构复制同一处错误；`oracle_f_syn_mag` 复刻正本明确排除的 `10^(−0.4·G)` 通道；oracle 在三处把口径来源交给被测（偶数中位取法、背景上中位、TAN 退化阈 `1e-12` 与 `1e6` 哨兵），文档只就 I2 一条披露 | S2 | 缺：oracle 不进产品；成立的是"独立佐证"这一证据类型在本链的 F_syn 族上不提供任何独立信息 ⇒ C1（奇区间端点项）这类错误只能靠闭式族抓 | 通读-CR-06 CR-06-07；通读-CR-03 CR-03-1-01／CR-03-1-02 |
| A12 | 绝对刻度锚的证据资格被说过：`median`＋MAD、按 G 分箱的那套锚对其声明的 **level** 完全判别，对**色项型**缺陷无资格（解耦样本上读出塌到 2.45%，读数随样本 CMD 结构单调变化）；而唯一在跟踪面上的 1050 星对拍表两臂同取绝对浮点谱、不经容器解码 ⇒ 按其建门恒绿 | S2 | 缺：该锚是文档证据口径，仓内无任何 ctest/CI 门实现它（`git grep` 0 命中）⇒ 不是产品门漏判，而是 §02 里"已验证"的资格范围要按此收窄 | 测光 DEV-02／DEV-03 |
| A13 | 两条配置溯源门本身判据退化：CFG002 `check_09_defaults_anchor_survival` 靠 token 前 2 字巧合放行（`ma` ⊂ `star_matcher`），对测光四条既没判红也没走豁免；CFG001 `KEY_ANCHORS` 只覆盖 6 条测光键里的 1 条，且硬写的期望锚（`:30,"3.0 mag"`）静态可判必红；全仓无"注册表值 == 代码字面量"的等值核对（仅 `calibration.master_flat_median_range` 有） | S2 | 缺：不进产品；成立的是"空头溯源账户"可长期在绿灯下存续，本包 §03 的登记订正步骤必须连这两条门一起改，否则改完仍判不出 | 测光 DEV-06 |

---

## B. 实现缺失或与设计相反（判据可信之后补）

| 编号 | 事实 | 档 | 触发路径（到产品字节逐跳） | 成稿锚 |
|---|---|---|---|---|
| B1 | 配置合同列为"强制"的解析器第三条责任——`channel` 非空校验——在全仓唯一实现里零落地：`channel` 只作为诊断字符串的一部分出现，`load_curve` 四步无一步取它；库内 `channel==""` 的曲线实数 7 条（`Johnson I/U`、`SDSS u/g/r/i/z`），且这 7 条同时是三份 phase_config schema 的合法枚举成员 | S1 | CLI `normalize` → `module_adapters.cpp`（p1_photometry descriptor，`docs/traceability/TRACEABILITY_MATRIX.csv:9`）→ `frame_photometry_fit.cpp:92` → `filter_curve_json.h:374`（库键原样返回）→ `:444 load_curve` → `:472 return kOk` → 一条未声明通带类别的曲线完成 `F_syn` 积分并写进像素；同时 `p1_phot.json` 的 `passband_identity.gate` 仍写 `enforced` | 通读-CR-01 CR-01-01 |
| B2 | `map_filter_name` 的比较器用 `std::equal` 取 first range 长度 ⇒ 从不核对长度：输入为空串恒真、是表内项前缀即真 ⇒ `FILTER` 关键字缺失即静默解析成 `"Baader R"`，`"Gree"→Baader G`、`"H-al"→Baader 7nm H-alpha` 等按表内首条"包住它"的目命中；上游 `orchestrator.cpp:2629-2633` 无"缺失即拒"分支 | S1 | legacy 编排路径：FITS 无 `FILTER` → `orchestrator.cpp:2631`（`filter_str` 保持 `""`）→ `:2633` → `filter_curve_json.h:362` → `:472 kOk` → `:2660` 之后按红通带装配 `F_syn` → 该帧 `k_photo`/`zero_point_mag`/`sigma_residual` 全部错通带出值。**适用域差异**：新 `normalize` 路径在 `frame_photometry_fit.h:63` 处对空 `FILTER` 是 fail-closed 的，只是诊断词错报成 mismatch | 通读-CR-01 CR-01-02 |
| B3 | 单帧的曲线装载失败一律记 `kEnvironment` ⇒ 一帧坏输入中止整轮，与已裁的"帧间独立"相反，且诊断词把归因引向配置。AIO 在无 `FILTER` 时写字面量 `"Unknown"` → `map_filter_name` 原样返回 → `kCurveNotFound` → `failure_scope=kEnvironment` → `fail_global("PHOT_FIT_ENVIRONMENT")` | S1 | FITS 缺 `FILTER` → `aio_fits.cpp:482` → `frame_photometry_fit.cpp:92`→`:114-117`→`:133` → `module_adapters.cpp:5732-5734` 全局失败 ⇒ 49 帧规模里任一帧不规范即整节点无 `p1_phot.json`、`photometry_applied` 全空。缺：跟踪面内没有"确有真实帧缺 `FILTER`"的帧清单（未升格为已发生事实） | 通读-CR-01 CR-01-09 |
| B4 | QE 曲线"配置了但解析失败"只写 stderr 后按 `Q(λ)≡1` 继续，接口与产品面没有任何承载该事实的字段（`FramePhotFitResult` 无 `qe_*`；`passband_identity` 落盘清单只有滤镜曲线的 `curve_source`），注释把判定权交给调用方但该判定权在接口上不可执行 | S1 | `normalize` → `module_adapters.cpp:5699-5701`（填 `qe_json/qe_name`）→ `frame_photometry_fit.cpp:145-157`（clear + WARNING）→ `:217` C 入口传 `nullptr` → `:304 out.k_photo` → `:6179` 落盘 ⇒ `k_photo`/`ZP_syn` 按缺 Q 因子的口径出值。方向：`Q≡1` 使 `F_syn` 偏大、`r` 整体下移、被 `location` 吸收为**零点平移**，逐星散度门不亮 | 通读-CR-01 CR-01-10 |
| B5 | 同一 ABI 面上 `PhotometricDiag` 20 个成员无 QE 在场/缺席标志、返回码只有 0/−1/−2/−3/−4，`Q≡1` 走 rc=0；正本 §2a.4 要求"没配 QE"与"QE 已计入"下游可区分，并给出 `Q≡1` 的跨星散度 `0.0082 mag` 不被零点吸收、`sigma_mag` 含 Q 后 `+11.6%/+16.0%` | S2 | 缺：本仓生产路径由上一层 `frame_photometry_fit` 满足；失真的是对外冻结 ABI 面与跨帧 `sigma_residual` 比较的可复核性 | 通读-CR-05 CR-05-8 |
| B6 | 实现向 `PcMatchRecord.reject_reason` 写出合同域外值 **7**（饱和星），`photometric_calib.h:57-58`、`DATA_SEMANTICS.md:691`、`PUBLIC_API.md:513` 三处都钉 0..6，`pc_api.cpp:1157-1163` 原值透传无校验 | S2 | 缺：生产 `frame_photometry_fit.cpp:207-214,:363-364` 只消费 `status`/`residual`，`module_adapters` 内 `reject_reason` 零命中；唯一写该块的是 orchestrator 通道（`docs/architecture/execution_inventory.csv:4` `:2879`），该通道无 CLI 调用点且其 DLL 未编入根构建（`CMakeLists.txt:760`） | 通读-CR-05 CR-05-6；打回报告 B §5.5.2 |
| B7 | 饱和/质量剔除的逐星归因在生产产品里被无条件覆写成 3（"无效通量"）：`:433` 写 7 后，`:452-464` 只用 `quality_pass` 构造 `valid_idx`，`:504-508` 对所有 `!in_valid` 一律写 3 ⇒ **饱和类别在产品里永不可达**；退化早退分支 `:468-470` 同样覆写 | S1 | `module_adapters.cpp:5689`（`freq.psf_quality` 取 `p1_sources[].quality` 的饱和位）→ `frame_photometry_fit.h:49` → `frame_photometry_fit.cpp:217 pc_calibrate_simple_with_gaia_f64_v2_qf` → `pc_api.cpp:1119-1121 cleanAndScale(...)` → `star_matcher.cpp:430`（命中位）→ `:433`（写 7）→ `:507`（覆写为 3）→ `pc_api.cpp:1157/:1163 rec.reject_reason` → `p1_phot` 逐星记录。现有验证件 `p1_qf_oracle.cpp:137` 只验"该星被排除"，不验归因码 ⇒ 该缺陷能长期绿着 | 通读-CR-06 CR-06-12 |
| B8 | `rejected_quality` 是四类剔除（质量位＋通量无效＋星等不一致＋IRLS 离群）的混合计数，而目录合同 `DATA_ARTIFACTS.md:21` 仍按"饱和拒 = rejected_quality"使用该列（`DISP-PHOT-006` 已在 `DATA_SEMANTICS.md:690` 登记为混计）⇒ 同一字段两处对外表述互斥且都未标退役 | S2 | 缺：不改 scale/像素字节；改的是 `photo_stats.REJECTED_QUALITY` 与 `p1_phot.rejected_quality` 的读法（`orchestrator.cpp:2965/2998/3054`） | 通读-CR-06 CR-06-13 |
| B9 | 空间增益阶数同一键四面取值（结构体默认 0／实验文档登记默认 1／同一文档"验收前保持 0"／生产回退常量 **2**），且该键与 6 个降级门键在 `defaults.json`、normalize schema、`config_registry.json` **零登记** ⇒ 配置面表达不了这个旋钮，出厂按文档自己实测"分半不可复现（corr 0.929/p10 0.41）、聚集分布下退化到 24%、N=100 时 13.18% 比不做更差"的那一档乘到整帧像素上 | S1 | `module_adapters.cpp:5471 constexpr int kDefaultSpatialGainOrder = 2` → `:5473-5476`（配置无键 ⇒ 不覆盖，仅夹到 [0,2]）→ `:5708 freq.spatial_gain_order` → `frame_photometry_fit.cpp:354`（`>0` 才走）→ `:383 fit_spatial_gain` → `spatial_gain.cpp:327`（`order_req=2`）→ `:457` 阶梯首尝 order 2 → `:530/:548` 发布 `f.order=2` 与 coef → `module_adapters.cpp:6064-6073 apply_photometry_spatial` → 像素 | 通读-CR-06 CR-06-01；通读-CR-01 CR-01-13 |
| B10 | 阶段 3（空间增益）没有实现锚文档 §2.3 点名的逆方差权重：`SpatialGainSample` 无 σ 通道、`build_weighted_H` 的 `w` 只含 Tukey 项，而解析噪声底 `S·√(B̃ᵀH⁻¹B̃)` 按同一个**等权 H** 导出 ⇒ 按 Gauss–Markov，等权解系数方差 ≥ 逆方差解 ⇒ `noise_floor_dex` 系统性偏小，"场幅度 > 噪声底"这一类比较一律偏绿 | S2 | 触发链同 B9（同一施加路径）。缺：真实数据上逐星 σ_r 动态范围的实跑证据，只做到估计量层面的推导 | 通读-CR-06 CR-06-04 |
| B11 | 复化 Simpson 的奇区间分支在 `n_int==3` 时多算一项 `2·y[0]·h/3`（`n_13=0` 仍按"两端点各一权重"计入），积分恒偏大：`h=1, y=[1,1,1,1]` 真值 3、代码 3.666667（+22.2%）；`y=[0,1,2,3]` 恰为真值 ⇒ 误差正比于左端点取值 | S2 | 缺：当前生产网格 343 点/342 区间（偶）⇒ 不触发；触发面是非生产 `compute_f_syn`（1nm 网格、交集宽 ∈[3.5,4.5)nm）与任何 4 点谱网格，而 `frame_photometry_fit.cpp:185` 只校验 `wl_count<=0`/`wl_step<=0`，**无最少点数守卫**，网格宽度由外部 DB 决定。方向链一旦触发即 `F_syn↑ ⇒ r↓ ⇒ k_photo↑ ⇒` 像素乘数偏大 | 通读-CR-06 CR-06-06 |
| B12 | FOV 半径"钳位 [1,10] 度"是**条件**钳位：只有 `fov<=0` 或 `fov>=30` 才钳，(0,1) 与 (10,30) 完全不钳；且奇异/缺失 CD 矩阵被判成"像素尺度=0"⇒ 命中该分支 ⇒ 被抬成 1.0 度锥搜继续取 Gaia 星拟合，不报错。该半径同时是 `ZP_syn` 星族的取样半径 | S2 | `frame_photometry_fit.cpp:166-174` → `:250-252`（ZP 锥搜用同一 `fov_radius_deg`）→ `:273` ZP_syn → 落盘 `zero_point_*`。缺：现成"CD 奇异仍出标度"的真实帧样件。方向：半径变大 ⇒ 候选星变多 ⇒ `|r_consistent|≥3` 更易满足 ⇒ 偏绿 | 通读-CR-01 CR-01-11 |
| B13 | `kMinFitStars` 把条文的 `|r_consistent|≥3` 换成 `n_matched≥3`（头文件自述该计数是 **IRLS 后 inliers**），并新增一条 §16.5 明文禁止的"星数准入"；同一常量又被复用作 `ZP_syn` 的样本下限（无任何条文给 ZP 设下限） | S2 | `frame_photometry_fit.cpp:332`（rc=−6、`failure_scope=kFrame`、`k_photo=1.0`）→ 该帧从 `photometry_applied` 面缺失 ⇒ 跨帧 `sigma_residual` 分布被"低 inlier 帧被剔除"的选择效应改写。方向：inliers ≤ consistent ⇒ 此门只会比 §4 更严（偏红），不是误绿 | 通读-CR-01 CR-01-12 |
| B14 | 直通导出的入参 `gaia_fsyn` 未声明是哪一种 `F_syn` 约定（无单位、无"必须与 §2a 同式"约束），而零点因子对约定的选择逐星敏感：喂含 `10^(−0.4G)` 的历史口径时偏差随星等变化、**不是**能被零点吸收的常数；同一入口的 `out_scale_factor` 单位合同要求"显式声明参考通量单位后才可称无量纲" | S2 | 缺：未找到这两个导出的现存外部调用方。风险面是对外冻结 ABI（`§13.5` 登记为 ABI 兼容通道保留，生产主路径是 v2/_f64_v2） | 通读-CR-05 CR-05-7 |
| B15 | 生产 `pc_api` 内部对光谱格长度不一致只告警，积分器对坏输入逐星返 0 ⇒ 网格不一致时**每一颗星**被拒：`n_valid_fsyn=0` 不被任何判据拦，流程继续、`n_matched=0`、`scale` 保持初值 1.0、`return 0`（"成功"）；诊断字段 `valid_fsyn` 无任何被要求查看它的消费者 | S2 | 缺："只看 rc 的生产消费者"证据。生产侧由 `frame_photometry_fit.h:110-124` 的 `fit_ok`（`rc==0 ∧ n_matched≥kMinFitStars ∧ scale 有限且>0`）兜住并转 rc=−6 ⇒ 当前产品字节不受影响；`normalize → module_adapters.cpp:5716 → frame_photometry_fit.cpp:217 → pc_api.cpp:1315 → :1073/:1170` 逐跳已写到 | 通读-CR-04 CR-04-24 |
| B16 | `apply_photometry_spatial` 声明 `−6` = "场的定标量非有限或 <=0"，实际逐列判 `isfinite` 时**漏掉 `center[]` 一列**；`center=NaN` 时 `photo_spatial_log10_m` 把每个像素写成 NaN 而函数返回 0（成功） | S2 | `module_adapters.cpp:6077-6088`（只在 `arc != 0` 报错）→ `arc==0` 时 `p1_write_fits_atomic` 写出全 NaN 的 `photoapplied_*` 并计入 `photometry_applied` → 下游 drizzle 以 `isfinite` 跳过 ⇒ 该帧"消失"而无红灯。缺：现网不可达——唯一生产者 `spatial_gain.cpp:500-536` 在发布前于整幅帧网格求 `amax=max|log10 m|` 并对非有限 `continue`，center 为 NaN 时必被该守卫命中 | 通读-CR-55 CR-55-3 |
| B17 | `photo_spatial_nterm` 把 `order>=2` 一律映射成 5 项、`order=3` 按二次式求值并返回成功，与同文件 :70 注释"阶 >=3 不启用"相反；`coef[5]` 也装不下三阶的 10 个系数，无越界也无拒绝 | S3 | 当前被两处独立夹逼挡住（`module_adapters.cpp:5476`、`spatial_gain.cpp:327`）。后果是 provenance 记的 `order` 与实际求值阶可不一致（`module_adapters.cpp:5407` 把 `sc.m_order` 原样写进 `spatial_gain` JSON） | 通读-CR-55 CR-55-1 |
| B18 | 施加面对"越域坐标"零判定：`(x,y)` 取输出图像自身的 0-based 下标，与场的 `x_ref/x_scale` 无一致性校验，`|x̃|>1` 的外推不受约束（二次项 ⇒ m 可任意大），而幅度守卫只在拟合帧网格上评估过；场的适用域只写在注释里 | S3 | 缺：触发要求"施加图像尺寸 ≠ 拟合帧尺寸"，现网 `module_adapters.cpp:6041 find_scale(lp)` 读同一 `p1_calibrated_path` ⇒ 尺寸同源。不可达时仍使"m(x,y) 只在被估计过的视场内有效"这一边界无法被机器守住 | 通读-CR-55 CR-55-4 |
| B19 | `matchAndClean` 是 public 的"一站式"入口，签名结构上没有质量位参数（兼容重载以 `nullptr` identity 转调 ⇒ `:429` 判据永假），用即绕过 §4/§10 饱和门且无任何编译期或运行期提示 | S3 | 缺：全仓零调用者（`git grep` 只命中本文件与头）。一旦有调用者即静默产出未做饱和过滤的 `k_photo` | 通读-CR-06 CR-06-16 |
| B20 | 产品自述字段 `filter_keyword` 装的是配置值（`fit.filter`/`filter_passband`），不是帧头 `FILTER` 关键字：新生产路径从不读帧头 KV（`git grep '"FILTER"' -- lib/infrastructure/scheduler` 零命中）⇒ §2a.7 声称的"配置声明与实际曲线在产品里可独立核对"在该项上不成立 | S2 | `module_adapters.cpp:5526-5527 → :5825 → :6240 {"filter_keyword", …}` 落 `p1_phot.json`。不改产物数值，失真的是对外自述面（与 A1 同根因、修法不同：这条改落盘来源，A1 改判据输入） | 通读-CR-01 CR-01-08 |
| B21 | 头声明 `filter_curve_name` 是"曲线对象自述值、不是把请求名回抄"，实现正是回抄：`load_curve` 成功路径丢弃 `CurveIdentity`，`object_name` 对外不可得 ⇒ 产品里 `curve_name` 与 `resolved_library_key` 结构性恒等。身份核对本身**确实在装配期执行过**，失真的是"把证据打印成自证" | S2 | `frame_photometry_fit.cpp:137-138` → `module_adapters.cpp:5827 → :6241 {"curve_name", …}`。后果：一旦身份门被绕过（C 节的缺字段/缺段分支，或将来有人复用 `extract_curve_arrays` 而不走门），产品面无痕迹 | 通读-CR-01 CR-01-14 |
| B22 | 身份核对"四项全查／缺字段即 fail-closed"被两处短路：`n_points` 字段缺失时第 ② 项恒成立（`has_n_points &&` 前置），provenance 段整体缺失时 ③④ 两项被跳过而 `id.ok=true`；同一种声明缺失按文件形态给两种判定。放大器是 `extract_curve_arrays` 的 `while (iss >> v)` 遇第一个不可解析 token 即停并静默丢弃其后所有元素 | S2／S3 | 缺：现用两份曲线文件 45/45 都带 `n_points` 与 provenance（本批全量复算）⇒ 触发需被手改/损坏的库文件；成立的是"门对被篡改库文件失效" | 通读-CR-01 CR-01-03／CR-01-04 |
| B23 | `kLengthMismatch` 是从未产生的死状态：`extract_curve_arrays` 把"数组缺失"与"两数组长度不等"折叠成同一个 `false`，唯一出口 `kArraysMissing` ⇒ 诊断菜单向调用方声明 7 种失败、实际只有 6 种，长度不一致被错报成数组缺失 | S3 | 不进产品字节；后果是定位库文件损坏时诊断词错指 | 通读-CR-01 CR-01-05 |
| B24 | 注释声明"T2/T3 Astrodon 暂用 Baader 曲线近似"，但该函数只看 FITS 串、结构上无法区分安装：`FILTER='H-alpha'` 无论装 Baader 7nm 还是 Astrodon 3nm 一律解析成 `"Baader 7nm H-alpha"`，且库内没有 Astrodon 窄带曲线；该近似在 `docs/`、`ASTROCS_DESIGN.md` 零提及，也未登记进 `KNOWN_LIMITATIONS` 或 GAP 清单 | S2 | 触发路径同 B1 的 normalize 链，需窄带真实数据。数值影响为二阶（连续谱定标星的带内通量近似 ∝ 带宽，该全局乘性因子又被零点吸收），**失真的是 §2a.7 要求的"产物声明实际使用的模型通带"**：声明出的是 Baader 7nm，帧的物理通带是 Astrodon 3nm，而 ①—④ 身份门只对库内声明负责 | 通读-CR-01 CR-01-06 |
| B25 | 非正本通量式 `∫S·T·Q·λdλ × 10^(−0.4·G)` 以两个公共函数存活，并被一条登记测试固化为 golden；`.cpp:279` 注释**虚报"被 OpenMP 并行循环调用"**，生产可达实为 0（生产四调用点全是 `compute_f_syn_cached_xpsd`：`pc_api.cpp:386,:796,:1076`、`frame_photometry_fit.cpp:268`）；`oracle_f_syn_mag` 与该注释同侧（见 A11） | S2 | 缺：产品数值风险（可达性已复算为 0）。真实缺陷是三条卫生问题：非正本口径公共函数被 golden 固化、注释虚报可达、文档机理句不可复算 | 测光 DEV-04；通读-CR-03 CR-03-1-02；通读-CR-07 CR-07-12 |
| B26 | `star_matcher.h:59` 把 `sigma_residual` 的对外消费公式写成 `SNR_phot = 1/(ln10×sigma_residual)`，而该式已被上位判为"非 SNR"（跨 3 个数量级），现行有效式是零点标准误 `1.253·sigma_residual/√N` 且必须除 √N | S3 | 无运行期效应（SNR 实现走 `snr_estimator.h:295` 的正确式）；后果是按该注释新写的消费方会造出跨数量级的假 SNR，属订正未贯穿，且牵动创新点②的对外主张 | 通读-CR-06 CR-06-18 |
| B27 | 测试桥把"容器自报"的谱网格写成三个死字面量（336/2/343 在批内四处独立重复），全套用例从不改动它，桩再把同一组字面量回吐给 `get_spectrum_params` ⇒ 设计点名的"网格与位深由容器自报而非硬编码"这条不变式零测试；闭式期望 `v2_fsyn_const()` 也写死积分端点而不引用自己的网格数组 | S2 | 缺：真实分片头就是 336/2/343，当前产物不错值。成立的是网格解析或 `wl_start + i*wl_step` 换算一旦回归（step 被当点数、`+336` 丢失），`F_syn` 在错网格上积分而测试仍全绿，正本"网格外不外推"的边界判据也失去检验 | 通读-CR-02 CR-02-3／CR-02-15 |
| B28 | 查询桩忽略全部查询条件 ⇒ 生产自适应星等锥搜的常走分支（首轮 `n_gaia>=2000` 即停、非默认网格）在整套用例里永不可达；夹具装配对"谱长度/条数不匹配"静默回退为常数谱，装配错误不会让用例失败 | S2／S3 | 缺：不进产品。后果是任何"已验证"的表述都要限定到"默认分支" | 通读-CR-02 CR-02-18／CR-02-4 |
| B29 | `fixture_f1/f2` 的格点上界 6×4=24 与 `n_star` 形参之间无任何校验 ⇒ perf 用例请求 48 实得 24，计时处按实际规模取值，报告标注的负载是真实负载的 2 倍 | S3 | 不进产品；perf 结论的适用负载被误述（`p1phot_tests_perf.cpp:51` vs `:71/:74`） | 通读-CR-02 CR-02-14；通读-CR-04 CR-04-3 |
| B30 | 非生产 `Photometer` 符号的四处口径失真：孔径误差式丢掉天空均值不确定度项 `n_ap²/n_sky·σ_sky²`（方向单调偏绿）；Poisson 项把 ADU 当 e⁻（隐含 gain=1，与本链"不建立增益标定"的合同口径同时成立）；同一"中位数"在本模块族内两套偶数约定且无条文裁定；孔径/天空环被图像边界裁切时仍返回 `valid=true` 且结果结构无"部分孔径"标志 | S3 | 缺：生产触发路径（`Photometer`/`photometer` 在 `module_adapters` 与 orchestrator 零命中，登记为待迁移符号 `DISP-PHOT-008`）。`docs/science/PHOTOMETRY.md:263` 还写着"无孔径背景扣除项"，与该实现做了背景扣除的事实相反 ⇒ 条文与实现互相指认 | 通读-CR-05 CR-05-2／CR-05-3／CR-05-4／CR-05-5 |

---

## C. 常数与锚的登记缺口（改文档面为主，但必须先于口径变更）

| 编号 | 事实 | 档 | 触发路径 / 缺哪一跳 | 成稿锚 |
|---|---|---|---|---|
| C1 | 六个测光常数的"权威载体"互指：同一文档 `:218` 指代码字面量、`:220` 指配置键；六键在 CI 基线里被自登为 `dead_config_key:photometry.*` ⇒ 改 `defaults.json` 的 `photometry.*` 不改变任何计算。逐键历史判定：4 键为锚漂移（曾成立、随文档重排失效），`tukey_c` 为**登记即错**（8/8 个历史版本从未成立）；4/6 回链行号指错（＋4/＋7/＋147/＋147）；`spatial_gain.cpp:40` 另有重复常数 `kTukeyC` | S2 | 不进产品字节（值本身正确，见 §02 B 组）；失真的是"可配"与"已溯源"两项对外表述。缺：两条门在 HEAD 的实际红绿状态未跑 | 测光 DEV-05；链路间口径对表 X6 |
| C2 | 空间增益的 IRLS 收敛门写死 `1e-9`，而它自称一致的锚、科学文档、登记配置三侧都是 `1e-6`；`4.685`、`50` 两个已登记为可配的标量常数在此写死（`git grep` 实测测光阶段不读这两个键）⇒ `photometry.irls_tolerance_dex` 等三键是空登记 | S3 | 方向：1e-9 更严 ⇒ 不反转判据方向，只让循环更常在 50 步上限处结束、发布未收敛到 1e-6 档以下的系数 ⇒ 影响乘到像素上的 `m(x,y)` 末位 | 通读-CR-06 CR-06-02 |
| C3 | 空间幅度硬界 `max|log10 m| ≤ 1.0 dex` 零条文锚，注释自认"约定值、不构成效应量门"；同一权威文档登记的真实污染幅度是 `0.1 dex` 量级 ⇒ 门比它点名的失效模式松约 10 倍，对该类失败恒不 binding；该值既不在 `defaults.json` 也不在任何 UNRESOLVED 台账（`git grep` 0 命中），而 §16.1④/§8 未写"越界 ⇒ 该帧 fail" | S2 | 生产触发链同 B9；缺："0.1–1.0 dex 的场确已被放行"的跟踪面证据件 | 测光 DEV-13；通读-CR-06 CR-06-03 |
| C4 | `mag_min=6.0 / mag_max=16.0` 无任何条文锚，且两个消费面口径不同：拟合端被登记为"入参静默失效"（`DISP-PHOT-005`，被自适应 `mag_max_arr{12..16}` 覆盖），ZP_syn 端**直接生效** ⇒ `F_syn` 的定标星族与 `ZP_syn` 的中位数星族由两套不同星等窗选出，两处对外都称"锥形搜索星族" | S2 | 缺：`pc_api.cpp` 自适应档位选择逻辑的逐行确认（域外）与"亮端 G≈6 的 XPSD 质量是否可用"。后果是跨帧公共锚 `ZP_syn`（下游 `frame_depth_m5_mag`、`reference_baseline`）所依据星族与定标星族不一致这点在产品与文档面都不可见 | 通读-CR-01 CR-01-16 |
| C5 | 自适应星等查询的四个关键数值全部无锚：阶梯 `{12..16}`、注释宣称的上界 `10000`（**根本没有实现**）、下界 `2000`、循环 5；同一份写死在三处副本里，加第 6 档被 `i<5` 与 `i==4` 静默忽略 | S2 | 有生产触发链（`pc_api.cpp:978` 起于生产唯一路径）；不产错值，产的是不可解释的样本量与不受控成本；`M42` 这类拥挤场会自动扩到 mag 16 并取到远超"上限主张"的星数 | 通读-CR-04 CR-04-25 |
| C6 | `match_radius_px = 2.0` 决定"匹配／歧义／超距"三分类（进而决定参与定标的星集），代码侧唯一"依据"是空括号注释 `(: 收紧 3.0 -> 2.0)`，`docs/` 两处正本无登记行；夹具 F3 的三处判据全部写死在注释与坐标里 | S3 | 缺条文而非值错，不改产物字节；后果是任何一次再"收紧/放宽"都不触发合同检查 | 通读-CR-02 CR-02-17 |
| C7 | FOV 半径式的三个数（缓冲 `1.2`、钳位界 `1.0/10.0`、异常窗 `30.0`）条文面零命中；`docs/algorithms/GAIA_QUERY.md` 里的 `1.2` 是**赤道带 bbox 拒绝裕量**（另一对象），不得充当锚 | S2 | 同 B12 的跳；缺"哪一帧在真实数据上因该钳位而出值"的样件 | 通读-CR-01 §2.1 表 :171/:172/:173 三行 |
| C8 | 曲线身份指纹的相对容差 `1e-9` 零条文（`PHOTOMETRY.md §2a.7` 只说"一致"）；45/45 全量复算最大残差是该容差的 `3.93e-6` 倍 ⇒ 有约 1e4 倍无来源余量，收紧无收益、放宽无危害 | S3 | 只影响装配期身份门，不改科学数值 | 通读-CR-01 §1.1 表 :193 行 |
| C9 | `ZP_syn` 成立所需的最小样本量取 3（复用拟合常量），无任何条文给绝对参考锚设样本下限；3 星中位数的抽样误差远大于其宣称精度，且同族 `1.4826` 常数在 `n<20` 系统性下偏（正本自述"实现不修正"）| S2 | 现网从不生效（跟踪面实测 `zero_point_n_stars` = 2338/2338/2170）⇒ 缺"真实触发"一跳；后果面是 `zero_point_valid` 与下游 `frame_depth_m5_mag` | 通读-CR-01 §2.5 :276 行、§3.1 :138 行 |
| C10 | 极限星等初值式 `m_cut` 的常数 `6.0/1.5/2.0` 不在任何正本（`git grep "m_cut = 6"` 2 命中皆在该源文件内），同族稳健常数在 `docs/algorithms/PLATESOLVE.md:193` 有登记 ⇒ 属漏登；声明的输入集不含"画幅"⇒ 与实现不符；物理侧天光限域系数应为 1.25、读出噪声限域应为 2.5，代码取 2.0 两域都不对应；旧密度指数 1.3 使模型高估 4–5 个数量级 | S3 | 该式是割线迭代**初值**，常数取歪只多花 Gaia 查询次数 ⇒ 不产生科学偏置；定级为出处缺口＋输入项错配。位点在 `platesolve`，本条按链路归属登记在①（选星策略与 §02 V-6 的分箱灵敏度互为前提） | 测光 DEV-12 |
| C11 | 稳健常数的书目支撑缺失：`c=4.685`、`1/Φ⁻¹(3/4)` 的常数额已由独立数值复算自证，但书目侧本轮全部 UNPROVEN（Beaton & Tukey 1974、Mosteller & Tukey 1977、Huber & Ronchetti 2009、Holland & Welsch 1977 的"§2 效率表"均未读到正文）⇒ 关键科学结论按证据分级只到"自证 ＋ 未核书目"，缺 ≥1 条 A 级 | 登记 | 不进产品。修法是把独立推导（或复现命令）登记进正本 §14 旁，书目另找可取回的一手件；`c=4.685` 与 `1.345` 的挂载点按文献池推荐形态分开挂，不得把 `1.345`/95% 效率挂到 Holland & Welsch | 测光 DEV-11；链路间口径对表 W5 |
| C12 | 登记面根本没有承载出处的列：被 `defaults.json`/`config_registry.json`/资源契约点名的 620 个生产常数符号里 613 个无任何"出处／依据／权威"字段可查，剩下 7 个的锚指向的行不含该值 ⇒ "先给登记面加两列（支撑类型四选一 ＋ 出处锚）再谈逐条补值"，并上门：给不出锚即红、锚不含值即红、同键两值即红 | 结构性 | 不进产品；这条门落地之前，任何"常数已登记溯源"的表述都不成立（①是该族的首发面，实施面与⑤共用该门） | 打回报告 A §3.7 |
| C13 | 测光侧文档行号锚整体漂移：`snr_science.cpp:234` 处不是 m_5 约定（实际 `:241/:249/:261`）且未给目录路径；三处登记指向别的函数，其中唯一记录"某入参静默失效"的登记项锚错 ⇒ 读者按锚去看会判该缺陷**已修**；`filter_curve_json.h:354` 注释里的 `orchestrator.cpp:1342` 实际已漂到 `:2633` | S3 | 不改数值与判据；后果是虚假授权与归因错误 | 通读-CR-01 CR-01-15；通读-CR-04 CR-04-4／CR-04-30；通读-CR-01 §1.1 :354 行 |
| C14 | 合同与实现口径成批不同源：`photometric_calib.h:102/:154` 说 −3 = "无 Gaia/PSF 星（退化 scale=1.0）"，实现对该条件返回 0（按 `PHOTOMETRIC_FIT.md:266` §13.4 F5 才是合规侧，即**合同文字是错的一侧**）；同一"尺寸无效"在两族入口分别取 −2 与 −1；`-3` 在头/README/ALG 三处给出互斥语义且头内一句自相冲突 | S2／S3 | 后果：读 ABI 头的人按 `rc<0` 判"未产出标度"，真实行为是 `rc=0` ＋恒等校正；`fit_ok`（`frame_photometry_fit.h:114-124`）正是为堵这一误读另立的，其注释自认该误读**历史上已发生过一次**（伪造 `photometry_applied=true`） | 通读-CR-04 CR-04-26；通读-CR-05 CR-05-10；通读-CR-03 CR-03-5-02／CR-03-5-04；通读-CR-07 CR-07-04／CR-07-05 |
| C15 | `out_pixels` 的量纲在头内自相冲突：合同把 `scale` 定为有量纲因子 `[F_syn 单位]/ADU`，本头仍称输出为"校正后像素（float32）"且不给单位；`DATA_SEMANTICS` 另一处又用 `photo_scaled_adu` 块名 ⇒ 同一乘性操作在两个面给出两种量纲结论 | S3 | 缺：块级单位声明的权威面在何处（本批文件域内无第二处可判谁是正本）；后果是下游按 ADU 处理被乘过 `scale` 的数组时标度类别判错一档 | 通读-CR-05 CR-05-11 |
| C16 | `PhotometricDiag` 在追溯面被写成"17 字段"，结构体实为 20 个成员；该行的"VERIFIED"读数建立在失真计数上 | S3 | 不改产物字节；`r_p90/r_max/match_distance_*` 是否进对外产品无法从登记面判定 | 通读-CR-05 CR-05-9 |
| C17 | 两处对"谁是权威参照"各执一词：`spectrum_integrator.cpp:4/:249` 仍指向 `.h` 已判定"全仓不存在"的 `python/synthetic_photometry.py`；`.h:39/:47` 声明"与 scipy 一致"却既不钉版本、又与自家 golden 测试声明的"Python scipy.Akima vs C++ 自实现略有差异（容差放宽到 1%）"互斥 | S3 | 不进产品；后果是 F_syn 的数值谱系不可审计，`wl_step=1.0` 的原始出处不可查 | 通读-CR-06 CR-06-09／CR-06-08 |
| C18 | `planck_radiance` 的单位标注（`W·sr⁻¹·m⁻²·Hz⁻¹`，按频率形式）与其实现（`2hc²/λ⁵`，按波长形式）不符，并被 Python 双胞胎以"物理常数与 C++ 完全一致"的名义复制 ⇒ 错标注随"一致性"主张扩散 | S3 | 当前产物不受影响（输出按最大值归一并量化到 uint8，单位误差被归一化吸收）；风险在后来者去掉归一化直接乘 `flux_mul` 时用 | 通读-CR-07 CR-07-14 |
| C19 | 乘性空间场 `m(x,y)` 的**式子本身在科学侧零承载**：`−Σ`（施加因子是拟合量的倒数）、`B̃ = B − center`（中心化在施加端同样生效）、`10^(·)` 三件约定全部只存在于代码注释与头文件；正本 `PHOTOMETRY.md:13` 只写 `I_photo = k_photo·m(x,y)·I_cal` 与"标度类别"，不给基函数、规范中心化式与取负号；而拟合侧与施加侧**共用同一份内联实现** ⇒ 互为验证、无第三方期望。同族：`x_scale` 缺省 `1.0` 的含义在跟踪面无任何条文规定（现由唯一生产者显式赋值而不可达） | S2 | 缺：条文面零提及，指不出"哪一条被违反"，只能指出无锚。一旦符号或中心化写反（例如漏 `center`，注释自己说明后果是"`k_photo` 的全局语义被暗改"），下游没有任何独立面能判红；受影响主张即创新点①的逐帧 `k_photo` 语义 | 通读-CR-55 CR-55-2；通读-CR-55 §1 表 :67/:87/:96 行 |

---

## D. 证据落档与对外主张的自证面

| 编号 | 事实 | 档 | 缺哪一跳 | 成稿锚 |
|---|---|---|---|---|
| D1 | 正本 §2a.4/§2a.5/§2a.6/§8/:377 把定量结论的证据位置写成 `run/...`：`run/SCI-PHOT-FORMULA-01/evidence/*.json` 六条全悬空（`distinct run/ refs = 6`、`DANGLING = 6`，跟踪面无副本）；`spectrum_integrator.h:122-125` 与 `:30-31` 的绝对刻度读数同犯（`−0.0037 mag`、`0.459 dex = 1.147 mag` 等）⇒ 独立读者无法复核 §2a 的全部定量结论，本轮对这些数字**不背书也不判错** | S2 | 缺：可再生的复现命令 ＋ 跟踪面副本（`run/` 会被 `--prune-products` 主动回收） | 测光 DEV-09；通读-CR-06 CR-06-10 |
| D2 | "绝对刻度由本轮真实数据实证锚定"这一表述超过其证据：决定"能量谱 vs 光子谱"两支约定的唯一判据（生产解码链上的 `median(m_syn−G)`，现应 ≈0，若容器为光子谱则 ≈−7 mag）其结果件不入库，在库旁证（1050 星对拍表）对受检假设**零灵敏度** ⇒ 该约定目前是**未被判别**，既非"已被排除"、也非"存在问题" | 需复测 | 缺：`xpsd_production_validation.py` 的实跑结果件入库（脚本已跟踪、结果未跟踪） | 测光 DEV-02；通读-CR-07 CR-07-14 同族 |
| D3 | 台账与门表的历史证据指针指向不存在的载体：`FIX_LEDGER.csv:39,:41` 的证据指针写 `lib/photometric_calib/**`（`git ls-files` = 0）；`AGENTS.md §7` 与派工面写 `lib/infrastructure/gaia/`（实为 `lib/infrastructure/gaia_xpsd_client/`，15 个跟踪文件）⇒ 按名取物取不到 | S3 | 缺：无（静态可判）。后果是独立读者按路径取不到证据，"门是否绿"在仓库内不可自证 | 测光 DEV-14／DEV-15 |
| D4 | 唯一入库的质心原始读数出自已被门表明文降级为诊断脚本的产出者（`gate2_result.json` 的 `centroid_p95_px 1.1529` 对 `kGateP95Px = 0.3`），现行两条登记门的收口载体**晚于**该红读数落地；门读数汇总只在 `run/` ⇒ 现状既不能判红也不能判绿，此前"科学门超容差 3.8×"的定性已撤销 | 需复测 | 缺：`p1psf_(prodpath_centroid|centroid_gate|centroid_gate_neg)` 的当前读数落 `artifacts/ci/<sha>/`。据该历史读数判"`sigma_residual` 预算漏项"的说法不成立 | 测光 DEV-16；链路间口径对表 W6 |
| D5 | 一条"合法路径不受屏障影响 ⇒ 尺度 0"的断言实为退化绿：夹具几何使参考星与目标必然匹配不上 ⇒ `rc=0` 与 `scale=1.0` 都由"无匹配"退化路径给出，断言恒真；同类地，异常返回码的全部断言只走仓内自记"生产不可达"的两条简单入口，生产可达入口的同类路径零断言 | S2 | 缺：真实匹配发生的夹具 ⇒ 写不出产品侧后果；成立的是"合法路径未被破坏"与"异常面已覆盖"两项主张无证据 | 通读-CR-07 CR-07-02／CR-07-07 |
| D6 | 一整个数值族的权威锚 24/24 悬空：`eng/contracts/data/v6_clause_registry_v1.json` 里 24 条点源权重条款逐条声明钉在 `docs/science/PSF_SIGNAL_WEIGHT.md` 某行，实测该定位符在该文件不存在、12/24 连自己声明要"包含"的数值串都不在被引文件内，命中者多为 `"5"`、`"200"` 这类无判别力子串；校这条锚的门只断言"文件存在" ⇒ 门绿而被核对象不存在。（**归属说明**：该族属创新点②的面，登记在此只因整改顺序共用 C12 那条"锚必须含值"的门；实施时不并入①的产物判据。） | S2 | 缺：无（静态可判）；不改产物字节 | 打回报告 A §3.8.1 |
| D7 | "没测＝满分"的判据方向：零样本分支把"有效面积占比"填 1.0、散布与趋势填 0.0，加两个默认 0 的量使四条单侧判据在从未测量时恒过；同一模块别处已用"是否评估过"旗标做到 fail-closed ⇒ 是遗漏不是取舍，现役产品 JSON 已带 `valid_area_fraction=1.0` 在走（归属同 D6，属②的面，共用修法） | S2 | 不改①的判据数值；与 A2 同族（不可估计必须具面值） | 打回报告 A §3.8.2 |
| D8 | 反推仪器参数禁令目前只靠条款文字：产品面确无由标定系数反推的 `gain/口径/曝光`（现状合规），但 `eng/ci/` 无该检查 ⇒ 注入 `gain = f(k_photo)` 不会红，属判据空档 | S3 | 缺：grep 型机器门；无既有违规 | 测光 DEV（§4 第 7 行「反推仪器参数禁令」） |
| D9 | 本点目录的取证负结果：`通读-CR-19`、`通读-CR-20`（`lib/algorithms/coverage` 两段）经全文关键词检索（测光／photometr／F_syn／星等／k_photo／ZP／SCI-PHOT／PHOTOMETRY／spectrum_integrator／star_matcher）后，**无一条落在创新点①的链上**；其中出现的 `overlap_photometry.py` 只是覆盖模块孪生脚本的文件名巧合，其 `seam_band_deg`／`bin_deg`／p95 分位定义三项属④面积交叠的待确认常数 | — | 记下以免后续节点再到这两份成稿里找测光缺陷 | 通读-CR-19／CR-20 全文检索 |
