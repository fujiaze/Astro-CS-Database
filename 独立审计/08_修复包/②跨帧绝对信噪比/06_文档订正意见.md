# 06_文档订正意见（跨帧绝对信噪比）

> 本文件对应派单：AUDIT-06 · 阶段 A · 第②点（跨帧绝对信噪比）  
> 判据来源：`独立审计/派单规程/DISPATCH-SPEC-PREAMBLE.md` §4（五字段、错/缺/乱三档、不列责任归属、与 05 互指、检索式自证）  
> 扫描范围：**仓库正式文档** (`docs/**` + 根规范中本点相关文件)；不包含本修复包自身
>
> **PROGRESS**: 已写入 5 条订正（错 2／缺 1／乱 2）。

---

## 一、错（现行文字错误/矛盾/误导性表述）

| # | 被订正文件 :行 + 内容锚 | 现状原文（短引） | 订正后完整文字 | 依据 | 同句还出现在哪些文件（逐处列全） |
|---|------------------------|------------------|----------------|------|----------------------------------|
| 1 | `docs/science/NOISE_MODEL.md:9` (§1 目的句) | "估计校准后空背景随机分量的逐像素方差 `variance` 及其倒数 `ivar=1/variance`，作为 Phase2 逐像素科学权重入 `var(x,y)=a+b·x+c·y` 空间场 + 全局兜底，用于 UPM 控制光度拟合与加权积分。" | "估计校准后空背景随机分量的逐像素方差 `variance` 及其倒数 `ivar=1/variance`，作为天光建模与 UPM 控制点拟合的权重、误差报告与诊断、背景受限域叠加权的近似入 `var(x,y)=a+b·x+c·y` 空间场 + 全局兜底。" | 《已确立》§1.1/§1.4：背景逆方差只在两情形等于最优权——掩膜外像素与背景受限；"逐像素科学权重"的准确口径为§5c 加权方差面（含源项）的倒数；本段所指的 background variance 不承载叠加侧的科学权重职责。同批见 D-26 | `docs/science/NOISE_MODEL.md:328` (§9a 权重归一与适用域句)、`docs/plugins/algorithms_phase1/07_noise_snr.md:201` (§4.6 HiPS 只存句) — 该三处同属"指称越界"，需同步订正 |
| 2 | `docs/science/NOISE_MODEL.md:328` (§9a 权重归一与适用域) | "`ivar` 作为 Phase2 逐像素科学权重直接入加权（归一在消费侧 `Σw/Σ`）……" | "`ivar` 作为天光建模与 UPM 拟合权、误差报告与诊断、背景受限域叠加权近似入加权（归一在消费侧 `Σw/Σ`）……" | 依据同第 1 条；《已确立》§1.4 取等条件明确 ivar=1/σ_bg² 仅在"掩膜外 (S_src≡0)"与"背景受限 (S_src/g ≪ σ_bg²)"两情形成立；无条件写成定义属于指称越界而非数学分歧 | 同上 `docs/science/NOISE_MODEL.md:9`、`docs/plugins/algorithms_phase1/07_noise_snr.md:201` |

---

## 二、缺（关键定义缺失/公式缺项/参数缺说明/约束缺声明）

| # | 被订正文件 :行 + 内容锚 | 现状原文（短引） | 订正后完整文字 | 依据 | 同句还出现在哪些文件（逐处列全） |
|---|------------------------|------------------|----------------|------|----------------------------------|
| 1 | `docs/science/NOISE_MODEL.md:99` (§5a 天空预算推导) | "预算推导（a priori）: 单 patch 相对误差 c ≈ 1.152/√N，中位数效率 1.25 ⇒ SE(σ̂)/σ ≈ 1.44/√N_sky；取 SE ≤ 1.5% ⇒ N_sky ≥ 9216" | "预算推导（a priori）: 单 patch 相对误差 c ≈ 1.152/√N，中位数效率 1.25 ⇒ SE(σ̂)/σ ≈ 1.44/√N_sky；取 SE ≤ 1.5% ⇒ N_sky ≥ 9216；导出式 (1.44/0.015)² = 9216，其中 1.44 = 1.152 × 1.25（单样本相对误差×中位数效率），0.015 为 SE 目标值（无量纲）。" | 《已确立》§3 行 7、P-CST-08：9216 是公式导出值，非字面量常数；注释应给出导数链以支持复核人验证；同时纠正 `noise_model.cpp:1055-1059` 与 `NOISE_MODEL.md:301` 处误将 `1.144` 支当作同一常数的错误（D-39） | `lib/algorithms/noise_snr/cpp/src/noise_model.cpp:1055-1059`、`:310/:1328`；`docs/science/NOISE_MODEL.md:301` |

---

## 三、乱（结构混乱/术语不一致/符号歧义/流程步骤错位）

| # | 被订正文件 :行 + 内容锚 | 现状原文（短引） | 订正后完整文字 | 依据 | 同句还出现在哪些文件（逐处列全） |
|---|------------------------|------------------|----------------|------|----------------------------------|
| 1 | `docs/science/NOISE_MODEL.md:301` (§5a 预算常数) | "SE(σ̂)/σ ≈ 1.144/√N_sky" | "SE(σ̂)/σ ≈ 1.44/√N_sky" | D-39：该注释支 (1.144) 与实现求值支 (1.44) 不同源；正确导出式应为 (1.44/0.015)² = 9216，而 (1.144/0.015)² = 5811；需与代码侧收敛为一支 | `lib/algorithms/noise_snr/cpp/src/noise_model.cpp:1055-1059`（引 1.144）、`:310/:1328`（用 1.44 导出 9216）— 需三者同批改 |
| 2 | `docs/science/NOISE_MODEL.md:9` 与 `docs/plugins/algorithms_phase1/07_noise_snr.md:201`（§4.6） | 两处将 background variance 的用途指称为"Phase2 逐像素科学权重"与"HiPS 里'只存'帧级 SNR 与稀疏绝对 SNR" | "只存该层与帧级信噪比"（补列举不全）；background variance 用途限定语"天光建模与 UPM 拟合权、误差报告与诊断、背景受限域叠加权近似"（见一、二节订正后文案） | D-26 指称越界；《已确立》§1.1 两张面用途分明；contract `UNIFIED_OBJECTS.md` §2 对象表规定 `frame_snr`/`sparse_snr_layer` 为层级内引用对象，但 variance/ivar 的统计面用途不在"科学权重"范畴 | 同第一节三处互引关系 |

---

**自报栏（最终计数）**  
- 06 共 P=5 条：错 X=2 / 缺 Y=1 / 乱 Z=2  
- 与 05 的互指清单：  
  - 第 1、2 条 ⇒ 见 05 §3.5（P-ALG-05 两案共同部分）、§3.10（P-ALG-10 权重换算唯一口径）  
  - 第 3 条 ⇒ 见 05 §2.1（P-CST-08 天空样本预算阈）  
  - 第 4 条 ⇒ 见 05 §2.3（P-CST-28 阈值的算出而非写死）  
  - 第 5 条 ⇒ 见 05 §1.2（术语记法）、§4.3（交换层平面）
