# 06_文档订正意见 · 面积交叠与分配

**目的**：把“第①点”相关仓库正式文档中错/缺/乱三类问题一次性订正到位；所有修改锚定 AGENTS.md §1 的索引链路并满足§4 判据（五字段、分三档、不自说责任归属、与 05 互指、自证检索式）。

**范围**：docs/** 与根规范中本点相关文件；不含本项目代码本身，也不改 08_修复包的内部说明。

**判据**：每条订正给五字段——
1) **被订正文件:行+内容锚**（能定位到句子/表头/公式编号）
2) **现状原文**（逐字短引，≤2 行或整段）
3) **订正后完整文字**（可直接粘贴的整句/整段）
4) **依据**（引用权威来源的条款号或公式号）
5) **同句还出现在哪些文件**（跨文件一致性点名）

---

## A｜错（当前文字导致实现歧义或与公式/合同冲突）

### A-1 `ASTROCS_DESIGN.md:210` 核权重分母口径错为`A_pixel`

**被订正文件:行+内容锚**: `ASTROCS_DESIGN.md:210`（创新点四第 5 条"任意 WCS footprint 与权重/variance 链的整合"末尾）

**现状原文**:
```text
drop 由任意 WCS（含 SIP 畸变）的源像素足迹给出，交叠面积直接进入 w_jp = a_jp / A_pixel 与 variance 传播。
```

**订正后完整文字**:
```text
drop 由任意 WCS（含 SIP 畸变）的源像素足迹给出，交叠面积直接进入 w_jp = a_jp / A_drop,j 与 variance 传播。
```

**依据**: 
- `05_正向规格.md §4.1`："唯一合规口径：w_jp = a_jp / A_drop,j"
- `02_已确立的算法与验证程序.md §2`："唯一实现口径是按 drop 面积归一 `w_jp = a_jp/A_drop,j`……`Σ_p w_jp = 1`只在 drop 归一下成立"
- `spherical_overlap.h:93`公共头虽误写但仍需订正
- `docs/science/DRIZZLE.md §10` 正本逐字禁止项"把核权重写回 `a_jp/A_pixel,j`（`FZ-COND-FLUX-CONSERV` 判红）"

**同句还出现在哪些文件**:
- `docs/science/DRIZZLE.md §7`（见订正面锚）
- `spherical_overlap.h:93`（公共头公式声明）
- `DATA_SEMANTICS.md:47`（登记面字段描述）
- `DRIZZLE_GEOMETRY.md:337-346`（几何合同面）

⇒ **见 05 §4.1**（核权重分母口径）

### A-2 `ASTROCS_DESIGN.md:211` 极区奇点论述错置为"无需回退"

**被订正文件:行 + 内容锚**: `ASTROCS_DESIGN.md:211`（创新点四"奇点条款"末尾）

**现状原文**:
```text
这些**奇点被精确定位、精确检测、有处理方式，全天正确性由此保证**；**面积口径不受任何分支影响**（`|J| = π/3` 为解析恒等）。本方案不主张"不存在奇点"，也不主张"任意输入都无需回退"。
```

**订正后完整文字**:
```text
这些**奇点被精确定位、精确检测、有处理方式，全天正确性由此保证**；**面积口径不受任何分支影响**（`|J| = π/3` 为解析恒等）。本方案明确主张：**nside ≥ 256 时旁路细分是缺陷**，必须启用自适应边细分路径（`subdivide_healpix_edge`），或对触点像元与冠界像元强制走细分。
```

**依据**: 
- `05_正向规格.md §2.4`："`nside ≥ 256`：**不得**直接取 4 角弦（`get_healpix_boundary4`），必须启用细分路径（或至少对触点像元与冠界像元强制走细分）"
- `01_缺陷清单.md` 缺 -3："真曲线细分例程在仓却被调用点旁路，注释与调用点相反"
- `spherical_overlap.cpp:506-541`/`:543-617` 子函数自述"对所有 NSIDE 统一使用自适应边细分"但生产调用点在 `nside≥256` 时旁路

**同句还出现在哪些文件**:
- 无直接复现，但该段落语义在 `DRIZZLE_GEOMETRY.md` 相应位置需对齐

⇒ **见 05 §2.4**（自适应边细分路径）

### A-3 `ASTROCS_DESIGN.md:212` 性能口径模糊化"不可达"

**被订正文件:行 + 内容锚**: `ASTROCS_DESIGN.md:212`（创新点四"性能口径"）

**现状原文**:
```text
快速路径相对球面裁剪路径的成本优势取决于 drop 跨越的 face 数与边界结构，回退路径本身不提供加速。**本仓当前无可复现的加速比实测证据，该面待实测**。
```

**订正后完整文字**:
```text
快速路径相对球面裁剪路径的成本优势取决于 drop 跨越的 face 数与边界结构，回退路径本身不提供加速。**chart 快速路径（`实验/healpix-polar/code/`）未进生产**，生产唯一路径为球面 S-H 逐边裁剪（`spherical_overlap.cpp:1283-1338`）。因此本节不对成本做任何数值主张，只记录需求：需实测 fast vs fallback 的分档剖面。
```

**依据**: 
- `01_缺陷清单.md` 缺 -21："chart 快速路径未进生产，却被写成已落地能力"
- `02_已确立的算法与验证程序.md §5.1` 台账："`poly_clip.h` 平面 S-H＋鞋带（`chart_native.h`）仅原型 → `lib/`全仓 0 命中"

**同句还出现在哪些文件**:
- `DRIZZLE_GEOMETRY.md` 相应章节需同步删除"快速路径可用"措辞

⇒ **见 05 §6**（导出腿重采样核，本条不涉及）

---

## B｜缺（该有定义/约束/边界却缺失）

### B-1 `ASTROCS_DESIGN.md §2.4` 缺少 polar touch 残差上界

**被订正文件:行 + 内容锚**: `ASTROCS_DESIGN.md:211`（奇点条款中段）

**现状原文**（该处只提到"有处理方式"，无定量上界）:
```text
drop 靠近这些位置时解析延拓可能跳变，这些**奇点被精确定位、精确检测、有处理方式，全天正确性由此保证**；
```

**订正后完整文字**（补充至"全天正确性由此保证"后）:
```text
drop 靠近这些位置时解析延拓可能跳变，这些**奇点被精确定位、精确检测、有处理方式，全天正确性由此保证**；从极冠边偏差 `0.0809·hp_res` 起算、逐层 ÷4，降到自身阈值 `1e−6·hp_res` 需 `d > log(8.09e4)/log(4) = 8.15` 层，而 `HP_ADAPTIVE_MAX_DEPTH = 8` ⇒ **极点邻边触底截断，残差 `1.234e−6·hp_res`（超阈 23%）**。回退策略：扩大候选半径至 `3.0×hp_res`，并记录触底像元数与残差上界。
```

**依据**: 
- `05_正向规格.md §2.3`（含极像元的特殊处理）："回退路径：若无法保证细分深度，该像元走保守回退路径（扩大候选半径至 `3.0×hp_res`），并记录触底像元数与残差上界。"
- `01_缺陷清单.md` 缺 -3 取证行末尾的"接线代价"

**同句还出现在哪些文件**:
- 无直接复现，该残差数据应在 `DRIZZLE_GEOMETRY.md` "容差与精度"章补入

⇒ **见 05 §2.3**（含极像元的特殊处理）

### B-2 `ASTROCS_DESIGN.md §2.1` 测光归一化施加位点缺失"photometry 一步"

**被订正文件:行 + 内容锚**: `ASTROCS_DESIGN.md:276`（normalize §4.2 "测光归一化落到像素"）

**现状原文**（较冗长，应更简洁且明确"一步完成"）:
```text
**测光归一化落到像素（photometry 一步完成，不设独立节点）**：同一节点内把 `I_photo = k_photo · m(x,y) · I_cal` 施加到像素（`m(x,y)` 为低阶空间乘法增益，用星点估计），其后所有节点与 drizzle 消费归一化后的像素。
```

**订正后完整文字**（补充"必须登记在 p1_phot.json"的证据要求）:
```text
**测光归一化落到像素（photometry 一步完成，不设独立节点）**：同一节点内把 `I_photo = k_photo · m(x,y) · I_cal` 施加到像素（`m(x,y)` 为低阶空间乘法增益，用星点估计），其后所有节点与 drizzle 消费归一化后的像素。**p1_phot.json 必须记 `photometry_applied` / `photscal` / 逐帧 `k_photo` / 施加后产物路径**，使「k 确实乘进了像素」可由独立读者用「calibrated 面 × k」逐像素复算核对。该步不可用时产品显式记录 `degraded_reason` 并 fail-closed；下游节点消费的像素以已施加归一化为前提。
```

**依据**: 
- `05_正向规格.md §10.1` normalize 路径调用图
- `docs/plugins/algorithms_phase1/08_drizzle.md §7`（错误与边界）

**同句还出现在哪些文件**:
- `docs/plugins/algorithms_phase1/06_photometry.md` 应有类似表述但不必重复

⇒ **见 05 §10.1**（normalize 路径调用图）

---

## C｜乱（同一概念多处写法不一、顺序不清、术语不统一）

### C-1 `support_semantics`与`nan_handling`登记面用语混乱

**被订正文件:行 + 内容锚**: `05_正向规格.md §7.2` manifest 字段映射

**现状原文**:
```json
{
  "drizzle": {
    "normalization": "drop_area",           // 核权重分母口径
    "pixfrac": 0.8,                         // 出厂配置值（非 1.0！）
    "support_semantics": "profile_aware",   // profile=0/1 不同行为
    "nan_handling": "mask_and_renorm",      // 保守默认口径
    "quantization_limit": "0.5/q"           // profile=1 的信号精度界
  }
}
```

**订正后完整文字**（统一术语，避免"保守默认口径"这类模糊说法）:
```json
{
  "drizzle": {
    "normalization": "drop_area",           // 核权重分母口径：w_jp = a_jp / A_drop,j
    "pixfrac": 0.8,                         // 出厂配置值（非 1.0！）
    "support_semantics": "profile_aware",   // profile=0 直写 double，profile=1 量化端（255 档）
    "nan_handling": "mask_and_renorm",      // NaN 样本剔除、合格集重归一、计数 n_rejected_nan
    "signal_precision_limit": "0.5/q"       // profile=1 的严格界（q = round(255·S)）
  }
}
```

**依据**: 
- `05_正向规格.md §5.2`："8 位量化映射……严格界：`|r| ≤ 0.5/q`"
- `05_正向规格.md §5.3`："两套互斥口径（待负责人裁决，保守默认）"→改为"掩膜＋重归一＋计数（保守默认）"

**同句还出现在哪些文件**:
- `docs/contracts/DATA_SEMANTICS.md` 对应登记项
- `eng/contracts/schemas/manifest.schema.json` machine-schema 需同步

⇒ **见 05 §7.2/§7.3**（manifest/provenance 映射）

### C-2 `flux_conservation_factor ≡ 1`与"`Σ_out = Σ_in` 无关 pixfrac"混为一谈

**被订正文件:行 + 内容锚**: `ASTROCS_DESIGN.md:214`（创新点四结尾）

**现状原文**:
```text
- **通量守恒本身合规**——真正在用的那份球面面积实现其量级论证成立 ⇒ "加钻丢通量"这一族怀疑**在球面那一份上不成立**，不要连它一起改。
```

**订正后完整文字**（拆成两条：一条讲 flux_conservation_factor，一条讲求和闭合资格）:
```text
- **flux_conservation_factor ≡ 1 是构造性质**：当且仅当 `w_jp = a_jp / A_drop,j` 时成立（定义性推论，**不是**被验证的经验命题，**不构成**面积分配正确性证据）。分配正确性由逐叶判据把门。
- **求和型闭合的门资格 = 完备性与全局标度**（抓得住漏候选、漏 face、量级错），**不含逐叶正确性**。不能拿求和门顶替逐叶门。
```

**依据**: 
- `05_正向规格.md §4.2`："flux_conservation_factor ≡ 1：当且仅当 `w_jp = a_jp / A_drop,j` 时成立（构造性质，非经验命题）"
- `05_正向规格.md §8.2`（求和型闭合容差）："资格范围说明：正当资格 = 完备性与全局标度……不含逐叶正确性"
- `01_缺陷清单.md` 缺 -11："求和型闭合被当作逐叶正确性证据"

**同句还出现在哪些文件**:
- `02_已确立的算法与验证程序.md §2` 已有"求和型恒等式的证据资格"小节，应与其对齐

⇒ **见 05 §4.2/§8.2**（通量守恒构造 / 求和型闭合容差）

### C-3 `quantization_limit`字段名在 manifest 与 provenance 间不一致

**被订正文件:行 + 内容锚**: `05_正向规格.md §7.2` manifest 与 §7.3 provenance 登记项

**现状原文**（manifest 写 `quantization_limit`，provenance 写`signal_precision_limit`）:
```json
// manifest
"quantization_limit": "0.5/q"

// provenance
"signal_precision_limit": "0.5/q"（profile=1 时）
```

**订正后完整文字**（统一用 `signal_precision_limit`，并在 manifest 里加注）:
```json
// manifest
"signal_precision_limit": "0.5/q"       // profile=1 有效（u8 量化）

// provenance
"signal_precision_limit": "0.5/q"       // profile=1 时的信号精度界（`q = round(255·S）`）
"quantization_profile": 0 | 1           // 是否启用 u8 量化
```

**依据**: 
- `05_正向规格.md §5.2`："接线要求：provenance 必须写 `signal_precision_limit = 0.5/q`，并把 u8 位深语义登记进 DATA_SEMANTICS §11。"
- `05_正向规格.md §7.3` provenance 登记项

**同句还出现在哪些文件**:
- `docs/contracts/DATA_SEMANTICS.md §11`（数据形态登记）

⇒ **见 05 §5.2/§7.3**（量化端 / provenance 登记项）

---

**PROGRESS**: 
- 已完成：读 dispatch preamble / 05 规范 / ASTROCS_DESIGN.md / DRIZZLE plugin / 02 验证程序
- 订正条目：**共 P = 8 条**：错 X = 3 条（A-1, A-2, A-3）／缺 Y = 2 条（B-1, B-2）／乱 Z = 3 条（C-1, C-2, C-3）
- 与 05 互指清单：
  - A-1 ⇒ 05 §4.1
  - A-2 ⇒ 05 §2.4
  - B-1 ⇒ 05 §2.3
  - B-2 ⇒ 05 §10.1
  - C-1 ⇒ 05 §7.2/§7.3
  - C-2 ⇒ 05 §4.2/§8.2
  - C-3 ⇒ 05 §5.2/§7.3
- A-3 不需 05 对应（因 chart 路径零实现属状态陈述）

**硬纪律自检**:
- ✅ 仓库只读，仅写本文件
- ✅ 不跑构建/ctest/run_checks.py
- ✅ 不 import Python
- ✅ 无 git 写

**下一步**: 按"每完成 3–4 条立即重写保存"的加固条款，本次已产出 8 条（>3），应进行整文件重写保存。
