# ③加性天光无缝 · 缺陷清单

编号规则：`D-NN` 本点内部编号；`成稿锚` 给通读批次 finding 号与科学核验报告条目号。
档位：`S1` 已写出到产品字节的逐跳触发路径；`S2` 机制与可达性 CONFIRMED、缺"真实输入命中"这一跳；`S3` 不改产物字节，失真在声明面/可审计性。
`需负责人裁` = 该条的修法有两个互斥方向，本包不代为择一（见 `02_已确立的算法与验证程序.md` §6 登记面）。
本件只列**要改什么**；已经核过为正确的式子、常数与判据方向在 `02`，禁止顺带改动的东西在 `02` §7。

---

## A. 退化输入的发布面（"无信息"被写成合法数值）

### D-01 零尺度 patch 的数值保护量被平方成伪方差并随观测发布
- 事实：`lib/algorithms/coverage/src/sampler.cpp:864` 在稳健尺度 `s0 <= 0` 时取 `1e-12` 作保护量，`:875/:877` 随后把它当真实尺度算出 `control_variance = 7.609e−27`、`control_ivar = 1.314e26`，`:1115/:1116` 无条件写入观测并出厂。冻结口径要求该分支发布"无尺度信息"：`control_ivar = 0`、`control_variance` 取非有限值（或以 `quality_flags` 置位并计数）。
- 档位：**S1**
- 触发路径：`lib/algorithms/coverage/tools/stage2.cpp:314 p2_sample_controls_cached`（另一现役入口 `lib/infrastructure/scheduler/src/module_adapters.cpp:9404`）→ `sampler.cpp:1267 p2_sample_controls_impl` → `:837 median_of` → `:843-844 s0` → `:864 地板` → `:875/:877 cvar/civar` → `:1115-1116 写入 obs` → `upm.cpp:644 p2_upm_raw_weight` → `upm.cpp:668-671` per-control 归一 ⇒ 该观测权重占比 →1.0，独占该 control 的相对光度解；`:1087/:1107-1111` 同时把同一地板写进 `snr` 字段。
- 成稿锚：通读-CR-48 CR-48-01；天光无缝核验报告 DEV-01（第一处）；链路间口径对表 X5。
- 同批必改（不改则本条修法被判红）：见 `03` S-04 的同批表与 `02` §7.3 门侧登记项。

### D-02 同族第二处地板在 `p2_sky_patch_estimate`
- 事实：`lib/algorithms/coverage/src/sky_plane.cpp:296` 的 `sigma = (s0>0)? s0 : 1e-12` 与 `:306/:307` 的 `variance/ivar` 是同一数学定义的第二处实例，该函数无任何门覆盖。只修 `sampler.cpp` 会留下这一处。
- 档位：S2（当前由 `eng/ci/ledgers/prod_wiring.json:344` 登记为生产调用图外；接线即 S1）
- 触发路径：`sky_plane.h:90-91` 自称与 sampler 的 patch estimator「同一数学定义」→ 任一处改用该函数替代 sampler 内联估计即进入产品。
- 成稿锚：DEV-01（第二处）；通读-CR-23 CR-23-03；通读-CR-47 CR-47-20（两处非法值落点不同一套数）。

### D-03 下游对"有限但荒谬"的逆方差无拒载能力，且整批 rc=2 与按观测剔除口径互斥
- 事实：`upm.cpp:2471` 与 `upm.cpp:1985` 只拒 `ivar <= 0` 与非有限 ⇒ D-01 的 1.3e26 全额放行；同一处对 `ivar<=0` 的处置是**整批 rc=2**，而条文要求退化观测按观测粒度剔除并计数。二者不协调时，D-01 修好即把生产判成全红。
- 档位：**S1**（与 D-01 同一触发链）
- 成稿锚：CR-48-01 修法③；天光无缝核验报告 §4「`control_ivar` 的 0/非有限兜底」行；链路间口径对表 §3。

### D-04 三道"σ 下限"互不代用却跨标度代用，UPM 侧地板拦不住采样侧伪逆方差
- 事实：`upm.cpp:292` 的 `sigma_floor=1e-3` 与消费侧 uncertainty 的面亮度标度差 13 个数量级 ⇒ 地板恒不生效；`p2_upm_raw_weight` 的 production 分支（`upm.cpp:1981-1986`）直接取 `control_ivar`，不经任何地板；`uncertainty = +inf` 时 `sigma_eff = inf ⇒ z = 0 ⇒ huber_w = 1`，完全不确定的观测获得满权重。
- 档位：S2
- 成稿锚：CR-48-14；通读-CR-25 CR-25-03；常数总台账 `sigma_floor` 行（多侧标度说法相反）。

### D-05 `uncertainty`／`control_variance`／`control_ivar` 三字段一致性零校验
- 事实：`upm.cpp` 的 `build_impl` 全程不校验 Huber 用的 `uncertainty` 与权重用的 `control_ivar` 是否同一方差口径，两路可来自两个互相矛盾的方差面而无人发现。
- 档位：S2
- 成稿锚：CR-25-14。

---

## B. "禁权重模式"贯穿到键名、没贯穿到概念

### D-06 `sky_plane.weight_mode` 是第二个权重口径枚举，配置零校验且该链默认启用
- 事实：`lib/algorithms/coverage/src/stage2_common.cpp:193` 原样收 `sky_plane.weight_mode`，`sky_plane.cpp:524` 把非法值静默改写为 0；取 1 时 `sky_plane.cpp:677` 用 `snr*snr` 作权重、`:1053/:1169` 用 `1/snr` 反算 σ；该链 `sky_plane_enabled` 缺省为真。
- 档位：**S1**
- 触发路径：生产 config → `stage2_common.cpp:193` → `module_adapters.cpp:9973` → `sky_plane.cpp:524` → `:676/:677` 权重构造 → `:1053` IRLS → `:1725` 落键进中间产物 JSON 并回读。
- 成稿锚：通读-CR-23 CR-23-01/CR-23-02；CR-48-10；打回报告 B §5.17 第 1 条。
- 附加事实：`snr²` 的分子是**局部稳健背景电平**（`module_adapters.cpp:9863`）＝天光本身，方向与"天光增大 ⇒ SNR 单调下降"相反。

### D-07 `integration.use_ivar_weight` 可把叠加权切到被点名禁止的 support×SNR 式
- 事实：`stage2_common.cpp:179-180` 解析、`:541` 显式透传，`upm.cpp:1981-1998` 判假后走 `qf·support^p·snr²/((1+snr²)·unc²)`；"仅 ablation/诊断"的限定无任何实现面承载（无编译期开关、无 rc 拒绝、无诊断标记）；该位还被 `upm.cpp:1176/:1397` 写进配置哈希与模型 JSON。
- 档位：**S1**
- 触发路径：`stage2_*.json` → `stage2_common.cpp:179` → `stage2_common.cpp:541` → `upm.cpp:644` → `upm.cpp:1981` → `:1992-1997` → `:668-671` 归一 → C 场/积分。
- 成稿锚：CR-48-09；通读-CR-47 CR-47-25；打回报告 B §5.53 第 ② 条。

### D-08 合同判"键不存在"的 `snr_weight_mode` 仍是解析器唯一读的键，合同现行键 `upm_weight_source` 全仓零实现
- 事实：`stage2_common.cpp:163-168` 只接受字符串 `"snr2_normalized"`，从不赋给 `cfg->snr_weight_mode`（`stage2_common.h:54` 的 struct 默认决定值）；`docs/contracts/PUBLIC_API.md:2208` 与 `docs/contracts/DATA_SEMANTICS.md:1970` 对该键是否在册说法相反；写现行键的 config 被静默丢弃且值任意。
- 档位：S2
- 成稿锚：CR-47-24；CR-48-12（"校验即弃"）。

### D-09 未实现的枚举开关被零校验静默接受
- 事实：`robust_loss`／`snr_weight_mode`／`quality_mode` 在本模块无消费者或只判等值，与同文件"权重口径必须出现即拒"的自订纪律相反。
- 档位：S3
- 成稿锚：通读-CR-25 CR-25-06；CR-48-12。

### D-10 `weight_mode=1` 下 Huber 的标准化残差带 ADU 量纲
- 事实：`sky_plane.cpp:1053` 取 `sigma = 1/snr` ⇒ `z = r·SNR`，却被与无量纲的 `δ=1.345` 比较；`sigma<=0` 时 `z=0` 给满权，即"不可定标"被当"完全正常"。同一函数 IRLS 拟合段（`:1049-1052`）不加 `gauge_shift`、报告段（`:1162`）加。
- 档位：S2
- 成稿锚：CR-23-13。

### D-11 天光面配置块整体无值域校验，`rank_rtol` 可被非有限配置静默替换
- 事实：`stage2_common.cpp:183-196` 的七个键全部原样收；`rank_rtol = 0` ⇒ 判决阈 `τ=0` ⇒ `identifiable ⟺ κ < 1/0` 恒真；NaN ⇒ 静默回落 1e−10，判决阈被改写而返回码不区分。
- 档位：**S1**（默认链在产＋配置可达＋判据方向可定）
- 成稿锚：CR-23-11；CR-23-07（`:514-524` 钳位面）；CR-48-10 后半。

---

## C. 排异（`lib/algorithms/coverage/src/rejection.cpp` 与配套件）

### D-12 三个 Siril 系方法核的"至少保留 N"停止闸把同一批剔除数扣了两次
- 事实：`rejection.cpp:1582`（winsorized）、`:1693`（linear_fit）、`:1883`（median_sigma）写作"剩余数 − 累计剔除数 ≤ 4 则停"，而"剩余数"每轮重取为 `n0 − r`、`r` 跨迭代累计 ⇒ 实际等价 `n0 − 2r ≤ 4`，剔除上限约为规则本意的一半（n0=10 例：应可剔 6，实剔 3）。
- 档位：S2（B 报告按 P0 呈报；缺"真实轮次被该闸截断的像素计数"一跳）
- 触发路径：`module_adapters.cpp:10865-10879`（按几何 n resolve，生产档 n≥6 → winsorized）→ `module_adapters.cpp:11111 p2_reject_stack_ex` → `rejection.cpp:2137-2140 reject_winsorized_impl` → `:1582` → 多出的离群样本进 `p2_integrate_pixel` 加权均值 → mosaic 的 signal/support 面被抬高。
- 同批必改：该闸在 `docs/science/REJECTION.md` 与 `docs/algorithms/REJECTION_ALGORITHMS.md` **零条文** ⇒ 必须先补条文再改实现，不许只改代码；改后过拒率类判据会变红（`REJECTION.md:68` 的 M3 基准 0.067% 属预期重测项）。
- 成稿锚：CR-22-23；打回报告 B §5.41 第 1 条。

### D-13 三类"应 hard fail"的状态被表达成成功码
- 事实：`p2_reject_stack_ex` 对非法输入／非法配置／非法方法一律返回 rc=0 并把原因写成"全 UNDERDETERMINED"，交付调用点只判 rc ⇒ 掩膜面把一次彻底失败读成"全部接受"。
- 档位：S2
- 成稿锚：CR-22-24；打回报告 B §5.41 第 2 条（同根两处之一）。

### D-14 排异工具在 resolve 之后覆盖 `normalization`
- 事实：`lib/algorithms/coverage/tools/rejection_cli.cpp:173-179` 用缺省串把 `extreme_value_clip_prior_sigma` 唯一合法的 `P2_NORMALIZE_NONE` 改回 `MEDIAN_CENTER`；该工具不暴露被覆盖方法所需的 `extreme_prior.*` 四键，且只复制了 percentile/rcr 两条互斥校验、漏掉第三条 ⇒ 方法 11 经该工具恒"全接受"。
- 档位：S3（生产侧由 `module_adapters.cpp:10858` 恒 AUTO 保护，不经此工具）
- 成稿锚：CR-22-26；打回报告 B §5.41 第 2 条（同根两处之二）。

### D-15 资格层对 support 做的是阈值判据，不是条文写的有限性判据
- 事实：`rejection.cpp:1349-1355` 的 support 档是 `support[i] > support_threshold` ⇒ `+Inf` 判合格并进候选栈（NaN 被归入 `invalid_support` 而非条文的"非有限 ⇒ INVALID_INPUT"）；support 还参与 `p2_integrate_pixel` 的 canonical max-support reducer 并写进产品字段。
- 档位：S2
- 成稿锚：CR-48-18。
- 需负责人裁（次级）：`docs/science/REJECTION.md §4` 的 support 语义应写成"有限性"还是"有限性 ∧ 阈值"。

### D-16 已禁用方法 `minmax` 的参数块仍被静默接受
- 事实：`stage2_common.cpp:250-256` 在 `method` token 面拒绝 `minmax`（FZ-REJ-NO-MINMAX），`:417-424` 却照样收 `rejection.minmax` 的三个参数键；`rejection_cli.cpp:145` 仍能点名该方法。
- 档位：S2
- 成稿锚：CR-48-11。

### D-17 各方法最小 n 与 AUTO 的 `NONE` 例外以实现为唯一注册表
- 事实：`rejection.cpp:982-999`（sigma 族/ESD/RCR/median_sigma=3、linear_fit=4、percentile=2、minmax=3、extreme=2、none=0）与 `:1184-1196` 的 NONE 例外在科学正本与算法文档无对应条款；`rejection.h:211` 的"注册表"即该函数自身（自指），`rejection.h:264-265` 把禁令写成绝对而实现带例外。
- 档位：S3
- 成稿锚：CR-22-22。

### D-18 V6 分类排异的八个常量只有键名与未签状态
- 事实：`rejection.cpp:2717-2721`（被 `rej_eq` 逐位钉死）、`:2773-2774`、`:2800`、`:2825`、`:2928/:2942/:2945` 的数值在权威链里零推导零标定；`recall` 实为剔除率（与统计学术语不同义）；`deleted`/`preserved` 允许同真；`bin_count × BINMIN` 合成"样本 <500 不可评估"的隐含下界未写进任何条文。
- 档位：S3（该函数在生产调用图外；`deleted/preserved` 若被下游用作删除决策即为无出处判据）
- 成稿锚：CR-22-20。

### D-19 公共头里排异默认值与路由档界的注释三处与实现相反
- 事实：`rejection.h:212` 的 `underdetermined_n` "默认 2" 与生产档 3 不合；`rejection.h` 把小 N 决策点的"当前实现"标反并按"待定"表述一个已定案的档界。
- 档位：S3
- 成稿锚：CR-22-14/CR-22-15/CR-22-19。

### D-20 RCR 折点搜索的哨兵与小栈阈值两版互不相同，大数值输入可越界读
- 事实：加权/非加权两版的 `min_error` 哨兵与"n≤3 阈值"不一致，大数值输入下 `m = -1` 直接索引数组。
- 档位：S2
- 成稿锚：CR-22-21。

### D-21 旧兼容模式用 `std::atoi` 解析方法号
- 事实：非法字符串静默得 0 = `P2_REJECT_NONE`，输出"全接受 + status=0"看似合法结果。
- 档位：S3
- 成稿锚：CR-22-27。

### D-22 k_corr 查表的门把"域外静默夹取"断言为期望，与正本直接相反
- 事实：`lib/algorithms/coverage/tests/kcorr_lookup_test.cpp` 把 pixfrac 维 clamp 判成期望行为，而正本合同规定域外一律取冻结默认、算法文档明文"禁止夹取"，用例名自带"不夹取"；其"独立硬编码参考"与被测实现同源，六个冻结表值在跟踪面无原始测量记录；条文冻结 `rtol 1e-12` 而门实现成绝对 `1e-12`（比冻结容差更严）。
- 档位：S2
- 成稿锚：CR-22-11（门与合同相反）、CR-22-12（同源）、CR-22-13（误红方向）；偏差量：控制点方差被低估 13.5%。

---

## D. 天光面求解、节点间距与求值哨兵

### D-23 §5d① 自校准栅栏在两帧输入下结构上不可能武装
- 事实（唯一自洽、可直接下的部分）：`lib/algorithms/coverage/include/astro/phase2/stage2_common.h:287-294` 的切分循环写作 `for (k = sorted.size(); k-- > 1;)`，`nf=2` 时帧对只有一个 ⇒ 循环体一次不执行 ⇒ 栅栏等于那唯一间距 ⇒ `sep <= fence` 恒真 ⇒ 两帧必并入同一指向；随后指向对循环不执行 ⇒ `band_trim` 空 ⇒ `overlap_band = frame_extent_median` ⇒ `constraining = min(extent, extent) = extent` ⇒ `upper = extent/2`。真实指向间距 `s ≤ extent`，故导出的 h 上界**偏大**，违反同头引用的 `docs/science/PHASE2_UPM.md §7a` 规则 1。
- 需负责人裁（**本点主裁项**）：两帧输入是否属该几何导出的合法定义域。
  - 若合法：`:346` 的 `nf < 2` 下限保留，缺口在"帧对数不足"必须有**具名降级路径**（不得静默按帧尺寸给上界），并给 `P2SkyPlaneGeometryProvenance` 加 `fence_armed` 位；
  - 若不合法：改在输入入口把两帧判为定义域外，`:346` 的下限与五份两帧 shipped config（`lib/algorithms/coverage/configs/stage2_overlap.example.json`、`stage2_real_overlap.json`、`stage2_real_overlap_cpu.json`、`stage2_t4_true_overlap.json`、`stage2_tiny_memory.example.json`）随之作废。
  - 两个方向的改动面互斥，本包不代为择一。
- 档位：**S1**
- 触发路径：`mosaic`/工具 → `module_adapters.cpp:9924 p2_sky_plane_geometry_from_controls` → `stage2_common.h:400-409` → `:466/:521` → `sky_plane.cpp:417-421` → `derive_node_spacing`（`upper = 0.5·min(...)`）→ `:10015 build_adaptive`。
- 成稿锚：CR-47-27；打回报告 B §5.46 第 ① 条。

### D-24 `max_nodes` 在两条入口取两值，内存截断与"判据不需要细化"共用同一采纳分支
- 事实：`sky_plane.cpp:467` 默认 2048、`stage2_common.h:88-90` 的 struct 初值 8192 且只有工具入口读它（`shipped configs`/`config_registry.json` 对该键零命中）；`sky_plane.cpp:647-652` 超限返回 `TOO_MANY_NODES`，`:1449-1452` 的细化探针把 rc=5 折成 `improved=false`，`:1467-1476` 采纳较粗解并 `break`，`representation_limited` 只在 `:1464`（到达分辨率下界）置位 ⇒ 最终 h 是判据给的还是门截的不可分辨。
- 档位：**S1**
- 成稿锚：CR-47-23；通读-CR-23 CR-23-09；打回报告 B §5.46 第 ② 条。

### D-25 约化 χ² 无定义时在天光面写成 `0.0`，同一事实两个求解器编码方向相反
- 事实：`sky_plane.cpp:1208` 在 `dof_eff <= 0` 时写 `chi2_red = 0.0` 且 `:1753` 无条件进产品 JSON（无 `defined` 伴生位）；`upm.cpp:1334-1338` 同条件写 `quiet_NaN()` 加 `chi2_red_defined=0` 并持久化 null。0 是 χ²_red 的下确界 ⇒ 任何"≤门即绿"读到"完美拟合"；且 `sky_plane.cpp:1451` 的 `improved` 因 `chi2_red > 0.0` 恒假而**永久关闭细化分支**。
- 档位：S2（接线即 S1 的条件已给：任一轮真实产品 provenance 出现 `chi2_red == 0.0`）
- 成稿锚：CR-23-14；CR-47-31；打回报告 B §5.17 第 2 条、§5.46 第 ③ 条。

### D-26 求值入口查不到瓦片覆盖元数据时回落到整张网格范围外推并正常返回 0
- 事实：`upm.cpp` 求值路径的"瓦片→格范围"查表命中失败时不报未覆盖，两轴各有一份"回落到全域范围"的插值/夹取；调用点把返回值当语句执行、不接返回码 ⇒ 模型内根本没有覆盖数据的瓦片以**外推来的**校正值出厂，产品面无任何读数区分"校正过"与"没模型所以凑了一份"。跨覆盖边界处用的正是该值。
- 档位：**S1**
- 成稿锚：CR-24-07（打回报告 B §5.30 已订正其机制描述为"回落到全域"而非"返回 0.0"）；打回报告 B §5.30 第 1 条。

### D-27 δ 分块求值把"越域/未知帧"落值为 0（合法校正量），块返回码在生产被丢弃
- 事实：`sky_plane.cpp:1592/:1649` 对非 OK 状态点写 `out_values = 0.0`，`:1594/:1651` 的 rc=2 在生产调用点 `module_adapters.cpp:10544-10552` 未被接住；无"未校正像素数"计数；`apply` 节点的诚实标记（`module_adapters.cpp:10692`）只写机制名不写覆盖比例。声明层 `sky_plane.h:458-469` 的标量越域 rc=0 与块越域 rc=2 两套。
- 档位：**S1**
- 触发路径：`mosaic` → `module_adapters.cpp:10541 sub_delta && sky_guard.m` → `:10544 p2_sky_plane_eval_delta_block` → `sky_plane.cpp:1620-1624` 越域分支 → `:1649` 落 0 / `:1651` rc 被丢 → `:10604` 写 tile。
- 成稿锚：CR-23-16；CR-47-17；打回报告 B §5.17 第 3 条。

### D-28 打开产品对 `degree/h/nx/ny/delta_order` 零校验；`degree ≥ 8` 栈越界，`h = 0` 静默夹紧求值
- 事实：`sky_plane.cpp:1842-1846` 从 JSON 读入即用于 `:1559-1561` 的 `int jx[8]`/`bspline_basis` 定长局部数组（`:105-106/:120-123`），build 侧 `:484` 钳到 {1,3} 而 open 侧无等价钳位；`h ≤ 0` 时 `span` 经夹紧返回"看起来合法"的面值并 `P2_SKY_EVAL_OK`。该中间产物无 schema 面。
- 档位：S2
- 成稿锚：CR-23-15。

### D-29 打开侧不做构建侧的非法值规整，缺键一律取合法值
- 事实：`p2_upm_open` 对 14 个求解器科学常数再抄一份"缺键默认"却不跑 build 侧规整；`model_hash` 缺键复原成 64 个 `0`，而全零串正是密集缓存判 stale 的唯一身份；`version/precision/target_order/control_count/observation_count/...` 缺键即取合法值，无必需键集合校验；`cell_index` 文件自报索引无上界校验（同函数对 `C`/`gauge` 都拒）。
- 档位：非 S1（本批 14 条仅 D-26 判 S1）
- 成稿锚：CR-24-01/CR-24-02/CR-24-04/CR-24-05；打回报告 B §5.30 第 2 条。

### D-30 MA 面收敛门是绝对判据且把三种量纲放在同一阈下；四条出口在产品面不可区分
- 事实：`upm.cpp` MA 面的收敛门与 polish 门按生产电平面级只能靠"增量恒等于零"退出；落盘结构里**没有"是否收敛"与"目标函数值"两个字段**，而审计字段清单点名要求它们；polish 环施加最多 8 步**无目标函数验收**的纯 Gauss–Newton 步；`ma_eig` 的收敛判据是"非对角元平方和恰好为 0"（无容差），而它算出的 `1/λ` 直接是对外发布的参数协方差。
- 档位：非 S1（同批随 D-29 处置）
- 成稿锚：CR-24-10/CR-24-10R/CR-24-11/CR-24-12；打回报告 B §5.30 第 3 条。

### D-31 同一产品里"未测/不可算"有三种表达，判据量缺失被填成 0
- 事实：`sky_plane.cpp:1888/:1899-1903/:1908-1910` 把缺失的 `kappa/lambda_*/dof_eff/rms_*/chi2_red/iterations` 一律填 `0.0`（κ≥1 数学上不可达 0、`lambda_min=0` 同时是"秩亏"与"缺失"两种语义），而 `:1890-1896` 的 `kappa_data/kappa_solve` 填 NaN；`rank_full`/`n_params_full` 不落盘；`normal_matrices` 对 open 模型返回 1 而头承诺 2。
- 档位：S2
- 成稿锚：CR-23-17。

### D-32 设计点名的 formulation 要素"参考平面用其他帧的加权组合（排除自身）"在天光面载体里不存在
- 事实：`sky_plane.cpp:804-843` 逐帧法方程装配把该帧**全部**自身采样点同时进 `H_data`/`rhs`，`:1027-1039` 的 δ_k 回代用同帧数据 ⇒ 是含本帧的联合解，不是 leave-one-out；`ASTROCS_DESIGN.md:411` 那句在主语上指参考平面构造，而 `docs/plugins/algorithms_phase2/11_upm.md` 全篇零次出现该词；仓库唯一叫"排除自身"的实现（`lib/algorithms/integration/v6/src/variance_propagation.cpp:145`）主语是叠加残差制造者/逐像素校正方差的分母。后果：单帧独占区里 `B_ref` 完全由该帧决定 ⇒ `δ_k ≈ 0` 且无判红。
- 需负责人裁（次级）：该要素约束的是参考面构造（补实现）还是被误挂（订正 `ASTROCS_DESIGN.md:411` 适用范围，走变更流程）。**两种实现是两回事，不能被一次改动顺带"视为已满足"。**
- 档位：S2
- 成稿锚：CR-23-19；打回报告 B §5.17 末段。

### D-33 自适应回路的两个声明常量一个是死档、一个无导出
- 事实：`sky_plane.h:266` 的 `P2_SKY_ADAPT_REPRESENTATION_LIMIT = 4` 全仓零赋值（实现写出的 action 只有 0/1/2/5，同一事实由 `representation_limited` 字段承载）；`P2_SKY_ADAPT_MAX_ATTEMPTS = 12` 无导出、运行期默认 6、数组富余 2 倍无出处、`max_attempts` 配到 >12 是否越界未证。
- 档位：S3（后者涉内存安全，优先级前移）
- 成稿锚：CR-47-19；CR-47-22。

### D-34 稳健宽度分位与切平面原点式无条文、且经纬度用两套不匹配的均值
- 事实：`stage2_common.h:510-511` 的 0.02/0.98 修剪头自称"2%–98% 稳健宽度"而无出处；`stage2_common.h:489-493` 的两指向切平面原点经度用三维向量和、纬度用正弦值的标量平均 ⇒ 原点一般不在两指向的大圆上，`overlap_band_width_deg`（→ h 上界）在纬差大时偏离真实投影宽度；`:270` 的 `|cosc| ≤ 1e-12` 退化分支把不可求值折成 `u=v=0` 且声明面未记该哨兵。
- 档位：S3
- 成稿锚：CR-47-30、CR-47-28、CR-47 表 `:510-511` 行（PARTIAL）。

### D-35 `smoothing_lambda` 两侧取值差一个数量级，冲突被写成"单一来源"且上呈锚文件不存在
- 事实：`stage2_common.h:22-24/:29` 称单一来源并指 `reports/RELEASE-02/conform-fix-b.md §009`（该文件既不在跟踪面也不在磁盘），`eng/packaging/config/config_registry.json:1878-1883` 的陈述是两条路径两个值（`auto`→0.1、键缺省→0.0）且 `registration:"none"`；`docs/algorithms/anchors/unresolved_registry.json` 对该冲突零命中；λ=0 是否合法只在代码注释里有裁决句。
- 需负责人裁（次级）：λ=0 是否合法（属 AGENTS §10"科学定义有歧义"）。
- 档位：S2
- 成稿锚：CR-47-26；CR-47-39。

---

## E. 采样器数值域、统计口径与可达性

### D-36 `inputs.target_order` 放行 0..29，而 leaf 寻址另加 9 阶
- 事实：`stage2_common.cpp:28-31` 只做 `0..29` 校验；`sampler.cpp:777` 的 `1u << (target_order + leaf_shift)` 在阶 ≥23 是未定义行为、阶 ≥21 越出写侧声明的 `nside ≤ 2^29`；`CellStat::tile` 用 `int` 承载 tile ipix 并在 `:1211` 回转 `uint64` ⇒ 阶 ≥14 即截断/符号扩展，第二遍 `std::map<int,…>` 邻域分组跨 tile 碰撞 ⇒ `controls[].ra_deg/dec_deg/leaf_ipix/tile_ipix` 全错而 rc=0。
- 档位：S2（域不一致与 UB 均 CONFIRMED，缺阶 ≥14 的实际生产用例）
- 成稿锚：CR-48-13；打回报告 B §5.53 第 ③ 条。

### D-37 同一 SNR catalogue 被两处不同上限读取，小上限一侧静默截断
- 事实：`sampler.cpp:78` 的 `1<<16` 经 `aio_hips_read_snr_catalog` 读取，返回值对"正好这么多行"与"被截断"不可分辨，取的是文件序前缀；`:406` 的身份哈希侧用 `1<<20` ⇒ 同一数据在"身份面"与"科学面"的覆盖集不同（换目录尾部会改 frame_id 却不改归约结果）。被截掉的尾部参与 catalog veto 与星点掩膜。
- 档位：S2
- 成稿锚：CR-48-04。

### D-38 k_corr 的定义域在采样侧入口不设防，逐帧标定分支生产恒不可达
- 事实：`sampler.cpp:517-518` 只挡 `<= 0`，`0 < k <= 1` 原样乘进 `control_variance`（方向：低估方差/高估权重）；`:96-99` 六格表唯一出处是被登记为**构建孤儿**的 `kcorr_matrix_test`，`:574` 的 `in_domain = [300,600]″/px` 与生产实测 0.8064″–0.9890″/px 差约 300 倍 ⇒ 逐帧标定生产永不执行；裁决 SC-ADJ-F05.4 明文"可复跑标定落地前禁止外推/内插"，而 `:110-116` 正是内插；`docs/contracts/DATA_SEMANTICS.md:1739` 仍写"逐帧查表优先于该值"而无后果句；`sampler.h:54-55` 的对外声明零提及该优先级链。
- 档位：S2
- 成稿锚：CR-48-03、CR-48-02、CR-47-2；打回报告 B §5.53 第 ④ 条。

### D-39 `min_samples` 默认值使方差公式跑在权威文档自己量化的失真域内
- 事实：`sampler.h:37/:51` 把 `control_variance = k_corr·(π/2)·σ_bg²/N_retained` 与默认 5 并列声明而不携带 N 下限；`docs/algorithms/PHASE2_SAMPLER.md:267-274` 实测 `N=5` 时渐近式**低估方差 8.5%**、`N ≥ 65` 才 <1%；`docs/science/NOISE_MODEL.md:52/:369` 给的是另一适用域的 64。文档把该偏差写成"属保守方向"，方向标反。
- 档位：S2（任一轮真实数据里 `N_retained ∈ [5,64]` 的 control 计数 >0 即 S1，而 `P2SampleStats` 九列里没有该计数）
- 成稿锚：CR-47-1。

### D-40 背景亮度容差门在邻域不足时静默免检
- 事实：`sampler.cpp:1025-1034` 邻域 <3 先回退到该 tile 全部候选，仍 <3 则 `continue` ⇒ 该 (cell,frame) 观测不经亮度判据即保留，`rejected_bright_tolerance` 不计数、无"门被跳过"事件码；`sampler.h:46/:74` 把该门写成无条件。
- 档位：S2
- 成稿锚：CR-47-11。

### D-41 声明为"共用同一实现"的统一统计量实际是三套内联拷贝
- 事实：`sampler.h:101` 称 patch estimator / MAD / SNR 邻域共用 `p2_stats_median/p2_stats_mad`，实测 `sampler.cpp` 内两者无内部调用（唯一消费者是 `tests/synthetic_gate.cpp`），patch estimator 用 `median_of`（`:199-212`），SNR 邻域容差门用第 3 种取位（`:1035-1040`，偶数 n 取上中位）且 `1.482602218505602` 在 `:470/:844/:859/:1040` 各写一遍 ⇒ 对该族符号的 Oracle 断言判不到产品路径。
- 档位：S2
- 成稿锚：CR-47-5。

### D-42 同一"帧 SNR 中位数"在同文件两种算法
- 事实：`sampler.cpp:657` 的秩统计量 `cp[n/2]`（偶数取上中位）用于 veto 与星掩膜，`:658` 的真中位数用于 snr 字段回退 ⇒ 上中位 ≥ 真中位 ⇒ 阈值偏高 ⇒ veto 偏松（漏排少数污染 control）。
- 档位：S3
- 成稿锚：CR-48-06。

### D-43 tile 读失败被记成"支撑不足"，且不使本次采样失败
- 事实：`sampler.cpp:763-766` 的三种 rc（空指针/resize 抛/aio 读失败）与"有效样本 < min_samples"共用 `reason=1` 与同一个 `insufficient_support` 计数器，只 `fprintf`；`pass1_cell` 仍 `return 0` ⇒ 部分读失败时观测静默减少而命令判绿（全零观测由 `upm.cpp:258/:2450` 兜住，部分丢失无兜底，该 control 只剩平滑先验决定 C 场）。
- 档位：S3
- 成稿锚：CR-48-05。

### D-44 V6 摘要面的分位数取法与两个对外散布量无条文
- 事实：`sampler.cpp:1396-1401` 的"最近下邻不内插"使发布的 p95/p05 偏小；`:1411-1417` 的 `max_systematic_deviation = max|v−p50|` 把随机散布与系统偏差混为一谈（偏大）、`model_error = RMSE(v 关于 p50)`；条文只写"必须带"未给计算式，两字段方向相反。
- 档位：S3
- 成稿锚：CR-48-08。

### D-45 采样器入口不校验合同值域，越域配置静默生效
- 事实：`sampler.cpp:503-522` 的入口修补只做下端保护，无任何上界或值域不变式 ⇒ 合同在 `stage2_common.cpp` parser 面声明的域在 C API 面不是不变式；`k_corr` 定义域（D-38）与本条同形态。
- 档位：S2
- 成稿锚：CR-48-32（该门的夹具正依赖此缺口构造非退化输入 ⇒ 同批项）。

### D-46 `cpu_workers` 的 `0` 在文案、实现与合同三面各异
- 事实：`stage2_common.cpp:486` 文案写 `0=auto`，`sampler.cpp:927` 把 0 走成串行；`tools/stage2.cpp:281` 直传原始值而其余阶段用 `effective_cpu_workers()`；`DATA_SEMANTICS.md:1976` 登记的来源是 runtime budget（只在 scheduler 路径）。
- 档位：S3
- 成稿锚：CR-48-15、CR-48-30；通读-CR-21 的 `execution_options_test` 族同族条目。

---

## F. UPM 标度、归属与收敛证据

### D-47 跨 tile 邻接的 RA 粗筛未按 `cos(dec)` 折算
- 事实：`upm.cpp:459` 的粗筛未折余弦 ⇒ `|dec| ≳ 51.32°` 天区的跨 tile 平滑邻接被系统性丢弃，"跨 tile 连续性由平滑邻接提供"在该天区不成立。
- 档位：**S1**（直指创新点③的无接缝主张）
- 成稿锚：CR-25-01；通读-CR-25 本批最高危第 1 条。

### D-48 观测→frame/control 归属用 `std::map::operator[]`，缺键静默落到 index 0
- 事实：`upm.cpp` 的 `:483-484/:629/:649/:813/:824/:827/:837/:925/:937/:946` 全部为非 const `operator[]`；`:371` 有合法的 `continue`（obs 推出的 `(tile,gx,gy)` 不在 `cell_index` 时该观测被排除于节点），但它仍进 `sums[ck]`（`:649`）与 objective（`:1023`）⇒ 归一化分母与目标函数含未被任何方程接受的观测；并行分支对同一 `std::map` 调 `operator[]`，一旦插入即数据竞争 UB，直接违背同文件 `:610-613` 的"位精确重复"主张。
- 档位：S2
- 成稿锚：CR-25-04。

### D-49 `chi2` 分子与 `dof_eff` 分母的观测集合不一致
- 事实：`upm.cpp:1240/:1245` 的分子只累加权重 `>0` 且有限的观测，`:1330` 的分母用全量入参 `n_obs` ⇒ `chi2_red` 系统性偏小（方向确定：偏绿），产品字段 `identifiability.chi2_red` 与对外读数失真；现有持久化门只断言"键存在"从不比数值。
- 档位：**S1**
- 触发路径：`tools/stage2.cpp:412-425` → `p2_upm_build_geo`（`upm.cpp:1377`）→ `build_impl` → `:1235-1330` → `p2_upm_save:1442` → 产品 `p2_upm_model.json`/manifest；生产输入里 `photo_rejected` 观测由 Phase1 SNR 产品 `quality_flags` 位 16 供给（`DATA_SEMANTICS.md:445`）。
- 成稿锚：CR-25-10。

### D-50 同一"权重和是否有效"的判据在同文件取了尺度无关与绝对两种形态
- 事实：`upm.cpp:668/:1127` 用 `s > 0 ∧ isfinite(s)`，而 M 更新分母 `:817/:833/:842/:869/:895`（含并行支）仍与绝对 `1e-12` 比较；跌破时不更新 `M[k]`、不记录、不计数，该节点的 `max_dM` 恒为 0 ⇒ `converged=1` 可覆盖"存在被冻结节点"的事实（偏绿）。
- 档位：S3（现网标度不可达，属"已固化但暂不触发"）
- 成稿锚：CR-25-09。

### D-51 设计点名的 per-control 可靠度只有接口，实现是全局标量常数
- 事实：`upm.cpp:72/:341-342` 给每个节点赋同一个 `cfg.control_reliability`，`P2ControlNode`/`P2ControlObservation` 无 per-node 入参 ⇒ `PHASE2_COVERAGE.md:193`、`PHASE2_MOSAIC_WRITE.md:24`、`DATA_SEMANTICS.md:1271` 的"按几何可靠度加权"在本模块为空操作，而 `model_hash` 与文档口径都当它存在。
- 档位：S3（已登记 SC-005）
- 成稿锚：CR-25-05。

### D-52 "未测"被写成合法数值：首轮相对改善与分量数
- 事实：`upm.cpp` 首轮 `rel_improve` 强制 1.0、`component_count` 系初值为 1。
- 档位：S3
- 成稿锚：CR-25-08。

### D-53 不可辨识/不收敛在空间 UPM 面 `return 0`，且点名警告四键无承载
- 事实：`upm.cpp:1369` 判红仍 `return 0`（同一"唯一判据"在 MA 面是 fail-closed rc=3）；`p2_upm_save` 未写 `docs/plugins/algorithms_phase2/11_upm.md:92` 点名的顶层 `warnings[]`/`warning_codes[]` 等四键 ⇒ "判红不阻塞"退化为"静默放行"，`11_upm.md:158` 要求的"欠定能红"在产品面无处可判。
- 档位：S2
- 成稿锚：CR-25-11＋CR-25-13（合案）；B §5.30 对四键的订正：无警告时确实写空数组，残余缺陷只剩"缓冲区不足时四键整体消失"（`upm.cpp` 的 `p2_upm_warnings_json`）。

### D-54 `tolerance_relative` 一处显式设、一处漏设回落违禁绝对判据
- 事实：`module_adapters.cpp:9571/:9573` 设 `tolerance_relative=1`，`lib/phase2_session/p2_session.cpp:204` 只设 `tolerance` ⇒ 该处按 `upm.h:113` 的默认 0 走绝对判据；按生产尺度 `scale_obs = 5.262929e13` 复算，绝对阈 1e−6 = `8.557e−5 × ulp`，**原理上不可达** ⇒ 只能落 2 或 0；该键在 `defaults.json`/CLI 白名单/phase_config schema 全无登记，session 侧覆盖块（`:212-218`）不读它 ⇒ 传该键被静默忽略。
- 档位：S2（不构成活的 fail-open：`p2_session` 旧通道不在生产调用面上，属 ARCH-DEBT-01）
- 修法边界：最小正确修法**不是**给违禁路径补一行配置（等于巩固它），而是随 ARCH-DEBT-01 下线该通道，或与适配器共用同一冻结装配函数。
- 需负责人裁（次级）：`p2_session` 通道的去留（与架构整治合案处置）。
- 成稿锚：天光无缝核验报告 DEV-07；链路间口径对表 §7（`tolerance/rank_rtol` 行）。

### D-55 收敛四态的"停滞"态无经验证据且配置面无法驱动
- 事实：`kStallPatience=5`、`kObjImproveFloor=1e-12` 是 `upm.cpp:732-733` 函数体内 const，配置面无法把任何算例逼进态 2；跟踪结果件里观测到的收敛态只有 {0,1}，被叫作 stalled 的臂实测迭代 300 次即耗尽（态 0）。属**证据缺口**，不属代码缺陷。
- 需负责人裁（次级）：状态 2 在当前生产判据下接近不可达，是否保留该态。
- 成稿锚：天光无缝核验报告 DEV-08。

### D-56 三个无锚量直接决定对外主张
- 事实：邻接粗筛的放大系数 `1.6`（全仓无出处，仅两处文档复述，它决定 D-47 那条高纬断裂的宽度）、质量标记降权的 `0.1/0.5` 两档权重值（无条文无标定）、目标下降门限 `1e-12` 与两组共轭梯度容差 `1e-30/1e-24`（低于双精度可达范围，属永不误触发形态）。
- 档位：常数支撑缺陷（打回报告 A §3.8 优先项，牵动对外主张者排在纯数值容差之前）
- 处置：一律按标准 02 §4 第 4 类登记"待确认＋保守方向＋影响范围"，登记在离值最近处（代码注释＋登记面＋该量文档条目三处同址）。
- 成稿锚：CR-25-02；打回报告 A §3.1／§3.8 末段。

---

## G. 接缝判据与其证据面

### D-57 出厂接缝门的判据非符号定界，"1.005% 必红"的成立条件未写
- 事实：`eng/tools/e2e/seam_footprint.py:336-363` 的分子是有符号电平差，梯度项与台阶代数相加 ⇒ 反号相消；实测注入门自己声明的确定性下限 `Δ/L = 1.005%` 在读到同量级反号梯度后给出 `−2.7799e−3`（仅门限 27.8%）判**绿**，1.50% 仍绿、2.00% 才红 ⇒ 实际漏检面抬到 ≈**1.73%**，而 `docs/science/PHASE2_UPM.md §17.3` 登记的 5 条漏检面里没有"梯度反号相消"这一条；`PHASE2_UPM.md:449` 的"Δ/L > 1.005% 必判红"须限定为"梯度项为零时"。
- 档位：S2（门禁判据缺陷，方向为**误绿**；误红方向余量另见 D-58）
- 成稿锚：天光无缝核验报告 DEV-02；论文3 §6.3 第 2 项；链路间口径对表 §7（`1.005%` 行，真互斥-资格）。

### D-58 门自检用例缺"相干梯度无台阶必须绿"这一例
- 事实：`seam_footprint.py:163-173` 的 `REQUIRED_SELFTEST_CASES` 九例（S1–S9）能红能绿，但没有"无台阶的相干背景结构必须绿"这一例（S1 夹具的整体斜坡只贡献门限的 0.67%）；门对法向梯度线性响应 `|rel_step| = 4g/bg`，M42 实测最陡边读到门限的 72.6%（余量 1.378×）⇒ 真实场偏**误红**。
- 处置：不放松门限；把"相干梯度无台阶必须绿"与反号相消算例补进 `REQUIRED_SELFTEST_CASES`。
- 档位：S2
- 成稿锚：天光无缝核验报告 §4「门自检用例 S1–S9」行、DEV-02。

### D-59 门限 `1e-2` 的标定证据与标定脚本不在跟踪面
- 事实：`run/SEAM-DERIV-01/**` 全在 gitignore 的 `run/*` 下、`git ls-files` 对 `SEAM-DERIV` 零命中 ⇒ 门可用 `--self-test` 复跑，**门限推导不可复跑**；同族缺失还包括 `evidence/e3e4_floor_structure.json`、`evidence/e3c_weight_pollution.json`（`docs/algorithms/PHASE2_SAMPLER.md:292/:297` 的"实测"字样无归档件）。
- 档位：S2（证据落档面）
- 需负责人裁（次级）：可跟踪证据落位（台账 §4.2 的核-6）。
- 处置：门的汇总读数 JSON（几百字节）登记进 `artifacts/ci/<sha>/` 或 `实验/*/results/`，文档只引用跟踪路径。
- 成稿锚：天光无缝核验报告 DEV-03；链路间口径对表 §7、§8"证据落档缺失"。

### D-60 接缝度量侧把上游 NaN 填 0 后计入且不计数
- 事实：`eng/tools/e2e/seam_footprint.py:386` 与 `eng/tools/e2e/render_vis.py:458` 把非有限面积/support 值填 0 后直接进接缝度量，而同一 NaN 档在 `sampler.cpp:820-821` 被 fail-closed 跳过 ⇒ 同一哨兵在一条链被拒、在两处被当 0 计入创新点③的门。
- 档位：S2（真互斥-下游处置，链路间口径对表 §6）
- 处置：统一到"要么计数、要么拒"，不得静默。

### D-61 同一处读数的两个百分比口径未同步
- 事实：门脚本 docstring 写"最陡处达门 78%"，正本 `PHASE2_UPM.md:474/:478` 写 `max|rel_step| = 7.2561e−3`＝门的 72.6%。以正本值 72.6% 为准；78% 登记为又一处口径未同步。
- 档位：S3
- 成稿锚：天光无缝核验报告 DEV-02 第 ④ 项。

### D-62 验收主张与门证据不是同一统计量
- 事实：`docs/science/PHASE2_UPM.md` 的"天体结构假阳性可区分"主张由 `实验/additive-sky-seamless/code/sci_c_common.py:356-410` 的去趋势＋off-locus excess 口径作证，而出货门 v2 已主动移除 off-locus（`seam_footprint.py:36-53`，理由＝修周期盲区）⇒ 主张与证据不同源、不同统计量；同一无接缝结构上两统计量最多差 48–∞ 倍（线性斜坡：门读 7.2561e−3、实验判据读 0）。
- 档位：S2
- 处置：验收证据里把"结构可区分"限定为需与 `step_net`、半距 d 扫描联合判读，并明写 C4 N5 的 off-locus 结论不自动迁移到 boundary-only 门。
- 成稿锚：天光无缝核验报告 DEV-02 第 ①/③ 项；论文3 §5.4。

---

## H. 声明面、登记面与文档卫生

### D-63 创新点③④的对外声明面在合同面零登记
- 事实：`docs/contracts/API_CONTRACTS.csv` 对 `sky_plane.h` 的 17 个 `P2_API` 零行、`docs/development/CONFIG_SCHEMA.md` 对整个 `sky_plane` 配置块零登记，而生产实际消费的键集是 `module_adapters.cpp:9868-9998` 的 14 个；`eng/tools/quality/contracts/check_api_contracts.py:58` 的核对方向是"表→头"单向 ⇒ 活符号不入表恒绿。
- 档位：S2
- 处置：登记面核对方向必须双向（表里有而代码无 ⇒ 红；代码有对外接口而表里无 ⇒ 红），但**先全仓补登记再改门**，否则一次性判红。
- 成稿锚：CR-47-16；CR-47-42（合同表仍把已退役符号 `p2_upm_normalized_weights` 记为 VERIFIED，而三个已接线的诊断 API 一行没有）；打回报告 B §5.46"合同面反向失守"段。

### D-64 同一结构字段在四个侧面取四套数
- 事实：`spline_degree` 在声明头 1、默认函数 3、非法修补 1、生产入口内联默认 3；`max_nodes` 头 8192、实现 2048、零初始化 4；`sky_plane.h` 自报"默认值见 `default_config`"而未说零初始化不等价 —— 实测 8 个字段 `{}` 与 `default_config` 落点不同，其中 `frame_gradient_order` 直接换模型。
- 档位：S2
- 成稿锚：CR-47-13、CR-47-14、CR-47-15。

### D-65 `sky_plane.enabled` 的注释与三条侧面相反
- 事实：`stage2_common.h:71` 写"缺省关闭…保持 RELEASE-01 基线不回归"，而 `:74` 初值 true、parser 行为与 `config_registry.json:1863-1871` 的陈述、`module_adapters.cpp:9835-9839` 的 `delta_wanted` 缺省链三侧均为默认开。
- 档位：S3
- 成稿锚：CR-47-29。

### D-66 `additive_mode` 的四处注释/文档口径为假，行为面正确
- 事实：`module_adapters.cpp:9820`"生产默认 additive_mode = c、δ 从不施加"为假（默认 delta、δ 在默认路径确实施加，且它自称的锚点 `:5503` 已漂移到 `mark_frame_fail`）；`:10300` 与 `docs/modules/phase2_upm.md:75-76`、`docs/plugins/algorithms_phase2/11_upm.md:14/:37/:132` 把 `c` 说成"全减含 B_ref"为假。同一符号 `C_k` 在三层文档＋一处代码注释被读成"全量"、在科学正本与实现里是"帧分量"。
- 档位：S3（口径缺陷，**不动码**）
- 处置：按 `docs/modules/phase2_upm.md:70-72` 的强制消歧规则一次批量订正到 `C_f = 帧分量（仅偏差）`／`b_k = 全量`；在变更流程里把"记法消歧"列为机器检查项（凡 `raw − C_k` 必须带限定词）。只改 `:10300` 一行会留下 `11_upm.md` 三处。
- 成稿锚：天光无缝核验报告 DEV-04／DEV-05。

### D-67 文档滞后于代码与行号锚成片漂移
- 事实：`docs/algorithms/PHASE2_UPM_IMPL.md §F2/§3` 仍写绝对归一化门 `s > 1e-12` 并仍列已删除的 `p2_upm_normalized_weights` 为有效导出符号；`PHASE2_UPM_IMPL.md:11/:12`、`PHASE2_SAMPLER.md:15` 的文件行数与实测不符（称 2793/384/1536，实为 2981/450/1503）；科学/算法文档指向 `upm.cpp`、`rejection.cpp` 的行锚系统性漂移（抽样偏移 +20 到 +345 行；CR-24 本批实测 5 对）；`DISP-P2UPM-006`、§16.3 的锚也已漂移。按文档实现会重新引入 100% 权重清零的根因。
- 档位：S3（卫生缺陷，但它是"沿索引下钻"读法失效的原因）
- 处置：行号锚改用**符号锚**（函数名）而非绝对行号；文档侧写"必须"的两条按现行代码口径订正。
- 成稿锚：天光无缝核验报告 DEV-06／DEV-14；CR-24-13；打回报告 B §5.41"移交线索"段。

### D-68 头注释的"实测/裁决依据"落在不入库面与已清理控制包
- 事实：`rejection.h` 的三处冻结判定出处指向 `run/` 与已清理的控制包目录 ⇒ 跟踪面上无可复核材料，冻结判定的出处链在字面上就断。
- 档位：S3
- 成稿锚：CR-22-16。

---

## I. 权威条文互斥（上呈类）

### D-69 "拟合权重 = 叠加权重同源"与另一条已裁科学条文正相反
- 事实：`upm.cpp:714-715` 与实现 `:1110-1128`（末端 `R_k = Σw(y−C)/Σw` 用同一 `w`）主张拟合权重与叠加权重同源；`docs/science/PSF_SIGNAL_WEIGHT.md:128` 逐字规定「**拟合权重 ≠ 堆叠权重**：带杠杆 `h` 的拟合值方差为 `σ²h`，不能按 `1/σ²` 当独立测量堆叠（实测方差高 52.3%，解析 50.4%）；等杠杆时两者严格等价」；第三侧 `docs/plugins/algorithms_phase1/07_noise_snr.md:162` 又给另一种定义（`w_i = (v_med/v_i)²`）。UPM 的权重逐观测不等（`control_ivar` 各不相同）⇒ "等杠杆"的等价条件不满足。
- 需负责人裁：两份权威互相打架，按 AGENTS §8 先派独立查证（加权均值与 leverage 的方差传播一手文献＋SCAMP/SWarp 的 gauge 处理核对），仍不收敛则上呈择一。
- 档位：S2（本段写不出"该 G 场进入产品后被哪条判据度量"的完整链）
- 边界：本条只影响 `02` §1.4 恒等式的**适用条件**，不影响该恒等式在"末端扣除场用叠加权重"前提下的代数成立。
- 成稿锚：CR-25-16。

---

## 汇总

| 档位 | 条数 | 编号 |
|---|---|---|
| **S1** | 11 | D-01、D-03、D-06、D-07、D-11、D-23、D-24、D-26、D-27、D-47、D-49 |
| S2 | 34 | D-02、D-04、D-05、D-08、D-10、D-12、D-13、D-15、D-16、D-20、D-22、D-25、D-28、D-31、D-32、D-35、D-36、D-37、D-38、D-39、D-40、D-41、D-45、D-48、D-53、D-54、D-57、D-58、D-59、D-60、D-62、D-63、D-64、D-69 |
| S3 | 20 | D-09、D-14、D-17、D-18、D-19、D-21、D-33、D-34、D-42、D-43、D-44、D-46、D-50、D-51、D-52、D-61、D-65、D-66、D-67、D-68 |
| 非 S1（随 CR-24 成批，本包未取到逐条定级） | 2 | D-29、D-30 |
| 证据缺口（不属代码缺陷） | 1 | D-55 |
| 常数支撑缺陷（打回报告 A 口径） | 1 | D-56 |
| **合计** | **68** | D-01…D-68 |
| 标"需负责人裁" | 8 | **D-23（本点主裁：两帧输入是否属几何导出的合法定义域）**、D-15、D-32、D-35、D-54、D-55、D-59、D-69 |
