# AUD-101 文档审计批次派单前言（所有 DB/DA 批次共用）

先完整读本文件，再读你的批次清单。你的批次编号在派单里给出，记作 `<BATCH>`。

## 1 环境与硬纪律

- 仓库：`F:\Astro dev\Astro CS Normalization Database`（**路径含空格，命令一律加引号**），冻结基线 HEAD=`c8f64e9a`。
- 本机是**独立只读审查节点**。不得修改、移动、删除仓库任何文件；零 git 写（只允许 `status`/`ls-files`/`grep`/`log`/`show` 这类只读命令）。
- **禁止执行**：构建、`ctest`、`run_checks.py`（任何参数，含 `--check`/`--self-test`）、任何 `eng/**` 下的检查器或生成器脚本、三命令端到端。负责人裁决：本阶段一切测试与端到端暂停，找错只靠读与推导。仓内有会覆写跟踪件的检查器，执行即污染被审对象。
- 不得 `import`/`importlib` 加载仓库内 Python 文件（生成 `__pycache__` 污染工作树）；需要值就读文件文本。
- 开工先跑 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 记基线（当前树应恰有两个未跟踪项：`?? ACSD整治工作包_AUDIT-06.zip`、`?? site/`）；收工再跑一次，把两次输出原文写进成稿。**出现任何新增项 = 你的取证动作弄脏了被审对象**：立即停用该动作并在成稿登记。

## 2 判据（先读，绝对路径）

1. `F:\Astro dev\独立审查\工包-AUDIT-06\ACSD整治工作包_AUDIT-06\standards\01_文档权威链整治标准.md` —— 全文，尤其 §2 角色表、§3 双向索引与唯一索引地图、§4 写法、§5 产出要求。**这是你的判据来源。**
2. `...\ACSD整治工作包_AUDIT-06\tasks\AUD-101_文档现状全量审计.md`
3. `...\ACSD整治工作包_AUDIT-06\templates\TPL_文档审计台账.md`（产出格式）
4. 上游正本 `ASTROCS_DESIGN.md`（至少读目录、§0、§8）——判"角色错放/层级倒置"要拿它比对。

## 3 范围与越界

- 你的范围 = 批次清单 `独立审计/批次清单/<BATCH>.txt` 里的每一行（仓库相对路径）。
- **清单是唯一范围依据**。派单文字与清单不符时以清单为准，并在成稿里指出差异。
- 不得代审别批文件。为判重复与上下游**可以读**别批的相关节，但只对清单项下结论。
- 清单里每一份都要**通读全文**。短文件不得跳读；长文件（>600 行）也要读完，读不完就登记为未读，不许猜。

## 4 每份文档的登记字段（顺序与列名固定，便于机器合并）

`路径` | `标题` | `行数` | `角色` | `主题` | `上游` | `下游` | `是否正本` | `重复或重叠对象` | `元信息块` | `写法违规` | `悬空引用` | `与上位冲突` | `处置建议`

- `角色` 用标准 01 §2 表里的角色名（科学定义 / 算法推导 / 架构设计 / 模块工作细节 / 合同说明 / 标准与检查 / 研究与佐证 / 操作与运营 / 负责人面），并标出它实际所在目录与应属目录是否一致（不一致 = 角色错放，给"应迁往 <路径>"）。
- `上游` = 该文档自己标明的最高设计条款；没标就写 `缺`（标准 01 §3 要求每份下层文档开头标明上游条款）。
- `下游` = 实际 grep 出的引用者列表；0 个则写命令与 0 命中证据（孤儿判定见 §5）。
- `重复或重叠对象` = **必须贴两处实际段落**（各带 `路径:行`）并说明重叠范围与差异；一份是另一份的下钻属合法分层，不算重复。
- `写法违规` 逐条给 `行号 + 原文片段`：日期、任务编号（EXP-/RELEASE-/FIX-/AUD- 等）、commit sha、sha256、"旧版/作废/废止/留痕/曾/原先/上一版"等历史叙事、开头元信息块。
- `悬空引用` = 指向不存在的文件或章节，逐条给原文与该路径在你机器上确认不存在所用的命令。
- `处置建议` ∈ {保留, 合并, 删除, 迁移} + 目标路径 + 一句话理由（理由必须引标准 01 的具体条款，不得写"看起来重复"）。

## 5 判定纪律

1. **不许凭印象判重复、凭命名判层级、凭注释判语义。**
2. 报"0 命中/无人引用/不存在"之前，先把检索词换两三套再试，并**拿同一条命令在一个已知应命中的对象上跑通**。本仓已知陷阱：路径由多个字面量拼成时整串检索不到；`rg` 在本机不可用（用 `git -C "..." grep`）。
3. 所有计数自己跑确定命令得出，并在条目里附该命令。别人口头汇总的数不得直接写进成稿。
4. 机械线索文件 `独立审计/批次清单/doc_mechanical.csv`（列：path,lines,h1,meta_block,upstream_pointer,dates,task_ids,commit_sha,sha256,history_narrative,dangling_links,unregistered_index）可以当**线索**。注意它是保守口径：以 `..` 开头的相对链接未判（**低估**悬空），7–8 位十六进制词未必是 commit（**高估**）。凡你发现的与机械列不一致处，显式写"机械列漏报/误报了 X"——你在校准工具，这本身就是交付物。
5. 判"同一主题两份正本"是本任务最高价值的产出之一，但要按 §4 的举证要求做；举不出两处段落的，降为"疑似重叠，待证"。
6. 状态词不得跨轴串用：`未实现` ≠ `未接线`（库里有但生产零引用）≠ `已退役` ≠ `文档没写`。写错档即缺陷。

## 6 输出契约

- 写到：`独立审计/证据/AUD-101-<BATCH>.md`（bash：`/f/Astro dev/独立审计/证据/AUD-101-<BATCH>.md`）。**只写这一个文件**，别碰 `整改/` 下的共享台账（避免与并行代理争写）。
- **每完成 3–4 份文档就 append 一次**（不要攒到最后一次性写）。文件末尾每次更新都留一行 `<!-- PROGRESS: 已审份数/清单总份数 -->`。中断只损失当前段。
- 台账之后附四节：
  1. `同主题组与差异`（跨批发现也算，但要注明别批路径）
  2. `索引与链接缺陷清单`（含与 `docs/DOCUMENT_INDEX.yaml` 的登记不符项：未登记 / 登记了但文件不存在 / 角色标注与你的判定不符）
  3. `覆盖率自报`：已审 N / 分配 M，并给出**未逐行读的名单**（名单非空即视为本批未完成，写明卡在哪）
  4. `我证伪了任务书的哪个前提`（AUD-101 任务书里的前提，如"约 233 份"，与你实际数到的值；找不到不成立的前提就如实写"未证伪"）
- 文风：只写现行判定，不写过程流水；不写"我接下来要"。
