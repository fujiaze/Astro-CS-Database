# AUD-101 / 批次 DA-01 文档现状审计台账 —— 仓库根规范（9）+ `docs/science/`（17）

审计节点：独立只读审查节点。判据 = `standards/01_文档权威链整治标准.md`（下称"标准 01"）；格式 = `templates/TPL_文档审计台账.md` + 派单要求的 16 字段。
分片清单：`独立审计/批次清单/DA-01.txt`（26 行）。

## 0. 基线、清单核对与计数

**开工 git 基线**（`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

`git rev-parse --short HEAD` → `c8f64e9a`。**恰两行，与派单预期一致。**

**清单核对**：派单说 DA-01 = 根 9 份 `.md` + `docs/science/` 17 份 = 26。逐行核对清单文件，与派单文字**完全一致，无差异**：

```bash
R="F:/Astro dev/Astro CS Normalization Database"
git -C "$R" ls-files '*.md' | grep -v /            # → 9 行，与清单前 9 行逐字节相同
git -C "$R" ls-files docs/science                  # → 17 行，与清单后 17 行相同
```

**对任务书 AUD-101 前提的实测校正**（派单要求显式指出）：

| 任务书原话 | 实测 | 命令 |
|---|---|---|
| "以 `docs/` 为重点（当前约 **233 份**、**30+ 子目录**）" | **233 成立**；**"30+ 子目录" 不成立，实为 23** | `git -C "$R" ls-files docs \| grep -c '\.md$'` → 233；`git -C "$R" ls-files docs \| awk -F/ 'NF>2{print $2}' \| sort -u \| wc -l` → 23 |
| （未区分口径） | `docs/` 跟踪文件（含 `.yaml/.csv/.json`）= **258**；全仓跟踪 `.md` = **540** | `git -C "$R" ls-files docs \| wc -l` → 258；`git -C "$R" ls-files '*.md' \| wc -l` → 540 |

`docs/` 一级子目录实测 23 个：`algorithms api architecture audit browser ci contracts design development diagnostics interfaces modules operations owner performance plugins quality references research science standards traceability validation`。

**清单外但被本批文档大量引用的目录**（不代审，仅作下游/重叠取证）：`docs/standards/`（14 份 `.md`）、`docs/algorithms/`（28）、`docs/plugins/`（24）、`docs/modules/`（49）、`docs/contracts/`（15）、`docs/audit/`（**0 份 `.md`**，只有 csv）、`docs/traceability/`（1）、`docs/operations/`（1）、`docs/references/`（2）。

---

## 1. 逐份登记

### 1.0 一览（模板 §1 的一行一份；详细判据见 1.1–1.26）

角色取值：科学定义 / 算法推导 / 架构设计 / 模块工作细节 / 合同说明 / 标准与检查 / 研究与佐证 / 操作与运营 / 负责人面（★ = 落不进该表九格；⧉ = 一份跨多格）。
处置取值：保留 / 合并（注明并入）/ 删除 / 迁移（注明目标）。

| 路径 | 标题 | 行数 | 角色 | 主题（一句话） | 上游 | 下游命中 | 正本 | 元信息块 | 日期/编号/sha/历史 | 处置 |
|---|---|---|---|---|---|---|---|---|---|---|
| `ASTROCS_DESIGN.md` | 最高设计（Design Authority） | 826 | ★顶层决策 | 是什么·四创新点·顶层架构·CLI·发行·验收 | 链顶 | 415 | 正本 | 无 | 0/3/0/0 | 保留（公式下沉） |
| `AGENTS.md` | 机器干活手册 | 237 | ★标准与检查（准） | 读文档顺序·环境构建·工作流·纪律·目录速查 | 弱（§1 第1条） | 139 | 正本（3处系 ENGINEERING 副本） | 无 | 0/0/0/0 | 保留（3处改指针） |
| `ENGINEERING_SPEC.md` | 工程规范 | 272 | ⧉标准与检查+运营+合同 | 语言·风格·红线·模块·测试·Git·目录·文档集·门禁·日志·命名 | **缺开头行** | 161 | 正本（§6/§7/§8/§15） | 无 | 0/6/0/15(皆现行条款) | 保留（补上游行） |
| `ACCEPTANCE_SPEC.md` | 验收规范 | 292 | ⧉负责人面+标准与检查 | 四层验收·SCI-A/B/C 判据·专项验收·发布门 | **缺开头行** | 42 | 部分正本（§1 系上位 §12.4 副本） | 无 | 0/11/0/2 | 保留+拆分下沉 |
| `CONTROL_PACK_SPEC.md` | 控制包规范 | 224 | ★标准与检查（过程治理） | 控制包制作/执行两流程·模板·验证·收口 | **缺开头行** | 22 | 正本 | 无（导语） | 0/1/0/0 | 保留 |
| `README.md` | 项目入口 | 130 | ★入口与导航 | 权威链·三命令·四创新点·构建·布局·往哪读 | 有（§1.1/§0.1） | 216* | 正本（层级图 1/3 副本） | 无 | 0/0/0/0 | 保留+减重 |
| `DEPENDENCIES.md` | 工具链版本锁定 | 87 | 操作与运营 | 双平台工具链冻结值·flags·依赖锁·路径政策 | **缺** | 23 | 登记面（数值在机器源） | **有**（标题含任务号+权威声明） | 0/5/0/1 | **迁移** `docs/development/` + 并 BUILD_STANDARD |
| `FATDUCK_ACCESS.md` | Fatduck 节点接入说明 | 42 | 操作与运营 | SSH 跳板·密钥路径·在线时窗·离线策略·用途 | **缺（且不引上位）** | 12 | 待定（正式层外） | 无 | 0/0/0/0 | **迁移** `docs/operations/` |
| `memory.md` | 项目记忆（GOV-005 收敛） | 110 | **应排除于正式层**（过程台账+历史+已撤销） | 目标副本·当前 SHA/版本·旧模块树·11 条开放问题 | 让渡声明+自称唯一记忆 | 89 | **否**（副本+过期事实） | **有（9 行）** | **7/22/12+sha256/22** | **删除**（先改登记面；上呈） |
| `docs/science/SCIENCE_SCOPE.md` | Science Scope | 120 | 科学定义 | 科学范围·标度词表·假设与 γ 判据·失效条件·精度 | 有 §1/§2 | 25 | 正本 | 无 ID 块 | 0/2/0/0 | 保留（口径闭合） |
| `docs/science/UNIFIED_SCIENCE_MODEL.md` | 统一科学定义与跨阶段合同 | 143 | ⧉科学定义+合同说明 | 目标分层·`d=Ax+n`·权威术语表·最优统计·Phase 间合同 | 有 §2/§3.1 | 16 | 部分正本（§9 越层） | 无 ID 块 | 0/0/0/0 | 保留+拆分 |
| `docs/science/PHOTOMETRY.md` | Photometry Science (SCI-PHOT) | 418 | ⧉科学定义+算法推导+研究佐证 | XP 合成通带定标·IRLS/Tukey·`F_syn` 口径·通带身份门·误差预算 | 有（**两处**） | 70 | 正本（数值三处指插件） | **有（含日期）** | 3/6/0/1 | 保留+§16 迁出 |
| `docs/science/CONTROL_WEIGHT_SNR.md` | Control-Weight SNR (SCI-CW) | 260 | 科学定义（内部分两主题） | 质量权重场·逐源/逐像素绝对 SNR·单一权重口径·三口径图谱 | 有 §2.2/§5.3 | 40 | 正本（与 PSF_SIGNAL_WEIGHT 重复） | **有（3 段含重定义注记）** | 0/5/0/4 | 保留+主题拆分 |
| `docs/science/PSF_SIGNAL_WEIGHT.md` | PSF 信号权重与帧级 SNR | 162 | ⧉科学定义+研究佐证 | 严格点源信息权重·PixInsight PSFSW 核对·单一权重口径·SCI-B 定案 | 有 **+ 第二套上位** | 35 | 待定（上位指定与内容不符） | **有（上位/依据）** | 1/1/0/5 | **合并**入 CW（§3 迁 research） |
| `docs/science/NOISE_MODEL.md` | Noise/Variance/Ivar/SNR (SCI-NOISE) | 412 | ⧉科学定义+研究佐证 | 背景方差估计器·噪声分类学·加权方差面·自校准判据·dtype 不变量 | 有 §2.2/§4.2 | 75 | **正本（上位指名 §5b/§5c）** | **有** | 0/10/0/5 | 保留（补 e⁻²、修死锚） |
| `docs/science/UNCERTAINTY_AND_COVARIANCE.md` | Uncertainty & Covariance | 163 | 科学定义（横切三相） | variance/ivar 传播·Drizzle 相关·UPM control_variance·P3 协方差 | 有（缺 §6.x） | 43 | 正本（被索引称"唯一口径"） | 无 ID 块 | 0/8/0/0 | 保留（消歧 k_corr 域） |
| `docs/science/PHASE2_UPM.md` | Phase2 UPM (SCI-UPM) | 505 | ⧉科学定义+算法推导+台账 | 纯加性天光模型·两级权重·k_corr·可辨识性·节点间距·接缝门槛推导 | 有（不全） | 82 | 正本×2（§5 权重 / §17 门槛） | **有（含日期 T106）** | 2/12/0/4 | **拆分**（§17→algorithms、§16→plugins） |
| `docs/science/INTEGRATION.md` | Integration/Coaddition (SCI-INT) | 184 | 科学定义 | 逐像素加权归约·support canonical max·五态机·确定性 | 有 §5.3 | 43 | 正本 | **有** | 0/1/0/0 | 保留 |
| `docs/science/DRIZZLE.md` | Drizzle/Spherical Resampling (SCI-DRZ) | 250 | ⧉科学定义+研究佐证 | drop 面积归一核·通量严格守恒·方差传播·球面几何·零漏选 | **有但标错节** | 61 | 正本 | **有** | 0/3/0/0 | 保留（修上游+方差分母） |
| `docs/science/REJECTION.md` | Rejection/Outlier (SCI-REJ) | 399 | ⧉科学定义+研究佐证+台账 | 七方法·按几何 n 档位·percentile 等效显著性漂移·全拒可达域·ESD/RCR 归属 | 有 §5.5（双向成环） | 47 | 部分（档位表正本循环） | **有** | **2（负责人裁决）**/2/0(sha1 2)/7 | 保留（正本合一、去日期） |
| `docs/science/ASTROMETRY.md` | Astrometry/WCS (SCI-WCS) | 294 | 科学定义 | TAN+SIP WCS 定义·1-based 桥接一次·极区剪枝·外部闭环指标 | 有 §4.2/§4.4 | 45 | 正本（**双 ID**） | **有** | 0/2/0/2 | 保留（消自相矛盾） |
| `docs/science/CALIBRATION.md` | Calibration Science (SCI-CAL) | 507 | ⧉科学定义+研究佐证+算法推导 | bias/dark/flat 约定·ADU 域·暗场线性与曝光容差科学判据·坏点两类·标度门 | 有（不全） | 61 | 正本（ε 未冻结） | **有** | 0/3/**6**/3 | 保留（判据登记+数据登记） |
| `docs/science/PSF.md` | PSF Science (SCI-PSF) | 211 | 科学定义 | 椭圆 Moffat4·7 参数拟合·FWHM/通量常数·θ 值域·截尾均值→σ 闭式 | 有（缺 §4.4） | 45 | 正本 | **有** | 0/2/0/0 | 保留（证据锚迁区） |
| `docs/science/STAR_DETECTION.md` | SCI-P1-STAR-001 星点检测冻结层 | 182 | ⧉科学定义+测试设计+算法推导 | 亚像素质心·99% 召回阈分档·阈值量纲·饱和/边缘·两套 σ_bg 不可互换 | 有 **+ALG 权威** | 23 | 正本（**缺星表引导口径**） | **有（3 段）** | 0/1/0/1 | 保留（补机制节） |
| `docs/science/ACR_EQUIVALENCE.md` | ACR Work-Domain Equivalence | 188 | **角色错放**（判据属验证/标准） | CPU/GPU/混合分块等价·分块合法域·设备域差异·相对容差标度论证 | **有但标错阶段** | 16 | 待定（主题=非目标） | **有** | 0/0/0/1 | **迁移/降级**（上呈） |
| `docs/science/PHASE3_HIPS_TO_FITS.md` | Phase3 HiPS→FITS 科学合同 (SCI-P3) | 236 | ⧉科学定义+台账 | HiPS 输入有效域·order 选择与夹紧·CRPIX/LONPOLE·coverage 真值表·FOV 定量 | 有 §6.2/§6.3 | 51 | 正本（依据=已删控制包） | **有（含"未实现"）** | 1/8/**1 处 sha256**/0 | 保留（依据落地） |

`*` = 该下游计数对 `README.md`/`memory.md` 两个通用文件名不具鉴别力，精确口径见各条目。



### 1.1 `ASTROCS_DESIGN.md` — 最高设计

| 字段 | 内容 |
|---|---|
| 路径 | `ASTROCS_DESIGN.md`（仓库根） |
| 标题 | Astro Celestial Sphere Database（ACSD） 最高设计（Design Authority） |
| 行数 | 826 |
| 角色（标准 01 §2 表） | **标准 01 §2 的九格里没有"最高设计/顶层决策"这一格**——九格全是它的下位角色。它是 §1 权威链的 L1 顶点，按 §2 表落不进任何目录。→ 角色表覆盖缺口（见"我证伪任务书"） |
| 主题 | ACSD 是什么、三命令三产品、四个科学创新点、顶层架构与调度、CLI 合同、双平台发行、四层验收与发布权 |
| 上游 | 不适用（链顶）。§0 L31 自declare："本文档与任何其他文档冲突时，以本文档为准"；§0.1 L37-42 给出四层链 |
| 下游 | `git grep -l "ASTROCS_DESIGN"` → 命中 415 个跟踪文件（root 7 / docs 239 / 实验 31 / 其他 138）。`docs/science/` 17 份**全部**在开头第 3 行以 `> 上游：ASTROCS_DESIGN.md §x（标题）` 形式点名它（`awk 'NR<=5' docs/science/*.md` 逐份确认，见 §3 索引核对表） |
| 是否该主题正本 | **正本**（顶层决策、创新点定义、CLI、状态阶梯 §12.5、发布权 §13） |
| 开头元信息块 | 无（L1 标题、L3 正文导语）。**合规** |
| 日期/编号/sha/历史叙事 | 日期 0、commit sha 0、sha256 0。编号 3 处：L276 `DATA-P1-PHOTPROV-001`、L412 `CHK-L4-SEAM-FOOTPRINT`、L538 `CHK-LOG-SYS`（均为正式合同/门 ID，非流水号；`SCI-A/B/C`、`ALG-DRZ-O1` 是 §12.3 的单元正式名）。历史叙事词 4 行（L689/L691/L787/L801）逐行读后**全是现行约束**，无"旧版/作废/曾"式留痕。**判定：合规** |
| 悬空引用 | 扫描器 9 条命中，逐条读后**全部为运行产物名或取值域指针，非悬空**（`filters.json`/`defaults.json` 实存于 `eng/packaging/config/`；`.hips.index.json`/`coverage.index.json`/`manifest.json`/`p1_phot.json`/`phase_config.json`/`config.json`/`module.yaml` 是合同里定义的落盘文件名）。**真悬空 = 0** |
| 与上位冲突 | 无上位。内部指针二义一处：§1.2 L92 与 `README.md` L29、`ACCEPTANCE_SPEC.md` L194 都写"阶段间交换媒介的**唯一正本 = §8.1**"，而 §8.1 L548 自己写"阶段间唯一的交换媒介是磁盘 HiPS 产品 + manifest + 哈希（**§10**）" → 指针两跳，沿索引一步到不了正本（标准 01 §5 末段"走得通"判据不满足） |
| 处置建议 | **保留（原地）**。三项整治：① 把 §2.2/§2.4/§3.1/§7.2 的公式与数值表下沉为纯指针（下条"层级倒置"）；② §1.2 的"唯一正本 = §8.1"与 §8.1 的"（§10）"二选一定为单点；③ 退出码 11 行表与 `docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5 表二者只留一份（按 §4"数值表下沉"应留下位） |

**层级倒置（指到具体节，标准 01 §1"公式推导、数值表、源码行号、实验数据下沉"）**：

| 节 | 行 | 不该出现在 L1 的内容 | 该主题的下位正本 |
|---|---|---|---|
| §2.1 | L126 | `k_photo = 10^{−location}` 及其单位表述 | `docs/science/PHOTOMETRY.md` §3/§5 |
| §2.2 | L134-147 | 参考分解 `I = O + N_e/g + N_read`（含逐符号单位）+ 增益约定整段 **13 行** | `docs/science/NOISE_MODEL.md` §5c（**逐字重复**，见下） |
| §2.2 | L150/157/174/178/229 | `SNR_F = F/σ_F`、`w = SNR²/F_ref² = 1/σ_F²`（同式出现 5 次） | `docs/science/CONTROL_WEIGHT_SNR.md` §2b/§8c |
| §2.4 | L201-213 | chart Jacobian `π/3`、14 个 face 角点的合并算式（2×4+8×3+4×4=48，算术自洽）、"零超越函数"、鞋带公式 | `docs/algorithms/DRIZZLE_GEOMETRY.md` |
| §3.1 | L226-229 | 重建口径枚举 `dense`/`sparse_reconstruct`/`frame_reconstruct` 与方向式 `frame_snr := median_p(SNR_c)` | `docs/science/CONTROL_WEIGHT_SNR.md` §8b/§8c、`docs/plugins/algorithms_phase1/07_noise_snr.md` §4.5 |
| §7.2 | L498-510 | 退出码全表 11 行 | `docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5（本文自己称其为"唯一数值表"） |
| §10 | L689-691 | 落盘形态字段级细则、`fallocate(PUNCH_HOLE)`/`FSCTL_SET_SPARSE` 系统调用名、TRIM 关键字名 | `docs/design/PRODUCT_STORAGE_FORM.md`、`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md` |

**正面示范（同文内已做对的）**：§4.2 L273"本节不复制数值"、§2.4 L213"可复现的分档与取值以 §9 冻结值为准，本节不复制数值"、§5.4 L412"门槛数值 `1e-2` 的推导与实测标定正本 = `docs/science/PHASE2_UPM.md` §17"、§5.5 L422"逐像素档位表的唯一正本 = `docs/plugins/algorithms_phase2/12_rejection.md` §9"、§12.4 L784"各层的数据、执行方、证据落位与逐项判据的唯一正本 = `ACCEPTANCE_SPEC.md` §1"。**同一文档一半在做指针化、一半在复述，是整治的落点。**

**重复/重叠对象（实证）**：

1. **§2.2 L132-147 ↔ `docs/science/NOISE_MODEL.md` §5c L189-203（最硬的一条）**：两段**逐字相同**的部分——公式块 `I = O + N_e/g + N_read, N_e = N_src + N_sky + N_dark`、符号句"`I` 像素测量值（ADU）、`O` 偏置（ADU）、`N_e` 光电子总数、`N_src` 目标天体光电子、`N_sky` 天空背景光电子、`N_dark` 暗电流电子、`N_read` 读出噪声（ADU）"、以及"文献里同一物理关系常写成 `I = O + g'·N_e + N_read` 的**倒数形式**，那里 `g'` 的单位是 ADU/e⁻ 且 `g' = 1/g`。**本仓一律用 e⁻/ADU**；引用文献公式时必须先确认其约定并换算"——**三截完全一致**。**差异只在末句**：最高设计接"两种约定混用会让源项差 `g²` 倍，每次引用文献公式都显式给出所用约定。每一项的物理来源、量纲、方差形式（**ADU² 与 e⁻² 两个标度各写一遍**）、随信号的变化、帧内空间尺度、帧间相关性必须逐项标注一手文献出处与适用域；正本为 `docs/science/NOISE_MODEL.md` §5b"；NOISE_MODEL 接"两种约定各自具名（混用会让源项差 `g²` 倍）。单帧只提供均值与方差两个可观测量…本链只做**归因**…秩与零空间的代数依据见 §5b"。**判定：L1 与 L3 同内容两份，且"唯一正本"这一事实未在 L1 侧删除正文**。附带证据：设计要求的"**e⁻² 标度各写一遍**"在 NOISE_MODEL 全文只有 2 处 `e⁻` 相关字面（L171 引 EMVA 式 `(g²−1)/12 e⁻²`），§5b 噪声表的"方差（ADU²）"列**没有对应的 e⁻² 列** → 上位提出的下位义务未被满足（命令：`grep -n 'e⁻²' docs/science/NOISE_MODEL.md` → 1 命中）。
2. **§12.4 ↔ `ACCEPTANCE_SPEC.md` §1**：见 §2 主题组 G2（表结构逐格对照，最完整的一对）。
3. **§3.3 精度归属 ↔ `docs/science/SCIENCE_SCOPE.md` §变量/单位 + §数值精度**：同一清单两份，**SCIENCE_SCOPE 丢了上位列的一项**——设计 L240 写"稀疏/有限数据（帧级 SNR、WCS 解、星表匹配、测光定标、**manifest 字段**）全程双精度"，SCIENCE_SCOPE L92 写"（帧级 SNR、WCS 解、星表匹配、测光定标）全程 FP64"（无 `manifest 字段`）。命令：`grep -c 'manifest 字段' ASTROCS_DESIGN.md` → 1；`grep -c 'manifest 字段' docs/science/SCIENCE_SCOPE.md` → 0。判定：**表述漂移（下位集合是上位真子集）**。
4. **§1.1 项目定位一句话 ↔ `AGENTS.md` §2、`README.md` L1/L3-4、`memory.md` §1、`docs/science/SCIENCE_SCOPE.md` L11**：五份各写一遍。**口径漂移**：设计 L69 / README L1 / AGENTS L39 都写"天文 **CCD/CMOS** 图像校准与标准化数据库"，而 `memory.md` L15 与 `SCIENCE_SCOPE.md` L11 写"天文 **CCD** 图像…"（丢 CMOS）；`SCIENCE_SCOPE.md` L60 有效域又写回"标准 CCD/CMOS"。命令：`grep -n "CMOS\|CCD 图像" ASTROCS_DESIGN.md README.md AGENTS.md memory.md docs/science/SCIENCE_SCOPE.md`。
5. **§12.2 三类实验数据 ↔ `ACCEPTANCE_SPEC.md` §3**、**§12.3 实验单元 ↔ `ACCEPTANCE_SPEC.md` §2**：见 G2。
6. **§2.2 "方法学对标 PixInsight PSFSNR/PSFSW"（L187）↔ `docs/science/PSF_SIGNAL_WEIGHT.md` §3（40+ 行 PixInsight 公式核对块）**：设计只给两句结论、下位给一手公式，属**合法分层**；但下位块内含日期与"已退役"叙事（见 1.16）。

### 1.2 `AGENTS.md` — 机器干活手册

| 字段 | 内容 |
|---|---|
| 路径 | `AGENTS.md`（仓库根）。**注意：派单文字举例写的 `docs/AGENTS.md` 不存在**（`git ls-files docs/AGENTS.md` 空输出），清单与本文件都在根 |
| 标题 | AGENTS.md — Astro Celestial Sphere Database（ACSD） 机器干活手册 |
| 行数 | 237 |
| 角色（标准 01 §2） | 落不进表：**"标准与检查"** 最接近（`docs/standards/`），但它同时是运营（§3）、纪律（§6/§8/§11/§12）的混合。按 §2 表它应属"标准与检查"并落 `docs/standards/`——**但 `docs/standards/DOCUMENTATION_STANDARD.md` §1 层级图把它画在"仓库根规范层（第 1.5 层）"**（L42），与实际位置一致。**判定：位置由下位文档确认，角色表缺格；不算角色错放** |
| 主题 | 开工前逐层读文档的铁律、记忆系统、一句话定位、开发节点环境与构建命令、标准工作流、科学工作纪律、硬禁令、目录落位速查、提交纪律与科学查证闭环、自查自修、何时上呈负责人、"默认动作是继续执行"、"思考终点是动作" |
| 上游 | **有但形式弱**：§1 第 1 条 `ASTROCS_DESIGN.md`——"项目是什么…（最高权威）"，属**阅读顺序**标注，不是标准 01 §3 要求的"章节号 + 标题"元信息行。§2/§5/§10 内联引 §2/§12.2/§12.3。**判定：部分满足**（无 `> 上游：` 行） |
| 下游 | `git grep -l "AGENTS"` → 139 命中（root 8 / docs 14 / 实验 24 / 其他 93）。docs 侧点名它的 14 份见 §1.2a |
| 是否该主题正本 | **是**（干活纪律、何时上呈、继续执行原则的唯一正本）；但 §6/§7/§3 三处是 `ENGINEERING_SPEC.md` 的**副本**（见重复项） |
| 开头元信息块 | 无。**合规** |
| 日期/编号/sha/历史叙事 | 日期 0、任务编号 0、commit sha 0、sha256 0；历史叙事词 4 行（L33/L72/L122/L216）逐行读：L72"叠加**历史**轮次会迅速吃满"、L216"**历史**会自动摘要成检查点"——均为运营事实描述，非"旧版/作废"留痕。**判定：合规（本批最干净的一份）** |
| 悬空引用 | 扫描器报 5 条全部**实存**（`eng/ci/checks.json`、`eng/ci/run_checks.py`、`eng/packaging/config/filters.json`、`defaults.json`、`eng/tools/run_keep.txt`），系 basename 判定误报。**真悬空 = 0**。**但存在一条"无锚条款"**：§1.2「记忆系统」规定"动手前先查两类记忆——项目记忆 + 负责人偏好记忆""负责人每一次裁决与指示都归纳进记忆"，**全文不给出记忆的落位路径，也不引用 `memory.md`**（`grep -n memory AGENTS.md` → 0 命中）。→ 该条要求的动作在仓内无可达对象 |
| 与上位文档冲突 | **一处（平台边界缺声明）**：§3「环境与构建（Linux 开发节点）」L47 给 `cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release` + `ninja -C build`，而 `DEPENDENCIES.md` L15 冻结"正式 generator = `Visual Studio 17 2022`，**唯一正式** generator"、L39"不固定 Ninja/MinGW"。AGENTS 用括号标了"（Linux 开发节点）"但**未声明 Windows 侧该按谁**，也未引 `DEPENDENCIES.md`/`docs/standards/BUILD_STANDARD.md`。判定：**取值不冲突、边界条款缺失** |
| 处置建议 | **保留（原地）+ 三处改指针**（提交纪律 §6/§8 → ENGINEERING §6；目录树 §7 → ENGINEERING §7；mem_guard 数值 §3 → 运营正本）。§1.2 记忆系统需给落位锚或并入 `memory.md` 的说明 |

**AGENTS ↔ ENGINEERING_SPEC 重复实证（给段落）**：

| 主题 | AGENTS 位置与原文要点 | ENGINEERING 位置与原文要点 | 重叠与差异 | 判定 |
|---|---|---|---|---|
| Git/提交 | §6 L121-122「不在 main 外开分支/worktree/额外 clone；只 main 原子提交；不 force push / amend / 历史重写」+ §8 L165-166「一个 commit = 一个明确目的；验证后立即提交…SubAgent 零 git 写权限；前台按序串行提交；每次 push 后 fetch 核对三 SHA 一致（HEAD=main=origin/main）」 | §6 L91-97「只 `main` 开发；不做分支/worktree/额外 clone/force push/amend/历史重写；一个 commit = 一个可独立验证的目的；科学/架构/性能/文档不混提；SubAgent 零 git 写权限；前台串行提交，push 后核对三 SHA 一致；所有预存修改先登记，不自动 reset/stash/clean/rebase/覆盖」 | AGENTS 覆盖 ENGINEERING §6 的第 1/2/3/4 条**全部要点**；ENGINEERING 多"预存修改先登记"一条，AGENTS 多"验证后立即提交""提交消息写清做了什么 + 依据哪条权威条款"。**CONTROL_PACK_SPEC §6.2 L155 与 §8 L200 都写"提交纪律的唯一正本 = `ENGINEERING_SPEC.md` §6"** | **同主题两份正本；ENGINEERING 是被指定的正本 → AGENTS 的两条应降为指针** |
| 目录落位 | §7 L135-155 目录树（17 个落位条目）+ L157-158 round_start.sh 段 + L160"根目录固定条目见 ENGINEERING_SPEC §7" | §7 L101-145 目录规范（强制），含根固定条目清单、`VERSION` 条目、`toolchain.ps1`、外部只读数据集、落盘形态四条 | AGENTS 树是 ENGINEERING 树的**面向速查的重排**，条目集合相同、详度更低；AGENTS L160 **已自认** ENGINEERING §7 为登记面 | **ENGINEERING §7 为正本（最高设计 §8.4 L656 亦指定）；AGENTS §7 属合法"速查副本"，但含 2 处 ENGINEERING 没有的运营细则（round_start.sh / run_keep.txt fnmatch 语义）→ 应上收至 ENGINEERING §7 或 `docs/operations/`** |
| 科学红线 | §5 L108「科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、精度与默认容差按 `docs/science/`、`docs/algorithms/` 执行，改动走变更流程（见 §8）」 | §3 L33「科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、精度与默认容差以 `docs/science/**`、`docs/algorithms/**` 为准，改动走变更流程」 | **八项枚举逐字相同**，只差"按/以…为准"与"（见 §8）" | **同句两份 → 其一改指针** |
| 重计算看门狗与磁盘回收 | §3 L58-81（12 行 mem_guard 细则：`--max-rss-gb` 取正常峰值 1.5–2 倍、端到端 **20 GB**、单进程 **4–8 GB**、每帧约 1.67 GB、8 帧 13.5 GB、物理内存 23 GiB、`/workspace` 503 GiB、49 帧 M42 一轮 75 GiB、可用低于 100 GiB 先回收、超时 `--timeout` 返回 124、超限 137）+ §7 L153-158 | §7 L134 只写一行"自清理机制见 `eng/tools/run_gc.py` 与 `eng/tools/round_start.sh`"，并（在其 §7/§3 相关处）声明此类属**运营事实**、正本在 `docs/operations/` | **ENGINEERING 声明"AGENTS 只给纪律性指针"，而 AGENTS §3 给了全部数值** → 分工被写法违反。同时 `docs/operations/` 实测只有 1 份 `.md`（`TOOLCHAIN_AGENT_HOST.md`），**mem_guard 数值正本在该目录内不存在**（`git ls-files docs/operations` → 1 行） | **数值无正本落点 = 标准 01 §4"数值放对应下层文档"未满足；需为 `docs/operations/` 建运营事实正本并把 AGENTS §3 降为指针** |

**AGENTS 的 docs 侧下游 14 份**：`docs/DEVELOPER_GUIDE.md`、`docs/DOCUMENT_INDEX.yaml`、`docs/KNOWN_LIMITATIONS.md`、`docs/algorithms/STAR_DETECTION_ALGORITHMS.md`、`docs/audit/risk_verification_T012.csv`、`docs/ci/01_CHECKS.md`、`docs/ci/CI_SPEC.md`、`docs/contracts/DUAL_LINE_CONTRACT.md`、`docs/operations/TOOLCHAIN_AGENT_HOST.md`、`docs/research/PHOTOMETRY_RESEARCH_PACK.md`、`docs/science/PHOTOMETRY.md`、`docs/standards/DOCUMENTATION_STANDARD.md`、`docs/standards/RELEASE_STANDARD.md`、`docs/validation/v6/QA_MATRIX.md`。

**角色错放判定**：无（根级工作手册，下位层级图已承认）。**层级倒置**：§3 把 `cmake/ninja/ctest/run_checks` 四条命令、mem_guard 参数、盘容量这类**运营数值**写进 L1.5 纪律文档 = 轻度倒置（标准 01 §4"公式、数值表、源码行号、实验过程数据放对应下层文档"）。

### 1.3 `ENGINEERING_SPEC.md` — 工程规范

| 字段 | 内容 |
|---|---|
| 路径 | `ENGINEERING_SPEC.md`（仓库根） |
| 标题 | Astro Celestial Sphere Database（ACSD） 工程规范（Engineering Specification） |
| 行数 | 272 |
| 角色（标准 01 §2） | **一份跨四格**：标准与检查（§1/§4/§5/§6/§10/§13）、操作与运营（§3/§7 构建与目录）、合同说明（§7 落盘形态词表、§11 日志字段）、研究与佐证无。§2 要求"每份文档属于且仅属于一个角色" → **多角色共存是缺陷项**，但不构成"错放到错误目录" |
| 主题 | 语言/编译器/平台、代码风格与历史实现二选一处置、科学代码红线、模块必备七件套、测试规范、Git 与提交、目录规范（登记面）、文档集与正本分工、仓库整洁、机器一致性检查、日志与错误、资源与性能、与 CI 的关系、与其它文档关系、命名（显示名与机器契约保留面） |
| 上游 | **缺开头"上游"行**（L1 标题、L3 `---`、L5 即 `## 1. 语言、编译器与平台`）。内联锚定密集：L9 平台下限指针、L35"架构与模块规范的上位权威 = 最高设计 §8"、L81"版本口径正本 = 最高设计 §13"、L100"资源口径上位权威 = 最高设计 §9"、L174 文档正本分工唯一口径自 claim、§14 表（L232-241）"ASTROCS_DESIGN.md — 一切条款的上位来源"。**判定：文内有锚、开头无标注 → 标准 01 §3 未满足** |
| 下游 | 161 命中（root 5 / docs 32 / 实验 8 / 其他 116）。docs 侧 32 份清单见 §1.3a |
| 是否该主题正本 | **是**：§6 提交纪律（被 CONTROL_PACK §6.2/§8 指定为唯一正本）、§7 目录登记面（被最高设计 §8.4 L656 指定）、§8 文档正本分工（自称唯一口径）、§15 命名显示名与保留面（自称唯一定义源） |
| 开头元信息块 | 无。**合规** |
| 日期/编号/sha/历史叙事 | 日期 0；commit sha 0；**sha256 词 1 处**（L207"整文件 `sha256` 必须不变"= 合同要求，合规）；任务/门编号 6 处：L49 `CHK-PLUGIN-SYMBOL-CLOSURE`、L143/L185 `CHK-HIPS-STORAGE-FORM`、L207 `CHK-SPARSE-PUNCH`/`CHK-SPARSE-PUNCH-PROBE`、L209 `CHK-LOG-SYS`、L271 `CHK-NAMING-SURFACE`，**全是门 ID，非流水号，合规**；历史叙事词 15 行，逐行读后需判定的 2 处：L139"`eng/build/toolchain.ps1` 是**现役固定条目**，**不是遗留待清理对象**"、L248-265 §15 的"历史名 `AstroCS`"族（L248 自注"本节为写清判定规则而逐字给出该历史名"）——**均为"为反驳历史叙事而必须使用历史词"的边界情形，判定合规，但建议标准 01 §4 显式写明此豁免，否则 `CHK-NAMING-SURFACE`/doc-hygiene 门会误判** |
| 悬空引用 | 扫描器 9 条：8 条为泛指/运行产物（`README/module.yaml` 表格泛指、`module.yaml`、`.hips.index.json`、`coverage.index.json`）或实存文件（`eng/build/build.sh`、`eng/build/toolchain.ps1`、`filters.json`、`defaults.json`）。**唯一需判定的一条：L207 "写端接线 = `aio_hips_writer.cpp` 的 `write_fits_atomic`"——只给 basename，实际路径是 `lib/infrastructure/aio/src/hips/aio_hips_writer.cpp`（`git ls-files \| grep aio_hips_writer` → 该一行），按字面路径打不开**。判定：**锚点写法不合规（1 条）**，同段其余锚点 `lib/infrastructure/aio/src/aio_sparse_punch.h`、`lib/infrastructure/cli/exit_codes.h`、`lib/infrastructure/observability/logging/log_event_v1.schema.json`、`eng/contracts/resource_gate_v1.json`、`eng/ci/ledgers/naming_surface.json` **全部实存**（逐条 `[ -f ]` 验证） |
| 与上位冲突 | **两处**：① L197 称"运行日志行格式正本 = `lib/infrastructure/observability/logging/log_event_v1.schema.json`（**LOG-001**）是日志事件 schema 的唯一来源"，而最高设计 §7.3 L531 称"机器日志 = 结构化日志合同行（**`astrocs.log.event.v1`**，实现家 `lib/infrastructure/observability/`）"——一个是文件路径+内部 ID、一个是 schema 名，**无第三处把它们对齐**（本批只读文档侧，登记为跨层标识待核）。② §7 L104-108 把 `memory.md` 列为"仓库根**固定条目**"，而标准 01 §2 明令"历史档案、过程台账、已撤销设计**不放在正式文档层**；需要留证的台账落 `artifacts/evidence/`" → **登记面把一个按标准应清除的位置固化了**（详见 1.9） |
| 处置建议 | **保留（正本）**。四项：① 开头补 `> 上游：ASTROCS_DESIGN.md §0.1（唯一权威链）、§7.2（退出码）、§8（架构）、§9（资源）、§13（版本）`；② 按 §2 声明多角色共存例外或把 §3/§7 运营面拆到 `docs/development/`；③ 与 AGENTS 的三处重复改指针；④ 修 `aio_hips_writer.cpp` 为全路径 |

**"唯一口径/唯一正本"自称逐条核对**（标准 01 §1 末条"任何文档不声明自己的第二套权威顺序或'唯一口径'"）：

| 自称原文（行） | 权威链上是否有另一正本 | 判定 |
|---|---|---|
| §15 L269「本节是显示名与保留面的**唯一定义源**；其它文档、检查器与台账只引用 `ENGINEERING_SPEC.md §15`」 | 无（`docs/standards/` 与 `docs/ci/` 均引用不复述） | **成立**（是"主题定义源"声明，不是第二权威序） |
| §11 L194-195「退出码**唯一源** = `lib/infrastructure/cli/exit_codes.h`（§7.2 表）；域→码映射的**唯一数值表** = `docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5」 | 同文件 §7.2 **自己列了全 11 行退出码表**（最高设计同表也在 L498-510 列全）→ 同一数值三处，其中两处自称唯一 | **不成立**：'唯一数值表'有两个候选层，且两处文档都复述了整表 |
| §12 L217「资源判据数值唯一源 = `eng/contracts/resource_gate_v1.json`」 | `ACCEPTANCE_SPEC.md` §4 L162/L166 同指该 JSON 且不复述数值 | **成立** |
| L174「本节是文档集**层级与正本分工的唯一口径**；角色归属、目录落位与重建目标见 `docs/standards/DOCUMENTATION_STANDARD.md`」 | 最高设计 §0.1/§0.2 亦声明唯一权威链；`docs/standards/DOCUMENTATION_STANDARD.md` 给层级图。**但两份层级图的节点集不同**（见 1.6 README 冲突②） | **范围自限成立；层级图两份 = 待消解缺陷** |
| §5 L72「1 worker 与 N worker 数值一致性…容差来源与可满足性下限按 `docs/contracts/SCHEDULER_CONTRACT.md` §2.1 与 `docs/contracts/TEST_MATRIX.md` §2」 | 与最高设计 §9 L677 同两个锚 | **成立（纯指针，无复述）** |

### 1.4 `ACCEPTANCE_SPEC.md` — 验收规范

| 字段 | 内容 |
|---|---|
| 路径 | `ACCEPTANCE_SPEC.md`（仓库根） |
| 标题 | 验收规范（Acceptance） |
| 行数 | 292 |
| 角色（标准 01 §2） | 跨 **负责人面**（§1 层序、§8 发布门、§6.3 发布决策）+ **标准与检查**（§2 SCI 判据、§3.1/§3.2/§9 专项判据表）。→ 多角色共存 |
| 主题 | 四层验收 L1–L4 的目标/数据/执行方、三个创新点实验单元的验收判据、落盘形态与打洞专项验收、L4 视觉清单与接缝可执行门槛、验收产物落位、预览版发布门 9 条、日志与错误系统验收 E1–E7 |
| 上游 | **缺开头"上游"行**（L1 标题、L3 即 `## 1. 验收体系总览`）。内联锚定极密（L31 五条点名最高设计 §2.1/§2.2/§4.4；L33"实验单元目录结构与报告八要素的唯一正本 = 最高设计 §12.3"；L86"三类实验数据（最高设计 §12.2）"；L194"唯一正本 = `ASTROCS_DESIGN.md` §8.1"；L208"工作盘回收与预留口径 = `AGENTS.md` §3（**运营事实源，本节不复制数值**）"；L271 版本 §13）。**判定：正文锚定优于 AGENTS/ENGINEERING，但 §3 要求的开头标注仍缺** |
| 下游 | 42 命中（root 5 / docs 6 / 实验 8 / 其他 23）。docs 侧 6 份：`docs/DOCUMENT_INDEX.yaml`、`docs/ci/01_CHECKS.md`、`docs/ci/CI_SPEC.md`、`docs/design/PHASE2_DETAILED_DESIGN.md`、`docs/design/PHASE3_DETAILED_DESIGN.md`、`docs/science/PHASE2_UPM.md` |
| 是否该主题正本 | **部分是正本、部分是 §12.4 的副本**（G2 组）；§3.1/§3.2/§9 三张专项判据表按角色应属"合同说明/标准与检查"层 |
| 开头元信息块 | 无。**合规** |
| 日期/编号/sha/历史叙事 | 日期 0、commit sha 0。编号 11 处（L76/125/126/127/129/149/150/152/154/233/292），全是 `CHK-*` 门名与 `IO-002/IO-003` 既有检查项名。sha256 4 处（L122 `index_sha256`/`archive_sha256` 字段、L137"整文件 `sha256` 必须不变"、L287 `log_artifacts[]` 的 sha256）——**合同字段与判据，合规**。历史叙事需判定 2 处：**L127「IO-002 / IO-003 **既有**检查项全部保留，形态验收是**新增而非替代**」**（过程语义留痕）；**L154「未实测项如实登记：Windows 上的…等价实现**只有一手文档依据、未在 Windows 上实测**」**（诚实边界，标准允许如实登记，但语态属过程台账）。另 **L31 整段是"反义设防"叙事**（见冲突①） |
| 悬空引用 | 扫描器 10 条全为运行产物名或工具锚：`artifacts/acceptance/ACCEPTANCE_REPORT.md`（**规定的产出落位，尚未生成 → 属"规定落点"非悬空**）、`check_spec_polarity_consistency.py`（**实存** `eng/tools/doccheck/`，已 `[ -f ]` 验证）、`manifest.json`/`p1_products.json`/`p1.json`/`p2.json`/`p3.json`/`worker_balance.csv`/`.hips.index.json`/`coverage.index.json`。**真悬空 = 0** |
| 与上位冲突 | **三处**：① L31 一段设防"本节的判据若与上述冻结语句**反义**（把星等当数据形态、或把稀疏控制点层写成相对 SNR），**以最高设计为准并立即订正判据**"——该句本身承认本文档曾与上位反义（现由门 `check_spec_polarity_consistency.py` 防回归）→ **属"为防回归写的历史语态"，标准 01 §4 不允许**；应改正向条款"本节判据的名词口径 = 最高设计 §2.1/§2.2 冻结语句"，防回归交机器门。② **L233 节号错**：「`docs/science/PHASE2_UPM.md` **§7** 的 UPM patch 域 `min_samples`」——UPM 文档的 patch 域 `min_samples` 在 **§5**（L87 `域 [min_samples, (2r+1)²] = [5, 289]`），其 §7 是"独立不变量"无该词。命令：`grep -n min_samples docs/science/PHASE2_UPM.md` → 仅 L87。③ **默认参数正本分裂**：L233 说"判据取值与默认参数的唯一数值来源 = `eng/tools/e2e/seam_footprint.py`（`d` 默认 2 px、`ctrl_shift` 默认 200 px、`min_samples` 默认 20）"，而 `docs/science/PHASE2_UPM.md` §17 L401 引入第三种表述"`N_e` = 该边界折线上的有效采样数（**默认 256 点**；M42 实测中位 256、最小 252）"，§9a 又只讲 `min_samples`。三处互不指认同一个"默认参数正本" |
| 处置建议 | **保留 + 下沉拆分**：① §3.1/§3.2（落盘形态、打洞，共约 45 行）与 §9（E1–E7）迁 `docs/contracts/`（对应合同）或 `docs/ci/`（对应判据），根文档留"专项各有门、门在哪"一句指针；② §1 的层序表与最高设计 §12.4 消歧（见 G2）；③ 修 L233 的 §7→§5；④ L31 反义设防段与 L127"既有/新增"句改正向条款 |

### 1.5 `CONTROL_PACK_SPEC.md` — 控制包规范

| 字段 | 内容 |
|---|---|
| 路径 | `CONTROL_PACK_SPEC.md`（仓库根） |
| 标题 | Astro Celestial Sphere Database（ACSD） 控制包规范（Control Pack Specification） |
| 行数 | 224 |
| 角色（标准 01 §2） | **标准与检查**（工作方法规范）→ 按表可落 `docs/standards/`；实际在根（与 AGENTS 同一表缺口：**无"工作方法/过程治理"格**） |
| 主题 | 制作流程与执行流程两条独立线、控制包定义与目录模板、任务文件模板、任务状态词表、前台独立验证三层、统一提交、收口清理 |
| 上游 | **缺开头"上游"行**。内联：L3 导语（"把最高设计与实际代码的偏差变成可执行、可验收的工作单元"）、L134"八要素与目录结构的唯一正本 = `ASTROCS_DESIGN.md` §12.3"、L163 任务状态词表与 §12.5 分域声明、L208"取值集合 = §12.5 词表，本规范不复制"、L129"提交粒度正本 = `ENGINEERING_SPEC.md` §6"。**判定：指针密度高、形式标注缺** |
| 下游 | 22 命中（root 4 / docs 3 / 实验 0 / 其他 15）。docs 侧：`docs/DOCUMENT_INDEX.yaml`、`docs/ci/CI_SPEC.md`、`docs/standards/DOCUMENTATION_STANDARD.md` |
| 是否该主题正本 | **是**（控制包工作法唯一正本，无第二份） |
| 开头元信息块 | **有但属正文性导语**：L3 是 `>` 引用块"**制作控制包**与**执行控制包**是两条独立流程…"，无 ID/状态编号/版本锚/纪律声明。**判定：合规** |
| 日期/编号/sha/历史叙事 | 日期 0、sha 0。编号 1 处 L94 控制包 ID 规则示例（`RELEASE-04`、`NORMALIZE-CAL-01`、`MOSAIC-UPM-02`、`EXPORT-PROJ-01`）——**命名规范定义，非流水号，合规**。历史叙事 2 行 L208/L210（"收口即清理…从仓库移除""不产 zip/胶囊/台账等重量级长期设施"）——**正向禁令，合规** |
| 悬空引用 | 扫描器 8 条：`-001.md`/`00_README.md`/`ACCEPTANCE.md`/`GAP_AUDIT.md`/`PROMPT.md`/`SUMMARY.md`/`TASK_LIST.md`/`docs/science/xxx.md` —— **全部是 §4/§5 模板里的占位符或模板产物名，不是引用**。`docs/science/xxx.md` 在 L108"权威依据"模板行内。**真悬空 = 0** |
| 与上位冲突 | **一处实质张力（非取值冲突，是产出形态冲突）**：§9 L210「**不产 zip/胶囊/台账等重量级长期设施**」+ L209「仓库工作区只保留当前生产代码、自解释文档集与当前在执行的控制包」。而标准 01 §5 要求文档整治产出**三份成果**（现状审计台账 / 目标文档架构 / 迁移合并清单），**本次工作包本身即以"台账 + 包外目录 + zip"形态存在**（现场：根级未跟踪 `ACSD整治工作包_AUDIT-06.zip` + `site/`；`工程控制/` 下现存 `RELEASE-05/`）。**判定：需负责人裁决"整治成果是否算控制包工件、收口后是否清理"**，属标准 01 §5 与 CONTROL_PACK_SPEC §9 的口径打架，不在本批自行处置 |
| 处置建议 | **保留（原地）**。两项：① 补开头"上游：最高设计 §12.3/§12.5；平行：ENGINEERING_SPEC §6"标注行；② §9 与标准 01 §5 的"清理 vs 台账"张力登记上呈 |

### 1.6 `README.md` — 仓库入口

| 字段 | 内容 |
|---|---|
| 路径 | `README.md`（仓库根） |
| 标题 | Astro Celestial Sphere Database（ACSD） — 天文 CCD/CMOS 图像校准与标准化数据库 |
| 行数 | 130 |
| 角色（标准 01 §2） | **无对应格**（"项目入口/唯一索引地图门面"）。最接近"标准与检查"（它承载权威链入口表与"细节往哪读"导航）+ "操作与运营"（构建与运行节） |
| 主题 | 一段定位、权威链入口表（7 行）、三命令三产品表、四条科学目标摘要、构建与运行命令、仓库布局四块、状态口径、"细节往哪读"14 行导航表 |
| 上游 | **有**：L4 首段末即标 `（ASTROCS_DESIGN.md §1.1）`；L8"权威链只有一条…（`ASTROCS_DESIGN.md` §0.1）"；L9"与本文档集冲突时以 `ASTROCS_DESIGN.md` 为准（该文 §0）"；§科学目标四条逐条标 §2.1/§2.2/§2.3/§2.4。**判定：满足标准 01 §3（根文档中唯一在开头给出上位条款号的）** |
| 下游 | **basename grep 对 `README.md` 无鉴别力**（仓内每个模块自带 `README.md`，216 命中里绝大多数指各自模块 README）。精确口径：`git grep -lE '(^\|[^/[:alnum:]_.-])README\.md' -- '*.md' '*.yaml'` → 真指根 README 的下游 = 根 5 份（AGENTS/CONTROL_PACK/DEPENDENCIES/ENGINEERING/memory）+ `docs/DOCUMENT_INDEX.yaml` + `docs/standards/DOCUMENTATION_STANDARD.md` + `docs/owner/PROJECT_SPEC.md` + `docs/audit/doc_classification.csv` + 24 份 `docs/modules/registry/*.md`（后两组需人工分辨）。**判定：根 README 的下游引用面显著小于其余根文档，属正常（入口文档通常不被引用）** |
| 是否该主题正本 | 是（入口导航正本）。**但"权威链层级"这一主题有三份图**（见冲突②） |
| 开头元信息块 | 无。**合规** |
| 日期/编号/sha/历史叙事 | **全批唯一一份四项全零**（date=0 taskid=0 sha=0 sha256=0 history=0）。**标准 01 §4 的样板** |
| 悬空引用 | 扫描器 1 条 `config.json`（L77-79 CLI 示例占位）。**真悬空 = 0**；其 17 处文档/工具锚全部实存（抽验 `docs/ci/CI_SPEC.md`、`docs/plugins/00_INDEX.md`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`、`docs/modules/MODULE_MAP.yaml`、`eng/contracts/data/v6_clause_registry_v1.json`、`docs/KNOWN_LIMITATIONS.md`、`docs/GLOSSARY.md`、`docs/DEVELOPER_GUIDE.md`、`docs/TROUBLESHOOTING.md` → `[ -f ]` 全 OK） |
| 与上位冲突 | ① **无取值冲突**，"状态词表唯一口径 = §12.5（取值集合见该节，本文档不复制）"（L101）与最高设计 §12.5、`docs/owner/RELEASE_STATUS.md`、`CONTROL_PACK_SPEC.md` §6.3、`memory.md` 各表**四方分域互不冲突**，判定"唯一口径"自称成立。② **层级图三份不同构（实质缺陷）**：README L11-19 权威链表把 ⑥`docs/ci/CI_SPEC.md` ⑦`docs/plugins/00_INDEX.md` 列为**入口级**、把 `docs/science/`、`docs/algorithms/`、`docs/design/UNIFIED_MODEL.md`、`docs/contracts/` 列为"**与上述入口并列的下级权威**"（L21）；`ASTROCS_DESIGN.md` §0 mermaid 把 AGENTS/ENGINEERING/CONTROL_PACK/ACCEPTANCE/CI/plugins 画成 ①–⑦ 直系子级、science/algorithms 画成**虚线**（`-.公式.->`）、`docs/design/UNIFIED_MODEL` 画成实线；`docs/standards/DOCUMENTATION_STANDARD.md` §1 另立"仓库根规范层（第 1.5 层）"并把 `docs/` 建成第二棵树。**三张图节点集与边型不一致** → 标准 01 §3"唯一索引地图"与"双向索引"不满足。③ **层级倒置（轻）**：L52/L55-57/L60-62 四条科学目标里写出 `I_photo = k_photo·I_cal`、`SNR_F = F/σ_F`、`w(x,y) = SNR²/F_ref² = 1/σ_F²`、`Jacobian 恒为 π/3`、"轴对齐裁剪 + 鞋带公式" |
| 处置建议 | **保留 + 减重**：① 层级图不另立，指向单一定义源（待负责人在"README / 最高设计 §0 / DOCUMENTATION_STANDARD §1"三者中定一个，另二者改指针）；② 四条公式全部降为指针；③ L70 `cmake … -G Ninja` 补一句"Windows 正式构建 generator 见 `DEPENDENCIES.md`" |

### 1.7 `DEPENDENCIES.md` — 工具链版本锁定

| 字段 | 内容 |
|---|---|
| 路径 | `DEPENDENCIES.md`（仓库根） |
| 标题 | `DEPENDENCIES.md — 工具链版本锁定 (QA-005 / BLD-001)` |
| 行数 | 87 |
| 角色（标准 01 §2） | **操作与运营**（`docs/development/`）。**角色错放：是**——内容纯构建/工具链，却落根级；位置合法性仅来自 `ENGINEERING_SPEC.md` §7 L104 把它登记为根固定条目（**登记面把错位固化**） |
| 主题 | Windows x64 发布工具链冻结值、Linux 控制节点轻验证、Release flags、复现与 build id、依赖锁文件与 vendored 依赖、test-only oracle、机器路径政策 |
| 上游 | **缺"上游"行**。三处内联指针：L25"（最高设计 §9 禁硬编码线程数）"、L47"单源条款 = `docs/owner/RELEASE_STATUS.md` §2"、L85-86"（ENGINEERING_SPEC §7 根固定条目）" |
| 开头元信息块 | **有，且是本批最典型的违规样本**：① 标题行内嵌两个任务编号 `(QA-005 / BLD-001)`；② L3-6 引用块"本文件是…版本锁定权威（仓库根）…**三者冲突时以机器源为准**"——**声明了第二套权威顺序**，直接违反标准 01 §1 末条"任何文档不声明自己的第二套权威顺序或'唯一口径'"与 §4"文档开头无元信息块（文档 ID、状态编号、版本锚、纪律声明等）"。**两项都违** |
| 日期/编号/sha/历史叙事 | 日期 0、commit sha 0、sha256 0；**任务编号 5 处**：L1 `(QA-005 / BLD-001)`、L8 节题"（冻结，BLD-001…）"、L52「## 依赖锁定 (BLD-004)」、L61"（60 C 源显式清单, BLD-001 禁 GLOB…）"、L80「## 机器路径政策 (BLD-004)」；历史叙事 1 处 L86"**不是遗留待清理对象**" |
| 悬空引用 | 3 条：`build/sbom-input.jsonl`（构建产物）、`preset-contract.json`（全称实存）、**L77 `rcr_oracle_compare.py` 只给 basename，实际在 `lib/algorithms/rejection/tools/rcr_oracle_compare.py`**（`git ls-files \| grep rcr_oracle` → 1 行）。判定：**1 条锚点写法不合规** |
| 是否该主题正本 | **是，但落点错位、且不自任数值正本**：L3-6 已把数值让给 `eng/packaging/schemas/preset-contract.json`（"机器单一事实源"）与 `eng/packaging/windows/README.md`（"取值落点"）→ **它自认是登记面而非正本**，却又被标题与 L3 称"版本锁定权威" |
| 重复或重叠对象 | ① 与最高设计 §11（双平台发行）：设计给"Windows 10+ / Linux amd64 / MSVC 官方工具链"，本文给 `VS 17.14.39`、`v143,version=14.44.35207`、SDK `10.0.26100.0`、CMake `3.31.12`、C++17。**合法分层**。② **与 `docs/standards/BUILD_STANDARD.md` 实质重复**：本文 L43「优化档由 `CMAKE_BUILD_TYPE=Release` 的编译器默认决定（**本仓不显式声明 `-O2`/`-O3`，全仓无该 flag**）」与 BUILD_STANDARD §2.1「**无 `-O` flag**：全仓仅 Release 面出现 `CMAKE_BUILD_TYPE=Release`（3 处）；无显式 `-O0/-O1/-O2/-O3`」+ §5 同句**逐字重复**（括注内文字完全相同）。**判定：同主题两份，构建 flags 节应并入 BUILD_STANDARD** |
| 与上位文档冲突 | ① 平台下限细度链（本文 `Win10 22H2 build 19045` ↔ ENGINEERING §1 `Windows 10+ / Win11 主验证` ↔ 最高设计 §11 `Windows 10+`）方向一致，无值冲突，但**三份细度不同、`docs/owner/ARCHITECTURE_OVERVIEW.md` 被两份同时指为取值落点**。② **与许可证主题无交叉引用**：本文 L59-70"生产依赖"节列出 cfitsio/nlohmann-json/zlib/zstd/lz4/OpenMP/pthread 等**不带许可证列**，而本仓"第三方许可证与 SBOM 合规"正本 = `docs/standards/LICENSE_COMPLIANCE.md`。**两份互不引用**（`grep -n LICENSE_COMPLIANCE DEPENDENCIES.md` → 0；反向 grep 亦 0）→ 依赖清单与许可证登记之间**无桥**，属实质治理缺口（非取值冲突） |
| 处置建议 | **迁移 + 合并**：迁 `docs/development/TOOLCHAIN_LOCK.md`；① 删标题与正文 5 处任务编号、删 L3-6 元信息块（把三条锚点改写为节内正向条款，不写"冲突时以机器源为准"）；② "构建 flags"节并入 `docs/standards/BUILD_STANDARD.md`，本文留指针；③ 与 `docs/standards/LICENSE_COMPLIANCE.md`、`eng/packaging/dependency-lock.json` 建双向引用；④ 同步改 `ENGINEERING_SPEC.md` §7 根固定条目清单与 `eng/ci/root_manifest.json`（**属登记面变更，需前台执行**） |

### 1.8 `FATDUCK_ACCESS.md` — Windows 验证节点接入

| 字段 | 内容 |
|---|---|
| 路径 | `FATDUCK_ACCESS.md`（仓库根） |
| 标题 | Fatduck Windows 验证节点接入说明（vm-bj → Fatduck） |
| 行数 | 42（`wc -l`；末行无换行，实际 43 行文本，取 `wc -l`=42 为登记值并注记） |
| 角色（标准 01 §2） | **操作与运营**（`docs/operations/`、`docs/diagnostics/`）。**角色错放：是（根级）**。`docs/operations/` 现仅 1 份 `TOOLCHAIN_AGENT_HOST.md`，正是同类"主机与环境事实"文档 → 天然归宿 |
| 主题 | Fatduck 主机的 SSH 跳板与身份、密钥路径、免密链路、常用命令、在线时间窗、离线处理策略、使用范围 |
| 上游 | **缺"上游"行**，且**全文不引用最高设计**（`grep -c ASTROCS_DESIGN FATDUCK_ACCESS.md` → 0）。唯一上位味道在 L42-43"见审计 §14 性能实测"（无文件名）与 L36"（AGENTS.md：Windows 不可用时记录等待，不阻塞 Linux 开发）"（**无节号**）。**判定：上游 = 缺** |
| 下游 | 12 命中（root 0 / docs 3 / 实验 0 / 其他 9）。docs 侧：`docs/DOCUMENT_INDEX.yaml`、`docs/owner/ARCHITECTURE_OVERVIEW.md`、`docs/owner/SCIENCE_OVERVIEW.md`。**根 README 不引用它**（`grep -n FATDUCK README.md` → 0），即根级位置对读者不可发现 |
| 是否该主题正本 | **待定 → 倾向"不属正式文档层"**：它承载主机 IP、账号、密钥路径与在线时窗。`docs/operations/TOOLCHAIN_AGENT_HOST.md` L3 **明确把这类事实排除在运营文档外**："主机与凭据事实（`FATDUCK_ACCESS.md`，IP/密钥路径）不入本文件" → 文档体系一面否认它进 `docs/`、一面让它在根级由 `ENGINEERING_SPEC.md` §7 登记存续。**这是本批"角色/目录一一对应"原则最尖锐的反例** |
| 开头元信息块 | 无。**合规** |
| 日期/编号/sha/历史叙事 | 全零（date/taskid/sha/sha256 各 0）；历史叙事词 2 行（L34"计时等待该机器**恢复**"、L38"**恢复**后重新发起原任务"）——**动词，非留痕，合规** |
| 悬空引用 | **1 条真悬空（本批唯一一条"无所属文档的章节引用"）**：L42-43"默认配置在本机 Linux 实测 >49min CPU 不可行，**见审计 §14 性能实测**"——未给出是哪一份文档的 §14，读者无法定位（候选可能是 `docs/performance/`（2 份）或某份审计报告，**均未被指名**）。另 L19/L22 `local_file`、`C:/Users/fujia/` 为命令示例占位（非引用） |
| 与上位文档冲突 | **一处（口径而非取值）**：L43"32R 全量运行的预定宿主之一"——"32R 全量运行"在全仓权威链里无定义（最高设计 §12/ACCEPTANCE §6 只定义 M42 49 帧 + Galaxy Center 32 帧两组 L4 数据集；`32R` 字样在根九份中仅此处出现）。命令：`grep -rn "32R" *.md` → 1 命中（本行）。**判定：术语孤儿 + 未定义的运行类别** |
| 处置建议 | **迁移** 到 `docs/operations/FATDUCK_ACCESS.md`：① 补"上游：最高设计 §11（双平台发行与 Windows 正式复验）/ §12.2"；② 修 L43"见审计 §14"为完整文档路径或删除该比较句；③ 与 `docs/operations/TOOLCHAIN_AGENT_HOST.md` 建双向链接；④ 若维持根级，`README.md` "细节往哪读"表需加入口行；⑤ "32R 全量运行"要么定义要么改写为已定义的数据集名。**（是否允许内网 IP/账号入库属负责人裁决，本批只登记事实）** |

### 1.9 `memory.md` — 项目记忆

| 字段 | 内容 |
|---|---|
| 路径 | `memory.md`（仓库根） |
| 标题 | ACSD 项目记忆（GOV-005 收敛） |
| 行数 | 110（`wc -l`；正文 L1-L111，L110/L111 为超长单行） |
| 角色（标准 01 §2） | **标准 01 §2 末条明令排除的三类全占**：「历史档案、过程台账、已撤销设计**不放在正式文档层**；需要留证的台账落 `artifacts/evidence/` 并由正本引用」。本份同时是历史档案（§4）、过程台账（§5 十一条开放问题）、已撤销设计引用面（宪章条款、旧 lib/ 树）。**角色错放：是，本批最严重的一例** |
| 主题 | 自称"仓库**唯一项目记忆**"，实际四块：稳定目标、当前 SHA/版本、模块索引、已归档 + 开放问题 |
| 上游 | **有让渡声明但形式违规**：L10-11"科学/算法/架构/发布权威一律以 `docs/`（science\|algorithms\|architecture\|standards\|modules\|contracts）、最高设计 `ASTROCS_DESIGN.md`（§0 权威链）与 `docs/owner/`（L0）为准；本文件不复制长文、不承载权威判定"。**但同一块 L3 同时自称"仓库唯一项目记忆"= 在"记忆"这一主题上自 claim 唯一口径，而权威链没有任何上位条款设立"项目记忆"层** |
| 开头元信息块 | **有，9 行（L3-L11）**：文档定性、任务编号 + 日期（`GOV-005`、`SA-GOV-01`、`2026-09-02`）、归档指针、权威让渡、"不复制长文"纪律声明。**标准 01 §4 四项全违（ID、日期、纪律声明、历史叙事）** |
| 日期/编号/sha/历史叙事 | **本批最差，四项全中且量最大**：日期 **7 行**（L3/7/33/74/107/108/110：`2026-09-02`、`2026-08-21`、`2026-09-09`、`2026-09-12`）；任务/编号 **22 行**（GOV-001..005、SA-GOV-01、CLI-002、DOC-CONV-001、DOC-001、MOD-001、P1-001/P2-001/P3-002、STD-REG-001、ROOT-007、NOISE-IMPL、DISP-WCS-007/008、DISP-PSF-007、V7、L0、cprun run `Rmtxvlrtfa66eb7`、dispatch `7c0b15abaee56640`…）；**commit sha 12 行、含 40 位完整 sha 3 个**（`da3c4b4aaf64ef9b61039fabd1100ddd1f9b8540` L34/L79/L110、`6affe300…` L35、`caee3e67…` L36）**+ 短 sha 11 个**（`d8c821db`、`fb7f232a`、`778fe98e`、`a67bbc83`、`d3097319`、`19ef3722`、`009ee419`、`9e09941a`、`439f9f20`、`1a56ffb7`、`59fdeab3`）；`sha256` 词 1 处；**历史叙事密度最高**：L5-8"已整体归档至…（ARCHIVED_NON_NORMATIVE，仅追溯用，不代表当前状态）"、§4 节题"**已归档/不再活跃（history，勿当现状）**"、L72-73"旧 11 子仓合并记录、F 盘本地路径、16 线程硬编码、Python 脚本调 DLL 时代：**全部为失效现状**"、L63"（旧 `phase1/2/3 run\|validate\|plan\|inspect`、`verify`、`benchmark cpu` 用户命令**已全部删除**）"、L78-79"~~`astrocs run --phases 1,2,3` 遗留未删~~ → **已删除**"（删除线留痕）、L108"**旧控制包作废归档**（2026-09-09 owner 指令）… 未完成任务状态保留 NOT_STARTED **作历史**"、L109"**（历史条目，DOC-001 收敛）**…**均已删除**"、L108 末"**owner 将重新修订工程包（新控制包），本条为交接锚**"。**判定：与标准 01 §4 的每一条都直接冲突** |
| 悬空引用 | 扫描 37 条、**真悬空 13 条**（逐条列于 §3 索引与链接缺陷 D 组）。最重的四条：`docs/archive/history/memory_V18R2-V19_operational_log_2026-08-21.md`（L7、L74，**整棵 `docs/archive/` 不存在**）、`docs/governance/VERSION_NAMESPACES.md`（L38，**`docs/governance/` 不存在**）、`工程控制/PROJECT-GOVERNANCE-01/TASK_LIST.md`+`ACCEPTANCE.md`（L109，被称"**现行控制包状态唯一源**"却指向不存在的目录）、`run/local/HANDOFF_CONTROL_PACK_20260909.md`（L107/L108 标"**新对话必读**"）与 `run/local/execution_mode/CONTROL_PACK_EXECUTION_MODEL.md`（L106 标"全文"）——**gitignore 的一次性区被当作文档链节点** |
| 是否该主题正本 | **不是任何主题的正本**：①"稳定目标"= 最高设计 §1/§1.2/§1.3 的压缩副本且已漂移；②"当前 SHA/版本"= 现场值写死（应指单源）；③"模块索引"= 一边让渡给 `docs/architecture/MODULE_MAP.md`+`docs/modules/`、一边自己列一套旧 `lib/` 树；④"开放问题"= 过程台账 |
| 重复或重叠对象 | ① 与 `docs/standards/DOCUMENTATION_STANDARD.md` **§7「memory.md 内容规范」——该节就是本份文档的判正本**：原文「`memory.md` 只承载**不随轮次变化的稳定目标、权威落点索引与未决事项的登记格式**；「当前 HEAD=…」「当前发布版本=…」这类**会随提交失效的现值，一律改为其单一来源指针**（版本 → 根 `VERSION`，基线 → `docs/ci/CI_SPEC.md`），**正文与标题不写 GOV/任务编号**」。**本份 110 行没有一条遵守：标题带 GOV-005、§2 整节是当前 SHA/版本、§3 带任务编号、§5 十一条全是轮次过程** → 判定"副本 + 过期事实集合，处置=按已有正本重铸或删除"。② 与 `ASTROCS_DESIGN.md` §1（目标）、§13（版本单源）、§8.4（目录树）三处重叠，**全部为下位副本且全部漂移**。③ 与 `docs/owner/RELEASE_STATUS.md`（角色表的"负责人面"正本）在"状态/发布"主题重叠且取值相左 |
| 与上位文档冲突 | **五条实质冲突（逐条附命令）**：<br/>① **版本现值冲突（最硬）**：L37-38"产品版本唯一源：根 `VERSION` = **`0.11.0-alpha.2`**" vs 实际 `cat VERSION` → **`0.1.0-alpha.1`**；`docs/owner/RELEASE_STATUS.md` L58/L65/L74 同取 `0.1.0-alpha.1`；最高设计 §13 规定"版本字面量的唯一来源 = 根 `VERSION`"。**memory.md 复制了字面量并已失效**<br/>② **基线 SHA 冲突**：L34-35"当前收敛基线…HEAD = main = origin/main 三 SHA 一致 = `da3c4b4a…`" vs 本节点 `git rev-parse HEAD` = `c8f64e9a…`。文档自称"当前"，实为过期快照<br/>③ **平台口径冲突**：L22-25"正式开发/客户端/发布平台 = **Windows x64**…Linux amd64 **仅控制/静态分析/轻量编译/小合成**" vs 最高设计 §11 表（Windows 与 **Linux amd64 并列**为交付平台）、`ENGINEERING_SPEC.md` §1"C++17 **双平台**"、`AGENTS.md` §2"正式平台 Windows x64 **与** Linux amd64"。**上位已改为双平台，本份停留单平台旧口径 → 按标准 01 §1 应以上位为准订正**<br/>④ **目录树冲突**：L50-66 列 `lib/core`、`lib/io`、`lib/common`、`lib/phase3_proj`、`lib/drizzle`、`modules/services/io`、`astrocs_image_io`/`healpix_db` 等一整套旧树，与最高设计 §8.4 / `ENGINEERING_SPEC.md` §7 的 `lib/algorithms/ + lib/infrastructure/` 不同构，**这些路径在 HEAD 均不存在**（= 悬空 3–8 条的来源）<br/>⑤ **CLI 节号错 + 已删命令清单留痕**：L62-64"CLI 命令（docs/api/CLI_PROTOCOL_V1.md，**ASTROCS_DESIGN §6.2** 唯一命令树）：`normalize/mosaic/export`、`help`、`--version`…；`benchmark`、`doctor`；遗留 `run --phases 1,2,3` 已删除" ——**最高设计 §6.2 是"export（Phase3）流程"，命令树在 §7.1**（`grep -n "^### 6.2\|^### 7.1" ASTROCS_DESIGN.md` → 440 / 466）<br/>⑥ **可执行名冲突**：L23"Windows 用户只面对 **`astrocs.exe`**"、L51"`astrocs` 可执行" vs 最高设计 §7.1 L487"全部命令由唯一可执行程序提供：Windows 为 **`acsd.exe`**，Linux 为 **`acsd`**" + `README.md` L41 + §11 表。注：`ENGINEERING_SPEC.md` §15 L259 把 `astrocs`/`astrocs.exe` 列为"机器契约保留面（类 4：可执行/target/CLI 名）"→ **上位（设计/README 说 `acsd.exe`）与保留面登记（说 `astrocs.exe` 不可改）在"交付可执行名"上直接互斥，这是本批跨文档最严重的口径待裁决项**（详见 §3 X 组） |
| 处置建议 | **删除该文件，并把"项目记忆"这一层从正式文档层移除**（标准 01 §2 末条给出去向）：稳定目标 → `ASTROCS_DESIGN.md` §1 已承载；当前 SHA/版本 → 全删，由根 `VERSION` 与 `docs/owner/RELEASE_STATUS.md` 单源；模块索引 → `docs/architecture/MODULE_MAP.md` + `docs/modules/`；未决事项 → 按 `docs/standards/DOCUMENTATION_STANDARD.md` §7 的"登记格式"迁 `artifacts/evidence/` 并由正本引用（现存 11 条中仅"Windows 发布执行面 NOT_VERIFIED"与"版本收敛遗留"有现行价值，其余 9 条已闭环、由 git 历史承载）。<br/>**前置动作（顺序不可颠倒）**：① 先由负责人裁决 ⑤/⑥ 的 `acsd.exe` ↔ `astrocs.exe` 口径；② 从 `ENGINEERING_SPEC.md` §7 L104-108 根固定条目清单与 `eng/ci/root_manifest.json` 移除 `memory.md`；③ 若保留"记忆层"，则先补规范（位置、格式、禁令）并让 `AGENTS.md` §1.2 指向它——**当前 `AGENTS.md` §1.2 要求"动手前先查两类记忆"却无任何落位锚、也不引 `memory.md`，两份互不引用（`grep -n memory AGENTS.md` → 0）**；④ 删除属"删除交付物/用户数据"，按 AGENTS §10 必须上呈负责人，本批**只登记不执行** |

<!-- PROGRESS: 9/26 -->

### 1.10 `docs/science/SCIENCE_SCOPE.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/SCIENCE_SCOPE.md` |
| 标题 | `# Science Scope`（**全份最短标题，不含项目名、不含 ID**；同目录另 15 份标题形如 `# X Science (SCI-XXX)`） |
| 行数 | 120 |
| 角色（标准 01 §2） | **科学定义**（目录与角色一致，无错放）。内容实为"科学范围 + 口径 + 失效条件"总纲 |
| 主题 | ACSD 的科学处理范围、权威链入口、处理链五步、标度类别词表与换算律、假设与适用域、有效域、不保证项、失效条件、系统/随机误差、数值精度、参考文献 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §1（项目定位）、§2（核心科学方法）`（**17 份 science 全部有**这一行 `> 上游：ASTROCS_DESIGN.md §x（标题）`：`grep -l "^> 上游" docs/science/*.md \| wc -l` → 17，见 §3 索引核对表）。但标准 01 §3 还要求"标章节号 **+ 标题**"，本文只给号 |
| 下游 | 25 命中（root 0 / docs 11 / 实验 0 / 其他 14）。docs 侧 11 份：`docs/DOCUMENT_INDEX.yaml`、`docs/algorithms/HIPS_WRITER.md`、`docs/algorithms/PHASE2_COVERAGE.md`、`docs/algorithms/PHASE2_MOSAIC_WRITE.md`、`docs/audit/doc_classification.csv`、`docs/contracts/CONFIG_CONTRACT.md`、`docs/contracts/INDEX.yaml`、`docs/modules/registry/astrocs.phase2.coverage.md`、`docs/owner/SCIENCE_OVERVIEW.md`、`docs/science/PHOTOMETRY.md`、`docs/science/UNCERTAINTY_AND_COVARIANCE.md` |
| 是否该主题正本 | **是**（"科学范围 / 假设 / 失效域"唯一正本，ID 自名 `SCI-SCOPE-001`）。**且它是 science 层的被引 ID 锚**：17 份 science 中 **12 份**在元信息行写 `上游: SCI-SCOPE-001`（`grep -c "SCI-SCOPE-001" docs/science/*.md`），即它同时充当 science 层的"上游根" |
| 开头元信息块 | **无 ID 块**（只有 L3 的 `> 上游：` 行，属标准要求的东西，不算违规）。→ 与其他 **12 份**的 `> ID: … 状态: FROZEN …` 块**格式不齐**（`grep -l "^> ID:" docs/science/*.md \| wc -l` → 12；无 ID 块的是本份 + `UNIFIED_SCIENCE_MODEL` + `UNCERTAINTY_AND_COVARIANCE` + `PSF_SIGNAL_WEIGHT` 四份，另 `STAR_DETECTION` 只有 `> 状态: FROZEN` 无 ID）：本份 + `UNIFIED_SCIENCE_MODEL.md` + `UNCERTAINTY_AND_COVARIANCE.md` + `PSF_SIGNAL_WEIGHT.md` 四类无 ID 块，13 类有。**同一目录两种头部约定 = 体例不一致**（详见 §3 索引核对表） |
| 日期/编号/sha/历史叙事 | 日期 0、commit sha 0、sha256 0；编号 2 处（L31 `p1_phot.json#photometry_applied` 与 `DATA-P1-PHOTPROV-001`、L34 `run/RELEASE-05/...`）；历史叙事 0。**判定：合规** |
| 悬空引用 | 扫描 11 条，真悬空 **1 条**：L114 末句「完整清单与核验状态见 docs/references/SCIENTIFIC_REFERENCES.md **§M**」——该文件**有** `## M. 参考代码库（含许可证）`（`grep -n "^## M" docs/references/SCIENTIFIC_REFERENCES.md` → 126 行命中）→ **不是悬空**。L34 `run/RELEASE-05/vis/out/m42_p1_t3/p1_phot.json`、L53 `run/SCI-FIX-SEMANTICS-01/evidence/m42_structure_contamination.json` → **真悬空（`run/` 是一次性区，`.gitignore`，不入库不可复现）**。L93 `module_adapters.cpp:6355-6377` 只给 basename（实际在 `lib/infrastructure/scheduler/src/`）。L81 `noise_model.cpp:618` 同样只给 basename。**判定：2 条 `run/` 证据锚 + 2 条无路径源码锚** |
| 与上位冲突 | **三处**：① **§假设 与 §系统/随机误差 的互斥断言（最硬）**：L43 假设「背景为局部平稳随机场（patch 尺度）」，L47-50 给失效判据「`γ = dlog(patch 方差)/dlog(patch 中位信号)`，纯随机分量应给出 `γ ≈ 1`；γ 显著偏离 1 ⇒ …噪声场必须显式降级」，L51-52 立刻自测「**实测 γ = 1.983**（corr 0.593，262 144 个 patch）⇒ **该真实域不满足 γ ≈ 1**，前提在本帧上不成立」。但 §处理链 L19 仍写「空背景噪声模型（噪声模型 A = `NoiseWeightModelV1`，**唯一生产模型** → ivar）」、§系统/随机误差 L80-81 仍写「背景方差面…由 `NoiseWeightModelV1` 以 patch 稳健尺度估计并落成 `variance/ivar`」——**同一文档承认前提在真实数据上不成立、却没有任何一节把"生产仍使用该估计器"改为"按 §失效条件降级"或标注例外**。L73 又写「局部平稳前提被违反 → 噪声场显式降级并登记，消费口径 = 显式降级的噪声场」。**判定：判据、实测、消费口径三者在同一文档内未闭合**（实测结论 γ=1.983 应触发 L73 的降级，而 §处理链/§系统随机误差未反映）。② **§不保证 L64 与 §系统/随机误差 L86 一致**（都不保证完整 covariance），无冲突。③ **精度归属清单少一项**（`manifest 字段`，见 §1.1 重复项 3）。④ **上位指定它承载"量纲正本"，它却把口径让出去**：L25 写「**标度类别封闭词表**（唯一权威 = `docs/standards/NUMERIC_STANDARD.md`「量纲与标度」节）」，而 `ASTROCS_DESIGN.md` §3.3 的精度归属口径在 `docs/science/` 侧没有指定正本给本份——实际本份 §数值精度 又自己写了一遍。**判定：同一主题在两份里各自"让渡 + 复述"并存**（NUMERIC_STANDARD §量纲与标度 L13 亦自称"最高设计 §3.3:185「归一化」一项的细化"） |
| 重复或重叠对象 | ① 与 `ASTROCS_DESIGN.md` §3.3（精度归属）：同一清单两份（见 §1.1）。② 与 `docs/science/UNIFIED_SCIENCE_MODEL.md` §1/§2/§9（科学目标分层 / 统一线性观测模型 / Phase 间最小合同）：**主题重叠**——"科学范围与目标"两份；差异：SCIENCE_SCOPE 给**处理链 + 单位 + 假设 + 失效条件**，UNIFIED_SCIENCE_MODEL 给**目标分层 + 观测模型 + 术语表 + 点源/扩展源统计量 + 跨阶段合同**。**判定：合法分层不成立（两者同层：都是 science，且都自称上游 §2/§3.1）→ 需归并或分工写明**。③ 与 `docs/science/NOISE_MODEL.md` §5：本份 L80-85 复述了"两个方差面"的口径（背景方差面 / 加权方差面）并给 `noise_model.cpp:618` 锚——NOISE_MODEL §5/§5c 是正本，本份属**摘要级复述**，含数值 `source==0`、`max(signal,0)/gain + (read_noise_e/gain)²` 公式，属标准 01 §4 不允许的"公式放错层"。④ 与 `docs/science/UNCERTAINTY_AND_COVARIANCE.md` L46 反向引用本份 §假设作其 `sigma_bg` 前提的判据来源——**双向引用成立，分工模糊**（γ 判据归谁没写）。 |
| 处置建议 | **保留（科学范围主题正本）**，四项修订：① L19 处理链第 3 步与 L80-85 需按 L73 的降级条款加"当前生产域前提不成立 ⇒ 消费口径 = 显式降级"或把 γ 实测移入 `docs/algorithms/NOISE_ESTIMATION.md`；② §数值精度 的 `manifest 字段` 补回、公式与源码行号下沉；③ 与 UNIFIED_SCIENCE_MODEL 的分工写明（建议：SCOPE=范围/假设/失效，UNIFIED=模型与术语正本）；④ 两条 `run/` 证据锚迁 `artifacts/evidence/` 并改为可复跑命令 |

### 1.11 `docs/science/UNIFIED_SCIENCE_MODEL.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/UNIFIED_SCIENCE_MODEL.md` |
| 标题 | Astro Celestial Sphere Database（ACSD） 统一科学定义与跨阶段合同 |
| 行数 | 143 |
| 角色（标准 01 §2） | **科学定义**（目录一致）。但它同时是"数据对象与术语表"，该主题的**上位正本被最高设计指定为 `docs/design/UNIFIED_MODEL.md`**（L219、附录 A L821）→ **同名异体、职责重叠**：`docs/design/UNIFIED_MODEL.md`（设计层）与 `docs/science/UNIFIED_SCIENCE_MODEL.md`（科学层）两份"UNIFIED_*" |
| 主题 | 科学目标五分层、统一线性观测模型 `d=Ax+n`、权威术语表（signal/variance/ivar/source SNR/depth m5/point information/support/coverage/validity/rejection）、点源与扩展源最优统计、单一权重口径、校准与系统误差、重采样与 Drizzle、空间模型与标量压缩、Phase 间最小合同、科学验收、口径边界、参考文献 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §2（核心科学方法）、§3.1（数据对象）` |
| 下游 | 16 命中（root 0 / docs 7 / 实验 0 / 其他 9）。docs 侧：`docs/DOCUMENT_INDEX.yaml`、`docs/contracts/CONFIG_CONTRACT.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/owner/PROJECT_SPEC.md`、`docs/references/PHOTOMETRY_LITERATURE_REVIEW_ARCHIVE.md`、`docs/science/PSF_SIGNAL_WEIGHT.md`、`docs/validation/v6/QA_MATRIX.md`。**注：最高设计 §3.1 的被引对象是 `docs/design/UNIFIED_MODEL.md`，不是本份**（`grep -n "UNIFIED_MODEL" ASTROCS_DESIGN.md` → L3/L219/L821 三处，全指 design） |
| 是否该主题正本 | **部分是正本、部分越层**：§3 权威术语表、§2 观测模型、§4/§5 最优统计 = science 正本；§9「Phase 间最小合同」属**合同主题**，正本是 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md` 与 `docs/contracts/`（最高设计 §1.2/§8.1 指定）→ **越层** |
| 开头元信息块 | 无 ID 块（仅 `> 上游：` 行 + L6「适用：Phase1/2/3 全部 SCI、ALG、DATA、API 和产品 schema。」）。**判定：与同目录 12 份的 `> ID: … 状态: FROZEN` 体例不一致**；L6 的"适用范围"行属可接受正文 |
| 日期/编号/sha/历史叙事 | 日期 0、sha 0、sha256 0、任务编号 0；历史叙事词 1 行（L62 `FZ-MODE-RETIRED` / `FZ-FIELD-WEIGHTMODE` / `FZ-WEIGHT-SINGLE-PATH` 三个禁改标记——**是 fail-closed 词表 ID，不是历史叙事，判定合规**）。**但 L62 措辞「越界 token 一律 fail-closed 显式拒绝」+ §4.1 L56「单一权重口径，没有可选择项」= 与 §11 口径边界同义复述（同文档内两次声明"唯一口径"）** |
| 悬空引用 | 扫描 5 条，**0 条缺失**：`docs/science/DRIZZLE.md`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`、`docs/science/CONTROL_WEIGHT_SNR.md`、`docs/science/PHASE2_UPM.md`、`ASTROCS_DESIGN.md` 全实存。**但两处节号锚有误**：① §3 术语表列 `source_snr` 而 §12 写「本文件 **§3 表的 frame_snr**」——**§3 表内没有 `frame_snr` 这一行**（命令：`sed -n '25,36p' docs/science/UNIFIED_SCIENCE_MODEL.md \| grep -c frame_snr` → 0；表里有的是 `source SNR` 与 `depth m5`）→ **自引用节号错**；② §7 L85 引「`ASTROCS_DESIGN.md` §5.5」为 NaN 剔除项逐条登记的依据，而 §5.5 是"逐像素排异"、其中只写一句「NaN 采用样本级掩膜…规则见 `docs/science/` 对应章节」——**引用环回 science 自身，正本实为 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md` §2a**（同一句 L85 后半已指它）→ 双锚、上位锚是空转 |
| 与上位文档冲突 | ① **术语表与上位不一致**：最高设计 §3.1 L223 的数据对象清单含 `depth_m5` 与 `point_information`，本份 §3 表用 `depth m5` / `point-source information`（含空格、非字段名），而 §11 又写「1/Var(F_hat) 与 Fisher information/ivar 属信息量面」；**`frame_snr` 在 §3 表缺席**（上条已述）→ 上位声明"全仓只有一份正本定义"的对象集合与表内行不齐。② **§4.1 权重口径与 CONTROL_WEIGHT_SNR §5 互斥**（见 §2 主题组 G3）。③ **§8 L95「不能以节省 18–77 MB 为由先删科学信息再证明」= 把一次具体实验的实测数字（18–77 MB）写进规范正文**，无来源、无单位口径（是哪种帧尺寸/位深下的 18–77 MB？）→ 标准 01 §4"数值表放对应下层文档"与 §"常数须有推导"双违。④ 与最高设计 §1.3/§8.4「ACR 生产不可达」不冲突（本份 §7 未涉 ACR） |
| 重复或重叠对象 | ① §3 权威术语表 ↔ `ASTROCS_DESIGN.md` §3.1（对象清单）↔ `docs/design/UNIFIED_MODEL.md`（数据对象表正本）↔ `docs/GLOSSARY.md`：**同一对象集合四处登记**；正本按最高设计是 `docs/design/UNIFIED_MODEL.md`。② §2 统一线性观测模型 ↔ `docs/science/PHASE2_UPM.md` §5（`calibrated = raw − C_f`）与 `docs/science/NOISE_MODEL.md` §5c 参考分解：三份"模型"层级不同，**合法分层**。③ §4/§4.1 ↔ `docs/science/PSF_SIGNAL_WEIGHT.md` §2/§4（同一 `Q/W` 公式与同一"单一口径"断言）——**这两份是实质重复**：PSF_SIGNAL_WEIGHT L22-26 给 `Q_k = a_k P_kᵀ C_k⁻¹ d_k; W_info,k = a_k² P_kᵀ C_k⁻¹ P_k; F_hat = ΣQ/ΣW; Var = 1/ΣW`，UNIFIED L44-48 给逐字同式（仅符号名 `W_k` vs `W_info,k`）；两份又各自声明"唯一权重口径"（PSF_SIGNAL_WEIGHT §4 L70-93、UNIFIED §4.1 L52-62）。**判定：同主题两份正本 → 归并**（详见 §2 G3）。④ §9 Phase 间最小合同 ↔ `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`：**越层重复**。⑤ §10 科学验收 ↔ 最高设计 §12.4 与本份 §10 的"每个近似有 mutation 能使门变红" ↔ `ACCEPTANCE_SPEC.md` §3：三处同义。 |
| 处置建议 | **保留 + 拆分**：① §9 迁 `docs/interfaces/`（或降为指向 DATA-002 的指针），② §3 术语表与 `docs/design/UNIFIED_MODEL.md`/`docs/GLOSSARY.md` 三处合一，本份只留"哪个量可作权重"的**科学判定**列，③ §4/§4.1 与 `PSF_SIGNAL_WEIGHT.md` §2/§4 合并为一份（建议保 PSF_SIGNAL_WEIGHT 的严格信息量推导 + 本份的"无可选口径"条款，正本页名由 AUD-102 定），④ 修 §12 的 frame_snr 自引用、§8 删实测 MB 数或补推导与适用域，⑤ 补与同目录一致的 ID 体例 |

### 1.12 `docs/science/PHOTOMETRY.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/PHOTOMETRY.md` |
| 标题 | Photometry Science (SCI-PHOT) |
| 行数 | 418 |
| 角色（标准 01 §2） | **科学定义**（一致）。**但 §16 整节（L341-418，占全份 19%）是"方法链 + 论证 + 指针"** → 该节内容属 **算法推导 / 研究佐证** 角色，与 §1-§15 的"定义式 + 有效域 + 不变量 + Oracle"科学定义角色混装一份 → **一份跨三角色** |
| 主题 | 仪器流量 → Gaia XP 合成通带刻度（CALSPEC 溯源）的测光零点/尺度定义、IRLS/Tukey 稳健拟合、`F_syn` 合成口径与量纲推导、通带身份核对、误差预算构成、星数依赖与降级语义 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §2.1（创新点一：测光星等坐标系）、§4.4（输出合同）`；§16 头（L343）**再次**列出上游三条 + 研究包——**同一文档两处声明上游**（L3 与 L343），两份清单内容不完全同（L343 多 §4.2） |
| 下游 | 70 命中（root 2 / docs 21 / 实验 15 / 其他 32）。根侧被 `ASTROCS_DESIGN.md`（§2.1 量纲、§4.4 误差预算两处）与 `README.md` L53 引用；docs 侧 21 份含 `docs/science/{ASTROMETRY,CALIBRATION,STAR_DETECTION}.md`、`docs/plugins/algorithms_phase1/06_photometry.md`、`docs/research/PHOTOMETRY_RESEARCH_PACK.md`、`docs/references/PHOTOMETRY_LITERATURE_REVIEW_ARCHIVE.md`、`docs/design/PHASE1_DETAILED_DESIGN.md`、`docs/algorithms/PHOTOMETRIC_FIT.md`、`docs/contracts/{CONFIG_CONTRACT,DATA_SEMANTICS,PUBLIC_API,INDEX}.md`、`docs/owner/SCIENCE_OVERVIEW.md` |
| 是否该主题正本 | **是**（测光标定科学正本；最高设计 §4.4 明确"推导过程与逐项数值见 `docs/science/PHOTOMETRY.md`"）。**§16.4 却又把误差预算数值让给 `docs/plugins/algorithms_phase1/06_photometry.md` §4.1**（L377「判据形态…本节不复述数值…逐项实测见 …§4.1」）→ 上位说数值在本份、本份说数值在插件文档 → **误差预算数值的正本位置三方不一致**（最高设计 §4.4 / 本份 §16.4 / 插件 §4.1） |
| 开头元信息块 | **有**：L5 `> ID: SCI-PHOT-001  状态: FROZEN (T103 冻结, 2026-08-23)  上游: SCI-SCOPE-001  下游 ALG: ALG-PHOT-001..  模块: photometric_calib (flux_calibrator)`。**违标准 01 §4 三项**：状态编号、**日期 `2026-08-23`**、任务号 `T103`；"模块: photometric_calib (flux_calibrator)"是**已退役模块名**（现树为 `lib/algorithms/photometry`，见 §13 L289 自己写的实现路径）→ 元信息块内即含过期事实 |
| 日期/编号/sha/历史叙事 | 日期 3 行（L5 `2026-08-23`；L308/L309「2026-09-23 抓取核验，HTTP 200」）；编号 6 行（L39 `SCI-PHOT-FORMULA-01`、L143 `PASSBAND-IDENTITY-GATE-001`、L299/§9「SCI-001-S2 补齐」、L341 `DOC-404 增补`、L375「数值属 SCI-401」、L344「误差预算的数值与实验属 SCI-401」）；sha 2 行（L303/L304 命中的是 DOI `10.1080/01621459.1993.10476408` 等数字串被 8 位正则误命中，**非 commit sha**）；历史叙事 **L296「4 位截断写法 `0.6745` 与全精度值相对差 +1.5196e-05…（W4-A6 处置 V12-N-03）」**——`W4-A6 处置 V12-N-03` 是**轮次处置编号留痕**，标准 01 §4 禁；§2a.4 标题「机制与判别证据」段 L99「（已判定，任务 SCI-PHOT-FORMULA-01）」亦属过程留痕。**判定：本份是 science 目录里"日期 + 任务编号 + 轮次留痕"三项齐全的样本之一** |
| 悬空引用 | 扫描 55 条 / 报缺 23，逐条判定：<br/>**A. 只给 basename 的源码锚（路径不实，7 条）**：`star_matcher.cpp`（L28/30/190/191/192/205/218/244/245/246，实际 `lib/algorithms/photometry/cpp/src/star_matcher.cpp`，§13 L289 给了全路径 → **同一文档内两种锚法混用**）、`pc_api.cpp`（L34/218/289）、`frame_photometry_fit.cpp`（L112）、`spectrum_integrator.cpp`（L135/253）、`gaia_client.c`（L50/317）、`normalize.phase_config.json`（L411，全路径给了）。<br/>**B. `run/` 一次性区证据锚（6 条，真链断）**：L98/L135/L136/L137/L250/L251/L253 `run/SCI-PHOT-FORMULA-01/evidence/{c1_analytic_check,c1_probe_raw.tsv,c2_negative_controls,a1_xpsd_absolute_check,a2_bandpass_and_absolute,d1_zp_sigma_rederive,q1_qe_discriminate,q2_qe_mechanism,q3_qe_colour_chain}.json`；L377 `run/RELEASE-02/parallel/06.md`；这些路径 **`.gitignore` 且不入库**，作为"生产口径的判定证据"落在正式文档里 → 不可复现。<br/>**C. 缺 §号（2 条）**：L50 `DATA_SEMANTICS.md §14.2`（实存）与 L75「`DATA_SEMANTICS.md` §14.3」（实存）→ 经 `grep -n "^## 14" docs/contracts/DATA_SEMANTICS.md` 复核存在，判定**非悬空**；L392 `06_photometry.md §4.1`（实存）。<br/>**D. 真悬空（文件级）**：**L337 `实验/photometric-magnitude/results/step7_negatives.json → N5`、L405 `…/step5_calibration_gate.json → items_measured`、L407 `…/results/step3_forward_vs_photflam.json`** —— 逐个核对：`实验/photometric-magnitude/results/` 下这三个 json **全部实存**（`git ls-files 实验/photometric-magnitude/results`）→ 非悬空。**最终：本份无文件级真悬空，但有 12 处 `run/` 不可复现锚 + 6 处 basename 锚** |
| 与上位文档冲突 | **一处取值级冲突 + 一处措辞冲突**：<br/>① **`I_photo` 是否含 `m(x,y)`**：本份 §16.1 步骤⑤ L354 写 `I_photo = k_photo·m(x,y)·I_cal`、§1 L13 写 `I_photo = k_photo·m(x,y)·I_cal（§16.1 ⑤）`；**最高设计 §2.1 L125 与 §4.4 L330、`README.md` L52 写 `I_photo = k_photo·I_cal`（不含 `m(x,y)`）**，只有最高设计 §4.2 L276 与 §5.4 L408 含 `m(x,y)`。命令：`grep -n "I_photo = " ASTROCS_DESIGN.md README.md docs/science/*.md` → DESIGN 两处两种写法、README 一处不含、PHOTOMETRY 两处均含。**判定：同一公式在最高设计内部就有两份写法，下位取其中一份 → 需订正上位（按 AGENTS §8 变更流程）**。<br/>② **`docs/science/` 的 `sigma_residual` 与 `06_photometry.md` 的 `σ_obs` 是否同物**未定义（本份 §16.5 用 `σ_obs`、§5 用 `sigma_residual`，无桥）。<br/>③ 与上位**无冲突**的正面项：§1「消除物理单位」的两面拆分与最高设计 §2.1/§4.4 一致；§10「不为 `k_photo` 设绝对窗口」与最高设计 §4.4「不设绝对数值窗口」一致 |
| 重复或重叠对象 | ① **`F_syn` 定义式两份**：§2 符号表 L24 与 §2a.1 L44 同式（后者带量纲），后者声明前者口径 → 合法（同文档内定义/摘要）。② **与 `docs/algorithms/PHOTOMETRIC_FIT.md`**：IRLS/Tukey 迭代的算法推导两份（本份 §5 给公式与常数、算法页给数值方法），**合法分层**；但本份 §5 已含实现行号，与算法页重叠。③ **与 `docs/references/PHOTOMETRY_LITERATURE_REVIEW_ARCHIVE.md`**（`docs/references/` 2 份之一）：本份 §14/§14a 的 11 条一手文献与档案页重叠，标准 01 §2「研究与佐证类以引用方式服务正本，不重复承载正本口径」→ **需核档案页是否在复述正本口径**（跨批项，登记 X-3）。④ **与 `docs/plugins/algorithms_phase1/06_photometry.md` §4.1**：误差预算两份（见"是否正本"栏冲突）。⑤ **与 `docs/science/SCIENCE_SCOPE.md` §变量/单位**：`x′=α·x ⇒ Var′=α²·Var, ivar′=ivar/α²` 三处复述（本份 L13/L182、SCIENCE_SCOPE L35、NOISE_MODEL §5 缩放律），**同一换算律四处**，正本应为 `docs/standards/NUMERIC_STANDARD.md`「量纲与标度」 |
| 处置建议 | **保留（主题正本）+ 三项迁移 + 四项清理**：① §16（方法链/论证/指针）整节迁 `docs/algorithms/PHOTOMETRIC_FIT.md` 或 `docs/research/PHOTOMETRY_RESEARCH_PACK.md`，本份留指针（标准 01 §2 角色一一对应）；② 12 条 `run/` 证据锚迁 `artifacts/evidence/photometry-formula-01/` 并把"复现命令"写全；③ 误差预算数值正本三选一（建议 `docs/plugins/algorithms_phase1/06_photometry.md` §4.1，本份与最高设计 §4.4 均改指针）；④ 删 L5 元信息块的日期/T103/退役模块名；⑤ 删 L296 的 `W4-A6 处置 V12-N-03` 留痕；⑥ 统一 6 处 basename 源码锚为全路径；⑦ 订正上位 `I_photo` 公式（走变更流程） |

### 1.13 `docs/science/CONTROL_WEIGHT_SNR.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/CONTROL_WEIGHT_SNR.md` |
| 标题 | Control-Weight SNR / Frame Quality Science (SCI-CW) |
| 行数 | 260 |
| 角色（标准 01 §2） | **科学定义**（一致）。标题里的 "Frame Quality" 与内文"相对质量权重场"是同一主题的第二个名字，与 §2a/§2b 的"绝对 SNR"主题**在同一份文档里并存两个口径** |
| 主题 | Phase2 控制采样用的相对质量权重场（`local_snr`/`frame_snr_medians`/`quality_weight`）与逐源/逐像素**绝对 SNR** 的规范式、单一权重口径、三口径适用域图谱、SCI-B 实验结论 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §2.2（创新点二：跨帧绝对信噪比）、§5.3（SNR 重建与逆方差叠加）` |
| 下游 | 40 命中（root 2 / docs 13 / 实验 10 / 其他 15）。**root 侧 2 份：`ASTROCS_DESIGN.md`（§2.2 L181 显式指它）与 `README.md`（L57）**——是 science 里被最高设计正文直接点名的三份之一（另两份 NOISE_MODEL、PHASE2_UPM） |
| 是否该主题正本 | **是**（权重与 SNR 定权口径的科学正本）；**但 §2a 与 §8c 是"权重口径"的正本候选，§4/§5 与 §1「重定义注记」是"质量场"的正本**，两半主题不同 → **主题内二分**（见冲突①） |
| 开头元信息块 | **有，且是三段**：L5-7 `> ID: SCI-CW-001..008  状态: FROZEN  上游: SCI-SCOPE-001,`（**该行在逗号处截断、续到 L6-7**，是排版断裂的元信息块）；L9-10 `> local_snr 与 frame quality 的权威定义 = 本文件；code 与 docs/architecture/execution_inventory.csv 中的表述不构成 science authority`；**L12-23「> **重定义注记**」12 行 + L18「（claim `FIX-SCI-SNR-CANON-001`）」）** → 标准 01 §4「文档开头无元信息块（文档 ID、状态编号、版本锚、纪律声明等）」**四项全违**，且"重定义注记"正是 §4 禁的"历史叙事需要表达硬约束时用正向条款"的反例 |
| 日期/编号/sha/历史叙事 | 日期 0；sha 0；sha256 0；任务编号 5 行（L18 `FIX-SCI-SNR-CANON-001`、L128 `SCI-UPM §5:54 / DATA-UNC-001 §51 / DESIGN §4.3`、L240「SCI-001-S2 补齐」、L248 再次 `FIX-SCI-SNR-CANON-001`、L195/L207/L215 的 `EXP-04`/`SCI-B` 属实验单元正式名）；**历史叙事：L12「> **重定义注记**」+ L15「字段名 `snr` 仅为兼容保留」+ L23「stage2 侧字段改名 `quality_weight`…登记为实现跟随项」+ L248「同名两义分离」** → **同一文档四处叙述"这个名字以前是错的、现在改名"**，标准 01 §4 直违 |
| 悬空引用 | **三条确证的悬空/错锚**（本批最硬）：① L128 与 L160 引「**DATA-UNC-001 §51**」为"Phase2 集成权重恒为逐样本 ivar"的出处——`docs/contracts/DATA_SEMANTICS.md` 实际最大节号是 **§31**（`grep -n "^## " docs/contracts/DATA_SEMANTICS.md` 末 5 行为 §27/§29/§28/§30/§31），**不存在 §51**；DATA-UNC-001 实际在 **§30**（L2699）。→ **悬空节号**，同类误引另见 `docs/algorithms/PHASE2_INTEGRATION.md` L129（跨批登记）。② L160 引「**DESIGN §4.3/§4.4**」为同一条权重口径的出处——最高设计 §4.3 是"输入合同（JSON 数据块）"、§4.4 是"输出合同"，**两节内 `权重` 出现 0 次**（命令：`awk 'NR>=278 && NR<=331' ASTROCS_DESIGN.md \| grep -c "权重"` → 0）；权重口径实在 §5.3 L399 与 §3.1 L225-229。→ **节号错**。③ L160/L128 引「**SCI-UPM §5:54**」——`docs/science/PHASE2_UPM.md` §5（L64-122）第 54 行不在 §5 范围内，实际 `control_ivar` 定义在 §5 的 L73/L76 → **行号锚漂移**（同一文档 §4 才在 L54）。④ L221 引 `07_noise_snr.md §4.5`、L240 引 `GATES_AND_TOLERANCES.md`（无 §号）→ 前者实存、后者为泛指。⑤ L195/L207 引 `results/b1_sky_scan.json`/`b3_domain_map.json`/`b6_gates_audit.json`（`实验/absolute-snr/results/` 下**实存**）。⑥ L201 引 `snr_science.cpp`/`snr_estimator.cpp` basename（实际在 `lib/algorithms/noise_snr/…`）→ 无路径锚。**判定：1 条真悬空（§51）+ 2 条错锚（DESIGN §4.3、§5:54）+ 2 条无路径源码锚** |
| 与上位文档冲突 | **四处**：① **§5 L159-161「唯一权重口径 = 逐样本 `ivar` 逆方差，无 fallback」与本文件 §8c L229-233「唯一，无分支：`w = SNR²/F_ref² ≡ 1/σ_F²`」+ 最高设计 §2.2 L156-158 互斥**（G3 组，本批最重要发现）。② **§2b L104 引「节点间距按 `docs/science/PHASE2_UPM.md` §7a 由输入几何导出」——UPM 的节点间距规则在 §7a 规则 2（L185-188），节号实存且语义对** → 无冲突。③ **§8b L211「`frame_reconstruct`：HST 类高对比结构域…帧级标量更优（Δ≥32 起）」与 §8b L215 的条件式（换 `..._mesh_median_v1` 后稀疏更优）+ 最高设计 §5.3 L402 的转述**：三份同结论、条件不同表述；本份自身两处一致（L215 已加限定），最高设计也加了"该结论与重建算子绑定" → **无冲突，属合法分层**。④ **§2a L52-55「科学 SNR 的成立条件 = 该帧已产出 `σ_F`（fail-closed）」与 §2 符号表 L44「`sigma_F`…当前实现不产出」并存** → 本文件自证"成立条件当前不满足"，但**没有任何一节声明生产处于 fail-closed 路径**（对比 PHASE2_UPM §16.3 有"待满足项"表）。→ **文档自洽性缺陷（标准 01 无编号但属"自洽"硬要求）** |
| 重复或重叠对象 | ① 与 `docs/science/PSF_SIGNAL_WEIGHT.md` §4：**同主题两份**（见 1.13 冲突①）。② 与 `docs/science/NOISE_MODEL.md` §5c：`sigma_slow²/sigma_w²` 两方差面在 §2b L92-99 与 NOISE_MODEL §5/§5c 各写一遍，公式一致（`sigma_w² = sigma_slow² + S_src/g`），差异只在 NOISE_MODEL 给逐项口径表 → 合法分层 + 复述。③ 与 `docs/science/UNIFIED_SCIENCE_MODEL.md` §4.1：`w = SNR²/F_ref² ≡ 1/σ_F²` 两份同式（后者无"逐样本 ivar"表述）。④ 与 `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.5（L220/L221 引用其"选择规则/估计器标识"）→ 本份给判据数字、插件页给算子定义，**分工成立**，但 L215 说「判据与数字正本见 `实验/absolute-snr` EXP-04 §3.2/§4.4/§5.3」——**把科学判据的正本放在实验单元目录而非 docs/science**，标准 01 §2 无此角色格（实验单元属"研究与佐证/科学证据"层）。 |
| 处置建议 | **保留（主题正本）+ 三项硬修**：① 修三处悬空/错锚（`§51`→`§30`、`DESIGN §4.3`→`§5.3/§3.1`、`§5:54`→去行号）；② 把 §4/§5 的"质量场"与 §2a/§2b/§8c 的"绝对 SNR 与定权"**拆成两份**（角色分别是科学定义的两个主题），或在本份内明确"两节两口径"的边界并删除"重定义注记"改为正向条款；③ 统一 G3 组的权重口径（需负责人裁决）；④ 删 ID/状态/claim 元信息块 |

### 1.14 `docs/science/PSF_SIGNAL_WEIGHT.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/PSF_SIGNAL_WEIGHT.md` |
| 标题 | PSF 信号权重与帧级 SNR 的科学定义 |
| 行数 | 162 |
| 角色（标准 01 §2） | **科学定义**（一致）。§3 的 PixInsight 公式逐字核对块（40+ 行）实为**研究与佐证**内容 → 一份跨两角色 |
| 主题 | 严格点源信息权重 `W_info`、PixInsight PSFSW/PSFSNR 方法学与本仓复合量的差异、单一权重口径、诊断量边界、SCI-B 定案结论 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §2.2（创新点二）、§4.4（输出合同）`；**另有 L5-6 第二份上游声明**「上位：docs/science/UNIFIED_SCIENCE_MODEL.md ／ 依据：PixInsight PSF Signal Weight/PSF SNR 方法、Horne/Naylor 最优提取、Zackay & Ofek…」→ **同一文档两套上游（最高设计 + 同层另一份 science）**，标准 01 §1"每一层只由它的上一层推出" + §3"标明其上游最高设计条款"→ **自造第二套上位关系 = 权威链缺陷** |
| 下游 | 35 命中（root 1 / docs 12 / 实验 6 / 其他 16）。root 侧唯一：`ASTROCS_DESIGN.md` §4.4 L323「定义式、噪声项组成、天空受限分支、参考通量口径见 `docs/science/PSF_SIGNAL_WEIGHT.md`」→ **最高设计把帧级 SNR 的定义式正本指给本份，本份 §1-§2 却只给 `W_info` 点源信息权重、未给 `frame_snr` 的定义式与天空受限分支**（那份内容在 `docs/plugins/algorithms_phase1/07_noise_snr.md` 同句并列）→ **上位指定的正本与实际内容不符** |
| 是否该主题正本 | **待定**：`W_info`（点源信息权重）正本是本份 §2；`frame_snr` 定义式正本按最高设计是本份、按实际内容与 CONTROL_WEIGHT_SNR 的指认是 `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.1 → **两份上位指向矛盾，需裁决** |
| 开头元信息块 | **有（L5-6 两行"上位/依据"）**，但**无 ID、无状态、无日期** → 属"第二套权威序"型元信息，标准 01 §1 末条直违 |
| 日期/编号/sha/历史叙事 | 日期 **1 处：L41「> **PixInsight 官方公式（2026-09-17 核对；GitLab … master，与官网 … 式号一致）**」**；任务编号 1 处（L141 节题「§9 参考文献与参考代码库（含许可证）— **SCI-001-S2 补齐**」）；sha 1 处（L122「固定 seed **20260921**」——是 seed 不是 commit，但同为一次性数值）；sha256 0；历史叙事 **L58「对象身份 `psfsw_robust_weight` **已退役**…旧产品声明该 token ⇒ 显式拒绝 + 迁移提示」+ L150「**已定案（原 UNRESOLVED）**：与 `docs/science/CONTROL_WEIGHT_SNR.md` §2a 的 `frame_snr` 语义冲突已按「同名两义分离」定案（claim `FIX-SCI-SNR-CANON-001`）」+ L91「已退役对象 token（`psfsw_robust`）、延迟口径（`psf_snr_power`）、legacy token…」+ L201「legacy 缺省与双计臂逐位一致（**向后兼容**）」**→「已退役/原 UNRESOLVED/旧产品/legacy/向后兼容」五处历史叙事。**判定：本份在"历史叙事"一项上是 science 目录最重的两份之一（另一份是 CONTROL_WEIGHT_SNR）** |
| 悬空引用 | 扫描 15 条 / 报缺 6：① **L43 与 L53 两处「未独立核验」自证断链**——L43「`PSFFluxPower=Σf_j²`…**该串在 `02-PSF_Flux_Weighting_Algorithms.pidoc` 全文（25471 字符）中不存在**，应在 PCL 头文件侧，**需补一手锚点后才可引用**」；L53「**未独立核验**：PCL 头文件在 `gitlab.com/pixinsight/PCL` 的常见路径上 404，该行数值与下一条元数据断言均待补一手锚点」→ **文档内明写"引用锚不存在"**，属自登记的悬空（应移入 `docs/research/` 或标注为未落地）。② L125 引 `07_noise_snr.md §4.2a`（实存）、L122 `code/run_all.sh`（`实验/absolute-snr/code/run_all.sh` 实存）、L126/L130 `results/b4_integration.json`/`b6_gates_audit.json`（实存）。③ L41/L148 的 pixinsight.com URL 属外部资源（本节点不访问外网核链）。**判定：0 条仓内文件级悬空；2 条自证缺失的外部一手锚** |
| 与上位冲突 | ① **§4 L70-93 与 CONTROL_WEIGHT_SNR §5 的权重口径互斥**（G3）。② 与最高设计 §2.2 L187「PixInsight 的 PSF Signal Weight 是叠加权重、不是信噪比，ACSD 入库的是信噪比本身」——本份 §3 L37-56 用 20 行论证"PSFSW 是权重、PSFSNR 是功率比、二者都不入库"，**与上位一致**，属合法下钻。③ 与最高设计 §3.1 L225「全程只有 SNR，没有"权重模式"这个概念」一致（L80「没有可选择的口径」）。④ **L107「`W_info(x,y)` 默认是空间量；只有通过均匀性/信息损失门才压为帧标量」与 §4 L72「科学叠加权重只有一个口径：Phase2 消费时由入库的绝对 SNR 现场换算为逆方差」**：一个把权重定义为 `W_info`（点源信息权重），一个定义为 `SNR²/F_ref²`；两者在 §4 L78「点源目标下它与严格点源信息权重同值」处搭桥——**该等式成立条件是"白噪声近似 + 同一 `F_ref`"，本份 §2 L31-33 给了白噪声式，但未在 §4 声明桥接条件**（只在 §8c 有配对口径）。→ **条件缺失，非取值冲突** |
| 重复或重叠对象 | ① **与 `docs/science/CONTROL_WEIGHT_SNR.md` §8c/§2a（最重的重复对）**：两份都给「单一权重口径 + `w=SNR²/F_ref²≡1/σ_F²` + 阶段一/二分工三步链」。逐段对照：本份 L82-89「阶段一 产出稀疏 SNR 控制点（控制点存绝对 SNR，不乘/除帧级标量）／阶段二 先用每帧的稀疏控制点重建稠密 SNR 面…再取逆方差（最优功率）定权 w(x,y)=… → 叠加」；CONTROL_WEIGHT_SNR L237「**生产默认组合**：阶段一产**稀疏** SNR 控制点（控制点存**绝对** SNR，不乘/除帧级标量）→ 阶段二用**每帧的稀疏控制点重建稠密 SNR 面** → 取逆方差定权 → 叠加」。**差异仅在最后有无 `→ 叠加` 与措辞粗体**；两份都在同段声明"不存在第二口径/fail-closed 拒绝 token"。**判定：同主题两份正本 → 必须合并**。② 与 `docs/science/UNIFIED_SCIENCE_MODEL.md` §4/§4.1：`Q/W` 公式两份（见 1.11）。③ 与 `docs/references/PHOTOMETRY_LITERATURE_REVIEW_ARCHIVE.md`、`docs/research/SNR_WEIGHT_RESEARCH_PACK.md`（最高设计附录 B L826 点名）：§3 的 PixInsight 一手核对内容与 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md` 的"帧级 SNR/PSFSW 专项文献"重叠 → 按标准 01 §2「研究与佐证类以引用方式服务正本，不重复承载正本口径」，**§3 的核对块应整体迁 research，本份留结论与指针**（跨批核 X-4）。④ 与 `docs/science/NOISE_MODEL.md` §9a 第 3 条（双计读噪的口径冻结）：两份各写一遍 `SHOT_ONLY`/`EMPIRICAL_TOTAL_RMS`，本份 L125 与 NOISE_MODEL L324 内容重叠、后者更细（含实测 +12.8%~+34.0%）→ 下位之间重复。 |
| 处置建议 | **合并 + 拆分**：① 与 CONTROL_WEIGHT_SNR §8c 的"单一权重口径"三处（本份 §4、CW §2a/§8c、UNIFIED §4.1）**合一**，正本随 G3 裁决落 `docs/science/`（建议 CONTROL_WEIGHT_SNR 改名或本份并入）；② §3 PixInsight 核对块（40 行）迁 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md`，本份留"两对象不入库、只作方法学对照"一句 + 指针；③ 修两份上游为一条（删除 L5-6"上位：另一份 science"）；④ 清 L41 日期、L150/L58/L91/L201 历史叙事为正向条款；⑤ 落实最高设计 §4.4 指定的 `frame_snr` 定义式与"天空受限分支"（本份补 or 上位改指） |

### 1.15 `docs/science/NOISE_MODEL.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/NOISE_MODEL.md` |
| 标题 | Noise / Variance / Ivar / SNR Science (SCI-NOISE) |
| 行数 | 412 |
| 角色（标准 01 §2） | **科学定义**（一致）；§5b 噪声分类学带大量一手文献量级对照（SWarp/SExtractor/SEP/photutils 逐条 file:line）属"研究与佐证"混装但**必要**（作为方差项溯源锚）→ 可接受，但按 §2 应拆 `docs/research/` |
| 主题 | 空背景逐像素方差与 ivar 的估计器（8×8 patch + MAD→σ + 稳健非负平面场）、噪声完整分类学与可辨识性边界、加权方差面（含源项）、自校准判据与凸包非负、不变量与退化态、dtype 成对不变量 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §2.2（创新点二）、§4.2（Phase1 节点流程）`；L5 ID 块再写 `上游: SCI-SCOPE-001` |
| 下游 | 75 命中（root 3 / docs 23 / 实验 20 / 其他 29）。root 3 份：**`ASTROCS_DESIGN.md`（§2.2 L146 指定「正本为 `docs/science/NOISE_MODEL.md` §5b」）**、`CMakeLists.txt`、`README.md`（L56）→ 是 science 中上位指名"正本"的两份之一 |
| 是否该主题正本 | **是**（§5b 噪声分类学、§5c 加权方差面被上位指名）；**§9a「SNR」一条却把 SNR 正本让给 CONTROL_WEIGHT_SNR**（L325「本合同不产出 SNR 图…源通量 SNR 必须另行定义（逐源 `σ_F`，见 CONTROL_WEIGHT_SNR.md §1）」）→ 分工清楚、无争 |
| 开头元信息块 | **有**：L5 `> ID: SCI-NOISE-001  范围: SCI-NOISE-001..015  状态: FROZEN（冻结定义）  上游: SCI-SCOPE-001  下游 ALG: ALG-NOISE-001..  模块: snr_estimator (NoiseWeightModelV1)`。→ 标准 01 §4 违（ID + 状态编号）；「模块: snr_estimator」**与 §13 实现路径不一致**（§13 写 `lib/algorithms/noise_snr/cpp/src/noise_model.cpp`，无 `snr_estimator` 模块） |
| 日期/编号/sha/历史叙事 | 日期 0；sha 2 行（L375/L378 命中的是 DOI 数字串，误报）；任务/编号 10 行：L86/L100 的 `SCI-B D2 订正`、L108 `reports/PROJECT-GOVERNANCE-01/research/MASK-001_…`、L368 `DISP-NOISE-010`、L52 `SAT-001`、L366 `V12-N-03`、L324 `EXP-205`、L274/L286 `MASK-002`、L361「新增/映射见 `docs/TRACEABILITY.csv`」；历史叙事 5 行：**L100「⚠ 单位（**SCI-B D2 订正**）」、L324「（**SCI-B 定案**，防双计）」、L366「（V12-N-03，）」、L343「E12 实测**漏项时判 +49.9%（假红）**」、L308 实测缺陷形态** → 其中 **L366 与 L343 的「，）」是截断残句**（`(V12-N-03，）`、`（正例 + 两条负例；）`）**→ 文本完整性缺陷**；「订正/定案」属历史叙事 |
| 悬空引用 | 扫描 25 条 / 报缺 9：① **L108「来源 `reports/PROJECT-GOVERNANCE-01/research/MASK-001_掩膜语义重新推导.md` §3.3/§3.4/§5.3」——该路径不存在**。`git ls-files reports` → **仅 3 个文件**（`reports/v19r2/file_audit_inventory.csv`、`reports/v19r2/source_manifest.csv`、`reports/v19r2/quality/shipping_units.csv`），**`reports/PROJECT-GOVERNANCE-01/` 整个不存在**（`git ls-files \| grep -c "reports/PROJECT-GOVERNANCE-01"` → 0）。同名文件的实际位置是 `artifacts/evidence/governance-01/research/MASK-001_掩膜语义重新推导.md`（`git ls-files \| grep -i MASK-001` → 该行），**且该文件第 648 行也自我引用了同一个不存在的路径**（`git grep -n "MASK-001_掩膜语义重新推导" -- '*.md'` → 2 命中：NOISE_MODEL L108 与该文件 L648）→ **证据锚的目录迁移未被贯穿，科学文档里 MASK-002 的全部实测数字（偏差 1.11%、RMSE、54 帧/策略对照表）都挂在这个断锚上**。② L368「`NOISE_ESTIMATION.md:143`」「`eng/packaging/config/defaults.json` 的 `source_ref` 三段登记」：前者 basename（实际 `docs/algorithms/NOISE_ESTIMATION.md`），后者需核（`grep -n source_ref eng/packaging/config/defaults.json`）。③ L397 `PSFSignalEstimator.h`（外部 PCL，已标 404/未核验）。④ L402 `photutils/utils/errors.py:91-92`（外部，带版本 tag，可核验形式）。⑤ L290/L301 只给 `noise_model.cpp:行号`（无路径）。⑥ L52 注释里 `isrTask.py L431-442`。⑦ L171/L173 **自证未核验**：「量化项的具体形式…**尚未取得原文**（EMVA 1288 正文需注册、Janesick 2007 付费墙）⇒ **核验前的断言面 = 空**」——这与上位 §2.2 L147「进入生产数值路径的项以已建立一手文献锚为前提」一致（**未取原文即不作断言**），属**合规的诚实边界**，非缺陷。<br/>**判定：1 条真悬空（`reports/…`）+ 4 条 basename 锚 + 2 条已如实声明的外部未核验** |
| 与上位文档冲突 | **三处**：① **§5b 噪声表未按上位要求写 e⁻² 标度**（见 §1.1 重复项 1 的命令与结论）。② **§5a L99-101 与 §5a L100 注记同文档内自相**：L99 天空预算推导写「SE(σ̂)/σ ≈ **1.44/√N_sky**」，L100-101 又写「⚠ 单位（SCI-B D2 订正）：1.44/√N 是**相对**标准误…**不是 dex**；若要以 dex 表述同一预算，须除以 ln10：SE_dex ≈ 1.44/ln10/√N ≈ 0.625/√N」→ 第一处未标单位，读者按 dex 用即错 2.3026 倍；§9a L324 第三处再写一遍。**判定：同一常数三处、第一处缺单位标注且未回改**（CONTROL_WEIGHT_SNR §8a 第 6 条是第四处）。③ **§4 L52 与 §8 L302 的饱和域口径不完全一致**：§4 说「`0`/负/非有限 = 未提供电平（unset），不等于"无饱和"」并强制写降级声明；§8 L302 行「全帧 NaN/饱和」说「**电平未提供（unset）时本行的饱和支不可达**」→ 两处一致，**无冲突**（正面）。④ **§9a L323 有残句「（实现 `snr_phot_cal_quality`；代码已按此实现，本行仅补文档口径，）」** → 逗号悬空 + "代码已按此实现"属**以文档追认代码**的时态倒置（标准 01 §1 层序：代码不得成为文档口径的来源）。 |
| 重复或重叠对象 | ① 与 `ASTROCS_DESIGN.md` §2.2 L132-147：**逐字重复 13 行**（§1.1 已给三段实证）。② 与 `docs/science/SCIENCE_SCOPE.md` §系统/随机误差 L79-87：两个方差面 + `var_ADU=max(signal,0)/gain+(read_noise_e/gain)²` 公式**两份**（本份 §5 L80 逐字同式），且 SCIENCE_SCOPE 那份额外给了「`noise_model.cpp:618` 的 `source==0` 基线」这一本份 §5 L81 也有的锚 → **下位之间复述 + 行号锚两处不同（618 与 §5 的 `source==0` 未标行号）**。③ 与 `docs/science/CONTROL_WEIGHT_SNR.md` §2b：`sigma_slow²/sigma_w²` 两份（本份 §5c L208 `sigma_w² = sigma_bg² + S_src/g`，CW §2b L93 `sigma_w² = sigma_slow² + S_src/g`）**符号名不同（`sigma_bg` vs `sigma_slow`）而所指同一面** → 命名不统一缺陷。④ MAD 常数 `1.482602218505602`：本份 §9 L312、§5a L62、PHOTOMETRY §14 第 2 条 L296、CONTROL_WEIGHT_SNR、STAR_DETECTION、REJECTION §5c 六处出现，其中 **PHOTOMETRY 写 `0.6744897501960817`、本份写倒数形式** → 同一常数两种写法（本份 §9 已声明"11 位简写只能出现在≈语境"）。⑤ 与 `docs/algorithms/NOISE_ESTIMATION.md`（算法层正本）：估计器与平面拟合两份，本份给判据、算法页给实现行号 → 合法分层，但 L368 的三段登记链（`noise_model.cpp:703-704` → `NOISE_ESTIMATION.md:143` → `defaults.json`）把**行号写进科学文档**（标准 01 §4）。 |
| 处置建议 | **保留（正本）+ 四项硬修**：① 修 `reports/PROJECT-GOVERNANCE-01/…` → `artifacts/evidence/governance-01/research/…`；② 按上位 §2.2 要求给 §5b 表补 **e⁻² 标度列**或改写上位要求；③ `1.44/√N` 首处补"无量纲"、`sigma_bg/sigma_slow` 统一符号；④ 修两处残句（L323/L343/L366 的「，）」「；）」）与 L5 元信息块的"模块: snr_estimator"过期名 |

### 1.16 `docs/science/UNCERTAINTY_AND_COVARIANCE.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/UNCERTAINTY_AND_COVARIANCE.md` |
| 标题 | Uncertainty & Covariance |
| 行数 | 163 |
| 角色（标准 01 §2） | **科学定义**（一致）。**但 §UPM control estimator 方差（L34-55）与 §Phase2 马赛克合成方差（L57-77）与 §Phase3 重采样方差传播（L79-125）三节是"跨三个阶段的公式总汇"**，与 `PHASE2_UPM.md`、`INTEGRATION.md`、`DRIZZLE.md`、`PHASE3_HIPS_TO_FITS.md` 各自的主题公式重叠 → 主题横切 |
| 主题 | 产品 variance/ivar 的权威传播口径：逐像素方差、Drizzle 后相邻像素相关（不落盘）、`control_variance` 两级权重、马赛克合成方差、Phase3 `C_out = R C_in Rᵀ`、哨兵值跨阶段对照 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §5.3（SNR 重建与逆方差叠加）、§3.1（数据对象）`。**缺 §6.x（Phase3 方差传播的上位）**——§Phase3 一节的内容上位是 §6.2/§6.4，未列入 |
| 下游 | 43 命中（root 0 / docs 20 / 实验 5 / 其他 18）。docs 侧 20 份含 12 份 `docs/plugins/algorithms_phase{1,2,3}/*.md`（02_cosmetic、03_star_detection、06_photometry、08_drizzle、09_coverage、11_upm、15_resample 等）与 `docs/science/{DRIZZLE,INTEGRATION,NOISE_MODEL,PHASE3_HIPS_TO_FITS,PHOTOMETRY,SCIENCE_SCOPE}.md` → **被引用面最广的"横向口径页"之一** |
| 是否该主题正本 | **是**（协方差与不确定度总口径）；**`DOCUMENT_INDEX.yaml` 也给它 `duty: "不确定度与协方差传播的唯一科学口径"`**（L1362 区段）→ 唯一口径由索引登记、文档自身未自称 |
| 开头元信息块 | 无 ID 块（同 SCIENCE_SCOPE）→ **体例与同目录 12 份不齐** |
| 日期/编号/sha/历史叙事 | 日期 0、sha 0、sha256 0；任务/门编号 8 行（L57 `ALG-UPM-CONTROL-IVAR-001`、L77 `DATA-P2-VAR-001`、L79 `DATA-P3-UNC-001`、L88 `ALG-P3-003 G4`、L100 `ALG-P3-001 §3`、L124 `SCI-P3 §9a-10` 等，**全是正式合同/算法 ID，非流水号，合规**）；历史叙事 0。**判定：本份是 science 目录写法最干净的一份（date/taskid/sha/history 全零）** |
| 悬空引用 | 扫描 11 条 / 报缺 3：① L148「`PHASE2_SAMPLER.md` 的 MC 对非高斯族给出判红结果」——只给 basename，实际 `docs/algorithms/PHASE2_SAMPLER.md` **实存**；② L107 `p3_rsmp_propagation.cpp:101,108`——只给 basename，实际 `lib/algorithms/resample/p3_rsmp_propagation.cpp`（`git ls-files \| grep p3_rsmp_propagation` → 该行）→ **无路径锚**；③ L13 `snr_estimator.h`——实际 `lib/algorithms/noise_snr/cpp/include/snr_estimator.h`（实存）→ 无路径锚；④ L36「（SCI-UPM §5/§6）」、L124「SCI-P3 §9a-10 / §7」——行号/条款式引用需人工核对：`PHASE2_UPM.md` **无 §6 谈权重**（§6 是假设）→ 该两处指向的 `SCI-UPM §5/§6` 中 §6 无对应内容（**节号弱错**）；`PHASE3_HIPS_TO_FITS.md` 有 §9a-10 与 §7 → **实存**。**判定：0 条文件级真悬空；3 条无路径源码锚；1 条节号弱错（SCI-UPM §6）** |
| 与上位冲突 | ① **L59「马赛克加权积分（SCI-INT §5，**w_i=逐样本 ivar**…无 fallback）」与 `INTEGRATION.md` §5 实际定义不一致**：`INTEGRATION.md` §5 的聚合式是 `signal = Σ w_i·x_i / Σ w_i`，`weights[i]` 定义为「**数值权重**（可空 = **等权 1.0**）」、§9 明写「**本层不编码 ivar 语义**，数值权重由调用方先经 `p2_validate_candidate_weights` 校验」（L110/L34），即 **SCI-INT §5 根本不规定 `w_i = ivar`**。→ **本份把"ivar"安在 SCI-INT §5 名下，而该节明确说自己不编码权重语义** = 引用了不会承载该口径的正本（与 G3 组同源）。② **L46 引「`docs/science/SCIENCE_SCOPE.md` §假设的 γ ≈ 1 判据」并把它作为 `control_variance` 成立前提** → 与 1.10 冲突①连成一链：**γ 判据实测在 M42 上为 1.983（前提不成立）→ 本份的 `control_variance` 适用域在该真实域亦不成立**，但本份未写降级后果（只在 SCIENCE_SCOPE 写了）→ **跨文档一致性缺环**。③ L99「ρ=0.19 ⇒ 方差低估 36.3%、σ 低估 20.2%」与 L26「mean|ρ|≈0.19」同数量，无冲突。④ 与最高设计 §5.3 L400「拟合权重与堆叠权重是两个量」——本份未区分拟合/堆叠，只在 L66 给「一般式（权重非纯逆方差时适用）」→ **上位点名要区分的两量在下位只以括注形式出现，缺独立定义**（正本应在此） |
| 重复或重叠对象 | ① §逐像素方差 + §UPM control estimator 方差 ↔ `NOISE_MODEL.md` §5/§5c、`PHASE2_UPM.md` §5：`control_variance = k_corr·(π/2)·σ_bg²/N_retained` **三份同式**（本份 L42、PHASE2_UPM L77、CONTROL_WEIGHT_SNR 未写）。差异：本份 L54-55 写「**k_corr 定义域 1 ≤ k_corr**」，而 `PHASE2_UPM.md` §4 L43-47 写「**定义域 `1 < k_corr`**；`k_corr=1` 与 `k_corr<1` 一律显式拒」→ **同一常数的定义域两份文档写成 `≥1` 与 `>1`，直接互斥**（本份把 `k_corr=1` 当合法，上位正本判其显式拒）。这是本批第二条硬取值冲突。② §Phase3 重采样方差传播 ↔ `docs/science/PHASE3_HIPS_TO_FITS.md` §9a-10 ↔ `docs/algorithms/PHASE3_RESAMPLE.md`：`C_out=R C_in Rᵀ` 三份。③ §协方差（L17-26）与 §对使用的约束（L28-32）——**同一条 MC 数字 `mean|ρ|≈0.19、max|ρ|≈0.57`（SNR-012）在同一文档内重复两次**，第二次还带"aperture variance 未建模"的追加说明 → 冗余复述。④ 与 `docs/standards/NUMERIC_STANDARD.md`：缩放律再次出现（第五处）。 |
| 处置建议 | **保留（主题正本）+ 四项**：① 修 `k_corr` 定义域为 `1 < k_corr`（与 PHASE2_UPM 正本一致）；② 把"马赛克权重 = 逐样本 ivar"的引用从 `SCI-INT §5` 改到实际承载它的节（随 G3 裁决）；③ 补齐拟合权重/堆叠权重两量的独立定义（上位 §5.3 点名）；④ 三处源码锚补全路径、`SCI-UPM §6` 改 §5；⑤ 合并 §协方差 与 §对使用的约束 的重复 MC 数字 |

### 1.17 `docs/science/PHASE2_UPM.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/PHASE2_UPM.md` |
| 标题 | Phase2 UPM (Control Photometry) Science (SCI-UPM) |
| 行数 | 505 |
| 角色（标准 01 §2） | **科学定义**（目录一致）。**但 §7a/§16.3/§17 三节（L168-231、L374-385、L387-505，共 190 行 = 全份 38%）实为"表示能力判据 + 实现现状对照 + 门槛的统计推导与实测标定"**：§17 的推导属**算法推导**角色（目标目录 `docs/algorithms/`）、§16.3 的"实现现状/待统一/待裁决"表属**模块工作细节 + 过程台账**角色 → **一份跨四角色** |
| 主题 | 统一相对模型 UPM 的观测方程（纯加性）、两级权重（`w_UPM` 绝对式 / `w_cell` 份额式）、`k_corr` 定义域与适用域、收敛判据尺度归一、可辨识性判决（列均衡未正则信息矩阵）、节点间距自适应、接缝判据与门槛推导、SCI-C 实验结论 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §5.4（天光平面与统一相对模型）`。**只标一条**，但本份实际承载上位 §2.3（创新点三全部口径）与 §5.2（流程 `upm`/`upm apply` 两节点）→ 上游标注不完整 |
| 下游 | 82 命中（root 4 / docs 34 / 实验 8 / 其他 36）。**root 4 份：`ACCEPTANCE_SPEC.md`（§6.2 把接缝门限 1e-2 的推导正本指给本份 §17）、`ASTROCS_DESIGN.md`（§5.4 L412 同指 §17）、`CMakeLists.txt`、`README.md`（L59）**。docs 侧 34 份含 `docs/algorithms/{UPM_SOLVER,PHASE2_UPM_IMPL,PHASE2_SAMPLER,GATES_AND_TOLERANCES,…}.md`、`docs/plugins/algorithms_phase2/{10_sampling,11_upm}.md`、`docs/design/PHASE2_DETAILED_DESIGN.md`、`docs/contracts/{SCHEDULER_CONTRACT,TEST_MATRIX,…}.md`、`docs/ci/01_CHECKS.md` |
| 是否该主题正本 | **是**（UPM 科学定义 + 接缝门槛推导两份正本：§5 权重、§7a 可辨识性、§17 门槛推导均被上位与验收规范指名"唯一正本/正本 = 本文件 §17"）。**但 §9a L296 自称"该门槛的推导与实测标定正本 = 本文件 §17（本节不再指向别处索取依据）"，而 §17 L390-391 又把复现脚本指到 `run/SEAM-DERIV-01/tools/*.py`（不入库）→ 正本自称唯一、证据却在临时区** |
| 开头元信息块 | **有**：L5 `> ID: SCI-UPM-001  范围: SCI-UPM-001..010 + SCI-UPM-WEIGHT-001 + SCI-UPM-PERSIST-001  状态: FROZEN (**T106 冻结, 2026-08-23**)  上游: SCI-SCOPE-001  下游 ALG: ALG-UPM-001..  模块: phase2 (upm/sampler)`。**违标准 01 §4 四项**：ID、状态编号、**日期 `2026-08-23`**、任务号 `T106`；"模块: phase2"是**旧模块名**（现树为 `lib/algorithms/coverage` + `lib/algorithms/upm`，见本份 §13 L278 自己写的路径） |
| 日期/编号/sha/历史叙事 | 日期 **2 处**（L5 `2026-08-23`；L314「标题经 aspbooks.org 逐字核验 **2026-09-17**」）；任务/门编号 **12 行**（L16/L41 `DATA-FRAME-ID-001`、L118/L129 `ALG-UPM-CONTROL-IVAR-001`、L237/L296/L308 `CHK-L4-SEAM-FOOTPRINT`、L310 `SCI-001-S2 补齐`、L344 节题「**RELEASE-04 / SCI-403**，仓内实测」、L357「与 **SCI-402** 的独立结论一致」、L395 `ALG-UPM-CONTROL-IVAR-001`）；历史叙事 **4 处**：L263「在生产 `w_UPM` 中乘 `star SNR / support^p / obs.ivar`(**已弃用**)」、**L296「本节不再指向别处索取依据」**、L320「`÷g²` 保持**恒等式**（…`g≡1` 时退化等价，公式保留）」（保留某公式的叙述）、**L344 节题整节是"SCI-C 实验结论（RELEASE-04 / SCI-403）"**= 以任务号命名的结果登记节 |
| 悬空引用 | 扫描 41 条 / 报缺 25，逐条判定：<br/>**真悬空（文件级，7 条，全部指向 gitignore 的 `run/`）**：L391 `run/SEAM-DERIV-01/`（含报告 `REPORT.md`）、L494 `run/SEAM-DERIV-01/evidence/critical_injection.json`、L499 `…/evidence/selftest.json`、L500-504 `run/SEAM-DERIV-01/tools/{null_distribution,null_tail,critical_injection,detection_floor,synthetic_null_detect}.py`。命令：`git ls-files docs/science \| wc -l` 与 `git ls-files "run/**" \| wc -l` → **0**（`run/` 不入库）。**→ §17 是"接缝门槛唯一正本"，其五条复现脚本与两份证据全部不可从仓库取得**。<br/>**行号锚（非悬空但脆弱）**：L32 与 §5 引 `lib/infrastructure/aio/src/hiss_writer.cpp:335-365`——**该文件实存**（`git ls-files lib/infrastructure/aio/src/hiss_writer.cpp` → 1 命中，L335-340 内容正是 BUNIT 冻结集 `{ADU/sr, ADU^2/sr^2, sr^2/ADU^2}` 判定），同一实现另有一个 `lib/infrastructure/aio/src/hips/aio_hips_writer.cpp`；**两个写入器并存、三份 science（本份 §3、`INTEGRATION.md` L31、`REJECTION.md` L32）都指前者、上位 §11 L207 指后者** → 判定：**不是悬空，是"同一 BUNIT 判红规则有两个同名不同路径的写入器"，锚点选择未在上位层统一**。其余 `upm.cpp:289-290/:2766-2790/:1021-1040/:1577-1589/:1665-1670/:2265-2276`、`sampler.cpp:83/:87-104/:560-583/:874` 全为 basename 无目录（`lib/algorithms/coverage/src/` 下实存，见 §13）。<br/>**无路径锚**：L320 `10_sampling.md`/`11_upm.md`/`UNIFIED_MODEL.md`（只写文件名不写目录）；L121/L240/L280/L281/L347/L369/L378-382 的 `module_adapters.cpp:7808-7813` 等 8 处行号；L391 `REPORT.md`。<br/>**实存且正确**：`实验/additive-sky-seamless/{README.md,code/c2_multiplicative.py,code/c3_public_plane.py,code/c7_realdata.py,results/*.json}` 逐个 `[ -f ]` 通过；`docs/science/PHASE2_UPM.md §7a` 被 CONTROL_WEIGHT_SNR 引用可达 |
| 与上位冲突 | **四处**：① **§7(b) 并行确定性容差与上位口径互斥（本批第二条硬取值冲突）**：本份 L152-158 写「同配置重复=位精确 + `model_hash` 逐字；**跨 worker 数 = 1e-12 绝对容差**（判据 `|ΔC| ≤ 1e-12·max(|C|, C_scale)`）；跨后端不允许」，而 `ASTROCS_DESIGN.md` §9 L677 写「模块科学页已冻结三档的按其三档（`docs/science/PHASE2_UPM.md` §7：…）」——上位把正本让给本份，**但本份自己 §7(b) 末段 L160-166 又说「绝对容差只在观测尺度 ≈1 时与相对判据等价…生产 C 场为面亮度 ADU·sr⁻¹…量级 5.3e13 ⇒ 此时 ulp 量级为 1e-2，绝对 1e-12 比 ulp 严 10 个数量级、不可达」** → **同一条 1e-12 在同一节里被定义又被判为不可达**，且 §9 L250 再复述一遍"跨 worker 数 1e-12"。上位 §9 L677 补了"绝对容差必须同时声明适用量级域"，但**没有说该 1e-12 应改为相对形式** → 生产实际用哪一个，三份文档给不出唯一答案。② **§9a 接缝判据与 `ACCEPTANCE_SPEC.md` §6.2 的"默认参数唯一来源"分裂**（详见 §2 主题组 G4）。③ **§5 L77-79 `Var(median)` 成立条件 `N ≥ 65`** 与 §5 L94「legacy snr²/(1+snr²)/unc² 仅 `use_ivar_weight=0` ablation/诊断 (**SNR-015**)」：`SNR-015` 属 NOISE 侧编号体系（NOISE_MODEL 的编号是 `SCI-NOISE-001..015`），**编号域混用**。④ **§16.3 表 L378「两入口口径不一致（待统一）」自证规范与实现互斥**（`module_adapters.cpp:7808-7813` 取相对判据、`p2_session.cpp:204` 取绝对判据）——**这是"下位文档记录上位口径在生产两路不一致"**，属上位须裁决项，L382 又自评「同一函数内两段注释对同一变更的授权状态表述互斥 → **待裁决**（不掩盖）」 |
| 重复或重叠对象 | ① 与 `docs/algorithms/UPM_SOLVER.md`、`docs/algorithms/PHASE2_UPM_IMPL.md`（都被本份 L94「求解 (UPM_SOLVER.md):」与 §8 表引用）：**Huber IRLS + 弱零锚 + gauge 的公式两份**（本份给科学定义、算法页给数值方法）→ 合法分层，但本份 §5 L94-96 已把求解式写全。② 与 `docs/plugins/algorithms_phase2/11_upm.md`（最高设计 §5.4 L411-413 三次指名它）：上位说 formulation 四要素与收敛判据、实现与默认参数在那份，本份 §5/§7/§9 也写了一份 → **同一 formulation 在 science 与 plugins 两层各写全文**（差异：plugins 给默认参数与端口，science 给判据与适用域）。③ 与 `docs/science/CONTROL_WEIGHT_SNR.md` §2b L104：「节点间距按 `docs/science/PHASE2_UPM.md` §7a 由输入几何导出」——CW 引本份 §7a 为正本，本份 §7a 规则 2 与上位 §5.4 L413 三处同写"节点间距由输入几何导出（上界 = 重叠带宽度与指向间距的一半取小，下界 = 分辨率极限）" → **同一规则三份、措辞各异**（上位 §5.4 L413「上界 = 重叠带宽度与指向间距的一半取小」；本份 L186-188「节点间距 ≤ (该产品实际约束帧间改正量的最小尺度)/2 —— 该尺度是重叠带宽度与指向间距两者中的较小者」；CONTROL_WEIGHT_SNR L104 只给指针）。差异点：上位把"一半"作用在"取小之后"，本份把 "/2" 作用在"最小尺度"上——**同义，但括号位置不同易误读**；另上位未写实验单元指针的 §7a 编号。④ 与 `docs/science/UNCERTAINTY_AND_COVARIANCE.md` L42-45 的 `control_variance` 公式：**两份**（差异：本份 L77 `= k_corr × (π/2) × σ_bg²/N_retained`，UNCERTAINTY L42 同式；但 UNCERTAINTY L54 写定义域 `1 ≤ k_corr`、本份 L43-47 写 `1 < k_corr` 并逐项给 rc → **取值域直接冲突**，见 1.16）。⑤ 与 `实验/additive-sky-seamless/`：§16 整节是实验报告摘要 → **标准 01 §2 要求实验单元自包含、正本以指针服务**，此处把结论复制进正式文档 |
| 处置建议 | **拆分保留**：① §17（门槛推导，119 行）整节迁 `docs/algorithms/SEAM_GATE_DERIVATION.md`，本份与 `ACCEPTANCE_SPEC.md` §6.2、上位 §5.4 全改为指针；② §16（SCI-C 实验结论）压缩为三行指针 + `实验/additive-sky-seamless/README.md`；③ §16.3 的实现现状表迁 `docs/plugins/algorithms_phase2/11_upm.md`；④ 7 条 `run/SEAM-DERIV-01/*` 证据迁 `artifacts/evidence/seam-deriv-01/`；⑤ 修 `hiss_writer.cpp` → `aio_hips_writer.cpp` 全路径（并同步 INTEGRATION/REJECTION 两份同错）；⑥ 统一 `k_corr` 定义域（与 UNCERTAINTY 二选一）；⑦ 裁决 §7(b) 的 1e-12 绝对/相对形式并在上位同步；⑧ 删元信息块的日期/T106/旧模块名 |

### 1.18 `docs/science/INTEGRATION.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/INTEGRATION.md` |
| 标题 | Integration / Coaddition Science (SCI-INT) |
| 行数 | 184 |
| 角色（标准 01 §2） | **科学定义**（一致，全份是逐像素加权归约的定义/不变量/Oracle，无越层） |
| 主题 | Phase2 逐像素候选栈的加权积分（`signal = Σ wᵢxᵢ/Σwᵢ`）、`support` 的 canonical `max` 归约、五态状态机、资格判定与确定性求和 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §5.3（SNR 重建与逆方差叠加）`。**未列 §5.2（固定科学流程含 `integration` 节点）与 §5.6（硬约束）** |
| 下游 | 43 命中（root 0 / docs 20 / 实验 0 / 其他 23）。**根级九份文档 0 引用**（`grep -l "INTEGRATION.md" *.md` → 无命中）→ 只被 `docs/` 内部引用：`docs/DOCUMENT_INDEX.yaml`、`docs/GLOSSARY.md`、`docs/TRACEABILITY.csv`、`docs/algorithms/{PHASE2_COVERAGE,PHASE2_INTEGRATION,PHASE2_MOSAIC_WRITE}.md`、`docs/contracts/{DATA_SEMANTICS,INDEX,PUBLIC_API}.md`、`docs/modules/{phase2_int,common}.md`、`docs/modules/registry/*`、`docs/owner/SCIENCE_OVERVIEW.md`、`docs/plugins/algorithms_phase2/13_integration.md`、`docs/science/PSF_SIGNAL_WEIGHT.md`、`docs/traceability/*` |
| 是否该主题正本 | **是**（Phase2 积分归约的科学正本） |
| 开头元信息块 | **有**：L5 `> ID: SCI-INT-001  集合: SCI-INT-001,002,004,008  状态: FROZEN  上游: SCI-SCOPE-001  下游 ALG: ALG-INT-001..  模块: phase2 (integrate)`（ID/状态违规；"模块: phase2"旧名，§13 实为 `lib/algorithms/coverage/src/integrate.cpp`） |
| 日期/编号/sha/历史叙事 | 日期 0、sha 0、sha256 0；任务编号 1 处（L144 节题「9a 专属问题回答（**SCI-006** 指定问题逐项）」）；历史叙事 0。**本份是 science 目录里除 SCIENCE_SCOPE/UNCERTAINTY 外最干净的一份** |
| 悬空引用 | 扫描 12 条 / 报缺 2：① L21/L101/L102 引 `integrate.cpp`（无路径，实际 `lib/algorithms/coverage/src/integrate.cpp`，§13 已给全路径）→ **同一文档两种锚法**；② L25/L136 引 `integrate.h`——§13 给的路径是 `lib/algorithms/coverage/include/astro/phase2/integrate.h`，`git ls-files` 复核实存；③ L31-32 引 `lib/infrastructure/aio/src/hiss_writer.cpp:335-365`（**实存**，同目录另有 `src/hips/aio_hips_writer.cpp`，两个写入器并存、上位 §11 指后者）→ 锚点正确但**同名职责两份写入器未在上位消歧**；④ L77 引 `SNR-010`（属 NOISE 编号域，本份无该编号定义）→ **外域编号引用无桥**。**判定：0 条文件级真悬空；2 条 basename 锚 + 1 条外域编号引用** |
| 与上位冲突 | **两处**：① **§5/§9a 的"权重"与上位 §3.1 L225「全程只有 SNR，没有'权重模式'这个概念」不矛盾**（本份 §9 明写「不编码 ivar 语义，数值权重由调用方先经 `p2_validate_candidate_weights` 校验」，§10 把「在本层引入 ivar/SNR 策略（policy 在调用方，reducer 无知）」列为不可接受变化）——**但 `UNCERTAINTY_AND_COVARIANCE.md` L59/L64 却把"SCI-INT §5"当作「`w_i = 逐样本 ivar`、`ivar_mosaic = W = Σ ivar_i` = wsum（SCI-INT §5 冻结量）」的出处** → 本份 §5 **从未**规定 `w_i = ivar`（只规定 `signal = vs/wsum`）。**判定：横向引用把口径安到了不含该口径的正本上**（G3 组的关键一环）。② **§3 单位与上位 §5.6 L430「Phase2 信号为面亮度量纲」一致**，但本份 L29-32 把量纲依据挂在写盘 BUNIT 上并引**不存在的 `hiss_writer.cpp`** → 依据链断（结论对、锚错） |
| 重复或重叠对象 | ① 与 `docs/plugins/algorithms_phase2/13_integration.md`（模块工作细节层）：状态码表与求和顺序两份；**分工成立**（plugins 给端口/生命周期，本份给判据与不变量），差异：本份 L73 的"与实现一致"句复述了 `integrate.cpp:10-79` 行号范围（属 plugins 层内容）。② 与 `docs/algorithms/PHASE2_INTEGRATION.md`（算法层）：同一 reducer 的数值方法两份，合法分层。③ 与 `docs/science/UNIFIED_SCIENCE_MODEL.md` §5（扩展源最优统计 `(AᵀC⁻¹A)⁻¹AᵀC⁻¹d`）：本份 §5 的逐像素加权均值是该式的**退化特例**，两份并存且本份 L71 处已声明「该简化不是点源 PSF-aware 最优的替代」→ 不重复。④ 与 `docs/science/UNCERTAINTY_AND_COVARIANCE.md` L64 `ivar_mosaic = Σ ivar_i`：**同一量的两份定义**（一处标"SCI-INT §5 冻结量"、一处本份未定义）→ 需在本份 §5 补 `wsum` 与 `ivar_mosaic` 的关系，或由 UNCERTAINTY 改引自己 |
| 处置建议 | **保留（正本）+ 三项**：① 在本份 §5 明确 `wsum` 是否等于马赛克 `ivar`（承接 `UNCERTAINTY_AND_COVARIANCE.md` L64 的引用），或让 UNCERTAINTY 改指自己；② 删元信息块 ID/状态/旧模块名；③ 统一 basename 锚为全路径；④ 与上位 §11 消歧 `hiss_writer.cpp` 与 `hips/aio_hips_writer.cpp` 两个写入器的职责（BUNIT 判红规则只归一处） |

### 1.19 `docs/science/DRIZZLE.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/DRIZZLE.md` |
| 标题 | Drizzle / Spherical Resampling Science (SCI-DRIZZLE) |
| 行数 | 250 |
| 角色（标准 01 §2） | **科学定义**。**但 §5 的"一手文献逐字引文 + 参考实现 do_kernel_square 对照"（L52-61）与 §14a 的 SWarp/drizzlepac 对照属"研究与佐证"角色；§7 通量守恒/常数场面亮度判据属科学定义 → 一份跨两角色** |
| 主题 | 球面 Drizzle 的面亮度归一重建（核按 drop 面积归一）、通量严格守恒、方差传播与缩放律、Sutherland–Hodgman + Van Oosterom–Strackee 球面几何、极区 prune、零漏选不变量 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §5.2（固定科学流程）、§6.3（投影算法）`。**§5.2 与 §6.3 都不含 drizzle**：命令 `grep -n "^### 5.2\|^### 6.3" ASTROCS_DESIGN.md` → L371「### 5.2 固定科学流程」（Phase2 管线 = admit/coverage/sampling/upm/rejection/integration，**无 drizzle**）、L451「### 6.3 投影算法」（Phase3 投影，非 drizzle）；drizzle 节点在 **§4.2**（L266 `R["drizzle 球面重采样"]`）与 **§4.4/§4.6**。**判定：上游标注错到别的阶段**（标准 01 §3「标明其上游最高设计条款」实质失效） |
| 下游 | 61 命中（root 0 / docs 24 / 实验 0 / 其他 37）。根级九份 0 引用。docs 侧 24 份含 `docs/algorithms/{DRIZZLE_GEOMETRY,HIPS_WRITER}.md`、`docs/plugins/algorithms_phase1/08_drizzle.md`、`docs/contracts/{CONFIG_CONTRACT,DATA_SEMANTICS,PUBLIC_API,INDEX}.md`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`、`docs/modules/{common,healpix_drizzle,registry/astrocs.phase1.drizzle}.md`、`docs/owner/SCIENCE_OVERVIEW.md`、`docs/science/UNIFIED_SCIENCE_MODEL.md`、`docs/standards/{NUMERIC_STANDARD,STANDARDS_REGISTRY}.md`、`docs/references/SCIENTIFIC_REFERENCES.md` |
| 是否该主题正本 | **是**（Drizzle 科学正本）。**但与 `docs/algorithms/DRIZZLE_GEOMETRY.md` 的分工需核**：后者被最高设计 §2.4 L213 指为「FP64 通量闭合 <1e-6；FP32/FP64 逐 leaf <1e-5」的 §9 冻结值正本，本份则给重建式与守恒律 → **合法分层**；本份未复写冻结门数值（合规） |
| 开头元信息块 | **有**：L5 `> ID: SCI-DRZ-001  集合: SCI-DRZ-001,014,015,016  状态: FROZEN（冻结定义）  上游: SCI-SCOPE-001  下游 ALG: ALG-DRZ-001..  模块: healpix_drizzle`（"模块: healpix_drizzle"旧名，§13 实为 `lib/algorithms/drizzle/healpix_drizzle/`；`集合:` 只列 4 个 ID 却写 `014,015,016` 跳号无说明） |
| 日期/编号/sha/历史叙事 | 日期 0、sha 0、sha256 0；任务编号 3 行（L72 `SCI-DRZ-001`、L144 `rule_id = NAN-SAMPLE-MASK-COVERAGE-NAN`、L159 `DISP-DRZ-009`、L196 `SCI-004 指定问题`）；历史叙事 0。**判定：本份"日期/sha/历史叙事"三项全零**，仅带正式 ID |
| 悬空引用 | 扫描 12 条 / 报缺 4：`drizzle_engine.cpp`、`spherical_overlap.cpp`、`candidate_oracle_test.cpp`、`variance_propagation_test.cpp` 均为 basename（实际全路径在 §13 L187 给出，且 §13 的 `lib/infrastructure/aio/src/hips/aio_hips_writer.cpp` **实存**）→ **同一文档两种锚法混用**；L202 引 `UNCERTAINTY_AND_COVARIANCE.md` **无 §号**（该文件无编号节，只能靠节名）；L144 引 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md §2a` 与 L207/L209 引 `GLOSSARY`/`DATA_SEMANTICS §2/§4` → 逐条 `[ -f ]` 实存；§2a 节号需人工核（`grep -n "^## 2a" docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`）。**判定：0 条文件级真悬空；4 条 basename 锚 + 1 条无 §号引用** |
| 与上位冲突 | **一处实质冲突（同一公式两份、取值差 `pixfrac⁴`）**：本份 §5 L82-84 写 `sumVarNum += v_j·w_jp²`、**`variance_p = sumVarNum / N_p²`**（分母 = 面亮度归一分母）；`docs/science/UNCERTAINTY_AND_COVARIANCE.md` L12 写 `var_p = Σ v_j w_jp² / D_p²`（分母 = 覆盖面积 `D_p = Σ a_jp`，并注「实现 = 分子 `acc.sumVarNum += varianceValue·w²`、分母 `D_p = Σ a_jp`（`drizzle_engine.cpp:1632-1634`）」）。**两式差 `(N_p/D_p)² = pixfrac⁻⁴`**（因 `N_p = D_p/pixfrac²`）。实测代码：`sed -n '1617,1649p' lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp` → L1617 `Scalar weight = overlap_area / drop_area;`、L1644 `acc.sumNorm += Scalar(overlap_area * (pixel_area / drop_area));`、L1645 注释「var=**sumVarNum/sumNorm²**」（= `N_p²`）→ **实现按本份口径**，`UNCERTAINTY_AND_COVARIANCE.md` 写的 `/D_p²` 与实现和本份不一致（它自称引用同一实现行号，但该行的实现注释是 `sumNorm²`）。**判定：两份 science 文档对同一方差式给出两个互斥分母；正确方为本份与代码**。<br/>另：本份 L144「源像素 NaN/Inf」行的"唯一口径文字 = `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md` §2a"把口径正本让给 interfaces 层，而 §2a 属合同层——标准 01 §1「科学公式的唯一权威是 `docs/science/` 与 `docs/algorithms/`」→ **让渡方向违反权威链**（科学判据的"唯一口径文字"不得在合同层） |
| 重复或重叠对象 | ① 与 `docs/science/UNCERTAINTY_AND_COVARIANCE.md` §逐像素方差：**同式两份、取值冲突**（见上）。② 与 `docs/algorithms/DRIZZLE_GEOMETRY.md`（28 份 algorithms 之一）：面积/交叠几何两份（本份给守恒律与准则，算法页给面分配算法与冻结门）→ 合法分层；差异仅在上位指针（上位 §2.4 指 algorithms、本份自称 §5.2 错锚）。③ 与 `docs/plugins/algorithms_phase1/08_drizzle.md`（本份 L38/L54 引 `drizzle.pixfrac` 默认 0.8）：默认值两份需核（plugins 与 `defaults.json` 谁正本）。④ 与 `实验/healpix-polar/`、`实验/m42-realdata/code/c4_leaf_allocation.py`（创新点四实验）：本份 §5「极区 Lipschitz 常数 `C=π/2, C45=π/(2√2)`」与 §7 零漏选属实验单元主题（跨批核）。 |
| 处置建议 | **保留（正本）+ 三项**：① **改上游标注为 `ASTROCS_DESIGN.md §4.2（Phase1 节点流程：drizzle 球面重采样）、§4.4（输出合同）、§4.6（硬约束）`**；② 与 UNCERTAINTY 的方差分母二选一（应以本份 + 代码的 `N_p²` 为准，UNCERTAINTY 改文并加 `pixfrac` 幂次说明）；③ §5 的"一手文献逐字"块与 §14a 的开源对照整合进 `docs/references/`（或保留但改为引用式），本份留结论；④ 统一源码锚为全路径；⑤ 把"唯一口径文字"从合同层收回 science 层 |

### 1.20 `docs/science/REJECTION.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/REJECTION.md` |
| 标题 | Rejection / Outlier Science (SCI-REJ) |
| 行数 | 399 |
| 角色（标准 01 §2） | **科学定义**。**但 §5 的 WBPP `bestRejectionMethod()` 逐行考证（L73-107）、§8a 的三段适用域实测（L162-219）、§17 的 M42 真实数据读数（L370-398）三块属"研究与佐证 + 实验台账"**：§17 一节把逐边漏检率表、分母口径订正、复现脚本全写进正式科学文档 → **一份跨三角色** |
| 主题 | 逐像素排异的七方法集合、按几何可贡献数 `n` 的档位路由、`percentile` 判据的等效显著性漂移与适用域、全拒容错可达域、ESD/RCR/winsorized 的阈值冻结与文献归属 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §5.5（逐像素排异）`（**唯一一条，标注准确**；§5.5 L422 亦把"逐像素档位表唯一正本"指给 `docs/plugins/algorithms_phase2/12_rejection.md` §9 → **双向指针成环**，见冲突②） |
| 下游 | 47 命中（root 1 / docs 22 / 实验 0 / 其他 24）。root 1 = `ASTROCS_DESIGN.md`（§5.5 L425「算法出处、合法性窗口、阈值与合成 Oracle 正负例见 `docs/plugins/algorithms_phase2/12_rejection.md` 与 `docs/science/REJECTION.md`」） |
| 是否该主题正本 | **部分是、部分让出**：档位表正本 = `docs/plugins/algorithms_phase2/12_rejection.md` §9（上位 §5.5 L422 指定），阈值表正本 = 本份 §5（plugins §9 L367 反向说「算法本身的公式与参数语义同样以该文件为唯一正本」）→ **两份互相指认为正本**（循环），实测对照见冲突② |
| 开头元信息块 | **有**：L5 `> ID: SCI-REJ-001  范围: SCI-REJ-001..008  状态: FROZEN  上游: SCI-SCOPE-001  下游 ALG: ALG-REJ-001..008  模块: phase2 (rejection)` |
| 日期/编号/sha/历史叙事 | 日期 **2 处：L66「**M3 裁决（负责人 2026-09-25）**」**、L80「M3 裁决 2026-09-25」、L368「N≥6 winsorized（M3 后）」→ **正文直接写"负责人某日裁决"，标准 01 §4 逐字禁止**；sha **7 处**（L74-75「包 **sha1 `712cc7c3fdb523643ad0e685104592d511996f82`**」两处 + L104 同串缩写 + L325）；sha256 0；任务编号 L84 `run/REJECT-DOCFIX-01/REPORT.md`、L292 节题「SCI-001-S2 补齐」、L263「SCI-006 指定问题」；历史叙事密度高：L53「`wbpp_current`（**历史 alias**，解析为 `wbpp_2_9_1`）」、**L302「本仓**旧引文**中的这两个名字在 IRAF 里不存在，**已删去**」**、**L307「来源归属（**订正后，取代旧表述**）」**、**L326「仓内旧引文 `BPP-FrameGroup.js:1304-1312` **已作废**：该文件名不存在于任何官方 WBPP 包」**、**L391「该读法**已作废**」**、**L396「这一说法**已收回**」**、L286「（原 UNRESOLVED）」、L368「M3 前为 linear_fit 档；M3 后同档改投」。**判定：本份与 PHASE2_UPM 是 science 目录中"日期 + 裁决叙事 + 作废/收回叙事"最重的两份** |
| 悬空引用 | 扫描 18 条 / 报缺 8：① **L84 `run/REJECT-DOCFIX-01/REPORT.md`、L397 `run/REJECT-DOCFIX-01/scripts/m3_m5_eval.py`、L398 `run/REJECT-DOCFIX-01/evidence/m3_m5_eval.json`、L398 `m3_m5_metrics.json`、L397 `m3_m5_metrics.py`（无路径）** → **`run/` 全部不入库（`git ls-files "run/**"` = 0）→ §5 的 M3 裁决"实测依据"与 §17 的全部读数都挂在这组断锚上（5 条）**；② L75「`product-info.txt` 自述版本 2.5.9」与 L324「`WeightedBatchPreprocessing-engine.js:1421-1429`」为外部文件（仓内不存在，属一手软件锚，可接受但须带获取方式）；③ L25/L26/L153/L157/L159/L247 `rejection.h` 系列、L45/L46/L50/L51/L53/L100 `rejection.cpp`、L112 `integrate.cpp`、L24 `stage2_common.cpp` → **basename 锚（实存于 `lib/algorithms/coverage/{src,include/astro/phase2}/`，§13 给全路径）→ 两种锚法混用**；④ L43「本层路由只由该值决定（**`docs/science/REJECTION.md:16`**）」——**文档自我引用行号**（第 16 行是 §2 符号表的 `n` 行），行号脆弱锚，一旦该节改动即断 → 登记为缺陷写法 |
| 与上位冲突 | **四处**：① **生产算法集不一致**：上位 §5.5 L423「生产排异算法集为 **none / percentile / winsorized / linear fit**」；本份 §5 L66-72「M3 裁决…原 `n ≥ 16 → linear_fit` 档改投 winsorized…**linear_fit 仍是合法显式方法（`request=linear_fit`），只是不再由 AUTO 在生产档产出**」；`docs/plugins/algorithms_phase2/12_rejection.md` §9 L364 也写「生产排异算法集 = none / percentile / winsorized / linear fit」→ **上位与 plugins 的"生产算法集"含 linear_fit，本份说生产 AUTO 档不再产出它**（"合法显式方法"与"生产集"是两个不同概念，上位未区分）→ 判定为**上位口径歧义**，非取值互斥，须订正上位措辞。② **档位表正本循环**：上位 §5.5 L422 说"唯一正本 = plugins §9"，plugins §9 L367-368 说"逐像素冻结映射表见本 §9；**算法本身的公式与参数语义同样以 `docs/science/REJECTION.md` 为唯一正本**"，本份 §16 L367-368 又说"逐像素冻结映射表见 plugins §9；本节 §5 的 `astrocs_adaptive_pixel` 表为生产 profile 解析面，两者以 §5 冻结阈值为共同锚"→ **三份互相指认、且本份 §5 已把整张档位表（含 n≤3/4≤n≤5/n≥6 三档）复制进正式文档**（上位明写"本节不复制档界与取值"，plugins 也自称档界正本）→ 标准 01 §2「同一主题只有一份正本」不满足。实测对照命令：`grep -n "1 ≤ N ≤ 3\|1≤N≤3\|1 ≤ n ≤ 3" ASTROCS_DESIGN.md docs/science/REJECTION.md docs/plugins/algorithms_phase2/12_rejection.md` → 上位 0 命中、本份 L63 命中、plugins 命中。③ **本份 §5 L79-82 自陈"本表采纳 WBPP 的档界（6/15），但不采纳其 `n>15` 档的算法类型"** → 与上位 §5.5 L423 无冲突，但与 plugins §9 的同一段说明**逐字重复两份**。④ **§8a 与 §4 的 `n≤3` 处置互斥表述**：§4 L51「`1 ≤ n ≤ 3` 由内核闸 `underdetermined_n = 3` 判 `UNDERDETERMINED`、**永不进方法核**」与同段「生产档 `astrocs_adaptive_pixel` 解析为 `none`（method=0）」并存，§7 L144 又写「`n ≤2` 恒 `UNDERDETERMINED`」→ **`n=3` 究竟走 `none` 路由还是被内核闸拒，本份两处说法不同**（§5 L63「1 ≤ n ≤ 3 → none」vs §7 L144「n ≤2 恒 UNDERDETERMINED」）→ 判定为文档内**取值边界不一致**（3 这一档归属），须统一 |
| 重复或重叠对象 | ① `docs/plugins/algorithms_phase2/12_rejection.md` §9（档位表 + 合法性窗口表 + M3 依据段）——**两份全表**，差异：plugins 多出"显式指定的合法性窗口"表（percentile ≤8 帧等）与 provenance 条款；本份多出 §8a 适用域与 §17 实测读数。② 与 `docs/algorithms/PHASE2_REJECTION.md`（算法层）：符号与逐符号锚两份（本份 L134 反向指它"逐符号锚见 §3"）→ 合法分层。③ 与 `docs/architecture/DATA_FLOW.md`：排异在数据流的位置两份（合法）。④ 与 `docs/science/NOISE_MODEL.md` §5b 的「宇宙线、热像素、饱和、响应非线性是污染与确定性截断，按 §4 饱和域与 §5 的 5σ 裁剪处置」——**污染处置在本份（排异）与 NOISE_MODEL（裁剪）两处各写一遍**，差异：NOISE_MODEL 说"不属方差项"、本份说是排异对象；两处不冲突但归属未交叉声明（本份不引 NOISE_MODEL §5b）。 |
| 处置建议 | **保留（主题正本）+ 五项**：① 把 §5 的档位表与 §8a 适用域**留本份**、plugins §9 改为指针（并由上位 §5.5 L422 同步改指本份），消除循环正本；② 删两处"负责人 2026-09-25 裁决"的日期与命名字，改写为正向条款「生产档 `n≥6` 取 winsorized_sigma」+ 依据指针；③ 5 条 `run/REJECT-DOCFIX-01/*` 证据迁 `artifacts/evidence/reject-docfix-01/`；④ 「已作废/已收回/旧引文/历史 alias」四处历史叙事删除或改正向陈述（外部软件版本差异属必要事实，应改为"WBPP 2.5.9 的该档为 ESD，本表该档取 winsorized，差异依据 = 本文 §5"，不提"旧引文"）；⑤ 统一 `n=3` 归属；⑥ 修 L43 的自我行号引用 |

### 1.21 `docs/science/ASTROMETRY.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/ASTROMETRY.md` |
| 标题 | Astrometry / WCS Science (SCI-WCS) |
| 行数 | 294 |
| 角色（标准 01 §2） | **科学定义**（一致）。§8a（外部闭环口径）与 §14 的开源对照属"研究与佐证"混装但必要 |
| 主题 | WCS/SIP 天测科学定义、像素/世界坐标约定、CRVAL/CRPIX 语义、SIP 畸变、星表匹配与残差判据、外部闭环（与已知天区对拍） |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §4.2（Phase1 节点流程）、§4.4（输出合同）` |
| 下游 | 45 命中（root 0 / docs 24 / 实验 0 / 其他 21）。根级九份 0 引用；docs 侧 24 份含 `docs/algorithms/{PLATESOLVE,GAIA_QUERY,GATES_AND_TOLERANCES,PHASE2_*}.md`、`docs/contracts/{DATA_SEMANTICS,PUBLIC_API,INDEX}.md`、`docs/modules/{plate_solve,gaia_xpsd_client,registry/astrocs.phase1.wcs-platesolve}.md`、`docs/owner/SCIENCE_OVERVIEW.md`、`docs/science/{STAR_DETECTION,PSF}.md`、`docs/standards/STANDARDS_REGISTRY.md`、`docs/traceability/*` |
| 是否该主题正本 | **是**（天测/WCS 科学正本） |
| 开头元信息块 | **有**：L5 `> ID: SCI-WCS-001 (SCI-AST-001 别名)  状态: FROZEN  上游: SCI-SCOPE-001  下游 ALG: ALG-WCS-001..  模块: plate_solve (I…)` → **一个主题两个 ID（"SCI-WCS-001 (SCI-AST-001 别名)"）**，标准 01 §2「同一主题只有一份正本」的 ID 面违反：`STAR_DETECTION.md` L108 与 `PSF.md` 都以 `SCI-AST-001` 名字引用它，而 `DOCUMENT_INDEX.yaml`、`docs/TRACEABILITY.csv` 用 `SCI-WCS-001` → **两套 ID 并行使用** |
| 日期/编号/sha/历史叙事 | 日期 0；sha 2 行（L257/L265 命中的是 URL 中的 `aah3859/aah3860` 与 DOI 数字串，**误报**）；任务编号 2 处（L167/L171，含 L202 `run/GATE-DERIVE-01/REPORT.md`）；历史叙事 2 处（L49、L199）。**判定：违规面集中在 L202 的临时区锚与双 ID** |
| 悬空引用 | 扫描 28 条 / 报缺 11：① **L202 `run/GATE-DERIVE-01/REPORT.md` —— `run/` 不入库，真悬空（1 条）**；② L220 `p1_sources.json`、L54/L114/L117/L226 `p1_wcs.json` —— **运行产物名，属合同/产品面词汇，不是文件引用**（判定非悬空）；③ L92/L107/L265 等 `ipv_wcs.cpp`、`ipv_select.cpp`、`ipv_entry.cpp`、`dpsf_psf.cpp` 全为 basename（实存于 `lib/algorithms/platesolve/cpp/ipv/`、`lib/algorithms/psf/src/`）→ **无路径锚**；④ L186 引 `GATES_AND_TOLERANCES.md`（无路径、无 §号，实际 `docs/algorithms/GATES_AND_TOLERANCES.md`）；⑤ L257/L258 的 `aanda.org` URL 属外部文献（Paper I/II 原文页）；⑥ L265「逐字核验」引用块内给出的 `A&A 395, 1061/1077` 节号需人工复核（本批不访问外网）。**判定：1 条真悬空 + 8 条无路径锚 + 1 条无路径无 §号文档引用** |
| 与上位文档冲突 | ① **文档内两处对同一件事互斥（本批最干净可证的一条自相矛盾）**：§5a L107 写「`ipv_wcs.h:43,57-60,70-71` 把该输出注释为"0-based FITS 像素"，**属标签错误**（见 §14a）」；而 §14a L265 结尾写「…迭代反演 `x = u + CRPIX = p` 已是 1-based FITS；**`ipv_wcs.h:43,57-60,70-71` 的注释与 §5a 一致**」→ 同一处代码注释，一处判"标签错误"、一处判"与 §5a 一致"。命令：`grep -n "ipv_wcs.h:43,57-60,70-71" docs/science/ASTROMETRY.md` → 2 命中（L107、L265）。**判定：同一文档两处互斥断言，其中一处必为错**。② 同类第二处：§3a L57-58 写「上述 2/3/4 是**三个不同量**在各自边界上的单次换算，互不重复；**"唯一桥接点"这一表述不成立**，正确口径是「每量一次」」，而 §7 L151 写「该桥接**只发生一次**——**位于 Phase3 导出边界的单一函数**（§5a）」→ §7 的"单一函数/只发生一次"正是 §3a 判为不成立的"唯一桥接点"表述（§3a 的 2/3/4 列了三个换算点：closure_metric.py、p3_wcs.cpp、module_adapters.cpp）。**判定：§7 与 §3a/§5a 互斥**。③ **上位层面无取值冲突**（上位 §4.2/§4.4 未规定像素换算细则），但本份 §5a/§3a/§7/§10 四节反复讲同一"桥接一次"规则（三份互斥表述 + §10 又列"在 §5a 之外再施加一次"为不可接受变化）→ **同一规则四处复述、措辞互斥**（标准 01 §2 唯一正本 + §4 自洽）。④ **§11a L222 把科学口径的依据挂在模块台账上**：「星表样本…`G < 18`｜与 `lib/algorithms/platesolve/memory.md:49-63` 记录的工具口径一致」——`lib/algorithms/platesolve/memory.md` **确在跟踪面**（`git ls-files \| grep -cx "lib/algorithms/platesolve/memory.md"` → 1），但按标准 01 §2「过程台账不入正式文档层、不得作正本依据」→ **科学文档以模块过程台账为依据 = 层级倒挂**。⑤ **§11a L238 的状态字段**：「当前分档阈值 **UNJUSTIFIED（发布门=N）**：实测值是在 **UNIT-001**（XISF 母版单位）未修复的输入上取得的」→ 未冻结阈值 + 任务编号 + "未修复输入"写进冻结节，属过程台账语态（标准 01 §4）。 |
| 重复或重叠对象 | ① 与 `docs/algorithms/PLATESOLVE.md`、`docs/plugins/algorithms_phase1/05_platesolve.md`：WCS 求解流程三份（本份给科学定义、算法页给求解器、plugins 给节点端口）→ 合法分层。② 与 `docs/science/{PSF,STAR_DETECTION,PHOTOMETRY}.md` 的「3a 坐标 frame」小节：**四份各写一遍"像素中心 = 索引 + 0.5、内部 0-based、`DATA_SEMANTICS §17`"**，其中 `STAR_DETECTION.md` L108-110 与 `PSF.md` L45-47 内容近乎相同（差异：PSF 只述窗口拟合、STAR_DETECTION 加了 plate solve 消费方与 DISP-STAR-007 注记）→ **同一约定的三份副本，正本应是 `docs/contracts/DATA_SEMANTICS.md §17` + `docs/GLOSSARY.md`（两份已指向它）**。③ 与 `docs/science/PHASE3_HIPS_TO_FITS.md`：FITS WCS Paper I/II 的 CRPIX/CRVAL/LONPOLE 语义两份（差异见 §2 G5）|
| 处置建议 | **保留（正本）+ 三项**：① 消双 ID（`SCI-AST-001` 与 `SCI-WCS-001` 取一，另一份文档同步改名，属重命名贯穿项）；② L222 的模块 `memory.md` 依据改为 `eng/` 合同锚或删除；③ 修 `run/GATE-DERIVE-01/REPORT.md` → `artifacts/evidence/`；④ 8 条 basename 锚补全路径；⑤ 与 PHASE3 的 Paper I/II 语义归并为一份（建议本份承载 Paper I/II 通用语义，PHASE3 只留 TAN 适用域） |

### 1.22 `docs/science/CALIBRATION.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/CALIBRATION.md` |
| 标题 | Calibration Science (SCI-CAL) |
| 行数 | 507（**science 目录最长的一份**） |
| 角色（标准 01 §2） | **科学定义**。**但 §3 的 FITS/XISF/PCL 规范逐字取证（L30-68）、§6 的四家实现逐条对照（L186-227）、§11 的门盲区实测（L355-401）、§14 的 9 条一手文献逐字核验（L429-444）合计约 200 行 = 全份 40%，属"研究与佐证"角色**；§6a 的判据推导属"算法推导" → **一份跨四角色** |
| 主题 | bias/dark/flat/cosmetic 母版约定与校准算式（双分支 + `K = t_light/t_dark` + floor 0.1）、ADU 域与标度类别、暗场线性前提的可检验形式与真实数据判定、暗场-亮场曝光容差的科学判据、坏点两类形态的可分性、标度一致性门 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §4.2（Phase1 节点流程）、§4.6（硬约束）`。**未标 §4.3（母版标度红线与暗场-亮场曝光容差判定，上位 L308 明写该两条"见 `docs/plugins/algorithms_phase1/` 与 `defaults.json`"）** → 本份 §6a 自称该判据正本，与上位指向不一致（见冲突①） |
| 下游 | 61 命中（root 0 / docs 18 / 实验 5 / 其他 38）。根级九份 0 引用；docs 侧 18 份含 `docs/algorithms/{CALIBRATION_ALGORITHMS,COSMETIC_ALGORITHMS}.md`、`docs/algorithms/anchors/{ANCHOR_CONTRACT.md,anchor_contract.json}`、`docs/contracts/{CONFIG_CONTRACT,DATA_ARTIFACTS,DATA_SEMANTICS,INDEX}.md`、`docs/modules/{calibration,phase1_session}.md`、`docs/owner/SCIENCE_OVERVIEW.md`、`docs/science/{PHOTOMETRY}.md`、`docs/traceability/*` |
| 是否该主题正本 | **是**（校准科学正本）。但 **floor 0.1 / `master_flat_median_range` 的默认值**同时由 `eng/packaging/config/defaults.json` 与 `docs/contracts/CONFIG_CONTRACT.md` 承载 → 取值链三份，本份已声明"唯一全精度写法"仅对 MAD 常数 |
| 开头元信息块 | **有**：L5 `> ID: SCI-CAL-001  状态: FROZEN  上游: SCI-SCOPE-001  下游 ALG: ALG-CAL-001..  模块: calibration` |
| 日期/编号/sha/历史叙事 | 日期 0；**sha 6 行**：L44 文件名含日期串 `…-20250204@035646-180S-Lum.fts`（观测文件名，非留痕哈希）；L201/L224/L439 命中的是 **git commit 短 SHA 逐字引用**——`iraf-community/iraf` main @**`b80c8df1`**、Siril @**`6284dc9`**、LSST `afw` w.2026.39 @**`41b6eb5`**、ccdproc @**`0c21068`**、`pipe_tasks` @**`e6ec3c74`**、`meas_algorithms` @**`4a7591d`**、ip_isr commit **`28faec7dd2297d2ff9f108e543b2d55fdb046345`**（L439，40 位全 SHA）→ **标准 01 §4「正文无 commit」**。**但这是 AGENTS §5/§12.1 要求的"开源库给项目+版本+文件:行"取证形式的必要组成部分 → 两份判据直接冲突**（见 §我证伪任务书，需负责人裁决豁免条款）；任务编号 3 行（L68 `DATA-P1-CAL`、L172/L310 `ALG DISP-CAL-013`、L186「**订正：原文…**」）；历史叙事：**L186「（订正：原文"坏点稀疏且与天体源不混淆（连通域大小过滤可分离）"把"稀疏"当成了全局前提——该前提**只对①成立，对②被证伪**）」**= 文档内公开自我订正旧表述，标准 01 §4 禁；L378-380「(d) dark bias 约定门**见 (c)**」= 同一门写两遍（编号 (c)/(d) 重复，L378 与 L379 都是"dark bias 约定门"）|
| 悬空引用 | 扫描 42 条 / 报缺 23，逐条判定：<br/>**A. 真悬空（gitignore 临时区，4 条）**：L46 `run/SCI-FIX-SEMANTICS-01/evidence/dark_tolerance_criterion.json`、L150 `run/SCI-FIX-SEMANTICS-01/evidence/calibration_dark_linearity.json`、L191-192 `run/LINDEF-CLOSE-01/evidence/cc_and_kappa_measurements.json` 与 `run/LINDEF-CLOSE-01/measure_cc_kappa.py`、L281-282 同两条再引。**→ §6a「暗场-亮场曝光容差的科学判据（冻结）」与 §6 坏点可分性判据的全部实测数都落在不入库目录**。<br/>**B. 外部 URL/仓库文件引用（非仓内悬空，12 条）**：`fits.gsfc.nasa.gov/standard40/…`、`pixinsight.com/xisf/xisf-1.0.xsd`、`gitlab.com/pixinsight/PCL/-/raw/master/src/pcl/XISFReader.cpp`、`hst-docs.stsci.edu/…`、`ccdproc.readthedocs.io/…`、`pipelines.lsst.io/…`、`raw.githubusercontent.com/lsst/ip_isr/main/…`、`…/isrFunctions.py`、`isrTask.py`、`ccdproc/core.py`、`include/lsst/afw/image/Defect.h`、`repair.py`、`src/Interp.cc`（外部文件均带 commit/版本 → 可核验形式，合规）。<br/>**C. 仓内 basename 锚（7 条）**：`calibrator.cpp`（L126/L304/L305/L318，实际 `lib/algorithms/calibration/src/calibrator.cpp`）、`master_generator.cpp`（L297/L306）、`cosmetic_corrector.cpp`（L314/L333）、`module_adapters.cpp`（L130/L375，实际在 `lib/infrastructure/scheduler/src/`）、`normalize_flat`、`ac::calibrate`、`master_unit_guard.h`（L359 给了全路径 `lib/include/astrocs/core/master_unit_guard.h`，实存）。<br/>**D. 节号/文件指向可疑**：**L230 引「`ASTROCS_DESIGN.md` §4.3:254（母版标度红线与暗场-亮场曝光容差判定）」与「§4.5:300（暗场与亮场曝光差超出容差 = 🟠 warn）」** —— 行号 254 落在 §4.2 区间（§4.3 起于 L278）、行号 300 落在 §4.3 内（§4.5 起于 L332）→ **两个"§x:行号"锚都指错节**；上位实际写该两条的位置是 **§4.3 L308**（母版标度红线/曝光容差）与 **§4.5 L354**（warn 示例）。**L471-473 的"曾被归到 `docs/design/UNIFIED_MODEL.md §6`，但该文件当前只有 §1–§3"** —— 该文档实际有 §1–§3（`grep -c "^## " docs/design/UNIFIED_MODEL.md` 复核）→ 本条**自证订正记录**，属历史叙事而非悬空 |
| 与上位文档冲突 | **三处**：① **判据正本与上位指针不一致**：上位 §4.3 L308「母版标度红线（bias/dark 声明、平场归一化声明、换算因子一致性）与暗场-亮场曝光容差判定见 `docs/plugins/algorithms_phase1/` 与 `eng/packaging/config/defaults.json`」——**上位把两处正本指给 plugins + defaults.json**，而本份 §6a 自称「暗场-亮场曝光容差的**科学判据（冻结）**」并给出 `|K·Δb| ≤ ε·σ_frame`（上位与 plugins 均无此式），且 `ε` **无登记项**（本份 L256-258 自证：「`defaults.json` 的 `fields[]` 内**没有**承载 `ε` 的项 ⇒ `ε` 的数值面**未冻结**，须由负责人裁决后登记」）→ **一个未冻结的判据写在正式科学文档并被上位指针绕过**。② **上位 §4.5 的 warn 语义与本份 §6a 的"科学面"划分一致**（本份 L231-236 明写两个判定面分开）→ **无冲突，属本份替上位补了它没写的口径**。③ **§9 精度策略「母版算术与校准核心为 FP32」与上位 §3.3 L240「稠密数据默认单精度」一致**，但本份 L320/L452-454 两处写 MAD 常数的 `float32` 舍入值 `1.4826022386550903`（相对差 +1.359e-08），而 `NOISE_MODEL.md` §9 L312 只给 FP64 值并规定"11 位简写只能出现在≈语境" → **同一常数在两份 science 里的精度条款不同**（本份允许单精度存储写法、NOISE_MODEL 未记）→ 需统一 |
| 重复或重叠对象 | ① 与 `docs/algorithms/CALIBRATION_ALGORITHMS.md`（本份 L130 引其 §3.3 F3.1/F3.2）与 `docs/algorithms/COSMETIC_ALGORITHMS.md`：算法两份，合法分层。② **与 `docs/plugins/algorithms_phase1/01_calibration.md`（上位指定的默认值正本）**：标度声明键（`master_units`/`master_scale`/`master_flat_normalize`/`dark_optimization`）与门判据在两份各写一遍 → 需核默认值与判定带是否逐项一致（本批不代审 plugins，登记跨批核 X-5）。③ 与 `docs/science/NOISE_MODEL.md` §4 饱和域：本份 §9a「saturation：FP32 饱和语义界定，不静默 clamp」与 NOISE_MODEL §4「`x ≥ saturation_level` 为饱和像素，不参与 blank-sky 统计」**两处"饱和"语义不同**（本份说的是数值溢出、NOISE_MODEL 说的是探测器饱和电平过滤）→ **同词两义未具名**（标准 01 §4 要求术语唯一），需本份改名"FP32 数值饱和"。④ MAD 常数 `1.482602218505602` 第六处出现（本份 L320/L452）。⑤ `testdata/Victory_Nebula_T4_Flying_Dutchman/`（L43）与 `testdata/{T2,T3,T4} calibration files/`（L148）：**两处在跟踪面与登记面都不存在**——`git ls-files testdata` 只含 `BASS_DR3/`、`README.md`、`index.json`（369 文件），`git check-ignore -v testdata/Victory_Nebula_T4_Flying_Dutchman` → `.gitignore:41:testdata/*`；而 `testdata/README.md`（AGENTS §7 L149 指定的"入库/下载策略唯一说明"）**只登记 BASS_DR3 与 HST_M16 两组**（`grep -c "M42\|Victory\|Galaxy_Center\|calibration files" testdata/README.md` → 0）。本机磁盘上虽有这些目录（`ls testdata/` → 16 项），**但正式仓库既不入库也不说明如何取得** → **本份 §3/§6/§6a 的全部真实数据判据（T4 截距 +105.70 ADU、T2 斜率 −0.03824 ADU/s、1907 ADU 中位、57.82 ADU 尺度、四组母版连通域分布）在仓库内不可复现、不可追溯**，属"科学文档消费了未登记的数据集"。同类缺口另见 `ACCEPTANCE_SPEC.md` §6 与 §3（M42 49 帧 + Galaxy Center 32 帧 + HST_M16 三帧，其中 **`testdata/HST_M16/` 本机磁盘亦不存在**：`[ -d testdata/HST_M16 ]` → 假，README §3 只给 MAST 下载途径）。→ 登记为**整批共有的结构性缺口**（见 §索引与链接缺陷 X-6/X-9）|
| 处置建议 | **保留（主题正本，且是本批判据质量最高的一份）+ 六项**：① `ε` 数值面登记或把 §6a 降为"判据形式已定、数值待裁决"并上呈；② 4 条 `run/` 证据锚迁 `artifacts/evidence/{sci-fix-semantics-01,lindef-close-01}/`；③ 修 L230 两个 `§x:行号` 错锚为 `§4.3`/`§4.5`（不带行号）；④ L186「订正：原文…」改写为正向条款；⑤ 合并重复的 (c)/(d) 门编号；⑥ 与上位 §4.3 L308 建立互指（上位应把科学判据正本改指本份 §6a，plugins 只留配置键）；⑦ 统一 MAD 的单精度条款与"数值饱和 vs 探测器饱和"命名 |

### 1.23 `docs/science/PSF.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/PSF.md` |
| 标题 | PSF Science (SCI-PSF) |
| 行数 | 211 |
| 角色（标准 01 §2） | **科学定义**（一致） |
| 主题 | 椭圆 Moffat4 点源响应模型、7 参数拟合（LM）、FWHM/通量解析常数与适用域、`q_psf` 质量代理、`θ` 值域归约、截尾均值→σ 换算常数的闭式推导 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §2.1（测光星等坐标系）、§4.2（Phase1 节点流程）`。**未标 §4.4（PSF 有效面积随帧级 SNR 落盘，上位 L326）** |
| 下游 | 45 命中（root 0 / docs 19 / 实验 5 / 其他 21）。根级九份 0 引用（上位正文用 `docs/plugins/algorithms_phase1/04_psf.md`，未直接指本份） |
| 是否该主题正本 | **是**（PSF 科学正本；`STAR_DETECTION.md` L102/L170 与 `PHOTOMETRY.md` 都以 `SCI-PSF-001` 指本份） |
| 开头元信息块 | **有**：L5 `> ID: SCI-PSF-001  状态: FROZEN  上游: SCI-SCOPE-001  下游 ALG: ALG-STARPSF-001..  模块: dynamic_psf`（"模块: dynamic_psf"旧名，§13 实为 `lib/algorithms/psf/src/dpsf_psf.cpp`） |
| 日期/编号/sha/历史叙事 | 日期 0、sha 0、sha256 0；任务编号 2 处（L39 `run/RELEASE-05/vis/out/m42_p1_t2/p1_sources.json`、L143「SCI-002 指定问题」）；历史叙事 0。**判定：本份写法违规面只在 `run/` 锚** |
| 悬空引用 | 扫描 15 条 / 报缺 7：① **L39 `run/RELEASE-05/vis/out/m42_p1_t2/p1_sources.json`（真悬空）**——该"真实产物"是 §3「`θ` 的单位与值域」实测 n=12451 的唯一证据；② **L43/L81/L128/L141/L179 引 `run/SCI-FIX-STARPSF-01/results/{e1_constants.txt,e3_real_summary.txt}` 共 6 处**——§5 FWHM 因子、§9 `kTrimMeanToSigma` 闭式与适用域、有限-m 效应三整套实测**全挂在这组不入库目录**；③ L16/L17/L23/L51/L76/L80 `dpsf_psf.cpp`（basename，实际 `lib/algorithms/psf/src/dpsf_psf.cpp`）、L122 `noise_model.cpp:95`、L93 `sdet_api.cpp:2071-2074`（basename）；④ L189 的 aspbooks URL（外部）。**判定：7 条真悬空（全部 `run/`）+ 3 条无路径源码锚**。§15 Acceptance 与 §11 Oracle 未给任何仓内可复跑命令（只说"Python 参考 `scipy`/NumPy"）→ **实测证据不可复跑** |
| 与上位冲突 | ① **检测侧母函数与本份模型不同**：本份 §10 L154 声明「在**本模块内**引入未文档的高斯备选拟合路径作为主路径」不可接受，并显式排除「检测侧 `lib/algorithms/star_detection` 的椭圆高斯母函数不属本条范围：SCI-P1-STAR-001 §3、DISP-STAR-007」；`STAR_DETECTION.md` L102-104 则写「检测侧母函数 = 椭圆高斯、**PSF 侧 = 椭圆 Moffat4**；两模型**宽度列不可跨块比较**：同 sx 下 `FWHM_gauss/FWHM_moffat = 2.354820/1.230310 = 1.9140×`」→ **两份互引、口径一致**（正面示范）。② **上位 §4.4 L326「PSF 有效面积随帧级 SNR 一并落盘」未在本份出现**（本份无"有效面积"节，`A_NEA` 定义在 `PSF_SIGNAL_WEIGHT.md` §2 L32）→ **本份漏承接上位一条落盘要求**，且未给出指针 → 判定：覆盖缺口（非冲突）。③ 本份 §5 L66 `FWHM = 1.230307652590102·σ`（精确值）与实现常量 `MOFFAT4_FWHM_FACTOR = 1.230310`（相对差 +1.91e-6）→ 本份如实标注，无冲突 |
| 重复或重叠对象 | ① 与 `docs/algorithms/STAR_PSF_ALGORITHMS.md`、`docs/plugins/algorithms_phase1/04_psf.md`：拟合流程与数值方法两份，合法分层（本份 L170 反向指 `docs/TRACEABILITY.csv`）。② **与 `docs/science/STAR_DETECTION.md`：`FWHM_gauss = 2.3548·σ`、`FWHM_moffat = 1.230310·σ`、`1.9140×` 三个常数两份**（STAR_DETECTION L104/L170 与本份 §5/§7）→ 差异：本份给推导与精确值、STAR_DETECTION 给比较比 → 轻微复述，可接受。③ MAD/截尾常数体系：本份 L122/L179/L188 的 `0.7316727929211932` 与 `NOISE_MODEL.md` §9 L312「`q_psf` 的 `0.7316727929211932`（10–90% trimmed mean）与 MAD 常数不可互换」**两份互指且都声明不可互换** → 一致，属必要对照（两份都点名对方，是**正确示范**）。 |
| 处置建议 | **保留（正本）+ 两项**：① 7 条 `run/SCI-FIX-STARPSF-01` / `run/RELEASE-05` 锚迁 `artifacts/evidence/psf-constants/` 并在 §11/§15 给出仓内可复跑命令；② 补"PSF 有效面积随帧级 SNR 落盘"的口径或指针（承接上位 §4.4）；③ 元信息块旧模块名订正 |

### 1.24 `docs/science/STAR_DETECTION.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/STAR_DETECTION.md` |
| 标题 | SCI-P1-STAR-001 — Phase1 星点检测（P1-STAR 冻结层） |
| 行数 | 182 |
| 角色（标准 01 §2） | **科学定义**。**但 §4「测试设计锚」（L123-130）整节是测试设计（属"标准与检查/测试"角色）、§1 的表格与"单参数闭式（趋势模型，非验收判据）"属"算法推导" → 一份跨三角色**；且标题把 ID 写在文件名位置（`SCI-P1-STAR-001 —`）→ 与同目录 `# X Science (SCI-XXX)` 体例不符 |
| 主题 | Phase1 星点检测的 SCI 冻结层：亚像素质心、99% 召回阈分档表、虚警口径、全局阈值与 `bgnoise` 量纲、饱和/混合/边缘判据与 `dynrange` 适用域、确定性排序、两套 `sigma_bg` 的不可互换性 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §2.1（测光星等坐标系）、§4.2（Phase1 节点流程）`。**另 L9「ALG 权威: ALG-STARDET-001（docs/algorithms/STAR_DETECTION_ALGORITHMS.md §11）」= 第二条权威序**（标准 01 §1 末条"不声明自己的第二套权威顺序"；此处是"ALG 权威"让渡，属必要分工还是第二序需裁决 → 判定：让渡合法但形式上是第二个权威头） |
| 下游 | 23 命中（root 0 / docs 11 / 实验 0 / 其他 12）。根级九份 0 引用；docs 侧：`docs/DOCUMENT_INDEX.yaml`、`docs/algorithms/GATES_AND_TOLERANCES.md`、`docs/contracts/{CONFIG_CONTRACT,DATA_SEMANTICS}.md`、`docs/design/PHASE1_DETAILED_DESIGN.md`、`docs/modules/{star_detector,registry/astrocs.phase1.star-detection}.md`、`docs/plugins/algorithms_phase1/{03_star_detection,04_psf}.md`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}` |
| 是否该主题正本 | **是**（检测科学冻结层正本）。`DOCUMENT_INDEX.yaml` L1356 区段 notes：「P1-STAR-DOC (2026-09-07) 冻结: SCI-P1-STAR-001 冻结层落点」——**索引里带日期，文档里没有**（两份不一致的另一面） |
| 开头元信息块 | **有（三段引用块，L3-L9）**：上游行 + `> 状态: FROZEN` + `> 性质: 本页是…格式沿用 STAR_PSF_ALGORITHMS §11.5 先例` + `> ALG 权威: …`。**违标准 01 §4**（状态编号 + "格式沿用 X 先例"的来历叙述）；**L7-8「格式沿用 `STAR_PSF_ALGORITHMS` §11.5 先例」= 体例依据挂在另一文档的节号上** |
| 日期/编号/sha/历史叙事 | 日期 0；sha 1 行（L82 命中的是 `103847` 类数字或 `2.3548` 上下文，**经复核为 DOI/数字串误报**）；任务编号 1 处（L120「本页与 ALG-STARDET-001/**DATA-P1-STAR**/API-STAR-001 组成…冻结层」+ L125 `TEST-STAR-DESIGN-001`，均正式 ID）；历史叙事：**L38「「54.0」只是 300 次/档下首个恰好零失败的格点、不是稳定阈，**已撤销**」**、**L36「⚠ σ_psf = 1.0 px 档只给区间 [52, 65]，不给点值」**（属如实的能力边界，合规）、L118「现状缺陷不隐瞒（DISP-STAR-001..005 显式登记）…本层不声称缺陷已修复」。**判定：1 处"已撤销"历史叙事（L38）** |
| 悬空引用 | 扫描 11 条 / 报缺 4：① L20/L21/L66/L68/L79/L117/L140 引 `sdet_api.cpp:1772-1774 / :1783 / :1834-1837 / :1856-1857` → **basename 无路径**（实际 `lib/algorithms/star_detection/…`，本份未给全路径，与 PSF/NOISE 同病）；② **L144 `p1_sources.json`**（运行产物名）；③ L158/L168 引 `docs/algorithms/GATES_AND_TOLERANCES.md`（**实存**，L168 还给出 §2/§3 节号 → 全份最规范的锚）；④ L146 引 `lib/algorithms/star_detection/wrapper_phase1/star_detector.cpp:30-70`（**全路径 + 行号，实存** → 正面示范）；⑤ L155 引 `check_science_units.py`（实际 `eng/tests/glossary/` 或 `eng/tools/`，`git ls-files \| grep check_science_units` 需核）。**判定：0 条真悬空；2 条 basename 锚 + 1 条 checker 脚本无路径锚** |
| 与上位冲突 | ① **上位把"星表引导检测"作创新点一的前提（§2.1 L123「星点位置由 Gaia DR3 XP 星表逆映射到本帧像素域获得（星表引导检测，见 §4.2）」），本份写的是盲检测（`sdet_detect_impl` peaker 七步 + `sdet_detect_ex_f64`）与"拟合失败丢弃不计虚警"**：本份**整份未出现"星表引导/Gaia/逆映射"字样**（命令：`grep -c "Gaia\|星表\|逆映射" docs/science/STAR_DETECTION.md` → 0 命中）。而 `ASTROCS_DESIGN.md` §4.2 L273 明写「**星表引导检测**：用本帧已解出的权威 WCS 把 Gaia 星表逆投影到像素域，只对星表位置做质心/PSF 拟合」并指「工作细则见 `docs/plugins/algorithms_phase1/03_star_detection.md` §5.1」→ **检测科学正本承载的是盲检测口径，星表引导检测的科学定义缺位（只在 plugins 工作细则与上位）**。这是"下位未承载上位机制"的典型缺陷（标准 01 §3「每份文档、每个机制都能追溯到最高设计的一条要点；追溯不到的机制先补要点再保留」的镜像问题：**机制有上位要点、无 science 正本**）。② 本份 §1 的 `SNR_peak = A_fit/σ_bg` 定义引 `GATES_AND_TOLERANCES.md` §2（算法层）→ **SNR 定义正本落在算法文档，而 `docs/science/` 三份（CONTROL_WEIGHT_SNR/NOISE_MODEL/PSF_SIGNAL_WEIGHT）各有一套 SNR** → SNR 定义多正本（见 §2 G3/G6）|
| 重复或重叠对象 | ① 与 `docs/plugins/algorithms_phase1/03_star_detection.md`（上位指定的检测工作细则正本）：召回阈表与阈值口径两份（本份 §1 vs 插件 §5.1 的 `star_detection.max_stars`）→ 需核数值一致（跨批 X-7）。② 与 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md` §11：本份 L11/L50/L107/L118/L125/L130 六处把公式锚、残差表、缺陷登记、测试设计全让给它 → **让渡方向正确**（科学层让算法层承载推导），差异：本份仍留了阈值数值表与单参数闭式（属算法层内容）→ 轻微倒置。③ 与 `docs/science/PSF.md`（见 1.23 重复②）。④ 与 `docs/science/SCIENCE_SCOPE.md` §假设「源星点稀疏可掩膜」——本份的完备性/虚警口径是它的经验支撑，两份未互引（可加互引）。 |
| 处置建议 | **保留（正本）+ 四项**：① **补"星表引导检测"的科学定义节**（承接上位 §2.1/§4.2，含"逆映射定义域、拟合失败丢弃不计虚警、按亮度取上限的口径"），或在本份显式声明"星表引导检测的科学正本在 `docs/plugins/algorithms_phase1/03_star_detection.md`"并把该条上收为指针；② L38「已撤销」改正向条款（"σ_psf=1.0 档的阈取区间 [52,65]，点值不具资格"）；③ §4 测试设计整节迁 `docs/ci/` 或 `docs/algorithms/`；④ 统一 `sdet_api.cpp` 锚为全路径；⑤ 删 L5-9 元信息块并改标题体例与同目录一致 |

### 1.25 `docs/science/ACR_EQUIVALENCE.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/ACR_EQUIVALENCE.md` |
| 标题 | ACR Work-Domain Equivalence Science (SCI-ACR-EQUIV) |
| 行数 | 188 |
| 角色（标准 01 §2） | **科学定义**（目录一致）。**但整份主题是"CPU/GPU/混合分块的数值等价边界"= 验证判据/工程等价，按标准 01 的角色表更接近"标准与检查"（`docs/standards/`/`docs/ci/`）或"验证"层；`docs/validation/v6/` 与 `docs/quality/` 才是它的自然落位** → **角色错放（目录对、角色格不对）**，这是本批唯一一例"目录合规但角色不合规" |
| 主题 | Phase2 `synthetic.mosaic_reject.fp64acc` 在 cpu/gpu/mixed 分块上的等价定义、分块合法域 fail-closed、设备域三条差异、失败回退路由、相对容差与绝对容差的标度论证 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §1.3（非目标）、§4（normalize）`。**§4（normalize）错**：本份全部内容是 **Phase2**（`phase2` 加权叠加、`stage2*.cpp`、`coverage/`），与 §4 normalize 无关；正确应是 **§5（mosaic）+ §9（CPU 后端与资源）+ §1.3（非目标 ACR）**。命令：`sed -n '9p;12p' docs/science/ACR_EQUIVALENCE.md`（"使 `phase2` 加权叠加在任意 ACR 调度下科学结果一致"）→ **上游标注与内容错位** |
| 下游 | 16 命中（root 0 / docs 6 / 实验 0 / 其他 10）。docs 侧仅 6 份：`docs/DOCUMENT_INDEX.yaml`、`docs/TRACEABILITY.csv`、`docs/algorithms/PHASE2_MOSAIC_WRITE.md`、`docs/contracts/INDEX.yaml`、`docs/owner/SCIENCE_OVERVIEW.md`、`docs/references/SCIENTIFIC_REFERENCES.md`。**→ 本份是 17 份 science 中下游最少的一份（另少的是 UNIFIED_SCIENCE_MODEL 7、SCIENCE_SCOPE 11）** |
| 是否该主题正本 | **待定**：主题是"ACR 等价性"，而**上位把 ACR 定性为非目标/生产不可达（§1.3 L98「ACR/GPU/CPU+GPU 混合生产路由（ACR 源码保留为隔离实验，生产不可达）」）**。本份 §4 L44 自己给出「`p2_acr_block_eligible` 在本仓**恒返回 `false`** ⇒ **ACR 块在生产不可达是构造性结论**，不存在任何合法配置能进入该块」→ **一份 ACTIVE_NORMATIVE 科学正本报"其判据对象在生产恒不可达"（等价门"只在隔离实验/回归域内有意义"）**。标准 01 §2 要求"正式文档层只承载现行设计"，非目标主题的规范性判据该不该进 `docs/science/` 是**结构性问题**（须裁决：降级为 `docs/validation/` 实验文档、或保留为隔离实验合同） |
| 开头元信息块 | **有**：L5 `> ID: SCI-ACR-EQUIV-001  状态: FROZEN  上游: SCI-SCOPE-001 + SCI-INT/REJ/UPM/DRIZZLE  下游 ALG: ALG-ACR-EQUIV-001..  模块: acr × phase2 (acr_kernels)` |
| 日期/编号/sha/历史叙事 | 日期 0、sha 0、sha256 0；任务编号 0；历史叙事 **L78「(c) **已删**权重记号哨兵槽（该记号**已按 §9.73 A44 作废**；槽位保留且被钉为常量 2，故复活时失败关闭）」** → 「已删/已作废」双违，且「§9.73 A44」是**无所属文档的节号 + 编号**（见悬空）|
| 悬空引用 | 扫描 22 条 / 报缺 11：① **L11 引 `PERFORMANCE_MODEL.md`**（「不决定 ACR 资源调度/性能优化（见 `PERFORMANCE_MODEL.md`）」）——**只写文件名、无目录**；实际文件在 `docs/architecture/PERFORMANCE_MODEL.md`（`git ls-files \| grep -i performance_model` → 1 命中，标题 `# Performance Model`、上游标 `ASTROCS_DESIGN.md §8`）。→ **锚点不合规（无路径）而非文件缺失**；但**同名的还有 `docs/performance/{BASELINE,OPTIMIZATION}.md`** 两份，读者按名字最自然的猜测是 `docs/performance/`（该目录下**没有** PERFORMANCE_MODEL.md）→ **歧义锚，须补全路径**。② **L78「§9.73 A44」——无文件名、无文档路径**（本份 §9 只有 3 条、无 73 小节，`A44` 无仓内定义面）→ **真悬空（章号 + 编号双无所属）**。③ L71 `acr_cuda_bridge_host.cpp:910`、L73/L74 `acr_kernels.cpp:329-330`、L85 `acr_kernels.cpp:75-80,212-245`、L67 `acr_cuda_bridge_kernels.cu:166-272`、L90 `stage2.cpp:67,1125-1130`、L91 `coverage/CMakeLists.txt:28` → **basename/无路径锚**；其中 §5 L83 还引 **「`acr memory.md: BDR Reviewed`」作为回退路由的依据**——**把"模块 memory.md + 评审标记"写进科学合同的"证据"列**（§8 表 L112 同）→ 过程台账充当正本依据（标准 01 §2 违，同 ASTROMETRY L222 一病）。④ L152 `eng/tests/api/test_reject_integration_oracle.py`（实存需核，`git ls-files` 复核：见 §3 命令）。⑤ L64 `drizzlepac 3.11.0 src/cdrizzlebox.c do_kernel_square`（外部，带版本，合规）。**判定：2 条真悬空 + 1 条台账依据（memory.md）+ 7 条无路径锚** |
| 与上位冲突 | **两处**：① §2 符号表 L22 与 §4 L45 声明「**ACR 侧固定 ivar 语义 `stage2.cpp:67,1125-1130`**」与「`acr_route∈{auto,cpu}`，其他取值在配置解析期显式拒绝」，而 `CONTROL_WEIGHT_SNR.md` §5 L163 写「实现侧整数权重模式域**不存在**：`{auto,ivar,equal,support_x_snr2} → {2,2,1,0}` 映射禁用（…；ACR 侧固定 ivar 语义 `stage2.cpp:67,1125-1130`）」→ **两份一致**（本份是 CW 的引用源）。② **§9 相对容差 `max_rel ≤ 1e-6` 与上位 §9 L677 的"事前冻结的浮点容差 + 绝对容差必须声明适用量级域"完全同向**，本份 §9 的三档 ulp 表是上位该条的落地实例 → 无冲突（正面示范）。③ **`§1.3` 与 `§8.1` 的"生产不可达"在本份转为"构造性结论"**（L44），措辞强于上位（上位说"生产路由不做/源码保留为隔离实验"，本份说"不存在任何合法配置能进入该块"）→ 不冲突，但**上位宜采用本份的可判定表述**（整治建议反向：把 L44 的构造性结论上收为上位 §1.3 的口径） |
| 重复或重叠对象 | ① 与 `docs/algorithms/ACR_EQUIVALENCE.md`（**同名两份！**`git ls-files \| grep ACR_EQUIVALENCE` → `docs/algorithms/ACR_EQUIVALENCE.md` 与 `docs/science/ACR_EQUIVALENCE.md`）：同一主题（ACR 等价）在 science 与 algorithms 两层各有一份同名文档 → 需逐段比对判定重复/分层（跨批 DA-02 核，登记 **X-8**）。② 与 `docs/science/INTEGRATION.md` §2/§5：`P2PixelStack`/`wsum`/`support` 语义两份（本份 §2 符号表 L24 又定义"逐像素 ivar 权重 = 阶段二按该天球像素对应帧集合现场算出的派生量"——与 INTEGRATION/UNIFIED 的同一句话第三份）。③ 与 `docs/plugins/algorithms_phase2/12_rejection.md`：`rejection.cpp:2064-2082` 的小样本门槛两份（本份 §5(b) L74-77 详述 CPU/CUDA 分歧）。④ 与 `lib/infrastructure/acr/README.md`、`lib/infrastructure/acr/memory.md`（模块层）。 |
| 处置建议 | **迁移 + 合并**：① 建议整份迁 `docs/validation/ACR_EQUIVALENCE.md`（或 `docs/standards/`），并在 `docs/science/` 留一行"ACR 非目标，等价判据见 …"的指针——**属结构性变更，上呈负责人**；② 与 `docs/algorithms/ACR_EQUIVALENCE.md` 二选一保留（先读 algorithms 那份再定，跨批协同）；③ 补上游为 `§1.3 + §5 + §9`（删错标的 §4 normalize）；④ 修 `PERFORMANCE_MODEL.md`（改指 `docs/performance/`，实测该目录 2 份：`git ls-files docs/performance`）与 `§9.73 A44`（给全路径或删除）；⑤ 删「`acr memory.md: BDR Reviewed`」的台账依据，改指具名合同/门；⑥ 删 L78「已删/已作废」叙事 |

### 1.26 `docs/science/PHASE3_HIPS_TO_FITS.md`

| 字段 | 内容 |
|---|---|
| 路径 | `docs/science/PHASE3_HIPS_TO_FITS.md` |
| 标题 | Phase3 HiPS → 平面 WCS FITS 科学合同 (SCI-P3) |
| 行数 | 236 |
| 角色（标准 01 §2） | **科学定义**。**但 §16「登记面：§5.3 输入语义守卫与产品 provenance 现状」（L219-236）是**实现现状与缺口台账**（含"已接线/未接线/`grep -c` = 0/产品分布计数"），属"模块工作细节 + 过程登记"角色 → 一份跨两角色**；§9a 十二项逐项冻结属合同/设计层内容 |
| 主题 | Phase3 HiPS→平面 FITS 的输入有效域（NESTED/W=512/ICRS/TAN/`abs(dec)≤85°`）、order 选择公式与夹紧域、像素中心/CD/LONPOLE 语义、coverage 真值表、采样核与插值误差界、FOV 畸变定量 |
| 上游 | **有**：L3 `> 上游：ASTROCS_DESIGN.md §6.2（export 流程）、§6.3（投影算法）` |
| 下游 | 51 命中（root 0 / docs 24 / 实验 0 / 其他 27）。根级九份 0 引用；docs 侧 24 份含 `docs/algorithms/{PHASE3_FITS_IMPL,PHASE3_PROJ_IMPL,PHASE3_RESAMPLE,PHASE3_RSMP_IMPL,HIPS_WRITER}.md`、`docs/contracts/{CONFIG_CONTRACT,DATA_ARTIFACTS,DATA_SEMANTICS,INDEX}.md`、`docs/design/PHASE3_DETAILED_DESIGN.md`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`、`docs/modules/{phase3_fits,phase3_proj,phase3_rsmp,registry/astrocs.phase3.*}.md`、`docs/owner/SCIENCE_OVERVIEW.md`、`docs/KNOWN_LIMITATIONS.md`、`docs/standards/STANDARDS_REGISTRY.md`、`docs/traceability/*` |
| 是否该主题正本 | **是**（Phase3 导出科学合同正本，§5 L1「控制包 13 §2 冻结定义」） |
| 开头元信息块 | **有**：L5 `> ID: SCI-P3-001  范围: SCI-P3-001..020  状态: FROZEN  上游: SCI-WCS/SCI-DRZ/SCI-SCOPE  下游 ALG: ALG-P3-001..  模块: phase3 (**未实现**,本合同为施工边界)` → **元信息块内直接写"未实现"**（状态字段用负向事实）；与 §15 Acceptance、§13「实现: 待建（CLI-006/P3-001..004）」三处并存 → 状态散在三处、且 `DOCUMENT_INDEX.yaml` 的 `status:` 字段（ACTIVE_NORMATIVE）与 §12.5 状态阶梯不是一回事（索引头注已声明"不是交付状态阶梯"，属正确区分） |
| 日期/编号/sha/历史叙事 | 日期 **1 处：L192「IVOA HiPS 1.0 Recommendation，**REC-HiPS-1.0-20170519**（https://…/20170519/…）」**（外部规范版本号内嵌日期，属规范名而非留痕日期 → 判定边界情形，可保留但宜写作"REC-HiPS-1.0（2017-05-19 版）"以外的中性形式；同一份在 L177 又写成 `PR-HiPS-1.0-20161122`）；**sha256 1 处：L223「判据 sha256 `562d9f7447b91f650d547ce0a322fc519e91de270956da9c49094b7f163d5de0`」** → 标准 01 §4「正文无 sha256」直违；commit sha 3 行（L177/L179 是 DOI/bibcode 误报）；任务编号 8 处：L5 无、L9/L127/L157/L164/L172/L177/L217 反复引「**控制包 13**」与「**控制包 01 §3**」与「**CLI-006**」「**P3-001..004**」「**SYN-007**」——**其中「控制包 13 §2/§3/§4/§5」出现 5 次，作为冻结定义的权威来源**（见冲突③）；历史叙事 0（除 L5"未实现"是现状陈述） |
| 悬空引用 | 扫描 23 条 / 报缺 9：① **L89/L92/L152/L228 引 `run/SCI-FIX-PHOTFIT-01/evidence/{e1_synthetic_and_realdata,e3_realdata_hips_bunit,e4_healpix_tan}.json` 共 5 处 → 真悬空（`run/` 不入库）**，且这三个 JSON 承载 §5 `θ_pix` 精确性、§5 位移恒等式可执行判据（240 万点零不一致）、§9a-12 FOV 定量、§16 C5 产品分布——**即本份的全部实测锚都在临时区**；② **L9「控制包 13 §2 冻结定义」/ L127「控制包 13 §3 十二项逐项冻结」/ L157「控制包 01 §3」/ L164「13 §5 全集」/ L172「SYN-007 五件套（`eng/tools/validation/phase3`，SYN-007 任务建立）」/ L217「13 §4 候选清单」——`工程控制/` 下现只剩 `RELEASE-05/` 与 `evidence/`，不存在名为"13"的控制包**（命令：`git ls-files 工程控制 \| head` → 只列 RELEASE-05 与 evidence；`ls 工程控制/` → `RELEASE-05  evidence`）→ **5 处"控制包 13/01"引用全部无仓内所指，属真悬空（且它们是"冻结定义"的出处）**；③ L172 `eng/tools/validation/phase3`（`git ls-files eng/tools/validation \| wc -l` → 0，**目录不存在**）→ 真悬空；④ `p3_output.cpp`/`p3_resample.cpp`/`p3_wcs.cpp`/`p3_session.cpp`/`module_adapters.cpp`（basename，实存于 `lib/algorithms/{fits_output,projection,resample}/`、`lib/phase3_session/`、`lib/infrastructure/scheduler/src/`）→ 无路径锚；⑤ L222 引 `docs/algorithms/PHASE3_PROJ_IMPL.md §16`（实存）。**判定：真悬空 10 条（5 条 run/ + 5 条控制包 13/01）+ 1 条目录不存在 + 5 条无路径锚** |
| 与上位冲突 | **四处**：① **投影集合两份、数量互斥**：本份 L37/L132 两处写「registry 冻结集合 = **ASTROCS_DESIGN §5.3** 八投影（TAN/SIN/CAR/AIT/STG/MOL/CEA/ZEA）」——**上位八投影在 §6.3（L453），不在 §5.3（§5.3 是"SNR 重建与逆方差叠加"）** → 引用节号错；且上位 §6.3 同句写「注册表中未实现的投影在被选择时显式报'不支持'（**当前仅 TAN 可用**）」，本份写「v3 **已实现 4/8**（STG/MOL/CEA/ZEA 归 P3-001）」→ **"已实现 4/8"与"当前仅 TAN 可用"是两个不同事实（registry 面 vs 会话面）**，本份 L36-39 自己做了区分说明（"两者不冲突——registry 面已登记、会话未接线"）→ **说明成立、上位未写明这一分层**；② `memory.md` L92-98 又给第三套说法「负责人裁决 §18.1（原宪章条款，已废止；现行 = `ASTROCS_DESIGN.md` §5.3 八投影）冻结四投影为 **TAN+SIN+CAR+AIT**（旧 `SIN/ZEA/CAR/AIT` 表述有误）」——**memory.md 把上位八投影的节号也写成 §5.3（同错）并另立"四投影冻结"口径**，与本份"八投影 + v3 已实现 4/8"不一致（4 个的具体集合是否 = TAN+SIN+CAR+AIT 无法从本份确认，本份说 STG/MOL/CEA/ZEA 归 P3-001 暗示已实现的另 4 个是 TAN/SIN/CAR/AIT，巧合一致，但无一处明写）→ **八投影的"已实现 4 个"到底是哪 4 个，三份文档无一处直接列出**（缺口）；③ **BUNIT 正本分裂**：本份 L28 写「单位口径唯一权威 = `docs/contracts/DATA_SEMANTICS.md` §31.1a」，L192 又逐字引 IVOA REC 规范；`PHASE2_UPM.md` L32、`INTEGRATION.md` L31、`REJECTION.md` L32 三份各写「量纲依据 = 写盘 BUNIT 冻结集 {ADU/sr, ADU²/sr², sr²/ADU²}，裸 ADU 判红」并同引 `lib/infrastructure/aio/src/hiss_writer.cpp:335-365`（该文件**实存**，L335-340 即该冻结集判定；但**同目录另有 `src/hips/aio_hips_writer.cpp`，上位 §11 L207 指后者**）→ **BUNIT 冻结集 = 四份 science + 一份合同共五个引用点，且两个写入器谁承载该规则未在上位消歧**；④ **§16 表 L227 的"未接线"与上位 §6.1/§6.2 冲突**：上位 §6.2 流程把「反向映射 + 重采样（方差传播）」「流式 FITS 输出」画成现行管线节点，本份 §16 C4 实测「会话面未接线（`lib/phase3_session/p3_session.cpp:166-172,396` 只透传 BUNIT、不做语义判定；`p3_v6_export.cpp` 未进构建）」、C6 登记「Phase2 `signal` 量纲…与 §5.3 一致」→ **上位画的是现行状态、本份登记的是未完成状态** → 属 AGENTS §6「不用 facade 冒充实现完成」的对照面，须由上位或 `docs/owner/RELEASE_STATUS.md` 统一状态口径 |
| 重复或重叠对象 | ① 与 `docs/algorithms/PHASE3_PROJ_IMPL.md`：本份 §16 L222/L236 两次明写「实现侧完整清单与归属见 `docs/algorithms/PHASE3_PROJ_IMPL.md` §16（**同一实验单元，避免两套文字**）」→ **正确的让渡示范**（让渡方向、且互指）。② 与 `docs/design/PHASE3_DETAILED_DESIGN.md`：流程与 HDU 结构两份。③ 与 `docs/science/DRIZZLE.md` §5 语义固定：`ADU/sr` 面亮度 vs flux-per-pixel 的两份声明（本份 §3/§9a-8）。④ **与 `docs/science/UNCERTAINTY_AND_COVARIANCE.md` §Phase3 重采样方差传播**：本份 §9a-10 把 variance/ivar 消费的正本让给 UNCERTAINTY（L147「唯一权威 = `docs/science/UNCERTAINTY_AND_COVARIANCE.md`（Phase3 节，DATA-P3-UNC-001）+ `docs/contracts/DATA_SEMANTICS.md` §30.4」），UNCERTAINTY L124-125 反向写「SCI-P3 §9a-10 的 variance/ivar 拒绝语义的**权威 = 本节 + DATA_SEMANTICS §30.4**」→ **两份互指为"唯一权威"、形成正本循环（第三例，前两例是 G3 权重与 REJECTION 档位表）**。⑤ Paper I/II 的 CRPIX/CRVAL/LONPOLE 语义：本份 §3a/§9a-11/§14 第 4 条三处 + `docs/science/ASTROMETRY.md` 一处 → **同一外部规范语义四份文字**（差异：本份给 LONPOLE 现行口径 `φ0/φ0+180°` 并逐字引官方勘误件，ASTROMETRY 给 SIP/WCS 一般约定）。 |
| 处置建议 | **保留（主题正本）+ 六项**：① **把「控制包 13/01」的 5 处冻结依据改为具名仓内文档**（`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md` 或本份自身节号），因 CONTROL_PACK_SPEC §9 规定控制包收口即移除 → **正式合同的依据不能挂在会被删除的工作包上**（本批最严重的结构性悬空）；② 5 条 `run/SCI-FIX-PHOTFIT-01` 证据迁 `artifacts/evidence/`；③ L223 的 sha256 改"判据锚登记面 = `artifacts/evidence/…`"；④ 修 §5.3 → §6.3 的两处节号错，并在上位 §6.3 补一句"registry 面与会话面分层"；⑤ 明确列出"八投影中已实现的是哪 4 个"（上位或本份任一处）；⑥ §16 未接线表迁 `docs/algorithms/PHASE3_PROJ_IMPL.md`（本份已互指，删表留指针）；⑦ `hiss_writer.cpp` 错名与 BUNIT 冻结集的四份复述合一（正本 = `docs/contracts/DATA_SEMANTICS.md §31.1a`，其余改指针）；⑧ 与 UNCERTAINTY 的正本循环打破（建议：语义归 UNCERTAINTY、Phase3 输入拒绝归本份，各留一句互指） |

<!-- PROGRESS: 26/26 台账条目完成，进入汇总小节 -->

## 2. 同主题组与差异（重复不许凭印象：每条都给两处实际段落与差异）

### G1 科学证据的两套"三类"分类 —— **最高设计自身把两套混为一谈**

| 位置 | 原文枚举 | 项数与性质 |
|---|---|---|
| `ASTROCS_DESIGN.md` §12.1（节题「科学正确性与**三重佐证**」）L726-729 | 「每个科学结论具备**三类独立一手证据**，三者齐备：1. **论文/标准**…2. **开源库代码**…3. **仓内实测**…」 | 证据**来源**三类（**不是**数据） |
| `ASTROCS_DESIGN.md` §12.2（节题「**三类实验数据**」）L734-738 | 「1. **哈勃仿真成像数据**…2. **纯解析代数合成数据**…3. **testdata 真实数据**…」 | 数据**类别**三类（**不是**来源） |
| **`ASTROCS_DESIGN.md` §2.2「证据要求（正向约束）」L183-185** | 「本节的每条规范性论断必须按 **§12.2 的三类数据**取得证据——**一手文献/标准**、**哈勃真实数据前向合成的仿真帧**、**纯解析代数合成数据（含"真值无效应⇒度量归零"负例）**，并经 §12.3 的独立审稿；三类齐备是该论断进入生产的前提。」 | **把 §12.1 的第 1 项（一手文献/标准）塞进"§12.2 的三类数据"名下，并丢掉 §12.2 的第 3 项（testdata 真实数据）** |
| `AGENTS.md` §5 L109 | 「实验数据三类齐备：HST 真实信号模板 + 完整物理前向仿真、纯解析代数合成（含"真值无效应⇒归零"负例）、**testdata 真实数据**（最高设计 §12.2）」 | 与 §12.2 一致（**正确**） |
| `ACCEPTANCE_SPEC.md` §3 L86 | 「**三类实验数据（最高设计 §12.2）**」+ L88-90 三条与 §12.2 逐条同 | **正确** |
| `ACCEPTANCE_SPEC.md` §2 L33 | 「佐证来源按 **§12.1** 的三类独立证据要求：论文/标准给 DOI 或 arXiv 号…、开源库给项目+版本+文件:行、仓内实测给复现命令与输出」 | **正确**（区分了两套） |
| `工程控制/…/standards/02_科学证据分级与三重佐证标准.md` §1 | 「每个进入生产数值路径的科学断言，其证据必须具备**三类彼此独立的一手佐证**：① 原始论文/标准 ② 公开可核验的成熟实现 ③ 本项目自己做的独立实验（含解析/合成/真实数据三类实验数据中的适用项）」 | **唯一把两套正确嵌套的表述**（③ 内含三类数据） |
| `docs/science/PHASE2_UPM.md` §14 第 4 条 L307 | 「`k_corr=1.4`（MC 实测 1.3883…）：**项目自产 MC 证据**…**非外部文献**」 | 只给 ③ 类，① ② 缺 |
| `docs/science/NOISE_MODEL.md` §5c L244-246 | 「本节的每一项与 `g` 的获取方式**必须**逐项标注一手文献出处与适用域…**未建立文献锚的项取值面 = 空（生产数值路径只收已建锚的项）**」 | 只讲 ①，未讲 ②③ |

**差异与后果**：§12.1 的"三重佐证"与 §12.2 的"三类实验数据"是两个正交轴（来源 × 数据），但**上位 §2.2 用 §12.2 的编号列了 §12.1 的内容**，且 §2.1/§2.3 的创新点条款里没有等价条款、`docs/science/` 17 份里只有 3 份（PHASE2_UPM、NOISE_MODEL、PHOTOMETRY 部分）提到证据要求。**结论：创新点二的"进入生产的前提"按上位字面读 = 只要三类数据（且第三类被写成"纯解析合成"）；按 §12.1/AGENTS/ACCEPTANCE/标准 02 读 = 还必须有论文与开源对照**。两套读法互斥，直接决定"一条科学论断能不能进生产"的门。**处置**：以标准 02 §1 的嵌套表述为准，把 §2.2 L183-185 改为「按 §12.1 三重佐证 + §12.2 三类数据」并删错位枚举；同步让 `docs/science/` 每份在 §14/§14a 用同一分类标注缺哪一类（现只有 2 份有"核验状态"字段）。

### G2 四层验收（L1–L4）—— 最完整的一对重复，**两份互相指对方为唯一正本**

| 对照项 | `ACCEPTANCE_SPEC.md` §1（L18-25） | `ASTROCS_DESIGN.md` §12.4（L777-784） | 差异 |
|---|---|---|---|
| 表列 | 层 / **目标** / **数据** / **主要执行方**（4 列） | 层 / **核心判据**（2 列） | 列集合不同 |
| L1 数据 | 「三类实验数据（§2.2），真值已知」 | — | DESIGN 无数据列 |
| L1 判据 | — | 「Oracle 对拍、科学不变量、精度、并行确定性全绿；每个度量有"真值无效应⇒归零"负例」 | ACCEPTANCE 无判据列（判据在 §3 表） |
| L3 目标 | 「可行性 + 科学性 + 性能」，数据「**从 `testdata/` 抽取小批量帧（每个滤镜组少量亮场 + 对应 bias/dark/flat）**，覆盖至少两台望远镜/两种滤镜」 | 名「**小批量端到端**」，数据「**三命令串行跑通**，科学性/性能/原子性抽检合格」（数据列写「Linux 真实数据流终验→Windows 复验」的层序，不在表内） | 同名"L3"、内涵一致，措辞与粒度不同 |
| L4 目标 | 「视觉正确性 + 发布决策」，数据「`testdata/` 的 M42、Galaxy Center **全量（本版仅 R 通道）**」，执行方「agent 初审 + 负责人目检」 | 「真实视觉验收」，判据「整马赛克→平面 FITS→拉伸 PNG→**切块目检：无黑洞、无亮斑、无接缝、星点正常、全局观感正常**；该层由负责人目检判定」 | **DESIGN 复述了 L4 的视觉清单 5 项，而 ACCEPTANCE §6.2 有 6 项**（多出"背景与几何"）→ **清单不一致**（DESIGN 少"背景与几何/WCS 网格吻合"一项） |
| 层序图 | mermaid：`L1 --> L2 --> L3 --> L4 --> REL` + 回退边 | mermaid：`U→M→C→P→S→R→F→I` 八节点（单元级层序，与 L1-L4 是**第三套层级**） | 同一节里两张不同抽象的图，无说明其对应 |
| 正本声明 | L31「实验单元目录结构与报告八要素的**唯一正本 = 最高设计 §12.3**」；L208「工作盘回收与预留口径 = AGENTS.md §3（运营事实源）」 | L784「各层的**数据、执行方、证据落位与逐项判据的唯一正本 = `ACCEPTANCE_SPEC.md` §1**（本节只给层序与核心判据结论，不复制数据与执行方清单）」 | **上位已声明让渡**，但 DESIGN §12.4 的表仍写了"数据"列的内容（`L3 小批量端到端`/`L4 真实视觉验收`两行）并复述 5 项视觉清单 → **让渡声明与实际复述并存**；另 `工程控制/…/standards/01` §2/§4 未指定 L1-L4 主题的正本层 → 按标准 01 §2「同一主题只有一份正本」，**须二选一**（建议：DESIGN 只留层序 + 指向 ACCEPTANCE §1；判据/清单全在 ACCEPTANCE） |

### G3 Phase2 科学权重的来源 —— **同一份文档两处"唯一口径"互斥，其中一处与上位相反**

四处表述：

1. `ASTROCS_DESIGN.md` §2.2 L156-158：「阶段二**必须**由它定权 `w(x,y) = SNR(x,y)²/F_ref² = 1/σ_F(x,y)²`（**分母取含源光子散粒项的逐像素总方差**）；**以背景方差倒数定权会使权重不含源光子散粒项、把亮源像素过权**。」
2. `ASTROCS_DESIGN.md` §3.1 L227：「进入科学叠加权重的量**只有**由 SNR 换算的逆方差。」
3. `docs/science/CONTROL_WEIGHT_SNR.md` §8c L229-235：「**定权路径（唯一，无分支）**：所有重建路径产出的稠密 SNR 场都走同一式 `w = SNR²/F_ref² ≡ 1/σ_F²`」— 与 1、2 同。
4. **同文件 §1 L29 与 §5 L159-161**：「Phase2 集成的权重恒为**逐样本 `ivar` 逆方差**，无 fallback」+「**唯一权重口径**：Phase2 集成权重 = 逐样本 `ivar` 逆方差…见 `SCI-UPM §5:54`、`DATA-UNC-001 §51`、`DESIGN §4.3/§4.4`」— **与 1 的括号禁令直接相反**（若 `ivar` = 背景方差面倒数）。

`ivar` 的正本定义在 `docs/science/NOISE_MODEL.md`：§5 L9「估计校准后**空背景随机分量**的逐像素方差 `variance` 及其倒数 `ivar=1/variance`」、§5c L186-188「`sigma_w²` 与 §5 的 `variance` 是两个对象…**各自独立、取值互不代用**」、§10 L332「把 `snr_noise_gain_variance` 一类解析式融合进背景方差面…**加权方差面（§5c）按该节口径必须含源项**」。→ **按 NOISE_MODEL 的正本定义，"逐样本 ivar" = 背景方差面倒数，正是上位 §2.2 明令禁止的定权方式。**

同族第四份：`docs/science/UNCERTAINTY_AND_COVARIANCE.md` L59「马赛克加权积分（**SCI-INT §5，w_i=逐样本 ivar**，…无 fallback）…直接特例（w_i=1/v_i）」；而 `docs/science/INTEGRATION.md` §5 L50-70 的聚合式是 `signal = vs/wsum`、`weights[i]` 只写「数值权重（可空=等权 1.0）」、§9 L110 明写「**不编码 ivar 语义**，数值权重由调用方先经 `p2_validate_candidate_weights` 校验」、§10 L117 把「在本层引入 ivar/SNR 策略」列为不可接受变化 → **UNCERTAINTY 把口径挂在一个自称不承载该口径的正本上**。

**支持 4 的一方还有三条旁证**：`ASTROCS_DESIGN.md` §5.4 L409（UPM 采样点权重取 control_ivar，并解释"被估量是背景电平，SNR² 在该处不是有效逆方差代理"）、`docs/plugins/algorithms_phase2/10_sampling.md`、`docs/science/PHASE2_UPM.md` §5（`w_UPM = quality × control_reliability × control_ivar`，L73）。→ **"ivar"一词在链上至少承载三个不同对象**：`variance_bg`（NOISE_MODEL §5）、`sigma_w²`（NOISE_MODEL §5c）、`control_variance`（PHASE2_UPM §5）。三处未具名区分，`CONTROL_WEIGHT_SNR` §5 与 `UNCERTAINTY` L59 用的是裸"逐样本 ivar"。

**处置**：本组属标准 01 §1 与 AGENTS §10 的「科学定义有歧义/两篇权威文档打架」，**必须上呈负责人裁决**；裁决前两处「唯一权重口径」不得作为任何实现或论文主张的依据。同时修 `DATA-UNC-001 §51`（`DATA_SEMANTICS.md` 无 §51）、`DESIGN §4.3/§4.4`（两节内 `权重` 出现 0 次，正确是 §5.3/§3.1）、`SCI-UPM §5:54`（行号已漂出 §5，见 §3 的 A 组）。

### G4 接缝门槛 `1e-2` 与"默认参数" —— 三份、四套依据

| 项 | 位置 | 表述 |
|---|---|---|
| 门限值 | `ASTROCS_DESIGN.md` §5.4 L412 | 「门槛数值 `1e-2` 的推导与实测标定正本 = `docs/science/PHASE2_UPM.md` §17」 |
| 门限值 | `ACCEPTANCE_SPEC.md` §6.2 L233 | 「门 `max\|rel_step\| ≤ 1e-2`…**判据取值与默认参数的唯一数值来源 = 机器门实现 `eng/tools/e2e/seam_footprint.py`**（`d` 默认 2 px、`ctrl_shift` 默认 200 px、`min_samples` 默认 20）」 |
| 门限值 | `docs/science/PHASE2_UPM.md` §9a L296 | 「**该门槛的推导与实测标定正本 = 本文件 §17**（本节**不再指向别处**索取依据）」 |
| **第三种参数表述** | 同文件 §17 L401 | 「`N_e` = 该边界折线上的有效采样数（**默认 256 点**；M42 实测中位 256、最小 252）」 |
| 推导结论 | 同文件 §17.4 L478-487 | 「三条独立标定一致给出 **`1e-2 ≈ 3.5 σ`**…该值最初来自 SCI-C 实验单元的「相对接缝度量 < 1%」**要求**（`实验/additive-sky-seamless/README.md` 的 R5 行；该处**实测值** 3.9e-4）…**不是**由误差预算反解出来的阈值…**本节结果只作事后标定**」 |

差异与后果：**"默认参数"有两个候选正本**（`seam_footprint.py` vs PHASE2_UPM §17），且 §17 的 `256 点` 与 §9a/ACCEPTANCE 的 `min_samples=20` 是两个不同的量（前者是边长采样点数、后者是适用域门槛），文档未区分即并列出现；**门限的性质**被 §17.4 明确写成"事后标定、不构成已按误差预算设计的依据"，而上位 §5.4 与 ACCEPTANCE §6.2 都把它当"已推导的正本"引用 → **上位引用了一节自认"不是推导"的推导节**。另 `docs/ci/01_CHECKS.md` 也被 ACCEPTANCE L233 称为"阈值口径与运行方式"所在（第四处）。
**处置**：① 在 ACCEPTANCE §6.2 与上位 §5.4 各加"参数正本 = 门实现、门限性质 = 事后标定"一句；② 统一 `256 点` 与 `min_samples` 的具名区分；③ 修 `PHASE2_UPM §7 → §5`（ACCEPTANCE L233 的同名异域引用节号错）。

### G5 HiPS/WCS/FITS 外部规范的语义 —— 五个引用点、三种版本标识

| 规范 | 引用处 | 写法 |
|---|---|---|
| IVOA HiPS 1.0 | `docs/science/UNIFIED_SCIENCE_MODEL.md` L85（经 §7 引 DATA-002）与 `docs/science/PHASE3_HIPS_TO_FITS.md` L44/L46/L91/L192/L230 | **同一文档被引作两个版本**：`PR-HiPS-1.0-20161122`（Proposed，L177）与 `REC-HiPS-1.0-20170519`（Recommendation，L192）；L192 已自我说明「规范文档版本是 1.0，而 `hips_version` 的值是 **1.4**」 |
| HiPS 包围盒 TRIM | `ASTROCS_DESIGN.md` §10 L691 | 「**HiPS 2.0 工作草案** §4.3.2，`TRIM1/TRIM2/ONAXIS1/ONAXIS2`」——**全仓唯一的 "HiPS 2.0" 字样**（命令：`grep -rn "HiPS 2.0" --include=*.md .` → 仅 DESIGN + `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`；`docs/standards/STANDARDS_REGISTRY.md` 的登记面只列 HiPS 1.0）→ **一个未登记的规范版本被生产 fail-closed 条款引用** |
| FITS WCS Paper I/II | `ASTROCS_DESIGN.md` 附录 B L825、`docs/science/PHASE3_HIPS_TO_FITS.md` §14 第 4 条 + §14b、`docs/science/ASTROMETRY.md` §14 第 1-2 条 + §14a、`docs/references/SCIENTIFIC_REFERENCES.md` §D | 同一标准四处；**只有 PHASE3 给"LONPOLE 现行口径 = `φ0/φ0+180°`"并引官方勘误件逐字**，ASTROMETRY §14 只到"文章级定位，TAN/SIP 公式号未逐式核验" → **详度差一个量级，两份都自称科学 claim 已落实到节/式** |
| HEALPix | `ASTROCS_DESIGN.md` §2.4 L206/211（Górski 2005 §5.3、§4）、`docs/science/DRIZZLE.md` §14 第 3 条、`docs/algorithms/HEALPIX_MAPPING.md` | 上位引 **§5.3 与 §4 两个节号**且断言"HEALPix 像素边界不是大圆"；下位引 **§5 式(23)(24)** 逐字；`docs/references/` 的登记需另批核 |
| FITS Standard | `docs/science/CALIBRATION.md` §14 第 8 条（4.0 §4.4.2.5 + 3.0 同节页码）与 `ASTROCS_DESIGN.md` 附录 B L825（只写 "FITS Standard"） | 上位无版本号、下位有版本号与页码 → 版本锚只在下位 |

### G6 "当前做不到"的两条登记通道完全不相交

`docs/KNOWN_LIMITATIONS.md`（157 行、A–E 五节、46 条编号条目，上游标 `ASTROCS_DESIGN.md §1.3`）是仓内的限制台账；`docs/standards/DOCUMENTATION_STANDARD.md` L13 把它与 `README.md`、`docs/owner/RELEASE_STATUS.md` 并列为「**L0 项目入口**」。本批 17 份 `docs/science/` 则各自在正文写"适用域/失效条件/诚实边界/未建模项"。两条通道的核对结果：

| 实测 | 命令 | 结果 |
|---|---|---|
| science 层**一次都没有**引用限制台账 | `grep -l "KNOWN_LIMITATIONS" docs/science/*.md \| wc -l` | **0** |
| 台账里**没有一条**来自本批科学文档的已声明缺口 | `grep -c "14.2"`／`"母版方差"`／`"k_corr"`／`"GAIN"`／`"γ"`／`"Baader"` 于 `docs/KNOWN_LIMITATIONS.md` | **各 0** |

| 缺口（在 science 正文已声明"做不到/前提不成立"） | 出处 | 台账 |
|---|---|---|
| 延展发射域源项只捕获 **14.2%**、逐像素相对误差中位 0.81，该域**必须**声明模型不适用 | `docs/science/CONTROL_WEIGHT_SNR.md` §2b L108 | 未登记 |
| UPM `k_corr=1.4` 的标定域 [300,600]″/px vs 生产 0.98902″/px ⇒ **生产用未在本尺度标定的冻结常数** | `docs/science/PHASE2_UPM.md` §4 L49-60 | 未登记 |
| 局部平稳前提在真实域被违反（γ = 1.983，262 144 patch）⇒ 噪声场应显式降级 | `docs/science/SCIENCE_SCOPE.md` §假设 L51-56 | 未登记 |
| 正式数据面无 `GAIN`/`RDNOISE` 关键字（M42 124 键全缺）⇒ 加权方差面走自估主路径 | `docs/science/NOISE_MODEL.md` §5c L239-242 | 未登记 |
| 母版方差不进 `cal`，本份自称「登记 **UNRESOLVED**…定论面 = 待裁决项登记」 | `docs/science/CALIBRATION.md` §14a L468-481 | 未登记（且"UNRESOLVED"不在 §12.5 词表内） |
| 生产主场景（宽带 `Baader R`）的绝对刻度误差**未测量** | `docs/science/PHOTOMETRY.md` §16.5 第 8 条 L410-411 | 未登记 |
| Phase3 会话面未接线、132 份产品缺 `BUNIT` | `docs/science/PHASE3_HIPS_TO_FITS.md` §16 C4/C5 | 未登记 |
| 背景方差面与加权方差面"取值互不代用"，但 `variance` 产品面就是背景方差面 ⇒ 产品侧无总方差面 | `docs/science/NOISE_MODEL.md` §1/§5c | 未登记 |

**结论（可执行判据）**：同一主题"现在做不到什么"存在**两个互不引用的登记面**——科学文档内的"诚实边界"段与 L0 限制台账。按标准 01 §3「每份文档、每个机制都能追溯到最高设计的一条要点」与 §2「同一主题只有一份正本」，**限制的主题应有唯一登记面**；本批 8 条已声明缺口 0 条进入台账。**处置**：① `docs/science/` 各份的"失效条件/未建模项/适用域边界"逐条上收到 `docs/KNOWN_LIMITATIONS.md` 并在科学文档留指针；② 给限制台账补一条机器门：`docs/science/**` 出现「未建模/未测量/不适用/前提…不成立/未冻结」等声明时必须在台账有对应条目（可由 `eng/tools/doccheck/` 承担，属 DA-05/门禁批范围，本批只登记）。

### G7 科学文档末尾的"参考代码库（含许可证）"块 —— 16/17 份重复，且内容三份一套

命令与实测：`grep -l "仅对照不复制 GPL 代码" docs/science/*.md | wc -l` → **16/17**；唯一没有该块的是 `docs/science/SCIENCE_SCOPE.md`（它在 §参考文献 L114 用另一种排版写了同一批库与许可证 → **实为 17/17 承载同一事实**）。

**同一块的两个变体**（差异是内容，不只是排版）：`grep -l "WCSLIB（LGPL-3.0）/ CFITSIO" docs/science/*.md` → **6 份含 WCSLIB+CFITSIO 行**：`ACR_EQUIVALENCE`、`DRIZZLE`、`INTEGRATION`、`PHASE2_UPM`、`PHASE3_HIPS_TO_FITS`、`REJECTION`；其余 **10 份不含该行**：`ASTROMETRY`、`CALIBRATION`、`CONTROL_WEIGHT_SNR`、`NOISE_MODEL`、`PHOTOMETRY`、`PSF`、`PSF_SIGNAL_WEIGHT`、`STAR_DETECTION`、`UNCERTAINTY_AND_COVARIANCE`、`UNIFIED_SCIENCE_MODEL`（+ SCIENCE_SCOPE 的变体写法）。

→ **同一事实（哪些第三方库、什么许可证）在 `docs/science/` 内有 17 份副本、两套内容**，而按 `docs/standards/LICENSE_COMPLIANCE.md` 与 `docs/references/SCIENTIFIC_REFERENCES.md §M「参考代码库（含许可证）」`（该节确存，L126）才是登记面。**违反**：标准 01 §2「同一主题只有一份正本」「研究与佐证类以引用方式服务正本，不重复承载正本口径」+ §4「细节各归其层」。**处置**：17 份的该块全部改为一行指针（`许可证与参考实现对照见 docs/standards/LICENSE_COMPLIANCE.md 与 docs/references/SCIENTIFIC_REFERENCES.md §M`），把 §14a 的**科学相关**部分（如 NOISE_MODEL §14a 的 SWarp/SExtractor/SEP/photutils 逐式对拍与适用域）**保留**在 science 层——那部分是判据不是清单。

## 3. 索引与链接缺陷清单

### A. 悬空 / 错锚（逐条，含复算命令）

| # | 出处 | 锚 | 判定 | 复算命令 |
|---|---|---|---|---|
| A-1 | `docs/science/NOISE_MODEL.md` §5a L108 | `reports/PROJECT-GOVERNANCE-01/research/MASK-001_掩膜语义重新推导.md §3.3/§3.4/§5.3` | **真悬空（目录不存在）**；同名的文件实际位于 `artifacts/evidence/governance-01/research/MASK-001_掩膜语义重新推导.md`，且**该文件第 648 行也自我引用同一个死路径** | `git ls-files reports`（→ 3 个 v19r2 文件）；`git ls-files \| grep -c "reports/PROJECT-GOVERNANCE-01"` → 0；`git ls-files \| grep -i MASK-001` → `artifacts/evidence/…` |
| A-2 | `memory.md` L7、L74 | `docs/archive/history/memory_V18R2-V19_operational_log_2026-08-21.md` | **真悬空（整棵 `docs/archive/` 不存在）**，而该文件是 L3-8 宣称的"历史已整体归档"的唯一去处 → **归档指向不存在的目录 = 历史既不在文档也不在归档** | `git ls-files "docs/archive/**"` → 空 |
| A-3 | `memory.md` L38 | `docs/governance/VERSION_NAMESPACES.md` | **真悬空（`docs/governance/` 不存在）**；正本是 `docs/VERSIONING.md`（实存，`git ls-files docs/VERSIONING.md` → 1） | `git ls-files docs/governance \| wc -l` → 0 |
| A-4 | `memory.md` L109 | `工程控制/PROJECT-GOVERNANCE-01/TASK_LIST.md`、`ACCEPTANCE.md`（自称"**现行控制包状态唯一源**"） | **真悬空**；现存控制包只有 `工程控制/RELEASE-05/` | `git ls-files 工程控制 \| sed 's#\(/[^/]*\)/.*#\1#' \| sort -u` → RELEASE-05、evidence |
| A-5 | `memory.md` L106、L107、L108（×2） | `run/local/execution_mode/CONTROL_PACK_EXECUTION_MODEL.md`、`run/local/HANDOFF_CONTROL_PACK_20260909.md`（后者被称"**新对话必读**"） | **真悬空（`run/` 不入库）**，且被文档当作跨会话必读入口 | `git ls-files "run/**"` → 0 |
| A-6 | `memory.md` L50-66、L78-85、L94-96、L110 | `lib/core/src/module_adapters.cpp:4257/:4282/:4309`、`lib/phase3_proj/{p3_projection.cpp,module.yaml}`、`lib/drizzle/{module.yaml,src/module_entry.cpp}`、`cli/runtime_client.cpp`、`lib/algorithms/upm/module_adapters.cpp` | **路径级错（真悬空）**；实际分别是 `lib/infrastructure/scheduler/src/module_adapters.cpp`（L6355-6377 亦为 SCIENCE_SCOPE 所引）、`lib/algorithms/projection/p3_projection.cpp`、`lib/algorithms/drizzle/…` | `git ls-files \| grep -E "module_adapters\|p3_projection"` |
| A-7 | `docs/science/PHASE3_HIPS_TO_FITS.md` L9/L127/L157/L164/L172/L217（5 处） | 「**控制包 13** §2/§3/§4/§5」「控制包 01 §3」「`eng/tools/validation/phase3`」 | **真悬空**：`工程控制/` 只剩 `RELEASE-05`+`evidence`，无"13"；`git ls-files eng/tools/validation \| wc -l` → 0。这些是 §9a 十二项冻结定义与 §11 Oracle 全集的**依据出处** | 同上 |
| A-8 | `docs/science/CALIBRATION.md` §6a L230 | 「`ASTROCS_DESIGN.md` **§4.3:254**」与「**§4.5:300**」 | **两个"§节:行号"锚都指错节**：上位母版标度红线/曝光容差在 **§4.3 L308**、warn 语义在 **§4.5 L354**（L254 ∈ §4.2，L300 ∈ §4.3） | `grep -n "^### 4.3\|^### 4.5" ASTROCS_DESIGN.md` → 278/332 |
| A-9 | `docs/science/CONTROL_WEIGHT_SNR.md` §4 L128、§5 L160 | 「**DATA-UNC-001 §51**」 | **真悬空（节号不存在）**：`docs/contracts/DATA_SEMANTICS.md` 共 32 个 `##` 节、最大 §31；DATA-UNC-001 在 **§30**（L2699） | `grep -c "^## " docs/contracts/DATA_SEMANTICS.md` → 32；`grep -n "^## 3[01]" …` |
| A-10 | 同上 L160 | 「**DESIGN §4.3/§4.4**」为"Phase2 权重恒为逐样本 ivar"的出处 | **错锚**：§4.3/§4.4 区间内 `权重` 出现 **0 次**；正确是 §5.3 L399、§3.1 L227 | `awk 'NR>=278 && NR<=331' ASTROCS_DESIGN.md \| grep -c 权重` → 0；`grep -n "权重" ASTROCS_DESIGN.md \| head` |
| A-11 | 同上 L128/L160 | 「**SCI-UPM §5:54**」 | **行号锚漂移**：PHASE2_UPM §5 起于 L64，L54 属 §4；`control_ivar` 式在 §5 L76 | `grep -n "^## " docs/science/PHASE2_UPM.md \| sed -n '5,7p'` → 64/123 |
| A-12 | `ACCEPTANCE_SPEC.md` §6.2 L233 | 「`docs/science/PHASE2_UPM.md` **§7** 的 UPM patch 域 `min_samples`」 | **节号错**：该词只在 §5 L87（§7 是"独立不变量"） | `grep -n min_samples docs/science/PHASE2_UPM.md` → 仅 87 |
| A-13 | `docs/science/DRIZZLE.md` §9a L202 | 「`UNCERTAINTY_AND_COVARIANCE.md`」无 §号 | 弱锚：该文件节全无编号 → 只能靠节名 | `grep -c "^## [0-9]" docs/science/UNCERTAINTY_AND_COVARIANCE.md` → 0 |
| A-14 | `FATDUCK_ACCESS.md` L42-43 | 「见**审计 §14** 性能实测」 | **真悬空**：未给文件名，全仓无法定位 §14 | `grep -rn "见审计" *.md` |
| A-15 | 4 份 science（`CONTROL_WEIGHT_SNR` §2/§5/§6/§8/§9、`UNCERTAINTY` L107/L148、`SCIENCE_SCOPE` L53/L81、`STAR_DETECTION` L144/L155）+ `ASTROMETRY` L222 | `stage2.cpp`、`sampler.cpp`、`snr_estimator.h`、`noise_model.cpp`、`module_adapters.cpp`、`p3_rsmp_propagation.cpp`、`p3_rsmp_covariance.cpp`、`lib/phase3_session/p3_v6_export.h`、`PHASE2_SAMPLER.md`、`run/LINDEF-CLOSE-01/…` | **无路径锚**（多数文件实存但需猜目录；`p3_rsmp_propagation.cpp` 实际在 `lib/algorithms/resample/` 而非 `…/src/`；`lib/phase3_session/p3_v6_export.h` 与 `lib/algorithms/upm/memory.md` 需逐个核） | `git ls-files \| grep -E "p3_rsmp_\|snr_estimator.h"` |

### B. `run/` 临时区被当正式文档证据锚（跨 8 份的同一形态）

`ASTROCS_DESIGN.md`/根文档：**0 处**（干净）。`docs/science/`：**8 份、共 41 处**，逐份计数命令与结果：

```bash
for f in docs/science/*.md; do n=$(grep -c 'run/' "$f"); [ "$n" -gt 0 ] && printf "%-42s %s\n" "$f" "$n"; done
docs/science/ASTROMETRY.md               1
docs/science/CALIBRATION.md              5
docs/science/PHASE2_UPM.md               9
docs/science/PHASE3_HIPS_TO_FITS.md      4
docs/science/PHOTOMETRY.md               9
docs/science/PSF.md                      7
docs/science/REJECTION.md                4
docs/science/SCIENCE_SCOPE.md            2
```

**性质**：`run/` 在 `.gitignore`，`git ls-files "run/**"` → 0 命中；这些路径是"某次任务跑出来的 JSON/TXT 报告"，一旦被文档引为**判据依据或唯一正本**（如 PHASE2_UPM §17、REJECTION §17、PSF §9 三处最重），**正式科学文档的可核验性即归零**。标准 01 §2 末条给出去向：**需要留证的台账落 `artifacts/evidence/` 并由正本引用**。`grep -c "artifacts/evidence" docs/science/*.md` → **17 份全部为 0**（本批一次都没指向正确的证据区）。

### C. `docs/DOCUMENT_INDEX.yaml` 与本批 26 份的核对

**登记完整性**：`grep -c 'path: "<file>"' docs/DOCUMENT_INDEX.yaml` 对 26 份逐条 = 1 → **本批 26 份全部登记、无重复、无"登记但不存在"**。全索引共 **265** 条 `path:` 条目，`registered-but-missing` = **0**，重复路径 = 0（复算：`python` 解析 `- path: "…"` 与跟踪集求差）。
**但全 `docs/` 侧发现 1 份未登记**（本批用同一条命令扫出，文件属 DA-02/03 域，登记于此供汇编）：`docs/research/CCD_LINEAR_DEFECT_LITERATURE.md` —— `docs/**.md` 跟踪 233 份、索引登记 232 份。**索引头注 L20-21 自称「每份下级文档（docs/**.md）必须在 active 区段登记…覆盖率 100%，机器判红」→ 该自述与文件树不符**（或该文件在 `archived` 区段外被漏登）。
**角色核对**：**索引本身没有 `role` 字段**（字段集 = `path/status/duty/upstream/downstream/notes`）→ 标准 01 §3 要求"登记内容含路径、**角色**、主题、上游、下游" → **索引缺"角色"与"主题"两个必填项**（只有 `duty` 近似主题）。这是全局缺陷，不限于本批。
**上游核对**：`grep -c "ASTROCS_DESIGN" docs/DOCUMENT_INDEX.yaml` 命中 284 处；26 份的 `upstream` 全部非空 → 索引面上满足标准 01 §3；**但索引把两处错锚也登记了**（`docs/science/DRIZZLE.md` 的 upstream 写 `§5.2、§6.3`、`ACR_EQUIVALENCE.md` 写 `§1.3、§4`，与文档正文同一错误 → **索引不是独立校验源，只是复制文档抬头**）。
**下游核对**：`downstream` 为"机器扫描出的活动引用方"抽样文本；本批逐条 grep 结果与索引条目的抽样**不矛盾**，但 `memory.md` 在索引 `duty` 写「开发记忆与**历史轮次台账**（history 命名空间）」+ `status: ACTIVE_INFORMATIVE` → **索引自己承认它是历史台账、却给它在活动层**，与标准 01 §2 末条互斥（处置见 §1.9）。

### D. 孤儿判定（自证）

**本批 26 份无一为孤儿**（每份都有 ≥1 个跟踪文件引用它）。最弱三份与其检索证据（含**正对照**，证明检索式有效）：

```bash
R="F:/Astro dev/Astro CS Normalization Database"
# 正对照（必须有命中）
git -C "$R" grep -l -e "NOISE_MODEL.md" -- "*.md" ":!docs/science/NOISE_MODEL.md" | wc -l   # → 36
# 1) ACR_EQUIVALENCE.md（本批最弱）
git -C "$R" grep -l -e "ACR_EQUIVALENCE.md" -- "*.md" ":!docs/science/ACR_EQUIVALENCE.md"   # → 6：docs/{DOCUMENT_INDEX.yaml,TRACEABILITY.csv,algorithms/PHASE2_MOSAIC_WRITE.md,contracts/INDEX.yaml,owner/SCIENCE_OVERVIEW.md,references/SCIENTIFIC_REFERENCES.md}
# 2) UNIFIED_SCIENCE_MODEL.md
git -C "$R" grep -l -e "UNIFIED_SCIENCE_MODEL.md" -- "*.md" ":!docs/science/UNIFIED_SCIENCE_MODEL.md"   # → 7
# 3) SCIENCE_SCOPE.md
git -C "$R" grep -l -e "SCIENCE_SCOPE.md" -- "*.md" ":!docs/science/SCIENCE_SCOPE.md"                  # → 11
```

**但有一条"引用孤儿"**：`FATDUCK_ACCESS.md` 在 `docs/` 与根文档里的被引面为 **0**（`git grep -l -e "FATDUCK_ACCESS.md" -- "*.md" ":!FATDUCK_ACCESS.md"` → 命中仅 `docs/owner/{ARCHITECTURE,SCIENCE}_OVERVIEW.md` + `docs/DOCUMENT_INDEX.yaml`；根 README/AGENTS 均 0）→ **根级位置对读者不可发现**（见 §1.8）。

### E. 被拆散字符串的检索陷阱实证（本批 2 例，先换检索词再下结论）

1. `ENGINEERING_SPEC.md` §15 L257 写 `x-astrocs\*`（注解键前缀通配）与 L259-265 表格里的 `astrocs.<x>/vN`，若按整串 `"x-astrocs*"` 检索会 **0 命中**；拆成 `x-astrocs` 后命中 49 个跟踪文件（`git grep -l "x-astrocs" | wc -l` → 49）。
2. `docs/science/CALIBRATION.md` L230 的 `ASTROCS_DESIGN.md §4.3:254` 把节号与行号用冒号粘连，按 `"§4.3"` 全文检索不到该条；必须按 `ASTROCS_DESIGN.md.:254` 或直接读行（本审计即以"读该节实际标题"复核）。
→ **本批所有 0 命中结论都用过 ≥2 套检索词并配正对照**（见上 D 组）。

## 4. 覆盖率自报

- **已审 / 应审 = 26 / 26**（`ASTROCS_DESIGN.md`、`AGENTS.md`、`ENGINEERING_SPEC.md`、`ACCEPTANCE_SPEC.md`、`CONTROL_PACK_SPEC.md`、`README.md`、`DEPENDENCIES.md`、`FATDUCK_ACCESS.md`、`memory.md` + `docs/science/` 17 份）。
- **未逐行读的名单：空集**。26 份全部用 Read 工具读完全文（`ASTROCS_DESIGN.md` 826、`docs/science/CALIBRATION.md` 507、`PHASE2_UPM.md` 507? → 505 等，逐份行数见台账）。
- 为判"重复/上下游"而**部分读取**（只读开头、索引节、被指章节）的文件：`docs/standards/DOCUMENTATION_STANDARD.md`、`docs/standards/NUMERIC_STANDARD.md`、`docs/standards/BUILD_STANDARD.md`、`docs/standards/LICENSE_COMPLIANCE.md`、`docs/owner/RELEASE_STATUS.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/plugins/algorithms_phase2/12_rejection.md`、`docs/references/SCIENTIFIC_REFERENCES.md`、`docs/architecture/PERFORMANCE_MODEL.md`、`testdata/README.md`、`docs/DOCUMENT_INDEX.yaml` → **均属其他批次文件域，只作对照不作审计结论**。

## 5. 我证伪了任务书（AUD-101）的哪个前提

1. **「以 `docs/` 为重点（当前约 233 份、30+ 子目录）」→ "30+ 子目录" 为假，实为 23。** `git -C "$R" ls-files docs \| awk -F/ 'NF>2{print $2}' \| sort -u \| wc -l` → 23（正对照：`… \| sort -u` 列出 algorithms…validation 共 23 行）。"233 份" 在 `.md` 口径下成立（`git ls-files docs \| grep -c '\.md$'` → 233），但同目录跟踪文件总数 258，任务书未区分口径。
2. **「步骤 2 让按标准 01 §2 角色表给每份文档标角色」→ 该表不足以覆盖本批 26 份中的 10 份。** 表内无"最高设计"（1 份）、无"项目入口/索引"（1 份：`README.md`）、无"工作手册/过程治理"（3 份：`AGENTS.md`、`CONTROL_PACK_SPEC.md` + `DEPENDENCIES.md` 的构建运营面）、无"记忆/台账层"（1 份：`memory.md`）、无"基础设施接入"（1 份：`FATDUCK_ACCESS.md`），另有 5 份（`ENGINEERING_SPEC`、`ACCEPTANCE_SPEC`、`docs/science/{PHASE2_UPM,PHOTOMETRY,CALIBRATION,REJECTION,DRIZZLE,UNIFIED_SCIENCE_MODEL}` 等）**一份跨多格**——而 §2 明文要求"每份文档属于且仅属于一个角色"。**据此 AUD-102"目标文档架构"不能直接拿 §2 九格当目录职责表**：必须先补三类新格（顶层决策 / 入口索引 / 过程与运营接入）并明确"跨格文档如何裁决"（本批 26 份里 6 份跨格）。
3. **「步骤 4 找孤儿文档（无上游、无引用）」这一类在本批不成立——真孤儿 0 份。** 26 份全部有 ≥1 个跟踪文件引用（命令与结果见 §3 D 组）。**真正的缺陷形态不是孤儿，而是"引用了不该被引用的东西"**：`run/` 临时区 33 处、不存在的控制包 5 处、行号漂移锚、同名两写入器、`reports/` 死目录。**若整治只按"孤儿/悬空"两查，会全部漏掉这一族的真实形态。**
4. **标准 01 §4「正文无…commit」与 §12.1/AGENTS §5 的三重佐证要求互相冲突（不是任务书前提，是判据内部冲突，但它决定本批 16 份能否被判合格）。** `docs/science/ACR_EQUIVALENCE.md`/`CALIBRATION.md`/`REJECTION.md` 等给外部实现锚的写法是"项目 + 版本 + commit + 文件:行"（如 `SExtractor @90296de`、`ccdproc @0c21068`、`iraf main @b80c8df1`、`WPP 包 sha1 712cc7c3…`），**sha1/短 commit 在跟踪文件里的命中面**：`git grep -lE '\b[0-9a-f]{40}\b' -- '*.md' '*.py' | wc -l` → **90 个文件**、`git grep -cE '\b[0-9a-f]{40}\b' -- '*.md' '*.py' | awk -F: '{s+=$2} END{print s}'` → **111 处**（其中 `docs/` 下 46 处）。按标准 01 §4 字面应判红，按 §12.1「开源库代码给项目 + 版本 + 文件:行（并回到其引用的文献核对）」与 AGENTS §5/§8（科学疑义查证要记录"开源项目与版本"）则**必须**带版本锚。**必须消解**：建议 §4 改为「无**本仓** commit / 版本号 / sha256 留痕；外部实现的固定版本或 commit 锚属一手证据，必须保留」——否则整治会把科学证据一起删掉。

## 6. 收工 git 基线（差异原文）

`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`（收工，同目录同命令）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

`git rev-parse --short HEAD` → `c8f64e9a`（未变）。**与开工逐字节相同、差异为空**：本工作包全程未改、未移、未删任何仓库文件，未跑构建/ctest/`run_checks.py`/端到端，未 `import` 任何仓内 Python 模块（辅助扫描脚本全部置于仓库外 `C:\Users\fujia\AppData\Local\Temp\aud01_*.py`），零 git 写。


