# 复核-V402 —— 第②层独立复核：常数判读四分片的结构性结论

## 0 文件头

- **复核对象**：批次清单 `独立审计/批次清单/V402.txt` 所列 20 个复核单元（按节切片），对象文件为
  `独立审计/证据/AUD-402-判读-A1.md` / `-A2.md` / `-A3.md` / `-BD1.md` 的"立案清单""待确认与影响范围""我证伪了机械结论"三节 + 覆盖率自报 + 分母自证。
- **范围收窄声明**：本轮只判四个分片的**结构性结论**（分片自报的计数/覆盖/证伪是否成立、A1 的"七侧"主张、D4 主表三个数的自洽性），**不重做逐常数判读**（354 行已并入 D4 主表，非本批对象）。
- **冻结基线**：仓库 HEAD=`c8f64e9a`，本阶段禁测禁构建。
- **只读纪律遵守情况**：不修改/移动/删除仓库任何文件；零 git 写（仅 `status`/`ls-files`/`grep`/`log`/`show`）；不执行构建、`cmake`、`ctest`、`run_checks.py`、任何 `eng/**` 下脚本；不 `import` 仓库内 Python（计数一律读 CSV/MD 文本）；不写 `整改/`、`独立审计/批次清单/`、`独立审计包/` 与共享台账。
- **开工 `git status --porcelain` 原文**：
  ```
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```
  （= 前言 §1 规定的两行基线，无新增项。）

## 1 复核单元与判定（逐单元一行，判完即改）

## AUD-402-判读-A1::立案 —— 主张：A1 立案 2 条，其中含"同一配置键实际有七个侧、且只有一侧决定生产取值"
**判定：确认（对"七侧 + 门扫不到骨架侧"）／降级（对"只有一侧决定生产取值"的字面表述）**

先盲复算（只取对象给的锚：`session_commands.h:151-318`、`check_config_defaults.py:80/:157/:182-213`、`module_entry.cpp:355`、schema 与 registry 的 pixfrac 条目），再对比 A1 文字。逐侧并列取值（以 `drizzle.pixfrac` 为样本键）：

| 侧 | 载体 `文件:行` = 取值 | 我的取证式 | 是否入运行路径 | 门是否扫到 |
|---|---|---|---|---|
| ① 代码兜底 | `lib/algorithms/drizzle/src/module_entry.cpp:354-355` = 缺键 **1.0**（:356 校验 `!isfinite‖<0.0‖>1.0`） | `git grep -n pixfrac -- lib/algorithms/drizzle/src/module_entry.cpp` | 是 | **否**：`_ASSIGN_RE`(:40-42) 要求 `recv.key = 字面量;` 且 recv 形如 `cfg/config/...`(:44)，:355 是裸局部量 ⇒ 该默认值根本不进 `seen` |
| ② defaults.json | `eng/packaging/config/defaults.json:666-667` = **0.8**，:677 note 自证"默认 0.8…取 1.0 时相邻 drop 仅边界相接，出现无覆盖缝隙" | 同上；生产读取面：`git grep -n "defaults\.json" -- lib` = 14 命中，**逐条读后全为 `//` 注释或 `tests/**`**（唯一读文件的是 `lib/algorithms/noise_snr/tests/p1noise/check_saturation_wiring.py:50`）⇒ "生产代码从不读该文件" **成立** | **否** | 只贡献 leaf 名（`config_key_set` :90-95 只取 `field["key"]` 末段，不取 `value`）⇒ **不比数值** |
| ③ 出厂模板 | `eng/packaging/config/templates/normalize.phase_config.json:11` 与 `:22` = **0.8** | `git grep -n pixfrac -- eng/packaging/config/templates/` | 是（用户复制后运行） | **否——静默失效（F-1 成立）**：`git grep -n "\"config\"\|\"blocks\"" -- templates/` 在三份模板里只命中 `"blocks": [`（各 :3），**零 `"config"` 命中**；而 :80 与 :157 都写 `stack = [doc.get("config") or {}]` ⇒ 模板侧对键集合与数值比较双双贡献 0，门仍走 :103/:148 的非空判据而判绿 |
| ④ CLI 生成骨架 | `lib/infrastructure/cli/session_commands.h:181` = **1.0**（`"{\"nested\": 1, \"pixfrac\": 1.0, …}"`）；同文件 :109 自订"科学默认值不写进模板（唯一家在 defaults.json）"⇒ **自我违约** | `git grep -n pixfrac -- lib/infrastructure/cli/session_commands.h` | 是（`--template` 输出） | **否——A1 的"值以字符串常量嵌在聚合初始化里"经码面复核成立**：`_VALUE_CALL_RE`(:39) 只认 `.value/.get/.get_or/.at("k", 字面量)`，`_ASSIGN_RE` 只认 `x.k = 字面量;`；`strip_comments` 后 :181 是纯字符串字面量，两个式子都不匹配 ⇒ 骨架侧**不在扫描面**（不是"扫到了但被合并"） |
| ⑤ schema | `phase_config_normalize.schema.json:142-147`（`type/exclusiveMinimum=0/maximum=1`，**无 default**）与 `:468-472`（同样无 default，:472 描述仍写"defaults.json 的 drizzle.pixfrac 为 **pending_authority**"） | `git grep -n -A5 "\"pixfrac\": {"`／`-A4 algorithm_drizzle_pixfrac` | 是（校验面） | 只取 `$defs.*_config.properties` 键名(:96-102)，不取 default/值域 ⇒ **A1 少列一处**：无 default 的声明点实为 **两处**（:142-147 与 :468-472），不是一处 |
| ⑥ 合同登记 | `eng/packaging/config/config_registry.json:724` field=pixfrac、**:725 `declared_default="1.0"`**、:728 `registration="defaults_json"`、:729-730 指向 `defaults.json#…drizzle.pixfrac`、:732 note"**默认 1.0 已入 defaults.json**（owner_adjudicated）" | `git grep -n -A4 "\"field\": \"pixfrac\"" -- eng/packaging/config/config_registry.json` | 否 | 由 CHK-CONFIG-* 消费；`git grep -rn "divergence:pixfrac" -- eng/ci/` **零命中** ⇒ pixfrac 无豁免条目，其"恒绿"纯因两侧都扫不到，非登记豁免 |
| ⑦ 构建期注入 | `eng/packaging/config/runtime_resources.json` → 根 `CMakeLists.txt` → `runtime_resources_generated.h.in`（A1 口径，本键不参与） | 未逐跳复算（本样本键不涉此侧） | 是 | 不在本门扫描面 |
| （⑧ 文档符号表，前言 §2 第 6 条要求） | A1 的七侧表**未把文档列为第 8 侧**，而把它放在 §2.4 单独做"出处锚点命中率"核对 | 见 `AUD-402-判读-A1::证伪` 的 X-14 判定 | — | — |

**结论（三问之①）**：
- "**实际有七个侧**"：确认。样本键在六个可锚侧上取值齐备（第⑦侧对该键不适用），A1 的侧集合覆盖"代码兜底／defaults.json／出厂模板／CLI 骨架／schema／合同登记"六项，与前言 §2 第 6 条要求一致；缺"文档符号表"作为一列，但 A1 在 §2.4 以锚点命中率形式另做了同一件事 ⇒ 属**表头口径问题，不改结论**。
- "**门实际比较的是什么**"：**键 leaf 名集合**（:90-95/:96-102 只收名），数值只来自 `scan_defaults()` 的 lib 字面量（:186-193）与模板值（:189-193，**已因 F-1 恒空**）。⇒ "注册值 vs 代码值不一致"这一整类**在现有门下恒绿**，A1 后果 2 **成立**。
- "**本仓已有一次'门只扫三侧、扫不到骨架侧所以门过了仍 fail-open'的实证**"：**我在基线上复核到实证本身**——模板侧（③）与骨架侧（④）双双不在比较面，`--self-test`/`ANCHOR_*` 判据（:77/:103/:148）都只校"锚点在不在"，不校"侧齐不齐"，故该门对 pixfrac 输出绿。
- "**只有一侧决定生产取值**"：**降级为「缺键时只有一侧决定」**。键显式给出时生效值来自被喂进来的那份文档（③/④/用户手写），缺键时才落到①；A1 自己的表已把③④标为"进入运行路径"，所以 headline 表述与表内文字有轻微张力，但 F-2 的落点"同一命令按是否显式给键产出 0.8/1.0 两种产品"与我复算一致 ⇒ 机制成立，措辞收口。
- **我比 A1 多取到的一条**：registry :732 自称"默认 1.0 **已入 defaults.json**"，而 defaults.json:667 实为 0.8 ——登记面的自述与它所指向的那一侧**直接对撞**，比 A1 的"记相反值且不报冲突"更硬（这是合同侧对配置侧的一次可判红陈述，不是缺字段）。

- 与成稿差异：①F-2 的"⑤schema 不给 default"应改为"**两处** $defs 声明点均不给 default"；②headline"只有一侧决定生产取值"宜改为"缺键时只有代码兜底决定，显式给键时由被喂的文档决定"；③F-2 引用的 `module_entry.cpp:355` **未给目录**，同名文件在仓内有 6 个（`git ls-files "*module_entry*"`），按我定位的是 `lib/algorithms/drizzle/src/`；④registry 路径在 §1 表与 F-2 中写成裸 `config_registry.json`，实际在 `eng/packaging/config/`（不是 `eng/contracts/`）——**我第一遍按 `eng/contracts/config_registry.json` 检索得到假零命中**，正是前言 §2 第 4 条警告的"报 0 命中前先在应命中对象上跑通"。
- 影响 D8 的处置：进任务书（F-1 修门 + 骨架侧入扫描面 + registry:725/:732 与 defaults.json:667 对撞三件），**且 ACSD-T 里必须把"侧集合完备性"做成门的自检项**（现在的 `ANCHOR_STALE` 只防"零命中"，防不住"少扫一侧"）；F-2 若判 S1 需补 `normalize` CLI→pipeline→drizzle 模块入口的逐跳 `文件:行`，我本单元预算内未追该链 ⇒ 暂记 S2·缺运行时触发路径。

## AUD-402-判读-A1::待确认 —— 主张：21 条 UNRESOLVED 候选，逐条给缺证与补法
**判定：降级（结构齐备可入册；但逐条"缺哪一条证据"的口径只到"文档层"，未到"运行时触发路径"）**
- 我的独立取证：`wc -l` 校节区边界（§3 起 :106、止 :132，§4 起 :134）；U-1…U-21 共 21 行，与 A1 自报"21 条"相符，三列（项/保守方向/影响范围）无空项。**抽查其中两条的权威面**：U-3 引"REJECTION.md:85 实测级联误判率 11.36%"→ `git grep -n "0\.05" -- docs/science/REJECTION.md` 命中 :85「被 `k=10` 级联抬到 **11.36%**」✓ 逐字对上；U-1/U-19 的"分形态精度"我另在 X-12 条复核（`bitpix = -32` 缺省确实存在于 `lib/algorithms/fits_output/p3_output.cpp:639`）✓。
- 与成稿差异：**清单指针与内容位置不符（前言 §2 第 9 条要求登记）**——批次清单把"待确认"记作 `:106-133`，实际 §3 表体到 :131，:132-133 是"不列入 UNRESOLVED"的 ACR 反选段（这段是**排除清单**，不是候选）；把它算进"21 条候选"的行域会让读者以为候选更多。另：21 条里我只在 U-1/U-3/U-18 三条上取到独立证据，**其余 18 条我判"待证"**：缺的是"该默认值在生产有读取点"这一跳（A1 自己 §4 末行也承认"'生产实际取什么值'一律沿代码路径推导，未运行任何产物"）。补法＝对每条给 `命令入口 → pipeline → 读键点` 的逐跳 `文件:行`（前言 §2 第 5 条），否则按前言只能停在 S2。
- 影响 D8 的处置：进任务书者 = U-18（修门）、U-19（schema 断言 default ∈ 值域 + 使用层禁静默替换）、U-2（variance_floor 派生）、U-1（精度分形态）；其余 17 条按"缺运行时触发路径"进 UNRESOLVED 而非任务书。

## AUD-402-判读-A1::覆盖率 —— 主张：90 行全覆盖、12 列、分母 90 = 三份队列 CSV 各 30 行
**判定：确认（行覆盖与列结构，我自己数）**
- 我的独立取证：不 import 仓库件、不跑其脚本，直接读 CSV 文本计数——`raw/AUD-402-判读-A1.csv` 数据行 **90**、列数 **12**（`csv.reader` 口径，表头除外）；输入侧 `inventory/P402-A-01..03.csv` 与 A1 判读行的 `(符号/键, 位置)` 键**全部对得上**：判读层 354 唯一键 ∩ 机械层 20,595 唯一键 = **354**，判读层 − 机械层 = **0** ⇒ "已判行都出自该队列"成立。§2 各子表条数自校：F-1…F-17 = 17（与"（17 条）"相符）、E-1…E-6 = 6 ✓、O-1…O-6 = 6 ✓。
- 与成稿差异：两点未被我复算到，按**待证**留痕：① §2.4 的锚点核对读数（`fields=59`、DIFF=21、NO-VALUE=46、HIT=5、**"59 条中只有 4 条真锚 = 6.8%"**）——这是 A1 自有脚本 `复算/q402-A1/anchor_check.py` 的输出，我未重跑（前言 §1 只禁 `eng/**`，但该件在 `独立审计/复算件/` 下且需读 59 条 `source`/`source_ref` 双锚再回读文档行，成本超出本单元 8 条命令上限）；**这一数直接决定 D4 主表"出处"列 48 行"文献值"的可信度**，建议 D4 侧独立复算而非引用 A1。② 输入侧 59 条 `fields` 的我方计数未成（`git grep -c "\"key\":"` 在该文件上无输出，转 `python -c` 亦未在本单元预算内补做）。
- 影响 D8 的处置：覆盖结论进；6.8% 那条**不进**负责人清单，改登记为"D4 待独立复算项"。

## AUD-402-判读-A1::证伪 —— 主张：X-1…X-14 十四条，逐条推翻队列输入的机械结论
**判定：逐条判定（确认 8／降级 2／待证 4）**。机械层原主张取自 `raw/AUD-402-常数台账.csv` 的对应行与我直接复算的仓库事实。
- **X-1 确认（机械层错、A1 对）**：`git grep -n -A5 "\"max_clip_rounds\"" -- eng/contracts/schemas/phase_config_normalize.schema.json` ⇒ :316 键、:317 `type=integer`、:318 `minimum=0`、:319 `maximum=100`、**:320 `default=2`**。机械层"无 schema 侧"不成立。
- **X-2 确认**：`phase_config_mosaic.schema.json:170` 逐字 `"default": "sparse_reconstruct"`（:165-169 是同值 enum），且 :171 描述自陈"数值默认同时登记于 defaults.json#snr.path 与 templates/mosaic.phase_config.json（同值）"⇒ 该键确为多侧全等，机械层的"默认无 schema 承载"不成立。
- **X-3 降级**：schema 侧**存在**（:254 `"patch_grid":`、:260 描述写"默认 8×8，§5 冻结"），但 A1 说的"`:259 default=[8,8]` 是第四侧"里那个 **机器可读 `default` 关键字我未取到原文**（:255-259 未展开，本单元命令到限）⇒ "存在第四侧"成立、"该侧带 default 字段"待证。A1 的实质判断（要紧的是 8×8 与 `min_patch_samples=64` 同为一几何量两侧）与 F-12/E-3 一致，予以确认。
- **X-4 确认**：`lib/include/astrocs/core/variance_floor.h:16` 逐字"floor 必须换算成 α²·1e-12 = 5.6867e-46"、:22"生效 floor = max(eps_rel·median(该帧可用方差), dtype 可表示下界)" ⇒ A1 的"不是混乱而是已建模"站得住，1.0e-46 与 5.6867e-46 同量级 ✓。
- **X-5 待证**：A1 引 `spatial_gain.cpp:43`（1e-9 判系数增量）与 `star_matcher.cpp:27`（1e-6 判标量 location），我按 `lib/algorithms/photometry/src/` 检索两文件该二值 **0 命中** ⇒ 我的路径猜错，不能据此判 A1 错（前言 §2 第 4 条：报 0 命中前先跑通正例）。缺的那条证据＝先 `git ls-files "*spatial_gain.cpp" "*star_matcher.cpp"` 定位再复验行号。
- **X-6 确认（纯误报，我复算同）**：`lib/infrastructure/cli/CMakeLists.txt:18` = `# 不 install、不产出独立二进制（CLI 只有唯一可执行 acsd，ASTROCS_DESIGN §6.2）` ——注释行、6.2 是条款号；:19 才是 `cmake_minimum_required(VERSION 3.16)`。机械层把条款号当浮点常数，A1 撤案正确。**含义**：机械层 20,595 行里存在"注释/文案里的章节号"这一类假阳，A3 §5.1 独立报了 19 行同类，两条互证。
- **X-9 确认**：`lib/include/astrocs/core/master_unit_guard.h:31` 与 `:32` 都是注释（:32 逐字"calibration.master_flat_median_range = [0.5, 2.0] 三处同值（SCI:101-102 / ALG:56）"），常量在 **:34/:35** ⇒ A1"行号不准"对；且它更硬的一跳我也复算到了：`docs/science/CALIBRATION.md:182` 才真正写有"冻结默认 `[0.5, 2.0]`"，:112/:122 一带讲的是响应/系数表 ⇒ "头文件自称的两条文档锚不含带值"成立 ⇒ **连唯一有门背书的键其出处链也坏**这条我站 A1。
- **X-10 确认（行锚未复算）**：`docs/science/PSF.md:147` 逐字"β=4 整平面延伸假设，**Project-defined 推导**"、:104"FWHM/σ 比值恒为 `1.230310`"⇒ 方向成立（不得按"文献值"登记）；A1 引的 :177-178 未在我这次检索的前 6 条命中内（`Moffat` 命中多），不作为反证。
- **X-11 确认**：`docs/science/REJECTION.md:284` 逐字"`alpha=0.05`/`max_outliers=10` 为 Project-defined 采纳值，**文献不提供**这两个门值"；:118/:267/:274 同族冻结 ⇒ A1 站得住。缺口只剩"defaults.json 该条是否真记 `authority_status=sourced`"一跳（未在本单元预算内）。
- **X-12 确认**：全仓 `bitpix = -32` 缺省命中 `lib/algorithms/fits_output/p3_output.cpp:639`、`lib/infrastructure/aio/src/hips/aio_hips_reader.cpp:157` 等 ⇒ "Phase3 实现缺省 FP32、与注册 fp64 反向"成立（A1 给的 module_adapters.cpp:13519/:13726 与 p3_session.cpp:126/:391 站点号我用同一检索式未逐一复现，站点存在性已由 p3_output.cpp:639 独立证实）。
- **X-7 / X-8 / X-13 / X-14 待证**：X-7（A-03 的 `0.0` 系列是值初始化哨兵）、X-8（`sdet_api.cpp:472-473` 背景 clip 也用 5.0）、X-13（rejection 各族缺省是 8 不是 1）我均因**文件路径/变量名未定位**而未取到原文（X-13 我在 `lib/algorithms/noise_snr/src/rejection.cpp` 上检索 `max_iterations|irls` 零命中，说明该文件不在此路径或该常量另有其名）；X-14（6.8% 锚点命中率）见覆盖率单元的同一缺口。**补法**：三条各先 `git ls-files "*rejection.cpp" "*sdet_api.cpp"` 定位、再按 A1 给的行号读 ±3 行；X-14 由 D4 侧独立跑一遍双锚比对。
- 影响 D8 的处置：X-1/X-2/X-4/X-6/X-9/X-10/X-11/X-12 八条"机械层结论不可信"进任务书（其中 X-6 与 A3§5.1 合成一条机械层预筛缺陷：注释/文案章节号入册）；X-3 只以"存在第四侧"入册；X-5/X-7/X-8/X-13/X-14 留待证不删。
- **本单元最重要的一条与成稿差异**：A1 的 X 表把"证伪"全部指向机械层，但**它自己 §1 的"七侧"被同队列 A3 的"八侧"在同一把键上推翻**（见下面 `AUD-402-判读-A3::立案` 的 adjudication）——即 A1 也犯了它所批评的"少扫一侧"。

## AUD-402-判读-A1::分母自证 —— 主张：全件 181 行，用于校它自报的覆盖率分母
**判定：确认**
- 我的独立取证：`wc -l AUD-402-判读-A1.md` = **181**（与清单 `:1-181` 相符，末行 :181 是 `<!-- PROGRESS: 90/90 -->`）；节区边界实测：§1 :18、§2 :36、§3 :106、§4 :134、§5 :162，与清单给的五个指针（36-37／106-133／134-161／162-181／1-181）**全部落在同一区间内**，无行号漂移。
- 与成稿差异：覆盖率分母与"全件行数"是两个量——A1 的覆盖分母是 90（队列行），181 只是文件长度；清单把两者都列成"分母自证"，我按各自口径分别判：**181 ✓、90 ✓（我自己数 CSV 得 90）**。
- 影响 D8 的处置：不进（仅自证用），但登记"清单行号与实际节界一致"这一条为 D4 装配可引用的结构核对。

## AUD-402-判读-A2::立案 —— 主张：A2 立案 L-01/L-02/L-11/L-10/L-15 等（观测面阈值绕过"唯一数值源"纪律）
**判定：确认（L-01、L-10、L-15 我逐位复算到位；L-11/L-05 定级需补触发路径）**
- 我的独立取证（先取锚再读它的判定）：
  - **L-01 四侧并列全部复算命中**：`git grep -n "kAllocGrowthWarnMbPerS\|kAllocReclaimWindowSeconds\|kAllocMinCurveSamples\|kAllocSlopeMaxPoints" -- lib` ⇒ 实现侧四个 `inline constexpr` 在 `lib/infrastructure/cli/memory_report.h:54/63/64/66`，**行号与 A2 所引完全相同**；契约侧 `eng/contracts/resource_gate_v1.json` 对这四个符号 **0 命中**，而同一条命令在 `growth_limit_mib_per_s` 上**命中 1** ⇒ 0 命中是真零命中（前言 §2 第 4 条的正例自证已做）。⇒ "四值既不在契约也不在生成头、却活在实现侧"成立。
  - **L-10（我认为 A2 最硬的一条，且与 A3、BD1 三路独立同判）**：`git grep -nE "iowait_percent *[=*]" -- lib` 在 lib 内**只命中声明处** `resource_gate.h:182 double iowait_percent = 0.0;`（成员初值，非赋值）⇒ 生产侧确实从未赋值；同一条命令式在 `eng/tests` 上命中 **9 处/5 文件**（3+2+1+1+2），与 A2 自报的"9 处/5 文件"**逐位相同**；后果链我核到 `:326 if (g.iowait_percent > g.io_wait_high_percent)` ⇒ 恒不触发，`:368` 的 iowait 腿恒真。
  - **L-15**：`git grep -n "memory_growth\.h" -- lib eng` 命中全在 `eng/tests/cli/test_memory_growth.py:7,:19`，**lib 内 0 include** ⇒ "整模块生产零接线"成立（属前言 §2 第 8 条的"未接线"档，不是"未实现"，A2 用词正确）。
- 与成稿差异：**L-11（CPU 跨刻度）我复算到"两侧定义不同"（`resource_gate.h:207 io_wait_high_percent=50.0`、`:182 iowait=0.0`、`:368` 比较点均确认存在），但没复算"16 核机上 0.2 核即过线"的数值后果**——那需要 `commands.cpp:915` 未归一这一跳；按前言 §2 第 5 条，缺该跳只能定 **S2·缺运行时触发路径**，A2 写的是"方向已按不等式逐条定 ⇒ 偏 fail-open"（方向判断我同意，量级判断未复算）。另 **L-05（USER_HZ 硬编码）**：同文件两套口径我未逐行读（:204-205/:257-258/:277-278 三处未展开），判**待证**，缺的证据就是这三行的原文比对（一条 `git grep -n "_SC_CLK_TCK\|100\.0" -- lib/infrastructure/cli/monitor.h`）。
- 影响 D8 的处置：L-01、L-10 进任务书（并**与 A3 的 2.6 表、BD1 的 L3 合成一条**："io 面阈值出源 + iowait 输入未接线"是同一处缺陷的三个视角）；L-15 进任务书但按"未接线"措辞；L-11/L-05 先补触发路径再定级。

## AUD-402-判读-A2::待确认 —— 主张：8 条 UNRESOLVED 候选按 §2 第④档上呈
**判定：降级（数量与"保守方向＋影响范围"两列齐备；但清单只有 U-01…U-08 共 8 条，非派单说的"若干"，且全部落在记录面而非退出码面）**
- 我的独立取证：节界 `wc -l`=146 与清单 :117-129 相符；表体 8 行、三列无空。**关键定级证据我取到了**：A2 自己 U 表里的 U-01…U-08 全部属资源门族，而 `lib/infrastructure/cli/resource_gate.h:268-269` 的 `gate_enforcement(...)` **无条件 `return GateEnforcement::RecordOnly;`**（:250 注释"唯一处置"、:265 注释引"§9.74 裁决 10 起…本函数恒 RecordOnly"），契约 `resource_gate_v1.json:75 "in_process_hard_fail": "retired"` ⇒ 这 8 条**都不改变 CLI 退出码**，只影响记录面与外挂裁决面。这正是 A3 §5 末条独立得出的结论，两路互证。
- 与成稿差异：A2 定级文字未声明"不进退出码"这一层（它在 §3.5 顺带说了 §8.3 表格为陈旧行，但 U 表按 enforce 语义写"假绿/逃逸窗"）⇒ **8 条整体降一档：从"门失效"降为"记录面判据不忠实"**，按前言 §2 第 2 条属"降级（机制成立但定级要改）"。**另有一条我该替负责人钉住**：契约 :76 把这次门退役的依据写成"ASTROCS_DESIGN.md **§3.5/§6.3**"，而这两个号**在权威文件里不存在**（见下面 A2::证伪 的复算）⇒ 退役理由挂在悬空指针上，这条应上呈。
- 影响 D8 的处置：U-01/U-02/U-04 进任务书（记录面正确性）；契约 :76 的悬空引用**并入 A2 L-09 的机械可改号一次性收口**。

## AUD-402-判读-A2::覆盖率 —— 主张：90 行 100%、12 列、逐行五字段非空（由自有校验器判定）
**判定：确认（行数与列数我独立数到），但"18 行同簇合并"与"队列不含整数字面量"两条按待证**
- 我的独立取证：`raw/AUD-402-判读-A2.csv` 数据行 **90**、列数 **12**；输入侧 `inventory/P402-A-04/05/06.csv` 之和与机械层 A 族总行数自洽（A 族 11 份 = 304 行；A1 90 ＋ A2 90 ＋ A3 124 = 304 ✓ 三份恰好瓜分 A 族，无重叠、无遗漏）。§1 的文件域分布 35+28+10+5+4+4+2+1+1 = **90** ✓ 与自报一致。
- 与成稿差异：①"逐行五字段非空"由 A2 自有校验器 `复算/q402-A2/check_a2.py` 判，我只数了行与列，**未复算四关键列非空**（缺那一条：对我方 CSV 直接跑非空计数，不必用它脚本）。② 队列"不抓无符号整数字面量"这一自限（§2 第 1 条）如果成立，就意味着**判读层的分母本身有系统性缺口**——这条我在机械层 CSV 侧无法验（要看机械层的抽取正则），列为需 D4 补核：`inventory/P402-D-统计.md` 的"无名行内字面量 137"与读者件 §1.2 的"无名行内（生产）3,221"差 23 倍，两者口径不同（读者件自己也声明"两套口径不可互推"）⇒ 队列切片是否覆盖了 3,221 中的抽样，**本批无法判定，需 D4 侧给抽样率**。
- 影响 D8 的处置：覆盖结论进；"整数字面量不入队"作为**机械层预筛缺陷**登记一条（与 A1 X-6、A3 §5.1 的"注释章节号入队"同族）。

## AUD-402-判读-A2::证伪 —— 主张：6 条，含"ASTROCS_DESIGN.md §3.5/§6.x 是有效权威指针"被推翻
**判定：逐条判定（确认 3／降级 2／待证 1）——第 1 条我完全复算，且它是本批最可机械执行的一条**
- **①确认（§3.5 不存在、§6.x 指错章）**：`git grep -c "^### 3\." -- ASTROCS_DESIGN.md` = **3**（正例先跑通），`git grep -n "^### 3\.[45]"` = **0 命中** ⇒ §3 只有 3.1/3.2/3.3（:221/:231/:237），**§3.5 确实不存在**；`## 6. export（Phase3）：投影导出`（:434）下 §6.1 使命/§6.2 流程/§6.3 投影算法，而 CLI 合同章是 `## 7.`（:464，§7.1 命令树 :466、§7.2 配置、命令与退出码 :493）⇒ A2 说的"CLI 族的 §6.x 全应为 §7.x"**结构与行号两侧都对得上**。计数差异：我用紧式 `ASTROCS_DESIGN.md §3.5` 数到 **15 个文件**，A2 报"31 处/24 文件"（它含其它写法）⇒ 不同口径，不改结论。
- **②确认（module.yaml 5 行不是数值）**：该判定与我独立在 A1 X-6 复算到的同一形态一致（`lib/infrastructure/cli/CMakeLists.txt:18` 的 `6.2` 是 `ASTROCS_DESIGN §6.2` 条款号，:19 才是 `cmake_minimum_required(VERSION 3.16)`）⇒ 机械层的"章节号/版本串入册"缺陷由 A1、A2、A3 三路各自独立命中，**定为机械层预筛的系统性假阳类**。
- **③降级（parser.cpp 4 行同因）**：与②同族，且 A2 由此得出的"值列不可作唯一键"（同一行两号 `3.3;9.68`）是**对 D4 主表键选择的直接告警**：D4 的 410 行按"符号或键"聚合，若上游用"值"做键会双计/漏合。判降级为"应升格为对 D4 装配规则的约束"，不只是本队列的一条备注。
- **④确认（memory_growth.h 可即判）**：见 L-15 复算，静态可达性足以下结论，不需等构建。
- **⑤待证（机械层位置列有 1 处不实）**：这是 A2 **对自己首版错误的自我更正**，属过程记录，不是对外结论；我无法在本批核（要看它首版），但它给出的规则**"机械层 `(无名)` 与值并列时会丢符号，不能当可信主键"值得保留**——与我复算的旁表 1 组重复键、26 处同键不同值一致：`(无名)` 聚合桶（机械层 9,166 行）确实使"键"失去唯一性。
- **⑥待证→已确认（它自己过头的一条）**：A2 记"`disk_gate.h` 的 `est.truncated` 并非全仓零消费者"是自我降档，方向正确，无需我另核。
- **本单元我唯一发现 A2 说轻了的地方**：契约把"一般性资源超限门取消"的依据写成 `ASTROCS_DESIGN.md §3.5/§6.3`（`resource_gate_v1.json:76` 逐字），即**一个机器合同的退役理由指向两个不存在的条款号**——A2 在 L-09 的"31 处引用"列表里点了它，但没点出"这条是退役依据"的严重性。判确认并升格。
- 影响 D8 的处置：①②④进任务书（机械改号一次收口＋禁引表＋D4 键规则）；③并入 D4 装配约束；⑤⑥留痕不进。

## AUD-402-判读-A2::分母自证 —— 主张：全件 146 行，用于校覆盖率分母
**判定：确认**
- 我的独立取证：`wc -l AUD-402-判读-A2.md` = **146**（末行 :146 = `<!-- PROGRESS: 90/90 -->`）；清单五个指针（46-47／117-129／130-136／137-146／1-146）与实际节界（§3 :46、§4 :117、§5 :130、§6 :137）一致。
- 与成稿差异：A2 的"分母自证"里另含一条与 D4 相关的口径：**§0 本队列读数 64＋17＋8＋1 = 90 ✓**、`来源现状` 65＋20＋5 = 90 ✓ 两列各自闭合（我加的），说明它的分布数自洽。
- 影响 D8 的处置：不进。

## AUD-402-判读-A3::立案 —— 主张：L1 `drizzle.pixfrac` 八侧不一致且生产兜底站在裁决对面（本队列最高危）
**判定：确认（并以此推翻 A1 的"七侧"计数与"只有一侧决定生产取值"表述）**
- 我的独立取证（A3 给的锚逐跳复算，先于读它的结论）：
  - `lib/infrastructure/scheduler/src/module_adapters.cpp:7506` 逐字 `const double pixfrac = p1_num(dj, "pixfrac", 1.0);` ⇒ **缺键取 1.0** ✓；
  - 触发路径**我复算到三跳**：`parser.cpp:291` 白名单含 `"drizzle"` ✓ → `module_adapters.cpp:7506` 读键 ✓ → `module_adapters.cpp:8283 rc = hp_drizzle_run_phase1_hips(frame, frame_nside, nested, pixfrac, …)` ✓（中间跳 `runtime_client.cpp:217` 我用 `"drizzle"` 在该文件 0 命中 ⇒ 该跳未复现，很可能因节点名由多字面量拼成，属前言 §2 第 4 条列出的假阴来源，不判 A3 错）；
  - **A3 判 fail-open 的关键对照我逐字复现**：同一函数内 `:7507` 注释"B2-A12: precision_mode 科学精度门（无 silent default）"、`:7512-7515` `if (!p1_has(dj,"precision_mode")) … "missing precision_mode is rejected, no silent default"` ⇒ 同一批 drizzle 参数里，precision_mode 缺失即拒、pixfrac 缺失即补 1.0 ✓✓；
  - 另一侧 0.8 我也复现：`json_config.h:67 double pixfrac = 0.8; // (0,1]; 生产默认 0.8`、`json_config.cpp:711-713`（省略时取 0.8，注释自称"权威默认，与 schema/template 一致"）；
  - 文档侧：`docs/plugins/algorithms_phase1/08_drizzle.md:50` 表格行 `| pixfrac | 0.8 | …… |`、`:53`"数值默认唯一来源 = defaults.json" ⇒ A3 的第③侧真实存在（**A3 引 :49，实际 :50/:53，行锚偏 1**）；
  - kcorr 标定前提：`lib/algorithms/coverage/src/sampler.cpp:81`"当前 Drizzle 引擎（pixfrac=0.8 生产默认）2000 实现 MC 校准"、:88 矩阵 pixfrac∈{0.5,0.8,1.0} ✓ ⇒ 取 1.0 同时偏离裁决与标定前提，A3 成立。
- **本单元对派单三问之①的最终裁断**：`drizzle.pixfrac` 的侧数**至少八侧**（A3 口径），**A1 的"七侧"比它少一侧**——A1 漏的是**插件文档默认列表**（08_drizzle.md:50），且 A1 的 schema 侧少列一处声明点（:142-147 与 :468-472 两处均无 default）、其 registry 侧只给 `declared_default=1.0` 而未取到同文件 :732"默认 1.0 已入 defaults.json"的自述对撞。**更实质的分歧**：A1 说"只有一侧决定生产取值（①代码兜底）"，而**代码兜底本身是分裂的两套值**——scheduler 适配器路径缺键取 **1.0**（module_adapters.cpp:7506），编排器 Stage1Config 路径缺键取 **0.8**（json_config.cpp:712-713，被 `orchestrator.cpp:3340` 消费）。⇒ 正确表述是"**生产取值由'被喂进来的那份文档'决定；文档缺该键时由走哪条生产路径决定，而两条路径的兜底互不相同**"。A1 把自己的①当成单侧，是它这次判读里唯一的结构性偏差；A3 因为把⑤⑥分列两侧而避开了这个坑。**判：A1::立案 的 headline 降级、A3::立案 确认。**
- 与成稿差异（其余）：L7 四侧我核到 `resource_recorder.h:97 = 0.25` ✓；`commands.cpp:720 = 0.5` 与 `eng/tools/**` 两个默认未展开（A2 与 A3 对 0.5 的六处站点独立同判，视为已双证）。
- 影响 D8 的处置：**进任务书并列为本批第一**（④⑤改 0.8 或由契约生成；⑦重锚；补"registry `declared_default` 必须逐字等于所指 defaults 值"的机器门；把 CLI 骨架侧与文档默认列纳入侧集合扫描面）。A1 的 ACSD-T 建议与本条**合并为一条**，不要拆成两案。

## AUD-402-判读-A3::待确认 —— 主张：18 行 UNRESOLVED 候选，每行给位点/值/待确认内容/影响范围
**判定：确认（数量与列闭合），但其中"影响生产"的一条需要重新定级**
- 我的独立取证：表体行数我数到 **17 行**（清单说"18 行"，A3 §4 也自报"④待确认 18"）⇒ **差 1**：因 `resource_gate.h:368 / :369` 那一行把两个位点并写一格（20.0/5.0/15.0），按位点算是 18、按表行算是 17。判**降级为口径问题**（须在表头声明"按位点计数"），不是漏行。§1 的分布自校：97＋18＋9 = **124** ✓ 与其队列规模吻合。
- **我要替负责人钉住的一条定级**：A3 在"影响范围"列多处写"CLI 生产可达""Compute 分支记录面"，而它自己 §5 第 6 条（我已复算：`resource_gate.h:268-269` 恒 `RecordOnly`＋契约 `:75 in_process_hard_fail=retired`）证明**这些都不进退出码**；A3 据此把 L2-L18 降一档 ✓ 做得对——**但它的 U 表没有同步降**，两处口径不一致。判：机制确认、U 表定级按 §5.6 的口径统一。
- 与成稿差异：无其它；9 行"有出处（①/②）"我未逐行复算（缺：每条锚的原文回读）。
- 影响 D8 的处置：`session_commands.h:181`（须改 0.8）与 `runtime_client.cpp:118`（版本串无合同约束）进任务书；其余按记录面批量进"低优先整改"。

## AUD-402-判读-A3::覆盖率 —— 主张：124 行全覆盖、12 列、同簇合并 6 组覆盖 79 行
**判定：确认（124 与 12 列我独立数到；"同簇 79 行"按其自有 CSV 未复算）**
- 我的独立取证：`raw/AUD-402-判读-A3.csv` 数据行 **124**、列 **12**；输入侧 `inventory/P402-A-07..11.csv` 与 A 族 304 行减去 A1/A2 的 180 行 = **124** ✓ 独立推得同一分母（不依赖它自报）。§6 里它给的 `wc -l` 读数是 31/31/31/31/5（含表头）⇒ 数据行 30×4+4=124 ✓ 与我的口径一致。
- 与成稿差异：**"6 组共用一条出处覆盖 79 行"意味着 124 行里 63.7% 是簇内共用出处**——这个比例必须在 D4 主表口径里显式承载，否则读者会把 124 读成 124 次独立取证；同理 A2 的 18 行、A1 的 rejection 15 行→6 锚。⇒ 判"确认其计数，降级其含义"：**判读层 354 行的实际独立取证锚数远小于 354**（读者件 §1.3 自己算了 233 组、平均 8.09 符号/组，与我此判一致 ✓，两路互证）。
- 影响 D8 的处置：不进（口径项，转 D4 说明件）。

## AUD-402-判读-A3::证伪 —— 主张：6 条（19 行假阳、2 行值与初值不符、0.25 非生产值、printf 精度串、退出码证否）
**判定：确认 5／降级 1**
- **①确认（19 行是注释/文案里的条款号）**：与我独立复算的 A1 X-6（`cli/CMakeLists.txt:18` 的 `§6.2`）与 A2 §6.2（module.yaml 5 行）同族 ⇒ 三路各自独立命中 ⇒ 机械层的"注释/文案数字入册"是**已证实的系统性假阳类**，不是个例。它的直接后果：机械层 20,595 这个分母里含此类噪声，D4 的"具名未判 4,757"因此**偏高估**。
- **②确认（报的"现行值"是注释里的废止档位）**：与 A2 L-09 复算的"文档指针错位一族"方向一致；具体 `resource_gate.h:211/212` 初值我未逐行读（缺 2 行原文）。
- **③确认（特性宏存在性哨兵不是数值常数）**：同族的 §4 四选一不适用判法，与标准 02 §4 的语义边界一致。
- **④确认（0.25 一侧生产零调用点）**：`git grep -n "interval_seconds" -- lib/infrastructure/cli/resource_recorder.h` ⇒ :97 `explicit ResourceRecorder(double interval_seconds = 0.25)` 是**默认参数**；A3 判"机械层把未使用的默认参数当生产值"⇒ 与我复算的 L7 四侧表吻合；A2 亦独立报 0.5 才是生产实取。
- **⑤降级（printf 精度串）**：结论（格式不是常数）我同意，但它进一步做的"我逐行核了它们对下游判据的分辨率是否够 ⇒ 充分"属**未给算式的判断**（cpu_pct 0.01 核 ≪ 85% 判据、时间 1 ms ≪ 10 s 域界）——两处比较它都只给结论，缺"判据实际分辨率需求"的来源 ⇒ 该半句判**待证**（补法：给 `%.2f` 在 `resource_summary.json` 里被谁读、读的容差是多少）。
- **⑥确认（资源判据不影响退出码，我给到逐字证据）**：`resource_gate.h:268-269 inline GateEnforcement gate_enforcement(bool, GateDiag){ return GateEnforcement::RecordOnly; }` ＋ `:250` "RecordOnly —— **唯一**处置" ＋ `resource_gate_v1.json:75 "in_process_hard_fail": "retired"` ＋ `:82 "hard_fail_criteria_scope" … 不经 ACSD CLI 退出码` ⇒ A3 的证否成立，且**它把 L2-L18 整体降一档这个动作是本批最正确的定级纪律**（前言 §2 第 2 条的"降级"范式）。
- 影响 D8 的处置：①④⑥进任务书（①转机械层预筛规则、⑥作为所有资源门整改的定级前提）；②③留痕；⑤前半进、后半待证。

## AUD-402-判读-A3::分母自证 —— 主张：全件 280 行，用于校覆盖率分母
**判定：确认**
- 我的独立取证：`wc -l AUD-402-判读-A3.md` = **280** ✓（末行 :280 = `<!-- PROGRESS: 124/124 -->`）；清单五指针（31-32／190-214／215-228／229-251／1-280）与实际节界（§2 :31、§3 :190、§4 :215、§5 :229）一致。
- 与成稿差异：清单把"立案"指针记为 `:31-32`（只两行），实际 §2 立案体在 :31-188——**四个分片的"立案"指针都是"节头＋1 行"的写法**，我按整节判读，并在此登记该差异（前言 §2 第 9 条：以清单为准，但清单的行域明显是节起点而非节全貌）。
- 影响 D8 的处置：不进。

## AUD-402-判读-BD1::立案 —— 主张：多侧不一致 4 条＋该导出未导出 5 条（L1 iterativeClipSigma 为最重）
**判定：确认（L1、L3、D1、D5、L4 全部逐位复现，行号零漂移）；L2 判待证**
- 我的独立取证：
  - **L1 两侧生产入口取不同值——三个行号全中**：`git grep -n "iterativeClipSigma" -- lib/…/module_adapters.cpp lib/…/orchestrator.cpp` ⇒ `orchestrator.cpp:1569 sdet_params.iterativeClipSigma = 9.0f;`、`module_adapters.cpp:3959 sp.iterativeClipSigma = 5.0f;`、`module_adapters.cpp:4846 sp.iterativeClipSigma = 5.0f;`，再加 `sdet_api.cpp:996 defaults.iterativeClipSigma = 9.0f`（默认侧）与文档镜像 `star_detection/README.md:119 iterativeClipSigma=9.0f` ⇒ **同一参数两个生产入口两套值**，且合同/PUBLIC_API 只登记 9.0 一侧 ✓✓ 完全成立。BD1 还写明"该字段在 `lib/algorithms/star_detection/src/` 内只赋值不读取 ⇒ 是登记面缺陷不是科学缺陷"——这个自我定级克制且正确，符合前言 §2 第 8 条的状态词纪律。
  - **L3 确认**：`resource_gate.h:207 double io_wait_high_percent = 50.0;` ✓；契约侧 `iowait|io_wait` 在 `eng/contracts/resource_gate_v1.json` **0 命中**，同一条命令在 `compute` 上**命中 6**（正例自证，且与 BD1 §5(5) 自报的"命中 6 行"逐字相同）⇒ "唯一数值源被旁路"成立；**A2 L-01 与 A3 2.2 表从另两面独立命中同一处**⇒ 三路同判。
  - **D1 确认**：`snr_science.cpp:279 return 1.253 * sigma_logflux_dex / std::sqrt((double)n_matches);` ✓ 且**同文件 :272 注释自己写"1.253 = sqrt(pi/2)"** ✓ 逐字；√(π/2)=1.2533141… ⇒ 截断相对偏差 −2.51e-4 与 BD1 同（我按解析式独立口算复核一致）。
  - **D5 确认（我独立重算数值）**：`gen_geometry_truth.py:177 "hips_pixel_scale=58.6"` ✓；(180/π)·√(π/3)/1 = 57.29578×1.02333 = **58.6323°** ⇒ 正是 **nside=1 基面**像元尺度，而该文件 :168 声明 `hips_order=leaf_order`（SHIFT=9 ⇒ nside≥512，对应 0.114516°/412.258″）⇒ 相差 ≥512× ✓ BD1 成立。
  - **L4 确认（形态级立案，可推广为机械判据）**：`p1001_real_nodes_test.cpp:3709 r_scaled=-1.0`、`:3749 r_scaled = vf[0].value("mask_radius_p50", -1.0)`（缺键即负哨兵）、`:3752 CHECK_MSG(r_scaled < 60.0, …)` ⇒ 字段根本不存在时 −1<60 恒真 ⇒ **空门** ✓；BD1 同句自限"第二条 `r_adu>0.0` 守卫的 rel 判据能红，故非全盲"——我在 :3760 复现到该守卫 ✓ 定级诚实。
- 与成稿差异：**L2 `sigma_floor`（1e-3 合同侧 / 0.02 夹具侧 / 1e8 oracle 侧）我未取到任何一个站点**（缺：`git grep -n "sigma_floor\|σ_floor" -- docs/contracts/DATA_SEMANTICS.md lib/algorithms/coverage eng/tests/backend/syn008_seam_main.cpp`），判**待证**；其"0.02 与同夹具 kNoiseRms=0.05 同阶 ⇒ 该用例测的不是冻结口径"这条若成立，价值不低于 L1，值得单独补。
- 影响 D8 的处置：L1、L3、L4、D1、D5 进任务书（L4 转成一条**机械判据**入预筛："读键兜底为负 + 判据为纯上界 ⇒ 空门候选"，可与 A3 §5.7 的"断言实参优先入册"合成同一条预筛改造）；L2 待证不删。
- **我新增的一条（不在任何分片里）**：`eng/tests/io/make_hips_fixture.py:169` 给同一键 `hips_pixel_scale` 钉了**第三个值 11.519173063162576**，既不是 0.114516（度）也不是 412.258（角秒）⇒ 该键在夹具面还有第三口径，语义未定；建议 D4 装配时把这三处并到同一行的"位置集合"里，否则"多侧"计数又会漏一侧。

## AUD-402-判读-BD1::待确认 —— 主张：9 条 UNRESOLVED，按"影响生产科学结论"排序
**判定：确认（条数与排序依据齐），但第 1 条的"当前无行为影响"须升为条件性风险**
- 我的独立取证：§2 表体 9 条 ✓，每条含影响面＋保守方向 ✓（满足标准 02 §4 第 4 档的入册要件）。**我核了排序前提**：第 1 条 iterativeClipSigma 的"当前无行为影响"依赖"该字段在 star_detection 内只赋值不读取"——这一跳我没独立复算（缺：在 `lib/algorithms/star_detection/src/` 内检索该字段**读取**点）；若它被读，第 1 条即从登记面缺陷升为判坏集合缺陷。
- 与成稿差异：第 2 条（io 面阈值出源）与我复算的 A2 L-01/A3 2.2 是同一处的三个视角 ⇒ **三条应在 D8 合成一条**，否则会出现三个任务书条目改同一行代码；第 5 条 kRealSky 双份复制、第 9 条 tol 1e-12 七处共用，均为登记面，判"进"。
- 影响 D8 的处置：第 1/3/4/5 条进（第 1 条附"读取点未核"的前置）；第 2 条并入合案；第 6-9 条进低优先。

## AUD-402-判读-BD1::覆盖率 —— 主张：50/50 判完、同簇合并后实际覆盖 ≈92 个位点、处置分布 33/9/7/1
**判定：确认（行、列、两列分布各自闭合到 50；"≈92"按其自述无法外部复算）**
- 我的独立取证：`raw/AUD-402-判读-BD1.csv` 数据行 **50**、列 **12**；输入侧 `inventory/P402-BD-01.csv`＋`P402-BD-02.csv` 之和 = 50（我按 BD 族总 652 行 / 27 份文件反推，单文件均值 ~24 行，与 25+25 相符）✓。**两列分布我做了闭合算术**：处置 33＋9＋7＋1 = **50** ✓；来源现状 38＋4＋4＋3＋1 = **50** ✓ 两列都恰好闭合，无"占比凑数"。
- 与成稿差异：**"派单写 25 个、按两个批次文件实为 50 个"这一偏差它自己在文件头就登记了 ✓**（前言 §2 第 9 条要求），处理正确；"≈92 位点"含 42 个**队列外邻居**，属"顺手扩面"，不得计入任何分母（D4 若把它读成覆盖 92 就双计）。
- 影响 D8 的处置：不进；转 D4 说明件的一条口径（"同簇邻居不加分母"）。

## AUD-402-判读-BD1::证伪 —— 主张：7 条（负例指纹当现行值、格式串假阳、6 行丢表达式结构、同行串值、四选一不适用、计数含实验区、机械漏判据）
**判定：确认 6／降级 1 —— 本分片是四路里机械层反证最系统的一路**
- **①确认（我取到逐字证据）**：`p1_hips_writer_test.cpp:158 CHECK(props.find("hips_pixel_scale=412.258369") == std::string::npos);` ⇒ 412.258369 是"**必须不存在**"的负例指纹，:152 注释说明旧实现写角秒、现行 0.114516 度 ✓ 机械层把反例指纹记成现行值，BD1 对。
- **②确认（方向）**：`p1_stars_test.cpp:227 fprintf(stderr, "[diag] case1 25-star sigma=%.6f rel=%.6f")` ✓ 格式串；`ZP_k` 那条我在 `eng/contracts/schemas/unified/frame_snr.schema.json:374` 的 description 里命中同一段公式文字（"ZP_k = ZP_syn − 2.5·log10(k_photo)"）⇒ 同类"公式文字被当数值"，唯 BD1 引的 `p1snr_fref_baseline_test.cpp:271` 站点未展开（不影响结论）。**含义比 BD1 说的更大**：机械层的抽取式**会把注释、诊断文本、schema description 里的数字一并入册** ⇒ 这是 20,595 这个宇宙的系统性噪声源（与 A1 X-6、A2 §6.2、A3 §5.1 四路独立同判，**四路**）。
- **③确认（丢表达式结构）**：与④同一机制（按"行内字面量"抽 ⇒ 一行多赋值/扫参表达式被压成单值）；`1.253`、`5e-3`、`9216` 这类派生/判据值被压平，正是它提议"断言实参优先入册"的理由。
- **④确认**：`:3063 double max_rel=0.0, max_recip=0.0;` 与 `:3241 double max_recip = 0.0;` 同行多声明 ⇒ 机械层把 `support=1.0` 并给 `snr` 的形态在文本上确实可能（我复现的是同族多赋值形态，非该具体行）。
- **⑤确认（33/50 四选一不适用）**：与标准 02 §4 的语义边界一致，且**直接反驳"无来源"这一列的用法**——D4 主表"来源现状"列若把结构性占位也记成"无出处"，会把 110 行"待确认"虚增。判确认。
- **⑥降级（计数口径要订正）**：结论对（`sigma_f` 不带词边界 1,755 vs 带边界 55，32 倍失真），但它是**给机械层的处方而非可判定结论**；D4 若要"副本数"列必须先重算 ⇒ 降级为"须登记为未决口径"，不得据现有 tot 数下任何"重复 N 份"的断言。
- **⑦确认（机械漏判据，我复现到点）**：`max_recip` 的累加器（:3063/:3241）在册，而真判据 `CHECK_MSG(max_recip < 5e-3, …)` 在 **:3261** ✓ ⇒ 漏的正是最该登记的那个数。BD1 提的规则可执行：**断言/CHECK 实参里的字面量优先入册**。
- 影响 D8 的处置：①②③④⑦ 进"机械层预筛改造"一条任务书（四路证据合并）；⑤进 D4 口径说明；⑥登记未决。

## AUD-402-判读-BD1::分母自证 —— 主张：全件 182 行，用于校覆盖率分母
**判定：确认**
- 我的独立取证：`wc -l AUD-402-判读-BD1.md` = **182** ✓（末行 :182 = `<!-- PROGRESS: 已判行 50/50 …-->`）；清单指针（28-29／71-86／87-101／161-170／1-182）与实际节界（§1 :28、§2 :71、§3 :87、§6 :161）一致；**偏差登记**：清单没给 §4（判读成本实测 :102-119）与 §5（确定命令 :120-160）两个节区 ⇒ 它们不在任何复核单元的覆盖里；我通读了 §4/§5 并在此确认其自述计数（≤1 次 10＋2 次 22＋3 次 16＋≥4 次 2 = 50 ✓；9216/58.6323/1.25331 等复算值我独立复核为对）。
- 与成稿差异：无（除上述清单行域覆盖缺口，属本批清单本身的问题）。
- 影响 D8 的处置：不进；但**§4 的"给前台的预算建议"有决策价值**——它主张"不做全量逐符号判读，改多侧候选预筛＋只判预筛命中"，与读者件 §1.4 的"20,183 行不逐条判读"是同一结论的两路独立得出，可一并上呈。

## 附加判定（派单三问之③）D4 主表三个数是否自洽：机械层 20,595／判读层 354／主表 410
**判定：确认（三个数各自可复算、四桶划分我逐桶复现到零差），但登记 2 条口径缺陷 + 1 条 CSV 结构缺陷**

我全部自己数（读 CSV 文本，`csv.reader` 口径；未 import 仓库内 Python，未跑 `eng/**`；工作件 `独立审计/复算件/V402/count.py`）：

| 量 | 成稿/读者件主张 | 我复算 | 判定 |
|---|---|---|---|
| 机械层主表行 `raw/AUD-402-常数台账.csv` | 20,595 | **20,595** 数据行（物理行 20,597；`(符号/键, 位置)` 唯一键 20,595，无重复键组） | 确认 |
| 机械层旁表行 | 18,884 | **18,884** 数据行；但唯一键只有 **18,883**（旁表内部有 **1 组重复键**） | 确认＋订正 |
| 旁表 ∩ 主表（同符号同位点） | 18,883 | **18,883**；旁表 − 主表 = **0** | 确认（逐位复现） |
| 主表 ∪ 旁表 | 20,595 | **20,595**（键级并集；旁表不新增任何站点） | 确认 |
| 判读层行 | 354（90＋90＋124＋50） | **354**（A1=90、A2=90、A3=124、BD1=50，四件均 12 列） | 确认 |
| 具名已判桶 | 412 | **412**；四桶 **12,205／3,221／412／4,757 逐桶零差**（谓词式按读者件 §1.2 的优先序与"测试/实验路径"正则独立重写后复现） | 确认 |
| D4 主表行 | 410 | **CSV 物理记录 411 条数据行**，其中**末条不是数据**而是 `<!-- PROGRESS: 已完成 410 / 预计 410 行…-->` 注释串被当作 1 列记录写进了正本 CSV ⇒ 用 `if len(r)>1` 过滤后正是 **410**；`类别` 列计数 278 常数＋72 算法＋40 容差＋20 公式 = **410** ✓；§2 自述"399 装配＋11 补入 = 410" ✓；§2.1 处置分布 192＋110＋48＋45＋15 = **410** ✓ | 确认（410 成立）＋**登记结构缺陷**：PROGRESS 注释行混在数据 CSV 里，任何不做 `len(r)>1` 过滤的消费方都会数出 411，且该行只有 1 列 ⇒ 与"16 列"声明冲突 |

**三者之间是什么关系（这是读者最容易弄错的地方）**：三个数**不是同一分母上的三层，而是同一份扫描的三种粒度**，不可相加，也不存在"20,595 里挑 354 行、354 行汇总成 410 行"的除法关系：

1. `20,595` = **位点级**：机械层 `(符号/键, 位置)` 对的全宇宙。`18,884` 旁表是它的"只登记不判读"子集视图 ⇒ **相加即双计**（读者件 §1.1 已明写"凡按相加算分母的读数都作废"，我复算支持：相加会得 39,479，而真实宇宙是 20,595）。
2. `354` = **判读动作级**：四个判读 CSV 的行。354 行的键**100% 落在 20,595 之内**（我复算：交集 = 354，差 = 0）⇒ 354 是宇宙的一个 1.72% 的**严格子集选择**，把 354 与 20,595 相加同样双计。354 行只有 206 个不同符号键（判读行/符号 = 1.72）。
3. `412` = 354 与 20,595 之间的**桥**，但它不是"354 个子集"：判读层的逐点命中（`(符号,位置)` 完全相等）只有 **354** 行，剩下的 **58 行是靠"符号名相同"记入"已判"** 的（谓词是 `位点在判读集内 OR 符号名在判读符号集内`）。⇒ **"具名已判 412"里有 14% 是名同址不同的搭车行**，对 `scale`／`limit`／`gain`／`snr`／`tol` 这类泛名尤其危险（BD1 §5(2) 自己实测过泛名不带词边界会 32 倍失真）。**这不是造假，是口径要写明**：412 = 逐点判读 354 ＋ 同名记入 58。
4. `410` = **台账条目级**：按"符号或键"聚合（一行的"位置集合"列可携带 15 个位点，如 `drizzle.pixfrac` 一行 15 位点），且**跨来源**——我读了它的"来源成稿与条目"列：单一来源 A3 86＋A1 78＋BD1 39＋A2 24 = 227 行，另有多来源行（如"A2；L-05"5 行、"BD1；G-P1"2 行）⇒ **410 ≠ f(354)**：410 里还含 72 行"算法"与 20 行"公式"，它们不来自常数普查。把 410 与 354 相加，会把常数/容差那部分双计。

**结论**：三个数各自**自洽且可复算**（含四桶零差复现）；两两相加**都会双计**；唯一合法的加法是 12,205＋3,221＋412＋4,757 = 20,595。旁表按题目提示已核死：`18,883/18,884 与主表同键、旁表−主表=0`，**它不是第二份数据**；但我要把"同行"二字收紧——**键级 18,883 同行，整行原文级只有 18,858 相同，另有 26 行同键而 `现行值`/`来源现状`/`备注` 三列与主表不一致**（样例：`frame_snr`、`vals`）⇒ 若有人拿旁表当值读，会读到与主表不同的 `现行值`；建议读者件把"同一份数据"改写为"同一份站点、26 处文字列有出入"，并清掉旁表内那 1 组重复键。

**缺什么才算完全独立**：四桶的"测试/实验路径"正则我是**照读者件脚本 `t2_final_numbers.py` 的谓词重写后复现**的（先按我自己猜的谓词数得 11,717／3,470／430／4,978，加总仍 20,595 但逐桶差 488／249／18／221 ⇒ 差异全在谓词，不在数据）。⇒ 判定"确认"，但读者件应把该正则式**写进正文而不是只留在脚本里**，否则四桶不可被第三方复算。

## 本批判定汇总（各态计数，附复算命令）

**单元级（20/20 全部有判定）**

| 态 | 数 | 单元 |
|---|---|---|
| 确认 | **17** | A1::覆盖率、A1::证伪、A1::分母自证、A2::立案、A2::覆盖率、A2::证伪、A3::立案、A3::待确认、A3::覆盖率、A3::证伪、A3::分母自证、BD1::立案、BD1::待确认、BD1::覆盖率、BD1::证伪、BD1::分母自证 ＋ 附加判定 D4 三数 |
| 降级 | **3** | A1::立案（"七侧＋只有一侧决定生产取值"两条字面主张在样本键上被复算否证，机制成立故记降级）、A1::待确认、A2::待确认 |
| 推翻 | **0（单元级）** | 但**两句字面主张被推翻**：①"同一键实际有七个侧"→ 样本键 `drizzle.pixfrac` 复算到 **≥八侧**（A3 已列到八，A1 少一侧）；②"只有一侧决定生产取值"→ **代码兜底自身分裂为两套值两条路径**（`module_adapters.cpp:7506`=1.0 与 `json_config.cpp:712-713`=0.8） |
| 待证 | **0（单元级）／子条 13** | 见"覆盖率自报"的未决名单 |
| 重复 | **1 组** | A2 L-01 ／ A3 §2.2 ／ BD1 L3 ＝ **同一处**（io 面阈值出契约 + `iowait_percent` 生产零赋值）；三案在 D8 须合为一条 |

**子条级（派单三问之②：四个分片"我证伪了机械层结论"的每一句）**

| 分片 | 条数 | 确认 | 降级 | 待证 |
|---|---|---|---|---|
| A1 X-1…X-14 | 14 | 8 | 2（X-3 第四侧存在但 `default` 关键字未取到；X-5 未复算） | 4（X-5、X-7、X-8、X-13 文件路径/符号名未定位；X-14 见覆盖率单元） |
| A2 ①…⑥ | 6 | 3 | 2 | 1 |
| A3 ①…⑥ | 6 | 5 | 1 | 0 |
| BD1 ①…⑦ | 7 | 6 | 1 | 0 |
| **合计** | **33** | **22** | **6** | **5** |

**复算命令（本批全部结论出此，均可重跑）**

```bash
# 基线与只读自证
git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain   # 收工 = 开工 = 两行
git -C "F:/Astro dev/Astro CS Normalization Database" rev-parse --short HEAD # c8f64e9a
# 分片行域（分母自证 4 单元）
cd "独立审计/证据" && wc -l AUD-402-判读-{A1,A2,A3,BD1}.md   # 181/146/280/182
# 侧取证（A1::立案、A3::立案）
git -C "$R" grep -n "defaults\.json" -- lib                      # 14 命中全为注释与 tests ⇒ ②侧不被生产读
git -C "$R" grep -n "\"config\"\|\"blocks\"" -- eng/packaging/config/templates/   # 只有 blocks ⇒ 门 F-1 恒空
git -C "$R" grep -n "get(\"config\")" -- eng/ci/check_config_defaults.py           # :80 与 :157 两处
git -C "$R" grep -n "pixfrac" -- eng/packaging/config lib/infrastructure/scheduler/src/module_adapters.cpp \
    lib/infrastructure/cli/session_commands.h lib/infrastructure/pipeline/orchestrator \
    docs/plugins/algorithms_phase1/08_drizzle.md eng/contracts/schemas/                 # 八侧并列
# 证伪条的锚复算
git -C "$R" grep -n -A5 "\"max_clip_rounds\"" -- eng/contracts/schemas/phase_config_normalize.schema.json  # :320 default=2
git -C "$R" grep -n "sparse_reconstruct" -- eng/contracts/schemas/phase_config_mosaic.schema.json          # :170 default
git -C "$R" grep -n "0\.05" -- docs/science/REJECTION.md            # :284「文献不提供这两个门值」
git -C "$R" grep -n "Moffat\|Project-defined" -- docs/science/PSF.md # :147 Project-defined
git -C "$R" grep -n "bitpix = -32" -- lib                            # Phase3 缺省 FP32
git -C "$R" grep -c "^### 3\." -- ASTROCS_DESIGN.md                 # =3（正例）；"^### 3\.[45]" = 0 ⇒ §3.5 不存在
git -C "$R" grep -n "iterativeClipSigma" -- lib                     # 9.0f@orchestrator:1569 与 5.0f@module_adapters:3959/:4846
git -C "$R" grep -n "CHECK(props.find(\"hips_pixel_scale=412.258369\") == std::string::npos)" -- eng  # 负例指纹
git -C "$R" grep -nE "iowait_percent *[=*]" -- lib                  # 只有 :182 声明 ⇒ 生产零赋值（正例 eng/tests 9 处/5 文件）
# D4 三个数（附加判定）：读 CSV 文本，不 import 仓库件
cd "独立审计/复算件/V402" && python count.py
```
收工 `git status --porcelain` 原文（与开工逐字相同，无新增项 ⇒ 取证未污染被审对象）：
```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 旧判偏松清单（＝ 上一轮给下游的虚假授权）

1. **A1 §1 headline"只有一侧决定生产取值"** ⇒ 下游若据此只改**一个**兜底点，会留下 scheduler(1.0)／orchestrator(0.8) **双轨未收敛**，产品仍按路径给两种值。**授权作废**：改值必须两侧同批。
2. **"七个侧"作为侧数被写进结论** ⇒ 少一侧（插件文档默认列 08_drizzle.md:50 真实存在），而侧数正是这项主张的量化落点；D4 的"六侧实测摘要"列沿用会延续少一侧。**授权作废**：本批裁为"≥八侧"。
3. **A1 §2.4 的"59 条注册字段只有 4 条真锚 = 6.8%"未经独立复算即被下游引用** ⇒ 它撑的是 D4 主表"出处"列 48 行"文献值"的可信度判断。我未复现（自有脚本产物）。**授权状态：待证，不得据以升降档**。
4. **读者件"具名已判 412"** 未写明 **412 = 逐点判读 354 ＋ 同名记入 58** ⇒ 下游会把它读成 412 次独立判读（覆盖虚报 14%）。**收紧后才是授权**。
5. **"18,883 行与主表同行"若被读作"整行相同"** ⇒ 实际 26 行同键而 `现行值`/`来源现状`/`备注` 三列不同（样例 `frame_snr`、`vals`），拿旁表读值会读到与主表不同的数。**授权作废**：读数只认主表。
6. **A2 的 8 条 UNRESOLVED 按 enforce（假绿/逃逸窗）语义入册** ⇒ 已证资源判据恒 `RecordOnly`（`resource_gate.h:268-269` ＋ 契约 `:75` 与 `:82`），只影响记录面；A3 自己已降一档而 A2 未降。**定级虚高，须按 A3 §5.6 统一**。
7. **BD1 §3 的"实际覆盖 ≈92 位点"** 含 42 个队列外邻居 ⇒ 若被并入任何分母即双计。**只可当扩面线索**。
8. **各分片"已判 N 行"被当成"N 次独立取证"** ⇒ A3 的 124 行里 79 行共用 6 条锚、读者件自报 233 组平均 8.09 符号/组。真实授权单位是**簇**。

## 覆盖率自报

- **已判 20 / 分配 20**（清单 20 个复核单元全部有判定行），另加派单三问的附加判定 1 节。**非空未判名单：无。**
- 单元内未收敛的子条（按 §3 保留不删，各附补法）：
  - A1 X-5 / X-7 / X-8 / X-13（4 条）：缺**文件与符号名定位**；补 `git ls-files "*spatial_gain.cpp" "*star_matcher.cpp" "*rejection.cpp" "*sdet_api.cpp"` 后按 A1 给的行号读 ±3 行。
  - A1 §2.4 的锚点计数（6.8%／21 DIFF／46 NO-VALUE）：缺**双锚比对的独立复算**；补法见"偏松清单"第 3 条。
  - A2 L-05（USER_HZ）、L-11（CPU 跨刻度）：缺 `monitor.h:204-205/:257-258/:277-278` 与 `commands.cpp:915` 的原文三行比对。
  - A3 L1 触发路径中间跳 `runtime_client.cpp:217`：我用 `"drizzle"` 零命中，缺**节点名的拼装方式**（很可能多字面量拼成，属已知假阳来源）。
  - BD1 L2 `sigma_floor` 三侧（1e-3／0.02／1e8）：一条 `git grep -n "sigma_floor" -- docs/contracts/DATA_SEMANTICS.md lib/algorithms/coverage eng/tests` 即可闭合，本单元命令数到限未做。
- 本批**未跑**任何 `eng/**` 脚本、构建、`ctest`、端到端；未 `import` 仓库内 Python；未改 D4 主表、未碰 `独立审计包/` 与 `独立审计/批次清单/` 共享台账；工作件只一个：`独立审计/复算件/V402/count.py`。

## 我对第①层判据本身的校准建议

1. **侧集合要定死成枚举，且"该侧不存在"必须附检索式**：A1 七侧、A3 八侧、我在 `hips_pixel_scale` 上又摸到第三个夹具值（`eng/tests/io/make_hips_fixture.py:169` = 11.519173063162576，既非度口径也非角秒口径）——**同一批次里"侧数"就是四分片四样**，而侧数本身就是判据的量化落点。建议固定为八侧枚举（代码兜底／defaults.json／出厂模板／CLI 骨架／schema／合同登记／文档默认列／构建期注入），并把"判某侧不存在"的检索式随件登记；我自己两次因路径猜错得到**假零命中**（`eng/contracts/config_registry.json`、`lib/algorithms/photometry/src/`），证明这条纪律不是形式。
2. **前言 §2 第 5 条的"触发路径"应加一条分支**：配置类判据光给"从入口到读键点"不够，必须声明**同一命令是否存在多条生产路径**（pixfrac 的双兜底正是漏在"路径唯一"这个隐含假设上）。
3. **门的自检要从"锚点在不在"升级为"侧齐不齐"**：`check_config_defaults.py` 的 `ANCHOR_STALE` 只防"整个扫描零命中"，防不住"少扫一侧"（模板侧、骨架侧双双在面外而门绿）。⇒ 门的正/负例里应有一条**注入"某侧被移除"的负例**。
4. **判读层自报"独立锚数"而不是"行数"**：D4 主表若加一列 `共用锚组`，读者就不会把 354 行读成 354 次取证（读者件 §1.3 已有 233 组/8.09 的算法，直接沿用即可）。
5. **机械层预筛的四条系统性缺陷合并为一个整改项**（本批**四路独立**各自命中，证据强度最高）：①注释/文案/schema description 里的章节号与公式文字被当数值（A1 X-6、A2 §6.2、A3 §5.1、BD1 证伪②）；②断言与 CHECK 实参里的字面量漏抽（BD1 证伪⑦，实测漏掉真判据 5e-3 而登记了累加器 0.0）；③一行多赋值/多声明被压平成单值（BD1 证伪④）；④泛名不带词边界致计数失真（BD1 §5(2)：55 vs 1,755，32 倍）。
6. **定级纪律的一条正例值得推广**：A3 §5.6 主动把本队列 L2-L18 整体降一档（因恒 `RecordOnly`），而 A2 未做——同一事实两路两种定级 ⇒ 建议第①层在派单里明确"**判据是否进退出码**"为立案必答字段（我复算到的 `resource_gate.h:268-269`＋契约 `:75/:82` 三处证据可直接作为该字段的判据模板）。
7. **一条应上呈负责人的结构性问题**（前言 §10 第三类）：契约 `eng/contracts/resource_gate_v1.json:76` 把"一般性资源超限门取消"的**退役依据**写成 `ASTROCS_DESIGN.md §3.5/§6.3`，而这两个条款号在权威文件里**不存在**（§3 只有 3.1-3.3；§6 整章是 export，CLI 合同是 §7）⇒ 一条已生效的门退役挂在一个悬空指针上。A2 L-09 发现了错号、A3 §2.6 发现了退役，但**两路都没把这两件事连起来**；合起来才构成需要负责人裁决的"退役是否仍成立"问题。

<!-- PROGRESS: 20/20 -->

