# AUD-101 · DB-14 文档审计成稿（`实验/absolute-snr/` reverse_verify README + data/README + EXP-01/02/03）

批次：`DB-14`｜清单：`独立审计/批次清单/DB-14.txt`（7 份）｜冻结基线 HEAD=`c8f64e9a`
判据已读：`DISPATCH-DOC-PREAMBLE.md`、`standards/01_文档权威链整治标准.md`（全文）、`standards/02_科学证据分级与三重佐证标准.md`（全文）、`tasks/AUD-101`、`templates/TPL_文档审计台账.md`、`ASTROCS_DESIGN.md` §0/§2.2/§8 目录/§12.1–§12.5。

**派单与正本的一处口径冲突（先登记）**：派单写"报告必须七要素齐"并列出八项；正本 `ASTROCS_DESIGN.md:761` 写"实验报告**八要素**：可证伪假说、方法（公式/判据）、数据来源与生成方式、结果（真实数字+不确定度+对照）、可判定结论（成立/不成立/证据不足）、诚实边界（适用域/反例/未验证部分）、复现命令、佐证来源"。本批按**八要素**判定（以正本为准）。

开工基线（`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 1. 逐份登记

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `实验/absolute-snr/code/reverse_verify/f_instr/README.md` | F-INSTR — 星点通量口径调研与定案 (RELEASE-02 裁决 A6) | 47 | 研究与佐证（工作区说明）；其"结论一句话"实际承载科学口径定案 → 该段应属科学定义层（`docs/science/`）经 `docs/f-instr-canon.md` 下钻 | 星点通量口径 `F_instr` 的 seeing 敏感度与孔径无关性 | 缺（开头只给工作项 ID 与两份仓内报告路径，未标最高设计条款；机械列 upstream_pointer=False 一致） | 0（`git grep -n "f_instr/README"` 全仓 0 命中；工作区目录被 `docs/f-instr-canon.md:3,500-504`、`results/REVERSE_VERIFY_CANON.md:46`、`实验/additive-sky-seamless/code/reverse_verify/m16_scene/README.md:44`、`实验/shared/REVERSE_VERIFY_MIGRATION.md:17` 引用） | 否（正本 = `docs/f-instr-canon.md`） | 是：与 `docs/f-instr-canon.md:14,16` 及 `results/REVERSE_VERIFY_CANON.md:21-22` 三处重复同一结论，且**数字不一致**（本行 17 `≤ 0.005 mag` vs canon `:16` `M_seeing ≤ 0.004 mag`） | 有（机械列漏报）：`:3` `> 逆向验收工作区工作项 **F-INSTR-SURVEY**`＝工作项 ID＋裁决编号块 | `:1` 标题含任务编号 `RELEASE-02`；`:3` 工作项 ID 与"裁决 A6"；`:14` 源码行号锚 `star_detector.cpp:139-151`（已漂移，实测 `m00` 在 `:214-230`）；`:45` "按负责人令的替代条款" | `:46` `run/RELEASE-02/L4-rebuild/norm/*/calibrated_*.fts`（`run/` 不入库，HEAD 无此路径）；`:41` "见报告 §1 核对状态"——`docs/f-instr-canon.md` §1 标题为"调研结论（文献与开源科学软件）"，无"核对状态"节 | 无与最高设计 §2.2 的实质冲突（"改用 PSF 总通量"与 §2.2 的 PSF 拟合域口径一致）；与同单元 `data/README.md:3` 的"仓内既有资产"表述**互相矛盾**（本行 45 断言"本仓无哈勃/HST 数据"） | 合并：把 `:13-17` "结论一句话"段并入正本 `docs/f-instr-canon.md`，本文件只留文件表＋复跑＋底数据声明（标准 01 §2"同一角色内同一主题只有一份正本"、§4"以引用方式服务正本，不重复承载正本口径"） |
| `实验/absolute-snr/code/reverse_verify/frame_snr/README.md` | frame_snr —— 帧级 SNR 定案的实验与红线测试 | 56 | 研究与佐证（工作区说明＋复现规程） | 帧级 SNR 红线测试（解析 T8–T12、物理 P8–P14）与生产盘点 | 缺（`:6` 只给仓内报告路径，无最高设计条款） | 1：`eng/ci/ledgers/naming_surface.json:456`（机器登记表，非文档指针）；工作区目录被 `docs/frame-snr-canon.md:4-5,208-209,463` 引用 | 否（正本 = `docs/frame-snr-canon.md`） | 无实质重复（`:6` 指向 canon/survey，属合法分层下钻）；`:39-43` 的"判据先行纪律"与 `docs/f-instr-canon.md:3.2 判据先行` 同口径但非同一主题正本，属单元内通则重复叙述 | 有（机械列漏报）：`:3-6` `> 工作项 **FRAME-SNR-CANON**（RELEASE-02）`＋构建登记申明＋报告路径块 | `:3` 任务编号 `RELEASE-02`；`:54` 数据文件名内嵌日期 `20251212`（机械列误报为 commit_sha）；`:56` "本目录**不修改** `lib/` `docs/` `eng/tests/` `ci/`，**零 git 写**"＝纪律声明 | `:4-5` `reverse_verify/CMakeLists.txt`——**文件不存在**（`git ls-files "实验/absolute-snr/code/reverse_verify" \| grep -i cmake` 0 命中；`ls` 该目录仅 4 个子目录；全仓 `git ls-files 实验 \| grep -i cmake` 0 命中）；`:54` `run/RELEASE-02/perf-drz/norm_t2_m1_red/cleaned_*.fts`（`run/` 不入库）；`:49` 产物四项全在 `run/`（未入库） | 无与最高设计 §2.2 的冲突；`:53` "全仓 `find -iname '*hst*'` 命中 0"与 `testdata/README.md:55` 已登记 `HST_M16/` 只读数据集的口径**未对齐**（本文件与 `data/README.md`、`docs/EXP-01/02/03` 对"有无 HST 数据"给出四种表述，互不引用） | 保留（就地，本目录唯一的工作区说明）；须修 `:4-5` 悬空 CMakeLists 申明、`:49` 产物清单漏 `branch_discriminator.json`、补 seed 与"依赖 `run/` 生产产物"前置声明 |
| `实验/absolute-snr/code/reverse_verify/snr_design/README.md` | SNR-DESIGN 数值实验 | 60 | 研究与佐证（实验清单＋判据规格） | 支撑 SNR 传播设计的 5 个数值实验与 6 个复核脚本 | 缺（`:3` 只指向仓内设计文档，未标最高设计条款；机械列 upstream_pointer=False 一致） | 0（`git grep -n "snr_design/README"` 全仓 0 命中；工作区被 `docs/snr-propagation-design.md:4-5,1101,1227,1302-1308,1343` 引用） | 否（正本 = `docs/snr-propagation-design.md`） | 是：`:25` "解析式与 MC 必须 5 位小数一致；共模 ratio 必须恰为 1.0"与 `docs/snr-propagation-design.md:23-24` "`Var_approx/Var_opt = 1 + Var_p(δ) + O(δ³)`…（EXP-1，MC 与解析在 5 位小数一致）…共模权重误差完全免费（实测 ratio = 1.000000）"同一论断两处落点；`:27` 的 0.063%/`audit_exp3_physical.json` 结论亦在 `results/REVERSE_VERIFY_CANON.md:38` 第三次落点 | 有（机械列漏报）：`:6-8` `> **复核分片 SNR-EXP-AUDIT**` 元信息＋"原实验脚本与 JSON 一律保留不改"历史申明 | `:1` 无日期；`:7-8` 条款号引用与"原结论/复核后并列"的历史叙事；`:59` `GAP_AUDIT §9.42`；`:22` 表头"关键判据（先写后跑）"合规；`:48-49` "EXP-3 首轮因此静默失败（`need at least one array to stack`）"＝过程流水 | `:7` `GAP_AUDIT §9.41/§9.42/§9.46`——**条款不存在**：全仓唯一 GAP_AUDIT 正本为 `工程控制/RELEASE-05/GAP_AUDIT.md`，其节次仅 `G0–G4`；`:59` 同上；`:44-46` `run/RELEASE-02/L4-rebuild/norm/<tile>/calibrated_*.fts`、`p1_snr.json`、`p1_sources.json` 全在 `run/`（不入库）；`:18` 结果落 `run/`（未入库） | 与 `docs/EXP-01` §7 的"行号会漂移一律按符号名定位"无冲突；与 `ASTROCS_DESIGN.md` §2.2「链路必须接通」一致。**与 AGENTS.md §7/§6 落位规范冲突**：`:19` "在本目录 / `audit/` 留一份小 JSON 作为证据"——实际 10 份结果 JSON 与 `exp3.log` 入库在 `code/reverse_verify/snr_design/`，而 §12.3 规定实验结果落 `results/` | 保留（就地，判据规格属实验单元自身职责）；结论列改为指向 `docs/snr-propagation-design.md` §0/§13 的纯指针（标准 01 §2 研究与佐证类"不重复承载正本口径"），并把 `GAP_AUDIT §9.4x` 换成现存正本条款（`docs/science/PHOTOMETRY.md` §10） |
| `实验/absolute-snr/data/README.md` | SCI-B 数据指针（只读声明） | 38 | 操作与运营（数据取得与只读纪律） | 三类实验数据的取得路径、处理口径与复现入口 | 缺（`:3` 只写"本单元不新增、不复制、不修改"，未标最高设计 §12.2/§12.3 条款） | 2：`实验/absolute-snr/README.md:3,249`、`实验/absolute-snr/REPORT_paper.md:137` | 是（数据指针主题在本单元唯一） | 轻度重叠：`:28` 与本单元 `README.md:63` 都写 `1 px 棋盘 hold-out（fam=(y+x)%2）…族 0 估计/族 1 真值`；两处均给 `eps_ref` 推导（本行 29 vs `README.md:63`）。按 §12.3 目录分工属合法分层，但 `eps_ref` 口径写了两遍 | 有（机械列漏报）：`:1` 标题即"（只读声明）"，`:3` 整段是纪律申明块 | `:17` `seed=20260921`（与 `code/sci_b_common.py:20 SEED_BASE = 20260921` 一致，属可核常数而非日期；机械列把它记为 dates 之一）；`:38` `run/SCI-402/` 任务编号 | `:9` `testdata/HST_M16/hlsp_heritage_hst_wfc3-uvis_m16_f657n_v1_drz.fits`——HEAD 未跟踪（`git ls-files testdata \| grep -i hst` 0 命中），本机该目录为空；`testdata/README.md:19` 明写 `HST_M16/*.fits（3 × 268,917,120 B = 770 MB）\| ❌ 不入库`；`:38` `run/SCI-402/`（不入库） | **`:3` "全部数据以只读方式引用仓内既有资产"与 AGENTS.md §7 及 `testdata/README.md:19` 的入库策略冲突**：第 1 类（HST）与第 3 类（M42 `.fts`）大文件按政策永不入库，故不是"仓内既有资产"；且未给取得数据的指针（应指向 `testdata/README.md` §3） | 保留（就地，§12.3 规定的 `data/` 指针位）；须把 `:3` 改为"引用仓内文档与仓外只读数据集（取得见 `testdata/README.md` §3）"，并补登 EXP-01/02/03 与 reverse_verify 的实际输入（当前只登记 b1–b7 系列，未登记 8/16 帧 M42 与 `run/` 生产产物） |
| `实验/absolute-snr/docs/EXP-01-DELTA-AND-ESTIMATOR.md` | EXP-01 —— 天光语义容差 δ 与逐源 SNR 估计量错配的**实验判定** | 772 | 研究与佐证（B 级实验报告，八要素齐备） | 天光语义容差 δ 的证据性取值 + 逐源 SNR 估计量方案 A/B 判定 | **缺**（开头 `:3-11` 元信息块只给"任务单元 SCI-B-EXP-01"与负责人指示；`ASTROCS_DESIGN §2.2/§3.1/§12.2/§12.3` 迟至 `:716` §8 才出现，且只给章节号无标题；机械列 upstream_pointer=False 一致） | 4：`实验/absolute-snr/README.md:285,333`、`docs/EXP-02-STRUCTURE-CONTAMINATION.md:4,757`、`docs/EXP-03-REGIONAL-SIGMA.md:947`、`eng/ci/ledgers/naming_surface.json:458`；**`docs/` 正式文档层 0 引用**（`git grep -n "EXP-01" -- docs` 0 命中） | 是（δ 取值与 A/B 判定在本单元唯一） | 与 `docs/science/NOISE_MODEL.md §5a` 是"被订正对象"关系而非重复（`:33`、`:613` 指其 `1.44/√N` 用错对象）；与 `docs/plugins/algorithms_phase1/06_photometry.md §4` 是"实验证据支持既有规范"关系（`:127`、`:560`、`:623`），属合法分层。**与本批 EXP-02 `:154-166` 存在真实重复**：同一 16 帧 M42 的 σ̂_prod/保留比表在两处各写一遍（本文件 `:211-220` 8 帧 vs EXP-02 `:145-152` 同 8 帧同数值） | 有（机械列漏报）：`:3-11` 整块为元信息＋纪律声明（"未运行任何 ACSD 可执行文件／未使用 `ulimit -v`／未修改 lib／未执行 git 写"） | `:5` 负责人指示原文引述；`:6` `seed：20260924`；`:3` 任务编号 `SCI-B-EXP-01`；`:95,112,725-767` 整节 §9 独立审稿与修正记录＝历史叙事（"初版 §2.6 已被证伪并按 §2.6 重写"、"已改"、"审稿期间本报告被并发改写"）；`:685-696` "未核实"清单编号**重复**（两个 13、两个 14，且缺 15 前的连续性，17/18 落在另一子表）；`:758` 修正表称"S9 已改（RSS 962→961 MB）"但正文 `:651-652` 仍写"3:48~4:10、峰值 RSS 959~962 MB"——修正动作与正文现状不符；`:630`/`:648` 用 `` `~~~bash` `` 反引号包裹围栏，渲染后代码块失效 | `:24` `run/SCI-SNR-01`、`:40` `run/SNR-REVIEW-01`、`:594`/`:719` `run/SCI-SNR-01/REPORT.md`、`run/SNR-REVIEW-01/REVIEW.md`、`run/SNR-ESTIMATOR-01/REPORT.md`、`:328` `run/PERF-401/work/p1_blk{1..4}/p1_sources.json`、`:652` `run/SCI-B-EXP-01/logs/exp01_run_all.log`——`run/` 全部不入库，HEAD 无一份副本；`:720` `testdata/HST_M16/hlsp_heritage_hst_wfc3-uvis_m16_f657n_v1_drz.fits`（未入库）；`:737` `p1snr_science_test.cpp:209`（按行号引用测试，符号名未给） | 无与上位冲突：`:560`/`:623` 的判定明确以 `docs/plugins/algorithms_phase1/06_photometry.md §4` 冻结规范为准；`:506` 引 `ASTROCS_DESIGN.md §3.1` 作判据依据，引用与条款一致。**唯 `:761` 的 §12.3 八要素被本文件自身满足**，而其"约束遵守"段（`:7-9`）与 AGENTS.md §6 重复叙述，属载体错放 | 保留（本批最高价值件，δ 与 A/B 判定的正本）：把 `:3-11` 元信息块换成一行上游指针（`ASTROCS_DESIGN.md §2.2 创新点二：跨帧可用的绝对信噪比`）、把 §9 审稿修正记录移出正文（标准 01 §4 禁历史叙事；证据需要留证则落 `artifacts/evidence/` 并由正本引用）、把 `run/` 引用改为落档副本或显式标注"仓外不可复核" |

## 2. 逐份细则（本批特有判据）

### 2.1 `实验/absolute-snr/code/reverse_verify/f_instr/README.md`

- **八要素缺项**：缺①可证伪假说（全文无假说与证伪条件，`:10` 只问"`F_instr` 该用哪种口径"）；缺④结果的不确定度与对照（`:15-17` 给三个数字，无误差、无对照组，负例只在 `:27` 的文件表里以一句话提及）；缺⑧佐证来源（无文献、无开源项目/版本/文件:行，`photutils`/`sep` 仅以"未安装"出现）。要素②③⑤⑥⑦（方法/数据/结论/诚实边界/复现）在。
- **判据非退化性**：`README:27` 声明 `exp3_seeing_null.py` 是"seeing 负例：真值无效应 ⇒ 度量必须归零（能红能绿，判据写死在代码里）"，`README:28` 声明 exp4 含"正对照 + 负例 + 联合"。**声明合格，但仓内无法核验**：本工作区全部产物写 `run/reverse_verify/f_instr/`（`f_instr/exp0_scene_and_noise.py:19` 等 6 处 `OUT`），results/ 无一份对应 JSON ⇒ 能红能绿的实际读数无落档。
- **关键数字落档**：`−0.72 mag`、`假增益 0.52×`、`7.78/4.93/3.81/4.25%`、`≤0.005 mag`、`seeing 2.0→4.0 px`——**在 results/ 全部归档件中 0 命中**（`git grep` 只命中本文件、`docs/f-instr-canon.md:14,16,356`、`docs/surveys/f-instr-survey.md:484`、`results/REVERSE_VERIFY_CANON.md:21`，均为文档散文，非实测产物）⇒ 结论无落档支撑。**且两处文档数值互相矛盾**：本文件 `:17` 写 `≤0.005 mag`，`docs/f-instr-canon.md:16` 与 `results/REVERSE_VERIFY_CANON.md:22` 写 `M_seeing ≤ 0.004 mag`。
- **seed 与复现命令**：复现命令在（`:36-39`），`run_all.sh` 存在且路径自洽（`HERE/../../../../..` 回到仓根）；**但 README 未声明任何 seed**（代码里是分散的硬编码常量：`exp1_recovery.py:53 seed=4242`、`:62 seed0=1000`、`:99 seed=11`、`:119 seed0=9001`，无统一 seed 说明）⇒ 不满足标准 02 §1 B 级"固定 seed"的**可查性**。**命令不可执行**：`README:41` 称"全程约 2.5 分钟（exp0–exp5）"，但 `exp5_psf_shape.py:52` 把输入目录写死为 `OUT = "/workspace/Astro CS Database/run/reverse_verify/f_instr"`——绝对路径＋**另一个仓名**（本仓为 `Astro CS Normalization Database`），exp0 写入的是仓根相对 `run/reverse_verify/f_instr/`，故 exp5 在 `run_all.sh` 里必然 `FileNotFoundError`；`docs/f-instr-canon.md:500-501` 还停留在"run_all.sh = exp0-exp4，exp5 单跑"，与 `run_all.sh` 实际循环（exp0..exp5）也不一致。违反 AGENTS.md §3"在仓库内工作，不写死服务器绝对路径"。
- **是否被正本当作 A/B 级证据引用**：**否**。`git grep -n "F_instr\|f_instr" -- docs/science docs/algorithms` 命中的是同名科学量讨论，未把本工作区或其报告列为证据锚；本工作区结论只在本单元内部流转（`docs/f-instr-canon.md` → `results/REVERSE_VERIFY_CANON.md`），且 `results/REVERSE_VERIFY_CANON.md:26` 把它对接到 `docs/science/PHOTOMETRY.md:95` 作**符合性对照**（是引用别人，不是被引用）⇒ 相对正式科学链为**孤立单元**。

### 2.2 `实验/absolute-snr/code/reverse_verify/frame_snr/README.md`

- **八要素缺项**：缺①可证伪假说（无假说条目，只有"判据先行的纪律"）；缺④结果（无任何数字，PASS/FAIL 与阈值全在 `run/` 里的 JSON）；缺⑧佐证来源（`:16` 提"与 photutils / sep / SExtractor(源码级) 对拍"，但无项目+版本+文件:行；`:53` 反而以"全仓 find 命中 0"作论据）。②③⑤⑥⑦在（⑤结论缺席但指向 canon，⑥为 `:51-56` 诚实登记）。
- **判据非退化性（合格，且有可读证据）**：`:40-41` "绿例（canon）与红例（未扣背景／功率比／未扣背景窗口）用**同一个** `monotone_criterion()` 判，红例必须被否决"——代码属实：`run_redlines.py:42-43` 定义"唯一判据（同一函数同时用于绿例与红例）"，`run_redlines_physical.py:56-57,210` 的 P8 门是 `monotone_criterion ∧ agree ∧ unbiased ∧ asym_ok` 四条件合取，红例（`:136`、`:354`）以同一函数被否决 ⇒ 非恒真。`:42-43` 还显式登记一处例外并把 P8 的 `B→∞` 极限改由解析式断言，理由（`B>=10^6` 时单次实现散度恒为 1，有限次 MC 中位数分辨不出）与 canon §3.1 对应，属如实收窄而非放宽。
- **关键数字落档**：本文件不列实测数字，只列判据与产物名，故无"数字无落档"问题；但 `:49` 产物清单**漏一项**：代码还写 `branch_discriminator.json`（`branch_discriminator.py:29,169`），而 `run_all.sh` 确实执行该脚本 ⇒ 清单与实现不符。`docs/frame-snr-canon.md:421,453` 反过来把 `run/reverse_verify/frame_snr/{branch_discriminator,p1_snr_inventory}.json` 的读数（0.023–0.533、13/13、31/32、快照 2026-09-19T11:50:14Z）当证据引用——**这些读数在仓内一份都不存在**（`run/` 不入库），故 canon 侧的"结论无落档支撑"根子在 `:47-49` 的产物落位决定。
- **seed 与复现命令**：复现命令在（`:24-27`），但 **README 未声明 seed**；代码 `run_redlines_physical.py:53 SEED = 20260919` 且 `:570` 写入产物，说明 seed 存在但未写进文档 ⇒ 判"seed 未写明"。**前置依赖未声明**：P13 与生产盘点/分支判别都读 `run/RELEASE-02/**`（`run_redlines_physical.py:439-440` 两条候选路径；`inventory_p1_snr.py:61,129` 扫 `run/**/p1_snr.json`、`p1_sources.json`；`branch_discriminator.py:37`），fresh clone 上无这些产物 ⇒ README 的"一键复跑"实际只有一半能跑，且未告知缺件行为。另 `crosscheck_photutils.py:37` 的 `--out` 默认值是相对路径 `run/reverse_verify/frame_snr`，而 `run_all.sh` 不 `cd` 到仓根，从别处调用会把产物写到调用目录下的 `run/`（其余脚本用 `REPO` 锚定，不受害）。
- **悬空引用**：`:4-5` `reverse_verify/CMakeLists.txt`——**不存在**（`git ls-files` 与 `ls` 双向确认：`code/reverse_verify/` 下只有 `f_instr frame_snr p7_noise snr_design` 四个目录，全仓 `实验/**` 无 CMakeLists.txt）。该句不但指向缺失文件，还把"本目录未登记进构建系统"这一决定建立在缺失文件之上 ⇒ 决定无依据（`cpp/p1snr_probe.cpp` 的实际构建方式由 `run_all.sh` 之外的 `g++` 直编，与该申明无关）。
- **是否被正本当作 A/B 级证据引用**：**否**。`docs/science/`、`docs/algorithms/` 里出现的 `frame_snr` 全是**数据对象名**（`docs/contracts/UNIFIED_OBJECTS.md:47`、`docs/design/UNIFIED_MODEL.md:43`、`docs/algorithms/PHASE2_SAMPLER.md:384-397` 的 `frame_snr_med`），与本工作区无关；`docs/plugins/algorithms_phase1/07_noise_snr.md` 未引用本工作区路径。本工作区的下游只有一个机器登记项 `eng/ci/ledgers/naming_surface.json:456` ⇒ **孤立单元**（其结论经 `docs/frame-snr-canon.md` 后也未进入 `docs/science/` 正本）。

### 2.3 `实验/absolute-snr/code/reverse_verify/snr_design/README.md`

- **八要素缺项**：缺①可证伪假说（`:23-29` 的"验证的论断"是待验命题而非带证伪条件的假说）；缺④结果（全篇无一个实测读数；数字只出现在 `:36-40` 复核分片的一句话摘要里，如"0.063% 的窗口敏感性"、"算术加常数 = 0.0000%"）；缺⑧佐证来源（`:7` 与 `:59` 引 `GAP_AUDIT §9.41/§9.42/§9.46`，但该条款体系在仓内不存在，见下）。②③⑤⑥⑦在（⑤是 `:3` 与设计文档 §13 的结论承接，⑥为 `:48-49,52-60` 陷阱登记）。
- **判据非退化性——两处恒真门，单独立案**：
  1. **`共模 ratio 必须恰为 1.0`（`:25`）＝恒真门**。权重归一化 `w → w/Σw` 后，对所有帧同乘一个因子 `c` 的权重向量给出**逐位相同**的归一化权重，于是 `Var_approx/Var_opt ≡ 1` 是代数恒等式，与估计器实现是否正确无关。归档件坐实其恒等性：`exp1_weight_penalty.json` 的 `C1_common_mode.common_only_ratio_mean = 1.0`、`common_only_var_p = 4.49e-34`、`C1_penalty_scan[0]` 在 `rel_weight_err_rms = 0` 处 `ratio_mc_mean = 1.0`／`ratio_mc_p95 = 1.0000000000000002`。**该门的上位文档自己已判定它无区分能力**——`docs/snr-propagation-design.md:24-25` 原文："共模权重误差完全免费（实测 ratio = 1.000000）——该定理对绝对与相对两种表示同样成立（绝对表示根本不产生该因子），因此**没有区分能力**"。本 README 把同一 ratio=1.0 列为"关键判据（先写后跑）"却**不带该限定** ⇒ 按标准 01 §1（下位与上位冲突以上位为准）与 §4，本行是以恒真门冒充判据的缺陷，且与上位口径倒挂。
  2. **`天光红线（算术加常数 = 0.0000%）`（`:36`）＝恒等式型负例，作为"红线"无证据资格**。`audit/audit_sim_validation.json` 的 `S_sky_red_line.rows` 在 `delta_sky_adu = 0/50/150/400` 四档下 `armA_sigma_bg_adu = 13.924816230906277` **逐位不变**、`armA_snr_change_pct = 0.0` 恒成立：算术加常数被"两次独立实现差分求 σ_bg ＋ 平面背景扣除"在构造上完全吸收，故 0.0000% 由构造给出。作为"真值无效应 ⇒ 度量归零"的负例形式合规（§12.2 要求的正是这种归零），但把它称作**天光红线**（即"天光进入 SNR 的正确通道"的证据）不成立；真正有判别力的是同件的 armB（光子天光：`delta_sky=400` 时预测 −37.74% vs 实测 −37.19%）与 `armA_naive_snr_change_pct`（27.7%/83.0%/221.3%，即"把天光算进信号"的错误口径会显著起变）。README 只留了恒真的那一半，把有判别力的一半省掉 ⇒ 判"结论呈现口径错配"。
  3. 其余门（`:25` 解析/MC 5 位小数一致、`:26` 打乱控制点 RMSE 变差、`:28` nugget 必须物理、`:29` 每一项必须有证据来源否则红）形式上均带"无效应/反例"面，`exp4_kriging_scaling.py`/`audit/audit_sp0.py` 的负例在归档件里可见（`audit_sp0.json` `A_algebra.max_abs_error_exact_form = 4.44e-16`、`max_abs_error_document_form = 0.139`），属真判据。
- **关键数字落档**：本 README 的"0.063%""约 6 分钟""(N+1)/(N−1)"等：0.063% 与 6.8% 出现在 `audit/audit_exp3_physical.json`、`docs/snr-propagation-design.md §13.4.1` 与 `results/REVERSE_VERIFY_CANON.md:38`；`REVERSE_VERIFY_CANON.md:38` 同时给出订正后值（窗口换成 70× 之差、理想标量惩罚 0.0002%、稀疏层 0.175%/0.088%）——即 README 以**已被复核订正的旧数**作为"复核分片内容"描述，读数口径落后于同单元结论页 ⇒ 判"与同单元结论页不一致，需以正本为准改写"。
- **seed 与复现命令**：复现命令写明（`:12-16`）且脚本存在（`run_all.sh`、`audit/run_all_audit.sh` 均在）；**seed 未写明**：代码 `exp1_weight_penalty.py:31 default_rng(20260919)`、`exp2_sparse_snr_reconstruction.py:290 default_rng(7)`、`exp5_error_budget.py:36 default_rng(11)`，exp3/exp4 无随机性；**10 份证据 JSON 全部无 `seed` 字段**（逐个 grep `"seed"` 0 命中）⇒ 落档件不自证可复现。
- **悬空引用**：`:7`、`:59` 的 `GAP_AUDIT §9.41/§9.42/§9.46`——确认不存在：仓内唯一 GAP_AUDIT 正本 `工程控制/RELEASE-05/GAP_AUDIT.md` 只有 `## G0 … ## G4`＋"本包不做"，`grep "9\.41\|9\.42\|9\.46"` 0 命中；被引的 `工程控制/RELEASE-02/`（`docs/snr-propagation-design.md:201` 给出的完整路径）整体不存在（`git ls-files 工程控制/` 仅 33 项，全属 RELEASE-05）。同一规则的**现行正本在** `docs/science/PHOTOMETRY.md:14,271`（§10 禁止用物理闭合式反推仪器参数），README 与该规则的实际权威源失联，只挂一个已消失的条款编号。`:44-46` 的三份输入（`calibrated_*.fts`、`p1_snr.json`、`p1_sources.json`）在 `run/RELEASE-02/`，不入库 ⇒ 本工作区的 exp2/exp3/audit 三个臂在 fresh clone 上不可跑。
- **产物落位**：`:19` 自述"在本目录 / `audit/` 留一份小 JSON 作为证据"，实际入库 `exp1..exp5.json`（5 份，共 35 KB）＋`audit/*.json`（5 份，共 88 KB）＋`exp3.log`，全部在 `code/reverse_verify/snr_design/` 下——与 `ASTROCS_DESIGN.md:760`（实验单元 `results/` 目录承载结果）和 AGENTS.md §7 目录速查（结果落 `results/`、日志落 `run/<task>/logs/`）不符；`exp3.log` 是把运行日志入库跟踪的实例。
- **是否被正本当作 A/B 级证据引用**：**否（反向引用）**。`docs/snr-propagation-design.md:4-5` 声明"所有数值证据由 `…/snr_design/exp1..exp5` 独立产出"，`:1101`、`:1227`、`:1302-1308` 给出复跑命令——是**同一单元内部**的设计文档指向实验脚本；`docs/science/`、`docs/algorithms/` 对 `snr_design` 0 命中 ⇒ 对正式科学链仍是孤立单元，其结论经 `results/REVERSE_VERIFY_CANON.md` 汇总后亦未上升为 `docs/science/` 的证据锚。

### 2.4 `实验/absolute-snr/data/README.md`

- **八要素缺项**（以"实验报告"要求衡量该件承担的要素）：缺①假说、②方法（只给处理参数，不给判据/公式）、⑤结论；③数据来源与生成方式在（三类数据逐条给路径＋处理＋真值口径，是本批最完整的一份数据节）；④结果只在 `:29` 出现一个不确定度常数（`1.166/ln10/√512=0.0224` dex）；⑥诚实边界在（`:3` 只读声明＋fail-closed）；⑦复现命令在（`:35-36`）；⑧佐证来源缺（不引文献，数据本身也不需要）。⇒ 该件的角色是"数据指针"，缺项**不构成八要素违规**（八要素约束的是报告，见 §12.3），但它被单元 `README.md:3,249` 当作"数据"要素的落点使用，因此**覆盖不全**即成缺陷：只登记 b1–b7 系列输入，未登记 EXP-01/02/03 与 reverse_verify 各工作区实际读的 `run/PERF-401`、`run/RELEASE-02`、8/16 帧 M42。
- **数字与口径核verified（可核部分全部对得上）**：`seed=20260921` ✔ `code/sci_b_common.py:20 SEED_BASE = 20260921`；`1 px 棋盘 hold-out fam=(y+x)%2` ✔ `code/b3_domain_map.py:108-110`；`eps_ref = 1.166/ln10/√512` ✔ `b3_domain_map.py:102 EPS_REF = 1.166/np.log(10.0)/np.sqrt(P_PATCH*P_PATCH/2.0)`（P_PATCH=32 ⇒ √512）；`s=0 即真值无空间效应负例` ✔ `b3_domain_map.py:42 S_FIELD_SYNTH = [0.0, 0.03, 0.10, 0.30]` 与 `:50` 文档串；`三帧 M1/M2/M4` ✔ `b3_domain_map.py:35-38`；`缺件 fail-closed 不静默降级` ✔ `b3_domain_map.py:252 raise RuntimeError("testdata 缺失（fail-closed，禁止静默少面）")`。
- **判据非退化性**：本文件不给判据，只给 `:20` "`s=0` 即'真值无空间效应'负例（帧级臂 RMSE≡0）"——该句是**构造性陈述**：`σ(x)=10^(s·g(x))` 取 `s=0` 时场恒平，RMSE≡0 是代数必然，不是实测；作为负例合规，但 `RMSE≡0` 的"≡"应说明是真值定义而非运行读数（`results/b3_domain_map.json` 里对应值需另行核对，本批不展开，属 DB-15/结果件范围）。
- **悬空引用**：`:9` HST FITS（HEAD 未跟踪、本机空目录；`testdata/README.md:19` 明示不入库）；`:38` `run/SCI-402/`（不入库，任务编号）。`:26` 的 M42 glob 目录 `testdata/M42_T2T3_mosaic_Flying_dutchman/` **本机存在**，但同名 `.fts` 大文件同样不入库（AGENTS.md §7），故第 3 类数据的复现也依赖仓外取得。
- **与上位冲突（本件的核心缺陷）**：`:3` "**全部数据以只读方式引用仓内既有资产**"——与 AGENTS.md §7（`testdata/` 只读数据集索引、FITS 大文件不入库）和 `testdata/README.md:19`（HST_M16 三帧 770 MB ❌ 不入库）直接冲突：第 1、3 类数据的载体**不可能是仓内既有资产**。同时该文件不给取得方式指针（`testdata/README.md:55-58,83` 才有目录说明与取数示例），读者按本文件无法完成复现 ⇒ 判"最高设计 §12.2 三类实验数据的取得链在本件断裂"。
- **是否被正本当作 A/B 级证据引用**：不适用（数据指针件）；其被引用面见登记表（单元 README×2、REPORT_paper×1）。

### 2.5 `实验/absolute-snr/docs/EXP-01-DELTA-AND-ESTIMATOR.md`

- **八要素**：**齐**（①`:95-115` §0.1 H1–H8，每条带证伪条件与判定，并登记一次自查证伪；②`:118-131` 生产代码事实＋`:136-139` 判据式；③`:712-721` §8 数据行；④全文，含 ±SEM/±3σ；⑤`:368-378`、`:547-565`；⑥`:657-708` §7 分"解析可证/依赖仿真假设/未核实"三档；⑦`:628-653` §6；⑧`:712-721` §8）。`§9` 还额外记录独立审稿与逐条修正闭环——内容合格但载体错（历史叙事，见写法违规）。
- **判据非退化性（全批最强）**：`:569-590` §4 列 15 门，逐门给"正常实测＋注入故障＋合格"，`results/exp01_negatives_selftest.json` 坐实 `n_gates=15, n_unqualified=0` 且每门 `normal_green=true / fault_injected_green=false`。G1 归零 −0.0046%（3σ 0.0179%）、G3 RN=0 ⇒ 偏差 `0.000e+00`（JSON `c_negative_controls.rel_dev_rn_zero = 0.0`，故障注入臂 `rel_dev_with_rn = 0.3463` 判红）、G6 决策规则三态 `S_sys=0⇒None; A_eff=0⇒0.01; A_eff>S_sys⇒None`＋"规则恒真"注入判红、G8 z=+88.18 红、G9 z=−1.30 绿、G12/G13 9.98% vs 2.69%。
  - **一处恒等式型负例需点名**：G10 "同帧恒等对照 = `[0.0, 0.0, 0.0]`"（`:500`、`:586`，JSON `gates[G10]`）与 `:502` "解析臂同 seeing 重复计算：伪差严格 0（门 N3）"（`exp01_q2a_pairing.json` `negatives.N3_same_seeing.spurious_flux_* = 0.0`）。二者都是**同一对象自比**：度量定义为同帧同口径两口径之差，恒等情形下必为 0，任何实现（含错实现）都得 0 ⇒ 无判别力，只能证"代码路径没写错变量"。它作为 §12.2 的"无效应⇒归零"形式合格，但**不能支撑 (c) 跨帧可比结论**；真正支撑该结论的是同节 `:501` 的"同 seeing、两次独立噪声实现"伪差臂（盒和 −0.22%，自助 z=−0.008；A +0.06%，z=+0.002）与 G12/G13。判：立案为"负例证据强度被高置"，不判红整节（因为有 arm 完整的补偿对照）。
  - 另记一处门被放宽并有据：G7 容差 5e-3 → 0.05（`:583`、`:749`），理由是该门自身抽样噪声 1.58%、400 次复算仅 22.5% 落在旧容差内 ⇒ 旧绿灯靠运气。修正方向正确（容差≈3×抽样噪声）并写进代码注释，属"门禁本身不合理时改进门禁"的合规样本，登记为正面事实。
- **关键数字落档（逐条命中，本批唯一有完整落档的件）**：
  - `−1.3847%±0.0035%` ✔ `exp01_q1_delta.json a_gaussian_mc[N=16000000].bias_mean = −0.01384689754 / bias_sem = 3.4695e−05`
  - `−1.4279%`（HST pure）✔ `a_hst[0].pure_rel`；`−1.1672%`（配对差）✔ `a_hst[0].paired_rel`
  - 源密度扫描 `−1.428 → −0.532 → +5.022%` ✔ `a_hst_star_scan[0..5].rel`
  - 结构污染 `+2.97% → +16.1% → +165% → +11371%` ✔ `a_hst[*].nostar_rel`（0.0296957／0.1604914／1.6516676／113.71106）
  - 16 帧 M42 表（16.1581／0.9794／5.00%／0.00546…）✔ `a_real[*]`
  - `0.7870%`@N=9216、`1.44/√N=1.5000%`、常数 `0.7555/√N` ✔ `b_stat_mc`（0.007869998867／0.015）与 `se_prod×√N` 三点 75.55/76.62/76.82
  - 弹性 `0.2356 vs 0.6283（2.67×）`✔ `b_elasticity["fwhm=1.6,Be=100"]`
  - 紧致度 `0.963／0.930／0.905` ✔ `c_ambiguity[*].tightness_at_F_ref`；阈值 `49.751244` ✔ `c_threshold_selftest[δ=0.01].Be_over_RN2_shot`
  - `δ*=1.4%`、`S_sys_max=1.4279%` ✔ `decision_A_at_F_ref`／`decision_A_max`／`S_sys_max_used`；δ=1.6% 刀刃余量 0.0004pp ✔ `A_eff(0.016)=0.014283054` vs `S_sys=0.014279294`
  - 两套帧集 0/16 与 4/8 ✔ `c_rho_feasibility_real`／`c_rho_feasibility_real_8frames`（审稿 I1 订正后有独立产物）
  - 方案 A/B：配对 `0.495/0.747/0.866`、`0.985/1.030`、膨胀 `1.95/1.33/1.18`、CRLB `1.452/1.316/1.086`、`pairing_if_no_correction 0.689/0.760/0.921` ✔ `exp01_q2a_pairing.json mc[F=*].pairing / estimators / fisher_inflation`
  - 跨 seeing `7.18%／9.98%／24.3%`、`0.760~0.944`、`0.42%`、`1.23031`、落带 84.5% ✔ `exp01_q2b_data.json analytic_seeing_scan / hst_cross_frame.controls(0.0997738, 0.0268712) / real_products(box_flux_frame_to_frame_spread_rel=0.2432443, psf_valid_frac_p50=0.0042497, n_psf_valid_p50=473.5, n_sources_p50=174030, in_band_frac_median=0.84463, fwhm_x_over_sx_p50=1.23031)`
  - 帧级表 `29.223/29.199（−0.09%）`…`17.654/16.458（−6.77%）`✔ `frame_level.rows`
  - `3,639,783` 源 ✔ `c_real_distribution.frames` 16 帧 `n_src` 求和
- **落档缺位之处（该件的失分点）**：① §5 整节（`:594-624`）的对照对象 `run/SCI-SNR-01/REPORT.md`、`run/SNR-REVIEW-01/REVIEW.md`、`run/SNR-ESTIMATOR-01/REPORT.md` 与 `:719-720` 的数据行、`:652` 的运行日志、`:328` 的 `run/PERF-401/work/p1_blk*/p1_sources.json` 全部不入库 ⇒ "分歧 D1–D8" 里**既有结论那一侧的数值无仓内对照**，读者只能看到本单元一侧；② 真实产物臂与 §2.6 可满足性臂在 fresh clone 上不可复跑（依赖 PERF-401 与 testdata 大文件），README 的 §6 复现命令未声明此前置；③ `:737` 引 `p1snr_science_test.cpp:209` 的 `1.9140054498711294` 作独立佐证，未给文件完整路径（本单元其他代码引用一律给路径），也未给符号名。
- **seed 与复现命令**：seed 写明（`:6` `20260924`）且与三份产物 JSON 的 `meta.seed` 一致（q1/q2a/q2b 全部 `meta.seed = 20260924`）✔；复现命令写明（`:630-648`，含一键、分步、`--quick`、单元总入口），脚本齐备（`code/exp01/run_all.sh`、4 个 py 在列），退出码语义（`:640` 15/15 门）与产物字段对应 ✔。**一处渲染缺陷**：`:630` 与 `:648` 用反引号包裹的 `~~~bash` 围栏，代码块在 Markdown 渲染后不成块（同文件其余围栏均为裸 `~~~`/` ``` `）。
- **与上位冲突／内部不一致**：无对 `ASTROCS_DESIGN.md` 的实质抵触（`:506` 引 §3.1、`:565` 引 §2.2/§3.1 均与条款相符，`:127/:560/:623` 明确以 `docs/plugins/algorithms_phase1/06_photometry.md §4` 为规范依据）。内部不一致 3 处：① `:758`（S9 称"4:10 应为 4:02、RSS 962→961 已改"）与正文 `:651-652`（仍写 3:48~4:10、959~962 MB）不符；② `:685-696` 未核实清单编号重复（两个 13、两个 14）；③ `:318` 把 G6 的第二例写成"δ* = S_sys_max = 1%"，而本单元冻结的 `S_sys_max = 1.4279%`（`:297`），产物里该例的键名是 `S_sys1pct_Aeff0_delta_star`（即子测试临时把 S_sys 设为 1%）——正文未说明取项被改动，读者按冻结口径会得到 δ*=1.4% 而非 1%。
- **是否被正本当作 A/B 级证据引用**：**否**。`git grep -n "EXP-01" -- docs` 0 命中；`docs/science/NOISE_MODEL.md` 只引用本单元的 `results/b2_noise_terms.json`、`results/DOC_CORRECTIONS.md`（`:324`）与 b 系列 JSON，`docs/plugins/algorithms_phase1/07_noise_snr.md:195` 引用的是 EXP-03/EXP-04/EXP-05，`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md:114` 与 `docs/design/PRODUCT_STORAGE_FORM.md:100` 引用的是 EXP-02 的 mesh 粒度。**EXP-01 的全部结论（δ*=1.4%、方案 A 判定、结构污染 D8）目前只在本单元内部被 EXP-02/03 承接**，未进入任何 `docs/science/`／`docs/algorithms/` 正本的证据锚 ⇒ 单元级孤点（其 D8 反而被 EXP-02 独立复核并给了修法，说明真正需要上升的是 D8 与方案 A 判定，不是本文件本身）。

| `实验/absolute-snr/docs/EXP-02-STRUCTURE-CONTAMINATION.md` | EXP-02 天光噪声估计的「结构污染」缺陷：独立复核、修法与判据 | 762 | 研究与佐证（B 级实验报告，对 EXP-01 D8 的独立复核＋修法＋判据） | 全帧裁剪 RMS 作为 σ_sky 的结构污染缺陷、F1 结构感知估计与 F2 无真值代理判据 | **有**（`:3-5` 标 `ASTROCS_DESIGN.md §2.2、§5.3、§12.1、§12.2、§12.3` ＋ `AGENTS.md §5/§6`；机械列 upstream_pointer=True 一致；缺条款**标题**，标准 01 §3 要求"章节号＋标题"） | 4：`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md:114`、`docs/design/PRODUCT_STORAGE_FORM.md:100`（均以"EXP-02 mesh"引用其 mesh 粒度，非以其科学结论为证据）、`docs/EXP-03-REGIONAL-SIGMA.md:6,947`、`eng/ci/ledgers/naming_surface.json:459` | 是（结构污染缺陷与 F1/F2 的实验层正本）；科学口径层正本是 `docs/science/CONTROL_WEIGHT_SNR.md §8b` 与 `docs/plugins/algorithms_phase1/07_noise_snr.md:195`（引用本件，不重复本件口径 ✔） | 真实帧表重复：本件 `:145-152`（M1-20251212 16.1581／kf 0.9794…8 行）与 `docs/EXP-01-DELTA-AND-ESTIMATOR.md:211-220` 同一批 8 帧同一数值各写一遍（EXP-02 `:141` 自述"逐帧逐位一致"＝以复核为目的，属合法分层，但两张表数值同源却两处维护） | 有（机械列漏报）：`:3-7` 元信息＋纪律声明块（任务单元 ID、seed、"只读／不运行可执行文件／不使用 ulimit -v／无 git 写"） | `:3` 任务编号 `SCI-B-EXP-02`；`:168-172` "过程留痕（诚实）：本单元在中期**曾**一度判定…**该判断错误并已撤回**"；`:678-690` §11.4 过程留痕 8 行（"初版""曾""已改"）；`:481-489` §8.2"本单元在自审中发现并修正的一处口径错误"；`:750` commit sha `5e82a8d17e19a0ffc981b59d6776630c885f388f`（标准 01 §4 禁 commit；同一行已有"SExtractor 2.28.2"满足标准 02 §1 的版本要件 ⇒ sha 冗余）；`:312,318,354,373-376` 的 DOI/arXiv 属必需证据要件（不算违规）；机械列 history_narrative=[168,678] 正确命中两处，漏报 `:483`、`:663`（"负面清单"式过程语） | `:226,252,257,293,426,728` `results/exp02_figs/*.png`（5 张：`a_analytic_scan`、`a_box_dependence`、`a_gate_calibration`、`b_hst_scan`、`c_real_frames`）——目录**从未入库**：HEAD `ls results/` 无该目录，`git log --all --diff-filter=A -- "*exp02_figs*"` 0 命中（无新增亦无删除史）；同缺陷在 `results/EXP02_TABLES.md:7-11` 再列一次；`:67,713,729` `run/SCI-B-EXP-02/logs/*`（不入库）；`:211,762` `testdata/HST_M16/*.fits`（`testdata/README.md:19` 明示不入库） | 无与最高设计 §2.2/§12.2 的实质冲突（:380 判定"现行全局裁剪 RMS 不能给出 σ_sky 语义"正是 §2.2"信号经独立局部背景估计"的落实）。**两处上位要求未达且自认**：`:674-676` "本单元没有独立子代理审稿…主结论尚未经第三方复核"——违 `ASTROCS_DESIGN.md §12.3`"每个单元经独立子代理多轮审稿"与 AGENTS.md §5；`:668` A2 判据的参考面（差分）其无偏性在本件未证，由 EXP-03 §2.2/§5.3 才补上 ⇒ 本件 §0 #6"判据非退化：是"的论证只覆盖"能红能绿"，不覆盖"参考面无偏" | 保留（本批第二价值件，缺陷＋修法＋判据齐）：删 `:3-7` 元信息块改一行上游指针（含条款标题）、把 §11.4 过程留痕移出正文（标准 01 §4；需留证落 `artifacts/evidence/`）、`results/exp02_figs/*.png` 随包入库或删引用、`:750` 去 sha、§6.1/6.2 的对照实测数字落 JSON 归档件 |
| `实验/absolute-snr/docs/EXP-03-REGIONAL-SIGMA.md` | EXP-03 区域化 σ_sky 的绝对准确性与跨帧一致性：前提实证、口径、误差界与文献对照 | 973 | 研究与佐证（B 级实验报告：裁决前提实证＋口径定义＋误差界＋文献对照） | 区域化 σ_sky 的口径（R0/R1/R2）、三项误差-尺度关系、跨帧一致性与 12 个开源实现对照 | **有**（`:3-7`：`ASTROCS_DESIGN §2.2、§5.3、§5.4、§12.1、§12.2、§12.3` ＋ `07_noise_snr.md §4.2a/§4.3/§4.5` ＋ `docs/science/NOISE_MODEL.md`、`CONTROL_WEIGHT_SNR.md`；机械列 upstream_pointer=True 一致；缺条款标题；`:5` 是负责人裁决**原话带日期**，属元信息不是上游标注） | 3：`docs/plugins/algorithms_phase1/07_noise_snr.md:195`（**唯一把本件按编号当作 B 级证据引用的正式层文档**："估计器认证、逐区域偏差与适用域见 `docs/science/CONTROL_WEIGHT_SNR.md §8b` 与 `实验/absolute-snr`（EXP-03 的 R0/R1/R2）"）、`docs/EXP-06-SNR-PHYS.md:5`、`eng/ci/ledgers/naming_surface.json:460` | 是（区域 σ 的实验层正本）。**但科学口径层正本缺位**：`:679` 自己建议"新增 `docs/science/` 小节——区域化 σ_sky 的口径、可信域与尺度选择"，说明该定义尚未在 `docs/science/` 落纸 ⇒ 目前"σ_sky 是位置函数×逐帧电平"的口径只由此实验件承载，违反标准 01 §1"科学公式的唯一权威是 `docs/science/`" | 与 EXP-02 的真实帧判定**互相冲突且互不订正**：EXP-02 `:301-302`/`:616-618` 判"16/16 帧 `fail_closed` ⇒ 星云场帧系统性无法参与跨帧标定"并据此上呈架构问题；本件 `:651-661` 判"EXP-02 的 fail_closed 在区域级被部分推翻——平坦面板 79%~87%、星云主体 39%~59% 的区域可通过"。两份都标"现行"、同批数据、结论相反方向（EXP-02 未加"作用域＝帧级"限定，也未链到本件）。本件内部重复：`:84-91` panel 表与 `:451-456` AUTO 表对同三个量（帧级标量、区域 σ 中位、帧内离散）各以不同统计口径写一遍（M1 帧内离散：`:86` 区间 1.23–1.29 vs `:451` 中位 1.26），两处互不标注 | 有（机械列 meta_block=False 漏报）：`:3-9` 七行块＝实验单元／任务单元／负责人裁决原话＋日期／上游缺陷／权威依据／seed＋只读纪律 | `:5` **日期＋负责人裁决原话引述**（"负责人裁决（2026-09-22，原话）：「…」"）——标准 01 §4 明文禁止，也是负责人"文档内禁日期与历史"硬口径；`:3` 任务编号 `SCI-B-EXP-03`；`:61-65` "调研子代理**曾**提出…**即 EXP-02 的账目正确**"；`:497` "完整调研（927 行）见…"；`:852-865` §9.5 过程留痕 **10 行**（"修正为""改为""判定该方法无效"）；机械列 history_narrative=[852] 只命中表头，漏报 9 行与 `:61`；`:155,319` 内嵌图片、`:3` 编号；`:467` `M5-20260108` 等日期是**数据帧名**（非文档日期，不算违规，但机械列 dates=[4] 未区分） | `:155` `![…](../results/exp03_figs/b_real_premise.png)`、`:319` `![…](../results/exp03_figs/a_error_vs_scale.png)`、`:901,905` `results/exp03_figs/*.png`——目录从未入库（`git log --all --diff-filter=A -- "*exp03_figs*"` 0 命中，HEAD 无该目录）⇒ **两条图片嵌入在渲染时即坏链**；`:497` `run/SCI-B-EXP-03/research/R1-regional-sky-implementations.md`（927 行）、`:908` `R2-per-region-snr-and-scale.md`（789 行）、`:909` `run/…/research/sources/**`、`:906` `run/…/logs/*.log`——`run/` 不入库；`:683` `p1snr_science_test.cpp` 给的路径是 `eng/tests/unit/.../p1snr_science_test.cpp`，实际跟踪路径是 `lib/algorithms/noise_snr/tests/p1noise/p1snr_science_test.cpp`（**目录错＋省略号路径**，按给定串找不到文件；行号 `:124-128` 本身对得上）；`:57-58` `module_adapters.cpp:2486`/`:4629-4630` 已漂移（实测写入点在 `:4186`、读取点在 `:7151`）；`:56` `star_detector.cpp:135` 指向**调用点**（`estimate_background` 定义在 `:75`）；`:952` `testdata/HST_M16/*F657N*.fits`（不入库）。锚点正确者亦已核：`:59` `snr_frame_science.cpp:89-90` ✔、`:683` `snr_science.cpp:148` ✔、`:685` `PHOTOMETRY_LITERATURE_REVIEW_ARCHIVE.md:89` ✔、`:683` 同文件 `:97` ✔（该行确为"孔径/annulus 的 n_sky 项必须进入 CCD 方程"）、`:684` `p1snr_science_test.cpp:124-128` 行号 ✔、`:64` `07_noise_snr.md` "尚未挂 variance 块" ✔（实文 `:138`）、`:45` `10_sampling.md §4.1` 掩膜排除清单 ✔（实文 `:29-30` 逐条对得上）、`:9.4-8` `sky_plane.cpp` `k_corr = 1.4` ✔（实文 `:233`） | 与上位无冲突：`:21,368` 的推荐 B=64 显式对齐 `ASTROCS_DESIGN §5.3` 与 `07_noise_snr.md §4.5`（`Δ = tile_width/8 = 64`）✔；`:49-50` 指出 §4.3 的"标量降级门"已从可选变为必需 ⇒ 是**补上位口子**，不是抵触。**实质缺陷是数字与落档不符**：`:22`/`:286`/`:387`/`:783` 四处声称"解析臂 **65 组配置（13 种结构 × 5 个尺度）**"，归档 `results/exp03_e1_analytic.json` 的 `a2_structure.rows` 与生成表 `results/EXP03_TABLES.md` 表 A2 均为 **45 行 / 9 种结构**；`:387` 同表把帧级标量 S 的区间写成"−1.4% ~ **+100095%**"，本件归档 `scalar_rel_err` 实为 **−1.58% ~ +2304%**，且本件 `:252` 自己写"整帧标量 S 偏差 **+2303.66%**"——`+100095%`（ramp r=1001.96）与 `+100328%`（smooth_corr128 r=1004，`:287`）是 **EXP-02 归档件**里的配置，被计入"本单元解析臂"⇒ 跨件张冠李戴，样本面被高估 44% | 保留（本批证据链最完整的科学件：前提实证、三项误差式、闭式对拍、跨帧功率闭合全部落档）：①删 `:3-9` 元信息块，负责人裁决改为正向条款（不引原话、不带日期）；②**订正 4 处"65 组/13 种结构"为 45/9**，并把 `:387` 的标量区间改回本件归档值、`:287` 的两个极端配置注明"证据在 EXP-02 归档件"；③`:9.1` 表把 `null_zero` 的预算写成 3σ，代码实为 `max(3σ, 2e-3)`（`e4_gates_selftest.py:66`）⇒ 按文档口径该门应判红（|Δ|=0.093% > 3σ=0.033%），须把 0.2% 绝对地板写入文档；④图片随包入库或删引用；⑤两份 R1/R2 调研与 sources 快照落 `artifacts/evidence/` 并由本件引用；⑥`:57-58` 行号锚与 `:683` 文件路径订正；⑦与 EXP-02 的可用性判定互相加限定与链（见 §3 同主题组） |

## 2b. 逐份细则（续：EXP-02、EXP-03）

### 2.6 `实验/absolute-snr/docs/EXP-02-STRUCTURE-CONTAMINATION.md`

- **八要素**：**齐**（①假说：无独立编号的 H 表，但 `:82-108` §2.1/§2.2 给出可证伪的机理命题"裁剪窗口被结构自身抬高 ⇒ keep_frac→1 ⇒ σ̂/σ_n≈√(1+r²)"并附实测检验，`:94-108` 进一步证伪"rms 比值是充分统计量"——形式弱于 EXP-01 的 H1–H8 但满足"可证伪假说"；②方法 `:71-126`＋`:206-219`；③数据 `:206-219` 三类齐备含 seed；④结果 `:222-302` 全带对照与不确定度；⑤结论 `:11-21` 结论页＋`:466-496`；⑥诚实边界 `:631-676` 分"解析可证/依赖仿真假设/未验证"三档；⑦复现 `:693-731`；⑧佐证 `:306-391`＋`:735-762`）。
- **判据非退化（有实证，一处论证不完整）**：`results/exp02_e4_gates.json` 逐条对上：`analytic.n_rows=240 / n_certified=115 / n_false_certified_total_budget=0`、`hst.n_certified=10 / 假绿(总预算)=0 / 假绿(excess 口径)=5`、`worst_abs_total_err=0.017487`（→ 正文 1.75%）与 `0.024718`（→ 2.47%）。三类故障注入 `identity_fix_red`／`global_mesh_red`／`gate_disabled_certifies_bad` 均判红。**恒真门审查**：`analytic.gate_not_always_red`（80 条 ok）与 `gate_not_always_green`（125 条 fail_closed）只统计 verdict 分布，不检验任何物理量——这是**退化性自检**而非科学判据，用法合规（其存在恰恰是为了排除恒真/恒红）。真正的科学判据 `A2 ≤ 1+δ` 有 0 假绿标定 + 无结构归零（Δ=+2.246e-05，3σ=6.03e-05）+ 注入判红 ⇒ 非恒真。**不完整之处**：A1/A2 的分母是"差分参考 σ_diff"，其无偏性（位置稳定分量精确对消）在 EXP-02 阶段只是假设（`:668` 亦承认真实臂无真值），直到 EXP-03 §2.2/§5.3 才以逐区域功率闭合与 Δσ²=ΔS/g 的 <0.1% 闭合补上 ⇒ 本件 `:20` 的"判据非退化：是"应限定为"能红能绿已证，参考面无偏性待 EXP-03"。
- **关键数字落档**：臂 A/臂 B/臂 C 全部命中 `exp02_e1_analytic.json`（`ramp` r=1001.955 `prod_rel_err` → +100095.5%；r=0.30059 → +2.79%；r=3.00587 → +216.59%；corr128 r=1004.27 → +100327.7%；blob_sigma8 → −1.20%；blob_sigma32 → +4.18%）、`exp02_e2_hst.json`（22 行）、`exp02_e3_real.json`（16 帧）、`exp02_e4_gates.json`（240/115/0 假绿、Δ/3σ/max、`serial_vs_pairwise_rel=8.2e-15`、`upper_vs_np_median_rel=−0.009422` → 正文 −0.94%、`crosscheck` −2.2e-10）。**唯一无落档的一组**：§6.1/§6.2 的"对照实测"数字 `10.4673／9.9034／18.5386／9.8811／18.72`（SExtractor 本地编译）与 `10.523／18.500`（photutils）——`git grep` 全仓**仅命中本件散文**（`:324-328,344-345`），results/ 无对应 JSON，且 `:658` 自认"由子代理本地编译/安装后实测，本单元未独立重跑"。这组数字支撑了 `:330`"保护来自 mesh 空间分解而非估计器公式"与 `:345`"精确复现 ACSD 的失效模式"两条**关键判定** ⇒ 该判定在仓内无数值落档（引文的源码行号部分可独立复核，属 A 级；实测部分属 B 级形式要件缺失）。
- **seed 与复现命令**：seed `20260925` 写明（`:6`、`:210-212` 逐臂表、`:731`），命令完整（`:698-714`，一键＋分臂＋带 RSS 记录），6 个脚本齐备；`results/exp02_*.json` 的 `meta` 带 seed。不可复跑处：臂 B 依赖未入库的 HST FITS、臂 C 依赖未入库的 M42 `.fts`（`testdata/README.md:19`）；文档未给取得数据的前置步骤（`data/README.md` 也只登记 b 系列输入）；`:707` 的 `e3_real_data.py --crop 2048` 参数、`:701` "约 8 分钟"与 `:705-709` 的分臂耗时属运行读数，仓内无日志副本可核。
- **是否被正本当作 A/B 级证据引用**：**部分**。正式层两处引用只取其 mesh 粒度做几何论证：`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md:114`"与…EXP-02 mesh 父级同构"、`docs/design/PRODUCT_STORAGE_FORM.md:100`"EXP-02 mesh 的父级"；**其核心科学判定（现行 σ_sky 语义失效、F1/F2 修法）未被 `docs/science/`／`docs/algorithms/` 任何条款引为证据**——`docs/science/CONTROL_WEIGHT_SNR.md §8b`（`:205-215`）引的是本单元的 `results/b3_domain_map.json`/`b6_gates_audit.json`/EXP-04，`docs/plugins/algorithms_phase1/07_noise_snr.md:195`（"控制点局部 σ 必须结构感知"）引的是 **EXP-03 的 R0/R1/R2**，未引 EXP-02。⇒ 结论：EXP-02 相对正式层是**半孤立单元**（其缺陷判定由 EXP-03 承接后才进入插件正本的证据链）。

### 2.7 `实验/absolute-snr/docs/EXP-03-REGIONAL-SIGMA.md`

- **八要素**：**齐**（①假说＝`:5` 负责人裁决前提"同一位置天光跨帧相同"＋`:17` 的可证伪化（空间形态 vs 电平分拆，各自给证伪读数），形式上是"待验前提"而非自拟假说，但 §2.2 明确**单侧判据**（前提被破坏只会使 excess 变正）⇒ 可证伪性成立；②方法 `:231-298`（R0/R1/R2 定义＋闭式误差界）；③数据 `:78-98`＋`:381-421`（三类，含真值口径与对齐）；④结果 `:124-182`、`:317-491` 全部为 AUTO 生成表（`<!-- BEGIN AUTO:* -->` 三处），数值与归档件逐位一致；⑤结论 `:13-27` 十条判定；⑥诚实边界 `:777-832`（三档＋12 条未验证＋15 条负面清单）；⑦复现 `:869-911`；⑧佐证 `:495-616`＋`:915-952`）。
- **判据非退化——一处必须点名的恒真面与一处判据呈现与代码不符**：
  1. **恒真面（报告自陈，产物坐实）**：`results/exp03_e4_gates.json` 的 `injections[0]`（`identity_diff`）note 逐字："**恒等映射下 A2==1 必判 ok —— 这正是『门无法自证』的故障**，必须由 N2（能红能绿）与 I2（退化 box 判红）联合覆盖"。即：以差分参考认证 R1 的门，在"参考面退化为恒等"时**恒绿**，无判别力。之所以仍判该单元判据整体非退化，是因为补位成立——I2 的归档读数 `verdict=fail_closed, A2=3.1971`、N2 的 `analytic.gate_not_degenerate` 在结构场上给出 6 ok／2 diagnostic_only／3 fail_closed ⇒ 门集合能红。**登记为"恒真子门已被如实立案并由其他门覆盖"**，不是隐瞒。
  2. **状态轴串用**：该注入的字段是 `"pass": true`——`pass` 在此表示"注入按预期表现为门失效"，与其余门的"pass=判据通过"语义相反（判定纪律第 6 条：状态词不得跨轴串用）。产物与正文 `:803`（"PASS（如实登记该门自身恒绿）"）都用了同一个词 ⇒ 读者无法区分"门绿"与"门被证明无效"。
  3. **判据呈现与代码不符（本件最需修的实质项）**：`:791` 把 `analytic.null_zero` 的预算写成 **3σ**，关键量写"Δ 均值 −0.09%，3σ = 0.03%"；代码 `code/exp03/e4_gates_selftest.py:66` 实为 `ok = abs(mu) <= max(3.0*sem, 2e-3)`——**存在一个 0.2% 的绝对地板**，该门是靠地板而非 3σ 通过（0.0926% > 0.0330%）。按文档口径此门应判红。归零门的容差不应写成一个文档里没有的常数。（对照：`R2_unbiased` 代码 `worst <= DELTA + 0.01` = 0.024 ⇒ 文档"2.4%"✔；`R0_closed_form` 代码 `worst <= 0.02` ⇒ "2.0%"✔；两条都对得上。）
  4. 其余门均非恒真且落档：`scalar_cannot_track_varying_sigma`（标量 p95 21.1% vs R1 3.65%／R2 3.62%，σ 真值跨 1.667 倍）、`real.premise_power_closure`（15 对，最差中位 6.66%、p95 上限 5.48%、单帧/差分功率最小 9.12，预算 10%/10%/3.0，全部命中归档）、`hst.R2_beats_R1_on_structure`（R1 46.6%→91.2% vs R2 1.44%→1.82%，帧级标量 `frame_scalar_abs_rel_err_vs_local_p95` = 1.3329 → 文档"133.3%"✔）。
- **关键数字落档**：15 行前提表（`:127-141`）与 15 行结构函数表（`:163-177`）**逐位命中** `exp03_e2_real.json`（如 M2 2-3：excess 中位 −6.66%、p05 −89.1%、p95 +0.1%、subpix 0.062 px、单帧 S(256)/S(1)−1 = +531.0%、差分 +7.5%）；`:451-456` 跨帧表命中 `cross_frame`；`:463-465` 的 M5 逐帧解（13.92／14.25／15.70，σ² 193.8／203.0／246.5）与 `sigma_solve` 逐位一致，Δσ² = 52.675 → 文档"52.7"、`79/1.5 = 52.667` ⇒ "差 <0.1%"成立；`:354-355` 分辨率曲线命中 `resolution_curve`（M5 20.8%／23.1%／双线性 24.9%／离散 9.02）；`:387` 的 R0 −1.79%~+1105%、R1 −1.87%~+213% 命中 `a2_structure`（max 11.0508、2.1337），R2 −1.85%~−1.30% 命中（min −0.0185、max −0.0130）；`:269-273` 的 5 行闭式对拍逐位命中 `a4_formula`（2.74925e-05／6.54e-4／7.23e-4／1.30%／−1.81%）。**不落档的三处**：①"65 组/13 种结构"（见登记表）；②帧级标量区间"+100095%"（见登记表）；③`:728-747` §8.4 的 provenance **示例**自称"取自 M5-20251212 帧的实测统计"，但 `dispersion: 9.02` 与 `sigma_p95: 117.9` 出自 **panel 级 `resolution_curve`**，该帧自身字段是 `region_sigma_dispersion = 9.9146`、`region_sigma_p95 = 129.68`（两值均确在归档，但**取数口径与文中说明不同**）；`:746` 的 `c_scale: 0.019` 是 EXP-02 的 C 量，不在本件归档件里。
- **seed 与复现命令**：seed `20260925` 写明（`:8`、`:872`、`:911`）并落在 `results/exp03_e{1,2,3,4}*.json` 的 `meta.seed` ✔；6 条命令＋一键齐备，7 个脚本全部在位（`exp03_common.py`、`e1_analytic.py`、`e2_real_premise.py`、`e3_hst.py`、`e4_gates_selftest.py`、`make_tables.py`、`run_all.sh`）。**可执行性缺口**：①臂 P 读 `testdata/M42_T2T3_mosaic_Flying_dutchman/T2/**/*-300S-Red.fts`、臂 B 读 `testdata/HST_M16/*F657N*.fits`，两者均不入库 ⇒ fresh clone 上 `run_all.sh` 不能跑全；②门 P3（`scalar_cannot_track`）在模板缺失时 `return {"pass": False, "error": "M42 模板不可用"}`（`e4_gates_selftest.py:128-129`）⇒ 缺数据时**判红而非 SKIP**（fail-closed，方向正确），但报告 `:787` 的"7 个门全绿（`ALL_GATES_PASS = True`）"未说明这一读数依赖仓外大文件在位；③`:877` 的"实测 11 分 42 秒；峰值 RSS 3.17 GB"与 `:72` 的"≤3.17 GB"是运行读数，日志在 `run/`（不落档），无法仓内核对。
- **A 级佐证的可核性**：`:502-516` 的 12 个实现对照表每条都带"项目＋版本＋文件:行"（SExtractor 2.28.2 `back.c:846`、photutils 2.3.0、Gnuastro 0.24、LSST 30.0.11、IRAF v2.18.1 `apcomags.x:49`、SEP 1.4.1、SWarp 2.41.5、ProFound 1.24.5、Siril 1.2.4、ccdproc 2.5.1、Montage、DAOPHOT II 手册），`:919-943` 另给 15 条带 DOI/arXiv 的文献 ⇒ **形式要件齐备、可由独立读者复核**；`:542-551` 还**证伪了任务书自身给的 4 条引用**（两个 DOI 错标、Byun 标题错、NoiseChisel "0.8 分位定 mesh" 查无此物），`:553-566` 明确"背景尺度选择无一手理论依据"并说明本件 §4 的三项误差式是**补位而非引用**（不伪造依据）。缺的是**核验过程**：`:497,908-909` 两份调研与 PDF/源码快照全在 `run/`，仓内无副本（`:9.4-13,14` 亦登记了本机反爬与 `web_search` 失效）。

## 3. 同主题组与差异（跨批发现注明别批路径）

**组 1 · 星点通量口径 `F_instr`（三处落点，数字不一致）**
正本候选 `实验/absolute-snr/docs/f-instr-canon.md`（DB-15 范围）。三处段落：
- `实验/absolute-snr/code/reverse_verify/f_instr/README.md:17`："定案：改用 PSF 总通量…实测孔径无关性 ≤ **0.005 mag**"
- `实验/absolute-snr/docs/f-instr-canon.md:16`："实测 over seeing 1.5–5.0 px：**M_seeing ≤ 0.004 mag**，假增益 0.998"
- `实验/absolute-snr/results/REVERSE_VERIFY_CANON.md:22`："**M_seeing ≤ 0.004 mag**…生产口径同测 **1.353 mag / 0.516**"
差异：同一测量阈值 0.005 vs 0.004；同一文件内部另有 `f-instr-canon.md:189`（"≤0.005 mag"）与 `:356`（"`box5` 给出 **0.504**（−0.74 等的假增益）"，而 `:14`/`:143` 写 0.516／−0.72）。⇒ 判"同主题三份，数值口径未收敛"，整改须以 `f-instr-canon.md` 单一表为准并在 `results/` 落 JSON。

**组 2 · 全帧裁剪 RMS 的结构污染（EXP-01 → EXP-02 → EXP-03 三级链，末级与首级判定相反）**
- EXP-01 `:619`（D8）发现并"建议单独立项，本单元只给证据不做修法设计"；
- EXP-02 `:301-302`、`:616-618` 判"16/16 真实帧 `fail_closed`；若绝对 SNR 链坚持帧级标量 σ_sky，则星云场帧将系统性无法参与跨帧标定"，并 `:627` 按 AGENTS §8 上呈负责人；
- EXP-03 `:651-661` 判"EXP-02 的 fail_closed 在区域级被部分推翻——平坦面板 79%~87%、星云主体 39%~59% 的区域可通过；整帧聚合恰恰是把这 4~8 成可用区域一起丢掉的那一步"。
差异性质：不是同一命题的反转（帧级 vs 区域级作用域不同），而是**上位结论未随下位实证加限定**——EXP-02 的结论页与 §10.5 都不含"作用域＝帧级"限定，也不链 EXP-03；而 `docs/plugins/algorithms_phase1/07_noise_snr.md:195` 只引 EXP-03。按标准 01 §1（冲突本身是缺陷）与 §4（只写现行、库内保留最新版本），整改项 = EXP-02 `:21`/`:301`/`:616` 三处补作用域限定＋指向 EXP-03 §7.2，且两条上呈记录（`工程控制/RELEASE-05/*` 或 `artifacts/evidence/`）需以合并后的口径重述。

**组 3 · 真实 M42 帧的 σ̂ 与保留比（EXP-01 与 EXP-02 重复表）**
EXP-01 `:213-220`（8 帧：16.158／0.979／MAD 粒度 5.00%／棋盘 SE 0.00546…）与 EXP-02 `:145-152`（同 8 帧：16.1581／0.9794…＋块间 p95/p05 9.08）数值同源。差异：EXP-02 为独立重算并多给逐位小数与 p95/p05 列；两份都由各自 JSON 生成（`exp01_q1_delta.json.a_real` vs `exp02_e3_real.json`），可复算但**人工抄表**。整改建议：由 `make_tables.py` 单点生成、正文只引用表名（标准 01 §4"数值表放下层文档"已满足，重复维护是缺陷）。

**组 4 · reverse_verify 三份工作区 README 与一份缺失**
`f_instr/README.md`、`frame_snr/README.md`、`snr_design/README.md` 结构同构（文件表＋复跑＋判据纪律＋产物落 `run/`＋诚实登记），互不重复；但**同一工作区第 4 个目录 `code/reverse_verify/p7_noise/` 无 README**（6 个脚本 `p7lib.py`、`exp1..exp4`、`make_figures.py`，seed `20260924` 写在各脚本 `SEED=`），且 `results/REVERSE_VERIFY_CANON.md:47` 的复跑清单里给它写"若存在"（该文件确无 `run_all.sh`）⇒ 本批清单覆盖 3/4，p7_noise 处于"有代码、无报告、无复跑入口、无 seed 声明文件"状态，属实验单元自包含缺口（`ASTROCS_DESIGN §12.3` 目录结构要求）。另 `实验/shared/REVERSE_VERIFY_MIGRATION.md`（别批）登记了迁移映射，可作 p7_noise 归属线索。

**组 5 · 恒等式型负例的分级（三处，同一问题）**
本批三份文档各有一处"以恒等式充当负例"：EXP-01 `:500/:586`（G10 同帧恒等对照严格 0.000）、snr_design/README `:25`（共模 ratio 恰为 1.0）与 `:36`（天光红线＝算术加常数 0.0000%）、EXP-02 `:474`（gate_not_always_red/green 只数 verdict 分布）。差异：EXP-01 与 EXP-02 都另有**真正有判别力的对照臂**（独立噪声实现；0 假绿标定＋注入）；snr_design/README 的共模门则被其上位文档 `docs/snr-propagation-design.md:24-25` 明文判为"没有区分能力"，README 仍列为"关键判据"⇒ 只有这一处是"文档与上位口径倒挂"，须改。

## 4. 索引与链接缺陷清单

**4.1 登记状态（先固定口径）**
- `docs/DOCUMENT_INDEX.yaml:30-31` 自定扫描口径："git 跟踪文件，排除 问题扫描/、reports/、artifacts/、run/、build/、lib/third_party/、**实验/**、工程控制/、logs/ 等过程与证据目录"；`:38` `coverage: "docs/**（含 docs/validation/v6/**）+ 仓库根治理文档"`。
- ⇒ 本批 7 份**均不在索引覆盖声明之内**（机械列 `unregistered_index=True` 全部命中，但成因是索引自声明排除，不是文档漏登记）。
- **与标准冲突**：标准 01 §3 写"`docs/DOCUMENT_INDEX.yaml` 是全文档集唯一索引地图：现存文档层全部登记…未登记即缺陷"；`ASTROCS_DESIGN.md:54` 写"**现存文档层**全部在其中登记"。二者对"实验/ 是否属于'文档层'"口径不一致 ⇒ 需负责人裁决（属"顶层合同/唯一索引地图"范围界定）。裁决前，本批 7 份的"未登记"不应记为文档缺陷。
- 本批件在机器登记表里的现存踪迹：`eng/ci/ledgers/naming_surface.json:456,458-460`（frame_snr/EXP-01/EXP-02/EXP-03 四件路径被登记），`f_instr/README.md`、`snr_design/README.md`、`data/README.md` 三件**在任何登记表与索引中 0 命中**（`git grep -n "f_instr/README\|snr_design/README\|absolute-snr/data/README"` 除 reports/ 外无结果）⇒ 若索引扩到实验层，这三份是新增未登记项。

**4.2 悬空链接（逐条，含确认命令）**
| 项 | 出处行 | 目标 | 确认方式与结果 |
|---|---|---|---|
| CMakeLists 申明 | `frame_snr/README.md:4-5` | `实验/absolute-snr/code/reverse_verify/CMakeLists.txt` | `git ls-files 实验 \| grep -i cmake` → 0；`ls code/reverse_verify/` → 仅 4 子目录；`git log --all --diff-filter=A -- "*CMakeLists*"` 无该路径 ⇒ **从未入库** |
| 图：真实帧结构函数 | `EXP-03:155` | `../results/exp03_figs/b_real_premise.png` | 目录不存在＋从未入库（`--diff-filter=A` 0 命中） |
| 图：误差-尺度关系 | `EXP-03:319` | `../results/exp03_figs/a_error_vs_scale.png` | 同上 |
| 图清单 | `EXP-03:901,905` | `results/exp03_figs/*.png` | 同上（产物清单声明生成但未入库） |
| 图 5 张 | `EXP-02:226,252,257,293,426,728` | `results/exp02_figs/{a_analytic_scan,a_box_dependence,a_gate_calibration,b_hst_scan,c_real_frames}.png` | 目录不存在＋从未入库；`results/EXP02_TABLES.md:7-11` 重复列同一批路径 |
| 调研 R1/R2 | `EXP-03:497`、`:908`、`:909` | `run/SCI-B-EXP-03/research/R1-regional-sky-implementations.md`（927 行）、`R2-per-region-snr-and-scale.md`（789 行）、`sources/**` | `run/` 不入库（AGENTS §7）⇒ §6 的 A 级证据核验记录无仓内副本 |
| GAP_AUDIT 条款 | `snr_design/README.md:7,59` | `GAP_AUDIT §9.41/§9.42/§9.46` | 唯一现存 `工程控制/RELEASE-05/GAP_AUDIT.md` 的节次为 `## G0…## G4`，`grep "9\.4"` 0 命中；被引载体 `工程控制/RELEASE-02/GAP_AUDIT.md` 曾入库、由提交 `ae0f1e63`（CLEAN-402 清理历史控制包）删除 ⇒ 冻结基线上不可解析。规则现行正本 = `docs/science/PHOTOMETRY.md:14,271`（§10） |
| 生产代码行号 | `EXP-03:57-58` | `module_adapters.cpp:2486`、`:4629-4630` | 实测 `{"noise_sigma", cat.noise_sigma}` 在 `:4186`、`cfg.sigma_sky_adu = ...` 在 `:7151` ⇒ 漂移 1700/2500 行 |
| 生产代码行号 | `EXP-03:56` | `star_detector.cpp:135` | `:135` 是调用点，`estimate_background` 定义在 `:75` ⇒ 锚点指向错位置 |
| 测试文件路径 | `EXP-03:684` | `eng/tests/unit/.../p1snr_science_test.cpp:124-128` | 真实路径 `lib/algorithms/noise_snr/tests/p1noise/p1snr_science_test.cpp`；`:124-128` 行内容 ✔ 对得上，目录与省略号路径 ✘ |
| 源码行号锚 | `f_instr/README.md:14` | `star_detector.cpp:139-151` 的 `m00` | `:139-151` 实为检测阈值与饱和/动态范围注释；`m00` 累加在 `:214-230`（`if (v <= 0) continue;` `:220`、`s.flux = m00` `:226`）⇒ 漂移约 75 行；同一错锚被 `results/REVERSE_VERIFY_CANON.md:20` 复制 |
| 大文件（政策不入库） | `data/README.md:9`、`EXP-01:720`、`EXP-02:211,762`、`EXP-03:952` | `testdata/HST_M16/*.fits`、`testdata/M42_T2T3.../**.fts` | `git ls-files testdata \| grep -i hst` → 0；`testdata/README.md:19` "❌ 不入库（3 × 268,917,120 B）"，`:55-58,83` 给只读取得方式 ⇒ 属**已知外部只读数据集**，不是丢件，但四处均未给取得指针 |
| `run/` 运行件 | `EXP-01:24,40,594,652,719,720`；`EXP-02:67,713,729`；`EXP-03:906`；`f_instr/README.md:46`；`frame_snr/README.md:54`；`snr_design/README.md:44-46`；`data/README.md:38` | `run/SCI-SNR-01`、`run/SNR-REVIEW-01`、`run/SNR-ESTIMATOR-01`、`run/SCI-B-EXP-0{1,2,3}/**`、`run/PERF-401/**`、`run/RELEASE-02/**`、`run/SCI-402/` | `run/` 按 AGENTS §7 不入库；EXP-01 §5 的"既有结论一侧"与 EXP-03 §6 的调研记录因此**在冻结基线上不可复核** |
| 报告节名 | `f_instr/README.md:41` | "报告 §1 核对状态" | `docs/f-instr-canon.md:23` §1 标题为"调研结论（文献与开源科学软件）"，无"核对状态"节 ⇒ 章节名悬空 |

**4.3 机械列（`doc_mechanical.csv`）校准反馈——本批 7 份的差值**
- **`meta_block` 全量漏报**：机械列对 7 份都给 `False`，实际 7/7 都有开头元信息块（标准 01 §4 列举的"文档 ID、状态编号、版本锚、纪律声明"四类齐全）。EXP-02/EXP-03 的块首是 `>` 引用块且含"任务单元：**SCI-B-EXP-02**"，工具未识别 ⇒ 建议判据加"引用块内含任务编号／seed／纪律声明"三取二。
- **`dangling_links` 系统性低估**（工具只判 markdown 链接语法，且不判 `..` 前缀，前言已声明）：漏报 `EXP-02` 的 5 个 `results/exp02_figs/*.png`（反引号路径）、`EXP-03` 的 2 个 `../results/exp03_figs/*.png`（`..` 前缀链接）、`snr_design/README` 的 `GAP_AUDIT §9.4x` 与 3 个 `run/` 输入、`frame_snr/README` 的 `reverse_verify/CMakeLists.txt` 与 `run/RELEASE-02` 帧、`EXP-03:684` 的 `eng/tests/unit/.../` 省略号路径。
- **`commit_sha` 高估**：EXP-01 报 4 个（`20251212/20251216/20251224/20260924`）全部是**数据帧名与 seed**，不是 commit；EXP-02/EXP-03 各报 6–7 个，其中真 commit 只有 `5e82a8d17e19a0ffc981b59d6776630c885f388f`（SExtractor 2.28.2），其余是 DOI 数字碎片（`1996164`、`0006217`）、seed（`20260925`）与帧名。⇒ 建议工具对"7–8 位纯数字"与"含字母十六进制"分档，并把 DOI 数字排除。
- **`dates` 判定基本正确但缺语义**：EXP-03 报 `[4]`（第 4–5 行负责人裁决日期）✔；`EXP-01:213-220`、`EXP-02:145-152`、`EXP-03:463-465` 的帧名日期（`M1-20251212`、`M5-20260108`）未被当日期 ✔ 合理；但 `data/README.md` 的 `20260921` 被记进 `dates`（实为 seed，代码 `sci_b_common.py:20` 可证）⇒ 误报 1 处。
- **`history_narrative` 命中过疏**：EXP-01 只报 `[745]`（实际 §9 整节 `:725-771` ＋ `:114` ＋ `:698-703` 皆为历史叙事）；EXP-02 报 `[168,678]`（漏 `:483`、`:663`、`:686-688`）；EXP-03 报 `[852]`（漏 `:61`、`:853-865` 其余 9 行）。⇒ 建议词表加"已撤回／已改／初版／修正为／过程留痕／审稿期间"。
- **`lines` 全部与 `wc -l` 一致**（47／56／60／38／772／762／973）✔ 无差值。

## 5. 覆盖率自报

- 已审 **7 / 7**；7 份**全部逐行通读**（最长 973 行）。未逐行读名单：**空**。
- 为判定上下游而读（不在清单、未下结论，仅取证）：`ASTROCS_DESIGN.md` §0/§2.2/§12.1–12.5、`docs/science/CONTROL_WEIGHT_SNR.md §8a/§8b`、`docs/science/NOISE_MODEL.md:324`、`docs/plugins/algorithms_phase1/07_noise_snr.md:96-216`、`docs/plugins/algorithms_phase2/10_sampling.md §4.1`、`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md §4.4`、`docs/design/PRODUCT_STORAGE_FORM.md`、`docs/DOCUMENT_INDEX.yaml:20-45`、`docs/f-instr-canon.md`/`docs/frame-snr-canon.md`/`docs/snr-propagation-design.md` 的节次与结论行、`results/REVERSE_VERIFY_CANON.md`、`results/EXP02_TABLES.md`、`results/EXP03_TABLES.md`、`results/exp01_*.json`/`exp02_*.json`/`exp03_*.json`、`code/exp01`~`exp03` 与 `code/reverse_verify/*` 的 seed/输出路径/判据行、`lib/algorithms/star_detection/wrapper_phase1/star_detector.cpp`、`lib/infrastructure/scheduler/src/module_adapters.cpp`、`lib/algorithms/noise_snr/{cpp/src/snr_science.cpp,wrapper_phase1/snr_frame_science.cpp,tests/p1noise/p1snr_science_test.cpp}`、`lib/algorithms/coverage/src/sky_plane.cpp`、`testdata/README.md`、`工程控制/RELEASE-05/GAP_AUDIT.md`。
- 卡点：**无**。本阶段未执行任何构建/测试/检查器/端到端，未 import 仓内 Python（所有数值均用只读 `json.load` 读文件文本，输出重定向到本审查工作区外）。

## 6. 我证伪了任务书的哪个前提

1. **"每个实验单元的报告必须七要素齐"不成立**：正本 `ASTROCS_DESIGN.md:761` 写的是**八要素**，派单自己列的也是八项。判据按正本改为八要素（本批 3 份 EXP 报告八要素齐全，4 份 README/数据件按角色不承担八要素）。
2. **"约 233 份"只在一种口径下成立**：`git ls-files docs \| grep '\.md$' \| wc -l` = **233**（精确）；但任务书把范围定义为"根目录各 `.md`、`docs/` 下全部文档、`实验/` 与 `工程控制/` 下的说明性文档"，按该口径实数 = 根 9 ＋ docs 233 ＋ 实验 62 ＋ 工程控制 31 = **335**（`git ls-files <目录> \| grep '\.md$' \| wc -l` 逐目录数）。⇒ "约 233 份"作为**总量**前提不成立（口径混用）；我不引用别处流传的 360 之类的数（未经我复核）。
3. **"30+ 子目录"不成立**：`docs/` 一级子目录 = **23**（`git ls-files docs \| awk -F/ 'NF>2{print $2}' \| sort -u \| wc -l`，含只放非 `.md` 的 `docs/audit/`）；只含 `.md` 的一级子目录 = 22。
4. **"未登记即缺陷"不能直接套到本批 7 份**：`docs/DOCUMENT_INDEX.yaml:30-31,38` 显式把 `实验/` 排除在扫描与 coverage 之外，与标准 01 §3 的"全文档集唯一索引地图"互斥 ⇒ 属**标准与索引的口径冲突**（需负责人裁决范围），不是这 7 份文档的缺陷。我据此把机械列的 7 个 `unregistered_index=True` 全部改判为"口径待定"。
5. **AUD-101 步骤 4"找出孤儿文档（无上游、无引用）"在本批需要拆轴**：4 份 README／data 件既无上游标注也无文档层引用者（`f_instr/README`、`snr_design/README` 为 0 命中），但其**工作区**被设计文档与结论页引用 ⇒ 是"报告层有链接、README 层无人指"，不是内容孤儿；而 EXP-01 内容**在正式层 0 引用**、结论只被本单元下游承接 ⇒ 按内容轴才是真孤点。混用两个轴会把 4 份 README 误判为孤儿。
6. 未发现"任务书说有、实际没有"的清单级前提：清单 7 行全部存在、全部可读、行数与机械列一致，无缺件、无多余 ⇒ 该前提**未证伪**。

收工基线（同命令再跑一次，与开工一致，取证动作未污染被审对象）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

<!-- PROGRESS: 7/7 -->
