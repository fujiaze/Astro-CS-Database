# 复核 V404-S · AUD-404 退役/死代码清单——节级复核（第②层，窄范围：判据提案／三组分类口径／覆盖率自证）

- 复核对象：`独立审计/证据/AUD-404-退役与死代码清单.md`（227 行，12 个节，按 `独立审计/批次清单/V404-S.txt` 逐节判定）＋配套 `AUD-404-退役与死代码清单.csv`（**只读其列值复算分组口径，本轮不逐行抽核**，留下一波）。
- 基线 HEAD=`c8f64e9a`；仓库 `F:\Astro dev\Astro CS Normalization Database`。
- 只读纪律：未运行构建/`cmake`/`ctest`/`run_checks.py`/任何 `eng/**` 脚本；未 `import` 仓库内 Python（只读其源文本）；零 git 写（只 `status`/`ls-files`/`grep`/`log`）。一切"现网读数"由我按门源码逐字重写的**离线近似**得出，均标明为近似。
- 开工 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文：
  ```text
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```
- 收工（同上命令，逐字一致）：
  ```text
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```
  差异：无。取证对仓库零写入。
- 本轮只答三件事（派单窄范围）：**Q1** 门的存在性语料与匹配方式 → 「改对反而判红」是否成立；**Q2** 判据提案能否两边都走到＋豁免分支有无负例＋追加两字段是否真向后兼容；**Q3** 三组分组键是否互斥且并集=全集，并答"不许删组里是否留了还能再删一层的东西"。
- 前置事实（供各节引用）：清单里所有 `CSV 行 N` 是**文件行号（表头＝行 1）**，即数据行 N−1。四个独立锚互证：`api_inventory.csv:325` 自述在"行 14"→数据行 13 ✓；`待证` 自述在"行 115"→数据行 114（类别=待证 ✓）；`upm.cpp:1999` 自述在第 1 组首行"7"→数据行 6 ✓；`executor.h:22`/`commands.cpp:548` 自述"36/37"→数据行 35/36 ✓。清单未声明这一约定，25 处行号指针全靠反推。

---

## 1. 三组分类（按"能不能直接删"） · md:28-38
**判定：降级**
- 我的独立取证：CSV 复算——9 列、**125 数据行、无 ragged 行**、UTF-8 BOM；`类别` 分布 占位模块 20／待证 1／未跟踪产物名 3／模板复制残留 10／死代码 14／登记面残留 41／退役未清理 29／重复实现 7 ＝ 125，与批次清单头逐字吻合。对象自己的机器表 `独立审计/复算件/retire/groups.json`：`A=14 / B=53 / C=57 / U=1`，引用总数 125、去重后 125、任意两组交集为空、覆盖文件行 2..126 无缺行 ⇒ **互斥且并集＝全集，确认**。14+53+57+1=125 算术闭合 ✓。
- 与成稿差异：**范围/口径**。md:30 声称"分组由 CSV 的 `处置`＋`连带改动`＋`风险` 三列共同决定"，并给出下沉规则；按这条规则我跑不出它的分组：`处置` 是自由长文本（开头为"先解引用再删"26 条、"删除…"27 条、"保留"17 条、其它 55 条），按 md 逐字的第 1 组谓词（处置删除 ∧ 连带=无 ∧ 风险不以高开 ∧ 门承载=否）只得 **8 条**，而实际 `A` 有 14 条。⇒ 分组本身对，**自述口径不产生该分组**（真规则在 groups.json 的生成脚本里，md 未记）。定级不变（不影响处置方向）。
- 影响 D8 的处置：进任务书，但**以 `groups.json` 为分组正本**；md §1 须补一条可机械复现的分组谓词，否则下游无法自证分组。

## 1.1 第 1 组：可直接删（14 条） · md:39-46
**判定：降级**
- 我的独立取证：`groups.json.A` 的行号集合 {7,8,9,10,11,18,24,32,36,37,47,49,51,53} 与 md:41 **逐元素一致** ✓。md:45 点名的两条"改而非删"= 文件行 36（`lib/include/astrocs/core/executor.h:22`）与 37（`lib/infrastructure/cli/commands.cpp:548`）✓ 实物逐字对应。md:43 的"5 条 .cpp 墓碑最干净"= 文件行 7-11（`upm.cpp:1999`/`upm.cpp:2849`/`sampler.cpp:1275`/`sampler.cpp:1277`/`rejection.cpp:2223`）✓ 全部存在、全部是单行 `/* RETIRED 2026-09-25 (CHK-PROD-WIRING W1): … */`。**其"因为 `check_doc_symbols.py` 只 `rglob("*.h")`"的机理确认**：源码 `eng/tools/quality/contracts/check_doc_symbols.py:24-25` 只在 `("lib","include")` 两基下 `rglob("*.h")` ⇒ `.cpp` 文本永不进语料；另一面 `eng/ci/check_retired_code.py` 的 R2 只看前 40 行（`:106 BANNER_WINDOW=40`、`:492`）、R1 要求连续 ≥3 行注释（`:148-149`）⇒ 第 1999/2849/1275/1277/2223 行的单行墓碑既不触 R2 也不触 R1 ⇒ **删这 5 条对两道门都零效应，处置成立**。
- 与成稿差异：**范围（自述条件与数据冲突 4 条）**。md:41 写"连带改动为'无'，且风险不以'高'开头，且门/合同承载列为'否'"，实际：文件行 18（`docs/algorithms/UPM_SOLVER.md:115`）门承载列＝**是**（`ALG-UPM-003` 算法锚）；文件行 49（`dark_optimizer.cpp`）、51（`gaia_xpsd_client/example/demo.c`）、53（`aio/healpix_db/archive/legacy/…`）`连带改动` 均为**非"无"**。分组没错（删除动作确实不产生跨面先决），错的是那句话。
- 影响 D8 的处置：进任务书（14 条照旧），md:41 的三条件句须改写为"删除/改写只发生在对象自身文本内，不要求先改他处"。

## 1.2 第 2 组：先解引用再删（53 条）·批 A 机理 —— **Q1 主判** · md:47-85
**判定：降级（机理成立；"改对反而判红"成立但不均匀，须按 3 枚符号改写顺序判据）**
- 我的独立取证（先读源码，后看成稿）：
  1. **接线**：`eng/ci/checks.json` 中 `CHK-DANGLING` 的 `steps` 含 `CON-DOC-SYMBOLS`，`command` 逐字 `["python3","eng/tools/quality/contracts/check_doc_symbols.py"]`，`timeout_seconds:300`、`waivable:false`、`mutates_workspace:false` ⇒ md 的门 ID/步 ID/command 三项引用全对（md 未给 300s 这一格，非缺陷）。
  2. **存在性语料的文件集合与匹配方式**：`_header_corpus`（`:13-33`）对 `(repo/"lib").rglob("*.h")` 与 `(repo/"include").rglob("*.h")`（后者无跟踪文件＝空操作），仅跳过路径含 `third_party`/`archive` 者，`h.read_text()` **原样拼接全文**；`_defined_in_public_header`（`:36-38`）`re.search(r"\b"+re.escape(token)+r"\b", corpus)` ⇒ **全文正则、不剥注释、不排除退役标注**。⇒ md:54 的机理句**逐字成立**。
  3. **"唯一存活文本"**：`git grep -n -e <5 枚符号> -- '*.h'`（排除 third_party）＝**恰 5 行命中，全是墓碑本身**：`rejection.h:416`、`sampler.h:133`、`sampler.h:135`、`upm.h:181`、`upm.h:355`；另用同一 rglob 语料自建匹配复核，逐枚 1 处、类型=COMMENT。`p2_sample_sky` 不会误配 `p2_sample_sky_cached`（`\b` 后接 `_` 不成词界）✓。
  4. **但红不红是分符号的**（成稿没查的支路）：`:155` 先读 `docs/architecture/api_inventory.csv` 的 `symbol` 列（**448 行在册**），`:287` 的短路是 `token not in api_syms and not any(token in s for s in api_syms) and not any(s in token for s in api_syms)`。实测：`p2_upm_normalized_weights` 在 `api_inventory.csv:325` **精确在册**；`p2_reject_stack_resolve_ex` 因在册的 `p2_reject_stack`、`solve` 是它的**子串**而被短路。这两枚**根本不查头文件语料** ⇒ 单删它们的墓碑注释**不红**。只有 `p2_sample_sky`、`p2_sample_sky_cached`、`p2_upm_ma_component_of_control` 走到 `:298`。
  5. **被点名的文档引用行数复算**（按门自己的取 token 口径：6 个 auth 目录 `.md` 的反引号 token，逐条过 `:184-293` 的全部 skip 支路）：可判红的 **findings = 6 处，落在 3 行**——`docs/contracts/PUBLIC_API.md:20`（`p2_sample_sky`、`p2_sample_sky_cached` 各 2 次）、`:21`（`p2_upm_ma_component_of_control` 2 次）。另 `docs/algorithms/PHASE2_UPM_IMPL.md:61` 的 `` `p2_upm_normalized_weights` `` 因第 4 点**不红**。成稿/机器表的"文档侧 16 处"我复算＝ `docs/**/*.md` 中 5 枚符号的**任意形态提及行数 16**（连 `docs/TRACEABILITY.csv`＋`api_inventory.csv` 则 18），口径与判红面不同轴。
  6. 批 A 其余先决面抽核：`docs/contracts/API_CONTRACTS.csv` 只有 1 行涉及五枚符号（第 280 行 `API-p2_upm_normalized_weights`），其**末列逐字 `VERIFIED`** ✓、行内含字面 `ADU/pixel/deg per header` ✓（故 `F-025` 拿该串行数当复算命令的读数确会被清理动作改变 ✓）；`check_api_contracts.py:19` 的 `api_csv` 逐字为 `docs/contracts/API_CONTRACTS.csv` ✓、`:128` 产 `API-SIG-NOREF`(P1) ✓。
- 与成稿差异：**范围与数字**，机理与结论不变。"顺序必须文档侧→头文件注释，反向即红"**成立**（对 3 枚符号是硬红；对另 2 枚，红/绿由 `api_inventory.csv:325` 决定）；而 md 把它写成"5 枚一律即红"。⚠ **新增第三条先决面（清单未登记）**：`api_inventory.csv:325` 与批 A 互为先决——该行的"可直接删"处置见本清单 §3.1 更正#1（我在该节判推翻），若照它删行、再删注释，`PUBLIC_API.md:22` 与 `PHASE2_UPM_IMPL.md:61` 立刻判红；两个动作分属第 1 组与第 2 组，跨组无顺序约束 ⇒ **按清单字面执行会踩红**。
- 影响 D8 的处置：进任务书，但顺序判据按"3 枚符号必红＋2 枚经 inventory"改写，并把 `api_inventory.csv:325` 并入批 A 同批。分组键方面：批 A 的行号枚举 {12-23、25、26} 与 `groups.json` 冲突——18→A、25/26→C；批 F 的 {60-64、85} 与第 3 组表 {62、85} 重号（实物：62→C、85→B）。**机器表互斥，散文枚举不互斥**。

## 1.3 第 3 组：不许删（57 条）· md:86-104
**判定：降级（"不许删"对 ≥3 件过强：第 3 组里确实留着还能再删一层的东西）**
- 我的独立取证：载体表内枚举行号合计 **49**（6+4+3+6+5+3+5+1+7+9），比自报 57 少 8；md:88 自己写"完整 57 条见 groups.json" ⇒ 属自认摘录，不算虚报，但 62/85 两处与批 F 重号（见上）。实物核对：文件行 116 的对象是 `lib/algorithms/calibration/src/v6_calibration_covariance.cpp:54 与 :680-706`（md §1.3/§2.2 均只写裸文件名，易误读为 coverage 目录）；该文件**通篇不含 `RETIRED-CODE-RETAINED`**（marker=False，745 行），:54 起是 `/* 裁决 PSFSW-RETIRE-01 … */` 注释块。
- **还能再往下删一层的实例**：`healpix_core.cpp:1`、`healpix_core.h:1`、`orchestrator/cpp/include/orchestrator.h:1` 三件被第 3 组钉成"合规保留件·唯一载体"。它们的块文本自述是"整文件退役"＋`EXIT: 引用清点为零（drizzle 模块内 grep 无 #include "healpix_core.h" 消费点…）`——即**块内自证的负断言**，不是任何门的输入。我用忠实重写的 R2/R3 判据实测：现网 R2 命中的 RED=0（含 `MARKER in text` 全文短路语义），删这三件不会让任何门失去输入；`CHK-RETIRED-CODE` 只读注释文本、不锁文件名。⇒ 它们的"唯一载体"身份不成立，实际作用只是给 §2.2 的 D2 当负例样本（可换 tempfile 夹具）。这三件应从第 3 组下沉第 2 组。
- 反证第 3 组大方向正确：`coverage.cpp:369-390`/`coverage.h:78-93`/`stage2_common.cpp:445`/`retired_object_reject_negative_test.cpp`（文件行 86 处置逐字"不许删：它们是标准 04 §4:42…唯一防回流机制"）等确为门的输入，`groups.json.C` 与之一致 ✓。
- 影响 D8 的处置：进任务书，但**三件（healpix_core.cpp/.h、orchestrator.h）改判"先解引用再删"**；第 3 组的枚举行号须补齐成 57 条或改为引用 `groups.json.C` 原文。

## 2. 可机械化判据提案：怎么判"退役标注不合规" · md:105-106（含 §2.0 前提计数）
**判定：降级（提案方向与"不新造第二套块格式"的结论确认；§2.0 三个自报数两个精确、一个虚高 2）**
- 我的独立取证：
  - `1950` **逐字命中**：`git ls-files` 中扩展名 ∈ {c,cpp,cc,cxx,h,hpp,py} 者 2065，排除路径含 `third_party` ⇒ **1950**。
  - `RETIRED-CODE-RETAINED 块 12（合规 11／假阳性 1）` **不成立**：含该字样的跟踪文件共 11 个，其中 `eng/ci/check_retired_code.py` 是被查面之外的门自身（其 `:53`/`:102` 明确排除 `SELF_REL`，10 处命中全是 `--self-test` 夹具字符串），`eng/tests/unit/v6_p3_proj/sin_roundtrip_gate.py` 是 md 自认的假阳性 ⇒ **真块 = 9**，且我用 R3 的同注释 run 判据逐个跑，9 块全部六 token 齐备（`missing=[]`）：`rejection.cpp:2622`、`healpix_core.cpp:1`、`healpix_core.h:1`、`p3_proj_v6.cpp:1`、`p3_proj_v6.h:1`、`star_detector.cpp:30`、`orchestrator.h:1`、`p3_v6_export.cpp:1`、`p3_v6_export.h:1`。⇒ 应为 **9 块 / 9 合规 / 1 假阳性**。
  - `R2 候选 0 条` **确认**：我按源码语义（`BANNER_TOKENS` 十枚、`_COMMENT_LINE_RE`、`BANNER_LABEL_RE` 的去装饰与前缀「状态:」、`BANNER_WINDOW=40`、排除 self/`lib/infrastructure/aio/`/*oracle*、`eng/ci/retired_code_allowlist.json` 的 13 条 pattern）离线重跑 ⇒ **RED=0、ALLOWED=0**。附带一条 md 没写的成因：9 个块文件的前 40 行确有 RETIRED 横幅，但 `:491` 的 `has_marker` 是**全文** `MARKER in text`（`:432`）⇒ 被短路。所以 R2=0 是"被 MARKER 全文短路"而非"无横幅"，这个区别决定了 §2.2 D1 的写法（见下节）。
  - §2.0 的"六字段里没有'替代实现'、没有'日期'"**确认**：`check_retired_code.py:87-88` 逐字 `FIELD_TOKENS=("WHAT:","WHY-KEPT:","STATUS:","EXIT:","AUTHORITY:")`，别无他字段。
  - §2.3.4 引用 allowlist 语义"**只抑制标记提及、不抑制字段缺失**"**确认**：源码 `:56` 与实现——`allowlisted()` 只出现在 R1（`:467`）与 R2（`:505`）支路，**R3 支路无 allowlist 判断** ⇒ 字段缺失不受豁免。
- 与成稿差异：仅数字（12/11→9/9）与"R2=0 的成因"。
- 影响 D8 的处置：提案可进任务书；§2.0/§3.1 两处计数须订正。

## 2.2 三形态判据（含正负例） · md:141-149 —— **Q2 主判**
**判定：降级（D3 确认；D1 有空转支；D2 的豁免支两侧都无证据且是纯文字通行证；"向后兼容"确认但提案漏写一条硬约束）**
- **"在既有 6 token 之后追加两字段是否真兼容"→ 兼容，确认**（逐跳）：R3 只检查 `REQUIRED_TOKENS`（6 枚）是否在同注释 run 内、且只对 `FIELD_TOKENS` 逐枚查空值（`:439-447`）⇒ 未知字段行**既不会让整行判废也不会被静默当成缺项**，现网 9 块加两行仍绿；R1 对含 MARKER 的块整块豁免（`:460`）⇒ 新字段不触发"被注释掉的旧逻辑"；R2 看 `has_marker` 全文（`:432/:491`）⇒ 与新字段无关。**没有"行数必须=6"或"末字段后必须紧跟结束线"之类的假设**，md 的"向后兼容"判断站得住。
- ⚠ **提案漏写的硬约束（会造成一次性恒红）**：新字段必须落在**同一注释 run 内**。`comment_runs`（`:279-293`）以非注释行/空行断块；现格式块尾有一行 `// ────` 分隔线，而 §2.1 的示意（md:128-136）**没画这条分隔线**。实施者若把 `REPLACED-BY:`/`AS-OF:` 写在分隔线之后或隔一行写，两字段就掉到块外 ⇒ 按"块内必须有 REPLACED-BY"实现会把现网 9 块全判红；反之若退化成"同文件任意位置"就成了贴哪儿都算的 fail-open（`has_marker` 已经是这个反面教材）。判据须写死"块=含 MARKER 的注释 run，扩展字段位于该 run 内、结束分隔线之前"。
- **D1**：负例 `upm.h:181` 能红——但只可能由第二支（存在 `/* RETIRED … */` 单行标记）触发：第 181 行 > `BANNER_WINDOW=40`，而我实测第一支（前 40 行横幅）现网 RED=0 ⇒ **第一支在基线上空转**。第二支无形状约束（未给正则、未限定注释标签位），正是本门在 CLEAN-401 校准中已放弃的 token-anywhere 口径（`:109-115` 注释记载：该口径得 44 命中、抽样 25 条全为散文 ⇒ 恒红）。**提案 §2.3.5 自己引了这条教训，却没在 D1 里防它**。
- **D2（本批最高优先缺陷）**：豁免支＝"块内含 已删除/物理删除/接受面 = 空/拒绝面 白名单陈述"。我逐字统计 9 个真块：这四组词出现 **0 次**、`REPLACED-BY` 出现 **0 次** ⇒ (i) 提案字面实现下 **D2 存量是 9 块，不是 §2.5 说的 3 块**（少报 6）；(ii) 该豁免**没有任何负例探它**（给出的负例 `healpix_core.cpp:1` 是无块外一切字段的普通块，落在非豁免支）；(iii) 更糟：它给出的**唯一豁免正例锚是空锚**——`v6_calibration_covariance.cpp:54` 所在文件通篇无 RETIRED 块，D2 只对"块"生效，所以那条"绿"是因为规则不适用，而不是"命中白名单"。⇒ **豁免支在两侧都无证据 = 任何块粘一次"拒绝面"三字即永绿**，与本仓已抓到的"自检只注入非豁免行 ⇒ 豁免即 fail-open"同型。
- **D3**：**确认**。正例 `rejection.cpp:2622` 块的 `EXIT/AUTHORITY` 逐字含"DOC-402 整体退役…""ENGINEERING_SPEC.md §2 / §3""RELEASE-04 GAP_AUDIT G2-2""eng/contracts/data/v6_clause_registry_v1.json:196-197" ⇒ 有可解析条款凭据 ✓；"须把 门ID＋步序号 列入合法值表否则错判 10 条"的告诫**有力**：10 条墓碑（5 .h＋5 .cpp）**逐字都带 `(CHK-PROD-WIRING W1)`**，我逐条读到 ✓。
- **D4**：正例确认——`rejection.cpp:2632` 的 `EXIT` 指 `coverage.cpp:376`，`lib/algorithms/coverage/src/coverage.cpp` 在跟踪集且共 **455 行** ⇒ 锚存活、不红 ✓；同字段另一路径 `eng/tests/unit/v6_p2_samp/v6_p2_samp_test.cpp` 亦在跟踪集 ✓。**但 §2.3.6 的实例指错**：它说"CSV 行 87 就是行号式 EXIT 不可核对的实例"，实物（`healpix_core.cpp/.h`，L87）的 EXIT 里**没有行号**，写的是"引用清点为零（模块内 grep 无 #include 消费点）"＝**已完成事实的自述**，D4 抓不到。缺陷类别对、实例错，需另立 D5"EXIT 必须是条件，不是自证负断言"。
- 影响 D8 的处置：**不照现文字进任务书**。补 4 条后才有"能红能绿"资格：① D2 豁免支加一条"落在豁免内的负例"（粘白名单词仍须给 `REPLACED-BY: NONE＋理由` 才算绿）；② D2 存量 3→9；③ D1 第二支给形状约束并声明第一支基线为 0；④ 扩展字段的块内位置写成判据；⑤ D4 实例改指、并加 D5。

## 2.4 需要负责人裁的一条 · md:159-167
**判定：确认（可上呈，且我这轮的证据让裁决更省）**
- 我的独立取证：代码侧确实**没有**"替代实现/日期"两维（`check_retired_code.py:87-88` 逐字六枚，无他字段）⇒ md 的"缺替代不可判、缺日期不可判"两句成立 ✓。`AS-OF` 拟接受的"负责人裁决号"在仓库文本里**真实存在**：`lib/algorithms/calibration/src/v6_calibration_covariance.cpp:54-56` 一带逐字出现 `PSFSW-RETIRE-01`（md §2.1 举的示例编号不是编造的）✓。
- 新增事实（利于裁决）：10 条墓碑**同时**带裸日期 `2026-09-25` 与门凭据 `(CHK-PROD-WIRING W1)` ⇒ 裁"代码注释可留日期"则 D3 一条都不红；裁"禁日期"则只需把凭据字段形式化（门 ID＋步序号已在册），**无需新增第二套块格式**，也不与文档禁日期口径打架。md:166"我按第一种写了判据，但这是规范层的取舍"这一定位恰当。
- 与成稿差异：无（仅补 `PSFSW-RETIRE-01` 在册这一硬证据）。
- 影响 D8 的处置：不进任务书，**上呈负责人**（§10 类：科学/规范口径两难）。

## 2.5 存量不合规清单 · md:168-179
**判定：降级（D1/D3 精确确认；D2 少 6 条；"一次改动消三形态"高估为两形态）**
- 我的独立取证：D1 的 5＋5＝10 处行号**逐字命中**（`upm.h:181/355`、`sampler.h:133/135`、`rejection.h:416`；`upm.cpp:1999/2849`、`sampler.cpp:1275/1277`、`rejection.cpp:2223`），且 10 条全部是单行 `/* RETIRED 2026-09-25 (CHK-PROD-WIRING W1): … */`，无一处带 RETAINED 块 ⇒ D1 与 D3 的 10 处重合**确认** ✓。D4 的 1 处（`rejection.cpp:2632`）抽样一致 ✓（其余 7 块未穷尽）。**D2 的 3 处不成立**：按提案字面谓词跑 9 个真块，无 `REPLACED-BY`、无白名单词 ⇒ 应命中 **9**（新增 6：`p3_proj_v6.cpp/.h`、`star_detector.cpp:30`、`p3_v6_export.cpp/.h`、`rejection.cpp:2622`）。
- md:178"一次改动可同时消三形态"**部分推翻**：删 10 条墓碑消掉的是 D1 与 D3（两者同址），**消不掉 D2**（D2 判的是 RETAINED 块，与墓碑不交）；且其中 2 条 `.h` 墓碑的删除对 `CON-DOC-SYMBOLS` 无门效应（被 `api_inventory` 短路，见 §1.2 第 4 点）。"最高优先动作"这一排序仍成立（它同时是 D2 与第 2 组批 A 的前置）。
- 影响 D8 的处置：进任务书，但 D2 行须按 9 块重写，"消三形态"改"消两形态"。

## 3. 覆盖率自报 · md:180-181
**判定：确认（标题行，无独立主张；实质随 §3.1/§3.2）**
- 与成稿差异：无。
- 影响 D8 的处置：不进。

## 3.1 扫了多少（每条都附实际命令，命令即口径） · md:182-207
**判定：降级（1950／R2=0／125 行／8 档类别／"是 74" 全部复算吻合；两处不吻合，其中"自我更正#1"方向性错误）**
- 我的独立取证（能复现的都复现了）：`1950` ✓ 精确；`R2 候选 0` ✓（离线重跑 RED=0）；`CSV 125 数据行`✓；`类别 8 档计数` ✓ 逐字（20/1/3/10/14/41/29/7）；`门/合同承载=是 74` ✓（我按该列首词统计得 74）。
- **不吻合 1**：`RETIRED-CODE-RETAINED 块 12（合规 11／假阳性 1）` → 复算 **9（合规 9／假阳性 1）**，虚高 2（依据见 §2 节：11 个含字样文件 − 门自身 − 假阳性）。
- **不吻合 2（推翻该子条目）**：md:204 的自我更正#1 把 `docs/architecture/api_inventory.csv:325` 改判为"**无门承载、可直接删**"（CSV 文件行 14）。事实：`check_doc_symbols.py:155` 逐字 `open(repo/"docs/architecture/api_inventory.csv", encoding="utf-8")` 并取 `symbol` 列，而该脚本正是注册门 `CHK-DANGLING` 的步 `CON-DOC-SYMBOLS`（`command` 逐字、`waivable:false`）。它给出的理由（`API-DOCS` 的 command 是 `check_api_docs.py`、`api_doc_consistency.py` 未注册）我复核：`git grep -c "api_doc_consistency|check_api_docs" -- eng/ci/checks.json` 得 1，且 1 处是 `check_api_docs` ⇒ 那句本身对，**但它漏了第三个读者**。该行还是 `p2_upm_normalized_weights` 此刻唯一的存在性凭据（在册即短路，不回头查头文件语料），效应链我已按门逻辑跑通：删 325 → token 落语料支 → 语料唯一文本是 `upm.h:181` 墓碑 → 仍绿；**再删墓碑 → `PUBLIC_API.md:22`＋`PHASE2_UPM_IMPL.md:61` 判 DOC-BAD-SYMBOL**。⇒ 正确处置是"先解引用再删＋与批 A 同批"，属第 2 组（现属第 1 组，`groups.json.A` 亦记 A）。**作废不得再引用"无门承载"这一句。**
- 附："报 0 引用前先自证"这一纪律我核到它确实执行了两处（`p2_upm_raw_weight` 应命中面 10 文件、`cheat_backend.cpp` 反查 harness）；但 R2=0 的自证只到"我没命中"，没到"我的判据面非空"——真正让它为 0 的是 `has_marker` 全文短路（见 §2 节末），这一层缺失使"0"的证据强度低于它自述的强度。
- 影响 D8 的处置：进任务书须改这两格；`CSV 行 14` 从第 1 组移入第 2 组并与批 A 绑同批。

## 3.2 未覆盖的目录与原因 · md:208-220
**判定：确认（附 1 条补强）**
- 我的独立取证：md:212 说 `CHK-RETIRED-CODE` 以 `PRUNE_DIRS` 排除 third_party ✓ 逐字（`:99-100`）；"所有门的现网读数未取 ⇒ 一律记 未裁／置信 中"与我这轮观察一致（md 未把任何依赖读数的条目写成已证），CSV `置信` 列实测 高 87／中 27＋若干"中——…"长句／低 1，无"低"冒充"高"；`eng/tools/quality/ 7 TU ＋ eng/tests/validation/ 2 TU` 的待证项＝CSV 文件行 115、`groups.json.U` 唯一条目 ✓ 实物对应。
- 补强（清单没登记的结构性双免）：该门扫描面**整体排除 `lib/infrastructure/aio/`**（`:101/:391`），而 `check_doc_symbols` 语料含 `lib/**/*.h`——两处叠加后，aio 目录内的单行墓碑既不受 RETIRED 门约束、其符号也只经 inventory/语料两条支路判定。凡第 1/3 组里落在 `aio/**` 的条目（如 `healpix_db/`、`hip_core.c`），其"无门承载"结论的成立范围比字面更窄。
- 影响 D8 的处置：不进任务书；补强句写进 md §3.2。

## 4. 本清单三条最重要的结论 · md:221-227
**判定：降级（结论 1 机理确认＋扩围；结论 3 的退役标注子项确认；结论 2 本轮待证）**
- **结论 1（墓碑注释给符号可解析判据喂活＝fail-open）——机理确认**：语料是头文件**全文**拼接（`check_doc_symbols.py:29-32`）、匹配是 `\b<token>\b` 全文正则（`:38`）、不剥注释 ⇒ "一条注释满足了一个存在性判据"成立；修法（先文档侧、再把语料收窄为声明面）**方向确认**。须改三处范围：(a) 5 枚中只有 3 枚经此通道，2 枚被 `api_inventory` 短路；(b) "文档侧引用"的可判红面是 6 findings/3 行，16/18 是任意形态提及行数；(c) **同一病第二处比第一处更大且清单没发现**——`:287` 的 `any(s in token for s in api_syms)` 子串兜底使**任何包含在册短名的文档符号免检**（448 行在册，含 `solve` 这种 5 字符名），无需任何注释参与。⇒ 结论 1 应改写为"存在性判据的两条兜底（头文件全文、inventory 子串）都在被非声明文本喂活"。
- **结论 2（"不被任何 target 编译"这一档最危险，54 条落该档，≥5 组有运行期消费者）**：本轮**待证**。缺的证据：按 `四档定位` 列逐行统计"不被任何 target 编译"档的行数（我未做，属派单明令留给下一波的 CSV 逐行活）；以及 5 组"运行期消费者"的 harness `文件:行`。可先记的两条旁证：CSV 确有 `四档定位` 列、`处置` 列里"不许删（改注册）"的行（文件行 112/113/114）逐字写明"由 pytest 现场 cc"，与结论 2 的定性一致。补法：下一波按列统计＋抽 3 组读 harness。
- **结论 3（同一能力第二套事实源是主要成因）**：其中"第二份退役标注格式（单行 `/* RETIRED 日期 */` 对六字段块）"我**确认并加硬**：现网确实两套并存——10 条单行墓碑 vs 9 个六字段块，而且**机器面为零判据**（R3 只对含 MARKER 的块生效；R2 的 40 行窗口对 10 条全不覆盖；`has_marker` 全文短路使带块文件免 R2）⇒ 双格式不是风格问题，是"其中一套完全没人查"。其余四个"第二套"（Makefile／PipelineFrame／同名实现／ID 命名空间）属其它批复核范围，本批不判。
- 影响 D8 的处置：结论 1、3 进任务书（按范围订正后）；结论 2 挂"待证"进下一波。

---

## 本批判定汇总（各态计数，附复算命令）
- 12 节：`确认 3`（§2.4、§3 标题、§3.2）／`降级 9`（§1、§1.1、§1.2、§1.3、§2、§2.2、§2.5、§3.1、§4）／`推翻 0`（节级）／`待证 0`（节级，§4 内 1 项子结论待证）。
- 节内子条目另有 2 处**推翻**：§3.1 自我更正#1（"api_inventory.csv:325 无门承载可直接删"）、§2.5 "一次改动消三形态"；1 处待证：§4 结论 2。
- 主要复算命令（全部只读）：
  - `git -C "F:/Astro dev/Astro CS Normalization Database" grep -n -e p2_reject_stack_resolve_ex -e p2_sample_sky -e p2_upm_normalized_weights -e p2_upm_ma_component_of_control -- '*.h'` → 恰 5 行、全为墓碑。
  - 读 `eng/tools/quality/contracts/check_doc_symbols.py:13-38,155,287,298` ＋ `eng/ci/checks.json`（`CHK-DANGLING`→`CON-DOC-SYMBOLS`）；离线按门的 token/skip 逻辑复算 → 可判红 6 findings/3 行；文档任意形态提及 16 行。
  - 读 `eng/ci/check_retired_code.py:87-88,106,148-159,279-293,432-455,460-505`；离线按 R1/R2/R3 语义重跑 → R2 RED=0；跟踪集含 MARKER 文件 11 → 真块 9（六 token 全备）；9 块内 `REPLACED-BY` 0 次、D2 白名单四词 0 次。
  - `git ls-files` 扩展名 {c,cpp,cc,cxx,h,hpp,py}＝2065，排除 third_party＝**1950** ✓。
  - 只读 CSV＋`独立审计/复算件/retire/groups.json`：A14/B53/C57/U1，两两交集空、覆盖文件行 2..126 无缺 ⇒ 分组互斥且并集=全集；散文枚举行号与之冲突 5 处（18、25、26、62、85）。
  - `sed -n '280p' docs/contracts/API_CONTRACTS.csv`（末列 `VERIFIED`、含 `ADU/pixel/deg per header`）；`grep -n "api_csv" eng/tools/quality/contracts/check_api_contracts.py`（:19）；`grep -n API-SIG-NOREF` 同文件（:128）。

## 旧判偏松清单（= 上一轮给下游的虚假授权）
1. **`api_inventory.csv:325` "无门承载、可直接删"（§3.1 更正#1）** —— 它被注册门 `CON-DOC-SYMBOLS` 当符号存在性白名单读取；照此实施＝把 5 枚符号里唯一被 inventory 免检的那枚推回语料支，与批 A 撞车。作废。
2. **`D2 豁免支两侧都无证据，且唯一正例锚（`v6_calibration_covariance.cpp:54`）在该文件无 RETAINED 块`** —— 白名单陈述（含"拒绝面"三字）成为纯文字通行证；照 §2.3.2"沿用现网 self-test"还会继承同型盲区（`_case_green_allowlisted_marker` 只测"豁免抑制得住"，没有"被豁免文件里的 R1/R3 硬缺陷仍红"这一负例）。
3. **`D2 存量＝3`** —— 按提案字面谓词应为 9；整改面少 6 块。
4. **`第 3 组把 healpix_core.cpp/.h、orchestrator.h 钉成唯一载体`** —— 门的输入不含文件名，删这三件不使任何门失去输入；这一层可做的删除被挡住。
5. **`§1.1 的第 1 组三条件口径`** —— 4 行反例（18 门承载=是；49/51/53 连带≠无）。下游若拿它做机器分组会分错。
6. **`§2.0/§3.1 的块数 12（合规 11）`** —— 实为 9（合规 9）；覆盖率自证偏高 33%。

## 覆盖率自报（已判 N / 分配 M）
- 已判 **12 / 12**（`V404-S.txt` 全部 12 个 SEC），无"非空未判名单"。
- 本轮按派单**不做**：CSV 125 行逐行归属（下一波）、`§4` 结论 2 的档面逐行统计、门/合同现网读数（禁跑 `eng/**`，全部以离线语义重算替代并逐处标明"近似"）。

## 我对第①层判据本身的校准建议
1. **判"某文本对某门有无门效应"必须写到支路级**：先确定该 token/该行进到哪条 skip、查哪个语料，再下结论。§1.1 对 `.cpp` 的论证做到了（只 `rglob("*.h")`），但 §1.2 批 A 对 `.h` 的 5 枚没做同等支路核对，于是把 2 枚免检的写成必红、把 1 处真正免检的（inventory）没看见。
2. **每条豁免支配一条"落在豁免内"的负例**，并把这条要求写成给 D1-D4 的硬验收项（豁免正例还必须是"块内命中白名单"，不是"该文件语义像拒绝面"）。判据扩展要顺手给现网 `--self-test` 补 `red_allowlisted_but_hard_defect`（被豁免文件仍含 R1/R3 缺陷 ⇒ 必红），否则豁免语义是"没测过"而非"测过有效"。
3. **计数自报要"命令＋判据"两行都可离线复现**：本轮凡可离线复现的（1950、R2=0、125 行、类别 8 档、"是 74"）全部吻合，凡只能靠作者自实现近似的（12/11 块、16 处文档引用）全部有偏差——偏差集中在"自实现近似"处，故近似实现的关键中间量（含 MARKER 的文件数、被排除的门自身）应一并写进表。
4. **"CSV 行 N"须声明是文件行号还是数据行号**（本清单 25 处指针靠 4 个锚才能反推，差 1 就会把第 1 组与第 2 组的行读混，我最初的 62/85 重号判定正是被这个坑绊过一次）。
5. **存在性判据应收窄为声明面，且同步废掉子串兜底**：`_defined_in_public_header` 剥注释后再匹配，`:287` 的 `any(s in token …)` 改为精确匹配。只收窄语料不补子串，等于关掉一扇窗留着一道门。

<!-- PROGRESS: 12/12 -->
