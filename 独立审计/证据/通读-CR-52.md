# 通读 CR-52 · star_detection 图像原语 / 日志 / 两测试件

- 被审基线 HEAD=`c8f64e9a`；仓库 `F:\Astro dev\Astro CS Normalization Database`
- 派单：`DISPATCH-CODE-READ-PREAMBLE.md` §0–§5 全条为硬约束；批次 CR-52
- 独占清单（6 项）：
  1. `lib/algorithms/star_detection/src/sdet_image.cpp` —— 已读：是（表 54 项，findings 12 条 CR-52-01…12）
  2. `lib/algorithms/star_detection/src/sdet_image.h` —— 已读：是（表 4 项，findings 2 条 CR-52-13…14）
  3. `lib/algorithms/star_detection/src/sdet_log.cpp` —— 已读：是（表 14 项，findings 计入 CR-52-15…18）
  4. `lib/algorithms/star_detection/src/sdet_log.h` —— 已读：是（表 2 项，与 .cpp 合并成 §3/§4）
  5. `lib/algorithms/star_detection/test/sdet_fp64_test.cpp` —— 已读：是（表 25 项，findings 4 条 CR-52-19…22）
  6. `lib/algorithms/star_detection/test/sdet_saturation_cursor_test.cpp` —— 已读：是（表 19 项，findings 2 条 CR-52-23…24）
- 开工 `git status --porcelain` 原文：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

<!-- PROGRESS: 6/6 -->

---

# 1 `lib/algorithms/star_detection/src/sdet_image.cpp`（1029 行，全读）

## 1.0 本文件的消费面（先定事实，后面所有定级都按它收敛）

生产 FP64/FP32 检测通道 `sdet_api.cpp:1761/1763`（`sdet_detect_impl<float|double>`，σ=2.0）与
`:2543/2545` 只调用本文件的 **4 个**原语：`sdet_gaussian_blur_yvv`、`sdet_gaussian_blur_yvv_d`、
`sdet_robust_median(_d)`（经 :1775/:1777 与 `sdet_compute_bgnoise` sdet_api.cpp:463/466）、
`sdet_robust_mad(_d)`（sdet_api.cpp:464/467/480/483、1200/1498）。
其余原语的外部引用计数（`git grep -n "<名>" -- . ':!sdet_image.cpp' ':!sdet_image.h'`，本会话实测）：

| 原语 | 外部引用 | 唯一消费链 |
|---|---|---|
| `sdet_median_filter`(→3x3/5x5) | 1 | sdet_detector.cpp:23 ← sdet_get_structure_map ← sdet_api.cpp:1045/1342 |
| `sdet_dynamic_regional_background` | 1 | sdet_detector.cpp:31 ← 同上 |
| `sdet_truncate_and_rescale` | 1 | sdet_detector.cpp:45 ← 同上 |
| `sdet_local_maxima_map` | 1 | sdet_detector.cpp:62 ← `sdet_get_local_maxima_map` ← **零调用**（全跟踪面无第三处） |
| `sdet_gaussian_filter_separable` | 0 | 仅 :94 内部退化分支 |
| dilate/erode ×4、`sdet_downsample`、`sdet_upsample_bilinear`、`sdet_atrous_b3v_filter`、`sdet_extract_lowfreq_atrous`、`sdet_iterative_sigma_clip` | 0 | 无（`dynamic_regional` 不调它们） |

而 `sdet_detect`/`sdet_detect_debug` 两个入口在登记面为**非生产/不可达**：
`eng/ci/prod_wiring_baseline.json:209-210`（`unreachable:manifest.source_symbols:…`）、
`docs/contracts/PUBLIC_API.md:835`「旧 uint16 入口…非生产，DISP-STAR-005」、
`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:243/255/276`「旧结构图路径（非生产）…生产 impl 不调用结构图…已退出生产」。
⇒ 凡只挂在这条链上的缺陷，本成稿一律按「缺生产触发路径」封顶 S2/S3，并逐条写明；不借用邻批结论。

## 1.1 §2.5 数值处置表（sdet_image.cpp）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :14,:16（y 向 :16,:17 同族） | `-x-1` / `2*w-x-1` | 1、2 | 越界像元反射映射（REFLECT_101 整样本对称）的位移量 | 结构性不适用 | 定义式：i<0→−i−1、i≥n→2n−i−1 即 OpenCV `BORDER_REFLECT_101`；本文件 :47/:61/:76/:298/:325/:353/:371/:397/:459/:599/:615/:628/:709/:725/:738 全部经它取像元 | 非科学量（不含量纲、不是阈值）。但**镜像约定本身条文面零登记**：检索式 `git grep -in "mirror\|镜像\|reflect" -- docs/algorithms/STAR_DETECTION_ALGORITHMS.md docs/science/STAR_DETECTION.md docs/plugins/algorithms_phase1/03_star_detection.md README module.yaml` → 仅 03_star_detection.md:66/:72 命中，且那里的「镜像」= WCS parity 位，不是边界条件 | CONFIRMED |
| :14-19 的第二次折叠 | 单次折叠＋`max/min` 钳位 | — | 折叠只做一次，再越界即钳到端点 | 结构性不适用 | 半径 < w 时单次折叠必够（\|x+k\|≤w−1+radius）⇒ 仅 radius≥w（大核/大 SE/大 step）时退化为端点复制 | 影响：radius≥w 的形态只出现在 :334/:364/:381 的 SE 半径与 :588 的 2·step，均非生产 σ=2.0 路径 ⇒ 见 CR-52-12 | CONFIRMED |
| :26、:93、:199 | σ 下限 | `0.5` | 低于此不做任何滤波（memcpy／逐元复制） | 待确认 | 条文面零提及（`git grep -n "0\.5" docs/algorithms/STAR_DETECTION_ALGORITHMS.md` 无 σ 下限条目；文档只登记 σ=2.0，:52/:228/:122） | 保守方向：0.5→更小（更严格地拒绝退化请求）。影响范围：所有调用方传入 σ≤0.5 时得到恒等图；生产传 2.0 ⇒ 不触发 | CONFIRMED（值无锚） |
| :31 | FIR 截断窗 | `3.0 * sigma`（+`ceil`，+`2*radius+1`） | 可分离高斯核半径 = ±3σ | 待确认 | 条文面零提及；文档登记的高斯核是 **YvV IIR 无限支撑**（SCIENTIFIC_REFERENCES.md:78 文章级），未登记任何截断窗 | 保守方向：3.0→4.0（窗更大 ⇒ 更逼近真高斯、更慢）。影响范围：仅 :25-90 FIR 路径，即 :94 的小画幅退化分支与 σ≤0.5 分支（后者在 :26 已先返回）⇒ 实际只在 w<4 或 h<4 且 σ>0.5 时生效 | CONFIRMED（值无锚） |
| :37 | 高斯指数 | `-0.5`、`σ*σ` | k(d)=exp(−d²/2σ²) | 公式导出 | 归一化高斯定义式；代码 :37 逐字按式（含 d=i−radius） | — | CONFIRMED |
| :40 | 截断重标 | `kernel[i] /= sum` | 截断后重新归一 ⇒ DC 增益=1 | 公式导出 | Σk=1 的归一化定义；我的复算（复算/CR-52/yvv.py 参考段）确认 FIR 与 IIR 两路对常数输入偏差均 ≤2e-14 ⇒ 「截断重标」这一支**实现与定义一致，无 finding** | — | CONFIRMED（复算） |
| :93、:199 | IIR 最小画幅 | `w < 4 \|\| h < 4` | 递归初值用到 t[0..2]、边界块用到 t[w−3..w−1] ⇒ 至少 4 列 | 结构性不适用 | 由 :148-150/:156-162/:245-259 的下标范围直接导出（不是科学量） | 非科学量。但**两副本在同条件下降级成不同算子** ⇒ CR-52-03 | CONFIRMED |
| :100、:206 | YvV 分支点 | `2.5` | σ<2.5 与 σ≥2.5 两套 q(σ) 近似的分界 | 待确认 | 候选源 Young & van Vliet 1995（docs/references/SCIENTIFIC_REFERENCES.md:78、docs/science/STAR_DETECTION.md:167），**两处均自标「核验状态：文章级，未逐式核验」**⇒ 给不出式号即落此档 | 保守方向：分界取小值一侧用更长核（更准、更慢）。影响范围：σ=2.0 走 :101 分支（生产值 2.0 在界内） | UNPROVEN（走过的途径：①docs 全文 `git grep -i "triggs\|gauss\.cc\|calculateYvVFactors"` → 仅本文件与 sdet_image.h 自引；②文献池只有文章级条目；③无第三方源码在跟踪面可比式） |
| :101/:207 | q(σ) 低支系数 | `3.97156`、`4.14554`、`0.26891` | σ<2.5 时的等效极点间距 q | 待确认 | 同上（文章级、未逐式） | 方向：q 偏大 ⇒ 核偏宽 ⇒ 平滑过强。影响范围：生产 σ=2.0 正用这一支 | UNPROVEN（同上） |
| :103/:209 | q(σ) 高支系数 | `0.98711`、`0.96330` | σ≥2.5 时 q≈0.98711σ−0.9633 | 待确认 | 同上 | 同上；σ=2.0 不走此支 | UNPROVEN |
| :105/:211 | b0 | `1.57825`、`2.44413`、`1.4281`、`0.422205` | 三极点归一化分母 | 待确认 | 同上 | b0 偏 ⇒ 整体增益偏（DC 由 :109 的 B 强制归一，故只改形状） | UNPROVEN |
| :106/:112、:107/:113、:108/:114 | b1、b2、b3（含 `2.85619`、`1.26661`） | 同族 | 递归反馈系数 | 待确认 | 同上 | 直接决定核的冲击响应；我的实测：实现核 ‖k₁‖₂²=0.133174 vs 精确高斯 0.141047（−5.582%）、峰高 +2.3% ⇒ 见 CR-52-01 | UNPROVEN＋CONFIRMED（数值表征自测） |
| :109、:215 | `B = 1 − (b1+b2+b3)/b0` 的 `1.0` | — | 前向增益，使 DC 增益为 1 | 公式导出 | 代码按式算 ✓（复算：常数 7.0 输入经整条 2D 链后最大偏差 1.688e-14） | — | CONFIRMED（复算） |
| :115/:220、:116-124/:221-229 | `M[3][3]` 九个分量 | — | Triggs-Sdika 镜像边界条件矩阵 | 待确认 | 代码自称「Triggs-Sdika 边界条件矩阵 M」（:114）＋ `gauss.cc:139-171`（:98）⇒ **两者在跟踪面与文献池均零命中**：`git grep -in "triggs" -- docs` → 0；`git grep -in "gauss\.cc\|gaussHorizontal\|gaussVertical\|calculateYvVFactors"` → 只有 sdet_image.cpp:98/:126/:142/:169 与 sdet_image.h:6 自身 | 方向：M 错 ⇒ 边界带值错、内部不受影响（复算证实内部对称到 1e-17）。影响范围：距边 O(几σ) 的 `smooth` 值 | UNPROVEN（一手锚不可得；替代证据＝复算：输出在 x↔w−1−x 下不对称，σ=2 时 2D 最大 6.37/1050≈0.61% 峰、1D 边界 0.03% 局部值 ⇒ 见 CR-52-02） |
| :129-130/:233-234 | M 归一化因子 `(1+b2+(b1−b3)b3)` / `((1+b1−b2+b3)(1−b1−b2−b3))` | — | 把 M 折算到本实现的状态 ordering | 待确认 | 同上（锚不存在） | 同上 | UNPROVEN |
| :134-138 | `Bf/b1f/b2f/b3f`、`Mf` 的 float 舍入 | — | FP32 通道的系数表示精度 | 结构性不适用 | 数据类型/精度选择，不含物理量（对照：文档要求 FP64 通道不降级 STAR_DETECTION_ALGORITHMS.md:274） | 非科学量。真正的问题是 :199 的退化分支，见 CR-52-03 | CONFIRMED |
| :148-150、:245-247 | 前向初值的 `3`（t[0..2]）与循环起点 3 | 3 | 三极点递归的起动样本数 | 结构性不适用 | = 递归阶数（极点个数），不是科学量 | — | CONFIRMED |
| :155-162、:252-259（y 向 :179-186/:275-282 同族） | `w-1`、`w-2`、`w-3`、`w-4` | — | 反向递推的边界装载下标 | 结构性不适用 | 同上（下标算术） | 下标顺序决定左右两端处理是否等价 ⇒ 复算结论见 CR-52-02 | CONFIRMED |
| :312、:328-329、:356-357 | `p[4]`、`buf.begin()+12`/`buf[12]`、`n/2` | 4、12、n/2 | 9 元／25 元／任意窗的中位下标 | 结构性不适用 | 中位统计量下标 = ⌊n/2⌋（奇数窗唯一定义） | — | CONFIRMED |
| :302-309 | 19 条 `SW(a,b)` 网络 | 下标对 | 3×3 中值的排序网络 | 结构性不适用 | 我穷举全部 9!=362880 种互异输入复算（复算/CR-52/median9.py）：**0 个反例，p[4] 恒为真中位** ⇒ 该网络正确，不报 | — | CONFIRMED（穷举复算） |
| :368、:395、:457 | `±1e30f` 哨兵 | 1e30 | 形态学 max/min 与极大值图的初值 | 待确认 | 条文面零提及 | 保守方向：用 ±FLT_MAX 或显式"无有效邻域"标志。**影响范围**：全窗非有限时该哨兵被原样写成像元值（`std::max/min` 的 `b<a` 形如 `NaN<a` 恒假 ⇒ NaN 被吞）⇒ 详见 CR-52-04/CR-52-11 | CONFIRMED |
| :426-428 | `range <= 0.0f` ⇒ 全 0 | 0.0 | max≤min 时把整幅归一化图写成 0 | 待确认 | 条文面零提及 | 方向：全 0 与"真实归一化结果恰为 0"不可分 ⇒ 冒充合法值（CR-52-11） | CONFIRMED |
| :432-434 | `v<min`/`v>max` 钳位＋`(v−min)/range` | — | 截断到区间后线性重标到 [0,1] | 公式导出 | 代码按式 ✓；但 NaN 对两个比较恒假 ⇒ **NaN 直通"截断"**（Inf 被钳到端点）⇒ 同一支内两行为不一致（CR-52-04） | — | CONFIRMED |
| :467 | `1.0f / 0.0f` 标志＋两个严格 `<` | 1、0 | 极大值图编码"是/否" | 待确认 | 条文面零提及；登记面只说「11×11 局部极大」（STAR_DETECTION_ALGORITHMS.md:283）而未定并列平台约定 | 保守方向：平台按"非极大"（现状）只会少判极大 ⇒ 偏漏检不偏多检（方向已在 CR-52-08 钉死） | CONFIRMED |
| :472/:502、:486/:516 | `n <= 0` ⇒ `0.0f`／`0.0` | 0 | 空样本时中位与 σ 的返回 | 待确认 | 条文面零提及 | 方向：0 是 σ 的**下界合法值** ⇒ 被下游当"零噪声"用（阈退化为 med 本身）⇒ CR-52-11 | CONFIRMED |
| :474-480/:504-510 | `n/2 − 1`、`* 0.5f`、`* 0.5` | 2、0.5 | 偶数样本中位 = 两中心次序统计量均值 | 公式导出 | 代码真按式算 ✓；与登记口径一致：`docs/algorithms/PHASE2_REJECTION.md:87`「nth_element 中位（偶数均值）」、`NOISE_ESTIMATION.md:213` | — | CONFIRMED |
| :495 | `1.482602218505602f` | 1.482602218505602（float 舍入） | MAD→σ 换算（FP32 通道） | 文献值 | `docs/science/NOISE_MODEL.md:312`（"唯一权威写法"）＋:366/:375（=1/Φ⁻¹(3/4)，Φ⁻¹(3/4)=0.6744897501960817）＋ Rousseeuw & Croux 1993 JASA 88(424)1273；float 域舍入的许可见 `docs/algorithms/COSMETIC_ALGORITHMS.md:71/:250` | 我独立复核 1/0.6744897501960817 = 1.482602218505602 ✓ | CONFIRMED |
| :524 | `1.482602218505602`（double） | 同上 | MAD→σ（FP64 通道） | 文献值 | 同上；FP64 全链路要求见 `docs/algorithms/NOISE_ESTIMATION.md:105`、`NOISE_MODEL.md:312` | — | CONFIRMED |
| :493/:523 注释里的 `135` | `NOISE_MODEL.md:135` | 135 | 常数出处的行锚 | **锚错**（见 CR-52-06） | 实测 `sed -n '135p' docs/science/NOISE_MODEL.md` 为空行；正确锚 :26/:62/:312/:366/:375 | — | CONFIRMED |
| :528-529 | `(float)sw/(float)dw`、`(float)sh/(float)dh` | — | 降采样网格比（面积元，可 <1） | 结构性不适用 | 网格映射定义，非科学量 | — | CONFIRMED |
| :534-535/:541-542 | `(int)` 截断、`ceil`、`min(iy1, sh)` | 1 | 求像元覆盖区间 | 结构性不适用 | 取整/钳位算术 | 但区间为空时见 :553 | CONFIRMED |
| :553 | `count > 0 ? sum/count : 0.0f` 的 `0.0f` | 0 | 空覆盖单元填 0 | 待确认 | 条文面零提及 | 方向：填 0 与"真值为 0"不可分（CR-52-11）；r≥1 时区间不空，仅 dw>sw 的误用会触发 | CONFIRMED |
| :559-560 | `(sw − 1)/(dw − 1)` 的 `1` | 1 | 栅格映射（半样本对齐） | 结构性不适用（定义常数） | 双线性映射定义 | 但 dw==1／dh==1 ⇒ 除零 → `(int)inf`/`(int)NaN` 为 UB（CR-52-09） | CONFIRMED |
| :587 | `b3v[5] = {1/16, 1/4, 3/8, 1/4, 1/16}` | 16、4、3/8 | à trous 三次 B 样条掩膜 | 公式导出（值）＋待确认（选型） | 值可导出：= 二项式 (1,4,6,4,1)/16 = 三次 B 样条的 dyadic 细分掩膜，Σ=1 ✓ 直流保持；**选型在本仓零登记**：`git grep -in "atrous\|à trous\|b3v\|小波" -- docs/algorithms/STAR_DETECTION_ALGORITHMS.md docs/science/STAR_DETECTION.md docs/plugins/algorithms_phase1/03_star_detection.md README module.yaml` → 0 命中 | 保守方向：掩膜归一化保持 Σ=1（现状 ✓）。影响范围：:676 的 `sdet_extract_lowfreq_atrous`，零调用者 | CONFIRMED（值）／UNPROVEN（一手文献锚：跟踪面无） |
| :588/:698 | `1 << scale` | 1、2^s | à trous 膨胀步长 | 公式导出 | 空洞算法定义 step=2^s ✓ | — | CONFIRMED |
| :595、:603、:623 | `2 * step` | 2 | 5 抽头核半宽（=⌊5/2⌋） | 结构性不适用 | 由核长导出 | — | CONFIRMED |
| :662/:805 | `med + clip_sigma * mad`（**无上侧对称项**） | — | 单侧（高值）σ 截尾阈 | 公式导出但**与登记口径相反定义** | 同模块登记的是双侧裁剪：`STAR_DETECTION_ALGORITHMS.md:430`「2 轮 `median±3σ` 裁剪」、实现 `wrapper_phase1/star_detector.cpp:110` `fabs(keep[i]-med) <= 3.0*s`；本文件的单侧式无任何条文登记 | 方向钉死：单侧裁剪只删高值 ⇒ 存活集中位**下降**（与双侧比较），见 CR-52-05 | CONFIRMED |
| :668/:811 | `clipped.size() < 10` 的 `10` | 10 | 裁剪后继续迭代的最小样本数 | 待确认 | 条文面零提及；**同族副本取 100**：`lib/algorithms/psf/src/dpsf_image.cpp:380`「only %d pixels left (< 100), stopping」 | 保守方向：10→100（更早在小样本上停手，避免末轮统计量建立在极少样本上）。影响范围：:787 的 `sdet_sigma_clip_med_of_rounds`（唯一生效副本）与零调用的 :645 | CONFIRMED（10 无锚＋两副本异值） |
| :677-678 | `(w + df − 1)/df` | 1 | 向上取整降采样尺寸 | 结构性不适用 | 取整算术 | df≥w ⇒ dw=1 ⇒ 触发 :559 除零（CR-52-09） | CONFIRMED |
| :687/:688/:691 | `n_scales >= 2` 的 `2`、`scale=1` | 2、1 | 只支持 1/2 级，≥3 静默按 2 级、≤0 按 1 级 | 待确认 | 条文面零提及 | 方向：静默截断不报错（参数被吞）。影响范围：零调用函数 | CONFIRMED |
| :695-785 | `#if 0` 内 `lin3={0.25,0.5,0.25}`、`layers[5]`、`n_layers>=3/>=4` | 0.25/0.5、5、3/4 | 已封存的 3 抽线性 à trous 分解 | 结构性不适用 | 不进编译单元（`#if 0`＋头文件 :35-36 同步注释掉）；线性掩膜 (1,2,1)/4 亦为二项式细分掩膜 | 非科学量，但**与在役 b3v 是同族两套掩膜**，登记面只见 b3v 无择因 ⇒ 计入 CR-52-10 | CONFIRMED |
| :790-794 | `n <= 0 ⇒ med=0, mad=0` | 0 | 空样本退化返回 | 待确认 | 条文面零提及 | 同 :648（CR-52-11） | CONFIRMED |
| :799/:811/:815-816 | `max_rounds`（调用点=3）、末轮 med/mad 取"各轮序列的中位" | 3 | 迭代上界的实现选择＋估计器定义 | 上界=结构性不适用；估计器定义=待确认 | 条文面零提及（`dynamic_regional` 一词在全部三份正本文档 0 命中，实测） | 方向钉死：把未裁剪的第 0 轮计入 ⇒ 背景中位偏高 ⇒ 检出偏少（偏红，不 fail-open）；`max_rounds<=0` ⇒ meds 空 ⇒ med=mad=0 ⇒ 阈=0（CR-52-11） | CONFIRMED |
| :831-833 | `step = block_size − block_overlap` 的 1（`max(1, …)`）与 −1（向上取整） | 1 | 块网格步进 | 结构性不适用 | 取整算术 | step==0（overlap==block_size）⇒ :835-836 除零；step<0 ⇒ 只覆盖部分像元 ⇒ :935 的 `mad_map=1.0` 分支被激活 | CONFIRMED |
| :831-833 的形参取值（本文件不持默认值） | block/overlap/sub/sub-overlap/clip/rounds/fill | 100/20/20/4/3.0f/3/16 | 区域背景网格与裁剪参数 | 待确认 | 取值在 `sdet_detector.cpp:31`（硬编码，非配置）；**条文面零提及**（同上检索式）；登记面 `STAR_DETECTION_ALGORITHMS.md:253-255` 只说这些"仅旧 sdet_get_structure_map 路径消费" | 保守方向：块越大 ⇒ 背景越平 ⇒ 低对比星漏检。影响范围：仅 sdet_detect/_debug 的诊断输出 out_detail/out_smap | CONFIRMED（无锚＝本成稿 CR-52-07） |
| :864 | `med + 1e-30f` | 1e-30 | 防除零的偏置 | 待确认 | 条文面零提及 | 方向：med<0（已扣背景的帧）时 ratio 变号 ⇒ 判据失义；med==0 且 mean>0 ⇒ ratio≈1e30 ⇒ 一律"异常块" | CONFIRMED |
| :865 | `0.707`（两处：下界与 1/上界） | 0.707 | 块"正常性"判据 mean/median ∈ [0.707, 1.4142] | 待确认 | 条文面零提及。文档里唯一的 0.707 族是**另一个量**：`STAR_DETECTION_ALGORITHMS.md:35`「行中位 → ×0.7071（1/√2）」= FnNoise1 的 √(1−ρ_adj) 噪声因子，与块均值/中位比无关 ⇒ 不可借作锚 | 保守方向：窗收窄（如 0.9）⇒ 更多块走子块细化 ⇒ 背景更局部。**影响范围**：非生产诊断结构图 | CONFIRMED（CR-52-07） |
| :915/:923 | `wt += 1.0f` | 1 | 重叠块等权累加 | 结构性不适用 | 等权定义（不含物理量） | — | CONFIRMED |
| :931 | `wt > 0` 的 0 | 0 | 是否有块覆盖该像元 | 结构性不适用 | 谓词定义 | — | CONFIRMED |
| :935 | `mad_map[i] = 1.0f` | 1.0 | 未被任何块覆盖时"伪造"的噪声 σ | 待确认 | 条文面零提及 | 方向：1.0 ADU 与真实 σ_bg（本模块量级 10¹–10² ADU）不可比 ⇒ 阈退化为 med+clip_sigma·1 ⇒ 几乎全图判星。**可达性**：现有调用点 100/20 ⇒ 覆盖完整 ⇒ 不触发（需 overlap≥block 的病态参数才触发） | CONFIRMED |
| :943 | `src > med_map + clip_sigma * mad_map` 的 clip_sigma（=同一个 3.0） | 复用 | 星掩膜阈与背景裁剪阈共用一个旋钮 | 待确认 | 条文面零提及；两角色无分别登记 | 方向：阈更低 ⇒ 掩膜更大 ⇒ 背景被抬高（自增强环，单轮无迭代故不发散） | CONFIRMED |
| :1004 | `c > 0.5` | 0.5 | 邻域有效像元数门（少于此改用 med_map） | 待确认 | 条文面零提及 | 方向：0.5 = "至少 1 个有效样本"，只保证分母非零，**不保证分子有限** ⇒ NaN 污染整片的入口（CR-52-04） | CONFIRMED |
| :949/:1028 | `100.0`（百分比）、`%.2f s` | 100 | 日志换算与格式 | 结构性不适用 | 单位换算定义常数（§4 明列不报） | — | CONFIRMED |
| :824/:1027 | `omp_get_wtime()` 计时差 | — | 分段耗时日志 | 结构性不适用 | 不含科学量 | — | CONFIRMED |

**本文件数值项合计 54 行处置**（同族合并处已逐一列位点）。漏项自查方式：对 :1-1029 逐行读过一遍，
字面量按「出现在表达式/下标/初始化列表/比较/日志格式」四类各自归入上表；无一行留白。

## 1.2 findings（sdet_image.cpp）

### CR-52-01 · 检测阈的"σ_smooth 单位换算"按理想高斯解析导出，实现核的噪声增益比它低 5.58%
- 轴：S（主）＋C
- 位点：`lib/algorithms/star_detection/src/sdet_image.cpp:106`（`double b1 = 2.44413 * q + 2.85619 * q * q + 1.26661 * q * q * q;`，同族 :105/:107/:108 与 :211-:214）
- 上位依据：`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:52-54`「**量纲声明**：阈值**作用图像**是 `σ_smooth = 2.0 px` 的 YvV 平滑图…而 `bgnoise` 取自未平滑原图 ⇒ 阈在平滑图噪声单位下 = `5.0/‖k‖₂ = 35.45 σ_smooth`（连续 2D 高斯核 `‖k‖₂ = 1/(2σ_smooth√π) = 0.14105`）」；同文 :57-60 由该换算继续推「过渡点 `SNR_peak = 5·(1+σ_smooth²/σ_psf²)`…实测系数 6.22 ± 0.50」
- 现状：代码实现的是三极点 IIR（:100-108 系数集 → :148-166/:171-191 双趟递归），不是连续也不是离散高斯。我把 :198-288（double 版）逐字转录成 Python（`独立审计/复算件/CR-52/yvv.py`＋`yvvp1d.py`）跑单位脉冲响应，实测：实现核 ‖k₁‖₂² = **0.133174**，精确归一化离散高斯 = **0.141047**（与文档解析值 1/(2σ√π)=0.141047 逐位相同）⇒ 实现核的噪声增益 **−5.582%**；同时实现核峰 = 0.204087 vs 精确 0.199471（+2.31%），max|IIR−镜像FIR| = 5.17% of 精确峰。
- 差在哪：把一个理想化解析值当成实现量使用——注册的换算系数没有按实现核取值或实测，且 :53 的等号写成"="而非"≈理想情形"。
- 后果：`5σ` 名义阈在**实际平滑图**上的显著性是 `5.0/0.133174 = 37.55 σ_smooth`，不是 35.45；一切由该换算派生的单位解释（§2:57-60 的过渡点式、"引用 5σ 必须声明 σ 属于哪幅图"那条纪律的数值落点）系统性偏 5.6%。该偏差与该处自报的实测散布 ±0.50（≈8%）同量级 ⇒ 名义"5σ 阈"与"SNR_peak"之间的对应关系不可复核。
- 定级：S2。**不改任何产物字节**（阈在代码里就是 `median + 5·bgnoise`，与 ‖k‖₂ 无关），失真的是**换算/解释主张**；按 §3「写不出后果即降级」的反向要求，此处后果是"对外主张解释失真"而非"产物错值"，且我未把 :57-60 的召回阈表证伪（那是实测表，不依赖此换算），故不升 S1。
- 怎么算修好：`STAR_DETECTION_ALGORITHMS.md:52-54` 把 ‖k‖₂ 改为**按实现核实测**（附脚本/证据位，且须落在跟踪面而非 `run/`），或明写"连续理想化对照值"并补记实现核偏差 −5.6%；若反过来要让实现逼近解析值（改 :105-108 系数或改用更高阶近似）则属数值等价前置变更，必须同批复跑 §11.4 的召回阈表与 `p1_snr`/`p1_sources.json` 容差基线，不得只改一侧。
- 置信：CONFIRMED（自复算；方法：转录 → 四个不变量核对：常数输入 DC 偏差 1.688e-14、脉冲响应关于源点对称到 9.0e-17、宽隆起内部偏差 0.059% 峰、δ 结构 5.17%；转录风险已用三个独立不变量压住，但按纪律声明：本成稿未与编译二进制比对，因构建/ctest 禁用）

### CR-52-02 · YvV 的"镜像边界"不成立：同一文件内左右两端处理不等价，输出破坏 x↔w−1−x 对称
- 轴：R（主）＋S
- 位点：`sdet_image.cpp:156`（`float temp2Wm1 = sW + Mf[0][0] * (trow[w - 1] - sW) + …`）与 :148-150（左端初值装载）、:161-162（右端两步特殊装载），double 版 :245-247/:253-259
- 上位依据：一手定义面「Triggs-Sdika 边界条件」——`sdet_image.h:6`「参考 gaussHorizontalSse + gaussVerticalSse 的 M 归一化与 **Triggs-Sdika 边界条件**」；`git grep -in "triggs" -- docs` → **0 命中**，`git grep -in "gauss\.cc\|gaussHorizontal"` → 仅本文件与 :h 自引 ⇒ 条文面零提及＋锚文件不在跟踪面；本文件另一条镜像定义是同文件 :13-21 `sdet_mirror_fetch`（REFLECT_101，被 :47/:76/:298/:353/:371/:397/:459/:599/:628 使用）
- 现状：递归把左端当作"初始条件"（:148-150 用 s[0..2] 组合出 t[0..2]），把右端当作"用 M 折算的反向状态"（:156-162），两者不是同一对称变换的两侧。
- 差在哪：真正的镜像对称滤波对**关于图像中心镜像对称的输入必须给出镜像对称的输出**（精确到舍入）。复算（`yvvp1d.py` 测试 2，N=241、输入严格对称）：输出不对称度 **1.60e-2 ADU**（边界值 50.34 ADU 的 3.2e-4），2D（`yvv.py`，33×33、峰 1050）σ=2.0 时 **6.37 ADU = 0.61% 峰**；σ=4.0 时 27.2 ADU = 2.6%。同时源点邻域内部不对称度只有 1e-17 ⇒ 不对称**只来自两端处理不等价**，不是舍入。
- 后果：`smooth` 在距边 O(几σ) 的带上取值随"从左还是从右算"而异；同文件的 FIR 路径（:94 的退化分支）用 REFLECT_101，两路径在边界带彼此不等价 ⇒ 「两副本同一算子」不成立。量级 0.6% 峰 ≪ 5σ 阈（几十 ADU），且生产 peaker 只扫 `x ∈ [r, w−r)`（r=5，见 sdet_api.cpp:1788 注释）⇒ **判定不改变产物字节**，故本条是定义一致性缺陷而非产物错值。
- 定级：S3（明写：若将来把 YvV 用于测光/背景数值而非候选筛选，本条升 S2）
- 怎么算修好：把 :148-150/:156-162（及 :245-259）改写成对偶形式（左端正解与右端正解由同一 M、同一顺序给出），并加**对称性锁**：输入 `img[x]=img[w-1-x]` ⇒ 逐位判 `out[x]==out[w-1-x]`（非退化控制，AGENTS §5）；同批在 `docs/science/STAR_DETECTION.md` 或模块正本写清边界约定（101 还是 111），并与 `sdet_mirror_fetch` 对齐。
- 置信：CONFIRMED（复算，方法同上）

### CR-52-03 · FP64 变体在小画幅下**完全不滤波**，与 float 变体（真做 FIR 高斯）不是同一算子，且注释断言为假
- 轴：C（主）＋R
- 位点：`sdet_image.cpp:200`（`// 退化路径: 直接复制 (浮点高斯在该参数下近似恒等)`）与 :199-203
- 上位依据：`sdet_image.h:8`「FP64 变体 - double 图像, **无 float 降级**」；`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:274`「FP64 通道（sdet_detect_ex_f64）**不降级**」；本文件 :196 自称「与 float 版逻辑一致, 系数/缓冲/递归全部 double」
- 现状：`_d` 版 :199 `if (sigma <= 0.5 || w < 4 || h < 4) { for (…) dst[i] = src[i]; return; }`；float 版 :93 同条件 `sdet_gaussian_filter_separable(src, dst, w, h, sigma)`（真做 FIR＋REFLECT_101 镜延拓，σ=2.0 时半径 6、13 抽头）。
- 差在哪：注释的"近似恒等"只对 `sigma <= 0.5` 那一支成立（该支 float 版也确实在 :26 memcpy）；对 `w<4 || h<4` 而 σ=2.0 的请求，高斯**远离恒等**，两版下降级成两个不同算子。
- 后果：同一条输入在 FP32/FP64 两通道上平滑图不同（一个是低通、一个是恒等），候选集与阈的可比性前提被破坏；PREC 名义的"不降级"（精度不降级）在此变成"换算法"。窄画幅（条带、裁剪子图、单列剖面、分块边带）是数据库常见输入形态。
- 定级：S2（生产画幅 ≥4 ⇒ 缺常规触发路径；但可达，条件明确）
- 怎么算修好：为 double 版补 `sdet_gaussian_filter_separable_d`（同一系数、同一 `sdet_mirror_fetch`），或在 :199 分支上 `sdet_log(SDET_LOG_ERROR, …)` 并让上层可判定失败；订正 :196/:200 注释与 `sdet_image.h:8` 的契约文字；同批加一条"σ=2、w=2 时 FP32/FP64 两路输出必须一致"的锁（现无任何锁）。
- 置信：CONFIRMED（读码＋条件逐项比对）

### CR-52-04 · 非有限输入在本文件 21 个原语里零处理：NaN 在 IIR 灌满整行以下、在 `nth_element` 触发 UB、在积分图污染右下整片、在 morph 处输出 ±1e30、在截断处直通；同危险类只在 wrapper 副本修过（生产入口未修）
- 轴：R
- 位点：（逐支，全部为本文件）
  1. `sdet_image.cpp:249`（`trow[x] = Bf * srow[x] + b1f * trow[x-1] + b2f * trow[x-2] + b3f * trow[x-3];`，float :152 同）＋ :253-259 边界块 ＋ :272/:285 垂直递归 ⇒ 一个 NaN 使**整行**变 NaN，并使**该行以下所有行的对应列**变 NaN（单向递推＋右端边界块回灌）；
  2. `sdet_image.cpp:474`（`std::nth_element(tmp.begin(), tmp.begin() + n / 2, tmp.end());`，同族 :478、:504、:508、:328、:356）⇒ NaN 破坏严格弱序＝**UB**；
  3. `sdet_image.cpp:943`（`if (src[i] > med_map[i] + clip_sigma * mad_map[i])`）＋ :961（`double val = star_mask[idx] ? 0.0 : (double)src[idx];`）＋ :969（`sum_img[idx] = val + sum_left + sum_up - sum_diag;`）＋ :987（`double s = sum_img[(ry1-1)*w + (rx1-1)];`）＋ :1004-1005（`if (c > 0.5) background[idx] = (float)(s / c);`）⇒ NaN 判"非星"→进前缀和→**其右下整片** rect_sum=NaN，而门只查计数不查值的有限性；
  4. `sdet_image.cpp:372-373`（`if (is_dilate) val = std::max(val, v); else val = std::min(val, v);`，:398-399、:460 同族）＋ :368/:395/:457 哨兵 ⇒ 全窗非有限时**输出 ±1e30f**；
  5. `sdet_image.cpp:432-433`（`if (v < min_val) v = min_val; if (v > max_val) v = max_val;`）⇒ NaN 两判均假 ⇒ **原样穿过"截断"**（Inf 被钳住，NaN 不受限：同一支内两行为不一致）；
  6. `sdet_image.cpp:467`（`dst[i] = (dilated[i] < src[i] && src[i] < limit) ? 1.0f : 0.0f;`）⇒ NaN 静默判"非极大"；
  7. :549/:579-581/:599-637/:861 算术直通（sum、双线性、卷积）。
- 上位依据：同模块**已修副本**自述机理与修法——`lib/algorithms/star_detection/wrapper_phase1/star_detector.cpp:50-61`「原 NaN 输入非 fail-closed —— **nth_element 遇 NaN 破坏严格弱序（UB）**＋ :68 的 `*sigma < 1e-9` 挡不住 NaN ＋ 阈值退化为 ≈bg ⇒ 2% NaN 帧产生 6844 个假源…；已实施修法（两道）：① **入口**逐像素 isfinite 归约…② 返回前 `if (!std::isfinite(*bg) || !std::isfinite(*sigma)) return false;`」，代码落点 :85-92、:124。条文面对本文件的有限性前提：**零提及**（检索式 `git grep -n "isfinite\|isnan" -- lib/algorithms/star_detection` ⇒ 命中 nls_lm.cpp / sdet_angle_guard.h / sdet_api.cpp / wrapper／tests，**sdet_image.cpp 命中 0**（1029 行）；`sdet_detect_impl` 的入参校验只有 sdet_api.cpp:1749 的非空指针检查）。
- 现状：生产通道逐层都不拦：`lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp:1899-1910` 的参数校验只覆盖 focal/pixel_size/指针/尺寸；:1935 `const T* pixel_data = pixels;`；FP32 支 :1946-1951 用 `v<0 / v>65535` 钳位后 `static_cast<uint16_t>(v)`——NaN 两判均假 ⇒ 依赖**未定义转换**吸收成任意整数；FP64 支 :1972-1976 把 `pixel_data` **原样**交给 `sdet_detect_ex_f64`。
- 差在哪：同一危险类在本模块的 wrapper 副本按 fail-closed 修了，生产 FP64 检测通道真正调用的那份（本文件＋sdet_api）**一行未修**，且入口的"钳位"在 FP32 支是 UB 兜出来的偶然行为、FP64 支根本没有。
- 后果（S1 触发路径，逐跳 `文件:行`）：
  ① `lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp:1972` `det_ret = g_dll.sdet_detect_ex_f64(detector_handle, pixel_data, img_w, img_h, …)`
  ② `lib/algorithms/star_detection/src/sdet_api.cpp:1763` `sdet_gaussian_blur_yvv_d(image, smooth.data(), width, height, 2.0);`
  ③ `sdet_image.cpp:249`→`:253-259`→`:262`（水平双趟）⇒ 含 1 个 NaN 的**整行**变 NaN
  ④ `sdet_image.cpp:272`/`:285`（垂直双趟）⇒ 该行以下所有行的该列变 NaN
  ⑤ `sdet_api.cpp:1845`/`:1855` peaker 在 `smooth[...]` 上比较 ⇒ NaN 与任何阈比较恒假 ⇒ 该区域**零候选**
  ⑥ `sdet_api.cpp:1777` `img_median = sdet_robust_median_d(image, (int)n);` ⇒ 命中 ② 的 UB 支
  ⑦ `lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp:1978` `if (det_ret != 0 || det_count <= 0) { … return -1; }` ⇒ 只要 NaN 位置之上/之左还剩 ≥1 颗，就判成功 ⇒ **返回一份被静默截断的星表，无任何降级标记**。
  **判据方向钉死**：候选集合只会**变小**（NaN 区域不产生候选），成功谓词只要求 `det_count > 0` ⇒ 少星不可能使该谓词变红 ⇒ **偏绿**（漏检方向的失败被吞）。
- 定级：**S1**
- 怎么算修好：① 在 `sdet_detect_impl<T>` 入口（sdet_api.cpp:1749 之后）加**逐像素 isfinite 归约**并 fail-closed，照 `star_detector.cpp:85-92` 已落地的同一写法（不改有限输入的逐位结果）；FP32 支 :1946-1951 的钳位改成显式判非有限，别靠 `static_cast` UB；② 本文件的中位/MAD 家族签名加失败通道（先滤非有限或返回状态码）⇒ 同批改 `sdet_image.h:24-28` 与全部调用点判红；③ 积分图填补的门（:1004）从"只查计数"改为"计数与分子有限性都查"；④ morph/极大值哨兵 ±1e30 改显式"无有效邻域"；⑤ 补 NaN/Inf 的正例＋负例锁（本批两个测试件都没有，见 §5/§6 节），负例判据方向：注入 1 个 NaN ⇒ 必须**报错或整帧判红**，不得只"不崩"。
- 置信：CONFIRMED（①-⑦ 全部读码到行；**唯一 PARTIAL 处**：我没有穷举 normalize 侧在把帧交给 platesolve 之前是否已有全局 NaN 填充/掩膜，故"生产输入实际含 NaN"这一前提记 PARTIAL；`pixel_data` 是 double 内存图且本链路上无逐像素门，这一条是 CONFIRMED）

### CR-52-05 · 同文件两份近似 σ 截尾副本返回值定义不同（末轮 vs 各轮中位）、裁剪方向与登记口径相反、最小样本门与 psf 副本异值
- 轴：C（主）＋R
- 位点：`sdet_image.cpp:815`（`*out_med = sdet_robust_median(meds.data(), (int)meds.size());`）与 :816、对照 :672-:673；裁剪式 :805/:662；样本门 :811/:668
- 上位依据：`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:430`「`estimate_background` 的返回值 = 2 轮 `median±3σ` **裁剪**后…RMS」、:432「两者互不调用」；双侧裁剪的代码正例 `wrapper_phase1/star_detector.cpp:110` `if (std::fabs(keep[i] - med) <= 3.0 * (s > 0 ? s : 1e-9))`；另一副本的样本门 `lib/algorithms/psf/src/dpsf_image.cpp:380`「only %d pixels left (< **100**), stopping」
- 现状：① :645 `sdet_iterative_sigma_clip` 返回**末轮存活集**的 med/mad；:787 `sdet_sigma_clip_med_of_rounds`（在用的那份，被 :858/:901 调用）返回**各轮 med/mad 序列的中位**（含完全未裁剪的第 0 轮）；② 两份都只写上侧裁剪 `threshold = med + clip_sigma*mad; if (buf[i] <= threshold) keep`（:666/:809），无下侧对称项；③ 本文件门为 10，psf 副本为 100。
- 差在哪：同一"迭代 σ 截尾"名目下三个面各有两值：返回值定义、裁剪侧数、最小样本门。条文面只登记双侧口径。
- 后果：区域背景（诊断结构图 out_smap/out_detail 的来源）系统性偏**高**——方向已钉死：单侧裁剪只会使存活集中位下降，把未裁剪的第 0 轮计入 ⇒ `out_med` 高于末轮值 ⇒ 背景抬高 ⇒ `out_detail = src − background`（:1018）在背景区偏负 ⇒ 同时 :943 星掩膜阈 `med + clip·mad` 抬高 ⇒ 判星更少 ⇒ 背景估计更少剔除亮像素，两效应当前配置（rounds=3）下不自激（只迭代固定轮数）。**判红方向：偏漏检，不 fail-open。** 而 :645 那份死副本一旦被再实现者取用，同一输入会得到另一个数值（末轮 vs 各轮中位），且无任何测试能发现（零调用者）。
- 定级：S2（生效那份的偏差量级未经实跑标定；死副本无运行时后果 ⇒ 不升 S1）
- 怎么算修好：二选一并在 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md` 登记为唯一口径（推荐：末轮存活集，:672-673 的做法），删除或 `#if 0` 封存另一份；裁剪侧数按条文改双侧或登记"高侧单侧是有意为之"并给理由；样本门与 `dpsf_image.cpp:380` 对齐（同族两处必须同值）；同批补一条"纯噪声输入 ⇒ 裁剪后 med/mad 与不裁剪差的解析预期"负例锁。
- 置信：CONFIRMED（读码＋方向推演；偏差量级未跑数 ⇒ 该子项 PARTIAL，缺"3 轮后各轮中位与末轮中位的实测差"）

### CR-52-06 · MAD→σ 常数的代码注释锚指向 `NOISE_MODEL.md:135`，该行实测为空行（同一错锚已被台账复制）
- 轴：C
- 位点：`sdet_image.cpp:493`（`// MAD→σ 换算: NOISE_MODEL.md:135 冻结 1.482602218505602 = 1/Φ⁻¹(3/4)`）与 :523 同文重复
- 上位依据：`docs/science/NOISE_MODEL.md` 该常数的实际登记点 = :26（表格行 `σ_bg | 1.482602218505602·MAD(|x−median|)`）、:62、:312（"唯一权威写法"）、:366、:375；`docs/algorithms/NOISE_ESTIMATION.md:213` 亦列同锚
- 现状：`sed -n '135p' docs/science/NOISE_MODEL.md` → 空行（实测，我贴的第 133-137 行原文里 :135 只有空格）。
- 差在哪：注释自称的"出处＋行号"不可判真伪（值本身正确，我复核 1/0.6744897501960817 = 1.482602218505602 ✓）。
- 后果：按锚核验的审计会判"锚不成立"，从而把一个**本已合规**的常数误降为无出处；同一错锚已在 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:45`（`docs/science/NOISE_MODEL.md(:17,:46,:91,:135)`）被复制，扩散到台账面。
- 定级：S3
- 怎么算修好：:493/:523 改指 `NOISE_MODEL.md:312`（或 :366/:375）；:17/:46/:91 三个同批错锚归 ledger 那条的宿主域处理，不在本批改。
- 置信：CONFIRMED

### CR-52-07 · 块"正常性"判据 `0.707`、防除零 `1e-30f` 与整套区域背景网格参数在权威链零登记，且隐含"输入为正且天光主导"的未声明适用域
- 轴：S（主）＋D
- 位点：`sdet_image.cpp:865`（`bool is_normal = (ratio >= 0.707) && (ratio <= 1.0 / 0.707);`）与 :864（`double ratio = mean_val / (med + 1e-30f);`）
- 上位依据：**条文面零提及**。检索式与结果：`git grep -n "dynamic_regional" -- docs/algorithms/STAR_DETECTION_ALGORITHMS.md docs/science/STAR_DETECTION.md docs/plugins/algorithms_phase1/03_star_detection.md lib/algorithms/star_detection/README.md lib/algorithms/star_detection/module.yaml` → 0；`git grep -n "0\.707" -- docs` → 唯一命中 `STAR_DETECTION_ALGORITHMS.md:35`，那里的 0.7071 是 **FnNoise1 行差分的 √(1−ρ_adj) 噪声因子**（:37-41 明确定义 `σ̂_n = σ_n·√(1−ρ_adj)`），与本处"块均值/块中位比"是不同物理量，不可借作锚；`git grep -n "1e-30\|1\.0 / 0\.707" -- docs` → 0
- 现状：网格参数由调用点硬编码 `sdet_detector.cpp:31`（`…, 100, 20, 20, 4, 3.0f, 3, 16)`），不经配置；判据把"块是否可信"归到一个无出处的 ±41.4% 相对窗上。
- 差在哪：科学语义的判据（哪些块要做子块细化，直接决定背景图与 detail 图）无任何条文/推导/标定锚；注释也未声明其前提（med 必须显著大于 0）。
- 后果：`sdet_detect_debug` 导出的 `out_smap`/`out_detail`（合同面 docs/contracts/API_CONTRACTS.csv:377 标 `deterministic per SCI`）无法被判真伪；若该路径被喂入已扣背景的帧（本模块的 detail 图本身就是扣背景产物，误接风险真实）⇒ med≈0 ⇒ ratio 失义 ⇒ **每块都判异常**并整体改走子块细化，背景统计量悄然换含义而无任何告警。可达性：`sdet_detect`/`_debug` 登记为非生产（prod_wiring_baseline.json:209-210）⇒ 不改正式产品字节。
- 定级：S2（缺生产触发路径）
- 怎么算修好：在 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md` 增补该路径的唯一口径（判据定义、0.707 的推导或标定、网格参数进配置）；或反向裁决该路径为退役并从合同/公共 API 面摘除（`#if` 删除不算裁决，需条文）；`1e-30f` 改成显式 `if (med <= eps) …` 分支并说明 med≤0 时的语义；**禁止**只改数值不改 `API_CONTRACTS.csv:377` 的 `deterministic per SCI` 主张（防改对判红）。
- 置信：CONFIRMED（"值无锚"＝检索式在案；"喂入已扣背景帧的后果"为推演 ⇒ 该子项 PARTIAL）

### CR-52-08 · `sdet_local_maxima_map`：radius≤0 时结构元为空 ⇒ 全图判为局部极大（fail-open），并列平台永不判极大
- 轴：R
- 位点：`sdet_image.cpp:445`（`if (dx == 0 && dy == 0) continue;`）与 :457（`float val = -1e30f;`）、:467（`dst[i] = (dilated[i] < src[i] && src[i] < limit) ? 1.0f : 0.0f;`）
- 上位依据：条文面零提及（`git grep -n "sdet_local_maxima_map\|局部极大" -- docs/algorithms/STAR_DETECTION_ALGORITHMS.md` → 只有 :283 描述 peaker 的 11×11 极大，属另一实现，且未定并列平台约定与 SE 半径语义）
- 现状：radius=0 ⇒ 双重循环只走 (0,0) 且被 continue 掉 ⇒ `se` 空 ⇒ :457 的 `val` 保持 −1e30f ⇒ `dilated ≡ −1e30` ⇒ :467 第一个条件对一切有限像素恒真 ⇒ 凡 `src < limit` 的像素全部标 1。
- 差在哪：签名对 radius 无下界约束，空结构元与"极大"结论之间没有守卫。
- 后果：一个参数误值即把"极大值图"变成"全 1 图"（下游按峰去重会退化成取满图）。方向钉死：这是**过度检出**（偏多）；而平台严格 `<` 判据是**欠检出**（偏漏）——两个方向相反的问题同在一支。可达性：唯一调用点 `sdet_detector.cpp:62` 传常量 `1`（4-邻域十字 SE），且该链为非生产 ⇒ 现不触发。
- 定级：S3（缺运行时触发路径，最高只到 S2，我按可达性进一步压到 S3）
- 怎么算修好：:438 入口 `if (radius < 1) { sdet_log(SDET_LOG_ERROR, …); 返回全 0 或状态码; }`（并写明"全 0"不等于"未测"，见 CR-52-11）；平台约定要么改 `<=` 要么在条文登记"并列不算极大"并给一个非退化锁（两个等高像元的人造图 ⇒ 期望输出必须可判定）
- 置信：CONFIRMED

### CR-52-09 · `sdet_upsample_bilinear` 在 `dw==1`／`dh==1` 时除零 ⇒ `(int)inf`/`(int)NaN` 为 UB 并随后越界读 `src`
- 轴：R
- 位点：`sdet_image.cpp:559`（`float x_ratio = (float)(sw - 1) / (float)(dw - 1);`）与 :560、:564（`int iy0 = (int)sy;`）、:574-577（`src[iy0 * sw + ix0]`）
- 上位依据：条文面零提及（本函数与 `sdet_downsample`/`sdet_atrous_b3v_filter`/`sdet_extract_lowfreq_atrous` 在 docs 面 0 命中，检索式见 §1.0 表下方）
- 现状：dw=1 时 `x_ratio = (sw−1)/0 = inf`，dx=0 ⇒ `sx = 0*inf = NaN` ⇒ `ix0 = (int)NaN`（C++ UB，典型实现给 INT_MIN）⇒ `ix1 = min(ix0+1, sw−1)`、:574 `src[iy0*sw + ix0]` 越界读；`sdet_extract_lowfreq_atrous` :677 `dw = (w + df − 1)/df` 当 `df >= w` 时恰为 1，即调用方能构造出该条件。
- 差在哪：唯一的尺寸守卫在 :528-529 的 downsample 侧也不存在，两个函数都假定 1≤dw≤sw 而无人检查。
- 后果：越界读 ⇒ 崩溃或读到无关内存（诊断产品路径）。
- 定级：S3（可达性差：`sdet_extract_lowfreq_atrous` 全跟踪面零调用者 ⇒ **缺运行时触发路径**，按 §3 封顶后仍因不可达压一档）
- 怎么算修好：两函数入口统一 `if (dw < 1 || dh < 1 || (dw == 1 && sw > 1) …)` 显式处理（复制或判红）；同批给 `sdet_extract_lowfreq_atrous` 的 `n_scales>=3` 静默截断（:687 只分 1/≥2 两档）与 `downsample_factor` 下界一起补登记或删除该死链（CR-52-10）。
- 置信：CONFIRMED（读码推得；未跑，构建禁用）

### CR-52-10 · 头文件以可调用契约暴露 21 个原语，其中 15 个在全跟踪面（含测试）零消费者，权威背景估计器同时存在三套且彼此关系只登记了两套
- 轴：D（主）＋C
- 位点：`sdet_image.h:11-33`（`sdet_median_filter_3x3` … `sdet_extract_lowfreq_atrous` 共 15 个声明，实测外部引用计数见 §1.0 表）；`sdet_image.cpp:695-785`（`#if 0` 封存的 `sdet_atrous_linear3_filter`/`sdet_atrous_decompose_layer2`，与头文件 :35-36 的注释行成对）
- 上位依据：`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:430-432` 登记了 wrapper `estimate_background` 与 `bgnoise` 两套估计量的关系（"两者互不调用"、实测比 1.5012），并明写 `:255`「生产 impl 不调用结构图」、`:276`「旧结构图局部背景路径已退出生产」；`AGENTS.md §7` 目录落位与 `docs/plugins/algorithms_phase1/03_star_detection.md:36`「保留，**不是**权威路径」
- 现状：本文件提供**第三套**背景估计（块网格＋子块细化＋积分图填星，:819-1029），但 :430-432 那段"三套关系"的登记里没有它，`docs` 面亦无 `dynamic_regional` 字样。
- 差在哪：条文穷举的"几个互不调用的估计量"漏了本文件这一套；死/半死原语仍作为契约面暴露（`module.yaml:86` 一类的登记面只列 sdet_detect，未覆盖这些内部符号）。
- 后果：再实现者/审计者无法判定哪个背景定义是权威（本批 CR-52-07 的 0.707 判据因此也无从对照）；`SDetParams.iterativeClipSigma/iterativeMaxRounds` 在配置面可设却**不影响本文件**（:822/:858 走的是调用点硬编码 3.0f/3）⇒ 配置面 4 字段无效的老问题在本文件侧有新证据（与登记 :253-255 一致，非新指控）。
- 定级：S2（D 轴结构性；不改产物字节）
- 怎么算修好：要么在 `STAR_DETECTION_ALGORITHMS.md` 的"互不调用的估计量"段补第三套并标消费面，要么把 15 个零消费原语随 `sdet_detect/_debug` 的去留裁决一并处置（裁决归 P1-STAR-IMPL，见 :297）；`#if 0` 块要么删要么登记为封存理由
- 置信：CONFIRMED（全部计数与登记面本会话独立检索）

### CR-52-11 · 六处退化返回一律以合法域内的值冒充"未测/不可用"，函数签名没有失败通道
- 轴：R（主）＋S
- 位点：（退化取值同列）`sdet_image.cpp:427-428`（`range<=0` ⇒ 全 0）、:472/:486/:502/:516（`n<=0` ⇒ med/mad = 0）、:553（`count==0` ⇒ 填 0.0f）、:648-652 与 :790-794（`n<=0` ⇒ `*out_med=*out_mad=0`）、:934-935（`wt==0` ⇒ `mad_map[i]=1.0f` 且 `med_map[i]` 保持 0）、:368/:395/:457（全窗非有限 ⇒ 输出 `±1e30f`）
- 上位依据：`AGENTS.md §5`「判据必须非退化：真值无效应时度量归零或判红」与 §9「空断言、SKIP 充数都算未完成」；条文面对"这些量取不到时该报什么"**零提及**
- 现状：全部六种失败态都返回可被下游当真实值使用的数（0、1.0、±1e30），且 `sdet_log` 只在正常路径出现（:660/:827/:949），失败分支**静默**。
- 差在哪：`mad=0`/`mad_map=1.0` 直接进 :943 与 :662/:805 的阈：阈 = med + clip·0 = **med 本身** ⇒ 任何高于中位的像素判为星（掩膜接近半图），或阈 = 0 + 3·1.0 = 3 ADU ⇒ 几乎全图判星；`±1e30` 会被 :39-40 的 `smap[i] > max_val` 吞掉（NaN/哨兵同型问题，见 CR-52-04）后进入归一化。
- 后果：背景/detail 图在退化帧（全黑块、全 NaN 块、极小帧）上给出"看起来正常"的图，星掩膜统计日志（:949 `Star mask: %zu / %zu pixels (%.2f%%)`）会把 fail-open 的 90%+ 掩膜当普通信息打出来，不判红。可达性：`wt==0` 与 `max_rounds<=0` 需病态参数（现调用点 100/20、3 不触发）；`n<=0`、`range<=0`、全窗非有限**在现有调用点上可达**（空块/全等值块/NaN 输入）。
- 定级：S2（同链非生产 ⇒ 封顶；判据方向：阈被压低 ⇒ **偏多检/偏绿**，已钉死）
- 怎么算修好：给这批函数加状态返回（改 `sdet_image.h:20-25`、:30-46 签名 ⇒ 同批全部调用点判红），或至少失败分支 `sdet_log(SDET_LOG_ERROR, …)`＋在输出图上写显式不可用码；禁止用 0／1.0／1e30 作"没测到"的暗号
- 置信：CONFIRMED

### CR-52-12 · 大结构元／大膨胀步长下"镜像"退化为端点复制，与同一函数的中等尺寸行为不连续
- 轴：R
- 位点：`sdet_image.cpp:14-19`（单次折叠后 `std::max(0, std::min(x, w-1))`）被 :353（`sdet_median_filter`）、:371（box SE）、:397（circle SE）、:459（local maxima）、:599/:615/:628（atrous 大 step）调用
- 上位依据：条文面零提及（镜像约定本身无登记，见 CR-52-02 位点行的检索式）
- 现状：折叠只做一次 ⇒ 当 `radius ≥ w`（或 `2*step ≥ w`）时，`2w−x−1` 仍可能越界 ⇒ 落进钳位 ⇒ 语义从"镜像反射"静默变成"端点复制"。
- 差在哪：同一函数在不同半径下悄悄换了边界条件；无一条断言区分两域。
- 后果：大结构元/大膨胀步长下背景与形态学输出的边界带取值与"镜像"定义不符（潜在不一致）。
- 定级：S3（仅影响 `hotPixelFilterRadius` 或 `fill_radius` 被配到与画幅同量级的形态；现调用点 radius=1、fill=16 ⇒ 不触发，缺运行时触发路径）
- 怎么算修好：`sdet_mirror_fetch` 改成折叠循环（`while (x<0 || x>=w)`）或在 :334/:364/:381/:438/:586 入口拒绝 `2*radius ≥ min(w,h)`；同批把镜像约定写进条文（与 CR-52-02 合并修）
- 置信：CONFIRMED

---

# 2 `lib/algorithms/star_detection/src/sdet_image.h`（46 行，全读）

## 2.1 §2.5 数值处置表（sdet_image.h）

逐行读完：本头文件**不含任何数值字面量、阈值、缓冲系数或默认配置值**——所有带数字的记号都在标识符或注释里。
为避免"漏项＝未完成"的歧义，把可能被当数的记号逐个处置如下：

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :11、:12 | `sdet_median_filter_3x3` / `_5x5` 名中的 3/5 | — | 窗口边长写进了函数名 | 结构性不适用 | 名称里的数是**声明的核尺寸**（定义域常数，非科学量：它由 :290-332 的两段循环边界 `dy=-1..1`／`-2..2` 唯一决定） | — | CONFIRMED |
| :32 | `sdet_atrous_b3v_filter` 名中的 `b3v` | — | à trous 三次 B 样条掩膜的代号 | 结构性不适用 | 标识符；真实数值在该函数体内（`sdet_image.cpp:587`，处置见 §1.1 该行） | 掩膜值的锚缺失已记在 .cpp 侧，勿在此重复计数 | CONFIRMED |
| :9、:26-28 | 注释 `FP64`、`_d` 后缀 | — | 双精度变体的命名 | 结构性不适用 | 类型标记；但 :8 的**契约文字**与实现不符 ⇒ 记 finding CR-52-03（位点已含本行） | — | CONFIRMED |
| :3、:5、:7、:11-13、:15-22、:24-28、:30-33、:38-46 | 全部形参 `sigma` / `radius` / `scale` / `limit` / `clip_sigma` / `max_rounds` / `block_size` / `block_overlap` / `sub_block_size` / `sub_block_overlap` / `fill_radius` / `min_val` / `max_val` / `n_scales` / `downsample_factor` | 头文件**不给任何默认值** | 11 个科学/结构参数的传入通道 | 结构性不适用（本文件无值可处置），并登记一条 D 轴缺陷：默认值只存在于调用点硬编码（`sdet_detector.cpp:31` 的 `100, 20, 20, 4, 3.0f, 3, 16` 与 `:62` 的 `1`），声明面零说明 | `AGENTS.md §6`「不硬编码…由 profile 决定」的相关侧证＋`STAR_DETECTION_ALGORITHMS.md:253-255`（配置字段与消费面的对应） | 保守方向：数值应在配置面；影响范围见 CR-52-07 | CONFIRMED |

**核查方式**（可复核）：本文件 46 行逐行读，数字字符只出现在 :9/:11/:12/:26/:27/:28/:32/:33 的标识符与
`FP64`/`3x3`/`5x5`/`b3v`/`_d` 注释里；`:38-46` 的 `sdet_iterative_sigma_clip` 与
`sdet_dynamic_regional_background` 声明形参表无默认实参（无 `= <数字>` 出现）。

## 2.2 findings（sdet_image.h）

### CR-52-13 · 21 个原语声明零契约：无输入前提、无边界约定、无失败通道，退化行为只在实现里且两份不一致
- 轴：D（主）＋C
- 位点：`sdet_image.h:20`（`void sdet_truncate_and_rescale(float* data, int n, float min_val, float max_val);`）——该函数在 `max_val < min_val` 时把整幅写 0（sdet_image.cpp:426-428），声明面一字未提；同类：:3（σ≤0.5 时恒等）、:7/:9（`w<4||h<4` 两版降级成不同算子）、:13（radius 决定 NaN 行为从"吞掉"变"UB"）、:22（`limit` 语义是"上界"还是"下界"未写；实现 :467 取上界）、:33（`n_scales≥3` 静默按 2）、:42-46（11 个参数无一有取值域）
- 上位依据：条文面零提及（本文件的 21 个符号在 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md`、`docs/science/STAR_DETECTION.md`、`docs/plugins/algorithms_phase1/03_star_detection.md`、`README.md`、`module.yaml` 上的命中：`dynamic_regional`、`median_filter/中值滤波`、`atrous/b3v/小波`、`rescale/重标` 四组检索式均 **0 命中**，实测见 §1.1 各行"依据锚"列）
- 现状：全 21 个声明返回 `void` 或标量且无状态码；头文件里唯一的语义说明是 :5-6/:8/:14 的三行注释（其中 :8 为假，见 CR-52-03）。
- 差在哪：一个模块的"图像学原语层"把科学量（σ、clip 宽度、SE 半径、重标区间）的取值域与失败语义完全下沉到实现分支里，声明面既不给默认值也不给约束 ⇒ 调用点可以合法地构造出 CR-52-08（空结构元⇒全图判极大）、CR-52-09（dw=1⇒UB）、CR-52-11（退化值冒充测量）三种失败态而无一处编译期/运行期拦截。
- 后果：这些原语中确有 4 个在生产链上（σ=2.0 平滑＋中位/MAD）；契约缺失使得对这些原语的"确定性/可复现"主张只能靠读实现来核，任何再实现都可能取不同的退化行为（本文件内部已经先出现两例不一致）。
- 定级：S3（结构性/文档性，不直接产生错值）
- 怎么算修好：修 CR-52-03/08/09/11 时**同批**改本头文件——为每个原语加一行"取值域＋退化行为＋是否可失败"，并把失败态表达成返回码或显式不可用值（不是 0/1.0/1e30）；`#if 0` 对应的 :35-36 两行注释声明要么删要么登记封存理由（现在既非退役也非在役，属 §4 不可借用的"半登记"态）
- 置信：CONFIRMED

### CR-52-14 · `radius` 这一个整数在 `sdet_median_filter` 里切换了两套 NaN 语义不同的中值实现（声明面不可见）
- 轴：R＋C
- 位点：`sdet_image.h:11-13`（三个中值声明并列，无差异说明）；实现侧 `sdet_image.cpp:335-341`（`if (radius == 1) sdet_median_filter_3x3…; if (radius == 2) sdet_median_filter_5x5…`）
- 上位依据：条文面零提及（`git grep -n "median_filter\|中值滤波" -- <五份正本/README/module.yaml>` → 0 命中，实测）；另需注意 `docs/contracts/UNIFIED_OBJECTS.md:112` 与 `docs/plugins/algorithms_phase1/07_noise_snr.md:185/:189` 登记的是**噪声/稀疏重建侧**的 3×3 mesh 中值（边界 replicate、无条件替换），与本处热像素前置滤波不是同一条目 ⇒ 不能借作本函数锚
- 现状：radius=1 走 19 条比较-交换网络（`:302-310`，我穷举 9! 复算**无重复值反例**，含并列情形亦正确）；radius=2 与 radius≥3 走 `std::nth_element`（:328、:356）——后者遇 NaN 破坏严格弱序＝UB。同一个 `sdet_median_filter` 调用（`sdet_detector.cpp:23`，radius 来自 `P.hotPixelFilterRadius`）的**失败模式由配置整数值决定**。
- 差在哪：并列取值下两条实现的排序语义不等价（一条对任意输入有定义、一条对非有限输入是 UB），而声明与注释都不区分。
- 后果：同一 NaN 输入下 `hotPixelFilterRadius=1` 与 `=2` 给出"确定的吞 NaN 中位"与"未定义行为"两种结局 ⇒ 一个配置整数值切换失败模式；本条同时是 CR-52-04 位点 2 的分支细化
- 定级：S3（作为 CR-52-04 的取证组成部分，不重复计罪）
- 怎么算修好：统一为一个实现（网络版仅覆盖 n=9，通式版本先滤非有限再 nth_element）；在 :11-13 三行声明上写明"非有限输入的处置"；补一条 NaN 注入锁
- 置信：CONFIRMED（网络正确性＝穷举复算，脚本 `独立审计/复算件/CR-52/median9.py`；nth_element+NaN＝标准明文 UB，且本仓 wrapper 副本注释 star_detector.cpp:50-51 已作同一认定）

---

# 3 `lib/algorithms/star_detection/src/sdet_log.cpp`（89 行，全读）
# 4 `lib/algorithms/star_detection/src/sdet_log.h`（10 行，全读）

## 4.0 权威日志级别面（本批 ③ 的比对基准，全部实测行号）

| 面 | 位置 | 原文（短引） |
|---|---|---|
| 统一枚举（ABI） | `lib/include/astrocs/abi/status_codes.h:181-186` | `/* 日志 severity（ACS_LOG_*; 与 legacy 数值一致） */ enum { ACS_LOG_DEBUG = 0, ACS_LOG_INFO = 1, ACS_LOG_WARN = 2, ACS_LOG_ERROR = 3 };` |
| 统一枚举（公共 ABI v1） | `lib/include/astrocs/common_abi_v1.h:70` | `enum { ACS_LOG_DEBUG = 0, ACS_LOG_INFO = 1, ACS_LOG_WARN = 2, ACS_LOG_ERROR = 3 };` |
| 统一枚举（core，CORE-008） | `lib/include/astrocs/core/context.h:28` | `enum class LogLevel : uint8_t { DEBUG = 0, INFO = 1, WARN = 2, ERROR = 3 };` |
| 配置正本 | `docs/plugins/infrastructure/21_observability.md:34` | `log_level`｜默认 `info`｜`debug/info/warn/error` |
| 本模块 | `sdet_log.h:4-7` | `SDET_LOG_INFO = 0, SDET_LOG_DEBUG = 1, SDET_LOG_WARN = 2, SDET_LOG_ERROR = 3` |
| 同族副本 | `lib/algorithms/psf/src/dpsf_log.h:3-8` | 与 sdet 完全同型（INFO=0, DEBUG=1） |

⇒ 四面权威都是 DEBUG=0 < INFO=1，本模块与其副本是 **INFO=0 < DEBUG=1**，前两级编号互换。

## 4.1 §2.5 数值处置表（sdet_log.cpp ＋ sdet_log.h）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| sdet_log.h:4-7 | `SDET_LOG_INFO/DEBUG/WARN/ERROR` | 0/1/2/3 | 模块内部日志级别编号 | **公式导出＝不适用；属跨面一致性问题**→落 `待确认`（编号顺序与四面权威相反） | 见 §4.0 表：status_codes.h:181-186、common_abi_v1.h:70、context.h:28、21_observability.md:34 一律 DEBUG<INFO | 保守方向：本模块以 ACS_* 为准（DEBUG=0）⇒ 现默认档 0 的语义会从"全放行"变"只 DEBUG"，**改编号必须同批改 :22-:24 的默认与回退档**，否则会把现存的 `STAR_DETECTOR_LOG_LEVEL=3`（psf 测试 CMakeLists.txt:143-144）以外的所有档位解释翻转 | CONFIRMED |
| sdet_log.h:4-7 枚举基数 | 4 | 级别个数 | 结构性不适用（枚举基数，§4 明列不报） | 结构性不适用 | — | — | CONFIRMED |
| :22 | `v >= 0 && v <= 3` 的 `0`、`3` | 0、3 | 环境变量取值合法域 | 待确认 | 权威值域面是**字符串** `debug/info/warn/error`（21_observability.md:34），整数值域在条文面**零登记**（检索式 `git grep -n "LOG_LEVEL" -- lib docs eng` → 5 命中全为代码/CMake，docs 0 命中） | 保守方向：上界应写 `SDET_LOG_ERROR` 而非字面 3（否则将来加 FATAL=4 时 4 被当越界 ⇒ 静默落到最_verbose 档）。影响范围：本模块全部日志量 | CONFIRMED |
| :22 | 越界回退档 `SDET_LOG_INFO` | 0 | 非法值时的回退级别 | 待确认（且方向为**放大**日志量） | 条文面对"非法/越界"的处置：`21_observability.md:47`「静默回退到低优先输入/静默保持缺省值/静默跳过校验都按故障上行」⇒ 回退本身未被授权 | 保守方向：越界 ⇒ 取"最静默"档或直接判红（exit 2/ARGS 同 :46「目录解析失败 → exit 2」）⇒ 现方向相反。影响范围：运维把值写错时（`"debug"`、`"true"`、`"5"`）atoi 给 0 ⇒ 恰好落在合法域内 ⇒ **全量 DEBUG 打开并落盘**，无声无息 | CONFIRMED |
| :24 | 默认档 `SDET_LOG_INFO`（无环境变量时） | 0 | 默认级别 | **待确认→实为缺陷**：同族副本 `dpsf_log.cpp:23-25` 的默认档是 `LOG_WARN` 并附理由「默认只输出WARN+ERROR，避免DEBUG日志导致性能问题」；两份同型代码取两个不同默认档 | 见 §4.0 与 dpsf 副本行；条文面 `21_observability.md:34` 默认 `info`，但那里 info=1（会滤掉 debug），本文件 info=0（什么都不滤） | 保守方向：与副本对齐取 WARN，或按条文把 info 解释为"滤掉 DEBUG"（需先修编号）。影响范围：生产每次检测调用 `sdet_api.cpp:1747` 等 INFO/DEBUG 全量写 stderr＋文件 | CONFIRMED |
| :58 | `if (level < sdet_get_log_level()) return;` 的比较方向与 `<` | — | 级别过滤谓词 | 公式导出（谓词本身正确：level 数值越大越严重） | 与 :4-7 的编号耦合；编号错 ⇒ 语义翻转 | — | CONFIRMED |
| :15、:18 | 缓存哨兵 `-1` 与判据 `>= 0` | −1、0 | "尚未读环境变量"的哨兵 | 结构性不适用（哨兵取值不含科学语义；0 是合法级别故用 −1） | — | 但见 CR-52-18：该缓存的读写发生在锁外 | CONFIRMED |
| :33/:35/:40/:42 | 四个路径字面量（`lib\star_detector\logs\…` 与 `lib/algorithms/star_detection/logs/…`） | — | 日志落点 | 待确认→已由 CI 台账登记为 `known_gap`（LSL-003/LSL-004，line_hint 40/42），本批独立复核**属实**；两平台分支**指向不同路径**这一点台账未记 ⇒ CR-52-16/17 | `ASTROCS_DESIGN.md:529-530`「日志目录默认 `<output_dir>/logs`（日志落点的唯一来源 = 块级 output_dir）」、:696；`21_observability.md:35`「禁落 CWD/源码树/run/」；`eng/ci/ledgers/log_system_ledger.json` LSL-003/004 | 保守方向：唯一修复=落点由 output_dir 派生（台账 exit_condition 原文即此）。影响范围：Windows/Linux 双正式平台落点不同 | CONFIRMED |
| :31、:38、:86-87 | 三处失败处置（`(void)make_dirs`、`append_open(..., nullptr)`、`if (g_sdet_log_file)`） | — | 目录创建/文件打开/写入的失败通道 | 待确认→实为缺陷（CR-52-17） | `aio_atomic_file.h:208`「失败: append_open 返回 nullptr; append_write 返回 0 成功 / errno」；`21_observability.md:45-46`「日志/事件写入失败 → 记 stderr 脱敏摘要 + 本次运行以非 0 退出码结束（IO=7…）」「目录创建失败 → exit 7」、:47「静默…按故障上行」 | 保守方向：任一步失败 ⇒ 显式 stderr＋上行退出码。影响范围：只读安装目录、盘满（AGENTS §3 明确警告工作盘会满）、路径不存在 | CONFIRMED |
| :87（无 `append_close` 调用） | 句柄永不关闭／无 fsync | — | 收尾语义 | 待确认→实为缺陷（CR-52-17） | `aio_atomic_file.h:206-207`「多次 append_write → append_close (flush + close)」＝该边界的合同；`ASTROCS_DESIGN.md:533`「日志是运行产物：经 aio 唯一 I/O 边界写出，**收尾 fsync + 算哈希 + 原子发布**，并在 run manifest…」 | 保守方向：至少 atexit/析构里 append_close。影响范围：崩溃/断电时日志尾部丢失；日志文件永不进 manifest ⇒ 无哈希无发布语义 | CONFIRMED |
| :69 | `char time_str[32]` | 32 | `"%Y-%m-%d %H:%M:%S"`（19＋NUL）缓冲 | 结构性不适用 | 19+1 ≤ 32，含最长时区无关格式 | 非科学量 | CONFIRMED |
| :72 | `char msg[2048]` | 2048 | 用户格式化的落地缓冲 | 结构性不适用 | 缓冲区尺寸（§4 明列不报） | — | CONFIRMED |
| :78 | `char line[2304]` | 2304 | 行缓冲 = msg＋前缀 | 待确认（尺寸的算术余量） | 由 :79 的格式串可算：32(时间)＋7("UNKNOWN")＋4(三个 `]`/空格)＋2048(msg)＝2091 ⇒ 剩 **213 字节**给 `module`，而 `module` 是未限长的 `const char*`（:57） | 保守方向：尺寸应表达成 `sizeof(msg)+固定前缀` 或对 module 限长；现依赖"调用点都传短字面量"（本文件与 :660/:827/:949/:1028、sdet_detector.cpp:26/:49/:57/:70 实测均为 ≤10 字节字面量 ⇒ 现不触发） | CONFIRMED |
| :82 | `std::fprintf(stderr, …)` 无条件 | — | 每行同时写 stderr | 待确认 | `21_observability.md:26`「stdout 无日志污染（CLI 合同）：日志走文件/JSONL」——字面只禁 stdout，stderr 面 :45 只用于"失败脱敏摘要" ⇒ 全量日志走 stderr 无条文授权 | 保守方向：stderr 只留 ERROR/WARN。影响范围：生产 run 的 stderr 体积与噪声（默认档＝全量，见 :24 行） | CONFIRMED（引用为准，方向为解读，已如实标注） |
| :89/:80 之外的 `:60` 锁、`:63-68` 时区分支 | — | — | 并发与时区 | 结构性不适用 | — | 但见 CR-52-18（:58 在锁外） | CONFIRMED |
| :19 | `STAR_DETECTOR_LOG_LEVEL` | — | 环境变量名 | 待确认（登记面缺失） | 同族 `DYNAMIC_PSF_LOG_LEVEL`（dpsf_log.cpp:19）、`AIO_LOG_LEVEL`（aio_log.cpp:20）；三者在 `eng/packaging/config/config_registry.json` 与 `docs` 面均**无登记条目**（该 registry 只有字符串型 `log_level` 字段，:34 权威），检索式 `git grep -n "LOG_LEVEL" -- lib docs eng` → 0 docs 命中 | 保守方向：要么进 config_registry 登记，要么删除 env 通道改用统一日志 API。影响范围：运维旋钮不可发现、不可校验 | CONFIRMED |

**合计 16 行处置**（sdet_log.cpp 14 ＋ sdet_log.h 2）。sdet_log.h 内除枚举值 0/1/2/3 外无其它数（逐行读过）。

## 4.2 findings（sdet_log.h ＋ sdet_log.cpp）

### CR-52-15 · 日志级别枚举把 INFO 与 DEBUG 的编号与四面权威定义互换，导致"默认档"实为最verbose档
- 轴：C（主）＋R
- 位点：`sdet_log.h:4-5`（`SDET_LOG_INFO  = 0,` / `SDET_LOG_DEBUG = 1,`）；后果落点 `sdet_log.cpp:58`（`if (level < sdet_get_log_level()) return;`）与 :24
- 上位依据：`lib/include/astrocs/abi/status_codes.h:180-186`（`/* 日志 severity（ACS_LOG_*; 与 legacy 数值一致） */` ＋ `ACS_LOG_DEBUG = 0, ACS_LOG_INFO = 1, …`）、`common_abi_v1.h:70`、`core/context.h:28`、`docs/plugins/infrastructure/21_observability.md:34`（值域顺序 `debug/info/warn/error`）
- 现状：`sdet_get_log_level()` 默认返回 `SDET_LOG_INFO`=0 ⇒ 谓词 `level < 0` 对任何级别为假 ⇒ **零过滤**，所有 `SDET_LOG_DEBUG` 行（本文件内如 sdet_image.cpp:660 每轮裁剪一行、sdet_detector.cpp:26/:49 每阶段一行）在生产默认档下全部写 stderr＋落盘。
- 差在哪：状态码注释自称"与 legacy 数值一致"，而本模块这份 legacy 恰好把最低两级调了个个儿；按权威编号设定 `info` 会得到"只剩 DEBUG"的反向结果（`level<1` 滤掉 0=INFO、放行 1..3），按本模块编号设定又与运维文档/其它模块不通。
- 后果：三处：① 运维按权威编号调档得到相反verbose度；② `sdet_log(level,…)` 无法安全接收 `ACS_LOG_*` 值（0↔1 错绑）；③ 默认全量 DEBUG 落盘与写 stderr，与同族副本 `dpsf_log.cpp:23-25` 明示的性能理由（"避免DEBUG日志导致性能问题"）自相矛盾——两副本同一决策只落了一份（本批 ② 的形态在日志侧的第二例）。
- 定级：S2（不改产物字节；改的是观测面与性能，且"默认档语义"这一对外主张不成立）
- 怎么算修好：枚举改为 DEBUG=0/INFO=1/WARN=2/ERROR=3 与 ACS_* 对齐 ⇒ **同批必须**改 :22 的默认与回退档、:24 的默认档（建议对齐 dpsf 副本取 WARN 或按条文取 INFO=1）、`eng/ci` 里已设 `STAR_DETECTOR_LOG_LEVEL=3` 的 `lib/algorithms/psf/tests/p1psf/CMakeLists.txt:143-144`（该处在新编号下含义不变，但要复核），并同步 `dpsf_log.h:3-8` 副本；改完给一条"设 info 不得出现 DEBUG 行"的锁
- 置信：CONFIRMED

### CR-52-16 · 同一模块的日志落点在 Windows 与 Linux 两支指向**不同目录**，Windows 分支的名字在跟踪面根本不存在
- 轴：C
- 位点：`sdet_log.cpp:33`（`"lib\\star_detector\\logs"`）与 :40（`"lib\\star_detector\\logs\\star_detector.log"`）对照 :35/:42（`"lib/algorithms/star_detection/logs…"`）
- 上位依据：`AGENTS.md §7` 目录落位（模块正体为 `lib/algorithms/star_detection/`）；`ASTROCS_DESIGN.md:696`「产品与**运行日志**只落块级 output_dir」；`docs/plugins/infrastructure/21_observability.md:35`「禁落 CWD/源码树/run/」
- 现状：Windows 写 `<CWD>/lib/star_detector/logs/star_detector.log`，POSIX 写 `<CWD>/lib/algorithms/star_detection/logs/star_detector.log`。`git ls-files lib/star_detector | wc -l` = **0** ⇒ 前者的父目录在跟踪面不存在（按前言 §1 的口径，以跟踪集判"不存在"）；`make_dirs` 会在运行时把它建出来。
- 差在哪：同一次运行在两个正式平台（AGENTS §2「正式平台 Windows x64 与 Linux amd64」）落到的日志文件路径不是同一个名字族，且 Windows 侧用的是改名前的模块旧名——与 `docs/contracts/API_CONTRACTS.csv` 里仍写作 `lib/star_detector/include/star_detector.h` 而 `docs/architecture/api_inventory.csv:421` 写作 `lib/algorithms/star_detection/include/star_detector.h` 的同一种半贯穿形态。
- 后果：跨平台证据比对时"日志在哪"两说；按 POSIX 路径写的采集/校验脚本在 Windows 上找不到文件；源码树里多出一个 legacy 目录（`.gitignore:16` 的 `logs/` 只兜住目录名兜不住语义）。落点违反禁令本身已由 `eng/ci/ledgers/log_system_ledger.json` LSL-003/004 登记为 `known_gap`（本批独立复核属实，**不另计一条新罪**），本条只报台账未覆盖的"两平台路径互不相同＋Windows 支名字不存在"。
- 定级：S3
- 怎么算修好：随 LSL-003/004 的 exit_condition 一并解决（落点由 output_dir 派生后删条目）；在过渡期至少把两支 `#ifdef` 合并成同一个相对路径常量，别让分平台分叉继续扩大
- 置信：CONFIRMED

### CR-52-17 · 日志通道的三处失败与"永不收尾"全部静默，直接顶撞"日志写入失败必须上行退出码／降级必须显式"的合同条款
- 轴：D（主）＋R
- 位点：`sdet_log.cpp:31`（`(void)aio_atomic::make_dirs(`）、:38-44（`g_sdet_log_file = aio_atomic::append_open("…", nullptr);` —— 错误出参显式传 `nullptr`）、:86（`if (g_sdet_log_file) {` ⇒ 打开失败就**只写 stderr、不写文件、不报错**）；全文无 `append_close`
- 上位依据：`lib/infrastructure/aio/src/aio_atomic_file.h:206-209`「多次 append_write → **append_close (flush + close)**」「失败: append_open 返回 nullptr; append_write 返回 0 成功 / errno」；`docs/plugins/infrastructure/21_observability.md:45`「日志/事件写入失败 → 记 stderr 脱敏摘要 ＋ **本次运行以非 0 退出码结束（IO=7；磁盘满=10）**」、:46「目录创建失败 → exit 7（IO）」、:47「**降级必须显式**…静默回退…都按故障上行」；`ASTROCS_DESIGN.md:533`「日志…收尾 fsync + 算哈希 + 原子发布，并在 run manifest…」
- 现状：`make_dirs` 返回值被 `(void)` 丢弃；`append_open` 的 `err` 出参被显式丢弃；写返回值（errno）被 `(void)` 丢弃；句柄从不 `append_close`（进程存活期内不 flush 由 stderr 兜住，但文件尾部的落盘时机完全交给 OS）；`sdet_log` 无返回码，调用点（sdet_api.cpp、sdet_detector.cpp、sdet_image.cpp 全文）一律不看结果。
- 差在哪：把合同明列的三类故障（目录创建失败、打开失败、写失败）实现成"继续跑"，且不是显式降级（无 `degraded_reason`、无 manifest 条目）。
- 后果：AGENTS §3 与 config 面都假定盘会满／目录会失败；在这些情形下运行**照常以 0 退出**，而 `run manifest` 的 `log_artifacts[]` 里没有该模块日志（本模块日志根本不在 `<output_dir>/logs/run_<id>.{log,jsonl}` 体系内，见 :20），事后既无退出码信号也无哈希 ⇒ "每次运行（含失败与取消）产出完整运行日志"这一对外主张在该模块不成立。
- 定级：S1（对外主张不成立；触发路径逐跳：`ipv_select.cpp:1972`→`sdet_api.cpp:1747`（`sdet_log(SDET_LOG_INFO,…)`）→`sdet_log.cpp:57`→:85→:29-45→:86 静默跳过→`ipv_select.cpp:1978` 谓词不看日志状态→run 以 0 结束）
- 怎么算修好：`sdet_ensure_log_file()` 返回状态并在失败时 `fprintf(stderr, 脱敏摘要)`＋向调用层传可上行的失败（模块无 exit 通道 ⇒ 与 observability 侧统一走 `astrocs::core` 日志 API，即 :41「公共日志/事件 API（C ABI 版本化），所有模块调用」）；补 `append_close`（`atexit` 或显式 finalize）；把落点改为 output_dir 派生后，同步删 `log_system_ledger.json` LSL-003/004（台账 exit_condition 要求"删条目即恢复判红"）
- 置信：CONFIRMED

### CR-52-18 · 级别缓存 `g_sdet_log_level` 在互斥锁外读写，与本模块被并行调用的事实构成数据竞争
- 轴：R
- 位点：`sdet_log.cpp:58`（`if (level < sdet_get_log_level()) return;`）在 :60 的 `std::lock_guard<std::mutex> lock(g_sdet_log_mutex);` **之前**；:18/:22/:24 对 `static int g_sdet_log_level` 的读与写都在锁外
- 上位依据：`AGENTS.md §3/§6`（并行由 benchmark profile 决定线程数）、本文件所有被 sdet_image.cpp:660/:827/:949/:1028 这类 OpenMP 区内/区外混合调用；条文面对"日志级别初始化"零登记
- 现状：首次并发调用可能多线程同时 `getenv`+`atoi`+写同一 static；后续读取无 happens-before。
- 差在哪：按 C++ 内存模型是 data race（UB）：:18/:22/:24 对同一 static 的读写发生在 :60 的锁之外。
- 后果：同一次运行内不同线程可能先后看到不同档位（一线程用默认 0、另一线程用 getenv 解析值）——两值皆幂等赋值且同源 ⇒ **不产生产品数值错误**（如实降级）；真实后果是（a）UB 使该文件在 TSan/OMPT 类机器门下必红，（b）后续把它当"有锁保护"扩展（如加 set 接口）时踩真坑，（c）首帧多线程并发时 getenv 被重复调用（无写坏环境的后果）。
- 定级：S3
- 怎么算修好：改成 `static std::once_flag` + `std::call_once`，或把 :58 挪进锁内（代价：每行一次加锁）；推荐 `std::atomic<int>` 一次性初始化
- 置信：CONFIRMED（读码即可判；未跑）

---

# 5 `lib/algorithms/star_detection/test/sdet_fp64_test.cpp`（102 行，全读）

## 5.1 §2.5 数值处置表（sdet_fp64_test.cpp）

判据类数值（:91/:92/:94）与配置类数值（:52-:60）逐列；夹具内容类数值按 §4「测试期望值与夹具内容差」
不报为缺陷，但**仍逐个给档**（前言 §2.5 的硬验收要求）。

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :91,:92 | 坐标一致性容差 | `1.0` px | 「FP32 与 FP64 两入口坐标差」的判红线 | **待确认**（并实为缺陷 CR-52-20） | 注册面：`docs/algorithms/GATES_AND_TOLERANCES.md:53` G-P1-CENTROID-U16 门 = **0.5 px**，且同行标定「u16 量化贡献 median 0.0018 / p95 0.0036 / **max 0.0056 px**」；:55 G-P1-PSF-ORACLE-F64 的 FP64 通道门 = **0.05 px**。本行取 1.0 px，比两条注册门分别宽 **2×／20×**，比它自称要锁的差异（u16 量化 max 0.0056 px）宽 **≈180×** | 保守方向：收到 0.05 px（与 FP64 门同级）或至少 0.5 px（与 U16 门同级）。影响范围：本文件 :92 一条判据；1 px 内的质心约定错（半像元偏置、`+0.5` 漏项、行列表转置）全部不可见 | CONFIRMED |
| :94 | 通量一致性容差 | `1000.0` ADU | 两入口 flux 差的判红线（绝对量，非相对） | **待确认**（并实为缺陷 CR-52-20） | 注册面查无此量（`git grep -n "1000" docs/algorithms/GATES_AND_TOLERANCES.md` 无通量一致性条目；:55 的 FP64 振幅门是**相对** 1e-3）；本测试注入的振幅 3000–9000 ⇒ 1000 ADU = 最暗星的 **33%** | 保守方向：改相对容差并按 :55 的 1e-3 量级；影响范围：一整个数量级以内的通量系统偏差（孔径/窗差、半像元偏置）不可见 | CONFIRMED |
| :52 | `params.structureLayers` | 5 | 结构图层数 | 结构性不适用（本测试所调入口不消费）→ 但登记为 CR-52-22 | 与 `sdet_api.cpp:994` 的 `defaults.structureLayers = 5` 同值；消费面登记 `STAR_DETECTION_ALGORITHMS.md:253-255`「structureLayers/hotPixelFilterRadius/iterativeClipSigma/iterativeMaxRounds/medianFilterDetail **仅旧 sdet_get_structure_map 路径消费**…生产 impl 不调用结构图」；本测试调 `sdet_detect_ex/_f64`（:72/:75）⇒ 不消费 | 保守方向：无（值与默认同）；影响＝读者误判覆盖 | CONFIRMED |
| :53 | `hotPixelFilterRadius` | 1 | 热像素中值半径 | 同上（与 sdet_api.cpp:995 同值；本入口不读，实测 `git grep -n hotPixelFilterRadius -- sdet_api.cpp` 只命中 :995 默认赋值） | 同上 | 影响：`sdet_median_filter*` 在本测试里**未被执行** | CONFIRMED |
| :54 | `iterativeClipSigma` | 9.0 | σ 截尾宽度 | 同上（sdet_api.cpp:996 同值 9.0f） | 同上 | 影响：`sdet_iterative_sigma_clip`／`sdet_sigma_clip_med_of_rounds` 未被执行 | CONFIRMED |
| :55 | `iterativeMaxRounds` | 5 | 截尾轮数 | 同上（sdet_api.cpp:997 同值） | 同上 | 同上 | CONFIRMED |
| :56 | `medianFilterDetail` | 1 | 细节图中值开关 | 同上（sdet_api.cpp:998 同值） | 同上 | 同上 | CONFIRMED |
| :57 | `maxStars` | 100 | 输出星数上限 | 结构性不适用（测试自订参数，§4 明列不报）；但与权威默认 2000（sdet_api.cpp:999）**不同值**，且 20 星夹具远未触顶 ⇒ 不触发 | sdet_api.cpp:999；`21_observability` 无关 | 影响：本测试运行在一个生产不用的取值上（无实害，因未触顶） | CONFIRMED |
| :58 | `fitRadius` | 0 | 拟合半径＝自动模式 | 结构性不适用；语义锚在 `sdet_api.cpp:1117`「fitRadius=0表示自动模式」，权威默认是 6（:1000） | 同上＋`STAR_DETECTION_ALGORITHMS.md:251-252`（fitRadius 仅驱动 auto 半径日志推导） | 影响：与生产默认不同的分支被走到，但无断言覆盖该分支差异 | CONFIRMED |
| :59 | `fwhmClipSigma` | 3.0 | FWHM 裁剪 σ | 结构性不适用（本入口不消费：:252-253「fwhmClipSigma **仅 debug 入口消费**」） | 同上 | 计入 CR-52-22 | CONFIRMED |
| :60 | `maxAxisRatio` | 2.0 | 长轴比门 | 结构性不适用（与 sdet_api.cpp:1002 默认同值，且本入口确实消费 :2143-2145） | 同上 | — | CONFIRMED |
| :80 | `r32 == 0 && r64 == 0` 的 0 | 0 | 返回码成功 | 结构性不适用（返回码定义） | `STAR_DETECTION_ALGORITHMS.md:259`「入口级: 0=成功（**含 0 星**）」⇒ 正是本条断言可被"零检出"满足的法律依据（CR-52-19） | — | CONFIRMED |
| :82 | `n32 == n64` | — | 星数相等（不含"等于注入数"） | 结构性不适用（比较式）；缺陷计入 CR-52-19/21 | 夹具 `NSTAR = 20`（:24）从未被当期望值使用 | 保守方向：加 `n32 == NSTAR`；影响：两路同漏亦绿 | CONFIRMED |
| :84 | `if (n32 > 0 && n32 == n64)` | 0 | 两条数值断言的执行前提 | 结构性不适用（控制流）；fail-open 计入 CR-52-19 | — | 方向：条件不满足时 CHECK 数从 4 降到 2，`g_fail` 仍 0 ⇒ 退出 0 | CONFIRMED |
| :85,:89 | `max_dx/max_dy/max_df` 初值 | 0 | 最大值归约初值 | 结构性不适用 | — | — | CONFIRMED |
| :23 | `W`, `H` | 512 | 夹具画幅 | 结构性不适用（夹具内容，§4） | — | — | CONFIRMED |
| :24 | `NSTAR` | 20 | 注入星数 | 结构性不适用（夹具内容）；被 :82 忽略一事记 CR-52-21 | — | — | CONFIRMED |
| :26,:27 | 背景 | 800.0f / 800.0 | 夹具天光 | 结构性不适用（夹具）；**无噪声**这一点记 CR-52-21 的退化控制缺失 | — | — | CONFIRMED |
| :29,:30 | 星位栅格 | 50.0、`%5`/`/5`、100.0、0.37、0.61 | 5×4 栅格与半像元偏移 | 结构性不适用（夹具）；`0.37/0.61` 是为考察亚像元约定而设，但**没有任何断言把它们与输出坐标比较** ⇒ CR-52-21 | — | — | CONFIRMED |
| :31 | `A = 3000.0 + k*300.0` | 3000、300 | 峰值振幅 | 结构性不适用（夹具） | — | 峰值 3800–9800 ⇒ u16 不截顶（:45 注释自述）✓ 一致 | CONFIRMED |
| :32-:33 | 注入窗 | ±8 | 高斯支撑（≈2.7σ_psf） | 结构性不适用（夹具窗宽，实现选择） | — | 影响：17×17 截断使最外圈缺失，两路输入同一处理 ⇒ 不构成 FP32/FP64 差异源 | CONFIRMED |
| :36-:37 | 采样与高斯 | `0.5`（像元中心）、`2.0`、`3.0`（σ_psf²） | 像元中心定义＋exp(−r²/2σ²) | 结构性不适用（单位/定义常数，§4 明列排除） | — | — | CONFIRMED |
| :47 | u16 换算与钳位 | `0`、`65535` | 满量程定义 | 结构性不适用（定义常数） | — | 但 `v<0 / v>65535` 对 NaN 恒假的性质在 :47 同样成立（本夹具无 NaN ⇒ 不触发）；生产同型代码的问题已记 CR-52-04 | CONFIRMED |
| :78 | `char name[160]` | 160 | 证据行缓冲 | 结构性不适用（缓冲尺寸） | — | `snprintf` 限长，不溢出 | CONFIRMED |
| :100,:101 | 计数与退出码 | 0 | PASS/FAIL 汇总 | 结构性不适用 | — | 汇总口径"只看 g_fail"＝ CR-52-19 的机制 | CONFIRMED |

**合计 25 行处置。**

## 5.2 findings（sdet_fp64_test.cpp）

### CR-52-19 · FP64 验证件的两条数值断言被 `n32>0` 门住：两路同时零检出 ⇒ 该件全绿
- 轴：R
- 位点：`lib/algorithms/star_detection/test/sdet_fp64_test.cpp:84`（`if (n32 > 0 && n32 == n64) {`）与 :92（`CHECK(max_dx < 1.0 && max_dy < 1.0, name);`）、:94（`CHECK(max_df < 1000.0, name);`）、:101（`return g_fail == 0 ? 0 : 1;`）
- 上位依据：入口返回码语义 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md:259`「入口级: **0=成功（含 0 星）**」；退化控制要求 `AGENTS.md §5`「判据必须非退化：真值无效应时度量归零或判红」、§9「空断言、SKIP 充数都算未完成」
- 现状：无条件断言只有两条——:80 `r32 == 0 && r64 == 0`（合同上"0 星也是 0"）与 :82 `n32 == n64`（0==0 成立）；三条比较坐标/通量的断言整体挂在 `n32 > 0` 上，且 CHECK 宏只累加 `g_fail`，不检查"应有的判据是否跑过"。
- 差在哪：**判据方向**——检出集合变空只会让"少执行两条 CHECK"，永远不会让 `g_fail` 变正 ⇒ 该件在"检测功能完全失效"的世界里仍然绿；这就是 §3 要求先钉死方向的"误绿"，方向已钉死为**偏绿**（丢项 ⇒ 断言数变少 ⇒ 不可能判红）。
- 后果：本文件被 `docs/modules/star_detector.md:85-86` 登记为「`sdet_fp64_test.cpp` 为 NON_PRODUCTION_TOOL_ONLY 手工验证程序」⇒ 它不是 CI 门（这点已登记，我不据此报缺陷）；但它同时是 `lib/algorithms/star_detection/README.md:187` 与文件头 :4-7 声称的 FP64 证据，而该证据**能容纳"零检出"**，任何引用它来支撑"FP64 通道已验证"的说法都不成立。
- 定级：S2（不改产物字节；使"FP64 已验证"这一登记面主张失去证据资格）
- 怎么算修好：把 :82 改成 `CHECK(n32 == NSTAR && n64 == NSTAR, …)`（真值 20）＋ :84 条件不满足时 `++g_fail`（缺判据即判红，而不是跳过）；再加一条负例：把注入星数改成 0 时**必须**判红（证明门有牙）。同批不要顺改引擎数值（本条只动测试）
- 置信：CONFIRMED（读码即判；未跑，构建/ctest 禁用）

### CR-52-20 · 该件自称锁"FP32 输入舍入范围"，实际差异源是 uint16 截断，且容差比注册门宽 2–180 倍
- 轴：S（主，归因错）＋C（与注册门不一致）＋R
- 位点：`sdet_fp64_test.cpp:6`（`// 检测星数一致, 坐标/通量差异在 FP32 输入舍入范围内`）、:45（`// 同一物理值: uint16 直接存 fimg (峰值 ~9800 < 65535), double 图存 dimg``）、:47（`uimg[i] = (uint16_t)(v < 0 ? 0 : (v > 65535 ? 65535 : v));`）、:92、:94
- 上位依据：`GATES_AND_TOLERANCES.md:53`（G-P1-CENTROID-U16＝0.5 px，同行标定"u16 量化贡献 median 0.0018 / p95 0.0036 / max 0.0056 px ⇒ 余量 ~90×"）、:55（G-P1-PSF-ORACLE-F64＝0.05 px / 1e-3）；`DISP-STAR-001`（同文 :272-274）明写 FP32 通道经 uint16 量化、FP64 通道不降级
- 现状：FP32 路的输入是**截断取整**后的整数图（:47 无舍入，`(uint16_t)` 向零截断），FP64 路的输入是带小数的原值 ⇒ 两路输入的每像元差 = 0～1 ADU（均值 0.5 ADU，**单向偏暗**），而不是"FP32 舍入"（float32 在 1e4 量级的 1 ulp ≈ 5.96e-7×9800 ≈ **0.006 ADU**）。
- 差在哪：注释把差异归到一个比实际小约 **2 个数量级**的机制上，并据此为 **1.0 px／1000 ADU** 的容差辩护；而它比仓内自己注册的两条门（0.5 px、0.05 px）还宽 2×／20×，比它自称要锁的量化贡献（0.0056 px）宽 **≈180×**。数量级是我按 §4 排除之外的两条注册值直接算的，未预设。
- 后果：① 该件对"FP64 通道是否真的不降级"没有判据力（1 px 容差能吃掉整个半像元约定错误、u16 截断引起的全部影响、以及亚像元定位错一位）；② 1000 ADU 绝对容差对最暗星（A=3000）＝33%，通量方向的可比性主张（README:187、文件头 :5-7）不成立；③ 与注册门不一致 ⇒ 同一件事两处口径（本件 1.0 px vs 门 0.5/0.05 px）在整改时会互相打架（改紧本件到门级会暴露现网差异，属"改对反而判红"，必须同批预备）。
- 定级：S2（缺生产触发路径：它不改产物，只使"FP32/FP64 一致"的对外主张失去支撑）
- 怎么算修好：① 订正 :5-7/:45 的归因（差异源＝u16 截断，量级 0.5 ADU/像元）；② 若要锁"FP64 不降级"，正确构造是让两路输入**同一物理值**（例如 FP32 路也喂含小数的 float32 图，或把 u16 量化项从比较中扣除），再把容差收到注册门同值（0.05 px 级、相对通量 1e-3 级）；③ 同批在 `GATES_AND_TOLERANCES.md` 给"FP32↔FP64 跨通道一致性"单列一行（现面无此条目，检索式 `git grep -n "1000\|跨通道" docs/algorithms/GATES_AND_TOLERANCES.md` → 无通量一致性条目），否则收紧判据会与该文件的 NON_PRODUCTION 定性发生口径冲突
- 置信：CONFIRMED（注册值逐项引用＋我自己算 ulp/截断量级；未跑，构建禁用）

### CR-52-21 · 文件头列的"验证项 2（输出布局一致）"零断言，mag/sat/extras 收而不用，真值星位从不参与比较，且夹具无噪声（退化控制缺失）
- 轴：D（主）＋R
- 位点：`sdet_fp64_test.cpp:7`（`// 2. 两入口输出布局一致 (坐标 double, flux/mag float)`）；对照 :68-69/:73/:76（`mag32/mag64`、`sat32/sat64`、`has32/has64` 声明并传给入口，全文再无一处读取）、:74/:77（`nullptr, 0, nullptr` ⇒ extras 路不测）、:28-42（注入的 cx/cy 从不与输出比较）
- 上位依据：`AGENTS.md §5`「实验单元自包含…判据必须非退化」、`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:238`（`sdet_sort_stars | mag 升序 NaN 末尾`）与 :292-294 DISP-STAR-004（mag/has_saturated 列语义本就是已登记缺陷面）——即"mag 与饱和列"正是需要锁的地方，而本件把它们当摆设
- 现状：4 条 CHECK 全在 rc/星数/坐标/通量上；文件头自称的第二项验证（布局与 mag）没有任何对应断言；夹具是**纯无噪声**合成场（背景恒定 800，无泊松/读噪）⇒ 任何"两路统计量在噪声下会分叉"的情形本件一律测不到。
- 差在哪：声明的验证范围与执行的断言集合不重叠的部分正是 mag/饱和/extras/真值位置。
- 后果：`sdet_fp64_test.cpp` 作为 README:187 与模块正本登记的"FP64 验证"证据，覆盖度比其自述小一半；"两入口输出布局一致"这一主张在本批证据面里无人验证（其它模块是否验证：`GATES:55` 的 p1star_oracle 走独立 oracle，不比较两入口输出）
- 定级：S3（覆盖面缺口，不产生错值；且该件登记为手工程序）
- 怎么算修好：把 :28-31 的 cx/cy 存成表并断言"每个真值位置在 0.05 px 内有且仅有一条输出"（同时修 CR-52-19/20）；补 mag/sat/has 的跨入口比较与 extras 列（`extra_names/extra_count`）一条；再加一档**带噪声**夹具（固定 seed）以证明无退化
- 置信：CONFIRMED

## 5.3 不报的核过项（本文件）
- `:52-:56` 五参数与 `sdet_api.cpp:994-998` 权威默认逐值相同 ⇒ 不报"测试自订参数"（§4 排除），只在 §5.1 记为"入口不消费"（CR-52-22 的组成部分）。
- `:36` 的 `px + 0.5` 半像元约定与 `:37` 的 `2.0*3.0*3.0` ⇒ 定义常数，按 §4 不报。
- `:78` 的 `name[160]` 配 `snprintf` ⇒ 无溢出风险，不报。
- 本件未进 ctest：`docs/modules/star_detector.md:85-86` 已显式登记为 NON_PRODUCTION_TOOL_ONLY 手工程序 ⇒ 按 §4「已由合同/登记面显式声明」不报为缺陷（仅用于定级）。

### CR-52-22 · 本件设置的 7 个参数在其所调入口上不生效，因而本批"图像原语"在两个测试件里的实际覆盖为：只覆盖高斯与中位/MAD，其余 15 个原语零覆盖
- 轴：D
- 位点：`sdet_fp64_test.cpp:72`/`:75`（`sdet_detect_ex` / `sdet_detect_ex_f64`）＋ :52-:56/:59（参数块）；`sdet_saturation_cursor_test.cpp:131`/`:150`（同一入口）＋ :112-:116
- 上位依据：`STAR_DETECTION_ALGORITHMS.md:250-255`（逐字段消费面）＋ 我的独立复核：`git grep -n "hotPixelFilterRadius\|iterativeClipSigma\|iterativeMaxRounds\|medianFilterDetail\|structureLayers\|fwhmClipSigma" -- lib/algorithms/star_detection/src/sdet_api.cpp` ⇒ 在 sdet_api.cpp 中**只命中 :994-:998 的默认赋值与 :1010-:1011 的日志**，无任何消费点；消费点在 `sdet_detector.cpp:22-23`（属 `sdet_get_structure_map` 链）
- 现状：两个测试件都按"生产参数块"的样子设了 9 个字段，其中 7 个（structureLayers/hotPixelFilterRadius/iterativeClipSigma/iterativeMaxRounds/medianFilterDetail/fwhmClipSigma ＋ fitRadius 的 auto 分支）在 `sdet_detect_ex[_f64]` 上不起作用；而 `sdet_median_filter`、`sdet_dynamic_regional_background`、`sdet_truncate_and_rescale`、`sdet_local_maxima_map`、4 个形态学、4 个 à trous/重采样、`sdet_iterative_sigma_clip` 只能由 `sdet_detect`/`sdet_detect_debug` 触发（全跟踪面零调用者，见 §1.0）。
- 差在哪：注释与参数块共同暗示"这些原语被验证过"，实际路径从未被走到。
- 后果：`sdet_image.cpp` 的 11 个原语（§1.0 表列 0 引用者除外）**在本仓内没有任何可执行证据**；配合 CR-52-03（FP64 小画幅不滤波）与 CR-52-05（两副本口径分叉），这些原语的回归风险由零证据承担。
- 定级：S2
- 怎么算修好：要么给 legacy 入口补一个共址件（调 `sdet_detect_debug`，断言 smap/detail/binary 三张导出图的形状/取值域/非退化），要么在 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md` 的测试设计段（:218「由 P1-STAR-TEST 落地」）显式登记"这些原语无测试覆盖"，与 DISP-STAR-005 的去留裁决合并处理；两个测试件的参数块应删掉不消费字段（留着是误导源）
- 置信：CONFIRMED

---

# 6 `lib/algorithms/star_detection/test/sdet_saturation_cursor_test.cpp`（183 行，全读）

## 6.1 §2.5 数值处置表（sdet_saturation_cursor_test.cpp）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :137,:159,:165 | `has_star_near(..., tol, ...)` | `2.5` px（欧氏距离） | "某颗星被检出"的位置容差 | **待确认**（并记 CR-52-24） | 注册面：`GATES_AND_TOLERANCES.md:53` G-P1-CENTROID-U16 = **0.5 px**、:55 G-P1-PSF-ORACLE-F64 = **0.05 px**；本件取 2.5 px ⇒ 比两条门宽 **5×／50×**；条文面对"回归锁容差"无独立条目 | 保守方向：收到 0.05–0.5 px 级。影响：2.5 px 内任何错误质心（半像元约定、邻近假峰、平台边缘峰）都算"检出" | CONFIRMED |
| :171,:172 | 计数字段的 box 判据 | `135.5/101.5`、`60.5/200.5`、`±2.5` | "最终星表含 B/C 各 1 条"的 5×5 像素框 | 同上（同一族 2.5，合并登记） | 同上 | 保守方向同上；另：框选两条轴各自 ≤2.5 ⇒ 比 :76 的欧氏 2.5 更宽（角点 3.54 px）——同一文件内两种"邻近"度量并存 | CONFIRMED |
| :175 | `nb == 1 && nc == 1` | 1、1 | 唯一性断言（该件最强的一条） | 结构性不适用（比较式） | 它只约束 B/C 两条，**不约束总数 n**（无 `n == 2`）⇒ 记 CR-52-24 的方向结论 | 方向：多出不相关的假源不可能使本条变红 | CONFIRMED |
| :70 | `double best = 1e30;` | 1e30 | 最近距离归约初值 | 结构性不适用（哨兵；`n==0` 时返回 false，:76 判 `1e30 <= tol` 假 ⇒ 正确判红） | — | 已核：不构成缺陷（§6.3） | CONFIRMED |
| :135,:153 | `CHECK(rc == 0, name)` | 0 | 入口返回码 | 结构性不适用（返回码定义） | `STAR_DETECTION_ALGORITHMS.md:259`「0=成功（含 0 星）」 | 场景1 的实质断言只剩这条＋一条对照星 ⇒ 场景1 接近"不崩"门 | CONFIRMED |
| :80 | `W`,`H` | 256 | 夹具画幅 | 结构性不适用（夹具内容，§4） | — | 平台右缘 160＋B 的位置 135 ⇒ 画幅够宽，未触边 | CONFIRMED |
| :81 | `BG` | 800.0 | 夹具天光 | 结构性不适用（夹具） | — | 注释 :17 的 `meanhigh−bg = 46200` 与之自洽（47000−800） | CONFIRMED |
| :85-:87 | 场景1 平台 | x,y ∈ [80,140]（61×61）、`65535.0f` | 全饱和平台 | 结构性不适用（夹具内容） | 与注释 :31-33 自洽 | — | CONFIRMED |
| :88 | 对照星 C′ | (200.5, 60.5)、A=3000、σ=3.0 | 远离平台的回归星 | 结构性不适用（夹具） | — | — | CONFIRMED |
| :99-:101 | 场景2 平台 | x,y ∈ [80,160]（81×81）、`47000.0f` | "仍走饱和分支"的平台（未截顶） | 结构性不适用（夹具内容）；**但其成立条件（走饱和分支）无断言** ⇒ CR-52-23 | 注释 :16-25 给推导 | 若饱和判据变化，本夹具可能不再进饱和分支 ⇒ 锁失效（偏绿） | CONFIRMED |
| :102 | 星 B | (135.5,101.5)、A=8000、σ=2.0 | 落在"修复前被跳过区间 [101,158]"内的探针星 | 结构性不适用（夹具内容） | 注释 :21-27；算式自洽：100＋xr=58 ⇒ 158 ✓；135 ∈ [101,158] ✓ | 敏感性仅限**锚点同一行 y=101 的平台右向**（其它行的游标污染、左向污染不会被本件捕获）⇒ 记 CR-52-24 的范围限制 | CONFIRMED |
| :103 | 对照星 C | (60.5,200.5)、A=3000、σ=3.0 | 平台外回归星 | 结构性不适用（夹具） | — | — | CONFIRMED |
| :92,:107 | u16 换算钳位 | 0、65535 | 满量程定义 | 结构性不适用（定义常数，§4） | — | 与 fp64 件同型（NaN 直通，本夹具无 NaN） | CONFIRMED |
| :56-:57 | `add_gaussian_star` 窗 | ±12 | 注入支撑（B 的 6σ、C 的 4σ） | 结构性不适用（夹具窗宽） | — | — | CONFIRMED |
| :58 | `px + 0.5 - cx` | 0.5 | 像元中心约定 | 结构性不适用（定义常数） | — | — | CONFIRMED |
| :112-:120 | 参数块（5/1/9.0/5/1/100/0/3.0/2.0） | 同 §5.1 各行 | 与 fp64 件逐字相同的 SDetParams 块 | 结构性不适用；其中 7 个字段在本入口不消费 ⇒ 全部计入 **CR-52-22**（不重复计数） | `STAR_DETECTION_ALGORITHMS.md:250-255` ＋ §5.2 CR-52-22 的独立复核 | — | CONFIRMED |
| :122 | `char name[200]` | 200 | 证据行缓冲 | 结构性不适用 | — | `snprintf` 限长 | CONFIRMED |
| :16-:20 注释内的 `45314`(minsatlevel)、`6473`(satrange)、`58`(xr)、`(100,101)` 锚点、`[101,158]` 跳区、`55000`、`46200` | 7 枚"实证几何"数值 | 见左 | 支撑"本夹具确实触发了被修路径"的推导量 | **待确认**（且证据链不合格） | 注释 :15 自述来源：「实证几何（由插桩版逐锚点观测确认, 参数见 `run/local/bughunt_p1_batchN/p1_sdet/`）」——该路径**不在跟踪面**：`git ls-files \| grep -i bughunt` → 0 命中（实测）。前言 §2.5 档 3 明写"标定件…必须落在跟踪面，指 `run/` 即判不合格" | 保守方向：把三枚派生量（45314/6473/58）的观测结果连同插桩口径落到 `实验/` 或 `artifacts/evidence/`，或把它们变成**断言**（打印/比较 has_saturated 与候选数）。影响：一旦生产侧饱和判据（minsatlevel/satrange 的计算）漂移 ⇒ 平台候选不再进饱和分支 ⇒ 跳区不发生 ⇒ B 无论修复与否都被检出 ⇒ **本件恒绿** | CONFIRMED（跟踪面判定）／PARTIAL（45314/6473 的实际数值我无法在不构建的前提下复算，缺"运行插桩版"一角证据） |
| :17,:22,:24 | `47000 − 800 = 46200`、`55000 − 47000 = 8000 > 6473` | 见左 | 注释内部算式 | 结构性不适用（推导核对，非独立常数） | 我逐式复核：46200 ✓、8000 ✓、55000<65535（不截顶）✓ ⇒ 注释的**算术**自洽 | — | CONFIRMED |

**合计 19 行处置。**

## 6.2 findings（sdet_saturation_cursor_test.cpp）

### CR-52-23 · "实证几何"的派生数只存在于注释、自述证据落在不入库的 `run/`，且机制面（饱和分支归属）无任何断言
- 轴：S（证据资格）＋R
- 位点：`sdet_saturation_cursor_test.cpp:15`（`// 实证几何 (由插桩版逐锚点观测确认, 参数见 run/local/bughunt_p1_batchN/p1_sdet/):`）、:16-:18（`Vp=47000 (仍走饱和分支: meanhigh-bg = 46200 >= minsatlevel 45314; …<= satrange 6473)`）、:19-:20（`edge-walking 得 xr=58 → 修复前 y=101 行扫描游标从 x=100 直接跳到 158`）；对照 :129/:149（`int *sat = nullptr, *has = nullptr;`）、:131/:150（把 `&sat/&has` 传给入口）、**全文再无一处读 sat/has**
- 上位依据：前言 §2.5 档 3「`实验标定` —— 给标定件路径与结果位（**必须落在跟踪面，指 `run/` 即判不合格**）」；`AGENTS.md §5`「实验单元自包含…结果位…」；§7 目录落位「`run/` 临时产物/日志（gitignore，不入库）」
- 现状：三枚决定"本件是否还在锁它想锁的分支"的数（45314、6473、58）由注释声明，无断言、无跟踪面结果位；`sat`/`has` 两数组被接收却从不检查，而文件名（"饱和游标"）与注释 :24-25 的主张（「B 自身走非饱和分支 (has_saturated=0)」）正是要看候选进哪条分支。
- 差在哪：该件锁**结果**（B 被检出），把**机制**（B/平台各进哪条分支）留在注释里；机制一改，同一结果可由完全不同路径产生，锁即失去含义而不报错。
- 后果：`x += xr` 类"游标回写"缺陷的回归保护依赖一组今天无法复核的观测数；若生产侧饱和判据漂移（本模块 mag/饱和列语义本就是已登记缺陷面 DISP-STAR-004），平台候选可能不再进饱和分支 ⇒ 跳区不再发生 ⇒ 修复与否 B 都被检出 ⇒ **恒绿**。方向钉死：破坏机制前提只会让它更绿，不会让它红（断言集只测位置与条数）。
- 定级：S2（不改产物；使"回归锁已落地"的登记主张失去可复核性）
- 怎么算修好：① 把 :24-25 的分支归属变成断言：B 的输出条目 `has_saturated[i] == 0`、平台候选存在且 `== 1`（数组已在手）；② 45314/6473/58 的观测落到跟踪面结果位（`实验/…/results/` 或 `artifacts/evidence/`），或在件内当场重算（用 `sdet_detect_debug` 的图导出）后打印；③ 做不到就把注释从"实证几何"降级为"构造动机"，不保留观测性措辞
- 置信：CONFIRMED（读码＋跟踪面检索；三枚数值本身＝PARTIAL，缺可运行插桩件）

### CR-52-24 · 场景1 是"不崩"门；场景2 断的是行为，但容差比注册门宽 5–50 倍、不查总数、探针只覆盖锚点同一行右向
- 轴：R
- 位点：`sdet_saturation_cursor_test.cpp:135`（`CHECK(rc == 0, name);`）＋ :139（场景1 唯一行为断言＝远处对照星检出，注释 :32-33 亦自认"不断言平台自身"）、:159/:161（B 检出，tol=2.5）、:171-:172（box ±2.5）、:175（`nb == 1 && nc == 1`）；全文**无** `CHECK(n == 2)` 或任何 `n` 的期望值
- 上位依据：注册门 `GATES_AND_TOLERANCES.md:53`（0.5 px）、:55（0.05 px）；判据力要求 `AGENTS.md §9`「对"看起来通过"的结果保持怀疑：空断言、SKIP 充数都算未完成」
- 现状：我按两支分别定性（不预设）：**(a) 场景1 = "不崩"＋对照星**；**(b) 场景2 = 真行为断言**（B/C 各恰好 1 条，注释 :26-27 说明修复前为何 FAIL）——(b) 比 §5 的 fp64 件强，如实记。但：容差 2.5 px 是注册 FP64 门的 50 倍；`nb/nc` 的轴对齐框比 :76 的欧氏 2.5 更宽（角点 3.54 px）；不查 `n`；探针 B 只在锚点同一行 y=101 的平台右向。
- 差在哪：方向钉死——**多出不相关假源不可能使本件判红**（断言集对"过检出"单调不敏感），只有漏 B/漏 C 才红；修复若被**部分**回退（例如只在 `yd`/`yu` 分支回退游标），本件仍绿。
- 后果：该件对"edge-walking 游标污染"缺陷族的覆盖是"一方向、一行"；配合不查总数，本模块假源/漏检回归面（DISP-STAR-002 讨论的星云场域）无保护。
- 定级：S3（范围与容差问题，现有断言本身不假、无错值）
- 怎么算修好：① 容差对齐注册门（0.5/0.05 px）；② 加 `CHECK(n == nb + nc)` 或显式登记允许的平台候选上界，闭合过检出方向；③ 探针加到锚点上一行/下一行与左向各一条（三条同构夹具），使"部分回退"必红；④ 场景1 若要保留"基线回归"名义，至少补一条平台候选 `has_saturated` 断言（与 CR-52-23 合并修）
- 置信：CONFIRMED

## 6.3 不报的核过项（本文件）
- `has_star_near`（:68-:77）在 `rc != 0`、`x == nullptr`、`n == 0` 时不会解引用空指针（循环 0 次，返回 `1e30 <= 2.5` 假 ⇒ 判红），**不报**空指针风险。
- 两个句柄 `sdet_create`/`sdet_destroy` 与两组数组 `sdet_free_detect_ex`（:140-141、:177-178）配对完整，**不报**泄漏。
- 夹具算式（47000−800=46200、55000−47000=8000、100+58=158、135∈[101,158]、81×81）逐式复核自洽，**不报**注释与夹具矛盾（其问题是"无断言＋无跟踪面证据"，已记 CR-52-23）。
- 本件已注册为 ctest（`eng/tests/unit/CMakeLists.txt:1657-1663` `w34_sdet_saturation_cursor`），但 `eng/ci/prod_wiring_baseline.json:79` 把同一路径标为 `unbuildable_test:` —— 两面矛盾，**不据此报缺陷**（构建结果不在本派单能力内），移入「读不动／需实跑」节。
- `params` 先 `memset` 清零再逐字段赋值（:110-120）⇒ 无未初始化读，**不报**。
- 本件不调用本批 .cpp 里的形态学/中值/atrous 原语（除经生产链的高斯与中位/MAD）——覆盖缺口统一记在 CR-52-22，**不重复计数**。

---

# 本批最高危三条（只挑，不复述）

1. **CR-52-04（S1，R 轴）** — 生产 FP64 检测通道对非有限像素**没有任何一道门**：`ipv_select.cpp:1972` → `sdet_api.cpp:1763` → `sdet_image.cpp:249/262`（整行变 NaN）→ `:272/285`（该行以下全 NaN）→ `sdet_api.cpp:1845/1855`（NaN 区零候选）→ `ipv_select.cpp:1978`（`det_count>0` 即成功）⇒ **静默截断的星表以成功退出**；同一危险类在本模块 wrapper 副本（`star_detector.cpp:85-92/:124`）已按 CLEAN-401 修，生产真正调用的这份未修。
2. **CR-52-17（S1，D 轴）＋ CR-52-15** — 日志通道的目录/打开/写三处失败与"永不 `append_close`"全部静默，顶撞 `21_observability.md:45-47`（写入失败⇒非 0 退出码 IO=7；降级必须显式）与 `ASTROCS_DESIGN.md:533`（收尾 fsync＋哈希＋manifest）⇒ "每次运行产出完整可核验运行日志"的主张在该模块不成立；叠加级别枚举 INFO/DEBUG 与四面权威互换 ⇒ 默认档实为最 verbose 档且与同族副本 `dpsf_log.cpp` 取不同默认。
3. **CR-52-01＋CR-52-03（S2，S/C 轴）** — 检测路径的**滤波核**与文档换算不一致（实现核噪声增益 −5.58%，`5σ` 实际＝37.55 σ_smooth 而非注册的 35.45），且 FP64 变体在小画幅下降级成**恒等**（float 同条件仍做真高斯）⇒ "两通道可比"与"不降级"主张同时失真；两者都挂在生产 σ=2.0 调用点上。

# 我读不动或需要实跑才能定的（列清单＋该跑什么）

| 未定案项 | 卡在什么 | 该跑什么（不在本派单权限内） | 跑完能定什么 |
|---|---|---|---|
| CR-52-01/02 的转录保真度 | 前言 §1 禁构建/ctest ⇒ 只能用 Python 转录比对，未与二进制核 | `mem_guard.py --max-rss-gb 4 -- ctest -R p1star_oracle --output-on-failure`（注册面已有 FP64 独立 oracle，GATES:55），或写"逐像元比对 :198-288"的探针 | 实现核 ‖k₁‖₂²=0.133174 与边界不对称 6.37/1050 是否为二进制真值（我预判一致：DC／内部对称／响应形状三不变量已过） |
| CR-52-04 的"生产输入是否实际含 NaN" | 只查到 ipv 入口层无逐像素门（:1899-1910），未穷举 normalize 侧是否已填充/掩膜 NaN | 追 calibrated 帧→`pixel_data` 的填充证据链；或跑 `eng/tests/cli/test_phase1_inprocess.py` 的 `flat_nan.fits` 注入族（:328 一带已有同类负例件） | 该条从"前提 PARTIAL"升为全 CONFIRMED，或确认上游已有门而降为纵深缺陷 |
| CR-52-05/07/11 的偏差量级 | 需真值帧与逐轮统计输出 | 在 `实验/` 造纯噪声＋恒星污染场，分别跑 :787（各轮中位）与 :645（末轮）两份，比 `out_med` 与阈变化 | 给出"背景偏高⇒漏检 N%"的可量化偏差，决定是否升 S1 |
| `w34_sdet_saturation_cursor` 到底能不能建／是否在跑 | 登记面自相矛盾：CMakeLists:1657-1663 注册为 ctest vs prod_wiring_baseline.json:79 标 `unbuildable_test:` | 构建 + `ctest -R w34_sdet_saturation_cursor`（需负责人许可在本轮跑构建） | "回归锁是否真在跑"；两登记面哪侧失真 |
| CR-52-23 的 45314/6473/58 | 自述证据在 `run/local/bughunt_p1_batchN/`，跟踪面 0 命中 | 重跑插桩版并把结果位落到 `实验/` 或 `artifacts/evidence/` | 三枚派生量是否成立、本件是否仍在锁饱和分支 |
| `gauss.cc` / Triggs-Sdika 的一手定义 | 锚文件与文献条目均不在跟踪面（`docs` 内 triggs 0 命中）；无网络一手可判文本可得 | 由负责人批准引入上游实现（含版本与文件:行）或提供该文 PDF，再逐式核 M 与归一化因子 | 把 §1.1 的 12 个 YvV 系数与 M 九式从 UNPROVEN/待确认 升为文献值 |
| 15 个零消费原语的"权威 vs 死"裁决 | 条文只登记"非生产/已退出生产"，未登记退役 ⇒ 我不能自行判"该删" | 负责人就 DISP-STAR-005 去留裁决（`STAR_DETECTION_ALGORITHMS.md:297` 已把裁决权归 P1-STAR-IMPL） | CR-52-07/08/09/10 是缺陷还是"退役件不计"（**我按未退役报，宁多不漏**） |

# 派单文字与清单的差异

1. **清单与派单①-⑤逐条对得上**，无需以清单覆盖派单；但派单①"逐原语核其算法与一手定义是否一致"对本文件的**可核性极不均等**：21 个原语里只有 4 个在生产链（σ=2.0 两版高斯＋中位/MAD 两版），其余 17 个在跟踪面无调用者或只挂在两个已登记为非生产的入口上（§1.0 逐名计数）。全数处置已给（表 55 行）；à trous/形态学/重采样族只能给到"值可导出、选型无锚"，因为 `docs` 面零登记（检索式逐行附在表内），**不是未查**。
2. **派单②的先验框架可用但指向的具体文件对不同**：我未照抄，独立取证结果是——已修的副本是 `wrapper_phase1/star_detector.cpp`（:85-92 入口 isfinite 归约＋:124 返回值判红，:50-61 自述机理），未修的是生产 FP64/FP32 通道真正调用的 `sdet_image.cpp`（1029 行 isfinite 命中 0）＋ `sdet_api.cpp:1772-1777`；而"两份副本取不同值"的字面形态在本批另有两处：日志默认档（`sdet_log.cpp:24` 的 INFO=全放行 vs `dpsf_log.cpp:25` 的 WARN，后者还写明性能理由）与 σ 截尾最小样本门（本文件 `10` vs `dpsf_image.cpp:380` 的 `100`）。以本成稿位点为准。
3. **派单④"自己数，别预设"已执行**：1.0 px vs 注册 0.5/0.05 px（2×／20×）、2.5 px vs 同两条门（5×／50×）、1000 ADU 绝对 vs 1e-3 相对；并如实记反向结论——饱和游标件的**场景2 断的是行为**（`nb==1 && nc==1`），不是"不崩"，只有场景1 接近"不崩"；这与"测试件一律恒真"的常见先验相反。
4. **派单未提但清单包含的边界事实**：`sdet_fp64_test.cpp` 已由 `docs/modules/star_detector.md:85-86` 显式登记为 NON_PRODUCTION_TOOL_ONLY 手工程序（未进 ctest）⇒ 按前言 §4 我**不**把"没进 CI"报为缺陷，只用于定级；`sdet_saturation_cursor_test.cpp` 则已注册 ctest（两面处置不同已在各节 §x.3 写明）。
5. **本批为只读节点，未改任何代码/文档/配置**；未建仓库内文件，临时件全在 `独立审计/复算件/CR-52/`（`median9.py`、`yvv.py`、`yvvp1d.py`），未做任何下载。前言 §5.1 的骨架→逐文件"先表后 finding"→翻牌→bump 节奏已逐项执行。

- 收工 `git status --porcelain` 原文：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```





