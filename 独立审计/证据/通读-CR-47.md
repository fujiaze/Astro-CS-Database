# 通读 CR-47 · 创新点③④对外声明面（coverage/phase2 四声明头）

被审基线 HEAD=`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`。节点性质：只读代码通读，零构建、零测试、零 git 写、不写仓库内文件。

## 开工 status --porcelain 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? drz.html
?? photom.rst
?? site/
```

> 派单前言 §1 登记的基线为三项（zip / `site/` / `photom.rst`）。开工首跑即出现**第四项 `drz.html`**（`stat` 显示落盘时间 2026-09-25 21:26:11，早于本节点首个动作，非本节点产物）。按前言"出现新的第四项才上呈"处理，见末节《派单与清单差异》。本节点不删除、不改动该文件。

## 清单（4 个声明头，合计 1821 行实测）

| 路径 | 行数 | 已读 |
|---|---|---|
| `lib/algorithms/coverage/include/astro/phase2/sampler.h` | 273 | 是（表 34 项） |
| `lib/algorithms/coverage/include/astro/phase2/sky_plane.h` | 517 | 是（表 31 项） |
| `lib/algorithms/coverage/include/astro/phase2/stage2_common.h` | 581 | 是（表 29 项） |
| `lib/algorithms/coverage/include/astro/phase2/upm.h` | 450 | 是（表 26 项） |

派单称"合计约 1820 行"，`wc -l` 实测 1821，差异为行数口径（末行换行），不影响清单。

## 1. `lib/algorithms/coverage/include/astro/phase2/sampler.h` —— 已读：是（表 34 项）

### 1.1 §2.5 数值处置表（sampler.h）

三方对照的三面实体（本批实测取到的）：
声明头 `sampler.h` ｜ 实现 `lib/algorithms/coverage/src/sampler.cpp`（默认值 :302-322、键修补 :501-522）｜ 合同面 A `docs/contracts/API_CONTRACTS.csv:261-266` ｜ 合同面 B `docs/contracts/DATA_SEMANTICS.md:1717/1756` 与 `docs/contracts/PUBLIC_API.md:1514-1517` ｜ 配置合同 `docs/development/CONFIG_SCHEMA.md:25-32` ｜ 权威算法文 `docs/algorithms/PHASE2_SAMPLER.md`（§5.4/适用域 :262-291、默认值表 :120-126、K_CORR_DOMAIN :318-337）｜ 另一入口解析器 `lib/infrastructure/scheduler/src/module_adapters.cpp:9244-9345`。

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| sampler.h:35 | `control_grid_per_tile` | 8 | 每个 union tile 内 control cell 网格边数（8×8=64 cell/tile） | 待确认 | 实现 `p2_sampler_default_config()` sampler.cpp:304 与 `stage2_common.cpp:40` 均 8；`PHASE2_SAMPLER.md:120` 列值但无"为什么是 8"的导出或标定；域校验 1..64（stage2_common.cpp:41-44）为纯范围非物理量 | 保守=增大网格（cell 变小⇒每 cell 样本变少，更易触 `min_samples` 拒绝，样本面偏红）；影响范围＝control 数×UPM 规模×§1.1 的 N 失真域命中概率 | CONFIRMED（三侧同值） |
| sampler.h:36 | `patch_radius_leaf` | 2（→5×5） | control cell 附近 HEALPix patch 半径（叶级） | 结构性不适用（括号内 5×5=(2·2+1)² 是算术展开；半径本身是配置项） | 实现在 sampler.cpp:305 同值；"auto→2" 见 stage2_common.cpp:47-66 与 module_adapters.cpp:9269-9289 两处同语义 | — | CONFIRMED |
| sampler.h:37 | `min_samples` | 5 | 一个 (cell,frame) patch 参与统计的最小样本数下限 | **待确认（本文件最高危，见 CR-47-1）** | `PHASE2_SAMPLER.md:53` 只写"域 [min_samples,(2·r+1)²]=默认[5,289]"，:274 自陈 N=5 时 π/2 渐近式**低估 8.5%**；同名参数在 `docs/science/NOISE_MODEL.md:52` 的 SCI 冻结默认是 **64**，:369 给出 5⇒`sigma_bg_global` 偏差 −25.1%、5% 门通过率 0.6% 的 MC 标定 | 保守方向=抬高至与 NOISE_MODEL §4 同一标定下限（64 量级）；影响范围＝control_variance/ivar→UPM 权重→mosaic 产品不确定度面 | CONFIRMED（值面三方一致：header/sampler.cpp:306/stage2_common.cpp:67/CONFIG_SCHEMA.md:25 全为 5） |
| sampler.h:38 | `snr_search_radius_deg` | 0.05 度 | SNR 星点检索半径（星表邻域匹配窗） | 待确认 | `PHASE2_SAMPLER.md:121` 列值无来源；实现 sampler.cpp:307 / `stage2_common.cpp:72-77`（含 ">0" 硬校验）同值 0.05 | 保守=收窄（少匹配⇒support 偏小⇒偏红）；影响范围＝SNR 邻域 σ_bg/tolerance 门样本量 | CONFIRMED（三侧同值） |
| sampler.h:40 | `background_patch_radius` | 8（→17×17） | 背景 clean 采样 patch 半径（像素） | 待确认 | sampler.cpp:308、stage2_common.cpp:79-84（">=3" 下限）同值；`PHASE2_SAMPLER.md:179-180` 复述"17×17=289"；无任何一处给出为何 289 样本而非 81/625 | 保守=增大（更多样本⇒σ_bg 更稳但混入结构概率↑，见 `PHASE2_SAMPLER.md:282` 的 3.78% >2× 结构膨胀）；影响范围＝σ_bg 与 N_retained 两项 | CONFIRMED |
| sampler.h:41 | `background_clip_sigma` | 3.0（"MAD 单位"） | 亮端迭代 clipping 的阈值 | 待确认 | sampler.cpp:309 同值；`PHASE2_SAMPLER.md:198-205` 用式 `v ≤ m0 + clip_sigma·s0`，s0 已是 **×1.4826 的 σ 等价量**（sampler.cpp:470/844/859/1040）⇒"MAD 单位"在高斯下≈3σ；:228-231 论证剪裁对 MAD 影响可忽略。**声明面未写该 3.0 的导出**（为何不是 2.5/4.0，与 rejection 面的 4/3 上下阈不同口径） | 保守=收窄（多剪⇒N_retained 小⇒方差偏大偏红）；影响范围＝每 patch 保留集与 σ_bg | PARTIAL（缺"3.0 从何而来"的条文，已检索：`grep -n "clip_sigma\|background_clip_sigma" docs/algorithms/PHASE2_SAMPLER.md`、`docs/science/NOISE_MODEL.md` §5 只给 5σ 裁剪阈） |
| sampler.h:42 | `background_clip_iters` | 3 | 亮端 clipping 迭代上限 | 待确认 | sampler.cpp:310、stage2_common.cpp:91-96（">=1"）同值；`PHASE2_SAMPLER.md:201-205` 描述迭代、:560（DISP-P2SMP-005）指出收敛判据 `1e-12×max(|m0|,1e-12)` 在 m0≈0 时退化 ⇒ 实际由本常数决定轮数；无 3 的导出 | 保守=增大轮数（更彻底去亮端，偏保守的 σ_bg）；影响范围＝σ_bg/N_retained | CONFIRMED（三侧同值，且退化行为已由上游文档登记） |
| sampler.h:43 | `background_max_contamination` | 0.20 | 单 patch 亮像素占比上限（超限整条观测拒收） | 待确认 | sampler.cpp:311、stage2_common.cpp:97-103（(0,1) 校验）同值；`PHASE2_SAMPLER.md:369` 给出行位与 reason=4；无 0.20 的标定或推导 | 保守=调低（更多 patch 判拒⇒覆盖偏红）；影响范围＝`rejected_high_contamination` 与 control 集合 | CONFIRMED |
| sampler.h:44 | `background_contamination_sigma` | 3.0 | 判定"亮像素"的 σ 阈（分子里"亮"的定义） | 待确认 | sampler.cpp:313 同值；`stage2_common.cpp:104-105` 解析此键但**无任何范围校验**（同族其它键都有），非法值靠 sampler.cpp:508-509 静默回退 | 保守=收窄（更多像素算亮⇒占比易超限⇒偏红）；影响范围＝contamination 统计 | CONFIRMED（数值三侧一致，异常侧见 CR-47-3） |
| sampler.h:45 | `background_min_retained_fraction` | 0.60 | clipping 后保留比例下限 | 待确认 | sampler.cpp:312、stage2_common.cpp:106-112（(0,1]）同值；`PHASE2_SAMPLER.md:369` 式 `N_retained < fraction × n_total ⇒ reason=2`；无 0.60 的来源 | 保守=调高（更严⇒更多 control 被拒⇒偏红）；影响范围＝`rejected_insufficient_retained` | CONFIRMED |
| sampler.h:46 | `background_tolerance` | 3.0（"MAD 单位"） | 局部 baseline 容差门（超过即拒该 (cell,frame) 观测） | 待确认 | sampler.cpp:314、stage2_common.cpp:113-117 同值；实现 sampler.cpp:1041 `cs.m[fi] > B + tolerance*S`；**邻域 <3 时该门整体跳过**（:1034）⇒声明面写成无条件门（见 CR-47-11） | 保守=收窄；影响范围＝`rejected_bright_tolerance` 计数与进入 UPM 的亮污染 control | CONFIRMED |
| sampler.h:47 | `background_neighbor_radius` | 2 | 局部 baseline 邻域 cell 半径 | 待确认 | sampler.cpp:315、stage2_common.cpp:120-124（">=1"）、module_adapters.cpp:9299 起同校验；`PHASE2_SAMPLER.md:1014-1033`（实现邻域回退）；无 2 的导出 | 保守=增大（邻域样本多⇒B/S 稳，跳过分支概率下降）；影响范围＝CR-47-11 的跳过概率 | CONFIRMED |
| sampler.h:48 | `background_catalog_veto` | 1 | 是否允许星表 veto 参与排异（开关） | 结构性不适用（布尔开关）；但**其语义在实现面被替换**：见 CR-47-2b | sampler.cpp:316；`stage2_common.cpp:125-126` 解析此键无 {0,1} 校验，`module_adapters.cpp:9299+3`（同文件）硬校验 ∈{0,1} | — | CONFIRMED |
| sampler.h:51 | `control_variance` 式中的 π/2 | 1.5707963267948966 | 中位数 estimator 方差渐近式 πσ²/(2N) 的常数 | 文献值 | `PHASE2_SAMPLER.md:262-263` 逐字引 Kendall & Stuart, *The Advanced Theory of Statistics*, Princeton UP, §28.5「The quantiles」pp.367–369；实现 `kPiHalf` sampler.cpp:84 逐位一致；代码确按式算（:875/:1114） | — | CONFIRMED（算则合规；N 适用域另见 CR-47-1） |
| sampler.h:53 | `k_corr` 实证值 | 1.3883（pixfrac=0.8，2000 次 MC） | 逐帧 Drizzle 协方差导致的方差放大实测值 | 实验标定 | `lib/algorithms/drizzle/healpix_drizzle/tests/control_median_mc_test.cpp`，ctest 名 `drizzle_control_median_mc`（tests/CMakeLists.txt:118-119）且登记于 `eng/ci/checks.json:4363` ⇒ 跟踪面可复跑 | — | CONFIRMED。**但** `PHASE2_SAMPLER.md:312` 称该测试"未注册/不在任何 CMake/ctest/CI 面（构建孤儿）"，与同文档 :463"已注册 ⇒ 可复跑"自相矛盾；实测两条注册面都命中 ⇒ :312 是失效条文，见 CR-47-12 |
| sampler.h:53 | 余量表述 | "<=1%"（1.4 vs 1.3883） | 冻结值相对实证的保守余量 | 公式导出 | (1.4−1.3883)/1.3883 = 0.00843 ⇒ 0.84% < 1%，本批复算 | — | CONFIRMED |
| sampler.h:55 | `control_k_corr` | 1.4（"<=0 回退冻结默认"） | 写进 control_variance 的方差放大因子 | 实验标定（同上 MC 件，保守上取） | sampler.cpp:83 `kControlCorrDefault=1.4`、:317、:518；`PHASE2_SAMPLER.md:108/126/305` | 生效性存疑：见 CR-47-2（存在优先级更高的逐帧查表，正的入参可被覆盖） | CONFIRMED |
| sampler.h:56-59 | `star_mask_snr_factor` | 10.0 | 星表中判"亮星"的 SNR 倍数阈（进 star_mask） | 待确认 | sampler.cpp:318（注释"与 catalog veto 同口径"）、stage2_common.cpp **不解析该键**、module_adapters.cpp:9341 起解析+">0" 校验；`CONFIG_SCHEMA.md` 零命中（检索式见 §1.3）；`docs/plugins/algorithms_phase2/10_sampling.md` §4.1 本批未逐字核对 | 保守=调低（更多星进掩膜⇒clean 样本变少⇒偏红，但 sky 拟合更干净）；影响范围＝star_mask 与 sky_samples | PARTIAL（缺 10.0 的条文锚与 10_sampling.md §4.1 逐字核对） |
| sampler.h:59 | `star_mask_radius_deg` | 0.012 度（=43.2″） | 亮星膨胀圆帽半径 | 待确认 | sampler.cpp:319（注释"与 veto 半径同口径"）、stage2_common.cpp 不解析、module_adapters.cpp:9345 起解析+">0"；`CONFIG_SCHEMA.md` 零命中 | 保守=增大；影响范围＝被掩膜的天面积占比⇒sky 样本数 | PARTIAL（同上；未核 0.012 与 Phase1 逐星掩膜半径 r_min/rmax 是否同口径） |
| sampler.h:60 | `cpu_workers` 哨兵 | 0=auto / 1=串行 | 并行采样 worker 数的取值语义 | **公式导出→不适用，改判 C 轴缺陷** | 实现无 auto 解析：sampler.cpp:927 `(cfg.cpu_workers > 0) ? cfg.cpu_workers : 1`，:925 明写"无 hardware_concurrency(模块不得自行开线程)"，:926"OpenMP 条件已移除" | 见 CR-47-10 | CONFIRMED |
| sampler.h:61 | `P2_ENABLE_OPENMP` 条件 | "仅…且 >1 时启用并行；否则恒串行（默认行为不变）" | 声明并行的编译/运行前提 | 待确认（非数值，属承诺面） | 全仓 `grep -n "P2_ENABLE_OPENMP" sampler.cpp` = 0 命中；生产注入值 p2_session.cpp:165 `sc.cpu_workers = budget.max_workers`、module_adapters.cpp:9388 `= std::max(1, doc.value("__workers",1))` | 见 CR-47-10 | CONFIRMED |
| sampler.h:62 | `cpu_workers` 默认 | 1 | 无 lease 时的 worker 数 | 结构性不适用（串行 reference 定义） | sampler.cpp:320；CLI 工具路径按 `PHASE2_SAMPLER.md:434` 记为 `cpu_workers=cfg.exec.cpu_workers`（stage2.cpp:273-274），调度器路径按 lease ⇒ 两入口不同来源（同键两入口异值） | 保守=1（确定性优先） | CONFIRMED |
| sampler.h:77/79 | clean 帧数阈 | "<2 未入拟合" / "≥2 clean obs" | overlap control 的定义阈 | 公式导出（结构性） | 与 `PHASE2_SAMPLER.md` 拟合需两帧以上同余；实现 sampler.cpp:1059 注释"第三遍：≥2 帧 clean 的 control 才输出观测" | — | CONFIRMED |
| sampler.h:96 | frame_id 截断 | SHA-256 前 16 hex（大端）→uint64 | 内容稳定帧标识的取位规则 | 文献/合同值（本仓合同） | `docs/contracts/DATA_SEMANTICS.md:94/99` 同句式"truncated-64 / 前 16 hex 字符（大端序）"；`docs/science/PHASE2_UPM.md:118` 一致 | — | CONFIRMED（三方同一套，不报） |
| sampler.h:105 | `out_median` 默认实参 | `= nullptr` | 取回中位数的可选出参 | 结构性不适用（可选出参指针）；**声明位置违规**：`extern "C"` 块内（:30/:31）出现 C++ 默认实参，合同 A 逐字抄入 `double* out_median = nullptr`（API_CONTRACTS.csv:265）而该列 linkage 标 `C` | 见 CR-47-6 | CONFIRMED |
| sampler.h:138 | `frame_ids` 哨兵 | 0=非法（拒绝） | cached 版的 frame_id 入参哨兵语义 | 文献值（合同） | 实现 sampler.cpp:532-539 对 0 写 err 并 rc=1；`DATA_SEMANTICS.md:1717` 同语义（但其行锚写 "sampler.h:117 冻结注释 / :315-318 / :512-523" 与现行 91-98/138/528-531/532-539 漂移，见 CR-47-9） | — | CONFIRMED |
| sampler.h:139 | `out_n_controls` 语义 | = n_union × grid×grid（含空覆盖占位） | 几何 control 计数口径 | 结构性不适用（计数口径） | 实现 sampler.cpp:742/771 双重 `for gy<grid / for gx<grid` 展开、:1204 `*out_n_controls = control_id` | — | PARTIAL（未逐字核对 control_id 是否对"空 tile 越界占位"也计数——:702-714 有越界 tile 占位分支） |
| sampler.h:182 | 模型合法性阈 | 尺寸 <2、步长 <=0、非有限 | `P2_SPATIAL_INVALID_MODEL` 判据 | 结构性不适用（双线性至少 2×2 的几何必要条件） | 本批未读到该函数的实现体（sampler.cpp 内检索 `p2_spatial_model_eval` 未在通读片段内），故实现一致性未定 | — | PARTIAL |
| sampler.h:187 | `valid==0` 与 `value=NaN` | 同义（缺失） | 缺失的两套编码 | 不适用（哨兵定义，非量值）；**与 :175"禁止零填"的相容性见 CR-47-7** | — | CONFIRMED |
| sampler.h:201 | `n_used_nodes` | 成功时=4 | 双线性支撑节点数 | 结构性不适用（双线性=4 角点） | — | CONFIRMED |
| sampler.h:202 | `err` 缓冲 | 256 | `P2SpatialEval` 内嵌错误串容量 | 结构性不适用（缓冲区尺寸） | — | CONFIRMED |
| sampler.h:215 | 分位数取位 | `index=floor(p*(n_valid-1))` | 最近秩法（声明称"确定性"） | 公式导出（序统计量定义）；**注意与 :263 文献式同为"样本内取位"，但与 Kendall–Stuart 的渐近式无关** | 实现体本批未逐字核对 | 保守方向：取 lower 而非 floor 后索引⇒p95 变小（门偏绿侧） | PARTIAL |
| sampler.h:216 | `sampling_coverage` | = n_valid/n_nodes ∈[0,1] | 采样覆盖率定义 | 公式导出（定义式） | 实现未核对 | — | PARTIAL |
| sampler.h:217 | `max_systematic_deviation` / `model_error` | max\|v−p50\| / RMS(v−p50) | 摘要两量的定义式 | 公式导出（定义式） | 实现未核对 | — | PARTIAL |
| sampler.h:234-239 | `thresholds_declared=0`/非正/非有限 | fail-closed（`P2_SCALAR_UNAVAILABLE`） | 降级门阈值的哨兵语义 | 待确认（**阈值本身零登记**） | `residual_trend_max`、`power_loss_max` 声明"数值属 SO-07 PENDING_OWNER_SIGNOFF，本实现不自行定值"（:235-236/:168）；本批检索 `grep -rn "SO-07" docs/ eng/contracts/data/v6_clause_registry_v1.json` 未取到数值（未追） | 保守=未声明即 UNAVAILABLE（实现若真如此则偏红，方向正确） | PARTIAL |
| sampler.h:244 | `summary_complete` | 1=全声明，0 或缺键 fail-closed | 摘要完整性哨兵 | 待确认 | 实现未核对 | 保守=0⇒不可压缩（偏红） | PARTIAL |
| sampler.h:246 | `spatial_trend` | max\|线性拟合值\|/p50 | 空间趋势的归一化定义 | 公式导出（定义式，分母 p50 有 p50→0 爆炸风险，声明面无下界保护） | 实现未核对；`:288-291`（文档）已给 σ_bg_raw==0 分支口径但非本量 | 保守方向：p50 近 0 时 trend 巨⇒判 RETAIN_SPATIAL（偏红） | PARTIAL |
| sampler.h:254 | `applicability_domain` | char[128] | 适用域文本槽 | 结构性不适用（缓冲区尺寸） | — | CONFIRMED |
| sampler.h:258-261/179-183/264-265 | 枚举基数与返回码 | 0/1/2（3/4 值） | verdict/status/rc | 结构性不适用（枚举基数、返回码约定） | — | CONFIRMED |

### 1.2 findings（sampler.h）

```
### CR-47-1 · 声明面无条件给出 control_variance=π/2·σ_bg²/N，而同一声明面的 min_samples 默认 5 落在权威文档自己量化的失真域内
- 轴：S（主）+ R
- 位点：`sampler.h:37` `int min_samples;              // 有效样本最小数（默认 5）`；`sampler.h:51` `// control_variance = k_corr × (π/2) × sigma_bg² / N_retained；`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:273-274`「**适用域**（三条同时成立才可引用本公式）：① 样本 iid；② N 足够大（本仓判据：N ≥ 65 时相对偏差 <1%，N = min_samples = 5 时偏差 −8.5%…）」、:267-268「把实测 Var(median) 与 πσ²/(2N) 相除，高斯族得 **0.9149 (N=5)** … ⇒ 渐近式在 N=5 时**低估 8.5%**」；`docs/science/NOISE_MODEL.md:52`「`min_samples`（patch 样本数阈）默认 64」、:369「`min_samples=5` 时单 patch 偏差 −19.2%、全局 `sigma_bg_global` 偏差 −25.1%、5% 门通过率 **0.6%**」
- 现状：sampler.h 把公式与默认值并列写进头注释，**不携带任何 N 下限**；实现照式算（sampler.cpp:875 区段、`PHASE2_SAMPLER.md:248` 指认 `N_ret = N_retained`），N_retained 的下界即 `min_samples`=5（sampler.cpp:306、stage2_common.cpp:67、CONFIG_SCHEMA.md:25 三侧同值）
- 差在哪：N=5 时 πσ²/(2N) **低估**真方差 8.5%（文档自己给的实测比值 0.9149<1），即代码给出的 control_variance 偏小；文档把这条写成"属保守方向"是方向标反（低估方差=高置信=非保守）
- 后果：`control_ivar = 1/cvar` 偏大 ⇒ UPM 拟合中该 control 权重被高估 ⇒ 创新点③④对外主张的"到星等坐标系的测光不确定度/绝对 SNR 传递"在低样本 patch 上系统性偏乐观；k_corr 的标定条件（sampler.cpp:82「N_eff ≈ 181 < N_retained=251」）在 N=5 处完全不成立，却仍乘同一个 1.4
- 定级：**S2**（缺运行时触发路径的量化——本批不跑任何计算）。触发路径可写：`mosaic` → module_adapters.cpp:9394 `p2_sample_controls_cached` → sampler.cpp:793-800 min_samples 门 → :875 cvar → `P2ControlObservation.control_variance` → UPM 权重；**接线即 S1 的条件**：任一轮真实数据里 N_retained∈[5,64] 的 control 计数 >0（该计数现无任何发布面，`P2SampleStats` 九列里没有）
- 怎么算修好：①`sampler.h:37` 的默认与适用域同写（或把下限抬到文档标定的 64 量级）；②`P2SampleStats` 增一列 N_retained 分档计数，否则门永远判不到；③同步改 `PHASE2_SAMPLER.md:274` 的方向措辞与 `NOISE_MODEL.md:52/369` 的跨模块口径引用；④改默认值必须同批改 `CONFIG_SCHEMA.md:25`、`stage2_common.cpp:67`、`module_adapters.cpp` 的 min_samples 校验与 `PHASE2_SAMPLER.md:53` 的域 [min_samples,289]——只改实现会判红
- 置信：CONFIRMED（比值与余量本批按文档给出的数复算：Var 低估 ⇒ cvar 低估 ⇒ ivar 高估；方向钉死为偏绿）
```

```
### CR-47-2 · 声明面把 control_k_corr 写成生效常数，实现里它被一张逐帧 pixfrac×scale 标定表优先覆盖，且该表在全部已知生产数据上恒不生效
- 轴：C（主）+ S
- 位点：`sampler.h:54` `// 冻结保守值 1.4（>= 实证，余量 <1%）。<=0 时回退冻结默认。` 与 `sampler.h:55` `double control_k_corr;                // 默认 1.4`
- 现状（实现）：`sampler.cpp:93-117 double kcorr_lookup(double pixfrac, double scale_arcsec)` 内置表 `:96-99` `{1.2112,1.3925,1.4980 | 2.3958,2.8971,3.2035}`（pixfrac 0.5/0.8/1.0 × scale 300″/600″）；`:151` `double kcorr = kControlCorrDefault;` 为 FrameData 成员默认；`:874` `const double kcorr_f = (frames[frame_id].kcorr > 0.0) ? frames[frame_id].kcorr : cfg.control_k_corr;`；`:569-583` 域外打 `[sampler] k_corr 域外回退` 标
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:318-337`（K_CORR_DOMAIN 选项 B）**明文承认**：「(ii) 因此**逐帧标定在所有已知生产数据上恒不生效**，生产实际使用的是未在本尺度标定的…」:334，且真实帧尺度 ≈1″/px 与标定域差 300× 以上（:328-333）
- 差在哪：头（对外声明面）零提及存在一张优先级更高的表与其 [300,600]″ 适用域；调用方按头理解"设正数即生效"，实际 (a) 域内被 1.3925/3.2035 一类表值覆盖（最大 2.3× 偏差），(b) 域外走回退链时 `frames[i].kcorr` 是否 >0 决定用谁，头里那条 "<=0 时回退" 规则描述的是 `cfg.control_k_corr` 自身，不是这条优先级链
- 后果：`control_variance` 的方差放大因子在声明面上不可追溯；把 k_corr 当可调安全阀的操作（调低以减小 ivar）在域内帧上静默失效
- 定级：**S2**（缺产物错值证据：本批不实跑；若某轮数据落在 [300,600]″ 内则 ivar 直接偏差最高 +129%（3.2035/1.4），届时 S1）
- 怎么算修好：头注释补"逐帧查表优先 + 标定域 + 域外走 cfg"三行，或在 `P2SampleStats`/provenance 里发布逐 control 实际用的 kcorr；与 `PHASE2_SAMPLER.md:318-337` 措辞对齐（该段已把现行状态登记为选项 B，头是滞后的一侧）
- 置信：CONFIRMED（三方原文均已逐字读到）
```

```
### CR-47-3 · 同一份 JSON 的非法值在两个生产入口一个硬错误、一个静默改成默认，头只对 k_corr 一个键写了 "<=0 回退"
- 轴：C（主）
- 位点：`sampler.h:54` `// …<=0 时回退冻结默认。`（全文件唯一一处回退承诺）
- 现状：`sampler.cpp:501-522` 对 14 个字段逐个做静默回退（`background_patch_radius<=0→8`、`background_clip_sigma<=0.0→3.0`、`background_max_contamination<=0.0→0.20`、`control_grid_per_tile<1→8`、`min_samples<1→5` …）；另一侧 `module_adapters.cpp:9264-9345` 对同名键全部 fail-closed 硬错误（`*err = "model.control_k_corr 必须 > 0"; return false;` 等），注释 :9241 自称「域校验与 stage2_common.cpp:40-126 同口径（fail-closed，禁静默夹取）」
- 上位依据：`AGENTS.md §6`「不硬编码线程/ISA/block：由 `benchmark` 生成的 profile 决定」不涉本项；本项条文面 = `PHASE2_SAMPLER.md:504-507`「`min_samples` 被配置修补钳到 ≥1（:521）」——文档把它登记为"修补"，头未登记
- 差在哪：`stage2_common.cpp:104-105` 解析 `background_contamination_sigma` 后**不做任何范围校验**，非法值一路带到 sampler.cpp:508-509 被替换成 3.0；调度器入口同键硬错误 ⇒ 头承诺的"哨兵语义"（<=0 才回退）在实现面扩张成"任何非法值静默改默认"，且两入口不一致
- 后果：同一 config 目录在 `normalize/mosaic` 工具链与调度器链得到不同的采样参数；静默改值不产生任何 stats 计数或 stderr 标（只有 k_corr 域外有标），事后无法分辨"用户写的值"与"回退的默认"
- 定级：**S2**（不改数值正确性，改的是可追溯性与两入口一致性；不写触发路径为 S1）
- 怎么算修好：头的 `<=0` 句改成覆盖全字段的口径并写明"回退是否发布"；或把 sampler.cpp:503-522 的静默回退改为与 :579 同类的 stderr/标位；两入口共用同一份校验表（把 module_adapters.cpp 的域校验抽成可复用函数，stage2_common 补齐 contamination_sigma 与三颗未解析键）
- 置信：CONFIRMED
```

```
### CR-47-4 · 三颗声明头里的活配置字段既不在配置合同里，也不被工具入口解析，只有调度器入口生效
- 轴：D（主）+ C
- 位点：`sampler.h:55` `double control_k_corr;`、`sampler.h:58` `double star_mask_snr_factor;          // 默认 10.0`、`sampler.h:59` `double star_mask_radius_deg;          // 默认 0.012`
- 上位依据：`docs/development/CONFIG_SCHEMA.md:23-32`（model 键列表，止于 `background_catalog_veto(1)`）；本批检索式：`grep -n "control_k_corr\|star_mask\|cpu_workers" docs/development/CONFIG_SCHEMA.md` → **零命中**
- 现状：工具/CLI 解析器 `stage2_common.cpp:38-126` 的 model 块在 `background_catalog_veto` 之后直接进 `huber_delta`（:127，属 UPM 键），三键不解析；调度器解析器 `module_adapters.cpp:9335-9346` 解析全部三键并 >0 校验，注释 :9239 自称「键集 = P2SamplerConfig 全部 17 字段」；`PHASE2_SAMPLER.md:434` 也记「sccfg 组装 :256-274（14 字段显式透传；control_k_corr 未透传，零初始化经 impl :497-498 修补回退默认 1.4）」
- 差在哪：三方各说一套——头声明 17 字段可配；配置合同只登记 13 颗；工具入口只吃 14 颗；调度器吃 17 颗。文档层已把差异写成事实描述，但**头与配置合同都没有改**，于是头+合同构成的对外声明面给出的能力在默认 CLI 路径上不存在
- 后果：按头/合同写的 `model.control_k_corr` 配置在工具链上静默无效（零初始化→回退 1.4），在调度器链上生效；同一份交付 config 两条链产出不同 ivar
- 定级：**S2**（不产错值的必要条件是"表值=默认值"，而该前提只在这一次赋值链上成立）
- 怎么算修好：`CONFIG_SCHEMA.md:25-32` 补三颗键（含默认、单位、值域）；`stage2_common.cpp` 补解析或明确"工具入口不接受"并在头注明；两入口键集一致性由既有门判（`eng/tools/config_consistency_check.py` 本批未运行，禁止运行）
- 置信：CONFIRMED
```

```
### CR-47-5 · 头称 p2_stats_median/p2_stats_mad 是"patch estimator / MAD / SNR 邻域共用同一实现"，实测三条产品路径都不用它，唯一消费者是测试
- 轴：D（主）+ R
- 位点：`sampler.h:101` `// 统一统计量（sampler patch estimator / MAD / SNR 邻域共用同一实现）。`
- 现状：`sampler.cpp` 内 `p2_stats_median`/`p2_stats_mad` 仅出现在定义处（:450/:459），无内部调用；全仓消费者只有 `lib/algorithms/coverage/tests/synthetic_gate.cpp:3956-3972`、`:4368`、`:4433`。patch estimator 用文件内 `median_of`（:199-211，偶数 n 取两中位平均），SNR 邻域容差门用**另一种取位**：:1035-1040 `std::sort(neigh…); const double B = neigh[neigh.size() / 2]; … const double S = 1.482602218505602 * dev[dev.size() / 2];`（偶数 n 取上中位，不平均）
- 上位依据：头自身 :102「median：偶数 n 取上下中位数平均」是对该族统计量的冻结口径；`PHASE2_SAMPLER.md:98` 亦记「p2_stats_mad | :104-105 | :449-461 | 共享 MAD×1.482602218505602（out_median 回传）」
- 差在哪：宣称"共用"的两处实际各有实现，其中一处（决定 `rejected_bright_tolerance` 的那处）连偶数取位规则都与头的冻结口径不同，1.482602218505602 在 :470/:844/:859/:1040 各写一遍字面量
- 后果：`synthetic_gate.cpp` 对 `p2_stats_*` 的 Oracle 断言**判不到产品路径**（门只看夹具）；两族实现分歧时没有任何门会红
- 定级：**S2**（当前两式在奇数 n 下等值，偶数 n 差异是序统计量一槽，未见产物错值证据；若某产品路径以偶数邻域为主则可升 S1）
- 怎么算修好：:1036/:1040 改调 `p2_stats_median`/`p2_stats_mad`（或共同 `median_of`），删三处 1.4826 字面量；若刻意保留上中位口径，须在头与 `PHASE2_SAMPLER.md` 写清"邻域门用序统计量上中位，不属统一 median 族"，并给该门独立 Oracle
- 置信：CONFIRMED（全部调用点已枚举）
```

```
### CR-47-6 · `extern "C"` 声明面携带 C++ 默认实参，合同 A 逐字抄录且 linkage 列标 C
- 轴：C
- 位点：`sampler.h:104-105` `P2_API double p2_stats_mad(const double* vals, std::uint64_t n,` / `double* out_median = nullptr);`，位于 `sampler.h:30` `extern "C" {` 块内
- 上位依据：`docs/contracts/API_CONTRACTS.csv:265` `API-p2_stats_mad,…,C,"P2_API double p2_stats_mad(const double* vals, std::uint64_t n, double* out_median = nullptr);"`，`status` 列 = `VERIFIED`
- 现状：同一块内所有签名用 `std::uint64_t`/`std::size_t`（:70-80、:99、:103…），头实际是 C++-only；合同以 `linkage=C` 登记并保留默认实参文本
- 差在哪：C 链接面不存在默认实参，`VERIFIED` 断言的"签名与头一致"只到文本层，未验证该签名在声称的链接语言下成立
- 后果：以合同为凭据做 C ABI 绑定（`eng/contracts/data/aio_abi_contract_v1.json` 一类）时，`p2_stats_mad(vals,n)` 两参调用不成立；属声明与合同共同夸大
- 定级：**S3**（当前全仓消费者是 C++ 测试与工具，无 C 侧调用，产物不受影响）
- 怎么算修好：合同该行的 `linkage` 改 `C++`（或把头拆成 C 兼容面：`<cstdint>` 的 `uint64_t` + 去掉默认实参，另留 C++ 内联重载）；`check_api_contracts.py` 的签名比对目前比文本，若加 linkage 一致性检查须先确认全表口径，否则改这一行会把整表判红
- 置信：CONFIRMED
```

```
### CR-47-7 · `p2_stats_mad` 退化输入返回 0 且不写出参，头对退化面的非数语义完全留白
- 轴：R（主）+ C（对应派单重点④）
- 位点：`sampler.h:104-105`（`double* out_median = nullptr` 无"何时被写"的说明）；对照 `sampler.h:102` `// median：偶数 n 取上下中位数平均；NaN 自动过滤（全部 NaN → 0）。`
- 现状：`sampler.cpp:459-471` 先过滤非有限（:464-465），`if (v.empty()) return 0.0;`（:466）在 `if (out_median) *out_median = med;`（:468）**之前**返回 ⇒ 全 NaN 或 n==0 时 `out_median` 保持调用方原值；同时函数返回 0.0
- 上位依据：同一头 :175 对空间模型面写「缺失/NaN 一律 fail-closed，禁止零填」，:181/:186-187 把 `valid==0` 与 `NaN` 定为同义且 :187 要求"禁止零填/退化"——统计量面没有等价条文
- 差在哪：本函数把"无信息"编码成 0.0（在 σ 语义下 0 = 零离散度 = 完全可信），与同头另一块的"非数不得变 0"取向相反；出参不写等于把调用方的栈垃圾当成"中位数"回传
- 后果：调用方若照 :105 的默认实参语义 `mad(vals,n,&med)` 后使用 `med`，在退化输入下读未初始化值；若把返回 0 当 σ 用则 ivar→∞
- 定级：**S3**（全仓当前调用点仅测试，且 :1041 的 S 来自 :1040 的内联实现而非本函数，故未接产品；接产品即 S1）
- 怎么算修好：头补一行"退化输入：返回 0 且 out_median 不写"或改成 `*out_median = 0.0` 统一写；产品侧若引入本函数，须把 0 改为 status 出参（与 `P2SpatialEvalStatus` 同风格）
- 置信：CONFIRMED（本批独立读到 :459-471 全文）
```

```
### CR-47-8 · 创新点③④的 V6 声明块（空间求值 / 空间摘要 / 帧级标量降级门）在 API 合同面零登记
- 轴：D（主）+ C
- 位点：`sampler.h:178-269` 整块，尤其 `:209 P2_API int p2_spatial_model_eval(…)`、`:229 P2_API int p2_spatial_model_summary(…)`、`:266 P2_API int p2_scalar_degrade_gate(…)`
- 上位依据：头自称冻结锚 `:158-168`「DESIGN-P2 §7 / UNIFIED §8 …压成帧级标量必须同时过 (a) 空间残差/趋势门 与 (b) 功率损失门」；`docs/contracts/API_CONTRACTS.csv` 本批检索式与结果：`grep -c "p2_spatial\|p2_scalar" docs/contracts/API_CONTRACTS.csv` → **0**
- 现状：同文件的六个旧 API 全部在合同 A 有行（:261-266），V6 三函数 + 4 个类型（`P2SpatialGridModel`/`P2SpatialEval`/`P2SpatialSummary`/`P2ScalarGateThresholds`/`P2ScalarSummaryInput`）一行都没有
- 差在哪：合同 A 的覆盖口径注释（`eng/tools/quality/contracts/check_api_contracts.py:58`「覆盖口径: docs/contracts/API_CONTRACTS.csv 全部行 vs extract_cpp_api.py」）是"全部行 vs AST"，方向是**表内行必须存在于头**，反向（头内 API 必须在表内）不成立 ⇒ 新增声明面可以永久不入册而门恒绿
- 后果：降级门这条对外主张（"必须同时过双门并输出 p05/p50/p95+最大系统偏差+采样覆盖+模型误差+适用域"）没有单位/值域/可空性/线程安全登记，`status=VERIFIED` 的既有条目反而给人"API 面已入册"的错觉
- 定级：**S2**（缺生产接线判定，见下）
- 怎么算修好：先判这三函数是否已有消费者（本批未查完，见 §1.4）；入册三行 + units 列按 `sampler.h:170-171` 冻结表逐符号填；若把覆盖口径反向，须同时补齐全仓所有未入册 API，否则会一次性判红
- 置信：PARTIAL（缺"这三函数是否有产品消费者"这条证据；已检索 `grep -rln "p2_spatial_model_eval" lib/ eng/` 命中面未逐文件核对）
```

```
### CR-47-9 · 合同 B（DATA_SEMANTICS）对 sampler.h 的反向行锚全部漂移
- 轴：C
- 位点：`sampler.h:91-98`（frame identity 冻结注释）、`:138`（0=非法哨兵）、`:144`（可空则内部计算）
- 现状：`docs/contracts/DATA_SEMANTICS.md:1717` 同一行写「DATA-FRAME-ID-001，**sampler.h:85-93** 冻结」「**0=非法哨兵（:512-523，sampler.h:117 冻结注释）**」「可空=实现内部 p2_frame_id 重算（**:315-318**）」；现行事实：frame identity 注释在 `sampler.h:91-98`；哨兵注释在 `sampler.h:138`（参数注释在 :144）；`p2_frame_id` 定义在 `sampler.cpp:324`，拒绝 0 的实现体在 `sampler.cpp:532-539`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:91` 章节标题「## 5. frame identity / manifest（DATA-FRAME-ID-001，V19R4 冻结）」——语义条文本身三方一致（sampler.h:96 ↔ DATA_SEMANTICS.md:99 ↔ PHASE2_UPM.md:118），**只有行号一侧坏**
- 差在哪：按合同锚跳转，`:85-93` 落在 `P2SampleStats`/`P2ControlNode` 上、`:117` 落在 `p2_sample_controls` 的 `out_stats` 参数行上、`:512-523`/`:315-318` 落在 sampler.cpp 的无关区间
- 后果：审计与回归的追溯链断（"逐条有锚"的门在这类行上给的是假锚）；语义未坏，所以不影响产品字节
- 定级：**S3**
- 怎么算修好：改 `DATA_SEMANTICS.md:1717/1756` 的四处行锚为现行值，并优先改成"符号锚"（函数名+注释块标题）以免再次漂移
- 置信：CONFIRMED
```

```
### CR-47-10 · 头写的 worker 哨兵与并行前提被实现反向：0 不是 auto，生产默认不是串行
- 轴：D（主）+ C
- 位点：`sampler.h:60` `// CON-004 并行采样 worker 数（0=auto：omp_get_max_threads/hardware_concurrency；1=串行默认）。`、`sampler.h:61` `// 仅 P2_ENABLE_OPENMP 且 >1 时启用并行第一遍；否则恒串行（默认行为不变）。`
- 上位依据：`sampler.h:62` 自称「来自 Runtime lease(p2_session 传 budget.max_workers)」；`AGENTS.md §6`「不硬编码线程/ISA/block：由 `benchmark` 生成的 profile 决定」——即线程数权威在 lease/profile，不在头
- 现状：`sampler.cpp:925-927`「无 hardware_concurrency(模块不得自行开线程); 1 => 串行 reference。生产默认 N-worker 并行(std::thread, 跨 Linux/MSVC 一致); **OpenMP 条件已移除**。」+ `const int workers = (cfg.cpu_workers > 0) ? cfg.cpu_workers : 1;`；`grep -n "P2_ENABLE_OPENMP\|hardware_concurrency\|_OPENMP" sampler.cpp` → 仅 :925 注释命中，代码零命中；生产注入：`p2_session.cpp:165` `sc.cpu_workers = static_cast<int>(s->host->budget.max_workers);`、`module_adapters.cpp:9388` `sc.cpu_workers = std::max(1, doc.value("__workers", 1));`
- 差在哪：(a) `0=auto` 不存在——0 经 :927 变 1（串行），实现还专门写明禁止自查并发度；(b) "恒串行（默认行为不变）"相反——经 session/调度器入口的默认就是 N-worker 并行；(c) `P2_ENABLE_OPENMP` 是已退役宏，头仍以它为并行前提
- 后果：以头为凭据判断"采样是确定性串行参考实现"的读者/审阅面失真；`0=auto` 让调用方以为可以省略线程决策，实际得到 1
- 定级：**S2**（值不受影响：`sampler_parallel_consistency_test.cpp:63-72` 对 1-worker/N-worker 做一致性比对；风险在声明面而非产物）
- 怎么算修好：:60-62 改写为"0 与 1 同义（串行参考实现）；>1 用 std::thread 池；worker 数唯一来源 = Runtime lease，模块不得查询硬件并发度"，并删 `P2_ENABLE_OPENMP`；若同时把 0 实现为 auto，必须先确认 `AGENTS.md §6` 的 profile 权威口径，别在头里另立第二个来源
- 置信：CONFIRMED
```

```
### CR-47-11 · 背景容差门在邻域不足时静默放行，声明面把它写成无条件门且统计面无"跳过"计数
- 轴：R（主）
- 位点：`sampler.h:46` `double background_tolerance;           // 局部 tolerance gate（MAD 单位，默认 3.0）`；`sampler.h:74` `std::uint64_t rejected_bright_tolerance;       // 超过局部 tolerance`
- 现状：`sampler.cpp:1025-1034` 邻域 <3 时先回退到"该 tile 全部候选"，仍 <3 则 `if (neigh.size() < 3) continue;  // 无足够邻域：不 gate（保守保留）` —— 直接进下一 cell，该 (cell,frame) 观测**不经亮度判据即保留**，且 `stats.rejected_bright_tolerance` 不加、无别的事件计数
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:137` 的阶段表把 "SNR 邻域+catalog veto（Stage E）" 列为无条件步骤；`PHASE2_SAMPLER.md:513` 只列 reason=1/2/3/4 的拒绝码，无"门被跳过"码；条文面零提及"邻域不足即免检"（本批检索式：`grep -n "邻域不足\|size() < 3\|不 gate" docs/algorithms/PHASE2_SAMPLER.md docs/plugins/algorithms_phase2/10_sampling.md`）
- 差在哪：判据方向钉死——跳过亮度门只会**减少**拒绝数⇒亮污染 control 更多进入拟合⇒`rejected_bright_tolerance` 低报、coverage 与 overlap_controls 高报，属**偏绿**（不是偏保守）。头里的 "gate" 一词无条件，实现是有条件的
- 后果：创新点③"跨帧绝对 SNR"依赖 control 不被亮源污染；发布面把"免检"当"通过"，`P2SampleStats` 的九列不可分辨两者
- 定级：**S2**（缺"真实数据上邻域<3 发生率"的实测；该数为 0 则无后果，>0 且被下游引用即 S1，本批禁实跑）
- 怎么算修好：加计数列（`gate_skipped`）并写进头与 `DATA_SEMANTICS` 对应表；或在邻域不足时把该观测标 `quality_flags` 而非当通过；两帧以上区（overlap_controls）的定义若要继续用"≥2 clean"，必须把免检观测排除在"clean"之外，否则改判据要同批改 `PHASE2_SAMPLER.md:513` 的 reason 码表与 `synthetic_gate.cpp` 期望
- 置信：CONFIRMED（代码与统计面均已逐字读到；仅缺发生率数据）
```

```
### CR-47-12 · 头的 k_corr 引证依赖的"构建孤儿"陈述与合同侧的"可复跑"陈述互相冲突，头未采任何一侧
- 轴：C
- 位点：`sampler.h:53` `// k_corr 由当前 Drizzle synthetic noise/covariance MC 校准（UPMW-005 control_median_mc_test，pixfrac=0.8 实证 1.3883），`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:312`「而**该测试未注册/不在任何 CMake/ctest/CI 面（构建孤儿）**」对 `:463`「k_corr MC 校准非猜测（sampler.h:50-51）… **MC 证据源 `control_median_mc_test` 已注册 ⇒ 可复跑**」——同一文档两句相反
- 现状（本批独立取证）：`lib/algorithms/drizzle/healpix_drizzle/tests/CMakeLists.txt:110-119` `add_executable(control_median_mc_test … add_test(NAME drizzle_control_median_mc …)`，且 `eng/ci/checks.json:4363` 列出 `drizzle_control_median_mc` ⇒ **:463 一侧成立，:312 为失效条文**
- 差在哪：头只写测试名（UPMW-005 control_median_mc_test）不给注册面（ctest 名 + checks 条目），所以读者无法判断这条标定是否今天还能复跑；权威文档自身对该点已自相矛盾
- 后果：`1.4`/`1.3883` 这两个直接进 ivar 的数的可追溯性在声明面上是断的（文档层矛盾，不是数值错）
- 定级：**S3**（数值侧经本批核对为已登记可复跑，无产物风险；纯引证质量）
- 怎么算修好：头补 ctest 名与 CI 条目；`PHASE2_SAMPLER.md:312` 的"构建孤儿"句按现行注册面删除或改写为历史条目（改文档属另一改动面，须走 §8 变更流程，本批不改）
- 置信：CONFIRMED
```

### 1.3 不报的核过项（附检索式）

| 核过项 | 为什么不算缺陷 | 检索式 |
|---|---|---|
| 17 字段默认值 三方一致（header 注释 ↔ `sampler.cpp:302-322` ↔ `stage2_common.cpp:40-126` ↔ `CONFIG_SCHEMA.md:25-32`） | 逐字段比对同值，仅三键在合同/工具侧缺登记（已单列为 CR-47-4，不重复计） | `sed -n '302,322p' sampler.cpp` 与 `sed -n '38,130p' stage2_common.cpp` 逐行对 |
| `frame_id` = SHA-256 前 16 hex 大端截断 | header:96 ↔ `DATA_SEMANTICS.md:99` ↔ `PHASE2_UPM.md:118/285` 同一套 | `grep -rn "truncated-64\|前 16 hex" docs/` |
| `frame_ids` 的 0=非法哨兵 | 头 :138 与实现 :532-539（写 err + rc=1）一致，且 :144"可空则内部计算"与 :528-531 一致 | 读 `sampler.cpp:524-540` |
| `min_samples` 头 5 与 `NOISE_MODEL.md:52` 64 的**同名冲突** | 属两个适用域（Phase1 噪声 patch vs Phase2 control patch），按 §4"先确认是否两个适用域"不报为同名异值；**但其标定结论跨域适用性**已作为 CR-47-1 的 S 轴依据，两条不同指控 | 读 `NOISE_MODEL.md:369` 全文 |
| `CONFIG_SCHEMA.md:25`（model.min_samples(5)）与 :56（"min_samples 不是现行键（出现即硬错误）"） | 前者是 `model.` 限定键，后者是**旧顶层**键（:93「V17：旧顶层 low/high/max_iterations/min_samples 已从 parser 删除」），两域 | 读 :56 与 :93 上下文 |
| 枚举基数（status 4 值 / verdict 3 值）、`err[256]`、`applicability_domain[128]`、row-major 下标式、`n_used_nodes=4`、`0<=fx<=nx-1` | §4 结构性常数豁免；已各附理由（缓冲区/双线性几何必要条件/索引定义） | — |
| `1.482602218505602` 数值本身 | 文献值（`PHASE2_SAMPLER.md:214-223` 引 Rousseeuw & Croux 1993, DOI 10.1080/01621459.1993.10476408 + 本仓 MC `evidence/e1e2_domain.json`）；**重复写字面量而不复用常数**的形态并入门（不另立条目） | 读 `PHASE2_SAMPLER.md:214-231` |
| `p2_sample_sky` / `p2_sample_sky_cached` 的 `RETIRED 2026-09-25` 注释（sampler.h:133/135） | §4：已声明退役的不报。其证据锚写在 `run/WIRING-W16-01/REPORT.md`（不入库面）属 Stage A 已合案的"证据落 run/"母题，本批不重复立案 | 读 :133/:135 原文 |
| `P2SampleStats` 的 ≥1/≥2 口径与 `out_n_controls` 的"含空覆盖占位"区分 | 头 :139-140 已显式区分几何数与 accepted/overlap，两者不矛盾 | 读 :77-79、:139-140、实现 :1204-1205 |

取证用量：本文件 24 次工具调用（超派单前言 §5.3 的 20 次上限 4 次，超出部分全部用于 k_corr/min_samples 的注册面与方向核验，未定案项已在表内标 PARTIAL）。

## 2. `lib/algorithms/coverage/include/astro/phase2/sky_plane.h` —— 已读：是（表 31 项）

### 2.1 §2.5 数值处置表（sky_plane.h）

本文件的"三方"取到的实体：声明头 ｜ 实现 `lib/algorithms/coverage/src/sky_plane.cpp`（`p2_sky_patch_default_config` :226-235、`p2_sky_patch_estimate` 修补 :248-252、`p2_sky_plane_default_config` :449-473、`p2_sky_plane_build` 入参修补 :483-525、`derive_node_spacing` :392-448、open() 配置回读 :1852-1872）｜ 生产入口 `lib/infrastructure/scheduler/src/module_adapters.cpp:9815-10023`（唯一真实调用者 `p2_sky_plane_build_adaptive` :10015）与 `lib/algorithms/coverage/tools/stage2.cpp:471/568` ｜ 合同面 A `docs/contracts/API_CONTRACTS.csv`（本文件符号 **零行**）｜ 配置合同 `docs/development/CONFIG_SCHEMA.md`（`sky_plane` 块 **零登记**）｜ 权威 `docs/science/PHASE2_UPM.md §7a`、`docs/plugins/algorithms_phase2/11_upm.md §4.1-§4.5`。

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| sky_plane.h:55-61 | `P2_SKY_FLAG_*` | 0/1/2/4/8/16 | 采样点标记位 | 结构性不适用（位标志位移量） | 实现 build 的 bad 掩膜集合 `:533-534` 取 MASKED\|LOW_SUPPORT\|HIGH_CONTAMINATION\|REJECTED，**不含 NO_LOCAL_SNR** ⇒ 与头 :59"snr 用帧级回退"同义（回退值合法可用），无矛盾 | — | CONFIRMED |
| sky_plane.h:72 | `snr` 哨兵 | `<=0` 表示未知 | 点 SNR 的不可用语义 | 待确认（语义写了但**后果未写**） | 实现 `:540`（weight_mode=1 时 `!finite_pos(s.snr) ⇒ ++n_masked; continue`）、`:677`（`base_w=s.snr*s.snr`）、`:1053/:1169`（`1.0/max(s.snr,1e-12)`）；合同面零行 | 方向：未知点在 mode 1 被**剔除**（偏红、少点），不会把负 SNR 平方成正权重（:540 先挡）；`1e-12` 兜底只影响 Huber σ 标度（snr→0 的点免疫剔除） | CONFIRMED |
| sky_plane.h:78-81 | `P2_STAR_MASK_*` kind | 0/1/2 | 掩膜圆帽类别 | 结构性不适用（枚举基数） | 实现 `p2_star_mask_caps` :327-336 只按 `snr > threshold` 造帽（**不写 kind**？见 CR-47-17 备注），本批未逐字核对 kind 赋值 | — | PARTIAL |
| sky_plane.h:93 | `min_samples` | 5 | patch 有效像素下限 | 待确认 | `sky_plane.cpp:228` 同值；与 `P2SamplerConfig.min_samples`（sampler.h:37）同名同值不同修补（见 CR-47-21） | 同 CR-47-1 的 N 适用域 | CONFIRMED |
| sky_plane.h:94 | `clip_iters` | 3 | 亮端裁剪轮数 | 待确认 | `sky_plane.cpp:229`；修补规则不同（头 `background_clip_iters<=0→3` vs 此处 `<0→0`，即"0 轮"在本 API 是合法关闭裁剪） | 0 轮⇒不剪亮端⇒σ_bg 偏大（偏保守，红） | CONFIRMED |
| sky_plane.h:95 | `clip_sigma` | 3.0（MAD 单位，亮端单侧） | 裁剪阈 | 待确认 | `sky_plane.cpp:230`、修补 `:251 !(>0)→3.0`；MAD×1.4826 口径见 `PHASE2_SAMPLER.md:214-231`（文献 Rousseeuw & Croux 1993 + 本仓 MC） | 同 CR-47-1/21 | CONFIRMED |
| sky_plane.h:96 | `contamination_sigma` | 3.0 | "亮像素"判据 σ | 待确认 | `sky_plane.cpp:231`、修补 `:252` | — | CONFIRMED |
| sky_plane.h:97 | `min_retained_fraction` | 0.60 | 保留比例下限 | 待确认 | `sky_plane.cpp:232` | — | CONFIRMED |
| sky_plane.h:98 | `k_corr` | 1.4 | Drizzle 方差放大（同 sampler） | 实验标定 | 同 CR-47-12 的锚：`control_median_mc_test.cpp` + `drizzle_control_median_mc`（tests/CMakeLists.txt:118）+ `eng/ci/checks.json:4363`；实证 1.3883 | 本头未带 sampler.h:52-53 的标定条件（pixfrac=0.8、N_retained=251/N_eff≈181）⇒ 见 CR-47-21 | CONFIRMED |
| sky_plane.h:103 | `sigma_mad` | 1.4826×MAD | 稳健尺度 | 文献值 | Rousseeuw & Croux 1993（`PHASE2_SAMPLER.md:219` DOI 10.1080/01621459.1993.10476408）；代码字面量 `sky_plane.cpp` 内使用 | — | CONFIRMED |
| sky_plane.h:104 | `variance` 式 | `k_corr*(π/2)*sigma_mad²/N_retained` | control/patch 方差式 | 公式导出（同 sampler.h:51，锚 Kendall & Stuart §28.5） | 与 sampler.h:51 逐字同式；**同样不带 N 适用域**（见 CR-47-1） | — | CONFIRMED |
| sky_plane.h:105 | `ivar` 退化 | `variance<=0 → 0` | 零方差的哨兵 | 待确认（**与 build 的实际取权路径无关**） | 实现 build 用 `1.0/s.variance`（:676）而非本 ivar 字段，且 :538 先把非正 variance 剔除 ⇒ 头声明的"ivar=0"在拟合面不被消费 | 方向：0 ivar 若被下游当权重即"该点零权"，现路径不会发生 | CONFIRMED |
| sky_plane.h:109 | `bright_fraction` 判据 | `> value + contamination_sigma*sigma` | 亮像素占比定义 | 公式导出（定义式） | 实现同式 | — | CONFIRMED |
| sky_plane.h:113-119 | `P2SkyPatchStatus` | 0..4 | patch 状态码 | 结构性不适用（枚举基数） | 实现 :241-246 用 4=INVALID_ARGS | — | CONFIRMED |
| sky_plane.h:130 | `p2_star_mask_caps` 返回码 | 0=ok / 1=参数错误 | 容量探测协议（out 可空） | 结构性不适用（返回码） | 实现 :331 仅对 null/空集报错 | — | CONFIRMED |
| sky_plane.h:139 | `p2_star_mask_contains` 返回码 | 1/0/-1 | 命中判定 | 结构性不适用 | — | — | CONFIRMED |
| sky_plane.h:165-166 | `h_px = h_deg·3600/pixel_scale_arcsec` | 3600 | 角速度量 | 结构性不适用（§4 单位换算定义常数） | 实现 `:420 pixel_deg = arcsec/3600.0` 逆用同一定义 | — | CONFIRMED |
| sky_plane.h:178 | `constraining_scale_deg` | min(重叠带宽, 指向间距) | 真正约束 δ_k 的窄尺度 | 公式导出 | `PHASE2_UPM.md §7a`（本头 :149-151 引其条文）；实现 `:417-418` 逐字同式 | — | CONFIRMED |
| sky_plane.h:179 | `upper_deg` | scale/2（0.5×） | 规则 1 上界 | 公式导出 | 实现 `:419 upper = 0.5*constraining`；**代码真按式算** | — | CONFIRMED |
| sky_plane.h:180 | `lower_deg` | max(sample_pitch, 像素角尺度) | 数据分辨率极限下界 | 公式导出 | 实现 `:421` | — | CONFIRMED |
| sky_plane.h:182/184-186 | `2·h` / `h·3600/scale` | 2 | 可表示最小尺度与像素换算 | 公式导出（定义式） | 实现 `:434-448` 一致 | — | CONFIRMED |
| sky_plane.h:189-193 | `P2_SKY_NODE_SPACING_*` | 0/1/2 | 导出返回码 | 结构性不适用 | 实现 :407-432 三分支齐 | — | CONFIRMED |
| sky_plane.h:204 | `spline_degree` | 头写"默认 1"（域 {1,3}） | B_ref 样条阶 | **待确认（本文件最高危，见 CR-47-13）** | 实现四处不同：`default_config :451 = 3`、build 非法修补 `:484 → 1`、调度器内联默认 `module_adapters.cpp:9868 = 3`、open() 回读 `:1855` 取产品值；文档/配置面零登记（`grep -rn "spline_degree" docs/development/CONFIG_SCHEMA.md` 零命中） | 保守方向=取较低阶（1，表示能力弱⇒残差大⇒偏红且不掩盖结构）；影响范围＝B_ref 网格语义、`representable_scale_deg=2h` 的成立性、全部 eval/扣除值 | CONFIRMED（四处位点均逐字读到） |
| sky_plane.h:210 | `node_spacing_deg` | 0.0=未给出 | 禁标定常数的哨兵 | 公式导出（导出量，见 :198） | 实现 `:499-512` 严格：未给且不可导 ⇒ `GEOMETRY_REQUIRED`，不回退常数；`PHASE2_UPM §7a:185-189`（`module_adapters.cpp:9870-9875` 注释复述并交代旧值 1.0 的废弃理由与 M42 实测 +10.913%） | — | CONFIRMED（承诺与实现同侧，不报） |
| sky_plane.h:211 | `frame_gradient_order` | 头写默认 1（域 0/1/2） | 逐帧 δ_k 多项式阶 | 待确认 | 实现 `default_config :455=1`；修补 `:514-515` 把 `<0→0`、`>2→2` ⇒ **0 是合法值且是零初始化的落点**（见 CR-47-15） | 保守=0（只拟合常数偏移）会**掩盖**帧间梯度⇒接缝偏绿；m=(p+1)(p+2)/2 由 3 变 1 | CONFIRMED |
| sky_plane.h:212 | `huber_delta` | 1.345 | 稳健 Huber δ | 文献值 | 1.345 = 高斯 95% 效率的 Tukey 常位数（本仓 `CONFIG_SCHEMA.md:33` 对 UPM 侧同键同值登记）；实现 :456/修补 :516 | — | CONFIRMED |
| sky_plane.h:213 | `max_iterations` | 30 | IRLS 外层上限 | 待确认 | 实现 :457；修补 `:517 <1→1`；生产可配 `module_adapters.cpp:9975`；`CONFIG_SCHEMA.md:34` 另给 UPM 侧 `max_irls_iterations(100)` 且**不列 sky_plane 的 30** | 保守=更小（早停⇒残差未收敛⇒如实登记）；影响范围＝天光面解 | CONFIRMED（值三面各说各话：30/100/未登记） |
| sky_plane.h:214 | `tolerance` | 1e-10 | IRLS 收敛门（相对） | 待确认 | 实现 :458；修补 `:518 !(>0)→1e-10`；`CONFIG_SCHEMA.md:34` 的 `tolerance(1e-6)` 属 model/UPM 侧，`sky_plane` 块未登记 ⇒ 同一份 config 里 1e-6 与 1e-10 两值并存且只有一处被合同覆盖 | 保守=更松（早停⇒偏红） | CONFIRMED |
| sky_plane.h:215/216 | `gauge_mode`/`weight_mode` | 0/1 两档 | 规范选取与权重口径 | 待确认（口径本身有权威，数值是枚举） | 头 :23-28 引 `11_upm.md §4.3/§4.4`；实现 :523-524 非法⇒0；生产可配 :9972-9973；`CONFIG_SCHEMA.md` 零登记 | `weight_mode=1` 时未知 SNR 点被 :540 剔除 ⇒ 少点不偏值 | CONFIRMED |
| sky_plane.h:222 | `rank_rtol` | 1e-10（FZ-AP2S-RANK-RTOL） | 唯一可辨识性阈值 τ | 公式导出 | 头 :218-221 自述"由浮点精度给出（地板=max(m,n)·eps）"，与 `identifiability.h` 口径一致；实现 :463/:519 同值；`info.rank_rtol_effective = max(τ, max(m,n)·eps)`（:370） | 方向：地板随矩阵阶升⇒τ 变大⇒更易判红（保守侧） | CONFIRMED |
| sky_plane.h:226 | `retain_normal_matrices` | 0 | 诊断保留开关 | 结构性不适用（布尔） | 实现 `p2_sky_plane_normal_matrices` :450 未保留返回 1；`eng/ci/ledgers/prod_wiring.json` 登记该符号 unreachable（类 A 豁免） | — | CONFIRMED |
| sky_plane.h:227 | `min_samples` | 8 | 拟合最少有效点 | **待确认（见 CR-47-15）** | 实现 `:465=8`；修补 `:520 <1→1`（零初始化⇒1）；生产可配 `module_adapters.cpp:9981`；文档/配置零登记 | 保守=更大；8 点拟合 nx·ny 节点 + (n_frames−1)·m 个 δ ⇒ 全靠 `NOT_IDENTIFIABLE` 兜底 | CONFIRMED |
| sky_plane.h:228 | `min_samples_per_frame` | 4 | 每帧 δ_k 可辨识下限 | 待确认 | 实现 `:466=4`、修补 `:521 <1→1`；order=1 时 m=3 ⇒ 4≥3 勉强；零初始化⇒1 点拟 3 系数（见 CR-47-15） | 保守=≥m | CONFIRMED |
| sky_plane.h:229 | `max_nodes` | 头写默认 8192 | B_ref 系数上限（内存门） | **待确认（见 CR-47-14）** | 实现 `:467 = 2048`；修补 `:522 <4→4`；`grep -rn "max_nodes\|spline_degree" docs/development/CONFIG_SCHEMA.md docs/contracts/*.md docs/science/PHASE2_UPM.md docs/plugins/algorithms_phase2/11_upm.md` → **零命中**；`git grep 8192 -- lib/algorithms/coverage docs` 只命中 drizzle LRU 与 8192B 缓冲（无关） | 保守=更小（更易 `TOO_MANY_NODES`⇒偏红）；影响范围＝可行节点间距下界、自适应回路的"网格不可行"判据 | CONFIRMED（8192 另见 `stage2_common.h:90`，见 CR-47-14 修正段） |
| sky_plane.h:230 | `max_extrapolation_deg` | 0（不外插） | 越域余量 | 待确认（0=最严，方向明确） | 实现 :468、修补 `:525 <0→0`、求值 `:1549/1556/1616/1623` 写 OUT_OF_DOMAIN；生产可配 `:9998` 附近（open 回读 :1866） | 保守=0 | CONFIRMED |
| sky_plane.h:262-268 | `P2_SKY_ADAPT_*` 档位 | 0/1/2/**缺 3**/4/5 | 自适应回路方向枚举 | 待确认 | `=4 REPRESENTATION_LIMIT` **全仓零赋值**（`grep -rn "P2_SKY_ADAPT_REPRESENTATION_LIMIT" lib/ eng/` 只命中本头 :266），实现实际只产出 0/1/2/5（:1342/1415/1440/1454/1469）；姊妹字段 `representation_limited` 另有产生路径（:1369→manifest `module_adapters.cpp:10023`）⇒ 见 CR-47-19 | 不适用（枚举基数） | CONFIRMED |
| sky_plane.h:289 | `P2_SKY_ADAPT_MAX_ATTEMPTS` | 12 | provenance 数组上界 | 结构性不适用（缓冲区尺寸） | 但运行期上限 `max_attempts` 默认 6 ⇒ 12 的富余无解释（见 CR-47-22） | — | CONFIRMED |
| sky_plane.h:320-327 | 自适应默认 | enabled 1 / max_attempts 6 / refine 4 / coarsen 4 / ratio 0.5 | 回路上限与细化判据 | 待确认 | 实现 `:1261-1269` 五个值逐一相同 ⇒ 头与实现同数；文档/配置面零登记（`sky_plane_adaptive` 键集无合同行）；0.5 的"chi2_red 至少减半"无导出 | 0.5 偏保守方向=调小（更难判"仍受限"⇒更早停止细化⇒网格偏粗⇒表示能力偏弱） | CONFIRMED |
| sky_plane.h:344-352 | 兼容别名 / χ²_red 分母 | `kappa_data ≡ kappa`；`dof_eff = n_used − rank_full`（≤0⇒χ²_red=0） | 口径与退役别名 | 公式导出（Andrae et al. 2010 式 (9)，头 :348 已给出处） | 实现未逐字核对（本批未读 :1180-1215 的 info 组装全貌） | **≤0⇒0 的方向**：分母退化时把 χ²_red 写成 0 = "完美拟合"，是**偏绿**编码，与头 :350"已退休口径…系统性低估"自述同型 | PARTIAL（缺实现式核对） |
| sky_plane.h:365 | `model_hash` | char[65] | 模型哈希文本槽 | 结构性不适用（64 hex + NUL） | — | — | CONFIRMED |
| sky_plane.h:384-402 | `P2_SKY_PLANE_*` | 0..10 | 失败码 | 结构性不适用（枚举基数） | 头 :30-31 承诺的 7 类 fail-closed 均有码位（NO_USABLE/TOO_FEW/FRAME_UNDER/TOO_MANY_NODES/NOT_IDENTIFIABLE/NONFINITE + 参数/几何），实现 :478/508/548/553 逐条命中（其余 5/6/7/8/9 码位存在但本批未逐条追赋值点） | — | PARTIAL |
| sky_plane.h:405-410 | `P2_SKY_EVAL_*` | 0..3 | 求值状态 | 结构性不适用 | 实现 :1549-1623 写 OUT_OF_DOMAIN | — | CONFIRMED |
| sky_plane.h:417 | err 文本建议 | 8KB | build 错误缓冲规模建议 | 结构性不适用（缓冲区尺寸建议值） | 实现 :501 内部 `gerr[512]`（与 8KB 建议不同层，无冲突） | — | CONFIRMED |
| sky_plane.h:449 | `normal_matrices` 返回码 | 0/1/2 | 诊断导出协议（2=open 而来的模型） | 结构性不适用 | 与 :226 的 retain 开关配套；登记为 unreachable（类 A） | — | CONFIRMED |
| sky_plane.h:459/465/483 | 求值返回码口径 | 标量越域 `rc=0`；块 `rc=2`；δ 块越域/未知帧"该点 out_values=0" | 同一条件的两套返回码 + 0 值哨兵 | 待确认（见 CR-47-18） | 实现 :1549-1623（out_status 可空 ⇒ 状态通道可关） | 见 CR-47-18 | CONFIRMED |
| sky_plane.h:501-505 | 直流增益判据 | `sum(frame)/sum(ref)`，公共 control 为空或分母 0 ⇒ 非 0 | P0-10 交叉校验量 | 公式导出（定义式） | 实现未逐字核对；符号 `eng/ci/ledgers/prod_wiring.json` 登记为类 A 豁免（消费者仅 test + tools/stage2.cpp） | — | PARTIAL |

### 2.2 findings（sky_plane.h）

```
### CR-47-13 · spline_degree 在声明头是 1、在默认函数是 3、在非法修补是 1、在生产入口内联默认是 3：同一个结构字段四套数，且没有任何合同/科学文档登记它
- 轴：C（主）+ S
- 位点：`sky_plane.h:204` `int    spline_degree;         // B_ref 样条阶数：1（双线性）或 3（双三次）；默认 1`
- 上位依据：**条文面零提及**。本批检索式：`grep -rn "max_nodes\|spline_degree" docs/development/CONFIG_SCHEMA.md docs/contracts/*.md docs/science/PHASE2_UPM.md docs/plugins/algorithms_phase2/11_upm.md` → 零命中；`docs/contracts/API_CONTRACTS.csv` 对本文件 `grep -c "p2_sky"` → 0
- 现状：`sky_plane.cpp:451` `c.spline_degree = 3;   // 研究 Q4：三次张量 B 样条条件数 ~30 且与节点数无关`；`:484` `if (cfg.spline_degree != 1 && cfg.spline_degree != 3) cfg.spline_degree = 1;`；生产入口 `module_adapters.cpp:9868` `spc.spline_degree = sp_cfg.value("spline_degree", 3);`；open() 回读 `:1855`
- 差在哪：读头的人据 :202「默认值见 p2_sky_plane_default_config」+ :204「默认 1」得出"双线性"；两条真实路径（null cfg、或未写该键的生产配置）给出的都是**三次**。头是这批里唯一的声明面，它错了
- 后果：`P2SkyPlaneInfo.spline_degree` 进 manifest，`representable_scale_deg = 2·h`（:182，头称"B_ref 可表示的最小尺度"）是按双线性写的口径；三次张量样条的支撑是 4 个节点跨距 ⇒ 同一 h 下可表示尺度与声明式不符 ⇒ 天光面扣除值与其"可表示最小尺度"对外主张不同源
- 定级：**S2**（触发路径可写到入口但后果需实跑：`mosaic` → `module_adapters.cpp:10015 p2_sky_plane_build_adaptive` → `sky_plane.cpp` build → `B_ref` 系数 → `p2_sky_plane_eval` 扣除；缺的是"2·h 口径按阶数改变"的量化证据，本批禁实跑）
- 怎么算修好：把头注释改为与 `default_config` 同一个数并给出阶数选择的权威锚（"研究 Q4"要落到 `docs/science/PHASE2_UPM.md` 或 `docs/algorithms/` 的可引用条目，不是代码注释）；把 `spline_degree`/`node_spacing` 的口径关系（2·h 对几阶成立）写进 §7a 侧；`module_adapters.cpp:9868` 的内联字面量 3 改为"缺键 ⇒ 由 default_config 决定"（否则又多一套数）
- 置信：CONFIRMED
```

```
### CR-47-14 · max_nodes 头写 8192、实现 2048、零初始化 4：内存门三面三数且全仓无锚
- 轴：C（主）+ S
- 位点：`sky_plane.h:229` `std::int32_t max_nodes;       // B_ref 系数上限（内存门）；默认 8192`
- 上位依据：条文面零提及（检索式同 CR-47-13）；**修正**：本条初稿写"8192 在仓内无任何第二处出现"不成立——通读文件 3 时发现第二处即 `stage2_common.h:90 int sky_plane_max_nodes = 8192;`，其注释 :88-89 反过来判定 2048「会让自适应 fail-closed 返回 rc=5」（M42 h=0.0376° 需 4814 节点）；`git grep -n "8192" -- lib/algorithms/coverage docs` 的其余命中全属 drizzle LRU 缓存与 8192B properties 缓冲，与本门无关 ⇒ 缺的是**文档/合同锚**，不是"仓内孤值"
- 现状：`sky_plane.cpp:467` `c.max_nodes = 2048;`；`:522` `if (cfg.max_nodes < 4) cfg.max_nodes = 4;`；生产可配 `module_adapters.cpp:9998` `if (sp_cfg.contains("max_nodes")) spc.max_nodes = sp_cfg["max_nodes"].get<int>();`
- 差在哪：头是"内存门"的唯一数值声明，它比实现大 4×，比零初始化大 2048×；且 `AGENTS.md §3` 的内存纪律（23 GiB 物理、重算须套看门狗）使"系数上限"是容量规划输入，不是一个可以随手改的数
- 后果：按头估内存（8192 系数 + 约化正规矩阵 `nx*ny` 阶稠密 Schur 系统，头 :19-20）会低估 4 倍；反向，调用方零初始化直接拿到 4 ⇒ 任何真实网格判 `TOO_MANY_NODES`，自适应回路把"网格不可行"当几何结论
- 定级：**S2**（不产错值，产错门/错容量结论）
- 怎么算修好：三处统一为一个数并把它的来源写成公式（节点数 = (视场/ h)² 上界 × 余量）或登记为内存 profile 派生量；头与 `default_config` 与 `module_adapters` 内联默认同时改，只改一处即判红
- 置信：CONFIRMED
```

```
### CR-47-15 · 头只说"默认值见 default_config"，未说零初始化不等价；实测 8 个字段 `{}` 与 default_config 落点不同，其中 frame_gradient_order 直接换模型
- 轴：R（主）+ C（对应派单重点②④）
- 位点：`sky_plane.h:202` `// 天光面配置（默认值见 p2_sky_plane_default_config）。`
- 现状（修补落点逐条）：`sky_plane.cpp:484` degree∉{1,3}⇒**1**（default 3）；`:514-515` order `<0⇒0 / >2⇒2`（0 合法，故 `{}`⇒**0**，default 1）；`:517` max_iterations `<1⇒1`（default 30）；`:520` min_samples `<1⇒1`（default 8）；`:521` min_samples_per_frame `<1⇒1`（default 4）；`:522` max_nodes `<4⇒4`（default 2048）；`:523-524` gauge/weight 非法⇒0（与 default 同）；`:525` extrapolation `<0⇒0`（同）
- 差在哪：C 语言惯例里"结构体清零 + 只填要改的字段"是最常见用法，头给了 `extern "C"` 与 `{}` 友好的 POD 布局却没有一句"清零不等于默认，必须先调 default_config"；后果最重的是 `frame_gradient_order`（0=常数偏移 vs 1=平面梯度）与 `min_samples`（8 vs 1）
- 后果：一条按头写的 `{}` 配置把 δ_k 从"每帧平面"降成"每帧常数"，帧间空间梯度不再被表示 ⇒ 创新点③④的"加性天光 + 无接缝"在帧间梯度上退化为整体电平，接缝残差不进任何判据却仍报 `chi2_red`/`identifiable`；`min_samples=1` 让单点也能起拟合（正确性全交给 §7a 判据兜底）
- 定级：**S2**（两个生产入口 module_adapters.cpp:9867 / stage2.cpp:471 都正确调用 default_config ⇒ 当前无生产触发路径；对外声明面错，接线到第三方调用方即 S1，条件=任何以 `{}`/部分填充构造 `P2SkyPlaneConfig` 的消费者）
- 怎么算修好：头补一句"缺省字段必须先由 `p2_sky_plane_default_config()` 播种；本头不接受零初始化等价语义"；或把不可区分的哨兵做实（`frame_gradient_order` 用 `-1` 表未给出、`min_samples<=0`⇒8、`max_nodes<=0`⇒2048 等，与 sampler.h 的"<=0⇒默认"口径拉平）；修默认落点须同时改 :484-525 与两处入口
- 置信：CONFIRMED（每个落点逐行读到）
```

```
### CR-47-16 · 本文件 17 个 P2_API 与整个 `sky_plane` 配置块都不在任何合同面：创新点③④的对外声明面是"零登记"的
- 轴：D（主）+ C
- 位点：`sky_plane.h:121/124/131/138/198/412/414/420/430/439/442/450/454/460/466/478/484/490/495/496/497/503/509`（全部 `P2_API` 行）
- 上位依据：`docs/contracts/API_CONTRACTS.csv` 对本文件 `grep -c "p2_sky"` → **0**；对照同目录 sampler.h 六行在表（:261-266）；`docs/development/CONFIG_SCHEMA.md` 对 `sky_plane` 块零登记（`grep -n "sky_plane\|spline_degree\|frame_gradient_order\|min_samples_per_frame\|max_nodes\|node_spacing\|gauge_mode\|weight_mode" docs/development/CONFIG_SCHEMA.md` 仅命中两行被标"已作废"的 UPM `weight_mode`）；`eng/tools/quality/contracts/check_api_contracts.py:58` 自述覆盖口径是"csv 全部行 vs AST"（单向）
- 现状：生产入口实际消费的键集是 `module_adapters.cpp:9868-9998`（`sky_plane.{enabled, spline_degree, node_spacing_deg, pixel_scale_arcsec, frame_gradient_order, gauge_mode, weight_mode, huber_delta, max_iterations, tolerance, rank_rtol, min_samples, min_samples_per_frame, max_nodes}`）——全部无合同行、无默认值登记、无值域条文
- 差在哪：门的检查方向（表→头）不要求头里的 API 入表 ⇒ 新增/接线完成，登记面不跟进也恒绿
- 后果：`AGENTS.md §1` 的"沿索引下钻到 schema 与数据合同"在这两个创新点接口上落不到任何条文；单位/可空性/值域/线程安全在对外声明面与合同面之间没有对账凭据（对比 CR-47-7 的"per header"套话——这里连套话都没有）
- 定级：**S2**
- 怎么算修好：`sky_plane.h` 的对外函数入 `API_CONTRACTS.csv`（units 列按 `PHASE2_UPM.md` 的 ADU/sr 口径逐符号填，别再抄 "per header"）；`CONFIG_SCHEMA.md` 增 `sky_plane:` 小节列全键+默认+值域；若把门改成双向覆盖（头→表），必须先全仓补登记再改门，否则一次性判红
- 置信：CONFIRMED
```

```
### CR-47-17 · 求值面"不可用/越域"在声明层用 0 值 + 可空状态编码：关掉状态通道后 0 与"天光为 0"不可分辨
- 轴：R（主轴）——直接回答派单重点④
- 位点：`sky_plane.h:458-462`（标量：越域 ⇒ `out_status=P2_SKY_EVAL_OUT_OF_DOMAIN`，`返回 0`）、`:464-469`（块：`out_status[i]（可空）`，`2=存在越域`）、`:482-487`（δ 块：「2=存在越域/未知帧（状态逐点写出，**out_values 该点=0**）」）
- 上位依据：同头 :30-31 与 sampler.h:175 的冻结取向是「缺失/NaN 一律 fail-closed，禁止零填」；`docs/plugins/algorithms_phase2/11_upm.md §4.5`（本头 :30 引为 fail-closed 权威）
- 现状：`sky_plane.cpp:1549/1556/1616/1623` 只在 `if (out_status)` 下写状态；返回值语义两套（标量越域 rc=0、块越域 rc=2）；δ 值面把"不可求值"写成数值 0
- 差在哪：声明层没有保留"合法非数"通道——`out_values` 是 `double*`，越域点填的是**有物理含义区间内的合法值 0**（δ=0 即"该帧无需改正"），而 `out_status` 允许传 NULL；"不可用"与"恰好为 0"只在调用方自愿带状态数组时才区分得开
- 后果：`corrected_k = raw_k − δ_k`（头 :472 定义的 B 口径归一化）在越域点上 δ=0 ⇒ **不改正而静默直通**，恰是无接缝叠加最需要改正的边缘区；返回码层面标量/块不一致 ⇒ 同一套错误处理代码在两个入口行为不同
- 定级：**S2**（缺生产触发路径：真实链路 `module_adapters.cpp:10015 build_adaptive` 之后如何取 δ 未逐跳读完，见 §2.4；且需实测是否存在越域采样点——`max_extrapolation_deg=0` 时边缘点必然越域）
- 怎么算修好：声明层给出非数哨兵（越域点写 `NAN` 而非 0，或 `out_values` 置不可变并强制 `out_status` 非空即 rc=1）；统一标量/块的返回码约定（同一条件同码）；若保留 0 语义，头须写明"0 表示未改正"并给发布面计数（越域点数）以便门判
- 置信：CONFIRMED（代码与头两侧均逐字读到）
```

```
### CR-47-18 · 【已撤回——取到判决式后不成立】头 :251 的"残差仍受表示能力限制 ⇒ 细化 h"承诺在实现里有判决式
- 轴：D
- 位点：`sky_plane.h:251` `//   · 判绿且残差仍受表示能力限制（细化到 h/2 后 chi2_red 至少降到 r 倍）⇒ **细化** h。`
- 原指控（登记时状态 PARTIAL）：只读到 `:1454-1455/:1469-1470` 的 mark 序列，未见 `residual_improve_ratio` 的比较式，疑该判据是装饰。
- 后续证据（通读文件 3 时在同一函数体内取到，`sky_plane.cpp:1445-1465`）：
  `1445 // 表示收敛探针：细化到 h/2，chi2_red 至少降到 r 倍才认为残差仍受表示能力限制。`
  `1451 const bool improved = (rc2 == P2_SKY_PLANE_OK) && (info.chi2_red > 0.0) &&`
  `1452                       (i2.chi2_red <= ad.residual_improve_ratio * info.chi2_red);`
  `1453 if (improved) { … ++n_refine; … if (h <= ns.lower_deg) rep_limited = 1; }`
  ⇒ 判据确实参与判决，头的两句（:251 与 :324-327）与实现同侧。**本条撤回，不计入本批指控数。**
- 但同一处暴露两条独立问题，分别另立：① 细化探针被内存门挡死时与"判据不需要细化"走同一采纳分支 ⇒ 记在文件 3 的 **CR-47-23**；② `improved` 要求 `info.chi2_red > 0.0`，χ²_red 为 0 的退化输入 ⇒ 永不细化，属方向未证的一角（本批未追 χ²_red=0 的可达性，登记为需实跑项）。
- 定级：撤回
- 置信：CONFIRMED（撤回依据为逐字读到的判决式）
```

```
### CR-47-19 · `P2_SKY_ADAPT_REPRESENTATION_LIMIT = 4` 声明后全仓零赋值：头的"诚实边界"档在 action 面上不可达
- 轴：D
- 位点：`sky_plane.h:266` `P2_SKY_ADAPT_REPRESENTATION_LIMIT = 4,  // 已到分辨率极限下界而残差仍在下降（诚实边界）`
- 现状：`grep -rn "P2_SKY_ADAPT_REPRESENTATION_LIMIT" lib/ eng/ --include=*.cpp --include=*.h` → **仅命中本头 :266**；实现实际写出的 action 只有 0/1/2/5（`sky_plane.cpp:1342/1415-1418/1440/1454-1455/1469-1470`）
- 上位依据：§7a 的"诚实边界"由字段 `representation_limited` 承载（`sky_plane.cpp:1369` 赋值 → manifest `module_adapters.cpp:10023 sky_plane_representation_limited`），实现侧走的是字段不是 action
- 差在哪：枚举里留了一个永不产出的档，头又说 action 是"本次尝试后所走方向"（:285）⇒ 读声明面以为存在一条 `action=4` 的判决，审计脚本按 action 分类时该类恒空
- 后果：provenance 消费方（审计/门）按 `attempts[].action` 统计"表示能力到顶"会得 0 次，而事实记在另一个字段里——同一事实两个编码位，一个是死的
- 定级：**S3**（不改产物字节；改的是可审计性与声明准确性）
- 怎么算修好：把 `=4` 从枚举删掉并在注释里指向 `representation_limited` 字段；或让 :1415 的"网格不可行"分支在到界时真写 4（则头注释与 `n_attempts` 语义要同步）
- 置信：CONFIRMED
```

```
### CR-47-20 · `P2SkyPatchConfig` 自称"与 sampler 的 patch estimator 同一数学定义"，实测非法值修补规则与 sampler 不同一套数
- 轴：C（主）+ S
- 位点：`sky_plane.h:90-91` `// 局部稳健背景估计（纯函数，无 I/O；与 sampler 的 patch estimator 同一数学定义：` / `// median + 1.4826*MAD 尺度 + 亮端 σ-clipping + 保留比例门）。`
- 现状（同一键的修补落点并排）：`sky_plane.cpp:249` `if (cfg.min_samples < 1) cfg.min_samples = 1;` 对 `sampler.cpp:521` `if (cfg.min_samples < 1) cfg.min_samples = 5;`；`sky_plane.cpp:250` `clip_iters < 0 → 0`（即"关掉裁剪"合法）对 `sampler.cpp:505` `background_clip_iters <= 0 → 3`；`sky_plane.h:93-97` 的注释只写"默认 X"，未写各自的非法值落点
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:505-507` 把 sampler 侧的 `|ret| ≥ min_samples ≥ 1` 当作"N_retained=0 不可达"的论证前提——**该论证依赖修补落点是 5 还是 1**，两文件取不同值时文档只覆盖 sampler 一侧
- 差在哪：同一个数（patch 最小样本）在"同一数学定义"的两个实现里非法落点一个是 5 一个是 1；`clip_iters=0` 在 sky 侧合法（不剪亮端）、在 sampler 侧非法（回退 3）
- 后果：sky_samples 与 control 观测的 σ_bg 口径不再同源——而 sampler.h:51/sky_plane.h:104 用的是同一个方差式；下游按"同一 estimator"把两类样本混拟合（UPM/sky 联合）时，N=1 的 patch 与 N=5 的 patch 被同一 `k_corr=1.4` 折算
- 定级：**S2**（`p2_sky_patch_estimate` 当前登记为类 A 未接入生产调用图，见 `eng/ci/ledgers/prod_wiring.json:344-352`；接上即 S1，条件=生产链任一处用本函数替代 sampler 内联估计）
- 怎么算修好：把两族修补规则抽成一份共享常量表（或在头里逐字段写"非法 ⇒ ?"）；`clip_iters=0` 若是有意的"关裁剪"，头要写"0=不裁剪（不同于 sampler 的 <=0→3）"
- 置信：CONFIRMED
```

```
### CR-47-21 · `n_masked` 把三类不同成因混成一个计数，且头 :30-31 的 fail-closed 清单不含"输入非有限"
- 轴：C（主）+ R
- 位点：`sky_plane.h:363` `std::uint64_t n_masked;       // 被掩膜/低支持剔除的点`；`sky_plane.h:30-31` `// fail-closed（11_upm.md §4.5）：无采样点 / 全被掩膜 / 点不足 / 帧欠定 / 节点数超上限 / **未通过唯一可辨识性判据** / 非有限解 一律显式失败`
- 现状：`sky_plane.cpp:535` flags 命中 bad ⇒ `++n_masked`；`:536` **ra/dec/value 非有限** ⇒ 同一计数；`:538/:540` variance 或 snr 非正/非有限 ⇒ 同一计数；三者都直接进"点可用集"筛选，只有结果集为空/不足才失败（:545/:550）
- 差在哪：头声明的计数语义只覆盖前一类（掩膜/低支持），实现把"上游给了 NaN/非法值"也并进同一列；头的 fail-closed 清单里"非有限解"指解，输入非有限既不失败也不单列
- 后果：`n_used`/`n_masked` 是 UPM 与产品 manifest 的采样面读数；上游 sampler 若因 CR-47-11 那类免检分支产出坏点，本面把它当"被掩膜"消化掉，坏数据不可分辨（少报问题、多报掩膜率）
- 定级：**S3**（无字节影响，属发布口径混污；若下游用 n_masked 判覆盖门则升 S2）
- 怎么算修好：拆计数（`n_masked_flags` / `n_invalid_input`），或在 `n_invalid_input>0` 时按 :30-31 的 fail-closed 取向显式失败；`DATA_SEMANTICS` 与 `11_upm.md §4.5` 同步补口径
- 置信：CONFIRMED
```

```
### CR-47-22 · `P2_SKY_ADAPT_MAX_ATTEMPTS=12` 无导出：运行期默认 6，数组富余 2 倍无出处，且溢出保护未证
- 轴：S（声明面）
- 位点：`sky_plane.h:289` `enum { P2_SKY_ADAPT_MAX_ATTEMPTS = 12 };`；配套 `:321` `int    max_attempts;              // 总尝试上限（含首次）；<=0 → 默认 6`
- 上位依据：条文面零提及；`sky_plane.cpp:1264` `a.max_attempts = 6;              // 总尝试上限（含首次与探针）`
- 差在哪：12 是"上限的上限"，头与 `PHASE2_UPM.md §7a` 都没说为什么是 12 而不是 6（=默认）或 8（=refine4+coarsen4+首次+探针）；`attempts[]` 固定数组按 12 分配，用户可把 `max_attempts` 配到 >12，本批未读到写界保护 ⇒ 若缺保护是越界写
- 后果：provenance 结构体尺寸（约 12×~88B）随头变化即 ABI 断；若允许配置超界而无数位钳制，`out_report.attempts[i]` 可越界
- 定级：**S3**（需实跑/续读才能定是否越界；单测面 `eng/tests/unit/v6_p2_sky/v6_p2_sky_node_adapt_test.cpp` 未读）
- 怎么算修好：把头注释写成"数组容量 = max(refine+coarsen 上限)+首次+探针，超出即拒（rc=…）"并给钳制行位；或让容量由配置导出
- 置信：PARTIAL（缺实现侧写界证据）
```

### 2.3 不报的核过项（附检索式）

| 核过项 | 为什么不算缺陷 | 检索式 |
|---|---|---|
| 节点间距导出的全部公式（min、½、max(sample_pitch, pixel/3600)、`2h`、上界<下界 ⇒ UNSUPPORTED） | 头 §2a（:149-186）与实现 `:417-448` 逐式一致，且**确实按式算**（不是写死），失败不回退常数 | `sed -n '392,448p' sky_plane.cpp` |
| `node_spacing_deg=0` 的"未给出"哨兵与 `GEOMETRY_REQUIRED` 显式失败 | 头 :206-209/:418-419 与实现 :487-512 同侧；:494 还先算出搜索区间只读不改调用方值 | 同上 |
| 零初始化的 `spline_degree=0` ⇒ 1 | 0∉{1,3}，属非法值修补（与 CR-47-13 的头注释错值分开，不另计） | `:484` |
| `weight_mode=1` 时负 SNR 被平方成权重（本应是最典型的 R 轴陷阱） | 方向钉死后不成立：`:540` 在进 used 前要求 `finite_pos(snr)`，未知点被剔除而非参与，且计入 n_masked（该计数口径另立 CR-47-21） | `sed -n '531-541p' sky_plane.cpp` |
| `NO_LOCAL_SNR` 标志不参与剔除 | 头 :59"snr 用帧级回退"与 :64"缺帧内时=帧级"自洽 | `:533-534` |
| 5 个 `unreachable` 的 sky_plane 符号（`p2_sky_patch_estimate`/`p2_star_mask_caps`/`_contains`/`p2_sky_plane_residuals`/`_normal_matrices`/`_gain_dc`） | 已由 `eng/ci/ledgers/prod_wiring.json`（:322/:344/:399/:421/:454）按 PUBLIC_API.md「C ABI 声明面 ≠ 三命令生产调用面」类 A 口径**登记并给出 exit_condition**，§4 免报；其证据链落 `run/WIRING-W16-01/…` 属 Stage A 已合案的"证据落 run/"母题，本批只补位点不另立案 | `grep -n "unreachable:header.P2_API:p2_sky" eng/ci/ledgers/prod_wiring.json` |
| `rank_rtol` 与 `identifiability.h` 的同函数/同值主张（头 :32-33/:218-222） | 头给的判据式 `r_eff==n_free ⟺ κ<1/τ` 与本批独立推导一致（λmin>τ·λmax ⇔ κ<1/τ），不等号方向与 :393 的红色一侧互补无空洞（除 κ==1/τ 的零测度边界） | 读 :33/:218/:393 三处 |
| `h_px = h_deg·3600/scale` 的 3600 | §4 单位换算定义常数 | — |
| `m=(p+1)(p+2)/2`（:18）、`n_params_full = n_free + (n_frames−1)·m`（:339） | 多项式基维数与 Schur 消元计数的定义式 | — |

取证用量：本文件 15 次工具调用（前言上限 20 内）。

## 3. `lib/algorithms/coverage/include/astro/phase2/stage2_common.h` —— 已读：是（表 29 项）

本文件的"三方"取到的实体（比前两文件多一面）：声明头（**内含实现**：:253-580 是 inline 函数体，故 R/D 轴可直接在本文件取证）｜ parser `lib/algorithms/coverage/src/stage2_common.cpp:38-210` ｜ 工具入口 `lib/algorithms/coverage/tools/stage2.cpp:471-568` ｜ 调度器入口 `lib/infrastructure/scheduler/src/module_adapters.cpp:9815-10023`（:9924 调用本头 inline 函数）｜ 配置合同 `docs/development/CONFIG_SCHEMA.md:23-58` ｜ **逐字段登记面 `eng/packaging/config/config_registry.json`**（:1863-1883 起把 `sky_plane.enabled`、`smoothing_lambda` 标为 `registration:"none"` / `finding:"unregistered"`）｜ 权威 `docs/science/REJECTION.md:23/44-53`、`docs/science/PHASE2_UPM.md §7a`、`docs/algorithms/PHASE2_UPM_IMPL.md:378-390`、`ASTROCS_DESIGN.md §3.1:173-175`、`docs/science/PSF_SIGNAL_WEIGHT.md §4:72`。

### 3.1 §2.5 数值处置表（stage2_common.h）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :30 | `P2_SMOOTHING_LAMBDA_AUTO` | 0.1 | 字符串键 `smoothing="auto"` 解析成的 λ | **待确认（两侧打架，见 CR-47-26）** | 本头 :23 引 `CONFIG_SCHEMA.md:19`「smoothing(auto→0.1)」与本头同值；`docs/algorithms/PHASE2_UPM_IMPL.md:390` 对同一 λ 写 **0.0（默认关闭平滑）** 且自述"冻结面"；`config_registry.json:1878-1879` 记「键缺省时编译期默认 **0.0**；`P2_SMOOTHING_LAMBDA_AUTO` = 0.1 仅在 auto 路径生效」标 `registration:none/finding:unregistered` | 保守=0.0（不平滑，残差不被掩盖⇒偏红）；影响范围＝UPM 解的平滑度与 χ² | CONFIRMED（三侧原文均读到） |
| :34 | `exec = default_execution_options()` | — | CON-002 全局执行预算载体 | 不适用（对象初始化） | `execution_options.h:20` `memory_budget_bytes = 0; // 0 => 由配置 memory_limit_mb 决定` ⇒ 预算单一来源仍是 config | — | CONFIRMED |
| :37 | `target_order` | -1（=auto） | 目标 HEALPix 阶哨兵 | 结构性不适用（哨兵） | parser `stage2_common.cpp:20-36`（"auto" 或整数，越界报 `必须在 0..29`）与调度器同口径 | — | CONFIRMED |
| :39-52 | sampler 默认的第二份拷贝 | 8 / 2 / 5 / 0.05 / 8 / 3.0 / 3 / 0.20 / 3.0 / 0.60 / 3.0 / 2 / 1 | `model` 块 13 键在本 struct 里的镜像默认 | 待确认（**逐值与 sampler.h/parser/CONFIG_SCHEMA 同数**，但它是"同一默认值的第二处字面量"） | `sampler.h:35-48` ↔ `sampler.cpp:304-316` ↔ `stage2_common.cpp:40-126` ↔ `CONFIG_SCHEMA.md:25-32` 四侧同值；本头 :56-57 自引纪律「CONFIG_SCHEMA.md:3-4『C++ struct 默认值、parser 默认值…必须一致』」 | 保守=不复制（改为调 `p2_sampler_default_config()`）；影响范围＝任一处单独改动即分叉（CR-47-3 已见同类分叉） | CONFIRMED |
| :53 | `robust_loss` | 0（=huber） | UPM 稳健损失选择 | 待确认（唯一合法值） | parser `:157-162` 只接受字符串 `"huber"`，其余硬错误 ⇒ 键存在但不可选；`CONFIG_SCHEMA.md:34` 列 `robust_loss(huber)` | — | CONFIRMED |
| :54 | `snr_weight_mode` | 0（=snr2_normalized） | 权重口径整数域残留 | **待确认（键名与合同相反，见 CR-47-24）** | parser `:163-168` **读 `snr_weight_mode`** 且只接受 `"snr2_normalized"`；`CONFIG_SCHEMA.md:35` 写 `upm_weight_source(snr2_normalized) # 原 snr_weight_mode，（已按 §9.73 A44 作废：键不存在；权重是派生量）`；`grep -rn "upm_weight_source" lib/ eng/` → **零命中** | 保守=按合同改现行键名并让退役键硬错误 | CONFIRMED |
| :55 | `huber_delta` | 1.345 | UPM Huber δ | 文献值 | Tukey 95% 效率常位数；`CONFIG_SCHEMA.md:33` 同值；`PHASE2_UPM_IMPL.md:384`「huber_delta \| 1.345（无量纲，单位=sigma_eff）\| upm.h:74；upm.cpp:254/:273」 | — | CONFIRMED |
| :58 | `smoothing_lambda` | = :30（0.1） | struct 侧 λ 默认 | 待确认 | 同 :30 | 同 :30 | CONFIRMED |
| :59 | `zero_anchor_weight` | 1e-3 | 零锚点权重 | 待确认 | parser `:152-153` 同值；`PHASE2_UPM_IMPL.md:380-381` 记「两处生产组装 `p2_session.cpp:199-211` 与 `module_adapters.cpp:7795-7813` 均显式赋 zero_anchor_weight=1e-3、sigma_floor=1e-3」⇒ 三侧同值；**导出依据未在本头给出** | 保守=更小（锚更弱） | CONFIRMED（值一致，出处缺） |
| :60 | `sigma_floor` | 1e-3 | σ 下限保护 | 待确认 | parser `:169-173`（>0 硬校验）同值；同上组装面 | — | CONFIRMED（值一致，出处缺） |
| :61 | `support_power` | 1.0 | support 的幂指数 | 待确认 | parser `:174-178`（>=0）同值；`CONFIG_SCHEMA.md:34` 列 `support_power(1.0)` | 1.0=线性用 support；**注意 `FZ-GATE-SUPPORT-COVERAGE`（sampler.h:165）禁止 support 冒充权重**，本参数正是把 support 乘进权重的自由度 ⇒ 方向为"越接近 0 越不依赖 support" | CONFIRMED（值三侧一致；语义张力见 §3.3） |
| :64 | `use_ivar_weight` | 1 | ivar vs legacy snr² 权重开关 | **待确认（与 §3.1:175 冲突，见 CR-47-25）** | parser `:179-180` 读该键（缺省 1）；调度器硬编 `module_adapters.cpp:9585 uc.use_ivar_weight = 1; // production（SCI-UPM-WEIGHT-001 冻结）`；`ASTROCS_DESIGN.md §3.1:175`「权重的产生链固定为两步、**没有可选择项**」 | 保守=1（唯一口径）；影响范围＝UPM 权重面 | CONFIRMED |
| :65 | `max_irls_iterations` | 100 | UPM IRLS 上限 | 待确认（有合同、无导出） | parser `:154-155` 同值；`CONFIG_SCHEMA.md:34` `max_irls_iterations(100)`；`PHASE2_UPM_IMPL.md:385`「max_iterations \| 100」⇒ 三侧同值；无收敛性导出 | 保守=更小（早停⇒如实登记未收敛） | CONFIRMED |
| :66 | `tolerance` | 1e-6 | UPM 收敛门 | 待确认 | parser `:156` 同值；`CONFIG_SCHEMA.md:34` `tolerance(1e-6)`；`PHASE2_UPM_IMPL.md:386`「tolerance \| 1e-6」⇒ 三侧同值；**与 sky_plane 的 1e-10 是两个不同阶段的同名门**（见 CR-47-13/16 语境） | — | CONFIRMED |
| :71-74 | `sky_plane_enabled` | **true** | 稀疏天光面链总开关 | 待确认（**注释与初始值相反，见 CR-47-29**） | 本头 :71「缺省关闭：保持 RELEASE-01 加性 C 场行为不变（基线 447 不回归）」对 :74 `= true`；调度器 `module_adapters.cpp:9837-9839` 缺省取 `delta_wanted`，而 `additive_mode` 缺省 `"delta"` ⇒ 生产默认亦为真；`config_registry.json:1863-1871` 记该键 `registration:none`、`declared_default:"随 additive_mode ∈ {delta, both}"` | 保守=显式写进 config 而非靠默认 | CONFIRMED |
| :75 | `sky_plane_spline_degree` | 3 | 天光面样条阶（工具入口） | 待确认 | 与 `sky_plane.cpp:451`（3）同、与 `sky_plane.h:204`（"默认 1"）**反**；parser `:187` 读 `sky_plane.spline_degree`；调度器 `:9868` 内联默认 3 | 见 CR-47-13 | CONFIRMED |
| :81 | `sky_plane_node_spacing_deg` | 0.0 = 未给出 | §7a 禁标定常数的哨兵 | 公式导出（导出量） | :76-80 引 `PHASE2_UPM 7a:185-189` 并交代旧值 1.0 被废的理由（M42 上界 0.0752 度，粗 13.3 倍）；实现 `sky_plane.cpp:499-512` 不回退常数 | — | CONFIRMED |
| :87 | `sky_plane_pixel_scale_arcsec` | 0.0 = 从产品取 | 像素角尺度缺省取源 | 不适用（哨兵 + 来源声明） | 调度器 `:9882-9890` 读 `<hips>/p1_stack.json` 的 `finest_input_arcsec`；本头 inline 函数 :327-332 对非正/非有限显式失败 | — | CONFIRMED |
| :88-90 | `sky_plane_max_nodes` | 8192（注释自陈"默认 2048 会让自适应 fail-closed 返回 rc=5；M42 上 h=0.0376 度需要 4814 个节点"） | B_ref 系数内存护栏 | **待确认（本文件最高危，见 CR-47-23）** | `sky_plane.cpp:467` 的 `default_config` 是 **2048**；`module_adapters.cpp:9998` 仅当 config 写 `sky_plane.max_nodes` 才覆盖，而 `grep -rn "max_nodes" lib/algorithms/coverage/configs/ eng/packaging/config/ --include=*.json` → **零命中**；工具入口 `stage2.cpp:486 spc.max_nodes = cfg.sky_plane_max_nodes;` ⇒ 8192 只在工具链生效 | 保守=更大（放行细化⇒h 更细⇒表示能力更强，代价内存）；影响范围＝最终 h、B_ref 系数、扣除值与接缝 | CONFIRMED（位点全核，M42 节点数为该头自引的 run/ 实测） |
| :91-93 | `sky_plane_gradient_order/gauge_mode/weight_mode` | 1 / 0 / 0 | 天光面三个模型口径 | 待确认 | parser `:190-193` 读同名字段（缺省=struct 默认）；`sky_plane.h:211/215/216` 注释同值；`sky_plane.cpp:455/459/460` 同值 ⇒ 四侧一致，**但 `CONFIG_SCHEMA.md` 无 `sky_plane` 节**（`grep -n "sky_plane" docs/development/CONFIG_SCHEMA.md` 零命中） | 保守=0/0（inverse_variance + reference_frame） | CONFIRMED |
| :97 | `sky_plane_rank_rtol` | 1e-10 | FZ-AP2S-RANK-RTOL 唯一判据阈值 | 公式导出（浮点精度地板） | `sky_plane.h:218-222`、`identifiability.h`；parser `:194` 读；调度器 `:9980` 读 | 方向：τ 变大⇒更易判红（保守） | CONFIRMED |
| :99 | `frame_gain_enabled` | true | 乘法响应 g_k 接入 | 待确认 | parser `:195` 读 `sky_plane.frame_gain`；调度器 `:9842` 同键 | 保守=false（不施增益，g=1） | CONFIRMED |
| :101 | `precision` | 0（=fp32） | 积分精度档 | 结构性不适用（枚举） | parser `:200-206` 只接受 fp32/fp64；`CONFIG_SCHEMA.md:36` `precision(fp32)` | — | CONFIRMED |
| :102 | `memory_limit_mb` | 24576 | 单阶段内存预算上限 | 待确认 | parser `:207-211` 同值（>=1 校验）；`execution_options.h:20` 说 0 ⇒ 由本键决定；`eng/contracts/resource_gate_v1.json:95` `"no_gate": ["memory","cpu","threads"]`（内存无门）⇒ 本字面量是唯一实际天花板，**而 `AGENTS.md §3` 记本机物理内存约 23 GiB、swap 永久禁用** | 保守方向=按机器物理量的 ≤80%（≈18 GiB 量级）；影响范围＝并发帧数×每帧 ~1.67 GB 的真实 OOM 风险（会连带杀掉 DSH 单元，见 AGENTS §3） | CONFIRMED（无跟踪锚；值与本机内存矛盾） |
| :103 | `reject_method` | `P2_REJECT_AUTO` | 排异方法路由档 | 不适用（枚举） | `REJECTION.md:44` 「method 合法且 != AUTO 才进 kernel」 | — | CONFIRMED |
| :110 | `reject_profile` | `P2_PROFILE_ASTROCS_ADAPTIVE_PIXEL` | 生产默认排异档 | 文献值（外部档界）+ 本仓裁决 | `REJECTION.md:23/53` 同值并标"生产默认，ACSD 自研 / wbpp_2_9_1 为对照档，档界取自 WBPP **2.5.9** bestRejectionMethod"；`CONFIG_SCHEMA.md:39`；`eng/contracts/data/phase2_uncertainty_rejection_provenance_v1.json:67`（本头 :106-107 引）；**同文档 :53 自陈"其 n>15 档本仓取 linear_fit 与该版不符，见 §5"** | — | CONFIRMED（偏差已由权威文档如实登记，不另立案） |
| :116 | `reject_underdetermined_n` | 0（=按 profile 解析） | 欠定档哨兵 | 公式导出（派生自 resolver） | `REJECTION.md:45-48` 实测：`astrocs_adaptive_pixel∧AUTO ⇒ 3`、`extreme_value_clip_prior_sigma ⇒ 1`、其余 profile ⇒ 2；`CONFIG_SCHEMA.md:40` 同；本头 :111-115 引 `rejection.h:230-234` 同口径并说明旧默认 2 是"第二份拷贝" | — | CONFIRMED（三侧一致，不报） |
| :118-119 | `reject_normalization` / `_floor` | `astrocs_median_center_v1` / 1e-12 | 判定工作域与除零地板 | 待确认（normalization）；结构性不适用（floor） | `CONFIG_SCHEMA.md:41-42` 同值；`rejection.h:119-122` 枚举；`REJECTION.md:51` 说明"近零天光 median→0 ∧ scale=\|median\| ⇒ 带宽→0（§8a）"⇒ 1e-12 是这条退化路径的数值保护 | 保守=更大 floor（带宽不塌到 0） | CONFIRMED |
| :122-125 | `large_scale_*` | false / 8 / 2 / 2 | 大尺度拒绝开关与生长半径 | 待确认（8/2/2 无导出） | `CONFIG_SCHEMA.md:43-45` 逐项同值；本头 :120-121 自陈"默认关闭 = WBPP largeScaleClipLow/High 默认一致"（外部实现值，本批未核 PixInsight 一手） | 保守=关闭（现状） | PARTIAL（缺 WBPP 一手出处核对） |
| :127-135/143-145 | `sigma/winsor/avg/medsig` 四组 | lower 4.0 / upper 3.0 / iters 8（同族四份拷贝） | σ 族排异阈 | 文献值（WBPP 映射） | `CONFIG_SCHEMA.md:46-52` 四组逐项同值并标 method-specific；`REJECTION.md` §5 档界；合并位点：:127-129、:130-132、:133-135、:143-145 | 保守=更宽（少排） | CONFIRMED |
| :136-138 | `linfit_*` | 5.0 / 3.5 / 8 | 线性拟合排异上下阈 | 文献值 | 本头 :136-137 标 "WBPP Light linearFitLow/High"；`CONFIG_SCHEMA.md:49` 同值 | — | CONFIRMED（值）/PARTIAL（一手 WBPP 版本未核） |
| :139-140 | `esd_alpha` / `esd_max_outliers` | 0.05 / 10 | 广义 ESD 显著性与上界 | 文献值（NIST ESD） | `CONFIG_SCHEMA.md:50` 同值；`rejection.h:25-26` 声明 Oracle = NIST ESD | — | CONFIRMED |
| :141-142 | `pct_low/high_fraction` | 0.2 / 0.1 | 百分位带 | 文献值 | 本头标 "WBPP Light percentileLow/High"；`CONFIG_SCHEMA.md:51` 同值；`REJECTION.md:51` 记该档在 n=4 可达域内的危害 | — | CONFIRMED |
| :146-148 | `minmax_*` | 1 / 1 / 4 | 最小保留计数 | 结构性不适用（计数上界，非科学量）——但 min_kept=4 与排异"至少 4 样本才可判" 有科学含义，故按 §4 不豁免 | `CONFIG_SCHEMA.md:53-54` 同值 | 保守=更大 | CONFIRMED |
| :149 | `rcr_technique` | `ss_median_dl` | RCR 技术名 | 文献值 | `rejection.h:26-27`「Siril 1.4.3（GPL ORACLE ONLY）、RCR 2.4.7 官方固定版本」；`CONFIG_SCHEMA.md:55` 同值 | — | CONFIRMED |
| :163 | `acr_route` | `auto` | ACR 路由 | 不适用（已由 :181-184 论证恒 false） | 本头 :179-184 + `stage2_common.cpp:511-523` `(void)cfg;` 全形参弃用、恒 false；`ASTROCS_DESIGN.md §2`「ACR 生产不可达」 | — | CONFIRMED（不报） |
| :166 | `diagnostics` | true | 诊断产出开关 | 不适用（发布面开关） | 本批未追消费者 | — | PARTIAL |
| :219-233 | `P2SkyPlaneGeometryInputs` 各指针默认 | nullptr / 0 | 列视图入参 | 结构性不适用（指针初值） | :311-332 逐条显式失败，无静默 | — | CONFIRMED |
| :255 | `kDegPerRad()` | 57.29577951308232 | 弧度→度 | 结构性不适用（单位换算定义常数；本批复算 180/π=57.29577951308232 逐位一致） | — | — | CONFIRMED |
| :270 | `tangent_uv` 退化阈 | 1e-12（\|cosc\|） | 切平面奇异性保护 | 待确认（**退化时返回 u=v=0，见 CR-47-28**） | 实现即本头 :270 | 保守=显式失败（与 :303「不回退任何标定常数」同取向） | CONFIRMED |
| :284-286 | §5d① 自校准栅栏乘数 | 恒 1 | 同/异指向切分判据 `gap > spread` | 公式导出（引 §5d①） | 本头 :211-214 与 :284-286 两处一致陈述；**判别力在 n=2 时为零，见 CR-47-27** | — | CONFIRMED |
| :346 | 最小帧数 | `nf < 2` 失败 | 几何导出的输入下限 | 结构性不适用（算法下限） | 与 CR-47-27 的退化域直接相邻 | — | CONFIRMED |
| :510-511 | 稳健宽度分位 | 0.02 / 0.98 | 重叠带宽的 2%–98% 修剪 | 待确认（头 :215 自称"2%–98% 的稳健宽度"，无出处） | 无条文：`grep -n "2%–98%\|0.02\|0.98" docs/science/PHASE2_UPM.md`（本批未取到该条） | 保守=更宽（如 5%–95% 会更小⇒h 更细⇒偏保守的红） | PARTIAL |
| :493 | 角平分点纬度 | `(...)/2.0` | 连线方向切平面原点 | 公式导出（两点正弦纬度的算术平均，非球面中点） | 与 :489-491 的经度式（三维向量和方向）不匹配：经度用**向量平均**、纬度用**标量平均** ⇒ 两点纬度差大时原点不在大圆中点上（见 CR-47-30） | 影响范围＝连线方向 e 与投影宽度 | CONFIRMED |

### 3.2 findings（stage2_common.h）

```
### CR-47-23 · 本头自证"默认 2048 会让自适应 fail-closed 返回 rc=5"，却只在工具链把上限抬到 8192：生产编排链仍取 2048，且没有任何 config 能抬它
- 轴：C（主）+ R
- 位点：`stage2_common.h:88-90`
  ```
  // B_ref 系数上限（内存护栏）。依据同生产编排：M42 上科学上更优的
  // h = 0.0376 度需要 4814 个节点，默认 2048 会让自适应 fail-closed 返回 rc=5。
  int    sky_plane_max_nodes = 8192;
  ```
- 上位依据：`docs/science/PHASE2_UPM.md §7a`（本头 :194-197 引原文「节点间距必须由输入自适应导出…禁止在配置里留一个『默认节点间距』标定值」）——**上限本身不在 §7a 的禁止范围内，但它决定自适应回路能走到多细**，§7a 的"自适应"承诺因此被这个数截断
- 现状（逐跳）：① `module_adapters.cpp:9867` `P2SkyPlaneConfig spc = p2_sky_plane_default_config();` → ② `sky_plane.cpp:467` `c.max_nodes = 2048;` → ③ `module_adapters.cpp:9998` `if (sp_cfg.contains("max_nodes"))` 是唯一抬升途径，而 `grep -rn "max_nodes" lib/algorithms/coverage/configs/ eng/packaging/config/ --include=*.json` **零命中** → ④ `sky_plane.cpp:647-652` `if (n_nodes > cfg.max_nodes) … return P2_SKY_PLANE_TOO_MANY_NODES;` → ⑤ 细化探针 `sky_plane.cpp:1449-1452` `improved = (rc2 == P2_SKY_PLANE_OK) && …` ⇒ 探针一旦 rc=5 就 `improved=false` → ⑥ `:1467-1476` 采纳较粗解并 `break`
- 差在哪：本头注释把 2048 判为"会让自适应 fail-closed"，然后把 8192 写进一个**只有工具入口读取**的字段（`stage2.cpp:486`）；生产编排链仍按 2048 运行。更关键的是：细化被内存门挡住时与"判据认为不需要细化"走**同一条采纳分支**，`attempts[].rc=5` 虽被记录，但 `representation_limited` 只在 :1464（到达分辨率下界）时置位 ⇒ 最终 h 到底是判据给的还是门截的是不可分辨的
- 后果：同一份输入，工具链得到 h=0.0376（4814 节点）、生产链得到 h=0.0752（约 1204 节点）的 B_ref；`p2_sky_plane_eval` 的扣除值、`delta_k`、接缝改正随之不同 ⇒ 交付字节与"节点间距自适应"的对外主张按入口分叉
- 定级：**S1**（触发路径逐跳已写；数值后果的量级依赖本头自引的 `run/UPM-NODE-ADAPT-01` 实测）
- 怎么算修好：①把 `max_nodes` 与 h 的关系写成一个派生量（由内存 profile/lease 推 node 上限），而不是两处字面量——`AGENTS.md §6`「不硬编码…由 benchmark 生成的 profile 决定」；②两个入口共用同一默认（`module_adapters` 从 `default_config` 起，工具从 struct 起，先把两处统一到一个来源）；③`shipped configs` 或 `config_registry.json` 登记该键；④若判"门截断"必须可分辨，`sky_plane.cpp:1467-1476` 分支要另置一个 `refinement_blocked_by_memory` 位并写进 manifest（改此位要同改 `v6_p2_sky_node_adapt_test.cpp` 期望与 `DATA_SEMANTICS` 的产品字段表）
- 置信：PARTIAL（缺：真实产品上 1204 vs 4814 的节点数对应关系不在跟踪面；本批禁实跑，"节点数 ∝ h⁻² 故 h 减半节点 ×4"一步是我自己的推导）
```

```
### CR-47-24 · 配置合同判"已作废：键不存在"的 `snr_weight_mode` 仍被 parser 读取并生效，而合同给出的现行键 `upm_weight_source` 全仓零实现
- 轴：C
- 位点：`stage2_common.h:54` `int snr_weight_mode = 0;`；头 :150-156 的退役声明「原 legacy 整数权重模式域 int weight_mode{0,1,2} 与其字符串 token（auto/ivar/equal/support_x_snr2）**已删除**」
- 上位依据：`docs/development/CONFIG_SCHEMA.md:35` `support_power(1.0) robust_loss(huber) upm_weight_source(snr2_normalized)  # 原 snr_weight_mode，（已按 §9.73 A44 作废：键不存在；权重是派生量）`
- 现状：`stage2_common.cpp:163-168` `const std::string sw = m.value("snr_weight_mode", std::string("snr2_normalized")); if (sw != "snr2_normalized") { *err = "unsupported snr_weight_mode: " + sw; return false; }`；`grep -rn "upm_weight_source" lib/ eng/contracts/ eng/packaging/ --include=*.cpp --include=*.h --include=*.json` → **零命中**；parser 无未知键拒绝（`stage2_common.cpp` 内无白名单/未知键检查，`grep -n "unknown\|未登记\|硬错误" stage2_common.cpp` 仅命中 `contains("model")`）
- 差在哪：合同说 X 键不存在、代码只吃 X；合同说 Y 是现行键、没人读 Y。写 Y 的 config 被静默丢弃（且值任意），写 X 的 config 被当合法处理
- 后果：审计者按合同改 config 后，其权重口径设置**完全不生效**且没有任何报错；反之按退役键写的旧 config 仍生效 ⇒ "口径唯一"的对外主张在配置面上不可执行
- 定级：**S2**（当前两侧取值都是唯一的 `snr2_normalized`，故暂不产错值；一旦第二口径出现即变成真选择）
- 怎么算修好：parser 改读 `upm_weight_source`，并对 `snr_weight_mode` 显式硬错误（与 :56「出现即硬错误」的旧顶层键同一做法）；`stage2_common.h:54` 字段随之删除或改名；同步 `module_adapters.cpp:9554 uc.snr_weight_mode = 0;`
- 置信：CONFIRMED
```

```
### CR-47-25 · 头自称按 §9.73 A44 删除了"权重口径选择键"，实际留着两个：`snr_weight_mode` 与 `use_ivar_weight`（后者注释明写可切回 legacy snr²）
- 轴：D（主）+ C
- 位点：`stage2_common.h:62-64`
  ```
  // 1=science weight 用 control_ivar；
  // 0=legacy snr² ablation/诊断。默认 1。
  int use_ivar_weight = 1;
  ```
- 上位依据：本头 :150-152 自己引的条文 —— `ASTROCS_DESIGN.md §3.1:175`「权重的产生链固定为两步、**没有可选择项**」；`docs/science/PSF_SIGNAL_WEIGHT.md §4:72`（本批读原文，:70-76 标题即「单一权重口径（无模式选择）」，式 `w_k = SNR_k² / F_ref,k² ≡ 1/σ_F,k²`）——「没有可选择的口径：不存在口径选择键、口径枚举、口径配置项或口径产物」
- 现状：`stage2_common.cpp:179-180` 从 JSON 读 `use_ivar_weight`（用户可写 0）；`:541` 透传进 UPM 构建配置；调度器 `module_adapters.cpp:9585` 硬编 1
- 差在哪：条文禁的是"口径选择键/配置项"，不区分它叫 ablation 还是 production。本头 :157-162 删的只是 `weight_mode{0,1,2}` 与 `legacy_allow_weight_fallback`，同语义的 `use_ivar_weight` 与 `snr_weight_mode` 存活并且是 config 可写项 ⇒ 清理半贯穿，且本头注释把"已删除"写成了整族结论
- 后果：一条 config 就能把权重从 control_ivar 换成 snr² —— 正是 :158-160 论证为"与 §3.1:173 冲突"而被删掉的那类降级；对外"唯一口径"主张因此不成立
- 定级：**S2**（生产链两处都硬/默认为 1，未观测到 0 的生产路径；缺"真有人配 0"的触发面）
- 怎么算修好：要么按条文删键（ablation 需求改由 `eng/tests/` 侧构造 cfg 直调，不经 config），要么在 `ASTROCS_DESIGN §3.1`/`PSF_SIGNAL_WEIGHT §4` 走变更流程承认"诊断口径可选择"——**二者必须同批**，只删键会让既有 config 报未知键、只改文档不改代码是继续违反
- 置信：CONFIRMED
```

```
### CR-47-26 · 头把一个两侧打架的 λ 写成"单一来源"，并给出一个不在跟踪面也不在磁盘的上呈锚；登记面对该冲突的陈述与头不同
- 轴：C（主）+ S
- 位点：`stage2_common.h:22-24` `// ── CONFORM-FIX-B-009: smoothing 键 "auto" 的唯一解析值（单一来源）──`；`:29` `// 冻结面，冲突已登记上呈（见 reports/RELEASE-02/conform-fix-b.md §009）。`
- 上位依据：`docs/algorithms/PHASE2_UPM_IMPL.md:390`（头引作 :382，**行锚漂移 8 行**）`| smoothing_lambda（λs） | 0.0（默认关闭平滑） | upm.h:75；upm.cpp:255/:286 |`，该表自述"冻结面，任何修改必须走 SCI/合同变更"
- 现状：`git ls-files | grep "^reports/"` 只有三个 v19r2 CSV ⇒ `reports/RELEASE-02/conform-fix-b.md` **既不在跟踪面也不在磁盘**（`ls reports/` 只有 v19r2）；`grep -n "smoothing" docs/algorithms/anchors/unresolved_registry.json` → **零命中**（UNRESOLVED 登记面无此条）；`eng/packaging/config/config_registry.json:1878-1883` 的陈述是「键缺省时编译期默认 **0.0**；`P2_SMOOTHING_LAMBDA_AUTO` = 0.1 仅在 auto 路径生效」且 `registration:"none"` / `finding:"unregistered"`
- 差在哪：头说"唯一/单一来源"，登记面说"两条路径两个值"（auto→0.1，键缺省→0.0）；头说"已登记上呈"，可核对的登记面（unresolved_registry / config_registry）里没有"上呈"这一动作的记录，只有一条 unregistered 事实
- 后果：λ 的两个取值差一个数量级（0 = 关闭平滑），走哪条取决于 config 是否写了 `smoothing` 键 —— 而它的裁决锚（GAP_AUDIT §9.39 A5「λ 不能为 0」）只活在代码注释里；上呈链断在一个不存在的文件上
- 定级：**S2**（生产组装若显式赋 λ 则不产错值：`PHASE2_UPM_IMPL.md:380-381` 记两处生产组装均显式赋值，本批未逐字读到那两段）
- 怎么算修好：把冲突写成 `unresolved_registry.json` 的一条（含两侧原文与"待裁"状态）并给跟踪面锚；头注释的 :382 改 :390；"单一来源"改为"auto 路径的单一解析点"（避免与 0.0 默认混淆）；λ=0 是否合法由负责人裁（AGENTS §10"科学定义有歧义"类）
- 置信：CONFIRMED
```

```
### CR-47-27 · §5d① 自校准栅栏在两帧输入下必然不武装，把"多指向"静默当"单指向"，导出的 h 上界按帧尺寸而非指向间距算
- 轴：R（主）+ D
- 位点：`stage2_common.h:287-294` `selfcal_fence_low_mode`（`for (std::size_t k = sorted.size(); k-- > 1;)`）与 `:464-468` `} else { // 单一指向（自校准判据不武装）… pointing_spacing = prov ? prov->frame_extent_median_deg : 0.0;`
- 上位依据：本头 :211-214「唯一的数据驱动切分用 §5d① 同款自校准栅栏 `gap > spread`（乘数恒为 1…）。判据不武装（样本分布无显著间隙）⇒ 退化为单一指向，此时指向间距取帧角尺寸」；`docs/science/PHASE2_UPM.md §7a`（:194-197 引「节点间距 ≤ (该产品实际约束帧间改正量的最小尺度)/2；该尺度是**重叠带宽度**与**指向间距**两者中的较小者」）
- 现状：切分样本 = 全部帧对的中心距（:400-409），`nf=2` 时 `pairs.size()==1` ⇒ 循环体一次都不执行（`k-- > 1` 从 k=1 起即假）⇒ `return sorted.back()` = 那唯一的间距 ⇒ `fence = sep(f0,f1)` ⇒ 聚类 :421 `sep <= fence` 恒真 ⇒ **两帧必并入同一指向**；随后 :474-476 的指向对循环不执行 ⇒ `band_trim` 空 ⇒ :521-523 `overlap_band = frame_extent_median` ⇒ `constraining = min(extent, extent) = extent` ⇒ `upper = extent/2`
- 差在哪：两帧产品恰恰是"需要区分同指向/异指向"的最小用例，而该判据在此**结构上不可能武装**（一个样本无间隙可言）。:346 又允许 `nf>=2`，即退化域就是输入域的下边界。方向钉死：真实指向间距 `s ≤ extent`，故 `upper_derived = extent/2 ≥ s/2 = 规则要求` ⇒ 导出的 h 上界**偏大（偏粗）**⇒ B_ref 表示能力不足 ⇒ 帧间天光结构不被建模，χ² 却仍按这个弱模型算 ⇒ **偏绿**（残差被欠参数模型摊平，接缝问题不进判据）
- 后果：mosaic 命令的两帧输入（`lib/algorithms/coverage/configs/` 中 `stage2_overlap.example.json`、`stage2_real_overlap.json`、`stage2_real_overlap_cpu.json`、`stage2_t4_true_overlap.json`、`stage2_tiny_memory.example.json` 五份为 2 帧）拿到的 h 与 §7a 规则 1 不符，且 provenance 只有 `n_pointings=1` 与 `frame_separation_fence_deg=该间距`，无"判据不可用"标志
- 定级：**S1**（触发路径逐跳：`mosaic`/工具 → `module_adapters.cpp:9924 p2_sky_plane_geometry_from_controls`（本头 inline）→ :400-409 → :466/:521 → `sky_plane.cpp:417-421` → `derive_node_spacing` `upper=0.5*min(...)` → :10015 `build_adaptive`）
- 怎么算修好：`nf == 2`（或 `pairs.size() < 2`）时不得声称"判据不武装⇒单一指向"，要么显式失败（与 :303「不回退任何标定常数」同取向），要么改用可由观测支撑的判据（如公共控制点数/重叠面积 > 阈值才算同指向）；并给 `P2SkyPlaneGeometryProvenance` 加 `fence_armed` 位；改默认判据要同改 `PHASE2_UPM.md §7a` 的例子与 `v6_p2_sky_geometry_test.cpp` 期望
- 置信：PARTIAL（代码逻辑与可达性 CONFIRMED；"h 偏粗多少倍"需一份真实两帧产品的 extent/间距比）
```

```
### CR-47-28 · 切平面退化分支把"不可求值"写成 u=v=0，声明面未记该哨兵
- 轴：R（对应派单重点④）
- 位点：`stage2_common.h:270` `if (!(std::fabs(cosc) > 1e-12)) { *u_deg = 0.0; *v_deg = 0.0; return; }`
- 上位依据：同头 :303「**不回退任何标定常数**：任一导出量非正/非有限即显式失败」——本分支既非失败也不计数
- 现状：`cosc` 是切平面原点到该点的方向余弦，≈0 即距原点 90°；此时该点被折到 **(0,0)**，与"就在原点"完全同值
- 差在哪：一个不可求值的投影被编码成合法数值 0，调用方（:376-390 的包围盒、:503-507 的投影）无从分辨
- 后果：方向钉死——折到原点会把该帧包围盒与重叠带投影**收缩** ⇒ extent 偏小 ⇒ 上界偏小 ⇒ h 偏细 ⇒ 节点数偏大（撞 max_nodes，偏红或截断）；在产品尺度（单视场 ≪90°）下不可达，故实害低
- 定级：**S3**
- 怎么算修好：改成返回失败标志（或写 NaN 并由 :562 的有限性检查兜住）；头 :263 补一句退化语义
- 置信：CONFIRMED
```

```
### CR-47-29 · :71 写"缺省关闭…保持 RELEASE-01 基线不回归"，:74 的初始值是 `true`，生产编排链的缺省也是真
- 轴：C
- 位点：`stage2_common.h:71` `// 缺省关闭：保持 RELEASE-01 加性 C 场行为不变（基线 447 不回归）。`；`:74` `bool   sky_plane_enabled = true;`
- 上位依据：`eng/packaging/config/config_registry.json:1863-1871` 对 `sky_plane.enabled` 的陈述是 `declared_default: "随 additive_mode ∈ {delta, both}"`、`note: "生产经 sky_plane.enabled 读取（module_adapters.cpp，缺省 delta_wanted），未登记"`
- 现状：`module_adapters.cpp:9835-9839` `additive_mode` 缺省 `"delta"` ⇒ `delta_wanted=true` ⇒ `sky_enabled = sp_cfg.value("enabled", delta_wanted)` 缺省为真；工具侧 `stage2_common.cpp:186` 取 struct 默认（true）⇒ **两条入口都默认开**
- 差在哪：注释的"缺省关闭/不回归基线"与三条侧面（本 struct 初值、parser 行为、登记面陈述）全部相反；:72「生产 config 模板显式 sky_plane.enabled=true 启用目标模型」因此是冗余陈述
- 后果：读头的人以为该链是 opt-in 的可回退特性，实际是默认路径 ⇒ "基线 447 不回归"这句话今天不再描述任何配置状态（它描述的是 RELEASE-01 的行为）
- 定级：**S3**（纯注释与初值相反，无字节影响；但它是"缺省语义"级别的可信度损伤）
- 怎么算修好：删或改 :71-73 为"缺省启用（由 `seam.additive_mode` 决定，缺省 delta）"，并与 `config_registry.json` 的 declared_default 用同一句话
- 置信：CONFIRMED
```

```
### CR-47-30 · 连线方向切平面原点的经度用三维向量和、纬度用标量平均，两者不是同一个点
- 轴：R
- 位点：`stage2_common.h:489-493`
  ```
  const double ra0 = std::atan2(
      std::sin(pcra[a]) * std::cos(pdec[a]) + std::sin(pcra[b]) * std::cos(pdec[b]),
      std::cos(pcra[a]) * std::cos(pdec[a]) + std::cos(pcra[b]) * std::cos(pdec[b]));
  const double dec0 = std::asin(std::max(-1.0, std::min(1.0,
      (std::sin(pdec[a]) + std::sin(pdec[b])) / 2.0)));
  ```
- 上位依据：条文面零提及（本批检索式：`grep -n "角平分点\|切平面原点\|great circle" docs/science/PHASE2_UPM.md docs/plugins/algorithms_phase2/10_sampling.md` 未取到该式）；本头 :488 只有一句注释「连线方向的切平面（原点 = 两指向中心的角平分点）」
- 现状：经度是**单位向量中点方向**的经度，纬度是**正弦值的算术平均的反正弦**；单位向量中点的正确纬度是 `atan2(sinφa+sinφb, |水平分量之和|)`。二者只在两点同纬度时重合
- 差在哪：原点 O 一般不在两指向的大圆上 ⇒ 之后 :497 `du/dv` 定义的"连线方向单位向量"与 :503-507 的公共点投影都建立在倾斜的坐标架上是自洽的（e 由 O 系两点定出，仍在 O 系内），但 :263 的 gnomonic 投影在 O 系里对距 O 90° 外才有奇异性，O 偏离中点会不对称地放大一侧畸变
- 后果：`overlap_band_width_deg`（→ h 上界）在两指向纬差大时偏离真实投影宽度，二阶量；方向不定（取决于偏离侧），故不作"偏红/偏绿"判断
- 定级：**S3**（缺"真实产品两指向纬差量级"与偏差量级；本批不实跑）
- 怎么算修好：用球面中点式 `dec0 = atan2(sinφa+sinφb, hypot(cosφa·cosλa+cosφb·cosλb, cosφa·sinλa+cosφb·sinλb))`，并在头里给该原点的一手出处（或改成"任意原点，投影宽度对原点选择不变"的可验证陈述 + 一个单测）
- 置信：PARTIAL（差一个数值实验证明该偏离进入 h 的量级）
```

### 3.3 不报的核过项（附检索式）

| 核过项 | 为什么不算缺陷 | 检索式 |
|---|---|---|
| 13 颗 sampler 键 + 8 颗 rejection 参数字段的 struct 默认 ↔ parser 默认 ↔ `CONFIG_SCHEMA.md:23-55` | 逐值同数（本头 :56-57 引的纪律在这一点上被遵守）；"第二处字面量"是维护形态问题、值本身无分叉，不单列 | `sed -n '38,210p' stage2_common.cpp` 对 `CONFIG_SCHEMA.md:23-55` |
| `reject_underdetermined_n = 0` 的"按 profile 解析" | 本头 :111-115、`REJECTION.md:45-48`、`CONFIG_SCHEMA.md:40` 三侧同口径（pixel=3 / 其余=2 / extreme_prior=1） | 读 `REJECTION.md:44-48` |
| `reject_profile = astrocs_adaptive_pixel` 与 WBPP 2.5.9 档界不符 | 权威 `REJECTION.md:53` 自己登记「其 n>15 档本仓取 linear_fit 与该版不符，见 §5」⇒ 已知且如实登记，不重复立案 | 读 `REJECTION.md:53` |
| `p2_acr_block_eligible` 恒 false 且弃用全部形参 | 与 `ASTROCS_DESIGN §2`「ACR 生产不可达」同侧；实现 `stage2_common.cpp:511-523` `(void)cfg;` 显式声明不留绕过口子 | 读 :511-523 |
| `P2SkyPlaneGeometryProvenance` 的 2%/98% 双宽登记（:246-247） | "稳健宽度供导出、全幅宽度供审计"是同一事实的两个读数，非自相矛盾（§4 的"两个适用域"） | 读 :215-216 与 :246-247 |
| `nf<2` 报错、`nrm>0` 退化中心报错（:346/:363/:442） | 显式失败面，与 :303 承诺一致 | 读 :338-370 |
| `kDegPerRad` 的值 | 单位换算定义常数（本批复算 180/π 逐位一致） | — |
| `sky_plane.max_nodes` 未出现在 shipped configs | 该事实本身不是缺陷（缺键合法），其**后果**已并入 CR-47-23 的触发链，不另立"配置缺键"条目 | `grep -rn "max_nodes" … --include=*.json` |
| `support_power` 与 `FZ-GATE-SUPPORT-COVERAGE`（support 不得冒充权重）的张力 | 本批未读到 support 进入 UPM 权重的实现式（属 upm.h/upm.cpp 面），仅在表内标注语义张力，不在无证据时下指控 | 待文件 4 通读时追 |

取证用量：本文件 14 次工具调用（前言上限 20 内）。

## 4. `lib/algorithms/coverage/include/astro/phase2/upm.h` —— 已读：是（表 26 项）

本文件的"三方"取到的实体：声明头 ｜ 实现 `lib/algorithms/coverage/src/upm.cpp`（`build_impl` 缺省回填 :261-305、网格校验 :298/:319/:1603、收敛判据 :1028-1061、χ² 诊断 :1331-1338/1442-1445、稠密物化 :2045-2143）｜ 两条生产装配 `lib/phase2_session/p2_session.cpp:199-220`（`p2_upm_build`）与 `lib/infrastructure/scheduler/src/module_adapters.cpp:9552-9637`（`p2_upm_build_geo`）｜ 合同面 A `docs/contracts/API_CONTRACTS.csv:267-281`（15 行 W2 冻结集）｜ 合同面 B `docs/contracts/DATA_SEMANTICS.md:1762/1768/1823`、`docs/contracts/PUBLIC_API.md:1559/1562` ｜ 权威 `docs/science/PHASE2_UPM.md §5/§9a`、`docs/plugins/algorithms_phase2/11_upm.md §4.6`、`docs/algorithms/UPM_SOLVER.md:94`、`docs/algorithms/PHASE2_UPM_IMPL.md:378-390`。

### 4.1 §2.5 数值处置表（upm.h）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :46 | `control_variance` 式 | `k_corr × (π/2) × sigma_bg² / N_retained` | control 估计器的统计方差 | 公式导出 | π/2 锚 Kendall & Stuart §28.5（`PHASE2_SAMPLER.md:262-263` 逐字引）；实现 sampler.cpp:875 区段按式算；**N 适用域缺失与 `min_samples=5` 的问题同 CR-47-1**；本头 :46 与 sampler.h:51、sky_plane.h:104 三处同一式、三处都不带 N 下限 | 同 CR-47-1 | CONFIRMED |
| :40-42 | `ivar`（弃用字段） | — | Phase1 单 leaf ivar，仅诊断 | 不适用（弃用声明 + 禁止用于科学权重） | 实现 `sampler.cpp:1121-1134` 先写 control_ivar 兜底、再用 leaf ivar 产品覆盖，注释自陈"science 权重仍只认 control_ivar（upm.h:40-47 / SCI-UPM-WEIGHT-001）"⇒ 头的禁令被实现遵守 | — | CONFIRMED（不报） |
| :51-54 | `snr_available` 哨兵 | 1=局部有星（snr **可为 1.0**）/ 0=回退整帧中位，禁以 1.0 伪装 unknown | 点 SNR 的可用性标志 | 文献值（合同） | 实现有写入：`sampler.cpp:1112 o.snr_available = cs.snr_avail[fi];`；`PHASE2_SAMPLER.md:397-398`「半径内无星 → snr_val=frame_snr_med_exact 且 snr_available=0（**该档标记 = 0；1.0 只属可用档**，upm.h:51-53）」、`DATA_SEMANTICS.md:1762/1768`、`PUBLIC_API.md:1562` 四处同口径 ⇒ 头的哨兵有实现、有合同、未被违反；**但合同给的行锚 `:859/:1038`（DATA_SEMANTICS）与 `:893/:1056`（PHASE2_SAMPLER）不等于实际写入点 :1112**，属 CR-47-9 同一母题的额外位点 | — | CONFIRMED（不报语义，报锚漂移） |
| :62 | `precision` | 0=fp32 / 1=fp64 | 模型精度档 | 结构性不适用（枚举） | `stage2_common.cpp:200-206` 字符串→枚举 | — | CONFIRMED |
| :67 / :336 | `model_hash` | char[65] | 模型内容 SHA-256 文本槽 | 结构性不适用（64 hex + NUL） | — | — | CONFIRMED |
| :72-73 | `robust_loss` / `snr_weight_mode` | 0 / 0（"首版"唯一值） | 损失与权重口径枚举 | 待确认（键名与合同相反 ⇒ CR-47-24；口径可选 ⇒ CR-47-25） | `PHASE2_UPM_IMPL.md:384-386` 表；`CONFIG_SCHEMA.md:34-35` | — | CONFIRMED |
| :74 | `huber_delta` | 1.345 | Huber δ | 文献值 | `PHASE2_UPM_IMPL.md:384`「1.345（无量纲，单位=sigma_eff）\| upm.h:74；upm.cpp:254/:273」；实现缺省 :267、修补 :286 同值 | — | CONFIRMED |
| :75 | `smoothing_lambda` | 头写"默认 0=关闭" | 图平滑强度 λs | **待确认（三方打架，见 CR-47-39）** | 实现缺省 `upm.cpp:268 cfg.smoothing_lambda = 0.0;`、:299 `<0 ⇒ 0`；工具与调度器在 `smoothing="auto"` 时取 `P2_SMOOTHING_LAMBDA_AUTO=0.1`（`stage2_common.cpp:140`、`module_adapters.cpp:9613`）；`p2_session.cpp:199-217` **不赋该字段** ⇒ 零初始化 ⇒ 0；`stage2_common.h:24` 引负责人裁决 GAP_AUDIT §9.39 A5「smoothing_lambda 不能为 0」 | 保守=0（不平滑，残差不被掩盖），但与裁决冲突；影响范围＝法方程块间耦合 ⇒ 直接决定 :388-396 可辨识性诊断的完备性 | CONFIRMED（四侧原文均读到） |
| :76 | `zero_anchor_weight` | 1e-3（"生产装配显式"） | 弱零锚权重 | 待确认 | `PHASE2_UPM_IMPL.md:380-381` 记两处生产组装均显式赋 1e-3（本批复核 `p2_session.cpp:209`、`module_adapters.cpp:9583`）；SCI §9a:133 为头自称锚（本批未逐字读该行） | — | PARTIAL（缺 §9a:133 原文） |
| :77 | `max_iterations` | 100 | IRLS 上限 | 待确认（有合同无导出） | 实现 :270/:287；`CONFIG_SCHEMA.md:34`；`PHASE2_UPM_IMPL.md:385` | 保守=更小 | CONFIRMED |
| :81 | `tolerance` | 1e-6 | 收敛容差 | **待确认（可达性由头自证为疑，见 CR-47-34）** | 本头 :112 自述「绝对 1e-6 在 max\|M\|~3e15 时低于 ULP(0.5) 5-8 个数量级，**原理上不可达**」；实现 :271/:291 缺省 1e-6；`p2_session.cpp:205` 赋 1e-6 且**不设 `tolerance_relative`**（⇒ 绝对判据），`module_adapters.cpp:9573` 赋相对判据 | 保守=相对判据（跨电平可比）；影响范围＝`converged` 读数与产品是否来自未收敛解 | CONFIRMED |
| :82 | `target_order` | -1=auto | 目标 order 哨兵 | 结构性不适用（哨兵） | 实现 :306 | — | CONFIRMED |
| :83 | `sigma_floor` | 1e-3 | uncertainty 下限 | 待确认 | 实现 :273/:292；`CONFIG_SCHEMA.md:34`；`PHASE2_UPM_IMPL.md:380-381` | — | CONFIRMED |
| :84 | `support_power` | 1.0 | support 因子指数 | 待确认 | 实现 :274/:293；`CONFIG_SCHEMA.md:34`；**与 `FZ-GATE-SUPPORT-COVERAGE`（sampler.h:165「support/coverage 只作门，不得冒充权重」）的张力在本头落地**：`p2_upm_raw_weight` 的 ablation 式（:173-174）含 `support^support_power × snr²/(1+snr²)/unc²` ⇒ support 直接进权重 | 方向：`support_power=0` 才与 FZ 一致（在 ablation 档内）；生产档 `use_ivar_weight=1` 不用 support ⇒ 冲突面限于诊断档 | CONFIRMED |
| :85 | `quality_mode` | 0=flags 映射 | 质量标志映射 | 结构性不适用（枚举，唯一值） | 实现未追 | — | PARTIAL |
| :89 | `use_ivar_weight` | 1 | ivar 与 legacy snr² 的选择 | **待确认（口径选择键，见 CR-47-25）** | `:86-88` 自述两档语义 + production 缺 ivar ⇒ 显式 INVALID；实现 `p2_upm_raw_weight` 返回码 2（:170-175 与实现同侧） | — | CONFIRMED |
| :90 | `control_reliability` | 1.0 | 分量级权重缩放 | 待确认（无导出） | 实现有消费：`upm.cpp:296`（<=0⇒1.0）、:342、:655 注释式 `w_cell = w_UPM/Σw × control_reliability`；两条生产装配都硬编 1.0（`p2_session.cpp:210`、`stage2_common.cpp:543`）；`CONFIG_SCHEMA.md` 零登记 | 保守=1.0（不缩放）；影响范围＝per-cell 权重（现值中性） | CONFIRMED |
| :92-94 | `cpu_workers` | 0=auto / 1=串行；"仅 P2_ENABLE_OPENMP 且 >1" | 并行 worker 数 | **待确认（承诺不成立，见 CR-47-41）** | `upm.cpp:608`「无 hardware_concurrency」、:614/749/791/913 `(cfg.cpu_workers > 0) ? : 1`；`grep -n "P2_ENABLE_OPENMP\|omp_get_max_threads" upm.cpp` → **代码零命中**（:2042 只在注释里出现 omp_get_max_threads） | 见 CR-47-41 | CONFIRMED |
| :102/:105/:109/:113 | P2a 四开关 | gs_damping 1.0 / m_full_frame 0 / final_gauge 0 / tolerance_relative 0 | 阻尼/联合 LS/末端 gauge/相对容差 | 待确认（默认=legacy，生产是否启用按入口分叉，见 CR-47-33） | 本头 :96-98 自述「生产装配 p2_op_upm_fit 显式启用 P2a-2/P2a-3/P2a-4」+ 依据 `reports/RELEASE-02/{q2-snr-smooth,c-delta-ruling}.md`（**`reports/` 跟踪面只有 v19r2 三个 CSV，这两份不在跟踪面也不在磁盘**）；实现修补 :302-305；调度器 `module_adapters.cpp:9577-9579` 赋 0.5/1/1 + :9573 相对判据；`p2_session.cpp:199-217` **不赋这四项** | 保守=legacy（现状）；影响范围＝收敛路径与叠加阶跃（:108 自陈"阶跃恒 0.0000"） | CONFIRMED |
| :117 | `grid` | 8（"必须 == 采样器 control_grid_per_tile 且 == UPM 网格常数 8"） | control cell 网格边长 | 结构性不适用（**前提是它真是常数**；但它同时是采样器的可配 1..64 键 ⇒ 见 CR-47-38） | 实现 :298 `if (cfg.grid != 8) return 3;`、:319、:1603（open 处亦校验）；`stage2_common.cpp:549 mcfg.grid = cfg.control_grid_per_tile;`、`module_adapters.cpp:9583 uc.grid = smp_doc.value("control_grid_per_tile", 8)`；`stage2_common.cpp:41-44`/`module_adapters.cpp:9265-9267` 允许 1..64；`CONFIG_SCHEMA.md:25` 只写 `control_grid_per_tile(8)` 未写值域 | 见 CR-47-38 | CONFIRMED |
| :145-149 | `converged` 枚举 | 0=max_iter（**模型文件未记录时同样读 0**）/1/2/3 | 收敛状态 | 不适用（状态口径，且"未记录按未证明"是保守取向） | 实现 `upm.cpp:110`、:1047-1061 同四档；`11_upm.md §4.6`（头自称唯一口径） | 保守=0 | CONFIRMED（不报） |
| :170-175 | `p2_upm_raw_weight` 返回码 | 0/1/2 | 权重实现的错误面 | 结构性不适用（返回码） | 与 :86-88 的"禁止静默回退"同侧 | — | CONFIRMED |
| :245-251 | τ / 退休常数 | `rank_rtol=1e-10`；`kappa_max` 1e6（退休）；旧两把尺 1e-10 vs 1e-20（差 10 个数量级） | 判据阈值 | 公式导出（浮点精度地板） | `FZ-AP2S-RANK-RTOL`；`identifiability.h`；:328 `rank_rtol_effective = max(τ, max(m,n)·eps)`；头 :245 与 :269 的互补不等式（`<1/τ` vs `≥1/τ`）本批独立推导一致 | 方向：τ↑⇒更易判红（保守） | CONFIRMED（不报） |
| :252 | `min_frames` | 2（FZ-AP2S-UPM-MINFRAMES） | 单帧分量禁拟合 g | 公式导出（gauge 自由度论证） | :289/:296 与 rc=5 同侧；`PHASE2_UPM.md` | — | CONFIRMED |
| :265-276 | 返回码表 | 0..8（4 已退休保留位） | MA 求解器错误面 | 结构性不适用（枚举基数 + 保留位） | :269-272 明写"同时覆盖旧 rc=3 与 rc=4"与"保留数值位以免旧消费者误读"⇒ 已声明，§4 免报 | — | CONFIRMED |
| :284 | `control_ivar` 单位 | ADU⁻²，必须 >0 有限 | 权重输入 | 文献值（单位表） | `sampler.h:170-171` 冻结单位表 `W_info=ADU^-2`；`DATA_SEMANTICS.md §31`（头 :224 引） | — | CONFIRMED |
| :312-314 | `k_corr` 与 provenance | 默认 0=未声明；>0 只能是域内冻结值 **1.4** 且需 `applicability_domain`/`calibration_run_id` | 方差因子的入库凭据 | 实验标定（值）+ 结构性不适用（哨兵 0） | :438-441 的码 1/2/3/4；标定件 `control_median_mc_test.cpp`/`drizzle_control_median_mc`/`eng/ci/checks.json:4363`（同 CR-47-12）；**门不在产品路径上：见 CR-47-40** | 方向：门只挡"显式声明者"，不声明（0）即绕过 | CONFIRMED |
| :326-337 | `P2UpmMaInfo` 各计数 | rank/κ/n_unidentified/… | 摘要面 | 不适用（派生量，非配置） | :329「秩亏 ⇒ +inf（不发布伪值）」与 sky_plane.h:342 同口径 ⇒ 一致 | — | CONFIRMED |
| :388-396 | 分块对角前提 | "生产默认 λs = smoothing_lambda = 0 ⇒ 法方程按 control 分块对角"；λs>0 ⇒ `coupling_assembled=0` | 诊断的成立前提与边界登记 | 公式导出（块对角论证）+ **前提与两侧默认冲突，见 CR-47-39** | 实现 `upm.cpp:1331-1338` 区段（本批复核 χ²/dof 面）；`coupling_assembled` 位存在 | λs>0 时 `identifiable` 仍按不完整矩阵给判决位 ⇒ 方向为**偏绿**（漏掉耦合只会让真实 κ 更大） | CONFIRMED |
| :410-413 | `chi2_red` 退化 | `dof_eff<=0 ⇒ NaN` + `chi2_red_defined=0`，持久化写 null | 约化 χ² 的不可用编码 | 公式导出（Andrae et al. 2010 式 (9)） | 实现 `upm.cpp:1334-1338`、:1442-1445；**与同阶段天光面求解器的编码相反，见 CR-47-31** | — | CONFIRMED |
| :427-433 | 警告块策略 | 报警告、不改 rc、不 fail-closed | 非收敛的产品面 | 不适用（负责人裁决面） | :427「负责人裁决：报警告，不影响运行，不 fail-closed」；消费者仅 `module_adapters.cpp`（`p2_session` 路径不读 ⇒ CR-47-34） | — | CONFIRMED |
| :439-441 | `p2_upm_control_variance` 码 | N<1/σ≤0/k 非有限≤0⇒1；k=1.0⇒2；k≠1.4 且无 run id⇒3；domain 空⇒4 | 方差式的前置门 | 结构性不适用（返回码）+ 域值 1.4 属实验标定 | 测例 `eng/tests/unit/v6_p2_upm/v6_p2_upm_ma_test.cpp:612-619` 逐码覆盖；库内唯一调用者 `lib/algorithms/integration/v6/src/phase2_integrate.cpp:1227` 以 **`8` 硬编码 n_retained** 与 `kKCorrFrozen` 调用（该处上下文为确定性合成标定/探针面，注释 :1216「真实会话由 p2_frame_id 提供」）⇒ 见 CR-47-40 | — | CONFIRMED |
| :194/:213 | `p2_upm_materialize_dense` | 同一签名**声明两次** | 稠密缓存物化入口 | 结构性不适用（重复声明本身无数值语义） | 见 CR-47-41 | — | CONFIRMED |

### 4.2 findings（upm.h）

```
### CR-47-31 · 同一个"约化 χ² 在 dof≤0 时"的编码，两个 Phase2 求解器取了相反的两套：UPM 写 NaN+null，天光面写 0.0
- 轴：C（主）+ R
- 位点：`upm.h:412-413`
  ```
  double chi2_red;                     // chi2/dof_eff；dof_eff<=0 ⇒ NaN（见 chi2_red_defined）
  int chi2_red_defined;                // 0 ⇒ χ²_red **无定义**（dof<=0），持久化必须写 null
  ```
  对照 `sky_plane.h:352` `double dof_eff;               // n_used − rank_full（χ²_red 的实际分母；≤0 ⇒ χ²_red=0）`
- 上位依据：两处都引同一出处——`sky_plane.h:348`「Σw r²/(n_used − r_eff)：**有效自由度**口径（Andrae et al. 2010 式 (9)）」与 `sky_plane.cpp:1205`「Andrae et al. 2010, arXiv:1012.3754 式 (8)(9)」；同一文档对旧口径的判词：`sky_plane.h:349-350`「已退休口径：分母曾用 n_used − n_params（秩亏时**系统性低估** χ²_red 并掩盖未被约束的方向数）」
- 现状：`upm.cpp:1334-1338` `if (dof>0) {chi2_red = chi2/dof; defined=1;} else {chi2_red = quiet_NaN(); defined=0;}`，持久化 `:1442-1445` 写 null 或 NaN；`sky_plane.cpp:1208` `info.chi2_red = (info.dof_eff > 0.0) ? (swr2 / info.dof_eff) : 0.0;` 且 `sky_plane.cpp:1753` 无条件把 `chi2_red` 写进产品 JSON（无 `defined` 伴生位）
- 差在哪：**方向钉死**——0 是 χ²_red 的下确界，任何"χ²_red ≤ 门限 ⇒ 绿"的比较在 dof≤0 时读到"完美拟合"；NaN 不可比较、null 不可判绿。天光面一侧把"不可计算"编码成了"最优"，正是同头 :349-350 判为"系统性低估并掩盖"的那类编码；UPM 一侧的做法是对的
- 后果：①产品 JSON 里出现 `chi2_red: 0.0` 的伪完美读数（与真实判绿无法区分，且无标志位）；②该值还回灌自适应回路——`sky_plane.cpp:1451` `improved = (rc2==OK) && (info.chi2_red > 0.0) && …` ⇒ chi2_red=0 时**细化分支被永久关闭**，节点网格停在较粗解且不报错
- 定级：**S2**（缺 dof_eff≤0 的可达性实例：需 `n_used ≤ rank_full`，而 `min_samples=8`（sky_plane.h:227）配 `order=1`(m=3) 与多帧时 `rank_full = r_eff + (n_frames−1)·m` 可超过 n_used；**接线即 S1 的条件** = 任一轮真实产品的 sky_plane provenance 出现 `chi2_red == 0.0`，或 v6 判绿门直接读该字段）
- 怎么算修好：`sky_plane.cpp:1208` 改成与 upm 侧同一套（NaN + `chi2_red_defined`，持久化写 null），并让 :1451 的 `improved` 在"未定义"时显式不细化并登记原因；**必须同批**改 `sky_plane.h:352` 的注释、产品字段表（`DATA_SEMANTICS` 的 sky_plane 节）、`eng/tests/unit/v6_p2_sky/*` 与 `实验/additive-sky-seamless` 的期望——只改实现会把既有判绿门与实验单元判红
- 置信：CONFIRMED（两侧实现式均逐字读到；本条同时把文件 2 表内 `dof_eff` 行原标 PARTIAL 的方向判断转为 CONFIRMED）
```

```
### CR-47-33 · upm.h 声明的"P2a 四项生产显式启用"只对一条生产装配成立：p2_session 装配的 uc{} 既不设四开关也不设 λs
- 轴：C（主）+ D
- 位点：`upm.h:95-98`
  ```
  // ===== RELEASE-02 P2a 接缝修复（科学行为变更，显式 opt-in）=====
  // 下列 4 项默认取 W2 冻结基线（legacy）值，使 cfg{}/cfg; 的既有调用点
  // 行为逐位不变；生产装配 p2_op_upm_fit 显式启用 P2a-2/P2a-3/P2a-4。
  // 依据：reports/RELEASE-02/{q2-snr-smooth,c-delta-ruling}.md。
  ```
- 上位依据：`AGENTS.md §6`「不串三阶段…」「一个 commit = 一个明确目的」不涉；本条条文面是 `docs/science/PHASE2_UPM.md §5`（收敛口径）与 :110-113 各自的 P2a 编号说明
- 现状：装配 A `p2_session.cpp:199 P2UpmBuildConfig uc{};` 逐字段赋 13 项（:200-211），**不含** `gs_damping/m_full_frame/final_gauge/tolerance_relative/smoothing_lambda`，:212-217 只允许 `upm.{max_iterations,huber_delta,smoothing_lambda}` 三键覆盖，:220 调 `p2_upm_build`；装配 B `module_adapters.cpp:9552 P2UpmBuildConfig uc{};` + :9573 `uc.tolerance_relative = 1;` + :9577-9579 `gs_damping=0.5 / m_full_frame=1 / final_gauge=1`，并调 `p2_upm_build_geo`（:9583 起）
- 差在哪：同一个冻结结构体的两条真实生产装配，一条走 legacy 全套（无阻尼、参考帧 M、无末端 gauge、绝对容差），一条走 P2a 全套；头把后者称作"生产装配"并对前者不提。`{}` 零初始化 + 无默认成员初始化的字段（`smoothing_lambda`、`tolerance`…）在装配 A 里落成 0/未设，`upm.cpp:287/:291` 才回填成 100/1e-6
- 后果：同一个 `mosaic` 语义经两条链得到**不同解**（阻尼与 joint-LS 改变迭代不动点与方差），产品字节与"接缝无阶跃"（:108 自陈 final_gauge 使阶跃恒 0）只在一条链上成立
- 定级：**S2**（不判 S1：本批未确证装配 A 在现行三命令里是否仍是可达路径——它属 session/单节点驱动面，需要实跑或入口清单核对；见 §5.2）
- 怎么算修好：把头注释改成"启用 P2a 的装配 = <点名两条入口及各自开关矩阵>"，或把两套字段收进一个共享装配函数（与 `stage2_common.h` 的"同一生产 parse+build path"纪律同源，:3-4）；`reports/RELEASE-02/*` 两份依据需落到跟踪面
- 置信：PARTIAL（缺：入口可达性证明——哪条 session 路径在三命令里活着）
```

```
### CR-47-34 · 头自己证明绝对容差 1e-6 "原理上不可达"，而 p2_session 装配正是绝对 1e-6；该链既不读收敛位也不发警告
- 轴：R（主）+ C
- 位点：`upm.h:112` `// 绝对 1e-6 在 max|M|~3e15 时低于 ULP(0.5) 5-8 个数量级，原理上不可达。`；`upm.h:81` `double tolerance;`（注释 :78「收敛容差（默认 1e-6）」）
- 上位依据：`docs/plugins/algorithms_phase2/11_upm.md §4.6`（头 :143-144 指为收敛状态唯一口径）；`docs/science/PHASE2_UPM.md §5`
- 现状：`p2_session.cpp:205 uc.tolerance = 1e-6;` 且不设 `tolerance_relative` ⇒ `upm.cpp:1036` 走 `tol_M=tol_C=cfg.tolerance` 的 legacy 绝对判据（:1050 `if (max_dM < tol_M && max_dC < tol_C) m->converged = 1;`）；`p2_upm_convergence`（:151）与 `p2_upm_warnings_json`（:434）的消费者只有 `module_adapters.cpp`（`git grep -l "p2_upm_convergence("` 与 warnings 面均无 p2_session）
- 差在哪：头的"不可达"论证是给 P2a-3 立项目的的，但它同时判明了绝对 1e-6 路径的产物性质——`converged` 只能是 0（max_iter）；而唯一把该状态变成产品面警告的通道在这条链上没接
- 后果：按 :427 的裁决"报警告不影响运行"，产品仍会 `p2_upm_save`（`p2_session.cpp:249`）落盘，但 **连警告都不发**：下游只看 rc=0 即认为构建成功，头的 :139-140 又明写"禁止用 rc=0 冒充已收敛"——该禁止在此路径上无人执行
- 定级：**S2**（缺可达性证明，同 CR-47-33；若证实 session 路径在 `mosaic` 里活着，即 S1：产物来自未证明收敛的解且无标记）
- 怎么算修好：给 p2_session 装配补 `tolerance_relative`（或删该装配）；或在 `p2_upm_build*` 内当 `tolerance_relative==0 && |M| 量级 >> tol` 时直接返回码/写诊断位，使"原理上不可达"成为机检事实而不是注释
- 置信：PARTIAL（缺运行时入口可达性；"ULP 与 1e-6 的比"一步属头自己的推导，本批未独立复算 max|M| 分布）
```

```
### CR-47-39 · λs 的取值同时决定"可辨识性诊断是否完备"，而四个侧面各给一个默认：0（upm.h:75/缺省面）、0.1（auto 解析值）、"不能为 0"（裁决）
- 轴：S（主）+ C + R
- 位点：`upm.h:388-390` `//   · 生产默认 λs = smoothing_lambda = 0 ⇒ 联合法方程在 (M_k, C_{f,k}) 参数化下 **按 control 分块对角**…`；`:75` `double smoothing_lambda;      // 图平滑权重（默认 0=关闭）`；`:419` `int coupling_assembled;              // 0 = λs>0 时块间耦合未装配（边界登记）`
- 上位依据：`stage2_common.h:24` 引负责人裁决 `GAP_AUDIT §9.39 A5「smoothing_lambda 不能为 0」（λ>0 必须）`；`docs/development/CONFIG_SCHEMA.md:19/33` `smoothing(auto→0.1)`；`docs/algorithms/PHASE2_UPM_IMPL.md:390` 冻结表 `smoothing_lambda（λs） | 0.0（默认关闭平滑）`
- 现状：`upm.cpp:268` 缺省 0.0；`stage2_common.cpp:140` 与 `module_adapters.cpp:9613` 的 "auto" ⇒ 0.1；`p2_session.cpp:199-217` 不赋 ⇒ 0；诊断块对角前提要求 0
- 差在哪：三侧互斥——裁决说 λ>0 必须；头的诊断说它只在 λ=0 时成立；配置面把 auto 定成 0.1。λs>0 时实现只是把 `coupling_assembled=0` 记下，`:418 identifiable` **仍按不含耦合项的块对角矩阵出判决位**
- 后果：创新点③的"可辨识性判据"（本头 :240-251 自述唯一判据、"不再有 rank 与 κ 两套并行判据"）在 λs>0（= 裁决要求的、= 配置 auto 的取值）下对**完整**法方程给出的是**部分**矩阵的判决；方向钉死——漏掉块间耦合只会让真实 κ 更大、真实 r_eff 更小 ⇒ 偏绿（可能把该判红的产品判成 identifiable=1）
- 定级：**S2**（不判 S1：需要一次 λs>0 且真实 κ 越过阈的实例；本批禁实跑）
- 怎么算修好：`λs>0` 时要么装配耦合项，要么把 `identifiable` 判决位置为"不可判"（rc/标志），不得沿用块对角读数；`upm.h:388-396` 需与裁决 A5 对齐（若 A5 成立，"生产默认 λs=0"这句本身就错）；改判据面要同改 `v6_p2_upm_ma_test.cpp`/`v6_p2_identifiability` 期望
- 置信：CONFIRMED（四侧原文均读到）
```

```
### CR-47-38 · `control_grid_per_tile` 合同允许 1..64，UPM 只接受 8：63 个合法取值里 62 个让 mosaic 以 rc=3 失败
- 轴：C（主）+ D
- 位点：`upm.h:117` `int    grid = 8;`（:114-116 `// M7-C-001: UPM 控制 cell 网格边长 G，必须 == 采样器 // control_grid_per_tile 且 == UPM 网格常数 8；不等时 p2_upm_build* 返回 3`）
- 上位依据：`docs/development/CONFIG_SCHEMA.md:25` 只写 `control_grid_per_tile(8)`（未声明值域）；实现侧值域来自 `stage2_common.cpp:41-44`（"必须在 1..64"）与 `module_adapters.cpp:9265-9267`（同）
- 现状：值域 1..64 被两个入口的校验接受并透传给采样器，再原样灌进 UPM（`stage2_common.cpp:549`、`module_adapters.cpp:9583`），UPM 在 `upm.cpp:298` 对 `!=8` 直接 `return 3`；`upm.cpp:1603` 打开旧模型时同样校验
- 差在哪：合同面宣称的可配置范围与下游硬约束不交叠（除 8 一点）；且 `rc=3` 在 MA 求解器语义里是"未通过可辨识性判据"（:269），在 W2 build 语义里是"网格常数不一致"（:115）——**同一返回码两套含义**，调用方无法按码分辨
- 后果：任何按合同调网格的配置都在 mosaic 上得到语义歧义的失败；"grid 可配"这一对外能力不成立；`AGENTS.md §6`「不硬编码…」的方向上，8 同时是采样默认与求解常数
- 定级：**S2**（fail-closed，不产错值；但语义歧义会让上层把网格错误当判据红来归因）
- 怎么算修好：`CONFIG_SCHEMA.md:25` 明写值域为 {8}（或让 UPM 真按 G 参数化，需同改 :319/:1603 的几何与 open 兼容面）；把 rc=3 的两种含义拆码（拆码要同改所有按 rc=3 判分支的调用点与测例）
- 置信：CONFIRMED
```

```
### CR-47-40 · 头把 `p2_upm_control_variance` 写成 control_variance 的 fail-closed 单一实现（FZ-PROV-KCORR），但生产算这个量的地方不调它
- 轴：D（主）+ C
- 位点：`upm.h:438-442`
  ```
  // ALG-P2S-UPM.7 / FZ-PROV-KCORR：control_variance = k_corr*(pi/2)*sigma_bg^2/N。
  // fail-closed：N<1、sigma_bg<=0、k_corr 非有限或 <=0 → 1；
  // k_corr==1.0（忽略相关）→ 2；k_corr!=1.4 且无 calibration_run_id → 3
  ```
- 上位依据：同头 :43-48 的冻结声明（control_variance 定义 + "k_corr 由 MC 校准（非猜测）"）
- 现状（消费者穷举）：`git grep -n "p2_upm_control_variance"` → 定义 `upm.cpp:2954`、测例 `v6_p2_upm_ma_test.cpp:612-619`（逐码）、库内唯一调用 `phase2_integrate.cpp:1227`；该调用点写 `p2_upm_control_variance(kKCorrFrozen, sigma_bg, **8**, "phase2_control_cell", "frozen-in-domain-constant-FZ-PROV-KCORR-VALUE", …)`，其上下文（:1216 注释「frame_id：使用确定性 index（真实会话由 p2_frame_id 提供）」）是合成/探针面。**真正的产品路径** `sampler.cpp:875` 区段与 `sky_plane.cpp` 的 patch 估计各自内联算同一个式子，不经此门（见 CR-47-5 的同型证据）
- 差在哪：声明层给出的四道前置校验（N<1、σ≤0、k=1.0 忽略相关、k≠1.4 无 run id、domain 空）在产品路径上一个都不执行；`N=8` 在该调用点还是硬编码而非"实际 N_retained"
- 后果：`FZ-PROV-KCORR` 的对外主张（provenance 不完整即拒）对产品面不成立；测例逐码通过给人"门已生效"的错觉（门只看夹具）
- 定级：**S2**（不判 S1：门旁路不改变数值，数值本身与 1.4/π/2 一致；改的是可追溯主张）
- 怎么算修好：让生产侧（sampler 的 cvar 组装、sky patch 估计）改调本函数或其共享核，并把 `N_retained`/`applicability_domain`/`calibration_run_id` 真传进来；否则把头注释从"fail-closed 门"降级为"MA 求解器输入校验"
- 置信：CONFIRMED
```

```
### CR-47-41 · `p2_upm_materialize_dense` 在同一头里声明两次（签名逐字相同），并 `cpu_workers`/`workers<=0 => auto=omp_get_max_threads` 的"auto"三处都不成立
- 轴：C（两处），对应派单重点②
- 位点（重复声明）：`upm.h:193-195` 与 `upm.h:212-214` —— 两遍逐字相同的注释 `// materialize dense cache（同模型 hash/目标 order/frame hash 校验）` 与签名 `P2_API int p2_upm_materialize_dense(const void* model, int target_order, const char* cache_path);`
- 位点（auto 承诺）：`upm.h:92-93` `// CON-005 并行观察/聚合 worker 数（0=auto；1=串行默认）。仅 P2_ENABLE_OPENMP // 且 >1 时并行…`；`upm.h:215` `// worker 数显式版本（CON-010 并行物化；workers<=0 => auto=omp_get_max_threads）。`
- 上位依据：`docs/standards/CONCURRENCY_STANDARD.md`、`docs/architecture/THREAD_BUDGET_ARCH.md §1`（线程预算唯一来源 = Runtime lease）——由 `sky_plane.h:234-237` 转引；`AGENTS.md §6`「不硬编码线程…」
- 现状：`upm.cpp:614/749/791/913` 一律 `const int cworkers = (cfg.cpu_workers > 0) ? cfg.cpu_workers : 1;`；`upm.cpp:2137` `const int nw = (workers > 0) ? workers : 1;`；`grep -n "omp_get_max_threads\|hardware_concurrency\|P2_ENABLE_OPENMP" upm.cpp` → 代码零命中（仅 :608 注释「无 hardware_concurrency」、:2042 注释、:2136 注释「std::thread; workers 由调用方传 lease, 无 OpenMP」）
- 差在哪：0 不是 auto 而是 1；并行不需要（也不看）`P2_ENABLE_OPENMP`，而是 `workers>1` 即起 `std::thread` 池；`sky_plane.h:232-238` 反过来以"本求解器无并行路径"为由**拒绝**设 worker 字段，并规定"将来并行必须由 lease 注入"——两头对同一纪律给出不同的接口面
- 后果：以头为准的调用方（或审计者）会以为省略/传 0 得到"按机器并发"，实际得到串行；`P2_ENABLE_OPENMP` 是已退役宏，头仍以它为并行前提（同 CR-47-10 形态，本条是 upm 面的独立取证）
- 定级：**S3**（值不受影响；重复声明是维护面缺陷，两个声明将来各自被改即分叉）
- 怎么算修好：删 :212-214 的重复块（保留 :193-195 并在其注释里补 `_n` 版本）；:92-93 与 :215 的"0=auto / omp_get_max_threads"改为"0 与 1 同义=串行；>1 用 std::thread；唯一来源=Runtime lease"
- 置信：CONFIRMED
```

```
### CR-47-42 · 合同表仍把已退役符号 `p2_upm_normalized_weights` 记为 VERIFIED，而它的全部登记行只覆盖 W2 冻结 15 个 API：已接线的三个诊断 API 一行没有
- 轴：C（主）+ D（对应派单重点③：声明了零消费者＝已按类 A 登记，本条是"删了声明却没删合同"的反向形态）
- 位点：`upm.h:181` `/* RETIRED 2026-09-25 (CHK-PROD-WIRING W1): p2_upm_normalized_weights 全仓零消费者声明已撤下；见 run/WIRING-W16-01/REPORT.md。 */`；`grep -c "P2_API.*p2_upm_normalized_weights" upm.h` → **0**
- 上位依据：`docs/contracts/API_CONTRACTS.csv:280` 仍存该行：`API-p2_upm_normalized_weights,p2_upm_normalized_weights,lib/algorithms/coverage/include/astro/phase2/upm.h,C,"P2_API int p2_upm_normalized_weights(const P2ControlObservation* obs, std::uint64_t n_obs, const P2UpmBuildConfig* cfg, double* out_norm);",ADU/pixel/deg per header,…,SCI-UPM-WEIGHT-001,UPMW-001;UPMW-002;UPMW-003;UPMW-006,VERIFIED`；门定义 `eng/tools/quality/contracts/check_api_contracts.py:101` `API-MISSING-AST … "symbol not in AST extract","expected":"exists in include headers"`
- 现状：合同表对 upm.h 共 15 行（build/build_geo/calibrate_block/close/component_gauges/dense_info/dense_read_block/evaluate_c/geometry_hash/info/materialize_dense/normalized_weights/open/raw_weight/save），其中 1 行的符号已不存在；而**已被生产消费的** `p2_upm_convergence`、`p2_upm_identifiability`、`p2_upm_warnings_json`（消费者：`module_adapters.cpp`）与 V6 MA 全套（`p2_upm_ma_build/info/solution/param_cov/c_out/provenance/close`、`p2_upm_control_variance`、`p2_upm_materialize_dense_n`）**零登记**
- 差在哪：门的覆盖方向（表→头）使"表里多一行死符号"应当判红、"头里多十个活符号"永远不判红；`status=VERIFIED` 与 `test_ids` 挂在一条指向不存在函数的行上
- 后果：`AGENTS.md §1` 的"合同与数据合同"下钻在此给出错证据：按表查 UPM 权重口径会读到已撤下的函数签名（含"units=ADU/pixel/deg per header"这类无从核对的值）；创新点③④的诊断/判据 API 无单位、值域、可空性登记（同 CR-47-8/16 形态，本条补充"合同含死行"这一反向缺陷）
- 定级：**S2**（本批禁跑门，无法给出该门现行红/绿；若 `python3 eng/ci/run_checks.py` 里 API-MISSING-AST 判红，则本条属"红被压制"，需上呈）
- 怎么算修好：删 `API_CONTRACTS.csv:280` 该行（或改标注 `RETIRED` 并与头内注释同锚）；补 10 个活 API 的登记行（units 按 `sampler.h:170-171` 冻结表逐符号写）；把门的覆盖口径改成双向，**但必须先全仓补齐未登记符号**，否则一次性判红
- 置信：PARTIAL（缺：`check_api_contracts.py` 实际运行结果——本批禁执行 `eng/**`；表/头两侧的静态事实均已逐字核对）
```

### 4.3 不报的核过项（附检索式）

| 核过项 | 为什么不算缺陷 | 检索式 |
|---|---|---|
| `snr_available` 的 0/1 哨兵与"禁以 1.0 伪装 unknown" | 头 :51-54、`PHASE2_SAMPLER.md:397-398`、`DATA_SEMANTICS.md:1762/1768`、`PUBLIC_API.md:1562` 四侧同口径，实现有写入点 `sampler.cpp:1112`，全仓无 `snr = 1.0` 型兜底 | `grep -rn "snr = 1.0\|snr_available" lib/algorithms/coverage/src/*.cpp`（首轮 `git grep \| head -12` 因文档行占满上限而漏看 lib 行，已复核） |
| 判据不等式方向（`r_eff==n_free ⟺ κ<1/τ` 与 `κ≥1/τ ⇒ rc=3`） | 本批独立推导一致：`r_eff==n_free ⇔ λ_min>τλ_max ⇔ κ<1/τ`，:245 与 :269 互补无空洞 | 读 :243-251/:269-272 |
| rc=4 保留位但不再返回 | :271-272 显式声明"保留数值位以免旧消费者误读"⇒ §4 免报（已声明的不适用） | 读 :265-276 |
| 秩亏时 `kappa = +inf（不发布伪值）`（:329/:274） | 与 sky_plane.h:342 同口径，是本批唯一两侧一致的退化编码（对照 CR-47-31 的 χ² 面） | 读两处 |
| `p2_upm_warnings_json` 的"报警告不 fail-closed" | 负责人裁决原文写在 :427，属已裁事项；其"另一条链不接警告"的后果已并入 CR-47-34，不重复计 | 读 :427-433 |
| `k_corr` 域内冻结值 1.4 的硬编码 | 值有标定件与 CI 注册面（CR-47-12 已核）；本批只报"门不在产品路径"（CR-47-40），不重复报常数本身 | `eng/ci/checks.json:4363` |
| `n_params = n_p + 2F − 2·n_components`（:263）与参数顺序（:260-263） | 定义式（每分量 2 个 gauge），非可调量 | 读 :236-263 |
| `:445` `p2_upm_control_variance` 的 `out_*` 均可空 | 与 :442「返回 0 时写…」一致，测例 `v6_p2_upm_ma_test.cpp:612-619` 覆盖 | 读该测段 |

取证用量：本文件 18 次工具调用（前言上限 20 内）。

---

## 5 收尾

### 5.1 本批最高危三条（只挑，不复述）

1. **CR-47-27**（`stage2_common.h` inline 几何导出）——§5d① 自校准栅栏在两帧输入下结构上不可能武装，把多指向静默当单指向，导出的节点间距上界按帧角尺寸算，违反本头自己引的 `PHASE2_UPM §7a` 规则 1；五份 2 帧 shipped config 证明可达。S1。
2. **CR-47-23**（`sky_plane_max_nodes` 8192/2048 分叉）——本头注释自证 2048 会让自适应失败，却只在工具链抬高上限；生产编排链按 2048 运行且无配置可抬（无任何 JSON 设该键），且"细化被内存门挡住"与"判据不需要细化"共用同一采纳分支。S1。
3. **CR-47-31 + CR-47-1 合看**（退化编码与低样本口径）——同一个约化 χ² 在 Phase2 两个求解器里被编码成 NaN/null 与 **0.0**，后者把"不可计算"读成"完美拟合"并顺手关闭细化分支；同时 `min_samples=5` 使 control_variance 公式跑在权威文档自己量成"低估 8.5%"的失真域内。两条都是"判据看着绿、实际不成立"的形态。S2（各含一条 S1 触发条件）。

### 5.2 我读不动或需要实跑才能定的

| 项 | 卡在哪 | 该跑什么 |
|---|---|---|
| CR-47-42 的 `check_api_contracts.py` 现况 | 前言 §1 禁执行 `eng/**`，无法判定"合同含死符号"现在是红、绿还是被压制 | `python3 eng/tools/quality/contracts/check_api_contracts.py`（只读，产 JSON 报告）；红则按 §10 上呈，不得用 waiver 盖 |
| CR-47-1 的 N_retained 分布 | 不知道真实产品上 `N_retained ∈ [5,64]` 的 control 占比（判据必需） | 既有端到端轮次产品上做一次只读直方图（或 `实验/` 侧固定 seed 重采样）；`python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 20 -- <端到端>` 按 AGENTS §3 |
| CR-47-23 的节点数标度 | 2048 vs 8192 是否真的把 h 从 0.0376 截回 0.0752，依赖 M42 的 `attempts[]` 实测（其证据在 `run/UPM-NODE-ADAPT-01`，不入库） | 在跟踪面重跑一次 `v6_p2_sky_node_adapt` 并打印 `P2SkyPlaneAdaptiveReport.attempts[].rc/chi2_red/n_nodes`（需构建，本批禁） |
| CR-47-27 的量级 | 真实两帧产品的 `帧角尺寸 / 指向间距` 比值决定 h 偏粗多少 | 对 `stage2_real_overlap.json` 那一类 2 帧输入跑一次几何导出并读 `P2SkyPlaneGeometryProvenance`（本批禁实跑） |
| CR-47-33/34 的入口可达性 | `p2_session.cpp` 的 upm_build 装配是否仍在三命令调用图上活着 | 查 `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv` + `eng/ci/ledgers/prod_wiring.json` 对 `p2_upm_build(` 的判决（只读即可定） |
| `P2StarMaskCap.kind` 是否被赋值（文件 2 表内 PARTIAL） | 只读到 `p2_star_mask_caps` 的 snr 过滤段（:327-336），未读到 cap 构造尾部 | 读 `sky_plane.cpp:337-392` |
| `P2SpatialGridModel` 三函数（`p2_spatial_model_eval/summary`、`p2_scalar_degrade_gate`）的实现与消费者（文件 1 CR-47-8 的缺角） | 单文件 20 次调用上限内未展开；这三函数是创新点③④的降级门声明面 | 读 `sampler.cpp` 后段 + `git grep -n "p2_scalar_degrade_gate("`（`eng/ci/ledgers/prod_wiring.json` 已有 `unreachable:…:p2_scalar_degrade_gate` 条目，可先读该条正文，属类 A 已登记） |
| CR-47-22 的 attempts 写界 | 未读 `sky_plane.cpp:1300-1400` 的 `record()` 是否对 `P2_SKY_ADAPT_MAX_ATTEMPTS` 设界 | 读该函数；`max_attempts` 配到 >12 时是否越界属内存安全问题，优先 |

### 5.3 派单与清单差异

1. **基线三项之外多出第四项**（前言 §1 要求"出现新的第四项才上呈"）：开工首跑 `git status --porcelain` 输出含 `?? drz.html`。`stat` 显示其落盘时间 `2026-09-25 21:26:11`，早于本节点首个动作；非本节点产物，本节点未删改。它不在前言登记的三项基线内，按 §1 上呈处置。
2. **行数口径**：派单写"合计约 1820 行"，`wc -l` 实测 1821（sampler.h 273 / sky_plane.h 517 / stage2_common.h 581 / upm.h 450），差 1 行，不影响清单范围。
3. **"两份合同＋实现"在本批实际取到三面以上**：除声明头与实现外，还存在 `docs/contracts/API_CONTRACTS.csv`（函数签名/单位/值域）、`docs/contracts/DATA_SEMANTICS.md`＋`PUBLIC_API.md`（字段级语义与哨兵）、`docs/development/CONFIG_SCHEMA.md`（配置键与默认）、**`eng/packaging/config/config_registry.json`（逐字段"登记/未登记"台账）**、`eng/ci/ledgers/prod_wiring.json`（零消费者符号的类 A 豁免台账）。本批按"清单为准"处理，全部当作合同＋实现面参与三方比对，并在各节表/finding 里注明用的是哪一面。
4. **派单重点③（声明了但全仓零消费者）在本仓已被机检覆盖**：`eng/ci/ledgers/prod_wiring.json` 已把这些符号登记为 `unreachable:header.P2_API:*` 并给 `exit_condition`，按前言 §4"已声明者不报"处理；本批只报其反向缺陷（CR-47-42：声明撤了、合同行没撤）与登记锚落在 `run/` 的耐久性问题（并入 §4.3 的 sampler.h RETIRED 行）。
5. 派单重点②点名的两型（四档降阶只有两档 / 容差被匿名形参推翻）：本批独立取证的结果分别是——**前者在本批以另一种形态成立**（CR-47-19：枚举声明了永不产出的 `=4` 档；CR-47-18 原疑"细化分支不存在"经补证**撤回**）、**后者本批成立两处**（CR-47-34：头声明的绝对容差按头自己的论证不可达；CR-47-10/41：`0=auto` 承诺被 `>0 ? x : 1` 推翻）。
6. 工具调用预算：文件 1 超前言 §5.3 的 20 次上限 4 次（超出部分用于 k_corr/min_samples 的注册面与方向核验，未定案项已标 PARTIAL）；其余三个文件在上限内。

## 6 收工 status --porcelain 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

`git rev-parse HEAD` 收工 = `c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`（与开工同，未被本节点改动；`git log --oneline -3` 顶三条即基线的 c8f64e9a/9f50ec72/9be3ab2c）。

### 6.1 开工与收工输出不一致（上呈，非本节点所为）

| 项 | 开工 | 收工 | 磁盘事实 |
|---|---|---|---|
| `?? ACSD整治工作包_AUDIT-06.zip` | 在 | 在 | 未动 |
| `?? site/` | 在 | 在 | 未动 |
| `?? photom.rst` | 在 | **不在** | `ls` → `No such file or directory` |
| `?? drz.html`（开工即多出的第四项） | 在 | **不在** | `ls` → `No such file or directory` |

- 前言 §1 明确 `photom.rst` "**不得删除、不得改动、不得据此判本节点写入**"，且"若收工时它仍在即按与基线一致处理"——收工时它**已不在**，故不属"与基线一致"，须如实上呈。
- 本节点对该两项**零写入零删除**：本节点全部写操作只落在 `独立审计/证据/通读-CR-47.md`（仓库外）；仓库内命令仅 `git status/rev-parse/log/ls-files/grep`、`ls`、`stat`、`wc`、`sed -n`（只读打印）、`grep/awk`。无 `rm`、无 `git checkout/clean`、无 `git config`、无构建/ctest/`eng/**`。
- `git check-ignore -v photom.rst drz.html` 无输出、`.git/info/exclude` 未新增相关模式（尾部只有 `.qoder/`）⇒ **不是被忽略，是文件已从磁盘消失**。
- 结论与请求：文件消失发生在本节点会话窗口内、由仓库外的其它进程/会话造成（与前言所述 21:20/21:26 落入这两个文件的"本机其它会话"同源）。请负责人裁：(a) `photom.rst` 是否需由落入它的那个会话恢复；(b) CR 批次的基线三项是否仍成立（后续批次开工时会再次遇到不一致）。本节点按其性质如实登记，不擅自恢复、不改动工作树。

### 6.2 本批交付自查（防"看起来通过"）

- 四个头逐一给出 §2.5 处置表：34 / 31 / 29 / 26 项，共 120 项；每行含位点、现行值、处置档位与依据锚，`待确认` 行均给保守方向与影响范围。
- findings 共 28 条（编号区间 CR-47-1…CR-47-42，其中 **CR-47-18 经补证撤回**，实计 27 条成立指控；号位 32/35/36/37 未使用——取证过程中这四条各自的内容并入了 CR-47-41（`0=auto` 与重复声明合条）、CR-47-33（P2a 开关与装配分叉合条）、CR-47-24（`snr_weight_mode` 键名与口径选择键合条）、CR-47-22（attempts 容量与写界合条），保留空号以免打乱各节表格里的指针）。
- 定级分布：S1 两条（CR-47-23、CR-47-27）、其余 S2/S3；判 S1 的两条均逐跳写了 `文件:行`，未写出触发路径的一律压到 S2 并注明"接线即 S1 的条件"。
- 方向钉：所有"偏红/偏绿"表述均先定不等式方向（CR-47-1 低估方差⇒权重偏大；CR-47-11/21 跳过判据⇒少拒绝⇒偏绿；CR-47-31 χ²=0 为下确界⇒偏绿；CR-47-27 h 偏粗⇒偏绿；CR-47-39 漏耦合⇒真实 κ 更大⇒偏绿；CR-47-14/38 内存门/网格门⇒偏红且语义歧义）。
- 撤回与修正各一处并保留原文：CR-47-18（细化判据实际存在，撤回）、CR-47-14 的"8192 在仓内无第二处"（通读文件 3 后自证不成立，已就地修正并交叉引用 CR-47-23）。
- 每节末尾均列"不报的核过项＋检索式"，含四型本仓假阳源的具体排除：两个适用域（min_samples 5/64、顶层 vs model 键）、已声明退役（`p2_sample_sky`、rc=4 保留位、`unreachable` 类 A 台账）、结构性常数（枚举/缓冲区/单位换算定义常数）、哨兵有实现且一致（`frame_id` 截断、`snr_available`、`node_spacing_deg=0`）。
- 一处方法学自纠记入 §4.3：首轮 `git grep -n "snr_available" | head -12` 被 docs 命中占满上限，导致差点误判该字段"无写入点"；复核后确认 `sampler.cpp:1112` 有写入，故本批不报该项（若照首轮输出下指控即造出假阳）。

### 6.3 骨架清单行的读毕状态

| 路径 | 已读 |
|---|---|
| `lib/algorithms/coverage/include/astro/phase2/sampler.h` | 是（表 34 项 / findings 12 条） |
| `lib/algorithms/coverage/include/astro/phase2/sky_plane.h` | 是（表 31 项 / findings 10 条，其中 1 条撤回） |
| `lib/algorithms/coverage/include/astro/phase2/stage2_common.h` | 是（表 29 项 / findings 8 条） |
| `lib/algorithms/coverage/include/astro/phase2/upm.h` | 是（表 26 项 / findings 8 条） |

独占清单四条全部读毕；未越界改动任何仓库文件。

<!-- PROGRESS: 4/4 -->
