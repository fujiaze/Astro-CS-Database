# AUD-101 · DB-20 文档审计台账（工程控制/RELEASE-05/tasks 控制包任务书 13 份）

批次：DB-20　判据：标准 01 §1–§5 + 标准 04 §4 + 仓库 `CONTROL_PACK_SPEC.md` §9　基线 HEAD=`c8f64e9a`　节点：独立只读审查节点

开工基线 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与派单前言 §1 预期的两项一致。全程只用只读命令（`status`/`ls-files`/`grep`/`log`/`show`/`sed -n`/`find`/`wc`），未执行构建、ctest、`run_checks.py`、任何 `eng/**` 检查器或生成器，未 import 仓库内 Python。

## 0 派单与清单核对

1. 清单 `独立审计/批次清单/DB-20.txt` 实有 13 行（`wc -l` = 13，均 `工程控制/RELEASE-05/tasks/` 下），与派单"13 份"一致。**但派单把范围写成"`工程控制/RELEASE-05/` 等控制包任务书"，清单只覆盖 `tasks/` 子层的 13/23 份**：`tasks/` 实际有 23 份任务书（`git ls-files 工程控制/RELEASE-05/tasks | wc -l` = 23），包级 8 份（`00_README`/`TASK_LIST`/`PROMPT`/`GAP_AUDIT`/`ACCEPTANCE`/`SUMMARY`/`OPEN_QUESTIONS`/`HANDOFF`）不在本批。本批只对清单 13 份下结论；包级与另 10 份任务书的事实只在**判定 13 份是否可执行/是否过期**所必需处引用，并注明属别批。
2. 前言 §4 的 14 字段与 `TPL_文档审计台账.md` §1 的 11 列不一致（模板缺 `行数`/`写法违规`/`悬空引用`）。按前言执行：每份给全 14 字段，末尾附合并用简表。
3. `doc_mechanical.csv` 对本批 13 行的 `lines` 列与实跑 `wc -l` **全部一致**；`unregistered_index=True` 全部属实且**不是缺陷**（`docs/DOCUMENT_INDEX.yaml:30` 与 `eng/tools/doccheck/check_doc_index.py:100` 明列 `工程控制/` 为扫描排除面，控制包不落正式文档索引）。两处机械列**漏报**、两处**误报**，见 §5 节。
4. **下游计数口径（全批统一，一条命令定死）**：`git -C "F:/Astro dev/Astro CS Normalization Database" grep -l "<ID>"` 去掉该 ID 自己的任务书 = "全仓命中"；再加 `-- ':!工程控制' ':!reports'` = "包外"（排除控制包自身与 `reports/v19r2/**` 两份历史台账 CSV 快照）。两个数都写，不用"引用者"含糊表述。13 份合计：**包外 84 个不同文件、246 处命中**（命令：对 13 个 ID 分别 `git grep -c "<ID>" -- ':!工程控制' ':!reports'` 求和＝246；`git grep -l … | sort -u | wc -l`＝84）。

## 0.1 包级判定：RELEASE-05 **未收口**，本批任何一份都不满足"收口即清理"的删除条件

收口判据取 `CONTROL_PACK_SPEC.md` §9 自己声明的条件（不看目录名）：① 全部任务 PASS；② `SUMMARY.md` 写就且长期结论沉淀进正式文档；③ **真实数据终验先于控制包完成声明**。逐条核：

| 条件 | 在册事实 | 出处 |
|---|---|---|
| 全部任务 PASS | **不成立**。23 份任务书：17 PASS、1 BLOCKED（ARCH-505）、4 NOT_RUN（CLEAN-501、PERF-501、BLD-501、VIS-501）、REPORT-501 记 PARTIAL | `工程控制/RELEASE-05/SUMMARY.md:11-13`、`ACCEPTANCE.md:20,24,26,28-29` |
| 结论已沉淀 | 部分成立：三篇 `实验/*/REPORT_paper.md`、`docs/` 多处订正、`artifacts/evidence/release-05/` 六件证据均在树内 | `git ls-files` 实测，见各份"下游" |
| 真实数据终验 | **不成立**：VIS-501 未跑完（M42 组阻塞 OQ-10、银心组链未走完、负责人未目检），L4 未过 | `SUMMARY.md:75-82`、`OPEN_QUESTIONS.md:156-238` |
| 包自己的状态声明 | `READY_FOR_OWNER_REVIEW`、"本包**未完成**，不进入 FIN" | `SUMMARY.md:3,15`、`ACCEPTANCE.md:72` |

⇒ 按 §9 现在**不得**清理本包。真正的问题是另一侧：**包内已有多处结论被后续裁决与后续提交推翻**（§6A 节 P0-1/P0-2），而 `eng/tools/doccheck/check_doc_index.py:108` 把 `CURRENT_CONTROL_PACK` 硬编码为 `工程控制/RELEASE-05`，机器门仍视其为"当前在执行的控制包"，下一个开工对话按 `工程控制/RELEASE-05/HANDOFF.md:11-16` 的开场指令读进来，就会接到已被作废的判据。

## 0.2 清理规则本身：有唯一去处，但没有归档层

- **该清哪些**：`CONTROL_PACK_SPEC.md` §9 整包（含 `tasks/` 全部 23 份 + 包级 7 份）——"任务的完整过程、证据与讨论由 git 历史承载，仓库工作区只保留当前生产代码、自解释文档集与当前在执行的控制包"。
- **清到哪**：**直接 `git rm`，无归档目录**。全仓唯一的"控制包归档位"是 `check_doc_index.py:112` 的 `ARCHIVED_CONTROL_PACKS` 白名单，其默认值 `()` 的注释写明"RELEASE-04 已按 §9 出库、整树删除，白名单无对象"（`OPEN_QUESTIONS.md:24-29` Q-3 同）——即 §9 的语义是**删除**，不是搬迁。标准 01 §2 第三条与标准 04 §4 第三条一致（过程台账不落正式文档层／历史控制包不留生产目录，需要留证的落 `artifacts/evidence/`）。
- **留证件已就位**：`artifacts/evidence/release-05/{AUDIT_PACKAGE.md,GATE-501_VERIFICATION.md,FAILCLOSED_SURVEY.md,D-14_STALE_PATH_CLEANUP.md,G2-9_CI_INCREMENTAL_STATUS.md}`（`ls` 实测 5 件 + `AUDIT_PACKAGE.md` 自述为八项索引）。
- **出库的硬前提（本批独有发现）**：任务 ID 已被**生产代码与正式文档当锚引用**。清 `工程控制/` 后立即悬空的外部引用数（命令：`git -C ... grep -l "<ID>" -- ':!工程控制' ':!reports' | wc -l`）：

  | ID | 外部引用文件数 | ID | 外部引用文件数 |
  |---|---|---|---|
  | GATE-502 | **33** | SCI-502 | 9 |
  | GATE-501 | **26** | DOC-502 | 6 |
  | PERF-501 | 14 | E2E-501 | 6 |
  | SCI-501 | 5 | VIS-501 / SCI-503 / SCI-505 | 3 |
  | SCI-504 / SCI-506 / REPORT-501 | 2 | | |

  抽样核对：`eng/ci/run.py:924`（"GATE-501（D-12 二选一落地，取「真强制」分支）"）、`eng/tests/config/check_cfg002_registry.py:556`（"GATE-502：…"）、`docs/contracts/PERF_GATE_CONTRACT.md`、`docs/contracts/DUAL_LINE_CONTRACT.md`（引 DOC-502/SCI-503）、`lib/algorithms/noise_snr/tests/p1noise/p1snr_science_test.cpp`（引 SCI-501/DOC-502）、`eng/tools/e2e/make_vis_configs.py`/`render_vis.py`（引 VIS-501）、`eng/tools/e2e/run_e2e_chain.py`（引 E2E-501）。⇒ **清理任务必须先做一轮"编号→落点改写"**（把注释里的任务编号改指 `artifacts/evidence/release-05/` 或 git 历史），否则按 §9 删目录会一次性制造 `ENGINEERING_SPEC.md:162` 规则 4 的"悬空即缺陷"。这是 `CONTROL_PACK_SPEC.md` §9 未写、`check_doc_index.py` 也不判的缺口（该门排除 `工程控制/`，只管目录残留不管反向锚）。
- **另一条反向咬合（本批读出的结构性发现）**：`CHK-DOC-HYGIENE`（`eng/ci/checks.json`，档位 fast/linux-main/windows-main，`waivable=False`）的 **D3b"进程/任务编号"命名空间前缀表是数据驱动、由 `run/` 与 `工程控制/` 的子目录名派生**（`eng/tools/doccheck/check_doc_hygiene.py:22`、`namespace_prefixes()` `:390-403`），且表空即判红（`:648-652` `D3b_namespace_table_empty`）。而 `run/` 只有 `run/.gitkeep` 入库（`git ls-files run` 实测 1 项），`.gitkeep` 又以 `.` 开头被跳过 ⇒ **干净克隆上 D3b 的前缀表唯一来源就是 `工程控制/<包名>`**。⇒ 按 §9 删掉最后一个控制包且不同时装入新包，写法门会**因表空 fail-closed 判红**（该门的 `--self-test` 甚至专门造 `工程控制/DOC-SELFTEST-01` 来供给前缀表，`:773-774`）。这不否定 §9，但说明**"收口即清理"必须与"下一包入库"同提交**，否则机器门与规范打架；同时说明 §9 所谓"仓库工作区只保留…当前在执行的控制包"实际是被门强制的不变量，而不是可自由执行的建议。

---

## 1 逐份登记

### 1.1 工程控制/RELEASE-05/tasks/DOC-502.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/DOC-502.md` |
| 标题 | `任务：DOC-502 科学定案结论订正入文档`（第 1 行） |
| 行数 | 29（`wc -l` = 29，与 `doc_mechanical.csv` 一致） |
| 角色 | 标准 01 §2 九类角色**无一项适用**——它是"把结论写进科学文档"的**过程工作指令**，属 §2 第三条"过程台账"。实际所在 `工程控制/RELEASE-05/tasks/` 是 `CONTROL_PACK_SPEC.md` §4 规定的唯一合法驻留地，**角色错放不成立**；应属目录 = 收口后从仓库移除（§9），**不得**迁入 `docs/`（§2 第三条明定不落正式文档层） |
| 主题 | 科学定案结论订正入文档（噪声口径、c_est 单位、UPM 表示能力边界、UPM 收敛语义、测光星数语义、权重退役、三口径适用域） |
| 上游 | `最高设计 §2、§4.2、§5.4、§8；三实验单元 results/ 与 REVIEW.md`（第 7 行）——四个 § 号在 `ASTROCS_DESIGN.md` 全部存在（§2:106、§4.2:254、§5.4:406、§8:542），**指对了但粒度倒挂**：本任务改的是科学文档口径，其上位条款应是 §2.1/§2.2/§2.3 三个创新点，写成整章 §2 等于没有指 |
| 下游 | 全仓命中 22 个文件（不含自身；`工程控制/` 内 14 + `reports/v19r2/**` 2 + 包外 6），**`工程控制/`、`reports/` 之外 6 个**（命令 `git grep -l "DOC-502" -- ':!工程控制' ':!reports'`）：`artifacts/evidence/release-05/AUDIT_PACKAGE.md`、`docs/contracts/DUAL_LINE_CONTRACT.md`、`lib/algorithms/noise_snr/tests/p1noise/p1snr_science_test.cpp`、`lib/infrastructure/scheduler/src/module_adapters.cpp`、`实验/absolute-snr/results/DOC_CORRECTIONS.md`、`实验/photometric-magnitude/REPORT_paper.md` |
| 是否正本 | **否**（就其内容而言）。它的 8 条步骤里 7 条是"把 X 写进 Y 文档"，**结论的正本在 `docs/`**：噪声口径 → `docs/science/NOISE_MODEL.md`/`docs/plugins/algorithms_phase1/07_noise_snr.md`；UPM 枚举 → `docs/plugins/algorithms_phase2/11_upm.md:87`；测光星数 → `docs/science/PHOTOMETRY.md:396`。任务书只是这些正本的**施工单**，不是第二正本。例外：步骤 1 的"决策树与默认路径"在任务书里是**唯一成文的口径叙述**（`docs/` 侧只写结论），若 `docs/` 未收决策树，此处构成临时承载（见"重复"列） |
| 重复或重叠对象 | 与 `docs/plugins/algorithms_phase2/11_upm.md:87`（`- \`converged\` 为**状态枚举**：\`0 = max_iter\` / \`1 = converged\` / \`2 = stalled\` / \`3 = invalid\`；`）重叠：`DOC-502.md:17`（`**UPM 收敛语义**：…converged 状态枚举 **0=max_iter / 1=converged / 2=stalled / 3=invalid** 的定义（以仓内权威 \`docs/plugins/algorithms_phase2/11_upm.md\` §4.6 为准）`）。**不算重复**——任务书自指权威、且权威就是它要写成的那份文档，属合法分层。真重复在别处：`DOC-502.md:29`（`不改公式去迁就代码；代码与文档冲突转 SCI 任务按证据修代码`）与 `ENGINEERING_SPEC.md:34`（`独立证据…证明文档与事实不符时，订正文档是义务…**文档正确而实现不符时改实现**`）是**同一纪律的两套文字**，任务书是第二份承载，且比上位少了"走变更流程 + 记录证据/影响面/一致性回归"三项义务 |
| 元信息块 | 无（第 1 行即 H1，无文档 ID/版本锚/纪律声明块；`## 权威依据` 是 `CONTROL_PACK_SPEC.md:107` 模板必备段，不算元信息块） |
| 写法违规 | `4:把 RELEASE-04 三个实验单元的定案结论与本轮裁决写入…` — 任务编号 `RELEASE-04` + 历史叙事"本轮裁决"；`7:…三实验单元 results/ 与 REVIEW.md` — 指向不存在的顶层 REVIEW.md（见悬空列）；`11:禁止改：五根文档与 PROJECT_SPEC（DOC-501 已定）` — 任务编号 + 状态叙事"已定"（状态属 `ACCEPTANCE.md` 台账，§2 第三条）；`14:…修复代码是 SCI-501，本任务只改文档` — 任务编号交叉引用（合法分层，但编号语义属控制包内部，按 §4 应指文件路径）；`19:…（与 CLEAN-501 联动删代码侧）` — 同上；`15:（SCI-B D1）`/`16:（SCI-B D2）`/`14:…（SCI-B D1）` — 实验编号 + 偏差编号 D1/D2：实验编号按 `标准04 §5` 可作证据指针保留，`D1/D2` 属过程台账编号，不可 |
| 悬空引用 | ① `7:三实验单元 results/ 与 REVIEW.md`：`实验/additive-sky-seamless/REVIEW.md` 不存在（`ls` → MISS），真实文件是 `实验/additive-sky-seamless/results/REVIEW.md`（`git ls-files 实验 | grep REVIEW` 命中该路径）；`实验/absolute-snr/results/REVIEW.md` 存在 ✓。② 步骤 8 `过 doc-index 与 doc-symbols 门` 的对象存在：`eng/tools/doccheck/check_doc_index.py` ✓、`eng/ci/checks.json:1643 "id": "CON-DOC-SYMBOLS"` ✓（本条不悬空，是**可执行性已失效**：见下列）。③ 步骤 6 的清理对象 `weight_modes`/`forbidden.weight_source_tokens` 仍在树内但语义已被改成正向登记面（`docs/contracts/DATA_SEMANTICS.md:3259,3265`、`docs/contracts/PUBLIC_API.md:2175,2186` + 机器门 `eng/ci/check_no_weight_mode.py`、`check_no_weight_mode_code.py`、`check_psfsw_retired.py` 存在）⇒ 该步骤**再跑一次即空转** |
| 与上位冲突 | 两处。① **文件域与产物落点不符**：步骤 5 说"星数不构成拒绝条件（不设适用域门槛）"，与 `ASTROCS_DESIGN.md:274`（`**测光标定不设星数门槛**…星数少到 SCI-PHOT-001 §4 冻结门…不成立时…走 NO_DATA 拟合失败路径`）和 `docs/science/PHOTOMETRY.md:396`（`1. **星数不构成拒绝条件**…不设固定星数门槛`）**完全一致**——但同一控制包的 `SUMMARY.md:25`/`ACCEPTANCE.md:56`（D-13）把它反过来写成"PHOTOMETRY.md §16.5 定案 n≥100、生产门槛仍 kMinFitStars=3 ⇒ 文档定案未落地"。冲突方是包级件，不是本任务书（详见 §6A 节 P0-1）。② `ENGINEERING_SPEC.md:160`（规则 2"正式文档…不堆任务编号与日期"）与本任务书把 7 条结论挂 `SCI-B D1/D2`、`RELEASE-04` 编号：任务书本身不受该条约束（非正式文档层），但它的**产物**受——`docs/` 侧是否因此带入编号，属别批（DB-03/04）判定范围 |
| 处置建议 | **保留**（未收口，§9 三个条件均不满足；该文件是当前唯一把 7 条口径与 7 个文档落点对应起来的施工单，`ACCEPTANCE.md:8` 的 PASS 结论也指它）。理由引 `CONTROL_PACK_SPEC.md` §9："收口即清理…随后把本控制包目录从仓库移除"——清理单元是**整包**，不是包内单份，逐份提前删会让在册状态（§6.3 任务状态词表）失去依据。**收口时随包 `git rm`，不迁 `docs/`**（标准 01 §2 第三条） |

### 1.2 工程控制/RELEASE-05/tasks/E2E-501.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/E2E-501.md` |
| 标题 | `任务：E2E-501 小批量端到端与预检矩阵`（第 1 行） |
| 行数 | 20（`wc -l` = 20，与机械列一致） |
| 角色 | 过程工作指令（标准 01 §2 无对应角色；内容最接近"操作与运营"+"标准与检查"的混合，但**按 §2 第三条不得进正式文档层**）。现目录正确，应属 = 收口后移除 |
| 主题 | 三命令串行端到端 + 运行前预检三档矩阵 + 中断/满盘原子性 + 1/N 一致性 |
| 上游 | **缺 `## 权威依据` 段**（全文只有 目标/步骤/验收门 三段）。步骤 3 内联一个上游锚"（最高设计 §6.2）"——**指错**：`ASTROCS_DESIGN.md:440` 的 §6.2 是"export（Phase3）流程"（HiPS→WCS 计划→重采样→FITS→验证→原子发布），预检合同在 **§4.5 运行前预检（三个命令通用）**（`ASTROCS_DESIGN.md:332`）。全仓其余引用者一致指 §4.5：`docs/api/CLI_PROTOCOL_V1.md:37,119`、`docs/contracts/CONFIG_CONTRACT.md:15,106`、`docs/architecture/PIPELINE.md:5` |
| 下游 | 全仓命中 15 个文件（不含自身；含 `reports/v19r2/**` 2 件），**`工程控制/`、`reports/` 之外 6 个**（命令 `git grep -l "E2E-501" -- ':!工程控制' ':!reports'`）：`eng/tools/e2e/make_e2e_configs.py`、`eng/tools/e2e/run_e2e_chain.py`、`eng/tools/quality/run_preflight_matrix.py`、`eng/ci/ID_MIGRATION_MAP.md`、`eng/ci/id_migration_map.json`（门 ID 迁移映射）、`artifacts/evidence/release-05/AUDIT_PACKAGE.md`。**注**：`eng/ci/checks.json` 不含 `E2E-501` 字面（注册的是门 ID 而非任务 ID） |
| 是否正本 | **是本主题的一处现行工作合同**（三命令端到端与预检矩阵的验收判据只在它与 `ACCEPTANCE_SPEC.md` §5 各有一套；后者是发布门层正本，前者是任务层）。它**不是**预检语义的正本（正本 = `ASTROCS_DESIGN.md` §4.5） |
| 重复或重叠对象 | 与 `ACCEPTANCE_SPEC.md:177`（`## 5. L3：小批量真实数据端到端验收`）重叠 L3 端到端的判据。`E2E-501.md:17`（`- [ ] 三命令串行 rc=0、manifest 链完整、数值在 Oracle 容差内`）对 `ACCEPTANCE_SPEC.md` §5 的同义判据——属"任务层引用发布门层"的合法分层（`CONTROL_PACK_SPEC.md` §5 模板本就要求逐任务给验收命令）。**真正的第二份正本**是 `E2E-501.md:20`（`- [ ] 产物落 output_dir、run/ 目录纪律遵守`）对 `AGENTS.md:123`（`不把运行产物散落根目录：一切输出落 \`output_dir\` 或 \`run/\`，不入库`）+ `ENGINEERING_SPEC.md:141`（`CLI 运行产物只落 \`output_dir\`；ctest 残留归 \`run/Testing_archive/\``）——同一条硬禁令在三处各写一套，任务书这一套既无 rc 判据也无落点细则 |
| 元信息块 | 无 |
| 写法违规 | `14:5. 1/N worker 全链逐位一致；分段计时记录（为 PERF/VIS 提供基线）` — 任务编号 PERF/VIS；`3:用小批量合成/真实子集验证三命令串行全链可行` — 无日期无 sha，本份最干净；`7:输出哈希与 Oracle 比对` 无编号问题。**全篇无日期、无 commit、无 sha256、无历史叙事**（机械列 dates/commit_sha/sha256/history_narrative 四栏均空，与实读一致） |
| 悬空引用 | 无文件级悬空。两处**锚失效**：① `9:预检矩阵（最高设计 §6.2）` 指错章（见上游列），按 §6.2 读不到预检内容 = 语义悬空；② `8:testdata 小子集（每数据集 3–5 帧）真实全链冒烟` 的对象存在（`ls testdata` = 11 个数据集目录）✓，但 `.fts` **不入库**（`git ls-files testdata | grep -c '\.fts$'` = **0**）⇒ 该步在干净克隆节点上不可执行，任务书未标此前提（`testdata/README.md` 的下载策略是唯一说明处，任务书未指它） |
| 与上位冲突 | **是，两处，均为一等问题**：① `14:1/N worker 全链逐位一致` 与 `ENGINEERING_SPEC.md:72`（`1 worker 与 N worker 数值一致性（判据 = **事前冻结的浮点容差**，不是逐位一致…）`）**字面相抵**——上位明文"不是逐位一致"，下位把"逐位一致"写成验收门。现行实现因归约顺序冻结而恰好两者都成立（`ACCEPTANCE.md:17,18` 记 ARCH-502/503 N=1/2/4/8/16 逐位一致），不构成误红，但这是任务层另立的一套判据表述（`docs/contracts/SCHEDULER_CONTRACT.md:33` 已把这层关系写清："合同层不要求逐位一致"，模块自有门可更严）。② `7:合成数据全链…输出哈希与 Oracle 比对` 与 `ENGINEERING_SPEC.md:72` 同一取向：以哈希/逐位代替容差判据。另有一处**覆盖失效（不是文字冲突但后果更重）**：步骤 1/2 的三命令端到端在 `eng/tools/e2e/make_e2e_configs.py:66` 与 `eng/tools/e2e/make_vis_configs.py:34` 一律钉 `pixfrac: 1.0`，而出厂模板是 `eng/packaging/config/templates/normalize.phase_config.json:11,22` 的 `pixfrac: 0.8`——`eng/tools/HANDOVER.md:186-206`（S1-18）证明生产默认口径 `pf<1` 的直写路径曾整体偏亮 0.48 星等而全链端到端测不到。⇒ E2E-501 的 `PASS`（`ACCEPTANCE.md:27`）在证据覆盖面上不成立：它跑的是生产从不用的配置 |
| 处置建议 | **保留**（本包唯一已跑通三命令真实数据全链的工作合同，且 `pixfrac` 覆盖缺口要在它身上改判据才有着落）。理由引 `CONTROL_PACK_SPEC.md` §9 收口条件未满足 + §6.3 状态词表以任务书为单位。**顺带必须修**：`§6.2`→`§4.5`、"逐位一致"改指 `ENGINEERING_SPEC.md` §5.1 的容差判据、验收门增补"含生产默认 `pixfrac` 一组" |

### 1.3 工程控制/RELEASE-05/tasks/GATE-501.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/GATE-501.md` |
| 标题 | `任务：GATE-501 门禁合理性审计与修复（B 线）`（第 1 行） |
| 行数 | 29（与机械列一致） |
| 角色 | 过程工作指令（内容属"标准与检查"域）。`CONTROL_PACK_SPEC.md` §4 目录合法驻留，角色不错放；应属 = 收口后移除 |
| 主题 | 门禁"有牙"：L2 性能门真判红、worker_balance 计算、监控字段语义、陈旧证据路径、fail-closed 普查、每门正负例、CI 增量收尾 |
| 上游 | `最高设计 §9；ENGINEERING_SPEC §10；CONTRACT-501 性能门判据与监控字段语义`（第 7 行）——§9 = `ASTROCS_DESIGN.md:669 CPU 后端与资源` ✓；`ENGINEERING_SPEC §10` = :176 机器一致性检查 ✓；CONTRACT-501 的对象 = `docs/contracts/PERF_GATE_CONTRACT.md`（存在，且引用 GATE-501）✓ |
| 下游 | 全仓命中 38 个文件（不含自身；`工程控制/` 内 10 + `reports/v19r2/**` 2 + 包外 26），包外 **26 个**（命令 `git grep -l "GATE-501" -- ':!工程控制' ':!reports'`）：`eng/ci/{run.py,run_checks.py,l2_frozen_gate.py,check_frozen_gate.py,check_worker_balance.py,failclosed_survey.py,monitor_evidence.py,id_migration_map.json,ID_MIGRATION_MAP.md}`（9）、`eng/ci/tests/test_ci001_failclosed.py`、`eng/ci/tests/test_gate_failclosed_selftest.py`、`eng/tests/backend/test_isa_*.py`（5 份）、`eng/tools/{make_capsule.py,make_rev2_capsule.py,pack_audit_package.py,quality/check_test_discriminative.py}`、`docs/ci/03_GATES.md`、`docs/contracts/PERF_GATE_CONTRACT.md`、`artifacts/evidence/release-05/`（4 件） |
| 是否正本 | **否**。四条 L2 冻结判据的正本在 `eng/ci/l2_frozen_gate.py` + `docs/contracts/PERF_GATE_CONTRACT.md`；fail-closed/正负例判据的正本在 `ENGINEERING_SPEC.md:179-182`。本文件是它们的施工单，但**把上位判据整套重抄了一遍**（见下列） |
| 重复或重叠对象 | **确证的第二份正本**：`GATE-501.md:18-19`（`5. **fail-closed 普查**：全部门禁逐项验证"缺失证据/坏证据/无输出"三种情况均判红…；6. **每门正负例**：检查器 --self-test 覆盖红/绿双向，无自测能力的检查器补齐`）对 `ENGINEERING_SPEC.md:179`（`- 每项检查有正例与负例（能红能绿）；豁免显式登记且只减不增`）+ `:181`（`- **fail-closed**：检查器在输入缺失、路径不存在、依赖不可用时判红；"文件不存在"按"无违规"通过视为假绿`）。两处段落同一主题（门禁 fail-closed + 正负例），**且已产生分叉**：上位还要求 `:182` 的"锚存活 `ANCHOR_STALE`"与 `:183` 的"注册表与 `docs/ci/01_CHECKS.md §2` 双向对齐"，任务书这套里没有——即第二份承载是**不完备副本**，按它执行会漏两项。第二处：`GATE-501.md:29`（`不通过删检查/放宽阈值求绿`）对 `AGENTS.md:195`（`门禁/判据本身不合理时改进门禁本身，配可执行正例/负例与 --self-test，而不是放松判据`）+ `AGENTS.md:127`（`不用 waiver 盖红灯`）——同一纪律第三套文字 |
| 元信息块 | 无 |
| 写法违规 | `4:修复 RELEASE-04 发现的恒真门与失效字段` — 任务编号 + 历史叙事；`14:1. **L2 性能门改真判红**：enforcement=record_and_justify 改为 fail-closed；四条冻结判据（平均利用率 ≥0.85、p50 ≥0.90、达标样本占比 ≥0.70、无连续 142s 低利用窗且无积压）` — 数值表进任务层（标准 01 §4：数值表放下层文档；正本已在 `eng/ci/l2_frozen_gate.py:10` 与 `docs/contracts/PERF_GATE_CONTRACT.md`）；`16:…（run.py:109-119 等）` — 源码行号（同 §4，且已腐烂，见悬空列）；`17:**D-14 陈旧路径**` — 台账编号 D-14；`20:7. CI 增量/并行化收尾（G2-9）` — 台账编号；`23:用历史证据回放`／`26:并行等价性 99/99 保持` — 一次性测量数字进任务层 |
| 悬空引用 | 三条**行号锚全部失效**（对象仍在，指向已错）：① `16:run.py:109-119` 现内容是 `SKIP_EXIT_CODE`/`HARD_FAILURE_VERDICTS`（`sed -n '105,125p' eng/ci/run.py`），requires_monitor 的 fail-closed 判定实际在 `eng/ci/run.py:139`、`:622`、`:1128`，mutates_workspace 在 `:924`；② `17:test_isa_variants.py:79` 现是 BENCH 解析行（`sed -n '76,82p'`），D-14 改的路径在 `:93-96`；③ `17:pack_audit_package.py:30` 尚可（`:30` 正是 D-14 注释行）——**机械列 dangling_links 对本份报 18 个**，其中行号锚 2 条已腐烂，属"机械列误报+漏报并存"：它按链接语法计数，不判行号锚（详见 §5 节）。无文件级悬空 |
| 与上位冲突 | `17:` 步骤 4 要求把输出统一到 `artifacts/evidence/prerelease-v5/` —— 与 `AGENTS.md` §7 目录速查的 `artifacts/证据与产物（artifacts/ci/<sha>/ CI 产物；artifacts/evidence/ 证据锚）` 一致，不冲突。真冲突在**可执行性方向**：步骤 4 的删除对象 `artifacts/prerelease_v5` 已不存在（`ls` → MISS，`ACCEPTANCE.md:49` D-9 记"已由 GATE-501 删除并端到端验证"）⇒ 步骤 4/验收门 `25:陈旧路径清零（grep）` 现在是**恒真门**（判据对象已被清除，重跑必绿），正是本任务自己要消灭的"恒真门"形态——按标准 01 §4/`AGENTS.md:195` 该门要么改指新对象要么随包出库 |
| 处置建议 | **保留**（B 线在册工作合同，`ACCEPTANCE.md:22` 记 PASS，但其 D-11 遗留 5 项未修：`utilization_pct` 恒 50.0 根因、`record_and_justify` 未翻转、`--self-test` 覆盖 22/78、UT-BACKEND test_05 编译缺 include、CHK-SCI-REF 档位作用域——保留是这 5 项在任务层的唯一载体）。理由引 `CONTROL_PACK_SPEC.md` §6.3（任务状态以任务书为单位）+ §9（整包清理）。**必改**：把 fail-closed/正负例两条判据删除、只指 `ENGINEERING_SPEC.md` §10（消除第二份正本），三条 `文件:行` 锚改成函数名/门 ID（行号锚在本仓的腐烂周期是周级） |

### 1.4 工程控制/RELEASE-05/tasks/GATE-502.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/GATE-502.md` |
| 标题 | `任务：GATE-502 测试体系审计与补充（B 线）`（第 1 行） |
| 行数 | 29（与机械列一致） |
| 角色 | 过程工作指令（内容属"标准与检查"）。目录合法，不错放；应属 = 收口后移除 |
| 主题 | 测试判别力：linux-main 15 红处置、CLI 协议措辞断言、CFG002-04 反例、空断言普查与防复发门、66/72 缺口补测、预检矩阵测试、测试分层复核 |
| 上游 | `最高设计 §12；ENGINEERING_SPEC §10；GAP_AUDIT G2-4..G2-7`（第 7 行）——§12 = `ASTROCS_DESIGN.md:721 验证体系` ✓；`ENGINEERING_SPEC §10` ✓；`GAP_AUDIT.md:37-40` 四条 G2-4/G2-5/G2-6/G2-7 全部在册 ✓（grep 实证） |
| 下游 | 全仓命中 **42 个文件**（不含自身；`工程控制/` 内 7 + `reports/v19r2/**` 2 + 包外 33），包外 **33 个＝本批最重**（命令 `git grep -l "GATE-502" -- ':!工程控制' ':!reports'`）：`eng/tests/**` **26 份**（api 1、backend 14、cli 2、config 1、quality 3、unit 5；含 `config/check_cfg002_registry.py`、`api/test_cli_protocol.py`、`cli/test_preflight_matrix.py`、`unit/p2_upm_synthetic_test.cpp` 等）、`eng/ci/{id_migration_map.json,ID_MIGRATION_MAP.md}`、`eng/tools/quality/check_test_discriminative.py`、`eng/packaging/config/filters.json`、`docs/contracts/CONFIG_CONTRACT.md`、`docs/modules/MODULE_MAP.yaml`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md` || 是否正本 | **否**。测试判据正本 = `ENGINEERING_SPEC.md` §5 + `docs/contracts/TEST_MATRIX.md`；空断言防复发门正本 = `eng/tools/quality/check_test_discriminative.py`。任务书是施工单 |
| 重复或重叠对象 | 与 `GATE-501.md:19`（`6. **每门正负例**：检查器 --self-test 覆盖红/绿双向，无自测能力的检查器补齐`）重叠于 `GATE-502.md:26`（`- [ ] 每个新增/修改测试有红绿双向证据（先验证它能红）`）——同包内两份任务书各写一套"必须能红"，而同一命题在 `ENGINEERING_SPEC.md:179` 已有正本：**三份**。属同层互叠（不是下钻），应合并到上位一条。另与 `AGENTS.md:195` 的重叠同 1.3 |
| 元信息块 | 无 |
| 写法违规 | `4:清除断言不存在措辞、失效反例、空断言` — 历史叙事（"失效"）；`7:…GAP_AUDIT G2-4..G2-7` — 台账编号；`11:- 禁止改：lib/、eng/ci 注册表（GATE-501 域）` — 任务编号（合法交叉引用但按 §4 应指文件路径）；`14:linux-main profile 15 条内容面红逐条处置` — 一次性计数；`17:（RELEASE-04 已发现 io_adapter 一例）` — 任务编号 + 历史叙事"已发现"；`18:66 路径缺口/72 能力缺口` — 一次性计数；`23:linux-main 15 红全部闭环` — 同上。`doc_mechanical.csv` 的 `task_ids` 列本行只报 `['RELEASE-04']`、`dangling_links=[18]`：**漏报了 GATE-501、G2-4..G2-7、FIX 系列与 D 系列编号**（它按 `EXP-/RELEASE-/FIX-/AUD-` 前缀匹配，`G2-4`/`D-11`/`CLEAN-501` 不在词表内） |
| 悬空引用 | 无。逐项核：`docs/api/**` ✓（`docs/api/CLI_PROTOCOL_V1.md` 存在）、`eng/packaging/config/filters.json` ✓、`eng/tests/config/check_cfg002_registry.py` ✓（步骤 3 的 bader 对象已按任务改造成现行失配反例：`check_cfg002_registry.py:556` 注释"RELEASE-04 换版后最高设计 §3.3 的 'bader r' 反例行已删"，夹具 `eng/tests/config/fixtures/negative/unknown_filter.phase_config.json:13` 仍用 `bader r` 作型号错反例，`test_cfg002_registry.py:60` 把它登记为 `non_key_examples`）⇒ 步骤 3 已闭合、对象真实。`linux-main` profile 存在（`eng/ci/checks.json:7,41,215`）✓。**唯一失效**：步骤 1 的"15 条红"清单来自 `ACCEPTANCE.md`/RELEASE-04 归档，RELEASE-04 树已删（`git ls-files 工程控制 | grep RELEASE-04` = 0 命中）⇒ 该数字在当前树不可复核 |
| 与上位冲突 | 两处字面口径分叉：① `18:` 与 `23:` 把"66/72 缺口"与"15 条红"当作在册对象，上位 `ENGINEERING_SPEC.md:170` 规定"控制包收口…过程留 git 历史"，这类一次性计数应在收口时并进证据件而不是留在任务层——现已成不可核数字；② `18:`"每个'待实现'项补对应测试（先红后绿或标记 DEFERRED 不进全绿集）"与 `docs/owner/RELEASE_STATUS.md:19` 的 `DEFERRED` 定义（"任务图/负责人裁决明确不在本轮范围"，判据要"控制包任务图条目"）一致 ✓，无冲突。**真冲突在验收门与结果的差**：`23:` 要"全量 ctest 零红"，`ACCEPTANCE.md:55` D-16 记 ④"2 条 UT-BACKEND 持续红：`test_p2007::test_04` 峰值 RSS 1.45GB 超 512MB 界、`test_p2003::test_02` 校正后 median 恶化"⇒ GATE-502 判 PASS 而它自己的第 1 条验收门（15 红全闭环）在册记录里被推翻。**属"状态词跨轴串用"**（前言 §5.6）：域外派单（D-16）不等于该门已达成 |
| 处置建议 | **保留**（B 线在册工作合同；D-16 六条域外派单的唯一任务层落点）。理由引 `CONTROL_PACK_SPEC.md` §6.3 + §9 未达收口条件。**必改**：删与 `ENGINEERING_SPEC.md:179` 重复的"红绿双向证据"一条（指上位即可）；把 15/66/72 三个一次性计数改指 `artifacts/evidence/release-05/` 内的普查件；PASS 状态与 D-16 的矛盾由前台在 `ACCEPTANCE.md` 侧订正（不在本份文件域内） |

<!-- PROGRESS: 已审 4/13 -->

### 1.5 工程控制/RELEASE-05/tasks/PERF-501.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/PERF-501.md` |
| 标题 | `任务：PERF-501 探针驱动的编排性能优化（A 线，最后）`（第 1 行） |
| 行数 | 26（与机械列一致） |
| 角色 | 过程工作指令（内容跨"操作与运营"+"标准与检查"）。目录合法（`CONTROL_PACK_SPEC.md` §4），不错放；应属 = 收口后移除 |
| 主题 | 探针驱动的三阶段编排优化（串行段、读放大、内存峰值） |
| 上游 | `最高设计 §9、§8.3；GAP_AUDIT G3-3；CONTRACT-501 性能门判据`（第 7 行）——§9 = `ASTROCS_DESIGN.md:669` ✓；§8.3 = `:597 三个阶段调度器` ✓（`eng/tools/HANDOVER.md:246` 明文"§8.3 编排策略六条是内存治理的正本；**下游不得复述**"——见冲突列）；G3-3 = `GAP_AUDIT.md:50` ✓ |
| 下游 | 全仓命中 26 个文件（不含自身；`工程控制/` 内 10 + `reports/v19r2/**` 2 + 包外 14），包外 **14 个**（命令 `git grep -l "PERF-501" -- ':!工程控制' ':!reports'`）：`docs/architecture/PERFORMANCE_MODEL.md`、`docs/architecture/THREADING_MODEL.md`、`docs/contracts/SCHEDULER_CONTRACT.md`、`docs/plugins/infrastructure/{19_runtime.md,21_observability.md}`、`lib/include/astrocs/core/{block_flow.h,mosaic_window.h,normalize_workflow.h}`、`lib/infrastructure/scheduler/src/module_adapters.cpp`、`eng/tests/unit/variance_floor_test.cpp`、`eng/tools/monitoring/{concurrency_sweep.py,node_waterfall.py}`、`eng/tools/quality/resource_monitor.py` |
| 是否正本 | **否**，且其现有文本与三份正式文档构成同一主题的两套表述（性能判据正本 = `docs/contracts/PERF_GATE_CONTRACT.md` + `eng/ci/l2_frozen_gate.py`；编排策略正本 = `ASTROCS_DESIGN.md` §8.3；探针事件正本 = `docs/plugins/infrastructure/21_observability.md`） |
| 重复或重叠对象 | `PERF-501.md:20`（`- [ ] L2 真门四条判据（均值、p50、达标占比、低利用窗且无积压）达标，或给出有证据的硬件/算法上限并登记 OPEN_QUESTIONS`）与 `GATE-501.md:14`（`四条冻结判据（平均利用率 ≥0.85、p50 ≥0.90、达标样本占比 ≥0.70、无连续 142s 低利用窗且无积压）任一违规 verdict=red`）——**同一套判据在本包两份任务书里各写一遍**，两份都不带指向既有登记 `eng/ci/l2_frozen_gate.py:10`（`违规（平均 0.245<0.85、p50 0.039<0.90、达标占比 0.069<0.70、连续 142.0s`）的指针；数值正本按 `CONTROL_PACK_SPEC.md` §5 模板应在"权威依据"里给条款号，此处给的是自设文字。第二处：`PERF-501.md:16`（`内存：峰值 RSS 给预算对照表（稠密 SNR 不驻留、块即时回收、窗口/子块流式）`）对 `ASTROCS_DESIGN.md` §8.3 编排六条 —— `eng/tools/HANDOVER.md:246` 明文禁止下游复述 |
| 元信息块 | 无 |
| 写法违规 | `4:消除 RELEASE-04 实测瓶颈：阶段串行段（1→16 worker 仅 2.37×、均值 3.92/16 核、p50 0.62 核）与约 3.9GB/帧读放大` —— 任务编号 + **历史实测数值表进任务层**（标准 01 §4：数值表放下层文档）；`7:GAP_AUDIT G3-3` 台账编号；`15:同 registry SHA 下 N=1/2/4/8/16 扫描 + 逐位一致性 + 探针对比` —— "逐位一致性"作为判据（见冲突列）；全篇无日期、无 commit、无 sha256（机械列一致） |
| 悬空引用 | 无文件级悬空：探针门与登记在册（`ACCEPTANCE.md:60` `CHK-SCHED-PROBE-SCHEMA`(6/6)）、步骤 4 的 `docs/architecture` ✓。但**步骤 1 无落点**："基线测量：16 帧合成 mosaic 全链"在仓内没有对应夹具——端到端侧只有 `eng/tools/e2e/run_e2e_chain.py`（真实数据、非 16 帧合成），`eng/tools/monitoring/concurrency_sweep.py` 是并发扫描工具而非 mosaic 全链驱动。任务书未给可复跑命令 ⇒ 属"不可执行"，不属"悬空"（前言 §5.6 分档） |
| 与上位冲突 | **四处**：① `15:`/`22:`"逐位一致性"对 `ENGINEERING_SPEC.md:72`（"判据 = **事前冻结的浮点容差**，不是逐位一致"）与 `docs/contracts/SCHEDULER_CONTRACT.md:33`（"合同层不要求逐位一致"）；② **前置不可满足**——`10:` 要求 ARCH-505+CLEAN-501 完成，`SUMMARY.md:12-13` 记 ARCH-505 BLOCKED（OQ-9）、CLEAN-501 NOT_RUN，而 OQ-9 属 `AGENTS.md` §10 第 1 类（顶层合同结构性变更）只能负责人裁决 ⇒ 本任务在现行授权下不可能开工；③ **与本包自己的后续实测结论相反**——`13:` 指定测 mosaic，`HANDOFF.md:88-93`（同包、更新提交）实测"normalize (P1) **原本完全帧串行**／mosaic (P2) **已经是帧级/tile 级并行**／export (P3) resample 已是行带并行…**真正的并行缺口只在第一阶段**"，根因是 Phase1 dispatch 从未注入 `__workers`；④ `4:` 的 2.37×/3.92 核归因于"阶段串行段"，同被 `HANDOFF.md:93` 订正为 P1 独有 |
| 处置建议 | **保留 + 就地订正**（4 条 NOT_RUN 之一，收口条件不满足，且它是 A 线性能优化在本包唯一的工作合同）。理由引 `CONTROL_PACK_SPEC.md` §9（收口前不移出）+ 标准 01 §1（"下位文档与上位文档冲突时以上位为准；**冲突本身是缺陷**，需在整治中消除"）。订正三项：删 L2 四条数值改指 `docs/contracts/PERF_GATE_CONTRACT.md`；"逐位一致性"改指 `ENGINEERING_SPEC.md` §5.1；步骤 1 测量对象按 `HANDOFF.md:88-93` 改为 normalize 帧级并行。**整份作废亦可取**——若由 AUDIT-06 后的新工作包重述，须前台裁决后把 NOT_RUN 状态一并迁移，不得留"过期待办"（§6A 节 P0-3） |

### 1.6 工程控制/RELEASE-05/tasks/REPORT-501.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/REPORT-501.md` |
| 标题 | `任务：REPORT-501 审核包与最终报告`（第 1 行） |
| 行数 | 25（与机械列一致） |
| 角色 | 过程工作指令，其**交付物**属"负责人面"（标准 01 §2 表：发布状态/验收/裁决记录 → `docs/owner/`）。任务书留在 `工程控制/` 正确、结论已落 `docs/owner/RELEASE_STATUS.md` 与 `artifacts/evidence/release-05/` ⇒ **角色不错放**；应属 = 收口后移除 |
| 主题 | 审核包八项汇总 + 给负责人的四问结论页 |
| 上游 | **缺 `## 权威依据` 段，全文无任何最高设计条款指针**（机械列 `upstream_pointer=False` 属实，本批唯二之一，另两份见 1.11/1.13）。应指 `ASTROCS_DESIGN.md` §12.4/§13 与 `ACCEPTANCE_SPEC.md` §7/§8。标准 01 §3 判不合规 |
| 下游 | 全仓命中 11 个文件（不含自身；`工程控制/` 内 7 + `reports/v19r2/**` 2 + 包外 2），包外 **2 个**：`artifacts/evidence/release-05/AUDIT_PACKAGE.md`（其第 1 行标题即"RELEASE-05 审核包索引（REPORT-501 §1–§7）"、第 3 行以本任务书为依据）、`artifacts/evidence/release-05/GATE-501_VERIFICATION.md` |
| 是否正本 | **是**（本包审核包结构的唯一成文处：八项清单只在此列出，`ACCEPTANCE_SPEC.md:247` §7 是上位"验收产物与报告"而非这八项）。产物侧另有索引件，属合法分层（索引件显式指回本文字） |
| 重复或重叠对象 | `REPORT-501.md:25`（`FIN 动作（README 更新、VERSION/宏改 0.0.1alpha、控制包收口）等负责人明确指令后执行`）对 `AGENTS.md:124`（`不宣布发布：发布决定只属负责人`）与 `ASTROCS_DESIGN.md:817`（`**最终发布决定只属项目负责人**；agent 至多声明 READY_FOR_OWNER_REVIEW`）——**同一条纪律三处各写一套**，且任务书这套把"发布"扩成"FIN 动作"三项（README/版本号/控制包收口）＝第二份口径。第二处：`20:`（`是否具备发布 0.0.1alpha 预览的条件（agent 只给建议，决定权在负责人）`）对 `ACCEPTANCE_SPEC.md:260-276` 九条发布门——任务书压成"四问"，**丢了第 7 条仓库整洁、第 8 条版本纪律、第 9 条日志与错误系统验收**（不完备副本，按它自查会漏三门） |
| 元信息块 | 无 |
| 写法违规 | `4:负责人认可成品帧后才进入 FIN（收尾另行授权）` — 状态叙事；`6:## 审核包内容（建议放 artifacts/evidence/release-05/ …）` — **把任务编号固化进正式证据目录名**（标准 01 §4 禁任务编号；实测该目录 5 件证据 + `ACCEPTANCE.md:5` 全部以此为锚，出库时须连带处理）；`7:三篇论文式实验报告（SCI-503/504/505 产物路径）`、`8:ACCEPT-501 独立验收报告` — 任务编号交叉引用；`25:VERSION/宏改 0.0.1alpha` — 写死版本号且已作废（冲突列）；全篇无日期、无 sha256、无 commit |
| 悬空引用 | `9:OPEN_QUESTIONS.md` ✓（239 行在册）；`7:` 三篇 `实验/*/REPORT_paper.md` ✓ 全部存在；`10:` 性能报告——**无数据**（`SUMMARY.md:72`"PERF-501 未执行…没有端到端优化前后对照与计时热点表"）；`11:` 视觉验收两张 FITS/PNG——产物落 `run/`（`.gitignore` 且已回收：`ls run/RELEASE-05` → MISS，`git ls-files run` 只返回 `run/.gitkeep`）⇒ 第 5 项证据当前树内不存在、不可从克隆重建；`ACCEPTANCE.md:29` 记 REPORT-501 PARTIAL"SUMMARY/审核包未成文"，与 `SUMMARY.md`（104 行）+ `AUDIT_PACKAGE.md`（成文、自述为 §1–§7 索引）**矛盾** ⇒ 该状态行指向的"未成文"对象其实已在，属把已完成写成待办（§6A 节 P0-4） |
| 与上位冲突 | 两处硬冲突：① `25:` 的"VERSION/宏改 0.0.1alpha"已被现实作废——根 `VERSION` 现为 `0.1.0-alpha.1`（`cat VERSION`；变更提交 `e9e4b603 产品版本进入 0.1.0-alpha.1，版本条款按单一事实源对齐`），而 `ASTROCS_DESIGN.md:808` + `ENGINEERING_SPEC.md:145` 定"根 `VERSION` 是产品版本唯一事实源"⇒ 任务书自设了与单源并列的第二套版本口径，且该值从未生效；② `10:`-`11:` 要求的性能/视觉证据在本阶段被上位禁止产出——`eng/tools/HANDOVER.md:70-83`（负责人："**先停止端到端**…**全部完成前不要再做任何测试与端到端**"）⇒ 八项中第 4、5 项在现行授权下**不可完成**，本任务已不是"待办"而是"待改判" |
| 处置建议 | **保留 + 就地订正**（它是负责人阅读件结构与四问口径的唯一成文处，出库后无处重建；且必须先把第 4/5 项标注为"待解禁后由新工作项承接"才能谈收口）。订正：补 `## 权威依据` 段指 `ASTROCS_DESIGN.md` §12.4/§13 + `ACCEPTANCE_SPEC.md` §7/§8（标准 01 §3）；删"VERSION/宏改 0.0.1alpha"改指根 `VERSION` 单源（标准 01 §1：冲突即缺陷）；八项清单与 `ACCEPTANCE_SPEC.md` §8 九条发布门做一次对齐（补齐缺的三条或改指上位不复制） |

<!-- PROGRESS: 已审 6/13 -->

### 1.7 工程控制/RELEASE-05/tasks/SCI-501.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/SCI-501.md` |
| 标题 | `任务：SCI-501 SNR 噪声项双计修复（FIX-407）`（第 1 行） |
| 行数 | 27（与机械列一致） |
| 角色 | 过程工作指令（内容属"算法推导/科学定义"的修复施工单；科学口径不在此层承载，现由 `docs/` 承载）。目录合法，不错放 |
| 主题 | σ_sky 双计读噪修复（噪声项组合语义显式声明 + MC 复算 + 双计负例） |
| 上游 | `最高设计 §2.2、§4.4；实验/absolute-snr/results/REVIEW.md（D1）、DOC-502 冻结的噪声口径`（第 7 行）——§2.2 = `ASTROCS_DESIGN.md:130 创新点二：跨帧可用的绝对信噪比` ✓ 正解；**§4.4 指偏**：`:316` 是"输出合同（Phase1）"，与噪声项组合无直接关系；本主题真正上位是 `docs/science/CONTROL_WEIGHT_SNR.md:201` 与 `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.2a。`实验/absolute-snr/results/REVIEW.md` ✓ 存在 |
| 下游 | 全仓命中 16 个文件（不含自身；`工程控制/` 内 9 + `reports/v19r2/**` 2 + 包外 5），包外 **5 个**（命令 `git grep -l "SCI-501" -- ':!工程控制' ':!reports'`）：`lib/algorithms/noise_snr/tests/p1noise/p1snr_science_test.cpp`、`lib/algorithms/noise_snr/tests/p1noise/CMakeLists.txt`、`实验/absolute-snr/REPORT_paper.md`、`实验/absolute-snr/results/DOC_CORRECTIONS.md`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md` |
| 是否正本 | **否**。双计口径的正本已就位：`docs/plugins/algorithms_phase1/07_noise_snr.md:113-120`（`sigma_sky_source` 决策表 + 两条默认路径）、`docs/science/NOISE_MODEL.md:324`、`docs/science/CONTROL_WEIGHT_SNR.md:201`。任务书是施工单＝合法分层；**但它 `4:` 目标行仍自带一份数值口径，与三份正本携带的数值不一致**（冲突列，本批最高价值发现） |
| 重复或重叠对象 | `SCI-501.md:14`（`按 DOC-502 冻结的决策树实现：σ_sky 输入语义显式声明（散粒 only / 经验总 rms），两条路径各加测试锁口径`）对 `docs/plugins/algorithms_phase1/07_noise_snr.md:119-120`（`1. sigma_sky_source=shot_noise_only 且 gain_e_per_adu>0 && read_noise_e>0 ⇒ 走 σ_sky,散粒²+(RN/g)²+F·P_i/g（设计本意路径）；2. sigma_sky_source=empirical_total_rms ⇒ 只加源泊松项，**不再加** (RN/g)²`）——**合法分层**（任务书显式以文档为准、不声明自己是口径），不计重复。真重复在 `27:`（`不改 SNR 定义式本身（定义已被实验证实）`）对 `AGENTS.md:118`（`不动科学公式、默认容差、SCI/ALG 冻结定义（除非按变更流程批准）`）与 `ENGINEERING_SPEC.md:33`——同一红线两套文字，且任务书这套**删掉了"除非按变更流程批准"的出口**，比上位更严 ⇒ 不完备的第二份正本 |
| 元信息块 | 无 |
| 写法违规 | `1:任务：SCI-501 SNR 噪声项双计修复（FIX-407）` — 标题携带上一轮缺陷编号（机械列 task_ids 正确报 `['FIX-407']`）；`4:修复前 σ_F 高估 +12.8%（基线）~+36.6%（最坏）` — 历史实测数值进任务层；`10:lib/infrastructure/scheduler/src/module_adapters.cpp 约 4258 行附近（σ_sky 组合）` — **源码行号锚**（标准 01 §4：源码行号放下层文档）；`15:26+29 扫描点全部 ≤3σ` — 一次性计数；`17:生产 C++ 与 Python 镜像对拍恢复 2.24e-14 量级` — 历史测量值 + "恢复"历史叙事。无日期、无 commit、无 sha256 |
| 悬空引用 | `10:` 的行号锚**已腐烂**：`sed -n '4252,4266p' lib/infrastructure/scheduler/src/module_adapters.cpp` 实测该行区是 P1 star-psf 节点的 manifest 写出（`n_fit_input`/`psf_mode`），与 σ_sky 无关；修复后的真实声明点在 `module_adapters.cpp:7159`（`cfg.sigma_sky_source = SNR_SIGMA_SKY_EMPIRICAL_TOTAL_RMS;`），漂移 ≈ +2900 行（该文件现 15532 行）。**文件在、指向的内容不在**。其余对象均在：`lib/algorithms/noise_snr/**` ✓、步骤 5 的 ctest 目标 ✓（`eng/tests/unit/p1snr/*.cpp` 4 份 + `lib/algorithms/noise_snr/tests/p1noise/`）。⇒ 机械列 `dangling_links=[]` 对本条**漏报**（它按链接语法判，不核 `文件:行` 型锚） |
| 与上位冲突 | 一处科学文档侧实质不一致（本批最高价值）：**任务书给的最坏值正确、三份正式文档给的上限偏低**。`4:` 的"+12.8%（基线）~+36.6%（最坏）"与 `实验/absolute-snr/README.md:111`（"最坏点 **+36.6%**（B1 暗源 F=100 e⁻、B=0），B2 的 RN 扫描最坏 +34.0%（RN=50）"）一致，且 `results/REVIEW.md:186`（M6）明确点名"最坏 +34.0% 低估了自己的最坏值"、`README.md:319` 已按 M6 订正为"+36.6%（最坏）／+34.0% 标注来源"。而 DOC-502/SCI-501 产出的三份正式文档**全部只写 +12.8%~+34.0%**（`docs/science/NOISE_MODEL.md:324`、`docs/plugins/algorithms_phase1/07_noise_snr.md:123`、`docs/science/CONTROL_WEIGHT_SNR.md:201`），即只引 b2 未纳入 b1 ⇒ **科学文档低估了自己登记的缺陷量级 2.6 pp**，而 DOC-502 验收门第 3 条（`DOC-502.md:26` 独立子代理审稿：文档表述与实验 results/JSON 数值一致）在 `ACCEPTANCE.md:8` 记为 PASS。② 澄清一处不误判：`23:` 只写"1/N worker 一致"（未写逐位），**不与** `ENGINEERING_SPEC.md:72` 冲突——本批"逐位一致"与上位相抵只发生在 E2E-501/PERF-501/GATE-502 三处（前言 §5.6 措辞不得串档） |
| 处置建议 | **保留 + 就地订正**（未收口；它仍是 SNR 双计"发现→修复→负例"在任务层的唯一叙述载体，`步骤 4` 的镜像对拍按 `ACCEPTANCE.md:46` D-6 仍未复跑）。订正：删 `约 4258 行附近` 改指符号 `p1_op_noise` / `sigma_sky_source`；`27:` 禁改条款改指 `AGENTS.md` §6 + `ENGINEERING_SPEC.md` §3（消除第二份正本）；**+34.0%/+36.6% 口径差属科学文档订正**，按 `AGENTS.md` §8 变更流程上呈（本节点只读，不代改），并在回执登记 DOC-502 的 PASS 结论对此不成立 |

### 1.8 工程控制/RELEASE-05/tasks/SCI-502.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/SCI-502.md` |
| 标题 | `任务：SCI-502 UPM 三个生产缺陷修复`（第 1 行） |
| 行数 | 27（与机械列一致） |
| 角色 | 过程工作指令（内容属"算法推导"修复施工单）。目录合法；应属 = 收口后移除 |
| 主题 | UPM 三缺陷：绝对容差不收敛（FIX-1）、converged 状态缺档（FIX-2）、正规方程近奇异（FIX-3） |
| 上游 | `最高设计 §5.4；实验/additive-sky-seamless/README.md §10（FIX-1/2/3）；DOC-502 收敛语义`（第 7 行）——§5.4 = `ASTROCS_DESIGN.md:406 天光平面与统一相对模型（UPM）` ✓；`实验/additive-sky-seamless/README.md:384` 确有 `## 10. 生产缺陷登记（FIX，**未改任何生产代码**）`，FIX-1/2/3 三行在 `:388-390` ✓ 可核 |
| 下游 | 全仓命中 19 个文件（不含自身；`工程控制/` 内 8 + `reports/v19r2/**` 2 + 包外 9），包外 **9 个**（本批"任务 ID 被生产件当锚"最典型；命令 `git grep -l "SCI-502" -- ':!工程控制' ':!reports'`）：`CMakeLists.txt`（根构建图）、`lib/algorithms/coverage/src/upm.cpp`、`lib/infrastructure/scheduler/src/module_adapters.cpp`、`lib/include/astrocs/core/mosaic_window.h`、`eng/tests/unit/v6_p2_sky_kappa/CMakeLists.txt`、`eng/tests/backend/test_p2003_seam_oracle.py`、`eng/ci/{id_migration_map.json,ID_MIGRATION_MAP.md}`、`实验/additive-sky-seamless/REPORT_paper.md`。**注**：`eng/tests/unit/p2_upm_synthetic_test.cpp` 不在命中集（它引的是门 ID 而非任务 ID） |
| 是否正本 | **否**。converged 枚举正本是 `docs/plugins/algorithms_phase2/11_upm.md:87`（`converged 为**状态枚举**：0 = max_iter / 1 = converged / 2 = stalled / 3 = invalid`）——任务书 `15:` 明文"以仓内权威…为准"，**指对且不另立口径**（合法分层）；κ/容差正本 = `docs/science/PHASE2_UPM.md` §5、`docs/algorithms/PHASE2_UPM_IMPL.md` |
| 重复或重叠对象 | `SCI-502.md:15` 对 `docs/plugins/algorithms_phase2/11_upm.md:87`（同串枚举）——**属合法分层**，两处段落已贴出供核。另与 `实验/additive-sky-seamless/README.md:388-390`（FIX-1/2/3 登记表，含同一 `module_adapters.cpp:5948-5949` 锚）重叠：任务书是登记表的下游指令，实验侧是证据正本 ⇒ 不算重复；**但同一腐烂锚被两处各写一遍**（实验 README 自注"行号在 2026-09-21 由并行任务的合规回退改动后核对于 15:0x"＝把行号锚与日期固化进证据件） |
| 元信息块 | 无 |
| 写法违规 | `1`/`7`/`14`/`15`/`16` 通篇 `FIX-1/FIX-2/FIX-3`（机械列正确报出）——按 `标准04 §5`"实验编号作为证据指针可保留并指向实验单元"，本处指向 `实验/additive-sky-seamless/README.md §10` 真实在册 ⇒ **可接受**；但与 RELEASE-04 的 `FIX-4xx` 系列**同号不同物**（`SCI-501.md:1` 用 `FIX-407`、`SCI-506.md:7` 用 `FIX-408`），三套 FIX 编号共存且 RELEASE-04 树已删 ⇒ 歧义（§6A 节 P1-4）；`10:module_adapters.cpp 约 5948-5949 行` — 源码行号；`16:（真实 M42 样本 κ=3.16e7）` — 历史测量值；`17:纯加性 12.5× 接缝压缩、非退化判据 36.4σ、稀疏-稠密等价 3.1e-15` — 数值表进任务层。无日期/commit/sha256 |
| 悬空引用 | ① `10:` 声明的文件域 `\`lib/algorithms/upm/**\`` **在树里但没有可改的代码**：`find lib/algorithms/upm -type f` = `README.md`、`module.yaml`、`memory.md` 三份，其 `module.yaml:2-4` 自述"状态: CONTRACT_READY —— 实现存在（生产源 `lib/algorithms/coverage/src/upm.cpp` 1565 行…）"，即模块化迁移未做 ⇒ 真实修复落点 = `lib/algorithms/coverage/src/upm.cpp` + `lib/infrastructure/scheduler/src/module_adapters.cpp:9561-9573`（`uc.tolerance_relative = 1;`），**越出声明域**；② `10:` 的 `约 5948-5949 行及 out_converged` 锚已腐烂（`sed -n '5944,5952p'` 实测该行区是 P2 apply 的逐帧判决 `PHOT_SCALE_MISSING`），漂移 ≈ +3600 行；③ `11:禁止改：SCI-C 实验脚本的判据` ✓ 对象存在（`实验/additive-sky-seamless/code/`）。⇒ 机械列 `dangling_links=[]` 对 ①② **漏报** |
| 与上位冲突 | ① **文件域不可执行**（上列①）：`CONTROL_PACK_SPEC.md` §5 模板规定"改动范围（文件域，互斥）"，本域内无代码可改，双线互斥判定（`00_README.md:45`）对真实落点失效；② **验收门与在册结果矛盾**：`21:- [ ] M42 真实样本有限迭代 converged=1（给迭代数与残差表）` 与 `24:- [ ] SCI-C 判据复跑全绿`，被同包 `ACCEPTANCE.md:47`（D-7："M42 真实样本未用修复后库复跑（κ=3.16e7 取自归档 `c7_realdata.json`）；c1–c7 亦未复跑。**未完成项**"）直接推翻，而 `ACCEPTANCE.md:10` 记本任务 **PASS** ⇒ 状态词跨轴串用（前言 §5.6）：**代码修好 ≠ 验收门达成**；③ `26:不通过放宽接缝判据换取收敛` 与 `AGENTS.md:195`、`GATE-501.md:29` 同义三写 |
| 处置建议 | **保留 + 就地订正**（D-7 复跑仍是待办，`实验/additive-sky-seamless/results/{c1..c7}*.json` 七件均在树内可复跑；文件域必须改）。订正：`10:` 域改指 `lib/algorithms/coverage/**` + `lib/infrastructure/scheduler/**`，`lib/algorithms/upm/` 的迁移另立任务（`标准04 §1` 要求阶段会话/模块归并后目录退役，该骨架目录本身是整改项）；行号锚换符号名；**PASS 与 D-7 的矛盾由前台在 `ACCEPTANCE.md` 侧改判**（不在本份文件域内，不越权） |

<!-- PROGRESS: 已审 8/13 -->

### 1.9 工程控制/RELEASE-05/tasks/SCI-503.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/SCI-503.md` |
| 标题 | `任务：SCI-503 论文式实验报告 · 测光星等坐标系`（第 1 行） |
| 行数 | 27（与机械列一致） |
| 角色 | 过程工作指令；交付物（`实验/photometric-magnitude/REPORT_paper.md`）属"研究与佐证"（标准 01 §2 → `docs/research/`/`实验/` 证据单元）。**任务书目录合法；交付物落在 `实验/` 与 §2 第三条一致（证据语料不入正式文档层）** |
| 主题 | SCI-A 测光星等坐标系的论文式报告（八段结构） |
| 上游 | `ACCEPTANCE_SPEC §2.1；实验/photometric-magnitude/{README.md,results/*}`（第 7 行）——`ACCEPTANCE_SPEC.md:35` `### 2.1 SCI-A：测光校准到测光星等坐标系` ✓ 精确；**未指最高设计条款**（§2.1/§4.2 侧的 `ASTROCS_DESIGN.md:120 创新点一` 才是源头）。标准 01 §3 要求标"上游最高设计条款"⇒ 半合规（有上位、层级差一级） |
| 下游 | 全仓命中 12 个文件（不含自身；`工程控制/` 内 7 + `reports/v19r2/**` 2 + 包外 3），包外 **3 个**（命令 `git grep -l "SCI-503" -- ':!工程控制' ':!reports'`）：`artifacts/evidence/release-05/AUDIT_PACKAGE.md`、`docs/contracts/DUAL_LINE_CONTRACT.md`、`eng/tools/doccheck/check_doc_hygiene.py`（`docs/contracts/DUAL_LINE_CONTRACT.md:19` 用"归 SCI-503/504/505"划定报告面归属；`check_doc_hygiene.py:119` 为此串**专门登记了一条写法豁免白名单**——即该任务编号已被卫生门的机器判据依赖，出库时不同步处理会让豁免项指向不存在的对象） |
| 是否正本 | **否**。SCI-A 结论的正本 = `docs/science/PHOTOMETRY.md`（科学定义层）；`REPORT_paper.md` 是负责人阅读件（研究/佐证层，按 §2"以引用方式服务正本，不重复承载正本口径"）。任务书本身是施工单 |
| 重复或重叠对象 | `SCI-503.md:19`（`**讨论**：误差预算各项…G6/N5 低星数边界（写明星数**不构成拒绝条件**、§4 求解前提不成立即 NO_DATA 拟合失败）`）对 `ASTROCS_DESIGN.md:274`（`**测光标定不设星数门槛**…星数少到 SCI-PHOT-001 §4 冻结门（\|r_consistent\| ≥ 3 才进 IRLS）不成立时，拟合**本就不产出标度** ⇒ 走 NO_DATA 拟合失败路径`）与 `docs/science/PHOTOMETRY.md:396`（`1. **星数不构成拒绝条件**…不设固定星数门槛`）——三处同串。判定：**合法分层**（任务书→文档→报告三级各写其职责，报告 `REPORT_paper.md:242` 已回指"PHOTOMETRY.md §16.5 第 1–2 条"），**不计第二份正本**。真正的第二份正本在别处且方向相反：`TASK_LIST.md:12` 把本任务的验收门写成"**n≥100 适用域表述**"（见 §6A 节 P0-1） |
| 元信息块 | 无 |
| 写法违规 | `4:把 photometric-magnitude 实验单元提炼为…供负责人阅读裁决` — 无编号问题；`11:- 不改：…正式科学文档（DOC-502 域）` — 任务编号交叉引用（合法分层）；`19:G6/N5` — 实验判据编号（`标准04 §5` 允许作证据指针，实测 `实验/photometric-magnitude/results/step7_negatives.json` 载 N5，可核）；`24:篇幅以讲清为准（建议 8–15 页）` — 非缺陷。**全篇无日期、无 commit、无 sha256、无历史叙事**（机械列一致，`history_narrative=False` ✓） |
| 悬空引用 | 无：`实验/photometric-magnitude/README.md` ✓、`results/` ✓、交付物 `REPORT_paper.md` ✓ 已在树（`ACCEPTANCE.md:11` 记 1efb5c9e 提交）。步骤 3 的"Gaia XP 光谱×QE×透过率积分"公式落点 `PHOTOMETRY.md` §2a.1 ✓（`:403` 第 7 条明定"F_syn 的唯一权威写法是 §2a.1"）。**本批唯一"验收门不可机器复跑"的份**：`26:独立子代理通读后能向非作者复述结论与边界` 无落盘判据（`ACCEPTANCE.md:11` 用"187 项数字对 results 复核无越界"替代，属人工结论），标准 01 §5 的"每项动作可独立执行与核对"不满足 |
| 与上位冲突 | 任务书现文本与上位**一致**（星数不设门槛，三处同串）。冲突发生在**版本历史**上：本文件第 19 行原文（`git show eccf07e6`）是"按负责人裁决写明生产适用域 n≥100、域外降级"，被 `eccf07e6 重建算子默认口径落地 + 收敛枚举与门槛清理收尾（RECON-DEFAULT-01 / GATE-REMOVE-01）` **就地改写**为新口径。⇒ 任务书不是不可变记录：`ACCEPTANCE.md:11` 记的 PASS 是针对**旧文本**的验收，`OPEN_QUESTIONS.md:40-50` Q-5 又记了新裁决，三者叠在同一文件上而无任何留痕（这本身符合标准 01 §4"无历史叙事"，但破坏了 `CONTROL_PACK_SPEC.md` §6.3 状态词与任务文本的对应关系） |
| 处置建议 | **保留**（未收口，交付物虽在但验收门第 3 条无落盘判据，出库前需补一条可核对的复述证据指针）。理由引 `CONTROL_PACK_SPEC.md` §9（整包收口后统一移除，不逐份提前删）+ 标准 01 §2 第三条（本份不迁 `docs/`）。收口时随包 `git rm`，长期价值已由 `REPORT_paper.md` + `PHOTOMETRY.md` §16.5 承载 |

### 1.10 工程控制/RELEASE-05/tasks/SCI-504.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/SCI-504.md` |
| 标题 | `任务：SCI-504 论文式实验报告 · 跨帧绝对 SNR`（第 1 行） |
| 行数 | 26（与机械列一致） |
| 角色 | 过程工作指令；交付物属"研究与佐证"。目录合法；应属 = 收口后移除 |
| 主题 | SCI-B 跨帧绝对 SNR 的论文式报告（含双计问题的完整披露） |
| 上游 | `ACCEPTANCE_SPEC §2.2；实验/absolute-snr/{README.md,results/*}；SCI-501 修复后的结果`（第 7 行）——`ACCEPTANCE_SPEC.md:51` §2.2 ✓；未指最高设计 §2.2（同 1.9 的层级差一级） |
| 下游 | 全仓命中 8 个文件（不含自身；`工程控制/` 内 4 + `reports/v19r2/**` 2 + 包外 2），包外 **2 个**（命令 `git grep -l "SCI-504" -- ':!工程控制' ':!reports'`）：`实验/absolute-snr/REPORT_paper.md`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md` |
| 是否正本 | **否**（SCI-B 口径正本 = `docs/science/CONTROL_WEIGHT_SNR.md`、`docs/science/NOISE_MODEL.md` §5a/`:324`、`docs/plugins/algorithms_phase1/07_noise_snr.md` §4.2a；报告是阅读件） |
| 重复或重叠对象 | `SCI-504.md:19`（`**讨论**：σ_sky 双计读噪问题（说明现象、SCI-501 修复口径与修复后数字）、c_est 单位问题…`）对 `docs/science/NOISE_MODEL.md:324`（`**σ_sky 入参口径与 c_est 单位（SCI-B 定案，防双计）**：…实测双计使 σ_F 高估 +12.8%~+34.0%…② 本文件 §5a 的 1.44/√N 是**相对**标准误（无量纲），dex 口径为 1.44/ln10/√N ≈ 0.625/√N`）。**属合法分层**（任务书规定"报告要讨论什么"，文档规定"口径是什么"）。需要点名的差异在**数字**：任务书未给数值，`SCI-501.md:4` 给 +12.8%~+36.6%，三份 `docs/` 正本给 +12.8%~+34.0% ⇒ 同主题三套数字（详见 1.7 冲突列与 §3 节 T2） |
| 元信息块 | 无 |
| 写法违规 | `7`/`19` 任务编号 SCI-501（交叉引用，合法分层）；`18:26+29 点 3σ、存储 64× 对照、拟合/堆叠权重分离（52.3% 方差对照）` — 历史实测数值表进任务层（标准 01 §4）；`15:本项目"入库 SNR、消费时算权重"的分工` — 无编号；无日期、无 commit、无 sha256、无历史叙事（机械列 `history_narrative=False` ✓） |
| 悬空引用 | 无：交付物 `实验/absolute-snr/REPORT_paper.md` ✓（`:222`、`:244`、`:460` 三处携 +12.8%/+34.0%/+36.6% 三点全套，可回溯 `b1_sky_scan.json`/`b2_noise_terms.json`）；`实验/absolute-snr/results/b1..b7*.json` ✓ 全部在树；步骤 3 的 Horne 1986 公式（`σ_F⁻²=ΣP_i²/σ_i²`）在 `docs/science/CONTROL_WEIGHT_SNR.md` 有正本 ✓。**唯一弱指针**：`21:参考文献…SWarp/Siril 文件:行` 要求外部开源锚，实际落点在 `docs/research/`（本份未指该目录，按 `标准01 §2` 研究与佐证的正本层） |
| 与上位冲突 | 无直接冲突。一处**判据缺口继承自上位任务**：`24:- [ ] 双计问题在报告中以"发现→修复→修复后结果"完整呈现，不掩盖` 已达成（`REPORT_paper.md:225` 主动披露"+12.8%/+34.0% 偏置带 ±3 pp MC 噪声、稳健表述是臂比值 ≤1.25 pp"）——这一披露恰恰**反证**了 `docs/` 三处以 +34.0% 为上限是不完备口径（只取 b2 域），即报告正确、科学文档滞后 |
| 处置建议 | **保留**（未收口；其"完整呈现"判据仍是 SNR 双计结论在任务层的验收依据）。理由引 `CONTROL_PACK_SPEC.md` §9 + 标准 01 §2 第三条（不迁 `docs/`）。**并建议把它的披露口径回灌 `docs/`**：`NOISE_MODEL.md:324`/`07_noise_snr.md:123`/`CONTROL_WEIGHT_SNR.md:201` 三处的 +34.0% 上限应补 b1 的 +36.6% 或改述为"+12.8%（基准）／最坏 +36.6%（B1 暗源 B=0）、RN 扫描最坏 +34.0%"——属 `AGENTS.md` §8 科学疑义查证流程的产物，走变更流程，本节点不代改 |

### 1.11 工程控制/RELEASE-05/tasks/SCI-505.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/SCI-505.md` |
| 标题 | `任务：SCI-505 论文式实验报告 · 加性天光与无接缝`（第 1 行） |
| 行数 | 26（与机械列一致） |
| 角色 | 过程工作指令；交付物属"研究与佐证"。目录合法；应属 = 收口后移除 |
| 主题 | SCI-C 加性天光与无接缝的论文式报告（猜想成立条件/失效条件） |
| 上游 | `ACCEPTANCE_SPEC §2.3；实验/additive-sky-seamless/{README.md,results/*,REVIEW.md}；SCI-502 修复后结果`（第 7 行）——`ACCEPTANCE_SPEC.md:65` §2.3 ✓；花括号展开隐含顶层 `实验/additive-sky-seamless/REVIEW.md` **不存在**（见悬空列）；未指最高设计 §2.3/§5.4 |
| 下游 | 全仓命中 8 个文件（不含自身；`工程控制/` 内 3 + `reports/v19r2/**` 2 + 包外 3），包外 **3 个**（命令 `git grep -l "SCI-505" -- ':!工程控制' ':!reports'`）：`实验/additive-sky-seamless/REPORT_paper.md`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md`、`eng/tools/doccheck/check_doc_hygiene.py`（`:804` 把 "SCI-505" 用作 N4 工作项编号规则的**负例夹具字面量**——该 ID 已是门自测用例的输入，出库时须换中性夹具，否则负例指向不存在的编号仍恒绿） |
| 是否正本 | **否**。SCI-C 接缝判据与节点间距正本 = `docs/science/PHASE2_UPM.md`（§7a、§17 门限推导）与 `docs/plugins/algorithms_phase2/11_upm.md`；报告是阅读件 |
| 重复或重叠对象 | `SCI-505.md:14`（`纯加性世界接缝压缩 12.5×、乘性归 Phase1（4.33×）、失效边界（不可表示的沿向相干小尺度分量，残余≈0.8×RMS）、非退化判据 36.4σ`）对 `DOC-502.md:16`（`参考面须能表示帧间天光差空间尺度；不可表示且沿向相干、尺度 ≲2 节点间距的分量使残余接缝 ≈0.8×RMS`）与 `docs/science/PHASE2_UPM.md`（表示能力边界条款）——同一失效边界结论三处（两份任务书 + 科学文档）。判定：**数值结论的科学承载义务在 `docs/science/`**，两份任务书各抄一遍属重复承载（标准 01 §4"公式、数值表…放对应下层文档"）；`DOC-502` 那份尚有"以…为准"的指向，`SCI-505` 这份只给数字不指权威 |
| 元信息块 | 无 |
| 写法违规 | `14`/`18`/`19` 密集历史实测数值（12.5×、4.33×、36.4σ、0.798/Pearson 0.896、5.07×、27.37→6.31、8.5%、3.1e-15、0.167%）进任务层 —— 标准 01 §4；`19:判据变更表（8 条原判据 FAIL 的处置理由，表格化简述）` — "**原判据**"属历史叙事用语，但此处是**要求交付物做变更披露**（`AGENTS.md` §9"如实"义务），落点在 `实验/` 证据单元（`REPORT_paper.md:352` `### 5.5 判据变更登记（8 条原判据 FAIL，表格化简述）`）⇒ **合规**：变更登记落在证据层而非正式文档层，正是标准 01 §2 第三条的正确处置；`7`/`20` 任务编号交叉引用（合法分层）。无日期、无 commit、无 sha256（机械列一致） |
| 悬空引用 | `7:` 花括号串里的 `REVIEW.md` 按顶层路径不存在：`ls 实验/additive-sky-seamless/REVIEW.md` → **MISS**；真实文件 `实验/additive-sky-seamless/results/REVIEW.md` ✓（`git ls-files 实验 | grep REVIEW` 命中）。属**路径不精确型弱悬空**（同串在 `实验/absolute-snr/` 恰好成立：那里 `results/REVIEW.md` 与顶层无 REVIEW，两单元形状不同，照抄必错）。其余对象均在：`README.md` §10/§11 ✓、`results/c1..c7*.json` ✓、交付物 `REPORT_paper.md` ✓（附录 C 实为 9 条 `C-1..C-9`，与 `SUMMARY.md:31` 一致；与任务书"8 条"不矛盾——8 条是 §5.5 判据变更表，9 条是附录 C 口径冲突，两个不同对象，机械计数时勿混） |
| 与上位冲突 | 无实质冲突。一处**层级缺指**：`20:对 UPM 工程参数（节点间距≲尺度/2、正则化）的要求` 与 `docs/science/PHASE2_UPM.md` §7a 的节点间距条款同主题，而 `REPORT_paper.md:104` 已记录两者**相差 2×**（"§7a 把节点间距写作 ≈hips.tile_width/8 = 64 px，那描述的是 UPM 的 8×8 control cell，不是本单元实测的 sky-plane 样条节点；两者相差 2×…引用 256 px 边界时应以 h≈128 px 为准（附录 C-3）"）⇒ 报告发现上位文档口径歧义并已登记为待 DOC 域收敛，任务书未反映该歧义（它把"≲尺度/2"当确定值下达） |
| 处置建议 | **保留**（OQ-10 使 L4 未过，SCI-C 的条件结论尚未被成品帧互证；报告交付物仍承担该互证义务）。理由引 `CONTROL_PACK_SPEC.md` §9（真实数据终验先于完成声明）。订正一处：`7:` 的 `REVIEW.md` 改写成 `results/REVIEW.md`（消除照抄即错的路径），`20:` 的节点间距改指 `docs/science/PHASE2_UPM.md` §7a + 附录 C-3 的 2× 歧义登记，不在任务层给数值 |

### 1.12 工程控制/RELEASE-05/tasks/SCI-506.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/SCI-506.md` |
| 标题 | `任务：SCI-506 p1001_real_nodes 产品行为红修复`（第 1 行） |
| 行数 | 22（与机械列一致） |
| 角色 | 过程工作指令（缺陷处置单，内容属"标准与检查/操作"）。目录合法；应属 = 收口后移除 |
| 主题 | 全量 ctest 唯一红 `p1001_real_nodes` 转绿 |
| 上游 | `RELEASE-04 ACCEPTANCE BLD-401 行（产品行为红，已转 FIX-408）`（第 7 行）——**整条上游不可核**：RELEASE-04 树已按 `CONTROL_PACK_SPEC.md` §9 整树删除（`git ls-files 工程控制 | grep -c RELEASE-04` = **0**；`OPEN_QUESTIONS.md:24-29` Q-3 记负责人裁决"旧版本直接删除"）⇒ 本任务书唯一的权威依据指向已不存在的对象，且**未指任何最高设计条款**（标准 01 §3 判不合规） |
| 下游 | 全仓命中 10 个文件（不含自身；`工程控制/` 内 6 + `reports/v19r2/**` 2 + 包外 2），包外 **2 个**（命令 `git grep -l "SCI-506" -- ':!工程控制' ':!reports'`）：`eng/tests/unit/p1001_real_nodes_test.cpp`（测试文件注释锚）、`artifacts/evidence/release-05/GATE-501_VERIFICATION.md` |
| 是否正本 | **否**（缺陷事实的正本 = ctest 目标 `p1001_real_nodes` 与 `ACCEPTANCE.md:14` 的根因行）。 |
| 重复或重叠对象 | `SCI-506.md:20-22`（`- [ ] 全量 ctest 零红（fast 与全量）；… - [ ] 零 waiver`）对 `GATE-502.md:23`（`linux-main 15 红全部闭环（绿或派单回执）；全量 ctest 零红`）——**同包两份任务书各立一条"全量 ctest 零红"**，判据相同、责任域不同（SCI 缺陷 vs 测试体系），属重复承载（应由 `ACCEPTANCE_SPEC.md` 机器门层单一承载）。第二处：`22:零 waiver` 对 `AGENTS.md:127`（`不用 waiver 盖红灯`）+ `CONTROL_PACK_SPEC.md:192`（"本规范的 waiver 面积为零"）——同一禁令第三套文字 |
| 元信息块 | 无 |
| 写法违规 | `1` 标题含测试目标名（可接受，是对象名）；`4:全量 ctest 唯一红门 p1001_real_nodes 转绿，根因是产品行为缺陷` — **结论前置**（把上一轮判定当成本轮事实，见冲突列）；`7:RELEASE-04 ACCEPTANCE BLD-401 行…已转 FIX-408` — 任务编号 + 缺陷编号 + 历史叙事"已转"三重；`17:与 ARCH-505 协调…本任务先给最小修复` — 任务编号（合法分层）。无日期/sha256；机械列 `task_ids=['FIX-408','RELEASE-04']` ✓ 报出 |
| 悬空引用 | **一条硬悬空**：`7:` 的 `RELEASE-04 ACCEPTANCE BLD-401 行` —— 确认命令 `git -C "F:/Astro dev/Astro CS Normalization Database" ls-files 工程控制 | grep RELEASE-04` → 空输出（0 命中）；对照：同命令换成 `RELEASE-05` → 31 命中（证明不是检索方法失效）。⇒ 对象（文件）不存在，不是路径拼写问题。其余在册：`p1001_real_nodes` 目标 ✓（`eng/tests/unit/CMakeLists.txt:428 add_executable(p1001_real_nodes_test p1001_real_nodes_test.cpp)` + `eng/ci/checks.json:6763` 注册 + `eng/ci/tests/test_ctest_registration.py:56` 映射 `CTEST-P1001-REAL-NODES`）；`11:禁止改：合同（如需改合同走 CONTRACT-501 变更登记）` ✓ 对象存在 |
| 与上位冲突 | **任务书前提被自己的结果证伪**：`4:` 断言"根因是产品行为缺陷，需定位修复而非改测试"，而 `ACCEPTANCE.md:14` 记本任务 PASS 的根因结论是"根因=**测试期望过时**（CLEAN-401 入口 fail-closed），**非产品缺陷**；新增 NaN fail-closed 负例"。⇒ 目标句与结果句相反（详见 §6 节证伪条目）。另 `16:确认该测试在 fast 与全量档行为一致` 现已不可核（`ACCEPTANCE.md:68` D-16②"linux-main 档步骤超时预算不足…该档全量结论无法完整复现"） |
| 处置建议 | **删除（随包出库时优先，不替换）**——理由：① 它的唯一权威依据已悬空（`7:`）；② 它的目标前提被证伪（`4:` vs `ACCEPTANCE.md:14`）；③ 它要防的复发已由 `eng/tests/unit/p1001_real_nodes_test.cpp` 内的 NaN fail-closed 负例与 `CTEST-P1001-REAL-NODES` 注册承载，任务层无剩余义务（标准 01 §5 判定"链路走得通"要求每项动作可核对，本份已无可核对动作）。**执行时机仍受 §9 约束**：本包未收口，不得单独删（否则 `ACCEPTANCE.md:14` 的 PASS 行失去依据、`test_ctest_registration.py:56` 与 `GATE-501_VERIFICATION.md` 的锚断链）⇒ 登记为"收口清单首项" |

### 1.13 工程控制/RELEASE-05/tasks/VIS-501.md

| 字段 | 判定 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/VIS-501.md` |
| 标题 | `任务：VIS-501 L4 两组成品帧与视觉验收`（第 1 行） |
| 行数 | 24（与机械列一致） |
| 角色 | 过程工作指令（内容属"操作与运营 + 负责人面"：视觉验收与交目检）。目录合法；应属 = 收口后移除 |
| 主题 | testdata 两组真实数据全链 → 平面 FITS + 切块 PNG → agent 逐块自验 → 负责人目检 |
| 上游 | **缺 `## 权威依据` 段，全文无条款指针**（本批唯二无上游的份之一，另见 1.6；机械列 `upstream_pointer=False` ✓ 正确）。应指 `ACCEPTANCE_SPEC.md:206` §6（"L4：真实数据端到端视觉验收（M42 + Galaxy Center）"）与 `ASTROCS_DESIGN.md` §12.4——两处都没写，正文用"L4""SCI-C 接缝度量"这类非正式名代指 |
| 下游 | 全仓命中 13 个文件（不含自身；`工程控制/` 内 8 + `reports/v19r2/**` 2 + 包外 3），包外 **3 个**（命令 `git grep -l "VIS-501" -- ':!工程控制' ':!reports'`）：`eng/tools/e2e/make_vis_configs.py`、`eng/tools/e2e/render_vis.py`（两份工具脚本以任务 ID 作注释锚）、`artifacts/evidence/release-05/AUDIT_PACKAGE.md` |
| 是否正本 | **否**：L4 判据的正本 = `ACCEPTANCE_SPEC.md` §6.2 视觉验收清单（含机器门 `CHK-L4-SEAM-FOOTPRINT`，其"唯一数值来源 = `eng/tools/e2e/seam_footprint.py`、门限 1e-2 的推导正本 = `docs/science/PHASE2_UPM.md` §17"）；数据集定义正本 = `ACCEPTANCE_SPEC.md:208` + `testdata/README.md`。任务书是执行单 |
| 重复或重叠对象 | `VIS-501.md:7-8`（`M42：T2+T3 Red 300s 49 帧（暗场 600s K=0.5）；银心：T4 panel1-3 Red 180s 32 帧（K=1）`）对 `ACCEPTANCE_SPEC.md:208`（`\`testdata/\` 中 M42（猎户座大星云，T2+T3 Red 300s 49 帧）与 Galaxy Center（银心，T4 Red 180s 32 帧）两组完整数据集`）——**帧数两处各写一套**（本批实测两处数字与磁盘一致：`find testdata/M42_T2T3_mosaic_Flying_dutchman -iname '*Red*' | wc -l` = 49、`find testdata/Galaxy_Center_T4 -iname '*Red*' | wc -l` = 32）；上位已声明"工作盘回收与预留口径 = `AGENTS.md` §3（运营事实源，本节不复制数值）"，任务书 `9:工作盘 ≥100GiB，TMPDIR 指源码树外可写目录` 却**把同一运营数值再抄一遍**（且与 `AGENTS.md` §3"可用低于约 100 GiB 就先回收"同义）⇒ 三处承载一条运营事实，属第二/第三份正本。第二处：`14:**agent 视觉自验**：逐块检查无黑洞、无异常亮斑、无接缝；分辨率不足处裁剪放大重读` 对 `ACCEPTANCE_SPEC.md:214`（`agent 逐块+全局初审 → 负责人目检`、`:219` "agent 对有疑问区域裁剪放大后再读，不靠低分辨率整幅图下结论"）与 `AGENTS.md:193`（"视觉层（L4）提交前自行逐块检查（分辨率不足时对疑问区域裁剪放大再读）"）——**同一条纪律三处各写一套** |
| 元信息块 | 无 |
| 写法违规 | `4`/`12`/`16` 任务编号交叉引用（E2E/FIN/PERF-501，`16:热点已在 PERF-501 处理的给对照`）；`16:状态置 READY_FOR_OWNER_REVIEW，**等负责人目检认可**，不自行发布` — 状态词进任务层（状态属 `ACCEPTANCE.md` 台账，`CONTROL_PACK_SPEC.md` §6.3）；`9:工作盘 ≥100GiB` — 运营数值复制（见重复列）。无日期、无 commit、无 sha256、无历史叙事（机械列一致） |
| 悬空引用 | 无文件级悬空，逐条核过：`testdata/M42_T2T3_mosaic_Flying_dutchman/{T2,T3}` ✓ 49 张 Red、`testdata/Galaxy_Center_T4/lights` ✓ 32 张 Red（**但 `.fts` 不入库**：`git ls-files testdata | grep -c '\.fts$'` = 0 ⇒ 干净克隆节点上本任务不可执行，任务书未标此前提）；拉伸/切块/接缝脚本 ✓（`eng/tools/e2e/render_vis.py`、`seam_footprint.py`）；`14:SCI-C 接缝度量` 指的不是正式门名——正式名 = `CHK-L4-SEAM-FOOTPRINT`（`eng/ci/checks.json` 已注册）与实现 `render_vis.py` 的 **V6 `seam_level_step`（max\|rel_step\| ≤ 1e-2）** |
| 与上位冲突 | 两处，都是"会让下一个开工对话跑错判据"的类型：① **接缝判据指代不明**：任务书只说"用 SCI-C 接缝度量互证"，而 `render_vis.py` 同时实现 **V4 `seam_metric`（seam_ratio ≤ 1.5，渲染分块一阶差分比）** 与 **V6 `seam_level_step`（rel_step ≤ 1e-2，上位 §6.2 的可执行门槛）**；按任务书字面执行只需过 V4（松约两个数量级），上位门要求的 V6 可被整条跳过而不被发现。同包的 `SUMMARY.md:78-79` 正是这样写的（"判据 **V1–V5** fail-closed：…接缝比 ≤1.5"）——**已发生的漏读实例**；② `12:HiPS→平面 FITS（单张整幅）` 与 `ACCEPTANCE_SPEC.md` §6.1 一致 ✓，无冲突；③ 与现行授权的冲突在可执行性：`eng/tools/HANDOVER.md:70-83` 本阶段禁止一切端到端 + M42 组阻塞 OQ-10（`OPEN_QUESTIONS.md:156-238`）⇒ 本任务在当前授权下**不可开工**，与 PERF-501 同档 |
| 处置建议 | **保留 + 就地订正**（它是 L4 在任务层唯一的工作合同，且 4 条 NOT_RUN 之一；出库即失去负责人目检的判据载体）。订正：补 `## 权威依据` 段指 `ACCEPTANCE_SPEC.md` §6 与 `ASTROCS_DESIGN.md` §12.4；`14:` 的"SCI-C 接缝度量"改成正式门名并显式要求 V6 `rel_step ≤ 1e-2`（数值不复制，指 `seam_footprint.py` 与 `docs/science/PHASE2_UPM.md` §17）；删 `9:` 的工作盘数值改指 `AGENTS.md` §3；补"testdata `.fts` 不入库，需按 `testdata/README.md` 先取数"的前提 |

<!-- PROGRESS: 已审 13/13 -->

## 2 合并用简表（14 字段压缩，供机器并台账）

| 路径 | 标题 | 行数 | 角色（现目录/应属） | 主题 | 上游 | 是否正本 | 重复或重叠对象（要点） | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `工程控制/RELEASE-05/tasks/DOC-502.md` | 科学定案结论订正入文档 | 29 | 过程指令／`工程控制/` 正确，收口后移除 | 7 条科学结论入 `docs/` | §2/§4.2/§5.4/§8 ✓（粒度过粗） | 否 | `:29` 对 `ENGINEERING_SPEC.md:34`（同纪律两套、少三项义务） | 无 | RELEASE-04/D1/D2/"已定"/行号 | `:7` 顶层 `REVIEW.md` MISS | 包级件反写它的结论为 n≥100 | 保留（随包出库） |
| `.../E2E-501.md` | 小批量端到端与预检矩阵 | 20 | 过程指令／同上 | 三命令全链 + 预检矩阵 | **缺段**；内联 §6.2 **错指**（应 §4.5） | 是（本主题工作合同） | `:20` 对 `AGENTS.md:123`+`ENGINEERING_SPEC.md:141`（三处一套） | 无 | 最干净：仅 PERF/VIS 编号 | `:8` testdata `.fts` 不入库 | `:14` 逐位一致 对 `ENGINEERING_SPEC.md:72` | 保留 + 订正锚与判据 |
| `.../GATE-501.md` | 门禁合理性审计与修复 | 29 | 过程指令／同上 | 门禁有牙 | §9 + `ENGINEERING_SPEC §10` ✓ | 否 | `:18-19` 对 `ENGINEERING_SPEC.md:179,181`（**第二份正本，且漏 :182,:183 两项**）；`:29` 对 `AGENTS.md:195` | 无 | 行号 ×3、D-14、G2-9、数值表、99/99 | 行号锚 2 条腐烂（run.py:109、isa:79） | 步骤 4 对象已删 ⇒ 验收成恒真门 | 保留 + 删重复判据 |
| `.../GATE-502.md` | 测试体系审计与补充 | 29 | 过程指令／同上 | 测试判别力 | §12 + `ENGINEERING_SPEC §10` + G2-4..7 ✓ | 否 | `:26` 对 `GATE-501.md:19` + `ENGINEERING_SPEC.md:179`（**三份**） | 无 | 15/66/72 一次性计数、"失效" | 无（15 红清单不可核：RELEASE-04 已删） | PASS 与 D-16 两条持续红矛盾 | 保留 + 计数改指证据件 |
| `.../PERF-501.md` | 探针驱动的编排性能优化 | 26 | 过程指令／同上 | 编排优化 | §9/§8.3/G3-3 ✓ | 否 | `:20` 与 `GATE-501.md:14` 同套判据两处各写；`:16` 复述 §8.3（`HANDOVER.md:246` 明禁） | 无 | 2.37×/3.92/0.62/3.9GB 历史数值表 | 步骤 1 无夹具落点 | **③处**：逐位一致；前置不可满足；测 mosaic 而缺口在 P1 | 保留 + 订正（或作废重述） |
| `.../REPORT-501.md` | 审核包与最终报告 | 25 | 过程指令（交付物=负责人面）／同上 | 审核包八项 + 四问 | **缺**（无任何条款指针） | 是（八项结构唯一成文处） | `:25` 对 `AGENTS.md:124`+`ASTROCS_DESIGN.md:817`（三处）；四问丢九门中三条 | 无 | 编号固化进 `artifacts/evidence/release-05/` 目录名；写死版本号 | 第 4/5 项证据不存在（run/ 已回收） | `0.0.1alpha` 对根 `VERSION=0.1.0-alpha.1`；4/5 项被 `HANDOVER.md:70-83` 禁做 | 保留 + 补上游、删版本字面量 |
| `.../SCI-501.md` | SNR 噪声项双计修复 | 27 | 过程指令／同上 | σ_sky 双计修复 | §2.2 ✓、§4.4 指偏 | 否 | `:27` 对 `AGENTS.md:118`（少了"走变更流程"出口） | 无 | FIX-407、行号 4258、2.24e-14"恢复" | `:10` 行号锚漂移 +2900 行（真点 `:7159`） | 文档侧 +34.0% 低估 b1 的 +36.6% | 保留 + 行号换符号；数值差上呈 |
| `.../SCI-502.md` | UPM 三个生产缺陷修复 | 27 | 过程指令／同上 | FIX-1/2/3 | §5.4 ✓ + `11_upm §4.6` ✓ | 否 | 枚举与 `11_upm.md:87` 同串（**合法分层**）；κ/间距与 `PHASE2_UPM.md` 分层 | 无 | FIX 编号、行号 5948、12.5×/36.4σ/3.1e-15 | **文件域 `lib/algorithms/upm/**` 无源码**；行号漂移 +3600 | PASS 与 D-7"c1–c7 未复跑"矛盾 | 保留 + 改域、改符号锚 |
| `.../SCI-503.md` | 论文式报告 · 测光 | 27 | 过程指令（交付物=研究与佐证） | SCI-A 报告 | `ACCEPTANCE_SPEC §2.1` ✓（未指最高设计） | 否 | 星数三处同串（合法分层）；**`TASK_LIST.md:12` 反向** | 无 | 无日期/历史（最干净之一） | 无 | 本文件被 `eccf07e6` 就地改写，PASS 针对旧文本 | 保留 + 补可核复述证据 |
| `.../SCI-504.md` | 论文式报告 · SNR | 26 | 过程指令（交付物=研究与佐证） | SCI-B 报告 | `§2.2` ✓（未指最高设计） | 否 | 与 `NOISE_MODEL.md:324` 分层合法；数字三套 | 无 | 历史数值表密集 | 无 | 无（报告披露反证文档口径不全） | 保留 + 回灌 +36.6% 口径（走变更流程） |
| `.../SCI-505.md` | 论文式报告 · 天光无缝 | 26 | 过程指令（交付物=研究与佐证） | SCI-C 报告 | `§2.3` ✓（未指最高设计） | 否 | `0.8×RMS` 三处（`DOC-502:16` 与本份各抄一遍） | 无 | 数值表密集；"原判据"（**合规**，落证据层） | `:7` 顶层 `REVIEW.md` MISS（实在 `results/`） | 节点间距 2× 歧义未反映 | 保留 + 路径与间距指针 |
| `.../SCI-506.md` | p1001 红修复 | 22 | 过程指令（缺陷单） | 唯一红转绿 | **整条不可核**（RELEASE-04 已删） | 否 | `:20` 与 `GATE-502.md:23` 各立"全量 ctest 零红"；"零 waiver" 三套 | 无 | "已转 FIX-408" 历史叙事 | **`:7` 硬悬空**（0 命中，反证法成立） | 前提被 `ACCEPTANCE.md:14` 证伪 | **删除**（登记为收口清单首项） |
| `.../VIS-501.md` | L4 两组成品帧与视觉验收 | 24 | 过程指令（操作+负责人面） | 两组全链 + 目检 | **缺**（无任何条款指针） | 否 | 帧数/工作盘/逐块检查 **各三处**承载 | 无 | READY_FOR_OWNER_REVIEW 状态词进任务层 | 无（`.fts` 不入库未标） | 接缝判据只到 V4（1.5×），漏上位 V6（1e-2）；本阶段禁端到端 | 保留 + 补上游与门名 |

## 3 同主题组与差异（含跨批）

| # | 主题组 | 本批各份 | 组外相关件（注明别批/层） | 差异与判定 |
|---|---|---|---|---|
| T1 | **测光星数门槛 / 适用域** | `DOC-502.md:18`、`SCI-503.md:19`（均"不构成拒绝条件"） | `ASTROCS_DESIGN.md:274`、`docs/science/PHOTOMETRY.md:396`、`实验/photometric-magnitude/REPORT_paper.md:242,275`（同串）；**反串**：`工程控制/RELEASE-05/{SUMMARY.md:26,ACCEPTANCE.md:56,69,HANDOFF.md:187,TASK_LIST.md:12,PROMPT.md:16,OPEN_QUESTIONS.md:40-50}`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md:13`（别批） | 本批两份与上位**完全一致**；"n≥100 定案"只存在于包级件与证据索引件里，且 `OPEN_QUESTIONS.md:40-50`（Q-5）已记负责人裁决"把相关门槛与限制都删掉"。⇒ **组内不存在第二份正本，存在的是一组过期结论**（§6A 节 P0-1） |
| T2 | **σ_sky 双计量级** | `SCI-501.md:4`（+12.8%~+36.6%）、`SCI-504.md:19`（不给数值） | `docs/science/NOISE_MODEL.md:324`、`docs/plugins/algorithms_phase1/07_noise_snr.md:123`、`docs/science/CONTROL_WEIGHT_SNR.md:201`（三份均 +12.8%~**+34.0%**）；`实验/absolute-snr/README.md:111,319`、`results/REVIEW.md:186`、`REPORT_paper.md:222,244,371`（+36.6% 为 B1 最坏，+34.0% 为 B2 RN 扫描最坏，且披露 ±3 pp MC 噪声） | **同主题三套数值**：任务层（对）／科学文档层（只取 b2，偏小 2.6 pp）／实验层（全套 + 不确定度）。正本应是 `docs/`，实测以实验层为最全 ⇒ 科学文档口径不完备，属 `AGENTS.md` §8 查证流程的整改项 |
| T3 | **1/N worker 一致性判据** | `E2E-501.md:14`、`PERF-501.md:22`（"逐位一致"）、`SCI-501.md:23`（只写"一致"）、`GATE-502.md` 无 | `ENGINEERING_SPEC.md:72`（"判据 = 事前冻结的浮点容差，**不是**逐位一致"）、`docs/contracts/SCHEDULER_CONTRACT.md:33`（"合同层不要求逐位一致"，模块自有门可更严）、`ACCEPTANCE_SPEC.md` §5 | 同一判据在任务层被抬成逐位、且四处表述互不相同 ⇒ 第二份正本 + 与上位字面相抵。现行实现因归约顺序冻结而偶合成立（`ACCEPTANCE.md:17,18`），不构成误红，属**口径分叉风险**而非现实故障 |
| T4 | **"门禁必须有牙 / 不放宽判据"** | `GATE-501.md:18,19,29`、`GATE-502.md:26,28`、`PERF-501.md:26`、`SCI-506.md:22` | `ENGINEERING_SPEC.md:179,181,182,183`（上位四条，任务层只覆盖两条）、`AGENTS.md:127,195`、`CONTROL_PACK_SPEC.md:192`（"waiver 面积为零"）、`eng/tools/HANDOVER.md:63` | **五条平行承载同一纪律**（两份任务书 + AGENTS + CONTROL_PACK_SPEC + HANDOVER），且任务层版本互不相同（`GATE-501` 讲门、`GATE-502` 讲测试、`PERF-501` 讲 record_and_justify、`SCI-506` 只写"零 waiver"）。典型第二份正本群，应收敛为指上位 |
| T5 | **发布/FIN 与版本口径** | `REPORT-501.md:25`（0.0.1alpha）、`VIS-501.md:16`（不自行发布） | `AGENTS.md:124`、`ASTROCS_DESIGN.md:817`、`ACCEPTANCE_SPEC.md:260-276`（九条发布门）、`ENGINEERING_SPEC.md:145` + 根 `VERSION`、`docs/owner/RELEASE_STATUS.md` | 任务层自设版本字面量，与"根 `VERSION` 单源"并列且**与现值不符**（`0.1.0-alpha.1`）；四问丢九门中三条 ⇒ 不完备第二份 |
| T6 | **UPM converged 枚举 / 表示能力边界** | `DOC-502.md:16,17`、`SCI-502.md:15`、`SCI-505.md:14` | `docs/plugins/algorithms_phase2/11_upm.md:87`、`docs/science/PHASE2_UPM.md` §5/§7a/§17、`docs/algorithms/PHASE2_UPM_IMPL.md` | 枚举三处一致（任务书显式"以仓内权威为准"）⇒ **合法分层**。但 `ACCEPTANCE.md:44`（D-4）仍在讲一个**已被 `eccf07e6` 改写到不存在**的冲突（§6A 节 P0-2）；表示能力边界数值 0.8×RMS 在两份任务书各抄一遍（轻度重复，正本在 `PHASE2_UPM.md`） |
| T7 | **L4 接缝判据** | `VIS-501.md:14`（"SCI-C 接缝度量"） | `ACCEPTANCE_SPEC.md:227-236` §6.2（`CHK-L4-SEAM-FOOTPRINT`、`rel_step ≤ 1e-2`、唯一数值源 `eng/tools/e2e/seam_footprint.py`、推导正本 `docs/science/PHASE2_UPM.md` §17）、`eng/tools/e2e/render_vis.py:22-46`（V4 与 V6 并存）、`工程控制/RELEASE-05/SUMMARY.md:78-79`（别批：只列 V1–V5） | 任务书用非正式名指代，实测已导致 SUMMARY 漏记 V6 ⇒ 该组需要单一门名 + 单一判据指针（现三处三个名字） |

## 4 索引与链接缺陷清单

**前提事实（不是缺陷）**：`docs/DOCUMENT_INDEX.yaml:30` 与 `eng/tools/doccheck/check_doc_index.py:100` 把 `工程控制/` 列入扫描排除面，故本批 13 份 `unregistered_index=True` **符合设计**（标准 01 §2 第三条：过程台账不落正式文档层，因此不进唯一索引地图）。派生缺陷如下。

| # | 类型 | 条目 | 证据（命令/行） | 说明 |
|---|---|---|---|---|
| I-1 | 唯一索引地图覆盖缺口 | `工程控制/**` 无任何索引可发现性 | `git ls-files 工程控制 | wc -l` = 31，`docs/DOCUMENT_INDEX.yaml` 中 `工程控制` 仅出现在注释与一条 `duty` 文本（`:30,:578,:580`） | 控制包只能靠目录浏览发现；机器侧唯一登记点是 `check_doc_index.py:108 CURRENT_CONTROL_PACK="工程控制/RELEASE-05"` —— **一个写在检查器源码里的常量**，不是数据。收口时要改源码才能解除，属治理隐患（别批：`eng/tools/doccheck/check_doc_index.py`） |
| I-2 | 规格层与任务书层分叉 | `TASK_LIST.md:8`（DOC-502 域"docs/science、plugins、research、algorithms"）vs `tasks/DOC-502.md:10`（多一项 `docs/design/UNIFIED_MODEL.md`） | 两文件对读 | 双线文件域互斥判定以 TASK_LIST 为准时会漏 UNIFIED_MODEL.md |
| I-3 | 同一事实三处三个数 | DOC-502 结论条数："四处"（`TASK_LIST.md:8`、`00_README.md:17`——**且 `00_README.md:17` 自称四处却列了 6 项**）、"7 条"（`ACCEPTANCE.md:8`、`tasks/DOC-502.md:24`）、8 步（`tasks/DOC-502.md:14-21`，`grep -c "^[0-9]\." ` = 8） | 计数命令实测 | 验收门写"上述 7 条"而步骤有 8 条（第 8 条是门禁要求，勉强自洽），但包级摘要写"四处"与两者都不合 |
| I-4 | 状态层与规格层分叉 | `TASK_LIST.md:12` 的 SCI-503 验收门"n≥100 适用域表述" vs `tasks/SCI-503.md:19`"星数不构成拒绝条件" | `eccf07e6` 只改了 tasks/ 三份，未同步 TASK_LIST | 同一包内**规格摘要与任务书正文口径相反**；下一个按 TASK_LIST 派活的对话会把已作废的 n≥100 再下达一次 |
| I-5 | 台账状态过期 | `ACCEPTANCE.md:29` REPORT-501 "PARTIAL：SUMMARY/审核包未成文" vs `SUMMARY.md`（104 行成文）、`artifacts/evidence/release-05/AUDIT_PACKAGE.md`（成文且自述覆盖 §1–§7） | 两文件对读 + `ls artifacts/evidence/release-05/` | 把已完成写成待办（§6A 节 P0-4） |
| I-6 | 证据指针失效 | `ACCEPTANCE.md:5,25`、`SUMMARY.md:5` 指向 `run/RELEASE-05/logs/`、`run/RELEASE-05/accept/ACCEPT-501_report.md`、`run/RELEASE-05/vis/` | `ls run/RELEASE-05` → MISS；`git ls-files run` 仅 `run/.gitkeep` | `run/` 是 gitignore 的过程目录 ⇒ 全部证据指针在树外、不可复现。`ACCEPTANCE_SPEC.md:247` §7 与 `CONTROL_PACK_SPEC.md:210` 要求"留证的落 `artifacts/evidence/` 并由正本引用"，这批证据未迁移 |
| I-7 | 上游锚错指（本批） | `E2E-501.md:9`、`GATE-502.md:19`："预检矩阵（最高设计 §6.2）" | `ASTROCS_DESIGN.md:440`（§6.2 = export 流程）vs `:332`（§4.5 = 运行前预检，含 correct/warn/error/-y/-force 全套）；旁证 `docs/api/CLI_PROTOCOL_V1.md:37,119`、`docs/contracts/CONFIG_CONTRACT.md:15,106` | 两处各错一处，同一错误来源（制作控制包时按旧章节号抄写） |
| I-8 | 行号锚腐烂（本批） | `SCI-501.md:10`（4258→实 `:7159`）、`SCI-502.md:10`（5948-5949→实 `:9561-9573`）、`GATE-501.md:16,17`（run.py:109-119→实 `:139/:924`；isa:79→实 `:93-96`） | `sed -n` 逐行实读 + `git grep -n` | 4 条锚全部指向漂移后的无关行；`ENGINEERING_SPEC.md:162` 规则 4"文档引用的代码路径必须真实"只覆盖路径不覆盖行号，机器门 `DOC_LINE_ANCHORS`（`SUMMARY.md:67` 报 879 锚 PASS）不扫 `工程控制/` |
| I-9 | 硬悬空（本批） | `SCI-506.md:7` `RELEASE-04 ACCEPTANCE BLD-401 行` | `git ls-files 工程控制 | grep RELEASE-04` → 0 命中（同命令换 `RELEASE-05` → 31 命中，反证检索法有效） | RELEASE-04 整树已按 `CONTROL_PACK_SPEC.md` §9 出库（`OPEN_QUESTIONS.md:24-29`）；同类还有别批 `tasks/CLEAN-501.md:7`（引 RELEASE-04 ACCEPTANCE D-2） |
| I-10 | 路径不精确（本批） | `DOC-502.md:7`、`SCI-505.md:7` 的 `REVIEW.md`（花括号串隐含顶层） | `ls 实验/additive-sky-seamless/REVIEW.md` → MISS；真实 `实验/additive-sky-seamless/results/REVIEW.md` ✓ | 两实验单元目录形状不同（absolute-snr 顶层无 REVIEW），照抄必错 |
| I-11 | 声明域指向空骨架 | `SCI-502.md:10` `lib/algorithms/upm/**` | `find lib/algorithms/upm -type f` = 3 份 md/yaml，无源码；`module.yaml:2-4` 自述 CONTRACT_READY、实现在 `coverage/src/upm.cpp` | 与 `标准04 §1`（模块并联放置、归并后目录退役）叠加的整改项（架构侧属别批 AUD-401） |
| I-12 | 编号被生产面当锚（阻塞出库） | 13 份合计：包外 **84 个不同文件、246 处命中**（单份最重：GATE-502=42 文件/包外 33、GATE-501=38/26、PERF-501=26/14、SCI-502=19/9） | `git grep -l "<ID>" -- ':!工程控制' ':!reports' \| wc -l`（逐份）；总和：对 13 个 ID 累加 `git grep -c`＝246、`sort -u`＝84 | 未做"编号→落点改写"前按 §9 删目录，将一次性制造 I-6~I-11 同型悬空 |
| I-13 | 上位引用路径错（别批，本批牵出） | `ACCEPTANCE.md:69` D-17② 写 `docs/algorithms/07_noise_snr.md` | `git ls-files docs/algorithms/07_noise_snr.md` → **0**；真实 `docs/plugins/algorithms_phase1/07_noise_snr.md`（`git ls-files` 命中） | SCI-501/504 主题的插件文档被记成不存在的算法文档路径；本批 1.7 的"上游指偏"与之同源 |
| I-14 | 写法门的命名空间来源与 §9 打架 | `check_doc_hygiene.py:22,390-403,648-652` D3b 前缀表由 `run/`+`工程控制/` 目录名派生、表空判红 | `git ls-files run` = 1（`.gitkeep`）⇒ 干净克隆上唯一来源是 `工程控制/<包>` | 清掉最后一个控制包必须与装上下一个包同提交，否则 `CHK-DOC-HYGIENE`（不可豁免）fail-closed 红（详见 §0.2 反向咬合条） |

## 5 覆盖率自报

- **已审 13 / 分配 13**（清单 `独立审计/批次清单/DB-20.txt` 13 行，逐行取路径，`wc -l` = 13）。
- **未逐行读名单：空**。13 份全部通读全文（每份 ≤29 行，`wc -l` 逐份实测：29/20/29/29/26/25/27/27/27/26/26/22/24，与 `doc_mechanical.csv` 的 `lines` 列 13/13 相同）。
- 判定所依据的**外部对象**均实读或实跑只读命令核对：`CONTROL_PACK_SPEC.md`（224 行全读）、`ENGINEERING_SPEC.md`（272 行全读）、`AGENTS.md`（定点行号 118/123/124/127/193/195）、`ASTROCS_DESIGN.md`（§4.5/§6.2/§13 全文读 + 全量章节号表）、`ACCEPTANCE_SPEC.md`（§2 标题表 + §6 全读 + §8 全读）、包级 6 份（`00_README`/`TASK_LIST`/`ACCEPTANCE`/`SUMMARY`/`OPEN_QUESTIONS`/`HANDOFF` 全读）、`eng/tools/HANDOVER.md`（285 行全读）、`eng/tools/doccheck/check_doc_index.py:100-115`、`docs/plugins/algorithms_phase2/11_upm.md` §4.6、`docs/science/PHOTOMETRY.md` §16.5、`docs/science/NOISE_MODEL.md:324`、`docs/science/CONTROL_WEIGHT_SNR.md:201`、`docs/plugins/algorithms_phase1/07_noise_snr.md:113-123`、`lib/algorithms/upm/{README.md,module.yaml}`、`实验/additive-sky-seamless/README.md` §10、`实验/photometric-magnitude/REPORT_paper.md`、`实验/absolute-snr/{README.md,REPORT_paper.md,results/REVIEW.md}`、`artifacts/evidence/release-05/{AUDIT_PACKAGE.md}`、`eng/tools/e2e/render_vis.py`、`eng/ci/run.py`、`eng/ci/l2_frozen_gate.py`、`eng/ci/checks.json` 定点。
- 未做（有意）：任何构建、ctest、`run_checks.py`、检查器/generator 执行、Python import；前言 §1 禁止项零违反。
- 收工基线（`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`，与本节同时刻）：见文末原文，**与开工一致，无新增项**（本批取证全为只读命令，未触发 `__pycache__`/产物写入）。

### 5.1 对机械线索文件的校准（前言 §5.4 要求的交付物）

| 列 | 判定 | 证据 |
|---|---|---|
| `lines` | 13/13 正确 | 逐份 `wc -l` |
| `h1` | 13/13 正确 | 与实读标题逐字一致 |
| `meta_block` | 13/13 正确（全部 False） | 全部以 H1 开头，无 ID/版本锚块 |
| `dates`/`commit_sha`/`sha256`/`history_narrative` | 13/13 正确（全空/False） | 实读无日期、无 sha；`SCI-501.md:17` 的"恢复"、`GATE-502.md:4` 的"失效"未算历史叙事，属保守口径，可接受 |
| `task_ids` | **系统性漏报**：只匹配 `EXP-/RELEASE-/FIX-/AUD-` 前缀 | `GATE-501.md` 实含 `D-14`（`:17`）、`G2-9`（`:20`）；`GATE-502.md` 实含 `G2-4..G2-7`（`:7`）、`CLEAN-501`（`:18`）、`CFG002-04`（`:16`），机械列只报 `RELEASE-04`；本批另含 `CONTRACT-501`/`ARCH-505`/`BLD-401` 等同族编号未计 |
| `dangling_links` | **本 genre 整列为盲区**：13 份中 8 份的引用对象写成反引号内联 `path`/`path:line`/花括号串，**不是 markdown 链接**，故按链接语法判定的列恒偏保守 | 漏报 5 处真失效：`SCI-501.md:10`、`SCI-502.md:10`、`GATE-501.md:16`、`GATE-501.md:17`、`SCI-506.md:7`（硬悬空）+ 路径不精确 2 处（`DOC-502.md:7`、`SCI-505.md:7`）。**疑误报**：`GATE-502.md` 报 `dangling_links=[18]`，而第 18 行（66/72 缺口）无链接对象，工具判据需回查 |
| `unregistered_index` | 13/13 正确但**语义应注明**：排除面而非漏登记 | `docs/DOCUMENT_INDEX.yaml:30`、`check_doc_index.py:100` |
| 检索方法教训（非工具缺陷） | 首轮把 `git grep` 限定在 `docs 实验 工程控制` 三个 pathspec 上，导致 `artifacts/evidence/release-05/AUDIT_PACKAGE.md:13` 的 `n≥100` 未进首轮命中 | 第二轮去掉 pathspec 后命中 ⇒ 与前言 §5.2"换两三套检索词"同源，**pathspec 也要换** |

## 6 我证伪了任务书的哪个前提

| # | 前提（出处） | 反证（可核） | 结论 |
|---|---|---|---|
| F-1 | `SCI-506.md:4`："全量 ctest 唯一红门 p1001_real_nodes 转绿，**根因是产品行为缺陷**，需定位修复而非改测试" | `ACCEPTANCE.md:14`：SCI-506 PASS，"根因=**测试期望过时**（CLEAN-401 入口 fail-closed），**非产品缺陷**；新增 NaN fail-closed 负例" | 前提被本包自己的验收结果证伪 |
| F-2 | `PERF-501.md:4,13`：串行瓶颈在"阶段串行段"，步骤 1 指定"16 帧合成 **mosaic** 全链"作基线 | `HANDOFF.md:88-93`：实测 normalize 原本完全帧串行、mosaic **已**帧级/tile 级并行、export resample **已**行带并行，"真正的并行缺口只在第一阶段"，根因是 Phase1 dispatch 未注入 `__workers` | 前提被同包更新件证伪；按现文本执行会优化错阶段 |
| F-3 | 包级件（别批）+ 派单关注点：`PHOTOMETRY.md` §16.5 "定案 n≥100 适用域"且"生产门槛仍 `kMinFitStars=3` ⇒ 未落地"（`SUMMARY.md:26`、`ACCEPTANCE.md:56,69`、`HANDOFF.md:187`、`AUDIT_PACKAGE.md:13`） | `ASTROCS_DESIGN.md:274` 与 `docs/science/PHOTOMETRY.md:396` 均写"**不设固定星数门槛 / 星数不构成拒绝条件**"，`kMinFitStars = 3` 在 `lib/algorithms/photometry/cpp/src/frame_photometry_fit.h:89` + `:121` 注释里定义为 §4 **求解前提**（`fit_ok=true ⇔ rc==0 且 n_matched>=kMinFitStars`），`OPEN_QUESTIONS.md:40-50`（Q-5）记负责人已裁决删门槛 | **不存在该定案，也不存在该落地缺口**：这是一条 phantom blocker，被当作发布门未满足的理由之一 |
| F-4 | `ACCEPTANCE.md:44`（D-4）："控制包 SCI-502 任务书的 converged 枚举与仓内权威 `11_upm.md` §4.6 冲突" | `tasks/SCI-502.md:15` 现文本与 `docs/plugins/algorithms_phase2/11_upm.md:87` **逐字一致**；`git show eccf07e6` 显示该行原为"0/1/2/3（收敛/未收敛/达 max_iter/stalled）"，由 `eccf07e6` 就地订正 | 冲突**曾经成立、现已不可核**（任务书被改写）。D-4 作为"未决冲突"在册会引导下一个对话去找一个不存在的冲突 |
| F-5 | `E2E-501.md:7,17`（"三命令串行 rc=0…数值在 Oracle 容差内"即算端到端验收）在**生产口径**上成立 | `eng/tools/e2e/make_e2e_configs.py:66` 与 `make_vis_configs.py:34` 钉 `pixfrac: 1.0`，出厂模板 `eng/packaging/config/templates/normalize.phase_config.json:11,22` 是 `pixfrac: 0.8`；`eng/tools/HANDOVER.md:186-206`（S1-18）证明 `pf<1` 直写路径曾使生产亮度整体偏亮 0.48 星等而全链测不到 | 端到端验收的覆盖面前提被证伪：**跑的是生产从不使用的配置**（"判据与缺陷正交"型空档） |
| F-6 | 派单前言 §1"当前树应恰有两个未跟踪项" | 开工 `git status --porcelain` = `?? ACSD整治工作包_AUDIT-06.zip`、`?? site/` | **未证伪**（成立） |
| F-7 | AUD-101 任务书 §范围"以 `docs/` 为重点（当前约 233 份、30+ 子目录）" | 本批范围是 `工程控制/`（13 份），未对 `docs/` 计数 | **本批不证伪亦不确认**（不越界代审别批）；提示：`工程控制/` 跟踪件实为 31 份（非派单"13 份 RELEASE-05 等控制包"给人的整层规模印象） |

## 7 收工基线原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
```

## 6A 本批五个特有问题的正面回答

**Q1 该控制包是否已收口** —— **未收口**，判据取它自己声明的完成条件而非目录名：`CONTROL_PACK_SPEC.md` §9 要"全部任务 PASS + 结论沉淀 + 真实数据终验先于完成声明"，本包 17 PASS / 1 BLOCKED（ARCH-505，OQ-9）/ 4 NOT_RUN（CLEAN-501、PERF-501、BLD-501、VIS-501）/ REPORT-501 PARTIAL，且 `SUMMARY.md:15` 自书"本包**未完成**，不进入 FIN，不申请发布"。⇒ 按 §9 现在**不该**清包，也不该清本批任何一份。真正的病灶是另一侧：包已被后续裁决与新工作阶段（`eng/tools/HANDOVER.md` §4.1 的"一切测试与端到端暂停"）实质取代，但仍以"当前控制包"身份在册（`check_doc_index.py:108` 常量硬编码）。

**Q2 已收口的任务书是否仍留在 `工程控制/`、未收口的还剩哪些条目** —— 本包**整包未收口**，所以"已收口的仍留着"这条不成立；成立的是**份级已完成却仍在待办堆里**与**份级未完成却已标 PASS** 两种畸形：
- 份级已交付（产物在树、结论已入 `docs/`）：DOC-502、SCI-501、SCI-502、SCI-503、SCI-504、SCI-505、SCI-506、GATE-501、GATE-502、E2E-501（10 份）——按 §9 属"收口时随包移除"对象；
- 份级未完成：PERF-501（前置不可满足 + 本阶段禁测）、VIS-501（M42 阻塞 OQ-10 + 本阶段禁测）、REPORT-501（八项中第 4/5 项无数据）、SCI-501 步骤 4（`ACCEPTANCE.md:46` D-6 镜像对拍未复跑）、SCI-502 验收门 1/4（`ACCEPTANCE.md:47` D-7 M42 样本与 c1–c7 未复跑）；
- **归档去处唯一且为删除**：`CONTROL_PACK_SPEC.md` §9 "把本控制包目录从仓库移除"+ `OPEN_QUESTIONS.md:24-29`（Q-3：RELEASE-04 整树删除、`ARCHIVED_CONTROL_PACKS` 白名单清空）⇒ 没有 `docs/archive/` 式的控制包归档层，留证只有 `artifacts/evidence/release-05/`（已有 5 件）与 git 历史。**出库前置作业**：Q2 的 I-12（包外 84 个文件、246 处编号锚）与 I-6（`run/` 证据迁 `artifacts/evidence/`）不做，删包即造新悬空；I-14（D3b 前缀表与 §9 的打架）不做，删包还会让不可豁免的 `CHK-DOC-HYGIENE` fail-closed 判红。

**Q3 与 `AGENTS.md`/`ENGINEERING_SPEC.md` 的口径重复承载** —— 确证 4 组（全部两处段落已在 §1 各份"重复或重叠对象"列贴出）：① T4"门禁必须有牙"五处平行承载（两份任务书 + `AGENTS.md:127,195` + `ENGINEERING_SPEC.md:179,181-183` + `HANDOVER.md:63`），且任务层版本**漏掉上位两条**（锚存活 `ANCHOR_STALE`、注册表与 `docs/ci/01_CHECKS.md §2` 双向对齐）＝不完备第二份；② T3 1/N 一致性（`E2E-501.md:14`/`PERF-501.md:22` 的"逐位一致"对 `ENGINEERING_SPEC.md:72` 的"不是逐位一致"）；③ T5 发布/版本（`REPORT-501.md:25` 自设 `0.0.1alpha` 对根 `VERSION` 单源与 `ASTROCS_DESIGN.md:817`）；④ 科学红线（`DOC-502.md:29`/`SCI-501.md:27` 各抄 `AGENTS.md:118`+`ENGINEERING_SPEC.md:33-34` 且删掉了"走变更流程"出口）。另有 L4 纪律三处（`VIS-501.md:14` 对 `ACCEPTANCE_SPEC.md:214,219` 对 `AGENTS.md:193`）、运营数值三处（`VIS-501.md:9` 对 `ACCEPTANCE_SPEC.md:208` 对 `AGENTS.md` §3）。**处置**：任务书只留"指上位条款 + 本任务特有的可跑命令"，删掉复述的纪律句——这是 `CONTROL_PACK_SPEC.md` §5.1 第 1 条（提交粒度正本=ENGINEERING_SPEC §6，本规范不复制）已经在用的写法，只是没贯彻到"禁止"段。

**Q4 文件域/验收条款现在是否还可执行**（逐份给命令与结论；**全部为只读核对，未执行任何构建/测试/检查器**）
- 域指向空骨架 ⇒ **不可执行**：`find "F:/Astro dev/Astro CS Normalization Database/lib/algorithms/upm" -type f` → 仅 3 份 md/yaml，无源码（`SCI-502.md:10` 声明的允许改域内无物可改）。
- 行号锚 4 条全部漂移 ⇒ **不可按字面执行**：`sed -n '4252,4266p' .../module_adapters.cpp`（PSF manifest，非 σ_sky）、`sed -n '5944,5952p'`（P2 apply 判决，非容差）、`sed -n '105,125p' eng/ci/run.py`（SKIP/HARD 常量，非监控字段）、`sed -n '76,82p' eng/tests/backend/test_isa_variants.py`（BENCH 解析）。
- 验收对象已消失 ⇒ **变成恒真门**：`ls "F:/Astro dev/Astro CS Normalization Database/artifacts/prerelease_v5"` → MISS（`GATE-501.md:25`"陈旧路径清零（grep）"现在必绿）。
- 证据在树外 ⇒ **不可复现**：`ls "F:/Astro dev/Astro CS Normalization Database/run/RELEASE-05"` → MISS；`git ls-files run` → 仅 `run/.gitkeep`（牵动 E2E-501/REPORT-501/VIS-501 三条验收门）。
- 上游不可核 ⇒ **不可追溯**：`git ls-files 工程控制 | grep RELEASE-04` → 0 命中（`SCI-506.md:7`）。
- 数据在位、但不在库 ⇒ VIS-501 帧数与磁盘一致（`find testdata/M42_T2T3_mosaic_Flying_dutchman -iname '*Red*' | wc -l` = 49；`find testdata/Galaxy_Center_T4 -iname '*Red*' | wc -l` = 32），但 `git ls-files testdata | grep -c '\.fts$'` = 0 ⇒ 只有本节点可跑。
- **仍可执行**（对象与门都在）：DOC-502 的 `11_upm.md §4.6`/`UNIFIED_MODEL.md`/`CON-DOC-SYMBOLS`；GAP_AUDIT G2-4..G2-7/G2-9/G3-3；`实验/additive-sky-seamless/results/{c1..c7}*.json` 与 `实验/absolute-snr/results/{b1..b7}*.json`（SCI-502/504/505 的复跑判据可执行，只是没跑）；`p1001_real_nodes` ctest 目标（`eng/tests/unit/CMakeLists.txt:428` + `eng/ci/checks.json:6763`）；`CHK-L4-SEAM-FOOTPRINT`（`eng/ci/checks.json:13189`）；L2 四条判据（`eng/ci/l2_frozen_gate.py:10`）。

**Q5 在留批次里会误导下一个开工对话的过期结论（逐条 + 处置）** —— 按优先级：

| 级别 | 误导条目 | 现册文字（可核） | 为什么误导 | 处置（谁做、怎么做） |
|---|---|---|---|---|
| **P0-1** | "测光 n≥100 适用域已定案、生产未落地"，被列为发布门未满足的理由 | `SUMMARY.md:25-26`、`ACCEPTANCE.md:56`（D-13）、`:69`（D-17③）、`HANDOFF.md:186-187,205`、`TASK_LIST.md:12`、`PROMPT.md:16`、`tasks/ACCEPT-501.md:14`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md:13` | 上位与文档侧**明文相反**（`ASTROCS_DESIGN.md:274`、`PHOTOMETRY.md:396` 均"不设星数门槛"），且负责人已在同包 `OPEN_QUESTIONS.md:40-50`（Q-5）裁决"把相关门槛与限制都删掉"。新对话照 `HANDOFF.md:11-16` 的开场指令读 ACCEPTANCE/SUMMARY，会去"落地一条不存在的 n≥100 门"，或反过来把 `kMinFitStars=3` 当缺陷删掉——后者会**删掉真实求解前提**（`frame_photometry_fit.h:121`） | 前台在 `工程控制/RELEASE-05/{SUMMARY,ACCEPTANCE,HANDOFF}.md` 与 `TASK_LIST.md:12`、`tasks/ACCEPT-501.md:14`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md:13` 六处删/改判为"已由 Q-5 裁决关闭：星数不构成拒绝条件，`kMinFitStars=3` 是 §4 求解前提不是门槛"。证据：`OPEN_QUESTIONS.md:40-50` + `PHOTOMETRY.md:396` |
| **P0-2** | "SCI-502 与 `11_upm.md §4.6` 存在 converged 枚举冲突"（D-4） | `ACCEPTANCE.md:44` | 冲突文本已被 `eccf07e6` 就地改写，现在两处逐字一致 ⇒ 引导新对话去"裁决一个已消失的冲突" | 前台把 D-4 改判为"已按仓内权威订正并闭合，指针 = `OPEN_QUESTIONS.md:31-38`（Q-4）"，删除"冲突"语 |
| **P0-3** | PERF-501/VIS-501/REPORT-501 的第 4、5 项仍作**现行待办**在册 | `SUMMARY.md:13`（NOT_RUN）、`tasks/PERF-501.md:10`、`tasks/REPORT-501.md:10-11` | 三者都以"跑重活/端到端"为内容，而 `eng/tools/HANDOVER.md:70-83` 现令"全部完成前不要再做任何测试与端到端"；`PERF-501` 的前置（ARCH-505+CLEAN-501）阻塞在 OQ-9（负责人裁决项）。新对话会**违令开工**或空等前置 | 前台把三条状态由 NOT_RUN 改判 **DEFERRED**（词表见 `docs/owner/RELEASE_STATUS.md:19`，判据要"控制包任务图条目"——正好由 TASK_LIST 承载），并注明解禁条件；`PERF-501.md:13` 的测量对象按 `HANDOFF.md:88-93` 改指 normalize |
| **P1-1** | "REPORT-501 PARTIAL：SUMMARY/审核包未成文" | `ACCEPTANCE.md:29` | 两份都已成文（`SUMMARY.md` 104 行、`artifacts/evidence/release-05/AUDIT_PACKAGE.md` 自述覆盖 §1–§7）⇒ 新对话会重做已交付物 | 前台改状态并给指针；`AUDIT_PACKAGE.md:13` 的 n≥100 串一并订正（同 P0-1） |
| **P1-2** | 全部 `文件:行` 锚与 `RELEASE-04 ACCEPTANCE BLD-401 行` | `SCI-501.md:10`、`SCI-502.md:10`、`GATE-501.md:16,17`、`SCI-506.md:7` | 锚全部指向漂移后的无关行或已删的包 ⇒ "按锚改代码"会改错位置；`SCI-506` 的权威依据整条不可核 | 改锚为符号名/门 ID（`p1_op_noise`、`sigma_sky_source`、`CHK-L4-SEAM-FOOTPRINT`）；`SCI-506` 登记为收口清单首项删除（见 1.12） |
| **P1-3** | L4 接缝判据只到 V4（`seam_ratio ≤ 1.5`），漏上位可执行门 V6（`rel_step ≤ 1e-2`） | `tasks/VIS-501.md:14`（"SCI-C 接缝度量"非正式名）、`SUMMARY.md:78-79`（"判据 V1–V5"） | 已发生实例：包级摘要把 V6 漏掉。新对话若按 `SUMMARY` 复核 L4，会以为 1.5× 就是接缝门 | 两处改列 V1–V6 并指 `ACCEPTANCE_SPEC.md` §6.2 + `eng/tools/e2e/seam_footprint.py`（数值不复制，遵上位"本节不复制取值"） |
| **P1-4** | 三套 `FIX-*` 编号同号不同物、且其中一套的宿主包已删 | `SCI-501.md:1`（FIX-407）、`SCI-506.md:7`（FIX-408）、`SCI-502.md` 通篇（FIX-1/2/3＝实验单元登记表号） | RELEASE-04 的 FIX-4xx 与 RELEASE-05 实验单元的 FIX-1..3 无法从字面区分，宿主 RELEASE-04 已不在树 ⇒ 追号即断链 | 任务层统一改写成 `实验/<单元>/README.md §10 的 FIX-nn`（实验内编号带宿主），FIX-407/408 类历史号改指 `artifacts/evidence/` 或直接删 |
| **P2-1** | 任务 ID 被生产代码/文档当注释锚 | I-12（13 份合计包外 84 文件/246 处；GATE-501/502 最重） | 出库时若不先改写锚，会一次制造 84 个文件的悬空 | 收口作业表首列"编号→落点改写"，与 `git rm` 同提交（`CONTROL_PACK_SPEC.md` §9"与清理任务同一提交"） |
| **P1-5** | 写法门的判据集合**依赖控制包目录在场**：`CHK-DOC-HYGIENE`（fast/linux-main/windows-main、`waivable=False`）的 D3b 进程编号前缀表由 `run/` 与 `工程控制/` 的目录名**数据驱动**派生 | `eng/tools/doccheck/check_doc_hygiene.py:22`、`namespace_prefixes():390-403`、表空即 `D3b_namespace_table_empty` 判红 `:648-652`；`git ls-files run` = 1（仅 `.gitkeep`，且以 `.` 开头被跳过）⇒ 干净克隆上前缀表唯一来源是 `工程控制/<包>` | 下一个对话若照 §9"收口即清理"独自删包，CI 会红且**看不出与删包有关**（报的是"命名空间表为空"） | 收口作业表增项：**清包与装下一包同提交**；并把该耦合从 `CONTROL_PACK_SPEC.md` §9 正面写清（现规范只说"移除"，未说必须同时有在位包）——属规范补条，走前台/负责人 |
| **P2-2** | `artifacts/evidence/release-05/` 目录名把任务编号固化进正式证据层 | `tasks/REPORT-501.md:6` | 标准 01 §4 禁任务编号；编号一旦成锚，出库要连带处理 | 保留现名（`CONTROL_PACK_SPEC.md` §7.2 已把 `artifacts/evidence/<控制包>/` 规定为验收记录格式，故**不判违规**），但在出库时确认指针不断链 |

---

## 7 收工基线原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? "ACSD\346\225\264\346\262\273\345\267\245\344\275\234\345\214\205_AUDIT-06.zip"
?? site/
$ git -C ... rev-parse --short HEAD  →  c8f64e9a
```

第二行为 git 的 `core.quotePath` 八进制转义形态，解码后即 `?? ACSD整治工作包_AUDIT-06.zip`（与开工时的显示形态同一项，只是开工那次终端按 UTF-8 直显）。**集合与开工一致、无新增项**：本批取证全为只读命令（`status`/`ls-files`/`grep`/`log`/`show`/`sed -n`/`find`/`wc`/`cat`/`python3 -` 只做 checks.json 的 stdin 解析且不落盘），未触发 `__pycache__`、未写仓库任何文件。

<!-- PROGRESS: 已审 13/13（本批完成） -->
