# 通读 CR-58 · export 阶段投影与 WCS

- 基线 HEAD：`c8f64e9a`
- 派单：`DISPATCH-CODE-READ-PREAMBLE.md` §0–§5 全条适用
- 独占清单（6 项）与实际路径核对：**第 6 项路径与派单不符**（派单写 `lib/algorithms/projection/p3_projection_registry_test.cpp`，仓库跟踪面实际在 `lib/algorithms/projection/tests/p3wcs/p3_projection_registry_test.cpp`），详见末尾「派单与清单差异」。

## 开工 git status --porcelain 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 清单与阅读状态

```text
lib/algorithms/projection/p3_projection.cpp —— 已读：是（表 31 项）
lib/algorithms/projection/p3_projection.h —— 已读：是（表 14 项）
lib/algorithms/projection/p3_projection_registry.h —— 已读：是（表 18 项）
lib/algorithms/projection/p3_wcs.cpp —— 已读：是（表 30 项）
lib/algorithms/projection/p3_wcs.h —— 已读：是（表 19 项）
lib/algorithms/projection/tests/p3wcs/p3_projection_registry_test.cpp —— 已读：是（表 16 项）
```

<!-- PROGRESS: 6/6 -->

---

## 0 本批前置事实（决定全部定级的三条，逐条给位点）

1. **`p3_projection.{h,cpp}` = RETIRED 的 v1 registry**，且**不在产品链接闭包**：
   `lib/algorithms/projection/CMakeLists.txt:22` 的 `astrocs_p3_projection_wcs` 只编
   `p3_wcs.cpp`；根 `CMakeLists.txt:961/:1010` 链接的也是该 target；
   `p3_projection.cpp`/`p3_proj_v6.cpp` 由测试 target 直编
   （`docs/algorithms/PHASE3_PROJ_IMPL.md:586-591` 自述「registry 现状为测试目标直编面：
   非生产构建成员」）。⇒ 落在 v1 的缺陷**写不出生产触发路径**，按派单 §3 上限 S2。
2. **`p3_wcs.cpp` 是出货路径**：`lib/infrastructure/scheduler/src/module_adapters.cpp:14065`
   （export 节点 `p3_wcs_make`）、`:14343`（逐像素 `p3_wcs_pix2world`）、
   `:14098`（`p3_wcs_fits_keywords` 进节点 JSON）、`lib/infrastructure/cli/runtime_client.cpp:67`
   （`p3_wcs_validate_request` 三面共用）。
   ⚠ 反向证据需登记：`eng/ci/ledgers/dormant_algorithms.json:81-140` 把
   `p3_wcs_make/pix2world/world2pix/fits_keywords` 记为
   `dormant_uncompiled`「不在任何生产静态库，也未链入 build/acsd」——
   与 1 的 CMake 事实与 2 的调用点**同时矛盾**（该 ledger 不是本批清单文件，只登记不追查）。
3. **一手文献取得情况**：Paper II（Calabretta & Greisen 2002, A&A 395, 1077）
   经本仓 `docs/references/SCIENTIFIC_REFERENCES.md:32` 给的全文 URL 取到
   **§5.1.3 TAN**：`x = R_θ sin φ`、`y = R_θ cos φ`、`R_θ = (180°/π)·cot θ`；
   **§5.1.5 SIN**：`R_θ = (180°/π)·cos θ`（含 LONPOLE 一般式的 ξ/η 附加项）。
   **§5.2/§5.3（CAR/AIT/MOL/STG/CEA/ZEA）与 §2.2 三 Euler 角未取得**——走过的途径：
   WebSearch（本环境 disabled by permission settings）、arXiv abs 页（仅元数据）、
   arXiv PDF（无文本层）、A&A `aa2294.right.html`（404）、astropy raw `wcslib/C/ait.c`
   （fetch failed）、同一 A&A 全文二次定向取文（工具明说截断在 §5.1.6 之后）。
   ⇒ 凡判定涉及 CAR/AIT/MOL 的**标准式子本身**，一律标 UNPROVEN，不凭记忆断言。
   替代口径沿用本仓自己的：astropy 7.0.1/WCSLIB 逐点对拍 22 组
   （`PHASE3_PROJ_IMPL.md:407-408`、§15.8:604-608）——我未实跑。

---

## 1 `lib/algorithms/projection/p3_projection.cpp`

### §2.5 数值处置表（31 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :38,:39 | `kDeg`/`kRad` | 180.0/π, π/180.0 | 角度↔弧度换算 | 结构性不适用 | 单位换算定义常数，不进科学语义（派单 §4） | — | CONFIRMED |
| :40 | `kMaxAbsDec` | 85.0 | 中心/世界点 \|dec\| 守卫（:148/:175/:331 消费） | 待确认 | `docs/science/PHASE3_HIPS_TO_FITS.md:47`（「请求域收窄，不是投影奇点」，理由=极点邻域球面面积路径未达冻结门）；`PHASE3_PROJ_IMPL.md:545-546` | 收紧（更小）；影响：\|CRVAL2\|>85° 的帧与 \|dec\|>85° 的世界点全拒 | CONFIRMED(条文在，非文献) |
| :43,:45 | `kMaxSide` | 20000（`ASTROCS_P3_MAX_SIDE` 可覆盖） | 画幅边长上界 | 结构性不适用 | `docs/contracts/CONFIG_CONTRACT.md:89`、`phase_config_export.schema.json:234` maximum 20000、SCI:47 —— 三面同值 | — | CONFIRMED |
| :49,:50,:214,:215,:216,:253,:254,:255 | 360.0／180.0／−180.0 | 见左 | RA 周期与最短角差归一 ∈(−180,180] | 结构性不适用 | 圆周定义；wrap 语义由 DESIGN §6.3:453「经度 wrap」要求 | — | CONFIRMED |
| :61,:62 | `sgn_x`/`sgn_y` | −1.0/+1.0，`sgn_y=−sgn_x` | CD 手性符号 | 结构性不适用（符号选择） | ALG §6.2/§6.3:164-190 G1 冻结式（`PHASE3_PROJ_IMPL.md`）逐元素一致 | — | CONFIRMED |
| :75,:76 | `+1.0` | 1.0 | 0-based→FITS 1-based 桥接 | 文献值 | Paper I §2.1.1（`PHASE3_PROJ_IMPL.md:601-603`）；STD-F1 唯一 +1 点（`p3_wcs.cpp:35-59`） | — | CONFIRMED |
| :85 | 1e-300 | \|det(CD)\| 除零哨 | 结构性不适用 | scale>0 ⇒ \|det\|=s²，越哨需 s<1e-150 deg/px，物理不可得；锚 ALG §15.2:461 | — | CONFIRMED |
| :138 | `M_PI/2.0` | 1.5708 rad | TAN pix2world 域上界 | 待确认 | ALG §15.3:486 同值同写法；但 ALG §9:288-294 已把它订正为「ρ\*=atan(π/2)=57.5183634°，**不是半球界**」；SCI:47 说 TAN 唯一数学奇点在距 CRVAL 90° | 收紧方向（多拒）；影响：视场半对角 57.52°–90° 的像素全判 HEMISPHERE（FOV≳115° 才可能） | CONFIRMED |
| :139,:140 | `atan2(1.0,r)`、`atan2(-xi,eta)` | 1.0 | TAN 反演 θ=arccot r（R=cotθ） | 公式导出 | 一手 §5.1.3 `R_θ=(180/π)cot θ`（rad 归一下 1 即 (180/π) 的单位球形），代码按式算：r=√(ξ²+η²)=cotθ ⇒ θ=atan2(1,r) ✓ | — | CONFIRMED(我逐式复核) |
| :165 | 1.0（`rho > 1.0`） | SIN 域界 ρ=sin(离轴角)≤1 | 公式导出 | 一手 §5.1.5 `R_θ=(180/π)cos θ` ⇒ ρ≤1 ✓；ρ=1 边界放行（θ=0）与 ALG §15.3:490 一致 | — | CONFIRMED |
| :183 | `1.0 − stheta²`、`max(0.0,·)` | 1.0/0.0 | SIN 由 cos 距离反算 sin 距离 | 公式导出（恒等式）+0.0 为数值兜底 | 同值实现；`ctheta=√(1−x²)` 在中心附近灾难性消去：ALG §16:649 对 v6 同款已登记「往返误差无上界」，**v1 未登记** | 保守=改用 `asin`/atan2 形；影响 SIN 全中心邻域往返 | CONFIRMED |
| :201 | `M_PI/2.0` | CAR 正向 \|θ\|>90° 才拒 | 待确认 | v3 权威口径是 **\|θ\| ≥ 90° → PARAM**（ALG §15.3:495-497「\|θ\|>90 会放行 =90，故取 ≥」）；v1 用 `>` ⇒ 极行放行 | 收紧为 ≥；影响：整行塌缩的 native 极行被当合法坐标 | CONFIRMED |
| :213,:252 | 90.0 | CAR/AIT world2pix 的 \|dec\| 界 | 待确认 | ALG §15.4:527 只说「TAN/SIN world2pix 端 \|dec\|>85°」；CAR/AIT 用 90.0 无条文；与本文件 :40 的 85.0 是两个数 | 收紧到 85（与其余投影统一）；影响：85°–90° 的 dec 在 CAR/AIT 可进映射 | CONFIRMED |
| :233 | 2.0、4.0（`dsq = 2.0 − (X*X/4.0 + Y*Y)`） | AIT 反演域量 | 公式导出 | 我独立复算：以 D=√(1+cosθcos(φ/2))、X=2cosθ sin(φ/2)/D、Y=sinθ/D ⇒ X²/4+Y²=1−cosθcos(φ/2)，D²=2−A ✓ 代码与自身恒等式同形；`A<2` 作域界是**已登记偏差 D4**（ALG §15.9:625） | — | CONFIRMED |
| :237,:239,:240 | 1.0、`/2.0`、`dsq − 1.0`、`2.0*half` | AIT 反演 \|sinθ\|≤1、φ=2atan2(xp D/2, D²−1) | 公式导出 | 同族恒等式；1.0 为正弦值域 | — | CONFIRMED |
| :258,:260,:261 | 1.0、2.0 | AIT 正向 D、X 系数 | 待确认（一手缺） | **UNPROVEN**：Paper II Table 1 的 AIT 行未取到（§0 途径清单）；本仓 v3 口径含 √2（ALG §15.3:498-501），v1 缺 √2 属**已登记偏差 D3** | — | UNPROVEN |
| :193,:199（注释） | `Y=−θ`、`θ₀=+90°` | CAR 反号／恒等旋转 | 已登记偏差 D1/D2（ALG §15.9:622-623）| — | CONFIRMED |
| :269,:271,:273,:275 | 85.0（×4 行） | registry 声明的中心守卫 | 待确认 | 同 :40；**该字段无任何消费点**（`git grep max_abs_crval_dec_deg` 全仓仅 selfcheck 的 `>0.0` 与 v6 测试） | 见 CR-58-4 | CONFIRMED |
| :269 | 20.0 | TAN 合法 FOV 声明 | 公式导出 | SCI §9a-12:151-152 给出定量推导（平面 FOV=20° ⇒ γ_max=9.9023°、径向 sec²γ_max=1.030462）✓ 数值我可复算 | — | CONFIRMED |
| :271 | 60.0 | SIN 合法 FOV 声明 | 待确认 | 锚只到 ALG §15.1:443「60°（声明值）」，无推导无标定 | 收紧；影响仅声明面（零消费） | CONFIRMED(无锚) |
| :273 | 180.0 | CAR 合法 FOV 声明 | 待确认 | **与权威不同数**：ALG §15.1:444 写 **<180°**（严格），§15.5:541 同 | 取 <180；影响声明面 | CONFIRMED |
| :275 | 360.0 | AIT 合法 FOV 声明 | 待确认 | **与权威相反**：ALG §15.3:504-509「全天空不可构造…可构造 FOV 声明面不含该表述」，上限 229.24°×114.56° | 取椭圆内接矩形上限；影响声明面 | CONFIRMED |
| :282,:296,:301 | 4、4、1 | 表行数/枚举基数/版本标号 | 结构性不适用 | 数组维度与枚举基数（派单 §4） | — | CONFIRMED |
| :309,:310 | `> 0.0`（两处） | selfcheck 对声明值的唯一断言 | 结构性不适用（非空哨） | 但它使注册值无任何数值门 ⇒ 见 CR-58-4 | — | CONFIRMED |
| :332,:333 | 0.0、1、`kMaxSide` | scale>0 与 W,H∈[1,20000] | 结构性不适用 | 锚 ALG §15.4:516-520 ✓ 序一致 | — | CONFIRMED |
| :338,:339 | `(width_px + 1) / 2.0` | CRPIX 像素中心 | 文献值 | Paper I §2.1.1；ALG §15.4:516；`p3_wcs.cpp:114-115` 同式 ✓ | — | CONFIRMED |
| :346-:348 | 0、`width_px − 1`、`height_px − 1` | 四角 0-based 索引 | 结构性不适用 | ALG §15.4:521-522 同点集 | — | CONFIRMED |
| :379,:383 | buf[128]、c1/c2[64] | 打印缓冲 | 结构性不适用 | 最长行 ≈ 27 字节 < 缓冲 < 80 卡宽 | — | CONFIRMED |
| :389-:392 | `%.10f` | CRPIX/CRVAL 打印精度 | 结构性不适用 | ALG §8:270-271 冻结同格式（与出货面 `p3_wcs.cpp:582-585` 逐字一致） | — | CONFIRMED |
| :393-:396 | `%.12e` | CD 打印精度 | 结构性不适用 | 同上 | — | CONFIRMED |
| :383-:384 | `"CTYPE1= '%s'"`（'=' 在第 7 列）、:387-388 `CUNIT1 = `（第 8 列）、:389-392（第 8 列）、:393-396（第 7 列） | 卡列位 | 待确认 | ALG §8:268 自称「FITS 卡形态」且 :275 示例为 `CD1_1  = `（'=' 第 9 列）；**FITS 列位条文我未取到一手**（未取 FITS Standard §3.2.1） | 统一为 8 字符关键字 + 第 9 列 '='；影响：直写头时严格 WCS 读者判错 | PARTIAL（缺一手列位条文） |

### findings

### CR-58-1 · v1 registry 把 AIT 的合法 FOV 声明成 360.0°，与权威「可构造 FOV 声明面不含全天空」直接相反（CAR 亦然：代码 180.0／权威 <180）
- 轴：C（上下级一致）
- 位点：`p3_projection.cpp:275` `    {P3ProjectionId::AIT, "AIT", "RA---AIT", "DEC--AIT", 85.0, 360.0,`；`p3_projection.cpp:273` `..., 85.0, 180.0,`
- 上位依据：`docs/algorithms/PHASE3_PROJ_IMPL.md:445`「| 3 | AIT | … | 椭圆域内（声明值） |」；`:509`「「全天空」只作展示语义（投影自身定义域），**可构造 FOV 声明面不含该表述**」；`:506-507`「可构造矩形帧族上限 **229.24°×114.56°** …全天空（360°×180°）不可构造——四角守卫下 360×180 帧四角 A=1.994>1 ⇒ 必判 HEMISPHERE」；CAR 侧 `:444`「<180°（声明值）」
- 现状：代码把 AIT 行的 `max_fov_deg` 写为字面量 `360.0`、CAR 行写 `180.0`（等号含入），并注释为「ALG §15 claim 值」（`p3_projection.h:64`）
- 差在哪：权威明确禁止在可构造 FOV 声明面写全天空，代码恰以声明字段给出 360.0；CAR 的权威域是严格小于 180，代码给出可达 180.0 的值 ⇒ 同一件事两面不同数
- 后果：注册表一旦被 §15.7 接线（AIT/CAR 进产品），registry 对外的「合法 FOV」承诺与投影自身域界矛盾——按声明选 360° AIT 帧的用户会在 `p3_projection_make` 四角守卫处被拒（ALG §15.3:507 已证必拒），即声明面诱导一个必红请求
- 定级：S3（v1 RETIRED、字段零消费 ⇒ 写不出生产触发路径，按派单 §3 上限降档）
- 怎么算修好：v1 若保留，须与 §15.1 表逐行同数（CAR→严格上界的可表示值或改字段语义为「声明上确界」，AIT→229.24 或改 `double` 为域枚举）；同时把「声明值」的推导/来源写进 ALG §15.1 表，禁再以「claim」二字交差。同批必须改 `eng/contracts/schemas/projection_registry.schema.json:49`（当前 `max_fov_deg` 无上界，改数值会连带该面）
- 置信：CONFIRMED（两侧文本逐字对读；未实跑）

### CR-58-2 · v1 的 CAR native 极行给「合法值」而不是 fail-closed，且这项偏差不在 §15.9 的四行偏差表内 ⇒ 「偏差集合只减不增」的对外主张不可证
- 轴：R（运行时退化返回）＋ D
- 位点：`p3_projection.cpp:201` `    if (std::fabs(theta) > M_PI / 2.0) return P3ProjectionStatus::P3_PROJ_PARAM;`；`:203-206` `    double ra_out = d->crval_ra_deg + phi * kDeg; … *dec_deg = theta * kDeg; return P3ProjectionStatus::P3_PROJ_OK;`；world 侧 `:213` `    if (std::fabs(dec_deg) > 90.0) return P3ProjectionStatus::P3_PROJ_PARAM;`
- 上位依据：`PHASE3_PROJ_IMPL.md:495-497`「**`|θ| ≥ 90° → PARAM`（native 极行 fail-closed：θ=±90° 是整行塌缩——Ω=0、RA 无定义，`|θ|>90` 会放行 `=90`，故判据取 `|θ| ≥ 90°`）**」；`:541` 六要素表同口径；`:612-625` §15.9 偏差表只列 D1（CRVAL2 不进映射）/D2（Y=−θ）/D3（缺 √2）/D4（A<2）
- 现状：v1 判据取严格 `>`，θ=±90° 整行被判 OK，逐像素给出 `(α₀+φ, ±90°)`；`car_world2pix` 端同样放行 |dec|=90，于是「极点」在不同 φ 下映射到不同像素（映射在该行不单射），且往返自洽（往返判据恒绿）
- 差在哪：权威把它列为 v3 三项科学口径之一（`:414-415`），v1 的实际行为与该点相反，而 §15.9 的四行偏差表**没有登记这一项** ⇒ 头注 `:7-9`「行为冻结不变（四项偏差…）」与 §15.1:431「偏差集合只减不增」在 v1 上不是可核验的完备陈述：`p3_proj_legacy_deviation.py` 的 `LEGACY_DEVIATION_TABLE` 只对四项断言，第五项不存在检测点
- 后果：以「v1 只带四项已登记偏差」为前提的任何对照/复用（`:8` 「legacy 偏差对照证据门」、以及未来把 v1 当回归基线的读者）都会把未登记的极点错值当成已知正确
- 定级：S2（无生产触发：v1 不在产品闭包 ⇒ 按派单 §3 上限 S2，注明缺运行时触发路径）
- 怎么算修好：二选一并同批改：① 在 §15.9 加第五行（CAR/AIT native 极行未 fail-closed）并给 `p3_proj_legacy_deviation.py` 加对应判据；② 把 v1 判据改 `≥`——但 ② 会使既有「D1..D4 仍复现」门的语义域变化，须同时更新 §15.9 前言与表；不可只改代码不改门（否则门红）
- 置信：CONFIRMED（判据方向由 :201/:213 原文确定；极行不单射由 :203 的 `α₀+φ` 与 :217-219 的 `X=φ` 直接可读）

### CR-58-3 · TAN 正向域守卫把 57.518° 的平面半径哨兵写成「半球」，与 SCI 自己声明的 TAN 数学奇点位置互斥（v1 与出货面同形）
- 轴：S（该量的凭什么）＋ C
- 位点：`p3_projection.cpp:138` `    if (r >= M_PI / 2.0) return P3ProjectionStatus::P3_PROJ_HEMISPHERE;`；同形出货代码 `p3_wcs.cpp:526` `    if (r >= M_PI / 2.0) return P3_WCS_HEMISPHERE;    // 跨 TAN 半球`
- 上位依据：`docs/science/PHASE3_HIPS_TO_FITS.md:47`「TAN 的唯一数学奇点在距 CRVAL **90°** 的大圆（`cos c = 0`）」；`PHASE3_PROJ_IMPL.md:288-294`「pix2world 侧判据是 `r = tan(ρ) < π/2`，即 **ρ < ρ\* = atan(π/2) = 57.5183634°**…**不是**半球边界…`{r<π/2}` ⊊ `{denom>0}`：环带 57.5183634° ≤ ρ < 90° 是前半球合法天区」
- 现状：`r` 是已归一到 rad 的平面半径（= tan ρ，一手 §5.1.3 复核），把它与 π/2 比较等于把 tan ρ 当成 ρ 判；代码与 `p3_projection.h:48`、`p3_wcs.h:6/:29` 一律称「半球」
- 差在哪：π/2 作为域上界无任何一手/推导依据（不是半球、不是 FOV 门、不是数值退化点），且名称口径与 ALG §9 已订正的表述相反
- 后果：**判据方向=偏红（多拒），不产错值**——被误判「不可达」的是 57.52°–90° 的前半球像素；SCI:47 的「四角与中心同半球」合同约束因此实际被实现成了更强的约束；ALG §9:298-299 已论证 alpha 域（FOV≤20° ⇒ ρ_max≈14.2°）内永不触发
- 定级：S3（v1 面）；**出货面同形位点 `p3_wcs.cpp:526` 与本条同判**——那里的注释仍写「跨 TAN 半球」，`p3_wcs.h:6` 写「TAN 半球守卫(输出四角同半球)」、`:29` 写「输出跨 TAN 半球」，而 `PHASE3_PROJ_IMPL.md:300` 明文规定「该判据的表述口径 = 『视场半对角越界』；用"半球"字样解释它会把 57.52°–90° 的合法天区误判为不可达」⇒ **口径订正只落到 ALG 一层，未贯穿到代码与头文件表述**（不产错值，判据方向为偏红）
- 怎么算修好：若要「半球」语义，判据应是 `denom<=0`（即 r 无界，反投影在 ρ→90° 时 θ→0 自然退化）或显式 `ρ = atan(r) ≥ 90°`；若保留 57.518° 哨兵，则把它改名/改注释为「数值域上界 ρ\*」并给出处（为什么是 atan(π/2)），且与 §15.4 域界语义同批改
- 置信：CONFIRMED（ρ\*=atan(π/2)=57.5183634° 我复算：tan 57.5184° = 1.5708 = π/2 ✓）

### CR-58-4 · registry 的 per-projection 声明字段（中心守卫/FOV）无强制点：`make` 读文件常量、selfcheck 只断言 `>0` ⇒ 「registry 声明即权威」不成立
- 轴：C ＋ D
- 位点：`p3_projection.cpp:331` `    if (std::fabs(centre_dec_deg) > kMaxAbsDec) return P3ProjectionStatus::P3_PROJ_PARAM;`（读 :40 的 `kMaxAbsDec`，不读 `spec->max_abs_crval_dec_deg`）；`p3_projection.cpp:309-310` `        if (!(kRegistry[i].max_abs_crval_dec_deg > 0.0) || !(kRegistry[i].max_fov_deg > 0.0)) return i + 1;`
- 上位依据：`ASTROCS_DESIGN.md:453`（§6.3）「每种声明适用域、奇点、经度 wrap、轴手性与 WCS 关键字」；`p3_projection.h:53-55` 自称该二字段是「适用天区/极点奇点守卫」「合法 FOV 声明」；`PHASE3_PROJ_IMPL.md:454-456` 已把 FOV 降格为「声明字段…非 make 硬门」，但 `:518` 仍说中心守卫「四投影统一保守冻结」——两处并存时中心守卫的权威载体是哪一面没有说
- 现状：全仓检索 `git grep -n "max_fov_deg\|max_abs_crval_dec_deg"`，v1 侧除 selfcheck 的 `>0.0` 外零消费（其余命中在 `p3_wcs.cpp:214-215/440/457` 的 TAN 适用域面与 v6 测试 `eng/tests/unit/v6_p3_proj/v6_p3_proj_test.cpp:165-166`）
- 差在哪：头注承诺「六要素声明（DESIGN §5.3）」是权威载体，实际权威在文件级常量；selfcheck 只保证「非零」，不保证「等于冻结值」⇒ 声明与实施可各自漂移而无机器门
- 后果：当前无错值（两面都是 85.0）；失真发生在 §15.5 六要素审计与 §15.1 表——按文档去读 registry 会以为天区/FOV 已被机器强制
- 定级：S3（latent）
- 怎么算修好：`make` 改读 `spec->max_abs_crval_dec_deg`；selfcheck 增「逐行注册值 == ALG §15.1 表冻结值」断言（正例=现表，负例=改任一数即红）；若 FOV 确认无强制点，则把字段与注释改称「诊断性声明」并在 §15.1/§15.5 同批改词，别让「守卫」二字留在无强制点的字段上
- 置信：CONFIRMED（读文件＋全仓消费点检索）

### CR-58-5 · v1 结构体自称「六要素（DESIGN §5.3）」，但缺 DESIGN 点名的「奇点／经度 wrap」两面，且与合同 schema 要求的必填字段集不同一套
- 轴：C ＋ D
- 位点：`p3_projection.h:52-56`「六要素声明（DESIGN §5.3）：code/ctype（经纬方向+CTYPE 规则）、`max_abs_crval_dec_deg`…`max_fov_deg`…pix2world/world2pix 函数指针」＋ `:58-69` 结构体实字段；`:80` `    P3ProjectionId projection = P3ProjectionId::TAN;  // registry id（非字符串——杜绝 CLI 散落）`
- 上位依据：`ASTROCS_DESIGN.md:453` 要求逐投影声明**适用域、奇点、经度 wrap、轴手性、WCS 关键字**；`eng/contracts/schemas/projection_registry.schema.json:39-41` 的 required 含 `singularity_kind`、`domain`、`reference_point`、`crval2_in_mapping`、`lonpole_default`、`implemented`
- 现状：v1 的 `P3ProjectionSpec` 无奇点/wrap/domain/reference_point 任一字段（奇点只以 `P3_PROJ_HEMISPHERE` 的状态码注释表达，`:48`）；v6 侧已补 `singularity_kind`（`lib/algorithms/projection/p3_proj_v6.h:145`），v1 未补
- 差在哪：头注把「DESIGN §5.3 六要素」当作自己字段集的说明，实际它承载 3/5 要素，合同 schema 另有 6 个必填项 v1 结构体无法产出
- 后果：以 v1 为「registry 驱动 dispatch 的权威声明面」（`:55`「实现不得散落 switch，registry 驱动 dispatch」）做审计会得到「奇点/wrap 已声明」的假结论；且 v1 永远无法产出符合 `projection_registry.schema.json` 的注册文档
- 定级：S3（RETIRED＋零消费）
- 怎么算修好：要么把头注改成「v1 部分要素（见 §15.9 退场登记），完整六要素以 v6 + 合同 schema 为准」，要么补字段；两者都需与 `docs/modules/phase3_proj.md:46`、`docs/owner/SCIENCE_OVERVIEW.md:103` 的表述同批改，避免声明面互相指认
- 置信：CONFIRMED（三面字段清单逐条对读）

### CR-58-6 · `p3_projection_fits_keywords` 与出货 `p3_wcs_fits_keywords` 同族：关键字卡 '='/值列位不满足自身文档给出的 FITS 卡形态
- 轴：C
- 位点：`p3_projection.cpp:383-384` `    std::snprintf(c1, sizeof(c1), "CTYPE1= '%s'", spec->ctype1);`（'=' 第 7 列）、`:387` `    add("CUNIT1 = 'deg'");`（第 8 列）、`:389` `"CRPIX1 = %.10f"`（第 8 列）、`:393` `"CD1_1 = %.12e"`（第 7 列）；出货面同形 `p3_wcs.cpp:578-589`
- 上位依据：`PHASE3_PROJ_IMPL.md:268-275` §8「逐行文本，每行 ≤80 字节（**FITS 卡形态**）」并给示例 `"CD1_1  = -1.234567890123e-05 / comment"`（'=' 在第 9 列）；`docs/standards/STANDARDS_REGISTRY.md:64` 对「Paper I §3（CD/CTYPE 关键词体系）」判 CONFORMANT
- 现状：12 行里没有一行把 '=' 放在第 9 列（CTYPE 系第 7、CUNIT/CRPIX/CRVAL 第 8、CD 第 7）；头注 `p3_projection.h:110-111` 只承诺「每行 ≤80 字节」
- 差在哪：同一文档 §8 给的示例与代码字节序列不同（列位差 1–2），而标准判定面（STANDARDS_REGISTRY:64）已对该文件判 CONFORMANT
- 一手状态：**UNPROVEN（就「第 9 列」而言）**——我未取到 FITS Standard/Paper I 关于列位的条文原文；已走途径：只读 §8 示例与 `STANDARDS_REGISTRY.md:64` 的条文表述（未登记列位式号）。**就「与 §8 示例不一致」而言 CONFIRMED**
- 后果：ALG §8:272-273 明确本函数是「descriptor→文本的探针/调试面，生产 FITS 头写路径在 p3_output 域」⇒ 产品 FITS 不受影响；受影响的是 `module_adapters.cpp:14098` 把它塞进节点 JSON `fits_keywords` 字段后的**证据/诊断可读者**（严格 WCS 读者判为坏卡）
- 定级：S3
- 怎么算修好：统一为 8 字符关键字左对齐 + 第 9 列 '='；同时把 §8 示例与断言面（`p3_wcs.h:61`、`p3_projection.h:110-111` 的行格式注释）改为可判性质「第 9 列为 '='，行长 ≤80」并补一条正/负例断言（只测长度会永远绿）
- 置信：PARTIAL（缺 FITS 列位一手条文；缺该文本被任何消费者按卡解析的实例）

### 本文件「核过、不报」的项（附检索式）
- **面积元／尺度因子的赤纬余弦（派单重点②）**：本文件**不存在**面积元或立体角计算，`cd` 只承担 deg/px 线性映射；检索式 `git grep -n "cos(.*dec\|omega\|Ω\|solid\|steradian\| area\|sr\b" -- lib/algorithms/projection/p3_projection.*` → 仅命中变量名 `cd = std::cos(d0)`（:100，是 cos δ₀ 用于旋转核，不是面积元）。⇒ 该项在本清单内无可指控对象。
- **极点/分支经线奇点在 TAN 路径**：`tan_pix2world` 的 `atan2` 与 `native_to_sky_zenithal` 对 (ξ,η)=(0,0)、Δα=±180° 均良态（atan2(0,0)=0 只发生在 φ 真不可定的中心，返回值 = CRVAL，符合切点语义）；往返我按 :139/:140 与 :120-125 逐式复核为互逆。
- **v1 四项偏差本身（D1..D4）**：与 `PHASE3_PROJ_IMPL.md:612-625` 登记一致 ⇒ 按派单 §4「已显式声明退役/不适用」不报（唯一例外是本文件未登记的第五项，见 CR-58-5/CR-58-2）。
- **RA wrap 与最短角差**：`:214-216` 的 `fmod`+双条件给出 (−180,180]，与 :253-255 同形；跨 ±360 输入良态。
- **`*count==4`、`i>=4`、enum 基数**：结构性常数，按派单 §4 不报。

---

## 2 `lib/algorithms/projection/p3_projection.h`

### §2.5 数值处置表（14 项）

| 位点 | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :33 | `kP3ProjectionRegistryVersion` | 1 | registry 版本标号 | 结构性不适用 | 版本标识，无科学语义；ALG §15.1:427-428 同值 ✓ | — | CONFIRMED |
| :34 | `kP3ProjectionRegistryRetired` | true | 退场标记 | 结构性不适用 | ALG §15.1:428 ✓（selfcheck :302 强制为真） | — | CONFIRMED |
| :36-:41 | `P3ProjectionId` | 0,1,2,3 | 枚举基数/位移 | 结构性不适用 | 表行序=枚举值（cpp:308 断言 id==i）；序与 ALG §15.1:421 冻结序前四行一致 ✓ | — | CONFIRMED |
| :44-:48 | `P3ProjectionStatus` | 0,1,2,3 | 状态码值域 | 结构性不适用 | ALG §15.4:524-525「与 P3WcsStatus 冻结对齐」✓ 与 `p3_wcs.h:25-30` 同值 | — | CONFIRMED |
| :46 | 注释 `parity/\|dec\|>85°/scale/尺寸/空指针/CAR \|dec\|>90°` | 85、90 | PARAM 触发清单 | 待确认 | 85 见 cpp:40；**90 与 cpp:213 一致但与 ALG §15.4:527 只写 85° 的口径并存两数** | 收紧统一 85 | CONFIRMED |
| :48 | 注释 `TAN r≥π/2 / SIN ρ>1 / AIT D²≤0` | π/2、1、0 | HEMISPHERE 触发清单 | 待确认／公式导出 | SIN ρ>1 ✓一手 §5.1.5；TAN r≥π/2 见 CR-58-3；AIT「D²≤0」= cpp:233 的 `dsq>0` ✓ 同式 | 见 CR-58-3 | CONFIRMED |
| :53-:55 | 「六要素声明（DESIGN §5.3）」 | — | 字段语义声称 | 待确认 | 见 CR-58-5；DESIGN 实际条文在 §6.3:453（非 §5.3） | — | CONFIRMED |
| :63 | 注释「四投影统一 85.0 保守冻结」 | 85.0 | 中心守卫声明 | 待确认 | ALG §15.1:442-445 同值；字段零消费见 CR-58-4 | — | CONFIRMED |
| :64 | 注释「TAN=20 SCI §9a-12 冻结；其余 ALG §15 claim」 | 20 | FOV 声明 | 公式导出（TAN 侧）／待确认（其余） | SCI §9a-12:151-152 有推导；「claim」侧无锚 | — | CONFIRMED |
| :73,:74,:76,:77,:78,:79 | 成员缺省 `crval_ra_deg/crval_dec_deg/crpix/cd/width/height` = 0 | 0 | 零初始化 | 结构性不适用（哨兵值由 `make` 的 :325 `*out = P3WcsDescriptor{}` 与 :85 除零哨兜住，非「未测当 0 计入」） | cpp:325 失败保持零初始化 ✓；cpp:85 det 哨 ✓ | — | CONFIRMED |
| :80 | `projection = P3ProjectionId::TAN` | 枚举 0 | descriptor 的投影成员缺省 | 待确认 | **缺省即一个合法投影** ⇒ 未设置的 descriptor 静默按 TAN 分派（cpp:361/:370/:377 全部经 `find_id(d->projection)`）；与 `p3_projection_registry.h:22-23`「不得静默回落 TAN」的口径冲突面见 CR-58-10 | 收紧方向：无「未设置」态（加哨兵枚举或强制显式入参） | CONFIRMED |
| :84 | 注释「`*count==4`；table 非空恒成立」 | 4 | 表长 | 结构性不适用 | cpp:282 ✓ | — | CONFIRMED |
| :90-:93 | 注释「行数=4/码互异/CTYPE 非空/函数指针非空/版本常量=1 且 RETIRED 为真」「否则返回首个失败项的行索引+1」 | 4/1 | selfcheck 断言面与返回码约定 | 待确认 | cpp:301-302 的版本/退场失败**也返回 1**，与「行索引+1」的 i=0 返回值碰撞 ⇒ 返回码不能区分「版本错」与「第 0 行错」 | 返回码分域（如版本类 ≥100，学 `p3_projection_registry.h:277-307` 的分段法） | CONFIRMED |
| :96-:97 | 注释校验序 `out 非空→parity→\|dec\|≤85°→scale>0→W,H∈[1,20000]→G1 CD→四角守卫` | 85、[1,20000] | 参数校验序 | 结构性不适用＋条文锚 | cpp:324-353 逐序一致 ✓；ALG §15.4:516-522 同序 ✓ | — | CONFIRMED |
| :110-:111 | 注释「每行 ≤80 字节」 | 80 | FITS 卡宽 | 待确认 | 见 CR-58-6（列位一手未取到；行长本身成立） | — | PARTIAL |

### findings

**本文件可辩护指控：CR-58-5（六要素声明与实际字段集）、CR-58-4（声明字段无强制点，头注把它写成守卫）、CR-58-6（卡格式）、CR-58-9（§5.3 权威锚错挂，见 §3 的 CR-58-9）、CR-58-10（`projection` 成员缺省为 TAN 的静默回落面）；
此外 `:18`「权威依据（与 v6 线一致）: ASTROCS_DESIGN.md §5.3 八投影冻结集合」的锚错挂见 CR-58-9。
不另立新条：本文件的公式性内容全部在 .cpp 侧，处置表与 findings 已并入 §1。**

### 本文件「核过、不报」的项
- `:11-15` 的四项偏差声明与 `.cpp` 实际行为逐条对读为**真**（D1 见 cpp:203-206/:217-219；D2 见 cpp:200/:219；D3 见 cpp:258-261 无 √2；D4 见 cpp:233 `2.0 − A`）⇒ 头注没有虚报已登记偏差，不报。
- `:43` 「值域与 lib/phase3_session P3WcsStatus 冻结对齐，0..3」：现址是 `lib/algorithms/projection/p3_wcs.h:25-30`（值同 0..3），**目录名 `lib/phase3_session` 已迁移** ⇒ 属改名未贯穿的面，本批只登记（检索式 `git grep -n "lib/phase3_session" -- lib/algorithms/projection`）；不作为 finding：不改语义。
- `:77` 「CD-only（禁 PC+CDELT）」与 `:73-80` 无 PC/CDELT 成员 ✓ 一致。

---

## 3 `lib/algorithms/projection/p3_projection_registry.h`

### §2.5 数值处置表（18 项）

| 位点 | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :40-:43 | `P3ProjProductStatus` | 0,1,2 | 产品声明三态 | 结构性不适用 | 枚举基数；selfcheck :283 卡 0..2 ✓ | — | CONFIRMED |
| :67 | 注释「CLI 错误文本上限 200 字节 (parser.cpp sanitize)」 | 200 | 错误消息截断长度 | 待确认 | 我未核 `lib/infrastructure/cli/parser.cpp` 的 200 是否现行值（本清单外）；该数决定 :171-174 的消息顺序设计 | 若实际上限更小 ⇒ 「已支持清单」仍可能被截掉 | PARTIAL |
| :69,:87 | `kTable[8]` / `*count = 8` | 8 | 冻结集基数 | 结构性不适用 | DESIGN §6.3:453 八投影 ✓（**但锚写作 §5.3，见 CR-58-9**） | — | CONFIRMED |
| :70-:85 | 逐行 status 与 reason | 1×TAN declarable / 3×kernelOnly / 4×notImplemented | 产品声明状态 | 待确认 | ALG §15.1:432-434、docs/owner/SCIENCE_OVERVIEW.md:103 同述 ✓；reason 为英文自陈，无锚要求 | — | CONFIRMED |
| :92-:94 | `2.5e-5 px`、`6.1e-3 px`、`1.7e-2 px` | SIN 往返消去量级 | 待确认 | 锚指向 §94「实测」，无仓内结果件 | 保守：把 SIN 视为不可用（现已是 kKernelOnly） | PARTIAL |
| :95 | `~2.5e-10 px` | 独立 Oracle 可达精度 | 待确认 | 同上（ALG §15.6:564 同值） | — | PARTIAL |
| :101 | `2026-09-21` | 实测日期 | 结构性不适用 | 非科学量；但负责人口径「文档内禁日期与历史」在代码注释面的适用性未定 ⇒ 登记不判 | — | CONFIRMED |
| :102-:104 | `7.39e-5`、`4.40e-3`、`0.25` px 步长、证据 `run/CLEAN-401/evidence/sin_roundtrip_gate.json` | CLEAN-401 实测 | **待确认（判据档 3 不合）** | 派单 §2.5 档 3 要求标定结果「必须落在跟踪面，指 `run/` 即判不合格」；本条证据指针正是 `run/`。缓解面：复现门 `eng/tests/unit/v6_p3_proj/sin_roundtrip_gate.py` 在跟踪面（`git ls-files` 命中）⇒ 数值可再生，但**当前不可核** | 保守：视该组数值为未标定 ⇒ 影响仅「登记面语气」，不改产品语义 | CONFIRMED(指针不合规) |
| :122,:140 | `kDeclared[1]={"TAN"}`、`kImplemented[1]={"TAN"}` | 1 | 声明集/实现集 | 待确认 | ALG §15.1:432、SCI §9a-3（会话仅 TAN）✓；两面各写一次＋ :70-:85 状态一次 ⇒ **同一事实三处**（漂移风险登记于此，未见当前漂移） | 单一事实源：由 frozen_table 的 status 导出 D/I | CONFIRMED |
| :166 | `code ? std::string(code) : std::string("TAN")` | 缺省 TAN | 请求面缺省投影 | 文献值（合同条文背书） | `phase_config_export.schema.json:206`「投影选择，缺省 TAN」、DESIGN §6.3:455「用户通过 `projection` 字段选择，缺省 TAN」、`p3_wcs.h:34` ✓ 三面一致 ⇒ **不算静默回落** | — | CONFIRMED |
| :196-:197 | 注释 `1e-8 px / 1e-6 px` | 紧门/全域门 | 公式导出（转引单一事实源） | `p3_wcs.cpp:218-219`＋门表 `docs/algorithms/GATES_AND_TOLERANCES.md:67-68`（含 ε≈C_env·u·sec²Δ/s_rad 推导）✓ 本函数不复写数值 ✓ | — | CONFIRMED |
| :212-:213 | 注释 `1.8"/px（0.0005 deg/px）`、`FOV 对角 0.045°`、`≥ 0.9"/px` | 探针几何 | 结构性不适用（探针选择） | 0.0005·3600=1.8 ✓；64·0.0005=0.032°、对角 0.0453° ✓ 与注释自洽；下限 0.9 与 `p3_wcs.cpp:220` 同值 ✓ | — | CONFIRMED |
| :216 | `p3_wcs_make(150.0, 2.0, 0.0005, 64, 64, "east_left", 0.0, &d, code)` | 150/2/0.0005/64/64/east_left/0.0 | 探针几何实参 | 待确认 | 无任何条文规定探针取该组；**PA=0.0 且只 east_left ⇒ 探针不覆盖旋转与反手性分支**（这两支在 `p3_wcs.cpp:137-146` 是分开的代码路径） | 保守=至少补 parity=east_right 与 PA≠0 两例；影响：判据覆盖面，不改产品值 | CONFIRMED(覆盖面) |
| :240,:246 | `> 0.0`、`max_err_px < gate.tol_px` | 门判据 | 公式导出（门语义） | 方向已钉：`!(err<tol)` ⇒ 红，NaN 亦红（NaN<tol 为假）✓ fail-closed | — | CONFIRMED |
| :277-:305 | 返回码 1、2+i、20+i、40+i、60+i、80+i、100、101+i、110+i、120+i、130+i | 13 个哨段位 | 结构性不适用（诊断码分域） | 位段互不重叠（i<8、i<nd、i<ni）✓ 可区分 | — | CONFIRMED |
| :283 | `s < 0 \|\| s > 2` | 2 | 枚举合法性上界 | 结构性不适用 | 枚举基数 | — | CONFIRMED |

### findings

### CR-58-7 · `p3_proj_probe` 自称「不查声明表、只看生产路径」，实际第一步就经 `p3_wcs_validate_request → p3_proj_declare` 读声明表 ⇒ 共址测试的「未实现码跑不通」是恒真断言
- 轴：R（判据方向）＋ D
- 位点：`p3_projection_registry.h:204` `    // 探针不查声明表/实现表 —— 只看生产路径的真实行为（判据独立性）。`，紧接 `:206` `    const P3WcsStatus vst = p3_wcs_validate_request(code, nullptr, nullptr, &why);`
- 上位依据：同文件 `:19-20`「判据: D == I == 实际可运行集 R…任一不等即判红：声明未实现=假声称（禁止）；**实现未声明 = 隐藏能力（禁止）**」；`tests/p3wcs/p3_projection_registry_test.cpp:11`「C1 机器判据…（R = 生产路径黑盒实跑）」、`:9`「MOD-01 🔴P17／ARCH-01 🔴14（文档说 1、代码说 4 ⇒ 以可运行验证为准）」、`ENGINEERING_SPEC §8`「每项检查能红能绿」
- 现状：调用链 `:206 → p3_wcs.cpp:68 → p3_projection_registry.h:165-167 → :127-131 → :55-61`；未进 `kDeclared` 的码在探针第一步即返回 `P3_WCS_UNSUPPORTED`，探针**从不**尝试 `p3_wcs_make`。测试侧 `test.cpp:144` `if (astrocs::phase3::p3_proj_is_declared(code)) continue;` 只对**未声明码**执行 `:172-174` 的断言「实跑探针: 未实现码跑不通」
- 差在哪：**判据方向钉死**——被检集合 = 未声明码 ⇒ `p3_proj_declare` 必返 UNSUPPORTED ⇒ `probe != OK` 对被测的每一个输入都恒真 ⇒ 该子判据零区分力（恒真=永不变红=误绿侧）。声称的「以可运行验证为准」在这条上不成立；R 集合按构造 ⊆ D 集合，故 `D==I==R` 中 R 项只检测「已声明码是否真跑得通」一个方向，「生产路径其实能跑而表没登记（隐藏能力）」这一方向只能靠人工登记的 `kImplemented` 常量，而 :20 恰好把该方向也写成由本判据检测
- 后果：对外主张「三层集合机器判据、隐藏能力禁止」的成立面缩小到 D/I 两个自写常量的一致性；若 export 接线 SIN（v6 已有内核），而 `kDeclared/kImplemented` 未同步，则本测试仍全绿（矩阵 `matrix_mode()` :591 明确「不参与断言」，恰恰只有它能看见内核真实可跑性）
- 定级：S2
- 怎么算修好：探针拆两步并各自断言：① **绕过产品声明门**直接实跑内核算「物理可跑集 R_phys」（现成材料即 `matrix_mode()` :629-652 的 v6 路径），把 R_phys 纳入断言；② 产品门另测「声明表与产品门的拒绝集合一致」。同批把 `:204` 注释与 `:19-20` 的判据范围改写为实际检测到的方向，并删除或转正 `test.cpp:172-174` 的恒真断言（改为对 R_phys 断言）。**注意防误红**：把 v6 内核纳入 R_phys 断言后，SIN/CAR/AIT 行会从「跑不通」变「跑得通」，必须同时把 §15.1 的 kKernelOnly 语义与之对齐
- 置信：CONFIRMED（全链逐跳读码；未实跑）

### CR-58-8 · 出货/登记两面对同一 `projection` 键给出互斥值域：本文件按大写精确匹配，`export_wcs` 合同面与插件文档/配置登记面只允许小写
- 轴：C（主）＋ R
- 位点：`p3_projection_registry.h:55-61`（`in_list` 用 `std::strcmp`）＋ `:122` `static constexpr const char* kDeclared[1] = {"TAN"};` ＋ `:48`「code 为静态存储的冻结码字面量（大小写敏感）」；对照 `eng/contracts/schemas/phase_config_export.schema.json:195-206`（`export_wcs.properties.projection` 的 enum 为 `tan/sin/car/ait/stg/mol/cea/zea` 全小写）与同文件 `:329-341`（`$defs/projection_code` 全大写）与 `eng/contracts/schemas/projection_registry.schema.json:45`（全大写「精确匹配，无 fallback」）
- 上位依据：`docs/plugins/algorithms_phase3/14_projection.md` §5 表 `:35`「| `projection` | `tan` | —— | tan/sin/car/ait/stg/mol/cea/zea |」；`eng/packaging/config/config_registry.json:1030-1041` 把该键登记为 `declared_default: "tan"`、`registered_at: …#/$defs/export_wcs/properties/projection`、`finding: "none"`、note「缺省 tan 与插件一致」；DESIGN §6.3:453-455 用大写
- 现状：实现侧唯一入口 `p3_proj_declare`（:165-190）大小写敏感；`p3_wcs.cpp:68` 把请求值原样透传；CLI `lib/infrastructure/cli/runtime_client.cpp:63` `const std::string proj = pdoc.value("projection", std::string("TAN"));` **不做大小写归一**；测试 `test.cpp:178` 明确把 `"tan"` 列为「未冻结/大小写变体 → UNSUPPORTED」
- 差在哪：同一键在四面上不是同一套值（两份合同互斥、插件与配置登记面取小写、实现取大写），且登记面对该互斥标注「finding: none／与插件一致」
- 后果：**方向=偏红（拒绝），不产错值**——按插件文档/`export_wcs` 写 `"projection": "tan"` 的合法配置，经 `runtime_client.cpp:63 → p3_wcs.cpp:68 → registry.h:166-167/127-131` 被判 `unknown projection code (not in the frozen 8-projection set)`，而该消息本身失真（`tan` 在冻结集内，只是大小写不同）。export 的对外主张「用户通过 `projection` 字段选择」（DESIGN §6.3:455）在小写面上不成立
- 定级：S2；**缺运行时触发路径的一角证据**：我未实跑 CLI，也未追到 `export_config.wcs.projection` → 节点 `wcs_plan.projection`（`module_adapters.cpp:13805` 同样原样透传）之间的转换层是否做过大小写归一
- 怎么算修好：先由负责人定唯一面；再同批改：schema 两份 enum 合一（或让 `export_wcs` 引用 `$defs/projection_code`）、插件文档 §5、`config_registry.json` 的 `declared_default`/note、实现侧（若定小写面则 `p3_proj_declare` 显式归一并把归一写进 `projection_registry.schema.json` 的「精确匹配」条文，不能留成隐式行为）、`test.cpp:178` 的负例语义与 `:189` 的消息断言。加一条机器门：三面投影码集合逐值（含大小写）相等
- 置信：CONFIRMED（三面文本＋调用链逐跳读到）／接线末段 PARTIAL

### CR-58-9 · 本模块把「八投影冻结集合 + 各数值」的权威锚统一挂到 `ASTROCS_DESIGN.md §5.3`，而 §5.3 实为 SNR 条款；被引为「原文」的句子含 DESIGN 零提及的内容
- 轴：S（凭什么）＋ D ＋ C
- 位点：`p3_projection_registry.h:2`「（ASTROCS_DESIGN.md §5.3 的唯一产品声明权威）」、`:5-9`「规范依据: ASTROCS_DESIGN.md §5.3: 首批冻结 8 投影 …『已实现并可作为产品声明的以实际注册表为准; 未实现的必须显式报「不支持」, 禁止声称支持』(当前登记: 仅 TAN 已实现); 每种投影必须声明适用域（含 TAN 的 |dec|≤85°、FOV≤20°、手性 det(CD)<0、CRPIX 用 FITS 1-based 像素中心、往返误差 < 声明容差（TAN: 1e-8 px, Oracle 冻结）), 违反 ⇒ 拒绝」；同族错挂：`p3_projection.h:18`、`p3_wcs.h:64`、`p3_wcs.cpp:157/:169/:195`、`tests/p3wcs/p3_projection_registry_test.cpp:5`、`eng/contracts/schemas/phase_config_export.schema.json:206`（「ASTROCS_DESIGN.md §5.3:264-266」）、`:340`、`:177`（「§5.3:267」）
- 上位依据（我核的是最高设计本体）：`ASTROCS_DESIGN.md:385` = `### 5.3 SNR 重建与逆方差叠加`；投影条文实为 `:434 ## 6. export（Phase3）：投影导出` 下 `:451 ### 6.3 投影算法`，原文 `:453`「内置多种投影，首批冻结 TAN / SIN / CAR / AIT / STG / MOL / CEA / ZEA；每种声明适用域、奇点、经度 wrap、轴手性与 WCS 关键字；注册表中未实现的投影在被选择时显式报"不支持"（当前仅 TAN 可用）」、`:455`「用户通过 `projection` 字段选择，缺省 TAN」；被引区间 `:264-:266` 实为 normalize 流程 mermaid 的节点行（`:258-:272` 段）
- 条文面零提及检索式：`grep -n "5\.3" ASTROCS_DESIGN.md`；`grep -n "首批冻结\|projection registry" ASTROCS_DESIGN.md` → 仅 :453/:454；`grep -n "85°\|FOV≤\|1e-8" ASTROCS_DESIGN.md` → 无投影相关命中
- 现状：被标为「原文」的引句含三处 DESIGN 没有的内容：①「以实际注册表为准／禁止声称支持」的措辞；② 逐投影声明清单缺「奇点、经度 wrap」而多了 85°/20°/det<0/CRPIX/1e-8 五个具体数值；③ 「违反 ⇒ 拒绝」
- 差在哪：锚指向的条文不含被引内容（数值与措辞都不是 §5.3 也不是 §6.3 说的），并且 ② 的五个数值的真实载体是 SCI §4/§9a-12 与门表 G-P1-WCS-BRIDGE
- 后果：AGENTS §1 的逐层下钻读法在该模块第一跳就断链；变更流程与「设计级冻结」的归属会被错判（例如把 1e-8 px 当作 DESIGN 冻结而绕过 §7 变更门，或在 §5.3 找不到投影条款后按印象另定域）
- 定级：S2（不产错值，但「DESIGN 冻结」这一对外主张不成立）
- 怎么算修好：把六处引用改指 `ASTROCS_DESIGN.md §6.3:453-455`，引句改为逐字摘录；五个数值改锚到各自正本（SCI `PHASE3_HIPS_TO_FITS.md` §4/§9a-12、`GATES_AND_TOLERANCES.md` G-P1-WCS-BRIDGE）；并核 `docs/algorithms/PHASE3_PROJ_IMPL.md:401-405` 同一段引文的"原文"标记（该文件不在本批清单，需同批改，否则文档层与代码层锚仍不同）
- 置信：CONFIRMED

### 本文件「核过、不报」的项
- `p3_proj_registry_selfcheck`（:272-308）的包含关系断言：`D ⊆ F`、`D ⊆ I`、`I ⊆ F`、`declared == kProductDeclarable` 逐条读码为**真实现**（返回码分域可诊断）；头注 :269-271 承诺的检查项与代码一一对上 ⇒ 不报「声明未实现」。
- 消息顺序设计（:171-174）与 CLI 200 字节截断的关系：顺序（请求码→unsupported→清单→原因）确实把清单放在原因之前 ⇒ 截断不吃清单，逻辑成立 ⇒ 不报。
- 空串 vs nullptr：`p3_proj_declare("")` → `empty projection code` + UNSUPPORTED（:176-177）✓ fail-closed ⇒ 不报。
- 面积元/赤纬余弦（派单②）：本文件无任何面积量；检索式 `git grep -n "cos\|area\|sr\|omega" -- lib/algorithms/projection/p3_projection_registry.h` → 零命中。
- 注册表测试是否注册进基线（派单⑤）：**是**——`lib/algorithms/projection/tests/p3wcs/CMakeLists.txt:24` `add_test(NAME p3_projection_registry …)`、`:26-27` `p3_projection_registry_selftest`；CI 面 `eng/ci/checks.json:10069-10080`（`p3_projection_registry`）、`:10166-10200`（selftest）、`docs/ci/01_CHECKS.md:105-106`（CHK-P3-PROJ-DECL / -SELFTEST，P0）⇒ 该项不成立为缺陷，见 §6 对判据力的具体分析。

---

## 4 `lib/algorithms/projection/p3_wcs.cpp`（出货 TAN 内核）

### §2.5 数值处置表（30 项）

| 位点 | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :18,:19 | `kDeg`/`kRad` | 180/π, π/180 | 角度↔弧度 | 结构性不适用 | 单位换算定义 | — | CONFIRMED |
| :20 | `kMaxAbsDec` | 85.0 | 中心/世界点 \|dec\| 守卫（:105/:314/:367/:545 消费） | 待确认 | SCI `PHASE3_HIPS_TO_FITS.md:47`（请求域收窄＋理由＋变更门）；`docs/contracts/PUBLIC_API.md:2002`；`phase_config_export.schema.json:319/461/479` 同值 ✓ 四面一致 | 收紧；影响：极区 5° 内全拒 | CONFIRMED |
| :24/:26 | `kMaxSide` | 20000（编译期可覆盖） | 画幅上界 | 结构性不适用 | `CONFIG_CONTRACT.md:89`、schema maximum 20000、ALG §11:326-327 ✓ | — | CONFIRMED |
| :30/:31 | 360.0 | RA 归一 | 结构性不适用 | 圆周定义 | — | — | CONFIRMED |
| :49 | `kFitsPixelOrigin` | 1.0 | 0↔1-based 唯一桥接量 | 文献值 | Paper I §2.1.1（`p3_wcs.cpp:35-59` 注释＋ALG §7.1；门 G-P1-WCS-BRIDGE 含「移除/错置 +1 必 ≥1px」负例） | — | CONFIRMED |
| :114/:115 | `(width_px + 1) / 2.0` | CRPIX 中心 | 文献值 | Paper I §2.1.1；`p3_wcs.h:84` 声明；测试 :231/:466 断言 ✓ | — | CONFIRMED |
| :138/:139 | −1.0/+1.0、`sgn_y=−sgn_x` | CD 手性 | 结构性不适用（符号） | ALG §6.2/§6.3:164-190 G1 冻结式逐元素一致 ✓ | — | CONFIRMED |
| :149-:151 | 0、`width_px − 1`、`height_px − 1` | 四角 0-based | 结构性不适用 | ALG §15.4:521 ✓ | — | CONFIRMED |
| :214 | 85.0 | `kTanApplicability.max_abs_crval_dec_deg` | 待确认 | 与 :20 同值但**写第二份字面量**（同一常数两处）⇒ 漂移风险；锚同上 | 单一事实源 | CONFIRMED |
| :215 | 20.0 | `max_fov_deg` | 公式导出 | SCI §9a-12:151-152 给定量推导（FOV=20° ⇒ γ_max=9.9023°、径向 +3.046%）✓ 且该门在 :457 真强制 | — | CONFIRMED |
| :218 | 1e-8 | 紧门 `roundtrip_tol_px` | 待确认（有推导件但件在 run/） | ALG §7.3:252-260、门表 GATES:67（含 ε 式与 C_env）；证据 `p3_wcs.cpp:209` 指 `evidence/kernel_roundtrip.json`＋`run/GATE-DERIVE-01/` ⇒ 跟踪面上只有门表文字与代码注释 | 收紧＝更严；影响 G-P1-WCS-BRIDGE 判据 | PARTIAL（推导件不入库） |
| :219 | 1e-6 | 全域保守门 | 待确认 | SCI-WCS-001 §11 STD-F1（`GATES:68` 登记）✓ | — | PARTIAL |
| :220 | 0.9 | `min_scale_arcsec` 紧门适用域下限 | 待确认 | GATES:67 推导（「上限取值 0.179/0.293″/px ⇒ 下限 0.9 覆盖仓内最小真实尺度 0.9586″/px，余量 ≥5.0×」）；数值本体只在代码注释＋门表 | 取大＝更严；影响：0.9″/px 以下紧门不适用 | PARTIAL |
| :221 | 128.0 | `envelope_c_env`（注释：实测 max 78） | 待确认 | 设计常数，取实测 78 的上取整；锚同上（实测面在 run/） | 取大＝包络更大＝更易判 OUT_OF_DOMAIN（**偏松**，因为 OUT_OF_DOMAIN 不判红） | PARTIAL |
| :225 | 1.1102230246251565e-16 | u = 2⁻⁵³ FP64 单位舍入 | 文献值 | 注释 :224 引 Higham 2002；数值我可复算 2⁻⁵³ ✓ 逐位等 | — | CONFIRMED |
| :227 | 206264.80624709636 | rad→arcsec | 结构性不适用 | 单位换算定义（我复算 180·3600/π ✓） | — | CONFIRMED |
| :228/:248 | 3600.0 | deg→arcsec | 结构性不适用 | 派单 §4 明列此类不报 | — | CONFIRMED |
| :233/:270/:432/:571 | `? : "TAN"` | 缺省投影码归一 | 待确认 | 请求面缺省有合同背书（DESIGN §6.3:455、schema:206）；**descriptor 面（:432/:571/:233）把空/未设也归一成 TAN ⇒ 见 CR-58-10** | 见 CR-58-10 | CONFIRMED |
| :239/:246/:249/:254 | −1.0 / 0.0 | 非法入参的退化返回 | 待确认 | 见 CR-58-11（0.0 的两种语义） | — | CONFIRMED |
| :257 | `c_env` 兜底 0.0 | 声明缺失时的包络 | 待确认 | 与 :254 同族（CR-58-11） | — | CONFIRMED |
| :261 | `1.0 + half*half`，注释「≤1.03046 @FOV=20°」 | sec²Δ 近似式 | 公式导出 | 一手 §5.1.3 的 TAN 径向尺度 sec²γ（SCI:152 同式）✓ 我逐步复算：FOV=20° → :259 `fov_rad`=0.34906585 → :260 `half`=0.17453292 → :261 `1+half²`=**1.0304617** ⇒ 与注释 1.03046 **同数**，与 SCI:152 的 `sec²γ_max=1.030462` 亦同数（差在 γ 与 tan γ 之间 1.6e-9，可忽略）⇒ 该行无 finding（详见 CR-58-13 的复算记录） | — | CONFIRMED |
| :263 | `c_env * u * sec2 / s_rad` | 包络式 | 公式导出（同 :218 的锚缺） | GATES:67 同式 | — | PARTIAL |
| :281/:284 | `scale >= 0.9`、`envelope <= 1e-6` | 门选择比较方向 | 结构性不适用（门语义） | ALG §7.3、GATES:67-68 ✓；方向已核：`err < tol` 才绿（:500 `if (!(err < gate.tol_px))` ⇒ NaN/Inf 皆红）✓ fail-closed | — | CONFIRMED |
| :289/:292 | 0.0（`tol_px`、`margin`） | OUT_OF_DOMAIN 的不参与哨 | 待确认 | 同一 0.0 在 :291 兼作「无余量」与「不适用」两义 ⇒ CR-58-11 | — | CONFIRMED |
| :300-:303 | `(W−1)/2.0`、`W−1` | 九点采样位 | 结构性不适用 | 注释与 :133-:138 声明一致 | — | CONFIRMED |
| :333/:334/:335 | 128、64、1024 | 密集域网格/边样/随机点数 | 待确认（采样密度） | GATES:67 把「≤128² 网格＋四边/四角/中心＋1024 随机点」写成判据定义 ✓ 有门表锚；但「9 点低估 1.41–5.57×」的实测比只在 run/ 件 | 取大更严 | PARTIAL |
| :347 | 9007199254740992.0 = 2⁵³ | xorshift64* 高位归一分母 | 结构性不适用 | FP53 尾数定义 | — | CONFIRMED |
| :412 | `0x9E3779B97F4A7C15` | 固定 seed | 结构性不适用（确定性选择） | 黄金比例常数；注释自述目的=逐位可复现 ✓ | — | CONFIRMED |
| :447/:559 | 1e-300 | \|det\| 除零哨 | 结构性不适用 | ALG §15.2:461 ✓ | — | CONFIRMED |
| :457 | `fov <= ap->max_fov_deg` | FOV 硬门（出货面**真强制**） | 公式导出 | SCI §9a-12:152「超出该域必须显式拒绝（p3_wcs.cpp:456-461）」✓ 指针与代码对得上 | — | CONFIRMED |
| :466 | `d->crpix_x != cx`（精确不等） | CRPIX 不变式强制 | 结构性不适用（精确相等是意图） | 与裁剪路径的互斥见 CR-58-12 | — | CONFIRMED |
| :526 | `M_PI/2.0` | TAN 域哨兵 | 待确认 | 见 CR-58-3／CR-58-9（口径）；ALG §9:288-300 已把它定为「ρ\*=57.5183634° 数值域上界，非半球界」 | — | CONFIRMED |
| :527/:528 | `atan2(1.0,r)`、`atan2(-xi,eta)` | TAN 反演 | 公式导出 | 一手 §5.1.3 `R_θ=(180/π)cot θ` ✓ 代码按式算；旋转核 :532/:534 我按球面三角独立复算为「以 (α₀,δ₀) 为参考点、ψ=arctan r、Π 由 (ξ,η) 定」的标准终点式（推导见 CR-58-14 行），与 :533 注释所引式**不同形** | — | CONFIRMED |
| :545/:552 | 85.0、`denom <= 0.0` | world2pix 极区拒 + 背面拒 | 待确认／公式导出 | 85 同 :20；denom=cos ψ ⇒ `<=0` 正是真前半球界 ✓（与 :526 的假界形成对照，ALG §9:292-294 同判） | — | CONFIRMED |
| :578-:589 | `"CTYPE1= '"`、`%.10f`、`%.12e` | 关键词文本 | 待确认 | 见 CR-58-6（列位一手未取到；与 ALG §8:275 示例不同数） | — | PARTIAL |

### findings

### CR-58-10 · descriptor 层的空/未设投影码被归一成 TAN 并据此判适用域、产 CTYPE —— 与本文件自述「未实现投影不产 CTYPE」互斥
- 轴：R（退化/静默回落的产出面）＋ C
- 位点（逐跳）：`p3_wcs.h:22` `    const char* projection = "TAN";` ⇒ `p3_wcs.cpp:432` `    const char* pj = (d->projection && *d->projection) ? d->projection : "TAN";` ⇒ `:433` `p3_wcs_applicability(pj)`；另一支 `:571` 同一表达式 ⇒ `:572` `if (std::string(pj) != "TAN") return {};` ⇒ `:573` `const std::string ctype1 = std::string("RA---") + pj;`
- 上位依据：`p3_projection_registry.h:22-23`「fail-closed：… **不得**静默回落 TAN」；`p3_wcs.h:35`「不得静默映射为 TAN」；`p3_wcs.cpp:569-570` 自述「未实现投影不产关键词（fail-closed，绝不输出与请求不符的 CTYPE）」；`docs/ci/01_CHECKS.md:105` CHK-P3-PROJ-DECL「静默回落 TAN 即判红」
- 现状：请求面（`p3_proj_declare(nullptr)`→TAN）有合同缺省背书 ⇒ **我不指控它**；但 descriptor 面把 `projection = ""` 或空指针也当作 TAN，于是 (a) 用 TAN 的 85°/20°/det<0/1e-8 判它合格，(b) `p3_wcs_fits_keywords` 输出 `CTYPE1= 'RA---TAN'`
- 差在哪：一个「投影码未确定」的描述符在出口文本面上被印成 TAN —— 这正是 :571 注释声称已排除的情形；判据方向＝**放宽**（本该返回空串的面给出可解析的 CTYPE），不是收紧
- 后果：任何绕过 `p3_wcs_make` 自行拼装 descriptor 的调用方（`p3_crop_apply_wcs` 即直接改写字段的同族先例，`p3_wcs.h:355-369`）能让产品/诊断面携带与实际映射不符的 CTYPE；当前生产入口只经 make（`module_adapters.cpp:13812` 在校验通过后硬写 `d->projection = "TAN"`、`:14064-14065` 必先 make）⇒ **写不出错值触发路径**
- 定级：S2（缺运行时触发路径）
- 怎么算修好：descriptor 层的投影码改为「必填且为空即拒」——`p3_wcs_check_applicability` 与 `p3_wcs_fits_keywords` 对 `!projection || !*projection` 返回 UNSUPPORTED/空串（与 :233/:270 的缺省区分开：只有 `p3_wcs_make`/请求门保留合同缺省语义）；成员缺省改 `nullptr` 并在 make 之后才允许非空；同批改 `p3_wcs.h:22` 与所有 `P3WcsDescriptor{}` 构造点（`module_adapters.cpp:13789/14064/14164/14636/15028/15234`）不依赖隐式缺省
- 置信：CONFIRMED（三处归一点逐字读到；生产入口按调用点读码判定为不可达）

### CR-58-11 · 非法入参的退化返回用 0.0 表达，而 0.0 在下游比较里同时是「误差为 0」与「不参与判定」两义
- 轴：R
- 位点：`p3_wcs.cpp:254` `    if (!(scale_arcsec_per_px > 0.0) || !(fov_deg > 0.0)) return 0.0;`；`:257` `    if (!(c_env > 0.0)) return 0.0;`；消费点 `:284` `    } else if (g.envelope_px <= ap->roundtrip_tol_global_px) {`；`p3_wcs.h:111` `double tol_px;  // ... OUT_OF_DOMAIN 时 = 0.0（不参与判定）`；`p3_wcs.h:114-115` `envelope_px`（诊断量）与 `margin`（OUT_OF_DOMAIN 时 = 0.0）
- 上位依据：`p3_wcs.h:121-124` 声明「入参非法 → 0.0」；GATES:67-68 对两门适用域的定义（OUT_OF_DOMAIN ⇒ 不判红）
- 现状：包络函数遇非法入参给 0.0 ⇒ 在 :284 的 `envelope <= 1e-6` 判定里为**真** ⇒ 走 GLOBAL（施加 1e-6 门），而不是走 OUT_OF_DOMAIN（不施加门）；同时 `margin` 用 0.0 兼表「不适用」，而 0 在余量语义里是「擦边」
- 差在哪：**方向钉死**——错分到 GLOBAL 是**更严**（多一份 1e-6 判定），故这不是 fail-open／误绿指控（我不这样主张）；成立的是：① 同一 0.0 承载「零误差」与「未测」两义，违反派单重点④要区分的口径；② 日志/证据面把「不适用」写成 margin=0 会被下游（人或脚本）读成「无余量」
- 后果：`p3_projection_registry.h:242-243` 与 `p3_wcs.cpp:480-489` 的 why 文本会把「超出适用域」与「包络为 0」混在同一数值上；证据文件（`artifacts/evidence/**`、ctest JSON 输出）里的 `margin=0` 语义不唯一
- 定级：S3
- 怎么算修好：非法入参返回 NaN 或 `-1.0`（与 `p3_wcs.cpp:239/:246/:249` 已用的 −1.0 同族），并在 :284 显式判 `envelope > 0 && envelope <= tol`；`margin` 的不适用态改用 `status` 单义表达（不再塞 0.0）
- 置信：CONFIRMED

### CR-58-12 · 导出裁剪帧必然违反本文件强制的 `CRPIX=(W+1)/2` 适用域不变式，而裁剪后的 descriptor 不再过任何适用域门
- 轴：D ＋ C
- 位点：`p3_wcs.cpp:463-471` `    if (ap->crpix_fits_1based_pixel_center) { const double cx = (d->width_px + 1) / 2.0; … if (d->crpix_x != cx || d->crpix_y != cy) { … return P3_WCS_PARAM; } }`；对照 `p3_wcs.h:355-369` `p3_crop_apply_wcs`：`:364` `    out->crpix_x = frame->crpix_x - static_cast<double>(win.x0);`＋`:366` `    out->width_px = win.width();`；生产链 `module_adapters.cpp:14645`（p3n_crop_from_plan）→ `:14648`（p3_crop_apply_wcs）→ `:14651-14652` 直接用 `owcs.width_px/height_px`，**无** `p3_wcs_check_applicability` 复检（同形第二处 `:15035-15042`）
- 上位依据：`p3_wcs.h:64-66`／`p3_projection_registry.h:8-9` 把「CRPIX 用 FITS 1-based 像素中心」列为 DESIGN 要求的适用域声明项，并写「违反 ⇒ 拒绝」；`PHASE3_PROJ_IMPL.md:539-542`（§15.5 六要素表 CRPIX 列）同口径；裁剪侧权威 `p3_wcs.h:179-181`（不变式④：CRPIX 减**整数**窗口原点 ⇒ 裁剪框内像素与不裁剪逐位相同）与 `eng/contracts/schemas/phase_config_export.schema.json:461`
- 现状：非对称裁剪即破坏该式，例：frame W=1024（CRPIX=512.5）、窗口 x0=100、x1=900 ⇒ 产品 W=800、CRPIX=412.5，而 (800+1)/2 = 400.5 ≠ 412.5；只有左右余量相等的对称裁剪才碰巧满足
- 差在哪：两面权威对同一量给出互斥要求，且负责执行不变式的门只跑在 make（未裁剪 frame）上，出货 descriptor 面（裁剪后）从不过门
- 后果：出口 FITS 的 CRPIX 可以违反「声明的适用域不变式」而无任何机器报警；DESIGN 面「违反 ⇒ 拒绝」这一对外主张对裁剪产品不成立。**不产错值**：裁剪 WCS 的 CRPIX 平移本身是正确的（Paper I 允许 CRPIX 非中心、可非整数，见 `docs/science/PHASE3_HIPS_TO_FITS.md:149`），所以危害面是「声明与判据失效」，不是坐标错
- 定级：S2（有生产调用点，但结论是门不覆盖，不是产物错值）
- 怎么算修好：把 `crpix_fits_1based_pixel_center` 的适用对象写清＝「仅 `p3_wcs_make` 产出的整幅 frame」，在 `P3WcsApplicability` 注释与 §15.5 同批标注；并给裁剪后的 descriptor 加一道**裁剪专用**不变式门（CRVAL/CD 逐位等于 frame、CRPIX 差为整数、W/H 与窗口一致），否则该产品的 WCS 完全不过门。防误红：新门必须放行对称与非对称两类裁剪（用 `win.x0` 作已知量比对，不能复用 :466 的中心式）
- 置信：CONFIRMED（数值例为手算：512.5−100=412.5 vs (800+1)/2=400.5）

### CR-58-13 · 包络式 `sec²Δ = 1 + (FOV_rad/2)²` 与注释自述上限「≤1.03046 @FOV=20°」：我逐值复算**一致**，本条登记为核过项而非指控
- 轴：S（数值凭什么）＋ C
- 位点：`p3_wcs.cpp:261` `    const double sec2 = 1.0 + half_fov_rad * half_fov_rad;         // ≤ 1.03046 @FOV=20°`（`half_fov_rad = 0.5 * fov_rad`，:260）；同族声明见 `:180`「sec²Δ = 1 + r_plane² ≤ 1 + (FOV_rad/2)² = 1.03046（FOV ≤ 20°）」、`p3_wcs.h:121-122`、`GATES_AND_TOLERANCES.md:67`「sec²Δ ≤ 1.03046 @FOV≤20°」
- 上位依据：`docs/science/PHASE3_HIPS_TO_FITS.md:152` 给出同一约定的正确口径：平面 FOV=20° ⇒ `ξ_half = FOV_rad/2 = 0.174533` ⇒ `r_plane = tan γ_max = 0.174533` ⇒ `sec²γ_max = 1 + r² = 1.030462` —— 即 **1.03046 是「半平面对角半径（度换算成 rad 的弦量）的 tan²」而不是「FOV_rad/2 的平方」**：FOV_rad 在这里指**对角全视场**（0.349066 rad），其一半的平方是 0.030461 ✓ 与 1.030462 吻合 —— 但代码里 `fov` 由 `p3_wcs_fov_deg` 给的是**度数**（:242 `scale_deg_per_px * sqrt(w²+h²)`），:259 先 `fov_rad = fov_deg*kRad`，:261 再取半 → 数值上 `1+(0.349066/2)² = 1.03046` ✓ **等号成立**
- 现状／差在哪：**我复核后判定该行数值正确、无 finding**——注释与实现一致（我把 FOV 当成全对角弧度一度算错，已按 :242/:259-:261 逐步复算确认：20°→0.3490659 rad→半 0.1745329→平方 0.0304617→1.0304617）。登记本条为「已复算一致」而非指控，防后续读者重复怀疑。唯一残留：该式的适用域（`r_plane ≤ tan γ_max = FOV_rad/2` 的近似等号）只在 γ 小时成立，FOV=20° 处 γ_max=9.9023° ⇒ tan=0.174533 vs FOV_rad/2=0.1745329 —— 差 1.6e-9，可忽略。
- 定级：不成立（撤回）
- 置信：CONFIRMED（我复算：20° 时 `sec2 = 1.0304617`，与注释 1.03046 同数）

### 本文件「核过、不报」的项（附检索式）
- **TAN 内核与一手式的一致性**：一手取到 §5.1.3 `x=R_θ sinφ, y=R_θ cosφ, R_θ=(180/π)cot θ`。我按球面三角独立复算 `:532/:534`：以 ψ=离参考点角距（代码 `sinψ=cos θ_code=r/√(1+r²)`）、Π 由 `(ξ,η)=(r sinΠ, r cosΠ)` 定义，得 `sinδ = sinδ₀cosψ + cosδ₀ sinψ cosΠ`（= :532 ✓）与 `Δα = atan2(sinΠ sinψ, cosδ₀cosψ − sinδ₀ sinψ cosΠ)`（= :534 ✓，代码里 `−ct·sp` 与 `φ=atan2(−ξ,η)` 的负号成对抵消）。⇒ TAN 正反映射与标准 gnomonic 同式，**不报**。唯一可报的是 :533 的**注释**把该式引成「`Δα = arg(−cosθ·sinφ, sinθ·cosθ0 + cosθ·sinθ0·cosφ)`」——与 :534 的实现不同形（第二项的角名与符号都不是本式），属注释与代码不同源；并入 CR-58-14（S3）。
- **`p3_wcs_world2pix` 的半球判据**：`denom = sinδ sinδ₀ + cosδ cosδ₀ cos Δα = cos ψ`，`<=0` 恰为真背面 ⇒ 正确（与 :526 的假界形成 ALG §9:292-294 已登记的不对称）。
- **面积元/赤纬余弦（重点②）**：本文件无面积量；`√|det(CD)|` 给的是平面角尺度，按定义不含 cos δ ✓；检索式 `git grep -n "cos\|area\|sr\b\|omega" -- lib/algorithms/projection/p3_wcs.cpp` → 命中全部是 `std::cos(d0)` 一类旋转核项与 `// rad ↔ arcsec` 注释。
- **退化返回哨兵（重点④）**：`p3_wcs.cpp:239/:246/:249` 对非法输入用 **−1.0**（尺度/FOV 的非物理值）而非 0/1.0 ⇒ 可区分，合格；唯一两义点是 :254/:257/:289/:292 的 0.0（CR-58-11）。
- **`P3_WCS_UNSUPPORTED` 枚举「无产生点」**（ALG §9:283 说它「无产生点」）：现码 `p3_wcs.cpp:69/:99/:100/:438` 均有产生点 ⇒ 该 ALG 行已过期，但 ALG 不在本批清单，只登记不判（检索式 `git grep -n "P3_WCS_UNSUPPORTED" -- lib/algorithms/projection/p3_wcs.cpp`）。

### CR-58-14 · `p3_wcs.cpp:533` 把实现式引作「Calabretta & Greisen」原式，但引文与下一行代码不同形
- 轴：S（式子凭什么）＋ C
- 位点：`p3_wcs.cpp:533-534`
  ```
    // Calabretta & Greisen: Δα = arg(−cosθ·sinφ, sinθ·cosθ0 + cosθ·sinθ0·cosφ)
    const double dra = std::atan2(-ct * sp, cd * st - sd * ct * cp);
  ```
- 上位依据：一手 §5.2/§2.2 未取到（§0 途径清单）；仓内二次口径为 `PHASE3_PROJ_IMPL.md:466-469` 的 zenithal 核四式（写作 `Δα=atan2(−cosθ sinφ, cosδ₀ sinθ − sinδ₀ cosθ cosφ)`，与代码一致）
- 现状：引文第二参数「`sinθ·cosθ0 + cosθ·sinθ0·cosφ`」把 δ₀ 与其余角互换且 cosφ 项变号 ⇒ 与实现不同形；实现本身我按球面三角复算为正确（见上节）
- 差在哪：注释把「我自己推的等价式」标成文献原式，且标的还不是该文献的式形；权威式（§2.2）在本仓无逐字摘录可查
- 后果：以后按注释去核对 Paper II §2.2 的人会以为代码与文献矛盾而「修正」正确的实现（本仓已有同型教训：`PHASE3_PROJ_IMPL.md:552-558` 记录 30° 错映射下往返仍 4.5e-15 px）
- 定级：S3
- 怎么算修好：注释改为「等价形式，推导见 ALG §15.2 zenithal 核四式（本行与之逐字同形）」，或把 §2.2 原式逐字摘录进 `docs/algorithms/PHASE3_PROJ_IMPL.md` 后按式号引用；不得只改注释不动 §15.2 的同族表述
- 置信：PARTIAL（缺 §2.2 一手原文，无法断定文献式究竟长什么样；能断定的是注释与本行代码不同形）

---

## 5 `lib/algorithms/projection/p3_wcs.h`

### §2.5 数值处置表（19 项）

| 位点 | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :15-:21 | 成员缺省 0（crval/crpix/cd/width/height） | 0 | 零初始化 | 结构性不适用 | :270/:447 除零哨兜底；make 失败保持零初始化（`p3_wcs.cpp:101`）✓ | — | CONFIRMED |
| :22 | `projection = "TAN"` | 合法枚举缺省 | descriptor 层静默回落面 | 待确认 | **CR-58-10** | 去掉缺省 | CONFIRMED |
| :27-:29 | 注释 85°/W,H 越界/projection≠TAN/跨半球 | 85、20 隐含 | 状态码语义 | 待确认 | 与 `p3_wcs.cpp` 实现一致；但 :6/:29 的「半球」措辞 = CR-58-9（ALG §9:300 已禁止该措辞） | — | CONFIRMED |
| :50 | `const char* projection = "TAN"` | 形参缺省 | 请求面缺省投影 | 文献值（合同背书） | DESIGN §6.3:455、schema:206、`p3_projection_registry.h:166` 三面一致 ✓ | — | CONFIRMED |
| :64-:66 | 「ASTROCS_DESIGN.md §5.3『每种投影必须声明适用域…违反 ⇒ 拒绝』」 | — | 权威锚 | 待确认 | **锚错挂：CR-58-9** | 改指 §6.3:453 | CONFIRMED |
| :81-:82 | 注释 85.0 / 20.0 | 声明值 | 待确认 | 与 `p3_wcs.cpp:214-215` 同值 ✓ | — | CONFIRMED |
| :85-:89 | 注释 1e-8 / 1e-6 / 0.9 / 0.9586 / 128 / 78 | 门与包络 | 待确认 | 同 `p3_wcs.cpp:218-221` 行（推导件在 run/ ⇒ 档 3 不合格）；0.9586「仓内最小真实尺度」无跟踪面出处（检索式 `git grep -n "0.9586"` → 只命中本文件 :76/:88、`p3_wcs.cpp:185/207/220` 与测试 :62/:303 ⇒ 全为同族复述，无一次来源件） | 保守：把 0.9 视为未标定 | CONFIRMED(无来源件) |
| :68-:69 | 「依据 run/GATE-DERIVE-01/REPORT.md 的推导与 run/GATE-WCS-01 的落地裁决」 | — | 证据指针 | 待确认 | 派单 §2.5 档 3：「指 `run/` 即判不合格」⇒ 头文件把两门依据挂在非跟踪面 | 需把推导落 `docs/algorithms/` | CONFIRMED |
| :112 | `3600·√|det(CD)|` | 3600 | 像素角尺度 | 结构性不适用＋公式导出 | 单位定义；实现 `p3_wcs.cpp:248` 同式 ✓ | — | CONFIRMED |
| :118 | 「退化 → -1.0」 | −1.0 | 非法输入哨兵 | 结构性不适用（可区分哨兵） | 实现 `p3_wcs.cpp:246/:249` ✓ | — | CONFIRMED |
| :121-:124 | `C_env·u·sec²Δ/s_rad`，u=2⁻⁵³；「入参非法 → 0.0」 | 2⁻⁵³、0.0 | 包络式与哨兵 | 公式导出／待确认 | GATES:67 同式；0.0 两义 ⇒ CR-58-11 | — | PARTIAL |
| :129-:131 | FOV = `scale × √(W²+H²)` | — | 平面 FOV 约定 | 公式导出 | SCI §9a-12:152 明文同约定（「本合同的平面 FOV 约定」）✓ 实现 `p3_wcs.cpp:242` 一致 | — | CONFIRMED |
| :134-:135 | 「9 点采样系统性低估最坏值 **1.41–5.57×**」 | 1.41、5.57 | 采样低估比 | 待确认 | 比值只出现在 `GATES:67`（「9 点采样是密集域子集」）与本文件/`p3_projection_registry.h:223` 的同族复述；实测件在 run/ | 保守：以密集域为唯一门证据（已如此） | PARTIAL |
| :143-:144 | 「网格 ≤128×128＋四边各 64 点＋1024 固定 seed 随机点」 | 128、64、1024 | 密集扫描参数 | 待确认 | GATES:67 把同三个数写进判据定义 ✓ 门表为锚；比值证据同 :134 | — | PARTIAL |
| :187-:191 | `P3CropWindow` 成员缺省 0、`long` | 0 | 零初始化 | 结构性不适用 | :239/:327 空窗校验兜住 ✓ | — | CONFIRMED |
| :196-:199 | `P3CropStatus` 0/1/2 | — | 枚举 | 结构性不适用 | — | — | CONFIRMED |
| :203-:205 | `kP3CropEdgeSamples = 257` ＋「2° 画幅实测 < 1e-3 px」 | 257、2°、1e-3 | 边界加密采样密度 | **待确认** | 依据只有本行注释的「实测」，无跟踪面标定件（检索式 `git grep -n "kP3CropEdgeSamples\|257" -- docs/ eng/` → 仅合同 schema `:461` 复述「边界加密采样」无数值）。⇒ 档 3 不成立（无标定件），档 2 亦不成立（无导出式） | 保守方向：加大 N 或对包围盒再外扩 1 px；影响：`crop.sky` 的「不切任何中心在矩形内的像素」保证的成立域 | CONFIRMED（数值无锚）／我实算 4 组几何未发现反例（见 :205 下一段） |
| :220-:241 | 1、`frame_w`、`x1−x0` | 1 | FITS 1-based 域与半开转换 | 结构性不适用 | 合同 schema `:461`＋DESIGN Paper I 1-based ✓；半开约定 `:237` 注释自明 | — | CONFIRMED |
| :266-:275 | [0,360]、[−90,90]、85.0、360.0 | 同左 | sky 矩形值域与 TAN 收窄 | 待确认（85.0）／结构性（360、90） | `phase_config_export.schema.json:461/479`「\|dec\| ≤ 85°…继承 p3_wcs_make 的同一条件」✓ 与本行注释同源同值 | — | CONFIRMED |
| :305-:316 | `n`、`n − 1` | 257、256 | 采样步长归一 | 结构性不适用 | 含两端点的等分定义 | — | CONFIRMED |
| :323-:326 | `floor(min)`、`floor(max)+1` | +1 | 外扩整数包围盒 | 公式导出 | 「中心落在矩形内」的充要条件 `floor(xmin) ≤ c ≤ floor(xmax)` ✓ 我按定义复核为真；注释 :247 同述 | — | CONFIRMED |
| :332/:338 | `cx1 <= 0`、`cx0 >= frame_w` | 0、frame_w | 空交/越界分离判定 | 结构性不适用 | schema:461「越界或为空 ⇒ 具名拒绝，不静默夹取」✓ 两支分开命名 ✓ | — | CONFIRMED |
| :364-:367 | `crpix − win.x0`、`width = win.width()` | — | 裁剪帧 WCS 限制 | 公式导出（平移不变式） | 但与本仓 CRPIX 不变式互斥 ⇒ **CR-58-12** | — | CONFIRMED |

**257 点采样我做了独立复算（自写脚本，未 import 仓库 Python，未跑构建）**：
`独立审计/复算件/CR-58/crop_bbox_check.py` —— 按 `p3_wcs.cpp:542-565` 逐式实现 world2pix，对
1024² @0.013 deg/px（FOV 18.8°，合同内）与 CRVAL2=84° 的高纬帧，把 257 点边界包围盒与
4001² 内部密集扫描的极值对比：**赤道全幅／半幅、高纬全幅、高纬窄跨、极窄跨四组 shortfall 全为 0.000 px**，
win_sample 与 win_true 完全相同 ⇒ **报不出「切像素」的反例，不成立 finding**。
残留（如实登记，不定级）：① 该保证是**采样性质**，无导出式覆盖 FOV 全域（注释只举 2°），
我测的是「极值落在角点/边端点」的几何；② RA 绕回矩形（355°→5°）在帧外时全部采样失败，
实现走 `:317 hemisphere` 具名拒绝 ✓ fail-closed（我的脚本当时未写该分支而抛 min(())，属脚本缺陷，不影响代码结论）。

### findings

### CR-58-15 · `kP3CropEdgeSamples = 257` 的唯一依据是同注释里的「2° 画幅实测 < 1e-3 px」，既无导出式也无跟踪面标定件，且举例域（2°）明显小于合同域（FOV ≤ 20°）
- 轴：S
- 位点：`p3_wcs.h:205` `constexpr int kP3CropEdgeSamples = 257;`，其依据全段见 `:202-204`「取值理由：TAN 在输出画幅内的映射是光滑近仿射；每边 257 点把「边界在像素域的弓高」压到远小于 1 px（2° 画幅实测 < 1e-3 px），保证外扩包围盒不切像素。」
- 上位依据：`ASTROCS_DESIGN.md` 无此数值（`grep -n "257" ASTROCS_DESIGN.md` 零命中）；`PHASE3_HIPS_TO_FITS.md`/`PHASE3_PROJ_IMPL.md` 亦无（`git grep -n "257\|EdgeSamples" -- docs/` 仅命中合同 `phase_config_export.schema.json:461` 的无数值复述「边界加密采样」）；FOV 适用域上界 = 20°（SCI §9a-12:151-152）
- 现状：257 是字面量，注释给的「实测」没有任何可指认的件（既不是 `实验/` 也不是 `artifacts/`），且举的例是 2° 画幅；实现按 `n−1` 等分（:307）⇒ 采样间隔随请求矩形线性放大
- 差在哪：派单 §2.5 档 3 要求「标定件路径＋结果位（必须落在跟踪面）」；此处两者皆无 ⇒ 只能落 `待确认`，禁止「看起来合理」
- 后果：`crop.sky` 的对外保证「外扩包围盒不切掉任何中心落在矩形内的像素」（`phase_config_export.schema.json:461`、`p3_wcs.h:247`）建立在一个未标定的密度常数上；我实算 4 组（含 18.8° 全幅与 CRVAL2=84°）未见反例 ⇒ 不指控现值有误，指控**依据缺失**
- 定级：S3（无已知错值；S 轴依据缺陷）
- 怎么算修好：给出弓高的解析上界（对 TAN 的边界曲线用 `d²x/dt²` 闭式界 ⇒ N 与 FOV、矩形跨度的关系式），或补一次跟踪面标定件（`实验/` 或 `artifacts/` 下的扫描表＋脚本）并把路径写进本行注释；同时把注释的举例从 2° 换成合同上界 20°
- 置信：CONFIRMED（依据缺失这一事实；数值本身未定对错）

### 本文件「核过、不报」的项
- `p3_crop_window_from_sky` 的 RA 绕回（:278-279 给 `ra_min + t·span`，可 > 360°）：TAN 公式只经 `cos(a−a₀)/sin(a−a₀)` ⇒ 周期性成立 ⇒ 不报（检索式：读 `p3_wcs.cpp:548-555` 全部 RA 用法）。
- `sample()` 把 PARAM 与 HEMISPHERE 合并成同一 `hemisphere` 标志（:290-293）：拒绝消息 :318-320 明写「outside the TAN hemisphere **(or in the pole exclusion zone)**」⇒ 未误导，不报。
- `xmin/xmax` 初值 0.0 配 `first` 标志（:280-281/:294-299）＋ 全失败时 :321-322 具名拒绝 ⇒ 无「未测当 0 计入」。
- `P3CropWindow::width()` 的 long→int 窄化（:192-193）：两支路（:239、:332-338）均已把窗口夹在 `[0, frame_w]`，`frame_w ≤ kMaxSide = 20000` ⇒ 无溢出可能，属结构性。
- `p3_crop_apply_wcs` 在 `!win.active` 时逐位复制（:358-361）＝设计①「默认不裁剪」✓。

---

## 6 `lib/algorithms/projection/tests/p3wcs/p3_projection_registry_test.cpp`

### §2.5 数值处置表（16 项）

| 位点 | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :60/:61/:62 | `kRoundtripTolPx`/`kRoundtripTolGlobalPx`/`kMinScaleArcsec` | 1e-8、1e-6、0.9 | 测试侧「冻结值回归锁」 | 待确认 | 与 `p3_wcs.cpp:218-220` 逐值同 ✓（注释 :55-59 自述这是第二份拷贝，用于回归锁）⇒ 与门表行的**共同来源**（推导件在 run/，见末节） | 值漂移时门红，属期望行为 | CONFIRMED |
| :62 | 注释 0.9586 | 0.9586″/px | 仓内最小真实尺度 | 待确认 | 与 `p3_wcs.h:88` 同族复述，无一次来源件（同 CR-58-15 型问题） | — | CONFIRMED(无来源) |
| :130 | `d.size() == 1 && d[0] == "TAN"` | 1、TAN | 声明集当前值 | 结构性不适用（对表内容的断言） | DESIGN §6.3:453「当前仅 TAN 可用」✓ | — | CONFIRMED |
| :140/:176 | `n == 8`、`n_neg == 7` | 8、7 | 冻结集/未实现数 | 结构性不适用 | 枚举基数 | — | CONFIRMED |
| :164/:166 | 哨兵 123.0；`p3_wcs_make(150.0, 2.0, 0.0001389, 64, 64, "east_left", 0.0, &d, code)` | 见左 | 负例几何＋失败不改写哨兵 | 结构性不适用（夹具选择） | 哨兵断言 :169 与 `p3_wcs.cpp:92-100`（先投影门、后 `*out` 零化）方向一致 ✓ | — | CONFIRMED |
| :178 | `{"ZZZ", "tan", ""}` | — | 未冻结/大小写/空串负例 | 待确认 | 把 `"tan"` 判为 UNSUPPORTED 是**当前实现的精确匹配行为**；与合同小写面冲突 ⇒ **CR-58-8 的测试侧体现**（若按 ⑧ 改归一，本行必须同批改） | — | CONFIRMED |
| :205 | `scales[4] = {0.0001389, 0.001, 0.005, 0.02}` | deg/px | 正例尺度覆盖（0.5/3.6/18/72 ″/px） | 待确认（选择） | 覆盖 0.9 两侧 ✓ 边界非退化；具体值无条文（夹具自订，派单 §4 不报） | — | CONFIRMED |
| :231/:232 | `(97 + 1) / 2.0`、`(89 + 1) / 2.0` | 97、89 | CRPIX 期望 | 结构性不适用（夹具尺寸） | Paper I §2.1.1 | — | CONFIRMED |
| :233 | `p3_wcs_fov_deg(s, 97, 89) <= 20.0` | 20.0 | FOV 断言 | 待确认 | 复写第二份 20（:294 第三份）⇒ 单一事实源仅部分成立 | — | CONFIRMED |
| :243/:245/:246 | `32.0`、`33.0`、1e-8 | — | 双桥接注入必败负例 | 结构性不适用 | 与 GATES:67「移除/错置 +1 桥接必须 ≥1 px 偏差 ⇒ 注入必败」同判据 ✓ 方向已定（1 px ≫ 1e-8 ⇒ 必红） | — | CONFIRMED |
| :253 | `150.0, 85.1, …` | 85.1 | \|CRVAL2\|>85 负例 | 结构性不适用（差 0.1° 的取值） | ALG §15.6:564 同口径 | — | CONFIRMED |
| :258/:260/:264 | 0.05/512/512（=36.2°）、0.02/512/512（=14.5°） | — | FOV 门两侧 | 结构性不适用 | 我复算 0.05·√(512²+512²)=36.20 ✓、0.02·724.08=14.48 ✓ ⇒ 注释自述与数值一致 | — | CONFIRMED |
| :294-:305 | 85.0、20.0、1e-8、1e-6、0.9、128.0 | 声明表逐项 | 待确认 | 逐项 == `p3_wcs.cpp:214-221` ✓ ⇒ 这是**声明表数值唯一被逐值比对的地方**（见 CR-58-4 的相反面：v1 registry 无此比对） | — | CONFIRMED |
| :313 | `nv == 4` | 4 | v6 内核 registry 行数 | 结构性不适用 | ALG §15.1:432「v3 已实现 4/8」✓ | — | CONFIRMED |
| :347/:353/:357/:364 | 85.0/5e-5/129/30.0；0.18；4.0；`err*4.0 < tol`、`n_dense > 1000` | 见左 | 最坏工况几何与余量断言 | 待确认 | 工况与 ALG §15.6/GATES:67 的「0.18″/px @\|CRVAL2\|=85°、PA=30°、129²」逐值同 ✓；余量 ≥4× 的期望值来自 run/ 扫描表（不入库） | 若推导件不可得，则「≥4×」这一非退化断言也只能复算不能溯源 | PARTIAL |
| :411/:419 | `0.001 / 3600.0`；`envelope_px > tol_global` | — | OUT_OF_DOMAIN 负例 | 结构性不适用 | 方向已核：该分支**返回 OK**（`p3_wcs.cpp:476-490`）⇒ 测试如实断言「报超出适用域而非判红」，并用 :433-446 的三条同尺度违规证明它不是恒绿 ✓ 判据非退化 | — | CONFIRMED |
| :473/:474 | `0.5*(nine+dense)`、`nine < tol_between && !(dense < tol_between)` | — | 9 点 vs 密集域区分性 | 结构性不适用 | 方向已定：dense==nine 时该式变假 ⇒ 红 ⇒ 强制 dense>nine 的真覆盖，非恒真 ✓ | — | CONFIRMED |
| :480 | `g.tol_px * 1e-6` | — | 「门缩 1e6 倍必红」 | 结构性不适用 | 能红侧证明 ✓ | — | CONFIRMED |
| :500-:502 | `1.1102230246251565e-16`、`206264.80624709636`、`scales[7]` | 见左 | 包络独立复算常数与尺度表 | 待确认 | ① u、rad→arcsec 为定义常数；②「独立复算」与被测**同式同常数**（`p3_wcs.cpp:261-263`）⇒ 它只验证常数未被改动，不验证式子本身；本文如实标注为**共源期望** | — | CONFIRMED |
| :532 | `<= 1e-15 * env_ref` | 1e-15 | 复算相对容差 | 结构性不适用 | 同式同序 ⇒ 期望 ~0 残差，取 1e-15 相对容差是 FMA/结合序差异的量级；无条文但无科学语义 | — | CONFIRMED |

### findings

### CR-58-16 · 该注册表测试的「跨注册表一致」判据（C5）与被测判据独立性（C2(d)）都读声明表，不读出货产品路径；唯一读到产品路径的探针第一步又被声明表预筛 ⇒ 三层集合等式的「隐藏能力」一侧无判据
- 轴：R（判据方向）＋ D
- 位点（逐跳）：`tests/p3wcs/p3_projection_registry_test.cpp:309-331`（C5 全体：读 `p3_proj_frozen_table` 与 `p3_proj_is_declared`，两面皆表）；`:324-325` `            CHECK_MSG(ft[k].status == P3ProjProductStatus::kKernelOnly, "v6 已实现但未接线的码标为 kKernelOnly（不得声称支持）");`（**ft 来自声明表**）；`:328-330` `CHECK_MSG(p3_proj_declare(code, &why) != P3WcsStatus::P3_WCS_OK, …)`（读的仍是同表）；`:144` `        if (astrocs::phase3::p3_proj_is_declared(code)) continue;` ⇒ `:172-174`；`:591` 「可运行矩阵（证据模式，**不参与断言**）」
- 上位依据：`p3_projection_registry.h:19-20`（判据范围含「实现未声明 = 隐藏能力」）＋ `:204`（探针独立性自述）＋ 测试头 `:16`「C5 跨注册表一致: v6 内核 registry 行不得被冒充为产品声明」＋ ENGINEERING_SPEC §8「每项检查能红能绿」
- 现状：C5 的三段断言（v6 行 ∈ 冻结集 / 未声明者状态必须为 kKernelOnly / declare 必须不放行）全部由**同一张表的三个视图**互证；真正能看见内核可跑性的是 `matrix_mode()`，而它显式不参与断言。⇒ 若 v6 内核被接线（`registry_find("SIN")` 能 make 且往返达标）而 `kTable`/`kImplemented` 未改，C1/C2/C5 全绿
- 差在哪：**方向钉死**＝恒绿侧（漏报，不是错值）。C1 的 R 项按构造 ⊆ D 项（CR-58-7 的同一链），C5 项是表自证
- 后果：对外主张「以可运行验证为准（文档说 1、代码说 4 ⇒ 以可运行验证为准）」（`:8-9`）与「隐藏能力禁止」（`p3_projection_registry.h:20`）在本测试面上没有对应的判据点；本批重点⑤问的「判据读的是注册表本身还是出货路径」答案是：**部分读出货路径（C3/C4/C7/C8/C9 真调 `p3_wcs_*`），关键的一条（C1 的 R、C2(d)、C5）读表**
- 定级：S2
- 怎么算修好：把 `matrix_mode()` 的 v6 内核可跑性从 printf 升级为断言，并把它与 `kKernelOnly` 的**互斥**写进判据（内核可跑 ⇒ 必须是 kKernelOnly 或已声明，且 kImplemented 必须同步）；C5 的第三条改为「经产品路径实跑该码必失败于**内核**而非声明门」（即在探针里绕开 `p3_wcs_validate_request`，见 CR-58-7 修法）；同批修 `p3_projection_registry.h:19-20` 与 `:204` 的范围表述，防「改对反而判红」
- 置信：CONFIRMED（逐跳读码，未实跑）

### 本文件「核过、不报」的项
- **是否注册进基线（派单⑤后半）＝是**：`tests/p3wcs/CMakeLists.txt:17-18`（`add_executable(p3_projection_registry_test p3_projection_registry_test.cpp …)`）、`:24`（`add_test(NAME p3_projection_registry …)`）、`:26-27`（`p3_projection_registry_selftest … --self-test`）、`:32-35`（`p3_projection_unsupported_cli` 端到端负例用 `$<TARGET_FILE:acsd>`）；CI 面 `eng/ci/checks.json:10069-10080/10166-10200`、`docs/ci/01_CHECKS.md:105-106`（两条 P0）⇒ 不报「未注册」。
- `--self-test`（:557-589）确为可执行负例面：对 `criterion_holds` 喂 6 组变异输入并断言必红/必绿 ⇒ 比较函数本身非恒真 ✓（但这只证明**比较函数**能红，不证明**取数链**能红，见 CR-58-16）。
- :468 `dense >= nine` 的超集性质断言 ✓ 由 `p3_wcs.cpp:376-409` 显式补齐角点/中心实现，非空断言。
- 测试直接编 `p3_proj_v6.cpp`（CMakeLists:21）做跨表比对 ⇒ 不是「只测夹具」：C3/C4/C7/C8/C9 全部调生产函数（`p3_wcs_make/pix2world/world2pix/fits_keywords/check_applicability/roundtrip_*`）✓。

## 本批最高危三条（只挑，不复述）

1. **CR-58-8**（C/R）：`projection` 键在两份合同＋插件文档＋配置登记面给小写、在实现与另两面给大写，登记面还标注 `finding: "none"` ⇒ 文档一致的配置可被运行时整拒。
2. **CR-58-16 + CR-58-7**（R/D）：产品声明注册表的三层集合判据里，「隐藏能力」一侧由表自证、探针独立性自述与实现相反、相应断言恒真 ⇒ 本批重点⑤问的判据力在此处失配。
3. **CR-58-12**（D/C）：导出裁剪后的 descriptor 必然违反 `CRPIX=(W+1)/2` 这一被声明为「违反 ⇒ 拒绝」的适用域不变式，且该产品面 descriptor 不再过任何适用域门。
（次高：CR-58-9 —— 六个文件的权威锚集体错挂到 `ASTROCS_DESIGN.md §5.3`，而 §5.3 是 SNR 条款、投影在 §6.3，且被引为「原文」的句子含 DESIGN 零提及的五个数值。它不产错值，但使上面三条的「条文依据」全部需要重新钉。）

## 我读不动或需要实跑才能定的

- **CAR/AIT（及 STG/MOL/CEA/ZEA）投影式的一手定义**：Paper II §5.2/§5.3/§2.2 未取到（途径清单见 §0 第 3 条）。要定案需要：可提取文本的 Paper II（或官方勘误件）＋对 `p3_proj_v6.cpp`（不在本批清单）与 astropy 的逐点对拍。⇒ 本批凡涉及「AIT 该不该有 √2、CAR 的 Y=+θ 是否为 Table 1 原式、A≤1 是否为 Paper II 域界」的表述一律 **UNPROVEN**，我引用的是本仓 ALG §15.3/§15.8 的二手口径。
- **`p3_projection.cpp` 是否真的「不进任何产品字节」**：CMake 与调用点读码支持该结论（§0 事实 1），但 `artifacts/`/`reports/` 里的历史证据（`FIX_LEDGER.csv:12` 指旧址 `lib/phase3_proj/`）不在跟踪语义面；要定案跑 `nm`/链接映射（本批禁跑构建）。
- **`dormant_algorithms.json:81-140` 与 `CMakeLists.txt:961/:1010` 的矛盾**：需实跑 `nm build/acsd | grep p3_wcs_` 或看链接映射；本批按调用点存在判「出货路径」。
- **CLI 端到端**：`"projection": "tan"` 与 `"TAN"` 各跑一次 `acsd export`（定 CR-58-8 是 S1 还是 S2）。
- **注入实跑**：把 `p3_wcs.cpp:218` 的 1e-8 改 1e-6、或给 `kImplemented` 加 "SIN"，看 `p3_projection_registry` / `v6_p3_proj_legacy_deviation` 是否红（定 CR-58-16/CR-58-2 的判据力，本批禁跑 ctest）。
- **包络常数 C_env=128、min_scale=0.9、0.9586″/px、1.41–5.57×、257 的标定件**：全部指向 `run/`（AGENTS §7 不入库）；需要 GATE-DERIVE-01 的推导件入库或重导出式才能定档。

## 派单与清单差异（以清单为准）

1. **第 6 项路径不存在**：派单写 `lib/algorithms/projection/p3_projection_registry_test.cpp`；
   仓库跟踪面唯一命中是 `lib/algorithms/projection/tests/p3wcs/p3_projection_registry_test.cpp`
   （`git ls-files | grep projection` 核）。本批按后者读。
2. **重点①/③ 的主要实施体不在清单里**：`PHASE3_PROJ_IMPL.md:425` 说「在役 registry = v6 线
   `p3_proj_v6.h/.cpp`（kProjectionRegistryVersion=3）」，而清单不含 `p3_proj_v6.{h,cpp}`；
   清单里的 `p3_projection.{h,cpp}` 是 RETIRED 的 v1（四处声明一致：`p3_projection.h:4`、
   `p3_projection.cpp:2-4`、ALG §15.1:427-431、`docs/owner/SCIENCE_OVERVIEW.md:103`）。
   ⇒ 落在 v1 的 5 条 finding 全部只能定 S2/S3（写不出生产触发路径）。
3. **重点②（面积元／赤纬余弦）在六个文件里无对象**：全清单无立体角/面积实现
   （逐文件检索式见各节「核过、不报」）；`√|det(CD)|·3600` 是平面角尺度，按定义不含 cos δ ✓。
   真正的球面面积路径在 `lib/algorithms/coverage`、`resample` 与
   `docs/science/ASTROMETRY.md:64/:150`（那里确有 `cos(dec)` 缩放），均不在本批 ⇒ 该项结论=「本批无对象」，
   不等于「已核无缺陷」。
4. **派单说「export 阶段的投影与 WCS（创新点链的出口）」，但清单不含写 FITS 头的
   `lib/algorithms/fits_output/p3_output*`**：ALG §8:272-273 明说 `p3_wcs_fits_keywords` 只是
   「descriptor→文本的探针/调试面，生产 FITS 头写路径在 p3_output 域」⇒ 本批的卡格式
   finding（CR-58-6）不触及产品字节，产品头的列位问题需在 `p3_output` 域另批核。
5. 清单 6 项**全部读完**，无漏项；未读清单外文件者不作为 finding 对象（只在触发路径与锚比对中引用）。

## 收工 git status --porcelain 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工逐字一致（基线两项）；本路未在仓库目录内落任何文件，临时件仅
`独立审计/证据/通读-CR-58.md` 与 `独立审计/复算件/CR-58/crop_bbox_check.py`。
