# AUD-101 文档现状审计台账 · 批次 DB-13（研究与佐证层 16 份）

仓库：`F:\Astro dev\Astro CS Normalization Database`　冻结基线 HEAD=`c8f64e9a`（`git rev-parse --short HEAD` 实测一致）
判据：标准 01 `01_文档权威链整治标准.md` §1–§5；批次清单 `独立审计/批次清单/DB-13.txt`（16 行，全部存在、全部通读）
本批特有关注点逐份落到"与上位冲突""重复或重叠对象""悬空引用"三列；跨文件的"两篇打架"对集中在 §2 同主题组。

## 0. 工作树洁净核对（开工 / 收工）

开工（原文）：

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与派单预告的两项一致，无多余项。本批全程只读（`git status/ls-files/grep/log/show` + 文件读取 + 只读 `python -c` 解析 JSON/CSV），未执行任何构建、ctest、`run_checks.py`、`eng/**` 脚本，未 import 仓库内 Python。收工输出见文末 §7。

---

## 1. 逐份登记

字段顺序与列名按派单 §4。为可机并：每份先给一行 14 列紧凑登记，再给举证段（段落必须贴两处实际位置）。

### 1.1 docs/research/SNR_WEIGHT_RESEARCH_PACK.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/research/SNR_WEIGHT_RESEARCH_PACK.md | 文献与开源研究包：帧级 SNR、PSF 信号权重与逆方差叠加 | 229 | 研究与佐证（目录 `docs/research/` 一致，非角色错放） | 帧级 SNR / PSF 权重 / 逆方差叠加的文献与开源对照 | 有：`:5` "上游：ASTROCS_DESIGN.md §2.2（创新点二）、附录 B"（两锚均存在：`ASTROCS_DESIGN.md:130`、`:823`） | 12 个引用者（命令见举证） | 否（佐证；正本 = `docs/science/PSF_SIGNAL_WEIGHT.md` + `docs/science/NOISE_MODEL.md`） | 与 `docs/science/PSF_SIGNAL_WEIGHT.md` §4 重叠（举证 A） | **有**：`:1` 整行作废横幅即开头元信息块 | `:1` 日期+裁决编号+历史叙事；`:43/:53/:209` "DOC-404 核验订正/已 404"；`:113` 节标题带日期"（2026-09-20）"；`:115` 状态声明块；`:121` 外部 commit `08b8eb85ae17`+日期；`:193` "历史留痕" | `:105` `UNIFIED_MODEL.md`（无目录前缀，仓内无 `docs/science/UNIFIED_MODEL.md`）；`:215-229` 全部 `run/DOC-404/**` 路径不在树内 | **有，未解决**：`:26` 仍写 PSFSW "仅显式 `weight_mode=psfsw_robust` 使用，四分量独立存储"，与 `ASTROCS_DESIGN.md` §3.1、`docs/science/PSF_SIGNAL_WEIGHT.md:70-74`、`docs/contracts/DATA_SEMANTICS.md:3259/3262` 相反；同文件 `:141` 又说"本项目不产出该权重对象"（自相矛盾） | 合并：正文 §2.2/§3–§5 的文献与开源清单并入 `docs/references/SCIENTIFIC_REFERENCES.md`（标准 01 §2"研究与佐证以引用方式服务正本，不重复承载正本口径"），删除承载公式与产品口径的段（`:25-27`、`:140-146`、`:158`） |

举证 A（重叠：同一口径两处并存，属"研究包承载正本口径"）

- `docs/research/SNR_WEIGHT_RESEARCH_PACK.md:27`：`| **逆方差权重 w=1/σ²** | 叠加时的最优统计权重 | Phase2 消费时由 SNR 现场换算 w=SNR²/F_ref²，不入库、不预计算稠密 |`
- `docs/science/PSF_SIGNAL_WEIGHT.md:72`（§4 单一权重口径）：`w_k = SNR_k² / F_ref,k²  ≡  1 / σ_F,k²`，同节 `:74`："**没有可选择的口径**：不存在口径选择键、口径枚举、口径配置项或口径产物"
- 重叠范围：权重换算式与"不入库"产品口径两处各写一遍。差异：研究包写成对照表条目并夹带 `weight_mode` 语义，正本写成正向唯一口径 ⇒ 研究包应降级为引用（标准 01 §2 行"研究与佐证"）。

举证 B（同文件自相矛盾 = 冲突未消除的具体位置）

- `:26`：`| **PSF Signal Weight（PSFSW）** | … | 是权重不是信噪比；仅显式 \`weight_mode=psfsw_robust\` 使用，四分量独立存储 |（已按 §9.73 A44 作废：该概念不存在）`
- `:141`：`| PSFSW | … | 本项目**不产出该权重对象**；HiPS 只存 SNR，权重在 Phase2 现场算（A44 裁决） |`
- 上游判据：`docs/contracts/DATA_SEMANTICS.md:3259` "`psfsw_robust` … **禁用 token** … 接受面 = 空"；`:3262` "该字段不存在任何合法取值"。

下游计数命令（0 命中风险已按派单 §5.2 用词换套复核）：

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" grep -l -F "docs/research/SNR_WEIGHT_RESEARCH_PACK.md" | grep -v -F -x -e docs/research/SNR_WEIGHT_RESEARCH_PACK.md
ASTROCS_DESIGN.md / docs/DOCUMENT_INDEX.yaml / docs/algorithms/anchors/unresolved_registry.json
docs/plugins/algorithms_phase1/07_noise_snr.md / docs/references/SCIENTIFIC_REFERENCES.md
docs/research/PHOTOMETRY_RESEARCH_PACK.md / eng/tests/version/test_version_consistency.py
实验/absolute-snr/README.md / 实验/absolute-snr/results/{DOC_CORRECTIONS.md,REVIEW.md}
reports/v19r2/file_audit_inventory.csv / reports/v19r2/source_manifest.csv        （计 12）
```

悬空复核：`ls docs/science/UNIFIED_MODEL.md` → 不存在；`find . -name "UNIFIED_MODEL*"` → 仅 `./docs/design/UNIFIED_MODEL.md`；另有同族异名 `docs/science/UNIFIED_SCIENCE_MODEL.md` ⇒ `:105` 的裸文件名指向不确定（两份候选）。`ls run/` 下无 `DOC-404`（`run/` 已 gitignore，仅 `.gitkeep` 跟踪）。

机械列校准：`doc_mechanical.csv` 对本件 **meta_block=False 误报**（`:1` 整行即元信息/作废块）；**task_ids=[] 误报**（`DOC-404` 在 `:43,53,113,115,193,209,215,217,219,222,223,225,229` 共 13 行命中，正则未含 `DOC-` 族）；**commit_sha 高估**：所列 `00401706/01621459/10476408/10489171/1996164` 全是 DOI/JASA 编号而非 commit，真正的 commit 形态 `08b8eb85ae17`（`:121`）反而未列；**dangling_links=[] 漏报** `:105` 的 `UNIFIED_MODEL.md` 与 `:215-229` 的 `run/DOC-404/**`。

### 1.2 docs/traceability/TRACEABILITY_LAYERS.csv

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/traceability/TRACEABILITY_LAYERS.csv | （无 H1，CSV 表头行即列名） | 10 | 合同说明（CSV schema 合同：列定义＋每层必填规则＋取值域）；现目录 `docs/traceability/` **不在标准 01 §2 任何目标目录内** ⇒ 应迁往 `docs/contracts/`（或与 TRACEABILITY_SPEC 一并新设"标准与检查"目录） | 追溯矩阵的层定义与取值域 | **缺**（CSV 无上游条款标注） | 13（含 `eng/tools/traceability/check_traceability_matrix.py`、`eng/tests/traceability/{test_traceability_matrix.py,fixtures/*5 件}`、`docs/traceability/TRACEABILITY_SPEC.md`、`docs/DOCUMENT_INDEX.yaml`） | 是（层合同唯一登记处，被检查器直读） | 与 `docs/traceability/TRACEABILITY_SPEC.md` §2 表重叠（举证 C） | 无 | 无日期/任务编号/sha/历史叙事（扫描 0 命中） | 无（列名自指，不引外部路径） | 与 SPEC §2 表的层数口径不一致：本件列 **9 层**（SCI/ALG/DATA/API/ARCH/MOD/SRC/TEST/EVIDENCE），SPEC:31 声称"展成 **8 个必填层**" ⇒ 计数冲突（详见 1.5） | 保留（角色标注改为"合同说明"并迁入 `docs/contracts/`，同时按标准 01 §3 在 `DOCUMENT_INDEX.yaml` 保上游字段补标） |

举证 C（重叠段落）

- `docs/traceability/TRACEABILITY_LAYERS.csv:2`：`SCI,science_definition,science_id;science_doc,VERIFIED;MISSING,every_module,id/doc 空缺显式 MISSING; 空串判红`
- `docs/traceability/TRACEABILITY_SPEC.md:36`：`| 科学定义 | SCI | science_id / science_doc | 全矩阵唯一（跨模块） | VERIFIED / MISSING |`
- 重叠范围：9 层的层名、必填列、取值域在两份文件各写一遍（CSV 无"唯一性域"列，SPEC 表无"notes"列）⇒ 二者是同一合同的双写，任一处改动即漂移。合法分层应是 SPEC 引用 CSV 为机器事实源，而非各自展开。

### 1.3 docs/traceability/TRACEABILITY_MATRIX.csv

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/traceability/TRACEABILITY_MATRIX.csv | （CSV，表头 = 25 列列名） | 31（1 表头 + 30 模块行） | 合同说明（模块化追溯事实源的人工视图）；目录未登记角色 ⇒ 与 JSON 同批处置 | 30 个已注册模块的八/九层锚 | **缺**（无上游标注；机械列判 True 系误报，见下） | 14（`eng/tools/{check_traceability_matrix.py,traceability/check_traceability_matrix.py,traceability/gen_traceability_csv.py}`、`docs/contracts/{DATA_ARTIFACTS.md,UNIFIED_OBJECTS.md,unified_object_registry.json}`、`eng/contracts/data/unified_object_compatibility_map_v1.json`、`docs/DOCUMENT_INDEX.yaml` 等） | 与 JSON 二选一为正本（现 JSON 是"权威、机器真相"） | 与 `TRACEABILITY_MATRIX.json` 全量重叠（举证 D） | 无（首行即列名） | `:2-:26` 各行 notes 内嵌任务编号族：`BLD-003`、`CPU-001`、`IO-001/002/003`、`ABI-005`、`W3/W4-A4`、`FIX-202`、`RT-006/008`、`DATA-004`、`P1-CAL-IMPL`；`:17` 含裁决日期叙事"原 N≥16 linear_fit 档改投"（该句在 JSON 同行 `:396`） | `:17/:396` 引 `ASTROCS_DESIGN.md §5.5 五档表` —— §5.5（`ASTROCS_DESIGN.md:417-427`）**不含档界表**且明文"本节不复制档界与取值"，唯一正本是 `docs/plugins/algorithms_phase2/12_rejection.md` §9 | **有，未解决**：`:17`（=JSON `:396`）的生产 AUTO 排异路由写作"1≤N≤3 none / 4≤N≤5 percentile / 6≤N≤15 winsorized / **N≥16 linear fit**"，与同批 `docs/validation/SCIENCE_FREEZE.md:18-19`"三档 … N≥6→winsorized（原 N≥16→linear_fit 档改投 winsorized）"和正本 `12_rejection.md` §9 表相反 | 保留为机读视图：CSV 由 `eng/tools/traceability/gen_traceability_csv.py` 从 JSON 生成 ⇒ 以 JSON 为唯一正本、CSV 标"派生视图不手改"，并先修 `:17` 排异档表 |

举证 D（两份同构视图重叠 + 一致性实测）

- `docs/traceability/TRACEABILITY_SPEC.md:16-17`：`- \`docs/traceability/TRACEABILITY_MATRIX.json\`（权威，机器真相） / - \`docs/traceability/TRACEABILITY_MATRIX.csv\`（同构视图，供人工/diff）`
- 实测（`python -X utf8` 逐字段比对 30 行 × 22 列）：`csv rows: 30 / in JSON not CSV: [] / in CSV not JSON: [] / total field diffs: 0` ⇒ 本批时点两视图一致；命令：`json.load('docs/traceability/TRACEABILITY_MATRIX.json')` 对 `csv.DictReader('docs/traceability/TRACEABILITY_MATRIX.csv')` 按 `module_id` 对齐后逐列比较。
- 差异：仅 `notes` 列不参与比对（CSV/JSON 同值）。风险点：两文件手工并写、无生成锁，任一模块填充即产生第二正本。

机械列校准：**meta_block=False 正确**；**upstream_pointer=True 误报**（匹配到 `:10` notes 内"编排级上游 API-P1-001"字样，文件头无上游标注）；**sha256=True 误报**（`:29` notes 中"sha256 严格封装禁假哈希"是词面出现，非哈希值）；**task_ids 未逐条列出**（实际 29 行含任务/编号族）。

### 1.4 docs/traceability/TRACEABILITY_MATRIX.json

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/traceability/TRACEABILITY_MATRIX.json | `astrocs.traceability-matrix/v1`（30 模块 × 9 层） | 788 | 合同说明（机器真相/模块化事实源）；目录未登记角色 | 模块↔SCI/ALG/DATA/API/ARCH/MOD/SRC/TEST/EVIDENCE 锚 | **缺**（顶层无 ASTROCS_DESIGN 指针；SPEC:3 才标 §12.4） | 31（含 `docs/DEVELOPER_GUIDE.md`、`docs/diagnostics/TROUBLESHOOTING.md`、`docs/standards/API_STANDARD.md`、`docs/algorithms/PHASE2_{COVERAGE,MOSAIC_WRITE}.md`、`docs/modules/registry/astrocs.phase1.hips-writer.md` 等） | 是（本主题正本） | 与 1.3 CSV 全量重叠（举证 D） | **有**：`:2-:6` `schema / version / authoring_task / authoring_owner / base_main_sha` 五行头部元信息块 | `:4` `authoring_task: DOC-001`、`:5` `authoring_owner: SA-QA-29`、`:6` `base_main_sha: 0d32c07d65c6…`（真实 commit，`git log -1` 实测 = 2026-09-03，距 HEAD **1145 个提交**）；notes 内 58 行含任务编号族（`BLD-003`/`CPU-001`/`IO-001`/`W3`/`FIX-202`/`RT-006`/`DISP-*`/`CHG-*`） | 见举证 E（`returns/` 不存在） | ①`:396`/`:366`/`:448` 等以 `ASTROCS_DESIGN.md §5.5` 为排异档表依据，§5.5 不承载该表；②`MOD-astrocs-phase1-star-psf` 的 `science_doc` = `docs/algorithms/STAR_PSF_ALGORITHMS.md::SCI-P1-PSF-001` —— 科学定义层锚到算法层文档，而 `docs/science/PSF.md` 存在 ⇒ 层级倒置（`phase1-star` 行同样把 sci 与 alg 两层锚到不同文档但 TEST 层锚到 `docs/algorithms/…`） | 保留（正本）＋三项定点整改：删头部元信息块并补上游、把 `base_main_sha` 移入 `artifacts/evidence/`、修 `:396` 排异档表与 PSF 行的 SCI 锚 |

举证 E（EVIDENCE 层 VERIFIED 指向不存在目录）

- `docs/traceability/TRACEABILITY_MATRIX.json:30-32`：`"evidence_id": "EVID-BLD-003", "evidence_status": "VERIFIED", "notes": "… evidence 锚 returns/BLD-003 EVID-BLD-003。"`
- `docs/traceability/TRACEABILITY_MATRIX.json:82-84`：`"evidence_id": "EVID-CPU-001", "evidence_status": "VERIFIED", "notes": "… 证据 returns/CPU-001 EVID-CPU-001。"`
- 复核命令与结果：`git ls-files | grep -c "^returns/"` → `0`；`ls -d */ | grep returns` → 无；根目录实际只有 `artifacts/ reports/ run/ 整改/ …`。SPEC §4 规则 3（`:109-110`）把 `returns/` 列为合法证据面 ⇒ 合同与树不符，且这两行 VERIFIED 无落点（按派单 §5.6 属"已登记状态词越轴"）。

举证 F（正向 10 条链可走通／反向 10 条链断）

- 正向（行 → 锚文件 + 符号）取 10 行：`phase1-calibration / phase1-noise-snr / phase1-photometry / phase1-star / phase2-reject / phase2-session / phase3-resample2 / phase3-wcs / catalog-gaia / services-io`。方法：取 `module_anchor/science_doc/algorithm_doc/src_path/test_path` 首路径判存在，符号在源文件文本内判命中。结果 **10/10 文件存在，抽到的符号全 HIT**（例：`lib/algorithms/projection/p3_wcs.h::p3_wcs_make` HIT）。
- 但 4 行的 `test_path` 是散文而非可解析锚（无法机器判链）：`:106`（calibration）、`:210`（noise-snr）、`:236`（photometry）、`:262`（star-psf），例 `:106` 原文 `lib/algorithms/calibration/tests/p1cal (ctest p1cal_units/…); eng/tests/backend/test_calibration_oracle.py; eng/tests/unit/CMakeLists.txt:441 master_flat_median + …:457 p1cal` —— 内含括号/分号/中文，与 SPEC §5 的 `<path>::<symbol>` 表达不兼容（SPEC:157 又要求检查器对 `test_path` 报 `DANGLING_REF`）。按"只取首段"宽松解析后目录确实存在，但**判链规则本身不可执行**。
- 层语义断点：`test_status=VERIFIED` 且 `test_path` 指向 `.md` 文档的行 **14/30**（`phase1-cosmetic/:132`、`phase1-drizzle/:158`、`phase1-hips-writer/:184`、`phase1-wcs-platesolve/:288`、`phase1-star/:782`、`catalog-gaia/:730`、`phase2-coverage/:340`、`phase2-integrate/:366`、`phase2-reject/:392`、`phase2-session/:444`、`phase2-write/:548`、`phase3-resample2/:626`、`phase3-wcs/:678`、`phase3-writer/:704`），notes 自己写明"可执行 TEST-* 待落地" ⇒ 验证层用设计文档冒充测试锚，`MISSING` 与 `VERIFIED` 在同一行内并存（状态词跨轴，派单 §5.6）。
- 反向（ID → 合同注册表 `docs/contracts/INDEX.yaml`，SPEC:88-89 指定其为"真实唯一性以合同注册表为准"）：矩阵内非占位 ID 共 137 个，**64 个在 INDEX.yaml 全文零命中**；其中 **53 个的层状态为 VERIFIED**（含 `API-P1-001`、`API-P2-001`、`API-P3-001`、`API-GAIA-001`、`API-CPU-001`、`API-IO-STREAM-V1`、`API-ABI-001`、`DATA-P1-FITS`、`DATA-HIPS-001`、`DATA-TILE-001`、`DATA-P2-RES`、`DATA-P3-PROPS`、`DATA-P3-VER`、`ALG-COS-001`、`ALG-P1-WR-001`、`ALG-P2-RES-001`、`ALG-P2-SMP-001`、`ALG-P3-005`、`SCI-P1-PSF-001`、`SCI-P1-STAR-001` 及全部 `TEST-*`/`EVID-*` VERIFIED 项）。⇒ 反向只能走到矩阵行，走不到 SPEC 承诺的合同注册表。命令：`python -X utf8 -c` 取 `docs/contracts/INDEX.yaml` 全文对 137 个 ID 做包含判定。
- 反向（`docs/TRACEABILITY.csv` ↔ 本矩阵）：**两表无可连接键**。`docs/TRACEABILITY.csv` 67 行、`module` 列取值 `calibration/coverage/phase2/psf/…`，加 `MOD-` 前缀后与本矩阵 `module_id` 命中数 = **0**；其 `algorithm_id` 列（4 行非空）在矩阵全文命中数 = **0**；SPEC:117-118 称二者"互补不冲突"，SPEC:94 又称"第 5 节给出两表关系"，而 §5 实为"SOURCE SYMBOL 表达"（`:120-126`），**未给任何关系** ⇒ 双向映射规范缺失（悬空小节指针 + 结构性断链）。
- 反向（矩阵 → registry 页）：`docs/modules/registry/` 下 26 个模块页全部被 `module_anchor` 引用，无缺；未被引用的第 27 项 `module_id_migration_baseline.json` 是数据件非模块页。

机械列校准：`h1` 缺（JSON 无 H1，正确）；`meta_block=False` **误报**（`:2-:6` 即元信息块）；`dangling_links=[]` **漏报**（`notes` 内 `returns/BLD-003`、`returns/CPU-001`，以及 4 处 `test_path` 散文锚）；`upstream_pointer=True` 系 notes 词面命中，**误报**。

### 1.5 docs/traceability/TRACEABILITY_SPEC.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/traceability/TRACEABILITY_SPEC.md | 机器追溯合同（TRACEABILITY_SPEC v1） | 172 | 标准与检查（应属 `docs/standards/` 或 `docs/ci/`）；现目录 `docs/traceability/` 不在标准 01 §2 目标目录 ⇒ **角色错放，应迁往 `docs/ci/TRACEABILITY_MATRIX_CONTRACT.md`** | 追溯 ID 格式、层定义、跨层关系、检查器行为契约 | 有：`:3` "上游：ASTROCS_DESIGN.md §12.4（验证层级与四层验收）"（`ASTROCS_DESIGN.md:764` 存在） | 11（`docs/ci/01_CHECKS.md`、`eng/tests/testkit/{registry.json,testkit.spec.md,examples/registry.example.json}`、`eng/tools/{check_traceability_matrix.py,traceability/check_traceability_matrix.py}`、`docs/DOCUMENT_INDEX.yaml` 等） | 是（追溯合同正本） | 与 `TRACEABILITY_LAYERS.csv` 层表重叠（举证 C）；与 `eng/tools/traceability/check_traceability_matrix.py` 检查项表重叠（`:149-158` 复述判据前缀） | 有（轻度）：`:5` "矩阵基线 base_main_sha=…" 独立引用块 | `:1` 标题内嵌版本锚 "v1"；`:5` commit sha；`:29` "权威链（控制包 16 号文 §1）"；`:133` "IO-001/IO-002 落地"、`:134` "BLD-003 SKELETON"、`:136` "CPU-001 落地"；`:172` "冻结约束（项目负责人）优先于本文件"（自设第二套权威顺序，违标准 01 §1 末条） | `:21` `schemas/traceability_matrix.schema.json`（根下不存在；实件在 `eng/contracts/schemas/traceability_matrix.schema.json`）；`:94` "第 5 节给出两表关系"（§5 实为 SOURCE SYMBOL）；`:110` `returns/`、`:110` `TASK_STATE evidence_refs`（树内无该文件）；`:134` "22 个 registry 生产模块"（实为 26 页/26 行） | **内部自相矛盾**：`:70` "ID 一律 ASCII 大写" + `:78` 正则 `^MOD-[A-Z0-9]+(-[A-Z0-9]+)*$`，同条 `:78` 示例 `MOD-astrocs-phase1-calibration` 含小写 ⇒ 30/30 行 `module_id` 全部不合法（举证 G）；层数口径 `:31`"8 个必填层" vs 表 `:36-44` 9 行 vs `LAYERS.csv` 9 层 | 保留（标准与检查层正本），迁移到 `docs/ci/` 并就地订正 §3 正则/示例、`:31` 层数、`:94` 小节指针、`:21` schema 路径、`:134` 计数 |

举证 G（合同判据与被审对象相互否证，命令可复算）

- `docs/traceability/TRACEABILITY_SPEC.md:70`：`ID 一律 ASCII 大写，段分隔符取 -；空格、点、下划线之外的字符与空段都判红`
- `docs/traceability/TRACEABILITY_SPEC.md:78`：`MOD      ^MOD-[A-Z0-9]+(-[A-Z0-9]+)*$          例如 MOD-astrocs-phase1-calibration`
- 复算：按 SPEC 自带正则校验矩阵 30 行 → `MOD violations: 30`，`SCI/ALG/DATA/API/ARCH/TEST/EVID violations: 0`（脚本：`re.match(rx[layer],part)`，`docs/traceability/TRACEABILITY_MATRIX.json`）。
- 附带缺口：SPEC §3 只给 8 个层的 ID 正则（`:73-:80`），**未定义 `SRC` 层 id 格式**，而 `:42` 要求 SRC 必填 `src_id` 且 `:86` 把它列入唯一性强制品 —— 检查器对该列无判据可依。

### 1.6 docs/traceability/traceability_warn_baseline.json

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/traceability/traceability_warn_baseline.json | `astrocs/traceability-warn-baseline/v1` | 47 | 实际角色 = 过程台账（机器门 WARN 燃烧基线）；标准 01 §2 明列"过程台账…不放在正式文档层" ⇒ **角色错放，应迁往 `artifacts/evidence/traceability/`** 并由 `TRACEABILITY_SPEC.md` 正本引用 | 追溯矩阵检查器存量 WARN 的豁免登记 | **缺** | 5（`eng/tools/traceability/check_traceability_matrix.py:900` `WARN_BASELINE_REL`、`docs/DOCUMENT_INDEX.yaml:1473`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/file_audit_inventory.csv`、`reports/v19r2/source_manifest.csv`） | 否（门的豁免面，不是文档口径） | 6 条 entries 的 `reason` 与 `burn_down` 全文逐字重复（举证 H）；与 `TRACEABILITY_SPEC.md:158` `REF_OUT_OF_SCOPE` 判据行重叠 | **有**：`:2-:8` `schema / task / note` 头部块 | `:3` `"task": "W4-A3"`；`:12,18,24,30,36,42` `"owner": "DOC-SCI-001/W4-A4"`；`:5` "附录 H.6" 历史叙事式来历说明 | `:5` "附录 H.6" 全仓零出处（`git grep -n "附录 H" -- docs` 仅命中本文件自身） | 条目自述与其规则相互否证（举证 H）；无与科学/算法上位的直接口径冲突 | 删除（按标准 01 §2 第三条迁 `artifacts/evidence/`，由 SPEC 正本一行引用） |

举证 H（台账条目与自己声明的燃烧规则相抵）

- `docs/traceability/traceability_warn_baseline.json:5`：`"烧毁式 WARN 基线（附录 H.6）。规则：①只降不升 —— 基线条目若在当前扫描面匹配不到，检查器判 BASELINE_STALE(ERROR)，必须随修复同步删除；…"`
- 同文件 `:11-:14`（同类 4 条）：`"sig": "REF_OUT_OF_SCOPE|MOD-astrocs-phase1-calibration|EVID|EVID-P1-CAL-001"` + `"reason": "VERIFIED 行的 sci/alg/data/api/arch id 尚未有 authority 文件可见"`
- 矛盾点：① sig 落在 **EVID** 层，reason 却一律陈述 **sci/alg/data/api/arch** 层缺文件——理由与被豁免对象不是同一层，条目按自身"匹配不到即 STALE"的规则应被清除；② reason 所述"authority 文件不可见"对这 3 个模块不成立：`docs/science/CALIBRATION.md`、`docs/science/NOISE_MODEL.md`、`docs/science/PHOTOMETRY.md`、`docs/algorithms/{CALIBRATION_ALGORITHMS,NOISE_ESTIMATION,PHOTOMETRIC_FIT}.md` 均在树内（`ls docs/science docs/algorithms` 实测）。
- 本审查未运行检查器（派单 §1 禁止），因此**不断言当前 WARN 集合**，只登记文档侧可自证的层错配。

### 1.7 docs/validation/SCIENCE_FREEZE.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/validation/SCIENCE_FREEZE.md | Science Freeze（V17 True Final Freeze） | 80 | 负责人面（冻结/验收状态与排除面记录，标准 01 §2 "发布状态、验收、裁决记录"）；现目录 `docs/validation/` 不在标准 01 §2 目标目录 ⇒ **角色错放，应迁往 `docs/owner/SCIENCE_FREEZE.md`** | Phase1/2/3 基础算法与契约的冻结状态、性能基线、冻结后允许/排除面 | 有：`:3` "上游：ASTROCS_DESIGN.md §12.1、§12.5"（两锚存在：`:723`、`:790`） | 5（`docs/DOCUMENT_INDEX.yaml:1478`、`docs/audit/doc_classification.csv`、`docs/validation/v6/QA_MATRIX.md:37`、`reports/v19r2/*` 两件） | 是（冻结状态唯一登记处） | 与 `ACCEPTANCE_SPEC.md` 的门状态面重叠：`:5` "G1-G10 全部 PASS" ↔ `ACCEPTANCE_SPEC.md:260` §8 预览版发布门汇总（举证 I） | 有：`:1` 标题带版本锚、`:5-:6` 开头"冻结结论"状态声明块 | `:1` "V17 True Final Freeze"；`:17` "M3 裁决 2026-09-25 后为三档"（负责人某日裁决 + 日期）；`:19` "原 N≥16→linear_fit 档改投 winsorized"（历史叙事）；`:22` "包 sha1 712cc7c3…"；`:23-:24` "该档 WBPP 2.4.0+ 为 ESD，本仓取 linear_fit = WBPP ≤2.3.x 旧表"；`:50` "clean-tree 74/74 gate"；`:76-:77` "旧 config 必须经 eng/tools/migrate_stage2_config.py 迁移" | `:5` `ACCEPTANCE_GATES.md` —— 仓内无此文件（举证 I） | **有，未解决**：`:11-:52` 以 `FROZEN / PASS / SUPPORTED / FINAL / NOT_CLAIMED` 在登记表里**预写状态**，与上位 `ASTROCS_DESIGN.md:790-:803` §12.5"状态阶梯（唯一口径）… 状态由检查与验收现场计算，**登记表不预写状态**"相反（§12.5 取值集内无 FROZEN/PASS/FINAL/SUPPORTED/NOT_CLAIMED） | 保留（内容需存续）＋迁移到 `docs/owner/`＋把状态字段改为"由检查现场计算"的引用式（标准 01 §1 下位与上位冲突即缺陷、§2 负责人面归目录） |

举证 I（悬空 + 与上位冲突）

- `docs/validation/SCIENCE_FREEZE.md:5`：`> 冻结结论：\`ACCEPTANCE_GATES.md\` G1-G10 全部 PASS、known P0/P1 = 0，` ／ `:42`：`FINALIZATION_SELF_REVIEW = PASS（含 clean-tree 74/74 gate + …）`
- 复核：`find . -iname "*ACCEPTANCE*" -not -path "./.git/*"` → `ACCEPTANCE_SPEC.md`、`artifacts/acceptance`、`工程控制/RELEASE-05/ACCEPTANCE.md` 等，**无 `ACCEPTANCE_GATES.md`**；`git grep -ln "ACCEPTANCE_GATES"` 仅命中本文件与 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv` ⇒ 引用的门清单文件不存在（门清单实际在 `ACCEPTANCE_SPEC.md`）。
- 与上位：`ASTROCS_DESIGN.md:803` 原文 `状态由检查与验收现场计算，登记表不预写状态；合成测试或历史节点不等于真实数据/Windows VERIFIED。` 而本文件 `:11-:52` 就是一张预写状态的登记表（21 条 `X = FROZEN/PASS/…`）。

同主题一致性（正面）：`:16-:19` 的生产排异 AUTO 路由三档与正本 `docs/plugins/algorithms_phase2/12_rejection.md` §9 表逐字一致，是对本批唯一正确的档表侧（对立方见 1.3/1.4）。

### 1.8 docs/validation/v6/BASELINE_COMPARISON_MATRIX.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/validation/v6/BASELINE_COMPARISON_MATRIX.md | 基线比较矩阵（QA-MATRIX-001） | 69 | 标准与检查（门规格/声明边界）；`docs/validation/` 不在标准 01 §2 目标目录 ⇒ 角色错放；按标准 01 §2 第三条，该轮历史档案整体应出正式文档层 | 集成模式 × 基线 × 可声明结论的预注册矩阵 | 有：`:5` "上游：ASTROCS_DESIGN.md §12（验证体系）" | 6（`docs/DOCUMENT_INDEX.yaml:1484`、`eng/tools/doccheck/doc_hygiene_exempt.json`、`artifacts/evidence/v6/qa-design/SUMMARY.md`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` 两件） | 否（其机器源为 `qa-design/data/baseline_matrix.json`） | 与机器源 `artifacts/evidence/v6/qa-design/data/baseline_matrix.json` 的 `modes[]` 全量重叠（渲染块自述机械写入） | 有：`:1` DOC-001 溯源注记块 | `:1` 日期 2026-09-16 + 任务编号 DOC-001/ROOT-007 + "上一轮治理产物"历史叙事；`:9` `DESIGN-P2-001 §6.3`、`控制器 C-004.1`；`:22` "属预注册（W10），本任务不发明"；`:59` "C-004.1" | `:7` `reports/v6/qa-design/data/baseline_matrix.json`、`render_docs.py`、`check_docs.py` —— `reports/` 下只有 `v19r2`，实件在 `artifacts/evidence/v6/qa-design/`（命令见举证 J） | **有，未解决（本批最高价值项）**：`:31` 把 `psfsw_robust` 标为 `class=production`、`:14/:57` 给它声明面 —— 与 `docs/science/PSF_SIGNAL_WEIGHT.md:70-74`（§4 单一权重口径、"没有可选择的口径"）与 `docs/contracts/DATA_SEMANTICS.md:3259`（`psfsw_robust` = 禁用 token，**接受面 = 空**）直接相反；本文件**没有**同目录其余 4 份的 A44 作废横幅 | 删除（出正式文档层，落 `artifacts/evidence/v6/qa-design/`，若需保留声明规则则把 §4/§5 的 `equal/pixel_ivar` 不得宣称最优两条并入 `docs/science/INTEGRATION.md` 的引用），理由：标准 01 §1"下位与上位冲突时以上位为准；冲突本身是缺陷"+ §2 第三条 |

举证 J（悬空机器源 + 冲突的机器侧同源）

- `docs/validation/v6/BASELINE_COMPARISON_MATRIX.md:7`：`- 机器规格：\`reports/v6/qa-design/data/baseline_matrix.json\`；渲染块由 \`render_docs.py\` 机械写入，\`check_docs.py\` 校验逐字一致。`
- `docs/validation/v6/BASELINE_COMPARISON_MATRIX.md:31`：`| \`psfsw_robust\` | production | psfsw_robust_weight | 1 | \`Wt_k=C_norm*S^alpha*Conc^beta/(N^gamma*B^delta); …\` | … | 只能声明在指定验收数据上优于指定基线 | ivar；fisher_information；… |`
- 复核：`ls reports/` → 仅 `v19r2`；`[ -e reports/v6 ]` → NO；实件 `artifacts/evidence/v6/qa-design/data/baseline_matrix.json` 存在，其 `modes[]` 内 `psfsw_robust` 的 `class` 同样为 `production` ⇒ 渲染块与机器源彼此一致，**共同与科学/合同上位相反**。合同侧的相同矛盾还在机器登记层内部：`eng/contracts/data/v6_clause_registry_v1.json` 的 `weight_modes.production = ["point_information","surface_gls"]` 与 `retired=["psfsw_robust"]`，同文件 `clauses[]` 里 `FZ-MODE-PRODUCTION` 的 `status` 仍为 `FROZEN`、`value` 仍写 `"point_information | surface_gls | psfsw_robust"`。

### 1.9 docs/validation/v6/NEGATIVE_MUTATION_CATALOG.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/validation/v6/NEGATIVE_MUTATION_CATALOG.md | 负向 mutation 目录（QA-MATRIX-001） | 70 | 标准与检查（门的负例清单）；目录未登记角色，属该轮历史档案 | 56 条 science/spec/doc mutation 及其目标门 | 有：`:5` "上游：ASTROCS_DESIGN.md §12（验证体系）" | 6（`docs/DOCUMENT_INDEX.yaml:1489`、`eng/tests/unit/v6_p1_psfw/p1psfw_tests_record.cpp`、`artifacts/evidence/v6/qa-design/SUMMARY.md`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` 两件） | 否（自述 `:7` "机器目录（唯一事实源）"是 JSON） | 与 `artifacts/evidence/v6/qa-design/data/mutations.json` 重叠；与 `docs/validation/v6/QA_MATRIX.md:88-91` §6 三类总则重叠 | 有：`:1` DOC-001 溯源注记块 | `:1` 日期 2026-09-16 + DOC-001/ROOT-007 + "上一轮治理产物"；`:56-:69` `V-*` 规则号与 `P0-0x` 门号成串过程编号 | `:8` `reports/v6/qa-design/data/mutations.json`、`:9` `reports/v6/qa-design/oracle/run_mutations.py`、`:10` 实测结果 `reports/v6/qa-design/evidence/mutations.json`（`reports/v6` 不存在） | 无与上位科学口径相反的语句；但 `:28/:33/:37-:39/:49` 的目标门（`G-ANA-07`、`G-MC-04`、`G-BASE-01..04`）以 `psfsw_robust` 为被测对象，随 1.8 的上位冲突连带失效（12/44 门含 psfsw，命令见举证 K） | 删除（出正式文档层），或降级为 `artifacts/evidence/v6/qa-design/` 内的一页并由 `docs/ci/` 现行门表引用 |

计数复核（本件数字与机器源一致，可正面登记）

```text
$ python -X utf8 -c "…json.load('artifacts/evidence/v6/qa-design/data/mutations.json')…"
total mutations: 56   by mech: {'science': 39, 'spec': 14, 'doc': 3}
cited in doc: 43   cited not in JSON: []
$ …re.search('psfsw|PSFSW', gate) over artifacts/evidence/v6/qa-design/qa_matrix.json
gates: 44   referencing psfsw: 12 -> G-ANA-02 G-ANA-06 G-ANA-07 G-MC-04 G-MC-05
            G-INJ-03 G-INJ-04 G-INJ-05 G-INJ-06 G-BASE-01 G-BASE-03 G-BASE-05
```

### 1.10 docs/validation/v6/ORACLE_AND_ZERO_CASE_POLICY.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/validation/v6/ORACLE_AND_ZERO_CASE_POLICY.md | 独立 Oracle、零用例即红与负向 mutation 政策（QA-MATRIX-001） | 46 | 标准与检查（判据纪律）；目录未登记角色，属该轮历史档案 | Oracle 独立性白名单、零用例即红规则、三类 mutation 驱动器 | 有：`:5` "上游：ASTROCS_DESIGN.md §12（验证体系）" | 6（`docs/DOCUMENT_INDEX.yaml:1494`、`eng/tests/unit/v6_p3_rsmp/p3_rsmp_oracle.h`、`artifacts/evidence/v6/qa-design/SUMMARY.md`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` 两件） | 否（`docs/ci/01_CHECKS.md` 与 `ACCEPTANCE_SPEC.md` 才是现行判据层） | 与 `ACCEPTANCE_SPEC.md:99`（"恒真门不计证据"）、`docs/science/CONTROL_WEIGHT_SNR.md:112/:218`（判据非退化）重叠：三处各写"门必须能红/恒真门不作证据"（举证 L） | 有：`:1` DOC-001 溯源注记块（含宪章作废说明） | `:1` 日期 2026-09-16 + DOC-001/ROOT-007 + "已废止"；`:10/:19` 标题与条目挂"对应根因 R2/R4""对应宪章 §14.2 与任务强制纪律"；`:44-:46` "本任务是验证设计…生产实现期（W5/W7/W8/W10）"（任务波次编号 + 过程语域） | `:7` `reports/v6/qa-design/oracle/{qa_oracle,validate_spec,run_mutations,run_all}.py`、`:8` `reports/v6/qa-design/evidence/*`（`reports/v6` 不存在）；`:19` "宪章 §14.2" —— `ASTROCS_PROJECT_CONSTITUTION.md` 全仓无文件（`find` 无命中），本文件 `:1` 自述其已废止 | `:19` 把"零用例即红"的规范来源记为**已废止宪章 §14.2**，同时 `:1` 又声明该宪章"不构成现行依据"——同文件自我否定权威来源；上位 `ASTROCS_DESIGN.md` §12 未承载该条款号 ⇒ 判据悬于废止文件 | 删除（其现行内容已被 `ACCEPTANCE_SPEC.md` §3 门表与 `docs/ci/01_CHECKS.md` 承载；标准 01 §1 末条"任何文档不声明自己的第二套权威顺序"） |

举证 L（三处同主题、判据措辞不一）

- `docs/validation/v6/ORACLE_AND_ZERO_CASE_POLICY.md:24`：`  executed_cases == 0 且非 pending                -> rc=2（不算 PASS）`
- `ACCEPTANCE_SPEC.md:99`：`| 判据判别力 | 每个度量有"真值无效应 ⇒ 归零/判红"的负例，恒真门不计证据 |`；`ACCEPTANCE_SPEC.md:63`：`| 非退化负例 | … 恒真门不出现 |`
- 差异：本件给的是 rc 规则（机器），`ACCEPTANCE_SPEC.md` 内部两条对"恒真门"一个写"不出现"、一个写"不计证据"，互不相容 ⇒ 由 `实验/absolute-snr/REPORT_paper.md:365`（C6）登记，但至今未在任何一侧订正（见 §2 冲突对 P8）。

正面复核（本件自述的机器约束成立）：`oracle.truth` 与 `oracle.must_not` 在 `artifacts/evidence/v6/qa-design/qa_matrix.json` 44 门中缺失数均为 0；`case_ledger.json` 含 `required_cases/executed_cases/counts_as_pass` 字段（命令：`python -X utf8 -c` 遍历 `gates[].oracle`）。

### 1.11 docs/validation/v6/P0_GATE_FAMILY.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/validation/v6/P0_GATE_FAMILY.md | P0 门族：历史根因 R1/R2/R4/R5/R10 与 785 合并层账本（QA-MATRIX-001） | 35 | 过程台账（历史根因销账建议）+ 标准与检查；标准 01 §2 第三条 ⇒ 不放正式文档层 | 6 条 P0 门的根因、判据与 mutation 承载 | 有：`:7` "上游：ASTROCS_DESIGN.md §12（验证体系）" | 5（`docs/DOCUMENT_INDEX.yaml:1499`、`artifacts/evidence/v6/qa-design/SUMMARY.md`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` 两件） | 否（其门规格机器源在 `qa-design/qa_matrix.json` 的 `P0-01..06`） | 与 `docs/validation/v6/QA_MATRIX.md:150-155` §8 的 `P0-01..P0-06` 行逐条重叠（同一门族两处各写判据） | **有**：`:1` A44 作废横幅 + `:3` DOC-001 溯源注记，共两块 | `:1` "⚠ 已按 §9.73 A44 作废…保持历史原样、仅作留痕"；`:3` 日期 2026-09-16；`:5` 标题含"历史根因""785 合并层账本"；`:11` "销账任务与处置权在控制器/负责人（ADJ-AR-03）"；`:33-:35` 建议项内嵌 `RUNTIME-CI-001`/`FINAL-AUDIT-001`/`W9`/`W13` 任务编号 | `:9` `reports/v6/review-audit/03_缺陷账本_重开清单.md`、`reports/v6/review-audit/05_未决风险与控制器裁决事项.md`（实件在 `artifacts/evidence/v6/review-audit/`）；`:10` `SCI-ADJ-001_CONFLICT_MATRIX.md` 全仓无此文件；`:17` `_cache/L*.md` 与 `ROOT_CAUSES.md` 未带可解析路径（`ROOT_CAUSES.md` 实位于 `artifacts/evidence/audit-2026-01/`） | `:27` 表行 `P0-04` 判据"退休 `support×snr²`/`weight_mode=0`/`auto`/`support_x_snr2` 仅登记为 retired token"＋行末内联"（已按 §9.73 A44 作废：该概念不存在）"——同一行既给判据又宣布判据对象不存在，门无承载对象；与同目录 1.8 未作废的 psfsw=production 属同一未消解冲突 | 删除（出正式文档层），理由：标准 01 §2 第三条"历史档案、过程台账…需要留证的台账落 `artifacts/evidence/` 并由正本引用" |

数字复核：`:15` 的合并层拆分 `667 + 83 + 29 + 4 + 2 = 785` 算术成立；`523` 叶子层 id 与被审对象无可核路径 ⇒ 该两条数字目前**不可复算**（其账本载体在 `artifacts/evidence/`，本批文档未给可解析指针）。

### 1.12 docs/validation/v6/QA_MATRIX.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/validation/v6/QA_MATRIX.md | QA-MATRIX-001 科学 QA 矩阵（解析 / Monte Carlo / 注入源 / 真实数据 / 负向 mutation / 基线比较） | 873 | 标准与检查（44 门规格）；目录未登记角色，属该轮历史档案 | 44 条科学/规格门的判据、容差来源、零用例即红、mutation、Oracle、适用域 | 有：`:7` "上游：ASTROCS_DESIGN.md §12（验证体系）" | 8（`docs/DOCUMENT_INDEX.yaml:1504`、`docs/validation/v6/NEGATIVE_MUTATION_CATALOG.md:9`、`eng/contracts/data/v6_clause_registry_v1.json`、`eng/tools/doccheck/doc_hygiene_exempt.json`、`artifacts/evidence/v6/qa-design/SUMMARY.md`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` 两件） | 否（自述 `:9` 机器规格是唯一事实源） | 与 `artifacts/evidence/v6/qa-design/qa_matrix.json`（渲染块逐字镜像）、与 1.9/1.11 三份互相分载同一门族（举证 M） | **有**：`:1` A44 作废横幅 + `:3` DOC-001 溯源注记 + `:13-:15` "本文件是验证设计规格…本文不改任何冻结门/容差、不写生产源码、不 commit/push、不派生子代理" 纪律声明块 | `:1-:3` 两块作废/溯源横幅（含日期 2026-09-16）；`:13` 纪律声明；`:27` §0 摘要表把执行计数当现行状态；`:112-:155` 每行 Owner 列内嵌 `ALG-P2-*`/`IMPL-P1-*`/`W3`/`W5`/`W10` 任务与波次编号；`:335/:464` "负责人签字（只登记）：SO-02/SO-03/SO-07"；`:753/:817` 行内"（已按 §9.73 A44 作废：该概念不存在）" | `:9` 四个 `reports/v6/qa-design/**` 路径、`:29-:30` 与 `:46` 的 `reports/v6/qa-design/oracle/*`；`:32` `ASTROCS_PROJECT_CONSTITUTION-001`（无文件）；`:38` `CONTROLLER_LOG.md`（无文件）；`:65/:250/:850` `FREEZE_LIST §1/§3.2/§7`（无文件）；`:835/:842` `DEFECT_LEDGER.json`（无文件）；`:10` 复跑入口 `reports/v6/qa-design/oracle/run_all.py`（无路径） | **有，未解决（两处）**：①`:44` "冻结 mode（原样继承）：生产 = `point_information \| surface_gls \| psfsw_robust`" 与 `docs/science/PSF_SIGNAL_WEIGHT.md:70-74`、`docs/contracts/DATA_SEMANTICS.md:3259/3262` 相反；②`:30-:38` §1 自设"本矩阵全部判据以下列上位权威为语义源，原样继承、不新增第三条口径"，把已废止宪章与控制器日志列为权威源 ⇒ 违标准 01 §1 末条 | 删除（出正式文档层；44 门的现行承载改由 `docs/ci/01_CHECKS.md` + `ACCEPTANCE_SPEC.md` 引用 `artifacts/evidence/v6/qa-design/`），理由：标准 01 §1 末条 + §2 第三条 |

举证 M（一份文档三重承载：人读渲染块 + 机读 JSON + 兄弟文档互引）

- `docs/validation/v6/QA_MATRIX.md:109`：`<!-- QA-MATRIX-TABLE-BEGIN -->` … `:156`：`<!-- QA-MATRIX-TABLE-END -->`（44 行表，自述由 `render_docs.py` 机械写入）
- `docs/validation/v6/QA_MATRIX.md:9`：`- 机器规格（唯一事实源）：\`reports/v6/qa-design/qa_matrix.json\`；mutation 目录 …；基线矩阵 …；零用例账本 \`reports/v6/qa-design/case_ledger.json\``
- 差异：机器源实际在 `artifacts/evidence/v6/qa-design/`（已出库），文档仍指向 `reports/`，且 `:26` 摘要表称"3 生产模式"（含 psfsw）——人读块、机读块与 1.8 三者互相同构重复，正本地位在链条里被三处分割。

作废处理不一致（本批可自证的直接缺陷）：同一 A44 裁决下，`:1`（QA_MATRIX）、`docs/validation/v6/P0_GATE_FAMILY.md:1`、`docs/research/SNR_WEIGHT_RESEARCH_PACK.md:1` 三份贴了作废横幅，`docs/validation/v6/BASELINE_COMPARISON_MATRIX.md`、`docs/validation/v6/ORACLE_AND_ZERO_CASE_POLICY.md`、`docs/validation/v6/README.md` 三份**没有**，而前者与后者的正文口径同等受影响（psfsw=production 分别见 `BASELINE_COMPARISON_MATRIX.md:31` 与 `QA_MATRIX.md:44`）⇒ 同一裁决在三份文档上留痕不一致。

### 1.13 docs/validation/v6/README.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/validation/v6/README.md | docs/validation/v6 索引（QA-MATRIX-001） | 29 | 模块工作细节（目录索引）／实为历史档案的导读页 | v6 验证档案 5 份文件的内容索引与条款 id 落点 | 有：`:5` "上游：ASTROCS_DESIGN.md §12（验证体系）" | 5（`docs/DOCUMENT_INDEX.yaml:1509`、`artifacts/evidence/v6/qa-design/SUMMARY.md`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` 两件） | 否（索引页） | 与 `docs/DOCUMENT_INDEX.yaml:1484-1511` 对本目录 6 条登记重叠（同一目录两张地图） | **有**：`:1` DOC-001 溯源注记块 | `:1` 日期 2026-09-16 + DOC-001/ROOT-007 + "上一轮治理产物"；`:22-:25` "已按 `CHG-2026-09-22-V6-CONTRACT-MERGE` 整体出库（出库清单见 …）"（变更编号 + 历史叙事）；`:29` "未 commit/push、未宣布发布"（过程语域，属会话状态而非文档内容） | `:16-:17` `reports/v6/qa-design/{SUMMARY.md,qa_matrix.json,case_ledger.json,data/*.json,oracle/*.py,evidence/}`、`:27` 复跑命令同路径（`reports/v6` 不存在，实件在 `artifacts/evidence/v6/qa-design/`） | 无科学口径冲突（本页只作索引）；但 `:19-:21` 指定 `eng/contracts/data/v6_clause_registry_v1.json` 为条款 id 的现行机器登记表，而该登记内 `FZ-MODE-PRODUCTION` 的 `value` 仍含 `psfsw_robust`——把读者导向一个自身矛盾的来源 | 删除（出正式文档层；若目标架构保留 `docs/ci/` 门表，则本索引改由 `docs/ci/01_CHECKS.md` 的档案节以一行指针承载），理由：标准 01 §2 第三条 + §3"唯一索引地图" |

目录清单核对：`:9-:14` 列 5 份文件，与 `ls docs/validation/v6/` 实际 6 项（含 README 自身）一致，无缺项、无多项。`:10` 称"44 条门"、`:12` 称"56 条 mutation"，与机器源实测（44、56）一致。

### 1.14 实验/absolute-snr/B7_ABSOLUTE_SNR_RECON.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 实验/absolute-snr/B7_ABSOLUTE_SNR_RECON.md | B7 要点：逐像素绝对 SNR 重建（SCI-RECON-SNR-01） | 57 | 研究与佐证（实验单元要点）；标准 01 §2 该角色目标目录为 `docs/research/`，但最高设计 §12.3 规定实验单元自包含于 `实验/<单元>/` ⇒ 目录**不算错放**，但须由 `DOCUMENT_INDEX.yaml` 登记（现未登记） | 逐像素绝对 SNR 四臂重建与归零判据 | **缺**（`:1-:7` 开头块只给"完整报告/结果/代码"指针，无最高设计条款） | **0 命中**（举证 N：三条检索词各跑一次，仅自动清单文件命中） | 否（要点页；完整报告不入库） | 与 `docs/science/CONTROL_WEIGHT_SNR.md:100-118` §2b 重叠：本文件 5 项核心数值已被正本逐字吸收（举证 O） | 有：`:3-:7` 指针块 | `:1` 标题内嵌任务编号 `SCI-RECON-SNR-01`；`:4-:5` 结果文件名内嵌 seed 日期 `20260921/20260922`；`:50` 节标题"我证伪的任务书前提"（过程语域） | `:3` `run/SCI-RECON-SNR-01/REPORT.md`（`run/` 下无该目录，仅 `.gitkeep` 跟踪） ⇒ 完整论证无入库载体 | 无相反口径；`:13` `= NOISE_MODEL §5c:208`、`:16` `§5c:198`、`:17` `§5b:140`、`:26` `§5c:214` 四个行锚经实测**全部命中**（唯一锚全对的批次件） | 保留并补上游；同时把 `:3` 的"完整报告在 run/"改为指向入库件（标准 01 §3"追溯不到的机制先补要点再保留"） |

举证 N（0 命中复核，含正例对照）

```text
$ git grep -l -F "实验/absolute-snr/B7_ABSOLUTE_SNR_RECON.md" | grep -v -F -x -e 同路径      → 0
$ git grep -ln "B7_ABSOLUTE_SNR_RECON"   → reports/v19r2/file_audit_inventory.csv, reports/v19r2/source_manifest.csv（自动清单，非引用）
$ git grep -ln "SCI-RECON-SNR-01"        → 仅本单元自身 7 件（B7 md / code/b7_* / results/b7_*）+ 两份自动清单
$ git grep -l "REPORT_paper.md"          → 6 命中（同一条命令在应命中对象上跑通，证明检索面有效）
```

本单元主报告 `README.md` 与 `REPORT_paper.md` 对 `B7`/`b7`/`EXP-02…EXP-06` 的提及数均为 **0**（`grep -c "B7\|b7" README.md` → 0；`grep -n "EXP-0[2-6]" README.md REPORT_paper.md` → 空）⇒ 单元自述索引（`README.md:244-286` 附录 A）不覆盖后 7 个子实验，B7 结论在单元内无人引用。

举证 O（结论被正本吸收、证据锚却不指向承载件——本批关注点 2 的确证）

- 正本：`docs/science/CONTROL_WEIGHT_SNR.md:114`：`天光双计臂（…）**不收敛**（实测方差比 1.36802）` 与 `:112`：`"各臂必须收敛"类判据在两臂上恒真、在一臂上恒假，**不携带信息**；证据资格 = 空。` 与 `:109`：`源项只捕获 **14.2%**、逐像素相对误差中位 0.81`
- 本件：`:36-37`：`但 **T_naive_sky 不收敛**（比值 1.36802，与预言相对差 1.1e-16）… 有源帧上同一判据 37.31 ≫ 0.05 ⇒ 归零判据非退化`、`:41-42`：`检测完备性 0.43，端到端 D_core 中位相对误差 **0.376**（判红）`、`:43`：`源项只捕获 **14.2%**`
- 锚位断裂：`1.36802`、`0.376`、`0.1424` 三个值**只**存在于 `实验/absolute-snr/results/b7_absolute_snr_recon.json`（实测：b1–b6 六件 JSON 中零命中），而正本 `CONTROL_WEIGHT_SNR.md:118` 写"本节数值与判据的证据锚在 §8a/§8b"，`:195/:207` 的依据行只列 `b1/b2/b4`、`b3/b6` ⇒ 吸收的结论与其声明的证据面不相交；`docs/` 全层对 `b7_absolute_snr_recon` 引用数 **0**（`git grep -ln "b7_absolute_snr_recon" -- docs`）。

### 1.15 实验/absolute-snr/README.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 实验/absolute-snr/README.md | SCI-B — 跨帧绝对 SNR 传递链 · 实验报告（实验单元） | 385 | 研究与佐证（实验报告，最高设计 §12.3 八要素载体） | 帧级 SNR 定义、三口径适用域、逆方差集成、Phase3 传递、退化门审查 | 有（但在元信息块内）：`:4` "权威依据：`ASTROCS_DESIGN.md` §2.2、§4.4、§5.3、§12.3；`ACCEPTANCE_SPEC.md` §2.2；`docs/science/…`" | 3（`eng/ci/check_naming_surface.py`、`eng/ci/ledgers/naming_surface.json`、`实验/absolute-snr/REPORT_paper.md`）；目录名 `实验/absolute-snr` 被 `docs/science/{PSF_SIGNAL_WEIGHT,CONTROL_WEIGHT_SNR,NOISE_MODEL}.md`、`docs/plugins/algorithms_phase1/07_noise_snr.md`、`docs/plugins/algorithms_phase2/13_integration.md`、`docs/design/UNIFIED_MODEL.md`、`docs/contracts/UNIFIED_OBJECTS.md` 等 73 件引用 | 否（正本 = `docs/science/CONTROL_WEIGHT_SNR.md` §8a/§8b、`docs/science/PSF_SIGNAL_WEIGHT.md` §7a） | 与 `REPORT_paper.md` 同主题双写（§3.1/§3.3/§3.4 ↔ 报告 §4.1/§4.4/§4.5），且两处数字不同（举证 Q） | **有**：`:3-:8` 六行块（单元目录 / 权威依据 / 固定 seed / 复跑 / 吸收的先行工作） | `:7` "日志落 `run/SCI-402/`"；`:8` "吸收的先行工作：EXP-205（`run/RELEASE-02/实验/E11-SNR三口径精度/`…）"；`:20` "转 FIX/DOC 处理"；`:113` "**已按独立审稿 B1/I1 修正后重跑**"；`:128` "该门已改名为 …"；`:156` "EXP-205 直接把 1.44 当 dex 常数用"；`:186-:187` 时刻 `13:14:04`/`13:38:11`/`12:4x`/`13:17`/`13:4x` 与"并发的 FIX-405 … 编辑中间态"；`:171` "扣分项已修"；§9（`:297-:329`）整节为审稿-修正台账（B1/I1–I6/M1–M12） | `:333` "完整报告见 `docs/EXP-01-DELTA-AND-ESTIMATOR.md`" —— 根 `docs/` 下无此文件，实件在 `实验/absolute-snr/docs/EXP-01-DELTA-AND-ESTIMATOR.md`；`:226` `run/SCI-402/*`、`:7` 同、`:344/:350` `run/SCI-SNR-01 §4.2`、`:350` `run/PERF-401` 全部不在树内；`:285` 附录 A 列 `docs/EXP-01-…` 未带单元前缀 | **有（文件内自相矛盾，两值不可同真）**：`:16` "HST 高对比结构上帧级标量最好（**0.0485** vs sparse **0.0994** / dense **0.0896**）" ↔ `:122` 表与 `:131` "帧级标量最好（**0.0538** vs sparse **0.1174** / dense **0.0940**）" ↔ `results/b3_domain_map.json` `gates.delta64_detail.hst_m16.at_delta64` 实测 `frame_median.rmse=0.0538 / sparse=0.1174 / dense=0.0940` ⇒ `:16` 为修正前值未清理；另 `:98` "95% CI 覆盖 **24/26**" ↔ `b1_sky_scan.json` `gates_bright.G3_def_in_mc_ci95_frac=0.9230769230769231`（=12/13）+ `gates_faint=1.0`（13/13）= **25/26** | 保留（单元报告）＋三项订正：删开头元信息块改正文一句、清 `:16`/`:98` 两个陈旧数值、把 §9 审稿台账移 `results/REVIEW.md`；附录 A 清单与实际目录对齐（标准 01 §4 写法 + §2 佐证层） |

补强证据（`README.md` 附录 A 与实际单元内容不符）：附录 A（`:246-:286`）列 13 个 code 文件 + 8 个 results + 1 个 docs；实际 `实验/absolute-snr/` 另有 `code/{b7_*,exp02,exp03,exp04,exp05,exp06,reverse_verify}`、`docs/EXP-02…EXP-06 + frame-snr-canon.md + snr-propagation-design.md + f-instr-canon.md`、`results/{exp02..exp06_*,EXP0*_TABLES.md,REVERSE_VERIFY_CANON.md}`（`git ls-files 实验/absolute-snr` = 196 件）⇒ 自述清单覆盖率不足一半。

### 1.16 实验/absolute-snr/REPORT_paper.md

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 实验/absolute-snr/REPORT_paper.md | 跨帧绝对 SNR 传递链（创新点二）精读报告 | 533 | 研究与佐证（负责人面精读报告）；位置在实验单元内，属佐证层 | SCI-B 实验单元的逐项精读、D1 修复闭环、与正式科学文档的一致性核对 | **缺**（`:3-:7` 开头块为"报告性质/对象/纪律声明/版本锚/一句话结论"，无最高设计条款指针；条款号迟至 `:13` 摘要与 `:32` §1.1 才出现） | 6（`工程控制/RELEASE-05/{ACCEPTANCE.md,SUMMARY.md,TASK_LIST.md,tasks/SCI-504.md}`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md`、`eng/ci/ledgers/naming_surface.json`） | 否（与 `README.md` 同主题，本报告为其复核件） | 与 `实验/absolute-snr/README.md` §3 结果节大面积重叠（举证 Q）；与 `docs/science/CONTROL_WEIGHT_SNR.md` §2b/§8a 重叠（举证 O） | **有**：`:3-:7` 五行块，含"纪律声明""版本锚" | `:3` "SCI-504 执行子代理产出"；`:6` "**版本锚**：… 生产修复 commit `cdcc0975`（SCI-501 / FIX-407，RELEASE-05）"；`:227` 同 commit 复述；`:314` "该订正已落 NOISE_MODEL.md §5a 与 §9a（本轮核对…）"；`:381` "并发 FIX-405 … 该并发红已消除"；`:392` 结论内复述 commit；`:435-:440` 表 A-0 六行 `generated_at` 时间戳；`:249` "**（本轮新增的独立复核…）**"；`:274` "已改名" | `:4` 断言 `docs/` 中存在旧路径名 `实验/SCI-B`（实测 `git grep -n "实验/SCI-B" -- docs` **0 命中**，全仓仅 `testdata/README.md:74` 一处）⇒ 该条自身失实；`:464/:528` `build/eng/tests/unit/p1noise/p1snr_science_test`（本审查禁构建，树内无 `build/`）；`:363` 断言 `CONTROL_WEIGHT_SNR.md §8b` 仍写 `N1_flat_field_sparse_never_wins`（实测该符号在 `docs/` 零命中，`:218` 现写 `DEGENERATE_flat_field_ranking_never_true`）⇒ 过时登记；`:366` C7 断言 `PSF_SIGNAL_WEIGHT §7a-1` 缺单位标注（实测 `:128` 已含"该式以 e⁻ 为单位…等价 ADU 口径见 07_noise_snr.md §4.2a"）⇒ 已被上游消解、仍挂"待办" | **有，登记为已解决但冲突仍在**：`:361` C2 正确指出正本 `docs/science/PSF_SIGNAL_WEIGHT.md:131` 的 `+0.301 dex` 与 `b3_domain_map.json` 实测 `+0.2626`（Δ=256）不符，本报告"采用 JSON 值"，但**正本那一行至今未订正** ⇒ 报告与正本对同一物理量给出两个数、报告未回写（见 §2 P5） | 保留（内容质量为本批最高），订正三项：开头块改正文条款指针、删 `:6/:227/:392` 的 commit 与任务编号、把 §5.3 的 C1/C4/C7 三条**失效登记**撤下并只留 C2/C3/C5/C6 |

举证 Q（同一实验单元两份报告对同一测量给两值）

- `实验/absolute-snr/README.md:16`：`HST 高对比结构上帧级标量最好（0.0485 vs sparse 0.0994 / dense 0.0896），且 cell 稳健 MAD 偏差随 Δ 从 +0.029 增到 +0.301 dex`
- `实验/absolute-snr/REPORT_paper.md:264`：`| HST M16（高对比） | 32.3 | 0.1131 | 0.0940 | 0.1174 (0.1152) | **0.0538** | 16 |`
- 真值侧：`results/b3_domain_map.json` → `dense 0.0940 / sparse 0.1174 / frame_median 0.0538`，sparse 臂 `level_bias_dex`：Δ=32 `0.02967`、Δ=128 `0.13769`、Δ=256 `0.26263` ⇒ `README.md:16` 的 0.0485/0.0994/0.0896 与 `+0.301` 均不可复算；`REPORT_paper.md:272` 与 `:361` 采用 JSON 值并注明 `+0.301`/`+0.127` 无法复现。

正面核验（本件可采信的部分，逐条跑过）：`results/REVIEW.md` 实为 **239 行**（与 `:5`/`:424` 自述一致）；`cdcc0975`、`0d32c07d…` 均为真实提交（`git log -1`）；`docs/science/NOISE_MODEL.md:99-101` 确含 `1.44/√N` 的相对/dex 单位订正（`:314` 的"已落"成立，但 `:20`/`:156` 引用的锚写作 `NOISE_MODEL.md:86` —— 该行是 `### 5a 掩膜半径的物理导出（MASK-002）` 标题，锚漂 13 行）。

<!-- PROGRESS: 已审 16/16 -->

---

## 2. 同主题组与差异（含"两篇打架"具体对）

本批四组同主题：① SNR/权重口径（研究包 + 实验单元 + `docs/science/*`）；② 追溯合同（SPEC + LAYERS + MATRIX 双视图 + `docs/TRACEABILITY.csv`）；③ V6 验证档案（`docs/validation/v6/*` 六件）；④ 冻结状态（`docs/validation/SCIENCE_FREEZE.md` ↔ `ACCEPTANCE_SPEC.md` ↔ `ASTROCS_DESIGN` §12.5）。

### 2.1 相互冲突的口径对（穷举，均给两侧 `路径:行`）

| 编号 | 同一物理量/机制 | A 侧 | B 侧 | 谁与正本一致 | 状态 |
|---|---|---|---|---|---|
| P1 | `snr = 1.0` 的语义（派单点名的已知例，本批复算确认为真） | `docs/GLOSSARY.md:15`「snr=1.0 一律按 unknown 显式标注」 | `docs/science/CONTROL_WEIGHT_SNR.md:153`「**snr=1.0 的语义面 = 合法测量值**」、`:168`「`snr=1.0` 不作为缺失标记」、`:175`「不可接受变化：以 snr=1.0 替代缺失回退」 | B；第三/四方佐证同向：`docs/algorithms/PHASE2_SAMPLER.md:398`「该档标记 = 0；1.0 只属可用档」、`docs/contracts/DATA_SEMANTICS.md:1762`、`docs/contracts/PUBLIC_API.md:1562` | **未解决**（GLOSSARY 自证锚 `CONTROL_WEIGHT_SNR.md#42` 指向 `:42` 的 `quality_weight` 符号行，不含该口径 ⇒ 锚错 + 口径反） |
| P2 | 生产 AUTO 排异路由档表（N = 几何覆盖帧数） | `docs/validation/SCIENCE_FREEZE.md:18-19`「三档：1≤N≤3 none / 4≤N≤5 percentile / N≥6 winsorized」 | `docs/traceability/TRACEABILITY_MATRIX.csv:17` 与 `.json:396`「五档…6≤N≤15 winsorized / **N≥16 linear fit**」并署"按 `ASTROCS_DESIGN.md` §5.5" | A；正本 `docs/plugins/algorithms_phase2/12_rejection.md` §9 为三档，且 `ASTROCS_DESIGN.md:420` 明文"本节不复制档界与取值" ⇒ B 侧的引用出处不存在 | **未解决**（B 侧含 CSV/JSON 双视图，改一处即漂移） |
| P3 | `psfsw_robust` 是否生产模式 | `docs/validation/v6/BASELINE_COMPARISON_MATRIX.md:31`（`class=production`）、`docs/validation/v6/QA_MATRIX.md:44`「生产 = point_information \| surface_gls \| psfsw_robust」、机器源 `artifacts/evidence/v6/qa-design/qa_matrix.json` `production_modes` | `docs/science/PSF_SIGNAL_WEIGHT.md:70-74` §4「单一权重口径…没有可选择的口径」、`docs/contracts/DATA_SEMANTICS.md:3259`「`psfsw_robust` = 禁用 token，接受面 = 空」、`:3262`「该字段不存在任何合法取值」 | B | **未解决且更深**：合同机器登记 `eng/contracts/data/v6_clause_registry_v1.json` 内 `weight_modes.production` 已是两值 + `retired=[psfsw_robust]`，而同一文件 `clauses[]` 的 `FZ-MODE-PRODUCTION` 仍 `status=FROZEN`、`value="point_information \| surface_gls \| psfsw_robust"` ⇒ 机器登记自身两处相反，`docs/validation/v6/README.md:19-21` 又把该文件指定为条款 id 的"现行机器登记表"。受影响门：44 门中 12 门含 psfsw（`G-ANA-02/06/07`、`G-MC-04/05`、`G-INJ-03/04/05/06`、`G-BASE-01/03/05`） |
| P4 | 权重模式/档位概念（A44 作废面） | `docs/research/SNR_WEIGHT_RESEARCH_PACK.md:26`「仅显式 `weight_mode=psfsw_robust` 使用，四分量独立存储」 | 同文件 `:141`「本项目不产出该权重对象」＋ `:1` 作废横幅 | B（同文件后半已订正） | **半解决**：`:26` 正文保留原样仅加行内括注，同一文件两处口径并存 |
| P5 | HST cell 稳健 MAD 偏差随 Δ（本批实测数字冲突） | 正本 `docs/science/PSF_SIGNAL_WEIGHT.md:131`「+0.029 → +0.301 dex」；`实验/absolute-snr/README.md:16/:131` 同值（含 `+0.127 @128`） | 证据 `实验/absolute-snr/results/b3_domain_map.json`（sparse `level_bias_dex`：Δ=32 0.02967 / Δ=128 0.13769 / Δ=256 **0.26263**）；`REPORT_paper.md:361` 已判定 `+0.301`/`+0.127` 不可复现 | B | **未解决**：报告侧采 JSON，正本那一行未回写 ⇒ 佐证与正本两个数（按派单关注点 2：吸收不完全） |
| P6 | 同一单元两份报告对 HST 三口径 RMSE 取值 | `实验/absolute-snr/README.md:16`（0.0485 / 0.0994 / 0.0896） | `实验/absolute-snr/README.md:122/:131` 与 `REPORT_paper.md:264`（0.0538 / 0.1174 / 0.0940）＋ JSON 同 B | B | **未解决**（同一文件 `:16` 与 `:131` 两个数并存） |
| P7 | 95% CI 覆盖点数 | `实验/absolute-snr/README.md:98`「24/26」 | `b1_sky_scan.json`（`gates_bright=0.923077`→12/13、`gates_faint=1.0`→13/13 ⇒ **25/26**）；`REPORT_paper.md:197` 采 25/26 | B | 未解决（陈旧值在报告首屏结论里） |
| P8 | 恒真门的处置措辞 | `ACCEPTANCE_SPEC.md:63`「恒真门**不出现**」 | `ACCEPTANCE_SPEC.md:99`「恒真门不计证据」＋ `docs/science/CONTROL_WEIGHT_SNR.md:218`、`实验/absolute-snr/README.md:171` 实际做法 = 识别+改名+移出计数 | B（与执行一致） | 未解决；由 `REPORT_paper.md:365`（C6）登记后未订正。**别批路径**：`ACCEPTANCE_SPEC.md`（根） |
| P9 | `sigma_sky_source` 缺失/不一致是否 fail-closed | `docs/plugins/algorithms_phase1/07_noise_snr.md:122`「声明与实际来源不一致 ⇒ **fail-closed 拒绝**」、`:240`「缺失即评审判红」、`docs/science/NOISE_MODEL.md:324` 同向 | 同文件 `:241`「`SNR_SIGMA_SKY_UNSPECIFIED=0` 保留为兼容缺省（与 `SHOT_ONLY` **逐位一致**，由向后兼容锁固定）」＋ `docs/science/CONTROL_WEIGHT_SNR.md:201`「legacy 缺省与双计臂逐位一致（向后兼容）」 | 无一致侧：规范要红、实现锁兼容 | **未解决**；由 `REPORT_paper.md:364`（C5）与 `实验/absolute-snr/README.md:157` 登记为未落地条款。**别批路径**：`07_noise_snr.md`、`NOISE_MODEL.md`、`CONTROL_WEIGHT_SNR.md` |
| P10 | 追溯层数 | `docs/traceability/TRACEABILITY_SPEC.md:31`「展成 **8 个必填层**」、`:138`「每行 8 层全部显式」 | 同文件 `:36-:44` 表 **9 行**、`:42` SRC 独立成层；`docs/traceability/TRACEABILITY_LAYERS.csv:2-:10` 9 层；矩阵每行 9 组列 | B（表与数据） | 未解决（同一合同两个层数） |
| P11 | 追溯 ID 大小写规则 | `docs/traceability/TRACEABILITY_SPEC.md:70`「ID 一律 ASCII 大写」＋ `:78` 正则 `^MOD-[A-Z0-9]+…$` | 同文件 `:78` 示例 `MOD-astrocs-phase1-calibration` 与矩阵 30/30 行 `module_id`（小写） | 无法两全：正则与全部数据互斥 | 未解决（机械可判：按 SPEC 自带正则 30 行全违规） |
| P12 | Siril 权重公式的开源锚（版本 + 行号） | `docs/research/SNR_WEIGHT_RESEARCH_PACK.md:51`「`median_and_mean.c:1111-1230`」、`:171/:197`「Siril **1.4.4** `:1091-1094`」 | `实验/absolute-snr/README.md:221` 与 `REPORT_paper.md:412`「Siril **1.2.4** `median_and_mean.c:868-870`」 | 本机快照 `siril-1.4.3/src/stacking/median_and_mean.c:1092` = `pweights[layer][i] = 1.f /` ⇒ **行号侧支持 A，版本号两侧都不符（1.4.3）** | 未解决（同一引用两说，且无一对上本地快照） |
| P13 | V6 三模式与信息权重期间的 SCI 冲突（既有登记，本批可核） | `docs/validation/v6/QA_MATRIX.md:30-38` §1「全部判据以下列上位权威为语义源，**原样继承**」（含已废止宪章 `:32`、不存在的 `CONTROLLER_LOG.md` `:38`） | 现行权威 `docs/science/{PSF_SIGNAL_WEIGHT,INTEGRATION,REJECTION}.md` 与 `docs/contracts/DATA_SEMANTICS.md` §31 | B | 未解决；该冲突当年即由 `artifacts/evidence/v6/review-audit/04_覆盖缺口与文档冲突.md:11` 记为「`DRIZZLE.md`、`CONTROL_WEIGHT_SNR.md`、`INTEGRATION.md` 仍是**与目标冲突但形式上 FROZEN** 的 L1 文档」。**别批路径**：`artifacts/evidence/v6/review-audit/04_*.md` |

层级倒置判定（派单关注点 2 的总结论）：**方向是"佐证被正本吸收"而非"实现按研究包做"**——`docs/science/CONTROL_WEIGHT_SNR.md` §2b/§8a/§8b、`docs/science/PSF_SIGNAL_WEIGHT.md` §7a、`docs/science/NOISE_MODEL.md` §9a、`docs/plugins/algorithms_phase1/07_noise_snr.md` §4.2a、`ASTROCS_DESIGN.md` §5.3 已吸收实验单元二（SCI-B）与 `07_noise_snr` 的结论；生产实现面由 `docs/ci/01_CHECKS.md:100`（CHK-NO-WEIGHT-MODE-CODE）反向锁"无模式选择"。**倒置发生在 V6 验证档案层**：`docs/validation/v6/*` 六件 + 其机器源仍以 `ACTIVE_NORMATIVE` 在册、并把 psfsw 列为生产模式，属"下层档案承载与上位相反口径且自设第二套权威顺序"（标准 01 §1 末条 + §2 第二条）。吸收的**遗漏项**：P5/P6/P7 三处数值未回写、B7 结论被吸收但证据锚未随之更新（举证 O）。

## 3. 索引与链接缺陷清单

### 3.1 与 `docs/DOCUMENT_INDEX.yaml` 的登记不符

| 问题类型 | 条目 | 说明与命令 |
|---|---|---|
| 结构缺陷（影响全批） | `doc_index` 全部 265 条目 | 条目字段只有 `path / status / duty / upstream / downstream / notes`，**无 `role`（角色）与 `主题` 两列**；标准 01 §3 要求"登记内容含路径、角色、主题、上游、下游"。命令：`yaml.safe_load(...) → sorted(entries[0].keys())` |
| 未登记 | `实验/absolute-snr/B7_ABSOLUTE_SNR_RECON.md`、`实验/absolute-snr/README.md`、`实验/absolute-snr/REPORT_paper.md`（本批 3 件） | `git grep` 在 `DOCUMENT_INDEX.yaml` 内检索 `实验` 作为 `path:` 前缀 → **0 命中**；且 `doc_index.coverage` 自述只覆盖 `docs/**` + 仓库根治理文档 ⇒ 全仓 `实验/` 62 份 `.md`、`工程控制/` 31 份 `.md` 均在唯一索引地图之外（标准 01 §3"现存文档全部登记…未登记即缺陷"） |
| 未登记（别批） | `docs/algorithms/anchors/unresolved_registry.json`、`docs/research/CCD_LINEAR_DEFECT_LITERATURE.md`、`docs/DOCUMENT_INDEX.yaml` 自身 | 命令：`git ls-files docs` 与索引 `path:` 集取差 → 3 件；`docs/` 258 个跟踪件里 255 在册 |
| 登记了但文件不存在 | 无 | 同取差的正向：255 个 `docs/` 登记项全部存在（`os.path.exists` 全 True） |
| 状态标注与文档实际相反 | `docs/research/SNR_WEIGHT_RESEARCH_PACK.md` = `ACTIVE_NORMATIVE` | 该文件 `:1` 自述"不构成现行规范…仅作留痕"⇒ 应为 INFORMATIVE/档案；索引把它列为现行规范件 |
| 状态标注与文档实际相反 | `docs/validation/v6/{BASELINE_COMPARISON_MATRIX,NEGATIVE_MUTATION_CATALOG,ORACLE_AND_ZERO_CASE_POLICY,P0_GATE_FAMILY,QA_MATRIX,README}.md` 全 6 件 = `ACTIVE_NORMATIVE` | 其中 4 件开头自述"历史/冻结层、不构成现行规范"，6 件的机器源路径整体已出库到 `artifacts/evidence/v6/`（`README.md:22-25` 自述"整体出库"）⇒ 应为档案（`doc_index.archived` 现为空表，0 条） |
| 角色/归属标注不符 | `docs/traceability/TRACEABILITY_LAYERS.csv`、`TRACEABILITY_MATRIX.csv`、`TRACEABILITY_MATRIX.json`、`traceability_warn_baseline.json` 的 `duty` | 四处 `duty` 被写成文件路径本身（`:1451/:1457/:1463/:1475`），无职责描述；本判定依次为：合同说明 / 合同说明（派生视图）/ 合同说明（正本）/ 过程台账 |
| 索引自身写法违标准 01 §4 | `doc_index` 顶层 `authoring_task: DOC-404`、`base_main_sha`；`notes` 内 "DOC-001 机器追溯合同…" | 索引文件本身带任务编号与 commit 锚（**别批路径**，登记供合并台账） |

### 3.2 悬空引用逐条（本批 16 件内）

| 文件:行 | 原文锚 | 确认命令与结果 |
|---|---|---|
| `docs/research/SNR_WEIGHT_RESEARCH_PACK.md:105` | `与 \`07_noise_snr.md\`、\`UNIFIED_MODEL.md\` 口径一致` | `find . -name "UNIFIED_MODEL*"` → 仅 `docs/design/UNIFIED_MODEL.md`；`docs/science/UNIFIED_MODEL.md` 不存在；另有同族异名 `docs/science/UNIFIED_SCIENCE_MODEL.md` ⇒ 裸名双候选 |
| `docs/research/SNR_WEIGHT_RESEARCH_PACK.md:213-229` | `run/DOC-404/{evidence,check_pack_oss_anchors.py,verify_*.py}` | `[ -d run/DOC-404 ]` → NO；`git ls-files run` → 仅 `run/.gitkeep` |
| `docs/traceability/TRACEABILITY_SPEC.md:21` | `schemas/traceability_matrix.schema.json` | `[ -e schemas/… ]` → NO；实件 `eng/contracts/schemas/traceability_matrix.schema.json` 存在 |
| `docs/traceability/TRACEABILITY_SPEC.md:94` | "第 5 节给出两表关系" | §5（`:120-126`）为"SOURCE SYMBOL 表达"，无两表关系 |
| `docs/traceability/TRACEABILITY_SPEC.md:110` | 证据面 `returns/`、`TASK_STATE evidence_refs` | `git ls-files \| grep -c '^returns/'` → 0；`find . -maxdepth 2 -name "TASK_STATE*"` → 空 |
| `docs/traceability/TRACEABILITY_MATRIX.{json:32,json:84}`（CSV 同行 `:3`、`:30`） | `returns/BLD-003`、`returns/CPU-001`（均记 `evidence_status=VERIFIED`） | `returns/` 不存在（同上命令）⇒ 两行 VERIFIED 无落点 |
| `docs/validation/v6/{README.md:16-17,27; QA_MATRIX.md:9,101-105; BASELINE…:7; NEGATIVE…:8-10; ORACLE…:7-8}` | `reports/v6/qa-design/**` | `ls reports/` → 仅 `v19r2`；`[ -e reports/v6 ]` → NO；实件全部在 `artifacts/evidence/v6/qa-design/`（`qa_matrix.json`/`case_ledger.json`/`data/mutations.json`/`data/baseline_matrix.json`/`oracle/*.py`/`SUMMARY.md`/`evidence/`）⇒ 共 5 件文档、≥12 处路径前缀失效 |
| `docs/validation/v6/QA_MATRIX.md:32` | 冻结宪章 `ASTROCS_PROJECT_CONSTITUTION-001 §4.1/§5.3/§6.3/§14.2` | `find . -iname "*CONSTITUTION*"` → 0 文件；本文件 `:3` 自述其已废止 |
| `docs/validation/v6/QA_MATRIX.md:38` | `CONTROLLER_LOG.md` C-004.1/2/3 | `find . -name "CONTROLLER_LOG*"` → 0 |
| `docs/validation/v6/QA_MATRIX.md:250,850,630` | `FREEZE_LIST §1/§3.2/§7`、`testdata/index.json v1.2` | `find . -name "FREEZE_LIST*"` → 0；`testdata/index.json` 存在（该条不悬空） |
| `docs/validation/v6/P0_GATE_FAMILY.md:9-10` | `reports/v6/review-audit/03_…`、`05_…`、`SCI-ADJ-001_CONFLICT_MATRIX.md §5` | `reports/v6` 不存在（实件 `artifacts/evidence/v6/review-audit/03_缺陷账本_重开清单.md`、`05_未决风险与控制器裁决事项.md`）；`find . -iname "*CONFLICT_MATRIX*"` → 0 |
| `docs/validation/v6/QA_MATRIX.md:835,842` | `DEFECT_LEDGER.json`（只读输入） | `find . -name "DEFECT_LEDGER*"` → 0 |
| `docs/validation/SCIENCE_FREEZE.md:5` | `ACCEPTANCE_GATES.md` G1-G10 | `find . -iname "*ACCEPTANCE*"` → 无该名文件（实为 `ACCEPTANCE_SPEC.md`）；`git grep -ln ACCEPTANCE_GATES` → 仅本件 + 一份证据 CSV |
| `docs/traceability/traceability_warn_baseline.json:5` | "附录 H.6" | `git grep -n "附录 H" -- docs` → 仅命中本行自身 |
| `实验/absolute-snr/README.md:333` | `docs/EXP-01-DELTA-AND-ESTIMATOR.md` | 根 `docs/` 无此件；实件 `实验/absolute-snr/docs/EXP-01-DELTA-AND-ESTIMATOR.md` |
| `实验/absolute-snr/README.md:7,186-187,226,344,350` | `run/SCI-402/`、`run/RELEASE-02/实验/E11-…`、`run/SCI-SNR-01 §4.2`、`run/PERF-401` | `ls run/` → 无 `SCI-402/RELEASE-02/SCI-SNR-01/PERF-401`；`git ls-files run` → 仅 `.gitkeep` |
| `实验/absolute-snr/B7_ABSOLUTE_SNR_RECON.md:3` | `run/SCI-RECON-SNR-01/REPORT.md` | 同上（`run/` 不入库，完整报告无入库载体） |
| `实验/absolute-snr/REPORT_paper.md:4,360` | 断言 `docs/` 内有旧路径引用 `实验/SCI-B` | `git grep -n "实验/SCI-B" -- docs` → **0 命中**（全仓唯一命中在 `testdata/README.md:74`）⇒ 该条为失实登记，非悬空链接 |

## 4. 覆盖率自报

- 已审 **16 / 16**（清单 `独立审计/批次清单/DB-13.txt` 全部 16 行逐行通读全文，含最长三件：`TRACEABILITY_MATRIX.json` 788 行、`QA_MATRIX.md` 873 行、`REPORT_paper.md` 533 行）。
- **未逐行读名单：空**。
- 派单与本批清单的差异（按派单 §3 以清单为准）：派单文字提到 `docs/references/`，清单内该目录 **0 件**（实际存在 `docs/references/SCIENTIFIC_REFERENCES.md` 与 `PHOTOMETRY_LITERATURE_REVIEW_ARCHIVE.md`，均不在本批）；派单点名的 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md` ✓ 在清单第 1 行；清单另有 3 件位于 `实验/absolute-snr/`，派单文字未逐一点名——按"清单是唯一范围依据"照审。
- 为判定上下游所读的**非本批**文件（只读、不下结论）：`ASTROCS_DESIGN.md`、`ACCEPTANCE_SPEC.md`、`docs/GLOSSARY.md`、`docs/science/{PSF_SIGNAL_WEIGHT,CONTROL_WEIGHT_SNR,NOISE_MODEL}.md`、`docs/plugins/algorithms_phase1/07_noise_snr.md`、`docs/plugins/algorithms_phase2/12_rejection.md`、`docs/contracts/{DATA_SEMANTICS,INDEX.yaml}`、`docs/validation/v6/*`（互引）、`eng/contracts/data/v6_clause_registry_v1.json`、`artifacts/evidence/v6/**`、`实验/absolute-snr/{results,docs}/**`、`docs/TRACEABILITY.csv`、`siril-1.4.3/src/stacking/median_and_mean.c`、`lib/algorithms/coverage/src/rejection.cpp`（仅用于判"哪一侧与实现一致"）。

## 5. 我证伪（修正）了任务书的哪个前提

| 前提（AUD-101 任务书 / 派单） | 实测 | 命令口径 |
|---|---|---|
| "`docs/` 为重点（当前**约 233 份**、**30+ 子目录**）" | **233 成立**，口径须钉死为 `docs/` 下的 `.md` 跟踪件数（`git ls-files 'docs/*.md' \| wc -l` → 233）。同一目录口径补一句：`docs/` 全部跟踪件 = 258，**另有 25 份非 `.md` 机器文档**（本批的 4 份追溯 CSV/JSON 与 `docs/TRACEABILITY.csv` 等皆在此列），任务书未声明该列是否计入。"30+ 子目录" **不成立**：`docs/` 一级子目录实测 **23** 个（任意深度 36 个），任务书未标口径 ⇒ 该数按一级口径被证伪 | 同上；`ls -d docs/*/ \| wc -l` → 23；`git ls-files docs \| wc -l` → 258 |
| 派单关注点 3："traceability 映射能否双向走通" | **正向可走、反向不可走**（不是部分断链，是缺规则）：正向 10/10 行的文件与符号锚解析成功；反向两类失败——① 53 个 `VERIFIED` 层 ID 在 SPEC:88 指定的合同注册表 `docs/contracts/INDEX.yaml` 中零命中；② `docs/TRACEABILITY.csv`（67 行细粒度账）与本矩阵**无可连接键**（`module` 列取值加 `MOD-` 前缀后命中 0、`algorithm_id` 列命中 0），而 SPEC:94 承诺"第 5 节给出两表关系"，§5 实为 SOURCE SYMBOL | 见 1.4 举证 F；`python -X utf8 -c` 集合交 |
| 派单关注点 1（已知一例 snr=1.0 两篇相反） | **成立且定位到行**（P1），并新查出 12 组同类"两篇打架"（P2–P13），其中 4 组为**同一文件内部自相矛盾**（P4/P6/P10/P11） | 全部给 `路径:行` 双侧 |
| 派单关注点 2（研究包结论是否被正本吸收） | 主方向为"已吸收"，但存在**吸收不完整**：B7 的四项结论进入 `docs/science/CONTROL_WEIGHT_SNR.md` §2b 却不在其自述证据锚（§8a/§8b 只列 b1–b6）之内；P5/P6/P7 三个陈旧数值仍留在正本与单元报告首屏 | 举证 O/Q |
| 未证伪 | 派单点名"16 份"与"含 `SNR_WEIGHT_RESEARCH_PACK.md`"两项与清单一致，未见不符 | `Read inventory\DB-13.txt` |

## 6. 机械线索文件的校准结果（派单 §5.4）

| 件 | 机械列 | 实测 | 判定 |
|---|---|---|---|
| `docs/research/SNR_WEIGHT_RESEARCH_PACK.md` | `meta_block=False`、`task_ids=[]`、`dangling_links=[]`、`commit_sha=[00401706,01621459,10476408,10489171,1996164]` | 首行即作废横幅；`DOC-404` 命中 13 行；`UNIFIED_MODEL.md` 与 `run/DOC-404/**` 悬空；五个"sha"全是 DOI 片段，真 commit `08b8eb85ae17`（`:121`）未列 | 漏报 3 项（元信息块/任务编号/悬空），误报 5 项（DOI→sha） |
| `docs/traceability/TRACEABILITY_SPEC.md` | `dangling_links=[docs/modules/registry/astrocs.phase*.md]` | 该行是通配写法，26 个实页全存在 | 误报 1 项；**漏报** `:21` schema 路径、`:94` 小节指针、`:110` `returns/` |
| `docs/traceability/TRACEABILITY_MATRIX.csv` | `upstream_pointer=True`、`sha256=True` | 首行是列名、无上游块；`sha256` 只作为词出现在 notes | 均误报；**漏报** `notes` 内 29 行任务编号 |
| `docs/traceability/TRACEABILITY_MATRIX.json` | `meta_block=False` | `:2-:6` 五字段头部块（含 `base_main_sha`） | 误报；**漏报** `test_path` 4 处散文锚、`returns/**` 2 处 |
| `docs/validation/SCIENCE_FREEZE.md` | `commit_sha=[712cc7c3]`、`dates=[17]` | `712cc7c3` 是"包 sha1"非 commit；`:17` 日期判定正确 | 半误报 1 项；**漏报** `:5` `ACCEPTANCE_GATES.md` 悬空、`:1` 标题版本锚 |
| `docs/validation/v6/QA_MATRIX.md` | `task_ids=[AUDIT-001]`、`dates=[3]`、`dangling_links` 未列全 | 44 门行内 Owner 列全为 `ALG-*/IMPL-*/W*` 编号（49 行命中），另有 5 个 `reports/v6/**` 与 3 个不存在文件 | **严重漏报**（任务编号 1→49 行；悬空 0→8 类） |

结论：机械列对 **CSV/JSON 载体件与渲染块内嵌编号**系统性失效（其扫描面按 Markdown 结构），对"以 `..` 开头未判"的说明亦不足以覆盖本批的 `reports/v6/**`（非 `..` 开头但已出库）这一类。建议整治时把 `dangling_links` 的判定改为"对任何形似路径的 token 做存在性解析"，并对 `meta_block` 加"首行横幅/作废声明"识别。

## 7. 收工工作树核对

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工输出**逐字相同**（两项，均为开工前既有）。本批取证动作未污染被审对象：未写入仓库任何路径、未生成 `__pycache__`、未创建临时件、未执行构建/测试/检查器。

## 8. 本批交付要点（一句话每条）

1. 派单点名的 `snr = 1.0` 两篇相反已定位到行并找出第三、四方同向证据，错的一侧是 `docs/GLOSSARY.md:15`，且其自证锚指错行（P1）。
2. 同类"两篇打架"另查出 12 组（P2–P13），其中 5 组是本批文件**自身内部**相互否证（研究包 `:26` vs `:141`、traceability SPEC 层数与 ID 规则、实验 README `:16` vs `:131`）。
3. 最重的一条不是措辞问题而是合同级矛盾：`psfsw_robust` 在 V6 验证档案与其机器源里仍是**生产模式**，而科学正本与合同登记判它"接受面 = 空"，且合同登记文件内部（`weight_modes` vs `clauses[FZ-MODE-PRODUCTION]`）就自相矛盾 ⇒ 需负责人裁决（P3）。
4. 追溯映射正向 10/10 走得通、反向走不通：53 个 VERIFIED ID 不在合同注册表、细粒度账 `docs/TRACEABILITY.csv` 与模块矩阵无可连接键、SPEC 承诺的"两表关系"小节内容不存在。
5. V6 六份文档的机器源整体出库到 `artifacts/evidence/v6/qa-design/`，文档侧全部仍指 `reports/v6/**`（≥12 处失效路径），另有 8 个被引用文件全仓不存在（宪章、`CONTROLLER_LOG.md`、`FREEZE_LIST`、`DEFECT_LEDGER.json`、`SCI-ADJ-001_CONFLICT_MATRIX.md`、`ACCEPTANCE_GATES.md`、`returns/**`、`run/DOC-404/**`）。
6. 同一 A44 裁决在六份同目录文档上的作废留痕不一致：3 份贴横幅、3 份没贴，而受影响口径同等（psfsw=production）。
7. 实验单元二的结论确已被科学/插件/最高设计三层正本吸收（无实现层倒置），但 B7 的数值被吸收后其**证据锚未随迁**（正本只列 b1–b6，B7 独有值 `1.36802/0.376/0.1424` 仅在 `results/b7_*.json`），且三处陈旧数值未回写（P5/P6/P7）。
8. 文献引用可疑项 2 条：Siril 权重锚在本批内有两说（1.4.4 `:1091-1094` vs 1.2.4 `:868-870`），本地 `siril-1.4.3` 快照实测行号为 **1092** ⇒ 行号支持前者、版本两说皆不符；另一条为 `实验/absolute-snr` 引 `NOISE_MODEL.md:86`（该 1.44 常数实在 `:99-101`，锚漂 13 行）。
9. `DOCUMENT_INDEX.yaml` 结构性缺列（无 `role`/`主题`）、`实验/` 与 `工程控制/` 全量未登记、6 件 V6 文档与 1 件研究包被标 `ACTIVE_NORMATIVE` 与文档自述相反、`archived` 分表为空。
10. 机械线索文件在本批 6 件上共 **漏报 13 处、误报 11 处**（按 §6 表逐格相加：漏报 3+3+1+2+2+2，误报 5+1+2+2+1+0），主要失效面是 CSV/JSON 载体与渲染块内嵌编号。

<!-- PROGRESS: 已审 16/16（完成） -->
