# AUD-101 · DB-03 文档审计台账（25 份：docs/modules/registry 全批 + 2 份 modules + docs/plugins 11 份）

批次：DB-03　判据：标准 01 §1–§5　基线 HEAD=`c8f64e9a`　节点：独立只读审查节点

开工基线 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与前言 §1 预期两项一致。全程只读（`ls`/`git grep`/`git ls-files`/`Read`/`sha256sum`/`awk`），未执行任何构建、`ctest`、`run_checks.py`、`eng/**` 下检查器或生成器，未 import 仓库内 Python。`sha256sum` 是系统工具、只读文件内容，不写盘。

## 0 派单、清单与机械列的四处不一致（先登记）

引用记法（避免歧义）：`标准 01 §n` 指 `standards/01_文档权威链整治标准.md` 的条款；`§n.m` 单用（如 `§8.4`、`§5.2`、`§4.6`）指仓库根 `ASTROCS_DESIGN.md` 的节；`§1.2-A`、`§3-B`、`§4.2` 指**本台账自身**的小节。行号一律 `路径:行`，冒号后的数字是该文件在冻结基线 `c8f64e9a` 工作树中的实际行号。

1. **派单说 25 份，清单实有 25 行**（`tr -d '\r' < DB-03.txt | grep -c .` = 25），与派单一致；25 个路径全部在树中存在（逐条 `[ -f ]` 复算，零 MISSING）。
2. 清单文件是 **CRLF** 行尾（`file DB-03.txt` = "ASCII text, with CRLF line terminators"）。按行读入不先 `tr -d '\r'` 时，`[ -f ]`、`git grep -F` 一律假阴。本批首轮即触发，已纠正后重跑。
3. 前言 §4 定 14 个字段，`TPL_文档审计台账.md` §1 只有 11 列（缺 `行数`、`写法违规`、`悬空引用`，且把"含日期/编号/历史叙事"并进一列）。按前言执行：每份给全 14 字段，每字段独占一行、字段名与前言列名逐字一致，便于机器按字段名切分合并。
4. 本批高优先项（派单指定："注册说明与机器登记面是否同源"）的结论先行：**registry 不是注册表的生成物，而是一套与注册面并行、且已经与注册面逐项背离的手写副本**。三条硬证据见 §1.13、§4.1、§4.2；本批 12 份 registry `.md` 中只有 3 份真是生成物，其余 9 份是手写副本，而 `docs/DOCUMENT_INDEX.yaml` 把其中 5 份标成 `GENERATED`。

---

## 1 逐份登记

### 1.1 docs/modules/registry/astrocs.phase2.resample.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase2.resample.md` |
| 标题 | `模块 astrocs.phase2.resample`（H1，第 13 行） |
| 行数 | 60（`grep -c ''` 复算；与机械列 lines=60 一致） |
| 角色 | **模块工作细节**（标准 01 §2 表），实际所在目录 `docs/modules/registry/` = 应属目录 `docs/modules/`，**不错放**（registry 是 modules 的注册子层，角色不变） |
| 主题 | Phase2 重采样模块的注册页（descriptor 派生） |
| 上游 | 有：`15: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`。**条款号错**：`ASTROCS_DESIGN.md` 的 §8.4 标题是"顶层结构"（`ASTROCS_DESIGN.md:618`），"模块与 ABI"是 §8.5（`ASTROCS_DESIGN.md:661`）。生成器把该错串写死在模板里（`eng/tools/quality/gen_module_readmes.py:76`），故全批同错，见 §4.3 |
| 下游 | 10 个引用者（命令 `git -C "F:/Astro dev/Astro CS Normalization Database" grep -l -F astrocs.phase2.resample.md -- . \| grep -v -x -F docs/modules/registry/astrocs.phase2.resample.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/contracts/UNIFIED_OBJECTS.md`、`docs/contracts/unified_object_registry.json`、`eng/contracts/data/unified_object_compatibility_map_v1.json`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`eng/tools/doccheck/doc_hygiene_exempt.json`、`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv`、`reports/v19r2/file_audit_inventory.csv`、`reports/v19r2/source_manifest.csv` |
| 是否正本 | **否**（该主题的口径正本在 `docs/algorithms/` 与 `docs/contracts/DATA_SEMANTICS.md`；本页只承载 descriptor 的机械展开，且其结论字段全为 `NOT_VERIFIED`）。作为"注册页"它是唯一实体，但它不承载任何现行设计判断 |
| 重复或重叠对象 | 与同批 `astrocs.phase3.resample.md:19-20`、`astrocs.phase3.properties.md:19-20`、`astrocs.phase3.verify.md:19-20` 逐字相同（`Registry production 模块(唯一源=module_adapters.cpp descriptor)。职责由 / SCI/ALG 合同定义(见链接); 不做 SCI/ALG 之外的扩展。`），§错误（:52）、§内存（:48）、§公共 header（:33-34）四节在四页间逐字相同——这是生成器常量（`gen_module_readmes.py:55-61` 的 `EXEC_NOTE/WORKER_NOTE/ERROR_SECTION/TEMPLATE`），不是三份文档互相抄，属"同一模板 26 份输出"的重叠；页内唯一有信息量的只有端口表（:26-27）与 §descriptor（:38-39） |
| 元信息块 | **有**（YAML frontmatter 1-9 行：`id/version/status/owner/source_commit/upstream/downstream`）+ 生成锚 11 行（含 4 个 sha256）。机械列 `meta_block=False` **漏报**：`doc_mech_scan.py:26` 的 `META_PAT` 只匹配中文"文档 ID/状态：/版本锚/纪律声明"，不认 YAML `---` 块，本批 25 份的 meta_block 列全部为 False，实际 12 份 registry `.md` 全部带 frontmatter |
| 写法违规 | `6: source_commit: 822b9c5391a14cc36979a7c550984f6ce363c713` —— 40 位 commit sha（§4 禁）；`11: ...tool-sha256=4fb971ad... source-sha256=db3a8ed2... evidence-sha256=- body-sha256=5689e6a2...` —— 4 处 sha256（§4 禁）；`44/56:` 正文反复抄写证据源路径与"NOT_VERIFIED（证据源 …absent）"——过程与工具状态叙述进了正文；`13: # 模块 astrocs.phase2.resample` 之上另有 `ownership=docs/DOCUMENT_INDEX.yaml status=GENERATED` 之类纪律声明。机械列 `commit_sha=['822b9c5391a14cc36979a7c550984f6ce363c713']` 命中 ✓、`sha256=True` 命中 ✓、但 `meta_block=False` 漏报（见上一行） |
| 悬空引用 | 1 处指向不存在的文件：`56/44:` `eng/ci/ledgers/module_page_evidence.json`——本机确认命令 `ls -la eng/ci/ledgers/ \| grep -i module` → 无输出；`sha256sum eng/ci/ledgers/module_page_evidence.json` → `No such file or directory`。页内自述该文件 `(absent)`，即**已知缺失仍作为结论源长期挂在页面上**（4 个生成页 + `astrocs.phase3.verify.md:44` 都挂它）。另 `60: 见 docs/KNOWN_LIMITATIONS.md` 存在（157 行）✓、`lib/infrastructure/cli/exit_codes.h` 存在（21 行）✓ |
| 与上位冲突 | **是**：上游条款号错（§8.4≠模块与 ABI，应为 §8.5）；§错误节把"exit 9"归给"最高设计 §6.3"，实际取消与退出码在 §7.2（`ASTROCS_DESIGN.md:508` 表行 `| 9 | 用户取消或超时 |`、`:512` 协作取消流程），§6.3 标题是"投影算法"（`ASTROCS_DESIGN.md:451`） |
| 处置建议 | **保留（占位）+ 就地修上游指针与生成器**：作为 descriptor 的机械镜像可保留（它是唯一"注册页↔descriptor 同源"的实现），但必须先改 `gen_module_readmes.py:76` 的 §8.4→§8.5、`ERROR_SECTION` 的 §6.3→§7.2，并去掉 frontmatter 的 `source_commit`（理由：标准 01 §4"正文无 commit"、§3"每份下层文档标明上游条款"——标错条款号等于没标）。**不得**在证据源缺失（`module_page_evidence.json` 不存在）时继续把它写成结论源 |

### 1.2 docs/modules/registry/astrocs.phase2.sample.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase2.sample.md` |
| 标题 | `模块 astrocs.phase2.sample`（H1:15）；frontmatter `id: MOD-astrocs-phase2-sample`（:3）+ `module_id: astrocs.p2.sampling`（:4）+ `aliases: [astrocs.phase2.sample]`（:5）—— 同一文件对"我叫什么"给了三套值，且 aliases 把**页名本身**登记为本名的别名 |
| 行数 | 171（机械列 171 ✓） |
| 角色 | **模块工作细节**，落位 `docs/modules/registry/` 与应属 `docs/modules/` 一致，**不错放** |
| 主题 | Phase2 控制点采样模块合同页 |
| 上游 | 有：`17: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错，见 §1.1 与 §4.3） |
| 下游 | 13 个引用者（命令同 §1.1，检索词 `astrocs.phase2.sample.md`）：含 `docs/modules/phase2_samp.md`、`lib/algorithms/sampling/{README.md,memory.md,module.yaml}`、`docs/contracts/UNIFIED_OBJECTS.md`、`docs/DOCUMENT_INDEX.yaml`、`eng/contracts/data/unified_object_compatibility_map_v1.json`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` |
| 是否正本 | **是（本页是采样模块合同落的现行手写正本）**，但状态自相矛盾：`13: > 人工内容（MANUAL）：本页无 GENERATED-ANCHOR，非生成器所有` 与 `docs/DOCUMENT_INDEX.yaml:959-964` 的 `status: GENERATED` + `notes: "eng/tools/quality/gen_module_readmes.py 生成"` 直接对立（见 §4.1） |
| 重复或重叠对象 | 与 `docs/modules/phase2_samp.md`（192 行，`41: - 模块页：docs/modules/phase2_samp.md`）构成同一模块的两份模块层页；本页自述职责（`45-54:` 三阶段 background-clean、control estimator 方差 `k_corr×(π/2)×σ_bg²/N_retained`）与 `docs/algorithms/PHASE2_SAMPLER.md`（650 行，权威 ALG-P2-SMP-001）§11 是同一口径的两处陈述：本页 `51-53:` "control estimator 方差权威 = k_corr×(π/2)×σ_bg²/N_retained（:840-842，ALG-UPM-CONTROL-IVAR-001…回退冻结 1.4 :82）"，而它自认权威源在 `169-170:` "ALG：docs/algorithms/PHASE2_SAMPLER.md（ALG-P2-SMP-001）"。模块页重述算法公式 = 标准 01 §1"公式推导下沉到对应下层文档"的反向操作 |
| 元信息块 | **有**（frontmatter 1-11 行，含 `module_id/aliases` 两个模板外字段）；机械列 `meta_block=False` **漏报** |
| 写法违规 | 日期：无（机械列 dates=[] ✓ 正确）；commit sha：`8: source_commit: 5ecc60df2d5021d18be04e0e6359d45b7b125b33`（机械列命中 ✓）；任务编号：`27: 由 P2-XX-INT 对齐`、`34: P2-SAMP-IMPL`、`36: 按 lib/algorithms/integration/→lib/algorithms/coverage/hips_p2/ 先例新建`、`139: 整改归 P2-SAMP-IMPL/TEST`、`135-139: DISP-P2SMP-001..005` —— 机械列 `task_ids=[]` **漏报**（`doc_mech_scan.py:22` 的 `TASKID_PAT` 要求 `X-<数字>`，仓内实际编号族 `P2-SAMP-DOC/P2-XX-INT/DISP-P2SMP-00x/TEST-P2-SMP-DESIGN-001` 全不匹配；且该列只扫头 40 行 + 尾 30 行，中段一律不判）；历史叙事：`19-21: > 本页自 gen_module_readmes 派生页事实修订 / > 为手写合同页（手写 registry 先例 astrocs.phase2.reject.md/…）`、`158-160: 本页旧派生内容（"错误码=ACS_ERR_*"、"取消=host cancel 回调"等 session 层词汇）以本合同页…为准修订` —— "旧内容/事实修订/先例"三类历史叙事全部命中 §4 禁条 |
| 悬空引用 | 见 §1.2-A：`26: module_adapters.cpp:642-654 p2_sample_descriptor` 指向的不是 descriptor（实测该 descriptor 在 `lib/infrastructure/scheduler/src/module_adapters.cpp:1065`，命令 `grep -n 'ModuleDescriptor p2_sample_descriptor()' …`）；其余文件级引用全部存在（`lib/algorithms/sampling/`、`sampler.cpp`、`sampler.h`、`docs/algorithms/PHASE2_SAMPLER.md`、`docs/modules/phase2_samp.md`、`docs/science/PHASE2_UPM.md` 逐条 `[ -f ]` 复算 OK） |
| 与上位冲突 | **是（三处，量化见 §1.2-A）**：端口表单位与 descriptor 相反；两个"签名头行数"自相矛盾且都与现值不符；行锚整体漂移 4–15 行 |
| 处置建议 | **保留为手写正本**（标准 01 §2"同一角色内同一主题只有一份正本"，本页是采样模块在模块层的唯一实质内容），但把 `status: GENERATED` 从 `docs/DOCUMENT_INDEX.yaml:960` 改为手写状态、删除与 `docs/algorithms/PHASE2_SAMPLER.md` 重复的公式与行锚段（理由：§1"公式推导…下沉"＋§4"公式、数值表、源码行号放对应下层文档"；本页保留身份/端口/边界，公式只留指针） |

#### 1.2-A 与注册面逐项比对（本批高优先项）

- **单位相反（同一条目两侧值）**：`68: | `samples` | `DATA-P2-SMP` | 可 | `UnitId::ADU` | …` 对 `module_adapters.cpp:1074: {"samples", "DATA-P2-SMP", false, UnitId::SURFACE_BRIGHTNESS, CoordinateFrame::PIXEL},` —— 同一 descriptor 的第二个端口，页面抄的是 ADU，注册面是 SURFACE_BRIGHTNESS。第一端口 `coverage/DATA-P2-COV/DIMENSIONLESS`（:67）对 descriptor :1073 ✓ 一致。即**表内一行对、一行错**，说明它是抄件而非派生件。
- **同一文件行数两个值**：`38-40: 生产源：lib/algorithms/coverage/src/sampler.cpp（1536 行…）+ 签名头正本 lib/algorithms/coverage/include/astro/phase2/sampler.h（288 行）` 与 `84-86: 签名头正本: …/sampler.h（136 行；…）`。现值：`sampler.cpp` 1503 行、`sampler.h` 273 行（`grep -c ''`）。即 288 vs 136 vs 实测 273，页内自相矛盾且两处都错。
- **descriptor 行锚错**：`26: （module_adapters.cpp:642-654 p2_sample_descriptor）` 与 `62: 编排层 descriptor 端口表（module_adapters.cpp:642-654…）`。实测 `p2_sample_descriptor` 在 **1065-1082**；642-654 落在 session adapter 的 manifest 捕获块（`module_adapters.cpp:643: acs_span_u8 man{};`）。
- **内核行锚漂移**：`87-91: p2_sampler_default_config（:60…）、p2_frame_id（:93…）、p2_stats_median（:97）/p2_stats_mad（:98-99…）、p2_sample_controls（:103…）、p2_sample_controls_cached（:120…）`。实测 `sampler.h:99: P2_API std::uint64_t p2_frame_id(const char* hips_path);`、`:103: P2_API double p2_stats_median(...)`，即 frame_id 在 99 非 93、stats_median 在 103 非 97，`p2_sample_controls` 已不在 103。
- **口径声明**：本页在 `22-24:` 自述"合同权威=lib/algorithms/sampling/ 三件套 + docs/algorithms/PHASE2_SAMPLER.md"——在一页里同时声明三个权威（三件套、ALG 文档、本页），属标准 01 §1"任何文档不声明自己的第二套权威顺序"。

### 1.3 docs/modules/registry/astrocs.phase2.session.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase2.session.md` |
| 标题 | `模块 astrocs.phase2.session`（H1:11） |
| 行数 | 67（机械列 67 ✓） |
| 角色 | **模块工作细节**，落位一致，**不错放** |
| 主题 | Phase2 装配会话（`p2_session` 五 C ABI）登记页 |
| 上游 | 有：`13: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错，同上） |
| 下游 | 7 个引用者（检索词 `astrocs.phase2.session.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/algorithms/PHASE2_SESSION.md`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` |
| 是否正本 | **是**（Phase2 会话在模块层的唯一登记页；`docs/algorithms/PHASE2_SESSION.md` 是算法层合同正本，非同一角色） |
| 重复或重叠对象 | 与 `docs/algorithms/PHASE2_SESSION.md`（413 行）在"五导出 C API、四段 canonical 节点、取消检查点"三点重叠：本页 `39-41:` "导出符号：SRC-P2-SESSION-001 五 C API（p2_session.h:17/:20/:23/:25/:27：p2_session_create/validate/run/inspect/destroy）+ C++ 诊断面 last_error（h:35…）"，同页 `48-52:` 又展开并发/取消点行锚，`55-56:` 展开 manifest 状态机——会话层合同的展开式重述；两处段落一在模块页一在 ALG 页，属同口径双写（合法分层只到"指针"为止）。另与 `astrocs.phase1.session.md`（别批）结构同构（两页都登记"descriptor 无 session 条目、五函数被工厂委托"这一同一事实） |
| 元信息块 | **有**：frontmatter 1-9 行；`5: owner: SA-P2-X24` 是**执行代理编号**而非负责人/团队；机械列 `meta_block=False` 漏报 |
| 写法违规 | commit sha：`6: source_commit: dc1ed210fecdd2981d3aaced916b73072e1ca067`（40 位，机械列命中 ✓）；任务编号：`25: 补齐归 P2-SESSION-IMPL`、`24: 占位词汇对齐归 P2-XX-INT`、`67: 可执行会话行为测试` 段中的 `P2-SESSION-DOC`（INDEX 侧）、`20: RT-005 IModule 工厂`（机械列 `task_ids=[]` 漏报，理由同 §1.2）；历史叙事：`59-60: 另有 h:19"拒未知键"与 validate 实现漂移、run 子键类型错未捕获路径（登记不改码）` —— "登记不改码"过程语；结构：本页**没有 `##` 小节**，整页是 bullet 串，与 registry 其余 25 页的九节模板结构不一致（同一目录内两种体例） |
| 悬空引用 | `16-17: registry descriptor 无 astrocs.p2.session 词汇（module_adapters.cpp:643 起 descriptor 族为 p2_sample_descriptor 等编排占位…）` —— `module_adapters.cpp:643` 实测内容是 `acs_span_u8 man{};`（在 session adapter 内），descriptor 族从 **:692** `ModuleDescriptor phase1_descriptor()` 才起（命令 `grep -n 'ModuleDescriptor [A-Za-z0-9_]*_descriptor()' lib/infrastructure/scheduler/src/module_adapters.cpp`）。同页 :18 的 `module_adapters.cpp:23-26 五 C ABI 声明` **正确**（实测 :20-28 是 P2-001 委托注释，23-26 恰为 sample/upm-fit/upm-apply/reject 四行）。文件级引用 `lib/phase2_session/p2_session.h`（39 行）、`p2_session.cpp`（318 行）、`docs/algorithms/PHASE2_SESSION.md`（413 行）、`docs/api/PHASE2_API_V1.md`（55 行）、`eng/tests/unit/p2_ir_facade_test.cpp`（78 行）均存在 ✓ |
| 与上位冲突 | 是：上游条款号错；`27-30: 层级：assembly（编排，纯 facade 直调 lib/algorithms/coverage 生产符号…）` 自认"纯 facade"，与 AGENTS §6"不用 facade/空骨架冒充实现"无冲突（会话层本就允许委托，`ASTROCS_DESIGN.md:652`"phase{1,2,3}_session 三阶段会话编排（引用算法模块，不重复实现）"），但同页 :17 的 descriptor 族行号错会让下游按 `module_adapters.cpp:643` 找不到任何 descriptor |
| 处置建议 | **保留 + 迁出过程语**：它是 Phase2 会话唯一登记页（§3 双向可追溯要求它有位置），行号锚与"登记不改码/补齐归 P2-SESSION-IMPL"类过程语按 §4 删除，权威改指 `docs/algorithms/PHASE2_SESSION.md`；页名 `astrocs.phase2.session.md` 在本页 :22 自己声明"现行 registry descriptor 无此 module_id"——一个注册目录里的页面对应一个注册表里不存在的条目，应迁往 `docs/modules/phase2_session.md`（与 `docs/modules/phase1_session.md` 同层同级），把 `docs/modules/registry/` 让给真有 descriptor 的条目（理由：§2"目录承载固定角色"+§3"索引由文档现状重建，与文件树一致"） |

### 1.4 docs/modules/registry/astrocs.phase2.upm-apply.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase2.upm-apply.md` |
| 标题 | `模块 astrocs.phase2.upm-apply`（H1:12）；frontmatter `module_id: astrocs.p2.upm`（:4）与页名/h1 不同名 |
| 行数 | 160（机械列 160 ✓） |
| 角色 | **模块工作细节**，落位一致，**不错放**；但 `49-59:` 与 `111-129:` 承载算法/实现级口径，实质越到 `算法推导` 层（应留 `docs/algorithms/PHASE2_UPM_IMPL.md`） |
| 主题 | UPM 合同 apply 职能页 |
| 上游 | 有：`14: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`；正文另有正向引用 `50-52:` "默认只扣偏差 δ_k、保留公共天光面 B_ref…（最高设计 §5.4…）"——§5.4 标题确为"天光平面与统一相对模型（UPM）"（`ASTROCS_DESIGN.md:406`）✓ |
| 下游 | 12 个引用者（检索词 `astrocs.phase2.upm-apply.md`），含同批 `astrocs.phase2.upm-fit.md:27`、`docs/modules/phase2_upm.md`、`docs/algorithms/PHASE2_UPM_IMPL.md`、`lib/algorithms/upm/{README.md,module.yaml}`、`docs/modules/registry/module_id_migration_baseline.json` |
| 是否正本 | **是（apply 职能唯一手写登记页）**，但与 §1.5 同源于一次 P2-UPM-DOC 重写，两页共同声明同一份 `astrocs.p2.upm` 合同 |
| 重复或重叠对象 | **与同批 `astrocs.phase2.upm-fit.md` 大面积逐字重复**（两页登记同一个 `astrocs.p2.upm` 模块，不是下钻关系）：<br>· fit`:22` 与 apply`:23`（同串）`生产源 lib/algorithms/coverage/src/upm.cpp 2793 行 + upm.h 384 行`<br>· fit`:40-42` 与 apply`:41-43`（同串）`生产源：lib/algorithms/coverage/src/upm.cpp（2793 行，根 CMakeLists.txt :337-346 astrocs_phase2 静态库成员，upm.cpp 列于 :338）+ 权威源 签名头 …upm.h（384 行）`<br>· fit`:81-88` 与 apply`:88-95`（同串）核心 symbol 16 导出清单含 `p2_upm_build（:929）、p2_upm_build_geo（:934）、p2_upm_save（:940）、p2_upm_open（:1008）、p2_upm_info（:1233）、p2_upm_calibrate_block（:1240）、p2_upm_evaluate_c（:1271）、p2_upm_raw_weight（:1292）、p2_upm_normalized_weights（:1325）…p2_upm_close（:1559）`<br>· fit`:131-134` 与 apply`:139-142`（同串）DISP-P2UPM-001..004 known_defects 清单<br>· fit`:36-39` 与 apply`:37-40`、fit`:43-44` 与 apply`:44-45`（owner/depends_on_int 行）<br>差异只在 §职责（fit=求解/apply=扣除）与 §端口表（fit 两端口、apply 三端口）。重叠范围 ≈ 两页共 313 行中的 60–70 行逐字相同，另有多段近似同构 |
| 元信息块 | **有**（frontmatter 1-10 行，含 `module_id` 模板外字段）；`6: owner: SA-P2-U21` 为代理编号；机械列漏报 |
| 写法违规 | commit sha：`7: source_commit: 5ecc60df…`（命中机械列 ✓）；任务编号：`16-18: registry 页保留先例 astrocs.phase1.session.md/astrocs.phase2.coverage.md/astrocs.phase2.reject.md`、`26: 由 P2-XX-INT 对齐修订`、`35: MISSING 语义归 P2-UPM-IMPL`、`105: 占位 ID 清单（旧派生词汇…）`、`139-143: DISP-P2UPM-001..004`、`152: 容差权威=ALG-P2-UPM-IMPL-001`（机械列 `task_ids=[]` 漏报）；历史叙事：`25-26: 旧 descriptor 派生词汇…由 P2-XX-INT 对齐修订`、`135-136: 无 ACS_ERR_*（旧 registry 派生词汇以本页修订）`、`159-160: 本页旧派生内容（"错误码=ACS_ERR_*"、"取消=host cancel 回调"等 session 层词汇）以本合同页…为准修订` —— "旧词汇/已修订/先例"三类齐备，§4 禁 |
| 悬空引用 | **1 个符号级失效**：`91: p2_upm_normalized_weights（:1325）` —— 该符号声明已在头文件撤下，`lib/algorithms/coverage/include/astro/phase2/upm.h:181: /* RETIRED 2026-09-25 (CHK-PROD-WIRING W1): p2_upm_normalized_weights 全仓零消费者声明已撤下；见 run/WIRING-W16-01/REPORT.md。 */`（命令 `grep -n 'p2_upm_normalized_weights' lib/algorithms/coverage/include/astro/phase2/upm.h`）；fit 页 :85 同样挂着它。文件级引用 `lib/algorithms/upm/`、`upm.cpp`、`upm.h`、`docs/modules/phase2_upm.md` 存在 ✓ |
| 与上位冲突 | **是（数值层面，三条，见 §1.4-A）** |
| 处置建议 | **合并**（并入 `astrocs.phase2.upm-fit.md`，去向：同一份 `astrocs.p2.upm` 合同只留一页，apply/fit 作该页两节）+ 删除全部行锚与 16 符号清单（改指 `lib/algorithms/upm/module.yaml` 与 `docs/algorithms/PHASE2_UPM_IMPL.md`）。理由：标准 01 §2"同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除"——两页登记同一模块同一合同，拆页只带来逐字重复与两处不同的错误值 |

#### 1.4-A 与注册面逐项比对

- **descriptor 行锚两侧值**：`66: 编排层 descriptor 端口表（module_adapters.cpp:680-694，descriptor 占位词汇…）` 与 `104: parallel_ok=True（module_adapters.cpp:680-694）`；实测 `p2_upm_apply_descriptor` 在 **1103-1121**（`grep -n 'ModuleDescriptor p2_upm_apply_descriptor()'`），而 680-694 实测内容是 `last_manifest()` 实现与 `phase1_descriptor()` 的前三行（`module_adapters.cpp:694: d.module_id = "astrocs.phase1.calibration";`）。
- **端口表三行全部单位相反**：页面 `71-73` 三行都写 `UnitId::ADU`；注册面 `module_adapters.cpp:1111-1113` 三行都写 `UnitId::SURFACE_BRIGHTNESS`（`upm_model`/`calibrated_frames`/`corrected`）。必/可 与 DATA 号一致，只有单位列背离。
- **生产源行数两侧值**：页面 `23/41` "upm.cpp 2793 行 + upm.h 384 行"；实测 `upm.cpp` 2981 行、`upm.h` 450 行（`grep -c ''`）；`docs/DOCUMENT_INDEX.yaml:977` 的 notes 又写"upm.cpp 1565 行 + upm.h 184 行"。同一事实三处三个值（页面／索引／现文件）。
- **16 导出清单的符号行号全部失效**：`p2_upm_build` 页面 :929 → 实测 `upm.cpp:1372`；`p2_upm_calibrate_block` 页面 :1240 → 实测 `upm.cpp:1907`；`p2_upm_dense_read_block` 页面 :1542 → 实测 `upm.cpp:2201`；`p2_upm_close` 页面 :1559 → 实测 `upm.cpp:2218`（命令 `grep -n '^int p2_upm_build(\|…' lib/algorithms/coverage/src/upm.cpp`）。14 个行号无一命中，偏移 300–660 行，且现头文件另有 `p2_upm_convergence/p2_upm_identifiability/p2_upm_warnings_json/p2_upm_control_variance/p2_upm_ma_*` 等导出，"16 导出"这个数本身已不成立。

### 1.5 docs/modules/registry/astrocs.phase2.upm-fit.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase2.upm-fit.md` |
| 标题 | `模块 astrocs.phase2.upm-fit`（H1:11）；frontmatter **无 `module_id` 字段**（只 `id: MOD-astrocs-phase2-upm-fit`），而同合同另一页 :4 有 `module_id: astrocs.p2.upm` |
| 行数 | 153（机械列 153 ✓） |
| 角色 | **模块工作细节**，落位一致；`48-55:`（Huber IRLS δ=1.345、图平滑、gauge）与 `113-121:` 属算法推导层内容越界，应指 `docs/algorithms/PHASE2_UPM_IMPL.md`（530 行） |
| 主题 | UPM 合同 fit 职能页 |
| 上游 | 有：`13: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错） |
| 下游 | 14 个引用者（检索词 `astrocs.phase2.upm-fit.md`），含同批 upm-apply:27、`docs/modules/phase2_upm.md`、`docs/algorithms/PHASE2_UPM_IMPL.md`、`docs/contracts/UNIFIED_OBJECTS.md`、`eng/contracts/data/unified_object_compatibility_map_v1.json`、`lib/algorithms/upm/README.md` |
| 是否正本 | **与 §1.4 并列的两份之一，不成立"两份各正"**：同一 `astrocs.p2.upm` 合同被两页分持，`docs/modules/MODULE_MAP.yaml` 只有 1 条 upm 条目（`module_id: astrocs.p2.upm`，`plugin_doc: docs/plugins/algorithms_phase2/11_upm.md`），说明权威登记面认为这是一个模块，两页都是它的副本 |
| 重复或重叠对象 | 与 `astrocs.phase2.upm-apply.md`：见 §1.4 的五行对逐字举证（fit:22↔apply:23、fit:40-42↔apply:41-43、fit:81-88↔apply:88-95、fit:131-134↔apply:139-142、fit:36-39↔apply:37-40）。另与别批 `docs/plugins/algorithms_phase2/11_upm.md`（189 行，MODULE_MAP 的正册 plugin_doc）在同一模块上重叠：本页是 registry 侧、那份是 plugin 侧，两份都在讲 upm 的 fit/apply 职责 |
| 元信息块 | **有**（frontmatter 1-9）；`5: owner: SA-P2-U21` 代理编号；机械列漏报 |
| 写法违规 | commit sha：`6: source_commit: 5ecc60df…`；任务编号：`16-17: registry 页保留先例 astrocs.phase1.session.md/astrocs.phase2.coverage.md/astrocs.phase2.reject.md`、`34: P2-UPM-IMPL`、`97: 占位 ID 清单（旧派生词汇）`、`139: 可执行 TEST-P2-UPM-001 MISSING（P2-UPM-TEST 建立，不冒认）`、`110: 既有验证=eng/tests/api/test_upm_parallel.py test_02`（机械列 `task_ids=[]` 漏报）；历史叙事：`127: 无 ACS_ERR_*（旧 registry 派生词汇以本页修订）`、`151-153: 本页旧派生内容…以本合同页与 SCI-UPM-001/ALG-UPM-001 为准修订`；§4"不写过程"违规样本：`53: save 前校验 frame_id_by_index 与 C 行数一致（upm.cpp :944-945）` —— 实测 `upm.cpp:944-945` 是 `for (std::size_t ii : m->controls[k].obs_idx) {` / `const auto& o = obs[ii];`，与所诉校验无关（锚失效，属悬空而非仅风格） |
| 悬空引用 | `62: 编排层 descriptor 端口表（module_adapters.cpp:661-678…）` 与 `96: parallel_ok=True（module_adapters.cpp:661-678）`：实测 `p2_upm_fit_descriptor` 在 **1084-1101**；661-678 是 session adapter 的 inspect/last_manifest 代码。`84-88:` 16 符号行号同 §1.4-A 全失效；`84/91:` 含已 RETIRED 的 `p2_upm_normalized_weights` |
| 与上位冲突 | **是**：`67-68` 端口表两行单位 `UnitId::ADU` 对注册面 `module_adapters.cpp:1092-1093` 两行 `UnitId::SURFACE_BRIGHTNESS`；`22/42` 行数与现值不符；`79` "upm.h（384 行）"对实测 450 行 |
| 处置建议 | **保留为合并后唯一页 + 去重**（与 §1.4 合并到一页，页名建议 `astrocs.phase2.upm.md`，或按 §1.3 同理迁 `docs/modules/phase2_upm.md` 之下——但 `docs/modules/phase2_upm.md`（263 行）已存在，故更准确的处置是**合并进 `docs/modules/phase2_upm.md`**，registry 目录只留生成页）。理由：标准 01 §2 同一主题一份正本＋§3"索引由文档现状重建，与文件树一致"——目前同一模块占 3 处（registry 两页 + phase2_upm.md）与 1 处权威登记（MODULE_MAP 单条）不成比例 |

### 1.6 docs/modules/registry/astrocs.phase2.write.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase2.write.md` |
| 标题 | `模块 astrocs.p2.hips_writer（registry 行 MOD-astrocs-phase2-write）`（H1:11）——**全批唯一在 H1 里并列两套 ID 的页**，且 H1 用合同名、文件名用占位名 |
| 行数 | 142（机械列 142 ✓） |
| 角色 | **模块工作细节**，落位一致；`30-59:` 的输入哈希链/逆归一公式属 `算法推导`，`56-58:` 的原子发布缺口属 `架构设计`（应指 `docs/architecture/IO_AND_ATOMICITY.md`，该文件在册且被它下游引用） |
| 主题 | Phase2 HiPS 写出（stage2 工具）合同页 |
| 上游 | 有：`13: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错）；正文另引 `57:` "违反最高设计 §9 的原子发布强制条款"——`ASTROCS_DESIGN.md:684` §9 标题是"CPU 后端与资源"，原子发布条款实际在 **§10 I/O 与原子产品**（`ASTROCS_DESIGN.md:688`"所有产品…私有临时区 → 校验 → fsync → 算哈希 → 原子改名发布"）→ **第二个条款号错**（不是模板带的，是本页自带） |
| 下游 | 13 个引用者（检索词 `astrocs.phase2.write.md`），**其中一个是生产代码**：`lib/infrastructure/scheduler/src/module_adapters.cpp:1173-1174` 注释"…（docs/modules/registry/astrocs.phase2.write.md 端口表同源: 输入 ADU / 输出 SURFACE_BRIGHTNESS）"——注册面反过来声称与本页同源 |
| 是否正本 | **是**（P2 HiPS 写出的唯一手写登记页） |
| 重复或重叠对象 | 与 `docs/algorithms/PHASE2_MOSAIC_WRITE.md`（530 行，`130-131:` 自引为 ALG-P2-HIPS-001..004 权威）重叠 §职责与容差：本页 `43-46:` "逆归一（area=sup×A_cell :527、flux=signal×area :1000-1017/:1219-1233）——⚠ 该 flux 是 writer 视图的中间量…落盘值 = flux_sum / covered_area = 面亮度"，同一口径的正本在 ALG 文档；另与 `lib/algorithms/coverage/hips_p2/README.md`（185 行）重叠——本页 `19-22:` 明写"模块级事实以 lib/algorithms/coverage/hips_p2/README.md（r1，CONTRACT_READY）+ module.yaml…为准"，即**同一事实的第三处**（README 自称准、本页抄展开） |
| 元信息块 | **有**（frontmatter 1-9）；`5: owner: SA-P2-I23` 代理编号；机械列漏报 |
| 写法违规 | commit sha：`6: source_commit: 5ecc60df…`；`1-11` frontmatter 的 `upstream:` 一次列 10 个合同 ID（`SCI-UPM-001, SCI-INT-001, SCI-REJ-001, ALG-P2-HIPS-001..004, API-P2-HIPS-001, API-P2-001`）；任务编号：`55-56: DISP-P2HIPS-003 如实登记`、`104: 迁移整改点，P2-HIPS-IMPL`、`128: 登记面=ALG 文档 §11.4 设计冻结 VERIFIED（COV TEST-COV-DESIGN-001 先例…）`、`138: DISP-P2HIPS-001..004`（机械列 `task_ids=[]` 漏报）；历史叙事：`15-16: 手写合同页：本页重写 registry 占位页（占位页以 module_adapters.cpp descriptor 为唯一源…）`；**自证式口径声明**：`26: 全部行锚 grep/sed 实测，抄录一律以实测锚为准` —— 标准 01 §1"任何文档不声明自己的第二套权威顺序或唯一口径"，且该声明被 §1.6-A 证伪 |
| 悬空引用 | 见 §1.6-A（descriptor 行锚、stage2 行数、1040 自证锚全错）。文件级：`lib/algorithms/coverage/tools/stage2.cpp`（2015 行）✓、`stage2_common.h`（581）✓、`hips_p2/{README.md,module.yaml}`（185/115）✓、`docs/algorithms/PHASE2_MOSAIC_WRITE.md`（530）✓、`lib/include/astrocs/core/artifact.h:36` ✓（该锚实测正确：`SURFACE_BRIGHTNESS = 6` 就在第 36 行）|
| 与上位冲突 | **是（三向矛盾，本批最重）**：见 §1.6-A 第 2 条——文档、descriptor、descriptor 注释三者对"输入端口单位"给三个互斥值 |
| 处置建议 | **保留（正本）+ 就地清锚**：删 `69-72:` 的 descriptor 行号与"实测 grep"自证段（只留"descriptor 词汇以 DATA-P2-HIPS 为准"的正向条款），把 `57:` 的 §9 改 §10，把 §职责中的哈希链/逆归一公式移回 `docs/algorithms/PHASE2_MOSAIC_WRITE.md`。理由：§1"公式…下沉到对应下层文档，每节末尾给出索引指针"＋§4"源码行号放对应下层文档" |

#### 1.6-A 与注册面逐项比对（含代码注释的第三向矛盾）

1. **descriptor 行锚两侧值**：`69:` 与 `91:` 都写 `module_adapters.cpp:1040-1057 p2_write_descriptor`，`71-72:` 更写"（行锚实测 `grep -n 'p2_write_descriptor' → lib/infrastructure/scheduler/src/module_adapters.cpp:1040`。）"。现算：`grep -n 'p2_write_descriptor' lib/infrastructure/scheduler/src/module_adapters.cpp` → **1162**（定义）、15494（注册表行）。1040-1057 实测是 `p2_coverage_descriptor`（1046）/`p2_sample_descriptor`（1065）区间。页面上一条"全部行锚实测"的自证，在它自己举例的那一条上就是错的。
2. **输入端口单位三向矛盾**：
   - 页面 `65:` `| `integrated` | `DATA-P2-INT` | 必 | `UnitId::ADU` | …（descriptor 词汇）|`
   - 注册面 `module_adapters.cpp:1175:` `{"integrated", "DATA-P2-INT", true, UnitId::SURFACE_BRIGHTNESS, CoordinateFrame::PIXEL},`
   - 注册面注释 `module_adapters.cpp:1173-1174:` "…docs/modules/registry/astrocs.phase2.write.md 端口表同源: 输入 ADU / 输出 SURFACE_BRIGHTNESS"
   即代码注释声称"与页面同源：输入 ADU"，而它所在的 descriptor 本身写 SURFACE_BRIGHTNESS；页面也写 ADU。三值互斥，且**代码与自身注释矛盾**、代码注释与代码矛盾，页面与 descriptor 矛盾。此为本批"第二套表"缺陷的最强实例。
3. **生产源行数两侧值**：`23:` "stage2.cpp（1965 行…）"→ 实测 2015 行（`grep -c ''`）。

### 1.7 docs/modules/registry/astrocs.phase3.properties.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase3.properties.md` |
| 标题 | `模块 astrocs.phase3.properties`（H1:13） |
| 行数 | 60（机械列 60 ✓） |
| 角色 | **模块工作细节**，落位一致，**不错放** |
| 主题 | Phase3 properties 节点注册页（descriptor 派生） |
| 上游 | `15: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（有标，条款号错，见 §4.3） |
| 下游 | 7 个引用者（检索词 `astrocs.phase3.properties.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/algorithms/anchors/anchor_contract.json`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`eng/tools/doccheck/doc_hygiene_exempt.json`、`reports/v19r2/*` |
| 是否正本 | **否**：60 行里只有端口表（:26-27）与 §descriptor（:38-39）随模块变化，其余 5 节是生成器常量；四个结论字段全 `NOT_VERIFIED`（:4/44/56）。它不具备"正本"的承载量 |
| 重复或重叠对象 | 与 §1.1 `astrocs.phase2.resample.md:19-20/33-34/48/52` 逐字相同（同模板输出）；`56:` 与 `44:` 重复同一句"证据源 eng/ci/ledgers/module_page_evidence.json(absent) 无 …条目" |
| 元信息块 | **有**（frontmatter 1-9 + 生成锚 11 行）；机械列 `meta_block=False` 漏报 |
| 写法违规 | commit sha `6: source_commit: 822b9c5391a14cc36979a7c550984f6ce363c713`；sha256 ×4（:11）；`status: NOT_VERIFIED`（:4）与 `44/56:` 的 NOT_VERIFIED 展开——把"未验证"当正文长期挂页（§4"只写现行设计要怎样"） |
| 悬空引用 | 同 §1.1：`eng/ci/ledgers/module_page_evidence.json` 不存在（`ls eng/ci/ledgers/ \| grep -i module` 无输出）。端口/文件其余 ✓ |
| 与上位冲突 | 是（§8.4 条款号错；§错误节 exit 9 归 §6.3，实际 §7.2）。端口表与 descriptor 现值**一致**（`module_adapters.cpp:761-762`：hips/DATA-HIPS-001/必/SURFACE_BRIGHTNESS/HEALPIX；props/DATA-P3-PROPS/可/DIMENSIONLESS/HEALPIX ↔ 页面 :26-27 逐字段同） |
| 处置建议 | **保留（生成物）**，与 §1.1 同批处理（修生成器 + 补证据源或删除结论位）。理由：§3"每份下层文档标明上游条款"——修好指针即可继续充当注册面镜像；不建议删（删了注册表在文档层无镜像，`DOCUMENT_INDEX` 与 `unified_object_registry` 反而出现悬空登记） |

### 1.8 docs/modules/registry/astrocs.phase3.resample.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase3.resample.md` |
| 标题 | `模块 astrocs.phase3.resample`（H1:13） |
| 行数 | 60（机械列 60 ✓） |
| 角色 | **模块工作细节**，落位一致，**不错放** |
| 主题 | Phase3 重采样 descriptor 注册页（**与同目录 `astrocs.phase3.resample2.md` 争夺同一模块**） |
| 上游 | `15: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错） |
| 下游 | 8 个引用者（检索词 `astrocs.phase3.resample.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/contracts/DATA_ARTIFACTS.md`、`docs/modules/registry/module_id_migration_baseline.json`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`eng/tools/doccheck/doc_hygiene_exempt.json`、`reports/v19r2/*` |
| 是否正本 | **否**（同 §1.7：模板壳 + NOT_VERIFIED） |
| 重复或重叠对象 | **与同批 `astrocs.phase3.resample2.md` 指向同一物理模块**：`docs/modules/MODULE_MAP.yaml:471` 权威 `module_id: astrocs.p3.resample`，`astrocs.phase3.resample2.md:15-16` 自述"矩阵权威 module_id=astrocs.p3.resample（MODULE_MIGRATION_MATRIX P3-RSMP 行）"，而本页标题是 `astrocs.phase3.resample`、端口 `hips(DATA-HIPS-001)+tile(DATA-TILE-001)`（:26-27），另一页端口是 `wcs_plan+hips+resampled`（:72-74）。`astrocs.phase3.resample2.md:50-52` 直接给本页定性："相邻占位行注记: MOD-astrocs-phase3-resample（module_adapters.cpp:300-318 phase3_descriptor，**P2 模板复制残留**）不属本页，由 P3-RSMP-INT 处理，本任务零触碰"。即两页并存且一页自认另一页是残留 |
| 元信息块 | **有**（frontmatter 1-9 + 生成锚）；机械列漏报 |
| 写法违规 | commit sha `:6`、sha256 ×4 `:11`、NOT_VERIFIED 正文化 `:44/:56`；§错误节 `:52` 抄"接线以实测为准"（过程语） |
| 悬空引用 | `eng/ci/ledgers/module_page_evidence.json` 不存在（同 §1.1）。其余 ✓ |
| 与上位冲突 | 是：§8.4 条款号错；exit 9 归 §6.3 错（应 §7.2）；端口表与 descriptor 现值一致（`module_adapters.cpp:738-739` ↔ 页面 :26-27 逐字段同），但与 `docs/modules/MODULE_MAP.yaml` 权威条目 `astrocs.p3.resample`（其 `plugin_doc=docs/plugins/algorithms_phase3/15_resample.md`）不同名，且 `docs/plugins/00_INDEX.md:54` 把 resample 描述为"球面反向映射与重采样"，而本页端口是 hips→tile（无 wcs_plan 入端）——两页对"Phase3 重采样的输入"给两套结构 |
| 处置建议 | **删除**（去向：`astrocs.phase3.resample2.md` 一页承载 Phase3 重采样；同时把 descriptor `phase3_descriptor()`（`module_adapters.cpp:730-747`）的 `astrocs.phase3.resample` module_id 并入 `p3_resample2_descriptor`，由注册面驱动页面消失）。理由：标准 01 §2"同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除"＋§5 判定"走得通"要求"沿途无同主题第二份正本"。若注册面暂不动，退一步为**保留但降级为非规范占位**并在 `DOCUMENT_INDEX` 标注与 resample2 的从属关系 |

### 1.9 docs/modules/registry/astrocs.phase3.resample2.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase3.resample2.md` |
| 标题 | `模块 astrocs.phase3.resample2`（H1:11）；frontmatter 无 `module_id` 字段，但 `:15-16` 声明权威名 `astrocs.p3.resample` |
| 行数 | 215（机械列 215 ✓） |
| 角色 | **模块工作细节**，落位一致；`§6 冻结公式`（:129-147）与 `§9 验证与测试面`（:181-196）属 **算法推导 / 标准与检查** 层内容，越界（应只在 `docs/algorithms/PHASE3_RSMP_IMPL.md`，425 行，它自己在 `:129` 写明"权威源=ALG-P3-RSMP-IMPL-001 §6"） |
| 主题 | Phase3 重采样（sampler 域）合同页 |
| 上游 | 有：`13: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错）；正文另引 `211:` NaN 规则"权威 = ASTROCS_DESIGN.md §5.5" ✓（`ASTROCS_DESIGN.md:417` §5.5 逐像素排异、`:426` 样本级掩膜+重归一+强制计数）与 `214:` 输出模式"最高设计 §6.3" ✓（`:456`） |
| 下游 | 11 个引用者（检索词 `astrocs.phase3.resample2.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/contracts/PUBLIC_API.md`、`docs/algorithms/anchors/anchor_contract.json`、`docs/modules/phase3_rsmp.md`、`lib/algorithms/resample/{README.md,memory.md}`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` |
| 是否正本 | **是**（Phase3 重采样模块层唯一实质合同页） |
| 重复或重叠对象 | ①与 `astrocs.phase3.resample.md`（§1.8）同一模块两份页，重叠证据见 §1.8；②`125-127:` 注册序整段与 `astrocs.phase3.wcs.md:109-111` **逐字相同**（`- 注册序: module_adapters.cpp:816 起序列（phase3_descriptor→p3_wcs_descriptor→p3_resample2_descriptor→p3_writer_descriptor，:797-798 收尾）；配置=phase config JSON（按 PHASE API 文档）。`）——两个不同模块页复制同一段错误的注册表坐标；③`209-214:` "## NaN 与写端口"节与 `astrocs.phase3.wcs.md:181-186`、`astrocs.phase3.writer.md:186-191` 三条逐字相同（NaN 规则四连抄），属同一段落的三份副本；④与 `docs/modules/phase3_rsmp.md`（106 行）同模块两页 |
| 元信息块 | **有**（frontmatter 1-9）；机械列漏报 |
| 写法违规 | commit sha `:6: source_commit: 5ecc60df…`（命中机械列 ✓）；任务编号：`18-19: 由 P3-RSMP-INT 对齐`、`26: 由 P3-RSMP-IMPL 建立，IMPLEMENTED 只由验收签发`、`164-177: DISP-P3RSMP-001..005 …归 P3-RSMP-IMPL/INT`、`196: EVIDENCE: EVID-MISSING（归 P3-RSMP-INT/验收补）`、`47: depends_on_int=P3-PROJ-INT;IO-003;CPU-005`（机械列 `task_ids=[]` 漏报）；历史叙事：`18: 保留不改动`、`51-52: P2 模板复制残留…本任务零触碰`、`88-90: 端口词汇…为 descriptor 派生…不作冻结依据`（§4 无历史叙事/无过程语） |
| 悬空引用 | **两处真实悬空**：<br>①`172:` "（仅探针 `p3_resample_probe_main.cpp:31` 消费）"与 `190:` "eng/tests/backend/p3_resample_probe_main.cpp（探针六模式）"——该路径不存在；本机确认命令 `git -C … ls-files \| grep -i p3_resample` → 实际文件在 **`lib/algorithms/resample/tests/p3rsmp/p3_resample_probe_main.cpp`**。<br>②`194-195:` "eng/tests/unit/p3_coverage_test.cpp（106 行…）"——文件存在但行数实测 **224** 行（`grep -c ''`），值错。<br>③行锚：`89-90/116:` "descriptor（module_adapters.cpp:425-439 p3_resample2_descriptor）"与 `125-127:` "注册序: module_adapters.cpp:816 起…:797-798 收尾"、`50-51:` "module_adapters.cpp:300-318 phase3_descriptor" 三处全错：实测 `p3_resample2_descriptor` = **791-809**、`phase3_descriptor` = **730-747**、注册序 = **15515-15519**（命令 `grep -n 'ModuleDescriptor p3_resample2_descriptor()\|ModuleDescriptor phase3_descriptor()' lib/infrastructure/scheduler/src/module_adapters.cpp` 与 `grep -n 'descriptor(),' …`）。文件级引用 `p3_resample.cpp`（586 行 ✓ 与页面 :33 "586 行"一致）、`p3_resample.h`（实测 202 行，页面 :34 写 201 行，差 1）✓/✗ |
| 与上位冲突 | **是**：`73:` 端口表 `hips | DATA-HIPS-001 | 必 | UnitId::ADU | CoordinateFrame::HEALPIX`，注册面 `module_adapters.cpp:800` 为 `UnitId::SURFACE_BRIGHTNESS`；`§6` 与 `docs/algorithms/PHASE3_RSMP_IMPL.md §6` 双写公式（见角色行）；`213:` 写"写端口 UnitId::SURFACE_BRIGHTNESS"与同页 `73:` 输入端口 ADU 并列——同页内部亦不自洽 |
| 处置建议 | **保留（正本）+ 三处动作**：①把 §6/§9 移回 `docs/algorithms/PHASE3_RSMP_IMPL.md`，只留指针（§1 公式下沉＋§4 行号下层化）；②测试路径改为共址实际目录（§3 与文件树一致）；③与 §1.8 合并为一页（§2 一主题一正本）。端口表在合并前按 descriptor 现值改为 SURFACE_BRIGHTNESS（§1"下位与上位冲突时以上位为准；冲突本身是缺陷"） |

### 1.10 docs/modules/registry/astrocs.phase3.verify.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase3.verify.md` |
| 标题 | `模块 astrocs.phase3.verify`（H1:13） |
| 行数 | 60（机械列 60 ✓） |
| 角色 | **模块工作细节**，落位一致，**不错放** |
| 主题 | Phase3 读回校验节点登记页 |
| 上游 | 有：`15: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错） |
| 下游 | 6 个引用者（检索词 `astrocs.phase3.verify.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` |
| 是否正本 | **形式上是（verify 唯一页），实质内容是空壳**：全页 60 行里 55 行是模板常量，唯一模块特有信息 = 端口表与 execution_class；且四个结论字段 NOT_VERIFIED |
| 重复或重叠对象 | **它是生成页的过期副本**：`11: > 人工内容（MANUAL）：本页无 GENERATED-ANCHOR，非生成器所有；重跑 eng/tools/quality/gen_module_readmes.py 不会覆盖本页。` 而 `:19-20/33-34/48/52` 与真生成页（§1.7 `astrocs.phase3.properties.md` 同行）**逐字相同**；`44:` 措辞为"证据源 eng/ci/ledgers/module_page_evidence.json 无 …条目；未读到的结论不写成 PASS/FAIL"，而现行模板输出为"…json(absent) 无 …条目"（比对 §1.1 `:44`、§1.7 `:44`）——**同一句话的两个模板修订版并存**，证明本页是从上一版生成器抄出后手加 MANUAL 标记，不是"人工内容" |
| 元信息块 | **有**（frontmatter 1-9）；机械列 `meta_block=False` 漏报 |
| 写法违规 | commit sha `:6: source_commit: 5ecc60df…`（机械列命中 ✓）；`status: ACTIVE`（:4）——与生成页 `status: NOT_VERIFIED` 用不同状态词但无来源标注；其余同 §1.1（sha256 无、日期无、任务编号无：本页确实没有任务编号与日期，机械列 task_ids/dates=[] 正确）；纪律声明：`:11` MANUAL 行本身即 §4 禁的"文档自述机制" |
| 悬空引用 | `eng/ci/ledgers/module_page_evidence.json`（:44/56）不存在，确认命令同 §1.1。端口表与注册面 **一致**（`module_adapters.cpp:838-839` fits/verified ↔ 页面 :26-27；`execution_class="io"`、`parallel_ok=false` 对 `:835-836` ✓，frontmatter `upstream: [SCI-P3-VER-001, ALG-P3-005, API-P3-001]` 对 `:841-845` ✓） |
| 与上位冲突 | 是：§8.4 条款号错；§错误节 exit 9→§6.3 错（应 §7.2）。**身份冲突最重**：`docs/DOCUMENT_INDEX.yaml:1007-1012` 标 `status: GENERATED` + `notes: eng/tools/quality/gen_module_readmes.py 生成`，与页面 `:11` 的"非生成器所有"直接对立（见 §4.1） |
| 处置建议 | **保留 + 改索引标注为手写**：把 `DOCUMENT_INDEX` 的 GENERATED 改为手写状态，或按生成器语义 `--adopt` 收编为真生成页（两条路只能选一条，不能同时挂着）。理由：§3"索引与文件树一致"＋§1"任何文档不声明自己的第二套权威顺序" |

### 1.11 docs/modules/registry/astrocs.phase3.wcs.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase3.wcs.md` |
| 标题 | `模块 astrocs.phase3.wcs`（H1:12）；frontmatter `module_id: astrocs.p3.projection`（:3）与 h1/文件名不同名 |
| 行数 | 187（机械列 187 ✓） |
| 角色 | **模块工作细节**，落位一致；`§6 冻结公式`（:113-128）属算法推导层，`§2 投影集口径`（:56-62）是产品级口径声明（与 `ASTROCS_DESIGN.md:451` §6.3、`lib/algorithms/projection/p3_projection_registry.h` 同题三写） |
| 主题 | Phase3 投影（TAN）域合同页 |
| 上游 | 有：`14: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错）；正文 `56:` 引"最高设计 §5.3"——`ASTROCS_DESIGN.md:385` §5.3 是"SNR 重建与逆方差叠加"，而 8 投影冻结在 **§6.3 投影算法**（`:451`/`:456`）→ **本页自带的第二个条款号错**（8 种投影的代码注释也写 §5.3：`p3_projection_registry.h:5`"ASTROCS_DESIGN.md §5.3: 首批冻结 8 投影"——错串被代码与文档同时抄，见 §4.4）；`183:` NaN 引 §5.5 ✓、`186:` 输出模式引 §6.3 ✓ |
| 下游 | 14 个引用者（检索词 `astrocs.phase3.wcs.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/algorithms/PHASE3_PROJ_IMPL.md`、`docs/contracts/PUBLIC_API.md`、`docs/modules/phase3_proj.md`、`lib/algorithms/{projection,resample}/{README.md,memory.md}`、`docs/modules/registry/module_id_migration_baseline.json`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv`、`reports/v19r2/*` |
| 是否正本 | **是**（Phase3 投影模块层唯一合同页） |
| 重复或重叠对象 | ①与 `docs/modules/phase3_proj.md`（94 行）同模块两页（`:178` 自引为"模块总页"）；②与 `docs/algorithms/PHASE3_PROJ_IMPL.md`（668 行）重叠 §6 公式——`:113` 自认"权威源=ALG-P3-PROJ-IMPL-001 §6/§7"仍展开写；③`109-111:` 注册序段与 `astrocs.phase3.resample2.md:125-127` 逐字相同（见 §1.9③）；④`181-186:` NaN 节与 resample2/writer 逐字相同；⑤`147-148:` 同一句话连续两行**完全重复**（copy-paste 缺陷）：两行都是"- 产品声明门 `p3_proj_declare` 对非 TAN 码**显式返回 `P3_WCS_UNSUPPORTED`**" |
| 元信息块 | **有**（frontmatter 1-10，含 `module_id` 模板外字段）；机械列漏报 |
| 写法违规 | commit sha `:7: source_commit: 5ecc60df…`；任务编号：`19-20: 由 P3-PROJ-INT 对齐`、`25: 由 P3-PROJ-IMPL 建立，IMPLEMENTED 只由验收签发`、`144: → P3-PROJ-IMPL/INT`、`152: DLL 挂载归 P3-PROJ-IMPL`、`168: EVIDENCE: EVID-MISSING（归 P3-PROJ-INT/验收补）`、`119: P0 修复 bughunt_p0_wcs（:65-68）已合并`（**这一条同时是历史叙事**："已合并"，机械列 `task_ids=[]` 漏报）；历史叙事：`19: 保留不改动`、`79-81: 端口词汇…不作冻结依据` |
| 悬空引用 | **三条测试路径整体错目录**（本机确认 `git ls-files \| grep -i p3wcs`）：<br>①`34:` "同批迁入共址测试 eng/tests/p3wcs/**（ctest p3_wcs）"——`[ -d eng/tests/p3wcs ]` → 不存在，实际在 `lib/algorithms/projection/tests/p3wcs/`；<br>②`165:` "eng/tests/unit/p3_wcs_test.cpp（474 行…）"——该路径不存在，实际 `lib/algorithms/projection/tests/p3wcs/p3_wcs_test.cpp`，**行数 474 正确**（即只错目录）；<br>③`167:` "eng/tests/backend/p3_wcs_main.cpp"——实际 `lib/algorithms/projection/tests/p3wcs/p3_wcs_main.cpp`。<br>④行锚：`80-81/101:` "descriptor（module_adapters.cpp:406-423 p3_wcs_descriptor）"——实测 **772-789**；`109-111:` "注册序 module_adapters.cpp:816 起…:797-798 收尾"——实测注册序在 **15515-15519**。<br>⑤内核行锚成片失效：`71-75:` 给 `p3_wcs.cpp` 的 :39-43/:95/:122/:123/:137/:104/:130/:80-88 八处锚，实测该文件 39-43 是"0-based/FITS 双重桥接"注释、:51 是桥接函数注释、:95 是 `if (pst != P3_WCS_OK) return pst;`（命令 `awk 'NR==39\|\|NR==51\|\|NR==95 {print NR": "$0}' lib/algorithms/projection/p3_wcs.cpp`）。<br>文件级 ✓：`p3_wcs.cpp`（593）、`p3_projection_registry.h`（312）、`docs/algorithms/GATES_AND_TOLERANCES.md`（128）、`test_p1002_gaps.py`（596） |
| 与上位冲突 | **是**：①§8.4 与 §5.3 两处条款号错；②`56-60:` "设计冻结 8 种…**当前登记：仅 TAN 已实现**（声明集 D = 实现集 I = {TAN}）"与代码一致（`p3_projection_registry.h:70-84` TAN=kProductDeclarable、SIN/CAR/AIT=kKernelOnly、STG/MOL/CEA/ZEA=kNotImplemented；`:122/:140` kDeclared/kImplemented 各 1 项 = TAN）✓ ——**但它与同批 `docs/plugins/00_INDEX.md:53` 冲突**（后者把 projection 的"一句话职责"写成"投影 registry（内置 TAN/SIN/CAR/AIT/STG/MOL/CEA/ZEA）"，未加任何"仅 TAN"限定，见 §1.16）；③`185:` "写端口 UnitId::SURFACE_BRIGHTNESS"与 `:68-69` 端口表 `UnitId::DIMENSIONLESS`/`DEGREE` 不在一个端口层，读者易误读（同页两轴混排） |
| 处置建议 | **保留（正本）+ 就地修**：删 §6 公式段与 §9 行号锚、把测试路径改共址真实目录、§5.3→§6.3 上游号订正、删除 `147-148` 重复行（理由：§3 双向索引可达＋§4 行号下层化＋§5"沿索引能到达承载该机制的唯一正本"） |

### 1.12 docs/modules/registry/astrocs.phase3.writer.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/astrocs.phase3.writer.md` |
| 标题 | `模块 astrocs.phase3.writer`（H1:13）；frontmatter `module_id: astrocs.p3.fits_output`（:3）+ `aliases: [astrocs.p3.fits_writer, astrocs.phase3.writer]`（:4）——别名又把**页名自身**列为别名 |
| 行数 | 192（机械列 192 ✓） |
| 角色 | **模块工作细节**，落位一致；`§9 独立 synthetic 验证命令与容差`（:145-164）逐条列 T1-T7 判据，属 **标准与检查** 层（正本在 `docs/algorithms/PHASE3_FITS_IMPL.md`，442 行） |
| 主题 | Phase3 FITS 写出（原子发布 + sha256）合同页 |
| 上游 | 有：`15: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错）；`188:` NaN 引 §5.5 ✓、`191:` 输出模式引 §6.3 ✓；`51-52:` "R10-C 原子发布序（tmp→flush→fsync→rename…）"未给最高设计节号（该条款在 `ASTROCS_DESIGN.md:688` §10） |
| 下游 | 13 个引用者（检索词 `astrocs.phase3.writer.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/algorithms/PHASE3_FITS_IMPL.md`、`docs/contracts/PUBLIC_API.md`、`docs/modules/phase3_fits.md`、`lib/algorithms/{fits_output,projection,resample}/{README.md,memory.md}`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` |
| 是否正本 | **是**（Phase3 写出模块层唯一合同页） |
| 重复或重叠对象 | ①与 `docs/modules/phase3_fits.md`（150 行）同模块两页（`:179` 自引"模块总页"）；②`186-191:` NaN 节与 `astrocs.phase3.wcs.md:181-186`、`astrocs.phase3.resample2.md:209-214` 逐字相同（三份副本，已各贴一段）；③`147:` "**双重陈述（照 P2-INT/P2-REJ/P2-UPM registry 页锚先例）**"——把别批三页的体例作为本页结构依据，属跨页重复的自认；④`§9` T1-T7 与 `docs/algorithms/PHASE3_FITS_IMPL.md §12` 双写（`:148-149` 自认"承载 ALG-P3-FITS-IMPL-001 §12 T1-T7；本节即锚"——同一判据两处都称锚） |
| 元信息块 | **有**（frontmatter 1-11，含 `module_id/aliases`）；机械列漏报 |
| 写法违规 | commit sha `:8: source_commit: 5ecc60df…`；sha256：`:64-65` 端口表出现"sha256"字段值与 `:154` "sha256 64hex"、`:156` "sha256 重算一致"（机械列 `sha256=True` 命中 ✓，但这些是**语义** sha256 不是文档锚，属机械列**误报**方向，需在下节校准说明）；任务编号：`20-21: 由 P3-FITS-INT 对齐`、`26: 由 P3-FITS-IMPL 建立`、`150: MISSING 归 P3-FITS-TEST 落地+EVIDENCE`、`166-177: DISP-P3FITS-001..002`、`157: WIN-001 替代 popen`（机械列 `task_ids=[]` 漏报）；历史叙事：`19-21: 为 descriptor 占位词汇，保留不改动`、`168-171: DISP-P3FITS-001: lib/infrastructure/aio/README.md 旧派生内容声称"零外部依赖、不依赖 cfitsio"，与现状 vendored third_party/cfitsio…矛盾`（"旧派生内容/矛盾"过程叙事） |
| 悬空引用 | **三处，均给出两侧值**：<br>①`33-34:` "签名头正本 p3_output.h（166 行）+ WCS 关键字源 **p3_wcs.h（166 行）**"——`lib/algorithms/fits_output/p3_wcs.h` 不存在（`git ls-files \| grep 'p3_wcs.h'` → 唯一命中 `lib/algorithms/projection/p3_wcs.h`，实测 **373** 行，166 是上一项 p3_output.h 的值被复制）；`70:` 又引"p3_wcs.h:25-26"作 abs(dec)≤85° 锚，实测 `projection/p3_wcs.h:25-26` 是 `enum P3WcsStatus { P3_WCS_OK = 0,`。<br>②`151:` "eng/tests/unit/p3_output_test.cpp（244 行 4 段，eng/tests/unit/CMakeLists.txt:442-447）"——文件存在且 **244 行正确**，但注册行实测在 `eng/tests/unit/CMakeLists.txt:845-850`（`add_executable(p3_output_test …)`/`add_test(NAME p3_output …)`），:442-447 实测是 `p2001_real_nodes_test` 的 include/link 段。<br>③`72:` 与 `92:` "descriptor（module_adapters.cpp:445-460 p3_writer_descriptor）"——实测 `p3_writer_descriptor` 在 **811-828**（`grep -n 'ModuleDescriptor p3_writer_descriptor()'`） |
| 与上位冲突 | 是：§8.4 条款号错；`§5` execution_class=`io`/parallel_ok=False 与注册面 `module_adapters.cpp:816-817` **一致** ✓、端口表 `64-65` resampled/fits 与 `:819-820` **一致** ✓（与 phase2 家族背离形成对照：**同一目录内一半抄对、一半抄错**，正是抄件特征）；`51:` "SCI-P3 §96 面"引用形式无法在 `docs/science/PHASE3_HIPS_TO_FITS.md`（236 行）按节定位（该文件无 §96；SCI 侧编号在 §9a-*），属指向不存在的小节号 |
| 处置建议 | **保留（正本）+ 就地清锚**：删 §9 判据展开（指回 ALG §12）、修正 `p3_wcs.h` 路径与行数、CMakeLists 行号、descriptor 行号、SCI §96 节号（理由：§3"每份下层文档标明上游条款…追溯不到"＋§4"源码行号放下层"） |

### 1.13 docs/modules/registry/module_id_migration_baseline.json

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/registry/module_id_migration_baseline.json` |
| 标题 | 无（JSON 无标题；机械列 h1="" ✓） |
| 行数 | 37（机械列 37 ✓） |
| 角色 | **不是"模块工作细节"**：它是 **过程台账/门禁棘轮基线**（`13: "rule": "烧毁式基线：每类只许下降不许上升…两类归零后删除本文件"`）。标准 01 §2 第三条明定"历史档案、过程台账、已撤销设计不放在正式文档层；需要留证的台账落 `artifacts/evidence/` 并由正本引用"→ **角色错放**，应迁往 `artifacts/evidence/<门禁域>/module_id_migration_baseline.json`（由 `docs/ci/01_CHECKS.md` 与 `eng/ci/checks.json` 注册项引用） |
| 主题 | `check_module_id_normalization.py` 三类错误的下降基线 + 模块数登记值 |
| 上游 | **缺**（JSON 无上游条款字段；`4: "method"` 只写复算命令，`14: "owner": "MOD-002（LEDGER-DOC 域）；CI 注册转 W4-A3"` 是任务编号不是设计条款）。前言 §4：没标就写"缺"，标准 01 §3 要求每份下层文档标明上游条款——若按 §2 迁出正式文档层，该要求由 `docs/ci/` 侧承载 |
| 下游 | 8 个引用者（检索词 `module_id_migration_baseline.json`）：`eng/tools/quality/check_module_id_normalization.py:6` `BASELINE=os.path.join(REGDIR,"module_id_migration_baseline.json")`、`eng/tools/quality/check_module_map.py:318/324`（读 `index_module_count`）、`eng/ci/checks.json:12926`、`docs/ci/01_CHECKS.md`、`docs/DOCUMENT_INDEX.yaml:1025-1029`、`docs/modules/registry/astrocs.phase2.upm-apply.md`、`astrocs.phase2.upm-fit.md`、`astrocs.phase3.resample.md`、`astrocs.phase3.wcs.md`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` —— **被生产代码读作输入**（属机器登记面，不是普通文档） |
| 是否正本 | **是**（该基线数值只此一份，且门真读它），但**同时是过程台账**，二者身份冲突——这是本批"第二套表"缺陷的第二个实例：`index_module_count` 与 `docs/plugins/00_INDEX.md §2` 表格行数、`docs/modules/MODULE_MAP.yaml` 条目数构成**三处模块数登记**，本文件是其中的"仲裁值"（`7: "门断言 docs/plugins/00_INDEX.md §2 解析出的模块数 == 本值，且 set(map_ids) == set(index_ids)"`） |
| 重复或重叠对象 | ①`6: "index_module_count": 23` 对 `docs/plugins/00_INDEX.md:22` "## 2. 模块总表（23 篇）"及其 23 行表格、`docs/modules/MODULE_MAP.yaml` 的 23 条 `module_id:`（现算 `awk '/^    module_id:/{print $2}' docs/modules/MODULE_MAP.yaml \| wc -l` = 23）——三处同源，本批现值一致（23/23/23 ✓，已核）。②`8-12: "classes"` 与 `15-36: "history"` 重叠同一组数字的两套陈述（阈值 vs 实测），见"与上位冲突" |
| 元信息块 | 整份文件即元信息（`baseline_id/generated_at/method/rule/owner/history` 六字段无一为设计内容）；机械列 `meta_block=False` **漏报**（JSON 无 `---` 也无中文标记，工具按 .md 头部模式判） |
| 写法违规 | 日期 ×6（机械列 `dates=[3,17,21,25,29,33]` ✓命中）：`3: "generated_at": "2026-09-17T06:15:00+08:00"` 与 history 五条 `"at"` 时戳；任务编号：`4: check_module_id_normalization.py`、`13: MOD-002 落地批`、`14: "owner": "MOD-002（LEDGER-DOC 域）；CI 注册转 W4-A3"`、`18/22/25/29/33` history 内 `P2-SAMP/P1-NOISE-DOC` 类语汇（机械列 `task_ids=[]` **漏报**，理由同 §1.2）；历史叙事：整段 `history`（`16: "建立基线 23/20"`、`21: 中面批次后下调 23/20 → 21/18`、`30: 大面步骤2…已回退`、`34: 实测下降（未下调 classes 阈值…）`）——"下调/回退/待补齐/如实登记"全套过程台账语，§4 明确禁 |
| 悬空引用 | **零文件级悬空**：`MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`eng/tools/quality/check_module_id_normalization.py`、`check_module_map.py` 均存在（逐条 `[ -f ]`）。**但有语义级失效**：`7` 声称 `set(map_ids) == set(index_ids)` —— 现算两侧均 23 且集合相等（`awk` 提取 MODULE_MAP 23 个 module_id 与 00_INDEX §2 表格 23 个模块名按 id↔短名映射核对，见 §4.2 命令与结果）✓ |
| 与上位冲突 | **是（三处，均可复算）**：<br>①`9-12 "classes"` 记 `PAGE_MISSING_MODULE_ID: 20`、`MODULE_WITHOUT_PAGE: 17`，而 `34`（history 末条）自述"实测下降…PAGE_MISSING_MODULE_ID 实测 20 → 14、MODULE_WITHOUT_PAGE 实测 17 → 11"。基线现值与自述实测不同轴（一个是阈值、一个是实测），文件里**没有字段承载现行阈值语义**，读者无法判定 20/17 是否仍是门槛；<br>②按本文件 `4: "method"` 的命令口径**现场重算**（不执行脚本，只按 `check_module_id_normalization.py:48-56` 的判定逻辑用 `grep/awk` 数）：`PAGE_MISSING_MODULE_ID = 26 个 registry .md − 10 个 frontmatter 带 module_id 的页 = **16**`（与 history 自述的 14 差 **+2**），`MODULE_WITHOUT_PAGE = MODULE_MAP 23 − 被页覆盖的 12 = **11**` ✓ 与自述一致；即台账的"实测 14"已过期、现值 16，而阈值 20 未动 → 门仍绿但台账失真；<br>③`11: "AUTHORITY_MISSING": 0` 是**恒真零**：`check_module_id_normalization.py:66` 的判定入口是 `if e.get("added_by")!="MOD-002": continue`，而 `grep -n "added_by" docs/modules/MODULE_MAP.yaml` → **0 命中**（MODULE_MAP 无该键）⇒ 该分支永不执行，AUTHORITY_MISSING 不可能非零；同期 `awk '/^    module_yaml:/{print $2}' docs/modules/MODULE_MAP.yaml` 逐条 `[ -f ]` → **5 条不存在**（`lib/infrastructure/{aio,scheduler,benchmark,observability,hips_browser}/module.yaml`）。一个永不红的门 + 一个登记为 0 却实测应有 5 的类别。标准 01 §5 与 AGENTS §9"恒真门没有证据资格"直接命中 |
| 处置建议 | **迁移** → `artifacts/evidence/gates/module_id_migration_baseline.json`，并把 `history` 六段删除、`classes` 三值改为现行实测（16/11/5 或补 `added_by` 后重算）、`generated_at` 去掉；同时在 `docs/ci/01_CHECKS.md` 与 `eng/ci/checks.json` 侧引用（理由：标准 01 §2 第三条"过程台账…需要留证的台账落 artifacts/evidence/ 并由正本引用"）。**注意**：迁移动作会改 `check_module_id_normalization.py:6`、`check_module_map.py:318` 与 `eng/ci/checks.json:12926` 三处硬编码路径，属跨层变更，需按变更流程走（本审计只登记不改） |

<!-- PROGRESS: 已审 13/25 -->

### 1.14 docs/modules/snr_estimator.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/snr_estimator.md` |
| 标题 | `Module: snr_estimator`（H1:1，英文体例，与同目录中文体例不一致） |
| 行数 | 53（机械列 53 ✓） |
| 角色 | **模块工作细节**，落位 `docs/modules/` ✓ 一致，**不错放** |
| 主题 | 噪声/SNR 模块摘要页 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）` —— 条款号错（§8.4=顶层结构，模块与 ABI=§8.5）；该模块真正的上游是 §2.2/§4.2/§4.4（同批 `docs/plugins/algorithms_phase1/07_noise_snr.md:12` 标的就是这三条），本页只标 ABI 节 |
| 下游 | 5 个引用者（`git -C … grep -l -F snr_estimator.md -- . \| grep -v -x -F docs/modules/snr_estimator.md`）：`docs/DOCUMENT_INDEX.yaml`、`docs/audit/doc_classification.csv`、`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv`、`reports/v19r2/file_audit_inventory.csv`、`reports/v19r2/source_manifest.csv` —— **无任何现行设计文档或代码引用它**（除索引与审计台账），半孤儿 |
| 是否正本 | **否**：`49-52:` 自述"本页为 ACTIVE_INFORMATIVE 摘要。模块合同权威 = lib/algorithms/noise_snr/README.md（r1，CONTRACT_READY）+ module.yaml…逐符号源码锚定与 DISP-NOISE-001..009 登记见 docs/algorithms/NOISE_ESTIMATION.md §13" —— 自己声明不是正本；链条核对通过：`docs/algorithms/NOISE_ESTIMATION.md:115 ## 13 模块合同冻结增补（实现锚定 / DISP-NOISE / TEST-NOISE-DESIGN-001）` ✓ |
| 重复或重叠对象 | **与同批 `docs/plugins/algorithms_phase1/07_noise_snr.md` 同角色同主题重叠**（标准 01 §2 把 `docs/modules/` 与 `docs/plugins/` 划为**同一角色**）：<br>· 本页 `7-8:` "三层噪声模型：PhotometricCalibrationQuality / PsfFitQuality / **NoiseWeightModelV1（空背景稳健方差 → ivar）**" ↔ 07 `136:` "**噪声模型 A 为唯一生产模型**：实现 `lib/algorithms/noise_snr/cpp/src/noise_model.cpp`，接口 `snr_noise_model_v1` / `_f64` / `_fill` + **`NoiseWeightModelV1`（逐像素）**，编入根 CMakeLists.txt:767-770 的 `astrocs_phase1_noise`（STATIC）"。差异：本页只给对象名，07 给实现文件+接口+构建位置。<br>· 本页 `12:` "q_psf 与 sigma_cal 不进 science weight" ↔ 07 `132:` "PSF 拟合质量代理（FWHM、残差尺度等）只作诊断，**不计入科学叠加权重**"。<br>· 本页 `32:` "无合格 patch → NO_DATA/fallback" ↔ 07 `159:` "`n < min_keep + 1` 时判据**不武装**…按不可用登记"（同一边界两套措辞） |
| 元信息块 | **有**：`47-53:` `---` 之后整段是状态与纪律声明（"本页为 ACTIVE_INFORMATIVE 摘要""冲突时以冻结合同为准""**噪声模型 A 为唯一生产模型**"）；机械列 `meta_block=False` **漏报** |
| 写法违规 | `50:` 状态编号 `ACTIVE_INFORMATIVE`（文档自述索引状态词）；`53:` "**噪声模型 A 为唯一生产模型**" —— 在本层声明"唯一"口径（§1"任何文档不声明自己的第二套权威顺序或'唯一口径'"，该口径正本在 `docs/science/NOISE_MODEL.md`）；`36:` "SCI-NOISE-001..015；ALG-NOISE-001..003" **核对通过**（`git grep -o "SCI-NOISE-0[0-9]*"` 得 001–015 全存在；`ALG-NOISE-001..003` 在 `docs/algorithms/NOISE_ESTIMATION.md` 各 6/4/2 次）；无日期/commit/sha256/历史叙事（机械列 ✓） |
| 悬空引用 | `16:` "snr_estimator DLL" —— **不存在该 DLL**：MODULE_MAP 登记 `target: astrocs_p1_noise_snr`（`:234` 附近）、`lib/algorithms/noise_snr/module.yaml:38 dll_name: astrocs_p1_noise.dll`，而根 `CMakeLists.txt` 中 `astrocs_p1_noise_snr` **零命中**（`grep -n astrocs_p1_noise_snr CMakeLists.txt` → 无输出），SHARED 目标 `astrocs_p1_noise` 在根图被注释（`CMakeLists.txt:323` "astrocs_p1_noise target 随之不在根图"、`:332` "本 add_subdirectory 仍**保持注释**（未恢复）"）。文件名 `snr_estimator` 本身是迁移前旧目录名（MODULE_MAP noise_snr 条目 note："源 = lib/snr_estimator + lib/phase1/noise 薄包装（ARCH-001 已迁入 target_dir）"）。文件级 `lib/algorithms/noise_snr/cpp/` ✓ |
| 与上位冲突 | **是**：①上游条款号错；②"snr_estimator DLL" 与注册面（MODULE_MAP target 名 + 根 CMake 实况）冲突；③与同批 07 的三处口径双写，而本页无任何指针指向 07（读者无从知道有第二份） |
| 处置建议 | **删除**（去向：`docs/plugins/algorithms_phase1/07_noise_snr.md`；本页全部有效信息已在 07 与 `lib/algorithms/noise_snr/README.md`+`module.yaml` 中更完整地存在）。理由：§2"同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除"。删除后按 §3 同步 `docs/DOCUMENT_INDEX.yaml` 与 `docs/audit/doc_classification.csv` |

### 1.15 docs/modules/star_detector.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/modules/star_detector.md` |
| 标题 | `Module: star_detector`（H1:1） |
| 行数 | 93（机械列 93 ✓） |
| 角色 | **模块工作细节**，落位 `docs/modules/` ✓；但 `14-20:`（peaker 七步、Moffat4 GSL TR-LM 7 参、`R=ceil(3.7172·S)`）与 `57-61:`（OpenMP 四处）属**算法推导**层，正本在 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md`（437 行，本页 `:75` 自认） |
| 主题 | Phase1 源检测模块摘要页（含权威路径声明） |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`（条款号错）；正文另有 `14-15:` "本实现 = 最高设计 §4.2 所述全图盲检测路径的生产源；**权威检测范式 = 星表引导拟合**…见 `ASTROCS_DESIGN.md` §4.2" ✓（§4.2 = 节点流程，`ASTROCS_DESIGN.md:254`） |
| 下游 | 4 个引用者：`docs/DOCUMENT_INDEX.yaml`、`docs/audit/doc_classification.csv`、`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv`、`reports/v19r2/*`。**同批 `03_star_detection.md` 不引用它，而它单向引用 03**（:68）→ 模块层页把插件层页当权威，层级倒置（§1 权威链自上而下） |
| 是否正本 | **否**：`5-10:` 自述"本页为诊断页（ACTIVE_INFORMATIVE，不属 gen_module_readmes.py 生成范围）。模块冻结合同页 = docs/modules/registry/astrocs.phase1.star-detection.md…本页以冻结合同为准，改动一律从合同层发起" |
| 重复或重叠对象 | **与同批 `docs/plugins/algorithms_phase1/03_star_detection.md` 同角色同主题重叠**：<br>· 本页 `14-15:` "**权威检测范式 = 星表引导拟合**——检测定义域是星表位置、用本帧 WCS 反向投影 Gaia 星表…全图盲检测连通域**不是**权威路径" ↔ 03 `25:` "**权威检测范式 = 星表引导拟合**（最高设计 §4.2）：检测定义域是**星表位置**（用**本帧已解出的权威 WCS**把 Gaia 星表反向投影到像素域）…**全图盲检测连通域路径不是权威路径**"。<br>· 本页 `38-40:` "统计量 `SDetGuidedStats{n_predicted,n_dropped,n_fit_failed,n_rejected,n_fit_ok,n_output}`…`n_pred=0` 返回 rc=0 + count=0（空定义域非错误）；指针参数非法返回 −1" ↔ 03 `35:` 表内 "统计量 `SDetGuidedStats{n_predicted,n_dropped,n_fit_failed,n_rejected,n_fit_ok,n_output}`"（同一结构体六字段逐字两处）。<br>· 本页 `29-33:` 10 导出符号全集 ↔ 03 `35-36:` 只列 3 个入口符号（差异 = 符号全集 vs 路径子集） |
| 元信息块 | **有**：`5-10:` 状态+纪律声明（"不属 gen_module_readmes.py 生成范围""以冻结合同为准，改动一律从合同层发起"）；机械列 `meta_block=False` **漏报** |
| 写法违规 | `5-6:` 状态词与生成器纪律声明；`60-61:` "ThreadBudget 接线与取消检查点缺失已登记（DISP-STAR，整改归 P1-STAR-IMPL）"；`84-86:` "可执行 TEST-P1-STAR-001 由 P1-STAR-TEST 落地（现状无共址测试套件…）"；`91-93:` "wrapper_phase1/（P1-003 桥接层…matrix legacy_paths 第二路径，整合归 P1-STAR-IMPL）"；无日期/commit/sha256（机械列 ✓）；机械列 `task_ids=[]` **漏报**（DISP-STAR/P1-STAR-IMPL/P1-003 不匹配 `TASKID_PAT`） |
| 悬空引用 | ①`91-92:` "合同头 `lib/include/star_detector.h`" —— 该路径**不存在**（`[ -f lib/include/star_detector.h ]` 假），而同页 `32:` 写"签名头正本 `lib/algorithms/star_detection/include/star_detector.h`"（实测 110 行 ✓）→ **同页两个头文件路径，一个悬空**。<br>②`52-53:` "唯一经 `sdet_free_detect_ex` 整组释放（sdet_api.cpp:2357-2372）" —— 实测该符号定义在 `src/sdet_api.cpp:2946`；:2357-2372 实测是"阶段9 饱和星跳过 / 阶段10 去重排序"（`:2357 // 饱和星现在直接从 peaker 候选中识别`、`:2364 sdet_dedup_stars(stars);`）。<br>③`70:` "退出码 STAR_DETECT_FAILED（orchestrator.cpp:2200-2212）" —— 实测赋值在 `lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp:2218`，:2200-2212 是 u16 转换与 `fn_sdet_ex` 调用，区间不含该语句。<br>④`90:` "src/sdet_api.cpp:1599-2353 sdet_detect_impl" —— 实测 `static int sdet_detect_impl(` 在 **:1738**。<br>文件级 ✓：`docs/architecture/api_inventory.csv`（449）、`test/sdet_fp64_test.cpp`（102）、`tests/p1star/`、`DATA_SEMANTICS.md §17`（:874） |
| 与上位冲突 | **是**：①上游条款号；②规模控制两处不同源：本页 `18-19:` 以 `SDetParams.maxStars` 为截断控制，03 `58/64:` 登记为 `star_detection.max_stars` 合同域 [20000,50000]；③**同一模块在 docs 层三份页面**：`docs/modules/star_detector.md`（本页）、`docs/modules/registry/astrocs.phase1.star-detection.md`（本页 `:6` 自认那份是"模块冻结合同页"）、`docs/plugins/algorithms_phase1/03_star_detection.md`（`MODULE_MAP` 的 plugin_doc） |
| 处置建议 | **删除**（去向：实现/算法细节并入 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md`，接口与边界并入 `docs/plugins/algorithms_phase1/03_star_detection.md`）。理由：§2 一主题一正本＋§4"公式、数值表、源码行号放对应下层文档"——本页 4 处源码行锚已全部失效，摘要又不独立成立 |

<!-- PROGRESS: 已审 15/25 -->

### 1.16 docs/plugins/00_INDEX.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/00_INDEX.md` |
| 标题 | `Astro Celestial Sphere Database（ACSD） 插件文档集（docs/plugins）`（H1:1） |
| 行数 | 112（机械列 112 ✓） |
| 角色 | **模块工作细节层的集索引**（§2 角色表无"集索引"，就近归入 `模块工作细节`；落位 ✓）。同时 §3/§4/§5（模板、必答问题、维护规则）是 **标准与检查** 角色 → **一文两角色**，违反 §2"每份文档属于且仅属于一个角色" |
| 主题 | 插件文档集结构、模块总表、统一模板、维护规则、三创新点指引 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §0.2（详细文档层与双向索引）` ✓ —— **本批 25 份中唯一一份上游条款号与标题完全匹配的文件**（`ASTROCS_DESIGN.md:47` = "### 0.2 详细文档层与双向索引"） |
| 下游 | 12 个引用者：`README.md`、`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、`docs/modules/registry/module_id_migration_baseline.json`、`docs/plugins/infrastructure/19_runtime.md`、`eng/tools/quality/check_module_map.py`、`eng/tests/quality/test_module_map.py`、`eng/tools/quality/fixtures/module_map_fixture.py`、`artifacts/evidence/governance-01/research/DOC-SCI-001_插件文档与科学权威冲突裁决.md`、`reports/v19r2/*` |
| 是否正本 | **是**（插件文档集集索引正本），但它与 `docs/DOCUMENT_INDEX.yaml`（§3 全文档集唯一索引）、`docs/modules/MODULE_MAP.yaml`（模块↔plugin_doc 机器登记）构成**同一"模块↔文档"映射的三处登记**；本文件是其中被**机器解析**的那份：`eng/tools/quality/check_module_map.py:17`（"M1 …ID 集合 == docs/plugins/00_INDEX.md §2 机器解析结果"）、`:296`（"从 §2 的模块总表机器解析 23 个模块 ID（表第 2 列）"）、`:318-324`（模块数登记值取自 `module_id_migration_baseline.json#index_module_count`） |
| 重复或重叠对象 | ①§2 的 23 行 ↔ `MODULE_MAP.yaml` 的 23 条 `plugin_doc`（例 `MODULE_MAP.yaml:54 plugin_doc: docs/plugins/algorithms_phase1/01_calibration.md` ↔ 本页 `30: \| \`01_calibration.md\` \| calibration \| …`）↔ `DOCUMENT_INDEX.yaml:1094+` 的插件文档登记——三表同源，**当前逐项一致**（复算见 §4.2）；②`106-112:` 三条创新点重述 ↔ `ASTROCS_DESIGN.md` §2.1（`:120`）/§2.2（`:130`）/§2.3（`:193`）——用一行摘要重述上位口径；③`73-82:` 八节模板 ↔ `check_module_map.py` 的 §3 模板判据（同一模板两处定义，机器侧为 M2/M3 输入） |
| 元信息块 | 无（机械列 `meta_block=False` **此处正确**） |
| 写法违规 | 无日期、无任务编号、无 commit、无 sha256、无历史叙事（机械列全空，复算亦空）✓。唯一可记：`22:` 把规模数字写进标题（"## 2. 模块总表（23 篇）"），该数与 `module_id_migration_baseline.json:6 index_module_count=23` 及 M1 门强耦合（数字入正文属 §4"数值表放对应下层文档"的边缘形态） |
| 悬空引用 | 无文件级悬空：`88:` `docs/design/UNIFIED_MODEL.md` 存在（76 行）✓；`18:` `lib/algorithms/`、`lib/infrastructure/cli/{normalize,mosaic,export}` 存在 ✓。**一处口径性失实**：`53:` 把 projection 职责写成"投影 registry（**内置** TAN/SIN/CAR/AIT/STG/MOL/CEA/ZEA）"，而注册表把 8 码分三态（`lib/algorithms/projection/p3_projection_registry.h:70-84` TAN=`kProductDeclarable`、SIN/CAR/AIT=`kKernelOnly`、STG/MOL/CEA/ZEA=`kNotImplemented`；`:122/:140` 声明集与实现集各 1 项 = TAN） |
| 与上位冲突 | **是**：`53:` 的"内置 8 种"与同批 `astrocs.phase3.wcs.md:56-60`（"设计冻结 8 种…当前登记：仅 TAN 已实现（D = I = {TAN}）"）及代码事实相反；`docs/DOCUMENT_INDEX.yaml:1172-1174` 的 14_projection duty 复制了同一过强表述。§2 表只有第 2 列受机器校验，第 3 列（职责句）无门 → 错误列无人守 |
| 处置建议 | **保留（正本）+ 两处动作**：①`53:` 改"设计冻结 8 码，产品声明集当前仅 TAN"，与代码及 registry 页对齐；②把 §3/§4/§5（模板与规则）拆往 `docs/standards/`（或 `docs/ci/`）使一文一角色成立（§2）。**不建议**并入 DOCUMENT_INDEX：§2 是 `check_module_map.py` 的解析输入，合并等于换掉 P0 门的输入面 |

### 1.17 docs/plugins/algorithms_phase1/01_calibration.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase1/01_calibration.md` |
| 标题 | `插件文档：calibration（定标）`（H1:1） |
| 行数 | 68（机械列 68 ✓） |
| 角色 | **模块工作细节**，落位 `docs/plugins/algorithms_phase1/` ✓（§2 目标目录含 `docs/plugins/`），**不错放** |
| 主题 | 定标（偏置/暗流/平场 + 方差传播） |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §4.2（Phase1 节点流程）` ✓（`:254`）；`12:` §4.6（硬约束）✓（`:359`） |
| 下游 | 8 个引用者：`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml:54`（`plugin_doc` 机器登记）、`docs/plugins/00_INDEX.md:30`、`eng/packaging/config/config_registry.json`（含 `01_calibration.md:48` 行锚）、`artifacts/evidence/governance-01/research/DOC-SCI-001_*.md`、`实验/absolute-snr/docs/snr-propagation-design.md`、`reports/v19r2/*` |
| 是否正本 | **是**（calibration 插件层唯一页，`MODULE_MAP` 的 plugin_doc） |
| 重复或重叠对象 | `24-34:` 公式段与 `docs/science/NOISE_MODEL.md`（412 行）、`docs/design/PHASE1_DETAILED_DESIGN.md`（227 行，`14:` 自引 §4）重叠：本页 `27:` `y_p = [r_p − b_p − alpha (d_p − b_p)] / f_p`、`33:` `V(y_p) = {V(r_p)+V(b_p)+alpha²[V(d_p)+V(b_p)]+y_p²V(f_p)} / f_p²`。`00_INDEX.md:78` 规定 §4"关键公式（引用权威推导，**不重复展开**）"——本页给可直接代入的完整式，与自身模板条款相冲；是否与科学层逐字重复未逐行核 `NOISE_MODEL.md`，按前言 §5.5 判**重叠、不判重复** |
| 元信息块 | 无 ✓ |
| 写法违规 | 机械列全空 ✓，复算亦空。可记：`30:` "独立近似下方差传播（**目标态**）"——目标态与现行混写同节，读者无法判断哪条是现行口径（§4"只写现行设计要怎样"） |
| 悬空引用 | `20:` `eng/contracts/schemas/data_light.schema.json`、`eng/contracts/schemas/calibration_output.schema.json` **均不存在**（`ls eng/contracts/schemas/*.schema.json` 全集 18 项 + `unified/` 无此二名）；机械列 dangling_links 命中 ✓；缺口已在 `docs/modules/MODULE_MAP.yaml:779-780` 以 `declared_absent_paths` 登记（owner BLD-401，reason："模块输出 schema 未落地（plugin 文档声明的 legacy 名；现行 canonical 对象 schema = eng/contracts/schemas/unified/**"）——**但页面自身不声明这是已登记缺口**，读者按链接不可达且无提示 |
| 与上位冲突 | **是（键名两套）**：§5 表 `bias_path/dark_path/dark_exposure/flat_path/gain/read_noise/clip_negative`，合同面 `phase_config_normalize.schema.json` 的 `normalize_block.properties` 用 `master_bias/master_dark/master_flat/dark_scale_factor/dark_optimization/master_units/master_scale`（JSON 解析实测）→ 同一模块两套键名，页面无一句说明哪套是 CLI 实际接受的键 |
| 处置建议 | **保留（正本）+ 两处最小改动**：§3 参考行改指 `eng/contracts/schemas/unified/**`（或就地标缺口并回指 MODULE_MAP 登记条目），§4 公式段改指针式（§1"公式推导下沉…每节末尾给出索引指针"＋§3"沿途无悬空链接"） |

### 1.18 docs/plugins/algorithms_phase1/02_cosmetic.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase1/02_cosmetic.md` |
| 标题 | `插件文档：cosmetic（坏点/宇宙线修正）`（H1:1） |
| 行数 | 55（机械列 55 ✓） |
| 角色 | **模块工作细节**，落位 ✓，不错放 |
| 主题 | 坏点/热像素/宇宙线修正与 validity |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §4.2` ✓ |
| 下游 | 7 个引用者：`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md:31`、`eng/packaging/config/config_registry.json`（含 `:33/:34/:35` 三个行锚）、`artifacts/evidence/governance-01/research/DOC-SCI-001_*.md`、`reports/v19r2/*` |
| 是否正本 | **是**（cosmetic 插件层唯一页） |
| 重复或重叠对象 | 默认值三处登记：本页 `35-37:` `cr_detection=true`/`cr_sigma=5.0`/`interpolation=neighbor` ↔ 合同面 `$defs.cosmetic` 描述"（cosmetic.enabled 等；值类型与默认见 docs/contracts/DATA_SEMANTICS.md §16.1）"（即合同面把权威推给 DATA 文档）↔ `eng/packaging/config/config_registry.json` 为这三键各建登记项并把 `02_cosmetic.md:33/:34/:35` 作 `conflict.evidence` 锚。另 `27:` "修正写入后，对应像素的 variance 必须同步更新（插值处方差按插值权重传播，原方差随之被替换）" ↔ `docs/science/UNCERTAINTY_AND_COVARIANCE.md`（163 行，`14:` 自引）同一口径两处 |
| 元信息块 | 无 ✓ |
| 写法违规 | 机械列全空 ✓，复算亦空；`47:` "**修正一律留可辨认痕迹**"、`48:` "零值填充只作具名降级" 为正向硬约束（§4 允许） |
| 悬空引用 | `20:` `eng/contracts/schemas/cosmetic_output.schema.json` 不存在（机械列 ✓；`MODULE_MAP.yaml:781` 登记缺口）。`eng/packaging/config/config_registry.json` 的三个 `conflict.evidence` 锚**逐条错位**：登记 `cr_detection` 的锚写 `:33`（实测 :33 = `cosmetic_map_path` 行，真行 :35）、登记 `cr_sigma` 的锚写 `:34`（实测 :34 = `saturation_level` 行，真行 :36）、登记 `interpolation` 的锚写 `:35`（实测 :35 = `cr_detection` 行，真行 :37）——**一致 off-by-2**（命令：取锚与所属 field 用 `grep -n "02_cosmetic.md:3[3-7]" -B22 eng/packaging/config/config_registry.json` 再看 `field` 行；取真行用 `awk "NR>=33 && NR<=37" docs/plugins/algorithms_phase1/02_cosmetic.md`） |
| 与上位冲突 | **是**：§5 五键 `cosmetic_map_path/saturation_level/cr_detection/cr_sigma/interpolation` 在合同面 `$defs.cosmetic` 中**无任何 properties 声明**（实测 `{"type":"object","description":…}`）；其中 `saturation_level` 实际归属合同面的 `noise` 段（`$defs.noise_config.properties` 含 `saturation_level`）→ **同一参数两处归属** |
| 处置建议 | **保留（正本）**，在 §3/§5 补一行键归属澄清（"配置键合同面 = `$defs.cosmetic`，当前不逐键约束；`saturation_level` 归 `noise` 段"），消除双归属（§2 一主题一正本 + §3 可追溯） |

<!-- PROGRESS: 已审 18/25 -->

### 1.19 docs/plugins/algorithms_phase1/03_star_detection.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase1/03_star_detection.md` |
| 标题 | `插件文档：star_detection（源探测）`（H1:1） |
| 行数 | 132（机械列 132 ✓） |
| 角色 | **模块工作细节**，落位 ✓；`25-29:` 与 `§4`（阈值式、矩、selection function）属算法推导层（自认正本 `docs/science/STAR_DETECTION.md` 182 行、`docs/algorithms/STAR_DETECTION_ALGORITHMS.md` 437 行） |
| 主题 | 源探测（权威 = 星表引导拟合）合同 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §4.2` ✓；`12:` §4.6 ✓、§2.1 ✓（`:120`） |
| 下游 | 23 个引用者（本批第二多）：**`ASTROCS_DESIGN.md`（最高设计直接下引本下层文档）**、`ACCEPTANCE_SPEC.md`、`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、`docs/modules/star_detector.md:68`、`docs/plugins/00_INDEX.md:32`、`docs/research/PHOTOMETRY_RESEARCH_PACK.md`、`eng/packaging/config/config_registry.json`、`artifacts/evidence/doc-hygiene/baseline.json`、`reports/v19r2/*` |
| 是否正本 | **是**（star_detection 模块工作细节层正本、`MODULE_MAP` 的 plugin_doc；`§7.1` 被同批 `star_detector.md:68` 指为 fail-closed 语义的权威源） |
| 重复或重叠对象 | ①与同批 `docs/modules/star_detector.md` 同角色重叠（逐段举证见 §1.15）；②`26:` 阈值式与 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md` 重叠（本页给实现锚 `sdet_api.cpp:1782-1792`，实测式在 **:1779** `T threshold = img_median + T(5.0) * bgnoise;`，漂 3 行）；③`52-56:` 声明"合同声明 = `eng/contracts/schemas/phase_config_normalize.schema.json #/$defs/star_detection_config`…段内未知键被 schema 拒（`additionalProperties: false`）"——与 `§5.2` 表冲突（见悬空引用②） |
| 元信息块 | 无 ✓ |
| 写法违规 | 机械列 `history_narrative=[108, 128]` **命中且准确**（两行都含"留痕"，HIST_PAT 命中）：`108:` "`catalog_guided` DATA 拒绝；`auto` 显式降级并留痕"、`128:` "…`blind_diagnostic`/`auto` 的绿例与留痕断言…"。另：`96:` "局部噪声自适应为目标态、当前未实现（GAP 登记），文档按现状描述"（现状/目标态混写）；`107:` "…本条判据即静默部分装载事故的封堵点"（事故过程叙事，机械列未记——该行无 HIST_PAT 词，属**语域问题非工具漏报**）；`12:` 用通配转引（"硬约束转引 `docs/plugins/algorithms_phase1/**` 与 `docs/science/**`"）——不可核对的指针；`112:` "本条判据即静默部分装载事故的封堵点"属事故过程叙事 |
| 悬空引用 | ①`21:` `eng/contracts/schemas/source_catalog.schema.json` 不存在（机械列 ✓；`MODULE_MAP.yaml:783` 登记缺口）。②**§5.2 四键在合同面 0 命中**：`detection_threshold`/`min_area`/`deblend`/`selection_function` 在 `phase_config_normalize.schema.json` 全文计数为 0（JSON 解析），而同一页 `52-56:` 声明 `star_detection_config` 的 `additionalProperties=false`（实测该 def `properties` 恰 10 键：`approx_wcs/gaia_data_dir/limiting_mag/limiting_mag_max_iter/limiting_mag_safety/max_stars/mode/parity/query_radius_factor/rotation_deg`）⇒ **§5.2 的键若写进 `star_detection` 段会被本页自己声明的合同面拒绝**（页内自相矛盾 + 与合同面冲突）。③`58:` "拟合样本上限 = `photometry.fit.max_stars`（默认 5000…）合同域正本 = `phase_config_normalize.schema.json`"：代码侧成立（`module_adapters.cpp:5531 const int max_stars = p1_int(fit_cfg, "max_stars", 5000);`），**合同面不成立**（`$defs.photometry` 只有 `{"type":"object","description":"…（fit.enabled/…/max_stars）…"}`，无 `fit`/`max_stars` 属性 ⇒ 所指"正本"里没有该键的域）。④`12:` 三个**行号锚**（`docs/science/STAR_DETECTION.md:18-19`、`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:36`、`docs/algorithms/GATES_AND_TOLERANCES.md:39`）指"检测阈值的冻结定义"——指路用行号而非节号，同批已证此类锚必漂（§1.23/§1.24）。文件级 ✓：`tests/p1star/p1star_guided_test.cpp`（310）、`p1stardet_node_gate_test.cpp`（583）、`eng/ci/check_registry_ir_parity.py`（642，其 `:285` 确为"C4 无幻边 / C5 序为注册表 DAG 拓扑序 / C6 psf 在 wcs 之后 / C7 非退化"，`:565` 为"正例 + 4 类负例"）⇒ `130-132:` 的断言与实现一致 ✓ |
| 与上位冲突 | **是**：①`58/64:` 与 schema 注释（`:392` "本 schema 的 minimum/maximum 即唯一事实源"）对"谁是 `max_stars` 合同域正本"口径不同（§1 禁第二套权威）；②§5.2 键集与 §5.1 声明的合同面互斥（上一条②）；③`eng/packaging/config/config_registry.json:313-318` 把本页 `min_area/deblend/selection_function` 判为 `finding: "gap"`（"未进任何配置类"）——即机器面认定这些键**没有登记面**，而本页把它们当现行配置项列出 |
| 处置建议 | **保留（正本）+ 三处修**：①§5.2 四键要么进合同面/defaults 登记，要么删除并注明只在 `eng/packaging/config/defaults.json#fields[].key=detection.threshold_sigma`（实测该键存在且 value=5.0，与本页 `84:` 一致 ✓）；②行号锚全改节号；③`photometry.fit.max_stars` 的合同域补进 schema 或改指代码常量。理由：§5"沿途无悬空链接"＋§3"追溯不到的机制先补要点再保留" |

### 1.20 docs/plugins/algorithms_phase1/04_psf.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase1/04_psf.md` |
| 标题 | `插件文档：psf（PSF 建模）`（H1:1） |
| 行数 | 55（机械列 55 ✓） |
| 角色 | **模块工作细节**，落位 ✓，不错放 |
| 主题 | 空间变化 PSF 建模与信息核 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §4.2` ✓；`8/48:` §3.1（数据对象）✓（`:221`） |
| 下游 | 8 个引用者：`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md:33`、`eng/packaging/config/config_registry.json`（含 `04_psf.md:33` 锚）、`artifacts/evidence/governance-01/research/DOC-SCI-001_*.md`、`reports/v19r2/*` |
| 是否正本 | **是**（psf 插件层唯一页） |
| 重复或重叠对象 | `19/27:` 的 `A_NEA = 1/ΣP²` 与 `W_psf = a²PᵀC⁻¹P = 1/Var(F_hat)` ↔ 同批 `07_noise_snr.md:33-34` 代码块（`W_psf,k = a_k² P_kᵀ C_k⁻¹ P_k = 1/Var(F_hat_k)`；`白噪声: … = a_k² / (σ_pix,k² A_NEA,k)`）与 `06_photometry.md:25`（`Var(F_hat)=1/W`）——**同一式三处**（两处在本批）。差异：本页不带逐帧下标与白噪声展开。正本自认 `docs/science/PSF_SIGNAL_WEIGHT.md`（`13:`）；另 `34:` "检测侧椭圆高斯归属 star_detection，见 docs/science/STAR_DETECTION.md" 与同批 `03:35` "椭圆高斯拟合（与盲检测同一 `sdet_gauss_fit`/GSL TR-LM 7 参）" 同一归属两处陈述 |
| 元信息块 | 无 ✓ |
| 写法违规 | 机械列全空 ✓，复算亦空。`34:` "（当前实现：椭圆 Moffat4 7 参数…）" 属边界澄清，不算历史叙事 |
| 悬空引用 | `21:` `eng/contracts/schemas/psf_output.schema.json` 不存在（机械列 ✓；`MODULE_MAP.yaml:785` 登记缺口）。锚错位：`eng/packaging/config/config_registry.json:394-406` 登记 `field: psf_spatial_order`（真行 :35）却把 `conflict.evidence` 写成 `docs/plugins/algorithms_phase1/04_psf.md:33`，实测 :33 是表分隔符 `|---|---|---|---|`（off-by-2 且落在非内容行） |
| 与上位冲突 | **是**：§5 四键 `psf_model/psf_spatial_order/psf_uniformity_gate/fit_residual_gate` 在合同面只有异名的 `algorithm_psf_model`（顶层），其余三键 0 命中 ⇒ 与 §1.17/§1.21 同形态（文档配置表 ≠ 合同面）。`00_INDEX.md:33` 的职责句与本页 `7:` 一致 ✓ |
| 处置建议 | **保留（正本）**，与 §1.17 同批处理（§3 参考改 unified；§5 键名与合同面合流；请 owner 重锚 `config_registry.json`）。理由：§3 双向可追溯 |

### 1.21 docs/plugins/algorithms_phase1/05_platesolve.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase1/05_platesolve.md` |
| 标题 | `插件文档：platesolve（天体测量/WCS）`（H1:1） |
| 行数 | 57（机械列 57 ✓） |
| 角色 | **模块工作细节**，落位 ✓，不错放 |
| 主题 | 天体测量解算与 ICRS WCS |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §4.2` ✓；`12:` §4.6 ✓ |
| 下游 | 10 个引用者：`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md:34`、`eng/packaging/config/config_registry.json`（**四个行锚：`:33` ×2、`:35`、`:36`**）、`docs/plugins/infrastructure/22_gaia_xpsd_client.md`（与本页 `15:` 互为引用）、`reports/v19r2/*` |
| 是否正本 | **是**（platesolve 插件层唯一页，`MODULE_MAP` plugin_doc） |
| 重复或重叠对象 | 与别批 `docs/modules/plate_solve.md`、`docs/modules/registry/astrocs.phase1.wcs-platesolve.md` 构成同模块三页（本批只登记重叠，不代下结论）。本页 `19-21:`（"生产节点 `astrocs.phase1.wcs-platesolve` 按帧读校准后像素自行做星点检测与匹配，**不消费** `star_detection` 节点的星表 ⇒ 本节点在节点序上先于 `star-psf`；近似指向由 `wcs.init_source`（`header_pointing`/`config`/`neighbor_crval`）给出…权威 WCS 是求解器…唯一输出"）与同批 `03_star_detection.md:40-42`（先验来源三级优先：`approx_wcs`/本帧 `p1_wcs.json`/`wcs.init_source`）**同一机制两处登记**，两页对"谁先谁后、谁消费谁"各自完整陈述而非互相指针 |
| 元信息块 | 无 ✓ |
| 写法违规 | 机械列全空 ✓，复算亦空。`21:` "解算轮次数是求解器实现细节，不是流程语义" 与 `docs/algorithms/PLATESOLVE.md`（`14:` 自引）同一划界两处 |
| 悬空引用 | `22:` `eng/contracts/schemas/wcs_output.schema.json` 不存在（机械列 ✓；`MODULE_MAP.yaml:787` 登记缺口）。**锚错位（三条，逐条给两侧值）**：`config_registry.json:445-457` 登记 `field: catalog` 的 `conflict.evidence` = `05_platesolve.md:33`（实测 :33 = §5 表头 `| 字段 | 默认 | 单位 | 说明 |`，真行 :35）；`:482-494` 登记 `tweak_order` 的锚 = `:35`（实测 :35 = `catalog` 行，真行 :37）；`:504-516` 登记 `max_matches` 的锚 = `:36`（实测 :36 = `catalog_version` 行，真行 :38）——**一致 off-by-2，且一条落在非数据行** |
| 与上位冲突 | **是**：§5 五键 `catalog/catalog_version/tweak_order/max_matches/residual_gate` 在合同面 `$defs.wcs_config`（实测 10 键 `crval1/2`、`crpix1/2`、`cd11/12/21/22`、`gaia_data_dir`、`init_source`，`additionalProperties=false`）中**全部 0 命中** ⇒ 本页配置面与合同面互不包含；若这些键写入 `wcs` 段即被合同面拒绝 |
| 处置建议 | **保留（正本）+ 键名合流**：§5 改列 `wcs` 段真实键，或明确标注"合同面未收录（目标态）"并给 owner。理由：§1"下位与上位冲突时以上位为准；冲突本身是缺陷"（上位合同面 = `eng/contracts/**`，AGENTS §7） |

### 1.22 docs/plugins/algorithms_phase1/06_photometry.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase1/06_photometry.md` |
| 标题 | `插件文档：photometry（测光/通量定标）`（H1:1） |
| 行数 | 119（机械列 119 ✓） |
| 角色 | **模块工作细节**，落位 ✓；但 §4.1（:29-63）是**科学判据推导 + 实测数据**、§4.2（:65-69）是**外部项目引用纪律（代码只读/分发面）** → 实际同时承担 `科学定义`/`研究与佐证`/`操作与运营` → **角色混装**（§2"属于且仅属于一个角色"）。判据应迁 `docs/science/PHOTOMETRY.md`（本页自认正本），实测预算应迁 `实验/photometric-magnitude` 或 `artifacts/evidence/` |
| 主题 | PSF 域测光与通量定标（`a_k`） |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §4.2` ✓；**另有硬错**：`12:` "最高设计 §3.6（硬约束：测光）"——`ASTROCS_DESIGN.md` §3 只有 §3.1/§3.2/§3.3（`grep -n "^### 3\." ASTROCS_DESIGN.md` 三条），**无 §3.6**；测光硬约束在 §4.6（`:359`）。`99:` 引 §4.4 ✓（`:316`） |
| 下游 | 20 个引用者：`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md:35`、`eng/packaging/config/config_registry.json`（`:31`/`:32` 锚）、`docs/research/**`、`实验/**`、`reports/v19r2/*` 等 |
| 是否正本 | **就 §4.1 判据而言否**：`34:` "**判据已推导**：完整推导 + 逐项出处见 `docs/science/PHOTOMETRY.md`"、`59-60:` "σ_psfsys(frame)（算法见 `docs/science/PHOTOMETRY.md`）"——判据正本在科学层，本页是**带完整伪码与数值的抄件**（`37-44:` A6-GATE 块）。§5 表与 §7 失败作用域为本页独有，故整页不判删除 |
| 重复或重叠对象 | ①`37-44:` A6-GATE 伪码（`σ_obs = 2.5·MAD(r_inliers)/0.6744897501960817`、`σ_floor=(1−3·1.166/√n)·σ_fit`、`σ_ceiling=(1+3·1.166/√n)·sqrt(Σ预算项²)`、`PASS ⟺ σ_floor ≤ σ_obs ≤ σ_ceiling`）与 `docs/science/PHOTOMETRY.md`（本页 `34:` 自认正本）同一判据两处；②`47-55:` 逐项误差预算数值（σ_fit(白) 0.0140、σ_psfsys 0.0307、σ_color 0.0054、σ_floor 0.0122、σ_ceiling 0.0422、"L4 49 帧中位 0.0611 / 最坏 0.2428 ⇒ PASS 1/49"）＝**实验过程数据**，与 `实验/photometric-magnitude` 单元同类内容两处存放（§2"过程台账不放正式文档层"）；③`102-103:` `pixel_scaling ∈ applied/partial/none` 与 `docs/contracts/DATA_SEMANTICS.md`/`DATA-P1-PHOTPROV-001` 字段重叠 |
| 元信息块 | 无（无 frontmatter）✓ 机械列正确 |
| 写法违规 | 任务编号：`117:` 与 `119:` 两处 "`PHOT-GATE-DROP-001`：组间一致性只作报告字段"、`57:` "② Gaia XP 合成通量 `F_syn` 定标误差（本仓 `*.xpsd` 的 `magBP/magRP` 实测恒为 0…）"、`63:` "`0.03 mag` 在实现/测试/文档中一律以误差预算推导值为准"——机械列 `task_ids=[]` **漏报**（`PHOT-GATE-DROP-001` 不匹配 `TASKID_PAT`）；历史/过程叙事：`55:` "**本判据当前判红 48/49 帧 —— 如实结论，各项判据取值保持原样。**"、`59-63:` "**落地状态**：判据**尚未在代码中生效**…在 ①–④ 完成前，本模块**的测光一致性门表述 = 尚未生效**"、`118:` "**判据未在代码中生效前**…保持未生效登记"（"当前/尚未/落地状态/保持登记"全属过程语域）；台账符号：`32-34:` 行首 ⚠/✅、`114-117:` "①…④ ⇒ 必须判红" 验收清单体；`76:` 用删除线承载旧值 `~~2×FWHM~~（**值待定**）`（§4 禁"旧版/作废"形态的正字例） |
| 悬空引用 | ①`20:` `eng/contracts/schemas/photometry_output.schema.json` 不存在（机械列 ✓；`MODULE_MAP.yaml:789` 登记缺口）。②`12:` "最高设计 §3.6" **指向不存在的节**（确认命令 `grep -n "^### 3\." ASTROCS_DESIGN.md`）。③`75-77:` §5 三键在 `phase_config_normalize.schema.json` 中全部 0 命中（`grep -c "\"aperture_radius\"" …` = 0、`"sky_annulus"` = 0；`$defs.photometry` 无 properties）⇒ "配置表无契约"同形态。④`eng/packaging/config/config_registry.json` 以 `06_photometry.md:31`、`:32` 作 `conflict.evidence` 锚——实测两行是 §4.1 的**散文判据行**（"判据只有一条…"、"⚠ 判据必须从误差预算推导…"），而配置表在 :73-77 ⇒ **机器登记面把科学判据行当作默认值出处**（off-by-42） |
| 与上位冲突 | **是**：①§3.6 节号不存在；②`104-105:` "通道未配置 ⇒ 显式降级 `degraded_reason=photscale_absent`（中性 `photscal=1.0`，下游按未归一化 ADU 消费并如实登记）" 与同批 `astrocs.phase2.sample.md:75-76` "snr_available=0 时 snr 回退整帧中位，**禁以 1.0 伪装 unknown**（upm.h:51-55）"——同类"以 1.0 代表未知"一禁一用（字段不同：`photscal` vs `snr`），登记为**疑似口径冲突，待负责人裁决**，本批不单方面定案；③`76:` 自述"取值与实现/README 的 `4.0px` / `6-10px` 表述未统一，登记为 finding: gap"——三处取值未合流且本页承认 |
| 处置建议 | **拆分**：§4.1 判据与误差预算 → `docs/science/PHOTOMETRY.md`（其自认正本）与 `artifacts/evidence/`（实测）；§4.2 PMM 引用纪律 → `docs/research/` 或许可登记面；本页只留八节模板内容并修 §2 的 §3.6→§4.6（理由：§2 一文一角色＋§4"公式、数值表、实验过程数据放对应下层文档"） |

### 1.23 docs/plugins/algorithms_phase1/07_noise_snr.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase1/07_noise_snr.md` |
| 标题 | `插件文档：noise_snr（噪声 / 帧级 SNR / 点源信息量）`（H1:1） |
| 行数 | 257（机械列 257 ✓；本批最长，已通读全文） |
| 角色 | **模块工作细节**，落位 ✓；但 §4.1/§4.2/§4.2a/§4.4a/§4.5（:38-196，约 160 行）承载**科学定义 + 算法推导 + 实验结论 + 算子选型规则**，越出角色（正本应为 `docs/science/NOISE_MODEL.md`（412）/`PSF_SIGNAL_WEIGHT.md`/`CONTROL_WEIGHT_SNR.md` 与 `实验/absolute-snr`） |
| 主题 | 噪声模型、帧级 SNR、点源信息量、稀疏绝对 SNR 层与重建算子 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §4.2` ✓；`12:` §2.2/§3.1/§4.4 ✓；`41:` §5.3 ✓（`:385`）。**一处错配**：`96:` "完整适用域图谱由 `实验/absolute-snr` 给出（最高设计 §5.3）"——§5.3 是 Phase2 的 SNR 重建与逆方差叠加，不是"适用域图谱"的载体（轻） |
| 下游 | **60 个引用者（本批最高）**：`eng/packaging/config/defaults.json`（**6 个行锚**）、`eng/packaging/config/config_registry.json`（`:111`、`:138`）、`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、同批 `08_drizzle.md`、`实验/absolute-snr/**`、`lib/algorithms/noise_snr/**`、`ACCEPTANCE_SPEC.md`、`reports/v19r2/*`（命令 `git -C … grep -l -F 07_noise_snr.md -- .` 去自身后计数 = 60） |
| 是否正本 | **是**（SNR 三条路径与重建算子冻结词表的唯一现行登记），但 `137:` 自认"模型定义、参数语义、稳健噪声估计与掩膜规则的正本见 docs/science/NOISE_MODEL.md；本文件只登记插件侧接口与接线事实，**不复制公式**"——而 §4（:33-36）`W_psf,k` 展开式、§4.2a（:110）`σ_i² = σ_sky,i² + (RN/g)² + F·P_i/g`、§4.5（:177）cell 中心几何式**都复制了公式**：自述与内容不符 |
| 重复或重叠对象 | ①**与同批 `08_drizzle.md` 就"生产路径是否挂 variance 块"给出相反结论**（两侧原文与代码裁决见 §1.23-A，本批最高价值单条）；②与同批 `docs/modules/snr_estimator.md` 重叠噪声模型 A / `NoiseWeightModelV1` / science-weight 红线（举证见 §1.15）；③`100-106:` 三条"实测适用域边界"与 `实验/absolute-snr/docs/EXP-04*` 同源数值两处；④`195:` 与 `docs/science/CONTROL_WEIGHT_SNR.md §8b`（自引）重叠估计器认证；⑤`213:` `sparse_snr_layer` 行与同批 `08_drizzle.md:55` 同一键同一默认（true）两处配置表 |
| 元信息块 | 无（无 frontmatter）✓ 机械列正确 |
| 写法违规 | 任务编号 **6 处 `EXP-*`**：`100:` EXP-04、`176:` EXP-04 §5、`192:` EXP-04 §4.3、`193:` EXP-05 §3.1/§3.2、`195:` EXP-03 R0/R1/R2、`251:` EXP-04——机械列 `task_ids=['EXP-04']` **漏报 5 处**（`doc_mech_scan.py:59` 只扫头 40 行 + 尾 30 行；EXP-04 因出现在 :251 才被抓到）；另 `108:` "（SCI-B D1 定案）"、`139:` "接入义务…（最高设计 §8.2）"、`168-169:` "清单的机器唯一源 = `astrocs::noise::variance_audit_required_fields()`"；历史叙事：`126:` "**负例保护**：**旧**组合方式（经验总 rms 再加 `(RN/g)²`）必须被测试判红"、`177:` "**旧**实现的角点求值**曾**造成半 cell 相位偏移，**已订正**"——机械列 `history_narrative=[]` **漏报这两行**（HIST_PAT 收 `曾经` 不收 `曾造成`，收 `作废/旧版` 不收 `已订正`）；自认不可核对：`146:` "**行号说明**：本节的 `文件:行` 以符号名/文件名核对为准；工作树并发改动会使行号漂移"；台账语域：`145:` "**影响面（诚实）**"、`106:` "判据口径两条已知局限（与结论同读）" |
| 悬空引用 | ①`28:` `eng/contracts/schemas/noise_snr_output.schema.json` 不存在（机械列 ✓；`MODULE_MAP.yaml:790` 登记缺口）。②**行锚成片失效且被机器面消费**：`defaults.json:683` 引 `07:112`（实测该行**空**；`sparse_snr_density` 真行 :215，漂移 +103）、`defaults.json:688` 引 `07:113`（实测是 `sigma_sky_source` 表头行）、`defaults.json:718/735/740` 引 `07:61`/`07:62`（实测是 §4.1 的 PixInsight 方法学 bullet；`scalar_gate_rd/trend` 真行 :210/:211）、`defaults.json:707` 引 `07:186`（实测是算子表 `bilinear_regular_grid_v1` 行；`snr_path` 真行 :216）、`defaults.json:729` 引 `07:76`（实测在 §4.1 公式框内；"标量门失败→升级空间模型"真行 :231）——**6 个机器行锚全部落到无关行**。机械列 `dangling_links=[]` 对此类缺陷**结构性不可判**（工具只判文件存在），见 §4.5。③构建锚错：`136:` "编入根 `CMakeLists.txt:767-770` 的 `astrocs_phase1_noise`（STATIC），经 `astrocs_phase1_session` 的 PUBLIC 闭包（`CMakeLists.txt:876`）"——实测 `add_library(astrocs_phase1_noise STATIC` 在根 **:807-810**；:767-770 是 photometry 四个源文件；:876 是 `# P1-001: sdet 生产实现` 注释行（命令 `awk "NR==767 || NR==807 || NR==876" CMakeLists.txt`；实质结论"A 编入 STATIC"为真，锚错）。④文件级 ✓：`docs/research/SNR_WEIGHT_RESEARCH_PACK.md`、`docs/science/CONTROL_WEIGHT_SNR.md`、`lib/algorithms/integration/v6/oracle/`、`eng/contracts/schemas/unified/sparse_snr_layer.schema.json`（含 `sparse_snr_semantics`/`invalid_repr`，与 `:236` 断言一致 ✓）、ctest 名 `p1noise_negative`/`p1noise_selfcheck`（`lib/algorithms/noise_snr/tests/p1noise/CMakeLists.txt:90` ✓） |
| 与上位冲突 | **是**：①与 `08_drizzle.md` 的 variance 接线矛盾（07 为陈旧方，§1.23-A）；②`136:` 构建锚与根 CMake 实况不符（上一条③）；③`121:` "此口径下 SNR **系统性偏高**…该值的用途仅限上界诊断（最高设计 §2.2 的交付物是绝对 SNR）"与同页 `232:` "无 `photscale_fit` / `zero_point_valid=false` ⇒ 按显式回退 `group_median`…**逐帧中位数回退为 fail-closed，该形态保持**" 对同一交付物给出并存的两态口径，需与 `docs/science/` 侧合流（本批只登记不裁决） |
| 处置建议 | **保留（正本）+ 大幅瘦身**：§4.1/§4.2a/§4.5 的公式与实测数值归还 `docs/science/NOISE_MODEL.md`、`PSF_SIGNAL_WEIGHT.md`、`CONTROL_WEIGHT_SNR.md`；EXP-* 实测边界表归还 `实验/absolute-snr/docs/`；本页只留接口/端口/配置键/错误语义（理由：§1"公式推导、数值表、源码行号、实验数据下沉到对应下层文档，每节末尾给出索引指针"＋§2 一文一角色）；§4.4 的 variance 结论必须与 `08_drizzle.md` 合流为一条（§1"同一主题只有一份正本"） |

#### 1.23-A 与同批 08 的事实冲突（本批最高价值单条）

同一事实——**生产调度路径是否挂 `variance` 块**——两份 ACTIVE_NORMATIVE 文档给出相反结论：

- `docs/plugins/algorithms_phase1/07_noise_snr.md:138:` "**逐像素方差接线现状（如实登记）**：A 已编入 `astrocs_phase1_noise` 并链入主程序，但**生产调度路径（`module_adapters.cpp`）尚未挂 `variance` 块** ⇒ `hp_drizzle_api.cpp:1024` 读不到 ⇒ `astro_sphere_sink.cpp:306–327` 不产出 variance/ivar 子产品面 ⇒ `p2_integrated.json` 报 `uncertainty_available=false`（原因 `ivar_product_missing_frame_snr_fallback`）。"
- `docs/plugins/algorithms_phase1/08_drizzle.md:69-70:` "**逐像素方差/ivar 产品面**：生产调度路径**已挂** `variance` 帧内命名块 —— `lib/infrastructure/scheduler/src/module_adapters.cpp`（`p1_op_drizzle`）…`aio_frame_add_block(frame, "variance", AIO_BLOCK_FLOAT32, …)`…**旧表述**「生产调度路径不挂 `variance` 块 ⇒ `has_variance=0` ⇒ `uncertainty_available=false`…」与工作区现状**不符，已作废**"

由代码裁决（只读）：`lib/infrastructure/scheduler/src/module_adapters.cpp:8156` 实有 `frame, "variance", AIO_BLOCK_FLOAT32, var_plane.data(),`，且 `lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.cpp:1022-1031` 确在执行 `aio_frame_get_block(frame, "variance")` ⇒ **08 对、07 陈旧**。缺陷形态有三：①同一事实在两份规范层文档并存两值（§2 一主题一正本被破坏）；②08 用"旧表述…已作废"处理，本身违反 §4 禁历史叙事，而 **07 未同步修订**——其 §4.4/§4.4a/§7 三条推论仍建立在已被否定的前提上；③`eng/packaging/config/defaults.json:694` 把 `docs/plugins/algorithms_phase1/07_noise_snr.md:138` 当作"同步"对象引用 ⇒ **机器登记面同步的是一条已被另一份文档作废的表述**。

### 1.24 docs/plugins/algorithms_phase1/08_drizzle.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase1/08_drizzle.md` |
| 标题 | `插件文档：drizzle（球面 Drizzle / HEALPix 累积）`（H1:1） |
| 行数 | 78（机械列 78 ✓） |
| 角色 | **模块工作细节**，落位 ✓；§4（:26-43）是**算法推导 + 文献依据**（F&H 2002 §7.2 式(7)、drizzlepac `cdrizzlebox.c` 的 `dover /= jaco`）应属 `docs/algorithms/DRIZZLE_GEOMETRY.md`/`docs/science/DRIZZLE.md`（:39/:53 已自引）；§7 后半（:69-70）是**接线状态与缺陷登记**，应属 `docs/KNOWN_LIMITATIONS.md`/evidence → **角色混装** |
| 主题 | 球面 Drizzle / HEALPix 累积与 HiPS 产品 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §4.2` ✓；`12:` §4.4 ✓（`:316`）、§2.2 ✓（`:130`）；`68:` "（最高设计 §5.5/§10…规则见 `docs/science/DRIZZLE.md`）"——§10 = I/O 与原子产品 ✓；**§5.5 是 Phase2「逐像素排异」的 NaN 条款**（`ASTROCS_DESIGN.md:417/426`），此处用于 Phase1 drizzle 属跨阶段借用同一条款（Phase1 侧无可指条款号 → 登记为"上游可指性不足"，非硬错） |
| 下游 | 12 个引用者：`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md:37`、`eng/packaging/config/config_registry.json`（**5 条本文件行锚**）、`eng/contracts/schemas/phase_config_normalize.schema.json`（**`sparse_snr_layer` 描述里写 `08_drizzle.md:46`**）、`lib/algorithms/drizzle/**`、`reports/v19r2/*` |
| 是否正本 | **是**（drizzle 插件层唯一页；pixfrac 归一口径的文档侧登记） |
| 重复或重叠对象 | ①与 `docs/science/DRIZZLE.md` 重叠归一化推导：`39:` "核按 `A_drop,j` 归一（canonical，`w_jp = a_jp / A_drop,j`）：依据 F&H 2002 §7.2 式(7) 正下方定义…与 drizzlepac `cdrizzlebox.c` 的 `dover /= jaco`"、`41:` "等价参数化（同一 `c_jp`）：`w'_jp=a_jp/A_pixel,j=pixfrac²·w_jp` 配分母 `Σ_j w'_jp=D_p/pixfrac²`"；②与同批 `07_noise_snr.md` 的 variance 冲突（§1.23-A：重叠且矛盾）；③`55:` `sparse_snr_layer` 与 `07:213` 同一键同一默认两处配置表；④`22-23:` 落盘形态（`storage_form`/`archive`/`bare`/`index_path`/`index_sha256`）与 `docs/architecture/PRODUCT_STORAGE_FORM.md`、`eng/contracts/schemas/hips_storage_form.schema.json`（存在 ✓）重叠 |
| 元信息块 | 无 ✓ |
| 写法违规 | 历史叙事（机械列 `history_narrative=[70]` **命中准确**）：`70:` "**旧表述**「生产调度路径不挂 `variance` 块…」与工作区现状**不符，已作废**"——§4 明禁"旧版/作废"式历史叙事（只写现行条款即可）；任务编号：`40:` "DISP-DRZ-009 负例判据"、`42:` "回归门 `p1drz_disp009`…与 `drizzle_acceptance`…`--inject-legacy-pixfrac2` 负例注入"、`69:` "按定案2"、`70:` "双实现分裂见 `07_noise_snr.md` §4.4"——机械列 `task_ids=[]` **漏报**（DISP-DRZ-009/定案2 不匹配模式）；过程状态语：`42:` "**现状**：`processPixelSharedTiled` 的权重 = `overlap_area/drop_area`…" |
| 悬空引用 | ①`24:` `eng/contracts/schemas/hips_product.schema.json`、`eng/contracts/schemas/manifest.schema.json` **均不存在**（机械列命中 ✓；`MODULE_MAP.yaml:791-792` 登记缺口）。②**结构缺陷（本文独有的实物证据）**：§5 表在 `:52` 结束，`:53` 插一条 bullet（`pixfrac` 数值来源说明），`:54` 空行，`:55` 又出现**无表头的孤立表行** ` sparse_snr_layer | true | … `——Markdown 不将其并入上表（前面无相邻表头/分隔行）。③由此 `eng/packaging/config/config_registry.json` 对本页的 5 条行锚**集体漂移**：`order` 锚 :48（真 :49）、`pixfrac` 锚 :49（真 :50）、`pixel_scale` 锚 :50（真 :51）、`nside` 锚 :51（真 :52）、`sparse_snr_layer` 锚 :52（真 :55）；`phase_config_normalize.schema.json` 的 `sparse_snr_layer` 描述引 `08_drizzle.md:46`，实测 :46 是 §5 标题后的空行（真行 :55） |
| 与上位冲突 | **是（同一键四侧三值，本批第二高价值）**：`50:` `pixfrac` 默认 **0.8** 并声明"唯一来源 = `defaults.json#drizzle.pixfrac`"（实测 `eng/packaging/config/defaults.json:666-667` `"key": "drizzle.pixfrac", "value": 0.8` 且 `"authority_status": "owner_adjudicated"` ✓ **文档与 defaults 一致**）；但 ①`eng/packaging/config/config_registry.json` 对 `drizzle/pixfrac` 登记 `declared_default: 1.0`（机器抄件陈旧）；②`phase_config_normalize.schema.json` 的 `algorithm_drizzle_pixfrac` 描述写"defaults.json 的 drizzle.pixfrac 为 **pending_authority**…本字段不声明数值默认"（与 defaults 现值 `owner_adjudicated` 矛盾）。⇒ **本批文档侧正确、两个机器登记面背离**，方向与 registry 手写页相反，说明"第二套表"在文档侧与机器侧双向发生 |
| 处置建议 | **保留（正本）+ 三处修**：①修复 §5 表结构（把 :55 并入表或删孤立行），修完由 owner 重锚 `config_registry.json` 5 处与 schema 1 处；②`70:` 删"旧表述…已作废"叙事，只写现行事实句并把纠正过程记入 evidence；③§4 推导段归还 `docs/algorithms/DRIZZLE_GEOMETRY.md`（留指针）。理由：§4（无历史叙事、行号下层化）＋§2（一文一角色）＋§1（同主题一份正本） |

### 1.25 docs/plugins/algorithms_phase2/09_coverage.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase2/09_coverage.md` |
| 标题 | `插件文档：coverage（覆盖联合）`（H1:1） |
| 行数 | 51（机械列 51 ✓） |
| 角色 | **模块工作细节**，落位 `docs/plugins/algorithms_phase2/` ✓，不错放 |
| 主题 | 覆盖联合与几何有效域 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §5.2（固定科学流程）` ✓（`:371` 标题逐字匹配）；`12:` §5.6（硬约束）✓（`:428`） |
| 下游 | 9 个引用者：`docs/DOCUMENT_INDEX.yaml`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md:43`、`eng/packaging/config/config_registry.json`（`:31` 锚）、`docs/modules/registry/astrocs.phase2.coverage.md`（别批）、`reports/v19r2/*` |
| 是否正本 | **是**（coverage 插件层唯一页） |
| 重复或重叠对象 | 与别批 `docs/modules/registry/astrocs.phase2.coverage.md`（79 行，索引状态 `ACTIVE_INFORMATIVE`）同模块重叠：本页 `8:` "**coverage 是几何/数据有效域，不是权重，不能作为 inverse-variance 或 SNR 权重**"与那份的职责/红线段同题（该页由别批逐份判定，本批只登记重叠、不代下结论）。与 `docs/design/PHASE2_DETAILED_DESIGN.md §3`（`13:` 自引）重叠"连通分量/断图" |
| 元信息块 | 无 ✓ |
| 写法违规 | 机械列全空 ✓，复算亦空（无日期/编号/commit/sha256/历史叙事）。唯一可记：`18:` 单条 bullet 内塞 5 条合同规则（含"按天区查帧集合走**块级覆盖索引**（不压缩；由**加性可选键** `coverage_index` 引用数据集级 `coverage.index.json`，缺失时由产品级索引现场倒排）"）——密度过高、无分节，属可读性缺陷非 §4 硬违规 |
| 悬空引用 | `21:` `eng/contracts/schemas/coverage_output.schema.json` 不存在（机械列 ✓；`MODULE_MAP.yaml:793` 登记缺口）。锚错位：`eng/packaging/config/config_registry.json:799-811` 登记 `field: connected_components`（真行 :34）而 `conflict.evidence` 写 `09_coverage.md:31`，实测 :31 是 §5 表头 `| 字段 | 默认 | 单位 | 说明 |`（off-by-3 且落在非数据行）。文件级 ✓：`phase_config_mosaic.schema.json` 存在（本页 `18:` 断言"输入合同不设 `storage_form` 键…出现即 REJECT"的合同面即该文件；本批不代审其内容） |
| 与上位冲突 | 否（§5.2/§5.6 号与题匹配）。§5 两键 `min_overlap`/`connected_components` 在 mosaic 合同面无对应（与 §1.17/§1.20/§1.21 同形态"配置表无契约"），登记为形态缺陷非冲突 |
| 处置建议 | **保留（正本）**：§3 参考行改指 `eng/contracts/schemas/unified/**` 或就地标注已登记缺口；请 owner 重锚 `config_registry.json` 的 `:31`。理由：§3"登记了但文件不存在同样是缺陷"反向适用——引用不存在即缺陷 |

<!-- PROGRESS: 已审 25/25 -->

---

## 2 同主题组与差异（跨批发现注明别批路径）

| # | 主题 | 各份路径（★=本批） | 正本候选 | 主要差异（已贴段落的指向） | 合并建议 |
|---|---|---|---|---|---|
| G1 | Phase3 重采样模块 | ★`docs/modules/registry/astrocs.phase3.resample.md`、★`docs/modules/registry/astrocs.phase3.resample2.md`、`docs/modules/phase3_rsmp.md`（别批）、`docs/plugins/algorithms_phase3/15_resample.md`（别批） | `astrocs.phase3.resample2.md`（唯一有实质合同内容者） | 同模块四页；resample2:50-52 直斥 registry 的 `astrocs.phase3.resample` 行为"P2 模板复制残留…本任务零触碰"；两 descriptor（`phase3_descriptor` `module_adapters.cpp:730-747` vs `p3_resample2_descriptor` `:791-809`）端口集不同（hips+tile vs wcs_plan+hips+resampled） | registry 两页合一：descriptor 侧删占位行，文档侧 resample 页并入 resample2 页（§2 一主题一正本） |
| G2 | UPM 模块 | ★`astrocs.phase2.upm-apply.md`、★`astrocs.phase2.upm-fit.md`、`docs/modules/phase2_upm.md`（别批 263 行）、`docs/plugins/algorithms_phase2/11_upm.md`（别批 189 行） | fit+apply 合一的新页（或并入 `docs/modules/phase2_upm.md`） | 同一 `astrocs.p2.upm` 合同两页分持，60–70 行逐字重复（§1.4 举证五行对）；`MODULE_MAP` 只有 1 条 upm 条目 ⇒ 页面数与登记面条数不成比例；两页对 upm.cpp 行数与 DOCUMENT_INDEX 的 notes 三值不一（2793/1565/实测 2981） | 合并为单页（fit/apply 作两节），删全部 16 符号行锚与 DISP 清单，改指 `lib/algorithms/upm/module.yaml` + ALG 文档 |
| G3 | Phase1 源检测 | ★`docs/modules/star_detector.md`、★`docs/plugins/algorithms_phase1/03_star_detection.md`、`docs/modules/registry/astrocs.phase1.star-detection.md`（别批） | `03_star_detection.md`（`MODULE_MAP` plugin_doc + `ASTROCS_DESIGN`/`ACCEPTANCE_SPEC` 直引） | 权威范式与 `SDetGuidedStats` 六字段两处逐字（§1.15 举证）；`star_detector.md` 的头文件路径一侧悬空、4 处源码行锚全失效；03 声明 `§7.1` 为 fail-closed 权威且被前者引用 ⇒ **层级倒置**（下层引用上层） | 删 `star_detector.md`（内容归还 ALG + 03）；registry 页保留但改标"非注册项"（descriptor 无 `astrocs.p1.star_detection` 行，见 `MODULE_MAP:119` 的 module_id 与 descriptor 集不同名） |
| G4 | Phase1 噪声/SNR | ★`docs/modules/snr_estimator.md`、★`docs/plugins/algorithms_phase1/07_noise_snr.md`、`docs/modules/registry/astrocs.phase1.noise-snr.md`（别批）、`lib/algorithms/noise_snr/README.md`（别批 248 行） | `07_noise_snr.md`（瘦身后）+ `lib/algorithms/noise_snr/README.md` | 噪声模型 A/`NoiseWeightModelV1`/science-weight 三处双写（§1.15 举证）；`snr_estimator.md` 用迁移前目录名当模块名并声称"snr_estimator DLL"（不存在，见 §1.14 悬空引用行） | 删 `snr_estimator.md`；07 瘦身公式与实测数据 |
| G5 | variance/ivar 是否入生产链（**事实冲突**） | ★`07_noise_snr.md:138`（未挂）、★`08_drizzle.md:69-70`（已挂，并称前者"已作废"） | 以代码为准 = 08 侧（`module_adapters.cpp:8156`、`hp_drizzle_api.cpp:1022-1031`） | 见 §1.23-A 两侧原文；`defaults.json:694` 还在同步 07 的作废表述 | 07 的 §4.4 四条推论按现行事实重写；08 删"已作废"叙事；过程记 `artifacts/evidence/` |
| G6 | 投影支持集口径 | ★`docs/plugins/00_INDEX.md:53`（"内置 8 种"）、★`astrocs.phase3.wcs.md:56-60`（"仅 TAN"）、`docs/plugins/algorithms_phase3/14_projection.md`（别批）、`docs/DOCUMENT_INDEX.yaml:1172-1174`（别批） | 代码 `lib/algorithms/projection/p3_projection_registry.h:70-84`（三态：TAN 可声明 / SIN·CAR·AIT 仅内核 / 其余未实现） | 00_INDEX 与 DOCUMENT_INDEX 的 duty 均过强；registry 页与代码一致 | 统一为"设计冻结 8 码 + 三态登记"表述，并把三态写进 14_projection（别批联动） |
| G7 | 模块↔文档映射（三套表） | ★`docs/plugins/00_INDEX.md §2`、`docs/modules/MODULE_MAP.yaml`（别批 23 条 plugin_doc）、`docs/DOCUMENT_INDEX.yaml`（别批）、★`module_id_migration_baseline.json:6 index_module_count` | 各自角色不同：MODULE_MAP = 机器登记面，DOCUMENT_INDEX = §3 唯一索引，00_INDEX §2 = 被 M1 门解析的阅读层索引 | 现算三侧一致（23=23=23，命令见 §4.2），但模块数被**四处**登记且 `baseline.json:7` 明文规定门断言"00_INDEX §2 解析数 == index_module_count 且 set(map_ids)==set(index_ids)" | 保留现状但在 DOCUMENT_INDEX 侧加"谁派生自谁"的单向声明（§1 禁第二套权威口径），长期应让 00_INDEX §2 由 MODULE_MAP 生成 |
| G8 | Phase2 写出（write/hips_writer） | ★`astrocs.phase2.write.md`、`docs/plugins/algorithms_phase2/*`（无对应篇，coverage 是唯一 phase2 插件页在本批）、`lib/algorithms/coverage/hips_p2/README.md`（别批 185 行） | ★`astrocs.phase2.write.md`（唯一手写合同页） | 页 `19-22:` 自认"模块级事实以 hips_p2/README.md（r1）+ module.yaml 为准"，却又展开全部门禁/哈希链/容差 ⇒ README 与本页两处口径并存；descriptor 注释（`module_adapters.cpp:1173-1174`）另称与本页端口表"同源"且值互斥（§1.6-A） | 保留本页为模块合同页；端口表与 descriptor 合流（改一侧值），README 只留实现事实 |

## 3 索引与链接缺陷清单

**A. 登记覆盖（本批 25 份）**：`grep -q -F "<path>" docs/DOCUMENT_INDEX.yaml` 逐条复算 → **25/25 全部登记，无未登记项**；`docs/modules/registry/` 目录内 索引条目 27 ↔ 磁盘 27，无"登记了但文件不存在"、无"存在但未登记"（命令见 §4.2）。

**B. 角色/状态标注与本批判定不符（4 类）**

| 条目 | 索引侧 | 实际 | 判定 |
|---|---|---|---|
| ★`astrocs.phase2.sample.md` | `DOCUMENT_INDEX.yaml:959-964` `status: GENERATED` + notes "gen_module_readmes.py 生成" | 页 `:13` 自述"人工内容（MANUAL）：本页无 GENERATED-ANCHOR，非生成器所有"；`grep -c '^<!-- GENERATED-ANCHOR '` = 0 | **不符**。按生成器语义（`gen_module_readmes.py:337-356` verify_page）该页状态 = UNOWNED，且因在册 GENERATED 会被记为 `MODULE_README_GEN_OWNERSHIP_CONFLICT`（`:429-431`），冲突只打印不判红（`:468-476` 返回值不含 conflicts） |
| ★`astrocs.phase3.verify.md` | `DOCUMENT_INDEX.yaml:1007-1012` `status: GENERATED` | 页 `:11` MANUAL 声明 + 无锚；正文是**上一版模板的过期抄件**（`:44` NOT_VERIFIED 措辞与现行 4 份生成页 `:44` 不同） | **不符**（同上；且它是"手写副本伪装生成物"的最清晰样本） |
| ★`module_id_migration_baseline.json` | `DOCUMENT_INDEX.yaml:1025-1029` `status: GENERATED`、downstream 只写 `check_module_map.py` | 文件自身 `rule: 烧毁式基线…只许在落地批内手改`；生成器只写 `.md`（`gen_module_readmes.py:218` 过滤 `endswith(".md")`），不产 JSON；实际读者是 `check_module_id_normalization.py:6` + `check_module_map.py:318` | **不符**：既非生成物，下游也漏登记一个消费者 |
| ★`docs/modules/registry/*.md` 的 `duty` 字段 | 例 `:971` `duty: "职责（apply）：按 frame_id 稳定绑定逐块校准——**默认只扣偏差 δ_k、保留公共"` | 是文档正文某段的**机械截断**：截在句中、残留 `**` 未闭合、丢单位后缀 | 索引质量问题（不是角色错）：`duty` 既非主题也非角色，对 §3"登记内容含路径、角色、主题、上游、下游"缺 **角色** 与 **主题** 两维（与 DB-11 对 DOCUMENT_INDEX 的整体判定一致，此处只按本批条目举证） |

**C. 上游标注系统性错误（索引与文档同步地错）**：`grep -c '§8.4（模块与 ABI）' docs/DOCUMENT_INDEX.yaml` 与全部 26 份 registry `.md` + ★`snr_estimator.md:3` + ★`star_detector.md:3` 同串 —— `ASTROCS_DESIGN.md:618` §8.4 标题实为"顶层结构"，"模块与 ABI"在 `:661` §8.5。索引与文档两侧同样错 ⇒ 不是抄错一份，而是**同一错误源**（生成器模板 `gen_module_readmes.py:76`）扩散。详见 §4.3。

**D. 悬空引用总表（本批 25 份，全部给出确认命令）**

| 类型 | 出处 | 目标 | 确认命令 |
|---|---|---|---|
| 文件不存在（合同 schema） | ★01:20、★02:20、★03:21、★04:21、★05:22、★06:20、★07:28、★08:24、★09:21 | `eng/contracts/schemas/{data_light,calibration_output,cosmetic_output,source_catalog,psf_output,wcs_output,photometry_output,noise_snr_output,hips_product,manifest,coverage_output}.schema.json` 共 11 个路径 | `ls eng/contracts/schemas/`（实际 18 文件 + `unified/`，无一名匹配）；缺口已在 `MODULE_MAP.yaml:779-796` declared_absent_paths 登记，但**插件文档正文不声明** |
| 文件不存在（结论证据源） | ★registry 3 份生成页（`:44/:56`）+ ★`astrocs.phase3.verify.md:44/:56` | `eng/ci/ledgers/module_page_evidence.json` | `sha256sum eng/ci/ledgers/module_page_evidence.json` → `No such file or directory` |
| 路径错目录（文件在别处） | ★`astrocs.phase3.wcs.md:34/165/167` | `eng/tests/p3wcs/**`、`eng/tests/unit/p3_wcs_test.cpp`、`eng/tests/backend/p3_wcs_main.cpp` | `git ls-files \| grep -i p3wcs` → 唯一命中 `lib/algorithms/projection/tests/p3wcs/`（`p3_wcs_test.cpp` 474 行与页面所记一致，只错目录） |
| 路径错目录（文件在别处） | ★`astrocs.phase3.resample2.md:172/190` | `eng/tests/backend/p3_resample_probe_main.cpp` | `git ls-files \| grep -i p3_resample` → 实际 `lib/algorithms/resample/tests/p3rsmp/p3_resample_probe_main.cpp` |
| 文件不存在（迁移遗留） | ★`astrocs.phase3.writer.md:33-34` | `lib/algorithms/fits_output/p3_wcs.h（166 行）` | `git ls-files \| grep 'p3_wcs.h'` → 唯一命中 `lib/algorithms/projection/p3_wcs.h`，373 行（166 是上一项 p3_output.h 的值被复制） |
| 头文件路径两值 | ★`docs/modules/star_detector.md:91-92` vs `:32` | `lib/include/star_detector.h` | `[ -f lib/include/star_detector.h ]` 假；`[ -f lib/algorithms/star_detection/include/star_detector.h ]` 真（110 行） |
| 节号不存在 | ★`06_photometry.md:12` | `ASTROCS_DESIGN.md §3.6` | `grep -n "^### 3\." ASTROCS_DESIGN.md` → 只有 3.1/3.2/3.3 |
| 节号错配 | ★registry 全部 + ★snr/star | `§8.4（模块与 ABI）` | `grep -n "^### 8\." ASTROCS_DESIGN.md` → 8.4=顶层结构、8.5=模块与 ABI |
| 节号错配 | ★registry 4 份生成页 `:52` | "exit 9，最高设计 §6.3" | §6.3 = 投影算法（`:451`）；退出码 9 与协作取消在 §7.2（`:508` 表行、`:512` 流程） |
| 节号错配 | ★`astrocs.phase2.write.md:57` | "最高设计 §9 的原子发布强制条款" | §9 = CPU 后端与资源（`:669`）；原子发布条款在 §10（`:688`） |
| 键无合同面 | ★01/02/04/05/06/09 的 §5 配置表 | `phase_config_normalize.schema.json`（mosaic 侧同理） | 逐键 `grep -c "\"<键名>\""` = 0（本批实测 11 键全 0 命中；见 §4.4） |

**E. 行锚漂移（机械列结构性不可判的一类）**：见 §4.2 汇总表——registry 手写页的 `module_adapters.cpp` descriptor 锚 **8/8 全错**（漂 −270 至 −430 行）、`upm.cpp` 14 个符号锚 **14/14 全错**（漂 +300 至 +660 行）、`defaults.json`→07 的 6 个锚 **6/6 落到无关行**、`config_registry.json`→本批 9 份插件文档的 `conflict.evidence` 锚 **9/15 落在表头/分隔符/散文行**。

<!-- PROGRESS: 已审 25/25；台账 2 完成 -->

## 4 注册说明与机器登记面是否同源（本批特有关注点，逐份判定 + 复算命令）

### 4.1 生成物 vs 手写副本（12 份 registry `.md` + 1 份 json 逐份定性）

判定口径（只用文件自身与生成器源码，不执行任何脚本）：
`eng/tools/quality/gen_module_readmes.py:337-356` 定义页的归属——有 `<!-- GENERATED-ANCHOR tool=… -->` 且 `tool/source/tool-sha256/source-sha256/body-sha256` 全对 = `OWNED_OK`（真生成物）；无锚 = `UNOWNED`（"无锚 ⇒ 人工内容，不覆盖"，`:431`）；锚在但 sha 不符 = `DRIFT`（`:350-355`）⇒ 在册 GENERATED 却无锚 = `MODULE_README_GEN_OWNERSHIP_CONFLICT`（`:460-464`）。

| 页 | 有无真锚 | 索引称 GENERATED | 定性 | 与注册面（`module_adapters.cpp` descriptor）是否逐项一致 |
|---|---|---|---|---|
| ★`astrocs.phase2.resample.md` | 有 | 是 | **生成物**，但 sha 已失配 | 一致（端口 `calibrated/DATA-P1-CAL/必/ADU/PIXEL`、`resampled/DATA-P2-RES/可/ADU/PIXEL` 对 `:719-720` ✓） |
| ★`astrocs.phase3.properties.md` | 有 | 是 | **生成物**，sha 失配 | 一致（对 `:761-762` ✓） |
| ★`astrocs.phase3.resample.md` | 有 | 是 | **生成物**，sha 失配 | 一致（对 `:738-739` ✓） |
| ★`astrocs.phase3.verify.md` | 无（自称 MANUAL） | **是** | **手写抄件伪装生成物**：正文与现行模板逐字相同、NOT_VERIFIED 句式却是**上一版模板**的措辞（`（证据源 …json 无 …条目；未读到的结论不写成 PASS/FAIL）` vs 现行 `（证据源 …json(absent) 无 …条目）`） | 一致（对 `:838-839/:835-836/:841-845` ✓）——即内容对、身份错 |
| ★`astrocs.phase2.sample.md` | 无（自称 MANUAL） | **是** | **手写副本**（有实质合同内容） | **不一致**：端口 `samples` 单位 ADU vs `:1074` SURFACE_BRIGHTNESS |
| ★`astrocs.phase2.session.md` | 无 | 否（ACTIVE_NORMATIVE） | 手写副本（对应 descriptor **不存在**：注册面无 `astrocs.p2.session`/`astrocs.phase2.session` 行） | 不适用（该页登记的是会话层，不在 descriptor 面） |
| ★`astrocs.phase2.upm-apply.md` | 无 | 否（ACTIVE_INFORMATIVE） | 手写副本 | **不一致**：三端口单位全 ADU vs `:1111-1113` 全 SURFACE_BRIGHTNESS |
| ★`astrocs.phase2.upm-fit.md` | 无 | 否 | 手写副本；frontmatter 缺 `module_id` ⇒ 被 ★baseline.json 的 `PAGE_MISSING_MODULE_ID` 计数 | **不一致**：两端口单位 ADU vs `:1092-1093` SURFACE_BRIGHTNESS |
| ★`astrocs.phase2.write.md` | 无 | 否 | 手写副本 | **三向矛盾**：页 `:65` 输入 ADU；descriptor `:1175` 输入 SURFACE_BRIGHTNESS；descriptor 注释 `:1173-1174` 自称"与本页端口表同源：输入 ADU / 输出 SURFACE_BRIGHTNESS" ⇒ 代码与自身注释矛盾、与页面矛盾 |
| ★`astrocs.phase3.resample2.md` | 无 | 否 | 手写副本 | **不一致**：`hips` 单位 ADU vs `:800` SURFACE_BRIGHTNESS |
| ★`astrocs.phase3.wcs.md` | 无 | 否 | 手写副本 | 一致（`:780-781` ✓ DIMENSIONLESS/HEALPIX + DEGREE/ICRS） |
| ★`astrocs.phase3.writer.md` | 无 | 否 | 手写副本 | 一致（`:819-820` ✓，execution_class=io/parallel_ok=False 对 `:816-817` ✓） |
| ★`module_id_migration_baseline.json` | 不适用 | **是** | **手写过程台账**（生成器只产 `.md`：`:218` 过滤 `endswith(".md")`） | 被 `check_module_id_normalization.py:6` 与 `check_module_map.py:318` 当输入读；数值现算见 §4.2 末段 |

结论：本批 12 份 registry `.md` 中 **3 份是生成物（且全部处于 DRIFT 态）、9 份是手写副本**；9 份里 **4 份与注册面逐项不一致（单位列）**、**5 份一致**。即"同一目录同名同构的 12 份文件"实际是两种东西混装，且索引对其中 5 份的身份标注错误（sample/verify/baseline.json 称 GENERATED，另 2 份生成页未标 DRIFT）。

**生成物全部过期（可复算）**：4 份带锚页的 `source-sha256=db3a8ed2fc7e26f232145d7e1e8b020f17c5a555f156a10290aec2a630c29979`，而唯一源现值 `0171c4ff52c18dafc673cbcec21f3005e0db1fc8db6fa7af982fd1e4f728a951`（命令 `sha256sum lib/infrastructure/scheduler/src/module_adapters.cpp eng/tools/quality/gen_module_readmes.py`；`tool-sha256=4fb971ad…` 与生成器自身现值一致 ✓ ⇒ 只有源变了）。按 `verify_page` 判据即 `DRIFT`（`--check` 应 exit 1）。同时 `evidence=eng/ci/ledgers/module_page_evidence.json` 该文件不存在 ⇒ 4 份生成页的 status/determinism/verification/tolerance 全为 `NOT_VERIFIED`，页面除端口表与 descriptor 串外**不承载任何可核对结论**。

### 4.2 行锚与数值漂移汇总（两侧值 + 确定命令）

**（a）registry 手写页 → `module_adapters.cpp` descriptor 锚：9 份页 10 处，10/10 全错**

| 页面锚（原文） | 实测真锚 | 实测该区间内容 |
|---|---|---|
| `sample.md:26/62` `:642-654 p2_sample_descriptor` | `:1065-1082` | `:643 acs_span_u8 man{};`（session adapter manifest 捕获） |
| `upm-apply.md:66/104` `:680-694` | `:1103-1121` | `:680-689 last_manifest()`、`:692-694 phase1_descriptor(){ d.module_id="astrocs.phase1.calibration"` |
| `upm-fit.md:62/96` `:661-678` | `:1084-1101` | `:661-678` session adapter inspect 分支 |
| `write.md:69/91` `:1040-1057`（并自称"行锚实测 grep → 1040"） | `:1162-1184` | `:1040-1057` = `p2_coverage_descriptor` 区（`:1046`） |
| `wcs.md:80/101` `:406-423` | `:772-789` | `:406-423` 落在会话适配器模板代码 |
| `resample2.md:89/116` `:425-439` | `:791-809` | 同上 |
| `resample2.md:50-51` `:300-318 phase3_descriptor` | `:730-747` | `:300-318` 非 descriptor |
| `writer.md:72/92` `:445-460` | `:811-828` | — |
| `session.md:16` `:643 起 descriptor 族` | 族始于 `:692` | `:643` = `acs_span_u8 man{};` |
| `resample2.md:125` / `wcs.md:109` 注册序 `:816 起…:797-798 收尾`（两页逐字同串） | 注册表在 `:15463-15519` | 命令 `grep -n 'descriptor(),' lib/infrastructure/scheduler/src/module_adapters.cpp` |

（同页正确锚的对照样本，证明不是全盘不可信而是**局部漂移**：`session.md:18` 的 `module_adapters.cpp:23-26` ✓、`write.md:66` 的 `lib/include/astrocs/core/artifact.h:36` ✓。）

**（b）`upm.cpp`/`upm.h` 符号锚：14/14 全错（两页共用同一张表）**

| 符号 | 页面 | 实测 | | 符号 | 页面 | 实测 |
|---|---|---|---|---|---|---|
| `p2_upm_build` | 929 | **1372** | | `p2_upm_raw_weight` | 1292 | **1966** |
| `p2_upm_build_geo` | 934 | **1377** | | `p2_upm_geometry_hash` | 1346 | **2001** |
| `p2_upm_save` | 940 | **1383** | | `p2_upm_component_gauges` | 1369 | **2024** |
| `p2_upm_open` | 1008 | **1506** | | `p2_upm_materialize_dense_n` | 1390 | **2045** |
| `p2_upm_info` | 1233 | **1803** | | `p2_upm_dense_read_block` | 1542 | **2201** |
| `p2_upm_calibrate_block` | 1240 | **1907** | | `p2_upm_close` | 1559 | **2218** |
| `p2_upm_evaluate_c` | 1271 | **1943** | | `p2_upm_normalized_weights` | 1325 | **符号已撤**（`upm.h:181` RETIRED 注） |

命令：`grep -n '^int p2_upm_build(\|^int p2_upm_calibrate_block(\|…' lib/algorithms/coverage/src/upm.cpp`、`grep -n 'p2_upm_normalized_weights' lib/algorithms/coverage/include/astro/phase2/upm.h`。附带两页仍称"upm.cpp 16 导出"，现头文件另有 `p2_upm_convergence/p2_upm_identifiability/p2_upm_warnings_json/p2_upm_control_variance/p2_upm_ma_*` 族 ⇒ 计数亦失效。

**（c）文件行数类（页面值 vs 实测值，`grep -c ''` 同口径；同批 4 份生成页的 lines 与机械列完全一致，证明口径可比）**

| 事实 | 页面值 | 第二处值 | 实测 |
|---|---|---|---|
| `sampler.cpp` | 1536（sample:38） | — | **1503** |
| `sampler.h` | 288（sample:40） | **136**（sample:85，同页另一处） | **273** |
| `upm.cpp` | 2793（fit/apply :22/:40） | 1565（`DOCUMENT_INDEX.yaml:971/977` notes） | **2981** |
| `upm.h` | 384 | 184（同上索引 notes） | **450** |
| `stage2.cpp` | 1965（write:23） | — | **2015** |
| `p3_resample.cpp` | 586（resample2:33） | — | 586 ✓ |
| `p3_resample.h` | 201（resample2:34） | — | **202** |
| `p3_output.cpp`/`p3_output.h` | 1082/166（writer:33） | — | 1082 ✓ / 166 ✓ |
| `p3_wcs.h` | 166（writer:34，且目录写成 `fits_output/`） | — | **373**（唯一份在 `lib/algorithms/projection/`） |
| `p3_coverage_test.cpp` | 106（resample2:195） | — | **224** |
| `p3_wcs_test.cpp` | 474（wcs:165，目录写成 `eng/tests/unit/`） | — | 474 ✓（只错目录） |
| `p3_output_test.cpp` + CMake 注册 | 244 ✓ / `CMakeLists.txt:442-447` | — | 244 ✓ / 实注册在 **:845-850**（:442-447 是 `p2001_real_nodes_test`） |

**（d）插件文档 → 机器登记面的行锚（`eng/packaging/config/config_registry.json` 的 `conflict.evidence` 与 `eng/packaging/config/defaults.json` 的 source 字段）**

| 机器条目 | 锚 | 锚落点 | 真行 |
|---|---|---|---|
| `01_calibration` `clip_negative`（主锚 `line:50` ✓） | `:48` | `gain` 行 | 50 |
| `02_cosmetic` `cr_detection` | `:33` | `cosmetic_map_path` 行 | 35 |
| `02_cosmetic` `cr_sigma` | `:34` | `saturation_level` 行 | 36 |
| `02_cosmetic` `interpolation` | `:35` | `cr_detection` 行 | 37 |
| `04_psf` `psf_spatial_order` | `:33` | 表分隔符 `|---|---|---|---|` | 35 |
| `05_platesolve` `catalog` | `:33` | 表头 `| 字段 | 默认 | 单位 | 说明 |` | 35 |
| `05_platesolve` `tweak_order` | `:35` | `catalog` 行 | 37 |
| `05_platesolve` `max_matches` | `:36` | `catalog_version` 行 | 38 |
| `09_coverage` `connected_components` | `:31` | 表头行 | 34 |
| `08_drizzle` `order/pixfrac/pixel_scale/nside/sparse_snr_layer` | :48/:49/:50/:51/:52 | 分隔符与上一行 | :49/:50/:51/:52/:55（**§5 表被 :53 bullet 截断致孤立行**） |
| `06_photometry`（两键） | `:31`、`:32` | §4.1 散文判据行 | 配置表在 :73-77 |
| `defaults.json` → `07_noise_snr` | `:112`、`:113`、`:61`、`:62`、`:76`、`:186` | 空行 / 表头 / 方法学 bullet ×2 / 公式框 / 算子表行 | :215 / :215 / :210 / :211 / :231 / :216 |

即：**主锚（`"line"`）正确、证据锚（`"conflict.evidence"`）与本批文档的当前行号普遍差 2–3 行，对 07 则差 30–154 行**。机器面自己也承认这类漂移：`defaults.json:663` note "行号锚由 W5-CFG-002 重锚（原 :27 在文档重排后落到无关段落，数值零改动）"、`07_noise_snr.md:146` "工作树并发改动会使行号漂移"。

**（e）`module_id_migration_baseline.json` 数值现算（不执行脚本，按 `check_module_id_normalization.py:48-56` 的判定逻辑用 `grep/awk` 数）**

| 类别 | 阈值（本文件 `classes`） | 本文件 history 末条自述实测 | **现算** | 现算命令 |
|---|---|---|---|---|
| `PAGE_MISSING_MODULE_ID` | 20 | 14 | **16**（26 页 − 10 页有 frontmatter `module_id`） | 逐页 `awk 'NR>1 && /^---/{exit} /^module_id:/{print $2}' docs/modules/registry/*.md` |
| `MODULE_WITHOUT_PAGE` | 17 | 11 | **11**（`comm -23 map_ids page_ids` = 11 条：`astrocs.catalog.gaia`、6 个 `astrocs.infra.*`、`p1.calibration`、`p1.cosmetic`、`p1.drizzle`、`p3.resample`） | `awk '/^    module_id:/{print $2}' docs/modules/MODULE_MAP.yaml \| sort` 与页侧 module_id 集求差 |
| `AUTHORITY_MISSING` | 0 | 0 | **恒真零**：判定入口 `if e.get("added_by")!="MOD-002": continue`（`:66`），而 `grep -n "added_by" docs/modules/MODULE_MAP.yaml` **0 命中** ⇒ 分支永不执行；同期 `MODULE_MAP` 声明的 `module_yaml` 有 **5 条不在树中**（`lib/infrastructure/{aio,scheduler,benchmark,observability,hips_browser}/module.yaml`）⇒ 若该键存在则应为 ≥5 | 命令见左 |
| `index_module_count` | 23 | — | 23 ✓（MODULE_MAP `module_id` 23 条 = `00_INDEX §2` 23 行 = 本值） | `awk '/^    module_id:/{print $2}' … \| wc -l` |

⇒ 台账的阈值（20/17）与自述实测（14/11）与现算（16/11）三值不同轴且互不一致，`AUTHORITY_MISSING=0` 是无证据资格的恒真门（AGENTS §9"恒真门没有证据资格"、标准 01 §5 非退化判据）。

### 4.3 上游条款号错误的唯一源

`eng/tools/quality/gen_module_readmes.py:76` 模板常量硬写 `> 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）`，`:57-61` 的 `ERROR_SECTION` 硬写"exit 9，最高设计 §6.3"。二者经生成器扩散到 4 份生成页，再被 9 份手写页照抄，并被 ★`snr_estimator.md:3`、★`star_detector.md:3` 与 `docs/DOCUMENT_INDEX.yaml` 的全部 registry 条目复制。正值：模块与 ABI = **§8.5**（`:661`）；取消与退出码 = **§7.2**（`:508/:512`）。同一族里另有两个非模板带的自造错号：`astrocs.phase2.write.md:57`（§9→§10）、`06_photometry.md:12`（§3.6 不存在）。索引侧同类：`DOCUMENT_INDEX.yaml:1202+` 把 `19_runtime.md` 的 upstream 写成"§8.1（顶层结构）"，而 §8.1 标题是"总原则：唯一 CLI 入口、阶段独立调度器"，"顶层结构"是 §8.4。

### 4.4 配置键与默认值的多侧登记（文档表 ↔ schema ↔ defaults ↔ config_registry）

| 键 | 文档值 | schema | defaults.json | config_registry | 判定 |
|---|---|---|---|---|---|
| `drizzle.pixfrac` | 0.8（★08:50，声明"唯一来源=defaults"） | `algorithm_drizzle_pixfrac` 不声明默认，且称 defaults 为 `pending_authority` | `0.8` + `owner_adjudicated` | **1.0** | 文档对；两机器面错（其中 schema 的 authority_status 断言与 defaults 现值直接矛盾） |
| `star_detection.max_stars` | 20000 / 域 [20000,50000]（★03:64） | `minimum:20000, maximum:50000, default:20000` ✓ | — | 主锚 ✓ | **一致**（本批唯一逐项合流的样本） |
| `star_detection.detection_threshold` 等 §5.2 四键 | 5.0 / 2 / true / true（★03:84-87） | **0 命中**，且 `star_detection_config.additionalProperties=false` | `detection.threshold_sigma = 5.0` ✓ 同值 | `finding: gap`（"未进任何配置类"） | 文档列了合同面会拒的键 |
| calibration §5 七键 | `bias_path/dark_path/…`（★01:44-50） | 实名 `master_bias/master_dark/master_flat/…` | — | 主锚对、证据锚 off-by-2 | 键名两套 |
| psf §5 四键 / platesolve §5 五键 / photometry §5 三键 / coverage §5 两键 | 各页 §5 | **11 键全 0 命中**（`grep -c "\"<键>\""`） | — | 证据锚 off-by-2/3 | 配置表无契约 |
| `sparse_snr.density` | ——（★07:215） | — | `null` + `pending_authority`，constraint 里引 `07_noise_snr.md:112`（错行） | — | 机器锚错 |

### 4.5 机械列校准（我在核对 `doc_mechanical.csv` 时的反审结论）

| 列 | 结论 | 证据 |
|---|---|---|
| `lines` | 25/25 正确（与 `grep -c ''` 全等） | §1 各"行数"字段 |
| `meta_block` | **本批漏报 14 份**：12 份 registry `.md` 全部有 YAML frontmatter（含 `id/version/status/owner/source_commit`）判为 False；★`snr_estimator.md:47-53`、★`star_detector.md:5-10` 的尾部状态+纪律声明块亦判 False | `doc_mech_scan.py:26` 的 `META_PAT` 只认中文标记（`文档 ID/状态：/版本锚/纪律声明`）且不认 `---` 块；★00_INDEX/★01-09 判 False **正确** |
| `commit_sha` | 命中准确（40 位 sha 全在 frontmatter `source_commit`）；但注意这**不是正文里的 commit 引用**，而是生成器模板产物 ⇒ 修一处（模板）即清全族 | `gen_module_readmes.py:67 source_commit: @@HEAD@@` |
| `sha256` | 命中准确（锚内 4 个 sha256 + 生成器自述）；★`08_drizzle.md` 的 `index_sha256/archive_sha256` 属**产品字段名**、★`06_photometry.md:154` 的"sha256 64hex"属语义用词 ⇒ 该列在这两例是**方向性误报**（判为"含 sha256"但非文档锚） | `doc_mech_scan.py:24` `SHA256_PAT` 含裸词 `sha256` |
| `task_ids` | **严重漏报**：只扫头 40 + 尾 30 行，且模式不含本仓实际编号族。漏报样本：★`upm-apply` 的 `P2-XX-INT/P2-UPM-IMPL/DISP-P2UPM-00x`、★`07_noise_snr` 的 EXP-03/EXP-05（5 处）、★`06_photometry` 的 `PHOT-GATE-DROP-001`、★`writer.md` 的 `P3-FITS-INT/TEST`、★`baseline.json` 的 `MOD-002/W4-A3` | `doc_mech_scan.py:22/59` |
| `history_narrative` | 命中 2 例准确（★08:70"已作废"、★03:108/128"留痕"），**漏报 7 份**：★`sample:19/158`、★`upm-apply:25/135/159`、★`upm-fit:97/127/151`、★`phase3.writer:19/168`、★`07:126/177`（"旧组合方式/曾造成/已订正"）、★`06:76`（删除线承载旧值 + "尚未生效"）、★`baseline.json:30`（"已回退"）——HIST_PAT 只收 `旧版/作废/废止/留痕/已撤销/曾经/原先/上一版/前一轮/历史遗留/原口径/曾规定/退回至/降级为旧`，不含裸 `旧`、`曾造成`、`已订正`、`已回退`、`尚未生效` | `doc_mech_scan.py:25` |
| `dangling_links` | 文件级判定准确（9 份插件文档的 11 个 schema 路径全部命中 ✓）。**结构性不可判 3 类**：①行锚漂移（§4.2 a-d，共 40+ 处）；②不带反引号的路径（★`wcs.md:165` 的 `eng/tests/unit/p3_wcs_test.cpp` 为裸文本 ⇒ `BAREPATH_PAT` 要求反引号包裹，漏判）；③**节号不存在**（★`06:12` 的 §3.6；★registry 全族的 §8.4 错号） | `doc_mech_scan.py:29` |
| `unregistered_index` | 本批 25 份全 False ✓（与我的 `grep -F "<path>"` 复算一致） | 子串匹配口径偏松，但本批无误判 |

<!-- PROGRESS: 已审 25/25；台账 + 4 节完成 -->

## 5 覆盖率自报

- 已审 **25 / 25**（清单 `独立审计/批次清单/DB-03.txt` 去 CRLF 后 25 行，全部 `[ -f ]` 存在）。
- **未逐行读名单：空。** 25 份全部用 Read 工具通读全文（含最长的 ★`07_noise_snr.md` 257 行、★`astrocs.phase3.resample2.md` 215 行、★`astrocs.phase3.writer.md` 192 行、★`astrocs.phase3.wcs.md` 187 行、★`astrocs.phase2.sample.md` 171 行）。
- 越界读取（为判重复与上下游，按前言 §3 允许）：`lib/infrastructure/scheduler/src/module_adapters.cpp`（descriptor 与注册表）、`lib/infrastructure/scheduler/src/{module_adapters}` 之外的 `lib/algorithms/coverage/src/{upm,sampler}.cpp` 与 `include/astro/phase2/{upm,sampler}.h`、`lib/algorithms/{projection,resample,fits_output,star_detection,noise_snr}/**`、`eng/tools/quality/{gen_module_readmes,check_module_id_normalization,check_module_map}.py`（只读源码，未执行）、`eng/ci/check_registry_doc_sync.py`、`eng/contracts/schemas/phase_config_normalize.schema.json`、`eng/packaging/config/{defaults,config_registry}.json`、`docs/modules/MODULE_MAP.yaml`、`docs/DOCUMENT_INDEX.yaml`、`ASTROCS_DESIGN.md` 节表。只对清单项下结论。
- 本批未执行任何构建/测试/门/生成器；未 import 仓库 Python；未产生 `__pycache__`（收工核对见 §7）。

## 6 我证伪了任务书的哪个前提

1. **步骤 4"找出孤儿文档（无上游、无引用）"在本批不成立**：本批 25 份**零孤儿**——最少引用者数是 ★`docs/modules/star_detector.md` 的 4 个（`git -C … grep -l -F star_detector.md -- . \| grep -v -x -F <自身>`）。真实风险形态不是零引用，而是**仅索引/审计台账引用**：★`snr_estimator.md` 的 5 个引用者里 3 个是 `reports/v19r2/*` 与 `docs/audit/doc_classification.csv`，★`astrocs.phase3.verify.md` 的 6 个里同样有 3 个是台账/报表 ⇒ 现行"无引用=孤儿"判据抓不到这类**事实上无设计侧消费者**的页面。建议判据改为"排除 `reports/**`、`artifacts/**`、`docs/audit/**`、`DOCUMENT_INDEX` 后引用者为 0 即孤儿"。
2. **步骤 5"核对 DOCUMENT_INDEX 与文件树：未登记 / 登记不存在 / 角色标注不符"中的"角色标注不符"无法执行**：索引无角色字段——`grep -c "^\ *role:" docs/DOCUMENT_INDEX.yaml` = 0、`grep -c "topic:"` = 0；字段全集只有 `path/status/duty/upstream/downstream/notes`（status 取值实测 `ACTIVE_NORMATIVE 178 / ACTIVE_INFORMATIVE 63 / GENERATED 24`）。⇒ 本批把该维度改判为**身份（status）标注不符**（§3-B 三条），并登记"索引缺角色维"为结构性缺陷（与标准 01 §3"登记内容含路径、角色、主题、上游、下游"直接冲突）。
3. **前言 §5.4 关于 `commit_sha` 列"7–8 位十六进制词高估"的告诫在本批未出现**：本批 25 行的 `commit_sha` 命中全部是 40 位 frontmatter `source_commit`（`gen_module_readmes.py:67` 产物），零误报。
4. **前提复算成立（不证伪，仅固定口径）**：任务书"docs/ 当前约 233 份、30+ 子目录"按 `.md` 跟踪件口径**准确**——`git ls-files -- docs \| grep -c '\.md$'` = **233**，`git ls-tree -r -d --name-only HEAD -- docs \| wc -l` = **37** 个子目录；docs 全部跟踪文件 = 258（含 25 份非 `.md`）。此前"233 偏低"的说法源于口径未固定，本批以 `.md` 口径为准并给出命令。
5. **对"代码注释可作语义证据"的隐含前提**：`module_adapters.cpp:1173-1174` 注释自称"docs/modules/registry/astrocs.phase2.write.md 端口表同源：输入 ADU"，而同一 descriptor 的 `:1175` 写 `UnitId::SURFACE_BRIGHTNESS` ⇒ 注释与被注释的代码相互矛盾，注释不能当语义证据（前言 §5.1 的纪律在生产代码里同样需要执行）。

## 7 收工基线核对

收工命令与原文（与开工同一条）：

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? "ACSD\346\225\264\346\262\273\345\267\245\344\275\234\345\214\205_AUDIT-06.zip"
?? _measure_copy.py
?? site/
$ git -C "F:/Astro dev/Astro CS Normalization Database" rev-parse HEAD
c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9
```

开工时的同一命令输出为 `?? ACSD整治工作包_AUDIT-06.zip` + `?? site/`（两条同一路径，差异只是 git 的 `core.quotePath` 显示模式，非内容变化）。

**收工多出一个未跟踪项，按前言 §1 如实登记**：`?? _measure_copy.py`（`F:\Astro dev\Astro CS Normalization Database\_measure_copy.py`，1693 字节，mtime `2026-09-25 15:40:28 +0800`）。该文件**不是 DB-03 的取证动作产物**：其内容是官网文案度量脚本，读取对象为 `site/src/pages/{Home,Guide,Downloads,Gallery}.tsx` 与台账语域正则，与文档审计无关；DB-03 全程对仓库零写入（所有落盘只指向 `独立审计/证据/AUD-101-DB-03.md`，命令侧只有 `ls`/`git status`/`git ls-files`/`git grep`/`grep`/`awk`/`sha256sum`/Read），且开工至收工期间仓库 `HEAD` 未变、跟踪内容无 `M` 标记。判定为**并行工作流（站点文案测量）在同一工作树留下的未跟踪脚本**，属前言 §1 预期两项之外的第三方新增，建议由其 owner 移入 `run/` 或 `site/` 域，不与本批混计。

除该新增项外两次输出集合一致，且**全程无任何跟踪文件出现 `M` 标记、`HEAD` 未变** ⇒ 本批取证未污染被审对象的跟踪内容：全程未执行任何 `eng/**` 脚本、未执行构建/ctest、未 import 仓库 Python（`sha256sum`/`grep`/`awk`/`python3` 仅读文件文本，且未以模块方式加载仓库内 .py，工作树无 `__pycache__` 新增）。

## 8 本批处置建议汇总（供迁移合并清单直接取用）

| 处置 | 份数 | 路径 |
|---|---|---|
| 保留（含"保留 + 就地修/清锚/瘦身"） | 19 | ★`astrocs.phase2.resample.md`、★`astrocs.phase2.sample.md`、★`astrocs.phase2.session.md`（含迁出 registry）、★`astrocs.phase2.write.md`、★`astrocs.phase3.properties.md`、★`astrocs.phase3.resample2.md`、★`astrocs.phase3.verify.md`、★`astrocs.phase3.wcs.md`、★`astrocs.phase3.writer.md`、★`00_INDEX.md`、★`01_calibration.md`、★`02_cosmetic.md`、★`03_star_detection.md`、★`04_psf.md`、★`05_platesolve.md`、★`07_noise_snr.md`、★`08_drizzle.md`、★`09_coverage.md`、★`astrocs.phase3.resample.md`（若注册面暂不动则降级为非规范占位） |
| 合并 | 3 | ★`astrocs.phase2.upm-apply.md`（并入 fit 或 `docs/modules/phase2_upm.md`）、★`astrocs.phase2.upm-fit.md`（承接 apply）、★`06_photometry.md`（§4.1/§4.2 拆往 science/research，本页保留八节） |
| 删除 | 2 | ★`docs/modules/snr_estimator.md`、★`docs/modules/star_detector.md`（内容已分别被 07/03 与 ALG 文档更完整承载） |
| 迁移 | 1 | ★`docs/modules/registry/module_id_migration_baseline.json` → `artifacts/evidence/gates/`（过程台账不落正式文档层；迁移需同步三处硬编码路径，走变更流程） |

执行顺序上的唯一硬前置：`eng/tools/quality/gen_module_readmes.py:76`（§8.4→§8.5）与 `:57-61`（§6.3→§7.2）先行修正，并补 `eng/ci/ledgers/module_page_evidence.json` 或删去结论位——否则本批 3 份生成页（及全仓另外 1 份）在下次再生成时仍会复写同一族缺陷，而 `DOCUMENT_INDEX.yaml` 的 8 处 `status: GENERATED` 误标（本批 2 处 + 别批 6 处：`astrocs.phase1.{cosmetic,drizzle,noise-snr,photometry,star-psf,writer}.md`，各条目行号 `DOCUMENT_INDEX.yaml:875/881/893/899/917/929`）会让门的 `OWNERSHIP_CONFLICT` 报告被持续忽略（该门对 conflicts 不判红，`gen_module_readmes.py:468-476` 返回值不含 conflicts 计数）。

<!-- PROGRESS: 已审 25/25（清单全量，含四节附报与处置汇总） -->
