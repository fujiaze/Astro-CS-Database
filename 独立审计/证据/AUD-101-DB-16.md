# AUD-101 · 文档审计台账 · 批次 DB-16

审查节点：独立只读审查节点。仓库 `F:\Astro dev\Astro CS Normalization Database`，冻结基线 HEAD=`c8f64e9a`。
判据：`standards/01_文档权威链整治标准.md`（全文）+ `tasks/AUD-101_文档现状全量审计.md` + `templates/TPL_文档审计台账.md` + 上游正本 `ASTROCS_DESIGN.md`（§0、§2.2、§3.1、§5.3、§8）。

## 0 基线核对（开工 / 收工 git status --porcelain 原文）

开工（本代理执行，`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与派单预期的两项一致；`git rev-parse --short HEAD` = `c8f64e9a`。

收工：见本文件末节「收工基线」。

本批全程零写仓库、零构建、零 `ctest`、零 `run_checks.py`、零 `eng/**` 脚本执行、零 Python import。取证只用 `git status/ls-files/grep/show`、`wc -l`、`ls`、`Read`。`rg` 在本机不可用（Grep 工具报 `spawn rg ENOENT`），全部检索走 `git -C ... grep`。

---

## 1 逐份登记

登记字段顺序（固定）：`路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议`

### 1.1 `实验/absolute-snr/docs/snr-propagation-design.md`

| 字段 | 值 |
|---|---|
| 路径 | `实验/absolute-snr/docs/snr-propagation-design.md` |
| 标题 | 信噪比（SNR）在全流程中的传播：从单帧校准到成品的方案设计 |
| 行数 | 1528（`wc -l` 确定） |
| 角色 | 实际承载三种角色混杂：**算法推导**（§2–§5 公式与推导）、**架构设计**（§1.2 数据流、§4.7 权重优先链、§7 差距表）、**研究与佐证**（§6 参考文献与负面清单）。按标准 01 §2 应属 **算法推导** → `docs/algorithms/`；其 §6 应属 **研究与佐证** → `docs/research/`。实际所在目录 `实验/absolute-snr/docs/` 与应属目录不一致 = **角色错放**，应迁往 `docs/algorithms/SNR_PROPAGATION.md`（§6 单独并入 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md`），但**见下方"是否正本"判定：本文不得作为正本迁移**，实际处置是"降为佐证 + 拆分迁移" |
| 主题 | SNR 全流程传播（单帧定义 → 稀疏层 → Phase2 权重链 → 成品误差预算） |
| 上游 | **缺**。开头 `:3` 写"本文是 ACSD RELEASE-02 `SNR-DESIGN` 工作项的设计文档"，是任务编号而非最高设计条款。全文无一处 `ASTROCS_DESIGN.md §x` 作为自己的上游标注（`:507` 提到 `ASTROCS_DESIGN.md §2.2/§3.1/§4.4/§5.3` 是在引述"权威落点已改为"别人，不是声明自己的上游）。违反标准 01 §3「每份下层文档在开头位置标明其上游最高设计条款」 |
| 下游 | 命令：`git grep -n "snr-propagation-design" -- .`（排除 `reports/`）→ 命中 6 个引用者：`实验/absolute-snr/docs/EXP-05-ABSOLUTE-SNR.md:227,230,341,500`、`实验/absolute-snr/results/REVERSE_VERIFY_CANON.md:4,28,39`、`实验/shared/REVERSE_VERIFY_MIGRATION.md:27`、`实验/absolute-snr/code/reverse_verify/snr_design/README.md:3`、`实验/absolute-snr/code/reverse_verify/snr_design/exp1_weight_penalty.py:4`（注释指向**旧路径** `reports/RELEASE-02/snr-propagation-design.md`）、`eng/ci/ledgers/naming_surface.json:467`。**`docs/` 与 `lib/` 内零引用**：权威链文档无一引用它。命令与 0 命中证据：`git grep -n "snr-propagation-design" -- docs/ lib/ eng/tests/` → 无输出 |
| 是否正本 | **不是正本，且不得升为正本文**。理由：本文承载的 SNR 传播口径已在权威链落地三处——`docs/science/PSF_SIGNAL_WEIGHT.md` §2/§4/§7a（SCI-B 定案结论，`:120` 明确"依据：实验单元 `实验/absolute-snr/`"）、`docs/plugins/algorithms_phase1/07_noise_snr.md` §4/§4.1/§4.2/§4.2a/§4.5/§4.6、`docs/science/NOISE_MODEL.md` §5/§5a/§9a。而本文的**核心口径主张与这三处正本相矛盾**（见"与上位冲突"），且正文里被负责人裁决取消的设计仍以现行式子书写。它是**实验阶段的设计草案**（自己 `:6` 声明"只做方案设计，不改任何生产代码"），标准 01 §2 末条要求这类过程件落 `artifacts/evidence/` 并由正本引用 |
| 重复或重叠对象 | **4 组**，见 §3 同主题组（本文是其中三组的当事方）：① `docs/science/PSF_SIGNAL_WEIGHT.md` §4/§7a；② `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.1–§4.6；③ `docs/science/NOISE_MODEL.md` §5/§5a；④ `docs/research/SNR_WEIGHT_RESEARCH_PACK.md`（§6 参考文献表与负面清单重叠承载） |
| 元信息块 | **有**，开头即元信息块：`:3` 论文雏形 + 任务编号 `RELEASE-02 SNR-DESIGN`；`:4` 工作区声明；`:5` 产物落点；`:6` **性质与纪律声明**（"只做方案设计，不改任何生产代码…零 git 写"）。违反标准 01 §4「文档开头无元信息块（文档 ID、状态编号、版本锚、纪律声明等）」——四类禁令命中三类。另 `:1336` §12 整节「交付边界声明」是第二处纪律声明，`:1345-1353` 是可复现性元信息块（含 commit sha 与日期） |
| 写法违规 | 逐条（行号 + 原文片段）：<br>① `:3` "本文是 ACSD **RELEASE-02** `**SNR-DESIGN**` 工作项的设计文档" — 任务编号；<br>② `:3` "**论文雏形**" — 状态标注；<br>③ `:4` "工作区：`reverse_verify/`（**逆向验收区**）" — 已解散的根目录（ROOT-CONSOLIDATION）；<br>④ `:6` "**性质**：只做方案设计…**零 git 写**" — 纪律声明；<br>⑤ `:35` "> **【复核后（SNR-EXP-AUDIT，§13）】原结论的方向成立、量级需订正。**" — 任务编号 + 历史叙事（"原结论…需订正"）；<br>⑥ `:23` 与 `:687` "该定理对绝对与相对两种表示**同样成立**…因此**没有区分能力**" —— 对已被取消方案的反驳式叙述；<br>⑦ `:356` "> **【订正记录（SNR-DESIGN-01）】式 (2.5) 的…分解已由负责人裁决取消**" — 任务编号 + 负责人裁决 + 历史叙事；<br>⑧ `:432` "> **【已裁决（负责人 `GAP_AUDIT §9.46`）】SP-0 不是开关，是诊断量。**" — 负责人裁决引用（标准 01 §4 明禁"负责人某日裁决/指令"）；<br>⑨ `:465,543,558,569,639,673,897,900,1046,1319,1437-1479` 共 **11 处** "【复核后（§13…）】""【已裁决…】""【订正记录…】" 块 — 系统性历史叙事；<br>⑩ `:513` "键名保留为**历史记录**" — 直接把历史留痕写进设计；<br>⑪ `:1319` "此处**保留原建议以存证**" — 留痕用语（标准 01 §4 禁用词表命中）；<br>⑫ `:1342-1343` "原本**误落在** `eng/tests/validation/release02/snr_design/` 的 5 个脚本…**已迁移**…原目录**已删除**" — 迁移史叙事；<br>⑬ `:1348-1349` "仓库修订: git rev-parse HEAD = **ddf692edd3eb503b26e5579e8525043a47d8abf4** (2026-09-19 feat(reverse_verify): 新建逆向验收工作区…)" — commit sha + 日期 + 提交消息；<br>⑭ `:1352` "环境: numpy 2.2.4 / scipy 1.15.3 / astropy 7.0.1" — 环境快照（与同批 `frame-snr-survey.md:11` 的 `astropy 8.0.1` 互相矛盾，见 §2）；<br>⑮ `:201,236,243,429,511,527` 等 **20+ 处** `工程控制/RELEASE-02/GAP_AUDIT.md §9.41/§9.42/§9.46` 与"负责人强制方法论""负责人令"引用 — 负责人指令叙事 + 控制包条款号；<br>⑯ `:237` "**实测：`run/RELEASE-02/L4-rebuild/norm/t2_m1_red/calibrated_*.fts`**"、`:600`、`:1101`、`:1339` — 以 gitignore 的 `run/` 树作为可复现证据；<br>⑰ `:1441-1449` §13.5 负例套件表标题"负例 | 构造 | 结果 | 判定"中 `EXP-3 N3` 行 "**有意不归零**" — 与 §9 判据非退化纪律正面冲突的自述（有证据资格争议的项被标注为故意）；<br>⑱ 全文无 mermaid（标准 01 §4 要求"流程图用 mermaid"），`:96-118` 的数据流是 `text` ASCII 图 |
| 悬空引用 | 逐条（原文 + 确认命令与结果）。机械列 `dangling_links` 只报 1 条（H1）——**机械列在本件漏报 7 条**（该文件几乎不用 markdown 链接语法，全部引用写成反引号内 `路径:行`，机械检查器看不见）。**判定口径**：判"不存在"只用 `git ls-files`（跟踪集）＋ `git log --diff-filter=D`（删除历史），不以磁盘存在性为据：<br>**H1** `:1029` 与 `:1434,1457,1057` 引用 `reports/RELEASE-02/unc-prop-audit.md` / `unc-prop-audit §2.3`，是 `eps_theta = +3.4%/+2200%` 与 §13 复核值 `+20.1%/+608.6%` 双方共同依赖的**唯一外部证据** → `git ls-files reports/RELEASE-02/unc-prop-audit.md` = 0 命中；`git log --all --diff-filter=D -- <该路径>` = **`ae0f1e63`（2026-09-21）`chore(governance): 清理历史控制包与一次性报告（CLEAN-402）`** ⇒ **该证据件曾存在、已被治理清理删除**，本文件仍按它在世时引用它并据以对表；<br>**H2** `:848` "冻结目标式，`docs/science/q2-snr-smooth` 与 `variance_propagation.h:71`" → `git ls-files` = 0 且 `--diff-filter=D` 无记录 ⇒ **从未存在的科学文档**（真实对象是测试目录 `eng/tests/validation/release02/q2_snr_smoothness/`，`git grep -l q2-snr-smooth` 命中 10 个 `eng/tests/**` 路径）。式 (4.3) 的唯一科学层出处指向一份不存在的 `docs/science/` 件；<br>**H3** `:1222` "判据设计的四条纪律（`reverse_verify/README.md §3`）" → 跟踪集 0、无删除记录在册但目录级解散已由 `docs/DOCUMENT_INDEX.yaml:31` 记为"（2026-09-21 **ROOT-CONSOLIDATION**：reverse_verify/ 根条目解散、内容迁入 实验/）"，实际同内容件 = `实验/absolute-snr/code/reverse_verify/snr_design/README.md`（跟踪集存在）；<br>**H4** `:1285-1289` §10 表全部 5 行脚本路径写作 `experiments/snr_design/exp1_weight_penalty.py` … `exp5_error_budget.py` → `git ls-files "实验/absolute-snr/**/experiments/*"` = 0；真实路径无 `experiments/` 段（`git ls-files 实验/absolute-snr/code/reverse_verify/snr_design/` 直接列出 `exp1_weight_penalty.py` 等）。**§10 的"可复跑"清单 5/5 条路径不可达**；<br>**H5** `:1361-1362` §13 头"复核对象：… `experiments/snr_design/exp1..exp5`；证据 `run/reverse_verify/snr_design/audit/*.json`；代码 `experiments/snr_design/audit/`" → 同 H4 的 `experiments/` 错段，且证据落 `run/`（跟踪集 0）；<br>**H6**（原判）`:1295` §10 复核分片表列脚本 `audit/physnoise.py` → **经跟踪集复核后撤回该条**：`git ls-files 实验/absolute-snr/code/reverse_verify/snr_design/audit/` 输出 12 件，**含 `physnoise.py`**（先前一次 `ls` 读数有误，已按"判不存在只用跟踪集＋删除历史"的口径复验）。本件确证的悬空为 **7 条（H1–H5、H7、H8）而非 8 条**。<br>**H7** `:1164` "列在 `bibliography.md §9`" → 跟踪集 0、无删除记录；该文件自己的 `:1106` 给出的是 `实验/shared/references/REVERSE_VERIFY_BIBLIOGRAPHY.md`（跟踪集存在），即文内两处对同一对象用两套路径，一处可达一处不可达；<br>**H8** `:501` 引用 "`docs/plugins/algorithms_phase1/07_noise_snr.md:75`" 作为"『实际 SNR = 帧级 × 帧内』**逐字一致**"的权威落点 → 该行实为代码块**收尾围栏** ` ``` `（`git show HEAD:docs/plugins/algorithms_phase1/07_noise_snr.md | sed -n '75p'` 输出 ` ``` `）；且**全 `docs/` 树内不存在该句式**：`git grep -n "实际 SNR\|帧级 ×\|帧级×" -- docs/` 只命中 `13_integration.md:94` 的"帧级×帧内权重"（另一处，语义是测试项）。这条正是本文 §13.9 判定"与负责人分解式逐字一致"的**唯一权威支撑**，实为**指向空行的虚构权威** |
| 与上位冲突 | **8 条硬冲突**（标准 01 §1「下位文档与上位文档冲突时以上位为准；冲突本身是缺陷」）。择其要：<br>**C1 `F_ref` 口径反转（P0）** — 见 §3 组一，两处段落齐备；<br>**C2 稀疏层值语义仍按已取消的相对因子书写** — 见 §3 组二；<br>**C3 重建算子推荐与冻结词表冲突**：`:537` 表列 kriging/GP 为"**推荐**（附条件）"、`:540` "设计决策 D3：主算子用 **GP/kriging**"，而 `07_noise_snr.md:193` 规定算子必须满足正齐次性 `R[a·v]=a·R[v]`，并点名"带**数据无关固定先验均值**或向固定值收缩正则的算子（如固定先验均值的 GP/kriging）**不满足**该条"；`:534` 把双线性定为"作**基线**，不作推荐"，上位 `07_noise_snr.md:186` 把 `bilinear_regular_grid_v1` 定位为"保留为**对照/回退**"、默认是 `natural_bicubic_spline_clip_v1`（`:184`）。`:535` 称最近邻"仅用于 `max_radius_px` 受限的诊断"，上位 `:187` 称其为"**散点层唯一合法算子**"；<br>**C4 成品 SNR 面**：`:117` `SNR_mosaic = √(Σ w)·F̂`、`:982` 式(5.3) `SNR_mosaic(p) = signal(p)/sqrt(variance_mosaic(p))`、`:1075` 目标态 `snr/ : signal/sqrt(variance)` 作为现行公式书写；上位 `07_noise_snr.md:7` "**不替 Phase2/Phase3 产出 SNR**——Phase2…不输出 SNR 面，Phase3 无 SNR 数据块"、`:224` 同义。本文 `:1075` 自己也标"← 本文新增建议，需走合同变更"、`:1330` 列为上呈事项 ⇒ **同一文件里一个不存在的产物既按现行公式推导、又按待批建议登记**；<br>**C5 U1 虚构的权威打架**：`:1245` "`frame_snr` 的语义：文档两处互斥（`CONTROL_WEIGHT_SNR.md:11-14` 说「相对质量权重场，不是科学 SNR」vs `07_noise_snr.md:39` 说「未加权的原始通量型 SNR」）…**本文无权裁决**"，并 `:1315` 上呈负责人。被引方自己已定案：`CONTROL_WEIGHT_SNR.md:18-23` "**同名两义分离（claim `FIX-SCI-SNR-CANON-001`）**：Phase1 HiPS 文件头的 `frame_snr` 是科学量…本文件的 `local_snr`/`frame_snr` 是 stage2 内部相对质量场…各自具名、互不指代"；`PSF_SIGNAL_WEIGHT.md:150` "**已定案（原 UNRESOLVED）**：与 `docs/science/CONTROL_WEIGHT_SNR.md` §2a 的 `frame_snr` 语义冲突已按「同名两义分离」定案"。⇒ **本文 §9 U1 / §7.1 P1-7 `:384` "文档两处互斥（UNRESOLVED）" / §11.1 三条均建立在已撤销的状态上**，且引用行号 `:11-14` 落在定案注记（`:12-23`）之上，读的是半句；<br>**C6 `frame_snr` 上游条款指错**：`:2.3/:336` 的帧级 SNR 定义未标上游；`07_noise_snr.md:66` 明确该定义的"正本见 `docs/science/PSF_SIGNAL_WEIGHT.md`"，本文 `:1112` 却写"Horne 1986…（项目冻结引用，`PSF_SIGNAL_WEIGHT.md` §9）"——§9 是参考文献节，公式在 §2；<br>**C7 schema 键名与取值就地改名**：`:702` 冻结 `value_semantics : "relative_to_frame_snr"`；合同机器事实 `eng/contracts/schemas/unified/sparse_snr_layer.schema.json:300-301` 是 `sparse_snr_semantics` + `const: "absolute_flux_type_snr"`，`:302` "**声明为任何相对语义 ⇒ schema 判红**"。键名换了、值反了，且该取值按合同**判红**；<br>**C8 符号被就地改名/换定义**：`:87` 用 `W_info,k`（`docs/science/PSF_SIGNAL_WEIGHT.md:23` 用 `W_info`，一致；但 `07_noise_snr.md:33` 与合同对象名 `point_information` 用 `W_psf,k` ⇒ 上位两处本身不同名，本文未采任何一处并声明映射）；`:352` 造 `intra_k(x,y)`、`:495` 造 `rho_c`，权威链中**无此符号**（`git grep "intra_k\|rho_c" -- docs/ eng/contracts/` 无命中）；`:923-924` 造"链 A / 链 B"两套权重命名，上位 `PSF_SIGNAL_WEIGHT.md:80` 是"**没有可选择的口径**：不存在口径选择键、口径枚举、口径配置项或口径产物"；`:706` `correlation_length_px` 作为"必需输入"入产品，`git grep -l correlation_length_px` 全仓只命中本文与该实验的两个脚本/JSON，**schema 与设计层均无此键**，而 `:471,528` 已按"本设计 §3.3 已要求 `correlation_length_px` 入产品"当既成事实陈述 |
| 处置建议 | **迁移 + 降级**（组合动作，非单一处置）。(a) 把 `:1336` §12 交付边界声明、`:1345-1353` 可复现性块、`:1357-1524` §13 复核记录整段移出文档层，落 `artifacts/evidence/`（标准 01 §2 末条：「需要留证的台账落 `artifacts/evidence/` 并由正本引用」）；(b) §6 参考文献与负面清单并入 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md`（标准 01 §2：「同一角色内同一主题只有一份正本」）；(c) 剩余的算法推导正文（§1–§5，剔除全部【复核后/已裁决】注记、剔除 C1–C8 与上位冲突的口径后）迁往 `docs/algorithms/SNR_PROPAGATION.md`，并补开头「上游：ASTROCS_DESIGN.md §2.2/§3.1/§5.3」（标准 01 §3 双向索引）。理由条款：标准 01 §1「权威链只有一条」+ §2「每份文档属于且仅属于一个角色」+ §4「只写现行设计要怎样」+ §5.6-01 判定"走得通"要求沿途"无同主题第二份正本、无悬空链接、无角色错放"，本文件四条全违 |

### 1.2 `实验/absolute-snr/docs/surveys/f-instr-survey.md`

| 字段 | 值 |
|---|---|
| 路径 | `实验/absolute-snr/docs/surveys/f-instr-survey.md` |
| 标题 | F-INSTR 逐条记录 —— 星点通量口径（文献 / 开源科学软件 / 本仓权威） |
| 行数 | 501 |
| 角色 | **研究与佐证**（文献/开源一手核对逐条台账），实际所在目录 `实验/absolute-snr/docs/surveys/`；应属目录按标准 01 §2 表 = `docs/research/` 或 `docs/references/`。角色本身与"实验单元内部佐证件"不冲突，但它被 `docs/science/PHOTOMETRY.md` 侧的定案（经 `f-instr-canon.md`）当作**核对状态的最终依据**（`f-instr-canon.md:4` "核对状态以该文件为准"）——这是**权威链倒置**（标准 01 §1「权威程度自上而下递减，每一层只由它的上一层推出」）。标 **角色错放**：应迁往 `docs/research/F_INSTR_SURVEY.md`（或由 `f-instr-canon.md` 定案吸收后随定案一起处理） |
| 主题 | 星点通量口径（孔径 vs PSF 总通量）与 Gaia XP 合成测光的一手出处核对 |
| 上游 | **缺**。`:3` 只有"工作项 **F-INSTR-SURVEY**（RELEASE-02 裁决 A6）"与"配套定案：`实验/absolute-snr/docs/f-instr-canon.md`"，无最高设计条款 |
| 下游 | 命令 `git grep -n "f-instr-survey"`（排除 `reports/`）→ 5 个引用者：`实验/absolute-snr/docs/f-instr-canon.md:4,26,492`、`实验/absolute-snr/code/reverse_verify/f_instr/README.md:4`、`实验/shared/REVERSE_VERIFY_MIGRATION.md:31`、`eng/ci/ledgers/naming_surface.json:468`、`eng/ci/check_cite_claim_mark_consistency.py:113,198,253`。**`docs/` 内 0 引用**：`git grep -n "f-instr-survey" -- docs/` → 无输出。非孤儿（有实验单元内与 CI 侧引用），但**不在权威链内** |
| 是否正本 | **不是**。同主题正本 = `docs/science/PHOTOMETRY.md`（`[F-30]` 自己 `:444` 承认直接读它）与 `docs/science/PSF.md`（`[F-31]`）。本文是它们的核对台账，标准 01 §2「研究与佐证类文档…以引用方式服务正本，不重复承载正本口径」——但本文 **§F 整节违反该条**（见下行） |
| 重复或重叠对象 | **重叠对象 = `docs/science/PHOTOMETRY.md` + `docs/science/PSF.md`（`docs/research/` 的对照面另见 §3 组四）**。举证（标准 01 §4 要求贴两处段落）：<br>本件 `:433-436`「**通量定义**（原文 `:14-17`）：`F_instr` \| 仪器通量 (ADU；e⁻ 需 gain，当前不可得) \| 输入 / `F_syn` \| 合成通量 = `∫F_λ(λ)·T(λ)·Q(λ)·λ dλ`（Gaia 星表模型）/ `r_i` \| `log10(F_instr/F_syn)` dex」<br>正本 `docs/science/PHOTOMETRY.md:14-17` 同四行定义（本件逐字搬运）<br>**重叠范围**：`F_instr`/`F_syn`/`r_i`/`delta_i` 四量的定义与单位口径被**整段复制**到佐证件；差异仅在佐证件加了 `:14-17` 行号——正本一改行号即失效。**同一重叠见 `:447-450`（对 `docs/science/PSF.md:21,52,87` 的 `flux = 2πA·sxsy/3` 与 ADU 单位说明逐字复制）** |
| 元信息块 | **有**：`:3-4` 工作项 ID + 裁决编号 + 配套定案 + 规则出处四行块。违反标准 01 §4「文档开头无元信息块（文档 ID、状态编号…）」 |
| 写法违规 | ① `:3` "工作项 **F-INSTR-SURVEY**（**RELEASE-02 裁决 A6**）" — 任务编号 + 负责人裁决；② `:4` "规则（`reverse_verify/README.md §5`）" — 已解散目录；③ `:20-22` "**工具限制（诚实登记）**：本会话 `web_search` 工具…ADS 记录页对脚本返回 405/JS 挑战；OUP(MNRAS) Cloudflare-403" — 会话过程流水（标准 01 §4 只写现行设计）；④ `:24` "## 本轮核对发现的引用订正清单（**重要**）" — "本轮"过程语；⑤ `:383` "这正是**负责人指出**的「只是用加法增加做实验，那没有区别」的问题" — 负责人指令叙事（同种另见 `:401` "**本工作项按负责人令**优先真实帧作底"）；⑥ `:431` "`docs/science/PHOTOMETRY.md`（SCI-PHOT-001，FROZEN，**2026-08-23**）" — 日期 + 状态编号；⑦ `:221` "（版本 v30_0_11 / Current **2026-08-06**）" — 抓取日期；⑧ `:41,105,148,157,169,…` 条目编号 `[F-xx]` 全文体系，且 **`[F-35]`/`[F-36]` 被排在 `# D` 节内 `F-24` 之后、`# E` 之前（`:344,357`）**，编号乱序且落位错节 —— 结构缺陷；⑨ 无 mermaid 图（标准 01 §4） |
| 悬空引用 | **D1** `:4` "`reverse_verify/README.md §5`" → MISSING（`test -e reverse_verify` 空；`docs/DOCUMENT_INDEX.yaml:31` 记"reverse_verify/ 根条目解散、内容迁入 实验/"）。<br>**D2** `:479`（及 `:3` 的配套声明）指向的定案 `实验/absolute-snr/docs/f-instr-canon.md` **存在**，但反向看：定案件 `:26` 与 `:492` 用 **`references/f-instr-survey.md`** 引本文 → 该相对路径解析为 `实验/absolute-snr/docs/references/f-instr-survey.md`，`test -e 实验/absolute-snr/docs/references` = **MISSING**（目录不存在）。⇒ **本文被自己的定案件以失效路径引用**，标准 01 §3「双向可追溯」在两个方向上都断。**机械列 `dangling_links` 对本件记 `['lib/include/lsst/meas/base/PsfFlux.h']` 一条，漏报了 D1 与 D2 两条**（`..`/相对路径与反引号内路径未被判定）。<br>**D3** `:221` 引用 `lib/include/lsst/meas/base/PsfFlux.h` 标注为"**源码逐字原文，已核对**，`lsst/meas_base@main`" —— 这是**外部仓库文件**（机械列把它当本地悬空文件报了，属**误报**：本条不是仓库内引用）。但需登记：正文未注明它是外部仓库的仓库内相对路径写法，读者按本地路径解析必失败 —— 写法歧义，非悬空。<br>**D4** `:254-256` 自证式悬空："arXiv:1612.05244 对应的 ApJS 卷/文章号本轮**未独立核对**…与 'Pan-STARRS Pixel Analysis…' **题名不同** ⇒ 两者关系**待核对**" —— 该条把未核关系当证据引用（见"与上位冲突"行的 Magnier 条号冲突） |
| 与上位冲突 | **K1（同批内互斥，非与 docs/）**：`:243-256` `[F-16]` 把 Magnier et al. 2020 记为 **ApJS 251, 6, DOI 10.3847/1538-4365/abb82a**，并把逐字引文取自 arXiv:1612.05244；同批 `frame-snr-survey.md:25` 已判"psphot 论文 = Waters et al. 2020 ApJS 251, 4"是**对调了**，正确是 "**Magnier et al. 2020, ApJS 251, 5**, arXiv:1612.05244, **DOI 10.3847/1538-4365/abb82c**"。⇒ 同一篇文献在**同一批次两份文件**里文章号差 1、DOI 尾码差一档；本文未采计。**这是本批最重要的对表项之一**。<br>**K2** `:469` "这些量目前**只服务 SNR 模块**，未进入测光标定路径"与 `:489` "**与 [F-30] `:95` 的冻结口径冲突**"是对生产实现的事实判定；现行正本已把它落成裁决（`docs/science/PHOTOMETRY.md` 的 PSF 拟合域口径 + `f-instr-canon.md:16` "A6 不是改规范，是修实现符合性"）。本文作为佐证件保留了"冲突"进行态，未登记其为已定案事项 —— 与上位状态不一致（状态轴：`已定案` vs `本文写"冲突"`）<br>**K3** `:470` "**HST 数据 —— 本工作区无哈勃数据**（全仓 `find -iname '*hst*'` 命中 0）"（此句实在 `frame-snr-survey.md:470`，本文 `:169,173` 反复以 HST/ACS 为对照场景）与 `testdata/README.md:55` "## 3. `testdata/HST_M16/` — HST WFC3/UVIS M16 三帧 drz（只读，不入库）"、同批 `EXP02_TABLES.md:78` "表 B1 **HST M16 真实模板** + 物理前向仿真"、`EXP03_TABLES.md:106` "表 B1 **HST 前向臂**"、`EXP04_TABLES.md:174` "**HST M16**（2048 crop）：Δ\* = 16" 相互矛盾。见 §2 组三 |
| 处置建议 | **迁移**。目标 `docs/research/F_INSTR_SURVEY.md`。理由：标准 01 §2 角色表把"文献综述、开源对照、研究包"固定到 `docs/research/`、`docs/references/`，而本文是**唯一持有 `[F-xx]` 核对状态**的件（定案件 `:4` 明文"核对状态以该文件为准"），留在 `实验/…/surveys/` 使权威链上"科学定义 → 佐证"的指向落到正式文档层之外；迁移同时必须删除 §F 对 `PHOTOMETRY.md`/`PSF.md` 的逐字复制（标准 01 §2「研究与佐证类文档…不重复承载正本口径」）并把 `[F-35]/[F-36]` 归位到正确节 |

### 1.3 `实验/absolute-snr/docs/surveys/frame-snr-survey.md`

| 字段 | 值 |
|---|---|
| 路径 | `实验/absolute-snr/docs/surveys/frame-snr-survey.md` |
| 标题 | 帧级 SNR（frame-level SNR）文献与开源实现调研记录 |
| 行数 | 473 |
| 角色 | **研究与佐证**（一手文献/开源 SNR 口径逐条台账）；实际目录 `实验/absolute-snr/docs/surveys/`，应属 `docs/research/`。同 1.2：**角色错放**，应迁往 `docs/research/FRAME_SNR_SURVEY.md` |
| 主题 | 帧级 SNR 在各巡天/工具/文献里的定义，以及"哪些口径会被天光抬高"的判定 |
| 上游 | **缺**。`:3-4` 给的是"工作项 FRAME-SNR-CANON（RELEASE-02）/ 代码 / 定案 / 中间产物"，无最高设计条款。它本该挂的上游（`ASTROCS_DESIGN.md` §2.2、`docs/science/PSF_SIGNAL_WEIGHT.md`）均未标 |
| 下游 | 命令 `git grep -n "frame-snr-survey"` → 5 个引用者：`实验/absolute-snr/docs/frame-snr-canon.md:3,25`、`实验/absolute-snr/code/reverse_verify/frame_snr/README.md:6`、`实验/shared/REVERSE_VERIFY_MIGRATION.md:30`、`实验/shared/references/REVERSE_VERIFY_BIBLIOGRAPHY.md:236`、`实验/shared/references/reverse_verify_bibliography.bib:386`、`eng/ci/ledgers/naming_surface.json:469`。**`docs/` 内 0 引用**（`git grep -n "frame-snr-survey" -- docs/` → 无输出）。注：`REVERSE_VERIFY_BIBLIOGRAPHY.md:236` 只写裸文件名"见 `frame-snr-survey.md`"，无路径 ⇒ 不可解析（写法缺陷，见下行） |
| 是否正本 | **不是**。正本 = `docs/science/PSF_SIGNAL_WEIGHT.md` §4（单一权重口径）与 `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.1（帧级 SNR 红线）。本文是二者的一手依据台账；`PSF_SIGNAL_WEIGHT.md:122` 的 SCI-B 定案注记引的是 `results/b1..b6*.json`，**未引本文** ⇒ 佐证链缺一环（正本知道 JSON，不知道给出这些 JSON 之口径判定的调研件） |
| 重复或重叠对象 | **重叠对象 = `docs/research/SNR_WEIGHT_RESEARCH_PACK.md`（同主题：PixInsight 图像加权方法学 + Horne/Naylor/Zackay-Ofek + 逆方差换算适用条件）。跨批？不跨，正本对在本件与 `docs/research/` 之间。**举证：<br>本件 `:430-437`「**一手依据（逐字，PixInsight 官方《New Image Weighting Algorithms》2.7 节）**：> "The SNR estimator is defined as [20] where is a scale estimate calculated for the image and is… **This corresponds to the ratio of powers standard formulation of signal-to-noise ratio.**" …> "The frame at index 13 … has a big airplane trail that introduces a **strong bias in the variance used as the numerator of the SNR equation** (see Equation [20])."」<br>重叠方 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md:132`「标准 SNR（式[20]）：全局尺度估计/噪声方差，官方指出受背景梯度与天光正向影响」（同一式号、同一结论），并在 `:119-122` 给出**带版本与行锚的同一份一手来源**（`gitlab.com/pixinsight/Reference-Documentation`，`master` = commit `08b8eb85ae17`，`02-PSF_Flux_Weighting_Algorithms.pidoc:303-320`）。<br>**差异**：本件给逐字英文引文与 Figure 9 说明、无版本化行锚；研究包给版本化行锚与 pidoc 定位、无逐字英文引文 ⇒ 同一份一手来源的两套独立核对记录，**互不引用**（本件 `:442` 只把常数甩给 `docs/science/PSF_SIGNAL_WEIGHT.md:44`）。另 `:383-393` `[C4]` Zackay & Ofek 与 `:345-357` `[C1]` Horne 与研究包 `:149-153`/`:157-159` 逐条重复同一书目与同一"结论要点"。**按标准 01 §2「同一角色内同一主题只有一份正本；多份内容合并为一份」→ 应合并** |
| 元信息块 | **有**：`:3-13` 十一行块 —— 工作项 + RELEASE-02 + 代码路径 + 定案 + `run/` 中间产物 + 记录规则出处 + 本轮核对方式 + 本机库版本快照（"photutils 3.0.0 / sep 1.4.1 / astropy **8.0.1**"，装在 `/dev/shm/...`）+ "本轮没有编译运行 IRAF/PSFEx/SExtractor 二进制"。全部命中标准 01 §4 禁令（文档 ID、状态编号、纪律声明） |
| 写法违规 | ① `:3` "工作项：**FRAME-SNR-CANON**（**RELEASE-02**）" — 任务编号；② `:6` "**记录规则**（`../README.md §5`）" — 相对链接（机械列不判，见下）；③ `:10-12` "**本轮核对方式**…装在 `/dev/shm/astrocs_fsnr/frame_snr_canon/pylibs`（**非系统环境**）" — 会话/环境流水；④ `:17` "## 0 必须先纠正的前提（比编一个更值钱）" — 过程语域；`:19` 表头 "任务书/常见说法" — 任务书叙事；⑤ `:63` "实现落点：`lsst/pipe_tasks`（**commit `0e56ae0`**）" — commit sha（同种：`:51` "Jones, R. L. (**2016/2026**)" 年份含斜杠区间、`:176,197,181` 多处"待核对/本轮未…"过程登记语）；⑥ `:21,270,272,393,404` "本环境不可达：TLS unexpected eof / 502"、"OpenAlex 记录 `oa_status: closed`…ADS 405 WAF、scixplorer 202 JS 挑战、IOPscience 反爬、Massey PDF 本工具链不解析" — 网络故障流水，标准 01 §4「只写现行设计要怎样」；⑦ `:331` "**这是 sep 比 photutils 强的地方**"、`:363` "**本调研最该抄的一条**"（`:263`） — 口语评价混入正文（非结论、非条款）；⑧ 无 mermaid |
| 悬空引用 | **D1（机械列漏报的典型）**`:6` "**记录规则**（`../README.md §5`）"：从 `实验/absolute-snr/docs/surveys/` 解析 → `实验/absolute-snr/docs/README.md`；确认命令 `test -e "实验/absolute-snr/docs/README.md" && echo EXISTS || echo MISSING` → **MISSING**（`find 实验/absolute-snr/docs -type f` 列 12 个文件，无 README）。真实规则件在 `实验/absolute-snr/code/reverse_verify/frame_snr/README.md` 与 `实验/shared/references/REVERSE_VERIFY_BIBLIOGRAPHY.md §0`（`REVERSE_VERIFY_MIGRATION.md:79` 记"并入 …§0（逐字保留）"）。**机械列 `dangling_links=[]` 对本件漏报此条**（以 `..` 开头未判，符合派单 §4.4 预告）。<br>**D2** `:4` "中间产物：`run/reverse_verify/frame_snr/`"、`:316` "`run/reverse_verify/frame_snr/external_crosscheck.json`"、`:425`/`:443`/`:455` "`run/reverse_verify/frame_snr/redlines_physical.json` 的 `P10` 红例 A/B/C" —— `run/` 按 `AGENTS.md` §7 与 `.gitignore` **不入库**；`git ls-files run/` → 0 命中。本文 §5 三条"本仓数值证据"与 §5 D4"能红能绿由同一函数保证"的**全部数值锚点都落在不可复现目录**，且**本件唯一被 `docs/` 引用的 SCI-B 定案（`PSF_SIGNAL_WEIGHT.md §7a`）引的是 `results/*.json`（入库），未引 `run/`** ⇒ 同一批证据两套落点，红例证据不可复核。<br>**D3** `:105` "草稿 PDF http://www.astro.Princeton.EDU/~rhl/photo-lite.pdf"、`:376` "`http://www.star.bris.ac.uk/~mbt/daophot/mud9.ps`"（f-instr 同件 `:376`）等外部 URL 本节点无法核验可达性（审查节点只读、不联网取档）；**不判悬空，登记为"本节点不可核"**，与 §2 的"待核对"一并留给前台。 |
| 与上位冲突 | **N1（与同批互斥，见 K1 同源）**`:11` "本机 … **astropy 8.0.1**" vs 同批 `snr-propagation-design.md:1352` "numpy 2.2.4 / scipy 1.15.3 / **astropy 7.0.1**" —— 同一实验单元（SCI-B）两份设计/调研件报出**不同主版本**的依赖快照，则两份的"实跑"证据不能互认。<br>**N2** `:25` Magnier et al. 2020 = **ApJS 251, 5, DOI …abb82c** 与 `f-instr-survey.md:243` 的 **ApJS 251, 6, DOI …abb82a** 互斥（见 1.2 K1）。<br>**N3** `:164-169` `[A6]` "**HSC 不定义任何帧级 SNR 标量**…论文**从未写出 `α_MF/σ_MF` 这个比值**"、`:34` "七个巡天/望远镜体系里，**没有任何一个在星表或图像产品中定义「帧级 SNR 标量」**"、`:44-45` "**ACSD 的帧级 SNR 不能声称'沿用'上述任何一家**"。上位 `PSF_SIGNAL_WEIGHT.md:41` 与 `07_noise_snr.md:57` 的写法是"**frame_snr 对标此量的**方法学"（只对标方法学、不声称沿用），两者**不矛盾**；但本文 `:5` 与 `:44` 未把这一层关系指向 `07_noise_snr.md` §4.1 的既有定案，而 `07_noise_snr.md` 也没引本文 ⇒ 该"不能声称沿用"的边界结论**在权威链里无落点**（读者只能从 `07_noise_snr.md:57` 的"独立标定常数、不照抄 c3/c4"倒推）。标准 01 §3「每份下层文档在开头标明上游条款」缺失导致这条只能靠推断。<br>**N4** `:202` 的 `W = 1/(Var × scale⁴)` 与 `:29` 对 DrizzlePac 手册"weight image is proportional to the inverse variance"的证伪，直接涉及上位 drizzle 口径（`docs/science/UNCERTAINTY_AND_COVARIANCE.md`、`docs/algorithms/DRIZZLE_GEOMETRY.md`）；本件未指向任一 drizzle 正本，`docs/` 侧也未采计本件的证伪 ⇒ 双向缺失 |
| 处置建议 | **合并**。并入 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md`（PixInsight 方法学与 Horne/Naylor/ZO 文献一节），并在合并后由 `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.1 与 `docs/science/PSF_SIGNAL_WEIGHT.md` §7a 增补指向该调研件的索引指针。理由：标准 01 §2「同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除」+ §3「每份文档、每个机制都能追溯到最高设计的一条要点」。`run/` 落点的三条红例证据随迁改登记为 `artifacts/evidence/`（§2 末条） |

### 1.4 `实验/absolute-snr/results/COMPARISON_TABLES.md`

| 字段 | 值 |
|---|---|
| 路径 | `实验/absolute-snr/results/COMPARISON_TABLES.md` |
| 标题 | SCI-B 对比表（由 results/*.json 自动生成，勿手改） |
| 行数 | 168 |
| 角色 | **研究与佐证**（实验结果表）。实际目录 `实验/absolute-snr/results/`，与 `AGENTS.md` §7 对 `实验/` 的定义（"报告…code、results、data"）**一致** ⇒ 目录不错。按标准 01 §2 表，"标准与检查/研究与佐证"的正式层目录是 `docs/standards/`、`docs/ci/`、`docs/research/`、`docs/references/`；本件留在 `results/` 属"实验单元自包含证据"，与 §2 末条"需要留证的台账落 `artifacts/evidence/`"是**两个未调和的落位口径**（本批 4 件共性问题，见 §2） |
| 主题 | SCI-B（b1..b7）七类结果表的机器汇总：天光扫描、σ_F 项扫描、三口径适用域、存储代价、逆方差集成对拍、判据非退化审查、Phase3 传递 |
| 上游 | **缺**。全文无上游标注，无指向生成它的 `README.md`/`REPORT_paper.md`，也无最高设计条款；`:3` 只给生成命令 |
| 下游 | 命令 `git grep -n "COMPARISON_TABLES"`（排除 `reports/`）→ 4 个引用者：`实验/absolute-snr/README.md`、`实验/absolute-snr/REPORT_paper.md`、`实验/absolute-snr/code/make_tables.py`、`实验/absolute-snr/results/REVIEW.md`。**`docs/` 内 0 引用**（`git grep -n "COMPARISON_TABLES" -- docs/` → 无输出）⇒ 权威链不认它，但 `docs/science/PSF_SIGNAL_WEIGHT.md §7a` 与 `07_noise_snr.md §4.2a` 引的 `results/b1..b7*.json` 正是它的输入 —— **它和正本读同一批 JSON，却不与正本建立引用**（标准 01 §3 双向断） |
| 是否正本 | **不是**（GENERATED 件）。它是 JSON 的人读投影；正本口径在 `docs/science/PSF_SIGNAL_WEIGHT.md` §4/§7a。按 `docs/DOCUMENT_INDEX.yaml` 字段口径，其 `status` 应为 **GENERATED**；按标准 01 §4"公式、数值表…放对应下层文档"，它位置正确、但**没有任何一处标明它对应哪份下层正文** |
| 重复或重叠对象 | **重叠对象 = `实验/absolute-snr/results/EXP04_TABLES.md`（同批内，存储代价表）与 `docs/science/PSF_SIGNAL_WEIGHT.md` §7a.7。举证：**<br>本件 `:104-115`「## T5 存储代价（4096^2/帧，**float32**；预算 1 MiB/帧）：… sparse Delta=64 \| **16384** \| **0.01562** \| 0.0156 x … frame \| **4** \| 0.000004」<br>重叠方 `EXP04_TABLES.md:225-233`「存储量（生产帧 4096²）：sparse \| 64 \| 4096 \| **32768** \| **0.0312** \| 0.0312 … frame \| — \| 1 \| **8** \| 0.0000」<br>**差异 = 全部存储数字差恰好 2×**（本件按 4 B/点 FP32，EXP04 按 8 B/点 FP64 且**未标 dtype**）。同批两份"自动生成"件对同一个结论性判据（"稀疏层远低于 1 MiB 预算 / 稠密超门 64×"）给出两套字节数，而**该判据的"64 倍"一侧两件一致（67,108,864 B = 64 MiB）**，只有"稀疏一侧"差 2×。正本 `PSF_SIGNAL_WEIGHT.md:131` 只写了稠密侧"67,108,864 B = 64 MiB/帧 = 1 MiB 预算的 64 倍，稠密超门结论成立"，未采计稀疏侧 ⇒ 稀疏侧数值当前**两份无正本地并存** |
| 元信息块 | **轻度**：`:1` 标题内含状态声明"（由 results/*.json 自动生成，**勿手改**）"、`:3` 生成命令。无文档 ID/日期/裁决。基本符合标准 01 §4，但缺"上游"一行 |
| 写法违规 | ① 无日期、无任务编号、无 sha、无 sha256、无历史叙事 —— **机械列对本件各项均为 False/空，与实读一致**（校准结论：本件的机械列无漏报无误报）；② 唯一写法缺陷是**列名不带定义**：`:7` 表头 `SNR_def \| SNR_MC \| prod skyonly+RN \| prod empirical+RN \| prod empirical only \| 传统口径 \| 不扣背景 \| 算术常数` —— 八个口径列在本文件内**无一给定义或指路**，需回到 `README.md`/`07_noise_snr.md §4.2a` 才知道 `skyonly+RN`/`emp+RN` 对应 `shot_noise_only`/`empirical_total_rms`（标准 01 §4「自然语言把设计讲清楚」：表自身不讲清且不指路）；③ `:13` 行 `B=10` 处 `SNR_def=42.7213` 而 `SNR_MC=44.0719`（MC **高于**定义值），`:14` `B=100` 同向 —— 与 `:7` "亮源 F=3000 e-" 扫描的"MC 与定义一致"用途相反，文件内**无任何解释或不确定度提示**（95% CI 给了但 CI 宽度覆盖不了方向性趋势的解释） |
| 悬空引用 | **无 markdown 链接**。`ls -d` 确认 `:3` 的 `python3 实验/absolute-snr/code/make_tables.py` **存在**（本件是全批唯一写成仓库根相对路径的生成命令；同批 `EXP02_TABLES.md:3` 写 `python3 code/exp02/make_tables.py`、`EXP03/EXP04` 同样写成相对 `实验/absolute-snr/` 的路径 ⇒ 同目录内复现命令书写规范不一致，其中 3 件按仓库根 cwd 直接不可执行） |
| 与上位冲突 | **P1 `rho` 符号残留（本件最高价值）**：`:75` 「## T4 三口径适用域（**RMSE(log10 rho)**，Delta=64；真实数据为 16 px hold-out 值）」—— 本件的适用域主表把 SNR 场写成**相对因子 `rho`**（`rho_c = SNR_c/SNR_frame`，`snr-propagation-design.md:495` 定义，且该定义已被负责人裁决取消、被 `sparse_snr_layer.schema.json:302` 判红）。合同事实 `sparse_snr_layer.schema.json:375`「每点的 sparse_snr_value 是该点的**绝对** SNR（**不是相对因子**）」，`07_noise_snr.md:236`「把控制点值按相对因子解释（含乘/除帧级标量做还原）⇒ **必须判红**」。⇒ 一份 GENERATED 结果表的度量名，用了上位明令判红的表示。<br>**P2 `frame_median` 与 `frame_mad` 恒等列**：`:77-102` 表 T4 第 6/7 列 23 行**逐行完全相同**（`0.0000/0.0000`、`0.0230/0.0230`、`0.0538/0.0538`、`0.0532/0.0532`…），两列承载零差异信息 —— 按 `AGENTS.md` §9「空断言、SKIP 充数…都算未完成」的同级纪律，恒等列要么生成器把同一量写了两遍（表自称区分 median 与 MAD 两种帧标量），要么两标量在该数据集上恒等而文件未声明。当前状态是**无解释的重复列**。<br>**P3 `Delta*` 列 15/23 行为 `None`**：`:77-102` 中"稀疏-帧级 Delta\* [px]"在全部 s>0 的合成面上取 `None`（未跨越），但 `:102` 与 `EXP04_TABLES.md:174` 都把 Δ\* 作为核心结论量；本件未给 `None` 的语义（未跨越 vs 未算 vs 不适用）。同一 Δ\* 在 `EXP04_TABLES.md:172` 定义为"RMSE 首次超过帧级臂"、在 `07_noise_snr.md:102` 定义为"E 首次劣于帧级臂"，两处定义不同（见 1.7 与 §2 组二） |
| 处置建议 | **保留**（位置与角色正确，属实验单元自包含证据），但**必须补两件事**才符合标准 01 §3/§4：① 开头补一行「上游：SCI-B 实验单元 `README.md`；口径正本 `docs/science/PSF_SIGNAL_WEIGHT.md` §4/§7a」；② 把 T4 表标题里的 `RMSE(log10 rho)` 改为绝对量名（与 `sparse_snr_semantics = absolute_flux_type_snr` 一致），并消除 `frame_median`/`frame_mad` 恒等列或注明其恒等条件 |

### 1.5 `实验/absolute-snr/results/DOC_CORRECTIONS.md`

| 字段 | 值 |
|---|---|
| 路径 | `实验/absolute-snr/results/DOC_CORRECTIONS.md` |
| 标题 | SCI-B 对研究包/先行实验的补充与订正条目（DOC_CORRECTIONS） |
| 行数 | 106 |
| 角色 | 实际角色 = **过程台账**（"现象 → 证据 → 建议处理 → 闭环状态"四方台账，条目按 FIX/DOC 域分派）。标准 01 §2 的角色表里**没有这一角色**；§2 末条明文「历史档案、过程台账、已撤销设计不放在正式文档层；需要留证的台账落 `artifacts/evidence/` 并由正本引用」。⇒ **角色错放**，应迁往 `artifacts/evidence/SCI-B_DOC_CORRECTIONS.md` |
| 主题 | 对 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md` 与先行实验 EXP-205 的 7 条订正/确认（读噪双计、`c_est` 单位、恒真门、Δ/ℓ 边界、SWarp 引用、两条确认项） |
| 上游 | **缺**（无最高设计条款）。`:3` 的"归属声明：本文件属 SCI-B 实验单元"是**归属**不是**上游** |
| 下游 | 命令 `git grep -ln "DOC_CORRECTIONS"` → **11 个引用者**，其中**两份权威链文档反向引用**：`docs/science/NOISE_MODEL.md:324`「…把它当 dex 阈值用会高估噪声项 2.3026 倍（EXP-205 的 τ_A/τ_B 即此误，**见 `实验/absolute-snr/results/DOC_CORRECTIONS.md` D2**）」、`docs/science/CONTROL_WEIGHT_SNR.md:201`「**量化见 `实验/absolute-snr/results/DOC_CORRECTIONS.md` D1**」、`:202`「（`DOC_CORRECTIONS.md` D2）」。其余：`实验/absolute-snr/{README.md,REPORT_paper.md,results/REVIEW.md}`、`实验/photometric-magnitude/{README.md,REPORT_paper.md,code/step1_analytic.py,results/REVIEW.md,results/step1_analytic.json}`、本件自引。**非孤儿；反而是被科学正本当量化依据取数的下游件** |
| 是否正本 | **不是**（`:3` 自declared 对研究包"只读"、只提"建议处理"）。但它被 `docs/science/` 两份文件当作 D1/D2 的**量化出处** ⇒ 事实上承担了正本不写的量化职责。标准 01 §1「权威链只有一条，权威程度自上而下递减，每一层只由它的上一层推出」——**科学定义层向过程台账取数 = 层级倒置** |
| 重复或重叠对象 | **重叠对象 = `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.2a 与 `docs/science/NOISE_MODEL.md` §9a（同一 D1/D2 的落地）。举证：**<br>本件 `:25`「**建议处理（FIX 域，本单元不改生产码）**：在 `07_noise_snr.md` 明确 `sigma_sky_adu` 的口径（**天光+暗流散粒，不含读噪**），并在调用点做**显式契约检查**：当 `gain_e_per_adu>0 && read_noise_e>0` 而 `sigma_sky_adu` 来源为经验总 rms 时，要么扣除 RN 项，要么 fail-closed 拒绝（禁止静默双计）。补一条负例锁定：RN 主导点 σ_F 偏差 ≤5%。」<br>已落地正本 `07_noise_snr.md:110-126` §4.2a「**σ_sky 口径冻结（防读出噪声双计；SCI-B D1 定案）**…**入参语义枚举（二选一，必填）**…④ 声明与实际来源不一致 ⇒ **fail-closed 拒绝**，**择一依据 = 显式声明**，告警不构成放行…**负例保护**：旧组合方式（经验总 rms 再加 `(RN/g)²`）必须被测试判红」。<br>**差异**：本件的阈值 "RN 主导点 σ_F 偏差 ≤5%" **未被正本采纳**（正本给 +12.8%/+34.0% 实测与枚举语义，无 5% 门）。D2 侧同型重叠：本件 `:53`「建议：`NOISE_MODEL.md:86` **补一句单位声明**（"相对"）」‖ 正本 `NOISE_MODEL.md:100-101` 已有「⚠ 单位（**SCI-B D2 订正**）：1.44/√N 是 σ̂ 的**相对**标准误…**不是 dex**；若要以 dex 表述…≈ 0.625/√N（两者差 ln10 = 2.3026 倍，禁止混用）」⇒ **建议已执行、文件仍是建议态** |
| 元信息块 | **有**，且是全批最重：`:3-5` 三行块 = 归属声明 + 每条格式纪律 + 复现命令；`:9/36/59/76/85/94/103` 每条标题内嵌**状态与域标签**「【重要·**FIX 域**】」「【重要·**DOC/实验域**】」「【次要·DOC 域】」「【**确认·无需修改**】」。标准 01 §4「文档开头无元信息块（文档 ID、**状态编号**、版本锚、纪律声明等）」四类全命中 |
| 写法违规 | ① `:3` "`docs/research/SNR_WEIGHT_RESEARCH_PACK.md` 由 **DOC-404** 负责修订" — 任务编号 + 过程分工；② `:3` "本单元**只读**，故所有补充/订正写在本文件，供 DOC-404 与研究包维护者采纳" — 纪律声明；③ `:5` "复现：`bash 实验/absolute-snr/code/run_all.sh`；原始数据 `results/**b1..b6\*.json**`"（`run_all.sh` 已核 EXISTS；实际文件为 `b1_sky_scan.json`…`b7_absolute_snr_recon.json` ⇒ **`b1..b6` 漏 b7，且 `..`+`*` 通写在文档里不可解析**）；④ 7 处【…域】状态标签（同元信息块行）；⑤ `:27` "**闭环状态（RELEASE-05 / SCI-501 已闭环）**：生产码**已按 DOC-502 冻结口径修复**" — 三个编号 + 历史完成叙事；⑥ `:38` "`run/RELEASE-02/实验/E11-SNR三口径精度/code/snr3_common.py:741`" — `run/` 不入库的证据锚 + 任务号；⑦ `:51` "EXP-205 的精度门 τ_A 与偏差门 τ_B 的噪声项被放宽 2.3 倍" + `:55` "**EXP-205 属 `run/` 临时产物（不入库）**，其结论已在 SCI-B 内以正确口径**重做**" — 对一个不存在于仓库的先行件作结论史；⑧ `:67` "**双向可假已实测**"、`:70` "研究包与 `ACCEPTANCE_SPEC` 相关判据表**应把**…标注为恒真、不作证据" — 口语 + 对他文档的待办祈使写进证据层；⑨ 无 mermaid。<br>**机械列漏报**：CSV 本件 `history_narrative=[]`，实读命中"已闭环／重做／原…口径／订正登记／曾"≥5 处 |
| 悬空引用 | **X1** `:14` "设计意图：`lib/algorithms/noise_snr/cpp/include/.../**snr_science.h**`" → **不存在**。确认命令：`git ls-files lib/algorithms/noise_snr/cpp/include/` 只输出 `snr_estimator.h`；`test -f lib/algorithms/noise_snr/cpp/include/snr_science.h` → **MISSING**。路径里**写死了字面省略号 `.../`**，任何工具都无法解析；而该头文件是 D1 整条"设计意图"唯一被指的代码权威（真正的 `σ_i²` 组合式实际在 `snr_science.cpp` 与 `07_noise_snr.md §4.2a`）。**机械列已捕获此条**（`dangling_links=['…snr_science.h']`）——本批机械列唯一命中项，登记为**校准正例**。<br>**X2** `:38` 与 `:53` 引 `docs/science/NOISE_MODEL.md:86` 作 "SE(σ̂)/σ ≈ 1.44/√N_sky" 原文行 → **行号漂移**：该句在 `:99`（`git grep -n "1\.44" docs/science/NOISE_MODEL.md` → 99,100,101,324）；`:86` 实为标题 `### 5a 掩膜半径的物理导出（MASK-002）`。**同一错锚已被权威链继承**：`docs/science/CONTROL_WEIGHT_SNR.md:202` 也写 "`NOISE_MODEL.md:86` 的 `1.44/√N`" ⇒ 一处错锚、两份科学文档 + 一份台账共错（跨批，取证在本批）。**机械列漏报**（`dangling_links` 只记了 X1）。<br>**X3（悬空状态，非链接）** `:87-88` "SWarp `src/coadd.c`…**未在该文件中定位到所述行**（该镜像可能滞后/结构不同）" ↔ 重叠方 `SNR_WEIGHT_RESEARCH_PACK.md:198` 已判「SWarp `coadd.c:1279-1311` \| SWarp 2.41.5 \| **有效**：`COADD_WEIGHTED` 分支的逐像素逆方差组合循环」⇒ 本件的开放问题与上游现存事实矛盾。<br>**X4** `:89` "**Siril 1.2.4** `src/stacking/median_and_mean.c:**868-870**`" ↔ `SNR_WEIGHT_RESEARCH_PACK.md:171/197` 给 **Siril 1.4.4** `:1091-1094`（复验）与历史锚 `:1111-1230` ⇒ **同一引用三个版本、两套行号，两份文件互不知情** |
| 与上位冲突 | **S1 状态词跨轴串用**：`:27` D1 "已闭环"、`:55` D2 "已量化 + 登记"、`:72` D3 "已在 SCI-B 内替换"、D4/D5（`:81/:90`）**无闭环状态字段**、`:99` D6 与 `:106` D7 "确认·无需修改"——同文件三套状态词且**不区分 `未实现` / `未接线` / `已退役` / `文档没写`**。按现状：D1 已进 `NOISE_MODEL.md:324` + `07_noise_snr.md §4.2a`、D2 已进 `NOISE_MODEL.md:100-101`、D3 已进 `PSF_SIGNAL_WEIGHT.md:130`（非退化判据 E）、D4 已进 `07_noise_snr.md:100-106`（三条适用域）⇒ **四条建议全部已被上位吸收，本件仍以建议态存在**。违标准 01 §4「只写现行设计要怎样」+「已经撤销的设计不出现在正式文档里」。<br>**S2（本批三方对表必查）** `:97` 「`w_k = SNR_k(**F_ref**)² / **F_ref,k**² ≡ 1/σ_F,k²：1.1e-16」——**同一行里分子 `F_ref` 不带下标、分母 `F_ref,k` 带下标**。这不是排版问题：不带下标的 `F_ref` 恰是同批 `snr-propagation-design.md:85` 定义"**组内公共**参考通量"的那个量，与上位 `PSF_SIGNAL_WEIGHT.md:75-77`「`w_k = SNR_k² / F_ref,k²`…**逐帧**参考通量 `F_ref`」相反向。若分子按组内公共读，该恒等式**不成立**（差帧间比例因子 `a_k²`）<br>**S3** `:45` 「文档常数 1.44/√N（相对口径）0.04500（比实测**保守 24%**）」+ `:106` 「偏保守 24%（**实测 1.166/√N**）」↔ 正本 `NOISE_MODEL.md:99` 「c ≈ **1.152**/√N」而 `:301` 「`SE(σ̂)/σ ≈ **1.144**/√N_sky`」⇒ **正本内部两个系数（1.152 / 1.144）+ 本件第三个（1.166），三值互不相等且无人调和**（跨批缺陷，取证在本批件内完成） |
| 处置建议 | **删除**（条件式）或 **迁移** `artifacts/evidence/SCI-B_DOC_CORRECTIONS.md`。首选：把 D1→`NOISE_MODEL.md §9a`/`07_noise_snr.md §4.2a`、D2→`NOISE_MODEL.md:100-101`、D3→`PSF_SIGNAL_WEIGHT.md §7a.6`、D4→`07_noise_snr.md §4.2` 的吸收关系逐一在正本文内写明后**删除本件**；`NOISE_MODEL.md:324` 与 `CONTROL_WEIGHT_SNR.md:201-202` 的两处"见 DOC_CORRECTIONS D1/D2"改指各自正本身。理由条款：标准 01 §2「需要留证的台账落 `artifacts/evidence/` **并由正本引用**」——规范方向是"正本 → 台账"，现在是"**台账 → 正本供数**"，属 §1 权威链倒置；§5.6 判定链"走得通"要求沿途无悬空链接，X1/X2/X4 三条在正本侧留了未清的引用尾 |

### 1.6 `实验/absolute-snr/results/EXP02_TABLES.md`

| 字段 | 值 |
|---|---|
| 路径 | `实验/absolute-snr/results/EXP02_TABLES.md` |
| 标题 | EXP-02 产物表（自动生成，勿手改） |
| 行数 | 124 |
| 角色 | **研究与佐证**（EXP-02 结果表）。目录与 `AGENTS.md` §7 对 `实验/`（报告 / code / results / data）的定义一致 ⇒ **无角色错放** |
| 主题 | 天光噪声估计的"结构污染"缺陷三臂结果（解析 A1、HST 前向 B1、真实数据 C1），度量 `F0 prod`/`F1b fix`/`F1a mesh-median`/`A1`/`A2`/`D`/`kf`/`verdict` |
| 上游 | **缺**。`:1` 标题 + `:3` 生成命令，**无一行指回 `实验/absolute-snr/docs/EXP-02-STRUCTURE-CONTAMINATION.md`**。同批 `EXP03_TABLES.md:3` 至少写了输入 JSON ⇒ 本件是四份生成表里**唯一零来源指认**的一份（引用方向只有"正文 → 表"，表内无反向指针） |
| 下游 | 命令 `git grep -n "EXP02_TABLES"` → 2 个引用者：`实验/absolute-snr/code/exp02/make_tables.py`（生成者）、`实验/absolute-snr/docs/EXP-02-STRUCTURE-CONTAMINATION.md:227`「节选（box=32；完整 60 行见 `results/EXP02_TABLES.md` 表 A1）」。**`docs/` 与 `eng/` 0 引用**：`git grep -n "EXP02_TABLES" -- docs/ eng/` → 无输出 |
| 是否正本 | **不是**（GENERATED）。度量定义在 `EXP-02-STRUCTURE-CONTAMINATION.md:423-434`（已逐字核到：`:423` `kf` = 生产裁剪的保留比、`:424` `A1` = `σ̂_prod/(σ_diff/√2)`、`:433` `no_structure := (A1 <= 1+δ) and (R <= 1+δ) and (kf >= 0.95)`、`:434` `fix_certified := (A2 <= 1+δ) and (C <= δ) and (D <= 1.25) and (kf >= 0.80)`）⇒ 正文持规范、本件持数值，**合法下钻**（标准 01 §4「数值表…放对应下层文档」） |
| 重复或重叠对象 | **无同主题第二份正本。**与 `COMPARISON_TABLES.md` T1/T2 不重叠（本件量是 σ̂ 结构污染比，COMPARISON 量是 SNR）。**与 `EXP03_TABLES.md` 表 A1/A2 登记为"疑似重叠，待证"**：本件 `A1 = σ̂_prod/(σ_diff/√2)` 与 EXP03 表 A1 的 `1/sqrt(2N)` 列同属"跨帧差分作噪声真值"口径族，但一个是偏差比、一个是理论统计离散 ⇒ 举不出同口径两处段落，按派单 §5.5 不升为重复 |
| 元信息块 | **轻度**：`:1` 标题内"（自动生成，勿手改）" + `:3` 生成命令。可接受 |
| 写法违规 | ① `:3` "生成命令：`python3 code/exp02/make_tables.py`" — **仓库根 cwd 下不可执行**（真实路径 `实验/absolute-snr/code/exp02/make_tables.py`，已核 EXISTS）；同目录 `COMPARISON_TABLES.md:3` 写成仓库根相对路径 ⇒ 同批 4 件生成命令两种写法，**3 件不可跑**；② `:13` "## 表 A1"与 `:15` 列头 `… \| A1 \| A2 \| D \| kf \| verdict` **用同一串符号**（表号与度量名同名），`:107` 表 C1 又出现 `A2(mesh-med)`、`A2(MAD)` 变体 ⇒ 文内自指歧义（违标准 01 §4「自然语言把设计讲清楚」）；③ **10 个度量列零定义零指路**（定义在 EXP-02 正文 `:423-434`，本件不提）；④ `:109-124` 真实文件名含 `20251212`/`20251216`/`20251224`/`20251228`/`20260108` 日期戳 —— 数据命名事实，不算违规，但**机械列 `commit_sha` 把这 5 个 8 位十进制串全记成 commit**：属派单 §4.4 预告的"7–8 位词未必是 commit（高估）"，本例连十六进制形态都不是 ⇒ **建议机械列对纯十进制 `20YYMMDD` 形态另判为日期** |
| 悬空引用 | **Y1（本件主要缺陷）** `:5-11`「## 图」列出 5 个图件：`results/exp02_figs/a_analytic_scan.png`、`a_box_dependence.png`、`a_gate_calibration.png`、`b_hst_scan.png`、`c_real_frames.png` → **全部不存在**。确认命令：`ls 实验/absolute-snr/results/exp02_figs` → `No such file or directory`；`git ls-files 实验/absolute-snr/results/ | grep -c "\.png$"` → **0**（results 树内无任何 PNG）。⇒ 生成表声明了 5 个从未入库（或从未生成）的产物；标准 01 §3「登记了但文件不存在同样是缺陷」的同构形态。**机械列 `dangling_links=[]` 对本件漏报 5 条**。<br>**Y2** `:78` "表 B1 **HST M16** 真实模板 + 物理前向仿真" 的数据源 `testdata/HST_M16/` 在工作树**不存在**（`ls testdata` 无该项），仅由 `testdata/README.md:55` 登记为"只读、不入库、公开 HLSP 可重新下载" ⇒ 表 B1 的 22 行在本基线**不可复现**。`:105` 表 C1 的 `testdata/M42 T2 300s Red` 对应目录 `testdata/M42_T2T3_mosaic_Flying_dutchman` **存在** ⇒ C1 可复现、B1 不可，件内未区分 |
| 与上位冲突 | **T1 verdict 不连续**：`:19` `ramp` r=0.30（`A1 = 1.047`）判 `diagnostic_only`，`:29` `smooth_corr8` r=0.30（`A1 = 1.045`）判 `fail_closed` —— **`A1` 相差 0.002 而 verdict 不同档**，本件无解释列；正文判据式 `fix_certified := (A2 <= 1+δ) and (C <= δ) and (D <= 1.25) and (kf >= 0.80)` 有 4 个合取项，本件 C 列**根本不存在**（列只有 `A1/A2/D/kf`）⇒ 判据式的 `C` 项在数值表里无对应可核量，verdict 的成因不可从本件复原。<br>**T2 单 verdict 掩盖三列分歧**：`:26` `ramp r=1001.96` 行 `F0 prod=+1000.955` / `F1b fix=+0.0108` / `F1a mesh-median=-0.0101`，两修法臂均合格而 verdict 仍 `fail_closed`；`:76` `blob_sigma32 r=1025.49` 的 `F1a = -0.0047`（合格）而该行被 `D=214.77` 拖红 ⇒ 作为表合规、作为结论表与正文四判据不一一映射<br>**T3 上下游指针缺失（非数值冲突）**：上位 `07_noise_snr.md:195`「控制点的局部 σ **必须**用**结构感知**估计器：mesh 局部背景扣除后的逐区域残差稳健尺度，或**跨帧差分（唯一零结构偏差口径）**；…估计器认证、逐区域偏差与适用域见 `docs/science/CONTROL_WEIGHT_SNR.md §8b` 与 `实验/absolute-snr`（**EXP-03 的 R0/R1/R2**）」——本件的 `F1a mesh-median`/`F1b fix` 两臂正是该条款的两个候选，其数据支撑该条款，但上位**只指 EXP-03、不指 EXP-02** ⇒ 本件对该冻结条款无入边（下游断链，标准 01 §3 双向可追溯不成立） |
| 处置建议 | **保留**（角色/目录/分层正确），三件必修：① 开头补「上游：`实验/absolute-snr/docs/EXP-02-STRUCTURE-CONTAMINATION.md` §4（`A1/A2/D/kf` 定义与判据式）/§7（三臂表）」；② 生成命令改仓库根相对路径（与本目录 `COMPARISON_TABLES.md:3` 对齐）；③ `:5-11` 五个 PNG 路径要么补齐落盘、要么把"## 图"一节改为正向声明（"图产物不入库"）——留 5 条不可达路径不算完成 |

### 1.7 `实验/absolute-snr/results/EXP03_TABLES.md`

| 字段 | 值 |
|---|---|
| 路径 | `实验/absolute-snr/results/EXP03_TABLES.md` |
| 标题 | EXP-03 表格（自动生成，勿手改） |
| 行数 | 177 |
| 角色 | **研究与佐证**（EXP-03 结果表）。目录正确，**无角色错放** |
| 主题 | 区域化 σ_sky 的绝对准确性与跨帧一致性：解析臂 A1–A4、HST 前向臂 B1、真实数据臂 C1–C2、判据与故障注入 D1 |
| 上游 | **缺**（无最高设计条款、无 EXP-03 正文指针）。`:3`「源：`results/exp03_e1_analytic.json`、`exp03_e2_real.json`、`exp03_e3_hst.json`、`exp03_e4_gates.json`」**四条均已核存在** ⇒ 数据血缘清楚、**文档血缘为空** |
| 下游 | 命令 `git grep -n "EXP03_TABLES"` → 2 个引用者：`实验/absolute-snr/code/exp03/make_tables.py`、`实验/absolute-snr/docs/EXP-03-REGIONAL-SIGMA.md`。**`docs/`/`eng/` 0 引用**（`git grep -n "EXP03_TABLES" -- docs/ eng/` → 无输出） |
| 是否正本 | **不是**。度量定义在 `EXP-03-REGIONAL-SIGMA.md`（其 `:423` 有与本件 B1 逐字同构的表头，含"认证比"列）。本件是数值附表，合法分层 |
| 重复或重叠对象 | **重叠对象 = `EXP-03-REGIONAL-SIGMA.md:423`（正文侧的同名表）。举证：**<br>本件 `:108`「| 场景 \| box \| 真值 σ_bg \| 真值 σ_local \| σ_local 离散 \| 帧标量 p95 误差 \| R0 p95 \| R1 vs σ_bg 中位 \| R1 vs σ_local 中位 \| R1 p95 \| R2 vs σ_local 中位 \| R2 p95 \| **区域跨帧 p95/p05 \| A2_reg 中位 \| 认证比** \|」<br>正文 `EXP-03-REGIONAL-SIGMA.md:423`「| 场景 \| box \| 真值 σ_bg \| 真值 σ_local \| **帧标量 p95 误差** \| R0 p95 \| **R1 中位偏差** \| R1 p95 \| **R2 中位偏差** \| R2 p95 \| 区域跨帧 p95/p05 \| A2_reg 中位 \| 认证比 \|」<br>**差异** = 正文那份少 `σ_local 离散` 与 `R1/R2 vs σ_bg` 列（13 列 vs 15 列），是**节选还是全量无标记**，两份并存且无"完整表见 results/EXP03_TABLES.md"以外的同步声明。按标准 01 §4 属合法下钻，但**同名义的两张表列集不同**须注明哪张是权威数值面。<br>与同批 `COMPARISON_TABLES.md` T1/T2：都是"帧标量 vs 区域估计"双列结构，但本件量是 σ̂ 逐区域偏差 p95、COMPARISON 量是 SNR ⇒ **不同量，非重复** |
| 元信息块 | **轻度**：`:1` 标题状态声明 + `:3` 源清单（**源清单是本批 4 件里唯一写了输入的，属优点**）。无文档 ID/日期/裁决 |
| 写法违规 | ① **单位标注不一致**：`:16` 表 A2 标"真值 σ_n=20 **ADU**"、`:133` C1 标 `[ADU]`、`:153` C2 标 `[ADU]`，而 `:108` B1 的 `真值 σ_bg`/`真值 σ_local`（12.169 等）**无单位** ⇒ 同文件四种表三种标法；② `:76` 表 A4 标题「R0 **闭式偏差式对拍** σ̂/σ_n = sqrt(1+(sB)²/(12σ²))」——闭式**写在标题里，无编号、无出处、无适用条件**（谁推的、在什么假设下成立，本件不给）；③ 度量 `R0/R1/R2`、`认证比`、`A2_reg`、`Δ(… vs 帧标量)`、`1/sqrt(2N)` 在本件内**全部无定义无指路**；④ `:3` 用裸文件名（实际在 `实验/absolute-snr/results/` 下），与 1.6 ① 同类相对性问题；⑤ **机械列误报**：CSV 本件 `upstream_pointer=True`，实读本件除 `:3` 的 JSON 源清单外**无任何上游指针** ⇒ 检查器把"列了输入文件"当成"标了上游条款"。这是本批对工具的第二处校准（第一处是 `commit_sha` 高估）；`meta_block=False` 亦偏低——`:1` 的"（自动生成，勿手改）"是状态标注，只是不构成 §4 禁的 ID/版本锚块，此处**判机械列正确** |
| 悬空引用 | **Z1** 本件**无悬空文件引用**：`:3` 四个 JSON 全部存在（`git ls-files` 已逐一确认），正文无 markdown 链接。<br>**Z2（证据不可复现）** `:106` 表 B1「**HST 前向臂** · 绝对准确性（有真值）与跨帧一致性」的 4 个场景 × 5 个 box = **20 行**依赖 `testdata/HST_M16/*.fits`，该目录在工作树不存在（`ls testdata` 无 HST_M16；`testdata/README.md:19` 记 `HST_M16/*.fits`（3 × 268,917,120 B = 770 MB）"❌ 不入库｜体积超限，公开 HLSP 可重新下载"）⇒ B1 全部数值在本冻结基线上**不可复算**<br>**Z3** D1 表阈值（`budget=0.0240`、`budget=0.0200`、`budget_min=3.0000`、`n=12.0000`）在件内**无任何出处**，未标门定义所在节/JSON 键 ⇒ "常数须有推导"不满足 |
| 与上位冲突 | **W1 三条门行"关键量"列为空（本件最高价值缺陷）**：`:170` `analytic.gate_not_degenerate \| PASS \| (空)`、`:171` `hst.R2_beats_R1_on_structure \| PASS \| (空)`、`:173` `inj:identity_diff \| PASS \| (空)` —— 三行**零数值**，从本件无法判定该门是否被执行、是否非退化；同表 `:166-168` 与 `:172` 都给了 `delta_mean/delta_3sigma/worst_abs_rel_err/budget/max_abs_region_excess_median` ⇒ **同一张 D1 表里一半门行可核、一半不可核，且都计 PASS**。这正是 `AGENTS.md` §9 点名的"空断言、SKIP 充数…都算未完成"。<br>**W2 帧内离散与标量降级门未接**：`:155-160` C2 表 `帧内区域离散` 列 M1=1.260 / M3=1.343 / M4=1.331 / M6=1.473，而 **M2=7.763、M5=9.915**（约 6 倍跳变），件内未标异常。上位 `07_noise_snr.md:131`「标量降级门：仅当帧内 `W_psf(x,y)` 鲁棒相对离散与系统趋势**低于阈值**才存帧级标量；否则存 map/控制点/多项式/HEALPix」与 `PSF_SIGNAL_WEIGHT.md:107`「`W_info(x,y)` 默认是空间量；**只有通过均匀性/信息损失门才压为帧标量**」⇒ M2/M5 两面板按该门必然不过，本件与 EXP-03 正文都没把这一列与该门对上：判据存在、数据在手、**结论未接**<br>**W3 同面板两表读数方向相反**：`:136-141` C1 的 M2 面板 6 个帧对"单帧功率@256"达 **+528.57%**、"全局口径（诊断）" +25~28%，"逐区域 excess 中位" −1.6%~−6.7%；而 `:156` C2 的 M2"同区域跨帧 p95/p05 中位"= **1.063**、`:160` M6 = 1.037 ⇒ 对"M2 帧间是否同质"两表给出相反读数，件内无调和段 |
| 处置建议 | **保留**（角色/目录/分层正确），三件必修：① 开头补「上游：`实验/absolute-snr/docs/EXP-03-REGIONAL-SIGMA.md` §表号」并把 `:3` 的 JSON 写成仓库根相对路径；② 给 `R0/R1/R2/认证比/A2_reg/Δ(…)` 一行式定义或指路（标准 01 §4）；③ 补齐 W1 三条空"关键量"，或把该行显式改标"本件不落数值，见 `results/exp03_e4_gates.json` 键 …"——**空行不得计为 PASS 证据** |

### 1.8 `实验/absolute-snr/results/EXP04_TABLES.md`

| 字段 | 值 |
|---|---|
| 路径 | `实验/absolute-snr/results/EXP04_TABLES.md` |
| 标题 | EXP-04 汇总表（由 code/exp04/make_tables.py 生成） |
| 行数 | 322 |
| 角色 | **研究与佐证**（EXP-03 结果表；GENERATED）。目录本身正确 —— **角色错不在本件，在上位把 GENERATED 件指成"数字正本"**（见"是否正本"） |
| 主题 | 稀疏→稠密 SNR 重建算子选型、三口径适用域、失效边界 Δ\*、时间/存储代价、边界行为（NaN 传播/阶跃超调/图像边界）、跨帧一致性、负例红绿、B3 复核 |
| 上游 | **缺**。`:1` 只给生成脚本（且 `code/exp04/make_tables.py` 是相对 `实验/absolute-snr/` 的路径，仓库根 cwd 不可跑）。**无 EXP-04 正文指针、无最高设计条款**——而同批 `EXP-04-RECONSTRUCTION.md:5-7` 有完整权威链（`AGENTS.md §5 → ASTROCS_DESIGN.md §5.3 → docs/design/UNIFIED_MODEL.md:46 → 07_noise_snr.md §4.2/§4.5 → 13_integration.md:19-41`，并写"判据正本：`docs/science/CONTROL_WEIGHT_SNR.md §8b`"）⇒ **正文有链、附表无链**；当上位单独指附表为"数字正本"时，链断在本件 |
| 下游 | 命令 `git grep -n "EXP04_TABLES"` → 3 个引用者：`code/exp04/make_tables.py`、`code/exp04/run_all.sh`、`docs/EXP-04-RECONSTRUCTION.md`。**`docs/`/`eng/` 0 引用**（`git grep -n "EXP04_TABLES" -- docs/ eng/` → 无输出）。**但** `docs/science/CONTROL_WEIGHT_SNR.md:215` 与 `docs/plugins/algorithms_phase1/07_noise_snr.md:176` 按"**EXP-04 §5 / §3.2/§4.4/§5.3**"指过来 —— **指章节不指本件**，本件与那三节的关系只靠文件名相似维系的 ⇒ 被上位指为"数字正本"的对象在文档图里**既无入边亦无出边** |
| 是否正本 | **本件自己不是、却被上位当正本指**（本批最高价值结构性发现）。两处指法：`07_noise_snr.md:176`「取值依据、失效边界与完整适用域**正本**见 `实验/absolute-snr`（EXP-04 §5）」；`CONTROL_WEIGHT_SNR.md:215`「算子定义与选择规则见 `07_noise_snr.md §4.5`，**判据与数字正本见 `实验/absolute-snr` EXP-04 §3.2/§4.4/§5.3**」。而 `EXP-04-RECONSTRUCTION.md:7` 反向写「**判据正本：`docs/science/CONTROL_WEIGHT_SNR.md §8b`**」⇒ **`docs/science/` ↔ `实验/absolute-snr/` 互为对方的"判据正本" = 循环正本指认**，违反标准 01 §1「权威链只有一条，权威程度自上而下递减」与 §5.6-01「同一主题只有一份正本」。本件是该循环的**数值落点** |
| 重复或重叠对象 | **三组，均贴两段实证：**<br>**（一）Δ\* 一个符号两种定义、两个值。** 本件 `:172-178`「## T3 失效边界 **Δ\*（sparse 双线性 RMSE 首次超过帧级臂）** ／ HST M16（2048 crop）：**Δ\* = 16（crossed）**」+ 表首行 `\| 16 \| 0.0543 \| 0.0538 \| True \|`（Δ=16 即已劣于帧级）‖ 上位 `07_noise_snr.md:102`「定义 Δ\* = 「该算子 **E** 首次劣于帧级臂的最小 Δ」：`bilinear_regular_grid_v1` **Δ\*=32 px**」‖ ⇒ 同符号、不同判据量（RMSE 判 vs E 判）、**结果 16 vs 32 差一档**；本件 `:174` 未声明"本文以 RMSE 计"以区别于上位。<br>**（二）上位承重数字不在本件。** `07_noise_snr.md:102`「默认档差 **2.6 倍**（**0.1375** vs 0.0530），只有 mesh 档（**0.0490**）胜出」与 `:191`「默认路径单独使用 E=0.1375…叠加滤波后改善到 0.0490」‖ 本件 `:10` `spline_natural` 在 `hst_m16` 列 = **0.1549 / 0.1291**、`:11` `sextractor_spline` = **0.0492 / 0.0581**；`grep -n "0\.1375\|0\.0490" 实验/absolute-snr/results/EXP04_TABLES.md` 只命中 `:19` `gpr_rbf` 的 **RMSE** 0.1375 与 `:10` `spline_natural` 在 `unres…au0.10` 列的 RMSE 0.0490 —— **同数字、不同量、不同面**。这两个 E 值的真实出处是 `EXP-04-RECONSTRUCTION.md:348-349`（「HST M16 \| `spline_natural` \| 0.1549 \| **0.1375** \| ×0.887」／「`sextractor_spline` \| 0.0492 \| **0.0490** \| ×0.998」）⇒ **被指为数字正本的本件里查不到那两个数；数在正文的一张对照表里。**<br>**（三）与 `COMPARISON_TABLES.md` T4/T5 重叠**：见 1.4「重复或重叠对象」行 —— 存储代价两套数**差恰好 2×**（本件 `:231` sparse Δ=64 = 32,768 B/0.0312 MiB vs COMPARISON `:111` = 16,384 B/0.01562 MiB），两件互不引用 |
| 元信息块 | **轻度**：`:1` "（由 code/exp04/make_tables.py 生成）"。**优点**：`:3` 表标题内嵌口径声明"estimated 模式：控制值来自数据稳健 MAD"，是本批唯一在表头写明生成模式的件 |
| 写法违规 | ① `:312` **表格单元格被生成器截断**：`G4_tautology_recheck \| 登记 \| '帧级臂 RMSE <= K*s_field' 在对抗洗牌场（E 高达 0.43）下仍恒真 ⇒ 该判据无证据资格（复核 §8b b6_gates_audit.j` ← 句尾在 `.j` 断掉、括号未闭合。同一 G4 的 E 在本件是 **0.43**、在 `COMPARISON_TABLES.md:158` 是 **7.17**、在 `DOC_CORRECTIONS.md:65` 是 **7.17** ⇒ 生成器列宽截断 + 跨件数字不同源；② `:314` `G6_frame_arm_selfzero \| True \| (说明列空)`；③ `:294` T6 `frame_median_scalar \| 0.0009 \| — \| — \| —` 四格三空，无"不适用"声明；④ `:305` T7 标题「负例**红/绿**」而 `:309-314` 结果列实际取值只有 `True` 与 `登记` 两种，把"登记、不作证据"与"绿"并列同列 ⇒ **状态词跨轴串用**；⑤ `:321` 「恒真判据：`{"1": false, "5": true, "10": true, "100": true}`」—— 一行**裸 JSON**，键 `1/5/10/100`（应为 K 值）无定义无单位；⑥ `:3/:5` 表头「arm（**E / RMSE dex**）」把无量纲 E 与 dex 域 RMSE 并置一格、以 `/` 分隔，全文**未说明斜杠语义**；⑦ `:24-25` 的 `-0.0000` signed-zero 出现在按定义应 ≥0 的 E 列，件内未声明该量非负及下溢约定（标准 01 §4「自然语言把设计讲清楚」）；⑧ 生成命令路径不可跑（同 1.6 ①） |
| 悬空引用 | **V1（本件最高价值：与本批 1.1/1.4 同源）** 本件出现 **0 次**上位冻结的四个算子标识。确认命令：`grep -c "mesh_median\|_clip_v1\|bilinear_regular_grid\|nearest_control_point\|natural_bicubic" 实验/absolute-snr/results/EXP04_TABLES.md` → **`0`**。T1 的 20 个臂名（`nn, bilinear, bilinear_prod, bicubic_cc, spline_natural, sextractor_spline, photutils_zoom3, photutils_zoom1, idw, tps, tps_smooth, rbf_mq, rbf_gauss, gpr_rbf, gpr_matern32, gpr_exp, dense_P32, dense_at_D, frame_median, frame_global_mad`）与 `07_noise_snr.md:182-188` 的**冻结词表**（`natural_bicubic_spline_clip_v1`／`natural_bicubic_spline_clip_mesh_median_v1`／`bilinear_regular_grid_v1`／`nearest_control_point_v1`）**零交集**（`bilinear`≈`bilinear_regular_grid_v1`、`nn`≈`nearest_control_point_v1` 是近似而非同名，且无映射表）。⇒ 被上位指为"数字正本"的件**不含上位词表任何标识**；上位据以冻结"mesh 滤波是必需项"的 `..._mesh_median_v1`（E=0.0490）**在本件 28 个面里没有对应臂行**。<br>**V2（复核自身取错行）** `:318` 「HST Δ=**16** sparse 水平偏差 = **0.0194** dex（B3 报 +**0.0297**）」→ 本件 `:195-196` 自己给 Δ=16 `sparse_bias = 0.0194`、Δ=**32** `sparse_bias = 0.0297` ⇒ **B3 的 +0.0297 属 Δ=32 行，本件按 Δ=16 去比**；而 `:199` Δ=256 `sparse_bias = 0.2626` 与 `:319` 的"B3 报 +0.2626"**逐位吻合** ⇒ 恰证 B3 未错、错在对照取行。**本件 T8 第一条"复核结论"失效**（可复算缺陷，非链接悬空）。<br>**V3** `:174` 「HST M16（2048 crop）」与 T1/T2 的 `hst_m16` 列依赖 `testdata/HST_M16/`（工作树不存在，仅 `testdata/README.md:55` 登记）⇒ 本件 T1/T2/T3/T8 的 hst_m16 各列**不可本地复现**。<br>**V4** `:186-190` 纯合成面 Δ\* 全为 `None（no_crossing）`、`:310/:312` 的 `登记` 均无门 ID → `results/b6_gates_audit.json` 键的映射 ⇒ 门与证据之间无可解析指针。**机械列 `dangling_links=[]` 对本件漏报 V1/V4（两者都不是链接语法而是"标识符集不匹配"与"键映射缺失"）** |
| 与上位冲突 | **U1 `best_kind` 列 13 行自相矛盾（机器可核）**。命令（`-F'|'` 分列后第 4 列 = `best_arm`、第 8 列 = `best_kind`）：`awk -F'\|' '/^\|/ {gsub(/ /,"",$4); gsub(/ /,"",$8); if ($4 ~ /^frame_/ && $8=="sparse") print NR}' 实验/absolute-snr/results/EXP04_TABLES.md` → 命中 13 行：`:31,35,36,37,38,40,48,51,52,53,54,55,154`，其 `best_arm` 是**帧级臂 `frame_global_mad`** 而 `best_kind` 写 `sparse`。上位 `07_noise_snr.md:102` 与 `CONTROL_WEIGHT_SNR.md:215` 的"结论与算子绑定"判读正依赖 `best_kind` 语义 ⇒ **该列不可用**。<br>**U2 `gpr_rbf` 失控值未被 fail-closed**：`:19` `gpr_rbf` 在 `grf_ell32.0_s0.300` 面 = **32396.3608**（同列其余臂 ≤ 5.65，跨 **4 个数量级**），本件按普通单元格排印；`EXP-04-RECONSTRUCTION.md:546` 已承认"无 nugget 的精确插值在 HST 上失控：E=1.53（Δ=64）、1.8e5（Δ=128）、7.8e5（Δ=256）"⇒ 该臂已知发散，汇总表无任何异常标记（按 `AGENTS.md` §9「对'看起来通过'的结果保持怀疑」，应带标记或 fail-closed）。<br>**U3 `frame_median` 与 `frame_global_mad` 两臂逐格恒等**。命令：`paste <(awk 'NR==24' …|tr '\|' '\n') <(awk 'NR==25' …|tr '\|' '\n')` 逐格比对 28 列 → **仅 3 格差在 signed-zero 形态**（`-0.0000` vs `0.0000`），其余 **25 格逐位相同** ⇒ 两个"帧级臂"在全部 28 面上给出同一结果，件内未声明恒等条件；与 `COMPARISON_TABLES.md` T4 的 `frame_median`/`frame_mad` 列 **24/24 行完全相同**（`awk -F'\|'` 比对，差异行数 = 0）**同型 ⇒ 同一缺陷在两份生成表里都在且都无解释**。这直接削弱上位 `snr-propagation-design.md:44-45`（"生产实际使用的帧标量是整帧未裁剪 MAD，**不是**理想标量：散差 1.058× 时惩罚 0.126%…"）把 median/MAD 当作关键分歧点的可判定性——**两张表都说两者无差别，没有一处说明为什么。**<br>**U4（上位状态未回写）** 本件 T1/T2 的臂集与 `07_noise_snr.md §4.5` 冻结词表不匹配（V1）同时，`:102`/`:191` 的 E=0.1375/0.0490 两条"实测"结论的**臂名在冻结词表内、数值不在本件内**（重复对象组二）⇒ 上位同时犯了"指一个不含其所引数值的正本"与"引一个不含其臂名的实验集"两件事 |
| 处置建议 | **保留 + 强制补映射表**，不迁移。本件是合法的下层数值表，缺陷全在上位指法与生成器。三件必修（按优先级）：① **消除循环正本**——`CONTROL_WEIGHT_SNR.md:215` 与 `07_noise_snr.md:176` 的"数字正本见 实验/absolute-snr EXP-04"改为「数值证据见 `实验/absolute-snr/results/EXP04_TABLES.md`（GENERATED 佐证，**不作规范**）；规范判据与算子词表的正本 = 本文件 §4.5」。理由条款：标准 01 §1「权威链只有一条」+ §2「同一角色内同一主题只有一份正本」；② 在本件 T1 前插入"冻结算子 ↔ 实验臂"映射表（至少覆盖 `natural_bicubic_spline_clip_v1` 与 `..._mesh_median_v1` 两行的对应臂与其 E/RMSE），否则把 `07_noise_snr.md:102` 的 0.1375 / 0.0490 / Δ\*=32 三处改由 `EXP-04-RECONSTRUCTION.md:348` 那张对照表指认；③ 修生成器三处机器缺陷：`:312` 截断、`best_kind` 13 行误标、`:24-25` 恒等臂未声明，并把 T7 的 `True` 与 `登记` 拆成两列 |

---

## 2 SNR 定义与权重换算口径主张原句清单（供前台三方对表）

按派单要求，逐条摘出本批 8 份中关于 **SNR 定义**与**权重换算**的口径主张**原句**，给 `路径:行`。本批 8 份全不属权威链，故末附"参照正本同题句"，否则对表无对照面。

### 2.1 `实验/absolute-snr/docs/snr-propagation-design.md`

| 行 | 原句（逐字摘录） | 对表要点 |
|---|---|---|
| `:20` | `SNR_k(F_ref) = F_ref / σ_F,k`，`σ_F,k⁻² = P_kᵀ C_k⁻¹ P_k` | 缺 `a_k²` 因子（正本 `07_noise_snr.md:33` 为 `W_psf,k = a_k² P_kᵀC_k⁻¹P_k`）；`F_ref` 无下标 |
| `:23` | `Var_approx/Var_opt = 1 + Var_p(δ) + O(δ³)`，`δ_f = ŵ_f/w_f − 1` | 本文 `:673` 自证精确式是 `1 + Var_p(δ)/(1+δ̄)²` |
| `:85` | `F_ref` \| **组内公共**参考通量 \| ADU | ⚠ 与正本"逐帧"**相反** |
| `:87` | `W_info,k = a_k² P_kᵀ C_k⁻¹ P_k = 1/σ_F,k²` \| 点源信息权重 \| **ADU⁻²** | 符号采 `W_info`（= `PSF_SIGNAL_WEIGHT.md:23`），与插件层 `W_psf` 不同名（组十） |
| `:88` | `w_k(p)` \| 叠加权重 \| **ADU⁻²** | 与 `weight_chain.h:32` 单位一致 ✓ |
| `:117` | `ivar_mosaic = Σ w_k ,  variance = 1/Σ w ,  SNR_mosaic = √(Σ w)·F̂` | ⚠ 成品 SNR（上位 `07_noise_snr.md:7/:224` 判其不存在） |
| `:287-288` | `σ_F⁻² = Pᵀ C⁻¹ P ,  C = diag(σ²_pix)`；`SNR_k(F_ref) = F_ref / σ_F,k` | 同 `:20` |
| `:311` | 逆方差换算 `w = SNR²/F_ref²` \| **严格成立** \| **不成立**（功率比量纲） | — |
| `:319` | **`F_ref` 必须是组内公共常数**，否则 `w = SNR²/F_ref² = a_f²/σ_f²` 的配对定理失效（引 `weight_chain.h:34-41`） | ⚠⚠ **P0**：所引文件 `:42-47` 正是作废该句之处 |
| `:336-338` | `SNR_k(F_ref) = F_ref · sqrt( W_info,k ) = F_ref / σ_F,k`；`W_info,k = a_k² P_kᵀ C_k⁻¹ P_k = 1/Var(F̂_k)`；白噪声 `W_info,k = a_k² Σ_p P_k,p² / σ²_pix,k = a_k² /(σ²_pix,k·A_NEA,k)` | 与 `07_noise_snr.md:33-34` 同形、异符号 |
| `:343` | ②Phase2 权重链的回退路径 `w_k = SNR_k²/F_ref²`；③深度指标 `m5 = ZP − 2.5 log10(5 σ_F(ref))` | `m5` 式与 `07_noise_snr.md:23` 一致 ✓ |
| `:351-352` | 实际 SNR(x,y) = **帧级 SNR_k × 帧内相对因子 intra_k(x,y)**；`intra_k(x,y) = sqrt( W_info,k(x,y) / ⟨W_info,k⟩ )` | ⚠⚠ 已被裁决取消；正文仍以现行式 (2.5) 书写 |
| `:355` | 控制点值是 **SNR**，不是权重——`w(p) = SNR(p)²/F_ref²`（差一个平方就会系统性偏权） | `:513` 又称 `rho_c` 键名"保留为历史记录" |
| `:494-496` | 控制点 c 的值（三选一，必须显式声明用的是哪一个）：(a) `SNR_c = F_ref/σ_F,c` **(b) 相对帧级因子（推荐）** `rho_c = SNR_c/SNR_frame = sqrt(W_info,c/⟨W_info⟩_frame)`，`p50(rho)=1` (c) 局部天光 SNR | ⚠⚠ "推荐 (b)" 即合同判红的相对语义 |
| `:516` | 再按 `W_info = a² ΣP² / σ²_pix` 换算为 SNR | — |
| `:663` | 效率惩罚: `Var_approx/Var_opt = 1 + Var_p(δ) + O(δ³)` (3.1) | 同 `:23` |
| `:670` | 判据 SP-0（可算形式）: 启用稀疏层 ⟺ `Var_p(δ_空间省略) > Var_p(δ_重建误差)` (3.3) | ⚠ `:432` 已改判"SP-0 不是开关，是诊断量"，式 (3.3) 未撤 |
| `:767` | `w_ki = 1/σ²_ki ∝ SNR_ki²` (4.1) | ⚠ `1/σ²` 与 `∝SNR²` 仅同帧成立（`07_noise_snr.md:74` 明写） |
| `:850` | `Var(corrected_k(p)) = [ σ²_y,k(p) + J_out(p) C_θ J_out(p)ᵀ ] / g_k²` (4.3) | 出处 `docs/science/q2-snr-smooth` **从未存在**（H2） |
| `:877-879` | `Var(corrected_i) = Σ_j P_ij² σ_j² + [参数项]`；`P = I − H`，`H_kj = w_j / W_{−k}` | ⚠ `:897-907` 自证所引 MC 是**不同算子** |
| `:923-924` | 链 A（帧级，现役）: `w_k = SNR_k(F_ref)² / F_ref² = a_k²/σ_F,k² = 1/σ_F,k²`；链 B（逐像素，目标）: `w_k(p) = 1/Var(corrected_k(p))` | ⚠ "链 A/链 B" 命名权威链内不存在；`PSF_SIGNAL_WEIGHT.md:80` "**没有可选择的口径**" |
| `:927` | `σ_F,k² = σ²_pix,k · A_NEA,k / a_k² ⇒ w_k = a_k²/(σ²_pix,k A_NEA,k)` | — |
| `:978-982` | `ivar_mosaic(p) = W(p) = Σ_k w_k(p)`，`variance_mosaic = 1/W` (5.1)；一般式 `variance = Σ w_k² v_k / W²` (5.2)；`SNR_mosaic(p) = signal(p)/sqrt(variance_mosaic(p))` (5.3) | ⚠ (5.3) 即成品 SNR 定义式 |
| `:1021` | `eps_tot² = eps_P1² + eps_sparse² + eps_theta² + eps_g² + eps_drizzle² + eps_master²` (5.4) | 本文 `:1050` 自证 drizzle 项从未进入求和 |
| `:1521` | 进叠加权重时 `w(p) = SNR(p)²/F_ref² = w_frame · rho(p)²`（§4.7 链 A/链 B） | ⚠ 全文最末一行仍把 `rho` 当现行量 |

### 2.2 `实验/absolute-snr/docs/surveys/f-instr-survey.md`

| 行 | 原句 | 对表要点 |
|---|---|---|
| `:45-46` | `${\rm SNR} = F_{\rm ap}/\sqrt{F_{\rm ap}/g + n_{\rm pix}\sigma_{\rm sky}^2(1+n_{\rm pix}/n_{\rm sky})}` | Howell 1989 孔径 SNR；含 `n_pix/n_sky` 背景项——本仓 `σ_F` 式无该项（对照 `frame-snr-survey.md:263`） |
| `:55` | `F = Σ p_i (d_i−b)/σ_i² / Σ p_i²/σ_i²`，`${\rm Var}(F)=1/\sum_i p_i^2/\sigma_i^2` | Naylor 1998；无 `a_k` |
| `:63` | `F = Σ p_i(d_i−b)/σ_i² \big/ Σ p_i²/σ_i²` | Horne 1986 |
| `:465` | `Var(F) = 1 / sum_i (P_i^2/sigma_i^2) ; SNR_F = F / sqrt(Var(F))`（`snr_science.cpp:110-118` 逐字） | 与 `PSF_SIGNAL_WEIGHT.md:125` 的 `σ_F⁻²=ΣP_i²/σ_i²` 一致 ✓ |
| `:106` | Moffat 总通量 `= πα²A/(β−1)` | 与 `:448/:450` 的本仓 β=4 式 `2πA·sxsy/3` 同族（β=4 ⇒ `πα²A/3`） |
| `:448/:450` | `flux` \| 解析通量 `2πA·sxsy/3` (β=4)；单位 **ADU**（`I,B` 为 ADU/pixel…β=4 整平面延伸假设，Project-defined 推导） | 逐字复制 `docs/science/PSF.md`（重叠对象行） |

### 2.3 `实验/absolute-snr/docs/surveys/frame-snr-survey.md`

| 行 | 原句 | 对表要点 |
|---|---|---|
| `:56-58` | `SNR = C / sqrt( C/g + (B/g + sigma_instr^2) * n_eff )`；`n_eff = 2.266 * (FWHM_eff / pixelScale)^2`；`sigma_instr^2 = (readNoise^2 + darkCurrent*expTime) * n_exp` | 唯一一手帧级公式；`n_eff` vs 本仓 `A_NEA`（`:72` 明写不套该系数） |
| `:85` | `instFlux = Σ image·w`，**`instFluxErr = sqrt(Σ variance·w²)`** | LSST；`w²` 形式 |
| `:144` | 内部矩：`SN = Sum/sqrt(Var)` | PS1 |
| `:155` | ① `Var = Σ w σ²`（权重不平方）**数学上不是加权和的方差**，会系统性高估 SNR（严格应为 `Σ w_i²σ_i²`） | 与 `:85` 的 `w²` 互为"正确写法"锚 |
| `:256-258` | `flux = sum - area*msky`；`err = sqrt( flux/epadu + area*stdev**2 + area**2 * stdev**2 / nsky )`；`SNR = flux / err` | DAOPHOT；**含 `nsky` 项**（`:263`"本调研最该抄的一条"），本仓 `σ_F` 式无该项 ⇒ 若本仓主张 σ_F 完备，此差异须显式声明 |
| `:308` | `aperture_sum_err = sqrt(Σ_{i∈A} w_i² σ_tot,i²)` | photutils |
| `:327` | `sumerr = sqrt(Σ_pixel var_i + (sum/gain 若 gain>0))` | sep |
| `:347` | `σ_F^-2 = Σ_i P_i²/σ_i²`，`SNR_F = F/σ_F` | Horne 1986（本仓规范式）✓ |
| `:390` | 也是"逆方差换算 `w = SNR²/F_ref²` **只在 PSF 齐化后严格成立**"的论据 | ⚠ 该条件（PSF 均质化）在 `07_noise_snr.md §4.1` 与 `PSF_SIGNAL_WEIGHT.md §4` 中**均无对应条款**；本仓走 `Zackay & Ofek` 的"各自 PSF matched filter"路线（`SNR_WEIGHT_RESEARCH_PACK.md:158`），二者不是同一条件 |
| `:398-399` | `σ_F^-2 = ΣP_i²/σ² ⇒ σ_F = σ/√(ΣP_i²) = σ·√A_NEA` | `A_NEA` 一手出处未定位（`:397`），故只能按"由 Horne 直接推出"引用（`:401`） |
| `:418-419` | 未扣背景的通量型 `(F + n_pix·B)/σ`；`B→∞` 时 `SNR → √(n_pix·B) → ∞`。**SNR 随天光上升而上升** | 红线负例；对照本仓红线 `07_noise_snr.md:48`"固定源通量、增大天光 ⇒ SNR **单调下降**（`B→∞` 时 `SNR→0`）" ✓ 同向 |
| `:429` | 功率比型 `SNR² = σ²/σ_n²`（全局尺度估计），**分子是图像方差** | 与 `07_noise_snr.md:59`"标准 SNR…不采用"一致 ✓ |

### 2.4 `实验/absolute-snr/results/COMPARISON_TABLES.md`

| 行 | 原句 | 对表要点 |
|---|---|---|
| `:7` | 表头 `\| B [e-/px] \| SNR_def \| SNR_MC \| prod skyonly+RN \| prod empirical+RN \| prod empirical only \| 传统口径 \| 不扣背景 \| 算术常数 \|` | 8 个 SNR 口径名**件内无定义**；`skyonly+RN`/`emp+RN` 对应 `shot_noise_only`/`empirical_total_rms`（`07_noise_snr.md:113-116`） |
| `:43` | 表头 `\| 扫描 \| 轴值 \| sigma_F 定义 \| sigma_F MC \| z_def \| z(skyonly+RN) \| z(emp+RN) \| 双计预言 \|` | 同上 |
| `:75` | ## T4 三口径适用域（**RMSE(log10 rho)**，Delta=64；真实数据为 16 px hold-out 值） | ⚠ `rho` = 相对因子符号，合同判红（P1） |
| `:121-126` | `\| ivar 组合方差 \| 1213.08 \| 1207.06 \| +0.50% \|`…`\| SigmaSNR^2 恒等 \| — \| — \| 2.2e-16 \|` | 后者即 `SNR_combined² = Σ SNR_k²`（`DOC_CORRECTIONS.md:96` 同数 ✓） |
| `:123` | `\| w∝SNR 组合方差 \| 1732.58 \| 1712.57 \| +1.17% \|` | 与 `:121` 逆方差臂并列 ⇒ 本表把 `w ∝ SNR`（错口径）作**对照臂**，件内未标"此为负例臂" |
| `:129-137` | ### T6a **F_ref 锚定适用域**（锚定权重 scatter / oracle 权重 scatter）：m=4.0→0.9999；**6.0→1.0000**；8.0→0.9998；10.0→1.0059；**12.0→1.0362** | ⚠⚠ `PSF_SIGNAL_WEIGHT.md:127` 写"…6 等（**10 倍通量**）处 **3.6%**" ⇒ 3.6% 属 **m=12.0** 行（`results/b4_integration.json:421-422` 给 `mag:12.0, Phi:0.00398`，比 `m_ref=6` **暗 251 倍**、不是 10 倍）；而 m=6.0 行比值恰 **1.0000**（零惩罚）。**正本的星等↔通量映射与本表矛盾** |
| `:139-147` | ### T6b **F_ref 错配负例**（ZP 散度 ⇒ 权重效率损失）：0.0→-0.0000；0.1→0.0056；0.3→0.0474；0.6→0.1576；1.0→**0.3018** | 与 `PSF_SIGNAL_WEIGHT.md:127`"效率损失随 ZP 散度 1.0 mag 达 30.1%（散度=0 时严格归零）"**一致** ✓ |
| `:158` | `\| 恒真门（三种真值场，含对抗打乱） \| 全绿 \| 无证据资格（对抗场 **E=7.17**）\|` | ⚠ 同一 G4 在 `EXP04_TABLES.md:312` 写 **E=0.43**、`DOC_CORRECTIONS.md:65` 写 **E=7.17** ⇒ 三处两值，件内无调和 |

### 2.5 `实验/absolute-snr/results/DOC_CORRECTIONS.md`

| 行 | 原句 | 对表要点 |
|---|---|---|
| `:14` | 规定 `σ_i² = σ_sky,散粒² + (RN/g)² + F·P_i/g`（**读噪只出现一次**） | 与 `07_noise_snr.md:110`、`NOISE_MODEL.md:324` 一致 ✓；但**出处头文件不存在**（X1） |
| `:22` | PSF 行路径 `snr_estimator.cpp:83`（`sigma_sky_adu = residual_scale/0.7316727929211932`，gain 未知 ⇒ 不加 RN 项） | 常数与 `NOISE_MODEL.md:312` 的 `q_psf` 一致 ✓ |
| `:67` | 替代判据：权重效率损失 `E = Var_w/Var_opt − 1`（全局尺度相消；**E=0 ⇔ σ̂∝σ_true**） | 与 `PSF_SIGNAL_WEIGHT.md:130`、`CONTROL_WEIGHT_SNR.md §8b`、`EXP-04-RECONSTRUCTION.md:106` **四处一致** ✓ |
| `:94/:96` | `SNR_combined²=ΣSNR_k²`：相对偏差 2.2e-16 | 同 `COMPARISON_TABLES.md:126` ✓ |
| `:97` | `w_k=SNR_k(**F_ref**)²/**F_ref,k**² ≡ 1/σ_F,k²`：1.1e-16 | ⚠ **同一行**分子无下标、分母有下标混用（S2）；正本 `PSF_SIGNAL_WEIGHT.md:75` 两侧均带 `,k` |
| `:99` | 入库量必须是**未加权原始 SNR**（PSFSNR 的功率比口径不可入库，PSFSW 是权重也不可入库） | 与 `PSF_SIGNAL_WEIGHT.md:126`、`07_noise_snr.md:57-59` 一致 ✓ |
| `:45/:106` | `1.44/√N` 为**相对**标准误；实测 **1.166/√N**；偏保守 24% | ⚠ 正本另给 **1.152**（`NOISE_MODEL.md:99`）与 **1.144**（`:301`）⇒ 三系数并存（S3） |

### 2.6 三份结果表（`EXP02/EXP03/EXP04_TABLES.md`）

- `EXP02_TABLES.md`：本件**无 SNR 定义与权重换算主张**，其口径主张全在 σ̂ 侧——`:15` 表头把 `F0 prod / F1b fix / F1a mesh-median / A1 / A2 / D / kf / verdict` 作列名，件内**不定义、不指路**（定义在 `EXP-02-STRUCTURE-CONTAMINATION.md:423-434`）。
- `EXP03_TABLES.md:76`「表 A4 解析臂 · R0 **闭式偏差式对拍** `σ̂/σ_n = sqrt(1+(sB)²/(12σ²))`」——权重换算前置量（σ̂ 偏差）的唯一闭式，写在标题里、无编号、无出处、无适用条件。
- `EXP04_TABLES.md:3`「T1 算子主对比表（Δ=64，**estimated 模式：控制值来自数据稳健 MAD**）」、`:5` 表头「arm（**E / RMSE dex**）」、`:27`「T2 适用域：各面各 Δ 的最优臂（**按 E 最小**）」、`:172`「T3 失效边界 Δ\*（sparse 双线性 **RMSE** 首次超过帧级臂）」（⇒ 与上位 `07_noise_snr.md:102` 的 **E 判** 同名不同判）、`:310`「恒真门：真值场为常数时**帧级常数臂 RMSE≡0**，'空间臂严格更优'数学上不可能」、`:321`「恒真判据：`{"1": false, "5": true, "10": true, "100": true}`」（键 = K 值，件内未定义）。

### 2.7 参照正本同题句（非本批，仅供对表；本批不对其下结论）

| 正本位置 | 同题原句 |
|---|---|
| `docs/plugins/algorithms_phase1/07_noise_snr.md:33-35` | `W_psf,k = a_k² P_kᵀ C_k⁻¹ P_k = 1/Var(F_hat_k)`；`SNR_k²(F_ref,k) = F_ref,k² W_psf,k` |
| `…07_noise_snr.md:69-74` | `SNR_k(F_ref,k) = F_ref,k · sqrt(W_psf,k) = F_ref,k / σ_F,k`；`w_k = 1/σ_F,k² = SNR_k(F_ref,k)² / F_ref,k² ⇒ w_k ∝ SNR_k²（**配对性只在同一帧内成立**）` |
| `…07_noise_snr.md:77` | **逐帧 `F_ref,k`（不是组内公共常数）**…`reference_flux_scope = frame_independent_fixed_magnitude`…**不设组间 `F_ref` 硬闸门** |
| `…07_noise_snr.md:94` | `sparse_reconstruct` **不乘帧级标量**（控制点值即绝对信噪比本身） |
| `…07_noise_snr.md:110-116` | `σ_i² = σ_sky,i² + (RN/g)² + F·P_i/g`，**读噪只出现一次**；`sigma_sky_source` 二选一必填 |
| `…07_noise_snr.md:236` | 把控制点值按**相对因子**解释（含乘/除帧级标量做还原）⇒ **必须判红** |
| `docs/science/PSF_SIGNAL_WEIGHT.md:23` | `W_info,k = a_k² P_kᵀ C_k⁻¹ P_k`（`W_info` 符号的正式出处） |
| `docs/science/PSF_SIGNAL_WEIGHT.md:75-88` | `w_k = SNR_k² / F_ref,k² ≡ 1/σ_F,k²`；**"没有可选择的口径"**；`阶段一 产出稀疏 SNR 控制点（控制点存绝对 SNR，**不乘/除帧级标量**）` |
| `docs/science/PSF_SIGNAL_WEIGHT.md:125-131` | `σ_F⁻²=ΣP_i²/σ_i²`；`w_k=SNR_k²/F_ref,k²≡1/σ_F,k²`（恒等相对偏差 2.22e-16）；`E=Var_w/Var_opt−1`；`m_ref=6.0` **逐帧独立、必须同帧配对** |
| `docs/science/CONTROL_WEIGHT_SNR.md:27-29` | `quality_weight = frame_quality_scalar × local_quality_proxy/median`…**无量纲、不产生权重** |
| `lib/algorithms/integration/v6/include/astrocs/v6/weight_chain.h:35-47` | 「⚠ 本节**曾写**「F_ref 为组内公共常数…逐帧 F_ref 不得使用」—— 该表述**已作废**…用组标量反而在跨指向时**破坏**同源性。变更 claim: WEIGHT-FREF-PERFRAME-001」 |
| `eng/contracts/schemas/unified/sparse_snr_layer.schema.json:301-302/:375` | `const: "absolute_flux_type_snr"`；「该层**不**存相对因子、**不**按中位归一…**声明为任何相对语义 ⇒ schema 判红**」；「每点的 sparse_snr_value 是该点的**绝对** SNR（不是相对因子）」 |
| `ASTROCS_DESIGN.md:226`（§3.1） | 稀疏层**不以帧级 SNR 为尺度基准**，消费时直接由绝对控制点重建为稠密 SNR 场（**不经帧级 SNR 乘除**）；同一**逐帧**参考通量 `F_ref` |
| `ASTROCS_DESIGN.md:157`（§2.2） | `w(x,y) = SNR(x,y)²/F_ref² = 1/σ_F(x,y)²`（**分母取含源光子散粒项的逐像素总方差**） |
| `ASTROCS_DESIGN.md:404`（§5.3） | 参考通量 `F_ref` 锚定固定参考星等（**逐帧**、只依赖本帧标定） |

---

## 3 同主题组与差异

（跨批发现按派单 §6 注明别批路径；对别批文件**只读不代审**。）

### 组一 · `F_ref` 是"组内公共常数"还是"逐帧"（P0，本批最高价值）

| 侧 | 段落 |
|---|---|
| 本批 | `实验/absolute-snr/docs/snr-propagation-design.md:85`「`F_ref` \| **组内公共**参考通量 \| ADU」；`:319`「`F_ref` 必须是**组内公共常数**，否则 `w = SNR²/F_ref² = a_f²/σ_f²` 的配对定理失效（`lib/algorithms/integration/v6/include/astrocs/v6/weight_chain.h:34-41`）」 |
| 上位 | `docs/science/PSF_SIGNAL_WEIGHT.md:75-78`「`w_k = SNR_k² / F_ref,k² ≡ 1 / σ_F,k²`…帧级 SNR 与稀疏控制点 SNR 共用同一物理定义与同一**逐帧**参考通量 `F_ref`」；`07_noise_snr.md:77`「**逐帧 `F_ref,k`（不是组内公共常数）**…**配对性只要求同一帧内** SNR 与 `F_ref` 同源，**不要求跨帧相等**」；`ASTROCS_DESIGN.md:404`「`F_ref` 锚定固定参考星等（**逐帧**、只依赖本帧标定）」 |
| 代码事实 | 被引文件自己在被引段落下 6 行（`weight_chain.h:42-47`）声明「本节**曾写**「F_ref 为组内公共常数…不得使用」——该表述**已作废**…变更 claim: WEIGHT-FREF-PERFRAME-001」 |
| 差异性质 | 不是"两份文档说法不同"，而是**下位引用了一段已在同一文件内被撤销的文字**。连带：`07_noise_snr.md:254` 已把"人为要求组内 `F_ref` 相等（组间硬闸门）"设为**必红负例** ⇒ 本批 `:319` 的硬约束 #2 正是上位明令判红的做法。同型残留：`results/DOC_CORRECTIONS.md:97` 同行 `SNR_k(F_ref)²/F_ref,k²` 下标不一致 |
| 整治动作 | 改本批两件（`snr-propagation-design.md:85/:319`、`DOC_CORRECTIONS.md:97`）为逐帧口径并删"组内公共常数"硬约束；正本无需改 |

### 组二 · 稀疏 SNR 层值语义：绝对 vs 帧级×相对（P0，含数值层 `rho` 残留）

| 侧 | 段落 |
|---|---|
| 本批（现行式） | `snr-propagation-design.md:351-352` 式 (2.5)「实际 SNR(x,y) = **帧级 SNR_k × 帧内相对因子 intra_k(x,y)**」；`:495`「(b) **相对帧级因子（推荐）** `rho_c = SNR_c/SNR_frame`」；`:1510-1517` §13.9「负责人给出的分解 `SNR(p) = SNR_frame × rho_c(p)`…与本文冻结式**一致**…**逐字一致**」；`:1521`「`w(p) = SNR(p)²/F_ref² = w_frame · rho(p)²`」 |
| 本批（同文件内自我否定） | `:356-357`「【订正记录（SNR-DESIGN-01）】式 (2.5) 的…分解**已由负责人裁决取消**」；`:505-513`「数值语义**冻结为 (a)**…**本节 (b)/(c) 保留为实验对照臂**…**键名保留为历史记录**」 |
| 上位 | `ASTROCS_DESIGN.md:226`「稀疏层**不以帧级 SNR 为尺度基准**…**不经帧级 SNR 乘除**」；`07_noise_snr.md:94`「`sparse_reconstruct` **不乘帧级标量**」；`07_noise_snr.md:236`「按相对因子解释（**含乘/除帧级标量做还原**）⇒ **必须判红**」；`sparse_snr_layer.schema.json:301-302` `const:"absolute_flux_type_snr"` |
| **第二处残留（数值层）** | `results/COMPARISON_TABLES.md:75`「## T4 三口径适用域（**RMSE(log10 rho)**，Delta=64…）」——生成结果表把适用域主表的度量写成相对因子的对数 RMSE。⇒ 取消裁决落了规范层与 schema 层，**未落数值表层** |
| 唯一权威支撑实为虚构 | `:501` 声称 (b)「与既有权威的合成规则**逐字一致**：『实际 SNR = 帧级 × 帧内』（`07_noise_snr.md:75`）」→ 该行是代码块收尾围栏；该句式全 `docs/` 不存在 ⇒ **该规则在权威链里根本没有**（H8） |
| 整治动作 | 删草案的式 (2.5)/(b)/§13.9 现行表述（"对照臂"用途若保留须显式标为负例并注明按 schema 必红）；`COMPARISON_TABLES.md` T4 表题去 `rho` 改绝对量名；正本无需改 |

### 组三 · "本仓无哈勃数据" vs 本单元三份 HST 前向臂（复现性 + 措辞失真）

| 侧 | 段落 |
|---|---|
| 本批（否定式声明） | `snr-propagation-design.md:207`「（本仓**无哈勃数据** ⇒ 用 L4 真实校准帧，见下）」、`:236`「**本仓无哈勃数据，该替换必须显式声明**」、`:1373`「**未用哈勃数据**：本仓无哈勃帧」；`surveys/frame-snr-survey.md:470`「**HST 数据 —— 本工作区无哈勃数据**（全仓 `find -iname '*hst*'` 命中 0）」 |
| 本批（同时给出 HST 结果） | `EXP02_TABLES.md:78`「表 B1 **HST M16 真实模板 + 物理前向仿真**」（22 行）；`EXP03_TABLES.md:106`「表 B1 **HST 前向臂** · 绝对准确性（有真值）与跨帧一致性」（20 行）；`EXP04_TABLES.md:174`「**HST M16**（2048 crop）：Δ\* = 16（crossed）」及 T1/T2 的 `hst_m16` 列；`COMPARISON_TABLES.md:99`「\| **hst_m16** \| 32.3 \| … \|」 |
| 登记事实 | `testdata/README.md:55`「## 3. `testdata/HST_M16/` — HST WFC3/UVIS M16 三帧 drz（**只读，不入库**）」、`:19`「`HST_M16/*.fits`（3 × 268,917,120 B = 770 MB）\| ❌ 不入库 \| 体积超限，**公开 HLSP 可重新下载**」；跟踪集实测 `git ls-files testdata/HST_M16` = **0**、`git log --all --diff-filter=D -- testdata/HST_M16` 无记录 ⇒ **从未入库**（非被删） |
| 差异性质 | 措辞把"**不入库**"写成"**无**"。后果一：三份生成表的 HST 臂（60+ 行）在离线节点不可复算；后果二（更重）：该 HST 域结论**已上升为正本条款**——`CONTROL_WEIGHT_SNR.md:215`、`07_noise_snr.md:100-106`（三条适用域边界）、`PSF_SIGNAL_WEIGHT.md:131`（三口径适用域），而 `ASTROCS_DESIGN.md:184` 把"**哈勃真实数据前向合成的仿真帧**"列为该创新点的强制证据类别 |
| 整治动作 | 全部改为正向可核表述：「HST 帧不在库内，属 `testdata/HST_M16/` 登记的可重新下载只读数据集；本节数值在离线节点不可复算」。是否在 §12.2 侧登记复现性依赖由前台判定 |

### 组四 · Δ\* 一个符号两种判据 + 上位数字不在被指的"数字正本"里 + 循环指认（P0）

| 侧 | 段落 |
|---|---|
| 本批定义 A | `EXP04_TABLES.md:172-178`「T3 失效边界 Δ\*（sparse 双线性 **RMSE** 首次超过帧级臂）… HST M16：**Δ\* = 16**（crossed）」（Δ=16 即 `0.0543 > 0.0538`） |
| 上位定义 B | `07_noise_snr.md:102`「定义 Δ\* =「该算子 **E** 首次劣于帧级臂的最小 Δ」：`bilinear_regular_grid_v1` **Δ\*=32 px**；默认 `natural_bicubic_spline_clip_v1` Δ\*=32 px；`..._mesh_median_v1` Δ\*=128 px」 |
| 上位承重数字 | `07_noise_snr.md:102/:191` 的 **0.1375 / 0.0490**；`CONTROL_WEIGHT_SNR.md:215` 的 **0.0490 vs 0.0530** |
| 本批实际数值 | `EXP04_TABLES.md:10` `spline_natural`@`hst_m16` = **0.1549**/0.1291；`:11` `sextractor_spline` = **0.0492**/0.0581；本件**无 E=0.1375、无 E=0.0490 的任何臂**，也**无四个冻结算子标识中的任一个**（`grep -c … → 0`） |
| 数字真正所在 | 别批文件 `实验/absolute-snr/docs/EXP-04-RECONSTRUCTION.md:348-349`：「HST M16 \| `spline_natural` \| 0.1549 \| **0.1375** \| ×0.887」「HST M16 \| `sextractor_spline` \| 0.0492 \| **0.0490** \| ×0.998」 |
| 循环指认 | `07_noise_snr.md:176`「取值依据、失效边界与完整适用域**正本**见 `实验/absolute-snr`（EXP-04 §5）」+ `CONTROL_WEIGHT_SNR.md:215`「**判据与数字正本**见 `实验/absolute-snr` EXP-04 §3.2/§4.4/§5.3」 ↔ `EXP-04-RECONSTRUCTION.md:7`「**判据正本**：`docs/science/CONTROL_WEIGHT_SNR.md §8b`」 |
| 整治动作 | ① 消环：正本条款收回规范职责，`实验/` 侧改标"数值证据（GENERATED，不作规范）"；② 上位三处数字改指 `EXP-04-RECONSTRUCTION.md:348`，或在 `EXP04_TABLES.md` 补"冻结算子 ↔ 实验臂"映射表；③ Δ\* 在两份文件各加判据量限定（E 判 / RMSE 判），或统一为 E 判后重算本件 T3 |

### 组五 · 判据 SP-0：设计层有名、权威链无定义、实验层已降级

| 侧 | 段落 |
|---|---|
| 设计层（`docs/` 唯一命中） | `docs/design/UNIFIED_MODEL.md:46`「三者精度对比是论文核心实验（**判据 SP-0**；稀疏优势由实测判定，不做预设）」——只点名，**不给式子** |
| 定义唯一所在（本批） | `snr-propagation-design.md:429-430`「**判据 SP-0（先行冻结）**：稀疏 SNR 层**仅当**它对最终叠加的加权效率增益大于其重建误差带来的损失时才启用」；`:670` 式 (3.3) |
| 同文件内的降级 | `:432`「【已裁决（负责人 `GAP_AUDIT §9.46`）】**SP-0 不是开关，是诊断量**」；`:1319-1322`「**SP-0 降为诊断量**」 |
| 差异性质 | 标准 01 §3「每份文档、每个机制都能追溯到最高设计的一条要点；追溯不到的机制先补要点再保留」——SP-0 反向：设计层引用它、全 `docs/` 无定义、定义只在一份链外草案里，而该草案自己声明它已不是判据。`git grep "SP-0" -- docs/ ASTROCS_DESIGN.md` 仅 2 命中（`DRIZZLE_GEOMETRY.md:61` 是 `DISP-009` 子串，非本判据）⇒ **有效命中 1** |
| 整治动作 | 在 `docs/science/CONTROL_WEIGHT_SNR.md §8b`（E 判据正本）补 SP-0 的现行定义，**或**删除 `UNIFIED_MODEL.md:46` 的 SP-0 名；本批草案的 (3.3) 随之删除或改标"已否决的历史提案" |

### 组六 · 存储代价 2× 分歧（本批内，机器可核）

`COMPARISON_TABLES.md:106-115`（表头标 **float32**）：`sparse Δ=64 → 16,384 B / 0.01562 MiB`、`frame → 4 B`；`EXP04_TABLES.md:227-233`（**未标 dtype**）：`sparse Δ=64 → 32,768 B / 0.0312 MiB`、`frame → 8 B`。稠密侧两件一致（67,108,864 B / 64 MiB / 64.0×，与 `PSF_SIGNAL_WEIGHT.md:131` 同）。差 = 每控制点 4 B 对 8 B。**动作**：`EXP04_TABLES.md` 存储表补 dtype 列；上位若引"稀疏 ≪ 1 MiB"须注明该结论与 dtype 无关（数值面不得两存）。

### 组七 · 帧级臂 median vs MAD 恒等（跨两件，机器可核）

`COMPARISON_TABLES.md:77-102` 的 `frame_median` 与 `frame_mad` 两列 **24/24 行逐格相同**（`awk -F'|'` 比对，差异行数 = 0）；`EXP04_TABLES.md:24-25` 的 `frame_median` 与 `frame_global_mad` 两行在 28 列中**仅 3 格差在 `-0.0000` 与 `0.0000` 的符号形态**，其余 25 格逐位相同。而 `snr-propagation-design.md:44-45` 把"理想帧标量 vs 生产整帧 MAD 标量"当关键分档（散差 1.058× 下 0.0002% vs 0.126%）。**⇒ 两张生成表都说两者无差别，没有一处说明为什么**；上位那段区分因此不可判定。动作：表内声明恒等条件，或按重复列去重。

### 组八 · `1.44/√N` 家族：三个实测系数并存（跨批，取证在本批）

`NOISE_MODEL.md:99` `c ≈ 1.152/√N` ‖ `NOISE_MODEL.md:301` `SE(σ̂)/σ ≈ 1.144/√N_sky` ‖ 本批 `DOC_CORRECTIONS.md:45/:106` 实测 `1.166/√N`；冻结预算常数 `1.44/√N`（相对口径）与 dex 口径 `0.625/√N` 见 `NOISE_MODEL.md:99-101`。另 1.44 的行锚在本批 `:38/:53` 与 `CONTROL_WEIGHT_SNR.md:202` 三处均写 `NOISE_MODEL.md:86`，实为 `:99`（X2）。

### 组九 · 文献条目级冲突（本批两份 survey 之间 + 与 `docs/research/`）

| 对象 | 侧 A | 侧 B |
|---|---|---|
| Magnier et al. 2020（psphot） | `f-instr-survey.md:243` **ApJS 251, 6**、DOI …**abb82a** | `frame-snr-survey.md:25` **ApJS 251, 5**、DOI …**abb82c**（并判 Waters=251,4 为对调） |
| Siril `median_and_mean.c` | `DOC_CORRECTIONS.md:89` **1.2.4 : 868-870** | `SNR_WEIGHT_RESEARCH_PACK.md:197` **1.4.4 : 1091-1094**（历史锚 `:1111-1230`） |
| SWarp `coadd.c:1279-1311` | `DOC_CORRECTIONS.md:88`「**未能在该文件中定位到所述行**」 | `SNR_WEIGHT_RESEARCH_PACK.md:198`「**有效**：`COADD_WEIGHTED` 分支…」 |
| Moffat 1969 bibcode | `snr-propagation-design.md:1140` `1969A&A.....3..455M` | `f-instr-survey.md:105` `1969A&A.....3.....455M`（多余点号，非有效 bibcode 形态） |
| 依赖快照 | `snr-propagation-design.md:1352` **astropy 7.0.1** | `frame-snr-survey.md:11` **astropy 8.0.1** |
| 调研主题重叠 | `frame-snr-survey.md:430-442`（PixInsight 式[18]/[20] 逐字核对，无版本行锚） | `SNR_WEIGHT_RESEARCH_PACK.md:117-147`（同一 pidoc 的版本化行锚，无逐字英文引文）——**两份独立核对同一手来源、互不引用** |

### 组十 · SNR 符号族在三份正本之间的分化（跨批；本批为受害者）

同一 `a²PᵀC⁻¹P`：`W_info` 用于 `docs/science/PSF_SIGNAL_WEIGHT.md:23`；`W_psf` 用于 `07_noise_snr.md:33`；对象名 `point_information` 用于 `07_noise_snr.md:24` 与 `ASTROCS_DESIGN.md:223` 数据对象表。本批 `snr-propagation-design.md:87` 采 `W_info`（与科学正本一致），另造 `intra_k`/`rho_c`/`链A·链B`（权威链与 schema 均无：`git grep "intra_k\|rho_c" -- docs/ eng/contracts/` 0 命中）。整治归属：正本层符号统一由 `docs/` 批次处理；本批只需删除自造符号或给出到 `W_info`/`point_information` 的映射。

---

## 4 索引与链接缺陷清单

### 4.1 与 `docs/DOCUMENT_INDEX.yaml` 的登记不符项

| 问题类型 | 条目 | 说明与命令 |
|---|---|---|
| **规范层口径冲突（结构性，非逐件缺陷）** | 本批 **8/8 份** `unregistered_index` | `DOCUMENT_INDEX.yaml:37` `coverage` 自declared 「**docs/\*\*（含 docs/validation/v6/\*\*）+ 仓库根治理文档**」；`:29-31` 扫描口径明列「排除 问题扫描/、reports/、artifacts/、run/、build/、lib/third_party/、**实验/**、工程控制/、logs/ 等过程与证据目录」。标准 01 §3 要求「`DOCUMENT_INDEX.yaml` 是**全文档集**唯一索引地图：**现存文档全部登记**，登记内容含路径、角色、主题、上游、下游；未登记即缺陷」，`ASTROCS_DESIGN.md:54` 同调。**⇒ 索引自declared 范围与标准 01 / 最高设计正面不相容。**真动作是一份范围裁决（索引扩面到 `实验/`+`工程控制/`，或标准 01 §3 改写并明确正式文档层边界），**不是补 8 条登记**。**§10 上呈候选** |
| 下游可见性缺陷（索引口径的必然后果） | `DOC_CORRECTIONS.md`（被 `NOISE_MODEL.md:324`、`CONTROL_WEIGHT_SNR.md:201-202` 引用）、`EXP04_TABLES.md`（被 `CONTROL_WEIGHT_SNR.md:215`、`07_noise_snr.md:176` 以"EXP-04 §…"间接指认） | 扫描面排除 `实验/` ⇒ **"正本 → 实验"的边在机器面上完全不可见**：既检不出、也修不了。注：本审查**未运行** `check_doc_index.py`（派单 §1 禁），该条是按索引头声明的口径做的结构推断，非门读数 |
| 登记了但文件不存在 | **本批 0 条**：`git grep -n "实验/" docs/DOCUMENT_INDEX.yaml` 只命中 `:30` 的排除声明一行，索引内无 `实验/` 条目可失效 | — |
| 角色标注与本判定不符 | **不适用**（`实验/` 层无索引条目可标注） | — |
| 悬空指针（别批，作证据登记不代审） | `DOCUMENT_INDEX.yaml:4`「重写：DOC-403（**2026-09-21**，基线 HEAD **d1ccfa1bed9c**）」、`:36-37` `authoring_task` / `base_main_sha` | 唯一索引地图自身携带日期、任务号与 sha 锚（标准 01 §4 禁项）。登记给管 `docs/` 根的批次 |

### 4.2 悬空引用与失效锚点汇总（本批 8 份：**编号项 17 条、展开 27 个失效指针**）

原编号 #1–#18；**#10（`audit/physnoise.py`）经跟踪集复验后撤回**，故 17 条。**判定口径**：`git ls-files <路径>`（跟踪集）+ `git log --all --diff-filter=D -- <路径>`（删除历史）；不以磁盘存在性为据。

| # | 文件:行 | 指向 | 跟踪集 | 删除历史 | 定性 |
|---|---|---|---|---|---|
| 1 | `snr-propagation-design.md:1029,1434,1457,1057` | `reports/RELEASE-02/unc-prop-audit.md` | 0 | **`ae0f1e63`（2026-09-21）CLEAN-402 清理一次性报告** | **曾存在、已被治理删除**；`eps_theta` 的原值与复核值双方共依此件 |
| 2 | `…:848` | `docs/science/q2-snr-smooth` | 0 | 无记录 | **从未存在的科学文档**；式 (4.3) 的唯一科学层出处 |
| 3 | `…:1222` | `reverse_verify/README.md §3` | 0 | 目录级解散已由 `DOCUMENT_INDEX.yaml:31` 记为 ROOT-CONSOLIDATION | 同名件在 `…/snr_design/README.md`（跟踪集存在） |
| 4-8 | `…:1285-1289` | `experiments/snr_design/exp{1..5}*.py`（**5 条**） | 0 | — | §10"可复跑"清单 **5/5 不可达**（真实路径无 `experiments/` 段） |
| 9 | `…:1361-1362` | `experiments/snr_design/exp1..exp5`、`…/audit/`、`run/reverse_verify/…` | 0 | — | 同因 + 落 `run/`（跟踪集 0） |
| 11 | `…:1164` | `bibliography.md §9` | 0 | 无记录 | 同文件 `:1106` 用的是可达全路径 ⇒ 文内两套路径 |
| 12 | `…:501` | `07_noise_snr.md:75`（"『实际 SNR = 帧级 × 帧内』逐字一致"） | 文件存在 | — | **空行锚 + 虚构权威**：该行 = ` ``` `；句式全 `docs/` 不存在 |
| 13 | `…:183` | `NOISE_MODEL.md:183`、`:202`（**2 个**） | 文件存在 | — | **行号漂移 194 / 196 行**：实在 `:377`、`:396`；`:183` 为空行 |
| 14 | `…:716` | `PSF_SIGNAL_WEIGHT.md:57`（ivar/variance/W_info 红线） | 文件存在 | — | **空行锚**：条款实在 `:65` 与 `:97`（对照同文件 `:303` 引 `:44` **正确命中** ✓） |
| 15 | `…`（9 处） | `07_noise_snr.md:39/:40/:51/:66/:70/:75/:82/:107/:114`（**9 个**） | 文件存在 | — | **行锚集体漂移 2–53 行**（`:39`→41、`:66`→79、`:70`→81、`:82`→131、`:107`→234；`:75`→见 #12）：该正本被重写后旧锚全废 |
| 16 | `f-instr-survey.md:4`、`frame-snr-survey.md:6` | `reverse_verify/README.md §5`、**`../README.md §5`**（**2 个**） | 0 | 无记录 | `实验/absolute-snr/docs/README.md` 从未跟踪；`..` 相对链接**机械列按设计不判** |
| 17 | 定案件 `f-instr-canon.md:26,492`（别批）→ 本批 | `references/f-instr-survey.md` | 0（目录不存在） | 迁移记录在 `REVERSE_VERIFY_MIGRATION.md:31` | **本批件被其定案件以迁移前旧相对路径引用** ⇒ 双向断 |
| 18 | `DOC_CORRECTIONS.md:14` | `lib/algorithms/noise_snr/cpp/include/.../snr_science.h` | 0（该目录只跟踪 `snr_estimator.h`） | 无记录 | **从未存在的头文件** + 路径含**字面 `.../`**；D1"设计意图"的唯一代码权威 |

**证据落在不入库目录**（跟踪集 0，不计入上表）：`snr-propagation-design.md:159,236,600,1339`；`frame-snr-survey.md:4,316,425,443,455`（`run/reverse_verify/frame_snr/{external_crosscheck,redlines_physical}.json`）；`DOC_CORRECTIONS.md:5,38,55`；`EXP02_TABLES.md:5-11`（`results/exp02_figs/*.png` ×5，跟踪集 0：`git ls-files 实验/absolute-snr/ | grep -c '\.png$'` = **0**，且无删除记录 ⇒ 从未入库）。

### 4.3 角色错放汇总（标准 01 §2 目录↔角色一一对应）

| 文件 | 现目录 | 应属 | 性质 |
|---|---|---|---|
| `snr-propagation-design.md` | `实验/absolute-snr/docs/` | 算法推导 → `docs/algorithms/`；§6 → `docs/research/`；§12–§13 → `artifacts/evidence/` | **一件三角色**，违 §2「每份文档属于且仅属于一个角色」+ §1「不另立权威链」 |
| `surveys/f-instr-survey.md` | `实验/…/docs/surveys/` | `docs/research/` | 角色对、目录错；被定案件当作"核对状态终据" = 层级倒置 |
| `surveys/frame-snr-survey.md` | `实验/…/docs/surveys/` | 并入 `docs/research/SNR_WEIGHT_RESEARCH_PACK.md` | 同上，且与研究包同主题两存（组九末行） |
| `DOC_CORRECTIONS.md` | `实验/…/results/` | `artifacts/evidence/`（被上位吸收后删除） | **过程台账**，角色表无此角色；被 `docs/science/` 反向取数 = 倒置 |
| `COMPARISON_TABLES.md`／`EXP0{2,3,4}_TABLES.md` | `实验/…/results/` | **目录正确**（GENERATED 佐证） | 无角色错放；缺陷是缺上游指针 + 被上位误指为"数字正本"（组四） |

### 4.4 机械线索文件校准（派单 §4.4 要求的交付：我在校准工具）

对 `inventory/doc_mechanical.csv` 本批 8 行逐列核对：

| 列 | 结论 |
|---|---|
| `commit_sha` | **系统性误报（高估）**。5 件被记入非 sha 值：`snr-propagation-design.md` 记 `['1996164','ddf692edd3…']`——前者是 **DOI `10.1051/aas:1996164` 的尾数**（正文 `:1114` 明写该 DOI），后者才是真 sha；`COMPARISON_TABLES.md` 记 `['1000000','67108864']`——是 **2²⁰ 与 4096²×4 的字节数**（`:21`/`:108` 表体），纯十进制；`EXP02_TABLES.md` 记 5 个 `['20251212'…]`——**全部是真实数据文件名里的 `20YYMMDD` 日期戳**（`:109-124`）；`EXP04_TABLES.md` 记 `['16777216','67108864']`（= 4096² 与字节数）；`EXP03_TABLES.md` 记 `[]` ✓ 正确。**建议判据**：仅当命中 `commit`/`sha`/`HEAD`/`rev-parse` 上下文，或以 40 位全形出现，才计入 sha 列 |
| `dates` | **误报 1 + 漏报 3**。`snr-propagation-design.md` 记 `[1131,1349]`：`:1349` 真（`2026-09-19`），`:1131` 假（该行 `DOI 10.1016/j.ascom.2018.07.001` 被切成日期）；**漏报** `frame-snr-survey.md:51`「(2016/2026)」、`f-instr-survey.md:221`「Current 2026-08-06」、`f-instr-survey.md:431`「FROZEN，2026-08-23」（三件 `dates` 列均为 `[]`） |
| `dangling_links` | **严重低估**（与派单 §4.4 预告同向，幅度未估到）。实测**编号项 17 条 / 27 个失效指针**，机械列共报 **2 条**（#1 `unc-prop-audit.md` ✓、#18 `snr_science.h` ✓）⇒ **漏报 15 个编号项 / 25 个指针**。另 `f-instr-survey.md` 报的 `lib/include/lsst/meas/base/PsfFlux.h` 属**误报**（外部仓库文件，`:221-224` 已注明"源码逐字原文，`lsst/meas_base@main`"）。**根因**：反引号内 `路径:行`、`..` 相对链接、**行锚越界/空行**三类均不在判定面——本批 12 个失效指针（#12/#13/#14/#15）属"文件存在但行号指错"，机械检查器结构上看不见。**建议新增子判据**：对每个 `file:N` 或 `file:N-M` 锚跑 `git show HEAD:file \| sed -n 'Np'`，返回空即判"空行锚"，超行数即判"越界锚" |
| `history_narrative` | **系统性漏报**。本批 8 件全记 `False`／`[]`；`snr-propagation-design.md` 实有 11 个【复核后】/【已裁决】/【订正记录】块 + "原结论""已作废""保留以存证""键名保留为历史记录"；`DOC_CORRECTIONS.md` 有"已闭环""重做""原…口径""订正登记"。`COMPARISON_TABLES.md`/`EXP0*_TABLES.md` 的"勿手改"是状态标注不是历史叙事，记 `False` **正确** ✓。**建议词表补**：复核后／订正记录／已裁决／已闭环／保留…以存证／曾写／原结论／历史记录 |
| `upstream_pointer` | **误报**。`EXP03_TABLES.md:3`「源：`results/exp03_e1_analytic.json`…」被判 `True`，但那是**数据血缘**不是**规范上游**（标准 01 §3 要的是上游最高设计条款）。`snr-propagation-design.md` 与两份 survey 记 `False` **正确** ✓。**建议**判据收紧为命中"上游"关键字 **且** 含 `ASTROCS_DESIGN` + 条款号 |
| `meta_block` | **低估**。`DOC_CORRECTIONS.md:3-5` 三行块 + 7 处【…域】状态标签被判 `False`；`frame-snr-survey.md:3-13` 十一行块（含环境快照）被判 `False`；`snr-propagation-design.md:3-6` 亦被判 `False`——而它命中 §4 四类禁令中的三类。`EXP0{2,3,4}_TABLES.md` 与 `COMPARISON_TABLES.md` 记 `False` **正确** ✓。**建议**：除首行关键字外，追加"前 15 行内引用块（`>`）行数 ≥3"判据 |
| `lines` / `h1` | 8/8 **与实读完全一致**（`wc -l` 逐件复核；`h1` 与首行 `#` 逐字同）⇒ 这两列可直接采信 |
| `unregistered_index` | 8/8 = True，**事实正确但归因错误**：根因是 `DOCUMENT_INDEX.yaml:29-31` 把 `实验/` 显式排除在覆盖与扫描口径外，**不是这 8 份各自漏登记**。当作 8 条逐件缺陷会计数，会掩盖真正需要裁决的范围问题（4.1 首行） |

---

## 5 覆盖率自报

**已审 8 / 分配 8。** 未逐行读名单：**空**。

| 清单行 | 文件 | 行数（`wc -l`） | 读法 |
|---|---|---|---|
| 1 | `实验/absolute-snr/docs/snr-propagation-design.md` | 1528 | **全文通读**（>600 行仍逐行读，一次 `Read` 取 1–1528；未跳读、未截断、未抽样） |
| 2 | `…/docs/surveys/f-instr-survey.md` | 501 | 全文通读 |
| 3 | `…/docs/surveys/frame-snr-survey.md` | 473 | 全文通读 |
| 4 | `…/results/COMPARISON_TABLES.md` | 168 | 全文通读（含 96 行表体逐行） |
| 5 | `…/results/DOC_CORRECTIONS.md` | 106 | 全文通读 |
| 6 | `…/results/EXP02_TABLES.md` | 124 | 全文通读（含 3 张表约 100 行数据） |
| 7 | `…/results/EXP03_TABLES.md` | 177 | 全文通读（含 A1–A4/B1/C1–C2/D1 七张表） |
| 8 | `…/results/EXP04_TABLES.md` | 322 | 全文通读（含 T1 的 28 列 × 22 臂、T2 的 141 行） |

清单核对：`独立审计/批次清单/DB-16.txt` 去空行 = **8 行**，与派单"8 份"一致；**派单文字与清单无差异**（派单点名的 `snr-propagation-design.md` 在清单第 1 行）。

**为判定而读、未下结论的非本批文件**（按派单 §3「可以读别批的相关节，但只对清单项下结论」）：`ASTROCS_DESIGN.md`（§0、§2.2、§3.1、§5.3）、`docs/plugins/algorithms_phase1/07_noise_snr.md`（全文）、`docs/science/PSF_SIGNAL_WEIGHT.md`（全文）、`docs/research/SNR_WEIGHT_RESEARCH_PACK.md`（全文）、`docs/science/CONTROL_WEIGHT_SNR.md`（§0–§2a 与 `:195-215`）、`docs/science/NOISE_MODEL.md`（标题结构 + `:62/:99/:301/:312/:324/:377/:396` 点读）、`docs/DOCUMENT_INDEX.yaml`（头部与口径段）、`docs/design/UNIFIED_MODEL.md:46`、`eng/contracts/schemas/unified/sparse_snr_layer.schema.json`（`:22/:244/:300-302/:354-399`）、`lib/algorithms/integration/v6/include/astrocs/v6/weight_chain.h`（`:28-47`）、`实验/absolute-snr/docs/{f-instr-canon,frame-snr-canon,EXP-02-STRUCTURE-CONTAMINATION,EXP-03-REGIONAL-SIGMA,EXP-04-RECONSTRUCTION}.md`（头部与判据段点读；DB-15/DA-06 批次所有）、`testdata/README.md`、`实验/shared/REVERSE_VERIFY_MIGRATION.md`、`results/b4_integration.json`（文本字段）。**上述任一件均未出登记结论。**

**本批未做的两件事（纪律，不是遗漏）**：① 未运行任何 `eng/**` 检查器、`ctest`、构建或端到端（派单 §1 禁），全部判定由通读 + 推导 + 只读命令得出；② 未 `import`/`importlib` 仓库内 Python——需要数值出处时读的是 JSON 文本字段。

---

## 6 我证伪了任务书的哪个前提

| 前提 | 出处 | 实测 | 判定 |
|---|---|---|---|
| 「以 `docs/` 为重点（当前**约 233 份**、30+ 子目录）」 | `tasks/AUD-101_...md:15` | `git ls-files "docs/" \| grep -c '\.md$'` = **233**（精确命中） | **未证伪**。`docs/` 一级子目录实测 25 个，"30+"偏高，属宽泛表述不列证伪。**口径须固定**：`_union.txt` 的 360 = 233 份 `docs/**/*.md` + 25 项 `docs/` 内非 `.md` 文档层件（yaml/csv/json/py，已逐项 `comm -23` 列全）+ `实验/` 62 + `工程控制/` 31 + 根 9；**360 与 233 是两个口径，不可互证为"少计"**（该对比此前已被作废过一次，此处只作澄清）。**对本批有意义的唯一结论**：本批 8 份**全部在 233 之外**（都在 `实验/` 的 62 里） |
| 「找出**孤儿文档**（无上游、无引用）」 | `tasks/AUD-101_...md:21` 步骤 4 | 本批：**无上游 8/8**（8 份开头都是任务号或生成命令，无一标最高设计条款）；**无引用 0/8**（最少 2 个下游；检索词按"全路径 / 裸文件名 / 目录相对段"三套各跑一遍，并在 `07_noise_snr.md` 这一已知应命中对象上跑通同一条命令） | **证伪（定义层面）**：在 `实验/` 层这两个条件**从不同时成立**——`实验/` 件普遍有下游（被单元内正文与 `eng/ci/ledgers/naming_surface.json` 引用）、普遍无上游。按任务书的"且"关系，本批孤儿数 = **0**，会给出"无孤儿"的错误安心。**建议判据改为**「无上游标注 **且** 不在权威链（`docs/`/`ASTROCS_DESIGN` 侧无入边）」。按此定义本批 **8/8 均为链外件**，其中 3 份（`DOC_CORRECTIONS.md`、`EXP04_TABLES.md`、`snr-propagation-design.md`）**反向被权威链指为正本或量化依据** ⇒ "链外却供数"才是本批的真实风险形状 |
| 「这批文件名里混着 `*-design.md` 这类**英文命名、层级不明**的文档」（本批特有关注点） | 派单本批段 | 8 份命名构成：`*-design.md` **1 份**、`*-survey.md` **2 份**、`results/*_TABLES.md` + `COMPARISON_TABLES.md` + `DOC_CORRECTIONS.md` **5 份** | **部分证伪（覆盖面被高估）**：`*-design.md` 只占 1/8。本批主体形状不是"设计说明的层级归属"，而是 **GENERATED 结果表被 `docs/science/` 与 `docs/plugins/` 指认为"数字正本 / 判据正本"**，并与实验正文形成**循环正本指认**（组四）。若只按派单视角整治 `*-design.md` 的归属，会漏掉本批唯一的 P0 结构性缺陷 |
| 「机械线索文件…`unregistered_index` 可以当线索」 | 派单 §4.4 | 本批 8 行该列全 = True | **证伪其判读方式**（非证伪事实）：True 的根因是索引覆盖面不含 `实验/`（4.1 首行），不是逐件漏登记。按逐件缺陷处理会导出"补登记"的错误动作 |
| 「清单里每一份都要通读全文。短文件不得跳读；长文件（>600 行）也要读完」 | 派单 §3 | 本批 >600 行者仅 1（1528 行），已整读；其余 7 份 106–501 行，均逐行 | **未证伪**（可执行且已执行，见 §5） |
| 「开工先跑 `git status --porcelain` 记基线（当前树应恰有两个未跟踪项）」 | 派单 §1 | 开工实测恰为 `?? ACSD整治工作包_AUDIT-06.zip` 与 `?? site/`；`rev-parse --short HEAD` = `c8f64e9a` | **未证伪**，逐项吻合 |
| 「`rg` 在本机不可用（用 `git -C "..." grep`）」 | 派单 §5.2 | 首选 Grep 工具报 `spawn rg ENOENT`；全程改用 `git grep` 可跑 | **未证伪**，且据此调整了检索手段 |

---

## 7 收工基线

命令：`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

**与开工输出逐字相同**（两项、顺序一致、无新增项）⇒ 本批取证动作未污染被审对象。全程未修改、移动、删除仓库任何文件；未执行构建、`ctest`、`run_checks.py`、任何 `eng/**` 脚本或端到端；未 `import` 仓库内 Python；git 操作仅 `status` / `ls-files` / `grep` / `show` / `log`（全部只读子命令）。

**需负责人裁决的两项（本批不自决）**：
1. **索引覆盖范围**：标准 01 §3「现存文档全部登记」 vs `DOCUMENT_INDEX.yaml:29-31` 自declared 排除 `实验/`、`工程控制/`。二者只能留一个（4.1 首行）。
2. **HST 证据的复现性依赖是否影响创新点二的验收主张**：`ASTROCS_DESIGN.md:184` 要求"哈勃真实数据前向合成的仿真帧"，而 `testdata/HST_M16/` 从未入库（跟踪集 0、无删除记录），本批三份生成表的 HST 臂与已上升为正本条款的"HST 类高对比域"结论（组三、组四）在离线节点均不可复算。

<!-- PROGRESS: 8/8 -->
