# 复核-V403 · 第②层独立复算（批次 V403）

- 复核对象：`独立审计/证据/AUD-403-注释与README.md`（全文 177 行，条目 10 条 `AUD403-01`…`AUD403-10`；清单 `独立审计/批次清单/V403.txt`）
- 基线：HEAD=`c8f64e9a`（冻结基线，本批禁测禁构建）
- 只读纪律：未执行构建/cmake/ctest/`run_checks.py`/任何 `eng/**` 检查器；未 import 仓库内 Python；未修改仓库任何文件；仅 `git status/ls-files/grep/log/show` 与读文件文本；`rg` 未使用。
- 节奏：按派单前言 §4（断线重派追加硬条款）执行——骨架先落盘，探索预算封顶 3 命令，单条取证上限 8 命令，判完一条立刻改该行。
- 开工 `git status --porcelain` 原文：
  ```
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```

## AUD403-01 · 注释与现行实现直接矛盾（additive_mode 生产默认）
**判定：确认（范围应扩：矛盾注释不止 1 处）**
- 注释原句：`module_adapters.cpp:9820` `⇒ 生产默认 additive_mode = "c"（本文件 :5503 起），δ 从不施加；`；同一注释块 `:9827` 的处置句又写 `缺省 = (additive_mode ∈ {delta,both})` —— 块内已自相矛盾。
- 真正的取值侧（并排）：`:9835-9836` `additive_mode_pre = seam_pre.value("additive_mode", std::string("delta"))` → `:9838 delta_wanted` → `:9839 sky_enabled`；`:10311-10312` `additive_mode = seam_cfg.value("additive_mode","delta")` → `:10317-10326 additive_mode_effective/sub_c/sub_delta`。两处默认字面量均为 `"delta"`，均在生产链上（非死代码）。
- 省略分支是否真走兜底：`eng/packaging/config/defaults.json` 全文 `git grep -c "seam"` → **No matches found**；`eng/packaging/config/config_registry.json:1848-1856` 登记 `field=additive_mode / declared_default="delta" / registration="none" / finding="unregistered"`，note 明写"生产经 seam.additive_mode 读取（module_adapters.cpp），但不在 defaults.json / CLI 白名单 / 任何 phase_config schema 中"。⇒ 出厂配置里该键**根本不存在**，生产取值就是代码兜底 `"delta"`，注释的 `"c"` 断言为假。
- 权威链核对（不信注释里的"依据"）：插件文档 `docs/plugins/algorithms_phase2/11_upm.md:132` 表格行 `| additive_mode | delta | … 默认 … 保留 B_ref |` 与代码一致（注册表登记的 `doc line: 98` 已漂移，现内容在 132）。
- 范围扩：`module_adapters.cpp:12543` 另有第二条矛盾注释 `天光面未建（如 additive_mode=c 的默认路径）`，把 `c` 称作默认路径；成稿只定位 9820 一处。
- 取证命令：`grep -n "additive_mode" lib/infrastructure/scheduler/src/module_adapters.cpp`、`git grep -c "seam" -- eng/packaging/config/defaults.json`、`git grep -n "additive_mode" -- eng/packaging/config/`、`sed -n '9817,9845p;10311,10326p;12540,12545p'`、`grep -n "additive_mode" docs/plugins/algorithms_phase2/11_upm.md`。
- 与成稿差异：机制与定级无异议（触发路径逐跳可给：节点入参 doc → `:9836` → `sky_enabled` → `:10312` → `additive_mode_effective` → 扣除分支）；仅"现状"里的**矛盾点计数偏低**（应含 `:12543`），且"应为"里可补一条：注释所引 `:5503` 锚已指向无关代码（该窗实为逐帧判词/mark_frame_fail）。
- 影响 D8 的处置：进任务书（S1 级"注释与现行实现直接矛盾"成立）；整改范围把 `:12543` 与 `:9817-9824` 依据段一并列入，删失效陈述、保留 `delta/c/both` 正向语义与 `11_upm.md` 路径锚。


## AUD403-02 · 注释内任务编号 / 缺陷流水号 / 控制包章节 / 日期 / commit / 历史叙述（普查规模与典型位置）
**判定：确认（典型位置全部复现；规模数字需按下列口径订正）**
- 7/7 典型锚点原文逐行核对全部命中：`:2` `P1-001 (attempt 2): …（ARCH-P0-001 整改）`；`:16` `2026-09-20 订正 [V5 分片 5 / R-2]: 旧文 hp_drizzle_run 已作废`；`:3242` 块内含 `(P2, 2026-09-14)`＋commit 短 sha `d3af6ffa`（:3243）与 `9e09941a`（:3247）；`:8005` `「写进 (*man)」曾经等于…`；`:11007` `原实现 value_stride=sizeof(double)`；`:11405` `原实现读整数 doc["weight_mode"]∈{1,2}`；`:12468` `G3-12：原实现以整数 weight_mode…`。
- 行号锚确已漂移（"现状"里的一句可核）：`:3246` 注释写 `module_adapters.cpp:1995 sp.maxStars=2000`，实测第 1995 行是 `if (governed) {`；AUD403-01 所指 `:5503` 同样指向无关代码 ⇒ 锚失效为事实。
- 我的独立计数（`git grep`，注释行下界，非其"首类去重"口径）：C/C++〔排除 third_party〕`//` 内含日期 **133** 行；`eng/**/*.py` 的 `#`/docstring 内含日期 **77** 行 ⇒ 仅日期一类 ≥ **210** 行；同范围 `FIX-` 514 行、`RELEASE-0x` 154 行、历史叙事词（原实现/旧口径/作废/废止/留痕/已撤销）186 行；`module_adapters.cpp` 单文件脏注释行 **191**（成稿报 116）。
- 与成稿差异（口径，不推翻机制）：成稿全局 `DATE=170`、分目录 `eng DATE=34` 与我实测 `eng` 单独 ≥77 行不一致——其"注释区提取 + 首个匹配类去重"会让同一行被前一类吞掉，属**低估**且他们自己在"诚实边界"里已声明"计数是线索非已证"。分母：成稿称"868 个跟踪源"，我 `git ls-files 'lib/*.cpp' 'lib/*.h' 'lib/*.hpp'` 排除 third_party = **867**（±1，不构成缺陷）。
- 分层判据核对：本条判据（标准 04 §5）施加在 `lib/**`＋`eng/**` **代码注释**层，方向正确；其附表把 `实验/**` 证据指针与 SCI-/ALG- 锚列为"保留"，未把正式文档层的尺跨套到控制包任务书/实验台账（后者以任务编号为主键），未越界。
- 取证命令：`sed -n '2p;16p;3242p;8005p;11007p;11405p;12468p' lib/infrastructure/scheduler/src/module_adapters.cpp`、`git grep -nE '//.*20[0-9]{2}-[0-9]{2}-[0-9]{2}' -- '*.cpp' '*.h' '*.hpp' ':(exclude)lib/third_party' | wc -l`、`git grep -nE '(#|""").*20[0-9]{2}-…' -- 'eng/*.py' | wc -l`、`git ls-files … | wc -l`。
- 影响 D8 的处置：进任务书（清洗面成立、典型样本可直接引用）；但**成稿计数不得作为验收读数**——任务书里的规模应写成"确定命令 + 下界"（如"日期类 ≥210 行"），并注明首类去重口径会重复压低；同时把"删除失效行号锚（:1995/:5503 等）"列为可机器验的子项。

## AUD403-03 · 注释卫生门的覆盖面缺口（在册门窄于自述，eng/ Python 注释无门）
**判定：确认（机制全部复现；处方项需订正 2 处）**
- 门的输入集合来源（不看自述看取集处）：`check_comments.py:65-67` `srcs = (repo/"lib").rglob("*.cpp") + rglob("*.h") + rglob("*.hpp")`，随后过滤 `third_party`/`archive` ⇒ **集合起点写死在 `lib/`**，`eng/**/*.py`（实测 560 个跟踪 py）与 3 个 `.cu/.cuh` 结构上不可能进入，无需看自述即定。
- 文档串与实现不符：`:2-6` 自述"禁止过期审计轮次、旧版本宣称、代码复述、错误线程/单位"；但 `:9-13` 的 `STALE_PATTERNS`（含 `TODO.*fix|FIXME.*legacy`、`thread.*16.*hard.*code`）在全文 182 行内**除定义处零引用**（死表），`_scan()` 实际只产两型 finding：`COMMENT-STALE`（:75 硬字面 `V19R2`/`V19R3`）与 `COMMENT-MISSING-ID`（:82-90，作用域 `INVARIANT_SCOPE="lib/algorithms"`）。⇒ "代码复述/错误线程/单位"三条判据**零实现**，成立。
- 比成稿更弱的一处：`COMMENT-MISSING-ID` 的"附近有 ID"其实是**整文件级**共存判定——`REQUIRE_ID_NEAR` 关键词与 `_has_authority(text)` 都对全文（含代码标识符）匹配，只要文件任意处出现过 `SCI-`/`docs/science/` 即绿；成稿描述为"不变量注释需带 SCI-/ALG- 锚"偏乐观。
- 接线现状（按 §2.7 四查）：注册为真——`eng/ci/checks.json:2350` 步骤 `CON-COMMENTS`，command = `python3 eng/tools/quality/contracts/check_comments.py`（不带 `--repo`/`--self-test`，跑真仓库），挂在 `CHK-STALE-DOC`（:2321）下 ⇒ "已接线但覆盖面窄"定性正确。
- 处方需订正（成稿"改法"两处过时）：① `--self-test` **已存在**（:94-147 `_self_test()`，5 例正负例，与其退役脚本头注"5 例 OK"自述相符），问题是**注册命令里没有它** ⇒ CI 永不执行负例面；② 入库夹具 `eng/tools/quality/contracts/fixtures/check_comments/{invalid_stale_claim.cpp,invalid_wrong_thread.cpp,valid_min.cpp}` 经 `git grep` 在 `eng/` 全域**零引用**（夹具孤儿），成稿未登记这一条。
- 卫生脚本状态词核：`eng/tools/quality/check_comment_hygiene.py:3` 自宣"V19R2 一次性审计脚本（**已退役，非门禁**）"；`git grep -n check_comment_hygiene -- eng/ci/checks.json` **零命中** ⇒ "已退役＋零注册"成立（未跨轴串用）。其退役理由 1)"判据面已有在册门承接"仅对 `V19R2/V19R3` 字面这条成立，对 FIX-/RELEASE-/日期/commit 类**不成立**——这反而加强本条。欠账清单指向 `run/DEFECT-REPRO-01/COMMENT_HYGIENE_INVENTORY.md`：`git ls-files | grep '^run/'` 只有 `run/.gitkeep` ⇒ 该留痕确不在跟踪集，不可长期追溯，成立。
- 与成稿差异：机制、定级、范围一致；差异仅在处方（上面 ①②）与"附近有 ID"的实现强度；另"门现在真红还是真绿"因本阶段禁测未证（成稿亦在"诚实边界"自陈未跑），不影响覆盖面结论。
- 影响 D8 的处置：进任务书，但把处方改写为"扩 `_scan()` 取集面到 `eng/**`+`.cu`、把 `--self-test` 与入库夹具接进注册命令、新增 FIX/RELEASE/日期/commit 判据"，不得写成"补 --self-test"（已存在）。

## AUD403-04 · 版本与状态口径单一事实源（值一致；两处副本 + 违禁元信息块）
**判定：确认（两处子结论需订正：副本已被 P0 等值门护住；"4 处"计数偏 1）**
- 版本取值我独立核（`cat VERSION` + 逐文件 `git grep -oE '[0-9]+\.[0-9]+\.[0-9]+(-alpha\.[0-9]+)?'`）：`VERSION`=`0.1.0-alpha.1`；`CMakeLists.txt:15` `project(acsd VERSION 0.1.0 …)`＋`:12-14` 同步规则句＋`:55` `file(READ …/VERSION ASTROCS_BASE_VERSION)`；`docs/owner/RELEASE_STATUS.md` 命中 **3 处**（:58/:65/:74）；`docs/DOCUMENT_INDEX.yaml:38` `base_product_version: "0.1.0-alpha.1"`（其 `version: "2.0.0"` 是文档索引自身版本，另轴）；根 `README.md` **零**版本字面量且 `:107-108` 明写"产品版本唯一事实源 = 仓库根 VERSION"。⇒ 5 源取值一致成立、README 不复制字面量成立。
- 计数订正：成稿"现状"写"复制进 RELEASE_STATUS.md（**4 处**）"，其自己的对象行号只列 :58/65/74，实测亦为 3 行 3 处 ⇒ 4 为笔误。
- **"门实际比较的是什么"（本条关键订正）**：`eng/tools/check_version_consistency.py:3` "扫描范围内任何 X.Y.Z 字面量必须等于当前唯一版本源 VERSION 的基础号"，`:39` `SCAN_ROOTS = ["docs", "eng/contracts/schemas", "eng/tests"]`、`:42` `SCAN_FILES` 含 `README.md`/`VERSION`/`eng/build/build.sh`/`toolchain.ps1` ⇒ `docs/owner/RELEASE_STATUS.md` 与 `docs/DOCUMENT_INDEX.yaml` 的副本**在该 P0 门扫描面内**；注册为真：`eng/ci/checks.json:13037` `CHK-VERSION-CONSISTENCY-VER001`（＋`:13062` 其 `--self-test` 12 组）、`:8766` `VERSION-CONSISTENCY`→`:8775` `eng/ci/check_version.py`（其 `[2]/[3]` 规则比的是**数值**：根 VERSION == expected、project 三元组 == expected 去 `-alpha.N`）。⇒ 成稿"升版需手工同步（次级单源风险）"应改写为"副本受全库等值门约束：升版不同步即判红，**不构成静默漂移的第二事实源**"（该子句按现状定级应下调）。
- 违禁元信息块（事实面）：`docs/owner/RELEASE_STATUS.md:149-154` 实测原文含 `authoring_task: GOV-004 / authoring_owner: SA-GOV-01 / base_main_sha: caee3e67…（40hex）/ convergence_task: DOC-CONV-001 / convergence_run: Rmtxvlrtfa66eb7 (rev23) / convergence_base_sha: da3c4b4a…（40hex）`；`docs/DOCUMENT_INDEX.yaml` 头部注释区含日期与迁移流水（实测 `2026-09-21 ROOT-CONSOLIDATION …`）且 `doc_index:` 块含 `authoring_task: "DOC-403"`＋`base_main_sha: "d1ccfa1bed9c…"（40hex）` ⇒ 两份"唯一事实源"文档自身违反正式文档层写法判据，成立。
- 状态轴未串用（三处对读）：`README.md:99-101` "状态词表唯一口径 = ASTROCS_DESIGN.md §12.5（取值集合见该节，本文档不复制）"；`RELEASE_STATUS.md:8-11` "状态词清单的唯一登记处 = §12.5"；`DOCUMENT_INDEX.yaml:9-10` `status` = 文档活动分类 …"**不是** §12.5 交付状态阶梯" ⇒ 三条自洽，成稿"未见跨轴串用"复现。
- 与成稿差异：仅上述"副本风险"定性与"4 处"计数；主结论（单源成立＋两处违禁元信息块）无差异。元信息块整改主责已由其自陈归 AUD-101，与本批不重复立项。
- 影响 D8 的处置：进任务书——版本轴**不进**整改清单（取值一致、门已护，写了反而是伪任务）；违禁元信息块一项进，但按归属并入 AUD-101 的文档写法整治，不另立版本条目；同时把"次级单源风险"从负责人清单里删除（读数会误导）。

## AUD403-05 · 排障手册（常见问题）指向不存在的命令与禁用工具链
**判定：降级（机制成立，但"四条命令全部不存在"应改为"三条路径不存在＋一条目录写错"；处方有一条错）**
- 应为哪档：标题面成立（活动排障文档教用户跑不存在的东西＋禁用工具链），但**四锚点须分档**：①②③路径不存在＝缺陷；④`tools\astrocs_diagnose.py`＝**目录写错**（工具在跟踪集内），其处方"改指 `acsd doctor`"是**基于假前提的错处方**。
- 路径存在性我独立核（跟踪集为准）：`git ls-files | grep -cE '^lib/snr_estimator/'` = **0**、`^lib/healpix_db/` = **0**、`^lib/phase2/` = **0**（现存同族目录是 `lib/phase2_session/`，非 `lib/phase2/`）⇒ 前三条源目录确不存在。
- 但④不存在之说为假：`git ls-files | grep -i astrocs_diagnose` → **`eng/tools/astrocs_diagnose.py`**；该脚本 docstring `:6` 自带正本用法 `py -3.12 eng/tools/astrocs_diagnose.py <run_dir> [--json out.json]`；`docs/diagnostics/TROUBLESHOOTING.md:33` 亦写对为 `eng/tools/astrocs_diagnose.py`。⇒ 正解是补 `eng/` 前缀，**不是**换成 `acsd doctor`（`doctor` 确在命令树：`lib/infrastructure/cli/command_tree.h:109 {"doctor", true, {"--json","--run-manifest"}}`，但它不扫 run_dir 日志，二者功能不同轴，不得互换）。
- 禁面与绝对路径（全成立）：`docs/TROUBLESHOOTING.md:9` 原文 `$env:Path="C:\msys64\mingw64\bin;$env:Path"; .\eng/build/toolchain.ps1 check`；对照 `DEPENDENCIES.md:25`（Windows 正式 preset 之外的 MinGW/MSYS2 不属正式工具链面，禁面正本在 `eng/packaging/schemas/preset-contract.json` 的 `forbidden` 段）、`:82` `msys2_mingw: FORBIDDEN`、`:81/:87` `machine_absolute_path: FORBIDDEN` ⇒ "禁用工具链＋硬编码机器绝对路径"两项均实。
- "源目录内 .exe"与构建模型不符：目标名其实**仍活着**——`lib/algorithms/coverage/CMakeLists.txt:103 add_executable(phase2_synthetic_gate tests/synthetic_gate.cpp)` ⇒ 真实形态是"唯一根构建 → build 树产物"，文档把 exe 写成源目录内文件属形态错（非目标已死）。
- 文档身份核对（成稿未做，我补）：`docs/DOCUMENT_INDEX.yaml:112-115` 给 `docs/TROUBLESHOOTING.md` 登记 `status: ACTIVE_INFORMATIVE`，`:705-709` 给 `docs/diagnostics/TROUBLESHOOTING.md` 同样 ACTIVE ⇒ 本条**不是**归档件的旧话，且**同一域存在两份排障手册**（42 行 / 33 行，后者写对了诊断工具路径）——成稿的对象面只取了前者，未登记第二份的存在与二者分工，属覆盖缺口。
- 与成稿差异：机制方向一致；差异＝④的事实认定（存在 vs 不存在）与处方、缺"两份排障手册并存"这一面、"四条全废"应改"三废一错"。
- 影响 D8 的处置：进任务书（S2/P1 文档面，非 S1：无生产运行时触发路径，仅误导使用者）；处方改写为"前三条命令改指现行 `build/` 产物名与真实源目录；④补 `eng/tools/` 前缀并保留该工具；删除 msys64 PATH 建议改走 MSVC 运行库＋`toolchain.ps1 check`；同时登记 `docs/TROUBLESHOOTING.md` 与 `docs/diagnostics/TROUBLESHOOTING.md` 的职责分工或合并"。

## AUD403-06 · 开发者指南与模块 README 完整性门覆盖面单薄
**判定：确认（覆盖面与 5/26 全部复现；"目标名仍有效"一句漏核 1 项且实为不成立，引用计数不复核通过）**
- 门的输入集合（不看自述看取集处）：`eng/tools/check_module_readmes.py:15-25` `MODULES` 硬编码 **5** 条（`lib/algorithms/{star_detection,platesolve,photometry,noise_snr}/wrapper_phase1/README.md` + `lib/phase3_session/README.md`）；我按目录枚举：`git ls-files 'lib/algorithms/*/*' 'lib/infrastructure/*/*' | sed -E 's#(lib/[a-z]+/[^/]+)/.*#\1#' | sort -u` → **TOTAL_DIRS=26** ⇒ 成稿"5/26"精确复现。
- 无同址 README 的 5 个目录逐条复现（同一取集法，`git ls-files "$d/README*"` 为空即判）：`lib/algorithms/shared`、`lib/infrastructure/benchmark`、`lib/infrastructure/hips_browser`、`lib/infrastructure/observability`、`lib/infrastructure/pipeline` ⇒ 与成稿名单**完全一致，零多零少**。
- 门确在 CI 在册：`eng/ci/checks.json` 步骤 `"id": "MODULE-READMES"`（:481 区）→ command `python3 eng/tools/check_module_readmes.py`（:490）；判据体是"README 必须含 合同 ID/header/source/test 路径且文件存在"（`:3-4` 与 :58-62 循环）⇒ "完整性由门保证"这一层覆盖面单薄成立。
- 自述强度订正：成稿称门"覆盖面单薄"，我读全文发现 `:2` 泛称"每模块 L2 README 完整性校验"、但 `:5` 又老实枚举"检查模块: phase1/{stars,wcs,photometry,noise}, phase3_session" ⇒ 属**抬头过 claim、正文有边界**的形态（比 AUD403-03 那条"自述四条判据零实现"轻），任务书里两者不应同档。
- DEVELOPER_GUIDE 事实面：`sed -n '1,35p'` 实测 `## 环境与构建` 只两条 `cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release` + `ninja -C build`（Linux 形态，无 Windows preset 序列）；`grep -n "acsd" docs/DEVELOPER_GUIDE.md` → **零命中** ⇒ "无 acsd 运行样例"为事实。`:24-31` 矩阵确用旧模块标签（`snr_estimator:`/`healpix_drizzle:`/`phase2:`/`astro_image_io:`）。
- **漏核项（我判为实质缺陷）**：成稿"（目标名本身仍有效，已核）"只给了 `phase2_synthetic_gate`、`variance_propagation_test` 两个。我逐项查注册：`phase2_synthetic_gate` = `lib/algorithms/coverage/CMakeLists.txt:103` ✓；`variance_propagation_test` = `lib/algorithms/drizzle/healpix_drizzle/tests/CMakeLists.txt:159`＋`add_test(... :300)` ✓；`pipeline_frame_contract_test`/`dataflow_fuzz` 在 `eng/tests/unit/CMakeLists.txt`、`lib/infrastructure/aio/tests/CMakeLists.txt` ✓；**但 `noise_model_science_test` 在所有 `*CMakeLists.txt`/`*.cmake`/`eng/ci/checks.json` 里零命中**，只有源文件 `lib/algorithms/noise_snr/cpp/test/noise_model_science_test.cpp`（其 `:20` 注释还写着"编译: g++ -O2 …"的手工路线，g++/MSYS 正属 `DEPENDENCIES.md:82` 禁面）⇒ 矩阵该项按"在册可跑"口径**不成立**，成稿以偏全。
- 引用计数不复核通过（不影响结论方向）：成稿"phase2_synthetic_gate 143 处、variance_propagation_test 14 处"，我 `git grep -o <名> | wc -l` 实测 **52** 与 **35**（一个高报约 2.8 倍、一个低报约 2.5 倍）⇒ 该两个读数不得进任务书；"有效性"应以 `add_executable`/`add_test` 注册处为准（我已逐条给到 `文件:行`）。
- 与成稿差异：覆盖面＝无差异（逐字复现）；差异在（a）"目标名仍有效"的以偏全、（b）引用计数、（c）门自述强度分档。
- 影响 D8 的处置：进任务书——①`MODULE-READMES` 门的 `MODULES` 改动态枚举并与 `docs/modules/` 对齐；②DEVELOPER_GUIDE 补 Windows preset 构建与 `acsd` 运行样例；③新增一条：矩阵内 `noise_model_science_test` 先定"补注册进 CMake 还是从矩阵删除"，不得留在指南里当作可跑面；④引用计数改由注册处 `文件:行` 承担。

## AUD403-07 · 平台角色口径张力：Linux 是发行平台还是轻验证面
**判定：确认（张力真实，且"轻验证"一侧比我预期更硬：三处同侧登记 ⇒ 应上呈裁，不宜只改措辞）**
- 三处原文逐字复现：`README.md:41` "正式平台为 Windows x64（交付 `acsd.exe` 与各 `.dll`）与 Linux amd64（`acsd`），纯 CPU 生产"；`DEPENDENCIES.md:27` 标题 "## Linux 控制节点（轻验证；非发布产物）"；`DEPENDENCIES.md:38-39` "Linux preset `linux-control` 仅供静态检查/轻量编译/小合成实验；Linux 性能不作为 Windows 发布性能结论"；最高设计 `ASTROCS_DESIGN.md` §11"双平台发行与安装"表 `:706` "| Linux amd64 | `acsd` | 解压目录：唯一 ELF + 各 .so + schemas + manifest |" ⇒ 上位与 README 同侧、DEPENDENCIES 另侧，成稿引用无误。
- **成稿未登记的加重事实（我补）**：轻验证一侧不止 DEPENDENCIES 一处——`CMakePresets.json:69-71` 在建 preset `linux-control` 的 displayName 即 "ACSD Linux 控制节点轻验证 (非发布产物)"、description 重申"不是发布产物"；`docs/operations/TOOLCHAIN_AGENT_HOST.md:36` 亦登记 `linux-control`（generator `Unix Makefiles`，仅非 Windows 主机条件生效）。⇒ 这是 **1（§11 发行表）:3（构建面＋两份运维/依赖文档）** 的口径对立，不是一处笔误，改一个文件收不了口。
- 同单元远处文字已读（防"找不到理由=没理由"）：§11 表下方 `ASTROCS_DESIGN.md` 的流程图把 Linux 节点画在"开发·构建·单测·合成·真实数据终验"位，发布链为 "CI 双平台构建 → **Windows 正式复验** → 发布候选·负责人终审"，即上位设计自身也只在"交付形态"层并列两套产物、在验证链层只给 Windows 正式复验 ⇒ "发行平台"与"当前验证/产出面"确是两条轴，成稿"区分目标形态与当前实现状态"的读法有上位依据，不是它自创。
- 状态词轴核对：`README.md:105` 已把 `VERIFIED` 明确挂"正式平台（Windows x64）与真实数据验收"，即 README 一边称 Linux 为正式平台、一边把发布判据只写 Windows ——与 §11 双平台并列亦不完全一致（此点成稿未指出，属同一条张力的第三处表现）。
- 与成稿差异：无定级差异（成稿自评 PARTIAL、并声明不把"未开工"判为"已坏"，与我一致）；差异＝范围应扩到 `CMakePresets.json` displayName/description 与 `docs/operations/TOOLCHAIN_AGENT_HOST.md:36`（否则整改只改 DEPENDENCIES 文字，构建面仍自证"非发布产物"）。
- 影响 D8 的处置：进任务书但**须先上呈负责人裁定口径**（"Linux amd64 是否为发行平台"是产品方向，AGENTS §10 属必停项）：若裁定"发行平台、产物未产出"，则 DEPENDENCIES 标题/preset displayName/TOOLCHAIN 文档三处统一改"（当前控制节点用途；发布产物未产出）"，并在发布面登记 Linux 产物为未开工；若裁定"Linux 不发行"，则要改的是最高设计 §11 与 README:41（走变更流程），二者互斥不可并陈。

## AUD403-08 · 根 README 完整性判定：六要素齐备、链接零悬空（正向结论 + 轻微前瞻注记）
**判定：确认（正向结论我独立复算成立；其取证口径需换基，另补两处我核过的"无缺陷"结论）**
- 六要素逐项亲读复现：项目是什么 `README.md:1-4`；三个命令 `:26-42`（表在 `:34-38`，正本指 §8.1、`:30` 唯一 CLI 入口 `acsd`）；科学目标 `:44-62`（含 §2.1–§2.4 与 `docs/science/*` 指向）；构建与运行 `:64-84`；仓库布局 `:86-97`；文档往哪读 `:113-131` 索引表；版本口径 `:107-108` 指 `VERSION` 单源（与我 AUD403-04 实测一致：README 内**零**版本字面量）。
- 构建块与 `AGENTS.md` §3 逐条比对：`cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release` / `ninja -C build` / `ctest --test-dir build --output-on-failure` / `python3 eng/ci/run_checks.py` 四条**同形同序**；运行块四条 `acsd normalize|mosaic|export|help/--version/doctor/benchmark` 与命令树在册（`lib/infrastructure/cli/command_tree.h:109` 见 `{"doctor", true, {"--json","--run-manifest"}}`）⇒ "无运行样例"之忧不成立。
- 链接零悬空我以**更严口径独立复算**（成稿用的是 `[ -e ]` 磁盘存在性，不属"仓库事实"基线）：`grep -oE '`…\.(md|yaml|yml|json|py|sh|ps1|txt|in)`|(docs|eng|lib|实验|testdata|工程控制)/…' README.md` → 去重 **48** 条，逐条 `git ls-files -- "$p"` 判跟踪集 ⇒ 唯一未命中是 `lib/phase`，系 `:32` 的 `lib/phase{1,2,3}_session` 被我的花括号正则切碎所致（三个目录 `git ls-files` 全在跟踪集）⇒ **零悬空成立**，且成稿"45 个被引路径"与我的 48 属抽取口径差（它未含 `.yml/.ps1/.txt/.in` 与非 docs 前缀），不影响结论。
- 两处我主动怀疑后**核为无缺陷**（记入以免下游误立项）：①"三个创新点互为前提"却编号四条 —— 与最高设计同构：`ASTROCS_DESIGN.md:116` "三个创新点互为前提"、`:118` 明写 §2.4 "不属于上面三个科学创新点的链条…单列为创新点四" ⇒ README 忠实，非缺陷（但成稿"四创新点"措辞易被下游读成"四条同轴"，任务书宜沿用上位措辞）；②README 同时指 `docs/architecture/MODULE_MAP.md`（`:88` 模块索引权威）与 `docs/modules/MODULE_MAP.yaml`（`:104` 状态与证据锚）—— 查 `docs/DOCUMENT_INDEX.yaml:385-387` = ACTIVE_NORMATIVE "Module Map"、`:746-748` = ACTIVE_INFORMATIVE "模块一致性映射表（MOD-001/CHK-MODULE-MANIFEST）"，两文件两轴，**不是**双权威冲突。
- 与成稿差异：结论一致；差异只在取证基线（`-e` → `git ls-files`）与"45/48"抽取口径；其"轻微注记②"（`:31-32` 会话层目录与"归并后退役"目标相反）我核为事实（`lib/phase{1,2,3}_session` 均在跟踪集），且它已明确判为"描述现状非错误、留待架构整治"，与我一致。
- 影响 D8 的处置：**不进**整改清单（正向结论，无核心缺陷）；两处注记以"观察项"随包带走——README 层的文档链接门若将来立项，判据必须用跟踪集而非磁盘存在性；另建议把 `:41` 平台句挂到 AUD403-07 的裁决链上（同一条文字，两处文档，裁决后一次改齐）。

## AUD403-09 · 注释总体在讲"为什么"，真缺陷是"为什么"被过程流水号包裹（对任务书前提的校正）
**判定：确认（对任务书前提的校正成立，且我用独立量化把"复述成灾"证伪；两点须订正）**
- 样本独立精读：`lib/algorithms/integration/v6/include/astrocs/v6/weight_chain.h:34-49` 载的是非显然约束——"配对性只要求**同帧内** SNR 与 F_ref 同源，**不要求跨帧相等**"，并给出代数式与旧表述作废理由（跨指向时组标量反而破坏同源性、实测 6/6 帧被拒）；`lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:294-302` 给核按 drop 面积归一的依据（F&H 2002 §7.2 式(7) + drizzlepac `cdrizzlebox.c` `dover /= jaco`）与 `k_corr` 冻结常数域。⇒ "注释在讲为什么"为真，且是**可核的因果链**而非套话。
- 独立量化（我自跑，用来验"未见复述/空注释成灾"这句正向断言）：`git grep -nE '//\s*(TODO|FIXME|XXX|HACK)'` 于 `lib/**` C/C++（排除 third_party）= **1 行**；裸复述式注释（`^ *// *(返回|设置|获取|初始化|调用|赋值)…$` 短语句）= **29 行**，同期 `lib/**` 注释行总量 `^ *//` = **46,234 行** ⇒ 复述占比 ~0.06%、占位 TODO 面 1 行。"成灾"前提**证伪**，与成稿一致（例外的散点见下）。
- 包裹现象量化：最脏单文件 `module_adapters.cpp` 注释行 **3,088**，其中脏行（日期/流水号/§章/历史词并集）**191** ⇒ 约 6% 的注释行被过程叙事污染，"主体是 why、缺陷是被包裹"这一结构判断有数支撑（成稿只给定性）。
- 订正一（承 AUD403-01）：本条"真正与现行实现矛盾的仅定位到 **1 处**硬冲突"不成立——我另核出 `module_adapters.cpp:12543` `天光面未建（如 additive_mode=c 的默认路径）` 同属"注释断言与在册默认相反"，硬冲突面应为 **2 处**。
- 订正二：本条"抽样未见只复述代码的空注释"应加限定"散点存在"——我实测两处：`lib/algorithms/calibration/cpp/cosmetic_corrector.cpp:227`（`// sigma = 1.482602218505602 * MAD…` 与紧邻 `:228` 同义）与 `lib/algorithms/noise_snr/cpp/src/snr_estimator.cpp:588`（`// 初始化输出`）。量级不影响定级，但"未见"与"仅散点 29 行"是两种可核读数，任务书取后者。
- 与成稿差异：主轴结论与定级一致；差异＝硬冲突计数（1→2）与复述型注释从"未见"改"可计数散点"。
- 影响 D8 的处置：进负责人清单作为**对任务书前提的校正**（防止按"复述注释有多少"立项，找错靶子）；整改读数改为"以剥离过程叙事为主、以 `文件:行` 与 191/3088 这类可复算比为验收面"，并同步 AUD403-01 的 2 处硬冲突清单。

## AUD403-10 · 模块 README 质量：内容达标、夹带 owner 叙事与行号锚
**判定：确认（内容面我逐项核过为真；但成稿所给行号锚自身失准，且行号锚已不只是"该删"而是"已错"）**
- 内容达标（四要素我独立对读 `lib/infrastructure/scheduler/README.md:1-35`）：职责 `:3-6`（运行时内核 + 三 Phase 生产节点适配，唯一真实节点实现集中在 `src/module_adapters.cpp`，入口 `p1_op_*`/`p2_op_*`/`p3_op_*`）；边界条款 `:8-9`（"本 README 只登记编排层语义，不得反向改写合同"）；权威锚 `:13` `docs/contracts/DATA_SEMANTICS.md §18.5`（真存在：`docs/contracts/DATA_SEMANTICS.md:1031` 即 "### 18.5 初始指向来源"）；非显然约束 `:18-22`（`s0 = 206.265·XPIXSZ/FOCALLEN` 与 206264.806 的适用条件分野）、`:24-25`（`neighbor_crval` 必须是本管线自解产物）、`:30`（传入即 DATA 拒绝）。构建段 `:34-35` 我另核为真：跟踪集内**无** `lib/infrastructure/scheduler/CMakeLists.txt`，根 `CMakeLists.txt:372 add_library(astrocs_core STATIC`、`:1043 add_library(astrocs_module_adapters STATIC`。
- 违禁写法逐条复现（但**行号须订正**）：owner 叙事在 `:15`（"负责人裁定：**帧头 WCS 未授权**"，成稿 `:15` ✓）；常量行号锚实在 `:21`（成稿记 `:23`）；`审计登记（F-10）` 实在 `:27`（成稿记 `:30`）；`旧值 header_crval 已移除` 实在 `:30`（成稿记 `:32`）⇒ 成稿在"禁源码行号"这一条上自己给了偏 2–3 行的行号，属其判据的自反例，任务书引用一律改**文字锚**。
- 加重事实（成稿未察）：README `:21` 内嵌的源码锚 `ipv_select.cpp:57 IPV_ARCSEC_PER_UM_PER_MM=206.265` **已经指错**——常量的真身在 `lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp:58`（`:283/:1120/:1425` 使用）⇒ 该行号锚不是"风格上该换成符号名"，是"当下就少一行、会把人带偏"，实证等级更高。
- 新发现（跨层）：`F-10` 不是 README 夹带物，**合同正本抬头自带**——`docs/contracts/DATA_SEMANTICS.md:1031` 标题即含 "（wcs.init_source，P9 / F-10）"。⇒ 只清 README 会留下上游裸号；按该工包附表"合同/条款 ID 分情形"口径，`P9/F-10` 属工单裸号（应留 §18.5 路径形态），故整改面需含 `docs/contracts/`，但那属文档层主责（AUD-101），本条登记为归属提示。
- 与成稿差异：结论一致（达标 + 夹带）；差异＝四处行号读数、行号锚"已错"升级、F-10 上游同现。
- 影响 D8 的处置：进任务书（S2/文档写法面）：README 侧改三件——owner 裁定转正向条款、行号→符号名（`IPV_ARCSEC_PER_UM_PER_MM`）、`F-10`/历史句转现行拒绝条款；`docs/contracts/DATA_SEMANTICS.md:1031` 的裸号并交 AUD-101 统一处置，不在本模块整改里顺手改合同抬头。

<!-- PROGRESS: 10/10 -->

## 本批判定汇总（各态计数，附复算命令）
- `确认` **9** 条：AUD403-01 / -02 / -03 / -04 / -06 / -07 / -08 / -09 / -10（其中 -01 范围应扩、-02/-03/-04/-06/-09/-10 均带**读数或处方订正**，见各条"与成稿差异"）。
- `降级` **1** 条：AUD403-05（四锚点应分档为"三条路径不存在＋一条目录写错"，其处方之一基于假前提）。
- `推翻` 0 条 / `待证` 0 条 / `重复` 0 条。
- 全批复算命令基座（未跑任何 `eng/**` 检查器、未构建、未跑测试）：
  `git -c core.quotePath=false grep -n[E]? <pat> -- <pathspec>`、`git ls-files -- <pathspec>`、`sed -n '<a,b>p' <file>`、`git -C … status --porcelain`；
  关键分母：`lib/**` C/C++（排除 third_party）**867** 跟踪文件、注释行 **46,234**、`eng/**/*.py` **560**、`.cu/.cuh` **3**、`lib/{algorithms,infrastructure}/*/*` 顶层子模块目录 **26**。
- 收工 `git status --porcelain` 原文（与开工逐字一致 ⇒ 取证未脏被审对象）：
  ```
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```

## 旧判偏松清单（= 上一轮给下游的虚假授权）
1. **AUD403-05 ④**："`astrocs_diagnose.py` 全域不存在 ⇒ 改指 `acsd doctor`"。事实：工具在册且活着（`eng/tools/astrocs_diagnose.py`，docstring `:6` 自带正本用法；`docs/diagnostics/TROUBLESHOOTING.md:33` 写对了路径）。下游若照做，会**把一条正确指引换成错指引**并让在册工具失去文档入口。
2. **AUD403-06**："（模块测试矩阵的目标名本身仍有效，已核）"——以 2/5 概全：`noise_model_science_test` 只有源文件 `lib/algorithms/noise_snr/cpp/test/noise_model_science_test.cpp`，在 `*CMakeLists.txt`/`*.cmake`/`eng/ci/checks.json` **零命中**，其文件头还写着 `编译: g++ …`（MinGW 路线，属 `DEPENDENCIES.md:82` 禁面）。下游据此把该矩阵当"在册可跑面"写进任务书 = 虚假授权。
3. **AUD403-04**："副本升版需手工同步（次级单源风险）"。事实：P0 门 `CHK-VERSION-CONSISTENCY-VER001`（`eng/tools/check_version_consistency.py:39-42` 扫 `docs` 全树＋README＋VERSION）强制全库版本字面量等值 ⇒ 据此立项"消除版本副本"是**伪任务**。
4. **AUD403-03 处方**："对 `check_comments.py` 补 `--self-test` 正负例"。事实：`_self_test()` 已存在（:94-147，5 例）；真缺口是注册命令（`eng/ci/checks.json:2350` 区）不带 `--self-test` 且入库夹具三件零引用。据此开工 = 重复劳动并掩盖真缺口。
5. **AUD403-01 / -09 计数**："硬冲突仅 1 处"。事实：第二处 `module_adapters.cpp:12543`。按 1 处验收必漏改。
6. **读数类**（不得进验收面）：`DATE=170`（我按注释行下界 ≥210）、`eng DATE=34`（我 eng 单侧 ≥77）、`module_adapters 116 脏行`（我 191）、"副本 4 处"（实 3）、"phase2_synthetic_gate 143 处 / variance_propagation_test 14 处"（我 52 / 35）、README "45 被引路径"（我 48）、"868 跟踪源"（我 867）。

## 覆盖率自报
- 已判 **10 / 分配 10**，无"非空未判名单"。骨架先行落盘（本批 10 条判定行在取证开始前已在文件中），每条判完即时改写该行并更新 `PROGRESS`。
- 单条命令用量：01=5、02=7、03=5、04=5、05=6、06=8（含 1 条因 `git ls-files --quiet` 不支持而作废的命令）、07=4、08=3、09=6、10=3 —— 全部 ≤8，无一条转 `待证`。
- 未覆盖/未证事项（如实登记，不算已判内容的一部分）：
  ① 本批**未运行任何门**，故"门现在真红还是真绿"全批未证（AUD403-03/-04/-06 三条涉及处均按"源码与注册表现状"下判，不写运行结果）；
  ② AUD403-06 未展开核 `docs/modules/` 与 `MODULE_MAP` 的模块数对齐度（门若动态枚举，覆盖目标数应为多少未定）；
  ③ AUD403-08 的链接抽取用我自己的正则面（含 `.yml/.ps1/.txt/.in` 与 `实验/`、`工程控制/` 前缀），与成稿 45 条不同集；两法结论同为"零悬空"，但均未覆盖**无扩展名的裸目录引用**之外的行内锚（如 `§x.y` 条款号）——条款号级悬空本批未判。

## 我对第①层判据本身的校准建议
1. **判"不存在"要分三级**：路径不存在 / 符号不存在 / 工具在别处但引用写错。AUD403-05 把第三类写成第一类，导致处方错。建议派单里把这条列为硬判据："写'不存在'前必须 `git ls-files | grep -i <名>` 与 `git grep -l <名>` 各跑一次并贴输出"。
2. **判"仍有效"要给注册处**：`add_executable` / `add_test` / `checks.json` 的 `"id"` 之一，配 `文件:行`；**引用次数不作有效性证据**（成稿两处计数我复算不通过，且高计数也可能全在 md 叙述里）。
3. **计数一律带三要素**：确定命令、分母、是否"首类去重/仅注释区"。否则同一现象两读（本批 DATE 170 vs ≥210、脏行 116 vs 191）会被下游当矛盾而弃用全部读数。
4. **行号锚引用改文字锚**：成稿自身给的行号有 4 处偏 2–3 行（AUD403-10），且被审对象里的行号锚确已漂移（`:1995`/`:5503`/`ipv_select.cpp:57`）——在"行号锚违规"条目里再用行号引用，等于给自己埋雷。
5. **正向结论要配可复算的量化负例探针**：AUD403-08/-09 的正向判定，我分别用"跟踪集逐条 ls-files 命中 48/48"与"TODO=1 行、复述式 29/46,234 行"支撑；建议第①层把"正向结论"也写成命令＋读数，而不是"逐项判：有"。
6. **判门覆盖面看取集处与参数面**：`rglob` 起点、`MODULES` 硬编码列表、注册 command 的参数（是否 `--self-test`/`--repo`）三样，比门的 docstring 权威；本批 `check_comments.py:2` 与 `:5` 自述两档、`check_module_readmes.py:2` 泛称"每模块"而 `:5` 枚举 5 个，都属"抬头过 claim"，应按**强度分档**登记（前者四条判据零实现、后者边界已声明），不宜同档。
7. **分层判据**：本批未见跨层套用（成稿附表对 `实验/**` 指针与 SCI/ALG 锚判"保留"，方向正确）；风险提示是它的"以 v19r4 剥离面为下限扩规则"若落到实施层，会把 `docs/` 与 `工程控制/**`（以任务编号为主键）一并扫进违规面——建议实施任务书把清洗规则按 `lib/**`+`eng/**` 注释、`docs/**` 正文、控制包/实验台账三层分别登记判据。
