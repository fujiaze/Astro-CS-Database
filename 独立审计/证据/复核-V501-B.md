# 复核-V501-B｜第②层独立复核成稿（批次 V501-B）

## 文件头

- **复核对象**：`独立审计/证据/AUD-501-门禁现状审计.md`（全文 789 行）
- **本批条目**：12 条（A-17 至 A-28），清单 `独立审计/批次清单/V501-B.txt`；对象行段 `443-655`
- **不负责**：前段（A-01…A-16）与 A-03/04/05/08/10（派单已声明交叉引用仅供参照，不判其本身）
- **冻结基线**：HEAD=`c8f64e9a`
- **只读纪律遵守情况**：未修改/移动/删除仓库任何文件；git 仅用 `status`/`ls-files`/`grep`/`log`/`show`；**未执行任何门禁、`run_checks.py`（含任何参数）、任何 `eng/**` 脚本、未构建、未 ctest**；未 `import` 仓库内 Python；本文件只写本路径，未碰共享台账与 `独立审计包/`
- **开工 `git status --porcelain` 原文**：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（恰为上述两行，与前言 §1 期望一致）

- **派单与清单一致性**：一致（12 条，ID 与行段对应），无差异需登记。

## 逐条判定

## A-17 · Q5：SKIP 的实际使用面——`exit 77` 合同只在 waivable 单元被承认，`SKIPPED(platform)` 在 `run_checks.py` 侧不阻断 rc=0，ctest 侧 12 条登记在案 —— 判定：确认

- 我的独立取证（先于阅读第①层判定文字）：
  - `eng/ci/run_checks.py:104-138` 值域定义 + `FAIL_VERDICTS = (V_FAIL, V_TIMEOUT, V_PREREQ, V_SCOPE, V_MISSING_OUTPUT, V_EMPTY_OUTPUT, V_GATE_MISSING, V_CRASH, V_INPUTS_MISSING, V_INPUTS_EMPTY)` —— 三种 `SKIPPED*`（`:109-110`、`V_SKIP_OPTIONAL_INPUT`）与 `V_REUSED` **确实不在失败集合内**（逐字核对表体，非凭注释）。
  - `eng/ci/run_checks.py:1932` `failures = [s["id"] for s in step_results if s["verdict"] in FAIL_VERDICTS]` ⇒ `:1966` `verdict = "FAIL" if failures else "PASS"` ⇒ 全 skip 运行落 `verdict="PASS"`；`:1967-1968` 只在有 CRASH 时改判，SKIP 不改。**"平台不符整批跳过被记成 PASS 且 rc=0"就住在这条路径**（不是住在 step 级，而是住在聚合级）。
  - `eng/ci/run_checks.py:2056` `return EXIT_OK if verdict == "PASS" else EXIT_FAIL` —— rc 由同一个聚合 verdict 唯一决定，故 PASS ⇒ **rc=0** 成立（这是逐跳可推的静态闭环，不需要跑门禁）。
  - `eng/ci/run_checks.py:1994` 一带确有 `skipped_platform` 计数面（读 `:1927-1931` 计数初始化含 `V_SKIP_PLATFORM`/`V_SKIP_WAIVABLE`/`V_SKIP_OPTIONAL_INPUT` 三档），即"可见但不裁决"。
  - `eng/ci/run_checks.py:732-744` `exit 77` 分支：waivable ⇒ `V_SKIP_WAIVABLE`；非 waivable ⇒ `V_FAIL`（"非 waivable 执行单元以 SKIP 退出码 77 结束"）—— 与第①层"只在 waivable 单元被承认"的表述一致。
  - 对照入口 `eng/ci/run.py:1236-1266`：`executed` 先剔除 `SKIP_VERDICTS`，随后 `:1256-1263` `elif not executed and skipped_executed: verdict = "FAIL"`（理由原文"零有效执行，不允许空集 PASS"）、`:1264-1266` 再堵 `no_checks_selected`。**两入口对同一情形判定相反**，此即不对称面。
- 与成稿差异：无（第①层给的 `:95-99`/`:136-138`/`:1994`/`:1909-1911` 行锚与我的读数一致；仅"`verdict = "FAIL" if failures else "PASS"`"它未给行号，实际在 `run_checks.py:1966`，rc 行在 `:2056`）。
- **守卫在外层、洞在被它嵌套的内层——本条实测形态**：外层自称"零有效执行不许算通过"的是 `run.py:1256-1263`（S1 级聚合守卫）；洞在 `run_checks.py:1966`+`:2056` 这条同样承担聚合的入口。`eng/ci/checks.json` 中 `eng/ci/run_checks.py` 以字符串出现在 20 个单元的数组字段里（`:13, 136, 221, 275, 413, 734, 811, 1085, 1621, 1908, 2085, 2186, 2330, 2387, 2542, 3656, 4315, 6063, 6607, 6743`），即大量在册单元的判定实际由**有洞那一侧**产出；"哪一条 P0 判据强制聚合必须经 `run_checks.py`"这一跳我只数出引用面、未在 8 条命令上限内定位到具体 P0 单元 ID（该子句属 A-08，不由本批复判）。
- 影响 D8 的处置：进任务书（S1/P0 聚合口径统一 + `zero_skip` 机器面）；上呈前需补"P0 强制经 `run_checks.py` 派发"的具体单元 ID 一跳。

## A-18 · 崩溃与判红分离面在两个 runner 都成立（正例，保留）；缺口是「显式声明崩」无注册表侧承载 —— 判定：确认（第三处引用不精确，见差异）

- 我的独立取证：
  - 退出码族逐字核对：`eng/ci/run_checks.py:86-93`（`EXIT_OK=0 / EXIT_FAIL=1 / EXIT_RUNNER_ERROR=2 / EXIT_CRASH=3`）、`eng/ci/run.py:48-54`（同四值，注释另标明 2 = "runner 自身配置/环境错误"）—— **两侧一致成立**。
  - `eng/ci/gate_trust.py:35-39` 实为 `EXIT_PASS=0 / EXIT_FAIL=1 / EXIT_RUNNER_ERROR=2 / **EXIT_TRUST=3**`：该文件**没有** `EXIT_CRASH`。值 3 在 gate_trust 侧语义是"门自述不可信"，在 runner 侧是"崩"，**同码异义**。第①层把 `gate_trust.py:36-39` 列为"三个退出码族两侧一致"的证据 ⇒ 引用不精确（它只能证明码族前 3 档一致，证不了 `EXIT_CRASH=3` 的一致性）。
  - 崩溃≠判红的分离面（聚合级）：`eng/ci/run_checks.py:1890` `bad = [... in FAIL_VERDICTS]` → `:1897` `if any(s["verdict"] == V_CRASH ...): verdict = V_CRASH` 排在 TIMEOUT/SCOPE/FAIL **之前**（崩溃优先），`:1899-1907` 保留具体成因；`:1893-1895` 原文注释即"塌平会让「红从哪来」在条目级不可见"（第①层引的 `:1892-1896` 行段与此重合）。`V_CRASH` 亦在 `FAIL_VERDICTS` 内（`:136-138`），故崩既判红又**不塌平成红**，且 `:1967-1968` 把总 verdict 改写成 `CRASH` → `:2056` rc=`EXIT_FAIL`… 注意：崩在顶层被写成 `verdict=V_CRASH` 而非 `"FAIL"`，rc 走 `:2056` 的 else 支 = 1；`EXIT_CRASH=3` 用于 `:2055`（runner 自身崩）。
  - `eng/ci/run.py:1056-1064` 同族守卫（注释原文"崩溃优先（GATE-TRIAGE-01）：没有 verdict 的执行单元不能记成「判红」"）⇒ **分离面在两个 runner 都成立**。
  - 「显式声明崩」承载面：`git grep -in "crash" -- eng/ci/checks.json eng/ci/checks.schema.json` ⇒ **0 命中**；`git grep -il crash -- eng/contracts` ⇒ 0 命中。正控同机跑通：`git grep -c "waivable" -- eng/ci/checks.json` = 439 命中，`git ls-files "*checks.schema.json"` 确认 schema 实际在 `eng/ci/checks.schema.json`（第①层与派单引用的 `checks.schema.json:121` 未带目录，路径唯一）。⇒ **注册表/schema 确无"声明崩"字段**。
- 与成稿差异：仅证据引用一处——`gate_trust.py:36-39` 不能作 `EXIT_CRASH=3` 一致性证据（同码异义）；机制、定级（正例保留 + 缺口在承载面）、范围均无差异。
- 影响 D8 的处置：进任务书（正例保留；承载面缺口并入 A-17 的聚合口径条目）；`gate_trust` 码 3 的异义建议单独登记为文档/命名订正，不升 S1。

<!-- PROGRESS: 2/12 -->

## A-19 · 派发面分裂：`fatduck.yml` 在注册表之外直接执行决策脚本 `eng/ci/select_candidate.py` —— 判定：确认（须附范围注记，不可写成"无机器校验"）

- 我的独立取证（CI 工作流 → 决策脚本逐跳）：
  - 跳 1 `.github/workflows/fatduck.yml:45` `steps:` → `:51` `- name: Select candidate via GitHub API` → `:52` `id: sel` → `:56` `run: |` → `:57` `set -euo pipefail` → `:59` `set +e` → **`:60` `python3 eng/ci/select_candidate.py \`**（`:61-66` 参数面 `--event/--head-branch/--conclusion/--run-id/--json/--log`）→ `:67` `rc=$?` → **`:70` `if [ "$rc" -eq 3 ]; then`** → `:74` `::error::no-candidate…` → `:75` `exit 3`；`:76-77` `elif [ "$rc" -ne 0 ]; then exit "$rc"`。⇒ 一条能决定"Windows 候选是否成立"的判据以**工作流裸 shell 分支**形态执行，绕开注册表。
  - 跳 2 注册表面：`git grep -n "select_candidate" -- eng/ci/checks.json` ⇒ **rc=1（0 命中）**，故无登记 timeout、无 `outputs` 证据、无 `waivable` 语义。正控同命令族跑通：`git grep -c "select_profile" -- eng/ci/workflow_binding.json` = 1 命中（同机同法可见），排除"grep 失灵"假阴。
  - 跳 3 普查覆盖：`eng/ci/failclosed_survey.py:57-66` `def units(registry)` 只遍历 `registry.get("checks", [])`（含 `steps` 展开），**不含任何 workflow 文件**；`git grep -n "workflows\|\.yml" -- eng/ci/failclosed_survey.py` ⇒ **0 命中** ⇒ 该判据确实不被 `CHK-FAILCLOSED-SURVEY` 三面普查覆盖。
  - 跳 4 规则承载：`eng/ci/validate_workflow_binding.py:46` B16 原文「范围外 workflow 文件（如 fatduck.yml）逐条进 notices，但仍参与 B12 反复制扫描」⇒ 范围外只进 notices（不计错）；`.github/workflows/ci-linux.yml:3-6` 自订"本文件 run: 体只允许三类调用（`eng/ci/run.py`／`eng/ci/wf_step.py`／`eng/ci/bootstrap.py` 与 `eng/ci/select_profile.py`）"，该白名单**只约束 ci-linux.yml**，对 fatduck.yml 无机器承载。
- **范围注记（必须与本条同读，否则下游会得到虚假授权）**：`select_candidate` 在全跟踪集的命中面为 `.github/workflows/fatduck.yml`、`eng/ci/select_candidate.py`、`eng/ci/tests/test_fatduck_workflow.py`、`eng/ci/tests/test_ci001_failclosed.py`、`eng/ci/ledgers/naming_surface.json`、`reports/v19r2/*.csv`。其中两条测试**在册**：`eng/ci/checks.json:12484`（`-p test_fatduck_workflow.py`，timeout 60、platform any）与 `:12670`（`-p test_ci001_failclosed.py`，profiles 含 `integration`/`linux-main`）。且它们真断言该分支：`eng/ci/tests/test_ci001_failclosed.py:555-561`（正则 `if \[ "\$rc" -eq 3 \]; then(.*?)(fi\b)` + `assertIn("exit 3", …)`）、`eng/ci/tests/test_fatduck_workflow.py:21-23,65`（进程内 mock gh_api 跑 `select_candidate` 的 exit-3 逻辑）。⇒ **缺的是"派发面登记"（timeout/outputs/waivable/分级表/普查），不是"该判据行为无任何在册校验"**；本条不得被改写为后者。
- 与成稿差异：定级与机制无差异；范围上我给出一条成稿未提的反向事实（两处在册单测覆盖 rc=3 面），供 D8 落笔时不越界。
- 影响 D8 的处置：进任务书（注册为 `fatduck` profile 单元或把 B16 改"范围内且必须注册"）；处方不变。

<!-- PROGRESS: 3/12 -->

## A-20 · 死件三件套：`polarity_probe.py` / `reconcile_state.py` / `verify_ciqa_report.py` 无任何调用方，但前者仍是在册证据的署名生成者 —— 判定：确认（逐档引用面已数）

- 我的独立取证（每档单独数引用面，命令：`git grep -ln "<符号>"` 全跟踪集 + `git grep -n "<符号>" -- "*.py" "*.yml" "*.json" "*.sh" "*.cmake" CMakeLists.txt` 取调用点形态）：
  - **`polarity_probe`**：命中 6 档 = `docs/DOCUMENT_INDEX.yaml`、`eng/ci/ledgers/registration_anchor_ledger.json`、`eng/ci/polarity_evidence.json`、自身、`reports/v19r2/{file_audit_inventory,source_manifest}.csv`。代码/配置类命中仅 3 行且全是**自述/挂账**：`registration_anchor_ledger.json:77`（owner「polarity_probe 登记册…生成」）、`:78`（exit_condition 原文「polarity_probe 落点改到持久面…删除本条目」）、`polarity_evidence.json:4`（`"generated_by": "eng/ci/polarity_probe.py"`）。⇒ **零调用点、但仍是证据署名生成者**，且挂账条目本身把它保活在登记册里。
  - **`reconcile_state`**：命中 5 档 = `artifacts/evidence/governance-01/research/R-6_…md`、`artifacts/evidence/review-package/04_设计大纲综述/TIMELINE.md`、自身、`reports/v19r2/*.csv` 两份；`*.py/*.yml/*.json/*.sh/*.cmake/CMakeLists.txt` 内**0 命中** ⇒ 无调用方。
  - **`verify_ciqa_report`**：命中 3 档 = 自身 + `reports/v19r2/*.csv` 两份；调用点面 **0 命中**。
  - `eng/ci/polarity_probe.py:193-197` `def run(argv, cwd, timeout=900): return subprocess.run(argv, cwd=cwd, capture_output=True, text=True, timeout=timeout).returncode`（`:195-196` 实读无 `encoding=` 参数）⇒ 它确实是"探针自己跑门"的普查驱动形态，与第①层所引 `:195` 一致。
  - 证据消费面（判"在册证据"这一子句）：`git grep -n polarity_evidence` 命中 `artifacts/evidence/governance-01/research/R-6_…md:5,8,90,400,641`（含以 md5/sha256 钉住该文件、并引用其 `gates_total 31 / proven 5 / not_proven 19 / inconclusive 7` 计数）、`docs/DOCUMENT_INDEX.yaml:111,1448`（列为 downstream）、`eng/tests/quality/test_env_adoption_negative.py:5`、`registration_anchor_ledger.json:61`；而 `git grep -c polarity_evidence -- eng/ci/checks.json docs/ci/03_GATES.md` ⇒ **No matches** ⇒ 证据被裁决面引用、却无在册生产者，**不可再生**。
- **假阳/假阴控制（前言 §2.4）**：判"polarity 未在册"若用宽词会翻车——`git grep -in polarity -- eng/ci/checks.json` 实有命中：`:13725,13735,13742,13755,13763,13770,13778` 的 `SPEC-POLARITY-CONSIST-01`（脚本是 `eng/tools/doccheck/check_spec_polarity_consistency.py`，与本条三件套**无关**）。本批判定以 `polarity_probe` 全名为准，不与此门混淆。
- 与成稿差异：无（含其对 `:195`／ledger `:77-78`／`polarity_evidence.json:4` 的行锚）。补一条成稿未写的支撑事实：`polarity_evidence.json` 被 R-6 裁决面以哈希钉住 ⇒ "不可再生"的后果比"证据自述"更重（下游据此复算过计数）。
- 影响 D8 的处置：进任务书（三选一：改造入 `mutation_gates` 体系 / 按退役契约留痕 / 删除并撤证据面）；`polarity_evidence.json` 的哈希锚面需与处置同步撤下，否则留下悬空引用。

<!-- PROGRESS: 4/12 -->

## A-21 · CTest 注册面配对：236 个可静态枚举 `add_test` 目标中 235 个落在闭包内；1 个例外暴露「命令侧正则 / 登记侧 glob」两种方言无门调和 —— 判定：降级（机制成立、计数与"例外"定性不成立，应为 S2 机制条目）

- 我的独立取证（复算脚本 `独立审计/复算件/V501-B/a21_pair.py`，只读跟踪集，不 import 仓库 Python；命令：`python a21_pair.py`）：
  - **静态枚举面（我重数）**：`eng/tests/**/CMakeLists.txt` 的 `add_test(NAME <名>` 共 **238 条捕获 / 237 distinct**；其中 **6 条名字含生成器表达式**（`v6_p1_psfw_${g}`、`v6_p2_samp_${case}`、`v6_p2_sky_${case}`×2、`v6_p2_sky_node_adapt_${case}` 等）⇒ 完全静态可枚的名只有 **232**；`NAME` 后换行 0 条；跨文件重名 1 组（`v6_p2_sky_${case}`）。旁控：`git grep -E "add_test\(NAME" -- eng/tests | wc -l` = 239 行（与我 238 条捕获差 1 行，未定位，不影响方向）。
  - ⇒ 第①层的"236 个可静态枚举＋**0 处生成器表达式**"**两处不成立**：总数对不上，且"0 处生成器表达式"与 6 条实测直接矛盾（这 6 条恰恰是"无法静态证明已注册"的那一类，门文档 `check_ctest_registration.py:11-13` C1 自己就把它列为判据对象）。
  - **登记面**：`eng/ci/checks.json` 的 `ctest_targets` = **280 条 / 145 distinct**（与成稿一致）；`eng/ci/ctest_baseline.json` `targets` = **161 条**（一致，顶层键 `schema_version/purpose/base_commit/generated_utc/sources/targets`）。
  - **闭包（并集 fnmatch，我重算）**：**238 / 238 全覆盖**，未覆盖清单为空 ⇒ **"1 个例外"在我的复算里不存在**。这与成稿自身的表述互相矛盾：它一边把 `drizzle_pf_sb_v6_area_invalid` 列为"唯一未被匹配者"，一边承认 `eng/ci/checks.json` 的登记模式 `drizzle_pf_sb_*` 在 fnmatch 下**可以**覆盖它（`git grep` 确认该模式唯一：登记侧 drizzle 系只有 `drizzle_pf_sb_*` 一条）。
  - **枚举范围本身是个更大的洞（成稿未涉及）**：`add_test` 不止住在 `eng/tests`。跟踪集里 `lib/**` 另有 **145 条** `add_test(NAME`（`git grep -h -E "add_test\(NAME" -- lib | wc -l`），逐目录如 `lib/algorithms/noise_snr/tests/p1noise`(23)、`lib/algorithms/psf/tests/p1psf`(13)、`lib/algorithms/platesolve/cpp/ipv/test`(11)、`lib/algorithms/star_detection/tests/p1star`(11)、`lib/infrastructure/pipeline/orchestrator/cpp/tests`(8)、`lib/phase1_session/tests/p1sess`(5) 等 14+ 个 CMakeLists。门文档 `eng/tools/quality/check_ctest_registration.py:9-11`（C1）自陈扫描面是"活动 CTest 面**全部** CMake 源（`CMakeLists.txt` 或 `*.cmake`）"，排除 `run/ build/ out/ artifacts/ .git/ third_party/` 与 archive/superseded ⇒ **第①层的"可静态枚举 236"只覆盖了约 62% 的活动 CTest 面**，其闭包结论只在子集内成立。
  - **方言分裂机制（成立，且我用比成稿更强的方式证到）**：同一串 `drizzle_pf_sb.*` 按正则展开命中 add_test 名 **1 条**（`drizzle_pf_sb_v6_area_invalid`），按 glob 解释命中 **0 条**（`.` 被当字面量）—— 由 `python a21_pair.py` 第 5 段实测打印。命令侧组装跳：`eng/tools/quality/deep_ci_driver.py:356` `_ctest_argv(["ctest", "-R", "^%s$" % target, …])`（`-R` 是正则语义）；登记侧语义跳：门文档 `check_ctest_registration.py:14-15`（C2 "精确名或 glob"）与 `:20-21`（**C6 只对"无 glob 字符的 ctest_targets 名"要求出现在 command 参数里**）。⇒ 含 glob 的登记模式被 C6 **明确豁免** ⇒ 登记侧 `drizzle_pf_sb_*`（glob）与命令侧 `drizzle_pf_sb.*`（regex）各说各话时 **C3/C4/C6 三者皆无感**，此即"两种方言无门调和"，成立。
  - **附带新发现（待证 1 跳）**：登记模式 `p1snr_science_*`（含 glob）在 `eng/tests` 面 **0 命中**，且**不在** `ctest_baseline.json` 的 161 条里 ⇒ 按 C4（`:18` "匹配不到任何现存目标 → FAIL"）若门的扫描面不含 `lib/**` 对应目标，该条应判红。缺的那一跳 = `p1snr_science_*` 是否命中 `lib/**` 侧某 `add_test` 名（命令：`git grep -h -o -E "add_test\(NAME +p1snr_science[^ ]*" -- lib`）；本条取证已到 8 条上限，按 §4.3 记 `待证`，**不写成结论**。
  - baseline 侧静态差集（C5 近似）：`ctest_baseline.json` 的 161 条里有 **64 条**在 `eng/tests` 的 add_test 面找不到（如 `acr_sanitizer_msvc`、`browser_backend`、`geometry_truth`、`p1cal_*` 系）⇒ 与"活动面含 lib/**"一致，多数应由 lib 侧目标解释，非陈旧；不据此判红。
- 与成稿差异：**计数**（236/235/1 → 我 238/238 覆盖、232 完全静态、6 生成器表达式）；**范围**（枚举漏 `lib/**` 145 条，实际活动 CTest 面 ≈383）；**"唯一例外"的定性**（自相矛盾，不成立）；**机制**（方言分裂 + C6 豁免）一致成立；**处方**（增 C7 做两侧集合等价校验）一致成立。⇒ 定级应为 **S2（一致性/门强度缺陷，非当前假绿）**：今天两侧恰好覆盖同一批目标，分叉只在将来出现新命名时发生，缺运行时触发路径。
- 影响 D8 的处置：进任务书，但**必须改写为**"C6 豁免含 glob 模式 ⇒ 登记/命令两方言无调和（机制）+ 审计枚举范围须按门自身口径覆盖 `lib/**`"；236/235/1 三个数不得进负责人清单（我的复算不能重现）。附带 `p1snr_science_*` 的 C4 疑点单列待证。

<!-- PROGRESS: 5/12 -->

## A-22 · 【反证 + 保留】ctest 族的「零命中即绿」已按 GAP-027 机器化堵死，且判据是非退化的 —— 判定：确认（反证已复算；覆盖面应为 111 条而非成稿的 46 条）

- 我的独立取证（反证复算，不引成稿结论）：
  - 守卫在两处都有：`eng/tools/quality/deep_ci_driver.py:324-334`（`cmd_ctest_full`：`:326` `matched, probe_reason = _ctest_match_count(build_dir, ".", CTEST_PROBE_TIMEOUT)` → `:327` `zero_hit = matched == 0` → `:328-329` `if zero_hit and rc == 0: rc = 1`）与 `:349-369`（`cmd_ctest_target`：`:352` 预检、`:361-363` 同一改写、`:364-368` 打印 `matched_tests`/`reason`/`verdict`）。原缺陷由 `:344-348` docstring 逐字记录：「构建树未 configure 或目标未注册时 ctest -R 打印 "No tests were found!!!" 且 **rc=0**，本驱动据此静默 PASS」。
  - **非退化性我逐跳验到实现体**（不是只看注释）：`_ctest_match_count` 在 `:262-290`，
    - `:274-278` 预检本身抛 `OSError`/`TimeoutExpired` ⇒ `return 0, "ctest -N 预检无法执行…（fail-closed）"` ⇒ **探针失败也判红**（不是退回 rc=0）；
    - `:279-286` 只在输出里找到 `Total Tests:` 且取到数字才令 `matched>0`；标签缺失/无数字 ⇒ `matched` 保持 0 ⇒ 判红（`:287-289`）；
    - `:270-273` 哨兵修正面：`target in (".", "", ".*")` 走不带 `-R` 的 `ctest -N`，注释自陈历史缺陷「哨兵被套成 `^.$`，只匹配单字符测试名 ⇒ 全量门恒 0 命中假红」⇒ 该族曾有两处退化，现都朝 fail-closed 方向。
    - 结论：PASS 需 `matched>0` **且** ctest rc=0 ⇒ 门空转必红、真绿需证据 ⇒ **判据非退化成立**。
  - **覆盖面我重数（与成稿不同）**：`git grep -h -o -E "\"id\": \"CTEST[^\"]*\"" -- eng/ci/checks.json | sort -u | wc -l` = **111** 个在册 `CTEST*` 单元。成稿写"46 条 CTEST-<TARGET> 门 + CTEST-LINUX-FULL"，那 46 是 `deep_ci_driver.py:347` **docstring 里的历史陈述**，不是注册表现值 ⇒ 引用来源要改，且守卫覆盖面比成稿报的更大（这条正例因此更有保留价值）。
  - **可执行负例面缺失（成稿未提）**：`git grep -n "zero_hit\|GAP-027\|No tests were found" -- eng/tools/quality/deep_ci_driver.py eng/tests eng/ci/tests` 的命中全在驱动自身注释与一处**无关**门（`eng/tests/quality/test_doc_machine_check.py:130`，讲的是命令树门退化为 0 检查）。⇒ 没有任何测试/门在"零命中 ⇒ 红"这条分支上跑负例；该守卫只能靠静态阅读或在未 configure 的树上实测（本阶段禁跑）来复核。
- 与成稿差异：数字（46 → 111，且来源应从 docstring 改为注册表实数）；补一条"负例无承载"的弱点；机制、定级（正例保留）、处方（作为其余单元的守卫模板）均无差异。
- 影响 D8 的处置：进任务书（保留 + 推广为模板）；"覆盖面"数以 111 落笔；负例承载建议与 A-20 的 `polarity_probe` 改造合流（逐门真跑命令的判别力普查正好补这一层）。

## A-23 · 性能门是真实判红（非"只记录不裁决"），且自带可执行负例面 —— 判定：确认

- 我的独立取证：
  - 两态断言（逐字读 `eng/tools/quality/check_resource_gate_real.py:57-93`）：`:73` `cases = [("serial", "fail", 10)] if args.fault-inject else [("busy", "pass", 0), ("serial", "fail", 10)]`（原文键名 `args.fault_inject`）；循环体 `:74-83` 对每态**同时**要求 `rc != want_rc` 与 `gate.get("verdict") != want_verdict` 各自进 `failures`；`:85-89` 有 failures 即打印 `RESOURCE_GATE_REAL_FAIL` 并 `return 1`；全对才 `RESOURCE_GATE_REAL_PASS` + `return 0` ⇒ **"记录不裁决"不成立**。
  - fail-closed：`:66-69` 依赖文件缺失 ⇒ `RESOURCE_GATE_REAL_FAIL: 依赖缺失 …（fail-closed）` + `return 2`（不当绿）。
  - 可执行负例面（自带）：CLI `--fault-inject serial`（`ap.add_argument("--fault-inject", choices=("serial",)…, help="只跑负例注入形态（期望门判红）"`）与 `--self-test`（默认正+负两态）⇒ 负例有独立可寻址入口。
  - 在册与形态：`eng/ci/checks.json:9291` `RESOURCE-GATE-REAL`（profiles `integration`+`linux-main`、platform `linux`、command 跑 `eng/ci/resource_monitor.py`、`outputs: ["run/ci/resource-gate-real/resource_gate_real.json"]`、`waivable: false`、`requires_monitor: true`、`changed_paths` 含 `eng/contracts/resource_gate_v1.json`）；`:9332` `RESOURCE-GATE-REAL-NEG`（command `check_resource_gate_real.py --fault-inject serial --seconds 20`、`timeout_seconds: 600`、`waivable: false`）；`:10493` `L2-FROZEN-GATE-SELFTEST`、`:10518` `L2-FROZEN-GATE-REPLAY` 亦在册 ⇒ 成稿点名的四条都在册。
  - 裁决单一实现点：`eng/ci/l2_frozen_gate.py:140` `def adjudicate(evidence, *, thresholds, …)`；共用方 `eng/ci/check_frozen_gate.py:36` `import l2_frozen_gate as L`（注释"同目录：判定核心，单一实现点"）与 `eng/ci/monitor_evidence.py:27` `import l2_frozen_gate as _l2`（其 docstring `:8`「阈值唯一数值源 `eng/contracts/resource_gate_v1.json`（经 l2_frozen_gate 读取）」）；`git ls-files "*resource_gate_v1.json"` ⇒ `eng/contracts/resource_gate_v1.json` **在跟踪集**。
  - 回放面的反恒真门（逐字）：`eng/ci/check_frozen_gate.py` replay 内 `if hist == "pass" and verdict["verdict"] != L.V_RED: failures.append(f"{rel}: 历史 pass 且违规证据未被判红（恒真门残留）")`，另加 `if hist == "pass" and not verdict.get("violations")` 一条；语料为空/阈值合同不可用 ⇒ `FROZEN_GATE_REPLAY_FAIL … fail-closed` + `EXIT_UNAVAILABLE` ⇒ **回放门自身也是非退化 + fail-closed**。
  - 缺口（成稿末句）复算：`sed -n '9291,9352p' eng/ci/checks.json | grep -c "\"inputs\"\|\"optional_inputs\""` = **0** ⇒ 这两条资源门**未声明输入面**，靠实现内部守卫，注册表面看不到（与成稿"唯一缺口同 A-14"一致）。
- 与成稿差异：无。补两条支撑细节：`RESOURCE-GATE-REAL-NEG` 的 `outputs` 为 `[]` 且 `waivable=false`（负例门无证据落盘面，属同一缺口）；`--self-test` 与默认行为等价（默认即两态）。
- 影响 D8 的处置：不进整改（正例保留）；仅把"补 `inputs` 声明"并入 A-14/A-08 的输入面条目。

<!-- PROGRESS: 7/12 -->

## A-24 · 派发集中度：31% 的执行单元共用同一个驱动脚本，单点缺陷即批量假绿 —— 判定：确认（集中度比成稿更极端：同一 subcommand）

- 我的独立取证（自己展平单元并计数，命令：`python -c` 展平 `eng/ci/checks.json`，展平口径与 `eng/ci/failclosed_survey.py:57-66` `units()` 相同：有 `steps` 的按 step 计，否则按 check 计）：
  - 单元总数 **370**（150 个 check 展开成 370 个执行单元）；入口点分布（取 `command[1]`，`command[0]` 为 python 时）：
    `eng/tools/quality/deep_ci_driver.py` **115（31.1%）**、`-B`（即 `python3 -B -m unittest discover …` 形态）**45（12.2%）**、`eng/ci/resource_monitor.py` **16（4.3%）**、其后 `check_v6_runtime_closure.py` 4、`check_conclusion_truth.py` 3、`check_isa_same_source.py` 3、`check_budget_single_source.py` 3、`eng/ci/wf_step.py` **3**。⇒ 成稿的 115 / 16 / 3 与"其余各 1-4 个"**全部复现**。
  - **集中度再加一跳（成稿未数）**：这 115 条的驱动 subcommand 分布 = **`ctest-target` 115 / 115**（零其它子命令）⇒ 不是"共用一个脚本"而是**共用同一个函数体** `cmd_ctest_target`（`deep_ci_driver.py:337-369`）；其中 108 条 id 以 `CTEST` 开头。与 A-22 合读 ⇒ 这 115 条的"绿"全部出自同一段 `zero_hit` 守卫代码：该守卫存在时它们同享保护，该守卫一旦被改坏则**同批 115 条一起假绿**。
  - "注册表与门禁清单没有共享驱动维度"复算：`git grep -c "shared_driver" -- eng/ci/checks.json eng/ci/checks.schema.json docs/ci/03_GATES.md` ⇒ **rc=1、0 命中**；正控同 pathspec 跑通（`git grep -c "timeout_seconds\|waivable" -- …` ⇒ checks.json 880 行、schema 8 行）⇒ 确无该字段，也无按驱动去重后报"独立判据数"的口径。
- 与成稿差异：无（数值全复现），范围上我把"共用脚本"精确为"共用同一 subcommand/同一函数体（115/115）"，并把 A-22 的守卫与这条的集中度接起来读（同一条代码路径既是唯一保护、也是唯一单点）。
- 影响 D8 的处置：进任务书（门禁清单加 `shared_driver` 维度 + 独立判据数按驱动去重）；`45` 条 unittest-dispatch 入口建议并列为第二集中点。

<!-- PROGRESS: 8/12 -->

## A-25 · 档位纪律只写在文档、无门承载：`fast` 档 194 个单元里 112 个登记超时 >120 s —— 判定：确认（数字逐档全复现）

- 我的独立取证：
  - 文档口径逐字：`docs/ci/CI_SPEC.md:93` 档位表 `| fast（默认） | 每次本地 / agent 运行 | 秒级一致性门：静态 / 文档 / 合同 / 治理 / 轻量单测 | ≤120 s |`；`:83`「每个 step 的 `timeout_seconds` ≤ max(60, 3 × 最近一次实测墙钟)，且不超过硬上限（默认 3600 s）」；`:85` 增量档总预算 `--budget-seconds` 默认 120 s 超出 rc=1。（文件在跟踪集：`git ls-files docs/ci` ⇒ `01_CHECKS.md 02_PIPELINE.md 03_GATES.md 04_ARTIFACTS.md CI_SPEC.md`）
  - 注册表实算（同一展平口径，按 `profiles` 成员分档，一单元可属多档）：

    | profile | units | 登记超时合计 s | max s | >120 s | >600 s |
    |---|---|---|---|---|---|
    | fast | **194** | **53,160** | **1,200** | **112** | **10** |
    | integration | **9** | 4,980 | 900 | 6 | 4 |
    | linux-main | **345** | **216,180** | **9,000** | 263 | 131 |
    | prerelease | **64** | 85,580 | **20,000** | 62 | 52 |
    | windows-main | 168 | 56,940 | 3,600 | 93 | 18 |
    | linux-deep | 10 | 41,600 | 20,000 | 10 | 8 |
    | fatduck | 3 | 480 | 300 | 1 | 0 |

    ⇒ 成稿点名的 `fast` 194/53,160/1,200/**112**/10、`integration` 9、`linux-main` 345/216,180/9,000、`prerelease` 64/20,000 **全部逐位复现**；且所有单元都带 `timeout_seconds`（`no_timeout_field=0`）。
  - "无门承载"我按远离锚点也读一遍再判（防把"我没找到"当"没有"）：
    - `eng/ci/validate_registry.py:10` R2 只要求 `timeout_seconds` **字段齐备且类型正确**，无任何按档上界；
    - `eng/tools/quality/deep_ci_driver.py:215` 与 `:397` 只在注释里**引用** §2.5（"各步超时上限（秒）。CI_SPEC §2.5：timeout ≤ max(60, 3 × 最近一次实测墙钟)"、"需要这个数才可复核"），并记录 `duration_seconds`（`:398`）供事后复核 ⇒ 提供数据、不做断言；
    - `eng/tools/quality/check_budget_single_source.py`（在册 `BUDGET-SINGLE-SOURCE-GATE`，`checks.json:2206-2217`，fast+linux-main）判据 E1–E4 全部针对**内存/资源预算分母源唯一与判别力**，与档墙钟无关 ⇒ 不是这一条的承载；
    - 唯一在跑的墙钟裁决：`eng/ci/run_checks.py:1938` `if scope == "changed" and args.budget_seconds and not fails and elapsed > args.budget_seconds:` ⇒ `:1943` `verdict: V_SCOPE`、`:1956` 理由原文 `BUDGET_EXCEEDED: 增量档实测 … 应拆分`、`:1962` `failures + ["SCOPE-BUDGET"]`。条件写死 `scope == "changed"` ⇒ `--all`/`--check` 不受预算约束（成立）；另注意还带 `not fails` 前置，即已有其它红时不再判预算红。
  - 补一条成稿未写的**部分承载**（避免下游把这条改写成"档位纪律全无机器面"）：`eng/ci/impact_map.json:4` notes 记载 `RESOURCE-GATE-REAL/-NEG` 因"20 s 设计测量窗 ×2，占 fast 档 18%"被**手工**移出 fast，并要求映射 id 必须是 fast 候选、由 `eng/ci/tests/test_impact_map.py` 机器校验 ⇒ **档位成员资格一致性有部分门**，缺的是"档墙钟/超时上界"这一维；同处 `run_checks.py:2047` 打印 `integration_not_run:` 并落 JSON 字段，属告知不属裁决。
  - 成稿"登记超时是上界，可能远大于实测 ⇒ 实测是否真超档需复测"我照搬为**待证**项（本阶段禁跑门禁，`duration_seconds` 需真实 CI 结果，基线内无该数据可读）。
- 与成稿差异：无（数值与结论全复现）；范围收窄一处——"无门承载"应精确为"**档墙钟/超时上界无门承载**"，档位成员资格有 `test_impact_map.py` 的部分机器校验。
- 影响 D8 的处置：进任务书（新增档预算门：读最近一次 CI 结果的 `duration_seconds` 断言 fast 档分布 + §2.5 的 3×实测上界）；实测超档与否留待证。

<!-- PROGRESS: 9/12 -->

## A-26 · 最强的一条「文档字符串冒充验证」：`lib/phase1_session/README.md` 点名的机器判据，其判据对象已被删除且脚本从未注册 —— 判定：确认（定性须收窄：同行已自披露"已删"，不构成隐瞒，构成无效判据点名）

- 我的独立取证（按前言：判"不存在/已删"只用**跟踪集＋删除历史**，不用磁盘存在性）：
  - 点名文字（逐字）：`lib/phase1_session/README.md:204-205`「符号映射完整性：`eng/tools/check_p1_symbol_map.py`（p1_session.h 导出 API ↔ 归档映射表 P1_SYMBOL_MAP.md（**已删，见 git 历史**））」，住在 `## 7 验证` 小节（标题在 `:196`）的要点列表里，与 `eng/tests/api/test_p1_api.py`、`p1_ir_facade_test.cpp`、`check_prod_reachability.py` 等**真判据并列**、语法上无从分辨。两档均在跟踪集（`git ls-files eng/tools/check_p1_symbol_map.py lib/phase1_session/README.md` ⇒ 两行都返回）。
  - 判据对象不存在（跟踪集口径）：`git ls-files docs/refactor` ⇒ **0 行**（注意 `git ls-files` 对空结果仍返回 rc=0，故以"零行"为准，不看 rc）；`git log --oneline --diff-filter=D -- docs/refactor/P1_SYMBOL_MAP.md` ⇒ 唯一一条，标题 `docs(governance): GOV-002 归档非当前工程文档` ⇒ **对象确已在本基线前被删除**。
  - 脚本一跑即抛（逐字）：`eng/tools/check_p1_symbol_map.py:13` `MAP = (REPO / "docs/refactor/P1_SYMBOL_MAP.md").read_text(encoding="utf-8")` 是**模块级**语句（与 `:14` HDR、`:15` INDEX 同级），`main()` 定义在 `:17` ⇒ 任何 `import`/直跑都在模块执行期 `FileNotFoundError`，判据主体（`:18-43`）永不到达。
  - 从未注册：`git grep -c "check_p1_symbol_map" -- eng/ci/checks.json` ⇒ **rc=1、0 命中**；全跟踪集引用面 `git grep -n "check_p1_symbol_map" -- lib docs eng` ⇒ 仅 6 处，全是**文档/台账自述**：`docs/ci/01_CHECKS.md:212,217`、`eng/tools/doccheck/dangling_ledger.json:349`、`lib/phase1_session/README.md:204`、`lib/phase1_session/memory.md:120`（"docs/refactor 旧路径断链…为 BASE 预存"）、`lib/phase1_session/module.yaml:31`（注释"…复核"）⇒ **零调用点、零派发面**。
  - 分类冲突（第①层引 `01_CHECKS.md:210-212`，我逐字读 `:206-222`）：`:210` 「**应删除（3）**——非检查器（无判据、无退出码语义）或旧副本，且无权威文档点名」，`:212` 列出本脚本；但脚本本体明写 4 条判据（`:19-22` API 未映射、`:24-27` SCI 未登记、`:29-32` module-ID 命名、`:34-35` blocker 断言）并 `:37 return 1` / `:43 return 0`、打印 `P1-001_MAP_VIOLATION` / `P1-001_PASS` ⇒ **"无判据、无退出码语义"该分类不成立**（成稿引 `:20-40`，实际判据体在 `:18-43`，方向一致）。同节 `:213-219` 又自陈"删除前逐条核过「零调用方 + **无文档点名**」"，紧接着 `:217-219` 承认它**被 README §7 点名**且"登记在 `eng/tools/doccheck/dangling_ledger.json`；两者处置需先动 `lib/**` 与历史取证脚本，超出本项文件域" ⇒ 文档内部自相矛盾为**在册事实**，不是我的推断。
  - 台账侧的相反定性（成稿未引，须并入）：`eng/tools/doccheck/dangling_ledger.json:348-357` 该条记 `token: "docs/refactor/P1_SYMBOL_MAP.md"`、`classification: "retired_namespace"`、`reason: "历史/退役路径登记面或陈旧注释指针（**非活动指针**，待所属域收口）"`、`owner/handoff → BLD-401`。⇒ 机器台账把它当"陈旧注释指针"，而模块 README 把它当"该模块的验证判据"——**两份登记面在同一文件上给出互斥定性**，这是本条最强的可复核证据。
- 与成稿差异（范围收窄一处，机制/定级不变）：README `:205` 同行已写"已删，见 git 历史"，故**不宜**定性为"隐瞒/冒充"；成立的是"**点名一条不可运行（模块级即抛）且从未在册的判据作为该模块的验证面**"，且 `已删` 自披露与 `retired_namespace` 台账定性一起，使读者无法从 README 判断该模块的符号映射完整性实际**没有任何机器判据**。三重缺陷（从未在册 / 一跑即抛 / 对象已删）**全部复现**。
- 影响 D8 的处置：进任务书；处方取成稿的 ①（删 README `:204-205` 两行 + 删脚本 + 同步订正 `01_CHECKS.md:210-212` 的分类理由与 `:217-219` 的挂账），并要求同步撤下 `dangling_ledger.json:348-357` 与 `module.yaml:31`、`memory.md:120` 三处指针，否则留下悬空登记。措辞禁区：不得写成"文档隐瞒"。

<!-- PROGRESS: 10/12 -->

## A-27 · Q3 之四「`--self-test` 负例不落该门自己的豁免分支」：runner 门自身做到了，其余检查器无承载 —— 判定：确认（反证复算通过；覆盖面数字须订正为 136 / 91）

- 我的独立取证（反证必须复算，不采信"它做到了"）：
  - 设计自述：`eng/ci/run_checks.py:1325-1335` `gate_trust_contract_self_test()` docstring —— `:1330`「kind=exemption 的用例**落在 runner 自身的豁免分支内**」，并写明理由「只验正例会漏掉『豁免面把该红的东西放过去了』这种失效」，`:1335`「全部经 `main()` 真跑（临时注册表 + 临时 repo 根），不写主工作区」。
  - 用例逐条读到断言体（`:1439-1477` 区，我数得 **6 组正负配对**，非"六条"）：
    `exemption_waivable_skip`→`exemption_pos_waivable_skip_granted`；`exemption_nonwaivable_skip`→`exemption_neg_nonwaivable_skip_denied`；
    `exemption_silent_unregistered` = `step("NOT-IN-SILENT-OK-UNITS", silent), V_EMPTY_OUTPUT, EXIT_FAIL` → `exemption_neg_silent_ok_units_not_blanket`；
    `exemption_silent_registered` = `step("API-DOCS", silent), V_PASS, EXIT_OK` → `exemption_pos_silent_ok_units_is_id_scoped`；
    `exemption_platform_skip`→`exemption_pos_platform_skip_counted`；`exemption_platform_any_not_skipped`→`exemption_neg_platform_any_not_skipped`。
  - **ID 作用域我用独立一侧证明**（先读到，非引成稿）：`eng/ci/run_checks.py:140-145` `SILENT_OK_UNITS = frozenset({"API-DOCS", "UNIT-CLOSURE"})`，注释原文「outputs 为空且按设计静默成功的执行单元（**显式登记，不设全局兜底**）；新增检查不得进入本表」⇒ 同一段判据在 `API-DOCS` 上放行、在 `NOT-IN-SILENT-OK-UNITS` 上判红，"豁免分支按 ID 生效、不是全局兜底"成立 ⇒ 第①层的 **REFUTED**（"runner 门自己也做不到"这一猜测）我也独立判为不成立。
  - 承载面缺口复算（覆盖面数字）：`git ls-files "*check_*.py" | wc -l` = **136**（成稿写 128）；`git grep -l -- "--self-test\|self_test" -- "eng/**/check_*.py" | wc -l` = **91**（成稿写 85）；`git grep -ln "gate_trust" -- eng` = **5 个文件**，其中 1 个是注册表（`eng/ci/checks.json`），Python 工具恰 **4 个**：`eng/ci/run_checks.py`、`eng/tools/quality/check_block_flow_conformance.py`、`eng/tools/quality/check_block_flow_ports_vs_code.py`、`eng/tools/quality/contracts/check_build_graph.py` ⇒ **"仅 4 个工具用类型化出口"完全复现，但分母要从 128/85 改 136/91**。
  - runner 侧识别机制（逐字）：`eng/ci/run_checks.py:418` `TRUST_DECLARED_RE = re.compile(r"GATE_TRUST_FAIL|SELFTEST_FAIL")`（成稿写 `:417`，实为 `:418`）；`:412-417` 注释说明为何要"rc=3 **且** 命中标记"双条件——「退出码 3 在通用工具里并不罕见…只看退出码会把第三方工具的正常非零退出误标成『门不可信』」。⇒ 识别靠**字面量约定**，与 A-18 我查到的"码 3 同码异义（runner=CRASH、gate_trust=TRUST）"互证。
  - 在册承载：`validate_registry.py` 是否强制在册门产出带 `kind` 的用例 JSON —— 我读 `:10` R2 必需字段清单（`id/profiles/platform/command/timeout_seconds/…`）**不含** `kind`/用例出口要求 ⇒ 成稿"应为 = `validate_registry.py` 校验在册门必须产出带 `kind` 的用例 JSON"作为缺口描述成立。
- 与成稿差异：仅数字（128→136、85→91、`:417`→`:418`）与用例计数口径（6 组配对＝12 条断言）；机制、REFUTED 定性、处方均无差异。
- 影响 D8 的处置：进任务书（`gate_trust.emit` 定为唯一出口 + 在册门必须有带 `kind` 的用例产物）；负责人清单里这条的覆盖面按 136/91 落笔。

<!-- PROGRESS: 11/12 -->

## A-28 · Q3 之五「把已作废模式硬锁成约束（改对反而判红）」：抽样两条均成立为"带显式逃生口的约束"，未见反向判红陷阱 —— 判定：确认（反证复算通过；并查出成稿未提的第二条静默放行向量）

- 我的独立取证（两条门逐字读到判据体，并核其在册）：
  - `eng/ci/check_psfsw_retired.py`：`:27` `DEFAULT_ROOT = "eng/contracts/schemas/unified"`、`:28` `TOKEN = "psfsw_robust_weight"`、`:29` `RETIRED_MARKERS = ("已退役", "retired")`；判据体 `:39-56`——命中 TOKEN 的行合成 `grep_line = f"{path}:{lineno}:{line}"`，`if any(m in grep_line for m in RETIRED_MARKERS): continue`，否则进 `hits`，`:56` `(1 if hits else 0)` ⇒ **"出现即红"不成立，同行/同行输出含标记即放行**。fail-closed 三处：`:35` 扫描根缺失 ⇒ rc=2、`:41` 根不可遍历 ⇒ rc=2、`:45` 文件不可读 ⇒ rc=2（docstring `:12-13` 是同义声明，成稿引此处，实现体在 `:35/:41/:45`）。
  - **成稿未提的第二条放行向量（路径名放行）**：`:50-52` 注释自陈「与原 bash 口径一致：过滤作用于 grep 的**整行输出（含 文件路径:行号: 前缀）**，故路径里带 `retired/已退役` 的负例文件（`n5_retired_*.schema-violation.json`）与行内标注**同样放行**」⇒ 不只是"一处误标即静默失效"，而是**文件名含 `retired` 即整文件豁免**。这把成稿的残留弱点说轻了，处方（禁"仅注释放行"、必须落 allowlist）应一并覆盖路径维度。
  - `eng/ci/check_retired_code.py`（`CHK-RETIRED-CODE`）：判据 R1（`:36-39` 连续 ≥3 行注释且 ≥2 行含代码样 token ⇒ 违规，**除非块内含 `RETIRED-CODE-RETAINED`**）、R2（`:40-46` 前 40 行显式横幅 token 且无保留块 ⇒ 违规；并自陈"正文散文里的小写提及不判红（实测：大小写不敏感的全文匹配会把 **62% 的在役文件判红 = 恒红门**）"——这是一条**为避免反向判红而做过的实测收窄**，直接支持"未见反向判红陷阱"）、R3（`:47-49` 保留块 6 token 不全/字段空 ⇒ 违规）；逃生口是 ENGINEERING_SPEC §2 的六 token 统一注释块（`:19-33` 逐字给出格式）。
  - allowlist 承载：`git ls-files eng/ci/retired_code_allowlist.json` ⇒ 在跟踪集；其 `purpose` 逐字「**逐条豁免台账**：只抑制 R1/R2 的已复核假阳性…每条必须带 reason，**只减不增**（ENGINEERING_SPEC §10「豁免显式登记且只减不增」）。**R3 不可豁免**」；`schema`=`astrocs.retired-code-allowlist/v1`、`authority` 指 ENGINEERING_SPEC §2/§9/§10、`entries[]` 每条带 `pattern/rule/reason`（首条 `nanoflann.hpp` = vendored 第三方）。⇒ 成稿所说"allowlist 承载合法再现"成立，且比"无合同承载"的说法更有约束（显式登记、只减不增、R3 不可豁免）。
  - 两门在册与档位：`eng/ci/checks.json:2272` `CHK-RETIRED-CODE`（fast+linux-main+windows-main，waivable=false，timeout 300）、`:2300` `CHK-RETIRED-CODE-SELFTEST`（fast）、`:10262` `CHK-PSFSW-RETIRED-STATIC`（含 steps `:10287` 主体与 `:10309` `-SELFTEST`，fast+linux-main，waivable=false，timeout 120）⇒ **两条都带配套 SELFTEST 单元、都不可豁免**；两条均未声明 `inputs`（与 A-14/A-23 同一缺口）。
  - 反向陷阱检验（我自己的判读，不引成稿）：约束对象是"作废符号不得无标注复现"，改对=写现役名 ⇒ 不触发 TOKEN ⇒ 不会红；"改对=补保留注释块"⇒ R1/R2 放行且 R3 要求块完整（字段空仍红），这是**要求更严**而非反向判红。故"改对反而判红"在两抽样上不成立。
- 与成稿差异：一处加强（第二条放行向量=路径名整文件豁免，应写进处方）；一处订正（allowlist 并非"无合同承载"，它自带 schema 与"只减不增/R3 不可豁免"纪律；缺的是把 `RETIRED_MARKERS` 两串字面量升为合同字段，这一句成稿方向仍成立）；抽样限制（其余同族门未逐条精读）照实保留 ⇒ 覆盖率见下节。
- 影响 D8 的处置：进任务书但按"抽样 2 条"限定语落笔，不得写成"同族门普遍安全"；处方补两条：路径维度放行要关闭、`RETIRED_MARKERS` 进 `eng/contracts/` 带 schema。

<!-- PROGRESS: 12/12 -->

## 本批判定汇总（各态计数，附复算命令）

| 态 | 条数 | 条目 |
|---|---|---|
| 确认 | **11** | A-17、A-18、A-19、A-20、A-22、A-23、A-24、A-25、A-26、A-27、A-28 |
| 降级 | 1 | A-21（机制成立；236/235/1 三个数不复现，枚举范围漏 `lib/**` 145 条，定级应为 S2） |
| 推翻 | 0 | — |
| 待证 | 2 个子件 | A-21 附带：`p1snr_science_*` 是否命中 `lib/**` 目标（C4 疑点）；A-25：`fast` 档实测墙钟是否真超档（需真实 CI 结果，本阶段禁跑） |
| 重复 | 0 | — |

计数：**12 条全判**＝确认 11 + 降级 1；其中确认里带"范围订正/收窄"注记者 6 条（A-18 引用不精确、A-19 反证须同读、A-20 补强事实、A-22 覆盖面改 111、A-25 收窄为"档墙钟无承载"、A-26 措辞改"无效点名"、A-27 分母改 136/91、A-28 放行向量补一条）。
主要复算命令（全部只读，未跑任何门禁）：

```bash
git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
git grep -n "SKIP_EXIT_CODE\|FAIL_VERDICTS\|skipped_platform" -- eng/ci/run_checks.py
git grep -n "select_candidate" -- eng/ci/checks.json            # rc=1 零命中（正控 select_profile 命中 workflow_binding.json）
git grep -ln "polarity_probe\|reconcile_state\|verify_ciqa_report"
git ls-files "*checks.schema.json"                              # 实际路径 eng/ci/checks.schema.json
python 独立审计/复算件/V501-B/a21_pair.py                             # add_test/glob/正则三侧复算
git grep -h -E "add_test\(NAME" -- lib | wc -l                  # 145（成稿枚举面漏掉的部分）
git ls-files docs/refactor                                      # 空 ⇒ 判据对象不在跟踪集
git log --oneline --diff-filter=D -- docs/refactor/P1_SYMBOL_MAP.md
git ls-files "*check_*.py" | wc -l                              # 136
git grep -ln "gate_trust" -- eng                                # 5 档（4 个 Python 工具 + 注册表）
```

## 旧判偏松清单（= 上一轮给下游的虚假授权）

1. **A-21 的三个数（236 / 235 / 1）**：下游若照抄即获虚假授权。我的复算：`eng/tests` 面 238 条 `add_test(NAME`（237 distinct、6 条含生成器表达式 ⇒ 全静态只 232），并集 fnmatch 覆盖 **238/238**；"唯一例外" `drizzle_pf_sb_v6_area_invalid` 被成稿自己承认可由 `drizzle_pf_sb_*` 覆盖 ⇒ 自相矛盾。且"0 处生成器表达式"与实测 6 条直接冲突，枚举面漏 `lib/**` 145 条（门自身 C1 口径是活动面全部 CMake 源）。**可保留的是机制**（C6 只查无 glob 字符名 ⇒ 两种方言无调和；同一字符串 regex 命中 1 条、glob 命中 0 条）。
2. **A-19 的读法风险**：成稿"无登记 timeout、无 outputs、不被普查覆盖"全部为真，但同段可被读成"候选判据无机器校验"。实况：两条在册单测覆盖该分支（`checks.json:12484`、`:12670`；`test_ci001_failclosed.py:555-561` 字面断言 workflow 的 `rc -eq 3 … exit 3`）。下游不得据此新立"补校验"任务。
3. **A-26 的定性措辞**："文档字符串冒充验证 / 三重虚假授权"里含"隐瞒"意味，而 README `:205` 同行已写"已删，见 git 历史"。成立的指控是"点名一条不可运行且从未在册的判据作为验证面"+"README 现在时陈述与 `dangling_ledger.json` 的 `retired_namespace`／"非活动指针"定性互斥"。照抄原措辞会让整改方向被带偏成"补披露"而非"删无效判据"。
4. **A-22 的覆盖面 46**：那是 `deep_ci_driver.py:347` docstring 的历史陈述；注册表现值 111 个 `CTEST*` 单元（A-24 复算：115 条驱动单元全部走 `ctest-target`）。下游按 46 估风险会低估单点半径。
5. **A-27 的分母 128 / 85**：应改 136 / 91（命令见上）。"仅 4 个工具"不变，比例被夸大得还不够狠。
6. **A-28 的"allowlist 无合同承载"**：allowlist 自带 `schema`、`只减不增`、`R3 不可豁免` 纪律；真正缺的是 `RETIRED_MARKERS` 字面量入合同 + 关闭"路径名含 retired 即整文件豁免"这条成稿未发现的放行向量。

## 覆盖率自报（已判 N / 分配 M）

- 已判 **12 / 分配 12**，本批无非空未判名单 ⇒ **本批已完成**。
- 每条取证命令数均在 §4.3 的 8 条上限内（A-21 用满 8 条，其子件 `p1snr_science_*` 按 §4.3 转 `待证`）。
- 未做（按前言与派单禁令）：任何门禁/`run_checks.py`/`ctest`/构建执行；`--self-test` 实测；CI 实测墙钟；`git grep` 之外的普查脚本（仅建 `独立审计/复算件/V501-B/a21_pair.py` 一个）。
- A-28 只覆盖成稿自报的两条抽样门，同族其余退役门未逐条精读（与成稿同限）。

## 我对第①层判据本身的校准建议

1. **凡"数出来的结论"必须附口径行**：A-21（枚举面只取 `eng/tests`）与 A-22（引 docstring 的 46 当现值）都因**口径不写**变成不可复算的数。建议第①层每条计数后面强制写"分母定义 + 命令"，并优先用门自身的扫描口径（如 `check_ctest_registration.py` C1 的"活动 CTest 面全部 CMake 源"）。
2. **"0 命中"要成对给正控**：本批 A-19/A-20/A-24/A-26 的零命中结论全部经正控复算站得住（同 pathspec 换关键字必命中）；但我在 A-23 上先用 `git grep -n "a\|b"` 得到一次**假零命中**，换 `-o -E` 后才见 11 条在册 id ⇒ 建议把"正控必须与零命中同命令族、同 pathspec"写成硬要求，否则复核者会误推翻。
3. **区分"隐瞒型"与"无效点名型"虚假授权**：A-26 属后者。两者的整改处方不同（改披露 vs 删判据/注册），混写会让任务书给出错的修法。建议第①层在"文档字符串冒充验证"名下再分两档。
4. **守卫/洞要成对报，且报在同一层**：A-22 的 `zero_hit` 守卫与 A-24 的 115 条集中度是同一段代码（`cmd_ctest_target`），A-17 的 `run_checks.py:1966` 洞与 A-17/A-08 的 `run.py:1256` 守卫是同一职责的两入口。第①层把"保护"与"单点"拆在两条里，读者拿不到"这 115 条的绿与这 115 条的风险同源"这一条真正决定优先级的信息。建议判据模板加一栏 `共址守卫/共址洞`。
5. **"无门承载"须写成"哪一维无承载"**：A-25 成立，但档位成员资格其实有 `test_impact_map.py` 的部分机器校验，缺的是档墙钟/超时上界。绝对式措辞会诱导下游新建重复门。

## 收工 `git status --porcelain` 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（与开工一致，无新增项 ⇒ 取证动作未弄脏被审对象；命令：`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`，收工实测于全部 12 条判定落盘之后）

<!-- PROGRESS: 12/12 -->
