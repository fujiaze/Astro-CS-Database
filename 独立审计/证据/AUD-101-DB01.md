# AUD-101 批次 DB-01 文档审计台账（独立只读审查节点）

判据：`standards/01_文档权威链整治标准.md` §2 角色表 / §3 双向索引 / §4 写法 / §5 产出要求。
范围：`独立审计/批次清单/DB-01.txt` 全 23 份（仓库相对路径），逐份通读全文。
仓库冻结基线 HEAD=`c8f64e9a`。

## 0. 工作树基线（开工时原文）

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（收工时第二次输出见文末「工作树基线（收工时）」节。）

## 1. 逐份登记

字段顺序 = 派单前言 §4：`路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议`。

---

### 1. docs/api/CLI_PROTOCOL_V1.md

- **路径**：`docs/api/CLI_PROTOCOL_V1.md`
- **标题**：Astro Celestial Sphere Database（ACSD） CLI 协议合同 v1（API-002 冻结）
- **行数**：168（`wc -l`，与机械列一致）
- **角色**：架构设计（CLI 接口契约）。实际目录 `docs/api/` **不在标准 01 §2 表的任何目标目录中**，也不在最高设计 §0.2 列举的详细文档层目录清单里 ⇒ **角色错放**，应迁往 `docs/interfaces/cli/`（与既有 `docs/interfaces/data/`、`docs/interfaces/io/` 同层同构）。
- **主题**：`acsd` 唯一命令树、退出码、stdout/JSONL 事件流 schema、`output_dir` 落点，以及 `export` 的 `crop` 裁剪键。
- **上游**：已标注 —— :3「> 上游：ASTROCS_DESIGN.md §7.1（命令树）、§7.2（配置、事件与退出码）」。**条款号与条款名均正确**（本批 6 份里唯一指针无瑕疵的一份）。
- **下游**：非孤儿，40 个在仓文件引用。命令：`grep -rl CLI_PROTOCOL_V1 docs lib eng ASTROCS_DESIGN.md ENGINEERING_SPEC.md ACCEPTANCE_SPEC.md | grep -v '^docs/api/CLI_PROTOCOL_V1' | wc -l` → `40`。命中样例：`docs/contracts/API-001.md`、`docs/contracts/CONFIG_CONTRACT.md`、`docs/design/LOG_AND_ERROR_SYSTEM.md`、`docs/design/PHASE3_DETAILED_DESIGN.md`、`docs/owner/PIPELINE_OVERVIEW.md`、`docs/plugins/infrastructure/21_observability.md`。
- **是否正本**：CLI 命令面/事件流合同 = **副本**（正本分别是最高设计 §7.1–7.2 与实现 `lib/infrastructure/cli/protocol.h`）；`crop` 节 = **片段**（正本自认在 `docs/design/PHASE3_DETAILED_DESIGN.md` §8）。整份文档定位是「上位条款的下位重述」，而非新增机制的正本。
- **重复或重叠对象**：
  - (a) 与上位 `ASTROCS_DESIGN.md` §7.2 退出码表重复。本文 :37「0 成功且门禁全过 / 2 CLI 参数或配置错 / 3 输入缺失格式 hash 错 / 4 科学验证或不变量失败 / … / 70 未分类内部错误」；上位 :498-511 同一张表逐行存在（`| 0 | 成功且门禁全过 |`…`| 70 | 未分类内部错误（脱敏 crash report） |`）。差异：本文多一句「内存 / CPU / 线程不设门」的来源标注，其余口径相同。本文 :39 自己承认「码值与含义**只有一份**，以 `lib/infrastructure/cli/exit_codes.h` 为唯一源」⇒ 加上头文件，同一张表在仓内有**三处**（头文件 + 上位 + 本文），违反标准 01 §2「同一角色内同一主题只有一份正本」。
  - (b) `crop` 节 :138 自认「设计正本：`docs/design/PHASE3_DETAILED_DESIGN.md` §8」，随后 :160-163 仍重述判据「越界 / 宽高非正 / 两形式同时给 / 裁剪后为空 / `sky` 边界点落在 TAN 半球外 ⇒ 拒绝」——正本已有该判据时，此处只应留指针。
  - (c) §4 事件流 schema 与 `docs/architecture/observability/STRUCTURED_LOGGING_CONTRACT.md` 的关系本文 :54-56 已主动切分（「两份流各用不同工件名、各自具名」），**不算重复**，是合法消歧。
- **元信息块**：**有**（违 §4）。:5「> ID: API-CLI-001  状态: FROZEN  上游: API-001/ARCH-002  下游: CLI-001/002/003, API-003..005(handler 追溯), BENCH-005」+ :1 标题内状态锚「（API-002 冻结）」。**机械列 `meta_block` 对本份报 `False` —— 漏报。**
- **写法违规**：
  - :1 标题含状态编号「（API-002 冻结）」；
  - :5 文档 ID / 状态 / 上下游元信息块（见上）；
  - :132-136 「> 权威：负责人裁决 2026-09-23 逐字：「默认导出的话是要求边框不得裁剪任何有效像素……需要保留接口。」」——**日期 + 「负责人某日裁决」+ 逐字引语**，同一处三重违 §4；
  - :74「读侧 CLI-004 / 读侧 FIX208」——任务编号（§4 明列 FIX- 类）；
  - :90 节标题「## 6 机器化一致性检查器合同(API-002 建立；现行落地 = `eng/tools/check_api_docs.py`)」——「API-002 建立」为历史叙事，「现行落地」隐含前序版本；
  - :49、:107「（GAP_AUDIT §4.3）」「（GAP_AUDIT §4.2）」——把一次性控制包工件当条款依据（详见悬空引用）；
  - :61「**本节不复制该清单**——复制即形成第二份定义」、:74「本节只给指针与语义，**不重复该判据**」——关于本文档如何治理自己的纪律声明，属 §4「元信息块（纪律声明）」的正文变体。
- **悬空引用**：
  - :49 / :107 `GAP_AUDIT §4.3`、`GAP_AUDIT §4.2` —— 无路径。命令：`find . -iname '*GAP_AUDIT*' -not -path './.git/*'` → 唯一命中 `./工程控制/RELEASE-05/GAP_AUDIT.md`。按 AGENTS.md §7 目录落位，`工程控制/` 是「控制包（收口后按 CONTROL_PACK_SPEC §9 清理）」⇒ 该工件按设计必然消失，「唯一性声明」的依据将悬空。
  - :5 / :87 / :90 / :96 的 `ARCH-002`、`ARCH-005`、`API-001`、`API-002`、`CLI-001/002/003`、`BENCH-005` 是一整族**不可解析的合同 ID**（见 §索引与链接缺陷清单第 A 条的举证命令）。
  - :44、:50、:52、:58、:108、:110、:130、:139、:140、:162 指向的文件全部实存（逐个 `ls` 核过：`lib/infrastructure/cli/{protocol.h,jsonl.h,exit_codes.h,commands.cpp,command_tree.h,resource_recorder.h,resource_events.h,parser.cpp,session_commands.h}`、`eng/contracts/schemas/jsonl_event_v1.schema.json`、`eng/contracts/schemas/phase_config_export.schema.json`、`lib/algorithms/projection/p3_wcs.h`、`docs/design/PHASE3_DETAILED_DESIGN.md`、`docs/plugins/infrastructure/21_observability.md`、`docs/architecture/observability/{STRUCTURED_LOGGING,RESOURCE_MONITORING}_CONTRACT.md`、`eng/ci/check_event_field_sets.py`、`eng/tests/cli/test_cli001_vpi.py`、`eng/tests/cli/test_phase123_pipeline.py`）。**本份无文件级断链。**
- **与上位冲突**：
  - :8-9 声明「`phase1|2|3` 用户命令与别名（含 config */modules */selftest/test synthetic/**verify***/drizzle/…）不在命令面上，调用返回 rc=2」，与上位 §7.1 一致、**正确**；但同批 `docs/api/MANIFEST_VERIFY_V1.md` §3/:70 冻结 `acsd verify --run-manifest --json`、§4/:74 冻结 `--config --cpu-profile --json` ⇒ 本份与同目录另一份**互相矛盾**（谁错见该份判定）。
  - :44「`--events-jsonl` **保留接受**」：上位 §7.1 命令树（:468-485）不含该旗标。下位自行扩一个「保留」的旗标而不回上位补要点，违标准 01 §3「每份文档、每个机制都能追溯到最高设计的一条要点；追溯不到的机制先补要点再保留」。
- **处置建议**：**迁移** → `docs/interfaces/cli/CLI_PROTOCOL.md`。迁时：删 §2 退出码整表改留 `exit_codes.h` 指针（依标准 01 §2 同主题单一正本）；删 :5 元信息块与 :1 状态锚、把 :132-136 负责人逐字裁决改写为正向现行条款、:49/:107 的 `GAP_AUDIT` 引用替换为被声明的现行条款本身（依 §4 写法）；`crop` 节缩为指向 `docs/design/PHASE3_DETAILED_DESIGN.md` §8 的指针（依 §1「每一层只由它的上一层推出」）。

---

### 2. docs/api/COMMON_ABI_V1.md

- **路径**：`docs/api/COMMON_ABI_V1.md`
- **标题**：Astro Celestial Sphere Database（ACSD） 公共 C ABI 基础层 v1 (Common Foundation — API-001 冻结)
- **行数**：86
- **角色**：架构设计（接口契约 / ABI）。目录 `docs/api/` 无对应角色 ⇒ **角色错放**，应迁往 `docs/interfaces/`（建议 `docs/interfaces/COMMON_ABI.md`）。
- **主题**：跨 DLL C ABI 基础层——`acs_head`/`acs_span_*`/`acs_handle`/`acs_status`/`acs_allocator`/`acs_logger`/`acs_cancel`/`acs_thread_budget` 的逐字段单位与所有权、并发合同五字段模板、头文件独立性验证。
- **上游**：已标注但**条款号错** —— :3「> 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）」。上位 §8.4 标题是「顶层结构」（:618），「模块与 ABI」是 §8.5（:661）。**条款名对、条款号错**，且本批 3 份共用同一处错误号（见 PHASE1/2/3_API_V1），是系统性拷贝。
- **下游**：4 个文件 —— `docs/DOCUMENT_INDEX.yaml`、`docs/owner/ARCHITECTURE_OVERVIEW.md`、`lib/include/astrocs/common_abi_v1.h`、`eng/tests/api/test_common_abi.py`。命令：`grep -rl COMMON_ABI_V1 docs lib eng ASTROCS_DESIGN.md ENGINEERING_SPEC.md ACCEPTANCE_SPEC.md | grep -v '^docs/api/COMMON_ABI_V1'`。实现头 :1 反向引用「docs/api/COMMON_ABI_V1.md」⇒ 文档↔代码双向已通。**本批引用面最薄的一份**，但仍非孤儿（有代码与测试引用）。
- **是否正本**：**正本**（公共 ABI 基础类型面唯一承载者；上位 §8.5 只给「跨 DLL 不传 STL/异常/RTTI」的原则，本文给结构体，属合法下钻分层）。
- **重复或重叠对象**：与 `docs/contracts/PUBLIC_API.md` 主题重叠但条款不重复：本文 :10「前缀 `acs_`(函数)/`ACS_`(类型/常量);所有 struct 首两字段 `uint32_t struct_size; uint32_t abi_version;`(handshake, ARCH-003 §4 同规)」登记**基础类型与握手规约**；PUBLIC_API.md :40「## C ABI（`extern "C"`，不跨边界抛 C++ exception）」起登记**按模块逐个的符号名册**（API-CAL-001 / API-COS-001 / API-GAIA-001 …）。差异 = 抽象层级不同（基础层 vs 模块层）。判为「主题相邻、非重复」，但在 DOCUMENT_INDEX 里两族未互为下游（PUBLIC_API 不指向本文），索引层面需补边。
- **元信息块**：**有**（违 §4）。:5「> ID: API-COMMON-001  状态: FROZEN  上游: ARCH-002/ARCH-003(backend host services 同构)  下游: API-002..005, ABI-001, CLI-001..003」+ :1 标题状态锚「API-001 冻结」。**机械列 `meta_block` 报 `False` —— 漏报。**
- **写法违规**：
  - :1 标题状态编号；
  - :5 ID/状态/上下游元信息块；
  - :19「/* CPU-001: 所有跨边界结构带 head(...) */」——把合同 ID 写进代码注释示例（历史/来源叙事混入正文）；
  - :33、:35 无。:72「取消点粒度(帧/行带/迭代/整模型/整文件, 与 ALG 5c 对齐)」——`ALG 5c` 是无路径的编号式指代（见悬空引用）；
  - :80-86「## 5 落点映射」整节是「本文件 §n → ABI-001 / API-002..005 / ARCH-003 / ARCH-004 §1」的任务派发映射表，属过程台账内容，标准 01 §2 规定「历史档案、过程台账不放在正式文档层」。
- **悬空引用**：
  - :10 `ARCH-003 §4`、:59 `ARCH-004 §1`、:5 `ARCH-002/ARCH-003`、:86 `ARCH-003 host services/ARCH-004 §1`、:84 `ABI-001`、:85 `API-002..005` —— 见 §索引与链接缺陷清单第 A 条（不可解析 ID 族的举证命令与结果）。
  - :72 `ALG 5c` —— 全仓无名为「ALG 5c」的文档；`grep -rn '5c' docs/algorithms/*.md` 命中的是各算法文档内部的 `## 5c SIMD 安全与取消点` 节号（`INTEGRATION_ALGORITHMS.md:85`、`NOISE_ESTIMATION.md:74` 等），即指代对象是「每个 ALG 文档各自的 5c 节」，未给任一具体文件 ⇒ 检索不到唯一落点，判**半悬空（无定位的编号指代）**。
  - :6 头文件路径 `lib/include/astrocs/common_abi_v1.h` **实存**（`ls` 核过）。
- **与上位冲突**：无实质冲突。唯一上位不符 = :3 上游条款号指向 §8.4（顶层结构）而内容是 §8.5（模块与 ABI），属标注错误级不符。
- **处置建议**：**迁移** → `docs/interfaces/COMMON_ABI.md`。迁时订正上游为「§8.5（模块与 ABI）」、删 :5 元信息块与 :1 状态锚、把 §5 落点映射台账移出正式文档层（依标准 01 §2「过程台账不落正式文档层」+ §4「无元信息块」）。角色本身正确，内容是本批最干净的一份，**不建议删除**。

---

### 3. docs/api/MANIFEST_VERIFY_V1.md

- **路径**：`docs/api/MANIFEST_VERIFY_V1.md`
- **标题**：Astro Celestial Sphere Database（ACSD） run manifest 与 verify 合同 v1 (CLI-003 冻结)
- **行数**：81
- **角色**：架构设计（I/O 与产物清单合同）。目录 `docs/api/` 无角色 ⇒ **角色错放**，应迁往 `docs/interfaces/io/`（与 IO_003 原子发布合同同族）或 `docs/contracts/`（`run_manifest` 的 schema 正本 `eng/contracts/schemas/run_manifest.schema.json` 已在，文档化说明按标准 01 §2「合同说明 → docs/contracts/」）。
- **主题**：`pipeline_config.json` v1 校验序、`run_manifest.json` v1 结构与 `provenance` 子对象、`verify` 与 `show-effective` 命令合同。
- **上游**：已标注 —— :3「> 上游：ASTROCS_DESIGN.md §10（I/O 与原子产品）」，:3 正确（§10 :684-697 正是 manifest 与原子产品）。**但正文内部另有一处错号**（见与上位冲突）。
- **下游**：10 个文件，命令：`grep -rl MANIFEST_VERIFY_V1 docs lib eng ASTROCS_DESIGN.md ENGINEERING_SPEC.md ACCEPTANCE_SPEC.md | grep -v '^docs/api/MANIFEST_VERIFY_V1'`。关键下游：`docs/ci/01_CHECKS.md:55` 把本文 §2/§2.1 登记为 `CHK-RUN-MANIFEST-SCHEMA` 的「**两份冻结合同**」之一（T1）、`eng/ci/check_run_manifest_schema.py:5`、`eng/ci/ledgers/run_manifest_schema_deviations.json:6`、`lib/infrastructure/cli/parser.cpp:553`「// pipeline_config.json v1 全量校验(合同: docs/api/MANIFEST_VERIFY_V1.md §1)」。**本文是机器门的判据源，不是孤儿，不可轻处置。**
- **是否正本**：manifest 结构 = **副本**（正本 `eng/contracts/schemas/run_manifest.schema.json` + `docs/interfaces/data/DATA-004_PRODUCT_PROVENANCE.md`）；`provenance` 子对象 = **副本**（正本 = 同批 DATA-004）；`verify`/`show-effective` 节 = **待定**（见冲突）。
- **重复或重叠对象**（本批最重要的一处同主题多正本，贴两段实际内容）：
  - 本文 :44-58 provenance 子对象字段表：
    `"source_sha": "<40hex>", // CMake **configure 期** HEAD（version_generated.h）——不是构建指纹；相等不蕴含同一二进制（VERSIONING §2.1）` / `"build_source_digest": "<64hex>", // **构建期**源集内容摘要 = 构建指纹（RUN-PROVENANCE-01）`
  - `docs/interfaces/data/DATA-004_PRODUCT_PROVENANCE.md`（同批第 9 份，本次实测其 :1 标题即「DATA-004 产物溯源与版本语义（Product Provenance）」）—— 该文档承载产物溯源与版本语义的正本口径。
  - 重叠范围 = `source_sha` / `build_source_digest` / `build_head_sha` / `build_dirty` / `configure_head_sha` 五个指纹字段的**语义定义**；差异 = 本文用 JSON 注释就地定义、DATA-004 单独立节。两处并存即标准 01 §2「同一主题只有一份正本」违例，且本文 :65 已自认「语义见 `docs/VERSIONING.md` §2.1」——同一个语义有 3 处（VERSIONING + 本文 + DATA-004）。
- **元信息块**：**有**（违 §4）。:5「> ID: API-MANIFEST-001  状态: FROZEN  上游: API-002(final 事件/verify 命令) §4-5  下游: CODE-P*/TST-P*/REL-002」+ :6「> 分离原则(硬性): …」+ :1 标题状态锚「(CLI-003 冻结)」。**机械列 `meta_block` 报 `False` —— 漏报。**
- **写法违规**：
  - :1 标题状态编号；:5 ID/状态/上下游元信息块；
  - :5 `REL-002` —— 机械列 `task_ids` **命中了本份**（`['REL-002']`，唯一命中我批内的一份，机械列此处**未漏**，与我批内其它文件的漏报形成对比）；
  - :48 注释「= 构建指纹（RUN-PROVENANCE-01）」、:6「(硬性)」纪律声明、:65「语义见 `docs/VERSIONING.md` §2.1」把版本语义的正本指向根文档却在正文重述全部字段。
- **悬空引用**：
  - :5 `CODE-P*/TST-P*/REL-002`、:23 `ARCH-002 §5`、:5 `API-002` —— 不可解析 ID 族（清单第 A 条）。
  - :26 `"astrocs_version":"<X.Y.Z-alpha.N+g12hex>"` 等尖括号占位与 :45 `<40hex>` 为示例占位串，**不是** sha 违规（机械列 `sha256`/`commit_sha` 对本份报 `[]`，正确，**未误报**）。
  - :80 `lib/infrastructure/cli/main.cpp`、:80 `eng/tests/cli/test_cli_protocol.py`、:65 `docs/VERSIONING.md`、:81 `lib/algorithms/shared/crypto` —— 全部实存（`ls` 核过）。
- **与上位冲突**：**两处，均为实质冲突。**
  1. :37「### 2.1 provenance 子对象（加性扩展；`ASTROCS_DESIGN.md` §9 manifest 必记项）」—— manifest 必记项在上位 **§10**（:693「manifest 至少记录：产品类型/schema 版本、软件来源、run ID、…」），上位 §9 是「CPU 后端与资源」（:669-680）不含 manifest 条款。条款号指向错误上位节。
  2. :70「## 3 verify 合同(`acsd verify --run-manifest --json`)」与 :74「## 4 show-effective 合同 `--config --cpu-profile --json`」—— 上位 §7.1（:466-485）的**唯一命令树**既无 `verify` 也无 `show-effective`；同批 `docs/api/CLI_PROTOCOL_V1.md` :8-9 更明文把 `verify*` 列为「不在命令面上，调用返回 rc=2」。本文却把这两个命令的合同标 `状态: FROZEN` ⇒ **下位自行设立上位未授权、且被同目录正本显式否决的机制**，同时违标准 01 §1「下位文档与上位文档冲突时以上位为准；冲突本身是缺陷」与 §3「追溯不到的机制先补要点再保留」。
  3. 次要：:14 示例 `"output_dir": "."` 与同批 CLI_PROTOCOL_V1 :116-117「进程 CWD(`"."`) 只标识进程自身位置;按 CWD 写出会在工作区根散落产物并触发 UT-CLI `mutates_workspace=false` 的 dirty 判定」不一致（示例即被兄弟文档判为触发脏树）。此缺陷在 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:267`（M5b-G-16）已被登记为 OPEN，状态=「未整改」，不得当作已修。
- **处置建议**：**拆分迁移** —— manifest/`pipeline_config` 结构节迁 `docs/contracts/RUN_MANIFEST.md`（依标准 01 §2「合同说明→`docs/contracts/`，对应 `eng/contracts/`」，与 `run_manifest.schema.json` 双向对应）；`provenance` 字段语义**合并**入 `docs/interfaces/data/DATA-004_PRODUCT_PROVENANCE.md`（同主题正本，依 §2「多份内容合并为一份」）；§3/§4 `verify`/`show-effective` 两节**删除或按上位补要点后再保留**（依 §3 + §1 冲突处理），删除前须同步 `docs/ci/01_CHECKS.md:55` 的 T1 判据指向，避免把机器门判据源改悬。

---

### 4. docs/api/PHASE1_API_V1.md

- **路径**：`docs/api/PHASE1_API_V1.md`
- **标题**：Phase1 API 定义 v1 (API-003 冻结 — 逐函数 create/validate/run/inspect)
- **行数**：50
- **角色**：架构设计（阶段会话 API 编排合同）。`docs/api/` 无角色 ⇒ **角色错放**；应迁 `docs/interfaces/`。另：本文登记的函数名册与并发五字段属**模块工作细节**性质，标准 01 §2 该角色目录是 `docs/modules/`、`docs/plugins/` —— 与 `docs/contracts/PUBLIC_API.md` 的角色切分未定，见重复对象。
- **主题**：Phase1 会话五段式 C ABI（`p1_session_create/validate/run/inspect/destroy`）+ 底层模块函数并发合同登记表。
- **上游**：已标注 —— :3「> 上游：ASTROCS_DESIGN.md §4（normalize）、§8.4（模块与 ABI）」。§4 正确（:246「normalize（Phase1）」）；**§8.4 号错名对**（同 COMMON_ABI，应为 §8.5）。
- **下游**：31 个文件，命令：`grep -rl PHASE1_API_V1 docs lib eng ASTROCS_DESIGN.md ENGINEERING_SPEC.md ACCEPTANCE_SPEC.md | grep -v '^docs/api/PHASE1_API_V1' | wc -l` → `31`。密度最高的下游组：`docs/algorithms/{CALIBRATION_ALGORITHMS,COSMETIC_ALGORITHMS,DRIZZLE_GEOMETRY,NOISE_ESTIMATION,PHOTOMETRIC_FIT,STAR_DETECTION_ALGORITHMS}.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/contracts/PUBLIC_API.md`。**非孤儿，且被算法层当作承诺基准反复引用**（下位引用上位落在此处方向正确）。
- **是否正本**：会话生命周期五段式 = **待定**（上位 §8.2/§8.5 未规定「五段式 session C ABI」这一机制，标准 01 §3 要求「追溯不到的机制先补要点」）；§2 底层模块函数登记 = **副本**（正本 = 现存头文件 + `docs/contracts/PUBLIC_API.md`）。
- **重复或重叠对象**（举证两段实际内容）：
  - 本文 :30-33 表格行：
    `| ac_generate_master_bias/dark/flat(+_f64)(astro_calibration.h) | yes | yes(无共享可变) | omp(budget, pixel 域) | 无(短任务) | TST-CAL-001 |`
    `| ac_set_num_threads(int)(同上) | yes | yes | — | — | TB-ARCH-004(checker 管控; 由 p1 budget 注入取代, ABI-001 收编) |`
  - `docs/contracts/PUBLIC_API.md:135` 起「## astro_calibration C API（API-CAL-001）」:145-147「导出符号（12 个函数 + 2 工具…）：`ac_generate_master_bias`、`ac_generate_master_dark`、`ac_generate_master_flat`、`ac_calibrate_frame`、`ac_correct_frame`…」；:157-158「线程安全：全部函数 reentrant、threadsafe（无共享可变全局；**API-P1-001 §2 登记一致**）」；:164「取消：模块内无取消检查点（DISP-CAL-008）；取消由调用方（phase1_session）在帧粒度实现」。
  - 重叠范围 = 同一批 `ac_*` 导出函数的**存在名册 + 线程安全 + 取消语义**。差异 = 本文 §2 给 `ac_calibrate_frame` 的取消点写「行带」（:31），PUBLIC_API 与算法层写「无取消检查点」⇒ **不是分层，是同一字段的两套口径且互相打脸**（第三方证据：`docs/algorithms/CALIBRATION_ALGORITHMS.md:517-518`「DISP-CAL-008 模块内无取消检查点（**PHASE1_API_V1 §2 承诺的"行带取消"未实现**；现状取消在 phase1_session 帧粒度实现）」；`docs/algorithms/PHOTOMETRIC_FIT.md:246`「（**PHASE1_API_V1 §2 为计划语义**）」）。同一主题两份正本 + 与实现冲突，本批已登记在册，不需再判谁对，整治时按变更流程定档。
- **元信息块**：**有**（违 §4）。:5-6 两行「> ID: API-P1-001  范围: API-P1-001..010  状态: FROZEN；**用户命令面 = `normalize`**  上游: API-001(API-COMMON-001)/API-002  下游: 用户命令 `normalize`(CLI-001;`phase1 run` 不在命令面上, rc=2)/TST-P1-*」+「> 模式: Phase1 = 现有 C ABI 模块链…的**编排合同**…」+ :1 标题状态锚「(API-003 冻结 …)」。**机械列 `meta_block` 报 `False` —— 漏报。**
- **写法违规**：
  - :1 标题状态编号；:5-6 ID/范围/状态/上下游元信息块；
  - :10「> CLI-001 说明: 本节四段式 C ABI **不变**(内部会话 1 的冻结合同)」——「不变」是相对历史版本的叙述（历史叙事，§4 禁），且把任务编号 `CLI-001` 当主语；
  - :50「`eng/tools/check_api_docs.py`(API-003 建立合同, CLI-002 落地全量)」——「建立 / 落地」= 过程叙事 + 任务编号；
  - :33「(check checker 管控; 由 p1 budget 注入**取代**, ABI-001 **收编**)」——「取代/收编」是历史叙事词，违 §4；
  - :41「错误码沿用各模块既有枚举…**session 层映射至 acs_status**(表由 CLI-002 **落地**)」——同上。
- **悬空引用**：
  - :24「与 production_call_paths_stage1.csv 的 7 路径一一对应」—— 裸文件名无路径。实际位置 `docs/architecture/production_call_paths_stage1.csv`（`find . -name 'production_call_paths*'` → `./docs/architecture/production_call_paths_stage1.csv`、`_stage2.csv`）。文件存在但引用不可解析（相对本文目录 `docs/api/` 找不到）。**机械列 `dangling_links` 对本份报 `[]`，未捕获此类无尖括号裸文件名 —— 漏报。**
  - :12「(ASTROCS_DESIGN §6.2; phase 仅为内部指代)」—— **§6.2 是「export（Phase3）」的「流程」节**（:440）。承载「phase 仅内部指代、命令名用 normalize/mosaic/export」的是上位 **§7.1**（:489）。这是指向存在文档的错误章节，属**悬空到错章节**级缺陷。
  - :5/:10/:41/:50 `API-001`、`API-002`、`API-003..005`、`CLI-001`、`CLI-002`、`TST-P1-*`、:33 `TB-ARCH-004` —— 不可解析 ID 族（清单第 A 条）。
  - :6 列出的模块名（calibration/star_detector/dynamic_psf/ipv/photometric_calib/algorithms/noise_snr/healpix_drizzle）与 :30-39 表内头文件名（`astro_calibration.h`、`star_detector.h`、`dynamic_psf.h`、`ipv_api.h`、`photometric_calib.h`、`snr_estimator.h`）：其中 `algorithms` 不是仓内模块目录名（上位 §8.4 :622-626 的 `lib/algorithms/` 下并联清单为 calibration/cosmetic/star_detection/psf/platesolve/photometry/noise_snr/drizzle/coverage/sampling/upm/rejection/integration/projection/resample/fits_output/shared，**无 `algorithms` 项、无 `ipv` 项**）⇒ 模块名册与上位 §8.4 目录树用词不一致（详见与上位冲突）。
- **与上位冲突**：
  1. :12 把命令面依据指向 §6.2（Phase3 流程）而非 §7.1（唯一命令树）。
  2. :6 模块链名「calibration/star_detector/dynamic_psf/**ipv**/photometric_calib/**algorithms**/noise_snr/healpix_drizzle」与上位 §8.4 :622-626 冻结的 `lib/algorithms/` 并联命名（star_detection / psf / platesolve / photometry / noise_snr / drizzle）不同名；上位并明文规定「**科学算法目录按并联命名、不用 phase**」（:655）而本文标题与 ID 空间整族用 `Phase1`/`p1_` 前缀（会话层目录允许，本文登记的是算法层函数）。
  3. :19 `async_io_depth∈{0,1,2}(ARCH-004 §2)` —— 引用不可解析 ID 建立硬约束值域，上位 §9 :674 只说「异步 I/O 一个专用线程」，未冻结 `{0,1,2}` ⇒ 机制无上位要点（标准 01 §3）。
- **处置建议**：**合并 + 迁移** —— §1 五段式会话合同迁 `docs/interfaces/PHASE1_SESSION_API.md`（角色：架构设计）；§2 底层模块函数登记**合并入 `docs/contracts/PUBLIC_API.md`** 的对应模块节（同主题单一正本，标准 01 §2），合并时以头文件为签名权威、把「行带 / 无检查点」两套取消口径按 DISP-CAL-008 与 DISP-PHOT-004 的现行登记收敛为一次定档。理由：两处函数名册并存且字段互相矛盾，属同主题双正本，保留双份即保留冲突。

---

### 5. docs/api/PHASE2_API_V1.md

- **路径**：`docs/api/PHASE2_API_V1.md`
- **标题**：Phase2 API 定义 v1 (API-004 冻结 — 数据所有权/thread budget/逐函数)
- **行数**：55
- **角色**：架构设计（阶段 API + 数据所有权合同）。`docs/api/` 无角色 ⇒ **角色错放**，应迁 `docs/interfaces/`。
- **主题**：mosaic 内部流水的七对象所有权图（谁创建/持有/释放/如何跨函数传递）+ 逐函数并发五字段登记 + thread budget 冻结分配。
- **上游**：已标注 —— :3「> 上游：ASTROCS_DESIGN.md §5（mosaic）、§8.4（模块与 ABI）」；§5 正确（:365），**§8.4 号错名对**（应 §8.5）。
- **下游**：14 个文件。命令：`grep -rl PHASE2_API_V1 docs lib eng ASTROCS_DESIGN.md ENGINEERING_SPEC.md ACCEPTANCE_SPEC.md | grep -v '^docs/api/PHASE2_API_V1'` → `docs/algorithms/PHASE2_COVERAGE.md`、`docs/algorithms/PHASE2_MOSAIC_WRITE.md`、`docs/contracts/DATA_SEMANTICS.md`（:1124「### 19.3 数据语义（**唯一权威，对齐 docs/api/PHASE2_API_V1.md 所有权图**）」）、`docs/contracts/PUBLIC_API.md`、`docs/DOCUMENT_INDEX.yaml`、`docs/modules/registry/astrocs.phase2.coverage.md`、`docs/modules/registry/astrocs.phase2.session.md`、`docs/owner/PIPELINE_OVERVIEW.md` 等。**非孤儿**，且被 `docs/contracts/DATA_SEMANTICS.md` 明文认作所有权图的权威对齐源。
- **是否正本**：**正本**（数据所有权图与 thread budget 绑定的唯一承载者；上位 §8.2/§8.3/§9 只给原则）。符号有效性已实测：`p2_coverage_build`、`p2_sample_controls_cached`、`p2_upm_build_geo`、`p2_reject_stack_ex`、`p2_integrate_pixel`、`p2_large_scale_apply`、`p2_acr_block_eligible`、`p2_rejection_semantic_id` 在 `lib/algorithms/coverage/include/astro/phase2/` 各命中 ≥1 头文件 ⇒ 本文 :6「签名权威=现存头文件」的自述**成立**（本批唯一逐条抽验通过的登记面）。
- **重复或重叠对象**：与 `docs/contracts/DATA_SEMANTICS.md` §19.3 重叠，但**方向是下位对齐本文**（DATA_SEMANTICS:1124 自称「唯一权威，对齐 docs/api/PHASE2_API_V1.md 所有权图」）。两份同时自称权威：本文 :6「本文件登记并发合同、**数据所有权**与 thread budget 绑定」，DATA_SEMANTICS:1124「### 19.3 数据语义（唯一权威…）」⇒ 重叠范围 = P2 对象所有权；差异 = 本文给表（:15-22 七对象 创建/持有/释放/传递），DATA_SEMANTICS 给语义散文。属**互指权威的双正本**（并违标准 01 §1「任何文档不声明自己的第二套权威顺序或"唯一口径"」——该条严格说是上位 §0.2 的「同一主题只有一份正本」）。
- **元信息块**：**有**（违 §4）。:5「> ID: API-P2-001  范围: API-P2-001..012  状态: FROZEN；**用户命令面 = `mosaic`**  上游: API-001/002/003  下游: 用户命令 `mosaic`(CLI-001;`phase2 run` 不在命令面上, rc=2)/TST-P2-*」+ :6「> 签名权威=…;本文件登记…;全局状态一律显式登记(验收)」+ :1 标题状态锚。**机械列 `meta_block` 报 `False` —— 漏报。**
- **写法违规**：
  - :1 标题状态编号；:5-6 元信息块（ID/范围/状态/上下游 + 纪律声明「验收」）；
  - :31「none(串行=确定性 **reference**, ARCH-004)」；:43「配置守卫(**ACR-IVAR-001**; 非 cpu/auto 拒)」——任务/实验 ID 混入正文（`ACR-IVAR-001` 与 `TEST-P2SAMPLE-*` 同族，机械列 `task_ids` 对本份报 `[]` ⇒ **漏报**，ACR-*/TEST-* 系 ID 未被其识别器覆盖）；
  - :11-13 所有权图为 ASCII `text` 块内嵌「(Owned: 调用方 session)(Owned: UPM Model 对象)…」，标准 01 §4 要求「流程图用 mermaid，图中文字自解释」——本批 6 份里唯二用 ASCII 图当流程图（另一份见 PHASE3 无图）。判**写法违规（图未用 mermaid）**。
- **悬空引用**：
  - :5/:22/:47 `API-001/002/003`、`CON-008`、`CLI-001`、`TEST-P2SAMPLE-*`、`TST-*`、:24/:45 `ARCH-004`、:51 `SCI-UPM/REJ/INT`、:43 `ACR-IVAR-001` —— `ARCH-004`、`CON-008`、`API-001..003` 属不可解析 ID 族（清单第 A 条）。
  - :6 `lib/algorithms/coverage/include/astro/phase2/*.h` **实存**（目录 12 个头文件，见上表）。
  - 文件级链接：本文无 markdown 链接，机械列 `dangling_links` 报 `[]` —— 与本文实际一致（本份该列**未误报也未漏报**）。
- **与上位冲突**：无实质冲突，两处标注/机制级不符：
  1. :3 上游条款号 §8.4（顶层结构）≠ §8.5（模块与 ABI），与 COMMON_ABI/PHASE1/PHASE3 同一处系统性拷贝错。
  2. :47「`mosaic` 运行预算分配冻结(内部会话 2): sampler=1(串行 reference);upm build=blocks(budget);rejection/integration=行带(budget);async I/O=1;**Σ≤全局 budget**」—— 上位 §9 :680 规定「**并行度沿两个轴分配，两轴之积即同时真正在算的线程数**：帧级并发与帧内并行……调度器按内存闸门与 Runtime lease 联合确定两者，取**使乘积最大**的组合」，且「模块不自决帧级并发、不硬编码帧内度」。本文把 sampler 冻结为 1（串行）且逐 stage 定死宽度，是**下位冻结了上位交给调度器动态求积的参数**，并未指回「取乘积最大」这一上位口径。整治时需说明「stage 内预算」与上位「两轴乘积」的映射关系，否则读作冲突。
- **处置建议**：**迁移** → `docs/interfaces/PHASE2_API.md`（角色：架构设计；标准 01 §2 目录承载固定角色）。迁时订正上游为 §8.5、删 :5-6 元信息块、把 ASCII 所有权图改 mermaid（§4）、与 `docs/contracts/DATA_SEMANTICS.md` §19.3 二选一确立唯一正本（§2 同主题单正本 + §1 不声明第二套唯一口径）；:47 预算冻结补一条指回上位 §9 两轴乘积口径的映射说明。**内容经抽验成立，不宜删除。**

---

### 6. docs/api/PHASE3_API_V1.md

- **路径**：`docs/api/PHASE3_API_V1.md`
- **标题**：Phase3 API 定义 v1 (API-005 冻结 — request/result/HiPS source/WCS output/sampler/coverage/FITS)
- **行数**：63
- **角色**：架构设计（阶段 API + 输入合同）。`docs/api/` 无角色 ⇒ **角色错放**，应迁 `docs/interfaces/`。
- **主题**：Phase3 会话五段式 ABI、`request` 十字段约束、`result` 结构、显式拒绝清单、逐条追溯锚。
- **上游**：已标注 —— :3「> 上游：ASTROCS_DESIGN.md §6（export）、§8.4（模块与 ABI）」；§6 正确（:434），**§8.4 号错名对**（应 §8.5）。
- **下游**：13 个文件（命令：`grep -rl PHASE3_API_V1 docs lib eng ASTROCS_DESIGN.md ENGINEERING_SPEC.md ACCEPTANCE_SPEC.md | grep -v '^docs/api/PHASE3_API_V1'`）：`docs/algorithms/PHASE3_PROJ_IMPL.md`、`docs/KNOWN_LIMITATIONS.md`、`docs/owner/PIPELINE_OVERVIEW.md`、`docs/owner/SCIENCE_OVERVIEW.md`、`lib/algorithms/projection/p3_projection.cpp`、`lib/algorithms/projection/p3_wcs.cpp`、`lib/infrastructure/cli/session_commands.h`、`eng/tests/api/test_p3_api.py`、`eng/tests/backend/test_p3001_science_freeze.py` 等。**非孤儿。**
- **是否正本**：**待定**。request 字段面的正本按标准 01 §2「合同说明→`docs/contracts/`（对应 `eng/contracts/`）」应是 JSON schema，而本文 :18 指向的 schema 文件不存在（见悬空引用）⇒ 现在它**事实上是唯一承载者但依规范不应是正本**。同一文档 :13 声明「Phase3 为新建模块:本文件=施工规格合同;实现(CODE-P3)必须逐字段落地」= 以任务派发口吻写成，非现行设计陈述。
- **重复或重叠对象**：与 `docs/science/PHASE3_HIPS_TO_FITS.md` §1（SCI-P3）重叠 = **输入拒绝面（非目标清单）**。贴两处：
  - 本文 :44「| variance/weight/ivar/flux-per-pixel 输入模式 | ACS_ERR_UNSUPPORTED |」
  - `docs/science/PHASE3_HIPS_TO_FITS.md:10`「**非目标（alpha 显式拒绝项）**：多通道/RGBA HiPS；JPEG/PNG 等 lossy/display-stretch tile；int+BLANK tile；weight/support 输入产品；flux-per-pixel 输入模式；SIN/CAR 等非 TAN 投影；极近极点视场…；GUI。**（variance/ivar 子产品输入为例外：按 §9a-10 必须显式消费传播，不属拒绝项。）**」
  - 差异 = 本文把 `variance/ivar` 一并拒收，SCI-P3 明文**豁免并要求显式消费传播**。同批 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md:303-305` 站在 SCI 一侧（「variance/ivar 按显式消费并传播（输出 `VARIANCE`/`IVAR` 扩展 HDU），两者皆无时显式 `unavailable`（不静默）」）。⇒ 三方（本文 / SCI-P3 / DATA-002）中本文是孤侧，且已有独立登记：`docs/KNOWN_LIMITATIONS.md:102` 把这条列为「FROZEN 合同互相打架」的在册缺陷。**这是两份同主题正本 + 一方与上位冲突，不是下钻分层。**
- **元信息块**：**有**（违 §4）。:5「> ID: API-P3-001  范围: API-P3-001..006  状态: FROZEN；**用户命令面 = `export`**  上游: API-001/002/004, ALG-P3-001..004, ARCH-P3-001  下游: 用户命令 `export`(CLI-001;`phase3 run` 不在命令面上, rc=2)/CODE-P3/SYN-007」+ :6「> Phase3 为新建模块…」+ :1 标题状态锚「(API-005 冻结 …)」。**机械列 `meta_block` 报 `False` —— 漏报。**
- **写法违规**：
  - :1 标题状态编号；:5-6 元信息块；
  - :6「实现(CODE-P3)**必须逐字段落地**,布局测试随 ABI-001 模板**建立**」——任务编号 + 派发口吻 + 历史叙事词「建立」；
  - :5/:35/:58 `CODE-P3`、`SYN-007`、`CLI-001`、`ABI-001` —— 任务/合同编号入正文（机械列 `task_ids` 对本份报 `[]`，**漏报**）；
  - :35「（S+C 合成或 COV 扩展, 由 CODE-P3 按 API-002 manifest 落实, **二选一在实现中冻结**）」——把未决项写进 FROZEN 合同，违 §4「只写现行设计要怎样」。
- **悬空引用**：
  - :18「## 2 request(JSON 字段, **schemas/phase3_request_v1.schema.json**)」—— **文件不存在**。命令与结果：`ls eng/contracts/schemas/ | grep -i -E 'phase3|request'` → 仅 `phase_config_export.schema.json`、`run_manifest.schema.json`；`find . -name 'phase3_request*'` → 0 命中（正对照同命令 `find . -name 'DATA-002*'` → 1）。该缺陷已在 `docs/KNOWN_LIMITATIONS.md:107` 在册：「`docs/api/PHASE3_API_V1.md §2` 引 `schemas/phase3_request_v1.schema.json`…**三者均不存在**」并记录了判据盲区（`check_doc_index.py --strict` rc=0 不覆盖该路径）。**机械列 `dangling_links` 对本份报 `[]`（其 :3/:1 报 `True`）—— 漏报**：该引用不是 markdown 链接语法，工具未捕获。
  - :24「见 GATES §3 G-P1-WCS-BRIDGE / -GLOBAL 与 `p3_wcs_roundtrip_gate()`」——「GATES」无路径；`p3_wcs_roundtrip_gate()` 在 `lib/algorithms/projection/` 可查（本次未展开代码核验，文档层判「无定位编号指代」）。
  - :5/:27/:47/:49/:55/:56/:57/:58 `ALG-P3-001..004`、`SCI-P3`、`ARCH-P3`、`API-001/002/004`、`CLI-001`、`CODE-P3`、`SYN-007`、`ABI-001` —— `API-001/002/004`、`ABI-001`、`ARCH-P3`（裸写，实存文件名为 `docs/architecture/PHASE3_MODULE_ARCH.md`，ID `ARCH-P3-001`）属命名不匹配族；`SCI-P3`/`ALG-P3` 通过 `docs/science/PHASE3_HIPS_TO_FITS.md`、`docs/algorithms/PHASE3_RESAMPLE.md` 可解析（`docs/owner/SCIENCE_OVERVIEW.md:100` 即如此映射）。
- **与上位冲突**：
  1. :44 variance/ivar 拒收 ← 与上位科学正本 `docs/science/PHASE3_HIPS_TO_FITS.md:10` 的显式豁免直接相悖（标准 01 §1「下位与上位冲突时以上位为准；冲突本身是缺陷」）。
  2. :24 `scale_deg_per_px` 一格内自述「**构造下限只有 >0**，与 WCS 往返门无关；但往返**紧门** 1e-8 px 的适用域是 `scale ≥ 0.9″/px`…退回全域保守门 1e-6 px」—— 一个字段约束格里塞进两套门的适用域与数值，属把科学/算法口径（数值容差）抄进接口合同；标准 01 §4「公式、数值表…放对应下层文档」⇒ **层级倒置**。
  3. :30「`coverage_output` | 枚举 | "mask"(二值) 单值;通道/weight 模式不存在」与 :44 weight 拒收一致，但与上位 §6.2/§6.4 的 coverage 语义条款未建立指针；本文 :5 声称上游含 §6，全文未出现 §6.x 的条款号引用 ⇒ §3「双向可追溯」在本份只到章节粒度。
- **处置建议**：**合并 + 迁移** —— 生命周期五函数与 `result` 结构迁 `docs/interfaces/PHASE3_API.md`（角色：架构设计）；§2 request 字段面**合并入合同说明层**（`docs/contracts/` + 对应 `eng/contracts/schemas/` 的实存 schema，标准 01 §2「JSON/schema/字段语义→docs/contracts/」，并**先补落 schema 文件**再删本文该节，避免把唯一承载者删成悬空）；§4 拒绝清单**按上位 SCI-P3 §1 的豁免订正后保留为指针**（不得与科学正本并存两套口径）。:18 断链、:6「二选一在实现中冻结」的未决项一并清除。

---

### 7. docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md

- **路径**：`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`
- **标题**：DATA-002 三阶段产品交换合同（Phase Product Exchange）
- **行数**：311
- **角色**：**混合** —— 主体是架构设计（数据交换接口合同），**§2a 的 `invalid_handling` 段（:107-206，约 100 行）是科学定义**（NaN 处置、方差传播式、量纲表）。目录 `docs/interfaces/` 对架构设计部分**落位正确**；科学段**角色错放 + 层级倒置**，应迁 `docs/science/`（标准 01 §2 科学定义行 + §1「科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`」）。
- **主题**：三阶段产品角色 ↔ type 绑定、交换对象文档结构、跨 Phase 仅磁盘交换、兼容矩阵与拒绝条件。
- **上游**：已标注 —— :3-6「> 上游权威：`ASTROCS_DESIGN.md` §1.2（三命令平级独立）/**§9（I/O 与原子产品）**…」。§1.2 正确（:71「三个命令，三个独立产品」）；**§9 号错** —— 上位 §9 标题是「CPU 后端与资源」（:669），「I/O 与原子产品」是 §10（:684）。:277「## 6. 验收映射（依据：本文件 §4 + `ASTROCS_DESIGN.md` §9）」同一处错号复现。
- **下游**：23 个文件按路径引用（命令：`grep -rl DATA-002_PHASE_PRODUCT_EXCHANGE docs lib eng ASTROCS_DESIGN.md ENGINEERING_SPEC.md ACCEPTANCE_SPEC.md | grep -v 'docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE' | wc -l` → `23`），含 `docs/science/DRIZZLE.md`、`docs/science/UNIFIED_SCIENCE_MODEL.md`、`docs/standards/NUMERIC_STANDARD.md`、`docs/standards/STANDARDS_REGISTRY.md`、`docs/contracts/DATA_SEMANTICS.md`、`lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp`。**注意引用方向倒挂**：科学正本与标准层反向引用一份 interfaces 文档，说明该文档实际被当作科学口径正本使用（与角色判定互证）。
- **是否正本**：交换合同（角色↔type、矩阵、拒绝条件）= **正本**（与 `eng/contracts/data/phase_product_exchange.schema.json` + `_matrix.json` 三形态对应，符合标准 01 §2「合同说明与 schema 双向对应」）；§2a NaN/方差处置段 = **副本**（正本在 `docs/science/`，见与上位冲突）。
- **重复或重叠对象**：§2a 的 NaN 处置文字在仓内**四处并存**，本文是其中最全的一份。贴两处：
  - 本文 :154-156「**样本级掩膜（只作用于不合格样本）**：不合格样本（值非有限）**从该输出像素的分子、分母、方差三项中一并剔除**（`F_p = Σ_合格 x_j w_jp`、`W_p = Σ_合格 w_jp`），并**重新归一**」
  - `docs/science/PHASE3_HIPS_TO_FITS.md:10`（拒绝项）与 `ASTROCS_DESIGN.md:426`（§5.5 末条）「NaN 采用样本级掩膜：污染样本掩除后重归一、覆盖级缺数置 NaN 并强制计数，**规则见 `docs/science/` 对应章节**」
  - 差异 = 上位只给一句并下沉到 `docs/science/`；本文给出完整定义表（:124-133 八术语）、三条分母量纲表（:141-143 含 `variance_p = Σ_j v_j·w_jp² / D_p²`）与处置规则 1/1a/2/3/4。本文 :208-210 自认「口径归属：NaN 处置以 `rule_id = NAN-SAMPLE-MASK-COVERAGE-NAN` 为准…**`docs/standards/NUMERIC_STANDARD.md`（§MUST）与 `docs/standards/STANDARDS_REGISTRY.md`（D.drizzle `DISP-DRZ-004`）引用同一份文字**」—— 同一段科学文字四处在手，且其中两处（NUMERIC_STANDARD、STANDARDS_REGISTRY）连目录角色都不该承载它。**同主题多正本，举证成立。**
- **元信息块**：:3-10 开头块含「上游权威 / 机器形态 / 下游」三行。判读要分清：**上游标注是标准 01 §3 明确要求的**（「每份下层文档在开头位置标明其上游最高设计条款」），不算违规；本块的缺陷只在于条款号错（§9≠§10）。「机器形态 + 下游」两行属登记表内容，宜由 `docs/DOCUMENT_INDEX.yaml` 承载。**无 ID/状态/版本锚 → 本份不判 §4 元信息块违规。**
- **写法违规**：
  - :168「**Phase3 承载面（已冻结，2026-09-23）**：`docs/contracts/DATA_SEMANTICS.md` §30.7…」——**日期 + 状态锚「已冻结」**入正文。机械列 `dates=[168]` **命中，未漏报**；
  - :95「`sparse_snr` 是稀疏 SNR 层在交换合同中的位置（**GAP_AUDIT G09**）」——控制包工件当条款依据；
  - :220-222「（Phase1 输出单帧标准化 HiPS 含 SCI/variance/support/mask 四平面；**13 标准 §2**）」三处「13 标准 §2」= 无路径的编号指代（见悬空引用）；
  - :104「本合同与该 schema 不一致时，**以本合同为准补齐 schema**」、:212「不一致时以本块为准」—— 下位文档自设冲突裁决顺序，违标准 01 §1「任何文档不声明自己的第二套权威顺序或『唯一口径』」与上位 §0.2 同条；正确写法是回上位/回 schema 定档；
  - :303-305 同一条目内「两者皆无时**显式 `unavailable`**（不静默）」整句**逐字重复两次**（:304 与 :305），且 :305 把 :303 的「variance/ivar 按显式消费并传播」整句重述一遍 —— 文档自身未经校对，违标准 01 §4「图与正文相互印证」所要求的自洽。
- **悬空引用**：
  - `DATA-001` —— 全文出现 12 次（:6、:31、:40-41、:67、:85-88、:135 系注释、:263、:267、:274-275、:297），并明文「本文档/本 schema 只做绑定，不重复登记；未知/未登记 type_id 由 **DATA-001 registry 拒绝并传播**」。**跟踪集内无任何 DATA-001 文档**：`git ls-files -- 'docs/contracts/DATA-001*' 'docs/interfaces/**/DATA-001*'` → 0（正对照同形命令 `git ls-files -- 'docs/contracts/ARCH-*'` → `docs/contracts/ARCH-001.md`）。它的"正本"在两处各自成立又互相错位：①`eng/contracts/data/artifact_manifest.schema.json:4` `"title": "ACSD Artifact Manifest v1 (DATA-001 冻结)"`（机器 schema 借用该 ID）；②**它原本是一份控制包任务书** `工程控制/PROJECT-GOVERNANCE-01/tasks/DATA-001.md`，首行「# 任务：DATA-001 建立统一数据对象合同链」、次行「状态：`NOT_STARTED`」，**已于 `ae0f1e63 chore(governance): 清理历史控制包与一次性报告（CLEAN-402）` 删除**（`git log --diff-filter=D -- '工程控制/PROJECT-GOVERNANCE-01/tasks/DATA-001.md'` 实证；`git merge-base --is-ancestor ae0f1e63 HEAD` → YES）。⇒ 本段「DATA-001 冻结」的引用**既无文档、也无任务书**，而原始任务状态是 `NOT_STARTED`。全仓 58 个文件引用 `DATA-001`（`grep -rl 'DATA-001' docs eng lib | wc -l` → 58），`docs/DOCUMENT_INDEX.yaml` 内 0 命中，`eng/ci/id_migration_map.json` 内 0 命中 ⇒ **DATA 系列的根条款无文档**，本份的 §2a/§5 全部把严格语义「DATA-001 冻结」当既定权威引用，冻结语义无上位可读载体。完整举证见 §3-A。
  - :10 `RT-002`、:234 `RT-002`、:289 `RT-002` —— 无文件（`find . -name 'RT-002*'` → 0；`docs/contracts/` 只有 `RT-001.md`，其 :5 自declare 下游「RT-002..RT-009」，即整个系列都不存在）。
  - :220-222「13 标准 §2」—— 无该文档名；唯一近似物在 SCI-P3 :9 也写作「控制包 13 §2」，即指向 `工程控制/` 一次性工件（同 CLI_PROTOCOL 的 GAP_AUDIT 类缺陷）。
  - :26、:282 的 `eng/contracts/data/examples/*.example.json` 与 `external_fixture_hips.example.json` —— **实存**（`ls eng/contracts/data/examples/` → `external_fixture_hips.example.json`、`phase1_product_v1.example.json`、`phase2_mosaic_v1.example.json`、`phase3_planar_fits_v1.example.json`、`frame_hips_manifest.example.json`）。**机械列 `dangling_links` 把 :26 的 glob 判成断链 —— 误报**（通配符被当字面路径）。
  - :3、:4、:5、:6、:7、:8、:9、:40、:240、:273、:291 指向的 schema/matrix/registry/validator/test 共 11 个路径**全部实存**（逐个 `[ -e ]` 核过）。
- **与上位冲突**：
  1. :3 上游条款号 §9（CPU 后端与资源）≠ §10（I/O 与原子产品）；:277 复现同错。
  2. :302「**阶段三接受域（最高设计 §5.1/§5.3）**：export 接受任一合同兼容 HiPS」—— 上位 §5 是 **mosaic（Phase2）**（§5.1 使命、§5.3 SNR 重建与逆方差叠加）；export 是 §6。条款号指向错误阶段。
  3. :104/:212「不一致时以本合同为准」自设权威顺序 ← 违上位 §0.1/§0.2（唯一权威链、下级不另立权威）。
  4. :126「（上游授权 = `ASTROCS_DESIGN.md` §5.5「NaN 采用样本级掩膜」，该条只对 NaN 授权；§0.1「每一层只由它的上一层推出」）」—— 本句是**下位替上位做授权范围裁定**（「该条只对 NaN 授权」），上位 §5.5 原文只说「规则见 `docs/science/` 对应章节」，未做此限定；越权解释上位。
  5. 与同批 `docs/api/PHASE3_API_V1.md:44`（variance/ivar 拒收）实质对立（本文 :303-305 要求显式消费传播）—— 同主题两份接口合同给出相反处置，本文与科学正本一致。
- **处置建议**：**保留主体 + 拆分科学段** —— §1/§2（角色↔type、交换对象结构）、§3（R-DISK-ONLY）、§4（矩阵）、§5（拒绝条件）**保留**于 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`（落位正确、与 `eng/contracts/data/` 三形态双向对应，符合标准 01 §2 + §3）；§2a 的 :107-206（`invalid_handling` 定义表 / 分母量纲表 / 处置规则 1-4 / 六行输入对照）**合并到 `docs/science/` 对应章节**（依上位 §5.5 的下沉指令 + 标准 01 §1「科学公式与算法推导的唯一权威是 docs/science/」），本文只留 `rule_id` 指针；:104/:212 的「以本合同为准」改为「不一致即为缺陷、按变更流程定档」（依 §1）；:168 日期与「已冻结」删；:220-222「13 标准 §2」与 :95「GAP_AUDIT G09」改指现行条款；**并先补落 DATA-001 的文档层正本（`docs/contracts/` 下的 artifact manifest 合同说明）或在 DOCUMENT_INDEX 登记 DATA-001 → `eng/contracts/data/artifact_manifest.schema.json` 的映射**，否则本份所有「DATA-001 冻结」引用继续悬空。

---

### 8. docs/interfaces/data/DATA-003_PRODUCTION_ARTIFACT_STORE.md

- **路径**：`docs/interfaces/data/DATA-003_PRODUCTION_ARTIFACT_STORE.md`
- **标题**：DATA-003 生产 ArtifactStore 接线（设计权威）
- **行数**：107
- **角色**：架构设计（I/O 与原子产品接线）。目录 `docs/interfaces/` 对架构设计**落位正确**；但内容主体（§3/§4/§7 的写路径、读路径、hash 重算步骤序列）落到函数级步骤，属**具体设计**层（标准 01 §1 L3），与 `docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md` 的职责重叠（见重复对象）。
- **主题**：生产 ArtifactStore 的接线结构、写/读路径、六类失败语义与恢复、spy 证明读写全经 Store、manifest hash 可重算。
- **上游**：已标注 —— :3「> 上游：ASTROCS_DESIGN.md §10（I/O 与原子产品）」，**条款号与条款名都对**（与 :11、:18-19 不一致，见与上位冲突）。
- **下游**：按路径引用仅 2 个文件 —— `docs/DOCUMENT_INDEX.yaml`、`lib/infrastructure/aio/runtime/artifact_store/production_store.py`（命令：`grep -rl DATA-003_PRODUCTION_ARTIFACT_STORE docs lib eng ASTROCS_DESIGN.md ENGINEERING_SPEC.md ACCEPTANCE_SPEC.md | grep -v 'docs/interfaces/data/DATA-003_PRODUCTION_ARTIFACT_STORE'`）。**但按合同 ID 引用有 18 个文件**（命令：`grep -rl 'DATA-003' docs eng lib | wc -l` → 18；样例：`docs/interfaces/data/DATA-002:10`、`DATA-004:6`、`docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md`、`eng/contracts/data/artifact_types.registry.json`、`lib/include/astrocs/contracts/artifact_abi_v1.h`、`lib/include/astrocs/io/aio_abi_v1.h`、`eng/tests/artifact/test_production_store.py`）。⇒ **不得按路径串 0/2 命中判孤儿**：本仓这些合同的引用键是 `DATA-00N` 裸 ID 而非文件路径，正是派单前言 §5.2 提示的整串检索陷阱。
- **是否正本**：ArtifactStore 接线与失败语义 = **正本**（唯一承载者）；manifest 字段形态 = **副本**（正本 `eng/contracts/data/artifact_manifest.schema.json`，即 DATA-001 的实身）。
- **重复或重叠对象**：与同批 `docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md` 重叠 = **原子发布序列与 run 私有磁盘布局**（本份读取 IO_003 后由「疑似」升为**举证成立**）。贴两处：
  - 本文 :59-60「原子 rename：先内容、再 manifest（manifest rename = 完成标记）、再 sidecar；每步前 fsync 文件与目录。」＋磁盘布局 :42-45「`{root}/runs/{run_id}/stage/` 临时对象（未发布） / `{root}/runs/{run_id}/objects/{artifact_id}` 已发布内容（原子 rename） / `{root}/runs/{run_id}/manifests/{artifact_id}.manifest.json` COMPLETE manifest / `…manifest.sha256` manifest hash sidecar」
  - IO_003 :21-22「`临时写（run 私有 stage）→ 关闭/fsync → fitsverify（结构 + DATASUM）→ sha256 → 原子 rename → 最后原子落 manifest.json(COMPLETE) = 完成标记`」＋布局 :58-60「`{output_root}/runs/{run_id}/stage/` run 私有临时写区（未发布） / `{output_root}/runs/{run_id}/products/{user_path}/` 已发布目标（唯一用户路径） / `…/products/{user_path}/manifest.json` 完成 manifest」
  - 差异 = **同一「原子发布 + 唯一完成标记」机制的两套实现与两套目录布局**：本文已发布内容落 `objects/{artifact_id}` 且 manifest 单独落 `manifests/`，IO_003 落 `products/{user_path}/` 且 manifest 与内容同目录；本文 rename 次序是「内容→manifest→sidecar」，IO_003 是「逐个 os.replace 全部内容 → 最后 manifest」；本文执行体 `lib/infrastructure/aio/runtime/artifact_store/production_store.py`，IO_003 执行体 `lib/infrastructure/aio/io/hips_output_store.py`（两者均实存，本次 `[ -e ]` 核过）。⇒ 同主题两份正本，违标准 01 §2「同一角色内同一主题只有一份正本；多份内容合并为一份」。IO_003 :10/:99/:184 自称「与 DATA-003 同模式 / DATA-003 语义 / 与 DATA-003 writer 唯一 producer 同纪律」——即它把本文当语义源，却另立一套布局与次序，属「引用而不复用」的双正本形态。
- **元信息块**：**有**（违 §4）。:5-9「> 权威文档形态（本文）+ 执行形态(`…production_store.py`) + 验收测试(`…test_production_store.py`)。**三形态必须同步修改**。> 前序合同: DATA-001（typed manifest schema + 唯一 producer）、DATA-002（三阶段产品交换、跨 Phase 仅磁盘交换）；运行时隔离: RT-002（phase-isolated Runtime）」—— 其中「三形态必须同步修改」是**纪律声明**，「前序合同 / 运行时隔离」是**依赖台账**，两者属标准 01 §4 禁的元信息块内容；**上游标注（:3）单独看是 §3 要求的、合规**。另 :1 标题「（设计权威）」是**版本/权威锚**。
- **写法违规**：
  - :1 标题内「（设计权威）」= 自设权威声明，直接违标准 01 §1「任何文档不声明自己的第二套权威顺序或『唯一口径』」。**本批唯一一处标题级自设权威，性质最重。**
  - :5-9 元信息块（纪律声明 + 依赖台账）；
  - :102-107「## 8. 边界（非目标）」整节为任务范围叙事：「不做 RT-007 checkpoint 表、不做 LOG/RT 溯源字段（DATA-004 范围）」、「Windows 正式 DLL 交付按同语义 C 接线复刻，**属于 DATA-004/WIN 后续范围**」—— 任务编号（RT-007/WIN）+ 未派生的排期叙事，属过程台账（§2「过程台账不落正式文档层」）；
  - 无日期、无 commit、无 sha256（机械列 `dates=[]`、`task_ids=[]`、`commit_sha=[]`、`sha256=[]` **均正确，未漏报未误报**；注：`RT-002`/`RT-007`/`DATA-001` 未被 `task_ids` 识别器覆盖，若按 §4「任务编号（EXP-/RELEASE-/FIX- 等）」的「等」字扩展，本份 :9/:105/:107 的 RT-* 与 :104 的「DATA-001/002 已冻结」**属机械列漏报**）。
- **悬空引用**：
  - `DATA-001` :9、:21、:36、:50、:53、:62、:97、:104 —— 见 DATA-002 条目的举证（`find . -iname '*DATA-001*'` → 0）。本份把它当作「manifest 完整校验」的字段权威源（:53「`stage_manifest(id, doc)`：**DATA-001 manifest 完整校验**（缺字段/NaN/重复 producer/未知 type/非法 digest/status≠COMPLETE 全拒）」）⇒ **校验规则的条款清单在本文档层无处可读**，只能读 schema JSON。
  - `RT-002` :9、`RT-007` :105、`WIN` :107 —— 无对应文件（`find . -name 'RT-002*' -o -name 'RT-007*'` → 0）。
  - :6 `lib/infrastructure/aio/runtime/artifact_store/production_store.py`、:7 `eng/tests/artifact/test_production_store.py` —— **实存**。
  - :20 `docs/architecture/ARCHITECTURE.md` —— 实存（但该份 :5 元信息块自declare「上游: ARCH-001」，属不可解析 ID 族；文件级链接本份不断）。
- **与上位冲突**：
  1. :3 上游标 §10（正确），**但 :11 节标题「## 1. 目标（依据：本文件 + `ASTROCS_DESIGN.md` §9）」与 :18-19「约束来源: `ASTROCS_DESIGN.md` §1.2/§9（三 Phase 隔离产品命令；阶段间只通过原子发布、哈希和 provenance 完整的磁盘产品/manifest 交换）」指向 §9=CPU 后端与资源**。所引语义实际在上位 §8.1（:548「阶段间唯一的交换媒介是磁盘 HiPS 产品 + manifest + 哈希（§10）」）与 §10（:688-693）。同一文档内上游标注自相矛盾（:3 对、:11/:18 错），且 :18 把「§9」注成了 §10 的内容 —— 与本批 5 份复现的「§9/§10、§8.4/§8.5 号名错位」同一根因。
  2. :33「Runtime 启动真实 Store；模块 `execute` 只能拿已校验 handle/reader/writer」与上位 §8.4 :658「科学模块不读全局配置、…、不绕过 aio」一致，无冲突；:35-36「任何真实文件系统路径解析只发生在 `ArtifactStore`/`StoreIO` 内部」与上位 §10 :686「`aio` 是文件级唯一 I/O 边界」**角色冲突** —— 上位把唯一 I/O 边界给 `aio`，本份把路径解析唯一权给 Store/StoreIO（`production_store.py` 位于 `lib/infrastructure/aio/runtime/` 之下，可解释为 aio 内部子层，但本份未如此声明）。整治时应补一句归属声明，否则读作第二个 I/O 边界正本。
- **处置建议**：**保留 + 就地订正** —— 落位（`docs/interfaces/data/`）正确、内容为唯一正本、下游经 ID 广泛引用，删除或迁移代价大于收益。必改：删 :1 标题「（设计权威）」并删 :5-9 的「三形态必须同步修改」纪律声明（依标准 01 §1 不自设权威 + §4 无元信息块）；把 :11/:18 的「§9」订正为「§8.1 + §10」；:102-107 边界节的 RT-*/WIN 排期叙事移出正式文档层（依 §2 台账不落正式层）；:9/:21/:36 等处的「DATA-001」在 DOCUMENT_INDEX 建立到 `eng/contracts/data/artifact_manifest.schema.json` 的显式映射（依 §3「登记了但文件不存在同样是缺陷」的反向情形：被引用而无处可登记 = 索引缺口）。

---

### 9. docs/interfaces/data/DATA-004_PRODUCT_PROVENANCE.md

- **路径**：`docs/interfaces/data/DATA-004_PRODUCT_PROVENANCE.md`
- **标题**：DATA-004 产物溯源与版本语义（Product Provenance）
- **行数**：165
- **角色**：架构设计（溯源与版本语义合同）；§2 的 digest 公式属算法推导性质（标准 01 §1 该权威在 `docs/algorithms/`）。目录 `docs/interfaces/` 对合同部分**落位正确**；§2 公式部分**层级偏低**（可接受，因 §4「公式放对应下层文档」中的「下层」相对最高设计而言 `docs/interfaces/` 已是下层 —— **不判违规**，只判「同一文档混两种角色」需在索引里标注主题双属性）。
- **主题**：六类 revision 的区分与校验规则、确定性 provenance digest 公式、ArtifactStore 的 provenance sidecar 接线、发布/消费五道语义门、privacy scan。
- **上游**：已标注 —— :3-6「> 上游权威：`ASTROCS_DESIGN.md` §1.2/§9（三阶段仅通过原子发布、哈希与 provenance 完整的磁盘产品/manifest 交换）、DATA-001…DATA-002…DATA-003…」。**§1.2 正确；§9 号错名对**（所引语义在 §8.1/§10，与 DATA-003 :18 同一处错）。:137「## 6. 验收映射（依据：本文件 + `ASTROCS_DESIGN.md` §9）」复现同错。
- **下游**：按路径 2 个文件（`docs/DOCUMENT_INDEX.yaml`、`lib/infrastructure/aio/runtime/artifact_store/provenance.py`；命令同 DATA-003 形如 `grep -rl DATA-004_PRODUCT_PROVENANCE …`）；**按合同 ID 20 个文件**（`grep -rl 'DATA-004' docs eng lib | wc -l` → 20，含 `docs/algorithms/PHASE3_PROJ_IMPL.md`、`docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md`、`docs/modules/registry/astrocs.phase2.coverage.md`、`docs/modules/registry/astrocs.phase3.wcs.md`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`lib/algorithms/coverage/module.yaml`）。**非孤儿，且被模块 `module.yaml` 与溯源矩阵引用 ⇒ 与 `docs/contracts/` 的机器登记有实绑定。**
- **是否正本**：**正本**（provenance 语义与 revision 类别的唯一文档层承载者）。但版本语义与根文档 `docs/VERSIONING.md` 有主题交叠（见重复对象）。
- **重复或重叠对象**：
  - 与同批 `docs/api/MANIFEST_VERIFY_V1.md` §2.1 重叠 = **溯源字段语义**。本文 :68「`source_commit`, # 40 hex 源码 commit（调用方/运行图给出；**绝不自行猜 git**）」对照 MANIFEST_VERIFY_V1 :45「`"source_sha": "<40hex>", // CMake **configure 期** HEAD（version_generated.h）—— **不是构建指纹**；相等不蕴含同一二进制（VERSIONING §2.1）」：字段名不同（`source_commit` vs `source_sha`）、语义层级不同（前者由调用方注入、后者由 configure 期生成），但都在描述「产物的源码版本锚」⇒ 两处并存且互不引用，标准 01 §2 同主题单正本不成立。
  - 与根文档 `docs/VERSIONING.md` 重叠 = **版本语义**。本文 §1 六类 revision 表（:39-44）自定 `revision.product` 形态 `0.1.0-alpha.1-linux-amd64-gcc14`；MANIFEST_VERIFY_V1 :65 已把该语义指向「`docs/VERSIONING.md` §2.1」⇒ 版本口径正本在 VERSIONING.md，本份 §1 的 product/abi/data_schema 三类定义属**副本**（本次未逐行读 VERSIONING.md 全文，按 §5.6 该条判「重叠成立、差异细节待证」，两处段落已贴）。
- **元信息块**：**有**（违 §4 部分）。:3-11 块中「上游权威」合规（§3 要求）；**「机器形态：… （provenance 层，执行校验器）、… （DATA-004 接线：… 版本门 / 消费门）、eng/tests/…（验收测试）」与「下游：RT-002…、RT-006…、IO-003…、LOG-001…」两行是登记台账**，应由 `docs/DOCUMENT_INDEX.yaml` 承载（§3「登记表与映射表承载定义与映射」）。**机械列 `meta_block` 报 `False` —— 本份判 `False` 可辩护（无 ID/状态/版本锚），但需注明工具的判定口径只认 ID/状态关键词。**
- **写法违规**：
  - :144、:148-149「测试：`eng/tests/artifact/test_provenance.py`（**61 测试**，无第三方依赖）；DATA-003 基线 `eng/tests/artifact/test_production_store.py` **30/30 保持通过**（未配置 Store 行为不变）」—— **运行结果计数台账**（具体通过数、"保持通过" 的时间对比叙事）写入正式文档；标准 01 §2「需要留证的台账落 `artifacts/evidence/` 并由正本引用」+ §4「只写现行设计要怎样」。只读节点无法复算该两数（禁跑测试），即其存在本身不可核 ⇒ 属应移除项；
  - :159-160「RT-006 trace 溯源字段、LOG-001 脱敏接线、Windows 正式 DLL 同语义 C 复刻**属于后续任务范围**」—— 排期叙事 + 任务 ID；
  - :35/:60/:90/:110/:127/:137 节标题内反复出现「（DATA-004 冻结语义）」「（production_store.py DATA-004）」= 合同 ID 当节名后缀（状态锚变体）；
  - :27「**privacy scan**：provenance 相关文本/诊断不泄露绝对用户路径与凭据」与 :129「（与 **LOG-001** redact 模式对齐）」—— 不可解析 ID；
  - 正文无日期、无 commit、无 sha256（机械列 `dates=[]`、`commit_sha=[]`、`sha256=[]` **正确**）。注：:68「40 hex 源码 commit」是**字段语义描述**，不判 §4 违规（机械列亦未误报，判定正确）。
- **悬空引用**：
  - `DATA-001` :6、:33、:41、:53、:135、:153 —— 无文档（举证见 DATA-002 条目）。本份 :135 甚至写明「storage_uri/artifact_id 词法层由 **DATA-001** 拒绝裸路径」，即一条硬禁的规则来源无文档。
  - `RT-002` :11、`RT-006` :11/:159、`IO-003` :11、`LOG-001` :11/:129、`IO-002` —— `RT-002`/`RT-006`/`LOG-001` **无对应文件**（`find . -name 'RT-002*' -o -name 'RT-006*' -o -name 'LOG-001*'` → 0；LOG 语义的实身是 `docs/architecture/observability/STRUCTURED_LOGGING_CONTRACT.md`，其内部 ID 为 LOG-001，见 CLI_PROTOCOL :54 —— 即 ID→路径的映射仅存在于文档正文的偶发说明里，索引层无登记）。`IO-002`/`IO-003` 对应 `docs/interfaces/io/IO_002_*`/`IO_003_*` —— **文件名用下划线 `IO_00N_*`、引用写连字符 `IO-00N`，同目录内两种命名法并存**（本批 IO 三份文件名全部 `IO_00N_` 前缀，而正文与 ID 引用写 `IO-001/002/003`）。
  - :7、:9 `lib/infrastructure/aio/runtime/artifact_store/provenance.py`、`…/production_store.py`、:10/:148 `eng/tests/artifact/test_provenance.py`、:149 `eng/tests/artifact/test_production_store.py` —— **全部实存**。
- **与上位冲突**：
  1. :3/:137 上游条款号 §9（CPU 后端与资源）≠ §8.1/§10（磁盘产品与 manifest 交换），与 DATA-003 :18 同一处错。
  2. :44 revision 类别表的 `history` 类与 :87-88「`history` / `doc_revision` 是文档形态与替换链展示，不参与 digest」—— 上位 §10 :693 的 manifest 必记清单**不含**「被替换版本链」这一机制；本份 :24 却把它列为**发布门**（「被替换的 product 版本不静默接收：发布门（history 已替换版本拒绝重发）」）⇒ 新增了一个上位无要点的硬门，违标准 01 §3「追溯不到的机制先补要点再保留」。
  3. :104「未配置的 Store（DATA-003 冻结形态）发布行为完全不变」与同批 DATA-003 :62-63「发布物保持严格 DATA-001 manifest 形态（`additionalProperties=false`，不附加内部字段）」一致（本份 :32 亦明文「provenance 以独立 sidecar 旁路持久化，manifest hash 语义不变」）⇒ **无冲突，且是两文档互相自洽的正面样本**，登记为整治时的对齐范式。
- **处置建议**：**保留 + 就地订正**（与 DATA-003 同判）—— 内容为唯一正本、下游绑定实（`module.yaml` + 溯源矩阵）。必改：删 :144/:148-149 的测试通过数与「保持通过」叙事（移 `artifacts/evidence/` 并由本文引用，依标准 01 §2 台账落证据层）；:3/:137 上游条款号 §9 订正为 §8.1/§10；:11/:159 的 RT-002/RT-006/LOG-001 换成可解析路径或先在索引里建立 ID→路径映射；:33/:41/:135 的 DATA-001 引用同上；§1 revision 表中 `product`/`abi`/`data_schema` 三类若与 `docs/VERSIONING.md` 口径重叠，按 §2「同主题单正本」收敛到 VERSIONING 并在此留指针；:24 的 history 发布门需上位补要点。

---

### 10. docs/interfaces/io/IO_001_FITS_STREAM_INTERFACE.md

- **路径**：`docs/interfaces/io/IO_001_FITS_STREAM_INTERFACE.md`
- **标题**：IO-001 FITS 流式接口冻结合同
- **行数**：206
- **角色**：架构设计（I/O 接口契约）。目录 `docs/interfaces/io/` = 标准 01 §2 架构设计角色允许目录 ⇒ **落位正确**。
- **主题**：FITS 流式 C ABI（header/plane/chunk 三级读 + 原子写 + DATASUM/CHECKSUM verify）、14 个 `ACS_FIO_*` 错误码、DLL 边界与第三方隔离、trace bytes hook。
- **上游**：已标注但**条款号与条款名同时错** —— :5「> 上游权威：`ASTROCS_DESIGN.md` §7.3（DLL C ABI 边界）+ 本文件 + `docs/DOCUMENT_INDEX.yaml` 登记」。上位 §7.3 是「错误传播与运行日志」（:515）；DLL/ABI 边界条款在 §8.5（:661-664「跨 DLL 不传 STL/异常/RTTI…」）与 §8.4 :659。**本批唯一一处条款号与条款名双双错位的上游标注**，且 :5 把「本文件」与「DOCUMENT_INDEX 登记」并列写进「上游权威」= 文档自称权威源（违标准 01 §1「任何文档不声明自己的第二套权威顺序或『唯一口径』」）。
- **下游**：按路径 7 个文件（`docs/DOCUMENT_INDEX.yaml`、`docs/standards/STANDARDS_REGISTRY.md`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`lib/infrastructure/aio/io/include/astrocs/io/fits_stream_v1.h`、`lib/infrastructure/aio/io/fits_core.c`、`eng/tests/io/`）；按合同 ID `IO-001` **89 个文件**（命令：`grep -rl 'IO-001' docs eng lib | wc -l` → 89）。**非孤儿，本批被引最广。**
- **是否正本**：**待定**。FITS 流接口面（`fits_stream_v1.h` 的 C ABI 与错误码）在 interfaces 层是唯一承载者；但 FITS 读写语义的正本按上位 §10 :686「`aio` 是文件级唯一 I/O 边界：任何文件读写经 aio」应属 `aio`，而本份 :18/:190-191 明文「`lib/infrastructure/aio`（aio_fits 等全图像读写实现）…保持原样」「AIO，含 CFITSIO 静态链：全图像读写（aio_read/write_fits），保留作兼容层；**IO-001 不迁移/不修改**」⇒ 同一仓内并存两套 FITS 读写接口合同，本份是新增的那一套且不自称正本。**判「待定」而非「正本」。**
- **重复或重叠对象**：与 `lib/infrastructure/aio`（既有 aio_fits 产线）重叠 = **FITS 读写 + checksum + verify 语义**。贴两处：
  - 本文 :128「`fits_compute_file_datadigest`：**独立实现**的 FITS DATASUM 算法（32 位 1 的补码块校验，按 2880 字节块、16-bit 大端字累加、进位回卷），返回 10 位十进制字符串；算法独立（不链接 CFITSIO）」
  - 同批 IO_002 :213-217「IO-002 **不重新实现 FITS 解析**：`acs_hips_read_tile_plane_f*` 内部对拼出的 `NorderK/DirD/NpixN.fits` 调用 IO-001 `acs_fio_reader_open_v1`」＋ IO_003 :104「`fits_verify.py`，与 IO-001 fits_core `fits_verify_file` 同一算法族」
  - 差异 = IO-001/002/003 三族内部互指「不重复实现」，是**族内合法分层**；与 `aio` 的关系则是「独立实现 + 不迁移 aio」= 同主题并存两份、无合并方案。**这是本批最需要上呈的角色/归属问题**：一个 I/O 边界机制有两套冻结接口，上位只承认一个（§10）。
- **元信息块**：**有**（违 §4）。:3「> doc_id: DOC-IO-INTERFACE-001」+ :4「> doc_status: ACTIVE_NORMATIVE」+ :5 把「本文件 + DOCUMENT_INDEX 登记」并列进「上游权威」。**机械列 `meta_block` 报 `False` —— 漏报**（工具的识别器不认 `doc_id:`/`doc_status:` 式样；同族 IO_002 :3-4、IO_003 :3-4 同判漏报，本批共 **3 份**）。
- **写法违规**：
  - :1 标题「**冻结合同**」= 状态锚；:3-4 doc_id/doc_status 元信息块；:5 上游权威含「本文件」自指；
  - :17「本合同**不改科学公式**（`scientific_change=false`），不迁移任何科学/图像处理算法」——`scientific_change=false` 是控制包/变更单的字段名，属任务派发痕迹入正文（§4 无任务编号）；「不改/不迁移」是**变更叙事**而非正向条款（§4「需要表达硬约束时用正向条款」）；
  - :32「允许写路径：`lib/infrastructure/aio/io/** lib/infrastructure/aio/io/** lib/infrastructure/aio/** lib/infrastructure/aio/healpix_db/** lib/infrastructure/aio/io/** eng/tests/io/** docs/interfaces/io/**`」——**工包文件域声明**（写权限清单）留在正式合同里，且清单内 `lib/infrastructure/aio/io/**` **逐字重复 3 次**（未经校对）。属标准 01 §2「过程台账不放在正式文档层」；
  - :170「含全零 CHECKSUM 占位卡同样判红 (**M2b-B-09**)」——审计整改项编号入正文。机械列 `task_ids` 报 `[]` ⇒ **漏报**（识别器只覆盖 EXP-/RELEASE-/FIX-/AUD- 式样；同族 `M2b-B-01` 见 IO_002 :102、`DISP-HIPS-005` 见 IO_002 :293、`ACR-IVAR-001` 见 PHASE2_API :43，本批共 4 份受累）；
  - :190-196 §13「与相邻接口/实现的边界」整节为「保留/不修改/不迁移」处置叙事（:191「保留作兼容层；IO-001 不迁移/不修改」、:192「保留」、:196「本接口不实现」）——上位 §0.3 明文「已经撤销的设计与已经退役的实现不出现在正式文档里」；
  - :200-206 §14「已知限制」为执行环境与排期叙事（:200「Linux 控制节点（本接口执行环境）无 MSVC/Windows DLL 构建」、:205「磁盘满负测用目录权限(EACCES)近似 + ENOSPC 码路径静态覆盖（**2c2g 无法真实填满磁盘**）」）——机器规格与测试取巧说明属台账，不落正式合同层（§2）。
- **悬空引用**：
  - :5 `ASTROCS_DESIGN.md` §7.3 标为「DLL C ABI 边界」—— 该上位文档**不存在名为「DLL C ABI 边界」的 §7.3 条款**（§7.3=错误传播与运行日志）。指向存在的文档、不存在的条款 ⇒ **章节级悬空**。
  - :28 表格行「模块公开 ABI 占位（module query 入口）| `lib/infrastructure/aio/io/include/astrocs/io/io_module_api_v1.h`（**该文件不存在**：全仓零命中，如实登记为缺口…）」—— 命令：`ls lib/infrastructure/aio/io/include/astrocs/io/io_module_api_v1.h` → No such file；同目录 `fits_stream_v1.h`、`hips_input_v1.h` 均实存（正对照）。**机械列 `dangling_links` 对本份命中此条 —— 命中正确，未漏报**；但需如实登记其性质：**文档自己已在同一格里声明该文件不存在**，所以它不是「隐蔽断链」，而是「正式合同里保留一条指向占位缺口的行」。整治动作是把该行删除、缺口移 `docs/KNOWN_LIMITATIONS.md` 或 `artifacts/evidence/`，不是补一个空文件。
  - :195「trace/bytes：由宿主注入 hook（**14 标准**）」—— 无名为「14 标准」的文档；与 DATA-002 :220 的「13 标准 §2」同一形态（控制包条款编号残留）。
  - :26、:27、:29、:30、:192、:193、:194 指向的 `lib/infrastructure/aio/io/`、`fits_core.c`、`fits_stream_v1.h`、`eng/tests/io/`、`lib/infrastructure/aio/io/tests/`、`lib/include/astrocs/io/io_adapter.h`、`lib/include/astrocs/contracts/artifact_abi_v1.h` —— **全部实存**。
- **与上位冲突**：
  1. :5 上游条款（§7.3=DLL C ABI 边界）在上位不存在；真实条款 §8.5 未被引用。
  2. :18/:191 保留 `aio` 全图像 FITS 读写为「兼容层」并另立 `acsd_io.dll` + `fits_stream_v1.h` 一套 FITS 通道 ← 上位 §10 :686「`aio` 是**文件级唯一 I/O 边界**：任何文件读写经 aio」。**两个 I/O 边界 = 与上位唯一性条款实质冲突。**
  3. :36「目标 DLL：`acsd_io.dll`（Windows）/ `libacsd_io.so`（Linux 技术预览）」与上位 §8.4 :657「基建按稳定职责合并为有限模块，**职责名全仓唯一**」+ :631 基建清单只列 `aio/` 一个 I/O 模块 ⇒ `io` 与 `aio` 职责名不唯一。
  4. :200「Linux 控制节点无 MSVC/Windows DLL 构建；…Windows 正式 DLL 构建（acsd_io.dll）用同一源码执行」—— 同句先断「无 Windows 构建」再断「Windows 用同一源码执行」，自相矛盾；且上位 §11 把 Windows x64 列为正式平台，此类「本平台未构建」的状态陈述按上位 §12.5 :802「**状态由检查与验收现场计算，登记表不预写状态**」不应由合同文档预写。
  5. :205「2c2g 无法真实填满磁盘」为开发节点规格，上位 §3 与 §11 均无该口径，属把环境事实写进合同（§4「只写现行设计要怎样」）。
- **处置建议**：**保留 + 就地订正（并须上呈 I/O 唯一边界归属）** —— 落位正确、内容具体、被 ID 引用 89 处，不可删。必改：删 :3-4 doc_id/doc_status 与 :5 的「本文件」自指，上游条款订正为 §8.5 + §10；删 :32「允许写路径」整行（工包文件域，移控制包）；:17/:190-196 的「不改/不迁移/保留」全部改写为正向现行条款（§4）；:28 缺口行删除；:170 `M2b-B-09` 与 :195「14 标准」换成可解析指针；:200-206 §14 移出合同层。**遗留需裁决**：`acsd_io.dll`/`fits_stream` 与 `aio` 谁是 FITS 唯一边界 —— 上位 §10 与本文不可同时成立，属标准 01 §1「冲突本身是缺陷」，需按变更流程定档（我推荐把 IO-001 降为 aio 的内部接口页，不再作为并列合同）。

---

### 11. docs/interfaces/io/IO_002_HIPS_INPUT_INTERFACE.md

- **路径**：`docs/interfaces/io/IO_002_HIPS_INPUT_INTERFACE.md`
- **标题**：IO-002 HiPS 输入读取接口冻结合同
- **行数**：303
- **角色**：架构设计（I/O 接口契约）。目录**落位正确**。
- **主题**：HiPS 输入读端合同——properties 必填键校验、NESTED tile 地址布局、FITS-only 科学平面、partial tree、MOC optional hint、缺 tile 状态、裸/归档两形态同读语义，及 `acs_hips_*_v1` C ABI 与 13 个 `ACS_HIPS_*` 错误码。
- **上游**：已标注但**双重错位 + 引用不存在的条款** —— :5「> 上游权威：`ASTROCS_DESIGN.md` §7.3/§9 + 本文件」（§7.3=错误传播与运行日志、§9=CPU 后端与资源；本份主题是 HiPS 读，正确条款是 §10 与 §8.5）+ :7「冻结约束 `ASTROCS_DESIGN.md` §7.3（DLL C ABI 边界）、**F.4**（接口字段/单位/shape/坐标/invalid/所有权逐项明确）、**E**（阶段间只经磁盘产品交换）」——上位**没有 F 章、没有 E 章**（全文仅「附录 A. 术语」:819 与「附录 B. 外部标准与文献」:823，附录 A 是无编号的术语逗号串，不含 F.4/E）。
- **下游**：按路径 12 个文件（`docs/algorithms/PHASE2_MOSAIC_WRITE.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`、`docs/design/PRODUCT_STORAGE_FORM.md`、`docs/DOCUMENT_INDEX.yaml` 等）；按 ID `IO-002` 39 个文件。**非孤儿。**
- **是否正本**：**正本**（HiPS 读端接口面唯一承载者：`lib/infrastructure/aio/io/include/astrocs/io/hips_input_v1.h` + `hips_core.c` 双向对应，本份 :51-52 列的路径全部实存）。但 §3.1 properties 校验表与 §3.3 地址布局与 `docs/contracts/DATA_SEMANTICS.md` 有正本归属竞争（见重复对象）。
- **重复或重叠对象**：与 `docs/contracts/DATA_SEMANTICS.md` 重叠 = **HiPS 磁盘布局与 `Dir/Npix` 词法**。贴两处：
  - 本文 :100-102「`D = (ipix / 10000) · 10000`（万进制块**起始值**），`N = ipix`（文件名带完整 tile 号）——IVOA REC-HIPS-1.0 §4.1 原式，示例 `10302@order6 → Dir10000/Npix10302.fits`；M2b-B-01 前的 `D = ipix / 10000`、`N = ipix % 10000` 与标准相反，现仅作**只读**回退」
  - `docs/contracts/DATA_SEMANTICS.md:471`「REC-HIPS-1.0 §4.1：Dir = 万进制块**起始值**，Npix = 完整 tile 号；M2b-B-01…」
  - 差异 = 同一条布局式与同一条整改编号在两处各写一遍（一处接口合同、一处数据语义），且 :102 与 :471 都保留了「M2b-B-01 前相反」的历史叙述 ⇒ 同主题双写，违标准 01 §2 同主题单正本；整治时二选一（我建议布局词法留在合同、历史叙述两处都删）。
- **元信息块**：**有**（违 §4）。:3「doc_id: DOC-IO-INTERFACE-002」+ :4「doc_status: ACTIVE_NORMATIVE」+ :5「+ 本文件」自指 + :8-9「> 上游: DOC-IO-INTERFACE-001（IO-001 FITS 流式接口）——IO-002 的 tile FITS 平面读取全部复用 IO-001 的 fits_core 契约与错误码」。**机械列 `meta_block` 报 `False` —— 漏报**（与 IO_001/IO_003 同因）。
- **写法违规**：
  - :1 标题「冻结合同」状态锚；:3-4 doc_id/doc_status；:5「本文件」自指为权威；
  - :102「M2b-B-01 前的 `D = ipix / 10000`、`N = ipix % 10000` 与标准相反，现仅作**只读**回退」——**整改项编号 + 「…前的旧行为」历史叙事**（上位 §0.3「已经撤销的设计与已经退役的实现不出现在正式文档里」）。机械列 `task_ids=[]` ⇒ **漏报**；
  - :42「本合同**不改科学公式**（`scientific_change=false`）；不做 tiles 解码…（科学层属于 P1/P2/P3 模块）」——控制包字段 + 变更叙事；`P1/P2/P3` 是控制包任务前缀（上位 §7.1 :489 明文「`phase1/2/3` 仅为内部指代」，此处用 P1/P2/P3 当模块代称亦无指针）；
  - :44/:275-278「`lib/infrastructure/aio`（aio_hips_*，CFITSIO 静态链）…**IO-002 不修改**」「`lib/infrastructure/aio/healpix_db`：浏览器参考；**不修改**」——退役/保留处置叙事入正文（§4）；
  - :57-58「允许写路径：`lib/infrastructure/aio/io/** lib/infrastructure/aio/io/** …`」——工包文件域，且 `lib/infrastructure/aio/io/**` 逐字重复 **3 次**（与 IO_001 :32 同一缺陷，两份各重复一次以上）；
  - :293-297「**MOC 读侧显式不 fail-closed（登记项）**：…**DISP-HIPS-005** 的「低阶 UNIQ 对本 reader 无效」…理由：若 fail-closed 会把「MOC 不可用但 tile 完好」的合法产品判死」——「登记项」是台账语，`DISP-*` 是算法层缺陷登记编号（可解析：`docs/algorithms/HIPS_WRITER.md:380`），把缺陷编号与取舍理由写进接口合同属层级混用。
- **悬空引用**：
  - :7 `ASTROCS_DESIGN.md` **F.4** 与 **E** —— 上位不存在（命令：`grep -n 'A\.3\|A\.4\|^### E\|附录' ASTROCS_DESIGN.md` → 只有 :819 附录 A、:823 附录 B；正对照 `grep -n '^## 附录' ASTROCS_DESIGN.md` 两条命中）。**并且同族的 `E` 在 IO_003 :7 被解释成另一个含义（「科学公式不变」），本份解释成「阶段间只经磁盘产品交换」⇒ 同一条款代号在两处指向不同条款，铁证这两条不是笔误而是引用自一份已不存在的上位文档（旧宪章式条款表）。**
  - :5 `§7.3/§9`、:279 `IO-003`（连字符）vs 文件名 `IO_003_…`（下划线）——命名法不统一（详见清单第 B 条）。
  - :64「`docs/architecture/ARCHITECTURE.md` §产品流」—— 文件实存；**但「§产品流」该节是否存在未逐节核**，登记为「待核」（不按悬空判）。
  - :279-280 `DATA-001` —— 无文档（举证见 DATA-002 条目）。
  - :51、:52、:53、:54、:55、:232 指向的 `hips_input_v1.h`、`hips_core.c`、`eng/tests/io/`、`lib/infrastructure/aio/io/tests/hips_core_selftest.c`、`eng/tests/io/make_hips_fixture.py` —— **全部实存**。
  - 机械列 `dangling_links` 对本份报 `[]` ⇒ **漏报**：F.4/E 与 §7.3 的错章节引用完全未被工具覆盖（工具只查 markdown 链接与 `..` 相对路径）。
- **与上位冲突**：
  1. :14/:74「IVOA HiPS **1.4** 兼容目录 / 本合同接受 "1.4"（含 "1.4" 前缀）」← 上位附录 B :825 登记的 IVOA 标准是「**IVOA HiPS 1.0**」，同句未列 1.4；本份 :78/:101 又自称引「IVOA REC-HIPS-**1.0** §4.4.1 / §4.1」。⇒ 同一文档内 1.0/1.4 并用，且与上位外部标准清单不符（标准 01 §3「每份文档、每个机制都能追溯到最高设计的一条要点」）。注：上位 §10 :691 另引「HiPS 2.0 工作草案 §4.3.2」⇒ 全仓存在 1.0 / 1.4 / 2.0草案 三档 HiPS 版本口径，需上位定档。
  2. :5/:7 上游条款（§7.3/§9/F.4/E）在上位均不存在 ⇒ 本份的上游追溯链**整条断**，其"冻结"状态无上位要点支撑。
  3. :44 保留 `aio_hips_*` 读写实现「不修改」+ 本份另立 `hips_input_v1.h` 读端 ← 同 IO_001 与上位 §10 :686 唯一 I/O 边界的冲突（本份是该冲突的读端侧）。
  4. :290-291「只支持 `hips_frame` ∈ {`equatorial`（写出值）, `icrs`（只读兼容别名）}」—— 上位 §3.3/§10 无 `icrs` 别名条款；别名兼容属实现选择，无上位要点（§3）。同段「galactic/tsv/png/jpg 目录拒绝」与上位无冲突（与 SCI-P3 §1 拒绝项一致）。
  5. :287「Linux 控制节点…无 MSVC/Windows DLL 构建；…Windows 正式 DLL 构建（acsd_io.dll）用同一源码执行」—— 同 IO_001 :200 的自相矛盾 + 违上位 §12.5 :802「状态由检查与验收现场计算，登记表不预写状态」。
- **处置建议**：**保留 + 就地订正（上游链须重建）** —— 内容为 HiPS 读端正本、代码双向齐备，删/迁皆损。必改：重建 :5-7 上游指针为「§8.5（模块与 ABI）+ §10（I/O 与原子产品）」并**删除 F.4/E 两条不存在的条款引用**，把 :7 想表达的「接口字段/单位/shape/坐标/invalid/所有权逐项明确」补进上位 §8.5（依标准 01 §3「追溯不到的机制先补要点再保留」）；HiPS 版本口径（1.0 / 1.4）与上位附录 B 对齐并补登；:102 的 `M2b-B-01` 历史回退叙述删除（只保留现行词法，历史去 `artifacts/evidence/`）；:57-58 允许写路径删除；:44/:275-278「不修改」改正向条款；`IO_00N` 文件名与 `IO-00N` 引用统一为一种命名法。

---

### 12. docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md

- **路径**：`docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md`
- **标题**：IO-003 原子 HiPS/manifest 输出发布冻结合同
- **行数**：186
- **角色**：架构设计（I/O 原子发布契约）。目录**落位正确**。
- **主题**：HiPS 子产品目录 + manifest 的原子发布状态机（run 隔离、路径穿越/符号链接拒绝、stage→fsync→fitsverify→sha256→rename→COMPLETE manifest）、完成 manifest 形态与 `tree_hash`、错误语义。
- **上游**：已标注但**条款号错位 + 引用上位不存在的条款** —— :5「> 上游权威：`ASTROCS_DESIGN.md` §7.3/§9 + 本文件」+ :6-7「> 冻结约束 `ASTROCS_DESIGN.md` §7.3（DLL C ABI 边界）、**A.3/A.4**（阶段隔离与原子性）、**E**（科学公式不变）」。上位 §7.3=错误传播与运行日志；上位**无 A.3、无 A.4、无 E**（附录 A 是无编号术语串）。原子发布与阶段隔离的正确上位条款 = **§10 :688** 与 **§8.1 :548**，本份一处都没指向。
- **下游**：按路径 22 个文件（`docs/algorithms/HIPS_WRITER.md`、`docs/algorithms/PHASE2_MOSAIC_WRITE.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`、`docs/design/PRODUCT_STORAGE_FORM.md` 等）；按 ID `IO-003` 47 个文件。**非孤儿，且与合同说明层（`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`）双向已连。**
- **是否正本**：原子发布语义 = **副本**（正本按上位 §10 :688 与同批 DATA-003 §3；本份是 HiPS 特化的一套，与 DATA-003 并存）；落盘形态 `storage` 段 = **片段**（:141 自认「字段与不变式（M1..M4）的唯一权威 = `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md` §10.3」，权威在他处，本份只留摘要）。
- **重复或重叠对象**：
  - 与同批 `docs/interfaces/data/DATA-003_PRODUCTION_ARTIFACT_STORE.md` 重叠 = **原子发布序列 + run 私有磁盘布局**（本批最强的一处双正本，两处段落与逐条差异已写在 DATA-003 条目「重复或重叠对象」栏，此处不重述；要点：两者是两个不同的 Python 发布器 `hips_output_store.py` vs `production_store.py`、两套已发布目录 `products/{user_path}/` vs `objects/{artifact_id}`+`manifests/`、两套 rename 次序）。
  - 与 `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`（别批）重叠 = **storage 段字段与不变式**，本份 :141 已显式让位（「唯一权威 = …§10.3」）⇒ **合法分层 + 正确指针，是整治时的对齐范式**，不判重复。
- **元信息块**：**有**（违 §4，且是本批最重的一个）。:3「doc_id: DOC-IO-INTERFACE-003」+ :4「doc_status: ACTIVE_NORMATIVE」+ :5-10 五行「上游权威 / 冻结约束 / 上游」块，其中 :5「+ 本文件」= 文档自列权威源，:8-10 是依赖台账。**机械列 `meta_block` 报 `False` —— 漏报**（同 IO_001/IO_002）。
- **写法违规**：
  - :1 标题「冻结合同」状态锚；:3-4 doc_id/doc_status；:5「本文件」自指；
  - :33「本合同**不改科学公式**（`scientific_change=false`），不做 tile 生成/投影（科学层属 P1/P2/P3）；`lib/infrastructure/aio` 与 `lib/infrastructure/aio/io` **保持原样不修改**」——控制包字段 + 变更叙事 + 与 :41-44 的自述冲突（见与上位冲突第 3 条）；
  - :46「允许写路径：`lib/infrastructure/aio/io/** lib/infrastructure/aio/io/** eng/tests/io/** docs/interfaces/io/**`」——工包文件域，`lib/infrastructure/aio/io/**` **逐字重复 2 次**；
  - :142「（§1 **A4** 口径）」—— A4 = :7 那个上位不存在的条款编号，二次出现；
  - :184「（与 DATA-003 writer 唯一 producer **同纪律**）」——「纪律」语 + 跨文档语义引用而非指针；
  - :177-187 §8「已知限制」= 环境/排期/取巧说明（:179「Linux 控制节点…无 MSVC/Windows DLL 构建」、:183「单 run 内并发发布同一 user_path 由调用方串行化」）——后者是把**未实现的并发约束**写成"限制"，属上位 §0.3 禁的「已退役/未落地实现叙事」形态；
  - 无日期、无 commit、无 sha256 违规。**注意：:135 `"created_utc": "2026-09-02T08:15:00Z"` 是 manifest JSON 示例的字段取值，不是正文日期** —— 机械列 `dates=[135]` 把示例时间戳判成正文日期 ⇒ **机械列误报**（本批唯一一处，另见 DATA-002 的 glob 误报）。
- **悬空引用**：
  - :7 `ASTROCS_DESIGN.md` **A.3 / A.4 / E** 与 :142 **A4** —— 上位不存在（命令与正对照同 IO_002 条目）。**且 `E` 与 IO_002 :7 的 `E` 指向不同语义**（本份「科学公式不变」/ IO_002「阶段间只经磁盘产品交换」）⇒ 两条同代号不同含义，同一条款被两处各自解释。
  - :5 `§7.3/§9` —— 条款名与内容不符（同上）。
  - :8-9 `DOC-IO-INTERFACE-001`、`DOC-IO-INTERFACE-002`、:10 `DATA-003`、`DATA-004` —— doc_id 族仅在这三份文档内部自封，`docs/DOCUMENT_INDEX.yaml` 以路径登记、不登记 doc_id ⇒ **ID→路径无索引映射**（清单第 B 条）。DATA-003/004 对应文件实存（本批第 8、9 份）。
  - :40、:41、:42、:43、:44 指向 `docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md`、`lib/infrastructure/aio/io/hips_output_store.py`、`lib/infrastructure/aio/io/fits_verify.py`、`eng/tests/io/test_hips_output_contract.py`、`eng/tests/io/hips_output_fixture.py` —— **全部实存**。:24 `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`、:141 `eng/contracts/schemas/hips_storage_form.schema.json` 实存。
  - 机械列 `dangling_links` 对本份报 `[]` ⇒ **漏报** A.3/A.4/E 与 §7.3 类条款级悬空（工具不查条款号）。
- **与上位冲突**：
  1. :5-7 上游与「冻结约束」全部指向**上位不存在的条款**（§7.3 名不符、A.3/A.4/E 无此条款）⇒ 依标准 01 §3「每份下层文档在开头位置标明其上游最高设计条款」，本份标注**形式齐备、实质无效**，比"缺标注"更危险（机械列 `upstream_pointer=True` 因此判定通过 ⇒ **工具在此列上系统性假绿**）。
  2. :58-60 run 私有目录 `{output_root}/runs/{run_id}/…` 与上位 §10 :696「运行目录隔离，产物路径逐段合法：产品与运行日志只落块级 `output_dir`…**`run/` 只放开发/CI 过程产物与过程日志，与运行日志不互替**」—— 本份用 `runs/` 作运行产品根且未说明与上位 `output_dir` 块的拼接关系；同批 DATA-003 :42 用 `{root}/runs/{run_id}/` 同样未定义 `root` 与 `output_dir` 的关系 ⇒ 两份共用一套未回上位的目录约定。
  3. :33「`lib/infrastructure/aio` 与 `lib/infrastructure/aio/io` **保持原样不修改**」与 :41-42 把新增文件 `hips_output_store.py`、`fits_verify.py` **放进 `lib/infrastructure/aio/io/`** ⇒ **同一份文档内自相矛盾**（标准 01 §4「图与正文相互印证」所要求的自洽不成立）。IO_001 :18 + :25-30 有同一形态的矛盾。
  4. :141 `storage` 段的 `form_source`（`config`/`default`）与默认 `archive` 已报 warn 的表述与上位 §10 :690 一致（「键缺失或留空 ⇒ 取默认并**报 warn**（默认值来源 = `eng/packaging/config/defaults.json`）」），:141 未指向 `defaults.json` 而指向合同 §10.3 ⇒ 默认值来源链在本份断一环（轻微）。
  5. :179「Linux 控制节点…无 MSVC/Windows DLL 构建」—— 违上位 §11 双平台正式发行 + §12.5 :802「登记表不预写状态」（同 IO_001 第 4 条）。
- **处置建议**：**合并 + 就地订正** —— 与同批 DATA-003 的原子发布序列**合并为一份正本**（建议正本 = `docs/interfaces/data/DATA-003_PRODUCTION_ARTIFACT_STORE.md`，本份只保留 HiPS 特化的部分：§3.2/§3.3 路径穿越与符号链接拒绝、§3.1 HiPS 文件名形态、§4.4 fitsverify 步骤、§5 完成 manifest 的 HiPS `tree`/`tree_hash` 形态），依标准 01 §2「同一角色内同一主题只有一份正本；多份内容合并为一份」+ §1「下位与上位冲突时以上位为准」。同时：重建 :3-10 头块（删 doc_id/doc_status/「本文件」自指，删 A.3/A.4/E 三条不存在条款，上游订正为 §8.1 + §10）；:33/:41-44 的「保持原样不修改」与新增文件位置矛盾必须二选一（改「在 `aio/io/` 下新增，属 §10 aio 唯一边界内部实现」并删「保持原样」句）；:46 允许写路径删除；:177-187 已知限制移出合同层。

---

### 13. docs/modules/MODULE_MAP.yaml

- **路径**：`docs/modules/MODULE_MAP.yaml`
- **标题**：ACSD 模块一致性映射表（MOD-001 / CHK-MODULE-MANIFEST）（:1，YAML 注释式标题）
- **行数**：931
- **角色**：**标准 01 §2 表里没有「映射表」这一角色**。它同时是①机器门的输入（:28 `tool: eng/tools/quality/check_module_map.py`）②登记表（:46 `module_yaml_required_keys`、:47 `product_manifest_file`）③缺口台账（:771-931）。按 §2 只能就近归「标准与检查」（→ `docs/ci/`）或「合同说明」（→ `docs/contracts/`）；现落 `docs/modules/`（模块工作细节角色目录）**角色与目录不符**。上位 §0.2 :53 承认「登记表与映射表承载定义与映射，状态由检查器现场计算」但未给目录 ⇒ 本条判「角色无对应格」，不是简单错放。
- **主题**：23 个模块的八元组映射（模块 ID → 目录 → README → module.yaml → 公开头 → CMake target → 产品清单 → 共址测试）+ 50 条路径缺口 + 16 条 target 缺口 + 70 条能力缺口登记。
- **上游**：已标注且**三处两条错** —— 头注释 :10「`ASTROCS_DESIGN.md` §7.1（顶层结构唯一）、§7.3（模块与 DLL/SO 边界）、§12.5（状态阶梯唯一口径）」+ :32 机器字段 `design: ASTROCS_DESIGN.md §7.1/§7.3/§12.5`。实际：§7.1 = 命令树（顶层结构 = §8.4），§7.3 = 错误传播与运行日志（模块与 DLL/SO 边界 = §8.5），只有 §12.5 正确。
- **下游**：22 个文件（`grep -rl docs/modules/MODULE_MAP docs lib eng | wc -l` → 22），含 `eng/ci/checks.json`、`docs/DOCUMENT_INDEX.yaml:746`、`eng/cmake/ARCH-001-migration-manifest.md`。**非孤儿，是本批机器耦合最深的一份。**
- **是否正本**：**正本**（模块↔代码八元组的唯一机器映射；`docs/plugins/**` 56 处被它指向，而我批的 `docs/modules/*.md` 11 份**一处都没被它指向** —— 命令：`grep -c 'docs/modules/' docs/modules/MODULE_MAP.yaml` → **0**，正对照 `grep -c 'docs/plugins/'` → **56**）。
- **重复或重叠对象**：与 `docs/plugins/`（23 篇插件规范）不重复（一个 YAML 八元组、一个散文规范，:3 自述用途即「把 docs/plugins/ 23 篇插件规范与代码落位八元组建立一一机器映射」= 合法分层）。**真正的重复在 :771-931 与 `artifacts/evidence/`/`eng/ci/ledgers/` 之间**：本表内嵌 136 条缺口台账，而仓内已有同类先例落 `eng/ci/ledgers/`（`run_manifest_schema_deviations.json`、`prod_wiring.json`，本批 MANIFEST_VERIFY 条目已引）。贴两处：
  - 本文 :14-16「本表是**期望映射**：不含任何模块状态声明。模块状态一律由 eng/tools/quality/check_module_map.py 现场计算，状态词只取 §12.5 词表」
  - 本文 :863「`- {module: aio, code: product_unit_skeleton, owner: BLD-401, reason: "产品清单 unit 状态 SKELETON（未安装；§12.5）"}`」（同型 :923 scheduler）
  - 差异 = :14 声明「不含状态声明」，:863/:923 却在 reason 里写 §12.5 的状态词 `SKELETON`/「未安装」⇒ **自述与内容矛盾**（标准 01 §4「自洽」/上位 §0.1 唯一权威链）。
- **元信息块**：:1-26 全部是文件头注释块，含「用途 / 权威依据 / 诚实性约定 / 维护」四段。其中 :7-13「权威依据」= 上游标注（§3 要求，合规但条款号错）；:14-25「本表是期望映射…」「硬规则…」「维护：本表随 DATA-001…ARCH-001/INT-001 更新；修改本表必须同时过 CHK-MODULE-MANIFEST」= **纪律声明 + 过程约定**，属 §4 禁的元信息块；:758-770 `gap_owners` 三条的 `note:` 全是包生命周期叙事。
- **写法违规**（逐条，本批最密）：
  - :738「# 声明路径缺失登记（缺口，逐条；**FIX-404 收口 v2 / 2026-09-21**）」——任务编号 FIX-404 + **日期**（机械列 `dates=[738,775,847,859]` **命中，未漏报**）；
  - :762 / :766 / :770 note 全文「RELEASE-04 包已按 **CONTROL_PACK_SPEC §9 出库**，缺口归属由 **RELEASE-05 CLEAN-501** 承接，见 **RELEASE-05 GAP_AUDIT G2-6**」——任务/包编号串 + 出库历史叙事；
  - :759/:763/:767 `gap_owners[].id: BLD-401` / `DOC-403` / **`RELEASE-04/未覆盖`** —— 把工包编号当机器主键，且第三个 id 内含中文「未覆盖」；
  - :775 `checked_at: 2026-09-21（FIX-404 复核实测）`、:859 `checked_at: 2026-09-23（前台复核：删除 star_detection 的陈旧 missing_co_located_tests 登记并同步计数 **71→70** … 上一轮 **2026-09-22** 删除 noise_snr 的陈旧 header_missing_abi_version 登记并同步计数 **72→71**）`——**逐日流水账 + 计数变化史**，标准 01 §2「过程台账不落正式文档层」的最重违例；
  - :79/:108/:137/:167/:197/:226/:256/:285/:314/:344/:373/:402/:431/:460/:489/:519/:548/:614/:643 等 19 条 `note:` 反复出现「ARCH-001 **已迁入** target_dir；不动点=迁移 map 条目 N」——**历史叙事**（「已迁入」），且 :18-22 明文承认这些是「**历史键**」；
  - :744-749「**硬规则**（FIX-404 / GAP_AUDIT G3-8/G1-5；条目一律如实保留）」——纪律声明 + 控制包编号；
  - :755-757「规模：路径缺口 50 条（G3-8 实测 53 条 − 3 条按移动后真实路径更新）+ target 缺口 16 条」——计数算式入文。
- **悬空引用**：
  - :761、:765 `authority: 工程控制/RELEASE-05/tasks/CLEAN-501.md`、:769 `authority: 工程控制/RELEASE-05/GAP_AUDIT.md G2-6` —— 本次实测**当前存在**（`find . -iname '*GAP_AUDIT*'` → `./工程控制/RELEASE-05/GAP_AUDIT.md`）。但 :748-749 立了 fail-closed 规则「kind=task 的 owner 必须有对应任务书文件，否则机器判红」，而 AGENTS.md §7 与本文 :762 都说控制包「收口后按 CONTROL_PACK_SPEC §9 清理 / RELEASE-04 包已出库」⇒ **包一清理，CHK-MODULE-MANIFEST 必判红**。这是一条可预登记的确定性断链，不是当下的文件级断链。
  - :24、:7「DATA-001」 —— 无文档（举证见 DATA-002 条目）。
  - :24、:672、:744「ARCH-001 / INT-001」 —— `docs/contracts/ARCH-001.md` 与 `eng/cmake/ARCH-001-migration-manifest.md` 实存；**`INT-001` 无对应文件**（`find . -name 'INT-001*'` → 0，正对照同命令 `-name 'ARCH-001*'` → 2）。
  - :14、:750、:853 `eng/tools/quality/check_module_map.py`；:12、:42 `eng/contracts/config/module_dll_contract.schema.json`；:47 `eng/packaging/astrocs.product.json`；:48 `docs/contracts/INDEX.yaml`；:8 `docs/plugins/00_INDEX.md` —— 实存性见清单第 C 条（本条未逐个展开，登记为「待核」，不猜）。
  - :779-844 的 50 条 `path:` 值**由本表自己声明为「当前在树中不存在」**（:740-742），故不计入悬空缺陷（它们是已登记缺口）；但 :779-828 的 `eng/contracts/schemas/*.schema.json` 与 :791「`eng/contracts/schemas/hips_product.schema.json`」、:806/:808「`export_product.schema.json`/`fits_product.schema.json`」也出现在 `modules[].schema_links` 正声明位（:274、:478、:508）⇒ **同一文件里既把某路径当合同事实源引用、又登记它不存在**（双写自相矛盾）。
  - 机械列 `unregistered_index=False`（即已登记）与 `dangling_links=[]` —— 后者对 50 条自声明缺口报空**是合理的**，但对本条「INT-001 无文件」「RELEASE-05 清理即断」两类均无覆盖 ⇒ 该列对本份**不适用而非正确**。
- **与上位冲突**：
  1. :10/:32 三条上游指针里两条指错条款（§7.1≠顶层结构、§7.3≠模块与 DLL/SO 边界）；正确是 §8.4/§8.5。
  2. :14「不含任何模块状态声明」←→ :863/:923 使用 §12.5 状态词；上位 §12.5 :802「状态由检查与验收现场计算，**登记表不预写状态**」。
  3. :577（cli 条目 note）「唯一入口 exe（**DESIGN §6.2 命令树唯一**）」与 :607（scheduler 条目 note）「本条目对齐 ASTROCS_DESIGN **§7.1：覆盖 §7.1 顶层结构的** scheduler」—— 前者把命令树指到 §6.2（export 流程），后者把顶层结构指到 §7.1（命令树）；两条互相把同一对条款号用反，且都与上位实际（§7.1 命令树 / §8.4 顶层结构）不符。**同一文档内两处相反方向的条款号误用**，与本批 PHASE1_API :12、orchestrator :80 构成同一根因的第 4、5、6 次复现。
  4. :732/:734（hips_browser 免除产品清单）引「ASTROCS_DESIGN.md **§7.1**（hips_browser/ 未来 GUI 可视化组件（不进产品 manifest)）」—— 该结论在上位是 **§8.4 :636** 目录树的行内注释，§7.1 无此内容。
- **处置建议**：**拆分迁移** —— ①八元组映射本体（:27-735）留 `docs/modules/MODULE_MAP.yaml` 或迁 `eng/ci/`（与 `checks.json`、`root_manifest.json` 同层，依 AGENTS.md §7「eng/ci/ 机器门注册表与检查器」），并把 :10/:32 上游指针订正为 §8.4/§8.5/§12.5、把 :577/:607/:732 三处条款号一并订正；②:737-931 的 50+16+70 条缺口台账**迁出正式文档层** → `eng/ci/ledgers/module_map_gaps.json`（与既有 `prod_wiring.json` 同构，依标准 01 §2「台账落证据/登记层并由正本引用」），并把 `authority:` 从控制包路径改为指向长期存续的判据文件（依 §3 悬空治理）；③:18-22 的 `legacy_paths`「历史键」机制整族评估：若 ARCH-001 迁移已 DONE（:70 位），历史路径应移出映射表（上位 §0.3「已经撤销的设计与已经退役的实现不出现在正式文档里」）。

---

### 14. docs/modules/acr.md

- **路径**：`docs/modules/acr.md`
- **标题**：Module: acr
- **行数**：53
- **角色**：名义上「模块工作细节」（目录 `docs/modules/` 对应该角色），**但对象不是生产模块** ⇒ 判**角色错放**，应迁 `docs/research/`（隔离实验说明）或 `docs/development/`。依据：上位 §8.4 :635「`acr/` **隔离实验（生产不可达）**」，标准 01 §2 模块工作细节行写的是「每个**生产**模块做什么」。
- **主题**：异构计算运行时（kernel registry、CPU/GPU 调度、device executor、route calibration、cuda bridge）的职责/API/线程/错误/测试面摘要。
- **上游**：已标注 :3「> 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）」——**条款号与条款名不匹配**（§8.4=顶层结构、§8.5=模块与 ABI）。本批该处错误共 **9 份**复现（6 份 docs/api 中 4 份 + 本批 11 份 docs/modules 全部 11 份，去重后见清单第 D 条）。
- **下游**：**实质引用者 0 个**。命令与结果：`grep -rl 'docs/modules/acr' docs lib eng | grep -v '^docs/modules/acr.md'` → 仅 `docs/audit/doc_classification.csv`、`docs/audit/inventory.csv`、`docs/DOCUMENT_INDEX.yaml` 三个**登记表**；换第二套词 `grep -rn 'acr\.md' docs lib eng | grep -v 'doc_classification\|DOCUMENT_INDEX'` → 仅 `docs/audit/inventory.csv`。同一条命令在正对照上应命中：`grep -rl 'docs/modules/healpix_browser_qt' docs lib eng` → 13 个（含 11 个真实代码文件），证明检索法有效。⇒ **按标准 01 §3「每份文档…双向可追溯」判 acr.md 为孤儿**（只有索引指向它，无人消费它）。另：MODULE_MAP 的 23 个模块 id 里**没有 acr**（我读完 23 条：calibration, cosmetic, star_detection, psf, platesolve, photometry, noise_snr, drizzle, coverage, sampling, upm, rejection, integration, projection, resample, fits_output, aio, cli, scheduler, benchmark, observability, gaia_xpsd_client, hips_browser），`grep -n 'id: runtime'` 亦 0 ⇒ 本页面**不在机器模块登记面内**。
- **是否正本**：**待定**。acr 的机制（GPU/CUDA 异构）在上位唯一口径是「生产仅纯 CPU」（§9 :671）+「隔离实验（生产不可达）」（§8.4 :635）⇒ 它承载的是一个**上位明令不进产品**的机制，本份文档层无人引用、机器登记面无条目。
- **重复或重叠对象**：无同主题第二份（`docs/plugins/**` 23 篇里没有 acr 篇，MODULE_MAP 里没有 acr 条目）。按 §5.6 判「无重复」，不凭命名臆断。
- **元信息块**：无 ID/状态块（:3 是 §3 要求的上游标注，合规）。**本份是标准 01 §4「开头无元信息块」的唯一完全合规的 docs/modules 页**（与 calibration/dynamic_psf/gaia/hips_p2/healpix_drizzle 的 :5-14 头块形成对照）。
- **写法违规**：
  - :28「work_pool 调度；dispatcher 纯 CPU 回退；partial 契约（**V19R2 前已修**）」——**历史叙事 + 审计轮次编号**（「…前已修」= 上位 §0.3 禁的「已退役实现叙事」）。机械列 `history_narrative=[]`、`task_ids=[]` ⇒ **两列同时漏报**；
  - :8/:48「cuda bridge（phase2 集成 CPU reference + CUDA）」/「GPU kernel 仅 phase2 `mosaic_reject_legacy` launcher 注册」——现状/排期口径混在职责里；
  - :44「cuda 不可用 **GTEST_SKIP**」——测试实现机制入合同摘要（与 :46-48「已知限制」重复表达同一事实）；
  - :36「依赖 phase2（ALG-UPM-* 等）；**无独立科学定义**」——负向声明；同份 :11-12「非职责」、:46「Known limitations」三段负向叙述，标准 01 §4 要求「需要表达硬约束时用**正向条款**」。
- **悬空引用**：
  - 本份**无文件级链接**（全文无 markdown 链接、无路径），机械列 `dangling_links=[]` 正确。
  - :16「astro/compute/*.hpp（acr.hpp、kernel_registry、task_traits、topology）」—— 无 `lib/` 前缀的裸相对写法；实际位置 `lib/infrastructure/acr/include/astro/compute/`（`docs/audit/inventory.csv:12` 即如此登记）。⇒ 指代可解析但不可机器核对（**软悬空**）。
  - :52-53「lib/infrastructure/acr/（api/backends/core/cost/diagnostics/profile/routing/scheduler/**eng/tools/tests**）」—— 括号清单把 `eng/tools`、`tests` 与 acr 子目录混列，读者无法判定 `eng/tools` 是否在 `lib/infrastructure/acr/` 下（实际不在）。**花括号/括号列表书写歧义**，与 common.md :65 同类。
  - :36 `ALG-UPM-*`、:48 `mosaic_reject_legacy`、:40「benchmark route estimator；qualification 矩阵」——均无指针，不可追溯（标准 01 §3）。
- **与上位冲突**：
  1. :7「异构计算运行时：kernel registry、**CPU/GPU 调度**、device executor、route calibration、**cuda bridge**」—— 上位 §9 :671「**生产仅纯 CPU**；支持 amd64 baseline、AVX2/FMA、AVX-512 子集」+ §8.4 :635「`acr/` **隔离实验（生产不可达）**」。本份**通篇未出现「生产不可达」限定**，读起来像一个生产模块 ⇒ 缺关键上位限定 = 与上位冲突（§1「下位与上位冲突时以上位为准」）。
  2. :34 节标题「## Science IDs」+ :36「依赖 phase2（ALG-UPM-* 等）」—— 上位 §8.5 :663 要求模块「端口引用**有效 DATA 合同**」，本份 :18「## Data contract: kernel 描述/路由配置/硬件 profile」**无任何 DATA-* 合同 ID** ⇒ 不满足上位 §8.5 的模块必备项。
  3. :40「## 性能特征 benchmark route estimator」—— 上位 §9 :672 规定 benchmark 输出 `cpu_profile` 到安装目录，本份的「qualification 矩阵」与上位 §9/§12 无指针关系（机制无法追溯，§3）。
- **处置建议**：**迁移 + 补齐或撤销** —— 二选一：①若 acr 仍是活跃隔离实验，迁 `docs/research/ACR_ISOLATED_EXPERIMENT.md`（依标准 01 §2：非生产、以佐证与对照为角色），开头补「生产不可达（依最高设计 §8.4）」正向限定，并把 :28「V19R2 前已修」删除；②若已无活跃实验面，**删除**（依 §2「已撤销设计不放在正式文档层」）。不判「合并」：无同主题第二份可并。**建议上呈负责人裁决**（依 AGENTS.md §10 第 5 类：登记面缺失导致文档与机器登记不可同时成立）。

---

### 15. docs/modules/astro_image_io.md

- **路径**：`docs/modules/astro_image_io.md`
- **标题**：Module: astro_image_io
- **行数**：82
- **角色**：模块工作细节（`docs/modules/` 对应）——**但页面名是已退役目录名**：MODULE_MAP :547 `aio` 条目 `legacy_paths: [lib/astro_image_io, lib/io, lib/healpix_db]`、:548 note「源 = lib/astro_image_io…（**ARCH-001 已迁入 target_dir**）」，规范模块是 `astrocs.infra.aio` / `docs/plugins/infrastructure/17_aio.md`。⇒ **角色对、页面身份错**（文件名指向我批另一份文档都未登记的历史目录），应重命名并入 aio 页。另 :24 使本份同时声称承载「块词表的唯一登记处」= **合同说明**职责 ⇒ 一份文档两角色。
- **主题**：aio 层（FITS/XISF/ahpx/HiPS/UPM 读写）的职责、API、数据合同、所有权、线程、错误、配置、缓存、诊断、测试面摘要 + 块词表登记处声明。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位（应 §8.5；对 I/O 层还应加 §10）。
- **下游**：按路径 2 个：`docs/DOCUMENT_INDEX.yaml:761`、`lib/infrastructure/aio/include/aio_upm.h`（`grep -rl 'docs/modules/astro_image_io' docs lib eng` → 3 行含 audit 台账）。有代码引用 ⇒ **不判孤儿**。
- **是否正本**：**副本**（aio 模块工作细节的正本按 MODULE_MAP :523 是 `docs/plugins/infrastructure/17_aio.md`）；:24 的块词表登记处声明使其另主张一份**合同正本**，而那按标准 01 §2 应在 `docs/contracts/` + `eng/contracts/`。
- **重复或重叠对象**：与 `docs/contracts/DATA_SEMANTICS.md` §11.1 重叠 = **PipelineFrame 命名块词表**。贴两处：
  - 本文 :24「**块词表的唯一登记处 = `lib/infrastructure/aio/include/aio_pipeline.h:287-305` 的「标准块定义表」**（aio 是文件级唯一 I/O 边界，块词表属 aio 的内存块合同 ⇒ 登记处归 aio）；**其它文档只作引用**——…**`docs/contracts/DATA_SEMANTICS.md` §11.1 的「帧内命名块」表只作引用**」
  - `docs/contracts/DATA_SEMANTICS.md:352`「### 11.1 输入（PipelineFrame 命名块；文件通道 FITS 另注）」（实存，本次 `grep -n '11\.1 '` 命中）
  - 差异 = 本份把**代码头文件的具体行号范围**立为唯一登记处并把合同文档降格为「只作引用」。这**直接逆上位 §0.1 :42**：「**代码：最低权威**，是具体设计的实现；与上位设计不一致时以上位为准」——一份文档不得把最低一层设为某主题唯一正本。同时 DATA-002 :141 已把 `F_p/W_p/D_p` 的词表定义在 interfaces 合同层承载 ⇒ 三处并存。
- **元信息块**：:3 上游标注合规；无 ID/状态块 ⇒ **本份不判 §4 元信息块违规**（与 acr.md 同）。
- **写法违规**（本批最重之一）：
  - :24 单条约 1200 字的段落内嵌「⚠ **Q8 待落地的代码侧项（登记，非本任务文件域）**：① 把 `variance` 块补进标准块定义表；② 删除 `:304`「未列出的自定义块名也允许」；③ 补机器判据（块名 ∉ 标准表 ⇒ 判红）」——**工包任务号（Q8）+「本任务」「非本任务文件域」派单语域 + 待办清单**写进正式模块文档。标准 01 §2「过程台账不放在正式文档层」+ §4「只写现行设计要怎样」双违。机械列 `task_ids=[]` ⇒ **漏报**（Q8 不在其 EXP-/RELEASE-/FIX- 式样集内）；
  - :78「UPM sparse **已于 V19R6R2 temp+rename 已修复**（**F-V19R2-IO-001 已闭环**，见 docs/architecture/IO_AND_ATOMICITY.md）」——**「已于…修复」「已闭环」= 历史叙事 + 审计轮次号 V19R6R2 + 缺陷编号 F-V19R2-IO-001**。机械列 `history_narrative=False`、`task_ids=[]`、`commit_sha=[]` ⇒ **三列同时漏报**（上位 §0.3 明文「已经撤销的设计与已经退役的实现不出现在正式文档里」）；
  - :78 同段「**HiPS tiles 仍非原子 —— 已登记的待修缺口（未闭合）**」+「orchestrator 日志路径嵌套 bug（非阻断）」——缺口/bug 台账写在模块页（应与 `docs/KNOWN_LIMITATIONS.md` 单正本对齐，标准 01 §2 同主题单正本）；
  - :23/:29「API-AIO-001..（**S2 注册**）」「（DATA-HIPS-SIGNAL-001 等，**S2 注册**）」——工包阶段号当依据；
  - :13「不写 testdata/」、:43「stale cache=2」等负向/裸码陈述，§4「用正向条款」。
- **悬空引用**：
  - :78「（最高设计 §9「**本期例外（如实登记）**」）」—— 该引语**不存在于最高设计**。命令：`grep -c '例外' ASTROCS_DESIGN.md` → **0**（正对照同命令形状 `grep -n '三个阶段调度器' ASTROCS_DESIGN.md` → 命中 :597/:630）。⇒ 模块页以引号形式引用了一条**上位从未写过的条款**，并把它当"原子发布缺口"的授权来源。**这是本批第二处伪造上位引语**（第一处见 orchestrator.md :60）。
  - :24「`lib/infrastructure/aio/include/aio_pipeline.h:287-305` 的「标准块定义表」」—— **行号范围错**。实测该表标题在 `:304`、表体至约 `:325`（命令：`grep -n '标准块定义表' lib/infrastructure/aio/include/aio_pipeline.h` → 130/133/151/304；表行 304-325）。所引 `287-305` 区间内容是 KV 块与缓存文件格式注释。⇒ 源码行号锚**指向错误区间**。
  - :24「`lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp` 的 6 个名字」——同段声明它「不是块词表」，为正向澄清，不算悬空。
  - 其余 :23、:26、:31、:34 指向的 `docs/contracts/DATA_SEMANTICS.md`、`docs/architecture/PERFORMANCE_MODEL.md`、`lib/infrastructure/aio/include/aio_hips.h`、`src/hips/aio_hips_writer.cpp` —— 见清单第 C 条；本次已核 `lib/infrastructure/aio/`、`lib/infrastructure/aio/healpix_db`、`docs/contracts/DATA_SEMANTICS.md` 实存。
- **与上位冲突**：
  1. **与上位 §0.1 唯一权威链冲突**（把代码行号设为某主题唯一登记处，把合同文档降为引用）——见重复对象栏。
  2. :67「日志 `run/logs/astro_image_io/`」与上位 §7.3 :530「日志目录默认 **`<output_dir>/logs`**」+ §10 :696「产品与**运行日志**只落块级 `output_dir`…；`run/` 只放开发/CI 过程产物与过程日志，**与运行日志不互替**」⇒ 模块页把运行日志落在 `run/` 下，与上位运行日志落点条款**直接相悖**。orchestrator.md :48 同型（见该份）。
  3. :24 的 Q8 三项**与现行代码相反**（三项都已落地）：①`variance` **已在**标准块定义表（实测该行存在：`variance | FLOAT32 | [H,W] | 逐像素方差 (signal 单位²; ivar=1/variance; 无信息=0 显式不可用)`）；②被要求删除的「未列出的自定义块名也允许」在头文件中**已不存在**（`grep -n '未列出的自定义块' lib/infrastructure/aio/include/aio_pipeline.h` → 无匹配），现行文字相反：「不存在"未列出的名字也允许"的隐式放行」；③判据已在（:151「返回: 0=成功, **9=块名不在标准块定义表且未显式注册**」）。⇒ **模块页把已闭合项登记为未闭合待办**，文档现状与代码相反（标准 01 §5 判定链「走得通」的反例）。
- **处置建议**：**合并 + 重命名** —— 本份内容按标准 01 §2「同一角色内同一主题只有一份正本」**合并入 `docs/plugins/infrastructure/17_aio.md`**（MODULE_MAP 认定的 aio 模块工作细节正本），页面随之撤销；:24 的合同正本声明**迁 `docs/contracts/`**（依 §2「JSON/schema/字段语义→docs/contracts/」对应 `eng/contracts/`），登记处回指符号名而非行号；:78 与 :67 的运行日志落点按上位 §7.3/§10 订正；「V19R6R2/F-V19R2-IO-001 已闭环」与 Q8 三项一并删除或移 `artifacts/evidence/`（§2 台账落证据层）。

---

### 16. docs/modules/calibration.md

- **路径**：`docs/modules/calibration.md`
- **标题**：Module: calibration
- **行数**：120
- **角色**：模块工作细节（`docs/modules/` 对应该角色）。但 MODULE_MAP :78 把 `lib/calibration` 列为 `legacy_paths`、:51 `id: calibration` 的正本页是 :54 `docs/plugins/algorithms_phase1/01_calibration.md` ⇒ 页面身份与 aio 一样属「旧目录名页 + 与 plugin 页双写」。判**重叠于 plugin 页、角色本身不错**。
- **主题**：calibration 模块（master 生成 / 单帧校准 / cosmetic 修复）的职责、调用点、API、数据合同、所有权、线程、错误、配置、科学 ID、性能、缓存、诊断、测试面摘要。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位（应 §8.5）。
- **下游**：7 个文件（`grep -rl 'docs/modules/calibration' docs lib eng` → `docs/algorithms/CALIBRATION_ALGORITHMS.md`、`docs/audit/doc_classification.csv`、`docs/contracts/UNIFIED_OBJECTS.md`、`docs/contracts/unified_object_registry.json`、`docs/DOCUMENT_INDEX.yaml`、`lib/algorithms/cosmetic/memory.md`、`eng/contracts/data/unified_object_compatibility_map_v1.json`）。**非孤儿**，且被 `eng/contracts/` 与 `docs/contracts/` 的登记面引用。
- **是否正本**：**副本**（同主题正本 = `docs/plugins/algorithms_phase1/01_calibration.md`（MODULE_MAP 指向）+ `docs/contracts/PUBLIC_API.md` §API-CAL-001 + `lib/algorithms/calibration/README.md`）。本份 :5-17 自己也这么说（见元信息块栏）。
- **重复或重叠对象**（三处并存，贴两段）：
  - 本文 :34-39「当前生产调用点：`lib/phase1_session/p1_session.cpp:243` 调 `ac_calibrate_frame`（calibrate stage）与 `:294` 调 `ac_correct_frame`（cosmetic stage，**c5629be6 引入**…）」
  - `docs/contracts/PUBLIC_API.md:135-137`「## astro_calibration C API（API-CAL-001）… > 头: `lib/algorithms/calibration/include/astro_calibration.h`（**唯一权威签名源，签名只取该头**）」+ :145-147 导出符号名册
  - 差异 = 本份给**调用点与行号**、PUBLIC_API 给**符号与并发合同**、`docs/plugins/algorithms_phase1/01_calibration.md` 给**模块规范**：三者主题同一（calibration 模块工作细节），拆成三处且互不指回唯一正本 ⇒ 标准 01 §2 同主题单正本不成立。第三处并存证据 = 本份 :5-11「本页为 calibration 模块摘要页：生产调用方、线程模型、错误语义、诊断/测试陈述、现状缺陷**一律以 lib/algorithms/calibration 现行源码为准**」——它自己承认不是正本，且把正本让给**代码**。
- **元信息块**：**有**（违 §4，且形态是"让权声明块"）。:5-17 共 13 行「> 本页为 calibration 模块摘要页：…一律以 lib/algorithms/calibration 现行源码为准；模块级合同与算法细节见 docs/algorithms/…；数据语义见…；API 合同见…与 docs/api/PHASE1_API_V1.md（API-P1-001）。cosmetic 域合同**已独立冻结**为 astrocs.p1.cosmetic（…）——与本页 P1-CAL 合同共享同一编译目标…本页**仅保留 P1-CAL 视角摘要**，cosmetic 域以 lib/algorithms/cosmetic/README.md 为**权威**。」—— 文档 ID 式让权 + 「已独立冻结」历史叙事 + 把权威让给代码 README。**机械列 `meta_block` 报 `False` ⇒ 漏报**（同批 dynamic_psf.md :5-11、gaia :5-7、healpix_drizzle :5-8、hips_p2 :5-14 同型，共 6 份）。
- **写法违规**：
  - :38「cosmetic stage，**c5629be6** 引入」——**commit 短 sha 入正文** + 「引入」历史叙事。机械列 `commit_sha=['c5629be6']` **命中，未漏报**（我批内该列唯一一次真命中，另两次命中 `5ecc60df…` 在别批 registry/phase2_* 页）；
  - :23-24「模块级状态 **CONTRACT_READY**；迁移目标 astrocs.p1.calibration / astrocs_p1_calibration.dll 由 **P1-CAL-IMPL 建立**」——**状态词预写**，直接违上位 §12.5 :802「状态由检查与验收现场计算，**登记表不预写状态**」；+ 任务编号 P1-CAL-IMPL + 「建立」（机械列 `task_ids=[]` ⇒ **漏报**）；
  - :39-41「`ac_generate_master_*`、`ac_set_num_threads` **当前无生产调用方**」、:65「（默认 team，**线程数非 16 硬编码**）」、:94「（无 ThreadLease，**迁移整改点**）」、:113「Python 对照为**历史层**（当前树无 python/ 目录）」——四处「现状/曾经/待改」叙事，其中 :65 是对已修 bug 的否定式自证、:113 明写「历史」；上位 §0.3 禁「已经撤销的设计与已经退役的实现不出现在正式文档里」。**机械列 `history_narrative` 报 `False` ⇒ 漏报**；
  - :66/:71-72/:94 反复引用 `DISP-CAL-001`/`DISP-CAL-002`/`DISP-COS-009` 缺陷编号 —— 与 `docs/algorithms/CALIBRATION_ALGORITHMS.md` §10 的 DISP 名册双写（标准 01 §2 同主题单正本）。
- **悬空引用**：
  - :11、:49、:119 等指向 `docs/api/PHASE1_API_V1.md`、`docs/contracts/PUBLIC_API.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/algorithms/CALIBRATION_ALGORITHMS.md`、`docs/science/CALIBRATION.md`、`lib/algorithms/cosmetic/README.md` —— 实存性见清单第 C 条（本批已核 `docs/contracts/PUBLIC_API.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/algorithms/CALIBRATION_ALGORITHMS.md`、`docs/api/PHASE1_API_V1.md` 存在）。
  - :35「`lib/phase1_session/p1_session.cpp:243`」—— **文件实存**（`ls` 已核），但**行号 243/294 未逐行核**（只读节点不展开代码复核，登记为「行锚待核」，不猜对错）。
  - :74「`cpp/cosmetic_corrector.cpp`」、:112「`lib/algorithms/calibration/tests/test_photometry_apply.cpp`」、:120「`cpp/cosmetic_corrector.cpp`（cc_* 通道）」—— 裸文件名/相对片段，无仓内唯一路径 ⇒ 软悬空（不可机器核对）。
- **与上位冲突**（本批**唯一一处与构建文件直接矛盾**，可复算）：
  1. :49「**未编译进 CMake 主构建**：cc_* DLL、optimize_dark_k、**apply_photometry**」+ :119「未编译：dark_optimizer.cpp、**photometry_apply.cpp**」←→ 构建事实：根 `CMakeLists.txt:589-598` 的 `add_library(astrocs_calibration STATIC …)` **源列表第 597 行即 `lib/algorithms/calibration/src/photometry_apply.cpp`**，其上方 :593-596 注释明写「此前只在测试 target 编译、生产零调用者 ⇒ Phase1 测光归一化从未应用到像素。**编入生产库后由 Phase1 photometry 节点调用（p1_op_photometry）**」。且 `docs/contracts/PUBLIC_API.md:177` 同口径：「`calibration::apply_photometry`（**已编译且生产已接线**：Phase1 photometry 节点同一步内施加到像素；RULING-DOC-01 订正）」；`docs/algorithms/CALIBRATION_ALGORITHMS.md:513-514` DISP-CAL-006 亦「**已订正**：已编译（根 `CMakeLists.txt`）且有生产调用方」。⇒ **本份保留了已被两处文档订正掉的旧口径，且与构建文件相反**。（`dark_optimizer.cpp` 未在 CMake 命中，那一半无矛盾 —— 精确到字段登记，不扩大。）
  2. :23 状态词 `CONTRACT_READY` 预写 ←→ 上位 §12.5 :802（见写法违规第 2 条）。
  3. :5-6「一律以 lib/algorithms/calibration **现行源码为准**」←→ 上位 §0.1 :42「**代码：最低权威**…与上位设计不一致时以上位为准」⇒ 权威链倒置（同 astro_image_io 第 1 条，本批 6 份模块页同型）。
- **处置建议**：**合并 + 就地订正** —— 内容按 §2 **合并入 `docs/plugins/algorithms_phase1/01_calibration.md`**（MODULE_MAP 认定正本），本份撤销；:49/:119 的 `apply_photometry` 编译状态**立即按 `CMakeLists.txt:597` 订正**（这是纯事实错，非风格问题，属最高优先级）；:38 `c5629be6` 与 :23-24 的 CONTRACT_READY/P1-CAL-IMPL、:65/:113 的现状叙事删除（§4 + 上位 §0.3/§12.5）；:5-17 让权块改为指向 plugin 正本的单向指针；DISP-CAL-* 只保留引用不在本页复述。

---

### 17. docs/modules/common.md

- **路径**：`docs/modules/common.md`
- **标题**：Module: common
- **行数**：65
- **角色**：模块工作细节（目录符合），但**对象在机器登记面里不存在**：MODULE_MAP 的 23 个模块 id 无 `common`（也无 `shared`），`docs/plugins/**` 23 篇里无 common/shared 篇。上位 §8.4 :626 只列 `lib/algorithms/` 下的 `shared/` 一行，未给它模块身份。⇒ 判**登记面缺格**而非角色错放。
- **主题**：`lib/algorithms/shared` 的三件基础库（HEALPix NESTED 实现、SHA-256、标量精度抽象）的 API 表、数据合同、所有权、线程、错误、配置、科学 ID、测试、已知限制。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位（ABI 在 §8.5；`shared/` 的目录依据才是 §8.4 :626，两处都该写）。
- **下游**：`grep -rl 'docs/modules/common' docs lib eng` → 仅 `docs/audit/doc_classification.csv` + `docs/DOCUMENT_INDEX.yaml`；第二套词 `grep -rn 'common\.md' docs lib eng` 排除这两台账 → **0**。**孤儿**。正对照（同形命令应命中）：`grep -rl 'docs/modules/calibration' docs lib eng` → 7（含 `eng/contracts/data/unified_object_compatibility_map_v1.json`、`docs/algorithms/CALIBRATION_ALGORITHMS.md`）。
- **是否正本**：**待定** —— 承载 :7/:17/:18 三项「唯一/权威」声明的文档，但既不在 MODULE_MAP 也不在 plugins 面 ⇒ 按标准 01 §3「追溯不到的机制先补要点再保留」，它目前是**无登记面的正本声明者**。
- **重复或重叠对象**：与 `docs/algorithms/HEALPIX_MAPPING.md` + `docs/contracts/DATA_SEMANTICS.md` §2-3 重叠 = **NESTED 映射与 tile 索引式**。贴两处：
  - 本文 :26「NESTED leaf local 18 bits `interleave(x,y)`，FITS index `(511-x)*512+y` 由 CDS Hipsgen oracle 冻结（`docs/contracts/DATA_SEMANTICS.md §2-3`）」
  - 本文 :52「详见 `docs/science/DRIZZLE.md` / `PHASE2_UPM.md` / `docs/contracts/DATA_SEMANTICS.md` §5 + `docs/algorithms/HEALPIX_MAPPING.md` **B4-01**」
  - 差异 = **同一条码内**把同一主题的权威同时许给三处（DATA_SEMANTICS §2-3、DATA_SEMANTICS §5、HEALPIX_MAPPING），再叠加 :17 自称「NESTED **唯一**实现」⇒ 三处承载、无一份被明确立为正本。判「重叠成立，本份为其中一腿」，非下钻分层（下钻需单一指回）。
- **元信息块**：无 ID/状态块（:3 上游标注合规）。
- **写法违规**：
  - :7「**共享权威基础库**」+ :17「NESTED **唯一**实现；…**唯一一套**（B4-01 去重）」+ :18「frame_id **唯一实现**」——标准 01 §1「任何文档不声明自己的第二套权威顺序或『唯一口径』」+ 上位 §0.1 :44「**科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`**」。模块页自称权威并三处「唯一」= 自设权威；
  - :17「唯一一套（**B4-01 去重**）」——审计条目号 + 「去重」历史叙事（曾有两套 ⇒ 上位 §0.3 禁的退役实现叙事）。机械列 `task_ids=[]`、`history_narrative=False` ⇒ **两列漏报**；
  - :32「（静态库，非纯 header-only —— 见 **ENG-C-04 澄清**）」——编号 + 「澄清」= 讨论史；
  - :61「当前**仅 Linux x86_64** 字节序路径（`sampler.cpp:250-364` payload 字节语义）」——现状叙事 + 跨模块裸行号（实测 `find lib -name sampler.cpp` → 唯一命中 `lib/algorithms/coverage/src/sampler.cpp`，属 coverage 模块）；
  - :44「`Sha256::final_hex` 后再 `update` → **未定义**（合同约定 = `final_hex` 为终态）」——以「未定义」承载硬约束，§4 要求正向条款（可写「不得在 `final_hex` 后调用 `update`」）。
- **悬空引用**：
  - :65 Source files 的花括号展开产出**不存在的路径**：`lib/algorithms/shared/{…,`**`lib/include/astro_scalar.h`**`,…}` ⇒ 展开为 `lib/algorithms/shared/lib/include/astro_scalar.h`。实测唯一真实位置：`find lib -name astro_scalar.h -o -name precision_context.h` → `lib/algorithms/shared/include/astro_scalar.h`、`lib/algorithms/shared/include/precision_context.h`。⇒ **同一文档 :19-20 表格写法正确、:65 汇总写法错误**（自相矛盾）。:65 末项 `Makefile` 实测**存在**，不判悬空。
  - :61 `sampler.cpp` 无目录前缀 ⇒ 不可唯一解析（软悬空）。
  - :52 的 `docs/science/DRIZZLE.md`、`docs/science/PHASE2_UPM.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/algorithms/HEALPIX_MAPPING.md` —— 见清单第 C 条；本份未逐个展开，不作断链判定。
- **与上位冲突**：
  1. :7/:17/:18 三处自设「唯一/权威」←→ 上位 §0.1 :44（算法推导唯一权威在 science/algorithms 两目录）+ 标准 01 §1。
  2. :61「仅 Linux x86_64」←→ 上位 :104 与 §11（双正式平台 Windows x64 + Linux amd64）。
  3. :11「不做图像校准/…/不做 FITS/XISF/HiPS I/O」——上位 §8.4 :626 的 `shared/` 行**不含 SHA-256 与精度抽象**；本份扩为「共享权威基础库」，而 MODULE_MAP 无条目 ⇒ **模块身份在上位树与机器登记面之间不一致**（上位 §0.2 :53「登记表承载定义」的缺格）。
  4. :48 `precision` 配置「由 `lib/infrastructure/pipeline/orchestrator/configs/stage1.schema.json` 经 orchestrator 透传」——该文件**在跟踪集里实存**（`git ls-files -- 'lib/infrastructure/pipeline/orchestrator/configs/stage1.schema.json'` → 1），但上位 §3.2 三类配置与 §7.2 `--json` 口径**不含 stage1.schema.json 通道**，且 orchestrator 自身归属正处争议（本批第 23 份）⇒ 下位把配置权威指向一个上位未承认、归属未收敛的入口，违上位 §3.2/§7.2。
- **处置建议**：**迁移 + 登记补格** —— ①拆分：HEALPix 映射式部分**合并入 `docs/algorithms/HEALPIX_MAPPING.md`**（上位 §0.1 :44 指定的算法推导权威），三件库的职责/端口/测试摘要并入 `docs/plugins/infrastructure/` 新建 shared 篇并在 **MODULE_MAP 补 `id: shared` 条目**（依 §3「追溯不到的机制先补要点再保留」）；②:65 改用 :19-20 的正确路径写法，消除展开歧义；③删 :7/:17/:18 的「唯一/权威」表述，改正向「实现家 + 指回 science/algorithms 正本」；④:61 Linux-only 限定按上位双平台条款改写；⑤B4-01/ENG-C-04 编号删。

---

### 18. docs/modules/dynamic_psf.md

- **路径**：`docs/modules/dynamic_psf.md`
- **标题**：Module: dynamic_psf
- **行数**：51
- **角色**：模块工作细节（目录符合），但**页面名是退役目录**：MODULE_MAP :166 psf 条目 `legacy_paths: [lib/dynamic_psf]`、:167 note「源 = lib/dynamic_psf（ARCH-001 **已迁入** target_dir）」；规范落位 `lib/algorithms/psf/`、规范名 `astrocs.p1.psf`、MODULE_MAP 认定正本页 `docs/plugins/algorithms_phase1/04_psf.md`（实存）。⇒ 与 astro_image_io/calibration/healpix_drizzle 同型。
- **主题**：Moffat4 动态 PSF 建模与拟合质量代理（q_psf / residual_scale）摘要页。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位。
- **下游**：**实质引用者 0**。三套检索词一致：`grep -rl 'docs/modules/dynamic_psf' docs lib eng` → 仅 `docs/audit/doc_classification.csv` + `docs/DOCUMENT_INDEX.yaml`；`grep -rn 'dynamic_psf\.md' docs lib eng` 排除两台账 → **0**；`grep -rn 'Module: dynamic_psf' docs lib eng` → **0**。正对照（同形命令应命中）：`grep -rl 'docs/modules/gaia_xpsd_client' docs lib eng` → 7。⇒ **孤儿成立**。
- **是否正本**：**副本，且无人消费的副本** —— :5-11 自认合同页在代码 README；同时同一模块已有 registry 页承载登记面。
- **重复或重叠对象**：与 `docs/modules/registry/astrocs.phase1.star-psf.md`（别批，本次为判重复而读）重叠 = **同一 PSF 模块页**。贴两处：
  - 本文 :1「# Module: **dynamic_psf**」+ :6-7「PSF 模块合同页 = lib/algorithms/psf/README.md（r1，CONTRACT_READY）+ lib/algorithms/psf/module.yaml（**astrocs.p1.psf**，迁移目标 astrocs_p1_psf.dll）」
  - `docs/modules/registry/astrocs.phase1.star-psf.md:1-4`（实读）「--- id: MOD-astrocs-phase1-star-psf / **module_id: astrocs.p1.psf** / **aliases: [astrocs.phase1.star-psf]** / version: 1.0.0」
  - 差异 = registry 页 front-matter 已承载 `module_id` 与规范别名（登记面事实），本份承载摘要 + 让权声明；**两份的标题名（`dynamic_psf` / `astrocs.phase1.star-psf`）都不是规范名 `astrocs.p1.psf`**，加上 `docs/plugins/algorithms_phase1/04_psf.md` = **同一模块四处承载、三个名字**。MODULE_MAP :149 已把 `astrocs.phase1.star-psf` 降为 `aliases`（注「MOD-002 中面：D 面将检测/PSF 拆两页」）⇒ 机器面已定档，文档面未跟上。标准 01 §2 同主题单正本不成立。
- **元信息块**：**有**（违 §4）——:5-11 七行「> 本页为模块说明页；PSF 模块合同页 = …（**r1**，**CONTRACT_READY**）…；冻结合同 SCI-P1-PSF-001 / ALG-STARPSF-001（STAR_PSF_ALGORITHMS §11）/ DATA-P1-PSF（DATA_SEMANTICS §15）/ API-PSF-001（PUBLIC_API）；测试设计 TEST-PSF-DESIGN-001；**现状缺陷 DISP-PSF-001..006**。本页其余章节**以 co-located README r1 为准**。」含版本锚 r1、状态词、缺陷区间，末句让权给代码。**机械列 `meta_block=False` ⇒ 漏报。**
- **写法违规**：
  - :6-7 `CONTRACT_READY` 状态预写 ←→ 上位 §12.5 :802「状态由检查与验收现场计算，**登记表不预写状态**」；
  - :6/:11 版本锚「**r1**」两处；
  - :7「**迁移目标** astrocs_p1_psf.dll」与 :51「Source files **lib/algorithms/psf/**」——同一页用退役名作标题、用新名作实现位置 = 「已迁完而页名未改」的历史残留（上位 §0.3）；
  - :10「现状缺陷 DISP-PSF-001..006」——缺陷台账入摘要页（应与 `docs/KNOWN_LIMITATIONS.md` / ALG 文档 §10 单正本对齐）；
  - :19「不进 Phase2 science weight（**SNR-008** 边界）」、:47「合成 Moffat 恢复（**PSF-001..008**）」——无出处编号。
- **悬空引用**：
  - :6-7 `lib/algorithms/psf/README.md`、`lib/algorithms/psf/module.yaml` —— **实存**（`git ls-files -- 'lib/algorithms/psf/*'` 与 `ls` 双核）。
  - :8「ALG-STARPSF-001（**STAR_PSF_ALGORITHMS §11**）」——裸文档名无路径；仓内实名 `docs/algorithms/STAR_PSF_ALGORITHMS.md` ⇒ 路径缺失（可猜不可核）。
  - :21「`dynamic_psf DLL`」—— **不存在的构建产物名**：MODULE_MAP :145 psf 条目 `target: astrocs_p1_psf`、:166 legacy `lib/dynamic_psf`；`git ls-files | grep -i dynamic_psf` → 无 target/文件命中该 DLL 名（跟踪集里 `dynamic_psf` 只作历史路径存在）。⇒ 指向不存在的产物。
  - :19 `SNR-008`、:47 `PSF-001..008` —— 本次在跟踪集内未找到定义处（`git grep` 未命中定义），登记为不可解析编号。
- **与上位冲突**：
  1. :5-11「合同页 = 代码 README」「本页其余章节以 co-located README r1 为准」←→ 上位 §0.1 :42「**代码：最低权威**，是具体设计的实现；与上位设计不一致时以上位为准」。文档把最低权威立为本页权威 = **权威链倒置**（本批第 3 份显式表述、累计 6 份同型）。
  2. :21 `dynamic_psf DLL` 与 :7 `astrocs_p1_psf.dll` 同页并存 ←→ 上位 §8.4 :657「基建按稳定职责合并为有限模块，**职责名全仓唯一**」⇒ 职责名不唯一。
  3. :13-15 职责「Moffat4 动态 PSF 建模」——Moffat4 这一具体模型属 `docs/algorithms/` 推导权威面（上位 §0.1 :44），本份未给指针 ⇒ 机制追溯断（标准 01 §3）。
- **处置建议**：**删除（并入正本）** —— 本份同时是**孤儿 + 自称副本 + 与 registry 页/plugins 页三重双写**：按标准 01 §2「同一角色内同一主题只有一份正本；多份内容合并为一份，**合并后其余删除**」，其独有信息（职责一句话、`q_psf`/`residual_scale` 代理语义）并入 `docs/plugins/algorithms_phase1/04_psf.md`，页面删除，同步撤 `docs/DOCUMENT_INDEX.yaml:772` 条目。判删除前已核：`docs/audit/{doc_classification,inventory}.csv` 属可由脚本再生的台账（别批），不构成保留理由。

---

### 19. docs/modules/gaia_xpsd_client.md

- **路径**：`docs/modules/gaia_xpsd_client.md`
- **标题**：Module: gaia_xpsd_client（astrocs.catalog.gaia / CAT-GAIA）
- **行数**：87
- **角色**：模块工作细节，**落位正确且与机器登记面一致**：MODULE_MAP :673 `id: gaia_xpsd_client`、:674 `scope: infra`、:683 `module_id: astrocs.catalog.gaia`、:676 `plugin_doc: docs/plugins/infrastructure/22_gaia_xpsd_client.md`。⇒ 本批唯一一份「页名 = 规范模块名 + 有 plugin 对页 + 有 MODULE_MAP 条目」的页面（与 6 份旧名页对照）。
- **主题**：本地 XPSD 星表客户端（Gaia DR3/DR3SP 离线解析、锥搜、DR3SP 光谱解码、两级缓存、极冠保守剪枝）摘要。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位。
- **下游**：7 个文件（`grep -rl 'docs/modules/gaia_xpsd_client' docs lib eng`，含 `eng/contracts/data/unified_object_compatibility_map_v1.json`、`docs/contracts/UNIFIED_OBJECTS.md`、`docs/DOCUMENT_INDEX.yaml`）。**非孤儿。**
- **是否正本**：**待定，实质偏副本** —— :21-29 自建「合同 ID」表把 SCI/ALG/DATA/API/ARCH/TEST 六面各指别处正本，且 plugin 规范页另在；本份承载的应是摘要，实际多了**一张合同 ID 路由表**（与 `docs/contracts/INDEX.yaml` 同职能）。
- **重复或重叠对象**（两处）：
  - 与 `docs/contracts/INDEX.yaml` 重叠 = **合同 ID↔路径映射**。本文 :23-27「SCI: SCI-AST-001（docs/science/ASTROMETRY.md）/ ALG: ALG-GAIA-001（docs/algorithms/GAIA_QUERY.md）/ DATA: DATA-GAIA-001（docs/contracts/DATA_SEMANTICS.md §8）/ API: API-GAIA-001（docs/contracts/PUBLIC_API.md §gaia_client C API）/ ARCH: ARCH-001（docs/contracts/ARCH-001.md）」——标准 01 §3「登记表承载定义与映射」+ 上位 §0.2 :53 同条 ⇒ 逐模块在正文重抄映射 = 第二份登记面，必然漂移。
  - 与 `docs/plugins/infrastructure/22_gaia_xpsd_client.md`（别批）重叠 = **同一模块工作细节**。差异 = plugin 篇按上位 §8.5 :663 的模板承载规范正文，本份是其摘要 + 路由表 + 现状注记；且本份**不在 MODULE_MAP 的映射面内**（`grep -c 'docs/modules/' docs/modules/MODULE_MAP.yaml` → **0**，正对照 `grep -c 'docs/plugins/'` → **56**）⇒ 摘要与规范并存且无指回，标准 01 §2 同主题单正本违例。
- **元信息块**：**有**（违 §4）——:5-7「> 全部条目以 `lib/infrastructure/gaia_xpsd_client/src/gaia_client.c/.h` **为准**（职责、输入输出、错误语义与测试面）。」+ :1 标题内「（astrocs.catalog.gaia / **CAT-GAIA**）」合同/工包 ID。让权声明 + 标题 ID 双违。**机械列 `meta_block=False` ⇒ 漏报。**
- **写法违规**：
  - :28-29「TEST: TEST-GAIA-DESIGN-001（GAIA_QUERY.md §5，设计冻结；可执行 TEST-GAIA-* 由 **CAT-GAIA-TEST 建立**）」——工包号 + 「建立」；机械列 `dates=[86]` 命中 :86（同型 CAT-GAIA-DOC），`task_ids=[]` ⇒ 漏报；
  - :61「分配故障路径全 checked（**V18R3**）」——审计轮次号；
  - :19「不负责：…（**迁移后**由 host executor/ThreadLease 授予）」与 :53-54「**现状**使用 OpenMP 默认 team（**迁移后**由 host ThreadLease 授予）」——迁移前后叙事，且**同一句在 :19 与 :53-54 重复两遍**（自洽缺陷）；上位 §0.3 禁退役实现入正式文档；
  - :63「**"network errors never alter scientific identity"** 由零网络 I/O 结构性满足」——英文裸引号句**无出处**（不指上位条款、不指需求号），违 §3 可追溯；
  - :81「module.yaml / README.md（**CAT-GAIA-DOC 冻结**）」、:76「当前**无 PASS 声明**」——工包号 + 测试结论叙述（属 `artifacts/evidence/`/CI 面）；
  - :33-36「12 个 `GAIA_EXPORT` C 符号（create/create_ex/destroy、cone_search、cone_search_for_solver、get_db_type、get_file_count、get_total_sources、cone_search_with_spectrum、query_spectrum_by_coords、cone_search_with_photometry、get_spectrum_params）」——声明 12、列出 10 ⇒ **计数与列出不一致**（自洽缺陷）。
- **悬空引用**：
  - :23-27 的 `docs/science/ASTROMETRY.md`、`docs/algorithms/GAIA_QUERY.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/contracts/PUBLIC_API.md`、`docs/contracts/ARCH-001.md` —— **全部实存**（逐个 `ls` 已核）⇒ 本条为**正例**，本批引用质量最高的一份。
  - :73「GAIA_QUERY.md §5」**丢了 :24 已给的完整路径**，退化为裸文件名 ⇒ 同文档内引用形式不一致。
  - :82-83「追溯行：`docs/traceability/TRACEABILITY_MATRIX.json` `MOD-astrocs-catalog-gaia`」——文件实存；JSON 内该键本次未展开 ⇒ 登记「待核」，不猜。
  - :6 `gaia_client.c/.h` —— 实存。
- **与上位冲突**：
  1. :5-7「全部条目以…**gaia_client.c/.h 为准**」←→ 上位 §0.1 :42 代码最低权威 ⇒ 权威链倒置（第 2 份显式表述）。
  2. :19/:53-54「现状 OpenMP 默认 team」←→ 上位 §9 :673「worker 数只来自 profile 与预算对象，模块**不硬编码线程数、不私建长期线程池**」。**本份如实登记了与上位不符的事实却未标为偏差**，以现行口吻陈述 ⇒ 上位冲突 + 写法缺陷叠加（对比 `docs/KNOWN_LIMITATIONS.md` 的在册形态）。
  3. :14「两级查询缓存（结果缓存 60s TTL 精确键 + 解压块缓存 8192 槽 **≤4GB**）」——上位 §9 :675 要求缓存具「容量、身份、失效、线程模型」四要素且「受**字节预算**与 LRU 约束」：本份给了容量/失效/身份，**未把该缓存的线程模型与 :51-54 绑定**，且 ≤4GB 常数无来源指针（上位 §3.2 规定默认值唯一来源 = `eng/packaging/config/defaults.json`；标准 01 §4 要求数值有推导或登记来源）⇒ 四要素缺一 + 常数无出处。
  4. :11/:17「Gaia DR3 / DR3SP，离线、**零网络**」与上位 §2/AGENTS.md §2「Gaia 本地星表解析（离线、零网络）」一致（**无冲突**，登记为正面项）。
- **处置建议**：**保留（作模块页模板）+ 就地订正** —— ①删 :5-7 让权块，改「符号与签名以 `docs/contracts/PUBLIC_API.md` §gaia_client 为准」的**正向指针**（恢复权威链，依上位 §0.1）；②删 :21-29 合同 ID 路由表，交回 `docs/contracts/INDEX.yaml`（标准 01 §3 唯一索引地图）；③删 :8/:28/:61/:76/:81 的状态词与轮次号（上位 §12.5 :802）；④:19/:53-54 合一并按上位 §9 :673 标为已登记偏差（引 `docs/KNOWN_LIMITATIONS.md` 条目号，不裸留）；⑤:14 的 8192/≤4GB/60s 补来源指针；⑥:63 英文引语补出处或改中文正向条款；⑦:33-36 的 12/10 计数订正。

---

### 20. docs/modules/healpix_browser_qt.md

- **路径**：`docs/modules/healpix_browser_qt.md`
- **标题**：Module: healpix_browser_qt
- **行数**：47
- **角色**：模块工作细节，但**对象不是产品模块**：上位 §8.4 :636「`hips_browser/` **未来 GUI 组件（不进产品清单）**」；MODULE_MAP :706-734 该条目 `product_manifest.required: false`、:732 `exemption_reason:` 引「23_hips_browser『未来 GUI 可视化组件（不进产品 manifest）』」。标准 01 §2 该角色行写「每个**生产**模块做什么」⇒ **角色错放**（应作未来组件设计说明，或至少在开头写明非产品身份）。另：本份标题 `healpix_browser_qt` 是 MODULE_MAP :715 `co_located_tests` 与代码里的**子目录名**，模块规范名是 `astrocs.infra.hips_browser`（:716）⇒ 以子目录名当模块名。
- **主题**：Qt6 + OpenGL 的 HiPS/HEALPix 球面浏览器（STF 显示、可选构建）摘要。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位，且本模块的上位关键限定在 :636 同一节内，**本份通篇未提**。
- **下游**：13 个文件（`grep -rl 'docs/modules/healpix_browser_qt' docs lib eng`），**全部位于 `lib/infrastructure/hips_browser/healpix_browser_qt/` 内部**（main.cpp、main_window.{h,cpp}、stf_panel.{h,cpp}、gl_renderer.{cpp,h}、abstract_view.{h,cpp}、browser_backend.h、README.md、CMakeLists.txt 等）。⇒ 本批唯一一份**代码逐文件反向引用文档**的页面（正面项），但**文档层实质引用者为 0**。
- **是否正本**：**待定** —— 与 MODULE_MAP :709 指向的 `docs/plugins/infrastructure/23_hips_browser.md`（实存）构成同主题对页；本份 47 行摘要且不在映射面内。
- **重复或重叠对象**：与 `docs/plugins/infrastructure/23_hips_browser.md` + MODULE_MAP :732 重叠 = **hips_browser 模块工作细节与其非产品豁免口径**。贴两处：
  - 本文 :7「HiPS/HEALPix 球面浏览器（Qt6 + OpenGL，STF 显示，**可选构建**）」——全文无「不进产品清单」字样
  - `docs/modules/MODULE_MAP.yaml:732`「exemption_reason: docs/plugins/00_INDEX.md §2（23_hips_browser「未来 GUI 可视化组件（**不进产品 manifest**）」）+ ASTROCS_DESIGN.md §7.1（hips_browser/ 未来 GUI 可视化组件（不进产品 manifest））」
  - 差异 = 登记面与上位面已就"非产品"给出正式口径，本份只字未提 ⇒ 属**口径缺失型重叠**（本份是不完整副本），不是重复表述。
  - 附注：:732 自身把该结论的上位条款号写成 **§7.1**（命令树），实际在 **§8.4 :636** ⇒ 该处条款号错（属第 13 份的缺陷，此处只作旁证）。
- **元信息块**：无 ID/状态块（:3 上游标注合规）——与 acr.md 并列**本批仅两份无元信息块的模块页**。
- **写法违规**：
  - :43「STF engine 单测；视觉验收（**工程控制 16 spec**）」——验收依据指向工包（且该工包文档已不在跟踪集，见悬空引用）；
  - :11「不参与科学处理链」、:35「无（展示层）」——负向陈述，§4 要求正向条款；
  - **本份无日期、无任务编号、无 commit、无历史叙事**（机械列 `dates=[43]`、其余 `[]`/False）⇒ **本批写法最干净的一份，建议作其余模块页的正向模板**。
- **悬空引用**（本批唯一一处经删除历史确证的断链）：
  - :43「**工程控制 16 spec**」—— **曾存在、已删除**。命令与结果：
    `git log --diff-filter=A --name-only -- '工程控制/' | grep 16_` → 命中 `工程控制/docs/16_BROWSER_VISUAL_ACCEPTANCE_SPEC.md`；
    `git ls-files -- '工程控制/docs/*'` → **空**（不在 HEAD 跟踪集）；
    `git log --diff-filter=D --format='%h %ad %s' --date=short -- '工程控制/docs/16_BROWSER_VISUAL_ACCEPTANCE_SPEC.md'` → **`b7b2dea7 2026-09-02 docs(governance): GOV-002 归档非当前工程文档`**。
    ⇒ 一份模块文档的**验收依据**指向 2026-09-02 已被归档删除的控制包文件，不可复算。标准 01 §2「需要留证的台账落 `artifacts/evidence/` 并由正本引用」+ §3 双向可追溯均不成立。
  - :15「healpix_browser_core.h；Qt widgets 层」——裸文件名无路径。
  - :19「HiPS tiles 读（**astro_image_io**）」——用退役模块名指 `aio`（MODULE_MAP :547-548 实证），与本批第 15 份同一命名残留；该名字在 HEAD 跟踪集里已无目录。
  - :47「lib/infrastructure/hips_browser/healpix_browser_qt/」—— **实存**（含 `tests/`），与本份 :7「可选构建」一致。
- **与上位冲突**：
  1. 通篇缺上位 §8.4 :636 + MODULE_MAP :732 的「不进产品清单」限定 ⇒ 按本份读会认为它是生产模块，**与上位产品清单/发布口径冲突**（上位 §12.4/§12.5 与 §13）。
  2. :3 上游只指「§8.4（模块与 ABI）」——号名错位，且指向的是同一节里与它无关的那一条（本模块的位置在 :636，ABI 要求在 §8.5）。
  3. :39「STF 0.5%/99.5% 分位」——常数无出处（标准 01 §4「数值表放对应下层文档」+ 上位 §3.2 默认值来源条款）。
  4. :19「astro_image_io」用词与上位 §10 :686「**`aio` 是文件级唯一 I/O 边界**」不一致（退役名）⇒ 与第 15 份同根因。
- **处置建议**：**改名 + 合并 + 补限定** —— ①与 `docs/plugins/infrastructure/23_hips_browser.md` **合并为一份**（标准 01 §2 同主题单正本；本份 47 行摘要无独有事实，合并成本最低），模块名对齐 `hips_browser`；②合并后**开头必须写**「不进产品清单（依最高设计 §8.4 与 MODULE_MAP `hips_browser` 条目 `required=false`）」；③:43 的「工程控制 16 spec」替换为长期存续的判据（上位 §12.4 四层验收 L4 视觉条款或其 `docs/ci/` 判据号），不得指向已删除工包；④:15/:19 补路径、退役名改 `aio`；⑤:11/:35 负向陈述改正向。本份自身写法干净，**其正文可作其它模块页的改写底本**。

---

### 21. docs/modules/healpix_drizzle.md

- **路径**：`docs/modules/healpix_drizzle.md`
- **标题**：Module: healpix_drizzle
- **行数**：99
- **角色**：模块工作细节；页面名是**迁移前生产源名**：MODULE_MAP :284 drizzle 条目 `legacy_paths: [lib/healpix_db/healpix_drizzle, lib/hips]`、:285 note「源 = lib/healpix_db/healpix_drizzle（生产源）+ lib/hips（P1-HIPS 目标目录）（ARCH-001 已迁入 target_dir 子目录）」；规范模块 `astrocs.p1.drizzle`，MODULE_MAP 认定正本页 `docs/plugins/algorithms_phase1/08_drizzle.md`（实存）。⇒ 与 calibration/dynamic_psf/astro_image_io 同型旧名页。
- **主题**：球面 Drizzle（HEALPix NESTED 重投影 + 通量守恒累加 + 方差传播 + auto nside + FP32/FP64 + Sphere→Plane 反向 + 操作计数诊断）摘要。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位；Drizzle 的科学上游（§2.4/§4.2 + `docs/science/DRIZZLE.md`）未指。
- **下游**：5 个文件（`grep -rl 'docs/modules/healpix_drizzle' docs lib eng`）。**非孤儿。**
- **是否正本**：**副本**（正本 = `docs/plugins/algorithms_phase1/08_drizzle.md`，MODULE_MAP :260 指向）。
- **重复或重叠对象**（本批**最重要的一处上位/下位实质相反**）：与 `docs/science/DRIZZLE.md`（科学正本，别批）重叠 = **源像素 NaN/Inf 在 drizzle drop 的处置**。贴两处：
  - 本文 :56-59「**值像素 NaN/Inf 经 `F_p=Σx_j·w_jp` 直接传播、drizzle 层不掩膜**（实现 `drizzle_engine.cpp:1899-1902` 对值像素直接传播；科学锚 **`docs/science/DRIZZLE.md:116`**，回归 finalize 层，covered_area≤0 → variance NaN）」
  - `docs/science/DRIZZLE.md:144`（实读）「| 源像素 NaN/Inf（值） | **样本级掩膜 + 重归一 + 覆盖级 NaN + 强制计数**（`rule_id = NAN-SAMPLE-MASK-COVERAGE-NAN`；**唯一口径文字 = `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md` §2a**）：不合格样本从 `F_p`、分母、方差三项中一并剔除并**重新归一**——**掩膜作用域 = 样本级**… | `spherical_overlap.cpp:192`（几何 NaN 显式拒绝） |」
  - 差异 = 科学正本命令**样本级掩膜**，模块页声明**不掩膜、直接传播**——同一物理量两套相反处置。且本文所引「科学锚 `DRIZZLE.md:116`」经 `sed -n '112,120p'` 实测是**通量守恒（FZ-COND-FLUX-CONSERV）**段，不含 NaN 处置 ⇒ **锚号错 + 实质与科学正本冲突**双缺陷。三层权威关系（上位 §5.5 :426 下沉 → `docs/science/DRIZZLE.md:144` → 该条又指 `DATA-002 §2a` 为唯一口径文字 → 本模块页相反）见 §2 表 G6。
- **元信息块**：**有**（违 §4）——:5-8「> 合同 ID = ALG-DRZ-001（**唯一合同引用**，registry 登记名同此）；模块合同落位 lib/algorithms/drizzle/（**迁移目标目录**，astrocs.p1.drizzle）；本页登记职责、端口、线程/确定性、错误与测试面，**全部条目以现行源码与冻结合同为准**。」含自设权威「唯一合同引用」+ 迁移历史 + 让权给代码。**机械列 `meta_block=False` ⇒ 漏报。**
- **写法违规**（与 MODULE_MAP 并列本批最密）：
  - **:77-85 整个 `## V19R3 bounded target-ipix geometry cache` 节** —— 节标题即审计轮次号；:80「计数**新增** target_boundary_builds / target_geometry_builds / geometry_cache_hits / geometry_cache_misses」、:83「科学等价：candidate oracle **9003/0**、freeze **42/42**、UPMW-005 MC **k_corr=1.3883 不变**」——**变更日志 + 实验数值**入模块文档（标准 01 §2「过程台账不放在正式文档层」+ §4「无历史叙事」+ 上位 §0.3）。⇒ **本批最重的一处历史叙事**；机械列 `history_narrative=[]`、`task_ids=[]` **双漏报**；
  - :5「唯一合同引用」——自设唯一口径（标准 01 §1 禁）；
  - :21-22「不做 master/校准/坏点（**P1-CAL/P1-COS**）」、:92「可执行 TEST-P1-DRZ-001 由 **P1-DRZ-TEST 建立**」、:23「ThreadLease 由 **P1-DRZ-IMPL** 接线」——工包号 + 「建立/接线」；
  - :56「无集中枚举——**登记缺陷**」、:98-99「poly_clip.cpp 编入但生产路径零调用——**DISP-DRZ-008**」——缺陷台账语；
  - :8「全部条目以**现行源码**与冻结合同为准」——让权给代码（第 5 份同型）；
  - :42「`hp_drizzle_api.cpp`（依据 **`ENGINEERING_SPEC.md:129`**）；**行号待随实现复核，以符号名为准**」——以行号当出处，且**文档自己承认自己的行锚不可靠**；
  - 源码行号密集（本批最多，11 处）：:17 `astro_sphere_sink.cpp:100`、:23/:98 `CMakeLists.txt:379-382`/`:356-366`、:27 `hp_drizzle_api.h:43,62,70,130,139,140`、:49/:51/:52 `drizzle_engine.cpp:1671`/`:1762-1785`/`:1659-1660`、:59 `drizzle_engine.cpp:1899-1902`。**行锚已实际漂移有旁证**：`docs/algorithms/DRIZZLE_GEOMETRY.md:330` DISP-DRZ-007 明记「**行号漂移**：现行方差锚 astro_sphere_sink.cpp:100 + aio_hips_writer finalize_tile | DRIZZLE.md:132 vs drizzle_engine.cpp:2-3」。
- **悬空引用**：
  - :59「科学锚 `docs/science/DRIZZLE.md:116`」—— 文件在跟踪集内实存，**行号指向错误条款**（:116 为通量守恒段）⇒ 锚级悬空，且真实条款（:144）与本份口径相反。
  - :23、:98 裸 `CMakeLists.txt:379-382` / `:356-366` —— 无目录前缀，全仓根 `CMakeLists.txt` 与多个模块级同名文件并存 ⇒ **不可唯一解析**（软悬空）。
  - :83「9003/0、42/42、k_corr=1.3883」—— 三组实测数**无 `artifacts/evidence/` 路径** ⇒ 不可复算（标准 01 §2 台账落证据层并由正本引用）。
  - :14/:17 `astro_sphere_sink.cpp:100`、:30 `spherical_overlap.h`、:42 `hp_drizzle_api.cpp`、:88 `lib/algorithms/drizzle/healpix_drizzle/tests/*.cpp` —— 无完整仓内路径（:42 自陈行号待复核）。
  - 机械列 `dangling_links=[]` 对本份**只算半对**：本份无 markdown 链接，但它对行号锚/裸文件名/错锚号三类完全无覆盖。
- **与上位冲突**：
  1. **与 `docs/science/DRIZZLE.md:144` 直接相悖**（不掩膜 vs 样本级掩膜）⇒ 上位 §0.1 :44 + 标准 01 §1「下位与上位冲突时以上位为准；冲突本身是缺陷，需在整治中消除」⇒ 本份该句按规范应作废。
  2. :17「归一（S=F/D、variance=sumVarNum/**D²**、ivar）在 astro_image_io finalize 层」与 `docs/science/DRIZZLE.md:119`「`variance_p=V·(Σ w_jp²/**N_p²**)`」——**同一归一式分母符号不同**（`D` vs `N_p`），且 DATA-002 :145-149 明文「`D_p` 与 `W_p` 量纲不同，**各自具名、不可互换**」⇒ 公式双写 + 符号不一致型上位冲突（需按 DATA-002 符号条款定档）。
  3. :49-51「1/N 确定性（同输入同线程数 bitwise；**跨线程数不保证 bitwise**）」——上位 §8.3 :616 + §9 :677「1 worker 与 N worker 的**等价判据是事前冻结的浮点容差，不是逐位一致**；容差来源按优先级取——模块科学页已冻结三档的按其三档，未冻结的按 `docs/contracts/TEST_MATRIX.md` §2」。本份只写"不保证 bitwise"却**不给本模块冻结的容差档与出处** ⇒ 追溯断 + 读作免责句。
  4. :37「tile_depth=9、**nside≥512 硬门**」——常数无出处指针（上位 §3.2 默认值唯一来源 `eng/packaging/config/defaults.json`），且「硬门」在上位 §4.5 预检三档里无对应条目 ⇒ 机制无上位要点（标准 01 §3）。
  5. :19/:23「不进 Phase2 science weight」「omp 为内部通道，生产调度走 Runtime lease」——上位 §9 :673「线程池的唯一来源是调度器」、§8.4 :658「科学模块…不建无预算线程池」：本份承认模块内 OpenMP 为现状（:48「单 run 内 OpenMP 行条带并行」）⇒ 与上位口径不符且未标为偏差（同 gaia 第 2 条形态）。
- **处置建议**：**合并 + 大幅订正** —— ①:77-85 V19R3 节整节移出正式文档层，实验数值改引 `artifacts/evidence/` 路径（标准 01 §2）；②:56-59 NaN 处置**按 `docs/science/DRIZZLE.md:144` 订正**并把科学锚改指该条所在节（§1 冲突处理）；③:17 归一公式删式留指针，分母符号与 DATA-002 :145-149 的 `D_p` 统一；④内容并入 `docs/plugins/algorithms_phase1/08_drizzle.md`（MODULE_MAP 认定正本），本份页面撤销；⑤:23/:98 裸 `CMakeLists.txt:行` 补全路径或删除（:42 的自认漂移即为证据）；⑥:8 让权块改正向指针链；⑦:37 的 512/9 补来源；⑧:49-51 补本模块冻结容差档并指回 `docs/contracts/TEST_MATRIX.md` §2 或科学页三档。

---

### 22. docs/modules/hips_p2.md

- **路径**：`docs/modules/hips_p2.md`
- **标题**：astrocs.p2.hips_writer — Phase2 HiPS 马赛克写出模块（P2-HIPS）
- **行数**：70
- **角色**：模块工作细节（角色对），但同页混入**登记面职责**（:16-29「身份与合同」段）⇒ 一份文档两角色；且与别批 registry 页构成同一模块双页。
- **主题**：Phase2 HiPS 马赛克写出（输入哈希链、DISCOVER→…→HIPS_VERIFY 生命周期、逐 tile 排异+积分+逆归一、FITS→NESTED 序转换、writer 库消费）的身份、职责、合同事实与验证面。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位；实质追溯链在 :24-29（SCI-UPM-001 + SCI-INT-001 + SCI-REJ-001 → ALG-P2-HIPS-001..004 → DATA-P2-HIPS / API-P2-HIPS-001 → TEST-P2-HIPS-001），链条完整（正面项）。
- **下游**：**实质文档层引用者 1**：`docs/algorithms/PHASE3_PROJ_IMPL.md:638`「| C2 | `astrocs.phase2.write.md:41` / **`docs/modules/hips_p2.md:39`** | writer 视图中间量 `flux` 与产品语义混淆 | ✅ 已补… |」。命令：`grep -rn 'hips_p2\.md' docs lib eng` → 仅该行 + `docs/DOCUMENT_INDEX.yaml:192/:792`。⇒ 非孤儿，但**唯一实质消费者用的是 `:39` 行锚，而该 ⚠ 订正段实际在 :42-43**（行锚已漂 3 行）。
- **是否正本**：**副本** —— 同一 `module_id: astrocs.p2.hips_writer` 的登记面正本在别批 `docs/modules/registry/astrocs.phase2.write.md:1-4`（实读：「--- id: **MOD-astrocs-phase2-write** / version: 1.0.0 / status: ACTIVE / owner: **SA-P2-I23**」）。本份 :18-19 自己承认「MOD ID：`MOD-astrocs-phase2-hips-writer`（registry 行 ID **沿用** `MOD-astrocs-phase2-write`）」= **两套 MOD ID 并存**。
- **重复或重叠对象**：
  - 与 `docs/modules/registry/astrocs.phase2.write.md`（别批，为判重复而读）重叠 = **同一模块的身份/owner/状态**。差异 = registry 是 YAML front-matter 登记表、本份是散文页，两者**各写一遍 owner 与 MOD ID**，且状态陈述互斥：本份 :21「dll_target：`astrocs_p2_hips_writer.dll`（**合同值，尚未存在**）」+ :65「可执行 `TEST-P2-HIPS-001` **MISSING**…登记面=设计冻结 **VERIFIED**」←→ registry :4「`status: ACTIVE`」⇒ 同一模块**三套状态陈述**（本批最重）。MODULE_MAP 亦无 `hips_p2` 条目，:313 `coverage.legacy_paths: [lib/phase2, lib/hips_p2]` 把它当**旧路径**。
  - 与 `docs/algorithms/PHASE2_MOSAIC_WRITE.md`（别批）重叠 = ALG-P2-HIPS-001..004 合同链 ⇒ **合法下钻**（模块页引算法页），不判重复。
- **元信息块**：**有**（违 §4，本批最长）——:5-14 十行「> 合同三件套落位 `lib/algorithms/coverage/hips_p2/`（**README r1** + module.yaml + memory.md，**CONTRACT_READY，entrypoint=MISSING**）——迁移目标目录按 `lib/algorithms/drizzle/hips/`（P1-HIPS）**先例新建**；`lib/algorithms/coverage/` 三件套**已被 P2-COV（astrocs.p2.coverage）占用，不可覆盖**。> 唯一生产源 `lib/algorithms/coverage/tools/stage2.cpp`（**1762 行**…）」含版本锚、状态词、工包号、施工冲突说明、文件行数。**机械列 `meta_block=False` ⇒ 漏报。**
- **写法违规**：
  - :1 标题含工包代号 `P2-HIPS`；:21「迁移归 **P2-HIPS-IMPL**」、:29「可执行测试 MISSING 归 **P2-HIPS-TEST**」、:61「整改归 **P2-HIPS-IMPL/INT**」、:65「（**P2-HIPS-TEST 建立**）」——工包号 4 处 + 「建立/整改归」；
  - :6/:21/:28-29/:65 状态词预写（CONTRACT_READY / MISSING / **VERIFIED**）——**同一文档内自相矛盾**：:28-29 与 :65 两次写「设计冻结 VERIFIED」＋「可执行测试 MISSING」。上位 §12.5 :802 原文「READY_FOR_OWNER_REVIEW…**不等于 VERIFIED**，也不等于发布」+「状态由检查与验收现场计算，**登记表不预写状态**」⇒ 机械列对该类完全无覆盖（`unregistered_index` 只判是否登记）；
  - :8「已被 P2-COV…**占用，不可覆盖**」——施工冲突记录（过程台账）；
  - :9「（**1762 行**，astrocs-stage2 工具，`lib/algorithms/coverage/CMakeLists.txt:103-110`）」——行数 + 行号锚（本仓行锚不可靠有旁证：PHASE3_PROJ_IMPL.md:636「行锚…**已漂移**」+ 本份自身 :39/实际 :42-43 的漂 3 行）；
  - :22「**owner SA-P2-I23**；depends_on_int=P2-INT-INT;IO-003；legacy_paths="lib/algorithms/coverage write sources;lib/infrastructure/aio/healpix_db"」——登记表字段串进散文；
  - :61「——**登记不改码**」——工包决议语。
- **悬空引用**：
  - :28「**MODULE_MIGRATION_MATRIX** P2-HIPS 行」—— **跟踪集内无此文件**：`find . -iname '*MODULE_MIGRATION*'` → 0；`git ls-files -- '*MODULE_MAP*'` → 1（`docs/modules/MODULE_MAP.yaml`）。⇒ 本份引用了一份不存在的映射表；其想引的对象应为 MODULE_MAP，但 **MODULE_MAP 里没有 `hips_p2` 条目**（23 个 id 已逐个读完），所以即便改名也引不到 ⇒ 双重断链。
  - :21「`astrocs_p2_hips_writer.dll`（合同值，**尚未存在**）」——正式模块页把不存在的构建产物登记为 `dll_target`；上位 §8.4 :657 要求「每个算法模块是独立 DLL/SO」。
  - :6-14 的 `lib/algorithms/coverage/hips_p2/`（README+module.yaml+memory.md）、`lib/algorithms/drizzle/hips/`、`lib/algorithms/coverage/tools/stage2.cpp`、`lib/algorithms/coverage/include/astro/phase2/stage2_common.h`、`lib/infrastructure/aio/src/hips/aio_hips_writer.cpp` —— 实测**前四项实存**（`ls` 已核），第五项见 `docs/modules/astro_image_io.md` 与别批 HIPS_WRITER 的引用一致 ⇒ 本份路径引用总体可核，仅 :21/:28 两处指向不存在对象。
- **与上位冲突**：
  1. :28/:65 预写 `VERIFIED` ←→ 上位 §12.5 :802（`VERIFIED` = 正式平台 + 真实数据验收通过，属验收结论级）⇒ **一份模块页写死了只有验收才能得出的状态**，本批最重的状态违规。
  2. :43「⚠ **该 `flux` 是 writer 视图的中间量**…**落盘值 = `flux_sum / covered_area` = 面亮度**（写端口 `UnitId::SURFACE_BRIGHTNESS`；**产品语义 = 面亮度**）」——单位/量纲语义写在模块页，而本份 :26 自己写了「DATA-P2-HIPS（**DATA_SEMANTICS §20**）/ API-P2-HIPS-001（**PUBLIC_API Phase2 mosaic write 节**）」⇒ **同一单位口径三处承载**（模块页 + DATA_SEMANTICS §20 + PUBLIC_API），上位 §0.1 :44 规定单位/科学口径权威在 science/contracts ⇒ 层级倒置 + §2 同主题单正本违例。（此条已被别批 `docs/algorithms/PHASE3_PROJ_IMPL.md:638` 当作 C2 订正项处理，说明矛盾已实害到第三方文档。）
  3. :5「合同三件套落位 `lib/algorithms/coverage/hips_p2/`」与上位 §8.4 :624 算法并联清单不符（`coverage` 是其一行，无 `hips_p2` 子域）；MODULE_MAP :290-292 的 `coverage` 条目 `target_dir: lib/algorithms/coverage` 亦不含该子域三件套 ⇒ **模块目录结构与上位树不一致且无上位要点**（上位 §0.2 :52）。
  4. :46「非职责：…原子发布（**IO-003** 编排层）」与本批 IO_003/DATA-003 的双正本（组 G3）相扣：本份把发布让给 IO-003，而 IO-003 又与 DATA-003 冲突 ⇒ 冲突会经此指针传导，整治顺序须先解 G3。
- **处置建议**：**拆分 + 合并** —— ①:16-29 身份/合同 ID 段删除，交回 registry 页与 `docs/contracts/INDEX.yaml`（标准 01 §2 登记表承载映射）；②:43 面亮度/`flux` 口径**合并入 `docs/contracts/DATA_SEMANTICS.md` §20**（同主题单正本），本份留正向指针；③:5-14 元信息块删状态词、r1、施工冲突、1762 行与行号锚；④:21 若 target 确不存在，改「规划目标，未安装」并去 :28/:65 的 `VERIFIED`（上位 §12.5）；⑤:28 `MODULE_MIGRATION_MATRIX` 改指 `docs/modules/MODULE_MAP.yaml`——但**须同时在 MODULE_MAP 补 `hips_p2`/`p2_hips_writer` 条目**，否则改完仍悬空（标准 01 §3）。本份**不判删除**：registry 页无它承载的模块语义（哈希链、生命周期、四概念分离）。

---

### 23. docs/modules/orchestrator.md

- **路径**：`docs/modules/orchestrator.md`
- **标题**：Module: orchestrator
- **行数**：89
- **角色**：模块工作细节（角色对），但 :58-82「## 归属与构建（ORCH-001 落位）」使同页兼**归属争议裁决记录**（另一角色）。且 **MODULE_MAP 的 23 个 id 里没有 `orchestrator`**——:578 `id: scheduler` 的 note（:606-614）明写该条目「覆盖…scheduler…**与 pipeline**（typed DAG、artifact、内存/数据管线；物理位 `lib/infrastructure/pipeline`）」⇒ 本份所描述的对象在机器登记面里是 `scheduler`/`pipeline`，本份却以独立模块自居。
- **主题**：Phase1 编排（stage1.json 驱动 READ→…→HIPS_WRITE、DllLoader 动态加载）摘要 + 其「位置与职责分离」归属主张 + 构建/target 事实。
- **上游**：已标注 :3「§8.4（模块与 ABI）」——号名错位；:60/:67/:80 另三处引上位，其中两条为**伪造或错号**（见下）。
- **下游**：4 个文件（`grep -rl 'docs/modules/orchestrator' docs lib eng`），含 `lib/infrastructure/pipeline/PENDING.md`（别批）。⇒ 非孤儿但面薄，且下游是一份 PENDING 登记。
- **是否正本**：**待定** —— 既非 plugin 规范页（MODULE_MAP 指 `docs/plugins/infrastructure/19_runtime.md`），其独有内容（ORCH-001 归属主张）与 MODULE_MAP `scheduler` 条目**互斥**，两者不能同为正本。
- **重复或重叠对象**：与 `docs/modules/MODULE_MAP.yaml:578-614`（同批第 13 份）重叠 = **orchestrator/pipeline 的归属与物理位**。贴两处：
  - 本文 :67-69「`ASTROCS_DESIGN.md` §8.4 顶层结构里的 pipeline 位（typed DAG、块生命周期、内存/数据管线）在本仓的代码实体是 `lib/infrastructure/scheduler/src/{pipeline,artifact,artifact_store}.cpp` 与 **`lib/infrastructure/runtime/**`（MODULE_MAP id=`runtime`）**，**不是**本模块」
  - `docs/modules/MODULE_MAP.yaml:607-609`「本条目对齐 ASTROCS_DESIGN §7.1：覆盖 §7.1 顶层结构的 scheduler（注册、资源预算、执行、取消、checkpoint；物理位 lib/infrastructure/scheduler）与 **pipeline**（typed DAG、artifact、内存/数据管线；**物理位 lib/infrastructure/pipeline**）」
  - 差异 = **互斥结论**：本份说 pipeline 的代码实体在 `scheduler/src/` + `lib/infrastructure/runtime/`，MODULE_MAP 说物理位是 `lib/infrastructure/pipeline`；且本份所引「MODULE_MAP id=`runtime`」**在该文件里不存在**（`grep -n 'id: runtime' docs/modules/MODULE_MAP.yaml` → 无输出；`git ls-files -- 'lib/infrastructure/runtime*'` → **0 个跟踪文件**）。⇒ 同主题双正本，且一方引用另一方的不存在的条目。
- **元信息块**：:3 上游标注合规、无 ID/状态块；但 :58 节标题「归属与构建（**ORCH-001** 落位）」与 :73「（登记 **ORCH-HOME-01**，**待收敛**）」在性质上等同元信息块（过程/裁决声明）。机械列 `meta_block=False`、`task_ids=[]` ⇒ **漏报**。
- **写法违规**：
  - :58-75 整段归属争议记录：:73「⇒ **位置与职责分离（登记 ORCH-HOME-01，待收敛）**：位置搬迁 = `git mv` + 引用/锚同步。因此：:74「任何按目录推断归属的判据（**检查器**/清单/文档）**一律按登记职责归位**」——文档层规定检查器如何归位（上位 §0.2 :53 与 MODULE_MAP :14-16 都定「状态由检查器现场计算」）= **下位改写上位定的判据行为**；含 `git mv` 操作指令（工包面）；
  - :44「spill/checkpoint（**V13+**）」——版本史；:48「**已知 bug**：`cpp/` 下嵌套 logs 目录（非阻断）」——缺陷台账；
  - :70-72「ARCH-001 迁移清单 #15…**登记的 DONE 位，该条依据写的是**「7.1 infrastructure/pipeline…」」——转述登记史并指出其依据写错（「旧版/留痕」叙事，§4）；
  - :89「…全集合一致（`eng/tools/docs_machine_consistency.py` 校验）**[B4-24]**」——审计条目号；
  - :16「`orchestrator.exe <stage1.json>`」、:36「stage1 JSON schema（configs/stage1.schema.json）」——以现行口吻写非产品入口（与 :79-80 自述冲突，见冲突第 5 条）。
- **悬空引用**（**本批性质最重：伪造上位逐字引语**）：
  - :60「**`ASTROCS_DESIGN.md:373` 逐字**「scheduler/ 注册、资源预算、执行、取消、checkpoint」，与本模块职责逐条对应」—— **最高设计不含此文字**。三重复核：
    `sed -n '371,375p' ASTROCS_DESIGN.md` → :371 是 `### 5.2 固定科学流程`、:373 起是 mermaid（`flowchart TD` / `A["admit 兼容性校验"]`）；
    `git grep -c '注册、资源预算' HEAD -- ASTROCS_DESIGN.md` → **exit 1（0 命中）**（**正对照同形命令** `git grep -c '三个阶段调度器' HEAD -- ASTROCS_DESIGN.md` → 命中 2）；
    上位对 scheduler 的真实表述是 §8.3 :599「调度器是阶段内的唯一执行者：按 DAG 调度模块、管理块生命周期、分配线程预算、处理异步预取、响应取消」与 §8.4 :630「`scheduler/` 三个阶段调度器（异步编排 / 窗口并行 / 子块流式）」——**没有「注册」，也没有「checkpoint」**。⇒ 以「逐字」形式引用了一份不含该文字的权威文档，并据它以「逐条对应」立论。**这是本批最严重缺陷**（同类第二处见 astro_image_io :78）。
  - :69「**`lib/infrastructure/runtime/**`（MODULE_MAP id=`runtime`）**」—— **两者皆不存在**：`git ls-files -- 'lib/infrastructure/runtime*'` → 0；`grep -n 'id: runtime' docs/modules/MODULE_MAP.yaml` → 无输出。⇒ 虚构目录 + 虚构登记条目，一条不剩。
  - :80「`ASTROCS_DESIGN §6.2` 的唯一命令树仍是产品 `acsd`」—— 条款号错（唯一命令树 = §7.1；§6.2 是 export 流程）。**本批第 3 次复现**（另见 PHASE1_API_V1 :12、MODULE_MAP :577）。
  - :68「`lib/infrastructure/scheduler/src/{pipeline,artifact,artifact_store}.cpp`」—— **实存**（`ls lib/infrastructure/scheduler/src/` → `pipeline.cpp`、`artifact.cpp`、`artifact_store.cpp` 三项俱在）。⇒ **机械列 `dangling_links` 把本条判为断链是误报**（工具把花括号简写当字面路径）。
  - :86「`…/cpp/Makefile:CXXFLAGS`」—— 以**变量名**代行号，机器不可核。
  - :88-89 `docs/standards/C_ABI_STANDARD.md`、`docs/architecture/ERROR_MODEL.md`、`eng/tools/docs_machine_consistency.py` —— 实存（`ls` 已核）。
- **与上位冲突**：
  1. :60 伪造上位逐字条款（见悬空引用）→ 违上位 §0.1 :44-45「每一层只由它的上一层推出；下级文档陈述与本设计一致的细化内容，**不另立权威链**」+ 标准 01 §1。
  2. :69 虚构 `lib/infrastructure/runtime/` 与 `MODULE_MAP id=runtime` → 上位 §8.4 :620-639 顶层结构树的 infrastructure 子项只有 cli/pipeline/scheduler/aio/benchmark/observability/gaia_xpsd_client/acr/hips_browser，**无 runtime**。
  3. :74「任何按目录推断归属的判据（**检查器**/清单/文档）一律按登记职责归位」←→ 上位 §0.2 :53「登记表与映射表承载定义与映射，**状态由检查器现场计算**」⇒ 层级倒置（下位命令上位机制改行为）。
  4. :20「运行产物 `run/`」、:48「阶段日志 `run/logs/orchestrator/`」←→ 上位 §10 :696「产品与**运行日志只落块级 `output_dir`**（日志目录 `<output_dir>/logs`，§7.3）；`run/` 只放开发/CI 过程产物与过程日志，**与运行日志不互替**」⇒ **直接冲突**（与第 15 份 astro_image_io :67 同型，两处一致地错；整治须一并改）。
  5. :7/:16/:36 的 `stage1.json` + `orchestrator.exe` + `configs/stage1.schema.json` ←→ 上位 §7.1 :466-490（唯一命令树 = `normalize|mosaic|export --json`，`--template` 由同一份键表生成）、§3.2 三类配置、§4.3 输入合同。本份 :79-80 已自述「入口可执行 `orchestrator_legacy_cli` 为**非产品**（不进 install 白名单、不进产品 manifest）」⇒ :16/:36 与 :79-80 在同一文档内互相打脸，前者未标非产品，违标准 01 §4「只写现行设计要怎样」。
- **处置建议**：**拆分 + 上呈负责人** —— ①:58-82 归属争议段整段移 `artifacts/evidence/`（标准 01 §2「需要留证的台账落 `artifacts/evidence/` 并由正本引用」），其结论**依 AGENTS.md §10 上呈**（`pipeline/` 与 `scheduler/` 顶层归属、且与本批第 13 份 MODULE_MAP 互斥，非文档整理可裁）；②:60 的伪造上位「逐字」引语**立即删除并改为引用 §8.3 :599 的实际文字**（这不是风格问题：它以引号形式向上位权威挂了不存在的条款）；③:69 的 `lib/infrastructure/runtime/` 与 `MODULE_MAP id=runtime` 两处虚构删除；④:20/:48 落点按上位 §10 :696 订正为 `<output_dir>/logs`；⑤:16/:36 与 :79-80 打通，非产品入口统一标 `orchestrator_legacy_cli`（非产品）；⑥:80 的「§6.2」订正为「§7.1」。本份**不判删除**：它含 `astrocs_infra_orchestrator`/`astrocs_orchestrator_jsv`/6 条共址 ctest 等别处无载的构建事实。

<!-- PROGRESS: 已审 23/23 -->

## 2. 同主题组与差异

各份的段落证据见 §1 各条「重复或重叠对象」栏；此处只归纳组结构。**「下钻分层不算重复」已逐条剔除**（如 hips_p2 ↔ PHASE2_MOSAIC_WRITE.md、DATA-002 ↔ `HIPS_STORAGE_FORM_CONTRACT.md`（其 :141 显式让位）均判合法分层，不入组）。

| # | 主题组 | 各份路径（批内/别批） | 正本候选 | 主要差异 | 合并建议 |
|---|---|---|---|---|---|
| G1 | CLI 命令面（含 `verify`） | `docs/api/CLI_PROTOCOL_V1.md`、`docs/api/MANIFEST_VERIFY_V1.md`、上位 §7.1-7.3 | 上位 §7.1-7.3 | CLI_PROTOCOL :8-9 判 `verify*` rc=2；MANIFEST_VERIFY :70/:74 把 `acsd verify`/`show-effective` 标 FROZEN；`ASTROCS_DESIGN.md` 全文 **0 次**出现 verify（`git grep -c -i verify HEAD -- ASTROCS_DESIGN.md` → 0，正对照 `normalize` → 15） | 命令面留上位；两份迁 `docs/interfaces/cli/`；MANIFEST §3/§4 删或先补上位要点 |
| G2 | run manifest / provenance 字段语义 | `docs/api/MANIFEST_VERIFY_V1.md` §2/§2.1、`docs/interfaces/data/DATA-004_*.md`、`docs/VERSIONING.md` §2.1 | DATA-004（溯源）+ VERSIONING（版本） | 三处各写一遍 `source_sha`/构建指纹；字段名两处不同（`source_commit` vs `source_sha`） | MANIFEST §2.1 并入 DATA-004；版本口径回 VERSIONING |
| G3 | 原子发布序列与 run 目录布局 | `DATA-003_PRODUCTION_ARTIFACT_STORE.md`、`IO_003_ATOMIC_OUTPUT_PUBLISH.md`、上位 §10 :688 | DATA-003 | 两套已发布布局（`objects/`+`manifests/` vs `products/{user_path}/`）+ 两套 rename 次序 + 两个 Python 发布器（均实存） | IO_003 只留 HiPS 特化，序列并入 DATA-003 |
| G4 | Phase1 底层 C API 名册 | `docs/api/PHASE1_API_V1.md` §2、`docs/contracts/PUBLIC_API.md`（别批）、`docs/algorithms/CALIBRATION_ALGORITHMS.md` §10（别批） | `docs/contracts/PUBLIC_API.md` | 取消点两处相反：PHASE1 写「行带」；PUBLIC_API:164 与 CALIBRATION_ALGORITHMS:517 写「模块内无取消检查点」 | PHASE1 §2 并入 PUBLIC_API；取消口径按 DISP-CAL-008/DISP-PHOT-004 定档 |
| G5 | Phase3 输入拒绝面（variance/ivar） | `docs/api/PHASE3_API_V1.md` §4、`docs/science/PHASE3_HIPS_TO_FITS.md` §1/§9a-10（别批）、`DATA-002_*.md` §7 | 科学正本 `PHASE3_HIPS_TO_FITS.md` | PHASE3_API 拒收 variance/ivar；科学页 :10 明文豁免并要求显式消费；DATA-002 :303-305 与科学页一致 | PHASE3_API §4 按科学页订正后降为指针（`docs/KNOWN_LIMITATIONS.md:102` 已在册） |
| G6 | NaN 样本级掩膜处置 | `DATA-002_*.md` §2a、`docs/science/DRIZZLE.md:144`（别批）、`docs/standards/NUMERIC_STANDARD.md`、`docs/standards/STANDARDS_REGISTRY.md`（别批）、`docs/modules/healpix_drizzle.md` :56 | 科学面 `docs/science/DRIZZLE.md`（其 :144 又指 DATA-002 §2a 为「唯一口径文字」⇒ 现状正本在 interfaces 层，**本身即层级倒置**） | 上位 §5.5 :426 把规则下沉 `docs/science/`；实际四处承载；模块页口径与科学页**相反**（不掩膜 vs 掩膜） | 口径迁 `docs/science/`；DATA-002 §2a 留 `rule_id` 指针；healpix_drizzle 按科学页订正 |
| G7 | HiPS tile 地址词法（Dir/Npix） | `IO_002_HIPS_INPUT_INTERFACE.md` §3.3、`docs/contracts/DATA_SEMANTICS.md:471`（别批） | DATA_SEMANTICS（语义）+ IO-002（读端校验） | 同一 IVOA 原式 + 同一条 `M2b-B-01` 回退叙述在两处各写一遍 | 布局留 IO-002，语义留 DATA_SEMANTICS；两处历史回退叙述都删 |
| G8 | FITS 读写接口（两套并存） | `IO_001_FITS_STREAM_INTERFACE.md` + `lib/infrastructure/aio/io/`、既有 `lib/infrastructure/aio`（aio_fits/aio_hips_*） | 上位 §10 :686 只承认 `aio` 一个 I/O 边界 | IO-001 自称「独立实现、不链接 CFITSIO」同时声明 aio「保持原样」；IO-002 :44、IO-003 :33 同型 | **上呈负责人**：唯一 I/O 边界归属（顶层合同级） |
| G9 | 模块工作细节双目录 | 本批 11 份 `docs/modules/*.md` ↔ 别批 `docs/plugins/**` 23 篇；机器面 `MODULE_MAP.yaml` | `docs/plugins/**`（MODULE_MAP 认定） | `grep -c 'docs/modules/' MODULE_MAP` → **0**；`grep -c 'docs/plugins/'` → **56** ⇒ 双目录**零交叉链接**；9 份 modules 页用退役目录名 | 逐组并入 plugins 正本后撤 modules 页（见 §1 各处置） |
| G10 | 同一模块两页 | `hips_p2.md`（本批）↔ `registry/astrocs.phase2.write.md`（别批）；`dynamic_psf.md`（本批）↔ `registry/astrocs.phase1.star-psf.md`（别批） | registry 页（front-matter 登记面） | 两套 MOD ID + 三种状态陈述 | modules 页并入 plugins 或改为单向指回 registry |
| G11 | orchestrator / pipeline 归属 | `docs/modules/orchestrator.md` :58-82 ↔ `docs/modules/MODULE_MAP.yaml` :578-614 | **需裁决**（两份结论互斥） | pipeline 代码实体位置相反；本份另引不存在的 `id=runtime` | **上呈负责人**（AGENTS.md §10）；裁决前本份不得作登记依据 |
| G12 | `docs/api/` vs `docs/interfaces/` | 本批 6 + 6 | `docs/interfaces/`（标准 01 §2 明列；`docs/api/` **未列**） | 二者**主题面基本不交**（见 §5 前提 2）；缺陷是目录无角色格 | 6 份全迁 `docs/interfaces/`；命名法统一（`DATA-00N` vs `IO_00N`） |
| G13 | 文档自称"以代码为准" | `calibration.md`:6、`dynamic_psf.md`:11、`gaia_xpsd_client.md`:6、`healpix_drizzle.md`:8、`hips_p2.md`:31、`astro_image_io.md`:24、`orchestrator.md`:74（**7/11 份模块页**） | 上位 §0.1 :42「代码：最低权威」 | 六份让权给代码 README/源文件、一份让权给"登记职责" | **整族清除**：改为「实现家 = <路径>」的正向指针，权威回到文档层（本组是 docs/modules/ 的系统性缺陷，非个案） |
| G14 | 上位条款号/引语错误（跨批系统族） | 号名错位 §8.4↔§8.5：**15 份**（4 份 api + 11 份 modules）；§6.2↔§7.1：3 份（PHASE1_API、MODULE_MAP :577、orchestrator :80）；§9↔§10：4 份（MANIFEST_VERIFY :37、DATA-002 :3/:277、DATA-003 :11/:18、DATA-004 :3/:137 + MODULE_MAP :732）；§7.3「DLL C ABI 边界」：3 份（IO_001/002/003）；不存在条款 A.3/A.4/E/F.4：2 份（IO_002、IO_003）；伪造逐字引语：2 份（orchestrator :60、astro_image_io :78） | 上位各节实际标题 | 同一对条款号在**同一文档内被反向误用**（MODULE_MAP :577 vs :607） | 整治须**按族批量修**，不能逐份改；建议给 `docs/` 加一条"上位条款号↔标题"机器一致性判据（属标准与检查角色，别批） |

## 3. 索引与链接缺陷清单

### A. 不可解析的合同 ID 族（本批最大一类结构性缺陷）

HEAD 跟踪集里合同类文档只有 `docs/contracts/{ARCH-001,API-001,RT-001}.md` + `docs/interfaces/data/DATA-00[234]_*.md`。但本批 12 份以「上游/下游/冻结依据」形式引用一整族**无文件、无登记、无映射**的 ID：`API-001..005`、`ARCH-002..005`、`CLI-001..004`、`ABI-001`、`BENCH-001..005`、`CON-006/008`、`RT-002/006/007`、`LOG-001/002`、**`DATA-001`**、`INT-001`、`CODE-P*`、`TST-P*`、`SYN-007`。

举证（**以跟踪集为准，不用磁盘存在性**；含正对照）：
```
git ls-files -- 'docs/contracts/ARCH-*' 'docs/contracts/API-*' 'docs/contracts/RT-*' 'docs/contracts/DATA-001*'
  → docs/contracts/ARCH-001.md / API-001.md / RT-001.md      （仅此三；ARCH-002..005、API-002..005、
                                                              RT-002..009、DATA-001 全无）
git ls-files -- 'eng/contracts/schemas/phase3_request*'      → 空
git ls-files -- 'lib/infrastructure/aio/io/include/astrocs/io/*'
  → fits_stream_v1.h / hips_input_v1.h                        （io_module_api_v1.h 不在跟踪集）
git ls-files -- 'lib/infrastructure/runtime*'                 → 0 个文件
find . -iname '*MODULE_MIGRATION*'                            → 0（正对照 git ls-files -- '*MODULE_MAP*' → 1）
grep -n 'id: runtime' docs/modules/MODULE_MAP.yaml            → 无输出（23 个 id 逐个读毕）
grep -c 'DATA-001' eng/ci/id_migration_map.json                → 0
grep -c 'CHK-BUILD-PROVENANCE' eng/ci/id_migration_map.json    → 8      （证明该文件确有内容）
grep -rl 'DATA-001' docs eng lib | wc -l                       → 58
```

**`DATA-001` 专项（本批最要害）**：它的"正本"在两处各自成立又互相错位——
- `eng/contracts/data/artifact_manifest.schema.json:4` `"title": "ACSD Artifact Manifest v1 (DATA-001 冻结)"`（机器 schema 借用了这个 ID）；
- 它**原本是一份控制包任务书**：`工程控制/PROJECT-GOVERNANCE-01/tasks/DATA-001.md`（首行「# 任务：DATA-001 建立统一数据对象合同链」，第二行「状态：`NOT_STARTED`」），**已于 `ae0f1e63` 删除**（`git log --diff-filter=D` 实证；`git merge-base --is-ancestor ae0f1e63 HEAD` → YES）。

⇒ 本批 `DATA-002/003/004` 三份合同反复引用的「DATA-001 冻结语义」（DATA-002 12 处、DATA-003 8 处、DATA-004 6 处）在 HEAD 上**既无文档、也无任务书**，其原始任务书状态是 `NOT_STARTED` 且已随 GOV 包归档删除。标准 01 §3「每份下层文档能追溯到最高设计的一条要点；追溯不到的机制先补要点再保留」在 DATA-001 上整条不成立 ⇒ **整治第一步应是补落 `docs/contracts/DATA-001_ARTIFACT_MANIFEST.md`（或把 ID 重定向到 `artifact_manifest.schema.json` 并在 DOCUMENT_INDEX 登记该映射）**，否则 DATA-002/003/004 的"冻结"二字无源。

### B. 两套并行 ID 命名族，互不映射

`DATA-00N`/`IO-00N`/`DOC-IO-INTERFACE-00N`（本批 interfaces 六份自封）与 `docs/contracts/INDEX.yaml` 的 `API-WCS-001`/`ALG-PHOT-001`/`DATA-P1-WCS` 是两套；`grep -n 'doc_id' docs/DOCUMENT_INDEX.yaml` → **0**（索引只登记路径，不登记 doc_id）⇒ 按 ID 的引用一律不可机器核对。命名法也不自洽：`docs/interfaces/io/` 三文件名用下划线 `IO_00N_*`，正文与 ID 引用写连字符 `IO-00N`（IO_001 :196、IO_003 :8-10）；`DATA-00N_*.md` 则文件名与正文一致（连字符）⇒ **同一目录内两套分隔符**。

### C. DOCUMENT_INDEX 登记核对（标准 01 §3）

- **登记覆盖**：23 份**全部已登记**（`docs/DOCUMENT_INDEX.yaml:290-802` 区段），机械列 `unregistered_index` 对 23 份全报 `False` ⇒ **该列本批无漏报**。
- **缺陷 1（角色列结构性缺失）**：索引对我批条目的键是 `path/status/duty/upstream/downstream/notes`（见 :746-802），**无 `role:` 键** ⇒ 标准 01 §3 要求的「登记内容含路径、**角色**、主题、上游、下游」在索引里缺一整列，本批 23 份的角色全部由本次读文取得，索引无法自证。
- **缺陷 2（上游错误被索引固化）**：`:749/755/760/765/770/775/780/785/790/795/801` 给 11 份 `docs/modules/*.md` 的 `upstream` 一律写 `"ASTROCS_DESIGN.md §8.4（模块与 ABI）"` ⇒ 号名错位被钉进索引。**订正须文档与索引同批**（组 G14），否则改一侧即产生新的不一致。
- **缺陷 3（duty 非主题而是硬截断）**：`:754` acr「异构计算运行时：kernel registry、CPU/GPU 调度、device executor、」以顿号结尾；`:794` hips_p2「输入哈希链：p2_frame_id → canonical manifest（frame_id|filter=;order=」括号未闭合且只描述 :33 的一个职责项、非模块主题（其 :1 标题是「astrocs.p2.hips_writer — Phase2 HiPS 马赛克写出模块」）。⇒ 索引「主题」字段不可用作判定依据。
- **缺陷 4（downstream 带会漂的行锚）**：`:796` hips_p2 → `docs/algorithms/PHASE3_PROJ_IMPL.md`，实测该引用确实存在但写作 `docs/modules/hips_p2.md:39`，而被引内容实际在 :42-43 ⇒ **索引所连的边带行号即失效**。
- **缺陷 5（索引自身违反 §4 写法）**：`:832`（phase2_samp）`notes:` 含「P2-SAMP-DOC (2026-09-09, commit **e654d4c2**) 新建…漏登 DOCUMENT_INDEX（**GOV-002 docs_fully_covered 缺口**），机械修复不改动文件内容」；`:797`（hips_p2）`notes:` 含「P2-HIPS-DOC (2026-09-09) 新建…」⇒ 索引正文有日期 + commit + 任务号 + 流水账。`docs/DOCUMENT_INDEX.yaml` **不在 DB-01**（别批），本条仅作跨批发现登记，并给出对我批的直接约束：**本次整治的文档改动不得写入 notes 类流水账**。
- **缺陷 6（登记了不存在的东西）**：`docs/DOCUMENT_INDEX.yaml:750` MODULE_MAP 的 downstream 列 `eng/contracts/schemas/projection_registry.schema.json` 等 21 处 —— 该 schema 在 :449 被 MODULE_MAP 当作 `projection` 模块的 `schema_links` 正声明源，而 MODULE_MAP :806 的缺口表把同族的 `export_product.schema.json` 登记为不存在 ⇒ 同一族的 schema 文件存在与否需逐个 `git ls-files` 定档（本次未逐个展开，登记为**待核**，不猜）。

### D. 机械线索文件 `doc_mechanical.csv` 的校准结论（23 份逐份复算后）

| 列 | 判定 | 证据 |
|---|---|---|
| `lines` | **准确** | 23/23 与 `wc -l` 一致（§1 各条行数即本次实测；注意源清单 `DB-01.txt` 是 **CRLF**，直接 `while read` 会带 `\r` 导致全部路径失效——本次以 `tr -d '\r'` 修正后才取得可信行数） |
| `h1` | **准确** | 抽查 MODULE_MAP（YAML 注释当标题）、acr/common/orchestrator（`# Module: x`）、hips_p2/gaia（标题含 ID）、IO 三份（连字符标题 vs 下划线文件名）均与实文一致 |
| `meta_block` | **系统性漏报：23 份中 15 份有元信息块，工具 23 份全报 False** | `docs/api/` 6 份（:5「> ID: … 状态: FROZEN …」）+ `docs/interfaces/io/` 3 份（:3-4「doc_id:/doc_status:」）+ `docs/modules/` 6 份（calibration :5-17、dynamic_psf :5-11、gaia :5-7、healpix_drizzle :5-8、hips_p2 :5-14、astro_image_io :24 内嵌让权块）。根因：识别器只认一种版式（关键词集不含 `doc_id:`/`doc_status:`/「以…为准」/「本页为…摘要页」）。**这是本批最严重的工具盲区**（负责人偏好记忆里已记有同类「YAML 头漏报 1→31」） |
| `dates` | **准确，含 1 处误报** | 真命中：CLI_PROTOCOL[134]（负责人裁决 2026-09-23）、DATA-002[168]（已冻结，2026-09-23）、MODULE_MAP[738,775,847,859]（FIX-404 收口 + checked_at）、gaia[86 区段]。**误报：IO_003[135]** —— 那是 manifest JSON 示例里 `"created_utc": "2026-09-02T08:15:00Z"` 的**字段取值**，不是正文日期 |
| `task_ids` | **严重漏报：23 份中只认出 `REL-002` 一个** | 未认出：`Q8`(astro_image_io:24)、`V19R2`/`V19R6R2`/`F-V19R2-IO-001`(:28/:78)、`M2b-B-01`(IO_002:102)、`M2b-B-09`(IO_001:170)、`B4-01`/`B4-22`/`B4-24`(common:17、healpix_drizzle:52、orchestrator:89)、`ENG-C-04`(common:32)、`V13+`(orchestrator:44)、`V18R3`(gaia:61)、`DISP-HIPS-005`(IO_002:293)、`ACR-IVAR-001`(PHASE2_API:43)、`FIX208`(CLI_PROTOCOL:74)、`13 标准`/`14 标准`/`工程控制 16`(DATA-002:220、IO_001:195、healpix_browser_qt:43)、`GAP_AUDIT G09/G3-8/G1-5/G2-6`、`FIX-404`/`BLD-401`/`DOC-403`/`RELEASE-04`/`CLEAN-501`/`INT-001`(MODULE_MAP)、`ORCH-001`/`ORCH-HOME-01`(orchestrator:58/:73)、`P2-HIPS-*`/`CAT-GAIA-*`/`P1-*-IMPL/TEST`(modules 六份)、`SA-P2-I23`(hips_p2:22)、`ARCH-003/004/005`(api 四份) |
| `commit_sha` | **准确** | `c5629be6`(calibration:38) 真命中，本批唯一；`MODULE_MIGRATION`/`id_migration_map` 类无假阳性 |
| `sha256` | **准确** | 23 份全报 `[]`。MANIFEST_VERIFY:45/:48 的 `<40hex>`/`<64hex>` 与 IO_003:130-131 的 `sha256` 字段名是**合同字段定义**，不是文档自述哈希 ⇒ 工具未误判，判定正确 |
| `history_narrative` | **系统性漏报：23 份全报 False/[]，实际至少 30 处** | 「**已于**…修复」「**已闭环**」(astro_image_io:78)、「旧行为现**仅作只读回退**」(IO_002:102)、「**已迁入** target_dir」×19(MODULE_MAP note)、「**已独立冻结**」(calibration:12)、「**上一轮**…计数 72→71→**71→70**」(MODULE_MAP:859)、「**去重**」(common:17)、「**历史层**」(calibration:113)、「计数**新增**」(healpix_drizzle:80)、「registry 行 ID **沿用**」(hips_p2:18)、「**V13+**」「**已知 bug**」(orchestrator:44/:48)、「API-002 **建立**」「现行落地」(CLI_PROTOCOL:90)、「由 p1 budget 注入**取代**…**收编**」(PHASE1_API:33)、「**保持原样**」(IO_001:18、IO_003:33)、「**保留作兼容层**」(IO_001:191)、「**不修改**」(IO_002:44/:275-278)。⇒ 词表缺「已 X / 沿用 / 上一轮 / 新增 / 去重 / 取代 / 收编 / 建立 / 落地 / 保持原样 / 不修改 / 历史」 |
| `dangling_links` | **漏报为主 + 3 处误报** | 正确命中：`io_module_api_v1.h`(IO_001:28，跟踪集内确无，且文档在同一格自陈"该文件不存在")。**误报 3 处**：①DATA-002:26 的 `eng/contracts/data/examples/*.example.json`（glob，5 个实例文件实存）；②PHASE1_API 的「校准→检测/PSF→plate」（阶段序列文本，非链接）；③orchestrator:68 的 `…/{pipeline,artifact,artifact_store}.cpp`（花括号简写，三文件全实存）。**漏报（工具只解析 markdown 链接与 `..` 相对路径，四类全瞎）**：裸文件名(`phase3_request_v1.schema.json`、`production_call_paths_stage1.csv`、`MODULE_MIGRATION_MATRIX`、`STAR_PSF_ALGORITHMS.md`、`sampler.cpp`、`CMakeLists.txt:NNN`)、合同 ID(§A 全族)、**上位不存在的章节号**(§7.3「DLL C ABI 边界」、A.3/A.4/E/F.4)、**伪造逐字引语**(orchestrator:60、astro_image_io:78) |
| `unregistered_index` | **准确但覆盖面窄** | 23 份均已登记；但该列不检查登记**内容**质量 ⇒ C 条六项缺陷它全看不见 |
| `upstream_pointer` | **本批系统性假绿** | 23/23 报 True（形式上确有「> 上游：…」），但：IO 三份指向上位**不存在的章节**；api 四份 + modules 十一份的 §8.4 号名不符；两份伪造上位条款。⇒ **「有上游标注」≠「能沿标注回到对应条款」**（标准 01 §5 的判据在该列上完全读不出来）。建议把该列拆成三判：标注存在 / 条款可解析（号与名对上上位目录）/ 引语逐字相符 |

**工具改造建议（可直接验收）**：①`meta_block` 关键词集补 `doc_id:`、`doc_status:`、`状态:`、`ID:`、「本页为…摘要页」、「以…为准」；②`task_ids` 改为「大写字母+数字连字符串、且不在 `ACS_/DATA_/SCI_/ALG_` 白名单内」的宽口径 + 排除真合同 ID；③`history_narrative` 词表按上表补 13 词；④`dangling_links` 增三类解析：裸文件名（在 `git ls-files` 里查唯一性）、上位章节号（与 `ASTROCS_DESIGN.md` 的 `^#{1,3}` 标题表比对）、花括号/glob 展开（消除 3 处误报）。

### E. `docs/api/` 与 `docs/interfaces/` 并存专项（派单点名核查项）

结论：**不是同一主题的两套正本**。六份 `docs/api/` 承载 CLI/公共 ABI/manifest/三阶段会话 API；六份 `docs/interfaces/` 承载 DATA 交换合同与 IO 接口合同，**主题面基本不交**（唯一交叠是 G4 与别批 `docs/contracts/PUBLIC_API.md` 之间，不属本核查项）。

真实缺陷在**目录层**：`docs/api/` 既不在标准 01 §2 表的任何目标目录格里，也不在上位 §0.2 :49 列举的详细文档层目录清单（`architecture/interfaces/standards/modules/contracts/development/validation` + 等）里；六份文档的角色全是 §2 的「架构设计（接口契约）」，其应属目录正是 `docs/interfaces/` ⇒ **6 份角色错放（目录级）**，处置为整体迁移而非内容改动。次生缺陷：迁移后 `docs/interfaces/` 内会出现 `cli/`、`io/`、`data/` 与三份 `PHASE*_API_*.md` 并列，**命名法须同时统一**（B 条），否则把 `IO_00N` vs `IO-00N` 的不一致带进新层。

### F. 上位条款号/引语缺陷族（整治须按族批处理，逐份改会互相拉齐失败）

详见组 G14。要点：同一对条款号（§8.4↔§8.5、§6.2↔§7.1、§9↔§10、§7.3）在 **15+ 份文档**里被系统性写错，其中 `docs/modules/MODULE_MAP.yaml` 在 :577 与 :607 把同一对号**朝两个相反方向**误用，`docs/DOCUMENT_INDEX.yaml` 又把 §8.4 的错误固化进 11 条 `upstream` 字段 ⇒ 任何逐份修改都会与索引/其它文档重新打架。**建议动作**：先出一份「上位条款号↔标题」对照表（属标准与检查角色），再按表批量替换，最后由 DOCUMENT_INDEX 同步；伪造引语两处（orchestrator:60、astro_image_io:78）单独优先修，因为它们不是号错而是**内容不存在**。

## 4. 覆盖率自报

- **已审 23 / 分配 23**（DB-01 清单全量，逐份通读全文）。
- **未逐行读名单：空。** 最长一份 `docs/modules/MODULE_MAP.yaml`（931 行）分五段读毕（1-120、121-340、341-520、515-725、725-931）；`DATA-002`（311）、`IO_002`（303）、`IO_001`（206）、`IO_003`（186）各一次读全。短文件（acr 53、dynamic_psf 51、healpix_browser_qt 47、PHASE1_API 50）未跳读。
- 派单许可下为判重复/上下游而读的**别批文件**：`ASTROCS_DESIGN.md`（§0/§3/§5.5/§6/§7/§8/§9/§10/§12.5/附录 A-B）、`docs/science/PHASE3_HIPS_TO_FITS.md:10`、`docs/science/DRIZZLE.md:112-120,144`、`docs/contracts/PUBLIC_API.md:1-30,135-185`、`docs/contracts/DATA_SEMANTICS.md:352,439,471,709,1124`、`docs/contracts/ARCH-001.md:1-10`、`docs/contracts/API-001.md:1-12`、`docs/contracts/RT-001.md:5`、`docs/contracts/INDEX.yaml:162-221`、`docs/KNOWN_LIMITATIONS.md:98-107`、`docs/algorithms/CALIBRATION_ALGORITHMS.md:513-520`、`docs/algorithms/PHOTOMETRIC_FIT.md:242-248`、`docs/algorithms/DRIZZLE_GEOMETRY.md:28,330,388`、`docs/algorithms/HIPS_WRITER.md:243,314,380`、`docs/algorithms/NOISE_ESTIMATION.md:74,181,196`、`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:215,267`、`docs/algorithms/COSMETIC_ALGORITHMS.md:230,393,408`、`docs/algorithms/PHASE2_COVERAGE.md:150-255`、`docs/modules/registry/astrocs.phase2.write.md:1-4,38-44`、`docs/modules/registry/astrocs.phase1.star-psf.md:1-4`、`docs/DOCUMENT_INDEX.yaml:290-802`、`CMakeLists.txt:588-601`、`lib/infrastructure/aio/include/aio_pipeline.h:130-330`、`lib/infrastructure/scheduler/src/`、`eng/cmake/ARCH-001-migration-manifest.md:25,70`、`eng/ci/id_migration_map.json`、`docs/ci/01_CHECKS.md:55`、`docs/audit/{doc_classification,inventory}.csv`。**只对清单内 23 份下结论**，别批内容仅作判定证据，未对其提出处置。
- 未复算项（登记为「待核」，未据以判定）：`eng/contracts/schemas/projection_registry.schema.json` 等 MODULE_MAP `schema_links` 的逐个存在性、`docs/traceability/TRACEABILITY_MATRIX.json` 内 `MOD-*` 键、`docs/algorithms/{GAIA_QUERY,HEALPIX_MAPPING,STAR_PSF_ALGORITHMS}.md` 的节内容、`docs/plugins/**` 各篇正文、各模块页 `文件:行` 锚的行位（只读节点不展开代码复核）。

## 5. 我证伪了任务书的哪个前提

派单前言/任务书前提与实测逐条：

1. **证伪：「本批以 `docs/api/`、`docs/interfaces/`、`docs/modules/` 开头部分为主」。** 实为**穷尽**：清单 23 行 = 这三前缀在 HEAD 里的全部（`ls docs/api/` 6 + `ls docs/interfaces/{data,io}/` 6 + `docs/modules/` 顶层 11）；`docs/modules/registry/*` 与 `docs/modules/phase*.md` 均不在清单。⇒ 「为主」低估了范围，本次按穷尽处理。
2. **证伪：「`docs/api/` 与 `docs/interfaces/` 并存本身是可疑点：核二者是不是同一主题的两套正本、谁的角色错放」。** 前半不成立——二者**不是**同一主题两套正本（主题面基本不交，见 §3-E）；后半成立但形式不同——错放在**目录层**（`docs/api/` 无角色格），不在内容层。
3. **证伪：「机械列会低估（相对 `..` 的链接未判）」——归因说错了。** 命令：对 23 份逐个 `grep -c '\.\./'` → 22 份为 0，唯一非零是 `docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md` 的 1 处，而它落在 :75「| 父目录穿越 | `../evil`、`a/../../b` |」——**词法拒绝的反例样本，不是链接**。⇒ 全批**不存在 `..` 相对链接**（工具自述的低估机制在本批无对象可作用）。`dangling_links` 漏报的真实原因是**工具只解析 markdown 链接语法**，而本批断链的主体形态是「裸文件名 / 合同 ID / 上位章节号 / 花括号简写」四类；且工具另有 **3 处误报**（glob、阶段序列文本、花括号简写），不是单纯低估。
4. **未证伪（反而确证）：任务书 §范围「`docs/` 下全部文档…当前约 233 份」。** 固定口径命令：`git ls-files 'docs/*' | grep -c '\.md$'` → **恰好 233**。⇒ 233 这个数在「docs 下跟踪的 .md」口径上准确，我不证伪它。（同口径 `git ls-files 'docs/*' | wc -l` → 258，即另有 25 份非 .md 跟踪件——索引判据、CSV、YAML、py 夹具等，整治时不可只按 .md 口径清点。）
5. **证伪：任务书 §范围「`docs/` 为重点（当前约 233 份、30+ 子目录）」中的「30+ 子目录」。** 命令：`git ls-files 'docs/' | awk -F/ 'NF>2{print $2}' | sort -u | wc -l` → **23**。⇒ 是 23 个子目录，不是 30+。（`ls docs/` 目视会数出 30 项，因为它把 `docs/` 根下的 7 份散文件（DEVELOPER_GUIDE/GLOSSARY/KNOWN_LIMITATIONS/TROUBLESHOOTING/VERSIONING/DOCUMENT_INDEX.yaml/TRACEABILITY.csv）也算进去了 —— 口径混淆的典型形态，与我批 `doc_mechanical.csv` 的假绿同源。）
6. **证伪（方法论级，对任务书 §步骤 2 的登记字段）：「登记『上游（文档自己标明的最高设计条款）』即可完成 §3 双向索引判定」。** 本批 23 份**全部形式上有上游标注**（`upstream_pointer` 23/23 True），但：`docs/interfaces/io/` 三份指向上位**不存在的章节**（§7.3 名不符 + A.3/A.4/E/F.4 无条）；15 份把 §8.5 写成 §8.4；**两份伪造上位条款**——`orchestrator.md:60` 以「`ASTROCS_DESIGN.md:373` **逐字**」形式引用了上位不含的文字（`git grep -c '注册、资源预算' HEAD -- ASTROCS_DESIGN.md` → 0，正对照 `三个阶段调度器` → 2），`astro_image_io.md:78` 引用了「最高设计 §9『**本期例外（如实登记）**』」（`grep -c '例外' ASTROCS_DESIGN.md` → **0**，即上位全文无"例外"二字）。⇒ **AUD-101 步骤 2 的单字段登记会把最严重的一类缺陷读成"合规"**；目标架构须把该字段拆成「标注存在 / 条款可解析（号与名对上上位标题）/ 引语逐字相符」三判（见 §3-D 表末行）。
7. **补充前提修正（任务书 §步骤 5「核对 DOCUMENT_INDEX 与文件树：未登记、登记不存在、角色标注不符」）**：本批该三步的实测结论分别是 **未登记 0 / 登记了但文件不存在 0 / 角色标注不符 23**——第三项不是"标注错了"，而是**索引根本没有角色列**（只有 `duty` 硬截断，见 §3-C 缺陷 1/3）。⇒ 任务书预设的"角色标注不符"判定在当前索引结构下**无法执行**，需先给 DOCUMENT_INDEX 补 `role:` 列（属别批文件，本条作跨批整改前置项登记）。

## 6. 工作树基线（收工时二次采样）

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工基线**逐字一致**（同两条未跟踪项，无新增、无修改、无删除）。本次全程只读：未改/移/删仓库任何文件，未跑构建/ctest/`run_checks.py`/任何 `eng/**` 检查器/端到端，未 `import` 仓库内 Python。为口径统一只读过一次 `git config core.quotePath false`（该键影响 git 对非 ASCII 路径的转义显示，不改仓库内容、不产生工作树变更；设置它是为了让 `git log --name-only` 输出 `工程控制/**` 的中文路径可读，见 §3-A 的 DATA-001 删除历史举证）。**这是一处需向前台报备的状态副作用**：它写在 `.git/config`（不入库、不出现在 `status --porcelain`），故上面两次采样看不到差异。

<!-- PROGRESS: 已审 23/23 — 台账 + 四节 + 基线对照 全部成稿 -->
