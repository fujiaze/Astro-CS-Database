# 通读 CR-09｜lib/algorithms/drizzle 第 1 段（6 文件）

- 批次：CR-09
- 被审基线 HEAD：`c8f64e9a`
- 清单来源：`独立审计/批次清单/CR-09.txt`（6 个文件）
- 派单定位：创新点④"平面→球面面积交叠分配"＋创新点②叠加落地面
- 上游口径（拿代码事实去核，不照抄）：
  - ① 核权重按 drop 分数交叠面积归一、通量守恒与像素收缩因子无关（合同对应条款签署状态仍未落）
  - ② "叶用四角大圆弦多边形表示"是逐片面积误差的地板，文档那条弧弦容差被实测超 2–5 个数量级

## 开工 `git status --porcelain` 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 文件清单与阅读状态

1. `lib/algorithms/drizzle/hips/src/aio_publish.cpp` —— 已读：是（表 12 行）
2. `lib/algorithms/drizzle/healpix_drizzle/astro_sphere_sink.cpp` —— 已读：是（表 26 行，findings CR-09-4..9，其中 CR-09-4 定 S1）
3. `lib/algorithms/drizzle/healpix_drizzle/astro_sphere_sink.h` —— 已读：是（表 12 行，findings CR-09-10..11）
4. `lib/algorithms/drizzle/healpix_drizzle/tests/bench_drizzle.cpp` —— 已读：是（表 14 行，findings CR-09-12..14）
5. `lib/algorithms/drizzle/healpix_drizzle/tests/bench_write.cpp` —— 已读：是（表 13 行，finding CR-09-15）
6. `lib/algorithms/drizzle/healpix_drizzle/tests/candidate_oracle_test.cpp` —— 已读：是（表 14 行，findings CR-09-16..18）

<!-- PROGRESS: 6/6 -->

---

## 文件 2 `lib/algorithms/drizzle/healpix_drizzle/astro_sphere_sink.cpp`（632 行，整读）

性质：Drizzle `TileAccumulator` → AIO HiPS 的**两个直写末端**（`write_hips_direct` :85-292 / `write_hips_phase1` :309-623）。
这里是创新点④产出面与②的落地面：面积/权重/方差归一分母全部以"入参累加量 + 发布因子 k"的形式出现，
本文件**不计算球面交叠面积**（面积在 `spherical_overlap.cpp`，不在本批）。

与两条上游口径的核对结论（先给判定，逐位点见处置表）：
- **口径①（核按 drop 分数交叠面积归一、通量守恒与 pixfrac 无关）＝本文件相符**。
  `k = sb_publish_scale(sumArea, sumNorm) = D_p/N_p`（:188/:537），signal 乘 `k`、variance 乘 `k²`（:193/:547）；
  与 `docs/science/DRIZZLE.md:9`（"保持面亮度 `S_p=Σ_j B_j a_jp/Σ_j a_jp`、严格守恒总通量 `Σ_p F_p=Σ_j x_j`，与 `pixfrac` 无关；核按 drop 面积归一"）、
  `:22`（`N_p = Σ_j w_jp·A_pixel,j` 面亮度归一分母）、`:112-116`（把 `pixfrac²` 当归一因子会把总通量压低 `pixfrac²` 倍，"正确取值为 1"）逐条同向；
  且 :148-159 备有复现该缺陷的可执行负例（`ASTROCS_DRZ_SB_FAULT=legacy_dp_normalization` ⇒ `k≡1`）。
  仍**未落**的是合同签署状态：`module_adapters.cpp:11504` 原文"键名与 Phase1 写入端尚未冻结（报告未闭合项）"——本文件不承担该缺口，代码侧不是错方。
- **口径②（四角大圆弦多边形＝逐片面积误差地板）＝本文件不与其相悖，但两末端对"面积算大了"的处置不一致**：
  `write_hips_phase1` 在 sink 内把 `S=sumArea/A_cell` 钳到 [0,1] 并量化到 u8（:521-526/:542），
  `write_hips_direct` 把未钳未量化的 `sumArea` 原样交给 writer（:191），钳位责任下沉到 writer（`aio_hips_writer.cpp:930 support_clamped_pixels`）。
  即：弦多边形 overshoot 这条地板在生产两档上分别由"sink 内钳＋256 级"和"writer 内钳＋连续值"处理 ⇒ 同一帧在两档下 support/signal/variance 字节不同（CR-09-6）。

### §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :188、:537 | `k = sb_publish_scale(sumArea, sumNorm)` | `D_p/N_p` | 把"分配通量"折回"面亮度"口径的发布因子 | **公式导出**：`k = Σ_j a_jp / Σ_j w_jp·A_pixel,j`；代码真按式算（声明在 astro_sphere_sink.h，唯一源） | DRIZZLE.md:22/:42-65/:127-128（两不变量只在 `N_p` 取此式时同时成立） | — | CONFIRMED |
| :193、:547 | `sumVarNum * k * k` | 幂次 2 | 方差分子与信号共用同一归一分母 ⇒ 同步折算 | **公式导出**：`var = (Σ v_j w_jp²·k²)/A_cov² = Σ v_j w_jp²/N_p²`，与 `drizzle_engine.h:56 variance_p = sumVarNum/sumNorm²` 代数恒等（**仅当 A_cov≡sumArea 精确成立**，见 CR-09-6 的破点） | DRIZZLE.md:81-86（SCI-DRZ-014）；`aio_hips_writer.cpp:1472 variance = var_num_sum/covered_area²` | 若 A_cov 被量化/钳位（:542），恒等式右端偏差见 CR-09-6 | CONFIRMED |
| :331-332 | `a_cell` | `4.0·3.14159265358979323846/(12.0·nside²)` sr | 单个 HEALPix 叶像素面积（support 的分母） | **公式导出**：HEALPix 等角面积 `A = 4π/(12·nside²)`（12·nside² 个等面积像素铺满 4π sr），代码按式算，非写死经验值 | DRIZZLE_GEOMETRY.md:254 区段；`aio_hips_writer.cpp:11 support = covered_area/A_cell` | π 字面量为 double 精度截断（20 位十进制），影响 <1 ulp，不改判 | CONFIRMED |
| :521-526 | `S=sumArea/a_cell`；钳 `[0,1]`；`q=lround(255·S)`；再钳 `[0,255]` | 0 / 1 / 255 | support 面积比的 u8 量化（旧容器口径） | 255 与两次钳位＝**结构性不适用**（uint8 产品位深的量化级数与饱和边界，不表达科学判据）；**0/1 的物理含义（比值域）是导出的**：S 是覆盖比，全域天然 [0,1]，>1 只能来自面积误差 ⇒ 见 CR-09-6 | DATA_SEMANTICS.md:382（support 面语义）；`aio_hips_writer.cpp:2781` 旧形态转换 `covered_area = su/255*A_cell` | 保守方向：量化使 support 与 `signal=flux/A_cov` 的分母同时变小/归零，偏的是**产品数值**不是判定；q=0 时该像素在 variance/ivar 面被判"无覆盖" | CONFIRMED |
| :542 | `(q/255.0)*a_cell` | — | 把 u8 比值反折成 writer 需要的 covered_area（绝对 sr） | **公式导出**（:2781 旧转换的逆运算，代码真按式算） | 同上 | 与 :191 的 raw 面积构成两末端不一致 | CONFIRMED |
| :191 | `dense_area[local] = acc.sumArea` | 原值 | 通用档的 covered_area（不钳不量化） | **公式导出**（A_cov := D_p = Σ_j a_jp，定义即等式） | DRIZZLE.md:22；writer:11 | 与 :542 冲突 ⇒ CR-09-6 | CONFIRMED |
| :434-435 | 组内公共 F_ref 相对容差 | `1e-9` | 判定 `snr_reference.{flux_adu\|flux_common}` 是否组内同值 | **待确认**：本仓无任何文档给该数条文（检索式 `git grep -n "1e-9" -- docs/science docs/algorithms docs/contracts` 未见与 F_ref 公共性配对的条款）；它是"同值性"判据，物理上等价于要求逐位相同，取 `1e-12`/`1e-9`/`2e-16` 都不改任何**合法**数据的通过性 ⇒ 唯一作用是拦掉"合法地不同"的数据 | 消费侧同数值已改作报告字段阈值：`module_adapters.cpp:11564 if (rel > 1e-9) ref_flux_spread_noncommon = true;`（负责人 GAP_AUDIT §9.49 定案 2，见 CR-09-5） | 保守方向：容差越**小** ⇒ 越多组被判"非公共" ⇒ 越**偏红**（不写键）；放大容差只会让写侧更接近消费侧的定案，不会掩盖真错 | CONFIRMED |
| :95/:318（并 :316-317 取值） | 直写硬门 `depth != 9` | 9 | 要求叶 tile 分组为 512×512 | 结构性不适用（HiPS tile 布局的枚举选择）＋**消息文本错**：`:96`/`:319` 自称"(nside>=512)"，而 auto 路径 `phase1_tile_depth` 给 9 需 `log2(nside)−4 ≥ 9 ⇒ nside ≥ 8192`；生产靠 `hp_drizzle_api.cpp:946 config.tile_depth=9` 强制才使 nside=512 合法 | DRIZZLE_GEOMETRY.md:254"HiPS 直写: tile_depth 必须 =9、nside≥512" | 见 CR-09-7 | CONFIRMED |
| :100/:324 | `nside < 512` 拒绝 | 512 | 叶级 nside 下界 | 结构性不适用＋有锚：`aio_hips.h:91` "叶级 nside >= 512（本文件 aio_hips_product_begin 的 nside 合法域下界）"，写侧 `aio_hips_writer.cpp:1200` 同一域（512≤nside≤2^29 且 2^K） | 同上 | 两侧同值，不构成多侧不一致 | CONFIRMED |
| :104/:329 | `n_leaf = 512*512`（=4^9） | 262144 | 每 tile 叶像素数 | 结构性不适用（数组维度） | 与 :95 的 depth=9 门自洽 | — | CONFIRMED |
| :112/:363 | `tile_width` 实参 | 512 | HiPS 瓦片边长 | 结构性不适用（HiPS 标准瓦片尺寸选择） | `module_entry.cpp` 同用 512；`lib/algorithms/drizzle/README.md:39`"tile_depth=9，512×512 leaf tile" | — | CONFIRMED |
| :334 | `std_parent_count = 12ULL·4^(leaf_order−9)` | 12 / 9 | 标准 HEALPix 父像素（order−9 级）基数 | **公式导出**：`N_parent = 12·(nside/512)²`，代码按式算；`leaf_order−9` 的 9 = log2(tile_width) | 与 :104 的 4^9 一致 | 无 | CONFIRMED |
| :34-42 | `phase1_tile_depth`（`<16→0`、`−4`、`>9→9`） | 16/4/9 | auto 档 tile 深度 | 结构性不适用（分组深度公式），**但它是 `hiss::compute_tile_depth`（`hiss_common.cpp:65-79`，注释标 `02_FROZEN §11`）的逐字第二份实现** ⇒ 违背"口径唯一"，见 CR-09-7 | — | 现两实现数值完全一致（逐位对照过），风险只在未来漂移 | CONFIRMED |
| :105-106 | `dtype = sizeof(Scalar)==sizeof(float) ? FLOAT32 : FLOAT64` | 4/8 字节 | 产品位深双轨 | 结构性不适用（位宽判别，非科学量） | DATA_SEMANTICS §12.4 | — | CONFIRMED |
| :113 vs :365 | `creator_did` | `"ivo://astrocs/phase1"` vs `"astrocs/phase1"` | HiPS properties 的 CREATOR_DID | **待确认**：两末端异值，且无一条文档钉住哪个是合法 IVOA resource-ID 形态（`ivo://` 前缀缺失使该键不符合 IVOA identifier 语法） | 保守方向：带 `ivo://` 的一侧更保守；无锚 ⇒ 落待确认 | 见 CR-09-9 | CONFIRMED（异值事实）/ PARTIAL（哪侧合规：未查 docs/contracts 的 DID 条款） |
| :109-110 vs :355-358 | `prod_flags` | `ALL(7)`/`ALL_V19(31)` vs `SIGNAL\|SUPPORT(\|VARIANCE\|IVAR)`=3/27 | 声明的子产品集 | **待确认**：`aio_hips.h:48` `ALL = 7 // signal+support+snr`、`:49 ALL_V19 = 31` ⇒ 通用档**总带 SNR 位**，Phase1 档**从不带**；同一帧两档 properties 声明的产品集不同 | 保守方向：不声明（Phase1 档）比声明而不写更安全 | 见 CR-09-9 ＋ 末节"读不动"第 1 条（SNR 位为真而 snr_pts 空时 writer 是否落空文件） | CONFIRMED（位值）/ PARTIAL（后果需实跑产品面） |
| :366 vs :114-117 | `exposure_s` / `obs_date` | `0.0` + `nullptr` vs `meta.exposure_s` + `meta.obs_time` | 产品头观测元数据 | **待确认**：:360 注释自称"product_begin 参数与旧 writer 逐项一致 (…exposure/obs_date/moc_order)"，但该端实参是硬编码 `0.0/nullptr`（`write_hips_phase1` 形参里根本没有 `DrizzleMeta`）⇒ 注释与实参不是一件事 | 保守方向：不写＝不伪造（优于写 0 冒充未知）；缺的是元数据不是科学值 | 见 CR-09-9 | CONFIRMED |
| :413-416 | `scope_str` 缺省 | `"group"` | p1_snr.json 未给 `snr_reference_scope` 时按 Convention A 解释 | 待确认：缺省侧为"公共 flux_adu"，若写侧实为 Convention B 而漏键，则逐帧 flux_adu 互不相同 ⇒ :434 判非公共 ⇒ 不写键 ⇒ 仍 fail-closed（已复算，不是 fail-open） | FREF-BASELINE-001（:405 注释）＋§9.49 定案 2 | 方向偏红 | CONFIRMED |
| :429/:430/:456/:457 | `.value(key, 0.0)`、`fv>0`、`snr>0`、`fref>0` | 0.0 | 缺键/非正 ⇒ 不参与、不写键 | 结构性不适用（哨兵值与正定性判别，禁伪造口径的载体）；0.0 不是"把缺失当 0 计入"，因为它使该帧被 `continue`/不写键 | :379-380 注释＋setter `aio_hips_writer.cpp:2163-2172` 双保险 | — | CONFIRMED |
| :180/:188/:134 | `> 0.0` / `<= 0.0` / `isfinite` 三态判别 | 0.0 | 归一可用性与尺度合法性 | 结构性不适用（域判别边界 0 与有限性，非科学阈值） | DRIZZLE.md:9 守恒前提 | — | CONFIRMED |
| :222/:575 | `rc == -5 \|\| rc == -2` | −5 / −2 | writer 的"本 tile 无有效方差→跳过"码 | 结构性不适用（错误码枚举） | `aio_hips_writer.cpp:1572`（"该 tile 无覆盖 ⇒ 无有效方差数据"） | — | CONFIRMED |
| :245/:593 | `n_written == 0` | 0 | 空产品拒绝 | 结构性不适用 | :246/:594 报错文本 | — | CONFIRMED |
| :75/:78 | `substr(8)` / `substr(11)` | 8 / 11 | 剥 `cleaned_` / `calibrated_` 前缀 | 结构性不适用（位移量＝对应字面量长度，逐字数过 8 与 11 正确）；手改字面量长度会静默错位，属脆弱写法不属错值 | — | — | CONFIRMED |
| :152/:351 | `ASTROCS_DRZ_SB_FAULT` / `ASTROCS_IVAR_FAULT` | 注入名 | 可执行负例开关（生产代码内的 env 分支） | 结构性不适用（注入点字符串；ENGINEERING_SPEC §8 规定的负例载体） | :148-151/:347-349 注释 | 风险面：env 泄漏进生产即按缺陷发布，但两档都有配套门（`drizzle_pf_sb_gate_legacy_injection`、IVAR-001） ⇒ 偏红 | CONFIRMED |
| :107/:365 | 标题字面量 `"ACSD Phase1"` / `"ACSD "+filter` / `"ACSD Phase1 single-frame stack"` | — | properties OBS_TITLE | 结构性不适用（元数据字符串） | — | 两末端标题串不同 ⇒ 已并入 CR-09-9 的"逐项不一致" | CONFIRMED |
| :259/:286-290/:616-621 | `%.3f` / `%.10g` | 3 / 10 | 计时与 SNR 打印精度 | 结构性不适用（日志格式化位数，不进产品字节） | — | 若被日志解析器当判据需另行核对，本文件无该消费者 | CONFIRMED |

（合并说明：本文件无浮点科学阈值除 `a_cell`、`1e-9`、`255.0`；`:27-31/:36-41/:178/:510/:517` 的 `0/1/2` 与 `>=` 越界判别均为循环/边界常数，已在 :34-42 行与 CR-09-8 内一并列出位点。）

### findings

### CR-09-4 · 编排层实际调用的那一档（profile=0 → `write_hips_direct`）从不写合同规定 HiPS 必存的帧级 SNR 两键，创新点②的帧级权重链在产品面拿不到输入
- 轴：C（主）＋ D
- 位点：`astro_sphere_sink.cpp:459` `if (aio_hips_set_frame_snr(ps, snr, fref) != 0) {`（全仓 lib 内该 setter 的**唯一**调用点，位于 `write_hips_phase1` 体内）；对照 `:111-117`（`write_hips_direct` 的 `aio_hips_product_begin`，其后 :147-260 全流程无任何 `set_frame_snr`）
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:382` "**HiPS 只存帧级 SNR** + 稀疏控制点上的绝对 SNR，权重是阶段二按天球像素对应帧集合现场算出的派生量（`ASTROCS_DESIGN.md` §2.1）"；`aio_hips.h:304-310` "RELEASE-02 SD-15 帧级 SNR 键通道（键名冻结: `ASTROCS_FRAME_SNR` / `ASTROCS_REFERENCE_FLUX`）"；`astro_sphere_sink.h:100` 同键名；消费侧 `module_adapters.cpp:11523-11526` `const bool has_snr = ds && p2_hips_prop_double(ds, "ASTROCS_FRAME_SNR", &fsnr) && fsnr > 0.0;`
- 现状（生产逐跳触发路径，S1 门槛）：
  `orchestrator.cpp:3311-3314` `dll_loader_.get_function(..., ModuleId::DRIZZLE, "hp_drizzle_run_hips")`
  → `hp_drizzle_hips_api.cpp:67-81` `run_drizzle_internal(..., /*hips_profile=*/0, /*hips_filter_passband=*/nullptr)`
  → `hp_drizzle_api.cpp:1202-1207` `write_hips_direct<double/float>(...)`
  → `astro_sphere_sink.cpp:111`（product_begin）… `:262`（finalize），全程不经 `:459`
  → `aio_hips_writer.cpp:1884-1885` 两键在 `ps->frame_snr_set` 为假时不入 properties
  → `module_adapters.cpp:11523` `has_snr=false` ⇒ `compute_inverse_variance_weights` 不闭合 ⇒ `:11573-11581` `Result::fail("... frame-SNR weight chain NOT closed ...")`。
- 差在哪：合同把"帧级 SNR 入 HiPS"写成产品必存内容，而**当前唯一被编排层取用的末端**没有该写入；写入了该键的另一末端（profile=1）又带 CR-09-5 的闸门，且 `hp_drizzle_run_phase1_hips` 在生产里无人调（`docs/traceability/TRACEABILITY_MATRIX.csv:6` 原文："registry descriptor(module_adapters.cpp:570-587 p1_drizzle_descriptor)**未接节点, entrypoint=MISSING**"）。
- 后果：创新点②（跨帧绝对 SNR → 帧级逆方差权重）在产品字节上不可达；两档各自缺一样（profile=0 缺帧级键，profile=1 缺 :250-260 的 SNR 点集与 `meta.exposure/obs_time`），**没有一档能产齐 DATA-P1-HIPS 要求的全集**。不等式方向：本条是"必存键缺失"⇒ 消费侧判红（偏红），不是误绿；但产品面主张（"HiPS 存帧级 SNR"）为**假绿**。
- 定级：**S1**（产物缺必存键 + 对外主张不成立，触发路径逐跳有 `文件:行`）
- 怎么算修好：① 在 `write_hips_direct` 内接同一套 `p1_snr.json → set_frame_snr` 通道（与 :374-488 同一实现，勿再复制第二份），或把两末端合一由 `meta` 决定是否带 sidecar；② 同批把 `hp_drizzle_run_phase1_hips` 接线或把合同（`DATA_SEMANTICS.md:395`"Phase1 末端为 hp_drizzle_run_phase1_hips"）改成实际末端，二者择一，否则文档继续互相打脸；③ 补一条产品级门：对**磁盘上的 HiPS properties** 断言两键存在且 >0（现在 `eng/tests/unit/p1001_real_nodes_test.cpp` 只测 phase1 档的夹具，不测生产档产物——判据④"门接夹具不接产品"的同型缺口）。
- 置信：CONFIRMED（调用点/两末端代码/合同条文/消费侧判定全部直读；未实跑 CLI，故"最终 rc"未观测 ⇒ 该子项 PARTIAL）

### CR-09-5 · 写侧仍把"组内 F_ref 必须逐帧相等（1e-9）"当 fail-closed 闸门，与负责人 GAP_AUDIT §9.49 定案 2 及消费侧现行实现相悖
- 轴：C（主）＋ S
- 位点：`astro_sphere_sink.cpp:434-437` `} else if (std::fabs(fv - group_fref) > 1e-9 * std::fabs(group_fref)) { group_fref_ok = false; }`，生效点 `:456-458` `... && group_fref_ok) {`
- 上位依据：消费侧 `module_adapters.cpp:11543-11554` 原文——"负责人 GAP_AUDIT §9.49 定案 2（帧间独立）／旧实现把「组内 F_ref 必须逐帧相等」当 fail-closed 闸门。该要求**科学上不成立**：配对性定理（WEIGHT-SCI-001）只要求**同一帧内** SNR 与 F_ref 同源（`w_k = SNR_k²/F_ref,k²`），**不要求跨帧相等**……旧闸门等价于「不同光学系统的帧混装即报错」……并使 weight_mode=2 在多指向拼接上完全不可用（实测：6 帧跨 t2_m1/t2_m2 两块 ⇒ 6/6 被拒，链路永不闭合）⇒ 逐帧用自己的 F_ref,k；跨帧一致性降级为**报告字段**，不再是门"；同文件 `:11564` 已把同一个 `1e-9` 只用作 `ref_flux_spread_noncommon` 报告位。
- 现状：本文件的 :398-404 注释仍以"P2b-4：写侧**组内公共 F_ref** 闸门"为正确性依据，并要求 scope="group" 时 `flux_adu` 逐帧相同；一旦不同即整体不写两键（:439-444 仅打印一行 stderr）。
- 差在哪：同一件事（跨帧 F_ref 是否必须相等）在生产两侧取相反判定——消费侧按定案 2 允许逐帧、写侧按旧口径拒绝；被写侧拒掉的正是定案 2 判定为**合法**的数据（不同滤镜/零点 ⇒ `F_ref,k = 10^(-0.4(m_ref−ZP_k))` 天然互异）。
- 后果：多指向/多滤镜组的帧集合永远拿不到 `ASTROCS_FRAME_SNR`，CR-09-4 的断链被再加固；对外主张"阶段二按帧集合现场算权重"在最常见抖动+多滤镜场景不成立。方向：闸门使写侧**不产出** ⇒ 偏红（对消费侧），但对"产品齐备性"是假绿（表面写成功、实则键缺）。
- 定级：S2 —— 写侧所在档（profile=1）当前无生产调用者（`TRACEABILITY_MATRIX.csv:6` entrypoint=MISSING），缺运行时触发路径；一旦按 `DATA_SEMANTICS.md:395` 接线即升 S1。
- 怎么算修好：删去"跨帧不等 ⇒ 不写键"这条门（保留 `scope` 合法性判定：:420-421 `scope_str` 值域检查必须留），改为逐帧各写自己的 `F_ref,k`＋把离散度作为 properties/报告字段；与 `module_adapters.cpp:11542-11564` 同批复核，并同步 `:398-412` 注释与 FREF-BASELINE-001 登记面。
- 置信：CONFIRMED（两侧条文与数值逐字对齐；§9.49 原文在 GAP_AUDIT 里未直接读，以消费侧转录件为据 ⇒ 该引用链 PARTIAL）

### CR-09-6 · 两末端对同一 `sumArea` 用两套分母口径（raw vs u8 量化且钳位），使 signal/variance 产品字节随档位变化，且量化尾数会把低覆盖叶判成"无覆盖"
- 轴：R（主）＋ C
- 位点：`:191` `dense_area[local] = acc.sumArea;` ↔ `:542` `area_buf[local] = (float)(((double)q / 255.0) * a_cell);`（`q` 来自 :521-526）
- 上位依据：`aio_hips_writer.cpp:10-11` "signal/ Image HiPS: signal = flux_sum/covered_area；support/ Image HiPS: support = covered_area/A_cell"；`:1472` "variance = var_num_sum / covered_area²"；`:1476` "无覆盖 (covered_area<=0) -> NaN"；`:1515` "有覆盖 = 该像素至少收到一个合格样本（valid ∧ covered_area>0 ∧ 有限）"。DRIZZLE.md:9 的面亮度不变量要求分母取 `Σ_j a_jp`（连续值）。
- 现状：sink 注释自称两末端同一口径（`:183` "与 write_hips_direct 逐字相同"、`:535-536` "唯一源 … 两末端禁止各自内联分母"），但被禁止内联的只是 `k`，**分母 A_cov 本身两处不同**；phase1 侧 `q = lround(255·clamp(sumArea/a_cell,0,1))` ⇒ `q=0` 当且仅当 `sumArea/a_cell < 0.5/255 ≈ 1.960e-3`，此时 `covered_area=0` 而同一叶的 `flux_buf[local] = sumFlux·k` 一般非零、`valid_buf[local]=1`（:548）。
- 差在哪：同一叶像素在 phase1 档出现"signal 有值、support=0、variance/ivar=NaN"的自相矛盾三态；且 u8 反折引入最大 1/510·A_cell 的分母偏差 ⇒ `signal` 与 `variance`（平方放大）系统性偏移，直接破坏 :193/:547 那条形恒等式（`var = sumVarNum/N_p²` 只在 A_cov≡sumArea 时成立）。
- 后果：`support` 面分辨率被钉死在 256 级（口径②的弦多边形 overshoot 与低覆盖叶都在那里饱和）；profile=1 档产品的 variance/ivar 相对 profile=0 档不可比 ⇒ 创新点②的跨帧方差传播在档位切换时不守恒。方向：低覆盖叶被判 0 ⇒ 该像素在方差面偏红（NaN），但同时**掩盖**了"signal 非零却无支撑"的不一致，属产物内部不自洽。
- 定级：S2 —— 生产可达的档位是 profile=0（连续面积，无此偏），profile=1 档无生产调用者（同 CR-09-4 的 entrypoint=MISSING）；缺运行时触发路径，但两档差异本身即产品口径分裂。
- 怎么算修好：① 若"逐字节等价于旧 writer"仍是要保的对外主张，则把 :542 与 :521-526 整体删掉、两末端统一传 raw `sumArea`，并改 `p1001_real_nodes_test.cpp` 的钉值；② 若保 u8 兼容档，则 `support` 与 `covered_area` 必须分列（u8 只进 support 面，signal/variance 的分母用连续面积），并在 `q==0 ∧ sumFlux≠0` 时报错而非静默；③ 同批补一条产品级门：同一输入在两档下的 signal/variance 相对偏差须显式登记（当前无任何门比较两档）。
- 置信：CONFIRMED（`q=0` 条件由 :521-526 逐值复算：`lround(255·S)=0 ⇔ S<0.5/255`；writer 三态语义直读）

### CR-09-7 · `phase1_tile_depth` 是 `hiss::compute_tile_depth` 的逐字第二实现，且拒绝消息给出的条件与代码实际条件不符
- 轴：C（主）＋ S
- 位点：`:34-42`（`if (nside < 16) return 0; … int d = (int)l - 4; … if (d > 9) d = 9;`）↔ `lib/infrastructure/aio/src/hiss_common.cpp:65-79`（同式，注释标 `02_FROZEN §11`）；`:96` `err = "HiPS 直写要求 tile_depth=9 (nside>=512), 当前 depth=" + …`
- 上位依据：负责人口径"口径唯一／常数须有推导"（`project-doc-doctrine-owner-ruling`，落点 `ENGINEERING_SPEC`）；`hiss_common.cpp:59-62` 原文 `// d = min(9, log2(NSIDE/16))  // tile_nside = NSIDE / 2^d  // 保证: 满 Tile 最多 4^9=262144 叶像素`；`drizzle_engine.cpp:338` 的 auto 路径已经调 `hiss::compute_tile_depth`
- 现状：同一模块内 `drizzle_engine.cpp:337-339 eff_tile_depth()` 与 `astro_sphere_sink.cpp:93-94/316-317` 两套 auto 深度取值入口；后者不链 `HISS_EXPORT compute_tile_depth` 而自带副本；:33 注释自认"原 legacy 容器 auto 口径"。消息文本 `(nside>=512)` 对 auto 路径为假（auto 下需 `nside ≥ 2^13 = 8192` 才得 depth 9）。
- 差在哪：一份导出函数 + 一份手抄副本 + 一条与代码不符的报错条件；文档 `DRIZZLE_GEOMETRY.md:254`、`DATA_SEMANTICS.md:394` 还把该判据的位点标成 `astro_sphere_sink.cpp:36-51`（现指 :34-42 的辅助函数，真正门在 :93-103/:316-328）——锚点漂移。
- 后果：改一处 auto 口径必然漂移出两档不同深度（当前值一致，属未爆）；报错文本误导排障者往"nside 太小"方向找，实际是 tile_depth 未强制 9。
- 定级：S3（无当前错值：生产经 `hp_drizzle_api.cpp:946` 恒置 `tile_depth=9`，副本与文档函数逐位一致）
- 怎么算修好：`astro_sphere_sink.cpp` 删 :34-42 改调 `hiss::compute_tile_depth`（drizzle 已链 hiss，`drizzle_engine.cpp:338` 即证据）；报错文本改述实际判据（`tile_depth 必须为 9`）；同批改 `DRIZZLE_GEOMETRY.md:254`／`DATA_SEMANTICS.md:394` 的 `文件:行` 锚。
- 置信：CONFIRMED

### CR-09-8 · 三处越界索引被静默丢弃（无计数、无告警），与同文件对"归一不可用"必计数告警的自订口径不一致
- 轴：R
- 位点：`:178` `if (local >= n_leaf || local >= tile.pixels.size()) continue;`、`:517` 同式、`:510` `if (tp->parent_ipix >= std_parent_count) continue;  // 越界 parent 不写出`
- 上位依据：本文件自订口径 `:169` `std::uint64_t n_norm_unavailable = 0;   // sumArea>0 但 sumNorm<=0 (归一不可用)` 与 :277-284 "不静默 —— 计数 + 显式告警"；`docs/science/DRIZZLE.md:9` 的严格通量守恒不变量 `Σ_p F_p = Σ_j x_j`
- 现状：三种丢弃都直接跳过，既不计入 `n_written` 也不打印；`n_written == 0` 才报错（:245/:593），所以"丢一半留一半"照常发布并打印 "直写完成: N tiles"。
- 差在哪：同一文件内，"有覆盖但无归一分母"必须计数告警，"索引越界导致整块数据不落盘"却零痕迹；后者破坏的是守恒不变量（分母丢项 ⇒ 产品总通量 < 输入总通量），比前者更重。
- 后果：任何上游把 parent 数算错（例如 nside 与 tile_depth 组合改变）时，产品缺块但报成功，通量守恒主张失真且无诊断线索。方向：缺块 ⇒ 守恒检验偏红（若真做守恒检验）；但"成功"状态偏绿。
- 定级：S3 —— 需要上游先出错才触发，本批文件内无产生越界索引的路径（`std_parent_count` 与 :334 的式子对 `nside=2^K, K≥9` 恒正确，已复算 12/48/192）。
- 怎么算修好：三处 `continue` 前加与 `n_norm_unavailable` 同型的计数＋收尾告警，非零即让函数返回 false（fail-closed 与"禁伪造"口径一致）。
- 置信：CONFIRMED

### CR-09-9 · `write_hips_phase1` 自称"product_begin 参数与旧 writer 逐项一致"，但该调用与通用档在 DID／flags 位／观测元数据三处异值，且两末端产品集不同名同实
- 轴：C
- 位点：`:360-366`（注释 + 实参 `prod_flags, "astrocs/phase1", "ACSD Phase1 single-frame stack", filter_passband.c_str(), 0.0, nullptr, 0`）↔ `:109-117`（`AIO_HIPS_PRODUCT_ALL_V19/ALL, "ivo://astrocs/phase1", title, meta.filter, meta.exposure_s, meta.obs_time, 0`）
- 上位依据：`aio_hips.h:48-49` `ALL = 7 // 向后兼容 (signal+support+snr)`、`ALL_V19 = 31 // 全产品 (含 variance/ivar)`；`DATA_SEMANTICS.md:395` "末端产物 = signal/support 全 tile + Moc.fits + metadata.fits"；`docs/contracts/PUBLIC_API.md:262` "hp_drizzle_run_phase1_hips（**Phase1 正式末端**…）"
- 现状：同帧两档的 flags 分别为 3 或 27（phase1，无 SNR 位）与 7 或 31（direct，恒含 SNR 位）；DID 与 OBS_TITLE 字面量不同；EXPTIME/DATE-OBS 在 phase1 侧硬编码为 `0.0/nullptr`（其形参表里根本没有 `DrizzleMeta`）。注释却写"参数与旧 writer **逐项一致** (creator_did/obs_title/obs_filter/exposure/obs_date/moc_order)"，把六个硬编码字面量称作"一致"。
- 差在哪："逐项一致"在合同语言里是可核对的等价断言；这里至少三项不等价，且 flags 的 SNR 位差异使两档 properties 声明的子产品集不同（一档声明 snr 位却可能 `snr_pts` 为空——见末节待实跑项）。
- 后果：以 properties 判别产品形态的消费者（`aio_hips_reader.cpp:614` 区段的读侧硬校验、hips_p2 采样器）在两档下得到不同的声明；对外"Phase1 末端与旧 writer 逐字节等价"主张的核对范围被夸大。
- 定级：S3（不产生错值，产生的是不可核对的措辞＋档位声明面分裂；与 CR-09-4/6 同根）
- 怎么算修好：把 :360-361 的"逐项一致"改成可核对的具体等价项（哪些字面量与旧 writer 同值、哪些**故意**不写）；两末端 flags 统一由一个函数从 `has_variance`+`snr_pts 非空` 导出，禁两处各自 OR。
- 置信：CONFIRMED

> 成稿顺序说明：本稿实际呈现顺序为 **文件 2 → 文件 3 → 文件 1 → 文件 4 → 文件 5 → 文件 6**（文件 2/3 是创新点④/②的落地面且互为上下文，先出；文件 1 是纯 I/O 事务面，放后；4/5/6 是剖析与证据面）。finding 编号仍按清单序归组：CR-09-1..3＝文件 1，CR-09-4..9＝文件 2，CR-09-10..11＝文件 3，CR-09-12..15＝文件 4/5，CR-09-16..18＝文件 6。

---

## 文件 3 `lib/algorithms/drizzle/healpix_drizzle/astro_sphere_sink.h`（116 行，整读）

性质：两直写末端的声明头，同时是发布因子 `k` 的**唯一源**（`sb_publish_scale`，:50-52 内联定义）。
本文件是本批唯一真正承载创新点④/②科学口径的代码。

先给核对结论：
- **口径①（核按 drop 分数交叠面积归一、通量守恒与 pixfrac 无关）＝成立，且本文件的推导站得住。**
  定义链（:27-33）与 `docs/science/DRIZZLE.md:41-50` 的冻结式逐符号对齐：
  `w_jp = a_jp/A_drop,j`（DRIZZLE.md:45）、`F_p = Σ_j x_j·w_jp`（:47）、`D_p = Σ_j a_jp`（:48）、
  `N_p = Σ_j w_jp·A_pixel,j`（:49）、`S_p = F_p/N_p = Σ_j B_j a_jp/Σ_j a_jp`（:50）。
  闭合式 `k = D_p/N_p = pixfrac²`（:39）我独立复算成立：`A_drop,j = pixfrac²·A_pixel,j` ⇒
  `N_p = Σ_j a_jp·A_pixel,j/(pixfrac²·A_pixel,j) = D_p/pixfrac²` ⇒ `k = pixfrac²`，
  与 `pixfrac` 是否为 1 无关；`pixfrac=1 ⇒` 每条累加项 `sumNorm += a_jp·(A_p/A_p)`，`A_p/A_p≡1.0` 逐位 ⇒ `sumNorm≡sumArea` ⇒ `k≡1.0`（:40-41 主张成立）。
  通量守恒的口径归属也自洽：守恒量是 `F_p`（DRIZZLE.md:47 `Σ_p F_p = Σ_j x_j`），发布量是 `S_p`；
  sink 发布 `flux_sum = F_p·k` 再由 writer 除 `covered_area` ⇒ `signal ≡ S_p`，与"通量守恒与像素收缩因子无关"同向。
- **口径②（四角大圆弦多边形＝逐片面积误差地板）＝本文件不冲突，但把地板转成了判据风险**：
  :36 与本文件全部推导都以"代码实测的 `Σ_j a_jp`"为分母，而非以 `pixfrac²` 字面量代入；
  这意味着弦多边形面积的系统偏差**不会**通过 `k` 抵消（`k` 是两个同样带偏差的面积比，偏差按相对方式残留），
  而 `support = D_p/A_cell` 用的是**解析** `A_cell`（文件 2:331）⇒ `support>1` 只能来自面积算法的 overshoot，
  正是文件 2 里钳位/量化（:521-526）与 writer 钳位计数（`aio_hips_writer.cpp:930`）存在的原因。口径②在此是**被承认**而非被反驳。
- 本文件的文档锚点我逐条回查通过：`:31` 引 `docs/science/DRIZZLE.md §5:50/§5:83` ⇒ 实读 `:50 = S_p = F_p / N_p = …# 最终信号为面亮度`、`:83 = variance_p = sumVarNum / N_p²`，**行号未漂移**（本批唯一一条精确到行的科学锚，通过）。

### §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :38-41 | `k := D_p/N_p = sumArea/sumNorm = pixfrac²` | — | 对接"累加器口径"与"writer 落盘口径"的唯一因子 | **公式导出**（闭合式 `k=pixfrac²` 我独立复算成立，见上节；代码用比值实算而非代入 `pixfrac²` 字面量 ⇒ 合规且更稳健） | DRIZZLE.md:44-50（逐符号定义）、:127-128（两不变量仅在 `N_p` 取此式时同时成立） | — | CONFIRMED |
| :51 | `sum_norm > 0.0 ? sum_area/sum_norm : 1.0` 的 **`0.0`** | 0.0 | 归一分母可用性的判别边界 | 结构性不适用（0 是符号域边界，不是调参阈值）；但它决定 :50 走哪支，见下行 | — | — | CONFIRMED |
| :51 | 回退值 **`1.0`** | 1.0 | `sumNorm<=0` 时不改写累加值（等价于"漏乘 k"） | **待确认**：条文面零提及——检索式 `git grep -n "退回\|fallback\|k=1\|sumNorm<=0\|sum_norm > 0" -- docs/science/DRIZZLE.md docs/algorithms/DRIZZLE_GEOMETRY.md docs/contracts/DATA_SEMANTICS.md`，DRIZZLE/GEOMETRY **零命中**（DATA_SEMANTICS 命中项全是权重/ivar 的其它 fallback 语境，与本分母无关） | 保守方向：该值应取 **fail-closed（判红/NaN）**而非 1.0；影响范围＝该叶的 signal/variance/ivar 三个面的绝对标度，偏 `1/pixfrac²`（pixfrac=0.8 ⇒ +56%；pixfrac=0.5 ⇒ 4×）。**同时必须登记**：在生产累加器下该分支不可达（见 CR-09-10 的推导），所以 1.0 的"错"目前只在手工构造累加器/FP 下溢时兑现 | CONFIRMED |
| :47-48 | 幂次 `+1`（signal）/`+2`（variance） | 1 / 2 | 同一分母下两面的折算幂次 | **公式导出**：`var_pub=(sumVarNum·k²)/D_p² = sumVarNum/N_p²`（:45 的"反解"即此式，代码文件 2:193/:547 真按式算） | DRIZZLE.md:81-84（SCI-DRZ-014）＋:87-88"信号与方差必须用同一 `w_jp`" | 幂次写成 1/2 以外的值即破坏 SNR 标度；无自由参数 | CONFIRMED |
| :56、:92 | `depth=9` / `512x512 叶 tile` | 9 / 512 | 末端入参约束（叶 tile 分组） | 结构性不适用（HiPS 瓦片布局选择），有锚：`docs/algorithms/DRIZZLE_GEOMETRY.md:254`"HiPS 直写: tile_depth 必须 =9、nside≥512" | 与文件 2 :95/:318 的门同值（本头是"要求"，实现侧的门文本有误 ⇒ 已记 CR-09-7） | CONFIRMED |
| :83 | `u8 = lround(255*clamp(sumArea/A_cell,0,1))`、`area=(u8/255)*A_cell` | 255 / 0 / 1 | 旧容器 support 的量化口径（写进头文件"等价要点"） | 255＝**结构性不适用**（uint8 位深决定的量化级数）；0/1＝**公式导出**（覆盖比的自然值域，>1 只能是算法误差 ⇒ 钳位即"地板承认"，见口径②） | 文件 2 :521-526/:542 的实现；`aio_hips_writer.cpp:2781` 旧形态 `covered_area = su/255*A_cell` | 两末端不一致 ⇒ CR-09-6（本头只声明 phase1 档采用它） | CONFIRMED |
| :85 | `AIO_HIPS_FLOAT32` | 32 bit | signal 面产品位深 | 结构性不适用（位深枚举，非科学量）；与 `hp_drizzle_api.cpp:1039-1049` 的"消费域是 float32 + 下溢地板"条款配对 | DATA_SEMANTICS §12.4 | — | CONFIRMED |
| :87-89 | `tile 按 parent_ipix 升序` | — | 累加顺序 ⇒ 浮点位级复现 | 结构性不适用（排序键选择） | 文件 2 :490-499 实现 | — | CONFIRMED |
| :100-101 | `ASTROCS_FRAME_SNR` / `ASTROCS_REFERENCE_FLUX` | 键名 | 帧级 SNR 两键（"键名冻结"） | 结构性不适用（字符串键名，无科学语义）；**键名的"冻结"状态在两处登记面并存但值域未定**：`aio_hips.h:304-305`"键名冻结"、`module_adapters.cpp:11504`"键名与 Phase1 写入端尚未冻结（报告未闭合项）" | 同左 | 该未闭合项是登记面冲突，非本头之错；本头与 aio_hips.h 同值 ⇒ 代码侧一致 | CONFIRMED |
| :27-33 | `sumFlux/sumArea/sumNorm/sumVarNum` 四量定义 | — | 累加器语义 | **公式导出**（对 DRIZZLE.md:45-50 与 :82-84 的逐项转录，已与 `drizzle_engine.cpp:1640-1645` 实际累加式核对同向：`sumArea+=overlap_area`、`sumNorm+=overlap_area*(pixel_area/drop_area)`） | DRIZZLE.md:44-50/:81-88 | — | CONFIRMED |
| :72/:90-91 | "逐字节等价/完全一致" 的对照物 | — | 与旧 `p1_op_writer` 的等价主张 | 见 CR-09-11（不是数值，但它是本头最强的对外主张，且当前不可核对） | — | — | CONFIRMED |
| :5-8 | `astro_image_io.dll aio_hips_product_begin/write_*/finalize` | — | 数据流归属声明 | 结构性不适用（架构指向）；**内容已过时**：本仓现路径是 `lib/infrastructure/aio/src/hips/aio_hips_writer.cpp`，`astro_image_io.dll` 属改名前命名（对照 `project-acsd-rename-pending`：改名未贯穿） | `docs/contracts/ARCH-001.md`、`lib/infrastructure/aio/` 目录实况 | 影响：仅注释指向错误模块名，不改字节 | CONFIRMED |

（合并说明：本头无独立阈值/容差/系数；除 :51 的 `1.0`/`0.0` 外全部数值都是位深、量化级数、tile 布局或排序声明。）

### CR-09-10 · `k` 的 `sumNorm<=0` 回退值 1.0 无任何条文授权：它复刻的正是本仓用可执行负例判为"必须判红"的那个缺陷，而该分支在生产累加器下不可达 ⇒ 文件 2 :277-284 的"不静默"安全网虚设
- 轴：S（主）＋ R ＋ C
- 位点：`astro_sphere_sink.h:51` `return (sum_norm > 0.0) ? (sum_area / sum_norm) : 1.0;`（配套注释 `:49` "sumNorm<=0（手工构造的累加器/空覆盖）退回 k=1.0（沿既有约定，计数并告警）"）
- 上位依据：`docs/science/DRIZZLE.md:50` 冻结发布量为 `S_p = F_p/N_p`；`:112-116` 通量守恒不变量（FZ-COND-FLUX-CONSERV）；`:127-128`"两条不变量的相容性（本节的非退化判据）：…只有在归一分母取 `N_p=Σ_j w_jp·A_pixel,j` 时同时成立"。**条文面零提及**任何"分母不可用 ⇒ 因子取 1"的回退（检索式见处置表 :51 行）。与"禁伪造/不静默退化"的仓内总口径（`DATA_SEMANTICS.md:1199` "具名 fail-closed 拒绝"、`module_adapters.cpp:11506` "任一帧缺键 → 权重链 fail-closed（不静默退化为等权）"）方向相反。
- 现状：`k=1.0` 时发布量变成 `sumFlux/covered_area = F_p/D_p`（covering-area 口径），而同一产品其余像素是 `F_p/N_p`（面亮度口径）⇒ 一个 FITS 平面内混两种量纲；文件 2 :180-181 计数、:279-284 打印告警后**照常发布**。
- 差在哪（两部分，都要成立才算这条）：
  ① 语义：与本文件 :48"任一末端漏乘 ⇒ signal 偏 1/pixfrac²、variance 偏 1/pixfrac⁴"完全同一数值形态——文件 2 :148-159 把该形态做成**必须判红**的注入负例（`ASTROCS_DRZ_SB_FAULT=legacy_dp_normalization` 就是恒取 `k=1.0`），而本行把同一件事写成合法回退。
  ② 可达性（我复算了方向）：`sumNorm` 的每条累加项 `= a_jp·(A_pixel,j/A_drop,j)`（`drizzle_engine.cpp:1644`），两个面积均取自同一次几何调用且 `>0` ⇒ `sumNorm>0 ⇔ sumArea>0`；故 `sumArea>0 ∧ sumNorm<=0` 只能来自 (a) 手工构造的累加器（:49 自认的唯一场景，即测试夹具）、(b) `Scalar=float` 下两条同时下溢/`inf` 污染。⇒ 生产面该分支不触发，`:279-284` 的告警与 `n_norm_unavailable` 计数在产品上恒为 0。
- 后果：① 一旦有人按 :49 的说法在测试里构造该状态，产品面出现混量纲且门不判红（`drizzle_pf_sb_gate` 只测全局注入，测不到逐像素回退）；② 对外"归一不可用时不静默"的承诺由一条不可达分支承载，属"门判不到"的形态。不等式方向：本条**不是**判据方向错，而是"回退值改了量纲却不改变通过/失败"⇒ 偏绿（对逐像素回退而言）。
- 定级：S2 —— 生产触发路径要求 `sumArea>0 ∧ sumNorm<=0`，按 ① 的推导在生产累加器上不可构造；能构造它的只有测试面（缺运行时触发路径 ⇒ 不升 S1）。
- 怎么算修好：① 把回退从 `1.0` 改为**不发布该叶并由调用方判红**（例如返回 `NaN` 哨兵或让 `sb_publish_scale` 返回 `bool`+值，函数内禁默认值）——与 :49 现注释的"沿既有约定"必须同批删除；② 若判定"保留 1.0 并计数"是有意口径，则必须在 `docs/science/DRIZZLE.md §5` 补一条正向约束（谁可缺 `N_p`、缺时产品面语义是什么），否则执行纪律上仍是无锚常数；③ 同批把文件 2 :279-284 的告警升级为 rc≠0（否则计数只服务日志），并补一条**逐像素**负例进 `drizzle_pf_sb_gate`（构造单叶 `sumNorm=0` 断言产品面判红）。
- 置信：CONFIRMED（可达性不等式我独立复算；"条文零提及"以两条检索式为据；未实跑门）

### CR-09-11 · 本头的"与旧 writer 逐字节等价/完全一致"没有仓内可核对的对照实现，且它声明的 `product_begin` 一致项与四面登记的缺省 DID/`ivo://` 形态直接冲突
- 轴：C（主）＋ D
- 位点：`astro_sphere_sink.h:72` `// 逐字节等价要点 (与 lib/infrastructure/scheduler/src/module_adapters.cpp 旧 p1_op_writer 同口径):`、`:90-91` `* product_begin 的 creator/title/filter/exposure/obs_date/moc_order 与旧 writer 完全一致。`；实参侧 `astro_sphere_sink.cpp:365` `"astrocs/phase1", "ACSD Phase1 single-frame stack"`（↔ 同文件 `:113` `"ivo://astrocs/phase1", title`，`title` 为 `"ACSD Phase1"` 或 `"ACSD "+filter`）
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:438` "**缺省 did=ivo://astrocs/phase1、title="ACSD Phase1"（:414-415）**"；`docs/algorithms/HIPS_WRITER.md:57` "缺省元数据：creator_did=\"ivo://astrocs/phase1\""；`lib/algorithms/drizzle/hips/include/astrocs/hips/types.h:68` `/* 缺省 ivo://astrocs/phase1 */`；`lib/algorithms/drizzle/hips/README.md:65` 同（四面同值）；`docs/contracts/DATA_SEMANTICS.md:395` 把 `p1_op_writer` 归入 **legacy 容器读面**（"legacy 容器含 p1_stack 与 p1_op_writer 的容器读面"）。
- 现状：`p1_op_writer` 符号仍在 `module_adapters.cpp`（`git grep -c p1_op_writer` = 5 处），但该文件内**没有任何 HiPS 写出调用**（`git grep -n "aio_hips_product_begin" -- lib/infrastructure` 只命中 `aio_hips.h` 与 `aio_hips_writer.cpp`）⇒ "与旧 writer 逐字节等价"的另一侧已不含被主张的那件事；同时 `write_hips_phase1` 传的 DID 无 `ivo://` 前缀、OBS_TITLE 也与四面登记的缺省串不同，而 :90-91 声称"完全一致"。
- 差在哪：一条对外等价主张既无对照物又与四面登记值冲突；DID 是标识符而非自由文本，`astrocs/phase1` 不符合 IVOA resource-ID 形态（`ivo://` 方案前缀）。
- 后果：任何按 properties 里 CREATOR_DID 认产品归属的消费者（含 `eng/tests` 与 hips_p2 侧读校验）在两档下拿到不同标识；对外"Phase1 末端产物与旧实现逐字节等价"的主张目前**不可复核**（既无对照实现，也无产品级门）。
- 定级：S2 —— `write_hips_phase1` 无生产调用者（`hp_drizzle_run_phase1_hips` 仅被 `hp_drizzle_hips_api.cpp:34` 导出，`docs/traceability/TRACEABILITY_MATRIX.csv:6` 记 descriptor "未接节点, entrypoint=MISSING"）；按 `DATA_SEMANTICS.md:395` 接线即升 S1。
- 怎么算修好：① DID 改回 `ivo://astrocs/phase1`（与四面登记一致），title 若有意区分则把 `DATA_SEMANTICS.md:438` 的缺省串改为允许的两个合法值之一并说明适用域；② 把 :72/:90-91 的"逐字节等价/完全一致"降级为可核对的具体项（列出哪些字段同值、哪些故意不同），等价证据要么落跟踪面（`实验/` 或 `docs/`，不得只落 `run/`），要么改为"产品级门比较两档同输入产物"；③ 同批清理 `module_ports.registry.json:820` 里 `p1_op_writer` 的 symbol 登记与该节点的实际职责（现仅剩 variance_audit 读面）。
- 置信：CONFIRMED（异值与登记面直读；"p1_op_writer 已不写 HiPS"以两条 git grep 的零命中为据 ⇒ 该子项 CONFIRMED-by-absence，若要判它仍有旁路写点需实跑一次 p1 全链，见末节）

---

## 文件 1 `lib/algorithms/drizzle/hips/src/aio_publish.cpp`（235 行，整读）

性质：HiPS 原子发布原语（staging→fsync→promote）唯一实现，纯 I/O 事务面。
**本文件零科学量**——派单口径①（核权重按 drop 分数交叠面积归一）与口径②（叶用四角大圆弦多边形）
在本文件无任何落点：文件内没有面积、权重、方差分母、弧弦容差任何一项（检索式
`git grep -n "area\|weight\|ivar\|var\|tol\|eps\|frac" -- .../aio_publish.cpp`，命中仅 `total_bytes`/`abort_rc`）。
派单把这文件归入"创新点④首段"仅就目录归属成立，就内容成立的是④产品的**发布事务**，不是④的算法。

### §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :38（并 :24 注释、:85-86/:150/:163-164/:179 使用点） | `PUBLISH_PATH_MAX` | 1024 | staging 路径与 parent/base 段字符缓冲上限 | 结构性不适用（缓冲区尺寸，不进科学语义） | publish.h:79 只写"容量不足 → PARAM; 推荐 1024"，且指**调用方**缓冲；本行把它升格为内部缓冲的"冻结推荐值" | 保守方向：无（越界路径由 :90 的 PARAM 兜住，实测推演见 CR-09-3） | CONFIRMED |
| :100 | 递归深度上限 | 64 | `publish_fsync_tree` 栈深护栏 | 结构性不适用（递归上界的实现选择，不表达科学语义） | 注释 :24 给的理由"HiPS 树深 ≤ Norder15 + 2 目录段"里 **Norder 上限 15 无锚**：`git grep -in "nside" -- lib/algorithms/drizzle/hips/README.md lib/algorithms/drizzle/hips/module.yaml docs/algorithms/HIPS_WRITER.md \| grep -i "max\|上限\|32768"` 零命中 | 即便按 MOC 域 Norder 29 计树深仍 <64 ⇒ 护栏不会误杀合法树；超限时返回 ERR_IO（把病态输入判红，方向偏红不偏绿） | CONFIRMED |
| :106 / :110 / :124 | `kind` 1 / 0 / 2 | 1=目录 0=常规 2=其他 | `for_each_child` 回传的项类型判别 | 结构性不适用（枚举基数） | aio_atomic_file.h:430 `S_ISDIR?1:(S_ISREG?0:2)` | Windows 分支 :409-411 只产 0/1 ⇒ :124 "符号链接不 fsync" 在 Windows 无第三类，目录/文件符号链接按常规处理 | CONFIRMED |
| :113 / :116 | `sz`、`*total_bytes += sz` | 0 / 累加 | 发布树字节数统计出参 | 结构性不适用（累加器初值，无科学语义） | publish.h:91-92 声明该出参可 NULL | 出参在生产被丢弃 ⇒ 见 CR-09-1 | CONFIRMED |
| :111 | `(*n_files)++` | 自增 | 发布树文件计数 | 结构性不适用（计数本身），**但其宣称的"校验"语义无锚** | publish.h:20 称第 3 步含"文件计数校验" | 无期望值入参 ⇒ 库内不可能"校验"，见 CR-09-1 | CONFIRMED |
| :118 / :133 | `fsync_path(child, 0)` / `(path, 1)` | 0 / 1 | 是否目录旗标 | 结构性不适用（布尔旗标） | aio_atomic_file.h:474 签名 | — | CONFIRMED |
| :120 / :135 | `ENOSPC`、`EDQUOT` | OS 常数 | 磁盘满判定集合 | 结构性不适用（OS 定义常数，非本仓发明的科学量） | publish.h:66/:94 同一条款点名 ENOSPC/EDQUOT → DISKFULL | 与合同一致，非自发明 | CONFIRMED |
| :127 → :131 | lambda 恒 `return 0` ⇒ `abort_rc` | 0 | 遍历中止码回传 | 结构性不适用，但登记为**不可达分支**：`for_each_child` 仅在 fn 返回非 0 时写 abort_rc（aio_atomic_file.h:412/:432），本 TU 的 fn 恒 0 ⇒ :131 `rc = abort_rc` 永不触发 | 无条文要求该分支 | 方向：不产生误绿（该处另有 rc 汇总兜底），仅使"读错误 → DISKFULL"这类映射在 walk 级缺失（walk 失败一律 :130 ERR_IO） | CONFIRMED |
| :64 | `while (n > 1 && …)` 去尾分隔符 | 1 | 保留根 "/" 不被剥空 | 结构性不适用（词法边界常数） | 无条文（纯词法） | — | CONFIRMED |
| :69-70 | `parent_cap - 1` / `base_cap - 1` | cap−1 | 预留 NUL 截断 | 结构性不适用（字符串终止预留）。**但截断静默**：见 CR-09-3 的推演结论＝最终仍被 PARAM 兜住 | publish.h:54"路径溢出 → PARAM" | 溢出后 total 必 >1024 ⇒ :90 判 PARAM，fail-closed | CONFIRMED |
| :73 / :77 | `'.'` 与 `"hips"` 回退名 | "." / "hips" | 相对根与空/`.`/`..` basename 的缺省段名 | 结构性不适用（词法回退字面量，不进科学语义）；登记为**合同零提及**：publish.h:75 只写 `<parent>/.<basename>.hips_staging.tmp`，未规定 basename 为空时的回退名 | 检索式 `git grep -n "hips_staging\|basename" -- lib/algorithms/drizzle/hips docs` | 影响面：仅 staging 目录命名，不改产物字节 | CONFIRMED |
| :88-90 / :145 / :154 / :159 / :169 / :172 / :227-229 | `n<=0`、`n>=cap`、`cap==0`、`remove_tree(...,0)`、`EEXIST`、`n+1`、`PROMOTE_OK/CROSS_DEVICE/STATE` | 0/1/EEXIST/0-2 | 参数校验、NUL 拷贝长度、初始深度、独占建目录冲突、枚举对齐 | 结构性不适用（边界比较常数与 OS/枚举对齐值） | 状态码映射逐条对 publish.h:75-105 与 :223-225 注释 | 与 publish.h:130-146 的 `_Static_assert` 冻结域一致（0..15/70/71），未见越域返回码 | CONFIRMED |

（合并说明：全文件无浮点常数、无容差、无阈值系数；`1024/64/0/1/2` 之外仅剩 `+1`、`-1` 类字符串算术，已在 :69-70/:172 行合并列出全部位点。）

### findings

### CR-09-1 · 发布流水线被称作"校验"的那一步只做自增计数，且唯一生产形态调用点把两个出参都传 NULL——IO-003 §4 点名的 tile 内容校验在 C 侧发布链上零执行点
- 轴：D（主）＋ C
- 位点：`aio_publish.cpp:111` `if (n_files) (*n_files)++;`（该 TU 内"校验"的全部实现）；文件头 `:4` "流水线: staging → 校验(计数) → fsync 树 → 原子 promote(rename) — 对齐 IO_003_ATOMIC_OUTPUT_PUBLISH §4"；`publish.h:20` "3. tree_fsync 递归 fsync stage 树 (文件+目录, 后序) + 文件计数校验"；调用侧 `module_entry.cpp:994` `publish_rc = aio_publish_tree_fsync_v1(stage_path, NULL, NULL);`
- 上位依据：`docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md:21` "临时写（run 私有 stage）→ 关闭/fsync → fitsverify（结构 + DATASUM）→ sha256 → …"；同文 `:103-104` "**`.fits` 科学平面 tile（`NpixN.fits`）必须通过结构 + DATASUM 校验**"；`:134` manifest 须记 `"fitsverify": {"performed": true, "checksum": "datasum", "tile_count": 1}`；`:147` "fitsverify 记录发布时已执行校验（performed/tile_count）——机器证据"。条文面零提及"仅计数即算校验"（检索式 `git grep -n "校验" -- docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md`）。
- 现状：`aio_publish_tree_fsync_v1` 的签名只允许**回填**计数与字节（`uint64_t* n_files_out, uint64_t* total_bytes_out`），不接受期望值 ⇒ 本 TU 内不存在任何可判定的比较；`module_entry.cpp:994` 两个出参均传 NULL，连回填都不取；全仓 `git grep -n "fitsverify\|fits_verify\|datasum" -- lib/algorithms/drizzle/hips lib/infrastructure/aio/src` 只命中**写入侧**（`aio_hips_writer.cpp:297-303` 写 DATASUM 卡）与 reader 侧（`aio_hips_reader.cpp:614-633`），promote 之前无回读校验点。
- 差在哪：把"数出多少个文件"写成"校验"并据此自称"对齐 IO-003 §4"，而 §4 的校验是逐 tile 的结构+DATASUM 判定；缺期望值入参使该步在本层永远不可能成立。
- 后果：staging 树里 tile 半写/截断（cfitsio 缓冲未落盘之外的写坏、tile 数与 `rows.n_tiles` 不符、方差平面整体缺失）时 `promote` 照样成功，`out_dir` 出现"看起来完整"的 HiPS 产品；对外主张 `publish.h:22-23`"全有或全无"与 IO-003 的"发布前已校验"两项同时失真。不等式方向：这是**少一道门**（无判据可红）⇒ 偏绿，不是偏红。
- 定级：S2 —— 缺运行时触发路径：本 TU 的唯一调用者是 `module_entry.cpp` 的 `astrocs_module_query_v1` 事务面，而生产导出末端是 `orchestrator.cpp:3310`（注释原文"Phase1: 正式末端 hp_drizzle_run_hips 直写 HiPS"）；见 CR-09-2。
- 怎么算修好：① 给 `aio_publish_tree_fsync_v1` 增期望入参（`expected_files`/`expected_bytes`）或在调用侧由 `rows.n_tiles × 平面数` 算出期望后比对，不等即不 promote；② 若要保留"对齐 IO-003 §4"字样，promote 前必须有逐 tile DATASUM 回读并把 `{performed, tile_count}` 写进发布 manifest（IO-003:134 的键名已定）；③ 同批必须改 `publish.h:20` 与本文件 `:4` 的措辞与 `eng/ci` 里引用该文件的接线断言，否则"补上校验反而判红"（现有 `eng/tests/unit/p1_hips/publish_atomic_test.c:382-388` 只钉状态码，不钉计数值）。
- 置信：CONFIRMED（签名、调用点、条文三处逐字核过；未实跑）

### CR-09-2 · 文件头自称 DISP-HIPS-001/004 已"模块事务面收口"，但实际导出末端的 sink 直写 out_dir、从不触碰这四个原语
- 轴：C（主）＋ D
- 位点：`aio_publish.cpp:5-6` "失败/取消 → stage_discard → 目标根无 partial (DISP-HIPS-001/004 模块事务面收口; 生产 writer 零改动)"
- 上位依据：`lib/algorithms/drizzle/healpix_drizzle/astro_sphere_sink.cpp:111` `AioHipsProductSet* ps = aio_hips_product_begin(` —— 检索式 `git grep -n "aio_hips_\|aio_publish_" -- lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp lib/algorithms/drizzle/healpix_drizzle/astro_sphere_sink.cpp`：sink 内 `aio_publish_` **零命中**，只有 `aio_hips_product_begin/…_tile/abort/finalize` 直写族；`eng/ci/prod_wiring_baseline.json:155-158` 冻结 `unreachable:manifest.source_symbols:aio_publish_{stage_create_v1,stage_discard_v1,tree_fsync_v1,promote_v1}`（同文件 `:unreachable:manifest.entrypoint:astrocs_module_query_v1` 说明该不可达部分源于门看不见动态入口，故此条只作辅证）；`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:27`（M2b-C-01，状态 OPEN，原文"HiPS 生产写出仍为直写（无 staging/rename/fsync/COMPLETE）"）。
- 现状：`aio_publish_*` 的调用集 = {`module_entry.cpp:866/900/994/999/1006/1009`, `eng/tests/unit/p1_hips/publish_atomic_test.c`}；生产 `mosaic`/`export` 落盘由 sink 完成，不经 staging。
- 差在哪："收口"在本仓语义里是缺陷已闭的判定词（对照 `CONTROL_PACK_SPEC`/FIX_LEDGER 用词），这里闭的是**旁路**（模块事务面），不是产生产品的那条路。
- 后果：审计与检索按本文件头把"HiPS 无半产品"记为已成立 ⇒ 对外"原子发布 / 不留 partial"主张在产品面不成立；同一 out_dir 重跑与上次残片混合的风险仍未闭（FIX_LEDGER M2b-C-01 原文）。
- 定级：S2 —— 缺陷方向是"生产入口绕过本 TU"，写不出"生产入口 → 本 TU 的错误值"路径；本条的力量在措辞与登记面打架，不在产物字节。
- 怎么算修好：① 把 `:5-6` 与 `hips/README.md`、`STANDARDS_REGISTRY` 的 DISP-HIPS-001/004 行改写为"能力已在库内、生产末端未接线"（正是 FIX_LEDGER M2b-C-01 ③ 的原文要求）；② 或在 sink 末端把 `aio_hips_product_begin` 的目标改为 staging 并接 `tree_fsync + promote`（与 `module_entry.cpp:861-1010` 同型事务）；两案都必须同批改 `prod_wiring_baseline.json:155-158`，否则接上线仍判红。
- 置信：CONFIRMED（调用集与 sink 直读）；对"CLI export 是否只有 run_hips 一条 HiPS 落盘路"我只核到 `orchestrator.cpp:3314`＋sink 零 `aio_publish_`，未穷尽其它 export 后端 ⇒ 该子结论 PARTIAL（缺：其它写 HiPS 的入口枚举）。

### CR-09-3 · staging 路径推导对超长 out_dir 静默截断，合同要求"段路径过深 → IO"，实现给的是 PARAM
- 轴：C
- 位点：`aio_publish.cpp:69-70` `if (plen >= parent_cap) plen = parent_cap ? parent_cap - 1 : 0;` / `if (blen >= base_cap) blen = base_cap ? base_cap - 1 : 0;`
- 上位依据：`publish.h:80` 原文"out_dir NULL/空 → PARAM; 段路径过深 → IO"；`publish.h:54` `AIO_PUBLISH_ERR_PARAM = 1, /* 参数非法 (NULL/空/路径溢出) */`
- 现状：截断本身不报错；`publish_stage_path:90` 用 `snprintf` 返回值判总长溢出后返回 PARAM。逐值推演：parent 截到 1023 ⇒ `"%s/.%s%s"` 结果 ≥1023+2+4+17 > 1024 ⇒ 必被 :90 判 PARAM；base 同理。⇒ **不误发布**（我先按"会写到错目录"假设，复算后撤销该指控）。真实偏差只剩：超长路径时返回 1 而合同说"段路径过深 → IO"(4)。
- 差在哪：两条款（:54 溢出→PARAM、:80 过深→IO）在"路径太长"这一情形上给出不同状态码，实现取了 PARAM；对上层是可区分的错误类。
- 后果：上层若按状态码分类重试（IO 类可重试、PARAM 类是调用方错），超长 out_dir 会被误分类为"参数非法"而不去修配置；产物字节不失真。故降级。
- 定级：S3
- 怎么算修好：在 `publish_stage_path` 里区分"入参本身超 1024"（PARAM）与"合法但超出 staging 缓冲"（IO），或在 publish.h 把两条款合并为单码；两者择一即可，别只改注释。
- 置信：CONFIRMED（对截断→必溢出→PARAM 的算术逐值推演过；未实跑）

观察项（不成条）：`module_entry.cpp:865` `char stage_path[1100];` 与本 TU 的 `PUBLISH_PATH_MAX 1024`、publish.h:79"推荐 1024" 三侧不同值；因函数只在 `< cap` 时写入且不越 1024，无溢出后果，仅说明"1024"未被任何一侧强制。


---

## 文件 4 `lib/algorithms/drizzle/healpix_drizzle/tests/bench_drizzle.cpp`（127 行，整读）

性质：小图 Drizzle 剖析驱动，输出 JSONL（:4 自称按 `DRIZZLE_PROFILE_SPEC`）。构建状态：`tests/CMakeLists.txt:228` 注册为剖析目标。
它是登记面性能锚的生产者：`docs/architecture/execution_inventory.csv:5` 的 Drizzle 行写 "scaled synthetic 256x256 -> 1076 out px -> wall 0.456s (V2 perf anchor)"，
字段形状（`input_pixels/output_pixels/nside/precision/wall_s`）与本文件 :118-120 的 JSONL 一致。
与口径①/②的关系：本文件 **不进入发布面**（只调 `drizzleTiled*`，不调 sink/writer），但它的 `pixfrac=1.0` 恰好落在口径①的"因子恒 1"退化点上（见 CR-09-14）。

### §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :46-48 | `sip.order`、`sip.a[0]`、`sip.b[3]` | 1 / `1e-7` / `-1e-7` | 自称"轻 SIP 畸变 (A11=1e-7)"的夹具系数 | **待确认**：按 `fits_reader.h:10` 的布局注释 `A[i*6+j] ↔ dx^i·dy^j`，`a[0]` 是 **A₀₀（常数偏移）**而非 A₁₁（A₁₁ 应为 `a[7]`），`b[3]` 是 B₀₃（三阶）；而 `wcs_sip.cpp:86-88` 的求值域是 `i≤order, j≤order−i` ⇒ order=1 时只读索引 0/1/6，**`b[3]` 永不参与，`a[7]` 也永不参与** | 无条文（夹具参数本身 §4 排除；**被排除的是夹具取值，不是"注释说的与实际算的不是一件事"**） | 保守方向：视本锚为"无非线性项的几何路径"计时；影响范围＝execution_inventory 的 V2 锚所代表的成本区间，不涉及产品字节 | CONFIRMED（布局＋求值域两处直读复算） |
| :43 | `scale = 6.3/3600.0` | 6.3″/px | 夹具Plate scale | **待确认**：6.3″ 无登记出处（`git grep -n "6.3" -- docs/algorithms docs/science lib/algorithms/drizzle` 只见本文件与 bench_write 互抄），而同目录 `representative_probe.cpp:5` 用 300″/px ⇒ 同模块基准的角尺度两侧不同 | 保守方向：锚的代表性以**最细**夹具（0.1″级候选多、面积交叠次数高）为下界来读；影响范围＝profile 选型输入的置信区间 | PARTIAL（未查 DRIZZLE_PROFILE_SPEC 是否规定夹具尺度；该文档我未在本批定位） |
| :63 | `cfg.pixfrac` | 1.0 | 基准的像素收缩因子 | **待确认**：负责人已裁出厂 pixfrac=0.8（记忆 `project-drizzle-pixfrac-default`；本批文件 5 :37 即取 0.8）⇒ 两基准异值；且 `pixfrac=1.0 ⇒ k≡1.0`（文件 3 :40-41）⇒ **本基准不可能触到归一/守恒路径** | DRIZZLE.md:112-120（守恒不变量"对全部 pixfrac∈(0,1] 成立"） | 保守方向：以 0.8 档补测；影响范围＝性能锚＋归一路径的基准覆盖空白 | CONFIRMED |
| :89-90 | `min_area/min_flux` 初值 | `1e300` | 极值哨兵 | **待确认**：无叶被触时 :124 把哨兵原样当测量值打印（`min_area=1e300`），无"空数据"标记 | — | 保守方向：初值应取 `NaN` 或另置 `n_leaf==0` 显式输出；影响范围＝仅基准日志 | CONFIRMED |
| :118-123 | `"status":"%s"` ← `ok ? "PASS":"FAIL"` | — | JSONL 的 PASS 位 | 结构性不适用（退出状态编码）＋如实说明：**PASS 只表达"函数返回 true"，不含任何守恒/精度判定**（`total_flux` 打印但无期望值比对） | §4（测试期望值差不算缺陷）⇒ 故只登记不指控 | — | CONFIRMED |
| :121 | `size * size`（`int`） | — | input_pixels 计数 | 结构性不适用（计数表达式）；**边界**：`size>46340` 时 int 溢出（:49-50 的 `(size_t)size*size` 是对的，此处漏转） ⇒ 仅影响该字段，夹具规模 4096² 内安全 | — | CONFIRMED |
| :39-40/:25-26(文件5) | `crval` | 272.886595 / −23.254083 | 夹具天区指向 | 结构性不适用（§4 夹具内容）；两基准同值 ⇒ 互可比 | 登记面未说明取自哪块真实天区（不影响产品） | — | CONFIRMED |
| :41-42 | `crpix = size/2.0 + 0.5` | 0.5 | FITS 1-based 像素中心换算 | **结构性不适用**：定义常数换算，`crpix=(N+1)/2` 为 1-based 中心，代码按式算 | FITS/WCS 惯例（Calabretta & Greisen 2002） | — | CONFIRMED |
| :37-38 | `strncpy(..., 15)` | 15 | `ctype1[16]` 的 n−1 | 结构性不适用（缓冲区尺寸−1） | `fits_reader.h:25` `char ctype1[16]` | — | CONFIRMED |
| :53 | `1000.0f + 0.01f·x + 0.005f·y` | 1000/0.01/0.005 | 常量通量＋轻梯度夹具 | 结构性不适用（§4 夹具内容；梯度幅度不进任何判据） | — | — | CONFIRMED |
| :19-21/:26/:65 | `argc < 4`、`argc >= 5` | 4 / 5 | 命令行必填/可选参数基数 | 结构性不适用（参数基数） | :20 usage 文本 | — | CONFIRMED |
| :28 | `putenv(const_cast<char*>(env.c_str()))` | — | 设 `OMP_NUM_THREADS` | 非数值；缺陷见 CR-09-13 | — | — | CONFIRMED |
| :64/:75/:91 | `prec==1` 双轨（fp32/fp64） | 0 / 1 | 精度档枚举 | 结构性不适用（枚举基数） | `drizzle_engine.h:62-64` FP32/FP64 双轨定义 | — | CONFIRMED |
| :124-125 | `%.6g` 等 | — | 日志格式 | 结构性不适用 | — | — | CONFIRMED |

### CR-09-12 · 夹具注释自称的 SIP 畸变从未被求值：`a[0]` 是 A₀₀ 常数偏移而非 A₁₁，`b[3]` 在 `order=1` 下根本不进多项式
- 轴：C（主）＋ R
- 位点：`bench_drizzle.cpp:46-48` `img.wcs.sip.order = 1;  // 轻 SIP 畸变 (A11=1e-7)` / `img.wcs.sip.a[0] = 1e-7;` / `img.wcs.sip.b[3] = -1e-7;`
- 上位依据：`lib/algorithms/drizzle/healpix_drizzle/fits_reader.h:10` "// SIP 系数 (最多 4 阶, 36 个系数, **A[i*6+j] 对应 dx^i*dy^j**)"；`wcs_sip.cpp:79-93` `for (int i = 0; i <= order; i++) for (int j = 0; j <= order - i; j++) result += coeffs[i*6+j]*pdx*pdy;`
- 现状：order=1 ⇒ 被读的索引只有 `0,1,6`（即 A₀₀、A₀₁、A₁₀）；`a[0]` 提供的是**与像素坐标无关的常数项**（对 RA 加 1e-7 度 ≈ 0.36 mas 偏移），`b[3]`（B₀₃）永不被读；A₁₁（应为 `a[7]`）同样永不被读。
- 差在哪：注释把"设了两个系数"说成"有畸变"，实际既不是它自称的那个系数项，也没有任何非线性项参与；畸变路径（dx/dy 的高次项）在本基准里是零。
- 后果：`execution_inventory.csv:5` 登记的 V2 性能锚是在**无 SIP 非线性**的几何路径上测的，而生产帧走真实 FITS 头的 SIP（`orchestrator.cpp:4431-4434` 有 SIP 阶数处理与 `SNR_SIP_MAX_ORDER` 上限）；用该锚做 profile/线程选型会系统性低估含 SIP 的成本。
- 定级：S3（基准文件，不改产品字节；影响的是被登记的锚的解释范围）
- 怎么算修好：① 注释改述实际生效项，或按布局把 A₁₁ 写成 `a[7]`／B₁₁ 写成 `b[7]` 并把 `order` 提到 ≥2；② 若要作为锚对外引用，在 JSONL 里加 `"sip_order"`/`"sip_terms_active"` 字段，让登记面能看出锚覆盖的是哪条几何路径。
- 置信：CONFIRMED（布局注释＋求值上界逐行复算）

### CR-09-13 · `putenv` 持有已析构 `std::string` 的指针：局部变量离开作用域后环境块指向已释放内存（UB），且线程数实际由 `cfg.threads` 决定
- 轴：R
- 位点：`bench_drizzle.cpp:26-29`
  ```cpp
  if (argc >= 5) {
      std::string env = std::string("OMP_NUM_THREADS=") + argv[4];
      putenv(const_cast<char*>(env.c_str()));
  }
  ```
- 上位依据：条文面零提及（这是 C 运行时契约，不是本仓规范条文）：`putenv(3)` 要求传入字符串由调用方**长期持有**（glibc 不复制，且不得 free/析构）。检索式：`git grep -n "putenv" -- lib` → 本仓另有 `lib/algorithms/coverage/tests/gpu_profile_ab.cpp:33` 用 `setenv`（复制语义，正确），即同仓两种写法并存。
- 现状：`env` 在 `}` 处析构，`environ` 内留下的指针悬垂；之后任何 `getenv("OMP_NUM_THREADS")`（含 OpenMP 运行时的惰性初始化）读的是已释放堆块，且该块可能已被复用为其它内容。线程数真正生效的通道是 :65 `cfg.threads = atoi(argv[4])`，而 `drizzle_engine.cpp` 用 `config.threads>0 ? config.threads : omp_get_max_threads()`（`execution_inventory.csv:5` 原文），故 :27-28 的 env 设置对结果并无必要。
- 后果：`omp_get_max_threads()` 报告的 `"threads"` 字段（JSONL 里与 wall_s 同行）可能与实际并行度不符，且悬垂读可致随机崩溃——一次基准崩溃会以"锚缺失"的形式回到登记面。
- 定级：S3（仅基准二进制）
- 怎么算修好：删掉 :26-29 整段（线程数已由 `cfg.threads` 承载），或改 `setenv("OMP_NUM_THREADS", argv[4], 1)`；两处剖析驱动（本文件与 `gpu_profile_ab.cpp` 已用 setenv）统一成一种。
- 置信：CONFIRMED（作用域与 putenv 语义直读；未实跑 ASan）

### CR-09-14 · 两个基准对 `pixfrac` 取不同值（1.0 与 0.8），且 1.0 档使创新点④的归一因子恒等于 1，导致基准面对"pixfrac<1 才存在"的那条路径零覆盖
- 轴：C（主）＋ D
- 位点：`bench_drizzle.cpp:63` `cfg.pixfrac = 1.0;` ↔ `bench_write.cpp:37` `cfg.pixfrac = 0.8;`（并在 :62/:65 写进 `grid.pixfrac`/`hmeta.pixfrac`）
- 上位依据：负责人裁决（记忆 `project-drizzle-pixfrac-default`："裁 0.8 但三侧仍在 1.0"）；`docs/science/DRIZZLE.md:112-120` 守恒不变量的适用域写明"对全部 `pixfrac∈(0,1]` 成立"；`astro_sphere_sink.h:40-41` `pixfrac=1 ⇒ sumNorm ≡ sumArea 逐位 ⇒ k ≡ 1.0`
- 现状：仓库内没有任何基准/门在 `pixfrac<1` 下跑通"累加 → sink 折算 → 产品面"整链：本文件（剖析）锁 1.0；`bench_write` 虽用 0.8，但它绕开 sink（手抄 writer 循环，见文件 5 CR-09-15），且其目标容器 HISS 的累加器结构里**根本没有承载 `N_p` 的字段**（`hiss_format.h:350-364` 只有 `sum_flux/sum_area/n_contrib/pixel_area`）。产品级门 `drizzle_pf_sb_gate` 用的是注入 env 的负例而非基准面。
- 差在哪：口径①的科学内容只在 `pixfrac<1` 时才非平凡（`k=pixfrac²`），而基准与剖析面全在 `k≡1` 的退化点上。
- 后果：任何"性能随 pixfrac 变化"或"归一折算在真实负载下开销可控"的对外说法没有可复算的基准支撑；`profile` 选型（AGENTS §6 禁止硬编码线程、要求由 benchmark 生成 profile）在 0.8 档缺数据。
- 定级：S2 —— 不改产品字节，但使一条对外科学主张缺少非退化证据面；缺"产品错值"路径故不升 S1。
- 怎么算修好：① 本文件把 pixfrac 做成第 5 个实参（默认取裁决值 0.8），锚的 JSONL 增 `"pixfrac"` 字段；② `tests/CMakeLists.txt` 的剖析注册里补 0.8 档一条（含 sink 的完整链，即 `hips_profile` 两档都测）；③ 同批把 `execution_inventory.csv:5` 的锚值按新档重测并更新（不更新＝登记面继续指向退化档）。
- 置信：CONFIRMED（两侧取值、HISS 结构体字段、sink 的 k≡1 推导均直读）



---

## 文件 5 `lib/algorithms/drizzle/healpix_drizzle/tests/bench_write.cpp`（104 行，整读）

性质：等规模 HISS 写入剖析（4096² @ NSIDE=65536）。构建状态：`tests/CMakeLists.txt:229` 注册为剖析目标；回读对拍件是同目录 `verify_bench.cpp:15`（默认读同一 `run/temp/bench_write.hiss`）；`lib/infrastructure/aio/memory.md:222` 把 "bench_write 4096²" 当 aio 侧改动的验证证据引用。
与口径①②的关系：本文件**结构上碰不到口径①**——HISS 的叶累加器没有承载归一分母的字段（`hiss_format.h:350-364` 只有 `sum_flux/sum_area/n_contrib/pixel_area`），而 :86-88 只回填前三项；口径② 无落点（不调 `compute_overlap_area_g`，面积来自上游累加器）。

### §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :59 | `A_p = 4.0*3.141592653589793/(12.0*nside*nside)` | 同文件 2 :331 的量 | HEALPix 叶像素面积（sr），喂给 `acc.pixel_area` | **公式导出**：`4π/(12·nside²)`（等面积像素定义式），代码按式算；`hiss_format.h:361-363` 要求"调用方需在 finalize_support 前设置为 hp.pixel_area，默认 1.0 会退化为 sum_area 直接作为 S"⇒ 本处正确赋值 | 同左 | 与 `astro_sphere_sink.cpp:331` 是同一公式的**两份手写副本**，π 字面量精度还不同（本处 16 位 / sink 20 位，转成 double 后同值）⇒ 见观察项 ① | CONFIRMED |
| :37、:62、:65 | `pixfrac` | 0.8（三处字面量） | 基准收缩因子＋写进 .hiss 的两处元数据 | 0.8＝**待确认**（与负责人裁决同值，但本文件无出处锚；三处字面量彼此不引用，改一漏二即自相矛盾） | 记忆 `project-drizzle-pixfrac-default`（裁 0.8）；`DRIZZLE.md:44` `A_drop = pixfrac²·A_pixel` | 方向：与文件 4 的 1.0 档构成两基准异值 ⇒ CR-09-14 | CONFIRMED |
| :19 | `nside` | 65536（=2^16） | 基准叶级 NSIDE | 结构性不适用（夹具规模选择，非科学阈值） | 与 :1 标题"等规模"自洽 | 与文件 4 的 256×256/nside 档不同规模 ⇒ 两份数字不可互推（:1 的"等规模"只指帧规模） | CONFIRMED |
| :18 | `size` 默认 | 4096 | 缺省帧边长 | 结构性不适用（CLI 默认值） | :1 标题 | — | CONFIRMED |
| :38 | `cfg.threads` | 16 | 基准线程数 | 结构性不适用（剖析自订参数，§4 排除）；**但**：AGENTS §6 要求并行度由 benchmark 生成的 profile 决定，本处把 16 写进被测夹具，测出的 add_tile 成本与 profile 无关 | — | 方向：仅作单点计时读，不外推线程标度 | CONFIRMED |
| :57-58 | `n_leaf_per_tile = 1u<<(2*depth)`、`shift = 2*(int)depth` | 4^depth / 未使用 | 每 tile 叶数、（死变量） | 结构性不适用（位移量）；`depth = compute_tile_depth(65536) = min(9, 16−4) = 9` ⇒ `1u<<18 = 262144` 与文件 2 :104 的 `512*512` 同值（我复算）；**`shift` 全文未使用** | `hiss_common.cpp:59-62` | — | CONFIRMED |
| :50-51 | `meta.filter="R"`、`meta.exposure_s=180.0` | R / 180.0 | 构造了 `DrizzleMeta` | 结构性不适用（夹具值）；**`meta` 之后从未被读**（写路径手抄，见 CR-09-15），与 :67-68 的 `hmeta.filter/"R"`、`hmeta.exptime=180.0` 各写一遍 | — | — | CONFIRMED |
| :61-62/:64-66 | `ordering=1`、`radesys=0`、`photappl=1` | 1 / 0 / 1 | .hiss 网格与元数据枚举 | 结构性不适用（枚举值，**且取值正确**：`hiss_format.h:51` `1=NESTED`、`:52` `0=ICRS`、`:301` `photappl 是否已应用`，与 `cfg.nested=true`、`cfg.photometry_applied_upstream=true`（:39）一致） | 同左 | — | CONFIRMED |
| :52 | `out = "run/temp/bench_write.hiss"` | — | 输出路径 | 非数值；硬编码相对路径且**不建目录**，`writer.open` 的 `wr` 只打印不判（:72-74），失败后继续进循环，最终由 :95 `add_tile fail` 返回 1 | — | 方向：失败仍判红（return 1），不 fail-open | CONFIRMED |
| :92 | `hiss::OccupancyMode::FULL` | FULL | 声明 tile 满占用 | 结构性不适用（枚举）；**语义注意**：未触及叶在 :83 `resize` 后保持 `sum_flux/sum_area/n_contrib = 0` 且按 FULL 模式写 ⇒ 空叶与零通量叶不可区分（该缺陷形态已由 FIX_LEDGER M2b-C-05 记在 `hiss_writer.cpp:417` 的 FULL 掩膜上；HISS 已被 `DATA_SEMANTICS.md:395` 判为非生产路径，故本处不作为缺陷指控） | FIX_LEDGER M2b-C-05 | — | CONFIRMED |
| :100-101 | `t_add / tiles.size() * 1000.0` | — | 每 tile 均耗时 | 结构性不适用（换算 1000）；`tiles.empty()` 时**除零**得 inf/nan 且退出码仍为 0（`printf` 后 :103 直接 return 0） | — | 方向：空产物时打印 inf 却 return 0 ⇒ 基准把"没活干"报成"成功"，属偏绿；影响范围仅该日志行 | CONFIRMED |
| :23-27/:32 | `strncpy 15`、`crval`、`crpix+0.5`、`6.3/3600`、`1000.0f+0.01f·x+0.005f·y` | 同文件 4 | 与文件 4 同一套夹具 | 结构性不适用（§4 夹具内容；`0.5`＝1-based 换算定义常数） | 文件 4 对应行 | 两文件的 6.3″/crval 逐字相同 ⇒ 跨基准可比的是几何而非精度档 | CONFIRMED |
| :12-15 | `now_s()` 用 `steady_clock` | — | 计时源 | 结构性不适用 | — | 单调时钟 ⇒ 不受系统时间调整影响（无缺陷） | CONFIRMED |

### CR-09-15 · 本基准点名"替代 `writeHisTilesT`"却用手抄循环计时，被测量不是被点名函数；且 `aio/memory.md:222` 把这份数字当 aio 侧改动的验证证据引用
- 轴：R（主）＋ C
- 位点：`bench_write.cpp:54` `// 手动 writer 循环 + 分段计时 (替代 writeHisTilesT)`；实环 :76-96（自建 `HissWriter` + `acc.pixels.resize` + `add_tile`）；死值 `:50-51` 的 `meta`（构造后未使用）、`:58` 的 `shift`（声明后未使用）
- 上位依据：`drizzle_engine.cpp` 的 `writeHisTiles` 族才是该容器写出路径（`drizzle_engine.cpp:1280`/`:2544` 处 `sum_area = ...sumArea` 的字段搬运即其内部实现）；`tests/CMakeLists.txt:216` 把本文件归类为"剖析驱动"；AGENTS §9"对'看起来通过'的结果保持怀疑"与本批前言 §2 的 R 轴（"门只看夹具不看产品"同族）。
- 现状：手抄循环跳过了 `writeHisTilesT` 的分组/寻址/校验部分（它直接吃 `tiles` 与 `tile.parent_ipix`），因此 :100-102 报的 `acc 构造/add_tile/finalize` 三项之和不等于被点名函数的成本；`meta`/`shift` 两个死值说明循环是从别处剪贴而来。
- 差在哪：文件名与注释宣称的是"HISS 写入测试"，实际测的是"我自己写的这个循环"；被引用为验证证据时，等价性并不成立。
- 后果：`lib/infrastructure/aio/memory.md:222`（"verify_bench 同文件 0.697s (日志 870→5 行), bench_write 4096²"）作为 HISS 写路径改动的性能证据，其测量对象与生产函数不符 ⇒ 登记面证据资格不足。产品字节不受影响（HISS 非生产路径）。
- 定级：S3（基准文件＋非生产容器；缺产品触发路径）
- 怎么算修好：① 改为直接调 `writeHisTilesT`（或它的公开入口）计时，把手抄循环降级为"仅分段计时"的补充；② 删 `meta`/`shift` 死值；③ `tiles.empty()` 时显式报错并非 0 退出；④ `aio/memory.md:222` 那句改成"手抄循环计时"或换成真函数的数字。
- 置信：CONFIRMED（死值与循环体直读；`writeHisTilesT` 的实际内容我只核到字段搬运两处行号，未整读该函数 ⇒ 该子项 PARTIAL）

观察项（不成条）：① `A_p` 与 π 字面量在本文件与 `astro_sphere_sink.cpp:331` 各写一份（同式不同精度写法），`6.3″`、`crval` 在两个基准里各写一份——按"口径唯一"应收敛为单一常量，但两份数值经我复算等值，不构成错值；
② HISS 累加器无 `sum_norm` 字段 ⇒ 凡以 `.hiss` 为对面对象的比较（含 `hp_drizzle_run` 的 `legacy_hiss_compare`）在 `pixfrac<1` 下比的是两套不同口径。HISS 已由 `DATA_SEMANTICS.md:395` 显式判为非生产路径，按前言 §4 不作缺陷指控，只登记为 CR-09-14 所述覆盖空白的另一半成因。

---

## 文件 6 `lib/algorithms/drizzle/healpix_drizzle/tests/candidate_oracle_test.cpp`（263 行，整读）

性质：TEST-DRZ-CAND-001「快速候选零漏选 Oracle 矩阵」。登记面身份：`docs/traceability/TRACEABILITY_MATRIX.csv:6`（drizzle 行 TEST 层锚，原文"候选零漏选 9003 例"）与 `docs/architecture/execution_inventory.csv:5`（Drizzle 生产行的测试证据列 "TEST-DRZ-CAND-001 (candidate_oracle_test.cpp:13)"）。
这是创新点④"平面→球面面积交叠分配"的**候选完整性**证据面：它判的是"哪些像素该进候选集"，不是"交叠面积算得对不对"。

与口径②的关系（正面回答）：**相悖于口径②所要求的判别力**。口径②说"叶用四角大圆弦多边形表示"是逐片面积的误差地板；本文件的"真值"却由同一个弦多边形例程 `compute_overlap_area_g` 的 `>0` 输出来定义（:6-7/:61/:75），因此该例程在真重合时给出 0（漏）时，真值集合本身也漏，`false_negatives==0` 照样成立。它**在构造上不可能发现口径②那类误差**。

### §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :61、:75 | 真值判据 `compute_overlap_area_g(...) > 0.0` | 0.0 | "该像素是否真与 drop 相交"的定义 | **待确认**：这不是被测例程的独立替代，而是**同一个例程**；判据 `>0` 无任何条文规定它可当 ground truth（`docs/algorithms/DRIZZLE_GEOMETRY.md` 的候选/精度条款我未在此读到，检索式 `git grep -n "零漏选\|false_negative\|candidate" -- docs/algorithms docs/science`） | — | 保守方向：真值应取独立几何（如 `healpix_cxx`/解析球面多边形面积）或至少取"解析包含判据"（drop 外接圆与像素内切圆）；影响范围＝TEST-DRZ-CAND-001 的证据资格，不改产品字节 | CONFIRMED（判据与被测函数同一，直读） |
| :90 | `nside <= 32 ? 穷举 : 保守参考` | 32 | 穷举 Oracle 的规模上界（超出即换成 `query_candidate_pixels` 的 buffer-3.0 超集） | **待确认**：实现选择的数值，但决定证据强度分档——穷举档只覆盖 `nsides[]` 里的 16/32，即矩阵 8880 例的一半 | :7（"小 NSIDE 全像素穷举"）、:147-148（"64/128: 保守参考"） | 保守方向：按"4440/9003＝49.3% 例具独立真值"读登记面的"9003 例"；影响范围＝对外证据表述，见 CR-09-16 | CONFIRMED（例数我独立复算：111 位置×5 尺度×4 pixfrac×4 NSIDE=8880，其中 nside∈{16,32}=4440；另 48+75=123 例全为非穷举档 ⇒ 合计 9003 ✓ 与登记一致） |
| :139-140 | 默认证据路径 `run/temp/precise_hardening/candidate_matrix.jsonl` | — | JSONL 证据落点 | **待确认**：`fopen` 失败只打印 `[WARN]`（:143），退出码不含该信息 ⇒ 证据文件可为 0 字节/不存在而矩阵仍报 PASS；且负责人"三留一不留"口径下 `run/` 产物会被回收 ⇒ 登记为 TEST-DRZ-CAND-001 证据的落点本身不稳定 | 记忆 `project-evidence-retention-doctrine`＋`AUDIT-401 结论`（证据件落 `run/` 属合案缺陷） | 保守方向：证据路径缺失应判失败；影响范围＝证据可核对性 | CONFIRMED，见 CR-09-18 |
| :102-105 | `fn`（false negatives）判定 | 0 | 唯一被断言的量 | 结构性不适用（计数比较，0 是"零漏选"的定义） | :9 断言声明 | **不等式方向**（按前言必给）：`truth=∅ ⇒ fn=0 ⇒ 通过` ⇒ 该情形偏绿，见 CR-09-17 | CONFIRMED |
| :109-112/:118 | `fp`（false positives）只写 JSONL 不断言 | — | 候选集多余度 | 结构性不适用（超集是设计目标，多余度不是判据） | :9 声明只断言 `false_negatives == 0` | 无（不作指控） | CONFIRMED |
| :144-146 | `scales[]`=0.1/1/10/60/3600″、`pixfracs[]`=0.1/0.25/0.5/1.0、`nsides[]`=16/32/64/128 | — | 矩阵维度 | 结构性不适用（§4：实验单元内自订参数；`3600.0`＝1° 的单位换算定义常数） | :11-12 场景声明 | — | CONFIRMED |
| :212-218/:224-241 | `nside_hi ∈ {1024, 65536}`、`4194304`（=2^22） | 1024/65536/2^22 | 高档覆盖的 NSIDE | 2^22＝**待确认**：注释称"生产上限"，但写侧合法域到 `2^29`（`aio_hips_writer.cpp:1199-1200` "512<=nside<=2^29"）⇒ "生产上限"要么指 HISS 侧要么过时；**`hp_res≈0.05"` 我复算正确**：`sqrt(4π/(12·(2^22)²))`＝2.536e-3 arcsec² ⇒ 边长 0.0504″ ✓ | 保守方向：按"2^22 是抽样档之一，非上限"读；影响范围＝矩阵覆盖面的对外表述 | CONFIRMED（数值复算）/ PARTIAL（"生产上限"所指载体未证） |
| :41 | `half_deg = 0.5*scale/3600*pixfrac` | 0.5、3600 | drop 半边长（度） | 结构性不适用（0.5＝半宽定义、3600＝度/角秒换算） | — | 见 CR-09-17 第二支：极位置下 `dec_c + half_deg > 90°` | CONFIRMED |
| :157/:168/:177/:191 | `ip < 12`、`per_face8 = 64`、`{0,7}`、边中点 `{0,3},{7,4},{3,0},{4,7}` | 12/64/7/3/4 | base face 数、nside=8 的面内像素数、边界索引 | 结构性不适用（枚举基数与位移量：12＝HEALPix base cells；64＝8²；7＝8−1；3/4＝面内两侧中点） | `healpix_core.h` 的 Npix=12·nside² | `spread3` 与 `real_ip = f*64 + (spread(ix) | spread(iy)<<1)` 的位交织与 nested 面内编码一致（我按 nested 定义核对） | CONFIRMED |
| :160/:184/:232 | `90.0 - th*(180/PI)`、`ph*(180/PI)` | 90、180 | 极角→纬度换算 | 结构性不适用（单位换算定义常数，§4 明列排除） | — | — | CONFIRMED |
| :27 | `PI_` | 3.14159265358979323846 | π 字面量（本批第三份） | 结构性不适用（数学常数）；与 `astro_sphere_sink.cpp:331`、`bench_write.cpp:59` 共三份精度/写法不一 | — | 见文件 5 观察项 ① | CONFIRMED |
| :36 / :29 | `D2R`、`g_fallback_cases` | — | 声明后全文未使用 | 结构性不适用（死常量/死计数器）；`used_fallback` 只进 JSONL（:121）不断言 ⇒ "边界回退是否被走到"无判据 | — | — | CONFIRMED |
| :124/:162/:229 | `msg[256]`、`name[32]`、进度 `%400` | — | 缓冲与打印间隔 | 结构性不适用（缓冲区尺寸/循环上界，§4 排除） | — | `snprintf` 全部带 cap，无溢出 | CONFIRMED |
| :212-214 | 高 NSIDE 档只对含 `_center` 的位置抽样 | — | 覆盖面裁剪 | 结构性不适用（抽样方案）；**后果登记**：极区与 RA 跨 0 两类病态位置在高 NSIDE 档**不测**（`ra_cross0`/`north_pole`/`south_pole` 只在 2^22 的 3 例里以 1″/pf1.0 出现，:238-240） | — | — | CONFIRMED |

### CR-09-16 · 登记为"候选零漏选 Oracle（9003 例）"的证据，其真值由被审的同一个面积例程定义；且只有 4440 例（49.3%）走与候选查询无关的穷举档
- 轴：S（主）＋ R
- 位点：`:61` `if (spherical::compute_overlap_area_g(g, hp, (uint64_t)ip) > 0.0) truth.push_back(...)`；`:74-76`（`oracle_high_nside`：先在 `query_candidate_pixels` 的超集里，再用同一 `compute_overlap_area_g` 判 `>0`）；自述见 `:6-7` "Oracle: 小 NSIDE 全像素穷举 — 对每个 HEALPix 像素计算 compute_overlap_area_g > 0 即为真集合 (**false negatives 定义基准**)"
- 上位依据：`docs/traceability/TRACEABILITY_MATRIX.csv:6` drizzle 行"候选零漏选 9003 例"；`docs/architecture/execution_inventory.csv:5` Drizzle 生产行的 TEST 列 = TEST-DRZ-CAND-001；AGENTS §5"实验单元自包含……结论经独立审稿"与前言 §0/§2 的 S 轴（凭什么）；口径②（四角大圆弦多边形＝逐片面积误差地板）。
- 现状：三层削弱叠加——① 真值判据与被测面积函数同源（弦多边形若漏，真值同漏）；② `nside>32` 时真值宇宙又是另一条候选查询（queryDisc buffer 3.0）的子集，"fast ⊇ truth"退化为"fast ⊇ slow∩area>0"，两条查询同族；③ 高 NSIDE 档的 123 例全部落在②这一档。真正与被测查询无关的证据只有 nside∈{16,32} 的 4440 例。
- 差在哪：把它当作创新点④（面积交叠分配）的证据来登记，超出了它的判别范围——它是**候选查询的一致性回归**，不是零漏选的绝对证明，更不是面积正确性证明。
- 后果：对外"候选零漏选 9003 例"这一条的强度被夸大一倍，且给口径②的误差地板留出一个"看起来已被测过"的盲区：面积例程在真相交像素上返回 0（弦多边形内缩所致）时，本测试全绿。
- 定级：S2（证据资格/对外主张面；不改产品字节 ⇒ 不升 S1）
- 怎么算修好：① 把 `nside<=32` 的穷举档扩到能覆盖生产叶级（或改用解析判据：drop 外接圆半径 vs 像素内切圆，独立于 `compute_overlap_area_g`）；② 断言集里增加一条独立面积上界核对（例如对 truth 中每像素的 `overlap` 与 `A_pixel` 比 ≤1、与外接圆面积比 ≥ 弦内接下界），使"地板"可判；③ 登记面（TRACEABILITY/execution_inventory）把该测试改称"候选查询与穷举档的一致性回归（9003 例，其中 4440 例穷举真值）"，不得再写作无定语的"零漏选 Oracle"。
- 置信：CONFIRMED（同源判据＋分档计数均逐行复算；"弦多边形内缩 ⇒ 真漏"这一侧我只在文件 2/3 的条文层面确认其存在性，未实测某例 ⇒ 该子项 PARTIAL，缺：对若干例做独立面积交叉核对）

### CR-09-17 · 唯一断言在 `truth` 为空时恒真（无守卫），且极位置＋大尺度构造出越过极点的退化 drop——两类静默通过
- 轴：R
- 位点：`:102-105` `int fn = 0; for (uint64_t ip : truth) { if (!std::binary_search(...)) fn++; }` 之后 `:125-133` `if (fn > 0) CHECK(false, msg); else g_pass++;`（**无 `truth.empty()` 守卫**）；构造侧 `:39-52` `make_drop` 直接以 `{ra_c±half_deg, dec_c±half_deg}` 取四角，`:207-209` `positions.push_back({"north_pole", {0.0, 89.99}})`
- 上位依据：AGENTS §5"恒真门没有证据资格"；前言 §2 的"判'误绿'先定不等式方向"
- 现状（不等式方向已定）：`truth` 的元素数只可能让 `fn` 变大或为 0；`truth=∅ ⇒ fn=0 ⇒ g_pass++` ⇒ **空真值恒通过，方向偏绿**。可达路径我数过：`north_pole (dec=89.99)` 与 `south_pole` 配 `scale=3600″, pixfrac∈{0.1,0.25,0.5,1.0}` ⇒ `half_deg = 0.5·1°·pf ≥ 0.05°` ⇒ 顶点 `dec = 90.04…90.49 > 90°`，经纬矩形绕极自交；这类例在矩阵里为 2 位置 × 1 尺度 × 4 pixfrac × 4 NSIDE = **32 例**，全部喂给同一套 drop 构造与同一套判据。
- 后果：极区（创新点④最容易出面积问题的区域）的候选覆盖证据可能建立在"空集合"或退化多边形之上而全绿；对外"南北极场景已覆盖"的登记（:11 场景声明）不成立。
- 定级：S2（测试面；不改产品字节，但它正是产品面所引用的证据）
- 怎么算修好：① `run_case` 开头加 `if (truth.empty()) { CHECK(false, "空真值例"); return; }`（或对每个 (位置,尺度,pixfrac) 断言 `truth.size() ≥ 1`）；② `make_drop` 对 `dec_c ± half_deg` 超界做钳位或改用作切平面四边形（与生产 `spherical_overlap` 的 drop 构造同型），并在越过极点时**报错**而非静默生成；③ 极区例改在小 NSIDE 档也进穷举（现 :238-240 只在 2^22 档以单一尺度出现）。
- 置信：CONFIRMED（`fn` 的符号方向与 32 例的算式我逐值复算；"退化多边形是否使被测例程返回 0"未实跑 ⇒ 该子项 UNPROVEN）

### CR-09-18 · 证据文件写不出来不影响退出码，且 `used_fallback` 只记不判——登记证据可零字节而门全绿
- 轴：R（主）＋ C
- 位点：`:141-143` `g_jsonl = std::fopen(jsonl_path, "w"); if (g_jsonl) printf("  JSONL 证据: %s\n", jsonl_path); else printf("  [WARN] 无法写 JSONL: %s\n", jsonl_path);`；`:262` `return g_fail == 0 ? 0 : 1;`
- 上位依据：AGENTS §9"证据文件为空、检查器静默退化都算未完成"；前言 §2 的 R 轴（"门只看夹具不看产品"同族）；记忆 `project-evidence-retention-doctrine`（运行产物一律不留 ⇒ `run/temp/**` 会被回收）
- 现状：默认路径在 `run/temp/precise_hardening/` 下，目录不存在或产物被回收时 `fopen` 返回 NULL，仅打一行 WARN，矩阵照跑、退出码只看 `g_fail`；同时 :29 的 `g_fallback_cases` 从未自增、:121 只把 `used_fallback` 写进那份可能不存在的 JSONL ⇒ "边界回退路径有没有被真正走到"没有任何断言。
- 差在哪：一个被 TRACEABILITY 引用的测试，其证据载体是可选的；最关键的覆盖面判据（fast 内部路径 vs 边界回退比例）落在无判据的 JSONL 里。
- 后果：`TRACEABILITY_MATRIX.csv:6` 上 TEST-DRZ-CAND-001 的 VERIFIED 可能对应 0 字节的证据文件；"9003 例里有多少走了边界回退"不可核对。
- 定级：S3
- 怎么算修好：① `fopen` 失败即 `return 2`（或以 `CHECK(g_jsonl!=nullptr,...)` 计入 `g_fail`）；② 结尾断言 `g_fallback_cases > 0` 与 `fast_interior 例数 > 0` 两类路径都非空（把 `used_fallback` 累加进 :29 那个现有但空置的计数器即可）；③ 证据落点改到跟踪面（`实验/` 或 `docs/`），符合"三留一不留"里"留报告与可复现代码"的口径。
- 置信：CONFIRMED（退出码路径与两处死值直读；"目录不存在时 fopen 是否失败"未实跑 ⇒ 该子项按 POSIX `fopen(3)` 语义推定）

---

## 本批最高危三条（只挑，不复述）

1. **CR-09-4**（文件 2，唯一 S1）：编排层真正取用的那一档 HiPS 直写不写合同点名的帧级 SNR 两键 ⇒ 创新点②的帧级权重链在产品字节上取不到输入；触发路径逐跳可查（`orchestrator.cpp:3311-3314 → hp_drizzle_hips_api.cpp:67-81 → hp_drizzle_api.cpp:1202-1207 → astro_sphere_sink.cpp:111-262 → module_adapters.cpp:11523`）。
2. **CR-09-5**（文件 2，接线即升 S1）：写侧仍把"组内 F_ref 必须逐帧相等（1e-9）"当 fail-closed 门，与负责人 GAP_AUDIT §9.49 定案 2 及同一 `1e-9` 在消费侧已降级为报告字段的现状相反 ⇒ 多指向/多滤镜拼接正是被这条门拒掉的那类合法数据。
3. **CR-09-16**（文件 6）：登记为"候选零漏选 Oracle（9003 例）"的证据，其真值由被审的同一面积例程自定，且只有 4440 例（49.3%）走与候选查询无关的穷举档 ⇒ 口径②的弦多边形误差地板在这条证据上是结构性盲区。

紧随其后（不占三条名额）：CR-09-1（发布流水线的"校验"步只做自增计数且出参在生产传 NULL）、CR-09-2（"DISP-HIPS-001/004 收口"措辞 vs 生产末端直写）、CR-09-6（两末端 covered_area 两套分母口径 ⇒ 同一输入产品字节随档位变化）。

## 我读不动或需要实跑才能定的（列清单＋该跑什么）

| # | 悬而未决的问题 | 卡在哪 | 该跑什么才能定 |
|---|---|---|---|
| 1 | `write_hips_direct` 用 `AIO_HIPS_PRODUCT_ALL(7)/ALL_V19(31)`，**恒含 SNR 位**；若 `snr_pts` 为空，properties 是否声明了一个磁盘上不存在的 `snr` 子产品 | `aio_hips_writer.cpp` 的 finalize 对 SNR 位的落盘条件不在本批 | 跑一次 profile=0 直写（`hp_drizzle_run_hips`，帧无 SNR 点集），列产品目录 + 读 `properties` 的子产品清单比对 |
| 2 | CLI `export`/`mosaic` 是否只有 `hp_drizzle_run_hips` 一条 HiPS 落盘路（CR-09-4 的"生产入口"依赖这一点） | 我只核到 `orchestrator.cpp:3314` 与 sink 的 `aio_publish_*` 零命中 | 跑 `eng/tools/arch/build_production_execution_inventory.py` 现场计算的生产可达性，或 `acsd export` 后核对产物 properties 里有无 `ASTROCS_FRAME_SNR` |
| 3 | `write_hips_phase1`（profile=1）接线后的真实后果：CR-09-5 的 1e-9 门在真实多组数据上拒掉多少帧 | 现无生产调用者（`TRACEABILITY_MATRIX.csv:6` 记 entrypoint=MISSING） | 按 §9.49 记录的"6 帧跨 t2_m1/t2_m2"场景跑一次 Phase1 全链，数两键是否出现 |
| 4 | 口径②的"弧弦容差被实测超 2–5 个数量级"我**无法从本批证实/证伪**（面积与容差代码在 `spherical_overlap.cpp`/`v6_spherical_overlap.cpp`，不在 CR-09 清单内） | 文件域外 | 由覆盖 `spherical_overlap.cpp` 的批次给逐片面积误差的实测分布；本批只能确认" sink 侧存在钳位/量化来吸收 overshoot" |
| 5 | F&H 2002 `PASP 114, 144 §7.2 式(7)` 的**节号/式号**是否名副其实（`DRIZZLE.md:53` 与本批文件 2 :528 都依赖它） | 需一手文献载体，属 AUD-301 范围 | 核原文 §7 是否存在及其式(7) 内容；载体与页码我已核对一致（PASP 114:144 为 Fruchter & Hook 2002 的正确出处） |
| 6 | `bench_drizzle` 的 6.3″/px 夹具是否 `DRIZZLE_PROFILE_SPEC` 规定值（影响 V2 锚的代表性，CR-09-12/14） | 该 SPEC 文档我未在本批定位到 | 找 `DRIZZLE_PROFILE_SPEC`（`docs/` 下检索 "DRIZZLE_PROFILE_SPEC"）核对夹具规定 |
| 7 | `putenv` 悬垂（CR-09-13）在本平台的实际后果 | 纯静态推断 | `cmake --build` 出 bench 目标后在 ASan 下跑一次（**不属本批禁止范围外，需另行授权**） |
| 8 | 登记锚漂移：`execution_inventory.csv:5` 把 TEST-DRZ-CAND-001 指到 `candidate_oracle_test.cpp:13`，而该 ID 实际在 `:15`（:13 是尺度行） | 只是锚号，无判据影响 | 改登记锚为 `:15` 或用 `path::symbol` 口径 |

## 交付自检

- 清单 6 个文件**全部整读**（Read 全文，非抽样）：`aio_publish.cpp` 235 行、`astro_sphere_sink.cpp` 632 行、`astro_sphere_sink.h` 116 行、`bench_drizzle.cpp` 127 行、`bench_write.cpp` 104 行、`candidate_oracle_test.cpp` 263 行。
- §2.5 逐数值处置表：6 张，共 91 行（文件 1＝12、2＝26、3＝12、4＝14、5＝13、6＝14），每行都选了五档处置之一；无"看起来合理"式空标。
- findings 18 条（CR-09-1..18）：1 条 S1、9 条 S2、8 条 S3；每条含轴/位点/上位依据/现状/差在哪/后果/定级/怎么算修好/置信九字段。判"偏绿/偏红"处均先定不等式方向（CR-09-1 缺门⇒偏绿、CR-09-5/10 拒写或回退⇒两侧方向已标、CR-09-17 `truth=∅⇒fn=0`⇒偏绿）。
- 派单与清单的差异（前言 §5.5）：派单称本批为"创新点④面积交叠分配＋创新点②叠加落地面"，**清单里没有任何面积计算/弧弦容差文件**（它们在 `spherical_overlap.cpp`/`v6_spherical_overlap.cpp`，不在 CR-09）；本批 6 个文件里只有文件 2/3 承载口径①，文件 1/4/5 为 I/O 与剖析面，文件 6 为候选面。以清单为准，已按实况逐文件给处置，未把口径②硬套到无落点的文件上。
- 未复现/未证实的上游结论：口径①（代码侧成立，唯"合同签署状态"仍在登记面缺，见 `module_adapters.cpp:11504` 原文）；口径②（本批无面积代码，只能确认其 overshoot 在 sink/writer 两侧被钳位吸收，见文件 2 处置表 :521-526 行）。

---

## 收工 `git status --porcelain` 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工输出逐字相同：本节点零仓库写入、零构建/测试/门禁执行。


