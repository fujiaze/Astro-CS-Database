# 通读 CR-33｜lib/algorithms/psf 第 2 段（6 文件）

- 被审基线 HEAD：`c8f64e9a`（`git rev-parse --short HEAD` 实测一致）
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 派单：`DISPATCH-CODE-READ-PREAMBLE.md`（§1 只读纪律／§2 四轴／§2.5 逐数值处置表／§3 finding 字段／§4 假阳源／§5 节奏）
- 清单：`独立审计/批次清单/CR-33.txt`（6 个文件，与派单文字一致，无差异）

## 开工 status 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 文件清单与状态

1. `lib/algorithms/psf/include/dynamic_psf.h` —— 已读：是（处置表 20 行，findings 7 条）
2. `lib/algorithms/psf/tests/p1psf/p1psf_center_contract_gate.cpp` —— 已读：是（处置表 19 行，findings 5 条）
3. `lib/algorithms/psf/tests/p1psf/p1psf_centroid_gate.cpp` —— 已读：是（处置表 24 行，findings 4 条）
4. `lib/algorithms/psf/tests/p1psf/p1psf_fixtures.hpp` —— 已读：是（处置表 25 行，findings 3 条）
5. `lib/algorithms/psf/tests/p1psf/p1psf_oracle.hpp` —— 已读：是（处置表 15 行，findings 4 条）
6. `lib/algorithms/psf/tests/p1psf/p1psf_prodpath_check.cpp` —— 已读：是（处置表 16 行，findings 4 条）

合计 findings 27 条（编号 CR-33-1…28，其中 **CR-33-8 作废未用**——原拟的"头文件 params 可 NULL"一条与 CR-33-5 合并，编号保留空位不复用以免打断交叉引用）。

## 1 · `lib/algorithms/psf/include/dynamic_psf.h`（216 行，整读）

### 1.1 §2.5 逐数值处置表

本头是纯声明头（签名＋schema 宏＋码表＋条款注释），文件内**不含任何科学阈值字面量**；
所有科学量（1.230310、0.3、1e-8/200、1.482602218505602、0.5、2.0 等）在
`src/dpsf_psf.cpp`，不属本文件处置面，只作交叉取证。

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| dynamic_psf.h:17-31 | `DPSFFitResult` 成员基数 | 13（1×int＋12×double） | 单星拟合结果结构体的字段集合 | 结构性不适用 | 字段由拟合参数集（B/A/x0/y0/sx/sy/theta）＋派生量（fwhm_x/fwhm_y/mad/flux/eccentricity）穷举，成员数＝集合大小，不承载科学语义；`docs/contracts/PUBLIC_API.md:621`「DPSFFitResult 12 字段（:17-31）」只数 double | — | CONFIRMED |
| :33 | `DPSF_FIT_OK` | 0 | 单星/旧批 API 的"拟合收敛"码 | 结构性不适用 | `docs/algorithms/STAR_PSF_ALGORITHMS.md:186` 冻结 0＝OK；0 同时是批 API 的"调用成功"ABI 约定（:102/:157），二者非同一判据 | — | CONFIRMED |
| :34 | `DPSF_FIT_NO_CONVERGENCE` | 1 | LM 未收敛/验证链一~三拒绝 | 结构性不适用 | §11.2:187 触发锚（现实现 :386-409 三段） | — | CONFIRMED |
| :35 | `DPSF_FIT_INVALID_PARAMS` | 2 | 入参/窗口非法 | **待确认** | §11.2:188；但同一数值 2 在 :119 被赋给语义无关的 `DPSF_PSF_STATUS_RECT_EMPTY` | 保守方向＝把 `DPSF_PSF_STATUS_*` 码域挪出 0..3（如自 16 起编）；影响范围＝任何 `out_status[i] = result.status` 式赋值与其后按名解码的消费方（CR-33-1） | CONFIRMED（码表事实）／UNPROVEN（尚无此类赋值） |
| :36 | `DPSF_FIT_ITERATION_LIMIT` | 3 | max_iter 耗尽但参数可用 | **待确认** | §11.2:189 冻结"仍回填当前最优参数／批接口非 OK→NaN 不计 valid"；同一数值 3 在 :120 被赋给 `DPSF_PSF_STATUS_ALLOC_FAILED`；消费侧 `orchestrator.cpp:2446` 把 3 判为有效，`DATA_SEMANTICS.md:742` 判 PHOTOMETRIC 仅 0 入匹配 | 保守方向＝"3 一律视为不可用"（偏红），并统一三处判据；影响范围＝天测星集与测光星集的差集（CR-33-1） | CONFIRMED |
| :39 | `DPSFFitParams::fitRadius` | 声明无值 | 裁窗半径（像素，半宽） | 结构性不适用（值在实现侧） | 默认 8 在 `dpsf_psf.cpp:842`（f32）与 `:1017`（f64）；生产两侧入口不同（CR-33-5） | — | CONFIRMED |
| :40 | `DPSFFitParams::maxIter` | 声明无值 | LM 最大迭代 | 待确认（**死参数**） | `STAR_PSF_ALGORITHMS.md:201` DISP-PSF-003「maxIter/tolerance 死参数（LM 硬编码 1e-8/200）」已登记；本头 :81/:147/:191 仍把它列为"拟合参数" | 死参数本身已登记，不另计缺陷；未登记部分是编排侧日志背书与兜底值（CR-33-6） | CONFIRMED |
| :41 | `DPSFFitParams::tolerance` | 声明无值 | LM 相对收敛阈 | 待确认（**死参数**） | 同上 DISP-PSF-003；求解实际用 `dpsf_psf.cpp:375` 的 `1e-8` | 同上 | CONFIRMED |
| :69 | "不做 0-65535 clip" 的 `0` / `65535` | uint16 值域端点 | 说明 float32 通道不经无损转换 | 结构性不适用 | 2^16−1 是 dtype 定义常数（派单 §4 明列为单位换算定义常数），非科学量 | — | CONFIRMED |
| :70, :107, :145 | `FLOAT64[N,6]` 的 `6` | 6 | star_det v1 检测块列基数 | 结构性不适用 | 宏 `DPSF_STAR_DET_SCHEMA_V1`（:107）具名；列序见下一行 | — | CONFIRMED |
| :78-79 | 检测块列序 `[0]=x_px [1]=y_px [2]=flux [3]=mag [4]=saturated [5]=has_saturated` | 0..5 索引 | 输入块的列↔语义映射 | 结构性不适用（映射声明），但 `[4]`/`[5]` 不消费与 `[2]`/`[3]` 不消费须与合同同说 | `DATA_SEMANTICS.md:732-733`「饱和列 star_det v1 [4]/[5] 现状不消费（dpsf_psf.cpp:741 仅解包 [0]/[1]）」；实测 `:1049-1051` 确实只取 [0]/[1] | — | CONFIRMED |
| :79, :98 | `saturated(0/1)` / `has_saturated(0/1)` | 0/1 | 布尔标志域 | 结构性不适用 | 布尔编码；剔除决策归 star_detector 侧（`DATA_SEMANTICS.md:733`） | — | CONFIRMED |
| :84, :150 | `大小 = N * 9 * sizeof(double)` 的 `9` | 9 | 布局 B 行距（缓冲尺寸公式） | 结构性不适用 | 消费者行距一致：`module_adapters.cpp:4102` `N_fit * 9`、`:4138` `row*9+7`、生产 `dpsf_psf.cpp:1061` `i*9` | — | CONFIRMED |
| :86-94 | 布局 B 九列语义与单位（B/A ADU、cx/cy/sx/sy/fwhm_x/fwhm_y 像素、theta 弧度） | 9 列序 | 本批必查的列序/单位条款 | **公式导出** | `fwhm_x = 1.230310·sx`（`dpsf_psf.cpp:393-394`）。我独立复算：模型 `I=B+A/(1+Q)^4`、`Q=u²/(2sx²)+v²/(2sy²)`（`dpsf_psf.cpp:97-106,113-115`），半高 ⇒ `Q=2^{1/4}−1=0.1892071` ⇒ `FWHM=2sx·√(2·0.1892071)=1.230310·sx` ✓ 与常量吻合，代码**按式算**（写死于常量但常量本身是导出式的一次求值，等价）。θ 消歧 4 候选（`:411`）与 `sx≥sy` 约定见 `README.md:49` | 若消费侧改用高斯因子 2.354820 换算同一段 sx，差 1.9140×，此模型差已由 `docs/science/STAR_DETECTION.md:170`（DISP-STAR-007）登记为"两列不可跨块比较"——但 PSF 侧消费注释正在做这个被禁换算（CR-33-4） | CONFIRMED（复算过） |
| :95-96 | "成功行按检测下标升序 compact 写入前 n_valid 行；其余行不承诺任何值" | 行语义条款 | 布局 B 的行↔星映射规则 | 结构性不适用（行为条款，无数值） | `DATA_SEMANTICS.md:764-768` 同说并加"禁留 NaN 占位行"；实现 `dpsf_psf.cpp:1028-1040`（全 NaN 预填＋状态默认 FIT_FAILED）、`:1120-1137`（串行升序 compact） | 与模块 README「拟合失败星 9 字段全 NaN」冲突（CR-33-3） | CONFIRMED |
| :97 | `大小 = N * sizeof(int)` | N | out_status 缓冲尺寸 | 结构性不适用 | 与 :114 `INT32[N]` 一致；C `int` 在两正式平台均 32 位 | — | CONFIRMED |
| :102, :157 / :103, :158 | 返回 `0` / `-1` | 0 / −1 | 批 API 调用级返回约定 | 结构性不适用 | ABI 约定；注意 0 与 `DPSF_FIT_OK` 的 0 同名不同判据（:33） | — | CONFIRMED |
| :108 | `DPSF_PSF_PARAMS_SCHEMA`＝`psf_params:FLOAT64[N,9]` | schema 名 | 布局 B 具名 | 结构性不适用 | `DATA_SEMANTICS.md:721`「双 [N,9] 布局并存（15.2 A/B）为冻结事实，两布局各自具名」——两布局同形 `[N,9]` 已被登记为冻结事实，**不计缺陷**；块 A 侧证据 `orchestrator.cpp:2398` `int dims[2]={n_stars,9}` ＋ `:2402` schema 注记 | — | CONFIRMED |
| :114 | `DPSF_PSF_STATUS_SCHEMA`＝`psf_status:INT32[N]` | schema 名 | out_status 具名 | **待确认** | 名字 `psf_status` 与 `star_measurements` 块 `[6]=psf_status`（`DATA_SEMANTICS.md:776`、`orchestrator.cpp:2460` `row[6]=prow[0]`）同名，但后者装的是 `DPSF_FIT_*` 码表（0..3 含 3＝ITERATION_LIMIT），前者码表 3＝ALLOC_FAILED | 保守方向＝列名加前缀区分（如 `fit_status`/`psf_status` 二选一并同批改合同、实现注释与测试断言）；影响范围＝按名解码 2/3 的一切消费方（CR-33-1） | PARTIAL（缺"确有按名解码并据此判 2/3"的消费点） |
| :117-120 | `DPSF_PSF_STATUS_{OK,FIT_FAILED,RECT_EMPTY,ALLOC_FAILED}`＝0/1/2/3 | 4 码 | 逐星状态码表 | 结构性不适用（枚举基数 4） | §11.2:187 与 `DATA_SEMANTICS.md:766` 均按四码冻结；2/3 与 :35-36 数值复用问题单列（CR-33-1） | — | CONFIRMED |

### 1.2 findings

### CR-33-1 · 同一头文件里两套 0..3 状态码表数值复用且 2/3 含义无关，`psf_status` 这一列名同时指两份码表
- 轴：C（主）／R
- 位点：`dynamic_psf.h:35` `#define DPSF_FIT_INVALID_PARAMS  2` 对 `:119` `#define DPSF_PSF_STATUS_RECT_EMPTY      2`；`dynamic_psf.h:36` `#define DPSF_FIT_ITERATION_LIMIT 3` 对 `:120` `#define DPSF_PSF_STATUS_ALLOC_FAILED    3`
- 上位依据：`docs/algorithms/STAR_PSF_ALGORITHMS.md:179`「状态码域 = {0,1,2,3}（dynamic_psf.h:33-36）」、§11.2 表（:184-189）逐码冻结语义；`docs/contracts/DATA_SEMANTICS.md:766`「`DPSF_PSF_STATUS_{OK,FIT_FAILED,RECT_EMPTY,ALLOC_FAILED}` =0/1/2/3」，:742 块 A「[0]=status | double（整值 0..3）…PHOTOMETRIC 仅 status=0 行入匹配」
- 现状：头文件只在 :117 用注释绑定 `DPSF_PSF_STATUS_OK 0 // 拟合成功 (DPSF_FIT_OK)`，对 1/2/3 未声明关系；实现里 out_status 由显式分支赋值（`dpsf_psf.cpp:1039/1066/1102/1113`），未做 `out_status[i]=result.status` 式传递。消费侧对块 A 状态列的判据不同：`orchestrator.cpp:2446` `bool psf_valid = (prow[0] == 0.0 || prow[0] == 3.0) && …`（0、3 皆有效，写进 row[1]/row[2] 天测坐标与 row[3] flux），而 `DATA_SEMANTICS.md:742` 说 PHOTOMETRIC 只认 0，§11.2:189 说批接口把 3 判为"非 OK→NaN，不计 valid"。
- 差在哪：数字 2、3 在同一头文件里各有两个互不相关的含义，且"3 算不算一次可用测量"在文档条文与两条生产入口各说一种——`module.yaml:7` 自称本头是"唯一权威签名头"，它恰恰没给出这两张表的边界。
- 后果：① 同一天区的天测星集与测光星集静默不一致（status=3 的星进 platesolve、不进测光匹配），动摇的是"逐星测光与天测同基"这一对外主张；② 任何按 `psf_status` 列名解码的消费方会把"迭代耗尽但参数可用"读成"内存分配失败"，把"入参非法"读成"窗口为空"，从而把可救的星登记为基础设施故障。不等式方向：3 被算作有效 ⇒ 天测侧**偏绿**（多收星），非恒红。
- 定级：S2（触发路径可写：`normalize` → orchestrator.cpp:2289-2396 产 psf 块 → :2446 判据 → star_measurements row[6]；但 §11.2 那一格是冻结登记，未直接产出错值，故不到 S1）
- 怎么算修好：`DPSF_PSF_STATUS_*` 码域挪出 0..3（或 :116 明文"与 :33-36 无数值对应、禁止赋值传递"）；同一批必须改 `STAR_PSF_ALGORITHMS.md` §11.2 表、`DATA_SEMANTICS.md:742/766`、`public_api` 对应条款与 p1psf 套件里逐码负例的期望值，否则改码即判红。
- 置信：CONFIRMED（四张条文与两处判据均逐字读到；"按名解码误判 2/3"这一子项 UNPROVEN，未找到实跑消费点）
  - **收批回填（见 CR-33-28）**：跨表比较已在跟踪面找到实例——`p1psf_prodpath_check.cpp:77` 用 `DPSF_FIT_OK` 比 `out_status` 值（0↔0 侥幸成立）。故"禁止赋值/比较传递"不再是假设风险，而是已发生的写法。

### CR-33-3 · 模块 README 说"拟合失败星 9 字段全 NaN"，本头与合同说失败星不占行且尾部行是不承诺值的残行
- 轴：C
- 位点：`lib/algorithms/psf/README.md:46` invalid 列「拟合失败星 9 字段全 NaN；批 API `out_n_valid` 仅计 DPSF_FIT_OK」；对置 `dynamic_psf.h:95-96`「成功拟合按【检测下标升序 compact】写入 out_psf_params 前 n_valid 行；其余行 (若调用方预填) 不承诺任何值」
- 上位依据：`DATA_SEMANTICS.md:764-765`「成功行…compact 写入第 0..n_valid−1 行；失败星不占据参数行（**禁留 NaN 占位行**）」；§11.2:187「B2-A2：失败星不再占参数行」；`README.md:131` 同文件另一处已按 B2-A2 改写
- 现状：实现先全量 NaN 预填（`dpsf_psf.cpp:1029-1034`）、成功星按检测下标原位写（`:1093-1101`），再在串行段把成功行前移（`:1120-1134`），**不清空被搬走的源行**。举例 N=8、成功星下标 {2,5,7}：compact 后 row0..2＝三颗成功星，row3/4/6＝NaN，而 row5/row7 仍是 row1/row2 的完整副本（非 NaN）。
- 差在哪：`README.md:46` 的"失败星 9 字段全 NaN"是 B2-A2 之前的旧口径，且按现行实现它就不是事实——行 ≥n_valid 里混有成功星参数的**残影**；同一文件的 :131 与合同 :764 说的是另一套。这不是"同一件事前后各说一遍的两个适用域"，是同一 API 同一失效场景的两种说法。
- 后果：以 `isfinite(row[1])` 之类办法扫全 N 行判定"哪些星有 PSF"的第三方消费方（正是本头 :1126 在 out_status 缺省时自己用的办法）会把两颗星数两遍，flux/A 在下游直方图与中值统计里重复计入。当前两个生产消费方都按 out_status 映射（`module_adapters.cpp:4135-4136`），未受害。
- 定级：S3
- 怎么算修好：`README.md:46` invalid 列改成与 :131／合同 :764 同口径（"仅前 n_valid 行为成功星参数，其余行不承诺值；星↔行映射只可由 out_status 判定"）；若要真兑现"其余行全 NaN"，须在 `:1120-1134` compact 段之后把 `[n_valid, N)` 行重置为 NaN，并同批把 :95-96 的"不承诺任何值"改写为强承诺，避免实现比条文松。
- 置信：CONFIRMED（compact 段逐行读到并按上例逐格推演过搬移结果）

### CR-33-4 · PSF 块消费点的注释用高斯因子解释 Moffat4 的 σ→FWHM，正是已登记为"不可跨块比较"的那一次换算
- 轴：C（主）／S
- 位点：`lib/infrastructure/scheduler/src/module_adapters.cpp:4137`「// [7]=fwhm_x [8]=fwhm_y; sx=sigma_x → fwhm=2.3548*sx（由 9 列取 [7]/[8] 权威值）」
- 上位依据：`docs/science/STAR_DETECTION.md:170`「**与 PSF 侧的模型差**：检测侧椭圆高斯 FWHM=2.3548·σ 与 PSF 侧 Moffat4 FWHM=1.230310·σ 相差 1.9140×（DISP-STAR-007），两列不可跨块比较」；`docs/science/STAR_DETECTION.md:104` 同一比值；`dpsf_psf.cpp:20-25` 与 `dpsf_psf.h:4` 给 PSF 侧因子 1.230310
- 现状：紧挨着正确取值语句（`:4138-4139` 直接取 [7]/[8]）的注释断言本块 `sx` 与 `fwhm` 的关系是 2.3548 倍；实际关系是 1.230310 倍（我在 §1.1 已按模型复算）。
- 差在哪：注释描述的换算与它注释的那段数据（`psf_params:FLOAT64[N,9]`）的生产定义差 1.9140×，且这条换算恰是 DISP-STAR-007 明文禁止跨块使用的那一条。
- 后果：注释不参与运算，产品字节不变；但它是后续维护者给 PSF 侧 aperture/FWHM 换算"补一个 2.3548"的授权书——按此把 σ 当 fwhm/2.3548 反推会把 PSF 宽度算窄 1.9140×，直接传到孔径选取与 SNR 面积项。
- 定级：S3
- 怎么算修好：该注释改为「sx/sy 是 Moffat4 的轴半宽参数，fwhm_{x,y}=1.230310·sx/sy（PSF 侧模型；检测侧高斯 2.354820 因子不得跨块套用，DISP-STAR-007）」；同批在 `docs/algorithms/STAR_PSF_ALGORITHMS.md` §11.2 旁注该禁令也约束注释面。
- 置信：CONFIRMED

### CR-33-5 · `params=NULL→fitRadius=8` 只在批 f32/f64 成立，且两个生产入口对 PSF 裁窗半径取不同路径（一侧硬走默认 8）
- 轴：C（主）／D
- 位点：`dynamic_psf.h:191`「params - 拟合参数 (fitRadius/maxIter/tolerance), 可为 NULL 使用默认值」（该段属 `dpsf_fit_batch_d`，声明在 :200-209）；`dynamic_psf.h:81`、`:147` 同一句
- 上位依据：`DATA_SEMANTICS.md:724` 节标题「15.1 输入（dpsf_fit_batch_f / _d 生产通道…）」＋ :730「NULL → 默认 fitRadius=8/maxIter=200/tolerance=1e-8」；`PUBLIC_API.md:632`「params 可 NULL（默认 fitRadius=8/…）」
- 现状：NULL→默认自建 `default_params` 只存在于 `dpsf_fit_batch_f32`（`dpsf_psf.cpp:841-845`）与 `dpsf_fit_batch_f64`（`:1016-1020`）；`dpsf_fit_batch_d` 在 `:722` 把 `!params` 直接判为参数错误返回 −1，`dpsf_fit_batch`/`_f` 在 `:490`/`:612` 无判空地解引用 `params->fitRadius`。两条生产入口因此走不同路：编排侧 `orchestrator.cpp:2296-2331` 恒传 `&params`（fit_radius 取自 `stage1_cfg_->psf.fit_radius`，:2289），调度侧 `module_adapters.cpp:4112` 实参为 `nullptr` ⇒ 无条件吃模块默认 8。
- 差在哪：本头与合同把"可 NULL"当成整个批族的性质，实现只有两个成员具备；并且同一阶段的 PSF 裁窗半径在两个入口一个是配置驱动、一个是写死默认，配置面无法覆盖调度侧。
- 后果：运行时方向是 fail-closed（`_d` 给 −1、编排侧 :2357 判 ret≠0 即退出），不产错值；但"由配置/profile 决定窗口"这一主张在 `normalize` 的调度入口上不成立——同输入经两个入口可得到不同 fitRadius 的 PSF 结果。
- 定级：S2（触发路径：`normalize` → 调度侧 `module_adapters.cpp:4110-4112`（fitRadius 恒 8）／编排侧 `orchestrator.cpp:2289-2331`（fitRadius 可配置），两路产出的 psf 块不保证逐字节一致）
- 怎么算修好：先定性质归一（推荐把 NULL→默认扩到批 `_d`/`_f`/`batch` 三个成员，或在 :81/:147/:191 与 DATA_SEMANTICS:730、PUBLIC_API:632 限定"仅 `_f32`/`_f64`"）；再让调度侧显式传参（复用 `stage1_cfg_->psf.fit_radius` 的同一条解析），或把"调度侧固定 8"写进合同并在 README 登记为入口差异。二者不可只做文档不做实现，否则改了 `_d` 的判空会让现有 −1 断言判红。
- 置信：CONFIRMED（四处入口与三处判空均逐字读到）

### CR-33-6 · `maxIter`/`tolerance` 死参数虽已登记，但编排日志把从未生效的 100/1e-6 当"本次调用参数"记入运行证据，且该兜底值与本头合同的默认 200/1e-8 不同
- 轴：C
- 位点：`lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp:2293` `if (max_iter <= 0) max_iter = 100;`、`:2294` `if (tolerance <= 0.0) tolerance = 1e-6;`、`:2325` `"…(FP64, fitRadius=%d, maxIter=%d, tol=%.1e) …"`
- 上位依据：`STAR_PSF_ALGORITHMS.md:201` DISP-PSF-003「`DPSFFitParams.maxIter/tolerance` 死参数（LM 硬编码 1e-8/200）」；`DATA_SEMANTICS.md:730` 默认「maxIter=200/tolerance=1e-8」；实际求解调用 `dpsf_psf.cpp:374-375` `lm_solve(…, 1e-8, 200)`
- 现状：死参数这一事实已登记（**我不重复报它**）；未登记的是 `:2327` 把 `max_iter=100`、`tolerance=1e-6` 打进 INFO 日志，日志文本读起来像"本轮以 maxIter=100/tol=1e-6 拟合"，而模块侧从不接受这两个数；同时 100/1e-6 与本头合同所指的默认 200/1e-8 是两套数。
- 差在哪：运营面证据（run 日志）与模块实际执行的收敛判据无对应关系；两处入口对"默认收敛参数"给两个数（100/1e-6 与 200/1e-8）。
- 后果：以日志为据回答"这批 PSF 拟合的收敛判据是什么"会得到错答（真实值恒 200/1e-8）；不影响产品字节。
- 定级：S3
- 怎么算修好：`:2325` 日志改记模块真实判据或在同句标 `(maxIter/tol 不生效，见 DISP-PSF-003)`；并统一 `:2293-2294` 的兜底常量与 `DATA_SEMANTICS.md:730` 的 200/1e-8，DISP-PSF-003 加一句"编排侧兜底值仅用于日志"。
- 置信：CONFIRMED

### CR-33-7 · `psf` 端口的单位类登记为 DIMENSIONLESS，而本头同一批列把 B/A 定为 ADU、cx/fwhm 定为像素
- 轴：C（主）／S
- 位点：`dynamic_psf.h:86-87`「// [0]=B (背景)」「// [1]=A (振幅）」＋ `dynamic_psf.h:93-94`「[7]=fwhm_x (X 方向 FWHM, 像素)」；登记面 `lib/infrastructure/scheduler/src/module_adapters.cpp:885` `{"psf", "DATA-P1-PSF", false, UnitId::DIMENSIONLESS, CoordinateFrame::PIXEL}` 与 `lib/algorithms/psf/README.md:46` 单位列「DIMENSIONLESS（θ 为弧度，FWHM 为像素）」
- 上位依据：`DATA_SEMANTICS.md:785`「像素坐标一律 0-based double 全图系（cx/cy）；θ 弧度 x 轴起边；FWHM/σ 像素；**B/A/flux/mad 为 ADU**（flux 为解析积分，非计数）」，:743-748 逐列同样写明 B/flux/A/mad 为 ADU
- 现状：合同面一个块里同时有 ADU 列与像素列，端口单位类只能取一个，取的是 DIMENSIONLESS；README:46 的括注只交代了 θ 与 FWHM，未提 B/A/flux/mad 是 ADU。`UnitId` 在 `eng/tests/unit/core_artifact_test.cpp`、`rt001_abi_test.cpp`、`eng/ci/ledgers/design_clauses.json` 等面被断言（即它是要被机器门核对的登记量）。
- 差在哪：端口级单一单位类与列级混合单位不是同一粒度，但登记面没有任何地方记录"本块的量纲真值在 §15.2/§15.3"，于是核对 UnitId 的门把一个含 ADU 的块判为"无量纲"是恒真判定——门读的是它自己写的那一面。
- 后果：ADU 定标若整体漂移（增益/平场归一顺序变化），`flux=2π·A·sx·sy/3` 与 B 一起变，而单位门不报警；flux→星等零点是 SCI-A 的输入，这条漂移正落在创新点的敏感面上。（A/mad 型峰值 SNR 对该整体缩放不敏感，故不据此报 SNR 失真。）
- 定级：S2（可达路径：`normalize` → 块 `psf` 由 orchestrator.cpp:2372-2399 写、descriptor 端口按 :885 标类 → 测光侧消费 flux）
- 怎么算修好：给含混合量纲的块定义端口类（例如新增 `MIXED` 并要求 descriptor 带 `column_units` 表），或在 `DATA_SEMANTICS.md:721` 同段明文"DATA-P1-PSF 的端口 UnitId 是形式登记，量纲权威＝§15.2 列表"，并让核对 UnitId 的门改读列表；两者都要同批改 README:44-46 的单位列以免自相矛盾。
- 置信：PARTIAL（缺：单位类是否真被某条 normalize 期机器门拦截／放行的可复现读数——我只核到 UnitId 出现在测试与台账文件名，未读到判定语句；需实跑或读 `eng/tests/unit/core_artifact_test.cpp` 断言体）

### CR-33-2 · 本头声明的调度器椭圆度 `1−min/max` 与合同冻结的 `eccentricity=√(1−(smin/smax)²)` 是两个不同的量
- 轴：C（主）／R
- 位点：`lib/infrastructure/scheduler/src/module_adapters.cpp:4141-4143`「`const double sy = psf_params[…+5]; const double mx = std::max(sx, sy), mn = std::min(sx, sy); f_ell[fi].push_back(mx > 0.0 ? 1.0 - mn / mx : 0.0);`」；对置 `dynamic_psf.h:30` `double eccentricity;`
- 上位依据：`DATA_SEMANTICS.md:749`「[8]=eccentricity | double [0,1) | √(1−(smin/smax)²)（:378）」；生产实现 `dpsf_psf.cpp:431-432` `eccentricity = std::sqrt(1.0 - (sx_min/sx_max)*(sx_min/sx_max))`
- 现状：调度侧不读块 B 的任何现成形状量，而用 sx/sy 重算 `1−mn/mx`（椭率 ellipticity），并把结果与 PSF 的 fwhm_x/fwhm_y 并排送进同一批星形状统计（`:4218-4220` `fwhm_xs.insert(...f_fx[fi]...); ells.insert(...f_ell[fi]...)`）；同一模块的另一条路径（编排侧 star_measurements row[11]＝`prow[8]`＝eccentricity，`orchestrator.cpp:2465`）用的却是平方根式。
- 差在哪：`x=b/a∈(0,1)` 时恒有 `1−x < √(1−x²)`（因 `1+x>1`），所以调度侧的值系统性偏小：b/a=0.5 时 0.500 对 0.866。两路对同一颗星给出不同数，而合同只定义后者。
- 后果：任何"形状超阈即剔除（拖线/双星/坏像元）"的判据，用调度侧的数会**偏绿**（同一条 `ell < T` 门放进更多被拉长的星）。方向已定：偏绿。
- 定级：S2（触发路径：`normalize` 调度入口 → `module_adapters.cpp:4110` 批拟合 → `:4141-4143` 算 ells → `:4220` 汇入帧级形状统计；缺：`ells` 的阈值消费点与 `f_ell` 是否被当作合同意义的 eccentricity 呈现，见 §待实跑）
- 怎么算修好：二选一——(a) 调度侧直接消费块 B 之外的权威值（改走 `_d`/结构体的 `eccentricity`，或按 √(1−(mn/mx)²) 重算并把变量与列名统一为 `eccentricity`）；(b) 若刻意要 ellipticity，则改名 `ellipticity`、在 `DATA_SEMANTICS.md` §15.2 增设该列定义与域，并核对用它做阈值的所有门是否需按 1.73× 量级重标阈值（防止改对反而判红）。
- 置信：PARTIAL（两个公式、不等式方向与两条路径已 CONFIRMED；`ells` 下游是否有硬阈值未读到）

### 1.3 派单本批必查项的取证结论（文件 1）
- **列序／维度／形状四处是否同一解释**：本头有两条 `[N,9]` 语义（布局 B 参数序 vs 块 A 派生序），合同 `DATA_SEMANTICS.md:721` 已明文把"双 [N,9] 并存"登记为冻结事实并要求两布局各自具名——**这一条不算缺陷**。查出的是另外三处不同解释：状态码表数值复用（CR-33-1）、失败行语义两说（CR-33-3）、单位类与列量纲冲突（CR-33-7）。行距 9 在生产（`dpsf_psf.cpp:1061`）、合同（`README.md:46`）、消费（`module_adapters.cpp:4138`）三处一致，未发现错位读。
- **半高全宽与孔径的换算与单位是否自洽**：FWHM＝1.230310·σ 我按模型独立复算吻合，生产按式算（`dpsf_psf.cpp:393-394`），单位像素自洽；不自洽处在消费侧注释套用高斯 2.3548（CR-33-4）与形状量定义分叉（CR-33-2）。块 A 进一步把 fwhm_x/fwhm_y 平均成单列 fwhm（`orchestrator.cpp:2392` `(r.fwhm_x + r.fwhm_y) * 0.5`），椭圆星的"平均 FWHM"不对应任何主轴方向上的真实宽度，用它定孔径会低估短轴方向的要求——本批文件面未见实际孔径换算，留 §待实跑。

## 2 · `lib/algorithms/psf/tests/p1psf/p1psf_center_contract_gate.cpp`（298 行，整读；另读其 CMake 接线与两条消费端生产源）

先给本批必查项的正证：**块 A（`status,B,flux,cx,cy,fwhm,A,mad,eccentricity`）的列序在生产者、消费者、合同三处同一解释。**
生产者 `orchestrator.cpp:2387-2395`、合同 `DATA_SEMANTICS.md:742-749`、消费者
`snr_estimator.cpp:857-859`（`row[0]=status, row[1]=B, row[3]=cx, row[4]=cy, row[6]=A, row[7]=residual_scale`）
与 `:66-69`（`row[2]=flux, row[5]=fwhm`）逐列吻合；SNR 侧还按 `sigma = fwhm/1.230310` 换算
（`snr_estimator.cpp:40,72`，常量 `kMoffat4FwhmFactor=1.230310`），与本批文件 1 复算的 PSF 侧因子一致，
并明文禁止套用检测侧高斯因子（`:60`「PSF 块 FWHM 不得填入 fwhm_px —— 该参数属检测块高斯列」、`:81`）。
**未发现列序错位。** 下文 findings 针对的是本门自身的取证强度与夹具自洽性。

### 2.1 §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :51 | `M_PI` 兜底宏 | 3.14159265358979323846 | 非 MSVC 平台的 π | 结构性不适用 | 数学常数定义，无科学语义 | — | CONFIRMED |
| :64 | `kCrpix1/kCrpix2` | 16.0 / 20.0 | 夹具参考像元（FITS 1-based） | 结构性不适用 | 夹具自订；1-based 域约束由 `Paper I §2.1.1`（本文件 :9/:84 引）与 `SCI-WCS-001 §3a/§5a` 决定，取值本身非科学量 | — | CONFIRMED |
| :65 | `kCrval1/kCrval2` | 10.0 / 20.0 deg | 夹具天球参考值 | 结构性不适用 | 夹具自订（切点位置不影响投影一致性判据） | — | CONFIRMED |
| :66 | `kCd11/kCd12/kCd21/kCd22` | −1e-4/0/0/1e-4 deg/px | 夹具线性 CD 矩阵 | 结构性不适用 | 夹具自订；CD11<0（RA 左增）为标准取向；:144 打印为 `CD=diag(%.1e,%.1e)` | — | CONFIRMED |
| :67 | `kPxScaleDeg` | 1.0e-4 | 角距→像素的换算尺度 | **公式导出（但写死）** | 注释给式 `sqrt|det CD|`，我复算 `sqrt(\|-1e-4·1e-4\|)=1e-4` ✓；代码不计算、直接写字面量 | 保守方向＝改由 CD 现算（`sqrt(fabs(cd11*cd22-cd12*cd21))`）；影响范围＝C5 的 px 阈值（CR-33-12） | CONFIRMED |
| :68 | `kAngTolDeg` | 1.0e-9 deg | C2/C3/C4 的一致性容差（=3.6 µas = 1e-5 px） | **待确认** | 唯一理由是本文件注释「(两路径均 FP64 同式)」；`docs/algorithms/GATES_AND_TOLERANCES.md` **无 G-P1-CENTER-CONTRACT-1 行**（git grep "G-P1-CENTER-CONTRACT-1" 于 eng+docs 命中 0） | 数值本身方向安全（收紧只会偏红）；风险在"容差脱离门表与变更流程"，见 CR-33-10 | CONFIRMED |
| :69 | `kMinOriginSepPx` | 0.9 | C5 的原点鉴别力下限（防恒真门） | **待确认** | 本文件 :30 给"实测 1.414 px = x/y 各 1px"；0.9 相对 1.414 的余隙与"为何不低于单轴偏移 1.0"均无条文 | 保守方向＝提到 1.2 px 才能把"只桥一轴"（恰 1.0 px）与"两轴桥"（√2 px）分开；影响范围＝仅本门（C4 已单独覆盖单轴桥错，故现状不致漏判） | PARTIAL（未跑门，1.414 为我按 CD 解析推得） |
| :70 | `kNumPoints` | 16 | 取样点数（循环上界） | 结构性不适用 | 实现选择；:127 的坐标序列随之定，不参与任何科学量 | — | CONFIRMED |
| :127 | `10.0 + 1.5*i`，`12.0 + 0.75*i` | 夹具坐标 | "psf 块取样点"的 x/y | 结构性不适用 | 夹具自订坐标，非拟合产物——本文件不链接任何 psf 生产者（`CMakeLists.txt:170-174` 四个 TU 无 `dpsf_*`），见 CR-33-9 | — | CONFIRMED |
| :161 | `182.9472 → 182.4472` | 0.5 差 | C1 的 sdet −0.5 桥接样本 | 结构性不适用 | 单点样本；差值恰 0.5 由 `star_coord_contract.h` 契约（`GATES_AND_TOLERANCES.md:71` G-P1-CENTROID-BRANCH-ORDER 冻结"fallback −0.5 / dpsf 恒等 / ipv +0.5"）决定 | — | CONFIRMED |
| :162 | `star_measurement_from_dpsf(182.4472) - 182.4472` | 恒等 | C1 的 dpsf 恒等支路 | 结构性不适用 | 同上（恒等式）；若实现被误改成 −0.5 该条即红，判据非恒真 | — | CONFIRMED |
| :161-163 | `1e-12` | 1e-12 | C1 三条恒等式的浮点容差 | 结构性不适用 | ulp(182.9)=2^(7−52)=2.84e-14 ⇒ 1e-12 ≈ 35 ulp，属浮点比较带宽，非科学容差 | — | CONFIRMED |
| :196-197 | `psf[9]={0.0,100.0,5000.0,x,y,3.0,900.0,12.0,0.1}` | 9 列 | 布局 A 的一行夹具 | **待确认（列间恒等式不成立）** | 块 A 列序由 `DATA_SEMANTICS.md:742-749` 冻结；其中两列由恒等式互绑：`flux=2πA·sx·sy/3`（:744，实现 `dpsf_psf.cpp:429`）、`fwhm=1.230310·sx`（:762/`dpsf_psf.cpp:393`）。按 fwhm=3.0 反解 sx=sy=2.43841 ⇒ flux 应为 `2.0943951·900·5.94585≈1.12e4`，夹具写 5000（差 2.24×）。见 CR-33-13 | 保守方向＝夹具改按恒等式取值（flux=2πA·(fwhm/1.230310)²/3）；影响范围＝本门 C3（只读 [3]/[4]，现状不受影响）与后续复用该行断言 SNR 的任何扩写 | CONFIRMED（复算过） |
| :203 | `sid=1`，`qf=1u`，`ps=0u` | 1 / 1 / 0 | 星 ID 与状态旗标夹具 | 结构性不适用 | 单星夹具标识；`ps=0` 与块 A 的 status=0 一致 | — | CONFIRMED |
| :205 | `n_stars=1`、`value_dtype=1`、`sigma_residual=0.1` | 1 / 1 / 0.1 | 批大小／FP64／残差 σ | 待确认（`sigma_residual` 在被调方为**死参**） | `snr_extract_model_v3` 只在 `snr_estimator.cpp:835` 用 `sigma_residual<=0` 做入口守卫，此后不再参与计算；SNR 实际由 `sourceSnrFromPsfRow`（:866，用行内 `mad/0.7316727929211932` 作 sky σ）给出。故 0.1 与行内 `mad=12.0` 隐含的 σ（16.4）不构成同一物理量 | 保守方向＝夹具把 sigma_residual 与行 [7] 按 `mad/0.73167` 对齐，或该参显式标为 retired；影响范围＝任何据本门措辞认为"SNR 由 sigma_residual 驱动"的读者 | CONFIRMED |
| :207 / :231 | `worst=1e30`、`min_origin_sep_px=1e30` | 1e30 | 极值累积哨兵 | 结构性不适用 | 循环哨兵初值（求 max / 求 min），不承载科学语义 | — | CONFIRMED |
| :147, :187, :217, :249 | `buf[320]` / `buf[256]` | 320/256 | snprintf 文本缓冲 | 结构性不适用 | 缓冲区尺寸；:147 那条最长串实测约 282 B（8×%.17g≈192＋标签≈90）< 320，未溢出 | — | CONFIRMED |
| :245 | `sep_deg(...)/kPxScaleDeg` | — | 角距折算像素 | 公式导出 | 小角近似下 px = deg/(sqrt|det CD|)；与本门 C5 用途一致（依赖 :67 的写死值，见 CR-33-12） | — | CONFIRMED |
| :260 | `min_origin_sep_px >= kMinOriginSepPx` | — | C5 的不等式方向 | 结构性不适用（判据形状） | 方向已核：值越小＝两读法越接近＝鉴别力越弱 ⇒ 该判据**偏红侧安全**（放宽 0.9 才会漏） | — | CONFIRMED |

### 2.2 findings

### CR-33-10 · 本门的门号 G-P1-CENTER-CONTRACT-1 与两条判据阈值在门表零登记，容差只活在测试源码里
- 轴：C（主）／D
- 位点：`p1psf_center_contract_gate.cpp:23`「判据（docs/algorithms/GATES_AND_TOLERANCES.md **待登记**为 G-P1-CENTER-CONTRACT-1:）」；`CMakeLists.txt:149`「G-P1-CENTER-CONTRACT-1 (W4-A1): psf 块像素中心口径门」
- 上位依据：`ACCEPTANCE_SPEC.md`／`docs/algorithms/GATES_AND_TOLERANCES.md` 是门与容差的登记面——同表 :54 与 :71 分别登记了 G-P1-CENTROID-1 与 G-P1-CENTROID-BRANCH-ORDER（含阈值来源与证据列），本门同级却无行；AGENTS.md §6「不动科学公式、默认容差…（除非按变更流程批准）」、§5「判据必须非退化」
- 现状：`git grep -n "G-P1-CENTER-CONTRACT-1" -- eng docs` 命中 **0**（同一命令对 `G-P1-CENTROID-1` 命中 4）；ctest 侧它确实在跑（`CMakeLists.txt:185-197` 注册三条用例并打 LABEL p1psf）。
- 差在哪：一道被当作"能红能绿"的口径门，其判据（1e-9 deg、0.9 px）与门号都不在门表面——按门表核对验收的人看不见它，改测试源码里的阈值不构成任何文档变更。
- 后果：① 容差绕过变更流程即可放宽（对外主张"发布工件 p1_wcs.json.samples[] 的像素原点口径由 G-P1-CENTER-CONTRACT-1 将住"失去登记凭据）；② 与本门同一背景修复（`module_adapters.cpp` 的 kP1FitsPixelOrigin）在门表里也没有对应行。
- 定级：S2（不产错值，但使既有对外主张缺登记面；触发面为 ctest 常驻，非生产入口）
- 怎么算修好：在 `GATES_AND_TOLERANCES.md` 的 P1 段补 G-P1-CENTER-CONTRACT-1 行（判据式、夹具规模、阈值、来源、证据列 `ctest:p1psf_center_contract_gate{,_neg,_astropy}`、是否科学门），并在 :23 删去"待登记"；同批核对 `eng/ci/checks.json`／台账是否要求门号有登记项（若要求则一并补，避免改文档反而判红）。
- 置信：CONFIRMED

### CR-33-11 · 唯一的负例注入只改 [C4] 的喂法，而负例态下 [C5] 必然同时判红 ⇒ 把 C4 容差放宽到任意值本套件仍全绿（误绿方向已定）
- 轴：R（主）／D
- 位点：`p1psf_center_contract_gate.cpp:235-241`「`if (g_negative_injection) { wcs.pix2sky(x, y, &rab, &decb); } else { wcs.pix2sky(x+1.0, y+1.0, …); }` … `wcs.pix2sky(x, y, &rau, &decu); // 未桥接读法 (永远算, 供鉴别力判据)`」＋ `:253`（C4 用 `worst_bridged <= kAngTolDeg`）＋ `:260`（C5 用 `min_origin_sep_px >= 0.9`）＋ `:288-295`（负例态只要求 `g_failures > 0`）
- 上位依据：`ENGINEERING_SPEC §8`（本文件 :32 自称遵循的"可执行负例面"）与本文件 :34-35「若注入未被检出（rc=1）说明本门失效」；AGENTS.md §9「判据本身不合理时…配可执行正例/负例」、§5「恒真门没有证据资格」
- 现状：负例态里 `rab/decb` 与 `rau/decu` 来自**同一次** `pix2sky(x,y)` ⇒ `sep_deg=0` ⇒ C5 无条件红；进程退出码只统计"有红"，不区分红在哪条。
- 差在哪：负例判据无法把 C4 单独证伪。把 `kAngTolDeg` 改成 1e-2 deg（≈100 px，比 C4 要防的 1 px 原点错还大 100 倍），正例 C4 照样绿（其 worst 实际≈0），负例 C4 也"通过"，但整套仍打印 `NEGATIVE INJECTION DETECTED`（因 C5 红）并 rc=0。不等式方向：C4 的判据是 `worst <= tol`，**tol 变大只可能让红变绿**，故放宽不可观测＝偏绿；而负例态 C5 的红与注入无关（恒红），因此 C5 在负例态不提供任何信息。
- 后果：G-P1-CENTER-CONTRACT-1 的对外主张"能红能绿"在 C4 上不成立——这条正是本门存在的理由（修复前形态是"0-based 直喂 1-based 契约"）；C4 被后续改动放宽时无人报警。
- 定级：S2（缺陷在门自身，不改产品字节；但使 p1_wcs.json 原点口径这一对外主张失去可执行凭据）
- 怎么算修好：负例态改为**只**放宽 C5 依赖的同一条比较、并按检查名计数红（例如 `if (g_negative_injection) { 只统计 C4 的失败 }`），使负例判据落在"C4 必须红且 C1/C2/C3/C5 必须绿"；或给 neg 目标加第二条注入（只放大 kAngTolDeg 的用例）验证 C4 的鉴别力。同批补 `--self-test`（本文件 :42 已有 fail-closed 导出面可复用）。
- 置信：CONFIRMED（三处判据与退出码逻辑逐字读到；未实跑，虚容差全绿是我按 :235-260 逐句推演所得）

### CR-33-9 · 名为"psf 块像素中心口径门"却不链接、不调用任何 psf 生产者，被核对的"块 cx/cy 是 index-is-center"这一前提在本门里是手填的
- 轴：D
- 位点：`p1psf_center_contract_gate.cpp:125-129`「// psf 块取样点 = index-is-center (0-based 数组下标即中心) … `pts->emplace_back(10.0 + 1.5*i, 12.0 + 0.75*i);`」；标题 :2「G-P1-CENTER-CONTRACT-1: psf 块像素中心口径门」与 :7「psf 块 cx/cy = dpsf 原始输出 = **index-is-center**」
- 上位依据：`CMakeLists.txt:170-174` 明列本门链接的"真实生产源"＝ `wcs_transform.cpp` / `snr_estimator.cpp` / `snr_science.cpp` / `wcs_tan.cpp`——**不含 `dpsf_psf.cpp`**；`GATES_AND_TOLERANCES.md:71` 把"PSF 支路恒等（dpsf 输出即 index-is-center）"的**证据列**登记为 `ctest:p1psf_centroid_gate`（不是本门）
- 现状：本门把"块里存的下标就是中心"当成夹具前提直接写死，三条消费链（photometry/SNR/导出）与独立参考解比对的是"消费方对不对得起 0-based"。头注与门名让人以为它同时核了生产者。
- 差在哪：门名/头注的主张范围大于其实际取证范围。生产者侧并非无人管（centroid_gate＋BRANCH-ORDER 行覆盖），所以这不是覆盖洞，是本门的自述越界。
- 后果：读门名即认为"psf 块原点口径已被端到端将住"的人，会漏掉 `dpsf_fit_batch_*` 若改为 1-based（例如未来有人"顺手统一 CRPIX 口径"）时本门仍全绿；此时只有 centroid 门会红。
- 定级：S3
- 怎么算修好：:2 与 :7 的措辞限定为"消费链口径一致性（生产者侧由 G-P1-CENTROID-1/BRANCH-ORDER 覆盖）"，或增补一条以 `dpsf_fit_batch_f64` 产出的真实 cx/cy 走同一条 C2/C3 判据（则需把 `P1PSF_PROD_SOURCES` 加进本 target）。
- 置信：CONFIRMED

### CR-33-12 · `kPxScaleDeg` 是 CD 矩阵的导出量却写死，夹具改 CD 即静默错标 C5 的像素阈值
- 轴：S（主）／R
- 位点：`p1psf_center_contract_gate.cpp:67`「`constexpr double kPxScaleDeg = 1.0e-4;        // sqrt|det CD| = 1e-4 deg/px`」
- 上位依据：§2.5 处置规则 2「公式导出——…算则合规，写死则同时报一条 S/D 轴 finding」；注释自己给出的式就是 `sqrt|det CD|`
- 现状：:66 的 `kCd11/kCd22` 是独立可改的夹具常数，:67 手抄其结果；C5 的像素判据（:245 除以 `kPxScaleDeg`、:260 与 0.9 比）完全依赖这个手抄值。
- 差在哪：写死导出量把"改 CD 会连带改什么"从编译器手里拿走，门表又没登记该值（CR-33-10），没有任何一层能发现它和 CD 脱钩。
- 后果：把 CD 改成 2e-4 deg/px 的夹具变体（例如为覆盖更大视场）会让 C5 的 0.9 px 门限实际对应 0.45 px ⇒ 原点鉴别力被腰斩而仍报绿（偏绿）。
- 定级：S3（现状夹具未变，无实际失真）
- 怎么算修好：`constexpr double kPxScaleDeg = std::sqrt(std::fabs(kCd11*kCd22 - kCd12*kCd21));`（:66 之后）；同批把门限 0.9 的出处一并登记（CR-33-10）。
- 置信：CONFIRMED

### CR-33-13 · C3 的 psf 行夹具违反块 A 自己冻结的两条列间恒等式，不构成产品侧可达的行形状
- 轴：S（主）／C
- 位点：`p1psf_center_contract_gate.cpp:196-197`「`double psf[9] = {0.0, 100.0, 5000.0, pts[0].first, pts[0].second, 3.0, 900.0, 12.0, 0.1};`」
- 上位依据：`DATA_SEMANTICS.md:744`「[2]=flux | double ADU | Moffat4 解析积分 2π·A·sx·sy/3」、:762「[7]=fwhm_x/[8]=fwhm_y | 1.230310·sx/sy」、:746「[5]=fwhm | (fwhm_x+fwhm_y)/2 平均」；`PUBLIC_API.md:656`「(flux=2π·A·sx·sy/3 Moffat4 解析积分)」；`STAR_PSF_ALGORITHMS.md:209`「flux=2πAsxsy/3 与 FWHM=1.230310·σ 恒等复核」
- 现状：行内 `[5]=fwhm=3.0`、`[6]=A=900`、`[2]=flux=5000`。按冻结恒等式反解 `sx=sy=3.0/1.230310=2.43841` ⇒ flux 应为 `2.0943951·900·2.43841²≈1.12e4`；夹具值差 2.24×。同时 `sigma_residual=0.1`（:205）与 `[7]=mad=12.0` 隐含的 sky σ（`mad/0.7316727929211932=16.4`，`snr_estimator.cpp:83`）也不是一个量级。
- 差在哪：派单 §4 免报"夹具内容差"，此处不属那一类——块 A 的列不是自由实参，合同把它们由两条恒等式互绑，生产者可复现的行必在恒等式曲面上；本行不在曲面上，故它是产品不可达形状。
- 后果：本门 C3 只取 [3]/[4] 比 ra/dec，现状不失真；危害面是"以本行为模板扩写 SNR 断言"——`sourceSnrFromPsfRow` 会照收 flux=5000 而不报错（`snr_estimator.cpp:73-77` 只在 flux 非正/非有限时兜底），届时"实测 SNR 对 2.24× 通量错"仍被当夹具正确。
- 定级：S3
- 怎么算修好：改按恒等式取值（`fwhm=3.0 → sx=2.43841 → flux=2πA·sx²/3`，或反之由 flux/A 反推 fwhm）；并在本文件头注声明"块 A 夹具必须满足 §15.2 的两条列间恒等式"，或让 oracle（文件 5）提供一个"行自洽性"helper 供夹具与消费门共用。
- 置信：CONFIRMED（flux 期望值我按 β=4 积分复算：∫A/(1+Q)⁴ dA = 2πA·sx·sy/3）

## 3 · `lib/algorithms/psf/tests/p1psf/p1psf_centroid_gate.cpp`（328 行，整读）

正证先记：本门确实链接 `sdet`(6 TU)＋`dpsf`(2 TU)＋坐标契约头（`CMakeLists.txt:116-133`），
[P5]（:28/:321）独立钉住"dpsf 原始输出＝index-is-center"，**文件 2 的 CR-33-9 因此不是覆盖洞**。
本门门号与阈值**已登记**于 `docs/algorithms/GATES_AND_TOLERANCES.md:54`（与 CR-33-10 形成对照）。

### 3.1 §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :59 | `kMoffat4FwhmFactor` | 1.230310 | 由目标 FWHM 反解 σ | 公式导出 | 与文件 1 §1.1 复算同值（`FWHM=2σ√(2(2^{1/4}−1))`）；生产者 `dpsf_psf.cpp:25`、SNR 消费者 `snr_estimator.cpp:40`、本门 :59 **三份独立写死** ⇒ CR-33-17 | — | CONFIRMED |
| :60 | `kGateMedianPx` | 0.1 px | [P2]/[P3] median 门 | **待确认** | `GATES_AND_TOLERANCES.md:54` 登记值，来源格写「标定：R-3 §2.9 + 门实测」；`artifacts/evidence/governance-01/research/R-3_PSF质心科学门与容差.md:196,200` §2.9 实测 median=0.0035（sdet）/0.0031（dpsf）⇒ 门值是实测的 ~30×，无独立推导式；上位科学门只冻结 \|Δc\|≤0.3 px（R-3:46-47 引 SCI-P1-STAR-001 §1／ALG-STARDET-001 §11.4 F1），**无 median≤0.1 条款** | 方向：比上位更严 ⇒ 只可能偏红，不致误绿；风险是"无锚阈值将来被当作权威"或场景一变即恒红。保守＝补一句裕度推导（例如 10× §2.9 实测）或在 R-3 侧给出式 | CONFIRMED |
| :61 | `kGateP95Px` | 0.3 px | [P2]/[P3] p95 门 | 文献值→**上位冻结值** | 同 GATES:54；权威条文＝R-3:46 所引 `docs/science/STAR_DETECTION.md:12-14`「合成场验收容差 \|Δc\|<=0.3 px（SNR>=20）」＋`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:256-258` F1 | 可达性依赖 SNR 定义：R-3:138 记 SNR=5 时 p95=0.428 px 超标 ⇒ 本门的 SNR 域由 `SNR_peak=A/(mad·1.4826)` 定义（`DATA_SEMANTICS.md:779`），夹具 A=snr·NOISE（:144）与此定义吻合 | CONFIRMED |
| :62 | `kGateSeparationPx` | 0.05 px | [P4] 双支路同系门 | 待确认 | GATES:54 登记；来源＝R-3:372 的建议式（原文即"<= 0.05 px"），R-3:202-203 实测修复前两支路 median 差 = −0.5000/+0.0001 ⇒ 0.05 是"卡 0.5 px 缺陷"的余隙选择，非推导 | 保守＝维持（该量在修复后实测 −0.0000/+0.0001，余量 500×）；GATES:54 已明文"禁用双支路分离 ≥0.5 px 的实现"⇒ 判据与缺陷量级挂钩，非任意 | CONFIRMED |
| :63 | `kGateDpsfFramePx` | 0.05 px | [P5] dpsf 输出系门 | 待确认 | GATES:54 第四项 `\|median(dpsf_cx − truthIndex)\| ≤ 0.05`；R-3:198 实测 `cx-truthIndex=+0.0000` | 同上（实测余量≫门）；无独立推导 | CONFIRMED |
| :64 | `kInjectionMinSeparationPx` | 0.4 px | 负例注入的"必须检出"下限 | 待确认 | 只在本文件 :34/:302-305 与 `CMakeLists.txt:108` 出现；GATES:54 的来源格写"分离恰卡在 ipv 去重阈值严格 `<0.5` 之外 ⇒ 同星双份进 ipv"，但**未登记 0.4 这个检出下限**；`git grep "0.4 px"` 在 docs 侧无本门命中 | 保守＝维持 0.4（注入真值恰 0.5 px，余隙 0.1 覆盖拟合噪声）；影响范围＝仅负例用例的判据强度 | CONFIRMED |
| :82 | `idx = p*(size-1)` | — | 分位数插值式 | 结构性不适用 | 标准 type-7 分位（我按 :85 线性插值式核对 n=120、p=0.95 ⇒ idx=113.05） | — | CONFIRMED |
| :113-114 | `(x + 0.5) - t.first` | 0.5 | 真值系定义（像素中心＝索引＋0.5） | 单位换算定义常数 | 与 `star_detector.h:74`「pred 为 0-based 像素坐标（像素中心 = 索引 + 0.5）」、`DATA_SEMANTICS.md:785` 同口径 | — | CONFIRMED |
| :115 | `q = 0.5*(dx²+dy²)/(s*s)` | 0.5 | 圆 Moffat4 的 Q 定义 | 公式导出 | 与生产 `dpsf_psf.cpp:97-106` 的 `inv_sx2 = 1/(2sx²)` 同式（θ=0、sx=sy 退化）——夹具与生产模型同一参数化 | — | CONFIRMED |
| :141 | `W=256, H=256` | 256 | 场尺寸 | 结构性不适用 | 夹具规模，登记于 GATES:54 夹具格 | — | CONFIRMED |
| :142 | `B=120.0`，`NOISE=8.0` | ADU | 背景与像素噪声 | 待确认（夹具自订） | 本文件 :18 自述；GATES:54 夹具格只记 "SNR_peak=50/300、FWHM 3 px"，未记 B/σ_n。σ_n=8 与 B=120 只通过 `A=snr*NOISE` 影响 SNR 档位，不单独进入判据 | 保守＝把 B/σ_n 补进门表夹具格，避免"SNR 档位固定但噪声水平可被随手改" | CONFIRMED |
| :143 | `S = 3.0/kMoffat4FwhmFactor` | 2.43841 px | σ（由目标 FWHM=3 px 反解） | 公式导出 | 目标 FWHM=3.0 px 登记于 GATES:54；反解式即 :59 的因子 | — | CONFIRMED |
| :144 | `A = snr * NOISE` | 400 / 2400 ADU | 峰值振幅 ⇒ 造出 SNR_peak=50/300 | 公式导出 | `DATA_SEMANTICS.md:779` 冻结 `SNR_peak = A_fit / sigma_bg`、`sigma_bg = mad·1.482602218505602`；高斯像素噪声的 10–90% 截尾平均残差 = 0.6745σ ⇒ mad·1.4826 = σ（我复算 `0.67449·8·1.482602=8.000`）⇒ A/(mad·1.4826)=snr ✓ 夹具的 SNR 标签与冻结定义自洽 | — | CONFIRMED |
| :146 | `seed ^ 0x5deece66dULL` | — | 位置流与噪声流解耦 | 结构性不适用 | 常数取自 Java LCG 乘子，此处仅作域分离盐值，无语义 | — | CONFIRMED |
| :147 | `ud(20.0, W-20)` | [20,236) | 星心均匀分布域 | 结构性不适用 | 边缘留白 20 px > fitRadius 8，避免裁窗截断（:176） | — | CONFIRMED |
| :176 | `p.fitRadius = 8` | 8 px | 本门的 PSF 裁窗半径 | 待确认 | 与模块默认同值（`dpsf_psf.cpp:1017`），但生产侧走配置（`orchestrator.cpp:2289`）且调度侧传 NULL 吃默认（`module_adapters.cpp:4112`）⇒ 见文件 1 CR-33-5；本门取 8 无条文说明 | 保守＝门改为显式声明"夹具域＝出厂 fitRadius"，并覆盖配置侧另一值 | CONFIRMED |
| :177-178 | `p.maxIter=200`，`p.tolerance=1e-8` | 200 / 1e-8 | 死参数（本门照样传） | 待确认（死参） | DISP-PSF-003（`STAR_PSF_ALGORITHMS.md:201`）；本门写它们只为与模块内硬编码同值，改之无效果 | 保守＝本文件加注，免得读者以为门扫了迭代档 | CONFIRMED |
| :183 | `bd = 4.0` | 4 px | 星–真值配对半径 | 结构性不适用 | 40 星/256² ⇒ 平均最近邻距 ~36 px，4 px 不会错配；只影响 matched 计数与样本集合 | — | CONFIRMED |
| :191 | `det[6] = {x,y,0,0,0,0}` | — | 造 star_det v1 行 | 结构性不适用 | [2]–[5] 生产者不消费（`DATA_SEMANTICS.md:732-733`，实测 `dpsf_psf.cpp:1049-1051` 只取 [0]/[1]）；副作用＝本门永远不喂"饱和星"，与 GATES:54 的"非饱和域"一致 | — | CONFIRMED |
| :200 | `truth_index = tx - 0.5` | −0.5 | 连续系→下标系换算 | 单位换算定义常数 | 同 :113-114 | — | CONFIRMED |
| :253 | `cases[] = {{42,40,50},{7,40,50},{42,40,300}}` | 3 场 | 门覆盖的场构型 | **待确认（seed 42 用两次）** | GATES:54 夹具格记 "256×256×40 星×3 场、SNR_peak=50/300" ⇒ 与代码一致；但场 1 与场 3 **共用同一 seed** ⇒ 同噪声实现＋同星位（仅 amp 不同），3 场的样本独立性低于门表读感 | 保守＝第三场换独立 seed；影响范围＝池化统计的自由度（并入 CR-33-14） | CONFIRMED |
| :285-286 | `percentile(psf,0.5) - percentile(sdet,0.5)` | — | [P4] 的有符号 median 差 | 结构性不适用 | 与 GATES:54 公式 `\|median_PSF − median_fallback\|` 同式（有符号，非 median\|d\|）✓ | — | CONFIRMED |
| :311 | `total_matched >= 25` | 25 | [P0] 防恒真下限 | **待确认** | 本文件 :22 自称"防恒真"；GATES:54 **未登记此下限**（判据列只到 median/p95/差）；25/120 潜在星＝21% | 保守＝改为逐场下限（如每场 ≥30/40），否则"一整场检测归零"仍能过（CR-33-14） | CONFIRMED |
| :312 | `fallback_exactness <= 1e-12` | 1e-12 px | [P1] 写读桥往返恒等 | 结构性不适用 | 浮点比较带宽（值域 ~200 px ⇒ 1e-12 ≈ 4e3 ulp，仍远小于任何像素口径错）；但见 CR-33-16 的判别力问题 | — | CONFIRMED |

### 3.2 findings

### CR-33-14 · 三门判据全部按 3 场池化统计，一整场退化到 2.5× 规格仍全绿；[P0] 下限 25/120 也拦不住"一场归零"
- 轴：R（主）／D
- 位点：`p1psf_centroid_gate.cpp:265-272`（`agg[a].sdet.insert(... st.ax[a].sdet ...)` 三场串接）＋ `:282-284`（`sdet_med[a]=median_abs(agg[a].sdet)` 等，均在池化数组上算）＋ `:315-318`（阈值判池化值）＋ `:311`（`total_matched >= 25`）
- 上位依据：`docs/algorithms/GATES_AND_TOLERANCES.md:54` 判据列只写 `median|astro_det − truth| ≤ 0.1 ∧ p95 ≤ 0.3 …`、夹具列写「256×256×40 星×3 场」——**没有规定统计是逐场做还是池化做**；R-3:372 的建议式同样只说"median/p95"。AGENTS.md §5「判据必须非退化」、§9「对'看起来通过'的结果保持怀疑」
- 现状：门只在池化样本上算一次 median/p95，逐场只打印 detected/matched（:226-227），不留逐场残差。我按门自己的分位数实现（:82-85，type-7）复算一个反例：3 场各 40 星（N=120），其中一场整体退化到 median|d|=0.25 px（另两场 ≤0.03 px）⇒ 排序后坏场占据 rank 80..119；`p95` 落在 idx=0.95·119=113.05 ⇒ 取到 0.25 px，**≤0.3 通过**；`median` 落在 idx=59.5 ⇒ 取到好场值 0.03 px，**≤0.1 通过**；[P4]/[P5] 是跨支路同点相减，坏场的共同偏差不影响 ⇒ 四条判据全绿，而样本里 1/3 的星质心精度只有规格的 40%。再叠加 [P0]＝25（21% 的样本量）与 seed 复用（:253 场 1/场 3 同一实现），一整场检测数塌到 0 时只要另两场 ≥25 仍不红。
- 差在哪：登记面说的是"每档 SNR 都要过"的语义（夹具格并列 SNR_peak=50/300），代码做的是"把三档混成一个分布再取分位数"；两者在单档塌陷时给出不同结论，而门表没写清是哪一个（歧义本身即缺陷）。
- 后果：G-P1-CENTROID-1 是 `Y` 档科学门（GATES:54 末列），对外主张"生产路径绝对位置 ≤0.1/0.3 px"；稀释后该主张对单档（尤其低 SNR=50 档，正是真实窄带帧的主档）失去保证，而 CI 读数仍为绿。
- 定级：S2（不改产品字节；触发面为 ctest 常驻门；缺陷是"门判不到"，与 stage A 结论的通读四形态之"门判不到"同类）
- 怎么算修好：判据改为**逐场独立**断言（每场 median≤0.1 ∧ p95≤0.3 ∧ 每场 matched≥30），池化值只作附报；同时在 `GATES_AND_TOLERANCES.md:54` 判据列写明"逐场"，并把 [P0] 的 25→逐场 30 一并登记（防止只改代码造成与门表打架）。第三场换独立 seed。
- 置信：CONFIRMED（分位数取法与稀释结论我按 :82-85 原式逐格算过；"实际不会整场退化"未作证据——那是回归场景假设，不是现状断言）

### CR-33-15 · "生产路径绝对位置门"跑的是一组生产侧并不使用的检测器参数（fitRadius 6 vs 出厂 0=自动），且合同把生产实参指向了另一段子系统代码
- 轴：D（主）／C
- 位点：`p1psf_centroid_gate.cpp:156`「`StarDetectorHandle h = sdet_create(nullptr);`」；对置 `lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp:1565-1578`（显式 `SDetParams`，其中 `:1576` `sdet_params.fitRadius = 0;  // 0 = 自动`）
- 上位依据：`docs/contracts/PUBLIC_API.md:833`「params=NULL→默认（…maxStars=2000/**fitRadius=6**/fwhmClipSigma=3.0/maxAxisRatio=2.0，:963-975）；生产实参 orchestrator.cpp:1593-1612（fitRadius=0=自动半径）」、`:844`「fitRadius 仅驱动 auto 半径推导（ALG-STARDET-001 §11.1）」；本文件 :2/:11 自称「生产路径绝对位置门」「链接真实生产源」
- 现状：`sdet_create(nullptr)` 取默认 ⇒ fitRadius=6；出厂路径取显式参数 ⇒ fitRadius=0（按测得 FWHM 推自动窗）。二者是门控星位置时唯一不同的检测参数，其余八项恰好同值（我逐项对过 :1567-1578 与 PUBLIC_API:833 的默认清单）。另外 PUBLIC_API:833 给的"生产实参"行区间 `1593-1612` 实际是 ipv solver 创建（`fn_ipv_create`/`ipv_set_gaia_handle`，见 :1590-1613），真区间是 `1565-1578`。
- 差在哪：门名与头注把"生产路径"当作已核实的前提，实际只有**代码**是生产源，**配置**是默认档；且合同锚指错位置，按锚核对的人读不到出厂参数。
- 后果：0.1/0.3 px 的对外 attestation 是在固定 6 px 拟合窗下取得的；出厂按 FWHM 自适应开窗（帧间窗宽不同），质心偏差对窗宽的依赖正是被门控住的量 ⇒ 门的达标不能外推到出厂配置。归属：这条不改产品字节，但削弱 G-P1-CENTROID-1 的证据资格（GATES:54 末列 Y）。
- 定级：S2
- 怎么算修好：本门改为用与 :1565-1578 同一份显式 `SDetParams`（含 fitRadius=0）构造检测器，并对 SNR=50/300 两档各加一条"自动窗"断言；若刻意固定 6 px，则头注与 GATES:54 夹具格须写明"检测配置＝fitRadius=6，非出厂 auto"。同批把 `PUBLIC_API.md:833` 的行区间锚订正为 `orchestrator.cpp:1565-1578`。
- 置信：CONFIRMED

### CR-33-16 · [P1] 是两条契约函数的**复合恒等式**，对"两侧同时翻转符号"零判别，其打印与注释措辞强于它所证明的东西
- 轴：R
- 位点：`p1psf_centroid_gate.cpp:312`「`check(fallback_exactness <= 1e-12, "fallback 支路输出 == sdet 原始坐标 (写读桥精确)");`」，其值来自 `:211/:216-217`（`astro_fallback = ipv_detection_from_star_measurement(star_measurement_from_sdet(sdet_px))`，再取 `|astro_fallback − sdet_px|`）
- 上位依据：`GATES_AND_TOLERANCES.md:71` G-P1-CENTROID-BRANCH-ORDER 行：「写端契约：PSF 支路恒等、fallback 支路 `−0.5`；读端统一 `+0.5` … 判据「精确相等（fallback 输出 − sdet 原始坐标 = 0）」，证据列＝`ctest:p1psf_centroid_gate`」
- 现状：P1 核对的是 `f∘g = id`，而登记条文规定的是 `g = x−0.5` 与 `f = x+0.5` 两个**各自**的映射。把两者同时改成 `g=x−0.4, f=x+0.4`（或都取 +0.5）时 P1 仍 0 差，全绿。
- 差在哪：登记面声称锁住"写端 −0.5／读端 +0.5"，实现只锁住"两者互逆"。本文件里唯一钉住绝对符号的是 P2/P3/P5，而它们走的是 `truth_px` 域，且 [P1] 的这条 check 文案（"写读桥精确"）会被读者当成已核对单侧符号。
- 后果：G-P1-CENTROID-BRANCH-ORDER 的四个映射方向在本门里只有 P5 对 dpsf 支路构成绝对约束；若 `star_coord_contract.h` 的 sdet/ipv 两侧被对称改写，登记面声称已被核对，实际只有 P2 的绝对位置（依赖 truth 与 sdet 系一致）会红——而 P2 的红会被读成"质心退化"而不是"桥方向翻转"，诊断指向错。
- 定级：S3
- 怎么算修好：[P1] 增加单侧断言（直接对契约函数喂已知值断言 `star_measurement_from_sdet(v) == v-0.5`、`ipv_detection_from_star_measurement(v) == v+0.5`，如文件 2 的 [C1] 那样），并把文案改成与判据同范围。
- 置信：CONFIRMED

### CR-33-17 · 科学常数 1.230310 在生产者、SNR 消费者、测试三处各抄一份，无单一权威定义点，脱钩后无门可 catch
- 轴：C（主）／S
- 位点：`p1psf_centroid_gate.cpp:59`（`constexpr double kMoffat4FwhmFactor = 1.230310;`）；另两份：`lib/algorithms/psf/src/dpsf_psf.cpp:25`（`static const double MOFFAT4_FWHM_FACTOR = 1.230310;`）、`lib/algorithms/noise_snr/cpp/src/snr_estimator.cpp:40`（`constexpr double kMoffat4FwhmFactor = 1.230310;`）
- 上位依据：`docs/algorithms/STAR_PSF_ALGORITHMS.md:199` DISP-PSF-001 只登记了「参数向量序…**与 MOFFAT4_FWHM_FACTOR=1.230310 常数依赖，重构时序耦合」这一"时序耦合"面，未覆盖跨模块三份副本；`docs/contracts/DATA_SEMANTICS.md:762` 把 `[7]/[8] = 1.230310·sx/sy` 写成合同条文；`snr_estimator.cpp:59` 注明 `sigma_px = fwhm / 1.230310 [SCI-PSF FWHM=1.230310*sigma]`
- 现状：因子是 β=4 Moffat 的导出常数（我复算成立），但它作为 `static/constexpr` 分别藏在三个 TU 的匿名/内部作用域里，`dpsf_psf.h:4` 只在注释里提它，没有任何公共头或登记表导出这一个数；跨模块用法还互为**逆运算**（生产 `fwhm = factor·σ`，SNR `σ = fwhm/factor`），两处必须严格同值才不失真。
- 差在哪：一个被合同条文点名的科学常数，其定义点是三份互不引用的实现细节；改一处不改另两处不会编译失败、不会被 psf 套件的"FWHM=1.230310·σ 恒等复核"（`STAR_PSF_ALGORITHMS.md:209`，只在 psf 套件内）发现。
- 后果：若生产者侧因子改动（例如有人"订正"成高斯 2.3548）而 SNR 侧不动，`sourceSnrFromPsfRow` 反解出的 `sigma_px` 会偏小 1.9140× ⇒ 归一 PSF 更尖 ⇒ `ΣP_i²` 偏大 ⇒ 逐源最优提取 SNR 被**高估**（方向：偏绿），直接落到创新点 SCI-B"跨帧绝对 SNR"的对外数值上；本门的 [P2]–[P5] 只测质心，抓不到。
- 定级：S2（当前三处同值，未产错值；缺陷是"无单一定义点＋无跨模块一致性门"，属可致产物错值的敞口）
- 怎么算修好：把因子提到公共权威位（例如 `lib/include` 的 PSF 常量头或合同生成面），三处 TU 同改引用；并加一条合同/单元断言：用生产 `dpsf_fit_batch_f64` 产出的行喂 `snr_extract_model_v3`，核 `sigma_px` 反解与拟合 sx 一致到 1e-9（现成的 p1psf 套件位置即可挂）。改公共头会牵动 ABI 面，须与 `PUBLIC_API.md:619` 同步。
- 置信：CONFIRMED（三份副本与两处用法均逐字读到；"SNR 偏绿"的定量方向按 `SNR_F = F·sqrt(ΣP²)/σ_sky` 判定，未实跑）

## 4 · `lib/algorithms/psf/tests/p1psf/p1psf_fixtures.hpp`（240 行，整读；另读其在 p1psf_tests_core 的消费点）

先记两项正证（本批必查项）：
1. **坐标口径自洽**：本文件三处采样器（:88、:117、:141）一律以裸下标 `(double)x,(double)y` 喂 `moffat4_eval`，即 **index-is-center** 系，与 `dpsf_*` 输出系同框；而 `p1psf_centroid_gate.cpp:113-114` 的造场用 `(x+0.5)`（连续/truthFITS 系）并另设 `truth_index=t−0.5` 换算（:200）。两套约定**各自闭合、且各自与自己所断言的框一致**，我核对了"贴边星 (0.5, 47.5)"：48 图、fitRadius=8 ⇒ 窗 `x∈[0,9) y∈[39,48)`，中心落在 patch 局部 (0.5, 8.5)（y 方向越出末像元中心 0.5 px），而 `p1psf_tests_core.cpp:133-135` 断言回收 1e-3 px 达标——无噪声解析场下 LM 能反解，故**不构成缺陷**，但"单侧外推"这一性质未在该 fixture 注释里写明（:157 只写"贴边星"）。
2. **fixture 声称的失效码确实在跟踪测试面兑现**（不是注释自证）：E 组 constant/all-NaN/center-NaN → `core:641-699` 断言 `status==DPSF_FIT_INVALID_PARAMS`；D 组 wide(σ=5.5) → `core:1026` 断言 `status==DPSF_FIT_NO_CONVERGENCE`。

### 4.1 §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :32 | splitmix64 增量 | 0x9E3779B97F4A7C15 | PRNG 状态推进 | 结构性不适用 | 黄金比 64 位常数，伪随机谱系实现细节，不进入任何科学判据（:8-10 声明其用途＝逐 bit 可复现） | — | CONFIRMED |
| :34-35 | 混淆乘数 | 0xBF58476D1CE4E5B9 / 0x94D049BB133111EB | splitmix64 标准乘子 | 结构性不适用 | 同上 | — | CONFIRMED |
| :34-36 | 移位量 | 30 / 27 / 31 | xorshift 三段 | 结构性不适用 | 位移量（派单 §4 明列为结构性常数） | — | CONFIRMED |
| :40 | `>> 11` 与 `9007199254740992.0` | 11 / 2^53 | 64 位→[0,1) 双精度映射 | 单位换算定义常数 | 2^53 为 double 精确整数域上界；11＝64−53 位截断 | — | CONFIRMED |
| :47 | `-2.0`、`2.0*M_PI` | — | Box–Muller 变换式 | 公式导出 | 本文件 :10 声明"Box-Muller 从 uniform 流构造 Gaussian"；代码确按式 `√(−2lnu₁)·cos(2πu₂)` 算（合规） | — | CONFIRMED |
| :50 | `kFwhmFactor` | 1.230310 | PSF σ↔FWHM 因子（测试侧副本） | **公式导出（但见 CR-33-18）** | 权威条文 `docs/science/PSF.md:74-80`：闭式 `2√2·√(2^{1/4}−1)=1.230307652590102`，实现常量与之相对差 **+1.91e-6**；适用域"仅 β=4"（:78）。本行注释给锚"(PSF.md)"✓ 存在。**但本文件内未见引用点，且套件恒等复核用的是另一名字 `kMoffat4FwhmFactor`（core:587）** | 保守方向＝删死副本或改为引用单一权威常量（CR-33-17）；影响范围＝多一份需要跟人改的数 | CONFIRMED（定义与锚）／PARTIAL（是否全仓无使用，待文件 5 核对） |
| :55 | `default_params(int fit_radius = 8)` 的 8 | 8 px | 拟合窗半宽 | **待确认** | 合同面只说 NULL→默认 8（`DATA_SEMANTICS.md:730`），**无任何一份权威给"为什么是 8"**；`docs/science/PSF.md` 全文对 fitRadius 无规定（检索式：`git grep -n "fitRadius\|fit_radius" -- docs/science`）。它的科学后果是可算的：拒绝判据 `fwhm > rect`（`dpsf_psf.cpp:396`，rw=2·8+1=17）⇒ 默认配置下 **FWHM ≥ 17 px 的星一律 NO_CONVERGENCE** | 保守方向＝把"可测 FWHM 上界 = 2·fitRadius+1"写进 `docs/science/PSF.md` 并给选型依据；生产侧另有配置通道（`orchestrator.cpp:2289`）但调度入口传 NULL 吃 8（`module_adapters.cpp:4112`，见 CR-33-5） | CONFIRMED |
| :58-59 | `maxIter=200`、`tolerance=1e-8` | 200 / 1e-8 | 透传的死参数 | 待确认（死参） | `STAR_PSF_ALGORITHMS.md:201` DISP-PSF-003；本文件 :52-54 **已如实披露**"实际无效"——对照：权威签名头 `dynamic_psf.h:81/147/191` 未作同样披露（CR-33-5 相关） | — | CONFIRMED |
| :80 | `fix_psf_a` 默认 `B=50, A=900, cx=cy=20, sigma=2.0, w=h=40` | 见左 | 圆单星解析场 | 结构性不适用 | fixture 自订参数（派单 §4）；值域安全：峰值 950 < 65535，FWHM=2.4606 px ≪ 窗 | — | CONFIRMED |
| :83,:90,:92 | 三份同源 `f64 / f32 / u16` | — | 同一场的三种 dtype 表示 | **待确认（u16 那份非"同源"）** | :92 `fx.u16[i] = (uint16_t)fx.f64[i];` 为**截断**（非舍入），f32 那份是舍入；`p1psf_tests_core.cpp:140-143` 已如实记"u16 走截断、f32 走舍入，两通道不 bitwise 相等"⇒ 测试面自洽，但本文件 :69 的"三份同源数据"措辞过强 ⇒ CR-33-20 | 保守方向＝措辞改"三份同源（u16 截断量化）"；影响范围＝任何以 u16 场反推 σ/A 精度的断言 | CONFIRMED |
| :92 | `(uint16_t)` 值域 | 0 / 65535 | 隐式域假设 | 待确认 | 无守卫：`fix_psf_a` 的 B/A 是**默认实参**，任何调用方给 A>65535 或负值时按 C++ 浮点→无符号整型转换规则为**未定义行为**（非"回绕"）；`[conv.fpint]` | 保守方向＝加 `assert(v >= 0 && v < 65536)`（CR-33-20）；现状所有调用点值域安全 | CONFIRMED |
| :112 | `bkg_mean + bkg_sigma*gauss` | 形参 | 噪声背景场 | 结构性不适用 | 形参 | — | CONFIRMED |
| :117 | `moffat4_eval(0.0, s.A, …)` | 0.0 | **丢弃 `PsfStar::B`** | 待确认 | 与 :88/:141（用 `star.B`）不一致 ⇒ CR-33-19；断言侧 `core:584` 却拿 `s.B` 当 B 真值 | 保守方向＝`FixPsfB` 增记 bkg 真值并让 oracle 断言对它；影响范围＝O1 组 B 回收断言 | CONFIRMED |
| :144 | `(int)(uniform*(w*h))` | — | 随机污染像元下标 | **待确认** | 可重叠 ⇒ 实际 NaN 像元数 ≤ `n_nan`；且全域撒点不保证落进拟合窗（窗仅 17×17）；本文件 :123 只说"seed 选污染像素" | 保守方向＝用无放回抽样并把 `nan_idx.size()` 作为真值上报（现在 `fx.nan_idx` 已含重复项，可核）；影响范围＝C 组（NaN 污染）断言的"污染数"语义 | CONFIRMED |
| :145,:200,:203 | `std::nan("1")` | payload 1 | NaN 标记 | 结构性不适用 | payload 不参与任何比较（生产侧 `dpsf_psf.cpp:290` 只用 `isfinite`） | — | CONFIRMED |
| :169 | `edge = fix_psf_a(100,2000, 0.5, 47.5, 2.0, 48, 48)` | 见左 | 贴边星 | 结构性不适用 | fixture 自订；本表上方正证 1 已核其窗口与断言一致 | — | CONFIRMED |
| :170-172 | `wide σ=5.5`、`sharp σ=0.45`、`wide_fail σ=5.0`（均 B=100、A=2000/5000、96×96） | 见左 | 三条失效路径的样本 | 待确认 | :158-163 以"probe 实证 st=1/3"为据；跟踪面兑现见 `core:1026`（σ=5.5→码 1）、:831/:1023 组，但 **sharp→ITERATION_LIMIT(3)** 的断言我未在 core 的 D 组命中（只核到 :1026 的 NO_CONVERGENCE 与 E 组三条） | 保守方向＝若 3 码在跟踪面无断言则本 fixture 的一项声称未兑现（§11.2:189 恰规定 3 的处置）；影响范围＝码 3 的对外语义 | PARTIAL（未在 core 内定位到 `status == DPSF_FIT_ITERATION_LIMIT` 断言，检索式：`git grep -n "ITERATION_LIMIT" -- lib/algorithms/psf/tests`，未穷尽 perf/selfcheck） |
| :178-183 | FIX-PSF-E 四条失效声称 | 码 1/2 | 负面矩阵说明 | 结构性不适用 | 与 `core:641-699` 断言逐条对上（本表正证 2） | — | CONFIRMED |
| :189,:194,:197 | `w=h=32`、`B=120.0` | 32 / 120 | E 组场尺寸与常数底 | 结构性不适用 | fixture 自订 | — | CONFIRMED |
| :196-200 | 17×17 全 NaN patch，`y,x ∈ [8,25)` | 8/25 | "fitRadius=8 的完整拟合窗" | 公式导出（已核） | 中心 (16,16) ⇒ `x0=max(0,16−8)=8`、`x1=min(32,16+8+1)=25` ⇒ 恰 17×17 ✓ 注释与实现一致 | — | CONFIRMED |
| :203 | `nan_center[16*w+16]` | 16 | 中心像元 | 结构性不适用 | 与 :194 尺寸联动的像元下标 | — | CONFIRMED |
| :216 | `seed = 20260909ull`、`grid=5`、`w=h=128` | 见左 | 性能负载构型 | 结构性不适用 | 日期形 seed 只是标识作用（可复现性由固定值保证）；规模属性能夹具自订 | — | CONFIRMED |
| :223-224 | `16.0 + (w-32.0)*gx/(grid-1.0)` | 16 / 32 / grid−1 | 网格布点 | **待确认（除零敞口）** | `grid==1` 时 `(grid-1.0)=0` ⇒ inf/nan 坐标；`grid` 是默认实参（:216），当前调用点均为 5 | 保守方向＝`grid >= 2` 断言；影响范围＝仅性能组 | CONFIRMED |
| :225-226 | `A=600+40(gx+gy)`、`sx=1.5+0.1gx`、`sy=1.3+0.1gy`、`θ=0.05(gx+2gy)` | 见左 | 形状扫描谱 | 结构性不适用 | 扫参网格步长（实现选择）；我核过其安全域：σ∈[1.3,1.9] ⇒ FWHM∈[1.60,2.34] px ≪ rw=17 ⇒ 不触发 `fwhm>rect` 拒绝；σ > 0.3 下界 ✓ | — | CONFIRMED |
| :229 | `fix_psf_b(seed, w, h, 80.0, 3.0, stars)` | 80 / 3 ADU | 性能场的背景与噪声 | 待确认 | 注意此处传 bkg_mean=80 而 :225 的 star.B=100 ⇒ 两者不等，正落在 CR-33-19 的错配面上（性能组若将来加 B 断言即错 20 ADU） | 保守方向＝同 CR-33-19 修法 | CONFIRMED |

### 4.2 findings

### CR-33-18 · FWHM 因子常量的验证证据锚指进 `run/`（跟踪面为零），且测试侧"恒等复核"与实现共用同一圆整副本，闭式精确值无任何可执行核对
- 轴：S（主）／C
- 位点：`p1psf_fixtures.hpp:50`「`inline constexpr double kFwhmFactor = 1.230310;  // MOFFAT4_FWHM_FACTOR (PSF.md)`」；证据锚 `docs/science/PSF.md:81`「证据：`run/SCI-FIX-STARPSF-01/results/e1_constants.txt` §A。」；测试断言 `p1psf_tests_core.cpp:587`「`rel_err(r.fwhm_x, kMoffat4FwhmFactor * r.sx) <= tol().fwhm_rel`」
- 上位依据：`docs/science/PSF.md:74-77`「精确值 `2√2·√(2^{1/4}−1) = 1.230307652590102`（FP64 闭式，可复算…）；实现常量 `MOFFAT4_FWHM_FACTOR = 1.230310` 与精确值相对差 **+1.91e-6**（…远小于 §9 的 1% 容差）」；工作包 AUD-402 §2.5 处置档 3 明文「标定件路径与结果位**必须落在跟踪面，指 `run/` 即判不合格**」；负责人已裁证据口径「运行产物一律不留」（三留一不留）
- 现状：该常数的科学条文本身在跟踪面且给了闭式（这点合格），但① 它把"已核实"的证据位写成 `run/...e1_constants.txt`——按仓库自己的证据裁定，该目录不入库、每轮回收，即证据不可复核；② 全仓 C++ 侧对该数的实现是圆整值 1.230310（`dpsf_psf.cpp:25`、`snr_estimator.cpp:40`、`snr_science.cpp:54`、`p1snr_science_test.cpp:53`、`p1psf_centroid_gate.cpp:59`、本文件 :50），而测试的"恒等复核"（core:587，登记为 `STAR_PSF_ALGORITHMS.md:209` 的 oracle 项）拿的也是同一圆整值，两边同值 ⇒ 该断言只能查列接线，查不到圆整量级；③ 没有任何一处按闭式 `2√2·√(2^{1/4}−1)` 现算并与实现常量比较。
- 差在哪：一个被 §2.5 归入"公式导出"的量，其"代码是否真按式算"的答案是"按式的圆整值算"，而唯一声称核对它的两条面（run 里的探针、测试恒等复核）分别不可复核与自指。
- 后果：不改变产品数值（1.91e-6 远小于 1% 容差）；被削弱的是对外主张"FWHM 常数已核"的凭据链——`README.md:95`、`module.yaml` 与 §11.4 都把它当已核事实引用。若将来有人把实现值"订正"为闭式值（−1.91e-6），无一面会红；反之写错成 1.2303 也无人红。
- 定级：S3
- 怎么算修好：把 `PSF.md:81` 的证据位改指跟踪面（`实验/` 下的单元或本套件的 results，按负责人"日志落 `实验/`"裁定），并在 oracle 里加一条**按闭式现算**的断言：`rel_err(kMoffat4FwhmFactor, 2*sqrt(2)*sqrt(pow(2,0.25)-1)) <= 1e-5`——同批必须先把六份副本收成一份权威定义（CR-33-17），否则这条新断言只锁住其中一处。
- 置信：CONFIRMED（六处副本、闭式值与差值、证据锚文本均逐字读到；"订正不红"是按判据形式推得）

### CR-33-19 · `fix_psf_b` 静默丢弃 `PsfStar::B`，而同一族的 oracle 断言把 `s.B` 当 B 真值——真值与注入值只靠调用方巧合相等
- 轴：C（主）／R
- 位点：`p1psf_fixtures.hpp:117`「`fx.f64[...] += moffat4_eval(0.0, s.A, s.cx, s.cy, …)`」（对比 :88 与 :141 分别传 `B` 与 `star.B`）；断言侧 `p1psf_tests_core.cpp:584`「`std::fabs(r.B - s.B) <= 0.5`」，其数据来自 `core:545-548`（星谱 `B=100.0`，`fix_psf_b(..., 100.0, 3.0, stars)` 的 bkg_mean 也是 100.0）
- 上位依据：本文件 :14-15 的自述契约「所有科学真值由 moffat4 采样器生成——**真值先于被测函数存在，期望值不来自被测函数**」；`STAR_PSF_ALGORITHMS.md:217`「容差来源：fixtures generator 注记（11 号标准 §5 元数据），不用本文手抄值」
- 差在哪：`PsfStar::B` 在 A/C 两个 fixture 里是"注入的场背景"，在 B 里是被忽略的死字段——同一结构体字段两种语义。O1 断言的真值 `s.B` 并不是造场用的数，二者当前靠调用方写同一个 100.0 才成立；而 `fix_psf_perf`（:225/:229）已经把它写成 100 对 80 的**不等**组合，说明这个巧合没有任何机制保证。
- 后果：① 任何人调 bkg_mean 而不改星谱（或反之）会得到一条与实现无关的红灯（"改对反而判红"），或反过来把真背景当被测缺陷；② 本文件对"期望值不来自被测函数"的承诺在 B 通道上不成立——B 真值实际来自 bkg_mean，而测试读的是另一处。
- 定级：S3
- 怎么算修好：给 `FixPsfB` 增记 `bkg_mean/bkg_sigma` 作为 B 真值字段，`core:584` 改断 `fx.bkg_mean`；同时二选一处理 `PsfStar::B`——要么在 `fix_psf_b` 里 `assert(s.B == bkg_mean)`（把它变成校验），要么把 `PsfStar` 拆成"星"与"场"两层，别在星里留场量。
- 置信：CONFIRMED

### CR-33-20 · "三份同源数据"里的 u16 份是截断且无值域守卫，越界时是未定义行为而非回绕
- 轴：R
- 位点：`p1psf_fixtures.hpp:92`「`for (size_t i = 0; i < fx.f64.size(); ++i) fx.u16[i] = (uint16_t)fx.f64[i];`」＋ :69 的声称「f64/f32/u16 三份同源数据」
- 上位依据：`docs/contracts/PUBLIC_API.md:655`「cx/cy/fitRadius/sx/sy/fwhm 像素；theta 弧度；B/A/flux/mad ADU」；`dynamic_psf.h:69` 对 float 通道明确"不做 0-65535 clip"（即 0-65535 是 u16 通道的定义域）；`p1psf_tests_core.cpp:141-143` 已如实记 u16 截断/f32 舍入的 bitwise 差异
- 现状：`fix_psf_a` 的 B、A 都是**默认实参**（:79-80），u16 转换无 `isfinite`、无 `>=0`、无 `<65536` 检查；截断本身与 f32 的舍入不对称（f64→u16 平均偏低 0.5 ADU），但该项与背景参数 B 简并、被拟合吸收，故对 cx/cy/fwhm 无系统影响（我核过：整场平移常数由 LM 的 B 列吃掉）。
- 差在哪：越界时按 C++ 的浮点→无符号整型转换规则属**未定义行为**（不是"回绕"），而本文件把它写成一条无条件的"同源"通道；现状所有调用点安全，安全性的唯一来源是调用方没写大数。
- 后果：`dpsf_fit_batch`（uint16 旧 API）是兼容通道，仓库里任何新用例复用 `fix_psf_a` 做大振幅（如 A=70000 模拟饱和）会拿到静默 UB 的 u16 场，且 f64/f32 两份仍是正确的 ⇒ 三条通道"同源"不成立时表现为**只有 u16 通道随机异常**，最难归因。
- 定级：S3
- 怎么算修好：:92 之前加 `assert(std::isfinite(v) && v >= 0.0 && v < 65536.0)`（或改 `static_cast` 为带饱和的显式 `dpsf_clip_u16`），并把 :69 措辞改为"三份同源（u16 为截断量化，值域 0-65535）"。
- 置信：CONFIRMED（截断与简并性我按常数平移对 LM 的影响判过；UB 结论按语言规则）

## 5 · `lib/algorithms/psf/tests/p1psf/p1psf_oracle.hpp`（141 行，整读）

本批必查项的最强正证在此：**oracle 与生产源的二次型确实是两条独立代数路线，我逐项做了代数对账。**
oracle `:40` 走主轴投影 `Q = 0.5(u²/sx² + v²/sy²)`，`u=c·dx+s·dy, v=−s·dx+c·dy`；
生产 `dpsf_psf.cpp:97-106` 走 `Q = p1·dx² + 2p2·dxdy + p3·dy²`。把前者展开：
`dx²` 系数 `= 0.5(c²/sx² + s²/sy²) = p1` ✓；`2dxdy` 系数 `= cs(1/sx² − 1/sy²)`，
而 `2p2 = 2[sin2θ/(4sx²) − sin2θ/(4sy²)] = cs(1/sx² − 1/sy²)` ✓；`dy²` 系数 `= p3` ✓。
两侧仅在数学定义上等价（:11 的自述成立）。偏心率式（:60）取 `√(1−(smin/smax)²)`，
与 `DATA_SEMANTICS.md:749`／`docs/science/PSF.md:21` 一致——这同时**反证 CR-33-2**：
调度器 `module_adapters.cpp:4143` 的 `1−mn/mx` 才是与冻结定义不同的那个量。

### 5.1 §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :20 | `M_PI` | 3.14159265358979323846 | π 兜底 | 结构性不适用 | 数学常数 | — | CONFIRMED |
| :28 | "β=4, α=√2σ" 的 `4` 与 `√2` | 4 / 1.41421 | Moffat 形状参数与其 α–σ 换标 | **公式导出** | 我复核 α=√2σ：Moffat 二阶矩 `⟨r²⟩=2α²/(β−2)`，β=4 时 `=α²`；高斯同量 `=2σ²` ⇒ `α=√2σ` ✓ 与 `dpsf_psf.cpp:23` 同结论；β=4 属项目冻结（`STAR_PSF_ALGORITHMS.md:231` Moffat 1969 为族出处） | — | CONFIRMED |
| :40 | `0.5*(u²/sx²+v²/sy²)` 的 `0.5` | 0.5 | `Q = r²/(2σ²)` 定义常数 | 公式导出 | 与本表上方对账一致（生产侧 `inv_sx2 = 1/(2sx²)`） | — | CONFIRMED |
| :41 | `std::pow(1.0 + Q, 4.0)` | 1.0 / 4.0 | Moffat 分母 `(1+Q)^β` | 文献值（β 由项目冻结） | Moffat 1969, A&A 3, 455（`STAR_PSF_ALGORITHMS.md:231`）；β=4 冻结见 `docs/science/PSF.md §5` 与本文件 :28 | — | CONFIRMED |
| :49 | `oracle_flux` 的 `2.0*M_PI/3.0` | 2π/3 | β=4 整平面解析积分 | **公式导出（但为死函数）** | 我独立积分：`∫A/(1+Q)⁴ dA = 2πA·sx·sy/(β−1) = 2πA·sx·sy/3` ✓ 值对；`docs/science/PSF.md:85-86` 并对任意 θ 给出 `det M` 不变量论证。**全仓无调用点** ⇒ CR-33-22 | 保守方向＝判据改调 `oracle_flux` 并加一条数值积分对照 | CONFIRMED |
| :53 | `kMoffat4FwhmFactor` | 1.230310 | oracle 侧 FWHM 因子 | **公式导出（写死圆整值，非现算）** | 同一行的注释就写着闭式 `2√2·√(2^{1/4}−1)`；权威 `docs/science/PSF.md:74` 给精确值 `1.230307652590102` 并记实现值相对差 **+1.91e-6** ⇒ CR-33-21（与实现的圆整值同抄，恒等判据失去对该常数的判别力） | 保守方向＝oracle 侧改按闭式现算并给 1e-5 带（`2*sqrt(2)*sqrt(pow(2,0.25)-1)`）；影响范围＝fwhm 列的一切"已核"主张 | CONFIRMED |
| :60 | `1.0 - (smin/smax)^2` 开方 | 1.0 / 2 | 偏心率定义 | 公式导出 | `DATA_SEMANTICS.md:749`、`docs/science/PSF.md:21`；代码确按式算（合规），并与 `dpsf_psf.cpp:431-432` 同式 | — | CONFIRMED |
| :74-77 | `M[0]=c²a+s²b` 等 | — | 旋转协方差矩阵元 | 公式导出 | `M = R diag(sx²,sy²) Rᵀ` 逐元展开 ✓ 我核过对称性 `M[1]=M[2]` | 注意：本文件的 `M` 与 `docs/science/PSF.md:86` 的 `M=[[p1,p2],[p2,p3]]`（`det M=1/(4sx²sy²)`）**互为逆量纲**（此处 `det=sx²sy²`），同名不同物，已记入 CR-33-21 附带项 | CONFIRMED |
| :82 | `rel_err` 分母地板 | 1e-30 | 防除零 | 结构性不适用 | 纯数值地板；:85-86 已如实声明其后果（want≈0 时发散，故另设 hybrid） | — | CONFIRMED |
| :90 | `close_hybrid` 的 `std::max(1.0, |want|)` | 1.0 | 混合容差的尺度地板 | **待确认（单位敏感）** | 无条文规定"以 1 个单位以下为绝对带"；`docs/standards/TEST_STANDARD.md:7` 只要求"容差＋说明来源" | 方向可判定：`max(1,|want|) ≥ |want|` ⇒ 带恒 **不小於** 相对带 ⇒ 恒偏绿。σ=0.45 的 sharp 星（`p1psf_fixtures.hpp:171`）`Mxx=0.2025`，`m_rel=3e-3` 退化为绝对带 ⇒ 等效相对容差 **1.48%**，而 σ=2 时是 0.3%（松 4.9×）。⇒ CR-33-23 | CONFIRMED |
| :101-105 | 特征值式 `0.5(M00+M11) ± √(...)` | 0.5 | 2×2 对称阵闭式谱 | 公式导出 | 闭式正确 ✓（我按 `λ = m ± √(d²+Mxy²)`，`m,d` 为半和/半差核对）；`max(0.0,·)` 为数值地板 | — | CONFIRMED |
| :124 | `b_abs` | 1e-6 | 无噪声 B 回收绝对容差（ADU） | **待确认** | 唯一来源是 :110-111 的一句"probe, 2026-09-09 实测 \|ΔB\|≤7e-9"——**无路径、无跟踪面标定件**；规则载体 `11_MODULE_SOURCE_TEST_STANDARD.md §5` 在跟踪面不存在 ⇒ CR-33-24 | 保守方向＝补跟踪面锚（`实验/` 或 results 登记）；影响范围＝7 项容差全部（同一来源句） | CONFIRMED |
| :125 | `a_rel` | 5e-6 | A 回收相对容差 | 待确认 | 同上，且自带"实测 1.46e-6@A=3000，留 ~3.4× 余量"——余量系数 3.4 亦无来源；我核对其算术 `5e-6/1.46e-6=3.42` ✓ 自洽 | 同上 | CONFIRMED |
| :126 | `cen_abs` | 1e-9 px | 质心绝对容差 | 待确认 | 同上（实测 ≤1e-14 ⇒ 余量 1e5×）。:114-121 已明文禁止用它冒充 SCI 验收（0.3 px @SNR≥20），这条自我设限是**好的**，但正向锚仍缺 | 保守方向＝维持 :116-121 的隔离声明＋补 probe 路径 | CONFIRMED |
| :127-130 | `s_rel`/`m_rel`/`fwhm_rel`/`flux_rel` | 1e-6 / 3e-3 / 1e-12 / 1e-12 | 形状/恒等式容差 | 待确认 | 前两项同 :124 问题；后两项注释自认"同一 double 乘法 / oracle 同式重算"⇒ 判据是**接线检查**而非公式检查（正是 CR-33-21/22 的落点）。`fwhm_rel=1e-12` 与 `+1.91e-6` 的常数差量级冲突：若实现改用闭式，`rel_err(1.2303076,1.230310)=1.9e-6 > 1e-12` ⇒ 该门会**判红一个正确修复** | 保守方向＝把 1e-12 的语义写清为"同式接线检查"，另立一条按闭式的常数核对（带 1e-5）；影响范围＝未来任何常数订正都会撞红 | CONFIRMED |

### 5.2 findings

### CR-33-21 · "独立 oracle"的 FWHM 因子抄的是实现的圆整值，且 `fwhm_rel=1e-12` 使恒等判据对该常数零判别，还会把"改用闭式"这一正确修复判红
- 轴：S（主）／R
- 位点：`p1psf_oracle.hpp:52-54`「`// FWHM 因子: 2√2·√(2^{1/4}−1) = 1.230310 (MOFFAT4_FWHM_FACTOR)` / `inline constexpr double kMoffat4FwhmFactor = 1.230310;` / `inline double oracle_fwhm(double sigma) { return kMoffat4FwhmFactor * sigma; }`」＋ `:129`「`double fwhm_rel = 1e-12;  // fwhm_x = kFwhmFactor·sx 恒等式 (同一 double 乘法)`」＋判据 `p1psf_tests_core.cpp:587`
- 上位依据：`docs/science/PSF.md:74-77`「精确值 `2√2·√(2^{1/4}−1) = 1.230307652590102`（FP64 闭式，可复算）；实现常量 `MOFFAT4_FWHM_FACTOR = 1.230310` 与精确值相对差 **+1.91e-6**」；`docs/algorithms/STAR_PSF_ALGORITHMS.md:210`「独立参考不调用生产 symbol（11 号标准 §5）」；本文件 :8-12 的独立性自述
- 现状：注释把闭式与圆整值写成等号（实际差 1.91e-6 相对），`oracle_fwhm` 用圆整值；判据 `rel_err(r.fwhm_x, kMoffat4FwhmFactor*r.sx) ≤ 1e-12` 两侧同因子 ⇒ 只能查"是否乘了同一个数"，查不到"这个数对不对"。设计面（§11.4:209）却把它登记为"FWHM=1.230310·σ 恒等复核"的证据项。
- 差在哪：一项被登记为"独立复核"的科学常数检查，其参考值与实现值同源同值；且 `1e-12` 的量级选择与条文承认的 `1.91e-6` 圆整差冲突——按条文自己给的精确值去改实现，本判据必红。
- 后果：① FWHM 常数（进入块 A 的 [5] 列与 SNR 的 σ 反解，见 CR-33-17）在 CI 上无任何有效核对；② 未来"按闭式订正常数"这一正确动作会被 p1psf 判红，形成"改对反而判红"的典型陷阱（AGENTS §9 明禁的就是这种门）。
- 定级：S2（不产错值——现值正确；破坏的是核对能力与修复通道）
- 怎么算修好：`oracle_fwhm` 改按 `2*std::sqrt(2.0)*std::sqrt(std::pow(2.0,0.25)-1.0)` 现算；判据拆两条——(i) 接线：`rel_err(r.fwhm_x, kMoffat4FwhmFactor*r.sx) ≤ 1e-12`（保留，语义改注为"同式接线"）；(ii) 常数：`rel_err(kMoffat4FwhmFactor, 闭式) ≤ 1e-5`。同批须先把六份 1.230310 副本收成单一权威定义（CR-33-17），否则 (ii) 只锁住 oracle 自己。
- 置信：CONFIRMED（三处文本、闭式与 1.91e-6 的算术我都核过）
- 附带项（同轴 S3）：本文件 :64-77 的 `M` 是协方差阵（`det = sx²sy²`），而 `docs/science/PSF.md:86` 的 `M=[[p1,p2],[p2,p3]]` 是其逆的一半（`det = 1/(4sx²sy²)`）；两处同名异义，且 :66 引的实现锚 `dpsf_psf.cpp:19-30` 实际是 FWHM 因子注释块＋`SamplePixel`，二次型系数在 `:94-101`/`:228-233`。修法：oracle 侧改名 `cov`，并把 `PSF.md` 的 `M` 记作 `M⁻¹` 或反之（一次改两侧，避免按 `det` 不变量核对的人取反）。

### CR-33-22 · `oracle_flux` 定义后全仓无调用；实际通量判据内联重算与被测方同一表达式 ⇒ β=4 解析积分式本身零独立核对
- 轴：S（主）／D
- 位点：`p1psf_oracle.hpp:47-50`（`oracle_flux` 定义）；实判据 `p1psf_tests_core.cpp:588`「`rel_err(r.flux, 2.0 * M_PI * r.A * r.sx * r.sy / 3.0) <= tol().flux_rel`」；被测侧 `dpsf_psf.cpp:429`「`double flux = 2.0 * M_PI * A * sx * sy / 3.0;`」
- 上位依据：`docs/science/PSF.md:186`「β=4 解析通量 flux=2πA·sxsy/3 与 FWHM/σ=1.230310：**Project-defined 解析积分**……**建议用独立符号/数值积分（SciPy quad 或 sympy，BSD-3-Clause）复算，不作文献引用**」；`STAR_PSF_ALGORITHMS.md:209-210`（oracle 须"独立参考不调用生产 symbol"）
- 现状：判据两侧是字面同一表达式（同常数、同乘序），`flux_rel=1e-12`，注释亦自认"oracle 同式重算"；文件 :48 提供的独立函数没人调用；权威文档要求的"数值积分复算"在 `lib/algorithms/psf` 与 `eng/tests/unit/p1psf` 内检索 0 命中（检索式：`git grep -n -i "quad\|sympy\|数值积分" -- lib/algorithms/psf eng/tests/unit/p1psf`）。
- 差在哪：通量是交付列（块 A [2]，ADU，下游星等零点输入），其公式只有"自己对自己"的一致性检查；`oracle_flux` 的存在让 README/设计面读起来像已有独立 oracle。
- 后果：若解析式本身写错（例如 β 相关因子记成 2 或漏 `sx·sy`），全套 p1psf 仍绿；受影响的对外主张是"PSF 通量＝解析积分"（`DATA_SEMANTICS.md:786` 明文"flux 为解析积分，非计数"）。我手工积分确认**现值正确**（见 §5.1 :49 行），所以本条指控的是核对缺失，不是当前错值。
- 定级：S2
- 怎么算修好：判据改调 `oracle_flux`，并新增一条按 `PSF.md:85-86` 的 `det M` 不变量做的数值二重积（在测试侧用梯形/Simpson 网格或直接引入 scipy 的既有 oracle 惯例，与 `实验/` 里的独立复算同构）对照 `oracle_flux`，带取 `1e-6`；同时把 `PSF.md:186` 的"建议"落成已核（并在 `STAR_PSF_ALGORITHMS.md:209` 的证据格里指向该用例）。
- 置信：CONFIRMED

### CR-33-23 · `close_hybrid` 的 `max(1,|want|)` 让形状判据随量的单位尺度自动变松，σ<1 px 的星（含本套件的 sharp 样本）相对容差被放大到 ~5×
- 轴：R（主）／S
- 位点：`p1psf_oracle.hpp:89-90`「`inline bool close_hybrid(double got, double want, double rel) { return std::fabs(got - want) <= rel * std::max(1.0, std::fabs(want)); }`」；调用点 `p1psf_tests_core.cpp:111-113` 与 `:353-355`（均传 `tol().m_rel`）
- 上位依据：`STAR_PSF_ALGORITHMS.md:211` 把形状判据定为"M 矩阵等价／谱等价"，`README §5` 与 `docs/standards/TEST_STANDARD.md:7`（"浮点断言用容差 + 说明来源"）；无任何条文规定"以 1 为分界混合绝对/相对带"
- 现状：判据带 `= rel·max(1,|want|)`。因 `max(1,|want|) ≥ |want|`，该带**恒不小于**名义相对带 ⇒ 当 `|want|<1` 时静默退化为绝对带。M 矩阵元的单位是 px²，故 `|want|<1` 就是 σ<1 px 的星：`m_rel=3e-3` 在 σ=0.45（`p1psf_fixtures.hpp:171` 的 sharp 样本，`Mxx=0.2025`）处等效相对带 `=3e-3/0.2025=1.48%`，而 σ=2 处是 `0.3%`——同一条判据在不同 fixture 尺度上严格度差 4.9 倍，且方向恒为放松。
- 差在哪：一个"混合容差"把量纲（px²）当成无量纲数与 1 比大小，判据强度依赖被测量恰好大于还是小于 1，这不是条文里的任何设计。
- 后果：PSF 形状（决定下游孔径面积与 SNR 的 `ΣP²` 项）在窄 PSF／锐星域可漂移 1.5%（甚至 σ→0.3 下界时 3.3%）而不红；`F_REC`（质心/形状回收组）与 `F_ID` 两组的形状结论都建立在该判据上。
- 定级：S3（测试面；方向已定为偏绿）
- 怎么算修好：改为"绝对带＋相对带"显式双项 `|got−want| ≤ abs_floor + rel·|want|`，`abs_floor` 用与像素尺度挂钩的量（例如 `rel·(1 px)²` 需先声明参考尺度，或直接给 `abs_floor=1e-6 px²` 并写明来源）；改判据须同批复核现有全部 `close_hybrid` 调用点是否仍绿（σ=0.45 样本的 M 元素实测偏差若接近 1.48% 会变红，属"改对而判红"，须按新带的真实数据重标 `m_rel` 并登记来源）。
- 置信：CONFIRMED（不等式方向与两处换算我按 :74-77 闭式复算过）

### CR-33-24 · 七项容差的"来源"是一条无路径的 probe，而规定"容差须引用来源"的规则载体 `11_MODULE_SOURCE_TEST_STANDARD.md` 在跟踪面不存在
- 轴：C（主）／S
- 位点：`p1psf_oracle.hpp:6`「…`11_MODULE_SOURCE_TEST_STANDARD.md` §5 (容差须引用来源)」、`:109`「容差表 (README §5 + 11_MODULE_SOURCE_TEST_STANDARD §5 元数据: 每项引用来源)」、`:110-111`「无噪声解析回收实证 (probe, 2026-09-09): …」
- 上位依据：本条自身即 `docs/standards/TEST_STANDARD.md:7`「确定性：固定 seed；浮点断言用容差 + 说明来源。」——**规则在跟踪面确实存在，只是被引到了不存在的载体上**
- 现状：`git ls-files "*MODULE_SOURCE*"` 空（同一匹配式对 `*API_STANDARD*` 命中，已自证式子有效）；`git log --diff-filter=D -- '*MODULE_SOURCE*'` 无删除记录；跟踪的 `docs/standards/` 全集 15 个文件均为**无编号**命名（`TEST_STANDARD.md` 等），而 `NN_*_STANDARD` 型跟踪文件数为 0；引用 `11_MODULE_SOURCE_TEST_STANDARD` 的跟踪文件共 **35** 个（`git grep -l | wc -l`），并被 12_–16_ 五份同号标准一起引用。本文件的七项容差各自的"来源"落到一句无路径、无跟踪结果位的 probe 记录。
- 差在哪：按 §2.5 处置档 3 的定义，"实验标定"必须给**落在跟踪面**的标定件路径与结果位；此处既无路径，其规则载体也不在跟踪集，等于整张容差表的来源不可解析——而它援引的实体规则其实在 `docs/standards/TEST_STANDARD.md` 里就管得着。
- 后果：① 任何人（含审计）无法核对 `1e-9`、`5e-6`、`3e-3` 这些数的来源，"容差有出处"这一对外口径在本模块不成立；② 整个 `11_–16_` 编号标准系列被 35＋个文件当作权威引用，链条顶上一环缺失，任何据此判定"符合"的条目都缺载体（与我引到的 stage A 结论"权威链顶断裂"同一形态）。
- 定级：S2（不改数值，但使本模块的容差体系与 35 个文件的规范引用失去可核对性）
- 怎么算修好：两步且必须同批——(a) 在 `docs/standards/` 补齐或指认 `11_–16_` 六份编号标准的实际落点（若其内容已由无编号文件承载，就把 35 处引用统一改指现行载体并在 `docs/DOCUMENT_INDEX.yaml` 登记）；(b) 给七项容差各补跟踪面标定件（按负责人裁定落 `实验/`，不留 `run/` 产物），或按 `NUMERIC_STANDARD.md` 的口径改述为"派生余量"并给式。
- 置信：CONFIRMED（存在性判定全部基于跟踪集＋删除历史，两条命令均已自证有效）

## 6 · `lib/algorithms/psf/tests/p1psf/p1psf_prodpath_check.cpp`（147 行，整读）

### 6.1 §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :49 | `kSciCentroidAbsPx` | 0.3 px | T1 引用的 SCI 绝对质心验收门 | **文献值（条文在位）** | `docs/science/STAR_DETECTION.md:12-14`（由 `R-3_PSF质心科学门与容差.md:46` 逐字引："合成场验收容差 \|Δc\|<=0.3 px（SNR>=20）见 ALG-STARDET-001 §11.4 F1"）；`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:256-258` F1 同值 | 门值本身有权威；**但其前置域（SNR≥20 含噪声合成场）在本文件未实例化** ⇒ CR-33-26 | CONFIRMED |
| :58 | `moffat4_eval(..., sx, sx, 0.0, ...)` | 0.0 | θ=0（圆对称场） | 结构性不适用 | 圆对称构型选择 | — | CONFIRMED |
| :67 | `det[6] = {x, y, 0,0,0,0}` | — | star_det v1 行 | 结构性不适用 | [2]–[5] 生产者不消费（`DATA_SEMANTICS.md:732-733`） | — | CONFIRMED |
| :69 | `p.fitRadius = 8` | 8 px | 拟合窗半宽 | 待确认 | 同文件 4 :55 一行——默认 8 在跟踪面无选型依据；本文件是"生产路径"锁却也不从配置取，等于把默认钉进门里 | 保守方向＝门内显式声明所用 fitRadius 与出厂配置的关系 | CONFIRMED |
| :70-71 | `p.maxIter = 200`、`p.tolerance = 1e-8` | 200 / 1e-8 | 死参数 | 待确认（死参） | DISP-PSF-003（`STAR_PSF_ALGORITHMS.md:201`）；本文件与 `p1psf_fixtures.hpp:52-54` 不同，**未披露其无效**，读者会以为 T1/T2/T3 扫了收敛档 | 保守方向＝照 fixtures 的写法补披露 | CONFIRMED |
| :72-73 | `out9[9]={0}`、`status = -1` | 0 / −1 | 输出缓冲与哨兵 | 结构性不适用 | −1 不属任一状态枚举 ⇒ 未写入即判失败，方向安全（fail-closed）；`rc==0 && nv==1 && status==OK` 三重门（:77）在批 API 失败时不产绿 | — | CONFIRMED |
| :77 | `status == DPSF_FIT_OK` 比较 `out_status` 值 | 0 | 用 `DPSF_FIT_*` 宏判 `DPSF_PSF_STATUS_*` 变量 | **待确认（跨枚举数值复用已兑现）** | `dynamic_psf.h:117` 注释自认两表在 0 上"同值"（`DPSF_PSF_STATUS_OK 0 // 拟合成功 (DPSF_FIT_OK)`），但 1/2/3 两表含义互不相干（`DATA_SEMANTICS.md:766` vs `STAR_PSF_ALGORITHMS.md:187-189`） | 现状良性（0↔0）；风险面＝有人照此写法比 2/3 即错。已回填 CR-33-1 的"尚无此类比较"子项 ⇒ 该子项升为 CONFIRMED | CONFIRMED |
| :86-87 | `W=H=64`、`B=100.0` | 见左 | 场尺寸与背景 | 结构性不适用 | fixture 自订；CX/CY 内点 ⇒ 窗不被裁 | — | CONFIRMED |
| :88 | `SIGMA = 8.0` 注释"背景噪声（本测试场无噪声, σ 仅用于标定 A）" | 8.0 | 名义噪声，实际场无噪声 | **待确认（名实不符）** | `make_field`（:52-60）不含任何噪声项（我逐行读到，仅 `moffat4_eval` 赋值）；本文件 :16 与 :48 却援引 "SNR≥20 域" 的 SCI 门 | 保守方向＝要么真注入噪声（可复用 `p1psf_fixtures.hpp:105` 的 `fix_psf_b`），要么把引用该 SCI 门的措辞改为"绝对精度门（无噪声解析场）" ⇒ CR-33-26 | CONFIRMED |
| :89 | `SNR = 20.0` | 20 | SCI 验收域下限的名义标 | 待确认 | 域前置条件未构造（同上）；20 这个数本身取自 `SCI-P1-STAR-001 §1` 的 "SNR>=20" ✓ 引文正确 | 同上 | CONFIRMED |
| :90 | `A = SNR * SIGMA` | 160.0 ADU | 峰值振幅 | 公式导出（在无噪声场里退化为无关量） | 定义式 `A = SNR·σ`；:90 注释亦自认"SNR 定义在 SCI 未冻结"。无噪声时 A 只改变 LM 的数值条件，不改变可达精度 | 保守方向＝噪声注入后此项才有物理意义 | CONFIRMED |
| :91 | `SX = 1.8` | 1.8 px | 轴 σ | 结构性不适用 | fixture 自订；我核过安全域：FWHM=1.230310·1.8=2.215 px ≪ rw=17 ⇒ 不触发 `fwhm>rect` 拒绝，且 σ>0.3 下界 ✓ | — | CONFIRMED |
| :93 | `CX=31.37, CY=27.61` | 见左 | 非整数真值中心（findings 要求的构型） | 结构性不适用 | fixture 自订，符合 :9 的构型要求 | — | CONFIRMED |
| :99 | `fit_one(img, W, H, CX + 0.5, CY + 0.5)` 的 `+0.5` | 0.5 | "sdet 系初值"的实现方式 | **待确认（不接生产契约）** | :11-14 自述其来源为 sdet 的 "array index + 0.5" 约定（`star_detector.h:74` 确实如此规定），但该 0.5 由**本测试自己加**，`star_coord_contract.h` 与编排桥都不在本 target 的链接面（`CMakeLists.txt:89-96` 只编 `dpsf_psf.cpp`/`dpsf_log.cpp`）⇒ CR-33-25 | 保守方向＝见 CR-33-25（改挂到 file 3 那条已链接 sdet＋契约的面上） | CONFIRMED |
| :120, :135 | `dc <= 1e-2`（T2/T3） | 0.01 px | 本征精度旁证门 | **待确认（与同套件的紧门差 7 个量级）** | 无来源锚；同是无噪声解析场质心回收，`p1psf_oracle.hpp:126` 的 `cen_abs=1e-9`（且有 probe 注记）比本门紧 1e7 倍 | 保守方向＝收紧到与 core 同带（或给 1e-2 的理由：T1/T3 初值偏移更大）；影响范围＝拟合器本征退化的可检出粒度 | CONFIRMED |
| :127 | 注释"~0.25px 偏差" | 0.25 | T3 偏移量描述 | 结构性不适用 | 实际初值偏移为 `(0.25, −0.25)` ⇒ 欧氏 0.354 px；注释只描述单轴，措辞不严（不构成矛盾） | — | CONFIRMED |

### 6.2 findings

### CR-33-25 · 自称 M3b-F-01 的"生产路径质心共址回归锁"不链接任何坐标契约或 sdet，其 T1 的 0.5 由测试自己施加 ⇒ 对被锁缺陷零判别力
- 轴：D（主）／R
- 位点：`p1psf_prodpath_check.cpp:4`「被验条目: M3b-F-01 "质心验收门按「非生产初始化配置」定义判据，生产路径质心无门"」、`:20-23`「回归锁语义 (未修复必红 / 已修复必绿): … T1 断言在 0.5px 级偏差下必红, 修复端到端坐标契约 (owner 裁决) 后方可绿」、`:99`「`FitOut r = fit_one(img, W, H, CX + 0.5, CY + 0.5);`」
- 上位依据：`CMakeLists.txt:82-87`「B4-5 共址回归锁 (M3b-F-01): p1psf_prodpath_centroid — 质心门必须按生产路径定义 (sdet 系初值…)」与其 target 源列表 `:89-91`（`p1psf_prodpath_check.cpp` ＋ `P1PSF_PROD_SOURCES`＝`dpsf_psf.cpp`＋`dpsf_log.cpp`）；真正的端到端面在 `CMakeLists.txt:123-126`（`p1psf_centroid_gate_test` 才链接 sdet 6 TU 与 `star_coord_contract.h`）
- 现状：本 target 不调 `sdet_*`、不 include `star_coord_contract.h`、不触碰编排写/读端。所谓"生产路径初值"是测试把 `CX+0.5` 直接喂进批 API；LM 从任意初值收敛到同一中心（本文件自己的 T3 就以 0.25 偏移证明了拉回），故 T1 的红绿与端到端坐标契约**在数学上无关**：契约若再次回归（正是 :5-7 描述的那个 0.5 px 双支路缺陷），本锁仍全绿。
- 差在哪：文件头与 CMake 注释把"已把 BLOCKER 文本变成可机跑断言"记在本锁名下；实际可机跑的断言只覆盖"dpsf 从带 0.5 偏移初值仍能收敛"，与 M3b-F-01 所指的"生产路径质心无门"不是同一命题。
- 后果：登记面上 M3b-F-01 有一道共址回归锁（且 `CMakeLists.txt:100-108` 另有一条真门），若日后 centroid 门被移动/改名而本锁留下，缺陷可复发而 CI 全绿；对外主张"P1-PSF 质心按生产路径有门"的凭据被高估。
- 定级：S2（不改产品字节；是"锁不锁得住"的凭据问题；真门在 file 3 确实存在，故不到 S1）
- 怎么算修好：把本文件的定位改成它实际做到的事——"dpsf 拟合器对非整数真值中心＋偏移初值的收敛性旁证"，并在 :4/:20-23 与 `CMakeLists.txt:82-87` 删去 M3b-F-01 共址锁的归属，改为交叉引用 `p1psf_centroid_gate`；若确需一道能抓契约回归的共址锁，就在本 target 里 include `star_coord_contract.h` 并对 `coord::star_measurement_from_dpsf` 的输出与 truth 断言（同 file 3 的 [P5]），并把该断言的可失败性用注入验证。
- 置信：CONFIRMED

### CR-33-26 · T1 引用 SCI 的 0.3 px 绝对门却不构造其前置域（SNR≥20 的含噪声合成场），把无噪声解析回收说成生产域验收
- 轴：S（主）／D
- 位点：`p1psf_prodpath_check.cpp:88`「`const double SIGMA = 8.0;         // 背景噪声 (本测试场无噪声, σ 仅用于标定 A)`」、`:90`「`const double A = SNR * SIGMA;     // A/σ = 20 (SNR 定义在 SCI 未冻结, 见 REPORT)`」、`:107-108`「`CHECK(dc <= kSciCentroidAbsPx, "t1_sci_gate: 生产路径 |Δc| <= 0.3 px (SCI-P1-STAR-001 §1)")`」
- 上位依据：`R-3_PSF质心科学门与容差.md:46` 所引 `docs/science/STAR_DETECTION.md:12-14` 的原文带域限定「\|Δc\|<=0.3 px**（SNR>=20）**」；`STAR_DETECTION_ALGORITHMS.md:256-258` F1 亦限定"合成高斯星场（已知中心/流量/FWHM/SNR）"
- 现状：`make_field`（:52-60）无噪声项，场的 SNR 事实上为 ∞；`SIGMA`/`SNR` 只用来算 A，A 在无噪声场里不影响质心可达精度。于是"SNR≥20 域"这个前置条件从未被构造，而断言文案却把它作为适用范围呈现。
- 差在哪：一条带适用域的科学门被搬到域外使用，且适用域靠两个不参与计算的常数"名义满足"。文件 :90 已自认 SNR 定义未冻结，但没自认"本域未构造"。
- 后果：T1 的 0.3 px 判据在无噪声场上没有失败可能（要 fitter 崩掉才红），因此它提供的"生产域质心达标"证据为零；而真正的含噪声证据在 `p1psf_centroid_gate`（NOISE=8、SNR_peak=50/300）。风险是台账把本锁的绿读成"SCI 绝对门在生产域通过"。
- 定级：S3（无产品字节；证据资格问题）
- 怎么算修好：二选一——(a) 用 `p1psf_fixtures.hpp:105` 的 `fix_psf_b(seed,…,bkg_sigma,…)` 造含噪声场（固定 seed），使 SNR 域真实成立，并保留 0.3 px 文案；(b) 保持无噪声场，则把 :107-108 文案与 :16/:48 的表述改为"无噪声解析回收本征精度门（SCI 域前置条件不在本锁内构造，见 p1psf_centroid_gate）"。
- 置信：CONFIRMED

### CR-33-27 · 同一套件内对"无噪声解析场质心回收"取两个相差 1e7 倍的容差，宽者无来源
- 轴：C
- 位点：`p1psf_prodpath_check.cpp:120`「`CHECK(dc <= 1e-2, "t2_intrinsic: 收敛初始化本征精度 <= 0.01 px (旁证门)")`」与 `:135`（同值）；对置 `p1psf_oracle.hpp:126`「`double cen_abs = 1e-9;    // cx/cy 绝对 px (无噪声回收 ≤1e-14)`」
- 上位依据：`docs/standards/TEST_STANDARD.md:7`「浮点断言用容差 + 说明来源」；`STAR_PSF_ALGORITHMS.md:217`「容差来源：fixtures generator 注记（11 号标准 §5 元数据），不用本文手抄值」
- 现状：两处判的是同一种量（无噪声 Moffat4 解析场的质心绝对偏差），一处 1e-9 px 并注了 probe 实测 ≤1e-14，另一处 1e-2 px 且无任何注记；差 7 个量级。
- 差在哪：不是两个适用域（T2/T3 与 core 的解析回收同域：同为无噪声、同为 `dpsf_fit_batch_f*` 回收 `moffat4_eval` 造的中心），是同域两个数。
- 后果：拟合器质心若退化到 5e-3 px 量级（例如采样式或 `img_cx = cx + x0` 回写被改动，`dpsf_psf.cpp:437`），本锁全绿而 core 组红——两个面给出互相矛盾的"生产精度"口径，且宽者无从追溯为何取 1e-2。
- 定级：S3
- 怎么算修好：统一到 `p1psf_oracle.hpp` 的容差表（本文件已 include 它，直接用 `p1psf::tol().cen_abs`），若 T1/T3 因初值偏移需要更宽，就在容差表里显式加一项并注明"初值偏移 ≤0.5 px 时的本征回收带"及其来源。
- 置信：CONFIRMED

### CR-33-28 · 本文件把 `out_status` 值与 `DPSF_FIT_*` 宏直接比较，兑现了 CR-33-1 所指的跨枚举数值复用（现为已证实现例）
- 轴：C
- 位点：`p1psf_prodpath_check.cpp:73-77`「`int nv = 0, status = -1; … dpsf_fit_batch_f32(..., &status); … if (rc == 0 && nv == 1 && status == DPSF_FIT_OK) r.ok = true;`」
- 上位依据：`dynamic_psf.h:98`「out_status[i] 对应检测下标 i，取值见 `DPSF_PSF_STATUS_*`」与 `:117-120`（该表 0/1/2/3）对 `:33-36`（另一表 0/1/2/3）；`DATA_SEMANTICS.md:766` 冻结前者四码，`STAR_PSF_ALGORITHMS.md:187-189` 冻结后者
- 现状：变量 `status` 承载的是 `DPSF_PSF_STATUS_*` 值（由 f32 批 API 写出），却用 `DPSF_FIT_OK` 宏去比；今天 0↔0 成立，故行为正确。
- 差在哪：这正是 CR-33-1 里我标为"UNPROVEN（尚无此类比较）"的形态，现已在跟踪面找到实例——同族写法若作用在 2 或 3 上（例如把 `status == DPSF_FIT_INVALID_PARAMS` 当作"窗口为空"来判）就会读错语义，且两个宏名在同一表达式里毫无提示。
- 后果：不改变产品字节与测试结果；提高的是 CR-33-1 的定级证据（从"码表风险"升为"已有用例跨表比较"），并说明为什么改判据时必须连测试一起改。
- 定级：S3（本条自身）；作为 CR-33-1 的证据项
- 怎么算修好：本行改用 `DPSF_PSF_STATUS_OK`；并同批按 CR-33-1 的修法把两表码域分开，使这类写法无法编译通过或至少被 grep 到。
- 置信：CONFIRMED

<!-- PROGRESS: 6/6 -->

## 收工 status 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工逐字一致（基线未被本代理触碰；全程零写、零构建、零 ctest、零 `eng/**` 执行）。

## 本批最高危三条（只挑，不复述）

1. **CR-33-14**（文件 3）——G-P1-CENTROID-1 三门判据按 3 场池化取分位数，我按门自己的 type-7 分位式复算出：一整场退化到 median|d|=0.25 px（另两场 ≤0.03）时 median 与 p95 双双通过，即 Y 档科学门对 1/3 样本质量塌陷无反应；[P0] 下限 25/120 亦拦不住。
2. **CR-33-21 ＋ CR-33-17 ＋ CR-33-18 合案**（文件 1/3/5）——FWHM 常数 `1.230310` 在六处独立写死、无单一定义点，"独立 oracle"抄的正是实现的圆整值且 `fwhm_rel=1e-12` 使该恒等判据对常数零判别（并会把"改用闭式 1.230307652590102"这一正确修复判红），其"已核"证据位落在 `run/`。三者合成的是：PSF 宽度换算链上没有任何一道门在跑。
3. **CR-33-24**（文件 5）——七项测试容差的规则载体 `11_MODULE_SOURCE_TEST_STANDARD.md` 不在跟踪集（35 个文件引用它，`NN_*_STANDARD.md` 型跟踪文件数 0，无删除记录），而同一规则实际存在于跟踪面 `docs/standards/TEST_STANDARD.md:7`；本模块"容差有出处"的对外口径因此不可核对。

次高（S2 其余项）：CR-33-11（负例注入掩盖 C4 失效）、CR-33-10（门号与阈值未入门表）、CR-33-15（"生产路径门"跑的是出厂不用的 fitRadius）、CR-33-5（两生产入口的 PSF 裁窗不同路）、CR-33-1（两状态码表数值复用＋"3 算不算有效"三处异说）、CR-33-2（调度侧 `1−min/max` 与合同 `√(1−(min/max)²)` 两个形状量，方向偏绿）、CR-33-7（含 ADU 列的块登记为 DIMENSIONLESS）、CR-33-25（共址回归锁对被锁缺陷零判别）。

## 我读不动或需要实跑才能定的

| # | 悬而未决的问题 | 该跑/该读什么 | 影响哪条 finding |
|---|---|---|---|
| 1 | `stage1_cfg_->psf.fit_radius` 的实际默认值与 JSON 可达性：`eng/contracts/schemas/phase_config_normalize.schema.json` 内我未检到 `psf.fit_radius/max_iterations/tolerance` 三键，只检到 `limiting_mag_max_iter` | 读 `Stage1Config` 的 psf 结构体默认初值（`lib/infrastructure/pipeline/orchestrator/cpp/include/*.h`）＋ 一次 `normalize --dump-config` 类只读输出（本代理禁跑） | CR-33-5／CR-33-15：若默认 `fit_radius<=0`，编排侧会把它 clamp 成 0，而 PSF 模块无 "0=自动" 分支 ⇒ 1×1 窗全星失败；我未证实默认值，故未定级到 S1 |
| 2 | `ells`（调度侧 `1−min/max`）与帧级形状阈值的连接点：`module_adapters.cpp:4220` 汇入后走向何处、有无 `> T` 剔除 | 顺 `f_ell`/`ells` 的两个消费点读到阈值字面量 | CR-33-2（方向已定为偏绿，缺阈值即缺后果钉死） |
| 3 | psf 块/`star_measurements` 的 `fwhm`（像素）→ 孔径半径的实际换算式在测光侧的落点：我只在 `noise_snr` 测试侧读到 `r_ap = 1.5·fwhm`（`p1snr_science_test.cpp:121`，我按 β=4 反算该半径≈95% 包围能量，量级自洽） | 读 photometry 的 aperture 半径推导（`lib/algorithms/photometry/**`，本批未覆盖） | 派单本查项"FWHM↔孔径换算"：生产侧唯一实算点尚未取证，暂无线索指向破绽 |
| 4 | 单位类 `UnitId` 是否在 normalize 期被任何门**拦截**（还是只在单测里自比对） | 读 `eng/tests/unit/core_artifact_test.cpp`/`rt001_abi_test.cpp` 的断言体，确认它比的是 descriptor 还是列定义 | CR-33-7（现定 PARTIAL；若门只复读 descriptor 则升为恒真门） |
| 5 | `p1psf_selfcheck`／`p1psf_tests_perf` 是否覆盖了码 3（ITERATION_LIMIT）与 D 组 sharp 样本 | 读 `p1psf_tests_selfcheck.cpp`（不在本批清单）；我已核 `p1psf_tests_core.cpp` 内 D/E 组只兑现码 1/2 | 文件 4 处置表 :170-172 行的 PARTIAL；`STAR_PSF_ALGORITHMS.md:208` 声称"4 状态码逐码负例" |
| 6 | 三条"能红能绿"是否实测成立（CR-33-11/14/25 的推断均为静态读码） | 需要实跑：`ctest -R "p1psf_(center_contract|centroid)"` 与一次"虚容差/单场退化"注入实验（本代理按派单禁令未跑） | CR-33-11、CR-33-14、CR-33-25 的 CONFIRMED 目前建立在逐句推演＋自算分位数上，非运行证据 |
| 7 | `docs/science/PSF.md:81` 的 `run/SCI-FIX-STARPSF-01/results/e1_constants.txt` 是否已在别处留跟踪副本 | 查 `实验/` 与 `artifacts/evidence/` 下同名结果（我已核 `run/` 按裁定不留产物） | CR-33-18 的"证据不可复核"结论强度 |

## 派单与清单一致性

清单 6 个文件与派单文字（"点源函数模块第二段，含动态点源头文件与核"）相符：1 个头文件（`dynamic_psf.h`）＋ 5 个 `tests/p1psf/` 面。**差异需指出**：派单说"含…核"，而清单里没有任何实现核文件（`src/dpsf_psf.cpp`、`src/dpsf_psf.h` 不在本批），实现面只作为交叉取证被读；本批 6 个文件中 5 个是测试面。按派单 §5.5「以清单为准」执行，未擅自扩面。全程 6/6 文件读毕：文件 2–6 均在 20 次/文件调用上限内完成；文件 1 小幅超出（约 22 次），超出部分全部用于派单本查项的取证（列序/形状在生产-消费-合同-文档四处的对账与 FWHM/形状量的消费端定位），未定案部分已按规则落 `PARTIAL`（见 §1.3 与文末表 #2/#3/#4）。
