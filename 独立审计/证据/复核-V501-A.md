# 复核-V501-A —— 第②层独立复核（批次 V501-A / 门禁现状审计 A 组）

- **复核对象**：`独立审计/证据/AUD-501-门禁现状审计.md`（全文 789 行），本批条目 11 条：A-01、A-02、A-06、A-07、A-09、A-11、A-12、A-13、A-14、A-15、A-16。
- **已排除**：A-03、A-04、A-05、A-08、A-10（已由 `raw/复核-门禁入口.md` 覆盖）。
- **基线**：HEAD=`c8f64e9a`。
- **只读纪律**：未修改/移动/删除仓库任何文件；git 仅用 `status`/`ls-files`/`grep`/`log`/`show`/`rev-parse`；**未执行任何门禁**（`run_checks.py` 任何参数、`eng/**` 检查器/生成器/driver 一律未跑）；未 `import` 仓库内 Python；"某门现在会红还是绿"的结论只由 `eng/ci/checks.json`、schema 与门源码文本推导，推导链逐跳标 `文件:行`。
- **开工 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文**：
  ```
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```

---

## A-01 · 注册表↔文档双向一致门在基线上必判红，且 8 个在册门在文档集中零登记 —— 判定：确认（子计数偏差见下）
**判定：确认**
- 我的独立取证（自行复刻 `_slice`/`_rows`/`ID_RE` 三判据面，未 import 仓库 Python）：
  - 分母钉死：`checks.json` 顶层注册项 **150**（`checks[]` 长度）；`docs/ci/01_CHECKS.md` §2 表（`## 2. 检查项清单` → `### 2.1` 切片）解析出 **142** 行、ID 列全不重复。
  - R1 差集 = **8**，与成稿名单逐项相同：`CHK-BUDGET-SINGLE-SOURCE`、`CHK-DOCS-MACHINE-CONSISTENCY`、`CHK-DOCS-MACHINE-CONSISTENCY-SELFTEST`、`CHK-DRZ-PF-AREA-S1`、`CHK-ISA-SAME-SOURCE`、`CHK-PROD-WIRING`、`CHK-TRUTHFUL-CONCLUSION`、`SPEC-POLARITY-CONSIST-01`；R2（文档承诺无实现）= **空**。
  - 判据面确认只读两个字段：`check_registry_doc_sync.py:96`（只取顶层 `c["id"]`）× `:71/:76`（§2 表首列 `cells[0]` 过 `ID_RE`），`:108` 出 `registered_not_documented=reg-lst`。
  - 必红推导链（未执行任何门，逐跳）：`eng/ci/checks.json` 顶层项 `CHK-REGISTRY-DOC-SYNC` → step `REGISTRY-DOC-SYNC` 命令 `["python3","eng/ci/check_registry_doc_sync.py"]`（`platform: any`、`waivable: false`、profiles fast/linux-main/windows-main ⇒ 本机 Windows 不落 `V_SKIP_PLATFORM`/`V_SKIP_WAIVABLE`）→ 门内 `registry_ids` 非空（`:201` 不触发）、`listed=142` 非空（`:206` 不触发）→ `:211` `evaluate` → `:212` `bad` 含 8 项 → `:221-225` 打印 `REGISTRY_DOC_SYNC_FAIL` 并 `return 1` → runner `run_checks.py:747`（rc≠0 且有判词 ⇒ `V_FAIL`）→ `:136-137` `V_FAIL ∈ FAIL_VERDICTS` → `:1966-1968` 收尾 `EXIT_FAIL(1)`。
- 与成稿差异：
  - **"8 个中 7 个在整个 `docs/**` 里零出现" 复算为 6 个**（且与成稿自己的 A-12 相互矛盾）。`git grep -l <ID> -- docs` 逐 ID 实测：零出现 6 项 = BUDGET-SINGLE-SOURCE / DOCS-MACHINE-CONSISTENCY / DOCS-MACHINE-CONSISTENCY-SELFTEST / DRZ-PF-AREA-S1 / ISA-SAME-SOURCE / TRUTHFUL-CONCLUSION；`CHK-PROD-WIRING` 命中 2 档（`docs/architecture/MODULE_MAP.md`、`docs/contracts/PUBLIC_API.md`，不在 CI 文档集）；**`SPEC-POLARITY-CONSIST-01` 命中 `docs/ci/01_CHECKS.md`**（`:239` 的"注册项待写入"散文），即它在 CI 文档集里出现、只是不在 §2 表内 —— 这恰是 A-12 的内容。同一条循环命令内含两个正控命中（PROD-WIRING / SPEC-POLARITY），故 6 个零命中不是命令形式问题。
  - 定级/机制/处方无差异；主链（8 项零登记、该门必红）成立。
- 影响 D8 的处置：进任务书（补 §2 表 8 行 + 保留该门红），并把上述子计数订正回写；不得用 `known_failures` 盖红（基线内该文件无门级项，见 A-13/A-10 面）。

## A-02 · 同一门的覆盖面只到顶层 ID：291 个 step（执行单元总数的 79%）不在任何文档映射面上 —— 判定：确认（口径 291 应为 287）
**判定：确认**
- 我的独立取证：
  - 分母：顶层 150 = 71（带 steps）+ 79（不带 steps）；step 总数 **291**；执行单元 = 79 + 291 = **370**（与成稿 §1 表一致）。291/370 = **78.6% ≈ 79%**。
  - 门实现只看顶层：`check_registry_doc_sync.py:94-96` 的 `load_registry` 遍历 `data["checks"]` 取 `c["id"]`，**全文无 `steps` 访问**（`git grep -n "steps" -- eng/ci/check_registry_doc_sync.py` 零命中 ⇒ 判据面对 step 粒度零感知）。
  - 我自己算的 step 名 × §2 表首列交集：`step_absent_from_sec2 = ` **287**（不是 291），命中的 4 个是 `API-DOCS`、`CHK-MODULE-MANIFEST`、`CHK-NAMING-SURFACE`、`STD-REG` —— 这 4 个"命中"**不是 step 级登记**，而是该 step 的 ID 与其父项 ID **字面同名**（见下），文档行登记的是父项。
- 与成稿差异：
  - 标题口径 **291** 与成稿自己复算的 **287** 不同源：严格说"不在 §2 表首列"的是 287；291 这个数只在"step 级映射面根本不存在"的意义下成立。建议成稿把标题改为「287 个 step 名不在 §2 表首列，且 291 个 step 无任何 step 级登记面（0 张 step 表）」。
  - **附带推翻一条 §1 表数字（非本批条目，但由本条取证顺带得到）**：§1 表「单元 ID 重复 0（顶层与 step 合并后无重名）」不成立。合并顶层+step ID 共 441 项、去重 436 ⇒ **5 组重名**，全部是"step ID 与父项 ID 同名"：`CHK-MODULE-MANIFEST`、`API-DOCS`、`STD-REG`、`CHK-NAMING-SURFACE`、`SPEC-POLARITY-CONSIST-01`。这也正是上面 4 个"意外命中"的来源。
- 影响 D8 的处置：进任务书（step 级映射面 / 或收窄门名），并把 §1「重复 0」这条登记为需订正项。

## A-06 · 157 个执行单元零登记证据面；runner 唯一防线是「stdout 或 stderr 至少一行非空」 —— 判定：降级（机制确认，计数按生效口径改 155/151）
**判定：降级**
- 我的独立取证（确定命令：`python -c` 文本级读 `eng/ci/checks.json`，未 import 仓库模块）：
  - 单元总数 370（79 顶层单步项 + 291 step）；**原始 JSON 口径**"无登记证据面" = `outputs: []` 的 **155** + **无 `outputs` 键**的 2（`SPEC-POLARITY-CONSIST-01`、`-SELFTEST` 两个 step）= **157**（复算复现成稿计数）。
  - **但 runner 实际生效口径不是 157**：`run_checks.py:222-238` `expand_steps` 按 `INHERIT_FIELDS`（`:155-159` 含 `outputs`、`waivable`）把父项字段并入 step，父项 `SPEC-POLARITY-CONSIST-01` 的 `outputs` = `["run/ci/spec-polarity/gate.json"]`（`eng/ci/checks.json` 实读）⇒ 那 2 个 step 继承到**非空**证据面。生效零证据面 = **155**。
  - 155 中 `waivable` 生效为真的 2 个（`TESTKIT-LIST`、`UT-CPU-AVX512`）与 `SILENT_OK_UNITS` 2 个（`run_checks.py:142-145` 实读 = `{"API-DOCS","UNIT-CLOSURE"}`，与成稿锚点一致）会被守卫豁免 ⇒ **`V_EMPTY_OUTPUT` 实际可触发的单元 = 151**。成稿"含 TRUTH-CONCLUSION-GATE、CHK-REGISTRY-VALIDATE、PROD-WIRING-SCAN、全部 UT-*"的列举方向正确。
  - "唯一防线"逐跳确认：`run_checks.py:720-728`（仅 rc==0 分支调用 `evidence_verdict`）→ `:370-373` `V_MISSING_OUTPUT` 只校验**已登记** outputs → `:374-379` 空面守卫四条件（无 outputs ∧ 非 waivable ∧ 不在 SILENT_OK ∧ `not stdout_tail and not stderr_tail`）→ `:685-686` 两个 tail 来自 `tail()`（`:303-304` **只做截断、不 strip**）⇒ 打印任意一个字符即满足留痕；`:380-382` 另一条 `monitor_evidence_gap` 只对 `requires_monitor=true` 的 13 个单元生效，不在本面。⇒ 结论"任何一句 `print("OK: 0 violations")` 即过"成立。
- 与成稿差异：仅**范围/计数口径**——157 是原始 JSON 数，runner 生效为 155、可触发 151；成稿同时引 `EMPTY_OUTPUTS_NONWAIVABLE=155`，两个 155 不同源（一个是"raw 空面且自身无 waivable 键"，一个是"raw 空面总数"），下游若照抄会把两个不同含义的 155 混为一谈。机制、判据链、处方均无差异。
- 影响 D8 的处置：进任务书（写结论门强制登记结构化证据 JSON + `SILENT_OK_UNITS` 冻结），但**数字改为 155（生效零证据面）/ 151（守卫可触发面）**。

## A-07 · 【反证】注册表与 schema 无 required/additionalProperties 违例；「step 缺 waivable」是继承面不是断链 —— 判定：确认（反证成立，但其论据里一处取值为反）
**判定：确认**
- 我的独立取证（自带 schema 读取 + 逐项反查，未跑 `validate_registry.py`）：
  - schema 实读：根 `required=[schema_version,checks]`、`additionalProperties=false`；顶层项 required **9 字段** `id/profiles/platform/command/timeout_seconds/heavy/mutates_workspace/outputs/waivable`；step required **仅 5 字段** `id/command/timeout_seconds/profiles/platform`，且 step 的 `properties` **允许** `waivable` 与 `inputs`（可选）。
  - 反查 `checks.json` 全 150 顶层 + 291 step：顶层 required 违例 **0**、顶层非法键 **0**、step required 违例 **0**、step 非法键 **0** ⇒ 复现成稿 0/0/0。
  - 继承面在位：`run_checks.py:155-159` `INHERIT_FIELDS` 含 `waivable`；`:233-238` `for field in INHERIT_FIELDS: 有则覆盖、无则取父项` ⇒ "step 缺该字段 = 继承父项"，不是断链。缺字段的 2 个单元确为 step（`SPEC-POLARITY-CONSIST-01`、`SPEC-POLARITY-CONSIST-01-SELFTEST`），`checks.json` 中无其它单元缺 `waivable`（普查 `waivable_dist = {False:360, True:8, 缺:2}`，与成稿 §1 表逐项相同）。
- 与成稿差异：
  - **成稿的一条关键取值取反**：A-07 正文写"其父项 `SPEC-POLARITY-CONSIST-01` 顶层 `waivable: true`"，实读为 **`"waivable": false`**（同项 `mutates_workspace: false`、`profiles: [fast, linux-main]`、`platform: any`）。不影响 REFUTED 结论（继承 false 是**更严**方向，非缺陷），但该句作为"为什么不算缺陷"的理由是错的——真正的理由是"继承后为 false，语义与默认一致，且 required 面本就允许缺省"。
  - 连带修正：由于父项 `outputs` 非空，这 2 个 step 在 runner 侧**根本不进 A-06 的零证据面**（见 A-06 生效口径 157→155）。
- 影响 D8 的处置：进任务书，但只保留"把 waivable 缺字段移入正常清单"这一条；父项 waivable 值需按 `false` 回写（成稿该句为事实错误，不得照抄）。

## A-09 · 门崩与判红三条出口分离（正例），但「显式声明崩」需 rc=3 且打印标记，后者在门侧无强制 —— 判定：确认（"少数门会用"量化为 9 档）
**判定：确认**
- 我的独立取证：
  - 三态出口分离（两入口同构）：`eng/ci/run_checks.py:86-93` `EXIT_OK=0 / EXIT_FAIL=1 / EXIT_RUNNER_ERROR=2 / EXIT_CRASH=3`，收尾 `:2054-2056`（`counts[V_CRASH]` ⇒ 3；否则 PASS⇒0、其余⇒1）、`:2057-2062` `RunnerError`/`IncrementalError` ⇒ 2；`eng/ci/run.py:48-54` 同常量、`:1512-1514` 同次序、`:1410/1517` ⇒ 2。分类面 `:136-138` `FAIL_VERDICTS` 含 `V_CRASH`；条目级传播 `:1898-1907`（子步有 `V_CRASH` ⇒ 条目 `V_CRASH` 优先）。⇒ "分离成立"为真。
  - 显式声明通道的合取条件：`:681-683` `trust_declared = (not timed_out and returncode == EXIT_CRASH and TRUST_DECLARED_RE.search(stdout+stderr))`；`:417` 正则字面量 `GATE_TRUST_FAIL|SELFTEST_FAIL`；`:708-712` 仅在 `trust_declared` 且**无** Traceback 时给"显式声明不可信"判词。⇒ "必须 rc=3 **且** 打印字面量"复现。
  - 门侧无强制：`eng/ci/checks.schema.json` 顶层与 step 的 property 全集实测为 `changed_paths / command / ctest_targets / dirty_ignore_exact / dirty_ignore_prefixes / fingerprint / heavy / id / inputs / mutates_workspace / optional_inputs / outputs / platform / prerequisite_tools / profiles / reads_run_results / requires_monitor / steps / timeout_seconds / waivable` —— **无"本门具备 rc=3 声明能力"字段**；`git grep -in "crash_declared|declares_crash|trust_declared" -- eng/ci/checks.schema.json eng/ci/checks.json` ⇒ 0 命中（正控：同一命令面在 `run_checks.py` 内命中 `trust_declared`，只是结果键不是登记字段）。
  - "该通道只有少数门会用"我给出可核数：打印 `SELFTEST_FAIL` 的 `eng/**` 文件 **48** 个，其中**同时**存在 rc=3 出口（`EXIT_TRUST` / `return 3` / `SystemExit(3)` / `exit(3)`）的只有 **9** 个（`eng/ci/{check_design_clause_wiring,check_registration_anchors,gate_trust,run_checks}.py`、`eng/tools/{canonical_product_hash,check_p2_symbol_map}.py`、`eng/tools/doccheck/check_doc_index.py`、`eng/tools/quality/{check_commits_csv,extract_cpp_api}.py`）；打印 `GATE_TRUST_FAIL` 的仓库文件只有 4 个（其中 `gate_trust.py` 是发射器本体）。**反例直证**：`eng/ci/check_registry_doc_sync.py:170-177` 自检失败打印 `SELFTEST_FAIL` 却 `return 1` ⇒ 有标记无 rc=3，走不到声明通道。
  - 缺口②复现：`looks_like_crash`（`:538-545`）要求 `CRASH_TRACEBACK_RE`（`:401`）命中**且** `VERDICT_TOKEN_RE`（`:403-409`，含 `PASS[:=]`、`FAIL[:=]`、unittest `OK/FAILED` 汇总行）**不**命中 ⇒ "先打 `verdict: PASS` 再抛 Traceback" 不记崩、按 `:746-754` 落 `V_FAIL`（rc≠0，方向安全，但把"门坏了"显示成"内容不合规"）——与成稿一致。
- 与成稿差异：无实质差异；补量化（48 / 9 / 4 与 `check_registry_doc_sync` 反例）、两入口常量行号，并把"缺口②后果"钉为 `V_FAIL`（非 `V_PASS`）。
- 影响 D8 的处置：进任务书（新增声明能力字段、由注册表门校验，替掉正则与命名约定），无需上呈。

## A-11 · 14 个 `check_*.py` 不在注册表任何位置，逐档归属（10 退役含 1 不合规 / 2 上呈 / 1 内部件 / 1 只剩自家单测） —— 判定：确认（分档文字自相矛盾，第 4 档定级偏低）
**判定：确认**
- 我的独立取证（判据 = "完整路径是否出现在 `eng/ci/checks.json` 文本中"，与成稿同口径但自行复算）：
  - 分母：`git ls-files` 中 `eng/tools/**` 下 `check_*.py` 共 **92**，其中完整路径在 `checks.json` 零出现者 = **14**，**逐档与成稿名单完全相同**（含 `eng/tools/check_traceability.py`、`eng/tools/check_traceability_matrix.py`；我第一遍用 basename 匹配只得 12，即成稿 §2.4 说的"路径由多字面量拼成"类假阴，改完整路径后得 14）。
  - 逐文件机器标记（`RETIREMENT_MARKER` / `self-test` / 任意拼写 `self[_-]?test` / `--legacy` / `return 2`）实测：
    - **9 个退役契约四条件齐**：`check_final_traceability`、`check_p2_symbol_map`、`check_release_consistency`、`check_release_layout`、`check_reproducible_build`、`check_traceability_matrix`、`quality/check_comment_hygiene`、`quality/check_commits_csv`、`quality/check_task_result_schema`（全部 RET+st+LEG+r2）。
    - **1 个退役契约不合规 = `eng/tools/check_traceability.py`**：`:119-123` 无参打印 `TRACEABILITY_RETIRED:` 并 `return 2`（在位），但入口是位置参数 `tables = sys.argv[1:]`（`:125`）非 `--legacy-*` 旗标，且 `self-test`/`selftest` 两种拼写均 **0 命中** ⇒ `docs/ci/01_CHECKS.md:206-209` 的"每个都带 `--self-test`""保留在显式 `--legacy-*` 入口下"对它两点都不成立 ⇒ 成稿"9 属实、1 不属实"复算成立。
    - **2 个上呈待定**：`check_p1_symbol_map.py`、`quality/check_source_inventory.py` —— 二者 RET/st/LEG/r2 **全 0**（连退役形态都没有），与 `docs/ci/01_CHECKS.md:210-219`"应删除（3）… `check_source_index_v61.py` 已删除；另两个保留并上呈"逐项吻合（第 3 个已删，故不在 14 里）。
    - **1 个在册门的内部件**：`quality/check_conclusion_anchors.py` 被 `eng/tools/check_module_readmes.py:32/36` 载入执行，而 `check_module_readmes.py` 在 `eng/ci/checks.json:490` 在册 ⇒ 非孤儿，判"未登记"为假阳性，成稿处置正确。
  - **第 4 档我改判（成稿定级偏低）**：`quality/check_p3_rejection_count.py` 成稿归"只剩自家单测"，实为**合同点名判据但零派发面**：它被三份合同文书指定为判据本体 —— `docs/contracts/DATA_ARTIFACTS.md:38`（DATA-P3-REJ-001 "判据 `eng/tools/quality/check_p3_rejection_count.py`"）、`docs/contracts/DATA_SEMANTICS.md:3100`（"六条 G1–G6 + `--self-test`"）、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md:175`，除 `eng/tests/quality/test_p3_rejection_count.py:26` 外无任何调用方，且 `eng/tools/HANDOVER.md:218` 已自记"既存**未登记门** `check_p3_rejection_count.py` 返回 rc=2"。⇒ 一份合同判据的机器出口从未被派发（比"孤儿工具"高一档）。
  - "在册/未被派发面触达"的 5 件我逐个复算：`polarity_probe.py`（`checks.json` 0、`.github` 0，仅 `eng/ci/polarity_evidence.json:4` `generated_by` 与 `eng/ci/ledgers/registration_anchor_ledger.json:77-78` 指向它 ⇒ **在册证据由死件生成**，成稿该点复现）、`reconcile_state.py` 与 `verify_ciqa_report.py`（跟踪集内除自身与历史取证文档外**零引用**；正控：同一命令对 `polarity_probe` 得命中）、`select_candidate.py`（`checks.json` 0，但 `.github/workflows/fatduck.yml` 直接调用 ⇒ 派发面在注册表外）、`ci_repair_round.py`（`checks.json` 唯一命中是 `:12701` 的 `"test_ci_repair_round.py"` 文件名，即"测试在册、工具无门位"，比成稿"只被自己的单测导入"更准确）。
- 与成稿差异：
  - 成稿正文写"**2 个** = 实际是在册门的内部件"却只点名 1 个文件，与标题的"1 个内部件、1 个只剩自家单测"分档文字互斥；14 档里 `check_p3_rejection_count.py` 在正文**未归类**。
  - 上述第 4 档定级偏低（改判为"合同判据未接线"）。其余 13 档归属逐条复算一致。
- 影响 D8 的处置：进任务书（5 件逐个定案 + `check_traceability.py` 退役契约补齐或改文档措辞）；`check_p3_rejection_count.py` 建议并入"合同判据必须有派发出口"这一类，与 A-13/A-14 的对象面层合并处理。

## A-12 · `SPEC-POLARITY-CONSIST-01`：文档自述"注册项待写入"，注册表却已有该项 —— 判定：确认（两个附注取值需订正）
**判定：确认**
- 我的独立取证：
  - 文档侧原文实读 `docs/ci/01_CHECKS.md:239`：「**注册项待写入**：按本文 §4…登记 ID `SPEC-POLARITY-CONSIST-01`（命令 `python3 eng/tools/doccheck/check_spec_polarity_consistency.py --json-out run/ci/spec-polarity/gate.json`，P0、waivable=false、changed_paths 含 `ASTROCS_DESIGN.md`/`ACCEPTANCE_SPEC.md`/`docs/**`/`eng/contracts/**`/`eng/packaging/config/**`）；`eng/ci/checks.json` 属本任务文件域之外…故本轮只落地实现 + 自检 + 本行登记说明」。
  - 注册表侧实读该项全文：顶层 `id=SPEC-POLARITY-CONSIST-01`、`profiles=[fast,linux-main]`、`platform=any`、`command=[python3, eng/ci/run_checks.py, --check, SPEC-POLARITY-CONSIST-01, --quiet]`、`waivable=false`、`outputs=["run/ci/spec-polarity/gate.json"]`、`changed_paths` 与文档声明逐字一致、step ① 命令与文档声明的命令行**完全相同**（含 `--json-out`），step ② `-SELFTEST` 跑 `--self-test` ⇒ "文档说待写入、注册表已写入且内容与文档声明的处方一致"= 登记时序倒置成立。
  - 同类第二处 `docs/ci/01_CHECKS.md:230`「`CHK-REGISTRATION-ANCHORS` | 实现已落地，注册项待写入」：该 ID 在 `eng/ci/checks.json` **在册**（grep 命中），且**不在**我复算的 §2 表缺失 8 项名单内 ⇒ 它既有 §2 行、又写着"待写入"，同一类自相矛盾（比我预期更弱一档：不是"文档无行"，是"文档行状态词过期"）。
  - 该 ID 在 §2 表零登记（它是 A-01 差集 8 项之一），而在 `docs/**` 内出现 1 次（就是 `:239` 这句"待写入"）——这正是负责双向一致的门看不见它的机制：`check_registry_doc_sync.py:76` 只取 §2 切片表格首列，散文行不入集。
- 与成稿差异：
  - **"实存顶层项（`waivable: true`…）"取值为反**：注册表实为 `waivable: false`，且与文档声明的"P0、waivable=false"一致 ⇒ 成稿"应为"里"把 `waivable` 与 §2 表声称等级对齐"这一项不存在偏差，须删（同错亦见 A-07）。
  - **"缺 step 级证据面"在生效口径下不成立**：两个 step 无 `outputs` 键，按 `run_checks.py:233-238` 继承父项非空 `outputs` ⇒ 证据面存在。
  - **新增一条我算出的隐患**：同一继承把父项证据文件压到 **不产出该文件的 `-SELFTEST` step** 上——`evidence_verdict`（`:370-373`）对每个 step 单独核 `V_MISSING_OUTPUT`，正常顺序执行时被 step ① 写出的 `gate.json` 掩盖；凡只派发该 step（或 step ① 未跑而 ② 跑）的路径即会红/或反向被旧文件掩盖成绿。补证需要 runner 对 step 级点名的派发语义（读 `run_checks.py --check` 展开段即可判定），本轮未取到 ⇒ 此点单列 `待证`，不影响 A-12 主判定。
- 影响 D8 的处置：进任务书（并入 A-01 的 §2 补行 + 文档删"待写入"过期状态词 + 新增 R6「文档内 `注册项待写入` ⟹ 该 ID 不得在注册表」），但**不得**照抄"waivable 需对齐"和"step 缺证据面"两条处方。

## A-13 · Q2 总答：在册派发脚本 167 个，读真实产品/真实数据的是少数 —— 判定：降级（结构结论确认并加强，三个数不可复算）
**判定：降级**
- 我的独立取证（规则先钉死再数）：
  - 我的规则①「每条 unit 的 `command` 里第一个含 `/` 的 `.py` 参数 = 被派发脚本」：370 单元中 325 条命中，**去重后 134 个脚本路径**，且**全部在 git 跟踪集内**（0 缺失，独立复现成稿 §1「命令引用的脚本目标缺失 0」）；**45 条命令根本没有脚本文件**（形态一律是 `python3 -B -m <模块>`，如 `CHK-FIX208-DISK-GATE`、`CI-CONTRACT-RESOURCE-PROBE` 的兄弟项），"167 个派发脚本"的叙述把这 12% 的模块态派发吞了。
  - 我的规则②「`command` 中任意 `.py` 参数（含作为参数传入的测试文件名）」：去重 **166**，其中 140 个是跟踪路径、26 个是裸文件名（`test_ci001_failclosed.py` 之类）。⇒ **167 在任何一条我能写出的规则下都不出现**，最接近的 166 含义是"被点名的 .py 对象"而非"派发脚本"。
  - 分类计数我用**全文本字面量**法（与成稿的 AST 字符串面法不同，故偏宽）：134 档中含 `docs/|ASTROCS_DESIGN|ACCEPTANCE_SPEC|ENGINEERING_SPEC|AGENTS.md` **104**、含 `lib/|.cpp` **87**、含 `eng/ci/|eng/contracts` **71**、含 `fixture|fake_|sample_` **63**、含 `eng/tests/` **38**、含 `run/|实验|testdata|gaia` **65**、无路径字面量仅 **1**。⇒ 与成稿的 43/98/77/58/20/39/33 **同序但不同量级**，尤其"读产品/真实数据"一侧：成稿 43（AST 精确、自陈 E-3 是下界）与我 65（文本、含注释与文档串，是上界）之间夹住真值 ⇒ **"少数"未被立住**（65/134 = 49% 已是相对多数）。
  - 我能独立立住的是**结构结论**：注册表唯一可表达"对象类别"的字段是 `reads_run_results`（`checks.schema.json` 顶层与 step 属性集实测均含此键），**声明它的单元只有 3 个 / 370**（`KNOWN-FAILURES-BASELINE`、`-VERIFY`、`-CHECK`，含继承后仍是 3）⇒ 注册表面向"派什么命令"极详尽、面向"判据读哪一类对象"几乎空白，A-13 的"应为＝按对象类别分层声明"由此直接成立。
  - 对外口径锚点实读：`docs/ci/03_GATES.md:51`「`gates-report` 汇总：全部 P0 绿 + P1 绿（或已豁免）→ 门禁通过 → 打包发布候选」——单轴"绿"确实不分层，成稿引文方向正确（其行号写作 §5，实际语句在 `:51`）。
- 与成稿差异：三个头条数（167 / 43 / "少数"）我均不能复算，且 43 与 65 的区间使"少数"这一判断处于未决；**结构与处方（分层声明、发布门按层取值）确认**。定级不变（它是口径类而非缺陷类）。
- 影响 D8 的处置：进任务书但**数字须重算**：钉一条取脚本规则（建议我的规则①，134/325/45 三个数一起给），"读产品面"用 `reads_run_results` + `inputs` 声明而非脚本字面量做分子；发布门分层定义按 `03_GATES.md:51` 该行改。

## A-14 · 「输入对象数为 0 即绿」：runner 侧只对 10/370 成立；抽样精读 5 例全部有零面守卫 —— 判定：降级（10/370 确认；"逐个堵住"是样本外推）
**判定：降级**
- 我的独立取证：
  - runner 侧：`inputs` 声明单元 **10 / 370**，逐个名单与成稿**完全相同**（`DEEP-COMPLEXITY`、`CTEST-REGISTRATION`、`WORKFLOW-REGISTRY-BINDING`、`SEAM-FOOTPRINT-SELFTEST`、`RENDER-VIS-V6-SELFTEST`、`ACTIONS-LOCK-OFFLINE/ONLINE/SELFTEST`、`WORKFLOW-GOVERNANCE`、`WORKFLOW-GOV-SELFTEST`）；`optional_inputs` **1** 个。字段在 schema 两侧均存在且 `minItems: 1`（`checks.schema.json:88-93` 顶层、`:252-257` step）。
  - 判定链：`run_checks.py:611` 执行前调 `declared_inputs_verdict` → `:331-350` 三类 kind 映射（`V_INPUTS_MISSING`/`V_INPUTS_EMPTY`/`V_SKIP_OPTIONAL_INPUT`）→ `:136-138` 前两者在 `FAIL_VERDICTS` 内、skip 不在 ⇒ "未声明 inputs 的 360 个单元 runner 无法独立复核它扫了几个对象"成立。
  - 单一实现点：`eng/ci/run.py:42` 与 `eng/ci/run_checks.py:74` 同 `import declared_inputs as _decl_in`（两处注释都自陈"单一实现点/W4-A3"）⇒ 这一半与 A-08 的"两入口判定不一致"不冲突（`inputs` 面是共享的）。
  - 5 例锚点我逐个命中（原文行号）：`eng/ci/check_frozen_gate.py:222`（`FROZEN_GATE_REPLAY_FAIL: 回放语料为空（{pattern}）-> fail-closed`）、`eng/ci/check_product_contract.py:504`（`PRODUCT-CONTRACT_FAIL: 无任何语料（fail-closed）`）、`eng/ci/check_config_defaults.py:77`（`raise gc.GateError("ANCHOR_MISSING: eng/packaging/config/templates/*.json")`）、`eng/tools/quality/contracts/check_science_units.py:21-22`（GAP-027 注释 + `SCI-UNITS-NO-DOCS` finding）、`eng/ci/check_workflow_governance.py:11` 与 `:138`（"扫描面为空（0 个 workflow 文件）⇒ 判红"）。⇒ 成稿的 5 个"文件:行"全部为真、无一虚构。
  - **但"5/5"不代表全体，我给一个可复算的相反指示**：对我的 134 个被派发脚本做守卫普查——宽松文本面（含 `fail-closed|为空|零命中|EMPTY|GateError|if not`）**132/134** 命中（几乎无判别力），严格面（"空 ⇒ 立刻 FAIL/GateError/return 2"同形语句）只有 **61/134（46%）** 命中。⇒ 以 5 例目的性抽样去支撑"该缺陷类在检查器实现内已被**逐个**堵住"不成立：这 5 例是文档里已写明 fail-closed 措辞的门，属高侧抽样。
- 与成稿差异：定级/机制无差异；**范围要改**——"checker 侧 5 例全部有守卫"只可作为"这 5 档已核"陈述，"缺陷类已被逐个堵住"须降为**待证**（需全量或分层随机样本：按读产品/读文档/读源码三类各抽 ≥10 逐档精读），且成稿自己已登记的 `census7.py` 33 项"无守卫文本命中"清单被排除在判据之外，恰好说明该面无机器普查支撑。
- 影响 D8 的处置：runner 侧 10/370 缺口进任务书（`inputs` 对 P0/P1 必填 + `object_class`）；checker 侧"普遍有守卫"不得进负责人清单，改列为**需全量普查**的待证项。

## A-15 · 「结论写成源码字面量 / 探针没抓到就 return 0」：11+19 候选，多数为反证，唯 `> 50` 型阈值成立 —— 判定：确认（11 侧逐项复现；三个真阳的"非门"理由须改写）
**判定：确认**
- 我的独立取证：
  - 11 处硬编码判定字面量我逐条复现：`eng/ci/gate_trust.py:57` `VERDICT_PASS = "PASS"`、`eng/ci/declared_inputs.py:31` `STATE_OK = "ok"`、`eng/ci/run.py:1271` 与 `:1274` `verdict = "PASS"`，加 `git grep -nE 'status = "PASS"$'` 精确 7 处 = `eng/tools/quality/contracts/{check_api_contracts.py:47, check_doc_symbols.py:132, check_execution_contracts.py:121, check_forbidden_patterns.py:17, check_science_units.py:17, check_test_contracts.py:17, check_traceability.py:21}` —— 与成稿名单**逐文件同行号一致**。
  - "累加初值 + 紧随下修"复核（正控两例）：`run.py:1267-1268` `elif hard_fail or known_fail or expired or invalid: verdict="FAIL"` 确在 `:1271` 之前；`check_doc_symbols.py:132` 之后 `:136-137` `if not ns_ok: status = "FAIL"`；同形下修亦见 `check_execution_contracts.py:125`、`check_traceability.py:39/45/57/61/67` ⇒ 该类候选确为反证。
  - "真零对象即绿"三处我逐个查到定义并核了语义：`eng/ci/resource_monitor.py:46-48` `if not argv: print(_capability()); return 0`（能力自述分支）、`eng/tools/monitoring/resource_probe.py:113-117` `parse_cpuinfo_logical`：`if not text: return 0`（**解析器返回计数**，不是判定）、`eng/tools/run_gc.py:273-275` `if not RUN.is_dir(): ... return 0`（GC 工具）。⇒ 三处均非"门结论"，成稿结论方向正确。
  - `> 50` 阈值在位：`eng/ci/failclosed_survey.py:202` `not good["failures"] and good["units"] > 50`（与成稿 A-04 的 201-203 锚点同行族），对照注册表实算 370 单元 ⇒ 阈值-观测量比 7.4 倍的关系成立。
- 与成稿差异（两点必须改，均不影响总结论）：
  - **`resource_monitor.py` 确实被 17 个单元的 `command` 派发**（`BUILD-GCC-RELEASE`、`DEEP-CLANG-BUILD`、`WIN-BUILD-RELEASE`、`V6-CTEST-*`、`DEEP-SAN-*`、`DEEP-COV-*`、`WIN-PACKAGE-CANDIDATE`、`RESOURCE-GATE-REAL`、`CHK-REALDATA-E2E`、`CTEST-P3-EXPORT-STREAM-RSS`），`resource_probe.py` 亦被 `CI-CONTRACT-RESOURCE-PROBE` 派发。成稿把三处一并标注"用法面/解析面/GC 工具**非门**"，其中"非门"对前两处是**错的措辞**：正确的理由是"这些脚本作为 wrapper/解析器被派发，但其空输入分支不在派发路径上"——例如 `RESOURCE-GATE-REAL` 的命令实为 `resource_monitor.py --timeout 300 --output run/ci/... --gate-required --gate-compute-interval 20 -- python3 eng/tools/quality/resource_gate_probe.py --mode busy --seconds 20`，argv 永非空。此点若照抄，会把一个真实在派发的脚本写成"非门"。
  - `run_gc.py` 只在 `changed_paths` 出现 1 次、无 command 派发 ⇒ "GC 工具非门"仅此一处属实。
  - 19 处的分母（AST 法）本轮**未逐条复算**：我是文本正则法，无法在预算内重现"19"与"其余 16 处是谓词函数 `return False`"的切分 ⇒ 该侧计数按 `待证` 处理（补法：一次性 AST 读文件普查 `Return` 节点，禁跑期内不需执行任何门），但其**结论**（无一处构成"门结论写死为绿"）已由我抽到的解析器/谓词样本与 11 侧全量复现支持。
- 影响 D8 的处置：进任务书，但"硬编码结论"类只保留 `failclosed_survey.py:202` 的 `> 50` 一条（与 A-04 合并）；`resource_monitor.py`/`resource_probe.py` 改登记为"派发路径上的 wrapper/解析器，空输入分支不在门路径"，不得写"非门"。

## A-16 · 正负例纪律：124 个 `check_*.py` 里 35 个无 `--self-test` 面，仅约 4 个走 `gate_trust` —— 判定：确认（35 是拼写口径产物，真零负例面 24）
**判定：确认**
- 我的独立取证（分母钉死）：
  - 124 的来源我复算到了：`git ls-files` 中 `eng/**` 下文件名以 `check_` 开头且 `.py` 结尾、**排除 `eng/tests/**`** = **124**（`eng/**` 全量 128、仓库全量 135；`eng/ci` 31 + `eng/tools` 92 = 123）。成稿未写口径，但存在唯一自然口径命中 124 ⇒ 判为可复算。
  - "无 `self-test` 字面量" = **35**（同口径复现，故 89 = 124−35 成立）。**但**判据换成不区分拼写 `self[_-]?test` 后降到 **24**：11 档是用 `--selftest` 拼写（`eng/ci/check_workflow_governance.py`、`eng/tools/check_ast_api.py`、`eng/tools/quality/{check_cmake_usebeforedef,check_complexity,check_ctest_registration,check_isa_leak,check_module_map,check_pipeline_graph,check_prod_reachability,check_serial_heavy}.py`、`eng/tools/v6/check_v6_runtime_closure.py`）。正控：`check_workflow_governance.py:25/189/244/247` 实有 `--selftest` 旗标与 `selftest()` 实现。⇒ 35 中 11 个是**普查口径误报**（成稿 E-2 自己登记了这点，但条目正文与标题仍按 35 对外陈述）。
  - `gate_trust` 类型化判据使用面 = **4**：`eng/ci/run_checks.py` + `eng/tools/quality/check_block_flow_conformance.py` + `check_block_flow_ports_vs_code.py` + `eng/tools/quality/contracts/check_build_graph.py`（`git grep -lE "^(import|from) .*gate_trust"` 得 3 档，runner 侧另 1 档；`git grep -ln gate_trust -- eng` 的第 5 个命中是 `eng/ci/checks.json:587` 的 `changed_paths` 字符串，不是使用者）。
  - 判据面实读：`eng/ci/gate_trust.py:39` `EXIT_TRUST=3`、`:43-55` 四类 kind（`discriminating/protective/exemption/content`）、`:92` `emit(...)`、`:97`+`:103-121` `require_exemption` 为真且缺 `KIND_EXEMPTION` 用例 ⇒ `verdict, rc = VERDICT_CRASH, EXIT_TRUST`。
  - runner 侧反面纪律在位：`eng/ci/run_checks.py:1439-1477` 一组 exemption 正负例（含 `:1462` `step("API-DOCS", silent) ⇒ V_PASS`、`:1476` 平台 skip 计数），与成稿"六条"规模一致。
- 与成稿差异：定级与机制无差异；**范围要改** —— "35 个连 `--self-test` 面都没有"应并列写「35（`self-test` 字面量口径）/ 24（不区分拼写的真实零自检面）」，否则下游会去给 11 个已有自检面的门"补负例"；89 随口径漂移（不区分拼写时为 100）。
- 影响 D8 的处置：进任务书（`gate_trust.emit` 定为唯一正负例出口 + 统一 `--self-test`/`--selftest` 拼写），整改名单按 **24** 档。

## 本批判定汇总（各态计数，附复算命令）

| 态 | 数 | 条目 |
|---|---|---|
| 确认 | 8 | A-01、A-02、A-07、A-09、A-11、A-12、A-15、A-16（四条附必改订正，见下） |
| 降级 | 3 | A-06（157 → 生效 155 / 可触发 151）、A-13（167/43/"少数"不可复算，结构结论保留）、A-14（"逐个堵住"降为待证） |
| 推翻 | 0 | —— |
| 条内子项 `待证` | 4 | A-12 的"继承把父项证据面压到不产出该文件的 SELFTEST step"需先钉 step 级点名语义；A-13 的类别计数；A-14 的 checker 侧全体守卫率；A-15 的"19 处"AST 侧切分 |

分母钉死一览（全部为我只读命令实算，未跑任何门）：
顶层注册项 **150** = 71 带 steps + 79 单步项；step **291**；执行单元 **370**；`outputs: []` **155**（+2 键缺省，继承后生效零证据面 155、可触发 151）；声明 `inputs` **10**；`optional_inputs` **1**；`waivable` 分布 **false 360 / true 8 / 缺 2**；`reads_run_results` **3**；文档 §2 表 ID **142**；R1 差集 **8**；R2 **0**；不在 §2 表首列的 step **287**；父子同名 ID 组 **5**；被派发脚本（路径法）**134**、（任意 .py 参数法）**166**、无脚本文件的模块态单元 **45**；`eng/tools/**` `check_*.py` **92** 其中完整路径不在注册表 **14**；`eng/**` `check_*.py`（除 `eng/tests`）**124**，无 `self-test` 字面量 **35**、无任意拼写自检面 **24**；`gate_trust` 使用者 **4**；打印 `SELFTEST_FAIL` 的文件 **48**、其中同具 rc=3 出口 **9**。
复算命令形态（同一批，逐条可回放）：`python -c` 文本/`json` 读 `eng/ci/checks.json`＋`eng/ci/checks.schema.json`＋复刻 `check_registry_doc_sync.py` 的 `_slice/_rows/ID_RE`；`git grep -n/-l/-c` 定位源码锚；**零**门禁执行、**零** `eng/**` 脚本运行、**零** `import` 仓库模块。

## 旧判偏松清单（= 上一轮给下游的虚假授权）

1. **A-07 / A-12 的 `waivable` 取值为反**：成稿写"父项 `SPEC-POLARITY-CONSIST-01` 顶层 `waivable: true`"，实为 `false`（与 `docs/ci/01_CHECKS.md:239` 自述的"P0、waivable=false"一致）。据 A-12"应为"开的"把 waivable 对齐 §2 声称等级"任务 = 修一个不存在的偏差。
2. **A-12"缺 step 级证据面"不成立**：两个 step 无 `outputs` 键即按 `run_checks.py:233-238` 继承父项非空面；据此"给 step 补 outputs"的整改无对象。
3. **A-06 的 157**：其中 2 档继承到非空证据面；按 157 列名单会把 2 个有证据面的单元写成缺陷。
4. **A-01"7 个在 `docs/**` 零出现"实为 6**：`SPEC-POLARITY-CONSIST-01` 就在 `docs/ci/01_CHECKS.md:239` —— 该句还与本条自己的 A-12 相互打脸。
5. **A-16 的 35**：11 档是 `--selftest` 拼写口径误报（正控 `check_workflow_governance.py:25/189/244`），真实零自检面 24；按 35 整改会给 11 个已有自检面的门重复造负例。
6. **A-14 从 5 例外推到全体**："该缺陷类在检查器实现内已被逐个堵住"——我的同形严格守卫普查只有 61/134（46%）档命中，5 例是文档已明写 fail-closed 的高侧目的抽样。这是本批**最危险**的偏松：它等于替"checker 侧无空扫绿"背书。
7. **A-15 的"非门"状态词**：`resource_monitor.py` 被 **17** 个单元的 `command` 实际派发、`resource_probe.py` 被 `CI-CONTRACT-RESOURCE-PROBE` 派发；写成"非门"会让下一轮不再查它们的退出码语义。正确状态词是"在派发路径上，但空输入分支不落在判定路径"。
8. **A-13 的对外口径**："167 个脚本、只有 43 读产品 ⇒ 绝大多数门从未读过产品"三个数皆不可复算（我 134/166、43–65 区间）；可复算的替身是"注册表唯一对象类别字段 `reads_run_results` 只有 3/370 单元声明"。口径若进负责人清单必须换后者。
9. **A-02 标题 291 与正文 287 混用**；且本批顺带推翻 §1 事实表一行：**「单元 ID 重复 0」不成立**（5 组父子同名：`API-DOCS`/`STD-REG`/`CHK-NAMING-SURFACE`/`CHK-MODULE-MANIFEST`/`SPEC-POLARITY-CONSIST-01`）——该行若被下游当分母用会错。

## 覆盖率自报

- 已判 **11 / 分配 11**（清单 `inventory/V501-A.txt` 11 条：A-01、A-02、A-06、A-07、A-09、A-11、A-12、A-13、A-14、A-15、A-16）；未判名单 **空**。
- 派单说与清单相符（对象 `raw/AUD-501-门禁现状审计.md` 789 行、A-03/04/05/08/10 排除）；无条目缺失或多余。
- 单条命令上限执行：均在 8 条内收敛（A-11 用了 6 条、A-15 用了 7 条）；未写普查脚本、未建 `复算/` 中间件目录；除 `git status`/`git grep`/`git ls-files`/读文件外无其它工具动作。

## 我对第①层判据本身的校准建议

1. **计数三件套强制化**：每个数字必须绑「取数规则一句话 + 确定命令 + 分母定义」。本批 9 处偏松全部源于同一实体在不同口径下取用（157/155、291/287、35/24、167/134/166、7/6、0/5），单看每一条都不算假，拼进事实基础表就互相不可比。
2. **"原始 JSON 口径"与"runner 生效口径"必须分栏**：凡判 step 字段（`waivable`/`outputs`/`inputs`），先应用 `run_checks.py:222-243` 的 `expand_steps` 继承再取数；A-06/A-07/A-12 三条同源错误都是未走继承。建议第①层普查表输出 raw / effective 两列，且 A-06 类"零证据面"结论一律以 effective 为准。
3. **反证型结论与主张型结论同一证据门槛**：A-07（schema 符合性，我 0/0/0 复现＝合格反证）与 A-14/A-15（5 例、19 候选外推为"普遍不成立"＝不合格反证）在成稿里被标成同一种"REFUTED/CONFIRMED"，建议在标签上区分"反证（全量复算）"与"反证（抽样提示）"，后者不得进负责人清单。
4. **状态词必须带派发证据**：`未实现`/`未接线`/`已退役`/`非门`/`文档没写` 五词各自要求一条"是否在 `checks.json` 的 `command` 中出现"的实测；`check_p3_rejection_count.py` 被三份合同文书指定为判据（`docs/contracts/DATA_ARTIFACTS.md:38` 等）却被写成"只剩自家单测"，就是状态词降级了严重性。
5. **§1 事实基础表逐行加"复算命令"列**：本批已证该表 8 行里至少 2 行需改（`单元 ID 重复 0`、`waivable 字段缺失 2` 的缺陷标注）；未附命令的行一律不得被下游当分母。

---

**收工 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文**（与开工逐字相同，未污染被审对象）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

<!-- PROGRESS: 11/11 -->
