# 通读 CR-14 — 只读代码通读审计

- 基线 HEAD：c8f64e9a
- 仓库：F:\Astro dev\Astro CS Normalization Database
- 批次：CR-14（6 文件）
- 派单前言：独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md（已完整读 §0–§5）

## 开工 git status --porcelain 原文

（见文末回填）

## 文件清单与阅读状态

1. lib/algorithms/drizzle/hips/src/module_entry.cpp —— 已读：是（表 24 项）
2. lib/algorithms/drizzle/src/module_entry.cpp —— 已读：是（表 26 项）
3. lib/algorithms/drizzle/healpix_drizzle/nanoflann.hpp —— 已读：是（表 5 项，vendored 不逐值）
4. lib/algorithms/drizzle/healpix_drizzle/tests/oracle_edge_crossing_test.cpp —— 已读：是（表 14 项）
5. lib/algorithms/drizzle/healpix_drizzle/tests/oracle_independent_test.cpp —— 已读：是（表 15 项）
6. lib/algorithms/drizzle/healpix_drizzle/tests/p0_sip_order_guard_test.cpp —— 已读：是（表 14 项）

<!-- PROGRESS: 6/6 -->

---

## 文件 1 · lib/algorithms/drizzle/hips/src/module_entry.cpp（1164 行，全读）

C ABI v1 adapter，把 legacy `aio_hips_*` 九导出包装成 `write_product` 单事务模块。科学纪律声明 scientific_change=false。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :54 | ns-per-ms | 1000000L | nanosleep 纳秒换算 | 结构性不适用 | 单位定义常数 | — | CONFIRMED |
| :86 | hips_err_msg | 1024 | 线程局部错误缓冲 | 结构性不适用 | 缓冲区尺寸 | — | CONFIRMED |
| :142/175/203 | pat[128] | 128 | JSON 键匹配模板缓冲 | 结构性不适用 | 缓冲区尺寸（键为宏常量，远短于 128） | — | CONFIRMED |
| :236-265 | base64 表/cap/acc | 4,3,6,8,0xFF,62/63,52..63 | RFC4648 编解码常数 | 公式导出 | RFC 4648 标准字母表与 3↔4 分组；代码按 `cap=(body/4)*3+(pad==1?2:pad==2?1:0)` 计算，与标准一致 | — | CONFIRMED |
| :293-305 | op[32]/out_dir[1024]/creator_did[256]/obs_title[256]/obs_filter[256]/obs_date[64]/last_op[32] | 见左 | config 字段缓冲 | 结构性不适用 | 字段缓冲尺寸 | — | CONFIRMED |
| :340 | nside 下界 | 512 | HiPS 叶级 nside 最小值 | 公式导出 | docs/algorithms/HIPS_WRITER.md:44「叶级 nside=2^K≥512（:399-401 拒绝 nside<512）」、DRIZZLE_GEOMETRY.md:254「tile_depth 必须=9、nside≥512（astro_sphere_sink.cpp:36-51）」——因 HiPS tile=2^9 ⇒ Norder≥9 ⇒ nside≥512；代码按 `nside<512` 拒绝，与条文一致 | — | CONFIRMED |
| :340 | nside 上界 | 1ull<<24 (=16777216) | u32/log2 精度上界 | 结构性不适用 | 保证 `hips_ilog2_u32` 与 double 精确表示（<2^25 精确）；非科学量 | — | CONFIRMED |
| :347 | tile_width | 512 | HiPS 瓦片边长（=2^9，唯一允许值） | 文献值 | IVOA REC-HIPS-1.0 §4.4.1（HIPS_WRITER.md:206 引用）+ 本仓产品约定 tile_width=512（PHASE2_COVERAGE.md:61「产品线冻结 tile_width=512」）；代码 `tw != 512` 强制 | — | CONFIRMED |
| :371/374 | creator_did/obs_title 默认 | "ivo://astrocs/phase1"/"ACSD Phase1" | 缺省元数据标识 | 结构性不适用 | 标识串，非科学量 | — | CONFIRMED |
| :379 | exposure_s 下界 | 0.0 | 曝光非负前置门 | 结构性不适用 | `!(x>=0.0)` 拒 NaN/负，边界 0 属合法（0 曝光） | — | CONFIRMED |
| :434 | n_tiles 上界 | 65535 (=2^16-1) | manifest 瓦片数硬帽 | 待确认 | docs 面零提及 65535 这一具体上界（HIPS_WRITER.md 负面矩阵只列「parent_ipix≥12·4^K」，未规定瓦片条目数上限）；检索式：`git grep -n 65535 -- docs` 见 findings CR-14-5 | 保守方向：偏红——>65535 瓦片的合法大产品被误拒；影响=高解析全天空产品 | PARTIAL |
| :454-455 | tile_elems/dtype_sz | width*width, 8/4 | 瓦片元素数与字节宽 | 结构性不适用 | f32=4B/f64=8B 字节宽定义 | — | CONFIRMED |
| :521 | snr_count 上界 | 100000000 (1e8) | SNR 点集条目硬帽 | 待确认 | 条文面零提及 1e8；分配护栏用途（1e8×sizeof(SnrPoint)≈数 GB）；检索式见 CR-14-5 | 偏红（超限拒）；影响=超大星表目录 | PARTIAL |
| :530-535 | SNR 平面宽 | *8,*4 | 六平面 ra/dec/value/id=f64、qf/status=f32 | 结构性不适用 | 字节宽定义 | — | CONFIRMED |
| :638 | n_pix_hp | 12ull*N² | HEALPix 总像素数 | 公式导出 | 标准恒等式 N_pix=12·Nside²；代码 `12ull*nside*nside` 按式算 | — | CONFIRMED |
| :639-640 | hier_acc | 12·(N/2)²·8·2 | 层次累加器内存估算 | 结构性不适用 | 调度内存估算（祖先层 f64 signal+coverage 全覆盖上界），非科学量、不进产品字节；随 config 元数据推导 | — | CONFIRMED |
| :646 | dtype_sz | 8/4 | 字节宽 | 结构性不适用 | — | — | CONFIRMED |
| :666 | plan max_workers | 1 | 单事务串行回显 | 结构性不适用 | HI-3 事务串行（:617 注释）；恒 1 | — | CONFIRMED |
| :1060-1067 | 输出 manifest 缓冲 | 384,64,32,5 | 计数基线+每产物余量+产物种类数 | 结构性不适用 | 缓冲区尺寸与枚举基数（signal/support/snr/variance/ivar=5） | — | CONFIRMED |

### findings

#### CR-14-1 · hips 入口把 max_workers 解析入结构体后从不消费，注释却自称"仅作回显"（实际既未消费也未回显）
- 轴：C（上下级一致，兼 doc-vs-code）
- 位点：`lib/algorithms/drizzle/hips/src/module_entry.cpp:305` `uint32_t max_workers;      /* 0 = 无 config 上限 (事务恒串行, 仅作回显) */`；赋值点 `:385` `c->max_workers = (uint32_t)json_get_u64(json, HIPS_CFG_KEY_MAX_WORKERS, &f);`
- 上位依据：AGENTS.md §7 目录落位把该 adapter 归入"科学算法"入口；本文件头 §线程纪律声明"事务并行度由 host executor 跨 instance 并发承载"。未发现任何条文要求 hips config 暴露 max_workers。检索式：`git grep -n max_workers -- lib/algorithms/drizzle/hips`（命中仅 :305 声明、:385-386 赋值、:617/:666 是硬编码字面量 1，均非对 `c->max_workers` 的读取）。
- 现状：`c->max_workers` 在 `hips_cfg_parse` 内被赋值后，全 TU 再无任何读取点；plan 的 `"max_workers":1`（:666）是常量字面量，inspect 的 `last_workers`（:743）来自 `leased`（恒 1），输出 manifest 的 `"workers":%u`（:1088）亦来自 `leased`。注释声称的"仅作回显"没有对应回显路径。
- 差在哪：注释语义（"仅作回显"）与代码事实（既不参与任何决策也不进任何输出面）不符；同时构成"同一参数在两入口是否同源"的取证点——对照文件 2，drizzle 入口对 max_workers 真读真用（见下）。
- 后果：读者据注释以为设了 config max_workers 会被模块回显核对，实则静默丢弃；不影响产品像素/字节，仅误导文档-代码一致性。因 hips 事务恒串行，丢弃本身语义上无害。
- 定级：S3（无对外产物主张失真，纯 doc-vs-code；缺产品后果故不升 S2）
- 怎么算修好：要么删除 hips_cfg 的 max_workers 字段与解析（本模块根本不并行），要么把它真正回显进 inspect/plan 并订正注释；三处（字段、注释、消费）同批改，防"改注释留死字段"。
- 置信：CONFIRMED（grep 全 TU 确认零读取）

**对照（不另立 finding）**：drizzle 入口（文件 2 `:373/:596/:915`）对 max_workers 真读——execute 期用它夹紧 `want`（`:915 if (c->max_workers > 0 && c->max_workers < want) want = c->max_workers;`）并回显进 plan（`:596`）。两入口对"同一参数"一个真读一个纯丢弃，形态确在；但 hips 侧丢弃属结构合理（串行），故只报其"注释失实"，不报"功能缺失"。

**不报的核过项**：nside≥512、tile_width==512 两处阈值经 HIPS_WRITER.md:44 与 DRIZZLE_GEOMETRY.md:254、PHASE2_COVERAGE.md:61 三重锚定（HiPS tile_depth=9 ⇒ Norder≥9），与代码 `:340/:347` 判定方向一致，非"凭空字面量"，检索式 `git grep -rn "nside.*512\|tile_width.*512\|tile_depth" -- docs/algorithms`。

---

## 文件 2 · lib/algorithms/drizzle/src/module_entry.cpp（1338 行，全读）

C ABI v1 adapter，包装 legacy `hp_drizzle_api.h` 六导出为 `drizzle`（帧→HiPS/.hiss）与 `reverse`（球→面）两 op。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :22 | _POSIX_C_SOURCE | 200809L | 特性宏 | 结构性不适用 | 编译期 API 可见性，非运行量 | — | CONFIRMED |
| :221-252 | b64_encoded_len/tab | (n+2)/3*4+1, tab | base64 长度与字母表 | 公式导出 | RFC 4648；代码按 ⌈n/3⌉*4 分组 | — | CONFIRMED |
| :297 | json_append_f64 | "%.17g", 40 | f64 十进制往返位数 | 结构性不适用 | IEEE754 double 需 17 位十进制精确往返（定义常数）；40=容纳该串缓冲 | — | CONFIRMED |
| :303-317 | op[16]/rev_proj[16]/… config 字段 | 见左 | config 字段缓冲 | 结构性不适用 | 缓冲尺寸 | — | CONFIRMED |
| :355 | pixfrac 缺省 | 1.0 | 缺键默认（方核全 drop） | 文献值 | Drizzle 规范默认 pixfrac=1.0（Fruchter & Hook 2002；本仓 DISP-DRZ-003 口径，:306 注释引用）；代码 `if (!found) pixfrac = 1.0` | — | PARTIAL（未逐字读 DISP-DRZ-003 条文，仅据 :306 注释指向） |
| :356 | pixfrac 值域 | [0,1]（`<0.0‖>1.0` 拒） | 核缩放下界 | 公式导出 | 值域随定义 0 开 1 闭；代码接受端点 0，:355 注释自称"契约只约束 (0,1]"——端点 0 交由 legacy 二次校验（:482-483 注明 REV 端点由 legacy 错误码 3 转发） | 偏绿方向仅到"接受 pixfrac=0 进 legacy"，legacy 拒；无产品字节 | CONFIRMED |
| :364 | precision_mode 缺省 | -1 | 哨兵值（auto→legacy KV 读取） | 结构性不适用 | 枚举哨兵，非科学量 | — | CONFIRMED |
| :365-366 | precision_mode 值域 | [-1,1] 整数 | -1/0/1 三态 | 结构性不适用 | 枚举基数 | — | CONFIRMED |
| :373 | max_workers 缺省 | 0（不设限） | 线程上限 | 结构性不适用 | 0=哨兵；真用于 :915 夹紧、:596 回显 | — | CONFIRMED |
| :403 | nside 值域 | [1, 4294967295.0]（2^32-1） | u32 上界 | 结构性不适用 | u32 全域 + is_pow2 校验；与 hips 的 [512,2^24] 不同源属合理（本 op 可写 legacy .hiss，非 HiPS 深度约束） | — | CONFIRMED |
| :437-439 | rev width/height 值域 | [1, 2147483647] | i32 上界 | 结构性不适用 | i32 全域 | — | CONFIRMED |
| :450 | rev_scale 下界 | >0.0 | 角分辨率正 | 结构性不适用 | 前置门边界 | — | CONFIRMED |
| :712/738-739 | A/B/AP/BP_{i}_{j} 枚举 | ≤5 | SIP 系数阶上界 | 文献值/合同值 | :721 `#define DRZ_SIP_ORDER_MAX 5` 注释「P0-1 order≤5 冻结」，与文件 6 p0_sip_order_guard_test 同源；i,j∈[0,5]⇒6×6=36 项 | — | CONFIRMED |
| :719 | DRZ_HEADER_KEY_N | 数组长度(37) | header 键基数 | 结构性不适用 | sizeof 除法，枚举基数 | — | CONFIRMED |
| :949 | 输出缓冲基线 | 512u（+64/+24/+40） | 计数产物 manifest 缓冲 | 结构性不适用 | :948 注释「G3-5 计数三字段+合计占 ~160B ⇒ 基线上调(256→512)」，纯缓冲尺寸决策 | — | CONFIRMED |
| :571 | n_pix_hp | 12ull*N² | HEALPix 总像素 | 公式导出 | N_pix=12·Nside² 标准恒等式 | — | CONFIRMED |
| :572 | acc_bytes | n_pix_hp·8·6（"6×f64"） | 累加器内存估算 | 待确认 | 与同函数头注释（:550-554「每叶 3 个 f64 + 1 u32 = 32B/叶」）自相矛盾，且与真结构 `TileLeafAccumulatorT<double>`（drizzle_engine.h:78-82：sumFlux/sumArea/sumNorm/sumVarNum 4×f64 + nContrib u32 ⇒ 40B/叶）三者各说各话——见 CR-14-3 | 偏保守（48>40 高估）→ 不会致 OOM，但三处口径须统一 | CONFIRMED |
| :573-574 | io_out_bytes | n_pix_hp·8·3 / ·4·3 | 产物 IO 估算 | 结构性不适用 | 3 产品(f64/f32)估算，非科学量 | — | CONFIRMED |
| :608 | reverse plane bytes | W·H·8·2 | 双面 f64 平面估算 | 结构性不适用 | 2 平面缓冲估算 | — | CONFIRMED |
| :1088-1089 | n_leaf 缺省回退 | b64_len/12ull | 缺 n_leaf 时按 b64 长度估条目 | 待确认 | 见 CR-14-4；12=经验除数（ipix 为 u64=8B，base64 密度 3/4 ⇒ 真应 ≈b64_len/10.67），条文面零提及 | 偏红（估小→长度校验必失配拒）→ 死回退，见 CR-14-4 | CONFIRMED |
| :1096-1097 | ipix_need/sig_need | n_leaf·8, ·4/8 | u64 索引与 f32/f64 字节宽 | 结构性不适用 | 字节宽定义 | — | CONFIRMED |
| :1170-1179 | n36 | 36 | SIP 系数数组长度 | 公式导出 | order≤5 ⇒ 6×6=36 系数；代码 `n36 != 36` 精确校验 | — | CONFIRMED |
| :255-284 | b64_decode pad/acc | 6,8,0xFF | base64 解码常数 | 公式导出 | RFC 4648 | — | CONFIRMED |
| :185-217 | json_get_f64_array | 1(最小 malloc) | 空数组占位 | 结构性不适用 | 避免 malloc(0) | — | CONFIRMED |

### findings

#### CR-14-2 · hips 入口对 prov_pixfrac/prov_scale 未作有限性/值域守卫即转交 provenance 写，而 drizzle 入口对同名参数 pixfrac 在 adapter 内即守卫
- 轴：C（同一参数两入口处置不同源）
- 位点：`lib/algorithms/drizzle/hips/src/module_entry.cpp:565` `r->prov_pixfrac = json_get_f64(manifest, HIPS_M_KEY_PROV_PIXFRAC, &f);` 与 `:567` `r->prov_scale = json_get_f64(manifest, HIPS_M_KEY_PROV_SCALE, &f2);`，随后 `:965` `int rc = aio_hips_set_drizzle_provenance(ps, rows.prov_pixfrac, rows.prov_scale);`
- 上位依据：对照文件 2 `:356` `if (!isfinite(pixfrac) || pixfrac < 0.0 || pixfrac > 1.0)`——drizzle 入口在 adapter 层就用 `isfinite` 拦 NaN/Inf 并夹 [0,1]。hips 侧 :963 注释「值域 legacy 校验 rc 2」把守卫完全下推给 legacy。
- 现状：hips 仅在 `:967` 检查 `aio_hips_set_drizzle_provenance` 返回码 rc≠0 才失败；对 `prov_pixfrac`/`prov_scale` 本身不做 `isfinite`/范围判断，NaN/Inf/负 scale 直接进函数调用。
- 差在哪：两入口对"pixfrac/scale 是否为合法有限值"这一同一前置，一个在 adapter 早拦、一个依赖 legacy 内部；若 legacy 的 rc 校验存在空洞（例如仅检 scale>0 不检 NaN 传播），非数 provenance 会被写进 HiPS 产品头。
- 后果：可能造成 HiPS 产品 provenance 字段含非数——但 provenance 是元数据、不改像素数值；且我无法在不读 `aio_hips_writer.cpp` 的前提下确认 legacy 是否真检（超本文件只读范围）。
- 定级：S2（PARTIAL：缺"legacy 是否校验 rc 的 NaN 覆盖"这一跳；写不出从入口到坏字节的确证路径，故不升 S1）
- 怎么算修好：在 hips `hips_rows_parse` 内对 `prov_pixfrac/prov_scale` 加 `isfinite` 与范围守卫（与文件 2 :356 同口径），或读 `aio_hips_set_drizzle_provenance` 证实并注释其已覆盖 NaN；两处入口统一守卫位置。
- 置信：PARTIAL（未核 legacy `aio_hips_set_drizzle_provenance` 的 NaN/range 实际校验，缺该跳）

#### CR-14-3 · drizzle plan 的 memory_accumulator_bytes 在同一函数内三处口径互斥（注释 3×f64/32B、代码 6×f64/48B、真结构 4×f64+u32/40B）
- 轴：C（上下级一致 / doc-vs-code 自相矛盾）
- 位点：`:572` `uint64_t acc_bytes = n_pix_hp * 8ull * 6ull;   /* 6×f64 累加器 */` 对 `:550-554` 函数头块注释「`accumulator_bytes = 12*nside^2 * 8B * 4`（每叶 3 个 f64 累加量 + 1 个 u32 计数, FP64 下按 8B 对齐 = 32B/叶）」
- 上位依据：真累加器定义 `lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.h:78-82`：`TileLeafAccumulatorT<double>` 含 `sumFlux, sumArea, sumNorm, sumVarNum`（4 个 Scalar=double）+ `nContrib`（uint32_t）⇒ 对齐后 4×8+8=40B/叶。
- 现状：块注释说"3 个 f64 + 1 u32 = 32B"，代码常量用"6×f64 = 48B"，实测结构是"4×f64 + 1 u32 = 40B"。同一 `memory_accumulator_bytes` 字段，三个来源给三个不同系数（4/6/实际≈5 个 8B 槽）。
- 差在哪：块注释对累加量个数（3）与真结构（4：sumFlux/sumArea/sumNorm/sumVarNum 全是 f64 累加量）不符；代码系数（6）既不等于注释（4）也不等于真值（5 个 8B 槽）。三者至少两处错。
- 后果：plan 输出的 `memory_accumulator_bytes` 是给 orchestrator 做 worker/内存预算的估算。当前 48>40 属高估，方向偏保守（不会因低估致 OOM），故不致产物错值；但估算口径与它所声称建模的对象不一致，属对外"估算由元数据推导、无魔数"主张（:550「全部由 config 元数据推导, 无魔数」）的可信度瑕疵。
- 定级：S3（估算值，不产产品字节；方向偏保守；主要是自相矛盾）
- 怎么算修好：核对 `TileLeafAccumulatorT` 真实 sizeof（含 `pixels`/`touched` vector 头与堆占用策略），把 :550-554 注释、:572 系数、内联注释三处统一为同一口径与同一数；若要计入 vector 开销须显式写出。
- 置信：CONFIRMED（已读真结构确认 4×f64+u32）

#### CR-14-4 · drizzle reverse 缺 n_leaf 时的 b64_len/12 回退经下游精确长度校验后必被拒，实为死回退（fail-closed 但注释误导）
- 轴：R（运行时正确——除数选择使回退恒失败）
- 位点：`lib/algorithms/drizzle/src/module_entry.cpp:1089` `if (!found) n_leaf = b64_len / 12ull;   /* 容错: 缺省按 b64 保守估计, 长度校验兜底 */`
- 上位依据：无条文规定该回退算法（检索式 `git grep -n "b64_len / 12\|n_leaf" -- lib/algorithms/drizzle` 仅命中本文件与其反向 manifest 字段说明）。属实现自订。
- 现状：`ipix_need = n_leaf*8`，而真实解码字节数 `got ≈ b64_len*3/4`。取 `n_leaf = b64_len/12` ⇒ `ipix_need = b64_len/12*8 = b64_len*2/3 ≈ 0.667·b64_len`，而 `got ≈ 0.75·b64_len`；`:1107 if (got < 0 || (uint64_t)got != ipix_need)` 精确相等判据下，0.75·L ≠ 0.667·L 恒成立 ⇒ 缺 n_leaf 的合法 manifest 一律被拒。
- 差在哪：注释自称"容错/保守估计+长度兜底"，实际该估计值与兜底判据互斥，兜底不是"纠正估计"而是"否决估计"，导致回退路径永不放行（连它想救的缺失场景也救不了）。
- 后果：偏红——缺 n_leaf 的合法 reverse 请求被误拒；不会产生错值产物（fail-closed），仅损失一条本应可用的容错通道，且误导后续维护者以为该回退有效。
- 定级：S3（fail-closed，无产物错值，仅可用性/文档误导；且 reverse 通道是否对外承诺"缺 n_leaf 可用"未见于条文）
- 怎么算修好：若真要容错，回退应取能让 `n_leaf*8==got` 成立的值（即 `n_leaf = decoded/8`，先解码再反推），或干脆删除回退、把"缺 n_leaf"明确判 PARAM 拒绝并订正注释。
- 置信：CONFIRMED（代数推导 got 与 ipix_need 恒不等）

#### CR-14-5 · hips 两个输入硬帽 65535 与 1e8 为无锚经验值（合并登记）
- 轴：S（支撑——阈值凭何）
- 位点：`lib/algorithms/drizzle/hips/src/module_entry.cpp:434` `if (!f || n_tiles > 65535ull)`；`:521` `if (m > 100000000ull)`
- 上位依据：检索式 `git grep -rn "65535\|100000000\|1e8" -- docs`（HIPS_WRITER.md 负面矩阵 :353-354 列 nside<512/tile_width≠512/parent_ipix≥12·4^K 等，但**无**瓦片条目数上限或 SNR 条目数上限的任何条文）。design/science 面零提及 65535 与 1e8。
- 现状：`n_tiles` 上限取 2^16-1=65535，`snr_count` 上限取 1e8，均为纯字面量，无出处、无推导、无标定；用途看似分配护栏（65535×W²×8B、1e8×sizeof(SnrPoint)）但阈值本身与"防 OOM"的关系未写成式。
- 差在哪：这是可拒绝合法输入的门限值，落在 S 轴——既非结构性常数（它决定"多大的合法产品被拒"）也无文献/公式/标定支撑。
- 后果：偏红方向——超过 65535 瓦片的全天空高解析产品被误拒（nside=512 全天空叶瓦片≈12 个，但高 nside 或多产品拼接可超）；无产物错值，仅上限选值无依据。
- 定级：S3（拒绝方向偏红、不产错值；主要是"门限凭何"缺锚）
- 怎么算修好：给 65535/1e8 补一条推导（如按可接受峰值内存反算条目上限并写成式），或改为动态按 `available memory / per-item bytes` 计算；若为产品约定须落 docs/algorithms 登记。
- 置信：PARTIAL（未确认这两帽是否在他处文档以另一措辞规定；已在本批可及 docs 检索式内零命中）

---

## 文件 3 · lib/algorithms/drizzle/healpix_drizzle/nanoflann.hpp（4091 行，读头部/许可/版本/局部改动指纹，按 §4 不逐值审计）

### vendor 判定（前言 §4 要求先判三件事）

- **是否 vendor 引入**：是。文件头第 33-62 行是 nanoflann 官方 `\mainpage` 文档，指向 `github.com/jlblancoc/nanoflann`；本仓 CMake `lib/algorithms/drizzle/CMakeLists.txt:96` 明写「nanoflann.hpp (snr_evaluator KD-tree) -isystem 隔离（若 vendored）」。生产消费方 `healpix_drizzle/snr_evaluator.cpp:17 #include "nanoflann.hpp"` 并用 `nanoflann::KDTreeSingleIndexAdaptor`（SNR 源邻域 KD-tree）⇒ 是活依赖，非死副本。
- **许可证**：BSD-2-Clause（:2「Software License Agreement (BSD License)」，版权 Muja/Lowe/Blanco，:9「THE BSD LICENSE」，两条款重分发条件 + AS-IS 免责）。上游版权头在本文件内完整保留（BSD 的源码保留要求已满足）。
- **我们是否改了它**：未改。证据三条：① `git log --oneline -- <本路径>` 仅命中 1 条 `eaf32aad refactor(lib): ARCH-001 目录等价迁移`，提交自述「lib/**：35 次 git mv… 源文件字节等同（R100）」；② 该提交对本文 numstat 为 `4091  0`（纯新增路径、0 删除，符合移动而非改写）；③ `git grep -in "astrocs|acsd|healpix|hiss|wcs|sip"` 于本文件零命中，无本仓特有改动指纹。`NANOFLANN_VERSION 0x190` = 上游 v1.9.0。
⇒ 按前言 §4：登记来源与版本，不逐值立卡。

### §2.5 数值处置表（仅登记随附来源，不审计上游内部值）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :89 | NANOFLANN_VERSION | 0x190 | 上游库版本（v1.9.0） | 结构性不适用 | 第三方版本标识，非本仓科学量；saveIndex/loadIndex 用它做格式校验（:1667/:1719 属上游逻辑） | — | CONFIRMED |
| :46-47 | NANOFLANN_NODE_ALIGNMENT 默认 | 16 | KD-tree 节点内存对齐字节（上游默认） | 结构性不适用 | 上游实现选择（对齐），非本仓引入 | — | CONFIRMED |
| 消费方 snr_evaluator.cpp:120 | KDTreeSingleIndexAdaptorParams(32) | 32 | KD-tree 叶最大基数（上游默认） | 结构性不适用 | 由 snr_evaluator.cpp（不在本批清单）传入的上游构造参数，此处仅注明来源不立卡 | — | CONFIRMED |
| 全文 | 上游 KD-tree 内部常数（分裂阈值/堆/边界等） | — | nanoflann 内部算法常数 | 结构性不适用 | vendored 未改，逐值处置不适用（前言 §4）；无本仓科学语义 | — | CONFIRMED |
| — | 本文件是否含本仓自订科学常数 | 无 | — | 结构性不适用 | 检索式：`git grep -in "astrocs\|acsd\|healpix\|wcs\|sip" -- <本路径>` 零命中 | — | CONFIRMED |

### findings

#### CR-14-6 · vendored 第三方 nanoflann（BSD-2、活依赖、编入 drizzle DLL）未登记进本仓许可证收集索引，也无随目录 NOTICE
- 轴：C（上下级一致：登记面 vs 实际依赖面）
- 位点：`eng/packaging/licenses/LICENSE-INDEX.txt`「文件清单」仅列 `CFITSIO_LICENSE.txt` 与 `nlohmann_json.MIT.txt`；被依赖件 `lib/algorithms/drizzle/healpix_drizzle/nanoflann.hpp:2` 携带 BSD-2 头，但：`git ls-files -- lib/algorithms/drizzle | grep -iE "notice|license|third"` 无结果；`git grep -in "nanoflann" -- eng/packaging` 于许可证索引零命中（唯一命中在 `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv`）。
- 上位依据：LICENSE-INDEX.txt 自订规则「每份第三方许可证文本对应一个受发行约束的文件/目录，只读复制…来源与 hash 见…本索引」；同类 vendored 头 `lib/third_party/nlohmann/json.hpp` 有 `nlohmann_json.MIT.txt`，`lib/algorithms/shared/healpix/` 有 `THIRD_PARTY_NOTICE.md`，`lib/algorithms/platesolve/LICENSE`。AGENTS.md §7 亦规定「lib/third_party/ 第三方依赖」。
- 现状：nanoflann 头被 `snr_evaluator.cpp` include 并编译进随发行分区的 drizzle DLL（header-only），其 BSD 文本仅存在于源文件内部，未复制进 `eng/packaging/licenses/` 收集、未登记索引、未落 lib/third_party、旁边无 NOTICE。对照 nlohmann（同为单头第三方）已被完整登记。
- 差在哪：本仓为其它第三方（含同为 header-only 的 nlohmann）建立了"许可证文本入收集 + 索引登记"的一致性做法，nanoflann 破坏了这一一致性；BSD 的源码内保留虽满足最低合规，但发行许可证清单/落位与仓库自订规则不符。
- 后果：影响对外"依赖与许可证边界"主张与安装包 licenses/ 完整性（BLD-004 范畴）——不产产品字节错值。注：索引「已知限制」把 zlib/zstd/lz4 等延后给 BLD-004，nanoflann 或亦属"有意延后"，故非确证的疏漏。
- 定级：S3（PARTIAL：无法判定是疏漏还是 BLD-004 有意延后；无产物错值）
- 怎么算修好：要么在 LICENSE-INDEX.txt + `eng/packaging/licenses/` 增 nanoflann BSD 条目（照 nlohmann 先例），要么在其"已知限制"补一句点名 nanoflann 延后至 BLD-004；若要求落位统一，再评估移至 lib/third_party（须同步 -isystem 与 CMake 路径）。
- 置信：PARTIAL（缺"nanoflann 是否已被 BLD-004 计划显式覆盖"的条文证据）

---

## 文件 4 · lib/algorithms/drizzle/healpix_drizzle/tests/oracle_edge_crossing_test.cpp（277 行，全读）

独立 edge-cross / sliver Oracle（ORA-101）。真值由本文件自实现的 `arcs_cross_strict`（大圆弧边-边严格交叉）+ `point_in_poly_strict`（严格点包含）+ 边中点包含组合判定（`independent_intersect`），**不调用**生产 `compute_overlap_area`/`query_candidate_pixels_fast` 作真值——就期望值独立性而言成立（非自证循环）。生产侧断言 `in_cand && prod` 与 `!ind ⇒ !prod`。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :27 | PI | 3.14159265358979323846 | 圆周率 | 公式导出 | 数学常数 | — | CONFIRMED |
| :37 | nrm 退化阈 | 1e-300 | 零向量除零保护 | 结构性不适用 | 数值下限保护，非科学量 | — | CONFIRMED |
| :63 | 弧端点严格阈 | 1e-10 | 交点距端点角距判"非共享端点" | 结构性不适用 | Oracle 自实现几何容差（实验单元内自订，前言 §4 不立缺） | — | CONFIRMED |
| :66-67 | 弧上共线容差 | 1e-8 | da0+da1≈la 判交点在段上 | 结构性不适用 | 同上几何容差 | — | CONFIRMED |
| :85 | 点在多边形内阈 | 1e-14 | d≤1e-14 判边界/外部不含 | 结构性不适用 | 同上几何容差 | — | CONFIRMED |
| :156 | nsides[] | 16384,65536,262144,4194304 (=2^14/16/18/22) | 覆盖 NSIDE 档 | 结构性不适用 | 实验覆盖选择，非科学量（§4） | — | CONFIRMED |
| :157 | pfs[] | 0.6,0.8,1.0 | pixfrac 覆盖档（band 长收缩） | 结构性不适用 | 实验覆盖选择（§4） | — | CONFIRMED |
| :167-170 | 目标位置系数 | 0.37,0.41,0.5,0.35,nside-2 | face 内/边/角/极冠取点 | 结构性不适用 | 实验自订位置 | — | CONFIRMED |
| :188-190 | fracs_a/b/c | 1e-4,1e-6,1e-8 | sliver 重叠面积占 A_t 比例档 | 实验标定 | 与 :182-185 注释互推：sliver 绝对面积须高于 S-H 数值噪声 ~1e-19，按 nside 选可表示档；随附于本 test 的 results（非仓库跟踪锚，见 CR-14-8） | — | PARTIAL |
| :199/250 | w_in 下限 | max(f·A_t/L, 1e-12/1e-11) | 带半宽数值地板 | 结构性不适用 | 防退化零宽，实验自订 | — | CONFIRMED |
| :209/238/257 | prod 判据阈 | ov > 1e-18 | 生产 overlap 面积"算相交"的门限 | 待确认 | 见 CR-14-7（判据方向与噪声地板关系无跟踪锚）；注释 :182 称噪声 ~1e-19 | 方向：偏绿侧容许 ov∈(噪声,1e-18] 的真相交被判 miss；反之 (−1e-18,0] 假阳性被压；关键见 finding | PARTIAL |
| :233 | 近相切带几何 | w_in=-1e-9, w_out=1e-8, gap | 构造"完全外侧有间隙"带 | 结构性不适用 | 实验自订几何 | — | CONFIRMED |
| :252 | 旋转角 | 30.0*PI/180 | 交叉 band 旋转 | 结构性不适用 | 实验自订 | — | CONFIRMED |
| :161/194/210/1065 循环界 | nsi<4, pf_i<3, targets[4] | 4,3 | 数组基数 | 结构性不适用 | 枚举基数 | — | CONFIRMED |

### findings

#### CR-14-7 · ORA-101 捕获 fast 查询的 used_fallback 旗标却从不断言它，使 face 边/角/极冠位置（本 Oracle 的核心靶点）可经保守回退 BFS 蒙绿，而非证明快速边缘枚举零漏选
- 轴：R（运行时：退化/回退路径冒充被测能力已验证）；兼 D（设计点名的"fast 路径边缘零漏选"未被真正检）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/tests/oracle_edge_crossing_test.cpp:204` `bool fb = false;` → `:205` `spherical::query_candidate_pixels_fast<double>(drop, hp, cands, &fb);` → `:207` `bool in_cand = std::binary_search(cands.begin(), cands.end(), ip);`——`fb` 之后**无任何读取/断言**（全文件 `fb` 仅出现在这三行）。
- 上位依据：本文件头 :14「生产侧: query_candidate_pixels_fast 必须包含目标像素」，:164「位置: face 内, face 边, face 角, 极冠」；被测函数文档 `spherical_overlap.h:396-404` 与实现 `spherical_overlap.cpp:1674-1677`（`in_polar_cap || box_touches_polar` → `query_candidate_pixels` 并置 `*used_fallback=true`）、`:1692-1696`（包围盒触 face 边界 → 同样回退 BFS 并置 true）。回退所用 `query_candidate_pixels` 注释（`spherical_overlap.cpp:1687-1688`）自述是"queryDisc 圆盘, 天然跨 face, buffer 3.0×hp_res"的保守完备查询。
- 现状：targets 中 face 边（`+2`，`:168`）、face 角（`nside-2`，`:169`）、极冠（`bighp 0`，`:170`）三类位置的 sliver band 紧贴这些边界/接缝构造，`query_candidate_pixels_fast` 对这些位置命中 `x0<0||x1>nside-1||box_touches_polar` 分支概率很高 ⇒ 走保守 BFS 回退（`fb=true`），而 BFS 按设计"零漏选"必然包含目标 ⇒ `in_cand` 恒真。测试既不要求 `fb==false`，也不区分"快速路径命中"与"回退路径命中"。
- 差在哪：Oracle 声称证明"快速 NESTED 边缘枚举对 sliver/edge-cross 零 false negative"，但对最可能暴露快速路径漏选的边/角/极冠靶点，判据实际由回退 BFS 达成——被测对象（fast 分支）未被这些 case 检验；作者已知有回退（否则不会传 `&fb`）却选择不作为门。
- 后果：对外主张"ORA-101 覆盖 face 边/角/极冠的 fast 候选完备性"不成立（这些绿可能全来自回退）；若 fast 分支真在接缝处漏选，本 Oracle 不会变红。方向=偏绿（false green，漏测试）。
- 定级：S2（PARTIAL：判据方向偏绿已定死——要求 `fb==false` 只会把现有绿 case 变红，绝不放行新绿；但"这些靶点当前究竟走 fast 还是回退"需实跑打印 `fb` 才能确证，故不升 S1）
- 怎么算修好：在 `ind==true` 分支对 fast 靶点补 `CHECK(!fb || <回退亦属预期>)`，或至少分桶统计并断言"至少 N 个真相交 case 在 `fb==false`（真走快速枚举）下 `in_cand` 成立"；把 face 内（必走 fast）与边/角/极冠（多走回退）分开计数，防聚合掩盖。同批须保留回退 case 的覆盖（回退也应对），不能因判绿而删靶点。
- 置信：PARTIAL（读了被测函数确认回退置 fb=true 且 BFS 恒含目标，但未实跑各靶点的 fb 取值分布）

**不报的核过项（判据独立性与求和残差）**：本 Oracle 的真值 `independent_intersect`（:91-115）为独立几何实现，未复用被测 `compute_overlap_area`，不自证；且它只判"相交是否存在"（布尔），不做面积求和闭合，故任务 ③ 关心的"求和残差掩盖逐格错配"在本文件不适用（该项针对文件 5）。检索式：`git grep -n "compute_overlap_area\|spherical_polygon_area" -- <本文件>` 仅见真值构造输入用面积、判据用布尔。

#### CR-14-8 · prod 判据阈 ov>1e-18 与被测 sliver 面积档/数值噪声 ~1e-19 的关系仅以注释自证，无跟踪标定件
- 轴：S（支撑——判据阈凭何）
- 位点：`:209` `bool prod = ov > 1e-18;`（另 :238/:257 同），噪声依据仅见 :182-185 注释「sliver 绝对值须高于 S-H 数值噪声 (~1e-19)…nside=2^22: 仅 f=1e-4（w_in×L≈5.9e-18）…」。
- 上位依据：1e-18 与 1e-19 的具体量级在 docs/science、docs/algorithms 面零命中（检索式：`git grep -rn "1e-18\|1e-19\|S-H 数值噪声" -- docs/algorithms/DRIZZLE_GEOMETRY.md`）。
- 现状：判据门 1e-18 由注释声称"高于噪声"，但噪声地板 ~1e-19 是经验描述，未见可复现标定件（本 test 目录未见 results 落跟踪面）。fracs 档（1e-4/1e-6/1e-8）随 nside 裁剪亦以注释里的近似乘积（"w_in×L≈5.9e-18"）自证。
- 差在哪：这是决定"真相交被判 prod 与否"的门限，落在 §2.5 的 `待确认`；缺可复现标定即无法排除"某档 sliver 面积恰落在 (噪声,1e-18] 被判红/漏"。（属实验单元自订，权重低。）
- 后果：影响本 Oracle 的判据可信度（可能把合法微 sliver 判 miss 或因门限选低而放过噪声假阳性），不改生产产品字节。方向双可能：门偏高→偏红（真 sliver 漏判），门偏低→偏绿（噪声假阳性）。
- 定级：S3（实验单元内判据，非产品语义；主要缺锚）
- 怎么算修好：把噪声地板与 1e-18 的推导/实测写进 results（落跟踪面）或 docs/algorithms，并在 test 里对"面积档下界"给可复现断言；或改用相对 `A_t` 的比例阈而非绝对 1e-18。
- 置信：PARTIAL（未见标定件，不能断定门限错，只登记缺锚）

---

## 文件 5 · lib/algorithms/drizzle/healpix_drizzle/tests/oracle_independent_test.cpp（273 行，全读）

候选零漏选独立 Oracle（R13 CAND-001）。真值 = 球面采样点归属（`radec2pix`，HEALPix 定义）+ 半平面点包含（`point_in_drop`，drop 边法向），**不调用**被测 `query_candidate_pixels_fast`/`compute_overlap_area` 作真值 ⇒ 期望值独立，非自证。判据：`fn = |Oracle真集 \ fast|`，`fn>0 ⇒ CHECK(false)`（单格错配即刻判红，无求和平均可掩盖）。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :29 | PI_ | 3.14159265358979323846 | 圆周率 | 公式导出 | 数学常数 | — | CONFIRMED |
| :58 | point_in_drop 容差 | -1e-12 | 半平面内含阈（≥−1e-12 算内） | 结构性不适用 | 实验单元自订几何容差（§4） | — | CONFIRMED |
| :75-79/96-100 | 边 4 分点 | t=1..3, /4.0 | 采样密度=尺度/4 | 结构性不适用 | 枚举/采样密度选择（§4） | — | CONFIRMED |
| :125 | make_drop half_deg | 0.5·scale/3600·pixfrac | RA/Dec 盒半宽（度） | 公式导出 | 0.5 半宽因子（结构）、3600=角秒/度定义常数、pixfrac 缩放；代码按式算 | — | CONFIRMED |
| :200/217/226 | 度↔弧度 | 180.0/PI_ | pix2ang→RA/Dec 换算 | 结构性不适用 | 单位换算定义常数 | — | CONFIRMED |
| :230-231 | 近极点 | 89.9, -89.9 | 极点测试位（避 RA 奇点，不取精确 90） | 结构性不适用 | 实验自订位置；精确极点在 RA 方向退化，取 89.9 规避 | — | CONFIRMED |
| :235 | scales[] | 0.1,1.0,10.0,60.0,3600.0 | 小 NSIDE 覆盖尺度档（角秒） | 结构性不适用 | 实验覆盖选择（§4） | — | CONFIRMED |
| :236 | pfs[] | 0.1,0.5,1.0 | pixfrac 覆盖档 | 结构性不适用 | 实验覆盖选择 | — | CONFIRMED |
| :238 | 小 NSIDE | 16, 64 | 穷举网格 | 结构性不适用 | 实验覆盖选择 | — | CONFIRMED |
| :194/203 | hp1(1)/hp8(8) 面中心/边/角取样 | 1, 8; 角 {0,7}/{0,3,4,7} | nside=1 取 12 面中心、nside=8 取面角/边 | 结构性不适用 | 位置生成，非科学量 | — | CONFIRMED |
| :206/212 | spread3 / <<2*i | Morton 位交织 | NESTED 索引位运算 | 结构性不适用 | 位移量（HEALPix NESTED 定义） | — | CONFIRMED |
| :251 | hi_nsides[] | 16384,262144,1048576,4194304 (=2^14/18/20/22) | 高 NSIDE 覆盖档 | 结构性不适用 | 实验覆盖选择 | — | CONFIRMED |
| :252 | hi_scales[] | 0.0503,0.1,1.0,10.0,12.883 | 高 NSIDE 端点尺度（角秒） | 公式导出 | :16 注释"节点余量 0.0503"/px(2^22)、12.883"/px(2^14)"=HEALPix 像素角尺度端点（`pixelResolutionArcsec` 口径）；取端点为余量边界 | 若余量口径与引擎 `HP_CIRCUMRADIUS_FACTOR` 不一致会弱验，见 CR-14-10 | PARTIAL |
| :259/264-266 | 高 NSIDE pixfrac | 固定 1.0 | 高 NSIDE 只测 pf=1.0 | 结构性不适用 | 实验覆盖选择（pf<1 高 NSIDE 未测） | — | CONFIRMED |
| :151/159/176 | fn 判据 | fn>0 ⇒ FAIL | 单格漏选即红 | 结构性不适用 | 布尔计数门，非阈 | — | CONFIRMED |

### findings

#### CR-14-9 · 本 Oracle 的"候选零漏选（false negative=0）"真值是**采样下界**，仅边缘/sliver 相交的像素对 Oracle 亦不可见，故"零漏选"对外主张只在采样可检子集上成立
- 轴：D（设计点名的完备性主张 vs 实际所检）
- 位点：`:2` 标题「候选零漏选独立 Oracle」、`:17`「硬门: false negative = 0 (fast 候选 ⊇ Oracle 真集)」、真值实现 `oracle_intersects`（:107-120）；高 NSIDE 分支真值更弱：`:152-177` 仅测「drop 采样点归属像素」+「中心像素及其 neighbors 的中心是否落入 drop」。
- 上位依据：兄弟 Oracle 文件 4（`oracle_edge_crossing_test.cpp:4-5`）明确写「采样型 Oracle 只检查像素/采样点归属, 对仅边缘交叉或细小 sliver 重叠 (1e-8~1e-4 目标像素面积) 会漏判真实相交」——即本文件所属的采样法被同一目录的另一件自认存在该盲区。被测函数文档 `spherical_overlap.h:397`「零漏选」是产品对外承诺。
- 现状：小 NSIDE 穷举分支的真值 `oracle_intersects` 判"相交"依赖 drop 的 13 采样点落进像素、或像素的 13 采样点落进 drop；两凸多边形仅边缘交叉/含一个不触任何采样点的 sliver 重叠时，Oracle 判"不相交"，于是生产 fast 若也漏选该像素则 fn 不增、测试仍绿。高 NSIDE 分支连"像素采样点→drop"方向都不做（只中心+邻居），盲区更大。
- 差在哪：标题与 :17 的"零漏选/真集"表述把"fast ⊇ 采样下界"当成"fast ⊇ 几何真集"来主张；单方向判据（无 false-positive/精度门）叠加采样下界，使"零漏选"仅在被采样命中的子集上被保证。（该盲区由文件 4 的边交叉 Oracle 补，属分工，非孤漏。）
- 后果：若生产 fast 在某接缝/角位置漏选一个仅边缘相交的像素，本 Oracle 不报警；"fast 候选完备"这一对外主张的独立证据需与文件 4 合读才成立，单独引用本件会高估其覆盖。方向=偏绿。
- 定级：S3（分工上由文件 4 覆盖 sliver/edge；本件自身主张措辞偏强，无产品字节错值）
- 怎么算修好：把 :17 措辞改精确为"fast ⊇ 采样可检真集（边/sliver 见 ORA-101）"，或在本件对穷举分支补一条"drop 每条边中点→像素"的额外采样以缩小盲区；两 Oracle 覆盖范围在文档中交叉引用。
- 置信：PARTIAL（推导采样下界<几何真集成立；未逐例统计本 test 实际有多少 case 落在盲区，需实跑）

**不报的核过项（求和残差与自证）**：任务 ③ 关心的"面积闭合/求和残差掩盖逐格错配"在本件不适用——本件不累加面积，`fn` 为漏选像素计数，`:180 if (fn>0)` 任一漏选即 `CHECK(false)`，无平均化；真值 `point_in_drop`/`radec2pix` 与被测 `query_candidate_pixels_fast` 独立实现，非自证。检索式：`git grep -n "sum\|+=.*area\|total" -- <本文件>`（无面积累加）。

---

## 文件 6 · lib/algorithms/drizzle/healpix_drizzle/tests/p0_sip_order_guard_test.cpp（167 行，全读）

P0-1 SIP order 越界写守卫复现测试（bughunt_p0_io）。黑盒驱动生产 `hp_drizzle_run`，断言 order∉[0,5] 硬失败 rc=-10、合法/缺省 order 不被 SIP 拒。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响 | 置信 |
|---|---|---|---|---|---|---|---|
| :63-64 | W,H / px fill | 4,3 / 100.0f | 最小 data 块尺寸与常数填充 | 结构性不适用 | 夹具尺寸/占位值，非科学量 | — | CONFIRMED |
| :74-77 | CRVAL1/2, CRPIX1/2 | 202.5,47.2,2.0,1.5 | 合法 WCS 参考点 | 结构性不适用 | 夹具 WCS 常数，非被测科学量（任意合法值即可） | — | CONFIRMED |
| :78-79 | CD1_1/CD2_2 | -1.0e-4, 1.0e-4 | 合法 CD 线性项（度/像素） | 结构性不适用 | 夹具 WCS 常数 | — | CONFIRMED |
| :86-90 | SIP 系数值 | 1.0e-6 | 复现越界写用的 A_8_8/A_7_7/A_6_6 系数 | 结构性不适用 | 恶意夹具载荷，非科学量 | — | CONFIRMED |
| :100 | nside | 4 | 运行网格（2 的幂合法值） | 结构性不适用 | 夹具运行参数 | — | CONFIRMED |
| :100 | pixfrac | 0.5 | 运行核缩放（合法域内） | 结构性不适用 | 夹具运行参数 | — | CONFIRMED |
| :100 | precision_mode | 0 | FP32 路径 | 结构性不适用 | 枚举 | — | CONFIRMED |
| :85/113/123/128 | 恶意/边界 order | 8, -1, 6 | 越界档位（>5 与 <0） | 结构性不适用 | 攻击夹具值，测守卫 | — | CONFIRMED |
| :133/155 | 合法 order | 5, 2 | 上界与低阶合法值 | 文献值/合同值 | SIP order 正式支持 [0,5]：本件 :8 注释、`hp_drizzle_api.cpp:465-466`、模块面 `DRZ_SIP_ORDER_MAX 5`（module_entry.cpp:721，标「P0-1 order≤5 冻结」）三处同源 | — | CONFIRMED |
| :139/142/145 | B/AP/BP 越界 order | 7, 9, -3 | 四键同口径攻击值 | 结构性不适用 | 攻击夹具值 | — | CONFIRMED |
| :100 (a[36]/6×6) | 系数数组 | 36=6·6 | wcs.sip.a 尺寸（order+1 的平方） | 公式导出 | order≤5 ⇒ 索引 i·6+j ≤ 5·6+5=35<36；越界 order=6 → 6·6+6=42>35 越界（本件复现的核心不等式） | — | CONFIRMED |
| :113 等 | 合法上界判据 | order∈[0,5] | 守卫读实际 atoi 值并逐一比界 | 文献值/合同值 | 见上 `hp_drizzle_api.cpp:457-469`（守卫确在 `a_order_str` 分支内读 A/B/AP/BP 四 atoi 并比 [0,5]，读到被守对象） | — | CONFIRMED |
| 全程 | 期望 rc | -10 | SIP 越界硬失败码 | 结构性不适用 | 与生产码约定：`hp_drizzle_api.cpp:397`「-10=SIP order 越界」 | 见 CR-14-10（-10 不唯一） | CONFIRMED |
| :99 | 构造失败码 | -999 | 夹具 build 失败哨兵 | 结构性不适用 | 测试内部哨兵 | — | CONFIRMED |

### findings

#### CR-14-10 · 仅 T1 用错误消息把 rc=-10 钉到"SIP order 越界"，T2/T3/T5a-c 只断言 rc==-10，而生产另有非-SIP 的 -10 出口（drizzle 运行失败），可致这些守卫用例误绿
- 轴：R（判据不唯一 ⇒ 假绿）；兼 D
- 位点：`p0_sip_order_guard_test.cpp:114-119` 对 T1 有 `CHECK(result.error_msg 含 "SIP order")`；但 `:124`（T2 A_ORDER=-1）、`:129`（T3 A_ORDER=6）、`:140`（T5a B_ORDER=7）、`:143`（T5b AP_ORDER=9）、`:146`（T5c BP_ORDER=-3）均只 `CHECK(rc == -10, ...)`，无消息校验。
- 上位依据：`hp_drizzle_api.cpp:468` SIP 越界 `return -10`（守卫）；**同文件 `:1102` 另有一处 `return -10`**，其上文 `:1099-1101` 是「Drizzle 失败」通用运行错误也返回 -10（`:397` 注释仅声明 -10=SIP 越界，与实际第二出口不符）。
- 现状：`-10` 在生产里不是 SIP 守卫的专属码；测试对 5 个越界用例只比 `rc==-10`，未像 T1b 那样确认错误来自 SIP 守卫。
- 差在哪：若守卫被改动/移除而某越界夹具转由其它 -10 路径（或反向件 `hp_drizzle_api.cpp:105` 同族）返回，T2/T3/T5 仍绿——守卫回归不被这些用例捕获。方向定死=偏绿（rc==-10 可被错误来源满足，断言不区分来源即放行）。缓解项：合法 order 用例 T4/T6 断言 `rc != -10`，若合法帧不会触发 :1102 的 drizzle -10，则间接说明这些帧确能过守卫到成功；但这仍不锁死越界帧的 -10 一定来自守卫。
- 后果：P0-1「SIP 越界写已封堵」这一对外安全主张的回归证据变弱——移除 SIP 上界校验时，T1 会红（有消息锚）但 T2/T3/T5 可能仍绿，给"守卫仍在"的错觉。
- 定级：S2（缺实跑触发路径确认"移除守卫后 :1102 是否会对这些夹具返回 -10"，故不升 S1；但误绿方向已定死）
- 怎么算修好：对每个期望 -10 的用例补 `strstr(error_msg,"SIP order")` 断言（照 T1b）；或生产把"Drizzle 运行失败"改用独立码（如 -12）使 -10 成为 SIP 守卫专属；两者同批改，并同步 `:397` 错误码文档。
- 置信：PARTIAL（读码确认存在第二个 -10 出口；未实跑"移除守卫"验证这些夹具是否会落到 :1102）

**不报的核过项（守卫确读被守对象 / T5d 语义）**：`hp_drizzle_api.cpp:457-469` 证守卫在 `if (a_order_str)` 内对 A/B/AP/BP 四值 `atoi` 后逐一比 [0,5]，读到被守对象（非仅检存在），系数写循环 `:472-478` 以已校验的 `a_order≤5` 为界（`i*6+j≤35`），确不越界；T5d「仅 B_ORDER=7 无 A_ORDER → 不激活 SIP、不越界」（`:147-151`）与该代码结构一致（整个 SIP 块在 `a_order_str` 门内），非漏测。检索式：`git grep -n "a_order_str\|i \* 6 + j" -- hp_drizzle_api.cpp`。

---

## 开工 / 收工 git status --porcelain 原文

基线 HEAD=c8f64e9a。两次输出一致，符合前言 §1 期望（`?? ACSD整治工作包_AUDIT-06.zip`、`?? site/`），零 git 写、零仓库文件改动。

开工：
```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```
收工：
```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

---

## 本批最高危三条

1. **CR-14-7（R，偏绿）· ORA-101 边缘/sliver Oracle 的核心靶点（face 边/角/极冠）大概率走 fast 查询的保守回退 BFS 蒙绿**：测试捕获 `used_fallback`（fb）却从不断言它（`oracle_edge_crossing_test.cpp:204-207`），而 `spherical_overlap.cpp:1674-1696` 在触边界/极冠时置 `*used_fallback=true` 并改调"天然零漏选"的 `query_candidate_pixels`——于是"fast NESTED 边缘枚举对 sliver 零 false negative"这一被点名的主张，在最可能暴露其缺陷的位置根本没被检验。修法：对真相交靶点断言 `fb==false` 或分桶统计。
2. **CR-14-10（R，偏绿）· P0-1 SIP 守卫回归证据被非专属错误码稀释**：5 个越界用例只比 `rc==-10`（`p0_sip_order_guard_test.cpp:124/129/140/143/146`），仅 T1b 用消息钉来源；而 `hp_drizzle_api.cpp:1102` 另有一处"Drizzle 运行失败"也 `return -10`（与 `:397` 注释" -10=SIP 越界"矛盾）。守卫若被移除，T1 变红但 T2/T3/T5 可能仍绿。修法：越界用例统一补 `strstr("SIP order")`，或给运行失败独立码。
3. **CR-14-3 / CR-14-1（C，低危但成对）· 两生产入口对"同一参数"处置与自述不一致**：`acc_bytes` 在同一 `drz_plan` 函数内注释（3×f64/32B）、代码（6×f64/48B）、真结构 `TileLeafAccumulatorT`（drizzle_engine.h:78-82：4×f64+u32/40B）三口径互斥（CR-14-3）；hips 入口把 `max_workers` 解析后从不消费、注释却称"仅作回显"，而 drizzle 入口真读真用（CR-14-1）。均不产产品字节，属"对外由元数据推导/文档-代码一致"主张的瑕疵。

---

## 我读不动或需要实跑才能定的

1. **CR-14-7 fb 分布**：需实跑 ORA-101 并打印每个 `ind==true` 靶点的 `fb` 取值，确认 face 边/角/极冠究竟走 fast 还是回退（只读代码只能证明"回退会置 fb=true 且 BFS 恒含目标"，不能证明当前 case 真的走了回退）。跑法：在 `oracle_edge_crossing_test.cpp:207` 后加 `printf("fb=%d\n", fb)` 单独编译该 test（**本节点不跑构建/ctest**，遵前言 §1）。
2. **CR-14-10 移除守卫后的落点**：需实跑或改码实验确认——若删 `hp_drizzle_api.cpp:465-469` 上界校验，T2/T3/T5 夹具（合法 WCS、4×3 data、nside=4）是会继续走到 `:1099` 的 drizzle 失败并返回那个 -10，还是走到别的码。决定该 finding 是 S2 还是升 S1。
3. **CR-14-8 噪声地板标定**：`ov>1e-18` 与"S-H 数值噪声 ~1e-19"仅见 `oracle_edge_crossing_test.cpp:182-185` 注释自证，未见落跟踪面的 results/标定件；本目录未见对应 results 文件，无法确认 fracs 各档面积确在 (噪声,1e-18] 之外。需查该 test 的 results 或在 test 内补可复现的面积档下界断言。
4. **CR-14-6 许可证延后 vs 疏漏**：nanoflann 是否已被 BLD-004（依赖锁/许可证边界）计划显式覆盖，需读 BLD-004 工包/登记面（不在本批可及文件）才能判定"未登记"是有意延后还是遗漏。
5. **CR-14-9 盲区规模**：本 Oracle 有多少 case 落在"仅边缘相交且无采样点命中"的盲区，需实跑统计；只读推导只能证明"采样真集 ⊆ 几何真集"这一必然包含关系成立。
6. **hips `aio_hips_set_drizzle_provenance` 是否校验 NaN（CR-14-2）**：需读 `lib/infrastructure/aio/src/hips/aio_hips_writer.cpp`（不在本批清单）确认其对 prov_pixfrac/prov_scale 的 NaN/范围是否真检，以定该 finding 是否升 S1（当前 PARTIAL，缺"legacy 是否覆盖 NaN"一跳）。
