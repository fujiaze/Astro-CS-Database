# 通读 CR-42 — 板解 IPv 头文件与鲁棒精化

- 基线 HEAD：`c8f64e9a`
- 批次：CR-42
- 清单（5 项，逐项通读，不漏不越）：
  1. `lib/algorithms/platesolve/cpp/ipv/include/ipv_solver.h` —— 已读：是（表 21 项，findings CR-42-19…21）
  2. `lib/algorithms/platesolve/cpp/ipv/include/ipv_triangle.h` —— 已读：是（表 16 项，findings CR-42-9…14）
  3. `lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h` —— 已读：是（表 46 项，findings CR-42-1…8）
  4. `lib/algorithms/platesolve/cpp/ipv/include/ipv_wcs.h` —— 已读：是（表 19 项，findings CR-42-15…18）
  5. `lib/algorithms/platesolve/cpp/ipv/src/ipv_robust_refine.cpp` —— 已读：是（表 37 项，findings CR-42-22…30）

<!-- PROGRESS: 5/5 -->

## 开工 git status --porcelain 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

本批实际通读顺序：3 → 1 → 2 → 4 → 5（先读类型定义才能判单位口径归属），清单五项全覆盖、无越出。

---

## 3. `lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h`（286 行，全读）

### 3-T §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| ipv_types.h:65,74-77 | `A[36]/B[36]/AP[36]/BP[36]` | 36 | 前向/逆向 SIP 系数定长数组容量（6×6 网格 i,j∈[0,5]） | 结构性不适用 | 不是科学量：只是系数存储槽位数，阶数语义由 `order` 字段承载；跨语言布局由 `ipv_abi_layout_probe.cpp:118-120` 逐字段 offsetof 锁 | — | CONFIRMED |
| ipv_types.h:65,66 | 索引式 `i*6+j` | 6 | 系数寻址步长（与消费方 `wcs_transform` evalSip 同址） | 结构性不适用 | 布局约定非科学量；由 `ipv_abi_layout_lock` 机器校验 C↔ctypes 一致 | — | CONFIRMED |
| ipv_types.h:65,78 | 前向阶上限 `order<=5` | 5 | 36 槽位下允许的最高 SIP 阶 | 待确认 | 条文面零提及（检索式 `git grep -n "sip_order\|SIP 阶\|order" docs/algorithms/PLATESOLVE.md` 只命中量测域与 UNJUSTIFIED 条目，无阶上限推导）；布局上 36 槽可容 order 7（(7+1)(7+2)/2=36），故 5 是网格步长 6 的副产物而非导出值 | 保守 = 低阶（高阶在内点少时过拟合畸变）；影响面 = 交付 SIP 系数与逆投影精度 | PARTIAL（未核 `fit_sip` 实际取阶逻辑） |
| ipv_types.h:67 | AP/BP「41 网格阶 5」 | 41 / 5 | 逆向 SIP 拟合采样网格每轴点数与阶 | 结构性不适用 | 实现锚 `ipv_wcs.cpp:418 const int NB_GRID = 41;`（注释「整改: 7 -> 41」）与本行一致；网格点数＝离散化分辨率选择，不承载科学量 | — | CONFIRMED |
| ipv_types.h:68,69,80,81 | `APx[100]/BPx[100]`、`i*10+j` | 100 / 10 | 扩展逆向布局槽位与寻址步长（WCS-003） | 结构性不适用 | 同上：容量与索引步长 | — | CONFIRMED |
| ipv_types.h:69,82 | `apx_order<=9` | 9 | 扩展逆向阶上限 | 待确认 | 本批未追到 `APX_ORDER` 定义值（`ipv_wcs.cpp:601` 只用不改），docs 零登记 | 保守 = 低阶；影响面 = 高畸变场逆表达精度 | PARTIAL |
| ipv_types.h:70 | 「81x81 网格」 | 81 | 高阶逆拟合采样网格每轴点数 | 结构性不适用 | 实现锚 `ipv_wcs.cpp:542 const int NB_GRID_X = 81;` | — | CONFIRMED |
| ipv_types.h:71-72 | 「偏移与 sizeof 兼容面不变」 | — | 追加尾部字段的 ABI 兼容主张 | 待确认 | 无运行时自描述兜底：镜像的对外结构 `IpvWcsResult`（`ipv_api.h:39-61`）无 `struct_size/abi_version`；见 CR-42-7 | — | CONFIRMED（零字段为事实核对） |
| ipv_types.h:92 | U「50 颗」 | 50 | 图像侧选星数说明 | 待确认 | 与 `img_n_target` 默认 20（:249）、生产硬覆盖 60（`ipv_solver.cpp:445,1516`）三值不合；见 CR-42-6 | 保守方向无定义（星数↑⇒投票更密但畸变敏感度↑） | CONFIRMED |
| ipv_types.h:92 | U「角秒坐标」 | — | U 向量单位声明 | 待确认 | 与实现相反：`ipv_select.cpp:1125 output.U[i].x = (det_x[idx] - cx); // 像素, 不乘 s0`；见 CR-42-1 | — | CONFIRMED |
| ipv_types.h:93 | W「~75-150 颗」 | 75-150 | Gaia 侧星数说明 | 待确认 | 导出关系 = `n_target × gaia_density_ratio × 面积比`（`ipv_select.cpp:306-316`），本批未复算逐帧取值 | 保守 = 多取（查询成本↑） | PARTIAL |
| ipv_types.h:96-97 | `gaia_ra/dec` 度 | — | 天球坐标单位 | 结构性不适用 | 单位定义面（度），与 `crval` 同口径 | — | CONFIRMED |
| ipv_types.h:109 | 「n_ret >= 每文件上限」 | — | 触顶判据 | 待确认 | 实现 `ipv_select.cpp:498-499` 与本行一致，但「每文件上限」为手抄字面量 ⇒ 见 CR-42-4 | 上限被下调时该判据静默失效（偏错） | CONFIRMED |
| ipv_types.h:113 | 「\|N-N_target\|/N_target <= tol」 | tol | 收敛判据式 | 公式导出 | 相对偏差判据，`tol = density_tolerance`（`ipv_select.cpp:426`），代码按式算 | — | CONFIRMED |
| ipv_types.h:115 | alpha = dlog10N/dmag | — | 星等-密度斜率定义 | 公式导出 | 定义式；实现用相邻两次查询有限差分更新（`ipv_select.cpp` 割线迭代） | — | CONFIRMED |
| ipv_types.h:124 | `s0` arcsec/pixel | — | 像元尺度单位口径 | 结构性不适用 | 单位定义常数；由 `206264.806·pixel_size_um/focal_length_mm` 导出（`compute_fov_density`，`ipv_select.cpp:287-295` 用 `diag_pix*s0`） | — | CONFIRMED |
| ipv_types.h:126 | 「100-300 颗」 | 100-300 | 精化阶段网格采样目标数 | 待确认 | 实现锚 `ipv_robust_refine.cpp:205`（本表见文件 5）；docs 零登记 | 保守 = 少采（样本不足⇒稳健估计退化） | PARTIAL |
| ipv_types.h:146 | `ctype[2][16]` | 2 / 16 | FITS 关键字缓冲 | 结构性不适用 | 数组维度；16 ≥ 8 字符关键字 + NUL | — | CONFIRMED |
| ipv_types.h:149 | `error[256]` | 256 | 错误串缓冲 | 结构性不适用 | 缓冲区尺寸；与 `IpvWcsResult.error_msg[256]`（`ipv_api.h:60`）同尺寸 | — | CONFIRMED |
| ipv_types.h:159,160 | `distances[5]`、`neighbor_idx[5]` | 5 | K-1 个邻星槽位 | 结构性不适用 | 数组维度（K=6 含 pivot） | — | CONFIRMED |
| ipv_types.h:159 | 邻星距离「角秒」 | — | 描述符单位口径 | 待确认 | 与 U 实际单位（像素）冲突；多边形路径整体被本仓判死（`ipv_dead_params_manifest.json:494-508`）⇒ 生产不触发 | — | PARTIAL |
| ipv_types.h:168 | confidence `max/(max+second+1)` | +1 | 置信度归一（防除零平滑） | 公式导出 | 定义式，+1 为结构性平滑项 | — | CONFIRMED（定义自洽） |
| ipv_types.h:181 | hash 乘子 `31` | 31 | 散列混合常数 | 结构性不适用 | 无科学语义（散列分布用），不参与任何判据 | — | CONFIRMED |
| ipv_types.h:201 | `PROSACResult::rms`「角秒」 | — | RMS 单位声明 | 待确认 | 唯一消费面自述为像素（`ipv_ransac.cpp:331,341,482,486` 打印 "px"、"good_rms_threshold 现在是像素单位"）⇒ 见 CR-42-1 | — | CONFIRMED（两处标注互斥为事实） |
| ipv_types.h:206 | `score = n_inliers/(1+RMS)` | +1 | 评分式 | 公式导出 | 实现逐字一致：`ipv_ransac.cpp:757 result.score = best_n_inliers / (1.0 + best_RMS);` | — | CONFIRMED |
| ipv_types.h:212 | `FlipModeResult::mode` 0/1/2/3 | 4 | flip 模式枚举基数 | 结构性不适用 | 枚举基数；类型本身无使用者 ⇒ 见 CR-42-8 | — | CONFIRMED |
| ipv_types.h:220-222 | 「24 字段」「无任何配置→IpvParams 注入路径」 | 24 | 可配置性全局主张 | 待确认 | 与事实不合 ⇒ 见 CR-42-3 | — | CONFIRMED |
| ipv_types.h:233 | `polygon_sides = 6` | 6 | 多边形描述符边数 | 待确认 | 条文面零提及（`git grep -n "polygon_sides\|K=6" docs` 零命中）；生产走三角形匹配 ⇒ 无科学效果 | 保守 = 少边（描述符退化风险↑） | CONFIRMED（锚缺失为事实） |
| ipv_types.h:234 | `n_pivot = 30` | 30 | pivot 星数 | 待确认 | 零锚；仅 `cpp/ipv/REPORT.md:77` 回显默认值（文档自引代码） | 保守 = 少 pivot（命中率↓⇒偏红，不产生错值） | CONFIRMED |
| ipv_types.h:235 | `sigma_d_arcsec = 0.0` | 0.0 | 「0=自适应」哨兵 | 结构性不适用 | 哨兵取值，不含量纲语义；自适应式 `max(0.5×FWHM×s0, 2.0")` 只见于 `cpp/ipv/REPORT.md:78`，其 0.5/2.0 零锚 ⇒ 该两数记 待确认 | 保守 = 大容差（误配↑但漏配↓） | PARTIAL |
| ipv_types.h:236 | `vote_threshold = 2` | 2 | 投票接受阈值 | 待确认 | 与 `IPV_PIPELINE.md:198`（"vote ≥ vote_threshold 默认 2"）同值，但本仓登记面自述生产判据为字面量 3（`ipv_dead_params_manifest.json:540-545`），且 `ipv_polygon.cpp:737-778` 另有实现 ⇒ 三处三口径 | 保守 = 高阈值（候选少⇒偏红） | CONFIRMED |
| ipv_types.h:239 | `ransac_max_iter = 2000` | 2000 | RANSAC 迭代上限 | 待确认 | 零锚（docs 无；`cpp/ipv/REPORT.md:80` 只回显）⇒ 属"迭代上界的实现选择"边缘：它直接决定最优解被找到的概率，不是纯结构常数 | 保守 = 多迭代（成本↑，成功率↑） | CONFIRMED |
| ipv_types.h:240 | `ransac_inlier_threshold_arcsec = 3.0` | 3.0 | 内点阈值 τ | 待确认 | 权威容差登记册 `docs/algorithms/PLATESOLVE.md §9` 逐条登记了 0.01″/5.0″/0.002/1.345/15/1e-6，本值零登记；单位两说见 CR-42-1 | 保守 = 小 τ（内点少⇒偏红）；影响面 = PROSAC 判据与内点集 | CONFIRMED |
| ipv_types.h:241 | `good_rms_threshold = 1.5` | 1.5 | 早停 RMS 阈 | 待确认 | 零锚（PLATESOLVE.md §9 无）；单位与 :240 同族争议（消费面标 "px"） | 保守 = 小阈（不早停⇒算力多花但解更优） | CONFIRMED |
| ipv_types.h:241 | 早停门「n_inliers>=4」 | 4 | 早停最小内点数 | 公式导出 | 4 = 相似变换未知数（s,θ,tx,ty）个数 ⇒ 少于此不可辨识；实现 `ipv_ransac.cpp:487` 真按此判 | — | CONFIRMED |
| ipv_types.h:242-243 | `s_min=0.90 / s_max=1.10` | 0.90/1.10 | 尺度合法窗（±10%） | 待确认 | 注释称"符合项目约束"，`git grep -n "s_min\|±10%" docs` 零命中；同一文件消费面另说默认 `[0.95, 1.05]`（`ipv_ransac.cpp:18`）⇒ CR-42-2 | 保守 = 窄窗（漏解⇒偏红）；宽窗⇒误配可入产品 | CONFIRMED |
| ipv_types.h:246-249 | `img_n_target = 20`（注释「默认 20 颗」） | 20 | 图像侧目标星数 | 待确认 | 见 CR-42-6（20/50/60 三值） | 星数↑⇒三角形组合爆炸稀释投票（:247 自述） | CONFIRMED |
| ipv_types.h:247 | `C(60,3)=34220`、`C(20,3)=1140`、「30倍」 | 34220/1140/30 | 组合数论证 | 公式导出 | 本批独立复算：C(60,3)=60·59·58/6=34220 ✓；C(20,3)=20·19·18/6=1140 ✓；比值 30.02 ✓ ⇒ 论证数值成立 | — | CONFIRMED |
| ipv_types.h:248 | 「20→40→60 自适应扩充」 | 20/40/60 | 扩档序列 | 待确认 | 实现锚仅见生产单点覆盖 `ipv_solver.cpp:445 p_adapt.img_n_target = n_target;` 与 `:1516 = 60`，未见 20→40→60 三档触发链（`max_vote < vote_threshold` 判据在死符号 `extract_consensus` 内） | 若三档不存在 ⇒ 注释描述的自适应能力未实现（D 轴面） | PARTIAL（未逐行追 solver.cpp 循环全貌） |
| ipv_types.h:250 | `gaia_density_ratio = 1.5` | 1.5 | Gaia 目标密度倍率 | 待确认 | `git grep -n "gaia_density_ratio" docs` 零命中（仅 API 清单签名）⇒ 条文面零提及 | 保守 = 高比值（多星⇒查询成本↑、误配↑） | CONFIRMED |
| ipv_types.h:251 | `gaia_query_radius_factor = 0.55` | 0.55 | 查询半径因子 | 待确认 | `docs/plugins/algorithms_phase1/03_star_detection.md:69` 只镜像默认值并注「缺省 = ipv::IPVSolverParams…」，无取值依据 | 保守 = 大半径（多星、边缘畸变入池） | CONFIRMED |
| ipv_types.h:254 | 「原 `m_lim_step=0.5`」 | 0.5 | 被替换的旧线性步长（历史说明） | 结构性不适用 | 已退役说明，非现行值（`git grep -n "m_lim_step" lib docs` 现值零命中） | — | CONFIRMED |
| ipv_types.h:255-260 | `m_lim_alpha_prior = 0.2885`（+「实测 0.243–0.456, R²>0.986」「10/10 帧通过」） | 0.2885 | 割线迭代 alpha 先验（实验标定） | 待确认 | 标定件路径 = `run/perf-fix/P4-magiter/REPORT.md`，而 `run/*` 被 `.gitignore:17` 排除（跟踪面仅 `run/.gitkeep`）⇒ 按 §2.5 档位 3「指 run/ 即判不合格」；见 CR-42-5 | 保守 = 取实测带下沿 0.243（步长偏小⇒迭代更慢、更易触 max_iter） | CONFIRMED |
| ipv_types.h:261-262 | `m_lim_alpha_min=1e-3 / _max=100.0` | 1e-3/100 | alpha 限幅带 | 待确认 | 零锚；作用 = 防除零/防发散（`ipv_select.cpp:414-419` fail-closed 前置校验） | 保守 = 带内（越窄越不会走飞） | CONFIRMED |
| ipv_types.h:263-265 | `m_lim_safety = 3.0`（+「余量 +0.36..+1.07 mag」） | 3.0 | N_target = n_target × safety 的倍率 | 待确认 | 同上 run/ 锚；登记面 `03_star_detection.md:68` 依据列写「——」即零依据，且该键实际可被配置覆盖（CR-42-3） | 保守 = 大 safety（多星） | CONFIRMED |
| ipv_types.h:266-267 | m0 公式系数 `6`、`1.5·log10(f_mm)`、`2·log10(t_s)`、上界 `13` | 6/1.5/2/13 | 极限星等初值经验公式 | 待确认 | 实现逐字按式算（`ipv_select.cpp:438-440`），但系数无任何文献/推导：`git grep -n "1.5 \* std::log10\|2.0 \* std::log10" docs` 零命中 | 保守 = 偏暗（少星）；影响面 = Gaia 查询数与选星池 | CONFIRMED |
| ipv_types.h:268-269 | `m_lim_m0_exposure_s=180.0` / `m_lim_m0_offset=-4.0`（+「系统性偏暗 3.5–5.5 mag」） | 180 / −4 | 名义曝光与系统偏差修正 | 待确认 | run/ 锚（同上，判不合格）；「偏暗 3.5–5.5 mag」的实测帧清单不在跟踪面 | offset 取带内更正值 = 更亮 ⇒ 更多星 ⇒ 保守；−4.0 落在 −3.5 端 ⇒ 偏不保守 | CONFIRMED |
| ipv_types.h:270-271 | `m_lim_clamp_lo=6.0` / `clamp_hi=22.0` | 6/22 | 迭代星等夹取带 | 待确认 | 「低于此无星」无锚；22 未说明与 Gaia 星表极限星等的关系 | 带宽方向对结果单调（下界↑⇒星少） | CONFIRMED |
| ipv_types.h:272 | `m_lim_zero_step = 3.0` | 3.0 | N=0 时的 +mag 步长 | 待确认 | 零锚；实现 `ipv_select.cpp:428` 带回退默认 3.0（同值二次写死） | 步长大⇒更快触星（偏多星） | CONFIRMED |
| ipv_types.h:273-276 | `m_lim_gaia_cap_per_file = 200000.0` | 200000 | Gaia 每文件返回上限（触顶检测用） | 待确认 | 注释自称「本模块只读该数量」，实为手抄副本（正本 `lib/infrastructure/gaia_xpsd_client/src/gaia_client.c:33 #define MAX_STARS_RESULT 200000`，当前两值相等）⇒ 见 CR-42-4 | 真上限下调⇒判据永不触发（偏错）；上调⇒误判触顶（偏保守） | CONFIRMED |
| ipv_types.h:277 | `m_lim_max_iter = 4` | 4 | 最大 Gaia 查询次数 | 待确认 | `03_star_detection.md:70` 只镜像默认值，零依据 | 次数↑⇒更可能真收敛（成本↑） | CONFIRMED |
| ipv_types.h:278 | `density_tolerance = 0.1` | 0.1 | 迭代终止相对容差 | 待确认 | 零锚（PLATESOLVE.md §9 未登记）；实现 `ipv_select.cpp:426` 回退同值 | 收紧⇒更难判收敛（偏红） | CONFIRMED |

（本文件数值项计 46 行，合并族已列全部位点。）

### 3-F findings

### CR-42-1 · U 向量单位在 `ipv_types.h` 内同时被写成"像素"和"角秒"，实现为像素；同族的 τ/RMS/good_rms 在消费面又被标成 "px"
- 轴：C（主轴，代码注释↔实现口径）＋ S（阈值量纲未定）
- 位点：
  - `lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h:38` `double x;          // X坐标 (U 向量: 像素 / W 向量: 角秒)`
  - `lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h:92` `std::vector<StarPoint> U;   // 图像侧星点 (角秒坐标, 50 颗)`
  - `lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h:127-128` `// 坐标同 U 约定: 像素坐标…` / `std::vector<StarPoint> U_full;   // 全部检测星点 (像素坐标, 原点图像中心, Y-up)`
  - `lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h:201` `double rms;                             // RMS (角秒)`、`:240` `ransac_inlier_threshold_arcsec = 3.0; // 内点阈值 (τ)`、`:241` `good_rms_threshold = 1.5;  // 足够好解即停阈值(角秒)…`
  - `lib/algorithms/platesolve/cpp/ipv/include/ipv_solver.h:39` `// U - 图像侧星点 (像素坐标, 原点图像中心)`、`:82-83` `U - 图像侧星点 (像素, …) / W - 星表侧星点 (角秒, …)`
- 上位依据：条文面零规定该结构单位口径（检索式：`git grep -n "StarSelection\|角秒坐标" docs/algorithms` 零命中；`docs/plugins/algorithms_phase1/03_star_detection.md` 只在 §5 讲配置键，不定义 U/W 向量单位）。字段名本身（`ransac_inlier_threshold_arcsec`）即量纲声明，属"实现自证"。
- 现状（原文，不转述）：`ipv_select.cpp:1125` `output.U[i].x = (det_x[idx] - cx);              // 像素, 不乘 s0` ⇒ 运行时 U 为像素；`ipv_select.cpp:1216-1217` `double fov_half_w = img_w / 2.0 * s0;  // 角秒` ⇒ W 为角秒；`ipv_ransac.cpp:440-444` 残差 `x_pred = tf.s*(cos_t*U.x - sin_t*U.y) + tf.tx` 与 `W[w].x` 相减 ⇒ 残差落在 **W 空间＝角秒**；而同一函数日志 `ipv_ransac.cpp:331 "tau=%.2f px"`、`:341 "默认 3 px"`、`:482 "RMS=%.4f px"`、`:486 "good_rms_threshold 现在是像素单位"`。
- 差在哪：同一批头文件里 U 的单位有两种互斥写法（:38/:127/:solver.h:39 = 像素，:92 = 角秒），且角秒残差的阈值在消费面被标为像素——一个 τ 只能有一个量纲。
- 后果：任何按 :92 采信"角秒"的调用/复核会把阈值与距离差一个 `s0` 因子（本仓真实尺度 0.9586″–6.3076″/px，见 `docs/algorithms/GATES_AND_TOLERANCES.md:89`、`docs/algorithms/PHOTOMETRIC_FIT.md:232`）；对外若把 τ=3.0 当作"3 像素"报告，则在 0.9586″/px 帧上实际是 3.13″（≈3.27 px），在 6.3″/px 帧上实际 3.0″＝0.48 px——同一字面量在不同帧上的**像素**含义相差 6.6 倍，判据不可跨帧比较。**与邻批同型**（邻批"畸变向量单位两说"），本批独立取证位点即上行五条，不引用邻批结论。
- 定级：S2 —— 文档/注释面与实现互斥为 CONFIRMED，但**缺本仓内可触发产物错值的路径**：唯一读取 τ/good_rms 的 `prosac_verify` 被本仓自己的 reachability 清单判为死符号（`ipv_dead_params_manifest.json:510-512`），生产精化阈在 `ipv_solver.cpp` 别处（三角形容差 5.0″，已由 `PLATESOLVE.md §9` 登记为 UNJUSTIFIED）。
- 怎么算修好：① `ipv_types.h:92` 的"角秒坐标"改为"像素坐标（原点图像中心，Y-up）"，与 :38/:127/:128、`ipv_solver.h:39,82` 及实现对齐；② `ipv_types.h:201/240/241` 与 `ipv_ransac.cpp:331/341/482/486` 的单位标注统一到同一口径（残差在 W 空间＝角秒），改一处必须同批改另一处，否则判据复核再次两说；③ `HexDescriptor::distances[5]`（:159）的单位随其输入向量口径同批核定；④ 补一条 grep 型机器锁：`StarPoint` 单位注释与 `output.U[...].x =` 实现的注释关键词不得互斥。
- 置信：CONFIRMED（单位链逐跳按原文核对；未实跑，实跑非必需）

### CR-42-2 · 尺度合法窗默认值两处不一致（0.90/1.10 vs 0.95/1.05），且"符合项目约束"在项目文档里零命中
- 轴：C
- 位点：`ipv_types.h:242` `double s_min = 0.90;                    // 尺度下限 (±10%, 符合项目约束)`；`ipv_types.h:243` `double s_max = 1.10;                    // 尺度上限 (±10%)`
- 上位依据：条文面零提及——检索式 `git grep -n "s_min\|±10%\|尺度上限\|尺度窗" docs`（命中仅 `docs/architecture/ISA_VARIANTS.md:38` 的 SIMD 带宽 ±10%，与尺度窗无关）。同仓消费面另说一套：`ipv_ransac.cpp:18` `// - 尺度约束: s ∈ [s_min, s_max] (默认 [0.95, 1.05])`。
- 现状：结构体默认 0.90/1.10（注释称 ±10%，算述自洽：0.9=1−0.1，1.1=1+0.1 ✓）；唯一按该窗判定的实现 `ipv_ransac.cpp:399,522,633 if (tf.s < params.s_min || tf.s > params.s_max) continue;` 与 `:1465 bool s_in_range = (sim_tf.s >= params.s_min && sim_tf.s <= params.s_max);`，其文件头自述默认却是 [0.95,1.05]。
- 差在哪：同一参数的"默认值"在两个文件里取两个不同的数，注释里的"项目约束"没有项目文档支撑。
- 后果：宽窗（±10%）相对窄窗（±5%）会放行偏离板尺度两倍的解——判据方向明确：`continue` 丢一项只会让候选变少⇒偏红，因此窗被**放宽**时是**偏绿**（更多 s 值进入内点评分），即"窗口取 1.10 还是 1.05"直接决定误配能否活到交付面。
- 定级：S3 —— `prosac_verify` 与本判定式在死符号内（`ipv_dead_params_manifest.json:510-512`），当前无生产触发路径；但注释作为对外口径错误。
- 怎么算修好：定一个窗（并给"为什么是这个窗"的锚：板尺度先验不确定度来自 `pixel_size_um`/`focal_length_mm` 的公差），同批改 `ipv_types.h:242-243`、`ipv_ransac.cpp:18`、`ipv_api.h:89-90` 镜像注释三处；单一事实源，禁止三处各写一遍。
- 置信：CONFIRMED（两处数值逐字比对）

### CR-42-3 · `[P27-DEAD-PARAMS]` 的"生产路径无任何配置→IpvParams 注入路径"不成立：三个字段实际由 normalize 配置注入
- 轴：C（登记面↔实现）＋ D
- 位点：`ipv_types.h:220-222` `【全局】生产路径无任何 "配置/CLI -> IpvParams" 注入路径 (p1_op_wcs 仅 ipv_get_default_params + 清零 log_dir; wcs 配置对象无 IPV 参数键) => 本结构体 24 字段全部不可由用户配置影响, 值恒为编译期默认值。`
- 上位依据：`docs/plugins/algorithms_phase1/03_star_detection.md:52-56` 声明该段是合法配置段、合同正本 = `eng/contracts/schemas/phase_config_normalize.schema.json`；同文档 :68-:70 把 `limiting_mag_safety` / `query_radius_factor` / `limiting_mag_max_iter` 三个键的默认值登记为 `IPVSolverParams` 对应字段。
- 现状（原文）：`lib/infrastructure/scheduler/src/module_adapters.cpp:3417` `out->ipv_params.m_lim_safety = p1_num(sd, "limiting_mag_safety", out->ipv_params.m_lim_safety);`、`:3418-3419` `out->ipv_params.gaia_query_radius_factor = p1_num(sd, "query_radius_factor", …)`、`:3420` `out->ipv_params.m_lim_max_iter = p1_int(sd, "limiting_mag_max_iter", …)`；合同面 `eng/contracts/schemas/phase_config_normalize.schema.json:368,443`、`eng/packaging/config/config_registry.json:1983-1989`、CLI 认键面 `lib/infrastructure/cli/session_commands.h:205` 均已声明该键。另：字段实计 25（含 `log_dir`；`ipv_dead_params_manifest.json:426-452` 亦列 25 项），注释写"24 字段"。
- 差在哪：注释把"某个入口（p1_op_wcs）无注入"写成了"生产路径无任何注入"，并把三个**可被用户配置改变**的字段一并归入"值恒为编译期默认值"。
- 后果：① 依赖该主张的判断失效——例如"该值不可配 ⇒ 无须逐帧复核"，而实际 `m_lim_safety` 一改即改 `N_target = n_target × safety`（`ipv_select.cpp:432`），改 Gaia 查询星数与选星池，进而可改解算结果；② 机器锁 `ipv_dead_params_lock`（`eng/ci/checks.json` 已登记）的清单前提被写错，锁"绿"不等于结论真；③ 字段计数错会误导 ABI 镜像核对。
- 定级：S2 —— 触发路径可逐跳写（`session_commands.h:205` → `module_adapters.cpp:3417-3420` → `ipv_select.cpp:425/432`），但错的是**登记面陈述**而非产物字节，故不判 S1。
- 怎么算修好：把 :220-222 改写为"唯一无注入的入口 = p1_op_wcs；star_detection 段三键实际注入 `m_lim_safety`/`gaia_query_radius_factor`/`m_lim_max_iter`（锚 module_adapters.cpp:3417-3420）"，字段计数改 25；同批在 `ipv_dead_params_manifest.json` 的对应条目把这三键从"配置不可达"移出，并让锁脚本对 `ipv_params.<field> =` 的注入点做存在性断言（正例：三键命中；负例：删掉一行必报警）。
- 置信：CONFIRMED

### CR-42-4 · Gaia 每文件上限 200000 在 IPv 侧是手抄副本，注释自称"只读"，无任何锁
- 轴：C（同一事实两仓两处写）＋ R（判据静默失效方向）
- 位点：`ipv_types.h:273-276` `// Gaia 客户端"每文件返回上限" (lib/infrastructure/gaia_xpsd_client/src/gaia_client.c MAX_STARS_RESULT, / // 该常量由 P1 持有, 本模块只读该数量用于饱和检测…)` ⇒ `double m_lim_gaia_cap_per_file = 200000.0;`
- 上位依据：`docs/algorithms/GAIA_QUERY.md:156` `| 结果上限 | 单文件 collector 200000 星静默截断（MAX_STARS_RESULT） | 源码 27, 1146, 1215 |`；正本定义 `lib/infrastructure/gaia_xpsd_client/src/gaia_client.c:33 #define MAX_STARS_RESULT 200000`。
- 现状：IPv 侧不调用任何接口读取该常量，而是把 200000 复制进结构体默认值；判据实现 `ipv_select.cpp:498-499` `const bool capped = (n_ret > 0 && cap_unit > 0.0 && static_cast<double>(n_ret) >= cap_unit);`。检索式（判"无锁"）：`git grep -n "MAX_STARS_RESULT" -- eng docs artifacts lib` 命中仅 gaia_client.c、GAIA_QUERY.md 与 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv`，无任一测试/合同比对 `m_lim_gaia_cap_per_file == MAX_STARS_RESULT`。
- 差在哪："只读"与实际"再声明一份"不符，且两份数字之间无机器一致性。
- 后果：判据方向已定——若真上限被下调（如 collector 改为 100000），`n_ret >= 200000` 永不成立 ⇒ `capped` 漏判 ⇒ 按 `:275` 自述"停用 alpha 更新"的保护不发生，截断样本进入 alpha 差分（`ipv_select.cpp:507`），割线步长可被截断数据带偏，最终 `m_lim_final`/选星池错值进产品；若真上限被上调，则误判 capped ⇒ 偏保守（`converged=false`，判红）。即"漂移一个方向静默、另一方向判红"。
- 定级：S2（当前两值相等，无现役错值；缺"上游改动⇒下游静默"的现网触发路径）
- 怎么算修好：① 把该常量放单一事实源（生成头或 `ipv_abi_mirror.py` 同族工具）或由 Gaia 客户端接口在运行时返回；② 若维持默认值，补一条机器锁断言 `m_lim_gaia_cap_per_file == MAX_STARS_RESULT`（解析两份源码常量比对，配正例/负例与 `--self-test`）；③ 注释措辞从"只读"改为"复制声明，须与 gaia_client.c:33 同批修改"。
- 置信：CONFIRMED

### CR-42-5 · `IPVSolverParams` 全部 m_lim_* 阈值的唯一依据是 gitignore 的 `run/` 报告，权威容差登记册一条未收
- 轴：S
- 位点：`ipv_types.h:255` `// 依据: run/perf-fix/P4-magiter/REPORT.md (实测 alpha 中位 0.2885;`；`ipv_types.h:257` `// 偏差登记: 尚未在 docs/algorithms 登记 (proposed patch)。`；数值位点 :260,261,262,265,268,269,270,271,272,276,277,278
- 上位依据：`AGENTS.md §7` 目录落位「`run/` 临时产物/日志（gitignore，不入库）」；`.gitignore:17-18` `run/*` / `!run/.gitkeep`（跟踪面 `git ls-files -- run` 仅 1 项 `run/.gitkeep`）；`docs/algorithms/PLATESOLVE.md §9 容差来源`（:183-205）逐条登记收敛 0.01″、三角形容差 5.0″、尺度容差 0.002、Huber 1.345、IRLS 15/1e-6，并对无推导者明标"**阈值来源 UNJUSTIFIED**，登记为待标定项"。
- 现状：§9 内 `git grep -n "m_lim\|alpha 先验\|density_tolerance\|vote_threshold\|n_pivot\|ransac_max_iter" -- docs` 零命中；本结构体的 12 个 m_lim_* 值与 5 个匹配类阈值的"依据"只写在代码注释里，指向不入库的 `run/perf-fix/P4-magiter/REPORT.md` 与 `run/perf-fix/P27-dead-params/REPORT.md`（:229）。
- 差在哪：§2.5 处置档 3 明定标定件"必须落在跟踪面，指 `run/` 即判不合格"；权威登记册把同族阈值标 UNJUSTIFIED 的纪律没有覆盖到本结构体，代码注释反而以"实测/符合项目约束"的完成时口吻出现。
- 后果：这些数值直接决定 Gaia 查询次数与极限星等（`ipv_select.cpp:432` N_target、`:462` 迭代圈、`:498` 触顶），即"选多少星、以哪张星表子集解算"——复核者无法在跟踪面复算其标定，对外若主张"参数经实测标定"拿不出证据；且 `test_mag_iter.cpp:86` 用同一组 0.243/0.2885/0.456 作输入只验证迭代自洽，不构成对该先验的独立验证（自指）。
- 定级：S2（不产生当前错值，但"实测标定"这一对外主张现在不可复核）
- 怎么算修好：把 P4-magiter 的标定记录（帧清单、N(m) 曲线拟合、safety 扫描、余量 +0.36..+1.07 mag 的逐帧表）落到跟踪面（`docs/algorithms/` 或 `artifacts/evidence/`），并在 `docs/algorithms/PLATESOLVE.md §9` 逐条登记：有推导的给式，无推导的照 §9 现例明标 UNJUSTIFIED + 待标定；同批删除 `ipv_types.h:257` 的"尚未登记"自称（登记后即过期），并把 :229 的 run/ 指针换成跟踪面锚。
- 置信：CONFIRMED

### CR-42-6 · 图像侧目标星数在本仓有 20 / 50 / 60 三个"默认值"并存
- 轴：C
- 位点：`ipv_types.h:246` `// 默认 20 颗`；`ipv_types.h:249` `int    img_n_target = 20;               // 图像侧目标星数`；`ipv_types.h:92` `// 图像侧星点 (角秒坐标, 50 颗)`
- 上位依据：`lib/algorithms/platesolve/IPV_PIPELINE.md:86` `饱和星数 ≥ img_n_target (默认 50)`、`:297` `| img_n_target | 50 | 图像侧目标星数 |`；`lib/algorithms/platesolve/cpp/ipv/REPORT.md:1037` `| P1-3 | 默认星数 60 vs Siril 20 | img_n_target 默认值 60→20 | ipv_types.h |`、`:1137` `img_n_target 50→20`。
- 现状：代码默认 20；生产路径在调用前硬覆盖——`ipv_solver.cpp:445` `p_adapt.img_n_target = n_target;`（n_target 由 `solve_post_select` 的字面量 60 给出，见 `ipv_dead_params_manifest.json:516-521 "const int n_target = 60;"`）、`:1516/:1592/:1663 p_adapt.img_n_target = 60;`；而同一文件的 `StarSelection::U` 注释仍写 50 颗，`IPV_PIPELINE.md` 两处也写 50。
- 差在哪：同一参数四个说法（20 默认 / 50 文档 / 50 注释 / 60 生产），且"50"在跟踪文档里仍是现行描述。
- 后果：按 `IPV_PIPELINE.md` 复核选星规模者会得到与实际差 20% 的池子；`REPORT.md:1137` 把"50→20"记为"降低匹配鲁棒性"的整改项，而实际生产是 60 ⇒ 该整改结论与现网不符。
- 定级：S3（注释/文档面，无判据方向）
- 怎么算修好：`ipv_types.h:92` 与 `IPV_PIPELINE.md:86,297` 统一为"代码默认 20，生产入口硬覆盖 60（锚 ipv_solver.cpp:445/1516/1592/1663）"，并把 `img_n_target` 的"三处硬覆盖字面量"收敛为单一命名常量（同批改 `ipv_dead_params_manifest.json:556-570` 的 pattern 锁）。
- 置信：CONFIRMED

### CR-42-7 · 跨 DLL 的输出结构 `IpvWcsResult` 未带版本＋尺寸自描述，"布局冻结"只由注释与本仓 ctypes 镜像锁兜住
- 轴：D（违宪章 §8.6 自家约定）＋ C
- 位点：`ipv_types.h:71-72` `//   追加尾部字段: A/B/AP/BP/order/ap_order 偏移与 sizeof 兼容面不变 / //   (fit_sip 唯一调用点 build_wcs 显式清零全部逆向字段, 零未初始化泄漏)。`（同族 :65 `C ABI (IpvWcsResult.sip_a[36])`、:66 `与消费方契约 (wcs_transform evalSip i*6+j) 兼容层, 布局冻结。`）
- 上位依据：`ipv_api.h:65-66` 引宪章 §8.6「跨 DLL 边界使用版本化 C ABI …… 结构体带 struct_size/abi_version」，并把该约定落成 `IpvParams` 首两字段（`ipv_api.h:79-81`）；`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:90`（V2-N-01，P0）记录的事故正是"ctypes 镜像 352 字节 vs C 侧 424 字节 ⇒ 72 字节越界写 + 字段全错位"，整改建议②为"给结构体补 struct_size（或 abi_version）首字段，令其结构上不可错配"。
- 现状：`ipv_api.h:39-61` 的 `IpvWcsResult` 首字段是 `double cd[4]`，无 size/version；C 侧对调用方缓冲的写入是无尺寸校验的整尺寸 memset（`ipv_entry.cpp:139 std::memset(dst, 0, sizeof(IpvWcsResult));`，另 :502/:539/:689/:735/:782 同型）。现行防护：`ipv_abi_layout_lock`（`test/ipv_abi_layout_lock.py:3` "IpvParams / IpvWcsResult 布局一致性机器锁"，探针 `test/ipv_abi_layout_probe.cpp:108-125` 逐字段 offsetof）比对**本仓** C 头与**本仓** `tools/ipv_abi_mirror.py`。
- 差在哪：§8.6 的约定按"跨 DLL 结构"表述，输出侧结构未落实；且现有锁只保证仓内两份定义同步，运行期对"外部消费者带旧头文件"这一 V2-N-01 的实际失效形态零防御（入口无从校验）。
- 后果：一旦 SIP 布局再扩展（如 :68 的 APx/BPx 之类"追加尾部字段"），带旧头的调用方会被按新 `sizeof` 覆写越界，字段整体错位——与 V2-N-01 同型事故在输出侧可原样复发。
- 定级：S2 —— 结构上暴露，但本仓内当前两份定义已被锁钉住一致，写不出本仓内可触发的产物错值路径（缺运行时触发路径）。
- 怎么算修好：给 `IpvWcsResult` 补 `uint32_t struct_size/abi_version` 首两字段并在**每个**写 result 的公共入口校验（照 `ipv_entry.cpp:206-217 validate_params_abi()` 的 fail-closed 形态）；同批改 `ipv_abi_mirror.py`、`ipv_abi_layout_probe.cpp` 字段清单、`docs/contracts/PUBLIC_API.md`，并加一条负例（旧镜像必报警）。
- 置信：CONFIRMED

### CR-42-8 · `FlipModeResult` 与"统一求解，无 flip_mode 区分"的自身声明并存，类型无任何使用者
- 轴：D
- 位点：`ipv_types.h:133` `// 移除 best_mode (统一求解, 无 flip_mode 区分)`；`ipv_types.h:210-217` `// 单个 flip_mode 的结果 / struct FlipModeResult { int mode; // 0/1/2/3 … }`
- 上位依据：同文件 :133 即自述；设计面 `docs/algorithms/PLATESOLVE.md` 无 flip_mode 条款（检索式 `git grep -n "flip_mode\|FlipModeResult" docs` 零命中）。
- 现状：`git grep -n "FlipModeResult" -- lib eng` 全仓仅 3 处：本定义（ipv_types.h:211）、`IPV_PIPELINE.md:231`（文档回显）、`ipv_entry.cpp:227` 一条注释（"…等) 可能覆盖 SEH 记录"）——无任何构造、传参或返回。
- 差在哪：本批"派单重点④/非缺陷清单"允许登记"已由合同显式声明退役的东西"为不报，但此处**没有任何登记**：:133 声明移除 flip_mode，:210 却继续以现行类型面目保留 flip_mode 结果结构。
- 后果：读者会以为四模式 flip 判别仍在流程内（与 :133 相反），并据此评估镜像/取向歧义风险；若被复用则引入未参与拟合的字段。
- 定级：S3（无生产路径，仅口径误导）
- 怎么算修好：删除该结构并在 `docs/contracts/`（或退役清单）登记"flip_mode 面已退役"，与 `:133` 合并成一条；`IPV_PIPELINE.md:231` 同批改。
- 置信：CONFIRMED

---

## 2. `lib/algorithms/platesolve/cpp/ipv/include/ipv_triangle.h`（97 行，全读）

### 2-T §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| ipv_triangle.h:7 | Valdes et al. PASP 107, 1119 (1995) | — | 算法出处标识 | 文献值 | `docs/algorithms/PLATESOLVE.md:369` `三角匹配/星表求解：…Valdes et al. 1995, PASP 107, 1119（DOI 10.1086/133670）`；文献登记册 `docs/references/SCIENTIFIC_REFERENCES.md` 内零条目（检索式 `git grep -n "Valdes" docs`）⇒ 出处有、核验状态无 | — | PARTIAL（未取原文核对算法逐式；见收尾节） |
| ipv_triangle.h:8 | 被"实现"的符号名 set_triangle / stars_to_triangles / make_vote_matrix / top_vote_getters | — | 把本仓函数名挂到文献名下 | 待确认 | 四个名字在仓内有定义：`ipv_triangle.cpp:582/595/605` 与 `stars_to_triangles`；文献里不存在这四个符号（本批无法核原文，检索式 `git grep -n "set_triangle" -- lib docs`）⇒ 见 CR-42-13 | — | PARTIAL |
| ipv_triangle.h:11 | `C(N,3)` | 3 | 三角形组合数 | 公式导出 | 组合定义式；本批复算 `C(60,3)=60·59·58/6=34220` ✓ 与 `ipv_triangle.cpp:491` 注、`ipv_types.h:247` 一致 | — | CONFIRMED |
| ipv_triangle.h:16 | 「尺度比为 1, 无需显式约束」 | — | 是否需要 scale 约束的口径 | 待确认 | 与同文件 :73-77 与实现 `ipv_triangle.cpp:566-574` 直接互斥 ⇒ 见 CR-42-9 | — | CONFIRMED |
| ipv_triangle.h:26,28 | `AT_MATCH_MINVOTES = 2` | 2 | 配对进入候选的最小票数 | 待确认 | 条文面零登记：`git grep -n "MINVOTES\|最小票\|单票" docs` 零命中；实现真按此过滤 `ipv_triangle.cpp:432 if (v >= AT_MATCH_MINVOTES)`；本仓另有一处帧级判据字面量 3（`ipv_dead_params_manifest.json:540-545` pattern `max_vote < 3`）与 `ipv_solver.cpp:439 vote_threshold = params.vote_threshold`（默认 2）并存 ⇒ 同域三档 2/2/3 | 取高 ⇒ 候选少（漏配 ⇒ 偏红）；取低 ⇒ 噪声对进初始 TRANS（误配 ⇒ 偏绿） | CONFIRMED |
| ipv_triangle.h:33,42,43 | `ba = b/a`、`ca = c/a` | — | 描述符定义 | 公式导出 | 比值定义式（尺度不变），实现按 a>=b>=c 排序后取比（`ipv_triangle.cpp:63-166`） | — | CONFIRMED |
| ipv_triangle.h:34-36 | 索引 "0..N-1" | N | 索引值域说明 | 结构性不适用 | 数组索引域，非科学量 | — | CONFIRMED |
| ipv_triangle.h:44 | `a_length`「最长边长度 (角秒)」 | — | 边长单位声明 | 待确认 | A 侧由图像星构建 ⇒ 实为像素（`ipv_triangle.cpp:342 // ratio = a_length_A / a_length_B (像素/角秒 = 1/s0)`）⇒ 见 CR-42-9 | — | CONFIRMED |
| ipv_triangle.h:61 | `fail_reason[160]` | 160 | 失败原因缓冲 | 结构性不适用 | 缓冲区尺寸；未见被逐字拷进对外 `error_msg[256]` 的路径（`ipv_solver.cpp:519` 写的是自述字面量），故 160<256 无截断风险 | — | CONFIRMED |
| ipv_triangle.h:57-59 | 「budget_exhausted=true 时 success=false 且 fail_reason 非空」 | — | 自描述保证条款 | 公式导出 | 实现逐条满足：`ipv_triangle.cpp:550-562` 该路径同时置 `budget_exhausted`、`success=false`、`snprintf(fail_reason…)`；**逆命题不成立**（其余失败路径 fail_reason 为空）⇒ 见 CR-42-12 | — | CONFIRMED |
| ipv_triangle.h:70,71,86,87 | `n_stars_A/B` 默认 60 | 60 | 两侧参与星数 | 待确认 | 生产确实按 60 传入（`ipv_solver.cpp:458-459`、`:1210 const int n_target = 60;`）；60 的取值锚只有实现内注释 `ipv_solver.cpp:432-434`（"之前 用 20 颗起跑…投票矩阵不稳定"），docs 零登记 ⇒ 与 `ipv_types.h:249` 默认 20 并存（见 CR-42-6） | 星数↑ ⇒ 组合按 n⁶ 涨、投票区分度变化；保守方向未定义（两向各有失效模式） | CONFIRMED |
| ipv_triangle.h:72,88 | `tolerance` 默认 0.002（「原 0.001 过严」） | 0.002/0.001 | ba/ca 空间欧氏距离阈 | 待确认 | 权威容差册登记为 **UNJUSTIFIED**：`docs/algorithms/PLATESOLVE.md:192` `尺度容差 0.002（各向异性 0.2%）：阈值来源 UNJUSTIFIED，登记为待标定项`——但同册 `:189` 把另一条标为"三角形匹配容差 5.0″"（对象错位）⇒ 见 CR-42-11；"原 0.001 过严"的实测记录不在跟踪面 | 收紧 ⇒ 漏配（偏红）；放宽 ⇒ 误配（偏绿）；本值是**活判据**（生产调用点 `ipv_solver.cpp:459/865/1214` 显式传 0.002） | CONFIRMED |
| ipv_triangle.h:74,76 | `percent_scale_range=20%`、`[1/(s0*1.2), 1/(s0*0.8)]` | 1.2 / 0.8 | 板尺度先验容许偏差窗 | 待确认 | 零锚（`git grep -n "percent_scale_range" docs` 零命中）；与本文件之外同族窗口互斥：`ipv_types.h:242-243` ±10%、`ipv_ransac.cpp:18` ±5% ⇒ 见 CR-42-10 | 窗宽 ⇒ 放行更多跨尺度三角形（偏绿）；窗窄 ⇒ 病态帧漏解（偏红）。20% 是三处里**最不保守**的一档，且落在唯一活路径上 | CONFIRMED |
| ipv_triangle.h:77 | 「s0<=0 时不做 scale 约束」 | 0.0 | 哨兵回退 | 结构性不适用 | 哨兵值语义（`ipv_triangle.cpp:570 if (s0 > 0.0)`），非科学量；但它使整条尺度判别可被静默关闭 ⇒ 后果记 CR-42-10 | — | CONFIRMED |
| ipv_triangle.h:79-80 | 「直接用 60 颗星, 不做自适应扩充」「若星数 < 60, 内部截断」 | 60 | 扩档能力声明 | 公式导出 | 与实现一致：`ipv_triangle.cpp:534-535` 截断、`ipv_solver.cpp:442 for (int n_target : {60})` 单档；与 `ipv_types.h:248`「20→40→60 自适应扩充」矛盾 ⇒ 见 CR-42-14 | — | CONFIRMED |
| ipv_triangle.h:497(impl) | `IPV_TRIANGLE_MATCH_MAX_VOTE_OPS`（本文件仅声明 budget 语义） | 2e9 | 投票枚举工作量上界 | 结构性不适用 | 算力上界（`ipv_triangle.cpp:490-498` 自述「n⁶ 增长…病态放大输入 n>=66 拦截」），非科学量；本批复算 `C(60,3)²=1.171e9 < 2e9` ✓ ⇒ 生产不触发 | — | CONFIRMED |

（本文件数值项计 16 行。）

### 2-F findings

### CR-42-9 · 同一头文件对"尺度是否必须约束"两说，且 `a_length` 的单位标注对图像侧为错（本批独立取证，与邻批同型）
- 轴：C（注释↔实现）＋ S（判据量纲）
- 位点：`lib/algorithms/platesolve/cpp/ipv/include/ipv_triangle.h:15-16` `// 描述符基于边长比值 (ba=b/a, ca=c/a), 翻转/旋转/平移不变, 仅对尺度 / // 不变 (两套星表同坐标系时尺度比为 1, 无需显式约束)。`；`:44` `double a_length;    // 最长边长度 (角秒)`；`:73-77` `// s0 - 像素尺度 (arcsec/pixel), 用于 scale 约束 / // percent_scale_range=20% / // U 是像素, W 是角秒, 预期 ratio = U.a_length/W.a_length = 1/s0 / // 允许范围 [1/(s0*1.2), 1/(s0*0.8)] (±20%)`
- 上位依据：`ipv_types.h:38`（U=像素/W=角秒）与实现 `ipv_select.cpp:1125`（U 写像素）、`ipv_select.cpp:1216-1217`（W 写角秒）；docs 面对尺度约束窗口零条文（检索式 `git grep -n "percent_scale_range\|scale 约束" docs`）。
- 现状：:16 断言"尺度比为 1、无需显式约束"；:73-77 却规定约束且明说预期比值为 `1/s0`；实现 `ipv_triangle.cpp:570-573` `expected_ratio = 1.0 / s0; scale_min = expected_ratio / 1.2; scale_max = expected_ratio / 0.8;` 并在投票处 `:344-345 if (ratio < scale_min || ratio > scale_max)` 执行。两侧星点本就不在同一单位系（像素 vs 角秒），比值恒为 `1/s0`，只有 s0=1 时才等于 1。
- 差在哪：:16 的"无需显式约束"与"尺度比为 1"在本项目数据流下均为假（比值是 1/s0，本仓真实 s0 跨 0.9586–6.3076 ″/px）；:44 把两侧的 `a_length` 统一标为角秒，而 A 侧实为像素。
- 后果：① `a_length` 的诊断读数若按角秒解释，图像侧偏大 `1/s0` 倍（1.0″/px 帧上偏 1 倍，6.3″/px 帧上偏 6.3 倍）；② 按 :16 认定约束冗余而删除 scale 约束后，判据方向 = 放行更多跨尺度配对 ⇒ **偏绿**（错误三角形进初始匹配对，再由 iter_trans 承接）；③ 与 CR-42-1 同族：**与邻批"畸变向量单位两说"同型**，本批独立取证位点为 :15-16/:44/:73-77 三条，未引用邻批结论。
- 定级：S2 —— scale 约束与 `a_length` 都在活路径（`ipv_solver.cpp:458-459` 生产调用），但当前实现行为自洽（按 1/s0 算），错的只是注释面；写不出"注释错误⇒产物错值"的现役路径。
- 怎么算修好：:16 改为"描述符对尺度不变；但两侧向量单位不同（U 像素 / W 角秒），故必须显式施加 `1/s0 ±20%` 尺度约束"；:44 改为"最长边长度（A 侧像素 / B 侧角秒，比值 = 1/s0）"；与 CR-42-1 的单位口径整改同批改，避免只改一处留下新的两说。
- 置信：CONFIRMED（比值与单位链逐跳按原文核对）

### CR-42-10 · 板尺度先验窗三处取三值（±20% / ±10% / ±5%），无任何一侧有锚
- 轴：C＋S
- 位点：`ipv_triangle.h:74` `// percent_scale_range=20%`、`:76` `// 允许范围 [1/(s0*1.2), 1/(s0*0.8)] (±20%)`；对照 `ipv_types.h:242-243`（`s_min=0.90 / s_max=1.10`，注释「±10%, 符合项目约束」）与 `ipv_ransac.cpp:18`（`// - 尺度约束: s ∈ [s_min, s_max] (默认 [0.95, 1.05])`）
- 上位依据：条文面零提及——检索式 `git grep -n "s_min\|percent_scale\|尺度窗" docs`（唯一相近命中 `docs/algorithms/PLATESOLVE.md:192` 的"尺度容差 0.002"是描述符距离阈，不是尺度窗）。
- 现状：活路径只用 ±20%（`ipv_triangle.cpp:572-573`）；±10% 与 ±5% 都落在被本仓判死的 PROSAC 路径（`ipv_dead_params_manifest.json:510-512`）。
- 差在哪：同一个物理量（板尺度先验可信偏差）在三处写了三个互斥窗口，且注释都把各自取值称作"默认/约束"，读者无法判定哪条是现行判据。
- 后果：判据方向已定：窗宽单调增加被放行的跨尺度配对 ⇒ 偏绿。±20% 对应 `1.2/0.8 = 1.5` 的比值跨度，是三档中最宽的一档，并且正是**唯一生效**的那一档——最不保守的取值住在活路径里，更窄的两档住在无人走的代码里。若某帧 `s0` 先验（来自 `pixel_size_um`/`focal_length_mm`）误差落在 10%–20% 区间，判据不会拒绝，错误尺度三角形可进入初始对。
- 定级：S2 —— 触发路径逐跳可写（`ipv_solver.cpp:458-459` → `ipv_triangle.cpp:570-574` → `:344-345` → `make_vote_matrix` 投票 → `top_vote_getters` → `iter_trans`），但"错值"需 `s0` 先验本身偏差 >10% 才显现，本批未见此类帧的现役证据。
- 怎么算修好：定一条"板尺度先验不确定度"正本（由 `pixel_size_um`、`focal_length_mm` 的来源公差导出，写进 `docs/algorithms/PLATESOLVE.md §9`），三处一律从该式导出并加锚；若确为三个不同对象（描述符比值窗 vs 拟合尺度窗），则在文档里分别命名并说明取值为何不同，禁止共用"尺度约束"一词。
- 置信：CONFIRMED

### CR-42-11 · 权威容差册把 5.0″ 标为"三角形匹配容差"，真正的三角形匹配容差是 0.002（无量纲）
- 轴：C（登记面锚绑错对象）
- 位点：`ipv_triangle.h:72` `// tolerance - 描述符匹配容差 (ba, ca 空间欧氏距离阈值, 默认 0.002)`、`:88` `double tolerance = 0.002,  // 默认 0.002 (原 0.001 过严)`
- 上位依据：`docs/algorithms/PLATESOLVE.md:189` `三角形匹配容差 5.0″：ipv_solver.cpp:660 实参。**量纲 arcsec**；**阈值来源 UNJUSTIFIED**…登记为待标定项`；同册 `:192` `尺度容差 0.002（各向异性 0.2%）：阈值来源 UNJUSTIFIED`。
- 现状：`ipv_solver.cpp:660 U, W_final, rep_result.trans, 5.0);  // 5" tolerance` 与 `:534 U, W, tri_result.top_pairs, 5.0, it_order;` 是 **iter_trans / hi_order_rematch** 的容差实参；三角形匹配的容差是 `:459/865/1214` 显式传入的 `0.002`。
- 差在哪：容差册条目名与实参所属函数不匹配：被称作"三角形匹配容差"的那条其实是重投影/精化容差，而三角形匹配自己的判据（ba/ca 空间 0.002）以"尺度容差（各向异性 0.2%）"名义登记。
- 后果：按 §9 复核"三角形匹配"的人会拿 5.0″ 当它的阈并据此评估鲁棒性；两条判据量纲不同（角秒 vs 无量纲比值距离），误绑使 UNJUSTIFIED 登记落在错对象上——后续标定整改会改错数（改 5.0″ 动不到真正待标的 0.002）。
- 定级：S3（登记面锚绑错，不改产物字节）
- 怎么算修好：§9 两条改为「三角形描述符匹配容差 0.002（ba/ca 无量纲欧氏距离，实参 `ipv_solver.cpp:459/865/1214`）」与「迭代重投影/高阶重匹配容差 5.0″（实参 `ipv_solver.cpp:534/660/670`）」；"各向异性 0.2%" 若无推导即删（它把无量纲距离说成各向异性百分比）。
- 置信：CONFIRMED

### CR-42-12 · 退化返回把"未测"与"测了零票"压成同一组数值（零内点／样本不足／预算耗尽不可区分）
- 轴：R
- 位点：`ipv_triangle.h:50-62` `struct TriangleMatchResult { std::vector<MatchPair> top_pairs; … int max_vote; bool success; bool budget_exhausted = false; char fail_reason[160] = {0}; }`
- 上位依据：`AGENTS.md §5`「判据必须非退化：真值无效应时度量归零或判红」；条文面对"未测 vs 零值"的可区分性无专门条款（检索式 `git grep -n "未测\|哨兵" docs/science docs/algorithms`）。
- 现状：四条性质不同的失败都返回 `top_pairs 空 + n_triangles_A/B=0 + max_vote=0 + success=false`——星数不足（`ipv_triangle.cpp:528-531`）、限制后星数不足（`:536-539`）、三角形数为 0（`:588-591`）、votes 为空（`:598-601`）；只有预算耗尽额外置 `budget_exhausted/fail_reason`（`:550-562`）。另有 `:617 result.success = (max_v > 0);` ⇒ `max_vote=1` 时 success=true 而 `top_pairs` 已被 `AT_MATCH_MINVOTES=2` 清空，"成功"可与"零可用对"共存。
- 差在哪：结构体没有"未测"表示位（无 NaN 哨兵、无实际入算星数字段），把病态输入与真零票写成同一组合法数值；且 `success` 的定义（max_v>0）与"有可用候选对"不等价。
- 后果：诊断/复盘面读到 `max_vote=0, n_triangles=0` 无法区分"星表侧只 2 颗星"与"两点共线"，与 P14-N-10 已订正的 `m_lim_capped` 那类失效面（`ipv_types.h:110-114`）同形；`success=true + top_pairs 空` 若被新调用方按 success 消费即拿到空集继续算。当前生产调用方以 `max_vote >= vote_threshold` 再判（`ipv_solver.cpp:471`），故未致错值。
- 定级：S3（现役路径有第二重判据兜住；错在对外可读性）
- 怎么算修好：补 `int n_stars_used_A/B` 与 `triangles_built` 计数，或把 `success` 定义为 `max_vote >= 阈值 && !top_pairs.empty()` 并同步改所有调用点；`fail_reason` 在全部失败路径写入，并加一条机器锁"任一 success=false ⇒ fail_reason 非空"（配正例/负例）。
- 置信：CONFIRMED

### CR-42-13 · 头文件把本仓四个函数名写成 Valdes 1995 的算法构件，出处面亦无核验状态
- 轴：S（文献角色错绑）
- 位点：`ipv_triangle.h:7-8` `// 实现 Valdes et al. PASP 107, 1119 (1995) 的三角形匹配算法 / // (set_triangle / stars_to_triangles / make_vote_matrix / top_vote_getters)。`
- 上位依据：`docs/algorithms/PLATESOLVE.md:365-369`（"本节只补出处与参考实现"，`:369` 给 DOI 10.1086/133670）；`docs/references/SCIENTIFIC_REFERENCES.md` 对该文献零条目（检索式 `git grep -n "Valdes\|133670" docs/references`），即无"核验状态"标注（同册其它文献均带 `**核验状态**：…`，如 :78）。
- 现状：括号内四个符号是本仓私有名（`ipv_triangle.cpp:582 stars_to_triangles`、`:595 make_vote_matrix`、`:605 top_vote_getters`、`set_triangle` 见 `:63` 注释），括号读起来像在列举文献的算法步骤名。
- 差在哪：把实现侧命名写成文献侧结构，等于给出"逐符号对应文献"的假锚；同时该文献在登记册无核验状态条目，属"列而不用/未核"。
- 后果：按 :7-8 去文献定位这四个符号的复核必然落空（与 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:155` 登记的"锚系统性漂移⇒复核整体落空"同形态）；对外若主张"算法逐式对应 Valdes 1995"，跟踪面拿不出核验记录。
- 定级：S3
- 怎么算修好：:7-8 改为"参考 Valdes et al. 1995（DOI …）的比值描述符投票思想；下列符号为本仓实现名"；并在 `docs/references/SCIENTIFIC_REFERENCES.md` 补该条与核验状态（文章级/逐式级，按实取得）。
- 置信：PARTIAL（未取原文核对算法本体与实现是否一致；见收尾节"读不动"）

### CR-42-14 · `ipv_types.h:248` 承诺的"20→40→60 自适应扩档"在生产里不存在，日志仍宣称"扩充星数到下一档"
- 轴：D（被点名的能力未实现）＋ R（日志与行为相反）
- 位点：`lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h:248` `// 自适应扩充 20→40→60 仅在 max_vote < vote_threshold 时触发`
- 上位依据：`ipv_triangle.h:79` `// 直接用 60 颗星, 不做自适应扩充`、`ipv_triangle.cpp:633` 同文、`ipv_solver.cpp:431` `// 直接用 60 颗星做三角形匹配, 不做自适应扩充`。
- 现状：`ipv_solver.cpp:442 for (int n_target : {60}) {` —— 初始化列表只有一档；`:470 n_target_used = n_target;` 后 `:471 if (tri_result.max_vote >= vote_threshold)` 通过即 break，未过则 `:476 logger_.warnf("[V4.22] max_vote=%d < threshold=%d, 扩充星数到下一档", …)`，随后循环自然结束（`:851-882` 第二处同型）。`ipv_triangle.cpp:18` 文件头仍写 `// 5. 自适应星数: max_vote < 3 时扩充 20 -> 40 -> 60`。
- 差在哪：注释描述三档回退，实现是单档；失败分支日志主动声称"扩充到下一档"，与随后不再重试相反。
- 后果：① 按 :248 评估鲁棒性/失败率的判断失效（60 星一次不成即整帧失败，无回退）；② 现场排障读日志会以为已试过三档，掩盖真实失效点；③ 与 CR-42-6（20/50/60 三值）同源。
- 定级：S2 —— 生产路径逐跳可写（`ipv_solver.cpp:442 → 458 → 471 → 476`），后果是"缺被承诺的能力 + 日志与行为不符"，非产物错值。
- 怎么算修好：要么把三档实现回来（`for (int n_target : {20, 40, 60})`，并写明 `vote_threshold` 与 `AT_MATCH_MINVOTES` 各自管什么），要么删除 `ipv_types.h:246-248`、`ipv_triangle.cpp:18` 的扩档叙述并把 `:476/882` 日志改为"60 星单档失败，不重试"；两处 solver 循环同批改，改后补一条锁：日志含"下一档"字样时循环档位向量长度必须 >1。
- 置信：CONFIRMED（单档初始化列表为逐字所见）

---

## 4. `lib/algorithms/platesolve/cpp/ipv/include/ipv_wcs.h`（86 行，全读）

### 4-T §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| ipv_wcs.h:12 | 「s 无量纲, θ 弧度, tx/ty 角秒」 | — | 相似变换三量单位口径 | 待确认 | 该口径仅在 U 与 W' **同为角秒**时成立；实现 `ipv_wcs.cpp:138 const double scale = s * s0 / 3600.0;` 正是把 s 当无量纲用（乘 s0 才得 度/像素），而生产向量 U 为像素 ⇒ 见 CR-42-15 | — | CONFIRMED（算术与前提逐字核对） |
| ipv_wcs.h:13 | `W' = s·R(θ)·U + t` | — | 前向模型定义式 | 公式导出 | 实现 `ipv_wcs.cpp:133-154` 按式取 s/θ/t 组合出 CD | — | CONFIRMED |
| ipv_wcs.h:14-18 | s0(角秒/像素)、img_*、ra0/dec0(度) | — | 单位口径声明 | 结构性不适用 | 单位定义面（与 `ipv_types.h:124`、`PLATESOLVE.md §9` 同口径） | — | CONFIRMED |
| ipv_wcs.h:19 | 「U - 图像侧星点 (角秒坐标…)」 | — | U 单位声明 | 待确认 | 与 `ipv_types.h:38`、`ipv_solver.h:39`、`ipv_triangle.h:66` 的"像素"两说，实现为像素（`ipv_select.cpp:1125`）⇒ 见 CR-42-1 与 CR-42-15 | — | CONFIRMED |
| ipv_wcs.h:20 | `W_flipped` 已应用 flip_mode 翻转（角秒） | — | W' 口径 | 待确认 | 生产 flip_mode 面已声明移除（`ipv_types.h:133`「统一求解, 无 flip_mode 区分」），本函数仍收 flip_mode 实参 ⇒ 见 CR-42-17 | — | CONFIRMED |
| ipv_wcs.h:22 | 「inliers - PROSAC 内点匹配对列表」 | — | 入参来源声明 | 待确认 | PROSAC 路径被本仓 reachability 清单判死（`ipv_dead_params_manifest.json:510-512`），故本函数的入参语义无生产来源 ⇒ CR-42-15 的后果面 | — | CONFIRMED |
| ipv_wcs.h:23-24 | flip_mode 0/1/2/3 与「决定 CD 矩阵符号方向」 | 4 | 镜像枚举基数＋符号式 | 结构性不适用（基数）＋公式导出（符号） | 实现 `ipv_wcs.cpp:142-154` switch 四态给 sign_x/sign_y，CD 按 `±scale·cos/sin` 组装 | — | CONFIRMED |
| ipv_wcs.h:27 | 「旧 build_wcs 仅为向后兼容保留, trans_order 固定为 1」 | 1 | 遗留面声明 | 待确认 | 本批未逐字核 `trans_order` 赋值行；生产零调用者（`git grep -n "build_wcs(" -- lib` 命中仅 decl/def 与 `test_synthetic.cpp:277`）⇒ CR-42-17 | — | PARTIAL |
| ipv_wcs.h:43,57,60,64-67,70 | 「0-based FITS 像素」/ `x = u + CRPIX1` / 「本函数不做 FITS 1-based 换算」/ `+1` 唯一责任方 = `p3_wcs::fits_pixel_1based` | 1 | 像素原点口径与桥接责任 | 待确认 | 同批 `ipv_types.h:139`「参考像素(1-based)」与 `ipv_wcs.cpp:161-162`「统一 CRPIX 为 width/2.0 + 0.5 (1-based FITS, frozen value)」＋本行 x=u+crpix ⇒ x 只能是 **1-based**，与 :70 的 "0-based" 互斥 ⇒ 见 CR-42-16；第三方比对已实证：`eng/tests/unit/p1wcs/p1wcs_astropy_cross.py:296` 必须 `crpix' = crpix + 1` 才能与 astropy 对上（:288-302 记为 finding WCS-003-F1，`artifacts/evidence/audit-2026-01/OWNER_DECISIONS.md:23` A-14 仍待裁） | 方向确定：两口径之差为恒定 1 px（不是随机误差），任一侧被误用即整幅 WCS 平移 1 px（本仓真实尺度下 0.96″–6.3″） | CONFIRMED（两标注互斥为逐字事实；"哪一侧为正解"需实跑/交付面比对，见收尾节） |
| ipv_wcs.h:45 | 「F2 冻结门 <1e-4 px」 | 1e-4 | 全链 roundtrip 冻结门 | 公式导出 | `docs/algorithms/GATES_AND_TOLERANCES.md:65` G-P1-WCS-RT = 1e-4 px，注「与 ALG-WCS-001 §11.4 F2 同值（SCI/ALG 冻结值，不放宽）」，并自述该门对迭代路径**判据力弱**（实测最坏 2.91e-9 px，余量 3.4e4×）⇒ 本行引用合规 | — | CONFIRMED |
| ipv_wcs.h:47-48 | `F(u) = u + A(u) = CD⁻¹·(ξ,η)`；`(ξ,η)` = TAN 正投影 | — | 反演方程定义 | 公式导出 | 实现 `ipv_wcs.cpp:875-886`（CD⁻¹·ξ）、`:934-936 F(u)−UV` 逐式一致 | — | CONFIRMED |
| ipv_wcs.h:49 | 初值 = `CD⁻¹·(ξ,η)` + APx(apx_order>0) / 一步 AP | — | 初值构造式 | 公式导出 | 实现 `ipv_wcs.cpp:889-918` 两级初值（APx 用 `idx=i*10+j`，AP 用 `i*6+j`）与声明一致 | — | CONFIRMED |
| ipv_wcs.h:50 | 收敛判据 `\|F(u)-UV\|∞ < tol_px` | ∞-范数 | 收敛条件 | 公式导出 | 实现 `ipv_wcs.cpp:938-939 f_now = std::max(std::fabs(fx), std::fabs(fy)); if (f_now < tol_px)` ⇒ 与"∞ 范数"一致 | — | CONFIRMED |
| ipv_wcs.h:52 | reject_code=1「投影背面/输入非有限（cosc≈0 或 NaN/Inf）」 | ≈0 | 背面判定的量化 | 待确认 | 实现只做有限性判定：`ipv_wcs.cpp:822 return std::isfinite(*xi) && std::isfinite(*eta);`（另 :799-800 对 ra/dec/crval 查 isfinite），**无 cosc 阈值** ⇒ 背面但结果仍有限的点不会落 1 号，会一路走到 2 号 ⇒ 并入 CR-42-18 | 保守 = 显式 cosc 下限（早拒、少算） | CONFIRMED |
| ipv_wcs.h:53 | reject_code=2「发散（超 max_iter 未收敛或步出合理像素域）」 | — | 发散类拒绝 | 公式导出 | 实现护栏 `ipv_wcs.cpp:929 const double guard = 1e6;`（像素域）、`:989-994` 非有限或步长超 guard ⇒ reject 2；超 max_iter 落 2 | `guard=1e6` 是像素域绝对值：帧尺寸 1e6 px 级才可误触 ⇒ 结构安全边际 | CONFIRMED |
| ipv_wcs.h:54 | reject_code=3「牛顿矩阵奇异 (\|det J\|<1e-15, 畸变场奇点域)」 | 1e-15 | 奇点阈 | 待确认 | 实现两处使用同一码：`:976 if (!std::isfinite(jdet) || std::fabs(jdet) < 1e-15) out.reject_code = 3;`（J 奇异，与声明一致）**与** `:877 if (!std::isfinite(det) || std::fabs(det) < 1e-15) out.reject_code = 3;  // CD 奇异 (构造病态)`（CD 奇异，声明未列）⇒ 见 CR-42-18；`1e-15` 本身零锚（`git grep -n "1e-15" docs` 无对应容差条目） | J 无量纲 ⇒ 绝对阈口径自洽；但 CD 的 det 量纲是 (deg/px)²≈1e-8 量级，绝对阈 1e-15 与之相差 7 个量级 ⇒ 实际几乎不触发（偏绿） | CONFIRMED |
| ipv_wcs.h:61 | 「converged=false 时 x,y 置 NaN」 | NaN | 未测表示 | 公式导出 | 实现 `ipv_wcs.cpp:854-859` 入口即 `out.x/out.y = quiet_NaN()` 且 `reject_code=0` 先置；四条早退路径均保留 NaN ⇒ 符合"不用合法数值冒充未测"（本批唯一正面样例） | — | CONFIRMED |
| ipv_wcs.h:72 | `iterations`「含拒绝前的次数」 | — | 计数语义 | 公式导出 | 实现 `:937 iters = k;` 在收敛判据之前赋值，早退路径带 `out.iterations = iters` ⇒ 与声明一致 | — | CONFIRMED |
| ipv_wcs.h:80 | `tol_px = 1e-9`（默认 τ） | 1e-9 | 迭代反演收敛容差 | 公式导出 | `docs/algorithms/GATES_AND_TOLERANCES.md:69` G-P1-WCS-RT-ITER：门 = `κ_iter·τ = 1e-8 px`，κ_iter=10 由**负对照实测**（τ↓1000× 仅误差↓4.23× ⇒ 存在 ~4e-10 px 结构地板）导出，且明写"τ 写进合同"；`docs/science/ASTROMETRY.md:147` 同口径 ⇒ 本值有跟踪面锚，**合规** | 收紧 τ ⇒ 更多迭代/更多 reject 2（偏红、不产生错值） | CONFIRMED |
| ipv_wcs.h:81 | `max_iter = 64` | 64 | 牛顿迭代上限 | 待确认 | 条文面零登记（检索式 `git grep -n "max_iter" docs` 无 64 的迭代上链条目；`GATES_AND_TOLERANCES.md:69` 只钉 τ 与 κ_iter）；测试面把 64 与 0 两个值各自写死（`eng/tests/unit/p1wcs/p1wcs_tests_apbp.cpp:100`、`:541`） | 上限↑ ⇒ 更多点被判收敛（偏绿）；上限↓ ⇒ reject 2 增多（偏红）；与阻尼回退叠乘：`:984 for (int d = 0; d < 20; ++d)` ⇒ 单点最坏 64×20 次残差评估，该 20 同样零登记 | CONFIRMED |

（本文件数值项计 19 行。）

### 4-F findings

### CR-42-15 · `build_wcs` 的算术要求"U 为角秒"，而生产向量 U 是像素：单位两说在本批文件里同时存在两套实现
- 轴：C（口径）＋ R（同一函数按另一口径即错）
- 位点：`ipv_wcs.h:12` `// transform - 相似变换 (s, θ, tx, ty)，s 无量纲, θ 弧度, tx/ty 角秒`、`:19` `// U - 图像侧星点 (角秒坐标, 原点图像中心, Y 轴向上)`、`:13` `// 注: 该变换是对 W_flipped (W') 求解的, 即 W' = s·R(θ)·U + t`
- 上位依据：`ipv_types.h:38`「U 向量: 像素 / W 向量: 角秒」；实现 `ipv_select.cpp:1125 output.U[i].x = (det_x[idx] - cx); // 像素, 不乘 s0`；`PLATESOLVE.md §9` 对本函数单位口径无条款（检索式 `git grep -n "build_wcs" docs/algorithms/PLATESOLVE.md` → 仅 :210 关联 API 一行）。
- 现状：`ipv_wcs.cpp:138` `const double scale = s * s0 / 3600.0;` ⇒ CD(度/像素) = s·s0/3600，只有当 s 无量纲（即 U 与 W' 同单位）时成立；而生产里 s 是 `U(像素)→W(角秒)` 拟合出来的，量纲为角秒/像素（`ipv_ransac.cpp:440-444`；`ipv_wcs.cpp:274` 注「TRANS: U(像素)->W(角秒), 线性项单位 = 角秒/像素」）。把生产的 U 直接喂进来，CD 会多带一个 s0 因子（本仓真实尺度 0.9586–6.3076 ″/px ⇒ 误差即 s0 倍）。
- 差在哪：同一批头文件里 U 有两种单位口径，且两种口径各自对应一套成立的算术——`build_wcs`（要角秒）与 `extract_wcs_sip`/`triangle_match`/`prosac_verify`（喂像素）。这不是注释笔误，是**两个口径都活着写在代码里**。
- 后果：`build_wcs` 目前无生产调用者（唯一调用 = `test_synthetic.cpp:277`，且该调用只 `std::cout` 打印 CD/CRVAL/CRPIX/RMS（:281-287），无一条断言 ⇒ 口径错也不会判红）；任何人按 `ipv_types.h:92` 或本文件 :19 把 `selection.U` 交给 `build_wcs`，即得板尺度错 s0 倍的 CD，而测试面零防护。
- 定级：S2 —— 错值如何产生与唯一消费者不校验均已写出，但缺现役生产调用点（§3 的 S1 门槛不满足）。
- 怎么算修好：① 先按 CR-42-1 定一次 U 口径（像素）并全批统一注释；② `build_wcs` 要么与生产一致（`scale = s / 3600.0`，s 视作角秒/像素），要么在 :12/:19 显式标注"仅限 U/W 同为角秒的旧口径，禁用于 selection.U"；③ 给 `test_synthetic.cpp` 的 WCS 输出补真断言（对照构造 WCS 的 CD/CRPIX），使口径错误可被判红。
- 置信：CONFIRMED

### CR-42-16 · 像素原点口径在同批文件里互斥（crpix"1-based"＋x=u+crpix ⇒ x 必为 1-based，本文件却标"0-based"），第三方比对被迫加 1 桥接
- 轴：C（同一件事两处不同说）＋ R（跨阶段恒定 1 px）
- 位点：`ipv_wcs.h:70` `double x;            // 0-based FITS 像素 x (Y-down, ipv 内部口径)`、`:57` `//   converged=true 时 x,y 有效 (x = u + CRPIX1, y = v + CRPIX2; …)`、`:64-66` `本函数属 ipv 内部 0-based 口径, **禁止**在此处或调用侧再施加一次 +1; FITS 1-based 桥接 (xp = x + 1) 的唯一责任方 = Phase3 导出边界`
- 上位依据：`docs/science/ASTROMETRY.md:119`（「`wcs_sky_to_pixel_iterative` 当前无生产消费方（仅测试面调用）」＋双重桥接 = 恒定 1px 系统偏移）；`lib/algorithms/projection/p3_wcs.cpp:41,49,52`（`像素坐标 xp = x + 1, 中间坐标 (xi,eta) = CD·(xp − CRPIX)`、`kFitsPixelOrigin = 1.0`、`fits_pixel_1based`）；`ipv_types.h:139`「参考像素(1-based)」、`ipv_wcs.cpp:161-162`「统一 CRPIX 为 width/2.0 + 0.5 (1-based FITS, frozen value)」、`module_adapters.cpp:3478`「`0.5*im.w()+0.5;   // FITS 1-based 中心`」。
- 现状：CRPIX 数值两阶段相同（P1 `W/2+0.5` = P3 `(W+1)/2`，本批复算 W=1024 同为 512.5 ✓）；但 P1 侧关系是 `u = x − crpix`（`ipv_wcs.cpp:945 out.x = u + wcs.crpix[0];`，生产消费方同口径 `module_adapters.cpp:481-482 const double u = x - crpix1;`），P3/标准侧是 `u = (x+1) − crpix` ⇒ 同一 crpix 下两口径的 x 相差恒定 1。第三方交叉已实证：`p1wcs_astropy_cross.py:296 crpix_bridge = [fx["crpix"][0] + 1.0, fx["crpix"][1] + 1.0]`，注释 :288-295 自述"两者相差常量 1px 原点平移…1px 语义分歧本身登记 finding 移交"，:297-302 记为 WCS-003-F1；`artifacts/evidence/audit-2026-01/OWNER_DECISIONS.md:23` A-14 仍列「注册表称已闭环/CONFORMANT 而同树测试标 owner decision」待裁。
- 差在哪：本批文件同时持有"crpix 是 1-based FITS 值"与"x 是 0-based"两个标注，而 `x = u + crpix` 使二者不可能同时成立；且定义口径的头文件（`ipv_wcs.h:63-67`、`ipv_types.h:139`）不指向已登记的 WCS-003-F1，读者无从知道这里有 1 px 已知分歧。
- 后果：按标准 FITS 语义解释交付 WCS（CRPIX/CD/SIP 三元组）的读者（astropy 即如此，故测试必须桥接）与本仓 P1 内部像素相差 1 px ⇒ 在 6.3076 ″/px 的帧上等于 6.3″ 系统偏移；对 1e-4 px 级 roundtrip 门与 2.0 px 的测光匹配半径（`docs/algorithms/PHOTOMETRIC_FIT.md:232`）是数量级超差。Phase1 与 Phase3 两条实现各自自洽、彼此不兼容，属"同一产品两口径"。
- 定级：S2 —— 标注互斥与 1 px 平移均 CONFIRMED；未定 S1 因为**没写出同一运行内两口径混用的现役调用点**（P1 消费方与 P3 导出各自成套），且"哪一侧应改"需交付面实跑比对（见收尾节）。
- 怎么算修好：① 由负责人按 A-14 裁决正解（技术建议：内部统一 0-based 且 `x = u + (crpix − 1)`，与 P3 一致）；② 裁决前先把互斥标注改成语义唯一的一条（`ipv_wcs.h:70`、`ipv_types.h:139`、`ipv_wcs.cpp:161` 三处同批），并在三处加"已知 1 px 口径分歧 = WCS-003-F1（待裁）"指针；③ `p1wcs_astropy_cross.py` 桥接注释与 `GATES_AND_TOLERANCES.md` 登记同批，避免只有测试脚本注释承载该事实。
- 置信：CONFIRMED（互斥与偏移量逐字核到；"哪侧为正"PARTIAL）

### CR-42-17 · 遗留 `build_wcs` 与其 `flip_mode`/`fit_sip` 关联在三个登记面口径不一
- 轴：D（设计说已统一）＋ C（登记面）
- 位点：`ipv_wcs.h:23-24` `// flip_mode - 镜像模式 (0=NONE, 1=FLIP_X, 2=FLIP_Y, 3=FLIP_XY) / // 决定 CD 矩阵的符号方向 (W = flip_mode(W'))`、`:27` `// 注: 旧 build_wcs 仅为向后兼容保留…`；对照 `ipv_types.h:133` `// 移除 best_mode (统一求解, 无 flip_mode 区分)` 与 `ipv_types.h:71-72` `//   (fit_sip 唯一调用点 build_wcs 显式清零全部逆向字段, 零未初始化泄漏)。`
- 上位依据：`docs/algorithms/PLATESOLVE.md:210` `API: ipv_api.h: ipv_solve_from_detections_v1, ipv_wcs.h: build_wcs`（权威算法文档把 build_wcs 列为关联 API 面，未标遗留）。
- 现状：`build_wcs` 生产零调用者（`git grep -n "build_wcs(" -- lib` 唯一调用 = `test_synthetic.cpp:277`）；`fit_sip` 唯一调用者即它（`ipv_wcs.cpp:165`）；types.h:71-72 却用"fit_sip 唯一调用点 build_wcs 清零"论证 **生产** SIPCoeffs 无未初始化泄漏——生产路径实为 `extract_wcs_sip` 自行填充 AP/BP/APx/BPx（`ipv_wcs.cpp:418/542` 网格拟合），真正的零初始化保证另有来源：入口值初始化堆对象（`ipv_entry.cpp:241 auto wcs_ptr = std::make_unique<ipv::WcsFitResult>();`，同型 :277/:592）与 `ipv_wcs.cpp:131 WcsFitResult result{};`。
- 差在哪：把生产结构体的"零未初始化"保证绑在一个只有测试调用者的遗留函数上；同时设计面（无 flip_mode）与该函数签名（仍收 flip_mode）及 PLATESOLVE 的 API 清单互相打脸。
- 后果：审计顺注释去找保证来源会落到一条测试路径（与本项目已登记的"锚漂移⇒复核整体落空"同形态）；若 `extract_wcs_sip` 日后新增字段而只改 build_wcs 的清零循环，注释会让改者以为已覆盖生产。
- 定级：S3（当前确无未初始化泄漏（`make_unique<T>()` 值初始化已核实）；错在论证绑错对象与遗留面登记不一致）
- 怎么算修好：types.h:71-72 的论证改指真实机制（`ipv_entry.cpp:241/277/592` + `ipv_wcs.cpp:131`），并给 `SIPCoeffs` 尾部字段补 `static_assert`/偏移锁；`build_wcs` 要么按 `PLATESOLVE.md` 正式登记其状态（遗留/仅测试），要么与 `FlipModeResult`（CR-42-8）一并退役并登记。
- 置信：CONFIRMED

### CR-42-18 · `reject_code` 的三类语义与实现不一：3 号兼指 CD 奇异、1 号只查有限性（背面点会落 2 号）
- 轴：R（失效面不可区分）＋ C
- 位点：`ipv_wcs.h:52-54`（`reject_code=1 投影背面/输入非有限 (TAN 正投影 cosc≈0 或 NaN/Inf); =2 发散…; =3 牛顿矩阵奇异 (|det J|<1e-15, 畸变场奇点域)`）、`:73` `int reject_code;  // 0=收敛, 1=背面/非有限, 2=发散/未收敛, 3=J 奇异`
- 上位依据：仓内已确立同一纪律：`ipv_types.h:110-119`（P14-N-10：旧实现把触顶错记为 converged ⇒ 补 `m_lim_converged/query_failed/empty_sweep/zero_queries`，使「真收敛／查询失败／触顶降级」可区分）；`docs/algorithms/PLATESOLVE.md:290`（对"无收敛判据即静默返回可疑值"的否定性描述）。
- 现状：`:877` 的 CD 奇异（实现自书 `// CD 奇异 (构造病态)`）也写 `out.reject_code = 3;`；1 号实现只有有限性判定（`:869-873` → `tan_project_iter` → `:822 return std::isfinite(*xi) && std::isfinite(*eta);`，另 :799-800），没有 cosc 阈值 ⇒ 位于背面但结果未溢出的点会带着巨大的有限 (ξ,η) 继续进牛顿，最终多以 2 号（步出 `guard`）收场。
- 差在哪：声明的枚举不是三个互斥失效面——"投影背面"在实现里无独立判据，"构造病态"与"畸变场奇点"共用 3 号。
- 后果：任何按 1/2/3 归因失败原因的对外统计会被系统性错分（真正背面 ⇒ 记成发散；CD 病态 ⇒ 记成畸变场奇点），与 P14-N-10 已在本仓另一处订正过的失效面同源。
- 定级：S3（数值结果仍是拒绝，不产生错值；错在诊断归因）
- 怎么算修好：1 号补显式 cosc 下限（给依据并进 §9），CD 奇异另立 4 号，头文件枚举与实现逐条对齐；补一条锁：每个 reject_code 值都有可独立复现的最小用例（正例）＋注入必判红（负例）。
- 置信：CONFIRMED

---

## 1. `lib/algorithms/platesolve/cpp/ipv/include/ipv_solver.h`（294 行，全读）

### 1-T §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| ipv_solver.h:19-24 | `ra0=0.0 / dec0=0.0 / n_matched=0 / n_iterations=0 / convergence=0.0 / success=false` | 0 | 结果结构体零初始化哨兵 | 结构性不适用 | 默认初始化值，不承载科学量；`success=false` 与 `valid` 位并存 ⇒ 未测由布尔位表达（本批正面样例之一） | — | CONFIRMED |
| ipv_solver.h:23 | `convergence`「最终收敛值 (角秒)」 | — | 收敛度量单位口径 | 待确认 | 与 :36 判据同为角秒 ✓；但 `convergence=0.0` 与"完美收敛"同值——本批检索 `git grep -n "\.convergence" -- lib` 显示该字段**无任何判据消费者**（仅赋值/日志）⇒ 不构成现役误读，登记不报 | — | CONFIRMED |
| ipv_solver.h:31-32 | 「用 TRANS 常数项 x00/y00 反推新中心」「常数项已是角秒, 直接用, 不乘 s0」 | — | 反推公式与单位口径 | 公式导出 | 与 :36/:75 的角秒口径一致；实现 `ipv_solver.cpp` iterative_reproject 段（本批未逐行核） | — | PARTIAL |
| ipv_solver.h:36 | 收敛判定 `sqrt(x00²+y00²) < 0.01"` | 0.01 | 迭代重投影自洽收敛阈 | 公式导出 | `docs/algorithms/PLATESOLVE.md §9 :185-188` 逐条登记：「收敛 0.01" (pixel/3600)：`ipv_solver.cpp:199 CONV_THRESH_ARCSEC = 0.01`，判据 `√(x00²+y00²) < 0.01″`…**量纲 arcsec**；适用域：只约束自洽收敛，不是天测精度界——产品级门另立（G-P1-WCS-CLOSURE，阈值 UNJUSTIFIED）」⇒ 锚在跟踪面且适用域写清，**合规** | 收紧 ⇒ 更多迭代（偏慢）；放宽 ⇒ 自洽界松（该值不进产品判据） | CONFIRMED |
| ipv_solver.h:39-45 | 入参单位口径（U 像素 / s0 角秒每像素 / ra0,dec0 度 / img_* 像素） | — | 单位声明 | 结构性不适用 | 单位定义面；`U` 本文件写"像素"与 `ipv_types.h:38`、`ipv_triangle.h:66` 一致（`ipv_types.h:92`、`ipv_wcs.h:19` 的"角秒"为异端）⇒ CR-42-1 | — | CONFIRMED |
| ipv_solver.h:63 | 「extract_wcs_sip: 从 TRANS 提取 WCS + SIP (U→W, 角秒)」 | — | 变换方向与量纲 | 结构性不适用 | 方向声明，与 `ipv_wcs.cpp:274` 注一致 | — | CONFIRMED |
| ipv_solver.h:67 | 「CD 矩阵: trans 线性项 / 3600 (度/像素)」 | 3600 | 单位换算定义常数 | 结构性不适用 | 角秒→度定义换算（§4 非缺陷清单明示不报）；实现逐字一致 `ipv_wcs.cpp:277-280 result->cd.cd11 = trans.x10 / 3600.0;` | — | CONFIRMED |
| ipv_solver.h:70 | 「CRPIX = 图像中心 (1-based FITS, width/2+0.5, **x0=CRPIX-1**) (ipv_wcs.cpp:154,276)」 | 2 / 0.5 | 参考像素公式＋行锚 | 公式导出（公式）＋待确认（锚与口径） | 公式本体正确（W=1024 ⇒ 512.5，本批复算与 `p3_wcs.cpp:114 (W+1)/2` 同值 ✓）；**两处行锚漂移**：CRPIX 实赋值在 `ipv_wcs.cpp:291-292`（build_wcs 侧 :162-163），:154 是 CD 装配、:276 是 CD 段注释；且本行 `x0=CRPIX-1` 的 0-based 换算与 `ipv_wcs.cpp:945 out.x = u + wcs.crpix[0]`（`ipv_wcs.h:70` 标 "0-based"）互斥 ⇒ CR-42-16、CR-42-20 | — | CONFIRMED |
| ipv_solver.h:71 | 「SIP 前向 A/B: 解析公式 A[i][j]=cd_inv·(trans.x_ij,trans.y_ij) (order>=2)」 | 2 | 解析导出式与最低阶 | 公式导出 | 声明为解析式（非拟合）；本批未逐式核 `ipv_wcs.cpp` 的 A/B 装配段 ⇒ 不判定 | order≥2 才装 SIP 与 `SIPCoeffs::order`（0=无 SIP）口径一致 | PARTIAL |
| ipv_solver.h:72 | 「SIP 逆向 AP/BP: **NB_GRID=7** 网格反变换拟合, AP[1,0]-=1, BP[0,1]-=1 (ipv_wcs.cpp:463-464)」 | 7 | 逆向拟合采样网格 | 待确认 | **值已过期**：实现 `ipv_wcs.cpp:418 const int NB_GRID = 41;  // 每轴网格点数 (整改: 7 -> 41)`；同批 `ipv_types.h:67` 与 `docs/algorithms/PLATESOLVE.md:176`（「实现 41×41/81×81，DISP-WCS-008」）均已按 41 说 ⇒ 本行是全仓唯一残留 7 的表述；行锚亦漂移（`AP[6] -= 1.0; // AP_10`、`BP[1] -= 1.0` 实在 :513-514）⇒ CR-42-20 | 网格加密方向单调提升逆表示保真度；按 7 复核会低估保真度并误判门余量 | CONFIRMED |
| ipv_solver.h:73-74 | 「Y-down 输出: cd12/cd22 取反, A' = A·(-1)^j, B' = -B·(-1)^j, AP/BP 同规则 (ipv_wcs.cpp:542-570)」 | −1 | Y-up→Y-down 符号变换式 | 公式导出（式）＋待确认（锚） | 式与实现一致（`ipv_wcs.cpp:658-664` 起为"8. 转换为标准 FITS WCS (Y-down) / CD: cd12, cd22 取反"）；**行锚指向别处**：:542-570 实为 APx/BPx 的 81×81 网格拟合块（:542 `NB_GRID_X = 81`）⇒ CR-42-20 | — | CONFIRMED |
| ipv_solver.h:75 | 「rms_px = rms_arcsec / s0」 | — | 双口径 RMS 换算式 | 公式导出 | 与 `ipv_types.h:141-142` 两字段、`ipv_wcs.cpp:736-754`（`isfinite(rms_px) \|\| rms_px > 0.5` 的 fail-closed 判据）同口径 | — | CONFIRMED |
| ipv_solver.h:82-83 | RMS 用 U(像素)/W(角秒) | — | 残差量纲声明 | 待确认 | 与 `ipv_ransac.cpp:331/341/482/486` 把同类残差标为 "px" 相斥 ⇒ CR-42-1 | — | CONFIRMED |
| ipv_solver.h:102-108 | 「v1.3 … 通过 C API 导出 … 用于 `wcs_closure_diagnostic.py` 的 `--authoritative-pairs` 模式 … 字段对应 `25_AUTHORITATIVE_MATCH_PAIR_CONTRACT.md` 契约」 | — | 两个对外权威指针 | 待确认 | 两份被指名的载体在跟踪面**均不存在**：`git ls-files \| grep -iE "closure\|diagnostic"` 无 `wcs_closure_diagnostic.py`；`git ls-files \| grep -i AUTHORITATIVE` 零命中；`git grep -ln "25_AUTHORITATIVE_MATCH_PAIR_CONTRACT"` 全仓仅本文件与 `lib/algorithms/platesolve/memory.md`、旧台账 ⇒ CR-42-19（真正承载 9 列合同的是 `docs/contracts/PUBLIC_API.md:942,962-963` 与 `DATA_SEMANTICS.md:1001`） | — | CONFIRMED |
| ipv_solver.h:109-118 | `matched`/`U_snapshot` 索引空间与 `robust_applied` 语义 | — | 索引口径声明 | 结构性不适用 | 口径声明；`U_snapshot` = `selection.U_full`（若精化应用）否则 `selection.U`（:111），两者单位同为像素（`ipv_types.h:128`）⇒ 与 CR-42-1 一致 | — | CONFIRMED |
| ipv_solver.h:119-131 | `SolveInlierCache` 各字段默认（s0=0.0, ra0/dec0=0.0, img_*=0, valid=false） | 0 | 缓存零值 | 结构性不适用 | 哨兵默认；实现有效性判据 `ipv_solver.cpp:1722 last_inliers_.valid = !matched.empty() && s0 > 0.0;` ⇒ 未测由 valid 位表达 ✓ | — | CONFIRMED |
| ipv_solver.h:187-188 | 「detections: FLOAT64 [N,6] star_det v1 格式」 | 6 | 输入行宽与格式版本 | 结构性不适用 | 数组行宽；版本以函数名后缀 `_v1` 承载（非首两字段约定），列语义正本 = `docs/algorithms/STAR_DETECTION_ALGORITHMS.md`（本批未核其 6 列定义与实现是否一致） | — | PARTIAL |
| ipv_solver.h:244 | 「大小 = max_count * 9 个 double」 | 9 | 对外行式 ABI 的列宽 | 结构性不适用（宽度本身）＋待确认（版本化） | 列宽是行式 ABI 的结构常量，本身非科学量；但**无任何尺寸/版本自描述位**，也无锁 ⇒ CR-42-21（合同面 `PUBLIC_API.md:962` 与 `DATA_SEMANTICS.md:1001` 各自复述"9 列"，三处各写一遍） | 列宽改而调用方未改 ⇒ 整行字段错位（与 V2-N-01 同型） | CONFIRMED |
| ipv_solver.h:246-254 | 9 列字段定义（含 `[4] pred_x_px — 内部 TRANS 预测 x (像素, **经 s0 缩放**)`） | — | 列语义与单位 | 公式导出 | 实现方向明确：`ipv_solver.cpp:1694-1696 apply_trans(...) -> (xi_asec, eta_asec) [TRANS: U(像素)→W(角秒)] / pred_x_px = xi_asec / s0` ⇒ "缩放"实为**除 s0**，头文件未标方向（同族单位口径问题）⇒ CR-42-1 的又一处位点 | — | CONFIRMED |
| ipv_solver.h:255 | 「返回实际写入的行数 (<= max_count), <0 表示错误」 | -1 | 错误返回约定 | 公式导出 | 实现逐条满足：`ipv_solver.cpp:1742-1747`（无缓存返 0；`out_buffer==nullptr \|\| max_count<=0` 返 −1）、`:1750 n_out = std::min(n_total, max_count)`、`:1763-1765` 索引越界写 NaN ⇒ 退化用 NaN 而非合法数值冒充 ⇒ 本批 focus ④ 的**正面样例** | — | CONFIRMED |
| ipv_solver.h:260 | 私有流程链（select → triangle_match → iter_trans → iterative_reproject → hi_order_rematch → robust_refine → extract_wcs_sip） | — | 归约顺序声明 | 公式导出 | 与 `ipv_solver.cpp` 各阶段调用序一致（`:458 triangle_match`、`:534 iter_trans`、`:660 hi_order_rematch`、robust_refine、extract_wcs_sip） | — | CONFIRMED |

（本文件数值项计 21 行。）

### 1-F findings

### CR-42-19 · 头文件把对外 inlier 合同指向两份仓内不存在的载体
- 轴：C（权威指针悬空）＋ D
- 位点：`ipv_solver.h:106` `// 用于 wcs_closure_diagnostic.py 的 --authoritative-pairs 模式。`、`:108` `// 字段对应 25_AUTHORITATIVE_MATCH_PAIR_CONTRACT.md 契约:`
- 上位依据：`AGENTS.md §1`（沿索引读权威）与 §7 目录落位（合同说明正本面 = `docs/contracts/`）；`docs/contracts/PUBLIC_API.md:942,962-963` 与 `docs/contracts/DATA_SEMANTICS.md:1001` 才是实际承载"9 列 inlier 缓冲"的跟踪面条目。
- 现状：`git ls-files | grep -i AUTHORITATIVE` 零命中；`git ls-files | grep -iE "closure|diagnostic"` 无 `wcs_closure_diagnostic.py`（只有 `eng/tools/astrometry/closure_metric.py` 等不同名）；`git grep -ln "25_AUTHORITATIVE_MATCH_PAIR_CONTRACT"` 全仓只有本文件、`lib/algorithms/platesolve/memory.md` 与 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv` 的历史记录；`docs/contracts/` 现有 17 份文件全部为命名式，无 `NN_` 序号系列。
- 差在哪：对外字段合同（`matched` 的 u/w 索引空间、`robust_applied` 语义）声明"对应某契约"，而该契约载体不在仓内；同时被指向的诊断脚本也不在仓内，"供 WCS Gate v2 双层闭环使用"的用途无法按锚复核。
- 后果：① 复核者按 :108 找正本整体落空（与 `FIX_LEDGER.csv:155` 已登记的"锚系统性漂移 ⇒ 复核整体落空"同形态）；② 真正定义 9 列的 `PUBLIC_API.md:942` 自述行锚 `:232`、`DATA_SEMANTICS.md:1001` 自述 `ipv_api.h:203-221`，而声明现在 `ipv_api.h:262` ⇒ 合同面与代码也已漂移，三处各说各话；③ 读者据此以为"另有更细的正本"，就不会去补这份字段合同。
- 定级：S2 —— 悬空即"字段对应契约"这一对外主张当前不成立；不改产物字节。
- 怎么算修好：① 把 9 列 inlier 缓冲的字段合同（u 索引空间随 `robust_applied` 切换、越界写 NaN、−1 的含义）正式落 `docs/contracts/`（或并入 `PUBLIC_API.md` 的 ipv 节）；② :106/:108 改指真实载体；③ 同批把 `PUBLIC_API.md:942,962`、`DATA_SEMANTICS.md:1001` 的行号锚改成符号锚（`ipv_get_last_inliers`）；④ 加一条"具名合同文件可解析性"检查（注释/文档里出现的 `*.md` 文件名必须能在 `git ls-files` 命中，配正例/负例）。
- 置信：CONFIRMED

### CR-42-20 · `extract_wcs_sip` 注释自称"与实现一致"，但一个参数值过期、三条行锚指向别处
- 轴：C（注释↔实现）
- 位点：`ipv_solver.h:65-66` `// 流程 (与 lib/algorithms/platesolve/cpp/ipv/src/ipv_wcs.cpp 实现一致, 见 / // docs/algorithms/PLATESOLVE.md Invariants):`、`:70`（CRPIX 锚 `ipv_wcs.cpp:154,276`）、`:72` `NB_GRID=7 … (ipv_wcs.cpp:463-464)`、`:74` `(ipv_wcs.cpp:542-570, 内部 Y-up → 标准 FITS Y-down)`
- 上位依据：`docs/algorithms/PLATESOLVE.md:176`「最小二乘(AP/BP，采样网格 ≥7×7，实现 41×41/81×81，DISP-WCS-008)」（权威算法文档按 41/81 记载）；同册 :329-330 F3 CRPIX/Y-down 不变量条款。
- 现状（实现侧）：CRPIX 实赋值 `ipv_wcs.cpp:291-292 result->crpix[0] = img_width / 2.0 + 0.5;`；网格 `:418 const int NB_GRID = 41;`（自注「整改: 7 -> 41」）；`AP[6] -= 1.0; // AP_10`、`BP[1] -= 1.0;` 在 `:513-514`；Y-up→Y-down 在 `:658-664` 起第 8 段；:74 引用的 `:542-570` 实为 APx/BPx 81×81 拟合块（`:542 const int NB_GRID_X = 81;`）。
- 差在哪：一条以"与实现一致"自我担保的注释里，`NB_GRID` 用的是整改前的旧值 7，且六个行锚（154/276/463/464/542/570）全部落在别的语义块上。
- 后果：① 按 :72 复核 SIP 逆向保真度会把采样网格低估 34 倍（7²=49 vs 41²=1681），进而误判 `PLATESOLVE.md:176` 不变量与门余量来源；② 行锚不可解析 ⇒ 顺锚回验的审计在此整体落空（本项目已为该形态立过 P1 条目）；③ 本行 :70 的 `x0=CRPIX-1` 与 `ipv_wcs.cpp:945`/`ipv_wcs.h:70` 的 `x = u + crpix` 合起来是 CR-42-16 那个 1 px 口径分歧的第三个位点。
- 定级：S2 —— 注释与实现不合为逐字事实；无产物字节直接由该注释产生，故不判 S1。
- 怎么算修好：:70/:72/:74 行锚改符号/段名锚（`extract_wcs_sip` 的 "3. CRPIX"、"5.3 段 AP/BP"、"8. Y-down"），`NB_GRID=7` 改 41 并注 DISP-WCS-008；与 CR-42-16 的像素原点口径同批处理；补一条锚可解析性检查（`file.cpp:NN` 须在容差内命中被描述符号，处置建议已见 `FIX_LEDGER.csv:155`）。
- 置信：CONFIRMED

### CR-42-21 · 行式对外 ABI（9 列 double）无版本/尺寸位，"首两字段自描述"约定在此无承载位
- 轴：D（违 §8.6 式约定）＋ C
- 位点：`ipv_solver.h:244` `// 输出 out_buffer: 调用方分配的缓冲区, 大小 = max_count * 9 个 double`、`:246-254`（9 列逐列定义）、`:256` `int get_last_inliers(double* out_buffer, int max_count) const;`
- 上位依据：`ipv_api.h:65-66`（宪章 §8.6「跨 DLL 边界使用版本化 C ABI …… 结构体带 struct_size/abi_version」）及其落地（`IpvParams` 首两字段 + `ipv_entry.cpp:206-217 validate_params_abi()` fail-closed + ctest `ipv_abi_layout_lock`）；`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:90`（V2-N-01 事故与建议②）。
- 现状：列宽 9 以"三处各写一遍"的方式存在（本文件 :244、`docs/contracts/PUBLIC_API.md:962`、`docs/contracts/DATA_SEMANTICS.md:1001`），签名里没有任何 `stride/version` 实参或出参；`ipv_abi_layout_lock` 探针只覆盖 `IpvParams`/`IpvWcsResult` 两个 POD（`test/ipv_abi_layout_probe.cpp:23-25,108-125`），行式 ABI 不在其面；机器面 `eng/ci/prod_wiring_baseline.json:170` 登记 `unreachable:manifest.source_symbols:ipv_get_last_inliers` ⇒ 生产无消费方。
- 差在哪：§8.6 的自描述约定只按"结构体"落地；跨 DLL 的**裸缓冲区**同样携带隐式布局合同（行宽＋列序），却无版本/尺寸位可校验——V2-N-01 的失效形态换了载体继续存在。这正是派单重点②问的"首两字段约定在哪些结构上缺失"：本批四个结构体（`IterativeReprojectResult`/`SolveInlierCache`/`WcsIterativeResult`/`TriangleMatchResult`）均为仓内 C++ 结构，约定本不适用；真正的对外面缺口在 `IpvWcsResult`（CR-42-7）与这条行式缓冲区合同（本条），后者连放版本位的地方都没有。
- 后果：将来给 inlier 行加一列（variance、frame_index 等）时，老调用方仍按 9 解释 ⇒ 自第 10 列起整行错位读到别的字段，而函数返回"写入 N 行"仍算成功 ⇒ **偏绿**（不报错、不判红、静默错位）。当前因无生产消费者仅暴露于诊断面。
- 定级：S3（生产不可达已被机器面登记；错在结构上可错配而无人校验）
- 怎么算修好：把行式导出包成带 `struct_size/abi_version` 的结构数组，或在签名加 `row_stride` 出参并由调用方断言等于本地期望；9 列定义收敛为单一事实源（`lib/algorithms/platesolve/tools/ipv_abi_mirror.py` 同族生成），三处引用同一处；补 ctest 正例（stride 一致）＋负例（篡改列序必报警）。
- 置信：CONFIRMED

---

## 5. `lib/algorithms/platesolve/cpp/ipv/src/ipv_robust_refine.cpp`（1132 行，全读）

被审文件为 `.cpp`；其判据阈值的**默认值住在 `include/ipv_robust_refine.h`（非本批）**，本表逐项标出"本文件如何消费该默认值"，默认数值只作为锚上下文引用，不据以立本批 finding。

### 5-T §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| ipv_robust_refine.cpp:4 | 「实现 spec: `.trae/specs/robust-augmented-wcs-refine/spec.md`」 | — | 设计依据指针 | 待确认 | 该 spec **不在跟踪面**（`git ls-files \| grep -iE "trae\|specs/robust"` 零命中）⇒ 本模块一切"对齐 spec"的理由句（:728）不可复核；权威算法文档只有 `docs/algorithms/PLATESOLVE.md:243` 一行条目 ⇒ CR-42-25、CR-42-29 | — | CONFIRMED |
| :48-50 | `RR_PI` / `RR_DEGTORAD=PI/180` / `RR_ASEC_PER_RAD` | 3.14159265358979323846 / 180 / 206264.80624709636 | 数学与角度换算定义常数 | 公式导出 | 本批复算 `180/π×3600 = 206264.80624709635` ✓ 与字面量逐位一致；角度换算定义常数属 §4 非缺陷清单 | — | CONFIRMED |
| :53 | `NUMERIC_DIFF_STEP` | 1e-6 | 前向差分步长（注释承诺用） | 待确认 | **死常数**：`git grep -n "NUMERIC_DIFF_STEP" -- lib` 仅命中定义行本身；实现改用解析雅可比（:730-742），而 :20、:653、:728 三处仍承诺"数值微分, 步长 1e-6" ⇒ CR-42-25 | — | CONFIRMED |
| :56-58 | `TRANS_ORDER` / `N_PARAMS_PER_AXIS` / `N_PARAMS_TOTAL` | 3 / 10 / 20 | 多项式阶与参数基数 | 结构性不适用 | 本批复算三阶完整基每边 (3+1)(3+2)/2=10 ✓、两侧 20 ✓；基数不承载科学语义（阶数选择的科学依据在 :936 强制升阶一条，见 CR-42-28） | — | CONFIRMED |
| :62-66 | `MONOMIAL_BASIS[10]` 排列 x00,x10,x01,…,x03 | 10 | 基函数索引表 | 结构性不适用 | 布局约定；本批复算阻尼所加下标 `{1,2,11,12}`（:780-783）确为 x10/x01/y10/y01 ✓ 与注释一致 | — | CONFIRMED |
| :89 | `valid = (cosc > 1e-10)` | 1e-10 | 切平面背面判定阈 | 待确认 | 零锚（`git grep -n "cosc" docs` 无条目）；同仓另两处对同一判定取不同做法：`ipv_wcs.cpp:822` 只查 `isfinite`（无 cosc 阈），`ipv_wcs.h:52` 承诺"cosc≈0"却无实现 ⇒ CR-42-24、CR-42-18 | 阈值越大 ⇒ 越早判背面（偏保守）；1e-10 相当于 ±0.0006° 的余量，几乎不生效 | CONFIRMED |
| :96-97 | 无效时 `xi_asec/eta_asec = 0.0` | 0.0 | 未测位置的返回值 | 待确认 | **用合法数值（切点）冒充"未测"**；调用方靠 `valid` 出参补救（:918-921）⇒ CR-42-24 | 若任一未来调用方漏检 valid ⇒ 该星被放在 (RA0,Dec0)，产生一个假配对（偏绿） | CONFIRMED |
| :157,164 | `rr_gauss_solve` 主元阈 `max_val < 1e-15` | 1e-15 | 正规方程奇异判定 | 待确认 | 零锚；正规方程元素量级 = Σ w·basis²，随像素坐标幂次急剧变化（三阶基在 4000 px 帧上 u³≈6.4e10）⇒ 绝对阈 1e-15 与矩阵尺度无关，实际几乎恒不触发（偏绿：不报奇异） | CONFIRMED |
| :205,214 | 注释「100-300 颗」「>300 截断到 300; <100 保持原样」 | 100/300 | 候选池上下限说明 | 待确认 | 与 `.h` 的 `max_stars=300 / min_stars=100 / total_target_normal=180` 数值一致；但 **min_stars 不进入任何判据**（:308 注释自认不填充，:314 只把它打进日志）⇒ CR-42-30 | 上限生效方向 = 按 mag 升序截断（保亮星，偏保守） | CONFIRMED |
| :208,231-233 | FOV 断点 `1.0°` / `3.0°` | 1.0/3.0 | 网格档切换视场阈 | 待确认 | 零锚（`git grep -n "FOV 断点" docs` 无对应条款）；断点两侧格数 2/3/4 在 `.h` ⇒ 三者共同决定空间均匀度 | 视场阈值调大 ⇒ 更多帧落"narrow=全选不限量"分支（候选池可达 `total_target_narrow=1000`，成本↑） | CONFIRMED |
| :209,236-240 | `N_long = round(G_short × W/H)`、`aspect` 回退 1.0 | — | 长边格数导出式 | 公式导出 | 实现按式（`H>0` 防除零，`N_long<1⇒1`），与注释一致 | — | CONFIRMED |
| :211,247 | `quota_per_cell = max(4, total_target / n_cells)` | 4 | 每格最小配额 | 待确认 | 零锚；4 决定"稀疏格是否入选"，直接影响空间均匀性下界 | 取大 ⇒ 每格星更多 ⇒ 池更偏亮星密集区；取小 ⇒ 更均匀 | CONFIRMED |
| :260-263,274-279 | `half_W/cell_w/cell_h` 与格索引 clamp | 2.0 | 网格几何式与越界钳位 | 结构性不适用 | 坐标换算与钳位（防越界），无科学语义 | — | CONFIRMED |
| :300-306 | `params.max_stars` 截断 | 300(`.h`) | 池上限 | 待确认 | 判据方向明确：超限按 mag 升序保留 ⇒ 牺牲暗星（保守侧）；取值零锚 | — | CONFIRMED |
| :347,356-358 | `tol_arcsec`、`tol2/ratio2/abs2` 平方化 | 0.75/0.5(`.h`) | 匹配容差与 Lowe 阈 | 公式导出 | 本批复算：`d1²/d2² < 0.75² ⇔ d1/d2 < 0.75` ✓、`d1² < 0.5² ⇔ d1 < 0.5` ✓ ⇒ 平方化无口径错误（**核过项，不报**）；两个阈值本身零锚（见 CR-42-29） | 阈小 ⇒ 少配对（偏红）；阈大 ⇒ 误配（偏绿） | CONFIRMED |
| :388,417 | `best_d2 > tol2 continue` / `best_d2 <= tol2` | 边界含等号 | 两个方向的一致边界 | 结构性不适用 | 等号归属为边界选择，非科学量 | — | CONFIRMED |
| :476 | `if (n < params.lowe_k_neighbors + 1) return weights;` | 8(`.h`)+1 | 样本不足早退 | 待确认 | **早退返回全 1.0 权重 = 用"全部一致"冒充"未测"**；方向：权重全 1 ⇒ 剔除层不做任何事 ⇒ **偏绿**。现役被上游 `min_matched_final=30 > 9` 兜住（:898/:995）⇒ CR-42-27 | — | CONFIRMED |
| :488,512,522 | `K=min(K, n-1)`、`mean/K`、`sigma_local=sqrt((var_rx+var_ry)/K)` | — | 邻域统计式 | 公式导出 | 有偏除 K 为一致选择；注意 `sigma_local` 是 x/y 两分量方差之和的开方，注释只写"std(r_neighbors)"（口径含糊，非缺陷） | — | CONFIRMED |
| :523 | `if (sigma_local < 1e-9) continue;` | 1e-9 | "邻居完全一致 ⇒ 不判定"阈 | 待确认 | 零锚；不判定时该点保持权重 1.0（与 :476 同形：未测⇒合格） | 保守侧应为"可疑"而非"通过"（当前取通过） | CONFIRMED |
| :529 | `dr > spatial_consistency_sigma × sigma_local` | 3.0(`.h`) | 3σ 剔除阈 | 待确认 | 3σ 无本仓推导（`git grep -n "3σ\|3×sigma" docs` 零命中）；与测光层的稳健阈（`PHOTOMETRIC_FIT.md` 用 Huber 1.345 / Tukey 4.685）不共用口径 | 阈小 ⇒ 多剔点（偏保守）；阈值大 ⇒ 保留外点 | CONFIRMED |
| :538-539 | 日志 `"σ=%.3f\"" … std::sqrt(rx[0]*rx[0]+ry[0]*ry[0])` | — | 诊断字段与实参不符 | 待确认 | 打印的是**第 0 个匹配点的残差范数**，标签却写 σ（真正的 σ 参数 `spatial_consistency_sigma=3.0` 未打印）⇒ CR-42-31 | — | CONFIRMED |
| :579 | `compute_rms`: `if (n == 0) return 0.0;` | 0.0 | 无样本时的 RMS | 待确认 | **0.0 = "完美解"与"没算"同值**；影响：`baseline_rms=0.0` 时 :1091 的回退门变成 `rms_cur > 0` ⇒ 任何正 RMS 都回退（**偏红**，fail-closed），且 :1087 空集检查兜住 ⇒ CR-42-24 | — | CONFIRMED |
| :584 | `sqrt(sum/n)` | — | RMS 定义式 | 公式导出 | 二维残差合方 RMS，与 `ipv_solver.h:75`、`WcsFitResult` 口径一致 | — | CONFIRMED |
| :589,593-598 | `c = 4.685`（`.h` tukey_c）、`sigma < 1e-12 ⇒ return 1.0` | 4.685 / 1e-12 | Tukey biweight 调参常数与除零护栏 | 文献值 | `docs/algorithms/PHOTOMETRIC_FIT.md:34` `statsmodels robust/_tables.py: tukeybiweight_eff[0.95]=(4.685065, 0.119414)`；`:304` 给出载体与"95% 渐近效率只对已知尺度严格成立"的适用域 ⇒ 值有锚；**但 platesolve 侧容差册 `PLATESOLVE.md §9` 未收本模块常数** ⇒ CR-42-29；本文件实现用 `c·sigma` 而非 `k·MAD` 直接式，与 :589 注释一致 ✓ | 1e-12 护栏方向 = sigma 极小时全部给权重 1.0（不剔点，偏绿） | CONFIRMED |
| :601,605,610 | `1.482602218505602 × MAD`；`if (n == 0) return 1.0;` | 1.482602218505602 / 1.0 | MAD→σ 一致性常数；无样本 σ 哨兵 | 公式导出（常数）＋待确认（哨兵） | 本批复算 `1/Φ⁻¹(0.75) = 1/0.67448975… = 1.482602218505602` ✓；**n==0 返回 1.0**：用 1.0 角秒冒充"无残差样本"，与 :579 的 0.0、:96 的 0.0、:920 的 1e18 构成同一模块内四种"未测"写法 ⇒ CR-42-24 | sigma=1.0 时若真有星 ⇒ 剔点按 4.685″ 尺度做（偏松）；现役被 :676 `n≥10` 挡住 | CONFIRMED |
| :615-631 | `cd_free_threshold 0.01` / `cd_freeze 0.03` / `damp_base 1e-4` / `damp_freeze 1e6` 与对数插值 | 见左 | CD 阻尼三段式 | 公式导出（插值式）＋待确认（四个值） | 实现真按式算（`log10` 线性插值后 `10^`，:625-630），与注释一致 ✓；四个值零锚 ⇒ CR-42-29。判据方向：`abs_rc < free ⇒ 0`（自由）、`> freeze ⇒ 最大阻尼` ⇒ 变化越大阻尼越大 = 保守方向 ✓ | — | CONFIRMED |
| :676 | `if (n < N_PARAMS_PER_AXIS) return false;` | 10 | 每边可辨识最小样本 | 公式导出 | 10 = 每边基函数个数 ⇒ 结构性下界（非经验值）；现役被 `min_matched_final=30` 掩盖 | — | CONFIRMED |
| :697-703 | `r_abs` 合并 x/y 与 `sigma < 1e-9 ⇒ sigma = 1e-9` | 1e-9 | 稳健尺度下限 | 待确认 | 零锚；后果：残差极小的"干净解"会被 1e-9 下限放大 u 值 ⇒ Tukey 权重趋 0 ⇒ `wi<1e-15` 全跳过（:759）⇒ A 阵全零 ⇒ `rr_gauss_solve` 返 false ⇒ 本轮"失败"并 break ⇒ 方向 = 偏红（放弃精化），但**放弃后仍可能以 success=true 交付** ⇒ CR-42-23、CR-42-26 | CONFIRMED |
| :710 | `params.tukey_c` | 4.685 | 同上 | 文献值 | 同 :589 行 | — | CONFIRMED |
| :727-733 | 「apply_params 是参数的线性函数, 雅可比直接是基函数值, 无需数值微分 / 但为对齐 spec 要求 (数值微分, 前向差分, 步长 1e-6), 仍用数值微分 / 这里用解析式」 | — | 算法选择说明 | 待确认 | **同一段落内三句互斥**（先说无需、再说须按 spec 用数值微分、最后用解析式），且所谓 spec 不在库内 ⇒ CR-42-25。实现用解析式（:734-742）本身正确且更稳 | — | CONFIRMED |
| :759 | `if (wi < 1e-15) continue;` | 1e-15 | 零权重跳过阈 | 待确认 | 零锚（结构护栏方向正确：不累加零权点） | — | CONFIRMED |
| :776-784 | 阻尼加于 `A[1][1], A[2][2], A[11][11], A[12][12]` | — | CD 四项位置 | 结构性不适用 | 索引映射已复算正确（见 :56-58 行） | — | CONFIRMED |
| :788-792, :1013-1017 | `rr_gauss_solve` 失败 ⇒ `break`（日志文案"跳过本轮"） | — | 奇异时的控制流 | 待确认 | **日志与行为相反**：`break` 结束整个精化循环，而 :789 与 :1014 均写"跳过本轮"；且 break 后仍走"最终验收"，只要 `matched_cur/rms_cur` 满足门即 `success=true` 交付未收敛解 ⇒ CR-42-26、CR-42-23 | CONFIRMED |
| :804-811 | `denom < 1e-12 ⇒ denom = 1e-12` 后算 CD 相对变化 | 1e-12 | 相对变化分母下限 | 结构性不适用 | 本仓 CD 线性项量级 ≈ s0/3600 ≈ 1e-4 ⇒ 该下限永不生效（本批复算），属护栏而非科学阈值；域外若生效则方向为"压小 rel ⇒ 少加阻尼（偏绿）" | — | CONFIRMED |
| :855-879 | 前置守卫：空集/长度不一致/`order<2`/`initial_rms<=0`/`s0<=0` ⇒ 回退 | 2 | fail-closed 入参域 | 结构性不适用 | 五道守卫齐备且 `fallback=true` 默认先行（:850-852）⇒ 正面样例；注意 `order<2` 与 :936 强制 `order=3` 的相互关系见 CR-42-28 | — | CONFIRMED |
| :895-903 | `pool_idx.size() < params.min_matched_final` ⇒ 跳过 | 30(`.h`) | 候选池最小星数 | 待确认 | **一名三用**：`min_matched_final`（"最终验收阈"）同时充当候选池下限（:898）、循环内匹配下限（:995）、最终匹配下限（:1097），而名义上的池下限 `min_stars=100` 不参与判定 ⇒ CR-42-30 | 池阈取高 ⇒ 更多帧不做精化（偏红）；取低 ⇒ 小池上拟 20 参数（易过拟合，偏绿） | CONFIRMED |
| :918-921 | 无效投影 ⇒ `W_gaia[j].x/.y = 1e18` | 1e18 | "未测"位置编码 | 待确认 | 用合法有限大数表示未测（与 :96 的 0.0、:579 的 0.0、:605 的 1.0、`ipv_wcs.cpp:856` 的 NaN 共五种写法并存）；现役无害（1e18 永不最近邻），但 `n_invalid`（:914/931）不进结果结构 ⇒ 交付面无法知道丢了几颗 ⇒ CR-42-24 | — | CONFIRMED |
| :936 | `trans_cur.order = TRANS_ORDER; // 强制 order=3` | 3 | 强制升阶 | 待确认 | 与入参域守卫 `order<2 ⇒ skip`（:867）合起来 ⇒ `order==2` 的 TRANS 被无声升到 3 阶拟合，高阶四项 x30/x21/x12/x03 初值来自上游未拟合过的字段 ⇒ CR-42-28（本批未追到该字段在上游的初始化来源） | 若上游留 0 ⇒ 初值无害；若留未初始化 ⇒ 首步残差被污染（偏错）——本批无法判定，需实跑 | PARTIAL |
| :944-970 | `initial_rms_arcsec = baseline_rms;`（入参在算完 tol_init 后被覆盖） | — | L5 回退门基准重算 | 待确认 | 注释自认换基准的理由（:944-947「若用 initial_rms_arcsec 做回退阈值, 几乎所有帧都会触发回退」）⇒ 直接后果是 L5 门失去对外锚 ⇒ **CR-42-22（本批最高危之一）** | 方向：baseline（≥180 星含边缘）通常 > initial（60 亮星 sig）⇒ 1.5× 阈值变大 ⇒ **偏绿** | CONFIRMED |
| :974 | `iter < params.max_iterations` | 10(`.h`) | 主循环迭代上限 | 待确认 | 零锚（`PLATESOLVE.md §9` 只登记 `ipv_sip.cpp` 的 IRLS 15/1e-6，且注明该路径非生产）⇒ CR-42-29；用尽后仅打 info 日志（:1083），结果结构无"未收敛"位 | 上限↑ ⇒ 更可能真收敛（成本↑）；↓ ⇒ 更多未收敛解被接受（偏绿） | CONFIRMED |
| :977-986 | 三档容差 `tol_factor_init/mid/final = 2.0/1.5/1.0`、`tol_floor_* = 1.0/0.5/0.3`、分档边界 `iter <= 2` | 见左 | 容差收紧日程 | 待确认 | 与文件头 L1 注释（`tol = max(2×initial_RMS, 1.0")`）一致 ✓；`iter<=2` 这个分档边界字面量在 `.h` 无对应参数、无任何依据 ⇒ 待确认；三档系数与三个 floor 全部零锚 | floor 方向：保底容差越大 ⇒ 越多配对被放行（偏绿）；本表 1.0″ floor 在 0.96″/px 帧上等于 1 px | CONFIRMED |
| :1034-1044 | `rms_new > prev_rms × (1 + diverge_rms_increase)`、`diverge_count >= diverge_consecutive` | 10%/2(`.h`) | L4 发散判定 | 待确认 | 与注释一致；**`diverge_count` 只在 RMS 分支自增/清零，`match_drop` 分支直接 `diverge=true`**（:1045-1052）⇒ "连续"语义只作用于 RMS 支，与设计句「连续 2 次上升 > 10% 或匹配数降至 50% 以下」一致，但 `diverge_count` 命名易误读为两支共享 | 阈松 ⇒ 继续迭代（成本↑）；阈紧 ⇒ 早退（可能停在未收敛处，偏绿） | CONFIRMED |
| :1045-1049 | `matched.size() < prev_n_matched * diverge_match_drop`；日志 `(int)(ratio*100)/100` | 0.50(`.h`) | 匹配数骤降判定与打印 | 公式导出（判据）＋待确认（打印） | 判据方向正确（下降⇒回退，保守）；打印式对任何 <1 的比率先乘 100 再整型截断再除 100 ⇒ **恒打 0**（`matched 30 < 0×prev=60`）⇒ CR-42-31 | — | CONFIRMED |
| :1068-1071 | `conv_dp = cd_rel_change < converge_dp_max`（1e-9）、`conv_rms = \|rms_new−prev_rms\| < prev_rms×converge_rms_rel`（0.001），两支 **OR** | 1e-9 / 0.001 | 收敛判据 | 待确认 | 注释 :1066 自称「\|Δp\|_max < 1e-9 (用 cd_rel_change 近似, 因 CD 是主要参数)」——把**绝对参数变化**与**CD 相对变化**混为一谈；两支取 OR ⇒ 判据完备性低于任一设计承诺 ⇒ CR-42-23 | 方向钉死：OR ⇒ 任一支满足即判收敛 ⇒ 比 AND **更容易判收敛 ⇒ 偏绿**；`conv_rms` 不约束 16 个高阶 SIP 参数 | CONFIRMED |
| :1081-1084 | `n_iterations = iter + (converged ? 1 : 0)`；未收敛仅 info 日志 | — | 迭代计数与未收敛表达 | 待确认 | `converged` 是局部布尔，**不进 `RobustRefineResult`**（`.h:83-97` 无该位）⇒ 交付面只能靠"n_iterations==10"反推，且 break（奇异/匹配不足）路径同样返回 success=true ⇒ CR-42-23 | — | CONFIRMED |
| :1087-1102 | 最终三道验收（matched 非空、`rms_cur <= final_rms_tolerance×initial`、`n >= min_matched_final`） | 1.5 / 30(`.h`) | L5 防护 | 待确认 | 结构方向正确（超阈⇒回退，保守）；但第一道基准已被 :967 重算 ⇒ CR-42-22；1.5 与 30 本身零锚 ⇒ CR-42-29 | — | CONFIRMED |
| :1110 | `pool_idx[matched_cur[i].u]` 索引反查 | — | 索引空间映射式 | 公式导出 | 与 `ipv_solver.h:111`、`.h:117` 声明一致（u 由 U_pool 索引转 U_full 索引）✓ 调用方 `ipv_solver.cpp:728,747-749` 同步消费 ⇒ 无口径断裂（核过项） | — | CONFIRMED |
| :1113-1114 | `rms_arcsec = rms_cur`、`rms_px = rms_cur / s0` | — | 双口径 RMS 换算 | 公式导出 | 与 `ipv_solver.h:75`、`WcsFitResult` 同口径 ✓；残差确在角秒空间（:8、:478 注），本文件单位口径与实现自洽（对 CR-42-1 的正面佐证：错的是别处的"px"标注） | — | CONFIRMED |

（本文件数值项计 37 行。）

### 5-F findings

### CR-42-22 · 鲁棒精化的"最终回退门"基准在函数内部被重算并覆盖入参，L5 防护失去对外锚
- 轴：R（判据基准被内部替换）＋ C（与设计句 L5 不符）
- 位点：`ipv_robust_refine.cpp:965-969` `// 用 baseline_rms 作为回退阈值基准 (而非 initial_rms_arcsec)` / `initial_rms_arcsec = baseline_rms;` / `rms_cur = baseline_rms;` / `prev_rms = baseline_rms;`，消费点 `:1091 if (rms_cur > initial_rms_arcsec * params.final_rms_tolerance) {`；设计句 `:17` `// L5: final RMS > 1.5×initial 或 matched < 30 → 回退`
- 上位依据：`docs/algorithms/PLATESOLVE.md:243` `| robust_refine_wcs | 调用点 ipv_solver.cpp:692-712；irls_fit_one_step ipv_robust_refine.cpp:661 | 稳健扩增精化（CD 阻尼 + Tukey biweight），失败回退不破坏主解 |`（权威面只承诺"失败回退不破坏主解"）；调用方 `ipv_solver.cpp:710-724` 传入 `initial_rms_arcsec = rep_result.trans.sig`，其注释 :712 写「hi_order_rematch 的 sig (角秒, 68.3% 百分位)」
- 现状：函数把入参 `initial_rms_arcsec` 只用于第一轮容差（:949、:978），随后在同一变量上写入在 `U_pool` 现场重算的 `baseline_rms`（:955），于是 L5 门实际比较"精化后 180 星池 RMS"与"精化前 180 星池 RMS"；原入参（60 亮星上的 68.3% 百分位 σ）被丢弃，而签名与 `.h:109`（"initial_rms_arcsec — 初始 RMS (角秒, 来自 hi_order_rematch)"）都把它当对外锚。
- 差在哪：入参承诺的对外基准被内部值替换，"回退门"变成自洽门；两侧还不同口径（68.3% 百分位 σ vs RMS）；:946-947 承认原基准会让"几乎所有帧触发回退"，于是选择**放宽基准**而不是把两侧换算到同一口径。
- 后果：判据方向钉死——`baseline_rms`（含边缘星的 180 星池）通常 ≥ `initial`（60 亮星），阈值随基准单调变大 ⇒ **偏绿**：精化把解搞坏（例如 CD 被边缘星过拟合拉偏）时，只要不超过自身首轮池 RMS 的 1.5 倍就仍判"成功"，`rob.trans` 随即交给 `extract_wcs_sip`（`ipv_solver.cpp:726-727`）⇒ 交付 WCS 的 CD/SIP 与 `rms_arcsec/rms_px`（:1113-1114）随之进产品；对外主张"精化不会让 RMS 恶化超 1.5 倍"（L5）当前不成立。
- 定级：S2 —— 触发路径逐跳可写（`ipv_solver.cpp:716-724 → ipv_robust_refine.cpp:955/967 → :1091 → :1105-1117 → ipv_solver.cpp:726-727 → extract_wcs_sip`），但"具体哪一帧因此放走了坏解"需实跑比对两值才能定 ⇒ 不判 S1。
- 怎么算修好：① 保留入参原值（改名 `initial_rms_arcsec_from_rematch`），`baseline_rms` 另立局部量；② 两侧换算到同一口径后再比较（68.3% 百分位 σ×√2→RMS，或在同一 U_pool 上重算上游 TRANS 的 RMS 作基准——后者即当前 baseline，但必须同时报告两个数）；③ `RobustRefineResult` 增 `baseline_rms_arcsec` 与 `initial_rms_arcsec_from_caller`，并落日志/manifest；④ 同批改 :17 设计句与 `.h:109` 参数说明，否则注释继续承诺一个不存在的门。
- 置信：CONFIRMED

### CR-42-23 · 收敛判据两支取 OR 且主支语义与注释不同；"未收敛"在结果里没有表示位
- 轴：R（判据不完备 + 未完成冒充完成）＋ C
- 位点：`ipv_robust_refine.cpp:1066-1072` `// |Δp|_max < 1e-9 (用 cd_rel_change 近似, 因 CD 是主要参数)` / `// OR RMS 变化 < 0.1%` / `bool conv_dp = (cd_rel_change < params.converge_dp_max);` / `bool conv_rms = (prev_rms > 0) ? (std::abs(rms_new - prev_rms) < prev_rms * params.converge_rms_rel) : false;` / `if (conv_dp || conv_rms) { … converged = true; break; }`；结果面 `.h:83-97`（`RobustRefineResult` 无 `converged`）；消费点 `ipv_solver.cpp:726 if (rob.success && !rob.fallback) { rep_result.trans = rob.trans; … }`
- 上位依据：`docs/algorithms/PLATESOLVE.md §9` 对"精化收敛"无条款（该册 :185-188 的 0.01″ 属 `iterative_reproject`；:200-203 的 IRLS 15/1e-6 属 `ipv_sip.cpp` 且明写"经 DISP-WCS-003 登记为**非生产**"）⇒ 本模块收敛判据条文面零提及（检索式 `git grep -n "converge_dp_max\|converge_rms_rel" docs`）。
- 现状：被拟合参数 20 个（`N_PARAMS_TOTAL`），判据一只看 CD 四个线性项的相对变化（:802-812 只统计 `xc[1],xc[2],yc[1],yc[2]`），判据二看 RMS 相对变化 <0.1%，两支 OR；RMS 每轮在**当轮新匹配的 matched 集**上重算（:826，matched 由收紧后的 tol 重新产生 :991），故"配对集变小"也计作 RMS 改善。奇异/匹配不足/发散三类 break 之后仍进入最终验收，通过即 `result.success = true`（:1116-1117）。
- 差在哪：① 注释称 `converge_dp_max` 为"参数变化阈值"（`.h:50` 亦写"参数变化收敛阈值"），实现是"CD 相对变化"，对象与量纲都不同；② OR 使 16 个高阶 SIP 参数的变化不参与判定；③ `converged` 是局部布尔，不导出 ⇒ `iter >= max_iterations`（:1082）与 break 路径的 `success` 与真收敛同值。
- 后果：方向钉死：OR 比 AND 更易判收敛 ⇒ **偏绿**；`conv_rms` 首轮即可能满足（prev_rms 与 rms_new 同源于 baseline 集）⇒ 一次 IRLS 步后即可宣布收敛；`success` 是调用方唯一开关（`ipv_solver.cpp:726`），一旦被"未收敛但已放弃"置真，精化后的 TRANS 即成交付 CD/SIP 来源，而 `n_iterations` 只进日志。
- 定级：S2 —— 生产路径可逐跳写（`ipv_solver.cpp:716 → :974/:1068-1072 → :1105-1117 → ipv_solver.cpp:726-727 → extract_wcs_sip → IpvWcsResult`）；未判 S1 因"早停造成的 WCS 误差量级"需实跑一帧逐步比较 SIP 系数变化才能定（见收尾节）。
- 怎么算修好：① 判据一改用全 20 参数变化上界（或把 `.h:50` 与 :1066 的措辞统一为"CD 相对变化"）；② 两支改 AND，或给 `conv_rms` 加"参数亦已稳定"前置；③ `RobustRefineResult` 增 `bool converged` + `int termination_reason`（收敛／迭代用尽／匹配不足／矩阵奇异／发散），并让 `success` 仅在收敛且验收通过时为真——改此必须同步 `ipv_solver.cpp:726` 的开关语义与 manifest 落位面，避免"判据改严⇒成功率降⇒被误判回归"；④ 两个阈值进 §9 登记（见 CR-42-29）。
- 置信：CONFIRMED

### CR-42-24 · 同一模块内"未测"有五种合法数值写法（0.0／0.0／1.0／1e18／NaN），失效面不可区分
- 轴：R（退化返回用合法数值冒充未测）＋ C
- 位点：`ipv_robust_refine.cpp:96-97` `xi_asec = valid ? xi_rad * RR_ASEC_PER_RAD : 0.0;`、`:579` `if (n == 0) return 0.0;`、`:605` `if (n == 0) return 1.0;`、`:920-921` `W_gaia[j].x = 1e18; W_gaia[j].y = 1e18;`；对照 `ipv_wcs.cpp:856` `out.x = std::numeric_limits<double>::quiet_NaN();`
- 上位依据：`AGENTS.md §5`「判据必须非退化」、`ipv_types.h:110-119`（P14-N-10 已在本仓确立"失效面必须可区分并落交付面"的纪律）；条文面无"未测如何编码"的统一条款（检索式 `git grep -n "NaN\|哨兵" docs/contracts/DATA_SEMANTICS.md docs/science`）。
- 现状：背面星投影返回 (0,0)（= 切点，一个完全合法的位置）；无样本 RMS 返回 0.0（= 完美解）；无样本稳健 σ 返回 1.0（= 1 角秒的真实尺度）；调用方把背面星改写成 1e18；`n_invalid`（:914/:931）只进 warn 日志，不进 `RobustRefineResult`。
- 差在哪：同一"未测"在本文件内四种表达、全仓五种，其中三种与"真值恰为该数"不可区分。
- 后果：① 现役不产生错值——`baseline_rms=0.0` 会把 :1091 门变成 `rms_cur > 0`（任何正值回退，偏红、fail-closed），且 :1087 空集检查在前 ⇒ 本批核对后**不**指控错值；② 暴露面是可观测性：交付的 `success` 不知道有多少 Gaia 星因投影被丢（`n_invalid` 不外传），而精化正是用这批星的坐标；③ 复用 `rr_gnomonic_forward` 而漏检 `valid` 的新调用方会拿到位于切点的假星点并被 NN 选中 ⇒ 偏绿。
- 定级：S3
- 怎么算修好：统一 NaN 表未测（与本仓 `ipv_wcs.cpp:856`、`get_last_inliers` 越界写 NaN 一致）；`compute_rms`/`compute_mad_sigma` 无样本改为返回 `bool`＋出参（或 NaN）；`RobustRefineResult` 增 `n_gaia_invalid` 并落日志与交付记录。
- 置信：CONFIRMED

### CR-42-25 · 死常数 `NUMERIC_DIFF_STEP` 与一段三句互斥的算法选择说明，其"依据"指向不在库内的 spec
- 轴：C（注释↔实现）＋ S（依据不可复核）
- 位点：`ipv_robust_refine.cpp:53` `static constexpr double NUMERIC_DIFF_STEP = 1e-6;`、`:20` `// - 数值微分计算雅可比 (前向差分, 步长 1e-6)`、`:653` 同句、`:727-733`「无需数值微分 / 但为对齐 spec 要求 (数值微分, 前向差分, 步长 1e-6), 仍用数值微分 / 这里用解析式 (更稳定, 与 spec 简化方案一致)」、`:4` `// 实现 spec: .trae/specs/robust-augmented-wcs-refine/spec.md`
- 上位依据：跟踪面无任何条款要求数值微分（检索式 `git grep -n "数值微分\|前向差分" docs`）；`AGENTS.md §1` 要求权威可逐层下钻。
- 现状：`git grep -n "NUMERIC_DIFF_STEP" -- lib` 只有定义行 ⇒ 零使用；实现用解析雅可比（:734-742，对参数的线性模型正确且更稳）；:728 所依据的 spec 不在跟踪面。
- 差在哪：7 行内对同一件事给出三个相反说法；理由挂在库外文档上；死常数仍以 `static constexpr` 留在编译单元里，会被常数台账当作"现行值"。
- 后果：维护者若"照注释补齐"会把解析式换回数值微分以求"对齐 spec"（倒退）；`1e-6` 会被当作真实的差分步值登记；审计按 :4 找 spec 整体落空（与 CR-42-19 同形态）。
- 定级：S3
- 怎么算修好：删 `NUMERIC_DIFF_STEP`（或真做数值微分并与解析式对拍留证据）；:20/:653/:727-733 统一为"解析雅可比（模型对参数线性）"；把 spec 内容并入 `docs/algorithms/PLATESOLVE.md` 的本模块小节并把 :4 指针改指该处。
- 置信：CONFIRMED

### CR-42-26 · 日志与行为相反两处：IRLS 奇异写"跳过本轮"实为终止整个精化；发散日志把 0.50 打成 0
- 轴：R（诊断与行为相反）
- 位点：`ipv_robust_refine.cpp:789` `logger->warn("  [robust_refine] IRLS: 正规方程奇异, 跳过本轮")`（随后 `return false`）→ `:1013-1016` `if (!fit_ok) { logger->warnf("  [robust_refine] iter %d: IRLS 拟合失败, 跳过本轮", iter); break; }`；`:1048-1050` `"… matched %zu < %d×prev=%d, 发散, 回退", iter, matched.size(), (int)(params.diverge_match_drop * 100) / 100, prev_n_matched`
- 上位依据：本仓日志规范 `docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md`（存在于跟踪面；本批未逐条比对其条款 ⇒ 引为语境）；同类问题在邻批文件已见（CR-42-14「扩充星数到下一档」而循环只有一档）。
- 现状：`irls_fit_one_step` 返 false ⇒ 调用点 `break` 终止整个循环，两处文案均称"跳过本轮"；发散比率的打印式 `(int)(0.50*100)/100 = 50/100 = 0`（整型除法）⇒ 日志恒打「matched 30 < **0**×prev=60」，一条永远成立的不等式。
- 差在哪：文案承诺继续迭代而代码终止；诊断把阈值系数截断成 0，读者看到的判据与实际判据不同。
- 后果：以日志为准的排障/复盘会认为奇异后仍在迭代（实际已终止并进入验收，且可能 `success=true` 交付未收敛解 —— 见 CR-42-23），并在发散判定上看到一条恒真文字，误判该帧"匹配数远低于阈值仍被判发散"。
- 定级：S3（不改产物字节，证据面失真）
- 怎么算修好：:1014 改「终止精化并进入验收」，:789 改「本轮不可解 ⇒ 返回失败」；:1049 用 `%.2f`/`%.0f%%` 直接打印比率；与 CR-42-14 共用一条日志一致性检查（文案含"下一档/跳过本轮"时对应分支必须真的是 continue/多档循环）。
- 置信：CONFIRMED

### CR-42-27 · 空间一致性层在样本不足时返回"全部合格"权重（潜在 fail-open；现役被上游下限兜住）
- 轴：R
- 位点：`ipv_robust_refine.cpp:475-476` `std::vector<double> weights(n, 1.0); if (n < params.lowe_k_neighbors + 1) return weights;`
- 上位依据：文件头设计句 `:15` `// L3: K=8 空间邻居残差一致性, 可疑点权重 ×0.1`；条文面无"样本不足时 L3 如何表态"的条款（`git grep -n "空间一致性\|K=8" docs` 零命中）。
- 现状：匹配点数 < 9 时返回全 1.0 权重，下游 IRLS 直接相乘（:711）⇒ 与"检验通过"不可区分。
- 差在哪：未执行检验与检验通过同值；方向钉死——权重全 1 意味着不降权任何点，比执行检验更能放行外点 ⇒ **偏绿**。
- 后果：现役不可达（:898 候选池阈与 :995 每轮匹配阈均为 `min_matched_final=30 > 9`）⇒ 不构成本批缺陷；一旦 `min_matched_final` 被下调（该字段一名三用，见 CR-42-30）或本函数被复用，L3 静默失效。
- 定级：S3
- 怎么算修好：早退返回"未检验"信号（空 vector 或 `bool performed` 出参），调用方在 `!performed` 时显式表态并在结果里标注 L3 未执行；补最小用例锁（n=K ⇒ 必须报"未执行"）。
- 置信：CONFIRMED

### CR-42-28 · `order==2` 的 TRANS 被强制升到 3 阶拟合，高阶项初值来源未定义
- 轴：D ＋ R
- 位点：`ipv_robust_refine.cpp:936` `trans_cur.order = TRANS_ORDER;  // 强制 order=3 (鲁棒精化用高阶)`；入参守卫 `:867 if (initial_trans.order < 2) { …跳过 }`；调用方门槛 `ipv_solver.cpp:710 … && rep_result.trans.order > 1)`
- 上位依据：`docs/algorithms/PLATESOLVE.md:243` 未规定精化模型阶与升阶语义；`.h:14` 只写"参数向量 = TRANS 系数 (order=3 时 20 个)"（条件句，未说何时必须 3）。
- 现状：`order==2` 被放行进入，进入后立即改 order=3 并把 `x30/x21/x12/x03`（及 y 侧四项）作为自由参数一并拟合（:682-683 从 `trans_in` 打包全部 10 项）；精化既不校验也不报告"升阶已发生"。
- 差在哪：升阶后这八个系数的初值取自上游 TRANS 字段，而上游在 order=2 时是否写过它们本批未证实。
- 后果：若上游留陈旧/未初始化值，首步残差被污染，而收敛判据宽松（CR-42-23）可能带着欠拟合的高阶项交付 ⇒ 对外表现为"三阶 SIP 项来源不明"。
- 定级：S3（证据缺一角，不上 S2）
- 怎么算修好：`order==2` 时显式把三阶项置 0 并在结果/日志记"升阶发生"；或要求 `initial_trans.order==3` 否则跳过；把选择写进 `PLATESOLVE.md` 本模块小节。
- 置信：PARTIAL（缺上游 `Trans` 高阶字段初始化来源这一角证据）

### CR-42-29 · 本模块 25 个阈值在权威容差册零登记，唯一依据指向不入库的 spec
- 轴：S
- 位点（本文件消费点；默认值位点为 `.h:39-77`）：`:231-233`、`:247`、`:300`、`:347/356-358`、`:476/488/523/529`、`:593/610/703`、`:622-623/778`、`:898/995/1097`、`:974`、`:977-986`、`:1034-1036/1045`、`:1068/1070`、`:1091`
- 上位依据：`docs/algorithms/PLATESOLVE.md §9`（:183-205）逐条登记 0.01″／5.0″／0.002／Huber 1.345／IRLS 15 与 1e-6，并对无推导者明标「**阈值来源 UNJUSTIFIED**，登记为待标定项」；本模块 25 个阈值一条没有（检索式 `git grep -n "tukey_c\|converge_dp_max\|tol_factor\|min_matched_final\|spatial_consistency" docs`）；同册 :243 的本模块条目还带两处漂移锚（写「调用点 ipv_solver.cpp:692-712」而实调用在 :716；写「irls_fit_one_step ipv_robust_refine.cpp:661」而定义在 :662）。
- 现状：文件头 :4 与 `.h:35`（"默认值对齐 spec"）把依据落在 `.trae/specs/…`（未入库）。本批在跟踪面唯一找到的锚是 Tukey `c=4.685`（`docs/algorithms/PHOTOMETRIC_FIT.md:34,304`，含 statsmodels 效率表与适用域"c=4.685 的 95% 效率只对已知尺度严格成立"）与 MAD 常数 1.4826…（公式导出，本批复算一致）。
- 差在哪：一个已进生产链（`ipv_solver.cpp:716` 等三处调用）的稳健拟合层，其全部判据阈值的"依据"不可复核；权威容差册对同链其它模块逐条登记（含 UNJUSTIFIED 标注）的纪律在此断档。
- 后果：AUD-402 的"常数逐个有处置"门在此无法通过（除 4.685 外全落 `待确认`）；"精化经标定"这一对外主张当前无证据；§9 :204-205 的预冻结声明把标定前引用面限定为"待标定项登记"，本模块连登记都缺 ⇒ 治理漏项。
- 定级：S2（证据/治理缺陷，非现役错值）
- 怎么算修好：在 §9 或本模块新增小节逐条登记 25 个阈值（有推导写式，如 `sigma=1.4826·MAD`、`N_long=round(G_short·W/H)`；无推导按现例标 UNJUSTIFIED＋待标定＋影响域）；spec 内容并入跟踪文档并改 :4/`.h:35` 指针；修 :243 两处锚为符号锚；`tukey_c` 条目须带 `PHOTOMETRIC_FIT.md:34` 的适用域，并说明 :703 的 `sigma≥1e-9` 下限是对"尺度估计退化"的兜底。
- 置信：CONFIRMED

### CR-42-30 · `min_stars`（候选池下限）不进入任何判据，`min_matched_final`（最终验收阈）一名三用
- 轴：C
- 位点：`ipv_robust_refine.cpp:308` `// 注意: 不强制填充到 min_stars (小视场星少不强制)`、`:314` `"  [robust_refine] 网格采样完成: 选中 %zu 颗 (上限 %d, 下限 %d)", selected.size(), params.max_stars, params.min_stars`；真正的下界判定 `:898`、`:995`、`:1097`
- 上位依据：`.h:45` `int min_stars = 100; // 候选池下限 (低于此值不强制填充)`（承诺存在池下限语义）、`.h:77` `int min_matched_final = 30; // matched < 30 → 回退`；条文面无三者与池/匹配/验收的关系条款。
- 现状：`params.min_stars` 在本文件仅出现在一条日志（无 if 使用）；候选池是否可用、循环是否继续、结果是否交付三件事都由 `min_matched_final` 一个字段判。
- 差在哪：声明为"候选池下限"的参数实为装饰；声明为"最终验收阈"的参数隐式兼任池阈与逐轮阈；日志把不生效的 100 当"下限"报出。
- 后果：① 调参者改 `min_stars` 期望收紧池规模 ⇒ 完全无效（静默）；② 收紧最终验收（30→60）会连带抬高"是否进入精化"的门槛 ⇒ 联动方向：值越大越容易整帧不精化（偏红），且该联动无任何文档；③ 日志"下限 100"与实际生效的 30 不符 ⇒ 证据面失真。
- 定级：S3
- 怎么算修好：`min_stars` 要么真正参与池判定、要么退役并登记；拆成 `min_pool_stars` / `min_matched_per_iter` / `min_matched_final` 三字段各判各的门；日志只打印生效值。
- 置信：CONFIRMED

---

## 6. 不报的核过项（＋检索式）

| 核过项 | 结论 | 检索式／复算 |
|---|---|---|
| `ipv_types.h:38`（U=像素）与实现一致 | 报的是"角秒"那一侧（CR-42-1），"像素"侧合规 | `git grep -n "output.U\[i\].x" -- lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp` → :1125 注「像素, 不乘 s0」 |
| `ipv_types.h:247` 组合数论证 34220/1140/30 倍 | 数值正确，不报 | 复算 C(60,3)=60·59·58/6=34220 ✓、C(20,3)=1140 ✓、比 30.02 ✓ |
| `ipv_types.h:206` `score = n_inliers/(1+RMS)` | 实现逐字一致，不报 | `git grep -n "result.score" -- lib/algorithms/platesolve/cpp/ipv/src/ipv_ransac.cpp` → :757 |
| `ipv_types.h:159-160`、`146`、`149` 数组维度；`36/100/5/16/256` | 结构性常数，按 §4 不报 | 定义与 `ipv_api.h:44-60` 镜像一致（`sip_a[36]`/`error_msg[256]`） |
| `ipv_types.h:241` 早停 `n_inliers>=4` | 4 = 相似变换未知数个数，属可辨识下界，不报 | `ipv_ransac.cpp:487`；式：s,θ,tx,ty 四未知 |
| `ipv_triangle.h:57-59` 「budget_exhausted=true ⇒ success=false 且 fail_reason 非空」 | 该保证在实现中成立，不报（逆命题另立 CR-42-12） | `ipv_triangle.cpp:550-562`；预算复算 `C(60,3)²=1.171e9 < 2e9` ⇒ 生产不触发 ✓ |
| `ipv_triangle.h:61` `fail_reason[160]` vs `error[256]` | 未见逐字拷贝路径 ⇒ 无截断风险，不报 | `git grep -n "fail_reason" -- lib/algorithms/platesolve`（solver 失败点写自述字面量 `ipv_solver.cpp:519`） |
| `ipv_wcs.h:61` 「converged=false 时 x,y 置 NaN」 | 实现入口即置 NaN 且四条早退保持 ⇒ 本批 focus ④ 正面样例，不报 | `ipv_wcs.cpp:854-859,863,871,877,921` |
| `ipv_wcs.h:80` `tol_px=1e-9`（τ） | 有跟踪面锚与负对照（门 = κ_iter·τ，κ_iter=10 由实测推出），不报 | `git grep -n "G-P1-WCS-RT-ITER" -- docs` → `GATES_AND_TOLERANCES.md:69`、`ASTROMETRY.md:147` |
| `ipv_solver.h:36` 收敛阈 0.01″ 与 `:75` `rms_px=rms_arcsec/s0` | 均已在 §9 逐条登记且口径自洽，不报 | `git grep -n "CONV_THRESH_ARCSEC" -- docs lib` → `PLATESOLVE.md:185-186` |
| `ipv_solver.h:255` `get_last_inliers` 的 `nullptr/max_count<=0 ⇒ -1`、越界 ⇒ NaN | 与声明一致（退化用 NaN），不报 | `ipv_solver.cpp:1742-1747,1763-1765` |
| `ipv_solver.cpp` 缓存 `last_inliers_` 的失效/残留 | 已由专项测试覆盖（避免"上一帧 inlier 残留被当本帧"），不报 | `git grep -n "get_last_inliers" -- eng` → `test_last_inlier_reset.cpp:129-134` |
| `ipv_robust_refine.cpp` 文件头 5 层防护（L1-L5）之**数值**与 `RobustRefineParams` 默认逐条相符（2.0/1.0″、0.75/0.5、K=8/3σ/0.1、2 次/10%/50%、1.5/30） | 一致性成立，不报（无锚问题另立 CR-42-29） | 对照 `ipv_robust_refine.cpp:13-17` 与 `ipv_robust_refine.h:60-77` |
| `ipv_robust_refine.cpp:356-358` Lowe 阈平方化、`:610` MAD 常数、`:50` 206264.806 | 复算无误（`0.75²` 与 `d1/d2<0.75` 等价；`1/Φ⁻¹(0.75)=1.482602218505602`；`180/π·3600=206264.80624709635`），不报 | 本批手算 |
| `ipv_robust_refine.cpp:776-783` 阻尼下标 `{1,2,11,12}` = x10/x01/y10/y01 | 与 `MONOMIAL_BASIS` 排列一致（含 n≥10 可辨识下界），不报 | 本批复算基序 |
| `[P27-DEAD-PARAMS]` 的"dead"档位（n_pivot/ransac_*/s_min/s_max/vote_threshold 等） | 这些字段确被 `ipv_polygon.cpp`/`ipv_ransac.cpp` 引用，但引用点全部落在本仓 reachability 清单判死的符号内 ⇒ 该标注在"生产可达"意义下成立，**不**按"0 引用点"字面报为假（仅"0 引用点"措辞易误读，已并入 CR-42-3 的修法讨论） | `git grep -n "dead_symbols" -- lib/algorithms/platesolve/cpp/ipv/test/ipv_dead_params_manifest.json` → :492-513（polygon_match/adaptive/geometric_vote/extract_consensus/prosac_verify） |
| `1 deg = 3600 arcsec`、`s0` 定义、`RR_PI` 等换算定义常数 | §4 非缺陷清单明示不报 | — |

---

## 7. 本批最高危三条（只挑，不复述）

1. **CR-42-16（＋ CR-42-1、CR-42-9、CR-42-15、CR-42-20）** — 交付 WCS 的像素原点语义在同批文件里被同时写成 `crpix 1-based` 与 `x 0-based`，而关系式 `x = u + crpix` 使二者互斥；第三方（astropy）比对已被迫加 1 桥接，Phase1 与 Phase3 两条实现各持一口。单位/原点口径链是本批最集中、跨文件最多的一类。
2. **CR-42-22 ＋ CR-42-23** — 鲁棒精化（生产链 `ipv_solver.cpp:716` 三处调用）的两道门同时松：L5 回退基准在函数内部被重算替换（失去对外锚、方向偏绿），收敛判据两支取 OR 且主支只盯 4/20 个参数、"未收敛"在结果结构里无表示位。二者叠加 ⇒ 交付 WCS 可能来自一次自认收敛而未真正收敛、且无人能事后分辨的精化。
3. **CR-42-3 ＋ CR-42-29（＋ CR-42-5）** — 治理面失守：登记面断言"IPVSolverParams 全部字段不可由配置影响、值恒为编译期默认"，而 normalize 配置实际注入三键（`module_adapters.cpp:3417-3420`）；同时生产稳健拟合层的 25 个阈值在权威容差册 §9 零登记，代码注释的依据全部落在不入库的 `run/` 与 `.trae/specs/`。

---

## 8. 我读不动、或必须实跑才能定的

| 未定项 | 缺什么 | 该跑／该读什么（由前台决定） |
|---|---|---|
| CR-42-16「哪一侧口径为正」 | 交付 FITS/HiPS 头里 CRPIX1 与实际源像素位置的对拍；本批禁跑 `eng/**` | 跑 `eng/tests/unit/p1wcs/p1wcs_astropy_cross.py` 的现有用例并检查 `semantic_bridge` 字段；或对一个真实帧把 `p1_wcs.json` 的 (crpix, CD, SIP) 交给 astropy 按标准语义（`origin=0/1` 两种）反算源位置比对 |
| CR-42-23「早停带来的 WCS 误差量级」 | 一帧上逐步的 20 参数变化与 RMS/matched 集变化对照 | 在 `irls_fit_one_step` 与主循环临时插桩（打印 `dp` 各分量与 matched 数），跑一帧真实 M42 或合成场（套 `mem_guard.py --max-rss-gb 4`），判 `conv_rms` 是否首轮即真 |
| CR-42-22「baseline 与 hi_order_rematch sig 的实测差」 | 两值同帧对照表 | 从既有 robust_refine 日志抓 `baseline: initial_rms=… U_pool RMS=…` 行（格式串 :958-962）统计若干帧 |
| CR-42-28 上游 `Trans` 高阶字段（x30/x21/x12/x03）在 order=2 时的初值来源 | 需读 `ipv_itertrans.cpp`/`hi_order_rematch`（不在本批清单） | 由该模块批次核 `Trans` 构造与赋值点 |
| CR-42-13「实现是否逐式对应 Valdes 1995」 | 原文（PASP 107, 1119, DOI 10.1086/133670）不可在本环境取得 | 按研究包研读一手文献核描述符定义与投票步骤；同时补 `docs/references/SCIENTIFIC_REFERENCES.md` 的核验状态条目 |
| CR-42-5 / CR-42-29 的标定内容本体 | `run/perf-fix/P4-magiter/REPORT.md`、`run/perf-fix/P27-dead-params/REPORT.md`、`.trae/specs/robust-augmented-wcs-refine/spec.md` 均不在跟踪面（`.gitignore:17`；`git ls-files \| grep -i trae` 零命中） | 需前台把这三份报告落到 `docs/` 或 `artifacts/evidence/`，否则任何"实测标定/对齐 spec"主张都不可复核 |
| `ipv_types.h` 表内两行 PARTIAL：前向 `order<=5` 上限的实质依据、`apx_order<=9` 与 `APX_ORDER` 实际值 | 未逐行读 `ipv_sip.cpp`/`ipv_wcs.cpp` 的取阶代码 | 属 SIP 拟合模块批次（邻批），本批不越界 |
| `ipv_wcs.h:27`「trans_order 固定为 1」是否属实 | 未逐行核 `build_wcs` 尾部对 `trans_order` 的赋值 | 一条 `git grep -n "trans_order" -- lib/algorithms/platesolve/cpp/ipv/src/ipv_wcs.cpp` 即可定；本批取证次数已用于更高危项 |

---

## 9. 派单与清单差异

1. **重点②（首两字段＝版本号＋尺寸的对外约定）在清单五文件内无一对应**：`IterativeReprojectResult`、`SolveInlierCache`、`WcsFitResult`、`SIPCoeffs`、`IPVSolverParams`、`Triangle*`、`WcsIterativeResult`、`StarSelection` 全部是 `namespace ipv` 内的 C++ 结构（含 `std::vector`/`std::string`），不跨 ABI，约定本不适用。真正落在约定范围内的缺口在两处**清单外**载体：`IpvWcsResult`（`ipv_api.h:39-61`，无 `struct_size/abi_version`，而 C 侧按 `sizeof` 整块 memset 写调用方缓冲）与 `get_last_inliers` 的裸缓冲区（行宽 9，无任何尺寸位）。已分别按本批位点（`ipv_types.h:71-72`、`ipv_solver.h:244-255`）立 CR-42-7、CR-42-21，并写明"缺生产触发路径"故未判 S1。
2. **重点③的阈值默认值不在清单内**：本批第 5 项是 `src/ipv_robust_refine.cpp`，其 25 个阈值的**默认值**住在 `include/ipv_robust_refine.h`（非清单文件）。按"以清单为准"处理：只对 `.cpp` 的消费点与判据方向立论，`.h` 仅作为锚上下文引用，未替该头文件出独立处置表（避免越出清单）。
3. **重点①（畸变向量单位两说）本批独立取证，未采用邻批结论**：本批三条独立证据链分别是 `ipv_types.h:38 vs :92 vs :127-128`（CR-42-1）、`ipv_triangle.h:16 vs :73-77` ＋ `a_length` 标注（CR-42-9）、`ipv_wcs.h:12/:19` ＋ `build_wcs` 的 `s*s0/3600` 算术（CR-42-15）；同族第四、五条为 `ipv_solver.h:39/82` 与 `:250` "经 s0 缩放"未标方向。凡与邻批同型者均在 finding 内写"与邻批同型"并给本批位点。
4. **通读顺序与清单序号不同**（通读 3→1→2→4→5，逐段落盘 3→2→4→1→5；先读类型定义才能判单位归属），五项全覆盖、无漏无越。
5. 取证附带发现（属登记面，非本批 finding，供前台并案）：`docs/contracts/API_CONTRACTS.csv` 与 `eng/tools/quality/contracts/fixtures/check_api_contracts/*.csv` 对同一批 ipv 符号给出两种源路径（`lib/plate_solve/...` 与 `lib/algorithms/platesolve/...`），后者为改名前的旧目录；这是 CR-42-19 所指"权威指针不可解析"的同族形态，但载体不在本批清单内，故只在 CR-42-19 的后果里作旁证引用。
6. 本批未发现清单中有多余文件（派单文字与清单一致）；也未发现清单文件被删除或重命名的痕迹（`git grep` 于基线树全部命中）。

## 10. 收工 git status --porcelain 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（与开工一致 ⇒ 本批零仓库写入：无 git 写、无 `git config` 写、未跑构建/ctest/run_checks/任何 `eng/**`、未 import 仓库 Python、未在仓库内落文件。本批全部取证为 Read/Grep/`git grep`/`git ls-files`/`git status`；未建 `独立审计/复算件/CR-42/`（无中间件产物）。）
