# SCI-A → 文档订正建议（交前台合并；本轮**未改** `docs/`）

> 本轮任务域不含 `docs/`（`docs/science/PHOTOMETRY.md` 正由 DOC-404 重写）。
> 下列每条都由 `实验/photometric-magnitude` 内的实测支持，给出复现命令与实测数字，供前台按变更流程合并。
>
> **修订记录（轮次 1 独立审稿后）**：原 C1 是**稻草人**，已**撤回**（见文末"已撤回"节）。
> 现 C1 为审稿后新增的真实发现。

---

## C1（建议订正）σ_floor 的下界在 n ≲ 22 时失效，过裁剪样本反而 PASS

- **判据原文**：`sigma_floor = (1 − 3·1.166/√n)·sigma_fit(白)`。
- **问题**：`1 − 3·1.166/√n` 在 `n < (3·1.166)² ≈ 12.2` 时为负；即使 n 稍大，floor 也迅速趋零。
  于是**把样本裁剪到只剩同质星**（σ_obs 单调下降）时，σ_obs 仍高于 floor ⇒ 判据不判红。
- **实测**（`step7_negatives.json → N5`，复现 `python3 实验/photometric-magnitude/code/step7_negatives.py`）：
  对帧 A 的**实测**通量按 `|r − median(r)| < tol` 逐步裁剪：

| 裁剪 tol | n | σ_obs [mag] | σ_floor | 判定 |
|---|---|---|---|---|
| 不裁剪 | 48 | 0.04534 | 0.00964 | PASS |
| 0.05 | 45 | 0.03803 | 0.00859 | PASS |
| 0.01 | 22 | 0.01976 | 0.00293 | PASS |
| 0.002 | 5 | 0.00356 | **−0.00491** | PASS |

- **后果**：该判据的下界对"把样本做干净"没有判别力。
- **建议**：下界改用 `max(rho_lo, 0)·sigma_fit`，或对 n 设最小样本量硬门槛（n ≥ 20）。

---

## C2（建议订正）σ_psfsys 的「PSF vs 大孔径」估计量在强星云场被结构污染，可达真值 10 倍

- **原文口径**：用 PSF 域通量与独立孔径通量之比的中位绝对偏差作为 σ_psfsys。
- **实测**（`step5_calibration_gate.json → items_measured`，复现 `python3 实验/photometric-magnitude/code/step5_calibration_gate.py`）：

| 帧 | r=10 px 孔径估计 | r=4 px 孔径估计 | 真值口径 |
|---|---|---|---|
| A（HST M16 模板，透明 1.0） | 0.2574 mag | **0.0250** mag | 0.0256 mag |
| B（透明 0.62） | 0.2557 mag | **0.0401** mag | 0.0344 mag |
| C（拥挤扩展星场） | 1.3775 mag | **0.1061** mag | 0.0394 mag |

- **结论**：大孔径把星云结构算进"方法系统误差"（A/B 高估 ~10×）；小孔径（≈2×FWHM）在稀疏场准确，
  在拥挤场仍高估 ~2.7×（保守方向，判据偏松不偏紧）。
- **建议**：明确 σ_psfsys 的孔径口径为"小孔径 + 低背景星子样本"，并登记拥挤场的 2.7× 上偏。

---

## C3（能力边界，建议写进文档）σ_obs 双边界判据对**算术相加的确定性天光结构**不敏感

- **实测**（`step7_negatives.json → N2.arithmetic_gradient_control`）：
  对帧**算术相加** `gx·(x−W/2)`（无散粒噪声）：
  gx = 0 → 0.4 ADU/px 时 σ_obs 0.04534 → 0.04897（**1.08×**），始终 PASS。
- **机理**：PSF 拟合自带局部常数背景，确定性加性结构只通过背景扣除残差进入误差，量级远低于光子噪声。
- **对照（物理口径）**：把天光抬升**经 Poisson 前向重画**（`sky×1/2/4`）：σ_obs 0.0252 → 0.0481 → 0.0734
  （**2.91×**，4× 时判红）。⇒ **判据对散粒噪声敏感、对确定性加性图样不敏感**。
- **后果**：该判据不能认证天光**扣除**质量；能认证的是天光**噪声**是否被正确预算。
- **建议**：把这条写进判据的适用域，并另设天光扣除残差检查。

---

## C4（事实订正）HLSP drz 头部 WCS 与 Gaia DR3 有 ~1.6″ 系统偏移；二轮精化必须是**平移**精化

- **实测**（`step3_forward_vs_photflam.json → wcs_refinement`，复现 `python3 实验/photometric-magnitude/code/step3_forward_vs_photflam.py`）：
  - F657N：shift = (23.10, 33.32) px = **1.622″**，精化后 83/88 在 4 px 内匹配；
  - F673N：shift = (23.12, 33.52) px = **1.629″**，80/88 匹配。
- **对照**：testdata 真实帧（FLI M42）的头部 WCS 残余仅 `(−0.263, −0.319)` px = **0.0068″**，827/827 匹配
  ⇒ "二轮精化"的必要性取决于上游 WCS 质量，不是流程固定开销。
- **建议**：把"二轮精化需覆盖 ~2″ 量级平移"写成明确的设计约束与失败模式。

---

## C5（数值，供 DOC-404 参考）XP 合成通量的绝对刻度在**窄带**上与 HST PHOTFLAM 差 0.01–0.26 mag（中位）

- **实测**（`step3_forward_vs_photflam.json → per_filter`，三滤镜、Gaia DR3 XP × SVO HST/WFC3_UVIS2 总系统透过率）：

| 滤镜 | n | 中位 Δmag | bootstrap σ | MAD | 色项斜率 | 干净子样中位/MAD |
|---|---|---|---|---|---|---|
| F657N | 42 | −0.147 | 0.113 | 0.491 | 0.464 | −0.014 / 0.255 (n=12) |
| F673N | 29 | −0.263 | 0.130 | 0.645 | 0.765 | −0.224 / 0.104 (n=12) |
| F502N | 75 | −0.080 | 0.032 | 0.213 | 0.565 | −0.007 / 0.049 (n=18) |

  同星跨滤镜（n=19）残差之差中位 0.117 mag、MAD 0.326 mag。
  透过率曲线独立校验：由 PHOTFLAM 反推有效面积 44425 / 45323 cm²（几何面积 38453 cm² 的 1.155 / 1.179 倍，
  差异来自望远镜/仪器的**遮挡与溢出**：几何面积按 π·120²·0.85 估算，未含 OTA 遮挡与 UVIS 芯片间缝，
  故有效面积大于几何估算属预期），枢轴波长与头部 PHOTPLAM 相对差 4e-6 / 7e-6。
- **对 06.md 的 0.0442 mag 参考侧未解释项**：本测量说明**窄带**（等效宽度 29–39 nm）上 XP 侧的
  绝对刻度不确定度是 0.05–0.65 mag 量级（取决于星云污染与星等范围）。
- **诚实边界**：宽带（Baader R，~144 nm）的同类误差**未直接测量**，不能据此断言宽带误差也这么大。

---

## C6（建议订正）"由标定系数反解增益/口径/曝光"的闭合式必须删除或降级

- **若 `docs/science/PHOTOMETRY.md` 仍保留形如 `k = g·h·c·1e9/(A·t)` 的闭合式**：本实验给出反例。
- **实测**（`step6_apply_and_units.json → unit_elimination.degenerate_family`，复现 `python3 实验/photometric-magnitude/code/step6_apply_and_units.py`）：

| 参数组 | A·t/g | 中位 F [ADU] | σ_obs [mag] |
|---|---|---|---|
| (A,t,g) = (1, 300, 1.333) | 225.06 | 4831.2 | 0.01891 |
| (A/2, 2t, g) | 225.06 | 4865.1 | 0.01812 |
| (A, 2t, 2g) | 225.06 | 4840.2 | 0.01344 |

- 前两组**统计上不可分**（期望图像与噪声都相同）⇒ (A, t) 是 1 参数退化；第三组均值相同但噪声不同，
  PTC 斜率可给 g，但 (A, t) 仍不可分 ⇒ 由 k_photo 反解 (A,t,g) 是**欠定的 2 参数族**。
- 零点平移不变量同时验证：F_instr 同乘 7.3 ⇒ Δlocation = 0.8633228601（期望 0.8633228601），
  k_photo 比 = 0.1369863014（期望 0.1369863014），Δσ_residual = 0。
- **建议**：该式只保留为量纲说明并明确标注"**不用于反解**"，或直接删除。

---

## C7（已核验，无需订正）"中位通量代表整帧"的 σ_ceiling 合成口径在当前样本跨度下足够

- **实测**（`step1_analytic.json → flux_distribution_composition`）：星等窗 14–19（通量跨度 100×）时，
  逐星 MC 合成与中位通量合成的比 = **0.9886**，两种口径都 PASS。
- **结论**：该近似在当前生产样本跨度下**不构成缺陷**，无需订正。

---

## C8（已核验，无需订正）Horne 解析式与"自由背景"精确 Fisher 式的差别不是文档错误

- `run/RELEASE-02/parallel/06.md` §2.2（第 100 行）给的是 **Horne 1986 最优提取结构**
  `σ_F/F = sqrt(1/(g·F) + N_eff·σ_pix²/F²)`，它假定**背景已知、无自由背景参数**；
  `06.md` §2.1 的 `σ_fit = 0.0140` 是生产拟合器 **200 次重复拟合的实测值**，不是该解析式的取值。
- 本实验的 `psf_fit_variance_exact` 多了一个**自由常数背景**参数，因此多出 H01 交叉项，方差更大。
  两者之差就是"自由背景简并"的代价（实测 1.13–2.15×），**不是文档错误**。
- **结论**：无需订正。仅建议在文档里补一句说明"解析式与生产实测值的口径差异来自自由背景项"。

---

## 已撤回

### ~~C1（撤回）06.md 的 σ_fit 一阶展开低估方差最多 2.15×~~

- **撤回原因**：轮次 1 独立审稿指出这是**稻草人**——被订正的"原文口径
  `Var ≈ σ_pix²·N_eff + F·w/g`（`w = ΣP³/ΣP²`）"**不存在于任何文档**：
  `docs/plugins/algorithms_phase1/06_photometry.md` 只有 §1–§8（无 §2.1/§3.1），
  `run/RELEASE-02/parallel/06.md` §2.2 给的是 Horne 形式，全仓 grep `P**3` 无命中。
- **核实**：作者已亲自复核，确认审稿意见成立。该条订正**撤回**；实测数字（1.13–2.15×）
  改记为 C8 的"自由背景简并代价"，不作为文档缺陷。
