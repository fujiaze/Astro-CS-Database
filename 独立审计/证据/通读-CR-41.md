# 通读批次 CR-41 · 声明头三方对账审计

- 基线 HEAD：`c8f64e9a`
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 派单：工包-AUDIT-06 / `DISPATCH-CODE-READ-PREAMBLE.md` §0–§5 全条适用
- 本批重点：对外类型与句柄声明面 = 三方对账批（声明面 vs 两份合同面 vs 实现）
- 纪律：只读；零 git 写；不构建、不 ctest、不 run_checks、不跑 `eng/**`；不 import 仓库 Python

## 独占清单（6 个声明头，不越出、不漏）

lib/algorithms/drizzle/hips/include/astrocs/hips/publish.h —— 已读：是（表 28 项）
lib/algorithms/drizzle/hips/include/astrocs/hips/types.h —— 已读：是（表 36 项）
lib/algorithms/drizzle/include/astrocs/drizzle/types.h —— 已读：是（表 27 项）
lib/algorithms/platesolve/cpp/ipv/include/ipv_robust_refine.h —— 已读：是（表 36 项）
lib/algorithms/platesolve/cpp/ipv/include/ipv_select.h —— 已读：是（表 33 项）
lib/algorithms/platesolve/cpp/ipv/include/ipv_sip.h —— 已读：是（表 11 项）

<!-- PROGRESS: 1/6 -->

## 开工 git status --porcelain 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（与前言 §1 要求的基线一致；收工再贴一次。）

---

# 1. lib/algorithms/drizzle/hips/include/astrocs/hips/publish.h

读法：整个文件 149 行从头读完（含头注释 1–32、宏 42–47、枚举 49–71、四原语声明 73–105、故障注入面 107–122、编译期断言 128–147）。
对账三面：`lib/include/astrocs/io/aio_abi_v1.h`（AIO-001 状态码数值冻结）、`docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md`（ACTIVE_NORMATIVE 发布流水线合同）、实现 `lib/algorithms/drizzle/hips/src/aio_publish.cpp` ＋生产调用方 `lib/algorithms/drizzle/hips/src/module_entry.cpp:861–1010`；第三份平行声明 `lib/infrastructure/aio/v6/include/astro/aio/v6_atomic_publish.h:12–45,111–118`。

## 1.1 §2.5 逐数值处置表（28 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| publish.h:42 | `ASTROCS_HIPS_PUBLISH_ABI_VERSION` | `1u` | 本 ABI 面版本标记 | 结构性不适用 | 非科学量：接口版本标识（无科学语义）；但全仓零消费者 ⇒ 见 CR-41-6 | — | CONFIRMED |
| publish.h:47 | `ASTROCS_HIPS_STAGE_BASENAME` | `".hips_staging.tmp"` | staging 兄弟目录后缀词根 | 结构性不适用 | 非科学量：路径词法常量；实现逐字使用（aio_publish.cpp:88–89 `%s/.%s%s`）⇒ 与声明 `<parent>/.<name>.hips_staging.tmp`（:17、:75）一致，无分叉 | — | CONFIRMED |
| publish.h:53 | `AIO_PUBLISH_OK` | `0` | 成功 | 结构性不适用 | 非科学量：状态码编码；与 aio_abi_v1.h:47 `AIO_OK=0` 同值，本头 :130 断言钉住 | — | CONFIRMED |
| publish.h:54 | `AIO_PUBLISH_ERR_PARAM` | `1` | 参数非法（NULL/空/路径溢出） | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:48；断言 :130 覆盖 | — | CONFIRMED |
| publish.h:55 | `AIO_PUBLISH_ERR_ABI_MISMATCH` | `2` | （声明标"保留，数值对齐"） | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:49；**断言未覆盖**，且唯一实现从不返回（aio_publish.cpp 全文） ⇒ CR-41-4/CR-41-5 | — | CONFIRMED |
| publish.h:56 | `AIO_PUBLISH_ERR_NOMEM` | `3` | 分配失败 | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:50；断言未覆盖；实现零返回点 ⇒ CR-41-5 | — | CONFIRMED |
| publish.h:57 | `AIO_PUBLISH_ERR_IO` | `4` | 建目录/打开/读失败 | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:51；断言 :131 覆盖；实现返回点 aio_publish.cpp:148/159/166/186/192/197/229 | — | CONFIRMED |
| publish.h:58 | `AIO_PUBLISH_ERR_UNSUPPORTED` | `5` | 不支持 | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:52；断言未覆盖；实现零返回点 ⇒ CR-41-5 | — | CONFIRMED |
| publish.h:59 | `AIO_PUBLISH_ERR_CANCELLED` | `6` | （声明标"保留；取消由调用方处置"） | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:53；断言未覆盖；实现零返回点（取消由 module_entry.cpp:951/977 走 ACS_ERR_CANCELLED） ⇒ CR-41-5 | — | CONFIRMED |
| publish.h:60 | `AIO_PUBLISH_ERR_STATE` | `7` | 状态非法（stage 缺失/目标非空） | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:54；断言 :131 覆盖；语义与 IO-003 §4.2 不同规则 ⇒ CR-41-3 | — | CONFIRMED |
| publish.h:61 | `AIO_PUBLISH_ERR_TRUNCATED` | `8` | （声明标"保留，数值对齐"） | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:55；断言 :132 覆盖 8；实现零返回点 ⇒ CR-41-5 | — | CONFIRMED |
| publish.h:62 | `AIO_PUBLISH_ERR_BAD_HEADER` | `9` | 头非法 | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:56；断言未覆盖；实现零返回点 ⇒ CR-41-5 | — | CONFIRMED |
| publish.h:63 | `AIO_PUBLISH_ERR_MISMATCH` | `10` | 不匹配 | 结构性不适用 | 同上（aio_abi_v1.h:57），无返回点 | — | CONFIRMED |
| publish.h:64 | `AIO_PUBLISH_ERR_CHECKSUM` | `11` | 校验和错 | 结构性不适用 | 同上（aio_abi_v1.h:58），无返回点；注意 IO-003 §4.4 要求 DATASUM 校验，本面既不解算也不报此码 ⇒ CR-41-2 | — | CONFIRMED |
| publish.h:65 | `AIO_PUBLISH_ERR_NANINF` | `12` | 非数 | 结构性不适用 | 同上（aio_abi_v1.h:59），无返回点 | — | CONFIRMED |
| publish.h:66 | `AIO_PUBLISH_ERR_DISKFULL` | `13` | fsync 落盘 ENOSPC/EDQUOT | 结构性不适用 | 非科学量：状态码；aio_abi_v1.h:60；断言 :134 覆盖；实现映射 aio_publish.cpp:120–121/135–136（errno==ENOSPC\|\|EDQUOT）；memory.md:263–265 登记 ENOSPC 在 fsync 遍历暴露 | — | CONFIRMED |
| publish.h:67 | `AIO_PUBLISH_ERR_DECL_INVALID` | `14` | 声明 digest 非法 | 结构性不适用 | 同上（aio_abi_v1.h:62，AIO 专属扩展）；断言未覆盖；本头四原语无 digest 入参 ⇒ 该码在此面永不可达 | — | CONFIRMED |
| publish.h:68 | `AIO_PUBLISH_ERR_HASH_MISMATCH` | `15` | 重算 sha256 与声明不符 | 结构性不适用 | 同上（aio_abi_v1.h:63）；断言未覆盖；本面无 sha256 步骤 ⇒ 不可达，见 CR-41-2 | — | CONFIRMED |
| publish.h:69 | `AIO_PUBLISH_ERR_INTERNAL` | `70` | 未分类内部错误 | 结构性不适用 | 状态码；aio_abi_v1.h:64；断言 :135 覆盖；实现零返回点（无 catch-all 分支） | — | CONFIRMED |
| publish.h:70 | `AIO_PUBLISH_STATUS_COUNT` | `71` | 哨兵"该值及以外均非法" | 结构性不适用 | 枚举基数；aio_abi_v1.h:65 同值但注释多一句"0..15 与 70 有效"；断言 :136/:145 覆盖；全仓无运行期使用 ⇒ CR-41-5 | — | CONFIRMED |
| publish.h:49–51 | 注释值域声明 | `0..15 与 70 全域一致 … _Static_assert 编译期对齐证明` | 声明与 AIO-001 数值全域等价 | 待确认 | 条文面：aio_abi_v1.h:45–66 与 :142–164 逐值断言齐备；本头 :130–137 只钉 0,1,4,7,8,13,70,71（8/17） | 保守方向：以 aio_abi_v1.h 为唯一数值源、本头改为引用其常量或补齐 17 值断言；影响范围=发布面错误码可读性与未来分叉风险（当前值逐值相同，无产物错值） | CONFIRMED |
| publish.h:79 | 注释推荐缓冲 | `1024`（"推荐 1024"） | stage_path_buf 调用方缓冲建议值 | 待确认 | 实现把它当**硬上限**用：aio_publish.cpp:38 `#define PUBLISH_PATH_MAX 1024`，:85–92 在 1024 内部缓冲上 snprintf，`n >= cap` → PARAM | 保守方向：声明应改述"内部上限 1024，超出即 PARAM，与调用方缓冲无关"；影响范围=深输出路径（Windows 长路径/run 目录嵌套）下发布直接失败为 ACS_ERR_IO，调用方放大缓冲无救济 | CONFIRMED |
| publish.h:116–117, 121 | 注入延时 | `600` ms（`p1_stage_slow_write`） | kill 中断用例驻留窗口 | 结构性不适用 | 非科学量：测试时序锚点，选择依据是"够长到被 kill 观察到"；实现落 module_entry.cpp:880–886 `Sleep(600)/hips_msleep(600)`；memory.md:277 记 asan 下 250ms 轮询不确定 ⇒ 延时本身不是断言 | — | CONFIRMED |
| publish.h:8 | V7 事故 tmpfs 容量 | `24G` | RAII 自愈立据的历史事实 | 待确认 | 跟踪面 `git grep tmpfs` 无该数值登记；`git grep "19.94\|make_tmp_dir\|status=107"` 于 HEAD 命中仅 publish.h 自身＋README:218（README 也只引用编号不引用数值）；`问题扫描/**/V7.md` 已在 9a2b5d11 删除，删除前 blob 内 grep 这些数值零命中 | 保守方向：把数值指回可复核登记件（run/ 不可，须跟踪面）；影响范围=仅可追溯性，不改任何产物字节 | PARTIAL（缺 V7 原件在 HEAD 的落点） |
| publish.h:8 | V7 事故已用量 | `19.94G` | 同上 | 待确认 | 同上（HEAD 与删除前 blob 均无该数值锚点） | 同上 | PARTIAL |
| publish.h:9 | cfitsio 错误码 | `107` | "假败"表征码 | 待确认 | HEAD `git grep "status=107"` 零命中；cfitsio 107 语义（读写操作错）由第三方库定义，本仓 `docs/references/` 未登记该码 | 保守方向：注明 cfitsio 版本与 107 的官方语义出处；影响范围=文档可追溯性 | PARTIAL |
| publish.h:130–137 / 139–146 | 断言字面量集 | `0,1,4,7,8` ＋ `13,70,71` | 编译期对齐证明覆盖面 | 待确认 | 被钉 8 值 vs 声明 17 值；未钉 `2,3,5,6,9,10,11,12,14,15`；本头不 include aio_abi_v1.h ⇒ 无任何 TU 跨枚举比对 | 保守方向：include AIO-001 头并逐值断言等式；影响范围=潜伏分叉（今日两侧值相同） | CONFIRMED |
| publish.h:26 | 代码定位引用 | `185-186` | 指 `aio_hips_writer.cpp` remove+create 行号 | 结构性不适用 | 非数值语义：文件行定位符，随被引文件漂移；已核 aio_hips_writer.cpp 仍存在 remove+create 直写并登记于 README:198–203（DISP-HIPS-004 维持） | — | CONFIRMED |

（本文件无浮点常数、无容差、无单位换算、无科学量：头注释 :25–27 自证 `scientific_change=false`，与实现面 aio_publish.cpp:22–24 一致。）

## 1.2 findings

### CR-41-1 · 声明面写"文件计数校验"，API 与生产调用都做不到也不做这个校验
- 轴：D（主）＋C
- 位点：publish.h:20 `3. tree_fsync     递归 fsync stage 树 (文件+目录, 后序) + 文件计数校验`；publish.h:96–97 `int aio_publish_tree_fsync_v1(const char* stage_path_utf8, uint64_t* n_files_out, uint64_t* total_bytes_out);`
- 上位依据：`docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md` §4 第 4–6 步（原文："4. **fitsverify**：`.fits` 科学平面 tile（`NpixN.fits`）必须通过结构 + DATASUM 校验……失败 → `PublishError`，无成功对象"、"5. **sha256**：每个文件内容 sha256/64hex 记录"、"6. **原子 rename**"）；§7 验收映射行"临时写、关闭、fitsverify、SHA256、原子 rename、最终完成 manifest | §4 + `TestAtomicPublishPipeline`（spy 证据）"。
- 现状：`aio_publish_tree_fsync_v1` 的两个计数是**纯输出**（`n_files_out/total_bytes_out`），签名里没有任何"期望值"入参，实现只做累加（aio_publish.cpp:111 `if (n_files) (*n_files)++;`、:116 `if (total_bytes) *total_bytes += sz;`），无比较、无期望值来源。生产调用点把它调成丢弃计数：module_entry.cpp:994 `publish_rc = aio_publish_tree_fsync_v1(stage_path, NULL, NULL);`。
- 差在哪：所谓"校验"在声明面是一个不存在的判据——既不与 `rows.n_tiles` 比对（写循环计数 n_written 只进了输出 manifest，module_entry.cpp:1052/1086，发布门从不读它），也不是 IO-003 §4.4/4.5 的内容校验；API 结构上无法表达"校验"。
- 后果：publish.h:23 的对外主张"`out_dir` 根要么保持旧态要么出现完整新树（全有或全无）"少了一层它自称有的完整性检查；发布门实际只剩"每个文件 fsync 成功"。fail-open 方向钉死：若补上期望计数并比对，`实际计数 < 期望` 只会让判定变**红**（多一条拒绝路径），因此当前不校验是**偏绿**（少拒绝路径），不是偏红。
- 定级：S2（触发路径可逐跳写出：module_entry.cpp:866 → :994 → aio_publish.cpp:189 → :98 计数丢弃；但不产生错误字节，故不升 S1）
- 怎么算修好：二选一并同批改：①把 publish.h:20 的"+ 文件计数校验"改为"文件计数（仅统计，不校验）"，并同步 README:188–189 的"正向发布（staging→fsync→原子 promote）"叙述；②真要校验则签名加 `uint64_t expected_files`（0=不校验），module_entry.cpp:994 传入由 `rows.n_tiles` 推导的期望文件数，同时补 publish_atomic_test.c 的"少一个 tile → 拒绝发布"负例（防改对反而判红：现正向例 NULL,NULL 必须一起改）。
- 置信：CONFIRMED（读遍实现四原语与生产调用点，无比较分支）

### CR-41-2 · 本头自称对齐 IO-003 §4 的"完成标记"，但声明面里根本没有完成标记与内容校验，而同仓另一发布面已实现该标记
- 轴：C（主）＋D
- 位点：publish.h:4–5 `对齐 IO-003 发布流水线 (docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md §4: 临时写 → 关闭/fsync → 校验 → 原子 rename → 完成标记) 的 C ABI 化最小面`；publish.h:16–23 发布状态机四步（`1. stage_create … 2. (writer 全量写 stage…) … 3. tree_fsync … 4. promote`，无第五步）；publish.h:73 `/* 1) 建 staging 目录` 起的"原语 v1 (4 函数)"
- 上位依据：IO-003 头部 `doc_status: ACTIVE_NORMATIVE`；§4.7 原文"**完成 manifest**：最后原子写 `manifest.json`（`status=COMPLETE`；= 唯一完成标记）。成功对象 = 内容 + COMPLETE manifest 齐全"；§5 给出 `manifest_schema: "astrocs.hips-output-manifest/v1"` 的 `tree[]{path,size,sha256}` 与 `tree_hash`；§1 原文"IO-002 读端/跨 Phase 消费只接受本接口发布的完整产物（DATA-002 R-DISK-ONLY）"。
- 现状：四原语里没有任何写完成清单/算 sha256/解算 DATASUM 的入口；实现 aio_publish.cpp:143–235 全文只有 mkdir/fsync/rename/unlink 语义，`n_files/total_bytes` 之外不产出任何指纹。模块侧登记把"完成标记"换了定义：memory.md:260–262 "writer manifest.json（:1086-1128，**无 COMPLETE 字**）作为树内完成标记随树原子出现；树级 COMPLETE manifest + sha256 tree_hash 归 IO-003 Python 层"。而同仓 P2 发布面按合同原义实现：module_adapters.cpp:12133–12134 "**最后**落完成清单 manifest.json —— 它是唯一的'完成'标记, 没有它消费者(aio_hips_open / aio_hips_verify_product_set)一律拒绝"。
- 差在哪：同名文件 `manifest.json` 在本头面里是 HiPS 1.4 内容清单（README:90：format_version/hips_version="1.4"/nside/…，无 status 字段），在 IO-003 §4.7/P2 面里是 `status=COMPLETE` 的完成标记；本头引用 §4 时把两者混为一谈，宣称"对齐……→ 完成标记"却声明零个完成标记入口，也没有一行"§4.4/4.5/4.7 在本 ABI 面不适用/延后"的显式排除（只有 memory.md/README 的模块级注记，规范面 IO-003 §7 验收映射未随之修订）。
- 后果：经 `astrocs.p1.hips_writer` 事务面发布的 P1 HiPS 树不满足 IO-003"成功对象"定义，却由 publish.h:4–5 与 README:188–189 对外呈现为"原子发布对齐 IO-003"；跨阶段消费若按 IO-003 §1（只接受完成标记齐全产物）判定，这些产品不具消费资格；反过来若消费者只按 manifest.json 存在性放行，则合同"唯一完成标记"判据被削弱为存在性检查。两条路都说明"对账"不成立。
- 定级：S2（缺"哪一侧判据在 CI 上真的翻案"的运行时证据；触发路径本身可写：module_entry.cpp:866/994/999 → promote → out_dir 树内仅 writer manifest.json）
- 怎么算修好：①publish.h:4–5 改为精确引用（列明本面只覆盖 §4.1/4.3/4.6，§4.4 fitsverify、§4.5 sha256、§4.7 完成 manifest 明确"不在此 ABI 面"），②IO-003 §7 验收映射同步加一行"P1 模块事务面覆盖范围"或按变更流程把 §4.7 标为分层实施，③若目标是真补完成标记：新增第 5 原语 `aio_publish_write_complete_manifest_v1`（status=COMPLETE＋tree[]{path,size,sha256}＋tree_hash，复用 AIO-001 `aio_content_hash_*`），并与 P2 面（module_adapters.cpp:12150/12133）共用同一 manifest schema 校验；补 publish_atomic_test.c 的"promote 后完成标记存在且 status=COMPLETE"正例与"标记缺失 ⇒ 消费者拒收"负例。
- 置信：PARTIAL（缺 IO-002 读端/`aio_hips_verify_product_set` 对"无 status 字段 manifest.json"到底放不放行的实跑证据；本批不跑测试）

### CR-41-3 · promote 的"非空即拒"把 IO-003 §4.2 要求自动清残重发的残留也一并拒了，登记面却称它是 §4.2 的树级对应
- 轴：C
- 位点：publish.h:100–101 `out_dir 已存在且非空目录/为普通文件 → STATE (唯一目标: 拒绝覆盖非空)`；实现 aio_publish.cpp:220 `if (ne != 0) return AIO_PUBLISH_ERR_STATE;          /* 唯一目标: 拒非空 */`
- 上位依据：IO-003 §4.2 原文"**覆盖清理**：默认不覆盖——目标为**成功对象**（含 COMPLETE manifest）且 `overwrite=False` → `PublishError`。中断/cancel 残留（目录存在但无 COMPLETE manifest）= **非成功对象**（DATA-003 语义）→ **自动清残重发**；成功对象仅在显式 `overwrite=True` 时清除后重建。"；§6 错误语义表行"目标为成功对象且 overwrite=0 → `PublishError`（默认不覆盖）"。
- 现状：实现不判定"成功对象"（本面无完成标记可比，见 CR-41-2），只用 `dir_is_nonempty` 作唯一判据；登记面 README:213–216 "①非空目标拒绝语义（out_dir 已含完整发布树（writer manifest.json）或任意内容 → STATE 拒绝，调用方先清理；空目录/不存在 → 原子替换）——IO-003 §4.2 的'成功对象默认不覆盖'树级对应"，memory.md:248–249 进一步把"旧残树合法拒发"写进测试期望。
- 差在哪：合同按"有无 COMPLETE 标记"二分（成功对象拒、残留清后重发），代码按"目录空/非空"二分；§4.2 明确要求自动恢复的那一支在这里变成永久失败，而登记把它说成与合同等价。
- 后果：同一 `out_dir` 上任何非本面留下的既有内容（legacy 直写面残留——README:198–203 明确 aio_hips_writer 的 remove+create 直写语义未改、DISP-HIPS-004 维持登记；或上一次非原子运行残留）都会让后续事务固定失败于 module_entry.cpp:999→1001→1027 的 `publish_fail` ⇒ `ACS_ERR_IO`（HIPS_ECODE_PUBLISH_*），且合同规定的自动清残不会发生；对外"默认不覆盖但可恢复"的主张与"发布面可自愈"（publish.h:19 声称 RAII 收口）不一致。
- 定级：S2（可写触发路径但不产生错值；且真实生产是否混用 legacy 直写同 out_dir 需实跑确认）
- 怎么算修好：先由负责人裁决落点：①按合同补判据——promote 前读 `out_dir/manifest.json` 的 `status`，等于 `COMPLETE` 才拒（STATE），否则清残重发（需与 CR-41-2 的完成标记定义一起改，否则任何 P1 树都不是成功对象⇒永远走清残支，反而更危险）；②或在 publish.h:100–101 明确写"本面以目录非空为拒绝判据，比 IO-003 §4.2 更严，残留不自愈，调用方须先清理"，并把 IO-003 §4.2/§6 走变更流程加"P1 模块事务面"注记。两改都必须同步 README:213–216 与 memory.md:248–249，否则改对反而与登记面判红。
- 置信：CONFIRMED（两判据条文与实现分支均逐字读到）

### CR-41-4 · "全域一致 + 编译期对齐证明"是过度声明：17 个值只钉 8 个，且同一数值域已有三份各自为政的副本
- 轴：C
- 位点：publish.h:49–51 `0..15 与 70 全域一致于 lib/include/astrocs/io/aio_abi_v1.h aio_status (AIO-001 数值冻结; _Static_assert 编译期对齐证明)`；断言本体 publish.h:130–137（C++）/:139–146（C11）
- 上位依据：AIO-001 冻结面 `aio_abi_v1.h:45–66`（`状态码 (v1 数值冻结)` 枚举）与 :138–164（该头自身断言 0–15/70/71 全部 17 值，并声明"0..13 必须与 acs_fio_status 全域逐值一致"）。
- 现状：publish.h 的断言只覆盖 `0,1,4,7,8,13,70,71`；`2,3,5,6,9,10,11,12,14,15` 无任何编译期等式；且本头**不 include** `aio_abi_v1.h`，两组枚举名不同名（`AIO_PUBLISH_*` vs `AIO_*`），因此全仓不存在任何一次跨枚举值比较。第三份副本 `v6_atomic_publish.h:26–45`（`enum class PublishStatus`）同样自行写死 0–15/70/71，其 :12–13 注释重复同一句"状态码 0..15/70/71 数值与 … 对齐（编译期 static_assert）"，而它的断言 :111–118 只钉 7 个值（连 `kErrTruncated=8` 都没钉）。
- 差在哪：三份声明各自把同一数值域写成字面量、各自声称"编译期已证明对齐"，实际证明覆盖面分别是 8/17、7/17，且没有一个断言是跨副本的等式——注释把"存在部分断言"说成"全域对齐证明"。
- 后果：任一侧改动（例如 AIO-001 升 v2 把 NOMEM 挪位）不会被任何编译期检查抓住，发布面与诊断码映射（如 module_entry.cpp 把 publish rc 折成 ACS_ERR_IO 的粗映射）会静默错位；对外"v1 数值冻结"的可信度依赖一条并不存在的证明。当前三副本值逐值相同 ⇒ 无产物错值。
- 定级：S3（潜伏一致性风险，无现值错误、无触发路径）
- 怎么算修好：让 publish.h 与 v6 头 `#include "astrocs/io/aio_abi_v1.h"`，并把断言改成逐值等式（`AIO_PUBLISH_ERR_NOMEM == AIO_ERR_NOMEM` … 全 17 项），或干脆用别名（`AIO_PUBLISH_ERR_NOMEM = AIO_ERR_NOMEM`）消除副本；v6 的 `PublishStatus` 同批处理；注释 :49–51/:12–13 措辞与实际覆盖面一致后再保留。
- 置信：CONFIRMED（三份头的断言项集逐条数过）

### CR-41-5 · 状态码域里 10 个值在本模块实现中永不可达，哨兵"该值及以外均非法"无 enforcement 且措辞与 AIO-001 副本已分叉
- 轴：C（主）＋D
- 位点：publish.h:70 `AIO_PUBLISH_STATUS_COUNT = 71     /* 哨兵: 该值及以外均非法 */`
- 上位依据：`aio_abi_v1.h:65` 同值注释为"哨兵: **等于该值及以外均非法 (0..15 与 70 有效)**"——本头副本丢了括号里的有效域限定；`aio_abi_v1.h:72` 把 `AIO_STATUS_COUNT` 放进 `aio_abi_info_v1.status_count` 作为握手暴露量，本头无任何等价暴露。
- 现状：`AIO_PUBLISH_STATUS_COUNT` 在全仓只出现在本头自身的 static_assert（`git grep AIO_PUBLISH_STATUS_COUNT` 命中 publish.h:70/136/145 三处，零消费者）；实现 aio_publish.cpp 实际返回集仅 {OK, PARAM, IO, STATE, DISKFULL}；测试 publish_atomic_test.c 断言集也只有 {OK, PARAM, IO, STATE}（`git grep AIO_PUBLISH_ERR` 于该测试：363/365/367/369/389/428 PARAM、387 IO、406/410 STATE）；`ERR_UNSUPPORTED=5 / ERR_BAD_HEADER=9 / ERR_MISMATCH=10 / ERR_CHECKSUM=11 / ERR_NANINF=12 / ERR_DECL_INVALID=14 / ERR_HASH_MISMATCH=15 / ERR_NOMEM=3` 既无返回点也无断言，而声明只把 2/6/8 标了"（保留）"。
- 差在哪：按本头注释读，`rc < 71` 即"合法"，而 16–69 恰恰未定义；同时"看起来在用"的 8 个码在实现面没有任何产生路径，声明面与实现面对同一枚举的有效域说法不一致（2/6/8 标保留、3/5/9–12/14/15 不标却同样不可达）。
- 后果：调用方若照本头注释写有效性判定（`if (rc < AIO_PUBLISH_STATUS_COUNT)`）会把 16–69 当合法状态码放行；诊断分类上，实现永远不会报出的码被列进公开面，读者无法据此写错误处置。
- 定级：S3（无现有错误处置代码因此判错；纯声明面风险——本头符号按 :11–13 保持 DLL 内隐藏，外部无消费者）
- 怎么算修好：①注释补齐"0..15 与 70 有效，其余（含 16–69）非法"，与 AIO-001 副本逐字一致；②对本面不可达的 3/5/9/10/11/12/14/15 一并加"（保留，本面不产出）"标注，或收缩为 0..1,4,7,13,70,71 的显式子集声明；③若要真做有效性判定，提供 `aio_publish_status_is_valid_v1()` 或在握手结构里暴露有效码位图，否则删除 COUNT 哨兵以免误用。
- 置信：CONFIRMED

### CR-41-6 · `ASTROCS_HIPS_PUBLISH_ABI_VERSION` 是零消费者且无从协商的未接线声明
- 轴：C（主）＋D
- 位点：publish.h:42 `#define ASTROCS_HIPS_PUBLISH_ABI_VERSION 1u`
- 上位依据：同头 :11–13 自述归属："对外仍唯一导出 `astrocs_module_query_v1`, 本头符号经 version-script/DEF 保持隐藏"；AIO-001 的对齐做法是握手结构带版本字段（`aio_abi_v1.h:34 #define AIO_ABI_VERSION_V1 1u` ＋ `:69–89 aio_abi_info_v1{abi_version, status_count, …}` ＋ `:96 aio_abi_query_v1` 入口，:94 规定失配即 `AIO_ERR_ABI_MISMATCH`）。
- 现状：`git grep ASTROCS_HIPS_PUBLISH_ABI_VERSION` 全仓仅命中定义行本身；本头没有 ABI 查询函数，也没有任何携带 `abi_version` 的结构；本面四原语签名里没有版本参数；`AIO_PUBLISH_ERR_ABI_MISMATCH=2` 同时被标注"（保留，数值对齐）"（:55）。
- 差在哪：声明了一个版本号却没有任何比较/暴露点，版本语义无承载体；与 AIO-001 面"版本宏必随握手结构暴露"的同族做法不一致，读者会误以为存在发布面 ABI 协商。
- 后果：对外主张层面——若将来 publish ABI 升 v2（例如按 CR-41-2 加完成标记原语），当前面无法在编译期或运行期被发现，测试适配器（eng/tests/unit/p1_hips/adapter_entry_impl.cpp 直链 TU）与 module_entry 会静默用新语义编译旧声明。
- 定级：S3（今日无分叉可触发）
- 怎么算修好：要么删该宏并在头注释注明"版本随 `astrocs_module_query_v1` 的 module descriptor 暴露"，要么补一个 `aio_publish_abi_query_v1()`＋带 `abi_version/struct_size/status_count` 的信息结构（对齐 aio_abi_v1.h:84–89 的 INIT 宏），并把 `AIO_PUBLISH_ERR_ABI_MISMATCH` 从"保留"改为该握手真实返回码（与 CR-41-4/CR-41-5 同批改）。
- 置信：CONFIRMED

## 1.3 不报的核过项（合规，附检索式）

- staging 词法与路径推导三处一致：`git grep -n "ASTROCS_HIPS_STAGE_BASENAME\|hips_staging"` ⇒ 声明 publish.h:17/44–47/75、实现 aio_publish.cpp:81–93（`snprintf(out, cap, "%s/.%s%s", parent, base, ASTROCS_HIPS_STAGE_BASENAME)`）、测试 publish_atomic_test.c:139/335/652、调度器 P2 面 module_adapters.cpp:12120–12128（自述"词法与 aio_publish v1 的 `<parent>/.<base>.hips_staging.tmp` 同族"，另用 `.p2_mosaic_staging.tmp.<pid>.<seq>` 属 P2 私有并发命名，不是同一原语的副本）。无分叉。
- 故障注入注册名五对五齐备且"未知名忽略"属实：`git grep -n "publish_fault_active"` ⇒ aio_publish.cpp:42–48（`strcmp(env, name) != 0` ⇒ 精确匹配，未知名不注入），五处调用点 :52/:147/:178/:192/:205 与 publish.h:111–117 列名逐一对应；测试对四个注名均有期望（publish_atomic_test.c:433/478/500/520/570）。
- 状态码 ENOSPC/EDQUOT→DISKFULL 映射与声明一致：`git grep -n "ENOSPC"` ⇒ aio_publish.cpp:120–121/135–136 与 publish.h:66 一致，且 memory.md:263–265 有语义说明。
- `tree_fsync` 的"树不存在 → IO"与实现一致：publish.h:94 对 aio_publish.cpp:196–197；`stage 不存在 → STATE` 对 :211–212；`目标为普通文件 → STATE` 对 :216；`EXDEV → IO` 对 :227＋aio_atomic_file.h:594–598。
- 递归深度上限 64 与"符号链接不跟随"属实现选择/机制约束（§4 非缺陷项），且 aio_publish.cpp:22–24 与 publish.h:86 已如实说明，不报。
- Windows 侧 `promote_dir` 用 `MoveFileExA(..., MOVEFILE_REPLACE_EXISTING)`（aio_atomic_file.h:586–592），"空目录被替换"在 POSIX 与 Win32 目录语义下均可能成立（Win32 要求目标目录为空），故未据此报"正式平台失败"的指控——但 Win32 分支把除 `ERROR_NOT_SAME_DEVICE` 外的**所有**失败折成 `PROMOTE_STATE`，经 aio_publish.cpp:228 变成 `ERR_STATE`，而 publish.h:60/:100–103 只把 STATE 定义为"stage 缺失/目标非空"，未声明"访问被拒/目录被占用也是 STATE"。这条差异需要 Windows 实跑才能定级，本批只读不跑，**记入"我读不动或需要实跑才能定的"**，不作为 finding 计入。

---

# 2. lib/algorithms/drizzle/hips/include/astrocs/hips/types.h

读法：整个文件 156 行从头读完（头注释 1–15、标识 25–45、op 词表 47–56、config 键 58–74、manifest 输入键 76–99、输出键 101–115、诊断码 117–137、legacy 错误码说明 139–150）。
对账三面：机器合同 `eng/contracts/config/module_lifecycle_contract.schema.json`（ABI-002 冻结数据）＋权威头 `lib/include/astrocs/abi/lifecycle_v1.h:204–221`；文档合同 `docs/contracts/{PUBLIC_API.md:324–338, DATA_SEMANTICS.md:438–440,456}`；算法正本 `docs/algorithms/HIPS_WRITER.md`；实现与登记 `lib/algorithms/drizzle/hips/src/module_entry.cpp`、`lib/algorithms/drizzle/hips/module.yaml`；签名源 `lib/infrastructure/aio/include/aio_hips.h`。

## 2.1 §2.5 逐数值处置表（36 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| types.h:28 | `ASTROCS_HIPS_MODULE_VERSION` | `1u` | 模块版本（声明面） | 待确认 | 非科学量，但属对外身份数值；三侧不同值：实现 `module_entry.cpp:68 "1.0"`、登记 `module.yaml:34 module_version: 0.1.0-alpha.1`；宏本身零消费者（`git grep -c` 仅命中定义行） ⇒ CR-41-7 | 保守方向：以 `module.yaml` 为唯一源、describe 由该值派生；影响范围=loader/registry 观测到的模块身份与安装清单 | CONFIRMED |
| types.h:29 | `ASTROCS_HIPS_ABI_VERSION` | `1u` | 模块 ABI 版本（声明面） | 待确认 | 零消费者；实际发布走通用 `ACS_ABI_VERSION_V1`（module_entry.cpp:590），`module.yaml:35 abi_version: 1` ⇒ 同值两处来源 | 保守方向：删除宏或让它成为 `module.yaml abi_version` 的编译期镜像；影响范围=版本协商可追溯性 | CONFIRMED |
| types.h:31 | `ASTROCS_HIPS_BUILD_ID` | `"p1-hips-impl-2026-09-07"` | 构建标识（含日期） | 待确认 | 非科学量；由 module_entry.cpp:69 进 descriptor.build_id；值内含任务日期，属"构建一次写死"的历史事实，跟踪面无第二处可核对（`git grep -c` 命中 types.h＋module_entry 各 1） | 保守方向：改由构建系统注入（gen_backends_manifest/版本戳），源码不写死；影响范围=产物溯源与"文档内禁日期"口径 | PARTIAL（未见 descriptor.build_id 的门禁核对） |
| types.h:42 | `ASTROCS_HIPS_CONFIG_SCHEMA_VER` | `1u` | config schema 版本 | 结构性不适用 | 非科学量：接口版本号；唯一消费者 module_entry.cpp:598（descriptor.config_schema_ver）⇒ 已接线 | — | CONFIRMED |
| types.h:45 | `ASTROCS_HIPS_PLAN_VERSION` | `1u` | plan 输出词表版本 | 结构性不适用 | 非科学量；消费者 module_entry.cpp:660（plan JSON `plan_version`） | — | CONFIRMED |
| types.h:56 | `ASTROCS_HIPS_OP_WRITE_PRODUCT` | `"write_product"` | 唯一 v1 op 词 | 结构性不适用 | 非科学量：词表；module_entry.cpp:329 精确比较；`module.yaml:40` 注记 node_operations 按 types.h op 词表登记 | — | CONFIRMED |
| types.h:64 | `nside` 下界 | `>=512` | 叶级 NSIDE 下界 | 文献值 | Górski 2005 HEALPix NESTED 约定经 `docs/algorithms/HIPS_WRITER.md:48`（A_cell=4π/(12·nside²)）＋ `aio_hips.h:144` "nside - 叶级 NSIDE (2 的幂, >= 512)"；实现 module_entry.cpp:340 逐条件核对 | — | CONFIRMED |
| types.h:64 | `nside` 上界（**声明面缺失**） | 实现 `(1ull << 24)` | 未在声明面出现的拒绝上界 | 待确认 | 条文面零提及：`git grep -n -e "1 << 24" -e "16777216" -- docs` 零命中；实现 module_entry.cpp:340 `nside > (1ull << 24)` → BAD_VALUE ⇒ CR-41-9 | 保守方向：声明面补 "nside ≤ 2^24"（或说明推导式 Norder≤12）；影响范围=调用方按声明构造 nside=2^25 得无解释的拒绝 | CONFIRMED |
| types.h:65 | `tile_width` | `恒 512` | HiPS 标准瓦片边长 | 文献值 | IVOA/CDSP HiPS 注册规范 512×512（`docs/algorithms/HIPS_WRITER.md:77` "512×512"）；实现 module_entry.cpp:347 `tw != 512` 拒 ⇒ 与声明逐字一致 | — | CONFIRMED |
| types.h:66 | `data_type` 值域 | `0=FLOAT32 1=FLOAT64` | 存储精度枚举 | 结构性不适用 | 非科学量：dtype 编码；`aio_hips.h:57–58 AIO_HIPS_FLOAT32=0, AIO_HIPS_FLOAT64=1` 逐值一致；实现 module_entry.cpp:353 只接受这两个值（但读法见 CR-41-10） | — | CONFIRMED |
| types.h:67 | `flags` | `AioHipsProductFlag 位或` | 子产品位掩码 | 待确认 | 声明未给允许位集与"0 是否合法"；实现 module_entry.cpp:359–365 限定 `SIGNAL\|SUPPORT\|SNR\|VARIANCE\|IVAR`（aio_hips.h:35–40，值 1…16）且 `fl <= 0` 即拒 ⇒ 声明面少两条约束 | 保守方向：声明补"非 0、仅这 5 位有效"；影响范围=调用方以 flags=0 试探得未声明的拒绝 | CONFIRMED |
| types.h:68 | `creator_did` 缺省 | `"ivo://astrocs/phase1"` | properties creator_did 默认 | 实验标定 | 非科学量：命名标识；锚 `docs/contracts/DATA_SEMANTICS.md:438` "缺省 did=ivo://astrocs/phase1、title=\"ACSD Phase1\"（:414-415）"；实现 module_entry.cpp:371 逐字一致 | — | CONFIRMED |
| types.h:69 | `obs_title` 缺省 | `"ACSD Phase1"` | properties obs_title 默认 | 实验标定 | 同上（DATA_SEMANTICS:438）；实现 module_entry.cpp:374 一致。**注意第二入口**：生产 sink `astro_sphere_sink.cpp:107` 用 `meta.filter.empty() ? "ACSD Phase1" : ("ACSD " + meta.filter)` —— 同一 properties 键在另一入口有第二条派生式，声明面只记其一 ⇒ 本行内登记为提醒项，不另计条（见 2.3 末条） | 保守方向：声明面注明"仅指本模块 config 缺省；sink 入口按 filter 派生"；影响范围=properties 字面值随入口不同 | CONFIRMED |
| types.h:71 | `exposure_s` 域 | `f64 秒, >=0` | 曝光时间 | 结构性不适用 | 非科学常量：入参域；实现 module_entry.cpp:379 `if (!(c->exposure_s >= 0.0))` 拒 NaN/负 ⇒ fail-closed 与声明一致；缺键默认 0.0（:378）而 DATA_SEMANTICS:438 规定 "exposure≤0 不写 obs_exptime/t_min/t_max"（声明面未记该下游效果） | — | CONFIRMED |
| types.h:73 | `moc_order` | `0=auto(=tile order)` | MOC 阶数 | 文献值 | IVOA MOC 阶语义经 `docs/contracts/DATA_SEMANTICS.md:439` "0=auto（=tile_order）；>0 取 min(moc_order, tile_order) 静默钳位（:419，DISP-HIPS-005）"；本声明只覆盖前半句，钳位属已登记缺陷 DISP-HIPS-005（不重复报） | — | CONFIRMED |
| types.h:74 | `max_workers` | `0=不设限` | config 并行上限声明 | 待确认 | 实现从不据此裁剪租约：module_entry.cpp:305 "0 = 无 config 上限 (事务恒串行, 仅作回显)"、:385–386 只存不回用、:848 `acquire(ex->user_data, 1)` 恒 1、plan 硬输出 `"min_workers":1,"max_workers":1`（:666）⇒ CR-41-8 | 保守方向：要么删键（事务串行、并行度=跨 instance），要么按兄弟模块取小语义实现；影响范围=调用方的资源上限意图被静默吞掉 | CONFIRMED |
| types.h:81 | `n_tiles` | `u32, >=0` | tile 行数 | 结构性不适用 | 循环上界/计数域；实现 module_entry.cpp:435–439 以 u64 读取并要求存在 | — | CONFIRMED |
| types.h:82 | `leaf_order` | `==log2(nside)` | 叶级阶交叉校验 | 公式导出 | `hip_ilog2_u32` 定义（module_entry.cpp:312–316 `while ((1u<<r) < n) r++`）即 2 为底的整数对数；实现真按式核对（:440–446 `leaf_order != hips_ilog2_u32(c->nside)` → 拒），合规 | — | CONFIRMED |
| types.h:83 | `width` | `==tile_width` | 瓦片边长交叉校验 | 结构性不适用 | 实现 module_entry.cpp:447–453 真按声明核对 | — | CONFIRMED |
| types.h:84 | `parent_ipix` 元素宽 | `u64×N` | NESTED ipix 宽度 | 结构性不适用 | 与 `aio_hips.h:122`/`AstroSphereTileView.parent_ipix`（uint64_t）一致；值域下界 ≥12·4^K 由 writer 判（`docs/contracts/DATA_SEMANTICS.md:440` rc=−3） | — | CONFIRMED |
| types.h:85–86 | `flux/coverage` 平面长度 | `dtype×N×W²` | 数据面字节数 | 公式导出 | 元素数 = N·width²、字节 = ×dtype（4 或 8）；实现真按式核对：module_entry.cpp:454 `tile_elems = width*width`、:455 `dtype_sz = (data_type==FLOAT64)?8:4`、:489/509 长度失配即拒 | — | CONFIRMED |
| types.h:87,89 | `has_mask/has_variance` 位图长 | `u8×N` | per-tile 可选性位图 | 结构性不适用 | 实现 hips_bitmap_any（:418–425）要求 `bn == n_tiles`，任一非 0 ⇒ 需要对应平面 | — | CONFIRMED |
| types.h:88,90 | `valid_mask`/`var_num` 平面长 | `u8×N×W²`、`dtype×N×W²` | 掩膜/方差面 | 结构性不适用 | 实现 :497–503、:506–514 逐长度核对 | — | CONFIRMED |
| types.h:91 | `snr_count` | `u32, >=0` | SNR 点数 | 待确认 | 实现用 u64 读取并加**未声明**上限：module_entry.cpp:518–525 `if (m > 100000000ull) → MANIFEST_FIELD 拒绝`；1e8 无任何条文/推导锚（`git grep 100000000 -- docs` 零命中） ⇒ CR-41-11 | 保守方向：声明面补上限并给依据（如 = 单产品点集容量上界或由 plan 内存估算导出）；影响范围=大目录 SNR 产品被无解释拒绝 | CONFIRMED |
| types.h:92–94 | `snr_ra/dec/val` 元素宽 | `f64×M` | SNR 点坐标与值 | 结构性不适用 | 与 `aio_hips.h:128–130 double ra_deg, dec_deg, snr` 一致；实现按 `m*8` 长度核对（:530–532）＋ memcpy 8B（:553–555） | — | CONFIRMED |
| types.h:95 | `snr_star_id` 元素宽 | `i64×M` | stable star_id | 结构性不适用 | `aio_hips.h:131 int64_t star_id`；实现 `lsid == m*8`（:533）＋ memcpy 8B（:556） | — | CONFIRMED |
| types.h:96–97 | `snr_qflags`/`snr_status` 值域 | `u32×M`（**未给值域**） | SNR 质量位/测光状态 | 待确认 | 权威签名面有闭合域：`aio_hips.h:132–134` `quality_flags 位标志: 1=PSF_OK 2=saturated 4=has_saturated 8=photo_matched 16=photo_rejected`、`photometric_status 0=unmatched 1=used 2=rejected`；声明面与实现均不校验（module_entry.cpp:557–558 仅 memcpy，legacy 面无值域拒绝：types.h:146 记 `aio_hips_write_snr_points: -1 null`）⇒ CR-41-12 | 保守方向：声明面写"位集⊆{1,2,4,8,16}"与"∈{0,1,2}"并在解码处 fail-closed；影响范围=snr 子产品可携带合同外状态字节 | CONFIRMED |
| types.h:98 | `prov_pixfrac` 域 | `f64 (0,1]` | drizzle 收缩因子 | 文献值 | SciPipe/Stroe(2005)/drizzle 定义经正本（`docs/science/` SCI-DRZ-001 链，types.h:147 亦复述 `pixfrac∈(0,1]`；项目记忆"drizzle pixfrac 默认 0.8"另案）；实现不在解码期判域，由 legacy `aio_hips_set_drizzle_provenance` rc=2 拒（module_entry.cpp:964–967） | — | CONFIRMED |
| types.h:99 | `prov_scale` 域 | `f64 >=0` | 弧秒像元尺度 | 结构性不适用 | 入参域；legacy 面判（types.h:147 `scale>=0`）；**单键给出时静默忽略** ⇒ CR-41-13 | — | CONFIRMED |
| types.h:103–115 | 输出 manifest 13 键 | 键名集合 | execute 输出词表 | 待确认 | 实现逐键产出且内容相符（module_entry.cpp:1076–1100 的 snprintf 序列，13 键全在、无多余键），**但 13 个宏零消费者**（`git grep -c HIPS_O_KEY_` → 仅 hips/types.h 命中 13 次＝定义行本身，实发用裸字面量）⇒ 词表不参与任何一致性检查，与 drizzle 侧同形态（CR-41-19 修法第①项对两头同时适用）；两文档合同面对该 JSON 零登记 ⇒ 归 CR-41-15 | 保守方向：实现改用宏，并加"宏集合 == 实发键集合"机检 | CONFIRMED |
| types.h:113 | `workers` | `==1` | 实际租借数 | 结构性不适用 | 由实现事实钉住：module_entry.cpp:848 `acquire(...,1)`、:617 "事务内串行 → max_workers=1"（与本行一致；与 :74 的"0=不设限"不一致 ⇒ CR-41-8） | — | CONFIRMED |
| types.h:117–118 | 诊断码分界 | `自定义从 100 起` / `通用域 (1..7)` | 诊断码命名空间规则 | 待确认 | 所引 ABI-002 两处权威面均无"100 起"条文：机器面 `eng/contracts/config/module_lifecycle_contract.schema.json:62–64` 只写 `detail_code: "MODULE_SPECIFIC"`，权威头 `lifecycle_v1.h:204–211` 通用域实为 **0..7**（NONE=0），且本模块自己传 `ACS_DIAG_ECODE_NONE`（module_entry.cpp:1023,1072）⇒ CR-41-14 | 保守方向：条文补"模块专属 ≥100 且与 module_id 组合唯一"，或把通用域写为 0..7；影响范围=诊断码空间治理与跨模块可对账 | CONFIRMED |
| types.h:122–135 | `hips_ecode` 9 值 | `100,101,102,110,111,120,121,122,123` | 模块诊断 detail_code | 结构性不适用 | 枚举基数/编码；全部有返回点（`git grep HIPS_ECODE_`：336/331/342–381/437–540/907/875/1030/1039，含 122=875、123=1030 各一处）；与同族 drizzle 头 100/101/102/110/111 逐值同义（drizzle/types.h:90–98） | — | CONFIRMED |
| types.h:6–7 | 复述的三条平面公式 | `flux_sum/covered_area`、`covered_area/A_cell`、`var_num_sum/covered_area²` | 声明头里的科学式摘要 | 公式导出 | 正本 `docs/algorithms/HIPS_WRITER.md:80–81,121`：`signal[p]=flux_sum/covered_area`、`support[p]=covered_area/A_cell` **`>1 钳 1.0`（:1301-1305）**、`variance=var_num_sum/(covered_area)²`；本头三条与正本同式但**漏掉 support 钳位**（⇒ CR-41-16），且本头 :5 自称"不含任何科学常量" | — | CONFIRMED |
| types.h:7 | MOC UNIQ 式 | `4·4^m+(c>>2(K−m))` | UNIQ 编码 | 文献值 | `docs/science/PHASE3_HIPS_TO_FITS.md:193` "IVOA MOC 1.0/2.0 Recommendation §4.3.1：uniq=4×4^order+index"；本式的 `index = c>>2(K−m)` 即 NESTED 降阶索引，与正本一致（代码实际算式在 writer/ALG-HIPS-004，本头不实现） | — | CONFIRMED |
| types.h:52,139–150 | legacy 返回码集合 | `-1..-8`、`1`、`2` | 九导出错误码语义复述 | 文献值（本仓正本） | 抽查两处一致：`HIPS_WRITER.md:107`（rc=−4 signal / −5 support）对 types.h:143；`:130`（−2 var_num_sum 缺失、−3 视图不匹配）对 types.h:144–145；完整逐码核对未做（超出本文件改动面） | — | PARTIAL（未逐码全核对九导出） |

## 2.2 findings

### CR-41-7 · 模块版本三侧不同值（`1u` / `"1.0"` / `0.1.0-alpha.1`），且 calibration 先例已证该判据可机检
- 轴：C（主）＋D
- 位点：types.h:28 `#define ASTROCS_HIPS_MODULE_VERSION  1u`
- 上位依据：`lib/algorithms/drizzle/hips/module.yaml:34 module_version: 0.1.0-alpha.1`；types.h:11–14 自称合同链终点是 module.yaml；既有判据先例 `eng/tests/unit/calibration_adapter_test.cpp:390` 断言 `"A: descriptor version == ASTROCS_CAL_VERSION (module.yaml module_version)"`、`eng/tests/unit/calibration_integration_test.cpp:587 "D4: module_version 三方一致"`。
- 现状：宏 `1u` 全仓零消费者（`git grep -c ASTROCS_HIPS_MODULE_VERSION` → 仅 types.h 命中 1 次）；真正对外发布的 descriptor 版本来自另一处字面量 `module_entry.cpp:68 static const char kVersion[] = "1.0";` → `:592 out_desc->version = acs_str_from(kVersion);`；登记面是 `0.1.0-alpha.1`。同一身份字段三个面三个值，且 hips 侧没有任何"三方一致"断言（calibration/cosmetic 有）。
- 差在哪：声明头给的数值既不是 loader 看到的值，也不是 registry 登记的值，而项目自己已确立"descriptor 版本必须与 module.yaml 一致"的判据—— hips 面同时违反判据与判据的可检出性。
- 后果：loader/registry/产物 provenance 里的模块身份无法互相对账（`module_build_id`、run 清单的 `module_version` 字段取自 descriptor，见 `lib/include/astrocs/core/contracts.h:213` 语义），"模块版本一致性"这一对外主张在 hips 单元上不成立。
- 定级：S2（生产路径可写：`astrocs_module_query_v1` → hips_describe:592 → host loader；但不改产品科学字节）
- 怎么算修好：单一源落地——`module.yaml` 的 `module_version` 作为权威，types.h 的宏改为同一字符串（`ASTROCS_HIPS_MODULE_VERSION "0.1.0-alpha.1"`），`kVersion` 改为该宏；同时把 calibration 的"三方一致"断言推广成 p1_hips adapter 测试的同类断言（`eng/tests/unit/p1_hips/adapter_test.c` 已有 descriptor 核对处），三处必须同批改，否则版本串一改就判红。
- 置信：CONFIRMED（三面数值逐字读到）

### CR-41-8 · config 键 `max_workers` 声明为"租约上限（0=不设限）"，实现读入后从不生效，且与同仓两兄弟模块同键不同义
- 轴：C（主）＋D
- 位点：types.h:74 `#define HIPS_CFG_KEY_MAX_WORKERS "max_workers"     /* u32 租约上限 (0=不设限) */`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:2496` "禁 hardware_concurrency（worker=budget.max_workers）"、`:1978` "cpu_workers = host->budget.max_workers（blocks(budget)，禁硬编码）"（worker 数唯一来源是 host budget/Runtime lease）；本模块 `threading_model=host_executor_lease`（types.h:14）。
- 现状：`module_entry.cpp:385–386` 解析进 `c->max_workers`；`:305` 注释即承认"0 = 无 config 上限 (事务恒串行, 仅作回显)"；execute 无条件 `ex->acquire(ex->user_data, 1)`（:848）；plan 输出硬编码 `"min_workers":1,"max_workers":1`（:666）；输出面 `workers` 键固定回显 1（types.h:113）。三个兄弟模块均真按该键语义裁剪租约：`lib/algorithms/drizzle/src/module_entry.cpp:913–918`、`lib/algorithms/calibration/src/module_entry.cpp:942–943`、`lib/algorithms/cosmetic/src/module_entry.cpp:760–761`，同为 `uint32_t want = ex->max_workers ? … ; if (c.max_workers > 0 && c.max_workers < want) want = (uint32_t)c.max_workers; acquire(want)`。
- 差在哪：同名键在三个 P1 模块里有两种语义（取小裁剪 vs 纯回显），而 hips 的声明文本描述的是它自己**不实现**的那种；"0=不设限"在恒租 1 的事务面上根本没有对应行为。
- 后果：调用方/编排层按声明下发 `max_workers` 期望并行时得到串行执行（性能主张失真）；配置审计面上该键是"已接线"的假象——属本批重点③"声明了但消费者只用其中一部分/不用"。不改产品字节。
- 定级：S3（无产物错值；方向钉死：忽略一个**上限**在此处只会让实际占用更小（恒 1），故偏保守而非 fail-open，不能报成资源超发）
- 怎么算修好：二选一：①删该 config 键（连同 types.h:74 与解析行），声明面写明"事务内不可并行（HI-3），并行度=跨 instance 由 host budget 决定"，与 :113 的 `==1` 呼应；②按 calibration/cosmetic 的取小语义实现并在 types.h 注明"上限语义，实际恒 ≤1"。若选②，须同时改 plan 的硬编码 1（:666）为 `min(1, want)` 之类真实值，否则改对反而与 plan 判据冲突。
- 置信：CONFIRMED

### CR-41-9 · 声明面 nside 值域缺上界，实现按未声明的 `nside > 2^24` 拒绝
- 轴：C
- 位点：types.h:64 `#define HIPS_CFG_KEY_NSIDE       "nside"           /* u32, 2 的幂, >=512 */`
- 现状：实现 module_entry.cpp:340 `if (!f || nside < 512 || nside > (1ull << 24) || (nside & (nside - 1)) != 0)` 四条件，声明面只记两条（≥512、2 的幂）；上界 2^24（Norder 12）在两文档合同面亦零提及（`git grep -n -e "1 << 24" -e "16777216" -- docs` 无输出）。
- 差在哪：`nside ≤ 2^24` 是一条会拒真的对外约束，只存在于实现字面量里，调用方与合同读者都不知道。
- 后果：以 nside=2^25 组包的调用方得到 `HIPS_ECODE_BAD_VALUE(101)`，而按声明它"合法"；该上界若被有意放宽/收紧，也无任何条文可对照（真正的资源约束是 hierarchy 内存，见 module_entry.cpp:619–622 的估算式）。
- 定级：S3
- 怎么算修好：声明面补上界并给推导（建议写成与内存估算挂钩的形式，如 `Norder = log2(nside) ≤ 12`，或按 `12·nside²·dtype·2` 的上界反推），同步 `docs/algorithms/HIPS_WRITER.md` 的参数校验节；改后必须同步 adapter 测试里构造合法 nside 的用例（防判红）。
- 置信：CONFIRMED

### CR-41-10 · `data_type`/`flags` 以 f64 读入再 `(int)` 截断，非整数落点被静默归位而非拒绝
- 轴：R（主）＋C
- 位点：types.h:66 `#define HIPS_CFG_KEY_DATA_TYPE   "data_type"       /* 0=AIO_HIPS_FLOAT32 1=FLOAT64 */`；types.h:123 `HIPS_ECODE_BAD_VALUE     = 101,  /* 值非法 (非有限/越位/非 2 幂/tile_width!=512) */`
- 现状：`module_entry.cpp:352 int dt = (int)json_get_f64(json, HIPS_CFG_KEY_DATA_TYPE, &f);` → `:353 if (!f || (dt != AIO_HIPS_FLOAT32 && dt != AIO_HIPS_FLOAT64))`；flags 同型（:358–362 `(int)json_get_f64(...)` 后再判位）。
- 差在哪：判定发生在截断**之后**——`"data_type": 0.7` 截成 0、`"flags": 1.9` 截成 1，都通过声明面承诺的"越位拒绝"，实际是未声明的静默取整；同函数内 `exposure_s` 反而做了 NaN 防护（:379），说明作者有此意识但整型通道漏了。
- 后果：调用方意图为 f64（1）而误写成 1.4 时得到 f32（0/截断为 1 尚可，但 0.7→0 即改变 dtype）；dtype 决定 `dtype_sz`（:455）与平面长度判据（:489），一旦错位，整条 manifest 长度校验是按错的 dtype 做的——要么误拒合法包，要么误接受错误字节数的面。
- 定级：S2（方向钉死：截断只会把非法值变成合法值 ⇒ 少一条拒绝路径 ⇒ **偏绿**；触发路径 module_entry.cpp:352→455→489 可逐跳写；但需调用方真的下发非整数，本批无实跑证据）
- 怎么算修好：读入即判整型（例如取原始 JSON token 类型，或 `double d = json_get_f64(...); if (d != floor(d) || !isfinite(d)) → BAD_VALUE`），`data_type`、`flags`、`nside`、`tile_width`、`moc_order`、`snr_count`、`n_tiles` 同批处理；并在 types.h 注明"必须为整数字面量"。
- 置信：PARTIAL（缺 json_get_f64 对非数字/整型 token 的行为确认——需读 json 解析助手实现或实跑一次，本批未展开）

### CR-41-11 · `snr_count` 声明 `u32, >=0` 无上限，实现另设未声明的 1e8 硬拒
- 轴：C（主）＋S
- 位点：types.h:91 `#define HIPS_M_KEY_SNR_COUNT     "snr_count"            /* u32, >=0 */`
- 上位依据：条文面零提及——`git grep -n "100000000" -- docs` 零命中（检索式：`100000000`、`1e8`、`snr_count`）。
- 现状：`module_entry.cpp:518–524` `uint64_t m = json_get_u64(manifest, HIPS_M_KEY_SNR_COUNT, &f); ... if (m > 100000000ull) { ... "hips: snr_count too large" }`，同时该值以 u64 读入（声明为 u32，>2^32 的值也一律被 1e8 门挡住，故两侧只在该门处相交）。
- 差在哪：一条会影响产品能否发布的阈值既不在声明面、也不在任何合同/科学文档，来源不明；1e8 也不是由 `m * sizeof(AioHipsSnrPoint)`（= 40 B/点 ⇒ 4 GB）之类可复核推导得到的。
- 后果：大目录 SNR catalogue（点集 > 1e8）在事务面被拒，而拒因不可从合同推出；且分配风险判断（真正约束是 40B×M 的 malloc 与内存看门狗）与 1e8 这个数字没有可写的对应关系。
- 定级：S3（不改产品字节；对外影响=大输入被无条文支持的拒绝）
- 怎么算修好：把上限写进 types.h:91 并给可复核推导（例如"由 plan 的 memory_bytes_estimate 与 host budget 上限反推的最大点数"，或直接以分配失败 → ACS_ERR_NOMEM 代替预设门），同步 plan 侧估算式（module_entry.cpp:619–622）。
- 置信：CONFIRMED

### CR-41-12 · SNR 的 `quality_flags`/`photometric_status` 声明只给宽度不给值域，实现按 memcpy 直写，合同外状态可进产品
- 轴：C（主）＋R
- 位点：types.h:96–97 `HIPS_M_KEY_SNR_QF_B64 "snr_qflags_base64" /* u32×M */` / `HIPS_M_KEY_SNR_ST_B64 "snr_status_base64" /* u32×M */`
- 上位依据：`lib/infrastructure/aio/include/aio_hips.h:132–134` `uint32_t quality_flags; // 位标志: 1=PSF_OK 2=saturated 4=has_saturated / 8=photo_matched 16=photo_rejected` 与 `uint32_t photometric_status; // 0=unmatched 1=used 2=rejected`；`PUBLIC_API.md:327` 明确 aio_hips.h 是"唯一权威签名源"。
- 现状：解码只做长度核对（module_entry.cpp:534–535 `lqf == m*4`、`lst == m*4`）后 `memcpy` 进结构（:557–558）；legacy 面也不判值域（types.h:146 自述 `aio_hips_write_snr_points: -1 null`）。
- 差在哪：闭合值域在签名源存在，但在**输入声明面与解码处双双缺席**；即"声明该域不存在、代码仍静默读并写出"（本批重点②形态）。
- 后果：snr 子产品（HiPS Catalogue 的 FITS 列）可携带 `photometric_status=7` 之类无定义值，读侧与下游按位/按值解释处会得到未定义结果；`n_snr_points` 仍报成功，无告警。
- 定级：S2（触发路径 module_entry.cpp:530→557→959 `aio_hips_write_snr_points` 可逐跳写；缺"生产输入真越域"的证据）
- 怎么算修好：types.h:96–97 补位集与值域（逐字对齐 aio_hips.h:132–134），解码循环内对每点判 `qf & ~0x1f` 与 `st > 2` → `HIPS_ECODE_MANIFEST_FIELD` 拒；同时补一条越域负例测试（若上游 PSF 面将来扩位集，两文件须同批改，否则判红）。
- 置信：CONFIRMED

### CR-41-13 · provenance 两键声明为各自可选，实为"双键齐才生效"，单键输入被静默丢弃
- 轴：C（主）＋R
- 位点：types.h:98–99 `HIPS_M_KEY_PROV_PIXFRAC "prov_pixfrac" /* f64 (0,1] 可选 */` / `HIPS_M_KEY_PROV_SCALE "prov_scale_arcsec" /* f64 >=0 可选 */`
- 现状：`module_entry.cpp:565–568` `r->prov_pixfrac = json_get_f64(..., &f); ... r->has_prov = (f && f2) ? 1 : 0;`（注释 `:564 /* drizzle provenance (可选; 双键齐才生效) */`）；单键给出时既不报错也不写 provenance（:964 的 `if (... rows.has_prov)` 直接跳过 `aio_hips_set_drizzle_provenance`）。
- 差在哪：声明面用两条独立的"可选"表达，实际语义是一条合取约束，且违反合取时静默降级——与同文件 `has_variance_base64`（位图非全 0 时必填平面，:90）那种成对约束的写法也不一致。
- 后果：properties 里 `ASTROCS_DRIZZLE_PIXFRAC/SCALE_ARCSEC` 两卡缺一（README:169–171 的生产 provenance 面），产品仍报成功；只有回查输出 manifest 的 `drizzle_provenance_set` 才发现（该键 types.h:112 已声明，属可观测但非强制）。
- 定级：S3
- 怎么算修好：声明面把两键改述为"成对可选：只给一个 ⇒ 视为非法（`HIPS_ECODE_MANIFEST_FIELD`）或按文档明确忽略并告警"；若选告警路线，需要在模块诊断面上新增 ecode 并同步 README:169–171。
- 置信：CONFIRMED

### CR-41-14 · 诊断码分界"从 100 起（ABI-002）"在所引权威面与机器合同面均无条文，且通用域被写窄成 1..7
- 轴：S（主，常数无出处）＋C
- 位点：types.h:117–118 `/* ───────── 诊断 detail_code (自定义从 100 起; ABI-002) ───────── / * 通用域用 ACS_DIAG_ECODE_* (1..7); 模块专属从 100 起。 */`
- 上位依据：ABI-002 的两处权威面——`lib/include/astrocs/abi/lifecycle_v1.h:204–221`（枚举 `ACS_DIAG_ECODE_NONE = 0` 到 `..._UNSUPPORTED_OP = 7`，并注 "execute 期其余错误 | 实际域(IO/DATA/SCIENCE_PRECONDITION/RESOURCE) | 模块自定义"）与机器合同 `eng/contracts/config/module_lifecycle_contract.schema.json:62–64`（`"detail_code": "MODULE_SPECIFIC"`）——**都没有规定数值 100 这个分界**；检索式：`git grep -rn -e "100 起" -e "从 100" -- docs`（零命中）、`git grep -n "MODULE_SPECIFIC" -- eng/contracts`。
- 现状：本头把 100 当既定条文引用，实际是全仓约定（drizzle/types.h:90 亦 100、calibration/cosmetic 同族），且同一行把通用域写成 "1..7"，而权威枚举含 NONE=0，本模块自己就在传 `ACS_DIAG_ECODE_NONE`（module_entry.cpp:1023、:1072）。
- 差在哪：一个决定诊断码命名空间不冲突的分界值以"引用条文"的形式出现，但被引条文没有该内容；同处的通用域范围与其自身用法矛盾。
- 后果：若通用 `ACS_DIAG_ECODE_*` 扩到 ≥100（该头是唯一权威，随时可加），各模块 100 段就会与通用域撞码而无人判红；跨模块诊断对账（`detail_code` 进 run 清单/CI 归因）失去唯一性前提。
- 定级：S3
- 怎么算修好：在 ABI-002 权威头（`lifecycle_v1.h` 错误码映射注记处）与机器合同 schema 补一条"模块专属 detail_code ∈ [100, ...) 且与 module_id 组合内唯一；通用域 0..7"，之后各 types.h 只引用不断言；同批把 types.h:118 的 "(1..7)" 改为 "(0..7)"。
- 置信：CONFIRMED

### CR-41-15 · 模块 adapter 的整套 JSON 词表（13 config + 19 manifest 输入 + 13 输出键）在两处合同面零登记
- 轴：D（主）＋C
- 位点：types.h:58–60 `/* ───────── config 键词表 (HIPS_CFG_KEY; v1) ───────── ... */`、types.h:76–79、types.h:101–102（三段词表的起点行）
- 上位依据：types.h:11–14 自称合同链 `SCI-DRZ-001 -> ALG-HIPS-001..005 -> API-HIPS-001 (docs/contracts/PUBLIC_API.md) -> module.yaml`；而 `PUBLIC_API.md:326–329` 的 API-HIPS-001 写的是 "状态: CONTRACT_READY…头: lib/infrastructure/aio/include/aio_hips.h（**唯一权威签名源**，签名只取该头…九导出 …）"——该节冻结的是 legacy C ABI 九导出，不含 adapter JSON 词表。
- 现状：`git grep -n -e flux_base64 -e has_variance_base64 -e snr_qflags_base64 -e prov_scale_arcsec -- docs/contracts/PUBLIC_API.md docs/contracts/DATA_SEMANTICS.md` 零命中；`git grep -rln "out_dir\|n_tiles" -- eng/contracts` 零命中；`module.yaml:40` 只登记 op 词表并回指 types.h。即这套词表的唯一事实源是代码头自身，文档合同面与机器 schema 面都没有它。
- 差在哪：本模块对外（跨 DLL、跨阶段、host↔module）的**唯一**数据入参合同没有任何登记面，而 AGENTS §7 与仓内纪律把 `eng/contracts/` 定为"机器校验的合同 schema（唯一事实源）"、`docs/contracts/` 定为合同说明——三方对账在"合同面"这一侧根本没有可比的对象（本批重点②的极端形态：合同面零提及，代码在读）。
- 后果：任何词表变更（键改名、平面元素宽度改、新增可选键）都不会被合同门判红；`ASTROCS_HIPS_CONFIG_SCHEMA_VER`/`PLAN_VERSION` 声称"词表演进必须升版本号"（types.h:41、:44）却无对账对象可判；跨阶段消费（mosaic 读 normalize 产物、CLI 组装 execute config）只能读源码。
- 定级：S2（不改字节，但"合同门保护对外 JSON 合同"这一主张在本单元不成立；触发面=任何词表演进都判不红）
- 怎么算修好：为该 adapter 面建登记面并接门——①`eng/contracts/config/`（或 `eng/contracts/data/`）新增 hips module config/execute-input/execute-output 三份 JSON Schema，键名/类型/值域逐条从 types.h 抄入；②`docs/contracts/PUBLIC_API.md` 的 API-HIPS-001 节补"模块 adapter JSON 面"子节并指向该 schema；③把 `module.yaml` 的 op 登记与 schema 路径挂钩；④CI 检查 schema 与 types.h 宏名逐条一致（否则改一头即漂移）。改后需同步 `eng/tests/unit/p1_hips/adapter_test.c`、`publish_atomic_test.c` 里手工拼的 manifest 字面量。
- 置信：CONFIRMED

### CR-41-16 · 自称"不含任何科学常量"的声明头复述科学公式，且 support 式漏掉正本的 `>1 钳 1.0`
- 轴：C（主）＋D
- 位点：types.h:5–8 `本头只承载 C ABI adapter 层的常量/词表/诊断码, 不含任何科学常量 (surface brightness=flux_sum/covered_area、support=covered_area/A_cell、variance=var_num_sum/covered_area²、MOC UNIQ=4·4^m+(c>>2(K−m)) 等公式全部留在 aio_hips_writer.cpp / ALG-HIPS-001..005 ...)`
- 上位依据：`docs/algorithms/HIPS_WRITER.md:78–85` 原文 "`signal[p] = flux_sum[p] / covered_area[p]`（:1300），`support[p] = covered_area[p]/A_cell`，`>1 钳 1.0`（:1301-1305）… `support` **无量纲**（sr/sr，∈[0,1]）"；`:153–155` 进一步规定"归约权重取自未钳制真实覆盖面积；钳后的 support 只作发布值，不进入权重"。
- 现状：本头给出的 support 式只有 `covered_area/A_cell`，没有钳位项，也没有 ∈[0,1] 值域；同时该头 :5 声明自己"不含任何科学常量"。
- 差在哪：(a) 科学式的复述不完整，钳位与值域正是该量的对外语义（gate7 复检就是判 `support∈[0,1]`，见 README:179–180）；(b) 同一行既说"不含科学常量"又罗列四条科学式，适用域自相冲突——按"口径唯一"纪律，科学式只应从 `docs/science`/`docs/algorithms` 单向引用。
- 后果：读 adapter 头的人/后续迁移者会认为 support 可 >1（部分覆盖塌缩类缺陷 HIPS_WRITER:89–90 明确列为"禁用写法＋负例判据"），并在头文件里形成第二份公式口径，正本改动时本头不会随动。
- 定级：S3（纯注释面，无代码按它算）
- 怎么算修好：把 :5–9 的公式枚举删成一句指针（"全部科学式与值域见 ALG-HIPS-001..005 / `docs/algorithms/HIPS_WRITER.md` §2/§3"），若必须保留则补 `support=min(covered_area/A_cell, 1)` 与 ∈[0,1] 及"钳后值不作权重"的引用。
- 置信：CONFIRMED

## 2.3 不报的核过项（合规，附检索式）

- `nside ≥512 且 2 的幂`、`tile_width 恒 512`、`data_type ∈{0,1}`、`exposure_s ≥0（含 NaN 拒）`、`leaf_order == log2(nside)`、`width == tile_width` 六条声明与实现逐条一致，且全部 fail-closed：检索式 `git grep -n "HIPS_CFG_KEY_\|HIPS_M_KEY_LEAF_ORDER" -- lib/algorithms/drizzle/hips/src`；实现位点 module_entry.cpp:339–351、379–381、440–453。
- `creator_did/obs_title` 缺省串与文档合同逐字一致：`git grep -n -e "ivo://astrocs/phase1" -e "ACSD Phase1" -- lib docs/contracts` ⇒ types.h:68–69、module_entry.cpp:371/374、DATA_SEMANTICS.md:438。（同键的第二入口派生式差异另记 CR-41-8b/下表）
- `has_mask/has_variance` 位图语义（"位图非全 0 时必填平面"）声明与实现一致：hips_bitmap_any（module_entry.cpp:418–428）＋ :489/509 长度核对。
- `snr_*_base64` 六平面在 `snr_count>0` 时全部必填且按字节长度精确核对（:530–535），与 types.h:92–97 的宽度声明一致：检索式同上。
- `hips_ecode` 九个值全部有返回点（含 122/123）：`git grep -n "HIPS_ECODE_" -- lib/algorithms/drizzle/hips/src`；与 drizzle 头共享的 100/101/102/110/111 语义逐值同义（drizzle/types.h:90–95）。
- 诊断码"通用域由 ABI-002 冻结"的实质（不与 0..7 撞码）成立，只是分界数值无条文（已并入 CR-41-14，不重复计条）。
- `ASTROCS_HIPS_SCI_ID/ALG_ID/API_ID` 三个字符串宏均被 describe 使用（module_entry.cpp:70–72→594–596），且 types.h:33–36 对 "describe 的 alg_id 槽只承载 ALG-HIPS-001（主公式面），002..005 同链" 的适用域写清了 ⇒ 不是自相矛盾，属两个适用域（§4 提醒项）。
- obs_title 的第二入口派生（`astro_sphere_sink.cpp:107` 用 `"ACSD " + filter`）与 types.h 缺省的差异**不作为独立 finding**：本头声明的是本模块 config 缺省，sink 是另一条生产路径且 DATA_SEMANTICS:438 同时登记 "obs_filter 恒写（参数 NULL→空串值，禁'不写键'）… Phase1 config filter_passband 透传"，两式各自适用域成立；仅把"声明面应注明仅指本模块缺省"作为 CR-41-8 表行的提醒项保留。
- `flags` 允许位集与 `AIO_HIPS_PRODUCT_*` 定义一致：`git grep -n "AIO_HIPS_PRODUCT_" -- lib/infrastructure/aio/include/aio_hips.h`（SIGNAL=1 … IVAR=16）。

<!-- PROGRESS: 2/6 -->

---

# 3. lib/algorithms/drizzle/include/astrocs/drizzle/types.h

读法：整个文件 114 行从头读完（头注释 1–11、标识 21–37、op 词表 39–49、config 键 51–68、manifest **输出**键 70–83、诊断码 85–101、legacy 码说明 103–108）。
对账三面：`docs/contracts/PUBLIC_API.md:255–301`（API-DRZ-001 签名与返回码权威）、`docs/contracts/DATA_SEMANTICS.md:34,60–80,379,438`＋`docs/algorithms/DRIZZLE_GEOMETRY.md:109,180,186–201,327`＋`docs/standards/STANDARDS_REGISTRY.md:168,181`、`eng/packaging/config/defaults.json:665–677`（默认值与取值域正本，ASTROCS_DESIGN.md:295 指认）；实现 `lib/algorithms/drizzle/src/module_entry.cpp`；引擎面 `lib/algorithms/drizzle/healpix_drizzle/{hp_drizzle_api.h,reverse_drizzle.h,reverse_drizzle.cpp}`；兄弟声明 `lib/algorithms/{calibration,cosmetic}/…/types.h` 与 `lib/infrastructure/gaia_xpsd_client/include/astrocs/gaia/types.h`（本头 :3 自认的"对齐先例"）。

## 3.1 §2.5 逐数值处置表（27 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| types.h:24 | `ASTROCS_DRIZZLE_MODULE_VERSION` | `1u` | 模块版本（声明面） | 待确认 | 零消费者（`git grep -c` 仅命中定义行）；descriptor 实发 `module_entry.cpp:60 kVersion[]="1.0"`；登记 `module.yaml:31 module_version: 0.1.0-alpha.1`；本头 :3 自称的对齐先例恰是单一源做法（`gaia/types.h:30 #define ASTROCS_GAIA_VERSION "0.1.0-alpha.1"` → `module_entry.c:52` 用它） ⇒ CR-41-17 | 保守方向：以 module.yaml 为唯一源；影响范围=模块身份可对账性 | CONFIRMED |
| types.h:25 | `ASTROCS_DRIZZLE_ABI_VERSION` | `1u` | 模块 ABI 版本 | 待确认 | 零消费者；实发 `head.abi_version = ACS_ABI_VERSION_V1`（module_entry.cpp:69/80/521），`module.yaml:32 abi_version: 1` ⇒ 同值双源 | 保守方向：删宏或令其为 module.yaml 值的编译期镜像 | CONFIRMED |
| types.h:27 | `ASTROCS_DRIZZLE_BUILD_ID` | `"p1-drz-impl-2026-09-02"` | 构建标识（含日期） | 待确认 | 消费者 module_entry.cpp:63（descriptor.build_id）；值内含任务日期且写死在源码，跟踪面无第二处可核 | 保守方向：由构建系统注入；影响范围=产物溯源 | CONFIRMED |
| types.h:34 | `ASTROCS_DRZZLE…CONFIG_SCHEMA_VER`（原文 `ASTROCS_DRIZZLE_CONFIG_SCHEMA_VER`） | `1u` | config schema 版本 | 结构性不适用 | 非科学量：接口版本；唯一消费者 module_entry.cpp:524（descriptor.config_schema_ver） | — | CONFIRMED |
| types.h:37 | `ASTROCS_DRIZZLE_PLAN_VERSION` | `1u` | plan 词表版本 | 结构性不适用 | 非科学量；消费者 module_entry.cpp:588 | — | CONFIRMED |
| types.h:48–49 | op 词 | `"drizzle"` / `"reverse"` | 两 op | 结构性不适用 | 词表；module_entry.cpp:346–347 精确比较；`module.yaml:40` 附近注记按 types.h op 词表登记 | — | CONFIRMED |
| types.h:54 | `nside` 域 | `u32, 2 的幂, >=1` | HEALPix 分辨率 | 文献值 | Górski 2005 NESTED NSIDE=2^K（`docs/algorithms/DRIZZLE_GEOMETRY.md` HEALPix 索引域；PUBLIC_API.md:277 记 `2=nside 非 2 幂` 拒绝）；实现 module_entry.cpp:403–416 逐条核对（u32 上界 4294967295＋整数性＋is_pow2） | 上界两处不一致：drizzle 允许到 2^32−1，hips 侧实现另有 2^24（aio 面），声明面互不引用 | CONFIRMED |
| types.h:55 | `nested` 域 | `仅 1=NESTED; 0(RING) 被实现硬拒绝` | 索引序选择 | 结构性不适用 | 编码选择；`docs/algorithms/DRIZZLE_GEOMETRY.md:180` "仅 NESTED"；实现 config 层只做非零归一（module_entry.cpp:418 `c->nested = (found && nested != 0.0) ? 1 : 0;`），拒绝发生在引擎；**缺键时按 0（=RING）下传**，故缺键路径的错误域是执行期而非 CONFIG | 保守方向：声明面注明"缺键视为 RING ⇒ 执行期拒"或改为缺键 MISSING_FIELD；影响范围=配置错误定位粒度 | CONFIRMED |
| types.h:56 | `pixfrac` 域 | `f64 [0,1] (DISP-DRZ-003 口径)` | drop 收缩因子 | 待确认 | 正本值域是 **(0,1]**：`eng/packaging/config/defaults.json:669 constraint "0 < pixfrac <= 1"`、`DATA_SEMANTICS.md:379 "引擎层 (0,1] 严格拒绝 ≤0/>1"`、`DRIZZLE_GEOMETRY.md:109,180,190`、`STANDARDS_REGISTRY.md:168 §2 第 1 行`；本头 :56 写 `[0,1]` 并把 DISP-DRZ-003 当口径，而该登记的适用域是**文件通道 API 层**（`DRIZZLE_GEOMETRY.md:191/326`：`api.cpp:191` vs `drizzle_engine.cpp:1570`） ⇒ CR-41-18 | 保守方向：改 `(0,1]`；影响范围=0 值在 config 层被放行、执行期才拒 | CONFIRMED |
| types.h:56 | `pixfrac` 缺省值（**声明面缺失**） | 实现缺键取 `1.0` | 缺省投影足迹 | 待确认 | 正本 `defaults.json:667 "value": 0.8`（`authority_status: owner_adjudicated`，note 解释 0.8 才有重叠覆盖）；实现 module_entry.cpp:355 `if (!found) pixfrac = 1.0;` 且注释自认"契约只约束 (0,1]"；另 `eng/packaging/config/config_registry.json:732` 记"默认 1.0 已入 defaults.json"，与 defaults.json:667 的 0.8 相反 ⇒ CR-41-18 | 保守方向：声明面写出缺省值并指向 defaults.json 为唯一源，实现兜底改取该值；影响范围=缺省足迹（缝隙覆盖与 `shared_vertices` 分支 DRIZZLE_GEOMETRY:223） | CONFIRMED |
| types.h:57 | `precision_mode` 域 | `0=FP32 1=FP64 -1=auto(header)` | 存储精度选择 | 结构性不适用 | 非科学量：模式编码；`PUBLIC_API.md:296–297` 同值同义（"0=FP32（默认）/1=FP64/-1=读 header \"PRECISION\" KV"）；实现 module_entry.cpp:363–371 校验 `isfinite && -1<=p<=1 && p==(int)p`（含整数性判据，是 hips 侧缺的那道） | 缺省 -1（auto）未写在声明面 | CONFIRMED |
| types.h:58 | `output_path` | 可空 | legacy .hiss 路径 | 结构性不适用 | 路径项；module_entry.cpp:377–384 `strdup`，:419 注记"双空 → execute 前置拒绝" | — | CONFIRMED |
| types.h:59 | `hips_dir` | — | HiPS 产品集根 | 结构性不适用 | module_entry.cpp:385–393 读入，:984 用于 artifacts 探测；但同名键在**输出**词表里另有 `DRZ_OUT_KEY_HIPS_DIR`（:80）而输出从不产该键 ⇒ CR-41-19 | — | CONFIRMED |
| types.h:60 | `max_workers` | `u32, 租借上限 (0=不设限)` | 并行上限 | 结构性不适用 | 非科学量：资源参数；实现真按声明语义执行：module_entry.cpp:913–918 `uint32_t want = ex->max_workers; ... if (c->max_workers > 0 && c->max_workers < want) want = c->max_workers; acquire(ex->user_data, want)`；与 calibration:942–943 / cosmetic:760–761 同语义 ⇒ 反证 hips/types.h:74 的同一声明在 hips 侧未实现（CR-41-8） | — | CONFIRMED |
| types.h:63 | `width`（reverse） | `u32 输出平面宽` | 反向投递平面宽 | 待确认 | 声明面只给类型；实现要求 `>=1`、`<=2147483647`、整数（module_entry.cpp:437–443，报 "must be i32 >= 1"）⇒ 声明的 u32 与实际 i32 上界冲突 | 保守方向：声明改 "i32, 1..2^31-1"；影响范围=大平面配置被无解释拒绝 | CONFIRMED |
| types.h:64 | `height`（reverse） | `u32 输出平面高` | 反向投递平面高 | 待确认 | 同上（:437–443 同判据） | 同上 | CONFIRMED |
| types.h:65 | `pixel_scale_arcsec`（reverse） | `f64 像元尺度` | 输出像元角尺度 | 待确认 | 实现要求有限且 `>0`（module_entry.cpp:448–452 BAD_VALUE），声明面未写值域；正本语义见 `DATA_SEMANTICS.md` 尺度域（角秒/像素） | 保守方向：声明补 ">0 且有限"；影响范围=0/负尺度配置的报错粒度 | CONFIRMED |
| types.h:66–67 | `ra_center_deg` / `dec_center_deg` | f64 中心坐标 | 反向投递视场中心 | 待确认 | 声明面无值域；本头未记 RA∈[0,360)、Dec∈[-90,90] 的约束（同类约束在 hips 侧由 leaf_ipix/球面校验承担，`DATA_SEMANTICS.md:440`）；实现侧的判定未展开核对 | 保守方向：声明面补有效域或在实现补显式拒绝；影响范围=极区/跨 0h 中心配置 | PARTIAL（未逐行读 reverse 的 crval 校验分支） |
| types.h:68 | `projection` | `字符串: TAN/SIN/ARC` | 反向投递投影类型 | 待确认 | 条文面：`docs/algorithms/` 的 reverse 投影域未逐条核对；实现读取与传递见 module_entry.cpp:1017 附近的 manifest 解析（本头声明的是 config 键，实现在 reverse 走 manifest 读键）| 保守方向：确认声明载体（config vs manifest）与投影支持集与引擎一致；影响范围=projection 配置是否被接受 | PARTIAL |
| types.h:72–83 | 输出词表 12 键 | 键名集合 | execute 输出 JSON | 待确认 | 与实现产出集不一致：`DRZ_OUT_KEY_HIPS_DIR`(:80) 与 `DRZ_OUT_KEY_HISS_PATH`(:81) 在 src 中**从不产出**（`git grep -n "hips_dir\\\\\"\|legacy_hiss_path" -- lib/algorithms/drizzle/src` 零命中）；实现另发 `n_rejected_nonfinite{,_value,_variance}`、`n_rejected_nonpositive_weight`（:970–975）与 reverse 的全部输出键，均未声明；且 12 个宏在 src/tests 零消费者 ⇒ CR-41-19 | 保守方向：词表与产出同源（实现改用宏＋宏补齐）＋加一致性门 | CONFIRMED |
| types.h:43 | 注释宣称 `signal_plane_base64` | 键名 | op=drizzle 的诊断回传 | 待确认 | 实现只在 **op=reverse** 的输出里产出该键（module_entry.cpp:1236；op=drizzle 输出见 :965–1004 无此键）⇒ 声明把键归给了错的 op，见 CR-41-19 | 保守方向：注释归位到 reverse，或在 drizzle 输出补该键 | CONFIRMED |
| types.h:41 | 注释宣称"输入经 manifest JSON 承载" | — | 输入面 | 待确认 | 声明面**没有任何输入键宏**；实现从 manifest 读 `width/height/data_base64`（:768/774/802）与 reverse 的 `nside/nested/pixfrac/output_fp64/no_data_as_zero/leaf_ipix_base64/n_leaf/leaf_signal_base64/leaf_support_base64`＋WCS（:1036–1129、:1017–1022）⇒ CR-41-20 | 保守方向：补输入词表段（含值域与必填性） | CONFIRMED |
| types.h:90–100 | `drz_ecode` 8 值 | `100,101,102,110,111,112,120,130` | 模块诊断码 | 结构性不适用 | 枚举编码；7 值有返回点（src 命中数：100=5、101=8、102=1、110=13、111=3、112=4、120=1），`130 DRZ_ECODE_REV_SHAPE` **零返回点** ⇒ CR-41-21；100/101/102/110/111 与 hips/types.h:122–127 逐值同义 | — | CONFIRMED |
| types.h:86 | 诊断码分界 | `自定义从 100 起` / `通用域 (1..7)` | 命名空间规则 | 待确认 | 与 hips/types.h:117–118 同一条无条文引用的复制（`lifecycle_v1.h:204–211` 通用域实为 0..7；ABI-002 机器面 `module_lifecycle_contract.schema.json:62–64` 只写 `MODULE_SPECIFIC`）⇒ 归 CR-41-14（同形态，不重复计条） | — | CONFIRMED |
| types.h:104 | legacy 帧通道码域 | `负值 -1..-13 (0=成功)` | 转发域说明 | 待确认 | 正本 `PUBLIC_API.md:280–283` 枚举为 `-1..-8`、`-9=无 WCS`、`-12=HiPS dir 空`、`-13=直写失败` ⇒ **不存在 -10/-11**，本头的连续区间写法比条文宽；转发实现只把原值进 message（module_entry.cpp legacy 通道），不解释 ⇒ 无判据风险，仅声明面过宽 | 保守方向：按条文列离散集合而非区间 | CONFIRMED |
| types.h:105 | reverse 码域 | `正值 1..7 (0=成功)` | 转发域说明 | 文献值（本仓正本） | `PUBLIC_API.md:284` "reverse 返回 1..6（+7=C 边界内部异常…）" ⇒ 与声明一致 | — | CONFIRMED |
| types.h:5–6 | 科学语义指针 | "Van Oosterom 剖分、面积权重、NaN-Inf 传播语义全部留在 drizzle_engine / SCI-DRZ-001" | 声明面不自持科学式 | 结构性不适用 | 非科学量：本头确实没有复述任何公式（与 hips/types.h:5–8 的复述行为不同，见 CR-41-16 对照）；本行的引用锚 = `docs/science/DRIZZLE.md`、ALG-DRZ-001 | — | CONFIRMED |

## 3.2 findings

### CR-41-17 · 模块版本三侧不一致（drizzle 实例），且违反本头自己点名的 gaia 单一源先例
- 轴：C
- 位点：types.h:24 `#define ASTROCS_DRIZZLE_MODULE_VERSION  1u`
- 上位依据：types.h:3 `对齐先例: lib/infrastructure/gaia_xpsd_client/include/astrocs/gaia/types.h (CAT-GAIA-IMPL, commit babe752d)`；该先例的实际形态是 `gaia/types.h:30 #define ASTROCS_GAIA_VERSION "0.1.0-alpha.1"` 且 `gaia_xpsd_client/src/module_entry.c:52 static const char kVersion[] = ASTROCS_GAIA_VERSION;`（一个源）；登记面 `lib/algorithms/drizzle/module.yaml:31 module_version: 0.1.0-alpha.1`。
- 现状：宏 `1u` 零消费者；descriptor 发 `module_entry.cpp:60 kVersion[] = "1.0"` → `:523 out_desc->version`；module.yaml 登记 `0.1.0-alpha.1`；同形态还出现在 hips（CR-41-7）与 `lib/algorithms/noise_snr/src/module_entry.cpp:62 "1.0"`，而全部 `lib/**/module.yaml` 一律 `module_version: 0.1.0-alpha.1`。
- 差在哪：声明面、自报面、登记面三个数值互不相等，且声明面那个没有任何机制约束它们收敛（calibration 侧已存在 "descriptor version == module.yaml module_version" 的测试断言，drizzle/hips/noise 侧没有）。
- 后果：run 清单与 provenance 里的 `module_version` 取 descriptor 值，与安装 registry 的 module.yaml 值不同名同义 ⇒ "模块版本可对账"主张不成立。
- 定级：S2（生产路径：`astrocs_module_query_v1` → describe:523 → loader；不改科学字节）
- 怎么算修好：按 gaia 先例收敛到单一源——types.h 的版本宏改为字符串并与 module.yaml 逐字一致；`kVersion` 改用该宏；把 calibration 的三方一致断言泛化为覆盖 drizzle/hips/noise_snr 的同族测试；三处同批改（只改一头必判红）。
- 置信：CONFIRMED

### CR-41-18 · `pixfrac` 声明值域含 0 且缺省值未声明，两处都与正本相反；所引 DISP-DRZ-003 的适用域不是本层
- 轴：C（主）＋S＋D
- 位点：types.h:56 `#define DRZ_CFG_KEY_PIXFRAC        "pixfrac"            /* f64 [0,1] (DISP-DRZ-003 口径) */`
- 上位依据：`eng/packaging/config/defaults.json:665–677`（`"key": "drizzle.pixfrac"`, `"value": 0.8`, `"constraint": "0 < pixfrac <= 1（严格校验，非法值显式 NO_DATA，不静默夹逼…）"`, `"authority_status": "owner_adjudicated"`）——`ASTROCS_DESIGN.md:295` 指认该文件为"默认值与取值域正本"；`docs/contracts/DATA_SEMANTICS.md:379` "引擎层 (0,1] 严格拒绝 ≤0/>1（不夹逼）"；`docs/standards/STANDARDS_REGISTRY.md:168` §2 第 1 行 "pixfrac∈(0,1]"；`docs/algorithms/DRIZZLE_GEOMETRY.md:191` "pixfrac==0.0（文件通道 API 层） | 放行后进引擎再拒（双轨） | api.cpp:191（DISP-DRZ-003）"。
- 现状：实现按声明面放行 0：`module_entry.cpp:356 if (!isfinite(pixfrac) || pixfrac < 0.0 || pixfrac > 1.0)`（0 合法）；缺键兜底 `:355 if (!found) pixfrac = 1.0;` 且注释自认"缺键默认 1.0 (契约只约束 (0,1])"；声明面 :56 既写 `[0,1]` 又不写缺省值，把 `DISP-DRZ-003`（其登记文本只覆盖 `hp_drizzle_api.cpp` 文件通道 vs 引擎的双轨）当作本层口径；另 `eng/packaging/config/config_registry.json:732` 声称"默认 1.0 已入 defaults.json"，与 `defaults.json:667` 的 0.8 直接相反。
- 差在哪：同一物理量的有效域在声明面是闭区间 [0,1]、在正本与引擎是 (0,1]；缺省值在声明面根本不存在，实现兜底 1.0 而正本裁定 0.8；被引登记被越层引用。
- 后果：(1) `pixfrac=0` 能通过 `validate_config`/`plan`（配置门判绿），到 execute 才被引擎拒（`drizzle_engine.cpp:1570`）——错误域从 CONFIG 漂到 SCIENCE_PRECONDITION，配置期合同门失去覆盖；(2) 缺省 1.0 与裁定 0.8 分叉，直接改变 drop 足迹（`defaults.json:677` note："取 1.0 时相邻 drop 仅边界相接，源像素落点错位处出现无覆盖缝隙"），并切换 `shared_vertices = (pixfrac == 1.0)` 的代码路径（`DRIZZLE_GEOMETRY.md:223`）。
- 定级：S2（缺省分叉有真实产物后果，但触发需要"缺键"这条输入路径；边界 0 只造成晚拒不改字节。方向钉死：配置层放行 0 ⇒ 少一条拒绝路径 ⇒ **偏绿**，与正本"严格拒绝 ≤0"相反）
- 怎么算修好：①`types.h:56` 改 `f64 (0,1]，缺省 = eng/packaging/config/defaults.json#drizzle.pixfrac`；②`module_entry.cpp:356` 判据改 `pixfrac <= 0.0 || pixfrac > 1.0`；③缺键兜底改取正本值（并让 `config_registry.json:732` 的措辞与 `defaults.json:667` 收敛为一条，二者现互相打脸）；④`DRIZZLE_GEOMETRY.md:191` 的 DISP-DRZ-003 行补记"模块 adapter config 层"或改为已消除。②③同批必须一起改 `eng/tests/unit/drizzle_adapter_impl.cpp` 与 p1003 系列里构造 pixfrac 的用例，否则改对反而判红。
- 置信：CONFIRMED（三侧数值条文逐字读到；未跑测试确认哪条现有用例依赖 1.0 兜底 ⇒ 该角缺证据）

### CR-41-19 · 输出词表与实现分叉：两个声明键从不产出、18 个实际产出键未声明（drizzle op 4 个＋reverse op 14 个），且 `signal_plane_base64` 被归给错误的 op
- 轴：C（主）＋D
- 位点：types.h:80–81 `#define DRZ_OUT_KEY_HIPS_DIR "hips_dir"` / `#define DRZ_OUT_KEY_HISS_PATH "legacy_hiss_path"`；types.h:43 `*                     (事务 sink 输出: HiPS 产品集 + signal_plane_base64 诊断回传)`
- 上位依据：types.h:4 "本头只承载 C ABI adapter 层的常量/词表/诊断码"（即词表以本头为事实源）；`PUBLIC_API.md:302` 附近对 execute 输出 JSON 的合同定位（API-DRZ-001 节）；对照 hips/types.h:101–115 的同族做法（其 13 键与实现逐键相符，只是同样零消费者）。
- 现状：op=drizzle 实际输出（module_entry.cpp:965–1004）为 `op,nside,nested,pixfrac,precision_mode,n_healpix_pixels,n_source_pixels,n_rejected_nonfinite,n_rejected_nonfinite_value,n_rejected_nonfinite_variance,n_rejected_nonpositive_weight,workers,artifacts`——无 `hips_dir`、无 `legacy_hiss_path`（`git grep -n "hips_dir\\\\\"\|legacy_hiss_path" -- lib/algorithms/drizzle/src` 零命中），四个 `n_rejected_*` 未声明；op=reverse 实际输出（:1217–1243）为 `op,width,height,nside,nested,pixfrac,output_fp64,n_source_leaf,n_target_pixel_touched,n_candidates,n_overlaps,n_invalid_ipix,n_nonfinite,n_skipped_outside,total_signal_in,total_signal_out,signal_plane_base64,coverage_plane_base64`——该 op 的**全部**输出键都不在声明词表里；`signal_plane_base64` 只在 reverse 产出，而 :43 把它写成 op=drizzle 的输出；12 个 `DRZ_OUT_KEY_*` 宏在全仓只出现在 types.h（`git grep -c DRZ_OUT_KEY_` → `…/types.h:12`）⇒ 实现与测试都用裸字面量，宏无一人使用。
- 差在哪：声明的"词表"既不是实现的词表（2 个幻影键＋18 个漏项：op=drizzle 实发 13 键中 4 键未声明，op=reverse 实发 18 键中 14 键未声明），也不是实现的单一源（零消费者），还与注释的 op 归属互相错位。
- 后果：任何按 types.h 写的消费者会在 op=drizzle 输出里找不到 `signal_plane_base64`、找不到 `hips_dir`/`legacy_hiss_path`（真正的产物 URI 只在 `artifacts` 数组里），而 `n_rejected_nonfinite*` 这四个**排异计数**（正本 `DRIZZLE_GEOMETRY.md:196` 要求"每个输出像素必须暴露被剔除样本计数"）不在对外词表里 ⇒ 判据只能从源码发现；合同门无从校验输出面演进。
- 定级：S2（不改产品字节；对外主张"词表以 types.h 为准"不成立，触发路径 module_entry.cpp:965/1217 逐跳可写）
- 怎么算修好：①实现与测试改用 `DRZ_OUT_KEY_*` 宏（消除裸字面量）；②补齐声明：新增 reverse 输出段与 `n_rejected_nonfinite*`、`coverage_plane_base64` 等键；③删除或落实 `DRZ_OUT_KEY_HIPS_DIR`/`HISS_PATH`（若意图是"artifacts 里的路径来源"，改注释而非留幻影键）；④:43 的 `signal_plane_base64` 归位到 reverse；⑤加一条机检：drizzle/hips 两头的输出宏集合 == 实现实际 emit 的键集合（否则改一头又漂移）。
- 置信：CONFIRMED

### CR-41-20 · reverse 输入 manifest 词表在声明面完全不存在，其中 `no_data_as_zero` 默认让"无覆盖"写 0 而非 NaN，与三态编码正本相反
- 轴：R（主）＋C＋D
- 位点：types.h:70 `/* ───────── manifest 输出键 (execute 输出 JSON; v1) ───────── */`（该段之前无任何输入键宏；对照 hips/types.h:76–99 有完整的"manifest 输入键"段）；相关声明缺失项另见 types.h:41 `输入经 manifest JSON 承载 (typed artifact 输入, base64 行数据)`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:60–62` "Phase 2 帧间集成与 Phase 3 投影重采样的分母用 W_p（§29.2 / §30.2 的 W_p=0 覆盖级 NaN …）编码三态见 §4a 表（**无覆盖 ⇒ NaN**）"；`:34` "`invalid`：`NaN` 或 `support<=0`；有效样本判定为 `finite && support>0`"；`docs/algorithms/DRIZZLE_GEOMETRY.md:196`（合格性只判 `isfinite(x_j)`，DRIZZLE.md §4 与 defaults.json:669 同调："非法值显式 NO_DATA，不静默夹逼"）。
- 现状：reverse 从 manifest 读 10+ 个键，声明面一个都没有：`module_entry.cpp:1036 nside`、`:1045 nested`、`:1046 pixfrac`、`:1053 output_fp64`、`:1055 no_data_as_zero`、`:1082 leaf_ipix_base64`、`:1088 n_leaf`、`:1113 leaf_signal_base64`、`:1129 leaf_support_base64`，外加 WCS `crval/crpix/cd`（:1017–1022 注释与 :1253–1255 释放清单）。其中 `no_data_as_zero`：`:1056 if (!found) no_data_as_zero = 1.0;`（缺省即"填 0"）→ `:1168 rin.no_data_as_zero = (no_data_as_zero != 0.0)`；库层默认同向（`reverse_drizzle.h:41 bool no_data_as_zero = true; // 无覆盖像素输出 0 (true) 或 NaN (false)`）；生效点 `reverse_drizzle.cpp:233–243`：`double s = (wsum > 0.0) ? … : 0.0; … if (!in.no_data_as_zero && acc.touched[i] == 0) { s = std::nan(""); c = 0.0; } out.signal[i] = s;`。全仓 `git grep -rn "no_data_as_zero" -- docs eng/contracts` **零命中**（仅 lib 实现＋两个测试文件）。对照：hips/types.h:78 还把 "drizzle reverse 通道 leaf_*_base64" 当作已成约定来引用，而该约定在 drizzle 声明面无任何登记。
- 差在哪：一个能改变科学面填充语义的开关（0 还是 NaN）既不在声明词表、也不在任何合同/科学文档；其缺省方向与"无覆盖 ⇒ NaN"的三态编码相反。
- 后果：reverse 投递回的 signal 平面里，无覆盖像素是**有限的 0.0**；凡按 `DRIZZLE_GEOMETRY.md:196`/`DATA-002 §2a` 的"合格性只判 isfinite"取样的消费者，会把无覆盖当成 0 通量有效样本计入（同一次 execute 的 `coverage_plane_base64` 虽给 c=0 可作旁证，但合同规定的无效标记是 NaN，不是"另起一面去查"）；产品字节与三态编码不一致且无人判红。方向钉死：用 0 取代 NaN ⇒ 有效性判定**放宽** ⇒ 统计量被拉低而**判绿**（少一条剔除路径），这是 fail-open 而非 fail-closed。
- 定级：S2（有完整生产路径：`execute(op=reverse)` → module_entry.cpp:1055→1168 → reverse_drizzle.cpp:238；缺"下游消费者真的按 isfinite 取 reverse 平面"的一角证据 ⇒ 不升 S1）
- 怎么算修好：①在 types.h 增补"manifest 输入键"段，把 10+ 键连同值域/必填性/缺省逐一登记，`no_data_as_zero` 明确写"缺省与合同方向"；②缺省改 NaN（与 `DATA_SEMANTICS.md:62` 一致），保留 `no_data_as_zero=1` 作为显式 opt-in 并说明其唯一合法用途（如与旧结果对拍）；③若保留 0 填充，则必须在合同面（DATA_SEMANTICS §4a 或 ALG-DRZ-001 reverse 节）显式登记该三态例外，并补一条负例：无覆盖像素在默认路径下必须是 NaN；④同批修 `reverse_drizzle.h:41` 的库层默认，使两层一致（只改 adapter 会留下 direct-vs-plugin 产物树对拍分叉）。
- 置信：PARTIAL（缺下游消费者对 reverse 平面的实际合格性判据读取路径，以及"是否有产品消费该 reverse 输出"的登记面确认）

### CR-41-21 · 诊断码 `DRZ_ECODE_REV_SHAPE = 130` 在实现中零返回点，reverse 形状失配实际走 111
- 轴：C
- 位点：types.h:99–100 `/* reverse (domain=DATA) */ DRZ_ECODE_REV_SHAPE = 130 /* 输入像素数 != width*height */`
- 上位依据：types.h:88–98 的同族做法（每个 ecode 都有返回点：100=5 处、101=8 处、102=1、110=13、111=3、112=4、120=1，`git grep -c DRZ_ECODE_<k> -- lib/algorithms/drizzle/src`）；`lifecycle_v1.h:214–221` 的"实际域 × 模块自定义"映射要求 detail 与实发错误一致。
- 现状：`git grep -n "DRZ_ECODE_REV_SHAPE" -- lib` 仅命中 types.h 定义行；reverse 通道的平面长度核对实际以 `DRZ_ECODE_DATA_DECODE(111)`（注释即"base64 解码失败 / 长度失配"，:95）报出。
- 差在哪：声明面登记了一条专用诊断码，实现从未发出；同一失败形态由另一条码承担 ⇒ 词表与实际诊断分布不一致（对照 hips/types.h 的 122/123 两条都有返回点）。
- 后果：按 types.h 写诊断处置表的调用方/CI 归因会为 130 预留分支而永不命中，真正需要区分的"解码失败"与"像素数与 width·height 失配"被并到 111 一条 ⇒ 诊断可解释性下降（不改产品字节）。
- 定级：S3
- 怎么算修好：要么在 reverse 的长度核对处（module_entry.cpp:1088–1129 一带的 `n_leaf`/平面长度判定）改用 130 并保持 111 只表解码失败，要么删除 130 并把 111 的注释扩为覆盖两种失配；两改都需同步该模块的测试期望。
- 置信：CONFIRMED

### CR-41-22 · op=drizzle 接的是旧变体 `hp_drizzle_run_hips`，而合同点名的 Phase1 正式末端是 `hp_drizzle_run_phase1_hips`；机器门只认后者
- 轴：D（主）＋C
- 位点：types.h:42 `*   op=drizzle        帧通道: 单帧 [H,W] f32/f64 -> hp_drizzle_run_hips`
- 上位依据：`docs/contracts/PUBLIC_API.md:262–264` "`hp_drizzle_run_phase1_hips`（**Phase1 正式末端**：帧通道直写标准 HiPS，与旧 writer 节点产物逐字节一致，P23 一级新增）"；`:270–273` 两变体签名差异（phase1 变体多 `filter_passband`）；`:293–294` "HiPS 目录树由模块写入、编排层负责 overwrite 清理"；`docs/contracts/DATA_SEMANTICS.md:438` "obs_filter 恒写（参数 NULL→空串值，禁'不写键'）"；`docs/algorithms/DRIZZLE_GEOMETRY.md:256` "provenance 写 pixfrac/源像素尺度"。
- 现状：实现 `module_entry.cpp:928 int rc = hp_drizzle_run_hips(frame, (int)c->nside, c->nested, c->pixfrac, …)`；该变体签名（`hp_drizzle_api.h:84–86`，注释 "hips_dir: …; legacy_hiss_path: 可选 legacy .hiss (validation 用)"）没有 `filter_passband` 入参，而 phase1 变体（`:94–102`）明确 "product_begin 元数据与旧 writer 一致"＋"filter_passband: obs_filter 透传"。机器门侧：`eng/tools/quality/check_block_flow_ports_vs_code.py:66 TREE_WRITE_VERBS = ("aio_hips_product_begin", "hp_drizzle_run_phase1_hips")`（只把 phase1 变体当写树动词），`check_prod_reachability.py:35` 又把 `hp_drizzle_run_hips` 列为 CLI 直连禁止项，`eng/tests/cli/test_p1003_drizzle_path.py:26–32` 解释旧判据在单 exe 形态下不可满足而改判 CLI 源文件。
- 差在哪：模块事务面接的不是合同点名的正式末端；该旧变体既没有 obs_filter 透传通道（合同要求 properties 恒写该键），也不在"写树动词"门的识别集合里 ⇒ 模块写 HiPS 树这一事实在门看来不存在。
- 后果：走 `op=drizzle` 的 HiPS 产品在 `obs_filter` 上无法携带 Phase1 config 的 `filter_passband`（DATA_SEMANTICS:438 规定的透传链在模块面断开），且"节点声明写树 ⇒ 必须有 ports/清理契约"这条机检对本模块零覆盖（门接不到产品路径）。
- 定级：S2（不改科学像素；改的是产品元数据面与门覆盖。触发路径逐跳可写：`astrocs_module_query_v1` → execute(:928)；缺"生产编排是否实际以 op=drizzle 出正式产品"的登记面确认 ⇒ 不升 S1）
- 怎么算修好：①把 op=drizzle 的通道切到 `hp_drizzle_run_phase1_hips` 并把 `filter_passband` 加进 config 词表（与 hips 侧 `HIPS_CFG_KEY_OBS_FILTER` 对齐），或②在 API-DRZ-001 节明确"`hp_drizzle_run_hips` 仍是模块面入口"并把它补进 `TREE_WRITE_VERBS`；两方案都要同步 `eng/tests/cli/test_p1003_drizzle_path.py` 与 `drizzle_adapter_test.cpp`（后者的 `hp_drizzle_run_hips hidden` 断言会与"继续调用它"的事实相互制约），并更新 types.h:42 的注释。
- 置信：PARTIAL（缺两变体产物树的逐字节差异证据与编排层实际选用哪个 op 的登记确认；本批只读不跑）

## 3.3 不报的核过项（合规，附检索式）

- `nside` 的"u32 / 2 的幂 / >=1"与实现逐条一致且含整数性判据：`module_entry.cpp:403–416`（`nside != (double)(uint32_t)nside` ＋ `is_pow2_u32`）。
- `precision_mode` 的 `-1|0|1` 与实现一致，且实现**做了**整数性判据（`module_entry.cpp:365–366 prec != (double)(int)prec`）——该写法正是 hips 侧 `data_type/flags` 缺的那道，本文件合规，反向支持 CR-41-10 的修法可照抄。
- `max_workers` 在本模块声明与实现一致（:373 读、:913–918 取小裁剪、:596 plan 回显），是 hips/types.h:74 同键声明的**正确实现样本**（用于 CR-41-8 的对账证据，本文件不报）。
- `ASTROCS_DRIZZLE_SCI_ID/ALG_ID/API_ID` 三宏均由 describe 使用（`git grep -n ASTROCS_DRIZZLE_ -- lib/algorithms/drizzle/src` ⇒ module_entry.cpp:57–66 定义、:521–528 发布）。
- 诊断码 100/101/102/110/111 与 hips/types.h 同值同义（跨模块共享码语义未分叉）：`git grep -n "= 1[01][0-9]," -- "lib/algorithms/**/types.h"`。
- legacy 转发纪律一致：本头 :106–108 与 hips 头 :128–130/:150 都声明"原样进 message、不重解释"，且实现只在 `DRZ_ECODE_LEGACY_REJECT`（1 处）转发；`-1..-13` 区间的过宽只写到"不存在的 -10/-11"，无判据后果（记在表内，不另计条）。
- `reverse` 通道的 `width*height` 输入像素数失配确实被拒（走 111），未 fail-open：`module_entry.cpp` 的 `n_leaf`/平面长度核对（:1088 一带）。

<!-- PROGRESS: 3/6 -->

---

# 4. lib/algorithms/platesolve/cpp/ipv/include/ipv_robust_refine.h

读法：整个文件 136 行从头读完（头注释 4–20、`[P27-DEAD-PARAMS]` 登记块 29–34、`RobustRefineParams` 37–78、`RobustRefineResult` 83–97、主入口与入参说明 99–132）。
对账三面：`docs/algorithms/PLATESOLVE.md:65–130（§4a）、:237–243（ALG-WCS-001 §11 源码锚表）`；`lib/algorithms/platesolve/README.md:82,109`；`lib/algorithms/platesolve/cpp/ipv/REPORT.md`；实现 `lib/algorithms/platesolve/cpp/ipv/src/ipv_robust_refine.cpp`（含 :12–17 的 L1–L5 注释、:211–315 网格采样、:422–440 m0/clamp、:1029 发散检测、:1086 最终验收）与调用点 `ipv_solver.cpp:716–724 / 1106–1114 / 1403–1411`。

## 4.1 §2.5 逐数值处置表（36 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :39 | `grid_g_short_narrow` | `2` | FOV<1° 短边格数 | 待确认 | 条文面零提及（检索式 `git grep -rn -e grid_g_short -e "配额采样" -- docs` 无输出）；实现读取点 ipv_robust_refine.cpp:231 | 保守方向：取"格数×每格配额 ≥ min_stars"的推导；影响范围=精化阶段候选池空间分布 | CONFIRMED |
| :40 | `grid_g_short_medium` | `3` | FOV 1–3° 短边格数 | 待确认 | 同上（实现 :232） | 同上 | CONFIRMED |
| :41 | `grid_g_short_wide` | `4` | FOV>3° 短边格数 | 待确认 | 同上（实现 :233） | 同上 | CONFIRMED |
| :42 | `total_target_narrow` | `1000`（注"全选不限制"） | 小视场目标星数 | 待确认 | **该值在唯一读它的路径上不参与计算**：ipv_robust_refine.cpp:245–247 `int total_target = narrow_fov_all ? params.total_target_narrow : params.total_target_normal;` 之后 `int quota_per_cell = narrow_fov_all ? N : std::max(4, total_target / n_cells);` —— narrow 分支用 N（全部检测）而不用 total_target；且随后 :300–306 仍以 `max_stars` 截断 ⇒ 注释"全选不限制"与"1000"都不成立 ⇒ CR-41-25 | 保守方向：删除该值或让其真正生效；影响范围=窄视场候选池规模的可解释性 | CONFIRMED |
| :43 | `total_target_normal` | `180` | 常规视场目标星数 | 待确认 | 条文面零提及；实现 :247 经 `max(4, total/n_cells)` 摊成每格配额（无出处） | 保守方向：给"格数×配额=目标数"的推导与实测标定；影响范围=精化样本量→RMS/畸变项拟合自由度 | CONFIRMED |
| :44 | `max_stars` | `300` | 候选池上限 | 待确认 | 实现 :300–306 在**按 mag 升序**排序后 `resize(300)`；与同文件 :8 宣称的"空间均匀, 替代纯按星等取 60 颗"相冲（超限即退回按星等截取）⇒ CR-41-24；另注意与 `docs/plugins/algorithms_phase1/03_star_detection.md:64` 的 `star_detection.max_stars`（默认 20000、合同域 [20000,50000]）**同名不同对象不同量级**，两处均不互相引用 | 保守方向：超限截断改为"按格配额等比缩"，并给 300 的算力/条件数推导；影响范围=稠密场精化的空间覆盖均匀性 | CONFIRMED |
| :45 | `min_stars` | `100`（注"低于此值不强制填充"） | 候选池下限 | 待确认 | 实现 :308 明示"不强制填充到 min_stars"，:313 只在日志里出现 ⇒ **该值无任何作用**（既不触发填充也不触发拒绝）⇒ CR-41-25 | 保守方向：删除或改为真实 fail-closed 判据；影响范围=星少帧的行为可预测性 | CONFIRMED |
| :48 | `max_iterations` | `10` | IRLS 主循环上界 | 待确认 | 条文面零提及；迭代上界属"实现选择"但此处是**收敛性**参数（影响收敛与否），正本未登记（ALG-WCS-001 §11 表 :243 只写"CD 阻尼 + Tukey biweight，失败回退"） | 保守方向：给收敛证据（收敛所需迭代数的实测分布）；影响范围=精化是否收敛 | CONFIRMED |
| :49 | `tukey_c` | `4.685` | Tukey biweight 尺度常数 | 文献值 | Beaton & Tukey 1974, Technometrics 16, 147（DOI 10.1080/00401706.1974.10489171）——本仓文献池锚在 `docs/algorithms/PHOTOMETRIC_FIT.md:299`，并附效率/崩溃点复算（同文件 :34「`ARE(4.685065)=0.950000`」、:35「若改为每轮重估尺度，c=4.685 下崩溃点降到 11.9%；要保 50% 崩溃点必须同时把 c 降到 1.548」⇒ **c 与尺度是否重估必须成对变更**）；platesolve 本层条文零登记，且本头未说明其尺度路线（固定尺度 or 每轮重估） | 保守方向：在 ALG-WCS-001 登记 c 值与所用尺度路线，并核对实现是否每轮重估尺度（若是则 4.685 的 95% 效率与 50% 崩溃点两个保证同时失效）；影响范围=鲁棒拟合的统计性质 | PARTIAL（未逐行确认 IRLS 是否每轮重估尺度） |
| :50 | `converge_dp_max` | `1e-9` | 参数变化收敛阈 | 待确认 | 条文面零提及；无量纲判据，无推导 | 保守方向：给"参数空间尺度→1e-9 对应角秒量级"的换算；影响范围=提前/滞后收敛 | CONFIRMED |
| :51 | `converge_rms_rel` | `0.001`（0.1%） | RMS 相对变化收敛阈 | 待确认 | 同上 | 同上 | CONFIRMED |
| :54 | `cd_free_threshold` | `0.01` | CD 相对变化 <1% 无阻尼 | 待确认 | 条文面零提及；"阻尼"是数值稳定手段，其阈值无出处 | 保守方向：给条件数/噪声传播推导或标定；影响范围=CD 是否被过度冻结 | CONFIRMED |
| :55 | `cd_freeze_threshold` | `0.03` | CD 相对变化 >3% 冻结 | 待确认 | 同上 | 同上 | CONFIRMED |
| :56 | `cd_damp_base` | `1e-4` | 1% 处的阻尼量 | 待确认 | 同上 | 同上 | CONFIRMED |
| :57 | `cd_damp_freeze` | `1e6` | >3% 时的阻尼量（等效冻结） | 待确认 | 同上；以 1e6 表达"冻结"是技巧性写法，声明面未给"等价于冻结"的语义 | 同上 | CONFIRMED |
| :60–62 | `tol_factor_init/mid/final` | `2.0 / 1.5 / 1.0`（iter 0 / 1–2 / ≥3） | 逐轮匹配容差倍率 | 待确认 | 实现注释同值（ipv_robust_refine.cpp:13 `L1: tol = max(2×initial_RMS, 1.0")`）；条文面零登记；**同一数值在实现注释与本头各写一份**，无单一源 | 保守方向：数值只留一处（头文件为源，实现注释改为引用）＋ALG 层登记退火日程；影响范围=匹配对数与内点率 | CONFIRMED |
| :63–65 | `tol_floor_init/mid/final` | `1.0 / 0.5 / 0.3` 角秒 | 容差保底 | 待确认 | 同上（实现 :13 只有 1.0 与 2×，中/末段的 0.5/0.3 在实现注释中不复现）；单位=角秒（与 :15 的残差空间一致） | 同上 | PARTIAL（未逐轮核对实现是否用同一 floor 日程） |
| :68 | `lowe_k_neighbors` | `8` | 空间一致性邻居数 | 待确认 | 实现注释 :15 `L3: K=8 空间邻居残差一致性`；条文面零登记 | 同上 | CONFIRMED |
| :69 | `lowe_ratio` | `0.75` | Lowe 比率阈 | 文献值 | Lowe 2004 Distinctive Image Features（ratio test）为本仓三角形/NN 匹配的通用出处；**但本仓文献池内该值未见登记为文献标识**（检索式 `git grep -rn -e "Lowe" -e "lowe_ratio" -- docs` 只命中不相关行），故严格说其文献锚不在跟踪面 | 保守方向：在 `docs/references/` 或 ALG-WCS-001 登记 Lowe 2004 的式/节；影响范围=误匹配剔除强度 | PARTIAL（文献锚缺失，值本身业界通用） |
| :70 | `lowe_abs_threshold` | `0.5` 角秒 | Lowe 绝对阈 | 待确认 | 实现 :14 `OR d1 < 0.5"` 同值；条文面零登记；0.5″ 与像元尺度 s0 的关系未给（同一物理距离在不同板尺度下含义不同） | 保守方向：改写为 `k·s0` 像素形式并给 k 的依据；影响范围=宽/窄视场下该阈的等效性 | CONFIRMED |
| :71 | `spatial_consistency_sigma` | `3.0` | 残差一致性 σ 阈 | 待确认 | 实现 :15 同值；条文面零登记（3σ 属惯例但按 §2.5 无锚即待确认） | 同上 | CONFIRMED |
| :72 | `spatial_weight_factor` | `0.1` | 可疑点权重因子 | 待确认 | 实现 :15 "可疑点权重 ×0.1"；条文面零登记 | 同上 | CONFIRMED |
| :73 | `diverge_consecutive` | `2` | RMS 连续上升次数 | 待确认 | 实现 :16 "RMS 连续 2 次上升 > 10%"；条文面零登记 | 同上 | CONFIRMED |
| :74 | `diverge_rms_increase` | `0.10`（10%） | 发散判据阈 | 待确认 | 同上 | 同上 | CONFIRMED |
| :75 | `diverge_match_drop` | `0.50`（匹配数降 50%） | 发散判据阈 | 待确认 | 同上 | 同上 | CONFIRMED |
| :76 | `final_rms_tolerance` | `1.5` | final RMS >1.5×initial 即回退 | 待确认 | 实现 :17 "L5: final RMS > 1.5×initial"；条文面零登记 | 同上 | CONFIRMED |
| :77 | `min_matched_final` | `30` | matched < 30 即回退 | 待确认 | 实现 :17 "matched < 30"；与 order=3 拟合所需最少点数（TRANS 20 参数 ⇒ 至少 20 对）的关系未给推导 | 保守方向：给"参数数 × 富余因子"式；影响范围=低匹配帧是否回退 | CONFIRMED |
| :14 | `order=3 时 20 个` | `20` | TRANS 参数向量维数 | 公式导出 | 已核 `ipv_itertrans.h:42–55`：order=3 时 x 侧 10 个（x00,x10,x01,x20,x11,x02,x30,x21,x12,x03）＋y 侧 10 个 = 20 ⇒ 数值正确（本头 :14 的枚举写法 "x00..x03, y00..y03" 只是简写，不构成数值错） | — | CONFIRMED |
| :8 | 目标规模区间 | `100-300 颗` | 网格采样宣称规模 | 待确认 | 与 :42 的 1000/`:45 的 100` 无一致关系（1000 不参与计算、min_stars 不生效），区间实际由 :43 的 180 与 :44 的 300 决定 | 保守方向：注释只写"实际区间由生效参数导出"，不写第二套数 | 见 CR-41-25 | CONFIRMED |
| :8 | 被替代的旧做法 | `60 颗` | "替代纯按星等取 60 颗" | 待确认 | 60 的实际来源是 `ipv_types.h:226` 登记的"img_n_target (=60 硬覆盖)"与 `ipv_api.h:277` "solve_from_memory_with_callback_f64 在选星前 `p_adapt.img_n_target = 60;` 硬覆盖"（而 `ipv_types.h:249` 的声明默认是 **20**）⇒ 本行引用的是另一文件的硬编码值，未在声明面建立单一源 | 保守方向：60 应由配置/正本给出；影响范围=选星样本基数（本批 CR-41-27 主对象） | CONFIRMED |
| :11 | 成功率主张 | `99.87%` | "不破坏 99.87% 成功率" | 待确认 | 跟踪面零锚：`git grep -rn "99.87"` 全仓仅命中 `artifacts/acceptance/l2_performance/gates/*.json` 里数值巧合的 `t`/`io_read_bytes` 字段，无任何文档/报告以 99.87% 表述 platesolve 成功率 ⇒ CR-41-26 | 保守方向：改为指向跟踪面验收件的具体数字或删除该百分比；影响范围=对本阶段"不破坏主解"的对外主张 | CONFIRMED |
| :19 | 文件日期 | `2026-07-09` | 创作日期标记 | 结构性不适用 | 非科学量：VCS 已提供时间史，源码内日期不承载语义（与负责人"文档内禁日期与历史"口径同向） | — | CONFIRMED |
| :32 | 机器锁名 | `ctest ipv_dead_params_lock` | 冻结字面量调用点的测试 | 结构性不适用 | 非数值；该测试存在（登记面），其作用见 CR-41-23 | — | CONFIRMED |
| :33 | 证据锚路径 | `run/perf-fix/P27-dead-params/REPORT.md` | 本头全部数值与"死参数"结论的证据 | 待确认 | **不合格锚**：`.gitignore:17` 即 `run/*`（`git check-ignore -v run/... → .gitignore:17:run/*`），故该报告不在跟踪面；§2.5 第 3 档明确要求标定件"必须落在跟踪面，指 run/ 即判不合格" ⇒ CR-41-26 | 保守方向：把结论性数值迁到跟踪面（如 `docs/algorithms/PLATESOLVE.md` 或 `artifacts/evidence/`）；影响范围=本头 31 个数值全部失去可核证据 | CONFIRMED |
| :84–96 | `RobustRefineResult` 零值初值 | `0`/`0.0` | 结果结构体默认 | 结构性不适用 | 非科学量：空结果初始化哨兵；无科学语义 | — | CONFIRMED |
| :130 | 默认实参 | `params = {}` | 头文件层面即默认构造 | 结构性不适用 | 与 :29–33 的死参数登记同义（三个生产调用点也传 `{}`）⇒ 归 CR-41-23 | — | CONFIRMED |

## 4.2 findings

### CR-41-23 · 31 个科学/数值参数以"可配置结构体"形式声明，但生产三个入口全部默认构造，且有机器锁把该死状态固化
- 轴：D（主）＋C＋S
- 位点：ipv_robust_refine.h:29–33 `[P27-DEAD-PARAMS] 生产路径**全部字段不可达**: 唯一生产调用点 (ipv_solver.cpp::solve_post_select) 传字面量 RobustRefineParams{}（默认构造），因此本结构体所有字段改变都不影响生产行为也不报错。机器锁: ctest ipv_dead_params_lock (frozen_literals 校验该字面量调用点)。`；ipv_robust_refine.h:35 `鲁棒精化参数 (默认值对齐 spec)`
- 上位依据：AGENTS.md §6 硬禁令"不硬编码线程/ISA/block：由 benchmark 生成的 profile 决定"所依托的同一纪律＋`ENGINEERING_SPEC.md`/`docs/` 的"常数须有推导、数值正本在文档层"口径（负责人已裁"层级向下·口径唯一·常数须有推导"）；本仓对同族问题的正确做法见 `docs/plugins/algorithms_phase1/03_star_detection.md:58`（"本段一切「规模/上限」类数字都由配置键与星表查询口径导出，本节只给导出关系与指针……数值/合同域正本 = 本文件 §5.1 表与 `eng/contracts/schemas/phase_config_normalize.schema.json`"）。
- 现状：三个生产调用点 `ipv_solver.cpp:716–724`、`:1106–1114`、`:1403–1411` 均以 `RobustRefineParams{}, &logger_` 传默认构造；`ipv_api.h:303–304` 另有一处同类结构体登记（"同类结构体 ipv::RobustRefineParams（ipv_robust_refine.h）全部字段亦不可达：生产调用点传字面量 `RobustRefineParams{}`（默认构造）"）；`docs/algorithms/PLATESOLVE.md` 全篇无这些字段名/数值（检索式：`git grep -rn -e cd_freeze_threshold -e diverge_match_drop -e min_matched_final -e max_stars -- docs` 仅命中他模块的 `star_detection.max_stars`）。
- 差在哪：(1) 声明面把 31 个阈值组织成"参数面"并自称"默认值对齐 spec"，但跟踪面上不存在任何规定这些默认值的 spec 条文——所谓 spec 的证据在 `run/`（不入库，见 CR-41-26）；(2) 数值因此实际是**源码字面量常量**，却不具备"文档层数值"的可审性；(3) 机器锁 `ipv_dead_params_lock` 把"生产传默认构造"这件事钉成不可改动，等于把"参数面不可接线"固化为通过条件——一旦将来要把某个阈值接进配置（正当需求），该锁必须先改，而它当前没有配套的"接线后应删锁"条文。
- 后果：科学侧对外主张"精化阶段的容差/阻尼/收敛/防护阈值有据可循"不成立（本表 36 项中 29 项只能落 `待确认`，即无一有跟踪面锚）；工程侧对外主张"参数可配置"不成立（无人能配置）；并且任何对这些阈值的调整都要走"改源码 + 重编 DLL"，与 `03_star_detection.md:58` 展示的"配置键 + schema 正本"路线分叉。
- 定级：S2（不改当前产物字节，但使 AUD-402 的"逐个有处置"与"配置真值复算门"在本单元失去对象；纯声明面＋调用面事实，逐跳可写：`ipv_entry.cpp` → `ipv_solver.cpp:716` → `robust_refine_wcs`）
- 怎么算修好：①在 `docs/algorithms/PLATESOLVE.md` ALG-WCS-001 增一节"鲁棒精化数值面"，逐个给数值与推导/标定来源（含 tukey_c 的尺度路线配对约束，见 PHOTOMETRIC_FIT.md:35）；②把"确实要调的"若干键接进配置并登记进 `eng/contracts/schemas/` 与 `config_registry.json`，其余数值改为 `constexpr` 常量并从"参数结构体"降级为"实现常量"（消除"可配置"假象）；③相应地更新 `ipv_dead_params_lock` 的判据为"未被接线的字段必须存在于常量表"，并把该锁的语义写进 `docs/ci/`（防"改对反而判红"：只接线不删锁必红）；④同批改 `ipv_api.h:303–304` 的同类登记。
- 置信：CONFIRMED

### CR-41-24 · 网格配额采样的候选池超限后"按星等截断到 300"，恰好取消了本头宣称要替代的旧做法
- 轴：R（主）＋D
- 位点：ipv_robust_refine.h:8 `1. 网格配额采样选星 (100-300 颗, 空间均匀, 替代纯按星等取 60 颗)`；配套数值 :44 `int max_stars = 300; // 候选池上限`
- 上位依据：本头自身的设计意图（空间均匀采样以消除星等偏置）；对照 `docs/algorithms/PLATESOLVE.md:65–99`（§4a.1/§4a.2 把样本定义域与密度基数写成硬约束，要求"rho_img 的分子必须是实际进入 U 样本的成员数"），其精神即"样本域的构成必须可陈述、不得由亮度单方面决定"。
- 现状：`ipv_robust_refine.cpp:299–306` `// 6. 总数 > 300 按 mag 升序截断到 300; < 100 保持原样` → `std::sort(selected..., [&](int a,int b){ return mag_full[a] < mag_full[b]; }); selected.resize(params.max_stars);`。即：格内配额取完后，只要总数超 300 就重新按星等排序取最亮 300。
- 差在哪：稠密帧（检测数 > 300，正是宽视场/高银经天的常态）下最终样本由亮度单方面决定，网格配额在 293 行 `take = quota_per_cell` 处做的空间均匀性被 :302–306 抹掉——本头 :8 声称"替代纯按星等取"的那条路径在超限时**原样回来**。
- 后果：精化阶段的 CD/SIP 项拟合样本在稠密场系统性偏亮（空间上聚在亮星密集区），使"空间均匀配额采样"这一对本阶段的主张失真；样本偏置会通过 IRLS 权重影响最终 RMS 与畸变项。方向钉死：截断只减少样本数、不会增加，故这是"配额均匀性保证"这一**判据被绕过**（偏绿：声明的防护未生效），不是更严。
- 定级：S2（生产路径可达：solve_post_select → robust_refine_wcs → :299–306；不改科学公式定义，故不升 S1）
- 怎么算修好：把 :300–306 的超限处理改成**按格等比缩配额**（保持每格相对份额），或按格内最亮→全局均匀二次采样；若坚持按星等截断，则本头 :8 的"空间均匀"与 :44 的"上限"必须改成真实口径（"上限截断退回亮度序"），并在 ALG-WCS-001 登记；两种改法都要同步 `RobustRefineResult::n_pool`/`n_grid_cells` 的观测语义。
- 置信：CONFIRMED

### CR-41-25 · 同一份声明里三处规模数值互相不能同时成立，其中两个（1000、100）在实现中根本不参与任何判定
- 轴：C（主）＋R
- 位点：ipv_robust_refine.h:42 `int    total_target_narrow = 1000; // 小视场目标星数 (全选不限制)`、:45 `int    min_stars = 100;  // 候选池下限 (低于此值不强制填充)`、:8 `100-300 颗`
- 上位依据：本头 :35 自称"默认值对齐 spec"（跟踪面无 spec，见 CR-41-23），故唯一可核对对象是实现；`docs/algorithms/PLATESOLVE.md:238` 对**上一阶段**同类量给了明确导出式（`n_target = min(60, max(50, round(ρ_target·query_area/img_area)))`），可作本阶段应有形式的对照。
- 现状：`ipv_robust_refine.cpp:245` 读 `total_target_narrow`，但 :247 的 narrow 分支 `quota_per_cell = N`（全部检测数）而不使用该值；:308 `// 注意: 不强制填充到 min_stars (小视场星少不强制)` ⇒ `min_stars` 只出现在 :313 的日志格式串里；同时 :300 的 `max_stars=300` 无条件生效 ⇒ 声明的"全选不限制"与"1000 目标"都不成立，:8 的"100-300"区间实际是"≤300（无下限）"。
- 差在哪：一个结构体内三个规模数各自描述不同规则，其中两个在实现里没有任何作用点；注释"全选不限制"与同一结构体另一字段 `max_stars=300` 直接矛盾（§4 提醒的"两个适用域"这里不成立：两条注释指向同一 narrow 分支，且实现对该分支同时施加 300 上限）。
- 后果：读声明面无法预测实际样本规模（1000 与 100 都是幻觉），任何按声明面做的容量/性能/统计推断（例如"小视场取 1000 颗 ⇒ 精化条件数充裕"）失真。
- 定级：S3（不改产物字节；声明面自相矛盾）
- 怎么算修好：二选一——①让二者真生效：narrow 分支按 `total_target_narrow` 定配额，`min_stars` 作为 fail-closed 下限（不足即回退，与 :77 的 `min_matched_final` 同族处理）；②删字段，并把 :8 与 :42/:45 的措辞改成实现事实（"上限 300，无下限；窄视场全选后仍受 300 限"）。
- 置信：CONFIRMED

### CR-41-26 · 本头全部数值与 99.87% 成功率主张的唯一证据指向 `run/`（gitignore，不入库），跟踪面零锚
- 轴：S
- 位点：ipv_robust_refine.h:33 `详见 run/perf-fix/P27-dead-params/REPORT.md。不改变任何默认值/公式/容差。`；ipv_robust_refine.h:11 `4. 失败回退到 hi_order_rematch 结果, 不破坏 99.87% 成功率`
- 上位依据：`AGENTS.md §7 目录落位速查`（`run/ 临时产物/日志（gitignore，不入库）`）＋工作包 AUD-402 的处置档位定义（§2.5 第 3 档"实验标定……必须落在跟踪面，指 run/ 即判不合格"）；负责人已裁证据留存口径为"日志落 `实验/`、留可复现代码＋报告数据"（项目记忆"三留一不留"）。
- 现状：`git check-ignore -v run/WCS-AB-01/REPORT.md` → `.gitignore:17:run/*`（即所有 `run/**` 均不入库）；`git grep -rn "99.87"` 在全跟踪面无任何以该百分比表述 platesolve 成功率的命中（唯一近似命中是 `docs/algorithms/PHASE2_SAMPLER.md:237` 的"高斯下第 99.87 百分位"，与此处主张无关）；`lib/algorithms/platesolve/cpp/ipv/REPORT.md`（跟踪面）不含 `RobustRefine`/`m_lim` 任何数值（`git grep -n -e RobustRefine -e m_lim -- lib/algorithms/platesolve/cpp/ipv/REPORT.md` 零命中）。
- 差在哪：本文件把 31 个阈值、"死参数"结论与一个具体成功率数字全部交给一个不入库、会被轮次 GC 回收的路径作证，跟踪面上找不到任何一个可复核的数值结果位。
- 后果：`99.87%` 这一对外质量主张不可复核（连"哪 10/多少帧、哪个 gate、哪份 results"都指不出）；`不改变任何默认值/公式/容差` 的整改声明失去基线；AUD-402 的"逐个有处置"门在本文件只能大量落 `待确认`。
- 定级：S3（不改字节；证据面缺失）
- 怎么算修好：把支撑数值搬到跟踪面——报告与 results 落 `实验/`（或 `docs/algorithms/PLATESOLVE.md` 的登记节＋`artifacts/evidence/` 证据锚），本头只引用跟踪面路径；`99.87%` 改为"由 <跟踪面验收件> 的 N/M 帧通过复现"或直接删除；同批把 `run/perf-fix/...` 与 ipv_select.h 里的同类 run/ 引用一起处理（见 CR-41-31）。
- 置信：CONFIRMED

<!-- PROGRESS: 4/6 -->

---

# 5. lib/algorithms/platesolve/cpp/ipv/include/ipv_select.h

读法：整个文件 291 行从头读完（迁移说明 4–24、句柄访问器 32–44、辅助函数 46–66、割线迭代块 68–132、§4a.5 块 134–182、四个选星入口 190–287、star_det v1 格式 223–287）。
对账三面：`docs/algorithms/PLATESOLVE.md:65–130（§4a.1–§4a.5）、:237–243（ALG-WCS-001 §11 源码锚表）`；`docs/plugins/algorithms_phase1/03_star_detection.md:58–70`（配置键正本）＋`eng/contracts/schemas/phase_config_normalize.schema.json:368–370,443–451`＋`eng/packaging/config/config_registry.json:1983–2019`；`docs/contracts/API_CONTRACTS.csv:328–335`；实现 `lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp`（:275–330 密度、:334–350 m_cut、:375–556 割线迭代、:595–664 封装、:853–900 选星）与 `lib/infrastructure/scheduler/src/module_adapters.cpp:3417–3420`。

## 5.1 §2.5 逐数值处置表（33 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :17 | 饱和全选阈 | `50` | "饱和>50 全选"的图像侧规则 | 待确认 | 与实现相反：`ipv_select.cpp:867` `// 候选池 = 非饱和检测 (饱和检测排除, 不回填)`，:873 `if (i < n_sat_flag && saturated[i]) { ++n_sat; continue; }`；正本 `PLATESOLVE.md:237` 亦写"非饱和候选按 mag 升序取前 img_n_target"（§4a.1）⇒ CR-41-27 | 保守方向：以 §4a.1 为准改注释；影响范围=U 样本构成（下游三角形匹配） | CONFIRMED |
| :17 | 补足目标数 | `50` | "饱和+非饱和补足 50" | 待确认 | 同上；且 50 与 `ipv_types.h:249` 的 `img_n_target = 20`、`ipv_types.h:226`/`ipv_api.h:277` 登记的"=60 硬覆盖"三个数互不相等 ⇒ CR-41-27 | 同上 | CONFIRMED |
| :72 | 旧密度公式系数 | `5` | ρ(G)=5×10^(1.3(G−10)) 的前因子 | 待确认 | 本头自称已被替换；跟踪面已无该公式（检索式 `git grep -rn "1.3" -- docs/algorithms docs/science` 无该式命中）；作为"被废弃公式"保留在注释里，无条文承载 ⇒ 属"文档内禁历史"反例 | 保守方向：删除或移入跟踪面报告；影响范围=无数值作用，仅误导读者 | CONFIRMED |
| :72 | 旧密度公式斜率 | `1.3` | 同上 α | 待确认 | 同上 | 同上 | CONFIRMED |
| :72 | 旧密度公式零点 | `10` | G−10 偏移 | 待确认 | 同上 | 同上 | CONFIRMED |
| :72 | 实测 α 中位 | `0.2885` | 新 alpha 先验的依据 | 待确认 | 实际生效值在 `ipv_types.h:260 double m_lim_alpha_prior = 0.2885;`，其证据被 :74 指向 `run/perf-fix/P4-magiter/REPORT.md`（不入库，`git check-ignore` ⇒ `.gitignore:17`）；跟踪面 `docs` 内 `git grep -rn "0.2885" -- docs` 零命中 ⇒ CR-41-31 | 保守方向：把 10 帧实测分布（中位/四分位）搬进 ALG-WCS-001；影响范围=迭代收敛速度与查询深度 | CONFIRMED |
| :73 | 旧法失效帧数 | `4/10` | "4/10 真实帧 FOV 内不足 n_target" | 待确认 | 同上（唯一承载在 run/ 报告） | 同上 | CONFIRMED |
| :73 | 旧兜底星等 | `mag=22` | 被移除的兜底查询值 | 待确认 | 现值仅存为迭代上界：`ipv_types.h:271 m_lim_clamp_hi = 22.0; // 仅迭代夹取, 不再作兜底查询值` ⇒ 本行描述的是已废止行为 | 保守方向：删除历史叙述 | CONFIRMED |
| :74 | Gaia 每文件返回上限 | `200000` | 触顶判定容量 | 结构性不适用 | 非科学量：第三方客户端容量；锚 `lib/infrastructure/gaia_xpsd_client/src/gaia_client.c:33 #define MAX_STARS_RESULT 200000`（＋ :63 "文档化截断上限 MAX_STARS_RESULT（200000/文件）不属丢弃"）；生效点 `ipv_types.h:276 m_lim_gaia_cap_per_file = 200000.0` | — | CONFIRMED |
| :74 | 证据锚 | `run/perf-fix/P4-magiter/REPORT.md` | 本节全部标定结论 | 待确认 | **不合格锚**（`.gitignore:17 run/*`；`ENGINEERING_SPEC.md:134` "run/（gitignore：开发/CI 过程产物与过程日志）"）⇒ CR-41-31 | 见 CR-41-31 | CONFIRMED |
| :85 | `query_count <= params.m_lim_max_iter` | 引用参数（默认 4，可配置） | 迭代次数上界 | 公式导出 | 实现真按参数判：`ipv_select.cpp:427 max_q = (m_lim_max_iter>0)?...:4`；配置面 `03_star_detection.md:70`＋schema :451＋`module_adapters.cpp:3420` | — | CONFIRMED |
| :86–99 | `MagIterOutcome` 各字段默认 | `0`/`0.0`/`false` | 空结果初值 | 结构性不适用 | 非科学量：哨兵初值；字段语义与 §4a.4 一一对应（`ipv_types.h:102–121` 同域） | — | CONFIRMED |
| :101 | 割线更新式 | `m_next = m + (log10(N_target) − log10(N))/alpha` | 迭代核心式 | 公式导出 | alpha ≡ dlog10N/dmag ⇒ 一步Newton式即由该导数定义导出；实现按 log10 域差分（`ipv_select.cpp:451 double m_prev = 0.0, logN_prev = 0.0;` 起的差分更新），代码真按式算 | — | CONFIRMED |
| :102 | m0 常数项 | `6` | 曝光-焦距经验式零点 | 待确认 | 实现同式（`ipv_select.cpp:438`）；**出处引用被删空**：`ipv_select.cpp:334 // compute_initial_mag_cut - 计算初始极限星等 ( 公式)` ⇒ CR-41-32 | 保守方向：补出该经验式的原始出处或本仓标定件；影响范围=初值质量→查询次数 | CONFIRMED |
| :102 | m0 焦距指数 | `1.5` | log10(f) 系数 | 待确认 | 同上（无条文、无推导；`git grep -rn "1.5 \* std::log10" -- docs` 零命中） | 同上 | CONFIRMED |
| :102 | m0 曝光指数 | `2` | log10(t) 系数 | 待确认 | 同上；另 :102 未写"缺真实曝光时取名义 180 s"（实现 `ipv_select.cpp:635` 传 `params.m_lim_m0_exposure_s`）⇒ CR-41-33 | 同上 | CONFIRMED |
| :102 | m0 偏置 | `+ m_lim_m0_offset`（−4.0） | 系统偏差修正 | 实验标定 | 实现注释 `ipv_select.cpp:435 "初值 m0 (曝光公式在真实帧上系统性偏暗 3.5–5.5 mag -> m_lim_m0_offset=-4)"`；该"实测 3.5–5.5 mag"的唯一承载仍是 `run/`（本头 :74）⇒ 无跟踪面标定件 ⇒ 落 `待确认` | 保守方向：给 10 帧偏差分布＋跟踪面 results；影响范围=首查询深度 | CONFIRMED |
| :102 | m0 上界 | `13`（写作常数） | 初值上限 | 待确认 | 实现是 `ipv_select.cpp:424 const double m0_hi = std::min(clamp_hi, 13.0);` —— 实为 `min(m_lim_clamp_hi, 13)`，与本头 :109 "m 全程夹取 [clamp_lo, clamp_hi]" 并写两个不同上界 ⇒ CR-41-29（同族：一个量两处口径） | 保守方向：声明改写为 `min(m_lim_clamp_hi, 13)`；影响范围=clamp_hi 被调时初值随之变（非固定 13） | CONFIRMED |
| :103 | `N_target = n_target × m_lim_safety` | 默认 safety 3.0 | 目标星数放大 | 公式导出 | 实现 `ipv_select.cpp:432`；配置面 `03_star_detection.md:68`＋schema :443＋`module_adapters.cpp:3417`；取值依据登记在 `ipv_types.h:264`（"10 帧样本上…最小值（余量 +0.36..+1.07 mag）"）——但该 10 帧样本结果只在 run/ ⇒ CR-41-31 | — | CONFIRMED |
| :104 | alpha 限幅 | `[min, max]`（默认 1e-3 / 100.0） | 差分更新限幅 | 待确认 | 实现 fail-closed 校验 `ipv_select.cpp:411/417`；`max=100` 对 dlog10N/dmag（正常量级 0.1–1）过宽 2–3 个数量级，无条文；本头只写"限幅 [min, max]"不给典型量级 | 保守方向：给 alpha 的物理上界（Gaia 计数对星等的最大斜率）；影响范围=异常差分时步长被放大 | CONFIRMED |
| :105 | 终止容差 | `density_tolerance`（默认 0.1） | 收敛判据 | 待确认 | 值在 `ipv_types.h:278`；**该键无任何配置面**（`git grep -rn "density_tolerance" -- eng/contracts docs/plugins` 零命中）却由 `ipv_types.h:227` 自记"代码真实消费但配置不可达"⇒ CR-41-30 | 保守方向：要么登记为配置键，要么在 ALG 层给 0.1 的推导；影响范围=收敛判定 | CONFIRMED |
| :106 | 空结果步进 | `m_lim_zero_step`（默认 3.0 mag） | N=0 时向更暗的步长 | 文献值（本仓条文） | `PLATESOLVE.md:100–108`（§4a.3 极限星等迭代的单调性与步进方向）规定"星等窗单调 ⇒ 唯一有信息方向是更暗"；实现 `ipv_select.cpp:428`；步长具体值 3.0 未见于条文（条文只给方向不给量） | 保守方向：条文补步长选择依据；影响范围=空窗帧的查询次数 | CONFIRMED |
| :109 | 全程夹取窗 | `[m_lim_clamp_lo, m_lim_clamp_hi]` = 6.0–22.0 | 星等搜索窗 | 待确认 | 6.0 注释"低于此无星"、22.0 注释"仅迭代夹取"（`ipv_types.h:270–271`），两值无跟踪面条文；Gaia XPSD 的实际星等覆盖域未在此引用 | 保守方向：给星表深度分布来源（Gaia DR3 极限星等）；影响范围=深场/浅场可达性 | CONFIRMED |
| :140 | 实测 n_sat | `94` | M42 T2 M1 首帧饱和数 | 实验标定 | 唯一承载 `run/WCS-AB-01/REPORT.md`（本头 :144）⇒ 不合格 ⇒ CR-41-31 | 保守方向：把该帧事实搬进 PLATESOLVE §4a.5 例证或跟踪面证据件；影响范围=例证可核性 | CONFIRMED |
| :140 | 实测 U 位次域 | `78..154` | 同上帧 U 在全亮度序中的位次区间 | 实验标定 | 同上 | 同上 | CONFIRMED |
| :142 | 旧窗口位次域 | `1..60` | "FOV 内最亮 n_target 颗" | 待确认 | 与 :17 的 50、`ipv_types.h:249` 的 20 不一致；60 的来源是 `ipv_select.cpp:318 int n_target_cap = 60;`（与 `PLATESOLVE.md:238` 的 `min(60, max(50, …))` 一致）⇒ 数值本身可锚，但本头同一对象写了 50 与 60 两个数 ⇒ CR-41-27 | — | CONFIRMED |
| :142 | 实测交集 | `0/60` | U∩W 交集 | 实验标定 | `run/WCS-AB-01/REPORT.md` ⇒ CR-41-31 | 同上 | CONFIRMED |
| :143 | 实测 Gaia G 域 / m_lim | `[11.92,13.68]` / `11.896` | 对应体星等与当时查询深度 | 实验标定 | 同上；`11.896 < 11.92` 的"落在窗外"结论自洽（差 0.024 mag），但该判据只存在于注释与 run/ ⇒ CR-41-31 | 同上 | CONFIRMED |
| :144 | 实测票数退化 | `1131 → 101` | 三角形投票真峰高度 | 实验标定 | 同上 | 同上 | CONFIRMED |
| :147/:151/:152 | `n_depth`、退化条件、fail-closed | 无量纲关系 | §4a.5 两条正向约束 | 公式导出 | 与 `PLATESOLVE.md:126–130`（§4a.5 已成正文）＋`:98`（"后者按 §4a.5 由样本亮度深度基数导出"）一致；实现交付面 `SelectDomain`（:156–160）与 `select_catalog_window`（ipv_select.cpp 侧）承载；`n_sat=0` 退化性声明与 :867 的饱和排除规则一致（⇒ 反证 :17 陈旧） | — | CONFIRMED |
| :196/:210/:241/:258 | 输出规模 | `U ~50 颗 + W ~75-150 颗` | 四个入口的输出声明 | 待确认 | 与 §4a.5 的位次对齐窗口冲突（`select_catalog_window` 返回与 `dom.sel_rank` 等长 ⇒ \|W\|=\|U\|）；且 50 与 img_n_target（20/硬覆盖 60）、n_target（50..60）三数并存 ⇒ CR-41-29 | 保守方向：改为"由 img_n_target 与 n_depth 导出"的指针式写法（照 `03_star_detection.md:58` 的口径纪律）；影响范围=调用方容量预估 | CONFIRMED |
| :226–227 | 检测结果布局 | `FLOAT64 [N,6]`，列 `0..5` | star_det v1 数据面契约 | 结构性不适用 | 非科学量：ABI 布局（x,y,flux,mag,saturated,has_saturated）；与 `aio_hips.h`/star_detector 侧一致；列 4/5 的取值域（bool 编码为 double 的哪些值）声明面未给 | 保守方向：注明"非 0 即真"或 0/1 域；影响范围=跨 DLL 读取时的判真方式 | CONFIRMED |
| :119 | 生产路径计数 | `4 条` | "4 条生产路径都经 gaia_query_mag_iterative" | 结构性不适用 | 计数陈述（非科学量）；与 :124 声明的"遗留"函数 `density_match_iterate` 并存 ⇒ 该函数在仓内是否仍有消费者见 CR-41-34 | — | CONFIRMED |

## 5.2 findings

### CR-41-27 · 本头开头写的图像侧选星规则与实现、与 ALG 正本、与本头自己的 §4a.5 块三处全部相反
- 轴：C（主）＋R
- 位点：ipv_select.h:17 `// - 图像侧选星 (饱和>50全选 / 饱和+非饱和补足50)`
- 上位依据：`docs/algorithms/PLATESOLVE.md:237` "选星 + U 构建 | ipv_select.cpp:787-837（`select_image_stars`：非饱和候选按 mag(box积分) 升序取前 img_n_target，§4a.1）"；同一文件 §4a.1 把 U 的定义域定为非饱和检测；本头 :138–140 自己也写 "U 的定义域是「非饱和检测」(§4a.1)。帧内存在饱和检测时, U 的成员在**检测表全亮度序**…中的位次整体下移"。
- 现状：实现 `ipv_select.cpp:867` 注释"候选池 = 非饱和检测 (饱和检测排除, 不回填)"，:873 直接 `continue` 跳过饱和星；不存在"饱和>50 全选"也不存在"补足"逻辑。目标数在实现里是 `img_n_target`（`ipv_types.h:249` 声明 20，而 `ipv_types.h:226`/`ipv_api.h:277` 登记"=60 硬覆盖"）。
- 差在哪：一条注释把"饱和星优先入选"写成本模块核心算法，而真实规则是"饱和星一律不入 U"；同一文件相隔 120 行两处对同一规则给出互斥表述；被引用的 50 与实现生效的 20/60 也不同。
- 后果：按 :14–21"核心算法"摘要复核本模块的人（复审、迁移、写测试）会构造含饱和星的 U 样本，而 §4a.1 与 `PLATESOLVE.md` 判其非法；饱和星的 box 积分星等不可信（这正是 §4a.1 排除它的原因），一旦按注释实现即把不可信样本送进三角形匹配与极限星等迭代。
- 定级：S2（声明面直接给出与正本相反的采样规则；不改当前字节，但它是本模块对外可读的算法摘要）
- 怎么算修好：:14–21 摘要改为与 §4a.1 一致的口径（"非饱和候选按 box 积分星等升序取前 img_n_target；饱和星仅参与全亮度序位次计数，不入 U"），并且不复制具体数字而指向 `img_n_target` 的载体；同时把 `ipv_types.h:249` 的 20 与"60 硬覆盖"这处分叉收口（要么删硬覆盖、要么把默认值改成 60 并在 ALG 层登记），三处同批改，否则改注释反而与实现数值对不上。
- 置信：CONFIRMED

### CR-41-28 · 头注释自称"§4a.5 本轮新增、待写入 docs/algorithms/PLATESOLVE.md"，而该节早已成正文并被同文档引用
- 轴：C
- 位点：ipv_select.h:135 `// ALG-WCS-001 §4a.5（本轮新增, 待写入 docs/algorithms/PLATESOLVE.md）——`
- 上位依据：`docs/algorithms/PLATESOLVE.md:126` `### 4a.5 图像侧样本 U 与星表侧样本 W 必须同亮度序位域`（已成文，:128 起给定义），且 `:98` 已前向引用它（"后者按 §4a.5 由样本亮度深度基数导出"）。
- 现状：代码注释仍标"待写入"，实现（`SelectDomain`、`select_catalog_window`）按 §4a.5 已接线。
- 差在哪：条文状态标记与条文存在性相反——读者据此会认为该约束"只有代码有、正本还没写"，从而不把它当作可审计的规范。
- 后果：ALG 层的"层级向下·口径唯一"关系被这条陈旧标记误导；`DISP/CHK` 类核对（活动文档状态与代码注释一致，见 `docs/ci/01_CHECKS.md:31 DOC-INDEX`"代码注释 docs/ 路径"项）失去可信输入。
- 定级：S3
- 怎么算修好：删除"（本轮新增, 待写入 …）"，改为 `ALG-WCS-001 §4a.5`（只给指针不复述），并全模块清一遍同类"本轮新增/待写入"残留（检索式：`git grep -n "待写入\|本轮新增" -- lib`）。
- 置信：CONFIRMED

### CR-41-29 · 同一"样本规模/窗口"量在本头被写成三套数，且与实现的导出式不属同一套
- 轴：C
- 位点：ipv_select.h:196（同 :210/:241/:258）`// 输出: StarSelection (U ~50 颗 + W ~75-150 颗 + 元数据)`
- 上位依据：`PLATESOLVE.md:238` 给的是导出式而非固定数——`n_target = min(60, max(50, round(ρ_target·query_area/img_area)))`；§4a.5 第二条约束（本头 :148–150 自述）要求 W 取"与 U 同位次的成员"，其长度等于 `dom.sel_rank.size()` = \|U\|。
- 现状：实现里 \|U\| 由 `img_n_target` 决定（声明默认 20、被硬覆盖 60），\|W\| 在 §4a.5 路径下与 \|U\| 同长；"~75-150"只在旧的"最亮 n_target × 密度富余"时代成立；本头 :17 又写 50。
- 差在哪：一个输出规模在四处注释里是"50/75–150"，在正本里是"50..60 由密度导出"，在实现里是"img_n_target（20，被 60 覆盖）"；且 §4a.5 改造后 \|W\|=\|U\| 使"75-150"直接失效。
- 后果：调用方按声明做缓冲/容量/性能预估会错（最大 3 倍偏差）；按注释复核 §4a.5 交付面的人会认为窗口仍是最亮 N 颗（即 CR-41-27 的旧规则）。
- 定级：S3
- 怎么算修好：四处注释统一改为指针式（"U 长度 = img_n_target；W 长度 = \|U\|（§4a.5 位次对齐）；n_target 见 ALG-WCS-001 §4a.2 导出式"），不复制数字；若确实需要上界，改为从 `StarSelection` 字段读，而非注释。
- 置信：CONFIRMED

### CR-41-30 · 迭代所依赖的 9 个参数在"可配置"这一事实上有三种互斥登记，其中 6 个始终没有任何配置面
- 轴：C（主）＋D
- 位点：ipv_select.h:104–110（把迭代写成 `m_lim_alpha_prior / 限幅 / density_tolerance / m_lim_zero_step / m_lim_clamp_lo/hi` 的函数）
- 上位依据：`docs/plugins/algorithms_phase1/03_star_detection.md:58` "本段一切「规模/上限」类数字都由配置键与星表查询口径导出，本节只给导出关系与指针"；`eng/contracts/schemas/phase_config_normalize.schema.json:368–370`（`limiting_mag_safety`/`query_radius_factor`/`limiting_mag_max_iter` 三键）＋ `:443–451`；`eng/packaging/config/config_registry.json:2007` "…本键此前代码可读但配置不可达，本次收口后可达"；`lib/infrastructure/scheduler/src/module_adapters.cpp:3417–3420` 确实把三键写进 `ipv_params`。
- 现状：`lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h:226–227` 仍登记 "代码真实消费但配置不可达: gaia_* 两项 + m_lim_* 全部 + density_tolerance"；schema 只收 3 个键，`m_lim_alpha_prior(0.2885)`、`m_lim_alpha_min/max`、`m_lim_m0_offset(-4)`、`m_lim_clamp_lo/hi`、`m_lim_zero_step(3.0)`、`density_tolerance(0.1)`、`m_lim_m0_exposure_s(180)` 全部只有源码字面量；本头对上述任何一者的来源只字不提。
- 差在哪：同一组参数在配置合同面（可配）、代码登记面（不可配）、算法声明头（不提来源）三处说法互斥；而 03_star_detection.md:58 的"一切规模/上限数字都由配置键导出"这条口径纪律对本头描述的迭代并不成立。
- 后果：迭代深度与查询次数（科学侧"宁多勿少"的极限星等）实际由无条文的源码常数决定，且 `docs/ci/01_CHECKS.md:237` 的"P4 配置真值复算门"只覆盖 `star_detection.max_stars / photometry.fit.max_stars / snr.max_sources` 三键，对这些键既不比较也不登记 ⇒ 配置默认值分叉无人判红。
- 定级：S2（方向钉死：无源常数不会自动改变行为，但任何一次源码改动都不被门拦截 ⇒ 一致性判据偏绿）
- 怎么算修好：①先把三处登记收敛成一条（推荐：以 schema 为准，删除 `ipv_types.h:226–227` 的过期句）；②把确有调优需要的键（至少 `density_tolerance`、`m_lim_alpha_prior`）纳入 `phase_config_normalize.schema.json`＋`config_registry.json`＋`module_adapters.cpp` 读取，并把它们加进"配置真值复算"门（`docs/ci/01_CHECKS.md:237` 的键清单）；③其余保持源码常数的，在 ALG-WCS-001 数值节登记并给推导；④本头改为"参数来源=配置键 X / ALG 登记 Y"的指针写法。
- 置信：CONFIRMED

### CR-41-31 · 本头 7 处标定/实测数值（含 0.2885 这一实际生效先验）的唯一证据全在 `run/`，跟踪面零承载
- 轴：S
- 位点：ipv_select.h:74 `改用割线迭代 + 触顶防御; 证据见 run/perf-fix/P4-magiter/REPORT.md。`；ipv_select.h:144 `iter_trans_solve 全阶数失败。证据: run/WCS-AB-01/REPORT.md。`
- 上位依据：`ENGINEERING_SPEC.md:134` "run/（gitignore：开发/CI 过程产物与过程日志…）"；`AGENTS.md §7`（`run/ 临时产物/日志（gitignore，不入库）`）；工作包 §2.5 第 3 档对"实验标定"的硬要求"必须落在跟踪面，指 run/ 即判不合格"；负责人证据留存口径"三留一不留：日志落 `实验/`、可复现代码＋报告数据保留、**运行产物一律不留**"（项目记忆）。
- 现状：`git check-ignore -v run/WCS-AB-01/REPORT.md` → `.gitignore:17:run/*`；跟踪面对 `0.2885`、`3.5–5.5 mag`、`4/10 真实帧`、`n_sat=94 / 78..154 / 0/60 / [11.92,13.68] / 11.896 / 1131→101` 全部零命中（检索式：`git grep -rn -e 0.2885 -e 11.896 -e 1131 -- docs 实验 artifacts`）。
- 差在哪：这些数字直接进入生产行为（`m_lim_alpha_prior=0.2885`、`m_lim_m0_offset=-4`、§4a.5 的两条约束），却没有任何一个能在跟踪面被复核；`run/` 会被 `eng/tools/run_gc.py` 按轮次回收（AGENTS §3），回收即证据彻底消失。
- 后果：AUD-402 的"逐个有处置"门在本头只能大面积落 `待确认`/不合格 `实验标定`（本表 33 项中 6 项标 `实验标定` 而唯一承载即该 run/ 报告、另有 3 项 `待确认` 的锚同样只写作 run/ 路径，合计 9 项直接进入生产数值的标定无跟踪面凭据）；对"极限星等迭代已按真实 Gaia 密度订正"与"§4a.5 是由实测 M42 帧逼出的失效面"两项对外主张，第三方复核者拿不到任何数据。
- 定级：S3（无产物错值；证据面不可核）
- 怎么算修好：把两份报告的结论性数值（帧列表、α 分布、位次/票数对照）搬进跟踪面——按负责人口径落 `实验/`（含可复现代码与 results），或落 `docs/algorithms/PLATESOLVE.md` §4a 的例证小节＋`artifacts/evidence/`；本头只引用跟踪面路径；同时把 `run/` 引用列入 CHK-DANGLING/DOC-INDEX 的扫描面，防止新代码再引入（`docs/ci/01_CHECKS.md:31`）。
- 置信：CONFIRMED

### CR-41-32 · 迁移来源引用被删空且被引文件从未入库，构成"悬空即缺陷"
- 轴：C（主）＋S
- 位点：ipv_select.h:14 `// 核心算法 (与 一致, 从 ss_core.cpp 迁移):`；:51 `// 计算 FOV 与密度 (从 迁移)`；:191 `// Phase 0: StarSelector (ipv_select.cpp) - 复用 算法`
- 上位依据：`ENGINEERING_SPEC.md:162` "4. **悬空即缺陷**：索引指向的文件/章节必须存在，文档引用的代码路径必须真实，CI 检查悬空引用"；`docs/ci/01_CHECKS.md:30 CHK-DANGLING`、`:31 DOC-INDEX`（含"代码注释 docs/ 路径；悬空即缺陷、fail-closed"）。
- 现状：`git ls-files | grep -c -E "ss_core|vm45"` = 0，且删除历史中亦无这些文件（`git log --diff-filter=D --name-only` 无命中）⇒ 按 §1 的"跟踪集＋删除历史"判据，`ss_core.cpp` 与 `vm45_select.cpp` 在本仓从未存在；另有 5 处引用对象被删空成 "( 迁移)"/"(与 一致)"/"复用 算法"/"( 公式)"（同形态在实现文件 `ipv_select.cpp:2/13/334`、`ipv_sip.cpp:12/364`、`ipv_angle.h:38` 亦有，合计 13 处）。
- 差在哪：三处引用语法上要求一个名字而名字位置为空；被引的迁移源文件在本仓无对应物 ⇒ 摘要声称"与 X 一致"这一核对关系不可执行；`( 公式)` 直接抹掉了 m0 常数 6/1.5/2 的唯一出处线索。
- 后果：常数出处链断（CR-41-31 的同一后果，但这条是引用面本身破洞）；`CHK-DANGLING`/`DOC-INDEX` 若覆盖代码注释则应判红，若未覆盖则该门有盲区——两种情况都需要指出。
- 定级：S3
- 怎么算修好：把删空处改写为**可解析**的表述：要么给出本仓内真实存在的锚（如 `docs/algorithms/PLATESOLVE.md §4a`、本仓自己的 SCI/ALG 编号），要么删除"与 X 一致"这类不可复核的等价声明；m0 公式给出本仓推导/标定件路径；若这些名字属外部项目而不宜出现，则按"引用只写本仓可核对象"的口径重写句子（不留空括号）。
- 置信：CONFIRMED

### CR-41-33 · 声明的迭代式两个自变量（`t_s` 与 `n_target`）在三条生产入口各自取不同含义/来源，本头自称"唯一映射点"实为模块内唯一
- 轴：C（主）＋S＋R
- 位点：ipv_select.h:102–103 `// - 初值 m0 = clamp(6 + 1.5*log10(f_mm) + 2*log10(t_s) + m_lim_m0_offset, clamp_lo, 13)` / `// - N_target = n_target × m_lim_safety`；ipv_select.h:119–121 `// 唯一映射点: ipv_select 的 4 条生产路径都经 gaia_query_mag_iterative 调它,`
- 上位依据：`docs/plugins/algorithms_phase1/03_star_detection.md:67` "缺省时由 `ipv::estimate_mag_lim_iterative` 按 `focal_length_mm`、画幅、`EXPTIME` 迭代派生（宁多勿少）"；`:68` "limiting_mag_safety | 3.0 | —— | 极限星等迭代的目标星数倍率（`N_target = n_target × safety`）"；`:64` "`max_stars` | 20000 | 颗 | 检测定义域上限…合同域 = **[20000, 50000]**"。
- 现状：同一函数有三处生产实参——
  ① `ipv_select.cpp:634–635`（ipv 模块内 4 条选星路径的公共封装，即本头 :119 所说的映射点）：`estimate_mag_lim_iterative(qf, n_target, focal_length_mm, params.m_lim_m0_exposure_s, params, logger)`，其中第 4 实参是**名义曝光 180.0 s**（`ipv_types.h:268 // 名义曝光(s); 调用方未提供真实曝光时使用`），`n_target` 为 `min(60, max(50, ·))`（`ipv_select.cpp:318–319`）⇒ `N_target ≈ 150–180`；
  ② `lib/infrastructure/scheduler/src/module_adapters.cpp:3655–3656`（IR / catalog_guided 权威检测路径）：`estimate_mag_lim_iterative(query_fn, cfg.max_stars, focal_mm, exptime, cfg.ipv_params, nullptr)`，`exptime` 是本帧真实 `EXPTIME`（:3586–3591 先 fail-closed 校验），`cfg.max_stars` = `star_detection.max_stars ∈ [20000, 50000]`（:3346–3354）⇒ 同一 `N_target = n_target × safety` 式在此给出 **60000–150000**；
  ③ 配置显式给 `limiting_mag` 时完全不走迭代（:3614–3616，且 :3636 直接 `converged = true`，与 §4a.4"未触顶才算真收敛"的语义不同源）。
  另：本头 :119 的"唯一映射点"仅对 `ipv_select` 内部成立（全仓第二处调用在 ②，见检索式 `git grep -n "estimate_mag_lim_iterative" -- lib`，共 2 个调用点）。
- 差在哪：`t_s` 在①是常数、在②是真实曝光 ⇒ 同一帧在 platesolve 与 IR 两条路径上的迭代初值不同（`2·log10(180)=4.58` 与 `2·log10(EXPTIME)` 之差）；`n_target` 在①是"选星样本数"、在②是"检测定义域 top-N"，相差 3 个数量级，而放大倍率 `m_lim_safety`（同一配置键 3.0）在两侧被当作同一个量使用；③的 `converged=true` 未经过迭代收敛判据。
- 后果：星表查询深度（极限星等）在两生产入口不一致 ⇒ "同一帧的权威检测定义域与 platesolve 星表窗一致"这一对外主张不成立；②侧 `N_target` 达 6–15 万使 `m_lim_gaia_cap_per_file=200000` 触顶概率与 `m_lim_max_iter=4` 截断概率显著上升（本头 :90/:107 定义的 capped 语义因此在两侧含义不同）；①侧 offset（−4 mag，按"真实帧"标定）与名义 180 s 叠加后与合同承诺的"按 EXPTIME 派生"不对应。方向钉死：`m_lim_max_iter` 到界即以末次成功结果继续交付（`converged=false` 不阻断），故迭代不足只会让查询窗**偏浅**（样本偏少），这是偏绿方向。
- 定级：S2（三入口实参逐字读到，触发路径逐跳可写；未实测净偏差与触顶率 ⇒ 不升 S1）
- 怎么算修好：①本头把 `t_s`/`n_target` 的**来源**写进声明（`t_s`=本帧 EXPTIME，缺失即 fail-closed 而非取名义值；`n_target` 的语义按调用方区分，或在②侧改用选星样本数）；②统一 `m_lim_safety` 的作用对象，或在 ALG-WCS-001/`03_star_detection.md:68` 拆成两个键（选星放大率 / 定义域放大率）；③③的显式 `limiting_mag` 路径不得置 `converged=true`（改为 `explicit_override` 观测位），否则 §4a.4 的"真收敛"口径被旁路；④`m_lim_m0_offset` 与①②的曝光来源一起重标定；⑤同批更新 :119 的"唯一映射点"表述为"本模块内 4 条路径；全仓另有 `module_adapters.cpp` 一处"。
- 置信：CONFIRMED

### CR-41-34 · 入参单位与值域在三方对账中无处落地：合同表把值域推给"per SCI"，所引 SCI 是不含数值域的范围文档，本头自身不给任何值域
- 轴：C（主）＋S
- 位点：ipv_select.h:52–61（`compute_fov_density` 的 15 个入参，除 `n_img_bright` 外无一处标注单位/有效域）
- 上位依据：`docs/contracts/API_CONTRACTS.csv:328` 对本函数的登记行：单位列 `ADU/pixel/deg per header`、值域列 `per SCI valid_range`、SCI 列 `SCI-SCOPE-001`、末列 `VERIFIED`；而 `SCI-SCOPE-001 = docs/science/SCIENCE_SCOPE.md`（"产品目标/处理链位置"权威，见 `docs/algorithms/PHASE2_MOSAIC_WRITE.md:35` "SCI-SCOPE-001: 处理链位置权威"），不承载任何参数值域。
- 现状：真实的单位/导出式只存在于别处与实现里——`PLATESOLVE.md:237` 记 `s0=206.265·pixel_um/focal_mm`、`:238` 记 n_target 导出式；本头对 `focal_length_mm`、`pixel_size_um`、`img_width/height`、`gaia_density_ratio`、`gaia_query_radius_factor` 一律不写单位与允许域；`focal_length_mm<=0` 的拒绝在实现（`ipv_select.cpp:275–280`：先预置 `rho_img=rho_target=n_target=0`，随后 `if (focal_length_mm <= 0.0) { logger->error(...); return; }` —— **返回 void 且 `s0 / fov_diag_deg / query_radius_deg / query_area_sqdeg / img_area_sqdeg` 五个出参不写**，调用方若未自行清零即沿用旧值）。另 `density_match_iterate`（:125–132）在 CSV 里同样标 `VERIFIED`，而本头 :124 自称"遗留; P4-magiter 起生产路径不再使用"。
- 差在哪：合同表把"值域"这一栏以模板文字推给一个不含值域的 SCI 文档；声明头自己不给值域；于是三方里没有任何一方真的规定了这些量的域——而该 CSV 行状态是 `VERIFIED`。
- 后果：任何按合同表写参数校验/负例注入的下游都会得到一个空判据；`compute_fov_density` 的非法入参分支没有错误码可判（签名 `void`），只留一条 error 日志，且五个出参不被写 ⇒ 调用方拿到"看起来返回成功、实为半初始化"的几何量（s0 直接进 `PLATESOLVE.md:237` 的样本坐标换算与 n_target 导出式）。
- 定级：S2（方向钉死：非法入参不产生错误码、出参半保留 ⇒ 少一条拒绝路径 ⇒ **偏绿**；触发路径 module_adapters.cpp:3599 的 `compute_fov_density(...)` 调用可逐跳写，但需 focal_length_mm<=0 的真实输入才会触发）
- 怎么算修好：①本头为每个入参写单位与允许域（照 `PLATESOLVE.md:237–238` 的既有形式），并把非法入参的语义改为"全部出参显式清零＋返回错误码（或 `bool ok`）"——现签名 `void` 无法表达失败 ⇒ 需改签名，属合同面变更，须同步 `API_CONTRACTS.csv:328` 的 sig 列与该 fixture 目录下的样本；②`API_CONTRACTS.csv` 的 `valid_range`/单位列改为指向具体条文（例如 `ALG-WCS-001 §4a.2`），不接受 `per SCI valid_range` 的模板值；③已废弃的 `density_match_iterate` 在本头标注状态与退役计划（或按 `ENGINEERING_SPEC §2` 的历史代码处置规范登记，纳入 CHK-RETIRED-CODE 台账）。
- 置信：CONFIRMED

<!-- PROGRESS: 5/6 -->

---

# 6. lib/algorithms/platesolve/cpp/ipv/include/ipv_sip.h

读法：整个文件 58 行从头读完（头注释 4–17、include 19–21、fit_sip 的模型/参数/返回说明 25–54）。
对账三面：实现 `lib/algorithms/platesolve/cpp/ipv/src/ipv_sip.cpp`（:12–17 残差模型与系数布局、:65–75 `get_basis_table`、:281–330 前置校验与隐式降阶、:344–400 归一化与采样、:472–477 反归一化）；类型面 `lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h:73–83`（`SIPCoeffs`）；状态/缺陷登记 `lib/algorithms/platesolve/README.md:79–86（生产符号链）、:109–112（DISP-WCS-003/004）`、`lib/algorithms/platesolve/cpp/ipv/REPORT.md:274–275,342`；`docs/standards/STANDARDS_REGISTRY.md`（本层偏差台账）。

## 6.1 §2.5 逐数值处置表（11 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :10 | 模型标注 | `( 像素空间)` | 迁移来源引用位 | 待确认 | 引用对象被删空（同形态 13 处，见 CR-41-32），且 `git ls-files` 内该模块无被引源文件 ⇒ 悬空即缺陷（`ENGINEERING_SPEC.md:162`） | 保守方向：改为可解析锚或删句 | CONFIRMED |
| :11 | 线性预测式 | `linear_pred = s·R(θ)·U + t` | 相似变换基线 | 公式导出 | 与 PROSAC/Umeyama 解出的 `SimTransform tf` 同式（`ipv_itertrans.h`/`ipv_types.h` 的 `SimTransform`），实现 `ipv_sip.cpp:364` 注释同式；本头不实现公式，只声明其形式 | — | CONFIRMED |
| :12 | 残差定义 | `r = W - linear_pred`（"W 已 flip"） | 拟合目标量 | 公式导出 | 实现按该式取样（`ipv_sip.cpp:12` 同式，:344–400 组装设计矩阵）；"W 已 flip"与 `ipv_robust_refine.h:104/:117` 的 Y-up 约定分属两阶段（已确认为两个适用域，非矛盾） | — | CONFIRMED |
| :13 | 多项式自变量 | `xn / yn`（归一化绝对像素坐标） | 畸变基底 | 公式导出 | 实现 `ipv_sip.cpp:359–362` `xn_arr.push_back((u.x + cx) / scale);` ⇒ 确有归一化（"避免数值不稳定"的说法与代码相符）；但本头 :31 又把同一自变量写作 `dx = U[u].x (像素, 减去图像中心)` ⇒ 与本行两种写法，见 CR-41-38 | — | CONFIRMED |
| :27–28 | 标准 WCS SIP 式 | `CD11*(ra-crval1)*cos(dec) + CD12*(dec-crval2) + CRPIX1 + A_poly(...)` | 前向 SIP 参照式 | 文献值 | SIP 约定（Minkowitz/Calabretta 式）经 `docs/algorithms/DRIZZLE_GEOMETRY.md:292–293`（SIP 键与 `sip_order` 域 [0,5]、DISP-DRZ-001）与本仓 `photometric_calib.h` 的 `sip_order/sip_a/sip_b/sip_ap/sip_bp` 参数族；本头仅作参照式复述，真实 WCS 写出在 `extract_wcs_sip`（`README.md:84–85`） | 该式把 `(ra-crval1)` 直接乘 `cos(dec)` 的写法是切平面近似下的简写，与本仓 TAN 权威实现（`DISP-WCS-006` 三套 TAN 并存）不完全同式 | PARTIAL（未与本仓 TAN 权威实现逐项对齐推导） |
| :32 | 多项式阶数关系 | `A_poly = sum_ij A_ij dx^i dy^j (i+j <= order)` | 基函数集 | 公式导出 | 项数已核：`ipv_sip.cpp:75 return k; // 项数: order=2 → 6, order=3 → 10` 即 `(n+1)(n+2)/2` ⇒ 与本式一致 | — | CONFIRMED |
| :40/:52 | `order` 默认值 | `3` | 请求阶数缺省 | 待确认 | 唯一调用点显式传 3（`ipv_wcs.cpp:165–166` `fit_sip(..., 3, nullptr)`）⇒ 声明的默认值在生产中从不生效（值恰好相同，属重复来源）；且本头不给 `order` 的有效域，实现另有 `order<2 → 返回 order=0` 与隐式降阶（见 CR-41-38） | 保守方向：删默认实参（由调用点显式给）或把有效域与降阶规则写进声明；影响范围=WCS SIP 项数 | CONFIRMED |
| :43 | 失败哨兵 | `失败时 order=0` | 拟合失败表达 | 待确认 | 同一字段在类型面被定义为 `ipv_types.h:78 "int order; // 前向 SIP 阶数 (0=无 SIP, >0 为拟合阶数…)"` ⇒ `0` 同时是"无畸变"与"拟合失败"；实现 `ipv_sip.cpp:281 result.order = 0;` 后所有早退分支（:294 n_pairs<7、:299 尺寸非法、:304 order<2）都返回 0；`REPORT.md:275` "失败兜底: fit_sip 返回 order=0 时 WCS 仍正常输出" ⇒ CR-41-36 | 保守方向：失败改为独立状态位或 `order=-1`（需同步 `SIPCoeffs` 类型面与所有消费点）；影响范围=交付 WCS 是否携带畸变项的可观测性 | CONFIRMED |
| :14 | 归一化目的 | "避免数值不稳定" | 设计理由 | 待确认 | 无条文，也无条件数/尺度实验（跟踪面 `git grep -rn "条件数\|数值稳定" -- docs lib/algorithms/platesolve` 无本层标定件）；反归一化式 `ipv_sip.cpp:474–477 A_orig[i,j] = coeff_norm[k] / scale^(i+j)` 存在 ⇒ 归一化确实服务于系数可转换，但"不稳定"的量级没有证据 | 保守方向：给一次 order=3、典型 2k×2k 图像下的条件数对照；影响范围=是否需要保留归一化 | PARTIAL |
| :16 | 文件日期 | `2026-07-04` | 创作日期标记 | 结构性不适用 | 非科学量（VCS 承载时间史） | — | CONFIRMED |
| :37/:38 | `tf`、`s0` | 无数值（类型/单位声明） | 相似变换与像元尺度 | 结构性不适用 | `s0` 单位在本头为 "arcsec/pixel"，与 `PLATESOLVE.md:237` 的 `s0=206.265·pixel_um/focal_mm`（角秒/像素）一致；`206.265` 是单位换算定义常数（180/π×3600，§4 非缺陷项） | — | CONFIRMED |

## 6.2 findings

### CR-41-36 · `order=0` 一字两用（无畸变 / 拟合失败），畸变拟合失败即静默交付无 SIP 的 WCS 且无状态位
- 轴：R（主）＋C
- 位点：ipv_sip.h:43 `// SIPCoeffs (失败时 order=0)`
- 上位依据：同仓对"失败必须以状态呈现、禁止坍缩值冒充解"的冻结判据：`lib/algorithms/platesolve/README.md:104` "失败-置信度语义冻结：**退化必须以 success=0 呈现，禁止坍缩值冒充解**（PLATESOLVE.md §11.3；验收 TEST-WCS-DESIGN-001 F4）"；类型面 `ipv_types.h:78` 把 `order` 同时定义为"0=无 SIP"与">0 为拟合阶数"。
- 现状：`ipv_sip.cpp:281 result.order = 0;` 后，四类早退（`n_pairs < 7` :294、图像尺寸非法 :299、请求 `order < 2` :304、以及后续求解失败路径）统一返回 `order=0`；调用方 `ipv_wcs.cpp:165–172` 拿到后只把 AP/BP 清零、不检查失败与否，模块 `REPORT.md:275` 自认"失败兜底: fit_sip 返回 order=0 时 WCS 仍正常输出"。同族问题在生产侧已登记为 `DISP-WCS-004`（`README.md:111–112` "AP/BP 拟合奇异半静默（ipv_wcs.cpp:477 仅日志文件 warn，无标志位；:322-325 跳过 SIP 同理）"），但登记面只覆盖 `extract_wcs_sip`，本头声明的 `fit_sip` 侧同样形态**未在该条登记范围内**。
- 差在哪：本层用"阶数=0"表达失败，正是被冻结判据禁止的"坍缩值冒充解"；且 `DISP-WCS-004` 的台账口径（登记"半静默"时把面限定在 ipv_wcs.cpp:477/:322-325）没有覆盖到本头的接口，属类型面台账缺项而非新代码问题。
- 后果：一份本应含高阶畸变的宽视场解，在拟合失败时输出**无 SIP 项的线性 WCS**，产物层面看不出畸变被丢（SIPORDER 不写），而精度主张（本头 :8 "与 WCS 一起输出, 提高大视场定标精度"）在该帧上不成立。方向钉死：失败被折进合法值 ⇒ 少一条 `success=0` 拒绝路径 ⇒ **偏绿**。
- 定级：S2（`fit_sip` 仅由 `build_wcs` 消费，`README.md:109–110` 把它登记为 legacy 路径、生产符号链走 `extract_wcs_sip`；因此不改正式产品字节 ⇒ 不升 S1）
- 怎么算修好：①`SIPCoeffs` 增加显式状态（`bool fit_ok` 或 `order = -1` 表失败），`ipv_sip.h:43` 改写为失败语义；②`ipv_wcs.cpp` 消费点按状态决定是否发布 SIP 键，并在失败时给出可观测标记（与 `DISP-WCS-004` 同批整改，避免只修一侧）；③`DISP-WCS-004` 的登记文本扩到含 `fit_sip`；④若确认 `fit_sip` 为退役路径，则按 `ENGINEERING_SPEC §2` 的历史代码处置规范登记并在本头标注"非生产接口"（见 CR-41-37），不再新增消费点。
- 置信：CONFIRMED

### CR-41-37 · 本头不提"接口状态"：`fit_sip` 是已登记的 legacy 双路径之一，读者从声明面无从得知
- 轴：C（主）＋D
- 位点：ipv_sip.h:5 `// ipv_sip.h - IPV SIP (Simple Imaging Polynomial) 多项式畸变拟合接口`（全文无"生产/legacy/待退役"任何标记）
- 上位依据：`lib/algorithms/platesolve/README.md:109–110` "DISP-WCS-003 双 SIP 拟合路径并存（extract_wcs_sip 生产 vs fit_sip IRLS+Huber legacy，ipv_sip.cpp:268/:161-263，**仅 build_wcs 消费**）"；`README.md:79–85` 的生产符号链末端是 `extract_wcs_sip（ipv_wcs.cpp:229…）`；对照同仓正确做法：`ipv_select.h:124` 对废弃函数显式标注 `// 自适应步长迭代极限星等 (线性步长, 遗留; P4-magiter 起生产路径不再使用)`。
- 现状：`fit_sip` 的实现内注释自称 IRLS+Huber（`ipv_sip.cpp:161–263`），与生产路径 `extract_wcs_sip` 是两套畸变拟合实现；本头唯一入口不带状态标记，其模型描述（:10–14 与 :26–32）也未说明与生产路径的 SIP 是否同一套系数口径。
- 差在哪：本仓对已废止接口有"就地标注状态"的先例（CR-41-34 的 ③ 同纪律）；DISP-WCS-003 登记的"双路径并存"这一事实在接口声明面零体现，读者会把它当作唯一 SIP 拟合接口使用或复核。
- 后果：新增消费者可能继续接 legacy 路径，使 `DISP-WCS-003` 的"并存"偏差固化；对"SIP 畸变来自哪条路径"的产品溯源主张在接口层不可判定。
- 定级：S3（声明面缺状态标记，无字节影响）
- 怎么算修好：在本头 :5 附近加一行接口状态（"legacy：仅 `build_wcs` 消费；生产 SIP 拟合见 `extract_wcs_sip`，DISP-WCS-003"），并在整改任务收口时按 `CONTROL_PACK_SPEC`/`ENGINEERING_SPEC §2` 处置（删除或转为唯一路径）。
- 置信：CONFIRMED

### CR-41-38 · 同一模型的自变量在两处写成两种口径（归一化 `(U.x+cx)/scale` vs "减去图像中心"），且 `order` 的有效域与隐式降阶在声明面完全缺失
- 轴：C（主）＋R
- 位点：ipv_sip.h:14 `// 其中 xn/yn 是归一化后的图像绝对像素坐标, 用于避免数值不稳定。`；ipv_sip.h:30–32 `// U[u].x (像素) = s*R*W[w] (像素) + t + A_poly(dx, dy)` / `// 其中 dx = U[u].x (像素, 减去图像中心), dy = U[u].y (像素, 减去图像中心)`；ipv_sip.h:40 `// order: SIP 阶数 (默认 3)`
- 上位依据：本仓对"同一系数布局的有效域必须有单一口径"已有前例登记：`docs/algorithms/DRIZZLE_GEOMETRY.md:324` "DISP-DRZ-001 | hp_drizzle_api.h:93 注释 sip_order \"0..4\" | hp_drizzle_api.cpp:98-103 校验 [0,5]（6×6 系数组支持 5 阶下标）"——即"注释声明的阶数域 vs 系数槽位布局支持的阶数"分叉已被登记为缺陷；`ipv_types.h:74–78`（`double A[36]; … order 受 36 槽位约束`）＋`ipv_sip.cpp:17`（`系数索引: SIPCoeffs.A[i*6+j] 对应 dx^i * dy^j (i+j <= order)`）给出本层的同型关系。
- 现状：①自变量两种写法：实现是 `ipv_sip.cpp:359–362 xn=(u.x + cx)/scale`（**加** cx 后除以 scale），:14 称"归一化后的图像绝对像素坐标"，:31 称"减去图像中心"且不带归一化除子；②实现有整套隐式降阶与前置拒绝，声明面零提及：`ipv_sip.cpp:294`（`n_pairs<7 → order=0`）、`:302–304`（`order<2 → order=0`）、`:312–325`（`n_pairs<12 → 2`、`n_pairs<30 → 2`、`n_pairs<60 → 3`）；③声明面不给 `order` 上界，而按 `A[i*6+j]` 布局其可用下标隐含 `order ≤ 5`（`A[36]` 若按 `(n+1)(n+2)/2` 项数解释则允许 n=7——两种解释给出的上界不同，正是 DISP-DRZ-001 的形态）。
- 差在哪：同一接口的输入语义（自变量是否中心化、是否归一化）在本头内两处不一致，且都与实现的第三种写法（先 `+cx` 再 `/scale`）不同；决定产物畸变阶数的降阶规则与 `order` 有效域都不在声明面。
- 后果：按 :31 写消费/复核代码的人会对系数做错误单位的换算（是否再乘 `scale^(i+j)`，见 `ipv_sip.cpp:474–477` 的反归一化）；按 :40 传 `order>5` 的请求在声明面合法，而按 `i*6+j` 布局已越出可用槽位——本层目前只有传 3 的唯一调用点，所以是潜伏风险；降阶不可见使"交付 WCS 的 SIP 阶数"成为匹配数的函数，而对外词表里不存在这一规则。
- 定级：S3（`fit_sip` 为 legacy 路径且唯一调用点固定传 3，不改变当前产物；若被重新接线即升 S2）
- 怎么算修好：①统一自变量描述：一处写清 `xn = (U.x + cx)/scale`（含 `+cx` 的方向依据，因 U 以图像中心为原点）并注明系数需按 `1/scale^(i+j)` 反归一化；②声明面补 `order` 有效域（含与 `A[36]`/`i*6+j` 布局一致的实际上界）与全部降阶阈值，或把阈值提为常量并指向 `ALG-WCS-001`；③与 `DISP-DRZ-001` 的整改同批（同一形态的阶数域问题在两模块各有一套写法，收敛成一条纪律）；④补 `n_pairs` 阈值与 `SIPCoeffs.order` 的对应表到 ALG 层，并加一条单测：给定匹配数断言交付阶数（现仅 `ipv_sip.cpp` 内部日志，无判据）。
- 置信：CONFIRMED

### CR-41-39 · 偏差台账 `DISP-DRZ-003` 的适用面登记为"两层双轨"，实际 pixfrac 边界已有第三层，台账未记
- 轴：C
- 位点：本条位点在类型面（`docs/standards/STANDARDS_REGISTRY.md:181` 与 `:269` 的 `DISP-DRZ-003` 条目行、`docs/algorithms/DRIZZLE_GEOMETRY.md:326` 的登记文本），由本批 CR-41-18 的核对引出，登记于此以便与 SIP 侧的 `DISP-DRZ-001`/`DISP-WCS-004` 缺项一并处理
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md:326` "| DISP-DRZ-003 | pixfrac∈(0,1] 单一边界 | 文件通道 API 层接受 0.0（<0 才拒），引擎层拒绝——**两层双轨** | api.cpp:191 vs drizzle_engine.cpp:1570 |"；`docs/standards/STANDARDS_REGISTRY.md:168`（§2 第 1 行 pixfrac∈(0,1]，偏差列只记 DISP-DRZ-003 的两层）。
- 现状：第三层已经存在且同样接受 0.0——`lib/algorithms/drizzle/src/module_entry.cpp:356`（模块 adapter config 层，`pixfrac < 0.0 || pixfrac > 1.0` 才拒），其声明面亦写 `[0,1]`（`lib/algorithms/drizzle/include/astrocs/drizzle/types.h:56`，引 DISP-DRZ-003 作为"口径"）。
- 差在哪：偏差台账按"两层"封闭登记，而实际是三层；且第三层的声明头把该两层登记当作自己的口径依据引用（把别的层的豁免文本升级成本层的许可）。
- 后果：`STANDARDS_REGISTRY` 的偏差面不再等于真实偏差面——按台账统计"pixfrac 边界双轨"影响范围会漏掉模块事务面；整改任务（登记责任方 `P1-DRZ-IMPL`）验收时也只会被要求修两层，第三层判绿通过。
- 定级：S3（台账口径缺项，不改字节）
- 怎么算修好：与 CR-41-18 同批：把 `DISP-DRZ-003` 的登记文本改为逐层列举（文件通道 / 引擎 / 模块 adapter config），或在模块层修成 `(0,1]` 后记"该层已消除"；`STANDARDS_REGISTRY.md:168/:269` 的偏差列随之更新；并规定"层数型偏差"的登记必须逐层可枚举（防止再有第四层静默加入）。
- 置信：CONFIRMED

---

# 收尾

## A. 本批最高危三条（只挑，不复述）

1. **CR-41-20（ipv_select/robust 面的姊妹：drizzle reverse 的 `no_data_as_zero`）** — 一个把"无覆盖 ⇒ NaN"改成"无覆盖 ⇒ 0.0"的语义开关，缺省即开，且**在三方（声明头 / 两处合同文档 / 机器 schema）里零登记**；有效性判据正本只判 `isfinite` ⇒ 0 填充的无覆盖像素会被当成 0 通量有效样本。方向已钉死为偏绿。位点：`lib/algorithms/drizzle/include/astrocs/drizzle/types.h:70` 段缺失＋`lib/algorithms/drizzle/src/module_entry.cpp:1055–1056/1168`＋`healpix_drizzle/reverse_drizzle.cpp:233–243`。
2. **CR-41-33（极限星等迭代的两入口异值）** — 同一函数的 `n_target` 在两生产入口相差 3 个数量级（50–60 vs `star_detection.max_stars` 2万–5万），同一 `limiting_mag_safety=3.0` 配置键在两侧放大不同量；一侧还以名义曝光 180 s 顶替本帧 EXPTIME。直接决定星表查询窗与"宁多勿少"主张。
3. **CR-41-15 与 CR-41-19 同型（模块 adapter JSON 词表无合同面 / 与实现分叉）** — hips 面三处词表（13 config＋19 manifest 输入＋13 输出键）在 `docs/contracts`、`eng/contracts` 均零登记；drizzle 面声明的输出词表有 2 个幻影键、18 个实际产出键未声明（op=drizzle 4＋op=reverse 14）、`signal_plane_base64` 被归给错误的 op，且两头的输出宏**零消费者**；四个"排异计数"（`n_rejected_nonfinite*`，`docs/algorithms/DRIZZLE_GEOMETRY.md:196` 要求逐输出像素强制暴露的量）不在任何对外词表里。合同门对这条对外 JSON 合同没有任何判据。

（次高：CR-41-23 的"31 个阈值以配置面假象存在＋机器锁固化"，CR-41-24 的"配额均匀性被按星等截断取消"，CR-41-2 的"自称对齐 IO-003 §4 却无完成标记与内容校验"。）

## B. 我读不动或需要实跑才能定的

| # | 未定项 | 为什么读不出来 | 该跑什么（不在本批只读范围） |
|---|---|---|---|
| 1 | hips `promote` 在 Windows 上把"访问被拒/目录被占用"折成 `ERR_STATE`（publish.h:60/:100–103 只定义"目标非空/stage 缺失"） | 需要 Windows 上真实 rename 失败注入；本批不跑构建/ctest | 在 Windows x64 上对 `aio_publish_promote_v1` 注入句柄占用（AV/打开目录），断言返回码与 `ERR_STATE` 语义一致或订正声明面 |
| 2 | P1 HiPS 树（无 `status=COMPLETE` 的 writer manifest.json）在 IO-002 读端是否被当作成功对象 | 需实跑 `aio_hips_open` / `aio_hips_verify_product_set` 对无 status 字段 manifest 的判定 | 用现成 p1_hips 产物目录跑读端校验（或加一条合同测），确认"完成标记"是存在性检查还是 status 检查 |
| 3 | hips 配置非整数落点（`data_type`/`flags` 经 `json_get_f64` 截断）是否真被任何生产入口触发 | 需要知道编排层生成的 config 是否恒为整数字面量 | 在 IR/CLI 侧 dump 一次 hips execute config JSON，核对 `data_type/flags/nside/snr_count` 的 JSON token 类型 |
| 4 | CR-41-33 的净偏差量级（名义 180 s vs 真实 EXPTIME 对 m0、查询次数、触顶率的影响） | 需要跑 10 帧真实数据的迭代轨迹 | 以 `docs/plugins/…/03_star_detection.md` 的 10 帧样本重跑 `estimate_mag_lim_iterative` 两支（真曝光 vs 名义），落跟踪面 results |
| 5 | `no_data_as_zero` 缺省 0 填充是否已污染既有交付面（Phase2/Phase3 是否有按 `isfinite` 消费 reverse 平面的代码路径） | 需要沿 IR→mosaic 消费链实跑或逐点静态确认 | 追 `coverage_plane_base64`/`signal_plane_base64` 的全部消费者（含 `eng/tests/backend/phase2_fixture_main.cpp`），并做一次"默认路径产物里无覆盖像素是 0 还是 NaN"的字节级断言 |
| 6 | `fit_sip`/`build_wcs` 是否在 12 个 C ABI 导出的某条 legacy 入口上仍生产可达 | 只读静态无法确定 reachability（`docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv` 由 `eng/tools/arch/build_production_execution_inventory.py` 现场计算） | 跑该生成脚本并查 `build_wcs`/`fit_sip` 的生产可达列 |
| 7 | `RobustRefineParams` 各阈值（1%/3% 阻尼、1.5×/30 回退）对成品率与 RMS 的实际影响方向 | 需要参数扫描实验 | 在跟踪面实验单元里对该结构做单参扫描（同时决定 CR-41-23 里"哪些真需要接线"） |
| 8 | `params` 声明的 `m_lim_alpha_max = 100`、`total_target_narrow=1000` 等"看着合理但无锚"的数值是否有任何历史标定支撑 | 证据只在 `run/`（不入库，已按 §1 判定为不可复核） | 由负责人决定 `run/perf-fix/P4-magiter`、`run/WCS-AB-01`、`run/perf-fix/P27-dead-params` 三份报告是否需要按"三留一不留"口径回填到 `实验/` |

## C. 派单与清单差异

1. **派单说"两份合同面"，本批实际对账面对象是 6 类**：`docs/contracts/`（PUBLIC_API / DATA_SEMANTICS / API_CONTRACTS.csv）、`eng/contracts/`（`module_lifecycle_contract.schema.json`、`phase_config_normalize.schema.json`）、`eng/packaging/config/`（`defaults.json`、`config_registry.json`——被 `ASTROCS_DESIGN.md:295` 指认为"默认值与取值域正本"）、`docs/plugins/algorithms_phase1/`（模块正本，含 star_detection 配置键表）、`docs/algorithms|science/`（公式与 §4a 类条文）、登记面（`module.yaml`、各模块 `README.md`/`memory.md`/`REPORT.md`）。本批按实际存在的对账面逐项列位点，未拘泥"两份"的字面数；差异如实登记于此。
2. **清单第 3 项的模块性质与派单重点不完全对齐**：`lib/algorithms/drizzle/include/astrocs/drizzle/types.h` 与两个 hips 头是"对外类型/词表声明面"，而三个 `ipv_*.h` 是**模块内部 C++ 接口**（`ipv_select.h:22` 原文"接口: 内部 C++ 函数, 无 ctypes 边界, 无 JSON 序列化"），其"句柄"是 `void* get_gaia_client_handle()` 一类型擦除访问器（已核：null ⇒ 返回 −1，合规）。派单重点 ①（枚举值域/单位/缺省/对齐与尺寸）对内部头仍可执行，但重点 ②（"合同声明该键不存在、代码仍静默读"）在 ipv 组的对应形态是"配置/文档声明该键可配、代码另有三套登记"，已按 CR-41-30/33 落位。
3. **未发现清单内文件缺项或多余项**：6 个路径全部存在且为跟踪件（`git ls-files` 命中），本批一项未漏、未越出独占清单；文件行数依次为 149 / 156 / 114 / 136 / 291 / 58，全部逐字读完。
4. **取证预算偏差如实登记**：单文件 20 次工具上限在 publish.h（约 19 次收口）与两个 hips 头（16–19 次）内完成；`ipv_select.h`、`ipv_robust_refine.h`、`drizzle/types.h` 三个文件因"三方对账批"要求跨模块（配置正本、机器 schema、兄弟模块实现、调度器入口）取证而超限。超限部分**没有**用来扩大改动面（全程零写仓库、零构建），全部是读与 `git grep`；未定案处已按 §5 第 3 条落 `PARTIAL`（见 §2.5 表"置信"列与上表 B）。

## D. 收工 git status --porcelain 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工完全一致：本批零写入仓库、零 git 写、零 `git config`、未跑构建/ctest/run_checks/任何 `eng/**`、未 import 仓库 Python；唯一新增文件为审查工作件 `独立审计/证据/通读-CR-41.md`（本文件，独立审查目录不在仓库内），未建 `独立审计/复算件/CR-41/`（无自写脚本需求）。

<!-- PROGRESS: 6/6 -->






