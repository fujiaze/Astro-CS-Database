# AUD-101 · 批次 D1 残余补登 —— `docs/algorithms/` 3 份 + `工程控制/RELEASE-05/tasks/` 10 份

判据与字段口径：`独立审计/派单规程/DISPATCH-DOC-PREAMBLE.md` §1–§6；角色表与写法判据 = 标准 01 §2/§3/§4；收口与处置另按标准 04 §4。
仓库 = `F:\Astro dev\Astro CS Normalization Database`，冻结基线 HEAD=`c8f64e9a`。本节点只读，未执行任何构建/测试/检查器。

## 0 开工基线与派单—清单差异

开工 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文：

```text
?? "ACSD\346\225\271\346\226\227\345\267\245\344\272\224_AUDIT-06.zip"
?? site/
```

即负责人预期的两项：`?? ACSD整治工作包_AUDIT-06.zip`、`?? site/`（八进制转义是 git 对非 ASCII 路径的默认输出，`quotepath=true` 行为）。HEAD 短 SHA 实测 `c8f64e9a` = 派单基线。

> 取证过程中本节点对 `.git/config` 有过一次**不应发生的写操作**，已复原并在 §6 登记。

### 0.1 派单文字与清单的差异（按前言 §3「清单是唯一范围依据」执行）

| 项 | 派单说法 | 清单实测 | 处理 |
|---|---|---|---|
| 份数 | 13 份 = DA-02-missing 3 份 + D1-residual 11 份「其中 TEST_STANDARD.md 跳过」 | `DA-02-missing.txt` 3 行、`D1-residual.txt` 11 行；`3 + (11 − 1) = 13` ✓ 自洽 | 按 13 份登记，`docs/standards/TEST_STANDARD.md` **不登**（另一路在做） |
| 「前序没出条目行」 | 13 份均"没落到逐份登记条目" | **仅 DA-02 那 3 份成立**：`AUD-101-DA02-算法推导.md` 的 `### 1.1–1.28` 不含这 3 份，其尾行 `PROGRESS: 已审份数 28/31` 与缺口一致。**D1 那 10 份不成立**：`AUD-101-DB-19.md` §1.16–1.25 **已逐份出条目**（各含标题/行数/上游/下游/是否正本/重叠/写法违规/悬空/冲突/处置） | 见 §0.2 |

### 0.2 D1 那 10 份为什么被算成「没登记」（本批对合并口径的校准结论）

`AUD-101-DB-19.md` 的小节标题写的是**包内相对路径** `### 1.16 \`tasks/ACCEPT-501.md\``，
而 `D1-residual.txt` 与机械台账用的是**仓库相对全路径** `工程控制/RELEASE-05/tasks/ACCEPT-501.md`。
按路径字符串取交集的合并器因此判 0 命中 ⇒ `DB-19-missing.txt` 与 `D1-residual.txt` 把这 10 份列为"未登记"。
**即：这不是漏审，是登记的 `路径` 字段写法不可合并。** 本批按前言 §4 的字段要求，对 10 份一律以**仓库相对全路径**重新出条目行，并**独立复读、独立取证**（不沿用 DB-19 结论）；
DB-19 已给的判定凡与本机实测一致者引用、不一致者订正，逐条标注。

本批补强的角度（DB-19 未做的轴）：**任务书的文件域与验收锚在 HEAD 上还可不可执行**——对象是否仍在跟踪集、锚是否漂到无关代码、已作废口径是否仍被当作现行结论留在册上。

## 1. 逐份登记

### 1.1 `docs/algorithms/CALIBRATION_ALGORITHMS.md`

| 字段 | 值 |
|---|---|
| 路径 | `docs/algorithms/CALIBRATION_ALGORITHMS.md` |
| 标题 | Calibration Algorithms (ALG-CAL) |
| 行数 | 636（`wc -l` 与机械列一致） |
| 角色 | 算法推导。实际目录 `docs/algorithms/` = 标准 01 §2 该角色的目标目录 ⇒ **一致，非角色错放** |
| 主题 | Phase1 校准算法层：master bias/dark 生成（sigma-clip 合并）、master flat 三步归一、单帧校准算术双分支、热/冷像素检测与结构过滤与修复、`optimize_dark_k` 回退估计器、`apply_photometry` 标量施加；复杂度、确定性/归约、退化行为表、测试设计与冻结容差、DISP-CAL 缺陷清单 |
| 上游 | L3 `> 上游：ASTROCS_DESIGN.md §4.2（Phase1 节点流程）`——实核 `ASTROCS_DESIGN.md:254` 的 §4.2 标题确为「节点流程」，且 §4 = 「normalize（Phase1）：单帧标准化」⇒ **指代成立**，本份不在"§号与标题错配"缺陷族内。L5 另标 SCI 上游 `SCI-CAL-001` |
| 下游 | 分片外 **36 个跟踪文件**引用（命令 `git grep -c -e "CALIBRATION_ALGORITHMS.md" -- ':!docs/algorithms'`）。含 `CMakeLists.txt:594`、`docs/science/CALIBRATION.md`、`docs/modules/calibration.md`、`lib/algorithms/calibration/README.md`、`eng/tools/quality/check_master_unit_guard.py`、`eng/packaging/config/defaults.json`、`eng/tests/unit/p1001_real_nodes_test.cpp`、`docs/traceability/TRACEABILITY_MATRIX.{csv,json}` ⇒ 非孤儿 |
| 是否正本 | 是——`docs/algorithms/` 层校准算法唯一正本；L7–8 自declare「本文为下游派生件，SCI 的修改从 SCI 自身发起」，SCI 层正本是 `docs/science/CALIBRATION.md`，两层分工不冲突 |
| 重复或重叠对象 | `docs/science/CALIBRATION.md` §5「连续定义」。**两处原文**：本份 L38 `cal = (raw − bias·[bias≠NULL] − K·dark·[dark≠NULL]) / max(flat, 0.1)` ↔ `docs/science/CALIBRATION.md:91` 同式逐字节同；本份 L41 `K = t_light / t_dark`（两分支同一 K）↔ `docs/science/CALIBRATION.md:96` 同；本份 L42 `flat_norm = max(flat / median(flat), 0.1)` ↔ `docs/science/CALIBRATION.md:98` 同。<br>重叠范围：SCI 的**连续定义整块**（三式 + 双分支）在本份 §2 被整段复述，非指针引用。**无值冲突**（含 MAD 常数 `1.482602218505602` 本份 L87 ↔ sci:318；`rtol=1e-6/atol=1e-7` 本份 L478 ↔ sci:346/496）。属标准 01 §1 允许的"下钻"，但 §4 要求「公式放对应下层文档」——同一公式在两处各存一套 ⇒ 改 SCI 时本份不会自动跟随，登记为**复述型重叠风险**（非重复正本） |
| 元信息块 | **有**：L5–9 `> ID: ALG-CAL-001 范围: ALG-CAL-001..006 上游 SCI: SCI-CAL-001 状态: CONTRACT_READY 模块: …` ⇒ 违标准 01 §4「文档开头无元信息块（文档 ID、状态编号…）」。 |
| 写法违规 | ① 状态编号入正文：L5 `状态: CONTRACT_READY`（标准 01 §4 点名禁项；状态口径唯一属 `ASTROCS_DESIGN.md §12.5`）。<br>② 任务/缺陷编号密集：`DISP-CAL-001..014`（§10 全节 L490–607）、`P1-CAL-IMPL`（L277/403/426）、`P1-CAL-TEST`（L428/615）、`TEST-CAL-DESIGN-001`（L428/615）、`BIAS-001`（L524）、`UNIT-001`（L554）、`FIX` 型无。<br>③ 历史叙事：L618 节标题 `参考文献与参考代码库（含许可证）— SCI-001-S2 补齐`、L620 `原有条款全部保留`、L492 `DISP-CAL-001（**部分关闭**）`、L498 `DISP-CAL-010（**已按现行语义落地**）`、L513 `DISP-CAL-006 … **已订正**`、L337 `**已退役**：逐条分歧 …` ⇒ 违 §4「无旧版/曾/已修式历史叙事，需表达硬约束时用正向条款」。<br>④ L592 `config_sha256` 是**字段名**不是哈希值。<br>⑤ L337 与 L340 之间「现状依据与 oracle 一律取自生产源」属过程性辩解。<br>⑥ 无日期、无 commit sha（全文未检出 YYYY-MM-DD 与 7–40 位十六进制字面量）。 |
| 悬空引用 | 1. **`02_FROZEN_STAGE1_HISS_SPEC` 三份引用全部悬空**：L255–256「规范依据 `02_FROZEN_STAGE1_HISS_SPEC §2.3`（engineering 控制，非 docs/science…）」、L281–282「规范依据 `02_FROZEN_STAGE1_HISS_SPEC §7`（I_photo = k_photo·I_cal，Drizzle 前应用）」、L512「02_FROZEN §2.3 语义未进入生产路径」。<br>确认命令：`git ls-files \| grep -i '02_FROZEN\|HISS_SPEC'` → 跟踪集 **0 命中**；`git log --all --diff-filter=D --name-only` → 唯一历史路径为 `_agent_package/AstroCS_Stage1_HISS_Agent_Package_2026-07-31/02_FROZEN_STAGE1_HISS_SPEC.md`，**已随该包删除**。<br>后果：`ALG-CAL-005`（dark 系数估计）与 `ALG-CAL-006`（Gaia 测光比例施加）两节的**唯一"规范依据"不存在**，其 §2.3/§7 条款内容不可复核。同一 ID 亦被 `CMakeLists.txt:594`、`lib/algorithms/calibration/src/photometry_apply.{h,cpp}`、`lib/infrastructure/aio/src/hiss_*` 等 15 个跟踪文件引用 ⇒ 仓库级悬空规范锚，本份是它在算法层最"权威"的载体。<br>2. 文件级链接：本份引用的 17 个路径（`docs/science/CALIBRATION.md`、`docs/api/PHASE1_API_V1.md`、`docs/contracts/{ARCH-001,PUBLIC_API,DATA_SEMANTICS}.md`、`docs/modules/calibration.md`、`eng/contracts/data/phase_product_exchange_matrix.json`、`lib/include/astrocs/core/master_unit_guard.h`、`eng/tools/quality/check_master_unit_guard.py`、`lib/algorithms/calibration/{README.md,batch_config.json}`、`src/{master_generator,calibrator,cosmetic_corrector,dark_optimizer,photometry_apply}.cpp`、`cpp/cosmetic_corrector.cpp`）逐个 `git ls-files --error-unmatch` → **全部 TRACKED，0 死链** |
| 与上位冲突 | **1. 源码行号锚系统性漂到无关代码**（与本份 §3 标题「与源码逐一锚定」及索引 duty「逐符号实现锚定」直接冲突）：<br>`CMakeLists.txt:503-523`（三处：L21、L275、L326 均称"astrocs_calibration 静态库唯一构建清单"）→ 实测 `add_library(astrocs_calibration STATIC` 在 **589**（`target_link_libraries` 604、OpenMP 607–608）；**503–523 是 zlib 的 `find_library(ACS_ZLIB_LIBRARY …)` 与 `ACS_ZLIB_ROOT` 块 ⇒ 锚落在完全无关的代码上**。<br>`astro_calibration.h:21-24`（错误码 AC_OK/AC_ERR_PARAM/…）→ 实测 `AC_OK`/`AC_ERR_PARAM` 在 **26–27**；21–24 是 `AC_COMBINE_MEDIAN`/`AC_METHOD_*`。<br>`astro_calibration.h:33-155`（"14 个 `AC_API` 公共符号"的锚）→ 该处是 `AC_COLSTAT_*` 坏列定位常量族（33–35）。<br>`astro_calibration.h:168-179`（"C++ 接口 `ac::optimize_dark_k`，头文件声明但无 AC_API 导出"）→ 该处是「坏列定位：dark/bias 母版路径 + 三路径仲裁」注释块。<br>`module_adapters.cpp:1154-1179,1230-1240`（`p1_op_calibrate`）→ 实测函数起于 **2285**；`:1992-2002`（`p1_master_flat_valid` 判定与拒绝）→ 定义 **1757**、调用 **2337**；`:2355`（`p1_op_cosmetic`）→ 实测 **2749**；`:2003-2125 / :2033,2037,2041 / :2127 / :2129-2147 / :2198-2216 / :2211 / :2220`（master_unit_guard 规则调用、声明换算、K 推导、容差、`ac_calibrate_frame` 调用点）同一区段整体后移（`st_cal["master_unit_guard"]` 落 **2466–2467**）。<br>`p1_session.cpp:372`（calibrate 调用）→ 实测 `ac_calibrate_frame(` 在 **378**；`p1_session.cpp:462-465`（cosmetic 调用）→ 实测 `ac_correct_frame(` 在 **481**。<br>对照组（**仍准确**，故不判全表失效）：`master_generator.cpp:67`/`:178`、`calibrator.cpp:71`/`:85`、`cosmetic_corrector.cpp:62`/`:119`/`:161`/`:230`、`dark_optimizer.cpp:97`、`photometry_apply.cpp:29` 全部命中 ⇒ 漂移集中在 **CMake、公共头、两处调用方**，正是"下游按锚复核"最需要的三类。<br>**2. 公共接口登记规模不实**：L17–18「唯一公共头，14 个 `AC_API` 符号」与 L315 表格的 14 项枚举 → 实测 `grep -c '^AC_API'` = **18**；差额是一整族未登记能力（`AC_COLSTAT_*` 坏列定位：dark/bias 母版路径 + 三路径仲裁，`astro_calibration.h:33` 起、`:307` 已有返回码说明），本份 §3 无对应算法条目 ⇒ **算法正本对模块现状登记不全**，下一个据锚写测试或改 ABI 的对话会按 14 个符号规划。<br>**3. 与科学上位无值冲突**：校准双分支、K、floor 0.1、MAD 常数、容差两组均与 `docs/science/CALIBRATION.md` 逐字一致（举证见「重复或重叠对象」）。 |
| 处置建议 | **保留（校准算法层正本）+ 就地整改三项**：①`CMakeLists.txt`/`astro_calibration.h`/`module_adapters.cpp`/`p1_session.cpp` 的行号锚整体重锚，或改由 `docs/algorithms/anchors/anchor_contract.json` 机器门生成、正文只留符号名（标准 01 §3「双向索引可追溯」：追溯不到的登记即缺陷）；②L255/L281/L512 的 `02_FROZEN_STAGE1_HISS_SPEC` 删除或改指现存正本，同时把 ALG-CAL-005/006「无现行 SCI 依据」如实写明（标准 01 §1「下位与上位冲突时以上位为准，冲突本身是缺陷」）；③删 L5–9 元信息块与「部分关闭/已按现行语义落地/已订正/已退役/补齐/原有条款全部保留」历史叙事，改写为现行正向条款；④补 `AC_COLSTAT_*` 坏列定位算法条目并把符号数改为实测值（标准 01 §2「同一角色内同一主题只有一份正本」——缺登记等于把该主题让给别处另写）。 |

#### 1.1.a 对机械列的校准（前言 §5.4 要求的交付物）

`doc_mechanical.csv` 本行：`meta_block=False, frontmatter=False, upstream_pointer=True, dates=[], task_ids=[], commit_sha=[], sha256=True, history_narrative=[], dangling_links=[], unregistered_index=False`。

- **漏报 1（元信息块）**：`meta_block=False`，实际 L5–9 是含「文档 ID + 状态编号 + 模块」的元信息块 ⇒ 工具只认 YAML frontmatter，认不出引用块形态。
- **漏报 2（任务编号）**：`task_ids=[]`，实际 `DISP-CAL-001..014`、`ALG-CAL-001..006`、`P1-CAL-IMPL`、`P1-CAL-TEST`、`TEST-CAL-DESIGN-001` 密集 ⇒ 正则只覆盖 `EXP-/RELEASE-/FIX-/AUD-` 前缀。
- **漏报 3（历史叙事）**：`history_narrative=[]`，实际「已退役/已订正/部分关闭/已按现行语义落地/补齐/原有条款全部保留」六词齐备。
- **漏报 4（悬空引用）**：`dangling_links=[]`，实际有 3 处指向已删除文档的 ID 式引用 ⇒ 工具只判 `path`/`相对链接`，判不到「`02_FROZEN_…§2.3`」这种**规范 ID 引用**（本仓该族 ID 有 15 个跟踪引用者，量大）。
- **误报 1（sha256）**：`sha256=True` 来自 L592 字段名 `config_sha256`，不是哈希值。
- **正确项**：`lines=636`、`unregistered_index=False`（确在 `docs/DOCUMENT_INDEX.yaml:127` 登记）与本机实测一致。

<!-- PROGRESS: 已登记份数 1/13 -->

### 1.2 `docs/algorithms/COSMETIC_ALGORITHMS.md`

| 字段 | 值 |
|---|---|
| 路径 | `docs/algorithms/COSMETIC_ALGORITHMS.md` |
| 标题 | Cosmetic Correction Algorithms (P1-COS) |
| 行数 | 435（`wc -l` 与机械列一致） |
| 角色 | 算法推导。实际目录 = 标准 01 §2 目标目录 ⇒ **一致，非角色错放** |
| 主题 | 坏点检测/修复算法层：全局阈值热/冷检测（median+MAD）、8 邻接连通域结构过滤、5×5 镜像反射中值与四方向 `1/dist` IDW 修复、`correct_frame` 帧级编排、统计基元、退化/NaN 语义、退役 cc_* 通道登记、测试设计与冻结容差、DISP-COS-001..012 |
| 上游 | L3 `> 上游：ASTROCS_DESIGN.md §4.2（Phase1 节点流程）`——§4.2 实为「节点流程」（`ASTROCS_DESIGN.md:254`）⇒ **指代成立**，本份不在 §号错配缺陷族内 |
| 下游 | 分片外 **24 个跟踪文件**（`git grep -c -e "COSMETIC_ALGORITHMS.md" -- ':!docs/algorithms'`）。含 `docs/modules/calibration.md`、`docs/modules/registry/astrocs.phase1.cosmetic.md`、`lib/algorithms/cosmetic/{module.yaml,README.md,memory.md}`、`lib/algorithms/cosmetic/tests/p1cos/*`（5 份）、`lib/phase1_session/{module.yaml,README.md}`、`eng/tools/check_traceability_matrix.py`、`artifacts/evidence/governance-01/research/DOC-SCI-001_插件文档与科学权威冲突裁决.md` ⇒ 非孤儿 |
| 是否正本 | 是（cosmetic 域算法层唯一正本）。§0 是全文档集中**少见的合格重叠界定**：L26–29 明确本份与 `ALG-CAL-004` 描述同一实现、并裁定「本模块文档是 cosmetic 域逐公式的权威登记」 |
| 重复或重叠对象 | `docs/algorithms/CALIBRATION_ALGORITHMS.md` §3.4 —— **同一实现的两份逐公式登记（实质重复，非下钻）**。<br>本份 L22–24 `逐公式锚定 **现行唯一生产实现** ac::detect_hot_pixels/detect_cold_pixels/filter_by_structure_size/interpolate_pixels/correct_frame（cosmetic_corrector.cpp:61-265，经 ac_correct_frame/ac_correct_frame_f64 导出）`<br>↔ `docs/algorithms/CALIBRATION_ALGORITHMS.md:222` `### 3.4 ALG-CAL-004 坏点检测/修复 ac::detect_hot_pixels / ac::detect_cold_pixels / ac::filter_by_structure_size / ac::interpolate_pixels / ac::correct_frame` + `:224` `源码锚: cosmetic_corrector.cpp:61-265`。<br>重叠范围：**同一源文件、同一行号区间、同一组 5 个函数**，两边各自写完整离散公式（CALIBRATION §3.4 的 F4.1–F4.4 是完整伪代码，不是摘要句）。差异：本份多「标度不变性实测」「`mad=0` 退化实测」「`max_size<=0` 实测」三组证据与极性口径；CALIBRATION 侧多 `ac_calibrate` 上下文，且**其 §3.4 未标"摘要引用/从属本域正本"**（CALIBRATION L222–249 无任何从属声明）。⇒ 改一侧不回改另一侧，是活的重复面 |
| 元信息块 | **有**：L5–15 两块引用（`ID 覆盖: ALG-COS-001..005 状态: CONTRACT_READY 上游: SCI-CAL-001 下游: …` + 实现源/迁移目标/科学定义指针 + 状态口径声明）⇒ 违标准 01 §4「文档开头无元信息块（文档 ID、状态编号）」 |
| 写法违规 | ① L15 「**状态词唯一口径 =** `ASTROCS_DESIGN.md` §12.5；IMPLEMENTED 只由验收签发」⇒ 直撞标准 01 §1 末条「任何文档不声明自己的第二套权威顺序或"唯一口径"」，也撞 §4「开头无元信息块（…纪律声明）」；同族句另见 L14「不修改 SCI」。<br>② 状态编号：L5 `状态: CONTRACT_READY`。<br>③ 任务/缺陷编号密集：`ALG-COS-001..005`、`DISP-COS-001..012`（§10 表）、`P1-COS-IMPL`（L11/307）、`P1-COS-TEST`（L28/311/313）、`TEST-COS-DESIGN-001`（L5/311）、`TEST-P1-COS-001`（L313/411）、`P1-COS-DOC`。<br>④ 历史叙事：L36/L289「**已退役**」、L306「退役源只作**历史登记**」、L416 节标题「— SCI-001-S2 **补齐**」、L418「原有的条款全部保留」（原文 `原有条款全部保留`）⇒ 违 §4「无旧版/留痕/曾/原 xx 等历史叙事」。<br>⑤ 时态/现状叙事混进规范条款：L10–11「迁移目标目录 …（落码由 P1-COS-IMPL 执行，**尚未存在生产符号**）」——该现状陈述在 HEAD 已不成立（见「与上位冲突」1），属 §4「只写现行设计要怎样」的反例。<br>⑥ 无日期、无 commit sha、无 sha256 值 |
| 悬空引用 | 1. **符号级悬空（指向已退役实现）**：L19–20「SCI-CAL-001 §12 将坏点检测/修复登记为 `ALG-CAL-004`（引用 cc_* 通道符号 `cc_detect_hot/cold + cc_correct_median`）」——被引符号仍在跟踪集（`lib/algorithms/calibration/cpp/cosmetic_corrector.cpp`，`git ls-files --error-unmatch` → TRACKED），但该通道本份自己判为已退役且「**无资格**解释或复算本模块行为」（L37–38）⇒ 与科学正本的绑定相反，见「与上位冲突」4。<br>2. 文件级：L404–410 引用的 `docs/contracts/DATA_SEMANTICS.md §10`（实测 `:304` = §10.4，§10 在）、`docs/contracts/PUBLIC_API.md` 的 `API-COS-001`（`grep -c` = 3）、`docs/api/PHASE1_API_V1.md §2`、`lib/algorithms/cosmetic/{module.yaml,README.md}`、`lib/algorithms/calibration/CMakeLists.txt:20-25` 均存在 ⇒ **0 文件级死链**（`git ls-files --error-unmatch` 逐个确认）。<br>3. 反例登记（本份自证正确的否定性判断）：L294「`lib/algorithms/calibration/python/` 与 `cosmetic_corrector.dll` 在当前树中不存在」→ `git ls-files lib/algorithms/calibration/python` = 0 ⇒ 该"不存在"陈述**成立**，不是悬空 |
| 与上位冲突 | **1. 全文核心"生产现状"结论在 HEAD 已失效（本批最高价值单条）**：L209–222「关键编排语义（生产事实，一律如实登记）」→「**现网生产调用点（两处，均传 NULL 检测源）**」→「⇒ 两条生产路径的 `hot_sigma/cold_sigma` 都 >0 而检测源为 NULL，**故热/冷检测全部关闭**，`out_hot=out_cold=0`，**模块退化为恒等 pass**」，并以 DISP-COS-009（L395）成条目。<br>实测 HEAD：① 调度器路径 `module_adapters.cpp:2748` 注释「op: cosmetic_correct（唯一真实入口 ac_correct_frame…）」、函数体 `:2749`、`ac_correct_frame(` 调用在 **`:2930-2933`**（传 `fixed.data(), hot_sigma, cold_sigma, method,…`），且 `:2796` 注释明写「master_dark/master_bias 同时是**坏点检测**（`ac_correct_frame` 的…）」；② legacy 路径 `p1_session.cpp:481-484` 传 `dark_plane, bias_plane`（`dark ? image_px(dark) : nullptr`），并在 `:470-478` 新增「cosmetic master size mismatch vs frame」fail-closed 尺寸一致性判红。<br>⇒ **检测源已接线、且新增了一致性门，文档的"检测从未在生产生效"结论与 DISP-COS-009 均已作废**；连带使 §9 L349–357「不变量②的非退化伴随断言」的立论前提（"两条生产路径当前正是 dark/bias=NULL"）失效——该段的设计本身仍有效，但它据以论证"测试会全绿而分支从未行使"的事实不再成立。<br>**误导路径**：下一个据本份开工的对话会去"接线母版"（已做），或据 DISP-COS-009 把 0 计数解释成"检测关闭"而非"无坏点"，而真实链路上二者已需重新判别。<br>**2. 与科学上位在 `ALG-CAL-004` 名下绑定相反符号**：`docs/science/CALIBRATION.md:420`（§12 关联 ALG ID）「`ALG-CAL-004` 坏点检测/修复 **`cc_detect_hot/cold + cc_correct_median`**」与 `:314` 的 Oracle 行「坏点全帧 `cc_correct_median` 仅修复 `bad_mask=1` 像素」（FROZEN SCI）↔ 本份 L21–24 把同一 ALG 域实现锚到 `ac::detect_hot_pixels/…/correct_frame`，并在 L37–38 判退役源「**公式、阈值或退化语义无资格**解释或复算本模块行为」。<br>⇒ 上位（SCI）指向退役实现、下位（ALG）指向生产实现，**方向与标准 01 §1「下位与上位冲突时以上位为准」相反**；因 SCI 为 FROZEN，消解须走变更流程，不能由本份单方裁定。<br>**3. 源码锚漂移（"现状依据"类锚集中失效）**：<br>`p1_session.cpp:462-465`（称 `ac_correct_frame(…, nullptr, nullptr, …)`）→ 实测 462–465 是 probe 宏与 `read_image`；真实调用 **`:481`**（文档同段引的 `:471` memcpy→实际 `:488`；`:472` aio_write_fits→`:489`；`:477-478` 计数→漂移）。<br>`module_adapters.cpp:2355`（p1_op_cosmetic）→ 实测 **`:2749`**；`:2364` memcpy、`:2370` 原子写盘、`:2394-2395` 计数写 manifest → 全部落在漂移区外；`:2328-2329`（§9③/DISP-COS-012 的 fail-open 证据）→ 实测该处是 calibrate 的 W/H 尺寸判红，**真实 method 解析在 `:2773`**：`const int method = p1_int(c,"method",AC_METHOD_MEDIAN) == AC_METHOD_BILINEAR ? … : …`（三元回落 median，fail-open 实质**仍成立**，锚号错）。<br>`ac_api.cpp:108,228`（L8「C ABI 导出」）与 §4 L226 `ac_api.cpp:108-122`、L232 `:228-263`、DISP-COS-001/004/005/010 同锚 → 实测 `ac_correct_frame` 在 **`:155`**、`ac_correct_frame_f64` 在 **`:277`**、`ac_set_num_threads` `:173`；`:108` 是 `ac::generate_master(`、`:228` 是 f64 的 `out_n` 计算 ⇒ 锚落在**别的算法**上。<br>`astro_calibration.h:97,142`（L9「签名权威」）→ 实测 `:97` 是 `dark_optimization` 注释（calibrate 面）、`:142` 是 `neighbor_k` 注释（**坏列路径**）；`:105-115`（L234 称 f64 降级声明）→ 实测该处仍是 calibrate 签名区；**`:23-24`（AC_METHOD_*）✓ 准确**。<br>仍准确组：`cosmetic_corrector.cpp:35/:46/:51`（median_inplace / compute_global_median / compute_global_mad）、`:62`、`:119`、`:140`、`:161`、`:230`；`lib/algorithms/calibration/CMakeLists.txt:20-25`（`CAL_PROD_SOURCES`）✓；`coverage/CMakeLists.txt:28` 同族。⇒ 与 1.1 同一形态：**模块内锚准、跨文件"生产现状/接口签名"锚系统性漂移**，而后者正是本份「一律如实登记」的支撑。<br>**4. cosmetic 主题正本缺一整族算法**：本份 `grep -c '坏列\|bad_column'` = **0**，而该能力在四处在册：科学层 `docs/science/CALIBRATION.md:208-223`（「② 列状缺陷…连通域大小过滤无效」）、合同层 `docs/contracts/DATA_SEMANTICS.md:304 §10.4 列状缺陷路径（bad column）`+`:328 §10.5 方差处置`（含「默认启用」开关）、配置层 `eng/packaging/config/defaults.json:754-824` 的 6 个 `cosmetic.bad_column_*` 键、代码层 `astro_calibration.h:33` 的 `AC_COLSTAT_*` 与 `lib/algorithms/cosmetic/tests/p1cos/p1cos_tests_badcol.cpp`。<br>⇒ 标准 01 §1/§3：该机制从最高设计经 SCI、合同、配置、代码都有落点，**唯独第三层（算法推导）无正本** → 权威链在本主题断档（缺条目，不是重复条目）。<br>**5. 与科学层无值冲突（如实登记）**：`1.482602218505602`（本份 L71/L250 ↔ `docs/science/CALIBRATION.md:318`）、`rtol=1e-6/atol=1e-7`（本份 L323 ↔ sci:346）、掩码极性 1=坏点（本份 L128-135 ↔ sci §9a）三处逐值一致。 |
| 处置建议 | **保留（cosmetic 算法层正本）+ 立即订正四项**：①**作废并改写 L209–222 与 DISP-COS-009 的"生产传 NULL/恒等 pass"结论**（HEAD 已接线母版），并把 §9 的非退化伴随断言的立论从"现状"改为"正向条款"（标准 01 §4「只写现行设计要怎样」）；②L19–20 与 `docs/science/CALIBRATION.md:420/:314` 的 `ALG-CAL-004` 符号绑定冲突走 SCI 变更流程统一（§1「下位与上位冲突以上位为准，冲突本身是缺陷」）；③跨文件锚（`ac_api.cpp`/`astro_calibration.h`/`p1_session.cpp`/`module_adapters.cpp`）整体重锚或改由 anchor_contract 生成（§3 双向可追溯）；④补坏列定位算法条目进本份（或另立 cosmetic 域第三层正本），并把 `CALIBRATION_ALGORITHMS.md §3.4` 收缩为指针＋ID 绑定，使同一主题回到"一份正本"（§2）。 |

#### 1.2.a 对机械列的校准

`doc_mechanical.csv` 本行：`meta_block=False, frontmatter=False, upstream_pointer=True, dates=[], task_ids=[], commit_sha=[], sha256=False, history_narrative=[], dangling_links=[], unregistered_index=False`。

- **漏报（元信息块）**：实际 L5–15 ⇒ 同 1.1 根因（只认 YAML frontmatter）。
- **漏报（任务编号）**：`task_ids=[]`，实际 `DISP-COS-001..012`/`P1-COS-*`/`TEST-COS-DESIGN-001` 密集。
- **漏报（历史叙事）**：`history_narrative=[]`，实际「已退役 / 历史登记 / 补齐 / 原有条款全部保留 / 尚未存在」五处。
- **漏报（悬空/失效引用）**：`dangling_links=[]`，工具无"符号绑定错指退役实现"这一判面（本份最重的一条缺陷恰是符号级，不是文件级）。
- **正确项**：`lines=435`、`sha256=False`（本份确无 sha256）、`unregistered_index=False`（确在 `docs/DOCUMENT_INDEX.yaml:132` 登记，且 `notes` 字段自称「P1-COS-DOC 冻结」）。

### 1.3 `docs/algorithms/PHASE2_SAMPLER.md`

| 字段 | 值 |
|---|---|
| 路径 | `docs/algorithms/PHASE2_SAMPLER.md` |
| 标题 | Phase2 Sampler Algorithms（P2-SAMP / astrocs.p2.sampling） |
| 行数 | 650（`wc -l` 与机械列一致） |
| 角色 | 算法推导。目录 = 标准 01 §2 目标目录 ⇒ **一致，非角色错放** |
| 主题 | Phase2 控制点采样：稀疏球面控制点几何、background-clean 三遍管线（patch 收集→亮端迭代 clipping→邻域 tolerance/contamination 门→catalogue veto）、`control_variance = k_corr·(π/2)·σ_bg²/N` 与 control_ivar、MAD 常数的适用域实证、frame_id 内容哈希、并行/确定性、冻结禁改清单、DISP-P2SMP-001..007、测试设计 |
| 上游 | L5 `> 上游：ASTROCS_DESIGN.md §5.4（天光平面与统一相对模型）`——实测 `ASTROCS_DESIGN.md:406` §5.4 = 「天光平面与统一相对模型（UPM）」⇒ **成立**。<br>但 L452 正文另引「`ASTROCS_DESIGN.md` §8（资源与并行的强制条款…）与 **§7.1（模块边界）**」→ 实测 §7.1 = 「命令树（唯一）」（`:466`）、§8 = 「软件架构」（`:542`），而被引内容（浮点归约顺序冻结、并行开关不改变科学数值、线程预算唯一来源）实在 **§9（CPU 后端与资源）`:677`** ⇒ **本份命中"§号与标题错配"缺陷族 1 处**（派单点名的那一族，逐条核后仅此处中弹） |
| 下游 | 分片外 **26 个跟踪文件**（`git grep -c -e "PHASE2_SAMPLER.md" -- ':!docs/algorithms'`）。含 `docs/science/UNCERTAINTY_AND_COVARIANCE.md`、`docs/contracts/{DATA_SEMANTICS,PUBLIC_API}.md`、`docs/modules/{phase2_samp.md,registry/astrocs.phase2.sample.md}`、`lib/algorithms/coverage/{src/sampler.cpp,include/astro/phase2/sampler.h}`、`lib/algorithms/coverage/tests/{kcorr_lookup,sampler_parallel_consistency}_test.cpp`、`lib/algorithms/sampling/{README.md,memory.md,module.yaml}` ⇒ 非孤儿 |
| 是否正本 | 是——L3–4 自declare「本文件是 Phase2 控制点采样模块（control sampler）算法层**唯一权威**」。注意同一文件承载两个 ALG ID（§12：`ALG-P2-SMP-001` 全集 + `ALG-UPM-CONTROL-IVAR-001` 方差子面），属"多 ID 同文档"的显式声明，非重复正本 |
| 重复或重叠对象 | `docs/science/PHASE2_UPM.md`（SCI-UPM-001，含 SCI-UPM-WEIGHT-001）。**两处原文**：本份 L248 `control_variance = k_corr × (π/2) × σ_bg² / N_retained    # :875` ↔ `docs/science/PHASE2_UPM.md:5` 页头「范围: SCI-UPM-001..010 + **SCI-UPM-WEIGHT-001** + … 模块: phase2 (upm/**sampler**)」（本份 L7-10 与 §5.4 L245 明指该 SCI 为公式权威，§11.4 L598-600 并钉死方向「两处冲突时以 docs/science/ 为准并回改本文档」）。<br>重叠范围：控制点几何与方差公式两处各写一遍；差异：本份给**离散实现 + 行号 + MC 适用域 + 退化分支发布口径**，SCI 给连续定义 ⇒ **合法下钻**，且本份显式声明方向与冲突裁决，是本批三层分工最干净的一份。另与 `docs/algorithms/UPM_SOLVER.md`（ALG-UPM-001，本份 L14 标为"下游消费方"）为分工不重复 |
| 元信息块 | **有**：L3–5 与 L7–20 两块连续引用，含「ID: ALG-P2-SMP-001 状态: CONTRACT_READY」+ 上游/下游/辅助 SCI/关联 ALG/模块/DATA/API/MOD 全清单 ⇒ 违标准 01 §4「文档开头无元信息块」 |
| 写法违规 | ① L4 `状态: CONTRACT_READY` 状态编号入开头；同块 L20「IMPLEMENTED 词由验收在建立后签发」属过程性纪律声明。<br>② 任务/缺陷编号密集：`DISP-P2SMP-001..007`（§11.2）、`P2-SAMP-IMPL`（L19/549）、`P2-SAMP-TEST`（L564/628）、`TEST-P2-SMP-DESIGN-001`/`TEST-P2-SMP-001`、`SC-005`（L318/464）、`FZ-PROV-KCORR`（L315）、`CON-004`（L434）、`P0-01`（L113/425）、`UPMW-004/005/007`、`SRC-P2-SMP-001`（L532）、`DATA-FRAME-ID-001`、`run/SCI-FIX-PHASE2-01` 型轮次号（6 处）。<br>③ 历史叙事：L325–326「**旧行为**「域外 clamp」把静默饱和固化成合同（**旧** `kcorr_lookup_test.domain_clamp`），已被 … 取代」、L557「`:1078-1079` 注释自述**曾**修 double-count，此残留与其意图矛盾」（"曾"= §4 点名禁词）、L464「`geometric_reliability` 为**禁用**旧名」、L450–451「仅影响**旧** target 编译面」、L633 节标题「— SCI-001-S2 **补齐**」、L635「原有条款全部保留」。<br>④ L3 与 L5 之间把「上游」指针夹在元信息块中间（同一块内又写状态与 ID），标准 01 §3 只要求开头标明上游条款——指针本身在，块体本身违规。<br>⑤ 无日期；`465` 行「wiki 语义版本 34A532A2...B2EB308」是**被省略号截断的哈希**，不可复核 |
| 悬空引用 | 1. **10 处 `evidence/*.json` + 6 处 `run/…` 证据指针全部落在不入库区**：L76-77 `run/SCI-FIX-PHASE2-01/real/r2_m42_rawframe.py`、`run/SCI-FIX-PHASE2-01/evidence/r2_m42_raw.json:R2e_unit_closure`；L222 `run/SCI-FIX-PHASE2-01/exp/e1e2_domain_calibration.py`；L278 `run/…/exp/e3e4_floor_and_structure.py`；L332 `run/VARIANCE-SEMANTICS-01/probe/out_probe_orig_m42_1024_1024/hips/signal/properties`；另有裸 `evidence/e3c_weight_pollution.json`、`evidence/e1e2_domain.json`、`evidence/e3e4_floor_structure.json`、`evidence/r2_m42_raw.json` 共 10 处相对写法。<br>确认命令：`git ls-files -- run` → 仅 `run/.gitkeep`（`.gitignore:17 run/*`）⇒ **本份所有定量证据（σ_bg 中位、k_corr 实证 1.3883、结构膨胀 18.4659、伪 ivar 1.314e26、22 dex 单位闭环）在跟踪集内无载体，独立读者不可复核**；这不是"文件被删"而是证据未入 `artifacts/evidence/`，违标准 01 §2「需要留证的台账落 `artifacts/evidence/` 并由正本引用」。<br>2. 文件级链接：`docs/science/PHASE2_UPM.md`（§7a 实测 `:168` = 「表示能力边界与近奇异处置」✓）、`docs/contracts/DATA_SEMANTICS.md §23`（`:1689` ✓）、`§23.1/:1704`、`§23.2/:1749`、`§31.1a`（`:31/:47/:451` 在）、`docs/science/SCIENCE_SCOPE.md`「## 处理链」（`:14` ✓）、`lib/algorithms/coverage/{src/sampler.cpp,include/astro/phase2/sampler.h,src/upm.cpp,tools/stage2.cpp(2015 行)}`、`lib/algorithms/sampling/`（3 个跟踪文件，与 L18-20「合同三件套、dll 尚未存在」一致，本份无 1.2 那类迁移态失真）⇒ **0 文件级死链** |
| 与上位冲突 | **1. 同一文档内对同一测试给出互斥结论（自相矛盾 + 与工程事实三方不合）**：<br>§5.4 L312–313 「当前生产取值 1.4 的来源是项目自产 MC（`control_median_mc_test`…），而**该测试未注册/不在任何 CMake/ctest/CI 面（构建孤儿）**⇒ 该常数在仓内**不可复跑**」<br>↔ §7 表第 6 行 L463 「k_corr MC 校准非猜测（sampler.h:50-51）…**MC 证据源 `control_median_mc_test` 已注册 ⇒ 可复跑**」。<br>实测三方：`lib/algorithms/drizzle/healpix_drizzle/tests/CMakeLists.txt:110 add_executable(control_median_mc_test …)`（在 CMake）、`eng/ci/checks.json:4363/:4687` 有 `drizzle_control_median_mc`（在 CI 面）、而 `eng/tests/unit/CMakeLists.txt:1540` 注释判「现行事实: control_median_mc_test / kcorr_matrix_test 属 **tracked-but-unbuilt**，PHASE2_UPM.md 必须显式标 MISSING」。<br>⇒ 三处各说一套，本份两处互斥且**都不完整**；`k_corr = 1.4`（冻结常数、§10 禁改清单第 2 条）的可复跑性因此**未定案**，是本批唯一影响科学参数的登记缺陷。<br>**2. 文件规模自declare不实**：L15–16 与 §3 标题 L89 两处称「sampler.cpp（**1536 行**）」「sampler.h（**288 行，实测**）」→ 实测 `wc -l` = **1503 / 273**（−33 / −15）。"实测"二字使其成为事实断言；且 §3–§5 的锚全部按 1536 行标定 ⇒ 尾部锚系统性偏大。<br>**3. 跨文件锚漂移**：`upm.cpp:2766-2790`（`p2_upm_control_variance`）→ 实测 **`:2954`**；`upm.cpp:2772-2783`（k_corr 定义域 rc=2/rc=1 判据）→ 同区漂移；`upm.cpp:565`（per-control 可靠度=配置常量 1.0 的归一化消费）→ 实测 `:565` 是 `const double lambda_s = std::max(0.0, cfg.smoothing_lambda);` ⇒ **锚落在 smoothing 参数上，与所claim 的语义无关**；`sampler_parallel_consistency_test.cpp:29`（`TEST(Phase2SamplerParallel, OneTvsTwoTDeterminism)`）→ 实测 TEST 在 **`:41`**，`:29` 是 `mix()` 哈希工具函数（§11.3 F8 与 §6 确定性两条判据的载体锚皆错）。<br>同一份内**自相矛盾的锚**：kcorr 表位置 L322「lookup 表 :96-99」↔ §11.3 F2「表值 :94-97」；而 §3 内部符号表 L110 写 `kcorr_lookup :88-109`（实测该函数区确在 88–117，`pf_grid/sc_grid/{1.2112,1.3925,1.4980}` 逐值命中）⇒ 三处三种区间，表值本身准确。<br>仍准确组：`coverage/CMakeLists.txt:28`（`option(P2_ENABLE_OPENMP …)` 逐行命中）、`upm.h:38-41`（obs.ivar 弃用）、`upm.h:51-53`（snr_available 禁以 1.0 伪装）⇒ 头文件锚准，`.cpp`/跨模块锚漂移，与 1.1/1.2 同形态。<br>**4. 同一实测量两处值不同（k_corr 标定域失配的"实测"角尺度）**：本份 L328–330 「本仓 M42 300 s R 帧实测 **0.9890″/px**（由 FOCALLEN=1877 mm、XPIXSZ=9 µm 推得 206264.806×9e-6/1.877）」 ↔ 代码注释 `lib/algorithms/coverage/src/sampler.cpp:90` 「生产真帧源像素角尺度 **0.9586″/px** 远在标定域 [300,600]″ 之外」。<br>⇒ 两处都自称"实测/真帧"，相差 3.2%；不影响"标定域外 300×"的结论方向，但作为**适用域声明的定量依据**必须二选一并给推导（前言 §4 要求贴两侧原文，已贴）。<br>**5. 与上位不构成冲突的一条（如实登记以免整治误删）**：§6 L444–446 与 §10 第 7 条「输出 obs 序列 **bitwise** 与 worker 数无关」看似与 `ASTROCS_DESIGN.md:677`「1/N worker 的等价判据是事前冻结的浮点容差，**不是逐位一致**」相抵，但同上位句续文允许「由构造保证位精确的路径（固定槽位写回、per-thread 定序归并）其模块自有门仍可断言 bitwise」⇒ 本份属被允许的特例，**不判冲突**。 |
| 处置建议 | **保留（采样算法层正本）+ 就地整改四项**：①消解 §5.4 与 §7 对 `control_median_mc_test` 的互斥结论，与 `eng/tests/unit/CMakeLists.txt:1540` 的 tracked-but-unbuilt 现行事实三方合并为一句可核口径（标准 01 §4「自洽」、§1「冲突本身是缺陷」）；②规模数字按 `wc -l` 实测更正（1503/273），`upm.cpp:565`、`upm.cpp:2766-2790`、`sampler_parallel_consistency_test.cpp:29` 三处错锚重锚，kcorr 表区间三处统一（§3 双向索引可追溯）；③10 处 `evidence/*.json` 与 6 处 `run/…` 证据迁入 `artifacts/evidence/` 并由本份引用（标准 01 §2 留证规则）；④删开头元信息块、L452 的「§7.1（模块边界）」改指真实条款，并把「旧行为/曾修/禁用旧名/补齐/原有条款全部保留」改写为正向条款（§1 唯一权威链 + §4 写法）。 |

#### 1.3.a 对机械列的校准

`doc_mechanical.csv` 本行：`meta_block=False, frontmatter=False, upstream_pointer=True, dates=[], task_ids=[], commit_sha=[], sha256=False, history_narrative=[], dangling_links=[], unregistered_index=False`。

- **漏报（元信息块）**：实际 L3–5 + L7–20 **两块** ⇒ 比 1.1/1.2 更重（同一根因，本份最密集）。
- **漏报（任务编号）**：`task_ids=[]`，实际含 `SCI-FIX-PHASE2-01`、`VARIANCE-SEMANTICS-01` 两个**轮次号**与 7 个 DISP、5 个 TEST/ID 族。
- **漏报（历史叙事）**：`history_narrative=[]`，实际「旧行为 / 已被取代 / 曾修 / 禁用旧名 / 旧 target / 补齐 / 原有条款全部保留」七处，其中「曾」是 §4 点名禁词。
- **漏报（悬空引用）**：`dangling_links=[]` ⇒ 工具的相对链接判面不覆盖 ``run/…`` 与裸 ``evidence/…`` 反引号路径（本份 16 处，是全文档集里最集中的一型）。
- **误报候选**：`sha256=False`，但 L465 含被省略号截断的 `34A532A2...B2EB308` 与 L410/L421 的「SHA-256 前缀 16 hex」——**判 False 正确**（哈希算法描述不是哈希值），列此为工具的负例样本。
- **正确项**：`lines=650`、`unregistered_index=False`（`docs/DOCUMENT_INDEX.yaml:200` 已登记，`upstream` 字段与正文一致）。

## 1.4–1.13 十份 `工程控制/RELEASE-05/tasks/` 任务书（先共用判定，再逐份登记）

**判据**：`CONTROL_PACK_SPEC.md §5`「任务文件模板（每任务一份）」给出**唯一模板六节**（`:98` §5，实测节名 `## 目标 / ## 权威依据 / ## 改动范围（文件域，互斥）/ ## 步骤 / ## 验收门（可机器复跑）/ ## 禁止`）；`§6.3`（`:161`）给任务状态词表 `NOT_STARTED→IN_PROGRESS→PASS/FAIL/BLOCKED` 且「PASS 仅由前台验收后写入」；`§9`（`:「## 9. 阶段汇总与收口清理」`）判收口处置——**收口即清理**：「SUMMARY 中有长期价值的结论沉淀进正式文档…随后把本控制包目录从仓库移除（与清理任务同一提交）」，标准 04 §4 第 3 条同向（「历史控制包、一次性过程脚本、过期台账不留在生产目录」）。

**收口状态事实**（取包内 `SUMMARY.md:5`「状态：**READY_FOR_OWNER_REVIEW**…本包**未完成，不进入 FIN，不申请发布**」与 `:9-11` 三档表）：

| 状态 | 本批十份中的归属（实测自 SUMMARY 表逐字） |
|---|---|
| PASS（17 项内） | DOC-501、CONTRACT-501、ARCH-501、ARCH-502、ARCH-503、ARCH-504、ACCEPT-501 |
| BLOCKED（1 项） | ARCH-505（OQ-9：块流合同与代码真实数据流不一致，18 偏差/13 blocker） |
| NOT_RUN（4 项内） | CLEAN-501、BLD-501（Windows 腿） |

**共用判定（十份一致，逐份不再重复）**：

1. **角色** = 操作与运营（控制包任务书）。实际目录 `工程控制/RELEASE-05/tasks/` 不在标准 01 §2 的任何目标目录内，但**本批不判"角色错放→迁往 docs/"**：§2 的目录表管的是**正式文档层**，控制包按 `CONTROL_PACK_SPEC §9` 是**临时设施**（收口即从仓库移除）；把任务书迁进 `docs/operations/` 反而会把它变成长期文档层里的过程台账，正撞标准 01 §2 末条「历史档案、过程台账…不放在正式文档层」。**正确处置 = 随包移除**（未收口前原地保留）。
2. **是否正本** = 否（十份皆是"产出指令"，其主题正本在 `docs/**`，逐份给出该正本路径）。
3. **索引登记** = 十份均未登记，且**不构成标准 01 §3 缺陷**：`docs/DOCUMENT_INDEX.yaml:40` 的 `coverage` 自定范围为「docs/** + 仓库根治理文档」，`:30` 扫描口径明列排除「实验/、工程控制/、logs/ 等过程与证据目录」。⇒ 此处**与 `AUD-101-DB-19.md` §3.2 的判定不同**：它把"控制包不在索引"升格为需负责人裁决的规范冲突，本批复判为**索引自洽、无冲突**；真实风险面是"若收口后不移除，则该目录既不在索引也不在正式文档层"，而那由 §9 的移除义务兜住。
4. **元信息块** = 十份均无（无 `>` 引用块、无 frontmatter）⇒ 机械列 `meta_block=False` 在这十份上**判对**（对照 1.1–1.3 的三例漏报，说明 `meta_block` 判面只对引用块形态失效，不是恒漏报）。
5. **上游指针** = 9 份有 `## 权威依据` 节，`BLD-501` 无该节（机械列 `upstream_pointer` 十份十判对）。按前言 §4 的定义（`上游` = 该文档自己标明的**最高设计**条款），**上游齐 8 份、缺 2 份**：`BLD-501` 整节缺；`CLEAN-501:6-7` 有「权威依据」节但三条全是包内/工程规范条款（`ENGINEERING_SPEC §2；GAP_AUDIT G2-6、G3-1/G3-2；RELEASE-04 ACCEPTANCE D-2`），**不含最高设计条款**——机械列对它判 `upstream_pointer=False` 亦正确（见 1.11）。
6. **下游** = 包内以**任务 ID**引用（非路径级）。复算命令与计数（`git grep -l -w <ID> -- ':!run'` 再排除该任务书自身）：

   | ID | 引用文件数 | 代表性引用者（含**包外**） |
   |---|---|---|
   | ACCEPT-501 | 18 | `artifacts/evidence/release-05/AUDIT_PACKAGE.md`、`eng/tests/unit/{calibration,cosmetic}_integration_test.cpp`、包内 ACCEPTANCE/SUMMARY/TASK_LIST |
   | ARCH-501 | 16 | **`CMakeLists.txt`**、`eng/ci/ID_MIGRATION_MAP.md`（`:88 CHK-ARCH501-BLOCK-FRAME`、`:264 CTEST-CORE-BLOCK-FRAME`）、`docs/contracts/…` |
   | ARCH-502 | 20 | `CMakeLists.txt`、`eng/ci/ID_MIGRATION_MAP.md`、`eng/tests/runtime/integration/…` |
   | ARCH-503 | 14 | `CMakeLists.txt`、`docs/contracts/PIPELINE_BLOCK_CONTRACT.md`、`eng/ci/ID_MIGRATION_MAP.md` |
   | ARCH-504 | 18 | `CMakeLists.txt`、`eng/tests/unit/export_stream_test.cpp`、`eng/ci/ID_MIGRATION_MAP.md` |
   | ARCH-505 | 31 | `CMakeLists.txt`、`docs/contracts/DUAL_LINE_CONTRACT.md`、`artifacts/evidence/known-limitations-ledger/LEDGER.md`、`eng/ci/ID_MIGRATION_MAP.md`（`:98/:151/:153/:262`） |
   | BLD-501 | 11 | `lib/infrastructure/scheduler/src/module_adapters.cpp`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md`、`reports/v19r2/…` |
   | CLEAN-501 | 15 | `docs/modules/MODULE_MAP.yaml`、`eng/contracts/data/v6_clause_registry_v1.json`、`reports/v19r2/…` |
   | CONTRACT-501 | **42** | `CMakeLists.txt`、`docs/DOCUMENT_INDEX.yaml`、`docs/contracts/DUAL_LINE_CONTRACT.md`、`docs/contracts/PERF…` |
   | DOC-501 | 8 | 包内 `00_README/ACCEPTANCE/…`、`reports/v19r2/…` |

   命令的已知应命中正例（前言 §5.2 要求）：`git grep -l -w 'CHK-RETIRED-CODE' -- eng` → `eng/ci/check_retired_code.py`、`eng/ci/checks.json`、`eng/ci/ID_MIGRATION_MAP.md` ✓ 命中。
   ⇒ **十份都不是孤儿**；且路径级引用为 0、ID 级引用密集，其中 **6 份的 ID 已进入 `CMakeLists.txt`、生产源注释与 CI 注册表**——这条对处置有决定意义，见 §共用判定 7。
7. **收口动作的可执行性（本批新判据，非 DB-19 覆盖的轴）**：`CONTROL_PACK_SPEC §9` 要求"把本控制包目录从仓库移除"，但 `eng/ci/checks.json` 与 `eng/ci/ID_MIGRATION_MAP.md` 里的门名以任务 ID 为词根（实测 `CHK-ARCH501-BLOCK-FRAME`、`CHK-ARCH505-BLOCK-FLOW`、`CTEST-CORE-BLOCK-FRAME`、`CTEST-BLOCK-FLOW`，另 `ID_MIGRATION_MAP.md:5` 自称由 `gen_id_migration_map_doc.py` 生成、`--check` 判绿）。⇒ **直接删目录会让机器门 ID 指向工作区里不再存在的任务书**，其含义只剩 git 历史可查。收口前置动作 = 先把 `CHK-<任务 ID>-*` 门名与其"任务名"字段解绑/改名（属 `eng/ci` 域，归 GATE 线，不由文档整治单方做）。

**写法违规的共用形态**（标准 01 §4 对任务书有例外吗？没有——但 §4 禁"任务编号"，而任务书的**任务编号是控制包形态的固有物**，`CONTROL_PACK_SPEC §5` 的模板与 §6.3 的状态词都以 ID 为键）：本批对"任务编号密集"只登记、**不据此判红**；判红的对象是三类越界：①把**一次性盘点读数**写进任务书（会随代码漂移失真，见 ARCH-501/CLEAN-501）；②把**已作废的科学定案当现行判据**（见 ACCEPT-501，本批最高价值单条）；③把**状态叙事塞进禁止项**（ARCH-504/505）。

### 1.4 `工程控制/RELEASE-05/tasks/ACCEPT-501.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/ACCEPT-501.md` |
| 标题 | 任务：ACCEPT-501 独立验收（五一致性自迭代） |
| 行数 | 33（`wc -l`，与机械列一致） |
| 角色 | 操作与运营（控制包任务书）。目录与处置见 §1.4–1.13 共用判定 1（不判角色错放，处置=随包移除） |
| 主题 | 独立验收的 8 大类五一致性清单、验收者角色约束（只读+出报告+派单）、回派—复验收敛流程、四项验收门 |
| 上游 | `:7`「最高设计 §12；ACCEPTANCE_SPEC 全文；CONTROL_PACK_SPEC §7。」三条均存在：`ASTROCS_DESIGN.md:721` §12「验证体系」、`ACCEPTANCE_SPEC.md` 在跟踪集、`CONTROL_PACK_SPEC.md:176` §7「独立验证（**前台**职责）」。**标题级错配 0 处**；但 §7 把独立验证定为**前台职责**而本任务书 :3 指定由「独立验收子代理」执行 ⇒ 条款归属与本件角色分配不同口径（轻，登记）。 |
| 下游 | ID 级 18 个文件（命令与列表见共用判定 6）；路径级 0 |
| 是否正本 | 否（产出物是验收报告；科学判据正本在 `ACCEPTANCE_SPEC.md` 与 `docs/science/**`） |
| 重复或重叠对象 | `ACCEPTANCE_SPEC.md` §2 的 SCI 判据表。**两处原文**：本件 `:13`「## 验收清单（逐项映射，出五列对照表：科学口径/文档条款/合同 schema/代码 file:line/测试用例）」+ `:14`「测光链：积分公式、IRLS、引导检测、k_photo/m(x,y)、适用域 n≥100、降级路径」↔ `ACCEPTANCE_SPEC.md` §2 的 SCI-A 逐行判据全文。<br>重叠范围：本件把判据**压缩成检索词式清单**，属合法下钻；差异＝压缩时**丢限定语并塞进一条正本没有的判据**（见「与上位冲突」1） |
| 元信息块 | 无 |
| 写法违规 | `:1`/`:11` 任务编号与 ARCH/GATE/SCI 跨任务编号（控制包形态，登记不判红）；`:32`「验收报告由非执行者署名（**子代理 ID 记录**）」机器 ID 入正文（轻，属判据所需）；`:30-33` 四项 `- [ ]` 未勾（任务书本身无状态字段，合规——状态词表属 `CONTROL_PACK_SPEC §6.3`，PASS 只写在 TASK_LIST/SUMMARY，不在任务书内） |
| 悬空引用 | `:26`「写入 `OPEN_QUESTIONS.md`」→ 包内文件存在（`工程控制/RELEASE-05/OPEN_QUESTIONS.md`，跟踪集 ✓），列出以证已核。`:15`「SCI-501 后无双计」、`:16`「（SCI-502）」指同包任务书 → `tasks/SCI-501.md`/`SCI-502.md` 均在跟踪集 ✓。**无文件级死链** |
| 与上位冲突 | **本批最高优先级单条：`:14`「适用域 n≥100」把已作废的科学定案当现行验收判据**。<br>① 科学正本现文 `docs/science/PHOTOMETRY.md:394` §16.5 标题「星数依赖与降级语义（SCI-A 实验定案）」，`:399` 第 1 条逐字：「**星数不构成拒绝条件**：不存在星点少到无法测光的图；任何可解析帧都完成测光定标并出产品…；**不设固定星数门槛**」——全文 `grep 'n≥100\|星数门槛'` 仅命中该否定句，**无任何 n≥100 门槛**。<br>② 裁决在案 `工程控制/RELEASE-05/OPEN_QUESTIONS.md:40`「## Q-5（科学文档 vs 代码落地缺口）测光星数门槛」，`:42`「**状态：已裁决**。负责人裁决：…把相关门槛与限制都删掉；星数真的少时应报拟合失败，而不是用门禁去卡」，`:43-46` 记录删除已贯穿 `ASTROCS_DESIGN.md`、`PHOTOMETRY.md §16.5`、`REPORT_paper.md`、`module_adapters.cpp`（`P1_PHOT_MIN_FIT_STARS` 已删）、`eng/ci/check_provenance_consistency.py`。<br>⇒ 独立验收者按 `:14` 执行，会把"没有 n≥100 门"记成**科学-文档一致性缺陷**并回派修复，即**错误的验收清单会生产错误的结论**。<br>③ **同一残留在包内还有一处**：`SUMMARY.md` §2「测光星等坐标系（SCI-A）…边界：适用域为拟合星数 n≥100（`PHOTOMETRY.md` §16.5）」——引用的正是那条写着"不设门槛"的节 ⇒ 该作废口径同时活在任务书与收口报告两处（`SUMMARY.md` 属 DB-19 范围，此处只作关联举证）。<br>④ 本条**独立复算并证实** `AUD-101-DB-19.md` §1.16 的同一判法（非沿用：两侧原文与 Q-5 原文均为本机 `grep`/`sed` 取证）。 |
| 处置建议 | **保留原地（未收口的控制包内合规形态）+ 收口前必改一处**：`:14` 删「适用域 n≥100」，或改为「星数不构成拒绝条件（`docs/science/PHOTOMETRY.md §16.5` 第 1 条）」的正向表述。理由：标准 01 §1「下位文档与上位文档冲突时以上位为准；**冲突本身是缺陷**，需在整治中消除」＋§4「只写现行设计要怎样」。目录层面按 `CONTROL_PACK_SPEC §9` 随包移除（前置动作见共用判定 7）。 |

### 1.5 `工程控制/RELEASE-05/tasks/ARCH-501.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/ARCH-501.md` |
| 标题 | 任务：ARCH-501 命名块机制与块生命周期（A 线） |
| 行数 | 27（`wc -l`，与机械列一致） |
| 角色 | 操作与运营（控制包任务书）。见共用判定 1 |
| 主题 | 阶段内命名块内存管线：block 元数据/引用计数/创建-消费-销毁状态机、DAG 校验器三类非法图、provenance 随块流转、内存即时归还的可观测单测 |
| 上游 | `:7`「最高设计 §8.1/§8.2；ENGINEERING_SPEC §4.1；CONTRACT-501 的块生命周期合同。」实测 `ASTROCS_DESIGN.md:544` §8.1「总原则：唯一 CLI 入口、阶段独立调度器」、`:571` §8.2「阶段内：命名块内存管线与块生命周期」、`ENGINEERING_SPEC.md:55` §4.1「管线纪律（对应最高设计 §8）」、合同 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md` 存在 ⇒ **四条依据全部可核，本批任务书中依据最实的一份**，无 §号错配 |
| 下游 | ID 级 16 个文件（含 `CMakeLists.txt`、`eng/ci/ID_MIGRATION_MAP.md:88/:264` 的 `CHK-ARCH501-BLOCK-FRAME`/`CTEST-CORE-BLOCK-FRAME`）；路径级 0 |
| 是否正本 | 否（块生命周期正本是 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`，本件是其产出指令） |
| 重复或重叠对象 | `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`。**两处原文**：本件 `:15`「实现 block 元数据（合同字段）、引用计数/消费者集合、创建-消费-销毁状态机；块被全部声明消费者用完即销毁、内存归还」↔ `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`（同名条款的合同全文；该件在跟踪集且被 `eng/ci/ID_MIGRATION_MAP.md` 引用）。<br>差异：本件用「合同字段」三字**引用**合同而不复制字段表 ⇒ 标准 01 §1 的合法分层，**不算重复** |
| 元信息块 | 无 |
| 写法违规 | `:14`「盘点现有 pipeline/ 与 orchestrator 中可复用的块/帧原语（**34 文件 18427 行**未接入生产…）」——一次性盘点读数写进任务书（详见「与上位冲突」2）；`:1`/`:11`/`:27` 任务编号（形态所需）；`:14-19` 步骤为时序指令（任务书允许） |
| 悬空引用 | **无文件级死链**：`:10` 允许改面 `lib/infrastructure/pipeline/**` 实测跟踪 65 个文件、`:11` 指名的「三阶段 Session（ARCH-505 域）」实测 `lib/phase1_session` 16 + `lib/phase2_session` 5 + `lib/phase3_session` 5 个跟踪文件，均在位 ⇒ 文件域**当前可执行**（对已 PASS 的任务而言，"可执行"意即仍能复跑其验收，见下） |
| 与上位冲突 | **1. 与包内 GAP_AUDIT 的同一口径互斥**：`:14`「34 文件 18427 行」↔ `GAP_AUDIT.md:11`「G0-3（D-2 B1） | pipeline/orchestrator **50 文件 18411 行**未接入生产」↔ 本机 HEAD 复算：`git ls-files -- lib/infrastructure/pipeline/orchestrator \| wc -l` = **53**，其中代码文件（cpp/h/hpp/c/cc）= **36 个 / 19052 行**。<br>⇒ 三个数两两不同，且两个文档数都不可从 HEAD 复跑；三处均未声明计数口径（是否含测试/头文件/非代码文件）。CLEAN-501 `:14` 又引「B1 orchestrator 50 文件 18411 行」，与 GAP_AUDIT 同值而与 ARCH-501 不同 ⇒ **包内一处 50/18411（两处引用）、一处 34/18427，两者都不等于实测**。<br>危害：下一个按数规划删除量与回归成本的对话会拿到互斥基线，且删除回执（CLEAN-501 验收门第 1 条）无法对账。<br>**2. 与上位不冲突的一条**：`:15` 的状态机/引用计数条款与 `ASTROCS_DESIGN.md §8.2`（`:571`）同向 ⇒ 登记为一致。<br>**3. 收口后可执行性**：本件已 PASS，其验收门（`:21-24`）四条均无门名/脚本名，只写"单测覆盖/可观测/各有红绿测试"，且对应门确实存在（`CHK-ARCH501-BLOCK-FRAME` 在 `eng/ci/ID_MIGRATION_MAP.md:88`，判据类别 doc/P0，绑 `CTEST-CORE-BLOCK-FRAME`）⇒ **锚未漂到无关代码**；唯一问题是盘点的三个数 |
| 处置建议 | **保留原地 + 订正 `:14`**：删「34 文件 18427 行」或改为可复跑命令（`git ls-files -- lib/infrastructure/pipeline/orchestrator \| wc -l` 与代码文件行数同给口径），只留"甄别可复用原语、避免重写"的正向条款。理由：标准 01 §4「公式、数值表、源码行号…**放对应下层文档**」的反面即"一次性读数不写进规范体"，§1「冲突本身是缺陷」；目录处置随包移除。 |

### 1.6 `工程控制/RELEASE-05/tasks/ARCH-502.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/ARCH-502.md` |
| 标题 | 任务：ARCH-502 normalize 异步工作流调度器（A 线） |
| 行数 | 28（`wc -l`，与机械列一致） |
| 角色 | 操作与运营（控制包任务书）。见共用判定 1 |
| 主题 | normalize 阶段调度器：帧内 DAG 流水 + 多帧并发 + 空闲预取、统一资源预算源、探针事件流（JSONL）、1/N 确定性与取消语义 |
| 上游 | `:7`「最高设计 §8.3（normalize 行）、§9；CONTRACT-501 调度器接口与探针合同。」实测 `ASTROCS_DESIGN.md:597` §8.3「三个阶段调度器」表内有 normalize 行（`:602`）、`:669` §9「CPU 后端与资源」⇒ **成立，无 §号错配** |
| 下游 | ID 级 20 个文件（含 `CMakeLists.txt`、`eng/tests/runtime/integration/**`、`eng/ci/ID_MIGRATION_MAP.md`）；路径级 0 |
| 是否正本 | 否（调度器接口/探针正本是 `docs/contracts/SCHEDULER_CONTRACT.md` 与 CONTRACT-501 系合同，本件为产出指令） |
| 重复或重叠对象 | `docs/architecture/THREAD_BUDGET_ARCH.md`。**两处原文**：本件 `:14`「统一资源预算源：线程数、内存上限、队列深度从配置/资源门读取；**模块不得私建线程池**」↔ `ASTROCS_DESIGN.md:610`「一个进程只有一个资源预算源（§9），线程池的唯一来源是调度器；科学计算内核单层并行」与 `:673`「worker 数只来自 profile 与预算对象，模块不硬编码线程数、**不私建长期线程池**」。<br>差异：任务书是禁令、上位是原则＋"长期"限定词 ⇒ 合法分层，但**限定词不一致**（见「与上位冲突」2） |
| 元信息块 | 无 |
| 写法违规 | `:1`/`:28` 任务编号（形态）；**`:19`「取消：SIGTERM 原子落盘语义、退出码 9 **保持**」**——"保持"是标准 01 §4 点名的历史口径词，应写「退出码 = 9」的现行条款；`:18`「JSONL」格式名入正文（可） |
| 悬空引用 | 无死链：`:10` 允许改面 `lib/infrastructure/scheduler/**` 实测 26 个跟踪文件、`lib/infrastructure/cli/normalize/**` 实测 1 个跟踪文件 ⇒ **在跟踪集**（`cli/*` 各只 1 个文件，"入口骨架在、实现分散在 scheduler"，不影响域可执行性）；`:19` 的退出码 9 在 `ASTROCS_DESIGN.md:508`「| 9 | 用户取消或超时 |」与 `:512`「取消（Ctrl-C）：…→ exit 9」、`docs/contracts/LOG_AND_ERROR_CONTRACT.md:105`「`CANCELLED` | 9（CANCELLED）」三处定义 ⇒ 条款可核 |
| 与上位冲突 | **1. 判据严格度与上位相反（本份主条）**：`:18`「确定性：固定 worker 数下归约顺序冻结，**1/N worker 逐位一致**」+ 验收门 `:22`「合成多帧任务：N=1/2/4/8 worker 结果**逐位一致**」↔ 上位 `ASTROCS_DESIGN.md:616`「**不变量**：数值结果与并发度无关——1/N worker **数值等价**，判据是事前冻结的浮点容差；**归约顺序冻结是达成手段，不是判据本身**。由构造保证位精确的路径（固定槽位写回、per-thread 定序归并）其模块自有门仍可断言 bitwise，但**合同层不要求逐位一致**」与 `:602` normalize 行同口径。<br>⇒ 本件把"手段"写成"判据"，且未声明"构造保证位精确"这一上位允许 bitwise 的前提。后果：以本件为验收依据会把"容差内的数值等价"判红，或反向逼实现为凑 bitwise 而牺牲并行度。<br>**注意不是全否**：上位明令允许模块自有门断言 bitwise，所以"红"的成立条件是"该门被当成合同层判据"——本件 `:22` 正是以任务书验收门身份出现 ⇒ 属**与上位口径不齐**，处置为改写而非删除。<br>**2. 禁令过严**：`:14`「模块不得私建线程池」无上位例外条款，而上位 `:673` 限定的是"**长期**线程池"；`mosaic` 预取等短生命周期线程在上位登记制下允许 ⇒ 本件字面禁令严于上位 |
| 处置建议 | **保留原地 + 订正两处**：①`:18`/`:22` 的"逐位一致"改为「1/N worker 数值等价，判据 = 事前冻结浮点容差；构造保证位精确的路径可另设 bitwise 自有门」（直接引 `ASTROCS_DESIGN.md §8.3` 不变量句，标准 01 §1「下位与上位冲突时以上位为准」）；②`:14` 补上位限定词「不私建**长期**线程池；需专用线程走 THREAD_BUDGET 登记」；③`:19`「保持」改正向条款「退出码 = 9」。收口随包移除。 |

### 1.7 `工程控制/RELEASE-05/tasks/ARCH-503.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/ARCH-503.md` |
| 标题 | 任务：ARCH-503 mosaic 天球窗口并行调度器（A 线） |
| 行数 | 28（`wc -l`，与机械列一致） |
| 角色 | 操作与运营（控制包任务书）。见共用判定 1 |
| 主题 | mosaic 阶段调度器：天球窗口（tile）为调度单元的窗口并行、输入帧按窗口路由消除整帧读放大、窗口内固定顺序、稠密 SNR 现场求值、归约顺序冻结、按窗口探针 |
| 上游 | `:7`「最高设计 §8.3（mosaic 行）、§5；CONTRACT-501 调度器接口合同。」实测 `:597` §8.3 表内有 mosaic 行（`:604`）、`:365` §5「mosaic（Phase2）」⇒ **成立** |
| 下游 | ID 级 14 个文件（含 `CMakeLists.txt`、`docs/contracts/PIPELINE_BLOCK_CONTRACT.md`、`eng/ci/ID_MIGRATION_MAP.md`）；路径级 0 |
| 是否正本 | 否（窗口/归约顺序现行结论在 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md` 与 `docs/science/**`） |
| 重复或重叠对象 | `ASTROCS_DESIGN.md §8.3` mosaic 行。**两处原文**：本件 `:16`「窗口内固定顺序：**coverage→UPM（SNR 加权稀疏采样）→排异→SNR² 集成**；窗口间独立」↔ `ASTROCS_DESIGN.md:604`「…窗口内 **UPM→排异→集成**顺序固定，窗口间无共享可变状态」。<br>差异：本件在窗口内顺序前端多列一个 `coverage` 节点、并把"集成"具体成"SNR² 集成"⇒ 是**下钻加细**而非矛盾（`§5.2` 固定科学流程含 coverage），但 §4 要求复述处给指针；轻。 |
| 元信息块 | 无 |
| 写法违规 | `:1`/`:11`/`:28` 任务编号（形态，含跨线引用 SCI-502/PERF-501）；**`:15`「消除整帧读放大——**RELEASE-04 实测约 3.9GB/帧**」**——跨控制包编号 + 一次性读数写进任务书（RELEASE-04 包已不在跟踪集，见「悬空引用」1）；`:17`「稀疏-稠密等价**已证 3.1e-15**」无载体路径（同条既当"已证"结论又无出处） |
| 悬空引用 | 1. **`:15` 的 RELEASE-04 载体不在跟踪集**：`git ls-files 工程控制 \| awk -F/ '{print $2}' \| sort \| uniq -c` → 仅 `31 RELEASE-05` ⇒ 包名 `RELEASE-04` 无工作区落点，"约 3.9GB/帧"不可复跑；同值读数另见 `SUMMARY.md`/`HANDOFF.md`（DB-19 范围），三处均无测量口径（帧数、看门狗采样点、RSS 还是 Pss）。<br>2. `:17` 的 `3.1e-15` 无 `run/` 或 `artifacts/evidence/` 指针 ⇒ 登记为**引用不完备**（不是死链，是缺载体）；<br>3. 文件级：`:10` 允许改面「scheduler、`lib/infrastructure/cli/mosaic/**`」实测 1 个跟踪文件（域在）、`:25`「复用 SCI-B/C Oracle」指创新点编号（`ASTROCS_DESIGN §2.2/§2.3`）与 `实验/absolute-snr`、`实验/additive-sky-seamless`（目录名实测存在）⇒ **无文件级死链** |
| 与上位冲突 | **1. 与 1.6 同一型的判据冲突（本份上位文本更直白）**：`:18`「归约顺序冻结（窗口 ID、像素序），1/N worker **逐位一致**」+ 门 `:23`「N=1/2/4/8/16 worker **逐位一致**」↔ 上位 mosaic 行 `ASTROCS_DESIGN.md:604` 逐字：「窗口内 UPM→排异→集成顺序固定，窗口间无共享可变状态；**归约顺序冻结，1/N worker 数值等价（判据=事前冻结的浮点容差，见 §9）**」。<br>⇒ 上位在 **mosaic 这一行**就把判据钉为"数值等价+冻结容差"，本件两处写"逐位一致"——**同一阶段同一判据，两说相反**；本份比 ARCH-502 更严重，因为它违反的是点名 mosaic 的那一行。<br>**2. 件内容差**：`:25`「与单窗口参考实现数值一致（**冻结容差**…）」与同包 `SUMMARY.md:46`「N=1/2/4/8/16 逐位一致且与单窗口参考**逐位相同**（容差 0）」（DB-19 引）在"容差 vs 容差 0"上两说——本批不改 SUMMARY（别批），登记为**包内待裁一致性**，建议按 1 的结论统一为"上位冻结容差"。<br>3. 科学口径无冲突：`:16` 的顺序、`:14` 的窗口配置化与 §5/§8.3 同向 |
| 处置建议 | **保留原地 + 订正三处**：①`:18`/`:23` 的"逐位一致"改引 `ASTROCS_DESIGN.md §8.3` mosaic 行原句（"数值等价＋事前冻结浮点容差"），若确要 bitwise 则须写明属"构造保证位精确的模块自有门"（标准 01 §1）；②`:15` 删 RELEASE-04 读数或改为可复跑命令＋口径；③`:17` 给 `3.1e-15` 的证据载体（迁 `artifacts/evidence/`）或删除该"已证"表述（§2 留证规则）。收口随包移除。 |

<!-- PROGRESS: 已登记份数 7/13 -->

### 1.8 `工程控制/RELEASE-05/tasks/ARCH-504.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/ARCH-504.md` |
| 标题 | 任务：ARCH-504 export 子块流式调度器（A 线） |
| 行数 | 24（`wc -l`，与机械列一致；`Read` 显示 25 行为末行无终止换行的同一条） |
| 角色 | 操作与运营（控制包任务书）。见共用判定 1 |
| 主题 | export 阶段子块流式调度：子块划分与 HiPS 按范围读取、三级有界流水线+背压、原子发布与磁盘满退出码、RSS 与总图解耦、按子块探针 |
| 上游 | `:7`「最高设计 §6、§8.3（export 行）。」实测 `ASTROCS_DESIGN.md:434` §6「export（Phase3）：投影导出」、`:605` §8.3 表内 export 行 ⇒ **成立**，无 §号错配 |
| 下游 | ID 级 18 个文件（含 `CMakeLists.txt`、`eng/tests/unit/export_stream_test.cpp`、`eng/ci/ID_MIGRATION_MAP.md`）；路径级 0 |
| 是否正本 | 否（投影与重采样数学正本是 `docs/algorithms/PHASE3_PROJ_IMPL.md`/`DRIZZLE_GEOMETRY.md`，原子发布条款在 `ASTROCS_DESIGN §10`） |
| 重复或重叠对象 | `ASTROCS_DESIGN.md §10`（I/O 与原子产品）。**两处原文**：本件 `:16`「原子发布：临时目录→完整 FITS→原子 rename，磁盘满 exit 10（保持已修语义）」↔ `ASTROCS_DESIGN.md:509`「| 10 | 磁盘写满/写盘失败 |」＋§10 章（`:684` 起）的原子产品条款。<br>差异：上位给原则与退出码表、本件给实现顺序（临时目录→rename）⇒ 合法分层。**exit 10 两侧取值一致**（本批唯一无争议的数值一致项之一，登记为否定性结论以免整治误删） |
| 元信息块 | 无 |
| 写法违规 | **`:11` 把状态叙事写进禁止项**：「禁止改：投影数学（TAN 冻结，**SIN 缺陷未解决前不启用**）」——标准 01 §4「需要表达硬约束时用正向条款，硬禁令可直接陈述」；此处禁令的限定条件是**未来的状态变化**，不可判；同型 `:16`「（**保持已修语义**）」——"已修"是 §4 点名的历史叙事词 |
| 悬空引用 | **无文件级死链**：`:10` 允许改面 `lib/infrastructure/cli/export/**`（实测 1 个跟踪文件）与 projection/resample/fits_output 调用面（`lib/algorithms/projection/` 在跟踪集）；`:21` 大图合成测试的载体 `eng/tests/unit/export_stream_test.cpp`、`eng/tests/unit/p3_export_stream_prod_test.cpp`、`eng/tools/arch/check_p3_export_stream_prod.py` 全部实测存在 ⇒ **验收锚当前仍可执行** |
| 与上位冲突 | 1. `:18`「RSS 与子块大小成正比、与总图大小无关」与 `ASTROCS_DESIGN.md:605` export 行「有界队列 + 背压，**不整幅驻留**」同向 ⇒ 无冲突。<br>2. **被禁止项保护的条件在 HEAD 仍成立（如实登记，以免整治误删）**：SIN 确未启用——`docs/algorithms/PHASE3_PROJ_IMPL.md:120`「projection 仅接受 "TAN"（缺省即 TAN）；SIN/CAR/AIT 即便已在…」，且 `docs/KNOWN_LIMITATIONS.md:116-125` 第 41/42 条仍登记 SIN 缺「往返误差上界 + 尺度域」声明、其条件数缺陷由反向门 `sin_roundtrip_gate.py` 单独承担 ⇒ **条款内容与现状一致，问题只在措辞形态**（禁状态叙事）。<br>3. 本件与 SUMMARY 侧对该任务的"已接线"陈述无冲突（不同于 DB-19 对 ARCH-504 判的那条，其冲突落在 `ACCEPTANCE.md:19`/`SUMMARY.md:47`，属别批文件，本批不复述其结论） |
| 处置建议 | **保留原地 + 两处措辞改正向条款**：`:11` 改「投影数学中 SIN 投影不启用（适用域与缺陷登记见 `docs/KNOWN_LIMITATIONS.md` 第 41/42 条）」为指针式正向条款；`:16` 删「保持已修语义」，写「磁盘满退出码 = 10」（引 `ASTROCS_DESIGN.md:509`）。理由：标准 01 §4（写法：禁历史叙事、硬禁令直接陈述）。收口随包移除。 |

### 1.9 `工程控制/RELEASE-05/tasks/ARCH-505.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/ARCH-505.md` |
| 标题 | 任务：ARCH-505 生产节点改写与旧架构退役（A 线） |
| 行数 | 30（`wc -l`，与机械列一致） |
| 角色 | 操作与运营（控制包任务书）。见共用判定 1 |
| 主题 | 20 个生产节点改块读写、三阶段 Session 与未采纳 orchestrator 退役、架构文档同提交重写、等价基线冻结与验证 |
| 上游 | `:7`「最高设计 §8（全文）、**§10.3**；ENGINEERING_SPEC §2（退役实现二选一处置）。」——§8 ✓（`:542`「软件架构」）、`ENGINEERING_SPEC.md:14` §2「代码风格与历史实现处置」✓；**§10.3 不存在**：`grep -n '^### 10\.' ASTROCS_DESIGN.md` → **0 命中**，§10（`:684`「I/O 与原子产品」）正文是无编号项目符号列表 ⇒ **悬空条款引用**（本批唯一一条"§号指向不存在的节"）。机械列 `dangling_links=[]` 判不到条款级 |
| 下游 | ID 级 **31 个文件**（本批最多之一）：`CMakeLists.txt`、`docs/contracts/DUAL_LINE_CONTRACT.md`、`artifacts/evidence/known-limitations-ledger/LEDGER.md`、`eng/ci/ID_MIGRATION_MAP.md:98/:151/:153/:262`（`CHK-ARCH505-BLOCK-FLOW`、`CTEST-BLOCK-FLOW`、`CHK-BLOCKFLOW-*-STEP`）；路径级 0 |
| 是否正本 | 否（目标态正本是 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md` + `eng/contracts/block_flow/stage_block_flow.json`；后者实测 `nodes` = **20** ⇒ 本件「20 个生产节点」的数字**可复跑且与注册表一致**，是十份里唯一被机器合同钉住的规模数） |
| 重复或重叠对象 | `ENGINEERING_SPEC.md §2` 与 `ASTROCS_DESIGN.md §8.1`。**两处原文**：本件 `:18`「退役：lib/phase*_session、orchestrator 中未采纳部分按 ENGINEERING §2 删除或统一注释块（含原因+替代指针）；CHK-RETIRED-CODE 绿；MODULE_MAP fake=0」↔ 标准 04 §4 第 1 条「退役实现二选一：直接删除，或以统一注释块标注退役原因、替代实现与日期口径」（同义条款的规范正本在 `ENGINEERING_SPEC §2`）。<br>差异：任务书给的是**机器门名**（`CHK-RETIRED-CODE`/`MODULE_MAP fake=0`），规范给处置形式 ⇒ 合法分层；两个门名实测都有实体：`eng/ci/check_retired_code.py` 存在、`CHK-RETIRED-CODE` 在 `eng/ci/checks.json` 2 处命中、`eng/tools/quality/check_module_map.py` + `docs/modules/MODULE_MAP.yaml` 存在 |
| 元信息块 | 无 |
| 写法违规 | `:1`/`:8`/`:15`/`:20` 任务编号密集（ARCH-502/503/504、SCI-506 跨任务引用，形态所需）；`:14`「**改写前**跑全量现有测试 + 三命令合成全链，归档输出哈希与关键数值」——时序列步骤（任务书允许）；`:19`「无 Session 架构残留」为否定式目标（可，属验收判据）。**无日期、无 sha 值**（`:14` 的"归档输出哈希"不落具体值，合规） |
| 悬空引用 | 1. `:7` 的 **`ASTROCS_DESIGN.md §10.3` 不存在**（命令与证据见「上游」列）。<br>2. `:14`「归档输出哈希与关键数值（容差表写入**任务回执**）」——任务回执落 `run/RELEASE-05/`，`git ls-files -- run` 仅 `run/.gitkeep` ⇒ **验收载体不在跟踪集**（本份验收门第 2 条「合成全链三阶段等价（哈希/容差表）」在工作区内无从复核，只有 `artifacts/evidence/release-05/AUDIT_PACKAGE.md` 里的转述）。<br>3. `:10` 点名的 `docs/owner/PIPELINE_OVERVIEW.md`、`docs/owner/ARCHITECTURE_OVERVIEW.md`、`lib/phase*_session/**`、`lib/infrastructure/pipeline/orchestrator/**` **全部在跟踪集**（实测 16/5/5 与 53 个文件）⇒ 无死链 |
| 与上位冲突 | **1. 包内对同一任务的状态陈述互斥（本批"误导下一个开工对话"的头号实例）**：<br>`OPEN_QUESTIONS.md:15-18` Q-2「ARCH-501 已交付命名块与生命周期基础；**ARCH-502/503/504（三阶段调度器）与 ARCH-505（20 个生产节点改写、Session/orchestrator 退役）未开始**」<br>↔ `SUMMARY.md:9` 把 **ARCH-502、ARCH-503、ARCH-504 明列为 PASS（17）**、ARCH-505 为 BLOCKED（1）<br>↔ `ACCEPTANCE.md:20` 判 ARCH-505「**BLOCKED（部分落地）**」并列出交付件（块流规格 20 节点/26 块、`StageBlockFlow` 执行器、12/12 与 10/10 机器门、`core_block_frame` 33/33）。<br>⇒ 同一包内三份文档对同一批任务给出"未开始 / PASS / 部分落地"三档，其中 `OPEN_QUESTIONS.md` 的"未开始"与另两份**直接矛盾**；下一个据 Q-2 排期的对话会重做 ARCH-502/503/504 已完成的工作。**这三份都是 DB-19 的清单项**，本批只在自己十份的行上登记后果，冲突源头的处置归其所属批次。<br>2. `:23`「生产路径无 Session 直接装配」的验收判据在 HEAD **未达成且可复跑**：`git grep -ln 'phase1_session\|Phase1Session' -- lib/infrastructure` → `lib/infrastructure/cli/commands.cpp`、`lib/infrastructure/acr/ci/check_acr_dormant.py` 命中 ⇒ 生产入口仍装配 Session；与 `OPEN_QUESTIONS.md:19-20`「生产路径**当前仍是整阶段 Session 装配**，不是最高设计 §8.1 的三独立调度器 + 命名块内存管线」一致 ⇒ 任务书该门如实。<br>3. 与上位无条款冲突：`:4`/`:15-17` 的目标态与 `ASTROCS_DESIGN §8.1/§8.2` 同向 |
| 处置建议 | **保留原地（本批唯一"活任务"：BLOCKED 且文件域与门全部可执行）+ 两处必改**：①`:7` 删「§10.3」或改为「§10（I/O 与原子产品）」——标准 01 §3「追溯不到的机制先补要点再保留」，指不存在的节即悬空；②`:14` 的等价基线载体从"任务回执（run/）"改指 `artifacts/evidence/`（§2 留证规则），否则 PASS 判据不可复核。目录处置：随包移除，但**先解 `CHK-ARCH505-*` 门名**（共用判定 7）。 |

### 1.10 `工程控制/RELEASE-05/tasks/BLD-501.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/BLD-501.md` |
| 标题 | 任务：BLD-501 双平台全门 |
| 行数 | 16（`wc -l`，与机械列一致） |
| 角色 | 操作与运营（控制包任务书，**收尾门任务**）。见共用判定 1 |
| 主题 | 双平台（Linux/Windows）全量构建 + 全门 + ctest 全绿、单入口产物清单、零 waiver/零 SKIP、版本字符串扫描 |
| 上游 | **缺**——`grep -n '^## '` 只有 `:3 目标`、`:6 步骤`、`:12 验收门`三节，**无「权威依据」节**（机械列 `upstream_pointer=False` 与实测一致）。双重违规：`CONTROL_PACK_SPEC §5` 唯一模板六节缺 3 节（权威依据、改动范围（文件域）、禁止）；标准 01 §3「每份下层文档在开头位置标明其上游最高设计条款」⇒ 本批唯一上游缺位的一份 |
| 下游 | ID 级 11 个文件（`lib/infrastructure/scheduler/src/module_adapters.cpp`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md`、`reports/v19r2/*`、包内 SUMMARY/TASK_LIST/OPEN_QUESTIONS）；路径级 0 |
| 是否正本 | 否（双平台发行正本 = `ASTROCS_DESIGN §11`「双平台发行与安装」；机器门表 = `docs/ci/01_CHECKS.md`） |
| 重复或重叠对象 | `docs/ci/01_CHECKS.md` + `eng/ci/checks.json` 的门禁全量口径。**两处原文**：本件 `:13-16`「- [ ] Linux 全门 rc=0；Windows CI 绿；- [ ] 全量 ctest 零红；- [ ] 构建产物清单符合唯一入口；- [ ] 版本信息扫描：代码/产物中无版本字符串（FIN 前）」↔ 门实体确实存在：`eng/ci/run_checks.py`（TRACKED）、版本面为 `eng/ci/checks.json` 的 `CHK-VERSION-CONSISTENCY-VER001`＋`…-SELFTEST`。<br>差异：本件把"全门"复述成 4 个 checkbox 而**不点名任何门 ID**；与 `docs/ci/` 的门表构成"同一件事两处各写一遍且本件那遍不可机器核"的重叠（标准 01 §4 要求复述处给指针）。**并漏掉了本任务自己的核心面**：Windows 腿的正本口径（双平台各一套构建、只复用核心科学代码）未给指针 |
| 元信息块 | 无 |
| 写法违规 | `:1`/`:8`/`:16` 任务编号与 **`G3-4`、`FIN` 等内部编号**（实测 `:8`「CTRL_CLOSE 退出码 9 平台限制复核登记（G3-4）」——G3-4 是 `GAP_AUDIT.md:51` 的行号 ID，跨文件裸引）；`:16`「（**FIN 前**）」时态限定；无日期、无 sha |
| 悬空引用 | 无文件级死链：`:7` `eng/build/build.sh`、`:8` `eng/build/toolchain.ps1`、`eng/ci/run_checks.py` 均 TRACKED；`:8` 的 G3-4 在 `GAP_AUDIT.md:51` 可解析。<br>**但 `:9` 的产物名指向不存在对象**：见「与上位冲突」1 |
| 与上位冲突 | **1. 「唯一入口」锚与 HEAD 构建面不符（本份主条，且它是收尾门的判据）**：`:9`「单入口验证：产物仅 **`astrocs`**（Windows `astrocs.exe` / Linux `astrocs`），无 phase 子入口」↔ 实测根 `CMakeLists.txt:981` 的唯一产品可执行目标是 **`add_executable(acsd`**，同文件 `:4`/`:968` 两处注释自证「唯一 project()/唯一 add_executable(**acsd**)」；`git grep -n 'add_executable(astrocs'` 在 `CMakeLists.txt`/`eng/**`/`lib/**` **0 命中**（仅 `lib/algorithms/coverage/CMakeLists.txt:206 add_executable(astrocs-stage2 …)` 是按需工具目标，非交付入口），历史 `add_executable(astrocs)` 只出现在 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv` 对一个已不存在的 `cli/CMakeLists.txt` 的转述。<br>⇒ 本份验收门第 3 条按字面执行必然判红（拿不到名为 `astrocs` 的产物），且"改名方向"（ACSD ↔ astrocs）本身是待裁事项；`ASTROCS_DESIGN §7.1` 命令树（`:466`）只给 `normalize/mosaic/export` 三个子命令，**通体不声明可执行文件名** ⇒ 本件锚在上位也无依据。 |
| 处置建议 | **保留原地（NOT_RUN，任务仍活）+ 收口前补三节**：①补 `## 权威依据`（应指 `ASTROCS_DESIGN §11` 双平台发行与安装 + `docs/ci/01_CHECKS.md`）、`## 改动范围`、`## 禁止` 三节以合 `CONTROL_PACK_SPEC §5` 模板（缺权威依据亦违标准 01 §3）；②`:9` 的产物名与根 CMake 的 `acsd` 统一（按负责人改名裁决取一），并给 `docs/ci/01_CHECKS.md` 门表指针而非复述 checkbox。理由：标准 01 §1（下位与上位冲突以上位为准）＋§4（复述处给指针）。 |

<!-- PROGRESS: 已登记份数 10/13 -->

### 1.11 `工程控制/RELEASE-05/tasks/CLEAN-501.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/CLEAN-501.md` |
| 标题 | 任务：CLEAN-501 死代码与注册表缺口收尾（A 线） |
| 行数 | 27（`wc -l`，与机械列一致） |
| 角色 | 操作与运营（控制包任务书）。见共用判定 1。**本份同时是标准 04 §4 的执行件**（「历史控制包、一次性过程脚本、过期台账不留在生产目录」的处置任务），其判据引用链本身即按该条写 |
| 主题 | RELEASE-04 被阻塞的删除项收尾：B1 orchestrator、B2 p3_v6_export + v6_p3、B3 v1 p3_projection、B4 healpix_drizzle shim、B5 psfsw/weight_mode、CLEAN-402 12 组、66 路径/72 能力缺口三选一、每批删除跑全量测试 + CHK-RETIRED-CODE |
| 上游 | **缺**——`:6-7` 有 `## 权威依据` 节，但三条为「ENGINEERING_SPEC §2；GAP_AUDIT G2-6、G3-1/G3-2；RELEASE-04 ACCEPTANCE D-2」，**无一条指最高设计** ⇒ 违标准 01 §3「每份下层文档在开头位置标明其上游最高设计条款」。机械列 `upstream_pointer=False` **判对**（它找的正是最高设计指针）。三条依据本身可核：`ENGINEERING_SPEC.md:14` §2 ✓、`GAP_AUDIT.md:39/:48/:49` 的 G2-6/G3-1/G3-2 ✓；**唯 `RELEASE-04 ACCEPTANCE D-2` 无工作区落点**（下条） |
| 下游 | ID 级 15 个文件（`docs/modules/MODULE_MAP.yaml`、`eng/contracts/data/v6_clause_registry_v1.json`、`reports/v19r2/*`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md`）；路径级 0 |
| 是否正本 | 否（死代码/退役处置的规范正本是 `ENGINEERING_SPEC §2` 与标准 04 §4；本件是执行任务书） |
| 重复或重叠对象 | `GATE-502`（同包任务书，**别批：`tasks/GATE-502.md`**）的步骤 5 —— 同一批缺口两处派工。**两处原文**：本件 `:18`「66 路径缺口 + 72 能力缺口逐项三选一：实现（立任务）/登记 DEFERRED（写明非目标与理由）/声明作废（清注册表与文档）；出全表」↔ `eng/tests/quality/test_module_map.py:415-420`（GATE-502 侧的现行登记）「FIX-404 缺口台账（**50 路径 + 16 target** + 能力缺口）的测试侧棘轮 … GATE-502 步骤 5 逐项甄别结论（当时 66 条路径/target + 72 条能力缺口；能力侧随后按「缺口落地 ⇒ 删登记」各减 1，**现为 70**）：66 条路径/target 缺口与能力缺口**全部为「待实现」**（模块/schema/unit 未落地），owner = **BLD-401**（全门收口）或 RELEASE-04/未覆盖（GUI/HiPS Browser，本包显式不做）」。<br>差异与后果：甄别结论已由另一线在册给出（全判"待实现"、owner 指向 RELEASE-04 的 BLD-401），而本件仍要求"逐项三选一并出全表" ⇒ **重复派工 + 结论不回写**；且本件用的号型是"当时"的 66/72，现行为 50+16/70 |
| 元信息块 | 无 |
| 写法违规 | `:1`/`:10`/`:11`/`:14` 等任务编号密集（形态所需，含跨包 `CLEAN-402`、`RELEASE-04`、`FIX-404`）；`:4`「RELEASE-04 **被跨域注册表阻塞的**删除项…**全部处置完毕**」——以过去包的状态叙事作目标句（§4 应写现行要怎样）；`:10`「（仅删除已退役条目，判据改动归 GATE-501，**前台登记**）」过程性；`:14`「B1 orchestrator **50 文件 18411 行**」一次性读数（与 1.5 同源三方不合）；`:16`「**旧**形态」类词无（本份不含）。**机械列校准**：`history_narrative=[18]` 抓到 `:18` 的「声明**作废**」——但那里是**对未来处置动作的命名**（三选一之一），不是历史叙事 ⇒ **工具误报（不分语境）**；而 `:4` 真正的时间叙事（"RELEASE-04 被阻塞…全部处置完毕"）工具**未报** |
| 悬空引用 | 1. `:7` 的 `RELEASE-04 ACCEPTANCE D-2`：`git ls-files 工程控制 \| awk -F/ '{print $2}' \| sort \| uniq -c` → 只有 `31 RELEASE-05` ⇒ RELEASE-04 包不在跟踪集，被引"ACCEPTANCE D-2"无工作区落点（含义只由 git 历史承载）。<br>2. `:18` 的 66/72 全表**无跟踪载体**：`git grep -ln '66 条路径\|72 条能力\|66/72'` → 只有 `GAP_AUDIT.md`、`TASK_LIST.md`、本件、`eng/tests/quality/test_module_map.py` 四处；源登记面 `问题扫描/`（FIX-404 产出目录）`git ls-files -- 问题扫描` = **0** ⇒ "出全表"的输入表不在工作区，须由门/注册表侧重建。<br>3. `:15` 的 **B4「healpix_drizzle shim」无确定路径**：`git ls-files lib/algorithms/drizzle \| grep -i shim` → 0（`shim` 一词只出现在 `lib/algorithms/drizzle/README.md`、`healpix_drizzle/Makefile`、`healpix_core.{cpp,h}` 等 9 个 lib 文件的叙述里）⇒ 删除对象是**角色称呼不是文件集**，回执无法核对。<br>4. B2/B3 的删除对象**全部在跟踪集**（`eng/tests/integration/v6_p3/p3_v6_export_{e2e_test.cpp,oracle.py}`、`eng/tests/{unit/p3_projection_test.cpp,backend/test_p3_projection_oracle.py}`、`lib/algorithms/projection/p3_projection.{cpp,h}`）⇒ 这两项仍可执行 |
| 与上位冲突 | 1. **`:18` 的号型与在册现行值不符**（本份主条）：本件与 `GAP_AUDIT:39` 写「66 路径缺口 + 72 能力缺口」，而 `eng/tests/quality/test_module_map.py:415/:417-418` 记现行为「50 路径 + 16 target」「能力侧…各减 1，**现为 70**」⇒ 验收门第 2 条「66/72 缺口全表闭环」按字面对不上账（门不可判）。<br>2. **`:16` B5 的"随 DOC-502 一并清"未发生，而 DOC-502 已 PASS**：合同面仍留着退役叙述——`docs/contracts/DATA_SEMANTICS.md:3259`「| ~~退役模式~~ | ~~`psfsw_robust`~~ | **禁用 token**（PSFSW-RETIRE-01/03；§9.73 A44）… 变更编号 `CHG-2026-09-20-PSFSW-RETIRE` …」（含**日期型变更号**）、`:3265`「**历史词表登记保留**」；`docs/contracts/PUBLIC_API.md:2175`「~~退役值~~ | ~~`psfsw_robust`~~ —— **已按 §9.73 A44 作废**」、`:2186`「与…的**历史词表登记保留**」。<br>⇒ 一个已 PASS 的前置任务（DOC-502）留下本任务依赖的收尾未做；下一个读者按 `:16`＋SUMMARY 的"DOC-502 PASS"会判 B5 已完成（**留在册上的作废口径式误导**）。同时这两处合同正文自身带「作废/历史/已按…/ ~~删除线~~」，正是标准 01 §4 禁词密集区（属别批文件，本批只作举证）。<br>3. 与上位无科学口径冲突：`:27`「不删 testdata/、实验/、docs/research 任何证据」与 `AGENTS.md §6`（不删除用户数据/交付物）同向 |
| 处置建议 | **保留原地（NOT_RUN，任务仍活）+ 三处必改**：①`:6-7` 补最高设计条款（应指 §8 架构与 §12 验证体系），否则违标准 01 §3；②`:18`/验收门第 2 条的 66/72 改为可复跑来源（`eng/tests/quality/test_module_map.py` 的棘轮现值或注册表普查命令），并与 GATE-502 步骤 5 的既有甄别结论合并、**只留一处派工**（标准 01 §2「同一主题只有一份正本」的派生要求：同一处置只一处下令）；③`:15` B4 的"shim"改为点名文件集或删除。收口随包移除。 |

### 1.12 `工程控制/RELEASE-05/tasks/CONTRACT-501.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/CONTRACT-501.md` |
| 标题 | 任务：CONTRACT-501 双线合同与冻结点约定 |
| 行数 | 29（`wc -l`，与机械列一致） |
| 角色 | 操作与运营（控制包任务书）。其产出物角色是**合同说明**，落 `docs/contracts/**`＋`eng/contracts/**`——本件自身不是合同正本，见「是否正本」 |
| 主题 | A/B 双线启动前的六类冻结合同：文件域归属、命名块生命周期、三阶段调度器接口与探针 schema、性能门判据、监控字段语义二选一、docs↔eng 双向对应；冻结评审与差异单 |
| 上游 | `:7`「最高设计 §8；ENGINEERING_SPEC §4.1、§8；CONTROL_PACK_SPEC 双线纪律（00_README §3）。」四条实测全部成立：`ASTROCS_DESIGN.md:542` §8；`ENGINEERING_SPEC.md:55` §4.1「管线纪律（对应最高设计 §8）」、`:149` §8「文档集：自解释与层级索引」；`工程控制/RELEASE-05/00_README.md:39` 「## 3. 双线纪律（重点）」⇒ **无悬空、无 §号错配** |
| 下游 | ID 级 **42 个文件**（本批最多）：`CMakeLists.txt`、`docs/DOCUMENT_INDEX.yaml`、`docs/contracts/DUAL_LINE_CONTRACT.md`、`docs/contracts/PERF_GATE_CONTRACT.md`、`eng/ci/ID_MIGRATION_MAP.md`、`artifacts/evidence/release-05/GATE-501_VERIFICATION.md`；路径级 0 |
| 是否正本 | 否（六类合同的正本已产出：`docs/contracts/DUAL_LINE_CONTRACT.md`、`PIPELINE_BLOCK_CONTRACT.md`、`SCHEDULER_CONTRACT.md`、`PERF_GATE_CONTRACT.md`、`docs/ci/03_GATES.md`，schema 面在 `eng/contracts/**`——均在跟踪集，`git ls-files docs/contracts` 18 项含前四件） |
| 重复或重叠对象 | 本件 `:15-16` 步骤 2/3 与已产出的合同正本同题。**两处原文**：本件 `:15`「**命名块生命周期合同**：block 元数据 schema（名字/形状/类型/单位/可缺性/生产者/消费者/生命周期阶段）、创建-消费-销毁状态机、缺块降级语义、provenance 流转」↔ `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`（该合同的字段表与状态机全文，且被 `eng/ci/ID_MIGRATION_MAP.md`、ARCH-503 的下游引用列名）。<br>差异：本件是**产出指令**（合同尚未存在时写的字段清单），交付后同一字段清单在任务书与合同正本**两处并存** ⇒ 已 PASS 的任务书继续携带字段级文本，属"口径两处各写一套"的时间性重复；标准 01 §2「同一主题只有一份正本」在收口移除时自动消解 ⇒ 判"随包移除"，不判合并 |
| 元信息块 | 无 |
| 写法违规 | `:1`/`:11`/`:18`/`:24` 任务编号密集（GATE-501/DOC-502/SCI-501/502 等，形态）；`:19`「（**真强制或改名降级**）」正向二选一（合 §4）；`:20`「冻结后**变更走差异单**」——过程机制无载体（见「与上位冲突」2）；无日期、无 sha |
| 悬空引用 | 无死链：`:10` 允许改面 `eng/contracts/**`、`docs/contracts/**`（18 个跟踪文件）、`docs/architecture/**` 均在；`:26`「doc-index/合同门全绿」的门实体可指：`eng/tools/doccheck/check_doc_index.py`、`eng/ci/checks.json` 内 `CHK-CONTRACT-REF`/`CHK-CONTRACT-TEST`/`CHK-CI-CONTRACT-SELFTESTS`/`CHK-PRODUCT-CONTRACT`（本机 `grep -o '"CHK-…"'` 命中）⇒ **验收锚可执行** |
| 与上位冲突 | 1. 与上位无冲突：`:14` 的文件域划分（A 线 lib/、B 线 eng/ci·eng/tests·eng/contracts·docs/contracts·docs/ci、共享面 docs/science·plugins·architecture·owner）与 `AGENTS.md §7` 目录落位、`00_README §3` 双线纪律同向。<br>2. **两处步骤无落点**：`:20`「冻结评审：前台组织 A/B 两线负责人（子代理）对合同逐条确认后冻结，**冻结后变更走差异单**」——"差异单"在跟踪集无模板、无目录、无门（`git ls-files eng/contracts` 与 `docs/contracts/INDEX.yaml` 均无该机制登记），验收门第 1 条「六类合同成文且 schema/说明双向对应检查通过」亦**不点名门 ID** ⇒ 该门可被判"散文式通过"。第 5 类「监控字段语义二选一」**已落地且可核**：`docs/ci/03_GATES.md:71`「`requires_monitor: true` = **真强制**（声明即必须监控，证据缺失判红）」、`:94` §6.3「mutates_workspace（**真强制**）」+ 门 `docs/ci/01_CHECKS.md:119` `CHK-GATE-FAILCLOSED-SELFTEST`（8 例含两旗标的红绿自测）⇒ 六类中至少第 2/3/5 类有实物，"六类"整体判**部分可核**（第 4 类判据落 `PERF_GATE_CONTRACT.md`，本批未逐条读该合同正文——属别批文件，登记为待证：需按 `PERF_GATE_CONTRACT.md` 与 `CHK-GATE-FAILCLOSED-SELFTEST` 的 8 例对表） |
| 处置建议 | **保留原地（PASS）+ 一处补强**：`:20` 的"差异单"给出载体（落 `eng/contracts/` 或 `docs/contracts/` 并接门）或删除该机制承诺；验收门第 1 条把"双向对应检查"点名到门 ID（`CHK-CONTRACT-REF`），使门可机器复跑（标准 01 §3 双向索引可追溯；`CONTROL_PACK_SPEC §5`「验收门（可机器复跑）」）。收口随包移除——**其字段级复述（重叠列）随移除一并消失，无需合并**。 |

### 1.13 `工程控制/RELEASE-05/tasks/DOC-501.md`

| 字段 | 值 |
|---|---|
| 路径 | `工程控制/RELEASE-05/tasks/DOC-501.md` |
| 标题 | 任务：DOC-501 替换根文档与 PROJECT_SPEC |
| 行数 | 25（`wc -l` 报 24 与机械列一致；末行无终止换行，`Read` 显示 25） |
| 角色 | 操作与运营（控制包任务书）。见共用判定 1 |
| 主题 | 用包内 `docs/` 原样替换六份根文档、单独提交、跑 doc-index 门、push 核对三 SHA；三项验收门 |
| 上游 | `:7`「CONTROL_PACK_SPEC §2；包内 `docs/ASTROCS_DESIGN.md`（§8 架构重写）。」——`CONTROL_PACK_SPEC.md:19` §2「控制包是什么」✓；但**「包内 docs/ASTROCS_DESIGN.md」不在跟踪集**（`git ls-files 工程控制/RELEASE-05` 全部 31 项 = 8 份顶层 + 23 份 tasks，**无 docs/ 子树**）⇒ 本件的上游一半是包外文件、一半是不存在的包内件；且**未标最高设计条款号**（引的是"包内那份同名文件"），按前言 §4 判 `上游 = 部分（CONTROL_PACK_SPEC §2 可核）/ 最高设计缺` |
| 下游 | ID 级 8 个文件（包内 `00_README.md`/`ACCEPTANCE.md`/`TASK_LIST.md`、`reports/v19r2/*` 等）；路径级 0。⇒ 本批最少的一份，但非孤儿 |
| 是否正本 | 否（它命令替换的六份**才是**正本层根文档，现均在跟踪集：`ASTROCS_DESIGN.md`、`AGENTS.md`、`ENGINEERING_SPEC.md`、`ACCEPTANCE_SPEC.md`、`CONTROL_PACK_SPEC.md`、`docs/owner/PROJECT_SPEC.md`，逐个 `git ls-files --error-unmatch` = TRACKED） |
| 重复或重叠对象 | `CONTROL_PACK_SPEC §2`（控制包定义）与标准 01 §4（根文档写法）。**两处原文**：本件 `:21`「根文档无日期、无任务编号、无迁移流水账（**机器扫描，"负责人批准/终审"角色表述除外**）」↔ `docs/algorithms` 之外无同判据副本，其规范正本为 `ENGINEERING_SPEC.md:149` §8「文档集：自解释与层级索引」＋标准 01 §4；机器面实体存在（`eng/tools/doccheck/check_doc_hygiene.py`、`eng/tools/doccheck/check_doc_index.py`）。<br>差异：本件复述了写法判据并**自带一个例外括号**——判据本身可核（本机对六份跑 `grep -c -E '20[0-9]{2}-[0-9]{2}-[0-9]{2}'` → **六份皆 0**；对三份根文档跑任务编号正则 → **0 命中**），但该括号把"角色表述"排除在扫描之外，属**门自带的放行口**（前言 §5：恒真门/放松判据）。登记为判据设计缺陷而非值冲突 |
| 元信息块 | 无 |
| 写法违规 | `:1`/`:16`/`:24` 任务编号（DOC-502/ARCH-505 跨任务引用，形态）；`:14`「git pull 最新 main，登记预存修改」、`:17`「push 核对三 SHA」——git 写操作指令写进任务书（AGENTS §6「零 git 写」是对审查节点；对开发节点属正常，不判违规，登记事实）；`:21` 的括号例外句（同上）；无日期、无 sha 值 |
| 悬空引用 | 1. `:15`「逐字节覆盖上述六份文件（包内路径 `docs/docs/owner/PROJECT_SPEC.md` 对应仓库 `docs/owner/PROJECT_SPEC.md`）」——机械列把 `docs/docs/owner/PROJECT_SPEC.md` 报为 dangling_links（**工具误报**：它是"包内相对路径 + 仓库路径"的对照说明，不是仓库内链接；`git ls-files docs/docs` = 0 亦证它本就不该存在）。<br>2. 但**它指代的源确实不在跟踪集**：包内无 `docs/` 子树 ⇒ 「逐字节覆盖」的源、`:7` 的「包内 docs/ASTROCS_DESIGN.md」、验收门第 1 条「六组 cmp/diff 为空」三项**同源于一个从未入库的包内文档树** ⇒ 本份的核心验收锚不可复跑（详见「与上位冲突」1）。<br>3. 仓库侧六份目标全部存在（见「是否正本」）⇒ 无仓库内死链 |
| 与上位冲突 | **1. 三条验收门里有两条现在只剩"历史可满足"性**：门 1（`:20`「六组 cmp/diff 为空」）需要包内 `docs/` 作对照物，工作区无该树 ⇒ 现在跑必然无对象可比；本件已 PASS（`SUMMARY.md:9`），其 PASS 只能由提交时的过程证据支撑，而过程证据落 `run/`（不入库）⇒ 该门**当前既不可复跑亦不可复核**，属标准 01 §3「双向可追溯」断链。<br>**2. 门 2 与门 3 现在仍可执行且成立**：门 2 见「重复或重叠对象」列的六份实测（日期 0、任务编号 0）；门 3「ASTROCS_DESIGN §8 含三独立调度器、块生命周期、三阶段调度形态、探针条款」逐条命中 `ASTROCS_DESIGN.md:544` §8.1「总原则：唯一 CLI 入口、阶段独立调度器」、`:571` §8.2「阶段内：命名块内存管线与块生命周期」、`:597` §8.3「三个阶段调度器」、`:542-668` 段内「探针」2 处 ⇒ **本批唯一一条"替换后至今仍可判绿"的锚**。<br>3. `:16`「因结构调整产生的悬空指针登记转 DOC-502/ARCH-505，**不阻断本任务**」——把已知缺陷外派且无回收闭环：`docs/DOCUMENT_INDEX.yaml:23-27` 头注仍记「CLEAN-402 删除 docs/{archive,review,backlog}/** 后遗留的 32 条悬空条目…已在本版清除」，说明同族悬空确曾需要专门清除轮次；本件的外派条款本身没有对应门 ⇒ 登记为**放行条款无载体**（与 1.12 `:20` 同一形态） |
| 处置建议 | **保留原地（PASS）+ 两处收口前订正**：①`:15`/门 1 改为可在跟踪集复核的判据（例："六份根文档与 `git show <SHA>:<path>` 逐字节一致"并登记该 SHA 的**外部证据落 `artifacts/evidence/`**，标准 01 §2 留证规则；或直接删门 1 并把依据改指门 2/门 3）；②`:21` 删括号放行口或把例外项穷举为可判清单（`CONTROL_PACK_SPEC §5`「验收门（可机器复跑）」＋前言 §5.2 对恒真门的排除）。收口随包移除。 |

## 1.14 横向合并表（14 字段固定列名，`路径` 一律用仓库相对全路径；1.1–1.13 的机器可读视图）

详细举证见对应 `### 1.n`；本表只为机器合并提供稳定键。

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `docs/algorithms/CALIBRATION_ALGORITHMS.md` | Calibration Algorithms (ALG-CAL) | 636 | 算法推导（目录相符） | P1 校准算法层：母版生成、双分支校准、坏点检测修复、dark 系数估计、测光标量施加 | 有：`:3` §4.2 节点流程（实核成立） | 36（索引口径 31）个跟踪文件 | 是（算法层） | 与 `docs/science/CALIBRATION.md §5:91/:96/:98` 三式逐字节复述（同值，非重复正本） | 有（`:5-9`，机械列漏报） | 状态编号、DISP-CAL/P1-CAL 编号密集、已退役/已订正/补齐等历史叙事（机械列均漏报） | 1 族 3 处：`02_FROZEN_STAGE1_HISS_SPEC`（已删包），另 17 个被引路径全部在跟踪集 | 源码行号锚成批漂到无关代码（CMake 503-523、`astro_calibration.h` 21-24/33-155/168-179、`module_adapters` 1992/2220/2355、`p1_session` 372/462）；公共符号登记 14 实为 18（缺坏列定位族） | 保留＋重锚、删已删规范引用、补坏列条目、删元信息块 |
| `docs/algorithms/COSMETIC_ALGORITHMS.md` | Cosmetic Correction Algorithms (P1-COS) | 435 | 算法推导（目录相符） | 坏点检测/修复：全局阈值、连通域过滤、5×5 中值与四方向 IDW、帧级编排、统计基元、测试设计、DISP-COS-001..012 | 有：`:3` §4.2（实核成立） | 24（索引口径 19）个跟踪文件 | 是（cosmetic 域） | 与 `CALIBRATION_ALGORITHMS.md §3.4` 同一实现两份逐公式登记（实质重复） | 有（`:5-15`） | 「状态词唯一口径」自declare（直撞标准 01 §1 末条）、已退役/历史登记/补齐/原有条款全部保留 | 无文件级死链；符号级：SCI 在 ALG-CAL-004 名下绑已退役 cc_* 符号 | 核心"生产现状"结论已作废（两处调用点现已传母版、检测在生产生效）；跨文件锚漂移（ac_api 108/228、header 97/142、module_adapters 2328-2355）；缺坏列整族算法 | 保留＋立即订正生产现状与 ALG-CAL-004 符号绑定、重锚、补坏列条目 |
| `docs/algorithms/PHASE2_SAMPLER.md` | Phase2 Sampler Algorithms（P2-SAMP） | 650 | 算法推导（目录相符） | P2 控制点采样：几何布置、三遍 background-clean 管线、control 方差与 ivar、frame_id、并行确定性、DISP-P2SMP-001..007 | 有：`:5` §5.4（实核成立）；正文 `:452` 另引「§7.1（模块边界）」＝节号错配 | 26（索引口径 16）个跟踪文件 | 是（自declare 算法层唯一权威，两 ID 同文档） | 与 `docs/science/PHASE2_UPM.md`（SCI-UPM-001/WEIGHT-001）合法下钻，方向已显式声明 | 有（`:3-5` 与 `:7-20` 两块） | 旧行为/已被取代/曾修/禁用旧名/补齐等（机械列 history 与 task_ids 双漏报） | 16 处证据指针落 `run/`（`.gitignore:17`，仅 `run/.gitkeep` 跟踪）⇒ 定量证据不可复核 | 同文件对 `control_median_mc_test` 给出互斥结论且三方不合；自declare 1536/288 行实为 1503/273；`upm.cpp:565`、`:2766-2790`、并行测试 `:29` 锚漂；实测角尺度 0.9890 与代码注释 0.9586 两处不同值 | 保留＋消解自相矛盾、按实测更正规模、重锚、证据迁 `artifacts/evidence/` |
| `工程控制/RELEASE-05/tasks/ACCEPT-501.md` | 任务：ACCEPT-501 独立验收（五一致性自迭代） | 33 | 操作与运营（控制包任务书；不判角色错放，处置随包移除） | 8 大类五一致性验收清单、验收者角色约束、回派复验流程 | 有：`:7` 最高设计 §12（实核成立）；§7 条款归属与"子代理执行"不同口径 | ID 级 18 个文件（含 `eng/tests/unit/*`） | 否（判据正本在 ACCEPTANCE_SPEC 与 docs/science） | 与 `ACCEPTANCE_SPEC.md §2` 判据表重叠（清单化下钻，但压缩时丢限定语） | 无 | 任务编号（形态）、验收报告署名含子代理 ID | 无死链（`OPEN_QUESTIONS.md` 在跟踪集） | **`:14` 把已作废的「适用域 n≥100」当验收判据**：`docs/science/PHOTOMETRY.md:399` 现文为「不设固定星数门槛」，`OPEN_QUESTIONS.md:40-42` Q-5 已裁决删除并贯穿代码 | 保留＋删 `:14` 的 n≥100（改引 §16.5 第 1 条正向句） |
| `工程控制/RELEASE-05/tasks/ARCH-501.md` | 任务：ARCH-501 命名块机制与块生命周期（A 线） | 27 | 操作与运营（同上） | 块元数据、引用计数、生灭状态机、DAG 校验器三类非法图、内存即时归还单测 | 有：`:7` §8.1/§8.2（实核成立，四条依据全可核） | ID 级 16 个文件（含 `CMakeLists.txt`、`CHK-ARCH501-BLOCK-FRAME`） | 否（正本 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`） | 与该合同同名条款重叠但只以"合同字段"三字引用 ⇒ 合法分层 | 无 | 一次性盘点读数入任务书 | 无死链（pipeline 65、phase*_session 16/5/5 均在） | 「34 文件 18427 行」与包内 `GAP_AUDIT:11`「50 文件 18411 行」及 HEAD 实测（53 跟踪文件／36 代码文件／19052 行）三方不合且均无口径 | 保留＋删或改为可复跑命令（附口径） |
| `工程控制/RELEASE-05/tasks/ARCH-502.md` | 任务：ARCH-502 normalize 异步工作流调度器（A 线） | 28 | 操作与运营（同上） | 帧内 DAG 流水、多帧并发、空闲预取、统一资源预算、探针事件流、确定性与取消 | 有：`:7` §8.3（normalize 行）＋§9（实核成立） | ID 级 20 个文件（含 `CMakeLists.txt`、`eng/tests/runtime/integration`） | 否（正本 `docs/contracts/SCHEDULER_CONTRACT.md`） | 与 `ASTROCS_DESIGN:610/:673` 线程池条款重叠（禁令 vs 原则＋"长期"限定词） | 无 | 「退出码 9 **保持**」历史口径词 | 无死链（scheduler 26、cli/normalize 1；退出码 9 在 `DESIGN:508/:512` 与 `LOG_AND_ERROR_CONTRACT.md:105`） | 「1/N worker 逐位一致」严于上位 `:616`（判据为事前冻结浮点容差，位精确仅限构造保证路径的模块自有门）；禁令漏上位限定词 | 保留＋判据口径对齐上位、补限定词、"保持"改正向 |
| `工程控制/RELEASE-05/tasks/ARCH-503.md` | 任务：ARCH-503 mosaic 天球窗口并行调度器（A 线） | 28 | 操作与运营（同上） | 天球窗口并行、按窗口路由消除读放大、窗口内固定顺序、稠密 SNR 现场求值、探针 | 有：`:7` §8.3（mosaic 行）＋§5（实核成立） | ID 级 14 个文件（含 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`） | 否 | 与 `DESIGN:604` mosaic 行顺序条款重叠（本件多列 coverage、把集成具体为 SNR²） | 无 | 「RELEASE-04 实测约 3.9GB/帧」跨包读数入任务书；`3.1e-15` 无载体 | RELEASE-04 包不在跟踪集 ⇒ 该读数不可复跑；文件级无死链 | 「`:18`/`:23` 逐位一致」违反上位 **点名 mosaic** 的同一行「1/N worker 数值等价（判据＝事前冻结浮点容差）」；与 `SUMMARY.md:46`「容差 0」两说 | 保留＋判据改引 mosaic 行原句、删历史读数、给证据载体或删"已证" |
| `工程控制/RELEASE-05/tasks/ARCH-504.md` | 任务：ARCH-504 export 子块流式调度器（A 线） | 24 | 操作与运营（同上） | 子块流式三级流水线、背压、原子发布与磁盘满退出码、RSS 与总图解耦、按子块探针 | 有：`:7` §6＋§8.3（export 行）（实核成立） | ID 级 18 个文件（含 `eng/tests/unit/export_stream_test.cpp`） | 否（数学正本 `PHASE3_PROJ_IMPL.md`、原则在 §10） | 与 `DESIGN:509` 退出码表重叠（exit 10 两侧一致，登记为无冲突） | 无 | 禁止项内写状态叙事「SIN 缺陷未解决前不启用」；「保持已修语义」 | 无死链；`:21` 测试载体 `export_stream_test.cpp`、`p3_export_stream_prod_test.cpp`、`check_p3_export_stream_prod.py` 全在 ⇒ 锚可执行 | 无实质冲突，且 SIN 未启用的条件在 HEAD 仍成立（`PHASE3_PROJ_IMPL.md:120`、`KNOWN_LIMITATIONS.md:116-125`）⇒ 只判措辞 | 保留＋两处措辞改正向条款（指针式引 KNOWN_LIMITATIONS 第 41/42 条） |
| `工程控制/RELEASE-05/tasks/ARCH-505.md` | 任务：ARCH-505 生产节点改写与旧架构退役（A 线） | 30 | 操作与运营（同上） | 20 节点改块读写、Session 与 orchestrator 退役、架构文档同提交、等价基线 | **部分**：`:7` §8 与 ENGINEERING_SPEC §2 可核，**§10.3 不存在**（`^### 10\.` 0 命中） | ID 级 31 个文件（含 `docs/contracts/DUAL_LINE_CONTRACT.md`、`CHK-ARCH505-BLOCK-FLOW`） | 否（目标态正本 `PIPELINE_BLOCK_CONTRACT.md`＋`eng/contracts/block_flow/stage_block_flow.json`，实测 `nodes` = 20 与"20 节点"一致） | 与 `ENGINEERING_SPEC §2`／标准 04 §4 退役处置二选一重叠（本件给门名）⇒ 合法分层 | 无 | 跨任务编号密集、时序步骤、"无 Session 架构残留"否定式目标 | `§10.3` 悬空；`:14` 任务回执落 `run/` 不在跟踪集 | 包内三处状态互斥（`OPEN_QUESTIONS:15-18` 未开始 vs `SUMMARY:9` PASS vs `ACCEPTANCE:20` BLOCKED 部分落地）；门名 `CHK-RETIRED-CODE`/`MODULE_MAP` 实测有实体；生产仍装配 Session（`cli/commands.cpp`）⇒ 本批唯一活任务 | 保留＋删或改 §10 条款号、等价基线证据迁 `artifacts/evidence/` |
| `工程控制/RELEASE-05/tasks/BLD-501.md` | 任务：BLD-501 双平台全门 | 16 | 操作与运营（同上） | 双平台构建与全量机器门、单入口产物清单、零 waiver/SKIP、版本字符串扫描 | **缺**（`:3/:6/:12` 只有目标、步骤、验收门三节，无「权威依据」；机械列判对） | ID 级 11 个文件（含生产源 `module_adapters.cpp`） | 否（双平台正本 `ASTROCS_DESIGN §11`；门表 `docs/ci/01_CHECKS.md`） | 与 `docs/ci/01_CHECKS.md`＋`eng/ci/checks.json` 的门禁面重叠（本件复述 4 个 checkbox 而不点门 ID） | 无 | 内部编号 G3-4/FIN 裸引；缺 §5 模板三节 | 载体齐（`build.sh`、`toolchain.ps1`、`run_checks.py`、`CHK-VERSION-CONSISTENCY-VER001`） | 「产物仅 `astrocs`」与 HEAD 不符：根 `CMakeLists.txt:981` 唯一产品目标是 `acsd`（`:4/:968` 自证），且上位 §7.1 通体不声明可执行文件名 ⇒ 门按字面必红 | 保留＋补三节（权威依据指 §11 与门表）、产物名与 `acsd` 统一（按改名裁决） |
| `工程控制/RELEASE-05/tasks/CLEAN-501.md` | 任务：CLEAN-501 死代码与注册表缺口收尾（A 线） | 27 | 操作与运营（同时是标准 04 §4 的执行件） | B1–B5 删除项、CLEAN-402 12 组、66 路径/72 能力缺口三选一、每批跑 CHK-RETIRED-CODE | **缺**（有「权威依据」节但三条无最高设计条款；机械列判对） | ID 级 15 个文件（含 `docs/modules/MODULE_MAP.yaml`、`v6_clause_registry_v1.json`） | 否（处置规范正本 `ENGINEERING_SPEC §2`／标准 04 §4） | 与 `tasks/GATE-502.md` 步骤 5（别批）**同一批缺口两处派工**，结论已在 `eng/tests/quality/test_module_map.py:415-420` 在册 | 无 | 「RELEASE-04 被阻塞…全部处置完毕」时间叙事作目标句 | `RELEASE-04 ACCEPTANCE D-2` 无工作区落点；66/72 源表在 `问题扫描/`（跟踪 0）；B4「healpix_drizzle shim」无确定文件集（B2/B3 目标在跟踪集） | 号型作废（现行为 50 路径＋16 target、能力 70）⇒ 门 2 不可判；B5 依赖的 DOC-502 已 PASS 但合同面仍留 `DATA_SEMANTICS.md:3259/3265`、`PUBLIC_API.md:2175/2186` 的退役叙述与日期型变更号 ⇒ 会被误读为已清 | 保留＋补最高设计条款、号型改可复跑来源并与 GATE-502 合并为一次派工、B4 点名文件集 |
| `工程控制/RELEASE-05/tasks/CONTRACT-501.md` | 任务：CONTRACT-501 双线合同与冻结点约定 | 29 | 操作与运营（产出物角色为合同说明） | 六类冻结合同（文件域、块生命周期、调度器接口与探针、性能门判据、监控字段语义、双向对应）＋冻结评审 | 有：`:7` §8、ENGINEERING_SPEC §4.1/§8、00_README §3（四条实核全中） | ID 级 **42** 个文件（含 `docs/DOCUMENT_INDEX.yaml`、`CMakeLists.txt`）——本批最多 | 否（已产出：`DUAL_LINE_CONTRACT.md`、`PIPELINE_BLOCK_CONTRACT.md`、`SCHEDULER_CONTRACT.md`、`PERF_GATE_CONTRACT.md`） | 已 PASS 后仍携字段级清单（`:15-16` 与合同正本并存）⇒ 时间性重复，随移除消解 | 无 | 跨任务编号（形态）；"变更走差异单"无载体 | 无死链（`eng/contracts`、`docs/contracts` 18 项、门 `CHK-CONTRACT-REF`/`CHK-CONTRACT-TEST` 实测在注册表） | 无上位冲突；监控字段语义已落地可核（`docs/ci/03_GATES.md:71/:94` 真强制 ＋ `01_CHECKS.md:119` 8 例自测）；门 1 不点门 ID ⇒ 可被散文式判过 | 保留＋差异单给载体或删除，门 1 点名 `CHK-CONTRACT-REF` |
| `工程控制/RELEASE-05/tasks/DOC-501.md` | 任务：DOC-501 替换根文档与 PROJECT_SPEC | 25（`wc -l` 24，末行无换行） | 操作与运营（同上） | 以包内 docs/ 原样替换六份根文档、单独提交、跑 doc-index 门、push 核对三 SHA | **部分**：`:7` CONTROL_PACK_SPEC §2 可核，另一条指向不存在的包内 `docs/ASTROCS_DESIGN.md`；未标最高设计条款 | ID 级 8 个文件 | 否（其替换对象六份才是根文档正本，逐个实测 TRACKED） | 与 `ENGINEERING_SPEC:149` §8／标准 01 §4 写法判据重叠（本件复述并自带放行括号） | 无 | git 写指令入任务书；「机器扫描，负责人批准/终审角色表述除外」＝门自带豁免口 | 包内 docs/ 源不在跟踪集（31 项＝8 顶层＋23 tasks）⇒ 门 1 不可复跑；机械列把 `docs/docs/owner/PROJECT_SPEC.md` 报为悬空＝**误报**（对照说明非链接） | 门 2 现在仍成立（六份根文档日期命中 0、任务编号命中 0）；门 3 逐条命中 `DESIGN:544/:571/:597` 与 §8 段内探针 2 处；`:16` 把已知悬空外派且不阻断、无回收载体 | 保留＋门 1 改为跟踪集可复核判据（或删），删或穷举放行括号例外 |

## 2. 同主题组与差异（含跨批发现，别批路径已注明）

**口径统一声明**：本批所有"下游引用数"有两套口径，写死以免后用者误引——
①`git grep -l -e "<文件名>" -- ':!docs/algorithms'`（未排除过程/证据目录）：1.1=36、1.2=24、1.3=26；
②按 `docs/DOCUMENT_INDEX.yaml:30` 自定扫描口径（再排除 `artifacts/`、`reports/`、`问题扫描/`、`run/`、`build/`、`lib/third_party/`、`实验/`、`工程控制/`、`logs/`）：1.1=**31**、1.2=**19**、1.3=**16**。1.1–1.3「下游」列记的是口径①，**与索引比对须用口径②**。

| 主题组 | 各份（含别批路径） | 正本候选 | 主要差异与本批判定 |
|---|---|---|---|
| 坏点检测/修复算法（P1 cosmetic） | 本批 `docs/algorithms/COSMETIC_ALGORITHMS.md`（ALG-COS-001..005）＋ 本批 `docs/algorithms/CALIBRATION_ALGORITHMS.md §3.4`（ALG-CAL-004）＋ 别批 `docs/science/CALIBRATION.md §12/:420`、`docs/modules/calibration.md`、`docs/modules/registry/astrocs.phase1.cosmetic.md` | COSMETIC_ALGORITHMS.md | **实质重复**（同一源文件同一行区间两份逐公式登记，见 1.2 重叠列）；且**科学层 FROZEN 的 ALG-CAL-004 绑定的是已退役 cc_\* 符号**，与算法层绑定相反 ⇒ 消解须走 SCI 变更流程 |
| 校准连续定义（P1） | 本批 `docs/algorithms/CALIBRATION_ALGORITHMS.md §2` ＋ 别批 `docs/science/CALIBRATION.md §5:88-133` | PHOTOMETRY 之外的 CALIBRATION.md（科学层） | 三式两分支两处各写一套，**逐字节同值**（无冲突）⇒ 判"复述型重叠"，非重复正本（见 1.1 重叠列） |
| 控制点采样与 control_variance | 本批 `docs/algorithms/PHASE2_SAMPLER.md` ＋ 别批 `docs/science/PHASE2_UPM.md`（SCI-UPM-001/WEIGHT-001，`:5` 页头即声明覆盖 "phase2 (upm/sampler)"）＋ 别批 `docs/algorithms/UPM_SOLVER.md` | 本批份（算法层） | **三层分工最干净的一组**：本件 L598-600 显式钉死"两处冲突以 docs/science/ 为准并回改本文档"（方向写明）⇒ 合法下钻，不算重复 |
| MAD→σ 常数 `1.482602218505602` | 本批三份（CALIBRATION:87/228/260/264、COSMETIC:71/250、SAMPLER:51/198/358/428/571）＋ 别批 `docs/science/CALIBRATION.md:318/:452` | 科学层 | **全批无值冲突**；`1.4826022386550903` 只在 `docs/science/CALIBRATION.md:454` 作为**float32 舍入值**出现并附相对差 `+1.359062e-08` ⇒ 与 1.14（DA02 草稿）的否定性结论一致，登记为"勿删"负例 |
| `k_corr = 1.4` 的可复跑性 | 本批 `PHASE2_SAMPLER.md §5.4:312-313` 与 `§7:463`（同文件互斥）＋ 别批 `eng/tests/unit/CMakeLists.txt:1540`、`eng/ci/checks.json:4363/:4687`、`lib/algorithms/drizzle/healpix_drizzle/tests/CMakeLists.txt:110` | 无（未定案） | 三方各说一套 ⇒ **科学参数 provenance 未定案**，需按 `eng/tests/unit/CMakeLists.txt:1538-1541` 的"三合一"判据（文件存在 ∧ 构建注册 ∧ CI 采集）裁一，不由文档整治单方定 |
| RELEASE-05 任务书的收口状态 | 本批十份 ＋ 别批 `工程控制/RELEASE-05/{SUMMARY,ACCEPTANCE,OPEN_QUESTIONS,GAP_AUDIT,TASK_LIST}.md` | SUMMARY/ACCEPTANCE（状态面） | 同一批任务三处状态互斥：`OPEN_QUESTIONS.md:15-18`「ARCH-502/503/504 与 ARCH-505 **未开始**」↔ `SUMMARY.md:9` 列 ARCH-502/503/504 为 **PASS** ↔ `ACCEPTANCE.md:20` 判 ARCH-505「**BLOCKED（部分落地）**」并列出交付件 ⇒ 本批按"任务书不携带状态字段（`CONTROL_PACK_SPEC §5` 模板无该节）"判任务书形态合规，**状态互斥的缺陷归那三份包内文件**（别批） |
| 66 路径 / 72 能力缺口 | 本批 `CLEAN-501.md:18` ＋ 别批 `GAP_AUDIT.md:39`、`tasks/GATE-502.md` 步骤 5 ＋ 现行值在 `eng/tests/quality/test_module_map.py:415-420` | 测试侧棘轮（唯一在册现行值） | 号型已漂（50+16 路径/target、能力现 70）⇒ **重复派工＋结论不回写**，见 1.11 |

## 3. 索引与链接缺陷清单

**3.1 与 `docs/DOCUMENT_INDEX.yaml` 登记不符项**

| 类型 | 条目 | 举证（路径:行 + 实测） |
|---|---|---|
| **登记低报（downstream）** | `docs/algorithms/CALIBRATION_ALGORITHMS.md` 的 `downstream` | 索引 `:131`＝「CMakeLists.txt、eng/packaging/config/defaults.json」两处且**无"等 N 处"写法**；本批按索引自身扫描口径实测 **31 个引用文件** ⇒ 低报 29。同组另两份（`:136`「…等 18 处」实测 19、`:204`「…等 16 处」实测 16）**吻合** ⇒ 缺陷在"省略写法的缺省"，不是索引普遍失真 |
| **上游标注 §号与标题错配（别批条目，本批逐条核）** | `docs/DOCUMENT_INDEX.yaml:313`（`docs/api/PHASE2_API_V1.md`）、`:770`（`docs/modules/common.md`）、`:631`（`docs/contracts/RT-001.md`） | 三条分别写「§8.4（模块与 ABI）」「§8.4（模块与 ABI）」「§8.1（顶层结构）」；实测 `ASTROCS_DESIGN.md`：**§8.1 = 总原则：唯一 CLI 入口、阶段独立调度器（:544）**、**§8.4 = 顶层结构（:618）**、**§8.5 = 模块与 ABI（:661）** ⇒ 派单点名的那一族缺陷在索引里成簇存在（两个方向同时错：8.5 的标题挂到 8.4，8.4 的标题挂到 8.1）。**本批 13 份的自有上游指针**：1.1/1.2（§4.2）✓、1.3（§5.4）✓ 均成立；**1.3 正文内 `:452` 的「§7.1（模块边界）」错配**（§7.1 = 命令树（唯一），`:466`），1.9（ARCH-505）的 §10.3 = 指向不存在的节 |
| **索引正文携带日期（写法违 §4）** | `docs/DOCUMENT_INDEX.yaml:627` 的 `notes` 字段 | 逐字含「P1-WCS-DOC **(2026-09-07)** 追加 …；P2-COV-DOC **(2026-09-07)** 追加 …」；`lib/algorithms/cosmetic/module.yaml:1` 亦含「(P1-COS-DOC 冻结, **2026-09-07**)」⇒ 登记面自身违标准 01 §4「正文无日期」。索引不在本批清单，仅登记不结论 |
| 未登记 | 无 | 本批 3 份算法文档全部在索引（`:127/:132/:200`）；10 份任务书未登记**但合规**（`coverage:40` 自定范围＝docs/**＋根治理文档，`:30` 扫描口径明排 `工程控制/`）⇒ **此处订正 `AUD-101-DB-19.md` §3.2**：它把同一条升格为需负责人裁决的"规范冲突"，本机复判为索引自洽、无冲突（详见 §1.4–1.13 共用判定 3） |
| 登记了但文件不存在 | 本批范围内 **0** | 命令：对 `docs/algorithms/CALIBRATION_ALGORITHMS.md`、`COSMETIC_ALGORITHMS.md`、`PHASE2_SAMPLER.md` 逐个 `git ls-files --error-unmatch` → TRACKED |

**3.2 悬空引用汇总（本批 13 份内，逐条已给确认命令）**

| # | 位置 | 悬空对象 | 确认命令与结果 |
|---|---|---|---|
| 1 | `CALIBRATION_ALGORITHMS.md:255/281/512` | `02_FROZEN_STAGE1_HISS_SPEC §2.3/§7`（被两份算法当"规范依据"） | `git ls-files \| grep -i '02_FROZEN\|HISS_SPEC'` → 0；`git log --all --diff-filter=D --name-only` → 唯一历史路径在已删除的 `_agent_package/AstroCS_Stage1_HISS_Agent_Package_2026-07-31/` |
| 2 | `PHASE2_SAMPLER.md:76/77/222/278/332/573`（6 处 `run/`）＋ 10 处裸 `evidence/*.json` | `run/SCI-FIX-PHASE2-01/**`、`run/VARIANCE-SEMANTICS-01/**` 及全部定量证据 | `git ls-files -- run` → 仅 `run/.gitkeep`；`.gitignore:17 run/*` ⇒ 跟踪集无载体（磁盘存在性不作判据） |
| 3 | `RELEASE-05/tasks/ARCH-505.md:7` | `ASTROCS_DESIGN.md §10.3` | `grep -n '^### 10\.' ASTROCS_DESIGN.md` → 0 命中；§10（`:684`）正文为无编号项目符号 |
| 4 | `RELEASE-05/tasks/DOC-501.md:7/:15` | 包内 `docs/ASTROCS_DESIGN.md`、`docs/docs/owner/PROJECT_SPEC.md`（"六组 cmp"的源） | `git ls-files 工程控制/RELEASE-05` 31 项无 `docs/` 子树 ⇒ 门 1 不可复跑 |
| 5 | `RELEASE-05/tasks/CLEAN-501.md:7/:18` | `RELEASE-04 ACCEPTANCE D-2`；66/72 全表的源登记面 | `git ls-files 工程控制 \| awk -F/ '{print $2}' \| uniq -c` → 仅 RELEASE-05；`git ls-files -- 问题扫描` → 0 |
| 6 | `RELEASE-05/tasks/CLEAN-501.md:15` | 「healpix_drizzle **shim**」无确定文件集 | `git ls-files lib/algorithms/drizzle \| grep -i shim` → 0；`shim` 只在 9 个 lib 文件的叙述里 |
| 7 | `COSMETIC_ALGORITHMS.md:19-20`（经 SCI 转引） | `ALG-CAL-004` 名下绑定的 `cc_detect_hot/cold + cc_correct_median` 属**已退役实现** | 文件在跟踪集（`lib/algorithms/calibration/cpp/cosmetic_corrector.cpp`）但本份 `:37-38` 判其"无资格解释本模块" ⇒ 符号级冲突而非死链 |

## 4. 覆盖率自报

- **已登记 13 / 分配 13**。范围 = `DA-02-missing.txt`（3 份）+ `D1-residual.txt` 去掉 `docs/standards/TEST_STANDARD.md`（10 份）= 13 份。`docs/standards/TEST_STANDARD.md` **按派单跳过**（另一路在做），不计入本批分母，亦未在本稿出条目。
- **逐份通读**：13/13 全文读毕（1.1 636 行、1.2 435 行、1.3 650 行用 Read 全量读；10 份任务书 16–30 行逐份全量读）。**未逐行读名单：空**。
- **本批未复算项（登记为待证，不写成结论）**：①1.3 §6/§11.1 引用的 `stage2.cpp:219-231/:256-274/:1149-1152/:1368-1369` 与 `synthetic_gate.cpp:3594/:4001/:4061/:4089`、`ivar_wiring_test.cpp:223`、`module_adapters.cpp:657` 未逐个对锚（超出 12 次工具调用上限即停）；②1.12 六类合同的第 4 类（性能门判据）未逐条读 `docs/contracts/PERF_GATE_CONTRACT.md`（别批文件）；③1.11 的 66/72 **条目数**未普查（`eng/tools/doccheck/dangling_ledger.json` 的 `entries` 上限 50 且 scope 是 docs 指针台账，不是该缺口表）。
- 取证动作合规：未执行构建/ctest/`run_checks.py`/任何 `eng/**` 检查器；未 `import` 仓库内 Python（仅对 `eng/contracts/**`、`eng/tools/doccheck/**` 的 JSON **数据文件**做 `json.load` 读取，不加载仓内模块）；全程未写仓库任何文件。

## 5. 我证伪了任务书的哪个前提

| # | 前提（出处） | 复算结果 | 命令 |
|---|---|---|---|
| 1 | 派单：「13 份文档没有落到逐份登记条目」 | **只对 3/13 成立**。DA-02 那 3 份确无条目（`AUD-101-DA02-算法推导.md` 的 `### 1.1–1.28` 不含它们，尾行 `PROGRESS: 已审份数 28/31`）；D1 那 10 份在 `AUD-101-DB-19.md` §1.16–1.25 **已逐份出条目**，缺的是"以仓库相对全路径书写 `路径` 字段"⇒ 属**合并键不可用**，不是漏审 | `grep -n '^### 1\.'` 两份成稿的标题行；比对 `D1-residual.txt` 与 `DB-19-missing.txt` 行集（完全相同，10 行） |
| 2 | `AUD-101-DB-19.md` §3.2：控制包不在索引 ⇒ 规范冲突，须负责人裁决 | **不成立**：`docs/DOCUMENT_INDEX.yaml:40` 的 `coverage` 自定为「docs/**（含 docs/validation/v6/**）＋仓库根治理文档」，`:30` 的下游扫描口径明列排除「实验/、工程控制/、logs/ 等过程与证据目录」⇒ 索引自洽，任务书不登记不是缺陷；真风险面只在"收口后仍不移除"，由 `CONTROL_PACK_SPEC §9` 兜 | `sed -n '20,40p' docs/DOCUMENT_INDEX.yaml` |
| 3 | `OPEN_QUESTIONS.md:15-18`：ARCH-502/503/504（三阶段调度器）**未开始** | **证伪**：三份任务书在 `SUMMARY.md:9` 列 PASS，且其门与测试在跟踪集有实体（`eng/ci/ID_MIGRATION_MAP.md:88/:264` 的 `CHK-ARCH501-BLOCK-FRAME`/`CTEST-CORE-BLOCK-FRAME`；`ARCH-502` 的 ID 出现在 `CMakeLists.txt` 与 `eng/tests/runtime/integration/**`；`eng/tests/unit/export_stream_test.cpp`、`p3_export_stream_prod_test.cpp`、`eng/tools/arch/check_p3_export_stream_prod.py` 三份 export 载体存在）⇒ "未开始"与在册交付互斥，据它排期会重做已完成工作 | `git grep -l -w ARCH-50{2,3,4}`；`git ls-files \| grep -E 'export_stream.*test'` |
| 4 | `CLEAN-501.md:18` 与 `GAP_AUDIT.md:39`：66 路径 + 72 能力缺口待逐项三选一 | **号型已作废**：现行值为「50 路径 + 16 target」与能力「**现为 70**（各减 1）」，且甄别结论已由 GATE-502 侧在册给出（「全部为『待实现』，owner = BLD-401 或 RELEASE-04/未覆盖」）⇒ CLEAN-501 步骤 5 是**重复派工**且验收门第 2 条按字面不可判 | `grep -n -A2 '66 条路径' eng/tests/quality/test_module_map.py`（`:415-420`） |
| 5 | `BLD-501.md:9`：唯一入口产物名为 `astrocs` / `astrocs.exe` | **与 HEAD 构建面不符**：根 `CMakeLists.txt:981` 的唯一产品可执行目标是 `acsd`，同文件 `:4`/`:968` 注释自证「唯一 add_executable(acsd)」；`git grep -n 'add_executable(astrocs'` 在 `CMakeLists.txt`/`eng/**`/`lib/**` 0 命中（只有按需工具目标 `astrocs-stage2`）；上位 `ASTROCS_DESIGN §7.1`（`:466`）通体不声明可执行文件名 ⇒ 该门按字面必然判红且无上位依据 | 见左列三条命令 |
| 6 | AUD-101 任务书 §范围：「`docs/` 当前约 233 份、30+ 子目录」 | **本批不复核**（口径未定；负责人已把先前一次「233 vs 360」的证伪判为作废，本批不重复以未定口径的数字去证伪它） | — |

## 6. 基线核验（开工 / 收工 `git status --porcelain` 原文）与本节点自记违规

**开工**（§0 同一条命令）：

```text
?? "ACSD\346\225\264\346\226\227\345\267\245\344\272\224_AUDIT-06.zip"
?? site/
```

**收工**（同一命令，HEAD 仍 `c8f64e9a`）：

```text
?? "ACSD\346\225\264\346\226\227\345\267\245\344\272\224_AUDIT-06.zip"
?? site/
```

⇒ **两次输出逐字相同，无新增项**：本批取证未向被审工作树写入任何文件、未生成 `__pycache__`、未产出构建物。

**本节点自记的一次越界动作（已复原，主动登记）**：

- 事实：为读非 ASCII 路径的输出，本批在第 4 条取证命令里执行了 `git config core.quotePath false`。这**超出前言 §1 许可的只读命令集**（`status`/`ls-files`/`grep`/`log`/`show`），且 `git status` 抓不到此类改动（不落在工作树里，落在 `.git/config`）。
- 影响：`.git/config` 新增一行 `core.quotepath = false`；跟踪集与工作树未被触碰。
- 复原收据：`git config --get-regexp '^core\.'` 显示该键为唯一新增项，执行 `git config --unset core.quotePath` 后 `--get` 返回空，且 `git status --porcelain` 重新输出上面那段**八进制转义形式**——与本稿 §0 的开工原文一致 ⇒ 键已回到"未设置（行为默认 `true`）"的先前状态，基线复原可验。
- 纪律结论：本批**停用**一切 `git config` 写；需要非转义路径输出时改用一次性内联覆盖 `git -c core.quotepath=false …`（不落盘）。

<!-- PROGRESS: 已登记份数 13/13（DA-02-missing 3 份 + D1-residual 去 TEST_STANDARD 后 10 份；全部逐行读毕，未逐行读名单为空）-->
