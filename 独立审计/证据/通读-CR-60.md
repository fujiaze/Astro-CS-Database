# 通读 CR-60 —— export 阶段重采样与"失败即关死"

- 基线 HEAD：`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 派单前言：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 完整读，按硬约束执行）
- 独占清单 6 项全部逐行读完；只读取证，仓库内零写入、零 git 写、未跑构建/ctest/任何 `eng/**`、未 import 仓库 Python（临时件未落盘，全部结论在本文内）。

## 开工 status --porcelain 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 收工 status --porcelain 原文

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与基线一致（两项），本路未在仓库目录内落任何文件。

## 清单与进度

| # | 文件 | 已读 |
|---|---|---|
| 1 | `lib/algorithms/resample/p3_rsmp_failclosed.cpp` | 是（表 10 项，findings 2） |
| 2 | `lib/algorithms/resample/p3_rsmp_kernel_registry.cpp` | 是（表 10 项，findings 1） |
| 3 | `lib/algorithms/resample/p3_rsmp_operator.cpp` | 是（表 8 项，findings 2） |
| 4 | `lib/algorithms/resample/p3_rsmp_propagation.cpp` | 是（表 9 项，findings 3） |
| 5 | `lib/algorithms/resample/p3_rsmp_units.cpp` | 是（表 11 项，findings 3） |
| 6 | `lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp` | 是（表 12 项，findings 1） |

**清单差异（登记，以清单为准）**：派单第 6 项写 `lib/algorithms/resample/p3_nan_mask_test.cpp`，仓库跟踪集中该路径不存在；实际为 `lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp`（`git ls-files lib/algorithms/resample/`）。本批按清单意图读实际路径。派单第 4 项说"方差/协方差/相关性跨重采样"、第 3 项说"核注册表"，与文件实际职责一致，无其他差异。

## 本批共用的一条事实（各条 finding 的定级根因）

`export` 命令的 CLI 入口 `lib/infrastructure/cli/export/export.h`（18 行）自陈"不含任何科学实现……执行/校验全部委托会话层"，SESSION_EXPORT 在 `commands.cpp` 只映射到字符串 `"export"`；生产重采样入口是 `module_adapters.cpp:14354` 与 `p3_session.cpp:291` 的 `astrocs::phase3::p3_sample_bilinear_ex`（`p3_resample.cpp`）。本批 5 个 `p3_rsmp_*` 实现件的对外调用者只有 `lib/phase3_session/p3_v6_export.cpp`，而 `export_product(`（`p3_v6_export.h:226`）的调用点只有 `eng/tests/integration/v6_p3/p3_v6_export_e2e_test.cpp:393,418,448,548`。⇒ 本批所有"错值/误绿"类结论都写不出生产触发路径，按前言 §3 一律封顶 S2，并注明"缺运行时触发路径"。这一整条本身作为 CR-60-1 立条。

---

# 1 · `lib/algorithms/resample/p3_rsmp_failclosed.cpp`（248 行）

## 1.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :17-23 | `forbidden_weight_source_tokens` | 19 个精确 token | 禁止充当权重/方差来源的词表 | 结构性不适用（基数不含量值；**内容**是科学面 ⇒ CR-60-2） | `eng/contracts/data/v6_clause_registry_v1.json:748-756` FZ-GATE-MEDIAN-SNR；FZ-GATE-SUPPORT-COVERAGE 同族 | — | CONFIRMED |
| :44-48 | `forbidden_psfsw_product_keys` | 15 个 key | psfsw 产品禁止键 | 结构性不适用（基数；本件内零使用者 ⇒ 见 1.3） | `lib/algorithms/photometry/cpp/src/psfsw.cpp:646-653`（逐项同值的第二份）＋ `v6_clause_registry_v1.json#weight_vocabulary §3` | — | CONFIRMED |
| :139 | `std::fabs(rec.psf_sum - 1.0)` 的 `1.0` | 1.0 | PSF 归一目标 | 公式导出（Σp=1；代码真按式算：先求和再比较） | `docs/contracts/DATA_SEMANTICS.md:2682`；`p3_rsmp.h:9` | — | CONFIRMED |
| :139 | `cfg.psf_sum_tol` | 1e-9（定义面 `p3_rsmp.h:321`，本行使用） | PSF 归一容差 | **待确认** | 零（`git grep -n "psf_sum_tol" -- docs eng/contracts` 无命中；`p3_rsmp.h:319` 自称"实现局部数值守卫"） | 保守方向：收紧到 1e-12（f64 求和噪声量级）；影响 = G-P3-PSF-02 与 Σpi=1 两处的宽严 | PARTIAL（未实跑求和噪声量级） |
| :151 | `!(rec.photometric_scale > 0.0)` 的 `0.0` | 0.0 | 光度响应尺度 a 的定义域下界 | 结构性不适用：正性域约束，不是可调阈值（§28.6 只规定"缺 a → REJECT"，未给数值） | `DATA_SEMANTICS.md:2689-2690` | — | CONFIRMED |
| :185、:190 | `cfg.epsilon_corr_ratified` | 默认 `false` | 近似 covariance 面是否已签字 | 结构性不适用：开关默认位（OPEN 条款未签字即关），不承载量值 | `p3_rsmp.h:19-20`；`v6_clause_registry_v1.json` CF-T-P3-CORR-EPSILON status=OPEN / SO-07 | — | CONFIRMED |
| :187、:191 | `Status::Unavailable` + code `CF-T-P3-CORR-EPSILON` | — | 未签字面显式不可用 | 结构性不适用（状态位；调用方以 `!=Ok` 即返回，不写盘） | 同上 | — | CONFIRMED |
| :176-179 | `CovarianceRepresentation::WeightDerived` | 枚举 | 唯一"枚举式"拒绝 | 结构性不适用（枚举基数） | `p3_rsmp.h:306-312`；FZ-FORMULA-COV-PROP | — | CONFIRMED |
| :206-207 | `DiagonalOnly` ∧ 两个审计 bool | — | 对角化乐观审计 | 结构性不适用（枚举/布尔；生产默认 false ⇒ 见 4.3） | `DATA_SEMANTICS.md:2695-2696` | — | CONFIRMED |
| :73-228 | 本文件发射的门号种数 | 23 个不同 code | 门族基数 | 结构性不适用（基数）；与登记面不一致 ⇒ CR-60-11 | `V6_PHASE3_RSMP_IMPL.md:24` 列 12；`v6_clause_registry_v1.json:2073` 只登记 `G-P3-SB-01` | — | CONFIRMED |

## 1.2 findings

### CR-60-1 · 本批 5 个 `p3_rsmp_*` 实现件不在 `export` 生产路径上：fail-closed 门与核 registry 在生产面零调用
- 轴：D（主轴）＋ C
- 位点：`p3_rsmp_failclosed.cpp:73`（`std::vector<GateResult> check_failclosed_all(const ProductRecord& rec, const GateConfig& cfg)`）、`p3_rsmp_kernel_registry.cpp:110`（`KernelRegistry::admit`）、`p3_rsmp_operator.cpp:101`
- 上位依据：`p3_rsmp.h:5` 自称"本模块实现 ALG-P3-001（SPEC §1–§9）的算法层**生产代码**"；`DATA_SEMANTICS.md:2649`"模式未声明 → REJECT（FZ-P3-FAILCLOSED）；三模式之外无第四态"；`AGENTS.md §7` 把 `lib/infrastructure/cli/{normalize,mosaic,export}` 定为三命令入口。
- 现状（逐跳调用点普查，`git grep`）：`check_failclosed*` 的非自测调用只在 `p3_rsmp_propagation.cpp:36,78,147`；`propagate_surface_brightness/point_source_flux` 只在 `p3_v6_export.cpp:601,660`；`KernelRegistry::frozen()/admit()` 只在 `p3_v6_export.cpp:502`；`export_product(` 只在 `eng/tests/integration/v6_p3/p3_v6_export_e2e_test.cpp`；`module_adapters.cpp` 内 `p3rsmp::` 共 6 处，实际符号只有 `parse_mode`（:14122）与 `resolve_bunit`（:13930）。
- 差在哪：设计点名的能力（三模式门、核 registry、C_y=R C_x Rᵀ、Q/W 输出帧重算）在生产入口链上没被接上，接上的只有 `p3_rsmp_units.cpp` 的两个纯函数；生产真正走的同目录 `p3_resample.cpp` 路径**不带任何 FZ-P3 门**。
- 后果：对外主张"Phase3 导出受 12+ 门 fail-closed 约束"目前只对 e2e 夹具成立。这条同时是本批其余 8 条定级封顶 S2 的原因。
- 定级：S2（证据链完整，但"生产因此产出错值"的路径不存在——生产不经过它）
- 怎么算修好：二选一并同批对齐登记面——①把 export 会话接到 `export_product`（并补 §2.5 里 `psf_sum_tol` 等守卫值的容差登记），或②在 `p3_rsmp.h:5`、`V6_PHASE3_RSMP_IMPL.md` 与 `docs/contracts` 的实现锚上把本模块标为"未接入生产"。
- 置信：CONFIRMED

### CR-60-2 · 权重/方差来源门拒的是**精确字符串**，且唯一调用方只在"注入旋钮"上填这些字符串——真实产品内容进不了黑名单
- 轴：R（主轴）＋ S
- 位点：`p3_rsmp_failclosed.cpp:52-55` `return std::find(v.begin(), v.end(), token) != v.end();`；同族 `:27-31` `return token == "psfsw_robust_weight";`
- 上位依据：`v6_clause_registry_v1.json:756` 逐字："median(SNR_F)/median_source_snr **等诊断别名**进 weight.sources/weight_value → REJECT（C-004.2 不得接入权重面）"——"等"是开放集；`FZ-P3-FAILCLOSED.value`（:800）另要求 BUNIT 非二次律/对角无相关核一律拒。
- 现状：唯一的枚举式拒绝是 `CovarianceRepresentation::WeightDerived`（:176-179）；其余来源判定全压在 19 个全等 token 上。生产侧填充分只有 `p3_v6_export.cpp:596-598`：`si.variance_from = "propagated_covariance";`（硬编码，必过门）、`if (!in.inject_weight_source.empty()) si.weight_sources.push_back(in.inject_weight_source);`（仅阴性注入时非空）、`si.uses_relative_weight_as_ivar = false;`（字面 false）；PSF 路径连 `variance_from_weight` 都不赋值（`p3_v6_export.h` 无该行，默认 false）。
- 差在哪：这不是"拒枚举值"，而是"拿封闭词表去比一个只有测试旋钮会填的字段"；合同要的是对任意相对复合权重冒充 variance/ivar 的来源做拒绝。
- 后果：上游产品在 `weight.sources` 写 `median_source_snr_legacy`、`SNR`、`ivar`、`psfsw_weight` 都不触发 G-P3-GLB-01 ⇒ 被禁止的权重来源带着合规门号发布，直接冲击 SCI-B"跨帧绝对 SNR"的权重来源主张。**方向已定**：词表少命中一项 ⇒ `check_failclosed_all` 违规集合变小 ⇒ 更判绿 ⇒ fail-open。
- 定级：S2（缺运行时触发路径：见 CR-60-1）
- 怎么算修好：①`weight_sources`/`variance_from` 改**白名单**（合同登记过的合法来源枚举），白名单外一律 REJECT；②生产入口真正从上游 provenance/manifest 读这两项（替掉 `:596` 硬编码与 `:597` 注入面）；③同批改 `p3_rsmp_gate_test.cpp` 的期望（改白名单后未登记词都要判红），否则会把现有正例判红。
- 置信：CONFIRMED

## 1.3 不报的核过项（附检索式）

- **"被拒后是否真的不产出"**：`check_failclosed_all` 非空 ⇒ `p3_rsmp_propagation.cpp:79-84`、`p3_v6_export.cpp:516-520/597-602/660-672` 一律在写盘前 `return out`；`aggregate()`（propagation.cpp:12-19）Reject 优先于 Unavailable ⇒ 无"拒了继续写"路径。检索式：`git grep -n "check_failclosed\|export_product("`
- **退役对象的双重表达**（:95-105 与 :27-40）与登记面**一致**：`eng/contracts/data/unified_object_compatibility_map_v1.json:141` 逐字"canonical schema 已不存在，无法解析；端口合同 enum 判红，负例 …/negative/n5_retired_psfsw_robust_weight.schema-violation.json"，:142 说明 v6 词表 token 仅存于 `v6_clause_registry_v1.json#weight_vocabulary` ⇒ 注释"端口合同枚举不接受"成立，不报。检索式：`git grep -n "psfsw_robust_weight" -- eng/contracts/`
- `forbidden_psfsw_product_keys()`（:42-50）本文件零使用者，是 photometry 侧同族的第二份副本（逐项相同）；"同一词表两份、无对账门"已由既有母题登记，本批不重复计条。检索式：`git grep -n "forbidden_psfsw_product_keys"`
- `ProductRecord` 默认位（`p3_rsmp.h:424-476`：`mode_declared=false`、`psf_sum=1.0`、`provenance_complete=true`、`covariance=ExactFull`）取"合规产品"侧，默认记录命中 G-P3-MODE-01 而非静默通过 ⇒ 不是 fail-open。
- `:151-152` 的 `std::isnan()` 分支被前一子式 `!(x>0.0)` 完全覆盖（NaN 比较恒 false）⇒ 冗余非错。

---

# 2 · `lib/algorithms/resample/p3_rsmp_kernel_registry.cpp`（251 行）

## 2.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :151-170 | `nearest` 描述符各位 | 枚举/布尔 | 离散核注册项 | 结构性不适用（无量值） | `DATA_SEMANTICS.md:2671` | — | CONFIRMED |
| :176-199 | `bilinear_4quad` 描述符（含 `production_science_default=false`） | 枚举/布尔 | 连续场核注册项 | 结构性不适用 | `DATA_SEMANTICS.md:2672`；`V6_PHASE3_RSMP_IMPL.md:26` | — | CONFIRMED |
| :183 | `error_bound_expression` | `(h^2/8)*(max\|Fxx\| + max\|Fyy\|)` | 双线性插值误差上界式 | 公式导出 | `ALG-P3-001_KERNEL_REGISTRY §3.3`（`p3_rsmp.h:126` 引用）＋ `V6_PHASE3_RSMP_IMPL.md:26` 逐字同式；**代码不据此算**，只做 :99 的 `max_interp_err <= analytic_bound` 比较 | — | PARTIAL（`p3_rsmp_oracle.h` 内该式如何代入 F 未读，见末节） |
| :192 | `oracle.max_interp_err` | `0.027395522883651657` | 一次复算得到的最大插值误差 | 实验标定 | 标定件（跟踪面）`eng/tests/unit/v6_p3_rsmp/p3_rsmp_oracle_test.cpp:34`（`run_kernel_oracle()`），:46 以 1e-6 对账 | 该数是**某一测试函数上的测量**，不是核的普适属性；保守方向：登记面上写清 fixture 的 F 与 h，否则换 fixture 即失效而面不变 | PARTIAL（未复跑） |
| :193 | `oracle.analytic_bound` | `1.0 / 36.0`（注释 0.0277777…） | 上界式代入值 | 公式导出（算术已核：1/36 = 0.027777… 与注释一致） | 同 :183 | 依赖 oracle 内 F 的二阶导，未在面内声明 ⇒ 与 :192 同根因 | PARTIAL |
| :194 | `oracle.err_over_bound` | `0.9862388238114597` | 误差/上界比 | 公式导出但**写死**；手算 `0.027395522883651657×36 = 0.98623882381145966` ⇒ 与两邻位自洽 | `p3_rsmp_oracle_test.cpp:48`（1e-4 对账） | 写死 ⇒ 按 §2.5 第 2 档附则记一条 S 轴偏差（证据位上是字面量而非算式；对账仅 1e-4，见锚列） | CONFIRMED |
| :195, :223 | `max_weight_sum_dev` | `0.0` / `0.0` | Σw=1 偏差 | 实验标定（:195，对账 `oracle_test.cpp:49` 1e-15）；:223 属公式导出（精确重叠无插值近似） | `DATA_SEMANTICS.md:2676-2677` | — | CONFIRMED |
| :197, :225 | `zero_fill_error` | `1.3223` / `0.0` | "零填替代语义"的误差量级（负向对照） | 实验标定（:197）**且无对账**：`oracle_test.cpp:53` 只判 `> 0.5`，从不与登记值比较 ⇒ 漂移不报 | `docs/algorithms/PHASE3_RESAMPLE.md §2 G4/§4`（禁零填；`p3_nan_mask_test.cpp:8` 逐字引用） | 保守方向：把 1.3223 纳入 1e-6 对账；影响 = 该证据还能否充当"零填有害"的凭据 | CONFIRMED（不对账已核） |
| :222 | `err_over_bound`（exact 核） | `1.0; // 0/0 -> 约定为 ≤ 上界` | 0/0 的人为约定 | **待确认** | 条文面零提及。检索式：`git grep -n "err_over_bound" -- docs eng/contracts` ⇒ 零命中 | 保守方向：留 `kNaN` 并在 `validate_registration` 显式跳过，而不是给一个长得像测量值的 1.0；功能影响小（无门读它），但它以"Oracle 证据"名义入库 ⇒ 证据位上是约定不是测量 | CONFIRMED |
| :231 | `{"bicubic","lanczos"}` | 2 个 id，`status=RequiresRegistration` | 高阶核占位 | 结构性不适用（枚举基数） | `DATA_SEMANTICS.md:2673` | — | CONFIRMED |

## 2.2 findings

### CR-60-3 · `bilinear_area_overlap_exact` 注册为"生产科学默认"并通过 `admit`，但**无任何实现产生该核邻域**；kernel_id 只是盖在算子上的标签
- 轴：D（主轴）＋ C ＋ R
- 位点：`p3_rsmp_kernel_registry.cpp:205` `k.kernel_id = "bilinear_area_overlap_exact";`、`:226` `k.production_science_default = true;`；消费侧 `p3_rsmp_operator.cpp:104`（`op.kernel_id = kernel_id;` 无条件采信）
- 上位依据：`DATA_SEMANTICS.md:2667`"采样核是**产品语义**，由版本化 registry 注册，取值来源 = registry 登记项"；:2675"`status != registered` 用于生产、或 `registered` 而无 `oracle_ref`/`boundary_definition` → REJECT"；§28.6 族表（:2671-2673）**只有三族** `nearest`/`bilinear_4quad`/`higher_order`。
- 现状（双向核对）：
  - 注册面 5 个 id，其中"生产科学默认 = `bilinear_area_overlap_exact`"（`V6_PHASE3_RSMP_IMPL.md:26`），`bilinear_4quad` 被标 `production_science_default = false`；
  - 实现面：`git grep -n "make_.*_neighborhood" -- lib` ⇒ 只有 `make_bilinear_4quad_neighborhood`（operator.cpp:226）与 `make_nearest_neighborhood`（:265），该 id **零实现**，其 `oracle_id="ALG-P3-001-KERNEL-ORACLE/area_overlap_exact"` 也没有独立复算件（`oracle_test.cpp` 只跑 bilinear 一份）；
  - 调用面：`git grep -n "bilinear_area_overlap_exact"` 全部命中是 registry 自身与 `eng/tests/**`；生产唯一选择点 `p3_v6_export.cpp:236-239` 是 `nearest ? make_nearest_neighborhood(...) : make_bilinear_4quad_neighborhood(...)`（**二选一**），随后 :528/:582/:636 `build_row_normalized(nb, kernel)` 把**请求串**写进 `op.kernel_id`，:271 再写进 `p.sampling.kernel_id`。
- 差在哪：registry 与实现不是一一对应——被标"生产默认"的核无人实现而 `admit` 放行；kernel_id 与"实际算了什么"零耦合。反方向同样断：真正在跑的 `bilinear_4quad` 登记为"非生产默认"。
- 后果：把 `ExportInputs::kernel_id`（默认 `bilinear_4quad`，`p3_v6_export.h:154`）设为 `"bilinear_area_overlap_exact"` 的运行得到的是 4 象限双线性数学，却在 provenance 上声明"精确面积交叠、误差界 0"（:212 `error_bound_expression = "0（解析重叠，无插值近似）"`）⇒ 产品元数据与字节不符。**方向**：声明优于实际 ⇒ 判绿（白拿"误差为 0"的证据）。另：`production_science_default` 字段在生产门里零使用者（`git grep production_science_default` ⇒ 只有 registry 赋值＋`gate_test.cpp:281-282` 两条断言），所以没有任何一处能发现这种错配。
- 定级：S2（缺运行时触发路径，见 CR-60-1；一旦 CLI 接上即 S1）
- 怎么算修好：①补出该核的邻域生成器（逐像素球面矩形交叠）＋独立 Oracle，或②从 registry 撤下、把生产默认改回有实现的核，并与 §28.6 族表同批对齐；③`NeighborhoodSet` 应携带生成它的 kernel_id，`build_*_normalized`/`admit` 核对不一致即 REJECT；④给 `production_science_default` 接真实判据（非默认核用于生产 ⇒ REJECT）或删字段。
- 置信：CONFIRMED

## 2.3 不报的核过项（附检索式）

- `validate_registration` 对 `KernelNormalization::Discrete` 提前返回 Ok（:72-79），跳过 Oracle/误差界——与 `DATA_SEMANTICS.md:2676`（`nearest: var_out=u_in`，无权重算术）的适用域一致，不是漏检；连续分支上 :90-106 仍全量核 Oracle。
- `admit` 里 `kernel_id == "nearest" && use == ContinuousField`（:124）与 `allows(use)`（:128）重复，两者同向判红 ⇒ 冗余非缺陷。
- 各 `to_string` 尾支 `return "unknown"` 在 switch 全覆盖后不可达 ⇒ 编译告警抑制位。
- 检索式：`git grep -n "G-P3-KRN"`、`git grep -n "OracleKind::Structural"`。

---

# 3 · `lib/algorithms/resample/p3_rsmp_operator.cpp`（294 行）

## 3.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :44 | `std::fabs(coverage[i] - 1.0) > tol` 的 `1.0` | 1.0 | 完全覆盖目标 | 公式导出（coverage ≡ Σ_j a_ij/Ω'_i，全分割恒 1；代码真算：:133 `area / n.omega_out_sr`） | `p3_rsmp.h:218`；`DATA_SEMANTICS.md:2661` | — | CONFIRMED |
| :41-47 | `full_coverage(double tol)` 的 tol | 由调用方给（生产**零调用**） | 覆盖完备性容差 | **待确认** | `git grep -n "full_coverage" -- lib` ⇒ 仅声明（`p3_rsmp.h:223`）＋本定义；只有 `p3_rsmp_core_test.cpp:45,348` 用 1e-14/1e-12 | 保守方向：由 `GateConfig::row_sum_tol`（1e-9）供给并登记；影响 = 局部覆盖缺失能否被发现 | CONFIRMED |
| :64,70,75,86,117,122,127,151,157,162,165 | `0.0` 正性比较族 | 0.0 | 立体角/重叠面积必须为正 | 结构性不适用：域约束，非可调阈值 | `DATA_SEMANTICS.md:2661`；C-P3-PROP-6（`p3_rsmp.h:194-196` 禁零填） | — | CONFIRMED |
| :70 / :75 | `w = overlap_sr / oo`（行）／`/ oi`（列） | — | R/S 归一式 | 公式导出 | `p3_rsmp.h:227-229`（ALG-P3-002 SPEC §1.3）；代码按式算 | — | CONFIRMED |
| :240-241 | `u = cx - 0.5`、`v = cy - 0.5` | −0.5 | 像元中心↔索引半格位移 | 结构性不适用：0 基索引下像元中心在 i+0.5 的位移量，不进科学语义 | `p3_rsmp.h:258`"输出像素 (0,0) **中心**在输入索引坐标中的位置" | — | CONFIRMED |
| :246-249 | `w00/w10/w01/w11`（`(1−fx)(1−fy)` 等） | — | 四象限最近中心双线性权重 | 公式导出 | `p3_rsmp.h:263-267`（ALG-P3-001 §3.1）；`DATA_SEMANTICS.md:2676-2677`（Σc=1、Σc²≠1） | — | CONFIRMED |
| :257, :285 | `overlap_sr = sm.w * omega_out_sr` | — | 把权重写成面积使 R_ij=w | 公式导出 | `p3_rsmp.h:266-267` 逐字"重叠面积记为 w * Omega'_i（使 R_ij = w_ij）" | 隐含"该像素 Ω' 为常数"；生产侧靠 `p3_v6_export.cpp:239-245` 覆写补救 ⇒ 见 3.3 | CONFIRMED |
| :282 | `accumulate(..., 1.0, ...)` | 1.0 | 最近邻权重 | 结构性不适用（离散核定义） | `DATA_SEMANTICS.md:2676` | — | CONFIRMED |

## 3.2 findings

### CR-60-4 · `apply_operator` 对非有限输入样本不做掩膜/重归一/计数：一颗 NaN 输入污染最多 4 颗输出，而 COVERAGE 仍写 1
- 轴：R（主轴）＋ D
- 位点：`p3_rsmp_operator.cpp:176-187`（`y[...] += op.weight[e] * x[...];` 之后只有 `if (!op.valid_out[o]) y[o] = kNaN;`）＋ `:101-136`（`build_row_normalized` 全程不读像素值）
- 上位依据：`DATA_SEMANTICS.md:2375` 逐字"无效像素 IEEE NaN 填充……**NaN 合法语义=无覆盖**，禁 ±Inf 伪装"；`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md §2a`（`p3_nan_mask_test.cpp:5-8` 逐字转述："样本级掩膜 + 重归一 + 覆盖级 NaN + 强制计数"）；`docs/algorithms/PHASE3_RESAMPLE.md §2 G4/§4`（禁"零填"替代语义）。
- 现状：`InputGrid2D.value` 在本文件里只被 `width/height` 用到（:212 越界判定），无函数读 `value`；`apply_operator` 是裸 `Σ w·x` ⇒ 任一 `x_j` 非有限即 `y_i` 非有限，而 `valid_out[i]`/`coverage[i]` 不变。导出侧 `p3_v6_export.cpp:589-590`（`out.signal=sb.y`、`out.variance=sb.variance`）与 `:628`（`make_layer("COVERAGE", ..., rop.coverage)`）分别落盘两个面。同目录 `p3_resample.cpp:420-434` 有合规实现（`ok[k]=std::isfinite(vv[k]);` ＋重归一＋`n_rejected_nonfinite`），且由本批第 6 项测试锁住。
- 差在哪：同一条 §2a 规则在同一个静态库（`lib/algorithms/resample/CMakeLists.txt:22-31` 把 `p3_rsmp_*.cpp` 与 `p3_resample.cpp` 编进同一个 `astrocs_p3_rsmp`）里只在一处生效；被审的三模式栈连"剔除计数"这个数据结构都没有（`SurfaceBrightnessResult`/`SparseOperator` 无 rejection 字段）。
- 后果：产品字节出现 `SIGNAL=NaN` ∧ `COVERAGE=1` 的自相矛盾像素（按 :2375 两读法互斥）；bilinear 半径使一颗坏像素污染最多 4 颗。**方向**：覆盖被高估 ⇒ 偏绿（数据空洞被标成满覆盖），冲击"无接缝叠加/覆盖度"主张（SCI-C）。
- 定级：S2（缺运行时触发路径，见 CR-60-1；接通 CLI 即 S1）
- 怎么算修好：在 `apply_operator`（或其调用方）按 §2a 做"样本级剔除＋合格集重归一＋计数"，把 `n_rejected_nonfinite` 带到 `SurfaceBrightnessResult` 与 provenance；同时钉死 COVERAGE 与 NaN 的一致语义（要么 NaN⇒coverage=0，要么合同区分"几何覆盖/有效覆盖"两面并各给 BUNIT）。**必须同批**：给 p3rsmp 路径补一份与 `p3_nan_mask_test.cpp` 同判据的正/负例——现那份测的是 `p3_resample.cpp`，只改这里不会使它变红。
- 置信：CONFIRMED

### CR-60-5 · `make_bilinear_4quad_neighborhood` 把**零权重**象限角算进 `missing_contributors`：输出中心恰与输入像元中心重合时最外一行/列整排 NaN
- 轴：R
- 位点：`p3_rsmp_operator.cpp:210-223`（`accumulate` 越界即 `++(*missing); return;`，不看 `w`）＋ `:251-254`（四角无条件送入，含可能恰为 0 的 `w10/w01/w11`）＋ `:115`（`if (n.missing_contributors > 0) continue;` ⇒ `valid_out=false`）
- 上位依据：`p3_rsmp.h:193-196`、:226（"缺 tile/越界 → NaN，不零填"，C-P3-PROP-6）；`DATA_SEMANTICS.md:2645` 的 SB 门只针对"做 flux 换算而无逐像素 Ω"。**条文面零提及**"零权重贡献者越界也算缺"。检索式：`git grep -n "missing_contributors\|缺 tile" -- docs`
- 现状与算术：`fx=0 ⇒ w10=w11=0`，但 :252/:254 仍对 `j0+1` 调 `accumulate`。取恒等网格 `GridPlan{out_origin_x=0.5, out_step=1.0}`：`ox=width−1 ⇒ cx=width−0.5 ⇒ u=width−1 ⇒ j0=width−1, fx=0 ⇒ j0+1=width` 越界 ⇒ 该列全部输出 `missing>0` ⇒ NaN；而数学上该像素权重为 `(w00,w10)=(1,0)`，本有满覆盖。
- 差在哪：把"需要的贡献缺失"与"权重恰为 0 的位置在网格外"混为一谈，判据严在合同没要求的地方。
- 后果：对齐网格导出时输出平面最外一行/列整排 NaN（`coverage` 同时保持 0，:133 不执行）；`propagate_point_source_flux` 对任何 invalid 输出整件 REJECT（propagation.cpp:169-176，门号 `G-P3-QW-FRAME`）⇒ point_source_flux 在 1:1 网格下恒不可用。**方向**：偏红（过拒），不是误绿；但它是产品可得性缺陷。
- 定级：S2（`p3_v6_export.h:187-189` 默认 `ResamplePlan{out_origin_x=0.0, out_step=1.0}` 时 fx=0.5，缺失的是真贡献 ⇒ 默认配置下行为正确，本缺陷只在半整数 origin/对齐网格时触发，故不给 S1）
- 怎么算修好：`accumulate` 仅在 `w > 0`（或登记容差）时计入 `missing`；同时给 `make_bilinear_4quad_neighborhood` 补"对齐网格＋边缘像素"正例（现 `p3_rsmp_core_test.cpp:342-356` 的 plan 不落在 fx=0 ⇒ 测不到）。
- 置信：CONFIRMED（算术手推；未实跑，见末节）

## 3.3 不报的核过项（附检索式）

- `from_entries`（:50-89）对 `g.omega_out_sr`/`g.omega_in_sr` 只判 `empty()` 后即按下标取值（:67-68、:72-73、:84）：当调用方给的 `n_out`/`n_in` 大于 Geometry 向量长度时是越界读。两个现调用点（`p3_rsmp_scenarios.h:62,66`、`p3_rsmp_core_test.cpp:61,63`）长度自洽且生产不经此函数 ⇒ 登记为需实跑确认的健壮性项（末节），不报缺陷。检索式：`git grep -n "operator_from_entries_row\|operator_from_entries_col"`
- 列归一算子也写同一 coverage（:171）：`Σ_j a_ij = Ω'_i` 与归一方式无关 ⇒ 不是错。
- `make_*_neighborhood` 用标量 Ω 填满 Geometry（:230-232、:269-271）看似"常数 Ω"（违反 FZ-P3-OMEGA-NONCONST），但生产调用方 `p3_v6_export.cpp:239-245` 立刻覆写为真实逐像素向量并按 `oo` 缩放 `overlap_sr` ⇒ 本件不立条；该覆写自身的正确性属会话层批次范围。
- `SparseOperator::at()`（:18-23）线性查找 ⇒ 性能面。

---

# 4 · `lib/algorithms/resample/p3_rsmp_propagation.cpp`（256 行）

## 4.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :24 | `rec.mode_declared = true;` | true | 使 G-P3-MODE-01 在本路径不可达 | 结构性不适用：模式来自 C++ 枚举（`base_record(mode)`），"声明"在类型层已成立 | `DATA_SEMANTICS.md:2649` | — | CONFIRMED |
| :57-59 | `rec.bunit_quadratic_law_ok` | 由 `is_quadratic_variance`/`is_inverse_pair` 现算 | 二次律审计 | 公式导出（真按式算，非写死） | `DATA_SEMANTICS.md:3135` FZ-P3-BUNIT-QUADRATIC | — | CONFIRMED |
| :62-64, :134-136 | `rec.correlation_kernel_present` | `covariance ∈ {ExactFull, ExactCorrelationKernel}` 推导 | "有相关核"的判定 | **待确认（实为自报枚举 ⇒ CR-60-6 主位点）** | `DATA_SEMANTICS.md:2689-2690`（"仅对角 covariance 无相关核 → REJECT"） | 保守方向：改由 `c_in` 的非对角实际内容判定（`max_abs_offdiag_correlation`）；影响 = SB-04/PSF-04/COV-01/QW-04 四门 | CONFIRMED |
| :121 | `in.psf_p.empty() ? kNaN : psf_sum` | kNaN 哨兵 | 空 PSF 使 :139 必判红 | 结构性不适用（哨兵位，方向偏红） | `p3_rsmp.h:139`（G-P3-PSF-02） | — | CONFIRMED |
| :190-191 | `d_j = x_j * omega_in_sr[j]` | — | SB→积分通量 | 公式导出 | `DATA_SEMANTICS.md:3169`"逆变换 `flux_sum = signal×area`"；ALG-P3-005 | — | CONFIRMED |
| :194-198 | `c_d(r,c) = Ω_r · c_x(r,c) · Ω_c` | — | `C_d = D C_x D`（D=diag Ω） | 公式导出 | `DATA_SEMANTICS.md:2694`（FZ-FORMULA-COV-PROP 族） | — | CONFIRMED |
| :209 | `std::fabs(pi_sum - 1.0) <= cfg.psf_sum_tol` | 1.0 / 1e-9 | 输出 effective PSF 归一 | 公式导出（`pi = S p`，Σ=1）；tol 同 1.1 表第 4 行 ⇒ **待确认** | `DATA_SEMANTICS.md:2682`；`p3_rsmp.h:9` | 同上 | CONFIRMED |
| :246-247 | `out.W = a²·πᵀC⁻¹π`、`out.Q = a·πᵀC⁻¹f` | — | 输出帧 Q/W 重算 | 公式导出（逐字同式，代码真按 Cholesky 解算） | `DATA_SEMANTICS.md:2683` 逐字 `Q = a * pi^T C_y^-1 f; W = a^2 * pi^T C_y^-1 pi` | — | CONFIRMED |
| :248-249 | `(out.W != 0.0) ? Q/W : kNaN`、`1.0/out.W` | 0.0 / 1.0 | F̂=Q/W、Var(F̂)=1/W 与零守卫 | 公式导出（两式逐字见 :2683）；`0.0` 是零守卫 ⇒ 结构性不适用（W 由 Cholesky 保证 SPD，非正定即 :226-231 REJECT） | 同上 | — | CONFIRMED |

## 4.2 findings

### CR-60-6 · `correlation_kernel_present` 由调用方自报的枚举推导，从不检查 `c_in` 的非对角内容 ⇒ 声明 ExactFull 而传对角 C_x 时协方差被静默丢弃
- 轴：R（主轴）＋ S
- 位点：`p3_rsmp_propagation.cpp:62-64` 与 `:134-136`：`rec.correlation_kernel_present = (in.covariance == CovarianceRepresentation::ExactFull || in.covariance == CovarianceRepresentation::ExactCorrelationKernel);`
- 上位依据：`DATA_SEMANTICS.md:2689-2690`"仅对角 covariance 无相关核 → REJECT"；:2695-2696 逐字"现行 `Σ c_k² u_k` 只在输入 `C_x` 对角时严格；Drizzle 输入 `mean|ρ|≈0.19` 下对角省略低估 23.3%，对角化后声明的 `1/W_diag` 比真实方差低 28.7%（须被检出，Oracle P3-O5）"。
- 现状：`covariance` 是调用方给的枚举，`correlation_kernel_present` 是它的纯函数；`propagate_covariance` 拿到什么 `c_in` 就乘什么，无任何一处检查 `c_in` 是否真的有非对角项；审计工具 `max_abs_offdiag_correlation`/`correlation_kernel`（`p3_rsmp.h:290-291`）在生产零调用（`git grep -n "max_abs_offdiag_correlation" -- lib` ⇒ 只有 `p3_rsmp_covariance.cpp:78` 定义与 `p3_rsmp_oracle_test.cpp:78`）。`optimism_audit_performed`/`diagonal_optimism_detected` 同样是调用方布尔（生产由 `p3_v6_export.h` 默认 false 传入）。
- 差在哪：门核的是"你自称是哪一族"，不是"矩阵里到底有没有协方差"；合同要求的"须被检出"落在一个无人调用的函数上。
- 后果：**方向已定**——声明 ExactFull ∧ 实际对角 ⇒ 违规集合为空 ⇒ 全部门判绿（偏绿），而 `var_out` 与 `1/W` 因省略正相关项而**系统性低估**、SNR 与 `Q/W` 高估；这正是 §28.6 点名的 23.3%/28.7% 失效模式，会直接污染创新点 SCI-B（跨帧绝对 SNR）与 VARIANCE/IVAR 产品字节。
- 定级：S2（缺生产触发路径，见 CR-60-1；`covariance` 默认值即 `ExactFull`（`p3_rsmp.h:333,377`、`p3_v6_export.h` 的 `covariance_diagonal=false`），所以只要上游给的是逐像素 variance 面就一定落进这个洞）
- 怎么算修好：①`correlation_kernel_present` 改由内容判定（`max_abs_offdiag_correlation(c_in) > 登记的 rho 门限` 或 `c_in` 非对角范数/对角范数比），门限须先在容差面登记；②`covariance == ExactFull` 而 `c_in` 实测对角 ⇒ REJECT（把"自称完整"变成可核事实）；③补一条 Oracle 正/负例（对角 C_x + 声明 ExactFull 必红；真含 ρ≈0.19 的 C_x 必绿且 var 增量 ≈ +23.3%），与 §28.6 的 P3-O5 对齐。
- 置信：CONFIRMED（推导式与调用点均已复算；23.3%/28.7% 两个数按 :2695-2696 引用，未独立复跑）

### CR-60-7 · 三模式传播把 `C_y` 与 `C_d` 做成 O(n²) 稠密矩阵、`Q/W` 走 O(n³) Cholesky，在合同规定的产品尺度上不可算，且无尺寸门
- 轴：D（主轴）＋ R
- 位点：`p3_rsmp_propagation.cpp:101`（`out.c_y = propagate_covariance(r_op, in.c_in);`）、`:193`（`c_d = DenseMatrix(ni, ni);`）、`:225`（`cholesky_solve(out.c_y, out.pi, ...)`）
- 上位依据：`DATA_SEMANTICS.md:451` 与 :1233 逐字规定产品 tile 为 `float32 或 float64 [512×512] NESTED local`；`docs/contracts/DATA_SEMANTICS.md:2694`（`C_y = R C_x Rᵀ`）与 :2683（Q/W 需 `C_y⁻¹`）。
- 现状：`propagate_covariance` 分配 `DenseMatrix m(n_out, n_in)` 与 `DenseMatrix cy(n_out, n_out)`（`p3_rsmp_covariance.cpp:28,38`），`git grep -n "bad_alloc\|max_size\|too large\|size_limit" -- lib/algorithms/resample/` ⇒ **零命中**，无尺寸门、无分块/稀疏/低秩路径。
- 差在哪：合同尺度下 n_out = 512×512 = 262 144 ⇒ `cy` 需 262144² × 8 B ≈ 5.1×10¹¹ B，`m` 同量级；Cholesky 另需 O(n³)（≈1.8×10¹⁶ 次）。即"完整 covariance 传播"这一被设计点名的能力只有小尺度实现，没有把它带到产品尺度的任何机制。
- 后果：`export` 的 VARIANCE/IVAR/Q/W 面在真实 tile 尺度上不可能由本路径产出（分配即失败/被内存看门狗杀，退出码 137）；现在能跑绿的只有 e2e 小夹具 ⇒ "FZ-FORMULA-COV-PROP 已实现"这一对外主张在产品尺度上不成立。方向：偏红（跑不出来），不是误绿——但"红"目前被夹具掩盖。
- 定级：S2（缺生产入口，CR-60-1；算术已复核量级）
- 怎么算修好：要么给出稀疏/分块（按输出邻域带宽）路径并登记规模上界与尺寸门（超限 ⇒ `Status::Unavailable` 带原因，禁静默降级为对角），要么把 §28.6 的 Q/W 条款实现改成带宽限制下的等价形式并在合同面说明适用域；两者都要补一条"产品尺度可得性"证据件。
- 置信：CONFIRMED（分配点逐字；尺度由合同 tile 尺寸算术）

### CR-60-8 · 门号语义 1:多：`G-P3-PSF-02` 与 `G-P3-QW-04` 各被两种不同判据复用
- 轴：C
- 位点：`p3_rsmp_propagation.cpp:211`（`out.code = "G-P3-PSF-02"; reason = "effective_psf_not_normalised_sum_ne_1"`）与 `:228`/`:236`（`out.code = "G-P3-QW-04"; reason = "full_c_y_solve_failed:..."`）
- 上位依据：`V6_PHASE3_RSMP_IMPL.md:24,27` 按门号登记语义（`G-P3-PSF-02` 属"PSF 未归一"，`FZ-P3-BUNIT-QUADRATIC` 另立）；`p3_rsmp_failclosed.cpp:140`（PSF-02 = **输入** PSF 归一）与 `:208`（QW-04 = **对角化乐观**审计）。
- 现状：同一 code 字符串在两个不同物理判据上发射——输入 PSF（failclosed.cpp:139）与输出 effective PSF（propagation.cpp:209）共用 `G-P3-PSF-02`；对角乐观审计（failclosed.cpp:206-210）与 Cholesky 非正定（propagation.cpp:225-239）共用 `G-P3-QW-04`。
- 差在哪：门号是登记面↔实现面的连接键，一个号两义 ⇒ 按门号做的证据统计与 mutation 断言无法区分是哪一条判据红。
- 后果：`eng/tests/integration/v6_p3/EVIDENCE.md` 与任何按门号计数红灯的判据会把"输出 effective PSF 未归一"读成"输入 PSF 未归一"，反之亦然；不影响产品字节 ⇒ 依前言 §3 降级 S3。
- 定级：S3
- 怎么算修好：给输出面归一另立号（如 `G-P3-PSF-07`/`G-P3-EPSF-02`）、给线性代数失败另立号（如 `G-P3-QW-05`），并在 `V6_PHASE3_RSMP_IMPL.md:24` 与 `v6_clause_registry_v1.json` 的门面登记上同步补条目（否则又造成 CR-60-11 主条的"发射未登记"）。
- 置信：CONFIRMED

## 4.3 不报的核过项（附检索式）

- Q/W 公式与 §28.6:2683 **逐字一致**，且真按 `C_y⁻¹` 线性求解（两次 `cholesky_solve`，非伪逆、非对角近似）；`cholesky_solve` 失败 ⇒ Reject（fail-closed 正确）。检索式：`git grep -n "cholesky_solve\|pi_cinv_pi"`
- `propagate_point_source_flux` 对任何 invalid 输出整件 REJECT（:169-176），未静默丢像素 ⇒ 与 C-P3-PROP-6 同向（偏红）。
- DiagonalOnly 在生产恒 REJECT：`p3_v6_export.h` 的 `covariance_diagonal` 默认 false，且 `optimism_audit_performed` 无生产置真点 ⇒ G-P3-COV-01/QW-04 方向正确；问题在反向（自称 ExactFull）⇒ 已并入 CR-60-6。
- `pi.effective_psf_normalization_declared`（:128 转入记录）无任何门读取——但 §28.6:2689-2690 要求的是"未输出 effective PSF → REJECT"，该条由 `effective_psf_present` 承载 ⇒ 死字段，登记不报。检索式：`git grep -n "effective_psf_normalization_declared"`
- `SurfaceBrightnessInput` 无 `variance_from_weight` 字段，:74 以 `in.variance_from == "weight"` 推导 ⇒ 与 `vf_forbidden`（failclosed.cpp:92-93，词表含 `"weight"`）完全等价，冗余非洞。

---

# 5 · `lib/algorithms/resample/p3_rsmp_units.cpp`（209 行）

## 5.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :95 | `units::signal_sb{1,-2}` | ADU 幂 1、px 幂 −2 | 面亮度冻结单位 | 公式导出（§31.1a 推导链 `signal = sumFlux/sumArea` ⇒ `ADU/sr`）；与登记面**同值**（`canonical()=="ADU/sr"`，`core_test.cpp:300` 断言） | `DATA_SEMANTICS.md:3124`、:3168；`v6_clause_registry_v1.json:23-26 units_table` | — | CONFIRMED |
| :96 | `pixel_variance_in{2,0}` | ADU² | 输入像元方差 | 结构性不适用（合同冻结定义位：幂次编码，非经验量值） | `DATA_SEMANTICS.md:3125`（FZ-UNIT-VAR-IN，PENDING/SO-01） | 该条状态是 **PENDING_OWNER_SIGNOFF** 而代码已当既成事实使用 ⇒ 见 5.3 | CONFIRMED |
| :97 | `sb_variance_out{2,-4}` | ADU²/sr² | 输出方差 | 公式导出（二次律 = (ADU/sr)²） | :3126、:3135 | 同上（SO-01） | CONFIRMED |
| :98 | `sb_ivar_out{-2,4}` | sr²/ADU² | 输出 ivar | 公式导出（ivar = 1/variance） | :3127、:3135 | 同上（SO-01） | CONFIRMED |
| :99 | `w_info{-2,0}` | ADU⁻² | 点源信息权重单位 | 结构性不适用（冻结定义位） | :3128 FZ-UNIT-WINFO **FROZEN** | — | CONFIRMED |
| :100 | `q_stat{-1,0}` | ADU⁻¹ | Q 统计量单位 | 结构性不适用 | :3129 FZ-UNIT-Q FROZEN | — | CONFIRMED |
| :101 | `flux{1,0}` | ADU | 积分通量单位 | 结构性不适用 | :3130 FZ-UNIT-FLUX FROZEN | — | CONFIRMED |
| :102 | `psfsw{0,0}` | 1（无量纲） | **已退役对象**的单位仍在冻结表内 | **待确认** | 与之矛盾的登记面：`DATA_SEMANTICS.md:3131` 该行划除并标 **OBSOLETE**/对象已真删；`v6_clause_registry_v1.json:2811` 逐字"units_table 移除该符号行（§31.1 已标 OBSOLETE/对象真删，此处只是对齐）" | 保守方向：删除该常量与 `p3_rsmp.h:16` 注释里的 `psfsw=1`；影响 = 仅表面对齐（无门读它），但"冻结单位表"的完整性主张与登记面相反 | CONFIRMED |
| :60, :66, :143 | `1`（无量纲串）、`px_power / 2`、`2 * e` | 2 | 内部线性像元幂次 ↔ 立体角符号幂次的映射因子 | 结构性不适用（定义常数：立体角是像元面积的一次方 ⇔ sr 幂次 = px 幂次/2）；**但整除截断无校验 ⇒ CR-60-9** | `DATA_SEMANTICS.md:31.1a`；`p3_rsmp.h:62`；units.cpp:56-58 注释自陈同一映射 | — | CONFIRMED |
| :184, :186 | `pixel_area_power == 0` / `== -2` | 0 / −2 | 裸 `ADU` 可读为通量/面亮度的 provenance 条件 | 公式导出（合同逐字规定 −2） | `eng/contracts/schemas/product_family_field_constraints.schema.json:232` const 逐字"…pixel_semantics=surface_brightness + **pixel_area_power=-2** + 目标像素面积"；`DATA_SEMANTICS.md:375,686`（裸 ADU 只在 §31.2(b) 声明齐备时可写） | — | CONFIRMED |
| :132, :142 | `std::stoi(f.substr(n + 1))` | — | 幂次解析 | 待确认（无 consumed 校验 ⇒ 与另一实现结论相反，CR-60-10） | 第二实现 `lib/algorithms/fits_output/p3_output.cpp:66-76` 逐字校验 `if (used != s.size() - bl - 1) return false;` | 保守方向：补 consumed 校验（向严格侧靠）；影响 = 所有经 `resolve_bunit` 的量纲可判定 | CONFIRMED |

## 5.2 findings

### CR-60-9 · `Bunit::canonical()` 的 `px_power / 2` 整除截断把非法内部态印成合法冻结串——写侧比读侧松，"标准键名下装着非标准量纲"
- 轴：R（主轴）＋ C
- 位点：`p3_rsmp_units.cpp:66` `const int sr = px_power / 2;`（读侧对应 `:144` `if (e % 2 != 0) return false;`）
- 上位依据：`DATA_SEMANTICS.md:3136-3137` 逐字"契约冻结串（逐字引用，登记表与 schema `const` 同值；**以该同值为唯一来源**）"；`eng/contracts/schemas/product_family_field_constraints.schema.json:232` 要求 BUNIT 必须"量纲可判"，且 writer 白名单只接受 `{ADU/sr, ADU^2/sr^2, sr^2/ADU^2}`（`DATA_SEMANTICS.md:375` 逐字）。
- 现状：`px_power` 是 public `int`（`p3_rsmp.h:63-65`），`bunit_mul`（:88-90）把两个 `px_power` **相加**，可产生任意奇数值；`canonical()` 对奇数不检查，C++ 向零截断：`Bunit{1,-3}` → `sr=-1` → 返回 `"ADU/sr"`；`Bunit{1,-1}` → `sr=0` → 返回 `"ADU"`（正是 :2378/:686 说"裸 ADU 无法量纲可判"的那个串）。
- 差在哪：写侧无合法性不变式，读侧有（奇数 px 幂直接 `return false`）⇒ 同一套幂次编码两侧不一致，非法量态能拿到合法外观的标准键名。
- 后果：任何经 `canonical()` 落盘的 BUNIT（`module_adapters.cpp:13960` 在诊断串里就调 `res.resolved.canonical()`）可能把"每立体角一次方 × 半格"之类的非标准量纲写成 `ADU/sr` 或 `ADU` ⇒ 下游按 §31.1a 的读法把它当面亮度/积分通量消费，标度类别判错。方向：偏绿（非法态被合法化）。
- 定级：S2（无生产构造奇数 px 的调用点：`git grep -n "bunit_mul" -- lib eng` ⇒ 生产零调用，只有 `p3_rsmp.h:73` 声明与 mutation 脚本；但 struct 字段 public，任何新调用一行即触发）
- 怎么算修好：`canonical()` 入口加奇偶校验（奇数 ⇒ 返回带 `?` 的显式非 canonical 串或走 `BunitResolution{resolvable=false}`），并把不变式收进类型（私有字段 + 构造期校验，或 `bunit_mul` 内断言偶数）；同时补一条 mutation 用例（`Bunit{1,-3}` 必被拒），与读侧 :144 的拒绝条件对齐。
- 置信：CONFIRMED（截断值手算：−3/2→−1、−1/2→0）

### CR-60-10 · 同一条 FZ-BUNIT-SEMANTICS/二次律有**两份独立实现**，对同一输入串结论相反；且 `canonical()` 写出的纯面积串自己读不回
- 轴：C（主轴）＋ R
- 位点：`p3_rsmp_units.cpp:119-168`（`parse_bunit_string`）与 `:61-64`（`canonical()` 的 `px^N` 输出）；对岸 `lib/algorithms/fits_output/p3_output.cpp:60-95`（`bunit_square_canonical`，同一律的第二份实现，被 :355/:764 用于写盘）
- 上位依据：`DATA_SEMANTICS.md:3136-3137`（"以该同值为唯一来源"）；:3138-3150（单位层/数值层分离的读法消歧，规定这条律只有一份口径）。
- 现状：
  1. **宽容差差异**——units.cpp:132 `try { *pow_out = std::stoi(f.substr(n + 1)); } catch (...) { return false; }` 不校验消费长度；`p3_output.cpp:71-73` 校验 `used != s.size() - bl - 1 ⇒ return false`。⇒ 同一串 `"ADU/sr^2garbage"`（或 `"sr^2/ADU^2x"`）在 units 侧 `resolvable=true`（reason `explicit_solid_angle_power`），在写盘侧被判不可解析。
  2. **写读不对称**——`canonical()` 对 `adu_power==0` 输出 `"px"`/`"px^-2"` 等（:61-64），本文件注释 :57-58 还逐字规定"纯像元面积单位（adu_power==0，support/coverage 的 `px^2`，§12.2）保持冻结符号 `px^2`"；但 `parse_bunit_string` 的两个分支（:155、:161）都要求串里含 `ADU` 因子，除 `"1"` 外**任何 `px^N` 串都 `return false`** ⇒ `resolve_bunit("px^2", prov)` 得 `resolvable=false, reason="unparsable_bunit_string"`。
- 差在哪：一条"唯一来源"的律被实现两遍且两侧松严不同；本件自己写的冻结串自己判"不可判"。
- 后果：①同一产品 BUNIT 在门侧与写侧得到相反的可判性结论 ⇒ "BUNIT 量纲可判"这一对外主张取决于走哪个入口，损坏/畸形串可能通过门而通过不了 writer（或反过来，被门放行写进诊断面）；②按 :57-58 规定写出 `px^2` 的 support/coverage 面在 p3rsmp 侧被误判"不可判" ⇒ 方向偏红（过拒），同时①方向偏绿。**两条方向各已定**。
- 定级：S2（`resolve_bunit` 有生产调用点 `module_adapters.cpp:13930`，但该处仅用于生成诊断/拒绝消息，未见产品字节直接由它决定 ⇒ 不给 S1；缺的是该返回值在适配器里是否参与判据的进一步确认）
- 怎么算修好：单位代数收一份实现（`p3_output.cpp` 的 `bunit_square_canonical` 与 `p3_rsmp_units.cpp` 合并到 `astrocs_p3_rsmp` 单一入口，另一侧只调用），两侧解析器同校验 consumed 长度；给 `px^N` 族补读侧分支（或在 `canonical()` 禁写 `px^N` 并改用 §12.2 登记的符号）；同批在 `v6_clause_registry_v1.json#units_table` 面上登记该族，否则 §31.1 表与代码又分叉。
- 置信：PARTIAL（两侧解析器逐字读过；未实跑畸形串的端到端结果，缺"适配器返回值是否参与判据"一角证据）

### CR-60-11 · 门号登记面双向都不一致：代码发射的门号在 docs/contracts 零登记，头文件声明的门号代码从不发射
- 轴：C
- 位点：`p3_rsmp_failclosed.cpp:78` `add_gate(&v, Status::Reject, "G-P3-MODE-01", "phase3_mode_not_declared");`；声明侧 `p3_rsmp.h:168` 逐字"注册前置结构门（缺 Oracle/误差界/边界定义 → REJECT）。**G-P3-KRN-03/04**"
- 上位依据：`V6_PHASE3_RSMP_IMPL.md:24`（12 门逐一列名，**不含** `G-P3-MODE-01`）；`v6_clause_registry_v1.json:2073`（kernel/omega 面上唯一登记的门是 `G-P3-SB-01`）。
- 现状与检索式：`git grep -n "G-P3-MODE-01" -- .` ⇒ 唯一命中是发射点自身；`git grep -n "G-P3-KRN-04" -- .` ⇒ **零命中**（既无发射也无登记）；`git grep -n "G-P3-KRN" -- docs eng/contracts` ⇒ 零命中（KRN-01..05 整族未登记，而 kernel_registry.cpp 实际发射 KRN-01/02/03/05）。
- 差在哪：登记面与实现面双向断裂——一个发射未登记，一个登记（在注释里）从不发射，还有一族完全没有登记位。
- 后果：任何按门号做的覆盖/mutation 统计（`ACCEPTANCE_SPEC` 的门表、`EVIDENCE.md` 的门↔条款映射）会把 MODE-01 当成不存在的判据、把 KRN-04 当成存在且已验证的判据；不影响产品字节 ⇒ S3。
- 定级：S3
- 怎么算修好：要么在 `v6_clause_registry_v1.json` 的 kernel/failclosed 面上补齐 MODE-01 与 KRN-01/02/03/05（含 `negative_mutation` 位），要么删掉头文件里的 KRN-04 与代码里的 MODE-01（后者需同批把"模式未声明 → REJECT"的 §28.6:2649 条款挂到某个已登记门号上，否则该条要求失去实现锚）。
- 置信：CONFIRMED

## 5.3 不报的核过项（附检索式）

- **面亮度/通量/星等三者换算**：本件不做星等换算，且这是设计明文规定的——`DATA_SEMANTICS.md:3177-3181` 逐字"**数据面不落盘星等**：需要星等时按 `m = ZP_k − 2.5·log10 F` … 现场派生"、"**必须保持线性的硬理由**…阶段二/三因此不做星等换算" ⇒ 派单问的"星等换算缺失"不是缺陷，是符合设计的零实现。检索式：`git grep -n "log10\|mag" -- lib/algorithms/resample`
- 8 个单位常量的幂次编码与 §31.1 表逐行**同值**（含 `flux` 行注的 variance `ADU^2`/ivar `ADU^-2` 与 `w_info=ADU^-2`、`q_stat=ADU^-1`）；`canonical()` 三串与 writer 白名单三串逐字一致（`core_test.cpp:300-302,317` 亦断言）。检索式：`git grep -n "ADU/sr\|ADU\^2/sr\^2\|sr\^2/ADU\^2" -- docs/contracts/DATA_SEMANTICS.md`
- `pixel_variance_in`/`sb_variance_out`/`sb_ivar_out` 三条在 §31.1 的状态是 **PENDING/SO-01** 而非 FROZEN：`v6_clause_registry_v1.json:20-21` 规定"生效前实现不得放宽、相应面 fail-closed"，本件未放宽任何数值（只登记单位）⇒ 不构成缺陷，登记备查（若 SO-01 最终改值，本表 5.1 前三行需同步）。
- `is_retired_mode_token`（:35-39）与 `is_deferred_weight_mode_token`（:41）在 p3rsmp 内零调用（生产 `module_adapters.cpp:14122` 只用 `parse_mode`，其拒绝语义由 :43-50 的白名单式三分支达成）⇒ 冗余辅助函数，不报（与 CR-60-2 的"白名单方向正确"是同一件事的正例）。
- `parse_bunit_string` 先去空白（:120-123）⇒ `"ADU / sr"` 可判，与 FITS 引号串的实际写法不冲突。

---

# 6 · `lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp`（437 行）

## 6.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :66-67 | `kW`、`kPx` | 512、512² | 夹具 tile 边长/像素数 | 结构性不适用（数组维度） | `DATA_SEMANTICS.md:451` 规定产品 tile = `[512×512]` ⇒ 夹具与产品尺度同尺寸 | — | CONFIRMED |
| :68 | `kBase` | 1000.0f | 非角点像素常量 | 结构性不适用（夹具内容，不是科学量；:71-77 用它做"值⇄索引"可反查编码） | 文件自述 :71 | — | CONFIRMED |
| :69, :316 | `kEpsF32`、`4.0 * kEpsF32` | 2⁻²³ ×4 ≈ 4.77e-7 | 值断言相对容差 | 实验标定（推导可核：f32 一次量化 + 3 次 f64 累加 ⇒ 若干 ulp；代码写成 4 ulp 的显式倍数） | `docs/standards/NUMERIC_STANDARD.md`（标度/dtype 律，:7 引用面） | 保守方向：容差只会因 f32 输出而**放宽**才有意义；写死 4 ulp 属实现选择，登记为夹具侧参数即可 | PARTIAL（未复跑 ulp 实测量级；§4 规定测试期望值差不算缺陷） |
| :83-84 | `kLeafOrder`、`kLeafNside` | 9、512 | 叶级 order/nside | 结构性不适用（HEALPix 定义：order-0 tile 宽 512 ⇒ nside=2⁰·512 ⇒ order=9） | 文件 :81-82 注释逐字"leaf 级 order = 0 + log2(512) = 9 … 与生产 read_leaf 的 `s->order + 9` 逐字一致" | — | CONFIRMED |
| :89 | `nested_local_to_fits_index(..., 9, kW)` | 9 | 同上（映射用的 order） | 结构性不适用 | 同上 | — | CONFIRMED |
| :159-160 | `i <= 200`、`0.001 * i` | 200、1e-3 | 方向搜索半径与步长（度） | 结构性不适用（循环上界/试探步长，实现选择） | 零（条文不规定搜索参数） | 保守方向：步长更小/上界更大只会更容易找到合格方向，不改判据；影响 = 用例可建立性 | CONFIRMED |
| :176 | `ww[0] < 0.5` ⇒ 要求角 0 权重 ≥ 0.5 | 0.5 | 非退化前提（注入点权重占优） | 实验标定（:152-154 注释给出理由：`w0 ≥ 0.5 ⇒ (1/(1−w0))² ≥ 4`） | 文件自述 :152-154；`docs/algorithms/PHASE3_RESAMPLE.md §2 G4` | 算术可核：w0=0.5 ⇒ (1/0.5)²=4 ⇒ 与 :392 的 ">5%" 锚自洽 | CONFIRMED |
| :259 | `wmax >= 0.45 && wmin <= 0.15` | 0.45 / 0.15 | 权重不对称前提复检 | 结构性不适用（前提复检阈值，比 :176 的 0.5 略松以吸收浮点/搜索离散） | 同上 | 保守方向：应与 :176 同值或写明为何允许 0.05 松弛；影响 = 前提失效时用例是否仍能红 | PARTIAL（未实跑实际命中的 w 向量） |
| :264-276 | 夹具角值 `2000/4000/8000`、`kNaN/kPInf/kNInf` | — | 掩膜用例注入值 | 结构性不适用（夹具内容） | T2/T3/T4 判据自述 :12-15 | — | CONFIRMED |
| :330-335, :390 | `1e-12` | 1e-12 | 生效权重与方差项的相对容差 | 公式导出（期望与实得都是 f64 组合、无 dtype 截断 ⇒ ulp 级；代码按 `close_rel` 真算） | `docs/standards/NUMERIC_STANDARD.md` | — | CONFIRMED |
| :340, :392 | `> 0.01` / `> 0.05` | 1% / 5% | 非退化锚：真值与两种错值的最小可分差 | 待确认（合同要求"判据必须非退化"但未给分差数值；见 §`AGENTS.md §5`"判据必须非退化"与 `ACCEPTANCE_SPEC`） | `DATA_SEMANTICS.md:2695-2696` 给出同类失效量级 23.3%/28.7% ⇒ 5% 锚显著低于合同关心的效应量 | 保守方向：锚越大越有力；把 1%/5% 提到接近合同量级（>20%）更能防"判据贴地" | CONFIRMED（数值取位已核，效应量级未复跑） |
| :420 | `double u_out = 123.0;` | 123.0 | "输出必须被写、不得保留初值"的哨兵 | 结构性不适用（哨兵，非量值） | `DATA-002 §2a` 强制计数/显式语义 | — | CONFIRMED |

## 6.2 findings

### CR-60-12 · 同一测试资产在两个登记面上说法相反：已注册构建＋ctest，同时又作为"unbuildable_test"判红被冻结在豁免基线里
- 轴：C
- 位点：本文件整件（构建注册对象）；两侧登记面 `eng/tests/unit/CMakeLists.txt:1665-1672`（`add_executable(w34_p3_nan_mask_test ${ACS_W34_REPO}/lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp)` … `add_test(NAME w34_p3_nan_mask COMMAND w34_p3_nan_mask_test)`）与 `eng/ci/prod_wiring_baseline.json:78`（`"unbuildable_test:lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp"`）
- 上位依据：该基线文件 :3 自述 purpose："CHK-PROD-WIRING 棘轮基线：**已冻结的判红 finding id**。默认运行只对新增判红；**冻结项消解后运行 `--update-baseline` 收紧**"；判据定义 `eng/ci/check_prod_wiring.py:40`"W4 unbuildable_test —— tests/ 目录存在测试源但**无构建目标**"，实现 :883-889（`if p.name in compiled or rel in compiled: continue`；`if p.name in cmake_text ... continue`），而 `_cmake_blob`（:1110-1121）用 `repo.rglob("CMakeLists.txt")` 扫全部 CMakeLists（含 `eng/tests/unit/`）。
- 现状：本文件的名字逐字出现在 `eng/tests/unit/CMakeLists.txt`（:1666），按 W4 判据第二个 `continue` 即应豁免 ⇒ 今天它**不该**再被判 unbuildable；但它仍留在冻结基线里。另注：`target_include_directories(... ${ACS_W34_REPO}/lib/algorithms/resample/include)`（:1668）指向**不存在**的目录（`git ls-files lib/algorithms/resample/include` 零命中；头文件在模块根，经 `astrocs_p3_rsmp` 的 PUBLIC include 面传入），所以该 include 项是空转，不影响解析。
- 差在哪：豁免清单里留着一个已消解的红灯条目 ⇒ 基线"冻结项消解后须收紧"的自述纪律未被执行。
- 后果：该文件若哪天真的掉出构建图（例如 target 被删），W4 会因基线已含同名 finding 而**不报新增红** ⇒ 测试静默失效而 CI 全绿。**方向**：基线多一条过期 finding ⇒ 少一次判红 ⇒ 偏绿。
- 定级：S3（不产出错值，但削弱门本身；本批已确认测试**能红**：`failures` 在 :430/:434-435 汇聚为 `return 1`，非"不崩即绿"）
- 怎么算修好：跑一次 `--update-baseline` 收紧（由前台执行）并核对该文件在收紧后不再出现；同批检查同族 `unbuildable_test:*` 其余 20 余条是否同样过期（只查构建面命中，不放宽判据）。
- 置信：PARTIAL（两侧登记面与判据源码逐字读过；**未实跑** `check_prod_wiring.py`——前言 §1 禁跑 `eng/**`，故"今天它确实不再命中 W4"是静态推断而非实测）

## 6.3 不报的核过项（附检索式）

- **它断言的是行为，不是"不崩"**：T2 用独立解析 oracle（:186-197 由 Pass A 的几何权重 `w[]` 复算重归一加权和），T5 强制计数按原因分类（:297-305 三类 + `n_eligible`），T6 用 `wrong_geom`（:391）与 T7 用 `wrong_unweighted_mean`（:339）作非退化锚；判据失败 `return 1`。检索式：`git grep -n "failures" -- lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp` ⇒ :44,49,58,430,434-435。
- **T6 的期望值取自生产返回的 `ww[]`（:386-389），看似自参照，实际不是**：`want` 内部又除以 `Σww`，对"是否已归一"不变 ⇒ 单看 :390 判不出漏归一；但 :330-335 已在同几何的 1-剔除用例上把 `ww[k] == w[k]/Σw_elig` 与 `Σww == 1` 独立锁死（T2a-1NaN，:268），且 :392 锚住 5% 分差 ⇒ 归一缺失会让 :390 红。结论：**不报**（该疑点已复算排除）。
- 掩膜规则本身在生产侧成立：`p3_resample.cpp:322-326` 的 `p3_sample_bilinear_ex` 直接委派给 `p3_sample_bilinear_nanmask_ex`（rejection 传 nullptr），即生产调用点 `module_adapters.cpp:14354`、`p3_session.cpp:291` 走的正是被这份测试锁住的同一数学路径 ⇒ 不报"测试面与生产面不耦合"。检索式：`git grep -n "p3_sample_bilinear_ex\|p3_sample_bilinear_nanmask_ex" -- lib`
- 夹具的 `BUNIT = ADU/sr` + `ASTROCS_PIXEL_SEMANTICS=pixel_area_power=-2`（:100-104）与 §31.1a/:2378 一致；`variance` 子产品不带该 provenance（:143）与其 `ADU^2/sr^2` 自洽。
- 固定路径 `$TMPDIR/astrocs_p3_nan_mask`（:225）＋用例前 `remove_all`（:138）＋末尾 `remove_all`（:429）：单机串跑无冲突，未见并发跑同一测试的注册位 ⇒ 不立条。

---

# 收尾三节

## A 本批最高危三条（只挑，不复述）

1. **CR-60-6** 传播件把"有没有相关核"建在调用方自报枚举上（`p3_rsmp_propagation.cpp:62-64,134-136`），声明 `ExactFull` 而实际传对角 `C_x` 时协方差被静默丢弃且全门判绿 ⇒ 方差低估、SNR 高估（合同量级 23.3%/28.7%），直指 SCI-B"跨帧绝对 SNR"。审计工具 `max_abs_offdiag_correlation` 生产零调用。
2. **CR-60-3** registry 与实现双向不对应："生产科学默认" `bilinear_area_overlap_exact` 无人实现却通过 `admit`，kernel_id 只是盖在算子上的标签（`p3_v6_export.cpp:236-239` 只有两选一）⇒ 可发布"声明精确面积交叠/误差界 0，实为双线性插值"的 provenance。
3. **CR-60-2 ＋ CR-60-4** fail-closed 的两块招牌面都是空的：权重/方差来源门只比 19 个精确字符串、字段只在阴性注入旋钮上非空（真实产品内容进不去，方向 fail-open）；而 NaN 样本在 p3rsmp 路径无掩膜/无计数，产出 `SIGNAL=NaN ∧ COVERAGE=1` 的自相矛盾像素（方向 fail-open，冲击 SCI-C 覆盖度主张）。
   （根因 CR-60-1：本批 5 个实现件当前**不在 export 生产路径上**，所以这三条都封顶 S2；这也意味着一旦接通即集体升 S1，修序应先解 CR-60-1。）

## B 我读不动或需要实跑才能定的

| 待判项 | 缺哪一角 | 该跑什么（派单重点） |
|---|---|---|
| CR-60-9／CR-60-10 的实际可达性：生产能否构造奇数 `px_power`、畸形 BUNIT 串经 `module_adapters.cpp:13930` 是否参与判据 | 该适配器返回值的下游用法未逐跳读（属 scheduler 批次文件域） | 实跑 `v6_p3_proj` / 适配器侧用例并注入 `"ADU/sr^2junk"`、`Bunit{1,-3}` 两条负例，看是否判绿 |
| `0.027395522883651657 / 1·36 / 1.3223` 这组 bilinear Oracle 证据的 fixture 定义（`h`、F 与其二阶导） | 未读 `eng/tests/unit/v6_p3_rsmp/p3_rsmp_oracle.h`（测试件在本批清单外） | 读 `run_kernel_oracle()` 并复算 1/36 的来源式；把 fixture 标识登记进 registry 证据位 |
| CR-60-5 的边缘 NaN 是否已在真实导出网格触发 | 未实跑 1:1 对齐 plan | 跑 `p3_rsmp_core_test` 的一个新 plan（`out_origin=0.5, step=1`）或 e2e 用例并统计最外行列的 NaN 数 |
| CR-60-7 尺度不可算的确切失败形态（bad_alloc / 看门狗 137） | 未实跑（且 `AGENTS.md §3` 要求套 `mem_guard.py --max-rss-gb`） | 以 4096 px 尺度做外推基准，再决定是否给尺寸门 |
| CR-60-12 的 W4 判据今天是否真的不命中 | 前言 §1 禁跑 `eng/**` | 由前台跑 `check_prod_wiring.py` 的 `--self-test`/正常档，核对 unbuildable 计数是否下降 |
| `psf_sum_tol=1e-9` / `row_sum_tol=1e-9` / `bunit_tol=1e-12` 三个守卫值是否够用 | `row_sum_tol`、`bunit_tol` **全仓零使用**（`git grep -n "row_sum_tol\|bunit_tol"` ⇒ 只有 `p3_rsmp.h:321-322` 声明） | 实测算子行和/单位比较的 f64 噪声量级后登记容差，或直接删掉两个死字段 |

## C 派单与清单差异

1. **路径差异**（已在文首登记）：派单第 6 项 `lib/algorithms/resample/p3_nan_mask_test.cpp` 在跟踪集中不存在，实际是 `lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp`；按清单意图读实际路径。
2. **派单②问"注册项与实现是否一一对应"，本批答案是全否**：双向都断（被注册为默认的无实现；在跑的标为非默认；`production_science_default` 无判据使用者）。另发现登记面双向断裂（CR-60-11）：发射未登记 / 声明从不发射。
3. **派单③问"单位换算是否与标准键一致"，本批单位件的答案与问法错位**：单位件（`p3_rsmp_units.cpp`）只做**量纲编码**（幂次对），不做任何数值换算；"标准键名下装非标准量纲"在单位件的成立形态是 `canonical()` 的截断打印（CR-60-9），不是数值换算错误。数值侧的静默丢弃发生在传播件（CR-60-6）。
4. **派单④问"面亮度/通量/星等三者换算"**：星等换算在本模块是设计明令不做（`DATA_SEMANTICS.md:3177-3181`），不构成缺陷，已列入 5.3 核过项。
5. **派单①问"拒的是枚举值还是消息子串"**：都不是——拒的是**调用方自报的布尔位＋封闭字符串全等表**，唯一的真枚举面只有 `CovarianceRepresentation::WeightDerived`；"消息子串"匹配在本件不存在（`why` 串只做拼接输出，从不回读判定）。已按此改写为 CR-60-2。
6. 派单未点名、但影响最大的一条是可达性（CR-60-1）：本批审的这套三模式栈与 `export` CLI 之间没有生产连线，生产走同目录另一实现。

<!-- PROGRESS: 6/6 -->
