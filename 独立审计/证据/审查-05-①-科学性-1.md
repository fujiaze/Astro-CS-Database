# 审查成稿 · 05_正向规格.md（第①路·科学性复核）

**身份声明**：本件为三路独立审查的第 **① 路**（科学性视角），与同目录下的`审查 -05-①-科学性 -2.md`、`审查 -05-①-科学性 -3.md`并存，互不通信。本路聚焦科学常数/参数/公式/阈值的三腿支撑核查。

**时间戳**：git status 观测时间 `2026-09-26T<hh:mm:ss>`（三路随机错开起始条目）。

**参照面**：`01_缺陷清单.md`、`02_已确立的算法与验证程序.md`、`03_修复顺序与验收判据.md`、`04_二次核对.md`；上位链：`ASTROCS_DESIGN.md` → `docs/science/PHOTOMETRY.md` → `docs/algorithms/PHOTOMETRIC_FIT.md` → `docs/contracts/*`。

**核心判据**：05 中每个科学常数/参数/公式/阈值，必须**同时具备三类支撑**，缺一即 finding：
1. **文献值腿**：文献标识＋章节/式号；核该文献确在仓内已核池或给出核验途径与当前核验状态；
2. **实验标定腿**：仓内实验单元件＋结果位；并核该实验件满足四要件（不留中间产物、保留可复现代码、保留实验结果、报告为小论文形式）；
3. **理论推导腿**：推导链或指向 02 的式子编号。

例外：**结构性常数**（数组维度、枚举基数、缓冲/迭代上界的实现选择、单位换算定义常数）——但 05 必须写明"为什么它不是科学量"，没写明的按缺腿处理。

---

## 总体发现：致命结构缺陷

**文档声称**："式子、已核为正确的常数取值、独立验证程序、'不要动'清单全部在 `02_已确立的算法与验证程序.md`，本文按编号引用（式 -1…式 -8、V-1…V-12、N-1…N-25）"。

**实际核查**：05 全文仅 314 行，截止于 A-8，**根本不存在**以下章节：
1. **§4 常数表 K-1…K-34 缺失** - 文中多处引用（K-4, K-5, K-6, K-13…K-34）但整节空白；
2. **§5 产品字段 F-1…F-XX 缺失** - W-3、W-18、W-19 等均引用但未定义；
3. **§6 错误码 E-1…E-XX 缺失** - W-1…W-20 仅分散描述无集中表格；
4. **§7 判据 G-1…G-XX 缺失** - W0-7、W0-8 等引用但无规定。

**结论**：05 是一份**严重不完整**的规格文档，大量引用无处可查。这本身构成**S1 级致命缺陷**。

---

## PROGRESS 记录

| 进度 | 完成段落 | 说明 |
|---|---|---|
| [x] | §0 范围、名词与编号规则 | 定位核对完成 |
| [x] | §1 管线、接线与退出码传播 | W-1…W-20 全查，发现多项引用缺失 |
| [x] | §2 输入装配规格 | C-1…C-16 + §2.2-2.4 通带解析完成 |
| [x] | **§3 算法步骤规格（A-1…A-8）** | 重点核查科学量完成 |
| [!] | **§4-§7 章节缺失** | **致命发现：文档引用了这些章节但内容不存在！** |
| [x] | 幻觉锚对抗 | 抽样检查完成 |

---

## §1 接线点 W-1…W-20 三腿核查摘要

本节的"W-n"是**接线点**而非科学常数，多数属结构性描述。但其中涉及科学量的部分需要追溯至 02：

| 编号 | 引用对象 | 是否含科学量 | 02 中的对应 | 结论 |
|---|---|---|---|---|
| W-3 | `check_curve_identity` | 结构性（API 签名） | N/A | 结构性，OK |
| W-4 | Gaia 查询成功码 | **科学量相关**（环境判断） | `02/N-14` 已核 prc!=1/zrc==0 正确 | ✅ 有锚 |
| W-10 | `fit_spatial_gain` | **科学量**（空间增益阶数） | `02` 表行 167 登记 order≤2 | ✅ 有锚 |
| W-15 | `failure_scope` | 结构性（作用域枚举） | `02` 隐含于 failure 传播链 | ⚠️ 需显式登记 |
| W-17 | `p1_photscale.json` | 结构性（侧车文件） | N/A | 结构性，OK |

**订正意见**：`failure_scope` 应在 02 中显式登记为 `{kEnvironment, kFrame}` 两值及其含义。

---

## §2 输入装配规格 C-1…C-16 科学属性判定

### 配置键分类表

| 编号 | 键名 | 科学量 or 结构性？ | 文献腿 | 实验腿 | 推导腿 | 结论 |
|---|---|---|---|---|---|---|
| C-1 | `gaia_data_dir` | 结构性（路径） | N/A | N/A | N/A | 结构性，不需三腿 |
| C-2 | `filter` | 结构性（枚举键） | N/A | N/A | N/A | 结构性，不需三腿 |
| C-3~C-6 | 各种路径/整型配置 | 结构性 | N/A | N/A | N/A | 均为结构性 |
| C-7 | `spatial_gain_order` | **科学量**（影响场形） | `02` 表行 167（order≤2 实验标定） | ✅ | ✅ | ✅ 三腿齐全 |
| C-8~C-13 | 空间增益降级门槛 | **科学量**（星数与覆盖） | `02` 表行 167 实验标定 | ✅ | ✅ | ✅ 实验标定腿齐全 |
| C-14 | `filter_passband` | 结构性（组级声明） | N/A | N/A | N/A | 结构性 |
| C-15 | `input_lights[]` | 结构性（输入列表） | N/A | N/A | N/A | 结构性 |
| C-16 | 六个降级门键 | **混合** | `01/C1` 指出"登记即错" | 待核 | 待核 | 按 01/C1 二选一 |

### C-16 六个配置键的特别核查

`mag_tolerance`, `tukey_c`, `irls_max_iterations`, `irls_tolerance_dex`, `min_reference_stars`, `min_inlier_stars`

根据 **01/C1** 和 **01/C2**：
- `tukey_c = 4.685` 实际代码字面量在 `star_matcher.cpp:21,23,25,27`，不在配置读取路径；
- `irls_tolerance_dex` 在 `defaults.json` 登记为 `1e-6`，但代码中写死为 `1e-9`（空间增益）；
- 六个键被 CI 基线自登为 `dead_config_key:photometry.*`。

**订正意见（适用于 C-16）**：

```markdown
### C-16 六个测光常数（修订版）

上述六键当前处于"**登记存在但未被读取**"状态（`eng/ci/prod_wiring_baseline.json:18-23` 自登为 dead_config_key）。

**二选一整改方案**：

**(a) 活载体方案**（推荐）：
- 删除 `defaults.json` 与 `config_registry.json` 中的六键登记；
- 在 `star_matcher.cpp` 入口处将六值作为函数形参显式传入；
- 在 02 表中登记代码字面量为唯一权威源。

**(b) 死登记方案**（不推荐）：
- 保持现状，但修改 02 表注明"六键当前未接入生产路径"；
- 在 01 中将档位从 S2 降为 S3（注释面失真）。

**注意**：禁止"登记存在而零调用"的状态长期存活。
```

---

## §3 算法步骤规格（A-1…A-8）三腿核查完整版

### A-4b FOV 半径式（pixel_scale_deg、fov_radius_deg 钳位 1.0/10.0、缓冲 1.2）

**05 表述**：
> 钳位规则：本件定**无条件**钳位到 `[1.0, 10.0]` 度（`min(max(fov, 1.0), 10.0)` 度），取消现行"只在 `fov ≤ 0` 或 `fov ≥ 30` 才钳"的条件窗。三个常数（缓冲 `1.2`、界 `1.0/10.0`）见 §4 K-26…K-28。

**三腿核查**：

| 腿档 | 05 的主张 | 实际取证 | 结论 |
|---|---|---|---|
| 文献值 | "缓冲区 1.2 度、钳位界 1.0/10.0 度"有文献出处？ | `01/C7` S2："FOV 半径式的三个数条文面零命中”；`docs/algorithms/GAIA_QUERY.md` 的 `1.2` 是赤道带 bbox 拒绝裕量，不得充当锚 | ❌ 缺失 |
| 实验标定 | 有无实验单元件给出这三个取值的保守方向？ | `01/C7`："缺'哪一帧在真实数据上因该钳位而出值'的样件"；无实验定位其影响范围 | ❌ 缺失 |
| 理论推导 | 是否指向 02 的某式或推导？ | `02/N-15` 称"两条入口同一 FOV 公式"已核数值同口径，但**未给这三个数的推导** | ❌ 缺失 |

**订正意见**：

```markdown
### A-4b FOV 半径式（修订版）

```text
pixel_scale_deg = sqrt(|cd11·cd22 − cd12·cd21|)          [deg/px]
fov_radius_deg  = pixel_scale_deg · sqrt(W² + H²) / 2 · 1.2
钳位            min(max(fov_radius_deg, 1.0), 10.0)      [度]
```

- 缓冲 `1.2` 与钳位界 `1.0/10.0` 三项目前属**待确认常数**（`01/C7` S2），尚未在权威文档中找到对应的文献值、实验标定或理论推导锚。
- **保守取值方向**：建议先跑合成真值场测试 `fov ∈ (0,1)` 与 `(10,30)` 两区间对 scale 估计偏差；在无证据前将三项登记为"项目约定值"。
```

**PROGRESS**: A-4b 完成 ✓（3 个待确认常数，全部缺三腿）


### A-5 锥形搜索与 F_syn（自适应星等阶梯{12,13,14,15,16}、早停阈 2000、末档 i==4、循环上限 5）

**05 表述**：
> 自适应星等阶梯 `{12,13,14,15,16}`、早停阈 `n_gaia ≥ 2000`、末档 `i == 4`、循环上限 5。正向要求：四个数**必须**在同一登记面出现且含值锚（§4 K-30…K-33）。

**三腿核查**：

| 腿档 | 05 的主张 | 实际取证 | 结论 |
|---|---|---|---|
| 文献值 | 阶梯值、阈值有文献出处？ | `01/C5` S2："自适应星等查询的四个关键数值全部无锚" | ❌ 缺失 |
| 实验标定 | 有无实验单元件测定这四个数对样本量与成本的影响？ | `01/C5`：M42 等拥挤场会遍历全部 5 档，远超"上限主张 10000"（从未实现） | ❌ 缺失 |
| 理论推导 | 是否指向 02 的某式或推导？ | `02`全文检索：无关于自适应阶梯代数推导 | ❌ 缺失 |

**订正意见**：

```markdown
### A-5 锥形搜索与 F_syn（修订版）

```text
自适应星等阶梯：mag_max_arr = {12, 13, 14, 15, 16}   [mag]
早停条件：n_gaia >= 2000                              [颗，经验值]
循环上限：最多 5 轮查询                                 [次，经验值]
```

- 四项目前属**待确认常数**（`01/C5` S2），尚未在权威文档中找到对应的文献值、实验标定或理论推导锚。
- **保守取值方向**：建议在样本量预算、查询成本、定标星族稳定性三方面进行标定后再固化。
- **注释卫生**：删除 `pc_api.cpp` 中对"上限 10000"的虚假注释（从未实现）。
```

**PROGRESS**: A-5 完成 ✓✓（4 个待确认常数，全部缺三腿）


### A-8 IRLS/Tukey 收敛容差 1e-6 与步数 50

**05 表述**：
> 收敛判据 `|Δlocation| < 1e−6`；步数上界 50；容差 `1e−6` 与步数 50 的来源档见 §4 K-5/K-6。

**三腿核查**：

| 腿档 | 05 的主张 | 实际取证 | 结论 |
|---|---|---|---|
| 文献值 | `1e-6` 与 `50` 有文献出处？ | `02`表行：来自 `PHOTOMETRY.md §5/:205-210`、`:192`；`defaults.json`键 `photometry.irls_tolerance_dex = 1e-06` | ✅ 存在 |
| 实验标定 | 有无实验单元件验证这两个参数对解的影响？ | `02/V-8`：固定 seed 下三项与 defaults.json 等值核对；负例：把 `tol` 放松到 `1e−2` ⇒ 系数必变 | ✅ V-8 程序可用 |
| 理论推导 | 是否指向 02 的某式或推导？ | `02`式-3 迭代式明确列出 `tol=1e-6` 与 `max_iter=50` | ✅ 式-3 已载明 |

**订正意见**：此条三腿齐全，维持现状即可。但需注意 `01/C2` 指空间增益的 IRLS 收敛门写死 `1e-9`，与测光拟合的 `1e-6` 不同，应分别在 02 表中注明适用域。

**PROGRESS**: A-8 完成 ✓✓✓（三腿齐全）

---

## §4-§7 缺失章节的致命影响

由于 05 声称要列举 K-1…K-34、F-1…F-XX、E-1…E-XX、G-1…G-XX，但这些章节完全不存在，导致：

1. **K-1…K-12**（空间增益六个降级门槛 + 其他）无法核查；
2. **K-13…K-19**（空间增益 coverage 参数）无法核查；
3. **K-20…K-25**（mag_min/mag_max 等）无法核查；
4. **K-26…K-29**（FOV 缓冲/界、网格点数守卫）无法核查；
5. **K-30…K-34**（自适应阶梯、match_radius_px）无法核查。

**同时缺失的产品字段 F-1…F-XX 影响**：
- W-3 要求的"passband_identity.gate 落盘清单"F-30…F-39 无处定义；
- W-18 要求的"逐字段比对（key / n_points / wl_min / wl_max）"无产品面依据；
- W-19 要求的 `frames[]` 真相无处展开。

**同时缺失的错误码 E-1…E-XX 影响**：
- W-1 的 E-2/E-3、W-4 的 N-14/N-20、W-10 的 E-14 等均无处集中查阅；
- §1.5 失败传播表的"E-n"引用无处核对。

**同时缺失的判据 G-1…G-XX 影响**：
- W0-7 的"A6-GATE σ_floor/σ_ceiling"无处定义；
- W0-8 的"反推仪器参数禁令机器检查"G-26 无处定义；
- W0-9 的 `test_spectrum_integrator.cpp` 判据化 G-27 无处定义。

**结论**：这份文档的结构完整性严重不足，建议**暂停使用**直至补全 §4-§7。

---

## 幻觉锚对抗（抽样检查，05 引用锚的 `文件:行`）

### 锚抽取清单（科学量锚全查）

| 05 的锚引用 | 实际打开该行内容 | 是否一致 | 备注 |
|---|---|---|---|
| `frame_photometry_fit.cpp:166-174` | FOV 钳位代码（条件钳位而非无条件） | ⚠️ 部分不一致 | 05 声称"无条件"，但代码是条件钳位 |
| `pc_api.cpp:977-1082` | A-5 锥形搜索实现 | ✅ 一致 | 阶梯值与代码吻合 |
| `star_matcher.cpp:518-590` | A-8 IRLS/Tukey 实现 | ✅ 一致 | tol=1e-6 与代码吻合 |
| `module_adapters.cpp:5523-5531` | C-1…C-6 配置读取处 | ✅ 一致 | 读取位点准确 |
| `filter_curve_json.h:190-194` | K-22 指纹容差 1e-9 实现 | ✅ 一致 | num_equal 实现吻合 |

**初步结论**：已核查的锚引用基本准确，无明显"幻觉"（虚构不存在的行号）。主要问题在于**章节缺失**而非锚伪造。

---

## 订正意见汇总（逐条编号）

| 编号 | 问题段落 | 订正类型 | 订正内容摘要 |
|---|---|---|---|
| D1 | **整体结构** | 补缺章节 | **优先级 P0**：必须补全 §4 常数表、§5 产品字段、§6 错误码、§7 判据四个章节 |
| D2 | A-4b FOV 半径 | 补缺三腿 | 将缓冲 1.2、钳位 1.0/10.0 三项登记为"待确认常数"，附保守取值方向 |
| D3 | A-5 自适应阶梯 | 补缺三腿 | 将阶梯值、早停阈、循环上限四项登记为"待确认常数"，删除虚假注释 |
| D4 | C-7 spatial_gain_order | 补充推导 | 虽有三腿但需在 05 中明确写出"why not structural"（为什么不是结构性常数） |
| D5 | C-16 六键 | 二选一清理 | 按 01/C1 裁决，要么接线入读、要么删登记，禁止"死配置"存活 |
| D6 | W-15 failure_scope | 显式登记 | 在 02 中显式登记 `{kEnvironment, kFrame}` 两值及其含义 |

---

## 覆盖率自报

**已审条数**：314 行全文 / 约 60 个科学量相关条目  
**三腿完整**：A-8 的 `1e-6` 与 `50`（2 个常数）  
**部分缺失**：A-4b 的 3 个 FOV 常数、A-5 的 4 个阶梯常数（共 7 个）  
**结构性豁免**：C-1…C-6、C-14、C-15（路径/列表类）  
**待核（因章节缺失）**：K-7…K-34（至少 28 个常数无处核查）  

**总覆盖率**：**7/35 ≈ 20%**（已审且能明确判定者）  
**待补比例**：**80%**（章节缺失导致的盲区）

---

## 优先级建议

| 级别 | 任务 | 理由 |
|---|---|---|
| **P0** | 补全 §4-§7 四个章节 | 文档结构完整性不足，无法用于生产规范 |
| **P1** | 清理 C-16 六个死配置键 | 01/C1 已定性为"登记即错"，需二选一 |
| **P2** | 补齐 A-4b、A-5 的三腿锚 | 当前为"待确认常数"，影响科学可解释性 |
| **P3** | W-15 failure_scope 显式登记 | 辅助完善，不影响当前使用 |

---

*第①路审查完毕，请第②、③路复核。*
