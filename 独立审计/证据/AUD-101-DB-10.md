# AUD-101 文档审计台账 · 批次 DB-10（docs/design/ 与 docs/standards/、docs/ci/ 交界，22 份）

判据：标准 01《文档权威链整治标准》全文 + 标准 05 §5（门禁↔文档口径映射）+ AUD-101 任务书 + DISPATCH-DOC-PREAMBLE。
仓库基线 HEAD=`c8f64e9a`。本批只登记与举证，未执行任何门禁/构建/测试（前言 §1 禁令）。

## 0 开工基线（git status --porcelain 原文）

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 1 逐份登记

列序与列名按前言 §4 固定。行数 = `wc -l <文件>` 实测（与 `doc_mechanical.csv` 的 lines 列逐份相符）。
下游计数命令（每份替换 `<P>`）：`git -C "F:/Astro dev/Astro CS Normalization Database" grep -l -- "<P>"` 与 `... grep -l -- "<basename>"`，取并集、剔除自身与 `artifacts/**`。

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/design/LOG_AND_ERROR_SYSTEM.md | 日志与错误系统（详细设计） | 207 | 架构设计（详细设计）·实所在 `docs/design/` 与应属目录一致 | 日志/错误链的层次、落点、生命周期、判据 | ASTROCS_DESIGN.md §7.3、§10（:3 已标） | 16 份（ASTROCS_DESIGN.md、ENGINEERING_SPEC.md、ACCEPTANCE_SPEC.md、docs/contracts/LOG_AND_ERROR_CONTRACT.md、eng/tools/quality/check_log_system.py、eng/ci/ledgers/log_system_ledger.json 等） | 是（日志与错误详细设计唯一正本；字段级合同在合同层） | 与 docs/contracts/LOG_AND_ERROR_CONTRACT.md 是分层（设计↔合同），不重复；但 `docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md:5` 与 `:10-11`、`docs/standards/ERROR_HANDLING_STANDARD.md:10-11` 同主题另立口径（见 §2 组 G1） | 有：:3–:7 抬头除上游外还带"机器事实源"三行清单（含台账与检查器路径），属可下沉正文的登记面 | :3–:7 抬头块（上游之外的"机器事实源"登记属元信息）；其余正文干净（无日期/任务编号/历史叙事） | 无（本文件引用的 18 个路径与 8 个章节锚逐条实测存在） | 无与最高设计冲突；但**判据表只列 R1–R4（:180–:189），门禁实际有 R5**（见"与上位冲突"栏末注）→ 文档口径落后门禁 | 保留；§9 判据表补 R5 行（CONTRACT-ANCHOR），并把 :3–:7 的机器事实源清单移入 §9 |

> 注（LOG_AND_ERROR_SYSTEM 的门禁口径缺口）：`eng/tools/quality/check_log_system.py:13,21` 实装 R1–R5，`docs/ci/01_CHECKS.md:81` 的 `CHK-LOG-SYS` 条目亦写"R1…/R2…/R3…/R4…/**R5 台账落点默认值与设计+合同文档一致**"；本文件 §9 的判据表停在 R4。标准 05 §5 要求"每条门禁对应一条文档口径并登记映射"，此处映射少一档。

| docs/design/PHASE1_DETAILED_DESIGN.md | Phase1 目标态详细设计 | 227 | 架构设计（详细设计）·目录一致 | normalize 阶段的观测模型/节点序/产品/验收 | ASTROCS_DESIGN.md §4（:3 已标）；另自declare 上位 `docs/owner/PROJECT_SPEC.md`（:5） | 19 份（docs/plugins/algorithms_phase1/01…08、PRODUCT_STORAGE_FORM.md、docs/owner/PROJECT_SPEC.md、eng/ci/checks.json、eng/contracts/schemas/hips_storage_form.schema.json 等） | 疑似第二正本（与最高设计 §4 + 别批 `docs/science/*` 主题重叠，见举证） | ①ASTROCS_DESIGN.md:271 ↔ 本文件:44–46（节点序依据整段复述）；②`docs/science/DRIZZLE.md:21,159` ↔ 本文件:143（drizzle 归一分母两处口径相反）；③`docs/contracts/DATA_SEMANTICS.md` §31.1（本文件 :137 自称"唯一正本"）↔ 本文件 :139–147 又复述单位/量纲表 | 有：:5–:6 双"上位/下游"声明块（:6 并含"目标只由本文件定义"的自封唯一口径） | :5–:6（第二套权威声明 + 唯一口径声明，违标准 01 §1 与最高设计 §0.2）；:63 任务编号 `RULING-DOC-01` 与"裁决 B"引述；:80 审计编号 `P0-21`；:112 源码行号锚 `STAR_DETECTION.md:18-19` 实测错位 | :63 `run/RULING-DOC-01/REPORT.md` 不存在（`ls run` 无该目录；`git ls-files --error-unmatch run/RULING-DOC-01/REPORT.md` → `error: pathspec … did not match`）；:112 锚指向 `docs/science/STAR_DETECTION.md:18-19`，该两行实为召回阈表，冻结阈 `threshold = median(img) + 5.0·bgnoise` 实测在 :64–:65 | :143 "writer 归一 `Σ_j x_j·w_jp / Σ_j a_jp`"（分母 = 覆盖面积 `Σ a_jp`）与科学正本 `docs/science/DRIZZLE.md:159`「**将 `S_p` 的分母取覆盖面积 `D_p=Σ_j a_jp` 而非面亮度归一分母 `N_p`…为 DISP-DRZ-009 的负例控制**」直接对立；:34–46 复述最高设计 §4.2 权威内容 | 保留为阶段目标态正本候选，但：删除 :5–:6 的第二权威声明、:63 的 run/ 证据改指 `artifacts/evidence/`、:112 锚改 `:64-65`、:143 归一分母与 `docs/science/DRIZZLE.md` 对齐（科学疑义按 AGENTS.md §8 查证流程上呈，不自行改判） |

| docs/design/PHASE2_DETAILED_DESIGN.md | Phase2 目标态详细设计 | 145 | 架构设计（详细设计）·目录一致 | mosaic 的固定流程/UPM/排异/两类目标产品/权重产生链 | ASTROCS_DESIGN.md §5（:3 已标） | 16 份（docs/plugins/algorithms_phase2/09…13、PRODUCT_STORAGE_FORM.md、docs/owner/PROJECT_SPEC.md、eng/ci/ledgers/dead_config_keys.json、eng/tests/contracts/test_unified_object_contract.py、实验/absolute-snr/docs/EXP-05-ABSOLUTE-SNR.md 等） | 是（mosaic 详细设计正本；档界与算法名让位于 plugins 层） | ①最高设计 §5.5 ↔ 本文件 :54–:63（排异档界表被复制，而最高设计 :421 明文"唯一正本 = `docs/plugins/algorithms_phase2/12_rejection.md` §9，本节不复制档界与取值"）；②本文件 :21 ↔ ASTROCS_DESIGN.md:397（三条 SNR 重建口径与 `w=SNR²/F_ref²` 整段复述） | 无独立元信息块（:3 上游单行合规） | 正文无日期/任务编号/sha256；:54 小标题自述"最终五档表"与实际三档不符（属状态/结论陈述错误，非历史叙事） | 无（:11 `docs/contracts/DATA_SEMANTICS.md` §31.1a、:64 `docs/science/REJECTION.md`、:145 `ACCEPTANCE_SPEC.md` §6.2 实测存在且节号对得上） | :54「（最终**五档**表）」+ 三行表 ↔ 正本 `docs/plugins/algorithms_phase2/12_rejection.md` §9 明记"档位表由四档收成**三档**"；:56"几何覆盖帧数" ↔ 最高设计 :420 与正本用"几何**可贡献**帧数"（两名并存、无定义衔接） | 保留；§5.1 删去档界表只留指针（按最高设计 §5.5 的"不复制"要求），标题与 N 的定语随正本订正 |

| docs/design/PHASE3_DETAILED_DESIGN.md | Phase3 目标态详细设计 | 174 | 架构设计（详细设计）·目录一致 | export 的模式/WCS 计划/采样/裁剪/视觉验收判据 | ASTROCS_DESIGN.md §6（:3 已标）；§8 段内又另起一块上游（:79） | 19 份（docs/plugins/algorithms_phase3/14…16、docs/api/CLI_PROTOCOL_V1.md、docs/contracts/CONFIG_CONTRACT.md、eng/contracts/schemas/phase_config_export.schema.json、lib/algorithms/projection/p3_wcs.h、eng/tests/config/test_export_crop_contract.py 等） | 是（export 详细设计正本） | 与 `docs/plugins/algorithms_phase3/14_projection.md` 分层（设计↔模块工作细节）合法；§8 crop 与 §9 视觉判据在 `ACCEPTANCE_SPEC.md` §6.2 有同主题表述，本文件以指针引用、未复制门值 → 不判重复 | 有：:79 段中重复"上游/约束"抬头块（与 :3 同功能、不同范围） | 正文无日期/任务编号/sha256；:42「不能因现码存在就成为永恒目标」、:120「夹取不进入实现：把…」属论证性口语但仍是现行条款，不计违规 | :24 与 :15 把投影冻结清单与 signal 面亮度语义的上游记为 `ASTROCS_DESIGN.md §5.3` —— 实测 §5.3 是「SNR 重建与逆方差叠加」（ASTROCS_DESIGN.md:385），八投影冻结在 **§6.3**（:453），面亮度量纲硬约束在 **§5.6**（:430）→ 两处节号错位；:107 称"同名异义与 `docs/design/UNIFIED_MODEL.md` §3 冲突"，实测 UNIFIED_MODEL §3 只讲三类配置分离（:64–:76），不登记 `mode` 键位（真实占用面 = `eng/contracts/schemas/cpu_profile.schema.json` 的 `legacy_v1/kernel_v1`） | 同上：§2/§1 的上游节号指向与所引内容不同节；:70 验收把 TAN/SIN/CAR/AIT 四投影列为验收面，而 :24 自陈"当前仅 TAN"且最高设计 :453 同口径 —— 验收面与实现状态未标注分档（状态词按前言 §5 第 6 条属"未实现"，文档未点明） | 保留；:15/:24 上游节号改 §6.3/§5.6；:79 的重复抬头并入正文；:70 标注验收前置条件（实现后生效） |

<!-- PROGRESS: 已审 4/22 -->

## 2 举证（续）

### 2.1 docs/design/PRODUCT_STORAGE_FORM.md（256 行）

| 字段 | 判定 |
|---|---|
| 标题 | 产品落盘形态：裸 HiPS 与 zstd 归档包 |
| 角色 | 架构设计（详细设计）；实所在 `docs/design/` = 应属目录（标准 01 §2"架构设计"行）一致 |
| 主题 | HiPS 两形态（bare/archive）、两层索引、哈希口径、体积削减两机制、形态配置与清单登记 |
| 上游 | :3 已标 ASTROCS_DESIGN.md §10 + §4.4/§5/§6 + 附录 B；:5 又 declare 上位最高设计（重复声明） |
| 下游 | 22 份（含 ASTROCS_DESIGN.md、ENGINEERING_SPEC.md、ACCEPTANCE_SPEC.md、docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md、三个 PHASE* 设计、eng/ci/checks.json、4 份 schema） |
| 是否正本 | 是（落盘形态详细设计；字段词表让位 schema、合同细则让位 `HIPS_STORAGE_FORM_CONTRACT.md`，§10/:218 已显式让权） |
| 重复或重叠对象 | ①本文件 :42–:44 三阶段默认形态表 ↔ `ENGINEERING_SPEC.md` §7 条"形态配置与清单"（同口径复述，两者均称 Phase1 archive 默认 / Phase2 裸 / Phase3 裸 FITS）；②本文件 :230"形态是输入配置项而不是运行期开关" ↔ `docs/design/PHASE1_DETAILED_DESIGN.md:27` 同句复述。分层合法（规范↔目录规范），但三处并存已出现措辞漂移风险，无机器门断言 ENGINEERING_SPEC 与本文件一致（`CHK-HIPS-STORAGE-FORM --doc-consistency` 只声明覆盖 ENGINEERING_SPEC §7，见其 changed_paths 不含 docs/standards） |
| 元信息块 | 有：:3–:6（上游之外另立"上位/下游"两行清单，下游清单为自报且与实际引用面不完全一致） |
| 写法违规 | :46 与 :214 任务编号 + run/ 证据引用（`run/HIPS-PACK-01/REPORT.md` §3/§8）；:100 `EXP-02` 实验编号；:180「**包围盒 TRIM 纳入可选形态**：本节是它的设计落点」与「**9.1 的整产物百分比是一次性测量值，其原始计时/扫描记录不在当前工作区**（`run/` 受回收策略管理），故只作口径对照、不作门值」＝过程流水/留痕；:187「**关键区分（本次前提验证的核心结论）**」＝"本次"过程叙述；:184「**Windows 面未实测**」「行为面待实测」＝预写状态（最高设计 §0.2 要求状态由检查现场计算） |
| 悬空引用 | :46、:214 `run/HIPS-PACK-01/REPORT.md`：本机 `ls run` 输出无 `HIPS-PACK-01`，且 `git ls-files run/` 为空（run/ 由 .gitignore 排除）→ 正式设计文档引用不可再生的临时区证据；:180 `artifacts/evidence/compress-01/` 实测存在（不算悬空）。机械列漏报：`doc_mechanical.csv` 本行 `dangling_links=[]`，未判出上述两条 `run/**` 悬空（其口径只判 `](relative)` 型链接） |
| 与上位冲突 | 无实质冲突：§10.1 的 `storage_form`（archive 默认 / 留空取默认并 warn / Phase2·Phase3 出现即 REJECT）与 `ENGINEERING_SPEC.md` §7、`ASTROCS_DESIGN.md` §10 一致。**登记一项 eng 侧缺口**：`git grep -n "storage" eng/packaging/config/defaults.json` = 0 命中，`config_registry.json` 亦无 `storage_form` 行，而 ENGINEERING_SPEC §7 称"默认值来源 = defaults.json"、CFG002-01/02 要求旋钮逐行登记 → 默认值来源声明与登记面不符（缺陷属 ENGINEERING_SPEC/eng 面，非本文件） |
| 处置建议 | 保留（落盘形态主题唯一正本）；:46/:214 的 `run/**` 证据迁 `artifacts/evidence/` 并去掉 :180/:187 的过程叙述（标准 01 §4"无历史叙事、无任务编号"）；:3–:6 抬头只留上游一行 |

### 2.2 docs/design/UNIFIED_MODEL.md（76 行）

| 字段 | 判定 |
|---|---|
| 标题 | 统一观测模型与数据对象（细节文档） |
| 角色 | 架构设计（详细设计：数据对象与配置）；目录一致（最高设计 :3 与 §0.2 明示本文件承载"数据对象与配置"） |
| 主题 | 线性观测模型、13 个 canonical 数据对象及其可否作权重、三类配置分离 |
| 上游 | :3 已标 ASTROCS_DESIGN.md §3.1/§3.2（实测 §3.1 数据对象、§3.2 三类配置，号与题均对得上） |
| 下游 | 81 份（本批最高：README.md、ASTROCS_DESIGN.md、ENGINEERING_SPEC.md、docs/contracts/CONFIG_CONTRACT.md、DATA_SEMANTICS.md、UNIFIED_OBJECTS.md、docs/plugins/**、eng/packaging/config/config_registry.json 等） |
| 是否正本 | 是（数据对象唯一正本：最高设计 :223「全仓只有一份正本定义」、附录 A 与 ENGINEERING_SPEC §3 均指名本文件） |
| 重复或重叠对象 | ①:43/:45 的 `frame_snr`/`sparse_snr_layer` 口径长段 ↔ `ASTROCS_DESIGN.md:225–229`（"全程只有 SNR，没有权重模式…权重换算 w=SNR²/F_ref² 对两者一致"）——同一段科学口径在最高设计与本文件各写一遍，本文件 :45 还把算子选择规则让给 `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.5，形成"三层复述"；②:64–:76 三类配置 ↔ `docs/contracts/CONFIG_CONTRACT.md` §1「三类配置（现场清单）」；③:42/:43 的 `flux` 唯一口径句在 :42、:43 两行内**逐字重复两遍**（"全链只有一个 `flux` 口径 = PSF 拟合域…孔径仅作显式声明的诊断/交叉验证"），文件内自我重复 |
| 元信息块 | 有：标题后缀"（细节文档）"属层级自述（非状态编号，判轻）；:58 的 `> ` 引用块承载退役件说明 |
| 写法违规 | :58 整段＝退役件留痕叙述（"**不是现行对象**""旧产品若声明该对象 ⇒ 显式拒绝 + 迁移提示"），最高设计 §0.3 规定"已经退役的实现不出现在正式文档里"；:46「三者精度对比是论文核心实验（判据 SP-0）」＝过程/计划语域，且判据 ID `SP-0` 在本文件内不给出处指针（实际定义面 = `实验/absolute-snr/docs/frame-snr-canon.md`，`git grep -n "SP-0"` 命中该处与 `实验/absolute-snr/README.md:235`）——读者无法从本层沿索引到达判据正本（标准 01 §3 双向可追溯） |
| 悬空引用 | :43 引 PixInsight"功率比式[18] `(Σf)²/σ_n²`"——本文件内无参考文献表，编号不解析；其定义面 = `docs/science/PSF_SIGNAL_WEIGHT.md:43`，而该处**自标**「**未独立核验**：该串在 `02-PSF_Flux_Weighting_Algorithms.pidoc` 全文中不存在，应在 PCL 头文件侧，**需补一手锚点后才可引用**」⇒ 下层设计文档把一个被科学正本判为"不可引用"的编号当依据写进定义句（引用越权，非路径悬空）。:46 `实验/absolute-snr`、:58 `eng/contracts/data/unified_object_compatibility_map_v1.json#retired_entries`（该键实测存在且 `covers_object=psfsw_robust_weight`）、:45 `docs/contracts/UNIFIED_OBJECTS.md` §4b（实测 :103「## 4b. sparse_snr_layer 的重建声明面」）均可解析，**不判悬空** |
| 与上位冲突 | :58 退役件段落与最高设计 §0.3 冲突（退役件不应出现在正式文档层）；:56「canonical 数据对象 = 13 个」与最高设计 :223 的 13 项枚举逐字相符（不冲突） |
| 处置建议 | 保留（数据对象主题正本）；:58 迁出正式文档层（去向 `docs/contracts/UNIFIED_OBJECTS.md` 的兼容性表 + `artifacts/evidence/`），:42/:43 合并去重，:46 的 `SP-0`/论文语域改指针 |

## 4 docs/standards/ 与 docs/DEVELOPER_GUIDE.md · 逐份登记

系统性事实（一条命令可复算）：`git -C "…" grep -n "上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）" -- docs/standards` 命中 **12 份**（API/BENCHMARK/CODE/COMMENT/CONCURRENCY/C_ABI/DOCUMENTATION/ERROR_HANDLING/IO/LOGGING_DIAGNOSTICS/RELEASE/TEST）。而现行最高设计里 **§8.4 = 顶层结构**（ASTROCS_DESIGN.md:618）、**§8.5 = 模块与 ABI**（:661）；同目录 `NUMERIC_STANDARD.md:3` 引的正是 §8.5 ⇒ 这 12 份的上游**节号全部偏一位**（括注标题对、节号错）。历史核对：`for r in $(git log --format=%h -n 8 -- ASTROCS_DESIGN.md); do git show $r:ASTROCS_DESIGN.md | grep -c "### 8.4 模块与 ABI"; done` → 近 8 个修订全为 0，**不存在"§8.4 曾是模块与 ABI"的重编号解释**，即这 12 条上游标注自始是模板填错。
门禁侧：`eng/tools/doccheck/check_doc_index.py:356-357` 的 `has_upstream_header()` 只判同一行里出现"上游"+"ASTROCS_DESIGN.md"两个子串，**不校验节号是否存在、也不比对 `DOCUMENT_INDEX.yaml` 登记的 upstream 字符串** ⇒ 上述 12 处错号与 2 处索引/文档上游不一致对 `DOC-INDEX` 门完全不可见（标准 05 §2 判据判别力缺口）。

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/standards/API_STANDARD.md | ACSD Public API Standard | 23 | 标准与检查 ·目录一致 | 公共 API（C ABI 边界）注释与状态口径、API-* 契约 ID | :3 标 §8.4（模块与 ABI）→ 节号错，应 §8.5 | 4 份，且全部非生产文档层：`docs/DOCUMENT_INDEX.yaml`、`docs/audit/doc_classification.csv`、`reports/v19r2/file_audit_inventory.csv`、`reports/v19r2/source_manifest.csv` ⇒ 现行文档/代码零引用（近孤儿） | 否（主题与 `docs/contracts/PUBLIC_API.md`、`docs/interfaces/**`、`docs/api/CLI_PROTOCOL_V1.md` 同域） | ①:12"public raw pointer 必须注明 borrowed / owned / optional" ↔ `CODE_STANDARD.md:14`"公共指针必须声明所有权（borrowed / owned / optional）与空值语义"（同一句要求两处各写一遍，无互指）；②:11"失败时输出参数必须重置或文档化'未写入'" ↔ `C_ABI_STANDARD.md:6-7`"输出在失败时重置：`*out_model = nullptr` 语义由实现保证"；③:7-12 的 ABI 边界要求 ↔ `ASTROCS_DESIGN.md` §8.5（:663「跨 DLL 不传 STL/异常/RTTI…」）同一主题分层 | 无（:3 上游单行） | :22"接口冻结后（**W2/V15/V19** 冻结）改语义一律走显式变更"＝工作包/轮次代号历史叙事（标准 01 §4 禁任务编号与历史叙事） | :16 引用的 `docs/contracts/API_CONTRACTS.csv` 与 `docs/traceability/TRACEABILITY_MATRIX.json` 实测存在（`git ls-files` 命中），但 `git grep -n "API-AIO-001" docs/contracts/API_CONTRACTS.csv` = 0 命中 ⇒ 文中举的三个示例 ID（API-AIO-001 / API-P2-REJECT-001 / API-P2-UPM-001）在被指文件中不存在＝**举例与所引登记表对不上** | :20-23 的变更纪律只说"走显式变更"，未指 `ENGINEERING_SPEC.md` §3 的变更流程条款（权威链缺一段）；无与最高设计内容冲突 | 合并（并入 `docs/standards/C_ABI_STANDARD.md`＋`CODE_STANDARD.md` 的指针化，或降为 `docs/interfaces/` 的一节）；若保留则先补 §8.5 正确节号、删除 :22 代号叙事、把 :16-18 示例 ID 改为可解析行 |
| docs/standards/BENCHMARK_STANDARD.md | ACSD Benchmark Standard | 8 | 标准与检查 ·目录一致 | benchmark 运行档位与性能断言口径 | :3 标 §8.4 → 节号错，且主题（性能测量）上位应为 §9（CPU 后端与资源）/§12.4（L2 性能层） | 6 份（索引、`docs/audit/doc_classification.csv`、`lib/infrastructure/aio/tests/hiss_experiments.cpp`、`lib/infrastructure/aio/tests/results/performance_report.md`、2 份 reports/v19r2 台账） | 否 | 与 `docs/performance/`、`ACCEPTANCE_SPEC.md` §L2 与门 `L2-FROZEN-GATE-*` 的判据面同主题；本文件 :6"性能断言需 variance 报告（多次运行）" ↔ `docs/ci/01_CHECKS.md:115-118` 冻结四条判据（平均利用率/p50/达标占比/无低利用窗）——同一"性能结论怎么算"两处各写、无互指 | 无 | :2-8 全文无日期/编号；但整文件只有 4 条 bullet，无档位、无命令、无判据编号 ⇒ 不满足标准 01 §3"每份文档能追溯到最高设计一条要点 + 被下游索引"的可操作性 | :7"输出到 run/ 或 **reports/**"——`reports/` 不在 `ENGINEERING_SPEC.md` §7 仓库根固定条目白名单（§7 列 lib/eng/docs/testdata/实验/工程控制/artifacts/run，无 reports/），且 §9 规定"过期报告、归档目录…甄别清理" ⇒ 指一个未登记的根目录 | 与上位冲突：最高设计 §12.4 的 L2 判据与 `docs/ci/01_CHECKS.md:115`（`L2-FROZEN-GATE-SELFTEST`/`REPLAY`）已冻结四条判据与"采样独占资源"要求；本文件 :5"benchmark 只跑 Release"之外未登记这四条，亦无门禁映射（`git grep -n "BENCHMARK_STANDARD" eng/ci/checks.json` = 0） | 合并（并入 `docs/performance/` 或 `docs/ci/` 的门禁口径条）；若保留须把 :7 的 `reports/` 改为 `run/` 或 `artifacts/`，并对齐 L2 四条冻结判据 |
| docs/standards/CODE_STANDARD.md | ACSD Code Standard | 42 | 标准与检查 ·目录一致 | C++ 编码 MUST/SHOULD/MAY | :3 标 §8.4（错）+ :5-6 另declare"权威来源：`ENGINEERING_SPEC.md` §1/§4 + `ASTROCS_DESIGN.md` §10.2" ⇒ **同一文件两处上游声明** | 10 份（索引、audit 台账、`docs/standards/NUMERIC_STANDARD.md:131`、4 份 lib 源文件与 2 份测试 CMake） | 部分（编码规范主题与 `ENGINEERING_SPEC.md` §2 代码风格重叠） | ①:5-6/§MUST-1（:10-13）关于"Windows=MSVC v143 / Linux=GCC 或 Clang"↔ `ENGINEERING_SPEC.md:7`"C++17 双平台；MSVC v143（Windows）/ GCC 或 Clang（Linux）"——同一工具链冻结值在两份文档各写一遍（正本应是 ENGINEERING_SPEC §1，本文件称"实现级展开"却复述字面量）；②:16"异常边界 = C++ 侧；C ABI 失败时输出重置，单出口 cleanup/RAII" ↔ `C_ABI_STANDARD.md:5-8` 三条同义；③:27"science product 写盘：temp write → validate → atomic promote" ↔ `IO_STANDARD.md:7`同一条 | 有：:5-6"权威来源"块（与 :3 上游并存的第二抬头） | :12-13"MinGW64 的定位 = 本地开发/兼容性验证工具链"属正向条款（不判违规）；正文无日期/编号/sha256 | :5-6 与 :10-11 两次引用 `ASTROCS_DESIGN.md` **§10.2**——实测 §10（:684）为"I/O 与原子产品"且**无子节**，"Windows 官方工具链为 MSVC"实际在 **§11**（:709）⇒ 两处引用同一不存在节号 | :3 与 :5-6 并列两套上游声明＝标准 01 §1"任何文档不声明自己的第二套权威顺序"；工具链字面量双写（与 ENGINEERING_SPEC §1 无机器门比对，漂移无人判） | 保留（编码规范主题）；删 :5-6 第二抬头、§10.2→§11、工具链只指 ENGINEERING_SPEC §1 不复述字面量 |
| docs/standards/COMMENT_STANDARD.md | ACSD Comment Standard | 46 | 标准与检查 ·目录一致 | 注释必写面/清理面/长度 | :3 标 §8.4 → 节号错，主题上位应是 `ENGINEERING_SPEC.md` §2（代码风格与历史实现处置） | 4 份，全部为索引与历史台账（`docs/DOCUMENT_INDEX.yaml`、`docs/audit/doc_classification.csv`、`reports/v19r2/*`）⇒ 现行零引用（近孤儿） | 否（注释卫生主题的另一面在 `ENGINEERING_SPEC.md` §2 与门 `CON-COMMENTS`） | 与 `docs/standards/CODE_STANDARD.md` 同属"代码书写规范"一档，两份互不引用（CODE:41 关联列了 NUMERIC/C_ABI/CONCURRENCY/ERROR/IO，**未列 COMMENT**）⇒ 同主题拆两份且映射断裂 | 无 | :23-24 清理面枚举里出现"**第??号计划**"（占位符未填，条款不可执行）；:44-46 描述的检查器现状已变（见"与上位冲突"） | 无路径悬空；:23 的枚举是正则族非文件引用 | **门禁口径不一致（本批最典型的一条）**：:46"每个 production 文件记录 comment_hygiene = PASS/FAIL，由 `eng/ci/run_checks.py` 的**注释卫生检查项**产出并落 `run/ci/`"——注册表里没有任何名为注释卫生的门禁：`git grep -c check_comment_hygiene eng/ci/checks.json` = 0，其判据面由 `CHK-STALE-DOC` 的 step `CON-COMMENTS` 承接（`eng/tools/quality/check_comment_hygiene.py` 属 `docs/ci/01_CHECKS.md` §2.1 明记的"未注册、按退役契约处置"件）。文档承诺的产物（逐文件 comment_hygiene=PASS/FAIL 落 run/ci/）与注册表实态不符＝**超前门禁（文档有、注册表无）** | 合并（其"必写面/清理面"两条判据并入 `ENGINEERING_SPEC.md` §2，注释卫生门禁口径改指 `CON-COMMENTS`＋`CHK-RETIRED-CODE`）；:46 若保留须写实际门 ID |
| docs/standards/CONCURRENCY_STANDARD.md | ACSD Concurrency Standard | 26 | 标准与检查 ·目录一致 | 共享状态所有权、并行文档要素、线程数默认 | :3 标 §8.4 → 节号错，主题上位应是 §9（CPU 后端与资源） | 6 份（索引、audit 台账、`docs/standards/CODE_STANDARD.md:41`、`lib/algorithms/coverage/include/astro/phase2/sky_plane.h`、2 份 reports） | 否 | :15-16"每个并行模块文档必须写 parallel region/shared/thread-local/reduction/determinism/float accumulation order" ↔ `ASTROCS_DESIGN.md` §9（:672 浮点归约顺序冻结、worker 数来源）与 `docs/architecture/THREADING_MODEL.md`（本文件 :25 亦指向它）同一主题三处承载 | 无 | :26"ENG-THREAD-* 契约（**S2 注册**）"＝工作包代号历史叙事；:7-11 四条"全局设置与共享状态的所有权"条目**缺规范动词**（见下一栏）| 无路径悬空（:25 THREADING_MODEL.md 存在；:26 ENG-THREAD-001..003 在 `docs/TRACEABILITY.csv:12` 与 THREADING_MODEL.md:128 可解析） | ①**条款语义颠倒**：:7"库内随意修改全局 OpenMP setting（omp_set_num_threads 等）— 由 run context 统一控制"、:9"undocumented static mutable cache — 必须容量/身份/失效/线程模型"、:11"shared mutable scratch 无 ownership"——三条都没有"禁止/不得"，按字面读成允许项，标准 01 §4 要求硬约束用正向条款且可判定；②:21"线程数：外部可配置，**默认 min(可用核, 配置上限)**；取值来源 = 配置" ↔ 最高设计 §9（:672）"worker 数**只来自 profile 与预算对象**，模块不硬编码线程数"——默认来源两个说法；③"（16 不作硬编码值）"留下一个裸常数 16 而无推导 | 保留（并发规范主题）；补规范动词、:21 改为"profile + Runtime 预算对象"、:26 去 S2 代号 |
| docs/standards/C_ABI_STANDARD.md | ACSD C ABI Standard | 13 | 标准与检查 ·目录一致 | C 边界函数写法（异常/输出/句柄/字符串/整数/rc） | :3 标 §8.4 → 节号错，此处应 §8.5 | 9 份（索引、audit 台账、`docs/architecture/abi/ABI_003_SECURE_LOADER.md`、`docs/modules/orchestrator.md`、`docs/modules/photometric_calib.md`、`docs/standards/CODE_STANDARD.md`、`lib/algorithms/photometry/cpp/include/photometric_calib.h`、2 份 reports） | 否（与 API_STANDARD/CODE_STANDARD 三份同主题） | ①:6"输出在失败时重置：`*out_model = nullptr`" ↔ `API_STANDARD.md:11"失败时输出参数必须重置或文档化'未写入'"`；②:8"partial allocation 单出口 cleanup：RAII / delete 于失败路径" ↔ `CODE_STANDARD.md:16"异常边界 = C++ 侧；C ABI 失败时输出重置，单出口 cleanup/RAII"`；③:12-13"错误返回：0=success，非 0=hard error；recoverable science status 经输出状态字段表达" ↔ `CODE_STANDARD.md:17-18"稳定 success / recoverable science status / hard error 三层语义；三层语义各占其一（rc=0 + invalid status 即双语义违规）"` 与 `ERROR_HANDLING_STANDARD.md:7-11` —— **同一 rc/状态双层语义在 4 份文档各写一遍** | 无 | :5"（如 p2_upm_open）"＝以具体历史符号作示例（该符号仍在 lib 里，属可解析），但 :3-13 全文无一处指回 ABI 合同正本（`docs/architecture/abi/**`、`docs/contracts/PUBLIC_API.md`），标准 01 §3 双向索引缺下游方向 | 无路径悬空 | :13 的 rc 口径与 `docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5（域→退出码唯一表）与 `lib/include/astrocs/core/contracts.h:17-26` 的 `ErrorDomain` 未建立映射（该文件不提域，读者会把"非 0=hard error"当唯一收敛口径） | 合并（与 API_STANDARD 合为一份"C ABI 与公共 API 规范"，并把三层语义让给 ERROR_HANDLING_STANDARD，只留指针） |
| docs/standards/DOCUMENTATION_STANDARD.md | ACSD Documentation Standard | 36 | 标准与检查 ·目录一致（内容与最高设计 §0 同主题） | 文档集权威链、L0–L5 分层、各类文档必备要素 | :3 标 §8.4（模块与 ABI）——与其主题（文档规范）完全不相干，正确上位 = 最高设计 §0/§0.1–§0.3 | 5 份（`README.md`、索引、audit 台账、2 份 reports） | **否——与本批之外的整治标准同主题** | :5-9"权威链（`ASTROCS_DESIGN.md` §0，唯一）：ASTROCS_DESIGN.md → AGENTS.md → ENGINEERING_SPEC.md → CONTROL_PACK_SPEC.md → docs/ci/ → docs/plugins/；**另立** docs/science/（公式权威）、docs/algorithms/（推导权威）" ↔ `ASTROCS_DESIGN.md`:37-45「### 0.1 唯一权威链：1 本文档…2 详细设计…3 具体设计…4 代码」与 :55「**只有一条权威链**：每份文档的权威顺序由 §0.1 给出」——本文件把"链"重写成按文件的 6 段序列并把 science/algorithms 标为"另立"（不在链上），是**同主题的第二套口径** | 有：:5-9 权威链声明块 + :11-22 分层代码块（二者皆是元信息性质，非"现行设计要怎样"的条款） | :36"机器一致性：docs_machine_consistency.py 必须 PASS（**S8**）"＝任务/波次代号；:21"历史（非权威）docs/**/v6/**（V6 产品族冻结/设计档案，**仍在活动索引**）"＝历史叙事（标准 01 §4） | 无路径悬空（:13 提到的 `docs/owner/RELEASE_STATUS.md`、`docs/KNOWN_LIMITATIONS.md`、`memory.md`、:24 的 `eng/tools/doccheck/check_doc_index.py` 实测均存在；docs/validation/v6 等 6 份 v6 件存在，:21 断言可核） | ①与最高设计 §0.1 冲突（第二套权威顺序，见"重复"栏）；②:11-22 的 L0–L5 分层与 `ASTROCS_DESIGN.md` §0.2 的详细文档层枚举、与 `docs/DOCUMENT_INDEX.yaml`（唯一索引地图，标准 01 §3）**三处并存**，且本文件分层缺 `docs/design/` 除 UNIFIED_MODEL 外的其余 5 份（同目录文档在自己立的地图里没位置）；③:36 只写裸文件名 `docs_machine_consistency.py`（实为 `eng/tools/docs_machine_consistency.py`）且不写门 ID | 删除或彻底改写：文档规范主题的正本应是最高设计 §0 + 标准 01；本文件若保留，只留"各类文档必备要素"（:27-34）一节并改上游为 §0，权威链与分层两块删（标准 01 §1/§3） |
| docs/standards/ERROR_HANDLING_STANDARD.md | ACSD Error Handling Standard | 24 | 标准与检查 ·目录一致 | 三层错误语义 + 硬错误类别 + 模块必备项 | :3 标 §8.4 → 节号错（主题上位 = §7.3 与 §7.2） | 5 份（索引、audit 台账、`docs/standards/CODE_STANDARD.md:41`、2 份 reports） | 否（与 LOG_AND_ERROR 系列同主题） | :7-11 三层语义 ↔ `CODE_STANDARD.md:17-18`（同一三层式各写一遍）；:8-9 recoverable status 枚举 ↔ `docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5/§6；:15-18 违规判据 ↔ `docs/design/LOG_AND_ERROR_SYSTEM.md`:184-188 R1/R2 与 `ASTROCS_DESIGN.md`:521（"空 catch、忽略返回码、只写日志、降级为警告后继续四类形态都无错误码，属未传播"）——同一"四类吞错形态"三处承载 | 无 | 正文无日期/编号；:22"（P1.* / P2.*，见 …；**原引 `DIAGNOSTICS_STANDARD` 仓内不存在，已订正为实际文件名**）"＝"订正"类历史叙事（标准 01 §4 禁；`CHK-DOC-HYGIENE` 的 D1 判红面正是"订正/修订 + 流水"） | :19"（见 **findings.csv** F-V19R2-IO-001）"同类问题在 IO_STANDARD，本文件 :23 指 `docs/diagnostics/TROUBLESHOOTING.md` 实测存在 ✓ | **错误类别词表与权威域枚举不符**：:10-11 硬错误列为 `CONFIG / INPUT_CORRUPT / DEPENDENCY / NUMERIC / NO_DATA / RESOURCE / TIMEOUT / IO / SCIENCE_GATE / INTERNAL`；权威域 = `lib/include/astrocs/core/contracts.h:17-26` 的 `ErrorDomain{CONFIG, DATA, SCIENCE_PRECONDITION, IO, RESOURCE, BACKEND, CANCELLED, INTERNAL}`（8 值）＋ `docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5 的"域→码唯一表"。其中 `INPUT_CORRUPT`、`DEPENDENCY`、`SCIENCE_GATE` 在 `lib/**`+`eng/**` 零命中（`git grep -l -w INPUT_CORRUPT -- lib/ eng/` → 0），`DATA/SCIENCE_PRECONDITION/BACKEND/CANCELLED` 四个现行域在本文件的类别表里**缺席** ⇒ 按本文件写代码产出的 status 域落不进唯一映射表（未列域一律 70） | 合并（错误语义主题的正本 = `docs/design/LOG_AND_ERROR_SYSTEM.md` + `docs/contracts/LOG_AND_ERROR_CONTRACT.md`）；本文件只留"C 边界/模块必备项"并把域词表逐字对齐 contracts.h |
| docs/standards/IO_STANDARD.md | ACSD I/O Standard | 23 | 标准与检查 ·目录一致 | science product 写盘原子性 + 文档必写明面 + 只读约束 | :3 标 §8.4 → 节号错（主题上位 = §10 I/O 与原子产品） | 5 份（索引、audit 台账、`docs/standards/CODE_STANDARD.md:41`、2 份 reports） | 否（与 `docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md`、`docs/design/PRODUCT_STORAGE_FORM.md` §7 同主题） | :7"temp write → validate → atomic promote（同目录临时文件 + rename），失败清理 temp" ↔ `CODE_STANDARD.md:27`（同句）与 `docs/design/PRODUCT_STORAGE_FORM.md`:151-153（同一写路径的形态化表述）——三份并存、无互指 | 无 | :16-19 小节标题就是"**已知待改进（FINDING 挂账）**"，正文"当前为直接 trunc 写 + flush，未做 temp+rename：S6 修复或 backlog + rationale（见 findings.csv F-V19R2-IO-001）"＝挂账/流水（标准 01 §2：过程台账不放正式文档层；§4：无任务编号） | :19 `findings.csv` 不存在（跟踪集判据：`git ls-files | grep -i findings.csv` → 无输出；该 finding 实际登记在 `artifacts/evidence/v19r7-quality/audit_findings_io.md` 与 `docs/architecture/IO_AND_ATOMICITY.md`、`docs/modules/astro_image_io.md`，均非 .csv）⇒ 悬空文件引用 | **状态词档错（把曾经过失当现行）**：:18 声称 `aio_upm_write_sparse` 未做 temp+rename，实测 `lib/infrastructure/aio/src/aio_upm.cpp:86-108` 已是"写 `path.tmp` → 失败 `std::remove(tmp)` → `std::rename(tmp, path)`（Windows 覆盖语义另补一次）"＝已按本文件 :7 的条款实现；`lib/algorithms/coverage/src/upm.cpp:25` 注释亦写"（原子写 aio_upm_write_sparse，ENG-IO-001）"。文档比代码落后一个整改周期 | 删除 :16-19（挂账已闭合，结论应写进 `docs/architecture/IO_AND_ATOMICITY.md`）；本文件与 CODE_STANDARD 的 atomic promote 条款二选一保留并互指 |
| docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md | ACSD Logging & Diagnostics Standard | 11 | 标准与检查 ·目录一致 | 日志落点、stage 记录字段、诊断工具 | :3 标 §8.4 → 节号错（主题上位 = §7.3 运行日志） | 5 份（索引、audit 台账、`docs/standards/ERROR_HANDLING_STANDARD.md:22`、2 份 reports） | **否——与同域两份正本直接打架** | :5"日志统一写 `run/logs/<module>/<YYYYMMDD>/`，源码目录保持只读" ↔ ①`ASTROCS_DESIGN.md`:530-533「每次运行在块级 `output_dir` 下产出完整运行日志，日志目录默认 `<output_dir>/logs`（日志落点的唯一来源 = 块级 `output_dir`）」；②`docs/design/LOG_AND_ERROR_SYSTEM.md`:92-100「日志目录 `log_dir` 默认 `<output_dir>/logs`；判据红 = 进程 CWD 相对路径、源码树内目录、**`run/` 下的运行日志**、安装目录、用户家目录」——同一主题（运行日志落点）两份文档给出互斥取值，且其中一份被 P0 门判为红面 | 无 | :5 的 `<YYYYMMDD>` 目录分区是与"每次运行一份 `run_<run_id>.jsonl`"（设计 §4）不同的第二套命名口径 | 无路径悬空：:10 `eng/tools/astrocs_diagnose.py` 实测存在（`git ls-files` 命中） | ①:5 与最高设计 §7.3/设计文档 §4 **口径相反**（run/ 落点正是 `CHK-LOG-SYS` R3 的判红面）；②:6-8 的 stage 字段清单（stage_id/run_id/frame_id/config_hash/…）与设计文档 §3 的 `log_event` 字段面（run/task/node/module/phase/commit/host/level/event/units/elapsed/diagnostic）不同名同域，未指正本；③本文件不在 `CHK-LOG-SYS` 的文档一致面内：`eng/tools/quality/check_log_system.py:69-70,534` 的文档扫描面只含 `docs/design/LOG_AND_ERROR_SYSTEM.md` 与 `docs/contracts/LOG_AND_ERROR_CONTRACT.md` ⇒ 这条互相矛盾的落点口径**没有任何门禁读取**（门禁映射缺口） | 删除（内容并入 `docs/design/LOG_AND_ERROR_SYSTEM.md` §4/§8）——按标准 01 §2"同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除"；若暂留则必须先改成 `<output_dir>/logs` 再谈保留 |
| docs/standards/NUMERIC_STANDARD.md | ACSD Numeric Standard | 131 | 标准与检查 ·目录一致（但含大量应属科学/算法层的数值推导） | 科学量五件事、量纲与标度封闭词表、线性/面亮度标度律、NaN 处置、权重口径 | :3 标 §3.3（科学量与星表，**`:185`**）+ §8.5（模块与 ABI）——§8.5 号题相符（本批唯一），但行号锚错：`ASTROCS_DESIGN.md:185` 实测是 §2.2 的句子"并经 §12.3 的独立审稿"，"五件事"实际在 **:239**（§3.3 首行） | 20 份（`docs/contracts/DATA_SEMANTICS.md`、`DATA_ARTIFACTS.md`、`UNIFIED_OBJECTS.md`、`CONFIG_CONTRACT.md`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`、`docs/science/`{CALIBRATION,PHOTOMETRY,SCIENCE_SCOPE,UNCERTAINTY_AND_COVARIANCE}、`eng/tests/unit/p3_coverage_test.cpp`、`lib/algorithms/resample/**` 等） | 是（**标度词表**主题正本：`DATA_SEMANTICS.md:380,1400` 两处写"标度词表 = `docs/standards/NUMERIC_STANDARD.md`"） | ①:85-95 分母符号 `D_p`/`W_p` 逐分支表 ↔ `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md:139-149`（正本表，两处几乎逐字同文，且本文件 :110-111 自认"三处必须**逐字同口径**"而非指针化）；②:41-42 GUM 式(10) 一手证据 ↔ `docs/contracts/DATA_SEMANTICS.md:3139`（同引 JCGM 100:2008，两处各自展开）；③:59 的 `1/A_cell² = 4.306e21（21.63 dex）` ↔ `DATA_SEMANTICS.md:57` 同数（常数双写） | 有：:3 上游行内嵌被引句原文（把上位一句抄进抬头）；:15 又以"> 依据：…"重复同一条上游（一个文件两处上游声明） | :43 `run/SCI-FIX-SEMANTICS-01/evidence/scale_dimension_results.json`；:45 `run/RELEASE-05/vis/out/m42_p1_t3/p1_phot.json`（33 帧、α ∈ [1.1387e-17, 6.0083e-17]）；:67 `run/M42-VARIANCE-RCA-01/plane_fixed.f64` **并附 64 位 sha256 字面量**（`965e6fe5…46010c`）⇒ 任务编号 ×3 + sha256 + 一次性实验数据入规范正文（标准 01 §4：无 sha256/任务编号；实验过程数据应落实验单元）。机械列只报 `sha256=True`，**漏报了这三条 `run/**` 悬空与三个任务编号** | :43、:45、:67 三条 `run/**` 实测均不存在（`ls run` 输出：PROJECT-GOVERNANCE-01/ci/configs/knowledge-layer/logs/temp/test_cli_cwd/v6/win006 等，无 `SCI-FIX-SEMANTICS-01`/`RELEASE-05`/`M42-VARIANCE-RCA-01`；`run/` 整体 gitignore ⇒ 证据不可再生） | ①:108-109 称"与此相反的 `DISP-DRZ-004`「NaN 经 `F_p` 传播、不掩膜」为 **TRACKED/OPEN** 偏差，见 `STANDARDS_REGISTRY.md` D.drizzle 偏差表与 §3 索引"——同一注册表 :182 与 :270 实记 **CLOSED**，且其"唯一口径"就是本文件的样本级掩膜口径 ⇒ 状态词档错 + 把已闭合口径写成对立面；②:119-124 的上游节号错：§2.1（创新点一 测光）被当作"HiPS 只存 SNR/权重是派生量"与"阶段一、三不产权重"的依据（实为 §3.1:225），:121 把 `w=SNR²/F_ref²` 记为 §4.3/§4.4（实为 §5.3:397 与 §2.2:157）；③:110-111"如有分歧以 DATA-002 §2a 的 `invalid_handling` 块为准"＝给科学处置口径指定合同文档为仲裁者，与最高设计 §0.1「科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`」冲突（第二套权威顺序） | 保留（标度词表正本），但：:41-49/:51-71 的证据与数值推导块迁 `docs/science/UNCERTAINTY_AND_COVARIANCE.md`（该层已把本文件当依据：`docs/science/SCIENCE_SCOPE.md:36` 写"一手证据、合成实验与真实数据推导见 `docs/standards/NUMERIC_STANDARD.md`"——**这是一处需要负责人裁决的角色分工冲突**：标准 01 §2 表把"公式与推导"划给 science/algorithms，本仓现状把它放在 standards）；:85-95 改为指针（正本 DATA-002 §2a）；:108-109 的状态词按注册表订正；行号/节号锚全部重定位 |
| docs/standards/RELEASE_STANDARD.md | ACSD Release Standard | 16 | 标准与检查 ·目录一致（主题一半属负责人面） | 版本单源、交付包命名与内容、发布门字面量 | :3 标 §8.4 → 节号错（主题上位 = §13 版本与发布权、§11 双平台发行） | 4 份：索引、audit 台账、2 份 reports ⇒ 现行文档/代码零引用（近孤儿） | 否 | :5"版本唯一源 = 根 `VERSION`（`docs/owner/RELEASE_STATUS.md` §2）" ↔ `ASTROCS_DESIGN.md`:808-810（§13 同一条款，正本明指 RELEASE_STATUS §2）——本文件以指针写法（合规）；但 :8-11 交付包结构与 :12-15 门字面量与 `docs/owner/RELEASE_STATUS.md`、`ACCEPTANCE_SPEC.md` 的发布门清单同域双写 | 无 | :12-15 的 `PRE_RELEASE_ENGINEERING_FOUNDATION=PASS`／`FINAL_REAL_DATA_VALIDATION=PENDING` 是**状态字面量预写**（最高设计 §0.2/§12.5：状态由检查现场计算，登记表不预写状态）；:8 交付包名 `AstroCS_Review_<主题>_<YYYYMMDD>.zip` 用旧项目名（改名未贯穿，现行显示名 = ACSD） | 无路径悬空 | **门禁映射脱钩（超前）**：:12-15 规定的门字面量在注册表里不存在（`git grep -ln PRE_RELEASE_ENGINEERING_FOUNDATION` 命中 = 本文件、`docs/TRACEABILITY.csv`、`eng/tools/quality/build_v19r2_package.py`、`build_v19r3_package.py`；`eng/ci/checks.json` 0 命中），即"五个 AUTHORITATIVE_* 全 PASS 才算 PASS"的门既不在 `eng/ci/checks.json`、也不在 `docs/ci/01_CHECKS.md`，只活在两份退役打包脚本与一张追溯表里；现行发布门 = `ACCEPTANCE_SPEC.md` 四层验收 + `CHK-PACKAGE`/`CHK-PKG-CONSISTENCY`/`WIN-CANDIDATE-VALIDATE`，本文件与之无任何指针 | 合并（并入 `docs/owner/RELEASE_STATUS.md` 或 `ACCEPTANCE_SPEC.md` 的发布门条）；删除 :12-15 的字面量门或改为指向实际门 ID |
| docs/standards/STANDARDS_REGISTRY.md | 国际标准冻结注册表（STANDARDS_REGISTRY） | 404 | 标准与检查 ·目录一致 | 六域（投影/HiPS/HEALPix/Drizzle/星表/FITS）外部标准版本与条款冻结、符合状态与偏差指针 | :3–:6 自标 `ASTROCS_DESIGN.md` §5.3（**冻结八投影**）/附录 B + `ENGINEERING_SPEC.md` §8；`docs/DOCUMENT_INDEX.yaml` 登记的 upstream 是 §8.4（模块与 ABI）⇒ **文档自标与索引登记两条不同**（标准 01 §3：索引登记含上游） | 14 份（`docs/standards/NUMERIC_STANDARD.md`、`docs/interfaces/data/DATA-002_…`、`docs/owner/RELEASE_STATUS.md`、`docs/standards/checks/check_standards_registry.py`、`eng/tests/unit/p1wcs/{CMakeLists.txt,p1wcs_std_f1_bridge_cross.py}`、`lib/algorithms/projection/**`、`eng/tools/check_version_consistency.py` 等） | 是（外部标准符合性登记主题唯一正本，:12 自称"仓库唯一"） | ①:169/:187 与 :268 三处 drizzle 归一化式（同文件内部）；②:45/:156 域表 `CLAUSES` 与 :51-56 域节字段、:305-313 附录对应表——同一"域↔条款"映射在本文件内**写三遍**（§2 域表、§D.* 七字段、§4 附录对应表），其中两处含公式；③:160-164 与 :182 与 :270 三处复述 DISP-DRZ-004 的"唯一口径 = `NAN-SAMPLE-MASK-COVERAGE-NAN`"，另两处在 `docs/standards/NUMERIC_STANDARD.md:80-81` 与 `docs/interfaces/data/DATA-002…§2a`（正本）——同一 rule_id 四处承载 | 有：:3-:6 抬头四行（上游 + 机器事实源 + 登记状态 `status = ACTIVE_NORMATIVE`）——**状态编号写进文档开头**（标准 01 §4 明禁"文档 ID、状态编号、版本锚"元信息块；本仓状态正本 = `docs/DOCUMENT_INDEX.yaml`） | :75/:277/:298 的 `STD-F1`"实测 astropy 交叉 5.7e-14 deg、九宫格 18 格无 1px 偏移、负向注入必败"＝实验过程数据入规范；:100/:111-121 偏差表逐条含"（写路径无互斥包装）"等实现现状流水（可接受但应指 modules 层）；:138-141 引 `2016-07-22`/`PR-HiPS-1.0-20161122` 属外部标准版本标识（**机械列把 `20161122`/`1000000` 记为 commit_sha 与 dates 均为误报**，其中 `1000000` 出现在 :100 的 `hips_estsize="1000000"`） | 无外部路径悬空（§D.* 七字段的 EVIDENCE 与偏差表指针共 44 条路径逐条实测存在：见 §5.4 核对表） | ①**上游节号错两处**：§5.3 实为"SNR 重建与逆方差叠加"（ASTROCS_DESIGN.md:385），八投影冻结在 §6.3:453（原文"首批冻结 TAN / SIN / CAR / AIT / STG / MOL / CEA / ZEA…当前仅 TAN 可用"），附录 B 在 :823；②:66 记"Paper II §5 Table 1（TAN/SIN/CAR/AIT **四投影**）… CONFORMANT…registry v1 恰四行"，与最高设计 §6.3 的**八投影冻结集**及"当前仅 TAN"不符（四/八与实现档未登记为偏差）；③:336 与 :3 的 `ENGINEERING_SPEC §8` 指向"锚存活/fail-closed"——实测该规则在 **§10**（机器一致性检查，ENGINEERING_SPEC.md:176-188），§8 是"文档集：自解释与层级索引"；④:33 引"`SCIENCE_CORRECTNESS.md` 变更流程"**不给路径**且跟踪集无此件（`git ls-files | grep -i SCIENCE_CORRECT` → 无输出；该件曾入库于 `工程控制/PROJECT-GOVERNANCE-01/SCIENCE_CORRECTNESS.md`，`git log --diff-filter=D --name-only -- "*SCIENCE_CORRECTNESS*"` 显示在 `ae0f1e63` 删除）⇒ **悬空引用（指向已删除的治理件）**；⑤:282"跨域治理类偏差使用 **05 号 findings 登记册**的 `STD-F<n>` ID"与 :293-298"本表（§3.2）是这些 ID 的**唯一事实源**"自我矛盾，且该登记册已删除（`check_standards_registry.py:68-71` 注释：随旧控制包世代清理、`EXTERNAL_FINDINGS_REQUIRED = False`）；⑥**同文件科学口径两处相反且判据必红**：§3 偏差索引 :267/:268 的条款串"§3（线性重建 w_jp = a_jp / **A_pixel** 与面亮度语义）"在本文件 drizzle 清单里不存在（清单 :169 是"… / **A_drop** …"）——按其自身检查器 `C7_deviation_index_rows_resolve`（checker :513-515 做字符串相等）复算 ⇒ 这 2 行"域 drizzle 清单无条款"必判红；同时 :268 写"面亮度保持口径 `w=a/A_pixel`"而 :187 写"CLOSED：归一为 drop 面积归一核 `w=a/A_drop`"，科学正本 `docs/science/DRIZZLE.md:158` 把前者列为**禁止写法**（"把核权重写回 `a_jp/A_pixel,j` ⇒ FZ-COND-FLUX-CONSERV 判红"）⇒ 注册表把已被否定的旧口径当现行结论登记（前言 §5 第 6 条：状态/口径串档即缺陷），且 :14"本文件不复制公式"与自身四处公式复述冲突 | 保留（外部标准登记主题唯一，判据面不可废）；必改：抬头状态行删除、§5.3→§6.3、`ENGINEERING_SPEC §8`→§10、:33 的 SCIENCE_CORRECTNESS 与 :282 的 05 号登记册两处悬空改指 §3.2、:267/:268 条款串与 `w=` 分母按 `docs/science/DRIZZLE.md` 归一、四/八投影差异补偏差行 |
| docs/standards/checks/check_standards_registry.py | （无 H1；首行是编码声明） | 702 | 标准与检查（**可执行件**）——实所在 `docs/standards/checks/`，应属 `eng/ci/`（AGENTS.md §7：机器门注册表与检查器在 eng/ci/，工具与质量检查器在 eng/tools/）⇒ 角色与落位不一致 | STD-REG 门禁检查器：C1–C9 + C5′ 判据、9 个 fault-inject 场景、锚存活 fail-closed | 无上游标注（`DOC-INDEX` 的 `has_upstream_header` 不适用于 .py；索引却给它登记了 `upstream: ASTROCS_DESIGN.md §8.4（模块与 ABI）`——号与题均与本文件无关） | 8 份（`eng/ci/checks.json`（STD-REG 三步 command）、`eng/ci/polarity_evidence.json`、`eng/ci/polarity_probe.py`、`docs/standards/STANDARDS_REGISTRY.md`、`docs/owner/RELEASE_STATUS.md`、索引、2 份 reports） | 是（STD-REG 判据实现唯一件） | 与 `docs/standards/STANDARDS_REGISTRY.md` §5（机器检查合同表）互为"文档口径↔门禁"两端：:106-113 的列头/词表常量与:120-133 的 `GOVERNANCE_ROW_STD_F6` 字面量是对注册表的**第二处承载**（冻结串双写） | 不适用（代码件） | 注释违反同目录 `COMMENT_STANDARD.md:23-24` 自定的清理面：:68-70「原 05 号 findings 登记册随旧控制包世代清理（ROOT-007/ARCH-001/RETIRE-001）删除」、:126「2026-09-16 由 git 历史逐字取证迁移」、:177「2026-09-21 根目录整合：tests/tools/ci/contracts → eng/**」、:615-620「W4-A3 订正：原锚串漏了 DISP-WCS-001…⇒ str.replace 命中 0 次 = **注入空转**（实测…）」＝日期 + 工作项编号 + "订正/原/实测"历史叙事，逐条命中其禁列枚举 | 代码内锚：`EXTERNAL_FINDINGS_REL`（:66-71）指向 `工程控制/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909/05_FINDINGS_REGISTER_20260911.md`——该路径不存在（`ls 工程控制` → RELEASE-05、evidence），文件按 `EXTERNAL_FINDINGS_REQUIRED=False` 走"缺席（可选来源）"分支，**不判 ANCHOR_STALE**：即注册表 §5 承诺的"绝不静默返回空集"由一个显式登记项兑现（可接受），但 :282 的文档口径仍把它当 ID 来源 | **注入锚与注册表现文失配 ⇒ 场景失效**：:120-127 的 `GOVERNANCE_ROW_STD_F6` 逐字串在注册表里不存在（`grep` 实测 §3.2 行已是另一写法），`inject()`（:637）按 `re.escape(GOVERNANCE_ROW_STD_F6 + "\n")` 经 `_replace_once` 命中 0 次 ⇒ 抛 `InjectedNoOp` **exit 3**；而 `STANDARDS_REGISTRY.md:358` 声明"前 8 场景恒退出 0（注入协议：判定看 verdict）"⇒ 文档口径与实际行为不符。该场景（`drop-governance-deviation`）在 `eng/ci/checks.json` 里**未注册**（注册表只有 `STD-REG` / `STD-REG-FI-DANGLING` / `STD-REG-FI-VERSION-DRIFT` 三步，已实测），故门不会抓到它 | 迁移（→ `eng/tools/doccheck/` 或 `eng/ci/`，并同步 `eng/ci/checks.json` 的 STD-REG command 与注册表 §5 的复现命令）；迁移属门禁改造、需负责人批准；不迁移则至少：同步 `GOVERNANCE_ROW_STD_F6` 注入锚与注册表 §3.2 现文，并清理注释里的日期/工作项编号 |
| docs/DEVELOPER_GUIDE.md | Astro Celestial Sphere Database（ACSD） 开发者指南 | 41 | 操作与运营 ·**实所在 `docs/`（根），应属 `docs/development/`**（标准 01 §2 表"操作与运营"行）⇒ 角色错放，应迁往 `docs/development/DEVELOPER_GUIDE.md` | 构建/测试/机器门的标准动作与编码规范速览 | **两块上游**：:3 `ASTROCS_DESIGN.md §8（软件架构）`；:5-7 又一块"上游：`AGENTS.md` §3、`ENGINEERING_SPEC.md` §1/§6、`ASTROCS_DESIGN.md` §12（Alpha 前无版本信息）" | 6 份（`README.md`、索引、audit 台账、`docs/standards/DOCUMENTATION_STANDARD.md:14`、2 份 reports） | 否（与 `AGENTS.md` §3 与 `docs/ci/CI_SPEC.md` §5 的"本地运行方式"重叠） | ①:11-14 构建命令 ↔ `AGENTS.md` §3 同一段命令（`cmake -S . -B build -G Ninja …` / `ninja -C build` / `ctest --test-dir build --output-on-failure` / `python3 eng/ci/run_checks.py`）；②:35-38 编码纪律 ↔ `ENGINEERING_SPEC.md` §6（只 main 原子提交、一 commit 一目的）与 `AGENTS.md` §4/§6；两处均为复述而非指针 | 有：:3 与 :5-7 双上游块 | :27-31 "模块级科学矩阵"里的 `SNR-001..015`、`PR-UPM-001..010`、"phase2_synthetic_gate（**82 项**）"＝契约 ID 枚举 + 未标来源的计数断言（无机器门复算该 82；`git grep -n "82 项"` 仅此一处） | :41"追溯：`docs/traceability/TRACEABILITY_MATRIX.json`（机器真相）"——该件确有在册检验面（`CHK-CONTRACT-TEST` 的 step `TRACEABILITY-MATRIX` = `eng/tools/traceability/check_traceability_matrix.py`、step `UT-TRACEABILITY` = `eng/tests/traceability`），故"机器真相"成立，但本文件**不写门 ID**，读者无法从指南走到判据；另 `eng/tools/check_traceability_matrix.py`（重名旧副本）在 `docs/ci/01_CHECKS.md` §2.1.1 被列为"应退役"，本文件未区分两件 ⇒ 同题两份检查器而指南只点名"机器真相"不给口径。:16 的 `docs/ci/CI_SPEC.md`、`docs/architecture/MODULE_MAP.md` 与 :27-31 的 5 个测试目标名（`git grep -l -w <目标> -- eng lib CMakeLists.txt` 命中 3–13 份）实测存在 ⇒ 本文件**无路径悬空** | ①:5-7 把版本条款记为 `ASTROCS_DESIGN.md §12`——"Alpha 前无版本信息"实际在 **§13**（§12 是"验证体系"，:482 的 `--version` 行亦自指"见 §13"）⇒ 节号错；②:35"仅修改 `lib/` 源码"与 `ENGINEERING_SPEC.md` §7 目录规范（eng/tests、docs、eng/tools 皆为常规改动面）冲突；③:39"日志统一写 `run/logs/<module>/<YYYYMMDD>/`"与最高设计 §7.3/`docs/design/LOG_AND_ERROR_SYSTEM.md` §4 的运行日志落点（`<output_dir>/logs`）冲突（与 LOGGING_DIAGNOSTICS_STANDARD 同型错误，二者互为复述）；④:28 `variance_propagation_test`、:27 `noise_model_science_test` 等目标名实测在 `eng/**`+`lib/**` 有引用（`git grep -l -w` 命中 3–13 份），不判悬空，但"矩阵"与 `docs/contracts/TEST_MATRIX.md` 正本无指针关系 | 迁移（`docs/development/DEVELOPER_GUIDE.md`）+ 去重（构建/提交段改指针到 AGENTS.md §3 / ENGINEERING_SPEC §6），并修正 §12→§13 与日志落点 |

<!-- PROGRESS: 已审 20/22 -->
EOF_MARKER

## 3 逐份登记 · docs/standards/** 与 docs/DEVELOPER_G
</think>

<tool_call>
