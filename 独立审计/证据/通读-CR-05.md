# 通读 CR-05 · lib/algorithms/photometry 第 5 段（6 文件）

- 被审基线 HEAD：`c8f64e9a`
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 派单：`DISPATCH-CODE-READ-PREAMBLE.md` §1／§2／§2.5／§3／§4／§5
- 对应工作包：AUD-402（逐数值处置标注）

## 开工 status --porcelain 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（同批 `git rev-parse --short HEAD` => `c8f64e9a`，与派单钉定的基线一致。）

## 文件清单与阅读状态

1. `lib/algorithms/photometry/cpp/src/pc_api_qf.h` —— 已读：是（表 3 项）
2. `lib/algorithms/photometry/wrapper_phase1/photometer.cpp` —— 已读：是（表 14 项）
3. `lib/algorithms/photometry/wrapper_phase1/photometer.h` —— 已读：是（表 7 项）
4. `lib/algorithms/photometry/cpp/include/photometric_calib.h` —— 已读：是（表 21 项）
5. `lib/algorithms/photometry/cpp/src/psfsw.cpp` —— 已读：是（表 26 项）
6. `lib/algorithms/photometry/include/astrocs/v6/psfsw.h` —— 已读：是（表 31 项）

<!-- PROGRESS: 6/6 -->

## 文件 1 · `lib/algorithms/photometry/cpp/src/pc_api_qf.h`（50 行，整读）

性质：单个 C++ 链接期入口的**声明**（非 `extern "C"`、无 `PC_API` 宏）＋ 背景注释。定义在 `pc_api.cpp:1294`，生产调用点 `lib/algorithms/photometry/cpp/src/frame_photometry_fit.cpp:217`（`astrocs_phase1_photcal` 静态库，根 `CMakeLists.txt:766-773`），由 `module_adapters.cpp:119` 引入头。声明本身无数值字面量。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| pc_api_qf.h:7 | 「冻结的 6 个 PC_API 导出」 | 6 | 注释里的 ABI 面基数断言 | 结构性不适用 | 计数断言，不是科学量；须与 `photometric_calib.h` 的 `PC_API` 导出数核对（本批文件 4 已核：6 个 `pc_calibrate_simple*` 导出与 `docs/contracts/PUBLIC_API.md`、`TRACEABILITY_MATRIX.csv` SRC-PHOT-001 的六个符号名逐一对应） | —— | CONFIRMED |
| pc_api_qf.h:17/:34 | `quality_flags` 长度约定「= n_psf，按 PSF 行对齐」 | n_psf | 缓冲区长度合同（与被匹配 PSF 表同长） | 结构性不适用 | 数组长度关系，由调用方与 `cleanAndScale` 的下标映射共同保证；非科学量 | —— | PARTIAL（未逐行核 `pc_api.cpp`/`star_matcher.cpp` 的下标映射是否真按 PSF 行而非匹配序号取值；`star_matcher.cpp:424-431` 用 `(*match_indices)[i]` 转 row 再索引 `quality_flags[row]`，与本句一致，故本项成立） |
| pc_api_qf.h:33/:42 | `radius_deg`/`mag_min`/`mag_max`/`sip_order` 形参 | 无默认值 | 纯透传形参，本头不携带任何默认 | 结构性不适用 | 本文件零字面量；取值权威在调用方与 `config_registry.json`，不属本文件 | —— | CONFIRMED |

**本文件数值项合计 3 项，零科学常数、零阈值、零默认配置值。**

### findings

### CR-05-1 · 头注释自认"6 个冻结 C ABI 导出在生产路径上不带质量位"，但没有任何登记面承认这条 ABI 通道仍会放行饱和星
- 轴：C（主）／S
- 位点：`pc_api_qf.h:8-10` —— `pc_api.cpp 内部一律以 quality_flags=nullptr 调 cleanAndScale ⇒ star_matcher.cpp:428-438 的 SCI-PHOT-001 §4/§10 饱和/质量位有效域过滤（PC_QF_SATURATED|PC_QF_HAS_SATURATED）在**生产路径上不生效**`
- 上位依据：`docs/science/PHOTOMETRY.md` §4 输入有效域（原文）：「饱和星（`psf_status!=0` 或 `qf & (SATURATED|HAS_SATURATED)!=0`）不进入匹配与定标，计 `rejected_quality`」；§10 不可接受变化：「忽略饱和/质量标志使饱和星进入定标」。`docs/algorithms/PHOTOMETRIC_FIT.md:158` 写为无条件事实：「质量/饱和位过滤 | `PC_QF_SATURATED` 不入定标 | :415-443」。检索式 `git grep -n "quality_flags=nullptr\|不带逐星质量位\|饱和位" -- docs reports lib/infrastructure` 只命中 `PHOTOMETRIC_FIT.md:158` 与 `module_adapters.cpp:5609`，即 **ALG/登记面零提及"哪条通道不生效"**。
- 现状：本头以注释形式承认「冻结 6 导出⇒nullptr⇒饱和门失效」，并以新增 C++ 入口在 `frame_photometry_fit` 一侧补门；`pc_api.cpp:1229`、`:1275` 两个 v2 封装仍写死 `/*quality_flags=*/nullptr`；`lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp:2761`、`:2837` 仍按函数名 `pc_calibrate_simple_with_gaia_f64_v2`/`_v2` 从 DLL 取这两个封装并调用（`git grep -n "pc_calibrate_simple_with_gaia_f64_v2" -- .../orchestrator.cpp` 命中 4 处）。
- 差在哪：同一份科学条文（§4/§10）在两条实现通道上给出两种实际语义，而 ALG 文档以无条件句表述该门；`DISP-PHOT-007` 只登记「双通道四调用＋descriptor 占位 ID 双轨并存」，未登记"其中一条通道的饱和门整体失效"这一后果。
- 后果：任何按 `PUBLIC_API.md` 冻结 ABI 取用 6 导出的消费方（含仍在构建图内的 `astrocs_infra_orchestrator`，根 `CMakeLists.txt:485`）得到的 `scale_factor`/`sigma_residual`/`photo_stats` 含饱和星 ⇒ 零点被饱和星拉偏、`sigma_residual` 虚高，直接冲对外主张「测光校准到星等坐标系」的一致集纯度。
- 定级：**S2**，并注明**缺生产运行时触发路径**：`git grep -n "photometric_calib" -- CMakeLists.txt eng/cmake` 只有 `CMakeLists.txt:760` 的说明「photometric_calib.dll 未编入根构建」，且 `run_stage_photometric` 在 `lib/infrastructure/cli`、`lib/infrastructure/scheduler/src` 内零调用点（检索式见上），故该通道当前只在 MinGW/Makefile 手工构建 DLL 时才可达。
- 怎么算修好：三处同批改，否则改对反而判红——① `docs/algorithms/PHOTOMETRIC_FIT.md:158` 该行加适用域限定（"仅经 `_qf` 入口"）或在 §13.3 新增一条 DISP 登记 nullptr 通道；② 若判定 orchestrator 通道为死代码，则删注册或显式标 retired，并同步 `TRACEABILITY_MATRIX.csv` 的 SRC-PHOT-001 符号面；③ 若判定为活代码，orchestrator 改为链接 `astrocs_phase1_photcal` 走 `_qf`，且必须同时补 `p1_qf_oracle.cpp` 已建立的"红/绿"锁（丢成 nullptr 必红）。
- 置信：CONFIRMED（注释、`pc_api.cpp:1229/:1275`、`star_matcher.cpp:428-437`、orchestrator 解析名四处逐字读过；未实跑）

---

## 文件 2 · `lib/algorithms/photometry/wrapper_phase1/photometer.cpp`（100 行，整读）

性质：`astrocs::phase1::Photometer` 孔径测光实现。登记身份：`docs/algorithms/PHOTOMETRIC_FIT.md` §13.5 与 `lib/algorithms/photometry/README.md` §9 均记为「aperture 测光**待迁移符号**／非生产符号，未接入 orchestrator 管线（grep 实测无生产调用方）」，缺陷 `DISP-PHOT-008`。本批检索复核：`git grep -n "Photometer|photometer"` 的命中面为 `CMakeLists.txt:751`（静态库 `astrocs_phase1_phot`）、`eng/tests/**`（`p1_wcs_phot_test.cpp`、`p1001_real_nodes_test.cpp:518`、`eng/tests/backend/test_p1002_gaps.py`），`lib/infrastructure/scheduler/src/module_adapters.cpp` 与 orchestrator **零命中** ⇒ 与登记一致，本文件的 finding 一律封顶 S3（无生产触发路径）。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|---|
| photometer.cpp:19 | `w <= 0 \|\| h <= 0` | 0 | 图像维度合法性下界 | 结构性不适用 | 判空的自然边界，无量纲 | —— | CONFIRMED |
| photometer.cpp:26 | `cx < 0 \|\| cx >= w`、`cy < 0 \|\| cy >= h` | 0 / `w`/`h` | 中心越界显式失败判据 | 结构性不适用 | 域边界，非科学量；对应 `README.md` §6 的 `"center out of bounds"` | —— | CONFIRMED |
| photometer.cpp:38-41, 63-66 | 外接矩形外扩余量 | `±1` px | 把扫描域收到 annulus/aperture 的外接矩形 | 结构性不适用 | 遍历优化的安全余量（`\|y-cy\|<=R ⇒ y>=floor(cy-R)-1` 恒成立），不参与科学取值；`- 1`/`+ 1` 与 `h - 1`/`w - 1` 为整数边界 | —— | CONFIRMED（"逐位一致"主张我按包含关系＋行主序不变独立复推成立，未实跑对拍） |
| photometer.cpp:43 | `sky_vals.reserve(1024)` | 1024 | 环形像素 vector 的容量预分配 | 结构性不适用 | 纯分配提示，不影响取值 | —— | CONFIRMED |
| photometer.cpp:48 | `d >= sky_inner_ && d <= sky_outer_` | 6.0–10.0（来自 .h 默认） | 天空环内/外半径 [px] | **待确认** | 无科学锚。`docs/plugins/algorithms_phase1/06_photometry.md:77` 该键 `declared_default: null`、`registration: none`、`finding: "unregistered"`（`eng/packaging/config/config_registry.json:582-591`） | 环变宽（如 8–12px）⇒ n_sky 变大、`background`/`σ_sky` 更稳但混入邻近源概率上升；对 flux 的方向影响 = 环内若有邻近源则背景偏高⇒flux 偏低。影响范围仅本符号与其 4 组单测 | CONFIRMED（键面确认为未登记） |
| photometer.cpp:58 | `sky_vals[sky_vals.size()/2]` | 偶数取**上中位** | 背景 = 环内像素中位数 [ADU/px] | **待确认** | 定义式 `median` 见 `docs/algorithms/NOISE_ESTIMATION.md:20`（`σ_bg = 1.482602218505602·median(\|x−median(x)\|)`），但**偶数样本的中位约定未在该条文中定义**；仓内另一实现 `lib/algorithms/noise_snr/cpp/src/noise_model.cpp:100-108` `robust_median` 取**偶数均值** | 上中位 ≥ 均值 ⇒ 背景偏高 ⇒ flux 偏低（偏暗）、`σ_sky` 偏高 ⇒ flux_error 偏大 ⇒ snr 偏小（偏红）。量级 ~ `1/n_sky` 阶（n_sky≈300 时约 0.004σ），影响限于本符号 | CONFIRMED（两处实现逐字比对） |
| photometer.cpp:70 | `d2 <= aperture_radius_ * aperture_radius_` | r=4.0 px（.h 默认），硬边界像素中心判定 | 孔径内像素判定（含 0 面积加权，即不做亚像素） | **待确认** | `PHOTOMETRIC_FIT.md:210` 记为「孔径积分 `d²≤r²`」——描述实现事实、未给取值依据；`docs/plugins/algorithms_phase1/06_photometry.md:76` 原默认 `2×FWHM` 已划除并标「**值待定**」，`config_registry.json:559-576` 记 `finding: "gap"`，并注明「孔径不属生产口径」 | 半径增大⇒含更多天空噪声（`n_in↑`⇒`flux_error↑`）且 PSF  wings 更多（flux↑）；r=4.0px 对 FWHM≈3px 的星损失 >10% 流量且无孔径改正 ⇒ 若被接进任何比对，通量系统性偏低。**保守方向：以 r=4.0 得到的 flux 只可当下限** | CONFIRMED（登记面确认为 gap/值待定；未实跑量化损失） |
| photometer.cpp:76 | `n_in <= 0` | 0 | 空孔径显式失败 | 结构性不适用 | 计数下界 | —— | CONFIRMED |
| photometer.cpp:86 | `med = sky_vals[n/2]` | 同上 | MAD 的中心 | **待确认** | 与 :58 同族（同一约定问题，合并登记） | 同 :58 | CONFIRMED |
| photometer.cpp:92 | `_MAD_SCALE` 型常数 `1.482602218505602` | 1.482602218505602 | MAD→σ 高斯尺度一致因子 | **公式导出**（兼文献） | `1/Φ⁻¹(3/4)`，`Φ⁻¹(3/4)=0.6744897501960817`；锚：`docs/algorithms/CALIBRATION_ALGORITHMS.md:624`（给出恒等式实测 + Rousseeuw & Croux 1993 JASA 88(424) 1273–1283, DOI 10.1080/01621459.1993.10476408，并明确该文只把 `1.4826·MAD` 当既有基线、本仓不做有限样本校正）、`docs/algorithms/NOISE_ESTIMATION.md:105/:213`（唯一权威写法）。版次已钉（DOI＋卷页）。代码是否按式算：**是**——`sky_sigma = 1.482602218505602 * dev[dev.size()/2]`，`dev = \|x-\text{median}\|`，与 `NOISE_ESTIMATION.md:20` 逐符号一致；此字面量正是 `M3-A-006` 整改把原 `1.4826` 升为冻结全精度写法的结果（`FIX_LEDGER.csv:180`、`artifacts/evidence/governance-01/research/R-5_噪声SNR与统计口径.md:577` 记录整改前状态） | —— | CONFIRMED |
| photometer.cpp:94 | `std::sqrt(std::max(sum, 0.0) + n_in * sky_sigma * sky_sigma)` 的 `0.0` | 0 | 负净通量不入开方 | 结构性不适用 | 定义域保护，非科学取值 | —— | CONFIRMED |
| photometer.cpp:94 | Poisson 项 `sum`（ADU） | —— | 把孔径内 ADU 和直接当光子计数方差 | **待确认** | 仓内口径：`docs/contracts/DATA_SEMANTICS.md:3192`「本链**不**建立增益标定（`e⁻/ADU` 不入本层，`docs/science/CALIBRATION.md` §1 非目标）」⇒ 本式等价于**隐含 gain = 1 e⁻/ADU**；`PHOTOMETRIC_FIT.md:210`/`README.md` §9 只复述公式并标「简化误差」，无文献、无方向声明；相关缺陷 `M3-A-006`（`FIX_LEDGER.csv:180`，状态 **OPEN**）正是"测光/噪声诊断公式量纲不符"同一族 | gain 实际 0.5–5 e⁻/ADU 时 Poisson 项错 `1/gain` 倍：gain>1 ⇒ 方差被高估⇒snr 偏低（偏红）；gain<1 ⇒ 方差被低估⇒snr 偏高（**偏绿**）。影响范围：本符号的 `flux_error`/`snr` 两字段与其 4 组单测；不进产品字节 | CONFIRMED（合同与登记面逐字读过；未实跑） |
| photometer.cpp:94 | 缺失项 `n_in²/n_sky · σ_sky²` | 0（未实现） | 背景中位数自身的不确定度未进方差 | **待确认** | 条文面零提及。检索式：`git grep -n "flux_error\|aperture" -- docs/science/PHOTOMETRY.md docs/contracts/DATA_SEMANTICS.md` ⇒ 命中 `PHOTOMETRY.md:263`「aperture/flux/background：**无孔径背景扣除项**——背景已在 PSF/测光上游处理」（与本实现自相矛盾：本实现恰恰做了孔径背景扣除）、`:306`（把 aperture 误差口径推给 SExtractor/photutils 并声明语义不同）。即：本式在 `docs/science/` 内**无对应定义式** | 丢掉一个非负项 ⇒ `flux_error` 只会**偏小** ⇒ `snr` 只会**偏大** ⇒ 判据方向为**偏绿**（不等式方向已定）。保守方向：任何以本 snr 过 SNR 门的结论都必须按 `sqrt(var+ n_in²σ_sky²/n_sky)` 复算后再用 | CONFIRMED |
| photometer.cpp:95 | `r.flux_error > 0 ? r.flux / r.flux_error : 0.0` | 0 | 零方差退化为 snr=0 | 结构性不适用（退化约定） | 与 `DATA_SEMANTICS.md` 「variance=0/缺失 → ivar=0（显式不可用）」同族方向；本处 snr=0 是"不可用"而非"高 SNR"，方向偏红 | —— | CONFIRMED |

**本文件数值项合计 14 项（含 :58/:86 同族已并列）。**

### findings

### CR-05-2 · 孔径误差式丢掉天空均值不确定度项，方向为偏绿，且 `docs/science/PHOTOMETRY.md:263` 的说法与该实现相反
- 轴：S（主）／R／C
- 位点：`photometer.cpp:94` —— `r.flux_error = std::sqrt(std::max(sum, 0.0) + n_in * sky_sigma * sky_sigma);`；上游 `docs/science/PHOTOMETRY.md:263` —— `**aperture/flux/background**：\`F_instr\` 为仪器通量（ADU·px 或 e⁻ 同尺度）；无孔径背景扣除项——背景已在 PSF/测光上游处理`
- 现状：实现**确实**做了孔径背景扣除（`:71` `image[...] - r.background`），并用 `n_in·σ_sky²` 一项表示天空噪声贡献；天空估计本身的不确定度（经典 aperture 误差传播中的 `n_ap²/n_sky·σ_sky²`）没有任何来源声明，也没有在 ALG 侧登记为"已知省略"。
- 差在哪：`sci` 条文说"无孔径背景扣除项"，实现偏要扣背景并配一个误差式；误差式与条文不同源，且省略项的方向单调。
- 后果：`snr` 系统性偏高 ⇒ 若该符号按 `README.md` §9 的迁移计划被接进任何 SNR 门/诊断报告，会以偏绿方向放行低信噪源；本符号的对外可主张口径（`flux_error` ADU）与 `DATA_SEMANTICS` 的 aperture variance 语义不闭合。
- 定级：S3（**缺运行时触发路径**：`Photometer` 未接 orchestrator/module_adapters，检索见本节开头）
- 怎么算修好：三选一但必须同批：① 在 `docs/science/PHOTOMETRY.md` §5 或 §9a 为本符号补一条 aperture `flux_error` 定义式（含 `n_ap²/n_sky` 项与 gain 适用域），代码按式补项，并同步 `PHOTOMETRIC_FIT.md:210` 与 `README.md` §9 的复述行与 `p1_wcs_phot_test.cpp` 的 `m.snr > 3.0` 断言（否则补项后该断言可能翻红）；② 或明确写"仅中位数背景、误差非合同量、不含天空均值项"，并把 `:263` 的"无孔径背景扣除项"限定作用域；③ 修 `:263` 与实现的矛盾表述本身。
- 置信：CONFIRMED（省略项方向：丢一项 ⇒ 方差变小 ⇒ snr 变大 ⇒ 偏绿，已定）

### CR-05-3 · `flux_error` 的 Poisson 项把 ADU 当 e⁻，隐含 gain=1，与"本链不做增益标定"的合同口径同时成立
- 轴：S／C
- 位点：`photometer.cpp:94` 的 `std::max(sum, 0.0)` 项；单位声明 `photometer.h:15`（`double flux = 0.0; // ADU`）与 `README.md` §9「单位域：输入图像 ADU、flux ADU、flux_error ADU」
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:3192`「本链**不**建立增益标定（`e⁻/ADU` 不入本层，`docs/science/CALIBRATION.md` §1 非目标）」；`PHOTOMETRIC_FIT.md:210` 只复述 `flux_error=sqrt(max(sum,0)+n_in·σ_sky²)`；`docs/science/PHOTOMETRY.md:263` 对通量单位写「ADU·px **或 e⁻ 同尺度**」（两单位并列而不给换算）。检索式：`git grep -n "flux_error|aperture" -- docs/science/PHOTOMETRY.md docs/contracts/DATA_SEMANTICS.md` 无 gain 项命中。
- 现状：`sum`（ADU）直接进开方当方差 ⇒ 只有 gain=1 e⁻/ADU 时成立；本模块 API 根本没有 gain 形参（`photometer.h:26-28` 只有三个几何参数）。
- 差在哪：Poisson 方差必须与"计数发生的物理载体"同域；在 ADU 域写成 `sqrt(ADU)` 是把 gain 当 1 而不自知，与 `M3-A-006`（OPEN，`FIX_LEDGER.csv:180`：「测光/噪声诊断公式…与 SCI §5 诊断式量纲不符」）同族，但该条的处方只覆盖了 `noise_model` 注释与 fixture，`photometer` 侧只要求换 MAD 常数（已换），量纲问题在本文件仍在。
- 后果：真实增益下 `flux_error` 偏差 `sqrt(gain)` 量级（gain 0.5–5 ⇒ 0.7–2.2 倍），任何据本 snr 的诊断结论失真；gain>1 偏红、gain<1 偏绿。
- 定级：S3（同上，缺生产触发路径）
- 怎么算修好：要么在 `photometer.h` 增加显式 `gain_e_per_adu` 参数（默认必为**未设即拒绝**，不得默认 1）并把 Poisson 项改 `sum/gain`；要么在 `docs/science/PHOTOMETRY.md` §9a 明确声明"本符号只用于纯 ADU 域自洽性诊断，误差项不含光子统计"，同时把 `:94` 的第一项注释掉或改名。两者都必须同步 `p1_wcs_phot_test.cpp` 的 `m.snr > 3.0` 与 `test_p1002_gaps.py:180` 的 `Photometer phot(4.0,6.0,10.0)`。
- 置信：CONFIRMED（量纲推导独立复核）

### CR-05-4 · 同一"中位数"在本模块族内两套偶数约定，且无条文裁定
- 轴：C
- 位点：`photometer.cpp:58` `r.background = sky_vals[sky_vals.size() / 2];`（上中位） vs `lib/algorithms/noise_snr/cpp/src/noise_model.cpp:104-108`（`if (n % 2 == 0) { ... med = (v[n/2-1] + med) * 0.5; }` 偶数均值）
- 上位依据：`docs/algorithms/NOISE_ESTIMATION.md:20` F1 给出 `σ_bg = 1.482602218505602 · median(|x−median(x)|)`，未定义偶数样本约定；`docs/algorithms/PHASE2_REJECTION.md:87` 记第三处实现 `scratch_median`「nth_element 中位（**偶数均值**）」；`NOISE_ESTIMATION.md:213` 只钉 MAD→σ 常数的字面写法，不钉中位约定。
- 现状：三个模块各自决定偶数中位取法，photometer 与另两个不同。
- 差在哪：同一 F1 定义式在不同实现里给出不同数值，"同一件事同一说法"不成立。
- 后果：跨模块 σ_bg 比对存在系统性 `O(1/n)` 偏移（本符号方向：背景偏高⇒flux 偏低、σ_sky 偏高⇒snr 偏低，偏红）；若将来 aperture 符号与 patch 噪声符号对拍，差值会被误读成实现 bug。
- 定级：S3（本符号非生产；量级小）
- 怎么算修好：在 `NOISE_ESTIMATION.md` F1 处补一句偶数样本约定（建议统一到偶数均值，与 `noise_snr`/`phase2` 两处一致），并把 `photometer.cpp:58`、`:86` 改为同一约定；同批必须复核 `p1_wcs_phot_test.cpp:74` 的 `fabs(m.background - bg) < 5.0` 与 `m.snr > 3.0` 两条断言在新约定下仍绿（`add_psf` 夹具 n_sky 为偶数时会微移）。
- 置信：CONFIRMED（两处实现逐字比对；未实跑数值差）

### CR-05-5 · 孔径/天空环被图像边界裁切时仍返回 `valid=true`，结果结构里没有任何"部分孔径"标志
- 轴：R（主）／D
- 位点：`photometer.cpp:38-41`/`:63-66` 的 `std::max(0, ...)` / `std::min(h - 1, ...)` 夹取；`photometer.h:14-21` 的 `PhotometryResult`（无 truncated/partial 字段）
- 上位依据：`photometer.h:3` —— `失败不留貌似有效的空 catalog。(积分校验)`；`lib/algorithms/photometry/README.md` §6 —— `Photometer 旧符号（§9）失败显式：… 或 valid=false+failure_reason（"center out of bounds"/"no sky annulus pixels"/"aperture empty"），不留貌似有效结果`。检索式：`git grep -n "truncat|截断|partial" -- lib/algorithms/photometry docs/algorithms/PHOTOMETRIC_FIT.md` 无"边缘部分孔径"处置条文（`README.md` §6 只列三种失败态）。
- 现状：中心在图内但半径出图时，循环被夹到图内，`n_in>0`、`sky_vals` 非空 ⇒ `valid=true`，flux 为不完整孔径之和；`sky_vals` 只有 1 个像素也照样通过（`:52` 只判 `empty()`），此时 MAD 与中位数无统计意义。
- 差在哪：三条显式失败态之外存在第四种"数值失真但不失败"的状态，且 `PhotometryResult` 无字段承载 ⇒ 合同层面无法区分完整/截断孔径。
- 后果：任何消费 `flux`/`snr` 的下游（含 `eng/tests/backend/test_p1002_gaps.py` 的"生产同源 aperture 积分"driver）拿到的是系统性偏小的通量而标记为有效；边缘源的测光一致性判定被污染，方向单调（低估通量⇒偏暗）。
- 定级：S3（未接管线）
- 怎么算修好：`PhotometryResult` 增 `bool aperture_clipped; int n_sky_pixels;`（或在 `failure_reason` 里置 `"aperture clipped at edge"` 并保持 `valid=true`），由 `n_in` 与理论完整像素数比较得出；**同批必须改** `eng/tests/unit/p1_wcs_phot_test.cpp:92-105`（`test_photometry_integration_regression` 现在正断言边缘截断为 `valid`，注释「flux 低估 (aperture 截断); 不得误报成功为空」），否则加标志会把该用例判红。
- 置信：CONFIRMED（夹取逻辑与夹具断言均逐字读过；未实跑）

---

## 文件 3 · `lib/algorithms/photometry/wrapper_phase1/photometer.h`（40 行，整读）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|---|
| photometer.h:15-18 | `flux = 0.0` / `flux_error = 0.0` / `background = 0.0` / `snr = 0.0` | 0.0 | 结果结构体字段零初始化 | 结构性不适用 | 成员默认初值，`valid=false` 时不对外读数；无科学语义 | —— | CONFIRMED |
| photometer.h:19 | `bool valid = false` | false | 失败关闭式默认（未测即无效） | 结构性不适用 | 保守初值约定，方向偏红 | —— | CONFIRMED |
| photometer.h:15/:16/:17 | 单位注释 `ADU` / `ADU` / `ADU/px` | —— | 通量/误差/背景的单位声明 | **待确认** | `README.md` §9「单位域：photometer 输入图像 ADU、background ADU/px、flux ADU（孔径内和）、flux_error ADU、snr 无量纲——与 DATA-P1-PHOT §14 同域登记」；但 `docs/science/PHOTOMETRY.md:263` 对同类量写「ADU·px 或 e⁻ 同尺度」，两处单位口径不完全同一 | `flux` 是"孔径内和（无量纲计数的和）"，与 `F_instr`（`ADU·px`？）不同名不同式；保守方向：把本处 `ADU` 读作"孔径和 ADU"，勿与面亮度 ADU/px 混用 | PARTIAL（缺：`DATA_SEMANTICS.md` §14 对 `F_instr` 单位的逐字定义未在本批文件域内核对） |
| photometer.h:26 | `aperture_radius_px = 4.0` | 4.0 px | 默认孔径半径 | **待确认** | 仓内登记为 gap：`docs/plugins/algorithms_phase1/06_photometry.md:76`（原文）「`aperture_radius` \| ~~2×FWHM~~（**值待定**）\| px \| 孔径测光半径——**仅诊断 / 交叉验证**。（取值与实现/README 的 `4.0px` / `6-10px` 表述未统一，登记为 `finding: "gap"`；孔径不属生产口径）」；`eng/packaging/config/config_registry.json:559-576` `registration: none`、`finding: gap`、`owner: 负责人裁决` | 半径↔流量损失与天空噪声同向增大；对 FWHM≈3px 的 PSF，4.0px 硬边界孔径的完整度依赖中心相位，系统损失可达 10% 量级且无孔径改正。**保守方向：本值只能当"下限通量"用**；影响范围：`Photometer` 全部读数与其 4 组单测＋backend driver `test_p1002_gaps.py` | CONFIRMED（登记面逐字核过） |
| photometer.h:27 | `sky_annulus_inner = 6.0` | 6.0 px | 默认天空环内半径 | **待确认** | `config_registry.json:578-591`（`sky_annulus`，`declared_default: null`，`finding: "unregistered"`）；`06_photometry.md:77` 默认列为「——」 | 内半径↑⇒环更远离源（背景更干净）但样本数变化；6.0 与 4.0 的比值（1.5r）无任何条文说明。保守方向：若源密集，内半径过小会抬高背景⇒flux 偏低 | CONFIRMED |
| photometer.h:28 | `sky_annulus_outer = 10.0` | 10.0 px | 默认天空环外半径 | **待确认** | 同上（`06_photometry.md:76` 的「实现/README 的 4.0px / **6-10px** 表述未统一」即指本对值） | 外半径↑⇒n_sky↑、σ_sky 更稳但混入邻近源；与 `sky_vals` 仅判 `empty()` 相配，小环情形无最小样本数门 | CONFIRMED |
| photometer.h:26-28 | 三参数的**相对几何关系**（r : inner : outer = 4 : 6 : 10） | 4:6:10 | 孔径与天空环的尺度比 | **待确认** | 条文面零提及该比例的依据。检索式：`git grep -n "sky_annulus\|aperture_radius" -- docs lib eng`（40 行内命中，无一处给比例规则或文献） | 通用做法取 inner≈2–3×FWHM、outer≈3–5×FWHM；此处未与 FWHM 建立任何耦合（`Photometer` 不接收 FWHM），对 FWHM=1.5px（`p1_wcs_phot_test.cpp` 夹具 `sigma=1.5`）与 FWHM=5px 的帧使用同一组值 | CONFIRMED |

**本文件数值项合计 7 项。**

### findings

- 本文件（头）无独立可辩护指控：三条 finding 的实质都在 `photometer.cpp`（CR-05-2/3/4），默认值的无锚问题已由 `config_registry.json` 以 `finding: "gap"` / `"unregistered"` 如实登记并归负责人裁决，按派单 §4 不重复报成缺陷，仅以 §2.5 的 `待确认` 行喂 UNRESOLVED。
- 唯一新增观察（不构成 finding）：`photometer.h:19` 注释「失败显式 false, 不伪造有效」与 `:20` 「空串=成功」共同定义了二态语义，而 CR-05-5 说明实际存在第三态（截断）未被该语义覆盖。


## 文件 4 · `lib/algorithms/photometry/cpp/include/photometric_calib.h`（277 行，整读）

性质：测光定标的 **C ABI 合同头**（6 个 `PC_API` 导出 + `PhotometricDiag` + `PcMatchRecord`）。本头是 `docs/contracts/API_CONTRACTS.csv:284-285`、`PUBLIC_API.md` §API-PHOT-001、`DATA_SEMANTICS.md:687/:691` 的锚定对象，故其字面数字都是合同读数。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :8-11 | `_WIN32` → `__declspec(dllexport)`；否则空 | —— | 导出宏的平台分叉 | 结构性不适用 | 编译器链接机制，无科学语义；`docs/standards/C_ABI_STANDARD.md`（本头 :3 指向） | —— | CONFIRMED |
| :21-45 | `PhotometricDiag` 成员数 | 实测 **20** 个成员 | 逐阶段诊断结构体 | 结构性不适用（**但登记面计数与此不符，见 CR-05-9**） | 逐行点数：阶段组 14 + 统计组 6 = 20；`docs/traceability/TRACEABILITY_MATRIX.csv` MOD-astrocs-phase1-photometry 行写「PhotometricDiag **17 字段**」 | 若按 17 计数分配/序列化，则 3 个字段在登记面无描述 | CONFIRMED |
| :30-:39 | 阶段计数语义（`spatial_candidates`/`unique_matches`/`rejected_*`/`fit_used`） | —— | 诊断计数域的语义 | **待确认**（语义域，非取值） | `:32`「当前实现无双向过滤, 等于 spatial_candidates」与 `:34`「当前无双向, 保持 0」**已被登记为失实**（`PHOTOMETRIC_FIT.md` §13.3 **DISP-PHOT-001**）；`:36` `rejected_quality`「F<=0/非有限 + 星等不一致 + IRLS 离群 的总和」= **DISP-PHOT-006**（混合计数不可归因，且**不含**新增的饱和类） | 保守方向：读 `rejected_*` 任一项都不得当作单一原因；`:36` 的枚举零提及饱和位拒绝（值 7），而它现在确实计入 `n_quality_rejected`（`star_matcher.cpp:434`）⇒ `rejected_quality` 少了一项解释 | CONFIRMED |
| :40 | `scale_factor = 10^(-location(r))` | 指数 −1、底 10 | 稳健位置 → 乘性标度 | **公式导出** | `docs/science/PHOTOMETRY.md` §5：`scale = 10^{−location}`；实现 `star_matcher.cpp:589` `std::pow(10.0, -location)` **按式算**；`DATA_SEMANTICS.md:687` 声明其单位为 `[F_syn 单位]/ADU`（**不是**无量纲） | —— | CONFIRMED |
| :41/:72/:96/:148 | `0.6744897501960817`（MAD→σ 除数） | 0.6744897501960817 | `sigma_residual` 的高斯尺度换算 | **公式导出**（兼文献值） | = `Φ⁻¹(3/4)`；锚：`docs/algorithms/CALIBRATION_ALGORITHMS.md:624`（实测与 `1/1.482602218505602` 逐位相同；Rousseeuw & Croux 1993, JASA 88(424) 1273–1283, DOI 10.1080/01621459.1993.10476408；并声明本仓用渐近常数、不做有限样本校正，若要校正须引 Akinshin 2022 arXiv:2207.12005 或 Park-Kim-Wang 2020）、`NOISE_ESTIMATION.md:213`（唯一权威写法，4 位/11 位简写不可互换）；`PHOTOMETRIC_FIT.md` 参考文献节同。**代码是否按式算：是**（`star_matcher.cpp:21 _MAD_SCALE = 0.6744897501960817`、`:619` 逐字对应，与本头四处字面完全同值） | —— | CONFIRMED |
| :41/:72/:96/:148 | 记法 `MAD(r_inliers)` | —— | 残差散度的定义式写法 | **待确认**（定义歧义） | 权威定义在 `PHOTOMETRIC_FIT.md:19`（F4）`sigma_residual = median_i(\|r_i − **location**\|)/0.6744897501960817` 与 `:36`「实现的 MAD 中心是 IRLS 收敛后的 `location`，不是 `median(r_inliers)`…一阶偏差 `δ/σ`，实测纯高斯 5.6e-6、含污染 0.81%」；而本头四处写的 `MAD(r_inliers)` 按标准定义以自身中位为中心。`docs/science/PHOTOMETRY.md` §5 亦写 `MAD(r_inliers)` | 保守方向：以本头为唯一说明书的外部实现者会算出不同数（`δ ≳ 0.1σ` 时不可忽略）；影响面 = `sigma_residual`→`sigma_mag`→`sigma_cal_rel`→帧 QA 全链 | CONFIRMED |
| :43-44 | `r_p90` / `match_distance_p90` 与「线性插值」百分位 | 90（百分位点）；插值法=线性 | 诊断分布统计 | **待确认** | `star_matcher.cpp:29-30`（注释「百分位数计算 (线性插值)」）；`docs/contracts/**` 未登记**分位数约定**（inclusive/exclusive、是否线性插值）。检索式：`git grep -n "percentile\|百分位\|p90" -- docs/contracts` | 分位数约定不同在小样本（n_matched 常 <10）时可差数个 dex；只影响诊断字段，不进判据 | PARTIAL（缺：未逐行核 `percentile_of()` 与某标准的一致性） |
| :53 | `dr3sp_id` =「位置量化哈希; 见 wiki/06」 | —— | 逐星身份的派生规则指针 | **待确认** | 锚点 `wiki/06` 在跟踪集内**零命中**：`git ls-files \| grep -ci wiki` = 0；实际实现处为 `pc_api.cpp:743-752`（`make_dr3sp_id`，`PHOTOMETRIC_FIT.md` 记为 :884-894，行值已漂移）与 `DATA_SEMANTICS.md:691`「dr3sp_id=位置量化哈希（pc_api.cpp:743-752）」 | 量化位宽/取整方式无规范面可读 ⇒ 复算者无法重建 ID；影响 `photometric_match` 的 star_id 贯穿主张 | CONFIRMED（锚不存在以跟踪集为准） |
| :56 | `status` 枚举 0..3 | 4 值 | 逐星状态域 | 结构性不适用 | 枚举基数；与 `DATA_SEMANTICS.md:691`、`PUBLIC_API.md:513` 三处一致 | —— | CONFIRMED |
| :57-58 | `reject_reason` 枚举 **0..6** | 7 值 | 逐星拒绝原因域 | 结构性不适用（基数）——**但实现会写出值 7，见 CR-05-6** | 三处登记面同一断言：本头 `:57-58`、`DATA_SEMANTICS.md:691`、`PUBLIC_API.md:513`；写入点 `star_matcher.cpp:433`、拷贝点 `pc_api.cpp:1157-1163`（原样透传，无范围校验） | —— | CONFIRMED |
| :68 | 匹配半径「距离<2px」 | 2.0 px | PSF↔Gaia 像素域匹配阈 | **待确认** | `PHOTOMETRIC_FIT.md` §13.2 末条已作显式适用域裁决：「匹配半径 `match_radius=2.0 px` 是**像素域常数**…仓内真实帧像元尺度跨 **0.9586″–6.3076″/px（6.58 倍）**，同一常数对应天球半径 1.92″–12.6″…**跨仪器使用时必须按像元尺度复核该常数**」；无任何文献/标定给出 2.0 | 偏大 ⇒ 错配率上升（污染定标 ⇒ 偏绿）；偏小 ⇒ 星数下降（易触 `NO_DATA`，偏红）。当前"2.0 px 在 6.58 倍尺度差下恒成立"**只有条件性论证、无取值依据** | CONFIRMED（条文原文已引） |
| :69 | 星等一致性预过滤「> 3 mag」 | 3.0 mag | IRLS 前的粗筛窗 | **待确认** | `docs/science/PHOTOMETRY.md:220` 自我声明：「**Project-defined 冻结值**…本层**不引用文献**为该窗宽背书」；配置键 `photometry.mag_tolerance`（`defaults.json:540`，`authority_status: sourced`）；但 `eng/ci/prod_wiring_baseline.json:20` 把该键登记为 **`dead_config_key`**（生产无消费者），实际取值是 `pc_api.cpp:145/:435/:1120` 的字面 3.0 | 窗变宽 ⇒ 更多错配进 IRLS；窗变窄 ⇒ 星数下降。数值两侧一致（3.0），当前无产物错值；"改 defaults.json 不生效"已被 CI 基线如实登记，故**不另报 finding** | CONFIRMED |
| :70 | Tukey `c=4.685` | 4.685 | biweight 截断系数（单位 σ） | **文献值** | `PHOTOMETRIC_FIT.md` 参考文献节：Beaton & Tukey 1974, Technometrics 16, 147（DOI 10.1080/00401706.1974.10489171）；Mosteller & Tukey 1977；`defaults.json:554 photometry.tukey_c`。代码按式算：`star_matcher.cpp:23 _TUKEY_C=4.685`、`:598 cS=_TUKEY_C*S`、`:601 u=(r-location)/cS` | —— | CONFIRMED（注释称"对应 95% 正态效率"，与常用 k=4.685↔95% ARE 说法一致；本仓无推导，属文献可核陈述） |
| :70 | 「50 次迭代」/「收敛 1e-6」 | 50 iter / 1e-6 | IRLS 迭代上界与收敛门 | **待确认**（项目自订，五档无对应档） | `PHOTOMETRY.md` §4:196 与 §5 写死 `max_iter=50, tol=1e-6`，§10 把改值列为不可接受变化；`defaults.json:568/:582` 两键均被 `eng/ci/prod_wiring_baseline.json:18-19` 登记为 `dead_config_key`。代码：`star_matcher.cpp:25-27` | 1e-6 dex 远小于任何真实帧 `sigma_residual`（真实量级 0.018–0.046 mag）⇒ 收敛门实际由 50 步上界或不动点决定，不构成截断误差来源；风险：若 50 步内未达 1e-6，`location` 以未收敛值出 `scale` | CONFIRMED |
| :85/:137 | `qe_wl == nullptr ⇒ Q(λ)=1.0` | 1.0 | QE 缺省时的模型通带退化取值 | **待确认**（且与 §2a.4 的下游可区分要求冲突，见 CR-05-8） | `PHOTOMETRY.md` §2a.4：「`Q(λ)` **是通带的组成部分，不是可选项**…未配置 `Q` 时按 `Q(λ)≡1` 处理，其物理含义是『假设探测器为理想量子效率平坦器件』，**不是**『QE 已被折进 T(λ)』」；量级：合成星等差中位 +0.811 mag（被零点吸收）、**跨星散度 0.0082 mag（不被吸收，进 `sigma_residual`）**；**证据路径 `run/SCI-PHOT-FORMULA-01/evidence/c2_negative_controls.json`——跟踪集内 `run/` 下仅 1 个文件、`SCI-PHOT-FORMULA-01` 命中 0 ⇒ 按 §2.5 第 3 档判不合格，只能落待确认**（同族问题已在 AUDIT-06 阶段 A 登记为"证据件落 run/ 属合案缺陷"，此处不重复立 finding） | 方向单调：`Q≡1` 使 `sigma_residual` 相对含 `Q` 时**偏小 11.6%/16.0%**（§2a.4 表）⇒ 判据偏绿 | CONFIRMED |
| :86/:97/:137 | 「qe_* 仅作 API 一致性保留」/「供 SNR 模块 §14 计算 SNR_phot」 | —— | 参数无效性与下游消费者声明 | 结构性不适用 | `DISP-PHOT-005` 已登记：`pc_calibrate_simple` 的 QE 三参数被 `(void)` 丢弃 | —— | CONFIRMED |
| :90/:142 | SIP 系数「长度36」 | 36 = 6×6 | 调用方必须提供的系数槽位数 | 结构性不适用 | `wcs_transform.cpp:66-71`（无条件读 36 项）、`:90` 前置条件、`module_adapters.cpp:5034-5041`（生产侧 36 项数组）；求值 `evalSip :100-107` 用 `i+j<=order` 下三角 ⇒ 与 FITS SIP 三角约定兼容，6×6 只是容器 | 合同要求**任何 order 都提供 36 项**（FITS 惯例是 `(order+1)²`），按 FITS 惯例只给 9 项的外部调用方会越界读 27 项 | CONFIRMED |
| :102 | 「-1: 空指针参数, -2: 尺寸无效」 | −1/−2 | 错误码 | 结构性不适用 | 返回码枚举；`README.md` §6 与之一致 | —— | CONFIRMED |
| :102 | 「-3: **无Gaia星/PSF星**(退化scale=1.0)」 | −3 | 错误码语义 | **待确认**（与实现/README 冲突，见 CR-05-10） | `README.md` §6「退化路径不失败…rc=0」；`PHOTOMETRIC_FIT.md` §13.1「退化（无 PSF / 无光谱星 / 滤光片-QE 失败 → scale=1.0、rc=0）」；`pc_api.cpp` 内 `return -3` 只有三处（:309/:726/:996），全部是 **`gaia_client_cone_search_with_spectrum` 返回 rc≠0** | —— | CONFIRMED |
| :103-104 | 「-4: 内部异常；`sip_order` 正式支持 [0,5]，越界拒绝不截断」 | −4 / [0,5] | 异常屏障与 SIP 阶数上界 | 结构性不适用（上界由 6×6 缓冲 + `i*6+j` 索引决定：`6*order>35` 越界） | `wcs_transform.cpp:41-51`（构造层硬校验抛 `invalid_argument`，C 边界转 −4）；跨模块同口径 `healpix_drizzle` 的 [0,5] | 阶数上界 5 无 FITS/文献依据（标准不限阶数），但属缓冲区尺寸决定的实现选择，按派单 §4 不作缺陷报 | CONFIRMED |
| :126-128 | `F_syn = ∫F_λ·T·Q·λ dλ`（W·m⁻²·nm），**不含** `10^(-0.4·magG)` | −0.4（仅出现在"不含"断言） | 参考通带定义式 | **公式导出** | `PHOTOMETRY.md` §2a.1–2a.2（`λ` 因子=光子计数约定，`1/(hc)` 与星无关被零点吸收；`F_syn = hc·N_γ`，`hc = 1.986445857e-16 J·nm`——独立复算 `6.62607015e-34 × 2.99792458e8 = 1.98644586e-25 J·m = 1.98644586e-16 J·nm` ⇒ 逐位吻合）；PHOT-FSYN-CANON-001 同号命中 `docs/science/PHOTOMETRY.md`、`docs/algorithms/PHOTOMETRIC_FIT.md`、`lib/.../spectrum_integrator.cpp` | —— | CONFIRMED |
| :138 | 谱网格「[336,338,…,1020]nm（长度通常343）」 | 336 / 2 / 1020 / 343 | XPSD 采样波长网格 | **实验标定**（**标定件不在跟踪面 ⇒ 降待确认**） | 取值权威=容器自报：`PHOTOMETRIC_FIT.md` §13.1「真实 XPSD 分片文件头逐字为 `parameters="spectrumStart=336,spectrumStep=2,spectrumCount=343,spectrumBits=8"`（`gaia/GaiaDR3SP/gdr3sp-1.0.0-01.xpsd`，11 272 905 颗源、`magnitudeRange="-2.00,13.62"`）⇒ 网格与位深是**容器自报**而非硬编码；生产按文件头取参（`gaia_client.c:1437-1491`）」；版本已钉到分片名 `gdr3sp-1.0.0-01` | 头注释把**某个分片**的读数写成"通常343"的通用事实，而代码逐文件读头 ⇒ 换分片即失真；量化误差证据 `run/SCI-FIX-PHOTFIT-01/evidence/e2_realdata_xpsd.json` 同样不在跟踪面 | CONFIRMED |
| :158-271 | 6 个导出签名的全部形参（`radius_deg`、`mag_min/mag_max`、`filter_count`、`spectrum_count`、`width/height`、`n_psf`、`sip_order`） | 无默认值 | 纯参数面 | 结构性不适用 | 本头**不给任何默认值**；默认值权威在调用方与 `defaults.json`/`phase_config` | —— | CONFIRMED |
| :182 | 「out[i] = pixels[i] * scale（double 精度累加, 不损失有效数字）」 | —— | f64 通道像素校正式 | **公式导出** | `PHOTOMETRY.md` §5 `I_cal = I·scale`；实现 `image_corrector.cpp:62-78`。`DATA_SEMANTICS.md:687` 把 `scale` 定为有量纲因子 `[F_syn 单位]/ADU` ⇒ 乘后 `out_pixels` 量纲变为 `[F_syn 单位]`，与本头 :93「校正后像素」的 ADU 语义是否同一承载面见 CR-05-11 | —— | PARTIAL（缺：未核写盘块的单位声明，不在本批文件域） |

**本文件数值项合计 21 行（同族合并位点已在行内列全）。**

### findings

### CR-05-6 · 实现向 `PcMatchRecord.reject_reason` 写出值 **7**，本头与两份合同都把定义域钉为 0..6
- 轴：C（主）／R
- 位点：`photometric_calib.h:57-58` —— `int32_t reject_reason;  // 0=used, 1=mag-rejected, 2=IRLS-outlier, 3=invalid-flux,` / `// 4=no-spatial-match, 5=unmatched-other, 6=psf-invalid`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:691`「reject_reason 0..6」；`docs/contracts/PUBLIC_API.md:513`「(:47-59，status 0/1/2/3 + reject_reason 0..6）」。写入侧规范：`docs/science/PHOTOMETRY.md` §4「饱和星（`psf_status!=0` 或 `qf & (SATURATED|HAS_SATURATED)!=0`）不进入匹配与定标，计 `rejected_quality`」——只规定聚合计数，未规定逐星原因码取何值。
- 现状：`star_matcher.cpp:432-436` 对饱和星 `(*out_match_reasons)[(size_t)i] = 7;`；`pc_api.cpp:1157-1163` 把该数组**原值**赋给 `rec.reject_reason`，无范围校验、无回映射；三处枚举文字均止于 6。检索式：`git grep -n "reject_reason" -- lib docs/contracts eng/tests`，除上述外无第 7 值的任何登记（`eng/tests/validation/release02/fix_p1_photometry_apply/` 内亦无 `== 7` 断言）。
- 差在哪：C ABI 结构体的取值域是对外合同，实现新增了域外值而合同面未同步。
- 后果：`photometric_match` 块（`DATA_SEMANTICS.md:361` 列为实测生产块；`docs/architecture/execution_inventory.csv:4` 记 orchestrator `:2879` 写该块）里逐星原因码 7 无定义 ⇒ 按合同做枚举校验/分类统计的下游要么判产品非法，要么把饱和星归进"未知"桶；`rejected_quality` 聚合数与逐星码不可对账（不变量 I3 `Σdiag.rejected_*+fit_used=unique_matches` 仍成立，但归因断了）。
- 定级：**S2**，注明**缺生产写盘触发路径**：生产 `frame_photometry_fit.cpp:207-214`、`:363-364` 只消费 `status`/`residual`；`module_adapters.cpp` 内 `PcMatchRecord`/`reject_reason` 零命中；唯一已知写块者是 orchestrator 通道，该通道无 CLI 调用点（`git grep -n "run_stage1\|run_v2_with_timing" -- lib eng` 除 orchestrator 自身与 CI fixture 外零命中），其 DLL 亦未编入根构建（`CMakeLists.txt:760`）。
- 怎么算修好：先定方向。`DISP-PHOT-006` 说 `rejected_quality` 是混合计数不可归因，本条的逐星码是**唯一可归因**通道 ⇒ 应"合同跟实现"：① 本头 `:57-58` 加 `7=saturated-quality-bit`；② 同批改 `DATA_SEMANTICS.md:691`、`PUBLIC_API.md:513` 两处 `0..6`→`0..7`；③ 同批改 `PHOTOMETRY.md` §4 补逐星码语义；④ 补能红的锁：在 `_qf` oracle 里断言"被饱和位拒的星 `reject_reason==7`"（现有 `p1_qf_oracle.cpp` 只比对 scale 差异，抓不到枚举漂移）。缺②③即把一处不一致改成三处。
- 置信：CONFIRMED

### CR-05-7 · 直通导出的 `gaia_fsyn` 未声明是"哪一种 F_syn 约定"，而零点因子对约定的选择逐星敏感
- 轴：S（主）／C
- 位点：`photometric_calib.h:79` —— `// gaia_fsyn - Gaia星合成流量数组 [n_gaia]`（同一入参在 `:194` 的 f64 变体中同样无约定）
- 上位依据：`docs/science/PHOTOMETRY.md` §2a.1「**必须是什么**」列出「`F_syn` 中不出现 `G`：Gaia G 星等既不作乘性因子、也不作归一化因子进入参考通量」；`PHOTOMETRIC_FIT.md` §13.1 同时登记两条路径——生产 `compute_f_syn_cached_xpsd`（不含因子）与「**非生产通道（历史相对口径）** `compute_f_syn`/`compute_f_syn_cached`：`∫uint8·T·Q·λdλ × 10^(−0.4·magG)`」，并明言「**两条路径的网格不可直接对拍**」。`docs/contracts/PUBLIC_API.md:505`：「pc_calibrate_simple … 直通版：F_syn 由调用方传入（gaia_fsyn）」。
- 现状：本头对该入参只有"合成流量"四字，无单位、无约定、无"必须与 §2a 同式"的约束；`docs/contracts/API_CONTRACTS.csv:284` 的单位列是模板串「ADU/pixel/deg per header」。
- 差在哪：`r_i = log10(F_instr/F_syn)`、`location` 为其稳健位置 ⇒ 若调用方喂含 `10^{-0.4G}` 的历史口径，偏差随星等变化，**不是**能被零点吸收的常数；合同允许两种口径从同一入口进来且不被拒。
- 后果：`out_scale_factor` 的单位合同（`DATA_SEMANTICS.md:687`：「只有在**显式声明参考通量单位后**才允许称其为无量纲乘性因子」）在这两个导出上前提不成立；外部集成者喂错口径得到 rc=0、`n_matched>0` 的"合法"结果，`sigma_residual` 被星等色散支配。
- 定级：S2（§13.5 把这两个导出登记为"ABI 兼容通道保留"，生产主路径是 v2/_f64_v2 ⇒ 无生产触发路径；风险面是对外冻结 ABI）
- 怎么算修好：① `:79`/`:194` 补「单位与约定同 §2a：`W·m⁻²·nm`，**不含** `10^{-0.4·magG}`」，并镜像到 `DATA_SEMANTICS.md` 的 `pc_calibrate_simple` 入参行与 `API_CONTRACTS.csv` 的单位列（该 CSV 由门比对签名，改注释行须同步转录，否则 UT/合同门判红）；② 若要真闭环需运行时不变量（用 `gaia_mag` 与 `gaia_fsyn` 的相关性判"是否已含星等因子"并拒收）——那是新增科学判据，须走 §10 变更流程，不得由工程侧自行加门。
- 置信：PARTIAL（缺：未找到这两个导出的现存外部调用方，无法证明"今天有人可能喂错口径"）

### CR-05-8 · 头允许 `qe_wl=nullptr ⇒ Q≡1` 却不给"QE 未计入"任何输出通道，与 §2a.4 的下游可区分要求不匹配
- 轴：D（主）／C
- 位点：`photometric_calib.h:137` —— `// filter_wl/trans/count - 滤光片波长(nm)与透过率[0,1]` / `// qe_wl/trans/count - CCD QE 波长(nm)与透过率[0,1] (; 可为 nullptr, Q(λ)=1.0)`；同义句 `:85`
- 上位依据：`docs/science/PHOTOMETRY.md` §2a.4 末条：「未配置或解析失败时**必须**显式告警，使「没配 QE」与「QE 已计入」在下游可区分（`frame_photometry_fit.cpp` QE 分支）」；同节定量：`Q≡1` 相对含 KAF-16803 的跨星散度 **0.0082 mag 不被零点吸收**，`sigma_mag` 含 Q 后 **+11.6%（T2/M1）/ +16.0%（T3/M1）**；推论条：「比较不同帧/不同模型的 `sigma_residual` 时**必须声明所用模型通带**」。
- 现状：`PhotometricDiag`（`:21-45`）20 个成员里无任何 QE 在场/缺席标志；返回码只有 0/−1/−2/−3/−4，`Q≡1` 走 rc=0；本头把 `Q≡1` 写成与"提供了 QE"完全同权的合法输入。可区分性只在上一层 `frame_photometry_fit.cpp` 实现。
- 差在哪：设计点名的能力（下游可区分 + 必须声明通带）在本 ABI 面上没有承载字段，只对本仓自己的调用方成立。
- 后果：按本头取数的消费者无法声明通带，也无从知道该帧 `sigma_residual` 是否被 `Q≡1` 压低（方向：偏小 ⇒ 可信度判据偏绿）；跨帧一致性主张因此不可复核。
- 定级：S2（缺口在合同面/外部集成面；本仓生产路径由上一层满足）
- 怎么算修好：`PhotometricDiag` 追加 `int qe_curve_present;` —— **但结构体尾部追加即改 ABI**，`PUBLIC_API.md` 的 C ABI 条款与 `docs/standards/C_ABI_STANDARD.md` 兼容性策略须先裁定（若判不可改，则改为在 `out_diag==nullptr` 时禁止 `qe_wl==nullptr`，返回 −1）；同时把 `:85/:137` 文字从"可为 nullptr"改为"nullptr ⇒ 退化通带，必须显式登记"；同批改 §2a.4 的落点句与 `frame_photometry_fit.cpp` 告警分支，否则头与实现再次脱钩。
- 置信：CONFIRMED

### CR-05-9 · 登记面把 `PhotometricDiag` 写成"17 字段"，结构体实为 20 个成员
- 轴：C
- 位点：`photometric_calib.h:43-44` —— `double r_median, r_p90, r_max;` / `double match_distance_median, match_distance_p90, match_distance_max;`
- 上位依据：`docs/traceability/TRACEABILITY_MATRIX.csv`（`MOD-astrocs-phase1-photometry` 行 notes，同一文本亦见 `TRACEABILITY_MATRIX.json:240`）：「写 photo_stats KV 块（STATUS/N_MATCHED/SCALE_FACTOR/SIGMA_RESIDUAL + **PhotometricDiag 17 字段**，:2902-2935）」；`lib/algorithms/photometry/README.md` §8 同句转录。
- 现状：`:21-45` 成员逐个为 20（14 阶段计数/标量 + 6 个 `r_*`/`match_distance_*` 统计）。
- 差在哪：追溯面给出的字段数是别处 KV 写入与校验的依据，与结构体定义不同数。
- 后果：该行的 "VERIFIED" 读数建立在失真计数上；`r_p90/r_max/match_distance_*` 是否进入对外产品，无法从登记面判定。
- 定级：S3（纯登记面计数失真，不改产物字节）
- 怎么算修好：以 `orchestrator.cpp:2902-2935` 的**实际 KV 写入项数**为准回写矩阵与 README——注意别只看头改数字：登记句说的是"写进 KV 的字段数"，结构体是"成员数"，两口径须分别具名，否则改完还是两个数。
- 置信：PARTIAL（缺：orchestrator 的 KV 写入项数未逐行点，该文件不在本批清单内）

### CR-05-10 · 本头 `-3` 的语义与实现、README、ALG 三处相反
- 轴：C（主）／R
- 位点：`photometric_calib.h:102` —— `// -1: 空指针参数, -2: 尺寸无效, -3: 无Gaia星/PSF星(退化scale=1.0),`
- 上位依据：`lib/algorithms/photometry/README.md` §6：「退化路径**不失败**、显式登记：无 Gaia 星/无 PSF 星/锥形搜索无光谱星/滤光片预处理失败 → scale=1.0、n_matched=0、sigma_residual=0、恒等校正、**rc=0**」；`PHOTOMETRIC_FIT.md` §13.1「退化（无 PSF / 无光谱星 / 滤光片-QE 失败 → scale=1.0、**rc=0**）」，§13.4 负面矩阵把 −3 只用于「锥搜失败(rc=−3)/handle=null(rc=−2)」。
- 现状：`pc_api.cpp` 全文仅三处 `return -3`（:309、:726、:996），三处都在 `gaia_client_cone_search_with_spectrum(...) != 0` 分支；`n_gaia==0`/无 PSF 走 rc=0 退化分支。而 `:102` 把 −3 说成"无 Gaia 星/PSF 星"，同一项又把"退化 scale=1.0"（即 rc=0 的行为）绑在失败码上；`:155` 的「−3: 锥形搜索失败**或无光谱星**」后半句同样与 README 相反。
- 差在哪：同一返回码在头/README/ALG 三处给出互斥语义，且头自身一句话内自相冲突。
- 后果：按本头集成的调用方会用 `rc<0` 判"该帧未标定"，真实行为却是 rc=0 + 恒等校正（**判红方向**：漏报成功）；反向只认 `rc=0` 为"已标定"者会吃到无参考星的恒等校正而不自知（该信息只在 `out_n_matched=0`，**判绿方向**）。两个方向都写清，故本条不是误绿指控而是合同语义缺陷。
- 定级：S3（不改产物字节，纯合同文字与实现语义不符）
- 怎么算修好：`:102` 与 `:155` 按实现重写为「−3 = 外部锥形搜索调用失败」，并把"无参考星 ⇒ rc=0 且 `out_n_matched=0`（恒等校正）"作为独立成功退化态写明；**同批**核对 `docs/contracts/PUBLIC_API.md` 与 `README.md` §6 的错误码表（三处一起改，只改头 = 把不一致搬家）。
- 置信：CONFIRMED（三处 `return -3` 已定位并读上下文）

### CR-05-11 · `out_pixels` 的量纲在头内自相冲突：`scale` 被合同定为有量纲因子，本头仍称输出为"校正后像素（float32）"
- 轴：C／S
- 位点：`photometric_calib.h:93` —— `// out_pixels - 校正后像素 (调用者分配, float32 [H*W])`；`photometric_calib.h:182` —— `// 图像校正: out[i] = pixels[i] * scale (double 精度累加, 不损失有效数字).`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:687` —— `out_scale_factor … **[F_syn 单位]/ADU**（… **不是**无量纲——只有在显式声明参考通量单位后才允许称其为无量纲乘性因子，SCI-PHOT-001 §10）`；`docs/science/PHOTOMETRY.md` §2a.2 末段 —— 「`F_instr` 单位 ADU，与 `F_syn` **量纲不同**；…`scale = 10^{−location}` 单位 `[F_syn 单位]/ADU`（§3）」。
- 现状：本头四处描述 `out_pixels` 只写"校正后像素"，不给单位；`I_cal = I·scale` 之后按合同其单位应为 `[F_syn 单位]`（`W·m⁻²·nm`/px），不再是 ADU；`DATA_SEMANTICS.md` 另处（§"Phase1 逐帧 variance/ivar 三态"）用 `photo_scaled_adu` 块名与 α² 标度换算责任方条款，说明"scaled 之后仍是 ADU 域"是另一套口径。
- 差在哪：合同说 `scale` 有量纲且不可称无量纲，头说输出就是"校正后像素"而无单位声明 ⇒ 同一乘性操作在两个面上给出两种量纲结论；而 §2a/§3 的整个"物理量被零点吸收"论证依赖 `I_photo = k_photo·I_cal` 的单位链闭合。
- 后果：下游（SNR 模块吃 `sigma_residual`、Phase2 面亮度标度律 `1/A_cell²` 换算）若按 ADU 处理被乘过 `scale` 的数组，标度类别判错一档；对外"归一化后图像仍是 ADU"的读数失真。
- 定级：S3（**缺生产触发路径**：真正把该数组当 ADU 写盘的单位声明在 orchestrator 块名/`DATA_SEMANTICS` 侧，本批文件域内无第二处单位声明可判谁是权威；需实跑一次 f64 通道并读回块单位才能定案）
- 怎么算修好：先由负责人/合同面定一件事——`out_pixels` 的单位权威到底是"ADU（若 scale 被定义为归一化因子）"还是"`[F_syn 单位]`"；定后在本头 `:93/:145/:173/:249/:269` 五处输出描述统一加单位，并与 `DATA_SEMANTICS.md:687` 与 §"variance/ivar 三态"里的 `photo_scaled_adu` 命名对齐（若最终采 ADU 口径，则 `DATA_SEMANTICS:687` 的"不是无量纲"句须走变更流程，不能只改代码注释）。
- 置信：PARTIAL（缺：块级单位声明在 `DATA_SEMANTICS.md` §14/编排块表与本头之外，未逐处核对）

## 文件 5 · `lib/algorithms/photometry/cpp/src/psfsw.cpp`（954 行，整读）

身份：`IMPL-P1-PSFW-001`，PSFSW 诊断量族（四分量／复合权重／共同星集／depth 门／记录门）。生产可达性实测：`extract_psfsw_components`、`compute_psfsw_weights`、`validate_common_star_set` 被 `lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:420/:431/:460` 使用；但**四道判据门**（`depth_stability_gate`、`spatial_nonuniformity_gate`、`scalar_degradation_gate`、`validate_psfsw_record`）在生产面零调用点——检索式 `git grep -n "validate_psfsw_record|spatial_nonuniformity_gate|depth_stability_gate|scalar_degradation_gate|weight_dynamic_range_guard" -- lib eng`，除本文件/本头外命中全部落在 `eng/tests/unit/v6_p1_psfw/**`（＋一句 README）。故本文件所有 finding 封顶 S2，并附"门只在单测里跑"这一事实。

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :70-76 | `median_of` 偶数取**两中位均值**；空返回 `0.0` | 0.5/2 | 本族中位数约定 | 结构性不适用（定义选择）| 与 `docs/algorithms/PHASE2_REJECTION.md:87`「nth_element 中位（偶数均值）」和 `lib/algorithms/noise_snr/cpp/src/noise_model.cpp:104-108` 同约定（**与本批 `photometer.cpp:58` 的上中位取法相反**，那一侧的偏差已在 CR-05-4 登记）；空→0 是静默零值，见 :12 行的方向分析 | 空输入 ⇒ `background`/`σ` 读 0 而非"不可估计"；本文件调用处均有前置非空判据 | CONFIRMED |
| :85 | `kMadToSigma * median_of(dev)` | 1.482602218505602 | MAD→σ 高斯尺度因子 | **公式导出** | = `1/Φ⁻¹(3/4)`；锚 `docs/algorithms/CALIBRATION_ALGORITHMS.md:624`（含 DOI 10.1080/01621459.1993.10476408 与"不做有限样本校正"声明）、`NOISE_ESTIMATION.md:105/:213`（唯一权威写法）。代码按式算：**是**（:84 `dev[i]=|x_i-med|`、:85 乘子）| —— | CONFIRMED |
| :88-99 | `quantile_of` 位置式 `pos = p·(n−1)`，线性插值；越界取端点 | p；`n-1`；端点钳位 | 分位数定义（type-7/inclusive） | **待确认** | 仓内三处分位读数（`ComponentMeasure.p05/p50/p95`、`PcMatchRecord` 诊断 p90、`GATES_AND_TOLERANCES.md` 的 SNR 分位判据）**无一处定义分位数约定**；检索式 `git grep -n "percentile\|分位约定\|inclusive" -- docs/contracts docs/science` 零命中 | 小样本（n_common 常为 3–30）下 type-7 与 type-6 给出的 p05/p95 可差一档，直接改变 `spread=(p95−p05)/p50` ⇒ 影响 G18 的绿/红；方向不单调 | PARTIAL（缺：未与 numpy/R 的 9 种定义逐一对表，也未查测试 oracle 用的是哪一种） |
| :102-120 | `rank_with_ties` 并列取平均秩 `0.5·((i+1)+(j+1))` | 0.5、+1 | Spearman 的 mid-rank | **公式导出** | 秩相关标准定义（mid-rank）；`spearman_rho` = Pearson(秩) —— :123-139 逐式实现，与定义一致 | —— | CONFIRMED |
| :124 | `x.size() < 2 → return 0.0` | 2 | 秩相关最小样本 | 结构性不适用 | n=1 时秩相关无定义，返回 0 使 `|rho|<=0.8` 通过 | 偏绿方向：单点扫描 ⇒ rho=0 ⇒ 单调漂移门恒过；但 :424 的 `k>=5` 已把该情形挡在门外 | CONFIRMED |
| :137 | `sxx<=0 \|\| syy<=0 → 0.0` | 0 | 常数序列（零方差）退化 | 结构性不适用 | 常数序列的秩相关无定义，取 0 = "无趋势" | 偏绿：若某帧权重全同（median 归一后单帧组常出现），rho=0 通过 —— 语义上正确 | CONFIRMED |
| :160-166 | `samples.empty()` 分支：`p05=p50=p95=value`、`valid_area_fraction=1.0`、`spatial_spread_rel=0.0`、`linear_trend_max_rel=0.0` | 1.0 / 0.0 | **无空间样本时的四分量摘要默认值** | **待确认**（方向为偏绿，见 CR-05-12） | 条文面零提及"无样本时如何填"。检索式：`git grep -n "valid_area_fraction\|spatial_spread" -- docs/science docs/contracts` 只命中 `v6_clause_registry_v1.json:1124/:1149` 的 `fail_closed` 句（"任一分量>0.30 → 拆 region/tile"），未规定缺证据时的取值 | `spread=0` ⇒ :537 判据永不触发；`valid_area_fraction=1.0` ⇒ :882 判据永过。**丢证据只会让度量变小 ⇒ 偏绿**，方向已定 | CONFIRMED |
| :173 | `valid_area_fraction = finite/samples.size()` | —— | "有效**面积**比"实际是样本**计数**比 | **待确认** | `v6_clause_registry_v1.json` 的字段名 `valid_area_fraction` 与 `docs/science/PSF_SIGNAL_WEIGHT.md` §3「signal、concentration、noise、background 四个分量分别写入产品」；**无任何条文定义 area-weighted 还是 count-weighted** | 若样本非等面积（按 tile/region 采），计数比 ≠ 面积比 ⇒ 覆盖声明失真；方向取决于样本构造 | PARTIAL（缺：`samples` 的构造方在生产面尚未出现，无法判定是否等面积） |
| :174 | `spatial_spread_rel = (p95-p05)/p50`（`p50>0` 否则 0） | —— | 相对空间散度 | **公式导出** | `v6_clause_registry_v1.json:1115-1131`（PSFSW-T-NU）的 `fail_closed: "任一分量>0.30 → …"` 即该比式；代码 :506-514 同式复算 | —— | CONFIRMED |
| :177-195 / :516-536 | 趋势自变量 `xi = 样本枚举下标 i`；`trend = max|fit−mean|/p50` | `i`（0,1,2,…） | "空间线性趋势" | **待确认**（见 CR-05-14） | 条文侧只给判据不给自变量：`v6_clause_registry_v1.json:1138-1155`（PSFSW-T-TREND）`fail_closed: "趋势>0.10 → spatial_nonuniformity_gate_failed"`；本头 :112 只写 `samples; /* 空间样本 (有序), 供分位/趋势复算 */`，**"有序"未定义排序键** | 趋势门随生产者的样本排布次序而变：按散度/按位置/按 flux 排序给出三个不同 trend，把样本按 value 交替排序可使 `trend→0` ⇒ **偏绿逃逸通道** | CONFIRMED（:181 `xi = (double)i` 与 :521 同族） |
| :184-187 | OLS `denom = n·sxx − sx²`；`fabs(denom) > 0.0` 才除 | 0 | 拟合退化保护 | 结构性不适用 | 恰为 0 只在 n<2；用严格 `>0.0` 而非 eps 是数值选择，不承载科学语义 | n≥2 时 denom 严格 >0，无病态除法 | CONFIRMED |
| :204-232 | `a_nea>0`、`n>=kNCommonMin`、`fhat_valid.size()==n`、`valid.size()>=kNCommonMin` | 3（经 kNCommonMin） | 分量提取的四道 fail-closed 入口门 | **待确认**（阈本身待签，见 psfsw.h 表） | `v6_clause_registry_v1.json:1040-1060`（PSFSW-T-NMIN=3, PENDING_OWNER_SIGNOFF, `signoff: SO-07`） | 3 颗星的"星间通量散度"做 MAD→σ：n=3 时 MAD 由单个偏差决定，`N_k` 极不稳定 ⇒ `Wt∝N^{-2}` 把它平方放大 ⇒ 权重噪声主导 | CONFIRMED |
| :235 | `conc = (Σvalid/n)/a_nea` = `mean(fhat)/A_NEA` | —— | 浓度分量 | **公式导出** | 本头 :137-138 与 `docs/science/PSF_SIGNAL_WEIGHT.md` §3「ACSD 实现披露」条：`Conc_k=mean(fhat)/A_NEA`；`A_NEA=1/ΣP²`（FZ-COND-WHITENOISE，由 dynamic_psf 提供，本文件不计算） | —— | CONFIRMED |
| :236 | `n = robust_scale_mad(valid)` = `1.4826·MAD(fhat)` | —— | "噪声"分量 = **星间通量散度** | **待确认**（定义与名义不符） | 权威披露句：SCI §3「`N_k=1.482602218505602·MAD({fhat})`（**共同星的星间通量散度，不是图像噪声 σ_n**）」—— 条文自己点名它与 PixInsight `σ_n` 的差别；但 `psfsw.h:99` 的 name 词表与 `measurement_id="psfsw.noise"` 仍称 noise | 语义影响：该量随**视场内星等分布**而变（同一帧换天区即变），不是探测器性质；作为 `N^{-2}` 进权重 ⇒ 权重携带星族信息，与"权重只来自纯净信号与噪声之比"（ASTROCS_DESIGN §3.1）的口径相抵。方向不确定 | CONFIRMED（条文与实现均逐字读过） |
| :237 | `a_ref = (in.a_ref>0) ? in.a_ref : in.a_nea` | a_nea（回退值） | 参考面积缺省时的静默替代 | **待确认** | 本头 :120 `double a_ref = 0.0; /* 参考面积 [px^2]; 0 => a_nea */` 声明了该回退；**无任何条文说明为何 A_NEA 可作 A_ref 的替身** | `B_k = b̄·A_ref` ⇒ 回退使 B 被 `A_NEA/A_ref` 整倍缩放；`Wt∝B^{-1}` ⇒ 权重直接反比该比值，且无旗标。影响：单帧产品 `wt_unnormalized`（`phase1_product.cpp:437`）| CONFIRMED |
| :238 | `b = background_robust_mean * a_ref` | —— | 背景分量 | **公式导出** | SCI §3 披露句 `B_k=b̄_k·A_ref,k`（"稳健背景×参考面积"）| —— | CONFIRMED |
| :260-263 | 指数合法性 `(α>0∨β>0) ∧ (γ>0∨δ>0)` | 0 | 复合式的非退化判据 | 结构性不适用 | 定义域约束（避免 0/0 与常数权重）；G14 复用 | —— | CONFIRMED |
| :300-304 | `params.floor > 0.0` 否则 reject | 0 | 分量下限缺失即拒 | 结构性不适用 | `v6_clause_registry_v1.json` PSFSW-COMPOSITE-FLOOR（值 1e-12）；方向为**偏红**（缺参数即失败），符合 fail-closed | —— | CONFIRMED |
| :308-311 | `max(component, floor)` 四路钳位 | 1e-12（kComponentFloor） | 分量截断窗 | **待确认** | PSFSW-COMPOSITE-FLOOR，`PENDING_OWNER_SIGNOFF`，unit=`component_flux_unit`；其 `source_binding.contains="1e-12"` 在被引文件中**不出现**（见 CR-05-13） | `N_k` 或 `B_k` 真值 < 1e-12（ADU 域可能，如近零背景）时被抬到 1e-12 ⇒ `Wt` 上界被隐式钉死在 `floor^{-γ-δ}`；方向偏大 | CONFIRMED |
| :312-314 | `Wt = c_norm·S^α·Conc^β/(N^γ·B^δ)` | α=2,β=1,γ=2,δ=1 | 复合权重式 | **待确认**（SCI 自陈"无标定记录"） | `v6_clause_registry_v1.json` PSFSW-COMPOSITE-ALPHA/BETA/GAMMA/DELTA（status 全 `PENDING_OWNER_SIGNOFF`）；**决定性条文**：`docs/science/PSF_SIGNAL_WEIGHT.md` §3 实现披露「本项目是**受 PixInsight PSFSW 启发**而非**等价于式[16]**：指数（α=2,γ=2 vs 1,1）、`N` 的语义、`B` 的面积因子三处均不同。该复合的指数与阈值在实现中标注 `PENDING_OWNER_SIGNOFF`，**尚无本项目 L1 合成数据标定记录**；其最优性以本项目 L1 合成数据标定记录为判据」 | 代码**按式算**（:312-314 与条款式逐符号一致），故公式实现合规；不合规的是"取值依据"——四个指数既非导出也非标定，SCI 自己承认无标定记录 | CONFIRMED |
| :318-322 | 组内 median 归一；`apply_normalization=false` 的负向 mutation | —— | 归一步骤与测试用旁路 | 结构性不适用 | `FZ-FORMULA-PSFSW-COMPOSITE`：`W_psfsw,k=Wt_k/median_j(Wt_j)`；旁路只由内部 `composite_impl` 使用（公开入口 `compute_psfsw_weights` 恒传 true） | —— | CONFIRMED |
| :341 | `return 1e308`（不可比较时的哨兵） | 1e308 | `cnorm_invariance_deviation` 的失败值 | 结构性不适用 | 哨兵值（必大于任何容差 ⇒ 门判红），非科学量 | —— | CONFIRMED |
| :353/:397/:403/:431/:446/:484/:489/:504/:537/:555/:577/:771/:873/:940 | 各门读取的 `kNCommonMin`/`kDepthMinScanPoints`/`kDepthMinSpanMag`/`kDepthMaxRelDev`/`kDepthMaxAbsRho`/`kNonuniformityMax(Low)`/`kSpatialTrendMax`/`kPowerLossMax`/`kFluxBiasMax`/`kWeightRangeMax`/`kGroupMedianTarget`/`kCompositeMedianRtol` | 见 psfsw.h 表 | 判据阈值 | 逐条处置见**文件 6 的表**（同族合并） | 同一族常数，位点在此仅消费 | —— | CONFIRMED |
| :362-363 | `mag_max>mag_min`、`detection_threshold_sigma>0`、`matching_radius_arcsec>0` | >0 | selection function 的合法性判据 | **待确认**（数值从不参与科学判定，见 CR-05-15） | `v6_clause_registry_v1.json` FZ-GATE-PSFSW-FAILCLOSED 与 SCI §3 约束条「使用跨帧匹配的共同恒星集合或显式 selection-function 校正」；条文未规定这三个量必须**用于**计算 | 只判正负 ⇒ `mag_max=−3/25`、`matching_radius_arcsec=30″` 等荒谬值全部合法；角秒域参数与像素域匹配半径（`pc_api.cpp` 的 `match_radius=2.0 px`）之间**无换算点、无像素尺度入参** | CONFIRMED |
| :445 | `n_common_span_ok = (max_n >= 2 * min_n) && min_n > 0` | **2**（字面量） | depth 跨度不足的豁免条件 | **待确认**（**登记面零条目**） | 检索式：`git grep -n "PSFSW-T" -- eng/contracts/data/v6_clause_registry_v1.json` 全表中**无**任何条款承载这个 2 倍关系；`PSFSW-T-DEPTH-SPAN` 只给 `1.0 mag` | 该式把"星数随深度变化 ≥2 倍"当作"测深远不够"的替身：一个 0.2 mag 跨度但星数从 40 掉到 18 的扫描即获豁免 ⇒ **偏绿**。影响 G11／depth 门 | CONFIRMED |
| :446 | `if (span_mag < kDepthMinSpanMag && !n_common_span_ok) reject` | 1.0 mag | 深度跨度门（OR 型豁免） | **待确认** | `PSFSW-T-DEPTH-SPAN` value=1 mag，`contains: "1.0"` 在被引 SCI 中确实出现（我实测 `locator_ok=False`、`contains_ok=True`） | 见上一行：第二支路无条款 | CONFIRMED |
| :466-481 | `series[t] = scans[t].w_psfsw[f]` 对 `idx[t]=t` 做 Spearman | t（扫描点序号） | 深度单调漂移的自变量 | **待确认** | `PSFSW-T-DEPTH-RHO`（0.8）`fail_closed: "…|rho|>0.8 → depth_monotonic_drift"`；`DepthScanPoint` 结构（本头 :237-241）**不含排序约束**，:439-443 也只为求 span 而扫序 | 调用方未按 `mag_limit` 排序时 rho 无语义（可 0 可 0.9）⇒ 同一产品数据换个入参顺序得出门的两种结论；方向双向 | CONFIRMED |
| :504 | `tier==low ? kNonuniformityMaxLow : kNonuniformityMax` | 0.20 / 0.30 | 分档散度限 | **待确认** | `PSFSW-T-NU-LOW`（0.2）与 `PSFSW-T-NU`（0.3）；前者 `source_binding.locator` 是**乱码**（我实测该字段值为 `""` 形式的双字节残文，非任何 token）⇒ 见 CR-05-13 | 档位来源 `tier_from_record` 只看 `n_common`（:732-734），与"该分量是否真的空间均匀"无关 ⇒ 星多的帧享受更宽限（0.30），星少的吃 0.20；方向：多星 ⇒ 偏绿 | CONFIRMED |
| :555 | `!(power_loss <= kPowerLossMax) \|\| !(flux_bias <= kFluxBiasMax)` | 0.05 / 0.01 | 标量降级门（NaN 安全写法） | **待确认**（值）＋ 结构性不适用（`!(x<=t)` 的 NaN 归红是**正确**写法，我按不等式方向复核：NaN ⇒ 比较为假 ⇒ 取反为真 ⇒ reject） | `PSFSW-T-POWERLOSS`（0.05，`contains "0.05"` 在 SCI 中**不出现**）、`PSFSW-T-FLUXBIAS`（0.01，出现） | 见 CR-05-12：本仓 `PsfswRecord` 默认 `power_loss=0.0/flux_bias=0.0` ⇒ 从未计算 = 满分通过 | CONFIRMED |
| :575-577 | `g.range = wmax/wmin`（`wmin>0` 才算）；`exceeded = range > kWeightRangeMax` | 100 | 权重动态范围旗标 | **待确认** | `PSFSW-T-WRANGE`（100，`contains "100"` 出现）；本头 :285 注明「>100 仅强制旗标, 不判 unavailable」 | `wmin<=0` 时 `range` 保持 0 ⇒ 旗标不置位（G15 会把非正权重判红，故此处不独立成风险） | CONFIRMED |
| :604-613 | `denom = Σ_{valid} w_psfsw[f]`；`alpha[f][p]=w[f]/denom` | —— | conventional coadd 的组合系数 | **公式导出** | 本头 :303 与 `FZ-FORMULA-PSFSW-COMPOSITE`：`alpha_k(p)=W_k v_k(p)/Σ_j W_j v_j(p)`；代码按式算（`v` 为 0/1 有效性 ⇒ 与实现逐位等价） | —— | CONFIRMED |
| :630-637 | `var = Σ_{a,b} α_a α_b C_in[a·k+b]`；`α==0` 跳过 | 0 | 协方差二次型 | **公式导出** | 本头 :317 与 `FZ-GATE-PSFSW-COV`（`C_out = R C_in Rᵀ`；文献锚 `docs/science/PSF_SIGNAL_WEIGHT.md:149`「Fruchter & Hook 2002, PASP 114, 144；Zackay & Ofek 2017 II」）；跳过 0 项不改变和值 | —— | CONFIRMED |
| :651/:657-661/:757-758 | 禁词表（`ivar`/`variance`/`snr`/`support`/`coverage`/`fwhm`/…、`flux^-2`/`fisher`） | —— | 词法守卫 | 结构性不适用（枚举集合） | 本头 :19 与 `eng/contracts/data/v6_clause_registry_v1.json#weight_vocabulary`（:650 注释点名 §3 登记）；:711-718 区分"子串匹配"与"精确等值"两种口径并注明理由（防 `psfsw.signal` 误伤） | —— | CONFIRMED |
| :771/:873 | `fabs(median_target − 1.0) > 1e-9`；`fabs(med − 1.0) > 1e-9` | 1.0 / 1e-9 | 组内 median=1 的容差 | **待确认** | `PSFSW-T-EPSFTOL`（value 1e-09, `contains "1e-9"`）与 `FZ-FIELD-PSFSW-UNIT`；**实测 `contains` 与被引 SCI 不匹配**（见 CR-05-13） | 1e-9 对 double 的 `x/median(x)` 是**逐位级**要求：`median_of` 用两中位均值 `0.5*(a+b)` 与除法，任何归约次序差异都可能超 1e-9 ⇒ 门可假红；亦无条文说明该容差是"实现一致性"还是"物理精度" | PARTIAL（缺：未实跑验证归一次序是否真能落进 1e-9） |
| :776/:785-786 | `components.size() != 4`；`ids.size()!=4`；`names.size()!=4` | 4 | 四分量基数 | 结构性不适用 | `FZ-FIELD-PSFSW-4COMP`（signal/concentration/noise/background）—— 枚举基数，非科学量 | —— | CONFIRMED |
| :940 | `baseline_bootstrap_resamples >= 200 && baseline_confidence >= 0.95` | **200 / 0.95**（字面量） | 基线声明的统计证据门 | **待确认** | 登记面**有**条款：`PSFSW-T-BOOT`=200、`PSFSW-T-CI`=0.95（`PENDING_OWNER_SIGNOFF`，`contains "200"` 命中、`"0.95"` 不命中 SCI）；**但代码写成裸字面量，未进本头 `kXxx` 冻结常数面**（其余 20 个阈值全部走常数） | 改登记册不动代码、改代码不动登记册，两侧无绑定 ⇒ 与"同一常数单一权威定义点"（`ENGINEERING_SPEC §3` 与 M3-A-008 的裁决口径）相反；影响 G25 与 `PSFSW-BASELINE-NONINFERIOR` 主张 | CONFIRMED |
| :942-943 | 禁用主张词表 `fisher_optimal`/`equals_ivar`/`equivalent_to_w_info`/`looks_better` | —— | 对外措辞守卫 | 结构性不适用 | 枚举集合（`PSFSW-BASELINE-NONINFERIOR` 的措辞面） | —— | CONFIRMED |

**本文件数值项合计 26 行。**

### findings

### CR-05-12 · 零空间样本与零降级量两条路径让 G18/G19 恒过：丢证据只会让度量变小，方向为偏绿
- 轴：R（主）／D
- 位点：`psfsw.cpp:160-166` —— `if (samples.empty()) { c.p05 = c.p50 = c.p95 = value; c.valid_area_fraction = 1.0; c.spatial_spread_rel = 0.0; c.linear_trend_max_rel = 0.0; c.samples.clear(); return; }`；`psfsw.h:382-383` —— `double power_loss = 0.0;` / `double flux_bias = 0.0;`
- 上位依据：`eng/contracts/data/v6_clause_registry_v1.json:1115-1155` 两条 `fail_closed` 句（「任一分量>0.30 → 拆 region/tile 或 spatial_nonuniformity_gate_failed」「趋势>0.10 → …」）；`docs/science/PSF_SIGNAL_WEIGHT.md` §3 约束条「**指数、截断、稳健估计器和归一常数版本化，训练/调参样本与最终验收样本分立**」＋「signal、concentration、noise、background 四个分量分别写入产品」；`FZ-DEGRADE-SCALAR`：「空间残差/趋势门 + 功率损失门 **双重**」。
- 现状：`spatial_nonuniformity_gate`（:505-548）在 `c.samples` 为空时回落到 `c.p05/p50/p95`，而这三项在 `fill_summary` 的空样本分支里被填成同一个 `value` ⇒ `spread=(p95−p05)/p50=0`、`trend=0`；`scalar_degradation_gate(rec.power_loss, rec.flux_bias)`（G19，:895）读结构体默认 0.0/0.0。两条都是"没测＝通过"。
- 差在哪：**不等式方向已定**——`spread > limit` 与 `trend > kSpatialTrendMax`、`power_loss <= kPowerLossMax` 全是"越小越好"的单侧判据，而缺测值被填为该侧最优值（0），因此缺证据与通过不可区分；同一族的 G10/G11 却用显式 `depth_gate_evaluated` 旗标做到 fail-closed（:835-841），说明作者知道该写法，本处属遗漏而非取舍。
- 后果：`PSFSW-T-NU/-TREND/-POWERLOSS/-FLUXBIAS` 四条判据对"未提供空间样本"的产品**恒真**；而 `phase1_product.cpp:415-418` 的 `signal_samples` 等四项是从调用方原样透传，本仓生产面**没有任何一处**填它们 ⇒ 现役 v6 Phase1 产品的四分量必然走空样本分支，`valid_area_fraction=1.0`（"全覆盖"）也被凭空写进产品 JSON（`phase1_product.cpp:706` 的 `comp_json` 逐分量序列化，含 `valid_area_fraction`）。
- 定级：**S2**。降级理由（按派单"判 S1 的硬门槛"）：`spatial_nonuniformity_gate`／`validate_psfsw_record` 在生产面零调用点（检索式与结果见本节开头），失真的 `valid_area_fraction=1.0` 虽写入产品 JSON，但"门放行"这一步尚未接线，故产物本身不被该门把守；**这条同时构成一条接线缺失的事实**：被登记为 fail-closed 的四道门目前只在单测里跑。
- 怎么算修好：① `fill_summary` 空样本分支改为**显式不可知**：`valid_area_fraction` 取 `NaN`（或 0）并新增 `bool has_spatial_samples=false`（结构体已有 `has_spatial_map`，须一并说明三者关系），`spatial_nonuniformity_gate` 对"无样本"直接 reject（新原因串 `no_spatial_samples`，须同步 `PsfswReason` 白名单与 `v6_clause_registry_v1.json#FZ-GATE-PSFSW-FAILCLOSED` 的白名单成员，二者都是 FROZEN 条款 ⇒ 走变更流程）；② `PsfswRecord.power_loss/flux_bias` 默认改 `NaN` 或加 `bool degradation_measured`，G19 缺测判红；③ 同批改 `eng/tests/unit/v6_p1_psfw/p1psfw_tests_record.cpp`（其 `good` 夹具很可能正是空样本＋0.0 默认，加判红会立刻把现网单测打红：:85 `spatial_nonuniformity_gate(flat, standard).ok` 断言"平样本=过"），并把 :103/:104 的 0.25 档用例扩出一条"零样本必须红"的负例。
- 置信：CONFIRMED（分支与判据方向均逐字复核；未实跑）

### CR-05-13 · 24 条 PSFSW 数值条款的 `source_binding` 全部悬空；头自称的"冻结面"文件里一条 PSFSW 都没有
- 轴：S（主）／C
- 位点：`psfsw.h:3-6` —— `合同锚 (主: docs/science/PSF_SIGNAL_WEIGHT.md; Phase1: docs/design/PHASE1_DETAILED_DESIGN.md; **冻结: docs/algorithms/GATES_AND_TOLERANCES.md**; eng/contracts/data/v6_clause_registry_v1.json#weight_vocabulary)`；`psfsw.h:40` —— `/* 冻结阈值 (全部 PENDING_OWNER_SIGNOFF -> fail-closed 实现, 不放宽) */`
- 上位依据（登记面自述）：`docs/contracts/DATA_SEMANTICS.md:3341` —— 「`PENDING_OWNER_SIGNOFF` | **值与文本唯一确定**，但涉及 FROZEN 非 v6 SCI 修订/数值确认，须负责人按 `SO-xx` 签字后方可作为正式修订生效」；`:3354-3358` 把全部 `PSFSW-T-*` 列入 49 条待签条款；`eng/contracts/data/v6_clause_registry_v1.json` 每条 PSFSW 带 `source_binding{file, kind:"md_table_row", locator, contains}`，例：`PSFSW-T-DEPTH` → `{file: docs/science/PSF_SIGNAL_WEIGHT.md, locator: "PSFSW-T-DEPTH", contains: "0.05", reanchored_from: "PSFSW_FROZEN_THRESHOLDS.md", reanchored_by: "DOC-CONTRACT-MERGE-02"}`。
- 现状（我以 `git show HEAD:<file>` 逐条比对，脚本仅我自备、不改不跑仓库代码）：
  - **locator 命中 0/24（无一条可解析）**：`PSFSW-T-DEPTH`、`PSFSW-T-NU`、`PSFSW-T-BOOT`… 这些 token 在整个 `docs/` 里**只出现在 `docs/contracts/DATA_SEMANTICS.md:3352-3358` 的 id 清单**（无值、无判据），`docs/science/PSF_SIGNAL_WEIGHT.md` 一次都没有（`git grep -ln "PSFSW-T-" -- docs` ⇒ 单文件 DATA_SEMANTICS）。
  - **contains 命中 12/24，缺失 12**：含 `0.05`(DEPTH)、`0.8`(RHO)、`0.05`(POWERLOSS)、`0.03`(COVMC)、`0.95`(CI)、`1e-9`(EPSFTOL)、`0.20`(NU-LOW)、`alpha=2`/`beta=1`/`gamma=2`/`delta=1`（文档写的是希腊字母 `α=2, β=1, γ=2, δ=1`）、`1e-12`(FLOOR)。命中的多是 `"5"`/`"3"`/`"30"`/`"100"`/`"200"` 这类**任何长数字里的子串**（如 `"200"` 命中 `Fruchter & Hook 2002`）⇒ 该断言本身无判别力。
  - `PSFSW-T-NU-LOW` 的 `locator` 字段值是**乱码残文**（非任何 token）。
  - `reanchored_from: PSFSW_FROZEN_THRESHOLDS.md` 指向的原冻结表在跟踪集中**不存在**（`git ls-files | grep -ci PSFSW_FROZEN` = 0）⇒ 这是一次"把锚从被删文档搬走"的迁移，搬到的文件不含锚。
  - `docs/algorithms/GATES_AND_TOLERANCES.md`（本头点名的"冻结"面）含 `PSFSW` 的行数 = **0**（`git grep -c "PSFSW" -- docs/algorithms/GATES_AND_TOLERANCES.md` 无输出）。
  - 唯一的机器校验：`eng/tests/contracts/product_family/test_field_constraints_integration.py:164-174`（`test_clause_anchors_reanchored_to_living_docs`）只断言 `source_binding.file` 存在，**从不看 locator/contains**。
- 差在哪：条款值 `0.05/0.8/0.20/1e-9/200/0.95/α..δ/floor` 在"科学文档"这一层**根本没有被写下**；登记册把它们锚到一份不含它们的文档，且校验只查文件存在 ⇒ `PENDING_OWNER_SIGNOFF` 的语义前提「值与文本唯一确定」不成立，负责人按 SO-07 签字时拿不到条文。四个指数的实际处境更硬：`PSF_SIGNAL_WEIGHT.md` §3 自陈「尚无本项目 L1 合成数据标定记录；其最优性以本项目 L1 合成数据标定记录为判据」⇒ 既非文献值、非公式导出、也非实验标定。
- 后果：本模块全部对外"阈值有权威"的主张（psfsw.h 头注释、`FROZEN/PENDING` 状态机、SO-xx 签字链）失去可核验性；`Wt=C_norm·S²·Conc/(N²·B)` 的指数与 12 条判据值处于**唯一来源=代码字面量与登记册自指**的状态。
- 定级：**S2**（不定 S1 的理由如实写出：这些数值在代码↔登记册两侧当前逐条一致，产物字节不因锚失效而错；损失的是权威链与签字依据，而非读数）
- 怎么算修好：① 把 24 条按 §3 的写法逐条落进 `docs/science/PSF_SIGNAL_WEIGHT.md` §7（或在 §3 之下开一张"PSFSW 冻结阈值表"，行内含 token 与数值），令 `locator` 与 `contains` 都能被真命中；② 把 `test_clause_anchors_reanchored_to_living_docs` 从"文件存在"升级为"locator 在文内 ∧ contains 在 locator 之后同一段落内"，并配 `--self-test` 的正/负例（拿现有 12 条不命中当负例语料）；③ `PSFSW-T-NU-LOW` 的乱码 locator 单独修；④ `psfsw.h:5` 的"冻结: GATES_AND_TOLERANCES.md"要么把表补进该文件、要么改指真实落点；⑤ 四个指数的归属须上呈负责人裁决（SCI 已自陈无标定记录，这不是工程能补的锚）。**同批注意**：`contains` 断言要改成"带边界的数值 token 匹配"，否则 `"5"`、`"200"` 类命中会继续伪装成已核验。
- 置信：CONFIRMED（24 条逐条机检＋原文件 `git show HEAD:` 读取；未跑仓库任何脚本）

### CR-05-14 · "空间趋势"与"深度漂移"两道的自变量分别是样本枚举下标与扫描点入参序号，条文未钉排序键，判据可被排布方式左右
- 轴：R（主）／S
- 位点：`psfsw.cpp:181` —— `const double xi = static_cast<double>(i);`（趋势拟合的 x 轴）；`psfsw.cpp:470` —— `idx[t] = static_cast<double>(t);`（Spearman 的 x 轴）；`psfsw.h:112` —— `std::vector<double> samples; /* 空间样本 (有序), 供分位/趋势复算 */`
- 上位依据：`eng/contracts/data/v6_clause_registry_v1.json:1138-1155`（PSFSW-T-TREND）与 `:1015-1031`（PSFSW-T-DEPTH-RHO）的 `fail_closed` 句只给阈值（0.10 / 0.8），不给自变量语义；`docs/science/PSF_SIGNAL_WEIGHT.md` §3「改变不相关星表深度/检测阈值，共同星集 PSFSW 的稳定性判据 = 显著不变」是**结论要求**，未定义"沿哪个轴"。检索式：`git grep -n "趋势\|trend" -- docs/science/PSF_SIGNAL_WEIGHT.md eng/contracts/data/v6_clause_registry_v1.json`。
- 现状：`spatial_nonuniformity_gate` 把样本按数组下标当"空间坐标"做 OLS，`depth_stability_gate` 把 `scans` 的入参顺序当深度顺序；两处都没有坐标入参（`ComponentMeasure` 只有 1-D `samples`，`DepthScanPoint` 有 `mag_limit` 但门只对 `span` 求 min/max，从不按其排序）。
- 差在哪：二维空间被摊成一维下标后，"趋势"是**排布次序的函数**而非空间的函数；把同一组样本按数值交替排序即可让 `slope≈0`。深度门亦然：不按 `mag_limit` 排序时 `|rho|` 与"漂移"无关。
- 后果：`PSFSW-T-TREND`／`PSFSW-T-DEPTH-RHO` 两条判据既可被生产者无意中误判红（次序天然给出假趋势），也可被有意排布判绿；产品里 `spatial_trend` 读数不可跨帧比较，与 §3 的"显著不变"主张直接冲突。
- 定级：S2（同上：门未接生产；但 `spatial_spread_rel`/`linear_trend_max_rel` 两个数已经由 `phase1_product.cpp:706` 的 `comp_json` 写进产品 JSON）
- 怎么算修好：① `ComponentMeasure` 增坐标（`std::vector<double> sample_x, sample_y`，或显式声明 samples 必须是某固定空间填充序并把该序写进条款），趋势改**对坐标**拟合、并给出各向同性定义（如取 PCA 主方向或 max over 两个轴向）；② `depth_stability_gate` 内部先按 `mag_limit` 升序稳定排序再算 rho（一行），并把"自变量=按 mag_limit 排序后的秩"写进 PSFSW-T-DEPTH-RHO 的 `fail_closed` 文本；③ 同批补一条负例：同一组样本两种排布 ⇒ 门结论必须相同（现状必红，正好证明修复有效）。
- 置信：CONFIRMED（两处 x 轴取法逐字读过；偏绿构造由 OLS 对下标的对称性直接得出）

### CR-05-15 · selection function 的四个数值只做正负检查、从不参与判定，且角秒量在本链无像素换算点
- 轴：D（主）／C
- 位点：`psfsw.cpp:359-363` —— `!(sf.mag_max > sf.mag_min) || !(sf.detection_threshold_sigma > 0.0) || !(sf.matching_radius_arcsec > 0.0)`；`psfsw.h:208` —— `double matching_radius_arcsec = 0.0;`
- 上位依据：`docs/science/PSF_SIGNAL_WEIGHT.md` §3 约束条「使用跨帧匹配的共同恒星集合**或显式 selection-function 校正**」＋「排除饱和、混合、拖线、移动源、严重 PSF 失配和边缘截断源」；`docs/contracts/DATA_SEMANTICS.md` 侧的 `PSFSW-T-*`/`FZ-GATE-*` 无一条规定"数值必须被消费"。
- 现状：门只要求这些字段"存在且为正"；`n_common` 的算法（:384-394）只用 `ExclusionFlags` 与 `valid_in_frame`，从不按 `mag_min/mag_max` 复核成员是否真落在声明的星等窗内，也从不把 `matching_radius_arcsec` 与任何像素域匹配半径对照。
- 差在哪：设计点名的能力（selection function 参与有效性判定）在此只有**声明面**没有**实现面**；而 `matching_radius_arcsec` 是角秒量，本链匹配半径是像素量（`photometric_calib.h:68` 的 `2px`、`PHOTOMETRIC_FIT.md` §13.2 明写真实帧像元尺度跨 0.9586″–6.3076″/px 6.58 倍 ⇒ 同一个 `arcsec` 声明在不同帧对应 0.3–1.5 个匹配半径），代码里没有像素尺度入参、也没有换算点。
- 后果：一份"声明 30″ 匹配半径、mag_max=25"的共同星集能带着完整元数据通过 G08/G09/`validate_common_star_set`；对外"共同星集已做 selection-function 校正"的主张没有实现支撑；单位一致性（角秒↔像素）在本模块**未被任何一处核对**。
- 定级：S2（该门未接生产）
- 怎么算修好：二选一并写进条款：① **实质校验**——`CommonStarMember` 需带逐星 `mag`，门内按 `mag_min/mag_max` 复核成员归属，并把 `matching_radius_arcsec` 与 `(像元尺度, 像素匹配半径)` 的三元一致性做成显式判据（需新增 `pixel_scale_arcsec` 入参；该量本链已在 `PHASE2_SAMPLER.md`/drizzle 侧存在，不算新概念）；② **诚实降级**——把 `validate_common_star_set` 的该段改名 `declared_fields_present`，并在 `FZ-GATE-PSFSW-*` 条文里明写"本门不校验 selection function 的数值一致性，数值一致性由 X 承担"。不可只加注释了事。
- 置信：CONFIRMED（门体全文读完，判定路径逐行核过；未实跑）

---

## 文件 6 · `lib/algorithms/photometry/include/astrocs/v6/psfsw.h`（407 行，整读）

### §2.5 数值处置表

**共同处境（先说一次，下面各行以"锚"列指出例外）**：`psfsw.h:40` 自称「冻结阈值（**全部 PENDING_OWNER_SIGNOFF** -> fail-closed 实现, 不放宽）」；登记面 `eng/contracts/data/v6_clause_registry_v1.json` 对 24 条 PSFSW 条款逐条给出 `value`＋`unit`＋`gate`＋`source_binding`，**实测值与本头字面逐条一致（无一处数值分歧）**，但 `source_binding` 的 locator 24/24 悬空、contains 12/24 悬空 ⇒ 详见 CR-05-13。除 `kMadToSigma` 与结构常数外，本表每行的"处置"都取 **待确认**，保守方向写明"若签字否掉，红/绿各自会怎样翻"。

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :42 | `kMadToSigma` | 1.482602218505602 | MAD→σ 换算因子 | **公式导出** | `1/Φ⁻¹(3/4)`；`CALIBRATION_ALGORITHMS.md:624`（DOI 10.1080/01621459.1993.10476408，渐近常数、不做有限样本校正）、`NOISE_ESTIMATION.md:105/:213` 唯一权威写法；与本批 `photometer.cpp:92`、`star_matcher.cpp:21` 的 `0.6744897501960817` 互为倒数，**三处全精度写法一致** | —— | CONFIRMED |
| :43 | `kDepthMaxRelDev` | 0.05 | 深度稳定性最大相对偏差 | 待确认 | PSFSW-T-DEPTH（unit=1, gate=PSFSW-G11, `signoff: SO-07`）；SCI 侧零提及（contains 不命中） | 收紧⇒更多组判 `depth_instability`（偏红）；放宽⇒选择偏差型漂移被放行（偏绿） | CONFIRMED |
| :44 | `kDepthMinScanPoints` | 5 | 深度扫描点数下限 | 待确认 | PSFSW-T-DEPTH-K（unit=count）；contains "5" 以子串"命中"（无判别力） | 点数<5 时 median/ rho 不稳；放宽⇒统计量失真（偏绿） | CONFIRMED |
| :45 | `kDepthMinSpanMag` | 1.0 mag | 深度跨度下限 | 待确认 | PSFSW-T-DEPTH-SPAN；**但 :446 的 OR 豁免支路（2 倍星数）无任何条款** | 见 CR-05-12/13 | CONFIRMED |
| :46 | `kDepthMaxAbsRho` | 0.8 | 深度单调漂移的秩相关上限 | 待确认 | PSFSW-T-DEPTH-RHO；contains "0.8" 在 SCI 不出现 | 自变量次序未定义（CR-05-14）⇒ 阈值本身此刻不可解释 | CONFIRMED |
| :47-49 | `kNCommonMin/Robust/Preferred` | 3 / 10 / 30 stars | 共同星集分档 | 待确认 | PSFSW-T-NMIN/NROBUST/NPREF（PENDING）；**分档效应**：`n_common<10 ⇒ spread 限从 0.30 收到 0.20`（:504），即星少⇒更严，星多⇒更宽（多星帧可携带更大空间散度） | 若负责人认为应"星少⇒更不可信"，则本分档方向**反了**：现实现是 n≥30 的组享受 0.30 限、n<10 的组吃 0.20；请裁决档位语义（本行是给负责人的核对点，非既定缺陷） | CONFIRMED |
| :50 | `kNonuniformityMax` | 0.30 | 分量相对空间散度上限 | 待确认 | PSFSW-T-NU；`fail_closed` 句在登记册 :1124 | 与 :51 同时决定 G18；零样本时二者皆失效 | CONFIRMED |
| :51 | `kNonuniformityMaxLow` | 0.20 | LOW 档散度上限 | 待确认 | PSFSW-T-NU-LOW；**该条目的 `locator` 字段是乱码残文**（CR-05-13） | 同上 | CONFIRMED |
| :52 | `kSpatialTrendMax` | 0.10 | 空间线性趋势上限 | 待确认 | PSFSW-T-TREND | 自变量未定义 ⇒ 数值无意义（CR-05-14） | CONFIRMED |
| :53 | `kPowerLossMax` | 0.05 | 标量降级功率损失上限 | 待确认 | PSFSW-T-POWERLOSS；contains "0.05" 不命中 SCI | `PsfswRecord` 默认 0.0 ⇒ 门恒过（CR-05-12） | CONFIRMED |
| :54 | `kFluxBiasMax` | 0.01 | 标量降级通量偏置上限 | 待确认 | PSFSW-T-FLUXBIAS | 同上 | CONFIRMED |
| :55 | `kWeightRangeMax` | 100.0 | 权重动态范围旗标阈 | 待确认 | PSFSW-T-WRANGE；本头 :285 声明「仅强制旗标, 不判 unavailable」⇒ **它不参与任何判定**（`exceeded` 无消费者，`git grep -n "weight_dynamic_range_guard" -- lib` 除本模块与单测外零命中） | 无下游 ⇒ 死旗标；若被接线，非正权重会让 range 保持 0 而漏旗标 | CONFIRMED |
| :56 | `kComponentFloor` | 1e-12（`component_flux_unit`） | 分量下限截断窗 | 待确认 | PSFSW-COMPOSITE-FLOOR；contains "1e-12" 不命中 SCI；单位随 `component_flux_unit`（默认 "ADU"，:121） | `Wt∝floor^{-(γ+δ)}` ⇒ 权重上界 `1e24`；近零背景帧可被钳位主导 | CONFIRMED |
| :57-60 | `kCompositeAlpha/Beta/Gamma/Delta` | 2 / 1 / 2 / 1 | 复合式四个指数 | 待确认（**SCI 自陈无标定记录**） | PSFSW-COMPOSITE-ALPHA/BETA/GAMMA/DELTA（PENDING，contains 全不命中）；`docs/science/PSF_SIGNAL_WEIGHT.md` §3：「指数（α=2,γ=2 vs 1,1）… 三处均不同… **尚无本项目 L1 合成数据标定记录**」 | 指数直接决定权重对四个分量的弹性；无标定⇒`W_psfsw` 的相对次序随指数变化而变 ⇒ 任何"该权重更优"的对外主张暂不成立 | CONFIRMED |
| :61 | `kCompositeCNorm` | 1.0 | 归一常数 | **结构性不适用**（该值被 :266-273 的组内 median 归一**完全吸收**）| `cnorm_invariance_deviation`（:335-346）即为证明 `C_norm` 简并的门；PSFSW-COMPOSITE-CNORM 登记 1.0 | 保留登记是合理的（版本化要求），但若某日去掉组内归一，`C_norm` 立刻变成量纲载荷 | CONFIRMED |
| :62 | `kCompositeMedianRtol` | 1e-9 | 组内 median==1 的容差 | 待确认 | PSFSW-T-EPSFTOL（value 1e-09，`contains "1e-9"` 不命中 SCI）；用于 :771/:873 | 对 double 归一是逐位级要求：次序差异即可假红（我在 psfsw.cpp 表已标 PARTIAL） | PARTIAL（缺：未实跑测 `median(w/median(w))` 的往返误差量级） |
| :63 | `kGroupMedianTarget` | 1.0 | 组内归一目标 | **公式导出** | `FZ-FORMULA-PSFSW-COMPOSITE`：`W_psfsw,k=Wt_k/median_j(Wt_j)` ⇒ 目标必然是 1；代码按式算（:271 `w[i]=wt[i]/med`） | —— | CONFIRMED |
| :64 | `kCompositeVersion` | `"PSFSW-COMPOSITE-V1"` | 复合版本 token | 结构性不适用 | 版本字符串，随产品落盘（`phase1_product.cpp:565/:730/:872`） | —— | CONFIRMED |
| :70 | `PsfswReason::none = 0` | 0 | 枚举显式零值 | 结构性不适用 | 显式赋值防枚举漂移；与 `FZ-GATE-PSFSW-FAILCLOSED` 的 5 成员白名单（:28-34 与之逐一对应，`is_whitelisted_reason` 无 "none"）一致 | —— | CONFIRMED |
| :83 | `enum class NCommonTier{hard_fail,low,standard,preferred}` | 4 值 | 分档基数 | 结构性不适用 | 与 :47-49 三个阈一起构成 4 档 | —— | CONFIRMED |
| :104-108 | `value=0.0`、`p05=p50=p95=0.0`、`valid_area_fraction=1.0`、`spatial_spread_rel=0.0`、`linear_trend_max_rel=0.0` | 0.0 / **1.0** | `ComponentMeasure` 成员默认值 | 待确认 | 无条文；与 :160-166 的空样本分支同族 ⇒ **默认即"满分"**（CR-05-12）；`valid_area_fraction=1.0` 作为"未测量"的默认是**乐观默认**，与同族 `psfsw.cpp:473`、`psfsw.h:357` 的显式 `depth_gate_evaluated=false` 反方向 | 保守做法应是 `valid_area_fraction=0.0` 或 `NaN`＋显式 `measured` 旗标 | CONFIRMED |
| :109-111 | `has_spatial_map/has_model/has_control_points = false` | false | 三个"做到了没有"旗标 | 结构性不适用（但**零消费者**） | `git grep -n "has_spatial_map\|has_control_points" -- lib eng` 除本头外无判定使用 | SCI §3「须拆 tile / 存 map/model」（登记册 :1124）要求"存下来"，代码只有旗标字段、无任何门读它 ⇒ 声明与判据脱空 | CONFIRMED |
| :116 | `a_nea`（`px^2`，由 dynamic_psf 提供） | —— | 等效噪声面积 | **公式导出** | `FZ-COND-WHITENOISE`：`A_NEA = 1/ΣP²`（本头 :9；由 dynamic_psf 侧计算，本文件不实现） | 本头 :116 注释「px^2, 由 PSF 归一算得」与 :9「由 dynamic_psf 提供」两种说法并存（前者说怎么算、后者说谁算，非矛盾） | CONFIRMED |
| :117-118 | `fhat` / `fhat_valid`（"空=全有效"） | —— | 逐星通量与掩膜 | 结构性不适用（空掩膜=全有效是声明的默认语义） | :118 明示；`psfsw.cpp:223` 按此实现 | 默认"空⇒全有效"是乐观默认，但同一结构里 `ExclusionFlags` 才是排除载体 ⇒ 两处默认不冲突 | CONFIRMED |
| :120 | `a_ref = 0.0; // 0 => a_nea` | 0→a_nea | 参考面积的静默回退 | 待确认 | 本头自述回退；SCI §3 只写 `B_k=b̄_k·A_ref,k`，未规定 A_ref 缺省 ⇒ 用 A_NEA 顶替 | 见 psfsw.cpp 表 :237 行（`B` 被整倍缩放 ⇒ `Wt∝1/B`） | CONFIRMED |
| :121/:350 | `component_flux_unit = "ADU"` | "ADU" | 四分量单位默认 | **待确认** | `FZ-UNIT-*` 条款族与 `DATA_SEMANTICS.md:70` 「pixel variance ≠ aperture variance」；G23（:917-928）强制 `Conc` 单位 = `flux_unit + "/px^2"` ⇒ 单位**串**被机器校验，单位**取值**无权威 | 默认 ADU 与 `docs/science/PHOTOMETRY.md:263` 对同类量写「ADU·px 或 e⁻ 同尺度」不完全同一（本批 CR-05-7/11 同族） | PARTIAL（缺：`DATA_SEMANTICS.md` §14 的 `F_instr` 单位行未逐字核对） |
| :122 | `estimator_version = "psfsw-est-v1"` | v1 | 估计器版本 token | 结构性不适用 | 版本化要求（SCI §3「稳健估计器和归一常数版本化」） | —— | CONFIRMED |
| :138 | 「样本不足 (<3) … → insufficient_valid_stars」 | 3 | 头注释复述的分量提取下限 | 待确认（与 :47 `kNCommonMin` 同值，但**注释里写的是字面 3 而非引用常数名**） | 同上 PSFSW-T-NMIN | 注释与常数当前一致；此类"文档复述数值"是漂移高发点（本批 `photometric_calib.h` 表已见两例） | CONFIRMED |
| :206-208 | `mag_min=0.0, mag_max=0.0`、`detection_threshold_sigma=0.0`、`matching_radius_arcsec=0.0` | 0.0 | selection function 数值默认 | 待确认 | 默认 0 ⇒ `!(mag_max>mag_min)` 与 `>0.0` 判据必然先命中 `incomplete_common_star_set`（:359-367）⇒ **默认值是 fail-closed 的**，这点正确；但一旦填了任意正数就再无数值合理性检查（CR-05-15） | 方向：默认偏红、填值后偏绿 | CONFIRMED |
| :254-256 | 门定义式（`K>=5 且(跨度>=1.0mag 或 n_common 变化>=2 倍)`；`max_t|W−median|/median<=0.05`；`|Spearman(scan index, W)|<=0.8`） | 5/1.0/2/0.05/0.8 | 头注释给出的 depth 门全式 | 待确认 | 本头是**唯一**把「2 倍」这一支路写下来的面（登记册无该条款、SCI 无该 token）⇒ 属"注释承载规范而权威面无记载"；且注释在此**明写**自变量是 `scan index`，与我 CR-05-14 的观察一致（注释自述，但未说必须按深度排序） | 该 2 倍是判据的一部分却不进登记面 ⇒ 改它不触发任何条款变更流程 | CONFIRMED |
| :270-271 | 「任一 (p95−p05)/p50 > limit (LOW 档 0.20) 或趋势 > 0.10」 | 0.20/0.10 | 非均匀门全式 | 待确认 | 同 :50-52 | 同 CR-05-12/14 | CONFIRMED |
| :281 | 「power_loss<=0.05 且 flux_bias<=0.01」 | 0.05/0.01 | 标量降级门 | 待确认 | 同 :53-54 | 同 CR-05-12 | CONFIRMED |
| :287 | `concentration = sum a_k^2 / (sum a_k)^2` | —— | 权重集中度（Herfindahl 型） | **公式导出** | 标准 HHI 定义；**但该字段无任何消费者**（`git grep -n "\.concentration" -- lib` 在 p1psfw 外零命中） | 死读数 | CONFIRMED |
| :303 | `alpha_k(p) = W_k v_k(p) / Σ_j W_j v_j(p)` | —— | coadd 组合系数式 | **公式导出** | `FZ-FORMULA-PSFSW-COMPOSITE`／`FZ-GATE-PSFSW-COV`；代码按式算（`psfsw.cpp:601-613`） | —— | CONFIRMED |
| :317 | `Var(I_out(p)) = Σ_{k,l} α_k α_l C_in(k,l)` | —— | 协方差传播式 | **公式导出** | 同条款 `C_out = R C_in Rᵀ`；文献 `PSF_SIGNAL_WEIGHT.md:149`（Fruchter & Hook 2002, PASP 114, 144；Zackay & Ofek 2017）；代码按式算（:630-637） | —— | CONFIRMED |
| :325-326 | `forbidden_psfsw_product_keys()` 14 项、`forbidden_weight_source_aliases()` 18 项 | 14 / 18 | 词法守卫集合基数 | 结构性不适用 | `psfsw.cpp:646-663`；本头 :6/:19 指回 `v6_clause_registry_v1.json#weight_vocabulary` 为唯一登记面（注释 :650 自述"扩展守卫…登记"） | 两处集合是代码内字面量，与登记册 JSON 之间**无机器比对**（我未找到比对测试；检索式 `git grep -n "forbidden_psfsw_product_keys" -- eng` 零命中）⇒ 与 CR-05-13 同一族风险 | PARTIAL（缺：未确认 `product_family_field_constraints.schema.json:2896` 的 enum 是否与这两个 C++ 集合逐项相同——值得实跑比对） |
| :344/:345/:347/:349/:350/:367/:368/:379/:391-393 | `PsfswRecord` 其余默认值：`weight_mode="psfsw_robust"`、`weight_kind="relative_dimensionless"`、`group_normalized=true`、`normalization_scope="group"`、`normalization_median_target=1.0`、`component_flux_unit="ADU"`、`valid=true`、`has_weight_value=true`、`effective_psf_only_fwhm=false`、`effective_psf_normalization_declared=true`、`baseline_claim=""`、`baseline_bootstrap_resamples=0`、`baseline_confidence=0.0` | 见左 | 记录族的字段默认 | **逐条待确认/结构性不适用混合**：token 类属枚举默认（结构性不适用）；`valid=true`＋`has_weight_value=true`＋`effective_psf_normalization_declared=true` 三个"乐观默认"须指出：三者各自都有配套判据把住（G12/G13 检查一致性、G07 检查 `effective_psf_id` 非空），故不成 finding；`baseline_*` 默认 0 走"空 claim ⇒ 不判"分支（`psfsw.cpp:937` 以 `!baseline_claim.empty()` 为门前提），合理 | `FZ-FIELD-PSFSW-UNIT`（token 面逐字一致）；`v6_clause_registry_v1.json:940` 附近 | —— | CONFIRMED |

**本文件数值项合计 31 行（含 :254-256/:270-271/:281 三条"注释即规范"的合并行）。**

### findings

### CR-05-16 · 本头把"2 倍星数豁免"这一判据支路只写在注释里，登记面与 SCI 均无记载
- 轴：C（主）／S
- 位点：`psfsw.h:254` —— `/* K>=5 且 (跨度>=1.0 mag 或 n_common 变化>=2 倍); 每点 n_common>=3;`；实现 `psfsw.cpp:445` `out.n_common_span_ok = (max_n >= 2 * min_n) && min_n > 0;`
- 上位依据：登记册 `PSFSW-T-DEPTH-SPAN` 只冻结 `1 mag`，`PSFSW-T-NMIN` 只冻结 `3`；**无任何条款承载 `2×`**（我逐条 dump 24 条 PSFSW 条款的 `id/value/unit` 全表，见 CR-05-13 的取证）；SCI §3 与 `docs/contracts/DATA_SEMANTICS.md:3354-3358` 亦零提及。检索式：`git grep -n "n_common_span\|2 \* min_n\|变化>=2" -- docs eng lib`。
- 现状：该 OR 支路直接决定 G11 是否放行；注释是本仓唯一写下它的地方。
- 差在哪：`psfsw.h:20-22` 自称本族全部阈值受 `PENDING_OWNER_SIGNOFF` 治理，而这个决定性的第二判据既无条款也无签字项 ⇒ 治理声明与实际覆盖面不符。
- 后果：一个测深跨度不足 1 mag 的组，只要星数掉过 2 倍就算过 depth 门；"深度稳定性"主张的实际判据与登记面不一致，签字链覆盖不到它。
- 定级：S2（门未接生产）
- 怎么算修好：给它一个条款（`PSFSW-T-DEPTH-NCSPAN`，value=2，unit=1，gate=PSFSW-G11，走 PENDING 流程）并在 `PSF_SIGNAL_WEIGHT.md` §7 的表里落一行（顺带满足 CR-05-13 的 locator 可命中要求）；或删掉该支路，只留 `kDepthMinSpanMag`。
- 置信：CONFIRMED

### CR-05-17 · 200 与 0.95 两个统计门以裸字面量写在门里，未进本头的冻结常数面
- 轴：C（主）／S
- 位点：`psfsw.cpp:940-941` —— `const bool evidence_ok = (rec.baseline_bootstrap_resamples >= 200 && rec.baseline_confidence >= 0.95);`（本头 `:42-64` 的冻结常数块内**无对应 `kXxx`**）
- 上位依据：登记面**有**这两条：`PSFSW-T-BOOT` value=200 unit=count、`PSFSW-T-CI` value=0.95 unit=1（均 `PENDING_OWNER_SIGNOFF`）；`psfsw.h:20-21` 的条款清单把 `-POWERLOSS, -FLUXBIAS, -NMIN/-NROBUST/-NPREF, -WRANGE, COMPOSITE-ALPHA..FLOOR` 列为受签字治理，未提 BOOT/CI。
- 现状：其余 20 余项判据全部经 `constexpr` 常数引用，唯独基线证据门的两数写死在门函数体内。
- 差在哪：同一模块内两套取值习惯 ⇒ 登记册与代码无常量绑定，改一侧不动另一侧（正是本仓 M3-A-008 裁决「同一科学常数须有单一权威值与可机器核对的定义点」所禁止的形态）。
- 后果：`PSFSW-BASELINE-NONINFERIOR` 主张的证据门不可追溯；负责人若按 SO-xx 否掉 200/0.95，改登记册不产生任何代码效果。
- 定级：S3（值与登记册当前一致，且门未接生产）
- 怎么算修好：在本头 `:42-64` 块内加 `constexpr int kBootstrapResamplesMin = 200; constexpr double kConfidenceMin = 0.95;`（带 `/* PSFSW-T-BOOT */`/`/* PSFSW-T-CI */` 注释，与本块其余写法同构），门内改引用；同批把两条 token 补进 `psfsw.h:20-21` 的条款清单。
- 置信：CONFIRMED

### 无可辩护指控的项（本文件）
- 词法守卫、枚举、token 默认值、`A_NEA`/`C_out=R C_in Rᵀ`/`alpha_k` 三式（代码逐符号按式算）、`C_norm` 简并证明函数、`median_of`/`quantile_of`/`rank_with_ties` 的定义式实现：均无超出上表的指控。检索式与判据已逐行并入 §2.5 表。

---

## 本批最高危三条

1. **CR-05-13**（S 轴）：24 条 PSFSW 数值条款的 `source_binding` 全部悬空——locator 0/24 命中、`contains` 12/24 命中、命中的又多是 `"5"`/`"200"` 这类无判别力子串；被点名的"冻结面" `docs/algorithms/GATES_AND_TOLERANCES.md` 含 `PSFSW` 的行数为 0；唯一机器校验只断言"文件存在"。后果：整个 psfsw 数值族（含复合式四个指数 `α=2,β=1,γ=2,δ=1`）的权威面是**自指**的，SO-07 签字拿不到条文；SCI 自己承认指数"尚无 L1 合成数据标定记录"。
2. **CR-05-6**（C 轴）：实现向 `PcMatchRecord.reject_reason` 写出域外值 **7**（饱和星），而本头 `:57-58` 与 `DATA_SEMANTICS.md:691`、`PUBLIC_API.md:513` 三处合同面都钉在 `0..6`，且 `pc_api.cpp:1157-1163` 原值透传无校验。后果：逐星归因链与枚举校验双双失真；这是 FIX-P1 引入新拒绝类时未回写合同的半截改动。
3. **CR-05-12**（R 轴）：`fill_summary` 的零样本分支把 `valid_area_fraction` 填成 **1.0**、`spread`/`trend` 填成 **0.0**，加上 `PsfswRecord` 的 `power_loss=0.0/flux_bias=0.0` 默认值，使 G18/G19 四条判据在"从未测量"时恒过——而同一模块的 G10/G11 用 `depth_gate_evaluated` 旗标做到了 fail-closed，说明这是遗漏而非取舍。方向已定：丢证据⇒度量变小⇒偏绿；现役 `phase1_product.cpp:415-418` 正是原样透传空样本，且 `valid_area_fraction=1.0` 已随 `comp_json` 写进产品。

## 我读不动或需要实跑才能定的

| 悬置项 | 缺哪一角 | 该跑什么（本节点禁跑，交前台/工程侧） |
|---|---|---|
| `kCompositeMedianRtol = 1e-9` 是否可达（G03/G15 会不会假红） | 未实测 `median(w/median(w))` 的浮点往返误差量级 | 用 `p1psfw` 的性质测试面加一条"n=1..200 随机正向量 ⇒ `|median(w_psfsw)-1|` 的最大偏差"扫描；若 >1e-9 则该容差属假红风险，须与 PSFSW-T-EPSFTOL 一并裁决 |
| `quantile_of` 用的是哪一种分位数约定、与测试 oracle 是否同一种 | 未与 `eng/tests/unit/v6_p1_psfw/p1psfw_tests_*` 的期望生成式对表 | 读 `p1psfw_fixtures.hpp` 与 Python/NumPy 侧 oracle（若有），比对 `p·(n−1)` 与 `inclusive/exclusive` 选择 |
| `product_family_field_constraints.schema.json` 的 enum 是否与 `psfsw.cpp` 两张禁词表逐项相同 | 未逐 token 比对（JSON 侧我只看了 :2896 附近一行） | 写一条独立比对（**不**经 `eng/**`）：JSON enum 集合 vs `forbidden_psfsw_product_keys()`/`forbidden_weight_source_aliases()` 的差集 |
| `out_pixels` 的单位权威（CR-05-11） | 块级单位声明在 `DATA_SEMANTICS.md` 与本批文件域之外 | 读 `DATA_SEMANTICS.md` §14 全文与 `photo_scaled_adu` 块定义，定 `scale` 有/无量纲两条口径谁是正本 |
| orchestrator 的 `photometric_match` 通道是否算"活代码"（决定 CR-05-1/6 能否升 S1） | `run_stage1`/`run_v2_with_timing` 在 CLI/scheduler 内零调用点，但 `CMakeLists.txt:485` 仍构建 `astrocs_infra_orchestrator`，且 `docs/architecture/execution_inventory.csv:4` 记其生产可达性为 yes | 由 `eng/tools/arch/build_production_execution_inventory.py` 重算并核对 `PRODUCTION_EXECUTION_INVENTORY.csv` 的 Photometric 行（本节点禁跑 eng/**）；若判"死通道"，则应删注册面并同步 SRC-PHOT-001 |
| `defaults.json` 的 `source_ref.line` 是否有漂移（与本批 CR-05-13 同族） | 我只核到 2 条：`photometry.mag_tolerance` 指 `PHOTOMETRY.md:30`、`photometry.tukey_c` 指 `:21`/`source_ref.line=24`，而实际行分别是 **:34**（`mag_tolerance` 行）与 **:31**（`c` 行 Tukey 4.685）；`:30` 落的是 `S` 行、`:24` 落的是 `F_syn` 行 | 全表逐条按 `file:line` 读出该行原文与 `note/source` 括号内描述比对（`CONFIG_CONTRACT.md:36` 自述门只断言"文件:行必须真实存在"，不比内容） |
| 孔径/天空环默认 4.0/6.0/10.0 与 FWHM 的耦合 | 该符号不接收 FWHM，无实跑对拍 | 若 P1-PHOT-IMPL 要接线 `Photometer`，先补一条"r=4px 相对完整孔径的通量损失随 FWHM 曲线"（正例+负例），再定默认值；现值已由 `config_registry.json` 归"负责人裁决" |



## 收工 status --porcelain 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
$ git rev-parse --short HEAD
c8f64e9a
```

与开工逐字相同 ⇒ 被审工作树在本批只读通读中未被改动（本批未跑构建/ctest/门禁/任何 `eng/**`，未 `import` 仓库 Python，未写 `git config`；自备的一次性 `python3 -c` 只读 `git show HEAD:<file>` 与本仓 JSON，不落中间件文件，故未建 `独立审计/复算件/CR-05/`）。

## 本批交付计数（非判据，仅清点，见派单 §0"数量统计不作为交付"）

6/6 文件整读完毕；§2.5 处置表 6 张、共 102 行数值处置（3+14+7+21+26+31）；findings 17 条（CR-05-1 … CR-05-17），其中 S2 九条、S3 八条，**无一条定 S1**——降级理由统一登记在各条"定级"行：本批文件的两条生产通道（orchestrator 的 DLL 通道、`validate_psfsw_record` 等四道门）在跟踪集内均无 CLI/调度器调用点，故"可致产物错值"的触发路径写不出来；能写出触发路径的产物面失真只到"产品 JSON 里写了 `valid_area_fraction=1.0`"这一层（CR-05-12），已按判据把方向与限度如实写明而非拔高。

派单文字与清单一致（6 个路径逐条对得上，无差异需指出）。
