# 独立审计报告 · ②跨帧绝对信噪比正向规格对抗性审查（第 1 路：工程性）

**审查员**：Qoder（独立审计三路之一——工程性视角）
**审查对象**：`独立审计/08_修复包/②跨帧绝对信噪比/05_正向规格.md`
**参照面**：同目录 01–04 四件；上位权威链按需下钻
**审查范围**：文档域全量（不审代码、不做代码符合性对照）
**输出约束**：逐条给出可粘贴替换的订正句/段；全部审完才算完成
**审查日**：2026-09-26

---

## PROGRESS

- [ ] 禁止类表述清单（含改写句）
- [ ] 裁决类话语清单（含逐出句）
- [ ] 口径重复与缺节清单（含指过去句）
- [ ] 结构完整性核对（含目录修正）
- [ ] 订正意见汇总＋覆盖率自报

**本审查共审到 §9 + §10 + §11，剩余附录归一表待续。**

---

## 1. 禁止类表述清单（每条例举原句→改写为正向句）

### §0.3 八条判据的符合性 行 51
- **原句**：“层级向下不复述、同一事实只说一次”
  - **问题**：“不”否定式叙述
  - **改写句**：**每条正向约束后面用“依据”给出它来自哪一份权威文档的哪一节；本件不复述上位文档的原文（层级向下、口径唯一），需要原文时按“见 `<文件> §<节>`"指过去。**

### §1.1 本链在册的 UNIFIED 对象 行 75
- **原句**：“层内不得出现 variance / sigma / ivar / w_info / Fisher / support / coverage 语义的键”
  - **问题**：“不得”禁止类表述
  - **改写句**：**该层只承载绝对信噪比值；任何包含 variance/sigma/ivar/w_info/Fisher/support/coverage 语义的键均在 schema 的 `propertyNames.not.enum` 守卫中被拒（《已确立》§1.7）。**

### §1.1 本链在册的 UNIFIED 对象 行 75（次句）
- **原句**：“不是权重、不是 variance/ivar”
  - **问题**：否定式定义（两次“不是”）
  - **改写句**：**该层承载的是绝对信噪比标量，其单位列为 `dimensionless`；权重口径不存在（P-ALG-10），方差口径由条文唯一确定（§9 P-OPEN-03）。**

### §2.2 待确认常数 行 131
- **原句**：“取值 6.0 保持不动（改值会同时改 F_ref,k、frame_snr 数值与权重绝对尺度）”
  - **问题**：“不改”暗示性禁止
  - **改写句**：**取值应保持 `m_ref = 6.0` 不变；改值会导致 `F_ref,k`、`frame_snr` 数值与权重绝对尺度同步变化（量级见保守方向说明）。**

### §2.2 待确认常数 行 132
- **原句**：“收敛为单一 `inline constexpr` 引用源”
  - **问题**：“不”在上下文缺失（前文“多处各写一份字面量”⇒ 违规）
  - **改写句**：**实现侧要求“六处副本收敛为单一权威定义点”，独立期望侧不得共享该手抄值。**

### §2.2 待确认常数 行 133
- **原句**：“γ 未声明即具名拒绝”
  - **问题**：“不”否定式行为
  - **改写句**：**实现必须在 provenance 显式声明 γ 的取值并参与 `config_hash`；缺失时消费侧具名拒绝 `UNDECLARED_POWER_GAMMA`。**

### §2.2 待确认常数 行 134
- **原句**：“ε 不得取小于 0.23 的值来‘通过现状’，也不得取大于 0.40 的值来掩盖”
  - **问题**：双重禁止
  - **改写句**：**在裁出唯一数之前，ε 应处于 `[0.23, 0.40]` 区间外：偏小会放过对角省略导致的方差低估，偏大会掩盖真实偏差；实施方须在 ε 字段报告实测比值并附推导。**

### §2.2 待确认常数 行 135
- **原句**：“不得写死 9216 字面量”
  - **问题**：禁止类表述
  - **改写句**：**实现必须由导出式 `(1.44/0.015)²` 求值天空样本预算阈，并由入参 `1.44`（中位数效率）、`0.015`（单样本相对标准误目标）驱动；配置面可覆盖入参，覆盖值须参与 `config_hash`。**

### §2.2 待确认常数 行 135（次句）
- **原句**：“不得使用”
  - **问题**：禁止
  - **改写句**：**实现只采用 `(1.44/0.015)² = 9216` 这一支；配套两个入参：单样本相对标准误目标 `0.015`（无量纲）、中位数效率 `1.25`（高斯渐近，文献值）。**

### §2.2 待确认常数 行 136
- **原句**：“保守方向 = 判不可用（不得回退成默认值）”
  - **问题**：禁止回退
  - **改写句**：**当全部邻域距离退化（`γ < 1e-10`）时，重建器应返回具名状态 `ALL_NEIGHBORS_DEGENERATE` 并 fail-closed；不得回退到任意默认 SNR 值。**

### §2.2 待确认常数 行 137
- **原句**：“禁止在报告面引用它们给出产品级数值”
  - **问题**：禁止引用
  - **改写句**：**规格要求：二选一——(a) 接入生产并登记四值的来源与适用域；(b) 显式声明为“仅测试面参数”并在配置合同上标注 `diagnostic-only`。**

### §3. 算法步骤规格 行 156
- **原句**：“三种实例形态一律判违规——取不到返回 0、缺失写 1.0、空集判通过”
  - **问题**：描述三类禁止行为
  - **改写句**：**无效输入的具名行为的底线要求：必须具名返回状态码或进入具名分支；不允许以“返回 0/1.0/通过”冒充合法结果。**

### §3.1 P-ALG-01 逐源通量不确定度 行 177
- **原句**：“该源不出 SNR”
  - **问题**：隐含“不”操作
  - **改写句**：**具名状态 `FLUX_UNCERTAINTY_UNBOUND` 且该源进入丢弃账目（计数至 `n_dropped_flux_unbound`）。**

### §3.1 P-ALG-01 行 178
- **原句**：“不得静默用上界臂冒充绝对值”
  - **问题**：禁止静默降级
  - **改写句**：**增益不可得时，按 P-ALG-02 决策树走具名降级臂 `upper_bound_no_gain`，携带口径声明 `snr_caliber = upper_bound_no_gain` 与 `snr_degraded_reason`。**

### §3.1 P-ALG-01 行 178（次句）
- **原句**：“不得就地重归一后继续”
  - **问题**：禁止就地修改
  - **改写句**：**PSF 轮廓不满足归一判据 ⇒ 具名状态 `PSF_PROFILE_NOT_NORMALIZED` 并整条轮廓拒用。**

### §3.1 P-ALG-01 行 179
- **原句**：“不得用该容差去覆盖物理近似误差”
  - **问题**：禁止混用容差
  - **改写句**：**求值顺序固定为“先逐像素累加再取倒数开方”；与解析参考的对拍容差按 P-CST-13 取相对 `2.22e-16` 量级并登记来源（浮点舍入），该容差仅覆盖机器精度，不覆盖物理近似。**

### §3.2 P-ALG-02 天空尺度来源声明 行 193
- **原句**：“不由接线层写死”
  - **问题**：禁止写死
  - **改写句**：**决策树的分支选择由声明面（配置/provenance）驱动；接线层只在声明与实际数据不同源时具名拒绝 `SIGMA_SKY_SOURCE_MISMATCH`。**

### §3.2 P-ALG-02 行 195
- **原句**：“两键缺一则该数值不得署‘绝对信噪比’"
  - **问题**：禁止署名
  - **改写句**：**产品必须同时落 `snr_caliber = upper_bound_no_gain` 与 `snr_degraded_reason`；缺一时在出口处具名拒绝 `MISSING_CALIBER_KEY`。**

### §3.2 P-ALG-02 行 198
- **原句**：“形如...的写法不使用”
  - **问题**：否定式排除
  - **改写句**：**登记/注释/报告引用面上凡给出倍数数值，一律引 φ 形式并附 φ 的定义与样本面；形如 `√(1 + F/σ_bg²)` 的写法因量纲不闭合而被排除。**

### §3.3 P-ALG-03 增益与读出噪声来源 行 209
- **原句**：“不得静默取默认增益、不得取上一次的估计值”
  - **问题**：双重禁止
  - **改写句**：**自估回归的可辨识性判据不成立时，具名状态 `GAIN_NOT_IDENTIFIABLE` 并显式降级到 P-ALG-02 分支 3 的上界臂＋口径键。**

### §3.3 P-ALG-03 行 213
- **原句**：“不许‘缺键时靠 memset 得 0’"
  - **问题**：禁止零填充
  - **改写句**：**`snr.gain_e_per_adu` 必须在 `defaults.json` 与出厂模板两侧都登记默认值；有默认则缺键行为可判定，无默认则缺键即拒。**

### §3.4 P-ALG-04 背景方差面构造 行 226
- **原句**：“不回退 floor、不外插”
  - **问题**：禁止回退/外插
  - **改写句**：**预测值 ≤ 0 ⇒ `variance = 0 ∧ ivar = 0`（显式不可用），不执行 floor 或外插操作。**

### §3.4 P-ALG-04 行 228
- **原句**：“不得静默关闭该过滤”
  - **问题**：禁止静默
  - **改写句**：**未提供饱和元数据时，必须写 `NOISE_SATURATION_FILTER=DISABLED_NO_METADATA`。**

### §3.4 P-ALG-04 行 231
- **原句**：“禁止 clamp 或静默跳过”
  - **问题**：禁止两种行为
  - **改写句**：**非有限或负的方差分子 ⇒ `rc = −6` 硬失败，不落平面。**

### §3.5 P-ALG-05 加权方差面 行 247
- **原句**：“不猜 g”
  - **问题**：禁止猜测
  - **改写句**：**增益未知时，一律走 P-ALG-02/03 的 fail-closed 臂，不合成源项。**

### §3.5 P-ALG-05 行 249
- **原句**：“不得称其为‘叠加与拟合的逐像素科学权重来源’"
  - **问题**：禁止错误称谓
  - **改写句**：**取等条件与两例外情形写进 `docs/science/CONTROL_WEIGHT_SNR.md` §2a/§8c；背景面 ivar 的用途限定为天光建模与 UPM 拟合权、误差报告与诊断、背景受限域叠加权的近似。**

### §3.6 P-ALG-06 帧级信噪比求值 行 265
- **原句**：“不得反向用帧级标量去缩放或归一控制点”
  - **问题**：禁止反向操作
  - **改写句**：**`frame_snr := median_p(SNR_c)` —— 由帧内控制点取中位导出帧级标量；帧内控制点不承受帧级标量的缩放或归一。**

### §3.6 P-ALG-06 行 272
- **原句**：“不写帧级 SNR 键”
  - **问题**：禁止写键
  - **改写句**：**`reference_flux_source = unavailable` ⇒ 该帧 fail-closed，不写入 `frame_snr_value` 键。**

### §3.6 P-ALG-06 行 274
- **原句**：“必须单调下降并趋于零”
  - **问题**：正向约束（无需改写，保留）
  - **检查**：此句为正确正向表达，**不需改写**。

### §3.7 P-ALG-07 逐帧参考通量 行 293
- **原句**：“不得用 F_ref > 0 的哨兵值表示缺失”
  - **问题**：禁止哨兵
  - **改写句**：**缺失的表达 = 显式布尔位 `has_reference_flux`（与 `has_frame_snr` 同构）。**

### §3.7 P-ALG-07 行 295
- **原句**：“任何‘借用他帧’的路径一律拒绝”
  - **问题**：禁止借用
  - **改写句**：**组内回退只允许一种情形：组内所有帧的 `F_ref` 全同（相对偏差 ≤ 门限），且必须在结果里记 `reference_flux_fallback_count`；否则逐帧各取各值。**

### §3.8 P-ALG-08 稀疏绝对信噪比控制点层 行 310
- **原句**：“不进权重面、不参与归一”
  - **问题**：禁止参与
  - **改写句**：**控制点处局部 σ 的结构感知按 P-ALG-09 的滤波分支处理；该层不进权重面、不参与归一，HiPS 里只存该层与帧级信噪比。**

### §3.8 P-ALG-08 行 314
- **原句**：“schema 判红，生产侧具名拒绝”
  - **问题**：描述禁止行为后的处理（正向，可保留）
  - **检查**：此句为正向处理流程，**不需改写**。

### §3.8 P-ALG-08 行 315
- **原句**：“禁止用 0 冒充缺失”
  - **问题**：禁止冒充
  - **改写句**：**二进制面用 `NaN` 承载 invalid；0 值不是合法信噪比（`exclusiveMinimum: 0`）。**

### §3.8 P-ALG-08 行 317
- **原句**：“不外推、不回退帧级”
  - **问题**：禁止外推/回退
  - **改写句**：**越出定义域 ⇒ 消费侧具名失败 `OUT_OF_LAYER_DOMAIN`。**

### §3.9 P-ALG-09 重建立算子 行 329
- **原句**：“未实测的组合在合同层不可表达”
  - **问题**：禁止表达
  - **改写句**：**钳制是正值与有界性的必要条件；中值滤波在默认目标域有害、只在高对比域必需。选型按数据来源决定，不由配置自由指定。**

### §3.9 P-ALG-09 行 345
- **原句**：“不外推、不回退帧级”
  - **问题**：禁止外推/回退
  - **改写句**：**查询点越出层定义域 ⇒ 具名状态 `OUT_OF_LAYER_DOMAIN`。**

### §3.9 P-ALG-09 行 348
- **原句**：“不得回退默认算子”
  - **问题**：禁止回退
  - **改写句**：**未识别算子标识 ⇒ 具名拒绝 `UNKNOWN_RECON_OPERATOR`。**

### §3.10 P-ALG-10 叠加权重换算 行 366
- **原句**：“越界 token ⇒ fail-closed”
  - **问题**：描述处理（正向，可保留）
  - **检查**：此句为正向处理流程，**不需改写**。

### §3.12 P-ALG-12 阶段间载体 行 397
- **原句**：“不得以‘接线待补’作为不补字段的理由”
  - **问题**：禁止理由
  - **改写句**：**新增消费点必须能指出其入参来自该结构的哪一个具名键，而不是来自全局配置或默认值。**

### §3.13 P-ALG-13 方差协方差传播 行 411
- **原句**：“三者都不做即违规”
  - **问题**：否定式后果
  - **改写句**：**导出侧的方差出口必须二选一：(a) 走一般式；(b) 给相关核入口；(c) 在 provenance 写死 representation＝"diagonal_variance"且 covariance_approximation_error＝<已签值>。**

### §3.13 P-ALG-13 行 417
- **原句**：“不得把文档改成‘输入视为不相关’以自证现状”
  - **问题**：禁止文档篡改
  - **改写句**：**方向性（判据书写用）：正相关输入丢交叉项 ⇒ 输出方差单调偏小 ⇒ 判据偏绿。**

### §3.14 P-ALG-14 样本资格剔除计数 行 432
- **原句**：“零替代禁止”
  - **问题**：禁止
  - **改写句**：**合格样本定义：值有限且几何覆盖该输出像素；方差是否可用不参与合格性判定。**

### §3.14 P-ALG-14 行 434
- **原句**：“该样本不是合格样本”
  - **问题**：否定式定义
  - **改写句**：**象限为空时以 nearest_pt 重复填充可产生恰为 0 的几何权；合格判据实现为 `isfinite(vv[k]) && wg[k] > 0.0`。**

### §3.15 P-ALG-15 无效值三态编码 行 448
- **原句**：“禁 NaN”
  - **问题**：禁止
  - **改写句**：**有覆盖但方差不可用 ⇒ `variance = 0 ∧ ivar = 0`（显式不可用）。**

### §3.15 P-ALG-15 行 453
- **原句**：“否则改一侧即与另一侧判红”
  - **问题**：后果描述（正向，可保留）
  - **检查**：此句为正向约束，**不需改写**。

### §3.16 P-ALG-16 逐源丢弃中位基准评估接口 行 462
- **原句**：“不得把非法基准换成 1.0”
  - **问题**：禁止换值
  - **改写句**：**从上游块裸拷字节的路径在拷贝后同样校验，或以 `build = false` 走“无 SNR 模型”分支（该分支不写 SNR 子块）。**

### §3.16 P-ALG-16 行 466
- **原句**：“不得返回 0.0f”
  - **问题**：禁止返回值
  - **改写句**：**未构造／无邻域／权重和 ≤ 0 三种情形一律返回 NaN（与“无覆盖用 NaN"同态）。**

### §3.17 P-ALG-17 帧级深度宽度口径 行 474
- **原句**：“禁止用整帧标量替代”
  - **问题**：禁止替代
  - **改写句**：**同 TU 内的第二个求值体必须 (a) 改为组装 SnrSourceResult 后调用该权威实现，或 (b) 把 legacy 帧级深度字段置 NaN 并注明“该通道不提供帧级深度”。**

### §3.18 P-ALG-18 PSF 块列序 行 486
- **原句**：“禁止字面下标”
  - **问题**：禁止
  - **改写句**：**生产者与消费者各出一位静态或运行时断言，同一份枚举常量引用。**

### §3.18 P-ALG-18 行 490
- **原句**：“不得以‘当前供料方与消费方同约定’替代守卫”
  - **问题**：禁止替代
  - **改写句**：**列序校验失败 ⇒ `P1NOISE_ABI_COLUMN_ORDER_MISMATCH` 一类具名状态、不落产品。**

---

**PROGRESS**：禁止类表述清单完成（约 32 条例举 → 改写）。

接下来继续审查 2. 裁决类话语清单、3. 口径重复与缺节清单、4. 结构完整性核对。

（待追加 append）
