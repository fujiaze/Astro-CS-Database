# 复核-V401-A · 第②层独立复核（批次 V401-A）

- 复核对象：`独立审计/证据/AUD-401-架构对齐.md`（全文 452 行）
- 本批条目（6 条，依 `独立审计/批次清单/V401-A.txt`）：AUD401-002、-003、-004、-011、-013、-015
- 基线：HEAD=`c8f64e9a`
- 只读纪律声明：本机为独立只读审查节点；未修改/移动/删除仓库任何文件；git 仅用 `status`/`ls-files`/`grep`/`log`/`show`；**未执行**构建、`cmake` 配置、`ctest`、`run_checks.py`、任何 `eng/**` 脚本、任何端到端；未 import 仓库内 Python；判"在构建图里"一律读 `CMakeLists.txt` 源文本与 target 源清单逐跳得证；`rg` 本机不可用，检索一律 `git -C "..." grep`。
- 开工 `git status --porcelain` 原文：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## AUD401-002 · 阶段名大量出现在代码目录与 CMake target 名（应只存在于设计与命令层）
**判定：降级**（机制与三个计数全部独立复现成立；但"违反权威条文"的范围要收到"目录"一档，target 名/文件名属推论而非条文直证）
- 我的独立取证（先于读成稿判定段）：
  - `git ls-files lib | grep -E "phase[123]" | grep -v "/tests\?/" | sed 's|/[^/]*$||' | sort -u | wc -l` = **14**（与成稿同数）；逐条看这 14 行，其中 `lib/algorithms/integration/v6/include/astrocs/v6`、`lib/algorithms/integration/v6/src`、`lib/infrastructure/pipeline/fixtures` **三条的 token 来自文件名不是目录名**（如 `integration/v6/src/phase2_integrate.cpp`），真正"阶段名目录"是 11 条。
  - `git ls-files lib | grep -vE "/tests\?/" | grep -cE "(phase[123]|/p[123]_)"` = **81**（同数）。
  - target 名我自算：`git grep -h -E "^[[:space:]]*(add_library|add_executable)\(" -- 'CMakeLists.txt' '*/CMakeLists.txt'` → 去重后筛 `^(astrocs|acsd)` 且含 `phase|_p[123]` = **22 个，与成稿逐个同名**。
  - 权威条文原文核对：工包 `standards/04_代码梳理标准.md:18`「阶段（normalize/mosaic/export）是设计与命令层概念，**不在代码目录上直接体现**」、`:20`「归并后独立会话目录退役」；仓库侧 `ASTROCS_DESIGN.md:637`（树里列 `phase{1,2,3}_session/`）、`:655`「科学算法目录按并联命名、不用 phase，会话层目录**可用** `lib/phase{1,2,3}_session`」。
  - 打架登记成立，且成稿**漏引一处更强的仓库侧文字**：`ASTROCS_DESIGN.md:489`「`phase1/2/3` 仅为内部指代……**会话层目录可用 `lib/phase{1,2,3}_session` 命名（现状即此）**」——"现状即此"是最高设计对现状的追认，比 :655 更硬。
  - `git grep -n "命令层概念"` 在**跟踪面 0 命中**（同式自检 `git grep -n "科学算法目录按并联命名"` 命中 `ASTROCS_DESIGN.md:489/:655`）⇒ 标准04 只存在于工包目录、不在被审仓库内，引用必须写明是工包判据（成稿已这样标，本条复核确认其性质）。
- 与成稿差异：①范围——条文约束对象是"代码目录"，`astrocs_*` target 名与 `p3_*.cpp` 文件名不落在 §1:18 的字面辖域内，属"按精神推论"，应与目录项分档陈述；②计数语义——"14 个目录"里 3 条是文件名带 token，写成"14 条含 phase 的非测试目录路径"字面不错但易被读成 14 个阶段名目录（真值 11，其中 3 个是最高设计明文允许的 session 目录）；③机制与改法（W1 拆 `astrocs_phase2`、`p3_*` target 改模块名）无异议。
- 影响 D8 的处置：进任务书（保留 W1 改名批次，按"目录 / target 名 / 文件名"三档分别写判据与依据），"权威打架"那半继续上呈裁（补 `ASTROCS_DESIGN.md:489` 为第三处引文）。

## AUD401-003 · phase1/2/3_session 仍在构建图内并被生产可执行 `acsd` 直接链接（未退役）
**判定：确认**（钉在"构建图 / 链接声明"这一轴；不得外读成"运行期执行"）
- 我的独立取证：只读 `CMakeLists.txt` 源文本与 target 源清单，**未配置、未构建、未跑任何 eng 脚本**。
  - `CMakeLists.txt:913` `add_library(astrocs_phase1_session STATIC lib/phase1_session/p1_session.cpp)`；`:925` `astrocs_phase2_session STATIC lib/phase2_session/p2_session.cpp`；`:933-943` `astrocs_phase3_session STATIC lib/phase3_session/p3_session.cpp` ⇒ 三 target 在根图内，各 1 个源 TU。
  - `CMakeLists.txt:1008-1016` 即 `target_link_libraries(acsd PRIVATE ...)`，其中**第 1012 行**原文就是 `astrocs_phase1_session astrocs_phase2_session astrocs_phase3_session` ⇒ "被生产 exe 直接链接"逐字成立。
  - 第二跳：`:1068-1076` `target_link_libraries(astrocs_module_adapters PUBLIC ...)` 的**第 1070 行**同列三库；`:1092-1093` `astrocs_cli_runtime` 链 `astrocs_core astrocs_module_adapters`；`:1009` `acsd` 链 `astrocs_cli_runtime` ⇒ 传递路径闭合。
  - `:1031-1034` QA-001 严格 warning 名单确含三个 session target，`:1039` 另对 `acsd` 单独加严 ⇒ 构建脚本自身把它们当生产 target 对待。
  - "未退役"：`:912` 只有 `# phases sessions (facade, 后续迁移为 preset)` —— 缺工包标准 `04_代码梳理标准.md:39` 要求的**退役原因 / 替代实现 / 日期**三要素，也不是删除 ⇒ 二选一都不满足。
- 我这一轮补的边界（成稿未写）：自有构建**不使用 `--whole-archive`**（`git grep -n "whole-archive" -- CMakeLists.txt eng/cmake lib` 只命中 `lib/infrastructure/aio/third_party/cfitsio/configure` 的 autotools 文本）⇒ 静态库成员按"能否解析未定义符号"抽取。`p1_session_create/validate/destroy` 在生产侧唯一引用点是 `module_adapters.cpp:1194-1198` 的 `P1Api`（`git grep -n "p1_session_create" -- lib | grep -v lib/phase1_session` 只回 module_adapters 的声明 :265-269 与 P1Api 体内转发），而 `P1Api` 零使用 ⇒ **p1_session.cpp 是否真进 `acsd` 二进制未证**，只有 CMake 链接声明进图；P2/P3 因 `:15447/:15455` 有真实引用，其 TU 确会进闭包。此点须实跑 `nm`/`dumpbin` 定案，本阶段测试暂停 ⇒ 记"需复测"，不影响本条三判断。
- 与成稿差异：机制、行号、定级无差异；措辞需分轴——成稿 §四 用"链接但不执行"方向正确，但标题级文字"被生产可执行 acsd 直接链接"若不与 AUD401-004 显式对照，会被下游读成"生产在跑 session"，从而把整改动作误写成"接线"而非"删除"。
- 影响 D8 的处置：进任务书（R 组退役 + G1/G2/G3 归并照旧）；另加一条须复测项：删 session 链接后 `eng/tests` 中显式链 `astrocs_phase*_session` 的 target 需同步改链（成稿 §四已登记同一风险）。

## AUD401-004 · session 不在生产调用面上：p1 连注册都没有，p2/p3 以错误的 resample ID 注册且生产 IR 永不引用
**判定：确认**（钉在"运行期调度 / 引用"这一轴；与 003 不互斥，同时为真；但成稿给出的一条**取证命令逐字复跑不成立**）
- 我的独立取证：
  - `git grep -n "make_session_module"` 跟踪面命中：定义 `lib/infrastructure/scheduler/src/module_adapters.cpp:1188`，**调用只有 2 处**：`:15447` `make_session_module<P2Api>`、`:15455` `<P3Api>`；其余命中全在 `artifacts/`、`eng/ci/ledgers/`、各 `README/memory.md` 的历史叙述 ⇒ P1 无工厂注册成立。
  - `git grep -n "P1Api"` 在 `lib/**` 代码里只命中定义行 `:1193`（六函数 struct），无任何实例化点；对照 `git grep -n "P2Api\|P3Api"` 命中 `:1201`/`:1209` 定义 + `:15447`/`:15455` 使用 ⇒ "p1 纯 linked-but-unreachable、p2/p3 只挂注册位"成立。
  - 注册段实测：`module_adapters.cpp:15438 register_phase_modules()`；`:15443-15448` 把 `phase2_descriptor()` 注册为 `astrocs.phase2.resample`；`:15451-15456` 把 `phase3_descriptor()` 注册为 `astrocs.phase3.resample`；descriptor 体 `:711-728`（`d.module_id = "astrocs.phase2.resample"` 在 **:713**，sci/alg/data/test = `SCI/ALG-P2-RES-001`、`DATA-P2-RES`、`TEST-P2-RES-001`）、`:730-745`（`"astrocs.phase3.resample"` 在 **:732**，ports `hips`/`tile`，id 仍为 `*-P3-RES-*` 占位）。
  - 生产 IR 两条链（`lib/infrastructure/cli/runtime_client.cpp`）：phase2 = `:236-242` `astrocs.phase2.{coverage,sample,upm-fit,upm-apply,reject,integrate,write}`；phase3 = `:268-272` `astrocs.phase3.{properties,wcs,resample2,writer,verify}` ⇒ 两条链都不含上述两个注册 ID。
  - 入口面封闭性（我补的一环，成稿未证）：IR 只由 `build_pipeline_ir` 生成——`git grep -n "build_pipeline_ir" -- lib` 仅 5 处：定义 `runtime_client.cpp:104`、声明 `runtime_client.h:19`、调用 `commands.cpp:1015`、`commands.cpp:1730`（validate/plan 前置，注释 `:1707` 自述"零 Runtime 实例化、零科学执行、零 I/O 产物"）、`runtime_client.cpp:424` ⇒ **无"用户自带 IR 文件"的注入口**，占位 ID 不存在被外部 IR 调起的运行期路径。
  - 会话函数的生产引用面：`git grep -n "p[123]_session_run" -- lib | grep -v "/tests/"` 只命中声明 `module_adapters.cpp:267/273/279` 与三个 Api 的转发体 `:1196/1204/1212` ⇒ 除 `SessionModule` 外无第二调用者。
  - 台账：`grep -c '"module_id"' eng/ci/ledgers/registry_ir_parity.json` = **2**（`:12` `astrocs.phase2.resample`、`:20` `astrocs.phase3.resample`），理由文字自证"该模块永不被调度"。
  - 我这一轮新增两点精度：① `ModuleRegistry::register_factory` 会**立即实例化一次**做探针（`lib/infrastructure/scheduler/src/module.cpp:111-112` `auto probe = factory();`，注释"拒绝只注册 metadata 的假模块"），而探针只走 `SessionModule` 构造函数（`module_adapters.cpp:539-548` 仅存函数指针，不调 `fn_create`）⇒ 生产启动期确实**构造**两个 session 模块对象、且它们进入模块索引导出面（`module.cpp:156-172`，`mj["executable"] = has_factory(id)`），但 `validate_config/plan/execute`（`:552/:573/:584`）永不被调用 ⇒ "从不执行"要精确写成"**其会话编排函数从不被调用**"；② 该探针同时把 P2/P3 的会话 TU 拉进 `acsd` 闭包（有真实符号引用），与 003 里 p1 的"无引用点"形成对照。
- 与成稿差异：机制、范围、定级无差异。**差异在取证命令**：成稿写 `git grep -n "astrocs.phase2.resample\|astrocs.phase3.resample" -- lib/infrastructure/cli` → "0 命中"，我逐字复跑得 **1 命中** = `runtime_client.cpp:270` 的 `"astrocs.phase3.resample2"`（`resample` 是 `resample2` 的前缀，正则与固定串都吃这个子串）。结论方向不变（生产链引用的是 `resample2`，不是占位 ID），但坐实须换成带边界的写法：我用 `git grep -nF -e "astrocs.phase2.resample" -e "astrocs.phase3.resample"` 全跟踪面命中集里 `lib/infrastructure/cli/` 下确为 0。
- 影响 D8 的处置：进任务书（R1 删 `module_adapters.cpp:1187-1216` 三个 Api + 模板、R2 删 `:711-760` 两个占位 descriptor 与 `:15443-15456` 注册，台账 2 条转"已解除"），并把订正后的检索式写进任务书复算步骤。**与 003 的分轴结论**：003=构建图/链接声明轴，004=运行期调度/引用轴，"在链接闭包里"≠"生产运行期真引用"，两条同时为真，不是互斥，不判任何一条推翻。

## AUD401-011 · 注册了但无实现 / 有实现但未注册 / 模板复制残留占位 descriptor
**判定：确认**（三侧事实独立复现成立；两处行号锚要订正，且"有实现但未注册"一侧与 AUD401-015 是同族事实的双列，须定主从）
- 我的独立取证：
  - **注册但无实现**：`git ls-files lib/algorithms/upm lib/algorithms/rejection lib/algorithms/sampling lib/algorithms/coverage/hips_p2` = 13 文件、`grep -cE "\.(cpp|c|cc|cxx)$"` = **0 TU**；注册身份确有：`lib/algorithms/upm/module.yaml:59 id: MOD-astrocs-phase2-upm-fit`、`rejection/module.yaml:42 id: MOD-astrocs-phase2-reject`、`sampling/module.yaml:48 id: MOD-astrocs-phase2-sample`、`coverage/hips_p2/module.yaml:41 module_id: astrocs.p2.hips_writer`。实现实住在 coverage：`CMakeLists.txt:625/628/630` 把 `lib/algorithms/coverage/src/{upm,rejection,sampler}.cpp` 列进 `astrocs_phase2`，而生产 IR 对应节点是 `runtime_client.cpp:238/240/237` 的 `astrocs.phase2.{upm-fit,reject,sample}` ⇒ "有注册身份无实现体、实现寄在别的模块"成立。
  - 补一条成稿未写的佐证：`lib/algorithms/upm/module.yaml:60 module_id: astrocs.p2.upm` 与生产 IR 用的 `astrocs.phase2.upm-fit` **不同形** ⇒ 空壳目录连"它注册的是哪个可调度 ID"都与生产链不一致。
  - **占位 descriptor（模板复制残留）**：`module_adapters.cpp:711-728`（`phase2_descriptor`，`:713` `astrocs.phase2.resample`，`:722-726` SCI/ALG/DATA/API/TEST 全为 `*-P2-RES-*`）、`:730-745`（`phase3_descriptor`，`:732` `astrocs.phase3.resample`，sci/alg/data/test 亦 `*-P3-RES-*`）；注册进生产 registry 在 `:15443-15448`、`:15451-15456`，工厂即整阶段 session；台账 `eng/ci/ledgers/registry_ir_parity.json` 2 条未解除（`grep -c '"module_id"'` = 2）。
  - **死包装**：`make_session_module` 跟踪面 3 命中（定义 `:1188` + `<P2Api>` `:15447` + `<P3Api>` `:15455`），`P1Api`（`:1193-1200`）在 `lib/**` 代码里零引用 ⇒ 对 P1 参数从未实例化成立。
  - **有实现但未注册**：`wc -l < lib/phase3_session/p3_v6_export.cpp` = **829**；`git grep -n "p3_v6_export" -- '*CMakeLists.txt'` 只命中 `eng/tests/integration/v6_p3/CMakeLists.txt:36` 的 `${P3SESS_DIR}/p3_v6_export.cpp`，其 target 是 `:34 add_executable(v6_p3_export_test ...)` ⇒ 仅测试编译；`ahpx` 三件同理只出现在 `eng/tests/unit/CMakeLists.txt:1397/1398/1404`，而 `astrocs_aio` 源清单 `CMakeLists.txt:535-545` 无一件，`git grep -n "ahpx" -- CMakeLists.txt` 0 命中（正例对照：同式 `git grep -n "aio_fits.cpp" -- '*CMakeLists.txt'` 命中 `CMakeLists.txt:536`）；`lib/infrastructure/pipeline/module_loader/{secure_loader.c,module_registry.c}` 与 `lib/infrastructure/benchmark/cpu/**/{baseline,avx2,avx512}_provider.cpp`+`common/src/capability_detect.c`（4 TU）在**全部** `*CMakeLists.txt` 与 `eng/cmake/*` 里 0 命中 ⇒ 不被任何 target 编译。
  - **phase3_session 无 module.yaml**：`git ls-files lib/phase3_session` = 5 文件（README + `p3_session.{h,cpp}` + `p3_v6_export.{h,cpp}`），确无 `module.yaml`，与 `lib/phase1_session/module.yaml`、`lib/phase2_session/module.yaml` 并存成对照。
- 与成稿差异：机制与定级无差异；**两处锚要订正**——① 成稿写 `p3_v6_export.cpp` 的测试锚为 `eng/tests/integration/v6_p3/CMakeLists.txt:34`，:34 实为 `add_executable(v6_p3_export_test` 行，源项在 **:36**（且路径经 `${P3SESS_DIR}` 变量拼接，属本仓已知假阳形态）；② 成稿举 `SCI-P2-RES-001` 时写"见 `:1180-1183` 一带的 `d.data_id = "DATA-P2-RES"`"，而 `:1162-1184` 是 `p2_write_descriptor()`（合法的 `astrocs.phase2.write`），占位 descriptor 的 `d.data_id` 实际在 **:724**。另：本条"有实现但未注册"一侧的 4 组对象与 AUD401-015 的生产缺口清单重合，属同一事实的两处登记。
- 影响 D8 的处置：进任务书（R1/R2/R3 的删除项 + G4/G5 的归并项照旧）；与 015 定主从——**以 015 为"测试独占/无 target TU"主清单**，011 只保留"注册身份 ↔ 实现体 ↔ 生产 ID 三方不一致"这一注册表轴，避免 D8 里同一 TU 被计两次。

## AUD401-013 · 模块 CMake 跨模块重编源码（目标归属越界，10 个模块文件、约 60 TU 引用）
**判定：降级**（机制成立、修复方向成立，且实际比我复算引用的更严重——`integration/v6` 跨模块源项是 24 不是 23；但"10 个模块文件 / 约 60 TU"这个范围要拆两档：生产模块侧 5 文件 / 69 个跨模块源项，测试与生产不可达侧 5 文件另计）
- 我的独立取证（全程只读 `CMakeLists.txt` 源文本与 target 源清单；未配置、未构建、未跑 eng 脚本、未跑①的普查脚本）：
  - `lib/algorithms/integration/v6/CMakeLists.txt`：自身源 3 项（`:26/:27/:29` = phase2_integrate/weight_chain/variance_propagation），**跨模块源 24 项**（`:30/:32/:33/:34/:35` coverage 5、`:36` integration/v6_phase1 1、`:37-43` `aio/v6` 7、`:44` calibration 1、`:45` psf 1、`:46` noise_snr 1、`:47` photometry 1、`:48/:49/:50` drizzle 3、`:51/:52` shared 2、`:54/:55` `aio/src` 2）；`awk '/\.cpp/ && 非自身 src/ && 非注释' = 24` 与逐项列举一致。
  - `lib/algorithms/integration/v6_phase1/CMakeLists.txt`：跨模块 **15 项**（`:17-23` aio/v6 7、`:25` calibration、`:27` psf、`:28` noise_snr、`:29` photometry、`:31/:32/:33` drizzle 3、`:34` shared）——与成稿"同族 7 个模块共 15 TU"**逐字吻合**。
  - `lib/algorithms/drizzle/CMakeLists.txt`：`grep -c 'CMAKE_SOURCE_DIR}/lib/infrastructure/aio/src/.*\.cpp'` = **17**、`.../lib/algorithms/shared/.*\.cpp` = **2**（与成稿 17+2 吻合），`:61-65 add_library(astrocs_p1_drizzle SHARED ${DRZ_ADAPTER_SOURCES} ${DRZ_PROD_SOURCES} ${DRZ_AIO_SOURCES})` ⇒ 确为"SHARED 目标从源文件独立重编译生产源"。
  - `lib/algorithms/cosmetic/CMakeLists.txt:40-45` `set(COS_PROD_SOURCES ...)` = calibration 4 TU（calibrator/master_generator/cosmetic_corrector/ac_api），与成稿 :41-44 吻合。
  - `lib/algorithms/coverage/CMakeLists.txt:42 add_library(phase2 STATIC ...)`：跨模块项实为 **5 项** = acr 2（`:54 ../../infrastructure/acr/api/kernel_registry.cpp`、`:58 .../acr/scheduler/device_executor.cpp`）+ 平台 genex 1（`:56` Windows 才编的 `acr/backends/cuda/cuda_bridge_loader.cpp`）+ shared 2（`:59/:60`）；**calibration 1 + drizzle 1 不在 :42**，而在同文件 `:169/:170` 的另一 target 里（`v6_drizzle_science.cpp`、`v6_calibration_covariance.cpp`，其注释 `:163-164` 自陈理由正是"被测生产源直接编译，避免与 phase2 的重复符号面"）。
  - 成稿引的重复符号实证我核到原文：根 `CMakeLists.txt:1046-1048` 注释"不链 `astrocs_v6_phase2_integrate`: 后者重复编译 upm/rejection/sampler/coverage, 与本 target 已链的 `astrocs_phase2` 产生重复符号"；同文件 `:623` 注释"lib/algorithms/coverage (phase2 生产模块; **ACR 源不在此列 — LEG-004**)"——与 `coverage/CMakeLists.txt:54/:58` 真的列了 ACR 源构成**同仓自相矛盾**，成稿这一击成立。
  - 根图入口：`CMakeLists.txt:1116 add_subdirectory(lib/algorithms/integration/v6_phase1)`、`:1117 .../integration/v6)` ⇒ 两个 v6 target 确在生产闭包内（成稿 :1116/:1117 引用准确）。
  - 我方对"10 个文件"的复算：用 `git grep -l -E '(\$\{CMAKE_SOURCE_DIR\}/lib/|REPO\}/lib/|\.\./\.\./(infrastructure|algorithms))' -- '*/CMakeLists.txt'` 命中面远多于 10（含大量 `eng/tests/**` 与自目录引用），说明成稿的 10 来自其脚本的"路径归一化后逃出自身模块目录 + 排除根 CMake"判据，我**无法在不写普查脚本的前提下逐字复现该判据**；但成稿列举的 10 个文件里我直接读到源项的 5 个（v6、v6_phase1、drizzle、cosmetic、coverage）全部为真，其余 5 个（`pipeline/orchestrator/cpp/tests`、`eng/tests/unit`、`hips_browser/healpix_browser_qt:35`、`acr/{profile,qualification}`）方向与我看到的 `eng/tests/unit/CMakeLists.txt:1397/1398/1404/1692` 等直编生产源的事实一致。
- 与成稿差异：**定级/范围要改**——① 计数订正：`integration/v6` 是 **24** 项不是 23，且其分桶把 aio 的 9 项写成"`aio/v6` 9"（实为 aio/v6 7 + aio/src 2），同时漏列 `integration/v6_phase1/src/phase1_product.cpp` 1 项；② 归属订正：coverage 的 calibration/drizzle 两项不属 `:42` 的 `phase2` 兼容目标而属同文件 `:169-170` 的另一目标；③ 范围拆档：10 个文件里只有 5 个是**生产模块 target** 越界（v6/v6_phase1/drizzle/cosmetic/coverage，合计 24+15+19+4+7 = 69 个跨模块源项），另 5 个是测试 CMake 与"生产不可达"的 acr/hips_browser——两者对"目标归属越界"的严重性不同档，混在一档会让下游把测试直编也当同类违规处理。
- 影响 D8 的处置：进任务书，但按"生产模块侧 5 文件（必须改 `target_link_libraries` + `POSITION_INDEPENDENT_CODE ON`）"与"测试/不可达侧 5 文件（另立低一档清理项）"两条任务写；W4 的合并建议（v6+v6_phase1 → 单一 `astrocs_integrate`）保留。

## AUD401-015 · 生产缺口清单：代码在跟踪面、只被测试编译（p3_projection 同型情形共 10 条）
**判定：确认**（9 条实质缺口逐条独立复现成立；但**成稿指向的证据表是空文件**、且"10 条"里混进了 1 条它自己认定为设计选择的门控项）
- 我的独立取证：
  - 逐文件列源面复算（`for f in …; do git grep -n "$f" -- '*CMakeLists.txt' '*.cmake'; done`），命中集里**没有任何生产 target 的源项**，只有注释或测试语句：
    - `p3_projection.cpp` → 编译位仅 `eng/tests/unit/CMakeLists.txt:1426`、`eng/tests/unit/v6_p3_proj/CMakeLists.txt:48`；`lib/algorithms/projection/CMakeLists.txt:13` 是注释。
    - `p3_proj_v6.cpp` → 编译位仅 `eng/tests/integration/v6_p3/CMakeLists.txt:37`、`eng/tests/unit/v6_p3_proj/CMakeLists.txt:16/:25`、`lib/algorithms/projection/tests/p3wcs/CMakeLists.txt:19`。
    - `executor.cpp` → 编译位仅 `eng/tests/unit/CMakeLists.txt:243`（target `:242 add_executable(executor_provider_race_test …)`）；根图 `astrocs_core` 不列它。
    - `snr_estimator.cpp` → 编译位仅 `lib/algorithms/noise_snr/tests/p1noise/CMakeLists.txt:135`（`:133 set(P5SNR_SRC`→`:136 add_library(p5snr_under_test STATIC)`）与 `lib/algorithms/psf/tests/p1psf/CMakeLists.txt:172`。
    - `hips_core.c` → 仅 `eng/tests/unit/CMakeLists.txt:1691-1692`（`target_sources(w34_hips_core_selftest PRIVATE …)`），且该文件注释 `:1690` 自陈"hips_core.c 未进任何生产目标本身是缺口"。
    - `aio_ahpx_{writer,reader,api}.cpp` → 仅 `eng/tests/unit/CMakeLists.txt:1397/1398`（`ahpx_hips_format_test`，语句起于 `:1396`）与 `:1404`；`astrocs_aio` 源清单 `CMakeLists.txt:535-545` 无一件，`git grep -n "ahpx" -- CMakeLists.txt` 0 命中。
    - `p3_v6_export.cpp`（`wc -l` = 829）→ 仅 `eng/tests/integration/v6_p3/CMakeLists.txt:36`（`v6_p3_export_test`，语句起于 `:34`）；不在 `astrocs_phase3_session` 源清单（`CMakeLists.txt:933-943` 只列 `p3_session.cpp`）。
  - 正例对照（防"假 0 命中"）：同一命令式 `git grep -n "aio_fits.cpp" -- '*CMakeLists.txt'` 命中 `CMakeLists.txt:536`；`git grep -n "p3_wcs.cpp"` 命中 `lib/algorithms/projection/CMakeLists.txt:19 add_library(astrocs_p3_projection_wcs STATIC ${CMAKE_CURRENT_SOURCE_DIR}/p3_wcs.cpp)` ⇒ **该生产 target 只列 `p3_wcs.cpp`** 得到直接证明。
  - `probe.cpp` 单列核实：`CMakeLists.txt:443 if(ASTROCS_PROBES)` / `:444-445 add_library(astrocs_probes STATIC lib/infrastructure/observability/probes/src/probe.cpp)`，`:26 option(ASTROCS_PROBES … OFF)` ⇒ 它**被生产 target 列源**、只是门控默认 OFF，与"只被测试编译"不同型。
  - 独立普查（不复用①的脚本）：`git ls-files lib | grep -E '\.(cpp|c|cc|cxx)$' | grep -vE '/tests?/'` = **386** 个非测试目录 TU；其中 basename 不出现在任何**非测试** `CMakeLists.txt` 的 = 42，再按是否出现在测试 CMake 分档 = 测试提及 6 + 完全无提及 36。我这版偏少的原因是**注释行也被 `-o` 正则当成提及**（`p3_projection.cpp`/`p3_proj_v6.cpp`/`snr_estimator.cpp` 三件正是这样被误判为"生产 CMake 有列"），所以我改按上面的逐文件人工判读定案，不以此普查数推翻成稿数。
- 与成稿差异：① **证据表落空**——成稿"对象"字段写 `inventory/AUD-401/testonly_nontest.txt`，该文件实测 **0 字节 / 0 行**；真正装着这 10 行的是同目录 `test_nontest.txt`（`nl -ba` = 10 行，内容与本条列举一致）。② 计数口径——桶名"只被测试编译"含 `probe.cpp` 一条不属该型（成稿正文自己已标注"是设计选择而非缺口"），**实质缺口是 9 条**。③ 一处锚错——"`lib/algorithms/projection/CMakeLists.txt:8-9` 自陈"实为 **:13**（:8 是另一行注释），其余 `:19/:1424-1426/:34/:242/:1396,1403/:1691` 经我逐条核对是**语句起始行**口径（命中 target 名），不算错。
- 影响 D8 的处置：进任务书（N7/N8 接线或按标准04 §4 退役的二选一不变），但清单要改成 **9 条缺口 + 1 条门控说明**，并把证据表引用改为 `test_nontest.txt`；`probe.cpp` 移入"须复测：OFF 时 `ASTROCS_PROBE_SCOPE` 宏为空语句"的验证项（成稿已在 §四 记同一风险）。

<!-- PROGRESS: 6/6 -->

---

## 本批判定汇总（各态计数，附复算命令）

| 态 | 条数 | 条目 |
|---|---|---|
| 确认 | 4 | AUD401-003、-004、-011、-015 |
| 降级 | 2 | AUD401-002（范围收到"目录"一档 + 14/11 订正）、AUD401-013（计数与文件范围拆两档） |
| 推翻 | 0 | — |
| 待证 | 0 | — |
| 重复 | 0（但与 AUD401-015 有**部分重合**：AUD401-011 的"有实现但未注册"一侧 = 015 清单的子集，已在 011 内定主从） | — |

复算命令（全部为本轮实际跑过的确定命令，只读；无构建/ctest/run_checks/端到端）：

```bash
R="F:/Astro dev/Astro CS Normalization Database"
# 002 三个计数
git -C "$R" ls-files lib | grep -E "phase[123]" | grep -v "/tests\?/" | sed 's|/[^/]*$||' | sort -u | wc -l   # 14
git -C "$R" ls-files lib | grep -vE "/tests\?/" | grep -cE "(phase[123]|/p[123]_)"                              # 81
git -C "$R" grep -h -E "^[[:space:]]*(add_library|add_executable)\(" -- 'CMakeLists.txt' '*/CMakeLists.txt' \
  | sed -E 's/^[[:space:]]*(add_library|add_executable)\(([A-Za-z0-9_]+).*/\2/' | sort -u \
  | grep -E "^astrocs" | grep -Ei "phase|_p[123]" | wc -l                                                        # 22
# 003 构建图与链接面（只读源文本）
git -C "$R" grep -n "phase._session" -- CMakeLists.txt            # :913/:925/:933/:1012/:1031-1033/:1070
git -C "$R" grep -n "whole-archive" -- CMakeLists.txt eng/cmake   # 仅 cfitsio/configure（自有构建不用）
# 004 运行期引用面
git -C "$R" grep -n "make_session_module" ; git -C "$R" grep -n "P1Api"
git -C "$R" grep -n "build_pipeline_ir" -- lib
git -C "$R" grep -nF -e "astrocs.phase2.resample" -e "astrocs.phase3.resample" -- lib eng/ci/ledgers
grep -c '"module_id"' "$R/eng/ci/ledgers/registry_ir_parity.json"                                                 # 2
# 011 注册↔实现↔生产 ID
git -C "$R" ls-files lib/algorithms/upm lib/algorithms/rejection lib/algorithms/sampling lib/algorithms/coverage/hips_p2 | grep -cE "\.(cpp|c)$"   # 0
git -C "$R" grep -n "p3_v6_export" -- '*CMakeLists.txt'
git -C "$R" grep -n -e "capability_detect" -e "_provider.cpp" -e "secure_loader" -e "module_registry\.c" -- '*CMakeLists.txt' 'eng/cmake/*'   # 0（正例对照 aio_fits.cpp 命中 CMakeLists.txt:536）
# 013 跨模块源项
git -C "$R" grep -c -E 'CMAKE_SOURCE_DIR\}/lib/infrastructure/aio/src/.*\.cpp' -- lib/algorithms/drizzle/CMakeLists.txt   # 17
git -C "$R" grep -n "phase2_descriptor\|phase3_descriptor" -- lib
# 015 逐文件列源面
for f in p3_projection.cpp p3_proj_v6.cpp executor.cpp snr_estimator.cpp hips_core.c aio_ahpx_api.cpp aio_ahpx_reader.cpp aio_ahpx_writer.cpp p3_v6_export.cpp probe.cpp; do git -C "$R" grep -n "$f" -- '*CMakeLists.txt' '*.cmake'; done
# 独立普查（有注释污染，仅作旁证，未用于推翻成稿计数）
git -C "$R" ls-files lib | grep -E '\.(cpp|c|cc|cxx)$' | grep -vE '/tests?/' | wc -l                              # 386
```

## 旧判偏松清单（= 上一轮给下游的虚假授权）

本批 6 条里没有需要推翻的偏松判定，但有 4 处**会让下游按错锚或错口径施工**的表述，必须回写订正：

1. **AUD401-004 的取证命令给的是假 0 命中**：`git grep -n "astrocs.phase2.resample\|astrocs.phase3.resample" -- lib/infrastructure/cli` 我逐字复跑 = **1 命中**（`runtime_client.cpp:270` 的 `astrocs.phase3.resample2`，前缀子串）。下游若照抄该命令自检，会在"应 0 却命中"时误判本条已被推翻。→ 订正为带边界写法（`-F` 固定串 + 检查命中是否等于 `resample2`）。
2. **AUD401-015 指向的证据表是 0 字节空文件**（`inventory/AUD-401/testonly_nontest.txt`），实表是同目录 `test_nontest.txt`。按 AGENTS.md §9，证据文件为空即不算已完成——成稿该条的"对象"锚必须改指，否则 D8 里这 10 行无源可查。
3. **AUD401-015 的"10 条"含 1 条非该型**（`probe.cpp` 属生产 target 列源 + `if(ASTROCS_PROBES)` 默认 OFF 的门控项，成稿正文自己已认定为设计选择）⇒ 实质缺口 9 条。下游若按"10 条接线任务"排产会多排 1 条。
4. **AUD401-002 的"违反 §1:18"外延过宽**：§1:18 的字面辖域是"代码目录"，把 22 个 target 名与 81 个文件名也记在同一条文下，会让整改任务书出现"条文说不清"的空口授权。→ 拆"目录（有条文）/target 名与文件名（按精神推论，且与最高设计 :489/:655 明文允许打架）"两档，后者必须挂上呈裁。
5. 另记两处锚级小错（不影响判定）：AUD401-011 把占位 descriptor 的 `d.data_id` 指到 `:1180-1183`（那是 `p2_write_descriptor()`，占位在 `:724`）；AUD401-015 的"`projection/CMakeLists.txt:8-9` 自陈"实为 `:13`。

## 覆盖率自报

- 分配 6 条 / 已判 **6** 条（确认 4、降级 2）/ 空判名单 **为空** ⇒ 本批已完。
- 未做（划清边界，不算已审）：AUD401-013 里"10 个模块文件"这一档我未逐文件复现①脚本的归一化判据（生产侧 5 个逐一读到源项、另 5 个只核对方向），若要给 D8 报"文件数 10"的确定值，须由持脚本口径的一方复跑；`lib/algorithms/resample`、`fits_output` 等模块的跨模块项未纳入我本批列举（成稿未在我那批条目里主张，且 016-020 另派）。
- 全程只读：未改仓库、零 git 写、未构建/未配置/未跑 ctest、`run_checks.py`、`eng/**` 工具、未跑端到端、未 import 仓库 Python。
- 开工 `git status --porcelain`：`?? ACSD整治工作包_AUDIT-06.zip` / `?? site/`
- 收工 `git status --porcelain`：**逐字相同**（仍为上述两行），`git rev-parse --short HEAD` = `c8f64e9a` ⇒ 本批复核零污染被审对象。

## 我对第①层判据本身的校准建议

1. **"被链接"必须拆成三个可分别证伪的层次**，本仓三会话恰好一层比一层浅：`CMake 链接声明`（003 证到）→ `静态库成员是否被抽进二进制`（无 `--whole-archive`，按未定义符号需求抽取 ⇒ p1 存疑、p2/p3 因 `register_factory` 探针有真实引用 ⇒ 会进）→ `运行期 execute`（004 证到"永不调用"）。第①层现在用"被生产可执行 acsd 直接链接"一句话横跨三层，下游极易读成"生产在跑"，从而把 003/004 当成互斥条目、把主动作从"删除"误写成"接线"。建议成稿模板加一栏"本条证到第几层"。
2. **`register_factory` 的实例化探针（`module.cpp:111-112`）是"注册 ≠ 执行"判据的边界条件**：注册本身在生产启动期就有可观察后果（对象构造 + 模块索引 `executable=true`，`module.cpp:156-172`）。凡判"某注册项是纯死位"，应顺带核这一处，否则"零运行期影响"的措辞偏松。
3. **检索式必须自带正例对照并写成命令原文**：本轮两处假阳/假阴都出在命令而非结论（子串命中 `resample2`；我自己的普查因把注释行当"已列源"而少算 3 件）。建议第①层每条"0 命中"都并列写"同一命令式在应命中对象上的读数"（成稿 004/015 已部分做到，值得定为硬性字段）。
4. **行号锚要标口径**：第①层实际用的是"语句起始行"（`add_executable`/`target_sources`/`set(SRC` 行，命中 target 名，便于施工），这比"源项行"更有用，但没写明口径，导致我一度误判为漂移 1–3 行。建议在成稿开头一句话钉死："锚 = 语句起始行；源项行如需另行标注"。
5. **计数条目要区分"条文直证"与"精神推论"、区分生产侧与测试侧**：本批两条降级（002、013）的根因都是把不同强度/不同档的事实压进同一个数字（14 目录里 3 条来自文件名；10 个越界文件里 5 个是测试与非生产 target）。合成一个数会让 D8 的优先级排序失真。

## 附：派单指定问题的直接回答（AUD401-003 与 -004 是否互斥）

**不是互斥，是两个轴，两条同时为真，均判确认。** "在链接闭包里"（构建图声明 + 被生产 exe 的 `target_link_libraries` 点名，`CMakeLists.txt:913/:925/:933/:1012/:1070`）与"生产运行期真引用"（注册 ID 是否出现在生产 IR、会话编排函数是否被调用）是两条独立可分别证伪的命题：

- **003 钉在构建图/链接声明轴**：成立。措辞须加限定"链接 ≠ 执行"；且静态库无 `--whole-archive`，p1 是否真进 `acsd` 二进制仍未证（其导出符号在生产侧唯一引用点 `P1Api` 自身零使用）。
- **004 钉在运行期调度/引用轴**：成立。p1 无工厂注册，p2/p3 只挂在生产 IR 永不引用的占位 ID 上（生产两条链 `runtime_client.cpp:236-242`、`:268-272`），IR 只能由 `build_pipeline_ir` 生成、无外部 IR 注入口。唯一需要收紧的措辞是"从不执行"→"**其会话编排函数从不被调用**"，因为 `register_factory` 的探针（`module.cpp:111-112`）会在每次生产启动时构造这两个 `SessionModule` 对象并使它们出现在模块索引里。
- 对 D8 的后果：整改的主动作是**删注册 + 退役目录**，不是"接线"；两别写成一条会诱使下游去"把 session 接到生产上"。

<!-- PROGRESS: 6/6（本批完，四条文末小节齐） -->
