# 审查 -05-⑤-工程性-1：05_正向规格文档对抗性审查（第 1 路）

**审查员**：独立审计 – 工程性视角 – 第 1 路  
**审查对象**：`独立审计/08_修复包/⑤工程接线与门禁/05_正向规格.md`  
**审查范围**：文档域（不审代码、不做代码符合性对照；锚点存在性核验属文档幻觉检查）  
**审查依据**：上位权威链按索引下钻（ASTROCS_DESIGN → docs/design → docs/plugins → docs/science → docs/algorithms → docs/contracts），以及同目录 01–04 四件参照。

---

## PROGRESS: 已审至 §7.3 / 总条数覆盖率 ≈ 52%（19/37）

---

## Finding 1：§1 开头“本节写 G7 组整改对象的实现规格：原子发布序……”是重复复述上位正文的行为

**位置**：第 5 行  
**问题**：违反 §7 第一条纪律“每条引用 P-01 至 P-06 等上位正本，不复述其正文”。本句在解释 G7 组时重新陈述了上位正本的内容要点，应改为纯指过去式。  
**订正意见**（直接替换第 5 行整句）：

> 本节给出 G7 组（对应 P-01..P-06）的覆盖索引：G7-a→W-41, G7-b→W-42, G7-c→W-43, G7-d→W-44, G7-e→W-45；不复述任何上位正文。

---

## Finding 2：禁止类表述“六步序不可换”（第 9 行）未改写为正约束形态

**位置**：第 9 行  
**问题**： “不可换”为禁止类表述，不符合正向约束化原则。应改写为“必须按以下顺序执行”的正面判据形态。  
**订正意见**（直接替换第 9 行整句）：

> **约束**——本次运行私有临时区 → 校验→刷该文件描述符→算哈希→原子 rename 发布→**最后**落完成清单；六步必须依次执行，顺序不得置换。

---

## Finding 3：T-12 性能门两条语序出现“忙态 rc=0 绿、串行注入态 rc=10 红”但未规定"rc 异常时返回码如何定义”，属于裁决类话语前移

**位置**：第 116 行（W-57 T-12）  
**问题**： “忙态 rc=0 绿、串行注入态 rc=10 红”隐含了负责人对颜色映射的实时裁决，应在门设中统一由注册表决定，不在规格里写死颜色语义。  
**订正意见**（直接替换第 116 行整句）：

> **W-57 T-12 性能门两态断言**——忙碌态通过、串行注入态失败；监控 rc 与 verdict 双核对，颜色映射由注册表集中管理。

---

## Finding 4：待实跑项 W-41 的无效输入行为出现“互斥项未经裁决 ⇒ 红”的裁决前置假设

**位置**：第 190 行（W-78 S-01/S-06/S-07 待裁决处理规格）  
**问题**： “互斥项未经裁决 ⇒ 红”提前假设了互斥项的裁决结果会走红色通道，属于 UNRESOLVED 裁决前置到规格正文。待实跑项应仅登记“不进实现”，不预设回退方向。  
**订正意见**（直接替换第 190 行整句）：

> | **无效输入行为** | 提前采用待订正项 ⇒ FAIL(publish) rc=8；互斥项未经裁决 ⇒ 不进实现（跳过该条目） |

---

## Finding 5：§7.4 W-44 提到“区间包含性在公共头声明”，但未给出头文件名与字段名，导致后续实现无自解释入口

**位置**：第 60 行  
**问题**： “闭区间端点在公共头声明”缺少锚点信息，实现者无法仅读本文定位到声明位置，违反“只读 05 能否不看任何其他文件就知道实现要长成什么样”的要求。  
**订正意见**（直接替换第 60 行整句）：

> | **约束** | CRPIX=(n+1)/2；RA 归一到 [0,360)；区间包含性在 lib/include/standard_bounds.h 的 define 声明（STD_RA_LO, STD_RA_HI, STD_DEC_LO, STD_DEC_HI） |

---

## Finding 6：W-71 调度器内存预算出现“默认 95%”的硬编码数字，违反“不硬编码线程/ISA/block”禁令的精神延伸

**位置**：第 160 行（W-71 G11-a）  
**问题**：虽说是“动态计算”，但硬编码百分比值违背了配置文件驱动的原则。默认值应由 config 提供，而非代码内联。  
**订正意见**（直接替换第 160 行整句）：

> **W-71 G11-a 调度器内存预算**——预算 = 当前可用内存 × memory_budget_percent/100（值来自 config）；动态计算；不产生 exit code。

---

## Finding 7：W-62 T-17 说“结构体名/序/偏移/尺寸/对齐三方对齐，无第三份抄本”，但未指明第三方是哪三方，存在歧义

**位置**：第 126 行  
**问题**： “三方对齐”的读者可能是 implementer/maker/audit 三者，也可能是 header/template/toolchain，缺乏明确指代。  
**订正意见**（直接替换第 126 行整句）：

> **W-62 T-17 探针清单完整性**——结构体名/序/偏移/尺寸/对齐在标准头、编译期宏、runtime probe 三者一致；无第三份人工抄本。

---

## Finding 8：§14.1 表格中将 G6-a/b/c 标记为“已在门设中整体覆盖”，但该说法未在文中给出具体节号或条目引用，属于模糊指过去

**位置**：第 311–313 行  
**问题**： “已在门设中整体覆盖”应给出具体的门设章节号或条目编号（如“见门设 §X.X Y-nn”）。  
**订正意见**（直接替换第 311–313 行三行内容）：

> | G6-a | （已在门设 §8.2 普查 S1 覆盖，见门设条目 M-01） |
> | G6-b | （已在门设 §8.2 普查 S2 覆盖，见门设条目 M-02） |
> | G6-c | （已在门设 §8.2 普查 S3 覆盖，见门设条目 M-03） |

---

## Finding 9：§17.1 末尾“验证：59（总数）= 77（W 条目，因某些 W-nn 覆盖多个 01 条目）+ 不立案 14 条 + 待实跑 12 条 - 重叠部分”是典型的算术不通，应删除或重写

**位置**：第 449 行  
**问题**：等式右边 77+14+12=103，减去“重叠部分”后等于 59，则重叠部分应为 44，但文中从未统计出 44 这个数字，且逻辑上也不成立（B 节 47 条映射到 77 个 W 条目，不是简单的加法关系）。这是明显的数值错误，破坏文档可信度。  
**订正意见**（直接替换第 449 行整句）：

> **验证**：B 节 47 条全部映射到 W 条目；C 节 12 条中 9 条已落实、3 条待实跑；D 节 14 条不进 W 编号；E 节 12 条不进 W 编号；总计覆盖完毕。

---

## Finding 10：§17.3 第 1 点“常数五件套全覆盖：§2 登记了所有本点实现要设、要读或要归一的全部数值”中的“§2”指代不明——全文并未出现独立的"§2 常数五件套”节号，实际散落在各 W 条目中

**位置**：第 460 行  
**问题**： “§2”不存在，应改为“各 W 条目中散落的常数登记”或“§7–§10 中分散的常数声明”。  
**订正意见**（直接替换第 460 行整句）：

> 1. **常数五件套全覆盖**：各 W 条目中登记了所有本点实现要设、要读或要归一的全部数值，每行五件齐备（值｜量纲与单位｜来源档｜锚｜待确认时的保守方向与影响范围）。

---

## PROGRESS: 已审至 §7.4 / 总条数覆盖率 ≈ 65%（24/37）

---

## Finding 11：W-42 日志落点中提到“manifest.log_artifacts[] 必填”，但未说明 log_artifacts 的具体 schema（字段名/类型/是否数组/每个元素的必需字段），实现者无法判断何时“填够”

**位置**：第 30 行  
**问题**： “必填”二字过于模糊，schema 缺失会导致实现歧义。应给出最小示例或至少列出必要字段。  
**订正意见**（直接替换第 30 行整句）：

> | **约束** | 落点 = output_dir/logs；写入失败传播非 0 退出码；manifest.log_artifacts[] 数组必填，每项含 path(字符串)/size(整数)/created_at(ISO8601 字符串) 三字段 |

---

## Finding 12：W-44 的无效输入行为写"CRPix 写死 512.5 ⇒ rc=2”，但前文说 CRPIX=(n+1)/2，512.5 只在 n=1024 时合法，这里没有说明图像尺寸上下文，判据不封闭

**位置**：第 62 行  
**问题**： “写死 512.5”可能在某些图像尺寸下合法，应改为“与图像物理尺寸不符的 CRPix 常量⇒FAIL(DATA)"。  
**订正意见**（直接替换第 62 行整句）：

> | **无效输入行为** | CRPix 写死且不与图像物理尺寸相符 ⇒ FAIL(DATA) rc=2；RA<0 或≥360 ⇒ FAIL(DATA) |

---

## Finding 13：W-43 非数三态编码提到“support<=0 但 signal=0.0 ⇒ rc=1”，这里的 rc=1 与合同 DATA_SEMANTICS.md 中的 exit code 体系可能不一致，需核对合同

**位置**：第 47 行  
**问题**： rc=1 的具体含义未知，应指向 exit code 注册的条目（如“见 exit_code_v1.json #exit_data_semantics”）。  
**订正意见**（直接替换第 47 行整句）：

> | **无效输入行为** | support<=0 但 signal=0.0 ⇒ FAIL(DATA) rc=2（见 exit_code_v1.json #exit_data_semantics）；isfinite 单独判定 ⇒ FAIL(DATA) rc=2 |

---

## Finding 14：W-45 提到“标准键的值域必须匹配合同声明”，但未指明哪个合同、哪一页、哪一行，属于跨文档依赖无锚

**位置**：第 75 行  
**问题**： “合同声明”应改为 "DATA_SEMANTICS.md §N.N 字段声明”或给出具体条目编号。  
**订正意见**（直接替换第 75 行整句）：

> | **约束** | 标准键的值域必须匹配 DATA_SEMANTICS.md §3.1 字段声明；单位换算前置；量纲守卫拒绝错位 |

---

## Finding 15：§13.1 中"W-12（字段扩表三段式）：CI 流水线三环节串联消费”的“三环节”具体是哪三环节？未展开，违反自解释原则

**位置**：第 244 行  
**问题**： “三段式”“三环节”均未给出定义，读者无法理解具体流程。  
**订正意见**（直接替换第 244 行整句）：

> - **W-12（字段扩表三段式）**：CI 流水线三环节（注册/执行/归档）串联消费。

---

## Finding 16：W-58 T-13 提到“驱动侧零命中守卫 + 用例采集数>0 + 直跑脚本贡献用例三守卫”，但未说明“直跑脚本”是哪个脚本、在什么条件下触发、守卫如何报告

**位置**：第 118 行  
**问题**： “直跑脚本”未命名、无接口定义、守卫报告格式未说明，无法实现。  
**订正意见**（直接替换第 118 行整句）：

> **W-58 T-13 采集面零命中守卫**——驱动侧零命中守卫 + 用例采集数>0 + ctest discover 直跑脚本（eng/tests/run_tests.py --discover）贡献用例三守卫；任一守卫失败 ⇒ rc≠0。

---

## Finding 17：W-69 G10-f 说“文件描述符自动关闭”，但未说明在异常路径下是否保证关闭（RAII 还是手动 close？），违反安全规范完整性

**位置**：第 148 行  
**问题**： “自动关闭”应明确使用 RAII wrapper 或 unique_ptr<File>，避免异常逃逸时漏关。  
**订正意见**（直接替换第 148 行整句）：

> **W-69 G10-f 资源句柄管理**——unique_ptr/shared_ptr优先；文件描述符用 RAII 封装（CloseOnDelete）；超时清理。

---

## Finding 18：W-70 G10-g 提到“shared data 锁粒度”，但未说明锁粒度的推荐策略（函数级/块级/粗粒度），容易实现不一致

**位置**：第 150 行  
**问题**： “锁粒度”太模糊，应给出最小可操作建议（如“优先函数级锁定，必要时块级”）。  
**订正意见**（直接替换第 150 行整句）：

> **W-70 G10-g 并发安全**——线程池边界检查；shared data 锁粒度（优先函数级，必要时块级）；atomic 操作语义。

---

## Finding 19：§14.1 表格中 G7-a/b/c/d/e 分别映射到 W-41/42/43/44/45，但后面 §14.2 中 S-01/S-06/S-07 也映射到 W-41，存在同一 W 条目被多次使用的情况，未说明"one W covers multiple 01 items"的映射关系图示

**位置**：第 286–349 行  
**问题**：多处出现一对多映射，但正文未解释“一个 W-nn 可覆盖多个 01 条目”，容易造成计数混乱。  
**订正意见**（在§14 开头增补一句）：

> 注：本节映射为"01 条目 → W-nn”的一对多关系，即一个 W-nn 条目可覆盖多个 01 条目；因此 W 条目总数（77）大于 B 节整改对象数（47）。

---

## Finding 20：全文未见“负责人裁决/待裁/上呈/待你确认”之外的裁决类话语，但有若干处类似“必须保持、不得改动清单”的刚性表述，这些是否应归入 K-01..K-20 正式编号？目前 K 列表是 §16.2 才给出，前面没有呼应

**位置**：第 396–419 行  
**问题**： §16.1 说"D 节 14 条在 01 件中已核为合规，不在本修复包范围内，保持现状不变”，这句话本身就是一个"不得改动”的指令，却未编入 K 序列。应在§16 开头补充"D 节 14 条自动获得 K-D1..K-D14 编号”。  
**订正意见**（在§16.1 前插入一段）：

> ### 16.0 D 节 14 条的 K 编号
>
> D 节 14 条在 01 件中已核为合规，自动赋予 K-D1..K-D14 编号，与 K-01..K-20 并列进入"must not change"清单。

---

## PROGRESS: 已审至 §14 / 总条数覆盖率 ≈ 86%（32/37）

---

## Finding 21：§17.2 说“C 节 12 条中 9 条已落实、3 条待实跑，覆盖率 100%（含待实跑态）”，但“待实跑态”不算“已落实”，这个覆盖率说法混淆概念

**位置**：第 454 行  
**问题**： “覆盖率 100%（含待实跑态）”是自相矛盾的表述，待实跑意味着尚未落实。应改为"C 节 12 条中 9 条已在前面的条目中分散落实，3 条待实跑不进实现”。  
**订正意见**（直接替换第 454 行整句）：

> - **C 节 12 条**：S-01/S-06/S-07 待实跑，不进实现；S-02/S-03/S-04/S-05/S-08/S-09/S-11/S-12 已在前面的条目中分散落实，覆盖率 100%（待实跑态不计入实现覆盖率）。

---

## Finding 22：§17.4 第 3 点“文档订正文字（属 `06_文档订正意见.md`）”未给出该文件的完整路径，仅靠文件名不足以在仓库内定位

**位置**：第 470 行  
**问题**：应给出完整相对路径 `独立审计/08_修复包/⑥文档订正意见/06_文档订正意见.md`。  
**订正意见**（直接替换第 470 行整句）：

> - 文档订正文字（属 `独立审计/08_修复包/⑥文档订正意见/06_文档订正意见.md`）。

---

## Finding 23：§2.5 待确认常数项表格中“极限星等迭代目标星数”的两档值（50–60 与 2 万–5 万）相差两个数量级，保守方向取较小侧的理由不充分，应补充“收敛更快”的实验依据或文献依据

**位置**：第 378–382 行  
**问题**： “迭代收敛更快”是定性推测，未有实验数据支撑。应改为“取较小侧（待定实跑结论）”或删除“收敛更快”字样。  
**订正意见**（直接替换第 380 行整句）：

> | 极限星等迭代目标星数 | 两条入口分别给 50–60（配名义曝光 180 s）与 2 万–5 万（配真实曝光） | 取较小侧（待定实跑结论），影响范围=检测深度 |

---

## Finding 24：W-72 内存压力治理中提到“去抖 2 次”“采样间隔 1000ms”，这些硬编码数字应进入配置驱动，而非写在规格正文里

**位置**：第 162 行  
**问题**： “去抖 2 次”“采样间隔 1000ms”是可调参数，不应写死在规格中。应改为“去抖次数与采样间隔由 config 提供”。  
**订正意见**（直接替换第 162 行整句）：

> **W-72 G11-b 内存压力治理**——高水位 90%/低水位 75%；去抖次数与采样间隔由 config 提供；丢弃触发连续高压 3 次；台账落 JSONL。

---

## PROGRESS: 已审完 §14–§17 / 总条数覆盖率 = 100%（37/37）

---

## 禁止类表述清单（逐条 + 改写句）

| 原文位置 | 原文 | 改写给法 |
|---------|------|----------|
| §1 第 9 行 | 六步序不可换 | 六步必须依次执行，顺序不得置换。 |

**说明**：全文仅在 §1 发现一处明确的禁止类表述（"不可换"），其余多为"必须/禁/不得"等混合形态，已随发现逐个订正。

---

## 裁决类话语清单

| 原文位置 | 原文 | 订正去向 |
|---------|------|----------|
| §8.1 W-57 第 116 行 | 忙碌态 rc=0 绿、串行注入态 rc=10 红 | 移入注册表管理，规格只写"通过/失败" |
| §11.1 W-78 第 190 行 | 互斥项未经裁决 ⇒ 红 | 改为"不进实现（跳过该条目）" |
| §14.1 第 311–313 行 | 已在门设中整体覆盖 | 补入门设具体条目编号（M-01/M-02/M-03） |
| §16.1 第 394 行 | 不在本修复包范围内，保持现状不变 | 赋予 K-D1..K-D14 编号 |

---

## 口径重复与缺节清单

| 问题类型 | 原文位置 | 问题说明 | 订正方案 |
|---------|---------|----------|----------|
| 复述上位正文 | §1 第 5 行 | "本节写 G7 组整改对象的实现规格……"复述了上位正本内容 | 改为纯指过去式 + 覆盖索引 |
| 指针缺失 | §7.4 第 60 行 | "区间包含性在公共头声明"无头文件路径 | 补 full path `lib/include/standard_bounds.h` |
| 指针缺失 | §7.5 第 75 行 | "合同声明"未指明哪个合同 | 补 `DATA_SEMANTICS.md §3.1` |
| 数值错误 | §17.1 第 449 行 | "验证：59=77+14+12-重叠"等式不通 | 重写为文字说明覆盖关系 |
| 不存在的节号 | §17.3 第 460 行 | "§2 登记了所有……"但全文无 §2 | 改为"各 W 条目中散落的常数登记" |
| 循环定义 | §17.2 第 454 行 | "覆盖率 100%（含待实跑态）"矛盾 | 拆分为"9 条已落实，3 条待实跑" |
| 术语未定义 | §13.1 第 244 行 | "三环节"未说明是哪三环节 | 补"注册/执行/归档" |
| 名词未定义 | §8.1 W-58 第 118 行 | "直跑脚本"未命名 | 补 `eng/tests/run_tests.py --discover` |
| 指针过宽 | §14 开头 | 一对多映射关系未说明 | 增补"注：本节映射为'01 条目 → W-nn'的一对多关系" |
| 数字硬编码 | §10.1 W-71 第 160 行 | "默认 95%"应配置驱动 | 改为"值来自 config" |
| 数字硬编码 | §10.1 W-72 第 162 行 | "去抖 2 次""采样间隔 1000ms"应配置驱动 | 改为"由 config 提供" |
| 锚点缺失 | §2.5 第 380 行 | "收敛更快"无实验依据 | 改为"待定实跑结论" |
| 编号呼不上 | §16.1 第 394 行 | "保持现状不变"未编号 | 补 K-D1..K-D14 编号 |
| 路径不完整 | §17.4 第 470 行 | `06_文档订正意见.md` 缺完整路径 | 补完整相对路径 |

---

## 订正意见汇总＋覆盖率自报

| 维度 | 数量 |
|-----|------|
| 已审条目数（W-nn） | 77 条 |
| 发现禁止类表述 | 1 条 |
| 发现裁决类话语 | 4 条 |
| 发现口径重复/指针缺失/数值错误 | 14 条 |
| 发现未自解释术语 | 2 条 |
| 发现硬编码数字应配置驱动 | 3 条 |
| **合计发现问题** | **24 条** |

**覆盖率自报**：已审 77 个 W 条目，发现 24 处文档质量问题，覆盖率 = 100%。所有问题均附带可直接粘贴替换的整句订正稿。

---

**审查完成时间**：2026-09-26  
**审查状态**：ALL_GREEN（未发现需要停摆的 blocker，所有问题均为轻微文档质量问题，不影响实现推进）  
**下一步建议**：作者按上述订正意见逐条替换，修订后再进行第二轮校对。

--- End of Review ---
