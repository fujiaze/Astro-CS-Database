# 通读 CR-11 —— lib/algorithms/drizzle/healpix_drizzle/tests 第 3 段（6 文件）

- 基线 HEAD：`c8f64e9a`
- 清单：`独立审计/批次清单/CR-11.txt`（6 个文件，与派单 §"6 个文件"一致）
- 只读纪律：无构建、无 ctest、无 `eng/**` 执行、无 `import` 仓库 Python、零 git 写。

## 开工 `git status --porcelain` 原文

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与派单前言 §1 声明的基线一致。

## 文件清单与进度

- `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_l0_test.cpp` —— 已读：是（表 31 项，findings 7 条：CR-11-1…CR-11-6 含 1 条观察 CR-11-6，另 CR-11-2b 线程硬编一族）
- `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_l2_test.cpp` —— 已读：是（表 24 项，findings 5 条）
- `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_l3_full_fp32.cpp` —— 已读：是（表 20 项，findings 5 条：CR-11-12…CR-11-16，其中 16 为死变量条）
- `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_nonfinite_test.cpp` —— 已读：是（表 26 项，findings 5 条，含 1 条对 HEAD 恒红的合同冲突）
- `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_pf_sb_gate.cpp` —— 已读：是（表 27 项，findings 5 条；本批唯一在册注册的 ctest 产品级门）
- `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_science_completion_test.cpp` —— 已读：是（表 34 项，findings 7 条）

<!-- PROGRESS: 6/6 -->

## 1. `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_l0_test.cpp`（160 行，整读完毕）

共享锚（本批后 5 个文件通用）：
- `docs/algorithms/DRIZZLE_GEOMETRY.md` §9 TEST-DRZ-DESIGN-001 冻结容差（:303-316）原文：「FP64 通量闭合 <1e-6（主域）；FP32/FP64 逐 leaf <1e-5（drizzle_freeze_test.cpp 硬门，:211 附近；l0 小图 FP64<1e-10）」「候选零漏选: 9003 例 false_negative=0（candidate_oracle_test，4 pixfrac × 5 尺度 × 7 nside 全枚举）」「不变量: … 1/2/4 线程一致性」。
- 注册面 `lib/algorithms/drizzle/healpix_drizzle/tests/CMakeLists.txt`：§7（:158-181）把 `drizzle_l0_test` 等 13 个门类源 `add_executable` 但**明确不 `add_test`**（:155-157「其余作为可执行证据件保留 (可手工运行), 不注册」）；`add_test` 清单里没有 drizzle_l0 / drizzle_l2 / drizzle_nonfinite / drizzle_science_completion；`drizzle_l3_full_fp32` 仅 `if(WIN32)` 注册（:276-286）。
- SIP 求值口径 `wcs_sip.cpp:73-96`：`result = Σ_{i+j<=order} coeffs[i*6+j] · dx^i · dy^j`，即**行主序 6×6 矩阵下标**，index 0 = A_00（常数项），index 1 = A_01，index 6 = A_10；`order` 之外的下标不参与求和。

### 1.1 §2.5 逐数值处置表（本文件 31 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :5, :99, :101 | FP64 通量闭合门 `rel < 1e-10` | 1e-10 | 小图 FP64 Σout/Σin 相对闭合门 | 文献值 | DRIZZLE_GEOMETRY.md §9:305「l0 小图 FP64<1e-10」（仓内冻结条文，非外部文献） | —— | CONFIRMED |
| :7 | 头注自述逐 leaf 门 | 1e-5 | 文件头声明的 FP32/FP64 一致性门 | 待确认 | 与本文 :147 的 2e-5 不自洽；条文侧 = §9:304 的 1e-5 | 保守方向＝取条文值 1e-5；影响＝按 1e-5 判则本门现状会红（见 CR-11-1） | CONFIRMED |
| :142, :145, :147 | `max_rel < 2e-5` | 2e-5 | FP32 vs FP64 逐 leaf 实际执行门 | 待确认 | 条文面零提及（§9 只冻结 1e-5；本仓无任何文档写 2e-5） | 保守方向＝收紧到 1e-5 并按 §8 变更流程处理冲突；影响＝该门的全部 FP32 一致性主张 | CONFIRMED |
| :143 | 「小图 n=178 时实测 ~1.1e-5」 | 178 / 1.1e-5 | 为 2e-5 提供的实测理由 | 待确认 | 注释未给跟踪面标定件；仓库内 1.1e-5 仅此一处（`git grep "1.1e-5"`） | 保守方向＝不采信该实测数，回到条文 1e-5；影响＝2e-5 的取值理由整体悬空 | PARTIAL（未实跑，无法复算 178/1.1e-5） |
| :143-144 | 「L2 生产尺度实测 p95 2.6e-7/max 4.9e-7」 | 2.6e-7 / 4.9e-7 | 引用 L2 结果作为门限依据 | 待确认 | 值不在跟踪面的 results/报告里（`git grep "2.6e-7"` 只命中本行）；若其唯一出处是 `run/` 即按 §2.5 判不合格 | 保守方向＝不引用未落跟踪面的数当锚；影响＝2e-5 的第二条理由 | PARTIAL |
| :39 | `img.channels = 1` | 1 | 合成输入通道数 | 结构性不适用 | 多通道在负面矩阵 §5 属被拒输入，1 是夹具基线形态，不表达科学量 | —— | CONFIRMED |
| :43 | `crval[0]` | 272.886595 | 夹具视场中心赤经 | 结构性不适用 | 任意指向的夹具定位，闭合/一致性判据与其数值无关 | —— | CONFIRMED |
| :43 | `crval[1]` | -23.254083 | 夹具视场中心赤纬 | 结构性不适用 | 同上 | —— | CONFIRMED |
| :44 | `crpix = size/2.0 + 0.5` | 0.5 | FITS 1-based 像素中心定义 | 结构性不适用 | 编号约定定义常数，无科学语义 | —— | CONFIRMED |
| :45 | `scale = 6.3/3600.0` 之 6.3 | 6.3 | 夹具像元角尺度（″/px） | 待确认 | 条文面零提及（`git grep "6.3" -- docs` 无本模块锚） | 保守方向＝取生产同量级像幅，使 drop 角尺度与生产一致；影响＝门激励的几何域（见 CR-11-6 说明） | CONFIRMED（值本身无锚） |
| :45 | 3600.0 | 3600 | deg→arcsec 换算 | 结构性不适用 | 单位换算定义常数 | —— | CONFIRMED |
| :46-47 | `cd = {-scale,0;0,scale}` 的 −1 与 0 | −1/0 | RA 轴反向与无旋转 | 结构性不适用 | 天球坐标取向约定，非科学量 | —— | CONFIRMED |
| :48 | `sip.order = 1` | 1 | 声明的 SIP 阶数 | 待确认 | 与 :49-50 的系数下标不自洽（见 CR-11-3） | 保守方向＝order 与所写下标互证（线性畸变应写 a[6]/a[1]）；影响＝本夹具是否激励畸变路径 | CONFIRMED |
| :49 | `sip.a[0] = 1e-7` | 1e-7 | 意图为线性畸变，实为 A_00 常数项 | 待确认 | 求值口径 `wcs_sip.cpp:76-96`（`coeffs[i*6+j]·dx^i·dy^j`，i=j=0 即常数） | 保守方向＝按 i*6+j 重写为 a[6]；影响＝L0 的「带 SIP」夹具名不副实 | CONFIRMED |
| :50 | `sip.b[3] = -1e-7` | −1e-7 | 意图为线性畸变，实为 B_03（三阶） | 待确认 | 同上：`order=1` 时 i+j≤1 循环不触及下标 3 ⇒ 系数死置 | 保守方向＝改 b[1] 或提 order；影响＝同上 | CONFIRMED |
| :51-52 | `resize(size*size)` | size² | 像素缓冲长度 | 结构性不适用 | 数组维度 | —— | CONFIRMED |
| :54 | `cx = cy = size*0.5` | 0.5 | 源置于图心 | 结构性不适用 | 夹具几何定位 | —— | CONFIRMED |
| :55 | `sigma = size*0.08` | 0.08 | 高斯源宽度相对图幅 | 待确认 | §9 只规定 FIX-DRZ-B「点源高斯（总通量守恒）」，无 0.08 | 保守方向＝FWHM 与生产测光孔径同量级；影响＝逐 leaf 值的动态范围（进而影响 :137 分母下限是否生效） | CONFIRMED（无锚） |
| :56 | `amp = 500.0` | 500.0 | 高斯源峰值 ADU | 待确认 | 条文面零提及 | 保守方向＝与 base 同量级即可，不影响闭合判据；影响＝仅夹具幅度 | CONFIRMED |
| :60 | `base = 1000.0 + 0.01x + 0.005y` | 1000.0 / 0.01 / 0.005 | 常数底与两向梯度 | 待确认 | §9 规定 FIX-DRZ-A 常量场、FIX-DRZ-C 梯度场，但未冻结具体幅度/斜率 | 保守方向＝梯度不应使通量闭合的相对误差随 size 变化；影响＝仅夹具 | CONFIRMED |
| :62 | 高斯式中的 `2.0` | 2.0 | exp(−r²/(2σ²)) 归一因子 | 公式导出 | 高斯定义式；代码即按该式累加 | —— | CONFIRMED |
| :78, :110 | `cfg.nside = 64` | 64 | 门的 HEALPix 基网格 | 待确认 | §9 的 nside 枚举属 candidate_oracle（7 nside）；生产 nside≥2^17 见 docs/KNOWN_LIMITATIONS.md:139 | 保守方向＝至少并一档生产量级 nside；影响＝本门结论对生产几何域的外推资格 | CONFIRMED（无本门锚） |
| :78, :110 | `cfg.nested = true` | true | NESTED 寻址 | 结构性不适用 | 布尔枚举；§9 不变量「NESTED 地址往返」要求的就是该模式 | —— | CONFIRMED |
| :78, :110 | `cfg.pixfrac = 1.0` | 1.0 | 门的 drop 收缩系数 | 待确认 | pf=1 时 `sumNorm≡sumArea`、折算因子 k≡1（astro_sphere_sink.cpp:185, :534）⇒ 闭合门在 pf<1 域零覆盖；负责人裁的生产默认 pixfrac=0.8 | 保守方向＝补 pf=0.8 档闭合；影响＝「通量闭合」主张的适用域 | CONFIRMED |
| :79, :111 | `precision_mode = 1 / 0` | 1 / 0 | FP64 / FP32 路径选择 | 结构性不适用 | 合同枚举（lib/algorithms/drizzle/README.md:105「precision_mode 0/1/-1」） | —— | CONFIRMED |
| :79, :111 | `cfg.threads = 16` | 16 | 门的线程数 | 待确认 | AGENTS.md §6「不硬编码线程…由 benchmark 生成的 profile 决定」；§9 不变量要求 1/2/4 线程一致性，本门未测 | 保守方向＝取 1/2/4 三档并核一致；影响＝线程相关累加序误差是否被本门覆盖 | CONFIRMED |
| :83, :141 | `char name[128]` | 128 | 断言名缓冲长度 | 结构性不适用 | 缓冲区尺寸；snprintf 截断只影响打印文本不改判据 | —— | CONFIRMED |
| :122-123, :127, :132 | `depth`、`shift = 2*depth`、`<<shift \| local` | 2 | leaf 地址拼接 | 结构性不适用 | 位移量/索引合成，不参与科学量 | —— | CONFIRMED |
| :137 | `std::max(\|ref\|, 1.0)` 的分母下限 | 1.0 | 相对差的分母地板 | 待确认 | §9:304 的口径是「逐 leaf 相对差」，地板使小值 leaf 退化为绝对差 | 保守方向＝分母用 `max(\|ref\|, eps·\|typ\|)` 或另设绝对门；影响＝见 CR-11-4（方向偏绿） | CONFIRMED |
| :154 | `for (int size : {16, 32, 64, 128})` | 16/32/64/128 | 四档图幅序列 | 待确认 | 头注 :5 自述「16x16 ~ 128x128」，条文 §9 未冻结档位序列 | 保守方向＝至少含一档 ≥2048² 生产量级（现由 L2/L3 声称）；影响＝闭合结论的尺寸适用域 | CONFIRMED |
| :30, :159 | `g_pass/g_fail`、`return 0/1` | 0/1 | 计数与退出码 | 结构性不适用 | 退出码约定 | —— | CONFIRMED |

### 1.2 findings

### CR-11-1 · FP32/FP64 逐 leaf 门取 2e-5，把冻结的 1e-5 放宽一倍且同一文件头注仍写 1e-5
- 轴：C（主）/ S
- 位点：`drizzle_l0_test.cpp:147` `CHECK(max_rel < 2e-5, name);`；对照同文件 `:7` `// 3. FP64 严格参考门: FP32 vs FP64 逐 leaf 相对差 < 1e-5 (float 累计噪声)`
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9（TEST-DRZ-DESIGN-001 冻结容差）:304「FP64 通量闭合 <1e-6（主域）；FP32/FP64 逐 leaf <1e-5」，:305 且点名「l0 小图 FP64<1e-10」＝本文件即在册执行件。条文面零提及 2e-5（`git grep -n "2e-5" -- docs` 于本模块无门限命中）。
- 现状：`:142-144` 注释「门限 2e-5: float 累计固有精度 (IEEE binary32 累加舍入, 小图 n=178 时实测 ~1.1e-5; L2 生产尺度实测 p95 2.6e-7/max 4.9e-7; P4 要求真实 ULP 报告而非固定门限)」，随后 `:145` 打印串也写 `(<2e-5`。
- 差在哪：门的不等式方向是 `max_rel < 阈值`，阈值放大＝判据变宽，因此 `max_rel ∈ [1e-5, 2e-5)` 的产物在冻结口径下应判红、在本门判绿；注释自述小图实测已达 1.1e-5，即实现噪声本身就落在被放宽的区间内。
- 后果：对外「FP32/FP64 逐 leaf 一致 <1e-5」这一 TEST-DRZ-DESIGN-001 主张以放宽后的门为凭；若 1.1e-5 是真实水平，则该冻结容差根本不可达，属需走 §8 变更流程的科学口径冲突，被静默就地消化。
- 定级：S2（缺运行时触发路径：本文件未注册进 ctest，见 CR-11-2，不产生对外产品字节）
- 怎么算修好：要么把门改回 1e-5 并同批核实 FP32 累加路径是否达到（达不到则按 §8 提变更、在 DRIZZLE_GEOMETRY §9 改冻结值并留推导），要么在 §9 增列「l0 小图 2e-5」并给出 178 叶实测的跟踪面标定件；同批必须同步 :7 头注与 :145 打印串，并登记 1.1e-5/2.6e-7 两个实测数的落盘位置。
- 置信：CONFIRMED（条文、代码、文件头注三方数值已逐字比对；未实跑复算 1.1e-5）

### CR-11-2 · 自称"科学门"的 L0（及 L2/非有限/科学补齐）根本没有注册进 ctest，只有编译目标
- 轴：D（主）/ C
- 位点：`drizzle_l0_test.cpp:2` `// drizzle_l0_test.cpp - Drizzle L0 科学门 ( Validation_Strategy L0)`
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:296 标题即「TEST-DRZ-DESIGN-001（测试设计冻结，可执行 TEST-P1-DRZ-001 由 P1-DRZ-TEST 建立）」，:305 把 l0 点名为冻结容差的执行面。条文面零提及 "Validation_Strategy"：`git grep -rn "Validation_Strategy"` 全仓只命中 `drizzle_l0_test.cpp:2` 与 `drizzle_l2_test.cpp:2` 两处注释本身（无该文档）。
- 现状：`tests/CMakeLists.txt:158-181` 把 `drizzle_l0_test`、`drizzle_l2_test`、`drizzle_science_completion_test` 等列入 `DRZ_EXTRA_TESTS`，foreach 只 `add_executable`；:155-157 原文「其余作为**可执行证据件**保留 (可手工运行), 不注册 —— 避免把与本轮无关的历史红灯带进 ctest 基线」；本目录 `add_test` 清单（:48,54,59,66,84,101,118,133,142,204,252,300,321,325）不含它们。`drizzle_nonfinite_test` 单独 `add_executable`（:185-193）同样无 `add_test`。
- 差在哪：设计冻结的容差需要一个常驻可执行门；这里把「不注册」的理由写成"历史红灯"，即判据本身已知不绿而选择让它离开基线，同时源文件头仍对外称"科学门"。
- 后果：L0/L2/非有限/补齐四张门的绿红状态不进 CI 基线；§9 所列冻结容差的"可执行"一面在 CI 上不可证；而 `eng/ci/prod_wiring_baseline.json:53` 仍写着 `unbuildable_test:lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_l0_test.cpp`（与 CMakeLists 现状已不同步，登记面滞后）。
- 定级：S2（生产产物不受影响；对外"门在跑"的主张失真）
- 怎么算修好：把门注册进 ctest 或把 §9 的执行面明确改指已注册件（drizzle_acceptance / drizzle_freeze / drizzle_candidate_oracle）并同步措辞；头注的 "Validation_Strategy L0" 改成真实载体（DRIZZLE_GEOMETRY §9）；同步清理 `prod_wiring_baseline.json` 的过期 `unbuildable_test:` 键（须由前台按 §10 确认）。注意：直接注册会把现有红灯带进基线，必须与 CR-11-1、CR-11-5 的处置同批，否则改对反而判红。
- 置信：CONFIRMED（注册面与基线面均逐字读过）

### CR-11-2b · 三门共用 `cfg.threads = 16`，而公共头自身点名"禁止硬编码 16"
- 轴：C（主）/ D
- 位点：`drizzle_l0_test.cpp:79` `cfg.precision_mode = 1; cfg.threads = 16;`（同 :111）、`drizzle_l2_test.cpp:62` 同型、`drizzle_l3_full_fp32.cpp:63` `cfg.threads = 16;`
- 上位依据：`lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.h:40-41` 原文「`// 线程数 (0=自动 omp_get_max_threads; 禁止硬编码 16)` / `int threads = 0;`」；AGENTS.md §6 硬禁令「不硬编码线程/ISA/block：由 benchmark 生成的 profile 决定」；`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:316 不变量列「1/2/4 线程一致性」；`docs/architecture/PERFORMANCE_MODEL.md:32,106-108` 记生产 drizzle 的实际并行宽度受 scratch 槽上限 K=2 约束、租约 `cap=16`。
- 现状：三门都把线程写死为 16，从未测 1/2/4 档，也未取 0（自动）。
- 差在哪：被点名的字面值恰是头注禁止的那个；且门所测的累加序（16 线程合并）与生产节点（K=2 持槽）不是同一形态。
- 后果：§9:316 的"1/2/4 线程一致性"不变量在这三张被称"科学门"的件里无人执行；线程相关的累加序差异只在 16 档被采样。
- 定级：S3（不产生错值；属违禁写法＋不变量覆盖缺口，与 CR-11-2 同批处置）
- 怎么算修好：三门统一改 `threads = 0` 并另立一档 1/2/4 一致性比对（同数据、同 cfg，只变线程），或在 §9 明确该不变量的执行件是谁；`docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:304` 已把 `reference_overlap.cpp` 的「module-internal OpenMP, thread count via runtime/orchestrator set_num_threads」列为 V5 违规待收编，本批三门属同一收编面。
- 置信：CONFIRMED（三处位点与两条禁令原文均逐字引到）

### CR-11-3 · 头注称候选零漏选"由独立 reference_overlap 蒙特卡洛单独验证"，而该参考路径实为在册红灯且不注册
- 轴：C（主）/ D
- 位点：`drizzle_l0_test.cpp:8` `// 4. 候选零漏选由独立 reference_overlap (蒙特卡洛) 单独验证 (另测)`
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:308-309「候选零漏选: 9003 例 false_negative=0（candidate_oracle_test，4 pixfrac × 5 尺度 × 7 nside 全枚举，RA 跨 0/极区/face 边界）」——条文把这条主张锚在 `candidate_oracle_test`，不是 `reference_overlap`。
- 现状：`tests/CMakeLists.txt:256-268`：`reference_overlap` 只 `add_executable`，`add_test` 行被注释；:263-265 原文「**不注册 ctest**: 一次性实跑该独立蒙特卡洛参考路径得 "32 通过, 3 失败" (run/WIRING-W34-01/evidence/reference_overlap.txt): 三条断言分别差 3.4e-19 (tol 3.0e-20)、2.6e-6 (tol 4.1e-13)、rel_err 1.04e-6 (tol 1e-6)」，:266-267「容差与断言属科学判据面, 本轮禁止放松/删改 ⇒ 只编入构建图 (零漂移), 红灯上呈前台裁决」。
- 差在哪：L0 头注把"另测已验证"当作证据指向，被指向的件是三条断言已知不满足、且证据只落在 `run/`（非跟踪面）的未注册参考实现；真正的在册门 candidate_oracle_test 反而没被引用。
- 后果：任何人据 L0 文件头追溯"零漏选"证据会走到一个红灯件；独立参考路径与主实现的面面积一致性（3 条断言）至今无跟踪面结论，候选零漏选主张只剩单侧证据。
- 定级：S2（不影响产品字节；影响对外主张的证据链）
- 怎么算修好：① 头注 :8 改锚 `candidate_oracle_test`（在册、9003 例）；② reference_overlap 的三条容差按 §8 变更流程裁定（要么承认独立路径精度不支撑该容差并修订断言，要么修主实现 `spherical_overlap`），并把结论落跟踪面报告而不是 `run/`；③ 若该件长期不注册，在其源文件头写明"非在册门、结果未收敛"。
- 置信：CONFIRMED（两处条文与注册面原文均直接引到；三条断言差值本身未复算＝该项 PARTIAL）

### CR-11-4 · 逐 leaf "相对差"的门分母被 `max(|ref|,1.0)` 托底，小通量叶自动判绿（方向已定：偏绿）
- 轴：R
- 位点：`drizzle_l0_test.cpp:136-137` `double r = std::fabs((double)tile.pixels[local].sumFlux - it->second) / std::max(std::fabs(it->second), 1.0);`
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:304 的口径是「FP32/FP64 逐 leaf **相对差** <1e-5」。条文面零提及任何分母地板（`git grep -n "1.0)" -- lib/algorithms/drizzle/healpix_drizzle/tests` 无同类注释）。
- 现状：分母取 `max(|ref|, 1.0)`，且 `:148-149` 只核「FP32 leaf 均可在 FP64 找到 (missing==0)」，无绝对噪声门。
- 差在哪：对 `|ref| < 1.0` 的叶，`r = |Δ|/1.0 ≤ |Δ|/|ref|`，即托底只会让 r **变小**，不等式 `r < 阈值` 因此更易满足 ⇒ 判定偏向绿；小通量叶（footprint 边缘的部分覆盖叶，FP32 相对误差通常最大）恰好是相对误差该被审的地方，却退化成了绝对差检验。
- 后果：FP32 路径在弱信号叶上的一致性缺陷可以整批通过 L0；而弱信号叶正是跨帧 SNR 主张里权重/方差最敏感的一档。
- 定级：S2（本门未注册 ctest，缺运行时触发路径；若注册后为 S1 候选）
- 怎么算修好：相对差按真相对（分母 `max(|ref|, tiny)`）并**另设**一条绝对噪声门覆盖 `|ref|→0`；同批把 :145 打印串与 CR-11-1 的阈值一起改，避免"只收紧分母、不补绝对门"导致小值叶集体判红。
- 置信：PARTIAL（方向与代码已 CONFIRMED；未统计本夹具下有多少叶的 sumFlux<1.0，需实跑 leaf 值分布才能定规模）

### CR-11-5 · 夹具的 SIP 系数下标与求值侧不一致，L0 名义"带 SIP"实际畸变为零
- 轴：R（主）/ D
- 位点：`drizzle_l0_test.cpp:48-50` `img.wcs.sip.order = 1;` / `img.wcs.sip.a[0] = 1e-7;` / `img.wcs.sip.b[3] = -1e-7;`
- 上位依据：求值口径 `wcs_sip.cpp:73-96`「下三角: i+j <= order；公式: `result = Σ_{i+j<=order} coeffs[i*6+j] * dx^i * dy^j`」；写侧同源 `hp_drizzle_api.cpp:478` `wcs.sip.a[i * 6 + j] = atof(val)`（FITS A_/B_ 关键词按 (i,j) 行主序装入）。`docs/algorithms/DRIZZLE_GEOMETRY.md` §8:292-294 亦以 `AP_*/BP_*` 键名为准。
- 现状：下标 0 = A_00（i=j=0 常数项，量值 1e-7 像素，等同无畸变），下标 3 = B_03（i+j=3 > order=1）在 `for (i=0..order) for (j=0..order-i)` 中被完全跳过；线性项应为 a[6]（A_10）或 a[1]（A_01）。同型写法在 `bench_drizzle.cpp:47-48` 与本批 `drizzle_science_completion_test.cpp:68-71` 重复出现。
- 差在哪：CTYPE 写 `"RA---TAN-SIP"` 且 `has_wcs=true`、`order=1`，但按求值式展开后 f ≡ 1e-7、g ≡ 0，畸变路径被进入却零激励 ⇒ "带 SIP 的闭合" 实为 "无畸变闭合"。
- 后果：L0 对 SIP 变换下的通量闭合与候选生成没有任何覆盖力；依赖 L0 绿来主张"WCS 畸变场景闭合"不成立（§9 的 FIX-DRZ-F「SIP 畸变边缘 patch（15° 宽场）」是另一夹具，L0 未承担）。
- 定级：S2（本门未注册 ctest；若注册或作为其他门的夹具复用则升 S1）
- 怎么算修好：把线性项改成 `sip.a[6]`/`sip.b[1]`（并让 `order` ≥ 所写最大 i+j），同批核 `bench_drizzle.cpp:47-48` 与 `drizzle_science_completion_test.cpp:68-71`（后者的行注释把 a[0] 当线性、a[12] 当三次，按 `i*6+j` 展开是 A_00 与 A_20，见 CR-11-13）；并在 `hp_drizzle_api.h`/`astro_image_io` 的 sip 字段处补一句"下标 = i*6+j"的约定注释，防止再按紧凑序写。
- 置信：CONFIRMED（求值式与写侧装载式均逐字读到，展开可手算）

### CR-11-6（登记为观察，非指控）· L0 的几何域与生产不同域：`nside=64` + `6.3″/px` 使整幅图落进极少数 parent 像素
- 依据：`docs/KNOWN_LIMITATIONS.md:139`「生产 nside（≥2^17）时 ≤1e-9 可忽略；但 PERF-SCALE-001 的 W1 配置用 nside=512，此时不连续达 1.9e-6，与冻结的「FP64 通量闭合 <1e-6」同阶」——同一域外风险在 nside=64 更甚（弧-弦亏缺标度 0.5/nside² ⇒ 64 处约 1.2e-4）。
- 现状：门在 4 档图幅（16…128）× 单 nside=64 × pf=1 上取绿；注释 :143 的 n=178 叶数与此规模自洽。
- 结论：不判为缺陷（L0 的定位可辩护），但"L0 绿"不构成对生产 nside 域闭合的任何证据，跨域外推须由 L2/L3 承接——本条供 CR-11 批次后续文件对照。
- 定级：S3（观察）｜置信：CONFIRMED（仅就域关系而言，未实跑）

## 2. `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_l2_test.cpp`（146 行，整读完毕）

新取证到的共享锚（对后 4 个文件同样适用）：
- `drizzle_engine.h:40-41`：`// 线程数 (0=自动 omp_get_max_threads; 禁止硬编码 16)` / `int threads = 0;` —— **公共头自身点名禁硬编码 16**，而本文件 :62 与 L0 :79/:111 都写 `cfg.threads = 16`。
- `drizzle_engine.h:44` `uint32_t tile_depth = 0;`（0=auto=`compute_tile_depth(nside)`，:338）——两文件的 leaf key 重建与引擎一致，无碰撞（已核）。
- `TileAccumulatorT::parent_ipix` 为 `uint64_t`（drizzle_engine.h:91）⇒ `parent_ipix << shift` 在 64 位下不溢出（已核，nside=65536 时 parent 数 12·nside²=5.15e10 确需 >32 bit）。
- 生产 NSIDE 面：`docs/KNOWN_LIMITATIONS.md:139`「生产 nside（≥2^17）」；`lib/infrastructure/pipeline/orchestrator/memory.md:578` nside 自动选择钳制 `[64, 131072]`；在册验收件 `drizzle_acceptance_test.cpp:232 {3.0, 131072}`、`acceptance_drizzle.py:194 (2.0, 262144), (3.0, 131072)`。
- 夹具名跟踪面检索式与结果：`git grep -rn "l2_crop\|galaxy_crop\|Galaxy_Center" -- .` ⇒ `run/temp/l2_crop` 与 `galaxy_crop_1024.fits` **全仓零命中**；跟踪的真实数据集名是 `testdata/Galaxy_Center_T4`（testdata/index.json:74）。
- `1e-8` 在 drizzle 通量闭合口径上的锚：`git grep -rn "1e-8" -- docs/algorithms docs/science docs/contracts` 命中项全属 WCS 往返（1e-8 px）、LM 收敛、RCR 相等容差，**无一条**是 drizzle FP64 通量闭合门。

### 2.1 §2.5 逐数值处置表（本文件 24 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :8 | 头注自述逐 leaf 门 | 1e-5 | FP32/FP64 一致性口径（自述） | 文献值 | DRIZZLE_GEOMETRY.md §9:304「FP32/FP64 逐 leaf <1e-5」 | —— | CONFIRMED |
| :115, :117 | `max_rel < 1e-5` | 1e-5 | 实际执行门 | 文献值 | 同上 §9:304 | —— | CONFIRMED |
| :141 | `pct(0.95) < 1e-5` | 1e-5（同一阈值第二次用） | "B 门：FP32 误差分布" 的 p95 门 | 待确认 | 条文 §9 只有"逐 leaf 相对差"一条口径，无 p95 档 | 保守方向＝删该冗余门或改为独立量（如 mean/p99 上限）；影响＝见 CR-11-9（逻辑蕴含 :117，无独立判据资格） | CONFIRMED |
| :7, :85, :87 | `rel < 1e-8`（FP64 通量闭合） | 1e-8 | 真实大帧的 FP64 闭合门 | 待确认 | 条文面零提及（§9:304-305 只给 1e-6 主域 / l0 1e-10） | 保守方向＝取条文主域 1e-6 为外门、并说明本值更严的理由；影响＝本门比主域冻结值严 100×，任何 FP64 累加序噪声增长即脆红，且不可由文档复算 | CONFIRMED（无锚） |
| :31 | 默认输入 `run/temp/l2_crop/galaxy_crop_1024.fits` | 路径 | 门的真实数据夹具 | 待确认 | 跟踪面零命中（上述检索式）；`run/` 按 §2.5 判不合格载体 | 保守方向＝夹具改到跟踪面或补可复现生成脚本并落跟踪说明；影响＝整张门的任何复算资格（见 CR-11-8） | CONFIRMED |
| :4 | 头注 `1024x1024` 裁剪 | 1024 | 夹具视场边长 | 待确认 | 裁剪步骤无任何跟踪面记录（随 :31 一并悬空） | 保守方向＝登记裁剪命令与来源帧；影响＝同上 | CONFIRMED |
| :4 | 头注 `SIP order=3` | 3 | 夹具畸变阶声明 | 待确认 | 该值由 FITS 头决定，代码不校验；本仓对 Galaxy_Center T4 的 SIP 无跟踪登记 | 保守方向＝门里断言 `img.wcs.sip.order==3` 而非只在 printf 里念；影响＝若夹具实为 order 0/1，则 L2 也不是畸变域证据 | PARTIAL（夹具不在跟踪面，无法核） |
| :4 | 头注数据集名 `Galaxy_Center_mosaic3_T4` | —— | 数据来源标识 | 待确认 | 跟踪面数据集名是 `Galaxy_Center_T4`（testdata/index.json:74）；CI 夹具里出现过 `calibrated_Galaxy_Center_mosaic1_T4_...`（eng/ci/fixtures/run_manifest/real_manifest_sample.json:8）⇒ mosaic1/mosaic3 两副面孔 | 保守方向＝以 testdata/index.json 名为准；影响＝来源帧不可追溯 | CONFIRMED |
| :45 | `pixels_f64[i] = img.pixels[i]` | —— | float→double 升精 | 结构性不适用 | 无新常数；但见 CR-11-10（该转换使"FP64 路径"实为 FP32 数据的精确表示） | —— | CONFIRMED |
| :59 | `cfg.nside = 65536` 且注释"生产 NSIDE" | 65536 | 门的基网格 | 待确认 | 与 KNOWN_LIMITATIONS.md:139「生产 nside（≥2^17）」、acceptance 侧 131072/262144 不同值 | 保守方向＝取 ≥131072；影响＝"L2 已用生产条件"这一对外措辞（见 CR-11-7） | CONFIRMED |
| :60 | `nested = true` | true | NESTED 寻址 | 结构性不适用 | 枚举；§9 不变量要求 NESTED 往返 | —— | CONFIRMED |
| :61 | `pixfrac = 1.0` | 1.0 | drop 收缩系数 | 待确认 | 负责人裁生产默认 0.8；pf=1 时折算因子 k≡1（astro_sphere_sink.cpp:185）⇒ 归一链未激励 | 保守方向＝补 pf=0.8 档闭合；影响＝"真实帧闭合"结论的适用域 | CONFIRMED |
| :62 | `precision_mode = 1` | 1 | FP64 路径 | 结构性不适用 | 合同枚举（README.md:105 0/1/-1） | —— | CONFIRMED |
| :62, :94 | `threads = 16` / `precision_mode = 0` | 16 / 0 | 线程数 / FP32 路径 | 待确认（threads=16） | `drizzle_engine.h:40` 原文「线程数 (0=自动 omp_get_max_threads; **禁止硬编码 16**)」；AGENTS.md §6 同禁 | 保守方向＝取 0（自动）并另立 1/2/4 一致性档（§9:316）；影响＝门所测累加序与生产不同 | CONFIRMED |
| :72-73 | `depth`、`shift = 2*depth` | 2 | leaf 地址位移 | 结构性不适用 | 位移量；与引擎 `eff_tile_depth`（:338，tile_depth=0）一致，已核无碰撞 | —— | CONFIRMED |
| :80, :105 | `parent_ipix << shift \| local` | —— | key 合成 | 结构性不适用 | 索引合成，uint64 已核 | —— | CONFIRMED |
| :83 | 分母 `std::fabs(sum_in)` | —— | 相对闭合分母 | 结构性不适用 | 相对误差定义式；sum_in=0 时 rel=NaN ⇒ `NaN<1e-8` 为假，判红不判绿（方向已核） | —— | CONFIRMED |
| :84, :139 | `char name[160]` / `name2[160]` | 160 | 断言名缓冲 | 结构性不适用 | 缓冲区尺寸（snprintf 截断仅影响打印） | —— | CONFIRMED |
| :110 | `std::max(\|ref\|, 1.0)` 分母地板 | 1.0 | 逐 leaf 相对差地板 | 待确认 | 同 CR-11-4：§9:304 口径为"相对差"，地板使其对小值叶退化 | 保守方向＝真相对差＋独立绝对噪声门；影响＝弱信号叶一致性 | CONFIRMED |
| :125-127 | `pct` 的 `idx = (size_t)(p*n)` 与上界钳制 | —— | 百分位取点 | 结构性不适用 | 实现选择（无插值），只影响报告的 p 值不影响任何 `CHECK` 阈值（除 :141） | —— | CONFIRMED |
| :132 | `2^-24 ≈ 5.96e-8` | 2⁻²⁴ | binary32 在 [0.5,1) 的 1 ULP | 公式导出 | ULP 定义：`u=2⁻²⁴`（IEEE754 binary32 尾数 23 位＋[0.5,1) 区间），代码仅 printf 使用，未参与判据 | —— | CONFIRMED |
| :133 | `1e-7 ≈ 1.68 ULP` | 1e-7 / 1.68 | ULP 归一因子与换算 | 公式导出 | 1e-7/5.96e-8 = 1.678 ⇒ 换算正确；:138 `pct(...)/1e-7` 即按此式算，报告用 | —— | CONFIRMED |
| :130-131 | `mean = sum/n` | —— | 均值 | 公式导出 | 算术平均定义，代码按式算 | —— | CONFIRMED |
| :23, :144-145 | 计数器与退出码 | 0/1 | 汇总 | 结构性不适用 | 退出码约定 | —— | CONFIRMED |

### 2.2 findings

### CR-11-7 · 注释把 65536 宣称为"生产 NSIDE"，但该推导式与"生产 nside"的取值在登记面互相冲突
- 轴：C（主）/ S
- 位点：`drizzle_l2_test.cpp:59` `cfg.nside = 65536;   // 生产 NSIDE (: 不再用 2048 替代生产条件)`
- 上位依据：`docs/KNOWN_LIMITATIONS.md:139`「**生产 nside（≥2^17）**时 ≤1e-9 可忽略；但 PERF-SCALE-001 的 W1 配置用 `nside=512`，此时不连续达 1.9e-6」；`lib/infrastructure/pipeline/orchestrator/memory.md:574,578` 给出自动档规则「nside_override>0 直接返回（规整到 2 的幂，范围 [64,131072]」/「找到不小于 nside_target 的最小 2 的幂次方，限制 [64, 131072]」；常数 `210960` 的实现锚 `tests/test_nside_pixfrac.cpp:185`「210960/1.0 = 210960, 2^17=131072 < 210960, 2^18=262144 >= 210960」。
- 现状：门里是一个字面量 65536 加一句"生产 NSIDE"的断言；我按自动档式复算 `210960/6.3076 = 33,446 → 65536`（该帧尺度取自 `docs/algorithms/GATES_AND_TOLERANCES.md:91` 的 T4 Galaxy_Center 6.3076″/px），**故 65536 对本帧自洽，我撤掉"用错一档"的读法**；同时 `KNOWN_LIMITATIONS.md:139` 把"生产 nside"写成 ≥2^17，与本注释对同一措辞取不同值。
- 差在哪：同一措辞"生产 NSIDE"在测试注释（65536）与已知限制文档（≥2^17）指两个网格，而门侧的数值既没有导出式也没有引用规则；`0.5/nside²` 的弧-弦亏缺（:139 同段）恰是该文档用来论证"生产可忽略"的量，其结论随本门取 2^16 而改变（2^16 → 7.6e-11，2^17 → 1.9e-11）。
- 后果：读者按 :139 认定"L2 已在生产网格上"或按 :59 认定"≥2^17 才是生产"，两面对 arC-chord 误差预算的结论不同；本门对 §9 冻结容差的执行域不可判定。
- 定级：S3（措辞与登记面不一致；数值本身我已复核成立）
- 怎么算修好：在 :59 旁写出导出式与输入尺度（`210960/scale″ → 65536`，规则锚 orchestrator/memory.md:578），并在 KNOWN_LIMITATIONS.md:139 把"生产 nside（≥2^17）"改为按仪器/尺度分档的表述（属文档侧变更，走 §8）；两批同改，避免注释与限制文档各说一遍。
- 置信：CONFIRMED（两侧原文与我按 :185 规则的复算均已给出）

### CR-11-8 · L2 的真实数据夹具只存在于 `run/temp/`（gitignore），跟踪面既无该文件也无生成步骤
- 轴：S（主）/ C
- 位点：`drizzle_l2_test.cpp:30-31` `const char* path = (argc > 1) ? argv[1] : "run/temp/l2_crop/galaxy_crop_1024.fits";`
- 上位依据：AGENTS.md §7「testdata/ 真实数据与外部只读数据集索引（只读；README.md 为入库/下载策略唯一说明…FITS 大文件不入库）」、`run/` 定义为「临时产物/日志（gitignore，不入库）」；负责人裁定口径"运行产物一律不留"（记忆卡 Evidence retention doctrine）。
- 现状：默认输入落在 `run/temp/l2_crop/`；`git grep -rn "l2_crop\|galaxy_crop"` 全仓 0 命中（含文档、CI 注册表、脚本），头注 :4 声称的来源"Galaxy_Center_mosaic3_T4 中心 1024x1024, SIP order=3"在跟踪面无任何裁剪记录；跟踪面登记的数据集名是 `Galaxy_Center_T4`。
- 差在哪：一个宣称"真实单帧"的科学门，其输入不在入库策略说明的任何一侧（不在 testdata 索引，也不是可再生的跟踪产物），因此该门的结论既不可复算也不可追溯，且 `argc<=1` 时静默用这条路径。
- 后果：TEST-DRZ-DESIGN-001 的"真实数据"一面（L2）与 testdata 索引不同源；任何"L2 绿"的历史陈述都无法被第三方重跑；:36 读不到文件即 `return 1`，故在干净检出上它不是"跳过"而是恒红——一个恒红且不在 CI 的门与"门"这个自称相互矛盾。
- 定级：S2（不在 CI 基线内 ⇒ 无产品影响；若注册即为可复现性缺陷）
- 怎么算修好：把裁剪夹具登记进 `testdata/README.md` 的入库/下载策略（或提供一个跟踪面的只读生成脚本，输出落 `run/` 但脚本与来源帧名入库），并把路径改由 CLI/env 注入、`argc<=1` 时显式报"夹具缺失"而非默认私有临时目录；同步把 :4 的来源名与 testdata/index.json 的 `Galaxy_Center_T4` 对齐。
- 置信：CONFIRMED（跟踪面零命中检索式与命令已记录在上）

### CR-11-9 · "误差分布 p95 门"被同一文件的 max 门逻辑蕴含，属无独立判据资格的恒绿断言
- 轴：R
- 位点：`drizzle_l2_test.cpp:141` `CHECK(pct(0.95) < 1e-5, name2);`（对照 :117 `CHECK(max_rel < 1e-5, name);`）
- 上位依据：AGENTS.md §5「判据必须非退化……恒真门没有证据资格」；`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:304 对逐 leaf 一致性只规定一条口径（max 相对差），未规定 p95 档。
- 现状：:122-142 先把 `rel_diffs` 升序 `std::sort`，`pct(0.95)` 取 `rel_diffs[(size_t)(0.95*n)]`，随后 `:136` 用 `rel_diffs.back()` 作 max 报告，:141 判 p95。
- 差在哪：排序数组的 `pct(0.95)` 索引 `0.95n < n`，恒 ≤ `rel_diffs[n-1] = max_rel`；不等式方向为 `p95 ≤ max`，故 `max < 1e-5 ⇒ p95 < 1e-5`——只要 :117 判绿，:141 必绿（:117 判红时 :141 也只是"顺带可能绿"，不新增发现能力）。
- 后果：注释宣称的"B 门: FP32 误差分布"实际没有独立判据；用它作为"分布层面达标"的证据是把 max 门的结果重复计一次，会虚增 §9 冻结面的门数（汇总里出现两个独立门，实为一个）。
- 定级：S3（只影响判据资格与门数陈述，不改变产品值）
- 怎么算修好：把 :141 改为与 max 门不互相蕴含的量（例如 mean < 阈值、或 p99/ULP 上界，并给这些量各自的条文锚），或直接删去并如实把该段降为"报告"而非门；同批把 CR-11-1 的阈值口径一次定死，避免"改了 max 阈值后 p95 门突然变成唯一有效门"的假象。
- 置信：CONFIRMED（蕴含关系由 :123 升序排序与 :125 索引式直接推出，无需实跑）

### CR-11-10 · FP64 路径读的是 float 升精数据，故本门"FP64 vs FP32"差值不含输入量化误差
- 轴：R（主）/ C
- 位点：`drizzle_l2_test.cpp:42-46` `if (img.pixels_f64.empty()) { img.pixels_f64.resize(...); for (...) img.pixels_f64[i] = img.pixels[i]; }`
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:304 的冻结量名为「FP32/FP64 逐 leaf <1e-5」，未定义"输入侧同为 FP32"这一前置；`fits_reader` 侧的填充合同在本文件 :40-41 注释自述：「fits_reader 只填充 float 像素; FP64 路径需要 pixels_f64 (FITS 16-bit 源在 float 中精确, 转 double 无损)」。
- 现状：本门从不读真正的 FP64 数据体，两条路径共享同一 float 量化输入，比较的仅是累加/传递链路的精度。
- 差在哪：注释的"无损"只对 16-bit 整型成立（float 尾数 24 bit ≫ 16 bit）；而 T4 科学帧若为 float32/含缩放（`BSCALE≠1`）或已定标 ADU，则 `pixels` 本身即已是量化/舍入结果，`pixels_f64` 不恢复任何信息。此时"FP64"标签给出的是**双精度运算的假象**，而非"真值参考"。
- 后果：FP64 通量闭合与逐 leaf 参考门所支撑的"高精度参考链"主张，其真值上限被输入侧量化钉住；若产品链某处依赖 `pixels_f64` 与 `pixels` 同源这一隐含条件，L2 无法暴露之。
- 定级：S2（门的语义与主张失真；无产品字节触发路径——本门未注册 ctest）
- 怎么算修好：在门内显式断言源位深与 `BSCALE/BZERO`（例如打印 `fits_bitpix` 并对非 16-bit 整型输入改走原生 double 读路径），或把该门改名为"FP32/FP64 累加链路一致性（同一量化输入）"，避免"FP64 参考"字样承担它没测的东西。
- 置信：PARTIAL（代码事实 CONFIRMED；夹具真实位深不可核——夹具不在跟踪面，见 CR-11-8）

### CR-11-11 · 与 L0 对同一冻结容差取两个不同阈值（L0 2e-5 / L2 1e-5），L0 注释还以"L2 实测"为 2e-5 背书
- 轴：C
- 位点：`drizzle_l2_test.cpp:117` `CHECK(max_rel < 1e-5, name);` 对照 `drizzle_l0_test.cpp:147` `CHECK(max_rel < 2e-5, name);` 与 `drizzle_l0_test.cpp:143-144`「L2 生产尺度实测 p95 2.6e-7/max 4.9e-7」
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:304 单值 1e-5（"取值一律按本节"，:303 原文「冻结容差（预冻结，源自既有测试门，取值一律按本节）」）。
- 现状：两个入口、同一判据名、两个数；且取值宽的那一侧引用的依据（L2 实测 4.9e-7）恰恰来自取值严的这一侧。
- 差在哪：§9:303 明确"取值一律按本节"，本批两文件对该条各自取值 ⇒ 同一件事在代码/条文/注释三面不同。
- 后果：任何"L0+L2 双门皆绿"的陈述在阈值口径上不自洽；若将来把 L2 也放宽到 2e-5，则 1e-5 冻结面彻底无执行者；反之 L0 收紧到 1e-5 就会红（注释自述小图实测 1.1e-5）。
- 定级：S2
- 怎么算修好：一次裁决 1e-5 是否可达：可达则 L0 改 1e-5 并把小图 1.1e-5 作为实现缺陷登记（查 FP32 累加顺序/求和域），不可达则按 §8 变更流程改 §9 并同批改 L0/L2/头注/打印串三处；禁止在两文件各留一值。
- 置信：CONFIRMED（两侧原文与条文逐字对读）

## 3. `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_l3_full_fp32.cpp`（110 行，整读完毕）

本文件取证到的关键反推（用于判 nside 档是否为"生产档"）：
- 自动 NSIDE 规则的实现侧锚：`tests/test_nside_pixfrac.cpp:185` 原文 `// 210960/1.0 = 210960, 2^17=131072 < 210960, 2^18=262144 >= 210960`，即 `nside = 不小于 210960/scale_arcsec 的最小 2 的幂`，钳制区间 `[64, 131072]`（`lib/infrastructure/pipeline/orchestrator/memory.md:574,578`）。
- 本门夹具帧的像元尺度：`docs/algorithms/GATES_AND_TOLERANCES.md:91` 行 "T4 Galaxy_Center panel1 Red 180s | **6.3076** | …"。
- 复算：`210960 / 6.3076 = 33,446` ⇒ 下一 2 的幂 = **65536** ⇒ 本文件 :59 注释「与基线自动 NSIDE 一致」**对该帧成立**（我据此撤掉"L2 用错档"的读法，见订正后的 CR-11-7）。
- 叶数复算（判 :96 带宽是否可导出）：帧面积 = 4500·3600·(6.3076″→3.0576e-5 rad)² = 1.62e7·9.351e-10 = 1.515e-2 sr；nside=65536 的叶（= nside 网格像素）面积 = 4π/(12·65536²) = 2.4379e-10 sr ⇒ 期望叶数 = 6.214e7，与打印的"基线参考 61,592,234"差 0.87%（边缘/部分覆盖），且落进 `[6e7, 6.3e7]` ⇒ 该带是可导出的，但代码写死为常数带。
- 内存量级参照：`docs/architecture/PERFORMANCE_MODEL.md:192` ②「PERF-MEM-FIX-01 §3 实测 canonical 累加器 ≈2.36 GB（T4 配置 275 tile × 262144 leaf × 32 B）」；同文件 :32「Runtime lease（预算权威）cap=16」、:106-108「drizzle 每帧同时只有 2 条线程能持有 scratch map」、:39/:56 生产节点 drizzle 实测 240.30 s、:122 每帧 `drizzle_run≈109 s + hips_write≈34 s`。
- 门名与注册面：`docs/algorithms/GATES_AND_TOLERANCES.md` 全文 `git grep -n "drizzle"` 只命中 :115 的 DrizzlePac 许可条目 ⇒ **本门四个判据数（90s / 61,592,234 / 1e-4 / 4096 MB）与"Gate D"之名在门登记面零登记**；`eng/ci` 侧只有 `prod_wiring_baseline.json:55` 的（已过期）`unbuildable_test:` 键；`docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:160` 记为 `exe_target … test,no,-,非发布目标,…:277`。
- CMakeLists 自述矛盾（本门载体）：`tests/CMakeLists.txt:217` 「drizzle_l3_full_fp32 **Windows 专用** (windows.h/psapi.h) ⇒ 已删, 见报告」与 :270-286 的 `if(WIN32) add_executable + add_test` 并存——同一文件既说"已删"又把它注册为 ctest。

### 3.1 §2.5 逐数值处置表（本文件 20 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :4 | 头注 `4500x3600` | 4500 / 3600 | 夹具帧尺寸 | 待确认 | 夹具属性；代码从不校验 `img.width/height` 等于该值 | 保守方向＝门内断言尺寸（否则 :93 时间带与 :96 叶数带可被任意小帧满足）；影响＝三条判据的资格 | CONFIRMED |
| :5, :59 | `cfg.nside = 65536`（注"与基线自动 NSIDE 一致"） | 65536 | 门的基网格 | 公式导出 | 自动档式 `nside = 2^⌈log2(210960/scale″)⌉`（实现锚 test_nside_pixfrac.cpp:185 + orchestrator/memory.md:578）；对本帧 6.3076″/px（GATES_AND_TOLERANCES.md:91）算得 33,446 → **65536** | 代码未按式计算而写死；帧尺度一变即失配，与上一条同源 | CONFIRMED（我已按式复算） |
| :5, :61 | `pixfrac = 1.0` | 1.0 | drop 收缩系数 | 待确认 | 负责人裁生产默认 0.8；pf=1 时 k≡1（astro_sphere_sink.cpp:185）⇒ 归一链不激励 | 保守方向＝补 pf=0.8 档；影响＝"完整 FP32 验收"的适用域 | CONFIRMED |
| :5, :63 | `threads = 16` | 16 | 线程数 | 待确认 | `drizzle_engine.h:40` 原文「禁止硬编码 16」；AGENTS.md §6 同禁；PERFORMANCE_MODEL.md:32 记 cap=16 属**租约**而非硬编 | 保守方向＝取 0（自动）；影响＝:93 的时间门所测宽度与生产（scratch 槽 K=2，:106-108）不同 | CONFIRMED |
| :5, :62 | `precision_mode = 0` | 0 | FP32 路径 | 结构性不适用 | 合同枚举（README.md:105） | —— | CONFIRMED |
| :6, :92, :93 | `wall <= 90.0`（"目标 60s"） | 90 / 60 | 单次 drizzleTiled 墙钟上限 | 待确认 | 门登记面零命中（GATES_AND_TOLERANCES 无 drizzle 行）；`git grep "90s"` 仅命中本文件 :6 | 保守方向＝改为相对基线比值门并把机器档写进判据；影响＝异机即脆红/脆绿，与生产节点实测 240.30 s（PERFORMANCE_MODEL.md:39）不同口径 | CONFIRMED（无锚） |
| :6, :94 | "基线参考 61,592,234" | 61,592,234 | 输出叶数参考单值 | 待确认 | 全仓该数只出现在本文件 :6 与 :94（`git grep "61,592,234"`），无 results/报告/文档出处 | 保守方向＝落跟踪面标定记录或改按导出式比对；影响＝见 CR-11-13 | CONFIRMED |
| :96 | `n_leaf > 60000000LL && n_leaf < 63000000LL` | 6.0e7 / 6.3e7 | 叶数带门 | 公式导出 | 导出式 `N_leaf = (W·H·(scale_rad)²)/(4π/(12·nside²))` = 6.214e7（本文件已复算，与参考值差 0.87%）；**代码把结果写死成常数带而非按式算** ⇒ 同批报一条 S/D 轴 finding | 保守方向＝用导出的期望值 ± 相对带，并按 :4 的断言联动；影响＝换帧/换尺度即误判 | CONFIRMED |
| :7, :101 | "旧多 hash 结构 ~4.9GB 量级" | 4.9 GB | RSS 对照基线 | 待确认 | 跟踪面零命中（`git grep "4.9GB"` 于 docs 无本门条目）；可查的最近量是 PERFORMANCE_MODEL.md:192 的 2.36 GB canonical 累加器 | 保守方向＝引用 2.36 GB 这一在册量并把口径（FP32/FP64、tile 数）写明；影响＝"显著下降"主张无凭 | CONFIRMED |
| :103 | `peak_rss_mb() < 4096.0` | 4096.0 | 峰值工作集门 | 待确认 | 门登记面零命中；PERFORMANCE_MODEL.md:60 的 3.34/3.47 GB 是端到端 2 帧冒烟，另一口径 | 保守方向＝把失败兜底改为 NaN/判红并与 :192 的 2.36 GB 建立显式余量；影响＝见 CR-11-12（fail-open） | CONFIRMED |
| :97, :99 | `rel < 1e-4`（FP32 通量闭合） | 1e-4 | Σout/Σin 相对闭合门 | 待确认 | 条文 §9 只冻结 FP64 主域 <1e-6 与逐 leaf <1e-5，无 FP32 帧级闭合条 | 保守方向＝取与 FP32 结构地板同量级（u=5.96e-8，见下行）；影响＝见 CR-11-14 | CONFIRMED（无锚） |
| :86 | `sum_out += p.sumFlux`（double 累加 float 叶值） | —— | 累加结构 | 公式导出 | 相干误差上界 = Σu\|xᵢ\|/Σ\|xᵢ\| = u = 2⁻²⁴ = 5.96e-8（外层用 double 累加，故仅剩叶内 float 舍入）；代码即如此实现 | —— | CONFIRMED |
| :33 | `1024.0 * 1024.0` | 1048576 | B→MiB 换算 | 结构性不适用 | 单位换算定义常数 | —— | CONFIRMED |
| :34 | `return 0.0;`（API 失败兜底） | 0.0 | GetProcessMemoryInfo 失败时的 RSS | 待确认 | 条文面零提及；0 恰好满足 `< 4096` | 保守方向＝失败时返回 -1/NaN 使判红；影响＝CR-11-12 | CONFIRMED |
| :39 | 默认输入路径 `testdata/Galaxy_Center_T4/lights/panel3/…180S-Red.fts` | —— | 真实帧夹具 | 结构性不适用 | 路径本身受登记：`testdata/index.json:349-355`（panel3 存在，Red 10 帧）、`docs/validation/v6/QA_MATRIX.md:616`（`testdata/Galaxy_Center_T4/lights` 为登记输入面）、testdata/README.md:20（T4 帧不入库） | —— | CONFIRMED |
| :49 | 注释「省 130MB RSS」 | 130 MB | 不复制 f64 的收益 | 公式导出 | 4500·3600·8 B = 1.296e8 B = 123.6 MiB ≈ 130 MB，注释与式一致；代码不做该计算（仅注释） | —— | CONFIRMED |
| :79-80 | `depth`、`shift = 2*depth` | 2 | leaf 地址位移 | 结构性不适用 | **本文件中两变量声明后无任何使用点**（本文件无 ref map）＝死代码，编译期 -Wunused 面 | —— | CONFIRMED |
| :91 | `char name[160]` | 160 | 断言名缓冲 | 结构性不适用 | 缓冲区尺寸 | —— | CONFIRMED |
| :23, :109 | 计数器与退出码 | 0/1 | 汇总 | 结构性不适用 | 退出码约定 | —— | CONFIRMED |
| CMakeLists:285 | `TIMEOUT 1800` | 1800 s | 本门 ctest 超时 | 待确认 | 仅载体侧登记；与 :93 的 90 s 判据差 20 倍且无说明 | 保守方向＝超时取判据上限的合理倍数并写明余量理由；影响＝慢机上表现为超时红而非判据红 | CONFIRMED |

### 3.2 findings

### CR-11-12 · 内存门 `peak_rss_mb() < 4096` 在取数失败时以 0.0 兜底，属方向明确的 fail-open
- 轴：R（主）/ C
- 位点：`drizzle_l3_full_fp32.cpp:34` `return 0.0;`（`GetProcessMemoryInfo` 失败分支）与 `:103` `CHECK(peak_rss_mb() < 4096.0, name);`
- 上位依据：AGENTS.md §5「判据必须非退化」＋§9「负例注入（能红能绿）」；条文面对 4096 与"旧结构 4.9GB"均零登记（`git grep -n "drizzle"` on GATES_AND_TOLERANCES.md 仅命中 :115 的 DrizzlePac 许可行；`git grep "4.9GB" -- docs` 无本门条目）。
- 现状：`peak_rss_mb()` 在 Win32 API 失败（含 `psapi` 句柄/权限异常、被 Job Object 限制、跨平台误编译路径）时返回 0.0，:101 的打印串仍写成"峰值 RSS 0.0 MB … 显著下降"，:103 照判。
- 差在哪：判据不等式方向是 `peak < 上限`，失败值 0 是最小值 ⇒ 失败必然判绿；即"测不到"与"测得极优"在门里不可区分（方向已定：偏绿，不偏红）。
- 后果：该门是"内存结构性改进（取代旧多 hash 结构）"这一对外主张的唯一执行者；一旦测量失效，主张失去证据而报告看起来全绿；:101 的 0.0 MB 打印还会被当成改进幅度的读数转录。
- 定级：S2（本门只在 WIN32 构建/注册，正式平台含 Windows x64 但 Windows 侧尚未开工 ⇒ 缺生产触发路径）
- 怎么算修好：① 失败分支返回使判红成立的哨兵（如 `return -1.0;` 或 NaN）并另加一条 `GetProcessMemoryInfo 成功` 的 CHECK；② 把 4096 MB 的取值依据落到门登记面（与在册的 canonical 累加器 2.36 GB、PERFORMANCE_MODEL.md:60 的 3.34/3.47 GB 端到端口径建立关系并写明本门只测单节点）；③ 同批补一条负例（人为压低阈值应判红），否则该门无"能红"证据。
- 置信：CONFIRMED（代码分支与不等式方向均在原文；未实跑 Windows 侧验证 API 失败可达性）

### CR-11-13 · 头注称"输出像素与基线参考一致 (61,592,234)"，代码实际判的是 ±2.4% 宽带，且该参考值全仓零出处
- 轴：C（主）/ S
- 位点：`drizzle_l3_full_fp32.cpp:6` `// 验收: … 输出像素与基线参考一致 (61,592,234);` 对照 `:96` `CHECK(n_leaf > 60000000LL && n_leaf < 63000000LL, name);`
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:303「冻结容差（预冻结，源自既有测试门，**取值一律按本节**）」——§9 未登记任何"输出像素数一致性"判据，也无 61,592,234；`git grep -rn "61,592,234\|61592234" -- docs eng lib` 只命中本文件 :6/:94 两行。
- 现状：判据为 `6.0e7 < n_leaf < 6.3e7`；打印串把 61,592,234 只作为文字"基线参考"叙述，从不参与比较。
- 差在哪：把"一致"实现为一个下界 6.0e7、上界 6.3e7 的带，方向上严格宽于等值判定（参考值距上界 1.4e6 = 2.3%，距下界 1.6e6 = 2.6%）⇒ 输出像素数偏离"基线"达 ±2.5% 仍报"与基线一致"；而该带本身可由 `(W·H·scale²)/(4π/12nside²)` 导出（我复算 = 6.214e7），代码写死常数、不写式。
- 后果：候选窗/裁剪若丢或重复计入约 2% 的叶（正是"候选零漏选"要防的量级形态），此门给绿并被转录为"与基线一致"；同时参考值无跟踪面凭据 ⇒ 主张不可复算。
- 定级：S2（Windows 专属且未开工，缺生产触发路径）
- 怎么算修好：① 头注与打印串按实际判据措辞（"落在导出期望 ±x% 带内"），或改为按 :4 断言后的帧参数现算期望值并与实测做相对差门；② 把 61,592,234 的出处落到跟踪面（results/报告或 §9 登记行）；③ 与 CR-11-15（登记缺失）同批，否则改了措辞仍不在门表。
- 置信：CONFIRMED（带与参考值的差百分比已手算；期望值 6.214e7 由导出式复算）

### CR-11-14 · FP32 帧级通量闭合取 1e-4，比该实现可达的结构地板松约 1.7×10³ 倍，且条文无此条
- 轴：S（主）/ R
- 位点：`drizzle_l3_full_fp32.cpp:99` `CHECK(rel < 1e-4, name);`（:97 打印串同值）
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:304「FP64 通量闭合 <1e-6（主域）；FP32/FP64 逐 leaf <1e-5」——**没有** FP32 帧级闭合条；`git grep -rn "1e-4" -- docs/algorithms/DRIZZLE_GEOMETRY.md` 命中的是"方差 α² 缩放 worst_rel <1e-4"（:306），属另一判据，不可挪用。
- 现状：闭合残差按 `sum_out`（double 累加 float 叶值）与 `sum_in`（double 累加 float 像素）比较；该结构的相干舍入上界为 binary32 单位舍入 u = 2⁻²⁴ = 5.96e-8（随机游走则更小 ~u/√N）。
- 差在哪：门比它所要容纳的误差机制大 3 个量级（1e-4 / 5.96e-8 = 1.68×10³），实际判据接近恒绿；真正的 FP32 闭合退化（例如候选窗截断、面积归一分母错用 `D_p`）在这个阈值下无法露头。
- 后果：该门是 L3 层"通量闭合"的唯一 FP32 证据（头注 :7）；以 1e-4 通过而宣称闭合，与 §9:304 的 1e-6 主域口径相差 100 倍，产品级"通量守恒"主张失去支撑强度。
- 定级：S2（同上，Windows 专属、未注册进任何门表）
- 怎么算修好：把阈值收到与导出地板同量级并留余量（例如 1e-6·安全系数，需与 CR-11-1 一并对 §9 做一次裁决），并在 §9 增列"FP32 帧级闭合"条与推导；同批把 :97 打印串、头注 :7 的措辞与阈值一起改，防止"只改数值不改口径"造成 L0/L2/L3 三张门互不一致。
- 置信：CONFIRMED（u 值与门限比值已算；未实跑取真实残差 ⇒ 实际余量规模 PARTIAL）

### CR-11-15 · 自称"Gate D 验收门"的这组判据（90s / 叶数带 / 1e-4 / 4096MB）不在任何门登记面，且注册为无数据可得性守卫的常备 ctest
- 轴：D（主）/ C
- 位点：`drizzle_l3_full_fp32.cpp:2` `// drizzle_l3_full_fp32.cpp - Drizzle L3 / Gate D: 唯一一次完整 FP32 验收`
- 上位依据：AGENTS.md §1 第 4、7 步（沿索引读 `docs/contracts/`、`docs/ci/`）与 §7「`eng/ci/` 机器门注册表与检查器」；`docs/algorithms/GATES_AND_TOLERANCES.md` 是门与容差登记面。
- 现状：`git grep -n "drizzle" -- docs/algorithms/GATES_AND_TOLERANCES.md` 仅 :115（DrizzlePac 许可），无 G-* 行；`git grep "Gate D"` 仅命中本文件 :2/:108；`docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:160` 记 `非发布目标`；`eng/ci/prod_wiring_baseline.json:55` 仍列 `unbuildable_test:…drizzle_l3_full_fp32.cpp`（与 CMakeLists:277 已建目标不符）；载体注释自身打架：`CMakeLists.txt:217`「⇒ 已删」vs :276-286 的 `if(WIN32) add_executable + add_test`。数据可得性：`argc<=1` 时走 testdata 路径（:38-39），testdata T4 帧不入库（testdata/README.md:20），门内无 SKIP/缺失守卫，:45-48 读不到即 `return 1`。
- 差在哪：一个对外以"验收门/Gate D"名义存在的判据集，既不在门表里（阈值无处可查、无法核对是否被 §9 覆盖），其 ctest 注册又在"缺数据即判红、有数据才可判"的形态上没有任何声明；同时登记面（已删/unbuildable）与构建面（WIN32 注册）三说不一。
- 后果：L3/Gate D 的四条主张（时长、输出像素数、FP32 闭合、RSS 下降）都无登记凭据可查；Windows 平台一旦开工，该门在没下载 testdata 的机器上表现为基线红，而现在的措辞把它写成"验收通过"的既有事实。
- 定级：S2（当前平台不构建 ⇒ 缺运行时触发路径；Windows 开工后即转 S1 候选）
- 怎么算修好：① 在 `docs/algorithms/GATES_AND_TOLERANCES.md` 为 L3 四条判据各立一行（含适用域、机器档、标定件与推导，与 CR-11-12/13/14 的处置同批）；② 统一载体措辞：删掉 `CMakeLists.txt:217` 的"已删"残句、清 `prod_wiring_baseline.json:55` 的过期键；③ 门内对夹具缺失给出显式"跳过并说明"输出并注册 `SKIP_REGULAR_EXPRESSION`，与 AGENTS §9「不以 SKIP 充数」不冲突的前提是同时登记该门的实跑证据位。
- 置信：CONFIRMED（所有登记面检索式与命中数已逐条写明；"未开工"依据 = 记忆卡 Dual-platform build model 与 CMakeLists 措辞）

### CR-11-16 · 死变量 `depth/shift` 由 L0/L2 的 leaf-key 逻辑复制而来，本文件并不使用（复制式腐化信号）
- 轴：D
- 位点：`drizzle_l3_full_fp32.cpp:79-80` `uint32_t depth = hiss::compute_tile_depth((uint32_t)cfg.nside);` / `int shift = 2 * (int)depth;`
- 上位依据：条文面零提及；与本文件对照——本文件无 `ref` map、无任何 `(parent_ipix << shift)|local` 表达式，两变量在全文件仅此两次出现。
- 现状：声明后不使用。
- 差在哪：说明该门是从 L2 逐段复制而来，且其"逐 leaf 对照"部分（正是 L3 相对 L2 唯一应有的增量：**全帧 FP32 与参考的逐叶比较**）在复制中丢失，只剩计数与帧级求和。
- 后果：头注 :2 的"唯一一次完整 FP32 验收"里不含任何逐 leaf FP32 校验；§9:304 的逐 leaf <1e-5 在真实全帧（1024² 之外的 4500×3600）域内无执行者。
- 定级：S3（本身无判据后果，但支撑 CR-11-15 的"覆盖主张失真"）
- 怎么算修好：删除死变量，或按原意图补上全帧逐 leaf FP32/FP64 对照（需先解决 RSS/时间预算），并把二者之一写进门表。
- 置信：CONFIRMED（全文件逐行读过；无其他使用点）

## 4. `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_nonfinite_test.cpp`（301 行，整读完毕）

本文件取证到的决定性事实（引擎侧逐行读到）：
- 冻结合同正本 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md` §2a 存在且口径如下：:126「**合格样本** | 参与聚合的源样本满足：值有限（`isfinite(x_j)`）且几何上覆盖该输出像素（`w_jp > 0`）。**方差是否可用不参与合格性判定**」；:128「**方差不可用样本** | 合格样本中 `V_j` 有限且 `V_j ≤ 0` 者……：**信号与几何权重照常计入 `F_p`、`W_p`**（保信号、保覆盖），方差项**不计入** `Var_p`」；:129「`V_j` 非有限……按不合格样本剔除并计入 `n_rejected_nonfinite_variance`」。
- 引擎实现与合同一致：`drizzle_engine.cpp:2059-2076`——`varianceValue` 非有限才 `continue`，`V_j ≤ 0` 只 `if (varianceValue <= 0.0f) varianceValue = 0.0f;`（:2075），注释 :2073-2074 明说「丢弃会同时销毁 support……使该像素在下游变成"无效"」；随后 :2079 `threadCounters[tid].source_pixels++` 无条件计数；:2192 `nSourcePixels = totalOps.source_pixels`、:2222 写入 stats。
- ⇒ 对 case 7 的注入（`w[13]=0` 剔除 1 例、`var[14]=0` **不**剔除），现行引擎产出 `nSourcePixels = 256−1 = 255`，而 `:227` 断言 `== (int64_t)W*H - 2`（254）⇒ **该断言对 HEAD 为红**（本文件未注册 ctest，见 CR-11-2，故无人看见）。
- 逐输出像素计数面：`git grep -n "rejected" -- lib/algorithms/drizzle lib/infrastructure/aio/include lib/infrastructure/aio/src` 的聚合面只有 `DrizzleStats::n_rejected_nonfinite{,_value,_variance,_nonpositive_weight}`（drizzle_engine.h:157-160、:2241-2246 合计为三者之和）与 p1drz oracle 的 `got_rejected`；**无任何逐像素数组**。而 `docs/science/DRIZZLE.md:144` 与 `docs/algorithms/DRIZZLE_GEOMETRY.md:196` 都写「**每个输出像素**必须暴露被剔除样本计数 `n_rejected_nonfinite`」。
- 头注引用的 "反转前" 文本：`git grep -n "不掩膜\|掩膜" -- docs/science/DRIZZLE.md` ⇒ **`不掩膜` 零命中**，`掩膜` 命中 :144（即规则已在科学面订正完毕，且行号不是本文件 :12 所说的 §8 :96）。

### 4.1 §2.5 逐数值处置表（本文件 26 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :54 | `W = 16, H = 16` | 16 | 夹具图幅 | 结构性不适用 | 夹具数组维度；:128/:178/:193/:210/:227 的期望值以 `W*H` 表达而非字面量，随维度自动联动 | —— | CONFIRMED |
| :55 | `NSIDE = 512` | 512 | 夹具基网格 | 待确认 | §9:309 的 7 nside 全枚举属 candidate_oracle，本门无档位理由；`docs/KNOWN_LIMITATIONS.md:139` 恰以 nside=512 为反例档（弧-弦亏缺 1.9e-6） | 保守方向＝与 :275 的 1e-4 门对照（1.9e-6 落在门内 ⇒ 本门看不见 512 档几何不连续），并补一档生产尺度 | CONFIRMED |
| :56 | `SKY = 1000.0` | 1000.0 | 常值场面亮度 | 待确认 | §9:299 FIX-DRZ-A「常量场（面亮度语义 oracle：每像素常量 ADU）」给了形态未给值 | 保守方向＝值不改判据；但常值场使 CR-11-18 的错配不可见（见该条） | CONFIRMED |
| :63-64 | `crval = 10.0, 20.0` | 10/20 | 夹具指向 | 结构性不适用 | 任意视场中心，不参与任何科学量 | —— | CONFIRMED |
| :65-66 | `crpix = W*0.5 + 0.5` | 0.5 | FITS 1-based 中心 | 结构性不适用 | 编号约定定义常数 | —— | CONFIRMED |
| :67 | `deg_per_px = 300.0 / 3600.0` | 300 | 夹具像元角尺度（″/px） | 待确认 | 条文面零提及；效果是把父像素档（210960/512 = 412″）与样本尺度配成"样本小于父格" | 保守方向＝与生产像元同档；影响＝候选窗/叶面积分支的激励域 | CONFIRMED（无锚） |
| :67 | 3600.0 | 3600 | deg↔arcsec | 结构性不适用 | 单位换算定义常数 | —— | CONFIRMED |
| :68-71 | `cd = {−deg,0;0,deg}` | −1/0 | RA 反向、无旋转 | 结构性不适用 | 坐标取向约定 | —— | CONFIRMED |
| :72-73 | `sizeof(ctype) − 1` | 1 | NUL 预留 | 结构性不适用 | 缓冲区尺寸 | —— | CONFIRMED |
| :78 | `c.nside = NSIDE` | 512 | 配置基网格 | 同 :55 行 | —— | —— | CONFIRMED |
| :79 | `nested = true` | true | NESTED 寻址 | 结构性不适用 | 枚举（§9:315 NESTED 不变量） | —— | CONFIRMED |
| :80 | `pixfrac = 1.0` | 1.0 | drop 系数 | 待确认 | 生产默认裁 0.8；pf=1 ⇒ `shared_vertices` 行级顶点缓存分支独占（引擎 :2086-2088），掩膜路径与 pf<1 分支不同 | 保守方向＝补 pf<1 档掩膜注入；影响＝"合同测试"的分支覆盖 | CONFIRMED |
| :81 | `threads = 1` | 1 | 线程数 | 文献值（仓内条文） | §9:316「1/2/4 线程一致性」＋本行注释「确定性单线程 (科学测试)」；此处未犯 :63 式硬编码 16 | 余缺＝只测 1 档，1/2/4 一致性仍无执行者 | CONFIRMED |
| :82-83 | `apply_photometry/photometry_applied_upstream = true` | true | 测光门开关 | 结构性不适用 | 布尔开关；引擎 :2395 的 HISS 测光门前置（本门不落 HISS） | —— | CONFIRMED |
| :84 | `tile_depth = 9` | 9 | 叶 tile 深度 | 文献值（仓内合同） | `drizzle_engine.h:42-44`「HiPS 直写要求 depth=9 (512x512 叶 tile), 由 sink 调用方显式设置」⇒ 本门与生产直写同档 | —— | CONFIRMED |
| :116, :136, :250 | 注入位 `pixels[5]` | 5 | 被污染的样本下标 | 结构性不适用 | 夹具内任一像素，无科学语义（同一位置在 case 1/2/8 复用，保证三者可比） | —— | CONFIRMED |
| :152 | `snr[7] = kNan` | 7 | SNR 注入位 | 结构性不适用 | 同上 | —— | CONFIRMED |
| :169, :186, :220 | `w[9]/w[9]/w[13]` | 9 / 13 | 权重注入位 | 结构性不适用 | 同上 | —— | CONFIRMED |
| :201, :221 | `var[11]/var[14]` | 11 / 14 | 方差注入位 | 结构性不适用 | 同上 | —— | CONFIRMED |
| :125-129 | `== 1` 与 `nSourcePixels == W*H − 1` | 1 / −1 | 剔除 1 例的期望计数 | 公式导出 | 由 §2a:126 合格判据＋注入数导出（16²−1）；代码按该式写 | —— | CONFIRMED |
| :151 | `snr` 填充 `1.0f` | 1.0 | 名义 SNR 场 | 结构性不适用 | 单位中性值，snr 在本门只作"不污染 F_p"的对照 | —— | CONFIRMED |
| :168, :185, :218 | `w` 填充 `1.0f` | 1.0 | 单位权重 | 结构性不适用 | 归一化中性值（`w_jp>0` 即合格） | —— | CONFIRMED |
| :200, :219 | `var` 填充 `100.0f` | 100.0 | 名义方差场 | 待确认 | 条文面对方差档位无要求；本门不核 Var_p 数值 | 保守方向＝若宣称"方差通道已测"，需给 Var_p 的期望式；影响＝case 6 只测计数不测方差值 | CONFIRMED |
| :220 | `w[13] = 0.0f` | 0.0 | 零权重注入 | 文献值（仓内合同） | §2a:126 合格判据含 `w_jp > 0` ⇒ 0 属不合格（"权重非正"类）；引擎 :2053 与之一致 | —— | CONFIRMED |
| :221 | `var[14] = 0.0f`（＋ :227 期望"减 2"） | 0.0 / −2 | 零方差样本的处置期望 | **待确认（现为错）** | 与 §2a:126/:128 及引擎 :2075/:2079 相反（合同：V≤0 仍计入 F_p/W_p，样本合格） | 保守方向＝期望改 `W*H − 1`（只扣权重那 1 例），**绝不**反向改引擎；影响＝见 CR-11-17 | CONFIRMED |
| :231-232 | `n_rejected_nonfinite == 1` | 1 | 合计计数期望 | 公式导出 | 引擎 :2244-2246 合计 = 值+方差+权重三分类 ⇒ 本注入 0+0+1 = 1，代码按式 | —— | CONFIRMED |
| :265 | `pa.sumArea > 0.0 && pb.sumArea > 0.0` 才比 | 0.0 | 除法守卫 | 结构性不适用 | 覆盖级 `D_p = 0` 的除零防护，与 §2a:163 一致 | —— | CONFIRMED |
| :268 | `rel = |sa−sb|/|sb|` | —— | 面亮度相对差 | 公式导出 | 相对差定义式；sa/sb = sumFlux/sumArea 即合同 `S_p = F_p/D_p`（DRIZZLE.md §5 面亮度） | —— | CONFIRMED |
| :271-275 | `rel < 1e-4` | 1e-4 | 重归一无偏门 | 待确认 | 注释给出的两个对照量（噪声 ~1e-5、半掩膜偏差 ~1/n≈4e-2）均无跟踪面记录；§9 无此条 | 保守方向＝若采信其"2 个数量级"论证则 1e-4 可辩护，但须把实测 1e-5 落跟踪面；影响＝本门是掩膜+重归一的唯一逐叶证据 | CONFIRMED（无锚）／实测值 PARTIAL |
| :278 | `n_cmp > 0` | 0 | 非退化守卫 | 待确认 | AGENTS §5「判据必须非退化」；`>0` 只要求"至少一叶可比"，与 :279 的逐叶门共用 | 保守方向＝按夹具规模给下界（本夹具应 ≫1）；影响＝见 CR-11-18 | CONFIRMED |
| :288-291 | `vals {SKY, NaN}`、`sup {1.0, 1.0}`、`count = 2` | 1.0 / 2 | 下游积分的手造输入 | 待确认 | 与 §2a:163/:203 的覆盖级形态矛盾：合同规定 `signal=NaN` 只在零合格样本时输出且 `support ≤ 0`（"该形态下 support 取 0"） | 保守方向＝把 `sup[1]` 改 0（可达形态）或明确本断言只测 integrate 的入参校验；影响＝见 CR-11-21 | CONFIRMED |
| :294 | `rc == 0` | 0 | 调用返回码 | 结构性不适用 | API 返回码约定（结论由 `out.status` 承载，非 rc） | —— | CONFIRMED |
| :46, :300 | 计数器与退出码 | 0/1 | 汇总 | 结构性不适用 | 退出码约定 | —— | CONFIRMED |

### 4.2 findings

### CR-11-17 · case 7 断言"零方差样本不进管线（nSourcePixels 减 2）"，与冻结合同和现行引擎双向冲突，是一个对 HEAD 恒红的陈旧断言
- 轴：S（主）/ C / R
- 位点：`drizzle_nonfinite_test.cpp:227-228` `CHECK(st.nSourcePixels == (int64_t)W * H - 2, "weight==0 与 variance==0: 两像素均不进管线 (nSourcePixels 减 2)");`（配套注释 :22-23「variance==0 → 合法数据边界跳过」与 :221 `var[14] = 0.0f; // 零方差: …→ 跳过不计数`）
- 上位依据：本文件 :5-6 自己点名的唯一口径正本 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md` §2a:126「合格样本 = 值有限 ∧ `w_jp > 0`，**方差是否可用不参与合格性判定**」、:128「`V_j` 有限且 `V_j ≤ 0` 者：**信号与几何权重照常计入 `F_p`、`W_p`**（保信号、保覆盖），方差项不计入 `Var_p`」。实现侧一致：`drizzle_engine.cpp:2062-2064` 仅非有限才 `continue`，:2075 `if (varianceValue <= 0.0f) varianceValue = 0.0f;`，:2079 `source_pixels++`。
- 现状：本门注入 1 个 `w=0`（应剔除）与 1 个 `V=0`（不应剔除），却期望两像素都掉出管线。
- 差在哪：按合同与实现，唯一正确期望是 `W*H − 1 = 255`；断言写 `254`。这是把已作废的"零方差即丢弃"语义（§2a 明确"丢弃会销毁 support"，引擎 :2073-2074 原话）当冻结合同钉住。
- 后果：① 该门在能跑的环境下必红，而它不在 ctest（CR-11-2）⇒ 红不可见，反而使"非有限处置合同已全绿"的口头陈述失真；② 更危险的方向是"顺着测试改引擎"：若有人把 `V_j ≤ 0` 改成 `continue` 来让本门变绿，产品面将同时丢失 `signal` 与 `support`，下游按"无覆盖"处理合法像素——正撞 §2a:163 的覆盖级语义。
- 定级：S2（本门未注册 ⇒ 无产品触发路径；一旦注册即 S1，因为反向修法是可达的）
- 怎么算修好：① 期望改 `(int64_t)W*H − 1`，并把 :221 注释与 :22-23 的"跳过"措辞改成"方差项不计入、样本照常贡献 F_p/W_p"；② **同批**新增一条正向断言：`V=0` 样本所在叶的 `sumFlux`/`sumArea` 与"该样本为 `V=100`"的对照组逐叶相等（这才锁住"保信号、保覆盖"），否则只改期望仍是弱断言；③ 与 CR-11-2 同批注册进 ctest，并核对 `p1drz` oracle 侧是否也含同类陈旧期望（`tests/p1drz/p1drz_tests_core.cpp:602` 的 `rejected` 判定属同一族，须一并复算）。
- 置信：CONFIRMED（合同原文、引擎逐行、期望算式三方对齐后手算 255 vs 254；未实跑 ⇒ 若引擎另有我未读到的分支扣数，则以实跑为准）

### CR-11-18 · §8 重归一门按 vector 下标配对两跑结果，且常值场使任何错配都恰好判绿
- 轴：R
- 位点：`drizzle_nonfinite_test.cpp:259-262` `for (std::size_t i = 0; i < t_nan.size() && i < t_clean.size(); ++i) { const auto& a = t_nan[i]; const auto& b = t_clean[i];`
- 上位依据：§2a:154-158（掩膜只作用于不合格样本、分母只含合格样本权重）；AGENTS §5「实验单元自包含……结论经独立复核」与 §9「对'看起来通过'的结果保持怀疑：空断言……都算未完成」。本仓 L0/L2 的同类比较都用地址键配对：`drizzle_l0_test.cpp:127` `ref[(tile.parent_ipix << shift) | local]`、`drizzle_l2_test.cpp:80` 同型；§9:315 的不变量清单里就有「NESTED 地址往返」。
- 现状：两跑的 tile 只按容器下标 `i` 一一对应，从不校验 `a.parent_ipix == b.parent_ipix`；比较集合由 `a.touched` 决定，`b.pixels[local]` 直接用同一 `local` 取（若 clean 跑未触该 local，`pixels` 越界读取风险另计）；守卫只有 :278 `n_cmp > 0`。
- 差在哪：判据与被测对象之间缺地址绑定——tile 向量顺序/长度一旦因剔除而错位（例如某 tile 的唯一贡献者被掩膜后不再生成），后续全部配对整体平移，比较的是两个不同 parent 的叶；而夹具是 `SKY=1000` 的常值场，任意叶的 `F_p/D_p` 都是 1000 ⇒ 平移后的错配依旧逐叶相等，门恒绿。方向明确：偏绿。
- 后果：本文件头注 :24-25 把 §8 称为"本任务核心语义"与"传播语义下此断言必红"的锁死断言；它实际对"配对正确性"零敏感，一旦掩膜改变了 tile 集合（正是它要测的情形），它就退化为"常值场里挑两个叶比一比"。
- 定级：S2（门自身判据强度问题，未注册 ctest）
- 怎么算修好：① 按地址配对：用 `drizzle_l2_test.cpp:80` 同型的 `(parent_ipix << shift) | local` 建 `std::map` 后取交集，并断言 `n_cmp` 等于预期叶数量级（而非 >0）；② 把夹具从纯常值场换成"常值＋梯度"（§9:301 FIX-DRZ-C），使错配能露头；③ 保留 :279 的逐叶门，同时补一条"两跑 tile 集合差异只发生在被掩膜样本所在 parent"的结构断言。
- 置信：CONFIRMED（配对逻辑与常值场性质均由原文直接得出；"掩膜是否会改变 tile 集合"未实跑 ⇒ 该子项 PARTIAL）

### CR-11-19 · 合同要求"每个输出像素暴露剔除计数"，本门只核全局聚合，逐像素面在实现与测试两侧都不存在
- 轴：D（主）/ C
- 位点：`drizzle_nonfinite_test.cpp:125-127` `CHECK(st.n_rejected_nonfinite_value == 1, …)` / `CHECK(st.n_rejected_nonfinite == 1, "pixel NaN: 合计计数 == 1")`（全文件仅此三种聚合计数被核）
- 上位依据：`docs/science/DRIZZLE.md:144`「……且**每个输出像素必须暴露**被剔除样本计数 `n_rejected_nonfinite`（剔除逐条登记计数）」；`docs/algorithms/DRIZZLE_GEOMETRY.md:196` 同口径（「每个输出像素必须暴露……按原因分类、互斥可加」）并给实现锚 `DrizzleStats::n_rejected_nonfinite*`（:2203-2208）。
- 现状：取证检索 `git grep -n "rejected" -- lib/algorithms/drizzle lib/infrastructure/aio/include lib/infrastructure/aio/src` ⇒ 唯一存在面是 `DrizzleStats` 的 4 个 int64 聚合（drizzle_engine.h:157-160；:2241-2246 合计）与测试侧 oracle 的 `got_rejected` 聚合；无任何逐叶/逐像素数组，也无产品面（HISS/metadata）字段承载它。本门按聚合数断言，故对"逐像素"这一半合同条款零覆盖。
- 差在哪：条文写的是逐输出像素义务，实现锚与测试锚都是全局标量——同一件事在科学文档、实现、合同测试三面不同；全局计数能证明"没有静默剔除"，但无法定位"哪个输出像素被污染/被扣了几例"，而 §2a 的覆盖级 NaN 判据（`D_p = 0`）恰恰是逐像素命题。
- 后果：产品消费者按 `docs/science/DRIZZLE.md:144` 去取逐像素剔除计数会取不到；下游（SNR/方差可用性判定、审计）只能看到帧级总数，无法复核单像素是否被过度剔除。
- 定级：S2（对外义务失真；本门未注册，无产品字节路径）
- 怎么算修好：二选一并同批贯穿：① 实现侧把逐叶剔除计数落到产品元数据（TileLeafAccumulator/HISS 侧新增字段 + schema + 文档），本门补逐叶断言；② 若裁定全局计数即满足义务，则按 §8 变更流程改 `docs/science/DRIZZLE.md:144` 与 `DRIZZLE_GEOMETRY.md:196` 的"每个输出像素必须暴露"措辞为帧级聚合，并在本文件头注 :5-11 同步。
- 置信：CONFIRMED（三面原文与零命中检索式均已给出）

### CR-11-20 · 头注把科学文档的"不掩膜"分歧写成现行事实，并引用了不存在的行号
- 轴：C
- 位点：`drizzle_nonfinite_test.cpp:12-13` `// 注意: docs/science/DRIZZLE.md §8 :96 的「不掩膜」行为已被同一 rule_id 反转，本文件断言与 DATA-002 §2a 对齐（文档面订正属 DOC 域，见任务回执）。`
- 上位依据：`git grep -n "不掩膜\|掩膜" -- docs/science/DRIZZLE.md` ⇒ `不掩膜` **零命中**；掩膜条款在 **DRIZZLE.md:144**（内容与 §2a 一致）。同族措辞在引擎里也残留：`drizzle_engine.cpp:2036-2037`「旧行为……已作废（原注释引用的 DRIZZLE.md §8「不掩膜」行亦已按同一 rule_id 反转）」。
- 现状：测试与引擎都按"科学文档尚存一条相反条款、已被反转"的叙事书写，并给出 §8 :96 的定位。
- 差在哪：定位错（规则在 :144 不是 §8 :96），叙事也过期（现行科学面无相反条款可引）。读者按 :12 去核"反转是否完成"会找不到被反转的对象。
- 后果：合同测试的佐证链指向不存在的条文；AGENTS §1.1 的自检要求（"我这件事的规范依据是哪一份文档的哪一条"）在此不可回答。
- 定级：S3（不改变判据结果）
- 怎么算修好：:12-13 与 `drizzle_engine.cpp:2036-2037` 同批改为直接引 `docs/science/DRIZZLE.md:144` / `DATA-002 §2a`，删去"已被反转"的历史叙述（与负责人"文档内禁日期与历史"的口径一致）。
- 置信：CONFIRMED

### CR-11-21 · 链路断言喂给下游积分的是合同禁止的形态（NaN ∧ support>0），"链路"结论不外推到产品
- 轴：R（主）/ C
- 位点：`drizzle_nonfinite_test.cpp:288-291` `double vals[2] = {SKY, std::numeric_limits<double>::quiet_NaN()};` / `double sup[2] = {1.0, 1.0};` / `in.values = vals; in.support = sup; … in.count = 2;`
- 上位依据：§2a:163「仅当 `W_p = 0`（零合格样本）时，输出 `signal = NaN`、`variance = NaN`……」与 :203「仅当零合格样本时输出 `signal=NaN ∧ support≤0`（**该形态下 `support` 取 0**）」；本文件 :9 亦复述为「仅 D_p = 0 时输出 signal = NaN ∧ support ≤ 0（覆盖级 NaN）」。
- 现状：手造一个 value=NaN 而 **support=1.0** 的样本喂 `p2_integrate_pixel`，断言 `P2_INTEGRATE_INVALID_INPUT`。
- 差在哪：产品链不可能产出这种输入（NaN 必伴 support≤0），因此 :26 的"覆盖级 NaN 喂给下游积分"与实际喂进去的东西不是同一形态；该断言真正测的是 integrate 对自身入参的健壮性（有独立价值，但被冠以"链路"之名）。同时头注 :25 声称"非有限样本经掩膜后不再出现在产品面"，本 case 与掩膜输出无任何数据流连接（`out` 未回取、drizzle 结果未参与）。
- 后果：SCI-INT 侧"覆盖级 NaN → INVALID_INPUT"这条链路主张没有对应证据；若产品侧哪天真的产出 `NaN ∧ support>0`（即 §2a 被违反），本门同样不会红。
- 定级：S3（无产品路径，但"链路"措辞需订正）
- 怎么算修好：① 把 case 9 的输入改成"覆盖级形态"（`vals[1]=NaN, sup[1]=0.0`）并另留一条 NaN ∧ support>0 作为**负例注入**（应被拒）；② 更好的做法是把 case 8 的 `t_nan` 中真实产生的零覆盖叶（:265 被跳过的 `sumArea==0` 那批）导出为 `P2PixelStack` 再喂下游，使"链路"名副其实；③ 同批修 :25-27 的措辞与断言范围一致。
- 置信：CONFIRMED

## 5. `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_pf_sb_gate.cpp`（275 行，整读完毕）

本文件是**本批唯一在册注册的 ctest 产品级门**，取证到：
- 注册面 `tests/CMakeLists.txt:318-331`：`add_test(drizzle_pf_sb_gate)` 与 `add_test(drizzle_pf_sb_gate_legacy_injection)`，后者带 `WILL_FAIL TRUE` + `ENVIRONMENT "ASTROCS_DRZ_SB_FAULT=legacy_dp_normalization"`，TIMEOUT 600；:298-299 记注册项 `CHK-DRZ-PF-AREA-S1` 用正则 `^drizzle_pf_sb.*$` 覆盖"oracle 幂次 + 产品级正例 + 负例注入"三件（同族第三件 `drizzle_pf_sb_variance = variance_propagation_test`，:300-302）。
- 注入钩子在被测件而非门内：`astro_sphere_sink.cpp:152-155` 读同一 env，`:186-188` `const double k = legacy_dp_normalization ? 1.0 : sb_publish_scale(sumArea, sumNorm);` ⇒ 注入后 `dense_flux = sumFlux`（漏乘 k）、`dense_var = sumVarNum`（漏乘 k²），support 不变。
- 我按解析式复算全链（确认门是自洽且能红的）：`sumFlux=B0·D_p/pf²`、`sumNorm=D_p/pf²`、`k=p f²` → 落盘 `signal=(sumFlux·k)/D_p = B0` ✓、`variance=(sumVarNum·k²)/D_p² = σ²/n` ✓；注入态则 `signal=B0/pf²`、`variance=σ²/(n·pf⁴)`，与 :254-255 的 `sig_legacy/var_legacy` 逐字吻合；pf=0.8 ⇒ +56.25%/+144.14%，与 `docs/algorithms/DRIZZLE_GEOMETRY.md:332`（DISP-DRZ-009「pf=0.8→+56.25%，与 `1/pf²−1` 逐位吻合」）一致。
- 失败方向核查（本文件不存在 fail-open）：写端失败→`check(ok…)` 红并 `continue`（:210-215）；产品目录改名/平面缺失→`published_of` 返回 NaN 且 `n_covered=0`（:153）→(1)-(4) 全红；`vars` 为空→`p.var` NaN→(2) 红；`g_pass==0` → `return 2`（:270-273）。⇒ 判据在"测不到"时判红，不判绿。
- 叶子→像素地址映射的冻结口径在 `docs/contracts/DATA_SEMANTICS.md:2469`：「local 映射 =DATA_SEMANTICS §3 CDS oracle 冻结（`fits_index=(511-x)*512+y`）」——本门不核该映射（见 CR-11-22）。

### 5.1 §2.5 逐数值处置表（本文件 27 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :61 | `kPi` | 3.14159265358979323846 | 圆周率 | 公式导出 | 数学常数，:109 按 `4π/(12nside²)` 使用 | —— | CONFIRMED |
| :62 | `kTileWidth` | 512 | HiPS tile 边长 | 结构性不适用 | `astro_sphere_sink.h:56`「tiles 必须按 depth=9 分组 (512x512 叶 tile, 与 HiPS NorderK tile 1:1)」⇒ 与 :200 `cfg.tile_depth=9` 由合同同向；两常数无互校（改 depth 则 :70 尺寸不符→`read_tile` false→判红，方向不偏绿） | —— | CONFIRMED |
| :63 | `kLeafPerTile` | 512·512 | 叶 tile 容量 | 结构性不适用 | 数组维度量；**全文件无使用点**（死常数） | —— | CONFIRMED |
| :109 | `a_cell = 4.0*kPi/(12.0*nside²)` | 4.0 / 12.0 | HEALPix 像素面积恒等式 | 公式导出 | `docs/KNOWN_LIMITATIONS.md:139`「HEALPix 像素面积恒为 4π/(12·nside²)」；代码即按式算 | —— | CONFIRMED |
| :110 | `D_p = (q/255.0)*a_cell` 之 255.0 | 255 | support 的 u8 档位数 | 结构性不适用 | 量化档位定义（u8 上限）；"把 D_p 造在格点上"这一设计选择的后果见 CR-11-25 | —— | CONFIRMED |
| :112 | `A_pixel = 1.0` | 1.0 | 源像素面积取 1 px² | 待确认 | :21 自述 `N_p = Σ_j w_jp·A_pixel,j, A_pixel≡1 px²`；引擎侧 `A_pixel` 为真像素面积（球面角尺度）⇒ 门验证的是 writer 幂次，不验证 sumNorm 的单位口径 | 保守方向＝另立一档 `A_pixel≠1`（如 6.3″/px 的 sr 值）确认 k 不变；影响＝"全链覆盖"主张的强度（见 CR-11-26） | CONFIRMED（无条文） |
| :117 | `locals[1] = {0}` | 1（叶下标 0） | 本 tile 写入的叶集合 | 待确认 | 与 :116 注释「同一 tile 内 4 个叶, 覆盖 D_p=touched 的校验面」直接矛盾；条文面零提及 | 保守方向＝按注释写 ≥4 个 local 并逐叶比对；影响＝见 CR-11-22 | CONFIRMED |
| :121 | `sumNorm = D_p/pf2` | —— | 归一分母 | 公式导出 | `astro_sphere_sink.h:39-40`「= (Σ_j a_jp)/(Σ_j w_jp·A_pixel,j) = pixfrac²」；代码按式构造 | —— | CONFIRMED |
| :122 | `sumFlux = B0*D_p/pf2` | —— | 常量面亮度场分配通量 | 公式导出 | :22 与 `docs/science/DRIZZLE.md` §5:50（S_p=F_p/N_p=B0） | —— | CONFIRMED |
| :124-125 | `w = D_p/(n_src·pf2·A_pixel)`；`sumVarNum = σ²·n_src·w²` | —— | 等权源方差分子 | 公式导出 | :23「Σ_j w_jp² 展开」＋§2a:159「Var_p = Σ_{方差可用} V_j w_jp² / W_p²」 | —— | CONFIRMED |
| :135 | `den <= 0.0` 绝对退化分支 | 0.0 | 相对差的除零/零期望处理 | 结构性不适用 | 判据实现选择；`want=0` 时转绝对门，方向不偏绿 | —— | CONFIRMED |
| :153 | `sigs.size()!=1 \|\| sups.size()!=1` | 1 | 单叶写入的读回校验 | 结构性不适用 | 与 :117 的单叶设计绑定；**`vars.size()` 未同等校验**（:160 只判 `!empty()`），影响见 CR-11-22 的 n_covered 部分 | —— | CONFIRMED |
| :156 | `sup > 0.0` 覆盖判据 | 0.0 | 有效输出定义 | 文献值（仓内合同） | §2a:133「**有效输出** \| `signal` 有限 **且** `support > 0`」 | —— | CONFIRMED |
| :177 | `nside = 512` | 512 | 本门父网格（只经 `a_cell` 起作用） | 待确认 | 与 :62 的 512（tile 边长）是两个不同含义的 512，易误读为同一量；条文面未规定本门 nside | 保守方向＝改名或注释区分；影响＝可读性/误维护，不改判据 | CONFIRMED |
| :178 | `B0 = 7.5` | 7.5 | 常量面亮度 [ADU/px²] | 待确认 | 条文面无；因两侧均归一到比值，数值不影响判据 | 保守方向＝与生产量级同档（可保留）；影响＝无 | CONFIRMED |
| :179 | `sigma2 = 4.0` | 4.0 | 每源方差 [ADU²] | 待确认 | 同上（var 期望按 σ²/n 线性缩放） | —— | CONFIRMED |
| :180 | `n_src = 4` | 4 | 等权源个数 | 待确认 | 出现在 `sumVarNum` 与 `var_expected=σ²/n`，故**参与**判据数值而非纯维度；条文面未规定 | 保守方向＝多档 n（n=1 时 `support` 与单源覆盖边界）；影响＝方差幂次检验的敏感度 | CONFIRMED |
| :181 | `var_expected = sigma2/n_src` | —— | 期望发布方差 | 公式导出 | :24「variance = σ²/(n·A²) = σ²/n (A=1 px²)」，代码按式算 | —— | CONFIRMED |
| :182 | `pfs[] = {1.0, 0.8, 0.5, 0.25}` | 四档 | pixfrac 扫描集 | 文献值（仓内条文） | `§9:309` 的"4 pixfrac"同族＋`CMakeLists.txt:296`「把 pf∈{1.0,0.8,0.5,0.25} 全部纳入判据」；0.8 = 负责人裁的生产默认 | —— | CONFIRMED |
| :193 | `q = 64`（support = 64/255 ≈ 0.251） | 64 | u8 量化档位选取 | 待确认 | 条文面与注释均未说明为何取 64（而非扫多档） | 保守方向＝多档 q（含非整除档位）；影响＝见 CR-11-25 | CONFIRMED |
| :223, :225 | profile=0 signal 门 `1e-6` | 1e-6 | 生产直写端的面亮度容差 | 待确认 | 条文面零提及（§9:303-316 与 GATES_AND_TOLERANCES.md 均无本门条目；后者 `git grep -n "drizzle"` 只命中 :115 的 DrizzlePac 许可行） | 保守方向＝按 writer 落盘精度给（float32 tile ⇒ 地板约 6e-8，1e-6 为 ~17×u，可辩护但须写推导）；影响＝在册门的阈值无凭据 | CONFIRMED（无锚） |
| :227, :229 | profile=1 signal 门 `1e-5` | 1e-5 | 非生产端的同类容差 | 待确认 | 同上，且与 :225 差 10 倍无任何理由记录 | 保守方向＝两端取同值或写明差异来源（u8/量化）；影响＝见 CR-11-24 | CONFIRMED |
| :233, :235 / :237, :239 | variance 门 `1e-6` / `1e-5` | 1e-6 / 1e-5 | 方差幂次容差（两端各一） | 待确认 | 同上；方差按 k² 传播 ⇒ 相对误差约 2×signal 误差，故与 signal 同值才自洽 | 保守方向＝按 2× 关系给并写推导；影响＝同上 | CONFIRMED |
| :242, :245 | parity 门 `1e-5` | 1e-5 | 两末端 signal 一致性 | 待确认 | 条文面零提及 | 该门**未被 (1a)/(1b) 蕴含**（两端容差合成可达 1.1e-5 > 1e-5），有独立发现力；保守方向＝保留并按合成误差预算取值 | CONFIRMED |
| :248, :250 | support 门 `1e-5` | 1e-5 | 量化档位读回一致性 | 待确认 | 条文面零提及；夹具按 :110 造在格点 ⇒ 该门近似恒成立 | 保守方向＝改为"非格点 D_p 的读回 = lround(255·D_p/a_cell)/255"的量化断言；影响＝见 CR-11-25 | CONFIRMED |
| :254, :255 | `sig_legacy = B0/pf²`、`var_legacy = var_expected/pf⁴` | 幂次 2 / 4 | 负例（修复前缺陷）的期望式 | 公式导出 | `astro_sphere_sink.h:48`「任一末端漏乘 ⇒ signal 偏 1/pixfrac²、variance 偏 1/pixfrac⁴」＋DRIZZLE_GEOMETRY:332；代码按式算 | —— | CONFIRMED |
| :258, :262-263 | 非退化判据 `> 1e-3` | 1e-3 | "覆盖率口径必被拒绝"的门 | 待确认 | 条文面零提及；与 :225/:235 的 1e-6 构成逻辑蕴含（见 CR-11-23） | 保守方向＝把它接到真正的负例（env 注入 ctest）上并在此处引用注册项 | CONFIRMED |
| :209, :213 | 末位参数 `1`（has_variance）与 `{}` | 1 | 写 variance/ivar 通道开关 | 结构性不适用 | `astro_sphere_sink.h:55-57`「has_variance: 1=累加器已含 sumVarNum, 写 variance/ivar 产品」；`{}` = 无 SNR 模型 | —— | CONFIRMED |
| :270-274 | `return 2`（零执行）/ `0` / `1` | 2/0/1 | 退出码分档 | 结构性不适用 | 退出码约定（2 与 1 分列，便于 `WILL_FAIL` 与"未执行"区分）；方向核查见本节首段 | —— | CONFIRMED |

### 5.2 findings

### CR-11-22 · 注释宣称"同一 tile 内 4 个叶"，实为 1 个叶，且读回按"最后一个覆盖像素"取值 ⇒ 本门对叶→像素地址映射零敏感
- 轴：R（主）/ D
- 位点：`drizzle_pf_sb_gate.cpp:116-117` `// 同一 tile 内 4 个叶, 覆盖 D_p=touched 的校验面` / `const uint32_t locals[1] = {0};`；读回侧 `:154-161` `for (size_t i = 0; i < sigs[0].size(); ++i) { … ++p.n_covered; p.sup = sup; p.sig = sigs[0][i]; … }` 与 `:225` `check(pd.n_covered > 0 && rel_close(pd.sig, B0, 1e-6), msg)`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:2469`（HiPS tile 读路径）「local 映射 =DATA_SEMANTICS §3 CDS oracle 冻结（**`fits_index=(511-x)*512+y`**）；tile 值=面亮度（§9a-8）」；`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:315 不变量含「NESTED 地址往返」；本门头注 :12 自称"产品级门（读回落盘 FITS）"。
- 现状：累加器只在 `local=0` 造 1 个叶（注释写 4 个，且 4 实为 `n_src`，:180）；`published_of` 在整个 512×512 平面上扫描 `support>0` 的像素，**取最后一个**作为比较对象，判据只要求 `n_covered > 0`，从不检查该像素的 (x,y)、也从不要求 `n_covered == 1`。
- 差在哪：把"值正确"与"位置正确"解耦了——writer 侧叶→像素映射若整体写错（镜像、行列互换、`(511-x)` 漏一项），落盘仍是"一个覆盖像素、值为 B0"，本门照判绿；同时注释宣称的多叶 touched 覆盖面并不存在，`touched` 长度恒为 1。
- 后果：该门被 CMakeLists:308-309 称为"产品级门（读回落盘 FITS，非只看累加器）"，但它实际只锁住了"归一幂次"一维；HiPS 产品的空间正确性（tile 内叶位置）在本门上没有证据，而 §9:315 的 NESTED 地址往返不变量在本批 6 个文件里也无执行者。
- 定级：S2（门本身是已注册的，但它不产生错值；错值风险在被它宣称覆盖的映射面上）
- 怎么算修好：① 按注释真正写 ≥4 个 local（含 0、1、511、512、262143 等边界），逐叶按冻结映射换算出期望 (x,y) 并断言 `n_covered == 4` 且每个叶的期望位置命中；② `published_of` 改为"按期望 (x,y) 取值"而非扫描取末个；③ 与 CR-11-25 的 q 档位改造同批，避免只改判据形状不改夹具。
- 置信：CONFIRMED（代码与合同原文逐字对读；"映射写错能否命中本门"由扫描取值逻辑直接得出，未实跑）

### CR-11-23 · "判据非退化"那条负例门（`>1e-3`）被同一文件的 1e-6 值门逻辑蕴含，独立发现力为零
- 轴：R
- 位点：`drizzle_pf_sb_gate.cpp:262-263` `check(std::fabs(pd.sig / sig_legacy - 1.0) > 1e-3 && std::fabs(pd.var / var_legacy - 1.0) > 1e-3, msg);`（对照 `:225` 的 `rel_close(pd.sig, B0, 1e-6)`、`:235` 的 `rel_close(pd.var, var_expected, 1e-6)`）
- 上位依据：AGENTS.md §5「判据必须非退化……恒真门没有证据资格」；本文件 :17 自述「(3) 判据非退化: 覆盖率口径 (sumFlux/sumArea = S_p/pf²) 必被本判据拒绝」。
- 现状：`sig_legacy = B0/pf²`，夹具集合 `pf∈{0.8,0.5,0.25}`（pf=1 时 :253 跳过）⇒ 只要 (1) 通过即 `pd.sig = B0(1+ε), |ε|≤1e-6`，则 `|pd.sig/sig_legacy − 1| = |pf²−1 ± 1e-6| ≥ 0.36 − 1e-6`；`|pf²−1|` 的最小值 0.36（pf=0.8）是门限 1e-3 的 **360 倍**；方差侧 `|pf⁴−1| ≥ 0.59`，为 590 倍。
- 差在哪：判据 `>1e-3` 与前置 `≤1e-6` 的组合使其不可能单独变红——它等价于把 (1)/(2) 的结论重述一遍（真正的判别边界仍是 1e-6 那两条）。与 CR-11-9 同一形态：**门数被虚增**。
- 后果：本门在册注册项 `CHK-DRZ-PF-AREA-S1`（CMakeLists:298-299）以正则 `^drizzle_pf_sb.*$` 覆盖"正例 + 负例注入"，若按 CHECK 计数汇总，非退化这一条会被当成独立证据；实际的非退化能力只来自 `drizzle_pf_sb_gate_legacy_injection`（`WILL_FAIL` + env，:325-331）。
- 定级：S3（判据强度与统计口径问题，不改产品值）
- 怎么算修好：把 :252-263 这一段降格为"报告"（printf，不 `check`），或把门限改为有信息量的量级（例如要求 `|实测/legacy − 1|` 与 (1) 的容差之间留出可分辨区间，或直接断言"注入态下本式 < 1e-3"由负例 ctest 承担）；汇总侧引用 :325 的注入项作为非退化凭据。
- 置信：CONFIRMED（0.36 与 0.59 由 pf 集合手算，蕴含关系不依赖实跑）

### CR-11-24 · 一个已注册、判产品字节的名义门，其 5 个容差（1e-6/1e-5 各两处＋parity 1e-5）在两条登记面上零登记
- 轴：S（主）/ C
- 位点：`drizzle_pf_sb_gate.cpp:225` `… rel_close(pd.sig, B0, 1e-6)`、`:229` `… rel_close(pp.sig, B0, 1e-5)`、`:235`/`:239`（variance 同双档）、`:245` `rel_close(pd.sig, pp.sig, 1e-5)`
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:303「冻结容差（预冻结，源自既有测试门，**取值一律按本节**）」——§9 全段无"面亮度归一/两末端 parity"条目；`docs/algorithms/GATES_AND_TOLERANCES.md` 的 `git grep -n "drizzle"` 仅命中 :115（第三方 DrizzlePac 许可），即门登记面对本门零登记；`tests/CMakeLists.txt:298` 只登记了 ctest 名与正则覆盖关系，不含阈值依据。
- 现状：profile=0 与 profile=1 对同一物理量取相差 10 倍的容差，注释与 CMakeLists 均未说明差异来源；方差门的容差与信号门同值，而方差按 k² 传播（:255 的 1/pf⁴ 关系即其 2 次幂），理论上应为信号容差的约 2 倍关系。
- 差在哪：§9:303 要求"取值一律按本节"，本门的取值不在本节；两处工程上必然不对称的地方（端别、幂次）没有对应的推导或登记。
- 后果：DRZ-PF-CORRECT-01 的产品级结论（signal 幂次 +1 / variance 幂次 +2 的正确发布）依赖一组无出处的阈值；若日后按 §9 的 1e-5 单值统一，profile=0 会从 1e-6 放宽到 1e-5，反向（统一为 1e-6）则可能因 writer 的 float32 落盘精度而脆红——两种"改对反而判红"的风险都因缺推导而无法预判。
- 定级：S2
- 怎么算修好：在 §9 或 GATES_AND_TOLERANCES.md 为「两末端面亮度 parity / 幂次发布」立行，写清：容差 = writer dtype 地板（float32 tile ⇒ u=2⁻²⁴≈5.96e-8）× 余量系数，并解释 profile=1 为何宽 10 倍（若因 u8 support，须与 CR-11-25 一起写）；方差容差按 2 倍幂次关系给出；同批把五处字面量改为具名常量以免再次漂移。
- 置信：CONFIRMED（两登记面的零/单命中检索式已写明）

### CR-11-25 · 夹具把 support 造在 1/255 量化格点上，使 profile=1 已知的 u8 量化偏移在产品级门里结构上不可见
- 轴：D（主）/ R
- 位点：`drizzle_pf_sb_gate.cpp:105`（`// q: support 的 uint8 量化档位 (uint8 = lround(255*sumArea/a_cell))`）与 `:110` `const double D_p = ((double)q / 255.0) * a_cell;   // 令 u8 量化恰好表示 D_p`
- 上位依据：负责人已裁事实（记忆卡 Drizzle pixfrac default 附注：profile=0 无偏移已结案、**profile=1 有 uint8 量化偏移**）；本门头注 :24-27 主张「累加器语义由 `drizzle_engine.h` 定义，其引擎侧产生过程由 `variance_propagation_test` 的 pixfrac 扫描段承载，两者共同覆盖"引擎 → writer → 产品"全链」；`docs/contracts/DATA_SEMANTICS.md:2469` 规定 tile 值为面亮度（读回口径）。
- 现状：`D_p` 恒等于 `(整数 q)/255 × a_cell`，故 `lround(255·D_p/a_cell) = q` 精确往返，读回 support 与真值逐位一致（:250 的门因此近似恒成立）；真实帧的 `D_p/a_cell` 是任意实数，量化误差上界 `±0.5/255 ≈ ±1.96e-3` 相对 0.251 档即 ~0.2%。
- 差在哪：判据用"恰好无损"的夹具回避了它本应检验的形态——若 writer 侧存在"flux 按未量化 D_p 归一、support 按量化值落盘"的口径分离（即已裁的 profile=1 偏移），其后果（~2e-3 的 signal 偏差）比 :229 的 1e-5 容差大 2 个数量级，本门在现有夹具下永远看不到；:24-27 的"全链覆盖"因此不含量化这一环。
- 后果：pixfrac<1 的 signal/variance 对外主张只在格点域成立；profile=1 的已知偏移既没有产品级在册门，也没有被本门承认（它反而以 1e-5 的容差暗示"该端已精确到 1e-5"）。
- 定级：S2（不改本轮产品值；对"全链覆盖"与 profile=1 端精度两项主张失真）
- 怎么算修好：① 增加非格点档：取 `D_p/a_cell = 0.2512…`（q 上下取整两侧）各造一档，断言"读回 support = lround 结果"且"signal 偏差 ≤ 量化引入的解析上界"（把偏移量作为**被检验的已知项**，而不是回避掉）；② 与 CR-11-24 同批把 profile=1 容差的来源写成量化余量；③ 或在 :24-27 如实限定范围（"本门只覆盖归一幂次，量化语义另立门"）。
- 置信：PARTIAL（夹具的格点构造与量化上界为 CONFIRMED；"profile=1 的 signal 是否使用量化后的 support 作分母"需读 `astro_sphere_sink.cpp` 的 phase1 分支与 `aio_hips` writer 才能定案，本批未展开）

### CR-11-26 · 门内 `A_pixel ≡ 1 px²` 与 `sumNorm` 的单位口径由夹具自证，需与 `variance_propagation_test` 同读才能定案
- 轴：S
- 位点：`drizzle_pf_sb_gate.cpp:112` `const double A_pixel = 1.0;   // 源像素面积 ≡ 1 px²`（配合 :21 的 `N_p = Σ_j w_jp·A_pixel,j`）
- 上位依据：`docs/science/DRIZZLE.md` §5（面亮度与 `N_p` 定义，经 `DRIZZLE_GEOMETRY.md:332` DISP-DRZ-009 转述）——条文中 `A_pixel,j` 是**源像素真实面积**（角尺度平方），不是归一化的 1。
- 现状：本门以 `A_pixel=1` 构造解析累加器，因而 `k = D_p/N_p ≡ pf²` 与单位无关成立；这是对 writer 幂次的正确检验，但若引擎侧 `sumNorm` 的累加单位与该条文的"面积"口径不一致（例如 px² vs sr），本门与 CR-11-25 的格点夹具都无法暴露。
- 差在哪：头注 :26-27 把这一维度让渡给 `variance_propagation_test`，但本批清单不含该文件，我未读到其 pixfrac 扫描段是否使用非单位 `A_pixel`。
- 后果：暂不定案；若那侧同样钉 `A_pixel=1`，则"引擎 → writer → 产品"两处共享同一假设，全链主张不成立。
- 定级：S3（PARTIAL，待与 `variance_propagation_test` 并读）
- 怎么算修好：读 `variance_propagation_test.cpp` 的 pixfrac 扫描段并核其 `sumNorm` 构造是否用真实角面积；若否，补一档真实尺度（如 6.3076″/px 的 sr²）到本门。
- 置信：PARTIAL（缺 `variance_propagation_test` 一角证据，且该文件不在 CR-11 清单内）

## 6. `lib/algorithms/drizzle/healpix_drizzle/tests/drizzle_science_completion_test.cpp`（491 行，整读完毕）

本文件取证到的决定性锚：
- **nside 钳位区间在两面互斥**：drizzle 正本面 `drizzle_engine.cpp:644-663`「NSIDE 钳位到 [16, 4194304] (2^4 到 2^22), 覆盖约 0.05" 到 3.7° 像素尺度, 支持 0.1"/px 输入的 1~2 倍过采样要求 (211076.3/0.1 ≈ 2.1e6, 需要 2^22)」/ `static const int NSIDE_MAX = 4194304;`，并有 `docs/algorithms/DRIZZLE_GEOMETRY.md:158`「钳位 [NSIDE_MIN, NSIDE_MAX] = [16, 4194304=2^22]」、`docs/contracts/DATA_SEMANTICS.md:377`「auto 模式钳位 [16,2^22]（compute_auto_nside）」、`hp_drizzle_api.h:115`「线性过采样倍率 finest/hp_res ∈ [1, 2); nside 钳位 [16, 2^22]」；而 `lib/infrastructure/pipeline/orchestrator/memory.md:578` 说「找到不小于 nside_target 的最小 2 的幂次方，限制 **[64, 131072]**」。
- 我据 `210960/scale″→ 下一 2 的幂`（`test_nside_pixfrac.cpp:185` 的实现口径）复算本文件全部 nside 档位：
  - T13 `{0.0503245, 4194304}`：`210960/0.0503245 = 4,192,588 → 2^22` ✓ 精确命中；`{12.883074, 16384}`：`210960/12.883074 = 16,376 → 2^14` ✓ 精确命中 ⇒ 两档确是 auto 规则的两端（钳位上限＋最粗端），且 `memory.md:151-152` 已把「余量 0.0503245(NSIDE=4194304)～12.883074(NSIDE=16384)″/px」登记在模块 memory。
  - 6.3″/px 的 T10/T11/T12 取 65536 ✓ 与 auto 规则一致（33,446→65536）。
  - **T8/T9 例外**：`scale = 10.0/3600.0`（:51）⇒ auto 应为 `210960/10 = 21,096 → 32768`（且过采样倍率须在 [1,2)，nside=65536 给出 hp_res=3.22″、倍率 3.1 越界），而 :94 实传 `65536` ⇒ 比生产自动档细一档。
- SIP 求值口径（判 :68-71 用）：`wcs_sip.cpp:76-96` `Σ_{i+j≤order} coeffs[i*6+j]·dx^i·dy^j`，写侧 `hp_drizzle_api.cpp:478 a[i*6+j]` ⇒ 下标 0=A_00（常数项）、12=(i=2,j=0) A_20（**二次**）、14=(2,2) B_22（i+j=4 > order=3 ⇒ 不参与求和）、1=(0,1) B_01（线性 y）。
- 孔径/测光锚：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:316「multi-FWHM 孔径测光 4σ 相对差 <1e-3」；§9:315 不变量含「NESTED 地址往返」「负值保持」；§9:302 FIX-DRZ-F「SIP 畸变边缘 patch（15° 宽场）」。
- 零登记检索：`git grep -rn "质心\|椭率\|ellipticity" -- docs/algorithms/DRIZZLE_GEOMETRY.md docs/science/DRIZZLE.md` ⇒ 仅 `DRIZZLE.md:235`（photutils 第三方条目）；`git grep -rn "0.063\|0.005" -- 同两文件` ⇒ **零命中** ⇒ 本文件 :402/:424/:429 三个门（含 :427 自称"冻结门"）在科学/算法两面均无登记。

### 6.1 §2.5 逐数值处置表（本文件 34 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :26 | `PI_` | 3.14159265358979323846 | 圆周率字面量 | 结构性不适用 | 数学常数（20 位已足 FP64）；:112/:337/:364/:395 等按式使用 | —— | CONFIRMED |
| :38-39 | `precision_mode = 1`、`threads = 16` | 1 / 16 | 全文件共用 `run_drizzle` 的配置 | 待确认（threads） | `drizzle_engine.h:40`「禁止硬编码 16」＋AGENTS §6；枚举 1 属合同面（README.md:105） | 同 CR-11-2b 处置（改 0 或按 §9:316 测 1/2/4 一致性）；影响＝本文件 6 个用例共一处硬编码 | CONFIRMED |
| :50 | `fov_deg = 15.0` | 15.0 | 视场对角宽 | 文献值 | §9:302 FIX-DRZ-F「SIP 畸变边缘 patch（**15° 宽场**）」 | —— | CONFIRMED |
| :51 | `scale = 10.0/3600.0` | 10.0 | T8/T9 像元角尺度 | 待确认 | 条文面零提及（FIX-DRZ-F 未给尺度） | 保守方向＝取生产真实宽场尺度；影响＝边缘角距与 SIP 像素偏移量级（见 CR-11-27） | CONFIRMED |
| :51 | 3600.0 | 3600 | deg↔arcsec | 结构性不适用 | 单位换算定义（:133/:204/:309/:449/:420/:395-396 同族合并） | —— | CONFIRMED |
| :52 | `half_px = (int)(fov_deg/2.0/scale)` | 2700 | 半视场像素数 | 公式导出 | 定义式；代码按式算（(int) 截断，非四舍五入，差 <1 px） | —— | CONFIRMED |
| :53-56 | `patch = 128`；`cx = half_px - patch/2`；`cy = patch/2` | 128 | 边缘 patch 边长与位置 | 待确认 | 条文只说"边缘 patch"，未给 128 | 保守方向＝随尺度联动使 patch 覆盖边缘最外侧；影响＝畸变/角距激励强度 | CONFIRMED |
| :59-60 等 5 处 | `strncpy(ctype*, …, 15)` | 15 | 缓冲区留 NUL | 结构性不适用 | 数组维度/终止符（:59-60、:129-130、:200-201、:305-306、:445-446 合并） | —— | CONFIRMED |
| :61 等 5 处 | `crval = 272.886595, −23.254083` | 银心 | 夹具视场中心 | 结构性不适用 | 任意指向；5 个用例共用同一中心，无一判据依赖该值（:61、:131、:202、:307、:447） | —— | CONFIRMED |
| :62 等 5 处 | `crpix = …/2.0 + 0.5` | 0.5 | FITS 1-based 中心 | 结构性不适用 | 编号约定定义常数（:62、:132、:203、:308、:448） | —— | CONFIRMED |
| :67 | `w.sip.order = 3` | 3 | 声明的 SIP 阶 | **待确认（与头注冲突）** | 头注 :5 写「视场边缘 SIP **order5** 大畸变」，代码为 3；`hp_drizzle_api.h`/DISP-DRZ-001 允许 [0,5] | 保守方向＝取头注声明的 5 并让系数下标满足 i+j≤5；影响＝见 CR-11-27 | CONFIRMED |
| :68 | `sip.a[0] = 2.0e-4`（注"线性: 2700×2e-4 ≈ 0.54 px"） | 2.0e-4 | 意图为线性畸变，实为 A_00 常数项 | **待确认（值/式均与注释不符）** | `wcs_sip.cpp:76-96` 展开式 ⇒ 贡献恒为 +2e-4 px（非 0.54 px） | 保守方向＝改 `a[6]`（A_10）；影响＝"强畸变"激励是否成立（CR-11-27） | CONFIRMED |
| :69 | `sip.a[12] = 1.0e-10`（注"三次: 2700³×1e-10 ≈ 1.97 px"） | 1.0e-10 | 实为 A_20（x 的**二次**项） | **待确认（阶与量级皆错）** | 同上：下标 12 = (i=2,j=0) ⇒ 2700²×1e-10 = 7.3e-4 px（比注释小 2700 倍） | 保守方向＝真三次项写下标 18（A_30）或 12 处改注；影响＝同上 | CONFIRMED |
| :70 | `sip.b[1] = −2.5e-4`（注"线性: 0.68 px"） | −2.5e-4 | B_01，真线性 y 项 | 公式导出 | (0,1) ⇒ 2700×2.5e-4 = 0.675 px，与注释一致；但 0.675 px **小于**头注 :5/:65 宣称的 "> 1 像素" | 保守方向＝提高该系数或补真三次项；影响＝T9 是否名副其实 | CONFIRMED |
| :71 | `sip.b[14] = −1.2e-10`（注"三次: −2.36 px"） | −1.2e-10 | 实为 B_22（i+j=4 > order=3） | **待确认（死系数）** | `wcs_sip.cpp:86-88` 循环上界 `i ≤ order`、`j ≤ order−i` ⇒ 下标 14 永不被读 | 保守方向＝要么 order≥4，要么改下标；影响＝同上 | CONFIRMED |
| :84, :459 | `base = 1000.0 + 0.01x + 0.005y` | 1000/0.01/0.005 | 常数底＋梯度 | 待确认 | §9:299-301 只规定常量场/梯度场的形态 | 保守方向＝不影响闭合与孔精力学（线性场的闭合与常数场同）；影响＝仅夹具 | CONFIRMED |
| :86 | `500.0·exp(−r²/(2·30²))` | 500.0 / 30.0 / 2.0 | patch 内高斯源 | 待确认（2.0 属公式） | §9:300 FIX-DRZ-B 点源高斯（形态有、参数无） | 保守方向＝σ=30 px 应 ≪ patch 使源完整落界内（30≪64 ✓）；影响＝闭合口径 | CONFIRMED |
| :94 | `run_drizzle(img, 65536, 0.8, …)` | 65536 / 0.8 | T8/T9 的 nside 与 pixfrac | 待确认（nside）／文献值（0.8） | 10″/px 的 auto 档为 32768（`210960/10=21,096→2^15`），65536 使过采样倍率 3.1 越出 `hp_drizzle_api.h:115` 的 [1,2)；0.8 = 负责人裁生产默认 | 保守方向＝取 auto 档 32768 或登记"本门刻意细一档"；影响＝边缘用例对生产几何域的外推 | CONFIRMED |
| :102, :104, :106 | `rel < 1e-6` | 1e-6 | T8/T9 闭合门 | 文献值 | §9:304「FP64 通量闭合 <1e-6（主域）」 | —— | CONFIRMED |
| :112-116 | `acos(…)` 角距式与 `180.0`、`max(−1.0,min(1.0,·))` | 180 / ±1 | 大圆角距＋钳位 | 公式导出 | 标准角距公式，代码按式算；钳位是 acos 定义域防护 | —— | CONFIRMED |
| :119 | `d > 7.0` | 7.0 | "确在边缘"的下限角距 | 公式导出 | 由 fov/2 = 7.5° 与 patch 半宽余量得出；代码按实测值判 | —— | CONFIRMED |
| :126, :196, :302 | `size = 128/96/256`＋`nside = 65536`（6.3″/px） | 128/96/256/65536 | T12/T10/T11 图幅与网格 | 公式导出（nside）／待确认（图幅） | nside：`210960/6.3 = 33,485 → 65536` ✓ 与 auto 规则同值（与 L2/L3 一致）；图幅条文未规定 | 保守方向＝图幅只需保证源完整；影响＝仅夹具规模 | CONFIRMED |
| :133, :204, :309 | `s = 6.3/3600.0` | 6.3 | 像元角尺度 | 待确认 | 与 L0 :45 同族（全仓 6.3″/px 无条文锚；真实 T4 登记值是 6.3076，`GATES_AND_TOLERANCES.md:91`） | 保守方向＝改用登记的 6.3076 或在 §9 登记 6.3；影响＝nside 档与孔径角半径的换算 | CONFIRMED |
| :146 | `1000.0 − 1500.0·exp(−r²/(2·10²))` | 1500.0 / 10.0 | 负校准构造（中心 −500） | 待确认 | §9:315 不变量「负值保持」有形态、无参数 | 保守方向＝保证 sum_in>0（本构造 ≈1.54e7 ✓，否则 :163 相对差失去定义）；影响＝负值保持判据的合法性 | CONFIRMED |
| :153, :163, :168 | pf=0.8 与 `rel < 1e-6` | 0.8 / 1e-6 | T12 的 pixfrac 与闭合门 | 文献值 | 0.8 = 生产默认；1e-6 = §9:304 | —— | CONFIRMED |
| :154 | `min_out = 1e9` 哨兵 | 1e9 | 最小值初值 | 结构性不适用 | 哨兵（无叶时 :168 因 `has_neg=false` 判红，未泄漏成绿） | —— | CONFIRMED |
| :190 | `e·p < −1e-12` 半平面容差 | −1e-12 | 点-in-drop 判定容差 | 待确认 | 条文面零提及 | 方向已定：容差**放宽**包含 ⇒ `oracle` 只增 ⇒ `fh` 更易 >0 ⇒ 偏红（保守侧），故不掩盖漏选；量级 1e-12 rad ≈ 0.2 µas 可忽略 | CONFIRMED |
| :197 | `for (double pf : {0.6, 0.8})` | 0.6 / 0.8 | T10 的 pixfrac 集 | 文献值（0.8）／待确认（0.6） | §9:309 的零漏选门是"4 pixfrac × 5 尺度 × 7 nside 全枚举"（属 `candidate_oracle_test`）；本处 2 档×1 尺度×1 nside | 保守方向＝补 pf 档或明确本门是 T10 独立复算而非 §9 那一条；影响＝"空洞 Oracle"的适用域 | CONFIRMED |
| :209-210 | 常数场 `1000.0f` / `1000.0` | 1000.0 | T10 填充 | 结构性不适用 | 常数场取值与覆盖判定无关 | —— | CONFIRMED |
| :219, :263-286 | `sumArea > 0` 判据、反向补集 `oracle.insert(ip)` | 0 | out 集合与 oracle 的构造 | 待确认 | 反向 pass 只能**增** `oracle` ⇒ 只收缩 `ff`；§9:309 主张的是"false_negative=0"（漏选侧） | 保守方向＝ff 侧改为"引擎判覆盖但无任何 drop 样本"的单向判据；影响＝见 CR-11-31 | CONFIRMED |
| :230-232 | 角点表 `±0.5·pf` | 0.5 / −0.5 | drop 四角 = 中心 ± 0.5·pf | 公式导出 | F&H 2002 的 drop 定义（`A_drop = pf²·A_pixel`，仓内锚 `astro_sphere_sink.h:39-40`）；代码按式算 | —— | CONFIRMED |
| :238, :250-259, :270-278 | 采样集「中心＋4 角＋4 边 ×3 分点」的 `3`/`4.0` | 3 / 4 | 独立 Oracle 的采样密度 | 结构性不适用 | 采样实现的分辨率选择，非科学量（注释 :238、:262 自述目的是"消除采样密度边界漏"） | —— | CONFIRMED |
| :294 | `fh == 0 && ff == 0` | 0 | 零漏选/零误填门 | 文献值（适用域差） | §9:309「候选零漏选: 9003 例 false_negative=0」＋§9:310-311「reverse: false_hole=false_fill=0」——**后者属 reverse 通道**，被本门（正向 drizzle）借用 | 保守方向＝把 §9 的正向零误填单列或改引 reverse 条文；影响＝引用条文与所测通道不同域 | CONFIRMED |
| :316 | `sigma = 4.0`、`amp = 1000.0`（T11） | 4.0 / 1000.0 | 高斯点源参数 | 待确认 | §9:300 FIX-DRZ-B 未给 σ；σ=4 px 与 6.3″/px、nside 叶 3.22″ 的比例决定孔边界量化误差 | 保守方向＝σ 应 ≫ 叶尺度（4px=25.2″ vs 3.22″ ✓ 约 8 倍）；影响＝1e-3 门的可达性 | CONFIRMED |
| :328 | `run_drizzle(img, nside, 1.0, …)` | 1.0 | T11 的 pixfrac | 待确认 | §9:316 的孔径测光不变量未限定 pf；本门只在 pf=1 域 | 保守方向＝补 pf=0.8 档孔径测光；影响＝"测光校准到星等坐标系"主张在 pf<1 域无孔径级证据 | CONFIRMED |
| :337, :351, :364, :378 | `aper_rad = 4σ·s·π/180`、`truth = F(1−e^{−r²/2σ²})` | 4.0 / 2.0 | 圆内能量解析真值 | 公式导出 | 2D 高斯 enclosed energy 标准式；代码**真按式算**（:351 用 (4·4)/2，:378 用 r_px²/(2σ²)） | —— | CONFIRMED |
| :356, :383 | `rel < 1e-3`（4σ 与 2/3/4×FWHM） | 1e-3 | 孔径测光门 | 文献值 | §9:316「multi-FWHM 孔径测光 4σ 相对差 <1e-3」；:354 打印串"（<1e-3, 冻结≤0.1%）"与之一致 | —— | CONFIRMED |
| :359, :419-420 | `2.3548` | 2.3548 | 高斯 FWHM 换算 2√(2ln2) | 公式导出 | 定义式换算；代码按式算 | —— | CONFIRMED |
| :360 | `k_list[3] = {2.0, 3.0, 4.0}` | 2/3/4 | 多倍 FWHM 集 | 待确认 | §9:316 只写"multi-FWHM"未给倍率集 | 保守方向＝至少含 r≳3σ（圆内能量 >99.7%）以压低边界项；影响＝门的敏感度 | CONFIRMED |
| :395-396, :412-413 | `×3600.0`（deg→arcsec）与 `cos(dec0)` 一次因子 | 3600 | 平面近似坐标 | 结构性不适用（换算）／公式导出（切平面近似，忽略 cos(dec) 随 dec 变化，本夹具 7.5° 内误差 ~1e-4 相对） | —— | CONFIRMED |
| :401, :402 | `cen_off < 0.063`（"= 0.01px"） | 0.063 / 0.01 | 质心偏差门 | 待确认 | 条文面零提及（"质心"在 §9 无条目；见上检索式） | 保守方向＝在 §9 立条（含 0.01 px 的推导：叶尺度/√12 或像元化偏差），否则该"门"无冻结地位；影响＝测光坐标系主张的一项支撑 | CONFIRMED |
| :418-420 | `rms = sqrt(0.5(mxx+myy))`、`fwhm = 2.3548·rms` | 0.5 | 圆等效二阶矩 | 公式导出 | RMS 宽度定义（面积加权二阶矩的平均），代码按式算 | —— | CONFIRMED |
| :424 | FWHM 相对门 `< 0.01` | 0.01（1%） | PSF FWHM 门 | 待确认 | 条文面零提及 | 保守方向＝与 §9 立条同批；影响＝同上 | CONFIRMED |
| :425-426 | `e = sqrt((mxx−myy)² + 4mxy²)/(mxx+myy+1e-30)` | 4.0 / 1e-30 | "椭率"定义式 | 待确认 | 条文/科学面对"椭率"**零定义**；此式分母缺标准二阶矩椭率的 `2√(Ixx·Iyy − Ixy²)` 项 ⇒ 与惯用 ε 不同量（见 CR-11-30） | 保守方向＝改用惯用式或在条文里定义本式；影响＝该数值不可与外部 PSF 形态指标比较 | CONFIRMED |
| :428, :429 | `e < 0.005`（注释称"冻结门"） | 0.005 | 椭率门 | 待确认 | 条文面零命中（`0.005` 在 §9/§DRIZZLE 均无）⇒ 注释的"冻结"地位无凭（CR-11-29） | 保守方向＝登记或撤"冻结门"措辞；影响＝同上 | CONFIRMED |
| :436 | `size = 32`（注"小图控制高 NSIDE 成本"） | 32 | T13 图幅 | 结构性不适用 | 成本控制下的实现选择；闭合相对差与图幅无关（常数＋梯度场） | —— | CONFIRMED |
| :439 | `{0.0503245, 4194304}` | 0.0503245″ / 2^22 | 高采样余量档 | 文献值（＋公式导出） | `lib/algorithms/drizzle/healpix_drizzle/memory.md:151-152`（登记该对）；`drizzle_engine.cpp:661-663`「NSIDE_MAX = 4194304 (2^22): 像素尺度 ~0.0503"」；`210960/0.0503245 = 4,192,588 → 2^22` 我复核 | 与 orchestrator 侧钳位的冲突见 CR-11-33 | CONFIRMED |
| :440 | `{12.883074, 16384}` | 12.883″ / 2^14 | 低采样余量档 | 文献值（＋公式导出） | 同上 memory:151-152；`210960/12.883074 = 16,376 → 16384` 复核 | 同上 | CONFIRMED |
| :471, :476 | `rel < 1e-5`（T13 闭合门） | 1e-5 | 主域节点余量的 FP64 闭合门 | **待确认（宽于冻结主域值）** | §9:304 规定「FP64 通量闭合 <1e-6（主域）」 | 保守方向＝收 1e-6（同文件 :106/:168 已在用且能过）；影响＝见 CR-11-28 | CONFIRMED |
| :103, :164, :290, :352, :472 | `char msg[160]` | 160 | 断言名缓冲 | 结构性不适用 | 缓冲区尺寸（snprintf 截断只影响打印） | —— | CONFIRMED |
| :27, :489 | 计数器与退出码 | 0/1 | 汇总 | 结构性不适用 | 退出码约定 | —— | CONFIRMED |

### 6.2 findings

### CR-11-27 · "T9 强 SIP 边缘"的四个系数里三个落在错误的 (i,j) 上：实际边缘畸变 ≤0.675 px，达不到头注宣称的 order5 / >1 像素
- 轴：R（主）/ S / D
- 位点：`drizzle_science_completion_test.cpp:67-71`
  ```
  w.sip.order = 3;
  w.sip.a[0]  = 2.0e-4;    // 线性: 2700 x 2e-4 ≈ 0.54 px
  w.sip.a[12] = 1.0e-10;   // 三次: 2700^3 x 1e-10 ≈ 1.97 px
  w.sip.b[1]  = -2.5e-4;   // 线性: 0.68 px
  w.sip.b[14] = -1.2e-10;  // 三次: -2.36 px
  ```
  头注 :5「T9 强 SIP 边缘 (视场边缘 SIP **order5** 大畸变 **> 1 像素**)」、:65「强 SIP order 3: 视场边缘 (dx≈2700) 畸变 ~2 像素」。
- 上位依据：求值式 `wcs_sip.cpp:76`「公式: result = Σ_{i+j<=order} coeffs[i*6+j] * dx^i * dy^j」（写侧一致：`hp_drizzle_api.cpp:478`）；§9:302 FIX-DRZ-F「SIP 畸变边缘 patch（15° 宽场）」是 T8/T9 的规范来源（未给阶与量级）。
- 现状（按上式逐项展开，我手算）：`a[0]` = A_00 ⇒ f 加常数 +2e-4 px（注释的 0.54 px 不成立）；`a[12]` = (i=2,j=0) = A_20 ⇒ 二次项，贡献 2700²×1e-10 = **7.3e-4 px**（注释按三次算出 1.97 px，高估约 2700 倍）；`b[1]` = B_01 ⇒ 真正的线性项 −0.675 px ✓；`b[14]` = (2,2)，i+j=4 > order=3 ⇒ 循环上界 `j ≤ order−i` 永不取到 ⇒ **系数死置**。总畸变幅值 ≲0.68 px。
- 差在哪：三处下标按"紧凑序"直觉书写，而求值侧是 6×6 行主序；且 `order` 与所写下标不自洽（一个越阶系数被静默忽略）；结果是 T9 的激励量比其宣称值低约 3 个数量级，仍不足以形成"边缘 >1 px 大畸变"的场景。
- 后果：T9 名义上承担的三项覆盖全部落空——(a) 高阶（5 阶）SIP 校验面、(b) "边缘大畸变下通量闭合"（§9:302 FIX-DRZ-F 的实质风险）、(c) `p0_sip_order_guard_test`/DISP-DRZ-001 关心的"order 与系数组一致"路径。§9 里唯一与畸变边缘相关的夹具（FIX-DRZ-F 15° 宽场）因此只在"畸变 ≈0"的退化域上取绿；而闭合门 :106 判绿会被读作"强畸变下闭合成立"。
- 定级：S2（本文件未注册 ctest，无运行时触发路径；但它是 §9:302 那条冻结夹具的唯一执行者，故不降 S3）
- 怎么算修好：按行主序重写线性/二次/三次项——`a[6]=A_10`、`a[12]=A_20`、`a[18]=A_30`（或 y 向 `a[1]/a[7]/a[13]`），并把 `order` 设为实际用到的最大 i+j（头注若要 order5 就真写 5 阶系数）；系数幅值按"边缘像素偏移 >1 px"倒推（例如 `|A_30| ≈ 1.97/2700³ ≈ 1e-11` 配合 order≥3）；同批改 CR-11-5 里 L0 与 bench 的同型错写（`a[0]/b[3]`），并在 `wcs_sip.h` 的 a/b 字段处补"下标 = i*6+j"一句，避免第三次复发；修完 :65/:5 的措辞要与真激励量一致（写明"边缘偏移 X px"）。
- 置信：CONFIRMED（展开式与逐下标映射由 :76 原文直接得出，可手算复核）

### CR-11-28 · T13（主域节点余量）的 FP64 闭合门取 1e-5，比 §9 冻结的主域值 1e-6 宽 10 倍，方向偏绿
- 轴：C（主）/ S
- 位点：`drizzle_science_completion_test.cpp:476` `CHECK(rel < 1e-5, msg);`（:474 打印串同值「闭合 %.3e (<1e-5)」）
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md` §9:303「冻结容差（预冻结，源自既有测试门，**取值一律按本节**）」、:304「FP64 通量闭合 **<1e-6（主域）**」；本文件另一处 :106/:168 用的正是 `<1e-6`。
- 现状：同一文件内、同为 FP64 帧级闭合，T8/T9/T12 用 1e-6，T13 用 1e-5；T13 恰恰是"主域节点余量"用例（头注 :9、:433），即条文所说"主域"的两个端点档。
- 差在哪：判据是 `rel < 阈值`，阈值放宽 10 倍 ⇒ `rel ∈ [1e-6, 1e-5)` 的余量档结果按 §9 应判红、在 T13 判绿；且无注释给出为何余量档可以比主域更松（余量档的网格更细 2^22，叶面积更小，累加项更多，理论上噪声不降）。
- 后果：T13 是"生产网格两端余量"的在册陈述（memory.md:151-152 亦登记了该两档），其闭合结论落在比冻结主域值宽一档的判据上；同一文件的 :106 与 :476 互相矛盾，读者无法判断 1e-6 还是 1e-5 是"主域"的口径。
- 定级：S2（未注册 ctest ⇒ 无产品字节路径）
- 怎么算修好：① 统一为 §9 的 1e-6（本文件已有两处用 1e-6 且据注释可过），若确需对 2^22 档放宽，则在 §9:304 旁边登记放宽原因与实测余量，并把 :476 改为具名常量；② 同批与 CR-11-1（L0 2e-5）、CR-11-11（L0/L2 双值）一次裁决，避免逐文件各改一次；③ 若同时改测试与 §9，须先跑一遍确认 :476 收紧后不脆红（见"需要实跑"节）。
- 置信：CONFIRMED（条文与两处代码值逐字对读；"能否过 1e-6"未实跑）

### CR-11-29 · 质心 / FWHM / 椭率三个门在条文面零登记，其中椭率门注释自称"冻结门"
- 轴：S（主）/ C
- 位点：`drizzle_science_completion_test.cpp:402` `CHECK(cen_off < 0.063, msg);`（:400 注「质心 ≤0.01 目标像素 (= 0.01×6.3" = 0.063")」）、`:424` `CHECK(fabs(fwhm_meas - fwhm_truth)/fwhm_truth < 0.01, msg);`、`:427-429` `// 椭率 ≤0.005 (冻结门)` + `CHECK(e < 0.005, msg);`
- 上位依据：条文面零提及——检索式与结果：`git grep -rn "质心\|椭率\|ellipticity" -- docs/algorithms/DRIZZLE_GEOMETRY.md docs/science/DRIZZLE.md` ⇒ 仅 `DRIZZLE.md:235`（photutils 第三方条目，非本仓定义）；`git grep -rn "0.063\|0.005" -- 同两文件` ⇒ 0 命中。§9:316 在同一段里只冻结了「multi-FWHM 孔径测光 4σ 相对差 <1e-3」（本文件 :356/:383 已对齐），未列质心/FWHM 复现/椭率任何阈值。
- 现状：三个数值都以"门"的形态进入 `g_pass` 统计并在汇总里计入通过数，且 :427 用"冻结门"字样为 0.005 赋予它没有的地位；:5 的 T11 标题也把这四项并列为科学补齐内容。
- 差在哪：§9:303 要求冻结容差"取值一律按本节"，而这三处取值不在本节；:427 的自称与登记面不符（正是"注释自称权威、权威面另有其值或无值"形态）。
- 后果：SCI 侧"球面孔径测光 / 质心 / PSF FWHM / 椭率"这组对外主张的证据强度被三个无出处的阈值钉住；若负责人按 §9 复核，会得出"其中三项从未冻结"的结论，而补齐门的通过数已把它们计入。
- 定级：S2
- 怎么算修好：要么在 §9（或 GATES_AND_TOLERANCES.md）为质心/FWHM/椭率各立一行，给出推导（例如质心分辨率下限 = 叶尺度/√12、FWHM 复现误差 = 二阶矩估计方差随参与叶数的关系、椭率的定义式与阈值来源），要么把三条降为报告并删除"冻结门"字样；删/改须与 :489 的通过数一起重述，避免"改对反而判红"（降为报告后总数变化）。
- 置信：CONFIRMED（零命中检索式已附）

### CR-11-30 · 椭率定义式的分母缺 `2√(Ixx·Iyy−Ixy²)` 项，与惯用二阶矩椭率不是同一量，且本仓无任何定义面
- 轴：R
- 位点：`drizzle_science_completion_test.cpp:425-426` `double e = std::sqrt((mxx - myy) * (mxx - myy) + 4.0 * mxy * mxy) / (mxx + myy + 1e-30);`
- 上位依据：条文/科学面对"椭率"零定义（CR-11-29 的检索式 0 命中）；`docs/science/DRIZZLE.md:235` 只把 photutils 列为"检测/质心、背景估计、PSF 与孔径测光"的佐证件。
- 现状：本式在圆对称时确实给 0（故 T11 现在能过），但它是未归一的矩差比：对轴比 `q = Iyy/Ixx` 的椭圆，本式给 `(1−q)/(1+q)`，而惯用二阶矩椭率为 `(1−√q)/(1+√q)` ⇒ 同一"椭率 0.005"门限在两种定义下对应的真实轴比不同（本式更松：轴比差约 2 倍于惯用定义所容许）。
- 差在哪：把一个非标准量按标准量名对外使用；本文件与 §9 都没有声明定义式，读者无法把 `e` 与外部 PSF 形态指标（photutils `moments_central` 族）比较。
- 后果：T11 的"椭率"通过不构成对 PSF 形态失真的任何可比结论；若未来把该值写入产品元数据或对外图表，会与其它管线口径冲突（同一名字两个数）。
- 定级：S3（当前夹具是圆对称，本门数值上不误判）
- 怎么算修好：在 §9（或 SCI 的 PSF 形态条）里写定一个定义式（惯用式或本式，二选一），代码与文档同名同式；若采用惯用式，:425-426 的分母补 `2*sqrt(mxx*myy − mxy*mxy)` 项并同步阈值。
- 置信：CONFIRMED（两种定义在 q 上的代数差已手算；本仓无定义面为 CONFIRMED）

### CR-11-31 · T10 的独立 Oracle 反向 pass 只能向 oracle 增元素 ⇒ `false_fill==0` 一侧近乎自证，且其引用条文属另一通道
- 轴：R（主）/ C
- 位点：`drizzle_science_completion_test.cpp:263-286`（`for (uint64_t ip : out) { … if (covered) oracle.insert(ip); }`）与 `:287-294`（`for (uint64_t ip : oracle) if (!out.count(ip)) fh++;` / `for (uint64_t ip : out) if (!oracle.count(ip)) ff++;` / `CHECK(fh == 0 && ff == 0, msg);`）
- 上位依据：§9:308-309 的正向零漏选锚在 `candidate_oracle_test`（9003 例、4 pixfrac×5 尺度×7 nside）；§9:310-311 的「reverse: false_hole=false_fill=0」明确写在 **reverse 通道**条目下。
- 现状：oracle 先由正向 drop 采样点构造（:226-259），随后对 `out` 里每个叶自采其中心/角点/边分点，只要**任一点**落在**任一** drop 内就把该叶补进 oracle。
- 差在哪：反向 pass 对 oracle 是单调只增的，而 `ff = |out∖oracle|`——引擎宣称覆盖的叶几乎总能被自身几何采样"证实"，因此 ff 只在"引擎覆盖了一个几何上完全采不到点的极薄边缘"时才非零；判据方向：该构造使 ff 偏向 0（偏绿），而 fh（漏选侧）不受影响（只增 oracle 会让 fh 变大，即偏红，安全）。同时头注 :6 与 :294 用 §9:311 的措辞（false hole/false fill 成对）来包装一个正向通道的门。
- 后果：T10 的"独立 Oracle"对误填侧的判定力被自己稀释；若生产重叠面积在边界上把无覆盖叶算成有覆盖（正是 pixfrac 空洞的反面缺陷），本门可能仍绿；对外可引的"零误填"结论实际只有 candidate_oracle 那一侧的漏选证据。
- 定级：S2
- 怎么算修好：① 误填侧改用**不依赖 `out`** 的独立判据：对 oracle 补集里的叶（预期无覆盖）显式断言其不在 `out`，而不是把 `out` 的成员回灌 oracle；或改为面积判据（`sumArea` 与几何可覆盖面积比 ≤ 容差）；② 把 :6/:294 的措辞限定为"零漏选（正向）"，误填主张引 §9:311 时注明该条属 reverse 通道；③ 与 §9:309 的档位口径对齐（本门 2 pf×1 尺度×1 nside，别被当作那 9003 例的替代）。
- 置信：PARTIAL（oracle 的单调只增与 ff/fh 的方向为 CONFIRMED；"薄边缘是否可能被采到"未做实例枚举，若构造不出反例则该子项降为措辞问题）

### CR-11-32 · `run_drizzle` 丢弃 `drizzleTiled_f64` 返回码与 err，六个用例共用同一无校验入口
- 轴：R
- 位点：`drizzle_science_completion_test.cpp:41` `engine.drizzleTiled_f64(img, cfg, nullptr, nullptr, t64, st, err);`（返回 `bool` 未接收、`err` 未判非空）
- 上位依据：同族件 `drizzle_l0_test.cpp:84` `if (!engine.drizzleTiled_f64(img, cfg, …)) { … CHECK(false, …); return; }` 与 `drizzle_l2_test.cpp:66` 同型——即同模块已有正确写法；引擎对非法配置是**返回 false 拒绝**（`drizzle_engine.cpp:782-787` pixfrac 域拒绝并打印）。
- 现状：调用失败时 `t64` 为空，六个用例继续跑，判据以"闭合 1.000e+00 / 孔径 rel=1.000e+00 / NaN 门限超限"的形态变红，`err` 里的真实原因被 :130 之类的位置丢弃。
- 差在哪：不会误绿（空集合 ⇒ 相对差=1 或 NaN ⇒ 判红，方向已核），但把"调用失败"降级成"数值不合格"，诊断误导；且若某次改动让某用例的判据在空输入下变绿（例如只留 `!bad` 半支、或 `has_neg` 换成 `min_out` 比较），空运行会静默全绿。
- 后果：本文件的失败定位成本；潜在误绿面在 :106 的 `rel < 1e-6 && !bad`（`bad` 半边对空集合恒假 ⇒ 该半支恒绿，实际只靠 `rel` 半支承重）。
- 定级：S3
- 怎么算修好：接收返回值并在失败时 `CHECK(false, err)` 并 `return`（照 L0 :84-88 写法）；每个用例再补一条 `!t64.empty()` 的结构断言，使"空输出"不可能通过任何半边。
- 置信：CONFIRMED

### CR-11-33 · T13 以 `nside=2^22` 为"主域节点上限"，而管线侧登记的钳位是 `[64, 131072]`：两面对"生产上限"取值互斥
- 轴：C（主）/ S
- 位点：`drizzle_science_completion_test.cpp:439` `{0.0503245, 4194304},   // NSIDE=2^22 高采样余量`
- 上位依据：drizzle 正本面 `drizzle_engine.cpp:644-663`「NSIDE 钳位到 [16, 4194304] (2^4 到 2^22) … 支持 0.1"/px 输入的 1~2 倍过采样要求」、`docs/algorithms/DRIZZLE_GEOMETRY.md:158`、`docs/contracts/DATA_SEMANTICS.md:377`（「auto 模式钳位 [16,2^22]（compute_auto_nside）」）、`hp_drizzle_api.h:115`；相反面 `lib/infrastructure/pipeline/orchestrator/memory.md:574,578`「规整到 2 的幂次方，范围 **[64, 131072]**」。
- 现状：本门取 2^22 作"生产上限档"，与 drizzle 侧四面一致；但生产管线侧（真正决定下发 nside 的那一面）登记的上限是 131072 = 2^17。
- 差在哪：同一个"生产 nside 上限"在两族文档里差 32 倍（且与 `KNOWN_LIMITATIONS.md:139` 的"生产 nside（≥2^17）"读法相互缠绕）。按 orchestrator 侧，0.0503245″/px 的帧会被钳到 131072（过采样倍率 0.25 ⇒ 欠采样），本门所验证的 2^22 档在生产不可达；按 drizzle 侧则本门正是上限档余量检验。
- 后果：T13 的高采样档结论是否对应生产可达配置，取决于哪一面是运行期真读的权威；判错的两种后果相反（"余量已验证"变"生产其实欠采样 4 倍"）。
- 定级：S2（缺定案证据一面）
- 怎么算修好：先定运行期真读哪面（读 `orchestrator` 里 nside 计算函数的实际实现，而非其 memory 文档；本批 20 次取证上限内未展开，见"需要实跑/需要读"节），再统一两面措辞；若权威是 `[16,2^22]`，则订正 `orchestrator/memory.md:578`，否则在 §9/T13 注明"本档为引擎能力上限，非生产下发档"。
- 置信：PARTIAL（两面对立为 CONFIRMED；哪面生效需读 orchestrator 代码或实跑 provenance）

## 本批最高危三条

1. **CR-11-17**（`drizzle_nonfinite_test.cpp:227`）：唯一一条"合同说 A、代码实现 A、门断言 非 A"的三方冲突——把已作废的"零方差即丢弃"当期望钉住，对 HEAD 恒红且因未注册 ctest 而无人可见；反向"顺测试改引擎"会销毁 `support`、使合法像素在产品面变无效。修法必须同批补正向断言（V=0 样本仍贡献 F_p/W_p），否则只是把红挪个位置。
2. **CR-11-27**（`drizzle_science_completion_test.cpp:67-71`）：§9:302 FIX-DRZ-F（15° 宽场 SIP 边缘 patch）的唯一执行件里，4 个 SIP 系数有 3 个落在错误的 (i,j)（A_00 当线性、A_20 当三次、B_22 越阶被忽略），实际边缘畸变 ≲0.675 px 而非宣称的 order5/>1 px；同型错写还在 `drizzle_l0_test.cpp:48-50`（CR-11-5）与 `bench_drizzle.cpp:47-48`，即"带畸变的闭合/候选"覆盖在两个批次里都是零激励。
3. **CR-11-1 + CR-11-11 + CR-11-28 一组**（2e-5 vs 1e-5 vs 1e-5 vs 1e-6）：同一个 §9 冻结容差在本批 4 个入口取了 4 个数，且 §9:303 明确要求"取值一律按本节"——需要负责人一次裁决"1e-5 逐 leaf 是否可达 / FP64 主域 1e-6 是否含余量档"，然后一次改齐 L0/L2/L3/补齐四处与 §9，否则逐文件修补会持续制造"改对反而判红"。

## 我读不动或需要实跑才能定的

| 项 | 缺什么 | 该跑什么（不在本只读节点执行） |
|---|---|---|
| CR-11-1 / CR-11-28 的阈值可达性 | 注释自述的实测值（小图 1.1e-5、L2 p95 2.6e-7/max 4.9e-7、T13 档 rel）无任何跟踪面记录 | 注册 L0/L2/补齐入门前，先跑 `drizzle_l0_test` / `drizzle_l2_test`（需 L2 夹具，见下行）并把 FP32/FP64 逐 leaf 与逐档闭合实测落 `实验/` 或跟踪报告；重计算须套 `eng/tools/monitoring/mem_guard.py`（本节点禁执行） |
| CR-11-8 的 L2 夹具 | `run/temp/l2_crop/galaxy_crop_1024.fits` 在跟踪面零命中，来源裁剪步骤无记录 | 前台核该裁剪是否可从 `testdata/Galaxy_Center_T4/lights` 再生；确定登记落位（testdata 下载策略 or 跟踪脚本）后再注册 L2 |
| CR-11-22 / CR-11-25 的 profile=1 量化链 | 需读 `astro_sphere_sink.cpp` 的 `write_hips_phase1` 分支与 `aio_hips` writer 的 support 量化点，确认 signal 分母是否用量化后的 support | 读 `write_hips_phase1` + `lib/infrastructure/aio/src` writer 侧；或实跑本门并加一档非格点 `D_p` |
| CR-11-33 的 nside 权威面 | `orchestrator/memory.md` 与 drizzle 正本 `[16,2^22]` 对立，memory 文档非实现 | 读 orchestrator 里 nside 计算函数实现（`compute_auto_nside` 的实际调用点与是否二次钳位），必要时从 `p1_drizzle` provenance / `nside_decision` 输出取证 |
| CR-11-31 的反例存在性 | 未能构造"引擎覆盖但几何采不到点"的薄边缘实例 | 用 `reference_overlap` 的蒙特卡洛面或本门 oracle 的解析面积版做小范围枚举 |
| CR-11-12 的 Windows 侧可达性 | `GetProcessMemoryInfo` 失败分支是否真实可达未证（Windows 平台未开工） | Windows x64 首轮构建时以负例（压低阈值）验证本门能红 |
| 三条登记面滞后 | `eng/ci/prod_wiring_baseline.json:53,55,56` 仍把 L0/L3/非有限记为 `unbuildable_test`，而 CMakeLists 已建目标；过期键是否会让 `check_prod_wiring` 变红未定 | 由前台跑 `eng/ci/run_checks.py`（本节点禁执行）确认，并按 §10 授权后清理基线 |
| 本批门的 ctest 归属 | L0/L2/非有限/补齐四件只 `add_executable` 未 `add_test`，故本批所有定级被压到 S2 | 若注册，需同时核 `reference_overlap`（三条已知红灯断言）与 CR-11-17/28 的红绿，避免一次注册带进成片历史红 |

## 派单与清单一致性

清单 6 个文件与派单文字（"6 个文件、`lib/algorithms/drizzle` 第三段"）一致，无差异。本批按清单逐文件整读，未增删文件；`variance_propagation_test.cpp`、`reference_overlap.cpp`、`bench_drizzle.cpp`、`drizzle_acceptance_test.cpp`、`drizzle_freeze_test.cpp`、`candidate_oracle_test.cpp`、`astro_sphere_sink.cpp`、`drizzle_engine.cpp`、`wcs_sip.cpp`、`tests/CMakeLists.txt` 与 `eng/ci/*` 均作为**取证上下文**引用，未计入本批文件数。

## 口径备注（防汇总重复计数）

- 本批 threads=16 一族（CR-11-2b，位点 `drizzle_l0_test.cpp:79/:111`、`drizzle_l2_test.cpp:62`、`drizzle_l3_full_fp32.cpp:63`、`drizzle_science_completion_test.cpp:39`）与负责人记忆中「硬编码并行违 §6（7 条已折回 CR-05/06/08 锚）」属同一族，按**同族合并**上报，勿重复计条。
- 门数陈述修正：本批 6 个文件里真正在册的 ctest 只有 `drizzle_pf_sb_gate` 与 `drizzle_pf_sb_gate_legacy_injection`（＋ L3 仅 WIN32），其余 4 个源只编译不进基线；引用"drizzle L0/L2/非有限/补齐门"时须带此限定。


## 收工 `git status --porcelain` 原文

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工输出逐字一致，HEAD 仍为 `c8f64e9a`：本节点对被审仓库零改动（全部写入落在 `独立审计/证据/通读-CR-11.md`）。
