# 复核-AUD203 —— AUD-203「天光无缝核验」第②层独立复核

- 基线：HEAD `c8f64e9a`；仓库 `F:\Astro dev\Astro CS Normalization Database`；严格只读。
- 开工 `git status --porcelain` 原文（恰两行，与工包预期一致）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

- 复算件目录：`独立审计/复算件/aud203-v\`；全部脚本以 `PYTHONDONTWRITEBYTECODE=1 python -B` 运行，不 import 仓库模块。
- 顺序声明：以下每条均**先从源码/归档件自行复算**，再打开成稿对应小节比对数值；成稿只在 V1 复算完成后按小节切片读取（`sed -n '44,54p'` 一类），未整篇预读。

---

## V1 常数天光块下控制点权重不归零

`V编号`：V1（成稿对应 AUD203-03）

`判定`：**确认**

`我的独立复算（数 + 命令或脚本路径）`：

- 源码取式（逐行读，未依赖任何文档转述）：
  - `lib/algorithms/coverage/src/sampler.cpp:846` `s0 = 1.482602218505602 * median_of(|v−m0|)`
  - `:851` clip 阈值 `v <= m0 + 3.0*s0`；`:853` 收敛判据 `|nm−m0| < 1e-12*max(|m0|,1e-12)` → 常数输入首轮即 `break`
  - `:864` `const double sigma = (s0 > 0.0) ? s0 : 1e-12;`
  - `:872-875` `n_ret = max(n_retained,1.0);` `kcorr_f = frames[f].kcorr>0 ? … : cfg.control_k_corr` `cvar = kcorr_f * kPiHalf * sigma * sigma / n_ret`
  - `:877` `civar = cvar > 0.0 ? 1.0 / cvar : 0.0`
  - 常数 `:83` `kControlCorrDefault = 1.4`、`:84` `kPiHalf = π/2`；默认 `:306-308` `background_patch_radius = 8`（17×17=289）、`background_clip_sigma = 3.0`、`min_samples = 5`
- **解析极限（按冻结口径、去掉 1e-12 保护量）**：σ_bg_raw=0 ⇒ `cvar = k_corr·(π/2)·0²/289 = 0` ⇒ `:877` 的 `cvar>0` 为假 ⇒ `civar = 0`，即"无尺度信息"。这正是权威链要求的行为：`docs/science/PHASE2_UPM.md:84-85`（"σ_bg_raw = 0 ⇒ control_ivar 必须为 0，禁止以数值保护量生成有限方差发布"）与 `docs/algorithms/PHASE2_SAMPLER.md §5.4`（同一句 + 要求 `control_variance` 取非有限值）。
- **复刻算出的数**（`复算/aud203-v/v1_civar_floor.py`，输出 `v1_output.txt`）：17×17 全等值 patch → `sigma_raw = 0`、`sigma_used = 1e-12`、`n_retained = 289`（clip 不丢一个像素，等值满足 `v ≤ m0`）、`cvar = 7.609394e-27`、`civar = 1.3141651015302213e+26`。闭式核对 `n_ret/(k_corr·π/2·1e-24) = 1.314165e+26` 一致；电平从 0 到 5e4 结果逐位不变 ⇒ 与数据标度无关，纯由保护量平方产生。
- **它确实进权重**：`upm.cpp:1985` `if (!std::isfinite(civ) || civ <= 0.0) return 2;` 只拒 ≤0/非有限 ⇒ 1.3e26 全额放行；`upm.cpp:668-672` per-control 归一 `raw_w[i] = raw_w[i]/sums*reliability`。同算例 [D] 段：7 个正常观测（σ_true=1 ⇒ civar=1.37e2）+ 1 个常数观测，归一后常数点 `w = 1.0000000000`、每个正常点 `≈4e-25` ⇒ 该 cell 的 M/C 由退化点独取。相对生产 control_ivar 中位 5.6e-22（`PHASE2_SAMPLER.md:82`）为 **2.35e47 倍**。
- **同类第二处**：`sky_plane.cpp:296` `const double sigma = (s0 > 0.0) ? s0 : 1e-12;` → `:306-307` 同款 `variance/ivar`，同缺陷形态（成稿未列，见"与成稿差异"）。
- **反证：正确写法在同一模块已存在**：`upm.cpp:2954-2962` `p2_upm_control_variance()` 对 `!(sigma_bg > 0.0)` 直接 `return 1`（fail-closed），被 `lib/algorithms/integration/v6/src/phase2_integrate.cpp:1227` 消费；sampler 路径未复用它 ⇒ 同一算式两套口径，一套拒、一套造伪值。

`与成稿差异`：

- 数值可复现且一致：成稿 `≈7.6e-27 / ≈1.3e26`，我 `7.609394e-27 / 1.314165e+26`，文档登记值 `7.609e-27 / 1.314e26`——三者同一。
- 成稿引 `docs/science/PHASE2_UPM.md:85,§8`；实际条款正文在 `:84-85`（跨两行），引用可辩护，属行号漂移非误引。
- 机制定性（回答工包的二问）：**不是"除零未设守卫"，而是"有显式下限/钳位（1e-12）"**——正因有这个钳位，`civar` 落在"有限且为正"区间，故 `:877` 的 0 分支与 `upm.cpp:1985` 的 rc=2 兜底**双双不可达**，设计上的 fail-closed 路径被绕过 ⇒ fail-open 判定成立，且不是崩溃类缺陷而是**发布语义**缺陷。
- 成稿漏一项：`sky_plane.cpp:296/306-307` 的同款地板（单 patch 面，n_retained=289 时同为 1.314e26，441 时 2.005e26）未登记；改法若只动 sampler.cpp 会留第二处。
- 成稿称 DISP-P2SMP-007 "仍活"：核实为真——`git grep DISP-P2SMP-007` 全仓唯一命中 `docs/algorithms/PHASE2_SAMPLER.md:562`（算法文档偏差表），**eng/ci 与 eng/contracts 均无登记**，故无机器门盯它，属"文上有账、码上未修、门上无锁"。
- 附带发现（不在本条计分）：`PHASE2_SAMPLER.md:292/297` 引用的 `evidence/e3e4_floor_structure.json:E3b_constant_patch`、`evidence/e3c_weight_pollution.json` 在跟踪集中 `git ls-files` 零命中 ⇒ 该文档的"实测"数字目前无归档件支撑，我这条的 1.314e26 是靠**闭式 + 自复刻**独立站住的，不依赖那两个文件。

`定级建议`：**P1**（发布语义违反冻结口径 + fail-closed 路径不可达 + 命中即独取该 cell 权重；触发域为"patch 内 ≥50% 像素同值"，真实噪声帧不易命中但量化/填充/掩膜填零数据可命中，故未上 P0）

`缺什么证据（判待证时必填）`：不适用（判确认）。

---

## V2 出厂接缝门与验收证据用的不是同一个统计量

`V编号`：V2（成稿对应 AUD203-06）

`判定`：**确认**（机制成立、数值可独立复现，且我复算出一条成稿没有的**反向**后果）

`我的独立复算（数 + 命令或脚本路径）`：

- 两统计量定义（各自从源码读出，未采信文档转述）：
  - **出厂门** `eng/tools/e2e/seam_footprint.py:300-376`（`edge_metric`）：`seam = img(p+n*d) - img(p-n*d)`（d=2）、`step = median(seam)`（有符号）、`bg = median(|img(p+-n*d)|)`、判据 `rel_step = step/bg`，门 `max_e|rel_step| <= 1e-2`；`ctrl_step / step_net / v1 excess / noise_ratio` 在 v2 里**只落盘不判红**（适用域 `:317-329`、返回值面 `:354-376`）。判据是**未去趋势、未扣对照**的原始电平差分，对法向梯度线性响应：解析式 `|rel_step| = 2d*(dL/dn)/bg = 4g/bg`。
  - **归档实验判据** `实验/additive-sky-seamless/code/sci_c_common.py:356-374`（`_step_at`）+ `:377-410`（`seam_steps`）：profile 取 `nanmedian_y`，在 ±`base_win`(64) 窗口内**挖掉 ±(halfwin+2)** 后拟合 **order=2 多项式基线**，`step = median(右 halfwin 残差) - median(左 halfwin 残差)`；`excess = step(xb) - median(step(xb+delta))`, delta 属于 (-96,-64,-32,32,64,96)。即**先扣曲率再扣公共结构**。
- **我的复算**（`复算/aud203-v/v2_seam_stats.py` + `v2_output.txt`），全部输入**无任何阶跃**（纯相干结构）：

  | 输入（无接缝） | 出厂门 abs(rel_step) | 实验 abs(rel_excess) | 比值 |
  |---|---|---|---|
  | (a) 纯线性斜坡 g=2.176830 ADU/px（bg=1200） | **7.256100e-03**（门的 72.6%） | **0.000000e+00** | inf |
  | (d1) 纯二次曲率（中心偏 200 px） | 3.960392e-04 | 0.000000e+00 | inf |
  | (b) tanh 星云前锋 870.7 ADU / t=100 px | 1.267658e-03 | 7.717e-07 | 1.6e3 |
  | (c) 双正弦天光场 lambda=420/260 px | 8.812157e-04 | 4.593e-04 | 1.9 |
  | 正弦 lambda=420 px、幅 15% bg（单支） | 1.511980e-03 | **3.148869e-05** | 48 |
  | tanh t=150 px | 4.698800e-03 | 1.417800e-05 | 331 |

- **成稿那两个数我都独立复现出来了**（同族不同夹具）：`7.3e-3` 由恒等式 `4g/bg` 反解得 g=3.00 ADU/px @ bg=1643.8 给 `7.300158e-03`（`v2d_selfcheck.py` (3) 段），与成稿"3 ADU/px 平滑梯度 -> 7.3e-3"自洽；`3e-5` 我在 420 px（= 仓库 `sci_c_common.smooth_sky_field` 自己的波长参数）正弦族上算得 `3.148869e-05`。
- **移植自校验**（`v2d_selfcheck.py`，证明我复算的是同一台机器而不是文档措辞）：出厂门 d 扫描恒等式复现到 6 位——纯台阶 `ratio = 1.000000`、纯梯度 `ratio = 4.000000`（该恒等式由 `docs/science/PHASE2_UPM.md:428` 独立声明）；门的确定性下限复现——`delta/L = 1.0050% => |rel_step| = 0.00999975`（绿）、`1.0051% => 0.01000074`（红），与闭式 `delta/(L+delta/2)` 逐位一致。
- **我复算出成稿没有的一条（更重要）**：判据分子是**有符号**量，梯度项与台阶**代数相加**，反号相消 => **真接缝被静默放行**（`v2c_cancel.py` + `v2c_output.txt`）。在同一条边界上叠加"出厂门实测最陡边同量级"的反号梯度（g 使 4g/bg = 7.2561e-3）：

  | 真实注入台阶 | 出厂门 rel_step | 判决 | 实验判据 rel_excess |
  |---|---|---|---|
  | 0.70% | +2.5521e-04 | 绿 | 6.33e-03 |
  | 1.005%（门自己声明的确定性下限） | -2.7799e-03（仅 27.8% 门限） | **绿** | 9.07e-03 |
  | 1.50% | -7.6863e-03 | **绿** | 1.35e-02 |
  | 2.00% | -1.2618e-02 | 红 | 1.80e-02 |

  反号梯度把门的实际漏检面上推到 **delta/L 约 1.73%**（解析：`1e-2 + 7.2561e-3`），即 `PHASE2_UPM.md:449` 声明的"delta/L > 1.005% 必判红"**只在梯度项为零时成立**；§17.3 列出的 5 条漏检面里**没有**"梯度反号相消"这一条。
- **回答工包所问（证据是否同源）**：主张"天体结构假阳性可区分"**不是**出厂门自己给的证据。全仓 `git grep 假阳性` 只命中 `实验/additive-sky-seamless/README.md:181`（N5"强平滑公共梯度下的假阳性 0.0"）与 `REPORT_paper.md:236`，那里的判据 D = **`excess`（去趋势 + off-locus）**（`code/c4_seam_criterion.py:5-16` 明文写 N5 判的是 D）；出厂门 v2 **主动移除**了 off-locus（`seam_footprint.py:36-53` 载明理由 = 修 P0-7 周期盲区），其 `REQUIRED_SELFTEST_CASES`（`:163-173`）九例中**没有**"相干梯度无台阶必须绿"这一例——S1 夹具均值面只含 `0.02*x/npix` 的整体斜坡（`make_fixture:675`、`FIXTURE_NPIX=1200`），按同一恒等式对判据的贡献 = `4*(0.02/1200)/1.0 = 6.7e-5`，即门限的 **0.67%**，不构成该负例。主张与证据**不同源、不同统计量**成立。

`与成稿差异`：

- 方向差异（实质）：成稿只写**判红方向**（"更陡的场可能把 shipped 门判红而无接缝"），未写**判绿方向**（反号梯度吞掉 1.005%-1.7% 的真台阶）。后者是静默放行，按 AGENTS §9 判据非退化口径严重性高于前者；本条判"确认"正因机制在成稿之外被独立复算加强。
- 数值精度差异：成稿写"3 ADU/px 平滑梯度…off-locus excess rel = 3e-5"。**严格线性**斜坡在实验判据下是 `step = -2.274e-13、excess = -2.274e-13`（浮点零，`v2d_selfcheck.py` (4)），故 `3e-5` 不可能来自线性梯度，必来自带曲率的平滑前锋（我在 tanh t 约 100-150 px / 正弦 420 px 族复现 1.4e-5 至 7.3e-5）。成稿未给该曲率尺度且夹具未归档，故其"同一结构上 7.3e-3 vs 3e-5"的**配对**我未逐位复现（两个数各自可复现）。
- 成稿引 `seam_footprint.py` docstring 的"M42 最陡处达门 78%"：正本 `docs/science/PHASE2_UPM.md:474/478` 给的是 `max|rel_step| = 7.2561e-03`、"最差边 = 门的 72.6%，余量仅 1.378 倍"。78% 与 72.6% 是同一处的两个写法，属**又一处口径未同步**（成稿照抄 docstring 未发现与 §17.4 打架，我按正本取 72.6%）。
- 附带发现：`PHASE2_UPM.md:499`（§17.5 复现）与 §17.4 引用的证据/脚本全在 `run/SEAM-DERIV-01/**`，而 `.gitignore:17` 为 `run/*`、`git ls-files | grep -c SEAM-DERIV = 0` => **1e-2 门限的标定证据与标定脚本均未入库**；门本身（`--self-test`）可复跑，门限推导不可复跑。

`定级建议`：**P1**（不是"实现算错"，而是**门禁判据非符号定界 + 验收主张与证据跨统计量**：出厂门读数含可正可负的梯度项，其声明的漏检面下限在真实结构场上不成立。修向按成稿"不放松门"，并把相消算例（`v2c_cancel.py`）与相干梯度负例补进 `REQUIRED_SELFTEST_CASES`，同步订正 §17.3 漏检面条目与 docstring/§17.4 的 78% vs 72.6%）

`缺什么证据（判待证时必填）`：不适用（判确认）。成稿配对数所需原夹具未归档，属补齐项，不影响本条结论方向。

---

## V3 `additive_mode` 注释与代码默认打架

`V编号`：V3（成稿对应 AUD203-10）

`判定`：**确认**（三处注释逐处判真假已独立完成；判为**注释/口径缺陷，非行为缺陷**）

`我的独立复算（数 + 命令或脚本路径）`：

- **代码与合同默认面**（先查默认，再读注释）：
  - `lib/infrastructure/scheduler/src/module_adapters.cpp:9835-9836` `seam_pre.value("additive_mode", std::string("delta"))`
  - `:10311-10312` `seam_cfg.value("additive_mode", std::string("delta"))`（同函数唯一真读点），`:10313-10316` 校验 ∈{c,delta,both} 否则 fail
  - 合同面：`eng/packaging/config/config_registry.json:1849` `"declared_default": "delta"`，`:1855` `"finding": "unregistered"`，`:1856` note「负责人 §9.67 定案 1 定默认 delta（raw−δ_k，保留公共天光面 B_ref）…但不在 defaults.json / CLI 白名单 / 任何 phase_config schema 中 ⇒ 未登记」
  ⇒ **默认面 = `delta`，两侧一致**（与前台已复核值同）。
- **三处注释原文与判词**：
  1. `module_adapters.cpp:9819-9820`「依据：① FIX-SCI-SNR-CANON-001 §2/§3.1 明文…**⇒ 生产默认 additive_mode = "c"（本文件 :5503 起），δ 从不施加**」⇒ **假**（三处皆假：默认是 delta；δ 在默认路径**确实施加**——`sub_delta = (effective=="delta"||"both")` 为真，`:10541-10549` 调 `p2_sky_plane_eval_delta_block` 逐像素减 δ；且它自称的锚点 `:5503` 处是 `mark_frame_fail` lambda，与 additive_mode 无关 = **锚点也已漂移**）。
  2. `module_adapters.cpp:9834`「§9.67 定案 1：默认 "delta"（多退少补到公共天光面，保留 B_ref）」⇒ **真**（与 :9836/:10312/config_registry/科学正本一致）。
  3. `module_adapters.cpp:10298-10300`「配置 doc["seam"]["additive_mode"] ∈ {"delta"(默认), "c", "both"}」⇒ 这半句**真**；同段 `:10300`「`c = raw − C_k`（**全减，含 B_ref ⇒ 背景被剪掉**；仅对照）」⇒ **假**。
- **"全减含 B_ref" 为什么假（我不依赖产品跑，靠 gauge 与叠加恒等式钉死）**：
  - `lib/algorithms/coverage/src/upm.cpp:976-977`（同型分支 `:928-929`）：`// 该分量参考帧 gauge：C=0（每分量独立，非全局最小帧）` + `for (k) m->C[f][k] = 0.0;` ⇒ **分量参考帧的帧行恒等于 0**（gauge 由正本 `PHASE2_UPM.md:96`「每分量参考帧 = 最小 frame_id」冻结）
  - `upm.cpp:215-218`：`evaluate_c_field(m, frame_idx, ...) = evaluate_field_row(m, m->C[frame_idx], ...)` ⇒ 求的是**该帧的行向量 `m->C[f]`**；公共面另有其位（`upm.cpp:1104` `m->controls[k].M = M[k]`）
  - `upm.cpp:1907-1942`（`p2_upm_calibrate_block`）：`output = input - c - g`，`c` = 帧分量，`g` = 末端公共 gauge（生产 `module_adapters.cpp:9579` 显式 `uc.final_gauge = 1` ⇒ `m->gauge` 非空；结构体默认才是 0，`:283/:304`）
  - `upm.cpp:1933-1934` 原文：「末端残差场 G **对全部帧施加同一扣除**……`corrected = y − C − G`；**叠加 `Σw(y−C−G)/Σw ≡ M`** ⇒ 覆盖子集突变无阶跃」⇒ mode c 的叠加产物水平面**恒等于公共面 M**，即背景按定义保留；`module_adapters.cpp:10530-10533` 进一步记「G 是对所有帧相同的公共残差场（`full_frame=1` 时 ≈0）」（生产 `:9578` 取 `m_full_frame=1`；实验量得 G ≈ 0.0037 e⁻）
  - `upm.h:163-164` 对 `p2_upm_evaluate_c` 的注释原文只有「直接求值空间校正场 `C_frame(leaf_ipix)`（sparse/dense 同一科学语义）」
  - ⇒ **两条独立理由都否定"剪背景"**：① 参考帧在 mode c 下 `c ≡ 0`（只剩一个全帧相同的 G），若 mode c 真"含 B_ref"则该帧背景必被减光——但它减的是帧分量 0；② `upm.cpp:1934` 的叠加恒等式把产物水平面钉在 `M` 上，而 `M` 就是携带天光的公共面。
  - `复算/aud203-v/v3_mode_c_algebra.py`（输出 `v3_output.txt`）按 `y_ik = M_k + C_ik` + 最小 frame_id gauge 解一遍并逐模式求值：mode c 产物水平面 = [296.0 … 308.5]（= M，含全部天光），mode delta = [295.0 … 307.5]（= B_ref + src），**只有** `raw − b_k` 臂塌到 [15 0 40 3 0 22]（= 只剩源，背景归零）。中位数：mode c **303.25 ADU** vs B_ref 294.75 ADU vs 全减臂 **9.00 ADU**。同脚本另验 §7 常量场不变量：各帧同值 300 ADU 公共输入 ⇒ `M = 300 + src`、`C_f ≡ 0`（判真）。
- **与 `y = M + C` 不变量的矛盾点（工包所问）**：正本 `docs/science/PHASE2_UPM.md` §5（`:66-68`）`calibrated_f(p) = raw_f(p) − C_f(p)`、`C_f = 双线性(8×8 control cell, θ_f)`，`:96`「每分量参考帧 = 最小 frame_id」，§7 `:129`「常数公共输入（各帧同值 raw=C）时 **M=C、C_f=0**……**不写 C_f=C**」。若"raw − C_k 含 B_ref"为真，则公共天光必须住在 `C_f` 里 ⇒ 与 §7 直接互斥（同一输入下 §7 要 `C_f=0`，产品即不减背景）；且与 §16.1.1 `:351-353` 的实测互斥：「产品 `calibrated_k = raw_k − δ_k`（保留 B_ref）产品中位 **299.17 e⁻ ≈ B_ref 297.33 e⁻**；**全减背景臂（`raw − b_k`）产品中位 0.845 e⁻**」——"全减"这个名字在正本里挂在 **`b_k`**（= `B_ref + δ_k`，`sky_plane.h:456-479`）上，不是 `C_f`。
- **该"全减"量在生产里有没有产路**：**没有**。全量求值器 `p2_sky_plane_eval` / `p2_sky_plane_eval_block`（返回 `b_k = B_ref + δ_k + gauge_shift`）在 `lib/` 与 `eng/` 生产侧**零消费者**（`git grep` 命中仅 `sky_plane.cpp` 自身定义、`eng/tests/unit/v6_p2_sky/**`、`实验/**/sky_probe.cpp`）；生产 apply 只调 `p2_upm_calibrate_block` + `p2_sky_plane_eval_delta_block`（`module_adapters.cpp:10524/10533/10544`，并原样写进 manifest 的 `entry` 字段 `:10691-10692`）。⇒ 成稿"无全减背景产路"我独立复核为真。

`与成稿差异`：

- 结论同向，但成稿把"c 模式是否真剪背景"标为**中置信 + 需跑 C++ 产品复测**（其需复测清单第 3 条）。我**不需要跑产品**即把它定死：`upm.cpp:975-979` 的参考帧 gauge 把 `C_ref` 写成恒 0，mode c 对参考帧是恒等变换 ⇒ 该条从"待复测"升为**已判**（静态即可判，符合本阶段不跑产品的裁决）。
- 成稿引 `upm.cpp:554` 的 `y=M+C`：核对为真（该行原文 `// 残差 r_ik = y_ik - M_k - C_i,k`），但它是**残差式注释**，不足以单独否定"全减"；我改用 gauge 赋值 + 求值函数体 + 全量求值器零消费者三条。
- **成稿漏一处同型缺陷**：`docs/modules/phase2_upm.md:75-76`「`c` = 全量扣除……⚠ **实现面默认仍为 `c`，属待改**，以设计为准」——既把 c 说成全量（同上为假），又断言实现默认仍是 c（与 `:9836/:10312`、`config_registry.json:1849` 直接冲突）。**这条在文档层，比代码注释更需要先改**（它是下层文档对实现现状的陈述，会被当事实读）。
- **口径根因（成稿未归纳）**：`docs/modules/phase2_upm.md:70-72` 自己立了强制规则「记法消歧（强制）：凡写 `raw − C_k` 处必须写明「全量」还是「仅偏差」」。代码注释 `:10300` 与该规则冲突且取了"全量"读法；`docs/plugins/algorithms_phase2/11_upm.md:14/37/132` 同样取"全量"读法（`C_k ≡ B_ref + δ_k`），而科学正本 §5/§7 与代码取"仅偏差"读法 ⇒ 同一符号 `C_k` 在**三层文档 + 一处代码注释**里是全量、在**正本 + 实现**里是帧分量。这不是单条注释笔误，是符号口径未收敛；只改 `:10300` 一行会留下 11_upm.md 三处。
- 行为面复核（回答"注释缺陷还是行为缺陷"）：默认 delta、`sub_c=false`、缺天光面产物时显式降级为 c 并写 `additive_mode_degraded="no_sky_plane_artifact"`（`:10318-10321`、`:10743-10747`）、`c_subtracted`/`additive_mode_requested`/`additive_mode_effective` 全落 manifest（`:10699-10700`、`:10743-10744`）⇒ **行为正确且可审计**，判**注释/文档缺陷**。唯一的真实风险是"误用作对照臂"：把 `additive_mode=c` 当论文里的"全减背景退化臂"来跑，得到的其实是另一套（仍保背景的、对齐到参考帧的）加性扣除 ⇒ 对照结论会错，但不会静默产出错产品。

`定级建议`：**P2**（注释/文档口径缺陷；行为与默认面正确、有 manifest 审计面。修法：按 `docs/modules/phase2_upm.md:70-72` 的强制消歧规则，把 `module_adapters.cpp:10300`、`11_upm.md:14/37/132`、`docs/modules/phase2_upm.md:75-76` 四处统一到正本的 `C_f = 帧分量（仅偏差）` / `b_k = 全量` 记法，并把"实现默认已为 delta"写进后者；不动码）

`缺什么证据（判待证时必填）`：不适用（判确认）。

---

## V4 `tolerance_relative` 两处取值不同

`V编号`：V4（成稿对应 AUD203-09 后半）

`判定`：**确认**

`我的独立复算（数 + 命令或脚本路径）`：

- **两处取值（读码核定）**：
  - `lib/infrastructure/scheduler/src/module_adapters.cpp:9571` `uc.tolerance = 1e-6;` + `:9573` `uc.tolerance_relative = 1;` ⇒ **相对判据**
  - `lib/phase2_session/p2_session.cpp:204` 只有 `uc.tolerance = 1e-6;`，**整段不设 `tolerance_relative`**（我把 `:199-212` 全列赋值逐字段读过：robust_loss/snr_weight_mode/huber_delta/max_iterations/tolerance/target_order/sigma_floor/zero_anchor_weight/grid/support_power/use_ivar_weight/control_reliability/cpu_workers，无该键）⇒ 取结构体默认 `upm.h:113` `int tolerance_relative = 0;` ⇒ **绝对判据**
- **在哪一层**：定义层 = `upm.h:78-80`（语义）+ `:113`（默认 0）；**赋值层 = 两个装配点**（适配器 / session）；消费层 = `upm.cpp:1036-1040`（唯一消费处：`if (cfg.tolerance_relative) { s_eff = max(scale_obs,1.0); tol_M = tol_C = tolerance*s_eff; }`），并写入 `upm.cpp:1409 j["tolerance_relative"]` 持久化。⇒ 不是"配置文件两侧默认不同"，是**装配点一处显式设、另一处漏设回落默认**。
- **生产路径实际取到哪个**：**相对（=1）**。三条独立依据：① CLI 的 mosaic/block 路径走 Runtime phase-IR 子图（`lib/infrastructure/cli/commands.cpp:1195` 注释「RT-008: phase2 走 Runtime 单 phase IR 子图」，实际执行 = `run_with_resource_gate(ev,"phase2",…)` `:1226`）；② `commands.cpp:1219-1221` 原文「**CLI-002 入口迁移后 p2_session 旧通道不在生产调用面上**」；③ `eng/ci/ledgers/registry_ir_parity.json:9`：包 `p2_session_run` 的 `astrocs.phase2.resample` 节点**永不被调度**，且该形态违反 `ENGINEERING_SPEC.md §4` 第 4 条「不隐藏整阶段 Session」（ARCH-DEBT-01 已登记）。
- **是否构成 fail-open（工包所问）**：**不构成**。方向是偏红/偏保守：绝对判据在生产尺度上永不满足 ⇒ 产物落 `converged ∈ {0,2}` ⇒ `upm.cpp:1884 upm_converged_warning = (m->converged != 1)`、`module_adapters.cpp:9768` 打 WARNING 并写 `warning_codes=P2-UPM-NOT-CONVERGED`（rc 不变但显式登记，`:9762` 注释「下游与 CI 按 warning_codes 判定」）。
  但有一处**真的多侧不对称**：适配器侧接受配置覆盖（`:9650-9652` 读 `upm.tolerance` / `upm.tolerance_relative`），**session 侧的覆盖块（`:212-218`）只读 max_iterations/huber_delta/smoothing_lambda**，且 `tolerance_relative` 在全仓任何 JSON 配置/schema/defaults 里**零登记**（`git grep` 于 `*.json` 无命中）⇒ 若有人给 session 传 `upm.tolerance_relative`，会被**静默忽略**。这是"键可达但不生效"形态，属缺陷但不在生产路径。
- **它影响收敛判定还是只影响日志/元数据**：**影响收敛判定本身**。`tolerance_relative` 决定 `upm.cpp:1050` 步长判据的阈值 ⇒ 决定 IRLS 何时 break、决定 `m->converged` 落哪个态（`:1228` 出参、`:1414` 持久化、`:1837` 写警告、manifest `:9800 upm_converged` / `:9803 upm_tolerance_relative`）。日志/元数据只是附带面。
- **数（`复算/aud203-v/v4b_tolerance_scales.py` + `v4b_output.txt`）**：按正本给的尺度 `1210 ADU/px ÷ Ω_px 2.2991e-11 sr`（`docs/science/PHASE2_UPM.md:111-112`）算 `scale_obs = 5.262929e+13`：
  - 该尺度上的**最小可表示步长** = `ulp = 1.168605e-02`；
  - **绝对判据**阈值 1e-6 = **0.8557 倍 ulp 的千分之一**（阈值/ulp = 8.557e-05，即阈值比一个 ulp 还小 **1.17e4 倍**）⇒ 判据 `max_dM < 1e-6` **原理上不可达** ⇒ 该路径的终态只能是 2（stall 计数器赢）或 0（迭代耗尽，`max_iterations=100 ≥ patience 5`）；
  - **相对判据**阈值 = `1e-6 × 5.262929e13 = 5.262929e7`（折回像素单位 = **1.210e-3 ADU/px**）= 4.5e9 个 ulp ⇒ 可达；
  - 近零尺度侧（`scale_obs = 0.5`）由 `max(scale_obs,1.0)` 退回绝对 1e-6，与 legacy 逐位等价（合成算例不受影响）。
- **规范判定**：这不是"两种都可接受的选择"——正本 `docs/science/PHASE2_UPM.md:98` 标题行即「容差随观测尺度归一，**禁绝对容差**」、`:114-115`「绝对 1e-6 比该尺度上的 ULP 严若干数量级、原理上不可达 ⇒ **生产必须 tolerance_relative=1**」。⇒ `p2_session.cpp:204` 的 0 值是**对 SCI-UPM-CONV-001 的直接违反**（成稿引"设计 §5 明文"，我核对为真，行号 = :114-115）。
- **登记状态**：`docs/algorithms/PHASE2_UPM_IMPL.md:441 DISP-P2UPM-006`（状态"待裁决"）+ `docs/science/PHASE2_UPM.md:378` §16.3（"两入口口径不一致（待统一）"）——**两处登记锚点均已漂移**（写 `module_adapters.cpp:7808-7813`，现实际在 `:9571/:9573`；实验报告自己也在 `REPORT_paper.md:504` 登记过 `:5941-5942 / :5948-5949 / :5989-5994` 三个历史锚点）。

`与成稿差异`：

- 数值与结论一致，成稿"绝对 1e-6 低于 ULP 若干量级、原理不可达；走 p2_session 的帧永不收敛（converged=0/2）"我复算为 `阈值 = 8.557e-5 × ulp`（即比一个 ulp 小 1.17e4 倍），并把"0/2"里的**机理次序**补上：`upm.cpp:1041-1063` 是先判 1、后累加 stall 计数，故绝对判据下**只能**落 2 或 0，不会落 1。
- 成稿说"设计 §5 明文生产必须 =1"——核对为真（:114-115）；但成稿未指出**同一句还带"禁绝对容差"四字**（:98），使 session 侧不只是"不一致"而是"违禁"，这是定级依据的差别。
- **成稿漏两项**：① session 侧**不读** `upm.tolerance/tolerance_relative` 覆盖键（适配器读）⇒ 键在一条路上生效、另一条路上静默失效；② 该键在 defaults.json / CLI 白名单 / phase_config schema **全无登记**（同 `config_registry.json:1856` 对 additive_mode 的说法），故"改哪一侧"目前没有契约面可依。
- 成稿"改法：`p2_session.cpp:204` 段补 `uc.tolerance_relative=1`"我判方向正确但**不是最小正确修法**：该路径按 `ENGINEERING_SPEC §4.4` 与 ARCH-DEBT-01 本应整体下线（永不被调度的整阶段 Session），给违禁路径补配置等于巩固它。建议：要么随 ARCH-DEBT-01 删除，要么与适配器共用同一冻结装配函数（一处定义两处用），二者都比补一行更合规范。

`定级建议`：**P2**（生产路径取的是合规的一侧 ⇒ 不是活的缺陷；但它是"装配点漏设回落默认"这一族实例，且落在**永不调度但仍在树**的 session 通道上：一旦 ARCH-DEBT-01 处置时被复活，它产出的每一个模型都会带 `P2-UPM-NOT-CONVERGED`。修向 = 删除该通道或统一装配函数 + 把键登记进契约面）

`缺什么证据（判待证时必填）`：不适用（判确认）。

---

## V5 `stalled=2` 无归档 B 级证据

`V编号`：V5（成稿对应 AUD203-09 + 其"需复测清单"第 1 条 + 第 217 行自我修正）

`判定`：**确认**（属**证据缺口**，不属代码缺陷）

`我的独立复算（数 + 命令或脚本路径）`：

- **代码四态确实都可赋且 2 与 1 不混**（逐行读，不采信登记）：`upm.cpp:113 int converged{0}`（0 = 循环自然耗尽 = max_iter）、`:1047 →3`（objective 非有限）、`:1051 →1`（`max_dM < tol_M ∧ max_dC < tol_C`）、`:1061 →2`（`stall_run >= kStallPatience`）；`kStallPatience = 5`、`kObjImproveFloor = 1e-12` 在 `:732-733`。出口面：`:1893-1903` 访问器原样回、`:1414` 持久化、`:1837` 警告携带值、`:1884 upm_converged_warning = (converged != 1)`、`module_adapters.cpp:9768` 非 1 打 WARNING。⇒ **stalled 不会被当 converged**，此点成稿判"成立"，我复核为真。
- **归档取值分布穷举**（`复算/aud203-v/v5_converged_census.py` + `v5_output.txt`；语料 = 跟踪集 3896 文件，判"存在与否"只用 `git ls-files`/`git grep`）：命中 `converged/stalled` 键值对 **59** 处，按性质分：
  - **结果/证据件（22 处）取值分布 `{0: 6, 1: 12, 2: 4}`**，逐文件：`实验/additive-sky-seamless/results/c1_additive.json {0:3, 1:5}`、`c2_multiplicative.json {1:3}`、`c5_weights.json {1:4, 2:2}`、`results/REVIEW.md {0:2, 2:2}`、`artifacts/evidence/governance-01/research/R-2…md {0:1}`；
  - 那 **4 个"2" 全部不是观测值**：`REVIEW.md:101/171` 是叙述（「全仓无 converged=2/3 分支」）；`c5_weights.json:107` 在 `"convergence_enum_spec"` 里（图例：`{"converged":1,"max_iter":0,"stalled":2,"invalid":3}`），`:186` 在同名门 `W4_convergence_enum_spec` 的 `value` 里，且该门**自带 `"level": "meta"`**、desc 原文「规范转写的 0/1/2/3 状态机四例自洽（**meta：非实现对拍**）」；
  - 代码/文档层另有 `{2:…, 3:…}` 命中，全部是**枚举定义与判据叙述**（`upm.cpp:117/1047/1061`、`PHASE2_UPM.md:105`、`PHASE2_UPM_IMPL.md:411/413/526`）。
  ⇒ **跟踪结果件中，观测到的收敛态只有 {0, 1}；2 与 3 从未作为实测量出现过。**
- **"stalled 臂"当年测到的其实是 0**：`c5_weights.json` 的 `production_convergence = {"converged_case":1, "maxiter_case":0, "stalled_case":0, "iters":[4,1,300]}` ⇒ 被叫作 stalled 的那一臂 `iters=300` = 迭代耗尽（state 0），不是"5 轮低改善"的真 stall（`REVIEW.md:101` 同判，我以 JSON 原值独立复核）。
- **旧数据产自二态码——有序号可证的先后**：`git log -S "m->converged = 2" -- lib/algorithms/coverage/src/upm.cpp` ⇒ **`d77fd11f` 2026-09-22**「SCI-502：UPM 三个生产缺陷修复（相对容差 / 四态收敛 / 近奇异自适应）」；而 `c1_additive.json` / `c2_multiplicative.json` / `c5_weights.json` 的最后写入提交均为 **`9a2b5d11` 2026-09-21** ⇒ 归档数字**早于四态枚举一天**，其时二进制只能出 0/1。成稿"旧数据是在只有 0/1 的二进制上跑的"确认。
- **没有任何生产侧归档件记录过该字段**：`git grep -l upm_converged` 命中仅代码/头文件/文档 + `REVIEW.md`；`实验/m42-realdata/**` 中 `converg` 零命中 ⇒ 端到端产品的 manifest 里没有一条收敛态读数被跟踪（产品在 `run/`，`.gitignore:17 run/*`）。
- **并且今天也没有测试把它逼出来**：`stall_patience/floor` 是 `upm.cpp:732-733` 的**函数体内 `const`**（非配置面、非 `P2UpmBuildConfig` 字段）⇒ 无法用配置把某算例逼进 2；现有断言只覆盖 1/0（`eng/tests/backend/test_p2003_seam_oracle.py:257` 断言 `converged==1`；`lib/algorithms/coverage/tests/synthetic_gate.cpp:5950/5959/5969/5981` 做 legacy(0) vs relative(1) 对拍；`:5901 EXPECT_EQ(def.tolerance_relative, 0)`）。
- **该自认在实验报告里已存在**（不是我发现的新事实，但登记面在实验单元而非门禁）：`实验/additive-sky-seamless/REPORT_paper.md:29`「**但**：c1–c7 归档结果**未在修复后复跑**，且**状态 2/3 与 κ 自适应路径无专项测试**」、`:305`「C5 W5 的 stalled 臂在新四态枚举下**可能由 0 变 2，属未复跑项**」。⇒ 属"已如实登记的证据缺口"，缺的是**取证动作**，不是缺失的规范。

`与成稿差异`：

- 结论一致（成稿第 217 行已把"W5 验证了四态"的自我证伪写进去，并列入需复测第 1 条）。我补三点成稿没有的：① 取值分布的**穷举表**（22 处结果件命中、4 个"2"逐条归为图例/叙述，判"从未观测"是可复核而非印象）；② **日期/SHA 序**（d77fd11f vs 9a2b5d11）把"旧数据跑在二态码上"钉成机器事实；③ **不可达性机理**——结合 V4 的判据次序（先判 1 后判 2），生产相对判据下 `steps ≥ 1e-6·scale` 与「目标相对改善 < 1e-12 连续 5 轮」在生产尺度上互相排斥，而唯一必然落 2 的配置恰是**绝对判据那条（永不调度的 session 路）**（`v4b_output.txt`）⇒ 状态 2 在**当前生产判据下经验可达性未被任何跟踪件证明，且按数值分析接近不可达**。这使本条不止是"少跑一次"，还牵出一个待答问题：2 这个态是否要保留（若保留须给可驱动的夹具面，即把 patience/floor 上抬到配置或提供注入式收敛失败夹具）。
- 定性：成稿与 §16.3 表格 `:379` 对该项写"已满足"——我核"已满足"只覆盖**实现面主张**（访问器暴露 + 写入数据面），不覆盖经验可达性，两者不冲突；但同一行的锚点（`upm.cpp:1028-1050 / :1577-1589`、`module_adapters.cpp:7928-7935`）已全部漂移（实际 `:1041-1061 / :1893-1903`、`:9690-9734/:9800`），登记件按锚点复查会扑空——与 AUD203-12（行号漂移）同族。

`定级建议`：**P2（证据缺口，非代码缺陷）**。修法：给 stalled 分支一个可驱动的夹具（把 `stall_patience`/`kObjImproveFloor` 提升为 `P2UpmBuildConfig` 字段或加测试钩子），在 `synthetic_gate` 里出正/负例各一（正例=构造 plateau 逼出 2；负例=正常收敛必须不是 2），并把 SCI-C 的 C5 臂在 `d77fd11f` 之后复跑一次归档；同期把 §16.3 `:379` 的主张措辞限定为"实现面已满足、经验可达性未取证"。

`缺什么证据（判待证时必填）`：不适用（判确认）。所缺即本条内容本身：**一份 `converged=2` 的实测记录**（夹具级即可，不需端到端）+ 复跑后的 c5 结果件。

---

## 本批汇总：进 P0/P1 的条目（只列我判"确认"的）

5 条全部判 **确认**（机制与数值均经我独立复算，成稿无被推翻或降级项）。**按我的定级建议，进 P1 的是 2 条，P2 的是 3 条**：

| 条 | 判定 | 定级 | 一句话（问题／谁修／怎么算修好） |
|---|---|---|---|
| V1 常数天光块 control_ivar 不归零 | 确认 | **P1** | 采样侧 1e-12 数值保护量把"零方差"伪装成 7.609e-27 的有限方差、逆方差 1.314e26，绕过下游 rc=2 的 fail-closed 门并独取该 cell 权重；改 sampler（含 `sky_plane.cpp` 同款第二处），修好判据 = 常数 patch 上 `control_ivar == 0` 且 `control_variance` 非有限 |
| V2 出厂接缝门与验收证据不同统计量 | 确认 | **P1** | 出厂门判据含可正可负的法向梯度项，相干结构读数可达门限 72.6%，反号梯度可把 1.005%–1.7% 的真台阶静默判绿；"结构假阳性可区分"的证据属另一统计量（实验 off-locus excess）；改门自检用例 + §17.3 漏检面条目，不动门限 |
| V3 additive_mode 注释打架 | 确认 | P2 | 三处注释里 `:9820`（默认 c、δ 从不施加）与 `:10300`（c = 全减含 B_ref）为假、`:9834` 为真；另有成稿漏的第四处 `docs/modules/phase2_upm.md:75-76`；行为正确 ⇒ 纯注释/符号口径缺陷，按正本 `C_f = 仅偏差`／`b_k = 全量` 统一四处措辞 |
| V4 tolerance_relative 两处不同 | 确认 | P2 | 生产侧=1（合规），永不调度的 session 侧漏设回落 0（违正本"禁绝对容差"）；影响的是收敛判定本身而非仅日志；不构成活的 fail-open，但存在"覆盖键在一条路上静默失效"的不对称；修 = 随 ARCH-DEBT-01 下线该通道或统一冻结装配 |
| V5 stalled=2 无归档证据 | 确认 | P2（证据缺口） | 跟踪结果件里观测到的收敛态只有 {0,1}，2/3 从未实测出现，且四态码比归档数据晚一天落地；不是代码缺陷；修 = 加可驱动的 stall 夹具（正/负例）+ 复跑 c5 归档 |

**复核纪律自证**：5 条均为"先自行从源码/归档件复算 → 得数 → 再切成稿对应小节比对"；成稿读取按小节切片（`sed -n '44,54p' / '79,91p' / '117,145p'`），未整篇预读。本批**无推翻、无降级**。

**收工 git 状态核对**（与开工同命令、同基线 `c8f64e9a`）：

- 开工原文：`?? ACSD整治工作包_AUDIT-06.zip` / `?? site/`（两行）
- 收工原文（`git -c core.quotePath=false status --porcelain`，两行，与开工**逐字相同**）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

- 差异 = **零新增、零删除**；HEAD 仍 `c8f64e9a`。首次收工调用未带 `core.quotePath=false`，同一条 zip 记录被转成八进制转义显示（`?? "ACSD\346\225\264..."`），属显示口径非内容差异，已按工包纪律以跟踪集/引号关闭口径复核。
- 零写入自证：全程只跑 `status/rev-parse/ls-files/log -S/grep/sed/head`；未构建、未 ctest、未跑 `eng/**` 检查器、未跑端到端；未 `import` 仓库内 Python 模块。我的脚本全部落在 `独立审计/复算件/aud203-v/`（该目录内无 `__pycache__`，`PYTHONDONTWRITEBYTECODE=1` + `python -B` 双保险）。
- 主机残留说明（**不算仓库缺陷**）：`eng/**`、`docs/algorithms/anchors/`、`BASS DR3/tools/` 等处的 `__pycache__` 目录 mtime 为 2026-09-24/25 上午（早于本复核开工时刻），且被 `.gitignore:83-84` 覆盖，故不显现在 `status --porcelain` 中；本节点未新增任何此类项。

<!-- PROGRESS: 5/5 -->


