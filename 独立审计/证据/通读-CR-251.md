# 通读 CR-251 · `eng/packaging/config/filters.json` 第 1–1500 行（按行段分片）——只读代码通读审计

- 被审基线 HEAD：`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 批次：CR-251
- 派单：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 全条已读，作硬约束）＋本批特别说明（按行段分片、每约 250 行落一次盘、出厂配置面逐条处置）
- 独占范围：`eng/packaging/config/filters.json` 的 **1–1500 行**（全文件 14950 行、拆 10 段）
- 跨段纪律：凡结论须读完全部 10 段才能下的，本稿一律写"须与 CR-251…CR-260 合并定案"，不单方定案。
- 批次清单与派单一致性：`独立审计/批次清单/CR-251.txt` 首行内容 = `eng/packaging/config/filters.json:1-1500`，与派单**一致**，无差异需登记。

## 开工 `git status --porcelain` 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（实取自本节点第一条仓库命令，`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`；与派单声明的基线两项一致。`git rev-parse HEAD` = `c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`。）

## 独占清单（6 格，每格约 250 行）

1. `eng/packaging/config/filters.json:1-250` —— 已读：是（表 17 行／覆盖 96 个数值项，findings 6 条）
2. `eng/packaging/config/filters.json:251-500` —— 已读：是（表 15 行／覆盖 112 个数值项，findings 3 条）
3. `eng/packaging/config/filters.json:501-750` —— 已读：是（表 16 行／覆盖 112 个数值项，findings 3 条）
4. `eng/packaging/config/filters.json:751-1000` —— 已读：是（表 12 行／覆盖 157 个数值项，findings 3 条）
5. `eng/packaging/config/filters.json:1001-1250` —— 已读：是（表 5 行／覆盖 236 个数值项，findings 0 条：无可辩护新指控）
6. `eng/packaging/config/filters.json:1251-1500` —— 已读：是（表 5 行／覆盖 239 个数值项，findings 0 条新案；CR-251-05 的本方数组证据落在 :1312-1378）

<!-- PROGRESS: 6/6 -->

---

# 格 1 · `eng/packaging/config/filters.json:1-250`

本段内容：文件头（schema 标识／`task`／`authority` 两条 § 指针／`transcription` 定位符与哈希／`provenance` 头部与 GAP 登记）＋ `provenance.per_filter` 前 12 个滤镜条目（各 8 个派生统计量）。

## §2.5 数值处置表（格 1，17 行；本段数值项共 96 个滤镜统计量 + 5 个头部数/标识）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :2 | `filters_schema` | `astrocs.filter-library/v1` | 本合同卡的 schema 标识串 | 结构性不适用 | 版本标识符不承载科学语义；同值他侧：`docs/contracts/CONFIG_CONTRACT.md:102` 逐字同一串（唯一他侧，检索式 `git grep -n "filter-library/v1"`） | — | CONFIRMED |
| :3 | `library_id` | `config.filters` | 库 ID 标签 | 待确认 | 全仓仅本文件出现（检索式 `git grep -Fn "config.filters"` 除本行外零命中），无第二侧可比、无条款定义其取值 ⇒ 它是一个**无人消费的自述标签** | 保守方向＝保留但补登记点；影响范围＝仅本文件自述面（若有门按 library_id 定位本库则会落空，本批未找到这样的读取方） | PARTIAL（未穷尽"是否有代码按 library_id 索引配置卡"） |
| :4 | `task` | `CFG-001` | 建立本文件的任务编号 | 待确认 | 见 CR-251-02：任务包 `工程控制/PROJECT-GOVERNANCE-01/tasks/CFG-001.md` 已在本基线删除（`git log --diff-filter=D` 命中 `ae0f1e63`），跟踪面 CFG-001 仅存于引用处 | 保守方向＝把编号改指**现行**跟踪面权威（`docs/contracts/CONFIG_CONTRACT.md` §4）；影响范围＝本文件全部"任务编号锚"句的可复核性 | CONFIRMED（删除历史已核） |
| :6 | `authority.design` | `ASTROCS_DESIGN.md §3.3（…滤镜库…）` | 最高设计权威锚 | 待确认 | 见 CR-251-01：§3.3 真身是「科学量与星表」（`ASTROCS_DESIGN.md:237`），滤镜库条款实在 §4.3（:306、:312） | 保守方向＝改指 §4.3；影响范围＝任何按该锚回查权威的人/门会读到不含滤镜条款的小节 | CONFIRMED |
| :7 | `authority.preflight` | `ASTROCS_DESIGN.md §3.5（…-y 亦不可越过）` | 预检权威锚 | 待确认 | 见 CR-251-01：§3 只有 3.1/3.2/3.3，**§3.5 不存在**；被引内容实在 §4.5（:347 图节点、:355 红级 error 含"未知滤镜"、:356 "-y/-yes 不能越过"） | 保守方向＝改指 §4.5；影响范围＝同上，且该锚是"未知滤镜必须阻断"这一对外主张的唯一权威指针 | CONFIRMED |
| :11 | `transcription.source_sha256` | `76e919a0…cc5dcc` | 转录源内容冻结哈希 | 公式导出（哈希非科学量，按"定义式复算"处置） | 复算式 `sha256(lib/algorithms/photometry/data/response_curves/filters.json)`；本节点实算 = `76e919a0fd3745db1daa423e227ce4b7e5e4cfe1c5800e195ff0fed340cc5dcc` **逐字相等**；消费方 `eng/tests/config/test_cfg001_contracts.py:264`（源变即判红） | — | CONFIRMED（本节点独立算过） |
| :12 | `transcription.source_bytes` | `183763` | 转录源字节数 | 公式导出（观测量定义：`os.path.getsize(源)`） | 实算 `183763` 相等；消费方 `test_cfg001_contracts.py:265` | — | CONFIRMED |
| :14 | `transcription.mode` | `verbatim` | 转录方式声明 | 结构性不适用（声明枚举） | 逐字段复算 45 条：`{name,channel,wavelength_nm,value,n_points}` 与源**全等**，唯一差异是源上 `Astronomik UV-IR Block L-2` 多出 `default` 键（见 CR-251-06）；门 `test_cfg001_contracts.py:270-276` | — | CONFIRMED（45 条全复算） |
| :15-19 | `transcribed_fields` | `name,channel,wavelength_nm,value,n_points` | 转录字段闭包 | 结构性不适用（字段集合） | 与门 `test_each_filter_record_has_only_transcribed_fields`（:280）互钉：多列即判红 ⇒ 源侧 `default` 键**结构上不可能**被转录（CR-251-06 的根因） | — | CONFIRMED |
| :21 | `transcription.filter_count` | `45` | 声明的滤镜条数 | 结构性不适用（枚举基数） | 实算 `len(filters)=len(per_filter)=len(源)=45` 三者与声明全等；登记侧 `config_registry.json#filter_name_policy.library_facts.keys=45`；门 `check_cfg002_registry.py:526-527` | — | CONFIRMED |
| :22 | `transcription.statement` 末句 | `…test_filters_transcription_is_verbatim` | 指向"可复跑断言"的方法名 | 待确认 | 见 CR-251-04：`git grep -Fn "test_filters_transcription_is_verbatim"` 全仓**仅命中本行**；真实方法名 = `TestFiltersLibrary::test_verbatim_transcription`（`eng/tests/config/test_cfg001_contracts.py:270`），CONFIG_CONTRACT:104 用的是真名 | 保守方向＝把本行改指真名（不动判据）；影响范围＝按此句去找复跑件的人/文档门会找不到入口 | CONFIRMED（含控制样本的 `-F` 复验） |
| :25 | `provenance.source_path` | `lib/algorithms/photometry/cpp/test/filter_qe_provenance.json` | provenance 定位符 | 待确认 | 文件在跟踪面存在（`git ls-files --error-unmatch` 命中）；本批未逐字段核其与 45 条曲线的对应关系（属"六侧"之一，须与 CR-251…CR-260 合并定案）；注意该源位于 `cpp/test/` 目录——**出厂配置面的溯源锚挂在测试目录** | 保守方向＝溯源件应与科学数据同址（`lib/algorithms/photometry/data/**`）；影响范围＝测试目录被清理/重构时溯源锚蒸发（与 ㉜"清理炸锚"同型） | PARTIAL（未逐字段核该件内容） |
| :26 | `provenance.source_sha256` | `e0a44393…c4fe` | 溯源件内容冻结 | 公式导出（定义式 `sha256(该件)`） | 实算逐字相等；消费方 `test_cfg001_contracts.py:266-267` | — | CONFIRMED |
| :27-31 | `status=unverified`／`source_statement`／`gap_id=GAP-025`／`note`／`next_task` | 见原文 | 出处状态与缺口去向 | 待确认 | 见 CR-251-03：`GAP-025` 在跟踪面无定义位（`docs/KNOWN_LIMITATIONS.md`、`docs/algorithms/anchors/unresolved_registry.json` 内 `GAP-` 零命中；`工程控制/RELEASE-05/GAP_AUDIT.md` 内 `GAP-[0-9]{3}` 零命中）；`next_task` 所指"下一轮工程包"当前不存在（跟踪面 `工程控制/` 只剩 `RELEASE-05`） | 保守方向＝缺口须落在跟踪面台账（不是控制包目录名）；影响范围＝45/45 条曲线的出处主张全部依赖这条无台账编号 | CONFIRMED（三处台账各一次检索＋控制包一次检索） |
| :33,:41-48 | `per_filter["Antlia V Pro Series B"].curve_stats` | n=53, wl 420.0–524.0, val 0.002–0.978, wl_sum 25016.0, val_sum 37.243, val_sumsq 34.158384999999996 | 该曲线的自述包络＋逐元素指纹（身份核对第③④项） | 公式导出 | 式：`n=len(wl)`；`wl_min/wl_max=min/max(wl)`；`val_min/val_max=min/max(value)`；`wl_sum=Σwl_i`；`val_sum=Σv_i`；`val_sumsq=Σv_i²`。代码是否真按式算：**是**——运行期由数组现算再比对（`lib/algorithms/photometry/cpp/src/filter_curve_json.h:312-341`，不一致 → `provenance_curve_stats_mismatch`）；本节点对全部 45 条独立重算，8 项全等（含 `2 nm` 等差网格 `Σ=53×(420+524)/2=25016` 手算复核） | — | CONFIRMED |
| :51,:59-66 / :69,:77-84 / :105,:113-120 / :123,:131-138 / :159,:167-174 / :177,:185-192 / :195,:203-210 / :213,:221-228 / :231,:239-246 | 同族 `curve_stats`（Antlia G／Antlia R／Astrodon E-B／E-G／E-R／I-G／I-R／Deep Sky B／G／R，共 10 条 × 8 值 = 80 项） | G: 53/480.0–584.0/0.001–0.982/28196.0/34.699/31.963722999999998；R: 64/594.0–720.0/0.0–0.988/42048.0/35.592/33.05661；E-B: 67/386.0–518.0/0.01–0.99/30284.0/54.652/51.01658；E-G: 55/492.0–600.0/0.01–0.98/30030.0/31.226/25.401206；E-R: 88/576.0–750.0/0.007–0.98/58344.0/31.867/24.060075；I-G: 62/482.0–604.0/0.007–0.99/33666.0/40.115/35.740536999999996；I-R: 64/582.0–708.0/0.01–0.99/41280.0/52.878/50.71978；DS-B: 54/416.0–522.0/0.0–0.969/25326.0/41.728/38.904618；DS-G: 50/484.0–582.0/0.0–0.98/26650.0/33.094/30.148274；DS-R: 60/586.0–704.0/0.003–0.972/38700.0/40.558/36.653596 | 同上（各条曲线的包络＋指纹） | 公式导出 | 同上导出式；本节点独立重算 45/45 全等；`val_sumsq` 尾数带浮点噪声（如 `31.963722999999998`）⇒ 证明确为程序逐元素累加、非手填 | — | CONFIRMED |
| :87,:95-102 **与** :141,:149-156 | `Astrodon E-series B` 与 `Astrodon I-series B` 的 `curve_stats` | 两个条目的 8 个值**逐项全等**：67 / 386.0 / 518.0 / 0.01 / 0.99 / 30284.0 / 54.652 / 51.01658 | 两个不同商业型号名各自的通带指纹 | 待确认 | 见 CR-251-05：不是"两值抄同一数"，而是**两条曲线本身逐字节相同**（`filters["Astrodon E-series B"]` 与 `["Astrodon I-series B"]` 的 `channel/wavelength_nm/value` 全等，本节点整文件解析复算）；源头 `lib/.../response_curves/filters.json` 里同样相同 ⇒ 属"忠实转录了一条错配/重复的源数据"；`docs/science/PHOTOMETRY.md` §2a.1 规定 `T(λ)` 来源 = 本库曲线，§2a.7 的四项身份核对对本案**全部通过** | 保守方向＝在出处补齐前，二者之一必须从库里摘牌或显式标"与另一型号共用曲线（未证实）"，不得让两个型号各自声称有自己的 `T(λ)`；影响范围＝选这两条型号的帧的 `F_syn` → `k_photo`/`zero_point_mag`/`sigma_residual`（创新点一的可比性主张） | CONFIRMED（曲线数组与统计量双重复算） |

（本段另含 12×`verified: false`（:38,:56,:74,:92,:110,:128,:146,:164,:182,:200,:218,:236）、12×`url:null`、12×`retrieved:null`、12×`instrument:null`、12×`gap_id:"GAP-025"`——布尔/空值不是数值项，但作为"六侧同值"的键在 CR-251-03 与本表 :27-31 行一并处置，`verified:false` 与登记侧 `CONFIG_CONTRACT.md:105`「45/45 如实标注」全等，复算一致。）

## findings（格 1）

### CR-251-01 · 出厂滤镜库把"未知滤镜必须阻断"的权威锚指向最高设计里不含该条款的小节，其中一个（§3.5）根本不存在
- 轴：C（主轴）/ D
- 位点：`:6` `"design": "ASTROCS_DESIGN.md §3.3（eng/packaging/config/filters.json 滤镜库：型号、通带、波长等；滤镜型号必须与滤镜库匹配，未知滤镜 -> error）"`；`:7` `"preflight": "ASTROCS_DESIGN.md §3.5（滤镜不匹配/未知滤镜 -> 红色 error，强制阻断，-y 亦不可越过）"`
- 上位依据：`ASTROCS_DESIGN.md:237` 实为 `### 3.3 科学量与星表`，全节（:237-241）讲"每个科学量写清五件事／精度归属按数据形态／HEALPix 索引"，**零提滤镜、零提 `filters.json`**；§3 之下只有 3.1/3.2/3.3 三个子节（`git grep -n "^### 3\." -- ASTROCS_DESIGN.md` → 3.1/3.2/3.3，无 3.4/3.5）。被引内容的真身：`:306`「滤镜型号与 `eng/packaging/config/filters.json` 逐字匹配（精确、区分大小写），未知滤镜报 error；空值表示显式无滤镜」（属 §4.3）、`:312`「├── filters.json 滤镜库：型号、通带、波长」（同节配置树）、`:355`「🔴 error：文件找不到、路径错误、未知滤镜…」（§4.5）、`:356`「存在 error 时 `-y` / `-yes` 不能越过」。
- 现状：出厂配置卡的两条 `authority` 串都用 §3.x 编号；同一条款的**其余两侧却用对号**——`docs/contracts/CONFIG_CONTRACT.md:106` 写「`ASTROCS_DESIGN.md` §4.3 滤镜型号逐字匹配、§4.5 红级 error」，本文件 `:869` 的 `lookup.note` 自己也写「ASTROCS_DESIGN.md §4.3 … 与 §4.5 …」；三份 schema 的 description（`eng/contracts/schemas/phase_config_{normalize,mosaic,export}.schema.json:84/117/141`）与本文件 `:6` 同错（写 §3.3，mosaic/export 还多挂 `/:144`）。
- 差在哪：同一句话的权威锚在**同一文件内前后两套编号**（:6/:7 用 §3.x，:869 用 §4.x），且 §3.5 这个小节按跟踪面判定不存在；`/:144` 那半个锚落在 §2.2 噪声口径正文（讲 `g' = 1/g` 的约定），不含任何滤镜串。
- 后果：任何人或文档门按 `authority` 串回查权威即读到不含该条款的小节 ⇒ "未知滤镜必须红级阻断、`-y` 不可越过"这条对外承诺在本卡的权威可追溯性不成立；错号与对号只差一位，极易被后续批次当作已核（`docs/DOCUMENT_INDEX.yaml` 与合同面用的是对号，会反向"印证"）。
- 定级：S2 — 内容本身与 §4.3/§4.5 逐字对得上，不会直接改产物字节；坏的是权威链与按锚核验的机器面。
- 怎么算修好：`:6`→`§4.3`、`:7`→`§4.5`（删 `/:144` 类行号锚，改用节内可判定的短语锚）；**同批**必须一起改 `eng/contracts/schemas/phase_config_*.schema.json` 三处 description 与 `config_registry.json:7`（该处同样写 §3.3），否则按名比对的文档门（`eng/tests/quality/test_doc_machine_check.py`）可能因两侧编号同步变化而行为翻转。
- 置信：CONFIRMED（节号与逐字文本均本节点 `git grep`/`sed` 自取）

### CR-251-02 · `task: "CFG-001"` 的任务包已在本基线删除，跟踪面不存在任何定义 CFG-001 的载体
- 轴：C
- 位点：`:4` `"task": "CFG-001"`
- 上位依据：`docs/contracts/CONFIG_CONTRACT.md:5`「ID: CFG-001 状态: 建立（2026-09-16） 任务: `工程控制/PROJECT-GOVERNANCE-01/tasks/CFG-001.md`」——合同自己声明任务定义体在控制包内；`ENGINEERING_SPEC`/AGENTS §7 规定控制包收口后按 `CONTROL_PACK_SPEC §9` 清理。
- 现状：跟踪面 `git ls-files 工程控制` 只剩 `RELEASE-05/**`；删除历史命中 `ae0f1e63 chore(governance): 清理历史控制包与一次性报告（CLEAN-402）`，被删清单含 `工程控制/PROJECT-GOVERNANCE-01/00_README.md … tasks/CFG-001.md` 整包。CFG-001 现仅以**引用**形态存在于 `docs/contracts/CONFIG_CONTRACT.md:5,176,182,186`、`docs/contracts/UNIFIED_OBJECTS.md:147,153-155`、`docs/contracts/config_separation_anchors.json:9,11`、`docs/ci/01_CHECKS.md:55-56` 与本文件 `:4`。
- 差在哪：按 §1"判不存在以跟踪集＋删除历史为准"，该编号的定义载体真消失了——不是"本机没看到"。本卡的建立依据、未决项清单与收口结论（CONFIG_CONTRACT §8 表整节都在回收"CFG-001 未决项"）现在都锚在一个不存在的文件上。
- 后果：审计/复核面无法回查"CFG-001 当时承诺了什么、哪些已收口"；`CONFIG_CONTRACT.md:176`「§8 CFG-001 未决项的收口」逐条引用一个已消失的输入，收口结论失去可比对的前件。
- 定级：S3 — 不改产物字节，但属"登记面在指向不存在的载体"，与 ㉜ 的"清理炸锚"同型且本文件是受害者之一。
- 怎么算修好：把 `task` 键改成指向**现行跟踪面**的合同节（建议 `"contract": "docs/contracts/CONFIG_CONTRACT.md §4"`），或在 `docs/contracts/` 落一份最小 CFG-001 摘要件承接 §8 的引用；两法都要同批改 `CONFIG_CONTRACT.md:5` 的 `任务:` 字段，否则合同继续指向已删路径。
- 置信：CONFIRMED（跟踪集＋删除历史两口径均已取到证据）

### CR-251-03 · 45/45 条曲线的出处缺口全靠 `GAP-025` 一个编号承载，而该编号在跟踪面无台账位、去向指向不存在的工程包
- 轴：C（主轴）/ S
- 位点：`:29` `"gap_id": "GAP-025"`；`:31` `"next_task": "下一轮工程包（滤镜 provenance 补齐专项，GAP-025 处置）"`；`:30` `"note": "…补 provenance 属下一轮工程包专项。"`（本文件内该串共出现 47 次：头部 1 次＋每滤镜条目 1 次）
- 上位依据：`docs/contracts/CONFIG_CONTRACT.md:105` 只是**复述**同一编号（"…`gap_id=GAP-025`；…补 provenance 属下一轮工程包专项…"），没有"该缺口是什么、归谁、何时收"的登记录；AGENTS §7 把 `docs/` 定为自解释文档集、`工程控制/` 定为控制包（收口后清理）。
- 现状：本节点在跟踪面检索 `GAP-` 定义位：`docs/KNOWN_LIMITATIONS.md` 内 `GAP-` **零命中**；`docs/algorithms/anchors/unresolved_registry.json` 内 `GAP-025`/`gap_id` 零命中；`工程控制/RELEASE-05/GAP_AUDIT.md` 内 `GAP-[0-9]{3}` 零命中；全仓 `GAP-025` 命中面 = 本文件(47) + `CONFIG_CONTRACT.md`(2) + `eng/tests/config/test_cfg001_contracts.py:288`（断言值）+ `lib/algorithms/photometry/docs/algorithm.md:140` + `实验/photometric-magnitude/RESOLUTION_fsyn_formula.md:150`、`RESOLUTION_m42_curve_resolve.md:263`（复述）。`next_task` 里"下一轮工程包"在跟踪面也不存在（`工程控制/` 仅 `RELEASE-05`）。
- 差在哪：一个被 45 条科学数据条目共同引用的出处缺口，既没有可回查的台账行，也没有承诺承载它的开放任务包 ⇒ "已登记"只是"写了个编号"。
- 后果：`docs/science/PHOTOMETRY.md:52` 把 `T(λ)` 的来源直接指向本库；库里 45 条全部 `verified:false`＋`url:null`＋`instrument:null`，唯一的缺口凭据就是这个无台账编号 ⇒ 创新点一（测光校准到星等坐标系）所用的通带集，其"缺口已登记、有人负责、可收口"的对外主张目前不可复核。
- 定级：S2 — 出处缺失本身已如实标注（不是隐瞒），坏的是缺口的**承载面**不存在导致其无人认领；不直接改产物字节，故不升 S1。
- 怎么算修好：在跟踪面唯一缺口台账（建议 `docs/KNOWN_LIMITATIONS.md` 或 `docs/algorithms/anchors/unresolved_registry.json`，二选一并写进 `docs/DOCUMENT_INDEX.yaml`）登记一条 GAP-025 记录（内容＝缺什么、归属域、验收判据）；`next_task` 改指该条**节内锚**而不是"下一轮工程包"；同批给 `check_cfg001_contracts.py::test_provenance_marks_unverified_and_gap025` 补一条"编号必须能在台账里找到定义行"的断言（否则下次清理台账又会静默炸锚）。
- 置信：CONFIRMED（四处定义位候选逐个检索过，检索式已列）

### CR-251-04 · 本卡自称"可复跑断言"的测试方法名不存在，真名在合同面是对的、在配置卡里是错的
- 轴：C
- 位点：`:22` `"statement": "逐字转录：…未重采样、未插值、未改数值。可复跑断言见 eng/tests/config/test_cfg001_contracts.py::test_filters_transcription_is_verbatim。"`
- 上位依据：`docs/contracts/CONFIG_CONTRACT.md:104`「机器门逐条与源文件比对（`TestFiltersLibrary::test_verbatim_transcription`）」；`ENGINEERING_SPEC` §8「每项检查必须有可执行负例面」的前件是"入口可找到"。
- 现状：`eng/tests/config/test_cfg001_contracts.py:248` 的 `class TestFiltersLibrary` 实有方法为 `:259 test_source_locators_resolve_and_are_hash_frozen`、`:270 test_verbatim_transcription`、`:280 test_each_filter_record_has_only_transcribed_fields`、`:285 test_provenance_marks_unverified_and_gap025`。固定串全仓检索 `git grep -Fn "test_filters_transcription_is_verbatim"` **只命中本文件 :22 这一行**；同一条命令以真名 `test_verbatim_transcription` 作控制样本命中 2 处（合同＋定义行），排除了"工具误报零命中"。
- 差在哪：配置卡把一个不存在的方法名当作"可复跑断言"的入口对外承诺；同一条款在合同侧写的是真名 ⇒ 两侧说的不是同一个（一个存在的、一个不存在的）断言。
- 后果：照 `:22` 去复跑转录保真性会直接找不到入口；转录保真是本卡对外的第一主张（"未重采样、未插值、未改数值"），其"可复跑"凭据按本卡自己给的地址不成立。
- 定级：S3（若按 `:22` 建按名索引的门，则该门恒找不到目标 ⇒ 升 S2；现状未见此类门）
- 怎么算修好：`:22` 改 `…::TestFiltersLibrary::test_verbatim_transcription`（补类名，与合同侧一致）；若希望"按名可核"，同批在 `eng/tests/config/check_cfg001*.py` 侧加一条"配置卡里引用的 `文件::方法` 必须可解析"的断言（覆盖本文件所有 `statement/note` 里的方法名串），否则同类漂移还会发生。
- 置信：CONFIRMED

### CR-251-05 · 出厂库里 Astrodon E-series B 与 I-series B 是同一条曲线的两个名字，四项通带身份核对全部通过 ⇒ 库内错配在生产判据面前不可见
- 轴：S（主轴）/ C / R
- 位点：`:87-103`（`Astrodon E-series B` 条目）与 `:141-157`（`Astrodon I-series B` 条目）——两条的 `curve_stats` 八个值逐项全等：`"n_points": 67, "wl_min": 386.0, "wl_max": 518.0, "val_min": 0.01, "val_max": 0.99, "wl_sum": 30284.0, "val_sum": 54.652, "val_sumsq": 51.01658`
- 上位依据：`docs/science/PHOTOMETRY.md:52`「`T(λ)` 是滤镜透过率，无量纲，值域 `[0,1]`；来源 = 本帧配置声明的滤镜型号在 `filters.json` 中的曲线」；`PHOTOMETRY.md` §2a.7「判定『取到的就是声明的那条曲线』需要四项同时成立」并明写「**③ 不足以定身份、④ 是必需的**：min/max 只是包络，两支不同曲线可以共用同一包络（**把 `value` 数组整段换成另一支滤镜的值即为一例**）」。
- 现状：本节点整文件解析复算 `filters["Astrodon E-series B"] == filters["Astrodon I-series B"]`（`channel`、`wavelength_nm`[67]、`value`[67] 逐元素全等），且**转录源** `lib/algorithms/photometry/data/response_curves/filters.json` 里同样全等 ⇒ 本卡的 `mode: verbatim`（`:14`）成立、错不在转录；曲线体 `:1242-1380` 与 `:1689+` 两段（后者属后续分片，须与该段批次合并处置）。身份核对的指纹侧：`filter_curve_json.h:188` 自述「provenance 的 curve_stats 由转录脚本按同一数组算出 ⇒ 正常应逐位相等」，`:312-341` 运行期重算 `wl_sum/val_sum/val_sumsq` 再比。
- 差在哪：①两个不同商业型号名共用一条 `T(λ)`，物理上至多一个是真的，而本卡与登记面没有任何一句披露"两名同曲线"；②§2a.7 声称④能抓住"把 value 数组整段换成另一支滤镜的值"——本案正是该形态的实例，而④**全绿**：因为指纹与被检数组同源（`provenance.status: "unverified"`、`url: null` ⇒ 没有独立读数可锚）。"④ 是必需的"这一句在指纹自算的前提下给出的是虚假安全感。
- 后果：用户配置 `filter_passband: "Astrodon I-series B"` 时，`F_syn = ∫F_λ·T·Q·λdλ`（`PHOTOMETRY.md` §2a.1）用的是 E-series 形状 ⇒ 该帧 `k_photo`/`zero_point_mag`/`sigma_residual` 与真实通带不一致，且产物 `p1_phot.json` 的 `passband_identity.gate` 仍写 `enforced`；对外主张"测光校准到星等坐标系时通带身份已核对"在库内错配这一类上不成立。
- 定级：S2 — 触发路径可写到 `phase_config.photometric.filter_response → orchestrator.cpp:2638 → curve_json::load_curve(:2648)` 与 `scheduler/src/module_adapters.cpp:5528 → frame_photometry_fit.cpp:115`，但"哪一条是错的"需外部出处证据（GAP-025 未闭合），故不升 S1（缺"该型号真实曲线为何"这一角）。
- 怎么算修好：①对本卡两条各自 `verified:false` 之外再补一列 `shares_curve_with`（或把重复者从库里摘牌并走迁移说明），禁止"同数组两名并存"；②同批在 `check_cfg002_registry.py`/`test_cfg001_contracts.py` 加一条"45 条 `(channel,wavelength_nm,value)` 指纹两两不同"的门（现 `library_facts` 只核 `keys=45` 与大小写折叠零重名，**不核曲线重名**）；③订正 `PHOTOMETRY.md` §2a.7 那句：写清"④ 只对『数组被改而指纹未跟着改』有判别力；指纹与数组同源于同一未证实转录件时，④ 不构成通带身份凭据"——改②时须同时改 `config_registry.json#filter_name_policy.library_facts`，否则"新增库事实"与登记面不一致判红。
- 置信：CONFIRMED（曲线数组与统计量本节点独立复算；源侧同样复算）

### CR-251-06 · 登记面把"库里缺 `default` 列"记成待修转录偏差，而本卡的字段闭包门恰恰**禁止**补这一列 ⇒ 按登记的修法改会判红
- 轴：C（主轴）/ D
- 位点：`:15-19` `"transcribed_fields": ["name","channel","wavelength_nm","value","n_points"]`
- 上位依据：`docs/contracts/CONFIG_CONTRACT.md:126`「**转录偏差（登记 finding，修复归 `eng/packaging/config/filters.json` 域）**：逐字段复算 45 条曲线，44 条与源文件逐字段一致；`Astronomik UV-IR Block L-2` 在库中**缺少**源文件具有的 `default` 字段（曲线数值未变，仅该键缺失）」。
- 现状：本节点复算：源侧该条目键集为 `['name','channel','wavelength_nm','value','n_points','default']`，`default = True`，且 45 条中**仅此一条**带该键；库侧该条目键集去掉 `default`。守卫在 `eng/tests/config/test_cfg001_contracts.py:280-283`：`self.assertEqual(sorted(FIELDS_TRANSCRIBED), sorted(rec.keys()), "%s 出现了转录字段之外的列" % name)` —— 给某条补上 `default` 即判红；把它加入 `transcribed_fields` 则 `:270` 的逐字段全等断言对另外 44 条判红（源侧没有该键）。
- 差在哪：登记的"finding"描述与判定互斥：合同说这是要修的偏差，门却把它锁成不可修形态；两侧没有一处写明"`default` 列**故意不转录**及其语义归属"。（`ASTROCS_DESIGN.md:312` 只冻结"型号、通带、波长"三列，本就不含 default 语义 ⇒ 该列在权威链上无处安放。）
- 后果：下批按合同字面"补齐转录"必撞红，最省事的规避是删登记行或放宽字段闭包门 ⇒ 用一次假修消掉一条真登记；`default` 这个源侧信息位（"哪一条是默认滤镜"）在出厂配置面上永久不可表达，而设计 §4.3:306 又规定"空值表示显式无滤镜"⇒ 无默认语义可用。
- 定级：S3（当前无生产读取方按 `default` 取值：`git grep -n '"default"' -- lib/**/cpp/src` 命中的都是 schema 默认值声明，不读滤镜对象该键）
- 怎么算修好：三处同批——①`CONFIG_CONTRACT.md:126` 把"转录偏差"改判为"**按合同有意省略**：`default` 非三列之一，且 45 条中仅 1 条携带，转录闭包排除"；或 ②若确需该语义，先在 `ASTROCS_DESIGN.md` §4.3 或测光文档给它定义，再把 `transcribed_fields` 与源侧补齐（45 条全部要么有要么无）；两法都必须同时改 `test_each_filter_record_has_only_transcribed_fields` 的期望集合，否则改对判红。
- 置信：CONFIRMED（源侧/库侧键集与两条断言体均逐字读过）

## 本格"不报的核过项"＋检索式

| 核过项 | 判为不报的理由 | 用的检索式/复算 |
|---|---|---|
| `transcription.source_sha256`／`source_bytes`／`provenance.source_sha256` 是否已漂移 | 三值与现算全等，且消费门存在并逐项判红 | Python `hashlib.sha256(open(源,'rb').read())` 对比声明值；`git ls-files --error-unmatch` 两源均在跟踪面 |
| `filter_count=45` 与 `per_filter`/`filters`/源 是否同数 | 四处全等 45，键集合全等 | `set(per_filter)==set(filters)`、`len(源)==45` |
| "逐字转录、未重采样、未插值、未改数值"是否可信 | 45 条逐字段复算仅 1 处缺 `default` 键（已由 CR-251-06 单列），5 个转录字段数值零差异 | 按 `FIELDS_TRANSCRIBED` 逐键 `!=` 比较全表 |
| 45 条 `curve_stats` 是否为手填 | 全部与数组现算值全等（含 `val_sumsq` 的浮点尾数噪声），排除手填 | `Σwl/Σv/Σv²` 与 min/max/len 逐项比 |
| `channel` 值域与计数是否如登记所说 | 实测 `{B,G,R,L,HA,OIII,PAN,""}` = `10/10/10/4/1/1/2/7`，与 `config_registry.json#library_facts` 与 `CONFIG_CONTRACT:117` 三侧一致 | `Counter(v["channel"])` |
| 库键在"大小写+空白折叠"下是否重名（本卡 `:869` 断言 0） | 实测 0，登记 `case_or_space_collisions: 0` 成立 | 折叠后集合基数比对 |
| `lookup` 五键（`unknown_filter/match/case_sensitive/normalization/aliases`）多侧是否同值 | 四侧同值：本卡、`config_registry.json#filter_name_policy.rule`、`CONFIG_CONTRACT:212-213`、门 `check_cfg002_registry.py:512-521` 硬编码期望一致；`test_cfg002_registry.py:53-60` 亦断同 | 读四处原文比对（未逐字节复算，属字面量比对） |
| 曲线数组本身是否含 NaN/负值/越界/非单调/双数组长度不等 | 45 条全部单调递增、`value∈[0,1]`、无 NaN、`len(wl)==len(value)==n_points` | `integrity.py`（本批 `独立审计/复算件/CR-251/`） |

---

# 格 2 · `eng/packaging/config/filters.json:251-500`

本段内容：`provenance.per_filter` 第 13–26 个条目（Astronomik Typ 2c B/G/R、**UV-IR Block L-1/L-2/L-3**、Baader B/G/R、**Baader UV/IR Cut / L CMOS Optimized**、Baader 7nm H-alpha、Baader 8.5nm OIII、Chroma B/G）。本格出现全库唯一的"7 点 0/1 矩形曲线"族——本批最高危的 S1 即由这 4 个条目的统计量暴露。

## §2.5 数值处置表（格 2，15 行；本段数值项共 112 个）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :249,:257-264 | `per_filter["Astronomik Typ 2c B"].curve_stats` | 65 / 402.0–530.0 / 0.0–0.963 / 30290.0 / 40.16 / 33.037666 | 包络＋逐元素指纹（身份核对③④项） | 公式导出 | 同格 1 的式（`n=len(wl)`、`min/max`、`Σwl`、`Σv`、`Σv²`）；运行期按式现算再比（`filter_curve_json.h:312-341`）；本节点对 45/45 复算全等 | — | CONFIRMED |
| :267,:275-282 | Typ 2c G | 90 / 456.0–634.0 / 0.002–0.984 / 49050.0 / 44.327 / 33.841772999999996 | 同上 | 公式导出 | 同上 | — | CONFIRMED |
| :285,:293-300 | Typ 2c R | 69 / 542.0–678.0 / 0.001–0.991 / 42090.0 / 41.81 / 35.368719999999996 | 同上 | 公式导出 | 同上 | — | CONFIRMED |
| :303,:311-318 | **UV-IR Block L-1** | **7** / 300.0–800.0 / **0.0–1.0** / 3764.0 / **3.0** / **3.0** | 一条 L 通道通带的包络与指纹 | **待确认** | 无任何权威规定"通带可用 7 点、5–100 nm 间距的 0/1 阶梯承载"：`docs/science/PHOTOMETRY.md:52` 只声明 `T(λ)` 值域 `[0,1]`；`:230` 规定消费网格 336–1020 nm/2 nm 且"网格外不外推"；`:314` 规定 `F_syn` 用 **Akima + 复合 Simpson**。本节点按生产同式复算：Akima 重采样后 **`T_min = −1.5097`**、343 个网格点中 **50 点为负** ⇒ 直接违反 `:52` 的值域声明（见 CR-251-07）；`val_sum == val_sumsq == 3.0` 说明 7 个值全落在 {0,1}（`v=v² ⇔ v∈{0,1}`，配合 `:52` 的 `[0,1]` 域），即这是一条**手工矩形**而非实测曲线 | 保守方向＝在出处（GAP-025）补齐前，把 7 点矩形条目标为"不可用于定标"或改由分段常数/阶梯重采样承载；影响范围＝声明这四条型号之一的所有帧的 `F_syn → k_photo/zero_point_mag/sigma_residual`（创新点一） | CONFIRMED（复算件 `独立审计/复算件/CR-251/akima_overshoot.py`） |
| :321,:329-336 | **UV-IR Block L-2** | **7** / 300.0–800.0 / 0.0–**1.0** / 3765.0 / 3.0 / 3.0 | 同上 | **待确认** | 同上；复算 **`T_min = −0.5000`**、66/343 点为负。**本条是源文件里唯一带 `default` 键（值 `true`）的条目**，而本卡的 `transcribed_fields`（:15-19）把它排除在外（见 CR-251-06） | 同上 | CONFIRMED |
| :339,:347-354 | **UV-IR Block L-3** | **7** / 300.0–800.0 / 0.0–**1.0** / 3800.0 / 3.0 / 3.0 | 同上 | **待确认** | 同上；复算 **`T_min = −0.9622`**、81/343 点为负。L-1/L-2/L-3 三条的"开点"位置分别是 (367,500,710)/(400,500,675)/(420,500,680)：**同族三个版本被表示成带边相差最多 50 nm 的三条不同矩形**，而三列语义（型号/通带/波长，`ASTROCS_DESIGN.md:312`）与登记面（`config_registry.json#library_facts`）都没说这三个版本差在哪 | 保守方向＝未澄清前按"仅一条 L 通带可用"处置；影响范围＝L 通道跨版本的可比性主张 | CONFIRMED |
| :357,:365-372 | Baader B | 68 / 388.0–522.0 / 0.013–0.982 / 30940.0 / 56.288 / 52.429122 | 包络＋指纹 | 公式导出 | 同格 1 式；本节点复算全等，Akima 重采样后落在 `[0,1]` 内（45 条中仅上述 4 条越界） | — | CONFIRMED |
| :375,:383-390 | Baader G | 55 / 484.0–592.0 / 0.007–0.986 / 29590.0 / 43.008 / 40.326792 | 同上 | 公式导出 | 同上 | — | CONFIRMED |
| :393,:401-408 | Baader R | 73 / 572.0–716.0 / 0.008–0.988 / 47012.0 / 54.852 / 51.686136 | 同上；**本库被引用最多的示例键**（`ASTROCS_DESIGN.md:306` 的逐字匹配示例、模板 `normalize.phase_config.json:12`） | 公式导出 | 同上；`filter_curve_json.h:37-45` 记载的历史生产缺陷正是"请求 Baader R 却取到 Antlia B"，其修复判据即本行的 `curve_stats` | — | CONFIRMED |
| :411,:419-426 | **Baader UV/IR Cut / L CMOS Optimized** | **7** / 300.0–800.0 / 0.0–**1.0** / 3805.0 / 3.0 / 3.0 | 同上（第四条 0/1 矩形） | **待确认** | 同 L-1：复算 **`T_min = −1.2830`**、81/343 点为负（CR-251-07）。与 L-3 仅在一处开点位置（685 对 680 nm）不同 | 同 L-1 | CONFIRMED |
| :429,:437-444 | Baader 7nm H-alpha | 21 / 640.0–672.0 / 0.001–0.97 / 13776.3 / 4.728 / 3.313544 | 窄带 Hα 的包络与指纹 | 公式导出 | `Σwl` 复算全等（21 点非等距：步长集合含 0.7/1.0/1.3 nm）；Akima 重采样后仍在 `[0,1]` 内；但**窄带 7 nm 由 1.6 nm 平均步长承载**，其带宽由 21 点形状隐式给出，权威侧无"窄带最低采样密度"条款 ⇒ `PHOTOMETRY.md:135` 标定的"2 nm 网格离散误差 0.66–1.34%"是在 2 nm 均匀曲线上测的，不覆盖 0.7 nm 步长形态 | 保守方向＝对窄带条目单独给离散误差预算；影响范围＝Hα/OIII 帧的 `F_syn` 绝对刻度 | PARTIAL（未跑积分探针量化窄带误差） |
| :447,:455-462 | Baader 8.5nm OIII | 25 / 484.0–518.0 / 0.001–0.98 / 12518.7 / 7.8260000000000005 / 6.09811 | 同上 | 公式导出 | 同上（`val_sum` 尾数 `…0005` 亦证为累加所得） | 同上 | CONFIRMED |
| :465,:473-480 | Chroma B | 49 / 414.0–510.0 / 0.002–0.988 / 22638.0 / 43.319 / 41.852229 | 包络＋指纹 | 公式导出 | 同格 1 式；复算全等 | — | CONFIRMED |
| :483,:491-498 | Chroma G | 57 / 486.0–598.0 / 0.009–0.996 / 30894.0 / 43.505 / 41.134237 | 同上 | 公式导出 | 同上 | — | CONFIRMED |
| :251-255, :269-273 … :487-490 | 14 组 `url:null`／`retrieved:null`／`instrument:null`／`verified:false`／`gap_id:"GAP-025"` | 见左 | 出处五件套（非数值，按"六侧同值"核） | 待确认 | 14/14 与 `CONFIG_CONTRACT.md:105`「45/45 如实标注」一致（值侧一致成立）；缺的是这些"未验证"声明的**去向**：`gap_id` 无台账（CR-251-03），且库里无"矩形/实测"区分列（CR-251-07） | 保守方向＝补一列 `curve_kind ∈ {measured, rectangle_approx}` 并让 `verified` 依赖它；影响范围＝45 条曲线下游所有用带 | CONFIRMED（14 组逐条比对） |
| :312-313, :330-331, :348-349, :420-421 | 四条 L 曲线的波长域端点 `wl_min=300.0`／`wl_max=800.0` | 300.0 / 800.0 | 采样域端点 | 待确认 | 与 `PHOTOMETRY.md:230`「通带必须被 XP 覆盖（330–1050 nm）**完全包含**」的关系**取决于"通带"取采样端点还是 `T>0` 区**：按前者 300 nm 越出 ⇒ 该条应判"结论不成立"；按后者（L-1 的 `T>0` 区是 367–710 nm）⇒ 落在 336–1020 内。权威与本卡都没写取哪一种，且**没有任何机器门按 `wl_min/wl_max` 对 330–1050 做包含性检查**（检索式：`git grep -n "1050" -- lib` 无该语义读取点） | 保守方向＝以采样端点判包含（300 nm ⇒ 四条 L 全部不具备定标资格并显式报出）；影响范围＝这四条 L 是否可进定标 | PARTIAL（"零命中"只在 `lib/**` 面上取，未穷尽 CLI 预检面） |

## findings（格 2）

### CR-251-07 · 【本批最高危】四条 7 点 0/1 矩形 L 曲线经生产 Akima 重采样后产生最深 −1.51 的**负透过率**，`F_syn` 被带外负瓣污染，而库面/权威面都没有"采样密度下限"或"值域守卫"
- 轴：S（主轴）/ R / C
- 位点：`:311-318`（`"n_points": 7, "wl_min": 300.0, "wl_max": 800.0, "val_min": 0.0, "val_max": 1.0, "wl_sum": 3764.0, "val_sum": 3.0, "val_sumsq": 3.0`）、`:329-336`、`:347-354`、`:419-426`（同形态四条）
- 上位依据：`docs/science/PHOTOMETRY.md:52`「`T(λ)` 是滤镜透过率，无量纲，**值域 `[0,1]`**；来源 = 本帧配置声明的滤镜型号在 `filters.json` 中的曲线」；`:6` 定义式 `F_syn = ∫ F_λ(λ)·T(λ)·Q(λ)·λ dλ`；`:314`「**F_syn 数值积分（Akima + 复合 Simpson）**：Akima, H. 1970, J. ACM 17, 589…2 nm 网格离散误差 0.66–1.34%」；`:230`「本仓消费的采样网格为 336–1020 nm、步长 2 nm、343 点；网格外 `T·Q` 置 0 而**不外推**」。条文面对"滤镜曲线最低采样密度/形状要求"**零提及**（检索式：`grep -n "插值\|重采样\|采样间隔\|步长\|分辨率" docs/science/PHOTOMETRY.md` 的 6 处命中全为上述引用与谱插值条款，无一约束滤镜侧采样）。
- 现状：实现 `lib/algorithms/photometry/cpp/src/spectrum_integrator.cpp:339`「`// 4. Akima 插值: 滤光片透过率重采样到光谱波长网格 (范围外 = 0)`」+ `cache.filter_trans = akima_interpolate(fw, fv, cache.spectrum_wl, 0.0);`——`akima_interpolate`（`:46-125`）是分段三次 Hermite（Akima 切线），`:119` `y_dst[k] = h00*y0 + h10*dx*t[j] + h01*y1 + h11*dx*t[j+1]` **不做 y 侧钳位**（"范围外钳位为 0"只指 `x` 出源域，`:103-106`）；`grep "std::min(1.0\|trans > 1\|trans < 0\|clamp"` 在 `spectrum_integrator.cpp`、`frame_photometry_fit.cpp` 均零命中。
  本节点用**同式独立复算**（`独立审计/复算件/CR-251/akima_overshoot.py`，逐行照 `:46-125` 的切线扩展式与 Hermite 基重写，不 import 仓库代码）把 45 条曲线全部投到 336–1020 nm/2 nm 网格：
  - `Astronomik UV-IR Block L-1` → `T_min = −1.5097`，343 点中 **50 点 < 0**；
  - `Baader UV/IR Cut / L CMOS Optimized` → `T_min = −1.2830`，81 点 < 0；
  - `Astronomik UV-IR Block L-3` → `T_min = −0.9622`，81 点 < 0；
  - `Astronomik UV-IR Block L-2` → `T_min = −0.5000`，66 点 < 0；
  - 其余 41 条（2/1/0.5 nm 网格）**全部落在 `[0,1]`** ⇒ 越界与"7 点 + 0/1 阶梯"形态一一对应。
  又 `val_sum == val_sumsq == 3.0` 与 `T∈[0,1]` 联立 ⇒ 七个值只能是三个 1 与四个 0（`v−v²≥0`，等号仅 `v∈{0,1}`），即这四条不是实测曲线而是**手工矩形**；库面与登记面都没披露这一点（45 条共用同一句 `unverified: original curve source not recorded`）。
- 差在哪：被科学文档写死为"值域 `[0,1]`"的量，在生产重采样后取到 **−1.51**；负瓣落在 730–800 nm（XP 网格内、有真实谱数据），不是"网格外不外推"能挡掉的，是从声明域内**主动减去**一大块红端通量。库里既无采样密度下限、又无"矩形/实测"标记列，实现侧也没有 `[0,1]` 守卫——三层同时不设防。
- 后果：凡 `filter_passband` 声明这四条之一的帧，`F_syn` 带上一个幅度与主通带同量级的负瓣 ⇒ 参考通量的**颜色项**与绝对刻度同时偏（零点拟合吸收一阶平均偏移，把误差留下为色项畸变），`zero_point_mag`/`k_photo`/`sigma_residual` 的可比性主张（AGENTS §5 创新点一、`PHOTOMETRY.md:352` 表第 ③ 行）在该帧不成立；而 `p1_phot.json` 的 `passband_identity.gate` 仍写 `enforced`——它只核"取到的是不是声明的那条"，不核"那条重采样后是否物理"。
- 定级：**S1** — 逐跳生产路径：① 本卡 `filters["Astronomik UV-IR Block L-1"].wavelength_nm/value`（数组体位于后续分片，同一份数据的处置须与该段批次合并）→ ② `lib/algorithms/photometry/cpp/src/frame_photometry_fit.cpp:115` `curve_json::load_curve(req.filters_json, filter_key, &filter_wl, &filter_trans, …)`（`req.filters_json` 取自配置键 `photometry.fit.filters_json`，读点 `lib/infrastructure/scheduler/src/module_adapters.cpp:5528`；出厂 e2e 配置把该键指向本文件：`eng/tools/e2e/make_vis_configs.py:44`）→ ③ `frame_photometry_fit.cpp:255` `photo_calib::prepare_filter_cache(…)` → ④ `spectrum_integrator.cpp:339` Akima 重采样（负瓣在此产生）→ ⑤ `frame_photometry_fit.cpp:268` `compute_f_syn_cached_xpsd(…)` → `F_syn` → 零点与 `sigma_residual` 落产品。legacy 侧同链：`orchestrator.cpp:2638 → 2648`。
- 怎么算修好：①数据面——这四条（及任何 `n_points` 明显偏小者）改为分段常数表达或补实测采样，库里加一列区分 `measured` / `rectangular_approx`，`verified` 依赖它；②实现面——重采样后对 `filter_trans`/`qe_trans` 做 `[0,1]` **具名拒绝**（不是静默钳位，静默钳会把错值洗成合法字节）；③门禁面——给本库补"逐条曲线重采样到消费网格后必须落在 `[0,1]`"的机器门＋注入一条已知越界负例（现有 `test_curve_stats_match_curves` 只核自述统计量，核不到形状）；④科学面——`PHOTOMETRY.md` §2a.1 补"滤镜/QE 曲线最低采样密度与重采样算子对值域的影响"正向约束。①②③须同批。
- 置信：CONFIRMED（越界数值由本节点按实现逐式重算得到；缺"跑生产二进制看 `F_syn` 实际偏差量级"一角，已登记文末"需要实跑"）

### CR-251-08 · 通带身份判据第④项被科学文档称作"逐元素声明"，实为三个置换不变的和；对 0/1 矩形曲线形状零判别
- 轴：S（主轴）/ C
- 位点：`:317-318`（`"val_sum": 3.0, "val_sumsq": 3.0`）、`:335-336`、`:353-354`、`:425-426`——四条互不相同的通带给出**完全相同**的这两个指纹值
- 上位依据：`docs/science/PHOTOMETRY.md` §2a.7「**③ 不足以定身份、④ 是必需的**：min/max 只是包络，两支不同曲线可以共用同一包络（**把 `value` 数组整段换成另一支滤镜的值即为一例**）……**指纹字段是 `provenance` 对曲线的逐元素声明**，缺字段 ⇒ fail-closed」；实现 `lib/algorithms/photometry/cpp/src/filter_curve_json.h:312-341` 逐字按 `Σwl / Σv / Σv²` 比。
- 现状：`wl_sum` 是波长轴自身的和（与 `value` 的**配对无关**）；`val_sum`、`val_sumsq` 只依赖 `value` 的**多重集**。本卡四条 L 曲线（开点 (367,500,710)/(400,500,675)/(420,500,680)/(420,500,685)）给出逐项相同的 `val_sum=val_sumsq=3.0`，仅 `wl_sum` 相差 1–41。把任一 0/1 曲线的开关序列整体挪动（只要仍是三个 1），③④ 与 `n_points` 全绿，而通带位置改变。
- 差在哪：文档把④描述成能抓"整段换数组"的**逐元素**声明；按式子本身④对"值↔波长配对"零判别，对 0/1 形态退化为"数有几个 1"。且该指纹与被判数组**同源**（`filter_curve_json.h:188` 自述"provenance 的 curve_stats 由转录脚本按同一数组算出"；本卡 `:27` `status: "unverified"`）⇒ 转录期即错配的曲线会带着自洽指纹出厂（CR-251-05 是活体实例）。
- 后果：§2a.7 是 `PASSBAND-IDENTITY-GATE-001` claim 的正本，对外主张"合成 `F_syn` 用的那条曲线必须是配置声明的那条"由四项核对担保；④ 的实际判别力低于声明 ⇒ 该担保在"配对被置换""矩形近似"两类上不成立，而这两类恰是现存数据形态。
- 定级：S2 — 判据面主张被夸大；当前 45 条的指纹与数组自洽，不直接使产物字节错值。
- 怎么算修好：把第④项改成**配对敏感**指纹（如 `Σ v_i·λ_i` 加全数组哈希），或降格文档措辞为"④ 只对'数组被改而自述统计未同步'有判别力，不构成通带形状的独立凭据"；若改判据须同批改 `filter_curve_json.h:312-341` ＋ `docs/science/PHOTOMETRY.md` §2a.7 ＋ 给 `test_cfg001_contracts.py::test_curve_stats_match_curves` 补"置换反例必红"负例——只改一侧会让另一侧继续宣称旧口径（判据方向：置换后指纹不变 ⇒ 现判据**偏绿**）。
- 置信：CONFIRMED（四条指纹值复算＋代数推导；"置换后全绿"按判据定义直接推得，未实做注入）

### CR-251-09 · 登记面一句"channel 列语义未在任何权威文档定义"被同仓合同当场判伪；而"是否有型号共用曲线"这一库事实反而无登记列
- 轴：C
- 位点：登记面 `eng/packaging/config/config_registry.json:2125`（`library_facts.bandwidth_semantics`）；被判"未定义"的 `channel` 列在本段的实际字面行为 `:874`（`"channel": "B"`，Antlia B）、`:989`（`"G"`）、`:1104`（`"R"`）、`:1241`（`"B"`）、`:1384`（`"G"`）——**注意：`provenance.per_filter` 各块内没有 `channel` 键**（该列只出现在 `filters` 段），故本批关于 `Johnson V="G"`、`SDSS g=""` 等取值的证据来自整文件 JSON 解析复算，其字面行在后续分片（须与该段批次合并处置同一列）；该列的转录义务由本卡 `:15-19` 承担
- 上位依据：`docs/contracts/CONFIG_CONTRACT.md:116`「**`channel`（通带类别列）的角色与空值语义（冻结）**：最高设计 §4.3 冻结 `filters.json` 承载三列——「型号、通带、波长」；`channel` 即其中的**通带类别**列」＋`:117`「值域实测 `{B,G,R,L,HA,OIII,PAN,""}`，计数 `10/10/10/4/1/1/2/7`」
- 现状：登记面写「…`docs/contracts/**` 内**不存在**滤镜命名/带宽条款（**唯一提及为 CONFIG_CONTRACT §4**），故不存在旧名残留或别名冲突；`channel` 列语义与空值域**未在任何权威文档定义** -> 登记为缺口（归属：负责人裁决 / 测光文档域）」；同块已登记 `channel_domain` 八值与 `channel_empty_keys` 七键。本节点复算 45 条：分布 `10/10/10/4/1/1/2/7`，值域与空值键集合与合同 §4 逐字一致。
- 差在哪：一句事实性断言可判伪——`CONFIG_CONTRACT.md` 自身就在 `docs/contracts/` 下且其 §4 正是该条款（同一句里"不存在条款"与"唯一提及为 CONFIG_CONTRACT §4"自相斥）；登记面据此把一个已在合同侧标 `冻结` 的列挂成"待负责人裁决的缺口"。（同一登记块里的 `keys:45`、`case_or_space_collisions:0` 复算为真，不连坐。）
- 后果：登记面与合同面对同一列给出相反的"是否已定义"状态 ⇒ 后续批次按登记面行事会去"补一个已冻结的定义"，或以"未定义"为由绕开 `channel` 约束（例如把 7 条空 `channel` 当缺陷回填）；同时 `library_facts` 没有"共用曲线组"这一列，CR-251-05 的错配在登记面上根本表达不出来（现门只核键名重名与计数）。
- 定级：S3 — 不改产物字节，属登记面自相矛盾＋核不到位的库事实。
- 怎么算修好：①该句改为"`channel` 角色与空值语义已冻结于 `CONFIG_CONTRACT.md` §4（本列不新增取值）"；②给 `library_facts` 增 `duplicate_curve_groups`（现值 `[["Astrodon E-series B","Astrodon I-series B"]]`）并让门核"45 条 `(channel,wavelength_nm,value)` 指纹两两不同"，与 CR-251-05 修法同批；③若确需超越"角色冻结"的权威定义，走 §4.3 或测光文档补条款，不再挂"未定义"。
- 置信：CONFIRMED（三侧原文＋45 条复算）

## 本格"不报的核过项"＋检索式

| 核过项 | 判为不报的理由 | 复算/检索式 |
|---|---|---|
| 本段 14 条 `curve_stats` 是否与曲线数组自洽 | 112 个数值全部与现算值全等（含浮点尾数） | `dupes.py` 逐条比对 45/45 |
| 41 条非矩形曲线重采样后是否越界 | 全部落在 `[0,1]` ⇒ 不外溢成"整库都有问题"的错误指控 | `akima_overshoot.py`（4/45 越界，逐条列出） |
| `Baader 7nm H-alpha`／`8.5nm OIII` 中心波长与型号名是否自洽 | 21 点 640–672 nm（均值 656.0）、25 点 484–518 nm（均值 500.7）与 Hα 656.3／[O III] 500.7 一致 | `Σwl/n` 复算 |
| 本段是否还有"两名同曲线" | 无——14 条指纹互异，全库唯一重复对在格 1 已报 | `dupes.py` 分组 |

---

# 格 3 · `eng/packaging/config/filters.json:501-750`

本段内容：`provenance.per_filter` 第 27–40 个条目（Chroma R、IDAS LPS P3、Johnson B/I/R/U/V、Optolong B/G、OPTOLONG L-PRO、Optolong R、SDSS g/i/r）。本格是"标准测光系统族"（Johnson/SDSS）集中出现的一段——`channel` 列为空的 7 条全在这里，本格的三条 finding 都由这一段暴露。

## §2.5 数值处置表（格 3，16 行；本段数值项共 112 个）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :501,:509-516 | Chroma R | 69 / 578.0–714.0 / 0.011–0.985 / 44574.0 / 49.992 / 46.424597999999996 | 包络＋指纹（身份核对③④） | 公式导出 | 格 1 的导出式；本节点复算全等；Akima 重采样后落 `[0,1]` 内 | — | CONFIRMED |
| :519,:527-534 | IDAS LPS P3 Light Pollution | 504 / **299.0**–802.0 / 0.003–0.984 / 277452.0 / 185.257 / 164.203235 | 同上（光害抑制宽带，1 nm 网格） | **待确认** | 采样域自 **299.0 nm** 起且该处 `T=0.003 > 0` ⇒ 有效通带不被 `docs/science/PHOTOMETRY.md:230`「通带必须被 XP 覆盖（330–1050 nm）完全包含」所包含；该条同一行写明"只有完全落在 330–1050 nm 内的通带可复现"。本卡对该事实**无标注**，登记面 `library_facts` 也无此列（见 CR-251-11） | 保守方向＝未被出处证实前，把"有效带越出 330–1050"的条目在库里显式标 `reproducibility=out_of_xp_coverage`，并让 `filter_passband` 命中它时具名降级；影响范围＝以 LPS 宽带做定标帧的零点与色项 | CONFIRMED（45 条波长域逐一复算） |
| :537,:545-552 | Johnson B | 420 / 359.5–569.0 / 0.001–0.994 / 194985.0 / 201.512 / 156.459712 | 同上（标准 Johnson B 响应，0.5 nm 网格） | 公式导出 | 导出式复算全等；波长域落在 330–1050 内；`channel="B"`（见本表 :555 行的 channel 规则问题） | — | CONFIRMED |
| :555,:563-570 | Johnson I | 464 / 697.5–929.0 / 0.001–0.999 / 377348.0 / 298.691 / 260.32561499999997 | 同上（标准 Johnson I 响应） | 公式导出 | 数值全等；但本条 `channel == ""`（`filters["Johnson I"].channel`，位于后续分片），而 `docs/contracts/CONFIG_CONTRACT.md:121` 冻结「`resolve(name)` 在名字命中后**必须**再校验 `channel` 非空…`F_syn` 通带积分…必须 fail-closed」——**实现面零承载**（见 CR-251-10） | 保守方向＝合同该句要么在实现侧落地（7 条空 channel 一律具名拒绝进 `F_syn`），要么由变更流程撤销该义务；两法影响范围不同，不可只改文档 | CONFIRMED（lib 面零命中已复算） |
| :573,:581-588 | Johnson R | 772 / 546.0–931.5 / 0.001–0.995 / 570315.0 / 317.811 / 231.141389 | 同上；**全库点数最多的一条** | 公式导出 | 数值全等；931.5 < 1050 且 > 1020（本仓消费网格上界，`:230`）——按采样端点判仍在 330–1050 内、在 336–1020 消费网格外有 2 段无数据（932–1020 无谱 ⇒ `T·Q` 置 0 不外推），本条 T>0 一直延伸到 931.5 ⇒ 红端截断 | 保守方向＝对红端超过 1020 nm 的条目单独声明"红端截断"；影响范围＝Johnson R/I/SDSS i/z 的色项 | CONFIRMED |
| :591,:599-606 | Johnson U | 222 / **308.5**–419.0 / 0.001–0.994 / 80752.5 / 107.977 / 80.367371 | 同上（标准 Johnson U 响应） | **待确认** | 有效带 308.5–419.0 nm，**蓝端越出 330–1050**（且越出本仓 336 nm 消费网格下界）⇒ `PHOTOMETRY.md:230` 同句"结论不成立"的适用对象；`channel=""`（CR-251-10 的 7 条之一）；库里既无适用域列、也无具名降级 | 保守方向＝未补出处前该条不得进定标路径（显式拒绝而非静默截断）；影响范围＝U 波段帧的零点与色项，且被截断后实际带≈336–419 nm，与"Johnson U"之名不符 | CONFIRMED |
| :609,:617-624 | Johnson V | 517 / 473.0–731.0 / 0.001–0.998 / 311234.0 / 173.94 / 123.45833 | 同上（标准 Johnson V 响应） | 公式导出 | 数值全等；波长域合规；但本条 `channel` 被赋值为 **`"G"`**（`filters["Johnson V"].channel`，数组体在后续分片）——与同段 `SDSS g.channel == ""` 构成"同为绿端标准带、类别一个填一个不填"的相反处置（见 CR-251-12） | 保守方向＝在分配规则有权威前，标准系统（Johnson/SDSS）一律留空或一律给值，不得混用；影响范围＝任何按 `channel` 分组/筛选的面 | CONFIRMED |
| :627,:635-642 | Optolong B | 76 / 374.0–524.0 / 0.0–0.973 / 34124.0 / 60.829 / 55.802979 | 包络＋指纹 | 公式导出 | 全等 | — | CONFIRMED |
| :645,:653-660 | Optolong G | 58 / 464.0–578.0 / 0.0–0.977 / 30218.0 / 38.383 / 35.105935 | 同上 | 公式导出 | 全等 | — | CONFIRMED |
| :663,:671-678 | OPTOLONG L-PRO Light Pollution | 400 / 350.0–749.0 / 0.003–0.986 / 219800.0 / 174.001 / 148.389615 | 同上（1 nm 网格光害宽带） | 公式导出 | 全等；波长域 350–749 在 330–1050 内（**本段唯一越窗的是 IDAS 那条**）；`channel="PAN"`，与同族 `Astronomik UV-IR Block L-*` 的 `"L"` 分属两类而语义无权威（CR-251-12） | — | CONFIRMED |
| :681,:689-696 | Optolong R | 90 / 566.0–744.0 / 0.0–0.99 / 58950.0 / 52.052 / 49.094884 | 包络＋指纹 | 公式导出 | 全等 | — | CONFIRMED |
| :699,:707-714 | SDSS g | 313 / 389.0–545.0 / 0.001–0.993 / 146171.0 / 261.259 / 253.050895 | 同上（SDSS g 响应） | 公式导出 | 全等；`channel=""` ⇒ CR-251-10 的 7 条之一；且与 `Johnson V→"G"` 相反处置 ⇒ CR-251-12 | — | CONFIRMED |
| :717,:725-732 | SDSS i | 315 / 678.5–835.5 / 0.001–0.998 / 238455.0 / 244.32 / 236.127346 | 同上 | 公式导出 | 全等；`channel=""`；红端 835.5 在消费网格 1020 内 | — | CONFIRMED |
| :735,:743-750 | SDSS r | 299 / 539.0–688.0 / 0.001–0.994 / 183436.5 / 247.35399999999998 / 240.634322 | 同上（本段末条，闭合括号在 :751-752） | 公式导出 | 全等；`channel=""` | — | CONFIRMED |
| :503-507, :521-525 … :737-741 | 14 组 `url/retrieved/instrument:null`＋`verified:false`＋`gap_id:"GAP-025"` | 见左 | 出处五件套 | 待确认 | 与本段各条同形；登记面 `CONFIG_CONTRACT.md:105` 声称"45/45 如实标注"（值侧成立）；缺的仍是缺口台账（CR-251-03）与"适用域/波形类别"两列（CR-251-10/11） | 保守方向＝五件套之上再加 `applicability` 列；影响范围＝45 条全库 | CONFIRMED |
| 本段 14 条的 `n_points`（504/420/464/772/222/517/76/58/400/90/313/315/299/69） | 采样点数 | 见左 | 曲线采样密度 | 待确认 | 权威面**无**任何"滤镜曲线最低采样密度/步长"条款（检索式见 CR-251-07 上位依据条），而 `PHOTOMETRY.md:135/:314` 的离散误差标定（2 nm 网格 0.66–1.34%）是以 2 nm 均匀曲线为输入测的——本段同时存在 0.5 nm（Johnson/SDSS）、1 nm（IDAS/OPTOLONG）、2 nm（多数）、7 点矩形（格 2）四种密度 ⇒ 单一误差预算覆盖不了 | 保守方向＝按条给出离散误差预算或统一重采样到 2 nm；影响范围＝`F_syn` 的绝对与色项精度声明 | PARTIAL（未做逐条误差量化，需跑积分探针） |

## findings（格 3）

### CR-251-10 · 合同冻结"解析器命中名字后必须再校验 `channel` 非空，否则具名 fail-closed"，实现面零承载、测试面零覆盖，而 7 条空 `channel` 恰是全标准测光系统
- 轴：C（主轴）/ D
- 位点：本段的 `channel` 义务由 `:15-19` `"transcribed_fields": ["name","channel",…]` 建立；本段的 `provenance.per_filter` 块首行标出这 5 条空 `channel` 者——`:555` Johnson I、`:591` Johnson U、`:699` SDSS g、`:717` SDSS i、`:735` SDSS r（**`per_filter` 块内不含 `channel` 键**，其字面行在 `filters` 段、属后续分片；本批取值来自整文件 JSON 解析复算，另 2 条 SDSS u/z 亦同）
- 上位依据：`docs/contracts/CONFIG_CONTRACT.md:121`「**解析器的校验责任（冻结）**：`resolve(name)` 在名字命中后**必须**再校验 `channel` 非空；`channel == ""` 的曲线只走不依赖通带类别的路径：判定、筛选、换算与 `F_syn` 通带积分都需要通带类别 ⇒ **必须 fail-closed**（具名报错），通带类别只取自 `channel` 列」；`ASTROCS_DESIGN.md:312`「├── filters.json 滤镜库：型号、通带、波长」（`channel` 即"通带"列，是三列之一）。
- 现状：解析器 `lib/algorithms/photometry/cpp/src/filter_curve_json.h` 的 `check_curve_identity`（`:207-346`）四项核对为 name / n_points / 包络 / 指纹，**不含 channel**；全仓 `git grep -n "channel" -- "lib/**/*.cpp" "lib/**/*.h"` 的非图像命中只有 `:235` 的一句诊断字符串（"转录字段集 = name/channel/…"），即 `channel` 列在 `lib/**` 生产面**零读取点**；`git grep -n "通带类别" -- lib eng` **零命中**。测试面唯一涉及 channel 的是 `eng/tests/config/check_cfg002_registry.py:538-540`，它只核"空 channel 的键集合 == 登记的 7 个名字"，不核解析行为。
- 差在哪：合同用"必须…fail-closed（具名报错）"这种可执行句冻结了一项解析器责任，代码里根本没有这条判定；同一列又被最高设计点名为三列之一，却在生产面没有任何消费点（"声明必有用"缺口）。
- 后果：①声明 `filter_passband: "SDSS r"`（或 Johnson I/U、SDSS u/i/z、Johnson V 之外的 7 条）的帧会**照常**进入 `F_syn` 通带积分，而合同规定这类曲线"只走不依赖通带类别的路径"⇒ 合同的 fail-closed 承诺不成立；②因为无消费者，任何按"通带类别"做的分组/筛选在产品里根本不存在，若下游文档或插件按 §4 的说法认为有该保护即被虚假授权；③标准系统带（SDSS/Johnson）正是"校准到星等坐标系"最常被声明的键，覆盖面不是边角。
- 定级：S2 — 触发路径可达（`frame_photometry_fit.cpp:115 → :255 → :268`），但因 `channel` 无消费者 ⇒ 拿不到"该列被绕过导致产物错值"的逐跳证据，不到 S1；缺的是"合同所称需要通带类别的判定/筛选/换算"在生产里究竟指哪段代码（条文面零提及）。
- 怎么算修好：二选一并同批落地——①实现侧：在 `check_curve_identity` 加第⑤项"`channel` 非空"并给具名 reason（如 `channel_undeclared`），`F_syn` 路径命中即拒；同批改 `eng/tests/config/test_cfg002_registry.py` 与 `test_photometry_curve_resolve.cpp` 各加"空 channel 必红/非空必绿"正反例（否则 `library_facts` 计数门会先把登记侧弄红）。②合同侧：若产品不需要通带类别，撤销 `CONFIG_CONTRACT.md:121` 那条义务并说明 `channel` 降为纯文档列（同时 `ASTROCS_DESIGN.md:312` 的"三列"要跟着解释）。⚠ 只改文档不改代码＝继续虚假授权；只改代码不改 `check_cfg002_registry.py:538` 的空值期望＝改对判红。
- 置信：CONFIRMED（实现面与测试面各两次独立检索，含中文术语与 alternative 拼写）

### CR-251-11 · 8/45 条曲线的声明波长域不被 XP 覆盖窗包含（其中 4 条的有效带 `T>0` 区就在窗外），库里没有适用域列，也没有门把它判出来
- 轴：C（主轴）/ S
- 位点：`:528` `"wl_min": 299.0`（IDAS LPS P3）、`:600` `"wl_min": 308.5`（Johnson U）、`:312-313/:330-331/:348-349/:420-421`（四条 L，`wl_min: 300.0`）；SDSS u `wl_min: 304.0`、SDSS z `wl_max: 1100.0` 两条在本段之后（须与 CR-251…CR-260 合并处置同一条款）
- 上位依据：`docs/science/PHOTOMETRY.md:230`「通带必须被 XP 覆盖（330–1050 nm）**完全包含**；官方口径同此（Montegriffo et al. 2023, A&A 674, A3 §8.4："any photometric system whose passbands are fully enclosed in the 330-1050 nm wavelength range covered by Gaia BP and RP spectra"）。**本仓消费的采样网格为 336–1020 nm、步长 2 nm、343 点；网格外 `T·Q` 置 0 而不外推**」；`:63` 表行「通带显著超出 XP 覆盖（330–1050 nm）⇒ 网格外无数据，`T·Q` 置 0 而不外推；**颜色项误差上升，结论不成立**…只有完全落在 330–1050 nm 内的通带可复现」。
- 现状：本节点按 `curve_stats` 的 `wl_min/wl_max`（:501-750 段内逐条复算，另含后续段两条）列出 8 条不被 `[330,1050]` 包含：IDAS LPS P3(299–802)、四条 L(300–800)、SDSS u(304–395)、Johnson U(308.5–419)、SDSS z(800–1100)。其中**有效带（`T>0` 区）** 真的越窗的有 4 条：IDAS 自 299 nm 起即 `T=0.003>0`；SDSS u 304–395；Johnson U 308.5–419；SDSS z 到 1100 nm。四条 L 的 `T>0` 区（367–710 等）落在窗内，越界的只是值为 0 的采样端点。库里 45 条的元数据五件套完全同形，**没有任何一列**写适用域/是否可复现；生产面也没有按 `wl_min/wl_max` 对 330–1050 或 336–1020 做包含性判定的读取点（检索式：`git grep -n "1050" -- lib` → 无该语义命中）。
- 差在哪：科学文档自己把"完全包含在 330–1050 nm"定为**可复现的前置条件**，并对越窗者判"结论不成立"；出厂库却把 8 条（其中 4 条实质越窗）与 37 条合格者用**同一形态**并列供给，靠运行时"网格外置 0"把 U 波段静默截断成约 336–419 nm 的一条带，仍按 `Johnson U` 之名产零点。
- 后果：`filter_passband` 声明 Johnson U / SDSS u / SDSS z / IDAS LPS P3 的帧，其 `F_syn` 用的是被截断的通带 ⇒ 与创新点一"校准到测光星等坐标系"的标准系统定义不再同一；`PHOTOMETRY.md` §2a 的适用性条款被违反而产品面不报（`passband_identity` 会记录波长域，属**已披露但未拦截**）。
- 定级：S2 — 有可达路径（同 CR-251-07 的 ①→⑤），但"错值幅度"要跑积分才定量；且该条不是"实现错"而是"库无适用域表达＋无门"。
- 怎么算修好：①库里加一列（如 `applicability`）承载 `in_xp_coverage` / `truncated_at_blue` / `truncated_at_red` 之类事实，由 `curve_stats` 现算而非手填；②给 `eng/tests/config/test_cfg001_contracts.py` 加一条"逐条 `T>0` 有效带必须落在 330–1050（并按 336–1020 声明是否被消费网格截断）"的机器门＋把 SDSS u 的 `wavelength_nm` 首点改为 340 作负例；③装配期命中越窗条目前显式具名降级（warn→error 由负责人定档），并在 `p1_phot.json` 的 `passband_identity.gate` 里区分"enforced / applicable"。
- 置信：CONFIRMED（8 条与 `T>0` 区分由本节点逐条复算；"生产无包含性检查"按 `lib/**` 面零命中记）

### CR-251-12 · `channel` 取值规则实测自相矛盾：`Johnson V → "G"` 而 `SDSS g → ""`、`Optolong L-PRO → "PAN"` 而 `Astronomik UV-IR Block L-* → "L"`，登记面把该列判为"未定义"
- 轴：C（主轴）/ S
- 位点：段内可逐字读到的 `channel` 字面行为 `:874`（Antlia B=`"B"`）、`:989`（Antlia G=`"G"`）、`:1104`（Antlia R=`"R"`）、`:1241`（E-series B=`"B"`）、`:1384`（E-series G=`"G"`）（格 4-6）；**`provenance.per_filter` 块内不含 `channel` 键**，故 `Johnson I`（:555）、`Johnson U`（:591）、`SDSS g/i/r/u/z`（:699/:717/:735/:753/:771）、`Johnson V`（:609 为 provenance 块首行）诸条的 `channel` **字面行在 `filters` 段、属后续分片**，本批取值来自整文件 JSON 解析复算的 45 条映射（`domain.py` 输出，逐条打印 `name → channel`），处置与该段批次合并定案；`OPTOLONG L-PRO` 的 `channel="PAN"` 与四条 L 的 `channel="L"`（格 2/格 3 表内以 `:663`、`:303` 等 **provenance 块首行**标出该条，非 `channel` 行）同样在后续分片，本批按解析取值
- 上位依据：`docs/contracts/CONFIG_CONTRACT.md:116-120` 冻结「`channel` 即三列中的**通带类别**列」「`""` = **未声明通带类别**（≠「无通带」、≠「宽带」）」，判据只核"分组计数恒为 `10/10/10/4/1/1/2/7` 且空值组恰为列出的 7 个型号名"；`eng/packaging/config/config_registry.json:2125` 则称该列"语义与空值域未在任何权威文档定义"（CR-251-09 已判该句为伪）。
- 现状：45 条实测映射（本节点复算，逐条打印）显示：Johnson B→`B`、Johnson R→`R`、**Johnson V→`G`**、Johnson I→`""`、Johnson U→`""`；SDSS u/g/r/i/z **全部** `""`；同为"绿端标准带"的 `Johnson V` 与 `SDSS g` 一个给类别一个留空；光害/紫外红外抑制类里 `IDAS LPS P3`、`OPTOLONG L-PRO`→`PAN`，而 `Astronomik UV-IR Block L-1/2/3`、`Baader UV/IR Cut / L CMOS Optimized`→`L`。合同只冻结值域与空值语义、登记面自称语义未定义 ⇒ 这条列的赋值**没有任何规范约束**。
- 差在哪：同一族量（"通带类别"）在两种互斥规则下被赋值——而 `""` 在合同里被明确判为"未声明"（不是"宽带"、不是"无通带"）；把 `SDSS g` 记成"未声明"与把 `Johnson V` 记成"绿"不可能同时正确。加上 CR-251-10（该列零消费者），错误不会在任何判据上显形。
- 后果：合同 §4 用 `channel` 承担"通带类别只取自该列"的冻结语义，产品一旦按它分组（或有人据它写文档/查询），Johnson/SDSS 标准系统会被系统性错分：5 条 SDSS 被判为"未声明类别"、`Johnson V` 被判为"绿端宽带"。对外的"型号—通带—波长"三列自证一致的主张（§4.3:312）在"通带"这一列上不成立。
- 定级：S3（当前零消费者 ⇒ 不产生错值；一旦有消费者即升 S2）
- 怎么算修好：①给出可执行的分配规则（如"类别取带质心波长所落区间：475–585 nm ⇒ G"，或"仅按型号名后缀赋 B/G/R/L/HA/OIII/PAN，标准系统一律留空"）并写进 `CONFIG_CONTRACT.md` §4 或测光文档；②把 `Johnson V`/`SDSS g` 这一对作为门用例（同规则必须同处置）钉进 `check_cfg002_registry.py`；③与 CR-251-09、CR-251-10 同批改，避免"登记说未定义 / 合同说冻结 / 代码不读"三面继续各说一种。
- 置信：CONFIRMED（45 条 channel 映射逐条复算并打印）

## 本格"不报的核过项"＋检索式

| 核过项 | 判为不报的理由 | 复算/检索式 |
|---|---|---|
| 本段 14 条 `curve_stats` 与数组自洽性 | 112 个数值与现算值全等 | `dupes.py` |
| Johnson/SDSS 标准带的中心波长与标准值是否大致相符 | 采样均值 Johnson V 602.0（有效带 473–731）、SDSS r 613.5、SDSS g 467.0，与各自标准有效波长同量级；本批无权威规定的"标准带边"可判伪 ⇒ 不作为 finding | `Σwl/n` 复算 |
| 931.5 nm（Johnson R）等红端是否越出 1050 | 未越出 XP 窗，仅越出 1020 消费网格上界，已在表内以"红端截断"登记 | `domain.py` |
| `OPTOLONG` 与 `Optolong` 品牌大小写不一致 | **已登记**且规则自洽：`lookup.case_sensitive: true` ＋ `aliases: {}` ⇒ 二者是不同键；`library_facts.brand_case_inconsistency` 已列；折叠后无重名（复算 0 冲突） | `check_cfg002_registry.py:531-539` 逻辑 ＋ 本节点折叠重名检查 |
| `n_points` 是否等于数组长度（声明一致性） | 45/45 相等 | `integrity.py` |

---

# 格 4 · `eng/packaging/config/filters.json:751-1000`

本段内容：`provenance.per_filter` 末 5 条（SDSS u／SDSS z／ZWO B/G/R）＋ `provenance` 闭合 ＋ **整个 `lookup` 段（匹配语义与反例锚点）** ＋ `filters` 段开头（`Antlia V Pro Series B` 的完整两条数组、`Antlia V Pro Series G` 条目头与前 10 个波长点）。本段是本批"登记面备注可判伪"（派单口径③④）的主要命中段。

## §2.5 数值处置表（格 4，12 行；本段数值项共 157 个）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :753,:761-768 | SDSS u `curve_stats` | 183 / **304.0**–395.0 / 0.001–0.99 / 63958.5 / 154.952 / 145.50108 | 标准 SDSS u 带的包络与指纹 | 待确认 | 导出式复算与数组全等（`dupes.py`），但**有效带蓝端 304 nm 越出** `PHOTOMETRY.md:230` 的 330–1050 完全包含条件与本仓 336 nm 消费网格下界（CR-251-11）；出处仍为 `unverified`（GAP-025 无台账，CR-251-03） | 保守方向＝U 波段不进定标路径或显式声明"实际带 ≈336–395 nm"；影响范围＝声明 SDSS u 的帧 | CONFIRMED |
| :771,:779-786 | SDSS z `curve_stats` | 601 / 800.0–**1100.0** / 0.001–0.999 / 570950.0 / 550.163 / 543.765129 | 标准 SDSS z 带 | 待确认 | 同上口径：**红端 1100 nm 越出 1050**（`PHOTOMETRY.md:230` 与 `:63`"只有完全落在 330–1050 nm 内的通带可复现"），且 1020 nm 以上无消费网格数据；数值与数组全等 | 保守方向＝声明"z 带 1020/1050 以上被置 0 截断"并在产品面标不适用；影响范围＝SDSS z 帧的色项 | CONFIRMED |
| :789,:797-804 | ZWO B | 63 / 418.0–542.0 / 0.002–0.961 / 30240.0 / 44.907 / 40.476012999999995 | 包络＋指纹 | 公式导出 | `Σwl = 63×(418+542)/2 = 30240`（等差 2 nm 手算复核）＋逐元素复算全等 | — | CONFIRMED |
| :807,:815-822 | ZWO G | 57 / 502.0–614.0 / 0.002–0.982 / 31806.0 / 36.481 / 33.653788999999996 | 同上 | 公式导出 | `Σwl = 57×(502+614)/2 = 31806` ✓ | — | CONFIRMED |
| :825,:833-840 | ZWO R | 76 / 592.0–742.0 / 0.0–0.989 / 50692.0 / 49.431 / 45.905059 | 同上 | 公式导出 | `Σwl = 76×(592+742)/2 = 50692` ✓ | — | CONFIRMED |
| :846 | `lookup.unknown_filter` | `error` | 未知滤镜的处置档位 | 结构性不适用（枚举 token） | 六侧：本卡 `:846` ＝ 门硬期望 `check_cfg002_registry.py:512` ＝ `test_cfg002_registry.py:57` ＝ `CONFIG_CONTRACT.md:106/212` ＝ 最高设计 §4.3:306/§4.5:355 的"报 error"；代码侧 `filter_curve_json.h` 以 `LoadStatus::kCurveNotFound/kCurveNameOutsideLibrary` 显式返回而非兜底曲线。**注**：设计 §4.5:338-339/356 允许 `-force` 跳过整个预检 ⇒ "阻断"仅在未用 `-force` 时成立，本卡 `:7` 的 `authority.preflight` 未写这一层（不算矛盾，属披露不全） | — | CONFIRMED |
| :847,:848,:849,:850 | `match=exact`／`case_sensitive=true`／`normalization=none`／`aliases={}` | 见左 | 匹配语义四元组（冻结） | 结构性不适用（策略枚举/空集） | 四侧同值：本卡 ＝ `config_registry.json:2051-2052`（`rule` 串逐字含这四项）＝ `CONFIG_CONTRACT.md:213` ＝ 门 `check_cfg002_registry.py:515-521`；实测 45 键在大小写+空白折叠下 0 重名（本节点复算，与 `library_facts.case_or_space_collisions: 0` 一致） | — | CONFIRMED |
| :851 | `lookup.resolution_rule` | `resolve(name) = filters[name] if name in keys(filters) else ERROR(unknown_filter)。…不解析别名…` | 解析规则正本 | 结构性不适用（规则文本，无独立数值） | 与 `CONFIG_CONTRACT.md:212` 逐字、与 `filter_curve_json.h:20-27`（自述"逐字"引用）一致；实现按"键后紧跟 `:` 与 `{` 且对象含 `wavelength_nm`"定位（`:34-45` 修复注释） | — | CONFIRMED |
| :852-859 | `non_key_examples[0]`：`literal="bader r"`、`where="docs/contracts/CONFIG_CONTRACT.md:198"` | 行号 198 | 反例串及其权威锚 | 待确认 | **锚点不成立**：`CONFIG_CONTRACT.md:198` 在本基线是 **§9 之后的空行**（`awk 'NR>=196&&NR<=200'` 实证），含该串的负例行实在 **:215**（`git grep -Fn 'negative: "bader r"'`）；该文件的配套门 `check_cfg002_registry.py:569-580` 自述 fail-closed「该行真的含该串 ⇒ 锚点失效即判红」⇒ `TestCfg002Registry.test_all_ten_checks_green` 按代码推演必红（见 CR-251-13） | 保守方向＝把 `where` 改 `:215` 并让登记面同批重锚；影响范围＝CFG002-04 门的可信性与"反例已重锚"这一对外表述 | CONFIRMED（锚点行内容本节点逐字取到） |
| :860-866 | `non_key_examples[1]`：`literal="bader v"`、`where="…:198"`、`note` 内自述「现行权威锚点 = …**:197**」 | 198 与 197 | 同上一条（第二例） | 待确认 | 同一条目内 `where`（198）与 `note`（197）**给出两个不同行号**，且两者都不是 215；`:197` 是「## 9 旋钮与默认登记册」标题行，`:198` 是空行 ⇒ 两个说法互斥且都为假 | 同上 | CONFIRMED |
| :872-985 | `filters["Antlia V Pro Series B"]` 的 `wavelength_nm[53]`（:876-928）、`value[53]`（:931-983）、`n_points`（:985） | 波长 420.0→524.0 步长 2 nm 共 53 点；透过率 0.002,0.006,0.021,0.088,0.237,0.418,0.611,0.795,0.925,0.964,0.968,0.964,0.962,0.957,0.954,0.957,0.966,0.97,0.971,0.973,**0.978**,0.977,0.975,0.969,0.959,0.956,0.955,0.961,0.966,0.968,0.964,0.957,0.965,0.965,0.962,0.956,0.966,0.966,0.963,0.956,0.966,0.968,0.838,0.653,0.431,0.201,0.095,0.05,0.023,0.012,0.007,0.004,0.002（106 个数值，全部逐一列位） | 一条实测形状的通带透过率曲线（`T(λ)`） | **待确认** | 逐元素与转录源 `lib/algorithms/photometry/data/response_curves/filters.json` **全等**（本节点 45 条复算），`Σwl=25016`、`Σv=37.243`、`Σv²=34.158384999999996` 与 `:46-48` 声明自洽；但出处侧 `url/retrieved/instrument` 全 null、`verified:false`、GAP-025 无台账 ⇒ **无任何文献/标定锚**，值域 `[0,1]` 与单调性成立（`integrity.py`） | 保守方向＝在 GAP-025 闭合前该曲线只承担"配置声明的模型通带"，不得对外主张"与厂商/文献一致"（`CONFIG_CONTRACT.md:124` 已有同口径句，但库里无逐条 `applicability` 表达）；影响范围＝声明该型号的帧的 `F_syn` 与零点 | CONFIRMED（数值转录面）／PARTIAL（物理正确性面，无出处可核） |
| :987-1000 | `filters["Antlia V Pro Series G"]` 条目头：`name`（:988）、`channel="G"`（:989）、`wavelength_nm` 前 10 点 480.0→498.0（:991-1000，其余 43 点在 :1001-1043） | 10 个波长点 | 同族曲线（数组跨格 4/5） | 待确认 | 与源全等；`channel="G"` 属 CR-251-12 的分配规则问题面（本条为厂商 G 带，判据与标准系统混用不冲突）；**跨格事实**：本条数组体一半在格 5，其处置须与 CR-251…CR-260 合并定案 | 保守方向＝同 :872-985 行 | PARTIAL（本段只读到前 10 点） |

## findings（格 4）

### CR-251-13 · `lookup.non_key_examples` 两条权威锚点指向不含该串的行（一为空行、一为无关标题），而唯一被点名"逐元素锚定"的门按其自身代码必红；登记面还留着更早一代的死锚且完全在门的扫描面之外
- 轴：C（主轴）/ R（门判定面）
- 位点：`:855` `"where": "docs/contracts/CONFIG_CONTRACT.md:198"`、`:862` 同值、`:865` `"note": "…（现行权威锚点 = docs/contracts/CONFIG_CONTRACT.md:197）…"`
- 上位依据：`docs/contracts/CONFIG_CONTRACT.md:114`「`"bader r"`/`"bader v"` 的**唯一权威锚点 = 本文件 §10 负例表**（`eng/packaging/config/filters.json#lookup.non_key_examples[].where` 指向它）。**最高设计全篇不含 `bader`**」；`:219`「`non_key_examples` 的 `where` 锚点**必须指向本文件 §10 负例表所在行且该行必须真的含该串**」；门实现 `eng/tests/config/check_cfg002_registry.py:563` 注释「③ where 必须是「文件:行」锚点，文件真实存在且**该行真的含该串**（fail-closed，锚点失效即判红）」，实现体 `:569-580`。
- 现状（逐字取证）：
  - `CONFIG_CONTRACT.md` 在 :196 空行、**:197 = 「## 9 旋钮与默认登记册 …」**、**:198 = 空行**、:209 = 「## 10 滤镜名匹配语义」、**:215 = `negative: "bader r" | "bader v" | …`**（`git grep -Fn 'negative: "bader r"'` 命中 :215）。⇒ `where:198` 与 `note:197` 两个锚点均不含 `bader`，且两者互相矛盾。
  - 门 `check_04_filter_name_policy` 对 `lib["lookup"]["non_key_examples"]` 逐条执行 `lines[anchor_ln-1]` 含串判定 ⇒ 按代码推演会 append「`where` 锚点不成立」并 `raise Fail`；其 unittest 包装 `eng/tests/config/test_cfg002_registry.py:20-24 test_all_ten_checks_green` 对**真实仓库**断言 `fails == []`；该测试由注册步骤 `UT-CONFIG`（`eng/ci/checks.json` 中 `CHK-UNIT.steps[4]`，`profiles=["linux-main"]`、`waivable=false`）执行。
  - 登记面同族：`eng/packaging/config/config_registry.json:2051-2086` 的 `declared_negatives` 共 6 条，其中 2 条 `where = "ASTROCS_DESIGN.md:144"`／`:146`——实读该两行是 §2.2 关于 `g' = 1/g` 与 ADU/e⁻ 约定的正文，**不含 `bader`**（`git grep -in "bader"` 全仓 20 处命中里无 `ASTROCS_DESIGN.md`），与 `CONFIG_CONTRACT.md:114` 那句"最高设计全篇不含 bader"正面冲突；另 4 条 `where` 写成 `"大小写折叠反例"`、`"全大写反例"`、`"空白折叠反例（双空格）"`、`"前后空白反例"`，**根本不是 `文件:行` 锚点**。门对登记面这 6 条只取 `lit["literal"]`（`:623-627`），不读其 `where` ⇒ 登记面锚点全部处于"声明了但无人核"的状态。
- 差在哪：同一件事（反例串的权威出处）在三处各说一种且两处为假——本卡 `:855/:862`（指向空行）、本卡 `:865`（指向 §9 标题）、登记面（指向最高设计里不含该串的正文行 + 四条非锚点串）；而合同明文规定唯一锚点是 :215 那行，并有自述 fail-closed 的门守着。若该门实跑为绿，则它没被执行或被跳过（两种情形各是一种缺陷）。
- 后果：①"反例已按现行合同重锚（GATE-502，值与判据未变）"这一句对外主张（`CONFIG_CONTRACT.md:182` 表第 3 行、本卡 `:858` note）目前**指向不存在的锚**，重锚动作未完成；②CFG002-04 若确红，main 上 `linux-main` 档多一条不可豁免红灯（AGENTS §6 禁止用 waiver 盖红），修复压力会被错误引导去改判据；若它不红，则该 fail-closed 门有未执行路径（`UT-CONFIG` 仅在 `linux-main` profile 内，`fast` 档完全选不中它 ⇒ 本地/PR 快检看不到）。
- 定级：S2 — 不改产品字节，但属"合同声明 vs 出厂数据"直接矛盾 ＋ 门的可信性问题；不到 S1（无产物错值路径）。
- 怎么算修好：①`:855`/`:862` 的 `where` → `docs/contracts/CONFIG_CONTRACT.md:215`，`:865` note 的 `:197` → 同一处（**建议改用节内短语锚，避免行号再漂**）；②同批改 `config_registry.json:2051-2086` 的 6 条 `where`：前 2 条改指 :215，后 4 条要么给真锚点（同 :215 行确实含 `baader r`/`BAADER R`/`Baader  R`/` Baader R` 四串，可锚同一行），要么删掉"where"语义改叫 `kind` 描述；③把门从"只核 filters.json 侧"扩成**两侧都核**（`check_002_registry.py:623` 处补 `where` 格式与含串断言），并给 `--self-test` 注入"锚点行被删/改"的负例（现有 15 类注入是否覆盖 `where` 未知，需实跑确认）；④行号锚一律配"该行必须含该串"的机器断言——本条正是该断言该抓而数据自身不满足的活案例。
- 置信：CONFIRMED（三处锚点内容、门的判定逻辑与注册链均逐字读到）／门的**实跑红绿**为 PARTIAL（本节点禁止执行 `eng/**`，只做了代码推演）

### CR-251-14 · 出厂滤镜库被两条互斥的生产入口各按不同键名消费，本卡自己的"可复跑"与"消费方"表述都只覆盖一侧
- 轴：C（主轴）/ D
- 位点：`:22` `statement`（指向 `test_cfg001_contracts.py::…`）与 `:851` `resolution_rule`（规定 `resolve(name)=filters[name]`）；被点名的消费义务实际分两入口：`lib/infrastructure/scheduler/src/module_adapters.cpp:5528` `fit_cfg.value("filters_json", std::string())`（新生产节点路径）与 `lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp:2638` `stage1_cfg_->photometric.filter_response`（legacy 编排路径）
- 上位依据：`ASTROCS_DESIGN.md:306`「滤镜型号与 `eng/packaging/config/filters.json` 逐字匹配」——单数入口口径；`docs/contracts/CONFIG_CONTRACT.md:106` 把枚举门描述为"三份 phase_config schema 都保留 45 键 `$defs.filter_name` 枚举"（合同 schema 侧）；`lib/algorithms/photometry/cpp/src/filter_curve_json.h:31-34` 自述「调用方（唯一两份，均只做委托）」并分别点名这两条路径。
- 现状：两份配置键名不同（`photometry.fit.filters_json` 对 `photometric.filter_response`），且**三份出厂模板都不含其中任何一个**：`eng/packaging/config/templates/normalize.phase_config.json` 只有 `filter_passband`（:12/:23），无 `filters_json`/`filter_response` 键；模板给出 `filter_passband: "Baader R"` 却没给"从哪个文件解析该键"的路径键。另 `eng/tools/e2e/make_vis_configs.py:44` 用 `"filters_json"` 键名把 e2e 配置指向本文件。`lib/infrastructure/pipeline/orchestrator/configs/stage1*.json` 里 `filter_response` 指向 `lib/photometric_calib/data/response_curves/filters.json`（一个不存在的旧目录形态，见 `lib/algorithms/photometry/data/...` 才是现行路径），且写的是**绝对机器路径**。
- 差在哪：本卡与合同都按"一个滤镜库、一条解析规则"表述，实际有两条入口两套键名、模板侧一条都没有 ⇒ 用户照出厂模板跑时"逐字匹配滤镜库"这条设计义务在缺键侧由代码兜底（两处都是 empty ⇒ 显式报错不造 1.0，这一半是好的），但**出厂模板本身无法表达"匹配哪份库"**，而 `filter_response` 一侧的历史配置还指向不存在的目录。
- 后果：①"滤镜型号必须与滤镜库匹配"的对外主张对出厂模板用户不成立（模板里没有库路径）；②同名事实两处登记（`parser.cpp:308` 注释把 `filters_json` 列为 CLI 面键、orchestrator 用 `filter_response`）⇒ 任何按 key 名比对配置面的门（`eng/ci/check_config_defaults.py` 只扫三侧，见项目记忆）都读不到这条链；③`stage1_gc_panel*.json` 的绝对路径在干净检出上直接不可解析。
- 定级：S2 — 触发路径清楚（两条入口都能走到 `load_curve`），错值方向取决于运行者选哪条；模板缺键不改变科学取值（缺键即报错），故不升 S1。
- 怎么算修好：①`filter_response`／`filters_json` 统一到一个键名并写进 `docs/contracts/CONFIG_CONTRACT.md` 的滤镜节与本卡 `lookup.note`，同时把该键补进三份出厂模板（或显式声明"模板不带该键、由程序安装目录解析"并给解析点）；②`lib/infrastructure/pipeline/orchestrator/configs/stage1_gc_panel{1,2,3}_Red.json:30` 的绝对路径改为仓内相对路径或删除（它们是否仍在用须与 `docs/architecture/execution_inventory.csv` 的持有批次合并定案）；③给 `check_config_defaults.py` 扩成"值级＋键名贯穿"断言时，把这一对键名作为已知分叉样本纳入。
- 置信：PARTIAL（两入口键名、模板缺键、e2e 指向均逐字读到；`stage1_gc_panel*.json` 是否仍被任何生产入口装载未追完）

### CR-251-15 · 本卡称"未知滤镜 → error 供 CLI 预检第 3 项"，而 CLI 预检面根本没有滤镜检查项：设计要求的"运行前红级阻断"在预检页不发生
- 轴：D（主轴）/ C
- 位点：`:846` `"unknown_filter": "error"`；`:869` `"note": "未知滤镜名 -> error（供 CLI 预检第 3 项；ASTROCS_DESIGN.md §4.3 滤镜型号逐字匹配与 §4.5 红级 error）。…"`
- 上位依据：`ASTROCS_DESIGN.md:334`「normalize / mosaic / export 运行**同一套预检合同**：读 JSON → 逐项检查 → 显示完整检查页面…→ 按结果决定是否运行」；`:355`「**🔴 error**：文件找不到、路径错误、**未知滤镜**、配置无法解析等。error **阻塞运行**，原因必须完整列出在检查页面与机器可读输出中」；`:347`「阻断运行，报出全部原因　-y / -yes 不可越过」。§4.5 全节无编号清单 ⇒ "第 3 项"在权威面无来源。
- 现状（逐字读到）：预检唯一实现 `lib/infrastructure/cli/subcommand.h:302-358 precheck_config()` 产出的 `CheckLine` 只来自 `config_structure_errors()`（`:65-110`：退役逐帧形态、块键、`output_dir`、`InputContract` 字段）、`input_path_errors()`、`calibration_checks()`、`disk_precheck_lines()`——**不含任何滤镜判定**；`config_structure_errors` 为手写字段判据，不装载 `eng/contracts/schemas/phase_config_*.schema.json`（`git grep -n "schema" -- lib/infrastructure/cli` 命中面全是 cpu-profile 的 `schema` 串与 `schema_version` 字段）。`lib/infrastructure/cli/parser.cpp:282` 仅把 `filter_passband` 收进**键名白名单**，其注释自述真约束在下游：「写入 HiPS properties `obs_filter`，**Phase2 coverage 以此分组并 fail-closed**」。
- 差在哪：设计要求"未知滤镜"在**运行前**以红级阻塞并进检查页面，本卡也称自己供给该项；实际非法名要一路到测光节点（`filter_curve_json.h` 具名 `kCurveNotFound`）或 Phase2 分组才暴露。验证件同样是拿 schema 判而非拿产品面判：`eng/tests/config/fixtures/negative/unknown_filter.phase_config.json:13` 的 `"bader r"` 由 `validator.validate(...)` 判"被拒"（`check_cfg002_registry.py:600-635`），校验的是合同 schema 不是 CLI 预检 ⇒ "门接夹具不接产品"形态。
- 后果：对外主张「未知滤镜 -> 红色 error，强制阻断，-y 亦不可越过」（本卡 `:7` ＋设计 §4.5）在预检面不成立：用户在检查页面看到全绿并按 yes 继续，运行到阶段内才失败；失败点若被按"该节点不致命"处理，则退化为"无测光定标"而不是"运行前阻断"。
- 定级：S2 — 触发路径可写全：本卡 `lookup.unknown_filter` → `subcommand.h:302 precheck_config`（缺该项）→ §4.5 用户确认继续 → `module_adapters.cpp:5528` / `orchestrator.cpp:2638` → `filter_curve_json.h` 具名失败。不到 S1：最终仍具名失败，本批未取证到产物错值。
- 怎么算修好：①`precheck_config` 增滤镜项（取 `blocks[].filter_passband`／`inputs[].filter`，非空即须命中本库键），产出 `{"error", …}` 同时进检查页面与机器可读输出；②同批补**产品级**负例（用真 CLI 对 `unknown_filter.phase_config.json` 断言 rc 与检查页面文案），不得只留 schema 断言；③本卡 `:869` 的"第 3 项"改成可定位锚（节名＋条目文字），或在 §4.5 给预检项显式编号——后者属最高设计改动，走变更流程。
- 置信：CONFIRMED（预检实现、结构错实现、键白名单、负例夹具四处逐字读到）

## 本格"不报的核过项"＋检索式

| 核过项 | 判为不报的理由 | 复算/检索式 |
|---|---|---|
| `:869` 的"45 键在大小写+空白折叠下无重名"断言 | 复算 0 冲突，且被点名的断言确实存在于 `check_cfg002_registry.py:531-539` | 折叠后集合基数比对 |
| `resolution_rule` 与实现是否同一口径 | `filter_curve_json.h:20-27` 逐字引用本串，且拒绝形态是具名 `LoadStatus` 枚举（`kCurveNotFound`/`kCurveNameOutsideLibrary`），无静默回退到"第一条曲线" | 读实现体 ＋ `LoadStatus` 定义 |
| `Antlia B` 的 `value` 是否越域/非单调/与源不符 | 53 点全在 `[0,1]`、数组与源逐元素全等；波长轴严格递增 | `integrity.py`、`src_vs_lib2.py` |
| 本段 `curve_stats` 5 条 40 个数值 | 与数组现算全等（式在格 1） | `dupes.py` |
| `filters` 段与 `provenance.per_filter` 段是否同名同序 | 45 键集合相等、无只在一侧出现的名字（这本身是 CR-251-08 的"两份表述"结构，已在格 2 立案，不重复计数） | `hashes.py` 的 set 比较 |

---

# 格 5 · `eng/packaging/config/filters.json:1001-1250`

本段内容（全部是 `filters` 段的曲线体）：`Antlia V Pro Series G` 的波长数组尾段（:1001-1044）与透过率数组（:1045-1099）及 `n_points`（:1100）；`Antlia V Pro Series R` 整条（:1102-1237）；`Astrodon E-series B` 条目头与波长数组首段（:1239-1250）。本段**无新增键**，全部数值为转录曲线采样点。读法：把每条数组按其行段整体读出并逐元素与转录源比对（`独立审计/复算件/CR-251/reflow2.py` 输出下面每行的"值"即全部元素，无省略）。

## §2.5 数值处置表（格 5，5 行；本段数值项共 236 个）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :1001-1044（同条数组自 :990 起，本段含第 11–53 点） | `filters["Antlia V Pro Series G"].wavelength_nm` | 500.0 … 584.0（2 nm 等差 22 点，全数组 480.0→584.0 共 53 点） | 采样波长轴 | 结构性不适用（自变量网格） | 波长轴是**定义性网格**（2 nm 等差），不是被测量的科学量；本段 22 个值全部等于 `480.0 + 2·i`，与 `:59-60` 声明的 `wl_min/wl_max` 及 `:66` 的 `wl_sum=28196.0` 手算一致（`53×(480+584)/2=28196`）；逐元素与转录源全等（reflow2 输出 `与源逐元素全等=True`） | — | CONFIRMED |
| :1045-1099 | `filters["Antlia V Pro Series G"].value` | 53 点：0.001, 0.004, 0.009, 0.018, 0.053, 0.151, 0.357, 0.628, 0.908, 0.974, 0.975, 0.971, 0.968, 0.964, 0.961, 0.965, 0.972, 0.98, 0.982, 0.98, 0.97, 0.964, 0.96, 0.963, 0.967, 0.976, 0.981, 0.978, 0.975, 0.97, 0.965, 0.964, 0.968, 0.971, 0.972, 0.971, 0.972, 0.975, 0.974, 0.942, 0.832, 0.669, 0.429, 0.264, 0.143, 0.075, 0.037, 0.02, 0.012, 0.008, 0.005, 0.004, 0.002 | `T(λ)` 透过率采样值 | 待确认 | 与转录源逐元素全等（保真侧成立）；**物理侧无锚**：`url/retrieved/instrument` 全 null、`verified:false`、GAP-025 无台账（CR-251-03）；`Σv=34.699`、`Σv²=31.963722999999998` 与 `:63-64` 声明自洽 | 保守方向＝未补出处前不主张"与厂商曲线一致"（`CONFIG_CONTRACT.md:124` 同口径）；影响范围＝声明该型号的帧的 `F_syn` | CONFIRMED（转录）／PARTIAL（物理） |
| :1100 | `.n_points` | 53 | 数组长度自述 | 结构性不适用（长度自述） | 等于 `len(wavelength_nm)==len(value)`（`integrity.py` 45/45） | — | CONFIRMED |
| :1103-1104, :1105-1170, :1171-1236, :1237 | `filters["Antlia V Pro Series R"]` 的 `name`／`channel="R"`／`wavelength_nm`（594.0→720.0，2 nm 等差 64 点）／`value`（64 点：0.0, 0.001, 0.001, 0.002, 0.003, 0.004, 0.005, 0.006, 0.014, 0.026, 0.037, 0.098, 0.245, 0.482, 0.799, 0.984, 0.985, 0.982, 0.98, 0.978, 0.979, 0.981, 0.985, 0.984, 0.982, 0.979, 0.976, 0.977, 0.979, 0.982, 0.986, 0.987, 0.987, 0.988, 0.988, 0.988, 0.987, 0.985, 0.983, 0.981, 0.979, 0.972, 0.965, 0.96, 0.957, 0.953, 0.932, 0.85, 0.696, 0.468, 0.244, 0.134, 0.071, 0.046, 0.024, 0.016, 0.009, 0.006, 0.003, 0.003, 0.003, 0.002, 0.002, 0.001）／`n_points=64` | 见左 | 一条完整通带曲线 | 待确认 | 波长轴结构性（2 nm 等差，`Σ=64×(594+720)/2=42048` 与 `:45` 声明一致）；`value` 64 点为被测量 ⇒ 与源逐元素全等、物理侧仍无锚；`T(720)=0.001≠0`（红端不闭合）在 Akima 重采样下不外推（`:230`），故红端 0.001 残值只会带来 1e-3 量级的通量尾巴，不改变格 2 的结论 | 同上；另：本条起点 594 nm 为偶数 ⇒ 落进 336–1020/2 nm 消费网格节点，重采样为**精确取点**（无插值误差），与格 2 的 7 点异形网格情形不同 | CONFIRMED（转录）／PARTIAL（物理） |
| :1239-1241, :1242-1250 | `filters["Astrodon E-series B"]` 条目头（`name`/`channel="B"`）与 `wavelength_nm` 首 5 点 386.0, 388.0, 390.0, 392.0, 394.0（:1246-1250，数组起于 :1242） | 见左 | 跨格条目（数组体 1242-1310，尾部在格 6） | 结构性不适用（波长轴）＋跨格登记 | 与 `Astrodon I-series B`（条目体在 :1686+，属后续分片）**逐元素相同** ⇒ CR-251-05 的数组侧证据一半落在本格、一半落在格 6/后续分片；处置须与 CR-251…CR-260 合并定案 | 保守方向＝见 CR-251-05 | CONFIRMED |

## findings（格 5）

**本格无可辩护新指控。** 本段 236 个数值全部是 `filters` 段的采样点，逐条已按上面处置表给出锚与置信；为免"没读完就下判断"，列明本段用过的检索/复算式：

- 逐元素转录保真：`reflow2.py` 对 :1001-1250 内每个数组打印 `与源逐元素全等`（全部 `True`）＋ `src_vs_lib2.py` 对 45 条整表复算（唯一差异是 `Astronomik UV-IR Block L-2` 缺 `default` 键，已立案为 CR-251-06）。
- 形状合法性：`integrity.py`（严格单调、`value∈[0,1]`、无 NaN、双数组等长、`n_points` 自述相符）。
- 重采样越界：`akima_overshoot.py`（本段三条曲线全部落 `[0,1]` 内；越界只发生在格 2 的四条 7 点矩形上）。
- 与登记面一致：`channel="G"/"R"` 计数、`keys=45`、`case_or_space_collisions=0` 均复算成立（`CONFIG_CONTRACT.md:117` 与 `config_registry.json#library_facts`）。

---

# 格 6 · `eng/packaging/config/filters.json:1251-1500`

本段内容：`Astrodon E-series B` 的波长数组尾段（:1251-1310）、其**完整透过率数组**（:1311-1379）与 `n_points`（:1380）；`Astrodon E-series G` 整条（:1382-1499）；条目闭合行（:1500）。本段是 CR-251-05（两名同曲线）在数组侧的**本方证据**。

## §2.5 数值处置表（格 6，5 行；本段数值项共 239 个）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :1251-1310 | `filters["Astrodon E-series B"].wavelength_nm` 第 34–67 点 | 452.0 … 518.0（2 nm 等差） | 采样波长轴 | 结构性不适用（自变量网格） | 整数组 386.0→518.0 共 67 点、步长集合 `[2.0]`、`Σ=67×(386+518)/2=30284` 与 `:99` 声明一致；与转录源逐元素全等 | — | CONFIRMED |
| :1311-1379 | `filters["Astrodon E-series B"].value` | 67 点：0.01, 0.048, 0.151, 0.291, 0.45, 0.614, 0.767, 0.891, 0.972, 0.98, 0.98, 0.98, 0.98, 0.98, 0.99（连续 35 点 0.99 平顶，至 510 nm）, 0.98, 0.98, 0.97, 0.97, 0.89, 0.81, 0.79, 0.81, 0.83, 0.794, 0.71, 0.578, 0.398, 0.17, 0.114, 0.068, 0.034, 0.012（完整值列表见 `reflow2.py` 输出，本段逐元素读完） | `T(λ)` 透过率采样值 | **待确认** | 与转录源逐元素全等（保真）；物理侧无锚（`url:null`、`verified:false`、GAP-025 无台账）；**且本条与 `Astrodon I-series B` 的同一数组逐元素相同**（本方证据在 :1312-1378，对方在 :1689+）⇒ 见 CR-251-05。形态注记：35 点连续 0.99 平顶＋红端 0.79→0.81→0.83 的非单调回弹，与"图上读数后圆整"一致，与"仪器扫描实测"常见的抖动量级不同（此点**只作待确认理由**，不作独立指控） | 保守方向＝两条同曲线者至多一条可按本库名义进定标；影响范围＝Astrodon B 通道帧的 `F_syn` 与色项 | CONFIRMED（数组复算）／UNPROVEN（哪条为真，需 GAP-025 出处） |
| :1380 | `.n_points` | 67 | 长度自述 | 结构性不适用 | 等于两数组长度（`integrity.py`） | — | CONFIRMED |
| :1383-1384, :1385-1441, :1442-1498, :1499 | `filters["Astrodon E-series G"]`：`name`／`channel="G"`／`wavelength_nm`（492.0→600.0，2 nm 等差 55 点，`Σ=55×(492+600)/2=30030` 合 `:113`）／`value`（55 点：0.01, 0.072, 0.47, 0.81, 0.82, 0.83, 0.84, 0.91, 0.94, 0.97×5, 0.98×8, 0.973, 0.951, 0.92, 0.879, 0.829, 0.779, 0.742, 0.704, 0.665, 0.625, 0.583, 0.539, 0.449, 0.402, 0.353, 0.304, 0.253, 0.216, 0.149, 0.129, 0.111, 0.094, 0.079, 0.065, 0.053, 0.042, 0.033, 0.025, 0.018, 0.013 —— 完整序列见 `reflow2.py`）／`n_points=55` | 见左 | 一条完整通带曲线 | 待确认 | 波长轴结构性；`value` 为被测量、与源全等、物理无锚；起点 492（偶）与 336–1020/2 nm 消费网格**节点对齐** ⇒ 重采样为精确取点、无插值误差（与格 2 的 7 点异形网格不同，故不并入 CR-251-07） | 同 CR-251-05 的出处保守方向；影响范围＝Astrodon G 通道帧 | CONFIRMED（转录）／PARTIAL（物理） |
| :1500 | 条目闭合 `},` | — | 结构行 | 结构性不适用 | 本段结束，`Astrodon E-series R` 自 :1501 起（属下一分片） | — | CONFIRMED |

## findings（格 6）

本段无**新增**独立缺陷；CR-251-05 的数组侧本方证据落在 `:1312-1378`，已在格 1 立案并写明"对方数组体在 :1686+（后续分片）"，须与 CR-251…CR-260 合并定案同一处修（避免两侧各改一半）。

## 本格"不报的核过项"＋检索式

| 核过项 | 判为不报的理由 | 复算/检索式 |
|---|---|---|
| `Astrodon E-series B` 的 67 点 `value` 大量重复同一值（0.99 连续 35 点、0.98/0.97 成段） | 曲线**圆整/图上读数**的形态，属源数据分辨率问题而非本卡缺陷；已被 CR-251-03（GAP-025 无出处）与 CR-251-05（两名同曲线）覆盖，不另立案 | `reflow2.py` 打印整段值；与源逐元素全等 `True` |
| 红端 `0.79 → 0.81 → 0.83 → 0.794 → 0.71` 的非单调回弹（`value[21..25]`，约 508–516 nm） | 单一局部起伏不改变通量积分的量级判断，且无权威规定"边沿必须单调"；报它会造出无判据的指控 | 同上（逐元素读出） |
| `Astrodon E-series G` 波长轴 492.0→600.0/2 nm 与消费网格对齐 | 对齐 ⇒ Akima 重采样为精确取点，不产生格 2 那类越界，故不并入 CR-251-07 | `akima_overshoot.py`（本条 `T∈[0,1]`） |
| 本段两条曲线的 `Σwl`/`Σv`/`Σv²` 与 `:95-102`、`:113-120` 声明 | 逐项全等（含手算 `67×(386+518)/2=30284`、`55×(492+600)/2=30030`） | `dupes.py` ＋ 手算复核 |

---

# 收尾一 · 本批最高危三条（只挑不复述）

1. **CR-251-07（S1，唯一 S1）** — 四条 7 点 0/1 矩形 L 曲线（`:311-318`、`:329-336`、`:347-354`、`:419-426`）在生产 Akima 重采样后产生最深 `T = −1.5097` 的**负透过率**，直接违反 `docs/science/PHOTOMETRY.md:52` 写死的 `T(λ)∈[0,1]`，负瓣落在有真实谱数据的 730–800 nm；三层（库面/实现面/门禁面）同时无守卫。逐跳路径到 `compute_f_syn_cached_xpsd` 已写全。**这条改变产品字节并打在创新点一的对外主张上，优先级高于本批其余全部条目。**
2. **CR-251-05（S2）** — 出厂库把**同一条曲线**挂在 `Astrodon E-series B` 与 `Astrodon I-series B` 两个型号名下（数组与 8 项统计量逐项全等，源文件亦如此），而 §2a.7 的四项通带身份核对对本案全绿：它只核"取到的是不是声明的那条"，核不出"两名同曲线"。这是 CR-251-08（判据判别力被文档夸大）的活体实例，两条应同批处置。
3. **CR-251-13（S2）** — 反例串的权威锚 `where: "docs/contracts/CONFIG_CONTRACT.md:198"` 指向一个**空行**（真身 :215），同条目 note 又写 :197（§9 标题行）；登记面 `config_registry.json` 还留着更早一代的死锚 `ASTROCS_DESIGN.md:144/146`（实读为噪声口径正文，全仓 `ASTROCS_DESIGN.md` 不含 `bader`），且 6 条 `declared_negatives` 里 4 条的 `where` 根本不是 `文件:行`。合同自述该锚点由 fail-closed 门守着（`check_cfg002_registry.py:569-580`），按代码推演 `UT-CONFIG`（`CHK-UNIT.steps[4]`，仅 `linux-main` 档、`waivable=false`）必红——**红灯与"门未执行"两种解释都是缺陷，必须现场取因**。

次高（登记为缺陷但不进前三）：CR-251-10（合同冻结"`channel` 非空否则 fail-closed"，`lib/**` 零读取点、零测试覆盖，7 条空值恰是全标准测光系统）、CR-251-11（8/45 条声明波长域不被 XP 窗包含，其中 4 条有效带 `T>0` 真越窗，库里无适用域列）、CR-251-15（设计要求"未知滤镜 → 运行前红级阻断"，CLI 预检面无该项，验证只拿 schema 夹具判）。

# 收尾二 · 我读不动或需要实跑才能定的（含该跑什么）

| 未定项 | 本节点为何定不了 | 该跑什么（Linux 开发节点，按 AGENTS §3 套 `mem_guard`） |
|---|---|---|
| `UT-CONFIG` / CFG002-04 现在是红还是绿（CR-251-13 的后半） | 只读纪律禁止执行任何 `eng/**`；我只做了代码推演＋锚点行逐字取证 | `python3 -B -m unittest discover -s eng/tests/config -t eng/tests/config`（最小面），或 `python3 eng/ci/run_checks.py --check CHK-UNIT` 后看 `UT-CONFIG` 步骤 rc；注意 `--profile fast` 选不中该步（其 `profiles=["linux-main"]`），快检看不到 |
| 四条 L 曲线的负透过率对 `F_syn`/零点的**实际偏差量级** | 需执行 C++ 积分（本节点无可用构建） | 用 `lib/algorithms/photometry` 的解析探针（`test_spectrum_integrator`/`c1_analytic_check` 同族）对 `Astronomik UV-IR Block L-1` 与一条 2 nm 曲线各算一次，打印 `filter_trans` 重采样后的 `min` 与 `∫` 分解；预期 `T_min≈−1.51` |
| `filters["Astrodon I-series B"]` 与 `["E-series B"]` 哪一条是错的（或都不存在于厂商产品线） | 需厂商/文献出处，GAP-025 未闭合；本批只证明"两名同曲线"这一事实 | 出处核实属文献层（`docs/research/`＋一手厂商页），不在代码通读面 |
| `stage1_gc_panel{1,2,3}_Red.json:30` 的绝对路径 `F:\Astro dev\…\lib\photometric_calib\data\…` 是否仍被生产装载（CR-251-14 PARTIAL） | 需沿运行期配置装载路径实跑或全量追引用 | `git grep -n "stage1_gc_panel"` 的持有者＋`eng/ci/check_config_consumed.py`（该门是否覆盖这三个文件需实跑看清单） |
| `provenance.source_path` 指向的 `lib/algorithms/photometry/cpp/test/filter_qe_provenance.json` 内容与本卡 45 条 `per_filter` 是否逐字段一致 | 本批只核了它的 sha256 与"位于测试目录"这一结构事实，未逐字段读（47209 字节，读它属另一条取证链） | 读该文件并把 45 条 `source/verified/url` 与本卡逐字段比对；同时判"出厂配置的溯源件放在 `cpp/test/` 是否合适" |
| CLI 预检页对 `"bader r"` 的真实 rc 与文案（CR-251-15 已静态确认预检无该项） | 需跑 CLI | `astrocs normalize --config eng/tests/config/fixtures/negative/unknown_filter.phase_config.json --plan-only`（或等效预检入口），看检查页面是否出现滤镜 error 行、rc 是否为 3 |
| `library_id: "config.filters"` 是否有任何读取方（格 1 表内 PARTIAL 项） | 只做了字面检索，未穷尽"按 library_id 索引配置卡"的机制 | 追 `run_manifest`/`config_hash` 侧是否收集 `library_id` |
| 窄带（7 nm Hα / 8.5 nm OIII）在 2 nm 消费网格上的离散误差 | 需积分量化（`PHOTOMETRY.md:135` 的 0.66–1.34% 是在 2 nm 均匀曲线上测的） | 解析探针：常数谱 × 窄带曲线，对比闭式解 |

# 收尾三 · 与同文件其它段的合并待办清单（跨段项一律不在本批定案）

| 待合并事项 | 本方证据（本批段内） | 对方证据所在段 | 合并时必须定的一件事 |
|---|---|---|---|
| CR-251-05 两名同曲线 | `:87-103` / `:141-157`（统计量全等）＋ `:1242-1379`（E-series B 数组体，含闭合行 :1380） | `Astrodon I-series B` 条目体自 **:1686** 起 → 属 CR-252（1501-3000）段 | 由 CR-252 段确认数组体亦逐元素相同（本节点整文件解析已判为真），并**只保留一条** finding；修法是库里加"共用曲线组"表达＋门断言 45 条指纹两两不同，不得两侧各改一半 |
| CR-251-07 四条 7 点矩形的数组体 | `:311-318`、`:329-336`、`:347-354`、`:419-426`（统计量＋代数推出"值为 0/1、三个 1"） | 四条的 `wavelength_nm/value` 数组体（约 :2100-:2600 一带，属 CR-252/253 段） | 数据侧改矩形→分段常数或补采样的**落点在那一段**；本批提供的是"S1 判定＋Akima 越界数值"，两段合起来才是一条完整修法 |
| CR-251-11 波长域不被 XP 窗包含 | `:528`（IDAS 299.0）、`:600`（Johnson U 308.5）、`:762`（SDSS u 304.0）、`:780-781`（SDSS z 800.0-1100.0）、四条 L 的 300.0 | SDSS u / SDSS z 的数组体在后续段；`T>0` 边界的逐元素确认亦在那一段 | 定"适用域列"的表达形式（库里加列 vs 门里算），并确认 1100 nm 段的值是否 >0（决定 z 带是实质越窗还是仅采样端点越窗） |
| CR-251-12 `channel` 分配规则自相矛盾 | 本批读到的 `channel` 字面行只有 `:874`/`:989`/`:1104`（Antlia B/G/R）、`:1241`/`:1384`（Astrodon E-B/E-G），全部与厂商名后缀一致、无异常；`Johnson V="G"`、`SDSS g=""`、`Johnson I=""` 三者的**字面行在后续段**（`filters` 段 Johnson/SDSS 条目），本批是从整文件解析复算得到该映射 | `Johnson V`／`Johnson I`／`SDSS g,i,r,u,z` 的 `channel` 行，分散在 CR-252…CR-260 段 | 合并时按本批已复算的 45 条 `channel` 映射核对一次即可（本批已给出全量映射表），其它段无需重复统计；真正要定的是"分配规则写进哪一份权威"（`CONFIG_CONTRACT.md` §4 现只冻结角色与空值语义，未冻结取值规则） |
| 格 1 表头 `:6/:7` 的 §3.3/§3.5 错号 | 本卡两处 | 同文件 `:869` 用的是**对号**（§4.3/§4.5）；文件外还有三份 `phase_config_*.schema.json` 与 `config_registry.json:7` 带同一错号 | 全仓一次性重锚（含 `/:144` 这类行号锚），并把"引用最高设计节号"纳入机器可核（否则改一侧即两侧漂移） |
| 本文件其余 8 段（CR-252…CR-260） | — | — | 合并时需回答的两件全文件级问题：①45 条曲线的**重复度**是否只有 Astrodon B 这一组（本批已按 `(channel,wavelength_nm,value)` 全文件分组复算，唯一重复组即该组 ⇒ 其它段无需再查同一问题，只需在各自段内确认无新增列/键）；②顶层键闭包是否恰为 8 个（`filters_schema/library_id/task/authority/transcription/provenance/lookup/filters`，本批机械确认 ⇒ 其它段发现任何第 9 个顶层键即为异常）|

## 全文件机械事实（供各段引用，不作为本批统计交付）

`filters` 45 条 ↔ `per_filter` 45 条 ↔ 转录源 45 条，键集合相等；5 个转录字段逐元素全等（唯一差异 = `Astronomik UV-IR Block L-2` 缺 `default`）；45 条 `curve_stats` 与现算值全等；曲线全部严格单调、`value∈[0,1]`、无 NaN、双数组等长；Akima 重采样到 336–1020/2 nm 后越界者恰 4 条（即四条 7 点矩形）。复算件：`独立审计/复算件/CR-251/{hashes,dupes,src_vs_lib2,integrity,akima_overshoot,domain,reflow2}.py`。

## 派单与清单一致性

`独立审计/批次清单/CR-251.txt` 首行 = `# 批次 CR-251｜…eng/packaging/config/filters.json 的 1-1500 行（全文件 14950 行，拆成 10 段…）`，第二行 = `eng/packaging/config/filters.json:1-1500`；本批实读行数与之一致（`filters.json` 总行数经 `len(splitlines())` 复算为 **14950**，与派单/清单同值）。**无差异需登记。**

## 收工 `git status --porcelain` 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（与开工原文逐字相同，与派单声明的基线两项一致 ⇒ 本批未在仓库目录内落任何文件、未跑任何构建/测试/`eng/**`。本批全部取证产物落在仓库外：`独立审计/证据/通读-CR-251.md` 与本批 `独立审计/复算件/CR-251/*.py`。）

---

# 更正登记（本批成稿内部，不删原文、按落盘顺序留痕）

| 号 | 被更正的原表述 | 更正后 | 更正依据 |
|---|---|---|---|
| E-1 | CR-251-10 / CR-251-12 位点行曾把 `provenance.per_filter` 的块首行（`:555`、`:591`、`:609`、`:699`、`:717`、`:735`）与 CR-251-09 曾把 `:241` 写作 `channel` 的位点 | 这些行**不含 `channel` 键**（`per_filter` 块只有 `source/url/retrieved/instrument/verified/gap_id/curve_stats` 六件）；段内真实的 `channel` 字面行是 `:874`/`:989`/`:1104`/`:1241`/`:1384`；Johnson/SDSS 各条的 `channel` 字面行在 `filters` 段（>1500，属后续分片），本批证据取自整文件解析复算 | 本批 `reflow2.py` 与 `domain.py` 的实际输出比对；教训＝"用块首行代指该条的某个键"会造成假锚（同一错误若被下游照 `文件:行` 复验即判伪），已按派单口径改为显式声明证据来源 |
| E-2 | CR-251-13 的 `where` 锚点结论 | 不变（`:198` 为空行、`:197` 为 §9 标题、真身 `:215`），但补一条：`CONFIG_CONTRACT.md` 的行号在本基线可能被后续提交漂移，修法应改用**节内短语锚**而非再抄一个行号 | `awk 'NR>=196&&NR<=200'` ＋ `git grep -Fn 'negative: "bader r"'` 两次独立取行 |








</content>
</invoke>
