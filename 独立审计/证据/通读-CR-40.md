# 通读 CR-40 · 只读代码通读审计

- 基线 HEAD=`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 派单前言：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 全条已读，作为硬约束执行）
- 批次独占清单（6 文件，不得越出、不得漏）

## 开工 git status --porcelain 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 清单与逐文件读取状态

1. lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp —— 已读：是（表 41 项，findings CR-40-11…CR-40-16）
2. lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.h —— 已读：是（表 27 项，findings CR-40-11/CR-40-6 的声明面在此）
3. lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.cpp —— 已读：是（表 22 项，findings CR-40-7…CR-40-10）
4. lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.h —— 已读：是（表 14 项，finding CR-40-6）
5. lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp —— 已读：是（表 22 项，findings CR-40-1…CR-40-5 全部挂本文件）
6. lib/algorithms/drizzle/healpix_drizzle/wcs_sip.h —— 已读：是（表 6 项，本文件名下无独立 finding）

<!-- PROGRESS: 6/6 -->

---

## 本批重点（派单指定，逐条落判）

- ① 同一算法双轨实现时生产走哪一条（逐跳调用路径 `文件:行`）；两版常数是否已不一致（逐常数对比并列位点）
- ② 球面交叠本批这一侧的精确性是否弱于另一版（若弱，产品可达性决定定级：不可达 ⇒ S2 并写清"改接线即 S1"）
- ③ 多项式像元坐标变换阶数上界、系数换算与归一化是否与一手论文一致（须给一手依据或标 UNPROVEN）
- ④ "未测／不可估计"的数值表示

# 1. lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp


## 1.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :25-29 | `rel_diff(a,b) = \|a−b\| / max(\|a\|,\|b\|)`，`scale==0 → 0.0` | 0.0 | 全文件统一相对偏差 | 公式导出 | 相对误差定义（分母取两者较大者 ⇒ 对称）；`scale==0 ⇒ 返回 0`（两值同为 0 视为一致），方向：**两值同为 0 恒判一致**，不掩盖非零错误 | — | CONFIRMED |
| :58,:59,:60 | `{signal_sb,"ADU/sr",{1,−2}}`；`{sb_variance_out,"ADU^2/sr^2",{2,−4}}`；`{sb_ivar_out,"sr^2/ADU^2",{−2,4}}` | ±1/±2，∓2/∓4 | 冻结单位表的量纲指数 | 公式导出 | 由 `square_dimension`（:79-81，×2）与 `inverse_dimension`（:82-84，取反）导出：(1,−2)→²→(2,−4)→⁻¹→(−2,4) 逐条自洽；条文 `docs/science/DRIZZLE.md:123`＋本头 :10-15 | — | CONFIRMED |
| :61,:62,:63,:64 | `{pixel_variance_in,"ADU^2",{2,0}}`、`{w_info,"ADU^-2",{−2,0}}`、`{flux,"ADU",{1,0}}`、`{q,"ADU^-1",{−1,0}}` | ±1/±2 | 输入侧与通量/信息量的量纲 | 文献值（仓内条文面） | 本头 :11-15 FZ-UNIT-VAR-IN/WINFO/FLUX/Q 逐字；`w_info` 与 `q` 的关系（`q = 1/√W`？）在本文件**未被任何式子使用**，只登记 | — | CONFIRMED |
| :68 | `{pixel_area,"px^2",{0,2}}` | px^2 | "面积"单位行 | **不一致** | 与 CR-40-6 同族：几何给的 sr 在此被命名 px^2；`px_power` 字段本身承载立体角幂次（:67 头注释）⇒ 名称/语义交叉 | 保守方向：改为 `{pixel_area,"sr^2"?}` 或把字段改名 `solid_angle_power`；影响范围＝单位表可读性与误标复发率 | CONFIRMED |
| :74-76 | `return table[sizeof(table)/sizeof(table[0]) − 1]` | 表末项 | 未知 UnitId 的兜底 = `dimensionless` | 待确认 | 注释自述理由（"删行会使下标平移，硬编码即越界"，指向 PSFSW-RETIRE-03）成立——它避免了越界；**但它把"未登记的单位身份"降级成一个合法量纲**。见 CR-40-13 | 保守方向：兜底返回一个显式 invalid 行并使门判红；影响范围＝G-STRUCT-UNIT-LAW 的判据力 | CONFIRMED |
| :95 | `kRetiredUnitSymbol[] = "psfsw_robust_weight"` | 21 字符 | 退役对象符号面（字符串匹配） | 文献值（仓内条文面） | 本头 :16-22/:111-118 引 `ASTROCS_DESIGN.md §3.1` 与 `docs/design/UNIFIED_MODEL.md:58`（PSFSW-RETIRE-01/03）；本批按只读纪律不裁该裁决本身 | — | CONFIRMED |
| :107 | `const std::size_t n = sizeof(kRetiredUnitSymbol) − 1` | −1 | 去 '\0' 的全等长度 | 结构性不适用 | 字符串长度算术 | — | CONFIRMED |
| :129 | `legacy_drop_weight: a_jp / A_drop_j` | — | canonical 核权重 w_jp | 公式导出 | F&H 2002 §7.2（本头 :23-25 逐字引用）；代码按式算 | — | CONFIRMED |
| :131,:133,:136 | `sb_weight = a_jp/A_pixel_j`；`legacy_to_sb = pixfrac²·w`；`sb_to_legacy = w/pixfrac²` | 2 | 两参数化的换算 | 公式导出 | 由 `A_drop = pixfrac²·A_pixel` 导出 w' = w·pixfrac²；代码按式算 ⇒ 合规 | — | CONFIRMED |
| :138 | `sb_combination_coefficient = w_jp / N_p` | — | c_jp 定义 | 公式导出 | 本头 :28 | — | CONFIRMED |
| :143-146 | `flux_conservation_factor(pixfrac) { (void)pixfrac; return 1.0; }` | **1.0** | 通量守恒口径标记 | 公式导出（**条件成立**） | 本头 :36-38 自称"条件通量守恒"；恒等前提 `Σ_p a_jp = pixfrac²A_pixel` **不在 build 中校验**（:213-277 全程无闭合判据），故 1.0 是无条件返回；调用侧 `phase1_product.cpp:300` 把 1.0 当字面量写进 provenance | 保守方向：factor 应由实测闭合残差给出或 provenance 同时登记 `max_abs_closure_rel`；影响范围＝对外"绝对通量"主张 | CONFIRMED（无闭校验部分复算：`build` 内 `git grep -n "validate_overlap_closure"` 在本函数体内零命中） |
| :148-150 | `reference_sb_coefficient = (a_jp/A_pixel_j)/D_p` | — | 由 raw 重算 c_jp 的独立真值 | 公式导出 | 已复算两参数化等价：drop 侧 `c=[a/(pf²A)]·[pf²/D]=[a/(A·D)]`＝本式；与 :188 头注释"pixfrac=1 时逐位相同"一致（此处对任意 pixfrac 都同） | — | CONFIRMED |
| :181 | `pixfrac <= 0.0 \|\| pixfrac > 1.0` | 0 / 1 | pixfrac 冻结域 | 文献值（仓内条文面） | `docs/algorithms/DRIZZLE_GEOMETRY.md:180` "pixfrac∈(0,1]"；本头 :149 注释"显式拒绝，不夹逼"⇒ 与实现一致 | — | CONFIRMED |
| :194-197 | `!is_finite(sum_a_jp) \|\| !is_finite(A_pixel_j) \|\| A_pixel_j <= 0.0` | 0.0 | 闭合判据的输入域 | 结构性不适用 | 定义域边界 | — | CONFIRMED |
| :198,:199 | `expected = pixfrac²·A_pixel_j`；`rel = (sum − expected)/expected` | 2 | 闭合残差定义 | 公式导出 | 本头 :36/:176-179；`expected>0` 由 :195 保证 ⇒ 无除零 | — | CONFIRMED |
| :204,:205 | `rel > rel_tol → overlap_exceeds_drop`；`rel < −rel_tol → overlap_area_deficit` | 双向 | 闭合判据方向 | 文献值（仓内条文面） | **判据方向已钉死**：修复前只判 `rel > rel_tol` ⇒ 面积亏损（rel<0）恒不触发 ⇒ 偏绿；现两向具名 ⇒ 面积被吞时必红。:201-203 注释逐字记录了这一点 | — | CONFIRMED |
| :238 | `seen.reserve(overlaps.size() * 2 + 1)` | 2,1 | 哈希桶预留系数 | 结构性不适用 | 性能系数，不参与判定 | — | CONFIRMED |
| :240-242 | `e.src >= n_src \|\| e.dst >= n_dst`／`!is_finite(a_jp)`／`a_jp < 0.0` | 0 | 索引与数值域 | 结构性不适用 | 越界与定义域 | — | CONFIRMED |
| :243 | `(static_cast<uint64_t>(e.src) << 32) \| e.dst` | 32 | (src,dst) 复合键位移量 | 结构性不适用 | 位移宽度由两侧各 32 位决定 | — | CONFIRMED |
| :251 | `if (op.D_p_[e.dst] <= 0.0) return coverage_mismatch` | 0.0 | 逐 target 覆盖判定 | 结构性不适用 | **但注意**：该判定只在"出现在 overlaps 里的 dst"上执行 ⇒ `n_dst` 中没有任何 entry 的 target 保 D=0 且不被检查（见 CR-40-14） | — | CONFIRMED |
| :252-254 | `w = a_jp / (pixfrac²·A_pixel[e.src])`（drop 分支） | 2 | A_drop 由 A_pixel 与 pixfrac 导出 | 公式导出 | FZ-COND-FLUX-CONSERV（本头 :37）；**不是**用几何实测的 `drop_area`——两值之差恰是闭合门要测的量，故此处不引入额外偏差 | — | CONFIRMED |
| :258 | `if (op.D_p_[p] > 0.0 && !(op.N_p_[p] > 0.0))` | 0.0 | 覆盖/权重一致性 | 结构性不适用 | NaN 安全写法（`!(>0)` 对 NaN 为真）⇒ 偏红 | — | CONFIRMED |
| :293 | `ivar_sb: (var > 0.0) ? 1.0/var : 0.0` | 0.0 | 零/负方差时的 ivar 表示 | 结构性不适用（**方向论证后不报**） | 判据方向钉死：var≤0 ⇒ ivar=0 ⇒ 下游按 ivar 加权时该像素**权重为 0（被丢弃）**，不会伪装成高置信；若改返回 NaN 则更严但当前写法不偏乐观 | — | CONFIRMED |
| :333,:357 | `if (wp == 0.0) continue;` | 0.0 | 零权重跳过 | 结构性不适用 | 精确等于 0 的跳过不改数值（0 项对求和无贡献） | — | CONFIRMED |
| :352,:367 | `if (sumD <= 0.0) return 0.0;` | 0.0 | 父级归约在零覆盖时返回**零方差** | 待确认（**方向偏绿**） | 条文面零提及（检索式：`git grep -n "parent_variance\|父级归约" -- docs/`）；父级对角归约门 FZ-GATE-PARENT-VAR（本头 :42-44）只判记录形状，不判数值 | 保守方向：返回 NaN 或具名失败；影响范围＝`parent_deficit`（:376-380）与父级方差声明 | CONFIRMED（见 CR-40-14） |
| :378 | `if (exact <= 0.0) return 0.0;` | 0.0 | deficit 的除零保护 | 结构性不适用 | exact=0 时无相对亏损可言 | — | CONFIRMED |
| :423 | `rel_diff(x[j], B0*A_pixel(j)) > 1e-9` | **1e-9** | 常数 SB 门的**构造断言**（x 必须是面亮度构造） | 待确认 | 条文面只给 1e-3（`docs/science/DRIZZLE.md:123/:169`），**从未出现 1e-9**；检索式：`git grep -n "1e-9" -- docs/science/DRIZZLE.md docs/algorithms/DRIZZLE_GEOMETRY.md docs/algorithms/GATES_AND_TOLERANCES.md` ⇒ 唯一命中是 `docs/science/DRIZZLE.md:174` 的 "healpy 球面多边形面积对拍（rtol 1e-9）"（不同对象）。⇒ 该数值与条文里唯一的门数值 1e-3 之间**没有推导关系**，是第二个写死的经验值 | 保守方向：1e-9 比 1e-3 严 ⇒ 方向偏红；风险是它成为"事实规格"，改 1e-3 时无出处可依 | CONFIRMED（见 CR-40-15） |
| :431-434 | `if (B0 == 0.0) { if (\|s\| > rel_tol) fail; }` | 0.0 / rel_tol | 零面亮度分支（绝对判据） | 结构性不适用 | B0=0 时相对量未定义，退成绝对判 0；`rel_tol` 在此被当作绝对容差使用（量纲 ADU/sr），与 :435 的相对用法同参数不同语义——记入 CR-40-15 的第二缺陷 | — | CONFIRMED |
| :435 | `if (std::fabs(s / B0 − 1.0) >= rel_tol)` | rel_tol=1e-3 | 冻结常量 SB 判据 | 文献值（仓内条文面） | `docs/science/DRIZZLE.md:123` `\|S_p/B0−1\|<1e-3`：代码 `>=` 触发失败 ⇔ 条文 `<` 通过，方向一致 | — | CONFIRMED |
| :436 | `fail(g, "\|S_p/B0 − 1\| >= 1e-3 (frozen tolerance)")` | 文本内 1e-3 | 失败说明 | 待确认（**误导面**） | 该字符串写死 1e-3，而判据用形参 `rel_tol`；调用方传 1e-6 时失败原因会谎报为 1e-3 | 保守方向：格式化进实际 tol 值；影响范围＝证据可读性/复核判定 | CONFIRMED（见 CR-40-15） |
| :511,:517-518 | `rel_diff(ref, Cov(p,p)) > diag_rel_tol`；`tol = diag_rel_tol * std::max(scale, 1.0)` | **1.0** | 协方差门的两级容差 | **不一致** | 同一文件里 `rel_diff`（:25-29）分母**不带** 1.0 地板，而本处给相对容差加了绝对地板；`scale` 的量纲是 ADU²/sr² ⇒ 1.0 是带量纲的哨兵值，与 :511 的纯相对用法互斥 | 保守方向：删 `max(scale,1.0)` 或改写成与 :25-29 同一 helper；影响范围＝PSD 与非对角两项判据；见 CR-40-12 | CONFIRMED |
| :519-520 | `if (exact + tol < diag) fail` | — | PSD 违背判据（精确 ≥ 对角） | 公式导出 | 二次型 vs 对角和：`exact − diag = Σ_{p≠q}w_p w_q Cov ≥ 0`（权重与非负 c 时）；代码按式判，方向正确（exact 小于 diag 才红） | — | CONFIRMED |
| :522 | `const bool has_offdiag = (exact − diag) > tol;` | — | 非对角存在性判据 | 公式导出 | 同上；**方向：tol 变大只可能使本判据更难触发 ⇒ 偏绿**（CR-40-12 的后果链） | — | CONFIRMED |
| :604,:605 | `if (sp != −2) fail`；`if (vp != −4) fail` | −2 / −4 | BUNIT 面亮度幂次 | 文献值（仓内条文面） | 本头 :48-49 "pixel_area_power(−2 signal / −4 var)" 逐字 | — | CONFIRMED |
| :606 | `if (vp != 2 * sp) fail` | 2 | 二次律 | 公式导出 | variance = signal²；与 `square_dimension`（:79-81）同源 | — | CONFIRMED |
| :613-619 | `required[]` 14 个 provenance 键 | 14 | 最小集键数 | 结构性不适用 | 枚举基数；本头 :45 FZ-PROV-MINIMAL-SET 为口径来源（键清单本身在 schema 侧，不属本批） | — | CONFIRMED |
| :633 | `rel_diff(prov.flux_conservation_factor, expected) > 1e-12` | **1e-12** | 折算因子一致性 | 待确认 | 条文面零命中（`git grep -n "1e-12" -- docs/science/ docs/algorithms/`）；调用侧 `phase1_product.cpp:1199` 独立写死同值 ⇒ 两处一致但都无推导 | 保守方向：由 `rel_diff` 的 FP64 往返位数导出（≈1e-15~1e-13），或登记为"JSON double 往返护栏"（同类口径见 `docs/algorithms/GATES_AND_TOLERANCES.md:63` "记录内 1e-9 px 仅为 JSON 浮点往返护栏，不是科学容差"——**该先例支持本值可登记，但要求写明这一点**） | 待确认 |
| :636-638 | `!has_kcorr / !kcorr_has_domain / kcorr_calibration_ref.empty()` | — | k_corr 三条前置判据 | 文献值（仓内条文面） | `docs/algorithms/PHASE2_SAMPLER.md:314-317` "正向约束：(i) 所用 k_corr 值；(ii) 其标定域；(iii) 该标定在当前数据…"，":316 域为空即 rc=7"⇒ 三条与条文对齐 | — | CONFIRMED |
| :639 | `if (prov.k_corr == 1.0 && !prov.kcorr_zero_correlation_justified)` | **1.0**（唯一被钉值） | k_corr"忽略相关"拒绝 | **不一致（域判据缺一半）** | 权威域：`docs/algorithms/PHASE2_SAMPLER.md:52` "定义域 **1 < k_corr**"、`:306-307` "k_corr = 1 ⇔ 忽略相关…k_corr < 1 ⇔ N_eff > N_retained，正相关样本的有效样本量不可能大于样本数，**物理不可达**"。⇒ 条文把整个 `k_corr ≤ 1` 判为非法，本行只判 `== 1.0` | 保守方向：判据改 `!(k_corr > 1.0)` ⇒ 拒绝；影响范围＝control 方差、深度/探测率主张；见 CR-40-11 | CONFIRMED |
| :572 | `kPos[] = {"/sr","sr^","/px","px^","/pixel","pixel^"}` | 6 项 | 立体角幂次的字符串识别面 | 文献值（仓内条文面） | 注释引 `DATA_SEMANTICS §31.1a`"写侧一律 sr；legacy px/pixel 为读侧别名"。本批未逐字核该条（不在清单） | — | PARTIAL（缺：DATA_SEMANTICS §31.1a 原文对位；本批未读该节） |
| 合并行（浮点字面量） | `0.0/1.0` 单位元、哨兵与除零保护 | :27,:33-42,:145,:180,:195,:219,:233,:234,:235,:251,:258,:293,:318,:322,:330,:336,:350,:363,:377,:423,:432,:505,:541,:551,:597,:604,:605,:647-649 | 累加初值、枚举默认、判据 0 边界 | 结构性不适用 | 逐位点已列；除 :145/:639/:633 三条已在表内单独处置 | — | CONFIRMED |

## 1.2 findings（v6_drizzle_science.cpp ＋ 其头文件声明面）

### CR-40-11 · ④ "未测／不可估计"在 k_corr 上没有表示面：域判据只钉 ==1.0，而权威域是 1<k_corr，结构默认 0.0 属"物理不可达"值却能全绿
- 轴：C（主轴，与上位条文域不一致）＋ R（fail-open）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp:639`
  ```
  if (prov.k_corr == 1.0 && !prov.kcorr_zero_correlation_justified) {
      return fail(g, "k_corr == 1.0 ignoring correlation");
  }
  ```
  声明面同罪：`v6_drizzle_science.h:376-377` `bool has_kcorr = false; double k_corr = 0.0;`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:52` 逐字 "k_corr | Drizzle 输出协方差导致的 control estimator 方差放大因子；定义域 **1 < k_corr**（无量纲）"；`:305-307` 逐字 "常数权威：…k_corr 默认 1.4；**定义域 1 < k_corr**（k_corr = 1 ⇔ 忽略相关，`p2_upm_control_variance` 返回 rc=2；k_corr < 1 ⇔ N_eff > N_retained，正相关样本的有效样本量不可能大于样本数，物理不可达…）"；`:248` 逐字 "control_variance = k_corr × (π/2) × σ_bg² / N_retained"。
- 现状：本门检查 ① 有 k_corr ② 有适用域 ③ 有标定引用 ④ 不等于 1.0；**从不检查上下界**。而结构默认 `k_corr = 0.0`——一个既不表示"未测"又落在条文"物理不可达"区的数值。`v6_drizzle_science.h:46` 的冻结锚自身也只写 "k_corr 缺适用域或取 1.0 忽略相关 -> REJECT"，即代码与本头注释一致地**漏掉了 ≤1 的整个域**。
- 差在哪：`has_kcorr=true + k_corr=0.0`（未填真值的最省事写法）通过门；条文明令该值非法。"未测"没有可用表示面：要么填 0.0（非法但过门）要么 `unavailable=true`（另一条通道，且本门对 `unavailable` 不豁免 k_corr 判据）。
- 后果：按 `PHASE2_SAMPLER.md:248` 的式子，`control_variance = k_corr·(π/2)·σ_bg²/N_retained` ⇒ k_corr 取 0 使 control 方差**归零**，取 1 使相关贡献被忽略。**判据方向钉死（§3 硬门槛）**：k_corr 单调减小 ⇒ control_variance 减小 ⇒ 中位数控制量的不确定度变小 ⇒ 任何"σ 偏差 < N·σ"型判据更容易通过 ⇒ **偏绿**；且 `k_corr < 1 ⇒ N_eff > N_retained` 按 :307 为物理不可达，故该值不是"保守"而是"不可能"。对外主张受损面：`FZ-PROV-KCORR` 的"相关已按适用域折算"与 Phase2 的探测率/深度声明。
- 定级：**S2**。v6 provenance 面未接生产（`astrocs_v6_phase1_product` 只被 `eng/tests/integration/v6_p1/CMakeLists.txt:20` 链接；生产写盘侧的 k_corr 由另一条路径给）。改接线即 S1，逐跳：`lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:510/:515`（构算子）→ `:560-567`（跑门）→ `FZ-PROV-KCORR` 门 `v6_drizzle_science.cpp:610-643` → `phase1_product.json` 的 provenance 记录。
- 怎么算修好：① 本行改为域判据 `if (!(prov.k_corr > 1.0) && !justified) fail`，并把"justified"只对 >1 的例外开放（或干脆不允许例外，与 `p2_upm_control_variance` rc=2 同口径）；② 给"未测"一个真表示面：`std::optional<double> k_corr` 或 `k_corr_unmeasured = true` 必填，禁止 0.0 作默认（同族问题在本批还有 `closure_rel=0.0` 于失败路径，见 §3.1 :116 行）；③ 必须同批改：`v6_drizzle_science.h:46` 冻结锚文字（现文字把域写成"取 1.0 才拒"，是这次缺口的源头）＋ `kcorr_zero_correlation_justified` 字段名（"zero correlation"描述的其实是"无相关折算"，与本门守护的 `==1.0` 语义不符，改 `kcorr_unit_value_justified` 或按新判据改名）；④ 必须同批改：Phase2 侧 `sampler.cpp:83` 的 `k_corr 默认 1.4` 与本门的判据须同一来源，否则出现"装配侧允许、判定侧允许、取默认值时无人判"的第三态；⑤ 必须同批补负例：`has_kcorr=true, k_corr=0.0` 与 `k_corr=1.0` 各一条 ⇒ 均判红（现只有后者的负例资格），并断言 0.0 不会经 `* (pi/2) * sigma^2 / N` 变成零方差。
- 置信：CONFIRMED（三条条文逐字对读；方向按 :248 的乘法单调性确定）

### CR-40-12 · `gate_covariance_propagation` 把相对容差乘上带量纲的绝对地板 `max(scale,1.0)`，使 PSD 与非对角两项判据在小方差档失效
- 轴：R（主轴）＋ C（与同文件 :25-29 的相对差定义不一致）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp:517-522`
  ```
  const double scale = std::max(std::fabs(exact), std::fabs(diag));
  const double tol = diag_rel_tol * std::max(scale, 1.0);
  if (exact + tol < diag) { ... }
  const bool has_offdiag = (exact - diag) > tol;
  ```
- 上位依据：条文面零提及 1.0 地板（检索式：`git grep -n "max(scale\|绝对地板\|1.0)" -- docs/algorithms/GATES_AND_TOLERANCES.md docs/science/DRIZZLE.md`）。与本文件自身的相对差定义冲突：`v6_drizzle_science.cpp:25-29` `rel_diff` 的分母是 `max(|a|,|b|)`，**不含** 1.0 地板；同一个 `diag_rel_tol`（默认 1e-11，本头 :320）在 :511 走 `rel_diff`、在 :518 走带地板的 tol，两套刻度。
- 现状：`tol = 1e-11 × max(exact, diag, 1.0)`；`exact/diag` 的量纲是 ADU²/sr²（本头 :12 FZ-UNIT-VAR-SB）。
- 差在哪：给一个带物理量纲的量做 `max(·, 1.0)`，等价于在数值 1 处切换相对/绝对判据，而 1 这个分界与任何单位约定无关（ADU/sr 换 ADU/px 就整体平移）。
- 后果：**判据方向已钉死**。地板只可能使 tol 变大（`max(scale,1.0) ≥ scale`），而 tol 变大 ⇒ ① `exact + tol < diag` 更难成立（PSD 违背漏检）、② `exact − diag > tol` 更难成立（`has_offdiag` 漏判 ⇒ :523 的"声明对角精确却存在非对角"漏判）。两条都**偏绿**。生效域诚实说明：需 `max(exact,diag) < 1` ADU²/sr² 才使地板真正生效；SB 单位下（leaf 面积 ~1e-7 sr ⇒ 方差常 ≫1）通常不生效，故本批不声称已造成产物错值——它是"在低覆盖/低信号 target 或单位换到 ADU/px² 时失效的门"。
- 定级：**S2**（门本身在 v6 轨、由 `phase1_product.cpp:567` 调用；生产未接线，接线即 S1）
- 怎么算修好：① 删地板，与 :511 同用 `rel_diff(exact, diag) > diag_rel_tol`；若怕全零除零，显式写 `if (scale == 0.0) has_offdiag = false;` 的具名分支（同 :27 的既有约定），不要借 1.0 兜；② 必须同批改：`v6_drizzle_science.h:316-320` 的门注释现写"aperture 精确方差严格 >= 对角-only，且当权重与重叠非负时严格 >（存在非对角贡献）"，其中"严格 >"在实现里是 `> tol`（非严格、且被地板稀释），条文与实现要一次对齐；③ 必须同批补负例：构造 `exact − diag = 1e-13`、`diag ≈ 1e-6`（地板生效档）且 `claims_diagonal_exact=true` 的记录 ⇒ 断言判红（现必绿），该负例同时是修好的验收。
- 置信：CONFIRMED（方向与生效条件均已按表达式复算）

### CR-40-13 · `frozen_unit` 对未登记 UnitId 静默兜底为 dimensionless，使 G-STRUCT-UNIT-LAW 对垃圾单位恒真
- 轴：R
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp:74-76`
  ```
  /* 兜底 = 表末项 dimensionless。用表长表达式而非硬编码下标：删行会使下标平移，
   * 硬编码即越界（PSFSW-RETIRE-03 删 psfsw_robust_weight 行时即为此）。*/
  return table[sizeof(table) / sizeof(table[0]) - 1];
  ```
- 上位依据：`v6_drizzle_science.h:101` "冻结单位表唯一事实源（与冻结表逐字一致）"；AGENTS.md §5 与 §9"恒真门没有证据资格"（本仓口径亦见批判据"恒真断言"）。
- 现状：表内查不到 id 时返回 `{dimensionless, "1", {0,0}}`。对 `gate_unit_law`（:645-652）而言，三个越界 id 会同时拿到 (0,0)：`square_dimension({0,0})=={0,0}` 真、`inverse_dimension({0,0})=={0,0}` 真 ⇒ **pass**。同型：`unit_law_holds`（:86-91）恒真。
- 差在哪：兜底的动机（避免越界）正当，但兜成了**合法量纲**；"这个 id 不存在"与"这个量无量纲"被压成同一个答案。
- 后果：`G-STRUCT-UNIT-LAW` 失去对"单位身份未登记"的判据力。**方向已钉死**：越界 id ⇒ 返回 (0,0) ⇒ 二次律与倒数律自动成立 ⇒ 恒绿（偏绿），不会误拦。可达性诚实说明：本批检索未见把整数转成 UnitId 的调用点（`git grep -n "static_cast<UnitId>\|UnitId>(" -- '*.cpp' '*.h'` 零命中），故当前需要新增一条"从产品/枚举反序列化单位 id"的路径才触发。
- 定级：**S3**（不可达；但它是门本身的判据力缺陷，属 AGENTS §9 "门禁…不合理时改进门禁本身"）
- 怎么算修好：① 加显式 `UnitId::unknown` 行（dim 用不可运算的哨兵，例如 `adu_power = INT_MIN`）并让 `square_dimension/inverse_dimension` 对越界幂次传播为"不成立"；② 或在 `frozen_unit` 前置 `is_valid_unit_id(id)`，非法则 `gate_unit_law` 直接 `fail(g, "unregistered UnitId")`；③ 必须同批改：`v6_drizzle_science.h:102` 的返回类型 `const FrozenUnit&` 无法表达失败 ⇒ 需同时改签名（返回指针或 bool 出参），否则改了兜底也传不出去；④ 必须同批补负例：`frozen_unit(static_cast<UnitId>(9999))` ⇒ 门判红（当前恒绿），并把该负例接到 `--self-test`（AGENTS §9）。
- 置信：CONFIRMED（恒真链已复算：三处同返 (0,0) ⇒ 两律皆成立 ⇒ pass）

### CR-40-14 · `DrizzleOperator::build` 接受零交叠集合并返回 ok， resulting 算子的方差/通量恒 0，"零覆盖"与"覆盖完美但信号为 0"不可分
- 轴：R（主轴）＋ D（本头 :156 声称的 `coverage_mismatch // D_p 与 x 贡献不一致（零覆盖却贡献）` 未覆盖其反面）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp:219`
  ```
  if (n_src == 0 || n_dst == 0) return DrzError::invalid_argument;
  ```
  与之配套的空洞：`:250`（`for (const OperatorEntry& e : overlaps)` 循环体在空集合下不执行）、`:257-259`（`D_p_[p] > 0.0` 对全 0 恒假）
- 上位依据：本头 :176/:156 把"零覆盖"列为具名失败面（`coverage_mismatch`），但实现的覆盖判定是**逐 dst 且以 overlaps 的键集为定义域**——从未被任何 entry 触及的 target 落在判定域之外；条文面未规定"空 overlaps 是否合法"（检索式：`git grep -n "coverage_mismatch\|零覆盖" -- docs/`，零命中）。
- 现状：`build(n_src>0, n_dst>0, sources, pixfrac, kind, {}, out)` 返回 `ok`，得到 `D_p_≡0, N_p_≡0, rows_ 全空` 的算子；`signal_sb/variance_sb/flux_out/parent_variance_exact` 一律 0.0，`ivar_sb` 因 var≤0 返回 0.0，各门（gate_sb_definition / gate_variance_identity / gate_flux_conservation）在 `x≡0` 或 claim 全 0 时**全部 pass**（`rel_diff(0,0)=0` 见 :27）。
- 差在哪：一个空算子满足全部"恒等式门"——这些门的形式是"实现值 == 重算值"，两者同为 0 时恒真。判据方向：**偏绿**（漏掉的 target 只会让量变小或为 0，不会让任何相对残差变大）。
- 后果：装配层若因候选查询/索引映射失误给出空交叠，产品可以"全 0 且门全绿"落盘；`FZ-COND-FLUX-CONSERV` 的 `Phi_out == Σ x` 在 x≡0 时同样无判据力。可达性：本批文件内部不产生空集合（`v6_spherical_overlap.cpp:189` 已判 `n_dst == 0`；且 :170-185 只在有 hits 时 push），风险面在直接调用 `build` 的一方。
- 定级：**S3**（v6 未接生产，且本层适配器已挡住空集合；缺的是"确有非适配器调用方传空 overlaps"的证据——检索式：`git grep -n "DrizzleOperator::build" -- lib/ eng/` 命中仅 `phase1_product.cpp` 经适配器、`eng/tests/unit/v6_p1_drz/v6_p1_drz_test.cpp:242/357/454/489/520/531/542`）
- 怎么算修好：① `build` 增判据 `if (overlaps.empty()) return DrzError::coverage_mismatch;`；② 更彻底：要求 `n_dst` 中**每一个** p 都出现在 overlaps 键集内（现允许部分 p 全空），否则具名失败——但须与本头 :156 的语义一起改（"零覆盖却贡献"应扩为"零覆盖"），并在 `docs/` 侧登记该门的正向约束；③ 必须同批改：各门补一条"非退化"前置（AGENTS §5"判据必须非退化"）——如 `gate_flux_conservation` 要求 `Σ_j x_j ≠ 0` 或显式接受"全零输入 ⇒ 该门不适用并记 unavailable"，否则 0==0 的恒真门继续充当证据；④ 必须同批补负例：空 overlaps 与"半数 target 无覆盖"两种形状 ⇒ 断言具名失败。
- 置信：CONFIRMED（空集合下的控制流与门的恒真性已逐步复算）

### CR-40-15 · `gate_constant_surface_brightness` 内并存 1e-9 与 1e-3 两个数值，前者无条文出处，失败文本又写死后者
- 轴：S（主轴，数值无出处）＋ C（文本与实际容差脱钩）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp:423`
  ```
  if (!is_finite(x[j]) || rel_diff(x[j], expect) > 1e-9) {
      return fail(g, "x_j != B0 * A_pixel_j (constant-ADU construction)");
  ```
  以及 `:436`
  ```
  return fail(g, "|S_p/B0 - 1| >= 1e-3 (frozen tolerance)");
  ```
- 上位依据：`FZ-GATE-CONST-SB` 只有一个数值——本头 :41 逐字 "容差 \|S_p/B0 - 1\| < 1e-3（沿用，不改数值）"，`docs/science/DRIZZLE.md:123` 与 `docs/algorithms/DRIZZLE_GEOMETRY.md:371` 同值。1e-9 在条文面零命中（检索式：`git grep -n "1e-9" -- docs/science/DRIZZLE.md docs/algorithms/DRIZZLE_GEOMETRY.md docs/algorithms/GATES_AND_TOLERANCES.md`，唯一命中 `docs/science/DRIZZLE.md:174` 是 healpy 面积对拍 rtol，不同对象；另 `docs/algorithms/GATES_AND_TOLERANCES.md:63` 的 "1e-9 px 仅为 JSON 浮点往返护栏" 也非本量）。
- 现状：门内 (1) 段用 **写死的** 1e-9 判"输入是否真是面亮度构造"，(2) 段用形参 `rel_tol`（默认 1e-3）判 S_p；1e-9 不可由调用方调，也不在本头 :41 的任何冻结清单里。
- 差在哪：同一个门 `FZ-GATE-CONST-SB` 的 ID 承载两个数值，其中一个是**没有出处的经验值**，而条文只声明了一个且明写"不改数值"；失败文本又把 `rel_tol` 谎称成 1e-3。
- 后果：① 对外主张"常量 SB 场门按冻结容差通过"含糊化——真正决定该门能否通过的是未登记的 1e-9（构造断言失败时整个门红，与被冻结的 1e-3 无关），复核者按条文找数值会找不到对应判据；② 若有人把默认 rel_tol 改成 1e-6 收紧（本头 :300 允许），失败信息仍报 "1e-3 (frozen tolerance)" ⇒ 证据文本与实际判据矛盾，属 §4 之外的问题（文本误导复核）。**方向**：1e-9 比 1e-3 严 ⇒ 该子判据偏红，不会造成假绿。
- 定级：**S3**（不产生产物错值；损伤的是可复核性与门 ID 的唯一语义）
- 怎么算修好：① 给构造断言的数值一个身份：写 `constexpr double kConstructionRelTol`，或把条文补成"门含两段：构造断言 rel<1e-9（推导：x_j 由 `B0*A_pixel_j` 逐字构造，FP64 乘法残差量级）+ SB 断言 rel<1e-3"，两处同时落地；② :436 改成把 `rel_tol` 值格式化进字符串；③ 必须同批改：本头 :41 与 `docs/science/DRIZZLE.md:123/:169` 的门描述（否则新增常数又是"代码有、条文无"）；④ 必须同批补：以 `rel_tol=1e-6` 调用一次并断言失败文本含实际数值（现有 `eng/tests/unit/v6_p1_drz/v6_p1_drz_test.cpp:290` 只判 1e-3 档的布尔结果）。
- 置信：CONFIRMED（两个数值与条文面检索结果均已核实）

### CR-40-16 · `flux_conservation_factor` 无条件返回 1.0，而它成立的前提（几何闭合）不在算子构建路径上被校验
- 轴：D（主轴，条件恒等被写成无条件恒等）＋ S
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp:143-146`
  ```
  double flux_conservation_factor(double pixfrac) {
      (void)pixfrac;
      return 1.0;
  }
  ```
  配套：`:252-254` 用 `pixfrac²·A_pixel[e.src]` 当 A_drop（而非几何实测 `drop_area`），且 `build`（:213-277）全程不调用 `validate_overlap_closure`
- 上位依据：本头 :36-38 "FZ-COND-FLUX-CONSERV Phi_out = Σ_p Σ_j w_jp x_j = Σ_j x_j （与 pixfrac 无关）；flux_conservation_factor = 1（**口径标记**；必须落 provenance）"——键名本身带 "COND"（条件），且 :37 的恒等推导依赖 :30 的 `Σ_p w_jp = 1`；`v6_spherical_overlap.h:13` 逐字 "FZ-COND-FLUX-CONSERV Sum_p a_jp = A_drop_j = pixfrac^2 * A_pixel_j"。
- 现状：`DrizzleOperator` 层面 a_jp 与 A_pixel 的关系**不受约束**（`build` 只判有限性与正性），而 factor 与 provenance 记录（`phase1_product.cpp:299-300` `has_flux_conservation_factor=true; flux_conservation_factor=1.0`）都按"恒 1"写。
- 差在哪：条件性被实现方的调用顺序（先 `compute_overlap_row` 已判闭合）满足，而不是被 `DrizzleOperator` 自身满足；因此**沿 `build` 直接调用这条路（`v6_drizzle_science.h:214` 的公开入口，测试有 8 处）该因子可以是假的**。
- 后果：对外主张"绝对通量已按守恒因子 1 折算"在缺少闭合证据时不成立。可拦性核查（诚实边界，故本条不升档）：`gate_flux_conservation`（:529-548）确实拿 `flux_out(x)` 与 `1.0·Σx` 实测比较 ⇒ 若闭合破了，`Phi_out ≠ Σx` 会判红（前提是该门被调用且 `x` 非全零——后者见 CR-40-14）。所以本条的剩余危害是"provenance 里的 1.0 是声明而非测量"，而非无人检查。
- 定级：**S3**（有实测门兜住；缺的是"门被跳过/输入全零"的可达性证据，故不判 S2）
- 怎么算修好：① provenance 的 factor 值改由 `flux_out(x)/Σx` 的**实测值**（或"实测闭合残差 + 名义 1.0"两者并列）供给，`flux_conservation_factor(pixfrac)` 明确改名为 `nominal_flux_conservation_factor()` 并在注释里写"名义值，实测见 provenance.closure_residual"；② `build` 增可选闭合前置（把 `closure_rel_tol` 传入并逐 src 判），使"因子=1"成为构造保证；③ 必须同批改：本头 :36-38 补一句"该因子的成立条件由谁校验"，否则改动后条文仍读成无条件恒等；④ 必须同批补负例：故意把某 a_jp 放大 1%（使 `Σ_p a_jp > pixfrac²A_pixel`）⇒ 断言 provenance 侧的实测因子 ≠ 1 且门判红。
- 置信：CONFIRMED（`build` 内无闭合校验已逐行读；provenance 字面量见 `phase1_product.cpp:299-300`）

---

# 2. lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.h


## 2.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :26 | `B_j = x_j / A_pixel_j` | — | 面亮度定义（源像素 → SB） | 公式导出 | `docs/science/DRIZZLE.md:123` 冻结常量场面亮度构造；F&H 2002（文献池 #9 `SCIENTIFIC_REFERENCES.md:23`）§7.2 的 a_jp 记法；代码真按式算（`v6_drizzle_science.cpp:253/:254`） | — | CONFIRMED |
| :26-29 | `S_p = Σ_j B_j a_jp / Σ_j a_jp = Σ_j c_jp x_j`，`c_jp = w_jp/N_p` | — | v6 主算子式 | 公式导出 | 本行自称 FZ-FORMULA-DRIZZLE-SB；等价性已复算（drop 归一 `N_p=D_p/pixfrac²` 与 sb 归一 `N_p'=D_p` 给出同一 `c=a/(A_pixel·D_p)`，见 `v6_drizzle_science.cpp:148-150/:252-255`）⇒ 合规 | — | CONFIRMED |
| :30 | `Σ_p w_jp = 1`（drop 归一下） | 1 | 核权重的归一恒等 | 公式导出 | F&H 2002 §7.2 式(7) 下方 a 的定义（本行逐字引用"the fractional area overlap of **the drop**"并指 `drizzlepac cdrizzlebox.c dover/=jaco`）；**该逐字引用本批未对一手文献复核**（文献池 #9 已登记 F&H 2002，用途栏含"drop、pixfrac、线性重建、相关噪声"） | — | PARTIAL（缺：F&H §7.2 式(7) 原文逐字对读） |
| :32 | `variance_p = Σ_j c_jp² v_j`；`x→αx ⇒ var→α²var, ivar→ivar/α²` | 2 | 方差与缩放律 | 公式导出 | 线性算子的标准误差传播（c 为常数系数 ⇒ 二阶矩乘 c²）；实现 `v6_drizzle_science.cpp:287` 按式算 | — | CONFIRMED |
| :35 | `Cov(S_p,S_q) = Σ_j c_jp c_jq v_j` | — | 相关传播 | 公式导出 | 输入像素独立假设下的双线性型；实现 `v6_drizzle_science.cpp:296-314` 按式算（归并两稀疏行） | — | CONFIRMED |
| :37 | `Phi_out = Σ_p Σ_j w_jp x_j = Σ_j x_j`（与 pixfrac 无关） | 1 | 条件通量守恒恒等式 | 公式导出 | 恒等成立**前提是** `Σ_p a_jp = pixfrac²·A_pixel_j`（本行称"条件"，与 :47 的 `flux_conservation_factor = 1（口径标记）` 配对）；代码 `.cpp:253` 用 `pixfrac²·A_pixel` 当 A_drop，故恒等只在闭合成立时真 | — | CONFIRMED |
| :41 | 容差 `\|S_p/B0 − 1\| < 1e-3` | **1e-3** | 常数面亮度冻结门 | 文献值（仓内条文面） | `docs/science/DRIZZLE.md:123` 与 `:169` 逐字 `\|S_p/B0−1\|<1e-3`；`docs/algorithms/DRIZZLE_GEOMETRY.md:371` 同值。本行注释"（沿用，不改数值）"⇒ 数值与条文一致 | — | CONFIRMED |
| :48-49 | `pixel_area_power(-2 signal / -4 var)` | −2 / −4 | BUNIT 量纲可判的幂次要求 | 公式导出 | 面亮度 = 通量/立体角 ⇒ signal ∝ sr⁻¹、variance ∝ sr⁻²；本仓以"px"承载立体角幂次（:67 注释"canonical 串写 sr"），故 −2/−4；实现 `.cpp:604-606` 按式判 | — | CONFIRMED |
| :69,:70 | `UnitDimension::adu_power = 0` / `px_power = 0` | 0 | 量纲向量初值 | 结构性不适用 | 幂次初值；但**字段名 `px_power` 装的是立体角幂次**（:67 注释），与 `UnitId::pixel_area = "px^2"` 混用是 CR-40-6 的误标来源 | — | CONFIRMED |
| :148 | `ok = 0` | 0 | 枚举 0 值 = 成功 | 结构性不适用 | C 风格错误码惯例；本文件所有失败面返回非 0 具名码 | — | CONFIRMED |
| :155 | `overlap_exceeds_drop // Sum_p a_jp > A_drop_j（几何闭合失败上溢）` | — | 超额侧具名码 | 文献值（仓内条文面，弱锚） | 与 `:165-167` 的 `overlap_area_deficit` 成对；两者由 `.cpp:204/:205` 分向触发；ID `DRZ-PF-CORRECT-01` 在 `docs/` 零命中（见 §4.1 对应行） | — | CONFIRMED |
| :199 | `a_jp [px^2]` | px^2 | 交叠面积单位标注 | 待确认（**不一致**） | 见 CR-40-6：几何正本给 sr（`spherical_overlap.h:242-243`） | 保守方向：改 `[sr]`；影响范围＝整条 SB 单位链 | CONFIRMED |
| :205 | `A_pixel [px^2]（pixfrac=1）` | px^2 | 源像素面积单位标注 | 待确认（**不一致**） | 见 CR-40-6：`spherical_overlap.h:93-105` 正本为"球面度 (sr)" | 同上 | CONFIRMED |
| :205/:227-231 | `A_pixel_ / D_p_ / N_p_` 三个 `std::vector<double>` | — | 逐源/逐目标几何量 | 结构性不适用 | 容器无固定尺寸，无科学数值 | — | CONFIRMED |
| :263 | `pixfrac_ = 1.0` | 1.0 | 成员默认（build 必覆盖） | 结构性不适用 | `build`（`.cpp:218`）先 `validate_pixfrac` 再赋值，默认值不可能进产品 | — | CONFIRMED |
| :264 | `kind_ = NormalizationKind::drop_area` | — | 默认参数化 = canonical | 文献值（仓内条文面） | :23-30/:186-188 冻结 FZ-FORMULA-DRIZZLE-SB 指定 drop_area 为 canonical；两参数化同一 c_jp ⇒ 默认值无数值后果 | — | CONFIRMED |
| :293 | `gate_sb_definition(..., double rel_tol = 1e-11)` | **1e-11** | 声称 signal 与 raw 重算的相对容差 | 待确认 | 条文面零命中：`git grep -n "1e-11\|1e-12" -- docs/science/ docs/algorithms/`（命中的 1e-11/1e-12 属别的门，无本门）；数值语义上等于"FP64 重算应逐位近似一致" | 保守方向：更小（1e-12~1e-13）；放宽会把真算错的 signal 判绿 ⇒ 偏绿风险 | 待确认 |
| :300 | `gate_constant_surface_brightness(..., double rel_tol = 1e-3)` | 1e-3 | 常数 SB 门默认 | 文献值（仓内条文面） | 同 :41；实现 `.cpp:435` 用 `>=` 触发失败，方向正确（`<` 成立才通过） | — | CONFIRMED |
| :306 | `gate_variance_identity(..., rel_tol = 1e-11)` | 1e-11 | 方差恒等容差 | 待确认 | 同 :293（无条文数值） | 同 :293 | 待确认 |
| :313 | `gate_variance_scale_law(..., rel_tol = 1e-11)` | 1e-11 | α² 缩放律容差 | 待确认 | 同 :293 | 同 :293 | 待确认 |
| :320 | `gate_covariance_propagation(..., diag_rel_tol = 1e-11)` | 1e-11 | 协方差对角/PSD 判据容差 | 待确认 | 同 :293；**且该 tol 在实现里被乘上带量纲的 `max(scale,1.0)`** ⇒ CR-40-12 | 同 :293；另需去掉绝对地板 | CONFIRMED（缺陷在 .cpp） |
| :327 | `gate_flux_conservation(..., rel_tol = 1e-11)` | 1e-11 | Φ_out 对 Σx 的容差 | 待确认 | 同 :293 | 同 :293 | 待确认 |
| :337 | `owner_signed_threshold = 0.0` | 0.0 | 父级归约的签字阈值 | 待确认（默认与签字位耦合） | 本行只有形参默认；配套 `threshold_owner_signed = false`（:338），实现 `.cpp:559-563` 仅在 `threshold_owner_signed` 为真时比较 ⇒ 默认下 0.0 **永不参与判定**（不误伤），但也意味着"有默认数值却无条文" | 保守方向：去掉默认值（未签字不得有数值）；影响范围＝FZ-GATE-PARENT-VAR 的可审计性 | CONFIRMED |
| :370-371 | `bool unavailable = false; std::string unavailable_reason;` | false / "" | "未测／不可估计"的正式表示面 | 文献值（仓内条文面） | 实现 `.cpp:624-626`：`unavailable==true` 且 reason 为空 ⇒ fail。方向：偏红；这是本批唯一把"不可估计"建成独立布尔面的设计（对比 CR-40-11 用 0.0、CR-40-14 用全零算子表示"没有值"） | — | CONFIRMED |
| :376-377 | `bool has_kcorr = false; double k_corr = 0.0;` | 0.0 | k_corr 的"未填"默认值 | **不一致** | 权威域：`docs/algorithms/PHASE2_SAMPLER.md:52` "定义域 **1 < k_corr**（无量纲）"、`:306-307` "k_corr = 1 ⇔ 忽略相关…k_corr < 1 ⇔ 物理不可达"。⇒ 0.0 既不在域内，又是结构默认值，而门只钉 `== 1.0`（`.cpp:639`）见 CR-40-11 | 保守方向：域判据改 `k_corr > 1`；影响范围＝control 方差与对外深度主张 | CONFIRMED |
| :378-379 | `bool kcorr_has_domain; bool kcorr_zero_correlation_justified;` | false | 适用域/零相关 justification 开关 | 文献值（仓内条文面） | `docs/algorithms/PHASE2_SAMPLER.md:316-317` "`k_corr_applicability_domain` 为空即 rc=7"；本头文件字段名 `kcorr_zero_correlation_justified` 说的是"零相关"，而它守护的判据是 `k_corr == 1.0`（"单位相关"）——名与义不合，见 CR-40-11 的"怎么算修好"③ | — | CONFIRMED |
| :385 | `gate_provenance_minimal_set(prov, double expected_flux_conservation_factor)` | 无默认 | 期望折算因子由调用方给 | 待确认 | 实现 `.cpp:633` 用 `1e-12` 相对比较；调用侧 `phase1_product.cpp:1199` 独立写死同值 1e-12 与期望 1.0（两处一致 ⇒ 无漂移，但两处都无条文导出） | 保守方向：由 `.cpp:143-146` 的 `flux_conservation_factor()` 单点供给，勿在装配层再写 1.0 | CONFIRMED |

## 2.2 findings（v6_drizzle_science.h）

本文件名下无独立编号 finding；它的两处缺陷在 §2.1 表内已定位、指控文本随实现一并落在 §1.2：`:376-377`（`k_corr` 默认 0.0 无"未测"表示面）与 `:46`（冻结锚文字只写"取 1.0 才拒"）合入 **CR-40-11**；`:199/:205` 的 `[px^2]` 标注合入 **CR-40-6**。其余无可辩护指控；数值 `1e-11` 族（:293/:306/:313/:320/:327）无条文锚但方向为"更严"，统一按 `待确认` 记入 §2.1 而不另立 finding。

---

# 3. lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.cpp


## 3.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :19 | `kArcsecToRad = 3.14159265358979323846/180.0/3600.0` | π/648000 | 角秒→弧度换算 | 文献值 | 单位换算定义常数（§4 明列不入库）；与另一轨 `spherical_overlap.cpp:85 ARCSEC_TO_RAD = PI/(180.0*3600.0)` **数值逐位同、写法不同**（π 字面量也同） | — | CONFIRMED |
| :22 | `return std::sqrt(src.A_pixel);` 作为 `src_scale_rad` | √A_pixel | 传给自适应细分的"源像素边长（弧度）" | **不一致** | 形参契约 `spherical_overlap.h:226-228` "src_scale_rad: 源像素边长 (弧度)…通常传入 max_edge_rad"；另一轨按契约实现：`drizzle_engine.cpp:1451-1455` 真算 `max_edge_rad` 后于 :1483-1486 传入；`reverse_drizzle.cpp:362` 用 `0.5*(s1+s2)*(π/180)` | 保守方向：传真实弧度尺度；影响范围＝自适应细分收敛判据 ⇒ drop 顶点数 ⇒ 全部 a_jp | CONFIRMED（量纲不一致）／后果量级见 CR-40-8 |
| :23 | `return 1e-6;` | 1e-6 rad | A_pixel<=0 时的"保守初值" | 待确认 | 注释自述"保守初值；A_pixel<=0 时走固定采样路径，不依赖自适应阈值"；数值 1e-6 rad≈0.206″ 无任何条文/标定锚。**但该分支实际不可达**：唯一调用点 :45-47 在 `if (src.A_pixel > 0.0)` 内，且 A_pixel>0 ⇒ √A_pixel>0 ⇒ 永不返回 1e-6 | 保守方向：不适用（死值）；见 CR-40-10 | CONFIRMED（可达性已按调用点复算） |
| :32 | `if (!out \|\| !pixel_to_sky) return invalid_argument` | — | 空指针守卫 | 结构性不适用 | 指针有效性，无科学语义 | — | CONFIRMED |
| :33,:137 | `validate_pixfrac(...)` | 域 (0,1] | pixfrac 冻结域 | 文献值（仓内条文面） | `docs/algorithms/DRIZZLE_GEOMETRY.md:180` "pixfrac∈(0,1]"；实现 `v6_drizzle_science.cpp:181` | — | CONFIRMED |
| :49 | `std::max(1, src.samples_per_edge)` | 1 | 采样段数下界 | 结构性不适用 | 防 0 段死循环的实现选择；1 段 = 4 角顶点，与 `spherical_overlap.h:158` 一致 | — | CONFIRMED |
| :53,:58,:64 | `corners.empty()` / `!isfinite \|\| <= 0.0` 三处 | 0.0 | 空多边形与面积正值守卫 | 结构性不适用 | fail-closed 域边界，非容差；0 是面积的自然下界 | — | CONFIRMED |
| :59,:65 | 复用的错误码 `missing_wcs` / `invalid_source_area` | — | drop 面积非法→"缺 WCS" | 待确认（语义映射） | 面积非有限/≤0 被判为 `DrzError::missing_wcs`（`v6_drizzle_science.h:151` 该码语义是"缺 WCS / 尺度非法"）。**方向：偏红**（不掩盖失败），但 provenance 侧无法区分"没 WCS"与"球面多边形退化" | 保守方向：给 `invalid_drop_area` 单独立码；影响范围＝失败归因 | CONFIRMED |
| :63 | `out->drop_area / (src.pixfrac * src.pixfrac)` | — | A_pixel 反推式 | 公式导出 | 由 `A_drop = pixfrac²·A_pixel`（本文件 :13 FZ-COND-FLUX-CONSERV 逐字 "Sum_p a_jp = A_drop_j = pixfrac^2 * A_pixel_j"）取逆；代码真按式算 ⇒ 合规 | — | CONFIRMED |
| :70 | `hp.pixelResolutionArcsec() * kArcsecToRad` | — | leaf 分辨率（弧度），供 quick-reject | 公式导出 | `spherical_overlap.h:289-300` 约定由调用方 run-context 传入；与另一轨 `spherical_overlap.cpp:1523/:1615` 同式同值 | — | CONFIRMED |
| :78 | `a = quiet_NaN()`（受 `ASTROCS_V6_DRZ_FAULT=invalid_area` 触发） | NaN | 可执行负例注入 | 文献值（纪律条文） | AGENTS.md §9 "负例注入（能红能绿）"＋"门禁/判据本身不合理时改进门禁本身，配可执行正例/负例"；注入后必走 :90-93→:114-117 具名失败 ⇒ 不产生静默错值。**属 §4 不报项**，见 §12 | — | CONFIRMED |
| :90 | `!std::isfinite(a) \|\| a < 0.0` | 0.0 | "面积失效"判定 | 结构性不适用 | 球面多边形面积不可能为负/NaN ⇒ 0 与有限性是定义域边界 | — | CONFIRMED |
| :94 | `if (a == 0.0)` | 0.0（精确相等） | 良性零交叠判定 | 待确认 | 依赖上游 quick-reject **返回字面 0** 而非近似小量；本行 :84-85 注释自证该依赖指向 `spherical_overlap.cpp` 的 "quick-reject 返回 Scalar(0)" | 保守方向：若上游可能返回亚正常小值应改 `a <= tiny`；影响范围＝n_zero_overlap 计数（不进 sum，无数值后果） | PARTIAL（缺：我未逐行读 `spherical_overlap.cpp` 的 quick-reject 返回值，不属本批清单） |
| :114 | `if (out->n_area_rejected > kMaxInvalidAreaHits)` | 0 | 失效面积数上限判据 | 文献值（仓内条文面） | 判据方向已钉死：计数只增不减 ⇒ 只可能更**偏红**；与 :60-61 "默认 fail-closed" 一致 | — | CONFIRMED |
| :116 | `out->closure_rel = 0.0;` | 0.0 | 失败路径下把闭合残差写 0 | 待确认（可诊断性） | 失败时真值未算，但 `OverlapRow::closure_rel` 与 `OverlapDiagnostics::max_abs_closure_rel` 均以 0 为初值 ⇒ **产品 provenance 里"失败帧"与"闭合完美帧"同为 0，不可区分** | 保守方向：失败路径写 NaN；影响范围＝几何诊断的可信度（不参与数值） | CONFIRMED |
| :120,:166 | `validate_overlap_closure(..., closure_rel_tol, ...)` 调用两次 | 同一 tol | 行内一次 + 装配时再一次 | 待确认 | 数值见 §4.1 的 `closure_rel_tol` 行（无条文锚）。两次调用的输入分别是 `sum`（:111-112 去重后重算）与 `rows[j].sum_a_jp`（:113 刚由 sum 赋入），**同一量 ⇒ 第二次恒等于第一次**，不新增判据力 | 保守方向：删除冗余或让第二次取更严值；影响范围＝无（重复判定） | CONFIRMED（复算：:111-113） |
| :136,:139 | `sources.front().pixfrac` / `if (s.pixfrac != pixfrac)` | 精确不等 | "所有源同一 pixfrac"约束 | 结构性不适用 | 同一性约束而非容差；浮点精确比较用于"必须逐位相同"，方向偏红 | — | CONFIRMED |
| :163 | `std::max(diag.max_abs_closure_rel, std::fabs(rows[j].closure_rel))` | — | 诊断极值累加 | 公式导出 | 取绝对值最大者；代码按定义算 | — | CONFIRMED |
| :189 | `if (n_dst == 0) return coverage_mismatch` | 0 | 无 target 拒绝 | 结构性不适用 | 空算子拒绝，方向偏红 | — | CONFIRMED |
| :190 | `n_dst > 0xFFFFFFFFull \|\| sources.size() > 0xFFFFFFFFull` | 2³²−1 | 降为 uint32 前的溢出守卫 | 结构性不适用 | 索引宽度上限（`OperatorEntry::src/dst` 为 uint32，`v6_drizzle_science.h:197-198`） | — | CONFIRMED |
| :196 | `NormalizationKind::drop_area` | — | 写死 canonical 参数化 | 文献值（仓内条文面） | `v6_drizzle_science.h:23-30` FZ-FORMULA-DRIZZLE-SB 指定 drop 面积归一为 canonical；两种参数化给出同一 c_jp（已复算，见 §12）⇒ 写死不改变数值 | — | CONFIRMED |
| 合并行（浮点字面量） | `0.0`／`1` 单位元与初值 | :22,:34,:46,:47,:61,:73,:111,:116,:143,:148,:149 | 结构体/累加器初值与数组下标 | 结构性不适用 | 已逐位点列出；均为初始化或索引基数 | — | CONFIRMED |

## 3.2 findings（v6_spherical_overlap.cpp）

### CR-40-7 · ② 本侧 A_pixel≤0 分支退回固定 8 段采样，精确性弱于生产另一轨的自适应大圆弧细分，且闭合门结构上测不到这一维
- 轴：D（主轴）＋ S
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.cpp:49-51`
  ```
  const int n = std::max(1, src.samples_per_edge);
  corners = spherical::build_drop_polygon_sampled<double>(
      src.px, src.py, src.pixfrac, pixel_to_sky, user_data, n);
  ```
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md:105` "drop 四角: `half = 0.5·pixfrac`，`(x±half, y±half)` 经 WCS/SIP"；另一轨口径 `spherical_overlap.h:210-228`（改进3+5）逐字："SIP 投影: 递归细分到机器精度, 消除 SIP 畸变曲率面积误差"，`spherical_overlap.cpp:942` `wcs_epsilon = std::max(double(src_scale_rad) * 1e-12, 1e-11)`；生产调用点 `drizzle_engine.cpp:1483-1486` 只用自适应版。`spherical_overlap.h:195` 对固定采样的表述止于 "4 条边各采样 N 段"，未声称同等精度。
- 现状：本文件对 **A_pixel>0** 走自适应（:45-47），对 **A_pixel≤0** 走固定 n 段（:49-51），两条路径的顶点数与曲率保真度不同；`SourceDropSpec::samples_per_edge` 默认 8，且无处说明 8 段在本仓容差下够。
- 差在哪：同一个"球面交叠面积 a_jp"能力，本层允许退化到固定细分而生产侧是自适应细分；仓内对固定采样的精度**零登记**。
- 后果：SIP/宽场畸变下 drop 边被折成有限段大圆弧 ⇒ `drop_area`、全部 `a_jp`、以及由 `Σ_p a_jp = pixfrac²A_pixel` 决定的归一分母同时偏；产品侧对应 signal/variance 的空间分布。可拦性核查：**闭合门拦不住这一维**——`sum_a_jp` 与 `drop_area` 同源于被同一个欠分辨率多边形切割的结果，曲率细分不足在两侧同步出现。
- 定级：**S2**，按派单 ② 的口径写清"改接线即 S1"。理由：本层唯一产品侧调用者 `lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:515` 所属的 `astrocs_v6_phase1_product` 只被 `eng/tests/integration/v6_p1/CMakeLists.txt:20` 链接（检索式：`git grep -n "astrocs_v6_phase1_product" -- '*.txt' '*.cmake'`），生产正式末端是 `module_adapters.cpp:8283` 的 `hp_drizzle_run_phase1_hips`，其几何走 `drizzle_engine.cpp`＋`spherical_overlap.cpp`，**不经过本文件**。逐跳（接线后）：`phase1_product.cpp:515` → 本文件 :49-51 → `spherical_overlap.cpp` 的 `build_drop_polygon_sampled` → 本文件 :74-100 面积 → `v6_drizzle_science.cpp:213` 算子 → 产品 signal/variance。
- 怎么算修好：① 删除固定采样分支，A_pixel≤0 时先由 `hp` 的 leaf 面积或 WCS 尺度推出 `src_scale_rad`（弧度）后统一走自适应（与 CR-40-8 同一次改动）；② 若必须保留两分支，则给 `samples_per_edge` 一个**导出式**（例如按最大边角曲率使弦高 < 1e-3 像素）并写进 `docs/algorithms/DRIZZLE_GEOMETRY.md`；③ 必须同批改：闭合门需引入与被测 polygon 独立的一路（`docs/science/DRIZZLE.md:174` 已登记 `healpy` 球面多边形面积对拍，`rtol 1e-9`），否则"固定采样不够"在结构上测不到；④ 必须同批补负例：15° 宽场 + 强 SIP 的 drop，固定 8 段 vs 自适应，断言二者 a_jp 相对差被门判红。
- 置信：CONFIRMED（两分支例程与默认值已逐字对读；结构盲区由 `sum_a_jp` 与 `drop_area` 同源这一实现事实确定）

### CR-40-8 · ① 两版对同一形参 `src_scale_rad` 给了不同量纲的值（px vs rad），当前被下游绝对地板掩盖
- 轴：C（主轴）＋ R
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.cpp:21-23`
  ```
  double drop_src_scale_rad(const SourceDropSpec& src) {
      if (src.A_pixel > 0.0) return std::sqrt(src.A_pixel);
  ```
  消费点 `:45-47` `build_drop_polygon_adaptive<double>(..., drop_src_scale_rad(src))`
- 上位依据：形参契约 `spherical_overlap.h:226-228`；两版实际取值对照——生产版 `drizzle_engine.cpp:1451-1455`（`max_edge_rad` 由真实球面角距求得）、`reverse_drizzle.cpp:362`（`0.5*(s1+s2)*(π/180)`，度→弧度）；本版 `√A_pixel`，而 `A_pixel` 在本层既是 px²（:36 注释）又是 sr（实现事实，见 CR-40-6）。
- 现状：把 `A_pixel` 的面积开方当作"边长（弧度）"传给自适应细分。
- 差在哪：同一形参在两轨取不同量纲的数——正是派单 ① 要的"两版常数是否已经不一致"，且此处不只是数值差，是**单位差**。
- 后果：下游判据 `spherical_overlap.cpp:942` `wcs_epsilon = max(src_scale_rad*1e-12, 1e-11)`。代入：生产 s≈1e-6 rad ⇒ 1e-18，地板 1e-11 生效；本层 s=√A_pixel：若 A_pixel 以 sr 计（≈1e-7…1e-4）⇒ √≈3e-4…1e-2 ⇒ 仍 <1e-1 ⇒ 地板生效 ⇒ **两版 ε 相同，当前无实际偏差**；若 `A_pixel > 1e6` 则本层 ε 比生产**更松**（`1e-12·√A_pixel > 1e-11`），即更早停止细分。**方向钉死**：ε 变松 ⇒ 顶点数变少 ⇒ 只会让本侧比生产侧更不精确，不会更精确；不会误拦，只会误放（精度损失偏绿，不偏红）。
- 定级依据：**偏差当前被绝对地板吃掉 ⇒ 无可观测产物后果**。
- 定级：**S3**（若 `spherical_overlap.cpp:942` 的 `1e-11` 地板被移除或改成纯相对式，本条即升 S2、接线后 S1）
- 怎么算修好：① 本函数返回真实弧度：由 `hp.pixelResolutionArcsec()*kArcsecToRad` 或 drop 四角最大边角距（照 `drizzle_engine.cpp:1451-1455`）得出，不再用 `√A_pixel`；② 在 `SourceDropSpec` 上写明 A_pixel 单位（与 CR-40-6 同一次改动）；③ 必须同批改：`spherical_overlap.cpp:942` 的 `1e-11` 地板需同时登记出处或改写成相对叶分辨率，否则修了调用点仍看不出两版 ε 是否等价；④ 必须同批补：断言两轨在同一 WCS 下 **ε 相同**的对照测试（比较自适应返回的顶点数，而非比较面积，才能把这一维单独锁住）。
- 置信：CONFIRMED（两版取值与下游 ε 表达式均已复算；1e-6 rad 档代入为手算 1e-6·1e-12=1e-18 < 1e-11）

### CR-40-9 · ① 本层有、生产侧没有的"几何闭合门"既无默认值也无条文数值，两轨对同一几何缺陷一红一绿
- 轴：D（主轴）＋ C
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.cpp:120-122`
  ```
  const DrzError ce = validate_overlap_closure(sum, out->A_pixel, src.pixfrac,
                                              closure_rel_tol, &out->closure_rel);
  if (ce != DrzError::ok) return ce;
  ```
- 上位依据：条文面**零**闭合容差（检索式：`git grep -n "closure\|闭合" -- docs/algorithms/DRIZZLE_GEOMETRY.md docs/science/DRIZZLE.md docs/algorithms/GATES_AND_TOLERANCES.md`，命中的"闭合"是 WCS 往返闭合与 `Σ_p a_jp = A_drop` 的**恒等式声明**，无门容差数值）。生产侧对该恒等式**无任何执行面**（检索式：`git grep -n "closure\|闭合" -- lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp` → 零命中）。
- 现状：闭合门只存在于 v6 轨，容差由调用方给（测试给 1e-3：`eng/tests/unit/v6_p1_drz/v6_p1_drz_test.cpp:608/:620/:683`），本层不提供默认；生产轨 `drizzle_engine.cpp` 完全不判。
- 差在哪：同一份球面几何，两轨的有效性判据不对称——本批这一侧有 fail-closed 门，生产那一侧没有；且门的数值无出处。
- 后果：**对外主张不一致**。生产产品（`hp_drizzle_run_phase1_hips` 直写 HiPS）在候选漏选或面积亏损时可以全绿出货，而 v6 轨同输入判红。若两轨被当作"同一算法的两个实现"互证，则本门的 1e-3 会既当"已验证"的证据又无条文支撑。
- 定级：**S2**（本条只判"两轨不对称＋数值无出处"；"生产侧缺门"属另一批文件域，不构成本文件的独立指控）
- 怎么算修好：① 把闭合容差写进 `docs/algorithms/GATES_AND_TOLERANCES.md` 门表（含导出依据；`docs/algorithms/DRIZZLE_GEOMETRY.md:328` 已给同型量化 "θ<1e-3 时切平面偏差 <4e-8，球面 double 相消噪声 ~1e-4~5e-5"，可作 1e-3 量级来源起点）并给本层一个默认值；② 生产轨补同一道门（跨批项，需登记）；③ 必须同批改：本文件 :120 与 :166 的重复判定合并为一次（现同一输入判两次，见 §3.1 对应行）；④ 必须同批补负例：人为从 `candidates` 删一个 leaf ⇒ 断言 `overlap_area_deficit` 具名失败（现有负例是"面积置 NaN"，不覆盖"候选漏选"）。
- 置信：PARTIAL（缺：容差 1e-3 的出处；缺：生产轨是否在他处另有等价校验——我只 grep 了 `drizzle_engine.cpp` 一个文件，未读其全文）

### CR-40-10 · `1e-6` 兜底值所在分支按其自身写法不可达，注释却把它描述成生效的保守保护
- 轴：R（主轴）＋ D
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.cpp:23`
  ```
  return 1e-6; // 保守初值；A_pixel<=0 时走固定采样路径，不依赖自适应阈值
  ```
- 上位依据：条文面对"保守初值"零提及（检索式：`git grep -n "保守初值" -- docs/`；`git grep -n "1e-6" -- docs/algorithms/DRIZZLE_GEOMETRY.md docs/science/DRIZZLE.md`）。
- 现状：`drop_src_scale_rad` 的唯一调用点是 :45-47，位于 `if (src.A_pixel > 0.0)` 内；A_pixel>0 ⇒ `std::sqrt(A_pixel) > 0` ⇒ :23 永不执行。注释描述的 "A_pixel<=0 时…" 场景走的正是 else 分支（:49-51），那条路径**从不调用本函数**。
- 差在哪：注释声称的保护在结构上不生效；读代码的人会以为存在一个 1e-6 rad 的保守下界。
- 后果：无产物后果（死代码）。后果是可辩护性：一次针对该值的改动或标定（把 1e-6 当阈值去核）会得到零效应而被误当成"已标定"，并进入 §2.5 处置表成为假锚。
- 定级：**S3**
- 怎么算修好：① 删掉 :23 的兜底，把该函数改为只在 A_pixel>0 时存在的内联表达式（或 `std::optional` / `assert`），使"该分支不该存在"成为编译期事实；② 与 CR-40-8 合并为一次改动（同一函数体）；③ 必须同批改：头文件 :30-31 的 "此时几何闭合按构造精确成立" 应改为"该分支下闭合判据退化为同一多边形自身的自比，不构成 A_pixel 的证据"，否则 CR-40-7 的结构盲区会被这句注释当成已覆盖。
- 置信：CONFIRMED（可达性按调用点 :44-52 逐行复算）

---

# 4. lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.h


## 4.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :33,:34 | `SourceDropSpec::px / py = 0.0` | 0.0 | 源像素中心默认值 | 结构性不适用 | 出参结构的零初始化，非阈值；未填即用会落到 :53 的 `corners.empty()` 或算出错误位置，属调用方责任 | — | CONFIRMED |
| :35 | `pixfrac = 1.0` | 1.0 | drop 收缩因子默认（不收缩） | 待确认 | 域锚 `docs/algorithms/DRIZZLE_GEOMETRY.md:180` "pixfrac∈(0,1]"（本文件 :14 复述 "pixfrac in (0,1] 非法即拒绝"），但**默认取 1.0 这个选择没有条文**；1.0 是"不收缩"的中性值，同时是通量折算最平滑的档 | 保守方向：不给默认（强制显式传）；影响范围＝通量折算与 drop 面积 | PARTIAL（缺：pixfrac 默认值的权威条文面；本批未查 `eng/packaging/config/defaults.json`） |
| :36 | `A_pixel = 0.0` ＋注释 `[px^2]`（pixfrac=1） | 0.0 / px^2 | 0 ⇒ 走"由 drop 面积反推"分支；单位声明 | 待确认（数值）＋**不一致**（单位） | 数值 0 是分支哨兵（结构性）；单位注释与实现返回的球面度冲突 ⇒ CR-40-6 | 保守方向：单位注释改 `sr`；影响范围＝所有以本结构传 A_pixel 的调用方 | CONFIRMED（不一致部分已复算） |
| :37 | `samples_per_edge = 8` | **8** | A_pixel<=0 分支的 drop 边采样段数（4 边各 8 段 = 32 顶点） | 待确认 | 条文面零提及"8"。检索式：`git grep -n "samples_per_edge" -- docs/`；`docs/science/DRIZZLE.md:146` 只登记 `max_angle<1e-3 rad` 的切平面分支，未登记源 drop 的采样段数。同名参数在另一侧默认也是 8（`spherical_overlap.h:168` `int samples_per_edge = 8`），但那是**目标 HEALPix 赤道带边界**采样，与源 drop 是两个对象 | 保守方向：段数越大越准 ⇒ 若留固定采样则应升档或改走自适应；影响范围＝drop 面积与全部 a_jp ⇒ 见 CR-40-7 | PARTIAL（缺：8 段的面积误差上界实测） |
| :41 | `OverlapHit::target_ipix = 0` | 0 | NESTED 索引占位 | 结构性不适用 | ipix=0 是合法索引，故它不能当哨兵；本文件不用它作哨兵 | — | CONFIRMED |
| :42 | `OverlapHit::a_jp = 0.0` ＋注释 `[px^2]` | 0.0 / px^2 | 球面交叠面积 | 待确认（单位不一致） | 实际来源 `spherical::compute_overlap_area_g_ctx`，其声明单位是球面度：`spherical_overlap.h:242-243` "返回: 球面重叠面积 (球面度, steradian)" | 保守方向：改注释为 `sr`；影响范围＝闭合判据两侧量纲、w_jp 分母 | CONFIRMED |
| :46 | `OverlapRow::drop_area` 注释 `[px^2]` | px^2 | 球面 drop 多边形面积 | 待确认（单位不一致） | 同上：来源 `build_drop_geometry` 的 `geom.drop_area`，`spherical_overlap.h:264-266` 注释为"drop 球面面积"，微小 drop 分支用切平面 2D 面积（`docs/science/DRIZZLE.md:146`） | 保守方向：注释应写 "sr（微小 drop 时为切平面 2D 面积近似）" | CONFIRMED |
| :47,:48,:49 | `A_pixel / sum_a_jp / closure_rel = 0.0` | 0.0 | 行级诊断量 | 结构性不适用 | 出参累加量，零初始化；closure_rel 由 :120 的 `validate_overlap_closure` 写入 | — | CONFIRMED |
| :56,:57 | `n_area_rejected / n_zero_overlap = 0` | 0 | 计数面 | 结构性不适用 | 计数初值 | — | CONFIRMED |
| :62 | `constexpr kMaxInvalidAreaHits = 0` | **0** | 容许的"候选交叠面积失效"像素数上限（0 = 一个都不容许） | 文献值（仓内条文面） | 锚：:60-61 注释＋`DrzError::overlap_area_invalid`（`v6_drizzle_science.h:161-164`）自述的 "DRZ-PF-CORRECT-01 (S1 第 19 条)"；**但检索显示该 ID 只在代码与测试注释里**：`git grep -rn "DRZ-PF-CORRECT-01" -- docs/` 零命中（命中面：`eng/tests/unit/v6_p1_drz/{CMakeLists.txt,v6_p1_drz_test.cpp}`、`lib/.../tests/CMakeLists.txt`、`lib/algorithms/integration/v6_phase1/src/phase1_product.cpp`）⇒ 条文面无登记 | 保守方向：0 已是最严档，无更保守可取；缺口是"该 ID 在 docs/ 不存在"，故数值只有代码注释自证 | CONFIRMED（锚弱：ID 未上 docs 面） |
| :66,:67,:68,:69 | `OverlapDiagnostics` 四初值（0,0,0,0.0） | 0 | 诊断累加初值 | 结构性不适用 | 计数/极值初值 | — | CONFIRMED |
| :73,:79 | `closure_rel_tol`（形参，**无默认值**） | 由调用方 | 几何闭合相对容差 | 待确认 | 条文面零命中：`git grep -n "closure\|闭合" -- docs/algorithms/DRIZZLE_GEOMETRY.md docs/science/DRIZZLE.md docs/algorithms/GATES_AND_TOLERANCES.md`（无本门的数值行）；实际取值只有测试侧 1e-3 并附自述："相对残差可达 ~1e-3（与冻结常量场门同量级）"（`eng/tests/unit/v6_p1_drz/v6_p1_drz_test.cpp:606`） | 保守方向：数值应随 NSIDE/pixfrac/SIP 幅度**导出**而非定值；影响范围＝漏选候选与面积亏损能否被拦下 | PARTIAL（缺：真实帧上的闭合残差分布；未实跑） |
| :85-:93 | `build_operator_from_sources(..., closure_rel_tol, ...)` | 同上 | 算子装配入口的容差透传 | 待确认 | 同 :79；两层调用（`.cpp:120` 与 `.cpp:166`）对同一行用**同一个** tol 判两次 | — | CONFIRMED |
| :90 | `std::vector<uint64_t>* target_ipix_out = nullptr` 等三个可选出参 | nullptr | 诊断/映射回传开关 | 结构性不适用 | 可选出参指针初值 | — | CONFIRMED |

## 4.2 findings（v6_spherical_overlap.h）

### CR-40-6 · 本层与 .cpp 共同把球面几何的输出量纲标注为 px²，而冻结单位表要求 ADU/sr，按注释传值的调用方会得到面亮度带比例因子失真而字符串门面全绿
- 轴：C（主轴）＋ R
- 位点：`lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.h:42`
  ```
  double a_jp = 0.0;        // [px^2]
  ```
  同族：`:36` `double A_pixel = 0.0; // [px^2]（pixfrac=1）`、`:46` `drop_area ... [px^2]`、`:47` `A_pixel ... [px^2]`，以及 `v6_drizzle_science.h:199` `double a_jp = 0.0;   // 球面交叠面积 [px^2]`、`v6_drizzle_science.h:205` `double A_pixel = 0.0; // 源像素面积 [px^2]（pixfrac=1）`
- 上位依据：`v6_drizzle_science.h:10-15` 冻结单位表逐字："FZ-UNIT-SIGNAL-SB signal_sb = ADU/sr"、"FZ-UNIT-VAR-SB sb_variance_out = ADU^2/sr^2"；`docs/science/DRIZZLE.md:123` 冻结常量场面亮度门 `|S_p/B0−1|<1e-3`。几何侧的正本：`spherical_overlap.h:93-105` "未收缩源像素球面面积 A_pixel,j … 返回: 球面度 (sr)"、`spherical_overlap.h:242-243` "返回: 球面重叠面积 (球面度, steradian)"。⇒ 由 `S_p = Σ_j B_j a_jp/Σ_j a_jp`、`B_j = x_j/A_pixel_j`（`v6_drizzle_science.h:26-29`）要成立为 ADU/sr，A_pixel_j 必须是 sr。
- 现状：本层把 `a_jp`（球面度）连同 `A_pixel`（反推时 = `drop_area/pixfrac²`，也是球面度）标成 px²；`DrizzleOperator::build`（`v6_drizzle_science.cpp:228-232`）只验 `A_pixel > 0 且有限`，不验量级；`gate_bunit_semantics`（`v6_drizzle_science.cpp:594-608`）只判 BUNIT **字符串**的像素幂次（-2/-4）与二次律，不看任何数值。
- 差在哪：同一个量在几何正本里是 sr、在消费层注释里是 px²，而唯一的量纲防线是字符串面——按注释写的调用方传 px² 不会撞到任何声明性矛盾。
- 后果：面亮度链。若调用方真按 px² 传 `A_pixel`（例如 1.0），则 `B_j = x_j/A_pixel_j` 量纲为 ADU/px² ⇒ `signal_sb / sb_variance_out / sb_ivar_out` 全链带 `1 sr : 1 px²` 的比例因子，而产品 BUNIT 仍声明 ADU/sr ⇒ 对外"面亮度/方差/逆方差"三项主张同时失真。可拦性核查（诚实边界）：`validate_overlap_closure`（`v6_drizzle_science.cpp:192-207`）会比较 `Σ_p a_jp`（sr，典型 nside=512 leaf ≈1.3e-7 sr）与 `pixfrac²·A_pixel`（px²，典型 ≈1），rel≈−1 ⇒ **会**以 `overlap_area_deficit` 具名失败；但该防线在 `A_pixel<=0` 分支不成立——此时 A_pixel 由 sr 反推（`v6_spherical_overlap.cpp:61-63`），单位自洽而注释仍写 px²，**门全绿而注释是错的**。
- 定级：**S2**（v6 侧未接生产，见 §10 归属表；改接线即 S1，逐跳：`lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:515` → `v6_spherical_overlap.cpp:126` → `.cpp:61-63/:120` → `v6_drizzle_science.cpp:213` → 产品 signal/variance）
- 怎么算修好：① 把本批两文件内全部 `[px^2]` 标注改 `[sr]`（6 处，逐字位点已列）；② `UnitId::pixel_area` 的符号串（`v6_drizzle_science.cpp:68` `{UnitId::pixel_area, "px^2", {0, 2}}`）与 `UnitDimension::px_power` 命名需一并澄清——冻结表用 `px_power` 承载"立体角幂次（canonical 串写 sr）"（`v6_drizzle_science.h:67`），字段名与语义相反，正是本次误标的来源；③ 必须同批改：给 `DrizzleOperator::build` 加一条量级合理性检查（如 `A_pixel` 与 `hp.pixelArea()` 同量级），否则注释改完仍无防线；④ 必须同批补负例：以 px² 传 A_pixel 且**同时**把 a_jp 缩放到 px²（使闭合门放行）⇒ 断言仍有门判红，否则该缺陷只能靠注释防守。
- 置信：CONFIRMED（单位链已复算：`B_j=x_j/A_pixel_j`、`S_p=Σ B_j a_jp/Σ a_jp`，`v6_drizzle_science.h:26-29`）

---

# 5. lib/algorithms/drizzle/healpix_drizzle/wcs_sip.h


## 5.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| wcs_sip.h:19 | 像素基约定 `0-based / 1-based / CRPIX 1-based` | 偏移 1 | 声明入参像素坐标 0-based 而 CRPIX 1-based | 文献值 | 文献池 #56 Greisen & Calabretta 2002 §2.1.1（CRPIX/CRVAL/CD 的 1-based 定义性不变量）；实现落在 wcs_sip.cpp:250-251/315-316 | — | CONFIRMED |
| wcs_sip.h:45 | `double m_cdInv[4]` | 4 | 2×2 CD 逆矩阵的行优先存储长度 | 结构性不适用 | 它不是科学量：2×2 矩阵的元素基数，由维度而非任何容差决定 | — | CONFIRMED |
| wcs_sip.h:49 | 注释 `coeffs: A[36] 或 B[36] 或 AP[36] 或 BP[36]` | 36 | 系数组容量 = 6×6 | 待确认 | 容量本身是结构性的（定义见 fits_reader.h:12-15），**但它是 SIP 阶数上界 5 的唯一载体，且本头文件与 .cpp 都不写出这个上界** | 保守方向：显式写出 `order ≤ 5` 并在入口拒绝越界（现仅 hp_drizzle_api.cpp:103/465 有，本类无）；影响范围＝所有以 `WcsSip` 直接吃 `WcsParams` 的通道 | CONFIRMED（上界缺守卫部分见 CR-40-2） |
| wcs_sip.h:55,58 | `int order`（evalSipT/evalSip 形参） | 无界 | 多项式求和总阶上界 | 待确认 | 本头文件不声明合法域；`docs/algorithms/DRIZZLE_GEOMETRY.md:324` 登记在册偏差为 "api.h:93 注释 0..4 vs api.cpp 校验 [0,5]"，即合法域本身两处口径不一 | 保守方向：合法域取 [0,5]（与在册校验一致）并写进本头文件；影响范围＝畸变项阶数与内存越界 | CONFIRMED |
| wcs_sip.h:63 | 注释 `intermediate: [xi, eta] (度)` | 度 | 中间世界坐标单位声明 | 文献值 | 文献池 #15/#55 Calabretta & Greisen 2002 Paper II（中间世界坐标以度表出） | — | CONFIRMED |
| wcs_sip.h:20-22,54,65 | `pixelToSkyT<T>` / `evalSipT<T>` / `tanIntermediateToWorldT<T>` 的 T∈{float,double} 双实例 | 2 | FP32/FP64 双科学路径 | 结构性不适用 | 实例化基数，非容差；AGENTS §6 禁硬编码线程/ISA，精度路径按 RESCUE-FD-02 双轨，属设计选择 | — | CONFIRMED |

## 5.2 findings

本文件（头文件）除上表第 3、4 行指向的"合法域未声明"外无可辩护指控；该问题实质落在 .cpp，合并计入 CR-40-2 / CR-40-3。

---

# 6. lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp


## 6.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :9 | `M_PI` | 3.14159265358979323846 | 数学常数 π（20 位） | 文献值 | 数学定义常数；与 spherical_overlap.cpp:85 用的 `PI`、v6_spherical_overlap.cpp:19 用的字面 π 逐位同 | — | CONFIRMED |
| :17,:18 | `D2R` / `R2D` | π/180，180/π | 度↔弧度换算 | 文献值 | 单位换算定义常数（§4 明列不入库项） | — | CONFIRMED |
| :35,:36,:37 | `m_cdInv[0..3] = 0.0` / `m_cdDet = 0.0` | 0.0 | 逆矩阵与行列式的初值 | 待确认 | 初值本身结构性；**但 0.0 同时被 :50-51 用作"CD 奇异"的最终交付值**，而本文件 :209-210 明文禁止这一用法 ⇒ 见 CR-40-1 | 保守方向：奇异时写 NaN 而非 0.0，与 :213-214 同法；影响范围＝`skyToPixel` 全部输出 | CONFIRMED |
| :48 | `std::fabs(m_cdDet) < 1e-15` | **1e-15** | CD 行列式奇异判定阈（deg²/px²） | 待确认 | 与权威条文**冲突**：`docs/algorithms/DRIZZLE_GEOMETRY.md:180` 冻结为 "CD 行列式有限且 \|det\|≥1e-30"；同库另一实现 reverse_drizzle.cpp:285 逐字用 `1e-30` | 保守方向＝取 1e-30（条文值）。当前 1e-15 更严，但**严了不报错反而降级**（CR-40-1），故真正的保守缺口是行为不是数值 | CONFIRMED（见 CR-40-1） |
| :60-64,:65-66 | `%.6f %.3f %.3e %.3e` | 日志有效位 | 诊断打印精度 | 结构性不适用 | fprintf 格式宽度，不进任何数值路径 | — | CONFIRMED |
| :80 | `if (order <= 0) return T(0)` | 0 | 无 SIP 修正的下界开关 | 文献值 | SIP 语义：order=0 ⇒ 畸变为零，`docs/algorithms/DRIZZLE_GEOMETRY.md:292` "无 SIP → CTYPE 不含 -SIP 且不写任何 SIP 键" | — | CONFIRMED |
| :86,:88 | `for i in 0..order` / `for j in 0..order-i` | 下界 i,j=0 | 多项式求和域：含 (0,0)、(1,0)、(0,1) 项 | 待确认 | 条文面零提及"是否剔除 i+j<2 项"。检索式：`git grep -n "i + j >= 2\|i+j>=2\|二阶起\|A_00\|A_10\|A_01" -- docs/ lib/algorithms/drizzle/`（命中：ipv_wcs.cpp 只填 i+j≥2 的系数，无一处规定读侧必须跳过低阶项）；一手侧 `docs/references/SCIENTIFIC_REFERENCES.md:121`(#57 Shupe 2005) 登记为 **"核验状态：bibcode 级"**，即未核全文 | 保守方向：求和域下界取 i+j≥2；影响范围＝若外部头写 A_00/A_10≠0 则被当畸变吸收（平移/缩放被重复计入） | PARTIAL（缺：SIP 一手条文对 i+j<2 项的逐字处置；本仓文献池未核） |
| :89 | `coeffs[i * 6 + j]` | stride 6 / 容量 36 | 系数寻址；**阶数上界的唯一实际载体** | 待确认 | 6 与 36 是数组布局（结构性），但 `order≥6` 时同一表达式先串位后越界 ⇒ CR-40-2；权威链对"6×6 下三角"的表述见 `docs/algorithms/DRIZZLE_GEOMETRY.md:283`（只说"i*6+j"，未写合法域与越界行为） | 保守方向：本文件内加 `if (order > 5) return T(0)` 之前先由入口拒绝；影响范围＝readFits→WcsSip 通道 | CONFIRMED（复算索引：order=6,i=0,j=6→下标 6=A_1_0；i=6,j=0→下标 36 越出 double[36]） |
| :136 | `rho < T(1e-12)` | **1e-12 rad** | TAN 反投影切点保护（ρ 即角距） | 待确认 | 条文面零提及。检索式：`git grep -n "1e-12" -- docs/`（docs 侧仅命中别处容差，无本项） | 保守方向：更小（1e-15）更安全——阈值内直接返回 CRVAL 会丢掉一整圈像素；影响范围＝距切点 <1e-12 rad（≈2×10⁻⁷ arcsec）的针孔域，实际不可达 | 待确认 |
| :153,:154 | `sin_dec > 1 / < -1` 钳位 | ±1 | asin 定义域护栏 | 结构性不适用 | 定义域边界，非容差；不改变合法输入的结果 | — | CONFIRMED |
| :163,:164 | `2.0 * M_PI` 的 while 归一 | 2π | RA 归一到 [0,2π) | 文献值 | 定义常数；Paper II §2.1 的经度周期 | — | CONFIRMED |
| :211 | `!(cosc > 0.0)` | 0.0 | TAN 半球有效性边界（c=90°） | 文献值 | 文献池 #15/#55 Paper II（TAN 在 c=π/2 发散）；本行写法（NaN≠假 ⇒ 无定义一律拒）方向正确：cosc 为 NaN 时 `!(NaN>0)` 为真 ⇒ 拒绝 ⇒ 偏红 | — | CONFIRMED |
| :213,:214 | `quiet_NaN()` 哨兵 | NaN | 无定义投影的显式不可表示值 | 文献值 | 本文件 :209-210 自述口径："不得返回有限哨兵值 —— 哨兵值会被 isfinite 放行而静默映射到错误像素"（该口径与 AGENTS §5 fail-closed 一致）；**同一口径未被 :40/:241-244/:288-292 遵守**，见 CR-40-1/CR-40-4 | — | CONFIRMED |
| :219,:220 | `xi_rad = cdec·sdra/cosc`，`eta_rad = (cdec0·sdec − sdec0·cdec·cdra)/cosc` | — | TAN 正投影式 | 公式导出 | Paper II §5 TAN：ξ=cosδ sin(α−α₀)/cosc，η=(cosδ₀ sinδ − sinδ₀ cosδ cos(α−α₀))/cosc；代码逐项按式算，无写死 | — | CONFIRMED |
| :250,:251 | `x − T(m_wcs.crpix[0] − 1.0)` | 1.0 | 1-based CRPIX → 0-based 偏移 | 文献值 | 文献池 #56 Paper I §2.1.1；实现即 x₀−(CRPIX−1)=(x₀+1)−CRPIX，与 1-based 帧的 x−CRPIX 同 | — | CONFIRMED |
| :256,:307 | `if (m_wcs.sip.order > 0)` / `if (ap_order > 0)` | 0 | 前向/逆向 SIP 启用开关 | 文献值 | 同 :80；两侧各用各的阶（A/B 用 order，AP/BP 用 ap_order），与 fits_reader.cpp:394-395 的两阶记录一致 | — | CONFIRMED |
| :259,:260,:310,:311 | `dx += f; dy += g`（A 加在**像素域**偏移上） | — | 前向/逆向畸变项的合成位置 | 待确认 | 三面对同一数组给出三种量纲：本文件按"像素域"使用（A 的量纲 px^(1−i−j)）；`lib/algorithms/platesolve/cpp/ipv/src/ipv_wcs.cpp:364-366` 注释自陈"这是像素域约定；SIP 标准的 A_ij/B_ij 定义在中间世界坐标(度)上，量纲为 deg/像素^(i+j)，与本数组相差一个 CD 量纲"；`docs/contracts/DATA_SEMANTICS.md:373` 对同一 header KV 声明单位"无量纲" | 保守方向：本文件必须显式写出所读 A 的量纲并做一次 CD 换算（若上游为度域）；影响范围＝SIP 畸变项的整个幅度 | CONFIRMED（三面不一致）／一手标准怎么说＝UNPROVEN（见 CR-40-3） |
| :264,:265 | `xi = cd[0]·dx + cd[1]·dy` 等 | — | CD 线性映射（行优先） | 公式导出 | Paper I §2.1.1 中间世界坐标定义：ξ_i=Σ_j CD_ij(x_j−CRPIX_j)；代码按式算，cd[] 行优先布局与 fits_reader.h:22 注释一致 | — | CONFIRMED |
| :283,:306 注释 | `一次迭代通常足够 / 一次迭代即可` | 迭代 1 次 | 逆向 SIP 用 AP/BP 直加而非迭代反解 | 待确认 | 仓内在册的同类做法有另一条路并给了量化：`docs/algorithms/GATES_AND_TOLERANCES.md:69` 的迭代反演路径 τ=1e-9 px、实测最坏 2.91e-9 px；本文件是直加近似，条文未给该近似的误差上界 | 保守方向：对 AP/BP 做不动点迭代至 <1e-4 px（同表 :65 的全链冻结门）；影响范围＝world→pixel 方向（反向 drizzle、星表匹配像素位置） | PARTIAL（缺：本近似在真实畸变幅下的误差实测；未实跑） |
| :326 | `count <= 0` | 0 | 批量接口入参下界 | 结构性不适用 | 循环基数 | — | CONFIRMED |
| :341-346 | 显式实例化 `<float>`/`<double>` 各 3 处 | 6 | 精度双轨的实例化面 | 结构性不适用 | 模板实例化基数，非科学量；与 wcs_sip.h:20 的"阶段7 双实例"一致 | — | CONFIRMED |
| 合并行（浮点字面量） | `T(0)/T(1)/0.0/1.0` 单位元与初始化 | :35-37,:40,:82,:85,:87,:153,:154,:242,:290,:326 | 加法/乘法单位元、返回初值 | 结构性不适用 | 它们是 0/1 单位元而非阈值；表内已逐位点列出，合并判定 | — | CONFIRMED |

## 6.2 findings（wcs_sip.cpp）

### CR-40-1 · CD 行列式奇异时本文件把逆矩阵写成 0 并只打一条警告，用本文件自己禁止的"有限哨兵"方式把 skyToPixel 降级成恒等于 CRPIX
- 轴：R（主轴）＋ C（与权威条文数值不一致）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp:48`
  ```
  if (std::fabs(m_cdDet) < 1e-15) {
      WCS_LOG("警告: CD 矩阵行列式接近 0 (det=%.3e), 坐标转换将不可靠", m_cdDet);
      m_cdInv[0] = 0.0; m_cdInv[1] = 0.0;
      m_cdInv[2] = 0.0; m_cdInv[3] = 0.0;
  ```
  以及消费端 `wcs_sip.cpp:300-301`：`double dx = m_cdInv[0] * xi + m_cdInv[1] * eta;` → `wcs_sip.cpp:315-316`：`x = dx + (m_wcs.crpix[0] - 1.0);`
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md:180` 逐字："CD 行列式有限且 |det|≥1e-30"。同库另一实现遵守该值：`lib/algorithms/drizzle/healpix_drizzle/reverse_drizzle.cpp:285` `if (!std::isfinite(det) || std::fabs(det) < 1e-30 || ...) { error_msg = "ReverseDrizzle: WCS/CD 矩阵无效"; return false; }`。fail-closed 口径依据：本文件 :209-210 自己写下并执行的"必须显式拒绝：返回 NaN 让下游 isfinite 拦住。不得返回有限哨兵值 —— 哨兵值会被 isfinite 放行而静默映射到错误像素"。
- 现状：`skyToPixel` 无错误返回通道（`wcs_sip.h:29` 返回 void）。奇异/近奇异时它把 cdInv 全置 0，于是对**任意**输入天区都返回同一个有限像素值 `(crpix1−1, crpix2−1)`，函数返回成功，只留一行 stderr 警告。
- 差在哪：同一件事（CD 奇异）在本文件被实现为"降级为有限哨兵值"，在权威条文与同库另一实现被规定为"拒绝"；且本文件在 TAN 正投影处（:211-216）已论证有限哨兵值不可接受，两条实现互相否定。
- 后果：`skyToPixel` 的输出是像素位置。降级后所有天区落到参考像素一处 ⇒ 以它为输入的几何全部失真：反向 drizzle 的 target footprint 候选框（`reverse_drizzle.cpp:110`、`:188` 的 bbox）塌成一点，产品侧对应"目标平面像素 ↔ 球面 leaf"的重投影。同时该分支使 `DISP-DRZ-001` 一类"输入校验失败必须可判"的主张不成立——外层已按 1e-30 放行的 WCS，内层用 1e-15 静默改判为不可靠却不报错。
- 定级：**S2**，并注明"改接线即 S1"。理由：`WcsSip::skyToPixel` 的非测试调用点只有 `reverse_drizzle.cpp:110/188`，而其 C ABI 入口 `hp_drizzle_reverse_run`（`hp_drizzle_api.h:187`）在 `lib/infrastructure/` 零命中（检索式：`git grep -n "hp_drizzle_reverse_run\|reverse_run\|drizzle_reverse" -- lib/infrastructure/`），故生产调用路径当前不含本分支。**一旦反向 drizzle 接入调度器，逐跳路径即成立**：`lib/infrastructure/scheduler/src/module_adapters.cpp:8283`（同类正式末端调用点）→ `hp_drizzle_hips_api.cpp:43` → `hp_drizzle_api.cpp` 的 `run_drizzle_internal`（WCS 装配见 :424-450）→ `wcs_sip.cpp:33` 构造 → `wcs_sip.cpp:48` 降级 → `reverse_drizzle.cpp:110/188` 消费。
- 怎么算修好：① 把 :48 的数值改为与 `DRIZZLE_GEOMETRY.md:180` 一致的 1e-30（或把 1e-15 写进条文并说明为何两层不同）；② 奇异分支按 :213-214 同法把 cdInv 写成 `quiet_NaN()` 而不是 0.0，让下游 isfinite 拦住；③ 必须同批改的东西：`reverse_drizzle.cpp:255-334` 的校验清单需加"构造 WcsSip 后再往返自检"，否则外层 1e-30 与内层新阈值仍构成嵌套错配；④ 必须同批补的回归：对 det∈[1e-30,1e-15) 的合成 WCS，断言 skyToPixel 返回**非有限**（负例），避免改对反而把旧"有限"期望判红。
- 置信：CONFIRMED（复算：cdInv 全 0 ⇒ dx=dy=0 ⇒ x=crpix1−1、y=crpix2−1 与输入无关；阈值两面数值 1e-15 vs 1e-30 已逐字对读）

### CR-40-2 · SIP 阶数上界只由 6×6 存储布局隐式承载，本文件对 order 无上界守卫，order≥6 时寻址先串位再越界
- 轴：R（主轴）＋ C（合法域两处口径不一）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp:89`
  ```
  result += T(coeffs[i * 6 + j]) * pdx * pdy;
  ```
  循环上界 :86-88 `for (int i = 0; i <= order; i++)` / `for (int j = 0; j <= order - i; j++)`
- 上位依据：合法域的在册登记只有 API 层与一条被承认的偏差：`lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.cpp:103-104` `if (in->sip_order < 0 || in->sip_order > 5 || in->sip_ap_order < 0 || in->sip_ap_order > 5)`、`:465-467`（"正式支持 [0,5], 越界硬失败 (禁静默截断)"，:455 注释）；`docs/algorithms/DRIZZLE_GEOMETRY.md:324` 逐字登记 DISP-DRZ-001："`hp_drizzle_api.h:93 注释 sip_order "0..4"` vs `hp_drizzle_api.cpp:98-103 校验 [0,5]`"。一手侧对"阶数有无上界"：**条文面零提及**（检索式：`git grep -n "A_ORDER\|sip_order" -- docs/`；`docs/references/SCIENTIFIC_REFERENCES.md:121` 的 #57 Shupe 2005 标注"核验状态：bibcode 级"，未核全文）。
- 现状：本文件（类实现）内不出现任何 `order` 上界判定；`order` 由 `m_wcs.sip.order` 直通（:256/:307 只判 `> 0`），而 `fits_reader.cpp:288` 读 `A_ORDER` 时不钳范围（`a_order = parseInt(v)`），`fits_reader.cpp:162` `if (i < 0 || i > 5 || j < 0 || j > 5) return 0;` 把下标越界的系数**静默判为"非 SIP 键"丢弃**（无警告、无错误返回）。
- 差在哪：上界 5 是存储布局的副产品而不是被声明的合同，守卫只装在两个 C ABI 入口之一；`readFits → WcsParams → WcsSip` 这条同样存在于本库的通道（`hp_drizzle_api.cpp:236/253/284` 调 readFits）绕过了守卫，于是"禁静默截断"的口径在它指向的那一侧（读头侧）没有实现。
- 后果：`order=6` 时 :89 的第一处错误不是越界而是**串位**：i=0,j=6 → 下标 6 = `A_1_0`，于是把线性项系数乘在 dy⁶ 上参与求和——数值有限、不崩溃、不报错，直接进入像素→天球映射；`order≥7` 才走到 `coeffs[i*6+j]` 越界读（数组仅 36 项）。前者比后者危险：它产出确定的错值。
- 定级：**S2**。缺运行时触发路径的生产证据：本通道上游 `hp_drizzle_run` 自述零生产调用者（`drizzle_engine.cpp:1157` 注释："本路径为 legacy .hiss 容器出口（hp_drizzle_run 零生产调用者"），而正式末端 `hp_drizzle_run_phase1_hips` 走的是 KV+`[0,5]` 守卫侧（`hp_drizzle_api.cpp:455-467`）。改接线（或任何新调用方以 readFits 结果直接构造 WcsSip）即 S1。
- 怎么算修好：① 本文件 :79 入口加 `if (order > 5) order = ...`——**不要截断**，与 :455 "禁静默截断"同口径，改为 `evalSipT` 返回 NaN 或由调用方拒绝；② 把上界写成 `static_assert`/`constexpr` 并把 `SipCoeffs` 的 36 与 stride 6 由同一个 `kSipMaxOrder+1` 导出，使布局与合法域不可能再各自漂移；③ 必须同批改：`fits_reader.cpp:162` 的静默丢弃要变成具名失败，`hp_drizzle_api.h:150` 的 "0..4" 注释与 `DRIZZLE_GEOMETRY.md:324` 的 DISP-DRZ-001 一起收口（否则文档-代码偏差从两条变三条）；④ 同批补负例：`A_ORDER=6` + 仅 `A_6_0` 有值的头，断言拒绝而非产出确定值（现有 `tests/p0_sip_order_guard_test.cpp` 只覆盖 KV 通道与 A_ORDER=8/−1/6/5 的 **rc=−10** 面，未覆盖 readFits 通道）。
- 置信：CONFIRMED（复算下标：order=6 → (i,j)=(0,6)→6、(6,0)→36；double[36] 合法下标 0..35）

### CR-40-3 · 本文件按"像素域"使用 SIP 系数，而上游产出与合同声明给的是另外两种量纲，一手标准侧本仓未核
- 轴：C（主轴）＋ S（数值/量纲无一手锚）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp:259`
  ```
  dx += f;
  ```
  紧接着 :264 `T xi = T(m_wcs.cd[0]) * dx + T(m_wcs.cd[1]) * dy;`
- 上位依据（三面逐字）：
  - 读侧（本文件）：A/B 被加在**像素偏移**上，再乘 CD ⇒ A 的量纲必须是 px^(1−i−j)。
  - 写侧：`lib/algorithms/platesolve/cpp/ipv/src/ipv_wcs.cpp:363-366` "单位: (像素/角秒) * (角秒/像素^(i+j)) = 1/像素^(i+j-1)；注: 这是像素域约定; **SIP 标准的 A_ij/B_ij 定义在中间世界坐标(度)上, 量纲为 deg/像素^(i+j), 与本数组相差一个 CD 量纲**（见 docs/science/ASTROMETRY.md §5）"。
  - 合同：`docs/contracts/DATA_SEMANTICS.md:373` 对同一对象（"header KV: SIP A/B/AP/BP 系数"）声明单位"无量纲"。
  - 一手锚：`docs/references/SCIENTIFIC_REFERENCES.md:121` #57 Shupe et al. 2005（SIP 约定），"核验状态：**bibcode 级**"；Calabretta, Mink & Kisner 2012（SIP 的正式载体文）**未登记在文献池**。检索式：`git grep -n -i "tadhunter\|shupe\|mink\|kisner\|distortions and alignments" -- docs/references/`。
- 现状：本文件不出现任何 CD 归一/换算，也不声明所读 `A_i_j` 的量纲；`fits_reader.cpp:299-302` 把 FITS 标准键 `A_i_j/B_i_j/AP_i_j/BP_i_j` 的值原样 `parseDouble` 进数组；`lib/infrastructure/scheduler/src/module_adapters.cpp:4443/7864` 的 `p1_sip_write_header_frame` 把像素域系数写进这些标准键名。
- 差在哪：三条面（读侧假设像素域、写侧自称像素域并指出标准是度域、合同说无量纲）对同一数组给三个身份，而键名用的是 FITS 标准键——按标准键名交换时语义与键名不属于同一条约。
- 后果：本仓自产回路（platesolve → frame header → wcs_sip）三跳同为像素域，数值自洽，产品不受影响；**但任何外部真实 FITS 的 SIP 头**（度域 A_ij）经 `fits_reader.cpp:299 → wcs_sip.cpp:259` 会被再乘一次 CD（典型 |CD|≈1e-4 deg/px），畸变项被压缩约 10⁴ 倍 ⇒ 等效"无畸变校正"，drop 四角与 leaf 归属按线性 TAN 算，产品像素位置带 SIP 幅度的系统偏差。`docs/algorithms/DRIZZLE_GEOMETRY.md:302` 点名的验证场景恰是 "FIX-DRZ-F SIP 畸变边缘 patch（15° 宽场）"，即该幅度在本仓是要被证明修掉的东西。
- 定级：**S2**。缺两角证据故不判 S1：① 生产 drizzle 通道的 SIP 来自 frame header KV（`hp_drizzle_api.cpp:455-505`），是否会出现"外部度域头"未经证据；② 一手标准的实际约定本仓未核（见下"读不动"）。
- 怎么算修好：① 先补一手锚——把 #57 从"bibcode 级"升到已核，并把 Calabretta/Mink/Kisner 2012 登记进文献池（登记前**不要**动代码数值）；② 在本文件与 `fits_reader.h:11-18` 的 `SipCoeffs` 上写明单位与换算位置，二选一：键面遵循标准（度域，读侧做 `dx += CD⁻¹·A(dx,dy)`）或仓内自定像素域（则**不得**复用 FITS 标准键名，改用私有键并在 `DATA_SEMANTICS.md:373` 把"无量纲"改成真实量纲）；③ 必须同批改：`module_adapters.cpp:4443` 的写出侧、`docs/algorithms/DRIZZLE_GEOMETRY.md:281-286` 的桥接说明、`tests/p0_sip_order_guard_test.cpp` 与 `lib/algorithms/photometry/cpp/test/test_p0_sip_order_guard.cpp` 的期望（两通道同改，否则按 ROOT_CAUSES.md:145 "B2-A18 只修 session 通道、IR 通道无对称门"重演）；④ 同批加一条往返负例：度域合成头 → 断言畸变校正幅度 ≈ 真值，像素域头 → 断言被拒。
- 置信：PARTIAL（缺：SIP 一手条文中 A_ij 量纲的逐字核证；本仓文献池明标未核，且我未能取到可引全文。三面不一致本身为 CONFIRMED，已逐字对读）

### CR-40-4 · ④"WCS 无效"在本文件被表示成合法天球坐标 (0°,0°)，而不是不可估计值
- 轴：R
- 位点：`lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp:241-244`
  ```
  WCS_LOG("错误: pixelToSky 调用时 WCS 无效, 返回零值");
  ra = T(0);
  dec = T(0);
  return;
  ```
  同型：:289-292（`skyToPixel` 返回 `x = 0.0; y = 0.0;`）、:40（构造期"坐标转换将返回零值"）
- 上位依据：同文件 :209-210 已给出本仓对此类情形的口径："哨兵值会被 isfinite 放行而静默映射到错误像素"，并要求"必须显式拒绝"；AGENTS.md §5 "发现…先质疑前提"与 fail-closed 纪律。
- 现状：`has_wcs=false` 时前向返回赤道与本初子午线交点 (0°,0°)，逆向返回 0 号像素——两者都是有限、合法、可被 isfinite 放行的值。
- 差在哪：与 :211-216 的处置方式（写 NaN）自相矛盾；"无 WCS"是一个未知的语义状态，被编码成了一个具体的天区位置。
- 后果：若有调用方未经 hasWcs 判定直接使用返回值，整帧像素会被映射到天球上同一点。**生产不可达**：`drizzle_engine.cpp:665/818/943/1770` 与 `hp_drizzle_api.cpp:242/450` 均在构造/调用前判 `has_wcs`/`hasWcs()` 并失败退出（检索式：`git grep -n "has_wcs\|hasWcs" -- lib/algorithms/drizzle/healpix_drizzle/*.cpp`）。
- 定级：**S3**（判据方向：若可达则偏绿——有限值通过 isfinite；但守卫链在调用方，本文件不构成可达路径）
- 怎么算修好：把 :242-243/:290-291 的 0 换成 `quiet_NaN()`，与 :213-214 统一；同批把 `hasWcs()` 的判定从"调用方责任"变成本类的具名失败（返回 bool + 出参），并在 `drizzle_engine.cpp:665/818/943/1770` 处保留既有守卫（防"改对反而判红"：这些行已把 !hasWcs 当失败，改 NaN 后其错误文本与 rc 不变）。
- 置信：CONFIRMED

### CR-40-5 · 本文件不读 CTYPE1/CTYPE2，任何投影的头都被无条件按 TAN 换算
- 轴：D（设计点名的"严格校验"面在此根本没写）
- 位点：`lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp:33`
  ```
  WcsSip::WcsSip(const WcsParams& wcs) : m_wcs(wcs), m_hasWcs(wcs.has_wcs) {
  ```
  （`m_wcs.ctype1/ctype2` 在整个文件里从未被读取；`fits_reader.h:25` 注释自陈期望值是 `"RA---TAN-SIP"`）
- 上位依据：条文面零提及"drizzle 输入须拒绝非 TAN"。我查过的检索式：`git grep -n "ctype1\|CTYPE" -- lib/algorithms/drizzle/healpix_drizzle/*.cpp lib/algorithms/drizzle/healpix_drizzle/*.h`（结果：只有 fits_reader.cpp:283-284 存入、hp_drizzle_api.cpp:95 写常量 `"RA---TAN-SIP"`、:426-428 从 KV 拷入，**无一处判定**）；`git grep -n -i "TAN" -- docs/algorithms/DRIZZLE_GEOMETRY.md docs/science/DRIZZLE.md`（无"仅支持 TAN"的条文）；`docs/algorithms/DRIZZLE_GEOMETRY.md:179-182` 的"严格校验"清单逐条枚举 nside/NESTED/宽高/pixfrac/CD 行列式/cd 元素范围/crval 域，**CTYPE 不在清单内**。
- 现状：投影类型只写在注释与常量里；`pixelToSkyT`（:239）与 `skyToPixel`（:287）无条件走 gnomonic 正反投影。
- 差在哪：`m_wcs` 里带了 CTYPE 而实现不使用 ⇒ 声明与执行脱钩；本库对输入有效性的自述面（:179-182）是穷举式的，缺了这一维。
- 后果：`docs/references/SCIENTIFIC_REFERENCES.md:118` (#55) 登记 "ACSD 八投影冻结集合逐式出自此"——多投影是本仓的合法能力面；若一张 CAR/AIT/ZEA 头的帧进入本类，得到的是"看起来收敛"的 TAN 坐标 ⇒ drop 顶点、leaf 归属、面积权重全错而零报错。
- 定级：**S2**。逐跳生产路径可写（`module_adapters.cpp:8283` → `hp_drizzle_hips_api.cpp:43` → `hp_drizzle_api.cpp:426-428`（只拷不判）→ `wcs_sip.cpp:33` → `wcs_sip.cpp:239`），但**升级为 S1 的前提未定**：现行 frame header 由 ACSD 自产并写死 `"RA---TAN-SIP"`（`hp_drizzle_api.cpp:95`），外部帧的 CTYPE 能否进 p1 通道我在本批未取证。缺的正是这一角。
- 怎么算修好：① `WcsSip` 构造时对 `ctype1/ctype2` 做前缀判定（`RA---TAN` / 允许 `-SIP` 后缀），非 TAN 返回 `has_wcs=false` 或具名失败；② 把该判定登记进 `docs/algorithms/DRIZZLE_GEOMETRY.md` §严格校验清单（新增一条），使条文与代码同一处收口；③ 必须同批改：`hp_drizzle_api.cpp:426-428` 需在读 KV 后立即判，不能等到 WcsSip 构造；④ 必须同批补负例：`CTYPE1="RA---CAR"` 的合成头 → 断言具名拒绝（若判据只写成"结果不同"则会假绿）。
- 置信：PARTIAL（缺：外部/非 TAN 帧能否进入 p1 drizzle 通道的证据；本批未追 `p1_wcs` → frame header 的写入面）

# 10. 派单 ① 的正面回答：同名算法双轨，生产走哪一条（逐跳调用路径）


同一件事（球面 drizzle 的几何 + 权重/方差装配 + WCS/SIP 坐标变换）在本批与它旁边各有一套实现。归属判据只取"生产入口能不能走到"，不取注释自称。

## 10.1 生产轨（逐跳，全部 `文件:行`）

| 跳 | 位点 | 内容 |
|---|---|---|
| 1 | `lib/infrastructure/scheduler/src/module_adapters.cpp:8283` | `rc = hp_drizzle_run_phase1_hips(frame, frame_nside, nested, pixfrac, fdir.c_str(), ...)`（同文件 :7430 注释："op: drizzle_stack（唯一真实入口 hp_drizzle_run_phase1_hips; nside 科学参数无缺省）"） |
| 2 | `lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_hips_api.cpp:43` | `return run_drizzle_internal(frame, ..., write_hips=true, write_legacy_hiss=false, ...)` |
| 3 | `lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.cpp:426-428` | 从 frame header KV 取 `CTYPE1/2`（**只拷入 `wcs.ctype1`，不判定**，CR-40-5） |
| 4 | `lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.cpp:455-467` | SIP 阶数校验 `[0,5]`，越界硬失败（本批 `wcs_sip.cpp` 自身无此守卫，CR-40-2） |
| 5 | `lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp:33` | `WcsSip::WcsSip(const WcsParams&)`（本批文件，**在生产路径上**） |
| 6 | `lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp:239-268` | `pixelToSkyT` → 本批侧唯一被生产消费的坐标变换 |
| 7 | `lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp:1770/:1788` | 引擎判 `img.wcs.has_wcs` / `wcs.hasWcs()` 后建 `WcsSip` |
| 8 | `lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp:1451-1455`，`:1483-1486` | 真算 `max_edge_rad`（弧度）→ `spherical::build_drop_polygon_adaptive<Scalar>(...)`（**另一版几何**，非本批文件） |
| 9 | `lib/algorithms/drizzle/healpix_drizzle/spherical_overlap.cpp:85`，`:1523/:1615` | `ARCSEC_TO_RAD`、`hp_res_rad` 与逐候选交叠面积（另一版） |
| 10 | `drizzle_engine.cpp:2349-2371` | `drizzleTiled` / `drizzleTiled_f64`（:2351 注释"Tile 级 Drizzle (FP32 路径, 正式入口)"）→ HiPS 直写 |

**结论（生产走哪一条）**：`wcs_sip.cpp/.h` 在生产路径上（跳 5-7）。`v6_drizzle_science.cpp/.h` 与 `v6_spherical_overlap.cpp/.h` **不在生产路径上**。

## 10.2 v6 轨（本批四个 v6 文件）的可达面

| 证据 | 位点/检索式 | 结果 |
|---|---|---|
| v6 算子的唯一产品侧调用者 | `lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:515` `const DrzError de = build_operator_from_sources(` | 存在，但所属库未被生产链接 |
| 该库的消费者 | `git grep -n "astrocs_v6_phase1_product" -- '*.txt' '*.cmake'` | 只有 `eng/tests/integration/v6_p1/CMakeLists.txt:20 target_link_libraries(v6_p1_integrate_test PRIVATE astrocs_v6_phase1_product)` |
| `write_phase1_product` 的调用者 | `git grep -n "write_phase1_product" -- lib/infrastructure/` | 零命中（命中面：`lib/algorithms/integration/v6_phase1/{README.md:60,include/...phase1_product.h:165,src/phase1_product.cpp:337}`、`eng/tests/integration/v6_p1/v6_p1_integrate_test.cpp:239/245/413/608`、`eng/tests/integration/v6_p2/v6_p2_integrate_test.cpp:201`） |
| 生产末端声明 | `docs/contracts/DATA_SEMANTICS.md:395` 逐字："**生产路径只落标准 HiPS，不落任何 legacy 容器**：Phase1 末端为 hp_drizzle_run_phase1_hips 直写标准 HiPS（module_adapters.cpp p1_op_drizzle）" | 与 §10.1 一致 |
| 反向 drizzle（`WcsSip::skyToPixel` 唯一非测试用户） | `git grep -n "hp_drizzle_reverse_run\|reverse_run\|drizzle_reverse" -- lib/infrastructure/` | 零命中 ⇒ CR-40-1 定 S2 的依据 |
| `hp_drizzle_run`（readFits→WcsSip 通道的入口） | `drizzle_engine.cpp:1157` 注释逐字："本路径为 legacy .hiss 容器出口（hp_drizzle_run 零生产调用者" | ⇒ CR-40-2/CR-40-3 定 S2 的依据 |

## 10.3 两版常数逐条对照（派单 ① 后半："常数是否已经不一致"）

| 常数/判据 | 本批侧位点与值 | 另一版位点与值 | 是否已不一致 | 后果与处置 |
|---|---|---|---|---|
| 角秒→弧度 | `v6_spherical_overlap.cpp:19` `kArcsecToRad = 3.14159265358979323846/180.0/3600.0` | `spherical_overlap.cpp:85` `ARCSEC_TO_RAD = PI/(180.0*3600.0)` | **否**（逐位同值） | 合并成"同族同值"一行；不报（§12） |
| 自适应细分尺度 `src_scale_rad` | `v6_spherical_overlap.cpp:22` `std::sqrt(src.A_pixel)`（px 或 sr，随 A_pixel 单位而悬） | `drizzle_engine.cpp:1451-1486` `max_edge_rad`（真弧度）；`reverse_drizzle.cpp:362` `0.5*(s1+s2)*(π/180)` | **是（量纲级）** | CR-40-8，S3（被 `spherical_overlap.cpp:942` 的 `1e-11` 绝对地板掩盖） |
| drop 边离散化 | `v6_spherical_overlap.cpp:49-51` 固定 `samples_per_edge=8`（`h:37`），且默认无误差登记 | `spherical_overlap.cpp:911-952` 自适应二分至 `wcs_epsilon` | **是（精度档不同）** | CR-40-7，S2＋"改接线即 S1" |
| 几何闭合门 `Σ_p a_jp = pixfrac²·A_pixel` | 本批有：`v6_spherical_overlap.cpp:120/:166`，容差由调用方（测试 1e-3，`v6_p1_drz_test.cpp:608`） | 生产引擎**零执行面**：`git grep -n "closure\|闭合" -- .../drizzle_engine.cpp` → 0 命中 | **是（有无不对称）** | CR-40-9，S2 |
| CD 行列式奇异阈 | `wcs_sip.cpp:48` `1e-15`（零矩阵降级） | `reverse_drizzle.cpp:285` `1e-30`（具名失败）＋条文 `docs/algorithms/DRIZZLE_GEOMETRY.md:180` `1e-30` | **是（值与行为都不同）** | CR-40-1，S2 |
| SIP 阶数合法域 | `wcs_sip.cpp` 无守卫（`h:49` 只写 `A[36]`） | `hp_drizzle_api.cpp:103-104/:465-467` `[0,5]` 硬失败；`hp_drizzle_api.h:150` 注释仍写 `0..4` | **是（三侧，且在册）** | CR-40-2，S2；`docs/algorithms/DRIZZLE_GEOMETRY.md:324` DISP-DRZ-001 已登记该 0..4 vs [0,5] 分裂 |
| 常量 SB 门容差 | `v6_drizzle_science.h:41/:300` `1e-3` | 条文 `docs/science/DRIZZLE.md:123` `1e-3` | 否 | 一致；本批额外出现的 `1e-9`（`.cpp:423`）条文面无 ⇒ CR-40-15 |
| 通量折算因子 | `.cpp:145` `1.0`；比较容差 `:633` `1e-12` | 装配侧 `phase1_product.cpp:300` 字面 `1.0`、`:1199` 字面 `1e-12` | 数值否；**来源重复写死** | 记入 §1.1 表；因子条件性 ⇒ CR-40-16 |
| k_corr 合法域 | `.cpp:639` 只拒 `== 1.0`；默认 `0.0` | 条文 `PHASE2_SAMPLER.md:52` `1 < k_corr`；`sampler.cpp:83` 默认 1.4 | **是** | CR-40-11，S2 |
| 球面面积单位 | `v6_spherical_overlap.h:36/42/46/47`、`v6_drizzle_science.h:199/205` 全标 `[px^2]` | `spherical_overlap.h:93-105/:242-243` 声明 sr；冻结单位 `v6_drizzle_science.h:10-15` 要求 ADU/sr | **是（注释级，实现按 sr）** | CR-40-6，S2 |

---

# 11. 本批最高危三条（只挑，不复述）


1. **CR-40-11**（`v6_drizzle_science.cpp:639` ＋ `v6_drizzle_science.h:376-377`）——k_corr 的域判据只钉 `==1.0`，权威域是 `1 < k_corr`（`docs/algorithms/PHASE2_SAMPLER.md:52/:306-307`），而"未测"没有表示面、结构默认 0.0 属条文 declared 物理不可达值却能全绿。按 `:248` 的 `control_variance = k_corr × (π/2) × σ_bg² / N_retained`，方向单调偏绿；这是本批唯一直接损害**创新点 SCI-B（跨帧绝对 SNR）相关折算主张**的一条。
2. **CR-40-1**（`wcs_sip.cpp:48`）——CD 行列式奇异阈取 1e-15（条文与另一版都取 1e-30），且命中时把逆矩阵写成 0.0 只打警告：`skyToPixel` 对所有天区返回同一像素。这是**本批唯一有生产入口、只差一次接线的静默降级**（外层 `reverse_drizzle.cpp:285` 已按 1e-30 放行 ⇒ 嵌套守卫错配），且本文件 :209-210 自己论证过有限哨兵值不可接受。
3. **CR-40-7 ＋ CR-40-6 合起来**（`v6_spherical_overlap.cpp:49-51` 与 `.h:36/42/46/47`）——v6 侧几何精度弱于生产侧（固定 8 段 vs 自适应），而**唯一的几何正确性门（闭合）用的是同一个被欠分辨率的多边形自比**；同时该层的面积单位注释与冻结单位表 ADU/sr 冲突。接线 v6 轨时这两条必须一起处理，否则"更严格的门"会给出虚假的安全感。

---

# 12. 我读不动／需要实跑才能定的


| 项 | 为什么本批定不下 | 该跑什么（不属本批纪律范围，供负责人排期） |
|---|---|---|
| CR-40-3 的 SIP 系数量纲：一手标准到底把 A_ij 定在像素域还是度域 | 本仓文献池 `docs/references/SCIENTIFIC_REFERENCES.md:121` (#57 Shupe 2005) 明标"核验状态：bibcode 级"，Calabretta/Mink/Kisner 2012 未登记；我按"不得凭记忆断定"的口径未把它当依据，也未取到可引全文（WebFetch 两次 404） | 第③层文献核验：取 C-M-K 2012（A&C 1, 22）§2 与 Shupe 2005 式(1)-(2) 原文，逐式定 A_ij 量纲并升级文献池条目；之后才动 `wcs_sip.cpp` 的换算位置 |
| CR-40-7 的"8 段够不够"误差上界 | 需要真实球面几何下的面积残差分布，不能靠读 | `healpy` 球面多边形面积对拍（`docs/science/DRIZZLE.md:174` 已登记 rtol 1e-9 的该参考面），对 15° 宽场＋强 SIP 的 drop 分别跑固定 8 段与自适应，比较 a_jp 相对差 |
| CR-40-9 的闭合容差 1e-3 是否够／该定多少 | 需要真实帧上的 `closure_rel` 分布（测试注释只自述"可达 ~1e-3"） | 对 `testdata/` 真实帧跑 `build_operator_from_sources` 并导出 `OverlapDiagnostics::max_abs_closure_rel` 分布；须套 `mem_guard.py`（AGENTS §3） |
| CR-40-1／CR-40-2 的实际可达性（反向 drizzle 与 readFits 通道） | 我只做了 grep 级接线核查（§10.2），未读 `drizzle_engine.cpp`（2634 行）与 `hp_drizzle_api.cpp`（1431 行）全文，两者均不在本批清单 | 接线面专批：`module_adapters.cpp` 的 op 注册表 → C ABI 可达性图（含 `-fvisibility`/导出符号面，`eng/tests/unit/drizzle_adapter_test.cpp:76-81` 已在断言符号隐藏） |
| `spherical_overlap.cpp` 的 quick-reject 是否真返回**字面** 0（决定 §3.1 `:94` 行 `a == 0.0` 是否安全） | 该文件不在本批清单，我只读到它的头文件声明 | 读 `spherical_overlap.cpp` 的 quick-reject 分支返回值（属另一 CR 批的清单） |
| `pixfrac` 默认值 1.0（`v6_spherical_overlap.h:35`）与全局默认是否一致 | 默认配置的权威面在 `eng/packaging/config/defaults.json`，本批纪律禁止读 `eng/**` 执行面（我只按只读方式引用了 `docs/`，未打开该 json） | 由能读配置面的批次比对 `defaults.json` / `docs/contracts/` / 生产缺键兜底三侧的 pixfrac 默认 |

---

# 13. 不报的核过项（＋检索式）


| 核过为合规的项 | 为什么不报 | 检索式／复算 |
|---|---|---|
| `wcs_sip.cpp:250-251/:315-316` 的 `− 1.0`／`+ (crpix − 1.0)` | 1-based↔0-based 的定义换算，属 §4"单位换算定义常数"；且与文献池 #56（Paper I §2.1.1）逐字一致 | `git grep -n "crpix" -- lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp`；手算 `x₀−(CRPIX−1)=(x₀+1)−CRPIX` |
| `wcs_sip.cpp:211-216` TAN 背面返回 NaN 而非有限哨兵 | 方向正确且是**加严**：`!(cosc > 0.0)` 对 NaN 也判失败 ⇒ 偏红 | 逐字读 :208-216 与其理由注释 |
| `wcs_sip.cpp:83-92` 用整数幂递推而非 `std::pow` | 注释说明动机是"模板化不得改变 FP64 数值"（避免 pow 的 exp/log 舍入），是保数值一致的做法，非偏差 | `git grep -n "std::pow" -- lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp` → 零命中 |
| `v6_spherical_overlap.cpp:38-40/:77-80` 的 `ASTROCS_V6_DRZ_FAULT` 故障注入 | AGENTS.md §9 明文要求"负例注入（能红能绿）"；注入后必走 `:90-93→:114-117` 的具名失败，不产生静默错值 ⇒ fail-closed 一侧 | `git grep -n "ASTROCS_V6_DRZ_FAULT"`（命中仅本实现＋`eng/tests/unit/v6_p1_drz`） |
| `v6_spherical_overlap.cpp:114` `n_area_rejected > kMaxInvalidAreaHits` | 判据方向已钉死为偏红（计数单调增、上限 0） | 同上 §3.1 表行 |
| `v6_drizzle_science.cpp:204-205` 闭合判据双向具名 | 这正是 `DRZ-PF-CORRECT-01` 的修复本体；旧判据只判 `rel > rel_tol`（亏损恒不触发）已在此纠正 | `git grep -n "overlap_area_deficit" -- lib/ eng/` |
| `v6_drizzle_science.cpp:148-150` 与 `:252-255` 的两参数化 c_jp | 复算等价：drop 侧 `c=[a/(pf²A)]·[pf²/D]=a/(A·D)` ＝ `reference_sb_coefficient`；故"两版参数化"不构成数值分歧（本头 :30/:188 的声明成立） | 手算代换；`git grep -n "NormalizationKind" -- lib/algorithms/drizzle/healpix_drizzle/v6_*` |
| `v6_drizzle_science.cpp:293` `ivar = (var>0)? 1/var : 0` | 方向论证：var≤0 ⇒ ivar=0 ⇒ 下游权重为 0（该像素被丢弃），不伪装高置信 ⇒ 不偏绿 | §1.1 表行 |
| `v6_drizzle_science.cpp:57-76` 冻结单位表数值（±1/±2/−2/−4） | 由 `square_dimension`/`inverse_dimension` 两式导出且逐条自洽；`psfsw_robust_weight` 行的物理删除有 ID 与两处条文引用 | 手算 (1,−2)→²→(2,−4)→⁻¹→(−2,4)；`git grep -n "PSFSW-RETIRE" -- docs/ lib/` |
| `arcsec→rad`、`π`、`180.0`、`2π`、`asin 定义域 ±1`、`0xFFFFFFFFull`、`<<32`、数组维度 4/36/6 | §4 结构性常数与定义常数 | §2.5 各表内已按"结构性不适用"逐位点列出并注明"为什么不是科学量" |
| `v6_drizzle_science.h:199/:205` 与 `v6_spherical_overlap.h` 的 `[px^2]` 注释（**只**就"是否影响产物字节"这一维） | 该维度不报——纯注释不改字节；报的是它作为**唯一量纲防线**误导调用方的那条（CR-40-6），二者不是重复计罪 | 区分见 CR-40-6 的"可拦性核查" |

---

# 14. 派单与清单差异（收尾三节之三）


1. **派单 ① 要求"逐跳给调用路径"，但清单里三个文件的"另一版"不在清单内**：`v6_*` 的另一版是 `spherical_overlap.cpp`/`drizzle_engine.cpp`，`wcs_sip.cpp` 的另一版是 `reverse_drizzle.cpp` 与 `hp_drizzle_api.cpp` 的校验段。我按"取证可读、位点只落在清单内"处理：所有 finding 的"位点"字段均在本批 6 个文件内，另一版只作为调用路径与对照证据出现。**未越出清单去指控别人家文件。**
2. **派单 ② 的假设与本批事实相反**：派单问"球面交叠在本批这一侧是否弱于另一版"。事实是**两版共用同一几何例程**（`v6_spherical_overlap.cpp:3` "复用既有球面几何（spherical_overlap.{h,cpp}），不改其数值路径"），故"弱"只发生在 `A_pixel<=0` 的固定采样分支（CR-40-7）与 `src_scale_rad` 的量纲漂移（CR-40-8，被地板掩盖）两处，不是整体弱。按派单口径仍给了"不可达 ⇒ S2 ＋改接线即 S1"。
3. **清单文件数与派单表述的"同名算法双轨"不完全对齐**：派单说"同一算法存在两版实现"，但本批 6 文件里真正成双的只有 v6 那 4 个（对应 `spherical_overlap.*` ＋ `drizzle_engine.*`）；`wcs_sip.*` 在本批内**没有**第二版，它的"另一版"是同目录另一条通道对 WCS 的校验（`reverse_drizzle.cpp:255-334`、`hp_drizzle_api.cpp:424-505`）。已按事实写，不迁就派单措辞。
4. **finding 编号顺序与章节顺序不一致（非派单差异，是成稿口径）**：`CR-40-1…5` 挂 `wcs_sip`、`CR-40-6…10` 挂 `v6_spherical_overlap`、`CR-40-11…16` 挂 `v6_drizzle_science`——按取证与落盘先后编，不按章节重排（全文互引已按此编号写成，重编会打断引用）。阅读时以章节内的"位点"字段为准。
5. **开工/收工 status 出现基线外新增文件（非本批造成，登记不掩盖）**：收工多出一个未跟踪文件 `photom.rst`（12896 字节，mtime 21:20，`git ls-files photom.rst` → 0）。内容是 SExtractor 的 `Photom.rst` 文档（首行 `.. File Photom.rst` / `.. include:: global.rst` / `Aperture photometry`），落在**仓库根**。本批全程只读、未写任何仓库内文件（唯一写操作在 `产出/`），故它是同期其他进程写入的。按前言 §1"基线应为 `?? ACSD整治工作包_AUDIT-06.zip`、`?? site/`"，这一度属**基线外新增**；本报告定稿前的最后一次采样显示它**已消失、回到基线**。两次采样原文都在 §15，不事后只留干净的那一次。

---

# 15. 开工与收工 git status 原文


开工（本节点第一个 git 动作，读派单前言之后）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工（第一次采样，写完各节文件后）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? photom.rst
?? site/
```

收工（最后一次采样，本报告定稿前）——`photom.rst` **已自行消失**，回到基线：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

即：本节点会话期间，仓库根先出现后被移走一个未跟踪文件 `photom.rst`（SExtractor 文档，12896 字节，mtime 21:20，`git ls-files photom.rst` → 0）。两次采样都记原文，不事后只留"干净"的那一次——该文件的出现与消失都由**同期其他进程**造成（本批全程只读，唯一写操作在 `产出/`），登记为基线抖动事实，供负责人核对是否需要看 `run/` 或并发批次的写入面。

本批 6 个文件在任何一次 status 中均未出现（零改动）。

---
