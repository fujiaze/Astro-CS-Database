# AUD-101 文档审计原始台账 —— 批次 DB-06 + DB-07（`docs/contracts/` 合同说明层）

批次：DB-06（2 份）+ DB-07（2 份）合并一小批。清单 = `独立审计/批次清单/DB-06.txt`、`DB-07.txt`，各 2 行（`wc -l` 复算：2 + 2 = 4 行；派单文字与清单一致，无差异）。
仓库：`F:\Astro dev\Astro CS Normalization Database`，HEAD=`c8f64e9a`。本节点只读：未构建、未跑 ctest/run_checks/任何 `eng/**` 脚本、未 import 仓库内 Python。

## 0 基线取证（开工 / 收工 两次 `git status --porcelain` 原文）

开工：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

两次一致，无新增项 = 取证动作未污染被审对象。取证过程中的写操作全部落在本审查目录，仓库侧只有 `Read` / `git grep` / `sed -n` / 只读 `python3` 文本解析。
注：`eng/tests/config/__pycache__/` 在工作树内存在，但被 gitignore（不出现在 `--porcelain` 输出），非本批次动作产生（本批次未 import 任何仓库 Python）。

本批特有关注点的执行口径：合同说明层复述的**字段语义 / 必填性 / 枚举值 / 单位**逐条与 `eng/contracts/**`（机器合同唯一事实源，AGENTS §7）对账，不一致时以 schema 为事实源登记偏差；并判定该说明文档是否已越位成第二套事实源。

---

# 第一部分 · 批次 DB-06

## 1.1 逐份登记（列名与顺序按派单前言 §4）

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `docs/contracts/CONFIG_CONTRACT.md` | 配置合同（全局默认值 · 滤镜库 · 三命令 phase_config · cpu_profile · run_manifest） | 248（`wc -l` 复算；与机械列一致） | 合同说明；实际所在目录 `docs/contracts/` = 应属目录（标准 01 §2 表"合同说明"）**一致，无角色错放**；但 §2/§9 的数值快照表使局部内容越位到"登记面"（见"与上位冲突"） | 三类配置（phase_config / cpu_profile / run_manifest）的键表、块形态、默认值索引、滤镜库语义与匹配规则 | **有**：抬头 line 3 `ASTROCS_DESIGN.md §3.2（三类配置）、§7.2（配置、事件与退出码）`；§0 另列 8 条权威锚。**章节号全部有效**（`grep -n "^### 3.2\|^### 7.2"` → 231 / 493，标题逐字匹配），**§0 表内行号锚全部失效**（见明细 D-1） | 41 个文件引用（命令：`git -C "…" grep -l CONFIG_CONTRACT \| grep -v '^docs/contracts/CONFIG_CONTRACT.md$' \| wc -l` → 41），含最高设计与 5 份 `eng/contracts/schemas/**`。`docs/DOCUMENT_INDEX.yaml:573` 记"等 13 处"——口径差由该索引头 line 30 的排除清单（reports/artifacts/工程控制/实验）解释，非登记错误 | 说明面正本（三类配置"语义与索引"）；**数值面非正本**——正本 = `eng/packaging/config/defaults.json` + `filters.json` + `eng/contracts/schemas/phase_config_*.schema.json`（本文件 line 7 自己也这样划界） | 与 `eng/packaging/config/defaults.json` 的分组计数、`authority_status` 值域重复且已失真（贴段见 D-2、D-3）；与 `eng/packaging/config/config_registry.json#plugin_knobs` 的分布计数重复且已失真（D-4）；与 `docs/development/CONFIG_SCHEMA.md`（同主题 legacy 份，别批）重叠——本文件 line 192 自认 `CON-CONFIG-CONTRACTS` 检查的正是那份 | **有**：line 5-8 四行元信息块（`ID: CFG-001` / `状态: 建立（2026-09-16）` / `任务: 工程控制/PROJECT-GOVERNANCE-01/tasks/CFG-001.md` / `收口: W5-CFG-002（CFG-002，2026-09-17）`）→ 违反标准 01 §4"文档开头无元信息块" | 日期 12 处/10 行（5,6,32,181,182,193,194,199,202,203）；任务编号 34 处/28 行（CFG-001/002、W5-CFG-002、W5-CPU-001、MASK-002、BENCH-004、DOC-203、FIX-207、RELEASE-04、GATE-502、DOC-SCI-001、DATA-001、GAP-024/025/033、DISP-STAR-002、REJECT-DOCFIX-01、CFG002-ANCHOR×5）；历史叙事 25 处/18 行（"概念已作废""已随 §9.73 A44 作废""原记 96 行""已撤销""已闭合""原断言…已作废""旧合同分支保留""已退役""重锚"）；删除线 3 处（line 50、67、82 用 `~~…~~` 划掉旧键名并留括注）→ 标准 01 §4"无旧版/作废/留痕/曾等历史叙事；需要硬约束用正向条款" | ①line 5 `工程控制/PROJECT-GOVERNANCE-01/tasks/CFG-001.md`：`git ls-files 工程控制/` 仅 `RELEASE-05/**`，无 `PROJECT-GOVERNANCE-01` → 不存在；②line 185 `eng/tests/gaia_zlib`（称"新出现的目录"）：`ls eng/tests/` 无此目录 → 不存在；③line 5/6/192/182 的 `§9.73`（A44）：见 A-1 系统性悬空；④§0 与 §2/§4 的全部 `文件:行` 锚（D-1、D-5） | **8 条对机器事实源的偏差（D-1…D-8）**，其中 3 条为文档内部自相矛盾（§5 vs §8.1；§2 表 vs defaults.json；§9 分布 vs config_registry.json）。均"下位与上位/事实源冲突"，按标准 01 §1"冲突本身是缺陷" | **保留 + 就地订正**（不迁移、不合并）：角色与目录一致（标准 01 §2），是三命令配置语义不可缺的下层正本；订正三件事——数值快照改指针、锚点重定、清历史叙事与元信息块（标准 01 §4"公式、数值表…放对应下层文档"＋"正文无日期、无任务编号、无历史叙事"＋§1"任何文档不声明自己的第二套权威顺序或唯一口径"） |
| `docs/contracts/DATA_ARTIFACTS.md` | DATA-001 DataArtifact 注册表与 weight 歧义映射 | 128（`wc -l`；与机械列一致） | 合同说明；目录一致，**无角色错放** | 产品工件（DataArtifact / 统一对象）的 schema_id 登记、单位/shape/invalid/ownership 六列，与 `weight/scale/sigma/value/snr` 歧义映射 | **有**：抬头 line 3 `ASTROCS_DESIGN.md §10（I/O 与原子产品）`；`grep -n "^## 10"` → 684 标题逐字匹配 ✓。但**缺**标准 01 §3 要求的"沿索引下钻"的中间层锚：全文另引 `docs/contracts/DATA_SEMANTICS.md` 约 20 处、`docs/design/UNIFIED_MODEL.md §2`（line 44/45） | 23 个文件引用（同命令，排除自身 → 23），其中 `eng/**` 5 处含 `eng/tools/check_data_artifacts.py`、`eng/tests/contracts/test_data_artifacts_negative.py`、`eng/contracts/data/phase_product_exchange.schema.json` | 是（DataArtifact 层的 schema_id 登记与歧义映射唯一份） | **与本批 DATA_SEMANTICS 构成同主题双层**（见第一部分 §3 同主题组）：line 5 自述"权威: docs/contracts/DATA_SEMANTICS.md（语义）+ 本表（artifact schema 登记 + 歧义映射）" | **有**：line 5-6 两行（`> 权威: …` + `> 生成: V6 重构 DATA-001`）；H1 标题本身含任务编号 `DATA-001`（line 1）→ 标准 01 §4 | 日期 3 处（44"已于 2026-09-20 按负责人裁决 B 真删"、56"变更 claim `CHG-2026-09-20-PSFSW-RETIRE`"、64 节标题"（CI-DATA-REG-001，2026-09-12）"）；任务编号 17 处/16 行（DATA-001、DATA-002、V6、B2-A14、DET-001(D5)、FAILSEM-01、PSFSW-RETIRE、CI-DATA-REG-001、GAP_AUDIT、P3-RSMP-INT、SCI-P3-001、CHK-PROVENANCE-CONSISTENCY、FZ-* 等）；历史叙事集中在 **line 56 整行**——14 列全部填"已退役/已删除/退役留痕/不适用（只作留痕）"→ 标准 01 §4"无旧版、作废、废止、留痕…等历史叙事"，且 §2"历史档案、过程台账不放在正式文档层" | ①line 128 `` `DATA-SEMANTICS.md` ``（连字符）→ 实际文件名 `DATA_SEMANTICS.md`（下划线），`ls docs/contracts/` 确认无该名 → 悬空（机械列未报，见 §3.5 校准）；②line 70 `lib/infrastructure/pipeline/module_ports.registry.json:246/:277`（作为 `DATA-HIPS-001`/`DATA-TILE-001` 在用证据）：`grep -n DATA-HIPS-001` → 751/812/931/1207/1295/1421/1625/1684/1837/1965；`grep -n DATA-TILE-001` 该文件**零命中** → 行号与"两个 ID 都在用"同时失实；③line 72/89/95/97 `module_adapters.cpp:498-499 / :459/:498 / :521/:560`：实际在 :738/:739/:743/:761/:800（漂移约 +240 行）；④line 44 `GAP_AUDIT §4.5 C01` → 见 A-1 | **2 条实质偏差（D-9、D-10）+ 1 条权威声明越位（D-11）**：§1.2 的 13 对象清单、`psfsw_robust_weight` 已删、canonical schema 路径与 `$id` 形态均与 `eng/contracts/schemas/unified/` 实测**一致**（见 §5 一致项） | **保留 + 就地订正**：登记表面向机器合同、角色与目录一致（标准 01 §2）；但 line 5 的"权威: X（语义）+ 本表"是对权威链的**再切分**，须改写成"本表逐项转述 `eng/contracts/**` canonical schema，冲突以 schema 为准"的单向条款（标准 01 §1 末条"任何文档不声明自己的第二套权威顺序或唯一口径"）；line 56 退役留痕整行删除、§1.1/§1.2 顺序编号订正（现 §1→§1.2→§1.1，line 42/64） |

## 1.2 DB-06 偏差明细（schema / 数据为事实源）

**D-1（§0 权威链表：行号锚全部失效）** 表格声称 `ASTROCS_DESIGN.md §4.3（:224-261）`、`§4.5（:278-304）`、`§7.2（:446-467）`、`§4.4:273`、`§5.3:264-266 首批 8 种投影 + 缺省 TAN`。实测：`grep -n "^#\{1,3\} " ASTROCS_DESIGN.md` → §4.3 在 278、§4.4 在 316、§4.5 在 332、§7.2 在 493；`ASTROCS_DESIGN.md` 全文 826 行，"首批冻结 TAN/SIN/CAR/AIT/STG/MOL/CEA/ZEA"（8 种）在 **§6.3:453**，而 §5.3 是"SNR 重建与逆方差叠加"。→ 章节号可判、行号全错、投影条目的章节号也错。

**D-2（§2 字段总数）** 文件标题（line 32）："…（50 字段，2026-09-17 快照…）"。事实源 `eng/packaging/config/defaults.json`：`field_count = 59`、`len(fields) = 59`。line 35 自己规定"权威总数以 defaults.json 的 field_count 为准"→ 快照已过期 9 项。

**D-3（§2 分组表：4 行数值错 + 3 组缺失）** 文档表（line 44-55）与 defaults.json 实测（`collections.Counter(key.split('.')[0])`）：

| 组 | 文档记 | defaults.json 实测 |
|---|---|---|
| calibration | 1 | **2**（多 `master_flat_median_range`） |
| weight | 1 | **0（该组已不存在）** |
| sparse_snr / scalar_gate | 3 | **4**（`sparse_snr.density/spacing_px`、`scalar_gate.rd/trend`） |
| detection / psf / noise / rejection / photometry / precision / upm / hips / drizzle | 1 / 2 / 14 / 18 / 6 / 1 / 1 / 1 / 1 | 一致 ✓ |
| cosmetic（6）、paths（1）、snr（1） | **表内无此三行** | 存在 |

合计文档 50、实测 59。

**D-4（§9 登记册分布）** 文档 line 199/202/181："23 篇 / 117 行…117 = none 60 / gap 25 / conflict 0 / unregistered 32；归属 science_param 82 / runtime_policy 26 / cli_surface 6 / resource_binding 3；登记点 defaults_json 6 / phase_config 10 / inputs_block 8 / cpu_profile 2 / plugin_doc 25 / contracts_doc 3 / cli 6 / none 57"。事实源 `eng/packaging/config/config_registry.json`：`plugin_knobs` 长度 **120**、`totals.rows = 120`（同文件 `totals.plugin_knobs` 仍写 117，即数据文件内部也不自洽）；`by_finding = none 68 / gap 21 / unregistered 31`；`by_owner_class = science_param 85 / runtime_policy 26 / cli_surface 6 / resource_binding 3`；`by_registration = phase_config 18`（非 10）、`none 52`（非 57），其余 6/6/8/2/25/3/6 一致。`docs/plugins/*/*.md` 篇数 23 ✓。

**D-5（§2 rejection 组锚点：三处锚全落空）** 文档 line 48 记 `docs/science/REJECTION.md:97`，并注明"W5-CFG-002 重锚；REJECT-DOCFIX-01 二次重锚，原 :68/:131 均已因文档重排失效"。实测：defaults.json 的 18 条 `rejection.*` 中 9 条 `source_ref.line = 68`（正是文档宣布"已失效"的那个），6 条 = 166，2 条 = 24，其余 source 文本写 :97/:59/:60/:61；`sed -n` 逐行：REJECTION.md:68 = "与生产掩码 5744/5744 逐点一致：干净像素过拒 12.200% → 0.06…"、:97 = "WBPP 只提供档界与算法类型的参考…"、:166 = "工作域 = v − median…"，而真正的 `阈值冻结锚点 (SCI-REJ / ALG-REJ-001..008, rejection.cpp:1):` 在 **line 115**（位于 §5 连续定义内，`grep -n "阈值冻结锚点"`）。→ 文档锚、数据 `source` 文本、数据 `source_ref` 三者互不相同，且三者都不落在 115。

**D-6（§2 `authority_status` 值域）** 文档 line 37："`authority_status ∈ {sourced, owner_adjudicated, pending_authority}`"。实测 defaults.json 有 **5** 个取值：sourced 47 / owner_adjudicated 6 / pending_authority 3 / **measured 2 / derived 1**（`cosmetic.bad_column_sigma_master`、`cosmetic.bad_column_master_wide_frac_max` = measured；`cosmetic.bad_column_variance_kappa` = derived）。说明层声明的枚举比事实源窄 → 按本文档判定字段合法性会误拒 3 个现行字段。

**D-7（§5 kernel 接线：与 schema 冲突 + 文档内自相矛盾）** 文档 line 138："`$defs.kernel_v1`/`$defs.kernel_v2` 被 **0 处** `$ref`…约束已定义但未被 schema 施加"；同文件 line 193（§8.1）："**已闭合（W5-CPU-001，2026-09-17）**：…refs=1/1"。事实源 `eng/contracts/schemas/cpu_profile.schema.json`：`$defs.legacy_v1.properties.kernels.items.$ref = "#/$defs/kernel_v1"`、`$defs.profile_v2.properties.kernels.additionalProperties.$ref = "#/$defs/kernel_v2"`，计数各 1；该文件 `x-astrocs-notes` 第 4 条自述同一事实。→ **line 138 为失实陈述**，line 193 正确。

**D-8（§3 块形态：与 schema 分支结构冲突）** 文档 line 76："`mosaic/export` **仍为** `{phase_name, config, inputs}`"、line 72-75 把"多块形态"限定为 normalize。事实源：`phase_config_mosaic.schema.json` / `phase_config_export.schema.json` 根级均为 `if required:["blocks"] → then {schema_version, blocks}`、`else if required:["phase_name"] → 旧合同分支`、`else → 平铺单块`，即**三命令同构**，旧 `{phase_name,config,inputs}` 已降为 `else` 内的兼容分支；三份 schema 的 `x-astrocs-authority` 均写"ASTROCS_DESIGN.md §3.3（三命令通用输入合同…）"。→ 说明层把主分支与兼容分支说反。

**D-9（§1.1 既存 ID 的行号证据失效）** 见悬空引用 ②③：`module_ports.registry.json:246/:277` 与 `module_adapters.cpp:498-499/:521/:560` 全部漂移，且 `DATA-TILE-001` 在该 registry 文件中零命中。§1.1 想证明的"两 ID 早已在用"这一**结论**仍成立（`eng/tests/unit/core_pipeline_test.cpp:66-67` 实测两 ID 在位、`docs/traceability/TRACEABILITY_MATRIX.csv:25/:31` 实测 `DATA-TILE-001,VERIFIED` / `DATA-HIPS-001,VERIFIED`），只是举证锚点失效。

**D-10（§1.1 未决偏差登记：仍然成立，且是双向偏差）** line 93-100 登记 `DATA-HIPS-001` 的 coordinate 存在 `PIXEL` 与 `HEALPIX` 两值。复算：`module_adapters.cpp:738` = `{"hips","DATA-HIPS-001",true,UnitId::SURFACE_BRIGHTNESS,CoordinateFrame::PIXEL}`，`:761`/`:800` = 同 ID `CoordinateFrame::HEALPIX`；`core_pipeline_test.cpp:66` = `UnitId::ADU, CoordinateFrame::PIXEL`，比文档登记的口径**多一处**（该测试同时把 unit 记成 `ADU`，与 `DATA-HIPS-001` 行 line 39 声明的"面亮度、禁默认 Jy/beam"和 §31.1a canonical `ADU/sr` 不同面）。→ 偏差仍在、且单位面也漂移；文档只登记了坐标面。

**D-11（line 5 权威声明越位，本批"第二套事实源"判定核心）** `docs/contracts/DATA_ARTIFACTS.md:5`：

> 权威: docs/contracts/DATA_SEMANTICS.md（语义）+ 本表（artifact schema 登记 + 歧义映射）

对侧段落 `docs/contracts/DATA_ARTIFACTS.md:44`：

> 对象身份 / 单位（含 BUNIT 语义）/ 无效值与缺失表示 / 精度 / 可否作权重**一律以该 canonical schema 为准**；本表只登记它们在 DataArtifact 面的…六列。

line 44 把权威交给 `eng/contracts/schemas/unified/**`，line 5 又把权威切成"另一份说明文档 + 本表"两段。同一文件内两种权威口径，且都不指向机器合同作为唯一裁决者 → 标准 01 §1"任何文档不声明自己的第二套权威顺序或'唯一口径'"。**判定：越位为第二套事实源（局部）**——因为它与 line 44 同时成立时，一旦 line 5 一侧与 schema 冲突，文件内没有可判定的裁决规则（D-10 的 `ADU` vs `ADU/sr` 正是这种冲突的实例）。

**一致项（防"全是问题"的失真）**：§1.2 的 13 个统一对象与 `eng/contracts/schemas/unified/` 实测一一对应（该目录 13 份对象 schema + `port_contract.schema.json`），`$id` 形态 `https://astrocs.local/schemas/unified/<对象名>/v1` 逐份核对成立，`psfsw_robust_weight.schema.json` 确实不在目录内 ✓；§1 的 45 滤镜与 `$defs.filter_name` 枚举 45 项且集合相等 ✓；`storage_form` 键名与取值域与 `eng/contracts/schemas/hips_storage_form.schema.json#x-astrocs-field-vocabulary`（values `archive/bare`、default `archive`、empty_means `default_and_warn`）✓；`run_manifest` 字段族与 `eng/contracts/schemas/run_manifest.schema.json` 的 `required`/`pattern` 逐条 ✓；DATA-* ID 双向覆盖：`DATA_SEMANTICS` 声明的 14 个 `DATA-*-NNN` 全部能在本表登记（差集为空）✓。

<!-- PROGRESS: 已审 2/4 -->

---

# 第二部分 · 批次 DB-07

## 2.1 逐份登记

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `docs/contracts/DATA_SEMANTICS.md` | Astro Celestial Sphere Database（ACSD） Data Semantics（跨阶段唯一数据合同） | 3458（`wc -l` 复算；与机械列一致。**已逐行读完**：分 6 段覆盖 1-560、560-1259、1259-1959、1959-2658、2658-3218、3218-3458） | **跨三个角色的混合体**：主体（§1-§30 的逐模块 dtype/shape/unit/invalid 登记）= 合同说明，目录 `docs/contracts/` 一致 ✓；§31.1/§31.1a/§31.7/§28.6/§30.4 承载**科学定义与推导**（面亮度单位推导链、`π/2` 中位数渐近方差、GUM/EMVA 1288/FITS 4.0/IAU 逐字引文、variance 传播通式）→ 应属 `docs/science/`（标准 01 §1"科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`"，§2 角色表）；§31.10 的 `CTRL-*`/`AR-*`/`SO-*`/`DI-*` 台账承载**过程治理与签字状态** → 应属 `artifacts/evidence/`（标准 01 §2"历史档案、过程台账不放在正式文档层；需要留证的台账落 `artifacts/evidence/` 并由正本引用"）→ **角色错放（局部），应迁往 `docs/science/`（推导面）与 `artifacts/evidence/`（治理台账面）** | 全仓跨阶段数据语义：dtype/shape/单位/无效值/所有权/序列化，加 V6 条款集与签字台账 | **有**：抬头 line 3 `ASTROCS_DESIGN.md §3.1（数据对象）、§10（I/O 与原子产品）`，章节号与标题实测匹配（221 / 684）✓；但**§31.10（line 3457）把"上游"写成 `docs/contracts/DATA_SEMANTICS.md §31.1–§31.9、§28.6`——指向本文件自身** → 自指上游，标准 01 §3"每份下层文档标明其上游最高设计条款"在最深的一节上失效；§8-§30 各节另引 12+ 份 `docs/science/`、`docs/algorithms/` 条款，多数有效，失效项见"悬空引用" | 328 个文件引用（`git grep -l DATA_SEMANTICS \| grep -v 自身 \| wc -l` → 328；含 `ACCEPTANCE_SPEC.md`、`ASTROCS_DESIGN.md`、`README.md` 与 `eng/contracts/**` 30+ 处 back-ref）。`eng/contracts/` 反向把本文列为 gate 出处：`schemas/unified/*.schema.json` 每个 `x-astrocs-gate.source = "docs/contracts/DATA_SEMANTICS.md §31.6"`、`schemas/product_family_field_constraints.schema.json` 9 处、`data/v6_clause_registry_v1.json` 30 处 | 自称正本（line 1"跨阶段唯一数据合同"、line 5"权威：本文档。任何模块/文档的定义都只有这一份"、全文"唯一权威/唯一口径/只有这一份"命中 **89 处在 88 行**）。**判定：不是正本**——同一批字段的机器事实在 `eng/contracts/schemas/unified/` + `product_family_field_constraints.schema.json` + `v6_clause_registry_v1.json`，且本文与它们对同一字段给出不同取值（A-2、A-3）；本文的 `§4a` 又把同一三态编码在 §12.2/§12.4/§30.4 重复三遍，须靠 `finding F-UNC-001`（line 3012-3015）自设优先级才能读 | **与 `eng/contracts/schemas/unified/{variance,ivar}.schema.json` 就 `pixel_area_power` 取值直接冲突**（A-2，贴两段）；与 `docs/contracts/DATA_ARTIFACTS.md` §1 就 invalid 表示重叠（§4a NaN 三态 vs 该表 line 50/51 "null（无覆盖）"）；与 `docs/contracts/UNIFIED_OBJECTS.md`、`docs/contracts/unified_object_registry.json`、`eng/contracts/data/unified_object_compatibility_map_v1.json` 就"13 对象身份/单位/权重判定"四写（该组属 DA/DB 其他批次，跨批发现见 §3） | **无 H1 前元信息块**（机械列 meta_block=False ✓），但 line 127/174/262/339/356/417/525/651/713/797/876/953/1064/1167/1377/1479/1691/1863/2037/2275/2355/2442/2538 每节都有 `> ID: … 状态: CONTRACT_READY` 型块，且 line 2637/3110 出现 `状态：**CONTRACT_READY** / 现行` + 变更编号 → 按节散布的元信息块，违反标准 01 §4 | 日期 7 处/6 行（3110、3131、3140、3150、3252、3259）；任务编号 **164 处/132 行**（DATA-P1-*、SCI-*、ALG-*、DISP-*、B2-A*、FZ-*、CHG-2026-09-20/22、PSFSW-RETIRE-01/03、GAP_AUDIT §4.4/§9.68/A-04、RQS V2-N-08、P14-N-08、FREF-BASELINE-001、RESCUE-FD-02、M2a-H-3、SNR-PREC-001…）；历史叙事 8 处（317、551、1515、3131、3259、3305…）；删除线 **16 处在 14 行**（382、1198、1199、1599、1636、1647、1657、1670、2837、3131、3252、3259、3285、3305）；"唯一权威/唯一口径/只有这一份"89 处 → 标准 01 §1 末条 | ①`§9.73 A44` 15 处（382、1198、2837、3259、3262、3263、3265、3285、3305、3319、3320、3440…）与 `§9.68`/`§9.71`/`§9.44`：宿主文档 `工程控制/RELEASE-02/GAP_AUDIT.md` **不在库内**（`git ls-files 工程控制/` 只有 `RELEASE-05/**`；现存 `工程控制/RELEASE-05/GAP_AUDIT.md` 65 行、章节仅 G0-G4）→ 全文系统性悬空（A-1）；②`02_FROZEN §7`（line 375）、`01_SCIENCE_AUTHORITY_BASELINE §4`（2597、2784）：`git ls-files` 无任何同名文件；③`eng/tests/contracts/product_family/evidence/rc_summary.json`（3297，被作为"正例逐条通过"的证据）→ 文件不存在；④`GAP_AUDIT §4.4`（354、359）、`GAP_AUDIT A-04`（382）→ 同上悬空；⑤内部节号：`## 29`（2439）排在 `## 28`（2536）之前，而 §29.5（2518）又把 §28 当已给出的上游引用；`§7a`（2270）、`§9.60`/`§9.67`（589）在本文件无对应节 → 悬空内部锚；⑥`docs/design/PHASE{1,2,3}_DETAILED_DESIGN.md`、`eng/contracts/schemas/unified/{variance,ivar}.schema.json` 被机械列判悬空 → **实测存在**（误报，详见 §3.5） | **6 条实质冲突（A-1…A-6）**，含 1 条使现行合同与已删除代码相互矛盾（A-4，说明层与实现/合同两侧各说一套）。另有 §20.1 vs §20.3 的**文档内互斥条款**（A-4） | **拆分 + 迁移**（依标准 01 §2 角色表、§1 权威链）：①逐模块 dtype/shape/invalid 登记面留在 `docs/contracts/`，但删掉全部"唯一权威/只有这一份"自我声明，改为"本文逐项转述 `eng/contracts/**` canonical schema，冲突以 schema 为准"（标准 01 §1）；②§31.1a/§31.1/§31.7/§28.6/§30.4 的科学推导与文献逐字引文迁 `docs/science/`（该处才是公式权威）；③§31.10 的 `CTRL-*`/`AR-*`/`SO-*`/`DI-*`/`SUP-*` 治理台账迁 `artifacts/evidence/` 并由正本引用（标准 01 §2"过程台账不放正式文档层"）；④A-4/A-5/A-6 属正确性缺陷，先就地改红再谈搬迁 |
| `docs/contracts/DUAL_LINE_CONTRACT.md` | 双线合同与冻结点（CONTRACT-501 / RELEASE-05） | 41（`wc -l`；与机械列一致） | 标为"合同说明"（现目录 `docs/contracts/`），实际回答的是"两条并行工作线各自能改哪些文件、争议去哪登记、何时冻结"→ 属**标准与检查**（应属 `docs/standards/`、`docs/ci/`）或控制包任务纪律（`工程控制/<包>/`）；`docs/contracts/` 的角色定义是"JSON/schema/字段语义"，本文件不定义任何字段语义 → **角色错放，应迁往 `docs/standards/`（或随所属控制包落 `工程控制/RELEASE-05/`）** | A/B 双线文件域互斥、域边界纪律、数值等价基线、串行合并点、四份合同冻结 | **有**：抬头 line 3 `ASTROCS_DESIGN.md §8（软件架构）、§9（CPU 后端与资源）；ENGINEERING_SPEC.md §4.1（管线纪律）`，三处实测存在（542 / 669 / 55）✓；line 4 另引控制包 `RELEASE-05 00_README.md §3（双线纪律）`、`TASK_LIST.md`（文件域列）→ `grep -n "^## 3" 工程控制/RELEASE-05/00_README.md` = line 39 "## 3. 双线纪律（重点）" ✓ | **6 个文件**引用（`git grep -l DUAL_LINE_CONTRACT \| grep -v 自身` → docs/DOCUMENT_INDEX.yaml、eng/contracts/schemas/dual_line_file_domain.schema.json、eng/tools/contract_doc_sync.py、eng/tools/doccheck/check_doc_hygiene.py、reports/v19r2/file_audit_inventory.csv、reports/v19r2/source_manifest.csv）。按 DOCUMENT_INDEX 自身扫描口径（排除 reports/）活动下游 = **4**，其中 2 个是检查它自己的工具 → 除索引与工具外**无一份正式文档把它当上游读**（近孤儿，但未成孤儿） | 是（"双线文件域"主题唯一份） | **与本批 CONFIG_CONTRACT 无重叠**；与 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`/`SCHEDULER_CONTRACT.md`/`PERF_GATE_CONTRACT.md` 是"同批冻结"并列关系（line 41），非重复 | **有**：line 6 `> ID: CONTRACT-501-DUAL-LINE 状态: FROZEN（RELEASE-05 冻结点） 机器 schema: eng/contracts/schemas/dual_line_file_domain.schema.json` → 标准 01 §4 禁；H1 标题本身含 `CONTRACT-501 / RELEASE-05` 两个编号 | 任务编号 11 处/8 行（标题 CONTRACT-501/RELEASE-05；line 4 RELEASE-05；line 6 同；line 18 DOC-502；line 19 SCI-503/504/505；line 31 ARCH-505 步骤 1/4；line 36 RELEASE-05/ACCEPTANCE.md；line 41 CONTRACT-501）；日期 0 处（✓ 干净）；历史叙事词 1 处：line 16 "旧架构退役"；状态声明"FROZEN/冻结"承担的是 §12.5 交付状态之外的第二套状态语义 → 标准 01 §4 + AGENTS §7 状态口径 | 无路径级悬空（41 行内引用的 12 个路径全部存在：`docs/ci/`、`docs/owner/`、`docs/architecture/`、`docs/plugins/`、`docs/science/`、`eng/ci/`、`eng/contracts/`、`eng/tests/`、`lib/`、`实验/`、`工程控制/RELEASE-05/ACCEPTANCE.md`、`OPEN_QUESTIONS.md`、同批三份合同 ✓）。**唯一风险是生命周期**：§5/§6 把差异单与冲突登记的落点定在 `工程控制/RELEASE-05/ACCEPTANCE.md`（实测该文件 line 31 有"## 双线文件域冲突登记"表），而 `CONTROL_PACK_SPEC.md` §9 规定"收口即清理…随后把本控制包目录从仓库移除" → 现行合同的唯一变更登记面被指向一个按其自身规范必然消失的目录（A-7，非悬空而是"必将从悬空"） | 与上位（机器 schema）**无字段冲突**：`eng/contracts/schemas/dual_line_file_domain.schema.json` 的 `lines[].line` 枚举 = {A, B, shared, report}，与本文 §2 四行一一对应；`globs`/`duty`/`forbidden` 三列与文档表列同构；schema `x-doc` 回指本文件 ✓。**但**：`git grep -l "astrocs.dual-line-file-domain"` 只命中 schema 自身 → 无任何实例文档满足该 schema，即"机器 schema"仅是形状，四条文件域的具体 glob 集合只在本文以散文存在，schema 无法校验 glob 内容 | **保留内容、迁移位置**：按标准 01 §2 角色表（标准与检查 → `docs/standards/`、`docs/ci/`）迁出 `docs/contracts/`；同时在迁移后的文本里把差异登记面从控制包改到正式文档层（标准 01 §2"历史档案、过程台账不放在正式文档层"的反向要求：正式文档层的条款不得把裁决面挂在会被清理的过程目录上）；抬头与标题去编号、line 6 元信息块删除（标准 01 §4） |

## 2.2 DB-07 偏差明细（schema / 代码为事实源）

**A-1（`§9.73 A44` 是全仓无宿主的权威锚）** 本文 15 处、本批 CONFIG_CONTRACT 4 处、`docs/`+`eng/`+`lib/` 合计 **84 个跟踪文件**引用（`git grep -ln "§9.73" \| wc -l` → 84）。唯一被点名的宿主是 GAP_AUDIT：机器 registry 自己给出全称 —— `eng/contracts/data/v6_clause_registry_v1.json#a44_deprecation.banner`："（负责人 2026-09-20 裁决，GAP_AUDIT.md §9.73 A44…）"，`eng/ci/check_no_weight_mode.py:6` 写 `工程控制/RELEASE-02/GAP_AUDIT.md §9.73 裁决 A44`。实测：`git ls-files 工程控制/` 只有 RELEASE-05；`工程控制/RELEASE-05/GAP_AUDIT.md` 65 行、`grep -n "^#"` 只有 G0-G4 五节，**无 §9.73、无 A44**；RELEASE-02/03/04 全部不在库内（`CONTROL_PACK_SPEC.md` §9 的收口清理条款正是其消失的原因）。→ 承载"权重模式不存在"这一现行硬约束的**裁决正文在仓内无承载文件**。该约束本身在最高设计有对应文字（`ASTROCS_DESIGN.md:225`"**全程只有 SNR，没有"权重模式"这个概念**…权重是 Phase2 集成时按天球像素对应的输入帧集合现场计算的派生量"、`:229`"权重的产生链固定为两步、没有可选择项"），但两处都被引用成 `§2.1`（本文 382/1198/3305；`unified_object_compatibility_map_v1.json:139`"ASTROCS_DESIGN.md §2.1（全程只有 SNR，不存在权重模式）"）与 `§3.1:175`（本文 3262）——**§2.1 是测光创新点，行 175 属 §2.2 内部**，章节号与行号同时错位。处置指向：把 A44 约束的权威锚改指 `ASTROCS_DESIGN.md §3.1`，并删除对已清理控制包的悬空引用。

**A-2（`pixel_area_power` 取值：说明文档与 schema 互斥，schema 为事实源）**
说明侧 `docs/contracts/DATA_SEMANTICS.md:3224-3225`："内部量纲代数把立体角维记在 `px_power` / `pixel_area_power` 上（`signal_sb = -2`、**`sb_variance_out = -4`、`sb_ivar_out = +4`**）…`pixel_area_power = -2 ⇔ 串含 /sr`、**-4 ⇔ /sr^2**、**+4 ⇔ sr^2/…**"；`DATA_SEMANTICS.md:3249` 再以"canonical 缺省"复述同一组数（-4/+4）。
事实源侧：`eng/contracts/schemas/unified/variance.schema.json` 与 `ivar.schema.json` 的 `properties.units.properties.pixel_area_power.enum` **均为 `[-2, 0]`**；13 份 unified schema 逐一提取后除 support/coverage/rejection/validity/provenance 无该键外，其余含该键者全部只允许 -2/0。
→ 说明文档规定的方差/逆方差取值（-4/+4）在机器合同里**不可表达**：任何按 §31.1a 写的产品会被 unified schema 直接判违规。这条同时暴露 §31.1a 自称"由本 §31 正文为语义权威"（line 3111）与 unified schema 自称 canonical 的双头问题：冲突时无裁决规则。

**A-3（`§31.1a` 声称的机器强制点位与实际键位部分错位）** `DATA_SEMANTICS.md:3250` 声称"机器强制（canonical 对象层）：`provenance.schema.json` 的 `units.allOf/if-then`…"。实测 `eng/contracts/schemas/unified/provenance.schema.json` 的 `units` **不含** `pixel_area_power` 的 `enum`（该键只在 signal/variance/ivar/frame_snr/source_snr/sparse_snr_layer/depth_m5/point_information 8 份里带枚举），if-then 门落在各对象自身的 `allOf` 里。→ 门是存在的，但不在被点名的那份文件里；按本文指针去核会核空。

**A-4（`legacy_allow_weight_fallback`：同一文档内两条互斥条款，且其中一条与代码相反）**
`DATA_SEMANTICS.md:1199`（§20.1）："~~legacy_allow_weight_fallback~~（键不存在）…**禁用键**…ivar 产品缺失 → rc=7 显式科学错误（stage2.cpp:565-578）；配置中出现该键 ⇒ 具名 fail-closed 拒绝"。
`DATA_SEMANTICS.md:1286-1288`（§20.3）："红线: 禁 support 冒充 ivar…唯一例外 = **显式 legacy 降级键 `legacy_allow_weight_fallback=true`（降级 support 并 diagnostics 标红，stage2.cpp:565-578）**"。
两条引用**同一个代码锚**、给出**相反语义**（拒绝 vs 降级例外）。事实源：`lib/algorithms/coverage/include/astro/phase2/stage2_common.h:157`"原 legacy_allow_weight_fallback 开关**已删除**"；`lib/algorithms/coverage/src/stage2_common.cpp:455-460`"legacy_allow_weight_fallback **已删除** —— …if (in.contains(\"legacy_allow_weight_fallback\")) { *err = …已按 §9.73 裁决 A44 删除"；`sed -n '560,585p' lib/algorithms/coverage/tools/stage2.cpp` 显示 565-578 是 `p2_sky_plane_build_adaptive`（天光面自适应回路）代码，与被声称的 ivar 降级门无关。
→ §20.1 正确、**§20.3 错误**，且错误延伸三处：§30.1 unavailable 规则 2（line 2741-2743 仍以该键为 live 例外）、§30.5 V2 验证门（line 2968-2970 仍要求"缺 ivar 帧 + 降级键=true → unavailable"）、§20.1 自身的锚点行号。这些条款若被实现照做，会把"键出现即拒绝"退化为"键出现即降级"（fail-open）。

**A-5（§31.7 指向 schema 中不存在的 JSON 指针）** `DATA_SEMANTICS.md:3304`："依据…**`eng/contracts/schemas/product_family_field_constraints.schema.json#/$defs/psfsw` 的 `concentration.units` 收紧 pattern**"。实测该 schema 的 `$defs.psfsw` 顶层 `required` = [psfsw_schema, schema_version, components, composite, common_star_set, validity, covariance_ref, effective_psf_ref, provenance_ref, component_flux_unit]，**无 `properties.concentration`**（concentration 在 `properties.components` 之下），按文档字面指针解析落空。同一句里"`weight_mode`/`weight` **不在** `required` 内"经复算是**正确的**（该文件 `x-astrocs-retired-declaration-optional` 自述同一事实）→ 一条内既有可用结论也有失效指针。

**A-6（§20.2 自证的"provenance 缺口"说明本文与 §5.3 守卫面脱节）** `DATA_SEMANTICS.md:1238-1249` 登记：现行 Phase1/Phase2 FITS tile **不写 `BUNIT`**、`signal/properties` 无 `pixel_semantics`/`pixel_area_power` 键，因此 §5.3 输入语义守卫（`lib/algorithms/resample/p3_rsmp_units.cpp:137-171 resolve_bunit`）"即使接线也会拒绝当前产品"，并给出就地复现判据。与之相对：§27.1/§27.3（line 2375、2378）把 Phase3 写出面的 `bunit` 缺省冻结为 **`ADU/sr`**、§29.2 把读侧 BUNIT 也写成 canonical `ADU/sr`。→ 上游产品不携带 BUNIT 而下游合同以 BUNIT 为量纲判据，属**同文件内的两处口径未合流**；本文如实登记为缺口（这是好的），但它使 §31.1a "产品 FITS/HiPS 写盘 BUNIT 一律取该串"在现行产品上不成立 → 该条的"现行"资格需要一句适用域限定。

**A-7（结构缺陷：节序倒置与自指上游）**
- `## 27`（2353）→ `## 29`（2439）→ `## 28`（2536）：节序倒置，且 §29.5（line 2518）以"DATA-P3-WCS（§28，输出平面几何）"作上游引用，读者按物理顺序尚未见到 §28。
- `### 1.2`（DATA_ARTIFACTS line 42）先于 `### 1.1`（line 64）。
- `DATA_SEMANTICS.md:3457`（§31.10 末行）"上游：`ASTROCS_DESIGN.md §0/§12；docs/contracts/DATA_SEMANTICS.md §31.1–§31.9、§28.6`" → 一节把**自己所在文件的其他节**登记为上游，标准 01 §3 的双向可追溯在此闭合为环。
- `DATA_SEMANTICS.md:807`（§16 头）"本节权威等级由上位文档授予（依 §0.1：下级文档不另立权威链）"、`:814`"本节不自称「唯一权威」" —— 与同一文件 `line 5`"权威：本文档。任何模块/文档的定义都只有这一份"**同文档互斥**：一处否认、全局声明肯定。

**一致项**：§31.10 的条款计数与 `eng/contracts/data/v6_clause_registry_v1.json#counts` **逐字一致**（96 = FROZEN 39 / PENDING_OWNER_SIGNOFF 49 / OPEN 8；数值阈值 54、开放项 27、签字项 7、被取代节 10）；§31.4 声称的 registry 子键 `canonical_fields`/`dual_mapping`/`legacy_integer`/`forbidden_third_vocabulary_tokens` 在 `weight_vocabulary` 下全部存在；§31.6 声称的产品族记录层"十类记录"与 `$defs` 实测十个键（units/signal/covariance/psf/effective_psf/point_information/weight_mode/psfsw/provenance/phase3）一致；§22 line 1510 声称的 `phase_config_mosaic` `algorithm_rejection_method` 枚举 = {auto, sigma, winsorized, averaged_sigma, linear_fit, esd, percentile} 且不含 minmax，与 schema 逐字一致（schema 内 `minmax` 只出现在散文注记）；`algorithm_weight_mode` 在三份 phase_config 中确实不存在为属性（只在 A44 散文注记里出现）。

<!-- PROGRESS: 已审 4/4 -->

---

# 第三部分 · 台账后四节（DB-06 + DB-07 合计）

## 3.1 同主题组与差异

### G-1「跨阶段数据语义」组 —— 四写并存（本批 2 份 + 别批 2 份，均给 `路径:行`）

| 份 | 路径 | 该主题上它说什么 |
|---|---|---|
| ①说明层·全局 | `docs/contracts/DATA_SEMANTICS.md:5` | `权威：本文档。任何模块/文档的定义都只有这一份。` |
| ②说明层·工件 | `docs/contracts/DATA_ARTIFACTS.md:5` | `> 权威: docs/contracts/DATA_SEMANTICS.md（语义）+ 本表（artifact schema 登记 + 歧义映射）` |
| ③说明层·对象 | `docs/contracts/UNIFIED_OBJECTS.md`（别批）与 `docs/contracts/unified_object_registry.json`（别批） | 13 对象身份/退役映射（`DATA_ARTIFACTS.md:44` 自称"canonical 在 eng/contracts/schemas/"同时把上位锚交给 `docs/design/UNIFIED_MODEL.md §2`） |
| ④机器合同 | `eng/contracts/schemas/unified/variance.schema.json`（`x-astrocs-object.authority` = `docs/design/UNIFIED_MODEL.md §2；ASTROCS_DESIGN.md §2；ENGINEERING_SPEC.md §3/§4.7`）、`eng/contracts/data/v6_clause_registry_v1.json`（顶层键 `semantic_authority`） | 各自的权威声明 |

差异（同字段两侧实际值）：**`pixel_area_power` 对 variance/ivar 的合法取值**
①侧 `DATA_SEMANTICS.md:3225`：`pixel_area_power = -2 ⇔ 串含 /sr`、`-4 ⇔ /sr^2`、`+4 ⇔ sr^2/…`（并在 `:3249` 以"canonical 缺省"重申 `sb_variance_out=-4`、`sb_ivar_out=+4`）；
④侧 `eng/contracts/schemas/unified/variance.schema.json:103-109` 与 `ivar.schema.json` 同位：`"pixel_area_power": {"enum": [-2, 0]}`。
→ 两值集无交集（-4、+4 均不可表达）。判"同一主题多份正本"成立：**说明层与机器合同各有一套取值，且都自称权威**；按本节点口径以 ④ 为事实源，①侧记偏差 A-2。

### G-2「variance/ivar 无效值三态」组 —— 一文件三写 + 一列异值
①`DATA_SEMANTICS.md:77-89`（§4a 三态表，"本表是 variance/ivar 编码的唯一权威…凡与本表不一致的表述一律以本表为准"）
②`DATA_SEMANTICS.md:453-454`（§12.2 输出行散文重述同一三态）
③`DATA_SEMANTICS.md:2747-2758`（§30.1/§30.4 输出面表，Phase2/Phase3 版）
④`DATA_ARTIFACTS.md:14-15`（`DATA-IMG-VAR-001` invalid 列："**无方差信息 = 0（显式不可用，§4a）**；NaN/负 = 损坏"）、`DATA_ARTIFACTS.md:50-51`（`DATA-OBJ-VARIANCE-001` invalid 列："**null（无覆盖）**；负值/NaN=损坏"、`DATA-OBJ-IVAR-001`："0=显式不可用；**null=缺失**"）
差异：①侧规定"无覆盖 ⇒ `NaN`（与 `signal=NaN ∧ support=0` 同态）"；④侧 §1.2 对象行规定"无覆盖 ⇒ `null`"。二者不是同一层的合法分层（①已在 `DATA_SEMANTICS.md:65-67` 交代 `ivar = 1/variance` 只在同一承载面内成立，即"二进制面 NaN / JSON 面对象缺失"可读作两形态），但 `DATA_ARTIFACTS.md:44` 的划线口径是"对象身份/无效值与缺失表示…一律以该 canonical schema 为准"，而 `unified/variance.schema.json` 的 `missing_value` 是**由产品自选的枚举**（`missing_repr ∈ {null, absent_key, zero, null_or_super…}`、`invalid_repr ∈ {null, nan, zero, nan_or_negative, …}`）而非固定值 → ④侧既不等于 ①，也无法由 schema 判定，形成"无裁决者的双值"。
本文件自己已识别并登记：`DATA_SEMANTICS.md:3012-3015`（`F-UNC-001`："§12.4 与 §30.4 的表述分别指向前两者，**不存在双值**"）——需要 finding 才消解的三重表述 = 标准 01 §4"自洽"缺陷，不是重复份，但必须收敛成一处。

### G-3「A44 / 权重模式不存在」组（跨批，注明别批路径）
承载同一硬约束的四个锚点：`ASTROCS_DESIGN.md:225`（正文，唯一在库且语义完整）、`DATA_SEMANTICS.md:3262`、`CONFIG_CONTRACT.md:50/67/82/192`、`eng/contracts/schemas/phase_config_mosaic.schema.json:17`。四者**结论一致**（该键不存在、出现即拒绝），但**出处标注三套**：最高设计条款分别被写成 `§2.1`（`DATA_SEMANTICS.md:382/1198/3305`）、`§3.1:175`（同文 `:3262`）、无章节（`CONFIG_CONTRACT.md:50/67/82`），而三处都另加 `§9.73 A44` 作为主锚（A-1：宿主已不在库）。→ 同主题一份正本（§3.1 正文），三处下游标注错位；整治动作 = 统一改指 `ASTROCS_DESIGN.md §3.1`。

### G-4「配置数值快照」组（本批 1 份 + 机器数据）
`CONFIG_CONTRACT.md:32/44-55/199-202` 与 `eng/packaging/config/defaults.json`、`eng/packaging/config/config_registry.json`。差异见 D-2/D-3/D-4/D-6：字段总数 50 vs 59、`authority_status` 值域 3 vs 5、登记册行数 117 vs 120 且 5 项分布全部不同。性质：说明层**复制**了数值面而非索引化（尽管 `:35`/`:207` 两处都声明"本册不复制科学数值（数值唯一源 = defaults.json / phase_config schema）"）→ 复制件与其宣称的唯一源已经不等，是"说明层越位成第二套事实源"的第二例（第一例见 G-1）。
对照组（做得对的）：`CONFIG_CONTRACT.md:68`"本文件与登记册**各自只引用数值**"与 §4 滤镜库 line 104"逐字转录…机器门逐条与源文件比对"——45 键、`lookup` 六字段、`channel` 值集 `{B,G,R,L,HA,OIII,PAN,""}` 实测计数 `10/10/10/4/1/1/2/7`、`$defs.filter_name` 枚举与库键集合相等：**全部复算成立 ✓**。→ 同一文件内"转录+机器比对"与"快照+无对账"两种做法并存，问题在后者。

## 3.2 索引与链接缺陷清单

`docs/DOCUMENT_INDEX.yaml` 核对（本批 4 份）：4 份**均已登记且各恰 1 条**（`grep -c 'path: "docs/contracts/<F>.md"'` → 1/1/1/1）；`docs/contracts/` 全目录覆盖核对：磁盘 15 份 `.md` + 4 份非 md（`INDEX.yaml`/`API_CONTRACTS.csv`/`config_separation_anchors.json`/`unified_object_registry.json`）**全部登记，无未登记项、无"登记了但文件不存在"项**（脚本复算：`registered=19`，缺失集与未登记集均为空）。缺陷在**字段口径**与**第二张索引**：

| 类型 | 条目 | 说明与证据 |
|---|---|---|
| 登记字段与标准不符 | 全部 265 条（含本批 4 条） | 标准 01 §3 要求"登记内容含路径、角色、主题、上游、下游"；`DOCUMENT_INDEX.yaml` 实际字段只有 `path/status/duty/upstream/downstream/notes`（`grep -oE "^ +[a-z_]+:" \| sort \| uniq -c` → 265/265/265/265/148）。**无 `role`、无 `topic`** → 角色无法机器核对，本批的角色错放判定（DUAL_LINE）在索引层不可见 |
| 上游登记与文档抬头不符 | `docs/contracts/DUAL_LINE_CONTRACT.md`（索引 line 578-580） | 索引 `upstream: "ASTROCS_DESIGN.md §8（软件架构）、ENGINEERING_SPEC.md §4.1（管线纪律）"`；文档 line 3 是 `§8（软件架构）、§9（CPU 后端与资源）；ENGINEERING_SPEC.md §4.1` → **索引漏登 §9** |
| 下游登记与实际不符 | 同上（索引 line 580） | 索引 `downstream: "eng/contracts/schemas/dual_line_file_domain.schema.json、工程控制/RELEASE-05/ACCEPTANCE.md"`；`git grep -l DUAL_LINE_CONTRACT` 不含 `工程控制/RELEASE-05/ACCEPTANCE.md`（该文件 line 31 有"## 双线文件域冲突登记"表，但**不出现文档名**），且该索引自身头 line 30 规定扫描面排除 `工程控制/` → 既非扫描所得、又属被排除目录，两义冲突 |
| notes 夹日期/任务号 | `docs/contracts/DATA_SEMANTICS.md`（索引 line 617）、`CONFIG_CONTRACT.md`（line 575） | `notes: "P1-WCS-DOC (2026-09-07) 追加 §18 …; P2-COV-DOC (2026-09-07) 追加 §19 …"`；`notes: "CFG-001 …CI-003（2026-09-16）补入活动索引…"` → 标准 01 §4 的"正文无日期/无任务编号"要求若延伸到索引，此处为反例（索引本身另有 `重写：DOC-403（2026-09-21，基线 HEAD d1ccfa1…）` 头部块） |
| **第二张索引地图（结构性）** | `docs/contracts/INDEX.yaml`（别批文件，但由本批两份文档牵出） | 该文件 116 条 `- id:` 合同登记，自带 `type/status/owner/version/upstream/downstream`，其中 `- id: DATA-SEMANTICS-001 / path: docs/contracts/DATA_SEMANTICS.md / upstream: []`（line 645-652）、`- id: DATA-ARTIFACTS-001 / upstream: [DATA-SEMANTICS-001]`（line 733-740）。与 `DOCUMENT_INDEX.yaml:1`"全文档集唯一索引地图"（标准 01 §3 同源）**并存**；且它把 `CONFIG_CONTRACT.md`/`DUAL_LINE_CONTRACT.md` 完全**不登记**（`grep -c "CONFIG_CONTRACT\|DUAL_LINE" docs/contracts/INDEX.yaml` → 0），把 `DATA_SEMANTICS` 的上游登成空数组、`DATA_ARTIFACTS` 的上游登成另一份说明文档而非最高设计 → 同一份文档在两张地图里上游不同，属标准 01 §1"不声明第二套权威顺序"与 §3"唯一索引地图"双重违规 |
| 第二套状态词表 | `docs/contracts/INDEX.yaml:6-8` | `status 取自文档自身标注(ACTIVE/FROZEN/DERIVED→ACTIVE; 未标注→DRAFT; 退役→OBSOLETE)` → 与 `DOCUMENT_INDEX.yaml` 头声明的 `{ACTIVE_NORMATIVE, ACTIVE_INFORMATIVE, GENERATED, ARCHIVED_NON_NORMATIVE}` 及 `ASTROCS_DESIGN.md §12.5 状态阶梯`三套并存；`DATA_SEMANTICS.md:3118`/`§31.10` 又自建第四套 `{FROZEN, PENDING_OWNER_SIGNOFF, OPEN}` 用于条款 |
| 悬空（指向不存在文件） | 见每份"悬空引用"列 | 本批合计 11 处独立悬空：`PROJECT-GOVERNANCE-01/tasks/CFG-001.md`、`eng/tests/gaia_zlib`、`DATA-SEMANTICS.md`（连字符）、`module_ports.registry.json:246/:277`、`02_FROZEN §7`、`01_SCIENCE_AUTHORITY_BASELINE §4`（2 处引用）、`eng/tests/contracts/product_family/evidence/rc_summary.json`、`§9.73`/`§9.71`/`§9.44`/`§4.4`/`§4.5`/`A-04`（GAP_AUDIT 系，合计 90+ 处引用点）、内部节号 `§7a`/`§9.60`/`§9.67`。确认命令：`ls docs/contracts/`、`git ls-files 工程控制/`、`git ls-files \| grep -iE "02_FROZEN\|SCIENCE_AUTHORITY_BASELINE"`（零命中）、`ls eng/tests/contracts/product_family/evidence/`（无 `rc_summary.json`）、`grep -n "阈值冻结锚点" docs/science/REJECTION.md` → 115 |
| 行号锚漂移（文件在、行不对） | 本批 4 份普遍 | 最高设计 8 处（D-1）；科学文档 5 组（D-5、psf :7/:92→实测 :9/:94、precision :53→实测 :56、photometry :21,24,38,39→实测 30,24,192,44）；代码 40+ 处（`module_adapters.cpp` 系约 +240 行、`stage2.cpp:565-578` 内容错指、`hardware_inspect.cpp:259-265`→实测 261/263、`profile_gen_v2.cpp:571` 而 schema 写 :568）；机器 schema 反向锚 `phase_config_export.schema.json:497` 指 `docs/contracts/CONFIG_CONTRACT.md:81`（`output_mode` 必填实际在 :83/90-97） |

## 3.3 覆盖率自报

已审 **4 / 4**（清单合计 4 份：DB-06 = 2、DB-07 = 2）。
**未逐行读名单：空。** 四份全文逐行读完：`CONFIG_CONTRACT.md` 1-248、`DATA_ARTIFACTS.md` 1-128、`DUAL_LINE_CONTRACT.md` 1-41、`DATA_SEMANTICS.md` 1-3458（分 6 段读完；单段读取受 256KB 上限约束，最大一段 560 行）。
交叉读（非清单项，仅为判上下游与重复）：`ASTROCS_DESIGN.md`（目录、§0/§2.1/§3.1-3.3/§4.3/§6.3/§7.2/§10 定位 + 8 处行号实测）、`ENGINEERING_SPEC.md` §4.1、`CONTROL_PACK_SPEC.md` §9、`工程控制/RELEASE-05/{00_README,ACCEPTANCE,GAP_AUDIT}`、`docs/DOCUMENT_INDEX.yaml`、`docs/contracts/INDEX.yaml`、`docs/science/REJECTION.md` 等 14 处科学锚点、`eng/contracts/**` 12 份 schema/registry、`lib/algorithms/coverage` 4 处代码锚。所有计数与"存在/不存在"判断均由本节点自己跑命令得出并附命令，未采用任何口头汇总数。

## 3.4 我证伪了任务书的哪个前提

AUD-101 任务书前提清单中，本批的复算结果是一条修正、一条新增：

1. **"docs/ 为重点（当前约 233 份、30+ 子目录）"——本批复算后成立，不证伪，但口径要钉死**：`git ls-files 'docs/*.md' 'docs/**/*.md' | sort -u | wc -l` → **233**（恰等于任务书数）；`find docs -type d | tail -n +2 | wc -l` → **37 个子目录**（一层 23 个）。→ 233 的口径 = **`docs/` 下跟踪的 `.md`，含 `docs/` 一层 5 份（DEVELOPER_GUIDE/GLOSSARY/KNOWN_LIMITATIONS/TROUBLESHOOTING/VERSIONING），不含 `docs/DOCUMENT_INDEX.yaml` 与 `docs/TRACEABILITY.csv` 在内的 25 份非 md**。
   同一基线下另外三个可复现分母，合并台账时按需用、不得混用：`git ls-files docs | wc -l` = **258**（docs/ 全部跟踪文件，非 md 25 份：12 csv + 8 json + 3 yaml + 2 py）；`git ls-files '*.md' | wc -l` = **540**（全仓 `.md`，含 `lib/**` 的 memory.md/README.md、`实验/**`、`artifacts/evidence/**`、`工程控制/**`、根治理 9 份）；`docs/*.md + 根 *.md + 工程控制/*.md` = 233 + 9 + 12 = **254**。
   顺带证伪一个别处流传的数：**"实际 360" 在本基线（`c8f64e9a`）的任何口径下都不可复现**——上面四式（233/258/540/254）与 `find docs -type f`（258）全部离 360 有距离；若 360 来自别的提交或含未跟踪本机残留，须由给出该数的一方补命令，否则不应作为覆盖率分母。
2. **"标记同主题多份（如测光、SNR、UPM、接缝散落在多个目录的情况）"——本批发现任务书未列的更高价值形态**：合同说明层与机器合同层对**同一字段取值**给出互斥规定（G-1 的 `pixel_area_power` -4/+4 vs {-2,0}；G-4 的字段总数 50 vs 59），不是"散落多份"而是"两份都对同一件事说话且不能同时为真"。任务书的"逐份登记 + 主题分组"框架会把这类降级为"上下游引用"，需在目标架构里单列一类判据：**说明层复述机器合同字段的每一个取值，必须有可判定的相等性检查，否则该复述本身即缺陷**。

未证伪的前提：`范围`（根 `.md`+`docs/`+`实验/`+`工程控制/`）、`禁止`（不修改/不移动/不删除、不凭印象判重复）与 `交付物` 三项在本批内无冲突。

## 3.5 机械列校准（前言 §4.5 要求的交付物）

对照 `独立审计/批次清单/doc_mechanical.csv` 本批 4 行，逐列复算：

| 列 | 机械列值 | 复算 | 结论 |
|---|---|---|---|
| lines | 248 / 128 / 3458 / 41 | 同 | ✓ 准确 |
| meta_block | True / False / False / False | DUAL_LINE `:6` 与 DATA_SEMANTICS 每节 `> ID:/状态:` 块（23 节）均存在 | **漏报 2 份**：判据只看了 H1 前紧邻块；DATA_ARTIFACTS `:5-6` 判 True ✓ |
| dates | 10 / 3 / 6 / 0 行 | 10 / 3 / 6 / 0 | ✓ 准确（本批日期密度低是真实特征） |
| task_ids | [] / [] / [] / [RELEASE-05] | 实测 28 / 16 / 132 / 8 行（34 / 17 / 164 / 11 处） | **严重漏报**：机械列对 `CFG-001`/`DATA-001`/`FZ-*`/`SCI-*`/`DISP-*` 全类不识别；DUAL_LINE 行还漏了 `CONTRACT-501`/`DOC-502`/`ARCH-505` |
| commit_sha | [] / [] / [00000006] / [] | 本批 **0 处**真 commit；`00000006` 实为 `:2656` 的 `max/min = 1.00000006`（AIT 等积投影面积比） | **误报 1 处**（前言已预警的高估陷阱）。我另跑的宽松 hex 正则在 DATA_SEMANTICS 命中 8 处，逐一看全是浮点常量（`1.482602218505602`、`0.6744897501960817`、`2.3504430539e-11` 等）→ 该列在本仓语境下**只能靠人判**，建议改为"仅匹配 `[0-9a-f]{40}` 或紧邻 commit/sha 关键词" |
| history_narrative | [50,67,82,137,169,192] / [56] / [317,1515,3131] / [] | 我口径（含"已作废/已删除/已闭合/重锚/原记/留痕/退役/已撤销"）：18 / 1 / 6 / 1 行 | 双向偏差：机械列漏 `CONFIG_CONTRACT:181`（"原记 96 行…为快照"）、`:193`（"已闭合"）、`:194`（"已按…裁决闭合"）、`DATA_SEMANTICS:3259/3285/3305`（退役/留痕系）；`DATA_ARTIFACTS:56` 单行含 17 处历史词被记 1 行 ✓（口径为行不为次数，需在台账里说明） |
| dangling_links | 6 项（含 `docs/plugins/*/*.md`、`eng/contracts/schemas/phase_config_{…}.schema.json`、`lib/**/module.yaml`） | 这些**全部存在**（`git ls-files 'lib/**/module.yaml'` → 22 份、`docs/plugins/*/*.md` → 23 份、三份 phase_config schema 均在） | **误报 3/6**（glob/brace 未展开即判悬空）。同时**漏报 4 类真悬空**：`DATA-SEMANTICS.md`（连字符文件名）、`工程控制/PROJECT-GOVERNANCE-01/tasks/CFG-001.md`、`eng/tests/gaia_zlib`（文档称"新出现"）、`02_FROZEN §7` / `01_SCIENCE_AUTHORITY_BASELINE §4`（非路径形态的文档名）。反向 **误报 1 项**：`docs/design/PHASE{1,2,3}_DETAILED_DESIGN.md` 与 `eng/contracts/schemas/unified/{variance,ivar}.schema.json` 展开后全部存在 |
| unregistered_index | 每份各 1-5 项 | 4 份在 `docs/DOCUMENT_INDEX.yaml` **均已登记**且恰一次 | **误报**：该项列的实际是"文档里出现的未登记路径样式"，不是"文档未登记"；列名与语义不符，合并时若当"未登记缺陷"用会造出假缺陷 |
| upstream_pointer | True ×4 | ✓ 4 份抬头都有 `> 上游：` 且章节号可解析 | ✓ 准确（但**行号**失真不在其判据内，见 3.2 最后一行） |

对机械工具的一句总评：`lines`/`dates`/`upstream_pointer` 三列可直接用；`meta_block`/`commit_sha`/`dangling_links`/`unregistered_index` 四列在本批**必须人工复算**（合计 6 处漏报、5 处误报）；`task_ids` 列在本仓 ID 词表下近乎失效（4 份共 45 处，识别出 1 处）。

<!-- PROGRESS: 已审 4/4（DB-06 2/2 + DB-07 2/2，本批完成） -->
