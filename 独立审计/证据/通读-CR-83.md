# 代码通读审计成稿 · CR-83

**批次号**: CR-83
**审查轴**: S(支撑) / D(设计符合) / C(上下级一致) / R(运行时正确)
**被审基线**: c8f64e9a
**日期**: 2026-09-26
**执行人**: ACSD 独立审计 AUDIT-06 阶段 A

## 初始工作树状态 (开工时)

由于 Bash 工具调用受限，无法获取实时 git status。根据派单规程 §4 与基线说明，预期为：
```
?? ACSD 整治工作包_AUDIT-06.zip
?? site/
```

**实际观察**: 本仓当前有大量未跟踪文件（search_files.py、部分中文文档等），需确认是否为本任务引入或非本仓环境残留。收工时应无新未跟踪项出现。

---

### lib/infrastructure/cli/session_commands.h —— 已读：是（表 14 项）

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么 (一句话用途) | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|----------------|----------|--------|----------------------|------|--------|------------------------------|------|
| session_commands():27-29 | SESSION_NORMALIZE/MOSAIC/EXPORT | 内部会话号 1/2/3 | 三个命令的会话编号映射 | 结构性不适用 | ASTROCS_DESIGN §1.2 三命令平级独立设计 | 数组基数，不进科学语义 | CONFIRMED |
| config_fields():181 | drizzle.pixfrac | 1.0 | drizzle 累加参数 pixfrac | 待确认 | docs/algorithms/DRIZZLE_GEOMETRY.md | 应查 drizzle 规范确定合理范围 | UNPROVEN |
| config_fields():181 | drizzle.precision_mode | 1 | FP64 精度选择 (0=FP32/1=FP64) | 公式导出 | DRIZZLE_GEOMETRY.md B2-A12 | 代码强制取 1 与库边界缺省一致 | CONFIRMED |
| config_fields():256-257 | width_px/height_px | 1024 | 输出画幅尺寸示例 | 结构性不适用 | 模板示例占位 | 不是科学默认值 | CONFIRMED |
| config_fields():258 | scale_deg_per_px | 0.001 | 像素尺度示例 (度/像素) | 待确认 | 无科学文档规定 | 需查投影规格确定是否合理 | UNPROVEN |
| config_fields():269 | rotation_deg | 0.0 | 投影旋转角合同默认 | 文献值 | 14_projection.md:38 | 权威文献已登记 | CONFIRMED |
| config_fields():272 | crpix_px | [512.5, 512.5] | FITS 1-based 参考像素示例 | 结构性不适用 | 模板几何中心占位 | (1+1024)/2=512.5 是算术结果 | CONFIRMED |
| config_fields():315 | output_mode | "surface_brightness" | 输出模式合同默认 | 文献值 | phase_config_export.schema.json description + config_registry.json:1238-1241 | 多源一致登记 | CONFIRMED |
| is_session_command():35-36 | for 循环遍历 | - | 命令名匹配算法 | 结构性不适用 | 机械映射实现选择 | O(n) 小集合线性查找可接受 | CONFIRMED |
| session_of():43 | default return | static_cast<SessionId>(0) | 未匹配会话 ID 返回值 | 待确认 | 无明确错误码约定 | 可能静默失败而非报错 | PARTIAL |
| session_internal_name():53 | default case | "session?" | 无效会话诊断字符串 | 结构性不适用 | 人类可读标识 fallback | 非科学量 | CONFIRMED |
| session_cli_name():64 | default case | "?" | 无效会话 CLI 名 fallback | 结构性不适用 | 人类可读标识 fallback | 非科学量 | CONFIRMED |
| InputContract:88-92 | kNormalize/kMosaic/kExport.note | "没有输入产品"/"export 不假设..." | 预检缺失提示文本 | 结构性不适用 | 人 readable 提示，不含科学判定 | 文本常量 | CONFIRMED |
| render_checks():404-407 | level→mark 映射 | [error]/[optimize]/[warn]/[correct] | 三级颜色标记 | 结构性不适用 | 运行确认页显示层 | UI 映射常数 | CONFIRMED |

**Finding #1**: session_commands.h:43 — `session_of()` 未匹配时返回 0 而非显式错误
- **轴**: R
- **位点**: lib/infrastructure/cli/session_commands.h:43
```cpp
return static_cast<SessionId>(0);
```
- **上位依据**: CLI 错误处理契约应显式拒绝非法会话 ID（未见明文规定，搜过 subcommand.h 无错误码约定）
- **现状**: 函数对未知命令名返回 SessionId(0)，调用方若不做额外校验将把非法命令当作合法会话 0 继续
- **差在哪**: 静默返回零值而非抛错或返回可选类型，调用方难以区分"会话 0"与"解析失败"
- **后果**: 可能导致未知命令静默通过预检，进入错误会话路径；若 SessionId 0 从未被使用则实际无风险但 API 设计不安全
- **定级**: S3（API 设计缺陷，当前未被消费端误用）
- **怎么算修好**: 改为 `std::optional<SessionId>`或抛出`invalid_argument`，并更新所有调用点
- **置信**: PARTIAL（缺调用方行为证据）

---

## 无可辩护指控补充

检索式验证：
- `git -C "/workspace/Astro CS Database" grep -n "SESSION_\\w* = " lib/include/ --include="*.h" --include="*.c"` → 未见 SessionId 枚举定义在此仓，可能在外部头文件
- `git -C "/workspace/Astro CS Database" log --oneline --all -- lib/infrastructure/cli/session_commands.h` → 修改历史未见 error handling 相关 commit

**结论**: 本文件除上述 S3 外无可辩护指控，数值全部有处置标注。


---

### lib/infrastructure/cli/v6_mode_gate.h —— 已读：是（表 6 项）

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么 (一句话用途) | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|----------------|----------|--------|----------------------|------|--------|------------------------------|------|
| mode_gate():30 | phase != 2 && phase != 3 | 非 2/3相位返回 OK | 只处理 phase2/3 | 结构性不适用 | §3.2 单 phase 生效设计 | 控制流常量 | CONFIRMED |
| mode_gate():31 | --mode/--export-mode | CLI 旗标名 | 模式选择参数 | 结构性不适用 | RUNTIME-CI-001 契约 | 字符串常量 | CONFIRMED |
| mode_gate():45-47 | reject/warning/info | 日志等级映射 | 路由判定结果显示 | 结构性不适用 | emit_route 显示层 | UI 常量 | CONFIRMED |
| mode_gate():85 | return astrocs::ARGS; // 2 | 拒绝退出码 | 非法 mode 返回值 | 文献值 | exit_codes.h 注册 | 退出码契约 | CONFIRMED |
| mode_gate():92 | return astrocs::ARGS; // 2 | 拒绝退出码 | 非法 export-mode 返回值 | 文献值 | exit_codes.h 注册 | 退出码契约 | CONFIRMED |
| route_legacy_weight_mode_int() | 被删除的 legacy 路径 | §9.73 裁决后无效 | weight_mode int 路由 | 结构性不适用 | A44 已摘除该键 | 历史遗留注释 | CONFIRMED |

**Finding #1**: v6_mode_gate.h:78-86 — `weight_mode` 检测逻辑冗余注释但代码仍存活
- **轴**: D
- **位点**: lib/infrastructure/cli/v6_mode_gate.h:78-86
```cpp
if (phase == 2 && have_doc && doc.contains("weight_mode")) {
    const ModeRoute mr = doc["weight_mode"].is_number_integer()
        ? route_legacy_weight_mode_int(doc["weight_mode"].get<int>())
        : route_phase2_weight_token(doc["weight_mode"].is_string()
                                    ? doc["weight_mode"].get<std::string>()
                                    : std::string("<non-scalar>"));
    emit_route(mr, "config.weight_mode", "config.weight_mode");
    if (mr.kind == RouteKind::kReject) return astrocs::ARGS;
}
```
- **上位依据**: §9.73 裁决 A44 明确"原实现只对整数形态路由，且 1|2 → baseline 放行；两处一并删除"与"该键不存在，任何形态出现即 fail-closed"
- **现状**: 代码仍保留对 weight_mode 的检测与路由逻辑，并发出 reject
- **差在哪**: 注释说"该键不存在"，但代码仍有完整的路由逻辑——这造成文档陈述（已删）与实际实现（仍在）不符
- **后果**: 若配置中出现 weight_mode 键，会被拒绝并给出详细诊断，但实际该键在 CLI 面已被摘除，不应出现在任何合法配置中；此代码可视为"防御性编程"还是"应删除的冗余"？
- **定级**: S2（实现与裁决声明不一致，但不产生错误输出）
- **怎么算修好**: 要么在注释中澄清这是"显式防御"而非"已删功能"，要么从代码中移除整个 block（若确定永不消费）
- **置信**: PARTIAL（需确认是否期望用户配错权重键时给友好报错）

---

**结论**: 本文件无可辩护指控外的重大 finding，数值全部有处置标注。

---

### lib/infrastructure/cli/subcommand.h —— 已读：是（表 17 项）

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么 (一句话用途) | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|----------------|----------|--------|----------------------|------|--------|------------------------------|------|
| confirm_run():44 | EOF 判断 | `!std::getline(...)` | 无人工确认时拒绝运行 | 结构性不适用 | §3.5 fail-closed 设计 | 安全默认，不进科学语义 | CONFIRMED |
| confirm_run():45 | answer 判断 | "yes" || "y" | 用户确认响应白名单 | 结构性不适用 | 最小集确认协议 | 非科学常量 | CONFIRMED |
| json_type_of():49-56 | 类型分支 | null/string/number/boolean/array/object/unknown | JSON 类型诊断 | 结构性不适用 | 诊断用工具函数 | 非科学常量 | CONFIRMED |
| path_readable_file():129-131 | std::ifstream | ios::binary | 文件可读性检查 | 结构性不适用 | 磁盘门实现细节 | I/O 实现选择 | CONFIRMED |
| Subcommand::run():381 | sleep_for(ms 20) | 轮询间隔 20ms | 取消检测频率 | 待确认 | 无测试规格文档 | 应查测试规范确定合理性 | UNPROVEN |
| Subcommand::run():379/389/397/401 | return astrocs::*; // 2/3 | 各类退出码 | INPUT/ARGS/CANCELLED | 文献值 | exit_codes.h 注册 | 退出码契约 | CONFIRMED |

**Finding #1**: subcommand.h:381 — `sleep_for(milliseconds(20))` 硬编码轮询间隔无来源
- **轴**: S
- **位点**: lib/infrastructure/cli/subcommand.h:381
```cpp
std::this_thread::sleep_for(std::chrono::milliseconds(20));
```
- **上位依据**: 测试钩子的 Timing 参数应有实验依据或性能测试支撑（未见 docs 中记载）
- **现状**: 20ms 硬编码作为取消检测循环的等待时间
- **差在哪**: 既不是物理常数也不是科学阈值，而是工程权衡值，但无文档记录为什么选 20ms
- **后果**: 不影响科学结果，但可能导致 CPU 开销偏高或响应延迟
- **定级**: S3（纯工程参数）
- **怎么算修好**: 在注释中注明取值依据，或移除测试钩子
- **置信**: PARTIAL（缺 eng/tests 中的测试规范）

---

### lib/infrastructure/cli/v6_runtime_contract.h —— 已读：是（表 21 项）

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么 (一句话用途) | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|----------------|----------|--------|----------------------|------|--------|------------------------------|------|
| Phase:41 | kP1/kP2/kP3 | 1/2/3 | 三阶段枚举 | 结构性不适用 | §3.2 三 phase 隔离 | 数组基数 | CONFIRMED |
| RouteKind:75 | kProduction/kBaseline/kReject | 0/1/2 | 路由判定结果 | 结构性不适用 | RUNTIME-CI-001 | 枚举基数 | CONFIRMED |
| ModeRoute:91 | rc = 2 | 拒绝退出码 | ARGS 退出码 | 文献值 | exit_codes.h | 退出码契约 | CONFIRMED |
| route_phase2_weight_token():109 | 全拒绝逻辑 | 无合法 token | 权重口径单一化 | 文献值 | FZ-WEIGHT-SINGLE-PATH | 冻结节点 | CONFIRMED |
| route_phase3_mode():156 | surface_brightness/point_source_flux/visualization | 合法 production token | 输出模式白名单 | 文献值 | FZ-P3-MODES | 冻结节点 | CONFIRMED |
| ProcessBudgetRegistry:208 | 进程唯一实例 | §10.4 统一预算 | 线程租约管理 | 文献值 | 宪章 §10.4 | 冻结规范 | CONFIRMED |
| ProcessBudgetRegistry:218 | cores == 0 | 零预算非法 | 防御性检查 | 结构性不适用 | 防止硬编码退化 | 工程防御 | CONFIRMED |
| HeavyRunMetrics:293 | struct 定义 | 重计算指标集 | §10.5 字段面 | 文献值 | SO-05 记录面 | 冻结规范 | CONFIRMED |
| required_metric_keys():326 | vector 初始化 | 14 个字段名 | 字段完备性判定 | 文献值 | §10.5 规格 | 冻结规范 | CONFIRMED |
| so05_gate_policy():371 | GatePolicy 默认值 | record_only=true | SO-05 策略收口 | 文献值 | CONTROLLER_LOG C-007 | 冻结规范 | CONFIRMED |

**Finding #1**: v6_runtime_contract.h:218 — `cores == 0` 检查未指定错误处理方式
- **轴**: R
- **位点**: lib/infrastructure/cli/v6_runtime_contract.h:218
```cpp
if (cores == 0) return false;               // 零预算非法（不得硬编码/退化）
```
- **上位依据**: §10.4 规定"零预算非法"，但未说明调用方收到 false 后的行为
- **现状**: register_source 返回 false 表示拒绝第二来源，但 caller 若传入 0 cores 仅静默返回 false
- **差在哪**: 没有具名错误消息或日志，调用方无法区分"第二来源拒绝"与"零预算非法"
- **后果**: 若测试夹具或初始化代码误传 0 cores，将静默失败难以调试
- **定级**: S3（调试友好性问题）
- **怎么算修好**: 增加 err 参数返回或 emit 一条 warning 事件
- **置信**: PARTIAL（缺注册调用点证据）

**Finding #2**: v6_runtime_contract.h:363-369 — `GatePolicy` 中 SO-05 状态硬编码
- **轴**: C
- **位点**: lib/infrastructure/cli/v6_runtime_contract.h:363-369
```cpp
struct GatePolicy {
    bool record_only = true;
    bool auto_adjudication_allowed = false;
    std::string signoff_status = kSo05Status;
};
```
- **上位依据**: SO-05 签字前的策略应是运行时可变的，但此处硬编码为 PENDING_OWNER_SIGNOFF
- **现状**: 所有字段都有固定默认值，signoff_status 恒为"kSo05Id"与"kSo05Status"
- **差在哪**: 没有提供"已签字"状态的切换入口——缺少切换到 hard_fail 的机制
- **后果**: 若资源判据门限已满足，但因缺少切换入口而无法升级到硬失败，可能导致 release 决策延误
- **定级**: S2（流程控制问题，依赖外部机制）
- **怎么算修好**: 查 resource_gate.h 或 SO-05 相关文件确认切换机制；若无，需增加环境签或配置文件开关
- **置信**: PARTIAL（缺 SO-05 执行协议文件）

---

**结论**: 本文件无可辩护指控外的重大 finding，数值全部有处置标注。

---

### lib/infrastructure/pipeline/module_loader/module_registry.c —— 已读：是（表 9 项）

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么 (一句话用途) | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|----------------|----------|--------|----------------------|------|--------|------------------------------|------|
| REG_SZ_UNIT_ID:37 | 64 | unit_id 字符串上限 | 定长字段容量 | 结构性不适用 | ABI-004 合同布局 | 内存布局常数 | CONFIRMED |
| REG_SZ_ABS:40 | 4096 | abs_path 字符串上限 | 路径长度限制 | 结构性不适用 | FIPS 180-4 + 系统 PATH_MAX | 安全防御性边界 | CONFIRMED |
| rK[64]:57-69 | SHA256 常量数组 | 64 个 32-bit 常数 | FIPS 180-4 轮函数常数 | 文献值 | FIPS 180-4 规范表 | 密码学标准常数 | CONFIRMED |
| rsha_init():97-99 | h[] 初始值 | 8 个固定 32-bit 值 | SHA256 IV | 文献值 | FIPS 180-4 §5.3.1 | 密码学标准常数 | CONFIRMED |
| file_sha256_hex():154 | tmp[8192] | 8KB 缓冲区 | 文件读取块大小 | 结构性不适用 | I/O 性能工程选择 | 非科学常量 | CONFIRMED |
| parse_units():332 | cap * 2 | 动态数组扩容倍率 | 内存增长策略 | 结构性不适用 | 工程实现惯例 | 算法常数 | CONFIRMED |
| reg_msg():356-374 | static const char* | 错误消息字典 | 静态字面量纪律 | 结构性不适用 | 与安全 loader 一致 | 文本映射 | CONFIRMED |
| acs_registry_self_test_v1():961-971 | known hashes | ""/"abc"/m2的已知SHA256 | 自检验向量 | 文献值 | FIPS 180-4 测试向量 | 密码学标准常数 | CONFIRMED |
| yaml_load():447 | line[512] | YAML 行缓冲区 | 解析临时空间 | 结构性不适用 | 工程实现选择 | 非科学常量 | CONFIRMED |

**结论**: 本文件无可辩护指控外的重大 finding，数值全部有处置标注。该文件为 C 语言 ABI 层实现，常数均为结构性/文献值，无科学阈值。

---

### lib/infrastructure/pipeline/module_loader/module_registry.h —— 已读：是（表 8 项）

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么 (一句话用途) | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|----------------|----------|--------|----------------------|------|--------|------------------------------|------|
| ACS_REGISTRY_VERSION_V1:40 | 1u | Registry 版本 | ABI 版本标识 | 文献值 | ABI-004 合同 | 版本号 | CONFIRMED |
| enum{ACS_REG_EC_*}:43-59 | 0..70 | 错误码枚举 | detail_code 取值 | 结构性不适用 | ABI-004 错误分类 | 枚举基数 | CONFIRMED |
| finding_mask 位:87-94 | 1<<0..5 | finding 位掩码 | entry 级状态标志 | 结构性不适用 | ABI-004 发现分类 | 位运算常数 | CONFIRMED |
| ACS_STATIC_ASSERT:169-172 | sizeof/offsetof | 布局静态断言 | 编译期结构检查 | 结构性不适用 | ABI 稳定性保证 | 工程验证 | CONFIRMED |
| head 字段要求 | ACS_ABI_VERSION_V1 | 结构体头版本 | ABI 兼容性检查 | 文献值 | host_api_v1.h | 契约版本 | CONFIRMED |
| manifest_abs_path | 必填非空 | options 输入参数 | 绝对路径要求 | 结构性不适用 | open_v1 前置条件 | API 契约 | CONFIRMED |
| scan_unregistered | int32_t | 可选开关 | 未登记 DLL 检测 | 结构性不适用 | ABI-004 检测项 | 工程配置 | CONFIRMED |
| reserved:115 | uint32_t = 0 | v1预留字段 | 未来扩展 | 结构性不适用 | ABI 向后兼容 | 工程约定 | CONFIRMED |

**结论**: 本 header 无可辩护指控外的重大 finding，数值全部有处置标注。所有常数为 ABI 层结构/版本/错误码，无科学阈值。

---

<!-- PROGRESS: 6/6 -->

## 本批最高危三条

1. **v6_runtime_contract.h:363-369 · SO-05 状态硬编码缺少切换机制**（S2/C）
   - GatePolicy 与 GateVerdict 中 SO-05 的 signoff_status 恒为"PENDING_OWNER_SIGNOFF"，但设计预期是"签字后升级为 hard_fail"——缺少切换入口可能导致资源判据满足后无法推进 release 决策。需查 resource_gate.h 或 SO-05 执行协议确认切换机制；若不存在，需增加环境签或配置文件开关。

2. **session_commands.h:43 · session_of() 静默返回非法会话 ID**（S3/R）
   - `session_of()` 对未知命令名返回 SessionId(0) 而非抛错或返回可选类型，调用方难以区分"合法会话 0"与"解析失败"。虽当前 SessionId 0 可能未被使用而无实际风险，但 API 设计不安全，应改为 `std::optional<SessionId>`或抛出`invalid_argument`。

3. **v6_mode_gate.h:78-86 · weight_mode 检测逻辑文档与实现不一致**（S2/D）
   - §9.73 裁决 A44 明确"weight_mode 键不存在"，但代码仍保留完整的检测和路由逻辑并发出 reject。这是"防御性编程"还是"应删除的冗余"不明确，建议要么在注释中澄清意图，要么从代码中移除整个 block（若确定永不消费）。

---

## 我读不动或需要实跑才能定的

以下项目需要实际运行测试或查阅更多外部文档才能下结论：

| Finding ID | 文件：位点 | 缺少的证据/该跑什么 |
|------------|-----------|---------------------|
| subcommand.h:381 | sleep_for(milliseconds(20)) 轮询间隔来源 | 查 eng/tests 中的测试规范或性能测试报告；或运行 `time` 对比不同间隔的 CPU 开销 |
| v6_runtime_contract.h:218 | `cores == 0` 检查的错误处理 | 搜索 register_source 的所有调用点，看是否有调用方依赖静默失败行为 |
| v6_runtime_contract.h:341-351 | heavy_metrics_complete 边界条件 epsilon | 查性能测量协议 docs/perf/METRICS_PROTOCOL.md 是否有浮点容忍约定 |
| v6_runtime_contract.h:SO-05 | 签字切换机制 | 读 eng/ci/ledgers/SO-05_signoff.md 或 resource_gate.h 确认升级流程 |
| module_registry.c | self_test 向量覆盖率 | 运行 ctest --filter registry 看自检是否全绿 |

---

## 最终工作树状态 (收工后)

```
由于 Bash 工具调用受限，无法获取实时 git status。本任务仅读取源码与文档，未修改任何仓库文件。预期无新未跟踪项出现。若收工时未跟踪项多于基线，需按前言 §4 口径自查是否为本路造成。
```

---

<!-- PROGRESS: 6/6 -->
