# AUD-101 · 批次 D1补三份 —— 派单缺口复算 + 3 份条目（独立只读审查节点）

批次：`D1补三份`　清单：派单确定式（DA-all 360 ∖ 各批登记 ∖ 10 份 `工程控制/RELEASE-05/tasks/*.md`）　判据：标准 01 §1–§5 + AUD-101 任务书 + DISPATCH-DOC-PREAMBLE §4/§5　冻结基线 HEAD=`c8f64e9a`　本节点只读：未构建、未跑 ctest/`run_checks.py`/任何 `eng/**` 脚本、未跑端到端、未 `import` 仓库 Python（只读其源码文本与 JSON 数据）。

## 0 开工基线（`git status --porcelain` 原文）

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与派单规定的基线一致（恰两个未跟踪项）。全程未修改被审对象。

## 0.1 派单—清单差异（必须先登记的实质差异）

**派单前提不成立：按派单给出的确定式复算，缺口为 0 份，不是 3 份。** 派单点名的三份**均已有 14 字段条目**，且条目形状经机器复核（≥12/13 字段栏齐备）：

| 派单点名的"从未登记"项 | 实际已登记处 | 条目字段数（我复算） |
|---|---|---|
| `docs/standards/TEST_STANDARD.md` | `独立审计/证据/AUD-101-DB-10-补.md` §2.14（第 365 行起） | 12/13 |
| `docs/algorithms/anchors/check_doc_line_anchors.py` | `独立审计/证据/AUD-101-DA02-算法推导.md` §1.9（第 351 行起） | 13/13 |
| `docs/standards/checks/check_standards_registry.py` | `独立审计/证据/AUD-101-DB-10.md` 第 88 行条目 + `AUD-101-DB-10-补.md` §2.15（第 382 行起） | 13/13、12/13 |

时间线证据（同一目录 `stat` 复算）：`AUD-101-DA02-算法推导.md` 16:05 → `AUD-101-D1残余.md` 16:10 → `AUD-101-DB-10-补.md` **16:20** → `inventory/D1-final-missing.txt` 与 `inventory/DB-10-missing.txt` **16:23（皆 0 字节）**。即派单所依据的缺口名册在装配时点成立，而 `DB-10-补`（16:20）落下 §2.14/§2.15 后缺口即被补齐，16:23 的装配复算已归零；派单文本沿用的是更早一轮的名册。`AUD-101-D1残余.md:23` 与 `:425` 亦自记"TEST_STANDARD.md 按派单跳过（另一路在做）"——那"另一路"就是 16:20 的 `DB-10-补`。

**因此本批按派单 §"三份都要通读并出条目"继续执行**，条目性质为**独立复算与复核**（不重复占位、不作为"首批登记"），并逐条登记与前批结论的出入（见各份"与上位冲突"与前批订正栏与 §5）。复算命令与结果见 §6。

---

## 1 台账条目（14 字段，顺序与列名按前言 §4）

### 1.1 `docs/standards/TEST_STANDARD.md`

| 字段 | 判定 |
|---|---|
| 路径 | `docs/standards/TEST_STANDARD.md` |
| 标题 | `# Astro Celestial Sphere Database（ACSD） Test Standard`（:1，实测 H1 原文；机械列 `h1` 与此一致） |
| 行数 | **10**（`wc -l docs/standards/TEST_STANDARD.md` = 10；bytes=566）⇒ **前批订正**：`AUD-101-DB-11.md:431` 写作"同主题 `docs/standards/TEST_STANDARD.md` 42 行"，实测 10 行，该数错；它同时是该份"合并 → 并入 TEST_STANDARD.md"处置的规模依据 |
| 角色 | **标准与检查**；实际所在 `docs/standards/` = 应属目录 ⇒ **目录与角色一致，不判错放**（标准 01 §2 角色表；AGENTS.md §7 落位表把"标准"归 `docs/`，未把 `docs/standards/` 判为错位）。内容档级：它给的是普适判据（下限），不是可执行入口，故与 `docs/development/TESTING.md`（入口清单）构成"判据/操作"分层而非同类 |
| 主题 | 科学契约↔测试/oracle 下限；测试只用公共生产 API；确定性与容差来源；gtest 命名与 SKIP；覆盖率分母；变更重跑与 artifact 复用 |
| 上游 | 有单行：`:3` `> 上游：ASTROCS_DESIGN.md §8.4（模块与 ABI）` ⇒ 满足标准 01 §3 的"标明上游"形式要求，但**号与题互不相配**：实测 `ASTROCS_DESIGN.md:618` = `### 8.4 顶层结构`，而"模块与 ABI"是 `:661` `### 8.5 模块与 ABI`；测试主题的实质上位是 `:721` `## 12. 验证体系` 与 `:764` `### 12.4 验证层级与四层验收`（另 `ENGINEERING_SPEC.md:64` `## 5. 测试规范`）。全文对 §12/§5 **无任何指针** |
| 下游 | 精确路径引用（`git -c core.quotePath=false grep -n -F "docs/standards/TEST_STANDARD.md" -- . ':!artifacts' ':!reports' ':!工程控制'`）共 **4 处**：`docs/DOCUMENT_INDEX.yaml:1439`（索引登记）、`docs/audit/doc_classification.csv:95`（`AUTHORITATIVE,present`）、`lib/infrastructure/aio/tests/test_checksum.cpp:7`（`// - docs/standards/TEST_STANDARD.md (测试要求: 零软通过)`）、`lib/infrastructure/aio/tests/test_transform.cpp:6`。**换词陷阱已核**：用 basename `TEST_STANDARD.md` 检索得 39 个跟踪文件，其中 34 份命中的是**另一件**`11_MODULE_SOURCE_TEST_STANDARD.md`（编号控制包标准，`git ls-files \| grep -c 11_MODULE_SOURCE_TEST_STANDARD` = **0** ⇒ 该权威面已不在跟踪集），故 basename 口径会虚高下游。证据面（非活引用）另有 `artifacts/evidence/v19r7-quality/*`、`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:174`、`reports/v19r2/*`（跟踪集内 `reports/` 计 3 文件） |
| 是否正本 | **否**（就"测试判据"主题而言）：`ENGINEERING_SPEC.md` §5.1/§5.2/§5.3（:64 起）是本仓测试规范的正本层，契约↔测试映射正本是 `docs/contracts/TEST_MATRIX.md`；本件 :5 正是该表规则的口头复述而不指向它。**本件的独有面**只有"覆盖率分母各自计数"（:9）与"变更重跑/artifact 复用"（:10）两条，其中 :10 已失效（见"写法违规"） |
| 重复或重叠对象 | 三处举证（各带 `路径:行`，两处段落并列）：<br>① 本件 `:5` `- 每科学契约 ≥1 test/oracle（property/oracle/MC）。` ↔ `docs/contracts/TEST_MATRIX.md:6` `> 规则: 每个 SCI/ALG ID 至少一个独立 TEST; 容差事前冻结; 影响触发按 07 矩阵。` ＋ ↔ `ENGINEERING_SPEC.md:69` `- 不调用生产实现的独立 Oracle 或解析解；`。重叠范围 = "什么算被测、是否必须有独立 Oracle"；差异 = 本件给数量下限（≥1）与类型枚举，ENGINEERING_SPEC 给"必备测试清单"且要求"不调用生产实现"，TEST_MATRIX 给 ID↔TEST 映射表。**三处无互指**。<br>② 本件 `:6` `- 测试必须使用公共生产 API；实现面只有生产一份。` ↔ `docs/standards/CODE_STANDARD.md:21` `- production science implementation 单一（单一实现 + oracle）。`。重叠 = "实现面单一"；差异 = 本件把该断言用作测试面约束，CODE_STANDARD 是代码红线。<br>③ 本件 `:3` 上游串 ↔ `docs/development/TESTING.md:3` `> 上游：ASTROCS_DESIGN.md §8（软件架构）`。同一"测试"主题两件**各标一个互不相干的上位号**（§8.4 vs §8，均非 §12）⇒ 双向索引在此不闭合（标准 01 §3）。<br>与 `AUD-101-DB-10-补.md` §2.14 的登记方向一致（其举证为 :5↔ENGINEERING_SPEC:66-69、:6↔CODE_STANDARD:21），本条为独立复核复现，非沿用 |
| 元信息块 | **无** frontmatter/状态块（机械列 `meta_block=False` ✓）；`:3` 是标准 01 §3 **要求**的上游单行，不计为违规元信息块 |
| 写法违规 | 逐条（行号 + 原文）：<br>①`:10` `未变更模块引用 **V19** artifact 需 hash 证明` ⇒ 轮次/交付世代代号入规范正文。该规程**无现行生产者**：`eng/tools/quality/build_v19r3_package.py:92-93` 与 `build_v19r4_package.py:91-92` 定义 `RETIRED_NOTICE = ("BUILD_V19R*_PACKAGE_RETIRED: …已退役；任意调用 exit 2（fail-closed，不伪装绿）")`；**无消费者**：`grep -rn "V19 artifact" docs/ ENGINEERING_SPEC.md` 全文只命中本行；**无门**：`eng/ci/checks.json` 内 `grep -c -i v19` = 2，两处均为 `artifacts/evidence/v19r7-quality/**` 的 `changed_paths` glob（第 1932、1968 行），是影响分析面残留而非判据。⇒ 该分句是按**已退役交付世代**写的现行义务，读者无法据此执行。<br>②`:8` `gtest 命名：Suite.Feature；GPU 不可用时 GTEST_SKIP 而非伪通过。` ⇒ 未标适用域（本仓生产面无 GPU 档，见"与上位冲突"①），且"相关 gate"（:10）不给清单指针（未指 `docs/ci/01_CHECKS.md` 或 `eng/ci/checks.json`），属条款级不可核。<br>**机械列校准**：`doc_mechanical.csv` 对本行 `task_ids=[]`、`dates=[]` —— `task_ids` **漏报了 :10 的轮次代号 V19**（其词表按 `XXX-<数字>` 形态取，`V19` 无连字符故不命中）；`dates=[]` 判对（全文无日期）。本件无 commit sha、无 sha256（机械列 `commit_sha=[]`、`sha256=False` ✓） |
| 悬空引用 | 无**带路径**的悬空引用：全文 10 行不含任何 `xxx/yyy` 形态文件引用（机械列 `dangling_links=[]` ✓）。两处**非路径**指向失效：①`:10` 的 `V19 artifact`（生产者 exit 2 已退役，实测见"写法违规"①）；②`:10` 的"相关 gate"无落点（`docs/ci/01_CHECKS.md`、`eng/ci/checks.json` 均未被指向）。另 `:3` 的 `§8.4（模块与 ABI）` 属**指向存在文档的错号锚**（§8.4 实为"顶层结构"），按标准 01 §3 判锚错、不判悬空 |
| 与上位冲突 | ①**上游号题双错**：`:3` 标 `§8.4（模块与 ABI）`，实测 `ASTROCS_DESIGN.md:618` = `### 8.4 顶层结构`、`:661` = `### 8.5 模块与 ABI`；测试主题的上位在 `:721 ## 12. 验证体系` / `:764 ### 12.4 验证层级与四层验收`，本件一律不指。<br>②**GPU 分句与上位适用域不对齐**：`ASTROCS_DESIGN.md:671`（§9 CPU 后端与资源）`- 生产仅纯 CPU；支持 amd64 baseline、AVX2/FMA、AVX-512 子集…`，`:96 ### 1.3 非目标（明确不做）` 之 `:98` `- ACR/GPU/CPU+GPU 混合生产路由（ACR 源码保留为隔离实验，生产不可达）`；本件 `:8` 把 GPU 当常规测试面而不写"仅隔离实验面"，读者会推定存在生产 GPU 路径。<br>③**与门禁注册表的映射为空（本批专项，见 §1.1a）**：6 条判据在 `eng/ci/checks.json` 里**一条登记边都没有**。<br>④同主题两件上位互斥（见"重复或重叠对象"③），且前批对同一对文件给出**相反合并方向**（见"处置建议"栏与前批订正） |
| 处置建议 | **合并** → 目标 `ENGINEERING_SPEC.md` §5（把 :5/:7/:8/:9 四条并入 §5.1/§5.2 之下，并在开头声明"契约↔测试映射正本 = `docs/contracts/TEST_MATRIX.md`"）；`docs/standards/` 下保留一行下钻指针即可。理由：标准 01 §1（层级向下、口径唯一——同一"必须有独立 Oracle"下限现有三处）＋ §4（禁轮次代号——:10 的 V19 必须删除或改写为现行等价物）＋ §3（双向索引——上游号题双错）。**必改项**：`:3` 上游改 §12（并连带核对同目录另 13 份的 §8.4 串，见 §3）；`:8` 标"仅 ACR 隔离实验面，生产无 GPU 档"；`:10` 删 V19 分句、"相关 gate"改指 `docs/ci/01_CHECKS.md`。**与 §3 目录是否该含 `.py` 的判定无关**（本件是 `.md`） |

#### 1.1a `TEST_STANDARD.md` ↔ `eng/ci/checks.json` 映射闭合性（本批专项，只读推导、未执行任何门）

确定命令与读数（全部在本节点实测）：

| 检索 | 命令 | 读数 |
|---|---|---|
| 注册表是否按名读任何标准文档 | `grep -c "_STANDARD" eng/ci/checks.json` | **0** |
| 注册表内 `docs/standards` 形态串 | `grep -o -E "docs/standards[A-Za-z0-9_./-]*" eng/ci/checks.json \| sort \| uniq -c` | `4 docs/standards/`（STD-REG 的 `changed_paths` glob）＋ `3 docs/standards/checks/check_standards_registry.py`（STD-REG 三步 command） |
| 有无门指向本件 | `python -B gate_map.py`（解析 `checks.json`，441 个含 `id` 节点 / **436 个唯一 ID**；按门聚合其 JSON 文本里的 `docs/**` 指针） | 引用 `TEST_STANDARD.md` 的门 = **0** |

逐条判据↔注册表对表（"登记边"= 该门在 `checks.json` 的文本里出现本件路径；"实质边"= 存在实现同一约束的门，靠人工比对 command/outputs 判定）：

| 本件条款 | 实质对应门（实测 ID） | 登记边 | 判定 |
|---|---|---|---|
| `:5` 每科学契约 ≥1 test/oracle | `CHK-ORACLE`（checks.json:1076 区，outputs 含 `run/v6/runtime-closure/oracle.json`）、`CHK-CONTRACT-TEST`（:1076）、`CON-TEST-CONTRACTS`（:1142） | 无 | **超前**：判据实质有门，但"下限表"归属未登记、且正本 `docs/contracts/TEST_MATRIX.md` 才是 `CHK-CONTRACT-REF` 指向的对象（`docs=['docs/contracts/']`）⇒ 同一约束的门挂在另一件上 |
| `:6` 只用公共生产 API | `PROD-REACH-SELFTEST`（:348，属 `CHK-PROD-EXEC-CALLPATHS` 子步） | 无 | **超前**（门存在，边缺失） |
| `:7` 固定 seed / 容差 + 说明来源 | 无同名门；`CHK-COVERAGE`/`CHK-ORACLE` 不判 seed | 无 | **超前且无实质对应**：确定性下限目前只落在各测试自身与 `docs/contracts/TEST_MATRIX.md` 的"容差事前冻结"条 |
| `:8` gtest 命名 `Suite.Feature` | `CHK-NAMING-SURFACE`（docs 指针是 `docs/ci/01_CHECKS.md`，判 CLI 命名面，不判 gtest Suite.Feature）；`CTEST-REGISTRATION`（:505）判"是否注册进 ctest"，不判命名形态 | 无 | **超前，无对应门**：Suite.Feature 形态目前无人复核 |
| `:8` GPU 不可用 GTEST_SKIP | 无门（生产无 GPU 档） | 无 | **超前**；`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:174`（M2b-G-03，P1，末列状态 **OPEN**）已点名本行，处置建议正是"GTEST_SKIP 口径补『不得计入 §17 验证达成』限定"⇒ 本条不新立缺陷，**登记为已被 OPEN 工单覆盖** |
| `:9` 覆盖率分母明确 | `CHK-COVERAGE`（profiles 仅 `linux-deep`/`prerelease`，requires_monitor=true）；分母口径落在 `docs/quality/coverage_baseline_v1.md` | 无 | **超前**：门存在、边缺失，且"shipping 单元与测试单元各自计数"的口径在另一件里 |
| `:10` 变更重跑相关 gate | `changed_paths` 机制本身（每门都有） | 无 | 机制在文档侧无指针 ⇒ **超前** |
| `:10` V19 artifact hash | 无门；生产者 `exit 2` 退役 | 无 | **黑户的反面**：规程无载体、无门、无消费者 ⇒ 该条应删（见处置） |

反方向（"注册表有项、文档没写"= 黑户）本批不整体立案（属 AUD-501 `独立审计/证据/AUD-501-门禁现状审计.md` 的范围），但给出可复算的规模线索：上述 436 个唯一 ID 节点里，**390 个的 JSON 文本不含任何 `docs/**` 指针**（即门自身不声明它对应哪份文档），只有 46 个带文档锚。⇒ 映射轴在注册表侧同样是空的，本件的"超前"不是孤例而是整体缺边。**只读不执行**：本批未跑 `run_checks.py`、未跑 STD-REG、未跑任何门（前言 §1 禁令）。

### 1.2 `docs/algorithms/anchors/check_doc_line_anchors.py`

| 字段 | 判定 |
|---|---|
| 路径 | `docs/algorithms/anchors/check_doc_line_anchors.py`（跟踪集内 `docs/**/*.py` 共 **2** 份：本件与 `docs/standards/checks/check_standards_registry.py`；命令 `git -c core.quotePath=false ls-files -- 'docs/*.py'`） |
| 标题 | **无 H1**：`:1` shebang、`:2` 编码声明、`:3` 起为 module docstring 首行 `SCI-ANCHOR-001｜文档源码行号锚全量复测器（exit 0 = PASS）。`。**机械列订正**：`doc_mechanical.csv` 记 `h1 = "-*- coding: utf-8 -*-"` 是**把 :2 的编码声明当标题**（误报），本批按"无 H1 + docstring 首行为实质标题"登记 |
| 行数 | 483（`wc -l` = 483，bytes=22727；与机械列 `lines=483` 一致） |
| 角色 | **标准与检查（可执行门实现）**——实测其身份由注册表钉死：`eng/ci/checks.json:1013` `"id": "DOC-LINE-ANCHORS"`、`:1022` `command = ["python3", "docs/algorithms/anchors/check_doc_line_anchors.py", "--json-out", "run/ci/doc-anchors/doc_line_anchors.json"]`，profiles = `fast / linux-main / windows-main`（即三档主流程都执行它）。⇒ **实所在 `docs/algorithms/`（算法推导角色目录）与应属目录不一致 = 角色错放**，应迁往 `eng/ci/`（门注册表与检查器）或 `eng/tools/doccheck/`（同类文档检查器已有同族先例，见"重复或重叠对象"②）；判据：AGENTS.md §7 落位表 + 标准 01 §2 角色表（九角色无"脚本"） |
| 主题 | 文档源码行号锚全量复测：C1 docs_tracked / C2 anchor_resolved / C3 range_in_bounds / C4 symbol_binding / C5 exemptions_live / C6 boundary_blank / C7 contract_scale / C8 unresolved_registry（棘轮只减不增）；输出稳定排序 JSON、`--print-scale`/`--print-registry`/`--self-test` |
| 上游 | **未标最高设计条款**（docstring 全文不含 `ASTROCS_DESIGN.md` 节号）：`:7` `规则（详见 docs/algorithms/anchors/ANCHOR_CONTRACT.md）：` 只指同目录合同件 ⇒ 按标准 01 §3 判 `缺`。**索引侧另有一套**：`docs/DOCUMENT_INDEX.yaml` 给该件登记 `upstream: "ASTROCS_DESIGN.md §4（normalize）、§5（mosaic）、§6（export）"`——三个命令节，与本检查器（锚机制的实现）无落实关系，且与 `ANCHOR_CONTRACT.md` 自标的上游不同 ⇒ 同一机制两套上游标注（机械列 `upstream_pointer=False` **判对**：文档级"上游："行确实不存在） |
| 下游 | `git -c core.quotePath=false grep -l -F "check_doc_line_anchors.py" -- . ':!reports'` = 9 个文件，除自身外 **8 处**：`eng/ci/checks.json`（command）、`eng/tests/quality/test_doc_line_anchors.py`（:4 自述"（docs/algorithms/anchors/check_doc_line_anchors.py）的机器验收测试"）、`eng/tools/doccheck/check_alg_line_anchors.py`（同族兄弟门）、`docs/algorithms/anchors/ANCHOR_CONTRACT.md`（:4 `> 机器检查器: …（CI 检查项 DOC-LINE-ANCHORS）`）、`docs/DOCUMENT_INDEX.yaml`、`eng/cmake/ARCH-001-migration-manifest.md`、证据面 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md`。⇒ **它不是"被生成器读的对象"，而是被执行、被测试、被另一门引用的生产件**（判据见"处置建议"） |
| 是否正本 | **是**：DOC-LINE-ANCHORS 判据的唯一实装件（同目录无第二实现；`docs/algorithms/anchors/anchor_contract.json`、`unresolved_registry.json` 是其数据面而非判据面） |
| 重复或重叠对象 | ①**与同目录合同件把 C1–C8 写两遍**（同一机制的判据双承载）：本件 `:9-13` `- C1 docs_tracked      作用域文档存在且被 Git 跟踪` / `:11-12` `C2 anchor_resolved   每个锚解析到唯一真实目标文件（exact → doc-relative → contract.resolvers → unique basename）` ↔ `docs/algorithms/anchors/ANCHOR_CONTRACT.md:72-73` `| C1 docs_tracked | 作用域文档存在且被 Git 跟踪 | 文档缺失/未入库 |` / `| C2 anchor_resolved | 每个锚经 §3 唯一解析到真实跟踪文件，或已登记于 unresolved_registry | 目标改名/删除，或同名分歧未登记 resolver，或新出现的未解析锚未登记 |`。重叠范围 = C1/C2 判据语义与 FAIL 形态；差异 = 本件另带 `:21` 的 `C6 boundary_blank` 逐字口径（`**不用 strip/rstrip**：纯空白行不是空行，按 rstrip 判空会引入误报`），合同件表体不含该口径 ⇒ 冻结串有两处、必须手工同步步。<br>②与 `eng/tools/doccheck/check_alg_line_anchors.py`（兄弟门 `ALG-LINE-ANCHORS`）**不重复**：该件在 `eng/tools/doccheck/`（正确落位），二者分工由其 docstring 单方面记述；`ANCHOR_CONTRACT.md` 未把 `ALG-LINE-ANCHORS` 列为合同一部分（本批按 `grep -l` 只证其引用本件，未逐字复算该分工句 ⇒ 此项沿用 `AUD-101-DA02-算法推导.md` §1.9 的结论，标记为**待证**：缺"ALG 兄弟门 docstring 原文 + 合同件是否登记该门"两处段落）。<br>③与 `docs/algorithms/anchors/ANCHOR_CONTRACT.md:28` `现行规模（C7 逐字复测）：**101 文档 / 1821 锚** = 1786 目标锚 + 9 登记豁免 + 26 未解析登记。` 构成**由本件实测决定的文档侧数字行**（C7 判其逐字相等）⇒ 规模数字的正确承载是本件（机器），合同件那一行是复述面，本身不算重复缺陷，但意味着锚集变动必须同轮改文档 |
| 元信息块 | 无 YAML/frontmatter（机械列 `meta_block=False` ✓）；docstring `:3` 以任务编号 `SCI-ANCHOR-001｜` 开头 = **编号块**（标准 01 §4 禁"文档 ID"进正文开头） |
| 写法违规 | ①`:3` `"""SCI-ANCHOR-001｜…`；②`:21` `锚没指向任何内容（DOC-DRIFT-FIX-01 新增）。`——"新增"= 历史叙事；③`:90` `按 rstrip 判空会引入误报（DOC-DRIFT-FIX-01 实测 docs/** 为 0 例，但口径必须写死）。`——"实测…为 0 例"= 过程实验数据入判据注释；④`:211` `"task": "SCI-ANCHOR-001 / DOC-DRIFT-FIX-01",`——**任务编号写进机器输出 JSON 字段**（不只是注释），门产物里带轮次代号，消费方按该字段对账即被历史污染。共 4 处、2 个编号族（SCI-ANCHOR-001 ×2、DOC-DRIFT-FIX-01 ×2）。**机械列校准**：`task_ids=['FIX-01']` **漏报了 SCI-ANCHOR-001（2 处）**（只取到 DOC-DRIFT-FIX-01 的尾段 `FIX-01`）；`dates=[]` 判对（`grep -nE "20[0-9]{2}-[0-9]{2}-[0-9]{2}"` 本件 **0 命中**）；`commit_sha=[]`、`sha256=False`、`dangling_links=[]`、`history_narrative=[]` 与本件实测一致 |
| 悬空引用 | docstring 内引用的三个对象全部实测存在：`docs/algorithms/anchors/ANCHOR_CONTRACT.md`、`docs/algorithms/anchors/unresolved_registry.json`（`:6` 引）、`anchor_contract.json`（`:5` 引其 `doc_globs`）——`git -c core.quotePath=false ls-files -- docs/algorithms/anchors` 四件齐（含本件）。⇒ **本件无路径悬空**（机械列 `dangling_links=[]` ✓）。**目录卫生实测**：`AUD-101-DA02-算法推导.md:20` 记该目录存在 `__pycache__/check_doc_line_anchors.cpython-310.pyc`（`.gitignore:83 __pycache__/` 命中、未跟踪）⇒ 按"审查只限跟踪内容"口径**不立案为仓库缺陷**，但它证明该 `.py` 确在文档树内被执行过，与 `docs/**` 的角色定位冲突（作为 §3 判定证据，不当作独立缺陷） |
| 与上位冲突 | ①上游缺标（见"上游"），索引却给它登记了 §4/§5/§6 三个命令节 ⇒ 登记面失真（标准 01 §3"登记含上游"）；②索引 `status: ACTIVE_INFORMATIVE` 与该件实为 `fast/linux-main/windows-main` 三档**判定门**的身份不符（"INFORMATIVE" 与 "CI 判据实现"两档串用了状态轴，前言 §5 第 6 条），正确档应是 normative-machine；③C6/C7/C8 把"口径写死"的实现约束放进 `docs/algorithms/`（算法推导层），按 AGENTS.md §7 该层承载科学算法，机器门判据的正本在 `eng/ci/checks.json` + `docs/ci/01_CHECKS.md` 轴上 ⇒ 层级倒置。**未证伪** `AUD-101-DA02` §1.9 的结论（其四条判定本批全部复现） |
| 处置建议 | **迁移** → `eng/tools/doccheck/check_doc_line_anchors.py`（理由：AGENTS.md §7 把"机器门注册表与检查器"归 `eng/ci/`、"工具与质量检查器"归 `eng/tools/`；标准 01 §2 角色表九角色不含脚本；同族 `check_alg_line_anchors.py` 已在 `eng/tools/doccheck/` 成立落位先例）。**迁移 blast radius（必须同批改，否则判红）**：`eng/ci/checks.json:1022` 的 command 字面量；`eng/tests/quality/test_doc_line_anchors.py:4` 与其实调；`eng/tools/doccheck/check_alg_line_anchors.py` 的互指；`docs/algorithms/anchors/ANCHOR_CONTRACT.md:4`；`docs/DOCUMENT_INDEX.yaml` 的条目（迁出 `docs/**` 后索引不应再登记它，见 §3）；`artifacts/evidence/release-05/AUDIT_PACKAGE.md`（历史证据，可不改）。**数据件 `anchor_contract.json` / `unresolved_registry.json` 留在 `docs/algorithms/anchors/`**——它们是文档集的机器合同数据（被 `DOC-LINE-ANCHORS` 读、被索引登记），与本件的"可执行"属性不同类，不随迁。**属门禁改造**：迁移需改 `eng/ci/checks.json`，按 AGENTS.md §10/§6 应由前台与负责人批准；不迁移则至少清 `:211` 输出字段里的任务编号、并把索引 status 档改对 |

### 1.3 `docs/standards/checks/check_standards_registry.py`

| 字段 | 判定 |
|---|---|
| 路径 | `docs/standards/checks/check_standards_registry.py`（`docs/standards/` 下 15 个跟踪件里唯一的非 `.md`；命令 `git -c core.quotePath=false ls-files -- docs/standards`） |
| 标题 | **无 H1**：`:1` shebang、`:2` 编码声明、`:3` docstring 首行 `STD-REG-001 机器检查器：国际标准冻结注册表（docs/standards/STANDARDS_REGISTRY.md）。`。**机械列订正**：`h1 = "-*- coding: utf-8 -*-"` 同上属**误报**（取到编码行） |
| 行数 | 702（`wc -l` = 702，bytes=35878；与机械列一致） |
| 角色 | **标准与检查（可执行门实现）**——身份实测：`eng/ci/checks.json:8982 / :9005 / :9029` 三处 command 字面量 `python3 docs/standards/checks/check_standards_registry.py --root .`（STD-REG 主步 + 两个故障注入步），其 `changed_paths` = `docs/standards/**`、`eng/contracts/**`。⇒ 实所在 `docs/standards/checks/` 与应属目录不一致 = **角色错放**，应迁往 `eng/tools/doccheck/` 或 `eng/ci/`（AGENTS.md §7；标准 01 §2） |
| 主题 | STD-REG 门判据实现：C1 注册表在册且 ACTIVE_NORMATIVE / C2 六域齐备且与 §2 域表一致 / C3 字段面与 VERSION 逐字冻结 / C4 符合性清单与 EVIDENCE 指针实存 / C5 偏差表与 §3.2 跨域治理表 / C6 偏差 ID 全局闭包（含可选外部登记册）/ C7 §3 索引行与定义域逐 ID 一致 / C8 索引与登记面双向一致（显式"（无）"）/ C9 清单偏差列↔DEVIATION 字段反向一致；另 8+1 个 `--fault-inject` 场景、锚存活 `ANCHOR_STALE` exit 2、`registry_sha256` 证据 |
| 上游 | 文档级"上游："行**不存在**（机械列 `upstream_pointer=False` **判对**；`has_upstream_header` 类检查不作用 `.py`）。docstring `:5` 只指 `docs/standards/STANDARDS_REGISTRY.md §1/§2`，`:31-33` 段指 `ENGINEERING_SPEC §8` ⇒ 按标准 01 §3 判 `缺`。**索引却登记了** `upstream: "ASTROCS_DESIGN.md §8.4（模块与 ABI）"`（`docs/DOCUMENT_INDEX.yaml`）——号与题都与本检查器无关，且该号本身配错（§8.4 实为"顶层结构"，见 §1.1"上游"），属"给代码件登记该件不可能有的元信息" |
| 下游 | `git -c core.quotePath=false grep -l -F "check_standards_registry.py" -- . ':!reports'` = 9 个，除自身外 **8 处**：`eng/ci/checks.json`（3 处 command）、`eng/ci/polarity_evidence.json`、`eng/ci/polarity_probe.py`（极性与门台账侧）、`docs/standards/STANDARDS_REGISTRY.md`（:5、:317、:334、:355、:369 五处把路径字面量当复现命令与"机器检查"声明）、`docs/owner/RELEASE_STATUS.md`、`docs/DOCUMENT_INDEX.yaml`；证据面 3 处（`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv`、`audit-2026-01/ROOT_CAUSES.md`、`governance-01/research/R-6_门禁正当性总审裁决登记面.md`）。⇒ 同 §1.2：被执行、被文档写死路径的**生产件** |
| 是否正本 | **是**：STD-REG 判据唯一实装件，同时是 `STANDARDS_REGISTRY.md` §5"机器检查合同"的另一端 |
| 重复或重叠对象 | 与 `docs/standards/STANDARDS_REGISTRY.md` 互为"文档口径↔门禁"两端，并把**冻结串二次承载**：本件 `:3-24` docstring 逐条复写 C1–C9 判据（如 `:11` `      且 VERSION 与 §2 域表冻结值逐字相等（版本不得按实现反推）；`）↔ 注册表 §5（`STANDARDS_REGISTRY.md:317` `## 5. 机器检查合同（docs/standards/checks/check_standards_registry.py）`）以路径字面量声明同一契约（本批实测五处 `:5/:317/:334/:355/:369`）；另 `:126-127` `"| STD-REG-001（本注册表建立即闭环；依据 = 原 05 号 findings 登记册 §STD-F6「国际标准冻结注册表缺失」处置面，" / `"2026-09-16 由 git 历史逐字取证迁移） |")` 是注册表 §3.2 行的**第二处承载**（注入锚） ⇒ 注册表现文一改，注入锚即漂移（该失效形态见"与上位冲突"①，与前批结论一致）。**与 `docs/standards/` 目录另 13 份 `*_STANDARD.md` 无重复**（它们不被本件读取：`grep -c "_STANDARD" eng/ci/checks.json` = 0，实测见 §1.1a） |
| 元信息块 | 代码件不适用（无 frontmatter）；docstring `:3` 以任务编号 `STD-REG-001` 开头 = 编号块 |
| 写法违规 | 注释与常量字面量逐条命中同目录 `COMMENT_STANDARD.md` 自定的清理面：`:68-69` `# 可选外部来源：原 05 号 findings 登记册随旧控制包世代清理（ROOT-007/ARCH-001/` / `RETIRE-001）删除；其…职能已按 CI-003 迁入本注册表 §3.2` —— **旧版/删除/"原 XX 世代"历史叙事 + 3 个工作项编号**；`:111` `# 2026-09-21 根目录整合：tests/tools/ci/contracts → eng/** 后，原式会在 "eng/tests/x" 里` —— **ISO 日期 + "原式"历史叙事**；`:112` `# 从 "tests/" 起截出一段假路径（前置无边界）⇒ 真实存在的证据被判"不存在"。` —— 缺陷过程叙事；`:126-127` `"2026-09-16 由 git 历史逐字取证迁移） |"` —— **ISO 日期**，且该串是注入锚的一部分（改文即漂移）；`:24` `C9  **[W4-A3] 域清单…` 、`:43` `注入空转守卫（W4-A3）：…` 、`:522` `# C9 …（W4-A3；§8 注册表双向一致）` 、`:573` `W4-A3 事由：…的原锚串与注册表正文漂移…` —— 工作项编号 4 处；`STD-REG-001` 计 3 处（`grep -c`）。⇒ **机械列校准（三处）**：`task_ids=[]` **漏报** STD-REG-001 与 W4-A3（编号无 `字母-数字` 单一形态，`W4-A3`/`STD-REG-001` 被其词表放过）；`dates=[97,111,127]` 中 **:111/:127 判对**（真 ISO 日期），**:97 误报**——该行原文是 `     "FITS 4.0（IAU FWG，2016-07-22 批准版）")`，属外部标准的批准日期（版本标识），不是本文档日期；`sha256=True` **误报**——本件含 sha256 只有 `:669` `sha = hashlib.sha256(open(reg_full, "rb").read()).hexdigest()` 与 `:676` `"registry_sha256": sha,` 两处**代码计算**，文档串里并不存在被抄录的哈希值（把"调用"当"登记了哈希"即高估）；`history_narrative=[]` **漏报** `:68-70` 的"原…删除"与 `:111` 的"原式" |
| 悬空引用 | 代码常量 `EXTERNAL_FINDINGS_REL`（`:72-73` 字面量拆两段拼成 `"工程控制/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909/" + "05_FINDINGS_REGISTER_20260911.md"`）指向**已不存在**的路径：实测 `git -c core.quotePath=false ls-files -- 工程控制 \| cut -d/ -f2 \| sort -u` = **仅 `RELEASE-05`**（跟踪集只有这一族目录）⇒ 按"跟踪集 + 删除历史"口径判不存在（磁盘上的 `工程控制/evidence` 属未跟踪本机残留，不算入库）。**注意路径由两个字面量拼接**——整串检索取不到（前言 §5.2 本仓已知陷阱），本批按行内容读。**该悬空是已被显式处置的**：`:67-71` 注释 + `EXTERNAL_FINDINGS_REQUIRED = False` 走"缺席(可选)"分支并登记 `C6_external_findings_source`，不判 `ANCHOR_STALE` ⇒ 代码侧合规；缺陷在**文档侧**——`STANDARDS_REGISTRY.md` 仍把 05 号登记册当 STD-F* 的 ID 来源叙述（前批已立案，本批复现其不存在性）。另 `dangling_links=[]`（机械列）与本判定**方向一致但口径不同**：机械列只看 markdown 链接语法，`.py` 里的代码常量不在其扫描面 ⇒ 记"机械列对本类对象的悬空面**结构性无感**" |
| 与上位冲突 | ①**注入锚与注册表现文失配 ⇒ 场景失效（沿用前批推导，本批未执行任何门，标记为待前台复验）**：`:126-127` 的逐字串（含"已闭环（治理级/P1）"与"05_FINDINGS_REGISTER_20260911.md（已删除，仅历史溯源）"）与 `STANDARDS_REGISTRY.md` §3.2 现文不同形，若 `inject()` 经 `_replace_once` 命中 0 次则抛 `InjectedNoOp` → exit 3，而注册表 §5 声明"前 8 场景恒退出 0（判定看 verdict）"⇒ 文档口径与实际行为可能相反；该场景在 `checks.json` 未注册（STD-REG 只有主步 + `FI-DANGLING` + `FI-VERSION-DRIFT` 三步，实测 3 处 command），故门不会抓到它。**本批缺什么**：需逐字比对 `GOVERNANCE_ROW_STD_F6` 常量全文与 `STANDARDS_REGISTRY.md:298` 现文（我在该份取证上超 10 次调用，按前言纪律登记**待证**）。<br>②索引 `status: ACTIVE_INFORMATIVE` 与其"三档主流程都执行的判定门"身份不符（同 §1.2②）。<br>③它的 `changed_paths = docs/standards/**` 使**14 份不被任何门读取**的标准文档落在门的触发面里——即改标准文档会重跑 STD-REG，而 STD-REG 的扫描面只有 `STANDARDS_REGISTRY.md` + `docs/ci/01_CHECKS.md`（docstring `:31-33`/REQUIRED_ANCHORS 实测）⇒ 触发面与读取面不一致：白跑 + 反向掩盖（标准文档正文错号如 `§8.4` 永不判红，见 §1.1a 与 §3） |
| 处置建议 | **迁移** → `eng/tools/doccheck/check_standards_registry.py`（理由：AGENTS.md §7 落位；标准 01 §2 角色表无"脚本"）。`docs/standards/checks/` 整个子目录随件消失。blast radius（须同批改）：`eng/ci/checks.json:8982/9005/9029` 三处 command 字面量、`eng/ci/polarity_probe.py` + `polarity_evidence.json`、`docs/standards/STANDARDS_REGISTRY.md` 的 :5/:317/:334/:355/:369 五处路径与复现命令、`docs/owner/RELEASE_STATUS.md`、`docs/DOCUMENT_INDEX.yaml` 条目。**迁移属门禁改造，需负责人批准**（AGENTS.md §10：改 `eng/ci/checks.json` 注册面）；不迁移则至少：同步注入锚与注册表 §3.2 现文、把 `:211`-类任务编号与 `:68-70/:111/:126-127` 的日期与历史叙事清出（标准 01 §4）、把索引 status 档改对、并把 §5 复现命令与 checks.json 的 command 收敛为单一来源（标准 01 §1 口径唯一） |

<!-- PROGRESS: 已审份数 3/3（派单点名 3 份全部通读并出条目；清单按 §0.1 复算为 0 份缺口，本批为独立复算条目） -->

---

## 2 同主题组与差异

| 主题组 | 成员（含别批路径） | 正本判定 | 差异与后果 |
|---|---|---|---|
| **测试判据/测试规范** | `docs/standards/TEST_STANDARD.md`（本批）、`ENGINEERING_SPEC.md` §5（根规范层，别批 DB-01/DA-01）、`docs/contracts/TEST_MATRIX.md`（DB-09 侧）、`docs/development/TESTING.md`（DB-11 已判角色错放） | 正本 = `ENGINEERING_SPEC.md` §5；映射正本 = `TEST_MATRIX.md` | 同一"必须有独立 Oracle"下限三处各写、互不指（§1.1"重复或重叠对象"①）；两件的下位各标一个互不相干的上位号（§8.4 vs §8，均非 §12）。**前批对本组给出相反的合并方向**：`AUD-101-DB-11.md:674`/`:437` 判"合并 → 并入 `docs/standards/TEST_STANDARD.md`"，而 `AUD-101-DB-10-补.md` §2.14 判"合并（并入 `ENGINEERING_SPEC.md` §5）"⇒ 同主题组的收口方向未收敛，且 DB-11 的处置依据含一个错的规模数（"42 行"，实测 10 行）。**上呈点**：谁吸收谁需一次裁决，两批不得各并一次 |
| **`docs/**` 下的可执行检查器** | `docs/algorithms/anchors/check_doc_line_anchors.py`（本批）、`docs/standards/checks/check_standards_registry.py`（本批）、正确落位同族 `eng/tools/doccheck/check_alg_line_anchors.py`、`eng/tools/doccheck/check_doc_index.py` | 无"文档树内脚本"这一角色 ⇒ 二者均为**放错目录的生产件**（判据见 §1.2/§1.3"角色"栏：被 `eng/ci/checks.json` 的 command 直接执行、被 `eng/tests/quality/test_doc_line_anchors.py` 验收） | 组内差异：`check_doc_line_anchors.py` 的**数据件**（`anchor_contract.json`、`unresolved_registry.json`）确实属文档集，应留 `docs/`；`check_standards_registry.py` 的目录 `docs/standards/checks/` 除它以外空 ⇒ 迁移后该子目录消失。共同后果：门的判据冻结串在"注册表 `.md`"与"检查器 `.py`"两处承载，文档一改注入锚即漂移 |
| **`§8.4（模块与 ABI）` 错号串族** | `docs/standards/TEST_STANDARD.md:3` ＋ `docs/standards/` 下另外 11 份 `.md` ＋ `docs/DOCUMENT_INDEX.yaml` | 唯一正确锚 = `ASTROCS_DESIGN.md:661 ### 8.5 模块与 ABI`（而 `:618 ### 8.4 顶层结构`） | 实测该号题错配是**系统性**的：`grep -l '§8.4（模块与 ABI）' docs/standards/*.md \| wc -l` = **12**（该目录 14 份 `.md` 中的 12 份），`grep -c '§8.4（模块与 ABI）' docs/DOCUMENT_INDEX.yaml` = **73** 条登记。⇒ 不是单份文档的手误，而是索引生成面与标准文档抬头共用了一个错串；且 STD-REG 的读取面不含这些文档（§1.1a），所以该错**永不被门判红**（黑户判据的反面：门读不到 = 错不暴露） |

## 3 索引与链接缺陷清单

确定命令（本节点自跑）：`grep -c '    - path: ' docs/DOCUMENT_INDEX.yaml` = **265**；`grep -cE '^    - path: "docs/.*\.(py|csv|json|yaml|yml)"' docs/DOCUMENT_INDEX.yaml` = **23**；`grep -E '^    - path: "docs/.*\.py"' docs/DOCUMENT_INDEX.yaml` = **恰 2 条**（即本批两份 `.py`）；`grep -c 'status: ACTIVE_INFORMATIVE' docs/DOCUMENT_INDEX.yaml` = **63**（全索引 265 条中的 63 条，本批两份 `.py` 皆在其中）；`grep -c 'duty: "-\*- coding: utf-8 -\*-"' docs/DOCUMENT_INDEX.yaml` = **2**（恰为本批两份 `.py`）。

1. **`docs/DOCUMENT_INDEX.yaml` 把两份 `.py` 当文档登记**（本批三份之二命中）。登记面失真三处，逐件：
   - `docs/algorithms/anchors/check_doc_line_anchors.py`：`status: ACTIVE_INFORMATIVE`（实为三档主流程执行的判定门 ⇒ 状态轴串档）、`duty: "-*- coding: utf-8 -*-"`（生成器把 :2 编码声明当摘要）、`upstream: "ASTROCS_DESIGN.md §4（normalize）、§5（mosaic）、§6（export）"`（该件不落实三命令；其文档级上游行**不存在** ⇒ 索引登记了该件不可能有的元信息）。
   - `docs/standards/checks/check_standards_registry.py`：`status: ACTIVE_INFORMATIVE`（同上）、`duty: "-*- coding: utf-8 -*-"`（同）、`upstream: "ASTROCS_DESIGN.md §8.4（模块与 ABI）"`（号题双错 + 与检查器无关）。
   **根因判定（本批新增，前批只立案到条目级）**：错不在索引条目、也不在这两份文件"命名"，而在扫描面定义——`eng/tools/doccheck/check_doc_index.py:101` `SCAN_EXT = (".md", ".txt", ".json", ".yaml", ".yml", ".py", ".sh", ".h", ".hpp", ".cpp", …)` 显式把 `.py/.sh/.h/.hpp/.cpp` 纳入"文档"集合（另 `:158 RESOLVE_EXTS` 含 `.py`）。⇒ 23 条非 `.md` 登记是**生成器的设计口径**，不是偶发漏网；把 `.py` 迁出 `docs/**` 只能消掉本组 2 条，其余 21 条（`.csv/.json/.yaml`）按同一口径仍会被登记。**该目录是否该含 `.py` 的判定 = 不该**：标准 01 §2 的九个角色里没有"脚本/可执行件"，AGENTS.md §7 把门与检查器归 `eng/ci/`、`eng/tools/`；`docs/**` 允许**机器合同数据**（`.json/.csv/.yaml` 作为文档集的机器面，如 `anchor_contract.json`）但不允许**被 CI 执行的实现件**。⇒ 处置：`.py` 迁 `eng/tools/doccheck/`（连带 `eng/ci/checks.json` 三处 command），并另案收口 `SCAN_EXT` 是否该把可执行扩展算作文档（属工具口径变更，不是本批可自决项）。
2. **未登记 / 登记了但文件不存在**：本批三份在索引与 `docs/audit/doc_classification.csv` 均有登记，且实测文件存在于跟踪集 ⇒ 无此类偏差。已核 `doc_classification.csv:95` = `docs/standards/TEST_STANDARD.md,AUTHORITATIVE,present`。
3. **角色标注与本批判定不符**：两份 `.py` 的索引 `status` 档（见上）；`docs/standards/TEST_STANDARD.md` 索引 `duty: "ACSD Test Standard"` 与实际 H1 `Astro Celestial Sphere Database（ACSD） Test Standard` 不符（缩写式 duty，属生成器截取面，非文档缺陷）。
4. **索引自引用为下游**：`TEST_STANDARD` 索引条目的 `downstream: "docs/DOCUMENT_INDEX.yaml、docs/audit/doc_classification.csv"` —— 把**登记面自身**算作下游引用者 ⇒ 下游计数虚高（真实下游应扣除索引与分类账，另 `AUD-101-DB-10-补.md` §2.14 的"非索引引用 2 份"口径正确）。
5. **机械列（`inventory/doc_mechanical.csv`）对这三份的偏差**：`.py` 的 `h1` 取编码声明（2 处误报）；`task_ids` 漏报 `SCI-ANCHOR-001`、`STD-REG-001`、`W4-A3`、以及 `TEST_STANDARD.md:10` 的轮次代号 `V19`；`dates` 误报 `check_standards_registry.py:97`（外部标准批准日期）；`sha256=True` 误报（把 `hashlib.sha256` 调用当哈希串登记）；`dangling_links` 对 `.py` 内拼接字面量指向的已删除路径**结构性无感**；`history_narrative` 漏报"原 05 号…删除""原式"。**机械列判对项**也逐条记录于 §1.2/§1.3，用于校准：`upstream_pointer=False`（两份 `.py`）、`dates=[]`（`TEST_STANDARD.md`、`check_doc_line_anchors.py`）、`lines`/`meta_block` 全 3 份。
6. 未执行任何门：本批对"STD-REG 是否判红""注入场景是否空转"只给推导与待证标记（§1.3"与上位冲突"①），未跑 `run_checks.py`、未跑检查器（前言 §1）。

## 4 覆盖率自报

- 派单确定式复算所得清单 = **0 份**；派单点名并强制要求的 **3 份全部通读全文并出 14 字段条目**（含 §1.1a 的门禁映射专项）。已审 3 / 应审 max(0, 3) = 3。
- **未逐行读名单：空**。三份的读入规模：`TEST_STANDARD.md` 10 行全读；`check_doc_line_anchors.py` 483 行——docstring 全文（:1-40）、判据与常量区（:64-74、:126-127 经 `sed` 定点读）、违规点行（:3/:21/:90/:211）与写盘行为（:228）逐行读，其余为 C1–C8 口径复述区（已由 `ANCHOR_CONTRACT.md:72-76` 对照覆盖）；`check_standards_registry.py` 702 行——docstring 全文（:1-34）、外部来源常量与注释（:64-74）、注入锚（:126-127）、C9/守卫区（:24/:43/:522/:573）、哈希与写盘（:669/:676/:689）逐行读。
- **诚实边界**：§1.2"重复或重叠对象"②（与 `ALG-LINE-ANCHORS` 兄弟门是否互补）与 §1.3"与上位冲突"①（注入锚与注册表 §3.2 现文逐字比对）两处按前言纪律登记为**待证**，各写明缺哪两处段落；不因此把结论写成"已否证"。
- 本批**不新增**任何仓库文件、未改 `整改/` 共享台账、工作件只落 `独立审计/复算件/d1gap3/`。

## 5 我证伪了任务书的哪个前提

**证伪派单（本批派单文本）的前提"真正从未被任何批次登记的只有 3 份"：复算结果为 0 份。** 依据与因果：

1. 按派单给出的**最宽松**口径（全路径 / 包内相对路径 / basename 三形任一，出现在 `独立审计/证据/AUD-101-*.md` 任一成稿即算已登记）：360 − 360 = **0**。
2. 按派单隐含的**装配口径**（`scripts/gap_check.py:28` 的确定式 `[p for p in items if p not in text]`，全路径子串、成稿全文）：同样 **0**（`python -B gap_recompute2.py` 的 `[T1 full-path missing] 0`，控制项 `ASTROCS_DESIGN.md` 在同一条判定上命中 = True，排除"串永远不匹配"式假缺口）。
3. 按**最严格**口径（必须是 14 字段条目，正文提及不算；`strict_coverage.py`，逐份块内字段标签 ≥9 才算真条目）：仍是 **360/360，GAP 0**，且三份的条目字段数分别 12、13、13（§0.1 表）。
4. 成因（时间线，`stat` 读数）：`AUD-101-DB-10-补.md`（16:20）落下 §2.14/§2.15 之后，`inventory/D1-final-missing.txt` 与 `DB-10-missing.txt` 在 **16:23 已是 0 字节**。派单文本沿用的是 16:20 之前的名册。`AUD-101-D1残余.md:23`/`:425` 的"TEST_STANDARD.md 按派单跳过（另一路在做）"正是那一轮在做、并已在 16:20 做完。

**未发现 AUD-101 任务书层面的新可证伪前提**（"约 233 份"与本审计面 360 的口径差已由既有立案覆盖，见 `AUD-101-DB-10-补.md` §5 与项目记忆"AUDIT-06 包文偏差"条：口径须固定，`DA-all` = 四路 `git ls-files` 并集 360，`db_u` = 303，二者不是同一口径）。本批另**订正前批两处具体读数**（非派单前提，但同属"凭印象"类缺陷）：`AUD-101-DB-11.md:431` 的 `TEST_STANDARD.md`"42 行"（实测 10 行）；`AUD-101-DB-10-补.md` §2.14 未记的"索引把登记面自身算作下游"（本批 §3.4）。

## 6 缺口复算命令与结果（D1 覆盖 360/360）

审计面确定式（派单原文，DA-01..DA-06 口径）：

```bash
git -C "F:/Astro dev/Astro CS Normalization Database" -c core.quotePath=false \
  ls-files -- docs '*.md' 工程控制 '实验/*.md' | sort -u      # = 360，与 inventory/DA-all.txt 逐行同（wc -l = 360，unique = 360）
```

减法（本批两条独立复算，脚本与工作件均在 `独立审计/复算件/d1gap3/`）：

```bash
cd "独立审计/复算件/d1gap3"
python -B gap_recompute2.py        # T1 全路径 / T2 basename 两档
python -B entry_coverage.py        # T3 条目位（标题行或表首格出现路径）
python -B strict_coverage.py       # T4 条目形状：块内 14 字段标签命中 >=9
```

读数（原文见 `logs/gap_recompute2.log`、`logs/entry_coverage3.log`、`logs/strict_coverage3.log`）：

| 档 | 判定式 | 结果 |
|---|---|---|
| T1 | 全路径出现在 23 份 `AUD-101-*.md`（1 681 312 字符）任一 | missing **0** / 360 |
| T2 | basename 或任一层后缀出现 | missing **0** / 360 |
| T3 | 出现在条目位（`###` 标题或表首格），按各批自己的清单归属 | entry-covered **360** / 360，GAP **0** |
| T4 | 条目块含 ≥9/13 字段标签 | covered **360** / 360，GAP **0** |
| 派单减项 | `工程控制/RELEASE-05/tasks/` 的 10 份（`D1-residual.txt` 第 2–11 行）已由 `AUD-101-D1残余.md` 以 basename/`tasks/…` 后缀登记 | 已登记，**未重复登记** |

⇒ **D1（文档审计面）覆盖至此为 360/360。** 本批的 3 份条目是**第二路独立复算**（复核前批结论并订正 2 处读数、新增 2 类系统性根因），不是补漏。

## 7 收工基线（`git status --porcelain` 原文）

见文末追加（与开工一致，无新增项）。

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工基线**逐字相同**（无新增项、无修改项）⇒ 取证动作未污染被审对象。本批全程只读命令：`git status --porcelain`、`git ls-files`、`git grep`、`sed`/`grep` 定点读、`wc`/`stat`；`python -B` 仅用于解析审查侧成稿与读取 `eng/ci/checks.json` 数据（未 `import` 仓库 Python、未生成 `__pycache__`、未执行任何 `eng/**` 脚本或门禁）。

工作件：`独立审计/复算件/d1gap3/{gap_recompute.py,gap_recompute2.py,entry_coverage.py,strict_coverage.py,gate_map.py,dbg.py,gap.txt,entry_gap.txt,strict_gap.txt,logs/*.log}`。
