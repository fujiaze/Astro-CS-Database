# AUD-501 门禁现状审计（AUDIT-06 只读整治 · 事实基础）

审查节点：独立只读审查节点。冻结基线 HEAD=`c8f64e9a`。
判据来源：`standards/05_门禁重建设计原则.md`（下称「标准05」）+ 仓库 `docs/ci/CI_SPEC.md`、
`docs/ci/03_GATES.md`、`docs/ci/01_CHECKS.md`、`eng/ci/checks.schema.json`。

## 0. 只读纪律与工作树自证

开工基线（`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与任务书预期一致（恰两行）。本审计全程未跑构建 / ctest / `run_checks.py`（任何模式）/ 三命令端到端，
未 `import` 仓库内 Python 模块（所有普查脚本以文本/`json`/`ast` 读仓库文件，落在审查工作区
`独立审计/工具脚本/AUD-501/`，不写仓库）。仓库内既有的 `eng/ci/__pycache__/`、`eng/tools/quality/__pycache__/`
按负责人既有口径（审查范围只限 git 跟踪内容）不算仓库问题，且本审计未新增任何 `.pyc`。

收工差异见文末「附录 A 工作树收证」。

## 1. 确定性普查（在册规模与分布，Q1）

命令与产物（全部可复算）：

```bash
python "独立审计/工具脚本/AUD-501/census_registry.py"   # 总量/分布
python "独立审计/工具脚本/AUD-501/census2.py"           # 字段完备性/守卫声明普查
python "独立审计/工具脚本/AUD-501/census3.py"           # 注册命令 → 磁盘对象可达性
python "独立审计/工具脚本/AUD-501/census5.py"           # AST 精度普查（subprocess/空扫描/硬编码/self-test）
```

| 量 | 值 | 出处 |
|---|---|---|
| 顶层注册项 | **150** | `eng/ci/checks.json` `checks[]` 长度 |
| 执行单元（顶层单步项 + 全部 step） | **370**（其中 step 291 个） | 同上 |
| profile 分布（按单元计，单元可多档） | linux-main 345 / fast 194 / windows-main 168 / prerelease 64 / linux-deep 10 / integration 9 / fatduck 3 | 同上 |
| `waivable` 分布（按单元计） | false 360 / true 8 / **字段缺失 2** | 同上 |
| 声明 `inputs` 的单元 | **10 / 370**（2.7%） | `inputs` 键普查 |
| 声明 `optional_inputs` 的单元 | 1 | 同上 |
| 登记 `outputs` 为空的单元 | **157 / 370** | 同上 |
| `heavy` ⇒ `requires_monitor` | 13 / 13（无违例） | 同上 |
| 命令引用的脚本目标缺失 | 0 | `census2.py` `COMMAND_TARGETS_MISSING_ON_DISK` |
| 单元 ID 重复 | 0（顶层与 step 合并后无重名） | `census2.py` `DUP_UNIT_IDS` |

`waivable=true` 的 8 个单元：`DEEP-CLANG-BUILD`、`DEEP-COMPLEXITY`、`TESTKIT-LIST`、`UT-CPU-AVX512`、
`DEEP-SAN-TSAN`、`DEEP-COV-PY`、`CHK-REALDATA-E2E`、`ACTIONS-LOCK-ONLINE`。

`waivable` 字段缺失的 2 个单元：`SPEC-POLARITY-CONSIST-01`、`SPEC-POLARITY-CONSIST-01-SELFTEST`
（二者为 step；`checks.schema.json` 的 step `required` 列表不含 `waivable`/`mutates_workspace`，
见 A-07）。

---

## 2. 逐条结论

### A-01 注册表↔文档双向一致门（`CHK-REGISTRY-DOC-SYNC` / step `REGISTRY-DOC-SYNC`）在基线上必判红，且 8 个在册门在文档集中零登记

- **对象**
  - `eng/ci/check_registry_doc_sync.py:96` `return [c["id"] for c in data.get("checks", []) if isinstance(c, dict) and "id" in c]`
  - `eng/ci/check_registry_doc_sync.py:71` `sec2 = _slice(doc_text, "## 2. 检查项清单", ["### 2.1", "## 3."])`
  - `eng/ci/check_registry_doc_sync.py:108` `"registered_not_documented": sorted(reg - lst),`
  - `eng/ci/checks.json:8261`（step `REGISTRY-DOC-SYNC` 命令 `["python3","eng/ci/check_registry_doc_sync.py"]`，用默认 `--doc docs/ci/01_CHECKS.md`）
  - `docs/ci/01_CHECKS.md:19`（`## 2. 检查项清单`）与 `docs/ci/01_CHECKS.md:166`（`### 2.1 检查器退役与预留`）之间的 §2 表：解析得 **142** 行 ID
- **权威依据**：标准05 §5「每条门禁对应一条文档口径或合同条款，映射关系登记」＋ §5「门禁注册表与实际可执行检查一致」；`ENGINEERING_SPEC.md §8`（该门 docstring 第 5-7 行自引）
- **现状 → 应为**：现状 = 注册表 150 个顶层 ID 中 **8 个**不在 §2 表内，按 R1 应判 `REGISTRY_DOC_SYNC_FAIL` + rc=1：
  `CHK-BUDGET-SINGLE-SOURCE`、`CHK-DOCS-MACHINE-CONSISTENCY`、`CHK-DOCS-MACHINE-CONSISTENCY-SELFTEST`、
  `CHK-DRZ-PF-AREA-S1`、`CHK-ISA-SAME-SOURCE`、`CHK-PROD-WIRING`、`CHK-TRUTHFUL-CONCLUSION`、`SPEC-POLARITY-CONSIST-01`。
  其中 **7 个在整个 `docs/**` 里零出现**（仅 `CHK-PROD-WIRING` 出现在 `docs/architecture/MODULE_MAP.md`、
  `docs/contracts/PUBLIC_API.md`，不在 CI 文档集）。R2 方向（文档承诺但无实现）为空。
  应为 = 每个在册门在 §2 表有一行映射；红要么修要么显式登记。
- **改法**：① 补 §2 表 8 行（ID/类别/命令/门禁级/判据对象）；② 把 step 级 ID 也纳入映射面（见 A-02）；
  ③ 该门自身保留为 P0 红即可——不得进 `known_failures`（当前基线内只有 `p1_noise_adapter` 一条 ctest 项，未用它盖红，见 A-13）。
- **置信**：**CONFIRMED**（判据与差集为静态可读事实）；rc 实际值 **需复测**（本阶段禁跑）
- **取证命令**：
  `git grep -c "CHK-TRUTHFUL-CONCLUSION" -- docs/ci/01_CHECKS.md`（0 命中；同命令换 `CHK-NAMING-SURFACE` 得正命中）；
  §2 行集复算脚本见本文末附录 B（复刻 `_slice`+`_rows`+`ID_RE` 三个判据面）。

### A-02 同一门的覆盖面只到顶层 ID：291 个 step（执行单元总数的 79%）不在任何文档映射面上

- **对象**：`eng/ci/check_registry_doc_sync.py:96`（只取 `c["id"]`，从不取 `c["steps"][i]["id"]`）；
  普查复算：`step IDs absent from doc §2 table: 287`，`step IDs with zero textual mention in 01_CHECKS.md`
  至少含 `BUILD-GCC-RELEASE`、`V6-CTEST-UNIT`、`TRUTH-CONCLUSION-GATE`、`PROD-WIRING-SCAN` 等
- **权威依据**：标准05 §5「每条门禁登记其检验对象、命令、判据、正负例与所属档位」——登记面缺失即判据不成立
- **现状 → 应为**：现状 = 门名承诺「注册表 ↔ 文档 双向一致」，实现在「顶层 ID × §2 表第一列」这一个笛卡尔子集上；
  78% 的执行单元（真正被派发、真带 command/timeout/waivable 的对象）零文档映射。应为 = 一致面对齐到执行单元粒度，
  或把该门的名字与判据面收窄到「顶层项」并在 §2 表说明它不管 step。
- **改法**：`load_registry` 同时产出 `(top_ids, step_ids)` 两级差集，R1/R2 分别判；
  §2 表增列「执行单元」或在 §2.4 增「step 清单」表；不得用「文档提到过该字符串」代替表行
  （现在 287 个 step ID 中 4 个靠散文出现在 §2 之外，仍不算登记）。
- **置信**：**CONFIRMED**
- **取证命令**：附录 B 脚本第 3 段（`step_ids` vs `listed` vs 全文本提及）。

### A-03 `CHK-FAILCLOSED-SURVEY` 的「每门三注入」只测 runner 自身的证据管道，测不到被普查门的判别力

- **对象**
  - `eng/ci/failclosed_survey.py:79` `got = RC.evidence_verdict(step, root, stdout_tail, "")`
  - `eng/ci/failclosed_survey.py:37` `import run_checks as RC  # noqa: E402  (真判定函数单一实现点)`
  - `eng/ci/failclosed_survey.py:136` `ok = bool(applicable) and all(f in RED_VERDICTS for f in applicable)`
  - `eng/ci/failclosed_survey.py:44-55` `BAD_EVIDENCE = {...}`（硬编码一份违规监控证据字面量）
  - `eng/ci/failclosed_survey.py:70-75` `def _sandbox(...)`（自建临时沙箱 + 复制 `resource_gate_v1.json`）
- **权威依据**：标准05 §2「判据读取真实对象（产品文件、运行结果、注册表、合同）」＋ §2「恒真断言、空断言、
  不读真实对象的检查……判为无效门」；§2「每条判据在正确实现下判绿，在注入对应缺陷时判红」
- **现状 → 应为**：现状 = 三面注入全部作用在 **同一个共享函数** `run_checks.evidence_verdict` 上，
  注入物是本文件自造的 sandbox 目录与 `BAD_EVIDENCE` 字面量，被普查的门 **其命令从未执行**：
  A 面判红的充要条件是「该单元在注册表里声明了 outputs」，C 面判红的充要条件是「outputs 为空且非 waivable」——
  两者都是注册表行的形状属性，与该门实现读什么、能不能抓到缺陷**无因果**。
  因此普查表里 213 个「通过」不构成为这些门判别力的任何证据；`docs/ci/03_GATES.md:103-106` 把它当作
  「全门禁 fail-closed 普查」的事实来源呈给下游。应为 = 该门如实改名为「runner 证据管道 fail-closed 契约普查」，
  并另建一层「逐门缺陷注入」普查（对每个单元构造最小缺陷对象、真跑其命令、断言 rc≠0）。
- **改法**：① 门的 docstring/`--md-out` 表头与 03_GATES.md §6.4 明确写出「三面 = runner 判定函数，非该门判别力」；
  ② 新增 `polarity_probe` 式的 per-gate 注入面（见 A-09/A-10 对该文件的处置）；
  ③ `ok` 计算保持「零适用面不得算通过」——现状 `covered` 过滤已把无适用面排除在 failures 外，
  但 `--self-test` 的 G1 断言只要求 `not failures and units > 50`，`units>50` 阈值对当前 370 单元面是死数（见 A-04）。
- **置信**：**CONFIRMED**（读实现体即得；无需运行）
- **取证命令**：`git grep -n "evidence_verdict" -- eng/ci/failclosed_survey.py eng/ci/run_checks.py`
  证明唯一判定入口 = runner 函数；`git grep -n "subprocess\|run(" -- eng/ci/failclosed_survey.py` 零命中
  ⇒ 普查从不执行任何被普查命令。

### A-04 普查门自测的判别力锚是「单元数 > 50」，注册表已 370 单元仍用 50 当门限

- **对象**：`eng/ci/failclosed_survey.py:201-203`
  `cases.append(("G1", "真实注册表普查可跑通且无假绿", not good["failures"] and good["units"] > 50, ...))`
- **权威依据**：标准05 §2「判据必须有判别力」；§4「覆盖面只增不减」
- **现状 → 应为**：现状 = 若注册表被裁剪到 51 个单元（例如 step 展开逻辑回归），G1 仍绿；
  阈值与实测规模差 7.4 倍，属"把历史观测量抄成阈值"的同一形态。应为 = 断言 `units == 注册表实算单元数`
  （由 `len(registry)` 与 step 展开直接算），或至少单调棘轮（不得低于上次登记值并留痕）。
- **改法**：`good["units"] == expected_units(registry)`；把 03_GATES.md §6.4 的 230/78 同步换成机器生成值。
- **置信**：**CONFIRMED**
- **取证命令**：`git grep -n "units. > 50" -- eng/ci/failclosed_survey.py`。

### A-05 `docs/ci/03_GATES.md` §6.2/§6.3/§6.4 的门禁事实与注册表现值不一致，且无在册门负责这类数值口径

- **对象（文档侧锚）**
  - `docs/ci/03_GATES.md:103` 「`CHK-FAILCLOSED-SURVEY` 对 230 个执行单元（78 个注册项）注入三面，226 个适用面全部判红、4 个显式无适用面」
  - `docs/ci/03_GATES.md:69` 「### 6.2 requires_monitor 声明对照表（8 个检查 / 11 个执行单元）」
  - `docs/ci/03_GATES.md:97-99` 「7 个顶层声明（CHK-SCI-REF / CHK-CONTRACT-TEST / CHK-UNIT / CHK-SYNTH-P2 / CHK-NWORKER / LINUX-MAIN-FIXTURES / LINUX-MAIN-BUILD-TREE）与 6 个 step 声明」
  - `artifacts/evidence/release-05/FAILCLOSED_SURVEY.md:7-10` 「执行单元总数：**230**；有适用注入面的：**226**；判绿：**0**；无适用注入面：4」
- **对象（注册表现值，`census2.py` 复算）**
  - 注册项 150 / 执行单元 370；`requires_monitor=true` 单元 **13**（新增 `TEST-INDEX-LIVE`、`CTEST-LINUX-FULL`，未入 §6.2 表）；
    `mutates_workspace=true` 顶层项 **9**（新增 `CHK-MASTER-UNIT-GUARD`、`CHK-MASTER-UNIT-GUARD-SELFTEST`）
  - 三面适用性现算：A 面 213、B 面 13、C 面 155（复算脚本见附录 B 第 4 段）
- **权威依据**：标准05 §5「文档口径变更时同步更新对应门禁」；§6「退出码、判据值、日志与证据落档，可复核」；
  负责人既有口径「文档六条硬口径：口径唯一 / 自洽」
- **现状 → 应为**：现状 = 归档件（`FAILCLOSED_SURVEY.md`，230 单元）与当前注册表（370 单元）差 140 单元，
  03_GATES.md 把归档件数字当现状陈述 ⇒ **只回放归档件、不用当前产物现算**在文档层成立；
  §6.2/§6.3 的计数同样落后。应为 = 这类计数要么由生成器从 `checks.json` 现算并写入（同
  `gen_id_migration_map_doc.py` 之于 `id_migration_map.json` 的做法），要么文档不写绝对计数、只写判据与生成命令。
- **改法**：① 加一条在册门断言「文档内出现的门禁计数 == 注册表现算值」（或把这些数字从文档里删掉）；
  ② `CHK-FAILCLOSED-SURVEY` 的 `--md-out` 目标从 `artifacts/evidence/release-05/` 改为每轮 `run/` + 稳定证据锚，
  避免"证据锚永远指着一轮旧快照"。
- **置信**：**CONFIRMED**（差值来自注册表实算）
- **取证命令**：`census2.py` 的 `REQ_MONITOR_UNITS` / `mw_true` 行 + 附录 B 第 4 段。

### A-06 157 个执行单元零登记证据面（`outputs: []`）；runner 对"零证据"的唯一防线是「stdout 或 stderr 至少一行非空」

- **对象**
  - `eng/ci/run_checks.py:374-379`
    `if (not step.get("outputs") and not step.get("waivable") and step["id"] not in SILENT_OK_UNITS and not stdout_tail and not stderr_tail): return (V_EMPTY_OUTPUT, ...)`
  - `eng/ci/run_checks.py:142-145` `SILENT_OK_UNITS = frozenset({"API-DOCS", "UNIT-CLOSURE"})`
  - 普查：`OUTPUTS_COUNT_zero_total = 157`，`EMPTY_OUTPUTS_NONWAIVABLE` 列出 155 个（含 `TRUTH-CONCLUSION-GATE`、
    `CHK-REGISTRY-VALIDATE`、`PROD-WIRING-SCAN`、全部 `UT-*`）
- **权威依据**：标准05 §2「恒真断言、空断言、不读真实对象的检查、**证据为空即通过**的门不具备证据资格」；§6「退出码、判据值、日志与证据落档，可复核」
- **现状 → 应为**：现状 = 42% 的执行单元不落任何登记证据文件，判定物只有一条 rc + 一坨打印；
  防线只挡"一条字都不打印"的情形（`not stdout_tail and not stderr_tail`），任何一句 `print("OK: 0 violations")`
  即满足留痕。应为 = 判"结论"的门（尤其 A-11 起的聚合/写结论门）必须有结构化证据 JSON 进 `outputs`，
  使判据值可被独立读者复算；`SILENT_OK_UNITS` 白名单不得再扩。
- **改法**：注册表校验器（`eng/ci/validate_registry.py`）增一条规则 R：非 waivable 且 `outputs` 为空的单元，
  其 `command` 若被 `mutation_gates.json`/`polarity_evidence.json` 之类"结论面"引用 ⇒ 要求登记 `outputs`；
  过渡期至少把 3 个写结论的门（`TRUTH-CONCLUSION-GATE`、`KNOWN-FAILURES-BASELINE-*`）改为带证据 JSON。
- **置信**：**CONFIRMED**（判据实现体 + 普查计数）
- **取证命令**：`census2.py` `EMPTY_OUTPUTS_NONWAIVABLE`；`git grep -n "SILENT_OK_UNITS" -- eng/ci/`。

### A-07 【反证】注册表与其 JSON Schema 无 required/additionalProperties 违例；「step 缺 `waivable`」是设计上的继承面，不是断链

- **对象**：`eng/ci/checks.schema.json:17-27`（顶层 required 九字段）、
  `eng/ci/checks.schema.json:185-191`（step required 仅 `id/command/timeout_seconds/profiles/platform`）、
  `eng/ci/run_checks.py:155-158` `INHERIT_FIELDS = ("command","profiles","platform","timeout_seconds","heavy","mutates_workspace","outputs","waivable", ...)`
  与 `eng/ci/run_checks.py:234` `for field in INHERIT_FIELDS:`（step 缺字段即继承父项）
- **权威依据**：标准05 §5；`docs/ci/CI_SPEC.md §2.3`「step 未显式声明的字段继承父项」
- **现状 → 应为**：现状 = 静态复算 `checks.json` 对 schema 的符合性：顶层 required 违例 **0**、
  step required 违例 **0**、非法键 **0**。普查里 2 个「waivable 缺失」单元
  （`SPEC-POLARITY-CONSIST-01`、`-SELFTEST`）是 step，其父项 `SPEC-POLARITY-CONSIST-01` 顶层
  `waivable: true`，按 `INHERIT_FIELDS` 继承 ⇒ 不构成缺陷。应为 = 保持现状；
  但**继承只在 `run_checks.py` 一侧实现**，见 A-08。
- **改法**：无需改注册表；把「waivable 缺失」从异常清单移入正常清单（避免下轮误报）。
- **置信**：**REFUTED**（任务书隐含的「字段不完备」前提在本面上不成立）
- **取证命令**：附录 B 第 5 段（用 `checks.schema.json` 的 required 列表反查 `checks.json`，结果 0/0/0）。

### A-08 两个 runner 是并存的两套判定实现：字段继承与「空集不得 PASS」只在其中一侧

- **对象**
  - `eng/ci/run_checks.py:155`（`INHERIT_FIELDS`）与 `eng/ci/run_checks.py:234`（应用继承）——
    `git grep -n "INHERIT\|setdefault.*waivable" -- eng/ci/run.py` ⇒ `run.py` 侧 **零继承实现**
  - `eng/ci/run.py:1254-1255` `"fatduck profile 零成员（no_checks_selected）：不允许空集 PASS"`、
    `eng/ci/run.py:1260-1262` `零有效执行，不允许空集 PASS`、`eng/ci/run.py:1266`
    `"未选中任何可执行检查（no_checks_selected），不允许空集 PASS"`
    vs `git grep -n "不允许空集\|no_checks" -- eng/ci/run_checks.py` ⇒ **零命中**
  - `eng/ci/run_checks.py:1966` `verdict = "FAIL" if failures else "PASS"`；
    `eng/ci/run_checks.py:1932` `failures = [s["id"] for s in step_results if s["verdict"] in FAIL_VERDICTS]`；
    `eng/ci/run_checks.py:136-138`（`FAIL_VERDICTS` 不含任何 `SKIPPED*`）
  - `eng/ci/run_checks.py:603-605` `if step["platform"] != "any" and step["platform"] != platform: return finish(V_SKIP_PLATFORM, ...)`
    （在任何输入守卫与任何执行之前）
  - `eng/ci/validate_registry.py:383-385` R12「聚合项 command 必须经 `eng/ci/run_checks.py` 派发 steps」
- **权威依据**：标准05 §2「门的崩溃与判红分离」＋ §3「关键口径缺失、输入不满足前提时判失败（fail-closed）」；
  `docs/ci/CI_SPEC.md §2.1`（`--check` 点名不受 profile/改动面过滤）、`§2.4` 第 3 条（空选择判红）；
  两文件自我声明的一致性合同 `eng/ci/run_checks.py:96-98`「与 eng/ci/run.py 同合同（W4-A3：
  两入口对同一退出码的判定必须一致，否则同一 step 在两个入口一红一绿）」
- **现状 → 应为**：现状 = 存在两条把「什么都没执行」判成绿的通道，且都只在 agent 直跑的那条入口上：
  ① `run_checks.py --check <ID>` 点名的单元若 `platform` 与本机不符 ⇒ 每步 `SKIPPED(platform)` ⇒
  `failures` 为空 ⇒ `verdict="PASS"` ⇒ `return EXIT_OK`（`run_checks.py:2056`），rc=0；
  本机是 Windows，`platform: linux` 的单元共 345 个（占单元数 93%）全部落在此面。
  ② `--all --profile linux-main` 在 Windows 上同理全 skip 全 PASS。
  `run.py` 对同两种情形显式判 FAIL（1256-1266），CI 面成立、agent 直跑面不成立。
  应为 = 两入口共享同一份「零有效执行 ⇒ 红」判定（把 `run.py` 的三分支提取到 `gate_common.py` 或
  `declared_inputs.py` 同层模块，两侧各自调用），并让 `SKIPPED(platform)` 在非 `--explain` 面不得撑起 rc=0。
- **改法**：`run_checks.py` 收尾处加：`if not [s for s in step_results if s["verdict"] not in SKIP_*]: verdict=V_SCOPE; failures+=["NO_EFFECTIVE_EXECUTION"]`；
  同步把 `INHERIT_FIELDS` 提取为共享函数供 `run.py` 使用（R12 已保证聚合项都经 run_checks 派发，
  但顶层单步项仍由 run.py 直接执行）。
- **置信**：**CONFIRMED**（判定链逐行可读，含 rc 出口 `run_checks.py:2056`）；实际 rc 值 **需复测**
- **取证命令**：
  `git grep -n "INHERIT_FIELDS" -- eng/ci/run_checks.py eng/ci/run.py eng/ci/validate_registry.py`；
  `git grep -n "不允许空集" -- eng/ci/run.py`（正控）与 `git grep -n "不允许空集" -- eng/ci/run_checks.py`（零命中）。

### A-09 门崩与判红在三条出口上都分离（正例），但「显式声明崩」需要 rc=3 **且** 打印 `GATE_TRUST_FAIL|SELFTEST_FAIL`，而后者在门侧无强制

- **对象**
  - `eng/ci/run_checks.py:93` `EXIT_CRASH = 3`；`eng/ci/run_checks.py:137`（`FAIL_VERDICTS` 含 `V_CRASH`）
  - `eng/ci/run_checks.py:681-683` `trust_declared = (not timed_out and proc.returncode == EXIT_CRASH and bool(TRUST_DECLARED_RE.search(...)))`；`crash = looks_like_crash(...) or trust_declared`
  - `eng/ci/run_checks.py:417` `TRUST_DECLARED_RE = re.compile(r"GATE_TRUST_FAIL|SELFTEST_FAIL")`
  - `eng/ci/run_checks.py:401` + `:541`（启发式：`Traceback` 且**无**任何 `VERDICT_TOKEN_RE` 命中才算崩）
  - `eng/ci/run_checks.py:2054-2055` `if counts[V_CRASH]: return EXIT_CRASH`；`eng/ci/run.py:1512-1513` 同
  - `eng/ci/run_checks.py:1898-1907`（条目级三态传播：崩溃优先于超时/判红）
- **权威依据**：标准05 §2「门的崩溃与判红分离：检查器自身故障显式报错，不伪装成通过或失败结论」
- **现状 → 应为**：现状 = 分离面**成立**（崩 ⇒ rc=3、红 ⇒ rc=1、runner 自身错 ⇒ rc=2，三条不同码），
  且 `run_checks.py:1892-1896` 的注释明确拒绝把崩塌平成 FAIL。残余缺口两处：
  ① 显式声明通道依赖门自己打印 `SELFTEST_FAIL`/`GATE_TRUST_FAIL` 字面量，但注册表/schema
  无任何字段登记「本门具备 rc=3 声明能力」，也没有门要求打印该字面量 ⇒ 该通道实际只有少数门会用；
  ② 启发式把「先打印过 `verdict: PASS` 再抛 Traceback」的单元**不记崩**（`:541` 的合取），
  此类情形会落 `FAIL`（rc≠0）——方向安全，但会把「门自身坏了」显示成「内容不合规」。
  应为 = 保留三态退出码；把 `TRUST_DECLARED_RE` 的字面量要求改为注册表字段
  （如 `crash_declared: true`）并由注册表门校验，不靠正则与命名。
- **置信**：**CONFIRMED**（分离成立为正例；两处缺口为 CONFIRMED 弱项）
- **取证命令**：`git grep -n "GATE_TRUST_FAIL\|SELFTEST_FAIL" -- eng/ci/*.py eng/tools/quality/*.py | wc -l`
  与 `git grep -ln "GATE_TRUST_FAIL" -- eng/**`（看有多少门真的会打印该标识）。

### A-10 豁免面为零且 `known_failures` 未用于盖红（正例）；真正的灰区是 `waivable=true` 让 P0 门可静默 SKIP

- **对象**
  - `eng/ci/exemptions.json:22-29` `"high_water": {"max_entries": 0, ... "note": "本波负责人裁决：不得新增任何豁免…允许 CI 保持红"}`、`"approvals": []`、`"exemptions": []`
  - `eng/ci/known_failures.json` `failures` 长度 **1**，且唯一条目是 ctest 项 `p1_noise_adapter`
    （`kind: ctest`、`category: LEGACY_V7_GATED`、条件式登记），无任何 `CHK-*`/step ID 进基线
  - `eng/ci/run.py:1267` `elif hard_fail or known_fail or known_summary.get("expired") or ...: verdict = "FAIL"`
    ⇒ `KNOWN_FAIL` 在汇总面**仍判红**，不是洗绿通道
  - `docs/ci/01_CHECKS.md:96`（`CHK-REALDATA-E2E` 标 **P0**）vs `eng/ci/checks.json` 同项 `"waivable": true`
  - `eng/ci/run_checks.py:617-618` `if step.get("waivable"): return finish(V_SKIP_WAIVABLE, "prerequisite 未满足（waivable）…")`
  - `eng/ci/exemptions.json:6-7` `never_waivable: ["P0 门", ...]`
- **权威依据**：标准05 §6「红灯不以 waiver 静默覆盖」「不把 SKIP 计入通过；零豁免、零跳过的结论以实际输出为准」；
  `docs/ci/03_GATES.md §1`「P0 红灯无豁免」；`docs/ci/CI_SPEC.md §4`「只有负责人可批准豁免（写入 exemptions.json，只减不增）」
- **现状 → 应为**：现状 = **没有用 waiver 盖红**（豁免 0 条、基线内无门级红、`KNOWN_FAIL` 仍红）——
  这一条如实成立。但「零跳过」不成立：8 个单元 `waivable=true`，其中 `CHK-REALDATA-E2E`
  是文档标 P0 的真实数据端到端门，prerequisite/platform 任一不满足即 `SKIPPED(waivable)`，
  而 `V_SKIP_WAIVABLE ∉ FAIL_VERDICTS`（`run_checks.py:136-138`）⇒ 该 P0 门可以在一次 rc=0 的运行里从未执行。
  应为 = P0 门的 waivability 与豁免同等对待：要么在注册表校验器加一条规则
  （01_CHECKS.md 标 P0 的行 ⟹ `waivable: false`），要么把「P0 但 waivable」的例外显式登记并写明降级语义。
- **改法**：`validate_registry.py` 新增 R16：解析 `docs/ci/01_CHECKS.md §2` 表的「门禁」列，
  对 P0 行断言注册表对应项 `waivable == false`，命中即 `R16 p0_but_waivable: [...]`。
- **置信**：**CONFIRMED**（豁免零面 + P0-且-waivable 冲突均可读）
- **取证命令**：`PYTHONIOENCODING=utf-8 python -c "import json;d=json.load(open('eng/ci/exemptions.json',encoding='utf-8'));print(len(d['exemptions']),len(d['approvals']),d['high_water'])"`；
  `git grep -n "CHK-REALDATA-E2E" -- docs/ci/01_CHECKS.md`。

### A-11 未登记 / 未被任何派发面触达的检查器：14 个 `check_*.py` 不在注册表任何位置——10 个已退役（1 个退役契约不合规）、2 个上呈待定、1 个是在册门的内部件、1 个只剩自家单测

- **对象（普查，按「完整路径是否出现在 `eng/ci/checks.json` 任意位置」判定）**
  `eng/tools/` 与 `eng/tools/quality/**` 下共 77 个 `check_*.py`，其中 14 个零出现：
  `check_final_traceability.py`、`check_p1_symbol_map.py`、`check_p2_symbol_map.py`、
  `check_release_consistency.py`、`check_release_layout.py`、`check_reproducible_build.py`、
  `check_traceability.py`、`check_traceability_matrix.py`、`quality/check_comment_hygiene.py`、
  `quality/check_commits_csv.py`、`quality/check_conclusion_anchors.py`、
  `quality/check_p3_rejection_count.py`、`quality/check_source_inventory.py`、
  `quality/check_task_result_schema.py`
  - 其中 10 个 = `docs/ci/01_CHECKS.md:199-209`「应退役（10）…无参调用打印退役标识并 exit 2；
    原实现保留在 `--legacy-*` 显式入口下；每个都带 `--self-test`」——**逐个核对后 9 个属实、1 个不属实**：
    9 个同时具备 `RETIREMENT_MARKER` + 无参 `return 2` + `--self-test` + `--legacy-*`
    （如 `eng/tools/check_release_layout.py:37-40` `RETIREMENT_MARKER`、`:198-200`
    `print(RETIREMENT_MARKER, ...)`+`return 2`、`:192` `--self-test`）；
    **例外 = `eng/tools/check_traceability.py`**：退役标识与 `return 2` 在位
    （`:119-123` 无参打印 `TRACEABILITY_RETIRED:` 并 `return 2`），原实现也仍可复跑，
    但入口是**位置参数** `<claims.csv>` 而非 `--legacy-*` 旗标，且该文件 `'self-test'`
    表达式 **0 命中**（正控：同表达式跑 `eng/tools/quality/check_comment_hygiene.py` 得 18 命中）
    ⇒ 文档「每个都带 `--self-test`」对它不成立，「保留在显式 `--legacy-*` 入口下」的措辞与它不符
  - 2 个 = 文档点名「应删除但保留并上呈」：`check_source_inventory.py`、`check_p1_symbol_map.py`
    （`docs/ci/01_CHECKS.md:213-219`）
  - 2 个 = 实际是**在册门的内部件**，非孤儿：`quality/check_conclusion_anchors.py`
    由在册 `MODULE-READMES` 的实现 `eng/tools/check_module_readmes.py:35-39`
    以 `importlib` 载进来跑，且「载不进来也判红」（`:37-38` `CONCLUSION_ANCHORS_GATE_MISSING`）
- **在册但派发面在注册表之外（真正的口径缺口）**
  - `eng/ci/polarity_probe.py`、`eng/ci/reconcile_state.py`、`eng/ci/verify_ciqa_report.py`：
    三者在 `eng/ci/checks.json`、`.github/workflows/*`、`eng/ci/steps/*` **全部零命中**
    （`git grep -n "<name>" -- eng/ci/checks.json .github/workflows eng/ci/steps` ⇒ 无输出），
    也无任何 `import` 方（附录 B 第 6 段：`imported-by=NONE`）⇒ **死件**；
    而 `eng/ci/polarity_evidence.json:4` 仍写着 `"generated_by": "eng/ci/polarity_probe.py"`，
    即「一份在册证据由一个从未被跑的东西生成」
  - `eng/ci/select_candidate.py`：由 `.github/workflows/fatduck.yml:60` `python3 eng/ci/select_candidate.py`
    直接调用，注册表零登记 ⇒ 违反 `docs/ci/CI_SPEC.md §2.1`「注册表是唯一派发面」的口径
    （workflow 治理门见 A-12）
  - `eng/ci/ci_repair_round.py`：只被自己的单测 `eng/ci/tests/test_ci_repair_round.py:299` 导入
    ⇒ 有测试、无门位
- **权威依据**：标准05 §5「门禁注册表与实际可执行检查一致：登记即能运行，**孤儿检查器定案（注册、改造或删除）**」
- **现状 → 应为**：现状 = 与「一一对应」的偏差是可核的 3+1+1 件（polarity_probe / reconcile_state /
  verify_ciqa_report / select_candidate / ci_repair_round），外加文档在册性缺口（A-01/A-02）。
  应为 = 逐条定案：`polarity_probe.py`（其证据面仍有下游读者）→ 注册为门或删除并撤证据；
  `reconcile_state.py`、`verify_ciqa_report.py` → 删除或并入 `ci_repair_round` 面；
  `select_candidate.py` → 进注册表（fatduck profile 已存在，单元应带 `inputs` 与 `waivable` 声明）；
  `ci_repair_round.py` → 如实标注为「工具 + 单测」，不称门。
- **置信**：**CONFIRMED**（零命中均带正控：同一命令换 `check_naming_surface`/`select_profile` 得命中）
- **取证命令**：见本条正文 + 附录 B 第 6 段。

### A-12 `SPEC-POLARITY-CONSIST-01`：文档自述「注册项待写入」，注册表却已有该项且缺 step 级证据面

- **对象**
  - `docs/ci/01_CHECKS.md:239` 「**注册项待写入**：按本文 §4「新增检查项流程」第 1 步，登记 ID
    `SPEC-POLARITY-CONSIST-01`（…）；`eng/ci/checks.json` 属本任务文件域之外（并发轮次占用），
    故本轮只落地实现 + 自检 + 本行登记说明」
  - `eng/ci/checks.json` 实存顶层项 `SPEC-POLARITY-CONSIST-01`（`waivable: true`，两个 step 无 `inputs`/无 `outputs`）
  - `docs/ci/01_CHECKS.md:230` 另一处同类：「`CHK-REGISTRATION-ANCHORS` | **实现已落地，注册项待写入**」
- **权威依据**：标准05 §5「每条门禁对应一条文档口径…映射关系登记」；§6「结果纪律」
- **现状 → 应为**：现状 = 「先落注册表、后补文档」成为既成事实，且 `docs/ci/01_CHECKS.md` 里的
  「待写入」字样与注册表互相矛盾；负责双向一致的门（A-01/A-02）因只比顶层 ID × §2 表首列而看不见它。
  应为 = 该 ID 的文档行补齐（含 P 级、判据对象、正负例），并把 `waivable` 与 step 的 `outputs`
  按 §2 表声称的等级对齐；`CHK-REGISTRATION-ANCHORS` 的「待写入」状态同样需要一次收口。
- **改法**：并入 A-01 的 §2 表补行；新增一致性断言「文档内 `注册项待写入` 字样 ⟹ 该 ID 不得出现在注册表」
  （由 `check_registry_doc_sync.py` R6 承担）。
- **置信**：**CONFIRMED**
- **取证命令**：`git grep -n "注册项待写入" -- docs/ci/01_CHECKS.md`；
  `git grep -c "SPEC-POLARITY-CONSIST-01" -- eng/ci/checks.json`（>0 命中）。

### A-13 Q2 总答：在册派发脚本 167 个，读「真实产品/真实数据」的是少数；主体读文档、源码文本与自身配置

- **对象（普查脚本 `census6.py`，输出 `inventory/AUD-501/census6.txt`）**：
  对 167 个被注册表 `command` 派发的脚本做 AST 字符串面提取，按对象类别归类：
  | 读的类别 | 脚本数 | 说明 |
  |---|---|---|
  | `run/**` 产品面 或 `实验/testdata/gaia` | **43** | 唯一能构成「产品级证据」的一类 |
  | `docs/**` / 四份根规范 md / `AGENTS.md` | 98 | 文档文本一致性面 |
  | `lib/**`、`*.cpp/h` 源码文本 | 77 | 静态代码面（读源码字符串，不读运行结果） |
  | 自身配置 `eng/ci/**`、`eng/contracts/**` | 58 | 合同/注册表自审面 |
  | 夹具 `fixtures/`、`fake_`、`sample_` | 20 | 门的自造夹具 |
  | `eng/tests/**` 测试源码 | 39 | 以测试面为对象 |
  | 无任何路径字面量 | 33 | 多为 `run_checks.py --check` 递归派发壳、unittest 壳 |
- **权威依据**：标准05 §2「判据读取真实对象（产品文件、运行结果、注册表、合同）」
- **现状 → 应为**：现状 = 门禁体系的**主体**是「文档↔注册表↔源码文本」三方一致性裁判（这是它设计上的定位，
  与 `docs/ci/CI_SPEC.md §1` 三问中的第 1 问对齐），只有 43 个脚本触到真实产品/真实数据。
  问题不在"少"，在于**对外口径**：`03_GATES.md §5`「全部 P0 绿 + P1 绿 → 门禁通过 → 打包发布候选」
  把文本面门与产品面门混在同一个「绿」里，下游据此得到「产品正确」的授权，而绝大多数门从未读过产品。
  应为 = 门禁清单按「对象类别」分层声明（文本一致层 / 构建层 / 产品层 / 科学层 / 视觉层），
  发布门的"绿"必须按层取值，产品层为零时不得合成"发布可用"。
- **改法**：给每个单元增 `object_class` 字段（product/doc-text/source/contract/fixture），
  由 `validate_registry.py` 校验其 `inputs` 声明与 `object_class` 一致（见 A-14）；
  `03_GATES.md §5` 的通过定义改为分层定义。
- **置信**：**CONFIRMED**（普查数字可复算）；**普查工具自身的已知盲区**见「诚实边界」E-3
  （`(REPO / "run").glob(...)` 这类分段拼路径会被漏记，故 43 是下界——
  已用精读纠正一例：`eng/ci/check_product_contract.py:496` 确实读 `run/*/out` 产品树）
- **取证命令**：`PYTHONIOENCODING=utf-8 python 独立审计/工具脚本/AUD-501/census6.py`；
  `git grep -n "out_root in sorted" -- eng/ci/check_product_contract.py`。

### A-14 Q3 之一「输入对象数为 0 即绿」：runner 侧只对 10/370 单元成立；checker 侧抽样精读的 5 例**全部有**零面对守卫

- **对象（runner 侧）**：`eng/ci/checks.schema.json:94`（`inputs` 语义：「以 '/' 结尾者为扫描面目录，
  须递归非空；含通配符者 glob 展开须 >0 命中。缺失/空面 ⇒ FAIL(inputs_missing)/FAIL(inputs_empty)」）；
  `eng/ci/run_checks.py:331-350`（判定实现，注释第 339 行指向单一事实源）；
  `eng/ci/run_checks.py:610-613`（执行前调用）；`eng/ci/declared_inputs.py`（唯一实现，被 run.py/run_checks.py 共用）；
  普查：声明 `inputs` 的单元 **10/370**（`DEEP-COMPLEXITY`、`CTEST-REGISTRATION`、`WORKFLOW-REGISTRY-BINDING`、
  `SEAM-FOOTPRINT-SELFTEST`、`RENDER-VIS-V6-SELFTEST`、`ACTIONS-LOCK-OFFLINE/ONLINE/SELFTEST`、
  `WORKFLOW-GOVERNANCE`、`WORKFLOW-GOV-SELFTEST`），`optional_inputs` 仅 1 个单元
- **对象（checker 侧，逐个精读）**：
  - `eng/ci/check_frozen_gate.py:221-224` `if not paths: print("FROZEN_GATE_REPLAY_FAIL: 回放语料为空…fail-closed"); return EXIT_UNAVAILABLE`
  - `eng/ci/check_product_contract.py:504` `"PRODUCT-CONTRACT_FAIL: 无任何语料（fail-closed）"` ＋ `:524` T1「语料中不存在任何交换对象文档」
  - `eng/ci/check_config_defaults.py:76-77` `if not templates: raise gc.GateError("ANCHOR_MISSING: …templates/*.json")`
  - `eng/tools/quality/contracts/check_science_units.py`（`sci_docs` 零命中 ⇒ `SCI-UNITS-NO-DOCS` finding，注释标 GAP-027「扫描面零命中即空转绿」处置）
  - `eng/ci/check_workflow_governance.py`（W1「扫描面为空（0 个 workflow 文件）⇒ 判红」）
- **权威依据**：标准05 §3「关键口径缺失、输入不满足前提…判失败（fail-closed）」；`CI_SPEC.md §9`（GATE-SOLID-01）
- **现状 → 应为**：现状 = 缺陷类「空扫描判绿」在**检查器实现内**已被逐个堵住（抽样 5/5 命中守卫，
  且都带具名 fail-closed 判词），但**注册表层**的 `inputs` 声明覆盖 2.7% ⇒
  runner 无法独立复核「这一步到底扫了几个对象」；一旦某检查器把自己的守卫改掉，
  只有那 10 个单元会被 runner 抓到。应为 = `inputs` 成为 P0/P1 单元的必填字段，
  由 `validate_registry.py` 加规则「非 waivable 单元必须声明 `inputs` 或声明 `object_class: self-test`」。
- **改法**：先给 43 个读产品面的单元补 `inputs`（它们才是"产品级证据"的承载者），再全量铺开；
  普查文本面 `census7.py` 的 33 个"无守卫文本命中"清单作为精读工作清单（**不得当判据使用**，
  因 A-14 已证该文本普查有误报：`check_frozen_gate.py`/`check_config_defaults.py` 均在列而实际有守卫）。
- **置信**：**CONFIRMED**（覆盖缺口）＋ **REFUTED**（"检查器普遍缺零对象守卫"这一常见推断，抽样不成立）
- **取证命令**：`census2.py` 的 `UNITS_WITH_inputs`；`git grep -n "回放语料为空\|无任何语料\|ANCHOR_MISSING" -- eng/ci`。

### A-15 Q3 之二「结论写成源码字面量 / 探针没抓到就 return 0」：AST 精度普查给出 11 + 19 个候选，逐类精读后多数为反证，唯 `_ > 50` 型阈值成立

- **对象（普查，`census5.py`，AST 而非文本行）**：
  - 硬编码判定字面量 **11 处**：`eng/ci/gate_trust.py:57` `VERDICT_PASS = "PASS"`（三态词汇定义，非结论）、
    `eng/ci/declared_inputs.py:31` `STATE_OK = 'ok'`（同上）、
    `eng/ci/run.py:1271,1274` `verdict = "PASS"`（在 `hard_fail or known_fail ⇒ FAIL` 之后，见 A-08 行锚 1267）、
    `eng/tools/quality/contracts/{check_api_contracts.py:47, check_doc_symbols.py:132,
    check_execution_contracts.py:121, check_forbidden_patterns.py:17, check_science_units.py:17,
    check_test_contracts.py:17, check_traceability.py:21}` `status = "PASS"`
  - `if not X: return 0 / exit(0)` 型 **19 处**：其中真为「零对象即绿」的只有
    `eng/ci/resource_monitor.py:46`（`if not argv: return 0`，用法面）、
    `eng/tools/monitoring/resource_probe.py:115`（`if not text -> 0`，解析面）、
    `eng/tools/run_gc.py:273`（`if not RUN.is_dir() -> 0`，GC 工具非门）；
    其余 16 处是谓词函数的 `return False`（`_DECL_PREFIX_RX.match`、`isinstance` 判定等），语义正确
- **权威依据**：标准05 §2「恒真断言、空断言……判为无效门」；§6「性能门真实判红」
- **现状 → 应为**：现状 = 该两类形态在**本仓门禁实现里总体不成立**（`status = "PASS"` 是被 findings 下修的
  累加初值，精读 `check_doc_symbols.py`：`if not ns_ok: status = "FAIL"` 紧随其后）；
  真正的"抄来的阈值"命中点是 A-04（`failclosed_survey.py:201` 的 `units > 50`）与
  `03_GATES.md`/`CI_SPEC.md` 把归档普查数（230/226/4）当现状数陈述（A-05）。
  应为 = 保留现有 findings 累加写法；把普查阈值改为与注册表实算相等。
- **置信**：**CONFIRMED**（11/19 候选逐条归类完成）＋ 对「普遍硬编码结论」前提 **REFUTED**
- **取证命令**：`census5.py` 第 (2)(3) 段；`git grep -n -A3 'status = "PASS"' -- eng/tools/quality/contracts/check_doc_symbols.py`（正控：证明其后紧跟下修）。

### A-16 Q3 之三「正负例纪律」：124 个 `check_*.py` 里 35 个连 `--self-test` 面都没有；有 self-test 的 89 个中仅约 4 个走 `gate_trust` 的类型化判据

- **对象**
  - `census5.py` 第 (4) 段：`checker scripts WITHOUT --self-test surface = 35 of 124`
    （含在册的 `eng/ci/check_workflow_governance.py`（用 `--selftest` 拼写，故部分为普查口径问题，见 E-2）、
    `eng/tools/check_abi_boundary.py`、`check_api_docs.py`、`check_ast_api.py`、`check_contract_graph.py`、
    `check_data_artifacts.py`、`check_duplication.py`、`check_p3_status.py` 等）
  - `eng/ci/gate_trust.py:43-59`（`KIND_DISCRIMINATING / KIND_PROTECTIVE / KIND_EXEMPTION / KIND_CONTENT`
    与 `EXIT_TRUST = 3`）；`eng/ci/gate_trust.py:92 def emit(cases, *, tool, json_out=None, ...)`
  - 使用者仅 4 处：`eng/ci/run_checks.py`、`eng/tools/quality/check_block_flow_conformance.py`、
    `check_block_flow_ports_vs_code.py`、`eng/tools/quality/contracts/check_build_graph.py`
  - 反面纪律已在 runner 侧立起来：`eng/ci/run_checks.py:1330`「kind=exemption 的用例**落在 runner 自身的豁免分支内**」
    ＋ `:1439-1477` 六条 exemption 正负例（waivable skip 准许 / 非 waivable skip 拒绝 / 静默单元未 blanket /
    已登记单元按 ID 生效 / 平台 skip 计数 / `platform: any` 不得 skip）
- **权威依据**：标准05 §2「正负例随判据提供」；§7 第 2 项「门禁清单：…判据、正负例…」
- **现状 → 应为**：现状 = 「负例不落自己的豁免分支」这条纪律在**runner 自身**做到了机器化，
  但没有一条门要求**其它 124 个检查器**采用同一 `gate_trust.emit` 面 ⇒ 35 个零负例、85 个自带 `--self-test` 的
  检查器各写各的正负例格式（`SELFTEST_PASS`/`OK`/`verdict=PASS` 混用），
  runner 只能靠 `TRUST_DECLARED_RE`（`run_checks.py:417`）的字面量正则识别「门自述不可信」。
  应为 = 把 `gate_trust.emit` 定为唯一正负例出口，`validate_registry.py` 加规则：
  在册 P0/P1 检查器必须 `import gate_trust` 且其 `--self-test` 必须输出含 `kind` 字段的 JSON。
- **改法**：按危害排序先补 35 个无负例门中最强的"写结论类"门；统一 `--self-test`/`--selftest` 拼写。
- **置信**：**CONFIRMED**（计数可复算；普查口径缺口见 E-2）
- **取证命令**：`census5.py` 第 (4) 段；`git grep -ln "gate_trust" -- eng/ci eng/tools`。

### A-17 Q5：SKIP 的实际使用面——`exit 77` 合同只在 waivable 单元被承认（正例），但 `SKIPPED(platform)` 在 `run_checks.py` 侧不阻断 rc=0（见 A-08），且 ctest 侧有 12 条登记在案

- **对象**
  - `eng/ci/run_checks.py:95-99` `SKIP_EXIT_CODE = 77`…「**只在执行单元 waivable=true 时被承认**；
    非 waivable 的 77 = 自我豁免 ⇒ FAIL」（实现在 `:1909-1911` 的 `V_SKIP_WAIVABLE` 分支与 `probe_prerequisite` 分支）
  - `eng/ci/ctest_skip_register.json`：`skip_count` 字段 + `skips` 数组 **12 条** + `open_items` **3 条**
  - `eng/ci/run_checks.py:136-138` `FAIL_VERDICTS`（三种 `SKIPPED*` 全部不在内）
  - `eng/ci/run_checks.py:1994` `"skipped_platform": counts[V_SKIP_PLATFORM]`（计数落盘，可见但**不阻断**）
  - `eng/ci/run_checks.py:2042-2045`（optional_input skip 会打印「skip 不是 pass」提示语）
- **权威依据**：标准05 §6「不把 SKIP 计入通过；零豁免、零跳过的结论以实际输出为准」
- **现状 → 应为**：现状 = 计数与告知面做到了（每条 skip 都分类计数、打印、进 JSON），
  裁决面没做到：`verdict = "FAIL" if failures else "PASS"` 使"全部 skip"的运行仍写 `verdict=PASS` 且 rc=0；
  `run.py` 同情形判红（1256-1266）。应为 = 两入口统一为「零有效执行 ⇒ 红」，
  并在结果 JSON 顶层加 `zero_skip: true|false`，让"零跳过"结论有机器面（当前要读者自己看 counts）。
- **置信**：**CONFIRMED**（见 A-08 行锚链）
- **取证命令**：`git grep -n "SKIP_EXIT_CODE" -- eng/ci/run_checks.py eng/ci/run.py`；
  `git grep -n "\"skipped_platform\"\|skipped_waivable" -- eng/ci/run_checks.py eng/ci/run.py`。

### A-18 崩溃与判红分离面在两个 runner 都成立（正例，保留）；缺口是「显式声明崩」无注册表侧承载

见 A-09。补充：三个退出码族在两侧一致（`EXIT_OK=0 / EXIT_FAIL=1 / EXIT_RUNNER_ERROR=2 / EXIT_CRASH=3`，
`eng/ci/run_checks.py:86-93`、`eng/ci/run.py:54`、`eng/ci/gate_trust.py:36-39`），
且 `run_checks.py:1892-1896` 显式拒绝把崩塌平成 FAIL（"塌平会让红从哪来在条目级不可见"）。

### A-19 派发面分裂：`fatduck.yml` 在注册表之外直接执行决策脚本 `eng/ci/select_candidate.py`

- **对象**：`.github/workflows/fatduck.yml:56-72`（`set -euo pipefail` 后 `python3 eng/ci/select_candidate.py …`，
  取 `rc=$?`，`if [ "$rc" -eq 3 ]` 分支决定 Windows 候选缺失是否失败）；
  `eng/ci/checks.json` 内 `select_candidate` **零命中**；
  `eng/ci/validate_workflow_binding.py:46` B16「范围外 workflow 文件（如 fatduck.yml）逐条进 notices，
  但仍参与 B12 反复制扫描」；`.github/workflows/ci-linux.yml:4-6`（「本文件 run: 体只允许三类调用」自订规则，
  该规则对 fatduck.yml 无机器承载）
- **权威依据**：标准05 §5「门禁注册表与实际可执行检查一致：登记即能运行」；§2「判据值可由独立读者推导复算」；
  `docs/ci/CI_SPEC.md §2.1`（注册表是唯一派发口径）
- **现状 → 应为**：现状 = 一条能决定"候选是否成立"的判据（rc=3 ⇒ 失败）跑在注册表面外：
  无登记 timeout、无 `outputs` 证据、无 waivable 语义、不被 `CHK-FAILCLOSED-SURVEY` 三面普查覆盖（普查只遍历
  `checks.json`，`failclosed_survey.py:58-67`）、不进 `03_GATES.md` 分级表。
  应为 = 注册为 `fatduck` profile 单元（含 `inputs` 指向 GitHub API 前置与 `optional_inputs`），
  或把 B16 的"范围外"改成"范围内且必须注册"。
- **置信**：**CONFIRMED**（零命中带正控：同命令换 `select_profile` 在 `workflow_binding.json` 命中）
- **取证命令**：`git grep -n "select_candidate" -- eng/ci/checks.json`（0）vs
  `git grep -n "select_profile" -- eng/ci/workflow_binding.json`（>0）。

### A-20 死件三件套：`polarity_probe.py` / `reconcile_state.py` / `verify_ciqa_report.py` 无任何调用方，但前者仍是在册证据的署名生成者

- **对象**：`eng/ci/polarity_probe.py:195`（自带 `subprocess.run(argv, ..., text=True, ...)` 无 `encoding=`，
  即"探针自己跑门"的实现体，说明它原本是一个普查驱动）、`eng/ci/polarity_evidence.json:4`
  `"generated_by": "eng/ci/polarity_probe.py"`、`docs/DOCUMENT_INDEX.yaml` 与
  `eng/ci/ledgers/registration_anchor_ledger.json:77-78` 仍点名 `polarity_probe`
  （`:78` exit_condition：「polarity_probe 落点改到持久面（`artifacts/evidence/**`）并重建登记册，或整表清空重生成后，删除本条目」）
- **权威依据**：标准05 §5「孤儿检查器定案（注册、改造或删除）」；§2「不读真实对象的检查…判为无效门」
- **现状 → 应为**：现状 = 一份被引用为证据的 `polarity_evidence.json` 由一个当前无任何入口的脚本产出 ⇒
  该证据不可再生、不可复核（除非人工想起来直跑它）。`reconcile_state.py`、`verify_ciqa_report.py` 同类（零命中）。
  应为 = 三选一并留痕：注册进 `checks.json`（`polarity_probe` 的普查面正是 A-03 缺的那一层——
  逐门真跑命令的判别力普查，建议改造为在册门）、或明确退役（按 §2.1 退役契约：无参打印标识 exit 2）、
  或删除并把其证据面一并撤下。
- **改法**：优先「改造」：把 `polarity_probe.py` 的探针面接进 `mutation_gates.json` 体系，
  与 `check_mutation_gates.py`（在册 `CHK-MUTATION-GATES`）合流，形成"逐门缺陷注入"普查，
  补 A-03 指出的那一层空白。
- **置信**：**CONFIRMED**（零命中结论带正控：`git grep -l "polarity_probe"` 命中面全部是台账/证据自述，无调用点）
- **取证命令**：`git grep -n "polarity_probe" -- eng/ci/checks.json .github/workflows eng/ci/steps`（0 命中）、
  `git grep -n "polarity_probe" -- eng/ci/*.py`（0 命中，除其自身）。

### A-21 CTest 注册面配对：236 个可静态枚举的 `add_test` 目标中 235 个落在闭包内；1 个例外暴露「命令侧正则 / 登记侧 glob」两种方言无门调和

- **对象**
  - 静态枚举面：`eng/tests/**/CMakeLists.txt` 的 `add_test(NAME …)`，236 个可枚举名，0 处生成器表达式
  - 登记面：`eng/ci/checks.json` 的 `ctest_targets` 共 280 条模式（145 个 distinct）＋
    `eng/ci/ctest_baseline.json` `targets` 161 条冻结存量 ⇒ 并集按 `fnmatch` 匹配 235/236
  - 唯一未被匹配者：`eng/tests/unit/v6_p1_drz/CMakeLists.txt:44`
    `add_test(NAME drizzle_pf_sb_v6_area_invalid COMMAND v6_p1_drz_test geometry)`
    （它由 `eng/ci/checks.json:10014` 的登记模式 `drizzle_pf_sb_*` 在 fnmatch 下**可以**覆盖，
    但同一单元 `CHK-DRZ-PF-AREA-S1` 的**命令侧**用的是正则方言：
    `eng/ci/checks.json:9995` `"--target", "drizzle_pf_sb.*"`，
    经 `eng/tools/quality/deep_ci_driver.py:356` `_ctest_argv(["ctest", "-R", "^%s$" % target, …])` 组装）
  - 判据实现：`eng/tools/quality/check_ctest_registration.py:15-21`
    C3（未覆盖目标判红）/ C4（登记模式匹配不到任何现存目标判红，防陈旧注册）/
    C6（**无 glob 字符**的 `ctest_targets` 名必须出现在该检查项 command 全文）
- **权威依据**：标准05 §5「门禁注册表与实际可执行检查一致：登记即能运行」；§2「判据值可由独立读者推导复算」；
  `checks.schema.json:121`（`ctest_targets` 语义：「精确名或 shell glob」）
- **现状 → 应为**：现状 = 闭包本身几乎完整（235/236），但 C6 的"必须出现在 command"检查
  **只对无 glob 字符的名字生效** ⇒ 一旦登记侧与命令侧都写模式，两侧可以各说各话：
  登记侧 `drizzle_pf_sb_*`（fnmatch）与命令侧 `drizzle_pf_sb.*`（正则，被 `^…$` 包裹）今天恰好覆盖同一批目标，
  若将来出现 `drizzle_pf_sbX…`（无下划线）这类名字，两侧覆盖集会分叉而 C3/C4/C6 全部无感。
  应为 = C6 扩展到"含 glob 字符的模式也须与 command 侧的 target 表达式做集合等价校验"
  （两侧各自的匹配语义都要在门里实现一遍，然后比较命中的目标集合是否相等）。
- **改法**：`check_ctest_registration.py` 增 C7：对每个登记了 `ctest_targets` 且 command 含
  `--target <pat>` 的单元，分别按 glob 与正则展开现存目标集，差集非空即判红并打印两侧集合。
- **置信**：**CONFIRMED**（236/235 与方言分叉均由静态复算得；`ctest -R` 实际命中数 **需复测**，
  因构建树未生成，本阶段禁跑）
- **取证命令**：附录 B 第 7 段（`add_test` 枚举 + `fnmatch` 并集复算脚本）。

### A-22 【反证 + 保留】ctest 族的「零命中即绿」已按 GAP-027 机器化堵死，且判据是非退化的

- **对象**：`eng/tools/quality/deep_ci_driver.py:344-348`（docstring 记录原缺陷：
  「构建树未 configure 或目标未注册时 `ctest -R` 打印 "No tests were found!!!" 且 **rc=0**，
  本驱动据此静默 PASS（46 条 CTEST-<TARGET> 门 + CTEST-LINUX-FULL 同族恒绿）」）；
  `:352` `matched, probe_reason = _ctest_match_count(...)`；`:361-363`
  `zero_hit = matched == 0; if zero_hit and rc == 0: rc = 1`；`:364-368` 打印 `matched_tests` 与 verdict
- **权威依据**：标准05 §2「判据必须非退化」；§3 fail-closed
- **现状 → 应为**：现状 = 成立（这是全仓**最重要**的一类守卫，因为它覆盖 46+ 个执行单元）。
  应为 = 保留；并把它作为"其他 345 个 linux-main 单元"的守卫模板（当前只有声明 `inputs` 的 10 个单元享受 runner 级同类守卫）。
- **置信**：**CONFIRMED**（读实现体；实际 `matched_tests>0` **需复测**）
- **取证命令**：`git grep -n "_ctest_match_count" -- eng/tools/quality/deep_ci_driver.py`。

### A-23 性能门是真实判红（非"只记录不裁决"），且自带可执行负例面

- **对象**
  - `eng/tools/quality/check_resource_gate_real.py:73-83`：两态断言
    `cases = [("busy","pass",0), ("serial","fail",10)]`，对每态**同时**要求监控 rc 与 `frozen_gate.verdict` 相符
    （`:80-83`），任一不符 ⇒ `RESOURCE_GATE_REAL_FAIL` + rc=1（`:85-89`）
  - `:66-69` 依赖缺失 ⇒ rc=2（fail-closed，不当作绿）
  - 裁决面：`eng/ci/l2_frozen_gate.py::adjudicate`（被 `check_frozen_gate.py`、`monitor_evidence.py` 共用）、
    阈值唯一数值源 `eng/contracts/resource_gate_v1.json::compute`（`docs/ci/CI_SPEC.md §9.2`）
  - 回放面（负例证据）：`eng/ci/check_frozen_gate.py:213-248`
    `if hist == "pass" and verdict["verdict"] != L.V_RED: failures.append("历史 pass 且违规证据未被判红（恒真门残留）")`
- **权威依据**：标准05 §6「性能门真实判红：利用率、内存、热点等指标越界即判红，不只记录不裁决」
- **现状 → 应为**：现状 = 成立。应为 = 保留 `RESOURCE-GATE-REAL` / `-NEG` /
  `L2-FROZEN-GATE-SELFTEST` / `L2-FROZEN-GATE-REPLAY` 四条；
  唯一缺口同 A-14：这组面的输入声明（`inputs`）都没写，靠实现内部守卫，注册表面看不到。
- **置信**：**CONFIRMED**（读实现体；两态实测值 **需复测**）
- **取证命令**：`git grep -n "want_rc\|want_verdict" -- eng/tools/quality/check_resource_gate_real.py`。

### A-24 派发集中度：31% 的执行单元共用同一个驱动脚本，单点缺陷即批量假绿

- **对象**：`census1.py` 入口点普查——`eng/tools/quality/deep_ci_driver.py` 作为命令目标出现在 **115** 个执行单元；
  `eng/ci/resource_monitor.py` 16 个；`eng/ci/wf_step.py` 3 个；其余按脚本各 1-4 个
- **权威依据**：标准05 §4「并行执行的判据互不污染采样」；§2 判据有效性（单点 ⇒ 一次误判放大 115 倍）
- **现状 → 应为**：现状 = 驱动本身带 fail-closed 修正（A-22），但注册表与门禁清单没有"共享驱动"这一维度，
  因此下游读不到"这 115 条绿其实是同一条代码路径给的"。应为 = 门禁清单增 `shared_driver` 字段，
  统计口径按驱动去重后再报"独立判据数"，避免用 370 单元冒充 370 个独立裁判。
- **置信**：**CONFIRMED**
- **取证命令**：`census_registry.py` 的 `---COMMAND ENTRY POINTS---` 段。

### A-25 档位纪律只写在文档、无门承载：`fast` 档 194 个单元里 112 个登记超时 >120 s

- **对象**
  - `docs/ci/CI_SPEC.md §2.6` 表：`fast`（默认）「秒级一致性门：静态/文档/合同/治理/轻量单测」目标墙钟 **≤120 s**；
    `§2.5`「每个 step 的 `timeout_seconds` ≤ max(60, 3 × 最近一次实测墙钟)」
  - 注册表实算（附录 B 第 8 段）：
    `fast` units=194，登记超时合计 53,160 s，最大 1,200 s，**>120 s 的 112 个**，>600 s 的 10 个
    （`E2E-WCS-CLOSURE-REPRO-SELFTEST` 1200、`TRUTH-CONCLUSION-GATE` 900、`V6-NEGATIVE-MUTATION` 900 等）；
    `integration` units=9；`linux-main` 345（合计 216,180 s，最大 9000 s）；`prerelease` 64（最大 20000 s）
  - 唯一在跑的机制是 `changed` 档的墙钟预算（`eng/ci/run_checks.py:1936-1945` `BUDGET_EXCEEDED`），
    `--all`/`--check` 不受任何预算约束
- **权威依据**：标准05 §4「按耗时分档：快速档支撑高频回归…档位选择与触发规则显式定义」
- **现状 → 应为**：现状 = 分档靠 profile 成员身份声明，无门校验"fast 档单元必须是秒级"，
  也无门校验 §2.5 的 `timeout ≤ 3×实测`（注册表里没有任何实测墙钟字段）。
  应为 = 结果 JSON 已有 `duration_seconds`，加一条门：读最近一次 CI 结果，断言 fast 档单元墙钟分布
  满足档预算，超档单元必须改档或降判据（而不是把 timeout 写大）。
- **置信**：**CONFIRMED**（登记值实算）；「实测墙钟是否真超档」**需复测**（登记超时是上界，可能远大于实测）
- **取证命令**：附录 B 第 8 段。

### A-26 最强的一条「文档字符串冒充验证」：`lib/phase1_session/README.md` 点名的机器判据，其判据对象已被删除且脚本从未注册

- **对象**
  - `lib/phase1_session/README.md:204-205` 「符号映射完整性：`eng/tools/check_p1_symbol_map.py`
    （p1_session.h 导出 API ↔ 归档映射表 P1_SYMBOL_MAP.md（**已删，见 git 历史**））」
  - `eng/tools/check_p1_symbol_map.py:13` `MAP = (REPO / "docs/refactor/P1_SYMBOL_MAP.md").read_text(encoding="utf-8")`
    （**模块级**读取，对象缺失即在 import 期抛 `FileNotFoundError`，`main()` 永不到达）
  - 对象存在性（按跟踪集＋删除历史判定，不用磁盘存在性）：
    `git ls-files docs/refactor` ⇒ 空；`git log --diff-filter=D -- docs/refactor/P1_SYMBOL_MAP.md`
    ⇒ `b7b2dea7 docs(governance): GOV-002 归档非当前工程文档`
  - 注册面：`git grep -c "check_p1_symbol_map" -- eng/ci/checks.json` ⇒ **0 命中**
  - 分类冲突：`docs/ci/01_CHECKS.md:210-212` 把它列入「**应删除（3）——非检查器（无判据、无退出码语义）**」，
    而文件本体 `eng/tools/check_p1_symbol_map.py:20-40` 明写三条判据并 `return 1`/`return 0` ⇒ 该分类不成立
  - `docs/ci/01_CHECKS.md:217-219` 另记：「`check_p1_symbol_map.py` 被 `lib/phase1_session/README.md` §7
    点名为符号映射完整性的机器判据…两者处置需先动 `lib/**`，超出本项文件域」⇒ 已知未决，但**README 至今仍作现在时陈述**
- **权威依据**：标准05 §5「每条门禁对应一条文档口径…映射关系登记」；§2「不读真实对象的检查…判为无效门」；
  负责人既有口径「删整段、改写措辞都不算证据；判已修要看被点名的行本身」
- **现状 → 应为**：现状 = 模块级 README 向下游（读者、审计者、agent）声明"该模块的符号映射完整性有机器判据"，
  而该判据脚本 (a) 从未在册、(b) 一跑就抛、(c) 判据对象已不在库内 ⇒ **三重虚假授权**。
  应为 = 三选一当场定案并留痕：① 若符号映射不再是当前工程对象（GOV-002 已把它归档），
  则删 `README.md:204-205` 两行并删脚本（同步订正 `docs/ci/01_CHECKS.md` 的分类理由）；
  ② 若仍需该完整性面，则按归档后的新对象重写判据并注册；
  ③ 无论哪条，都不得保留"现在时点名 + 已删对象"的组合。
- **置信**：**CONFIRMED**
- **取证命令**：本条正文四条命令；`git grep -n "check_p1_symbol_map" -- lib docs eng` 全量命中面。

### A-27 Q3 之四「`--self-test` 负例不落该门自己的豁免分支」：runner 门自身做到了，其余检查器无承载

- **对象**：`eng/ci/run_checks.py:1330`（设计说明「kind=exemption 的用例**落在 runner 自身的豁免分支内**」）、
  `:1439-1477` 六条 exemption 用例（含 `exemption_neg_silent_ok_units_not_blanket`
  与 `exemption_pos_silent_ok_units_is_id_scoped`，分别用 `NOT-IN-SILENT-OK-UNITS` 与 `API-DOCS`
  两个 ID 走同一段判据的两侧 ⇒ 证明豁免分支按 ID 生效、不是全局兜底）；
  对照 A-16：全仓 128 个跟踪的 `check_*.py` 中，把正负例走 `gate_trust.emit`（类型化 `kind` + `EXIT_TRUST=3`）
  的仅 4 个工具（`run_checks.py`、`check_block_flow_conformance.py`、`check_block_flow_ports_vs_code.py`、
  `contracts/check_build_graph.py`）
- **权威依据**：标准05 §2「正负例随判据提供」、§6「红灯不以 waiver 静默覆盖」
- **现状 → 应为**：现状 = 该形态在 `CHK-GATE-FAILCLOSED-SELFTEST` 上**反证**（做到了），
  在其余 85 个自带 `--self-test` 的检查器上无机器承载（各写各的输出格式，
  runner 只能靠 `run_checks.py:417` 的 `GATE_TRUST_FAIL|SELFTEST_FAIL` 字面量识别"门自述不可信"）。
  应为 = `gate_trust.emit` 定为唯一出口，`validate_registry.py` 校验在册门必须产出带 `kind` 的用例 JSON。
- **置信**：**CONFIRMED**（覆盖面）＋ **REFUTED**（"runner 门自己也做不到"这一猜测）
- **取证命令**：`git grep -n "exemption_" -- eng/ci/run_checks.py | head -20`；`git grep -ln "gate_trust" -- eng`。

### A-28 Q3 之五「把已作废模式硬锁成约束（改对反而判红）」：抽样两条均成立为"带显式逃生口的约束"，未见反向判红陷阱

- **对象**
  - `eng/ci/check_psfsw_retired.py:26-29` `TOKEN = "psfsw_robust_weight"`、
    `RETIRED_MARKERS = ("已退役", "retired")`；判据 = 扫描根内出现 TOKEN 且该行不含标记 ⇒ 红；
    `:12-13` fail-closed：扫描根缺失 ⇒ rc=2、不可读文件 ⇒ rc=2
  - `eng/ci/check_retired_code.py` ＋ `eng/ci/retired_code_allowlist.json`（allowlist 承载"合法再现"）
- **权威依据**：标准05 §2 判据非退化；§3「降级只在显式、可溯…时合法」
- **现状 → 应为**：现状 = 约束对象是"已作废符号不得无标注复现"，逃生口是**同行标注**或**登记 allowlist**，
  两者都显式可溯 ⇒ 未发现"改对反而红"。残留弱点：同行 `# retired` 注释即可放行，
  一处误标即静默失效，且 `RETIRED_MARKERS` 含中英文两串、由同一文件的字面量决定，无合同承载。
  应为 = 保留判据；把标记语义挪到 `eng/contracts/` 的一等公民（带 schema），并禁止"仅注释放行"改为
  必须落在 allowlist 登记面。
- **置信**：**CONFIRMED**（抽样 2 条）；其余同族门 **未逐条精读**（见覆盖率自报）

---

## 3. 旧门禁处置清单（标准05 §7 第 3 项格式）

判定口径：只对**本审计实际读过实现体或读过其判据面**的对象下处置结论；未精读者归入「待定案」。

| # | 对象 | 处置 | 理由（可回指条号） |
|---|---|---|---|
| D-1 | `RESOURCE-GATE-REAL` / `RESOURCE-GATE-REAL-NEG` | **保留** | 两态非退化、真起监控、负例断言 rc 与 verdict 双件（A-23） |
| D-2 | `L2-FROZEN-GATE-SELFTEST` / `L2-FROZEN-GATE-REPLAY` | **保留** | 零命中即红（`check_frozen_gate.py:221-224`）＋"历史 pass 未判红即失败"（`:245-248`） |
| D-3 | `PRODUCT-CONTRACT`（`check_product_contract.py`） | **保留** | 读 `run/*/out` 真实产品树（`:496`）、无语料即红（`:504`）、T1/T2 判据回指 DATA-002 |
| D-4 | `CHK-TRUTHFUL-CONCLUSION` 三单元（GATE/SELFTEST/INJECT） | **保留 + 补登记** | 现算复算（`:319-325`、`:398`）与注入面（`:621-673`，要求"目标红且其余绿"）齐备；缺 §2 文档行（A-01） |
| D-5 | `deep_ci_driver` 的 `ctest-target` 零命中守卫 | **保留 + 推广** | GAP-027 处置正确（A-22），但只覆盖 ctest 族 |
| D-6 | `CHK-GATE-FAILCLOSED-SELFTEST`（runner 自测面） | **保留** | 唯一做到"负例落自己豁免分支"的门（A-27） |
| D-7 | `eng/ci/run_checks.py` 汇总判定 | **改造** | 零有效执行必须红（与 `run.py:1256-1266` 对齐）；平台 skip 不得撑 rc=0（A-08） |
| D-8 | `eng/ci/run.py` | **合并** | 与 D-7 同一条判定提取共享（继承面 `INHERIT_FIELDS` 亦须共享），保留两份必然漂移（A-07/A-08） |
| D-9 | `CHK-FAILCLOSED-SURVEY` | **改造** | 如实改名为"runner 证据管道普查"，并另建逐门缺陷注入面（A-03）；`units > 50` 改为实算相等（A-04） |
| D-10 | `eng/ci/polarity_probe.py` | **改造后注册** | 其"逐门真跑探针"正是 D-9 缺的那一层（A-20），且 `polarity_evidence.json` 依赖它的署名 |
| D-11 | `CHK-REGISTRY-DOC-SYNC` | **改造** | 覆盖到 step 级（A-02）、加 R6"注册项待写入⟹不得在册"（A-12）、加文档计数==实算断言（A-05） |
| D-12 | `eng/ci/validate_registry.py` | **改造** | 加规则：P0 行 ⟹ `waivable=false`（A-10）；非 waivable 单元 ⟹ 必填 `inputs`（A-14）；在册门 ⟹ 走 `gate_trust.emit`（A-27）；C7 调和 `--target` 正则与 `ctest_targets` glob（A-21） |
| D-13 | `eng/ci/checks.schema.json` | **改造** | step 的 `required` 补齐 `waivable/mutates_workspace/outputs`，或明确写"step 靠继承"并把继承校验进门——现状是 schema 只作装饰，无人校验（A-07） |
| D-14 | `docs/ci/03_GATES.md §6.2/§6.3/§6.4` 与 `artifacts/evidence/release-05/FAILCLOSED_SURVEY.md` | **改造** | 数字由生成器现算或删数（A-05）；普查证据落位从 release-05 固定目录改为轮次面 + 证据锚 |
| D-15 | 157 个零 `outputs` 单元中的"写结论类"子集 | **改造** | 至少 `TRUTH-CONCLUSION-GATE`、`KNOWN-FAILURES-BASELINE-*` 三条要有结构化证据 JSON（A-06） |
| D-16 | 已按 §2.1 退役的 10 个 `check_*.py` | **保留（不再当门算）+ 订正文档 1 处** | 退役退出契约（marker＋无参 `return 2`）逐个核实为真；但 `eng/tools/check_traceability.py` **无 `--self-test` 面**、复跑入口是位置参数而非 `--legacy-*`，与 `docs/ci/01_CHECKS.md:206-209` 的全称断言冲突 ⇒ 补齐该件负例面，或把文档口径改为逐件如实描述（A-11）；**不得计入在册门总数** |
| D-17 | `eng/ci/reconcile_state.py`、`eng/ci/verify_ciqa_report.py` | **删除（或说明用途）** | 零调用、零注册、零 workflow 引用（A-11/A-20）；若为人工应急工具，须在 §2.1 记为"工具非门" |
| D-18 | `eng/tools/check_traceability_matrix.py`（根级，16 KB） | **保留退役态 + 改名消歧** | 它已按 §2.1 退役（`docs/ci/01_CHECKS.md:204-205`：与在册 `TRACEABILITY-MATRIX` 同对象、RULE-A/B 不在合同判据内；核得 marker＋`return 2`＋`--self-test` 13 处在位），**不是未定案孤儿**；真正的问题是它与在册件 `eng/tools/traceability/check_traceability_matrix.py`（52 KB，不同实现）**同名不同物** ⇒ 改名（如 `check_traceability_matrix_retired.py`）或移入归档目录，杜绝下轮把两者当同一件 |
| D-19 | `eng/tools/check_p1_symbol_map.py` + `lib/phase1_session/README.md:204-205` | **删除两处（同步）** | 判据对象已随 b7b2dea7 删除、脚本 import 期即抛、从未在册（A-26）；`docs/ci/01_CHECKS.md:210-212` 的"非检查器"分类一并订正 |
| D-20 | `eng/tools/quality/check_source_inventory.py` | **改名或移目录** | 名为 check 实为枚举生成器（`main` 有 `return 0` 出口，`:146/:233`），被 `build_v19r2_package.py:99` 历史命令字面引用 ⇒ 按"任一调用方即停"未自处置；应改为 `gen_*` 或迁入 `build_*` 面（A-11） |
| D-21 | `eng/tools/quality/check_p3_rejection_count.py`、`check_task_result_schema.py` | **定案二选一** | 前者只被自身单测引用、后者只被死件 `polarity_probe.py` 引用 ⇒ 要么注册为门，要么与单测合并后删门壳（A-11） |
| D-22 | `eng/ci/select_candidate.py` | **改造：注册进 `fatduck` profile** | 其 rc=3 分支在 `.github/workflows/fatduck.yml:60-72` 直接决定候选成败，却在注册表面外（无 timeout/证据/普查覆盖）（A-19） |
| D-23 | `eng/ci/ci_repair_round.py` | **保留为工具，不计门** | 只有 `eng/ci/tests/test_ci_repair_round.py:299` 一个引用者（A-11） |
| D-24 | 未在册且未退役、亦未被精读的剩余单元 | **待定案** | 本审计未逐条读实现体，不下处置结论（见覆盖率自报） |


---

## 4. 覆盖率自报

**在册规模**：顶层注册项 150 ／ 执行单元 370（step 291）。

| 档 | 数 | 本审计处置 |
|---|---|---|
| 逐条**精读**（读实现体并给行锚）的判据面 | **18** 个对象的实现体：`run_checks.py`（判定/普查/退出码 4 段）、`run.py`（verdict 汇总 + 常量 + 继承缺位）、`failclosed_survey.py`（全文）、`check_registry_doc_sync.py`（全文）、`check_conclusion_truth.py`（头部 + T3/T4 + fault_inject）、`check_frozen_gate.py::replay`、`check_resource_gate_real.py`（全文）、`check_product_contract.py`（语料发现 + 零语料守卫）、`check_workflow_governance.py`（判据头）、`check_psfsw_retired.py`、`check_p1_symbol_map.py`（全文）、`check_traceability_matrix.py`（退役面）、`gate_trust.py`（API 面）、`declared_inputs.py`（薄封装面）、`deep_ci_driver.py::cmd_ctest_target`、`validate_workflow_binding.py`（B16 面）、`incremental.py::input_fingerprint/fingerprint_hit`、`check_ctest_registration.py`（C1-C6 面） | 已给结论（A-01…A-28 / D-1…D-23） |
| 仅**确定性普查**（结构字段/可达性/AST 形态），未读实现体 | 370 单元的全部字段分布、167 派发脚本的读对象分类、128 跟踪 `check_*.py` 的注册/退役/负例面、236 CTest 目标的闭包配对、7 类硬编码/空扫描形态候选清单（11+19+33+35 条） | 普查绿灯**未**当"已审"；每条普查结论都标了工具盲区（E-2/E-3） |
| 未判定（既未精读也无普查覆盖其判据语义） | 150 顶层项中 **132 个的逐条判据正文**（其 step 实现体未读，例如 `CHK-AIO-*`、`CHK-P1*`、`CTEST-P*` 族的断言逻辑、`check_prod_wiring.py` 70 KB、`check_version.py` 41 KB、`check_aio_io_boundary.py` 72 KB、`check_sparse_punch.py` 47 KB、`check_design_clause_wiring.py` 48 KB、`check_retired_code.py` 46 KB 等） | **待定案**，见 D-24 |

即：**已判定 18 个实现体 + 全部注册表字段级事实；仅普查 167 脚本；逐条判据正文未读者 132 个顶层项**。
本审计的"总数/分布/可达性/形态普查"结论是全量的；"某条门判据是否有判别力"的结论只覆盖精读面。

## 5. 我证伪了任务书的哪个前提

1. **「157 个单元的 `waivable`/`mutates_workspace` 字段不完备」不成立**（A-07）：普查报的 2 个"缺失"是 step，
   schema 的 step `required` 本就不含该字段，且 `run_checks.py:155-158/234` 有继承面。
   真实缺陷在别处：继承只在 `run_checks.py` 一侧实现（A-08）。
2. **「检查器普遍缺『零对象即红』守卫」不成立**（A-14）：抽样精读 5 个扫描型门，5/5 都有具名 fail-closed 守卫；
   ctest 族的同类守卫（GAP-027，A-22）覆盖 46+ 单元。缺口在**注册表层 `inputs` 只声明 10/370**，
   不是检查器实现普遍裸奔。
3. **「门的崩溃与判红没有分离」不成立**（A-09）：两 runner + `gate_trust.py` 三处一致用
   `0/1/2/3` 四码分离，`FAIL_VERDICTS` 含 `V_CRASH` 但退出码单独走 3，条目级还显式拒绝把崩塌平成 FAIL。
4. **「用 waiver/SKIP 盖红」在豁免面上不成立**（A-10）：`exemptions.json` 零条、高水位 `max_entries=0`，
   `known_failures.json` 唯一条目是 ctest 项且 `run.py` 仍把 `KNOWN_FAIL` 计入 FAIL 汇总。
   成立的是另一侧：`run_checks.py` 全 skip 仍 rc=0（A-08）与 P0 门被标 `waivable=true`（A-10）。
5. **「注册表指向的命令不可运行」不成立**：167 个派发脚本全部在盘（`COMMAND_TARGETS_MISSING_ON_DISK = 0`，A-11 前置普查）。
   反向缺口才是真的：14 个 `check_*.py` + 4 个 `eng/ci/*.py` 在册外（A-11/A-19/A-20）。
6. 部分证伪了自己的普查工具：`census6.py` 把读 `run/` 的 `check_product_contract.py` 误分类
   （分段拼路径），`census7.py` 的 33 条"无守卫"含误报（`check_frozen_gate.py` 等）——
   两处均已用精读纠正并在结论中如实标注，不作为判据使用。

## 6. 诚实边界（哪些结论必须实跑才能立证）

| 编号 | 结论 | 只读能立到什么程度 | 需要什么实跑 |
|---|---|---|---|
| E-1 | A-01 / A-08：`REGISTRY-DOC-SYNC`、`run_checks --check <linux-only>` 在基线上的**实际 rc** | 判据链与常量已逐行读定，推导为红/为绿的方向明确 | `python3 eng/ci/check_registry_doc_sync.py`（应 rc=1）；`python3 eng/ci/run_checks.py --check CHK-BUILD-LINUX`（Windows 上应 rc=0，即缺陷现形） |
| E-2 | A-16：「35 个 `check_*.py` 无 `--self-test` 面」 | 文本普查口径，已发现 `check_workflow_governance.py` 用 `--selftest` 拼写被误计入 | 逐门跑 `--self-test` 看 rc；或先订正普查口径为 `--self[-]?test` 后复算 |
| E-3 | A-13：读对象分类的 43/167 | AST 字面量提取，漏 `Path / "run" / ...` 分段拼法（已确认 1 例误分类） | 逐脚本跑一次并采集其真实 open 路径（strace/审计日志），或补一轮人工精读 |
| E-4 | A-21：`drizzle_pf_sb` 两方言"今天恰好等价" | 静态可读部分已给；ctest 实际命中集未知 | `ctest -N -R '^drizzle_pf_sb.*$' --build-config` 与 fnmatch 展开比对（需构建树） |
| E-5 | A-25：fast 档是否真的超预算 | 登记超时是上界，实测墙钟不可得 | 读最近一次 CI 结果 JSON 的 per-step `duration_seconds` 分布（实跑或取历史 JSON） |
| E-6 | A-23 / A-22：性能门两态、ctest 零命中守卫的**实际行为** | 实现体读定；未观测 | 跑 `check_resource_gate_real.py`（需 CPU 采样窗 ≥ `--seconds`，且按 AGENTS.md §3 套 `mem_guard`） |
| E-7 | 全体：本审计**未**执行任何门 ⇒ 不存在"我见过它红/绿"的经验性结论 | — | 阶段 B 落地时按 `docs/ci/` 规定逐档实跑 |

## 附录 A 工作树收证

收工命令与输出（与开工基线逐字节相同）：

```console
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
$ git rev-parse --short HEAD
c8f64e9a
$ git status --porcelain | diff - <(printf "?? ACSD整治工作包_AUDIT-06.zip\n?? site/\n") && echo IDENTICAL
IDENTICAL-TO-BASELINE
```

零新增项 ⇒ 全程无取证动作污染工作树；未产生 `.pyc`（普查脚本与输出均在
`产出/{scripts,inventory}/AUD-501/`），未改任何仓库文件，未做任何 git 写操作。

## 附录 B 取证命令（复算面）

所有普查脚本在审查工作区，只读仓库文件，空输入即非零出口：

```bash
S="/f/Astro dev/独立审计/工具脚本/AUD-501"
cd "$S"
python census_registry.py          # 1 总量/profile/waivable/入口点分布（census1.txt）
python census2.py                  # 2 字段完备性、inputs/outputs/fingerprint/monitor 覆盖（census2.txt）
python census3.py                  # 3 注册命令→脚本目标的可达性（census3.txt）
bash  < census4_callers.sh         # 4 每个孤儿候选的代码侧调用者（census4_orphan_callers.txt）
python census5.py                  # 5 AST：subprocess 编码/空扫描/硬编码字面量/self-test 面（census5.txt）
python census6.py                  # 6 167 派发脚本"读什么对象"分类（census6.txt）
python census7.py                  # 7 扫描面 vs 零面守卫候选清单（census7.txt，误报已知，见 E-3）
```

正文引用的其它单点命令（可直接粘贴复算）：

```bash
R="F:/Astro dev/Astro CS Normalization Database"
git -C "$R" grep -c "check_naming_surface" -- eng/ci/checks.json        # 正控（>0）
git -C "$R" grep -c "CHK-TRUTHFUL-CONCLUSION" -- docs/ci/01_CHECKS.md   # A-01 零命中面
git -C "$R" grep -n "INHERIT_FIELDS" -- eng/ci/run_checks.py eng/ci/run.py
git -C "$R" grep -n "不允许空集" -- eng/ci/run.py ; \
git -C "$R" grep -n "不允许空集" -- eng/ci/run_checks.py                # A-08 一侧有一侧无
git -C "$R" grep -n "evidence_verdict" -- eng/ci/failclosed_survey.py   # A-03 只测 runner 函数
git -C "$R" grep -n "units. > 50" -- eng/ci/failclosed_survey.py        # A-04
git -C "$R" grep -n "回放语料为空\|无任何语料\|ANCHOR_MISSING" -- eng/ci # A-14 零面对照
git -C "$R" ls-files docs/refactor                                      # A-26 对象已删
git -C "$R" log --oneline --diff-filter=D -1 -- docs/refactor/P1_SYMBOL_MAP.md
```

§2.4 的 CTest 闭包配对与 §A-25 的档位超时实算，是同一批内联 `python - <<'PY'` 命令，
其输出分别记在 `inventory/AUD-501/census2.txt` 与本文正文数字；复算需仓库内 `eng/ci/checks.json`
与 `eng/tests/**/CMakeLists.txt` 两个输入，两者皆为跟踪内容。

---

<!-- PROGRESS: 28/28 -->
