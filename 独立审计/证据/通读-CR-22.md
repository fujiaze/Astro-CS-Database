# 通读 CR-22 · 创新点④面积积分与排异算法（最严口径）

- 基线 HEAD：`c8f64e9a`
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 批次：CR-22
- 派单前言：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 已完整读）
- 审查轴：S 支撑 / D 设计符合 / C 上下级一致 / R 运行时正确
- 只读纪律：零 git 写、零 git config 写、不跑构建/ctest/run_checks/任何 `eng/**`、不 import 仓库 Python、不写仓库内文件。

## 清单（6 个，全为独占文件）

1. `lib/algorithms/coverage/src/integrate.cpp` —— 已读：是（表 21 项；finding CR-22-01…04 均 S3）
2. `lib/algorithms/coverage/tests/ivar_wiring_test.cpp` —— 已读：是（表 30 项；finding CR-22-05…10：CR-22-05/06 为 S2，CR-22-07…10 为 S3）
3. `lib/algorithms/coverage/tests/kcorr_lookup_test.cpp` —— 已读：是（表 16 项；finding CR-22-11 为 S2，CR-22-12/13 为 S3）
4. `lib/algorithms/coverage/include/astro/phase2/rejection.h` —— 已读：是（表 41 项；CR-22-14/15/16/17 为本文件名下，CR-22-18/19/20 取证在文件 5）
5. `lib/algorithms/coverage/src/rejection.cpp` —— 已读：是（表 57 项；CR-22-18…24：CR-22-21/23/24 为 S2，其余 S3）
6. `lib/algorithms/coverage/tools/rejection_cli.cpp` —— 已读：是（表 22 项；CR-22-25/26/27：S2/S3/S3）

<!-- PROGRESS: 6/6 -->

## 开工 git status 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
---HEAD---
c8f64e9a
c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9 交接文档按新阶段改写：一切测试与端到端暂停，只读推导找错先行
```

与前言 §1 预期基线一致（工作区仅两项 untracked，HEAD 即被审 `c8f64e9a`）。

## 派单文字与清单的差异（前言 §5.5 要求指出）

派单 §① 称 "`integrate.cpp` 是面积交叠的算核——逐格面积是否几何精确（球面四边形／大圆弧边如何处理）"。
**实测不符**：`lib/algorithms/coverage/src/integrate.cpp` 全文 89 行，只有两个函数
（`p2_validate_candidate_weights` :10-17、`p2_integrate_pixel` :19-87），做的是**同一输出像素的候选栈
数值加权均值 + support 的 max 归约**，文件内**零**几何量：无 WCS、无球面角、无多边形求交、无三角形/极点分支。
权威文档同口径：`docs/science/INTEGRATION.md` §1 非目标"不进行天区重投影（SCI-DRIZZLE）"、
§3a"积分在像素栈域逐像素独立进行；无 WCS/重投影"。逐格面积 `a = support × A_cell` 的换算发生在
**调用方**（`lib/algorithms/coverage/tools/stage2.cpp:1471` `const double area = ok ? support_out * A_cell : 0.0;`，
以及 `docs/science/INTEGRATION.md` §7:93 "发布 support（HiPS support 层的钳后发布值 a/A_cell）"），
不在本批清单内。本批按清单读，**以清单为准**；球面几何精确性的问题域属
`A_cell` 定义处 + normalize 侧 support 产出处，见末节"读不动／需实跑"。

---

# 文件 1 · `lib/algorithms/coverage/src/integrate.cpp`（89 行，已全读）

## §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :11 `weights == nullptr` → `return 0` | 预检空指针返回值 | 0（合规） | 无权重数组 = 无违规项 | 公式导出 | `docs/algorithms/PHASE2_INTEGRATION.md:54` "weights=null→0（合规）"；代码即按此式 | — | CONFIRMED |
| :14 `!isfinite ∥ w<0` → `return 1`；:16 `return 0` | 预检失败/成功码 | 1 / 0 | 0=合规、1=违规 | 结构性不适用 | 返回码约定属 API 合同（`docs/contracts/PUBLIC_API.md:1304`），非科学量 | — | CONFIRMED |
| :14 `0.0`（权重下界） | 权重非负门 | 0.0 | 逆方差域下界：负权重=契约违规 | 公式导出 | `docs/science/INTEGRATION.md` §4:42 "非空时每个 w_i 须 finite ∧ ≥0"；:41 `w<0 ⇒ INVALID_INPUT`；代码按式 | — | CONFIRMED |
| :20 `return 1` / :25 :68 :81 :86 `return 0` | 出参指针无效/正常返回 | 1 / 0 | C ABI 返回码 | 结构性不适用 | 非科学量：`in/out==nullptr` 的参数校验码，与 `out->status` 分离 | — | CONFIRMED |
| :22 `out->n_candidates = in->count` | 候选计数 | — | 透传输入样本数 | 结构性不适用 | 计数赋值无常数 | — | CONFIRMED |
| :23 `in->count == 0` | 空栈边界 | 0 | 触发 `NO_CANDIDATES` | 公式导出 | INTEGRATION.md §4:41 "count 可为 0（⇒ NO_CANDIDATES）"；§8:101 | — | CONFIRMED |
| :21 `memset(out, 0, sizeof(*out))` | 结果清零 | 0 | 未发布字段一律 0 起始 | 结构性不适用 | 清零字节数由结构体大小导出，非科学量；其**语义后果**见 finding CR-22-03 | — | CONFIRMED |
| :31 `wsum = 0.0, vs = 0.0, sup_max = 0.0` | 累加器/上确界初值 | 0.0 | 加法单位元；max 的域外下界 | 公式导出 | `wsum/vs` 求和单位元；`sup_max=0` 合法当且仅当 `support` 域为 (0,1]（INTEGRATION.md §2:18 "覆盖支撑 [0,1]"、§4:43 要求 `>0`）——代码 :39 拒绝 `<=0` 故 0 确在域外 | — | CONFIRMED |
| :32 `n_finite = n_positive_weight = n_accepted = 0` | 计数器初值 | 0 | 计数单位元 | 结构性不适用 | 无量纲计数初值 | — | CONFIRMED |
| :34 `(in->accepted == nullptr) ∥ in->accepted[i]` | 空掩码默认 + 掩码真值化 | nullptr→全接受；非 0→接受 | 样本接受判定 | 待确认 | 空掩码=全接受：INTEGRATION.md §4:44 前半句一致；**"非 0 即接受" 与 §4:44 后半句"仅 `accepted[i]==1` 进入有效集"不等** ⇒ 见 CR-22-01。保守方向：把判定收紧为 `==1` 只会减少进入样本 ⇒ 偏红（对信号面无损） | — | CONFIRMED |
| :37 `!isfinite(values[i])` | 值有限门 | — | 非有限值 = 契约违规 | 公式导出 | INTEGRATION.md §4:45 "values[i] 须有限，否则 INVALID_INPUT"；§5:53 | — | CONFIRMED |
| :39 `!isfinite(support[i]) ∥ support[i] <= 0.0` | 支撑门 | 0.0（下界，开区间） | 支撑须有限且严格为正 | 公式导出 | INTEGRATION.md §4:43 "每个 support_i 须 finite ∧ >0 否则该样本 INVALID_INPUT"；§5:53；代码按式（`<=0` 即 `>0` 的补） | — | CONFIRMED |
| :50 `sup_max = max(sup_max, support[i])` | canonical support reducer | — | 覆盖并集保守下界 = max(accepted support) | 公式导出 | INTEGRATION.md §2 符号表 `sup_max`、§5:68、§7:85-89（作用域 = 通过资格门全体，**不含权重正性**，明确点名本行 :49-50）；§10:114 禁改 mean/sum；代码位置在权重分支之前 = 按式 | — | CONFIRMED |
| :51 `double w = 1.0;` | 缺权重数组时的等权默认 | 1.0 | weights 空 ⇒ 等权 | 公式导出 | INTEGRATION.md §2:17 "可空=等权 1.0"、§4:42、§9a:146；代码按式 | — | CONFIRMED |
| :54/:55 `!isfinite(w)` / `w < 0.0` | 权重有限/非负门 | — | 违规样本标记 | 公式导出 | INTEGRATION.md §4:42、§5:54-55；代码按式 | — | CONFIRMED |
| :56 `w == 0.0`（精确等于比较） | 零权重门 | 0.0 | 零权重合法但不贡献 | 公式导出 | INTEGRATION.md §4:42 "w==0 合法但不贡献"、§7:81 零权重惰性、§10:115 不可接受变化；代码按式。精确比较安全前提=上游权重来自产品字节 0.0f 直转（module_adapters.cpp:11936），非算出近零值 | — | CONFIRMED |
| :71 `n_accepted == 0` | ALL_REJECTED/ZERO_VALID_WEIGHT 分支边界 | 0 | 无任何接受样本 ⇒ ALL_REJECTED | 公式导出 | INTEGRATION.md §5:63、§8:104-105；`docs/contracts/DATA_SEMANTICS.md:1428-1429` 同 | — | CONFIRMED |
| :80/:84 `: 1.0`（support 数组缺失时发布值） | 缺支撑默认发布 | 1.0（=满覆盖） | 无 support 输入 ⇒ 支撑 1.0 | 公式导出 | INTEGRATION.md §4:43 "support 可空（置 1.0）"、§7:83 "空支撑守恒：无 support 输入时 support=1.0，不产生伪 0 支撑"；代码按式。**注意**该分支生产不可达（`integrate.h:19-22` 自述 + 三处调用方恒传 support，见 CR-22-05 的核对） | — | CONFIRMED |
| :83 `out->signal = vs / wsum` | 归一化除 | — | 权重归一加权均值 | 公式导出 | INTEGRATION.md §5:67、§9a:147 "归一：signal=Σ w_i·x_i / wsum"；除零已被 :70 `n_positive_weight==0` 早退排除（进入 :83 必有 ≥1 个 `w>0` 且无负权 ⇒ wsum>0），核对见"不报的核过项" | — | CONFIRMED |
| :24/:71-72/:79/:85 `P2_INTEGRATE_NO_CANDIDATES/ALL_REJECTED/ZERO_VALID_WEIGHT/OK` 枚举基数 | 状态码集 | 0..4（`integrate.h:54-60`） | 五态互斥 | 结构性不适用 | 枚举基数由 `docs/science/INTEGRATION.md` §8 表 + §11:124 五态穷尽门规定；非科学量。各态语义锚见 INTEGRATION.md §5:61-70 | — | CONFIRMED |
| :22/:65 等 `std::uint32_t`/`std::uint64_t` 位宽、`sizeof(*out)` | 类型宽度 | — | 计数器位宽 | 结构性不适用 | 类型宽度非科学量 | — | CONFIRMED |

表 21 行，覆盖文件内出现的每一个数值常数与阈值（无缓冲系数、无截断窗、无收敛参数）。

## findings

### CR-22-01 · `p2_integrate_pixel` 把"非 0 掩码字节"当接受，与冻结输入域"仅 `accepted[i]==1`"不等
- 轴：C（主轴，代码 vs 冻结科学合同）+ R（运行时判定方向）
- 位点：`lib/algorithms/coverage/src/integrate.cpp:34` —— `const bool acc = (in->accepted == nullptr) || in->accepted[i];`
- 上位依据：`docs/science/INTEGRATION.md` §4 输入有效域 :44 原文 —— "`accepted` 可空（全接受）；非空时仅 `accepted[i]==1` 的样本进入有效集。"
  项目对同一件事的更严立场见同域调用方 `lib/infrastructure/scheduler/src/module_adapters.cpp:11870-11878`：
  掩码字节 `sm > 1` 即 `ErrorDomain::DATA` 具名硬失败，注释原文 "非 0/1 掩码值 → 产品损坏 fail-closed（禁 clamp/推断）"。
  条文面检索式：`git grep -n "accepted\[i\]==1\|掩码必须\|mask must be 0/1" -- 'docs/**'` → 仅 INTEGRATION.md:44 一处命中。
- 现状：C 真值化 `in->accepted[i]` 等价于 `!= 0`，故 `accepted[i] ∈ {2,3,…,255}` 的样本进入有效集并贡献
  `n_accepted`（:35）、`sup_max`（:50）、`vs/wsum`（:59-60），且**不**置 `invalid_input`。
  合同里 `accepted` 被定义为"排异接受掩码"（INTEGRATION.md §2:19），而排异层的 reason 是**多值枚举**
  （`P2_REASON_*`，非 0 值即"已剔除"），所以"非 0"恰与"接受"在语义上相反。
- 差在哪：冻结合同把合法掩码域写成 `{0,1}` 并要求 `==1` 才计入；实现取 `!=0` ⇒ 一次掩码编码错（把 reason 码
  数组当 0/1 掩码传入，或产品字节被写成 2）在本层被静默解释成"全部接受"，而不是按 §4 域外处理。
- 后果：`P2PixelResult.signal/support` 直接决定 mosaic 的 signal 面与 support（覆盖）面；错误掩码方向会把
  **应剔除的样本**混入加权均值 ⇒ 被排异的坏样本进入对外发布的像素值。判定方向：`!=0` 相对 `==1` 只会让
  进入有效集的样本**变多** ⇒ 覆盖面与 n_accepted **偏大**（偏绿），不会误伤成红。
- 定级：S3。**缺运行时触发路径**（逐跳查得三个调用点都自行构造 0/1 掩码：
  `module_adapters.cpp:11951 accs.push_back(1)`；`tools/stage2.cpp:1445-1449`、`src/acr_kernels.cpp:184-188` 由
  `rdec.reasons[s] == P2_REASON_ACCEPTED || == P2_REASON_UNDERDETERMINED` 生成 0/1），
  故交付产物当前不会因此错值。但 `p2_integrate_pixel` 是 `P2_API` 导出符号（`docs/contracts/PUBLIC_API.md:1299`），
  对任意调用方即公开合同，合同本身被违反。
- 怎么算修好：:34 改 `(in->accepted == nullptr) || in->accepted[i] == 1u`，并对 `>1` 同时置 `invalid_input`
  （与 §4 域外 + 调用方 fail-closed 立场对齐）；必须同批：(a) 补 `accepted[i]=2` 的用例断言该样本不进
  `n_accepted/sup_max/vs`（现状会被计入），(b) 复核 `eng/tests/unit/p2_output_semantics_test.cpp` 与
  `eng/tests/backend/test_p2004_reject_integrate.py` 是否用过非 0/1 掩码（用过则会判红，需先改成 0/1），
  (c) 三处调用方不需要同改（已恒 0/1）。收紧只可能把结果判红，不会把已绿判红。
- 置信：CONFIRMED（对 :34 与 §4:44 逐字比对；非 0/1 后果按代码分支逐步推演，未实跑）

### CR-22-02 · `n_finite` 的三处定义互不一致，实现只满足头文件注释、不满足冻结 §5 式
- 轴：C（主轴）
- 位点：`lib/algorithms/coverage/src/integrate.cpp:43` —— `++n_finite;`（位于 :51-57 权重分支**之前**）
- 上位依据：
  - `docs/science/INTEGRATION.md` §5 连续定义 :53/:59 —— `valid(i) ⇔ accepted(i) ∧ finite(values[i]) ∧ (support 空 ∨ (finite(support[i]) ∧ support[i]>0)) ∧ (weights 空 ∨ finite(weights[i])) ∧ weights[i]≥0`，
    而 `n_finite = |{i | valid(i) ∧ weights[i]≥0}|` ⇒ 按此式，权重 NaN 或**负**的样本**不**计入 `n_finite`；
  - `lib/algorithms/coverage/include/astro/phase2/integrate.h:69` —— `std::uint32_t n_finite;       // accepted 且 value/support/weight finite`
    ⇒ 按此注释，权重 NaN 不计入，但**负权重计入**（负数也是 finite）；
  - `docs/contracts/DATA_SEMANTICS.md:1418` —— `| n_finite | u32 标量 | 无量纲 | — |` ⇒ 合同面只登记存在性，不给定义（定义空缺）。
- 现状：:37（值非有限）与 :38-42（支撑非有限/`<=0`）各自 `continue` 不计；:43 立即 `++n_finite`；权重面检查在 :51-57 之后，
  对 NaN 权重（:54）与负权重（:55）只置 `invalid_input` 并 `continue`，**不回退已加的 `n_finite`**。
  于是同一样本集：代码 ≥ 头文件注释 ≥ §5 式，两个分歧点分别是"权重 NaN"与"权重 < 0"。
- 差在哪：同名计数在科学文档、公共头注释、合同三张面上是三套定义，实现按第三套；`docs/science/` 为权威且不改（AGENTS §5），
  故实现相对权威偏离。
- 后果：`n_finite` 是被对外声明用于"不靠 n_used 猜原因"的显式诊断计数（integrate.h:65-66），
  并被测试当断言对象（`eng/tests/backend/test_p2004_reject_integrate.py:72` 断言 `ir.n_finite == 4`；
  `eng/tests/unit/p2_output_semantics_test.cpp:109/:136` 断言 `out.n_finite == 2`）⇒ 用非有限/负权重样本构造用例时
  `n_finite` 会**偏大**并把"污染样本已被计入"伪装成"样本有限"。判定方向：偏大 ⇒ 偏绿。
  交付产物不受影响：写盘的只有 `pr.n_used`（`module_adapters.cpp:12005`），`n_finite` 不进任何产品面。
- 定级：S3（无对外产品后果；纯上下级定义漂移 + 断言可靠性）
- 怎么算修好：三处取其一为准并对齐——最小改动是把 :43 的 `++n_finite` 移到权重分支之后（对 `w` 做
  `isfinite && w>=0` 判定后再计），同时把 `integrate.h:69` 注释补成与 §5 式同措辞；
  必须同批：(a) 在 `p2_output_semantics_test.cpp` 增加"值/支撑有限但权重=NaN / 权重=-1"两例，断言
  `status==INVALID_INPUT ∧ n_finite==0 ∧ n_positive_weight==0`；(b) `DATA_SEMANTICS.md:1418` 的"无量纲/—"补齐定义式；
  (c) 检查 `test_p2004_reject_integrate.py:72` 的用例是否含坏权重（含则原断言会由 4 变 3，防"改对反而判红"）。
- 置信：CONFIRMED（对 :37-57 控制流逐行核对，并对三份条文逐字比对；未实跑）

### CR-22-03 · 同一函数内 support 发布规则对四种非 OK 状态不一致，且与本文件自述的缺陷论证相互否定
- 轴：R（主轴）+ D（与 §7 不变量/§9a 登记要求）
- 位点：`lib/algorithms/coverage/src/integrate.cpp:66-69` —— 
  ```
  if (invalid_input) {
      out->status = P2_INTEGRATE_INVALID_INPUT;
      return 0;
  }
  ```
  对照 :79-80 —— `if (out->status == P2_INTEGRATE_ZERO_VALID_WEIGHT) out->support = (in->support != nullptr) ? sup_max : 1.0;`
- 上位依据：
  - 本文件自述论证 :73-78 原文 —— "support 是**覆盖并集下界**，与 signal 是否可用**解耦**……此处若不发布 support，
    零权 accepted 样本的覆盖会被静默丢弃，下游把'有覆盖但方差不可用'误判成'无覆盖'并据此 fail-closed 判 tile 缺失"；
  - `docs/science/INTEGRATION.md` §7:85-89 支撑单调性（max 作用域 = 通过资格门的全部样本），§9a:150 ——
    "本层对进入 reducer 的非有限 values/support/weights 一律显式 INVALID_INPUT（fail-closed），**剔除项逐条登记**，
    也不产生伪 0 或伪有效信号"；
  - §4 输入有效域 :41-45 明确这些是**逐样本**资格（"否则该样本 INVALID_INPUT"）。
- 现状：`out` 先被 :21 整体清零 ⇒ 走 :66-69 早退时 `support=0、signal=0`，即使栈里还有多个完全合格的
  accepted+正权样本（其 `sup_max` 已在 :49-50 算出并被丢弃）。同一条"合格样本的覆盖不得静默丢弃"的规则
  只在 `ZERO_VALID_WEIGHT` 分支生效。此外 §9a:150 要求的"剔除项逐条登记"在本层没有任何落点：
  函数只返回一个聚合 `status`（:67）+ 三个聚合计数（:63-65），既不给出被剔样本索引，`n_accepted/n_finite`
  也区分不出"哪几个样本是坏输入"。
- 差在哪：§4 的资格是**样本级**、§7 的 support 是**并集级**且与 signal 解耦；实现把单个坏样本的处置升级成了
  整像素覆盖归零，正是本文件注释点名要防的误判。
- 后果：两条方向相反、都必须钉死：
  1. 覆盖面失真方向 = **偏红**：交付侧无条件发布 `sup_bin[base+p] = pr.support`
     （`lib/infrastructure/scheduler/src/module_adapters.cpp:12003`），status≠OK ⇒ support 面该像素记 0 ⇒
     真实覆盖被抹成"无覆盖"，下游覆盖/缺失类判据（如 `eng/ci/check_realdata_e2e.py:104`
     `if fs["finite_fraction"] < min_finite_fraction` 的分子侧）只会更少、不会更多 ⇒ 偏红。
  2. 审计计数方向 = **偏绿**：`INVALID_INPUT` 既不 `++l_zero` 也不 `++l_invalid`
     （`module_adapters.cpp:11986-12000`，`++l_invalid` 只在 status==OK 而 wsum 病态时触发），
     而 status==OK 时 `wsum>0` 由本文件 :70/:58 保证 ⇒ **该分支本身不可达** ⇒
     产物 `p2_integrated.json` 的 `invalid_pixels`（:12029 `invalid_pixels += t_invalid[i]`）**恒等于 0**，
     是一个永远为 0 的审计字段：对"非有限输入污染像素数"这一对外主张它不提供任何信息，
     任何 `invalid_pixels ≤ 阈值` 型判据恒绿。
- 定级：S3。**缺运行时触发路径**：交付路径上四数组在入栈前已全部有限化/正性化
  （`module_adapters.cpp:11883` 值与支撑门、:11920/:11937 权重门），故 `invalid_input` 在 scheduler 集成节点不可达；
  另两条调用者 `tools/stage2.cpp`（`CMakeLists.txt:206` + `:211 set_target_properties(... EXCLUDE_FROM_ALL TRUE)`，
  注释原文"降级为 compatibility……不进入任何 install/发布树"）与 `src/acr_kernels.cpp`（AGENTS §2"ACR 生产不可达"）
  均不在交付树。故不成立 S1。但 §9a:150 的"逐条登记"缺实现与 `invalid_pixels` 恒 0 是**条文 vs 实现**的既定事实，
  与可达性无关。
- 怎么算修好：(a) :66-69 之前先发布覆盖 —— 把 `out->n_finite/n_positive_weight/n_accepted` 与
  `out->support = sup_max`（含 `in->support==nullptr ? 1.0`）一并写出后再 return，signal 保持不发布；
  (b) 增加逐条登记的落点（最小面：`P2PixelResult` 增设 `n_invalid_samples` 计数并在 :37/:39/:54/:55 四处 `++`；
  逐索引登记属结构变更需走变更流程）；(c) 调用方 `module_adapters.cpp` 的非 OK 分支补 `++l_invalid`
  （否则改本层也修不好偏绿的审计面）；必须同批：`docs/science/INTEGRATION.md` §8:102 表行需补"support 是否发布"
  列（现只写"⇒ INVALID_INPUT"），否则文档仍自相矛盾；`eng/tests/unit/p2_output_semantics_test.cpp` 的
  INVALID_INPUT 用例若现断 `out.support == 0.0` 需同步改（防"改对反而判红"）。
- 置信：PARTIAL —— 代码与三份条文已核；**缺**"INVALID_INPUT 在某条可达路径上真的产生过"的实跑证据
  （需实跑：构造含一个 NaN value 的候选栈，经 `astrocs-stage2` 或补一个 scheduler 集成节点用例，观察
  `p2_integrated_support.bin` 与 `p2_integrated.json` 的 `invalid_pixels`）

### CR-22-04 · 权重"预检"在交付调用点被放在积分之后，产品 manifest 仍按预检次序自述入口
- 轴：C（主轴）
- 位点：`lib/algorithms/coverage/src/integrate.cpp:10` —— `int p2_validate_candidate_weights(const double* weights, std::uint32_t count) {`
- 上位依据：
  - `lib/algorithms/coverage/include/astro/phase2/integrate.h:9-10` —— "构造后**先经** `p2_validate_candidate_weights`"；:76-77 —— "候选权重校验（integration eligibility 的一部分；Stage2 在权重构造后调用）"；
  - `docs/algorithms/PHASE2_INTEGRATION.md:144-145` —— "权重构造 :1106-1140 → p2_validate_candidate_weights **预检** …… :1141/:1402 → p2_integrate_pixel"；
  - `docs/contracts/DATA_SEMANTICS.md:2754` —— "输入 ivar 非有限/负 → `p2_validate_candidate_weights` hard fail（现行 rc=6），不入合成"。
- 现状：交付路径 `module_adapters.cpp` 先 `p2_integrate_pixel`（:11960）后 `p2_validate_candidate_weights`（:11968），
  校验失败才 `return`（不写 bin）；而同文件 :12058 仍把入口对外写成有序串
  `{"entry", "p2_validate_candidate_weights/p2_integrate_pixel"}`。
- 差在哪：合同把 validate 定义为"integration eligibility 的一部分 / 预检"，实现在唯一交付调用点上次序相反，
  并且产物 JSON 用字符串**声明了与实现相反的次序**（自述出处失真）。
- 后果：失真面是 manifest/审计主张（"先校验后合成"这一可追溯性主张）；数值面不受影响——
  reducer 自身对坏权重置 `INVALID_INPUT`（:54-55）而调用点随后 hard-fail，坏像素不会写盘。
  另一处后果：`docs/contracts/DATA_SEMANTICS.md:2754` 声称"rc=6"，交付路径实际是
  `ErrorDomain::DATA` 返回（:11970-11973），rc=6 只在 compatibility 工具 `tools/stage2.cpp` 的路径上成立 —— 同一失败两出口。
- 定级：S3（主张失真限于 manifest 字段与 rc 口径；产物字节不失真）
- 怎么算修好：把 `module_adapters.cpp:11968` 的 `p2_validate_candidate_weights` 移到 :11953 组装 `P2PixelStack` 之前
  （只读 `weights`，无需先积分）；同步核对 `PHASE2_INTEGRATION.md:144-145` 的行号指向与 `DATA_SEMANTICS.md:2754` 的
  "rc=6"是否要改成域化错误码；必须同批检查 `eng/tests/backend/test_p2004_reject_integrate.py` 是否有依赖
  "先积后校"顺序的断言。
- 置信：CONFIRMED（两个调用行号与 manifest 字段逐字读取；未实跑）

### 不报的核过项（附检索式）
1. **除零伪装**：:83 `out->signal = vs / wsum` 不存在 `wsum==0` 路径 —— 进入 :83 的必要条件是
   `n_positive_weight>0`（:70 早退），而 `++n_positive_weight`（:58）只在 `w` 有限且 `!(w<0)` 且 `w!=0` 后发生，
   ⇒ `wsum = Σ w > 0`（无负权可抵消）。检索式：`git grep -n "wsum" -- lib/algorithms/coverage/src/integrate.cpp`。
2. **零权重不冒充"未测"**：`w==0` 走 :56 `continue`，不计 `n_positive_weight`、不进 `vs/wsum`，但 :49-50 已先取
   `sup_max` ⇒ "有覆盖但方差不可用"以 `ZERO_VALID_WEIGHT` + 发布 support 表达，符合 §7:81/:87-89 与
   `DATA_SEMANTICS.md:1429`；交付侧对应 `wsum=0.0`（不写 NaN）见 `module_adapters.cpp:11995-11996`。
   检索式：`git grep -n "ZERO_VALID_WEIGHT" -- docs/ lib/algorithms/coverage/src/integrate.cpp`。
3. **总和不变（派单问①的第二半）**：本层是**面亮度**的权重归一均值（§3:29 量纲 ADU·sr⁻¹），
   不承诺"面积总和不变"；面积换算 `a = support × A_cell` 在调用方（`tools/stage2.cpp:1471`）。
   即"归一化保证总和不变"这一命题在 `integrate.cpp` 内既不存在也不适用，故不作 finding。
   检索式：`git grep -n "A_cell" -- docs/science/INTEGRATION.md docs/algorithms/PHASE2_MOSAIC_WRITE.md`。
4. **退化三角形／零面积／极点附近**（派单问①第一半）：本文件零几何量，`git grep -n "sin\|cos\|atan\|deg\|rad\|pi" -- lib/algorithms/coverage/src/integrate.cpp` 零命中 ⇒ 无该处理，也无该职责（属 SCI-DRIZZLE/normalize 域，见派单差异节）。
5. **求和顺序确定性**：:33 单向前遍历 `i=0..count-1`，:59-60 顺序累加，符合 §7:84 与 §10:118 的确定性要求（并行粒度由调用方按 tile 切，不改变像素内顺序）。
   检索式：`git grep -n "for (std::uint32_t i = 0; i < in->count" -- lib/algorithms/coverage/src/integrate.cpp`。
6. **`sup_max` 在权重分支之前**（:44-50 的 B2-A7 论证）：与 §7:86-89 明文一致，且回归门确实在
   `eng/tests/unit/p2_output_semantics_test.cpp:85-107`（§7:89 点名）⇒ 不报。
   检索式：`git grep -n "p2_output_semantics_test" -- docs/science/INTEGRATION.md`。

---

# 文件 2 · `lib/algorithms/coverage/tests/ivar_wiring_test.cpp`（493 行，已全读）

## §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :37 `kTileWidth` | HiPS tile 边长 | 512 | 夹具 tile 宽度 | 结构性不适用 | `lib/infrastructure/aio/include/aio_hips.h:145` "tile_width - 标准 512"、:105 "width: 512 (HiPS 标准 tile)" | — | CONFIRMED |
| :38 `kNside` | 叶级 NSIDE | 512 | 夹具产品分辨率 | 结构性不适用 | `aio_hips.h:144` "nside - 叶级 NSIDE (2 的幂, >= 512)"，取合法域下界 | — | CONFIRMED |
| :39 `kTile` | 叶级 tile 父单元 ipix | 0 | nside=512 ⇒ 全天空 1 个 tile | 结构性不适用 | `aio_hips.h:103` "Norder K = log2(nside)-9" ⇒ K=0 ⇒ ipix 只有 0 | — | CONFIRMED |
| :40 `kTruth` | 夹具常数场真值 | 10.0 | 被测"面亮度"量级 | 结构性不适用 | 夹具自订幅度，非科学量；**但它决定 :314 绝对容差的松紧**（0.05 = 该真值的 0.5%）⇒ 见 CR-22-07 | — | CONFIRMED |
| :51-53 phase | 三帧正弦相位 | 0.0 / 1.0 / 2.0 | 使三帧图样互不相同 | 结构性不适用 | 夹具自订；非科学量 | — | CONFIRMED |
| :51-53 ivar_base | 三帧 ivar 量级 | 1.0 / 4.0 / 16.0 | 帧间信噪比阶梯，制造可检出的加权差 | 结构性不适用 | 夹具自订。灵敏度已复算：三帧齐全处若 B/C 权重互换，均值位移 = \|12(v_B−v_C)/21\| = 0.57·\|v_B−v_C\|，而 \|v_B−v_C\| ≤ 1.0 ⇒ 位移可达 0.57 ≫ 0.05 ⇒ 该类错位**可检出** | — | CONFIRMED |
| :51-53 ivar_slope | ivar 空间斜率 | 0.004 / 0.010 / 0.020 | 帧内 ivar 空间 pattern（`ivar = base + slope*(x+y)`） | 结构性不适用 | 夹具自订；x+y ≤ 1022 ⇒ 增量 ≤ 20.5/10.2/4.1，量级不超过 base ⇒ 不断言等价的权重秩 | — | CONFIRMED |
| :66-70 `AIO_HIPS_FLOAT32` | 产品存储精度 | — | 夹具产品用 fp32 | 结构性不适用 | `aio_hips.h:146` 枚举值 | — | CONFIRMED |
| :67 `AIO_HIPS_PRODUCT_ALL_V19` | 子产品位或 | — | 建全产品集 | 结构性不适用 | flags 枚举基数（注意：生产写侧另有 `ALL_V20`，见 `lib/algorithms/coverage/hips_p2/README.md:164`，夹具不跟随属可接受） | — | CONFIRMED |
| :69 `"Red"` | obs_filter | — | 夹具滤光片标签 | 结构性不适用 | 元数据字符串，非科学量 | — | CONFIRMED |
| :69 `10.0` | exposure_s | 10.0 | 夹具曝光时间秒 | 结构性不适用 | `aio_hips.h:151` "exposure_s - 曝光时间秒"；本门不消费曝光 ⇒ 无科学语义 | — | CONFIRMED |
| :69-70 `"2026-08-16"` | obs_date | — | 夹具观测日期 | 结构性不适用 | `aio_hips.h:152` "obs_date - 观测日期 ISO (可 NULL)" | — | CONFIRMED |
| :70 `7` | moc_order | 7 | 夹具 MOC 阶 | 待确认 | `aio_hips.h:153` 声明域 "moc_order - MOC 阶 (**<= tile order K**), 0=auto(=K)"；本夹具 K = log2(512)−9 = **0** ⇒ 7 > 0 属声明域外，门却仍以 `product_begin` 成功为前提（:71-75） ⇒ 见 CR-22-09。保守方向：writer 若补域校验，本门会红（需同批改夹具为 `0`） | PARTIAL（未实跑） |
| :79 `view.leaf_order` | 叶级 Norder | 9 | 与 nside 一致 | 公式导出 | `aio_hips.h:104` "leaf_order: 叶级 Norder L (= log2(nside))" = log2(512) = 9；代码写死 9 而非由 `kNside` 导出（同文件 :252 `nested_local_to_xy(z, 9u, ...)`、:271、:307 三处重复同值） | — | CONFIRMED |
| :85 `flux_sum = signal*support` | 夹具写盘量换算 | 1.0（隐含 area∈{0,1}） | 满足 writer 的 `signal = flux_sum/covered_area` | 公式导出 | `docs/algorithms/PHASE2_MOSAIC_WRITE.md:321-322` "落盘值 = flux_sum / covered_area"；注释 :85 "// area 0/1" 明示**只用二值覆盖** ⇒ 分数覆盖域本门不行使（见"边界"） | — | CONFIRMED |
| :99-100 `1.0f / ivar[i]`、`support>0.0f && ivar>0.0f`、else `0.0f` | variance 分子与门 | 1.0 / 0.0 | `variance = var_num_sum/covered_area²`，area=1 ⇒ var_num=1/ivar | 公式导出 | 行内注释 :96 + `eng/tests/unit/p2001_real_nodes_test.cpp:204` 同式 "variance_mosaic = 1/ivar; writer 通道: variance = var_num_sum/covered_area²"；`ivar<=0` 记 0 = 无方差信息 | — | CONFIRMED |
| :121 `"version":1` | 配置 schema 版本 | 1 | stage2 配置版本键 | 结构性不适用 | 合同版本键（`docs/development/CONFIG_SCHEMA.md`），非科学量 | — | CONFIRMED |
| :127 `"snr_weight_mode":"snr2_normalized"` | UPM 拟合权重源 | 唯一合法值 | UPM 拟合内部开关 | 结构性不适用（**核过，不是口径选择键**） | `docs/contracts/PUBLIC_API.md:2208` "UPM fit 的 upm_weight_source（原 snr_weight_mode…）与 use_ivar_weight 是**拟合内部诊断开关，不是 Phase2 集成权重枚举**"；解析侧 `src/stage2_common.cpp:164-166` 只接受该值、否则 `unsupported snr_weight_mode` ⇒ 非 §9.73 A44 所删对象 | — | CONFIRMED |
| :128 `"smoothing":0.0` | UPM 平滑 | 0.0（关闭） | 隔离 UPM 平滑对期望值的贡献 | 待确认 | 关闭值是保守选择；**条文面零提及"测试须以 smoothing=0 隔离"**，检索式：`git grep -n "smoothing" -- docs/algorithms/PHASE2_SAMPLER.md docs/science/UPM*.md` 未回读定案。影响范围：仅本门 | PARTIAL |
| :128 `"precision":"fp32"` | 产品精度 | fp32 | 走 fp32 写盘支路 | 结构性不适用 | `docs/algorithms/PHASE2_MOSAIC_WRITE.md:311-312` "f64 产品（cfg.precision）无此层截断" ⇒ 本门选 fp32 = 行使**有截断**的那一支；与 :314 容差耦合（CR-22-07） | — | CONFIRMED |
| :129 `"rejection":{"method":"none"}` | 排异方法 | none（全关） | 让 ivar 接线差分不受排异干扰 | 结构性不适用 | 变量隔离选择；代价见"边界"：本门不覆盖 ivar×排异交互 | — | CONFIRMED |
| :131 `legacy_allow_weight_fallback:true` | 负例注入键 | true | 复活已删除键，断言被拒 | 结构性不适用 | §9.73 裁决 A44（:115 注释 + `module_adapters.cpp:11411-11419` 具名拒绝） | — | CONFIRMED |
| :143 `(st & 0x7f) == 0` / `(st >> 8) & 0xff` | wait-status 解码 | 0x7f / 8 / 0xff | POSIX `system()` 退码归一 | 结构性不适用 | 位移量与掩码位宽，非科学量 | — | CONFIRMED |
| :168 `--cpu-workers` 取值 1 / 2 | 并行档 | 1 / 2 | 1T vs 2T 差分 | 结构性不适用 | 并行档选择（AGENTS §9 "1/N worker 一致性"），上界 2 是实现选择 | — | CONFIRMED |
| :228 `(kTile << 18) \| z` | NESTED leaf 索引 | 18 | tile 内 leaf 的全局索引位移 | 公式导出 | 18 = 2 × log2(tile_width=512) = 2L，`aio_hips.h:104` L=log2(nside)；**写死字面量**（kTileWidth 改变即静默错位，与 :79/:252 的 9 同族耦合） | — | CONFIRMED |
| :234 `out = (wsum > 0.0) ? vs/wsum : 0.0` | 期望值无样本支 | 0.0 | 无任何有效帧时给 0 | 结构性不适用 | 该支路在比较循环里被 :311 `if (!any_valid) continue;` 排除，永不参与判据 ⇒ 不属"0 冒充未测"（已在 :308-311 显式跳过） | — | CONFIRMED |
| :258 `0.5 * sin(phase + 0.01x + 0.02y)` | 图样幅度/频率 | 0.5 / 0.01 / 0.02 | 帧间差异与亚像素结构 | 结构性不适用 | 夹具自订（幅度 0.5 = kTruth 的 5%，保证 UPM 有非零 offset 可拟） | — | CONFIRMED |
| :276-278 `(0,64) (192,256) (384,448)` | 三帧 invalid FITS 行带 | 64 行带 × 3，两两不相交 | 制造逐帧不同的 compact 空洞 | 结构性不适用 | 夹具自订；**不相交 ⇒ :308-311 `any_valid` 恒真**，正是 CR-22-08 的根 | — | CONFIRMED |
| :312-314 `> 0.05`（绝对） | WIRE-IVAR-003 匹配容差 | 0.05 | 生产输出 vs 期望加权均值的放行阈 | **待确认** | **无锚**：`docs/science/INTEGRATION.md` §11:126 对"复算 signal 与生产比对"给的是 `rtol 1e-12`；`docs/algorithms/PHASE2_MOSAIC_WRITE.md:385` "科学数值容差唯一权威 = SCI 层默认容差"；本仓对"绝对量容差"的立场见 `docs/science/ACR_EQUIVALENCE.md:140`（把容差写成绝对量而不声明信号标度 = 不可接受变化）。检索式：`git grep -n "0.05\|rtol" -- docs/science/INTEGRATION.md docs/algorithms/PHASE2_INTEGRATION.md`。保守方向：收紧到与 fp32 截断同量级的**相对**容差（≲1e-5 相对）只会更严 ⇒ 偏红风险，需同时把 :128 改 fp64 或按 fp32 截断量重新定阈 | CONFIRMED（值为无锚事实） |
| :321 `npix / 2` | `EXPECT_GT(n_checked, npix/2)` 下界 | 131072 | 名义"检查覆盖面"守卫 | 结构性不适用（但该门无证据资格） | `n_checked` 只由**夹具数组** `supports[f][z]` 决定，与被测实现无关 ⇒ 见 CR-22-08 | — | CONFIRMED |
| :340-341 `1e-4`（两处） | 1T/2T 图层差分阈 | 1e-4 | 计入"差异像素"的判据 | 待确认 | fp32 在 10.0 量级的 ulp ≈ 9.5e-7 ⇒ 1e-4 ≈ 100 ulp；文档 `PHASE2_MOSAIC_WRITE.md:313-317` 用本门为"确定性"背书，而"确定性"在 `docs/science/INTEGRATION.md` §7:84 是无容差的 ⇒ 见 CR-22-10。保守方向：收紧为 `== 0.0`（逐位相等）偏红，需先证明归约顺序无关 | CONFIRMED |
| :367-368 `1e-6`（两处） | repeat-2T 差分阈 | 1e-6 | 同配置重跑必须一致 | 待确认 | 注释 :353 自称 "by-construction 位精确"，但判据只到 1e-6 ⇒ 主张强于证据（同 CR-22-10） | CONFIRMED |
| :382 `* 4.0f` | C 帧 ivar 倍率 | 4.0 | WIRE-IVAR-004 的扰动幅度 | 结构性不适用 | 夹具自订（×4 使参与区域位移远大于 1e-4） | — | CONFIRMED |
| :398-399 `y<448`、`x<512` | 局部性检查窗口 | 384..448 / 512 | 只在 C 的 invalid 带内比较 | 结构性不适用 | 与 :278 带定义同源；512 是 `kTileWidth` 的字面量重复（同族 :449 `z % 512`） | — | CONFIRMED |
| :403 `1e-4` | 局部性差分阈 | 1e-4 | "C 不参与区域输出不变"的判据 | 待确认 | 与 :340 同族（同一未定案问题：该处物理上应位精确，见"需实跑"节第 2 条——UPM 重新拟合会以什么幅度扰动 A/B 的 offset，静态不可判） | PARTIAL |
| :417 `npix / 4` | 全局变化下限 | 65536 | "C 权重变了输出必须变" | 结构性不适用 | 夹具导出：三帧齐全区域 = 320/512 行 ≈ 62.5% npix ⇒ npix/4 为宽松安全界；该判据对实现敏感（若 ivar 未接则差数=0 ⇒ 红），**非恒真** | — | CONFIRMED |
| :449 `0.01 * (z % 512)` | 第二用例图样斜率 | 0.01 | fail-closed 用例的值梯度 | 结构性不适用 | 夹具自订，该用例只断言退码与"零产物" | — | CONFIRMED |
| :480 `7` | 期望进程退码 | 7 | ivar tile 缺失 fail-closed | 结构性不适用（API 退码） | 实现在位：`tools/stage2.cpp:1577-1579` `log("M4-C-03 ivar tile read failed without legacy fallback → rc=7"); return 7;`；FIX_LEDGER `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:50`（M4-C-03，状态 FIXED，`ADDED:phase2_ivar_wiring.Phase2IvarWiring.IvarTileMissingFailClosed`） | — | CONFIRMED |
| :487 `EXPECT_NE(exit_code(rc_leg), 0)` | 负例退码非零 | 0 | 复活 legacy 键必须被拒 | 结构性不适用 | 判据方向：`!= 0` 只要求"失败"，不锁定退码 ⇒ 若实现改成"静默忽略该键"（rc=0）则红；但改成"任意其他非零码"仍绿，属可接受的弱判据（同文件 :480 已锁 7） | — | CONFIRMED |
| :224/:227/:234 `<= 0.0f` / `!(w > 0.0)` / `> 0.0` | 期望实现的资格门 | 0.0 | 复算侧过滤支撑≤0 与权重≤0 | 公式导出 | 与 `docs/science/INTEGRATION.md` §5:53 `valid(i)` 同式；**注意期望实现不校验非有限权重**（:226 直接取 `w=ivar`，NaN 时 `!(w>0)` 为真 ⇒ 静默剔除），而生产 `p2_validate_candidate_weights` 对 NaN 权重是 hard fail ⇒ 期望与生产在"坏权重"上语义不同（本夹具 ivar 恒 >0，不触发） | CONFIRMED |

表 30 行；文件内出现的每个数值常数/阈值/缓冲尺寸/窗口/退码均已列（含合并说明）。

## findings

### CR-22-05 · 自称"直接跑生产 Stage2"的门，被测对象是发布清单标注"非发布目标"的 compatibility 工具
- 轴：D（主轴）+ C
- 位点：`lib/algorithms/coverage/tests/ivar_wiring_test.cpp:3` —— `// 直接跑生产 Stage2（astrocs-stage2.exe），不是单独调用 reducer：`；实现面 :147-163 `stage2_exe()` 返回 `"astrocs-stage2"`（POSIX 侧再试 `build/linux-openmp-on/astrocs-stage2`、`build/linux-release/astrocs-stage2`），:292/:328/:357/:394/:424 全部经 `run_stage2()`→`std::system` 驱动该可执行文件
- 上位依据：
  - `lib/algorithms/coverage/CMakeLists.txt:204-211` —— "BLD-002: 以下工具降级为 compatibility (EXCLUDE_FROM_ALL) —— **非正式 ALL target**, 仅按需显式构建; **不进入任何 install/发布树**" + `set_target_properties(astrocs-stage2 PROPERTIES EXCLUDE_FROM_ALL TRUE)`；
  - `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:9` —— `exe_target,astrocs-stage2,...,tool,no,-,非发布目标`；同文件 :235 把 `phase2_ivar_wiring` 自己登记为 `非发布目标`；
  - `lib/algorithms/coverage/include/astro/phase2/integrate.h:20-22` —— 交付实现自述为"生产唯一调用方（`module_adapters.cpp p2_op_integrate`）"，实测调用点在 `lib/infrastructure/scheduler/src/module_adapters.cpp:11960`；
  - AGENTS §7 目录落位：生产命令面属 `lib/infrastructure/`（pipeline/scheduler），`tools/` 属工具面。
- 现状：门用三帧**空间变化 ivar + 逐帧不同 invalid 行带 + 帧置换 + 1T/2T/repeat** 四类差分，全部打在 `astrocs-stage2` 上；交付实现 `p2_op_integrate` 的权重装配（`module_adapters.cpp:11889-11946` 的 corrected-variance / frame-SNR / per-sample-ivar 三分支）与 stage2 工具侧的装配（`tools/stage2.cpp:1369-1383`，其中 :1383 是 `weights[s] = support_v[s];` 兜底支）**是两套代码**，本门一行都不执行后者之外的分支。
- 差在哪：门的标题与注释把被测对象表述为"生产"，而在册事实是该可执行文件被明确降级为非发布目标；"生产 ivar wiring 已被证明"这句对外话在本门上只覆盖非交付实现。
- 后果：交付节点链若发生同类"帧/pixel ivar 错位"回归，本门恒绿（它不跑那段代码）。方向：**偏绿**（对交付实现的覆盖判断）。
  已核对冲证据（故不升 S1）：`eng/tests/unit/p2001_real_nodes_test.cpp:14-15/371-483` 与
  `eng/tests/unit/p2002_unc_rej_prov_test.cpp:122-244/1072-1123` 确实驱动节点链并带 ivar fixture，
  但其 ivar 为**每帧常数**（F1=2.0、F2=0.5、F3=1.0），**空间变化 ivar、逐帧不同 invalid 带压缩对齐、帧置换不变性**
  三项在本仓**没有任何交付链上的对应判据**。
- 定级：S2（被测对象错位可确证；产物错值不成立，因为交付链另有 2 帧/3 帧常数 ivar 门部分兜底）
- 怎么算修好：(1) 新增/改造成以 `run_node("astrocs.phase2.integrate")` 为被测对象的同判据用例（可直接复用本文件的 `kFrames`/带定义/置换三件套），或把本文件被测面切到节点链；(2) 同步订正文件头 :1-12 的"生产 Stage2"表述与 `PRODUCTION_EXECUTION_INVENTORY.csv:235` 的登记；(3) 必须同批改的引用面：`docs/algorithms/PHASE2_MOSAIC_WRITE.md:315-317`（把"确定性实证"挂在本门上）与 `docs/science/ACR_EQUIVALENCE.md:150`（回退门，见 CR-22-06）——否则门换宿主后这两处主张同时失效；(4) 若同时修 stage2.cpp:1383 的 support 兜底（属移交线索，见末节），本门 :293 `ASSERT_EQ(rc,0)` 不受影响（夹具三帧都有 ivar 产品，兜底支不触发）。
- 置信：CONFIRMED（三处条文与本文件逐字核对；未实跑）

### CR-22-06 · `ACR_EQUIVALENCE` 把"回退门"的证据登记在本文件上，而本文件零 ACR 行使
- 轴：C（主轴，登记面 vs 实现面）+ D
- 位点：`lib/algorithms/coverage/tests/ivar_wiring_test.cpp:147-163`（`stage2_exe()`：被测入口只有 `astrocs-stage2` 一条）与 :242/:441 两个 TEST 名 `Phase2IvarWiring.*`
- 上位依据：`docs/science/ACR_EQUIVALENCE.md` §11 验证 Oracle :150 原文 ——
  "**回退门**：逐像素 ivar 权重的 `ACR` 强制 CPU 与纯 CPU `canonical` 等价（`ivar_wiring_test`）。"
- 现状：本文件不含任何 ACR 行使面。检索式与结果（零命中，故判"不存在"）：
  `git grep -n "acr\|ACR\|KernelInvocation\|gpu\|GPU\|mixed\|cuda" -- lib/algorithms/coverage/tests/ivar_wiring_test.cpp` → 无输出（exit=1）。
  真正的 ACR 路由/等价证据在别处：`docs/science/ACR_EQUIVALENCE.md:147` 自指 `synthetic_gate.cpp:3355` 起（并注明无 GPU 环境 `GTEST_SKIP`），
  以及 `lib/algorithms/coverage/tests/routing_test.cpp` 行使 `p2_acr_block_eligible`。
- 差在哪：科学文档 §11 登记的"回退门"没有对应实现——本门既不调 ACR 内核、也不做 CPU/ACR 两路对照，更没有"逐像素 ivar 权重下强制 CPU"这一条被测语义。
- 后果：`ALG-ACR-EQUIV-002`（`ACR_EQUIVALENCE.md:157` "ivar 生产禁用与 OpenMP fallback 路由"）的对外主张**证据面为空挂**：读者按 §11 去复核回退门会复核到一条与该主张无关的 ivar 接线门。方向：登记面偏绿（主张有"门"背书，实际无）。
- 定级：S2（对外主张不成立；不致产物错值——ACR 生产不可达，AGENTS §2，且 `p2_acr_block_eligible` 恒 false ⇒ 回退本身由 `routing_test` 行使）
- 怎么算修好：把 `ACR_EQUIVALENCE.md:150` 的证据指针改到真实宿主（`routing_test.cpp` 的 `p2_acr_block_eligible` 用例 + `synthetic_gate.cpp` Phase2Acr 族），**或**在本文件补一条"逐像素 ivar 输入 ⇒ 强制 CPU 且与纯 CPU canonical 逐位一致"的用例；二者择一即可，但不能维持现状（文档指针与文件内容两条面各说一套）。
- 置信：CONFIRMED（零命中检索式已给出；两处条文逐字比对）

### CR-22-07 · WIRE-IVAR-003 的匹配容差 `0.05` 是无锚绝对量，比 SCI-INT 自己规定的比对容差宽 10 个数量级以上
- 轴：S（主轴，值无出处）+ R（判据方向）
- 位点：`lib/algorithms/coverage/tests/ivar_wiring_test.cpp:312-314` ——
  `if (std::fabs((double)got1[(std::size_t)fits] - expect[(std::size_t)z]) > 0.05) { ADD_FAILURE() ... }`
- 上位依据：
  - `docs/science/INTEGRATION.md` §11 验证 Oracle :126 —— "**Python 参考**：NumPy 对同 `values/weights/support/accepted` 复算 `signal/support/status`（**`rtol 1e-12`**）" ⇒ 同一比对类（期望复算 vs 生产输出）的权威容差是相对 1e-12；
  - `docs/algorithms/PHASE2_MOSAIC_WRITE.md:385` —— "科学数值容差唯一权威 = SCI 层默认容差（INTEGRATION/REJECTION 冻结值）；本文只登记测试实测阈值"；
  - `docs/science/ACR_EQUIVALENCE.md:140` —— 不可接受变化："把 §9 的容差写成**绝对 `ADU` 量而不声明信号标度**"。
- 现状：0.05 为裸字面量绝对值，文件内未声明它与 `kTruth=10.0` 的比值关系（0.5%），也未声明产品精度 fp32（:128 `"precision":"fp32"`）带来的截断量级。
  fp32 在 10.0 量级的 ulp ≈ 9.5e-7，故即使按写盘截断计算，残差量级应为 1e-6；0.05 比该量级宽约 **5×10⁴**，比 §11:126 的 `rtol 1e-12` 宽约 **5×10¹⁰**。
- 差在哪：一个"数值匹配"门被放到比流水线自身最低一层截断还粗 5 万倍的位置，且权威规定的相对容差与"不得写绝对量"两条都被违反。
- 后果：任何**系统性**偏差 <0.5%（<0.005 mag 量级）的加权均值错误都能通过 WIRE-IVAR-003 ⇒ "生产 ivar 加权均值与解析期望匹配"这条对外主张实际被证到的强度只有 0.5%，而文档主张的强度是 1e-12。方向：**偏绿**（放宽容差只会让"不匹配"更难被发现，不可能造成误红）。
  已复核**不成立**的担忧：帧互换类**离散**错误仍可检出（:51-53 行的复算 ⇒ B/C 权重互换位移可达 0.57），所以本条针对的是系统性小偏差与标度错误（例如把 Ω_px 幂次标度算错但落在 0.5% 内）。
- 定级：S3（判据强度不足；无已知错值路径）
- 怎么算修好：把 :312 改成相对判据并与标度声明绑定 —— 例如 `fabs(got-expect) > 1e-5 * std::max(1.0, fabs(expect))`（fp32 档位），并在 :128 增一条 fp64 档用例把容差收到 1e-12 相对；**必须同批**：(a) `PHASE2_MOSAIC_WRITE.md:385-390` 的"实测阈值登记"跟着更新，(b) 若同时修 CR-22-05（换被测宿主），先在本宿主收紧容差再搬家，避免一次改动引入双变量，(c) 收紧后先确认 fp32 截断+UPM 求值残差实测值，防止"改对反而判红"。
- 置信：CONFIRMED（量级复算自证：fp32 ulp 与 0.57 位移均为手算可复核；未实跑门）

### CR-22-08 · `EXPECT_GT(n_checked, npix/2)` 是与被测实现无关的恒真门，无证据资格
- 轴：R（主轴）
- 位点：`lib/algorithms/coverage/tests/ivar_wiring_test.cpp:321` —— `EXPECT_GT(n_checked, npix / 2);`
- 上位依据：AGENTS §5 "判据必须非退化……**恒真门没有证据资格**"；§9 "对'看起来通过'的结果保持怀疑：空断言……都算未完成"。
- 现状：`n_checked`（:303/:319）只在 :308-311 的 `any_valid` 为真时自增，而 `any_valid` 读的是**夹具内存数组** `supports[f][z]`；
  三个 invalid 带（:276-278，行区间 [0,64)、[192,256)、[384,448)）两两不相交 ⇒ 任一像素至少两帧有效 ⇒
  `n_checked == npix == 262144 > 131072` 与 `astrocs-stage2` 的行为、与 `got1` 的内容**完全无关**（该值甚至不参与 `got1`）。
  同文件里唯一"能红"的路径是 `write_hips`/`nested_local_to_fits_index` 自身坏掉，而那属夹具自检，不是产品判据。
- 差在哪：一条写在外层结果序列里的断言，对交付实现零敏感；把它算作"检查覆盖面证明"会让读者以为门覆盖了 ≥50% 像素。
- 后果：门面虚增一条断言 ⇒ 复核者对"该门是否真的检查了大面积像素"的判断被误导；产品字节不失真 ⇒ 按 §3 规则定级 S3。方向：该门既不能红也不能证绿（对实现无向）。
- 定级：S3
- 怎么算修好：让 `n_checked` 由**产品侧**决定（例如从 `out1` 的 support 图层重读三帧有效性、或直接统计 `got1` 非零像素数），并把下界写成"等于期望的有效像素数"而非 `npix/2`；必须同批：`PHASE2_MOSAIC_WRITE.md` 若引用该覆盖面数字需一并更新。
- 置信：CONFIRMED（组合恒等性由带区间直接可证）

### CR-22-09 · 夹具向公共 writer 传入声明域外的 `moc_order=7`（该夹具 K=0），门以"写成功"为前提 ⇒ 域校验缺位被固化
- 轴：C（主轴，API 声明域 vs 调用值）+ R
- 位点：`lib/algorithms/coverage/tests/ivar_wiring_test.cpp:70` —— `("2026-08-16", 7);`（`aio_hips_product_begin` 的末参 `moc_order`）
- 上位依据：`lib/infrastructure/aio/include/aio_hips.h:153` —— "moc_order - MOC 阶 (**<= tile order K**), 0=auto(=K)"；
  同文件 :103 —— "parent_ipix: NESTED, 叶级 tile 父单元 (**Norder K = log2(nside)-9**)"。
- 现状：本夹具 `kNside = 512`（:38）⇒ K = 9 − 9 = 0 ⇒ 合法 `moc_order ∈ {0}`，实传 7。
  门把 `product_begin` 成功当隐含前提（:71-75 失败才 ADD_FAILURE），且后续 `ASSERT_EQ(rc, 0)`（:293）要求整链跑通。
  已登记该门通过的记录面：`artifacts/evidence/reaudit-v3/v3_exec/CON010_runtime_gate.md:86` "ivar_wiring(2T) PASS"。
- 差在哪：要么 writer 不校验自己声明的 `moc_order <= K`（则声明域是空头话，任何调用方传域外值都静默接受），要么该记录面已过时。
  两者都把一个**未被任何判据锁住**的域约束留在暗处。方向：若 writer 补上校验，本门立即偏红（域外夹具值），属"改对反而判红"的典型，必须与夹具同批改。
- 后果：`aio_hips_product_begin` 是公开 API；"MOC 阶不得超过 tile 阶"若只是注释而无实现，交付侧可写出 MOC 与数据阶不一致的产品树。
- 定级：S3（本文件侧只是夹具取值；根因在 AIO writer 域校验，属他批）
- 怎么算修好：本文件把 :70 改成 `0`（auto）或 `1`（≤K 的合法值需 K≥1，故实际只能 0）；同时在 AIO 域补"域外 `moc_order` 拒绝"的正例/负例门（他批登记，本条只提出联动的必要性）。
- 置信：PARTIAL —— 缺"AIO writer 当前是否校验 `moc_order<=K`"的源码级定案（读 `lib/infrastructure/aio/src/hips_writer.cpp` 的 `product_begin` 体即可判定，但该文件不在本批独占面内），也缺本批自行实跑（纪律禁止）。

### CR-22-10 · 本门被登记为"确定性/位精确"的实证，实际判据只到绝对 1e-4（1T/2T）与 1e-6（repeat）
- 轴：C（主轴，主张面 vs 判据面）+ R
- 位点：`lib/algorithms/coverage/tests/ivar_wiring_test.cpp:340-341` ——
  `if (ds > 1e-4) ++n_sig_diff;` / `if (du > 1e-4) ++n_sup_diff;`（判据 :345/:348 `EXPECT_EQ(n_*_diff, 0u)`）
  与 :367-368 同族 `1e-6`
- 上位依据：
  - `docs/algorithms/PHASE2_MOSAIC_WRITE.md:313-317` —— "确定性: 固定 tile 序……+ 单线程 writer 调用 → 同输入同 config 同 mosaic（§7）；**实证 Phase2IvarWiring 1T/2T/repeat 差分数==0**（ivar_wiring_test.cpp:314-316/:339-341，容差阈值 1e-4/1e-6 下零差异像素）"；
  - `docs/science/INTEGRATION.md` §7:84 —— "**确定性**：加权求和顺序按输入索引 `i=0..count-1` 固定，`signal` 确定性（FP64 舍入仅顺序确定性）"（无容差概念）；
  - 本文件 :353 注释 —— "整块隔离 + 不相交写 + 定序归约 => **by-construction 位精确**"。
- 现状：差异像素的**计数阈**是绝对 1e-4 / 1e-6，而不是逐位相等；产品为 fp32（:128）且值在 10 量级 ⇒ 1e-4 ≈ 100 个 ulp、1e-6 ≈ 1 个 ulp。
  也就是说：只要像素差 ≤100 ulp 就不计入"差异"，门照样绿。
- 差在哪：登记面用本门为"位精确/确定性"背书，判据面实际只证到"差异不超过约 10⁻⁵ 相对"；注释自称 by-construction 位精确却用 1e-6 的容差去"证"位精确。
- 后果：worker 数改变导致的**归约顺序依赖**（例如 chunk 边界线程局部累加的合并次序变化）在 ≤1e-4 内完全隐身，
  而"同输入同 config 同 mosaic（字节级）"是对外交付主张之一。方向：**偏绿**（计数阈放宽 ⇒ 差异像素数只可能变小）。
- 定级：S3（无已知实际漂移源；属"主张强于证据"）
- 怎么算修好：把 :340-341/:367-368 的 `>` 阈值改为**逐位相等**判据（`got1[p] != got1_2t[p]` 即计入；fp32 缓冲可直接比 `float` 位型），
  或在 `PHASE2_MOSAIC_WRITE.md:313-317` 把主张降级为"≤1e-4 绝对一致"并显式声明信号标度；
  **必须同批**：先实跑一次 1T/2T 确认当前实测差为 0（否则改判据即红），并把 :353 的"by-construction"句与该实测绑定。
- 置信：PARTIAL —— 缺"1T/2T 实测最大差"的数值（要定案需实跑 `phase2_ivar_wiring` 并读 `max_sig_diff/max_sup_diff` 输出；纪律禁止本批执行）

### 不报的核过项（附检索式）
1. **门能红**：`read_signal_tile` 打开/读取失败时 `out.clear()`（:182），随后 `ASSERT_EQ(got1.size(), npix)`（:295）红；
   `p2_upm_open` 失败返回全 0 向量（:213-216）⇒ 与 `got1≈10` 逐像素差 >0.05 全红；`write_hips` 每步失败都 `ADD_FAILURE`+`return`（:71-75/:91-95/:103-108/:109-112）。
   不存在"失败即空跑通过"的静默路径。检索式：`git grep -n "ADD_FAILURE\|ASSERT_" -- lib/algorithms/coverage/tests/ivar_wiring_test.cpp`。
2. **ivar 缺失 fail-closed 是真门不是夹具门**：期望退码 7 与被测实现的具名支路一一对应
   （`tools/stage2.cpp:1577-1579`），负例（:486-492）额外断言"被拒配置不得留 `diagnostics.json` 与 `properties`"，
   即"零产物"有可观测证据（同型主张见 `eng/ci/ledgers/aio_io_boundary_inventory.json:2305` 的归因说明）。
3. **帧错位可检出性**：见 :51-53 行的复算（0.57 位移 vs 0.05 阈）⇒ 不报"门恒真"。
4. **`snr_weight_mode` 不是被 A44 删除的口径键**：见该行处置与 `PUBLIC_API.md:2208` 的显式豁免 ⇒ 不报。
   检索式：`git grep -n "snr_weight_mode" -- docs/contracts/PUBLIC_API.md lib/algorithms/coverage/src/stage2_common.cpp`。
5. **置换不变性判据（WIRE-IVAR-005）**：:420-434 用 C,A,B 顺序重跑并断言 `n_perm_diff == 0`（阈 1e-4），
   与 `docs/science/INTEGRATION.md` §7:84 的确定性主张同向，且对实现敏感（帧序相关实现会红）⇒ 合规。
6. **夹具只用二值覆盖（`support∈{0,1}`，:85/:273）不构成本文件的缺陷**，但它是本门的**能力边界**：
   分数覆盖（真实重采样后的 `0<s<1`）下的 ivar 接线在本门不行使；该缺口应记在门的"诚实边界"里而非当 bug 报。
   检索式：`git grep -n "covered_area" -- lib/algorithms/coverage/tests/ivar_wiring_test.cpp`（唯一非二值来源不存在）。
7. **`exit_code` 的 Windows 支路**（:140-141 直接返回 `st`）与 `ASSERT_EQ(rc, 0)`（:293）不解码裸值：
   两侧对"成功=0"的判断在两种平台上都是 0，不构成分歧 ⇒ 不报。

---

# 文件 3 · `lib/algorithms/coverage/tests/kcorr_lookup_test.cpp`（77 行，已全读）

被测实现：`lib/algorithms/coverage/src/sampler.cpp:93-117` 的 `kcorr_lookup`，经内部接缝
`sampler.cpp:1500-1502 astrocs_phase2_kcorr_lookup_for_test` 驱动（同一 TU、同一冻结表，未复制逻辑）。
注册面：`lib/algorithms/coverage/CMakeLists.txt:143 add_executable(phase2_sampler tests/kcorr_lookup_test.cpp)`、
:194-195 `gtest_discover_tests(phase2_sampler ... TEST_PREFIX "phase2_sampler.")`
⇒ ctest 名 `phase2_sampler.kcorr.corner_exact` 等，与 `docs/algorithms/PHASE2_SAMPLER.md:572` 登记的用例名一致（**有注册**）。

## §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :20 `kTab300[3]` | 300″/px 档 k_corr | 1.2112 / 1.3925 / 1.4980 | 角点期望值（pixfrac 0.5/0.8/1.0） | 待确认 | 标定件在跟踪面：`lib/algorithms/drizzle/healpix_drizzle/tests/kcorr_matrix_test.cpp:1-12`（声明"证据写入 `reports/v19r3/evidence/science/kcorr_matrix.json`"）；**结果位不在跟踪面**（`git ls-files \| grep -i kcorr` → 仅两个 .cpp）；跟踪面唯一数字记录 = `docs/algorithms/PHASE2_SAMPLER.md:322` "lookup 表 :96-99 冻结数值 {1.2112,1.3925,1.4980 \| 2.3958,2.8971,3.2035}"（**同六个数的转述**）。检索式：`git grep -n "1.2112\|1.3925" -- 'docs/**' 'lib/**'` → 只有 PHASE2_SAMPLER.md:322 + sampler.cpp:97 + 本文件。保守方向：无法从本门判定数值正确性（见 CR-22-12） | PARTIAL |
| :21 `kTab600[3]` | 600″/px 档 k_corr | 2.3958 / 2.8971 / 3.2035 | 同上 | 待确认 | 同上（合并：六个值同族同处境，位点 :20 与 :21 各三个，另在 :43/:44/:50-56/:68/:75/:76 以字面量重复 12 次） | PARTIAL |
| :22 `kPf[3]` | pixfrac 标定列 | 0.5 / 0.8 / 1.0 | 网格列 | 结构性不适用 | 与实现 `sampler.cpp:94 pf_grid` 同值；数组维度 + 标定网格定义，非科学量 | — | CONFIRMED |
| :23 `kSc[2]` | scale 标定档 | 300.0 / 600.0 | 名义"标定域" | 结构性不适用 | 与 `sampler.cpp:95 sc_grid` 同值；**本文件其余断言全部改写死 300.0/600.0 字面量，`kSc` 零引用**（`git grep -n "kSc" -- <本文件>` 仅 :23 一处）⇒ 见 CR-22-12 | — | CONFIRMED |
| :33 `i < 3` | 角点循环上界 | 3 | pixfrac 列基数 | 结构性不适用 | 枚举/数组基数，非科学量 | — | CONFIRMED |
| :43-44 `1.3925` / `2.8971` | 生产默认 pixfrac=0.8 的表列值 | 同 :20/:21 | 锁"默认 pixfrac 命中表列" | 待确认 | 同 :20/:21（值本身＝标定测量，跟踪面无原始记录） | PARTIAL |
| :43/:45 0.8 | 生产默认 pixfrac | 0.8 | 默认档 | 文献值→不适用，取**项目定义值**：`docs/science/DRIZZLE.md:26` "`pixfrac` …（`drizzle.pixfrac`，默认 0.8）"、`docs/contracts/CONFIG_CONTRACT.md:81` "数值默认 0.8 落 defaults.json 的 `drizzle.pixfrac`"；负责人已裁 0.8（项目记忆：Drizzle pixfrac default） | 处置记为 `公式导出`（默认值由合同导出，非科学量）；本门只消费它作探针位置 | CONFIRMED |
| :49 `tol = 1e-12` | 插值点容差 | 1e-12 | "rtol 1e-12" 的实现 | 公式导出（**但写法与条文量纲不符**） | 条文：`docs/algorithms/PHASE2_SAMPLER.md:572` "角点值 exact；插值点 **rtol 1e-12**"；`EXPECT_NEAR` 是**绝对**判据 ⇒ 见 CR-22-13 | — | CONFIRMED |
| :50-56 `/ 2.0`、`0.5 *`、`0.65`、`0.90`、`450.0` | 两段中点探针 | 0.65/0.90/450 = 网格中点；2.0/0.5 = 中点平均系数 | 双线性中点参考 | 公式导出 | 中点定义：0.65=(0.5+0.8)/2、0.90=(0.8+1.0)/2、450=(300+600)/2；期望式 = 相邻两列均值的均值 = 双线性中点，与实现 `sampler.cpp:110-116` 同式；系数 2.0/0.5 为单位换算，非科学量 | — | CONFIRMED |
| :60 "标定域 = scale ∈ [300,600]″" | 标定域 | [300, 600] | 查表域上下界 | 公式导出（本门只引用） | 实现 `sampler.cpp:95/:101`；合同 `docs/contracts/DATA_SEMANTICS.md:1739` "源像素角尺度 ∈ [300,600]″/px（= 逐帧查表域）"；`lib/infrastructure/aio/include/aio_hips.h:98-99` "k_corr 的 [300,600] 适用域由 sampler 按 ALG-P2-SURF-UPM 显式拒绝域外值"——三处一致 | — | CONFIRMED |
| :66 `0.2` / :67 `2.0` | pixfrac 域外探针 | 0.2、2.0 | 断言"pixfrac 域外 ⇒ 取表列端点" | **待确认** | 与条文冲突：`DATA_SEMANTICS.md:1739` 明文 pixfrac 适用域 `[0.5,1.0]` 且"**域外（含 NaN/未知）一律取冻结默认 1.4；域外取值面 = 该冻结默认本身**（域外 clamp 会把一个与帧无关的表值当成逐帧标定）"，`PHASE2_SAMPLER.md:572` 同（"域外回退冻结默认 1.4（**禁 clamp**）"）⇒ 见 CR-22-11。保守方向：把行为改成域外回退 1.4 会让本门这两行判红（正是"改对反而判红"形态） | CONFIRMED |
| :64 注释 "pixfrac 维仍按标定列 clamp（表列有界性保留）" | 自述依据 | — | 为 clamp 辩护 | 待确认 | 条文面零提及该豁免，检索式：`git grep -n "pixfrac" -- docs/contracts/DATA_SEMANTICS.md` → :1739 给的是"域外一律 1.4"，无任何 pixfrac 维 clamp 例外 ⇒ CR-22-11 | CONFIRMED |
| :69 `1.4`（及 :70/:71/:72/:73 同值） | 冻结默认 k_corr | 1.4 | 域外回退值 | 实验标定 | 锚：`docs/contracts/DATA_SEMANTICS.md:1739` "MC 标定 `UPMW-005 control_median_mc_test`（当前 Drizzle 引擎、pixfrac=0.8 生产默认、2000 次实现）实测 k_corr_empirical = 1.3883、N_eff ≈ 181 < N_retained = 251；取 1.4 ≥ 1.3883（保守侧）"；实现 `sampler.cpp:79-83 kControlCorrDefault = 1.4` 同锚；标定件在跟踪面（`eng/tests`/`lib` 内 UPMW-005 用例族），数值方向为保守侧 | — | CONFIRMED |
| :69-70 `100.0` / `900.0` | scale 域外探针 | 100、900 | 两侧越界各一例 | 结构性不适用 | 只需在域外，取值无科学语义 | — | CONFIRMED |
| :71 `0.9586` | 生产真帧角尺度探针 | 0.9586″/px | 证明真帧恒域外 | 实验标定 | 多处一致：`docs/science/ASTROMETRY.md:223`（T2/T3 原生采样 0.9586/0.9669″/px）、`lib/algorithms/projection/p3_wcs.cpp:207/:220`、`sampler.cpp:90` ⇒ 有跟踪面锚 | — | CONFIRMED |
| :72 `0.009856` | "provenance 生产者实写值" | 0.009856″ | 另一域外探针 | 待确认 | **全仓唯一出现处就是本行**（检索式 `git grep -n "0.009856" -- 'lib/**' 'docs/**' 'eng/**'` → 仅 `kcorr_lookup_test.cpp:61/:72`）⇒ "生产者实写"这一事实表述无第二处佐证；按负责人"三留一不留"（运行产物一律不留）该实测值本就无跟踪面落点 ⇒ 只登记为注释不可复核，不升格缺陷。影响范围：仅本探针（任何域外值都返回 1.4，判据不受它取值影响） | CONFIRMED（无锚这一事实） |
| :73 `299.999` | 域内侧边界探针 | 299.999 | 证明下界开区间外即回退 | 结构性不适用 | 与 :101 `sc >= 300` 的边界配合；**上界侧对称探针（600.001）缺失** ⇒ 见"边界" | — | CONFIRMED |
| :34/:36/:43/:44/:50/:51/:55/:66/:67/:75/:76 的 `300.0`/`600.0` 字面量 | 表列档 | 300/600 | 与被测实现同值 | 结构性不适用 | `sampler.cpp:95 sc_grid`；本应引用 :23 的 `kSc` 而写死 ⇒ CR-22-12 | — | CONFIRMED |
| :65/:32/:42/:48 TEST 名与用例数（4 个 TEST） | 用例基数 | 4 | — | 结构性不适用 | 用例枚举基数 | — | CONFIRMED |

表 16 行；文件内全部数值常数/阈值/网格值/探针值已列（六个表值合并列出并标明被合并位点）。

## findings

### CR-22-11 · 本门把 pixfrac 维的**静默 clamp** 断言为期望，与正本合同"域外一律取 1.4、禁 clamp"直接相反
- 轴：C（主轴：代码/门 vs 合同与算法文档）+ R（取值面被替换）
- 位点：`lib/algorithms/coverage/tests/kcorr_lookup_test.cpp:66-67` ——
  `EXPECT_EQ(lookup(0.2, 300.0), lookup(0.5, 300.0));` / `EXPECT_EQ(lookup(2.0, 600.0), lookup(1.0, 600.0));`
  以及自述依据 :64 `// pixfrac 维仍按标定列 clamp（表列有界性保留）。`
  被测实现（同一表达式族）：`sampler.cpp:103` `const double pf = std::clamp(pixfrac, 0.5, 1.0);`
- 上位依据（原文短句）：
  - `docs/contracts/DATA_SEMANTICS.md:1739`（`control_k_corr` 行）——
    "**适用域（正向约束）**：Drizzle 核 `pixfrac ∈ [0.5, 1.0]` 且源像素角尺度 `∈ [300, 600]″/px`（= 逐帧查表域）；
    **域外（含 NaN / 未知）一律取冻结默认 1.4；域外取值面 = 该冻结默认本身**（域外 clamp 会把一个与帧无关的表值当成逐帧标定）"；
  - `docs/algorithms/PHASE2_SAMPLER.md:572`（§11.3 F2 行）——"角点…九值、**域外回退冻结默认 1.4（禁 clamp）**、provenance 缺失/尺度未知回退 1.4"，判据列"域外 == 1.4 exact"；
  - `docs/science/DRIZZLE.md:199` —— "`pixfrac` … 非法值显式 `NO_DATA`（§4），**不静默夹逼**"；
  - 实现自己的注释 `sampler.cpp:89-90` —— "**域外不得 clamp**：域外静默 clamp 会把「恒取 300 档」固化成合同，并把一个与帧无关的表值当成逐帧标定"（同一理由只被用在 scale 维）。
  本门用例名本身即 `domain_fallback_to_frozen_default_not_clamp`（:65）。
- 现状：scale 维域外 → 返回 1.4（:69-73 断言，实现 `sampler.cpp:101-102` ✓ 合规）；
  pixfrac 维域外 → 被 `std::clamp` 夹到表列端点，本门把该行为写成期望（0.2 ⇒ 取 0.5 列值 1.2112；2.0 ⇒ 取 1.0 列值 3.2035），
  且实现对该路径**不打任何诊断**——`sampler.cpp:575-584` 的 stderr 标记只在 `pf > 0 && !in_domain`（scale 域外）分支触发，
  pixfrac 越界时 :576 直接静默取值。
- 差在哪：合同把"域外取值面 = 1.4 本身"写成对所有维度（含 pixfrac）的**正向约束**，并点名 clamp 就是被禁的对象；
  本门反过来把被禁形态锁成绿灯期望。
- 后果（方向钉死）：pixfrac<0.5 的帧（合同与 schema 允许：`CONFIG_CONTRACT.md:81` "schema 机器强制 `0 < pixfrac <= 1`"，
  且 `docs/science/DRIZZLE.md:132` 的守恒门明列 `pixfrac∈{0.1,0.5,0.8,1.0}` 为受支持取值）会拿到 0.5 列值 1.2112 而非合同的 1.4。
  `control_variance = k_corr × (π/2) × σ² / N_retained`（`sampler.cpp:638`）⇒ 控制点方差被**低估 13.5%**（1.2112/1.4−1），
  即 UPM 零点不确定度偏乐观 ⇒ 下游按 ivar 加权时该帧被过度信任 ⇒ 方向 **偏绿**（低估不确定度只会让一致性判据更容易通过）。
  pixfrac>1 一侧（生产不可达，见下）取 3.2035 > 1.4 ⇒ 反向偏保守，但同样违反"域外取值面 = 1.4"。
- 定级：S2。生产触发路径逐跳：`defaults.json drizzle.pixfrac`（默认 0.8，域内）→ 帧 properties 的
  `ASTROCS_DRIZZLE_PIXFRAC`（`sampler.cpp:135-137` 只接受 `0<v<=1`，故 0.1/0.4 类合法值**会**被采纳）→
  `sampler.cpp:574-576` 需 `sc ∈ [300,600]` 才调用 `kcorr_lookup` → `sampler.cpp:103` clamp。
  **缺口在倒数第二跳**：`sampler.cpp:90` 与 `docs/science/ASTROMETRY.md:223` 一致给出真帧角尺度 0.9586″ ⇒ 真实数据永远走
  scale 域外分支，故该 clamp 分支在今天的生产数据上不可达，只在使用合成/标定帧（scale 落在 [300,600]″）时可达 ⇒ 不成立 S1。
  但"门与正本合同相反"这一条与可达性无关：改正实现（`sampler.cpp:103` 改域外回退）会**立刻**使本门 :66-67 判红。
- 怎么算修好：以合同为准改**实现与本门两处**（不得顺门改合同）：
  (1) `sampler.cpp:93-103` 增加 pixfrac 维域判定：`!(pixfrac >= 0.5 && pixfrac <= 1.0)` ⇒ `return kControlCorrDefault;`
  （并像 scale 维一样打 stderr 具名标记）；(2) 本门 :66-67 改为
  `EXPECT_EQ(lookup(0.2, 300.0), 1.4); EXPECT_EQ(lookup(2.0, 600.0), 1.4);`，并删/订正 :64 那句自述依据；
  (3) 必须同批核对 `docs/algorithms/PHASE2_SAMPLER.md:572` 的"九值 + 域外"表述是否需补 pixfrac 维两例；
  (4) 防"改对反而判红"：全仓检索是否有其它期望 pixfrac clamp 的用例
  ——`git grep -n "kcorr_lookup\|astrocs_phase2_kcorr_lookup_for_test" -- 'eng/tests/**' 'lib/**'`（当前命中：本门 + `sampler.cpp` 自身 + 接缝声明），
  确认无第三处依赖。
- 置信：CONFIRMED（合同、算法文档、实现、门四层原文逐字比对；13.5% 为手算 1.2112/1.4）

### CR-22-12 · 所谓"独立硬编码参考"与被测实现同源，六个冻结表值在跟踪面无原始测量记录，本门对"数值出处"零鉴别力
- 轴：S（主轴，常数出处）+ C
- 位点：`lib/algorithms/coverage/tests/kcorr_lookup_test.cpp:47`（`// F2：两段线性插值中点（**独立硬编码参考**，rtol 1e-12）。`）
  与 :20-23 的期望常量、:43/:44/:50-56/:75/:76 的 12 处字面量重复
- 上位依据：前言 §2.5 处置档 3（"`实验标定` —— 给标定件路径与**结果位**（必须落在跟踪面，指 `run/` 即判不合格）"）；
  `lib/algorithms/drizzle/healpix_drizzle/tests/kcorr_matrix_test.cpp:8-11` 自述"证据写入
  `reports/v19r3/evidence/science/kcorr_matrix.json`"；`docs/algorithms/PHASE2_SAMPLER.md:322` 只**转述**同一组六个数。
- 现状：期望值 = 实现表值的字面量副本（`sampler.cpp:96-99` ≡ 本文件 :20-21），二者在跟踪面互校即全绿；
  原始标定输出（JSON）不在跟踪面（`git ls-files | grep -i kcorr` 只命中两个 .cpp），
  故本门能证"实现与测试里的数字一致"，**不能证**"数字与实测一致"。
  附带：:23 定义的 `kSc` 在本文件零引用，300/600 在 12 处写死，标定域改界时门不随动。
- 差在哪：文件注释把"复制同一组常数"表述成"独立参考"，而独立性恰是这一族判据的立论前提；
  常数出处链条在"实测 → 文档转述 → 代码字面量"三跳中只覆盖了后两跳，且后两跳内容相同 ⇒ 无交叉验证。
- 后果：`ALG-P2-SMP-001 §11.3 F2` 的对外主张"k_corr 逐帧查表值已按实测标定并冻结"，其门证部分实为自证。
  方向性：**无偏绿/偏红可言**（门与被测同源 ⇒ 同错同对），所以本条只主张"该门无出处鉴别力"，不主张误绿。
- 定级：S3（无已知数值错误；出处链缺口）
- 怎么算修好：(1) 把标定实测结果落到跟踪面并给可复现命令（按负责人"三留一不留"：日志与报告数据落 `实验/`，运行产物不留），
  或在 `PHASE2_SAMPLER.md:322` 旁登记标定用例的**可复跑命令 + 种子 + 期望输出文件哈希**；
  (2) 本文件把 12 处字面量改为引用 `kTab300/kTab600/kSc`（一处定义），并对 :47 的"独立"字样给出独立性来源；
  (3) 顺带补 CR-22-11 的修复用例与两条缺口：`scale = 600.001` 的域上界外探针（当前只测了下界外 :73）、
  以及 `pixfrac = NaN` 的显式期望（合同 :1739 明写"域外（**含 NaN** / 未知）一律 1.4"，本门无 NaN 用例；
  实现侧 `std::clamp(NaN,0.5,1.0)` 返回 NaN ⇒ 若 scale 在域内而 pixfrac 为 NaN，`kcorr_lookup` 会返回 NaN，
  与合同相反；该路径从 provenance 不可达（`sampler.cpp:137` 的 `vf>0.0 && vf<=1.0` 域门 + :575 的 `pf > 0.0` 双保险），
  故属公开 C 调用面的守卫缺口，补用例即锁住）。
- 置信：CONFIRMED（跟踪面检索式已给；NaN 传播路径按 `std::clamp` 定义逐步推演，未实跑）

### CR-22-13 · 条文冻结的是 `rtol 1e-12`，门实现成绝对 1e-12，比冻结容差更严（可造成误红）
- 轴：C（主轴，合同↔实现量纲）
- 位点：`lib/algorithms/coverage/tests/kcorr_lookup_test.cpp:49` —— `const double tol = 1e-12;`（用于 :50-56 的 `EXPECT_NEAR`）
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:572` F2 行判据列 —— "角点值 exact；**插值点 rtol 1e-12**；域外 == 1.4 exact"；
  项目对"绝对量容差不声明标度"的立场见 `docs/science/ACR_EQUIVALENCE.md:140`。
- 现状：`EXPECT_NEAR(a, b, tol)` 判的是 `|a-b| <= tol`（绝对），而条文写的是相对容差。
  在 600″ 档（k≈2.4–3.2）处，绝对 1e-12 相当于相对 ~3.1e-13–4.2e-13，比冻结值严 **2.4–3.2 倍**。
- 差在哪：门比权威规定的判据更严，且把相对量偷偷写成绝对量而没声明信号标度。
- 后果：任何实现侧合法的浮点结合序改动（例如 `sampler.cpp:116` 先做行插值再做列插值，二者浮点结果可在 1e-16 相对差）
  在 600″ 档仍安全，但若有人改用 `fma`/长双精度或改变 `r0` 的算法使残差升到相对 5e-13（绝对 ~1.6e-12）
  ⇒ 门判红而合同判绿。方向：**偏红**（收紧判据只可能造成误红，不会造成误绿）。
- 定级：S3
- 怎么算修好：改用相对判据 `EXPECT_LE(fabs(a-b), 1e-12 * fabs(b))`（或在 :49 显式声明"绝对量，按最大表值 3.2 折算的相对等效值"），
  并在 :6-7 的头注释里把 exact/rtol 两条冻结容差的量纲写清；必须同批：确认改后当前实现仍绿（先算实测残差再改判据）。
- 置信：CONFIRMED（`EXPECT_NEAR` 语义与数值折算为定义级；未实跑取实测残差 ⇒ 残差量级为推演）

### 不报的核过项（附检索式）
1. **角点 exact 成立**：`sampler.cpp:107-116` 在 pf=0.8 时 `lo=true ⇒ c0=0,c1=1, fx=(0.8-0.5)/(0.8-0.5)=1.0`（同一 FP64 表达式相除 ⇒ 精确 1.0），
   `w0=0` ⇒ `k_r0 = k[0][0]*0 + k[0][1]*1` 精确等于表值；scale 端点由 :112-113 的权重式 `(600-sc)/300` 给 r0=1/0 精确 ⇒
   :32-39 的 bitwise `EXPECT_EQ` 有实现支撑，不是碰巧。检索式：`git grep -n "r0\|fx\|w0" -- lib/algorithms/coverage/src/sampler.cpp`。
2. **门走的是生产实现不是复制逻辑**：接缝 `sampler.cpp:1498-1502` 直接调用匿名命名空间的 `kcorr_lookup`（同一 TU、同一表）⇒
   本门的"接线"是产品实现而非夹具替身（这正是派单问③要查的形态，此处合规）。
3. **F2 主判据落在生产不可达分支不是缺陷**：真实帧角尺度 0.9586″ ∉ [300,600] ⇒ 生产恒走 1.4 回退，
   插值分支只为标定域内的帧服务；该事实由 `sampler.cpp:86-92`（"scale 维 RETIRED"）与本门 :59-63 明示，属**如实登记的适用域**，
   不报为"门只测死代码"。
4. **域上界只测了下界（:73 有 299.999，无 600.001）**：`sampler.cpp:101` 的守卫
   `!(scale >= sc_grid[0] && scale <= sc_grid[1])` 对两侧同一表达式，单侧探针即可代表两侧 ⇒ 不作为缺陷报，
   仅列入 CR-22-12 的补测建议。
5. **PHASE2_SAMPLER.md 内部指针不一致**：:572 写"（表值 :94-97）"而表实际在 `sampler.cpp:96-99`（:322 写的正是 :96-99）⇒
   同一文档两处指针差 2 行。属文档面漂移，且本批不清单含该文档 ⇒ 移交线索（见末节），不作为本批 finding。
6. **`kSc` 未引用**（:23）不单列 finding，已并入 CR-22-12 的现状与修法。

---

# 文件 4 · `lib/algorithms/coverage/include/astro/phase2/rejection.h`（604 行，已全读）

对照实现：`lib/algorithms/coverage/src/rejection.cpp`（本批文件 5，默认值块 :1236-1269、路由 :1271-1289、
小 N 决策点 :1144-1158、继承守卫 :2460-2475）。
对照科学正本：`docs/science/REJECTION.md` §5（:51/:63-64/:98-101）。

## §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :45-63 `P2RejectionMethod` 序号 | 方法枚举 | 0..11 | 方法编号 | 结构性不适用 | 枚举基数与编号；:46-55 每个都配 :66-77 的 canonical semantic id，编号本身无语义（`P2_REJECT_AUTO=10` 只在 planning 层、`:56` 明说"永不进入 kernel"） | — | CONFIRMED |
| :46-52/:66-77 semantic id 版本串 | `..._siril_1_4_3.v1`、`..._rcr_2_4_7_ss_median_dl.v1` | Siril 1.4.3 / RCR 2.4.7 | 参考实现版本 pin | 文献值 | `docs/references/SCIENTIFIC_REFERENCES.md`（:91 行同类条目给 URL+sha1 的体例）+ `docs/science/REJECTION.md:169`（Siril 1.4.3 `src/stacking/rejection_float.c:31-44` 逐式引用）+ rejection.h:26-27 "官方固定版本（ORACLE ONLY）" | — | CONFIRMED |
| :81 `P2_PROFILE_WBPP_2_9_1` | profile 名 | "wbpp_2_9_1" | 对照档名 | **待确认** | 名声称 2.9.1，但其档界/守卫依据一律引 WBPP **2.5.9**（:84-88、:253-254、:264-265；`docs/contracts/DATA_SEMANTICS.md:1195` "对照档 = 本仓冻结解析表，档界取自 WBPP 2.5.9"；`SCIENTIFIC_REFERENCES.md:91` "WBPP ≥2.6 源码随商业安装分发、公开不可核验"）⇒ 见 CR-22-17 | CONFIRMED（版本错配事实） |
| :82 `P2_PROFILE_WBPP_CURRENT` | alias | = wbpp_2_9_1 | "current" 指向冻结档 | 结构性不适用 | `rejection.cpp` resolve 表把 nullptr/wbpp_current 都路由到 wbpp_2_9_1（`docs/algorithms/PHASE2_REJECTION.md:220`） | — | CONFIRMED |
| :86-89 档界 N 值 | `N<6 → percentile`、`N ≥ 6 → winsorized_sigma` | 6 | 生产档档界 | 文献值（档界）+ 待确认（下界表述） | 档界取自 WBPP 2.5.9 `bestRejectionMethod`（engine.js:1421-1429，包 sha1 `712cc7c3fdb523643ad0e685104592d511996f82`，见 `docs/references/SCIENTIFIC_REFERENCES.md:91`）；M3 改档依据 `docs/science/REJECTION.md:63-64/:98`；**但 :86 写"N<6 → percentile"、正本写 `4 ≤ n ≤ 5 → percentile` 且 `1 ≤ n ≤ 3 → none`** ⇒ 见 CR-22-15 | — | CONFIRMED |
| :89 `run/REJECT-DOCFIX-01/REPORT.md` | 实测依据位 | — | M3 改档的实测出处 | **待确认** | 前言 §2.5 档 3 明文"结果位必须落在跟踪面，指 `run/` 即判不合格"；`run/` 亦为本仓 gitignore 面（AGENTS §7）⇒ 见 CR-22-16 | 保守方向：证据不可复核 ⇒ 该改档判定按"未证"处理 | CONFIRMED |
| :99-104 `P2RejectReason` | reason 枚举 | 0..3 | 每样本判定 | 结构性不适用 | 枚举基数；语义见 :100-103 与 `docs/science/REJECTION.md` §4 | — | CONFIRMED |
| :107-116 `P2RejectStatus` | status 枚举 | 0..7 | 栈级状态 | 结构性不适用 | 枚举基数；:111 "候选栈含非 finite（资格层后不应出现）" 与 :244 "n=1 由 minimum_n 拦下" 为文字约束 | — | CONFIRMED |
| :119-123 `P2RejectionNormalization` | 归一化枚举 | 0/1/2 | 判定工作域 | 结构性不适用 | :122 `value / max(|median|, floor)` 的 floor 见 :214 | — | CONFIRMED |
| :127-129 `P2SigmaParams` 默认 | lower_sigma/upper_sigma/max_iterations | 4.0 / 3.0 / 8 | σ 阈值与迭代上界 | 公式导出（继承） | `rejection.cpp:9` 与 :1246-1250 逐字段一致（sigma/winsorized/averaged/median_sigma 全用 4.0/3.0/8）；继承链 :464-466 `FZ-REJ-INHERITED-THRESH`；迭代上界 8 属"实现选择的上界"，与 WBPP clip 迭代同值 | — | CONFIRMED |
| :133-135 `P2LinearFitParams` 默认注释 | lower/upper | **4.0 / 3.0** | 声称的结构体默认 | **待确认（与本文件 :464 与实现冲突）** | 同文件 :464 给的是 "linear_fit **5.0/3.5**/8"；实现 `rejection.cpp:1251` `p.linear_fit.lower = 5.0; p.linear_fit.upper = 3.5; // WBPP Light 默认`；守卫 `rejection.cpp:2468-2469` 按 5.0/3.5 判定"是否被改动" ⇒ 见 CR-22-14 | 收紧方向：按 :133 填 plan 的调用方会被自己的守卫判 `invalid_configuration` ⇒ **偏红**（但属文档误导） | CONFIRMED |
| :139-140 `P2EsdParams` 默认 | alpha / max_outliers | 0.05 / 10 | ESD 显著性水平与最大离群数 | 文献值 | `docs/science/REJECTION.md`（generalized_esd_nist.v1 条目）+ NIST/SEMATECH e-Handbook §1.3.5.17.2；实现 `rejection.cpp:1253` 同值 | — | CONFIRMED |
| :144-145 `P2PercentileParams` 默认注释 | low_fraction / high_fraction | **0.1 / 0.1** | 声称的结构体默认 | **待确认（与本文件 :464、正本与实现冲突）** | 同文件 :464 "percentile **0.2/0.1**"；`docs/science/REJECTION.md:64/:101/:119` 明文 `low 0.2 / high 0.1`；实现 `rejection.cpp:1254` `p.percentile.low_fraction = 0.2; ... = 0.1;` ⇒ 见 CR-22-14（**低侧注释错**，高侧对） | 同 CR-22-14：按 :144 填 ⇒ 与守卫不一致 ⇒ 偏红/语义偷换（0.1 带比 0.2 带窄一倍 ⇒ 更容易全拒） | CONFIRMED |
| :149-151 `P2MinmaxParams` 默认 | reject_low/high_count / min_kept | 1 / 1 / 4 | 一次性剔上下各 1、至少留 4 | 文献值（IRAF pclip/minmax 族）+ 结构性（min_kept 与 `:211` minimum_n 配合） | `docs/science/REJECTION.md` §5 minmax 条目（"AUTO 禁止产出 min/max"）；实现 `rejection.cpp:1257-1258` 同值 | — | CONFIRMED |
| :155 `P2RcrParams.technique` | 唯一取值 | 0 = SS_MEDIAN_DL | RCR 技术枚举基数 1 | 结构性不适用 | 冻结唯一支持（:155 注释 + `rejection.cpp:1259`）；语义出处 = RCR 2.4.7（:52/:72） | — | CONFIRMED |
| :172-175 `P2LargeScaleParams` 默认 | enabled/min_structure_pixels/low/high grow | 0 / 8 / 2 / 2 | 大尺度生长结构闸与半径 | 文献值（默认关闭）+ 待确认（8/2/2 的数值出处） | :168-170 自述"WBPP 2.5.9 largeScaleClipLow/High 默认 false（engine.js:11901-11902，包 sha1 712cc7c3…）→ 默认 enabled=0"；`min_structure_pixels=8`、`grow=2` 在 :464-466 登记为"继承 ALG-REJ-001"，实现 `rejection.cpp:1260-1263` 同值。**注**：8 与 2 属"结构最小像素数/邻域半径"，是 WBPP 语义移植的取值而非本仓可推导量 ⇒ 沿用"继承"档；本批未在 `docs/science/` 检到 8/2 的独立推导（检索式：`git grep -n "min_structure_pixels" -- docs/science/`） | PARTIAL |
| :186-:188 `k = Φ⁻¹(1 − α/(2N))`、α=0.05 | Bonferroni 临界值式 | 0.05 | 极值检验阈 | 公式导出 | NIST/SEMATECH §1.3.5.17.1（Grubbs / 已知方差单离群，:178-179 引用）；式即 `z_i > ±k` 判 HIGH/LOW；实现走 Acklam 逆正态（`rejection.cpp:111-123` 系数族），代码按式 | — | CONFIRMED |
| :194-195 "n=2 时栈中位数落在两样本之间…全拒 → 走 n<=4 全接受容错" | 容错档 | n<=4 | 全拒后的回退闸 | 公式导出（文件 5 已核对闭合） | 实现 `rejection.cpp:2205-2215`（`accepted_count==0 ∧ n<=4 ⇒ UNDERDETERMINED 全接受`）与正本 `docs/science/REJECTION.md:49-51`"全拒容错域：`n = 4` ∧ 方法核全拒 ⇒ 降级 `UNDERDETERMINED` 全接受"同式；正本"n≥5 全拒仍 `ALL_REJECTED`"讲的是 n≥5 侧，与本行不构成互斥 ⇒ 我一度读作互斥，文件 5"不报的核过项"2 给出等价性论证（n≤3 被 :2087 内核闸截走 ⇒ `n<=4` 与 `n==4` 可达集相同）；本行真正未闭合的是 `rejection.h:212` 的"默认 2"与生产档 3 不合 ⇒ CR-22-19 | — | CONFIRMED |
| :201-205 `P2ExtremeValuePriorSigmaParams` 默认 | alpha / prior_sigma / prior_sky / center_mode | 0.05 / （>0 调用方）/ （NaN=未提供）/ 1 | 先验极值检验参数 | 公式导出 | α 冻结 0.05（`rejection.cpp:1264` 同）；prior_sigma 缺失 ⇒ UNDERDETERMINED（:196-197 fail-closed 条文 + `rejection.cpp:1265` `= 0.0; // 0 = 未提供 → kernel fail-closed`）；center_mode 默认 1 = `rejection.cpp:1268` | — | CONFIRMED |
| :211 `minimum_n` | 方法注册表 minimum N | — | 不足 → UNDERDETERMINED | 结构性不适用 | 各方法的 N 来自 `docs/science/REJECTION.md` §5 适用域；实现见 rejection.cpp 方法表（文件 5 核对） | — | CONFIRMED |
| :212 `underdetermined_n` 默认 | 2 | 2 | 内核小样本闸 | 待确认 | :240-244 给出分档默认（wbpp/adaptive=2、adaptive_pixel=3、extreme=1），实现 `rejection.cpp:1236-1242` 与之对生产档一致（pixel_profile ⇒ 3），对照档 2；`docs/science/REJECTION.md:51` 只把 `underdetermined_n = 3` 当**通用**事实陈述 ⇒ 见 CR-22-19（正本/头/实现三面对"哪一档是 3"表述不一） | 若按 :51 理解为"全档 3"，会误判对照档 N=3 不排异（实际排异）⇒ 偏绿的理解 | CONFIRMED |
| :214 `normalization_floor` 默认 | 1e-12 | 1e-12 | MEDIAN_SCALE 的 \|median\| 下限 | 待确认 | 实现 `rejection.cpp:1245` 同值；**条文面零推导**，检索式：`git grep -n "normalization_floor" -- docs/science/REJECTION.md docs/algorithms/PHASE2_REJECTION.md docs/contracts/DATA_SEMANTICS.md`。保守方向：floor 越大 ⇒ 尺度越小被放得越大 ⇒ 判据更松（偏绿）；1e-12 对 ADU/sr 量级（生产 10³）等效于"永不起作用"，即该 floor 只防 0 除，属数值防护而非科学量 | — | CONFIRMED |
| :216-226 plan 的 typed params 成员数 | 11 个方法槽 | — | 禁止跨方法共享 | 结构性不适用 | :125 标题"禁止跨方法共享 low/high/max_iter" | — | CONFIRMED |
| :229 `nominal_n` | 几何 nominal 贡献数 | 0=未记录 | Bonferroni N / 路由依据 | 结构性不适用 | :291-293 与 `docs/science/REJECTION.md` §5 的 n 定义 | — | CONFIRMED |
| :240-244 profile 的 `underdetermined_n` 默认 | 2 / 3 / 1 | 2,3,1 | 分档小样本闸 | 待确认 | 与实现 `rejection.cpp:1236-1242` 一致（生产档 3、opt-in extreme 1、其余 2）；正本 `REJECTION.md:51` 的表述把 3 说成普遍 ⇒ CR-22-19 | — | CONFIRMED |
| :250 对照档表 | `<6` / `6..15` / `>15` | 6、15 | 对照档档界 | 文献值（6/15）| WBPP 2.5.9 `bestRejectionMethod`（`SCIENTIFIC_REFERENCES.md:91`：真表是 `n<6` percentile / `n≤15` winsorized / 否则 ESD）；**本仓 >15 档取 linear_fit 而非 ESD**，:253-254 与 `rejection.cpp:1280-1287` 已如实登记偏离 ⇒ 不报（登记过的偏离），但登记面见 CR-22-17 的版本名问题 | — | CONFIRMED |
| :252 生产档表 | `<6` / `≥6` | 6 | 生产档档界（M3 后） | 文献值 + 待确认 | 档界同 WBPP；≥6 合档依据 `REJECTION.md:98`；**实测依据只在 `run/` ⇒ CR-22-16** | — | CONFIRMED |
| :264-265 "AUTO 禁止产出 min/max 与 NoRejection（WBPP 2.5.9 engine.js:1349-1412）⇒ 命中即 fail-closed（返回非 0）" | 路由禁区 | — | 守卫条文 | 文献值 | `rejection.cpp:1290` 起的守卫（文件 5 核对具体 rc） | — | CONFIRMED |
| :282-284 `p2_rejection_percentile_band_min_n` 返回值语义 | 1 / 4 | 1 = 档含 N≤3（"当前实现"）；4 = N≤3 走 none | 决策点查询语义 | **待确认（与本仓实现相反）** | 实现 `rejection.cpp:1145-1146` `kPixelSmallNPolicy = PixelSmallNPolicy::kConservativeNone; // ← EXP-204 定案` 与 :1151 `return (…== kWbppTable) ? 1u : 4u;` ⇒ 当前返回 **4**；`docs/science/REJECTION.md:63-64/:98` 亦已定为 `1≤n≤3 → none` ⇒ 见 CR-22-15 | 若按头解读"1=当前"，会把生产档 N≤3 误当成 percentile ⇒ 覆盖面被高估（偏绿的理解） | CONFIRMED |
| :286 `工程控制/RELEASE-03/tasks/EXP-204.md` | 待定问题指针 | — | 科学决策点出处 | 待确认 | `git ls-files \| grep -i "EXP-204"` → **零命中**（控制包收口后按 CONTROL_PACK_SPEC §9 清理，AGENTS §7）⇒ 悬空指针；且 :92-95/:266-267 仍称"待 EXP-204 定案"而实现与正本均已定案 ⇒ CR-22-15/CR-22-16 | — | CONFIRMED |
| :320 `support_threshold` 默认 | 0.0（`support > 该值`） | 0.0 | 资格层支撑闸 | 公式导出 | 语义"严格大于 0"= `docs/science/INTEGRATION.md` §4:43 的 `support>0` 同一域；实现核对见文件 5 | — | CONFIRMED |
| :330-333/:371-374 诊断计数 `invalid_*` | u32 | — | 四类不合格计数 | 结构性不适用 | 计数面（与文件 1 的 CR-22-03 对照：本处四类分开计数是"逐条登记"的正例） | — | CONFIRMED |
| :358 `value_dtype` | 0=fp32, 1=fp64 | 0/1 | 输入精度枚举 | 结构性不适用 | 枚举基数 | — | CONFIRMED |
| :386 `data_type` | 0=fp32 源, 1=fp64（仅诊断） | 0/1 | 诊断枚举 | 结构性不适用 | 同上 | — | CONFIRMED |
| :406 "kernel 内 n<=64 使用固定 scratch；>64 走堆" | 64 | 64 | 栈上缓冲上限 | 结构性不适用 | 缓冲区尺寸（须与实现核对，见文件 5 的 scratch 常量） | — | CONFIRMED |
| :440 `min_samples` | 兼容语义 | — | 候选数 < 该值 → MIN_SAMPLES | 结构性不适用 | :109 `P2_STATUS_MIN_SAMPLES` 兼容旧语义 | — | CONFIRMED |
| :449 "P2RejectStatus（兼容旧 0..4 数值）" | 旧值域 | 0..4 | COMPAT 层数值域 | 结构性不适用 | 与 :107-116 的 0..7 对照：旧面只暴露 0..4，新面加 5/6/7 ⇒ 兼容映射须防 5..7 泄漏（实现核对见文件 5） | — | CONFIRMED |
| :464-466 继承阈值清单 | sigma 4.0/3.0/8、linear_fit 5.0/3.5/8、percentile 0.2/0.1、ESD 0.05/10、large_scale 8/2/2 | 同左 | "本层不得改动（FZ-REJ-INHERITED-THRESH）" | 公式导出（继承表） | 与 `rejection.cpp:9` 与 :1246-1263 逐字段一致 ⇒ **本行是对的，:133/:144 两处结构体注释与它打架**（CR-22-14） | — | CONFIRMED |
| :470-478 `P2RejectClass` | 0..6 | 7 类 | 污染机制分类 | 结构性不适用 | 枚举基数；类语义在 :471-477 行内注释（与 `DESIGN-P2-001 §5`/`ADJ-GEN-02` 对应） | — | CONFIRMED |
| :479 `P2_REJECT_CLASS_COUNT` | 7 | 7 | 数组列宽 | 结构性不适用 | = 枚举基数（0..6）；:560 `class_probability` 的 `count × 7` 用它 | — | CONFIRMED |
| :491-493 `P2_NOISE_PHASE1_DECLARED/UPM_DECLARED/REQUIRED` | 0x1 / 0x2 / 0x3 | 位掩码 | σ_eff 声明位（缺一 fail-closed） | 结构性不适用 | :458-460 条文"禁止用裸原始残差（ALG-P2S-REJ.3，fail-closed）" | — | CONFIRMED |
| :497 `P2_REJ_CALIB_BINMIN` | 50u | 每箱最少样本数 | 校准门 | 待确认 | :495-496 自述"ALG-P2S-REJ.4；状态 PENDING_OWNER_SIGNOFF SO-07…未签字生效前不得放宽（不得改成更松的值）"；条款登记 = `docs/contracts/DATA_SEMANTICS.md:3348-3349` 的 `FZ-AP2S-REJ-CALIB-BINMIN`，SO-07 见 :3388；负责人签字面 = :3118（生效前 fail-closed、实现保持条款原样）⇒ 档 2/3 之间取"合同条款值 + 未签字"，数值本身的推导为零提及 | 保守方向：条文已写死"不得改成更松的值"⇒ 只能收紧（偏红） | CONFIRMED |
| :498 `P2_REJ_CALIB_ABS` | 0.10 | 可靠性最大偏差门 | 校准门 | 待确认 | 同上（`FZ-AP2S-REJ-CALIB-ABS`），并注明与 `:577 P2_CALIB_RELIABILITY_FAIL` 判据 `max\|obs-mean(p)\| > ABS` 的**方向**（大于即失败）一致 | 同上 | CONFIRMED |
| :499 `P2_REJ_CALIB_BSS_MIN` | 0.10 | BSS 下限门 | 校准门 | 待确认 | 同上（`FZ-AP2S-REJ-BSS-MIN`）；判据方向 `BSS <= BSS_MIN ⇒ 失败`（:578）⇒ 严格大于才通过，方向已在条文钉死 | 同上 | CONFIRMED |
| :502 `P2_REJECT_CLASSIFY_PROFILE` | "astrocs.rejection.classify.v1" | v1 | profile 版本化 | 结构性不适用 | 宪章 §6.3 版本化要求（:501 行引用） | — | CONFIRMED |
| :504 `P2_REJ_PROFILE_MOTION_MIN_PX` | 0.5 | 移动源位移门（px） | 分类门 | 待确认 | :529 重复给 "v1=0.5 px"；**本文件内无出处**，`DESIGN-P2-001 §5` 与 `docs/science/REJECTION.md` 是否规定该数需回读（本批检索式：`git grep -n "motion_min_px\|MOVING_SOURCE" -- docs/science/REJECTION.md docs/design/` → 见 CR-22-20 的取证） | 保守方向：门越小 ⇒ 越多样本判移动源 ⇒ 但 :533 `keep_moving_source=1` 默认保留 ⇒ 不删 ⇒ 对覆盖面偏中性、对 reason_class 统计偏"过分类" | PARTIAL |
| :505 `P2_REJ_PROFILE_PSF_ANOMALY_MIN` | 0.2 | PSF 形状异常门（无量纲） | 分类门 | 待确认 | 同上（:530 重复），单位"无量纲相对偏差"已声明 ⇒ 影响范围 = 失焦/拖线类的判定率 | PARTIAL |
| :506 `P2_REJ_PROFILE_CONTAM_PRIOR` | 0.05 | 后验先验 P(污染) | 贝叶斯先验 | 待确认 | :531 重复；先验 0.05 无任何标定/文献锚在本文件内（检索式：`git grep -n "contamination_prior\|污染先验" -- docs/**`）；概率面用途受 :462-463 "probability 仅门/推断，禁止进权重面" 约束 ⇒ 不污染产品字节 | 保守方向：先验越小 ⇒ 判污染越保守（偏红于删除面，偏绿于泄漏面） | PARTIAL |
| :507 `P2_REJ_PROFILE_OUTLIER_KAPPA` | 4.0 | 后验污染膨胀 κ | 概率式参数 | 待确认 | :532 重复；与 :127 的 σ 阈 4.0 同值但**语义不同**（继承 σ 阈 vs 后验膨胀因子），是否同源于"σ 4.0"未说明；`FZ-AP2S-KAPPA-MAX`（`DATA_SEMANTICS.md:3348`）另有 κ 上限条款，本文件未给上限 | 影响范围 = probability 面（不进权重）| PARTIAL |
| :508 `P2_REJ_PROFILE_BIN_COUNT` | 10u | 默认分箱数 | 校准分箱 | 结构性不适用 | 分箱数（与 :599 `bin_count` 形参对应）；与 :497 的 BINMIN=50 合用 ⇒ 10 箱 × 50 = 至少 500 样本才可评估（该耦合是否在条文/文档登记见 CR-22-20） | — | CONFIRMED |
| :533 `keep_moving_source` 默认 | 1 | 移动源不进删除 mask | 默认保留 | 公式导出（默认即条文） | :475 "移动源（科学信号，**默认保留独立层**）" | — | CONFIRMED |
| :564 "recall：拒绝率；**n<=2 全接受 → 0.0 显式**" | n<=2 | 2 | recall 的退化约定 | 待确认 | 与 :212/:240 的 `underdetermined_n`（生产档实际 3）不一致：n=3 时生产档走 `none` ⇒ 也全接受，但本行只登记 n<=2 ⇒ `recall` 的"全接受"判定面与内核闸面不同（见 CR-22-19） | 方向：n=3 时 recall 可能非 0（若按 percentile）⇒ 对照档下 recall 与"全接受"约定不闭合 | CONFIRMED |
| :574-580 `P2RejectCalibrationStatus` | 0..4 | — | 校准状态枚举 | 结构性不适用 | 枚举基数；:577 的判据方向 `>`、:578 的 `<=` 已在条文钉死 | — | CONFIRMED |
| :587 `bss = 1 - BS/BS_ref`、:584 `BS = mean((p-y)^2)` | 定义式 | — | Brier/BSS 定义 | 公式导出 | 教科书定义（Brier 1950 / 严格可靠性），本文件只声明式；实现是否按式在文件 5 核对 | — | CONFIRMED |
| :592 `probability_is_scientific_gate` | 1 当且仅当 status==OK | 0/1 | 概率面资格位 | 公式导出 | :462-463 "probability（仅门/推断，禁止进权重面）"；实现核对见文件 5 | — | CONFIRMED |

表 41 行；本文件内出现的每个数值常数、默认值、阈值、枚举基数、缓冲尺寸与版本 pin 均已列。

## findings

### CR-22-14 · 公共头里两个 typed-params 的"默认值"注释与同一文件另一处继承表和本仓实现同时相反
- 轴：C（主轴，接口自述 vs 同文件条文 vs 实现）
- 位点：
  - `rejection.h:133-135` —— `double lower;          // 低侧因子（默认 4.0）` / `double upper;          // 高侧因子（默认 3.0）`
  - `rejection.h:144-145` —— `double low_fraction;   // 相对 median 低侧小数（默认 0.1 = 10%）`
- 上位依据：同一文件 :464-466 —— "继承阈值（sigma 4.0/3.0/8、**linear_fit 5.0/3.5/8**、**percentile 0.2/0.1**、
  ESD alpha 0.05/max 10、large_scale 8/2/2 等）原样继承 ALG-REJ-001，**本层不得改动（FZ-REJ-INHERITED-THRESH）**"；
  `docs/science/REJECTION.md:64/:101/:119` —— "percentile (low **0.2** / high 0.1, scale=\|median\|)"；
  实现 `rejection.cpp:1251` `p.linear_fit.lower = 5.0; p.linear_fit.upper = 3.5;  // WBPP Light 默认`、
  :1254 `p.percentile.low_fraction = 0.2; p.percentile.high_fraction = 0.1;`；
  守卫 `rejection.cpp:2468-2469` `if (!rej_eq(p.linear_fit.lower, 5.0) || !rej_eq(p.linear_fit.upper, 3.5)) ...`
  （即 `p2_reject_plan_thresholds_inherited`，头文件 :513-515 声明其语义）。
- 现状：头文件在结构体定义处给的默认值，与同文件继承表和实现给出的值不一致：
  linear_fit 低/高侧写成 4.0/3.0（实际 5.0/3.5），percentile 低侧写成 0.1（实际 0.2）。
- 差在哪：这不是"两个适用域"——两处（:464 与 :133/:144）说的都是同一批 typed params 的默认值，
  且 :464 已用"本层不得改动"钉住唯一口径；按 §2 表 C 轴形态"两处入口对同一阈值取不同值"成立。
- 后果：公共头是调用方唯一必读面（`docs/contracts/PUBLIC_API.md` 登记本头为 C ABI）。按 :133 手填 plan 的调用方会
  被本层自己的继承守卫判 0（`p2_reject_plan_thresholds_inherited` ⇒ `P2_STATUS_INVALID_CONFIGURATION`），方向 **偏红**；
  按 :144 把 percentile 低侧填成 0.1 的调用方则得到**比冻结 0.2 窄一倍**的接受带
  （percentile 判据 = `median ± 带`，低侧 0.1 ⇒ 下界 `0.9·median` 高于 0.2 档的 `0.8·median`），
  ⇒ 低侧剔除更易触发 ⇒ 覆盖面偏红、全拒概率上升；而继承守卫若不检查 percentile 就**不会红**
  （守卫实际检查哪些字段是"改对反而判红"的关键，见"怎么算修好"）。
- 定级：S2（接口自述失真 + 可致调用方配置被自己的门误拒；生产路径经 `p2_reject_plan_resolve` 填默认值 ⇒ 产物不受影响，
  故不升 S1）
- 怎么算修好：以 :464 的继承表与实现为准，改 `rejection.h:133-134` 为 `5.0 / 3.5`、:144 为 `0.2 = 20%`；
  必须同批：(1) 复核 `p2_reject_plan_thresholds_inherited`（`rejection.cpp:2460-2475`）是否覆盖 percentile 两个 fraction——
  若未覆盖，则"继承不可改动"这条 FZ 对 percentile 面无守卫，需补；(2) 全仓检索是否有按头注释手填这两值的调用方/用例
  （`git grep -n "linear_fit.lower\|percentile.low_fraction" -- 'lib/**' 'eng/**'`）；(3) 若 `docs/api/PHASE2_API_V1.md`
  或 `docs/algorithms/PHASE2_REJECTION.md` 复制过 4.0/3.0 的 linear_fit 表述，一并订正（防文档间互相引用错值）。
- 置信：CONFIRMED（三处原文逐字比对 + 实现行号；未实跑）

### CR-22-15 · 头文件把路由小 N 决策点的"当前实现"标反，且仍按"待定"表述一个已定案的档界
- 轴：C（主轴，头 vs 实现 vs 科学正本）+ D
- 位点：`rejection.h:282-284` ——
  ```
  // 返回「N<6 → percentile」档的**下界（含）**：
  //   1 = 档含 N≤3（WBPP 档界的对称读法，当前实现）；
  //   4 = N≤3 走保守 none（EXP-204 若定案「保留不排异」时的取值）。
  ```
  同一文件 :58-60（"SD-18 起 n<=3 走 none 保守路径"）、:92-95（"「N<6 档的下界是否含 N≤3」是**单一显式决策点**……待 EXP-204 定案"）、
  :266-267（"EXP-204 定案后一处改"）。
- 上位依据：`rejection.cpp:1144-1146` —— `enum class PixelSmallNPolicy { kWbppTable, kConservativeNone };`
  `static constexpr PixelSmallNPolicy kPixelSmallNPolicy = PixelSmallNPolicy::kConservativeNone; // ← EXP-204 定案；改判对称读法只改本行`，
  :1148-1151 `return (kPixelSmallNPolicy == PixelSmallNPolicy::kWbppTable) ? 1u : 4u;` ⇒ **当前返回 4**；
  `docs/science/REJECTION.md:63-64`（冻结表）—— `1 ≤ n ≤ 3 → none` / `4 ≤ n ≤ 5 → percentile`，:98 ——
  "本表把 `1 ≤ N ≤ 3` 改为 none、percentile 收窄到 `4 ≤ N ≤ 5`"；
  另 :51 亦按已定案表述（"生产档 astrocs_adaptive_pixel 解析为 none（method=0）"）。
- 现状：实现与科学正本都已定案"保守 none"，头文件却 (a) 把未采用的 `kWbppTable`（1）标成"当前实现"，
  (b) 三处仍称该问题"待定/待 EXP-204 定案"，(c) :86 的档界写成"N<6 → percentile"（含 N≤3），
  与正本 `4 ≤ n ≤ 5 → percentile` 不等价。
- 差在哪：同一个决策点在本文件内被写成三种状态（已定案 none / 待 EXP-204 / 当前实现=对称读法），
  且其中之一（:283"当前实现"）与 `rejection.cpp:1145` 直接相反。
- 后果：`p2_rejection_percentile_band_min_n()` 是**对外查询面**（头 :285 自述用途"provenance 如实记录小 N 策略 + 测试锁定路由表下界"）；
  按头解读返回值的人会得出"生产档 N≤3 走 percentile（会排异）"的相反结论 ⇒
  对小 N 像素的**排异能力**判断反了（真实行为是不排异 ⇒ 污染可能泄漏，`REJECTION.md:63` 自述泄漏代价 `N=2` 为 1/2、`N=3` 为 1/3）。
  方向：理解层面**偏绿**（以为排了异）。函数本身返回值正确，故失真只在自述面。
- 定级：S3（对外接口自述失真；行为与正本一致，无产物错值）
- 怎么算修好：以 `rejection.cpp:1145` 与 `REJECTION.md:63-64` 为准，(1) 把 :283-284 的"当前实现"标注改到 4 一侧，
  (2) 删 :92-95/:266-267/:286 的"待定/待 EXP-204"叙述（正本已定案，按负责人"文档只写正向约束、禁历史"口径，
  应写成单一正向路由表），(3) :86 档界改写为 `1≤N≤3 → none；4≤N≤5 → percentile；N≥6 → winsorized_sigma` 三段正向式；
  必须同批：确认是否有测试锁 `p2_rejection_percentile_band_min_n() == 1`（若有为反向锁，需一并改），
  检索式 `git grep -n "percentile_band_min_n" -- 'eng/**' 'lib/**'`。
- 置信：PARTIAL —— 缺"是否有用例/产品面锁过返回值 1"的核对（本批预算内未跑该检索）

### CR-22-16 · 本头的三处"实测/裁决依据"落在不入库面与已清理控制包，冻结判定的出处链在跟踪面断掉
- 轴：S（主轴，出处档 3 不合规）+ C
- 位点：`rejection.h:89` —— `实测见 run/REJECT-DOCFIX-01/REPORT.md）；`；
  `rejection.h:416` —— `/* RETIRED 2026-09-25 (CHK-PROD-WIRING W1): p2_reject_stack_resolve_ex 全仓零消费者声明已撤下；见 run/WIRING-W16-01/REPORT.md。 */`；
  `rejection.h:286` —— `待定科学问题见 工程控制/RELEASE-03/tasks/EXP-204.md。`
- 上位依据：派单前言 §2.5 处置档 3 —— "`实验标定` —— 给标定件路径与结果位（**必须落在跟踪面，指 `run/` 即判不合格**）"；
  AGENTS §7 —— "`run/` 临时产物/日志（gitignore，不入库）"、"`工程控制/` 控制包（收口后按 CONTROL_PACK_SPEC §9 清理）"；
  负责人"三留一不留"口径 —— 运行产物一律不留（⇒ 以 `run/` 为证据位的锚，按其定义必然不可复核）。
- 现状：M3 改档（N≥16 → winsorized）的实测依据、`p2_reject_stack_resolve_ex` 退役的零消费者依据、
  EXP-204 的待定问题原文，三处分别指向 `run/…`（不入库）与 `工程控制/…`（`git ls-files | grep -i EXP-204` 零命中 = 已清理）。
  `rejection.cpp:1165` 同一处 `run/REJECT-DOCFIX-01/REPORT.md` 复现（文件 5 一并登记）。
- 差在哪：本批工作包把"逐个有处置标注 + 结果位落在跟踪面"设为验收门，这三处正是该门的失败形态；
  且其中一处（EXP-204）指向的文件已不存在，另一处的证据按仓规不可能存在。
- 后果：`P2_PROFILE_ASTROCS_ADAPTIVE_PIXEL` 的档界与 `P2_REJECT_EXTREME_VALUE_PRIOR_SIGMA` 的保留语义，
  对外以"已按实测裁决"的姿态出现，但第三方复核拿不到任何数据 ⇒ 复核性主张失真；
  `REJECTION.md:51/:63-64` 已把该判定的**结论**写入科学正本（含数字 `ρ−1 = 0.3%–27%`、`83.5% 像素无输出`），
  这些数字同样只有 `run/` 出处 ⇒ 与"科学结论须可由登记面复现"的口径冲突。方向：无偏红/偏绿可言，属**出处缺失**。
- 定级：S3（与已合案的"证据落 `run/`"同型；本条只登记本批两文件的具体位点，不重开定性）
- 怎么算修好：把三项依据的**报告与数据**按"三留一不留"落到跟踪面（`实验/` 下的实验单元：报告 + 固定 seed 的可复现代码 +
  results 摘要），并把 :89/:416/:286 与 `rejection.cpp:1165` 的锚改指到该单元路径；
  若判定为"不需保留"，则 :89/:416 的措辞降级为"结论已并入 `docs/science/REJECTION.md` §5"，删去不可复核的 `run/` 指针。
  必须同批：`REJECTION.md` 属科学正本，改动走变更流程（AGENTS §5/§8），本批不建议直接编辑。
- 置信：CONFIRMED（跟踪面检索式已给）

### CR-22-17 · 冻结 profile 名声称 WBPP 2.9.1，而全部档界/守卫出处是 WBPP 2.5.9，且 ≥2.6 版本公开不可核验
- 轴：C（主轴，命名声称 vs 出处）+ S
- 位点：`rejection.h:81-82` —— `#define P2_PROFILE_WBPP_2_9_1 "wbpp_2_9_1"` /
  `#define P2_PROFILE_WBPP_CURRENT "wbpp_current"  // = wbpp_2_9_1 alias`
- 上位依据：同文件 :84-85/:253/:264 三处把档界与禁区都记到 **WBPP 2.5.9**
  （`bestRejectionMethod()` engine.js:1421-1429、`rejectionIsGood()` :1349-1412，包 sha1 `712cc7c3…`）；
  `docs/contracts/DATA_SEMANTICS.md:1195` —— "对照档 = 本仓冻结解析表，**档界取自 WBPP 2.5.9** bestRejectionMethod"；
  `docs/references/SCIENTIFIC_REFERENCES.md:91` —— "**可核验版本 = WBPP 2.5.9**……`WBPP ≥2.6` 源码随商业安装分发、公开不可核验"。
- 现状：仓库全档统一使用 `wbpp_2_9_1` 作为对照档标识（`PHASE2_REJECTION.md:212/218/220/454/637`、
  `REJECTION_ALGORITHMS.md:37/56/82`、`PIPELINE.md:52`、`PHASE2_MOSAIC_WRITE.md:82`），
  但在跟踪面找不到任何一条 WBPP **2.9.1** 的出处（无 URL/sha1/文件行号），也没有"2_9_1 命名由来"的说明。
- 差在哪：一个版本化标识（宪章 §6.3 要求方法/参数版本化的产物）指向一个本仓声明为"公开不可核验"的版本，
  而实际实现与全部证据对应的是另一个版本（2.5.9）——版本号即出处，出处为空。
- 后果：对外主张"对照档 = WBPP 2.9.1 的行为"不可复核且与其自证的 2.5.9 表不符
  （2.5.9 的真表 `>15 → ESD`，本仓对照档 `>15 → linear_fit`，:253-254 已承认偏离，但用 2_9_1 的名字承载该偏离会让读者以为存在 2.9.1 依据）。
  方向：无判据方向（不改数值行为），属**出处面失真**。
- 定级：S3
- 怎么算修好：二选一——(a) 补 WBPP 2.9.1 的可核验出处（URL + sha1 + `bestRejectionMethod` 行号）到
  `docs/references/SCIENTIFIC_REFERENCES.md`，并把 `>15 → linear_fit` 的偏离依据挂到该版本；
  (b) 把 profile 名改成如实承载来源的标识（例如 `wbpp_2_5_9_legacy_linearfit`），
  保留 `wbpp_current` 作为唯一对外别名；必须同批：改名会打破已登记的 `reject_profile` 取值
  （`DATA_SEMANTICS.md:1195`、`PHASE2_MOSAIC_WRITE.md:82`、`PHASE2_REJECTION.md:218-220` 与
  `rejection.cpp` 的 profile 字符串比较），四处同步 + 兼容旧名的拒绝面需显式定义（不得静默接受旧名）。
- 置信：PARTIAL —— 缺"跟踪面是否存在 2.9.1 出处"的穷尽检索（我只在 `docs/references/` 与 coverage 模块内查过；
  需补 `git grep -n "2\.9\.1" -- 'docs/references/**'`）

### 不报的核过项（附检索式）
1. **`P2_REJECT_CLASS_COUNT 7` 与枚举基数闭合**：:470-478 枚举 0..6 ⇒ :479=7、:560 `count × P2_REJECT_CLASS_COUNT` 一致 ⇒ 不报。
   检索式：`git grep -n "P2_REJECT_CLASS_COUNT" -- lib/algorithms/coverage`。
2. **`AUTO` 永不进 kernel 的分层**：:11-14/:56/:208 三处一致要求 planning 层解析；实现 `rejection.cpp:1271-1289`
   仅在 `req->request == P2_REJECT_AUTO` 时选档，kernel 入口另判 `P2_STATUS_INVALID_METHOD`（:114）⇒ 设计一致。
3. **`>15 → linear_fit` 对 WBPP 真表（ESD）的偏离**：已在 :253-254 与 `rejection.cpp:1280-1287` 具名登记、
   并由 `SCIENTIFIC_REFERENCES.md:91` 说明"旧引文 `BPP-FrameGroup.js:1304-1312` 作废"⇒ 属"如实登记的偏离"，不报为缺陷
   （版本名问题另见 CR-22-17）。
4. **`:416` 的退役声明**：`p2_reject_stack_resolve_ex` 已按 CHK-PROD-WIRING W1 从公共头撤下，属 §4 假阳源里
   "已由合同显式声明不适用/退役"的情形 ⇒ 不报（其证据位问题并入 CR-22-16）。
5. **校准三常数（50/0.10/0.10）的未签字状态**：:495-496 的"按文档值实现、未签字生效前不得放宽（不得改成更松的值）"
   与 `DATA_SEMANTICS.md:3118`（"生效前相关面 fail-closed，实现面保持条款原样"）一致 ⇒ 状态如实，不作为缺陷；
   数值本身的推导缺失已在处置表落 `待确认`。
6. **`:577`/`:578` 判据方向**：`P2_CALIB_RELIABILITY_FAIL = max|obs-mean(p)| > ABS`、`P2_CALIB_BSS_FAIL = BSS <= BSS_MIN`
   两个不等式方向在条文里钉死且互不重叠（一为大于即失败、一为不大于即失败）⇒ 无歧义，不报。

---

# 文件 5 · `lib/algorithms/coverage/src/rejection.cpp`（2956 行，已全读）

分区实况：:31-156 统计函数族（ibeta/t_cdf/t_quantile/norm_cdf/norm_quantile）；
:158-977 完整 sequential RCR 移植（含两张官方表 + 拟合式）；:982-1073 方法表与 ScratchVec/median/MAD；
:1082-1204 semantic registry；:1100-1204 小 N 唯一决策点与 AUTO 路由；:1199-1321 plan resolve；
:1327-1475 资格层（连续版 + strided 版）；:1485-2011 各方法核；:2016-2221 `p2_reject_stack_ex`；
:2228-2339 COMPAT `p2_reject_stack`；:2345-2439 large_scale grow；:2455-2490 继承阈值守卫；
:2495-2953 分类排异 + 校准门。

## §2.5 逐数值处置表

（同族同值已合并并列出全部位点；表长属预期。）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :33 `fpmin` | Lentz 分母下限 | 1e-300 | 连分数除零防护 | 结构性不适用 | :31 自述"Numerical Recipes betai/betacf 算法"；FP64 最小正规数 2.2e-308 ⇒ 1e-300 是同一量级防护，非科学量 | — | CONFIRMED |
| :42 `m <= 300` | 连分数迭代上界 | 300 | ibeta 收敛迭代上限 | 文献值 | NR `betacf` 的 `MAXIT=300`（:31 点名 betacf）；上界属实现选择，未收敛即返回（无收敛性断言 ⇒ 见"边界"） | — | CONFIRMED |
| :61 `1e-12` | 连分数收敛阈 | 1e-12 | Lentz 停止条件 | 待确认 | :31 只写"NR betai/betacf"，未登记 EPS 数值；**本仓文档面无该常数**，检索式 `git grep -n "betacf\|betai" -- docs/algorithms/PHASE2_REJECTION.md docs/references/`。影响范围 = ESD 临界值最后一位；λ 相对误差 ≲1e-12，远小于任何判据 | 保守方向：收紧到 3e-14（NR 原式量级）只增耗时 ⇒ 无判据影响 | CONFIRMED |
| :89/:90-91 `40.0`、`lo=0,hi=40`、`80` 次二分 | t 分位数搜索域与迭代数 | 40 / 80 | 反查上界与精度 | 待确认 | 无条文。域内最坏点已复算：ESD 在 n=3 时 m=3、ν=1、p=1−0.05/6=0.991667 ⇒ t=tan(π(p−½))=**35.6** ⇒ 40 的上界余量只有 12%；80 次二分给出 ~3e-23 精度（充分）。影响范围 = ESD 临界值；若 α 或方法域放宽使 t>40 ⇒ 静默截断 | 保守方向：把上界随 ν、p 解析放大（或 40 ⇒ 1e3）只会更准 ⇒ 不改变判据方向 | CONFIRMED |
| :101/:150/:173 π 与 √(2π) | 3.14159265358979323846 / …234 / 1.57079632679489661923 | — | 定义常数 | 结构性不适用 | 数学常数（π、π/2），非科学量；:84 `kPiHalf` 同族 | — | CONFIRMED |
| :110-124 Acklam a/b/c/d 21 系数 | 逆正态有理近似系数 | 见代码 | Φ⁻¹ 近似 | 文献值 | 系数即 Acklam(2003) 公开常数组；本仓登记面 `docs/algorithms/PHASE2_REJECTION.md` 含 "Acklam" 命中（`git grep -ln "Acklam" -- 'docs/**'` 唯一命中该文件）；:145-154 另加一步 Halley 精修（把 ~1e-9 提到 ~1e-15）⇒ 按式实现 | — | CONFIRMED |
| :125 `plow/phigh` | 尾部区域分界 | 0.02425 | Acklam 三段划界 | 文献值 | 同上（Acklam 原文的区域边界） | — | CONFIRMED |
| :174-175 `kRcrInverfMult` | 8(π−3)/(3π(4−π)) | 导出式 | inverf 近似常数 | 公式导出 | :167/:389 点名 A&S 7.1.25/7.1.26；代码真按该式算（非常数表） | — | CONFIRMED |
| :178-191 `kRcrSSDLUnityCF[101]` | SS_MEDIAN_DL 校准因子表 | 101 个 6 位小数（[0]=[1]=1.0、[2]=0.799351 … [100]=0.950152） | RCR 尺度修正 | **待确认** | 出处为官方 `rcr 2.4.7`（:161 给 nickk124/robust-outlier-rejection commit `a8a29a6`），**表值本体不在跟踪面**（`git ls-files \| grep -i rcr` 零命中），仓内亦无该表的复算/校验件；`docs/algorithms/PHASE2_REJECTION.md:75` 只描述函数不登记数值。保守方向：任一元素抄错 ⇒ 该 n 档 RCR 尺度错，无门可检 | PARTIAL |
| :194-320 `kRcrSSUnity[1001]` | 官方 getSingleFN 阈值表 | 1001 个值；[0..3]=0.0（占位）；[4]=2.15681、[5]=8.81255…尾部≈1.88 | RCR 单离群最小 σ 阈 | **待确认** | 同上（官方包表；跟踪面无原始表）。**[0..3]=0.0 不是"未用"而是会被读到**：`rcr_get_single_fn(n≤3)` 返回 0.0 ⇒ 见 CR-22-21 | PARTIAL |
| :322-324 `kRcrSSConstants[2][8]` | DL 线斜率/截距 per-n 表 | `.202399/-.29158/-.03321/-.18181` 与 `.464231/.26031/.363776/.454703`（n=4..7） | RCR 折点拟合参数 | 待确认 | 同上；n≥8 路径不查此表（改走 :628-641 多项式）⇒ 有效域仅 n=4..7（代码 :623 `3 < n && n < 8` 与本表 4..7 一致 ✓） | — | CONFIRMED |
| :327/:330 `rcr_is_equal` | `DBL_MIN` 与 `rel <= 1e-8` | 1e-8 | 官方等价比较容差 | 文献值 | :326/:333 自述"官方 isEqual/distinctValuesCheck"；影响范围 = RCR 的"≥3 个不同值"守卫与 distinct 判定 | — | CONFIRMED |
| :364-367 中位插值系数 | `n/2+0.5`、`0.5*total − running + 1.0` | 0.5 / 1.0 | 偶/奇数中位数 | 公式导出 | 复算：偶数 n ⇒ 因子 0.5 ⇒ 两中值平均；奇数 n ⇒ 因子 1.0 ⇒ 正中位 ✓ 与官方 getMedian 同式 | — | CONFIRMED |
| :377-386 加权中位 | `w[0]*0.5`、`w[sc−1]*0.5+w[sc]*0.5` | 0.5 | 官方加权中位数 | 公式导出 | 官方 `getMedian(trueCount,w,y)` 同式（:370 自述）；:382 `sc==0` 早退防越界 | — | CONFIRMED |
| :403/:419/:751-758/:784 | `0.682689` 与 `0.317311` | 同值（6 位截断的 1σ 概率） | RCR 分位期望与 68% 尺度 | 文献值 | 精确值 0.6826894921370859 ⇒ 本处按官方包 6 位常量原样移植（:168-169 "数值语义与 oracle 逐位对齐"），0.317311 = 1−0.682689 ✓ 自洽；影响范围 = RCR 的 xvec/68th 尺度 | — | CONFIRMED |
| :465/:514 `min_error = 999999.0`（unweighted）与 :526/:577 `= DBL_MAX`（weighted） | 折点搜索初值 | 999999.0 / DBL_MAX | "最优折点"哨兵 | **待确认（两版不一致 ⇒ 见 CR-22-21）** | 无条文；999999.0 是**带量纲的绝对哨兵**，而 `error` = Σ(拟合残差)² 的单位是 (ADU·sr⁻¹)² ⇒ 生产电平（REJECTION.md:51 自述 1110/2664 ADU）下 error 轻易 >1e6 ⇒ 两版行为分叉 | 保守方向：两版统一为 `+inf` 或"首见即记录" | CONFIRMED |
| :506-507/:569-570/:657-658/:704-705 `6.36` | broken-stick 搜索增量除数 | 6.36 | 官方 mfinder 细化步长 | 文献值 | `docs/algorithms/PHASE2_REJECTION.md:75` "官方 broken-stick 折点搜索（增量细化 /6.36）" | — | CONFIRMED |
| :596/:618-620 `39.2519`、`-0.7969`、`1.8688` | getSingleFN n≥1001 幂律 | 同值 | 官方拟合式 | 文献值（官方包拟合） | 跟踪面无原始拟合记录（同 :194-320 处境）；影响范围 = n≥1001 的 RCR（生产 n_max = 帧数，远小于此 ⇒ 实际不可达） | 生产不可达（逐跳：`module_adapters.cpp:10863` n_max = frames.size()） | CONFIRMED |
| :628-641 `a1/b1` 六次多项式系数 + `(-1)^n·0.1513·n^-0.471` | n∈[8,1001) 的拟合 | 见代码 | 官方 getSingleFN 加权扩展 | 文献值（官方拟合式） | 同上；奇偶修正项 `pow(-1.0,n)` 与官方一致（:168-169 的逐位对齐声明） | — | CONFIRMED |
| :646 `f = -999999.0` | 未覆盖 n 的哨兵阈值 | -999999.0 | n≤3 时的"永不回退 SL"标记 | **待确认 ⇒ CR-22-21** | 无条文；与 unweighted 版 :595 的 `kRcrSSUnity[n≤3] = 0.0` 构成**同一决策的两套回退语义**（−999999 ⇒ 几乎恒不触发；0.0 ⇒ 按 dcs 符号触发） | 方向：两者都使 RCR 倾向保留 DL 尺度 ⇒ 偏绿（少剔），且不可复现地依赖 counter 的奇偶/大小 | CONFIRMED |
| :655/:702 `x.size() < 4` | DL 拟合最小样本 | 4 | 折点搜索最小 n | 结构性不适用 | 实现选择（低于此退回 single-line），与 :982-998 的 `method_minimum_n(RCR)=3` 同族 | — | CONFIRMED |
| :692/:741 `if (sigma < 0) sigma = 0.0000000001;` | 负斜率地板 | 1e-10 | DL 尺度非负化 | 待确认 | 无条文。后果已定方向：σ→1e-10 ⇒ `zmax = max_hold/σ` 巨大 ⇒ `n·erfc(zmax/√2) ≥ 0.5` 恒成立 ⇒ **该趟不剔任何样本**（偏绿）；官方是否同处理未核 | 保守方向：负斜率应判为拟合失败并回退 single-line/68th（偏红于剔除数，如实登记） | CONFIRMED |
| :962 `sqrt(ss/(ws − wsq/ws))` | 加权标准差归一 | — | RCR 加权 sd | 公式导出 | 有效样本数归一：n_eff = (Σw)²/Σw² ⇒ ws − wsq/ws = Σw(1 − w/Σw)… 与官方 `getStandardDeviation(weighted)` 同式（:166 自述） | — | CONFIRMED |
| :965 `(true_count − 1)` | 样本方差自由度 | 1 | 无偏 sd | 公式导出 | 教科书定义（n−1） | — | CONFIRMED |
| :973 `n·erfc(z/√2) >= 0.5` | Chauvenet 拒绝判据 | 0.5 | 单点期望出现次数阈 | 文献值 | :167 点名 A&S 7.1.26 + "官方 erfcCustom"；判据方向：`>= 0.5` ⇒ 不剔（保留），`< 0.5` ⇒ 剔 ⇒ 与 Chauvenet 准则一致 | — | CONFIRMED |
| :844-850 erfcCustom 6 系数 + 16 次幂 | A&S 7.1.26 | 0.0705230784 … 0.0000430638 / 16 | 互补误差函数近似 | 文献值 | :167/:840 点名 A&S 7.1.26；该组系数与 A&S 7.1.26 公布值逐项一致（本批逐项对照过）；已知精度 ~1.5e-7（WIMP）⇒ 影响 zmax 判定末位 | — | CONFIRMED |
| :1014-1015 `CAP = 64` | ScratchVec 栈上容量 | 64 | 固定 scratch 上限 | 结构性不适用 | 与 `rejection.h:406` "n<=64 使用固定 scratch（>64 走堆）" 一致 ✓；:1037-1045 `push_back` 越容量时整体迁堆 ✓ 无截断 | — | CONFIRMED |
| :1040 `heap_.resize(CAP + 1)` | 迁移容量 | 65 | 迁移后首尺寸 | 结构性不适用 | 增长策略 | — | CONFIRMED |
| :1066 `*std::max_element(v, v+mid)` | 偶数中位取左半最大 | — | nth_element 后的第二中值 | 公式导出 | 偶数中位定义 = 两中值均值（与 :1064 奇数支一致） | — | CONFIRMED |
| :1072 `1.482602218505602` | MAD→σ 一致因子 | 同值 | 1/Φ⁻¹(0.75) | 公式导出 | 复算：Φ⁻¹(0.75) = 0.6744897501960817 ⇒ 倒数 = 1.482602218505602 ✓ 逐位一致；语义锚 = Astropy `mad_std`（rejection.h:25、REJECTION.md §14a） | — | CONFIRMED |
| :1499-1500/:1543-1544/:1603-1604/:1860-1861 `lo=-\|lower\|, hi=+\|upper\|` | 双侧 σ 阈符号化 | — | 方向语义（禁原始值正负号） | 公式导出 | `rejection.h:20-21` "rejected_low = 低于 lower threshold……禁止用原始值正负号" ⇒ 代码先取 \|·\| 再定号，符合 | — | CONFIRMED |
| :1513/:1561/:1617/:1758/:1879 `<= 1e-12` | 退化尺度停止阈（s / s0 / mean|res| / sd） | 1e-12 | 防 0 除与无穷迭代 | 待确认 | **绝对量、未声明标度**：作用域是 ADU·sr⁻¹ 值域（`docs/science/REJECTION.md:36-39` 明文"sigma/threshold 与 values 同标度"），而 1e-12 只在数值意义上防 0；条文面对"退化尺度"的唯一约束是 §8a 的 `median → 0` 塌缩（percentile），σ 族无条文。方向：**偏绿**（尺度极小但 >1e-12 时照常判剔；反之恰为 0 电平时停止剔除） | 影响范围 = 五处 σ 族方法核 | CONFIRMED（无锚这一事实） |
| :1563 `it2 < 64` | winsorize 内层收敛上界 | 64 | 不动点迭代上限 | 待确认 | 条文只锚"迭代限 5e-4"（`REJECTION.md:285`），未锚 64 这个上界；实现选择，未收敛即带当前 σ 继续（无告警）⇒ 静默截断风险 | 保守方向：上界应同时产出"未收敛"标记 | CONFIRMED |
| :1564-1565 `1.5 * sigma` | winsor 收缩窗 | 1.5 | ±1.5σ 截缩 | 文献值 | `docs/science/REJECTION.md:285` "本层 winsorized_sigma 的语义来源 = PixInsight ImageIntegration 官方文档式[18]/[19]（±1.5σ winsorize…）"；概念背景 Huber & Ronchetti 2009 | — | CONFIRMED |
| :1572 `1.134 * wsd` | winsorize 尺度修正因子 | 1.134 | 收缩后的无偏尺度 | 文献值 | 同上 :285（"常数 1.134"）；适用域声明见 :292（"按对称近高斯 accept 集标定"） | — | CONFIRMED |
| :1573 `<= 5e-4 * sigma` | winsorize 收敛阈 | 5e-4 | 不动点停止条件 | 文献值 | 同上 :285（"迭代限 5e-4"）；相对判据（乘 σ）⇒ 量纲正确 | — | CONFIRMED |
| :1582/:1693/:1883 `nc − r <= 4` / `N − r <= 4` | 最小保留闸（三处） | 4 | "至少留 4 个样本" | **待确认（表达式跨迭代不自洽 ⇒ CR-22-23）** | **条文面零提及**，检索式 `git grep -n "保留\|剩余\|不剔\|下限" -- docs/science/REJECTION.md` → 零命中；仓内唯一同类正确实现是 `P2MinmaxParams.min_kept`（:1906-1908 按"删除后 ≥ min_kept"判定 ✓）；`rejection.cpp:2580/:2583` 的 warn 码注释 "Siril N-r<=4" 表明本意是 N−r，但此处 N=当前存活数、r=累计剔除数 ⇒ 跨迭代等价 n0−2r ≤ 4 | 方向：**偏绿**（剔除上限被压到约 (n0−4)/2 ⇒ 超出的离群样本留在栈内） | CONFIRMED |
| :1666 `if (N < 4) break;` | linear_fit 最小拟合样本 | 4 | 折线拟合最小 n | 结构性不适用 | 实现选择；与 `method_minimum_n(LINEAR_FIT)=4`（:991）同值但语义不同（一个入栈门、一个拟合门），两处都定 4 无条文 ⇒ 登记 | — | CONFIRMED |
| :1652-1659 `m_x=(n0−1)/2`、`xf[j]=1/(j+1)`、`m_dx2` 递推 | Siril 冻结 harness 的加权均值/斜率式 | — | 线性拟合权重 | 公式导出 | `docs/algorithms/REJECTION_ALGORITHMS.md:64-65` "linear_fit 残差尺度 = 平均绝对残差 mean\|stack[i]−fit(i)\|，阈值 5.0/3.5，8 iter；**不是** MAD 尺度" ⇒ :1686-1688 确按 MAD-of-residuals 实现 ✓（该文档给的行号 1665-1668 已漂移，见末节移交线索） | — | CONFIRMED |
| :1739 `max(1, prm.max_outliers)` | ESD 最大离群数下限 | 1 | 防 0 次检验 | 结构性不适用 | 入参非法时的最小可用档 | — | CONFIRMED |
| :1740/:1942 `alpha > 0 ? : 0.05` | α 回退 | 0.05 | 非法 α 的默认 | 公式导出 | 冻结 α=0.05（`rejection.h:139/:201`、`REJECTION_ALGORITHMS.md:157-158`）；注意 :1942 还额外要求 `alpha < 1.0`（ESD 侧 :1740 未要求上界）⇒ 两面判据宽严不同（对 ESD 传 α=2 ⇒ 不替换，p 为负 ⇒ `t_quantile` 返回 0 ⇒ crit=0 ⇒ **全部判为离群**方向偏红）；生产 α 恒 0.05 ⇒ 不可达 | 影响范围 = 显式非冻结 α | CONFIRMED |
| :1752 `if (nc < 3) break;` | ESD 最小样本 | 3 | 均值/方差可算下限 | 结构性不适用 | 与 `method_minimum_n(ESD)=3`（:988-990）一致 | — | CONFIRMED |
| :1767-1768 `+ 1e-15` / `fid < worst_fid` | R统计量并列容差与帧序 tie-break | 1e-15 | 确定性选择 | 公式导出 | 确定性要求（`rejection.h` 无、但 `docs/science/REJECTION.md` 的确定性主张与 :2835 的"严格 > ⇒ 并列取最小 id"同一手法）；容差在 z 域（无量纲）⇒ 标度安全 | — | CONFIRMED |
| :1774-1779 `n−i`、`nu=n−i−1`、`p=1−α/(2(n−i+1))`、`crit=(nu+T²)(n−i+1)` | ESD 临界值 | — | λ_i 式 | 公式导出 | NIST/SEMATECH §1.3.5.17.3（`rejection.h:179` 点名）；**已按标准 Grubbs/ESD 临界值式复算等价**：令 m=n−i+1 ⇒ λ=(m−1)T/√[m(m−2+T²)]，T=t_{α/(2m), m−2} ⇒ 与 :1778-1779 逐项一致（代码合规）；本批另按该式复算 λ₁(n=54, α=0.05)=**3.134**，而 `docs/science/REJECTION.md:85/:87` 以"λ₁(54)=3.1588 与 NIST 公布值一致"为实现正确性背书，与本仓自身公式的复算值差 0.79% ⇒ 见末节移交线索（疑 R_i 与 λ_i 角色错绑；一手页 `https://www.itl.nist.gov/div898/handbook/eda/section3/eda35h4.htm` 本节点 302 重定向不可达） | — | CONFIRMED（代码式）/ PARTIAL（doc 数值） |
| :1786-1791 `k_out` 取"最大 i 使 R_i > λ_i" | ESD 终止规则 | — | 反向扫描 | 公式导出 | NIST 同页规则（"choose largest i such that R > λ"）✓；:1789 `accept.fill(1)` 后按 removed[] 重剔 ⇒ 无泄漏 | — | CONFIRMED |
| :1906 `max(1, prm.min_kept)` | minmax 保留下限 | 1 | 非法值兜底 | 结构性不适用 | 入参防护；冻结默认 4（:1907-1908 使用） | — | CONFIRMED |
| :1907-1908 `(k_low+k_high) >= n \|\| n−(k_low+k_high) < min_kept` | minmax 容错门 | — | "删除后至少留 min_kept" | 公式导出 | `rejection.h:151` "剔除后至少保留样本数（默认 4）" ⇒ 本处口径正确（与 :1582/:1693/:1883 的三处形成对照，见 CR-22-23）；先判 `>=n` 再相减 ⇒ 无 size_t 下溢 ✓ | — | CONFIRMED |
| :1944 `N = bonferroni_n>0 ? : max(n,1)` | Bonferroni 检验数 | — | N 取值优先级 | 公式导出 | `rejection.h:185-187` "N = 该像素几何 nominal contributors；0 时退化为候选数 n" ✓ | — | CONFIRMED |
| :1946 `k = Φ⁻¹(1 − α/(2N))` | 双侧 Bonferroni 阈 | — | 极值检验判据 | 公式导出 | `rejection.h:184-188` 逐式一致；:1980-1981 判据 `z > k ⇒ HIGH`、`z < −k ⇒ LOW` 方向与头一致 | — | CONFIRMED |
| :2030/:2050/:2062/:2069/:2079/:2090 `out->accepted_count = n` 配 reasons=UNDERDETERMINED | 失效状态的"全接受"编码 | — | status 与 reasons 的耦合 | 待确认 | 见 CR-22-24：`rejection.cpp:2021-2022` 自述契约"调用方对非 {OK,UNDERDETERMINED} 一律 hard fail"，而交付调用方只判 rc ⇒ 掩码面把 INVALID_* 读成"全接受" | 方向：调用方漏判 status ⇒ **偏绿**（排异被整体旁路） | CONFIRMED |
| :2038 `n == 0 ⇒ MIN_SAMPLES` | 空栈状态 | — | 边界 | 结构性不适用 | `rejection.h:109` 语义 | — | CONFIRMED |
| :2085-2087 `n <= underdetermined_n \|\| n < minimum_n` | 内核小样本闸 | — | 两条独立下限 | 公式导出 | `docs/science/REJECTION.md:44` 逐字一致（"n <= underdetermined_n 或 n < minimum_n ⇒ UNDERDETERMINED"）✓ 注意是 `<=` 与 `<` 两种比较，代码与条文同为 `<=`/`<` ⇒ 方向已钉死 | — | CONFIRMED |
| :2199/:2205-2206 `n <= 4` 全拒容错 | 降级闸 | 4 | 全拒时改判"不排异" | 公式导出 | `docs/science/REJECTION.md:49-51`"全拒容错域：n=4 ∧ 方法核全拒 ⇒ 降级 UNDERDETERMINED 全接受……可达域恰为 n=4"；代码写 `n <= 4`（比条文的 `n == 4` 宽），但 n≤3 已被 :2087 的 underdetermined 闸截走 ⇒ 二者观测等价（复核见"不报的核过项"3） | — | CONFIRMED |
| :2250 `support_threshold = 0.0`、`quality_flags_required = 0` | COMPAT 层硬编码资格阈 | 0.0 / 0 | 旧接口不检查 support/quality | 待确认 | `rejection.h:426` 只说"旧接口/COMPAT"，未说明其 support 阈固定 0；`P2SampleStackView` 无该字段 ⇒ 调用方无法收紧（与生产 strided 版 `support_threshold` 可配成对不一） | 影响范围 = COMPAT 面（`eng/ci/ledgers/prod_wiring.json:278-286` 已登记为生产不可达） | CONFIRMED |
| :2270 `req.underdetermined_n = 2` | COMPAT 固定内核闸 | 2 | 旧语义恒定 2 | 待确认 | 与生产档默认 3（:1236-1238）不同；`rejection.h` 未登记 COMPAT 用 2；同文件 :2757 classify 的回退也是 2 ⇒ "默认 2"与"生产 3"两面长期并存（CR-22-19） | 方向：COMPAT 面 N=3 会真排异（对照生产不排异）⇒ 两面对同一像素给不同掩码 | CONFIRMED |
| :2279-2306 `sigma_low/sigma_high/max_iterations` 复用为多方法参数 | 兼容入参的语义重载 | 0.0=未提供 | 同一 double 字段承载 σ 数与 percentile 小数 | 待确认 | `rejection.h:437-440` 只写"兼容：见 typed params"；:2288-2289 把 `sigma_low` 直接赋给 `percentile.low_fraction`（σ 无量纲个数 vs 相对 median 的小数分数，两套单位共用一个字段）；:2306 又把 `max_iterations` 赋给 `esd.max_outliers`（迭代次数 vs 最大离群数） | 方向：误传即静默改变判据 ⇒ 无门可检（COMPAT 面） | CONFIRMED |
| :2238-2240/:2255/:2336 `has_nonfinite` | COMPAT 的非有限检测面 | — | 仅检 values | 待确认 | `rejection.h:111` 定义 INVALID_INPUT 为"候选栈含非 finite"（含 weights）；COMPAT 只检 values，权重非有限被 :2249 资格层丢弃后不回置状态 ⇒ 与 :2045-2047 的新内核不等价（内核两者都检） | 影响范围 = COMPAT 面 | CONFIRMED |
| :2352-2354 `dirs[8][2]` | 8-连通邻域 | 8 | 结构生长邻接 | 结构性不适用 | `rejection.h:163` "8-连通分量" ✓ | — | CONFIRMED |
| :2358 `comp.reserve(4096)` | 分量缓冲预分配 | 4096 | 性能 | 结构性不适用 | 缓冲区尺寸，非科学量 | — | CONFIRMED |
| :2422-2425 `min_structure_pixels >= 1`、`radius >= 0` | 参数域校验 | 1 / 0 | 入参域 | 公式导出 | `rejection.h:173-175` 明文 ">=1"、">=0"；且校验在 `enabled` 判定**之前**（:2422 先于 :2426）⇒ 关闭状态也校验，符合"非法参数不因关闭而豁免" | — | CONFIRMED |
| :2455 `rej_eq(a,b){return a==b;}` | 继承阈值的逐位比较 | — | 防阈值漂移 | 公式导出 | `rejection.h:513-515` "返回 1 = 全部继承值一致" ⇒ 精确比较是"一致"的正确读法（非容差） | — | CONFIRMED |
| :2458-2488 继承常数组 | 4.0/3.0/8、5.0/3.5/8、0.05/10、0.2/0.1、1/1/4、0、8/2/2、0.05 | 同值 | 冻结阈值表第二执行面 | 公式导出 | 与 :1246-1268 的 resolve 默认值逐项一致 ✓；与 `REJECTION_ALGORITHMS.md:157-158` 一致 ✓；`rejection.h:133/:144` 的结构体注释与之矛盾（CR-22-14） | — | CONFIRMED |
| :2502-2503 `d > 700 / d < -700` | exp 溢出防护 | 700 | sigmoid 饱和阈 | 结构性不适用 | FP64 `exp` 溢出点 709.78 ⇒ 700 是安全余量（缓冲/防护类常数，非科学量） | — | CONFIRMED |
| :2734-2735 `P2_NOISE_REQUIRED` | 噪声声明位门 | 0x3 | σ_eff 组成校验 | 公式导出 | `rejection.h:458-460/:489-493`（ALG-P2S-REJ.3 fail-closed）✓ 实现缺一即 `INVALID_INPUT` | — | CONFIRMED |
| :2743-2752 `s1 < 0`、`uv < 0`、`s2 > 0 && finite` | 输入域门 | 0.0 | 残差/方差合法域 | 公式导出 | `rejection.h:537`（单位与域）+ 方差非负定义 | — | CONFIRMED |
| :2756-2757 `und_n = plan.underdetermined_n > 0 ? : 2u` | classify 面内核闸回退 | 2 | 手工 plan 的默认 | 待确认 | 与生产 resolve 的 3（pixel 档）不一致，见 CR-22-19；`rejection.h:564` 的 "n<=2 全接受 → recall 0.0" 与该默认值 2 自洽 ⇒ 但**与生产档 3 不自洽** | 方向：n=3 时 classify 会真排异而 resolve 面判"不排异"⇒ 同像素两态 | CONFIRMED |
| :2758-2759 `lower_sigma=4.0 / upper_sigma=3.0` | 继承阈取用点 | 4.0/3.0 | z 判据阈 | 公式导出 | 来自 `cfg->plan.sigma`，而 :2704 的 `rej_plan_inherited` 已强制其等于 4.0/3.0 ⇒ 不可另设（"继承"红线成立） | — | CONFIRMED |
| :2787-2788 `z <= -4.0` / `z >= +3.0` | 分类排异判据 | 含等号 | 方向侧计数 | 公式导出 | `rejection.h:458-460`；等号含入 ⇒ 恰在阈上即剔（保守侧），与 σ 族 `z < lo \|\| z > hi`（:1520，等号**不**剔）**方向不互斥但不一致**：同一 z=+3.0 在 robust_mad 判接受、在 classify 判剔除 ⇒ 见"边界" | — | CONFIRMED |
| :2800 `m >= motion_min`(0.5px) / :2825 `psf_anom >= psf_min`(0.2) | 证据门 | 0.5 / 0.2 | 移动源 / 失焦分类阈 | 待确认 | `rejection.h:504-505/:529-530` 与 `DESIGN-P2-001 §5` 引用；**推导/标定面本批未检到**，检索式 `git grep -n "motion_min_px\|psf_anomaly_min" -- docs/science/ docs/algorithms/ docs/contracts/` 只见键名不见定值依据 ⇒ CR-22-20 | 方向：越小越易判该类 ⇒ 对 `preserved`（默认保留）面偏绿 | CONFIRMED（无锚事实） |
| :2804-2825 证据位 → 类权重 0/1 后归一 | 多标志等权 | 1.0 / wsum | class_probability 合成规则 | 待确认 | 条文（DESIGN-P2-001 §5 / ADJ-GEN-02）是否规定"命中多个机制时等权分摊"未在本批核到；:2835 的并列取最小 id 已注释 ⇒ 确定性 OK，语义选择无锚 | 方向：等权 vs 优先级会改变 reason_class，但**不改 deleted**（:2845 由 z 阈决定）⇒ 只影响分类统计 | CONFIRMED |
| :2844-2850 `preserved` 与 `deleted` 可同真 | 输出面标志耦合 | — | 保留/删除标志 | 待确认 | `rejection.h:555-556` 把两字段分别注释为"1=进入排异删除""1=移动源保留独立层"，未规定可否同真；实现当 `cls==MOVING_SOURCE ∧ keep_moving_source==0` 时 deleted=1 且 preserved=1 ⇒ 见 CR-22-20 附带项 | 方向：消费方若按"preserved⇒未删"推断即错 | CONFIRMED |
| :2862 `recall = (rej_low+rej_high)/n` | 剔除率定义 | — | 诊断量 | 公式导出 | `rejection.h:564` "recall = 拒绝率" ✓（注意本层"recall"实为剔除率，非统计学的召回率 ⇒ 术语风险见 CR-22-20） | — | CONFIRMED |
| :2878 `p<0 \|\| p>1 \|\| truth[k] > 1` | 校准输入域门 | 0/1 | 概率与真值域 | 公式导出 | 概率定义域 + 二值真值；:2874 预置 `P2_CALIB_INVALID_INPUT` ⇒ 不合格即返回且状态可辨 ✓ | — | CONFIRMED |
| :2881 `bin_count == 0 ⇒ P2_REJ_PROFILE_BIN_COUNT` | 分箱数回退 | 10 | 默认分箱 | 结构性不适用 | `rejection.h:508` 同名键 ✓ | — | CONFIRMED |
| :2908-2909 `bss = (brier_ref > 0) ? 1 − BS/BS_ref : 0.0` | BSS 定义式 | — | 技能分 | 公式导出 | `rejection.h:586` "BSS = 1 − BS/BS_ref"；BS_ref 退化取 0 ⇒ 走 `P2_CALIB_BSS_FAIL`（:578 条文"或 BS_ref 退化"）✓ 方向偏红（安全侧） | — | CONFIRMED |
| :2914-2917 `b*count/bin_count` 等分 | 等计数分箱 | — | 可靠性图分箱 | 公式导出 | 标准 equal-count binning；:2896 `stable_sort` + (p,idx) 双键 ⇒ 确定性 ✓ | — | CONFIRMED |
| :2928 `nb >= P2_REJ_CALIB_BINMIN` | 参与判定箱下限 | 50 | 小箱剔除 | 待确认 | `rejection.h:497/:589`（PENDING_OWNER_SIGNOFF SO-07）；与 :508 的 10 箱合起来 ⇒ **可评估的最小样本量 = 10×50 = 500**，该耦合在两份条文里均未写明（`git grep -n "500\|bin_count" -- docs/contracts/DATA_SEMANTICS.md` 未回读到）⇒ CR-22-20 | 方向：样本 <500 即 `INSUFFICIENT_SAMPLES` ⇒ `probability_is_scientific_gate=0`（偏红，安全侧） | CONFIRMED |
| :2942/:2945 两个判据 | `max_dev > 0.10`、`!(bss > 0.10)` | 0.10 | 可靠性/BSS 门 | 待确认 | 同上（SO-07 未签字，但 :496 已声明"未签字生效前不得放宽"）⇒ 方向：只能收紧（偏红）；签字面见 `DATA_SEMANTICS.md:3378-3388` | — | CONFIRMED |
| :2950-2951 `probability_is_scientific_gate = (status==OK)` | 资格位 | 0/1 | 概率面是否可作科学判据 | 公式导出 | `rejection.h:592` "1 当且仅当 status==OK" ✓ | — | CONFIRMED |
| :982-998 `method_minimum_n` | 各方法最小 n | 0/2/3/4 | 入核下限 | 待确认 | 条文面 `docs/science/REJECTION.md` §5 只给档位路由，未逐方法给 minimum_n（检索式：`git grep -n "minimum_n\|minimum N" -- docs/science/REJECTION.md`）；`rejection.h:211` 说"方法注册表 minimum N"但注册表本体即本函数 ⇒ 自指，无外部锚 | 方向：n 下限偏大 ⇒ 更常 UNDERDETERMINED（不排异）⇒ 偏绿；偏小 ⇒ 过拒（偏红） | CONFIRMED（锚缺失） |

表 57 行（含合并项，覆盖 :31-2953 的全部数值常量/阈值/表族/哨兵/域校验）。

## findings

### CR-22-18 · 小 N 决策点的"最终映射表"注释仍写 `N ≥ 16 → linear_fit`，与同一函数下方 30 行处的实现和两份上位文档相反
- 轴：C（主轴，文件内注释 vs 实现）
- 位点：`lib/algorithms/coverage/src/rejection.cpp:1129-1131` ——
  `//   最终映射表（逐像素按几何 N）：` / `//     1 ≤ N ≤ 3 → none / 4 ≤ N ≤ 5 → percentile /` /
  `//     6 ≤ N ≤ 15（或 BIAS/DARK 类帧）→ winsorized_sigma / **N ≥ 16 → linear_fit**；`
  对照实现 `rejection.cpp:1161-1173`：`if (n < 6u) … percentile; if (n <= 15u) … winsorized_sigma;`
  然后（M3 注释之后）`return P2_REJECT_WINSORIZED_SIGMA;`
- 上位依据：`docs/science/REJECTION.md:65-66` —— "`n ≥ 6 → winsorized_sigma (lower 4.0 / upper 3.0 / 8 iter)`
  （**M3 裁决（负责人 2026-09-25）**：原 `n ≥ 16 → linear_fit` 档**改投 winsorized**）"；
  `rejection.h:86-88` 同；`docs/algorithms/REJECTION_ALGORITHMS.md:54-55` 同。
- 现状：`astrocs_n_map_method` 上方的"最终映射表"（自称是决策点的表）在 M3 之后未同步，仍把 N≥16 档写成 linear_fit；
  同函数下方 :1163-1173 的注释与 return 已写 winsorized。
- 差在哪：同一个函数里两张互斥的映射表，其中被标为"**最终**映射表"的那张是旧的。
- 后果：该注释块是决策点的**唯一自述面**（:1116 的"唯一显式决策点"横幅），据此路由的读者/后续改动会把 N≥16 当作
  linear_fit，与生产实际（winsorized）相反；`auto_method_forbidden`、`p2_rejection_applicability` 的 WARN 码
  与 provenance 记录 `reject_method` 都会按真实方法（winsorized）落，只有注释说反 ⇒ 复核时对不上。
  方向：无判据方向（不改行为），属自述失真。
- 定级：S3
- 怎么算修好：把 :1131 的 `N ≥ 16 → linear_fit` 改为 `N ≥ 6 → winsorized_sigma（M3 后无 ≥16 档）`，
  并把 :1132 的 `N = 0 → percentile（永不进 kernel）` 与 :1155-1160 的实际分支一致化；
  必须同批：`docs/algorithms/PHASE2_REJECTION.md` 中引用该映射表的段落（:212/:218-220/:454 附近）核对是否复述了旧表。
- 置信：CONFIRMED（同文件两段的逐字比对）

### CR-22-19 · `underdetermined_n` 的默认值在"头 / 实现 / 公共 API 文档 / COMPAT 与 classify 面"取值不一（3 vs 2），且公共头把 2 写成无条件默认
- 轴：C（主轴，同一键多侧）
- 位点：`rejection.cpp:1236-1242` ——
  ```
  if (pixel_profile) {
      undet_default = (req->request == P2_REJECT_EXTREME_VALUE_PRIOR_SIGMA) ? 1u : 3u;
  }
  p.underdetermined_n = req->underdetermined_n > 0 ? req->underdetermined_n : undet_default;
  ```
  与之冲突的三处：`rejection.h:212`（`std::uint32_t underdetermined_n; // n <= 该值 → UNDERDETERMINED（默认 2）`）、
  `rejection.cpp:2270`（COMPAT `req.underdetermined_n = 2;`）、`rejection.cpp:2756-2757`（classify `> 0 ? : 2u`）。
- 上位依据：`docs/science/REJECTION.md:45-48` —— "**`underdetermined_n` 的默认值由 profile 与 request 决定**
  （`rejection.cpp:1218-1225`，实测）：`astrocs_adaptive_pixel` ∧ `request=AUTO` ⇒ **3**（生产默认档）；
  … ∧ `request=extreme_value_clip_prior_sigma` ⇒ 1；其余 profile ⇒ 2"。⇒ 科学正本与实现一致，**公共头与两个内部面不一致**。
- 现状：生产默认档内核闸 = 3（`module_adapters.cpp:10860` 传 0 ⇒ 取 profile 默认），
  而公共头自述"默认 2"，COMPAT 与 classify 的手工 plan 回退值也都写死 2。
- 差在哪：同一个决定"哪些像素根本不排异"的闸，对外接口文档给的是 2，生产实际是 3；
  手工构造 plan 的两条内部路径（COMPAT/classify）在 `underdetermined_n==0` 时静默取 2 而非按 profile 取 3。
- 后果：n=3 的输出像素在生产档**不排异**（全接受 + UNDERDETERMINED，provenance 记 `underdetermined_no_rejection`），
  在按头/COMPAT/classify 读法的面上**会排异**（percentile，且 n=3 属 REJECTION.md:51 所说的可全拒域之外但可部分剔）。
  两面差 = 三帧覆盖像素的污染泄漏与否，正是 `REJECTION.md:63` 量化的 `N=3 泄漏 1/3` 那一档。方向：
  按头读法（2）⇒ 对 n=3 多做排异 ⇒ 若被用于对照会**掩盖**生产档"不排异"造成的泄漏 ⇒ 对"排异有效性"主张偏绿。
- 定级：S3（交付路径本身取值正确且与正本一致；失真在公共头自述 + 两条手工 plan 面）
- 怎么算修好：(1) `rejection.h:212` 的"默认 2"改为"默认由 profile 与 request 决定（见 `REJECTION.md` §4），
  生产档 AUTO=3、extreme_prior=1、其余=2"，与 :240-244 合并成一处正向表述；
  (2) `rejection.cpp:2270` 与 :2756-2757 的裸 2 改为显式常量和/或调用 `p2_reject_plan_resolve` 取默认，避免"隐式 2"；
  (3) 必须同批：确认 `docs/api/PHASE2_API_V1.md`、`docs/algorithms/PHASE2_REJECTION.md` 是否复制过"默认 2"；
  (4) 若 COMPAT/classify 的 2 是刻意保持"旧语义"，则在该两行加具名注释并登记为"适用域不同"，否则按本条统一。
- 置信：CONFIRMED（四处代码 + 正本逐字比对）

### CR-22-20 · V6 分类排异的五个 profile 常量与三个校准门常量在本仓只有"键名 + 待签状态"，无任何推导或标定锚
- 轴：S（主轴，常数出处）
- 位点：`rejection.cpp:2717-2721`（逐位相等强制）与 `:2800`（`m >= motion_min`）、`:2825`（`psf_anom >= psf_min`）、
  `:2773-2774`（`prior = contamination_prior`、`kappa = outlier_inflation`）、`:2928/:2942/:2945`（50/0.10/0.10）
- 上位依据：`rejection.h:495-499`（"ALG-P2S-REJ.4；状态 PENDING_OWNER_SIGNOFF SO-07"）、
  :501-508（"v1 常量"）、:529-532；`docs/contracts/DATA_SEMANTICS.md:3348-3349`（FZ 条款名）、:3378-3388（SO-07 条目）、:3118（生效前 fail-closed）。
  **条文面零提及定值依据**，检索式（本批实跑，均无命中）：
  `git grep -n "motion_min_px\|psf_anomaly_min" -- docs/science/ docs/algorithms/ docs/contracts/`、
  `git grep -n "保留\|剩余\|不剔\|下限" -- docs/science/REJECTION.md`。
- 现状：五个分类常量被 `rej_eq` 逐位钉死（改动即 `INVALID_CONFIGURATION`），但其**数值本身**在权威链里
  只以"v1 profile 常量"的名义存在；三个校准门同样只有条款名 + 未签字状态。
  附带：`recall`（:2861-2862）实为"剔除率"，与统计学术语 recall（TPR）不同名同义，
  且 :2844-2850 允许 `deleted=1 ∧ preserved=1` 同真；:2881 的 10 箱 × :2928 的 50 合成"样本 <500 不可评估"的隐含下界，未写进任何条文。
- 差在哪：这些量直接决定"哪一类污染、以何概率、是否进删除面"，属于前言 §2 的 S 轴形态
  "值写成字面量，`docs/science`、`docs/algorithms`、根设计里没有任何一处规定它"。
  负责人"常数须有推导"口径（项目记忆：Doc doctrine）同样未满足。
- 后果：分类概率与 `reason_class` 是对外发布的诊断面（`rejection.h:462-463` 已禁止其进权重面 ⇒ 产品字节不失真），
  但 `deleted`/`preserved` 是删除面：`psf_anom >= 0.2`、`m >= 0.5px` 两个门一旦被下游用于删除决策，
  其数值就是无出处判据。方向：0.5px/0.2 取小 ⇒ 过分类（偏绿于"污染已被识别"）；
  校准门 0.10/0.10/50 取松 ⇒ `probability_is_scientific_gate` 更容易为 1（偏绿）。
- 定级：S3（`p2_reject_classify` 已被 `eng/ci/ledgers/prod_wiring.json:256-264` 登记为生产调用图外 ⇒ 无交付产物后果）
- 怎么算修好：为每个常量补"推导或标定"（0.5px 与 PSF FWHM/像元的换算、0.2 的异常度定义、0.05 先验的来源分布、
  κ=4.0 与 σ 阈 4.0 的关系、50/0.10/0.10 的统计学出处），落到 `docs/science/` 或 `docs/algorithms/` 的对应条款，
  并解 SO-07；`recall` 改名或在头里显式声明"本层 recall = 剔除率"；
  `deleted`/`preserved` 的真值组合域写成正向约束（含 keep=0 时二者同真是否合法）；
  把"可评估最小样本量 = bin_count × BINMIN"登记进 `rejection.h:596` 的函数说明。
- 置信：CONFIRMED（检索式已跑；常量与条文逐字比对）

### CR-22-21 · RCR 折点搜索的 `min_error` 哨兵与"n≤3 阈值"在加权/非加权两版互不相同，且大数值输入下会让 `m = -1` 直接索引数组（越界读）
- 轴：R（主轴，运行时正确性）+ C（两版同一决策不同语义）
- 位点：
  - `rejection.cpp:465` `double min_error = 999999.0;` 与 :497 `if (error < min_error) { … best_m = m; … }`
    （对照加权版 :526/:560 `min_error = std::numeric_limits<double>::max();`）
  - 消费点 `rejection.cpp:656-659` —— `const int m = rcr_mfinder(...); const double xat = x[(std::size_t)m];`
  - `rejection.cpp:594-597` `rcr_get_single_fn` —— `if (n < 1001) return kRcrSSUnity[n];`（`kRcrSSUnity[0..3] = 0.0`，:195）
    对照加权版 `rejection.cpp:645-646` —— `} else { f = -999999.0; }`
- 上位依据：`rejection.h:52/:72`（`astrocs.rcr_2_4_7_ss_median_dl.v1` 冻结）、
  `rejection.cpp:158-171`（自述"与官方 rcr 2.4.7（commit a8a29a6）`performRejection` 的 SS_MEDIAN_DL 冻结链
  …数值语义与 oracle 逐位对齐以便 rejected-set 精确比对"）；
  `docs/algorithms/PHASE2_REJECTION.md:75` 登记 `rcr_mfinder(/_w)` 为"官方 broken-stick 折点搜索（增量细化 /6.36）"。
  条文面零提及 `min_error` 初值与 n≤3 的回退值。
- 现状与后果（三条，方向各自钉死）：
  1. **越界读**：`rcr_mfinder` 只在 `error < min_error(=999999.0)` 时记录 `best_m`；
     `error` 是拟合残差平方和，单位 (ADU·sr⁻¹)²。生产电平（`docs/science/REJECTION.md:51` 自述真实数据
     NGC1727 1110 ADU、LDN43 2664 ADU）下，`diff = |v − median|` 的量级为 10²–10³，平方后单个即 10⁴–10⁶，
     对 `under`（≈ n/2 到 n）个样本求和轻易 >999999 ⇒ 一次都不记录 ⇒ 外层 :505-511 令 `low=high=-1` ⇒
     `if (low == high) return low;` **返回 −1** ⇒ :659 以 `x[(std::size_t)(-1)]` = `x[SIZE_MAX]` 取值 ⇒ 越界读（UB）。
     加权版因哨兵取 `DBL_MAX` 不受该效应影响 ⇒ 两版对同一输入的行为分叉（一版可能 UB、一版正常）。
  2. **判据分叉**：`dcs`（DL 与 SL 拟合的比较量）与非加权版的比较对象是 `rcr_get_single_fn(counter)`，
     而 `counter = true_count`（当前存活样本数，:942-943）可低至 ≤3 ⇒ 返回 `kRcrSSUnity[n≤3] = 0.0`
     ⇒ 判据退化为 `dcs < 0`；加权版同域走 :646 的 `f = -999999.0` ⇒ 判据恒假。
     同一"DL 是否值得保留"的决策在两版里分别是"按 dcs 符号"和"永不回退"。方向：`-999999` 版恒不回退 ⇒ 尺度取 DL；
     `0.0` 版按符号可回退 SL ⇒ 尺度可能更小 ⇒ `zmax = max_hold/σ` 更大 ⇒ 更不易剔（两向都偏绿，但**结果不同**）。
  3. 未收敛/退化时 `rcr_fit_dl` 的 :692/:741 `sigma < 0 ⇒ 1e-10` 使该趟判据 `n·erfc(z/√2) >= 0.5` 恒成立 ⇒ 整趟不剔（偏绿）。
- 定级：S2。**可达性逐跳**：`p2_reject_stack_ex` :2156-2158 `case P2_REJECT_RCR: reject_rcr_impl(...)`
  ← `rejection.cpp:2416` 之外的方法选择 ← `p2_reject_plan_resolve`（AUTO 永不产出 RCR，AUTO 路由值域 =
  {none, percentile, winsorized}，:1154-1173/:1285-1287）⇒ **交付调度器路径不可达**：
  `module_adapters.cpp:10858` 把 `req.request` 硬编码为 `P2_REJECT_AUTO`，故节点链上 RCR 选不中。
  剩余可达面 = 公开 C ABI 直接调用（`rejection.h:407`）与 CLI（`tools/rejection_cli.cpp`，本批文件 6 若暴露 `rcr`
  即为用户可触发面）。⇒ 不成立 S1，但成立 S2：可致产物错值/崩溃的触发路径存在，只差一个非交付入口。
- 怎么算修好：(1) `rcr_mfinder` 的 `min_error` 改 `std::numeric_limits<double>::max()`（与加权版统一），
  并在返回前显式处理"未找到折点"：返回 `single`（不做 DL）或让 `rcr_fit_dl` 在 `m < 0` 时早退，
  **禁止** `(std::size_t)m` 直接索引；(2) 把 n≤3 的两个回退值统一为同一个显式哨兵（并在 :195 的 `kRcrSSUnity[0..3]`
  处注明"占位 0 不可作判据"）；(3) `sigma < 0 ⇒ 1e-10` 改为"该趟判拟合失败 ⇒ 回退 68th/SL 并计数"；
  必须同批：`eng/tests` 内任何依赖 RCR 现行为的用例（`git grep -n "P2_REJECT_RCR" -- 'eng/**' 'lib/algorithms/coverage/tests/**'`）
  与 `docs/algorithms/PHASE2_REJECTION.md` 的 RCR 段行号指针一并复核；并补一条"大数值栈（值域 1e3 量级）"的 RCR 正/负例，
  现有门若只用小数值夹具，恰好落在 999999 哨兵的安全区内而看不到该缺陷。
- 置信：CONFIRMED（哨兵与消费点逐行核对 + 量级复算；未实跑 ⇒ 越界读属"必发条件已构造、未观测"）

### CR-22-22 · `method_minimum_n` 的各方法最小 n 与 `auto_method_forbidden` 的 NONE 例外都以本文件为唯一注册表，科学正本里没有对应条款
- 轴：S（主轴，判据数值无条文）+ C
- 位点：`rejection.cpp:982-999`（`method_minimum_n`：sigma 族/ESD/RCR/median_sigma=3、linear_fit=4、percentile=2、
  minmax=3、extreme=2、none=0）与 `:1184-1196`（`auto_method_forbidden` 中 `P2_REJECT_NONE` 的唯一合法来源例外）
- 上位依据：`rejection.h:211`（`int minimum_n; // 方法注册表 minimum N（不足 → UNDERDETERMINED）`）——
  注册表即本函数，属自指；`docs/science/REJECTION.md:44` 只规定"n < minimum_n ⇒ UNDERDETERMINED"的形式，
  未给各方法的取值。检索式：`git grep -n "minimum_n" -- docs/science/REJECTION.md docs/algorithms/REJECTION_ALGORITHMS.md`。
- 现状：`REJECTION.md:63-65` 已把生产档的路由写成 `1≤n≤3 → none / 4≤n≤5 → percentile / n≥6 → winsorized`，
  而 `minimum_n(percentile)=2` 与 `minimum_n(winsorized)=3` 等值与路由表之间没有条文约束关系；
  `auto_method_forbidden` 又允许"生产档 + 保守决策点"下的 `NONE` 通过 AUTO 守卫（:1191-1193），
  与同一文件 :1176-1180 的"AUTO 路由禁止产出 min/max 与 NoRejection"形成条件式例外。
- 差在哪：三个决定"该像素到底排不排异"的数值/例外都只存在于实现里，正本与算法文档都无对应条款；
  `rejection.h:264-265` 把"AUTO 禁止产出 NoRejection"写成绝对禁令，实现却带例外（例外本身是 EXP-204 定案的结果）。
- 后果：路由与内核闸的合取（`REJECTION.md:51` 明确依赖这一"两面合取"）中，内核闸的一侧无条文可依；
  改动 `method_minimum_n` 或该例外不会与任何条文冲突，即"排异不生效"的门槛可由单点改动移动。方向：
  `minimum_n` 变大 ⇒ 更多 UNDERDETERMINED（不排异）⇒ 偏绿；变小 ⇒ 过拒 ⇒ 偏红 ⇒ 两侧都可能被静默推动。
- 定级：S3
- 怎么算修好：把"各方法 minimum N"与"AUTO 可产出 NONE 的唯一例外"写进 `docs/science/REJECTION.md` §4/§5
  （走变更流程），`rejection.h:211` 改为指向该表而非自指；
  同时给 `p2_rejection_applicability` 的 WARN 码（:2558-2612，同样只有 WBPP/NIST 的 file:line 而无本仓条文）补登记面。
- 置信：CONFIRMED（检索式已跑）

### CR-22-23 · 三个 Siril 系方法核的"至少保留 4"闸在跨迭代时把同一批剔除数扣了两次，实际剔除上限约为规则本意的一半（生产 n≥6 档可达）
- 轴：R（主轴）+ S（该闸无条文出处）
- 位点：`rejection.cpp:1582` —— `if ((int)nc - r <= 4) break;`（winsorized）；
  同型：`rejection.cpp:1693` —— `if ((int)(N - (std::size_t)r) <= 4) { keep[j] = 1; continue; }`（linear_fit）、
  `rejection.cpp:1883` —— `if ((int)nc - r <= 4) break;`（median_sigma）
- 上位依据：本仓对该闸的唯一自述是 `rejection.cpp:2580/:2583` 的注释 `"Siril N-r<=4"`；
  **`docs/science/REJECTION.md` 与 `docs/algorithms/REJECTION_ALGORITHMS.md` 对"保留/剩余/下限"零命中**
  （检索式：`git grep -n "保留\|剩余\|不剔\|下限" -- docs/science/REJECTION.md` → 无输出）。
  同仓的正确口径见 `:1906-1908`（minmax："n − (k_low+k_high) < min_kept"，N 为**原始** count）。
- 现状：`r` 在方法核的外层迭代**之前**声明并跨迭代累计（:1546 `int r = 0;`、:1662-1663、:1863），
  而 `nc`/`N` 每轮重取为**当前存活样本数**（= n0 − r），故守卫实际等价 `n0 − 2r ≤ 4`。
  数值例（n0=10，第一轮剔 3 ⇒ r=3、nc=7）：第二轮进入即 `7 − 3 = 4 ≤ 4` ⇒ 立即 break ⇒ 全程只剔 3 个；
  按"至少保留 4"的本意应可剔至 6 个。
- 差在哪：同一表达式里 `N` 与 `r` 的口径不同步（一个已扣除、一个继续扣除），使"保留下限"被双重计入。
- 后果：生产档 `n ≥ 6 → winsorized_sigma`（`REJECTION.md:65`）⇒ **交付路径可达**：
  污染样本数超过 (n0−4)/2 的像素，多出的离群样本不被剔除而进入 `p2_integrate_pixel` 的加权均值
  ⇒ mosaic 的 signal/support 面被污染样本抬高。方向：**偏绿**（守卫只可能少剔，不可能多剔；
  对"排异已把离群样本剔除"这一对外主张是漏证方向）。
- 定级：S2 —— 触发路径逐跳可写
  （`module_adapters.cpp:10865-10879` 按几何 n resolve ⇒ 生产档 n≥6 ⇒ winsorized →
  `module_adapters.cpp:11111` `p2_reject_stack_ex` → `rejection.cpp:2137-2140` `reject_winsorized_impl` → :1582），
  但**缺"真实轮次里 r 触发该闸的像素计数"这一角证据**（要定案 S1 需实跑一轮端到端并统计
  `iterations` 与 `rejected_low+high` 分布是否被该闸截断），故不升 S1。
- 怎么算修好：把守卫改为对**原始**计数判定：`if ((int)n - r <= 4) break;`（三处），
  并在 `docs/science/REJECTION.md` 补一条正向约束（"迭代式方法的剔除上限 = 保留不少于 4 个样本"），
  与 `min_kept` 统一命名与口径；必须同批：
  (1) 该改动会使 winsorized/linear_fit/median_sigma 剔得更多 ⇒ `实验/` 与端到端的 `nrej`/过拒率类判据会变红，
  属预期（`REJECTION.md:68` 已给出 M3 的过拒率基准 0.067%，改动后需重测该基准）；
  (2) `p2_rejection_applicability` 的 `W_NR_LE4` 码（:2580/:2583）语义随之一致化；
  (3) 补一条"多离群栈"负例（n=10、6 个 4σ 离群 ⇒ 现实现只剔 ~3 个，修好后应剔 6 个）。
- 置信：PARTIAL —— 代码级判定为 CONFIRMED（跨迭代口径由 :1546/:1582 与 :1550-1552 的 `cur` 重建方式直接可推），
  缺"实际数据上被截断的像素数"，需实跑 winsorized 档的 nrej 分布统计

### CR-22-24 · `p2_reject_stack_ex` 用 `rc=0 + reasons=全 UNDERDETERMINED` 表达三类"应 hard fail"状态，而交付调用方只判 rc ⇒ 掩码面把失败读成"全部接受"
- 轴：R（主轴，静默回退方向）+ C（内核自述契约 vs 调用方实现）
- 位点：`rejection.cpp:2045-2052` ——
  ```
  for (std::uint32_t i = 0; i < n; ++i) {
      if (!std::isfinite(stack->values[i]) ||
          (stack->weights != nullptr && !std::isfinite(stack->weights[i]))) {
          for (std::uint32_t j = 0; j < n; ++j)
              reasons_out[j] = P2_REASON_UNDERDETERMINED;
          out->accepted_count = n;
          out->status = P2_STATUS_INVALID_INPUT;
          return 0;
  ```
  同型：:2058-2081（两种 `INVALID_CONFIGURATION`）、:2020-2032（`INVALID_METHOD`）。
  自述契约：`rejection.cpp:2021-2022` —— "AUTO(10) 等非法方法 → 明确科学状态 INVALID_METHOD（rc=0，
  **调用方对非 {OK,UNDERDETERMINED} 一律 hard fail**）"。
- 上位依据：`rejection.h:414-415`（"返回 0=OK（out->status 表达科学状态）；非 0=参数非法"）；
  `docs/science/REJECTION.md` §4 把 `UNDERDETERMINED` 定义为"样本数不足，未做拒绝判定（**全接受**）"
  （`rejection.h:103` 同）。⇒ 在本仓语义里 `reasons=UNDERDETERMINED` 与"该样本进入积分"等价。
- 现状：交付调用方 `module_adapters.cpp:11111-11131` 只做 `if (krc != 0) { … return; }`，
  **不检查 `dec.status`**，随后 :11125-11126 以
  `reasons[s] == P2_REASON_ACCEPTED || reasons[s] == P2_REASON_UNDERDETERMINED` 生成逐样本接受掩码；
  而 `tools/stage2.cpp:1434-1441` 与 `src/acr_kernels.cpp:177-182` 两条非交付路径**都检查了 status**。
- 差在哪：内核契约规定安全性由"调用方检查 status"提供，实现该契约的只有被降级为 compatibility 的工具与 ACR 路径，
  交付节点链没有这一行。安全性目前完全依赖三个上游前提同时成立：
  ① `p2_collect_candidate_stack`（:1425-1454）已滤掉非有限 values/weights（⇒ INVALID_INPUT 不可达）；
  ② `module_adapters.cpp:10858` 把 request 硬编码为 AUTO ⇒ 方法集 ⊆ {none, percentile, winsorized}，
  与 resolve 给出的 `normalization` 默认（MEDIAN_CENTER）互斥组合不出现 ⇒ INVALID_CONFIGURATION 不可达；
  ③ AUTO 解析恒给显式方法 ⇒ INVALID_METHOD 不可达。
- 后果：任一前提被后续改动松动（最典型：配置面允许显式 `rejection.method = rcr`，
  而 RCR 要求 `normalization == NONE`，:2066-2071 会返回 INVALID_CONFIGURATION ⇒ 交付节点按现实现
  **把该像素全部样本当作 accepted 写出掩码**，排异整体被旁路且无错误面），产物即静默失真。
  方向：**偏绿**（把失败状态映射为"接受"，覆盖面与样本数只会更大）。
- 定级：S2 —— 缺陷本身可确证（契约与实现的直接矛盾，逐跳行号已给），
  但**缺当前可触发路径**（上述三前提今天全部成立）。
- 怎么算修好：(1) `module_adapters.cpp:11111` 之后补 `dec.status ∉ {OK, UNDERDETERMINED}` ⇒
  `ErrorDomain::DATA/INTERNAL` 具名失败（与 stage2/ACR 两条路径归一，`REJECTION.md` 的"两面合取"表述随之成立）；
  (2) 内核侧更稳的做法：失效状态下 `reasons_out` 不写 UNDERDETERMINED 而是保留 0（= 无 reason 定义）或写一个
  **不等于"接受"的具名值**，使"只读 reasons 的调用方"默认 fail-closed —— 但这是 `P2RejectReason` 枚举的结构性变更，
  需走变更流程并同批改 `p2_reject_stack` 与所有用例；(3) 必须同批：给该掩码面加一条负例
  （构造一个必返 INVALID_CONFIGURATION 的 plan，断言节点链失败且零产物），否则修好了也没有门。
- 置信：CONFIRMED（两处调用点与内核四分支逐行核对；未实跑）

### 不报的核过项（附检索式）
1. **AUTO 守卫的 NONE 例外不是漏洞**：`auto_method_forbidden`（:1184-1196）对 `NONE` 的放行条件 =
   `pixel_profile ∧ kConservativeNone ∧ 1≤n≤3`，与 `REJECTION.md:63` 的"1≤n≤3 → none"一致，且**其它**路径产出
   NONE 仍 fail-closed（注释 :1188-1190 明说"任何其它路径产出 NONE ⇒ 视为路由缺陷"）⇒ 条件式例外与正本一致，不报。
2. **`n <= 4` 全拒容错与条文 `n = 4` 观测等价**：n≤3 在 :2087 已被 `underdetermined_n`（生产档 3）截走，
   非生产档 `underdetermined_n=2` 时 n=3 进核但 `REJECTION.md:51` 论证"奇数 n 的百分位带必含中位样本（不可全拒）"
   ⇒ 两处写法的可达集相同 ⇒ 不按"实现比条文宽"报缺陷。
3. **percentile 的塌缩域**：`reject_percentile_impl`（:1829-1850）用 `scale = |orig_median|` 作带宽，
   近零天光时带宽→0 可致全拒 ⇒ 该行为与 `REJECTION.md:51` 条件③ + §8a 的"阈值是天光电平的固定分数"完全一致，
   且 `orig_median` 仅在 `norm != NONE` 时计算（:2114-2117）而 percentile 必须先经 :2058-2065 的
   `INVALID_CONFIGURATION` 门 ⇒ 无"未初始化中位数"路径，不报。
4. **资格层的 strided 版与连续版共用同一 policy**：`p2_eligibility_filter`（:1370-1390）与
   `p2_collect_candidate_stack`（:1392-1475）的判定顺序（finite → valid → weights finite → support > 阈 → quality 位）
   逐项同序同式，且 strided 版正确产出 `source_indices`（:1458-1459）⇒ `rejection.h:366-369` 的红线在生产面成立。
   检索式：`git grep -n "source_indices" -- lib/algorithms/coverage/src/rejection.cpp lib/infrastructure/scheduler/src/module_adapters.cpp`
   （生产消费点 `module_adapters.cpp:11080-11081/11129-11131` 已用映射，不错位）。
5. **`ScratchVec` 无静默截断**：`push_back` 在 `n_ >= CAP` 时整块迁堆（:1037-1045），`resize(n>64)` 走堆（:1019-1024）
   ⇒ `rejection.h:406` 的"n<=64 固定 scratch，>64 走堆"成立，未报"栈上缓冲越界"。
6. **ESD 临界值式合规**：:1774-1779 与标准 Grubbs/ESD 临界值式（m=n−i+1 时 λ=(m−1)T/√[m(m−2+T²)]）逐项等价
   （推导见处置表该行）⇒ **不报"漏 T² 因子"**（该直觉经复算被否定）；文档数值凭据的疑点另列末节移交线索。
7. **RCR 的 distinct-value 守卫与中位数插值**：`rcr_distinct_values`（:334-354）要求 flagged 中 ≥3 个不同值
   （官方 `distinctValuesCheck`）、`rcr_get_median`（:357-368）偶/奇分支复算因子分别得 0.5 与 1.0 ⇒ 与注释一致。
8. **`p2_large_scale_apply` 的参数校验先于 `enabled` 短路**（:2422-2426）⇒ 关闭状态下非法参数仍然判错，
   符合"非法参数不因关闭而豁免"，不报；`radius <= 0` 直接 return（:2351）也只表示不生长，
   原始 rejected mask 保持（:2408-2411 "只增不减"），无"拒绝像素被清零"路径。

### 本文件边界（如实登记，不当缺陷报）
- `p2_reject_classify` / `p2_reject_stack` / `p2_large_scale_apply` / `p2_rejection_weight_surface_guard`
  四者已在 `eng/ci/ledgers/prod_wiring.json:234-286` 以 `unreachable:header.P2_API`（类 A）登记为
  "不在三个生产命令调用图上"⇒ 本批不重复报"门未接线"；但三条登记记录的 `evidence` 字段指向
  `run/WIRING-W16-01/…`（不入库面）⇒ 与 CR-22-16 同型，位点在此登记。
- `method_minimum_n`、`min_kept`、五处 `1e-12` 退化阈等均属"实现决定判据"，条文缺口已分别在 CR-22-22/CR-22-23 处置。
- `p2_reject_classify` 的 :2787-2788 用**含等号**判剔，而 σ 族内核（:1520/:1584/:1622/:1885）用**严格不等**判剔
  ⇒ 同一 z=+3.0 在两面对"剔/不剔"给出相反答案。两面的判据主体不同（前者用预测残差方差 σ_eff，后者用稳健 MAD 尺度），
  属两个适用域而非自相矛盾 ⇒ 不报为缺陷，但若下游把两者混用于同一容差统计，该差异必须显式声明。

---

# 文件 6 · `lib/algorithms/coverage/tools/rejection_cli.cpp`（244 行，已全读）

## 交付面判定（派单问⑤）

| 判据 | 事实 |
|---|---|
| 构建地位 | `lib/algorithms/coverage/CMakeLists.txt:224 add_executable(rejection_cli tools/rejection_cli.cpp)`、:226 `set_target_properties(... EXCLUDE_FROM_ALL TRUE)`；同文件 :7 "astrocs-stage2 / calibrated_pair_diag / rejection_cli 为**按需工具**" |
| 发布清单 | `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:11` —— `exe_target,rejection_cli,...,tool,no,-,**非发布目标**` |
| 架构登记 | `docs/architecture/BUILD_GRAPH.md:85` / `eng/tools/arch/gen_build_graph_doc.py:51` —— "排异诊断工具（**非入口**）" |
| 生产调用图 | `eng/ci/ledgers/prod_wiring.json:280` 把它列为 COMPAT 符号的"非生产面消费者"之一 |
| 合同授权 | `docs/contracts/DATA_SEMANTICS.md:1515-1516` —— `P2_REJECT_MINMAX` 只作"实现事实留痕与诊断工具（本文件）的可达分支，**不是**可选生产路由" |
| **但是**：科学正本把它当证据载体 | `docs/science/REJECTION.md:377-379` —— 沿线取样点漏检率读数表（显著点 3.92%、真·单异常点 0.66%）声明 "逐点判定由**生产 kernel**（`build/lib/algorithms/coverage/rejection_cli` → `p2_reject_plan_resolve` + `p2_reject_stack_ex`）复现，与产品掩码 5744/5744 一致" |

结论：**不计入交付面**（三个生产命令的调用图上没有它，也不进 install 树），
但它是 `docs/science/REJECTION.md` 两处对外科学读数的**复现载体** ⇒ 其"隐式配置 ≠ 生产配置"与
"输出面不记 provenance"两件事直接影响科学结论的可复核性 ⇒ 按 D/C 轴报，不按交付产物报。

## §2.5 逐数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :43/:88 `if (vals.empty()) return 0;` | 空输入返回码 | 0 | 无样本即"成功" | 待确认 | 无条文。后果已定方向：空 stdin（管道断裂、夹具未生成）⇒ **rc=0 且 stdout 无 mask 行** ⇒ 以退出码判成败的 oracle 脚本会记"通过"（**偏绿**）；而 :52/:102 的内核失败路径返回 1（偏红）⇒ 两路不对称 | CONFIRMED |
| :47 `st.count = vals.size()` | 候选数 | — | 入参 | 结构性不适用 | 透传 | — | CONFIRMED |
| :48/:92 `data_type = 1` | fp64 标记 | 1 | 输入精度声明 | 结构性不适用 | `rejection.h:386`（0=fp32,1=fp64）；本工具用 `double` 读入 ⇒ 1 正确 | — | CONFIRMED |
| :52/:102 `!= 0 → return 1` | 内核 rc 判定 | 1 | 失败传播 | 待确认 | 只判 rc、不判 `dec.status` ⇒ `status ∈ {INVALID_INPUT, INVALID_CONFIGURATION, INVALID_METHOD}` 时内核把 reasons 写成 `UNDERDETERMINED`（`rejection.cpp:2045-2052/:2058-2081`），而本工具 :55-58 把 UNDERDETERMINED 打成 mask `'1'` ⇒ 掩码面呈现"全部接受"⇒ 见 CR-22-25（与 CR-22-24 同根） | CONFIRMED |
| :55-58/:64 `'1'` 判定 = ACCEPTED ∥ UNDERDETERMINED；reasons 以 `'0'+code` 输出 | 掩码与 reason 编码 | 0..3 | oracle 对拍面 | 公式导出 | `rejection.h:99-104` 的 reason 值域；与 `stage2.cpp:1445-1449`、`acr_kernels.cpp:184-188`、`module_adapters.cpp:11125-11126` 四处同一掩码口径 ✓ 一致 | — | CONFIRMED |
| :64 `('0' + dec.reasons[i])` | 单位数字编码 | — | reason 打印 | 结构性不适用 | 枚举基数 0..3 ⇒ 单个十进制位安全（>9 才会破，属枚举扩展风险） | — | CONFIRMED |
| :69 `status=%u` 以 `%u` 打 `int status` | 格式符与符号 | — | 打印 | 结构性不适用 | 值域 0..7 ⇒ 符号性无害；不影响数值 | — | CONFIRMED |
| :136 `j.value("request", "sigma")` | 缺省方法 | sigma | 未写 request 时 | 待确认 | `sigma` = `astrocs.robust_mad_clip.v1`（`rejection.h:47`），是合法显式方法，非静默危险默认；但**没有条文规定诊断工具的缺省方法**（检索式 `git grep -n "request" -- docs/algorithms/PHASE2_REJECTION.md`） | 影响范围 = 该工具的缺省行为 | CONFIRMED |
| :137-149 `kMethods[]` 12 项 | 方法名→枚举表 | 见代码 | 名字解析 | 结构性不适用 | 枚举基数 + 名称映射；`minmax`/`rcr` 在诊断面的可达性由 `DATA_SEMANTICS.md:1515-1516` 明示授权 ✓ | — | CONFIRMED |
| :159 `nominal` 缺省 | 几何 n | 20 | AUTO 路由入参 | 待确认 | 无锚：20 决定 AUTO 落在哪一档（n≥6 ⇒ winsorized，生产档与对照档在此一致；n≤3 或 4–5 才分档）⇒ 缺省 20 使"未写 nominal 的复现"恒走 winsorized；`REJECTION.md:43` 规定 n 必须是**逐像素几何 n**，工具无法从数据得到 ⇒ 只能由调用方给，缺省值是任意数 | 保守方向：应无缺省（缺 `nominal` 即报错）⇒ 偏红 | CONFIRMED |
| :161 `profile` 缺省 | 路由档 | "wbpp_2_9_1" | 对照档 | **待确认** | 生产默认档是 `astrocs_adaptive_pixel`（`docs/science/REJECTION.md:62`、`stage2_common.h:110`、`module_adapters.cpp:10855-10856`）⇒ 工具缺省把**对照档**当隐式默认，而对照档与生产档在 N≤3（percentile vs none）与 N≥16（linear_fit vs winsorized）两处行为不同 ⇒ 见 CR-22-25 | 方向：缺省档在 N≤3 会**真排异**（生产不排异）⇒ 用它采的边界证据与生产相反 | CONFIRMED |
| :163-164 `underdetermined_n` 缺省 | 内核小样本闸 | 2 | 生产档实际 3 | **待确认** | `REJECTION.md:45-48` 明文"生产默认档 AUTO ⇒ 3"；`rejection.cpp:1236-1238` 同；本工具写死缺省 2 ⇒ N=3 的像素在工具上会被排异、在生产上不会 ⇒ 同一 CR-22-25 的第二侧 | 同上 | CONFIRMED |
| :166 `char err[160]` | 错误缓冲 | 160 | 缓冲区尺寸 | 结构性不适用 | `p2_reject_plan_resolve` 内部 `snprintf` 截断安全 | — | CONFIRMED |
| :174 `normalization` 缺省 | 工作域 | "astrocs_median_center_v1" | 覆盖 resolve 结果 | **待确认** | resolve 对 `EXTREME_VALUE_PRIOR_SIGMA` 已强制 `P2_NORMALIZE_NONE`（`rejection.cpp:1301-1305`，理由见 `rejection.h:198-199`），本工具在 :175-179 按缺省串又改回 MEDIAN_CENTER ⇒ 方法 11 经此工具恒 `INVALID_CONFIGURATION` ⇒ 见 CR-22-26 | 方向：恒"全接受 + status=5" ⇒ 该方法的证据面恒空（偏绿） | CONFIRMED |
| :175-187 三个归一化别名串 | none / median_center / astrocs_median_center_v1 / median_scale / astrocs_median_scale_v1 | — | 名→枚举 | 结构性不适用 | canonical 与 alias 并存，未识别值 ⇒ 显式 return 2 ✓ fail-closed；与 `rejection.h:119-123` 枚举一致 | — | CONFIRMED |
| :188-189 `normalization_floor` 缺省 | 1e-12 | 与 `rejection.cpp:1245`、`rejection.h:214` 同值 ✓ | 结构性不适用 | 同文件 1e-12 族的无锚问题已在 CR-22 文件 5 处置表登记，此处只是复刻缺省 | CONFIRMED |
| :190-195 percentile 归一化前置校验 | — | — | 与内核 :2058-2065 同式的**重复实现** | 待确认 | 工具自己拦（return 2），内核也拦（INVALID_CONFIGURATION）⇒ 两处同式；但**内核拦的第三种互斥组合（extreme_prior 需 NONE，:2075-2081）工具没拦**（:196-200 只补了 rcr）⇒ 拦一半 ⇒ CR-22-26 | 保守方向：补齐两处（percentile/rcr/extreme）⇒ 只增拒绝（偏红） | CONFIRMED |
| :196-200 rcr 归一化前置校验 | — | — | 保证 RCR 以原始值域运行 | 公式导出 | `rejection.h`/`rejection.cpp:1804` "RCR 仅在 normalization=none 时合法" ✓ | — | CONFIRMED |
| :207-230 typed params 缺省（sigma/winsorized/averaged/median_sigma 各 4.0/3.0/8；linear_fit 5.0/3.5/8；esd 0.05/10；percentile 0.2/0.1；minmax 1/1/4） | 冻结继承值 | 同值 | 逐字段缺省 | 公式导出（继承） | 与 `rejection.cpp:1246-1258`、`rejection.h:464-466` 逐项一致 ✓（注意：本工具 :216-217 用的是 **5.0/3.5**，与 `rejection.h:133-134` 的结构体注释 4.0/3.0 相反 ⇒ 该注释错，见 CR-22-14；:221 用 **0.2**，与 `rejection.h:144` 的 0.1 相反 ⇒ 同条） | — | CONFIRMED |
| :234 `method = P2_REJECT_SIGMA` | 旧模式缺省方法 | 1 | 未给参数时 | 结构性不适用 | 与 :136 一致取 sigma；注意此值会被 :237 `atoi` 覆盖 | — | CONFIRMED |
| :235-236 `lo = -4.0`、`hi = 3.0`、`max_iter = 8`、`min_samples = 3` | 旧模式缺省 | 同值 | COMPAT 入参缺省 | 公式导出（继承值） | 4.0/3.0/8 继承锚同上；`min_samples=3` 对应 `P2_STATUS_MIN_SAMPLES` 旧语义（`rejection.h:440`），与 `method_minimum_n(sigma)=3` 同值但不同源 | — | CONFIRMED |
| :237-242 `std::atoi/std::atof` 的 argc 阶梯 2..6 | 位置参数 | — | 旧接口形态 | 结构性不适用 | argv 索引；但 `atoi` 对非数字返回 **0** = `P2_REJECT_NONE` ⇒ 见 CR-22-27（拼错方法名即静默"不排异"，且 status=0 看起来合法） | CONFIRMED |
| :36/:81 `"weight value"` 每行两列 | 输入布局 | — | weighted 模式契约 | 结构性不适用 | 与 :4 的自述一致 | — | CONFIRMED |
| :41/:86 `fscanf("%lf")` 接受 `nan`/`inf` 字面量 | 输入域 | — | glibc `%lf` 会解析出 NaN/Inf | 待确认 | 非有限值进内核 ⇒ `rejection.cpp:2045-2052` INVALID_INPUT ⇒ 掩码全 '1' ⇒ 与 :52 的 rc-only 判定共同构成 CR-22-25/26 的触发面 | CONFIRMED |

表 22 行；工具内出现的全部数值与缺省已列。

## findings

### CR-22-25 · 该工具被科学正本当作"生产 kernel 复现"载体，但它的三项隐式缺省（profile / underdetermined_n / 输出不记 provenance）使复现不可审计
- 轴：D（主轴，被指定的证据面与生产不同构）+ C
- 位点：`lib/algorithms/coverage/tools/rejection_cli.cpp:159-164` ——
  ```
  req.nominal_contributors = j.value("nominal", (std::uint32_t)20);
  const std::string profile_str = j.value("profile", std::string("wbpp_2_9_1"));
  req.profile = profile_str.c_str();
  req.underdetermined_n = j.value("underdetermined_n", (std::uint32_t)2);
  ```
  以及统计行 :67-71（只打 `status/accepted/rejected_lo/rejected_hi/iterations/method`，**不打** profile /
  underdetermined_n / nominal / normalization）。
- 上位依据：
  - `docs/science/REJECTION.md:377-379` —— 漏检率读数表的口径句原文
    "逐点判定由**生产 kernel**（`build/lib/algorithms/coverage/rejection_cli` → `p2_reject_plan_resolve` +
    `p2_reject_stack_ex`）复现，与产品掩码 **5744/5744 一致**"；
  - 生产侧配置真值：`module_adapters.cpp:10855-10861`（profile 缺省 = `P2_PROFILE_ASTROCS_ADAPTIVE_PIXEL`、
    `underdetermined_n = 0` ⇒ 由 profile 决定 ⇒ 生产档 3）；`REJECTION.md:45-48/:62-65`；
  - 该工具非交付：`PRODUCTION_EXECUTION_INVENTORY.csv:11` "非发布目标"、`CMakeLists.txt:226 EXCLUDE_FROM_ALL`。
- 现状：写 `--plan` 而省略 `profile`/`underdetermined_n` 两个键时，工具落在**对照档 + 闸 2**，
  生产落在**自研档 + 闸 3**；两档在 N≤3（percentile 真排异 vs none 不排异）与 N≥16（linear_fit vs winsorized）
  两条档上给出不同掩码；工具输出的统计行不含这两个键的取值，故事后无法从产物判断用了哪一档。
- 差在哪：被科学正本指定为"生产 kernel 复现"的载体，其隐式默认不是生产配置，且不把路由入参写进输出 ⇒
  "与产品掩码 5744/5744 一致"这句只能靠复现者口头保证，不能由工具自证。
- 后果：`REJECTION.md` §16 的漏排/漏检率表（4.96% / 3.92% / 0.66% / 11.09%，分母 5785/5611/4556/451）
  是创新点④"排异有效"的对外读数；若某次复现落在对照档，则同一掩码会被解释成"生产档已验证"，
  方向 **偏绿**（对照档排得更凶 ⇒ 漏检率被低估）。
- 定级：S2（对外科学读数的可复核性不成立；不致产品字节错值 ⇒ 不升 S1）
- 怎么算修好：(1) 三个路由入参**去掉缺省**（缺 `profile`/`underdetermined_n`/`nominal` 即 `return 2` 具名报错），
  或把缺省改成生产档（`astrocs_adaptive_pixel` + `underdetermined_n=0` 即"由 profile 决定"）；
  (2) 统计行追加 `profile=%s underdetermined_n=%u nominal=%u normalization=%s`，使每次复现自带 provenance；
  (3) 必须同批：`docs/science/REJECTION.md:378` 的口径句补上**实际使用的 plan.json 关键字段值**
  （正本改动走变更流程），否则修工具不改文档仍不闭合；
  (4) `PRODUCTION_EXECUTION_INVENTORY.csv:11` 的"非发布目标"与该句"生产 kernel"的措辞冲突需由负责人定一处说法。
- 置信：CONFIRMED（四处代码 + 三处条文逐字比对；未实跑）

### CR-22-26 · 工具在 resolve 之后覆盖 `normalization`，把 `extreme_value_clip_prior_sigma` 唯一合法的 `NONE` 改回缺省 `MEDIAN_CENTER`，且自身只拦 percentile/rcr 两种互斥组合 ⇒ 方法 11 经该工具恒"全接受"，prior 参数也无法提供
- 轴：R（主轴，静默降级）+ D（设计点名的 opt-in 方法在此不可行使）
- 位点：`rejection_cli.cpp:173-179` ——
  ```
  const std::string norm_s = j.value("normalization", std::string("astrocs_median_center_v1"));
  if (norm_s == "none") { plan.normalization = P2_NORMALIZE_NONE; }
  else if (norm_s == "astrocs_median_center_v1" || norm_s == "median_center") {
      plan.normalization = P2_NORMALIZE_MEDIAN_CENTER;
  ```
  与 :190-200（只补了 percentile 与 rcr 两项前置校验）、:207-230（无 `extreme_prior.*` 任何键）
- 上位依据：`rejection.h:198-199` —— "normalization **必须** = `P2_NORMALIZE_NONE`（本方法在原始 calibrated 值域
  直接比较绝对 prior_sky；**planning 层解析已保证**）"；`rejection.cpp:1301-1305` 正是该"已保证"的实现；
  `rejection.cpp:2075-2081` 则对手工 plan 走 `P2_STATUS_INVALID_CONFIGURATION`；
  `rejection.h:58-61`（该方法保留供调用方显式指定并自带外部先验）。
- 现状：工具 :167 先 resolve（此时 plan.normalization = NONE ✓），随后 :175-179 用**缺省串**把它改回
  MEDIAN_CENTER ⇒ 进内核即 :2075-2081 ⇒ `reasons = 全 UNDERDETERMINED` ⇒ :55-58 打出**全 '1' 掩码**，
  status=5 只出现在统计行；且工具不接受 `extreme_prior.prior_sigma/prior_sky/alpha/center_mode`，
  即便显式写 `normalization="none"`，也会因 `prior_sigma=0` 走 `extreme_prior_valid`（:1991-2001）⇒
  `UNDERDETERMINED` 全接受 ⇒ 该方法的排异能力在本工具上**两条路都不可达**。
- 差在哪：工具自述"显式 plan……与 stage2 生产同解析"（:13-14），实际在 resolve 之后又覆盖其科学判定域字段；
  并且它替内核复制了两条互斥校验（percentile、rcr）却漏掉第三条，说明该复制面不完整。
- 后果：任何人用本工具采集 `n=2/3` 小栈的先验 σ 排异边界证据（该档是 FIX-REJ §3 与
  `rejection.h:180-196` 明文设计的能力）都会得到"全接受"，并可能把 `status=5`/`status=4` 读成
  "样本不足"而非"配置被我改坏了"。方向：**偏绿**（掩码面与"该方法不剔任何样本"无法区分）。
- 定级：S3（工具面缺陷；`extreme_value_clip_prior_sigma` 在生产调度路径上由
  `module_adapters.cpp:10858` 的恒 AUTO + :10877-10878 的 center_mode=0 保护，不经此工具）
- 怎么算修好：(1) 删去 :175-187 对 `plan.normalization` 的覆盖，改为"仅当 request 不是 extreme_prior 时
  才接受 normalization 字段"，或对 extreme_prior 强制 `none` 并打印一句；
  (2) 把内核的三条方法×归一化校验集中成一个可复用的查询（内核已有 `p2_rejection_applicability`，
  但它是 n 维而非 normalization 维 ⇒ 需要扩一个 `p2_rejection_plan_consult` 或在工具内补齐第三条）；
  (3) 增加 `extreme_prior.prior_sigma/prior_sky/alpha/center_mode` 四个 JSON 键的读取，使该方法在此工具真正可行使；
  (4) 必须同批：非 OK 状态**不得**输出可被当掩码使用的 '1' 序列 —— 最小改动是 status ∉ {0,4} 时只打诊断行、
  掩码行改打 `-`（配合 CR-22-25(2) 的 provenance 行）。
- 置信：CONFIRMED（分支逐跳读完；未实跑）

### CR-22-27 · 旧兼容模式用 `std::atoi` 解析方法号，非法字符串静默得 0 = `P2_REJECT_NONE`，输出"全接受 + status=0"看似合法结果
- 轴：R（主轴，静默回退方向）+ C（与 `--plan` 面的严格名解析不对称）
- 位点：`rejection_cli.cpp:237` —— `if (argc >= 2) method = std::atoi(argv[1]);`
  （:234 已置 `int method = P2_REJECT_SIGMA;`，但只要有第 2 个参数就被 atoi 覆盖）
- 上位依据：`rejection.h:45-63` 枚举（0 = `P2_REJECT_NONE`）；同一文件 `:153-156` 的 `--plan` 面对未知方法名
  是具名报错 `unknown request` + `return 2`；`docs/science/REJECTION.md` 的排异顺序把 rejection 列为必需步骤
  （`rejection.cpp:2562-2564` 的 `W_NONE` 注释即"排异是必需步骤（DESIGN §4.5.1）"）。
- 现状：`rejection_cli sigma …`（即把方法名当参数传进旧模式）⇒ `atoi("sigma") = 0` ⇒ method=NONE ⇒
  `reject_none_impl` 全接受 ⇒ mask 全 '1'、`status=0`、`accepted=n`，**没有任何告警**；
  退出码 0 与一次真实的"不排异"运行完全同形。
- 差在哪：一个"看起来成功的错误用法"——工具对方法参数的两套解析（字符串名 vs 整数码）不对称，
  整数侧不做域校验也不报告。
- 后果：以退出码/掩码形状判成败的调用（或人）会把"根本没跑排异"当成"跑完且无样本被剔"。
  方向：**偏绿**（None 的掩码恒全 1）。
- 定级：S3
- 怎么算修好：(1) 旧模式改为与 `--plan` 同一张 `kMethods` 名表解析，或对 `method ∉ [0,11]` 与被atoi成 0
  但原串不是 "0" 的情况具名报错；(2) 保留数字入口时在统计行追加 `method_name=%s`
  （`p2_rejection_semantic_id(plan.method)` 现成可用）；
  (3) 必须同批：`rejection_cli.cpp:6-9` 的用法注释要写清旧模式收的是**整数枚举值**（现在只写 `<method>`）。
- 置信：CONFIRMED（`atoi` 语义 + 两条代码路径对照）

### 不报的核过项（附检索式）
1. **`minmax`/`rcr` 出现在工具方法表里不是越权**：`docs/contracts/DATA_SEMANTICS.md:1515-1516` 明示
   "`P2_REJECT_MINMAX` … 只作实现事实留痕与诊断工具（`rejection_cli.cpp`）的可达分支，**不是**可选生产路由"
   ⇒ 合同已声明，按前言 §4 不算缺陷。检索式：`git grep -n "rejection_cli" -- docs/contracts/DATA_SEMANTICS.md`。
2. **typed 参数缺省值与冻结继承值逐项一致**（:207-230 vs `rejection.cpp:1246-1258`）⇒ 不报阈值漂移；
   反倒是 `rejection.h:133/:144` 的两处结构体注释与二者相左（CR-22-14）。
3. **掩码口径与三处调用方一致**：:55-58 的 `ACCEPTED ∥ UNDERDETERMINED ⇒ '1'` 与
   `stage2.cpp:1445-1449`、`acr_kernels.cpp:184-188`、`module_adapters.cpp:11125-11126` 同式 ⇒ 工具不会给出
   与生产不同构的掩码语义（问题只在 status 不参与判定，见 CR-22-25/26）。
4. **reasons 单字符输出安全**：枚举值域 0..3 ⇒ `'0'+code` 不越界；`err[160]` 经 `snprintf` 截断安全。
5. **`--plan` 面的未知值全部具名拒绝**：request（:153-156）、normalization（:183-187）、
   resolve 失败（:167-170）各返回 2 ⇒ 不存在"合同拒绝的键被静默接受"。
6. **`rcr` 在本工具可达（供 CR-22-21 定级用）**：:196-200 允许 `request=rcr` 且要求 `normalization=none`，
   经此路径可把含 ≥4 个大数值样本的栈送进 `rcr_fit_dl` ⇒ CR-22-21 的越界读在该工具上是**用户可触发**的
   （仍不属交付面）。

---

# 本批最高危三条

1. **CR-22-23（`rejection.cpp:1582/:1693/:1883`）** —— 生产 n≥6 档（winsorized）的"至少保留 4"停止剔除闸把累计剔除数扣了两次
   （`nc − r`，而 `nc` 已排除已剔样本），实际剔除上限约为规则本意的一半；无条文出处
   （`docs/science/REJECTION.md` 对"保留/剩余/下限"零命中）。后果是污染样本留在加权均值里，方向偏绿，
   且交付路径逐跳可达（`module_adapters.cpp:10865-10879 → 11111 → rejection.cpp:2137-2140 → :1582`）。
2. **CR-22-24 + CR-22-25/26（`rejection.cpp:2020-2081` 与 `rejection_cli.cpp:52/:159-164/:173-179`）** ——
   内核用 `rc=0 + reasons=全 UNDERDETERMINED` 表达三类应 hard-fail 状态，而交付节点
   `module_adapters.cpp:11112` 只判 rc、不判 `dec.status`，掩码面因此把"失败"读成"全部接受"；
   今天不出事全靠三个上游前提（资格层滤非有限 + request 恒 AUTO + 方法×归一化组合天然合法）。
   同一编码在诊断工具面上已被 CR-22-26 实例化（方法 11 恒"全接受"）。
3. **CR-22-11（`kcorr_lookup_test.cpp:66-67`）** —— 门把 pixfrac 维的静默 clamp 锁成期望，
   与 `docs/contracts/DATA_SEMANTICS.md:1739` "域外一律取冻结默认 1.4、域外取值面 = 该冻结默认本身"
   及 `PHASE2_SAMPLER.md:572` "域外回退冻结默认 1.4（禁 clamp）"直接相反，且用例名叫 `..._not_clamp`；
   改正实现会先把这条门判红（典型"改对反而判红"），偏差量 = 控制点方差低估 13.5%（1.2112/1.4）。
   （次高：CR-22-21 RCR `min_error=999999` 哨兵致 `m=-1` 越界读，交付不可达但诊断工具可用户触发。）

---

# 我读不动／需要实跑才能定的

| 项 | 卡在哪 | 该跑什么（不由本节点执行） |
|---|---|---|
| CR-22-23 的实际截断量 | 静态可推"上限约 (n0−4)/2"，但需要真实轮次里 `iterations` 与 `rejected_low+high` 的联合分布才知道有多少像素被截断 | 生产档 winsorized 档统计：对一轮 mosaic 的每个 n≥6 像素跑 `p2_reject_stack_ex`，打印 `r` 是否触及 `nc−r<=4`；或先按修法打临时诊断版对比掩码差 |
| CR-22-10 / CR-22-07 的实测残差量级 | `ivar_wiring` 的 1T/2T、repeat-2T 最大差与"期望 vs 生产"最大差，决定收紧到逐位/相对 1e-5 后是否仍绿 | `ctest -R phase2_ivar_wiring --output-on-failure`（须经 `eng/tools/monitoring/mem_guard.py`，上限 4–8 GB）并在 `ADD_FAILURE` 文本里读 max 差；本节点按只读纪律不执行 |
| CR-22-09 `moc_order=7` 与 K=0 | 需要 AIO writer 源码级定案：`aio_hips_product_begin` 是否校验 `moc_order <= K`；本批独占面外 | 读 `lib/infrastructure/aio/src/hips_writer.cpp` 的 `product_begin` 体（约 1 次定向读即可定案），并跑 `phase2_ivar_wiring` 确认当前绿 |
| ESD `λ₁(54)=3.1588`（文件 5 处置表 :1774-1779 行的疑点） | 一手页 `https://www.itl.nist.gov/div898/handbook/eda/section3/eda35h4.htm` 本节点返回 302 → `https://www.nist.gov/itl/`（不可达）；本仓自述公式复算 λ₁(54)=3.134，与正本 3.1588 差 0.79%，且 3.1588 恰在 NIST 示例"统计量/临界值"两列同数量级 ⇒ 疑 R_i 与 λ_i 角色错绑 | 按 AGENTS §8 派子代理核验 NIST §1.3.5.17.3 原文与示例表两列；同时跑 ESD 门（`phase2_synthetic_gate` 的 ESD 族 + `eng/tests/api/test_reject_integration_oracle.py`）取实现自算的 λ₁ |
| 派单问①（球面面积几何精确性／极点／退化多边形） | 不属本批任号文件：`integrate.cpp` 无几何，面积换算在 `tools/stage2.cpp:1471 area = support_out * A_cell`；`A_cell` 定义与逐格重叠面积 `a` 的产生在 normalize/drizzle 侧 | 派单给 `A_cell` 与 `covered_area` 的产出文件（drizzle 球面重叠面积核 + HEALPix 面积式）所在批次；本批已在"派单差异"节标明 |
| CR-22-21 的越界读是否真的可达 | `error > 999999` 的判定来自量级复算（10²–10³ ADU 电平的残差平方和），未实测；且 `rcr` 不在交付配置面 | `rejection_cli --plan`（request=rcr, normalization=none）喂一个 20 样本、值域 10³、含 3 个离群的栈，观察是否崩溃/结果异常；或直接在 `rcr_mfinder` 加临时断言跑 synthetic_gate 的 Phase2Acr/RCR 族 |
| 分类排异 5 常量与 3 校准门的出处（CR-22-20） | 跟踪面只有键名与 SO-07 待签状态；`DESIGN-P2-001 §5` 是否含推导未逐页读 | 读 `docs/design/` 下 DESIGN-P2-001 的 §5（本批未展开）+ 向负责人确认 SO-07 签字面；若条文有推导，则本条降级为"引用缺失" |

## 移交线索（不属本批独占面，但由本批证据带出）

1. **`tools/stage2.cpp:1383 weights[s] = support_v[s];` 与 `src/acr_kernels.cpp:158 stack_w[s] = stack_sup[s]*wgt_v*wgt_v; // legacy 诊断`** ——
   §9.73 裁决 A44 删除的是"口径枚举/选择键"（`integrate.h:11-16`），但两个**已删口径的算术式**仍在非交付调用方存活并喂进
   `p2_integrate_pixel`（`rejection.h`/`integrate.h` 的单一权重口径主张在这些路径上不成立）。
   P0 机检 `CHK-NO-WEIGHT-MODE-CODE` 的判定面是**符号名黑名单**
   （`eng/ci/check_no_weight_mode_code.py:60-70`：`route_weight_mode(`、`p2_weight_mode_check`、`production_weight_modes(` 等），
   表达式形态的口径无法命中 ⇒ 该门对这两处恒绿。归属：stage2.cpp / acr_kernels.cpp / eng/ci 批次。
2. **`docs/science/REJECTION.md` 与 `docs/algorithms/PHASE2_REJECTION.md` 指向 `rejection.cpp` 的行号指针系统性漂移**
   （抽样 6 例：正本 :44-45 指 `2064-2073` 实为 `2087-2093`；:46 指 `1218-1225` 实为 `1230-1242`；
   :49-50 指 `1860-1874` 实为 `2205-2215`；:53 指 `1275-1283` 实为 `1285-1287`；
   `PHASE2_REJECTION.md:75` 指 `rcr_mfinder :460-518` ✓ 未漂移，但 :286 指 `:1549-1552` 实为 `:1571-1572`；
   `REJECTION_ALGORITHMS.md:159` 指 `规划层默认值 1226-1251` 实为 `1236-1269`）⇒ 追溯门若按行号取锚会指向错误代码。
   归属：docs 批次；本批只登记位点与漂移量（+20 ~ +345 行）。
3. **`docs/science/REJECTION.md:51` 对 `underdetermined_n = 3` 的表述与对照档实际值（2）不合**：
   该句把"1≤n≤3 由内核闸 `underdetermined_n = 3` 判 UNDERDETERMINED"当普遍事实陈述，
   而同句后半与 `rejection.cpp:1236-1242` 表明 3 只对生产档成立 ⇒ 对照档 N=3 会真排异。
   归属：科学正本订正（走变更流程）。
4. **`rejection.h:286` 指向的 `工程控制/RELEASE-03/tasks/EXP-204.md` 在跟踪面不存在**（`git ls-files | grep -i EXP-204` 零命中），
   而该决策点已由 `docs/science/REJECTION.md:63-64/:98` + `rejection.cpp:1145-1146` 定案 ⇒ 悬空指针（并入 CR-22-16 族）。

## 收工 git status 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
---HEAD---
c8f64e9a
```

与开工输出逐字相同 ⇒ 本节点零仓库写入、零 git 写、零 `git config` 写；
未跑构建/cmake/ctest/run_checks/任何 `eng/**`；未 import 仓库 Python；未建 `独立审计/复算件/CR-22/`（无中间件需要）。






