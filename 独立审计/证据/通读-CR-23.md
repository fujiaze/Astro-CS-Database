# 通读 CR-23 —— 真载体文件 `lib/algorithms/coverage/src/sky_plane.cpp`（2030 行整读）

- 批次：CR-23
- 被审基线：HEAD=`c8f64e9a`
- 清单：`独立审计/批次清单/CR-23.txt`（1 个文件）
- 派单前言：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§1 只读纪律／§2 四轴／§2.5 逐数值处置表／§3 finding 字段／§4 假阳清单／§5 落盘节奏）
- 清单与派单文字一致性：一致（派单点名 `sky_plane.cpp` 2030 行，清单同）。

## 开工 git status --porcelain 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 文件清单与阅读进度

- `lib/algorithms/coverage/src/sky_plane.cpp` —— 已读：是（1-2030 全读；§2.5 处置表 201 行（A 113／B 59／C 29）；findings 19 条 = S1 五条／S2 十二条／S3 两条）

<!-- PROGRESS: 1/1 -->

## 取证锚（本批用到的权威面，逐字定位）

| 锚 | 说的是什么 |
|---|---|
| `ASTROCS_DESIGN.md:171-174` | 「加权方差必须含源项……只含空背景项的方差是**背景受限**口径，**只用于天光建模与误差报告**；最优加权的唯一来源是含源项的总方差」 |
| `ASTROCS_DESIGN.md:186` | 「传统信噪比把天光信号计入信号，被天光抬高而失真。ACSD 的帧级 SNR 只计真实源信号能量与噪声」 |
| `ASTROCS_DESIGN.md:196` | 「UPM 在全部帧上联合建立一张连续的绝对天光参考平面：星点掩膜之外取稀疏背景采样点，**采样点带噪声逆方差权重**」 |
| `ASTROCS_DESIGN.md:411` | 「参考平面用其他帧的加权组合（**排除自身**）、迭代带阻尼、拟合权重与叠加权重同源、末端用叠加权重计算并扣除残差场；formulation 四要素见 `docs/plugins/algorithms_phase2/11_upm.md`」 |
| `docs/plugins/algorithms_phase2/10_sampling.md:47` | 「**采样点权重 = 逆方差** `w_ki = control_ivar_ki = N_retained/(k_corr·(π/2)·σ_bg²)`……**为什么不是 `SNR²`**：`w = 1/σ² = SNR²/F_ref² ∝ SNR²` 的 `∝` 以固定参考通量 `F_ref` 为前提，而天光控制点的**被估量本身在变** ⇒ `SNR²` **不是**有效逆方差代理；**控制点权重一律取 `control_ivar`，SNR 只作 veto/质量门**」 |
| `docs/plugins/algorithms_phase2/10_sampling.md:46` | 「每点携带该位置的 SNR：有稀疏绝对 SNR 层时由控制点重建得到 `SNR(x,y)`，无稀疏层时用**帧级标量**」（⇒ `P2_SKY_FLAG_NO_LOCAL_SNR` 点参与拟合本身不是缺陷） |
| `docs/plugins/algorithms_phase2/11_upm.md:72` | 拟合目标 `w_ki = 1/σ²_ki ∝ SNR_ki²`（与 10_sampling:47 的"不得取 SNR²"是**两个适用域**：前者为一般式、后者为控制点专项裁定） |
| `docs/plugins/algorithms_phase2/11_upm.md:58/:127` | 节点间距由输入几何导出（上界 = 重叠带宽度与指向间距的一半取小、下界 = 数据自身分辨率极限）；标定常数属另一形态 |
| `docs/plugins/algorithms_phase2/11_upm.md:86` | 收敛门（**无量纲**）：`max_dM/max(scale_obs,eps) < tol_step` **且** `|Δobj|/max(|obj_old|,eps) < tol_obj` |
| `docs/plugins/algorithms_phase2/11_upm.md:103` | `λ_eff = τ·mean(diag(H_red))`，不是自由参数、不进判决 |
| `docs/plugins/algorithms_phase2/11_upm.md:129` | `rank_rtol` 默认 `1e-10`，冻结值 `FZ-AP2S-RANK-RTOL`；`docs/science/PHASE2_UPM.md:199` 称其「由浮点算术给出（精度地板 `max(m,n)·eps`）」 |
| `docs/plugins/algorithms_phase2/11_upm.md:130` | `max_iter` 默认栏 = **空**（「——」） |
| `docs/contracts/DATA_SEMANTICS.md:1198`／`:3320`／`docs/ci/01_CHECKS.md:100` | 「~~weight_mode~~（键不存在）……配置中出现该键 ⇒ 具名 fail-closed 拒绝（§31.8）」；`CHK-NO-WEIGHT-MODE-CODE` 断言 `lib/**`＋`eng/tools/**` **无模式选择机制** |
| `docs/contracts/DATA_SEMANTICS.md:1764` | `σ_bg = MAD×1.482602218505602`；`(π/2)` = 样本中位数渐近方差因子，一手依据 Cramér 1946 §28.5（pp.367-369） |
| `docs/contracts/DATA_SEMANTICS.md:1739` | `control_k_corr` 默认 1.4，适用域 `pixfrac∈[0.5,1.0]` 且角尺度 `∈[300,600]″`，MC 标定 `UPMW-005 control_median_mc_test` 实测 1.3883 |
| `docs/references/SCIENTIFIC_REFERENCES.md:101/:107` | 文献 44 Huber 1964、文献 64 Holland & Welsch 1977 —— δ=1.345（Gaussian 95% 渐近效率）来源 |
| `lib/algorithms/coverage/include/astro/phase2/sky_plane.h:204/:216/:229` | 声明 `spline_degree` **默认 1**、`weight_mode` **0=inverse_variance／1=snr2**、`max_nodes` **默认 8192** |
| `lib/infrastructure/scheduler/src/module_adapters.cpp:9862-9864/:9868/:9973/:9999` | 生产装配：`sk.variance = o.control_variance`；`sk.snr = \|value\|/uncertainty`；`spc.spline_degree = sp_cfg.value("spline_degree", 3)`；**`spc.weight_mode = sp_cfg.value("weight_mode", 0)`**；`else spc.max_nodes = 8192` |
| `eng/ci/ledgers/prod_wiring.json:344/:454`＋`eng/ci/prod_wiring_baseline.json:119/:129` | `unreachable:header.P2_API:p2_sky_patch_estimate`、`unreachable:header.P2_API:p2_star_mask_caps`（本仓接线台账自判不可达） |
| `docs/science/CONTROL_WEIGHT_SNR.md:106` | 「天光采样点**必须**落在源掩膜之外（生产 `p2_star_mask_caps` / `p2_star_mask_contains`、`p2_sky_patch_estimate`、`p2_reject_plan_resolve` / `p2_reject_stack`）」 |

## 数值处置表（§2.5）

### 段 A：`sky_plane.cpp:1-800`

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| sky_plane.cpp:50 | `kPi` | 3.14159265358979323846 | deg↔rad 换算与球面三角常数 | 结构性不适用 | 用法见 :78/:353/:601/:610-611 | —（数学定义常数，不承载科学语义） | CONFIRMED |
| sky_plane.cpp:51 | `kMadToSigma` | 1.482602218505602 | MAD→σ 的一致性因子 | 公式导出 | `1/Φ⁻¹(3/4)`；`DATA_SEMANTICS.md:1764`、`PHASE2_SAMPLER.md:305` | 代码 :276/:291 真按乘式使用，未写死结果 | CONFIRMED |
| sky_plane.cpp:52 | `kPiHalf` | 1.57079632679489661923 | `Var(median)=(π/2)σ²/N` 的渐近因子 | 公式导出 | Cramér 1946 §28.5（`DATA_SEMANTICS.md:1764` 逐字引）；代码 :306 按式 | — | CONFIRMED |
| sky_plane.cpp:54 | `finite_pos` | `v > 0.0` | 正有限性判据 | 结构性不适用 | —（定义域判据） | — | CONFIRMED |
| sky_plane.cpp:58-59 | `huber_w` 分段点 `d`、`d/a` | 由 cfg 传入 | Huber 权重 ψ/r 比值 | 公式导出 | Huber 1964（`SCIENTIFIC_REFERENCES.md:101`）；`PHASE2_UPM.md:132` | 代码按 `min(1, δ/｜r｜)` | CONFIRMED |
| sky_plane.cpp:64 | 空样本中位数 | `0.0` | `median_sorted_inplace` 空输入返回值 | 待确认 | 条文面零提及（检索式 `git grep -n "median" -- docs/science docs/plugins`，无该返回值语义条款） | 保守方向＝**返回非有限并让调用方判红**；影响范围＝:271/:276/:284/:291 的 `m0/s0`，0 与"中位数恰为 0"不可分（见 CR-23-07） | PARTIAL |
| sky_plane.cpp:66-72 | `mid=n/2`、`0.5*(a+b)` | — | 偶数样本取上下中位平均 | 结构性不适用 | 分位数定义与数组下标，不承载科学语义 | — | CONFIRMED |
| sky_plane.cpp:82-83 | `2.0*kPi` 环绕 | — | Δλ 归入 (−π,π] | 结构性不适用 | 角度规范化定义 | — | CONFIRMED |
| sky_plane.cpp:89 | `cosc <= 1e-12` | 1e-12 | 切平面背半球/奇点保护下限 | 待确认 | 条文面零提及（`gnomonic`／`cosc` 在 `docs/science`、`docs/plugins` 无该阈值条款） | 保守方向＝**加大到 ~1e-9**（更早判背半球⇒边缘采样点被丢⇒`n_used` 变小、面更粗）；影响范围＝投影有效性边界（:622 建面、:1548 求值） | CONFIRMED(值无锚) |
| sky_plane.cpp:101-104 | `span` 夹紧到 `[d, n_coef-1]` | — | 样条 span 边界夹紧 | 结构性不适用 | de Boor 递推的下标边界（实现选择） | — | CONFIRMED |
| sky_plane.cpp:105-106 | `N[8]`/`left[8]`/`right[8]` | 8 | 基函数缓冲尺寸（d≤3 只需 4） | 结构性不适用 | 缓冲区尺寸；调用方 :695-696/:1559-1560 同为 8，`spline_degree` 只允许 1/3（:484）⇒ 不可越界 | — | CONFIRMED |
| sky_plane.cpp:112-113 | `j<=d`、`r<j` | — | 递推循环上界 | 结构性不适用 | 阶数 d 的算法上界 | — | CONFIRMED |
| sky_plane.cpp:114 | `denom != 0.0 ? N[r]/denom : 0.0` | 0.0 | 基函数递去除零保护 | 待确认 | 条文面零提及；均匀节点且 `h>0` 时 `denom=h·(正整数)` ⇒ 该分支应不可达 | 保守方向＝**分支被走到即判红**（当前静默给 0 基函数值）；影响范围＝样条基值⇒设计矩阵与拟合面 | PARTIAL（未证不可达） |
| sky_plane.cpp:121 | `span-d+r` | — | 系数下标位移 | 结构性不适用 | 位移量，无科学语义 | — | CONFIRMED |
| sky_plane.cpp:126 | `(order+1)(order+2)/2` | 公式 | 二元全次数 ≤p 的单项式个数 m | 公式导出 | `sky_plane.h:18`；order=1⇒3、order=2⇒6，代码 :567 真按式 | — | CONFIRMED |
| sky_plane.cpp:131-137 | `ξ=(u-uc)/us`、`pow(x,a)*pow(y,b)` | — | δ_k 归一化多项式基 | 结构性不适用 | 基函数定义（归一化量 :659-662 由数据 bbox 导出） | — | CONFIRMED |
| sky_plane.cpp:142/:147-149 | `n*n`、循环上界 | — | 稠密 Cholesky 索引与上界 | 结构性不适用 | 实现选择 | — | CONFIRMED |
| sky_plane.cpp:151/:155 | `!(sum>0.0)`、`!(d>0.0)` | 0.0 | SPD 正定性判据 | 结构性不适用 | 代数判据（主元必须为正） | — | CONFIRMED |
| sky_plane.cpp:190-196 | `info{}`、`h=1.0`、`degree=1`、`nx/ny=0`、`delta_order=1`、`m=3` | 1.0/1/0/3 | `SkyPlaneModel` 成员初值 | 结构性不适用 | 建面时全部覆盖（:593-595/:639/:642-643/:661-662）；`open()` 路径的覆盖情况见段 C 表（:1601-2030 待读毕再定） | 若 `open` 未写 `h` 则 1.0 会流入求值——该点在段 C 复核 | PARTIAL |
| sky_plane.cpp:228 | `min_samples`（patch） | 5 | 单 patch 最少有效像素数 | 待确认 | 登记面只有 sampler 侧：`PHASE2_SAMPLER.md:121-126`/`DATA_SEMANTICS.md:542`（`min_patch_samples` 默认 **64**）；`10_sampling.md:71` 的 `min_sky_samples` 默认栏为空 | 保守方向＝**取 64（与 sampler 侧同名门一致）或按 §7a 由像素尺度导出**；影响范围＝patch 估计可否（更小⇒更多 patch 过关，偏绿） | CONFIRMED(两处入口异值) |
| sky_plane.cpp:229 | `clip_iters` | 3 | 亮端 σ-clipping 轮数 | 待确认 | 登记值 `PHASE2_SAMPLER.md:122`/`CONFIG_SCHEMA.md:28`（3），**无推导、无标定件** | 保守方向＝**增大轮数只会更严（偏红）**，减到 0 则不裁剪；影响范围＝`sigma_mad`/`n_retained`⇒`variance`⇒`control_ivar`⇒权重 | CONFIRMED |
| sky_plane.cpp:230 | `clip_sigma` | 3.0 | 裁剪阈（MAD 单位） | 待确认 | `PHASE2_SAMPLER.md:231` 记「clip_sigma=3.0 下单侧裁剪使 MAD 系统性偏」但仍未给推导 | 保守方向＝**减小阈**（多裁⇒σ 偏小⇒ivar 偏大⇒权重偏绿）；影响范围＝同上 | CONFIRMED |
| sky_plane.cpp:231 | `contamination_sigma` | 3.0 | 污染判定 σ 倍数 | 待确认 | 登记值 `DATA_SEMANTICS.md:1734`（3.0，h:42），无推导 | 保守方向＝**减小⇒判红更多**；影响范围＝`bright_fraction` 分子⇒:316 污染门 | CONFIRMED |
| sky_plane.cpp:232 | `min_retained_fraction` | 0.60 | 裁剪后保留比例下限 | 待确认 | 登记值 `DATA_SEMANTICS.md:1735`/`PHASE2_SAMPLER.md:125`（0.60），无推导 | 保守方向＝**提高下限⇒更多 patch 判红**；影响范围＝:309 门 | CONFIRMED |
| sky_plane.cpp:233 | `k_corr` | 1.4 | Drizzle 相关导致的中位数方差放大因子 | 实验标定 | MC 标定 `UPMW-005 control_median_mc_test`（实测 1.3883 取保守 1.4）：`DATA_SEMANTICS.md:1739`；测试件在跟踪面 `lib/algorithms/drizzle/healpix_drizzle/tests/control_median_mc_test.cpp`（`TRACEABILITY.csv` 行 ALG-UPM-CONTROL-IVAR-001） | **适用域未在本文件检查**：1.4 只在 `pixfrac∈[0.5,1.0]`、角尺度∈[300,600]″ 成立（sampler 侧有 `kcorr_lookup`/provenance 域判据，本文件 :306 直接用字面量，无域检查、无 provenance 键）⇒ 见 CR-23-08 | CONFIRMED |
| sky_plane.cpp:249 | `min_samples<1→1` | 1 | 非法配置静默钳位 | 待确认 | 同族已登记形态：`DISP-NOISE-007`（`NOISE_ESTIMATION.md:202`）、`DISP-P2SMP-001`（`PHASE2_SAMPLER.md:556`）——「静默钳位无返回码区分」 | 保守方向＝**具名 rc 或直接判红**；影响范围＝下限门被静默放宽到 1 | CONFIRMED |
| sky_plane.cpp:250-251 | `clip_iters<0→0`、`!(clip_sigma>0)→3.0` | 0/3.0 | 同上静默钳位 | 待确认 | 同上 | 同上 | CONFIRMED |
| sky_plane.cpp:253 | `!(min_retained_fraction>0.0)→0.0` | 0.0 | NaN/负值 ⇒ **保留比例门被关闭** | 待确认 | 条文面零提及（§9.73/§31.8 要求非有限即具名拒绝）；与 :232 默认 0.60 矛盾 | 保守方向＝**非有限即 fail-closed**；影响范围＝:309 门对 NaN 配置恒真通过（fail-open，见 CR-23-07） | CONFIRMED |
| sky_plane.cpp:254 | `!(k_corr>0)→1.4` | 1.4 | 非法 k_corr 回退冻结默认 | 待确认 | 域判据在 sampler 侧（`UPM_SOLVER.md:52`：`k_corr<1` 越域 rc=1/rc=7），本文件只查 `>0` ⇒ 允许 `0<k_corr<1`（物理不可达域） | 保守方向＝**按 `1<k_corr` 定义域拒绝**；影响范围＝:306 variance 被低估⇒ivar 虚高 | CONFIRMED |
| sky_plane.cpp:266 | `n_total<min_samples` | — | 采样不足判红 | 结构性不适用 | 数量门（依赖 :228 的值） | — | CONFIRMED |
| sky_plane.cpp:283 | `nr.size()<min_samples` ⇒ `break` | — | 迭代保护（不判红即停） | 待确认 | 条文面零提及 | 保守方向＝**该情形应判红**（当前 break 后由 :309 兜住，语义为"保留不足"而非"裁剪崩溃"） | PARTIAL |
| sky_plane.cpp:285 | `1e-12*max(\|m0\|,1e-12)` | 1e-12 | 裁剪收敛相对阈 | 待确认 | 同族缺陷已登记 `DISP-P2SMP-005`（`PHASE2_SAMPLER.md:560`：m0≈0 时阈≈1e-24 过严） | 保守方向＝**改用 patch 尺度（σ）作分母**；影响范围＝迭代轮数（性能观察级，无科学输出影响） | CONFIRMED |
| sky_plane.cpp:292 | `s1<=0.0` ⇒ `break` | 0.0 | MAD 退化即停迭代 | 待确认 | 条文面零提及 | 保守方向＝**退化即判红**；影响范围＝与 :296 合起来把零离散度写成 1e-12 合法尺度 | CONFIRMED |
| sky_plane.cpp:296 | `sigma=(s0>0)?s0:1e-12` | 1e-12 | 零离散度保护下限（σ 地板） | 待确认 | 条文面零提及；对照 `UPM_SOLVER.md:113` 的 `sigma_eff=max(\|uncertainty\|,sigma_floor)` 要求 floor 具名 | 保守方向＝**σ=0 即判红（不可定权）**；影响范围＝`variance≈2.3e-24`、`ivar≈4.3e23`（见 CR23-段A finding CR-23-03） | CONFIRMED |
| sky_plane.cpp:299 | `v > y+contamination_sigma*sigma` | — | 污染计数定义 | 公式导出 | `PHASE2_SAMPLER.md:367`（同式，:865-867） | — | CONFIRMED |
| sky_plane.cpp:305 | `max(n_retained,1.0)` | 1.0 | 除法保护 | 结构性不适用 | 防 0 除（:266 门已保证 n_total≥min_samples） | — | CONFIRMED |
| sky_plane.cpp:306 | `variance = k_corr*(π/2)*σ²/n_ret` | — | 控制/patch 方差冻结式 | 公式导出 | `ALG-UPM-CONTROL-IVAR-001`（`TRACEABILITY.csv:3`）、`DATA_SEMANTICS.md:1764`、`10_sampling.md:47` | 代码真按式；成立条件（i.i.d.+大样本）由 `DATA_SEMANTICS.md:1764` 明列，本文件不再检查⇒适用域见 CR-23-08 | CONFIRMED |
| sky_plane.cpp:307 | `ivar=(variance>0)?1/variance:0.0` | 0.0 | 逆方差，非正⇒0 | 待确认 | `DATA_ARTIFACTS.md:26` 要求 `ivar≤0→rc=2`（具名失败），此处写 0 不返回码 | 保守方向＝**ivar≤0 判红**；影响范围＝0 权重与"不可用"在下游不可分 | CONFIRMED |
| sky_plane.cpp:309 | `< min_retained_fraction*n_total` | 0.60 | 保留比例门 | 待确认（值同 :232） | 同 :232 | 同 :232 | CONFIRMED |
| sky_plane.cpp:316 | `bright_fraction > 0.20` | 0.20 | 高污染判红阈 | 待确认 | 登记面 `CONFIG_SCHEMA.md:28`/`PHASE2_SAMPLER.md:122`：sampler 侧是**配置键** `background_max_contamination(0.20)`；本文件写成**硬字面量、不入 `P2SkyPatchConfig`** | 保守方向＝**与 sampler 侧共用可配置键**；影响范围＝两处入口对同一阈异值（见 CR-23-04） | CONFIRMED |
| sky_plane.cpp:331-332 | 参数合法性返回 1 | 1 | 星表掩膜入参门 | 结构性不适用 | 返回码约定（`sky_plane.h:130`） | — | CONFIRMED |
| sky_plane.cpp:336 | `snr[i] > snr_threshold` | 由调用方给 | 星点入选阈 | 待确认 | 阈来自调用方；`10_sampling.md:69` `bright_star_mask` 默认栏为空 | 保守方向＝**阈降低⇒更多星被掩⇒采样点更少（偏红）**；影响范围＝掩膜集合⇒采样点（本函数台账不可达，见 CR-23-05） | PARTIAL |
| sky_plane.cpp:337 | `k < cap` | 由调用方给 | 容量门（计数不受 cap 限制） | 结构性不适用 | 缓冲区容量语义（头文件 :134「out 可空（查询容量）」） | — | CONFIRMED |
| sky_plane.cpp:340/:359-361 | `P2_STAR_MASK_STAR`、`min(1,max(-1,c))`、`acos` | — | 掩膜类型与 acos 定义域夹紧 | 结构性不适用 | 枚举基数与数值定义域 | — | CONFIRMED |
| sky_plane.cpp:377-391 | 注释「本函数不含任何标定常数」 | — | 规则自述 | 结构性不适用 | 与 :419-443 实际实现一致（只有 /2、×2、3600） | — | CONFIRMED |
| sky_plane.cpp:407-415 | 四个几何量 `finite_pos` 门 | — | fail-closed 定义域门 | 结构性不适用 | 与 `sky_plane.h:191`、§7a 一致 | — | CONFIRMED |
| sky_plane.cpp:419 | `0.5*constraining` | 0.5 | 规则 1 上界 = 最小约束尺度 / 2 | 公式导出 | `11_upm.md:58/:127`、`PHASE2_UPM.md` §7a（`sky_plane.h:150`）、`sky_plane.h:179` | 代码真按式（:438 取 upper 作间距） | CONFIRMED |
| sky_plane.cpp:420 | `/3600.0` | 3600 | 角秒→度 | 结构性不适用 | 单位换算定义常数（假阳清单豁免） | — | CONFIRMED |
| sky_plane.cpp:421 | `max(sample_pitch_deg, pixel_deg)` | — | 下界 = 数据自身分辨率极限 | 公式导出 | `sky_plane.h:180`、`11_upm.md:58/:127` | 代码真按式 | CONFIRMED |
| sky_plane.cpp:425 | `!(upper >= lower)` ⇒ GEOMETRY_UNSUPPORTED | — | 区间相容性判据 | 结构性不适用 | `sky_plane.h:192` | — | CONFIRMED |
| sky_plane.cpp:439 | `2.0*upper` | 2.0 | 可表示最小尺度 = 2h | 公式导出 | `sky_plane.h:182`、§7a（表示能力） | — | CONFIRMED |
| sky_plane.cpp:441-443 | `*3600.0/pixel_scale` | 3600 | 度→源像素 | 结构性不适用 | 单位换算 | — | CONFIRMED |
| sky_plane.cpp:451 | `spline_degree = 3` | 3 | B_ref 样条阶数默认 | 待确认 | **`sky_plane.h:204` 自称「默认 1」**；注释「研究 Q4：三次张量 B 样条条件数 ~30 且与节点数无关」在权威面**零命中**（检索式：`git grep -n "spline_degree" -- docs` ⇒ 无任何 docs 命中；`git grep -n "Q4" -- docs/science docs/plugins` 未见该结论登记） | 保守方向＝**按头文件声明取 1**（双线性⇒条件数更低、表示能力更弱，偏红）；影响范围＝`n_coef` 起点 `t0=−d·h`（:640-643）、`nb=(d+1)²`（:690）⇒ 法方程阶与κ⇒整张天光面 | CONFIRMED(与声明打架) |
| sky_plane.cpp:454 | `node_spacing_deg = 0.0` | 0.0 | 「未给出」sentinel（0 不是度数值） | 结构性不适用 | `sky_plane.h:206-210`、§7a 禁止默认标定间距；未给出且几何不可导 ⇒ `GEOMETRY_REQUIRED`（:499-509） | —（0 具"未提供"义且路径 fail-closed，非把 0 当度用） | CONFIRMED |
| sky_plane.cpp:455 | `frame_gradient_order = 1` | 1 | δ_k 多项式阶默认 | 文献值→文档登记 | `11_upm.md:128` 默认 1（0=仅偏移，1=平面） | — | CONFIRMED |
| sky_plane.cpp:515 | `order > 2 → 2` | 2 | δ 阶上限 | 待确认 | 权威面上限：文件头 :7「p<=2」与 `11_upm.md:128`（未给 2 的依据；`bkg_model` 只有 spline/constant） | 保守方向＝**允许更高阶需 §7a 重述 δ 表示能力**；影响范围＝m=6⇒`min_samples_per_frame` 有效下限抬到 6（:576） | CONFIRMED |
| sky_plane.cpp:456 | `huber_delta = 1.345` | 1.345 | Huber 阈（Gaussian 95% 渐近效率） | 文献值 | Huber 1964（`SCIENTIFIC_REFERENCES.md:101`）、Holland & Welsch 1977（:107）；`PHASE2_UPM.md:132`、`PHASE2_UPM_IMPL.md:384` | — | CONFIRMED |
| sky_plane.cpp:457 | `max_iterations = 30` | 30 | 稳健 IRLS 外层迭代上限 | 待确认 | `11_upm.md:130` 的 `max_iter` 默认栏为**空**；`docs/modules/phase2_upm.md:169` 另一 UPM 求解器取 **100** ⇒ 同一族两个入口异值 | 保守方向＝**取 100（与同阶段另一求解器一致）或补推导**；影响范围＝未到 30 轮即停 ⇒ `w` 未收敛、`iterations=30` 无 `converged` 状态枚举（§4.6 要求 `0=max_iter/1=converged/2=stalled/3=invalid`，本文件只落 `iterations`）⇒ 见 CR-23-06 | CONFIRMED |
| sky_plane.cpp:458 | `tolerance = 1e-10` | 1e-10 | 收敛门相对系数 | 待确认 | `11_upm.md:86` 要求**双条件无量纲**（`max_dM/max(scale_obs,eps)` 且 `|Δobj|/max(|obj_old|,eps)`）；本文件 :1059 只有单条件且分母是 `max(1.0, sb)` | 保守方向＝**分母改用观测量尺度 σ 或目标函数**；影响范围＝ADU 电平 ≪1 时退化为绝对阈 1e-10⇒提前/延后判收敛（见 CR-23-06） | CONFIRMED |
| sky_plane.cpp:459 | `gauge_mode = 0` | 0 | 参考帧 δ≡0 | 文献值→文档登记 | `11_upm.md:125` `gauge` 默认 `reference_frame`；`sky_plane.h:26` | — | CONFIRMED |
| sky_plane.cpp:460 | `weight_mode = 0` | 0 | 权重口径选择（0=1/σ²、1=SNR²） | 待确认 | **合同面宣布该概念不存在**：`DATA_SEMANTICS.md:1198/:3320`、`PUBLIC_API.md:1213`、`docs/ci/01_CHECKS.md:100`；`10_sampling.md:47` 规定控制点权重**一律** `control_ivar` | 保守方向＝**删除该字段、单一口径 `1/control_variance`**；影响范围＝整张 B_ref 与全部 δ_k（见 CR-23-01/02） | CONFIRMED |
| sky_plane.cpp:463 | `rank_rtol = 1e-10` | 1e-10 | 唯一可辨识性阈 τ | 待确认 | 冻结登记 `11_upm.md:129`（`FZ-AP2S-RANK-RTOL`）；`PHASE2_UPM.md:199` 又称「由浮点算术给出（地板 `max(m,n)·eps`）」——4814 节点的地板 ≈ 1e-12，与 1e-10 差约 100 倍（**两处说法不同源**，非本文件错，但本文件取 1e-10 无推导） | 保守方向＝**τ 取更大（如 1e-8）⇒判红更严**；影响范围＝判决（`identifiable`、`lam`，:895/:1099-1136）⇒ 是否发布天光面 | CONFIRMED |
| sky_plane.cpp:464 | `retain_normal_matrices = 0` | 0 | 诊断保留开关（默认零内存） | 结构性不适用 | `sky_plane.h:223-225` 诊断语义 | — | CONFIRMED |
| sky_plane.cpp:465 | `min_samples = 8` | 8 | 全帧联合拟合最少采样点 | 待确认 | 权威面默认栏为空（`10_sampling.md:71` `min_sky_samples`＝「——」）；`10_sampling.md:59` 只说「数量下限由 UPM 自由度决定」⇒ 应**由 `n_free`/`m` 导出**而非取 8 | 保守方向＝**下限 ≥ 判据矩阵阶 `n_free` 的可辨识要求**（当前 8 远小于生产 n_free=1333-4814 ⇒ 门几乎不起作用）；影响范围＝:550 门 | CONFIRMED |
| sky_plane.cpp:466 | `min_samples_per_frame = 4` | 4 | 每帧 δ_k 可辨识最少点 | 待确认 | 同上（默认栏空）；与 :576 的 `max(m, ·)` 合成有效下限：order=1⇒4、order=2⇒6 | 保守方向＝**≥ 2m（留出稳健裁剪余量）**；影响范围＝:579 帧欠定判红 | CONFIRMED |
| sky_plane.cpp:467 | `max_nodes = 2048` | 2048 | 节点数（内存）门 | 待确认 | **`sky_plane.h:229` 声明「默认 8192」**；`module_adapters.cpp:9995-9999` 自述「算法侧实现里的 2048 与该声明漂移；编排面按声明取值」并 `else spc.max_nodes = 8192`；取值依据（M42 需 4814 档）落在 `run/UPM-NODE-ADAPT-01`、`run/CHAIN-WIRE-ADAPT-01/REPORT.md` | 保守方向＝**默认与声明一致取 8192**；影响范围＝直接调用 `p2_sky_plane_build` 而未显式覆盖 max_nodes 的入口 ⇒ `rc=5 TOO_MANY_NODES` 挡掉科学上更优的 h（见 CR-23-09） | CONFIRMED |
| sky_plane.cpp:468 | `max_extrapolation_deg = 0.0` | 0.0 | 越域余量（0=不外插） | 结构性不适用 | `sky_plane.h:230`、`11_upm.md` §4.5 fail-closed；0 在此具"禁止外插"义而非误用 0 作度 | — | CONFIRMED |
| sky_plane.cpp:484 | `spline_degree∉{1,3}→1` | 1 | 非法阶数静默改写（**与默认 3 反向**） | 待确认 | 条文面零提及（§31.8 要求非有限/非法具名拒绝） | 保守方向＝**非法即判红**；影响范围＝显式传 2 的调用方被静默按 1（双线性）求解，而默认是 3 ⇒ 同一入参面三种结果 | CONFIRMED |
| sky_plane.cpp:487/:511 | `node_spacing_source` 1=显式／0=导出 | 1/0 | 来源记账位 | 待确认 | `sky_plane.h:378-379`、`DATA_SEMANTICS.md:2859`（"生效值与导出依据"）——自适应路径 :1491 无条件写 0，与本行语义冲突（见 CR-23-10） | 保守方向＝**如实记 1**；影响范围＝provenance 键 `solvers.sky_plane.node_spacing_source` | CONFIRMED |
| sky_plane.cpp:494-512 | 几何可导时仍不覆盖显式值 | — | 显式优先规则 | 结构性不适用 | §7a/`11_upm.md:58` 允许配置显式给出 | — | CONFIRMED |
| sky_plane.cpp:514-525 | 静默钳位族：`<0→0`、`>2→2`、`!(>0)→1.345`、`<1→1`、`!(>0)→1e-10`、`<1→1`、`<1→1`、`<4→4`、`gauge_mode→0`、`weight_mode→0`、`max_extrapolation<0→0.0` | 见左 | 非法/非有限配置一律改写为默认，**无返回码区分** | 待确认 | 同族缺陷已由本仓登记：`DISP-NOISE-007`（`NOISE_ESTIMATION.md:202`）、`DISP-P2SMP-001`（`PHASE2_SAMPLER.md:556`）；合同 §31.8/`DATA_SEMANTICS.md:3320` 要求"出现即具名拒绝" | 保守方向＝**NaN/非法即 fail-closed 具名 rc**；影响范围＝`rank_rtol`（判决阈）、`tolerance`（收敛）、`weight_mode`（权重口径）全部可被 NaN 静默改写（见 CR-23-11） | CONFIRMED |
| sky_plane.cpp:522 | `max_nodes<4→4` | 4 | 节点门最小可容网格 | 结构性不适用 | 网格基数下限（nx,ny≥d+1） | — | CONFIRMED |
| sky_plane.cpp:533-534 | `bad = MASKED\|LOW_SUPPORT\|HIGH_CONTAMINATION\|REJECTED` | 4 位 | 剔除标记集合，**不含 `NO_LOCAL_SNR`** | 结构性不适用 | `sky_plane.h:56-60`；`10_sampling.md:46` 明文允许"无稀疏层时用帧级标量" ⇒ 该类点参与拟合本身**不是**缺陷 | —（不作 finding；但见段 B 的权重混用行） | CONFIRMED |
| sky_plane.cpp:538 | `!finite_pos(s.variance)` ⇒ 剔除 | — | weight_mode=0 的定义域门 | 结构性不适用 | `sky_plane.h:71`「必须 >0 有限方可用于拟合」 | — | CONFIRMED |
| sky_plane.cpp:540 | `!finite_pos(s.snr)` ⇒ 剔除 | — | weight_mode=1 的定义域门 | 待确认 | `sky_plane.h:72`「snr>0；**<=0 表示未知**」⇒ 未知被**静默丢弃**而非判红；与同一"未测"在两处用两种合法数值表达（丢弃 / flag）并存 | 保守方向＝**"未测"落具名 flag＋计数入 provenance**；影响范围＝`n_used`/`n_masked` 计数与权重面 | CONFIRMED |
| sky_plane.cpp:550/:576 | `min_samples`、`need_k=max(m, min_samples_per_frame)` | — | 数量门与 δ 块可辨识要求 | 公式导出（`n_k ≥ m` 是 `M_k` 非奇的要求，:852 强制） | 值本身待确认（:465/:466） | — | CONFIRMED |
| sky_plane.cpp:568 | `m<1` | 1 | 基函数数量下限 | 结构性不适用 | 组合下界 | — | CONFIRMED |
| sky_plane.cpp:599-612 | 球面均值方向定切平面中心：`180.0/kPi`、`+360.0`、`min(1,max(-1,sz))` | — | 投影中心选择（确定性） | 结构性不适用 | 单位换算与定义域夹紧；中心选择无条文（见下行） | — | CONFIRMED |
| sky_plane.cpp:598-613 | 切平面中心 = 采样点球面均值 | — | 投影原点算法选择 | 待确认 | 条文面零提及：检索式 `git grep -n "切平面中心\|投影中心\|ra0" -- docs/science docs/plugins` 无该规定 | 保守方向＝**用指向（footprint）中心**而非采样点均值⇒掩膜不对称时面会倾斜；影响范围＝gnomonic 全域坐标⇒B_ref 形状 | PARTIAL |
| sky_plane.cpp:618-619 | `numeric_limits::infinity()` 初值 | — | min/max 初值 | 结构性不适用 | 实现选择 | — | CONFIRMED |
| sky_plane.cpp:636-637 | 最小跨度 = `h`（对称扩 `0.5*h`） | 0.5 | 退化网格保护 | 待确认 | 条文面零提及（§7a 只给间距规则，未给"采样共线时补出 h 宽网格"） | 保守方向＝**采样跨度 <h 即判红**（当前造出无数据的行/列）；影响范围＝:1069-1077 最近邻填充的空节点集合⇒发布系数含**无数据支撑**的角点 | CONFIRMED |
| sky_plane.cpp:640-643 | `t0=min−d*h`、`nx=ceil(跨度/h)+d` | d | 节点计数与前置 knot | 公式导出 | 均匀张量 B 样条覆盖边界所需系数数（`sky_plane.h:18`「只存 nx*ny 系数」） | — | CONFIRMED |
| sky_plane.cpp:644-645 | `nx<d+1→d+1` | d+1 | 网格最小系数数 | 结构性不适用 | 样条基维数 | — | CONFIRMED |
| sky_plane.cpp:647-653 | `n_nodes > max_nodes` ⇒ rc=5 | — | 内存门（fail-closed，注释 :632-633 明记"不静默放粗"） | 结构性不适用 | `sky_plane.h:229`、:467 值本身待确认 | — | CONFIRMED |
| sky_plane.cpp:654-657 | 有效域 `du_min=t0u+d*h`、`du_max=t0u+nx*h` | — | 样条可求值域 | 公式导出 | 样条定义域（`sky_plane.h:20-21`、:459「越域不外插」） | — | CONFIRMED |
| sky_plane.cpp:661-662 | `us/vs = max(0.5*跨度, 1e-9)` | 1e-9 | δ 归一化尺度下限 | 待确认 | 条文面零提及；`1e-9` 度 ≈ 3.6e−6 角秒 ⇒ 实际只在采样共线时起效 | 保守方向＝**用 `node_spacing_deg`（几何导出的 lower_deg）作下限**；影响范围＝`pu[t]` 基值⇒δ_k 系数尺度⇒条件数 | CONFIRMED |
| sky_plane.cpp:676 | `base_w[t] = 1.0 / s.variance` | — | **权重构造的除法本身**（canonical 口径） | 公式导出 | `10_sampling.md:47` 冻结式 `w = control_ivar = 1/control_variance`；`ASTROCS_DESIGN.md:196`「采样点带噪声逆方差权重」；被除量 = `module_adapters.cpp:9862` 的 `o.control_variance`（= `k_corr·(π/2)·σ_bg²/N_retained`，**背景受限口径、不含源项散粒方差**） | —（含不含源项由 `ASTROCS_DESIGN.md:171-174` 判定：天光建模**只**用背景方差 ⇒ 此处**合规**，非缺陷） | CONFIRMED |
| sky_plane.cpp:677 | `base_w[t] = s.snr * s.snr` | — | weight_mode=1 的 SNR² 口径 | 待确认 | **被专项条款否定**：`10_sampling.md:47`「为什么不是 `SNR²`……控制点权重一律取 `control_ivar`，SNR 只作 veto/质量门」；且生产 `sk.snr = \|value\|/uncertainty`（`module_adapters.cpp:9863`）分子是**天光电平**⇒ 违 `ASTROCS_DESIGN.md:186`「传统信噪比把天光信号计入信号，被天光抬高而失真」 | 保守方向＝**删除该分支**；影响范围＝全部权重⇒B_ref/δ_k⇒`calibrated = raw − δ_k` 产品字节（见 CR-23-02） | CONFIRMED |
| sky_plane.cpp:679 | `w = base_w` 初值 | — | IRLS 起点 | 结构性不适用 | 迭代初始化 | — | CONFIRMED |
| sky_plane.cpp:690 | `nb=(d+1)*(d+1)` | — | 张量积支撑基函数数 | 公式导出 | 样条局部支撑性 | — | CONFIRMED |
| sky_plane.cpp:691-692/:730-732 | `used.size()*nb` | — | 稀疏行存储尺寸 | 结构性不适用 | 缓冲区尺寸 | — | CONFIRMED |
| sky_plane.cpp:706 | `node_weight += bvv*bvv` | — | 节点数据支撑度量 Σb² | 公式导出 | 支撑度量定义（注释 :713-714） | — | CONFIRMED |
| sky_plane.cpp:715 | `support_floor = 1e-9*max(max_node_weight,1e-300)` | 1e-9／1e-300 | 自由节点相对阈 | 待确认 | 条文面零提及（`11_upm.md:115-117` 要求"无观测几何节点"单列 `n_unobserved_geometry_nodes` 且**不混进** `n_unidentified`——本文件的自由节点门是另一套机制，无登记） | 保守方向＝**阈取更小（1e-12）**⇒更多节点进入判据矩阵；影响范围＝`n_free`（= 判据矩阵阶、`info.n_params`）⇒ κ/r_eff 判决与发布系数集合 | CONFIRMED |
| sky_plane.cpp:725 | `n_free<4 \|\| n_free<(d+1)²` ⇒ rc=3 | 4 | 最少自由节点数 | 结构性不适用 | 4 = bilinear 零空间维（:736-741 推导）；(d+1)² = 最小完整支撑 patch ⇒ 均非科学量 | — | CONFIRMED |
| sky_plane.cpp:736-741 | 零空间 {1, ix, iy, ix·iy}（4 维） | 4 | 惩罚零空间基 | 公式导出 | 二阶差分 stencil 公共零空间 = 双向仿射⇒双线性；注释自证与 `11_upm.md` §4.3 的 3 维 {1,ξ,η} gauge **多一维**并说明理由（δ 只到一阶，bilinear 是真实模型方向而非规范自由度） | —（该论证本身无权威文档复述，见 CR-23-12） | PARTIAL |
| sky_plane.cpp:746-753 | `c<4`、`ix=idx%nx`、`iy=idx/nx` | 4 | 基列构造 | 结构性不适用 | 数组维数与索引 | — | CONFIRMED |
| sky_plane.cpp:768 | `nrm > 1e-9*orig` | 1e-9 | Gram-Schmidt 退化列丢弃阈 | 待确认 | 条文面零提及（数值线性代数选择） | 保守方向＝**取更严（1e-12）**⇒更多方向参与锚定；影响范围＝`mq`⇒锚定与 deflation 校正的维度 | CONFIRMED |
| sky_plane.cpp:774 | `gauge_q` 尺寸 n_free×mq | — | 基存储 | 结构性不适用 | 缓冲区尺寸 | — | CONFIRMED |
| sky_plane.cpp:792 | `iter < max_iterations` | 30 | IRLS 循环上界 | 待确认（同 :457） | 同 :457 | 同 :457 | CONFIRMED |
| sky_plane.cpp:811-812 | `lower_bound(used, gi)` 反查局部下标 | — | O(log N) 索引 | 结构性不适用 | 实现选择（正确性：used 升序） | — | CONFIRMED |
| sky_plane.cpp:814 | `!(wi>0.0)\|\|!isfinite(wi)` ⇒ skip | — | 权重定义域门 | 结构性不适用 | 数值定义域 | — | CONFIRMED |
| sky_plane.cpp:824-827 | `ib<0 ⇒ continue` | — | 越界写防护（注释 :824-825 记 heap-buffer-overflow 教训） | 结构性不适用 | 索引防护 | — | CONFIRMED |
| sky_plane.cpp:830/:835 | `if (!ref)` 分支：参考帧 δ≡0 | — | gauge = reference_frame | 公式导出 | `11_upm.md:125`、`sky_plane.h:26-27` | — | CONFIRMED |
| sky_plane.cpp:848-882 | Schur：`H_red = H_data − Σ S_k M_k⁻¹ S_kᵀ` | — | 按帧消元 | 公式导出 | 文件头 :9 与 `sky_plane.h:19-20`；代码真按式（含 rhs 项 :861-866） | — | CONFIRMED |
| sky_plane.cpp:852-856 | `!chol_spd(M_k)` ⇒ rc=4 | — | 帧 δ 块非正定即判红 | 结构性不适用 | 代数判据（§4.5 失败条件） | — | CONFIRMED |
| sky_plane.cpp:893-895 | `mean_diag`、`lam = rank_rtol*mean_diag`（否则 0） | — | 派生数值岭 λ_eff | 公式导出 | `11_upm.md:103`、`PHASE2_UPM.md` §7a；代码 :895 真按式且**不参与判决**（:1099 判在 H_red） | — | CONFIRMED |
| sky_plane.cpp:898-928 | `retain_p` 分支、stencil 遍历 `ix+2<nx` | 1.0 | 惩罚矩阵构造与诊断保留 | 结构性不适用 | 循环上界与算子系数 | — | CONFIRMED |
| sky_plane.cpp:905 | `v = {1, −2, 1}` | 1/−2 | 二阶差分核 | 结构性不适用 | 差分算子定义（不承载科学语义） | — | CONFIRMED |
| sky_plane.cpp:931-936 | 注释「惩罚 ~1e-3 vs 数据 ~1e-18，差 ~1e15」 | 1e-3/1e-18 | 数值根因实测陈述 | 待确认 | 证据位 = `run/UPM-KAPPA-UNIFY-01`（**run/ 不作跟踪证据**；按 §2.5 第 3 档判不合格，需迁 `实验/` 或 `artifacts/`） | 保守方向＝**证据落跟踪面**；影响范围＝锚定+deflation 路径的必要性论证 | PARTIAL |
| sky_plane.cpp:947-952 | `alpha = max_i(H_solve_ii − H_red_ii)`（惩罚对角量级） | — | 零空间锚强度 | 公式导出 | 注释 :931-936 与 :944-968（只锚惩罚零空间方向，随后 deflation 精确扣回） | — | CONFIRMED |
| sky_plane.cpp:964-968 | `B = B_a + Q G⁻¹(Qᵀ rhs − Qᵀ H_red B_a)` | — | deflation 校正 | 公式导出 | Sherman-Morrison/Woodbury 型低秩校正；代码 :969-1008 真按式 | — | CONFIRMED |
| sky_plane.cpp:1014-1017 | `!solved` ⇒ rc=6 "reduced normal matrix not SPD" | — | 数值失败即判红（不伪装） | 结构性不适用 | `sky_plane.h:391-392` | — | CONFIRMED |
| sky_plane.cpp:1020-1023 | `db=max\|ΔB\|`、`sb=max\|B\|` | — | 收敛读数定义 | 结构性不适用 | 判据构造（阈本身见 :458/:1059） | — | CONFIRMED |

> 段 A 计数：**113 行**（现场计数命令：`awk '/^### 段 A/{s="A"} /^### 段 B/{s="B"} /^### 段 C/{s="C"} /^\| sky_plane\.cpp/{c[s]++} END{print c["A"], c["B"], c["C"]}' 通读-CR-23.md` ⇒ `113 59 29`）。处置档的**分档计数不写**（§2.5 的验收项是"逐项有处置"，且若干行为"结构性不适用＋待确认"复合处置，分档会失真）；同族合并行（静默钳位 :514-525、单位换算 3600/180、循环上界）均在行内列出全部位点。

### 段 B：`sky_plane.cpp:801-1600`

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| sky_plane.cpp:814 | `!(wi>0.0)\|\|!isfinite(wi)` ⇒ skip | 0.0 | 权重非正即不参与法方程 | 结构性不适用 | 数值定义域门（权重必须为正） | — | CONFIRMED |
| sky_plane.cpp:818-833 | `a<nb`、`b<nb`、`q<m` 累加 | — | `AᵀWA`、`AᵀWP` 装配 | 公式导出 | 文件头 :9 与 `sky_plane.h:19-20` 的 Schur 式；代码真按式 | — | CONFIRMED |
| sky_plane.cpp:830/:835/:850/:1028 | `ref`（参考帧）分支 | — | 参考帧 δ≡0 的 gauge 实现 | 公式导出 | `11_upm.md:125`（`gauge` 默认 `reference_frame`）、`sky_plane.h:26` | — | CONFIRMED |
| sky_plane.cpp:845-882 | `H_red = H_data − Σ S_k M_k⁻¹ S_kᵀ`、`rhs −= Σ S_k M_k⁻¹ t_k` | — | 按帧 Schur 消元 | 公式导出 | 文件头 :9；`sky_plane.h:19-20`「内存与帧数无关」；代码真按式（:861-866 rhs、:867-882 矩阵） | — | CONFIRMED |
| sky_plane.cpp:852-856 | `!chol_spd(M_k)` ⇒ rc=4 | — | 帧 δ 块奇异即判红 | 结构性不适用 | 代数判据＋`sky_plane.h:389` | — | CONFIRMED |
| sky_plane.cpp:893-895 | `mean_diag`；`lam = rank_rtol*mean_diag`（否则 `0.0`） | — | 派生数值岭 λ_eff | 公式导出 | `11_upm.md:103`、`PHASE2_UPM.md` §7a、文件头 :11-12 | `mean_diag<=0` 时 λ=0 ⇒ 无正则直接 Cholesky（随后 :1014 判红，不伪装）✓ | CONFIRMED |
| sky_plane.cpp:898-919 | `retain_p = (cfg.retain_normal_matrices != 0)`；惩罚以 coef `1.0` 单记 | 1.0/0 | 诊断保留与惩罚算子导出 | 结构性不适用 | `sky_plane.h:223-225` 诊断语义 | — | CONFIRMED |
| sky_plane.cpp:904 | `f0/f1/f2 < 0 ⇒ return`（空节点不参与） | — | 无支撑节点不入惩罚 | 结构性不适用 | 与 :715-723 自由节点集合同源 | — | CONFIRMED |
| sky_plane.cpp:905 | `v = {1, −2, 1}` | 1/−2 | 二阶差分核（两方向 :911/:920） | 结构性不适用 | 差分算子定义（零空间推导见 :736-741，另见 CR-23-12） | — | CONFIRMED |
| sky_plane.cpp:931-936 | 注释「惩罚 ~1e-3 vs 数据 ~1e-18 ⇒ 差 ~1e15」 | 1e-3/1e-18 | 锚定分支的数值根因陈述 | 待确认 | 证据位 = `run/UPM-KAPPA-UNIFY-01`（**run/ 非跟踪证据面**，按 §2.5 第 3 档判不合格；负责人已裁「证据落 `实验/`」） | 保守方向＝**证据迁跟踪面**；影响范围＝:944-1013 锚定+deflation 分支是否必要 | PARTIAL |
| sky_plane.cpp:939-943 | 先直接 `chol_spd(H_solve)`；成功即逐位不变 | — | 常规档路径 | 结构性不适用 | 文件头 :17-18 | — | CONFIRMED |
| sky_plane.cpp:947-952 | `alpha = max_i(H_solve_ii − H_red_ii)`；`alpha>0` | — | 锚强度 = 惩罚项对角量级 | 公式导出 | 注释 :931-936（构造式：惩罚贡献的最大对角）；代码真按式 | — | CONFIRMED |
| sky_plane.cpp:953-960 | `H_solve += alpha·Q Qᵀ`（只加在 mq 个零空间方向） | — | 极小 Tikhonov 锚 | 公式导出 | 低秩改造；`sky_plane.h:259-261` | — | CONFIRMED |
| sky_plane.cpp:964-1008 | `B = B_a + Q·G⁻¹·(Qᵀ rhs − Qᵀ H_red B_a)`，`G = Qᵀ H_red Q` | — | deflation 扣回锚偏置 | 公式导出 | 注释 :966-968 自证（`P Q = 0` ⇒ 解满足 `(H_red+λDᵀD)B=rhs`）；代码真按式（:969-1008） | — | CONFIRMED |
| sky_plane.cpp:1000-1010 | `!chol_spd(G)` ⇒ 不 solved ⇒ rc=6 | — | 零空间块非正定即判红 | 结构性不适用 | 代数判据 | — | CONFIRMED |
| sky_plane.cpp:1020-1023 | `db = max\|ΔB\|`、`sb = max\|B\|` | — | 收敛读数 | 结构性不适用 | 判据构造（阈见下行） | — | CONFIRMED |
| sky_plane.cpp:1022 | 首轮 `B` 为全 0 ⇒ `db = max\|B_new\|` | 0.0 | 第 0 轮的"变化量"起点 | 结构性不适用 | 迭代初始化（:788 `B` assign 0） | — | CONFIRMED |
| sky_plane.cpp:1028 | 参考帧 δ 全量 `fill(0.0)` | 0.0 | gauge 落值 | 结构性不适用 | `sky_plane.h:26`（δ_ref≡0）——**0 在此是 gauge 选择的必然值，不是"未测"** | — | CONFIRMED |
| sky_plane.cpp:1029-1039 | `d = M_k⁻¹(t_k − S_kᵀ B)` | — | 回代求 δ_k | 公式导出 | Schur 补的标准回代（文件头 :9） | `:1037` 忽略 `chol_spd` 返回值，但 `M_k` 与 **同一轮** :852 已判正的矩阵同源（权重在 :1056 才更新，回代发生在更新之前）⇒ 非缺陷，已核 | CONFIRMED |
| sky_plane.cpp:1049-1052 | `fit = Σ b·B + Σ pu·δ_k` | — | 采样点模型值 | 公式导出 | `sky_plane.h:11-13` 模型式 | ⚠ **不含 `gauge_shift`**，而 :1162 的报告段加上了 ⇒ 同一 fit 两套口径，见 CR-23-13 | CONFIRMED |
| sky_plane.cpp:1053 | `sigma = (weight_mode==0) ? sqrt(s.variance) : 1/max(s.snr, 1e-12)` | 1e-12 | σ_eff（标准化残差分母） | 待确认 | `PHASE2_UPM.md:132` 要求「`Huber(δ=1.345)` 作用于**无量纲** `z`」；`UPM_SOLVER.md:113` 的 `sigma_eff=max(\|uncertainty\|, sigma_floor)` 是另一实现口径 | 保守方向＝**weight_mode=1 一律按 σ=√variance 或用 `F_ref` 归一**；影响范围＝`z`⇒Huber 权重⇒全平面（见 CR-23-13） | CONFIRMED |
| sky_plane.cpp:1055 | `z = (sigma>0)? r/sigma : 0.0` | 0.0 | 标准化残差 | 待确认 | sigma≤0 时 z=0 ⇒ **Huber 权重取 1（满权）**，把"不可定权"当"完全正常"；条文面零提及 | 保守方向＝**w=0 或判红**；影响范围＝离群点不再被降权 | CONFIRMED |
| sky_plane.cpp:1056 | `w[t] = base_w[t] * huber_w(z, cfg.huber_delta)` | — | IRLS 权重更新 | 公式导出 | Huber 1964（`SCIENTIFIC_REFERENCES.md:101`）；代码真按式 | — | CONFIRMED |
| sky_plane.cpp:1057/:1169 | `\|z\| > 5.0` ⇒ `++nrej` | 5.0 | "稳健剔除"计数阈 | 待确认 | 条文面零提及（检索式 `git grep -n "5.0\|n_rejected" -- docs/plugins docs/science/PHASE2_UPM.md` 无该阈规定）；且与 `huber_delta=1.345` 无关系推导 | 保守方向＝**减小即更多点被记为 rejected**；影响范围＝仅诊断量 `info.n_rejected`（真实降权由 Huber 完成，:1056）⇒ 计数与降权两套口径 | CONFIRMED |
| sky_plane.cpp:1042/:1057 | IRLS 内层 `nrej` 变量 | — | **累加后从未使用**（被 :1151 同名变量遮蔽） | 结构性不适用 | 死计数（不影响数值）；报告值取最后轮 :1169 | — | CONFIRMED |
| sky_plane.cpp:1059 | `iter>0 && db <= tolerance*max(1.0, sb)` ⇒ break | 1.0 | 收敛门 | 待确认（同 :458） | `11_upm.md:86` 要求双条件＋分母用观测量尺度 | 见 CR-23-06 | CONFIRMED |
| sky_plane.cpp:1063-1077 | 无支撑节点用**最近自由节点**系数填充（`bestd` 曼哈顿距离） | 0.0 | 角点空节点的系数回填 | 待确认 | 注释自称「SWarp/SEP 对无效 mesh 节点做最近邻填充的先例」——**本仓权威面零规定**（检索式：`git grep -n "最近邻填充\|nearest" -- docs/plugins docs/science` ⇒ 无该规则）；`11_upm.md:115-117` 只规定「无观测几何节点不参与判据、单列 `n_unobserved_geometry_nodes`」，未规定其**发布系数**如何取值 | 保守方向＝**空节点不参与产品（或落 null＋计数）**；影响范围＝`coeff` 全量入产品与 `model_hash`（:1236）⇒ 越域求值（含 `max_extrapolation_deg` 余量）读到的是**伪造的邻点值** | CONFIRMED |
| sky_plane.cpp:1077 | `best<0 ⇒ 0.0` | 0.0 | 无自由节点时系数写 0 | 结构性不适用 | :725 已保证 `n_free ≥ 4` ⇒ 该分支不可达 | — | CONFIRMED |
| sky_plane.cpp:1099-1101 | `p2_identifiability_assess(H_red, n_free, used.size(), rank_rtol, &id)` | — | 唯一判据（与 UPM/GLS 同一函数） | 公式导出 | `11_upm.md:97-100`、`PHASE2_UPM.md:199`、`identifiability.h` | — | CONFIRMED |
| sky_plane.cpp:1102-1109 | `id_rc != 0` ⇒ rc=6 | — | 判据不可定义即判红（不发布伪值）✓ | 结构性不适用 | `sky_plane.h:391-392` | — | CONFIRMED |
| sky_plane.cpp:1113 | `kappa_data = kappa` 兼容别名 | — | 两键同值以免下游断键 | 结构性不适用 | `sky_plane.h:343-345` | — | CONFIRMED |
| sky_plane.cpp:1115-1125 | `kappa_solve` 缺失时 `quiet_NaN()`；诊断不计入判决 | NaN | 求解稳定性诊断 | 结构性不适用 | NaN 在此是**具名不可计算**表达（正面例子，与 :1888 的 0.0 形成对照，见 CR-23-17） | — | CONFIRMED |
| sky_plane.cpp:1126-1137 | `!id.identifiable` ⇒ rc=7 + err 全读数 | — | 判据判红（rank/κ/τ/λ 全落 err） | 结构性不适用 | `sky_plane.h:393-396`、`PHASE2_UPM.md:217`（读数必须可观测） | — | CONFIRMED |
| sky_plane.cpp:1141-1147 | gauge_mode=1：`c = mean_k δ_k[0]`，各帧减 c，记 `gauge_shift` | — | 和为零 gauge（常数平移） | 公式导出 | `sky_plane.h:27-28`；只平移常数项（δ_k 基的第 0 个 = 1） | — | CONFIRMED |
| sky_plane.cpp:1141 | `n_frames > 1` 才做 sum gauge | — | 单帧不平移 | 结构性不适用 | 单帧时 δ 恒 0（:1028） | — | CONFIRMED |
| sky_plane.cpp:1150-1169 | `sw/swr2/sr2` 用 **base_w**（不含 Huber） | — | 报告用加权 RMS/χ² | 待确认 | 注释 :1164-1165 自称与 `p2_sky_plane_residuals` 独立复算一致（:1688-1689 确实用同一 base_w）✓；但**判据/拟合用的是稳健 w**，报告用非稳健 w ⇒ `11_upm.md:88` 的「拟合质量三元组」另要求 `Σw/Σraw_w`（稳健保留率）落盘，本文件**无该键** | 保守方向＝**补 `Σw/Σraw_w` 与 `rms_z`**；影响范围＝产品里 rms/chi2 与被审可读性 | CONFIRMED |
| sky_plane.cpp:1162 | 报告段 fit **加** `gauge_shift`；IRLS 段（:1049-1052）**不加** | — | 同一模型值两套口径 | 待确认 | `sky_plane.h:471-474` 规定 `δ_k = gauge_shift + 多项式项`（B 口径）⇒ 求值路径都加 gauge（:1633），而 IRLS 拟合与 :1199-1208 的报告残差在 gauge_mode=0 时无差（gauge_shift=0），**gauge_mode=1 时 :1049 的 fit 少一个常数 c** ⇒ 见 CR-23-13 | 保守方向＝**统一含 gauge 项**；影响范围＝`w`（Huber 降权对象整体平移）⇒ 迭代解 | CONFIRMED |
| sky_plane.cpp:1172 | `info.version = 1` | 1 | 信息结构版本 | 结构性不适用 | 版本枚举，非科学量 | — | CONFIRMED |
| sky_plane.cpp:1180-1184 | `n_params_full = n_free + (n_frames−1)·m`、`rank_full` 同构 | — | 全部未知量/秩 | 公式导出 | `sky_plane.h:339-341`、`11_upm.md:105`（rank(完整)=r_eff+(帧−1)·m，前提 δ 块满秩已由 :852 强制） | — | CONFIRMED |
| sky_plane.cpp:1199-1200 | `rms_weighted = sw>0 ? sqrt(swr2/sw) : 0.0`；`rms_unweighted = sqrt(sr2/used.size())` | 0.0 | 加权/未加权 RMS | 待确认 | `sw<=0` 时写 0.0 = "零残差完美拟合"合法值（`DATA_SEMANTICS.md:2868` 要求必落 `chi2_red` 等，但未规定不可定义时的表达） | 保守方向＝**写 null/具名不可得**；影响范围＝产品 `info.rms_weighted`（全零权重面可达） | CONFIRMED |
| sky_plane.cpp:1206-1208 | `dof_eff = p2_identifiability_dof(n_used, rank_full)`；`chi2_red = dof_eff>0 ? swr2/dof_eff : 0.0` | 0.0 | 有效自由度与约化 χ² | 待确认 | **正面条文反对 0**：`11_upm.md:112`「`dof_eff <= 0` ⇒ `chi2_red` 写 `null` 且 `chi2_red_defined = false`（无定义，取值 = `null`；**0 会冒充完美拟合**）」；`DATA_SEMANTICS.md:2862` 对 `upm_gls.chi2_red` 采同一「无定义时为 null」处置，而 `:2868` 对 `solvers.sky_plane` 只说必落 `chi2_red` ⇒ 同一量两侧不同表达 | 保守方向＝**改 null＋`chi2_red_defined`**；影响范围＝产品 `info.chi2_red` 与自适应细化判据（:1451 直接用 `>0.0`）⇒ 见 CR-23-14 | CONFIRMED |
| sky_plane.cpp:1224-1241 | `model_hash = sha256(coeff‖deltas‖网格‖gauge)`；`blob.reserve(n_full*8 + 4096)` | 4096/8 | 面系数哈希（provenance） | 结构性不适用 | 4096=容量余量、8=sizeof(double)；哈希覆盖系数不含 cfg/权重 ⇒ 同一系数不同权重不可分（记录在 :1719-1734 的 cfg 段里） | — | CONFIRMED |
| sky_plane.cpp:1242 | `last_weights = w`（稳健后权重） | — | 诊断缓存（声明见 :206） | 结构性不适用 | 诊断字段，无对外读数 | — | CONFIRMED |
| sky_plane.cpp:1263-1267 | `enabled=1`、`max_attempts=6`、`max_node_refinements=4`、`max_node_coarsenings=4`、`residual_improve_ratio=0.5` | 见左 | 自适应回路默认 | 待确认 | 头文件 :320-328 与注释「**没有任何标定常数**：搜索区间由输入几何给，判据是相对量」——区间确由几何给，但 **0.5 是"多少改善才算仍受限"的相对倍数**，权威面只写「至少降到 `residual_improve_ratio` 倍」（`PHASE2_UPM.md:191`）未给数值；`max_attempts/refinements/coarsenings` = 迭代上界（结构性） | 保守方向＝**ratio 取更小（0.3）⇒ 更难继续细化 ⇒ 采纳更粗网格、表示能力更低**；影响范围＝最终 `node_spacing_deg`⇒B_ref 可表示尺度 `2h`⇒接缝（`11_upm.md:39` 门 `1e-2`）；标定证据位 `run/UPM-NODE-ADAPT-01`（非跟踪面） | CONFIRMED |
| sky_plane.cpp:1291-1296 | `<0→6`、`>12→12`、`<0→4`、`!(0<r<1)→0.5` | 见左 | 非法配置静默回默认 | 待确认 | 同族 `DISP-P2SMP-001`；合同要求非法即具名拒绝（`DATA_SEMANTICS.md:3320` 精神） | 保守方向＝**具名 rc**；影响范围＝细化/放粗预算与判据倍数被静默替换 | CONFIRMED |
| sky_plane.cpp:1292/:1337/:1360/:1793/:1932/:1957 | `P2_SKY_ADAPT_MAX_ATTEMPTS = 12`（`sky_plane.h:289`） | 12 | 尝试数组容量/上界 | 结构性不适用 | 数组维度与循环上界（实现选择） | — | CONFIRMED |
| sky_plane.cpp:1317-1322 | 起点 `h = ns.upper_deg`；显式值在区间内**逐字沿用**；`>upper`/`<lower` 夹紧 | 2.0/0.5 | 搜索起点与夹紧 | 公式导出（起点取规则 1 上界）＋**待确认（夹紧即采用边界值）** | `sky_plane.h:426`「起点默认取上界（在规则内最粗 ⇒ 条件数最好）」；`PHASE2_UPM.md` §7a 规则 1 | 保守方向＝**越界显式值应判红而非夹紧**（夹紧=替调用方造一个他未要求的间距）；影响范围＝`node_spacing_deg` 生效值，并与来源位/`clamped_to_upper` 记账错乱（:1319/:1320，见 CR-23-10） | CONFIRMED |
| sky_plane.cpp:1408-1419 | `can_coarsen`；`h = min(upper, h*2.0)` | 2.0 | 放粗步长 | 待确认 | `sky_plane.h:251`「判红 ⇒ 放粗 h（区间内）」给了方向，未给倍率；`PHASE2_UPM.md:191` 只规定细化侧 `h/2` | 保守方向＝**倍率取 √2 之类更细步**（×2 可能一步跳过可行档⇒偏红退出）；影响范围＝自适应是否真能找到可判绿网格 | CONFIRMED |
| sky_plane.cpp:1436 | `h_next = max(lower_deg, 0.5*h)` | 0.5 | 细化步长 | 公式导出 | `PHASE2_UPM.md:191` 逐字「细化到 `h/2`」；代码真按式 | — | CONFIRMED |
| sky_plane.cpp:1451-1452 | `improved = rc2==OK && info.chi2_red>0.0 && i2.chi2_red <= ratio*info.chi2_red` | — | 表示收敛判据 | 公式导出（相对量，代码真按式）＋**受 :1208 的 0 口径污染** | `sky_plane.h:324-327`、`PHASE2_UPM.md:191` | 见 CR-23-14（`chi2_red` 无定义=0 ⇒ 永不 improved） | CONFIRMED |
| sky_plane.cpp:1464 | `h <= ns.lower_deg ⇒ rep_limited=1` | — | 表示能力到顶的诚实登记 | 结构性不适用 | `sky_plane.h:266/:298`、`DATA_SEMANTICS.md:2860` | — | CONFIRMED |
| sky_plane.cpp:1491-1496 | `node_spacing_source = 0`（无条件） | 0 | 来源记账位（自适应路径） | 待确认 | `sky_plane.h:378-379`、`DATA_SEMANTICS.md:2859`、台账断言 `eng/ci/ledgers/design_clauses.json:349` | 见 CR-23-10 | CONFIRMED |
| sky_plane.cpp:1499-1501 | `*out_model = best`；`err[0]='\0'` | — | 成功返回 | 结构性不适用 | — | — | CONFIRMED |
| sky_plane.cpp:1514 | `n = llround(sqrt(n2))`、`n*n != n2` ⇒ 1 | — | 由保留矩阵反推阶数 | 结构性不适用 | 数值一致性检查 | — | CONFIRMED |
| sky_plane.cpp:1522-1533 | `!out_h_red \|\| !out_penalty ⇒ 1`、`ld < n ⇒ 1` | 1 | 诊断导出参数门 | 待确认 | `sky_plane.h:449` 承诺「**2** = 该模型来自 open()」，实现只返回 1（空矩阵）⇒ 返回码与声明不符（见 CR-23-17） | 保守方向＝**按声明区分 1/2**；影响范围＝诊断调用方的分支判定 | CONFIRMED |
| sky_plane.cpp:1552-1554 | `mg = max_extrapolation_deg`；`eps = 1e-9`（度） | 1e-9 | 越域判定的浮点舍入容差 | 待确认 | 注释自称「不构成外插」；条文面零提及（`sky_plane.h:459`「越域→OUT_OF_DOMAIN，不外插」未给容差） | 保守方向＝**eps 取 0（严格）**⇒边缘像素被判越域而不扣 δ（`module_adapters.cpp:10548` 只在 OK 时扣）；影响范围＝域边界像素是否施加校正 | CONFIRMED |
| sky_plane.cpp:1559-1567 | `int jx[8], jy[8]; double nxv[8], nyv[8]`；`idx>=0 && idx<nx*ny` 才累加 | 8 | 求值端基缓冲与越界跳过 | 结构性不适用（缓冲尺寸）＋**待确认（越界静默跳过而非报错）** | `:484` 保证 build 侧 degree∈{1,3}；`open()` 侧不校验（见 CR-23-15） | 保守方向＝**越界索引即 P2_SKY_EVAL_INVALID**；影响范围＝求值完整性 | CONFIRMED |
| sky_plane.cpp:1592/:1649 | `out_values[i] = (st==OK) ? val : 0.0` | 0.0 | 越域/未知帧落 **0** | 待确认 | `sky_plane.h:484` 明文「out_values 该点=0」；但 **0 是合法校正量（=不校正）**，与"未测/越域"不可分；`11_upm.md:39/:92` 要求施加与警告面机器可检 | 保守方向＝**落非有限或配对状态必用**；影响范围＝生产调用点 `module_adapters.cpp:10547-10552` **确实按 `dstat` 分支**（不扣未 OK 点）⇒ 现网不错值，但**跳过像素数无任何计数/provenance**（见 CR-23-16） | CONFIRMED |
| sky_plane.cpp:1594/:1651 | `if (st != OK) rc = 2`（聚合返回码） | 2 | 块求值的部分失败信号 | 结构性不适用 | `sky_plane.h:468/:484`；生产调用点**未接住 rc**（`module_adapters.cpp:10544` 返回值丢弃）⇒ 计入 CR-23-16 | — | CONFIRMED |

### 段 C：`sky_plane.cpp:1601-2030`

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| sky_plane.cpp:1620 | `eps = 1e-9`（度，同 :1553） | 1e-9 | δ 求值越域容差 | 待确认（同 :1552-1554） | 同 :1552 | 同 :1552 | CONFIRMED |
| sky_plane.cpp:1633 | `*out_value = m->gauge_shift + dv` | — | B 口径 δ_k（施加量） | 公式导出 | `sky_plane.h:471-476`「δ_k = gauge_shift + 多项式项」；`11_upm.md:32` `calibrated = raw − δ_k` | — | CONFIRMED |
| sky_plane.cpp:1649 | 非 OK ⇒ `0.0` | 0.0 | 同上"未测用合法 0" | 待确认 | 同 :1592 | 同 :1592 | CONFIRMED |
| sky_plane.cpp:1664-1667 | `*out_n = dk.size()`；`k = min(cap, dk.size())` | — | 系数容量查询/截断 | 结构性不适用 | `sky_plane.h:491`「可空查询数量」 | — | CONFIRMED |
| sky_plane.cpp:1683-1689 | 独立复算按 `m->cfg.weight_mode` 选权重式（0⇒`1/s.variance`、1⇒`snr²`） | — | Oracle 复算与 build 同口径 | 待确认 | 同 :676-:677（`10_sampling.md:47` 只允许 control_ivar 一面）；⚠ 复算用的 weight 口径**来自产品文件里的 cfg**（:1860 回读）⇒ 口径切换可经产品传播 | 保守方向＝**复算面固定单一口径**；影响范围＝Oracle 对拍与 rms 读数 | CONFIRMED |
| sky_plane.cpp:1686-1693 | 掩膜位集合与 build 的 `bad`（:533）**同为 4 位不含 NO_LOCAL_SNR** ✓；越域 `continue` | — | 复算样本选择 | 结构性不适用 | 与 build 一致（:1164-1165 注释自证） | — | CONFIRMED |
| sky_plane.cpp:1697-1698 | `sw>0 ? sqrt(swr2/sw) : 0.0`；`used>0 ? … : 0.0` | 0.0 | 无样本时 RMS=0 | 待确认 | 同 :1199（0 = 完美拟合的合法值；`11_upm.md:112-113` 要求不可定义即 null＋具名不可得） | 保守方向＝**null/rc**；影响范围＝对拍判据可被"空集合"骗绿 | CONFIRMED |
| sky_plane.cpp:1708/:1836 | `format = "astrocs-sky-plane-v1"`（读时全等校验，否则 IO_ERROR） | v1 | 产物格式标识 | 结构性不适用 | 版本串；⚠ 无 schema 面（检索式 `git grep -n "astrocs-sky-plane" -- eng docs` ⇒ 仅 `eng/tests/unit/p2001_real_nodes_test.cpp:708/:712` 的 §7a 键存在性测试） | — | CONFIRMED |
| sky_plane.cpp:1725 | **`j["cfg"]["weight_mode"]` 写入产品文件** | — | 把口径位持久化进中间产物 | 待确认 | **合同禁用面**：`DATA_SEMANTICS.md:3319-3320`（legacy `weight_mode` 整数与键出现即 REJECT）、`:1198`（"键不存在"）；`eng/ci/check_no_weight_mode_code.py:50-75` 的判据形态不含产品 JSON 键面 | 保守方向＝**删键**；影响范围＝`astrocs-sky-plane-v1` 文件字节＋回读（:1860）⇒ 见 CR-23-01 | CONFIRMED |
| sky_plane.cpp:1740-1743/:1749-1750/:1760-1761/:1784-1785/:1798-1799 | `num_or_null`：非有限 ⇒ JSON `null` | null | 发散量不写伪值 ✓ | 结构性不适用 | `11_upm.md:111`「κ 在秩亏块上发散 ⇒ 产品写 null（JSON 不能表示非有限值）」——**本文件在这一点上与正本一致**（正面事实，供对照 :1208 的 0） | — | CONFIRMED |
| sky_plane.cpp:1753 | `{"chi2_red", m->info.chi2_red}`（**不经 num_or_null**） | 0.0 | 无定义 χ² 以 0 落盘 | 待确认 | 与 `11_upm.md:112` 的 null 口径直接冲突 ⇒ CR-23-14 | — | CONFIRMED |
| sky_plane.cpp:1754 | `rms_weighted/rms_unweighted/iterations/n_masked/n_rejected` 落盘 | — | 拟合质量与剔除读数 | 待确认 | `11_upm.md:88` 的三元组 `rms_z`、`Σw/Σraw_w` **无对应键**（见 :1150-1169 行） | 保守方向＝**补两键**；影响范围＝稳健剔除幅度不可核 | CONFIRMED |
| sky_plane.cpp:1765-1767 | `node_spacing_source/upper_deg/lower_deg` 落盘 | — | 间距来源与搜索区间 | 待确认 | `DATA_SEMANTICS.md:2859`（生效值与导出依据）⇒ 与 CR-23-10 的来源位错记联动 | — | CONFIRMED |
| sky_plane.cpp:1777 | `ja["clamped_to_upper"] = a.clamped_to_upper` | — | 夹紧登记（键名只说上界） | 待确认 | `sky_plane.h:299`「显式初值被规则 1 **上界**夹紧过」；实现 :1320 在**夹紧到下界**时同样置 1 ⇒ 键名/语义不符（`DATA_SEMANTICS.md:2860` 登记该键） | 保守方向＝**补 `clamped_to_lower`**；影响范围＝自适应 provenance 判读 | CONFIRMED |
| sky_plane.cpp:1852 | `m->cfg = p2_sky_plane_default_config()` 后逐键覆盖 | 默认含 `max_nodes=2048` | 回读基线 = 默认配置 | 结构性不适用（:1855-1864 覆盖有效字段；`retain_normal_matrices/min_samples` 等在回读模型里无用途） | — | — | CONFIRMED |
| sky_plane.cpp:1855-1864 | 缺键即取默认（旧产品兼容）；`roughness_penalty`/`kappa_max` 旧键**忽略**（注释 :1861-1862） | 默认值 | 已退休口径不得复活 ✓ | 结构性不适用 | `sky_plane.h:395`（退休名）、`PHASE2_UPM.md` §7a 规则 4（绝对常数退休） | — | CONFIRMED |
| sky_plane.cpp:1868-1871 | `geometry.*` 缺键 ⇒ `0.0` | 0.0 | 几何量缺失 ⇒ 后续 `derive_node_spacing` fail-closed | 结构性不适用 | 与 :407-415 门一致 | — | CONFIRMED |
| sky_plane.cpp:1878 | `m->ref_frame = 0`（最小 frame_id） | 0 | 回读模型的参考帧 | 结构性不适用 | `sky_plane.h:26-27` | — | CONFIRMED |
| sky_plane.cpp:1888 | `info.kappa = i.value("kappa", 0.0)` | **0.0** | κ 缺失/不可算 ⇒ **0**（κ≥1 恒成立，0 是不可达"完美"值） | 待确认 | `11_upm.md:111-113` 要求发散/无定义写 null、缺失具名登记；本文件同段 :1890-1896 对 `kappa_data/kappa_solve` 用 NaN、:1748 写 `kappa` 原值（**save 端未过 num_or_null**）⇒ 同一"未测"三表达（null／NaN／0） | 保守方向＝**与 kappa_data 同用 null/NaN**；影响范围＝判据读数 κ 的下游判读（`DATA_SEMANTICS.md:2868` 必落 `kappa`）⇒ 见 CR-23-17 | CONFIRMED |
| sky_plane.cpp:1897 | `identifiable = i.value(..., 0)` | 0 | 判决位缺失 ⇒ 0（红）✓ | 结构性不适用 | fail-closed 方向正确（与 :1888 相反，正说明 :1888 应改） | — | CONFIRMED |
| sky_plane.cpp:1899-1903 | `lambda_numerical/lambda_max/lambda_min/dof_eff` 缺失 ⇒ `0.0` | 0.0 | 谱量缺失落 0（`lambda_min=0` 在正本里恰是"秩亏"的表达，见 `sky_plane.h:373`「λ_n；秩亏 ⇒ 0」⇒ 但**同时**是"缺失"的默认 ⇒ 两义合一） | 待确认 | `PHASE2_UPM.md:217-218`（读数必须可观测）；`sky_plane.h:373` | 保守方向＝**区分 null（不可算）与 0（秩亏）**；影响范围＝谱读数判读与审计（`11_upm.md:113`「"字段缺失"是另一个状态」）⇒ 计入 CR-23-17 | CONFIRMED |
| sky_plane.cpp:1908-1911 | `rms_weighted/rms_unweighted/chi2_red/iterations` 缺失 ⇒ 0.0 | 0.0 | 拟合质量缺失 ⇒ "完美"值 | 待确认 | 同 :1888 | 计入 CR-23-17 | CONFIRMED |
| sky_plane.cpp:1914-1918 | `info.gauge_mode/weight_mode` 从 cfg 重建；`frame_gradient_order=s.delta_order` | — | 回读模型的口径位 | 待确认 | `info.rank_full`、`info.n_params_full` **既未落盘（:1744-1767）也未回读** ⇒ 打开的产品上 `p2_sky_plane_info` 报 `rank_full=0`/`n_params_full=0`，而 build 时它们是 χ² 分母的来源（:1206）| 保守方向＝**落盘并回读两键**；影响范围＝任何用打开模型复算 dof/χ² 的审计路径 ⇒ 计入 CR-23-17 | CONFIRMED |
| sky_plane.cpp:1930-1932 | `n_attempts` 负⇒0、>12⇒12 夹紧 | — | 回读容量防护 | 结构性不适用 | 数组维度 | — | CONFIRMED |
| sky_plane.cpp:1937 | `clamped_to_upper` 回读 | — | 同 :1777 | 待确认 | 同 :1777 | — | CONFIRMED |
| sky_plane.cpp:1984-1990 | `coeff.size()==nx*ny`、`deltas.size()==frame_ids.size()`、每帧 `dk.size()==m` ⇒ 否则 IO_ERROR | — | 结构自洽校验 ✓ | 结构性不适用 | 尺寸一致性（**唯一**的 open 校验；`h>0`、`degree∈{1,3}`、`delta_order∈[0,2]`、`nx,ny≥1` **均无校验** ⇒ CR-23-15） | — | CONFIRMED |
| sky_plane.cpp:2003-2004 | `frame == ref_frame ⇒ *out_g = 1.0` | 1.0 | 同帧增益恒等 | 结构性不适用 | 恒等式定义 | — | CONFIRMED |
| sky_plane.cpp:2007-2021 | `refv/framv` 按 `control_id` 取 `value` 求和 | — | 直流比 = Σvalue_frame / Σvalue_ref | **待确认** | 头文件 :499-502 自称「直流不受帧间加性结构影响，是独立于 UPM MA 结构定标的稳健增益量」；**权威面零规定**（检索式 `git grep -n "P0-10\|gain_dc\|直流比" -- docs ASTROCS_DESIGN.md`）；且该比值把**天光电平比**当增益比，与 `ASTROCS_DESIGN.md:186`（天光不得计入信号）、`CONTROL_WEIGHT_SNR.md:101`（分子只有源）方向相反 | 保守方向＝**仅诊断、不得进任何定标判据**；影响范围＝调用点只有 `lib/algorithms/coverage/tools/stage2.cpp:649`（台账登记 `unreachable:header.P2_API:p2_sky_estimate_gain_dc`，`eng/ci/ledgers/prod_wiring.json:322`）⇒ 计入 CR-23-18（低危） | PARTIAL |
| sky_plane.cpp:2022-2026 | `common==0`⇒2；`g<=0`⇒3 | 0 | 无公共 control / 非正增益即返回码 ✓ | 结构性不适用 | 头文件 :502「无公共 control 或分母为 0 → 返回非 0（调用方 fail-closed）」 | — | CONFIRMED |

> 段 B＋段 C 计数：段 B **59 行**、段 C **29 行**；全文件三表合计 **201 行**（`113 59 29`，命令见段 A 计数行）。合并族（静默钳位、单位换算、循环上界、JSON `value(...,0)` 默认族）均在行内列出全部位点。

## Findings（§3）

> 编号说明：19 条按"读取段"顺序编号（段 A＝01-10、段 B＝11-16、段 C＝17-19），但 **CR-23-16/CR-23-17/CR-23-18 是在段 A 成稿后回补的**，故物理顺序为 01…15、19、16、17、18。判读以编号为准、不以出现顺序为准。



### CR-23-01 · 合同宣布"不存在"的 `weight_mode` 键在生产配置读取路径被静默接受，并据以切换科学权重口径
- 轴：C（主轴）／D
- 位点：`lib/algorithms/coverage/src/sky_plane.cpp:460`（`c.weight_mode = 0;`）、`:524`（`if (cfg.weight_mode != 0 && cfg.weight_mode != 1) cfg.weight_mode = 0;`）、`:537`-`:541`（按 mode 选定义域门）、`:676`-`:677`（按 mode 选权重式）、`:1053`（按 mode 选 σ_eff）、`:1211`（`info.weight_mode = cfg.weight_mode;`）；生产入口 `lib/infrastructure/scheduler/src/module_adapters.cpp:9973` `spc.weight_mode = sp_cfg.value("weight_mode", 0);`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:1198`「~~weight_mode~~（键不存在）……**禁用键**：不存在「权重模式」概念（§9.73 A44；ASTROCS_DESIGN.md §2.1）；逆方差权重由阶段二按该天球像素对应的帧集合**现场算出**（派生量，非配置项）；配置中出现该键 ⇒ 具名 fail-closed 拒绝（§31.8）」；`:3320`「键 `weight_mode`……出现于配置（任何取值/形态）→ REJECT（具名 fail-closed）`FZ-FIELD-WEIGHTMODE`；`FZ-WEIGHT-SINGLE-PATH`」；`docs/contracts/PUBLIC_API.md:1213`「**已删字段**：不存在「权重模式」」；`docs/ci/01_CHECKS.md:100` `CHK-NO-WEIGHT-MODE-CODE` 断言「`lib/**` + `eng/tools/**` 无模式选择机制（枚举／解析路由／口径门……）」
- 现状：`sky_plane.h:216` 仍把 `weight_mode` 作为公共 C ABI 配置字段并带枚举注释（`0=inverse_variance；1=snr2；默认 0`）；`module_adapters.cpp:9973` 从产品配置块 `sp_cfg` **逐字取值**（键存在即用、不存在取 0），无拒绝、无告警、无 provenance 登记；`sky_plane.cpp:524` 把其它取值（含 NaN 经 `!=0 && !=1` 判定）**静默改写为 0**；`:1211` 把该口径位写进 `P2SkyPlaneInfo`
- 差在哪：合同要求"出现即具名拒绝"的禁用键，在**唯一的生产读取点**被当作合法配置读入，并驱动科学量定义（权重公式）切换；同时机器门宣称代码面无模式选择机制，说明门判不到这一面
- 后果：任何携带 `sky_plane.weight_mode` 的 mosaic 产品配置**本应被拒**，实际会产出一张按另一口径加权的天光面与逐帧 δ_k，`calibrated = raw − δ_k`（`11_upm.md:32`）直接进入标准化/叠加产品；同时 `G-LEGACY-MIGRATION`/`FZ-FIELD-WEIGHTMODE` 对外主张（"该键不存在、出现即拒绝"）不成立
- 定级：**S1**。触发路径：CLI `mosaic` → 阶段二 P2b 构建节点 → `module_adapters.cpp:9973`（读 `sp_cfg["weight_mode"]`）→ `sky_plane.cpp:483`（`cfg = *cfg_in`）→ `:524` → `:676`/`:677` → `:1101` 判决 → `:1246` 模型 → `module_adapters.cpp:10083` `p2_sky_plane_save` 落盘
- 怎么算修好：删 `sky_plane.h:216` 字段与 `sky_plane.cpp:460/:524/:537-541/:676-677/:1053/:1169/:1211` 的分支（单一口径 `1/control_variance`）；`module_adapters.cpp:9973` 改为"键出现即具名 fail-closed 拒绝"（与 `stage2_common.cpp`/`module_adapters.cpp` 的 `integration.weight_mode` 拒绝面同形，见 `v6_clause_registry_v1.json:100`）；同批必须改 `eng/ci/check_no_weight_mode_code.py` 的 `MODE_SELECTION_PATTERNS`（:50）与 `SCAN_DIRS`（:45）使 `sky_plane.h/.cpp` 落入判据（否则改对反而门仍绿、无人发现回归），并把 `sky_plane.h` 的文件头权重段（:23-24）与 `11_upm.md:72` 的 `∝ SNR²` 写法做**适用域澄清**（防"删了 mode 却留两套口径描述"）
- 置信：CONFIRMED（生产读取点、分支、合同条文均逐字取证；未跑构建）

### CR-23-02 · `weight_mode=1` 的 `snr²` 权重被专项条款明文否定，且其 SNR 分子是天光本身
- 轴：D（主轴）／S
- 位点：`sky_plane.cpp:677` `else base_w[t] = s.snr * s.snr;`
- 上位依据：`docs/plugins/algorithms_phase2/10_sampling.md:47`（逐字）「**控制点权重一律取 `control_ivar`，SNR 只作 veto/质量门**……`w = 1/σ² = SNR²/F_ref² ∝ SNR²` 的 `∝` 以固定参考通量 `F_ref` 为前提，而天光控制点的**被估量本身在变** ⇒ `SNR²` **不是**有效逆方差代理」（该文档正是 `sky_plane.h:7` 点名的权威）；`ASTROCS_DESIGN.md:186`「传统信噪比把天光信号计入信号，被天光抬高而失真」；`docs/science/CONTROL_WEIGHT_SNR.md:101`「`SNR` 的分子只有源……天光**只**经其散粒噪声进入分母」
- 现状：分子取自 `module_adapters.cpp:9863` `sk.snr = (o.uncertainty > 0.0) ? std::fabs(o.value) / o.uncertainty : 0.0;`，其中 `o.value` 是**局部稳健背景电平**（`sky_plane.h:70`「局部稳健背景估计（可负）」）；`:1053` 的 σ_eff 亦按 `1/snr` 反算
- 差在哪：把"天光电平/背景不确定度"当 SNR 平方后作权重 ⇒ 背景越亮权重越大的方向与 `CONTROL_WEIGHT_SNR.md:101`「天光增大 ⇒ SNR 单调下降」相反；而该量在条款里只允许作 veto/质量门
- 后果：亮背景区被**过权**、暗区被欠权 ⇒ B_ref 被拉向高天光电平、δ_k 帧间差被低估 ⇒ 接缝判据 `max|rel_step| ≤ 1e-2`（`11_upm.md:39`）在错误的面上度量；对外主张"绝对天光参考平面"（`ASTROCS_DESIGN.md:196`）失真
- 定级：S1（触发路径同 CR-23-01，入口需 `sp_cfg` 给 `weight_mode: 1`；默认 0 ⇒ 现产品不带该键时不触发，但键本身已属禁用面）
- 怎么算修好：随 CR-23-01 一并删分支；若要保留 SNR 作**门**（veto），另在采样侧（`sampler.cpp`/`snr_idx`）实现，不在权重式里出现
- 置信：CONFIRMED

### CR-23-03 · MAD=0 的 patch 被写成 1e-12 合法尺度 ⇒ 方差 ~1e-24、逆方差 ~1e23，静默成为全平面最大权重
- 轴：R（主轴）／S
- 位点：`sky_plane.cpp:296` `const double sigma = (s0 > 0.0) ? s0 : 1e-12;` 与 `:306` `out->variance = cfg.k_corr * kPiHalf * sigma * sigma / n_ret;`、`:307`
- 上位依据：条文面零提及（检索式：`git grep -n "sigma_floor\|1e-12" -- docs/science/PHASE2_UPM.md docs/plugins/algorithms_phase2 docs/algorithms/PHASE2_SAMPLER.md`；对照 `UPM_SOLVER.md:113` 的 `sigma_eff=max(|uncertainty|, sigma_floor)` 要求 floor 具名、`DATA_ARTIFACTS.md:26` 要求 `ivar≤0→rc=2`）
- 现状：`s0` 为 `kMadToSigma×MAD`；全常数 patch（如整片同值/被静默填零的 tile）MAD=0 ⇒ `sigma_mad` 与 `variance` 均写非零合法值，`status` 仍为 `P2_SKY_PATCH_OK`，`ivar = 1/(1.4·(π/2)·1e-24/n_ret) ≈ 4×10^23·n_ret`
- 差在哪：「未测/退化」被用合法数值表达（0 与 1e-12 都落在可计算域），既不判红也不打 flag；下游 `1/variance`（:676）把它放大成压倒性权重
- 后果：单个零离散度 patch 即可把 B_ref 钉在该 patch 的值上（判据矩阵被单点主导、κ 与残差失真）；`bright_fraction`/`min_retained` 两门都拦不住它
- 定级：S2 —— 缺生产触发路径：本仓接线台账 `eng/ci/ledgers/prod_wiring.json:344`＋`eng/ci/prod_wiring_baseline.json:119` 已把 `p2_sky_patch_estimate` 登记为 **unreachable**，生产权重来自 `sampler.cpp` 的另一实现（`module_adapters.cpp:9862` 取 `o.control_variance`）
- 怎么算修好：`s0<=0` 返回具名状态（新增 `P2_SKY_PATCH_DEGENERATE` 或复用 `INSUFFICIENT_SAMPLES`）并令 `variance/ivar` 为非可用（`ivar=0` 且 status 非 OK），删除 1e-12 地板；同批把 `sampler.cpp` 的同类地板一并核（两条实现同一数学定义，见 `sky_plane.h:90-91`）
- 置信：CONFIRMED（数值由 :296/:306 直接算得：1.4·1.5708·1e-24 ≈ 2.2e-24）

### CR-23-04 · 同一"高污染"阈两处入口：sampler 侧是配置键、此处是硬字面量且不入配置
- 轴：C
- 位点：`sky_plane.cpp:316` `if (out->bright_fraction > 0.20) {`
- 上位依据：`docs/development/CONFIG_SCHEMA.md:28`「`background_max_contamination(0.20)`」、`docs/algorithms/PHASE2_SAMPLER.md:122` 同值且为**可配置字段**；`P2SkyPatchConfig`（`sky_plane.h:92-99`）六个字段里**没有** contamination 上限位
- 现状：值 0.20 写死；调用方无法通过 `p2_sky_patch_estimate` 的 cfg 覆盖；`contamination_sigma`（:231）可调而阈值不可调
- 差在哪：`sky_plane.h:90-91` 自称与 sampler 的 patch estimator「同一数学定义」，但同一门的**可调面**在两份实现里不一致 ⇒ 同一输入在两处可给出不同判定
- 后果：`HIGH_CONTAMINATION` 判定与 `P2_SKY_FLAG_HIGH_CONTAMINATION`（:533 剔除位）在两实现间不一致 ⇒ 采样点集合与"星点/高结构必须排除"（`CONTROL_WEIGHT_SNR.md:106`）的可核对性下降
- 定级：S2（本函数台账 unreachable；但同族默认已在 :228-233 形成第二份登记面）
- 怎么算修好：把 0.20 提为 `P2SkyPatchConfig.max_contamination` 并与 sampler 侧共用同一默认来源（`eng/packaging/config/defaults.json`），或删本实现只留 sampler 单面；同批更新 `sky_plane.h:92-99` 字段表与 `10_sampling.md:47`
- 置信：CONFIRMED

### CR-23-05 · 科学文档点名的"生产"采样/掩膜函数被本仓接线台账登记为不可达（真载体另有其人）
- 轴：C（主轴）／D
- 位点：`sky_plane.cpp:237`（`p2_sky_patch_estimate`）、`:326`（`p2_star_mask_caps`）、`:349`（`p2_star_mask_contains`）
- 上位依据：`docs/science/CONTROL_WEIGHT_SNR.md:106`「天光采样点**必须**落在源掩膜之外（生产 `p2_star_mask_caps` / `p2_star_mask_contains`、`p2_sky_patch_estimate`、`p2_reject_plan_resolve` / `p2_reject_stack`）」
- 现状：`eng/ci/ledgers/prod_wiring.json:344/:454` 与 `eng/ci/prod_wiring_baseline.json:119/:129` 以 `unreachable:header.P2_API:p2_sky_patch_estimate`、`unreachable:header.P2_API:p2_star_mask_caps` 登记；`git grep -n "p2_sky_patch_estimate\|p2_star_mask_caps"` 的全部非头文件命中只在 `eng/tests/**`（`v6_p2_sky_test.cpp:108-136`）与上述台账/文档；生产采样实现在 `sampler.cpp`（`execution_inventory.csv:9` 记 `p2_sample_controls_impl`、`snr_idx.query`、catalog veto）
- 差在哪：条款把科学不变量的**执行主体**指向三个在生产不可达的函数 ⇒ 该"必须"由别的代码履行，而文档给出的复核锚（读者会去这三个函数找排除逻辑）落空；这三份实现还各自带一套默认值（:228-233）与另一套阈（:316），成为第二登记面
- 后果：`SCI-C`「星点掩膜之外取稀疏背景采样点」（`ASTROCS_DESIGN.md:196`）的证据链断裂（复核指向不执行的代码）；死实现漂移无人发现
- 定级：S2（写不出"由这三函数导致的产物错值"路径，因为它们在台账上不可达）
- 怎么算修好：二选一——① 把 `CONTROL_WEIGHT_SNR.md:106` 与 `10_sampling.md:4.1/4.2` 的生产锚改为 `sampler.cpp` 的实际实现（`sampler.cpp:812-875` 一类逐字行锚）；② 真接进生产并同步台账；另把不可达的重复实现（patch 估计 + 星掩膜）删除或标注只作 Oracle 参考，防两套默认漂移
- 置信：CONFIRMED（跟踪面＋台账为准；未跑不可达性脚本）

### CR-23-06 · 收敛门与权威规定的**双条件无量纲**形式不符，且无 `converged` 状态枚举
- 轴：D（主轴）／R
- 位点：`sky_plane.cpp:1059` `if (iter > 0 && db <= cfg.tolerance * std::max(1.0, sb)) break;`；`sky_plane.cpp:457`（`max_iterations = 30`）、`:1209`（`info.iterations = iterations;`）
- 上位依据：`docs/plugins/algorithms_phase2/11_upm.md:86`「**停止判据必须无量纲**，分母用**观测量的尺度**（`max|M|` 属另一口径）：`max_dM / max(scale_obs, eps) < tol_step` **且** `|obj_new − obj_old| / max(|obj_old|, eps) < tol_obj`」；`:87`「`converged` 为状态枚举：`0 = max_iter` / `1 = converged` / `2 = stalled` / `3 = invalid`」；`:130` `max_iter` 默认栏为空
- 现状：只用系数最大变化 `db`，分母是 `max(1.0, sb)`（`sb` = 系数最大绝对值），**没有**目标函数相对改善条件；`P2SkyPlaneInfo` 无 `converged` 字段，只落 `iterations`；上限 30 无权威值（同阶段另一求解器 `phase2_upm.md:169` 取 100）
- 差在哪：分母写 `1.0` 使门在系数尺度 ≪1（ADU/sr 天光电平常见 1e-3~1e0）时退化为**绝对阈 1e-10**，正是 §4.6 点名的"另一口径"；同时 `iterations == max_iterations` 与"真的没收敛"在产品里不可分
- 后果：`chi2_red`/`rms_weighted`/`kappa` 都取自最后一次迭代的面（:1199-1208）；未收敛时产品仍返回 `P2_SKY_PLANE_OK` ⇒ 下游 δ_k 施加（`11_upm.md:32`）与接缝判据基于一个未收敛面，且无人能只读产品分辨之
- 定级：S2（缺"必然误判"的运行时触发证据：需实跑生产权重尺度看 db 停在何值；数值方向已定——`sb<1` 时分母 1.0 ⇒ 门比规定**更松**（要 db≤1e-10 才停，实际上更难停 ⇒ 偏**多迭代**⇒ 偏绿方向是"迭代到 30 轮仍未收敛也照出"））
- 怎么算修好：按 §4.6 补第二条件（目标函数相对改善）并把 `scale_obs` 换为观测量尺度（如 `Σw r²` 或 `max|y|` 的稳健尺度）；`P2SkyPlaneInfo` 补 `converged` 枚举并落盘；`max_iterations` 默认值补登记进 `11_upm.md:130`（或由 `PHASE2_UPM.md` 给推导）；同批把 `11_upm.md:92` 的"不判红但必须落机器可检警告"形态套到天光面产物上（现无任何 warning 键）
- 置信：PARTIAL（缺"生产尺度上是否真的未收敛"的实测；本文件无该读数落盘）

### CR-23-07 · patch 估计的配置修补是 NaN/非法的 fail-open 面（含"关掉保留比例门"）
- 轴：R
- 位点：`sky_plane.cpp:253` `if (!(cfg.min_retained_fraction > 0.0)) cfg.min_retained_fraction = 0.0;`；同族 `:249-254`、`:484`、`:514-524`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:542` 对同类静默钳位已登记 `DISP-NOISE-007`（"参数下限静默钳位无返回码区分（调用方不可知被钳位）"）、`PHASE2_SAMPLER.md:556` 登记 `DISP-P2SMP-001`（"`<=0→默认` 吞显式 0（意图'禁用'的 0 被静默改写）"）；合同 §31.8/`:3320` 要求非法/禁用面**具名拒绝**
- 现状：`min_retained_fraction = NaN` 或负值 ⇒ 0.0 ⇒ `:309` 的保留比例门恒不触发；`clip_sigma = NaN` ⇒ 回落 3.0（调用方以为设了别的值）；`spline_degree` 传 2 ⇒ 静默按 1（而默认是 3）；`rank_rtol` 传 NaN ⇒ 静默 1e-10 ⇒ **判决阈被改写**
- 差在哪：同一份配置里"想关闭门"和"传了非法值"得到同一种静默处置，且被判据/口径类参数无差别吸收；`status`/返回码不回传"被钳位"事实
- 后果：门只可能偏**绿**（保留门关闭、污染面继续用）；`rank_rtol` 被改写则直接改判决松紧；产品与 provenance 均无痕迹
- 定级：S2（`p2_sky_patch_*` 面 unreachable，见 CR-23-05；`:514-524` 面在生产可达但需调用方给 NaN，实测无）
- 怎么算修好：区分"显式 0=关闭"（配置键存在且 =0 ⇒ 合法关闭并登记）与"非有限/越域"（⇒ 具名 `INVALID_ARGS`）；`spline_degree`/`weight_mode`/`gauge_mode` 非法值直接返回 rc=1；同批给 `eng/ci` 加"钳位必落 provenance"检查（防改对反而判红）
- 置信：CONFIRMED（逐行读 `:249-254`/`:484`/`:514-524`）

### CR-23-08 · `k_corr=1.4` 在本文件无适用域检查、无 provenance 承接（sampler 侧的域判据不覆盖这里）
- 轴：C（主轴）／S
- 位点：`sky_plane.cpp:233`（默认 1.4）、`:254`（`!(k_corr>0)→1.4`）、`:306`（直接乘入方差）
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:1739`「**适用域（正向约束）**：Drizzle 核 `pixfrac ∈ [0.5, 1.0]` 且源像素角尺度 `∈ [300, 600]″/px`；域外一律取冻结默认 1.4……实现锚 = `lib/algorithms/coverage/src/sampler.cpp` 的 `kcorr_lookup` / `kControlCorrDefault`」；`:3286`/`:3317`「`k_corr.value = 1` 忽略相关 → REJECT（`FZ-PROV-KCORR`）；`k_corr` 缺适用域/标定脚本/固定种子或跨域外推 → REJECT」；`PHASE2_SAMPLER.md:561`（`DISP-P2SMP-006`）已登记"provenance 键缺失时以 1.4 参与生产且回退是静默的"这一**已知缺口**
- 现状：`P2SkyPatchConfig` 只有一个裸 `k_corr` double（`sky_plane.h:98`），既不接收 `pixfrac` 也不接收角尺度，不返回所用域；方差与 ivar（:306-307）里 1.4 已含但无任何域声明面
- 差在哪：把"只在标定域内成立"的放大因子当成普适常数，且比 sampler 侧更弱（sampler 至少查 provenance 并打"域外回退"标）；`0<k_corr<1` 在此合法（物理不可达域，`UPM_SOLVER.md:52` 要求 rc=1/rc=7）
- 后果：域外产品（不同 pixfrac/角尺度）拿到的 control_variance 偏差无痕迹 ⇒ 与 `FZ-PROV-KCORR`/`G-KCORR-DOMAIN` 的对外主张不符
- 定级：S2（`p2_sky_patch_estimate` 面不可达；`P2_SKY_PATCH_*` 无产品落点）
- 怎么算修好：本文件若保留该实现，则 `P2SkyPatchConfig` 补 `pixfrac`/`source_scale_arcsec`/`k_corr_applicability_domain`，域外即 rc=7（与 `PHASE2_SAMPLER.md:316-317` 同判据）；更彻底的修法是按 CR-23-05 删除重复实现，只留 sampler 单面
- 置信：CONFIRMED

### CR-23-09 · 算法侧 `max_nodes` 默认 2048 与自身头文件声明的 8192 漂移，且调用方以"注释承认漂移"方式绕过
- 轴：C
- 位点：`sky_plane.cpp:467` `c.max_nodes = 2048;`（钳位下限 :522 `max_nodes<4→4`；门在 :647-653 返回 `P2_SKY_PLANE_TOO_MANY_NODES`）
- 上位依据：`lib/algorithms/coverage/include/astro/phase2/sky_plane.h:229` 逐字 `std::int32_t max_nodes; // B_ref 系数上限（内存门）；默认 8192`；`module_adapters.cpp:9995-9999` 逐字「并与 sky_plane.h 声明的『max_nodes 默认 8192』一致（算法侧实现里的 2048 与该声明漂移；编排面按声明取值，不改算法侧）」，随后 `else spc.max_nodes = 8192;`
- 现状：默认配置函数给 2048，声明与编排面给 8192；取值依据（M42 需 4814 档）落在 `run/UPM-NODE-ADAPT-01`、`run/CHAIN-WIRE-ADAPT-01/REPORT.md`（非跟踪面）
- 差在哪：同一"默认值"在两份登记面（头文件注释 vs 实现）不同值，且**由调用方绕过算法侧默认**来解决——`p2_sky_plane_build`/`p2_sky_plane_build_adaptive` 的"不传 cfg 即用默认"语义因此是 2048，不是文档说的 8192
- 后果：任何按文档默认（8192）理解并直接调 `p2_sky_plane_default_config()` 的入口，在科学上更优的 `h`（M42 实测 4814 节点）档上会 `rc=5` fail-closed 一个模型都不给；同时 `11_upm.md` §5/`DATA_SEMANTICS.md:2856` 一侧无该默认登记 ⇒ 无第三方可核对
- 定级：S2（生产唯一入口已显式覆盖为 8192 ⇒ 现网不触发；其余调用方只有 `eng/tests/**`）
- 怎么算修好：把 `sky_plane.cpp:467` 改成 8192 并与声明/模板/`defaults.json` 三侧对齐（改对的同时必须把 `eng/tests/unit/v6_p2_sky/v6_p2_sky_node_adapt_test.cpp:199-272` 里依赖 2048 判红的断言按新默认重定，否则判红）；或反过来把头文件改 2048 并让编排面显式给 8192 + 记由
- 置信：CONFIRMED

### CR-23-10 · 自适应路径把"调用方显式给的节点间距"一律记成"由输入几何导出"
- 轴：C（主轴）／R
- 位点：`sky_plane.cpp:1491` `best_info.node_spacing_source = 0;`（含 :1494-1496 写回模型、:1492-1493 上下界）；对照 :487 `int node_spacing_source = 1;`（1=显式给出）与 :511（导出时才置 0）
- 上位依据：`sky_plane.h:378-379`「节点间距来源：**0 = 由输入几何导出；1 = 调用方显式给出**」；`docs/contracts/DATA_SEMANTICS.md:2859`「`solvers.sky_plane.node_spacing_deg` / `node_spacing_source` / `node_spacing_upper_deg` / `node_spacing_lower_deg` —— 节点间距的**生效值与导出依据**（表示能力的唯一决定量）」；`eng/ci/ledgers/design_clauses.json:349` 的断言正是「`:9585` 对显式给出的 `node_spacing_deg` 走另一分支……即不落默认标定值」（该断言的成立依赖来源位可区分）
- 现状：`p2_sky_plane_build_adaptive` 无条件把最终解的来源写 0（:1489-1496 注释称"自适应在几何给的区间内搜索"），而 :1318-1322 实际允许 `h = cfg.node_spacing_deg`（显式值落在区间内时**逐字沿用**，既非导出也非夹紧）；生产入口 `module_adapters.cpp:9875-9877` 正是"配置显式给出则逐字沿用"
- 差在哪：provenance 键 `node_spacing_source` 在自适应路径上恒为 0 ⇒ 读者与审计脚本无法分辨"间距来自产品几何"还是"来自配置字面量"；台账 `design_clauses.json:349` 的分分支断言在产品读数上不可验证
- 后果：`solvers.sky_plane.node_spacing_source` 对外主张（§7a 禁止标定常数）失去可核性——配置里若塞回 1.0 这类常数并被区间接受，产品仍标"由输入几何导出"；`clamped_to_upper`（:1319/:1320）更在**夹紧到下界 lower_deg** 时也置 1（键名与语义相反），进一步误导
- 定级：S1（触发路径：`mosaic` CLI → `module_adapters.cpp:9875` 读 `node_spacing_deg` → `p2_sky_plane_build_adaptive` → :1491 写 0 → `module_adapters.cpp` manifest/provenance 落盘 → `DATA_SEMANTICS.md:2868` 的必落字段读数错）
- 怎么算修好：区分三态（0=纯导出／1=显式且原样采纳／2=显式但被区间夹紧），`clamped_to_upper` 与 `clamped_to_lower` 分开记；同步 `sky_plane.h:297-299` 字段注释、`DATA_SEMANTICS.md:2859-2860` 取值面，并给 `eng/ci` 增一条"source=0 时 upper/lower 必须非零且 h∈[lower,upper]"的产品级判据（防只改注释）
- 置信：CONFIRMED

### CR-23-11 · 判决阈值 `rank_rtol` 与权重口径 `weight_mode` 可被非有限配置静默替换，且无返回码区分（求解器侧，生产可达）
- 轴：R（主轴）／C
- 位点：`sky_plane.cpp:519` `if (!(cfg.rank_rtol > 0.0) || !std::isfinite(cfg.rank_rtol)) cfg.rank_rtol = 1e-10;`；`:516`（huber_delta）、`:517`（max_iterations）、`:518`（tolerance）、`:523`（gauge_mode）、`:524`（weight_mode）、`:484`（spline_degree∉{1,3}→**1**，而默认是 3）
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:3320`/§31.8 的通用形态「非法/禁用面 ⇒ 具名 fail-closed」；本仓已把同类形态登记为缺陷：`docs/algorithms/NOISE_ESTIMATION.md:202` `DISP-NOISE-007`（「参数下限静默钳位无返回码区分（调用方不可知被钳位）」）、`docs/algorithms/PHASE2_SAMPLER.md:556` `DISP-P2SMP-001`
- 现状：`rank_rtol` 传 NaN/0/负 ⇒ 静默改 1e-10 并照常返回 `P2_SKY_PLANE_OK`；`spline_degree` 传 2 ⇒ 静默按 **1** 求解（与"默认 3"反向）；`weight_mode` 传 7 ⇒ 静默按 0。生产入口 `module_adapters.cpp:9965-9971` 用 `sp_cfg.contains("rank_rtol") ? get<double>()` 逐字取值 ⇒ 配置里的 `null`/字符串数字之外的任何非正数值都会走到上述钳位
- 差在哪：τ 是**唯一判决量**（`identifiable ⟺ κ(H_red) < 1/τ`，`11_upm.md:97-100`），把它静默改写等于把判决松紧改掉却不告知；`FZ-AP2S-RANK-RTOL` 的"同一个值"承诺（`sky_plane.h:218-220`）在产品读数上不可验证
- 后果：`info.rank_rtol_effective`/`identifiable`/`lambda_numerical` 与 rc=7 判红面都可由一个非法配置静默决定；`spline_degree` 的静默降阶改变 B_ref 表示能力（`2h` 可表示尺度）而不落任何痕迹
- 定级：S2（能写出触发路径 `mosaic` CLI → `module_adapters.cpp:9965` → `sky_plane.cpp:519` → `:1099` 判决；但需配置真给非正/NaN 才触发，现网默认不带该键）
- 怎么算修好：`cfg` 校验段改成"非有限或越域 ⇒ 返回 `P2_SKY_PLANE_INVALID_ARGS` 并在 err 里点名键"；`spline_degree`/`weight_mode`/`gauge_mode` 枚举外值同样具名拒绝；同批在 `eng/ci` 侧补一条"判决量不得被钳位"的检查（`check_config_contracts.py` 面），并对 `eng/tests/unit/v6_p2_sky/**` 里依赖钳位行为的断言重定（防改对反而判红）
- 置信：CONFIRMED

### CR-23-12 · 惩罚零空间维数（4，bilinear）对 `11_upm.md` §4.3 的 3 维 gauge 的关系只有代码注释自证，权威面零复述
- 轴：S（主轴）／C
- 位点：`sky_plane.cpp:736-741`（注释「它比 `11_upm.md` §4.3 的 δ_k 一次多项式 gauge（3 维 {1,ξ,η}）**多一维**——δ_k 只到一阶、无法吸收 bilinear 项，故 bilinear 是真实模型方向，由数据决定，不是规范自由度」）、`:746`（`for (int c = 0; c < 4; ++c)`）、`:752-753`（基列 `1, ix, iy, ix·iy`）
- 上位依据：`11_upm.md` §4.3（`:64`「gauge 约束（固定参考帧或和约束）固定**加性**自由度」）与 §5 `:125` `gauge` 只有 `reference_frame / sum` 两值 ⇒ **`11_upm.md` 里没有"bilinear 是真实模型方向"这一论断**（检索式：`git grep -n "bilinear\|双线性\|零空间" -- docs/plugins/algorithms_phase2 docs/science/PHASE2_UPM.md`）
- 现状：锚定/deflation 两处都作用在这 4 维上，代码注释承担了整个科学论证（"多一维"的推论、"不是规范自由度"的定性）
- 差在哪：一个决定"哪些方向被认为由数据而非规范固定"的判断只写在实现里；下层文档（应当是该判断的正本）没有任何对应条文，故无法独立判定其对错
- 后果：若 bilinear 中确有规范自由度未被 gauge 固定，则 κ/r_eff 判据会把一个真正自由的（或反过来是规范相关的）方向计入 ⇒ 可辨识性判决与发布面的稳定性主张失真。方向不可定（缺文档），故不判 fail-open
- 定级：S3（写不出"该方向真被误判"的复算证据；缺 11_upm.md 层条文可对照）
- 怎么算修好：把「惩罚零空间 = 双向仿射 ⇒ 4 维；其中常数与单向线性部分由 gauge/δ_k 吸收情况决定」写进 `11_upm.md` §4.3（或 `docs/science/PHASE2_UPM.md` §7a），并给一个"真值无效应 ⇒ 该方向读数归零"的 Oracle/负例（`synthetic_gate.cpp` 或 `eng/tests/unit/v6_p2_sky/**`）
- 置信：PARTIAL（缺可复算的反例；缺文档条文）

### CR-23-13 · `weight_mode=1` 下 Huber 的标准化残差是 `z = r·SNR`（量纲错配），而 `δ_k` 施加口径在 gauge_mode=1 时与 IRLS 内部口径不一致
- 轴：R（主轴）／S
- 位点：`sky_plane.cpp:1053` `const double sigma = (cfg.weight_mode == 0) ? std::sqrt(s.variance) : (1.0 / std::max(s.snr, 1e-12));`、`:1055` `const double z = (sigma > 0.0) ? r / sigma : 0.0;`、`:1056` `huber_w(z, cfg.huber_delta)`；另一处 `:1049-1052`（IRLS 的 fit **不加** `gauge_shift`）对 `:1162`（报告段 **加** `gauge_shift`）
- 上位依据：`docs/science/PHASE2_UPM.md:132` 逐字「`sigma_eff=max(|uncertainty|,sigma_floor)`；`Huber(δ=1.345)` 作用于**无量纲** `z`」；`docs/algorithms/UPM_SOLVER.md:27`「δ=1.345（**无量纲**，单位=sigma_eff）」；`sky_plane.h:471-474`「δ_k = gauge_shift + δ_k 多项式项」
- 现状：`sigma = 1/SNR` 使 `z = r·SNR`——`r` 单位是 ADU（采样值电平，`sky_plane.h:70`），`SNR` 无量纲 ⇒ `z` 带 ADU 量纲，却被拿去与无量纲的 1.345 比较；`sigma<=0` 时 `z=0` ⇒ Huber 给**满权**；gauge_mode=1 时 IRLS 把常数平移留在残差里（每帧残差整体偏 −c）
- 差在哪：稳健判定的"离群"定义随 ADU 标度变化（α 缩放下 z 变 α 倍，δ 不变）⇒ 同一数据换单位给不同 Huber 权重；`z=0` 把"不可定标"当成"完全正常"
- 后果：稳健 IRLS 的降权集合改变 ⇒ B_ref/δ_k 改变 ⇒ 施加量 δ_k 与接缝判据 `max|rel_step| ≤ 1e-2`（`11_upm.md:39`）在改过的面上度量；标度不变性主张（`DATA_SEMANTICS.md:1764` 一类"与 α 免疫"条款）不成立
- 定级：S2（`weight_mode=1` 需配置带禁用键才触发，见 CR-23-01；gauge_mode=1 的残差平移差在 `sky_plane.cpp:1441` 的 gauge 是在解**之后**才做的，故只影响后续 build 复用模型的报告路径——本轮未复算出可指认的产物错值，故不升 S1）
- 怎么算修好：σ_eff 一律取 `sqrt(s.variance)`（观测方差的唯一来源 = control_variance），`sigma<=0` ⇒ 该点权置 0 并计数；gauge 平移在 IRLS 前固定（或统一在 fit 里加 `gauge_shift`）；同批给"换 ADU 标度 α 后权重不变"的负例门（`eng/tests/unit/v6_p2_sky/**`）
- 置信：CONFIRMED（量纲推理＋原文；未做 α 缩放实跑）

### CR-23-14 · `chi2_red` 在无定义时被写成 `0.0`，直接违反"0 会冒充完美拟合"的正本条款，并让自适应细化判据失效
- 轴：C（主轴）／R
- 位点：`sky_plane.cpp:1208` `info.chi2_red = (info.dof_eff > 0.0) ? (swr2 / info.dof_eff) : 0.0;`；落盘 `sky_plane.cpp:1753` `{"chi2_red", m->info.chi2_red}`（**未过 `num_or_null`**）；消费 `sky_plane.cpp:1451-1452` `improved = (rc2==OK) && (info.chi2_red > 0.0) && (i2.chi2_red <= ratio*info.chi2_red)`
- 上位依据：`docs/plugins/algorithms_phase2/11_upm.md:112` 逐字「`dof_eff <= 0` ⇒ `chi2_red` 写 `null` 且 `chi2_red_defined = false`（无定义，取值 = `null`；**0 会冒充完美拟合**）」；`docs/contracts/DATA_SEMANTICS.md:2862`「`solvers.upm_gls.chi2` / `dof_eff` / `chi2_red` ……（`chi2_red` 无定义时为 `null`）」对照 `:2868`「必落字段（`solvers.sky_plane` 子块）：`kappa`、`rank`、`n_params`、`chi2_red`、`node_spacing_deg`」（同仓两侧对同一量给了**不同**取值面）
- 现状：天光面一侧把无定义读成 0.0，产品 JSON 也写 0.0（同文件的 `kappa/lambda_*` 反而走 null，见 :1748-1761）；无 `chi2_red_defined` 键
- 差在哪：同一概念（约化 χ² 不可定义）在本文件内部有三种表达（null／0.0／缺键取 0），且与 §4.7 的正本判据相反；自适应细化判据以 `chi2_red > 0.0` 为前提 ⇒ "无定义"与"零残差完美拟合"都得到"不再细化"的同一动作
- 后果：`solvers.sky_plane.chi2_red` 对外读数可被 0 冒充（审计脚本按值判读时把不可算读成完美）；节点间距自适应在下界前静默停止细化 ⇒ 表示能力（`2h`）低于本可达到的水平，而 `representation_limited` 仍记 0
- 定级：S1。触发路径：`mosaic` CLI → `module_adapters.cpp:10015` `p2_sky_plane_build_adaptive` → `sky_plane.cpp:1400` `solve_at` → `:1206-1208`（`dof_eff<=0` 当 `n_used ≤ rank_full`）→ `:1451` 判据短路 → `:1469-1475` 采纳粗网格 → `module_adapters.cpp:10083` `p2_sky_plane_save` → `:1753` 产品 `chi2_red = 0.0`
- 怎么算修好：`sky_plane.cpp:1208` 无定义时置 `NaN`（或加 `chi2_red_defined` 位），`:1753` 过 `num_or_null`，`:1451` 判据改为要求 `chi2_red_defined`；同批把 `DATA_SEMANTICS.md:2868` 的 `solvers.sky_plane` 必落面按 `:2862` 的 null 口径改写（否则改了代码，合同一致性门反判红），并给 `dof_eff<=0` 的正例/负例测试
- 置信：CONFIRMED（三处位点与两条条文逐字对照）

### CR-23-15 · `p2_sky_plane_open` 对产品里的 `degree/h/nx/ny/delta_order` 零校验：`degree ≥ 8` 触发栈越界，`h = 0` 静默给出边界夹紧的求值
- 轴：R
- 位点：`sky_plane.cpp:1842-1846`（`m->h = j["h"].get<double>(); m->degree = j["degree"].get<int>(); m->delta_order = j["delta_order"].get<int>(); m->m = delta_basis_size(m->delta_order); m->nx = ...; m->ny = ...;`）；现有校验仅 `:1984-1990`（尺寸自洽）与 `:1836`（format 串）；下游 `:1561` `bspline_basis(u, m->t0u, m->h, m->nx, m->degree, jx, nxv)` 配 `:1559` `int jx[8], jy[8]; double nxv[8], nyv[8];`，被调函数 `:105-106` `double N[8]; double left[8], right[8];`、`:120-123` `for (int r = 0; r <= d; ++r) { out_j[r] = ...; out_n[r] = ...; }`
- 上位依据：`sky_plane.h:31`「fail-closed（11_upm.md §4.5）：……非有限解 一律显式失败」；`11_upm.md:81`「欠定、断图、不可辨识或显著模型失配 → 显式失败……不假装同基准」；检索式 `git grep -n "astrocs-sky-plane" -- eng docs` 显示该中间产物**无 schema 面**（只有 `eng/tests/unit/p2001_real_nodes_test.cpp:708/:712` 检 §7a 键存在性）
- 现状：`degree` 从文件读入即直接用；`d ≥ 8` 时 `bspline_basis` 写 `out_j[8]`/`N[8]`/`left[8]` 越界（build 侧由 :484 钳到 {1,3}，open 侧无任何等价钳位）；`h ≤ 0` 时 `floor((u-t0)/h)` 非有限 ⇒ `span` 经 :103-104 夹紧 ⇒ 求值返回一个"看起来合法"的面值，`P2_SKY_EVAL_OK`
- 差在哪：同一结构体字段的合法域在两条入口上一条被强制、一条被忽略；损坏/手工改过的产品不 fail-closed，而是产出静默错值（或 UB）
- 后果：P2b 施加节点（`module_adapters.cpp:10269` `p2_sky_plane_open` → `:10544` `p2_sky_plane_eval_delta_block`）直接把读入的 δ_k 扣进产品；面被污染 ⇒ 马赛克逐像素值与逐像素方差同时失真
- 定级：S2（生产触发路径完整，但需产品半损坏；本仓产品由 `:1703` save 自产 ⇒ 现网正常链路不会给出 `degree≥8`。缺的是"外部/损坏输入"这一角证据，故按 §3 规则不升 S1）
- 怎么算修好：`open()` 里按 `build` 侧同一组不变量校验并 `delete m; return P2_SKY_PLANE_IO_ERROR`：`degree∈{1,3}`、`delta_order∈[0,2]`、`h>0 且有限`、`nx,ny ≥ degree+1`、`du_min<du_max`、`us,vs>0`、全部字段 `isfinite`；并校验 `model_hash` 与 `coeff/deltas` 重算一致（save 已写哈希，:1239-1240）；同批给 `eng/tests/unit/v6_p2_sky/**` 加半损坏产品的负例（防"校验加了但没人测"）
- 置信：CONFIRMED（数组尺寸与循环上界逐行核对；未做 ASan 实跑）

### CR-23-19 · 设计点名的 formulation 要素「参考平面用其他帧的加权组合（排除自身）」在天光面载体里不存在，下层正本零复述，仓库唯一同名词做的是另一件事
- 轴：D（主轴）／C
- 位点：`sky_plane.cpp:804-843`（逐帧法方程装配：帧 k 的**全部**自身采样点同时进 `H_data`/`rhs`，无任何 leave-one-out）、`:1027-1039`（δ_k 回代用同一帧自身数据）；对照 `sky_plane.cpp:676` 权重与 `:1049-1052` 的 `fit`
- 上位依据：`ASTROCS_DESIGN.md:411` 逐字「**参考平面用其他帧的加权组合（排除自身）**、迭代带阻尼、拟合权重与叠加权重同源、末端用叠加权重计算并扣除残差场，使任意覆盖子集上的加权阶跃恒为零；formulation 四要素与收敛判据见 `docs/plugins/algorithms_phase2/11_upm.md`」；而 `docs/plugins/algorithms_phase2/11_upm.md` 全篇**零次**出现「排除自身」（检索式 `git grep -n "排除自身" -- docs` ⇒ 0 命中，仅 `ASTROCS_DESIGN.md:411` 1 命中），其 §4.3 只规定「每帧在参考面之上只拟合自己的平缓梯度 δ_k(x)」
- 现状：`B_ref` 与 `{δ_k}` 是**一次联合加权最小二乘**的解（Schur 消元只是消未知量、不是排除自身贡献）⇒ 第 k 帧自己的采样点参与决定该帧的参考面；被施加的 `δ_k = b_k − B_ref`（:1633）因此自带"自我拟合"成分
- 差在哪：设计那句的主语是**参考平面如何构造**（排除本帧），本文件的真实形态是**包含本帧的联合解**；仓库里唯一叫"排除自身"的实现是 `lib/algorithms/integration/v6/src/variance_propagation.cpp:145`「(c) 排除自身: 分母 `W_{−i}`」与 `include/astrocs/v6/variance_propagation.h:64`「`H_ii = 0`」——其主语是**叠加残差制造者/逐像素校正方差的分母**（`module_adapters.cpp:10556` 正是用它算 `Var(corrected)`），与"参考平面的构造"不是同一件事
- 后果：设计把「使任意覆盖子集上的加权阶跃恒为零」这一对外主张挂在四要素上；要素之一在天光面载体缺失且下层文档无表述 ⇒ 主张的成立性无法从代码核实（自拟合使 δ_k 系统性偏小、单帧覆盖区尤甚：一帧独占区域里 `B_ref` 完全由该帧自身决定 ⇒ `δ_k ≈ 0` ⇒ 帧间台阶不被校正却无判红）
- 定级：S2（升 S1 需 11_upm.md 层明确该要素适用于 `B_ref` 构造；现权威面只在设计顶层出现，下层文档未接 ⇒ 缺"适用域条文"一角证据）
- 怎么算修好：先在 `11_upm.md` §4.3/§4.7 判定并写清"排除自身"到底约束谁（参考面构造 / 残差制造者方差 / 两者）；若确指参考面，则在 `p2_sky_plane_build` 侧提供逐帧 leave-one-out 的 `B_ref^{(−k)}` 或改由 UPM GLS 侧供给，并补"真值无帧间差 ⇒ δ_k 归零"负例＋"单帧独占区 ⇒ 显式登记 `n_unidentified`/不可辨识"的判据；若判定不适用，则订正 `ASTROCS_DESIGN.md:411` 的适用范围（走 §8 变更流程，不擅自改公式）
- 置信：PARTIAL（缺 `11_upm.md` 层的适用域条文，无法定"必须实现"还是"文档需订正"）

### CR-23-16 · δ 分块求值把"越域/未知帧"落值为 0（合法校正量），块返回码在生产被丢弃，未校正像素数无任何计数或 provenance
- 轴：R（主轴）／C
- 位点：`sky_plane.cpp:1592` `out_values[i] = (st == P2_SKY_EVAL_OK) ? val : 0.0;`（同 :1649）；`:1594`/`:1651` `if (st != P2_SKY_EVAL_OK) rc = 2;`；生产调用点 `lib/infrastructure/scheduler/src/module_adapters.cpp:10544-10552`（`p2_sky_plane_eval_delta_block(...)` 的返回值未被接住，仅逐点 `dstat == P2_SKY_EVAL_OK` 才扣）
- 上位依据：`sky_plane.h:484`「返回 0=全部成功；2=存在越域/未知帧（状态逐点写出，**out_values 该点=0**）」；`docs/plugins/algorithms_phase2/11_upm.md:92`「不收敛/判红不阻塞……但必须在产品里落**机器可检**的警告：顶层 `warnings[]` / `warning_codes[]`……**无警告时 `warning_codes` 为空数组**（可断言的成功态，不存在"没写就是没问题"的歧义）」；`11_upm.md:39` 接缝门 `max|rel_step| ≤ 1e-2`
- 现状：越域点被赋 0.0，而 0.0 在 δ 语义里等于"该帧此点与公共面零偏差"（合法数值）；生产侧靠 `dstat` 分支避开了扣 0，但既不接 rc、也不统计有多少点没扣；`apply` 节点的诚实标记（`module_adapters.cpp:10692`）只写"用了 `/p2_sky_plane_eval_delta_block`"这一机制名，不写覆盖比例
- 差在哪：`11_upm.md:92` 要求的部分失败面（机器可检警告 + 空数组即成功态）在这里不存在；"未测"（越域/未知帧）与"已测且无偏差"在求值输出面上不可分
- 后果：`ASTROCS_DESIGN.md:411` 的「使**任意覆盖子集**上的加权阶跃恒为零」与接缝门的读数前提（校正已施加）失去可核性：产品可在全帧边缘/越域区一条 δ 都没施加，而 manifest 仍显示 δ 路径已启用；偏差方向明确（少扣 ⇒ 帧间台阶残留 ⇒ 接缝门偏**红**，但"校正覆盖率"这一对外主张偏绿）
- 定级：S1（触发路径：`mosaic` CLI → apply 节点 `module_adapters.cpp:10541` `sub_delta && sky_guard.m` → `:10544` `p2_sky_plane_eval_delta_block` → `sky_plane.cpp:1620-1624` 越域分支 → `:1649` 落 0 / `:1651` rc=2 被丢 → `:10604` 写 tile → 产品字节与接缝判据）
- 怎么算修好：求值失败点不改写 `out_values`（保持未定义，由调用方按状态决定）；apply 节点接住 rc，统计 `n_pixels_not_corrected` 并进 `warnings[]`/`warning_codes[]`（新增如 `P2-SKY-DELTA-PARTIAL-DOMAIN`）；同批让接缝门侧消费该计数（否则改了代码判据仍看不见）
- 置信：CONFIRMED


### CR-23-17 · 同一产品里"未测/不可算"有三种表达（null／NaN／0.0），且 `rank_full`/`n_params_full` 不落盘、`normal_matrices` 返回码与头文件承诺不符
- 轴：C（主轴）／R
- 位点：`sky_plane.cpp:1888`（`info.kappa = i.value("kappa", 0.0)`）、`:1899-1903`（`lambda_numerical/lambda_max/lambda_min/dof_eff` 缺失 ⇒ `0.0`）、`:1908-1910`（`rms_*/chi2_red/iterations` 缺失 ⇒ `0.0`）对照 `:1890-1896`（`kappa_data`/`kappa_solve` 缺失 ⇒ `quiet_NaN()`）与 `:1748-1750`（save 端 `kappa` **不过** `num_or_null`，`kappa_data/kappa_solve` 过）；`:1744-1767` 无 `rank_full`/`n_params_full` 键；`:1513`/`:1525` 对 open 模型返回 1，而 `sky_plane.h:449` 承诺「2 = 该模型来自 open()」
- 上位依据：`11_upm.md:111-113` 逐字「κ 在秩亏块上发散 ⇒ 产品写 `null`……；『**字段缺失**』是另一个状态；……缺键一律具名登记」；`DATA_SEMANTICS.md:2871`「必须另读到 `rank`、`chi2_red`、`rank_rtol`（**真值而非具名不可得**）」
- 现状：`kappa = 0`（数学上不可达：κ ≥ 1）被当默认值发布；`lambda_min = 0` 同时是"秩亏"与"缺失"两种语义；`identifiable` 缺失默认 0（这个方向对，恰说明其余默认取 0 是不一致而非刻意）；`sky_plane.cpp:1113` 的 `kappa_data` 别名在 save/open 两侧语义不对称（save 写原值、open 缺失写 NaN）
- 差在哪：判据读数的"不可算"在本文件里被判据自身的取值面污染——审计方按值判读时会把缺失读成"条件极好/谱为零"
- 后果：`solvers.sky_plane` 与天光面产品 JSON 的判据读数不可作为对外证据（`DATA_SEMANTICS.md:2868` 必落 `kappa`、`chi2_red`）；打开产品后 `rank_full=0` ⇒ 任何按 `dof = n_obs − rank_full` 复算 χ² 的路径得到错值（`11_upm.md:105`）
- 定级：S2（错值发生在"从产品重建模型再复算"的审计路径，生产施加路径不读 `rank_full`）
- 怎么算修好：`open()` 中所有判据量与 `kappa` 统一"缺失 ⇒ NaN／非有限 ⇒ JSON null"，并加 `*_defined` 位；save 增 `rank_full`/`n_params_full` 两键并回读；`normal_matrices` 按 `sky_plane.h:449` 区分 1/2；同批把 `DATA_SEMANTICS.md:2868` 的 sky_plane 必落面按 `:2862` 的 null 口径统一（防只改代码不改合同）
- 置信：CONFIRMED

### CR-23-18 · `p2_sky_estimate_gain_dc` 把"天光电平比"当"帧间增益"，且其科学定义只存在于头文件注释
- 轴：S（主轴）／D
- 位点：`sky_plane.cpp:2025` `const double g = num / den;`（`num/den` = 两帧公共 control 上背景估计值之和的比）
- 上位依据：`sky_plane.h:499-502`（唯一说明："直流不受帧间加性结构（相位/台阶）影响，是独立于 UPM MA 结构定标的稳健增益量"）；权威文档面零提及（检索式 `git grep -n "P0-10\|gain_dc\|直流比" -- docs ASTROCS_DESIGN.md`）；反向条文：`ASTROCS_DESIGN.md:186`「传统信噪比把天光信号计入信号，被天光抬高而失真」、`docs/science/CONTROL_WEIGHT_SNR.md:101`「`SNR` 的分子只有源……天光**只**经其散粒噪声进入分母」；`11_upm.md:35`「本期 `g_k ≡ 1`，不引入乘性」
- 现状：`value` 是局部稳健背景电平（`sky_plane.h:70`），比值即两帧天光电平之比；无不确定度、无最小公共点数要求（`common==0` 才返回 2，1 个公共点也给 0 返回码）
- 差在哪：两帧天光不同（本文件正在建模的对象就是它的帧间差）时该比值与增益无关；把它当"稳健增益量"作交叉校验，校验对象与误差源共线；`common` 无下限门
- 后果：若用于 P0-10 交叉校验判据（"MA 与该值不一致时不得施加 MA 增益"，`sky_plane.h:501`），会按一个非增益量决定"是否施加" ⇒ 定标路径的可信度主张失真
- 定级：S3（`eng/ci/ledgers/prod_wiring.json:322` 登记 `unreachable:header.P2_API:p2_sky_estimate_gain_dc`；非测试唯一调用点是 `lib/algorithms/coverage/tools/stage2.cpp:649`，不在 DAG 生产面 ⇒ 无产物触发路径）
- 怎么算修好：要么删该函数（本期 `g_k ≡ 1`，`11_upm.md:35`），要么把定义与适用条件写进 `docs/science/PHASE2_UPM.md` 并给"无帧间增益 ⇒ 比值 = 1"的负例，同时补 `min_common_controls` 门
- 置信：PARTIAL（未读 stage2.cpp:649 附近对该值的使用判据，只确认调用点存在）

## 本批最高危三条

1. **CR-23-01 ＋ CR-23-02**（合同禁用键在生产配置读取路径被静默接受并切换科学权重口径，且切换方向被专项条款明文否定）——`module_adapters.cpp:9973` → `sky_plane.cpp:524/676/677` → 产品 `:1725` 落键；`eng/ci/check_no_weight_mode_code.py:50-75` 的判据形态（只认 `enum class *WeightMode` 与五个具名路由/集合 API）判不到整型字段与 `sp_cfg.value("weight_mode", …)`。
2. **CR-23-14**（`chi2_red` 无定义写 0.0，与 `11_upm.md:112`「0 会冒充完美拟合」正面冲突，且短路节点间距自适应细化判据 :1451）——判据读数与产品字节同时失真。
3. **CR-23-19**（设计点名的 formulation 要素「参考平面用其他帧的加权组合（排除自身）」在天光面载体里不存在、下层正本 `docs` 面零命中，仓库唯一同名实现的主语是残差制造者方差而非参考平面）——顶层对外主张不可从代码核实。

## 我读不动或需要实跑才能定的

1. `:1059`/CR-23-06：生产权重尺度下 IRLS 是否真停在未收敛点——需实跑 `p2_sky_plane_build` 读 `iterations` 与 `db`（建议 `eng/tests/unit/v6_p2_sky/v6_p2_sky_node_adapt_test.cpp` 同型夹具 + 生产 `control_variance` 尺度，套 `mem_guard --max-rss-gb 4`）。
2. `:114` `denom == 0` 分支可达性——需解析证明或小规模数值探针（`h>0` 时 `denom = h·(r+1+j−r)` 应恒正，若成立则该分支应改为具名失败或删除）。
3. `:715` `support_floor = 1e-9` 对 `n_free`（= 判据矩阵阶）的敏感度——需 `retain_normal_matrices=1` + `p2_sky_plane_normal_matrices` 做谱扫描；本批无法定其保守方向的量化幅度。
4. CR-23-19：「排除自身」到底约束参考平面还是残差制造者——**不是实跑能定的**，需 `11_upm.md`/`docs/science/PHASE2_UPM.md` 层补条文或由负责人裁决（§10 第二类：科学定义有歧义）。
5. CR-23-15：半损坏产品的真实可达性——需确认 `p2_sky_plane_save` 之外是否有任何写该 JSON 的路径（本批检索 `astrocs-sky-plane` 只见 save/open 与该测试）；若存在外部改写面（手工修产品）则升 S1。
6. CR-23-18：`stage2.cpp:649` 是否把该比值用作判据（我只确认调用点存在，未读其分支）——需读 `lib/algorithms/coverage/tools/stage2.cpp:600-700`。

## 收工 git status --porcelain 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

`git rev-parse --short HEAD` = `c8f64e9a`（与开工一致；本批零写入仓库、未跑构建/ctest/门禁/任何 `eng/**` 脚本、未 `import` 仓库 Python、未碰共享台账与 `独立审计包/`）。

## 派单与清单一致性／节奏回执

- 清单 1 个文件（`lib/algorithms/coverage/src/sky_plane.cpp`），与派单文字一致；实测行数 2030（`Read` 至 :2030 `}  // extern "C"` 收尾），整读完毕，无未读区间。
- 节奏：骨架先行落盘，随后按 1-800／801-1600／1601-2030 三段 append（段 A 表与 10 条 finding 先落，段 B/C 表与其后 finding 后落）。
- 本批工具调用计数在 60 次上限内完成；未执行任何被禁动作。
- 附注（不作 finding）：`sky_plane.cpp` 与 `sampler.cpp` 的 patch 估计是两份同数学定义的实现（`sky_plane.h:90-91` 自陈），两份默认值已在段 A 表逐位点对照（`min_samples` 5 对 64、污染阈 0.20 配置键对硬字面量、`k_corr` 域判据有对无）；判"哪一份是正本"需读 `sampler.cpp` 全文，不在本批清单内。


