# 通读 CR-02 ｜ lib/algorithms/photometry 第 2 段（6 文件）

- 批次：CR-02
- 被审基线 HEAD：`c8f64e9a`
- 清单来源：`独立审计/批次清单/CR-02.txt`
- 派单前言：`DISPATCH-CODE-READ-PREAMBLE.md` §1/§2/§2.5/§3/§4/§5 已完整读取并照做

## 开工基线 `git status --porcelain` 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与派单 §1 期望的基线一致（仅两项未跟踪：`?? ACSD整治工作包_AUDIT-06.zip`、`?? site/`），无跟踪文件被改动。

---

## 文件清单与阅读状态

1. `lib/algorithms/photometry/cpp/include/log_macros.h` —— 已读：是（表 4 行，全文件 21 行逐行读完）
2. `lib/algorithms/photometry/tests/p1phot/p1phot_field_stub.hpp` —— 已读：是（表 5 行，全文件 51 行逐行读完）
3. `lib/algorithms/photometry/tests/p1phot/p1phot_fixgates.cpp` —— 已读：是（表 16 行，全文件 273 行逐行读完）
4. `lib/algorithms/photometry/tests/p1phot/p1phot_fixtures.hpp` —— 已读：是（表 26 行，全文件 252 行逐行读完）
5. `lib/algorithms/photometry/tests/p1phot/p1phot_gaia_stub.cpp` —— 已读：是（表 10 行，全文件 98 行逐行读完）
6. `lib/algorithms/photometry/tests/p1phot/p1phot_gaia_stub.hpp` —— 已读：是（表 9 行，全文件 57 行逐行读完）

<!-- PROGRESS: 6/6 -->

---

## 1. `lib/algorithms/photometry/cpp/include/log_macros.h`（21 行，整读完毕）

### §2.5 数值处置表

逐行核对 1–21 行：本文件**数值字面量 0 个**（无常数、无阈值、无缓冲尺寸、无截断窗、无收敛参数）。
本文件的全部"常量类"对象是 1 个默认配置值与 3 个非数值字面量，逐一登记如下，不因无数值而空表。

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| `log_macros.h:10` | `#ifdef PC_ENABLE_DEBUG` 的默认态 | 未定义 ⇒ 高频调试日志默认全关 | 编译期日志详细度开关的默认值（唯一真正的"默认配置值"） | 待确认 | 零：`git grep -n PC_ENABLE_DEBUG -- .` 全部命中仅 `log_macros.h:6,10`、`star_matcher.cpp:327`、`photometry/memory.md:54,284`；`git grep -n "target_compile_definitions"`（全仓 40+ 命中）无一处定义它；`git grep -n "日志"` 在 `ASTROCS_DESIGN.md`/`ENGINEERING_SPEC.md`/`docs/` 的 40 条命中里无任何条款规定调试日志开关 | 保守方向：维持默认关闭（关闭不改产物字节，只会少观测）。影响范围＝仅观测面：仓库内**不存在任何启用路径**，`memory.md:54/284` 声称"可通过定义 PC_ENABLE_DEBUG 启用"只在手工 `-D` 编译时成立；见 CR-02-2 | CONFIRMED（检索式已跑，命中集已列全） |
| `log_macros.h:13` | `((void)0)` | LOG_DEBUG 关闭态的展开 | 空语句惯用法，被禁日志的占位展开 | 结构性不适用 | 非科学量：它不含任何数值语义，只是"语句存在但什么都不做"的标准写法；其科学影响为零（该模块所有 `LOG_DEBUG` 实参只打印、无副作用，见 `star_matcher.cpp:328-332`） | — | CONFIRMED |
| `log_macros.h:11,17,18` | `"[DEBUG] "` / `"[INFO] "` / `"[ERROR] "` | 三级前缀串 | 人可读行的级别标签 | 结构性不适用 | 非科学量：纯显示串，不参与任何数值、判据或产品字节。备查事实：本模块只有 debug/info/error 三级，而机器日志正本 `lib/infrastructure/observability/logging/log_event_v1.schema.json:86-90` 的 `level` 枚举是 `debug/info/warn/error` 四级——**缺 warn**，而 §11 的显式降级正是 warn 级语义；该缺口作为 CR-02-1 的子项登记，不单列 finding（层级缺失本身不落产品字节） | — | CONFIRMED |
| `log_macros.h:11,17,18` | 落点实参 `stderr` + 终结符 `"\n"`；`::fprintf` 返回值 | stderr / LF / 丢弃 | 日志写出目标与写状态处理 | 待确认 | 冲突而非缺席：`ENGINEERING_SPEC.md:205`「日志经 `aio` 唯一 I/O 边界写出；**模块不自建文本 logger**、不自持日志文件句柄、不自行决定落点」；`ASTROCS_DESIGN.md:529-534`（§7.3）要求运行日志落 `<output_dir>/logs` 且机器日志为 `astrocs.log.event.v1` 事件行 | 保守方向：落点不由模块决定，改由 observability/`aio` 承接。影响范围＝Phase1 测光节点全部诊断的去向；`::fprintf` 返回值被丢弃与 §11:204「日志写失败即运行失败」在字面读取上不符，但 204 条的适用域是 `output_dir` 日志工件（本宏不产生工件），故**不据此单列 finding**，仅在 CR-02-1 内备查 | CONFIRMED |

### findings

### CR-02-1 · Phase1 测光生产库自带文本 logger 直写 stderr，本阶段诊断完全不进运行日志工件，而 CHK-LOG-SYS 的检测面看不见这一类违规
- 轴：D（主轴）＋C
- 位点：`lib/algorithms/photometry/cpp/include/log_macros.h:17`
  `#define LOG_INFO(...) do { ::fprintf(stderr, "[INFO] "); ::fprintf(stderr, __VA_ARGS__); ::fprintf(stderr, "\n"); } while (0)`（`:11` `LOG_DEBUG`、`:18` `LOG_ERROR` 同族）
- 上位依据：`ENGINEERING_SPEC.md:205`「日志经 `aio` 唯一 I/O 边界写出；模块不自建文本 logger、不自持日志文件句柄、不自行决定落点」；`ASTROCS_DESIGN.md:531-532`（§7.3）「机器日志 = 结构化日志合同行（`astrocs.log.event.v1`，实现家 `lib/infrastructure/observability/`）；人可读摘要与机器日志**同源**生成」；`ASTROCS_DESIGN.md:529-530`「每次运行（含失败与取消）在块级 `output_dir` 下产出完整运行日志，日志目录默认 `<output_dir>/logs`」。
- 现状：本头文件就是模块自建文本 logger（`fprintf(stderr)` + 自写级别前缀 + 自写换行），且被本模块当作对外行为写进文档：`lib/algorithms/photometry/README.md:128`「输入校验失败 → 返回负值（上表）；DLL 内 fprintf(stderr) 诊断 + LOG_INFO」。调用面在本模块三个生产 TU 里共 58 处（`git grep -c LOG_INFO -- lib/algorithms/photometry/cpp/src`：`pc_api.cpp` 21、`star_matcher.cpp` 33、`spectrum_integrator.cpp` 4），全部经本宏落到 stderr；全仓无一处把测光日志接入 observability 事件行。
- 生产触发路径（逐跳，满足 S1 硬门槛）：
  ① `lib/infrastructure/scheduler/src/module_adapters.cpp:119` `#include "frame_photometry_fit.h"` →
  ② `module_adapters.cpp:5522`「(1) 生产 star_matcher 拟合通道」（`photometry.fit.enabled=true` 时经 `frame_photometry_fit` 调 `pc_calibrate_simple_with_gaia_f64_v2_qf`，见 :5280-5281 注释自述）→
  ③ `CMakeLists.txt:1068-1074` `astrocs_module_adapters` 链接 `astrocs_phase1_photcal` →
  ④ `CMakeLists.txt:764-772` 该生产库编译 `star_matcher.cpp` / `pc_api.cpp` / `spectrum_integrator.cpp` →
  ⑤ `lib/algorithms/photometry/cpp/src/star_matcher.cpp:173` `LOG_INFO("[star_matcher] 初始化: KD-tree + IRLS/Tukey 清洗 (GAP-013)")` →
  ⑥ 本文件 `:17` `::fprintf(stderr, ...)`。链路两端都在跟踪面，非仅测试面。
- 差在哪：设计把"模块的日志"定义为经 `aio` 写出、落 `<output_dir>/logs`、与机器事件同源的**产品工件**；这里模块自己决定落点（stderr）、自己定义级别文本，等于该阶段的日志根本不存在于工件面。
- 后果：三条对外主张对 normalize 的 Phase1 测光节点失真——§7.3「每次运行产出完整运行日志」（该节点零行进入 `run_<run_id>.jsonl` / `.log`）、§7.3「人可读摘要与机器日志同源生成」（同源的实现家被旁路）、`run manifest` 的 `log_artifacts[]` 覆盖不了该阶段诊断（离开进程 stderr 即永久丢失，失败复盘只剩 ABI 负值状态码）。产物数值不受影响（错误仍靠统一状态码上行），所以是主张层失真而非字节层错值。
- 门禁侧（C 轴同条成立）：判据 `CHK-LOG-SYS` R3 的登记台账 `eng/ci/ledgers/log_system_ledger.json` 中 `log_landing` 段 `max_entries` 已等于条目数（8/8 满），登记的是 `lib/algorithms/psf/src/dpsf_log.cpp`、`lib/algorithms/star_detection/src/sdet_log.cpp`、`lib/infrastructure/aio/src/aio_log.cpp`、`orchestrator.cpp`——即同类"模块自建 logger"其它模块已入台账，唯独 photometry 零登记（`git grep -n "log_macros\|photometry" -- eng/ci/ledgers/log_system_ledger.json` 无命中）。根因在检测器：`eng/tools/quality/check_log_system.py:338-372 scan_log_landing()` 只匹配「字面量含 `logs/` 目录」或「赋给 `log_dir/log_path/log_file/event_dir`」两种形态，纯 `fprintf(stderr,...)` 型自建 logger 结构上不在它的命中面内 ⇒ R3 对本条恒绿，且台账已满也无法靠登记收口。
- 定级：S1（可致对外主张不成立，触发路径见上；不改产物字节）
- 怎么算修好：本模块 58 处 `LOG_INFO/LOG_ERROR` 调用点保留文本，但输出改接 `lib/infrastructure/observability/logging` 的 LOG-001 事件行并经 `aio` 落 `<output_dir>/logs`；`log_macros.h` 只留编译期转发、不再自持落点。必须同批改的东西（防"改对反而判红"）：(a) `lib/algorithms/photometry/README.md:128` 的行为描述；(b) 若走"先登记后台账"的过渡路线，`log_system_ledger.json` 的 `log_landing.max_entries` 必须先由检测器扩出 `text_logger_in_module` 命中类再谈条目数，**不得只把上限数字改大**（§11 明文"不得用本台账把门禁改绿"）；(c) 补 warn 级或说明降级用什么事件表达。
- 置信：CONFIRMED（宏体、58 处调用计数、构建链接两跳、检测器函数体与台账段计数均已逐字读取）

### CR-02-2 · `PC_ENABLE_DEBUG` 在跟踪构建面里从无定义点，"可启用"是只存在于手工编译的承诺
- 轴：D（主轴）＋C
- 位点：`lib/algorithms/photometry/cpp/include/log_macros.h:10` `#ifdef PC_ENABLE_DEBUG`；配套 `:13` `#define LOG_DEBUG(...) ((void)0)`
- 上位依据：条文面零提及。检索式：`git grep -n PC_ENABLE_DEBUG -- .`（命中仅本文件 2 行、`star_matcher.cpp:327`、`photometry/memory.md:54,284`）、`git grep -n "target_compile_definitions"`（全仓命中无一涉及）、`git grep -n "日志" -- ENGINEERING_SPEC.md ASTROCS_DESIGN.md docs/`（无调试日志开关条款）。
- 现状：唯一使用 `LOG_DEBUG` 的循环体（`star_matcher.cpp:326-332`）在 `#ifdef PC_ENABLE_DEBUG` 内**再包一层同名 `#ifdef`**，与宏自身的 `#ifdef` 重复；`cpp/Makefile`、`cpp/build.ps1`、根 `CMakeLists.txt` 均无该宏的定义或选项面。
- 差在哪：本文件 `:6` 与模块 `memory.md:54/284` 对外陈述"可通过定义 `PC_ENABLE_DEBUG` 启用"，但项目唯一被登记为权威的构建配置（根 CMake + `docs/ci/`）里没有这个开关，启用与否完全取决于仓外手工 `-D`，且没有任何文档裁定谁可以打开它。
- 后果：无产物字节影响；影响是"调试期高频日志面"在正式构建中永不编译，故 `star_matcher.cpp` 匹配循环内的诊断格式串长期不做类型检查（`%.2f`/`%d` 与实参错配不会被任何构建配置发现），同时"调试可启用"这条模块自述不可复现。
- 定级：S3（写不出产物错值或对外科学主张失真的后果，仅观测面 + 文档自述不可复现）
- 怎么算修好：要么在根 `CMakeLists.txt` 为 `astrocs_phase1_photcal` 加一个显式 option（`ASTROCS_PHOTOMETRY_DEBUG`，默认 OFF）并统一宏名到 `ASTROCS_` 前缀（现役 `PC_` 前缀属 `photometric_calib` 退役命名，与改名贯穿问题同族），要么删掉 `:6` 与 `memory.md` 里的"可启用"陈述；同批删除 `star_matcher.cpp:327/333` 的冗余外层 `#ifdef`（只留宏自身的门），避免双重门掩盖真实启用点。
- 置信：CONFIRMED（三条检索式命中集已列全，无 CMake 定义点）

（本文件另有 1 处备查事实不单列：`::fprintf` 返回值全部丢弃，与 `ENGINEERING_SPEC.md:204`「日志写失败即运行失败」的字面读法冲突；已判该条适用域为 `output_dir` 日志工件而非进程 stderr，故不成立为独立缺陷，并入 CR-02-1 现状。）

---

## 2. `lib/algorithms/photometry/tests/p1phot/p1phot_field_stub.hpp`（50 行，整读完毕）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| `p1phot_field_stub.hpp:24` | `wl_start`（默认参） | `336` | fake gaia_client 的 XP 谱网格起点 nm | 文献值 | `docs/science/PHOTOMETRY.md:308`（Gaia DR3 官方文档 §20.12.4 `xp_sampled_mean_spectrum` 原文 "343 values from 336 to 1020 nm with a step of 2 nm"，注明 2026-09-23 抓取核验 HTTP 200）；`docs/research/PHOTOMETRY_RESEARCH_PACK.md:39` 检索项 G2 同引原文。**注意**：值有锚，但"把它写死成测试默认参"这件事与条文口径不符，见 CR-02-3 | — | CONFIRMED |
| `p1phot_field_stub.hpp:24` | `wl_step`（默认参） | `2` | XP 谱网格步长 nm | 文献值 | 同上（§20.12.4 "step of 2 nm"）；本仓消费口径 `docs/contracts/DATA_SEMANTICS.md:678`「`spectrum_wl` double `[343]`（336..1020nm step 2nm）」 | — | CONFIRMED |
| `p1phot_field_stub.hpp:25` | `wl_count`（默认参） | `343` | XP 谱网格点数 | 文献值 | 同上（§20.12.4 "343 values"）；自洽式 `336 + 2×342 = 1020` 由 `docs/algorithms/PHOTOMETRIC_FIT.md:218` 逐字给出 ⇒ 三点互为公式导出，代码未用式算而是三个独立字面量（写死，见 CR-02-3 的"算则合规/写死则报"分支） | — | CONFIRMED |
| `p1phot_field_stub.hpp:22,37,38` | `spectrum_byte` / `flux_min` / `flux_mul` | 本文件**不取默认值**（形参无默认） | 常数谱字节与 uint8 谱线性还原参数 | 结构性不适用 | 本文件不携带这三个量的数值，仅透传 ⇒ 不属本文件处置项。跨文件观察（登记备查，不在本批定案）：16 个调用点全部传同一组 `200, 1.0e-15f, 5.0e-18f`（`git grep -n make_config -- lib/algorithms/photometry/tests` 全量命中，无一例外），与 `:4`「光谱/量化参数按用例选择」的自述不符——这三个数的科学口径处置属 `p1phot_tests_units.cpp` / `p1phot_fixtures.hpp` 批次，本批仅在 CR-02-3 附带指出 | CONFIRMED |
| `p1phot_field_stub.hpp:31,39,42` | `cfg.stars.resize(n)` / `spectra[i].size() == (std::size_t)wl_count` / `assign(wl_count, ...)` | 由入参推得的数组长度与循环上界 | 星数、谱长一致性判定、常数谱填充长度 | 结构性不适用 | 非科学量：全是容器维度与循环上界，取值由 `wl_count`/`f.gaia_ra.size()` 派生，不引入新数值语义 | — | CONFIRMED |

### findings

### CR-02-3 · 生产按 XPSD 文件头自报取谱网格，测试桥把同一网格写成三个死字面量且全套用例从不改动它，"容器自报"这条不变式零测试
- 轴：D（主轴）＋C
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_field_stub.hpp:24-25`
  `int wl_start = 336, int wl_step = 2,` / `int wl_count = 343) {`
- 上位依据：`docs/algorithms/PHOTOMETRIC_FIT.md:219`「**网格的实测来源**：真实 XPSD 分片文件头逐字为 `parameters="spectrumStart=336,spectrumStep=2,spectrumCount=343,spectrumBits=8"` ⇒ 网格与位深是**容器自报**而非硬编码；生产按文件头取参（`gaia_client.c:1437-1491`）」；`docs/science/PHOTOMETRY.md:230`「网格外 `T·Q` 置 0 而**不外推**」。
- 现状：生产侧确实按文件头取参——`cpp/src/frame_photometry_fit.cpp:183-185` `gaia_client_get_spectrum_params(client,&wl_start,&wl_step,&wl_count)` 后立刻 `if (prc != 1 || wl_count <= 0 || wl_step <= 0)` 才继续，波长数组在 `:193-194` 由 `wl_start + i*wl_step` 现算。测试侧则把 336/2/343 写成桥的默认参数，并由 `p1phot_gaia_stub.cpp:92-94` 把**同一组字面量**回吐给 `get_spectrum_params`，于是被测码在测试里"从容器取到的网格"恰好永远等于它自己写死的网格。全跟踪面内无任何一处把 `wl_start`/`wl_step` 设成非 336/2 的值：`git grep -n "cfg\.wl_start\|cfg\.wl_step\|wl_start =" -- lib/algorithms/photometry/tests` 命中仅本文件 `:24-25,27-29`、`p1phot_gaia_stub.hpp:42,44` 的重复默认值、`p1phot_tests_units.cpp:583-585`（后者仍写 `336`/`2`，只有 `wl_count` 取 `spec_wl.size()`）。
- 差在哪：设计把网格定性为**数据属性**（容器自报，生产不得写死），测试面却把它当**常量**；两侧对同一个量说的是两种来源，而这正是设计专门强调的区别（"而非硬编码"）。
- 后果：`frame_photometry_fit.cpp:185` 的守卫与 `:193-194` 的网格重建在 p1phot 全套用例里从未在"网格 ≠ 336/2/343"下执行过；若分片头解析或 `wl_start + i*wl_step` 的换算回归（例如 step 被当成点数、或 `+336` 偏移丢失），F_syn 会在错网格上积分而**测试仍全绿**，同时 `docs/science/PHOTOMETRY.md:230` 承诺的"网格外不外推"边界判据也失去检验（外推/截断的触发条件正是网格与通带的相对位置）。
- 定级：S2（不改当前产物字节——真实分片头就是 336/2/343；成立的是"设计点名的不变式无测试 + 上下级对同一量来源说法不一致"。未定 S1 因为写不出"当前生产输入下产物已经错值"的触发路径）
- 怎么算修好：给桥加一个变网格用例（如 `wl_start=340, wl_step=5, wl_count=137`，oracle 侧同步按该网格独立算 F_syn），让 `get_spectrum_params` 返回值真正参与被测码的网格重建；同时把三处默认值收敛到单一来源（本文件 `:24-25` 与 `p1phot_gaia_stub.hpp:42,44` 现在是两份独立字面量，改一处必漏另一处）。必须同批改的东西：`p1phot_oracle.hpp` 的期望积分必须用 `cfg.wl_*` 而不是自身常量，否则加了变网格用例反而判红。
- 置信：CONFIRMED（生产取参两处、守卫一处逐字读过；测试面网格赋值命中集已用 `git grep` 穷举；真实分片头字面值已由 `PHOTOMETRIC_FIT.md:219` 登记）

### CR-02-4 · 桥对"逐星光谱长度/条数不匹配"静默回退为常数谱，调用方装配错误不会让用例失败
- 轴：R
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_field_stub.hpp:39-42`
  `if (!spectra.empty() && i < spectra.size() && spectra[i].size() == (std::size_t)wl_count) s.spectrum = spectra[i]; else s.spectrum.assign((std::size_t)wl_count, spectrum_byte);`
- 上位依据：`AGENTS.md` §9「空断言、SKIP 充数……都算未完成」与 §5「判据必须非退化」的通则；本模块 `:4-5` 自述「光谱/量化参数按用例选择 (常数谱或 F4 随机谱)。装配过程不做任何被测域算法」——自述里只有"选哪种谱"，没有"选不到就换一种"。
- 现状：三个失败条件（未给 spectra / 条数少于星数 / 单条长度 ≠ `wl_count`）共用一条 `else`，一律填 `spectrum_byte` 常数谱，无 `assert`、无返回值、无日志。
- 差在哪：夹具装配的输入错误被夹具自己消化掉，SUT 收到的仍是一份"看起来合法"的星表。
- 后果：任何以"给定逐星光谱"为前提的用例，一旦装配写错（少一星、谱长少 1 点），实际执行的是常数谱分支；用例若仍通过，通过的结论与其声明的前提不同。不等式方向核对：本回退让**输入**偏离用例意图而不改变判据阈值，因此它既可能让本应红的负例变绿（负例若针对"畸形光谱输入的处置"），也可能让期望按常数谱生成从而恒绿——方向取决于调用点；本批 16 个调用点全传 `{}`，故**当前无实际受害者**，按此降级。
- 定级：S3（静默回退成立，但当前无调用点走该分支，写不出对外的失真主张）
- 怎么算修好：把 `else` 拆成"未提供 ⇒ 按设计取常数谱"与"提供了但形状不对 ⇒ `assert(false)`/返回错误"两类，形状错误不得静默；同时在头注释里写清两类的适用域。
- 置信：PARTIAL（缺"未来/仓外是否有调用点传非空 spectra"的证据；已确认跟踪面内 16 个调用点全传 `{}`）

（本文件另记一条不构成 finding 的事实：`make_config` 的逐星光谱形参在跟踪测试面内从未被使用，F4 用例改由 `p1phot_fixtures.hpp:190 fixture_f4_spectra` 自行装配并在 `p1phot_tests_units.cpp:583-585` 手写 `cfg.wl_*`——即"共用桥"对 F4 实际未收口，属重复装配而非缺陷，登记给 CR-02-3 的收敛建议一并处理。）

---

## 3. `lib/algorithms/photometry/tests/p1phot/p1phot_fixgates.cpp`（273 行，整读完毕）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| `:7,:8,:28,:117,:132,:145,:154,:180,:194,:247`（门限）＋`:154,:222` 的 `out.size()==3` | 冻结门 `\|r_consistent\|>=3` / `\|r_inliers\|>=2` | 3 / 2 | 参考星数门与"内点数不足则不估计离散度"门 | 待确认 | 只有"条文说它是冻结值"这一层：`docs/science/PHOTOMETRY.md:191`、`docs/algorithms/PHOTOMETRIC_FIT.md:161`。三档实证依据全空：无文献；无导出式（`n>=2` 才是 MAD 有定义的结构性下界，`>=3` 无推导）；标定件 `PHOTOMETRIC_FIT.md:31/34` 指向 `run/SCI-FIX-PHOTFIT-01/evidence/e1_synthetic_and_realdata.json` ⇒ 按 §2.5 第 3 档"指 `run/` 即判不合格" | 保守方向：门收紧（4+）更易 NO_DATA ⇒ 偏红不出值；放松到 `>=2` 即 2 星定标整帧，正是 `:142` 自己断言禁止的形态 ⇒ 偏绿出值。影响范围：Phase1 每帧测光零点是否产出 + `sigma_residual`→`sigma_mag`→SNR 传递链。附必须一并处置的纠缠：`PHOTOMETRIC_FIT.md:31` 自陈「冻结门 `>=3` 恰落在 MAD 期望偏差最大处（n=3 偏低约 1.49 倍），实现不修正」⇒ 取 3 这个数与"低星数帧 sigma 系统性下偏"互为因果，登记 UNRESOLVED | CONFIRMED（三处条文逐字读过；标定件路径按 §2.5 规则判不合格） |
| `:125,:140,:153,:173,:193,:221,:240,:256`（8 处同值） | `mag_tol` 实参 = `3.0` | 3.0 mag | 星等一致性预过滤窗宽 | 待确认 | `docs/science/PHOTOMETRY.md:34` 定义、`:219-220` 明言「**本层不引用文献**为该窗宽背书」＝项目自订冻结值；权威默认在 `eng/packaging/config/defaults.json:540-546`（`value: 3.0`, `authority_status: "sourced"`, `source: "docs/science/PHOTOMETRY.md:30"`）。**该登记锚行号漂移**：`docs/science/PHOTOMETRY.md:30` 处是 `location`/`S`/`c` 表区，`mag_tolerance` 实在 `:34`（已 sed 核对 28-36 行） | 保守方向：窗宽收紧⇒更多星被预过滤⇒更易撞门 1（偏红不出值）；放宽⇒错配/污染星进 IRLS，零点被拉偏（偏绿且错值）。影响范围：全部 Phase1 测光零点。另：本文件把 3.0 写成 8 处字面量，权威默认另在 defaults.json，改配置必漏这 8 处（登记备查，§4"测试夹具差"不另立 finding） | CONFIRMED |
| `:182`（注释内） | `4.685·S` | 4.685 | Tukey biweight 形状参数（用来说明"预过滤转离群数学不可达"） | 文献值 | `docs/algorithms/PHOTOMETRIC_FIT.md:299`「Beaton & Tukey 1974, Technometrics 16, 147（DOI 10.1080/00401706.1974.10489171）；Mosteller & Tukey 1977」；`docs/references/SCIENTIFIC_REFERENCES.md` 第 37 条同引；`docs/science/PHOTOMETRY.md:31` 表行 | — | CONFIRMED |
| `:155,:223` | `1e-12` | 1e-12 | 断言 `log10(scale)` 与 `−0.1` 的相等带宽 | 结构性不适用 | 不是科学量：它只是"退化常数 r 场下同值加权均值"的浮点回环余量（`r=pow(10,0.1)` 再 `log10` 的误差量级 ~1e-17），与科学容差无关；科学侧容差另有锚（IRLS `tol=1e-6`、`mag_tolerance=3.0`） | — | CONFIRMED |
| `:243` | `1e-6`（用作 `>` 分离带） | 1e-6 | 对照组断言"未过滤时 scale 明显不同"的下限 | 结构性不适用 | 是实现选择的分离带（不是收敛判据）；**易误读点登记**：权威 IRLS 收敛容差也是 `1e-6`（`PHOTOMETRIC_FIT.md:157` `_IRLS_CONVERGE`），两处同值不同义，读者可能误认为同一冻结量 | — | CONFIRMED |
| `:67,:186,:187,:210,:233` | `fsyn` / `1000.0` | 1000.0 | 夹具合成 F_syn 基准通量 | 结构性不适用 | 比值分母：实现只用 `r=log10(F_instr/F_syn)`（`star_matcher.cpp:456`），任意同值缩放被 log 抵消 ⇒ 无科学语义 | — | CONFIRMED |
| `:75,:186,:210,:233` | `std::pow(10.0, r)` 的底数 | 10.0 | dex 定义底数 | 结构性不适用 | 单位换算定义常数（dex↔通量比），§4 明确豁免 | — | CONFIRMED |
| `:76,:77,:187` | `-2.5` | −2.5 | 通量→星等系数 | 结构性不适用 | 星等标度定义常数（Pogson 定义），非经验值 | — | CONFIRMED |
| `:70,:71` | `m.x=10.0+3.0*i` / `m.y=20.0+2.0*i` | 10.0/3.0/20.0/2.0 | 匹配位置 | 结构性不适用 | `cleanAndScale` 不读 x/y（`star_matcher.cpp:445-465` 只取 `f_instr/f_syn/gaia_mag`＋质量位），位置仅供诊断 | — | CONFIRMED |
| `:74,:119,:134,:147,:167,:186,:208,:231` | `r_target`/偏移 `0.1,0.25,0.9,2.90,1.60` | 见左 | 夹具通量比构造值 | 待确认 | 其中 `0.9`（`:74`）分支**从未被启用**——16 个 `make_matches` 调用全部 `with_outlier=false`（本 TU 内 `:119,134,147,167,185,208,231,249`），而 `:62-63` 自述"若 true…用于 `\|r_inliers\|` 门"；`2.90`（`:186`）的前提注释算术错误，见 CR-02-9 | 保守方向：夹具偏移只需"跨过/不跨过"对应门即可，取值本身不进产品；风险不在数值大小而在"声称的判别路径没被走到"。影响范围：本 TU 的门覆盖结论 | CONFIRMED |
| `:155,:223,:127,:141,:257` | 期望 `scale=10^{-0.1}` / NO_DATA 时 `scale==1.0`、`sigma==0.0` | −0.1 / 1.0 / 0.0 | 断言的期望值 | 公式导出 | `docs/science/PHOTOMETRY.md:29`「`scale` = `10^{−location}` 校正因子」＋实现 `star_matcher.cpp:404-405` 入口初始化 `*out_scale=1.0/*out_sigma=0.0`；夹具 location 的解析值＝0.1（常数字段加权均值＝该常数）⇒ 期望式与实现一致，测试侧写死等价于按式算（此组合规，不需报 finding） | — | CONFIRMED |
| `:121,:136,:149,:169,:189,:217,:237,:252` | 哨兵 `123.0/456.0/999.0/−1.0/77.0` | 见左 | "未写即失败"哨兵 | 待确认 | `:121` 注释「哨兵: 未被覆盖即失败信号」与实现冲突：`star_matcher.cpp:404-405` 被调用即写 `1.0/0.0` ⇒ 哨兵在进入 callee 第一行就失效，"不写 scale"与"写 1.0"不可区分（见 CR-02-10） | 保守方向：哨兵值本身无科学语义；要保守就改判据形态（比较"是否被写为拟合值"而非"是否等于 1.0"）。影响范围：本 TU 对门 1 退化路径的判别力 | CONFIRMED |
| `:213,:214,:215,:216,:225,:251,:264,:265` | `0u` / `1u<<1` / `1u<<2` / `\|` 组合；`rejected_quality >= 3` | 见左 | 质量位位形与"计入 3 颗"计数断言 | 结构性不适用 | 位移量与枚举基数（§4 明确豁免项），锚为实现内部定义 `star_matcher.h:11,14,17`；`>=3` 里的 3 是夹具中被打饱和位的星数 | — | CONFIRMED |
| `:241` | `diag.rejected_quality >= 0` | 0 | 断言"nullptr 不做质量过滤" | 待确认 | 恒真，见 CR-02-11：字段类型 `photometric_calib.h:36` `int rejected_quality;`，实现三处赋值全为非负和（`star_matcher.cpp:471-472,519-521`）⇒ 无可达反例 | 保守方向：删掉或换成"与 :225 同族的等值断言"。影响范围：本 TU 的 PASS 计数与门的证据价值 | CONFIRMED |
| `:48,:267,:271,:272` | `g_failures=0` / `return 0/1` | 0 / 1 | 失败计数与进程退出码 | 结构性不适用 | 计数器初值与退出码约定，不进科学语义 | — | CONFIRMED |
| `:29`（注释） | "旧签名 7 参 cleanAndScale" 的 7 | 7 | 形参个数陈述 | 结构性不适用 | 形参基数（枚举基数类）；实测当前声明见 `star_matcher.h` 的 8 参重载（含 `out_match_reasons`），本 TU `:124` 以 7 参调用仍可编译 ⇒ 默认参存在，注释与签名数不自洽属表述问题，不单列 finding | — | PARTIAL（未逐行读 `star_matcher.h` 的默认参数声明） |

### findings

### CR-02-9 · 本 TU 声称锁住「`|r_inliers|>=2` 门」，实际那条用例在进入门 2 之前就被门 1 截住；删除门 2 本 TU 仍全绿
- 轴：R（主轴，误绿）＋C
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_fixgates.cpp:186`
  `m[2].f_instr = 1000.0 * std::pow(10.0, 2.90);  // delta 仍 <3 mag 预过滤`
  以及 `:194-200` 的 `if (out.size() < 2) { CHECK(sigma == 0.0 && diag.sigma_residual == 0.0, …); } else { CHECK(sigma >= 0.0, …); }`
- 上位依据：`docs/science/PHOTOMETRY.md:191`「`|r_inliers|>=2` 才估计 `sigma_residual`，否则 `sigma_residual=0`（`star_matcher.cpp:615-626`，判据在 `:616`）」；本文件自述 `:7-8`「冻结门: `|r_consistent| >= 3` 才进 IRLS…；`|r_inliers| >= 2` 才估计 sigma_residual」、`:22-29`「回归锁语义（未修复必红 / 已修复必绿）…判别力: 每个'必拒'用例都配'同构但合法'的对照用例」；`lib/algorithms/photometry/tests/p1phot/CMakeLists.txt:163` 亦登记本目标覆盖「`|r_consistent|>=3` / `|r_inliers|>=2` 门」。
- 现状：注释断言"2.90 dex 偏移后 delta 仍 <3 mag 预过滤"。按实现算（`star_matcher.cpp:457-460` `mag_inst=-2.5·log10(F_instr)`、`delta=mag_inst-gaia_mag`，`gaia_mag=-7.5`）：两颗 r=0.10 的 `delta=-0.25`，第三颗 r=2.90 的 `delta=-14.75-(-7.5)=-7.25`，`median_delta=-0.25` ⇒ `|delta−median| = 7.0 mag > 3.0 mag` ⇒ 第三颗在**星等预过滤**（`:486-500`）就被拒，`r_consistent.size()=2 < 3` ⇒ 走**门 1** `:509-536` 的 NO_DATA 早退（`return {}`）。于是 `out.size() < 2` 为真，断言的是 `sigma == 0.0`——而该 0.0 来自 `:404-405` 的入口初始化，不来自门 2。
- 差在哪（不等式方向已定，满足 §3 的误绿门槛）：门 2（`star_matcher.cpp:616` `if (r_inliers.size() >= 2)`）在这套夹具里**从未被执行**；把门 2 删成"无条件估计 sigma"，本 TU 的 12 条 CHECK 结果**逐条不变**（其余用例的 `r_inliers` 恒 ≥3，门 2 分支不参与；本用例连门 2 都不到达）⇒ 该门在本 TU 是 fail-open。另：`:74` 提供的 `with_outlier=true` 偏移 `+0.9` 正是"构造 IRLS 离群以触达 `|r_inliers|` 门"的旋钮（`:62-63` 自述），但 16 个 `make_matches` 调用无一传 `true` ⇒ 声称的判别路径连入口都没走。
- 后果：`p1phot_fixgates` 是登记在门的条目（`eng/ci/checks.json:3701,3905-3923` 要求 `run/ci/ctest/p1phot_fixgates.json`），其绿灯被当作"SCI-PHOT-001 §4/§8 两条冻结门都有锁"的证据；实际只有门 1 有锁。下游影响面：`sigma_residual` 供 SNR 模块使用（`star_matcher.cpp:613` 注释原文），门 2 的语义是"不可估计 ≠ 零离散度"，若该守卫回归则低内点帧会得到虚假离散度并进入创新点②的 SNR 传递链，而 CI 不会红。
- 定级：S2（判据面失真成立；**未定 S1**：本文件中无可达输入能让门 2 改变输出——见下一条的可达性分析，故写不出"当前生产输入下产物已错值"的逐跳路径）
- 怎么算修好：(a) 更正 `:7-8`、`:22-29` 与 `CMakeLists.txt:163` 的覆盖声明，只声称门 1；(b) `:186` 的注释算术改正并把该用例改名（它是"门 1 由预过滤触发"的正例，本身有价值：断言 `sigma==0.0` 恰好锁住了"早退路径不得留脏值"）；(c) 若确实要锁门 2，只能在 `sigma_residual` 的求值辅助函数上做直接单元测试（构造 `r_inliers` 长度 0/1），**不能**靠 `cleanAndScale` 的端到端夹具。必须同批改的东西：若采纳 (c)，须同时确认 `:404-405` 的入口初始化不会把"未估计"和"估计为 0"糊成同一个值，否则新用例仍恒绿。
- 置信：CONFIRMED（预过滤式、门 1 早退式、入口初始化三处均逐字读到；`out.size()`/`sigma` 分支按夹具输入手算复现：7.0 mag > 3.0 mag）

### CR-02-10 · 科学文档冻结的「`|r_inliers|>=2`」门在实现里对 `n>=3` 数学上不可触发，本 TU 注释已自陈却仍被登记为锁
- 轴：S（主轴）＋D
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_fixgates.cpp:181-183`
  「用星等预过滤把 3 颗一致星中的 2 颗转成"离群"(|delta|<3 mag 但 r 相差 >4.685·S) 在数学上不可达 (S 反比于 MAD 使 |u|<1 恒成立, 见下注)」
- 上位依据：`docs/science/PHOTOMETRY.md:191` 把 `|r_inliers|>=2 ⇒ sigma_residual=0` 列为**冻结**行为；`docs/algorithms/PHOTOMETRIC_FIT.md:161` 只登记了参考星数门一行（无门 2 行）。
- 现状：实现 `star_matcher.cpp:613-626`：`S = MAD(r)/0.6744897501960817`（`:540` 附近，一次定尺度），接受条件 `S <= 0.0 || |r-location|/(c·S) < 1`，`c=4.685` ⇒ 接受阈 `|r−location| < 6.945·MAD`。按 MAD 定义至少一半样本满足 `|r−median| <= MAD < 6.945·MAD` ⇒ `|r_consistent|=n≥3` 时 `r_inliers ≥ ⌈n/2⌉ ≥ 2`，`else` 分支不可达（`S=0` 时更是全收）。
- 差在哪：一条被冻结并写进科学文档 §4/§8 的判据，在其唯一调用面上恒不触发；文档承诺的"不可估计⇒不给离散度"保护实际由"根本进不到那一步"提供，而不是由该门提供。
- 后果：不影响当前产物（门不可达＝没有可被它改变的输出）；失真的是文档与验收面——`PHOTOMETRY.md:191` 与 `CMakeLists.txt:163` 让读者以为存在一条受锁保护的语义。属于 §8「科学疑义」里"文档说的与实现的可达域不一致"的登记项，不构成改代码的理由。
- 定级：S3（写不出产物或对外科学主张的失真后果；仅文档-实现可达域不一致）
- 怎么算修好：按 §8 流程走（不在本批改）：在 `PHOTOMETRY.md:191` 补门 2 的**适用域**（何时可达，或声明为防御性冗余并给出可达性论证），并把 `PHOTOMETRIC_FIT.md:161` 表补一行；同步把 `CMakeLists.txt:163` 与本 TU 头注释的覆盖声称收窄到门 1。若裁决为"防御性冗余"，则不得反过来删门 2（删了仍全绿，属危险动作）。
- 置信：CONFIRMED（`c·S`、MAD 定义与接受式逐字核对；不可达性由接受阈 6.945·MAD 与 MAD 的半数性质推出；本 TU 注释自陈同一结论）

### CR-02-11 · `diag.rejected_quality >= 0` 是恒真断言，却占据一条 PASS 名额并标称为"不做质量过滤"的证据
- 轴：R
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_fixgates.cpp:241`
  `CHECK(!out.empty() && diag.rejected_quality >= 0, "b43_no_flags_backcompat: nullptr 不做质量过滤 (旧行为)");`
- 上位依据：`AGENTS.md` §9「空断言、SKIP 充数……都算未完成」；字段定义 `lib/algorithms/photometry/cpp/include/photometric_calib.h:36` `int rejected_quality; // F<=0/非有限 + 星等不一致 + IRLS 离群 的总和`。
- 现状：实现对该字段的全部赋值均为非负和：`star_matcher.cpp:471-472`（`n_in - n_quality_rejected`）、`:519-521`（`n_quality_rejected + n_invalid + n_mag_rejected`）、`:225` 用例走到的路径同理；故 `>= 0` 无可达反例。真正判别"未过滤"的是紧随其后的 `:243`（`> 1e-6`）。
- 差在哪：一条永不失败的断言配了一条它并不能证明的说明文字（"不做质量过滤"由 :243 证明）。
- 后果：PASS 计数虚增（本 TU 打印 12 条 PASS，其中 1 条恒真），削弱这份锁作为验收证据的分量；不改产物字节。
- 定级：S3
- 怎么算修好：删掉 `>= 0` 这一半，保留 `!out.empty()`，并把"不过滤"的语义交给 `:243`；或改为与 `:225` 对偶的强断言 `diag.rejected_quality == 0`（nullptr 时不该计入质量剔除）。必须同批核对：`star_matcher.cpp` 在 nullptr 路径是否会把 `n_invalid`/`n_mag_rejected` 也计进 `rejected_quality`（字段注释表明它装三类拒绝的总和），若是则 `== 0` 不成立，须改用例前先固定字段语义——这就是"改对反而判红"的点位。
- 置信：CONFIRMED（赋值点三处全为累加和，未见可为负的路径）

### CR-02-12 · 本 TU 头注释的"生产一律 nullptr、生产行为与 HEAD 逐行等价"与跟踪代码相反：生产节点已走带质量位的 `_qf` 入口
- 轴：C
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_fixgates.cpp:18-20`
  `生产路径 (pc_api.cpp) 一律以 quality_flags=nullptr 调用 → 生产行为与 HEAD 逐行等价; 将来接线需上游先产出质量位`
- 上位依据：`docs/science/PHOTOMETRY.md:191` 附近与 `SCI-PHOT-001 §4/§10`（本文件 `:9` 引用）；接线条款即本仓生产接线登记面 `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv`。
- 现状：六个冻结 C 导出确实传 nullptr（`pc_api.cpp:200,500,1229,1275`），但非 ABI 的内部入口 `_qf` 把调用方数组一路转发：`pc_api.cpp:1313` 收 `quality_flags` → `:1328` 转 `run_*_impl` → `:1120` `cleanAndScale(raw_matches, &match_rows, quality_flags, 3.0, …)`；而生产节点正是调它：`frame_photometry_fit.cpp:217` `pc_calibrate_simple_with_gaia_f64_v2_qf(...)`，位值来自 `module_adapters.cpp:5612-5613`（`const int64_t q = s.value("quality", int64_t{0}); c.qf = (q & 1) ? (1u << 1) : 0u;`）与 `:5689`（`freq.psf_quality = pqf.empty() ? nullptr : pqf.data();`）。即"将来接线需上游先产出质量位"已经发生（上游 sdet 已产饱和位）。
- 差在哪：一份登记在门里的回归锁文件，其"改动范围声明/生产影响声明"与当前跟踪代码相反；`:229-245` 的"向后兼容：nullptr → 不过滤（旧行为）"因此不再对应任何生产通道，只对应库层内部。
- 后果：不改产物字节（饱和位在生产里被正确使用，方向上是**更安全**的一侧）；失真的是验收面结论——按本文件读会把"饱和门尚未接线"当成现状，从而对该路径免做检查。反向的覆盖缺口也存在：真正接线的 `_qf` 通道在本批 6 文件里零用例，其唯一锁 `eng/tests/validation/release02/fix_p1_photometry_apply/p1_qf_oracle.cpp`（`:138` 传真实位）在跟踪面上被标为 `eng/ci/prod_wiring_baseline.json:48` `unbuildable_test:…p1_qf_oracle.cpp` 与 `eng/tests/unit/CMakeLists.txt:1796` 的"非发布目标"（`docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:291`）。
- 定级：S2（上下级陈述与代码事实相反；未定 S1 因为它不产生错值，且已接线路径本身方向正确）
- 怎么算修好：把 `:13-20` 的范围声明改为事实描述（"冻结 6 导出仍传 nullptr；scheduler 节点经 `pc_calibrate_simple_with_gaia_f64_v2_qf` 传真实位"），并为本 TU 增一条经 `_qf` 入口 + 非空位数组的用例（与 :225 的库层用例互证）。必须同批改：若同时把 `p1_qf_oracle` 从 `unbuildable_test` 台账移出，需要该目标真能被注册构建——这条我没有实跑证据，见文末"读不动/需实跑"第 1 项。
- 置信：PARTIAL（生产接线链与 nullptr 事实全部逐字读到；缺"`_qf` 路径在 CI 里是否有已注册绿证据"，需 `ctest -R p1phot` 或查 `run/ci/ctest/` 产物才能定）

### CR-02-13 · `static_assert(&pc_abi_probe_simple != nullptr)` 恒真，且 `reinterpret_cast<void*>(&fn)` 根本不锁形参表
- 轴：R（主轴）＋C
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_fixgates.cpp:110`
  `static_assert(&pc_abi_probe_simple != nullptr, "B4 ABI: pc_calibrate_simple 缺失");`
  配套 `:98-109`（6 个 `static void *const pc_abi_probe_* = reinterpret_cast<void *>(&pc_calibrate_…);`）与 `:92-96` 的自述
- 上位依据：`:92` 自述「ABI 冻结锁（依据 `docs/contracts/PUBLIC_API.md` §「C ABI…」+ `ASTROCS_DESIGN.md` §8.5): 既有 6 个 C 导出**不得新增形参**」；`:16-17` 自述风险「orchestrator 用 raw 函数指针按位置调用既有导出, 给它们加尾部形参会让未初始化实参被解引用 (P0)」。（我查过的检索式：`git grep -n "define LOG_"`／`git grep -n "pc_calibrate_simple[a-z0-9_]*" -- photometric_calib.h`，公共头 `photometric_calib.h:106,158,191,207,233,…` 恰 6 个 `PC_API` 导出，与探针数量一致。）
- 现状：`&pc_abi_probe_simple` 取的是**变量**地址（对象指针），永不为 null ⇒ 断言恒真；而对函数取址再 `reinterpret_cast<void*>` 对**任意**形参表都合法，故"不得新增形参"这条在本 TU 没有任何编译期表达。真正起作用的只有"符号存在 ⇒ 链接通过"这一层。
- 差在哪：注释把自己没做的锁（形参表冻结）说成断言面，而该 TU 被登记为回归锁目标。
- 后果：不改产物；风险面是本 TU 声称已封的 P0（尾部加形参）实际由别处守：`docs/contracts/API_CONTRACTS.csv:288` 逐字登记了 `pc_calibrate_simple_with_gaia_f64_v2` 的完整签名，配套检查器 fixtures `eng/tools/quality/contracts/fixtures/check_api_contracts/invalid_sig_mismatch.csv` 存在（即 CHK-API-CONTRACTS 才是形参锁的正本）⇒ 本 TU 的这条属装饰性重复，不构成无保护。
- 定级：S3
- 怎么算修好：要么删掉这条恒真 `static_assert` 并把注释改准（"本 TU 仅锁符号存在性，形参表由 CHK-API-CONTRACTS 锁"），要么换成真正判别式的写法：`static_assert(std::is_same_v<decltype(&pc_calibrate_simple), int(*)(void*,…)> )` 逐签名比对。必须同批：若改用逐签名比对，签名正本必须引用 `API_CONTRACTS.csv` 同一份，不得在测试里另立第二套字面签名（否则变成新的双侧漂移源）。
- 置信：CONFIRMED（断言对象与 `reinterpret_cast` 的语义按 C++ 规则判定；形参锁的正本位置已查到文件与 fixture 名）

---

## 4. `lib/algorithms/photometry/tests/p1phot/p1phot_fixtures.hpp`（252 行，整读完毕）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| `:35,:36,:37,:38` | splitmix64 常数 `0x9E3779B97F4A7C15`/`0xBF58476D1CE4E5B9`/`0x94D049BB133111EB`，移位 30/27/31 | 见左 | 伪随机数发生体的混合常数 | 结构性不适用 | 非科学量：RNG 内部混合常数，不进任何科学语义；可复现性由"算法名＋seed 固定"提供（`ASTROCS_DESIGN.md:760` 要求"固定 seed、可复跑"）。**登记缺锚事实**：本仓文献池与规范面对该算法零登记（检索式 `git grep -n "splitmix\|SplitMix" -- docs ASTROCS_DESIGN.md ENGINEERING_SPEC.md` 零命中），即"这个 RNG 是谁的哪个实现"在仓内不可回溯；按 §4 不列 finding（夹具 RNG 属自订实现），但它是"算法身份无锚"这一族的实例 | — | CONFIRMED |
| `:42` | `(next() >> 11) * (1.0/9007199254740992.0)` | 2^53 | 64 位随机数→[0,1) 双精度 | 公式导出 | 式：取高 53 位 ÷ 2^53；`9007199254740992 = 2^53` 逐字相等，右移 11 = 64−53。代码真按式算 ⇒ 合规 | — | CONFIRMED |
| `:52,:53` | `width/height` | 64/64 | 夹具帧尺寸 | 结构性不适用 | 数组维度/循环域，实现选择；调用方按需覆写（`p1phot_tests_perf.cpp:47-48` 覆成 1024） | — | CONFIRMED |
| `:54,:55` | `crval1/crval2` | 180.0 / 45.0 | 夹具天球指向 | 结构性不适用 | `:50` 自述"fixture 常数；任意可行值，非被测域契约值"，且 oracle 与实现共用同一 WCS ⇒ 取值不进期望 | — | CONFIRMED |
| `:56,:57` | `crpix1/crpix2` | 32.5 | 参考像素（1-based） | 公式导出 | 式：1-based 帧中心 = (N+1)/2 = (64+1)/2 = 32.5；注释 `:56` 已标 "1-based"，perf 覆写 1024 时同步改 512.5=(1024+1)/2（`p1phot_tests_perf.cpp:49-50`）⇒ 两处一致，代码按式取值 | — | CONFIRMED |
| `:58,:61` | `cd11=-2.8e-4` / `cd22=+2.8e-4` | 2.8e-4 deg/px | 像素尺度（≈1.008″/px，北向上东左负） | 待确认 | 注释换算 `2.8e-4 × 3600 = 1.008″/px` 复算正确（单位换算定义常数豁免）；但**为什么选这个尺度**只有软锚：`docs/algorithms/GATES_AND_TOLERANCES.md:67` 记"仓内最小真实尺度 0.9586″/px"，夹具 1.008″/px 与真实域同量级 ✓ 却不对任何真实相机同值 | 保守方向：夹具尺度应不高于被测门的适用域下限（0.9″/px 一侧）——现值 1.008 在域内 ✓。影响范围：WCS 相关夹具（F1/F2/F3）的适用域代表性 | PARTIAL（未核该尺度是否刻意对齐某台真实相机；只核到量级与适用域） |
| `:99` | `100.0 * rng.uniform()` | 100.0 dex 通量抖动幅度 | 让逐星 F_syn 互异且为正 | 结构性不适用 | 实现选择（正值互异），期望由 oracle 从同一 seed 复算 ⇒ 无对外语义 | — | CONFIRMED |
| `:104,:165` | `-2.5` | −2.5 | 通量→星等系数 | 结构性不适用 | 星等标度定义常数 | — | CONFIRMED |
| `:108,:146` | `std::pow(10.0, …)` 底数 | 10.0 | dex↔通量比 | 结构性不适用 | 单位换算定义常数 | — | CONFIRMED |
| `:110` | `1000 + (int64_t)i` | 1000 | 逐星 id 基 | 结构性不适用 | id 命名空间起点，与 F3 的 2001..2006 不撞号即可 | — | CONFIRMED |
| `:120-122,:138-140` | 格点上界 `row<6`/`col<4`；`10.0+12.0*col+1.0*row`；`10.0+9.0*row` | 6×4 ⇒ **24 星硬上限** | F1/F2 的确定性像素格 | 待确认 | 间距常数（12/9/10/1）是实现选择（注释 `:118`"避开边缘 8px, 间隔≥3px"复算成立：min 邻距 ≈9.06 px，x≤51、y≤55 均在 64 帧的 8 px 边带外）；**但 24 这个上界与 `n_star` 形参之间无任何校验**，`n_star>24` 时静默少产 ⇒ 见 CR-02-14 | 保守方向：应"请求数 ≤ 上界"直接 assert（偏红）而不是静默截断（偏绿）。影响范围：`p1phot_tests_perf.cpp:51`（请求 48，实得 24）；F1/F2 其余调用点请求 4–20 均在上界内 | CONFIRMED |
| `:123,:141` | seed `0x5EED000000000001ULL` / `…0002` | 见左 | F1/F2 各自的固定 seed | 结构性不适用 | seed 取值本身任意，规范要求的是"固定"（`ASTROCS_DESIGN.md:760`）；两 seed 不同使 F1/F2 序列互不串扰，属实现选择。备注：seed 无登记面（无台账记录"哪个用例用哪个 seed"），换 seed 不触发任何门 | — | CONFIRMED |
| `:125,:143` | `fsyn_base` | 1000.0 | 直通通道的 F_syn 参考量级 | 结构性不适用 | 比值尺度：实现只用 `log10(F_instr/F_syn)`，同值缩放被抵消；v2 通道下该值被 `fsyn_ref` 覆盖（`:101`） | — | CONFIRMED |
| `:143` | `perturb_amp` | 0.02 dex | 逐星 r 抖动幅度（保证 S>0） | 公式导出 | 式：`S = MAD(r)/0.6744897501960817`、接受阈 `c·S = 4.685·S`；±0.02 均匀的 MAD≈0.01 ⇒ `c·S≈0.069` ⇒ 内点全收（\|dev\|≤0.02<0.069）且与 `:146` 的 0.8 分离 12 倍。夹具按该式选值，非写死科学量 | — | CONFIRMED |
| `:145,:146` | 离群偏移 `+0.8` dex（注释 `:130` 推 `mag 偏移 2.0<3.0`） | 0.8 | 构造"过星等预过滤、但被 IRLS 拒"的离群星 | 公式导出 | 两条式都成立且已复算：① Δmag = 2.5·Δdex = 2.5×0.8 = **2.0 mag < mag_tolerance 3.0** ⇒ 不被预过滤（预过滤量确为 mag，`star_matcher.cpp:457-460` `delta=mag_inst−gaia_mag`，`|delta−median|>tol` 拒）；② 0.8 ≫ `c·S≈0.069` ⇒ 必被 IRLS 拒。⇒ 该值有推导、非任意 | — | CONFIRMED |
| `:77` | `200.0 * (double)5.0e-18f + (double)1.0e-15f` | 200 / 5e-18 / 1e-15 | 常数谱 uint8 解码 f0 = byte·flux_mul + flux_min | 待确认 | 解码式与 `docs/contracts/DATA_SEMANTICS.md:678`「uint8 谱 F(λ)=byte·flux_mul+flux_min（spectrum_integrator.cpp:62-64）」逐字一致 ⇒ **式**有锚；但三个**值**是夹具自订，且与 16 个调用点（`p1phot_tests_units.cpp:52` 等 `make_config(f, {}, 200, 1.0e-15f, 5.0e-18f)`）各写一遍、无共同定义点 ⇒ 改一处必漏另十七处 | 保守方向：抽成一处常量（如 `fix::k_stub_byte/k_stub_fmin/k_stub_fmul`）。影响范围：v2 通道全部期望值（`v2_fsyn_const()` 与 SUT 必须同源） | CONFIRMED |
| `:78` | `f0 * 0.5 * (1020.0*1020.0 − 336.0*336.0) / 2.0` 中的 `0.5` | 0.5 | 两点常数滤光片 T0 | 结构性不适用 | 夹具响应曲线峰值，真值由 `p1phot_tests_units.cpp:57-58` 的 `const_filter` 提供（`tr={0.5,0.5}`），非科学量 | — | CONFIRMED |
| `:78` | 端点 `1020.0` / `336.0` | 336 / 1020 nm | 闭式积分区间＝XP 网格端点 | 文献值 | `docs/science/PHOTOMETRY.md:308`（DR3 §20.12.4 "343 values from 336 to 1020 nm"）；积分式本身 `F_syn=∫S·T·Q·λ dλ` 见 `pc_api.cpp:354` 注释与 `docs/science/PHOTOMETRY.md:352` 行③，λ 因子⇒原函数 ½λ² ⇒ `*0.5 … /2.0` 为公式导出 | — | CONFIRMED |
| `:200` | `for (int wl = 336; wl <= 1020; wl += 2)` | 336/1020/2 | F4 谱网格 | 文献值 | 同上（DR3 §20.12.4）；点数复算 = (1020−336)/2+1 = **343** ✓ 与 `p1phot_field_stub.hpp:25` 默认 `wl_count=343` 同值。**登记重复定义**：同一网格在本批内有三处独立字面量（`field_stub.hpp:24-25`、本文件 `:78`、本文件 `:200`）＋ `p1phot_gaia_stub.hpp:42-44` 共四处，无单一定义点 | — | CONFIRMED |
| `:203` | `s < 4` | 4 套光谱 | F4 星数 | 结构性不适用 | 夹具规模（数组基数） | — | CONFIRMED |
| `:205` | `rng.next() & 0xFF` | 0xFF | uint8 谱字节 | 结构性不适用 | 位掩码（位宽），谱字节值域由容器定义（8-bit，`docs/science/PHOTOMETRY.md:316`） | — | CONFIRMED |
| `:207,:208` | `1.0e-15 + 1.0e-16*u`（flux_min）、`5.0e-18 + 1.0e-18*u`（flux_mul） | 见左 | XPSD 逐星量化参数（W·m⁻²·nm⁻¹ 量级） | 待确认 | 零：`docs/science/PHOTOMETRY.md:316` 只说明"8-bit 归一化存储＋逐星还原参数"的**机制**（PixInsight XPSD 一手描述），未给参数量级；真实分片自报参数在只读数据集 `gaia/GaiaDR3SP`（非跟踪面，本批无法核值），`docs/algorithms/PHOTOMETRIC_FIT.md:219` 登记的也只有网格与位深，没有 flux_min/mul 数值 | 保守方向：夹具量级应不高于真实分片量程，否则 F4 只验证了自订域（偏乐观）。影响范围：F4 的解码/积分用例对真实量化域的**代表性**（不是对错的判定） | PARTIAL（缺真实分片头 flux_min/flux_mul 实测值；该值只能从 `gaia/GaiaDR3SP` 读，非跟踪面） |
| `:212,:213,:215` | 滤光片 21 点、`400.0+28.0*i`、峰 `0.9`、心 `678.0`、宽 `60.0` | 见左 | 合成通带曲线（高斯） | 结构性不适用 | 非真实响应：本仓真实响应曲线正本是 `lib/algorithms/photometry/data/response_curves/filters.json` 与 `qe_curves.json`，夹具不声称代表任何实测通带（`:210` 亦标"高斯形"）；复算：400+28×20=960 ✓ 与注释 `[400,960]` 一致 | — | CONFIRMED |
| `:219,:220,:222` | QE 17 点、`350.0+41.25*i`、基线 `0.6`、峰 `0.25`、心 `550.0`、宽 `180.0` | 见左 | 合成 QE 曲线 | 结构性不适用 | 同上（合成形状，非实测 QE）；复算：350+41.25×16=1010 ✓ 与注释 `[350,1010]` 一致 | — | CONFIRMED |
| `:228` | `fixture_f1(g, 1.25, 8)` 的 `1.25` / `8` | 1.25 / 8 | F5 基底的注入 k 与星数 | 结构性不适用 | 注入真值（期望由 oracle 用同值复算），8 ≤ 24 在上界内 | — | CONFIRMED |
| `:237,:238` | `(float)bg` 初值、`rng.sym(2.0)` | 2.0 | 背景噪声 ±2 ADU（产生非零 σ_sky） | 结构性不适用 | 夹具噪声幅度，F6 的期望通量由 oracle 逐像素求和复算（`:231-232` 自述）；备注 `:237` 的初值被 `:238` 全量覆盖，属冗余写法不是缺陷 | — | CONFIRMED |

### findings

### CR-02-14 · F1/F2 格点枚举把星数静默截到 24，perf 用例请求 48 实得 24
- 轴：R
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_fixtures.hpp:120-122`
  `for (int row = 0; row < 6 && (int)pixels.size() < n_star; ++row) for (int col = 0; col < 4 && (int)pixels.size() < n_star; ++col) pixels.emplace_back(10.0 + 12.0 * col + 1.0 * row, 10.0 + 9.0 * row);`（`:138-140` 同族复制）
- 上位依据：`:114` 自述「F1 精确 k 场 (**n_star 颗**, 无扰动…)」——即 `n_star` 是对外的用例参数；`ASTROCS_DESIGN.md:760` 与 `AGENTS.md` §9 要求实验/测试面自洽可复算（规模与声明一致）。
- 现状：格点总数 = 6×4 = 24，`n_star` 只作为提前 break 的条件，无上界校验、无返回值、无断言。跟踪面内已存在越界调用：`lib/algorithms/photometry/tests/p1phot/p1phot_tests_perf.cpp:51` `pf.f = fix::fixture_f1(pf.g, 1.25, 48);`，而计时处把规模如实按实际取：`p1phot_tests_perf.cpp:71,74` `(int)pf.f.gaia_ra.size()` / `(int)pf.f.psf_cx.size()` ⇒ 实际喂进 `pc_calibrate_simple_f64` 的是 24 颗 Gaia 星 / 24 颗 PSF 星。其余调用点（`p1phot_tests_negative.cpp:55,111,154,176,204,234,254,279,306,328,343` 取 4/6/8；`p1phot_tests_oracle.cpp:146` 取 20、`:231` 取 12；`p1phot_tests_properties.cpp:109,209,273,325` 取 8–20）均 ≤24。
- 差在哪：请求 48 得到 24 不报错——夹具把它自己想要的规模悄悄改了。
- 后果（方向已定）：perf 面报告的"48 星"其实测的是 24 星，实测耗时系统性偏小约 2 倍（匹配 + IRLS 的 O(n) 段）；若 perf 数字被引用为规模相关的对外指标，则该数字与其标注规模不符。星等/数值类用例（n≤20）不受影响。**这是测量面失真，不是产品字节失真**。
- 定级：S3（测试面规模失真；无对外科学主张依赖"48 星 perf"这一标注，我未在任何门里找到以星数标注的 perf 判据——检索式 `git grep -n "fixture_f1(" -- lib/algorithms/photometry/tests` 与 `git grep -n "G-P1-PHOT" -- docs/algorithms/GATES_AND_TOLERANCES.md`，后者未见按星数标注的时限门）
- 怎么算修好：在 `:120` 前加 `if (n_star > 24) abort/assert`（宁可红不要静默少），或把格点扩成 `ceil(n_star/4)` 行；并把 `p1phot_tests_perf.cpp:51` 的请求改成实际能给的数（或在 perf 报告里如实标 24）。必须同批改：`:138-140` 的复制体一并处理，否则 F2 仍是漏的一侧。
- 置信：CONFIRMED（越界调用点与"按实际 size 计时"两处逐字读到；其余调用点的实参已全部枚举）

### CR-02-15 · 闭式期望 `v2_fsyn_const()` 把积分区间写成字面量，而非取自它自己的网格数组，独立性靠"两边碰巧同值"维持
- 轴：C（主轴）＋D
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_fixtures.hpp:78`
  `return f0 * 0.5 * (1020.0 * 1020.0 - 336.0 * 336.0) / 2.0;`
- 上位依据：`:4-5` 自述「期望值一律由 `p1phot_oracle.hpp` 独立实现生成, fixture 只负责装配输入场」；`AGENTS.md` §5 要求创新点①（测光星等坐标系）的期望与实现相互独立佐证；`docs/algorithms/PHOTOMETRIC_FIT.md:218` 规定生产区间的**来源**是容器自报网格（336/2/343），不是常量。
- 现状：同文件 `:200` 的 F4 网格由循环生成（`for (int wl = 336; wl <= 1020; wl += 2)`），`p1phot_field_stub.hpp:24-25` 与 `p1phot_gaia_stub.hpp:42-44` 再各写一遍 336/2/343；而 v2 通道的闭式期望直接写死端点 1020/336，不引用 `spec_wl.front()/back()` 也不引用 `cfg.wl_*`。
- 差在哪：期望值的"独立性"只在数值层面成立（式不同），在**参数来源**层面不独立——期望与被测双方各自写死同一组字面量，谁也没读容器；一旦容器网格变（真实分片可以是别的 `parameters=`），实现侧跟着文件头走（`frame_photometry_fit.cpp:183-194`），期望侧不动 ⇒ 判红不是因为算法错，而是夹具漂移。
- 后果：与 CR-02-3 同族（本批已就"网格来源"立案），此处是该族在期望侧的落点：v2 通道 F_syn 期望对网格的依赖不可追踪到单一来源。当前两边同值，产物与测试均不失真。
- 定级：S3
- 怎么算修好：`v2_fsyn_const()` 改为接受 `(const std::vector<double>& spec_wl, double T0, double f0)` 并用 `front()/back()` 算区间；336/2/343 的三元组在本批四处收敛为一处定义。必须同批改：所有以默认参调用 `make_config` 的 16 个点与 `p1phot_tests_units.cpp:583-585` 的手写 `cfg.wl_*`。
- 置信：CONFIRMED

### CR-02-16 · 夹具自陈绕开 `S=0` 退化分支，而该分支把离群星计为内点并输出 `sigma_residual=0`，条文与用例双双不覆盖
- 轴：D
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_fixtures.hpp:131`
  `// 扰动基底保证 S>0, 否则 S=0 门直出 median 吞掉离群)。`
- 上位依据：`docs/algorithms/PHOTOMETRIC_FIT.md:66` 是条文对该分支的**全部**规定：「`S=0` 且 `|r_consistent|≥3` … 此时 `location=median(r)`、`scale` 照常发布、`sigma_residual=0`」；`docs/science/PHOTOMETRY.md:191` 只规定两条星数门。检索式：`git grep -n "S=0\|S <= 0" -- docs/science/PHOTOMETRY.md docs/algorithms/PHOTOMETRIC_FIT.md`。
- 现状：实现 `star_matcher.cpp:549-552` 在 `S<=0` 时跳过 IRLS（location 保持 median，与条文一致 ✓）；但 `:600-608` 收集内点时条件是 `if (S <= 0.0 || std::fabs(u) < 1.0)` ⇒ **S=0 时无条件全收**，一颗偏离 2 dex 的星照样进 `cleaned`/`r_inliers`/`fit_used`；随后 `:616-626` 对它算 `MAD(dev)`，只要另有两颗 r 逐位相等就有 `MAD=0 ⇒ sigma_residual=0`。条文只规定了 location/scale/sigma 三件事，**没有规定内点集合的构成**，因此"离群被当作内点"落在条文适用域之外。
- 差在哪：夹具注释已经把这条识别成"吞掉离群"并专门构造去避开它，但没有任何用例去检验它，也没有任何条文禁止它——即"实现自知的有害分支"既无规范覆盖也无测试覆盖。
- 后果（不等式方向已定）：`fit_used` 变大、`sigma_residual=0` 变小 ⇒ 帧级质量指标**偏乐观**（偏绿）：低星数帧可以报"3 内点、零离散度"，而其中一颗是 2 dex 离群；`sigma_mag = 2.5·sigma_residual`（`docs/science/PHOTOMETRY.md:33`）为 0 会顺着 SNR 传递链走（`star_matcher.cpp:613` 注释「供 SNR 模块使用」）。**未定 S1**：触发前提是需要 ≥3 颗 `|delta−median|≤3 mag` 且其中两颗 `r` 逐位相等，我给得出结构路径（`module_adapters.cpp:5689`→`frame_photometry_fit.cpp:217`→`pc_api.cpp:1313`→`star_matcher.cpp:549-626`），但给不出真实输入下逐位相等的发生概率证据。
- 定级：S2（注明"缺运行时可达性证据"）
- 怎么算修好：分两步且顺序不可反——(1) 按 §8 派子代理查一手依据（MAD=0 时稳健尺度惯例，如 MASS `rlm()` 的 `scale madma` 备选、Beaton & Tukey 的退化处理）后，在 `docs/algorithms/PHOTOMETRIC_FIT.md:66` 那行补齐"内点集合如何构成、离群在 S=0 时是否有权被拒"；(2) 依条文加一条 F 系列退化用例（r = {0.1, 0.1, 2.9}，过预过滤，断言 `fit_used` 与 `sigma_residual` 的规范值），并把 F2 注释从"绕开"改成"另有用例覆盖"。不得先改实现（改 `:601` 的短路方向会牵动创新点①的既有 Oracle 与 `PHOTOMETRIC_FIT.md:66` 的"scale 照常发布"承诺）。
- 置信：PARTIAL（实现三处逐字读到、条文缺口确认；缺"真实数据是否会落进该退化域"的统计证据，与 §9 要求的可达性判据）

### CR-02-17 · 本批夹具所依赖的两个生产常数，其在代码侧的出处锚被剥成空括号 `(: …)`
- 轴：C
- 位点（跨文件指认，触发面在本批）：`lib/algorithms/photometry/tests/p1phot/p1phot_fixtures.hpp:169-171` F3 几何注释「`d(g0)=0.4` → 双向唯一; `B(31.5,30.0) d(g0)=1.1<2` → 正向命中 g0 但…歧义; `E(15.0,50.0)` 距最近 Gaia `>2` → rejected_distance」——三处判据全部以 **2.0 px** 匹配半径为分界；该常数的代码侧写法是 `lib/algorithms/photometry/cpp/src/pc_api.cpp:137` `2.0,  // match_radius_px (: 收紧 3.0 -> 2.0)` 与 `:427` 同族，括注里只剩"(: …)"，无任何文档/条目号。
- 上位依据：负责人裁定的文档硬口径"常数须有推导"（项目记忆 Doc doctrine；仓内对应条款 `ENGINEERING_SPEC.md` §11 与 `AGENTS.md` §1.1「自检：交付前必须能回答"我这件事的规范依据是哪一份文档的哪一条"」）；`docs/algorithms/PHOTOMETRIC_FIT.md:9` 一行内给了 `c=4.685, mag_tolerance=3.0` 的冻结登记，但**无 match_radius 行**。检索式：`git grep -n "(\: " -- lib/algorithms/photometry/cpp/src lib/algorithms/photometry/tests/p1phot`（命中 `pc_api.cpp:127,137,145,354,417,427,435`、`spectrum_integrator.cpp:168`、`star_matcher.h:31`）、`git grep -n "match_radius" -- docs/algorithms/PHOTOMETRIC_FIT.md docs/science/PHOTOMETRY.md`（无登记行）。
- 现状：`mag_tolerance` 有 `docs/science/PHOTOMETRY.md:219-220` 专段（虽自陈不引文献）＋ `eng/packaging/config/defaults.json:540` 键；`match_radius_px` 两处都查不到，只在代码里以空锚注释声明"收紧 3.0 -> 2.0"（一次未留痕的改值）。
- 差在哪：一个直接决定"匹配/歧义/超距"三分类（进而决定参与定标的星集）的截断窗，其唯一"依据"是仓内已不可回溯的空括号注释。
- 后果：F3 的构造正确性、`rejected_ambiguous`/`rejected_distance` 两个诊断量、以及进 IRLS 的星集全部挂在 2.0 上；该值无登记面 ⇒ 任何一次再"收紧/放宽"都不触发合同检查，且本批夹具会静默失去判别力（若半径涨到 1.5 以下则 B 不再正向命中，若涨到 10 则 E 也进候选）。不改当前产物字节。
- 定级：S3（缺条文而非值错，指认面为注释/登记；无产物错值后果）
- 怎么算修好：在 `docs/algorithms/PHOTOMETRIC_FIT.md` 的常数表补一行 `match_radius_px`（含取值方式：像素尺度不变半径需要推导或与 FWHM 挂钩的理由），并 `eng/packaging/config/defaults.json` 增键（若它应可配置），然后把 `pc_api.cpp:137,427` 的空括注换成真实锚。必须同批改：F3 夹具的三处 `</>/2` 判据改为从同一常数派生（现写死在注释与坐标里），否则改半径只红不显。
- 置信：CONFIRMED（检索式与命中集全列；空括号注释逐字贴出）

## 5. `lib/algorithms/photometry/tests/p1phot/p1phot_gaia_stub.cpp`（98 行，整读完毕）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| `:55,:57,:60,:66,:85,:91,:95` | 桩返回码 `-1 / -2 / -3 / 0` | 见左 | 参数非法 / 故障注入 / 分配失败 / 成功 | 结构性不适用 | 桩与 SUT 之间的协议实现选择，不进科学语义；实测 SUT 只判 `rc != 0`（`pc_api.cpp:305`）并统一 `return -3`（`:310`），故 -1/-2/-3 对被测侧不可分辨 ⇒ 三个负值之间无科学含义。注释 `:57`「→ 被测 rc=-3 路径」把桩码与被测码混写，登记为 CR-02-19 的子项 | — | CONFIRMED |
| `:61` | `sizeof(GaiaSpectrumStar) * n` | — | 星记录数组尺寸 | 结构性不适用 | 数组维度/分配量，非科学量 | — | CONFIRMED |
| `:62,:78,:80,:81` | `cfg.wl_count` 作为谱行 stride | 343（来自 `FakeClientConfig` 默认） | 逐星光谱缓冲区行宽 | 文献值 | Gaia DR3 §20.12.4 经 `docs/science/PHOTOMETRY.md:308`；**本批内该网格的第 3 处独立字面量**（另两处：`p1phot_field_stub.hpp:24-25`、`p1phot_fixtures.hpp:78,200`），见 CR-02-3/CR-02-15 | — | CONFIRMED |
| `:77-81` | `n_copy = min(spectrum.size(), wl_count)`；尾部补 `0` | 0 | 谱长不足时零填充、超长时丢尾 | 结构性不适用（值）＋静默降级（行为） | 补 0 本身是实现选择；风险是形状不符被静默修正而非报错，与 `p1phot_field_stub.hpp:39-42` 的常数谱回退同族（已登记 CR-02-4） | — | CONFIRMED |
| `:42` | `gaia_client_create(const char* data_dir)` 的入参 | 忽略 | 不打开任何真实库，返回空配置 client | 结构性不适用（该形参本身无值）| 桩的定位是外部域替身（`:1-6` 自述），不加载容器 ⇒ `wl_start/step/count` 只能来自桩默认（`:92-94`），这正是 CR-02-3 的根：测试里 `get_spectrum_params` 的值与容器无关 | — | CONFIRMED |
| `:50,:51` | `ra/dec/radius_deg/mag_low/mag_high` | 全部忽略 | 查询条件不参与桩的返回集 | 待确认 | 无条文规定"桩可忽略查询条件"；`docs/algorithms/GAIA_QUERY.md:14` 把"查询锥 + 星等窗"定义为该接口的**语义本体**，桩把它整条抹掉。保守方向：桩至少实现 mag 窗与锥内计数。影响范围：见 CR-02-18（生产自适应锥搜的早停分支在测试面不可达） | CONFIRMED |
| `:58` | `n = cfg.stars.size()` | 4–24（夹具实际） | 返回星数 | 结构性不适用 | 由夹具规模派生；上限 24 的成因见 CR-02-14 | — | CONFIRMED |
| `:16-18` | `struct GaiaClient { FakeClientConfig cfg; };` | — | 完成 opaque tag | 结构性不适用 | 类型完整化，无数值 | — | CONFIRMED |
| `:70-76` | `ra/dec/magG/flux_min/flux_mul` 五字段赋值 | — | 记录填充 | 结构性不适用 | 已逐字段与被替身头核对：`lib/infrastructure/gaia_xpsd_client/src/gaia_client.h:56-66` 的 `GaiaSpectrumStar` **恰为这 5 个字段**（`double ra,dec,magG; float flux_min,flux_mul;`）⇒ 无"未初始化字段被 SUT 读到"的洞（我本来要报这一条，核对后撤销） | — | CONFIRMED |
| `:88-96` | `get_spectrum_params` 的形参与返回 | — | 网格回吐 | 结构性不适用 | 签名与 `gaia_client.h:131-136` 逐参数一致（`int* out_start_nm, out_step_nm, out_count`）；`:38` 的"签名与 gaia_client.h 一致"经核对**成立** | — | CONFIRMED |

### findings

### CR-02-18 · 桩忽略全部查询条件，使生产"自适应星等锥搜"的常走分支（首轮 `n_gaia>=2000` 即停）在整套 p1phot 里永不可达
- 轴：D（主轴）＋R
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_gaia_stub.cpp:48-59`
  `int gaia_client_cone_search_with_spectrum(GaiaClient* client, double /*ra*/, double /*dec*/, double /*radius_deg*/, double /*mag_low*/, double /*mag_high*/, …) { if (!client\|…) return -1; … const int n = (int)cfg.stars.size(); *out_count = n; }`
- 上位依据：`docs/algorithms/GAIA_QUERY.md:14`「给定查询锥（球心 `q=(RA_q, Dec_q)`，角半径 `ρ`…）与星等窗…」＝该接口的语义本体；`docs/algorithms/PHOTOMETRIC_FIT.md:206`「自适应锥搜 `mag_max_arr{12..16}`×5 :977-1006」。实现：`lib/algorithms/photometry/cpp/src/pc_api.cpp:290-291` 注释「从 mag_max=12.0 开始, 若返回星数 < 2000 则增大 mag_max (13/14/15/16), 直到星数在 2000-10000 范围或达 16.0 上限」，`:292` `static const double mag_max_arr[] = {12.0, 13.0, 14.0, 15.0, 16.0};`，`:297` `for (int i = 0; i < 5; ++i)`，`:315-318` 退出条件 `if (n_gaia >= 2000 \|\| i == 4) break;`。
- 现状：桩无视 `mag_low/mag_high`（与 ra/dec/radius 一起被注释掉），无条件返回注册的全部星；夹具实际星数 4–24（`p1phot_fixtures.hpp:120` 的 6×4 格点上限 24；调用点实参 4/6/8/12/20/48→24）。⇒ 在测试面 `n_gaia` 恒 <2000，`i==4` 才退出：**5 轮全跑满**，早停分支一次都不执行。
- 差在哪：生产输入下（真实 XPSD 分片含千万级源，`docs/algorithms/PHOTOMETRIC_FIT.md:219` 记 `11 272 905` 颗）首轮 `mag_max=12.0` 通常即满足 `n_gaia>=2000` 而 break；也就是说，**生产常走的那一支恰是测试从不走的那一支**，而测试反复走的是"查满 5 轮、每轮 free 后重查"这一支（`:321-323` 释放后继续循环）。
- 后果：（1）`n_gaia>=2000` 这条 2000 阈值的退出语义、以及"早停后不再 free/不再重查"的指针生命周期约定，在门禁里零覆盖——若早停分支被改坏（例如 break 前 free 了结果，或在第 1 轮就 break 前未赋值 `spec_stars`），测试不会红；（2）2000–10000 的目标域与 `MAX_STARS_RESULT=200000` 截断（`GAIA_QUERY.md:156/204`：「不返回不完整星表」）在测光侧完全脱钩。注：入参 `mag_max` 被 `mag_max_arr` 覆盖这一点已由 `PHOTOMETRIC_FIT.md:224-225,247-248`（DISP-PHOT-005）登记，**本条不重复该项**，只报"测试面分支不可达"这一角。
- 定级：S2（覆盖缺口＋与设计点名的能力不一致；不改当前产物字节，故未定 S1）
- 怎么算修好：让桩按 `mag_low/mag_high`（及锥内计数）过滤 `cfg.stars`，并加一条"首轮即达阈即停"的用例——阈值不该由夹具塞 2000 颗星实现，而是把 2000 提成可注入常量（或在桩里提供"本轮返回数"决策表）。必须同批改（防"改对反而判红"）：`p1phot_tests_negative/oracle/properties` 的期望值目前默认"桩返回全部注册星"，桩一旦开始过滤，夹具的 WCS 中心/半径/mag 窗必须同时进入期望侧生成逻辑，否则整批用例齐红。
- 置信：CONFIRMED（桩体、SUT 循环、退出条件、夹具规模上限四者逐字读到，并已枚举全部调用点的星数）

### CR-02-19 · 桩注释把"被测侧后果"写成"桩契约"，且 `fail_mode` 的实际值域宽于登记
- 轴：C
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_gaia_stub.cpp:57`
  `if (cfg.fail_mode != 0) return -2;  // 锥形搜索失败注入 → 被测 rc=-3 路径`
  对照 `p1phot_gaia_stub.hpp:41` `int fail_mode = 0;      // 0=ok, 1=cone search 失败注入` 与 `:8-10` 的三行契约
- 上位依据：无条文规定桩的返回码取值（`git grep -n "fail_mode" -- docs` 零命中；桩契约只在这两个文件内部自述）。
- 现状：实现接受**任意非 0**（`!= 0`），注释只登记 `1`；实际调用点唯一：`p1phot_tests_negative.cpp:157` `stub::set_fail_mode(client, 1);`。同时 `.hpp:10`「返回 0 星 → 被测退化路径 (scale=1.0, records reason=5)」把被测侧 `out_match_reasons` 的取值（`star_matcher.cpp:469-470` 的 `=3`／`:601` 邻域的 reason 语义）写进桩的契约段——桩本身不产 records。
- 差在哪：契约注释与实现值域不一致（注释窄、实现宽），且注释混写两层的因果；被测侧 reason 的取值在 `.hpp` 里被写成 5，而该路径实际写入的是 3（"无效/未进入一致集"）——按 `star_matcher.cpp:466-470` 的赋值。
- 后果：仅认知面：读者按注释以为只支持 `1`、并可能按 `.hpp:10` 去断言 reason=5（该断言若被写出会直接判红，因而属"注释误导"而非"误绿"）。
- 定级：S3
- 怎么算修好：注释改为与实现同值域（"非 0 即注入"）或把实现收紧为 `== 1`（与登记面一致，二选一，不得两边走）；`.hpp:10` 删去 `records reason=5` 或改成本层真实可断言的事实（`out_count=0`）。必须同批改：若收紧为 `==1`，`set_fail_mode` 的非法值要有显式失败，否则又成新的静默分支。
- 置信：PARTIAL（reason=5 vs 3 的判断只读了 `star_matcher.cpp` 的相关片段，未穷举 reason 全部赋值点；值域与注释的矛盾部分为 CONFIRMED）

---

## 6. `lib/algorithms/photometry/tests/p1phot/p1phot_gaia_stub.hpp`（57 行，整读完毕）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| `:42` | `wl_start` 默认 | `336` | 桩自报的谱网格起点 nm | 文献值 | `docs/science/PHOTOMETRY.md:308`（DR3 §20.12.4 原文）；**本批第 4 处独立字面量**（`field_stub.hpp:24`、`fixtures.hpp:78,200`、`gaia_stub.cpp:92`）⇒ 见 CR-02-3 与 CR-02-15 的收敛要求 | — | CONFIRMED |
| `:43` | `wl_step` 默认 | `2` | 谱网格步长 nm | 文献值 | 同上 | — | CONFIRMED |
| `:44` | `wl_count` 默认 | `343` | 谱点数 | 文献值 | 同上；自洽式 `336+2×342=1020`（`docs/algorithms/PHOTOMETRIC_FIT.md:218`） | — | CONFIRMED |
| `:41` | `fail_mode` 默认 | `0` | 正常返回（不注入故障） | 待确认 | 默认值本身合理但**注释值域与实际值域不一致**（注释 `0=ok, 1=…`，实现 `!=0`），见 CR-02-19 | 保守方向：默认 0（不注入）是安全侧；影响范围仅故障注入用例 | CONFIRMED |
| `:36` | 注释 `n=343 (与 wl_start/step 匹配)` | 343 | 谱长说明 | 文献值 | 同 `:44`；该注释是四处字面量之间唯一的"互相约束"声明，但无任何代码断言三者互洽（改 `wl_count` 不触发任何一致性检查） | — | CONFIRMED |
| `:34,:35` | `double ra, dec, magG; float flux_min, flux_mul;` | — | 记录字段与精度 | 结构性不适用 | 字段类型与被替身头逐字一致（`gaia_client.h:56-66` 同为 3×double + 2×float），且与 `docs/contracts/DATA_SEMANTICS.md:678` 的 uint8 解码式（float 存 min/mul）一致 ⇒ 精度选择有来源 | — | CONFIRMED |
| `:8,:9,:10` | `fail_mode=0 / =1 / 返回 0 星` 三态 | 0 / 1 | 桩行为模式表 | 结构性不适用 | 模式枚举基数；第 3 态"返回 0 星"的实际触发在 `gaia_stub.cpp:58-60`（由 `cfg.stars` 为空实现，非独立模式码） | — | CONFIRMED |
| `:13-14` | `free()` ↔ `malloc()` 配对契约 | — | 内存归属 | 结构性不适用 | 该声明经核对**成立**：SUT 确实 `free(spec_stars)/free(spectra_buf)`（`pc_api.cpp:307-309,321-323`），桩用 `std::malloc`（`gaia_stub.cpp:61-62`）⇒ 无 new/delete 与 free 混用（注：`create()` 用 `new GaiaClient`、`destroy()` 用 `delete`，与 out_* 是两条独立链，不交叉 ✓） | — | CONFIRMED |
| `:29` | `using GaiaClient = ::GaiaClient;` | — | opaque 句柄别名 | 结构性不适用 | 类型别名，无数值 | — | CONFIRMED |

### findings

### CR-02-20 · 桩的 `n==0` 分支不写 `*out_stars`/`*out_spectra`，与被替身真实实现的约定是否一致未经核对（当前被 SUT 的预置 nullptr 掩盖）
- 轴：R
- 位点：`lib/algorithms/photometry/tests/p1phot/p1phot_gaia_stub.cpp:58-60`
  `const int n = (int)cfg.stars.size(); *out_count = n; if (n <= 0) return 0;`
- 上位依据：`lib/infrastructure/gaia_xpsd_client/src/gaia_client.h:103-109` 的 out_* 语义（头内未写"0 结果时必须置 NULL"，故此项在条文面零提及；检索式 `git grep -n "out_stars" -- lib/infrastructure/gaia_xpsd_client/src/gaia_client.h docs/algorithms/GAIA_QUERY.md`）。
- 现状：桩在 0 星时只写 `*out_count`，两个 out 指针保持调用方传入前的值。被测侧安全：`pc_api.cpp:292-293` 声明处即 `= nullptr`，且 `:326` 判据是 `if (n_gaia <= 0 || spec_stars == nullptr)`，`free(nullptr)` 合法 ⇒ **当前无受害者**。风险面：任何直接调该接口的其它消费者（例如 `lib/infrastructure/gaia_xpsd_client/src/module_entry.c:706` 的 legacy 调用路径）若在栈上声明未初始化指针并只信 out_count，桩与真实实现可能给出不同结果。
- 差在哪：替身的契约里没写"0 结果时 out 指针如何处置"，实现也不写，读者只能靠被测侧的巧合才不炸——这是替身保真度缺口，不是当前 bug。
- 后果：仅当 `pc_api` 之外的调用方改用本桩时才会暴露（当前跟踪面内 p1phot 只有 pc_api 一个消费者）。不改产物字节。
- 定级：S3
- 怎么算修好：桩在成功路径的两个出口都显式 `*out_stars = (n>0)?stars:nullptr; *out_spectra = (n>0)?buf:nullptr;`，并在 `.hpp` 的契约段补一行"0 结果 ⇒ out_* 置 NULL"；同批需核一次真实 `gaia_client.c:2643` 的 0 结果分支是否同样置 NULL（不核就写，可能把桩改得比真实实现更"干净"，反而掩盖真实行为）。
- 置信：PARTIAL（缺 `gaia_client.c:2643` 0 结果分支的实际写法——本批预算内未读该函数体；已确认 SUT 侧不受影响）

（本文件另有一条撤销的自拟指控，如实登记以免下批复用：我一度怀疑桩漏填 `GaiaSpectrumStar` 字段导致堆残留被 SUT 读到（`:73-74` 的注释正是在警告这件事），核对 `gaia_client.h:56-66` 后确认该结构恰有 5 个字段且全部被赋值 ⇒ 指控不成立。）

---

## 收工基线 `git status --porcelain` 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工逐字一致 ⇒ 本批零写入被审仓库（未执行任何构建/ctest/门禁/`eng/**` 脚本，未 `import` 仓库 Python，未改 git 配置）。

<!-- STATUS-CLOSE -->

---

## 本批最高危三条

只挑，不复述证据（详见各条编号）。

1. **CR-02-1**（S1，D＋C）—— Phase1 测光生产库自带文本 logger 直写 stderr，本阶段诊断完全不进 `<output_dir>/logs` 运行日志工件；且 `CHK-LOG-SYS` 的 R3 检测器在结构上看不见这一类违规（只匹配硬编码 `logs/` 路径），`log_landing` 台账已 8/8 满、photometry 零登记。这是"§7.3 完整运行日志/同源"对外主张在一个生产阶段不成立，且**门禁无法自我发现**。
2. **CR-02-16**（S2，D）—— `MAD=0`（`S=0`）退化分支把 IRLS 应拒的离群星直接计为内点并输出 `sigma_residual=0`，帧级质量指标因此**偏乐观**（偏绿方向已定），`sigma_mag` 为 0 顺 SNR 传递链走；条文只规定了 location/scale/sigma 三件事、未规定内点集合构成，而本仓唯一发现该问题的地方（`p1phot_fixtures.hpp:131` 注释）选择绕开而非检验。
3. **CR-02-18 + CR-02-3**（各 S2，D）—— 同一根因的两面：谱网格在生产侧"容器自报"、在测试侧写成四处独立字面量，且桩忽略全部查询条件 ⇒ 生产常走的分支（首轮 `n_gaia≥2000` 早停、非 336/2/343 网格）在门禁里永不可达。表现为"门全绿"，实质是该阶段的对外证据与生产输入域脱钩。

## 我读不动或需要实跑才能定的

| # | 悬而未决的问题 | 为什么本批定不了 | 该跑什么（只读节点不跑，交前台/后续实跑节点） |
|---|---|---|---|
| 1 | `p1_qf_oracle`（饱和位接线的唯一锁）究竟是不是**已注册且能构建**的门？ | 跟踪面给出互斥信号：`eng/ci/prod_wiring_baseline.json:48` 记 `unbuildable_test:…p1_qf_oracle.cpp`，而 `eng/tests/unit/CMakeLists.txt:1796` 又 `add_executable(w34_p1_qf_oracle …)` 并链 `astrocs_phase1_photcal`；我只读不构建 | `cmake --build build --target w34_p1_qf_oracle` 后跑该可执行（按 AGENTS §3 套 `mem_guard.py --max-rss-gb 4`），并查它是否出现在 ctest 注册表；结论决定 CR-02-12 是否升 S1 |
| 2 | `S=0` 退化域在**真实输入**下的可达概率（两条 r 逐位相等的星） | 属数据统计，不是读代码能定的；CR-02-16 因此停在 S2 | 在 49 帧 M42 / M42-E2E 一轮的生产 F_syn 与 PSF 通量上统计 `log10(F_instr/F_syn)` 的逐位重复率（须套 mem_guard，上限 20 GB）；同时确认 `compute_f_syn_cached_xpsd` 对逐字节相同谱是否给出逐位相同的 F_syn |
| 3 | `eng/packaging/config/defaults.json:544` 的 `source: "docs/science/PHOTOMETRY.md:30"` 行号漂移是登记错，还是 PHOTOMETRY.md 表格在登记后移动过 | 需要按被钉提交做行级回溯，本批只核到"HEAD 上该锚不指向 `mag_tolerance` 行（实在 :34）" | `git log -L28,40:docs/science/PHOTOMETRY.md` 与 `git log -L540,546:eng/packaging/config/defaults.json` 对齐时间序；若属"登记在先、表格移动在后"，则只需订正锚而不必重裁取值 |
| 4 | 真实 XPSD 分片的 `flux_min/flux_mul` 量级，用于判 F4 夹具量化参数（`:207-208`）是否有代表性 | 数据在只读数据集 `gaia/GaiaDR3SP/`（gitignore，非跟踪面），且读它要跑外部工具 | 前台读 `gaia/GaiaDR3SP/gdr3sp-1.0.0-01.xpsd` 的头部/逐星量化参数样本，与 `1.0e-15±1.0e-16 / 5.0e-18±1.0e-18` 对量级；结论决定该行从"待确认"往哪一档落 |
| 5 | `gaia_client.c:2643` 的 0 结果分支是否把 `*out_stars/*out_spectra` 置 NULL | 本批预算内未读该函数体（CR-02-20 停在 PARTIAL 的唯一原因） | 读 `lib/infrastructure/gaia_xpsd_client/src/gaia_client.c:2643-2760` 的 0 结果返回路径 |
| 6 | `LOG_DEBUG` 的"避免日志 I/O 主导耗时"性能说法是否有实测支撑 | 需要基准跑；本条只作为注释可信度登记，未列 finding | 按 `docs/algorithms/GATES_AND_TOLERANCES.md` 的测法，在 `PC_ENABLE_DEBUG` 开/关两档跑 star_matcher 基准对比 |

### 派单与清单的一致性（§5.5）

清单 6 个文件与本批实际读取的 6 个文件一一对应，无缺项、无多余项；派单文字（"lib/algorithms/photometry 第 2 段"）与清单相符。开工基线为 `?? ACSD整治工作包_AUDIT-06.zip`、`?? site/`，与派单 §1 预期一致。**一处口径差异需指出**：派单说"6 个文件"，但其中 5 个是测试面（`tests/p1phot/`）、只有 `log_macros.h` 在生产编译面内；因此本批的 §2.5 表绝大多数行的正确落点是"结构性不适用/夹具自订"，真正带科学语义且影响产物的数值集中在三处——336/2/343 网格族、`mag_tolerance=3.0` 与冻结门 3/2、以及 `S=0` 退化域的行为，本批已逐条给出处置与依据锚。
