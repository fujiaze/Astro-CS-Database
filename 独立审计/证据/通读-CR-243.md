# 代码通读审查报告｜CR-243

**批次号**: CR-243  
**审查轴**: S（支撑）/ D（设计符合）/ C（上下级一致）/ R（运行时正确）  
**源码基线**: c8f64e9a  
**审查日期**: 2026-09-26  

---

## 文件清单与进度

### lib/infrastructure/scheduler/src/module_adapters.cpp:4501-6000
**读数**: 完整通读至行 7363+（本节代码延续超出 6000 行边界）  
**功能域**: `p1_op_wcs`（WCS 解算，显式配置路径 + 真实 ipv 求解链）、`p1_op_photometry`（测光归一化与空间增益拟合）、`p1_op_noise`（SNR 噪声模型与科学 SNR）、辅助函数 `p1_noise_cfg_apply`、`p1_noise_model_for_frame`、`p_variance_audit_missing_fields`、`p1_variance_audit_block`、`p1_op_drizzle`（drizzle 堆栈配置解析）

**数值处置表**（逐行数值的五档处置）：

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|----------------|---------|--------|---------|------|--------|--------------------------|------|
| module_adapters.cpp:4506 | kP9AsecPerUmPerMm | 206.265 | 板尺度常量：`(180×3600)/π × 1e-3` 将 XPIXSZ(μm)/FOCALLEN(mm) 转为角秒/像素 | 文献值 | ipv_select.cpp:57 IPV_ARCSEC_PER_UM_PER_MM；DATA_SEMANTICS §18.1 | — | CONFIRMED |
| module_adapters.cpp:4709 | (隐式阈值) | 1e-6 | WcsTan roundtrip 容许误差（线性 WCS，无 SIP）单位 px | 公式导出 | SCI-WCS-001 §3a/§5a；契约声明 | — | CONFIRMED |
| module_adapters.cpp:4718 | (隐式阈值) | 1e-9 | B2-A1 绝对前向交叉门：与独立 gnomonic 参考解的角距上限，单位 deg | 公式导出 | B2-A1 绝对正确性门注释；舍入误差分析 | — | CONFIRMED |
| module_adapters.cpp:5327 | P1_PHOT_MAX_SIGMA_DEX | 1.0 | 拟合散度 QA 上限，单位 dex（2.5 mag） | 实验标定 | SCI-PHOT-001 §16.5；GAP_AUDIT §9.29 | — | CONFIRMED |
| module_adapters.cpp:5328 | P1_PHOT_MAX_SPREAD_DEX | 0.02 | 组内帧间一致性上限，dex（≈0.05 mag 峰峰） | 实验标定 | GAP_AUDIT §9.49 定案 2：负责人判据 | — | CONFIRMED |
| module_adapters.cpp:5471 | kDefaultSpatialGainOrder | 2 | m(x,y) 低阶乘性空间增益默认阶数（钳制在 [0,2]） | 结构性不适用 | SCI-PHOT-001 §16.1④⑤；冻结配置面 | — | CONFIRMED |
| module_adapters.cpp:6794-6801 | sci_cfg.* | 0.0（多数） | SNR 科学配置可选参数默认值（gain/read_noise/ZP 等） | 结构性不适用 | P8-SNR-LINUX 注释：缺省 = 未知增益时的上界口径 | — | CONFIRMED |

**小结**: 本文件段共识别 7 处关键数值常数/阈值，全部完成五档处置标注。其中 206.265 为权威常量硬编码（与求解器同值），1e-6/1e-9 为 WCS 契约门限（可失败的非恒真门），1.0/0.02 为测光拟合 QA 门（来自 GAP_AUDIT 实验标定），kDefaultSpatialGainOrder=2 为结构性常数（不进科学语义，仅控制通道开关）。

---

## §2 数值处置详情

### 2.1 p1_op_wcs（行 4609–5120）

该函数实现 Phase1 WCS 初始解算，包含两条路径：
- **显式配置路径**（行 4629–4776）：调用方给出 8 个 WCS 参数时，做 roundtrip 与前向交叉绝对门校验，不调用真实求解器，写 `wcs_source="explicit_config"`
- **真实求解路径**（行 4777–5119）：使用 Gaia 星表 + StarDetector + ipv_solve_from_memory_with_callback_d 进行真实天测解算

#### Finding CR-243-01 · 显式 WCS 路径的像素原点桥接修正已在 explicit 与求解器双路径统一应用
- **轴**: C（上下级一致）
- **位点**: module_adapters.cpp:4677–4678, 5003–5004
- **上位依据**: SCI-WCS-001 §3a/§5a；文档明确「内部 0-based 数组下标」→「FITS 1-based」需施加恰好一次 xp = x + kP1FitsPixelOrigin
- **现状**: explicit_config 路径（4677）与 ipv 求解路径（5003）均注入 `const double xp = x + kP1FitsPixelOrigin;`，并在注释中标明 M1a-C-003 修复点；samples[] 的 (x,y) 声明为「0-based array index (index-is-center)」
- **差在哪**: 修复前双路径均缺桥接 ⇒ 所有 (ra,dec) 与自带 (x,y) 标签恒差 1px，两道门对原点平移零判别力（注释自陈旧缺陷偏差 ~5.6e-1 deg）
- **后果**: 若回退到此状态，roundtrip 门与 forward_cross 门将失去鉴别 1px 系统偏移的能力，导致产品 WCS 存在恒定 1px 偏而不自知
- **定级**: S2（缺运行时触发路径的具体数据验证，但逻辑推断清晰）
- **怎么算修好**: 双路径均保留桥接且注释保持一致；消费方（如 drizzle）不得再叠加一次 +1
- **置信**: PARTIAL（缺实际运行对比数据，需查看 M1a-C-003 任务回执）

#### Finding CR-243-02 · Gaia 星表装载完整性检查强化（file_count == entry_count && load_failed==0）
- **轴**: R（运行时正确）
- **位点**: module_adapters.cpp:4887–4894
- **上位依据**: FAILCLOSED-01；根因 run/WCS-DETERMINISM-01/REPORT.md §1.2：星表装载失败 shard 被静默丢弃 ⇒ 暗 shard 里最亮星不足 ⇒ iter_trans_solve 全败但原因不可诊断
- **现状**: 客户端增加双重断言：① gaia_files <= 0 拒绝；② gaia_load_failed != 0 || gaia_files != gaia_entries 拒绝
- **差在哪**: 修复前 file_count > 0 即可创建 client，即使部分 shard 装载失败；此处修复已到位
- **后果**: 修复前会在地址受限时静默降载星表并返回虚假成功指针；修复后 fail-closed，错误可诊
- **定级**: S1（可直接导致产物错值或静默失败）
- **怎么算修好**: 维持当前双重断言；测试需覆盖 shard 装载失败场景
- **置信**: CONFIRMED（注释中已有根因追溯与修复说明）

#### Finding CR-243-03 · IPv 求解失败原因归责克制（不替求解器编造 parity/尺度闸门拒止）
- **轴**: S（支撑）
- **位点**: module_adapters.cpp:4971–4982
- **上位依据**: OPEN_QUESTIONS OQ-10 指出的误报问题：早先把「星表查询返回 0」误报成「被 parity/尺度闸门拒绝」
- **现状**: 判词只陈述 solver 返回失败 + 求解器自报 error_msg，不替求解器归因到具体失败点
- **差在哪**: 修复前把多种失败点混为一谈；修复后仅转述求解器自报原因
- **后果**: 修复前误导调试方向；修复后可准确定位至求解器日志
- **定级**: S2（调试误导而非直接错值）
- **怎么算修好**: 维持克制归责；下游如需进一步诊断应查求解器日志
- **置信**: CONFIRMED（注释中有明确问题编号与修复依据）

---

### 2.2 p1_op_photometry（行 5122–6357）

该函数实现测光归一化：从 p1_sources.json 读取星点 → 测量通量 → 拟合 k_photo（Tukey-IRLS）→ 施加 I_photo = k_photo·I_cal → 写 photoapplied 产物与 provenance。含逐帧判决框架（FAILSEM-01）。

#### Finding CR-243-04 · F-INSTR-CONFORM-FIX：F_instr 唯一合法域 = PSF 拟合域而非检测盒和
- **轴**: D（设计符合）/ S（支撑）
- **位点**: module_adapters.cpp:5563–5615
- **上位依据**: SCI-PHOT-001 §9a「星点通量来自 PSF 拟合域（PSF.md）」+ SCI-PSF-001 §2/§5「flux = 2πA·sxsy/3」
- **现状**: 修复前取 srcs[].flux = 检测器 5×5 正性截断盒和 (m00)，并把每颗星标为 status=0（未区分拟合有效域）；修复后按 star_id 关联 p1_psf.json 的 psf_params 行取 PSF 域解析通量，psf_status!=OK 的星跳过（不回退盒和）
- **差在哪**: 盒和能量份额依赖 seeing，不是测光量；修复后严格限定在 PSF 拟合有效域
- **后果**: 修复前 k_photo 混入 seeing 依赖的系统偏移；修复后 k_photo 仅反映仪器常数
- **定级**: S1（直接影响测光标度的物理意义）
- **怎么算修好**: 维持 PSF 域过滤；确保上游 star-psf 节点正确写入 psf_params.psf_status
- **置信**: CONFIRMED（注释中有完整的修复前缺陷描述与修复方案）

#### Finding CR-243-05 · FAILSEM-01：逐帧判决语义分流（环境失败全局中止 vs 帧失败不影响其他帧）
- **轴**: R（运行时正确）
- **位点**: module_adapters.cpp:5289–5304, 5726–5740
- **上位依据**: ASTROCS_DESIGN §4.2「该步不可用时产品显式记录 degraded_reason 并 fail-closed」；SCI-PHOT-001 §1「门只有一个 = 单帧标定是否可信，与其它帧无关」
- **现状**: 定义 P1FrameVerdict 结构体，status ∈ {ok, fail}；fit 失败返回 fr.failure_scope 判断：kEnvironment ⇒ fail_global（中止整批）；其他 ⇒ mark_frame_fail（仅该帧 fail）
- **差在哪**: 修复前帧失败可能静默降级为 degraded_reason 或阻塞整批；修复后三态可辨（fail ≠ degraded ≠ closed）
- **后果**: 修复前要么过度降级（把帧失败伪装成全局降级），要么错误阻塞（一帧失败拖垮整批）
- **定级**: S2（影响产品数量统计与后续流程决策）
- **怎么算修好**: 维持 failure_scope 判断逻辑；下游按 frame_status[].status 逐帧判定消费
- **置信**: CONFIRMED（注释中有详细的失效语义树与负责人裁决引用）

#### Finding CR-243-06 · PHOT-MXY-01：低阶乘性空间增益 m(x,y) 的完整审计面落盘
- **轴**: D（设计符合）
- **位点**: module_adapters.cpp:5368–5447, 6097–6106
- **上位依据**: SCI-PHOT-001 §16.1④⑤「log10 m(x,y) = −Σ m_coef[j]·B_j」；ASTROCS_DESIGN §4.2「可核对性」
- **现状**: P1FrameScale 结构体含系数/规范/基函数/残差/可辨识性/退化原因；json lambda 输出 spatial_gain 块；photscale_detail[<frame>].spatial_gain 与顶层 photscale_fit 同源同值
- **差在哪**: 修复前 m(x,y) 系数只活在内存或未落盘；修复后完整审计面（含拟合样本）进 p1_phot.json
- **后果**: 修复后消费者可独立复算「加/不加 m(x,y) 的逐星残差」，区分 m 吸收的是真实响应还是背景梯度
- **定级**: S2（可核对性缺失，但不直接导致错值）
- **怎么算修好**: 维持 spatial_gain 完整字段；门禁侧 chk-provenance 核对其 presence
- **置信**: CONFIRMED（注释有完整的字段清单与消费方用途说明）

#### Finding CR-243-07 · PASSBAND-IDENTITY-GATE-01：通带身份组级一致性（帧间不得混用不同曲线）
- **轴**: C（上下级一致）
- **位点**: module_adapters.cpp:5448–5460, 5819–5850
- **上位依据**: docs/science/PHOTOMETRY.md §2a.4/§2a.5「比较 sigma_residual 必须声明所用模型通带」；config contract「滤镜名解析 = 字节精确、无别名」
- **现状**: passband_identity_valid 由首帧落定（filter_key/n_points/wl_min/max 等）；后续帧逐项比对，不等 ⇒ mark_frame_fail("passband_identity_inconsistent")
- **差在哪**: 修复前可能静默混用不同通带 ⇒ sigma_residual 比较失去共同参照；修复后强制同组同一曲线
- **后果**: 修复前跨帧合并的测光零点不可比；修复后保证同组产品基于同一合成 F_syn
- **定级**: S2（影响跨帧可比性，但不致单帧错值）
- **怎么算修好**: 维持 passband_identity 一致性格式；下游按 declared_filter_passband 核对
- **置信**: PARTIAL（缺真实帧混用案例的测试覆盖证据）

#### Finding CR-243-08 · 组间 k 散度门删除（帧间一致性非门禁）
- **轴**: D（设计符合）
- **位点**: module_adapters.cpp:5974–6014
- **上位依据**: SCI-PHOT-001 §10「不可接受变化」；GAP_AUDIT §9.49 定案 2：帧间独立，跨帧 k 不同正常；PhotometricMosaic 无任何「拒绝帧」跨帧一致性门
- **现状**: 删除原 fail-closed 分支，保留 spread_dex 计算与 photscale_spread_warn 提示（人工审阅用）
- **差在哪**: 修复前 L4 真实数据 t2_m1 两帧 k 散度 0.0311 dex = 0.078 mag > 0.02 dex ⇒ photometry_applied=false（测光归一化完全不执行）；修复后帧间散度只作报告字段
- **后果**: 修复前进件率大幅损失（合理帧被误杀）；修复后恢复生产测光归一化
- **定级**: S1（直接导致产品级 feature 缺失）
- **怎么算修好**: 维持删除分支；仅保留 warn 提示
- **置信**: CONFIRMED（注释有 E2E 真实数据实证与 PhotometricMosaic 佐证）

#### Finding CR-243-09 · FREF-BASELINE-001：固定参考星等的绝对共同基准（6 等星 via Gaia G）
- **轴**: S（支撑）
- **位点**: module_adapters.cpp:6920–6999, 7160–7182
- **上位依据**: GAP_AUDIT §9.49 定案 7「直接用 6 等星（或一个数值表示比较正常的星等来做基准就行）」；WEIGHT-SCI-001 配对性定理
- **现状**: ref_mag 默认 6.0，从 p1_phot.json 读取每帧 zero_point_valid=true 的 ZP_syn ⇒ 计算 ZP_k = ZP_syn - 2.5*log10(k_photo) ⇒ F_ref,k = 10^(-0.4*(ref_mag - ZP_k))
- **差在哪**: 修复前使用块级检出通量中位数 ⇒ 随帧集漂移，帧间不可比；修复后固定星等为物理公共锚
- **后果**: SNR_f 定义在统一 F_ref 下，w = SNR_f²/F_ref² 的配对性成立（Convention A）
- **定级**: S1（影响 SNR 的绝对口径与跨帧可比性）
- **怎么算修好**: 维持 fixed_magnitude 路径；头部 ASTROCS_REFERENCE_FLUX 写 flux_common（同波段同星场恒值）
- **置信**: PARTIAL（缺与旧口径的对比实验数据）

#### Finding CR-243-10 · P14-N-08/N-09：交付 SNR 样本真实性（解耦 psf.max_stars）
- **轴**: R（运行时正确）
- **位点**: module_adapters.cpp:7183–7230
- **上位依据**: P14-N-08「snr.max_sources = 交付样本上限」；P14-N-09「交付样本 = DATA-P1-SOURCES.sources 的全部测光有效源」
- **现状**: build_snr_rows 取自 sources[]（flux>0, fwhm_px>0），而非 psf_params（受 max_stars=5000 截断的子集）；snr_sample 注释明示「independent of psf.max_stars / psf_params」
- **差在哪**: 修复前 median_snr/frame_depth_m5_mag 由最亮≤5000 颗决定，系统性偏乐观；修复后使用全部可用源
- **后果**: 修复前深空 faint source 比例高的帧 SNR 偏高估计；修复后如实反映实际探测能力
- **定级**: S2（SNR 统计量的系统性偏差）
- **怎么算修好**: 维持 build_snr_rows 从 sources[] 取数；psf.max_stars 仅用于性能开关，不进 SNR 样本
- **置信**: CONFIRMED（注释清楚解释了冲突来源与解耦机制）

---

### 2.3 p1_op_noise（行 6359–7363）

该函数实现 SNR 噪声模型 v1（snr_noise_model_v1/v1_f64）与科学 SNR 计算，包含方差面生成、掩膜半径逐星适配、reference_flux_adu 确定（group_median/fixed_magnitude/config）。

#### Finding CR-243-11 · NOISE-MODEL-CANON-001：wrapper_phase1::NoiseModel 退役与 v1 升级
- **轴**: D（设计符合）
- **位点**: module_adapters.cpp:7040–7046
- **上位依据**: docs/science/NOISE_MODEL.md:71/165；ALG §13.1:120「生产符号唯一源」；SCI-NOISE-001 §5/§5a
- **现状**: 旧 wrapper 是退化子集（无掩膜/无 5σ裁剪/无饱和过滤/无 patch 网格/无平面场/无天空预算门）；本节点改用 snr_noise_model_v1（含自适应 patch 网格、逐星掩膜、饱和过滤、空间平面场）
- **差在哪**: wrapper 曾把整帧喂入 ⇒ 有星时整帧 MAD 偏差 +2.6%..+21.6%（超 SCI §7 的 2% 与 §11 的 5% oracle）；v1 全算法 ≤0.35%
- **后果**: 修复前 background/variance 系统性偏高；修复后达到设计精度
- **定级**: S1（直接影响权重 ivar 的准确性）
- **怎么算修好**: 维持 v1 调用；旧 wrapper 应标记 deprecated
- **置信**: CONFIRMED（注释有独立复现数据 3 seed 佐证）

#### Finding CR-243-12 · WIRING-W34-01：doc["noise"] 配置块的唯一消费点与未知键 fail-closed
- **轴**: C（上下级一致）
- **位点**: module_adapters.cpp:6391–6547
- **上位依据**: eng/packaging/config/defaults.json 的 noise.*14 键此前零消费点 ⇒ CHK-PROD-WIRING W3 红
- **现状**: p1_noise_cfg_apply 遍历 doc["noise"] 对象，逐键校验类型/值域，未知键 ⇒ rc=3 + 显式 err 消息；已知键写入 SnrNoiseModelConfig
- **差在哪**: 修复前 config 键存在但生产面不出现 ⇒ 声明了没消费；修复后每个键都有归属，未知键拒绝
- **后果**: 修复前用户以为配置生效实则静默忽略；修复后明确报错提示
- **定级**: S3（配置误解风险，不直接错值）
- **怎么算修好**: 维持 unknown key 拒绝；新增配置键时需同步更新 p1_noise_cfg_apply
- **置信**: CONFIRMED（注释列出 14 键完整词汇表）

#### Finding CR-243-13 · CHAIN-WIRE-ADAPT-01 / W3：掩膜通量标度换算（flux × data_scale）
- **轴**: R（运行时正确）
- **位点**: module_adapters.cpp:6686–6718
- **上位依据**: SCI-NOISE §5a「r_local = α_psf·sqrt((F(β−1)/(π α_psf² k σ_bg))^(1/β) − 1) 只在 F 与 σ_bg 同一标度时无量纲」
- **现状**: p1_sources.json 的 flux 由 star-psf 在 cleaned_<base>（ADU）上测得；若本帧数组为 photoapplied = α·ADU（α = data_scale ≈ 5.9e-17），则 flux_scaled = fl * data_scale 后再送入掩膜半径公式
- **差在哪**: 修复前直接传 ADU 面 flux 进 FP64 photoapplied 面 ⇒ F/σ_bg放大 1/α≈1.7e16 倍，半径一律顶到 rmax=60px（硬上界退化成操作默认值）
- **后果**: 实测 drizzle 路径四帧 mask_radius_p50 恒为 60.000000、mask_frac 0.199–0.445，而同一帧的 SNR 节点（cleaned 面、data_scale=1.0）给出 r_p50=4.84、mask_frac=0.018；§5a 设计意图「逐星半径由掩膜边缘残余面亮度 ≤ k·σ_bg 导出」完全失效
- **定级**: S1（直接影响掩膜面积与权重分布）
- **怎么算修好**: 维持 flux_scaled 换算；diag 记录 noise_mask_flux_scale 证明换算已生效
- **置信**: CONFIRMED（注释引 run/SCI-VAR-ZERO-01/REPORT.md §3.1 实测对比）

#### Finding CR-243-14 · WEIGHT-SCI-001：组内公共 F0 的两遍法 fallback
- **轴**: S（支撑）
- **位点**: module_adapters.cpp:6816–6919
- **上位依据**: reports/RELEASE-02/weight-sci-ruling.md「配对性定理：SNR_f = a_f·F0/σ_f ⇔ SNR_f²/F0² = a_f²/σ_f² = w_f」
- **现状**: (a) 首选 snr.reference_flux_adu 显式给出；(b) 次选两遍法——逐帧检出通量中位数 → 块级中位数 = F0；逐帧 F_ref 禁止（丢 a_f²）
- **差在哪**: 修复前可能用逐帧 F_ref（数据来源混杂）⇒ w 的配对性不成立；修复后全组同一 F0
- **后果**: w = SNR_f²/F0² 真正代表 a_f²/σ_f²，定权可解释
- **定级**: S2（定权理论的自洽性）
- **怎么算修好**: 维持两遍法逻辑；头部 manifest 写 reference_flux_source ∈ {"config","group_median"}
- **置信**: PARTIAL（缺实际配对的权重检验）

#### Finding CR-243-15 · DISP-NOISE-001/009：floor 注册表的互斥保护与 _free 成对释放
- **轴**: R（运行时正确）
- **位点**: module_adapters.cpp:7071–7078, 7112
- **上位依据**: PERF-P1 同批修复「无锁全局 map 改为互斥量保护 + 热路径零加锁」
- **现状**: nmc.rc == 3 || nmc.rc == -9 时调用 snr_noise_model_v1_free(&nm) 配对释放；并行体外归约避免竞态
- **差在哪**: 修复前无锁 map 可能竞态泄漏；修复后互斥量保护 + 任何非成功返回也 _free
- **后果**: 修复前长运行累积内存泄漏；修复后 RC 语义清晰（0 成功/1 退化/3 参数非法/-9 ABI 失配）
- **定级**: S3（资源泄漏，不影响单次运行正确性）
- **怎么算修好**: 维持 _free 成对；压力测试验证泄漏率接近 0
- **置信**: CONFIRMED（注释明确指向 PERF-P1 修复）

#### Finding CR-243-16 · SCI-NOISE-001 §5d：可观测量审计块落盘（var_diag → p1_stack.json → p1_final.json）
- **轴**: D（设计符合）
- **位点**: module_adapters.cpp:7365–7428
- **上位依据**: docs/science/NOISE_MODEL.md §5d:270-271「可观测量必须写入 provenance」
- **现状**: p1_variance_audit_block 将 var_diag 平铺写入 block（schema/clause/status/reason/degenerate/auditable/plane_used + required_fields/missing_fields）；p1_op_drizzle 消费 var_diag 写进 `<frame>/p1_stack.json`，p1_op_writer 读回写进 `<frame>/p1_final.json`
- **差在哪**: 修复前 var_diag 只汇入 man（从不落盘）⇒ 产品里搜不到任何一个§5d 可观测量；修复后四处同名（p1_stack/p1_final/variance_product_frames/manifest）
- **后果**: 修复后门禁侧可直接核验§5d 字段齐全性；消费者无需额外 fetch
- **定级**: S2（可核对性缺失，但不直接错值）
- **怎么算修好**: 维持 audit block 落盘路径；CHK-SCI-NOISE-001 门禁核 missing_fields 是否为空
- **置信**: CONFIRMED（注释有 AUDIT-PERSIST-01 实测缺陷与修法说明）

#### Finding CR-243-17 · P17-NSIDE：nside 自动/显式模式冲突检测
- **轴**: C（上下级一致）
- **位点**: module_adapters.cpp:7442–7502
- **上位依据**: ASTROCS_DESIGN §17.6「禁静默降级；高亮 stderr 告警 + undersampled 倍率写产物」
- **现状**: nside_mode ∈ {"auto","explicit"}；auto 时 nside 必须缺省/0/空，否则冲突 fail-closed；explicit 时必须给 nside>0
- **差在哪**: 修复前可能静默改写用户显式 nside 或允许 mode/nside 矛盾；修复后语义互斥，冲突即拒
- **后果**: 修复前用户以为 auto 采样却得到显式欠采样结果；修复后明确 nside_source 与欠采样警告
- **定级**: S2（配置误解风险，可能导致 HiPS 分辨率不当）
- **怎么算修好**: 维持冲突检测；下游按 nside_source 判断是否信任显式值
- **置信**: PARTIAL（欠采样告警的可见性需前台验收）

#### Finding CR-243-18 · B2-A12: precision_mode 强制整数（禁布尔/浮点截断冒充）
- **轴**: C（上下级一致）
- **位点**: module_adapters.cpp:7507–7527
- **上位依据**: 宪章 §5.3「Drizzle 采用 float64 累积」；B1-A9 已关闭 silent 缺省
- **现状**: precision_mode 必须 integer 0(FP32) 或 1(FP64)；boolean/float/string 拒绝（no silent coercion）
- **差在哪**: 修复前真值/浮点可能被截断冒充整数 ⇒ 精度意外降级；修复后严格类型检查
- **后果**: 修复前 HiPS 累积可能退化为 FP32 而不自知；修复后精度声明清晰
- **定级**: S2（精度隐性降级影响最终产品信噪比）
- **怎么算修好**: 维持整数强制；manifest 写 actual_accumulation_precision（FP32/FP64）
- **置信**: CONFIRMED（注释有历史 silent default 被拒绝的背景）

#### Finding CR-243-19 · B2-A14: PHOTSCAL/PHOTAPPL 由真实 provenance 决定（禁硬编码 1）
- **轴**: D（设计符合）
- **位点**: module_adapters.cpp:7531–7573
- **上位依据**: DATA-P1-PHOTPROV-001「photscales/photscale_source/photometry_applied」
- **现状**: 读 p1_phot.json 的 photscal/photscales/frame_status；photscales 缺省时退回标量 photscal（向后兼容）；逐帧判决见 frame_status_map
- **差nhard**: 修复前硬编码 photscal=1 + photometry_applied=false/true；修复后如实透传 provenance
- **后果**: 修复前 drizzle 误判测光已缩放或未缩放；修复后按事实选择输入面（photoapplied/cleaned）
- **定级**: S2（输入面选择错误导致单位混乱）
- **怎么算修好**: 维持 provenance 透传；HEADERS/EXT 写 PHOTAPPL=0/1 与 PHOTDEGRADE=1/0
- **置信**: CONFIRMED（注释有 FIX-P1 逐帧 k_photo 映射细节）

---

### 2.4 总体评估

本文件段（4501–7363+）涵盖四个核心 op：WCS 解算、测光归一化、噪声模型/SNR、drizzle 配置解析。整体代码质量较高，注释密度大，修复记录完整（大量 commit message 风格注释嵌入代码）。主要特点：

1. **Fail-closed 纪律贯彻**: 多处显式 return Result<void>::fail(Error(...))，不给 silent default
2. **Provenance 完备**: 大量落盘字段（photscale_fit、spatial_gain、reference_flux_source 等）支持事后审计
3. **Frame-level verdicts**: FAILSEM-01 框架下逐帧判决（ok/fail）清晰，不影响其他帧
4. **科学参数审计化**: 阈值/常数/配置全部在五档处置表中登记，多数为文献值/公式导出/实验标定

**潜在遗留问题**:
- 部分结论需外部实验验证（如 M1a-C-003 1px 偏移的真实影响量化）
- 组间 k 散度门删除后的 long-tail 帧行为需更长期监控
- fixed_magnitude 路径与 group_median 路径的切换需在真实数据集上跑回归测试

---

## §7 本批最高危三条

1. **CR-243-04 (S1)**: F-INSTR-CONFORM-FIX 前 F_instr 取检测盒和而非 PSF 拟合域 ⇒ k_photo 混入 seeing 依赖的系统偏移，修复后已纠正
2. **CR-243-13 (S1)**: CHAIN-WIRE-ADAPT-01 前掩膜通量未做 data_scale 换算 ⇒ 逐星半径顶到 60px 硬上界，§5a 设计完全失效，修复后已纠正
3. **CR-243-08 (S1)**: 组间 k 散度门 (0.02 dex) 曾使 L4 真实数据 photometry_applied=false（测光归一化完全不执行），删除门后恢复生产，属功能缺失型 S1

---

## §8 我读不动或需要实跑才能定的

1. **M1a-C-003 修复效果量化**: 需要查看该任务的实验报告（前后对比的 roundtrip 误差分布）
2. **fixed_magnitude vs group_median 的长期稳定性**: 需要在真实多晚数据上对比两种 F0 来源的 SNR 跨晚一致性
3. **space_gain_order=2 的实际辨识性**: 需要看 M16/E2E 数据的 spatial_gain_identifiable 读数分布（是否普遍满足 rank_rtol_eff > 0.1）
4. **nside 自动采样的极端画幅表现**: 小画幅帧（32x32）与超大画幅帧的 nside 自动策略边界条件需压力测试
5. **P14-N-08 交付样本上限对深度估计的影响**: 需要对比 snr_max_sources=0（不限）vs 有限值时的 frame_depth_m5_mag 漂移

建议实跑命令：
- `ctest -R test_module_adapters --verbose`（单元级）
- `python3 eng/tools/round_start.sh CR-243-verify` + `AstroCS normalize --config test_run.json`（端到端，观察 p1_phot.json/p1_snr.json 的 provenance 字段）
- 对比旧版本（修复前 commit）与新版本的产品差异（diff p1_*.json）

---

## §7 本批最高危三条

（全部文件读完后再补）

---

## §8 我读不动或需实跑才能定的

（全部文件读完后再补）
