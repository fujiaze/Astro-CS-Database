# 通读 CR-280 —— orchestrator.cpp 行段 1–1381（按行段分片）

- 批次：CR-280（独占范围 `lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp` **1–1381 行**；全文件 5522 行，四段切分 CR-280..283）
- 基线：HEAD=`c8f64e9a`；只读节点；派单前言 `DISPATCH-CODE-READ-PREAMBLE.md` §0–§5 全条生效
- 审查口径（本批特别指定）：① 从配置读的每个键（含缺键兜底值与兜底方向、被夹取/被丢弃的值）；② 每个被写进产品或 manifest 的量，其来源是测量、推导还是硬写；③ 返回码被丢弃、错误消息被吞、"取不到就记 0/1/空串"的位点；④ 日志与运行证据里记录为"本次参数"但实际未生效的量；⑤ 宣称"fail-closed/禁静默"的注释与实际行为的矛盾
- 跨段结论一律标注"须与 CR-280..283 合并定案"，本批不越段下结论
- 分格节奏：每 ~250 行一格，共 6 格（1–250 / 251–500 / 501–750 / 751–1000 / 1001–1250 / 1251–1381），每格读完即 append §2.5 处置表 + findings + bump PROGRESS

## 清单

- `lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp` 行段 1–1381 —— 已读：是（6 格全落，§2.5 处置表合计 110 行；findings CR-280-01…21 共 21 条：S1 七条〔02/03/06/14/15/16/17〕、S2 十条、S3 四条〔04/05/07/08〕）

## 开工 git status --porcelain 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（与基线两项一致；HEAD=`c8f64e9a`）

<!-- PROGRESS: 6/6 -->

## 格 1 · 行 1–250（文件头注释 + JSON 解析缓存 + `calculate_nside` + `orc_getJson*` 访问器）

### §2.5 数值处置表（格 1）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :3 | "5 个阶段" | 5 | 头注释声称的流水线阶段数 | 结构性不适用 | 只是注释里的计数；但 `stage_name_v2`（:365-377）的枚举与 v2 表（:5295-5305，CR-283 段）的 11 节点已不一致→见格 2 CR-280-07 | — | CONFIRMED |
| :4 | B4-24 / C++17 | C++17 | 语言标准锚 | 结构性不适用 | 构建口径由 CMake/CODE_STANDARD 决定，不进科学语义 | — | CONFIRMED |
| :126,:127,:202 | NSIDE 下界 | 16 | 自动/显式 nside 钳位下界 | 文献值 | `docs/algorithms/DRIZZLE_GEOMETRY.md:158-161` 钳位 `[NSIDE_MIN,NSIDE_MAX]=[16,4194304=2^22]` 并给出下界适用域（nside=16 ⇒ 13192.3″≈3.66° 最粗叶级）；`configs/stage1.schema.json:264` `minimum:16` | 方向：nside 越小越粗 ⇒ 16 为最粗可表达叶级，与条文一致 | CONFIRMED |
| :126,:127,:141-144,:204,:210 | NSIDE 上界 | 4194304 (2^22) | 自动/显式 nside 钳位上界 | 文献值 | 同上 `DRIZZLE_GEOMETRY.md:158`；`configs/stage1.schema.json:265` `maximum:4194304`；`orchestrator/README.md:48` "explicit 16..4194304" | — | CONFIRMED |
| :133 | `nside_override > 0` | 0 | "用户指定优先"哨兵界 | 结构性不适用 | 0 = "未给出"哨兵；与 `hp_drizzle_api.h:126-131` P17 语义"缺省(0/空)=>自动"一致 | — | CONFIRMED |
| :147-149 | `(n&(n-1))!=0` / `p*=2` | 2 | 2 的幂判定与向下取幂 | 结构性不适用 | 位运算结构，无科学语义 | — | CONFIRMED |
| :164 | 轴均分权重 | 0.5 | `pixel_scale = 0.5*(sx+sy)`，两轴 CD 范数取算术平均 | **待确认** | 条文面只给"最细输入尺度 finest"（`DRIZZLE_GEOMETRY.md:156-157`、源锚 :144），无任何文档规定用两轴平均；见 CR-280-03 | 保守方向：应取 `min(sx,sy)` ⇒ 现行平均值偏大 ⇒ nside 偏小 ⇒ 产品偏粗 | CONFIRMED |
| :164 | 度→角秒 | 3600.0 | 单位换算定义常数 | 结构性不适用 | 1 deg = 3600 arcsec 定义，派单 §4 明列不报 | — | CONFIRMED |
| :167 | 尺度非正/非有限判据 | 0.0 | CD 退化哨兵 | 结构性不适用 | "取不到"检测阈值，非科学量 | — | CONFIRMED |
| :168,:169 | CD 无效兜底 nside | **32768** | CD 取不到时写死的叶级 NSIDE | **待确认** | 条文面零提及（`git grep -n "32768" -- docs/** ASTROCS_DESIGN.md` 零命中）；`DRIZZLE_GEOMETRY.md:157` 只说"无效输入→0 由调用方兜底"，未规定兜底取值；见 CR-280-02 | 保守方向：显式失败（同文件 `:4902-4906` 即该口径）；影响范围=整份 HiPS 产品叶级分辨率与 tile 数 | CONFIRMED |
| :175,:177 | 被禁旧魔数 | 1186.18 / 210960 | 注释点名"禁止"的历史错值 | 结构性不适用 | 只出现在注释，代码不再使用；派单 §4"已登记退役值不报" | — | CONFIRMED |
| :176 | "约 178 倍"/512/65536/6.3"/px | 见左 | 历史缺陷量级叙述 | 结构性不适用 | 注释叙述，不参与计算 | — | CONFIRMED |
| :178,:198 | HEALPix 元面积式 | 4π/(12·nside²) ≡ π/(3·nside²) | 等面积单元定义 | 公式导出 | Górski+2005 ApJ 622:759 §4，经 `DRIZZLE_GEOMETRY.md:145-149` 收录；**代码确实按式算**（:198 `std::sqrt(PI/3.0)*(180.0/PI)*3600.0`）⇒ 合规 | — | CONFIRMED |
| :180,:198 | 注释抄写值 | **211034.6** | 注释给出的 :198 表达式结果 | **待确认**（实为权威面点名的被禁值） | 权威值 `211076.28514206142″`（`DRIZZLE_GEOMETRY.md:145`）；同条 :151-155 点名 `211034.6` 差 −1.97e-4 且"少取一阶"；复算相对差 −1.9749e-4；见 CR-280-05 | 方向：注释按权威值改写；代码值正确故不影响产品字节 | CONFIRMED |
| :182,:188,:191 | `drizzle_factor` 默认 | **1.5** | "1x_to_2x_drizzle" 取 1–2 倍过采样中间值 | **待确认** | P17 裁决只钉"采样率等价 drizzle 1x-2x"（`hp_drizzle_api.h:126-131`），`1.5` 这个中间值在 `docs/science/**`、`docs/algorithms/DRIZZLE_GEOMETRY.md`、`ASTROCS_DESIGN.md` 零命中；引擎自身口径是 `hp_res ≤ finest` 即 **1.0**（`DRIZZLE_GEOMETRY.md:156-157`）⇒ 两侧系数不同；见 CR-280-03 | 方向：与引擎统一 1.0，或把 1.5 登记进 `DRIZZLE_GEOMETRY.md` §3 并给适用域；影响范围=normalize/mosaic 产品叶级 NSIDE 可差 1 个倍频程 | CONFIRMED |
| :184 | `fixed`/`1x` 系数 | 1.0 | 策略别名→系数 | 待确认 | `1x`/`fixed` 合并为 1.0 无条文；`fixed` 无 override 时并非"固定"而是自动 1.0（与 :124 注释相反）；见 CR-280-04 | 方向：策略词表应与合同词表（auto/explicit）二选一 | CONFIRMED |
| :186 | `2x` 系数 | 2.0 | 策略别名→系数 | 待确认 | `"2x"`/`"1x"` 在配置面零出现（`git grep -n "\"2x\"\|\"1x\"" -- lib/` 仅命中本函数） | 方向：无消费方的别名应删 | CONFIRMED |
| :196 | π 派生 | `std::acos(-1.0)` | 不依赖 `M_PI` | 公式导出 | 同 `DRIZZLE_GEOMETRY.md:145` 式；代码按式算⇒合规 | — | CONFIRMED |
| :203,:204 | 起始幂/步进 | 16 / ×2 | 向上找 2 的幂 | 结构性不适用 | 循环上界与步进是实现选择；结果与条文"最小 2 次幂 ≥ C/finest"一致（`DRIZZLE_GEOMETRY.md:156`） | — | CONFIRMED |
| :212 | `char buf[256]` | 256 | snprintf 行缓冲 | 结构性不适用 | 日志缓冲尺寸；本行最长 ~90 字节 | — | CONFIRMED |
| :214 | `%.3f` | 3 | 日志格式化精度 | 结构性不适用 | 只影响日志可见位数 | — | CONFIRMED |
| :233,:243,:253 | 缺键兜底 | `""` / `def` / `def` | `orc_getJson*` 缺键或类型不符的返回 | 待确认 | 兜底方向由调用方给，本函数零日志；见 CR-280-01（整份 JSON 解析失败也走同一兜底） | 方向：应区分"键不存在"与"解析失败" | CONFIRMED |

**格 1 数值项合计：22 行（覆盖 :3–:253 全部字面量与注释数值）**

### 格 1 findings

### CR-280-01 · 配置 JSON 解析失败被 `catch(...)` 吞掉并降级为"所有键都不存在"，全程零日志
- 轴：R（主）+ C
- 位点：`orchestrator.cpp:107-117`
  ```cpp
  const nlohmann::json& parse_config_cached(const std::string& s) {
      if (s != s_parsed_config_src) {
          try { s_parsed_config = nlohmann::json::parse(s); }
          catch (...) { s_parsed_config = nlohmann::json::object(); }
  ```
  下游消费器 `:228-234`（缺键→`""`）、`:238-244`（缺键→`def`）、`:248-253`（缺键→`def`）。
- 上位依据：仓内"禁静默"口径逐字可见于 `docs/algorithms/DRIZZLE_GEOMETRY.md:207-212`（"**必须计数**，禁静默"）；`docs/contracts/IO_003_ATOMIC_OUTPUT_PUBLISH.md` §4/§6（失败不产生成功对象）。检索式：`git grep -n "catch (\.\.\.)" -- lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp`、`git grep -n "parse_config_cached" -- lib/`。
- 现状：解析异常被吞后写入**空对象**，同时 `s_parsed_config_src = s` 仍被赋值 ⇒ 同一字符串后续所有查询命中"已解析的空对象"，不再重试也不报错；`orc_getJsonString` 返回 `""`、`orc_getJsonNum`/`Bool` 返回 `def`，**没有任何日志区分"键缺失"与"整份 JSON 解析失败"**。
- 差在哪：实现把"整份文档读坏了"伪装成"这些键本来就没配"，后者是本函数对外的语义契约（`:106` 注释「解析失败时返回空 json::object (调用方按字段缺失处理)」即自我声明按缺键处理）。
- 后果：五个消费点 `:587`（`log_level`）、`:599`（`threads`）、`:606`（`gaia_data_dir`）、`:3548`（`precision_mode`，兜底 −1.0）、`:5413`（`allow_partial_output`，兜底 false）在解析失败时**同时**退到兜底值且运行照常出产品；对外"本次运行按配置参数执行"的主张失去依据。
- 定级：**S2**（缺运行时触发路径：未实跑确认到达 `:5403` 的字符串是否必过 `json_config.cpp:545-556` schema 校验；若必过则该 catch 不可达 ⇒ 降 S3）
- 怎么算修好：`catch (const nlohmann::json::exception& e)` + `LOG_ERROR(... e.what())` + 置"解析失败"标志供 stage 入口显式失败；缓存键应含"解析成功"位，避免失败结果被永久缓存为空对象。
- 置信：PARTIAL（缺：`:5390-5415` 的 config 字符串来源判定，属 CR-283 行段）

### CR-280-02 · CD 矩阵取不到时把叶级 NSIDE 写死成 32768，无条文来源，且与同文件另一入口的"显式失败"口径相反
- 轴：R（主）+ S + C
- 位点：`orchestrator.cpp:166-170`
  ```cpp
  if (pixel_scale_arcsec <= 0.0 || !std::isfinite(pixel_scale_arcsec)) {
      LOG_WARN("orchestrator", "[NSIDE] CD 矩阵无效, 回退默认 nside=32768");
      return 32768;
  ```
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md:157-158`「无效输入（finest≤0 等）→ **0 由调用方兜底**（:697-701）」——条文只把兜底责任交给调用方，**未规定任何兜底取值**；P17 过采样口径见 `lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.h:126`「oversample; // hp_res/finest（引擎口径; 0.5~1 = 1~2 倍过采样）」。检索式：`git grep -n "32768" -- docs/** ASTROCS_DESIGN.md`（零命中）、`git grep -n "回退默认 nside" -- lib/`。
- 现状：32768 是纯字面量；按 :198 式反算其等面积角尺度 = 211076.28514206142/32768 = **6.442″/px**。同文件另一 NSIDE 消费方 `run_stage_nside` 对**同一条件**（`cd11==0.0 && cd22==0.0`）的处理是 `error_msg` + `exit_code=GENERIC_ERROR` + `return false`（`:4902-4906`，CR-283 段）；而 `run_stage_drizzle`（`:3328-3335`，CR-282 段）CD 四键以 `0.0` 为默认值取回后**没有该 guard**，直接进 `calculate_nside`（`:3353`）。
- 差在哪：同一个"取不到 WCS"事实在两个入口一个判红一个继续装配产品；被兜底的 6.442″/px 相对典型生产输入 0.9586″/px（`artifacts/evidence/governance-01/research/R-2_phase2权重与UPM语义.md:225` 实测生产域）是 **6.7 倍欠采样**，正落在 P17 不得出现的区间。
- 后果：mosaic/normalize 的 HiPS 产品在该帧上以 NSIDE=32768 落盘并呈"完成"；对外"叶级 NSIDE 由帧内采样率自动决策、满足 1–2× drizzle 等价"主张不成立；且该路径绕过 `compute_auto_nside_ex` 的 `oversample`/`clamped` 输出（`hp_drizzle_api.h:119-127`），下游无字段可判红。
- 定级：**S1**。触发路径逐跳：`lib/infrastructure/cli/…`（normalize/mosaic 入口）→ `Orchestrator::run_stage_drizzle`（`orchestrator.cpp:3291`）→ `fn_kv_get_double(frame_,"header","CD1_1",0.0)` 缺键得 0（`:3328-3331`）→ `calculate_nside(cd11,cd12,cd21,cd22,strategy,override)`（`:3353`）→ `orchestrator.cpp:167-170` 返回 32768 → `hp_drizzle_run_hips` 以 nside=32768 装配产品。其中 `:3291/:3328/:3353` 属 CR-282 行段，**须与 CR-280..283 合并定案**。
- 怎么算修好：改为显式失败（与 `:4902-4906` 同口径）；若负责人裁定保留兜底，则须①把取值登记进 `DRIZZLE_GEOMETRY.md` §3 并给 `oversample` 后果，②把"兜底已启用"写进产品/manifest 可判红字段，③补负例（无 CD 帧 ⇒ 判红）。同批须检查是否有在册端到端夹具依赖"无 CD 也能出图"。
- 置信：CONFIRMED（数值方向与两入口对照均逐行读到；未实跑）

### CR-280-03 · `calculate_nside` 是 `compute_auto_nside` 的并行实现：判据量由"最细尺度"换成"两轴均值"、系数由 1.0 换成 1.5，而权威文档称 orchestrator 策略"最终映射到本函数"
- 轴：C（主）+ D + S
- 位点：`orchestrator.cpp:162-164`
  ```cpp
  double sx = std::sqrt(cd11 * cd11 + cd21 * cd21);
  double sy = std::sqrt(cd12 * cd12 + cd22 * cd22);
  double pixel_scale_arcsec = 0.5 * (sx + sy) * 3600.0;
  ```
  与 `:194` `double target_resolution_arcsec = pixel_scale_arcsec / drizzle_factor;`（`:182` 默认 1.5）
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md:144`「源锚: `drizzle_engine.cpp` `compute_auto_nside`」；`:156-157`「决策: 最小 2 次幂 ≥ 211076.28514206142/finest_arcsec（判据式 `hp_res_arcsec = C/nside ≤ finest_arcsec`）」；`:169-170`「**orchestrator 侧策略 `1x_to_2x_drizzle`/`fixed` 最终映射到本函数**（orchestrator.cpp:3298-3311）」。
- 现状：orchestrator 从不调用该函数族——`git grep -n "hp_drizzle_compute_auto_nside"` 全仓消费方仅 `lib/infrastructure/scheduler/src/module_adapters.cpp:7445`（注释）与 `:8242`（调用），`orchestrator.cpp` 零命中；引擎口径是"自适应四叉树采样 WCS+SIP 的**最细**局部尺度"（`drizzle_engine.cpp:650-745`），本函数只用 4 个 CD 数、取**算术平均**、除以 1.5、再向上取 2 的幂。
- 差在哪：三处同时偏离权威判据——①`mean ≥ min` ⇒ 判据分母偏大 ⇒ `nside_target = C/(mean/1.5)` 相对 `C/finest` **只会更少不会更多**（方向钉死：nside 偏小 ⇒ 像素偏粗 ⇒ 欠采样，非保守侧）；②1.5 系数在权威判据式中不存在（权威为 1.0）；③文档宣称"最终映射到本函数"而实现另起一套。
- 后果：同一帧经 `module_adapters` 通道与经 `orchestrator` 通道可得**不同叶级 NSIDE**（=不同产品字节），两处均自称"1x_to_2x_drizzle 自动"；`oversample`/`clamped` 在 orchestrator 通道恒不可得 ⇒ `DRIZZLE_GEOMETRY.md:160-162`「消费方必须读 `oversample` 字段判欠采样」在本通道无对象可读。
- 定级：**S1**。触发路径：CLI → `Orchestrator::run_stage_nside`（`:4879`）→ auto 分支 `nside = calculate_nside(cd11, cd12, cd21, cd22, "auto", 0)`（`:4910`）→ `:164` 平均 + `:194` /1.5 + `:203-206` 取幂 → `nside_used_`（`:4911`）→ `run_stage_drizzle` 以 `strategy="fixed", override=nside_used_`（`:3346-3353`）驱动装配。`:4879/:4910/:4911/:3346` 属 CR-283/CR-282 段，**须与 CR-280..283 合并定案**。
- 怎么算修好：二选一并同批做——(A) orchestrator 改调 `hp_drizzle_compute_auto_nside`（`hp_drizzle_api.cpp:1365`）并把 `finest/hp_res/oversample/clamped` 透传进 manifest；(B) 在 `DRIZZLE_GEOMETRY.md` §3 明文登记"pipeline 通道判据=两轴均值/1.5"及其偏差界。**防判红**：改后 `eng/tests/unit/p1001_real_nodes_test.cpp:2563-2653` 的 P17 断言族与 `configs/stage1_gc_panel*_Red.json:40,55` 的夹具 nside 会随判据变动，必须同批复核。
- 置信：CONFIRMED（三处判据差异为逐行对读所得；"两通道是否真算出不同 nside"需一次对照实跑，见"读不动"节）

### CR-280-04 · 策略词表不含 `auto`/`explicit`，本文件自己的内部调用把 `"auto"` 当"未知 strategy"记 WARN
- 轴：C（主）+ R
- 位点：`orchestrator.cpp:183-192`
  ```cpp
  if (strategy == "fixed" || strategy == "1x") { drizzle_factor = 1.0; }
  else if (strategy == "2x") { drizzle_factor = 2.0; }
  else if (strategy == "1x_to_2x_drizzle" || strategy.empty()) { drizzle_factor = 1.5; }
  else { LOG_WARN("orchestrator", "[NSIDE] 未知 strategy='" + strategy + "', 使用默认 1.5x"); drizzle_factor = 1.5; }
  ```
- 上位依据：配置面合法值只有 `auto`/`explicit`——`configs/stage1.schema.json:232-266`（`mode` 的两个 `const`）、`cpp/include/json_config.h:68` `std::string nside_mode = "auto"; // "auto" 或 "explicit"`；另一生产入口的合法集为 `"1x_to_2x_drizzle"|"auto"|"explicit"`（`module_adapters.cpp:7463-7466` 的显式报错文案）。
- 现状：`calculate_nside` 认识 `fixed`/`1x`/`2x`/`1x_to_2x_drizzle`/空串，**不认识 `auto` 也不认识 `explicit`**；本文件在 `:4910` 直接传字面量 `"auto"` ⇒ 每次 auto 推导都打一条 `未知 strategy='auto'` WARN；`explicit` 则在 `:3342-3344`、`:3349` 两处手工翻译成 `"fixed"`。
- 差在哪：同一概念在本文件内有两套互不识别的词汇表，靠调用点手工翻译，且其中一处漏翻译。
- 后果：①每次正常 auto 运行产生伪 WARN，真正的 strategy 拼写错误会被淹没（告警面失真）；②`fixed` 无 override 时实际含义是"系数 1.0 的自动推导"，与 `:124` 注释「"fixed": 使用 nside_override」相反——注释承诺固定，代码仍走自动分支。
- 定级：**S3**（不改产品字节；污染告警与注释语义）
- 怎么算修好：`auto` 并入 `1x_to_2x_drizzle` 分支（或直接接受 `nside_mode` 字符串、删掉调用点翻译）；`:124` 注释与实现对齐（"fixed 且 override<=0 退化为 1.0× 自动"应显式说明或直接拒绝）。
- 置信：CONFIRMED

### CR-280-05 · 注释把权威面点名的被禁抄写值 `211034.6` 写成 :198 表达式的答案
- 轴：C（注释与权威条文数值不一致；代码值正确）
- 位点：`orchestrator.cpp:198`
  ```cpp
  std::sqrt(PI / 3.0) * (180.0 / PI) * 3600.0;  // ≈ 211034.6
  ```
  同值另见 `:180`「转角秒: sqrt(π/3) / nside * (180/π) * 3600 ≈ 211034.6 arcsec」
- 上位依据：`docs/algorithms/DRIZZLE_GEOMETRY.md:145` `HEALPIX_SCALE_PER_NSIDE_ARCSEC = sqrt(π/3)·(180/π)·3600 = 211076.28514206142″`；`:151-155`「**数值纪律**：常数按上式求值…**`211034.6` 与上式相差 −1.97e-4（相对）**，会使 `nside` 决策在每个倍频程的 `finest ∈ (211034.6/2^k, 211076.285/2^k)` 窗口内**少取一阶**」。
- 现状：代码 `:198` 确实按式求值（合规），注释写的是条文点名的被禁值。复算：`sqrt(π/3)*(180/π)*3600 = 211076.28514206142`；`211034.6` 相对差 = −1.9749e-4，与条文的 −1.97e-4 吻合。
- 差在哪：注释传播的正是权威面为防"抄写导致少取一阶"而明令订正的错值。
- 后果：后续维护者按注释抄 `211034.6` 即命中 §3 的已知陷阱（条文给了实测例：finest=105527.7″ 时正确式给 nside=4、抄写值给 nside=2，像素尺度差 2 倍）。当前产品不受影响。
- 定级：**S3**
- 怎么算修好：两处注释改为 `211076.28514206142″` 并加指针 `docs/algorithms/DRIZZLE_GEOMETRY.md §3`。
- 置信：CONFIRMED（已复算）

### 格 1"不报的核过项"（附检索式）
1. **NSIDE 钳位 [16, 4194304] 与 `:135` 注释"CLI 已校验, 此处兜底"不构成缺陷**：显式值域在配置层真被强制——`configs/stage1.schema.json:263-265` `minimum:16/maximum:4194304`，`cpp/src/json_config.cpp:262-300` 的**内嵌 schema 副本**（`:28-29` 注明由 `gen_json_config.py` 从同一文件生成）同样带该值域，`json_config.cpp:566-571` 另加"2 的幂"运行时校验 ⇒ 兜底是纵深防御，不是"合同拒绝、实现静默接受"。检索式：`git grep -n "minimum\|maximum" -- lib/infrastructure/pipeline/orchestrator/configs/stage1.schema.json`；`sed -n '256,300p' lib/infrastructure/pipeline/orchestrator/cpp/src/json_config.cpp`。
   - 附带登记（位点不在本行段，故不立 finding，入合并待办）：`eng/contracts/schemas/phase_config_normalize.schema.json:134-140` 的 `nside` **只有类型没有 16/4194304 值域**，与上述两侧不一致。
2. **`sqrt(cd11²+cd21²)` / `sqrt(cd12²+cd22²)` 的轴取法本身不错**：CD 按 FITS 列优惯例，x 基向量 = (cd11,cd21)、y = (cd12,cd22)。只有"平均 vs 最细"报为 CR-280-03。
3. **`:147-154` 显式非 2 的幂向下取幂**：`nside_override>0` 的整条路径在配置层已被"必须是 2 的幂"硬拒（`json_config.cpp:568-570`），且向下取幂（属"尊重用户显式值"适用域）与 `:201-206` 向上取幂（属"满足判据的最小幂"适用域）是两个适用域，不构成自相矛盾。
4. **`:102-104` 文件作用域解析缓存的线程安全声明**：注释自陈"仅在单线程 stage 串行执行场景下使用"，与 v2 stage 表（`:5295-5305`）的串行 `for` 一致，不报并发缺陷（帧级并发在 `module_adapters` 层，不经此函数）。
5. **`:10` 头注释"内存使用查询返回 0（已知限制，见 docs/KNOWN_LIMITATIONS.md）"**：`docs/KNOWN_LIMITATIONS.md` 存在（`git ls-files | grep -i KNOWN_LIMIT` 命中），条目 30 现行口径为"实测估算（0 不作占位值）"。该注释是否已过期取决于 `get_memory_usage` 的实现位点，位于 CR-282/283 段 ⇒ 不在此下结论，入合并待办。

## 格 2 · 行 251–500（`orc_getJsonBool` 收尾 + 文件名解析 + 统计基元 + 阶段名表 + 取消/超时 + 原子清理）

### §2.5 数值处置表（格 2）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :259 | 文件名标签 | `"_EXPOSURE-"` | 从 masterDark 文件名反解曝光时间的约定标记 | 待确认 | 约定只存在于工具面：`eng/tools/realdata/match_plan.py:81` 正则 `(?:_EXPOSURE-(?P<exp>\d+(?:\.\d+)?)s)?`、夹具 `lib/algorithms/calibration/batch_config.json:11-13`（键即曝光秒数）；`docs/science/**`、`docs/algorithms/CALIBRATION_ALGORITHMS.md` 无该命名规范条款（`git grep -n "_EXPOSURE-" -- docs/` 零命中）；影响：曝光时间这一科学量的来源是**文件名**而非 header/配置，见 CR-280-11 | 保守方向：应优先读 dark FITS header 的 EXPTIME，文件名只作兜底并记录来源 | CONFIRMED |
| :261,:264,:269 | 解析失败哨兵 | −1.0 | 文件名解析失败的返回 | 待确认 | 消费点在 `:1079`（本行段），其写产品分支在 `:1281-1282` ⇒ 见格 5 处置；本函数内 `-1.0` 与合法值域（曝光秒数>0）不重合 ⇒ 方向上不是"0 伪装成已测"，属可判失败哨兵 | — | PARTIAL |
| :263 | 数字终止符 | `'s'` | `path.find('s', p)` 取数值右界 | 待确认 | 比 `match_plan.py:81` 的 `\d+(\.\d+)?s` 宽松：非数字/半角逗号（`_EXPOSURE-1,80s`→`stod` 得 1.0）、科学计数法等均被吞成数值而非报失败；见 CR-280-11 | 保守方向：应拒绝"整段不是纯十进制数"的名字（与 :81 正则同严格度），失败返回 −1.0 | CONFIRMED |
| :275,:283,:295,:307,:314,:326 | 空数组兜底 | 0.0 | `!data \|\| n<=0` 时 mean/median/std 返回 0 | **待确认** | 0 对"背景均值/噪声 σ"是物理合法值 ⇒ 测量失败与"真值为 0"在返回值上不可区分；`OWNER_DECISIONS.md:147`（审计裁决面）确立"兜底默认值禁止与显式拒绝值重合…0 在物理上常是合法值"；消费点 `:1205-1224` → 交付物 `cal_stats`（`:1262` 注释自称"交付物"）⇒ 见格 5 CR-280-19 | 保守方向：失败应返回 NaN（产品面已确立 NaN=不可用，`DATA_SEMANTICS` §4a）并由调用方判红；影响范围=cal_stats 三个字段的"已测"语义 | CONFIRMED |
| :277,:284,:290,:301,:309,:321,:331 | 累加/换算因子 | 0.5f / 0.5 / float→double | 偶数中位数取两值平均、float 升 double 累加 | 公式导出 | 中位数定义式（偶数样本取两中值算术平均）；代码真按式算 ⇒ 合规 | — | CONFIRMED |
| :285,:287 | `mid = n/2`、`n%2==1` | 2 | 中位数下标与奇偶分支 | 结构性不适用 | 索引算术，无科学语义；已复算 n=4 时给 (2nd+3rd)/2 正确 | — | CONFIRMED |
| :301,:332 | σ 估计分母 | `sum_sq / n`（**总体**标准差，非 n−1） | `compute_array_std` 的方差归一 | **待确认** | 仓内"背景噪声 σ"的权威口径是 `σ = 1.482602218505602·MAD`（`docs/algorithms/CALIBRATION_ALGORITHMS.md:87,:228`、`docs/algorithms/GATES_AND_TOLERANCES.md:35` `sigma_bg = mad·1.482602218505602`、`docs/algorithms/COSMETIC_ALGORITHMS.md:71`），**没有任何条文定义"全帧含星的总体标准差"这一统计量**；且无 Bessel/有限样本校正条款（`CALIBRATION_ALGORITHMS.md:624` 明确"本仓用渐近常数、不做校正"仅针对 MAD→σ 换算）；`git grep -n "LIGHT_STD\|OUT_STD" -- docs/ eng/contracts/` → 合同/文档零定义 | 保守方向：`LIGHT_STD/OUT_STD` 若被当噪声用应改 MAD 口径并在合同登记；若纯诊断则应在字段说明里钉死"含源全帧总体 σ，不作噪声估计" | PARTIAL（缺：`cal_stats.*` 是否有在册消费者把 STD 当 σ 用） |
| :339-348 | v1 `stage_name` case 集 | 5 | 阶段枚举→名字 | 结构性不适用 | 枚举基数（`PipelineStage` v1）；未覆盖者落 "UNKNOWN" | — | CONFIRMED |
| :363 | 注释节点数 | "9 节点" | 头注释对 spec §2.3.2 的节点计数 | **待确认** | 与同族三处互相不符：`orchestrator.h:81` 写"10 节点"、本行写"9 节点"、文件头 `:3` 写"5 个阶段"，而 `PipelineStageV2` 实为 11 个枚举（`orchestrator.h:96-106`，READ_FITS..BROWSER_VERIFY）⇒ 见 CR-280-07 | 保守方向：计数应删除或改为指向枚举，勿在注释里复写 | CONFIRMED |
| :366-376 | `stage_name_v2` case 集 | 8 | 阶段枚举→名字（并被登记为阶段名权威） | 结构性不适用（覆盖不全另立 finding） | 枚举基数本身不报；**缺 NSIDE(6)/HISS_VERIFY(9)/BROWSER_VERIFY(10) 三个 case** ⇒ CR-280-07 | — | CONFIRMED |
| :390,:395-397 | 取消/超时标志 | true/false + release | 原子标志存取 | 结构性不适用 | 无科学语义；`:384-388` 的 signal-safety 约束与实现一致（handler 路径只做一次 store） | — | CONFIRMED |
| :411 | 空配置短路 | `config_json.empty()` | 无配置串时返回空表 | 结构性不适用 | 哨兵判空 | — | CONFIRMED |
| :415,:423 | 键/类型判据 | `stage_timeouts` / is_number | 超时表读取 | 待确认 | 顶层键 `stage_timeouts` 在全仓配置面零存在（`git grep -n "stage_timeouts" -- "*.json" docs/` 零命中；权威面键名是 `execution.stage_timeout_sec`，`configs/stage1.schema.json:343`）⇒ CR-280-08 | 方向：同一旋钮两套键名，另一套恒不生效 | CONFIRMED |
| :444,:453 | 空路径/不存在 | true | "无操作即成功"返回 | 结构性不适用 | 幂等清理语义 | — | CONFIRMED |
| :465 | 清理手段 | `fs::remove_all(path)` | 失败时递归删除目标路径 | **待确认（缺陷）** | 判据面：`docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md` §4.2「默认不覆盖——目标为**成功对象**（含 COMPLETE manifest）且 overwrite=False → PublishError；中断残留=非成功对象→自动清残重发」——本函数**无任何"是否成功对象/overwrite"判据**，见 CR-280-06 | 保守方向：仅在"本轮创建"或"无 COMPLETE manifest"时删除；影响范围=同目录既有已发布产品 | CONFIRMED |
| :492,:498 | 取消/超时退出码 | CANCELLED / TIMEOUT | 失败分类 | 结构性不适用 | 枚举值，非科学量 | — | CONFIRMED |

**格 2 数值项合计：16 行**

### 格 2 findings

### CR-280-06 · 失败清理对目标路径做无条件 `remove_all`，没有"成功对象 / overwrite"判据，且返回码在全部 5 个调用点被丢弃
- 轴：R（主）+ D + C
- 位点：`orchestrator.cpp:463-474`
  ```cpp
  if (is_dir) {
      const auto removed = fs::remove_all(path, ec);
      ...
      LOG_WARN("orchestrator", "P04-004: 原子清理 - 已删除部分输出目录树: " + path ...);
      return true;
  ```
  函数签名 `:442 bool Orchestrator::cleanup_partial_output(const std::string& path)`；注释 `:440-441`「**overwrite 语义不变**: 本函数只在失败/取消/超时清理路径调用…**不触碰成功发布对象与 overwrite 分支**」。
- 上位依据：`docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md:91-117` §4（发布流水线）与 `:151-162` §6（错误语义），§4.2 逐字：「默认不覆盖——目标为**成功对象**（含 COMPLETE manifest）且 `overwrite=False` → `PublishError`。中断/cancel 残留（目录存在但无 COMPLETE manifest）= **非成功对象** → 自动清残重发；成功对象仅在显式 `overwrite=True` 时清除后重建。」本函数注释 `:437` 亦自称依据「IO_003_ATOMIC_OUTPUT_PUBLISH.md §4/§6」。
- 现状：删除判据只有三条——路径非空、存在、是目录；**既不看 `output.overwrite`，也不看目标是否含完成标记**。全仓 `overwrite` 只有一处消费：`:3401 if (stage1_cfg_->output.overwrite && fs::exists(hips_dir))`（CR-282 段，用途是"跑前清旧"），即"目标已存在且 overwrite=false"在任何入口都不被拒绝（`git grep -n "overwrite" -- lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp` 仅 :440/:3400/:3401 三处）。返回码方面：`git grep -n "cleanup_partial_output"` 的 5 个调用点 `:5103`（RAII 析构内）、`:5367`、`:5371`、`:5431`（RAII 析构内）、`:5508` **全部丢弃返回值**，清理失败既不改变 `result.success` 也不改变 `exit_code`。
- 差在哪：注释宣称"不触碰成功发布对象"，而实现触碰——它与"清残"用的是同一条无判据的递归删除；同时"清理失败"被降级为一条 ERROR 日志，不影响退出码，即"失败后无成功对象"这一 IO-003 §6 主张在实现层没有强制面。
- 后果：①数据丢失——同一 `output.hips` 目录（配置模板默认 `overwrite:false`，`configs/stage1.template.json:52`）在上一次成功产出后再次运行、本次中途失败时，上一轮的整棵 HiPS 产品树被 `remove_all` 删除，且用户从未要求覆盖；②对外主张失真——"任何失败不产生成功对象、残留可清理恢复"（§6）依赖清理真的发生，清理返回 false 时进程仍按原失败码退出，磁盘上留下的半截目录与"成功对象"在字节面上不可区分（`docs/algorithms/HIPS_WRITER.md:379` DISP-HIPS-004 自陈"manifest 无 COMPLETE 状态字/树哈希"⇒ C++ 侧根本没有完成标记可比对）。
- 定级：**S1**。触发路径逐跳：CLI normalize → `Orchestrator::run_stage1`（`:5066`，CR-283 段）→ `atomic_guard{this, cfg.output.hips, result.success}`（`:5106`）→ 任一 stage 失败使 `result.success=false` → `~AtomicOutputGuard`（`:5101-5105`）→ `cleanup_partial_output(path)`（`:5103`）→ `orchestrator.cpp:465 fs::remove_all` 删除既有产品树。`:5101-5106` 属 CR-283 段 ⇒ **须与 CR-280..283 合并定案**；另注 stage1 的 guard 结构体（`:5097-5106`）不含 `allow_partial` 项，而 stage2 的（`:5427-5434`）含——同一守卫两套条件，亦属合并项。
- 怎么算修好：①删除前判"本轮是否创建过该路径 / 是否存在完成标记"，二者皆否才清；②`overwrite=false` 且目标已存在 → 运行前显式 `CONFIG_ERROR` 拒绝（对齐 §4.2），此时不得进入任何删除分支；③调用点接收返回值，清理失败时把 `exit_code` 提升为"未清理残留"类错误并在日志/manifest 登记残留路径；④补负例：预置一个非空目标目录 + 注入中途失败 ⇒ 断言旧树仍在、退出码非 0。防判红：同批须核对 `eng/tests/**` 与 e2e 是否有"重复运行同一 hips 目录"的既有用例（若有，改判红后要一并调整其前置清理）。
- 置信：CONFIRMED（删除无判据、5 个调用点丢弃返回值、overwrite 仅一处消费均已逐行读到；未实跑验证"旧产品被删"）

### CR-280-07 · `stage_name_v2` 缺 3 个 v2 节点且零生产调用方，却被登记为阶段名权威；四处注释对同一管线给出 5/9/10/11 四种节点数
- 轴：C（主）+ D
- 位点：`orchestrator.cpp:365-377`（`stage_name_v2` switch，仅 8 个 case）；同族注释 `:3`「串联 5 个阶段」、`:363`「两段流水线 **9 节点**阶段名称」、`cpp/include/orchestrator.h:81`「两段流水线 **10 节点**」；实际枚举 `orchestrator.h:96-106` 为 11 个（含 `NSIDE = 6`、`HISS_VERIFY = 9`、`BROWSER_VERIFY = 10`），实际派发表 `orchestrator.cpp:5295-5305`（CR-283 段）列出 11 个 stage。
- 上位依据：`eng/tools/docs_machine_consistency.py:202` 把 `orchestrator.cpp` 的 `stage_name_v2` 登记为一致性来源之一，`:363-365` 的检查为 `all(s in tax for s in stages) and "stage_name_v2" in orc`，其中 `stages` 是**该 Python 文件内硬编码的 10 个 `P1.*/P2.*` 字符串**（`:359-361`）；`docs/contracts/API_CONTRACTS.csv:220` 将 `API-stage_name_v2` 标为 `VERIFIED`；`docs/architecture/api_inventory.csv:265` 收录该符号。
- 现状：机器门只检查字符串 `"stage_name_v2"` **出现在** orchestrator.cpp 中，从不解析其 case 集合 ⇒ 缺 3 个节点不可能判红；`git grep -n "stage_name_v2"` 全仓无任何运行时调用（仅 `.h:528` 声明、`.cpp:365` 定义、eng/tools 与两份登记 CSV），运行时用的是派发表里的 `sd.name` 字面量与 `current_stage_name_`（`:402-405`）。
- 差在哪：一个"零调用、由注释手抄阶段名"的符号被当作跨文档阶段 ID 的权威面，而真正的权威（枚举与派发表）与它内容不一致；宣称的"门"对它是恒真式。
- 后果：阶段面（ERROR_TAXONOMY / 文档 / 日志 stage_id）与真实 11 节点管线之间的偏差无法被机器门发现；`stage_name_v2(NSIDE)` 返回 `"UNKNOWN"` —— 一旦被后续代码接入日志/事件，NSIDE、HISS_VERIFY、BROWSER_VERIFY 三个节点的记录会是 "UNKNOWN"（当前无调用方，故为潜伏形态）。
- 定级：**S3**（当前无产品字节影响；写清后果为"门的判别力为零 + 后续接入即失真"）
- 怎么算修好：让 `stage_name_v2` 由 `PipelineStageV2` 全枚举覆盖并配 `switch` 默认分支判红（或删掉该符号，改由派发表单一来源）；把 `docs_machine_consistency.py` 的该检查改为**从枚举/派发表解析集合**再与 ERROR_TAXONOMY 双向比较；四处注释里的硬计数删掉或改为"见 `PipelineStageV2`"。
- 置信：CONFIRMED

### CR-280-08 · 超时旋钮存在第二套键名 `stage_timeouts`，仅被零调用的 `run_stage2` 消费，与在活的 `execution.stage_timeout_sec` 通道互不相通
- 轴：C（主）+ R
- 位点：`orchestrator.cpp:409-432`（`parse_stage_timeouts` 读顶层 `stage_timeouts`）与 `:5407-5410`（唯一调用点，位于 `run_stage2`）
  ```cpp
  auto timeouts = parse_stage_timeouts(config_json);
  if (!timeouts.empty()) { config_.stage_timeouts = timeouts; ... }
  ```
- 上位依据：`orchestrator/README.md:50` 登记的 `execution` 字段是 `stage_timeout_sec`；`configs/stage1.schema.json:322,343` 与内嵌副本 `json_config.cpp:353,374` 同；在活通道在 `:5082-5091`（typed `cfg.execution.stage_timeout_sec` 经 `kTimeoutKeyMap` 翻译后写入 `config_.stage_timeouts`，11 个键全覆盖，读取点 `:5199-5202`）。检索式：`git grep -n "stage_timeouts" -- "*.json" docs/`（**零命中**，仅 `orchestrator.h:243` 字段与 `:531` 注释给出的 `{"stage_timeouts":{"READ_FITS":10.0}}` 格式样例）。
- 现状：`stage_timeouts`（顶层、大写 stage 名）与 `execution.stage_timeout_sec`（嵌套、小写 gate 名）是同一旋钮的两套词法；前者只被 `Orchestrator::run_stage2`（`:5388` 定义）消费，而 `git grep -n "run_stage2" -- lib/` 显示该成员**无生产调用方**（仅 `.h:358` 声明与定义），且其注释 `:5385`「串行执行 2 个 stage: GRADIENT_SPHERE -> STACK」对应的 legacy 节点已按 `orchestrator.h:107`「legacy Stage2 (GRADIENT_SPHERE/STACK) removed」移除。
- 差在哪：实现保留了一条按文档头注释格式（`:531`）配置就会**静默失效**的输入通道，同时真实通道不认该格式；被这条通道覆盖的 `allow_partial_output`（`:5413`，同一死入口）同理恒取默认 false。
- 后果：无人受影响的死通道，但它使"stage 超时保护"的对外表述（`README.md:87`「每阶段受 `stage_timeout_sec` 保护」）与实现面存在两套读取路径；后续若有人按 `:531` 注释格式配 stage1 超时，会被 root schema 的 `additionalProperties` 直接拒（`json_config.cpp:236-` 内嵌 schema），故障形态是"配置被拒"而非"配置被忽略"，因此当前不产生 fail-open。
- 定级：**S3**（缺生产触发路径：唯一消费入口 `run_stage2` 无调用方）
- 怎么算修好：删除 `parse_stage_timeouts` 与 `:5407-5410`、`:5413-5418`，或把 `run_stage2` 也改走 typed 配置 + `kTimeoutKeyMap`；`:531` 的格式注释同步订正为合同真实键名。
- 置信：CONFIRMED

### 格 2"不报的核过项"（附检索式）
1. **`request_cancel`（`:389-391`）的 signal-safety 处理正确**：只做一次 `atomic store`，日志移到主循环消费点（`:493`/`:499`/`:5214`）；`:384-388` 注释与实现一致，不报"handler 内加锁"。检索式：`git grep -n "request_cancel" -- lib/`。
2. **`cleanup_partial_output` 的错误处理本身不吞码**：`:446-451`、`:456-461`、`:466-471`、`:477-484` 每条 `std::error_code` 都被检查并 `LOG_ERROR` + `return false`（缺陷在判据与调用点丢弃，已并入 CR-280-06，不重复立条）。
3. **`compute_array_median` 偶数分支**（`:287-290`、`:319-321`）：`nth_element` 后 `tmp[mid]` 为第 (mid+1) 小、下半区最大为第 mid 小，n=4 给 (2nd+3rd)/2 ⇒ 与中位数定义一致，不报。
4. **`kTimeoutKeyMap`（`:5084-5088`）覆盖完整**：11 个 gate 全在表内且与 `configs/stage1.template.json:60-70` 的 11 键一一对应 ⇒ 不存在"未映射键静默丢超时"（该结论对 stage1 成立；stage2 见 CR-280-08）。
5. **`get_current_stage_name`（`:402-405`）的 `const_cast`+lock**：`mutex_` 在 const 成员函数里加锁的常见写法，未见双锁路径，不报 UB 指控。
6. **`:305-332` 的 double 重载族**：`:304` 注释"FP64 模式下直接在 double 上计算统计, 不降级到 float32"与调用点 `:1205-1213`（f64 分支）/`:1216-1224`（f32 分支）一致，不报"双轨取错精度"。

## 格 3 · 行 501–750（构造/`load_config`/状态查询/检查点/`init_dlls` 根目录推导）

### §2.5 数值与默认值处置表（格 3）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :573 | `config_.log_dir` 缺省 | `"run/logs/orchestrator"` | 未指定时的日志目录 | 待确认 | `AGENTS.md §3/§7`「日志一律落 `run/<task>/logs/`」——该默认值无 `<task>` 层，且相对 CWD 解析；`docs/contracts/LOG_AND_ERROR_CONTRACT.md` 未登记该默认（`git grep -n "run/logs/orchestrator" -- docs/` 零命中） | 方向：应落带 run/任务 ID 的目录；影响范围=日志落点可散落到任意 CWD | CONFIRMED |
| :576 | `config_.output_dir` 缺省 | `"run"` | 未指定时的输出根目录 | 结构性不适用 | 兜底目录字面量，不承载科学语义；真实路径由 `output.hips` 驱动（`:5112-5118` 空路径校验） | — | CONFIRMED |
| :580 | 检查点子目录名 | `".checkpoint"` | 检查点落盘子目录 | 结构性不适用（该机制本身另立 finding） | 目录名字面量；机制与最高设计冲突 ⇒ 见 CR-280-10 | — | CONFIRMED |
| :587,:592 | `log_level` 键与缺省 | `"log_level"` / `"INFO"` | 顶层日志级别键 + 兜底级别 | **待确认** | 顶层 `log_level` 在合同面**不存在**：`configs/stage1.schema.json:6` 根 `additionalProperties:false`，声明的属性里无 `log_level`（`git grep -n "log_level" -- lib/infrastructure/pipeline/orchestrator/configs/ lib/infrastructure/pipeline/orchestrator/cpp/src/json_config.cpp` 零命中）⇒ 该读取对任何通过校验的配置恒为空，级别永远由 `:592` 兜底为 INFO 或 CLI 覆盖 | 方向：应删除死通道或把键登记进 schema；影响范围=仅日志详略 | CONFIRMED |
| :599 | `threads` 键 + 兜底 | 顶层 `"threads"` / `0.0` | 顶层线程数键 + "自动检测"兜底 | **待确认** | 合同面该键在 `execution.threads`（`configs/stage1.schema.json:339`、`json_config.cpp:741`），**顶层 `threads` 不属于 schema**；根 `additionalProperties:false` ⇒ 用户按本函数预期写顶层 `threads` 会被整份配置拒绝；真实值在 `:5080` 由 typed 配置覆盖 ⇒ 见 CR-280-12 | 方向：删死读取；影响范围=`:601` 日志行的"配置线程数"恒为 0 | CONFIRMED |
| :600 | double→int 截断 | `static_cast<int>` | 线程数取整 | 结构性不适用 | 类型转换；`execution.threads` 本身是 integer，截断路径只在死通道上 | — | CONFIRMED |
| :642-645 | `get_memory_usage()` | 恒 `0` | 内存使用查询 | **待确认** | 文件头 `:10` 与 `:643` 自称"已知限制…返回 0（见 docs/KNOWN_LIMITATIONS.md）"；`docs/KNOWN_LIMITATIONS.md:58` 现行口径是「内存预算 = 实测可用内存 × 比例（默认 95，唯一数值源 `eng/packaging/config/runtime_resources.json`）…取值 = 实测估算（**0 不作占位值**）」；`git grep -n "get_memory_usage"` 除 `.h:279` 声明与本定义外**零调用方** ⇒ 见 CR-280-13 | 方向：删除该方法或改为调 `aio_sysinfo`；影响范围=目前为零（无消费者），但登记面声称的限制实为死代码 | CONFIRMED |
| :649-653 | 线程数兜底 | `hardware_concurrency()`，再兜底 `1` | 未配置时的 worker 数来源 | **待确认** | `ASTROCS_DESIGN.md:673`「可用 CPU = **亲和性 ∩ cgroup ∩ Job Object**；worker 数只来自 profile 与预算对象，模块不硬编码线程数」；本仓已有合规口径 `lib/include/astrocs/abi/host_api_v1.h:65` `available_cpus; /* affinity∩cgroup∩Job Object */`、`core/runtime.h:107` `cpu_budget`（CLI 侧 `lib/infrastructure/cli/commands.cpp:100 cli_affinity_cpu_count`）、profile 缺失时 `lib/infrastructure/benchmark/backend_host/bench_harness.cpp:232-235 no_profile_policy(available_cpus)` ⇒ 见 CR-280-12 | 方向：应取 `cpu_budget`/`available_cpus`；影响范围=容器/Job Object 受限环境下的过度订阅（帧级并发与 OpenMP 线程） | CONFIRMED |
| :666-667 | 新建检查点初值 | `current_stage_id = 0`、`fully_completed=false` | load 失败时凭空创建的检查点内容 | 待确认 | 机制与最高设计冲突（CR-280-10），数值本身是枚举起点 | — | CONFIRMED |
| :675 | 完成哨兵 | `rs < 0` | "全部完成"的返回码解释 | 结构性不适用 | 哨兵符号判定；注意 `:675` 用 `rs<0`，而 `:681` 用 `rs==0 && !exists` ⇒ 同一函数内两种"无检查点"表达 | — | CONFIRMED |
| :688-693 | stage_id→枚举映射 | 0/1/2/3 + default→CALIBRATE | v1 四阶段恢复表 | 结构性不适用 | 枚举映射；`default` 把任何未知 id 静默当作"从头再跑"（不报错），但该函数零调用方 ⇒ 入 CR-280-10 的死面清单 | — | CONFIRMED |
| :719 | `MAX_PATH` | 260 | Win32 路径缓冲 | 结构性不适用 | API 定义常数 | — | CONFIRMED |
| :722,:725 | 目录回退级数 | **4** | "向上 N 级得到 `<root>`" | **待确认（缺陷）** | 注释 `:716/:722` 断言 exe 在 `<root>/lib/infrastructure/pipeline/orchestrator/cpp/`（该位置正确：`cpp/Makefile:83 TARGET = orchestrator.exe` 无目录前缀），而该位置到 `<root>` 需回退 **6** 级；4 级实得 `<root>/lib/infrastructure` ⇒ 见 CR-280-09 | 方向：级数应为 6，或直接改为"向上找含 `CMakeLists.txt`+`lib/` 的目录"；影响范围=Windows 上全部 DLL 加载与 `project_root_dir_` 派生路径 | CONFIRMED |
| :726 | 搜索起点 | `pos - 1` | `find_last_of` 递进 | 结构性不适用 | 索引算术（`find_last_of(set,pos)` 含 `pos`，故 −1 正确避免重复命中同一分隔符） | — | CONFIRMED |
| :736 | 推导失败兜底 | `"."` | base_dir 兜底 | 待确认 | 与 `:725` 的 4 级缺陷叠加：Linux 下 `#ifdef _WIN32` 整段不编译 ⇒ 恒走 `"."`，Windows 下恒走 4 级结果 ⇒ 两平台不同基准目录；见 CR-280-09 | 方向：应显式失败或从环境变量/安装目录取，不靠 CWD | CONFIRMED |
| :743 | `project_root_dir_` | = base_dir | 供相对路径派生的根 | 待确认 | 消费点在 `:1499/:1510`（gaia/sdet DLL 绝对路径拼接）与 `:1967`（plate_solve 日志目录），均属 CR-281 行段 ⇒ 合并待办 | — | PARTIAL |

**格 3 数值项合计：16 行**

### 格 3 findings

### CR-280-09 · "向上 4 级得到项目根目录"实为 `<root>/lib/infrastructure`，与注释自陈的 exe 位置差两级，且 `DllLoader` 无候选回退
- 轴：R（主）+ C
- 位点：`orchestrator.cpp:716-738`
  ```cpp
  // 自动推导项目根目录: orchestrator.exe 位于 <root>/lib/infrastructure/pipeline/orchestrator/cpp/
  ...
  for (int i = 0; i < 4; ++i) {
      pos = ep.find_last_of("\\/", pos == std::string::npos ? std::string::npos : pos - 1);
  ```
  注释 `:722-723`「exe_path = `<root>/lib/infrastructure/pipeline/orchestrator/cpp/orchestrator.exe` / 向上 4 级目录得到 `<root>`（去掉 cpp/orchestrator/lib 三级 + 文件名）」。
- 上位依据：`DllLoader::get_default_path`（`cpp/src/dll_loader.cpp:65-84`）的子路径全部以 **项目根为基准**（`lib/infrastructure/aio/`、`lib/algorithms/calibration/`、`lib/algorithms/photometry/cpp/`…），`load_all` 亦按 `lib_base_dir + "/lib/infrastructure/aio/"` 拼接（`dll_loader.cpp:266-271`、`:294-299`）⇒ 传入值必须是 `<root>`。`DllLoader::load_module` 只构造**一条**路径并做存在性检查、无回退（`dll_loader.cpp:136-165`：`info.status = NOT_FOUND; return false;`）。exe 真实位置由 `cpp/Makefile:83 TARGET = orchestrator.exe`（无输出目录前缀）确定为该 Makefile 所在目录。
- 现状：把注释自陈的路径按 C++ `find_last_of` 语义逐跳展开：4 次迭代依次命中「/orchestrator.exe」「/cpp」「/orchestrator」「/pipeline」前的分隔符，`substr(0,pos)` = `<root>/lib/infrastructure`；要得到 `<root>` 需 6 次迭代（还要过「/infrastructure」「/lib」）。注释里"去掉 cpp/orchestrator/lib 三级"的清单本身也自相矛盾（列了 3 个目录名却迭代 4 次，且漏了 infrastructure/pipeline）。同一函数在 `orchestrator.h:332` 又给出第三种说法：「默认 ".."（从 cpp 目录上一级）」——`".."` 也得不到 `<root>`。同一段推导还被复制进 `cpp/src/main.cpp:91-106`（注释逐字相同，用于 `--inspect`）。
- 差在哪：三处文字（`:723`、`h:332`、`main.cpp:91`）互相不一致且都与实际算术结果不符；结果不是 `<root>` 而是 `<root>/lib/infrastructure`。
- 后果：Windows（AGENTS §2 正式平台）上任何走 `init_dlls("")` 的路径都以 `<root>/lib/infrastructure/lib/...` 找 DLL ⇒ 全部 `NOT_FOUND` ⇒ `run_stage1` 的 `all_required` 判定（`:5125-5138`）返回 `AstroCsExitCode::DLL_LOAD_FAILED`，README 登记的用法 `orchestrator.exe <stage1.json>`（`orchestrator/README.md:10`）在该平台不能工作；Linux 控制节点上 `#ifdef _WIN32` 不编译 ⇒ 恒走 `"."`（`:736`），只有当 CWD 恰为仓库根时才成立 ⇒ **同一 exe 在两正式平台的基准目录语义不同**（一个是错的固定值，一个是 CWD 相对）。
- 定级：**S2**（缺运行时触发路径：本机为 Windows 但按只读纪律不得构建/运行 `orchestrator.exe`；且该 target 是否仍在生产交付面存疑——`cpp/CMakeLists.txt:2` 称本目录为"legacy Phase1 orchestrator"、`:4-7` 称"在此之前不在任何构建图…改动从未被编译过"，与 `:6`「当前版本: 生产实现」直接冲突，见格 6 合并待办 M-1）
- 怎么算修好：①级数改为按"存在 `lib/infrastructure/aio/` 的祖先目录"探测（或直接消费安装目录/`cpu_profile` 所在根），并加正例断言（给定 `<root>/a/b/c/d/e/f/x.exe` 推导出 `<root>`）与负例（推不出则显式失败，不静默退 `"."`）；②三处注释统一到唯一说法；③`main.cpp:91-106` 的同一段复制应改为调用同一函数（当前是两份并行实现）。
- 置信：CONFIRMED（算术已按 `find_last_of` 含 `pos` 的语义逐跳展开；平台实跑缺证据）

### CR-280-10 · 断点续跑（检查点）机制在代码与合同登记面都存在，而最高设计明文规定"没有断点续算"
- 轴：D（主）+ C
- 位点：`orchestrator.cpp:579-582`
  ```cpp
  // Task 3: 设置检查点目录为 <output_dir>/.checkpoint/
  std::string ckpt_dir = (fs::path(config_.output_dir) / ".checkpoint").string();
  checkpoint_mgr_.set_checkpoint_dir(ckpt_dir);
  ```
  以及 `:659-698`（`save_checkpoint` / `load_checkpoint`，含"已全部完成 ⇒ `resume_from = PipelineStage::STACK`"的续跑语义）、`:703-706`（`set_checkpoint_dir`）。
- 上位依据：`ASTROCS_DESIGN.md:90`（逐字）「三个命令是平级独立命令，各自独立启动、独立重跑、独立验收。重跑等于新运行目录加新 manifest，**没有断点续算**。」对照 `docs/contracts/API-001.md:62`「取消经 CancellationToken 传播；**checkpoint 只在 Artifact 原子提交后写入**」——两份上位/合同文档对该机制的存在性说法相反。
- 现状：实现按"有检查点"建模（`load_config` 主动建目录、`load_checkpoint` 决定起点），且 `docs/contracts/API_CONTRACTS.csv:137-153` 把 `CheckpointManager` 全部 17 个符号 + `:198 API-load_checkpoint` 标为 `VERIFIED` 公开合同；`eng/ci/ledgers/dormant_algorithms.json` 中 `checkpoint` 关键字 **0 命中**（`git grep -c` 复核），即既未登记退役也未登记 dormant。
- 差在哪：一个最高设计明令不存在的机制，同时占据实现面与"已验证合同"登记面，且无任何一处登记它的适用域；`ASTROCS_DESIGN.md:90` 与 `API-001.md:62` 打架时本文件选了后者。
- 后果：①对外合同声称的 `VERIFIED` 能力在生产调用图里根本没有调用方（`git grep -n "save_checkpoint\|load_checkpoint" -- lib/` 除声明/定义外零命中，仅 `eng/tests`…见下）⇒ 与"设计禁止"两罪并罚：既不合规又不可达；②`load_config` 每次运行都会在 `<output_dir>/.checkpoint/` 落目录，与 §90"重跑=新目录+新 manifest"的隔离口径冲突。
- 定级：**S2**（不改产品字节，但属"设计禁的东西在此发生 + 合同冒认已验证"；触发路径到 `load_config` 成立，再往后无生产调用方）
- 怎么算修好：按 §90 取唯一方向——删除 `Orchestrator::save_checkpoint/load_checkpoint/set_checkpoint_dir` 与 `:579-582` 的目录设置，把 `CheckpointManager` 从 `API_CONTRACTS.csv` 撤下或改标"不适用/退役（附 DISP 编号）"，并在 `docs/contracts/API-001.md:62` 同步订正；若负责人裁定保留该机制，则须反向订正 `ASTROCS_DESIGN.md:90` 并给出 manifest 隔离规则。**同批须核**：`eng/tests/.../test_checkpoint.cpp` 与 `eng/ci/ctest_baseline.json:44`（该 CMakeLists 在册）会因删除而需同步移除，避免"改对反而判红"。
- 置信：CONFIRMED

### CR-280-11 · dark 曝光时间取自文件名字面量、解析规则比工具面正则宽松，失败/半解析值与合法值混在同一返回域（位点 `:258-271`，消费点在格 5）
（编号顺延说明：位点属格 2 行段，判定所需消费点在 `:1079`、`:1281-1282`，故挂此写全。）
- 轴：S（主）+ R
- 位点：`orchestrator.cpp:259-270`
  ```cpp
  const std::string tag = "_EXPOSURE-";
  size_t p = path.find(tag); if (p == std::string::npos) return -1.0;
  p += tag.size();
  size_t end = path.find('s', p); if (end == std::string::npos) return -1.0;
  std::string num_str = path.substr(p, end - p);
  try { return std::stod(num_str); } catch (...) { return -1.0; }
  ```
- 上位依据：`docs/science/NOISE_MODEL.md` / `docs/algorithms/CALIBRATION_ALGORITHMS.md` 中 dark 扣除的暗流以 **EXPTIME 观测量**为输入（无一处规定"从文件名解析"）；`git grep -n "_EXPOSURE-" -- docs/` 零命中，命名约定只活在工具面：`eng/tools/realdata/match_plan.py:81` `(?:_EXPOSURE-(?P<exp>\d+(?:\.\d+)?)s)?`、夹具清单 `lib/algorithms/calibration/batch_config.json:11-13`（键即曝光秒数）。
- 现状：本实现右界用 `find('s')`、数值转换用 `std::stod`，**允许任意前缀可解析**：`_EXPOSURE-1,80s` → `stod("1,80")` = **1.0**（不抛异常，不报失败）；`_EXPOSURE-180.00s` 正常；而 `match_plan.py` 的同族解析只接受 `\d+(\.\d+)?s`。失败哨兵 −1.0 与合法域（>0）不重合 ⇒ 方向上可判失败，但**半解析得到的 1.0 落在合法域内**，下游无从分辨。
- 差在哪：一个科学输入（dark 曝光秒数）的取值来源是文件名约定，而该约定在权威链无登记；且解析比本仓自己的工具实现宽松，宽松的那部分会把错值当合法值放行。
- 后果：dark 曝光错 ⇒ `ACTUAL_K`/dark 扣除速率错（`:1276` 写入交付物 `cal_stats.ACTUAL_K`）⇒ 校准后像素值与 `cal_stats` 记录同时失真，而 `cal_stats` 是 `:1262` 自称的"交付物"。
- 定级：**S2**（触发需一个不合 `match_plan.py` 语法却含 `_EXPOSURE-` 的文件名；缺该形态的在册样本 ⇒ 缺运行时触发路径）
- 怎么算修好：①优先从 dark 文件 FITS header 的 `EXPTIME` 取值，文件名只作兜底并记录来源；②解析改为与 `match_plan.py:81` 同严格度的全串匹配（整段必须匹配 `\d+(\.\d+)?`），否则返回 −1.0；③把该命名约定登记进 `docs/contracts/CONFIG_CONTRACT.md` 或 `DATA_SEMANTICS.md`；④补负例：`_EXPOSURE-1,80s`、`_EXPOSURE-abc s`、`_EXPOSURE-0s` 三种输入均判失败。
- 置信：CONFIRMED（`stod` 部分解析语义与消费点均已读到；未实跑注入）

### CR-280-12 · `load_config` 的 `threads`/`log_level` 读取用的是合同里不存在的顶层键，其日志行在任何真实配置下都失真
- 轴：C（主）+ R（④"记录为本次参数但实际未生效"）
- 位点：`orchestrator.cpp:599-602`
  ```cpp
  double cfg_threads = orc_getJsonNum(content, "threads", 0.0);
  config_.threads = static_cast<int>(cfg_threads);
  LOG_INFO("orchestrator", "配置线程数: " + std::to_string(config_.threads)
           + (config_.threads == 0 ? " (0=自动检测)" : ""));
  ```
  同族 `:587-596`（`log_level`）。
- 上位依据：`configs/stage1.schema.json:6` 根 `"additionalProperties": false`，线程数的合同位置是 `execution.threads`（`:321`、`:339`），日志级别**在 schema 中根本没有对应属性**（`git grep -n "log_level" -- lib/infrastructure/pipeline/orchestrator/configs/ .../json_config.cpp` 零命中）；真实赋值在 `:5077 config_.threads = cfg.execution.threads;`（run_stage1 内，晚于本行）。
- 现状：`content` 是 `:561` 读入的原始配置文件文本，顶层没有 `threads` ⇒ `orc_getJsonNum` 走 `:243 return def` ⇒ `config_.threads = 0` ⇒ **每次运行**都在 `:601` 记一条"配置线程数: 0 (0=自动检测)"；随后 `:5080` 才把 `execution.threads`（模板里就是 0，真实任务可给非 0）写入同一字段。`get_thread_count()`（`:648-653`）因此有两条完全不同的取值路径，其中"日志里那条"不是生效的那条。
- 差在哪：日志把"未读取到的兜底值"记成"本次配置值"；同时留下一条按 `:599` 意图写配置就会被 schema 整份拒绝的伪通道。
- 后果：①运行证据（log jsonl）中"配置线程数"与产品实际并行度不对应，任何以日志为准的复现/审计都会读错；②`log_level` 同理（`:596` "日志级别设置为: INFO" 恒为兜底值，除非 CLI `--log-level` 先写 `config_.log_level`）；③线程数兜底本身还绕开设计口径（`ASTROCS_DESIGN.md:673` 要求 worker 数只来自 profile 与预算对象，见格 3 表 :649-653 行）⇒ 容器内按物理 CPU 数订阅。
- 定级：**S2**（不改产品科学值，但直接污染运行证据面 + 违反 §673 的线程预算来源；`get_thread_count()` 的返回值确实经 `dll_loader_.set_num_threads(...)` 进入各模块，见 `docs/architecture/execution_inventory.csv` 行 2 记录的 `orchestrator.cpp:767`）
- 怎么算修好：删除 `:599-602`、`:587-596` 两条死通道，把日志行移到 `:5080` 之后并按真实生效值打印（含来源：`execution.threads` 或 profile）；`get_thread_count()` 的兜底改取 `cpu_budget`/`available_cpus`（`lib/include/astrocs/core/runtime.h:107-113`、`cli/commands.cpp:100`）而非 `hardware_concurrency()`。
- 置信：CONFIRMED（键不存在、赋值时序、日志内容三处均逐行读到）

### 格 3"不报的核过项"（附检索式）
1. **`load_config` 的三重前置校验齐备不冒进**：`:539-543` 空路径、`:545-549` 不存在、`:552-557` 打不开，各自 `error_msg` + `LOG_ERROR` + `return false`，不报"错误被吞"。
2. **`:561` 整文件读入未检查读失败**（`ss << ifs.rdbuf()` 的 `badbit` 未查）：`ifstream` 以 `binary` 打开且已 `is_open()` 通过，短读会得到空串 → 后续 `orc_getJson*` 全部走缺键兜底；该形态与 CR-280-01 同族（解析失败降级），不单独立条，合并到 CR-280-01 的修复里一并处理。
3. **`state_` 是 `std::atomic<TaskState>`**（`orchestrator.h:420`），故 `pause/resume/interrupt`（`:619-632`）的直接赋值不构成数据竞争；`cancel_token_`/`timeout_flag_`/`stage_watchdog_stop_` 同为 atomic（`:429/:432/:435`）⇒ 不报"无锁状态竞态"。
4. **`~Orchestrator`（`:522-531`）先 `interrupt()` 再 `join()`**：工作线程回收顺序正确，且 `cleanup_platesolve_env()` 在 join 之后调用 ⇒ 不报"析构内使用已释放句柄"。
5. **`init_dlls` 的 ABI 一致性检查入口**（`:748-750` 起，"Gate 1 (Dataflow_ABI_Contract): AIO 是共享 ABI 基础, 结构不匹配硬失败"）：注释与实际 `get_function<const AstroAbiInfo*(*)(void)>` 取符号做法一致，判定分支在 751 行以后，留格 4 复核（不在此处下结论）。

## 格 4 · 行 751–1000（AIO ABI 握手 / 模块加载汇总与线程数 / `run_stage_calibrate` 前置与常量组）

### §2.5 数值与默认值处置表（格 4）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :759 | `abi->abi_version == 1` | 1 | AIO ABI 主版本握手 | 结构性不适用 | ABI 协商常数，属 `C_ABI_STANDARD`/`Dataflow_ABI_Contract` 语义（`:748` 注释点名 Gate 1），不进科学量 | — | CONFIRMED |
| :760-763 | 结构尺寸四项比对 | `sizeof(void*)/sizeof(PipelineFrame)/sizeof(AioBlock)/sizeof(AioKVEntry)` | 布局一致性硬门 | 公式导出（结构等价判定） | 由头文件定义式算出，代码真按式算 ⇒ 合规；**注意 `abi->struct_size` 只在 :771 被打印、从不参与判定** ⇒ 见 CR-280-13 | — | CONFIRMED |
| :781-785 | 必需模块集合 | 7 个（AIO/CALIBRATE/PLATESOLVE/PSF/PHOTOMETRIC/SNR/DRIZZLE） | 失败信息收集与必需性判定 | 结构性不适用 | 枚举基数；与 `load_all` 实际加载集合一致 | — | CONFIRMED |
| :777,:800,:801 | 日志文案"9 个模块"/"9 节点" | **9** | 加载成功日志的模块计数 | **待确认（失真）** | 同一函数 `:802-804` 的循环只遍历 **7** 个 ModuleId，`:781-785` 的必需集合也是 7 ⇒ 见 CR-280-13 | 方向：文案应与被枚举集合同源（用 `ids.size()`） | CONFIRMED |
| :811-812 | `set_num_threads(CALIBRATE, threads)` | 仅 CALIBRATE | 把线程数下发给模块 | 待确认 | `ASTROCS_DESIGN.md:673`「一个进程一个资源调度器与线程预算源…模块不硬编码线程数」——PSF/PLATESOLVE/PHOTOMETRIC/SNR/DRIZZLE 五个模块**无下发通道**（`:810` 注释自陈"其他模块暂无 set_num_threads 接口"），其 OpenMP 度由运行时默认值决定 ⇒ 与 CR-280-12 同族，合并处理 | 方向：统一由预算对象下发；影响范围=帧内并行度与内存峰值 | CONFIRMED |
| :900-902 | FP64/FP32 必需符号判据 | — | 双精度 ABI 的指针齐备性检查 | 结构性不适用 | 逻辑判据；缺符号 ⇒ `:903-907` ERROR + 非零退出码 ⇒ fail-closed 正确 | — | CONFIRMED |
| :917 | 缺 data 块退出码 | `BLOCK_MISSING` | 必需块缺失的硬失败 | 结构性不适用 | 枚举值，符合 `orchestrator.h:118`「必需阶段/DLL/块失败必须返回非零退出码」 | — | CONFIRMED |
| :920-922 | `dims[1]→width`、`dims[0]→height`、`n_pix` | 行优先 [H,W] | 块维度解释 | 结构性不适用 | 数组维度约定（`[H,W]`，与 `:1239 int dims[2] = {height, width}` 写回一致）；`static_cast<int64_t>(width) * height` 已先升位，无 int 溢出 ⇒ 不报 | — | CONFIRMED |
| :938 | `EXPTIME` 缺省 | `0.0` | 帧曝光时间取不到时的兜底 | **待确认（缺陷）** | `0` 与"合法曝光 0"重合；该兜底值直接决定 `:1078` 的曝光匹配判据是否执行 ⇒ 见 CR-280-16；`docs/science/CALIBRATION.md:231`（预检/科学两面分离） | 保守方向：缺 EXPTIME 应显式判失败（母版曝光门无输入即不可判） | CONFIRMED |
| :943-945 | `CCD-TEMP` 缺省 | `0.0` + `has_ccd_temp` 双轨 | 温度是否存在的分离表达 | 结构性不适用（正向例） | 与 :938 相反，这里**先判存在再取值**，是正确形态；但因 `require_temp=false` 该信息链整体不可达 ⇒ 见 CR-280-14 | — | CONFIRMED |
| :953-955 | `master_bias/dark/flat` 路径 | 来自 typed config | 母版路径来源 | 公式导出（配置驱动） | `configs/stage1.schema.json` input 块必填键；`:969` 注释「CFG-106 已修: 禁止自动推导/硬编码 testdata 目录」与实现一致 ⇒ 合规 | — | CONFIRMED |
| :960 | `require_size` | **true** | 母版尺寸匹配开关 | 待确认 | 常量写死、无配置面（`calibration` 块 `additionalProperties:false` 且只有 mode/light_exposure_s/dark_exposure_s/fallback 四键）；`docs/algorithms/CALIBRATION_ALGORITHMS.md` 未规定"可关闭尺寸匹配"⇒ 取值方向正确但**载体缺失**（不可配置、无条文） | 方向：应在合同登记"尺寸匹配恒开" | CONFIRMED |
| :961 | `require_exposure` | **true** | 母版曝光匹配开关 | 待确认 | 同上（写死为真，但 `:1078` 的 `frame_exptime > 0` 前置条件可使其整体失效 ⇒ CR-280-16） | 方向：无 EXPTIME 时应判"不可判定并登记"，见 `docs/science/CALIBRATION.md:278` | CONFIRMED |
| :962 | `exposure_tol` | **0.5**（单位 s） | 亮/暗曝光差容差 | **待确认（与合同面数值冲突）** | 合同面同一件事的键与值是 `calibration.dark_light_exposure_tolerance = 5 s`（`docs/contracts/CONFIG_CONTRACT.md:44,:65`、`eng/packaging/config/defaults.json:19`），科学判据另有其人（`docs/science/CALIBRATION.md` §6a `|K·Δb| ≤ ε·σ_frame`，且 §:257 记 `ε` 未冻结）；`0.5` 在 `docs/**` 零出处 ⇒ 见 CR-280-14 | 方向：0.5 比合同的 5 更严（判据 `diff > tol ⇒ 红`，tol 变小只会更红），故不致 fail-open，但两侧数值不同必有一侧失真 | CONFIRMED |
| :963 | `require_temp` | **false** | 母版温度匹配开关 | **待确认（缺陷）** | 写死为假且无配置面 ⇒ `:1100-1121` 温度判据整体死码、`:1284-1285` 的 `DARK_CCD_TEMP` 交付字段永不出现；`docs/science/NOISE_MODEL.md:123` 明列暗电流"逐帧变（**温度**、曝光）"⇒ 见 CR-280-14 | 方向：温度不匹配会静默放行（偏绿）；`temp_tol` 的 1.0 °C 同样无出处 | CONFIRMED |
| :964 | `temp_tol` | **1.0**（单位 °C） | 温度差容差 | **待确认** | 条文面零提及（`git grep -n "温度容差\|temp_tol" -- docs/science docs/algorithms docs/contracts` 零命中），且所在分支不可达（`require_temp=false`）⇒ 见 CR-280-14 | 方向：随 :963 一并定案 | CONFIRMED |
| :965 | `allow_no_calib` | **false** | 无母版退化模式开关 | **待确认（缺陷）** | 写死 false ⇒ `:1144` 的退化分支与 `:1288` 的 `"DEGENERATED"` 永不可达；而 `:1136-1137` 的错误文案指向配置键 `allow_no_calibration`，该键在 schema 里不存在（`calibration.additionalProperties:false`）⇒ 见 CR-280-18 | 方向：要么开该键并登记，要么删掉指向不存在键的文案与不可达分支 | CONFIRMED |
| :966 | `dark_opt` | **0** | 传给内核的暗场优化模式 | **待确认（缺陷）** | 与 `calibration.mode`（`enum: standard\|optimal\|exposure_ratio`，schema 必填）无任何映射 ⇒ 见 CR-280-17 | 方向：应由 mode 决定；影响范围=交付物 `cal_stats.DARK_OPTIMIZATION` 恒 0 | CONFIRMED |
| :967 | `dark_k` | **1.0** | 暗场标度 K 初值 | **待确认（缺陷）** | `lib/algorithms/calibration/include/astro_calibration.h:98`「`dark_scale_factor`: K初始值（**如 Light曝光/Dark曝光**）」——内核明示应取曝光比，实现恒给 1.0，而两个曝光值就在 :958-959 手上 ⇒ 见 CR-280-17 | 方向：K=t_light/t_dark；影响范围=暗场扣除标度（像素值本身） | CONFIRMED |
| :971-983 | mode → 路径必需性 | standard 需 dark+flat；其余需三者 | 唯一使用 `calib_mode` 的地方 | 待确认 | 只校验"路径字符串非空"，不校验"母版是否真的加载成功"⇒ 见 CR-280-15 | 方向：应对 `has_*` 判定；影响范围=平场/暗场整步被跳过 | CONFIRMED |
| :1006-1008 | `dark_exptime=-1.0` / `dark_temp=0.0` / `dark_has_temp=false` | 见左 | 母版元数据初值 | 待确认 | −1.0 是哨兵（正确）；`dark_temp=0.0` 与"0 °C 合法值"重合但被 `dark_has_temp` 分离（该形态正确，唯分支不可达）| — | CONFIRMED |

**格 4 数值项合计：19 行**

### 格 4 findings

### CR-280-13 · AIO ABI 握手"硬失败"不改 `init_dlls` 返回值：函数照样打日志"全部 9 个模块加载成功"并 `return true`，且 9 与实载 7 个模块不符
- 轴：R（主）+ ⑤（宣称 fail-closed 与实际行为的矛盾）+ C
- 位点：`orchestrator.cpp:745-746`、`:764-767`、`:776`、`:801`、`:815`
  ```cpp
  bool ok = dll_loader_.load_all(base_dir);
  dlls_loaded_ = ok;
  ... if (!abi_ok) { error_msg = "AIO ABI 不匹配 (硬失败, 不再继续)"; LOG_ERROR(...); dlls_loaded_ = false; }
  if (!ok) { ... return false; }        // ABI 失败时 ok 仍为 true ⇒ 不进入
  LOG_INFO("orchestrator", "全部 9 个模块加载成功");   // :801
  ...
  return true;                                     // :815
  ```
  注释 `:748`「Gate 1 (Dataflow_ABI_Contract): AIO 是共享 ABI 基础, 结构不匹配**硬失败**」、`:765` 文案「硬失败, 不再继续」。
- 上位依据：`orchestrator.h:118-119`「必需阶段/DLL/块失败必须返回非零退出码, **禁止静默跳过** (return true on skip)」；`AGENTS.md §6`「不用 facade/空骨架/no-op 冒充实现完成」。
- 现状：ABI 不匹配只把聚合标志 `dlls_loaded_` 置假，`ok`（`load_all` 的返回值）仍为真 ⇒ `if (!ok)` 不触发 ⇒ 继续执行 `:801` 成功日志、`:802-808` 版本打印、`:811-812` 线程下发，并 `return true`；`error_msg` 已写入"ABI 不匹配"但被调用方按成功处理。`struct_size`（`:771`）被读出来打进日志却不在 `:758-763` 的判定式里。
- 差在哪：一个自称"硬失败、不再继续"的门，在函数契约（返回值）层面根本没有失败；对外记录的是"全部模块加载成功"。
- 后果：①运行证据出现与事实相反的"成功"行，事后审计/复现读到的模块清单失真；②真正的失败原因被下游掩盖——各 stage 的 `if (!dlls_loaded_ || !is_loaded(...))`（本行段 `:825`；全文件 12 处）会以"CALIBRATE DLL 未加载"判红，`run_stage1` 的 `all_required` 分支（`:5125-5138`）因 `init_dlls` 返回真而**根本不执行** ⇒ 退出码与错误消息都指向错误原因；③`run_stage_nside`（`:4879-4924`）是唯一**不做** `dlls_loaded_` 检查的 handler（脚本核验：11 个 `run_stage_*` 中只有它两者皆无），它靠 `get_function` 返回空指针间接兜住，属偶然安全。
- 定级：**S2**（不直接产生错值产品——下游 stage 仍判红；失真的是门禁语义与运行证据）
- 怎么算修好：ABI 不匹配时 `return false`（并保留 `error_msg`），或在 `:776` 把条件改为 `if (!ok || !dlls_loaded_)`；`:801` 文案用 `ids.size()` 生成；`struct_size` 要么进判定式要么不打。同批须核 `run_stage1` 内 `if (!init_dlls(...)) { if (!all_required) ... }` 的语义是否要改成"任何 ABI 失败即终止"。
- 置信：CONFIRMED（控制流逐行读通；未实跑注入 ABI 不匹配）

### CR-280-14 · 校准母版三面防护（尺寸/曝光/温度）里温度门被常量永久关闭、曝光容差与合同面数值不同、且三者的开关都不在配置与合同面上
- 轴：S（主）+ C + R
- 位点：`orchestrator.cpp:963` `bool require_temp = false;` 与 `:964` `double temp_tol = 1.0;`；`:962` `double exposure_tol = 0.5;`；`:1100-1121`（温度判据）、`:1078-1097`（曝光判据）
- 上位依据：`docs/science/NOISE_MODEL.md:123` 暗电流散粒项「逐帧变（**温度**、曝光）」；`docs/science/CALIBRATION.md:231`「两个判定面必须分开（冻结）：①预检面 = `calibration.dark_light_exposure_tolerance`」；`docs/contracts/CONFIG_CONTRACT.md:65`「`calibration.dark_light_exposure_tolerance = 5 s`：**sourced**（值 5、单位 s）」；`configs/stage1.schema.json` 的 `calibration` 块 `additionalProperties:false`，属性只有 `mode/light_exposure_s/dark_exposure_s/fallback` ⇒ 温度匹配与该键面在合同上无承载（`eng/ci/prod_wiring_baseline.json:7` 另把该合同键登记为 `dead_config_key`）。
- 现状：温度判据的进入条件 `require_temp` 是写死的 `false` ⇒ `:1100` 条件恒假 ⇒ `:1101-1120` 整段（含 `dark_has_temp` 的赋值）不可达 ⇒ 交付物里的 `DARK_CCD_TEMP`（`:1284-1285`，条件 `if (dark_has_temp)`）**永不出现在任何产品**；曝光容差取本地常量 0.5，合同面同义键值是 5（两侧相差 10×，且代码不读该键）。
- 差在哪：①温度这一被科学面点名"逐帧变"的暗场驱动量，其匹配检查只存在代码形状、被判据常量关掉，且关掉它的是字面量而非配置或条文；②同一个"曝光容差"在实现与合同两侧取不同数值。
- 后果：①不同温度母版（暗电流随温度指数变化）被无警告放行，产品像素的暗场扣除失准，而 `cal_stats` 里连"温度未检查"这一事实都留不下（字段永不写出）；②合同声称的预检面阈值 5 s 与实际执行的 0.5 s 不一致 ⇒ 任何以合同为准的对外说明（或 `eng/tests/config/test_cfg001_contracts.py:197` 的键核对）与生产行为不同源。
- 定级：**S1**（温度门方向钉死：条件恒假 ⇒ 本应判红的母版错配**只会更绿**，且后果落在像素值与 `cal_stats` 交付字段）。触发路径：CLI → `run_stage1` stage 表 → `run_stage_calibrate`（`:821`）→ `:963 require_temp=false` → `:1100` 恒假 → `:1284` 永不写 `DARK_CCD_TEMP` → 校准值经 `:1241` 写回 `data` 块进入产品。
- 怎么算修好：把三个开关与两个容差改为**配置键并登记**（`CONFIG_CONTRACT.md` 已有 5 s 的键，改读它即消除冲突），温度门默认值取哪一侧须由负责人裁（若裁"不启用"，则应删除死码并把 `DARK_CCD_TEMP` 从交付字段清单撤下）；补负例：注入 `dark CCD-TEMP` 与帧差 5 °C 的母版 ⇒ 现网必须判红。防判红：`eng/tests/config/test_cfg001_contracts.py:197,:235` 与 `eng/packaging/config/defaults.json:19` 需同批对齐。
- 置信：CONFIRMED（不可达性由 `require_temp` 的唯一赋值点 `:963` 与 `:1100` 的合取式直接判定）

## 格 5 · 行 1001–1250（母版装载 lambda / 尺寸·曝光·温度三面验证 / 退化模式 / 缓冲分配 / 校准内核调用 / 统计）

### §2.5 数值与默认值处置表（格 5）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :1013 | `path.empty()` | 空串 | 未配置母版时不加载 | 结构性不适用 | 哨兵判空 | — | CONFIRMED |
| :1015,:1023 | 母版装载失败级别 | WARN / ERROR | 文件不存在 vs 读取失败 | **待确认（缺陷）** | 两者都只 `return false` 不改流程，且 `run_stage_calibrate` 从不因 `has_*=false` 判红（除非三者全缺，`:1134-1145`）⇒ 见 CR-280-15 | 方向：`calibration.mode` 声明为必需的母版，缺失应判红 | CONFIRMED |
| :1018-1020 | XISF 优先 / FITS 回退 | — | 母版容器读取顺序 | 结构性不适用 | `:876` 注释「XISF 优先, FITS 回退 — Gate 5 数据流修复」，与实现一致 | — | CONFIRMED |
| :1035 | `convert_buf.assign(pix_f32, pix_f32 + (size_t)w*h)` | — | float32 母版升 double | 公式导出 | 逐元素类型提升；`(size_t)w*h` 先升位无溢出 ⇒ 合规 | — | CONFIRMED |
| :1056-1075 | 尺寸匹配判据 | `!=` 精确相等 | 母版与帧的逐轴尺寸比对 | 公式导出 | 判据式"逐轴相等"；代码真按式算 ⇒ 合规（开关恒开问题在格 4 :960 已登记） | — | CONFIRMED |
| :1078 | 曝光门进入条件 | `require_exposure && has_dark && frame_exptime > 0` | 三条件合取 | **待确认（缺陷）** | `frame_exptime > 0` 一项使"EXPTIME 缺失（=0）"整体绕过该门且**无 else 日志** ⇒ 见 CR-280-16；对照 `docs/science/CALIBRATION.md:278`「曝光容差判定**必须**显式降级为『不可判定』并登记」 | 方向：0 应当是"不可判定并登记"，不是"跳过" | CONFIRMED |
| :1081-1082 | 曝光差判据 | `diff = fabs(dark-frame)`; `diff > tol ⇒ 红` | 预检面判据式 | 公式导出 | 与 `CONFIG_CONTRACT.md:65` 的判定变量 `|t_light − t_dark|` 同形；但 tol 取 0.5 非合同的 5，且判定输入取自**文件名**与 header，非合同的两个必填曝光键 ⇒ CR-280-11 / CR-280-14 / CR-280-17 | — | CONFIRMED |
| :1094-1096 | 文件名解析失败处置 | 仅 `LOG_WARN` | dark 曝光取不到时的降级 | **待确认（缺陷）** | 解析失败后 `dark_exptime` 保持 −1.0，门静默通过，交付物只是不写 `DARK_EXPTIME`（`:1281`）⇒ 与 `CALIBRATION.md:278` 的"显式降级并登记"不符 | 方向：应登记"未判定"状态位 | CONFIRMED |
| :1100-1121 | 温度判据 | `require_temp && …` | 母版温度比对 | **待确认（死码）** | 唯一赋值点 `:963 require_temp=false` ⇒ 整段不可达，`dark_has_temp` 永不置真 ⇒ `:1284-1285` 的 `DARK_CCD_TEMP` 交付字段永不存在；见 CR-280-14 | 方向：温度不匹配只会被放行（偏绿） | CONFIRMED |
| :1123-1131 | 验证失败处置 | `validation_ok=false ⇒ 判红` | 母版验证的失败出口 | 结构性不适用 | `error_msg`+`CALIBRATE_FAILED`+`return false`，资源逐项释放 ⇒ 该出口本身合规 | — | CONFIRMED |
| :1134-1145 | 无母版退化判据 | `!has_bias && !has_dark && !has_flat` | 三缺才判红 | **待确认（缺陷）** | 合取式 ⇒ **只缺一个母版时继续执行**；`allow_no_calib` 恒 false 使 `:1144` 分支永不可达 ⇒ 见 CR-280-15、CR-280-20 | 方向：应为"mode 声明必需的每一个都必须装载成功" | CONFIRMED |
| :1152-1155 | 输出缓冲分配 | `malloc(n_pix*sizeof(double/float))` | 内核要求的调用方分配 | 结构性不适用 | 分配失败在 `:1156-1164` 判红 ⇒ 不报"未检查分配" | — | CONFIRMED |
| :1168-1169 | `actual_k_f32/f64` 初值 | 0.0f / 0.0 | 内核输出 K 的落地变量 | 结构性不适用 | 出参占位，随后由内核覆写 | — | CONFIRMED |
| :1171,:1179,:1183 | `dark_opt` / `dark_k` 实参 | 0 / 1.0 | 传给内核的暗场优化模式与 K 初值 | **待确认（缺陷）** | `astro_calibration.h:98`「dark_scale_factor: K初始值（如 Light曝光/Dark曝光）」；实现恒 1.0 且 `mode` 不参与映射 ⇒ 见 CR-280-17 | 方向：曝光不匹配时 K=1.0 ⇒ 暗场扣除量按错误标度，像素值直接失真 | CONFIRMED |
| :1186-1196 | 内核返回码处置 | `ret != 0 ⇒ 判红` | CALIBRATE 失败出口 | 结构性不适用 | `ret` 未被丢弃：写 `error_msg`（含 ret 值）+ `CALIBRATE_FAILED` + `return false`，释放完整 ⇒ 正向例，登记在不报清单 | — | CONFIRMED |
| :1205-1213 / :1216-1224 | 统计与母版均值 | `has_x ? mean(...) : 0.0` | 校准前后统计量与母版均值 | **待确认（缺陷）** | 未装载的母版其均值以 **0.0** 进入交付字段 `MASTER_*_MEAN`（`:1267-1269`），与"该母版均值真的是 0"不可区分；`OWNER_DECISIONS.md:147`「兜底默认值禁止与合法值重合…0 在物理上常是合法值」⇒ 见 CR-280-20（`HAS_*` 是唯一旁证，但它不在任何合同 schema 里） | 方向：应写 NaN（产品面已确立 NaN=不可用，`DATA_SEMANTICS` §4a）或省略字段并登记 | CONFIRMED |
| :1238-1248 | data 块写回 | `FLOAT64`/`FLOAT32` 按 `use_fp64` | 校准结果回写 | **待确认（缺陷）** | 写回类型取自 `use_fp64`，而 `:928-932` 取指针时**未校验 `data_block->type`**（`aio_pipeline.h:41-51` 定义该字段）⇒ 见 CR-280-18 | 方向：进函数即断言块类型与精度模式一致，不一致判红 | CONFIRMED |

**格 5 数值项合计：18 行**

### 格 5 findings

### CR-280-15 · 母版装载失败只记日志，单个缺失时以 `nullptr` 传给校准内核继续出图，且交付状态仍写 "APPLIED"
- 轴：R（主）+ ② ；判据方向：**偏绿**
- 位点：`orchestrator.cpp:1014-1017`（不存在 ⇒ WARN + return false）、`:1022-1025`（读取失败 ⇒ ERROR + return false）、`:1134`（`if (!has_bias && !has_dark && !has_flat)`）、`:1177-1183`（把可能为 `nullptr` 的 `master_dark_f32/master_flat_f32/master_bias_f32` 直接作为实参）、`:1287-1288`（`CALIBRATION_STATUS = (has_bias||has_dark||has_flat) ? "APPLIED" : "DEGENERATED"`）
- 上位依据：`json_config.cpp:573-583` 的运行时校验只对配置里**写了**的路径要求存在（`inp[key].is_null() ⇒ continue`，:576）；`orchestrator.h:118-119`「必需阶段/DLL/块失败必须返回非零退出码, 禁止静默跳过」；`docs/algorithms/CALIBRATION_ALGORITHMS.md` 把 bias/dark/flat 三步列为归约顺序的组成步骤。
- 现状：`mode=standard` 只要求 `master_dark`、`master_flat` 两个**路径字符串非空**（`:972-976`）；路径写了但文件不在/读不动 ⇒ `load_master` 返回 false ⇒ `has_flat=false`、`master_flat_f32=nullptr` ⇒ 内核收到空平场指针、平场整步不应用；`:1134` 的判红是**三缺合取**，故"只缺 flat"不判红；最后 `:1288` 因 `has_bias||has_dark` 为真而写 `CALIBRATION_STATUS="APPLIED"`。
- 差在哪：一个"必需输入缺失"被降级为一次 WARN/ERROR 日志 + 少做一步科学处理，而交付状态字段仍然肯定；
- 后果：产品 `data` 块像素缺平场校正（或缺暗场扣除），而 `cal_stats.CALIBRATION_STATUS="APPLIED"`、`STATUS="OK"`（`:1263`）同时肯定"已应用/正常"；唯一旁证 `HAS_FLAT=0` 不在任何合同 schema 里（`git grep -n "cal_stats" -- docs/ eng/contracts/` 在合同面零命中），下游无义务读它。
- 定级：**S1**。触发路径：CLI → `run_stage1` stage 表（`:5294`）→ `run_stage_calibrate`（`:821`）→ `load_master(flat_path,…)` 返回 false（`:1050`/`:1014-1025`）→ `:1134` 合取不成立 → `:1181-1183` 以 `nullptr` flat 调内核 → `:1238-1248` 写回 `data` 块 → `:1288` 记 "APPLIED"。
- 怎么算修好：把"mode 声明必需的母版"逐个以 `has_*` 判定并判红（`CONFIG_ERROR`/`CALIBRATE_FAILED`），装载失败不得以 `nullptr` 继续；`CALIBRATION_STATUS` 只在"声明的三步全部实际应用"时写 APPLIED，否则写显式部分应用态并进入产品合同；补负例：删除 flat 文件 ⇒ 必须判红。
- 置信：CONFIRMED

### CR-280-16 · 帧头缺 `EXPTIME` 时暗场曝光匹配门整体绕过，且该绕过不产生任何日志或登记
- 轴：R（主）+ ③ ；判据方向：**偏绿**（丢一项只会让"本应判红"的错配通过）
- 位点：`orchestrator.cpp:1078`
  ```cpp
  if (require_exposure && has_dark && frame_exptime > 0) {
  ```
  其中 `frame_exptime` 来自 `:938` `fn_kv_get_double(frame_, "header", "EXPTIME", 0.0)`。
- 上位依据：`docs/science/CALIBRATION.md:278`「曝光容差判定**必须**显式降级为『不可判定』并登记」；`docs/contracts/CONFIG_CONTRACT.md:65` 把该判据定为"预检面"并要求有结论；`docs/science/NOISE_MODEL.md:123` 暗电流散粒项逐帧随曝光变化。
- 现状：合取的第三项在 EXPTIME 卡片缺失或非正时直接为假 ⇒ `:1079-1096` 整段（含解析、比对、失败日志）都不执行，没有 else 分支记录"曝光门未判定"；`dark_exptime` 保持 −1.0 ⇒ `:1281` 的 `DARK_EXPTIME` 字段也不写 ⇒ 产品与日志两侧都无痕迹；随后 `:1179/:1183` 仍以 `dark_k=1.0`（CR-280-17）做暗场扣除。
- 差在哪：判据的输入缺失被实现成"判据通过"，而权威条款要求"判据不可判定并登记"。二者在后果上完全不同：前者下游看不到任何红色信号。
- 后果：暗场曝光与亮场曝光差 3 倍量级的帧也能出图（生产夹具即有 180/300/600 s 三档母版并存：`lib/algorithms/calibration/batch_config.json:11-13`），暗场扣除按 1.0 标度 ⇒ 像素值系统性偏差直接进入 `data` 块与后续 SNR/测光；对外"母版经过曝光一致性预检"的主张在该帧上不成立，且无证据可查。
- 定级：**S1**。触发路径：CLI → `run_stage_calibrate`（`:821`）→ `:938` EXPTIME 缺 ⇒ 0.0 → `:1078` 条件为假（无日志）→ `:1179` `ac_calibrate_frame_f64(..., dark_opt=0, k=1.0, ...)` → `:1241` 写回 `data`。可达性前置"帧头可无 EXPTIME"取决于 `READ_FITS`（`:4030` 起，属 CR-283 行段）是否强制该卡 ⇒ 该前置须与 CR-280..283 合并定案；若上游已保证 EXPTIME 必存在，本条降为"缺纵深断言"的 S2。
- 怎么算修好：`:1078` 拆为显式三态——判定通过 / 判定失败判红 / **输入缺失 ⇒ 判红或按 `CALIBRATION.md:278` 登记"不可判定"**（并在 `cal_stats` 落一个 `EXPOSURE_CHECK=SKIPPED_NO_EXPTIME` 之类的枚举）；同时把 EXPTIME 列为必需头卡并在校验层拒绝。补负例：无 EXPTIME 帧 ⇒ 判红。
- 置信：CONFIRMED（代码事实与方向均钉死；EXPTIME 缺失的生产可达性待 CR-283）

### CR-280-17 · 合同必填的 `calibration.mode/fallback/light_exposure_s/dark_exposure_s` 四项只进日志不参与计算，暗场标度恒为 1.0
- 轴：D（主）+ C + ② ④
- 位点：`orchestrator.cpp:956-959`（读取四值）、`:984-987`（`calib_fallback`/`calib_light_exposure_s`/`calib_dark_exposure_s` 的**唯一**使用处，全在 `LOG_INFO` 字符串拼接里）、`:971-983`（`calib_mode` 唯一实质用途＝检查路径字符串非空）、`:966-967` `int dark_opt = 0; double dark_k = 1.0;`、`:1179`/`:1183`（以内核实参）
- 上位依据：`configs/stage1.schema.json` 的 `calibration` 块 `required:["mode","light_exposure_s","dark_exposure_s","fallback"]`、`additionalProperties:false`，`mode` 枚举 `standard|optimal|exposure_ratio`、`fallback` 枚举 `exposure_ratio|fail`（已用脚本逐键打印）；`lib/algorithms/calibration/include/astro_calibration.h:98`「`dark_scale_factor`: K初始值（**如 Light曝光/Dark曝光**）」。
- 现状：`:821-1799` 全段扫描结果——`calib_light_exposure_s` 只出现在 `:958`（声明）与 `:986`（日志）；`calib_dark_exposure_s` 只在 `:959`、`:987`；`calib_fallback` 只在 `:957`、`:985`；`dark_k`/`dark_opt` 的赋值点唯一且为常量 ⇒ 三种 `mode` 与两种 `fallback` 在像素算术上完全等价，"exposure_ratio" 承诺的按曝光比折算从未发生。
- 差在哪：合同把四个科学参数列为必填并给出语义枚举，实现只把它们打印成日志，另以常量决定真实行为——正是"记录为本次参数但实际未生效"。
- 后果：①交付字段 `cal_stats.ACTUAL_K`（`:1276`）与 `DARK_OPTIMIZATION`（`:1277`）记录的是常量输入而非按配置推导的标度，任何以 `ACTUAL_K` 反推暗场扣除量的复核都被误导；②用户以 600 s 暗场母版校 180 s 亮场并声明 `mode=exposure_ratio`、`light/dark_exposure_s=180/600` 时，合同语义是"按 0.3 折算"，实现是"按 1.0 扣除"；叠加 CR-280-16（EXPTIME 缺失 ⇒ 曝光门不触发）即过扣 3.3 倍，直接进入产品像素与下游 SNR/测光。
- 定级：**S1**。触发路径：CLI → `run_stage_calibrate`（`:821`）→ `:966-967` 常量 → `:1179` `fn_calibrate_f64(..., 0, 1.0, &k)` → `:1241` 写回 `data` → `:1276-1277` 写入交付字段。
- 怎么算修好：`dark_k = calib_light_exposure_s / calib_dark_exposure_s`（分母为 0 判红）；`dark_opt` 由 `calib_mode` 显式映射（`standard→不优化`、`optimal→内核优化`、`exposure_ratio→按比折算`）；`fallback="fail"` 与 `"exposure_ratio"` 必须在缺母版时走出不同分支。若负责人裁定"本阶段不做曝光比折算"，则反向删除合同枚举值并登记不适用。**防判红**：`lib/algorithms/calibration/tests/**` 与 `eng/tests/**` 中以 `K=1.0` 为期望的用例须同批改。
- 检索式：逐行扫描 `:821-1799` 内 `calib_mode|calib_fallback|calib_light_exposure_s|calib_dark_exposure_s|dark_k|dark_opt` 全部出现（输出已并入格 5 表 :1171/:1179/:1183 行）。
- 置信：CONFIRMED

### CR-280-18 · `data` 块指针按精度模式直接重解释，未校验块自身 dtype；同文件别处已有"dtype 与 precision 一致"的口径
- 轴：R（主）+ C
- 位点：`orchestrator.cpp:926-932`
  ```cpp
  float* light_f32 = nullptr;
  double* light_f64 = nullptr;
  if (use_fp64) { light_f64 = static_cast<double*>(data_block->data); }
  else          { light_f32 = static_cast<float*>(data_block->data); }
  ```
  `use_fp64` 取自 `:842` `config_.precision == PrecisionMode::FP64`，与 `data_block->type` 无任何比较；`:821-1300` 内出现 `AIO_BLOCK_FLOAT*` 的位置只有写回处 `:1241/:1245`。
- 上位依据：`lib/infrastructure/aio/include/aio_pipeline.h:41-51`（`AioBlockType`）与 `:72`（`AioBlock` 自带 `type` 字段，注释「数据类型」）；同文件 `run_stage_browser_verify` 验证清单第 1 项即「dtype 与 precision 一致」（`:4929-4930`，属 CR-283 行段）；`:748` 的 "Gate 1 (Dataflow_ABI_Contract)" 确立跨 DLL 边界必须校验类型/布局。
- 现状：`n_pix`（`:922`）由 `dims` 算出，与元素字节宽无关 ⇒ 若 `data` 块实为 FLOAT32 而 `precision=FP64`，按 `double*` 读取会**同时**发生数值错读与越界读（需 `8·n_pix` 字节而缓冲只有 `4·n_pix`），随后 `:1177` 以该指针调用 `ac_calibrate_frame_f64`。
- 差在哪：读取侧块类型完全由外部假设决定，而 AIO 合同在块内自带类型标签却不校验；与本仓验证阶段自己确立的"dtype 与 precision 一致"口径不对称。
- 后果：最坏情形是内核读越界内存并写出成品 `data` 块（`:1238-1248` 还把指针所有权转回 frame，不复制），产品像素整体错而 `CALIBRATION_STATUS=APPLIED`；即便 READ_FITS 现恒按 precision 分配，本处仍缺该断言，属"守卫在外层而洞在被嵌套的内层"形态。
- 定级：**S2**（缺生产触发路径：`run_stage_read_fits` 的分配 dtype 属 CR-283 行段 ⇒ 须与 CR-280..283 合并定案）
- 怎么算修好：`:913` 取块后立即断言 `use_fp64 ? type==AIO_BLOCK_FLOAT64 : type==AIO_BLOCK_FLOAT32`，不符 ⇒ 非零退出码 + `error_msg` 带两侧类型；同一模式推广到 `:1030-1041` 的母版取指针处。
- 置信：PARTIAL（缺：`run_stage_read_fits`（`:4030-4236`）是否按 precision 分配 FLOAT64 块）

### 格 5"不报的核过项"（附检索式）
1. **`:1186-1196` 内核返回码未被丢弃**：`ret != 0` ⇒ `error_msg`（含 ret 数值）+ `CALIBRATE_FAILED` + 释放缓冲与三张母版图像 + `return false`。本行段内 `ret` 的三处使用（`:1175` 声明、`:1186` 判定、`:1249` 写回判定）均被消费，不报"返回码被吞"。
2. **失败路径的资源释放完整**：`:1127-1129`、`:1160-1162`、`:1190-1194`、`:1253-1257` 各出口均 `free` 已分配缓冲并 `fn_free_image` 母版；`:1260` 注释所有权转移后不再 free，正确。
3. **`:1035` 的 `(size_t)w * h`**：先转 `size_t` 再乘，32 位不溢出；不报。
4. **`:1056-1075` 尺寸比对用精确 `!=` 而非容差**：像素网格尺寸是整数维度，精确相等即正确判据，不报"未用容差"。
5. **`:1211-1213`/`:1222-1224` 的 `has_x ? mean : 0.0`**：兜底方向已在格 2 表 :275 行与 CR-280-20 合并处理，此处不重复立条。
6. **`n_pix` 可为 0 时统计返回 0.0**：`dims` 由 READ_FITS 写入，0 尺寸帧应在更早判红；读侧不在本行段 ⇒ 入合并待办，不在此立条。

## 格 6 · 行 1251–1381（cal_stats 交付字段写出 + RA/DEC 串解析 + 滤镜/QE 曲线装载注释块 + `extract_qe_curve_name`）

### §2.5 数值与默认值处置表（格 6）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :1263 | `cal_stats.STATUS` | 硬写 `"OK"` | 交付块的总体状态字段 | **待确认（缺陷）** | 该值不来自任何判定：写出点在全部测量之前、且无任何分支可改写它；对照 `:1287-1288` 另有一个真的会变的 `CALIBRATION_STATUS` ⇒ 两态并存时"OK"不承担任何信息，见 CR-280-20 | 方向：常量状态字段应删除或改为由各步判定聚合 | CONFIRMED |
| :1264-1266 | `MASTER_*_PATH` | 配置串 | 母版路径溯源 | 公式导出（配置透传） | 值即 `:953-955` 的 typed 配置，代码真按"原样记录来源"算 ⇒ 合规 | — | CONFIRMED |
| :1267-1269 | `MASTER_*_MEAN` | `has_x ? 实测均值 : 0.0` | 母版均值交付字段 | **待确认（缺陷）** | 缺母版时以 **0.0** 写出（赋值点 `:1211-1213`/`:1222-1224`），0 是母版均值的物理合法值；`OWNER_DECISIONS.md:147` 的"兜底不得与合法值重合"；见 CR-280-20 | 方向：应写 NaN 或省略字段（`DATA_SEMANTICS` §4a 已确立 NaN=不可用） | CONFIRMED |
| :1270-1275 | `LIGHT_*` / `OUT_*` | 实测统计 | 校准前后统计交付字段 | 公式导出 | 由 `:274-332` 的基元对真实像素算出 ⇒ 来源是测量（分母/口径问题在格 2 :301 行登记，不在此重复） | — | CONFIRMED |
| :1276 | `ACTUAL_K` | 内核出参 | 实际使用的暗场标度 | 公式导出（但输入恒定） | 值确由 `ac_calibrate_frame` 写出（`:1179/:1183` 的第 10 实参），但 K 初值恒 1.0 ⇒ 见 CR-280-17 | — | CONFIRMED |
| :1277 | `DARK_OPTIMIZATION` | 常量 `0` | 交付字段记录的暗场优化模式 | **待确认（缺陷）** | `dark_opt` 唯一赋值点 `:966`，与合同 `calibration.mode` 无映射 ⇒ 见 CR-280-17 | 方向：应记录 mode→内核参数的真实映射结果 | CONFIRMED |
| :1278-1280 | `HAS_BIAS/DARK/FLAT` | `1.0 / 0.0` | 母版是否装载成功（布尔以 double 表） | 结构性不适用 | KV 通道只有 double setter（`fn_kv_set_double`），1/0 是编码选择；**这是 CR-280-15 唯一的旁证字段**，但它不在任何合同 schema 中（`git grep -n "HAS_FLAT" -- eng/contracts docs/` 零命中） | — | CONFIRMED |
| :1281-1283 | `DARK_EXPTIME` | 条件写出（`>0` 才写） | 暗场曝光交付字段 | 结构性不适用（正向形态） | "未测⇒不写"优于"未测⇒写 0"，该写法本身合规；问题在上游门被绕过（CR-280-16/11） | — | CONFIRMED |
| :1284-1286 | `DARK_CCD_TEMP` | 条件写出（`dark_has_temp`） | 暗场温度交付字段 | **待确认（死字段）** | `dark_has_temp` 唯一置真点在 `:1104`，位于 `require_temp`（`:963` 常量 false）守护之内 ⇒ 该字段在任何产品中都不出现；见 CR-280-14 | 方向：随温度门一并定案 | CONFIRMED |
| :1287-1288 | `CALIBRATION_STATUS` | `"APPLIED" / "DEGENERATED"` | 校准是否应用的交付状态 | **待确认（缺陷）** | `DEGENERATED` 需 `!has_bias && !has_dark && !has_flat`，而该组合在 `:1134-1145`（`allow_no_calib` 恒 false）已判红返回 ⇒ 写出点可达时三式必至少一真 ⇒ 字段恒为 `"APPLIED"`；见 CR-280-20 | 方向：字段应能表达"部分应用"，否则删除以免误导 | CONFIRMED |
| :1307,:1320,:1324,:1343 | RA/DEC 解析兜底 | `0.0` | 解析失败时的返回值 | **待确认（缺陷）** | 0.0 对 RA 与 Dec 都是**合法坐标**（0h / 0°）；无失败表达通道，调用点 `:1937-1938` 把它当真实初始指向；见 CR-280-19 | 方向：应返回哨兵（NaN）并由调用方判"不可判定" | CONFIRMED |
| :1313 | HMS→度换算 | `15.0`、`/60.0`、`/3600.0` | 时角单位换算定义常数 | 结构性不适用 | 1h = 15°、1° = 3600″ 定义常数，派单 §4 明列不报 | — | CONFIRMED |
| :1337 | DMS→度换算 | `sign*(d + m/60 + sec/3600)` | 赤纬换算 | 公式导出 | 定义式；代码按式算 ⇒ 合规 | — | CONFIRMED |
| :1309,:1333 | 分隔符归一 | `':' → ' '` | 兼容 `HH:MM:SS.S` | 结构性不适用 | 词法处理 | — | CONFIRMED |
| :1312,:1319 | 三段式回退单值式 | `iss >> h >> m >> sec` 失败后 `iss >> deg` | 双格式解析顺序 | **待确认（缺陷）** | 两 token 输入（如 `"12 34"`）时三段式失败、单值式成功 ⇒ 返回 **12.0**（真实应为 188.5°），非"失败"而是"给出错值"；见 CR-280-19 | 方向：token 数既非 1 也非 3 时应判失败 | CONFIRMED |
| :1353-1361 | 注释自陈 | "本文件只做委托，**不再自持任何解析分支**" | 单一实现的承诺 | **待确认（矛盾）** | 紧接其后的 `:1372-1381` 就是一个 `json_content.find("\"qe_curve\"")` 文本扫描分支 ⇒ 见 CR-280-21 | — | CONFIRMED |
| :1369 | 注释声称的配置形态 | `{ ... "frame": { "qe_curve": "GSENSE2020BSI" } }` | QE 键的位置与语义 | **待确认（缺陷）** | 合同面真实位置是 `photometric.qe_curve` 且**值是文件路径**（`configs/stage1.schema.json:175,:186`；`json_config.cpp:705 config.photometric.qe_curve = resolve_path(root["photometric"]["qe_curve"], base_dir)`；模板值 `"../responses/camera_qe.json"`）；schema 中不存在 `frame` 块；见 CR-280-21 | 方向：一个键不能同时是路径与曲线名 | CONFIRMED |
| :1375-1381 | `"qe_curve"` 文本扫描 | 首个 `':'` 后取字符串 | 提取 QE 曲线名 | **待确认（缺陷）** | 与 `:1353-1359` 记载的滤镜缺陷同类（"第一次文本出现"命中错段）；当前 schema 根 `additionalProperties:false` 使 `"qe_curve"` 在合法文档中只出现一次 ⇒ **文本扫描的错命中形态暂不可达（登记为潜伏）**，但"值语义=路径"是必然错；见 CR-280-21 | 方向：改读 typed `stage1_cfg_->photometric.qe_curve`，曲线名另设一键并登记 | CONFIRMED |

**格 6 数值项合计：19 行**

### 格 6 findings

### CR-280-19 · RA/DEC 字符串解析失败返回 `0.0`（合法坐标），且两 token 输入被静默当成"十进制度"给出错值
- 轴：R（主）+ ③
- 位点：`orchestrator.cpp:1306-1321`
  ```cpp
  static double parse_ra_hms(const char* s) {
      if (s == nullptr || s[0] == '\0') return 0.0;
      ...
      if (iss >> h >> m >> sec) { return (h + m/60.0 + sec/3600.0) * 15.0; }
      iss.clear(); iss.str(str);
      double deg;
      if (iss >> deg) return deg;
      return 0.0;
  ```
  同形态见 `parse_dec_dms` `:1323-1344`。
- 上位依据：`docs/contracts/DATA_SEMANTICS.md` 对"不可用"一律以显式标记/NaN 表达（同族口径见 `:1281-1286` 行表锚）；`OWNER_DECISIONS.md:147`「兜底默认值禁止与『显式拒绝值/非法值』重合…0 在物理上常是合法值」。
- 现状：三种失败/退化形态（空串、无法解析、**只有两个 token**）分别得到 `0.0` 与"首 token 的数值"。`"12 34"` 这类输入使 `iss >> h >> m >> sec` 在读取第三项时失败，但 `iss.clear(); iss.str(str); iss >> deg` 成功读到 `12` ⇒ 返回 12.0 度（真实含义若是 12h34m 则应为 188.5°）。调用点 `:1937-1938`（CR-281 行段）把 `ra0/dec0` 作为 plate-solve 的初始指向，`objctra/objctdec` 缺省时即得 0.0 ⇒ 求解中心落在 (RA 0h, Dec 0°)。唯一痕迹是 `:1940-1942` 的一条 INFO 日志（`OBJCTRA='' -> ra0=0.000000deg`）。
- 差在哪：解析器没有失败通道，兜底值本身是合法坐标；对不完整输入不报失败而给出另一个数值。
- 后果：①"未提供初始指向"与"目标在 (0,0)"在实现与产品面不可区分，plate-solve 在错误天区搜索；②若求解在该天区仍返回解（IPD 误匹配），WCS 整体错位而流程判绿——`docs/contracts/DATA_SEMANTICS.md:834-841`（帧头 WCS 未授权登记条目）说明本仓已把"头卡来源"当作可信性问题对待，此处是同族的更弱形态。
- 定级：**S2**（缺生产可达性：`OBJCTRA/OBJCTDEC` 缺失或两 token 形态是否出现在真实数据，需读 `READ_FITS`/`run_stage_platesolve` 的取卡逻辑，属 CR-281/CR-283 行段 ⇒ 须与 CR-280..283 合并定案；`ra0=0` 通常导致求解失败判红，故不判 S1）
- 怎么算修好：两函数改为 `bool parse_*(const char*, double& out)` 或返回 NaN 哨兵；token 数为 2 时显式判失败；调用点在 `ra0/dec0` 未获得时判红或改走"全天搜索"分支并登记；补正例/负例：`"12 34 56.7"`、`"12:34:56.7"`、`"187.5"` 三形态通过，`"12 34"`、`""`、`"abc"` 判失败。
- 置信：CONFIRMED（代码事实与两-token 复算：`"12 34"` → 三段式失败 → 单值式得 12.0）

### CR-280-20 · 交付块 `cal_stats` 里三个字段是常量或不可达态，两个字段以 0.0 冒充测量
- 轴：②（写进产品的量来源是硬写）+ R + C
- 位点：`orchestrator.cpp:1263`（`STATUS` 恒 `"OK"`）、`:1277`（`DARK_OPTIMIZATION` 恒 `0`）、`:1287-1288`（`CALIBRATION_STATUS` 恒 `"APPLIED"`）、`:1267-1269`（`MASTER_*_MEAN` 缺母版记 `0.0`）、`:1284-1285`（`DARK_CCD_TEMP` 永不写出）、`:1136-1137`/`:1144`（错误文案指向配置键 `allow_no_calibration`）
- 上位依据：`:1262` 注释自称「写入 cal_stats KV 块 (**交付物**)」；`json_config.cpp:573-583` 与 `configs/stage1.schema.json` 的 `calibration` 块中**不存在** `allow_no_calibration` 键（`additionalProperties:false`，属性只有 mode/light_exposure_s/dark_exposure_s/fallback）；`eng/ci/ledgers/dormant_algorithms.json` 对 checkpoint 之外无相关登记；合同面 `git grep -n "cal_stats" -- eng/contracts docs/` **零命中** ⇒ 该交付块的字段集合无任何 schema 约束。
- 现状：`STATUS="OK"` 在写出任何判定之前落笔且无改写路径；`DEGENERATED` 分支因 `:1134-1145` 的 `allow_no_calib=false` 恒判红而不可达；母版均值兜底 0.0；`DARK_CCD_TEMP` 因 `require_temp=false` 永不可达。
- 差在哪：一个自称"交付物"的块中，字段名承诺的是多态判据（OK/…、APPLIED/DEGENERATED、有无温度），实现给出的是常量、不可达态与 0 兜底；错误文案又指向一个合同里不存在的配置键，用户按文案改配置会被 schema 拒绝。
- 后果：任何读 `cal_stats` 的人/门会得到"校准已应用、状态 OK"，而真实形态可能是"缺平场"（CR-280-15）或"暗场未按曝光比折算"（CR-280-17）；`DARK_OPTIMIZATION=0` 会被当成"配置选择了不优化"。由于该块无 schema、无在册消费者，错误主张无法被机器门拦住。
- 定级：**S2**（与 CR-280-15/17 同族，此处只登记"字段形态"层面的失真，后果不重复计入以免双计严重度）
- 怎么算修好：①给 `cal_stats` 建 schema 并入 `eng/contracts/`（字段集合 + 每字段来源标注 measured/derived/configured）；②删除常量态字段（`STATUS`）或改为聚合判定；③`CALIBRATION_STATUS` 增 `PARTIAL:<缺哪一步>` 形态并可达；④母版均值缺测写 NaN；⑤错误文案改为指向真实存在的键或删去该分支。
- 置信：CONFIRMED

### CR-280-21 · 同一文件在声明"不再自持任何解析分支"的 11 行之后又新增一个文本扫描解析分支，且把 `photometric.qe_curve`（路径）当曲线名喂给 `load_curve` ⇒ QE 曲线在本通道必然加载失败
- 轴：D（主）+ C + ⑤ + S
- 位点：`orchestrator.cpp:1353-1361`（承诺）与 `:1367-1381`（自持分支）
  ```cpp
  // 修复前本文件自持一份**文本搜索**实现（content.find("\"<name>\"") 取第一次文本
  //   出现 → 再往后找第一个 "wavelength_nm"）：... ⇒ F_syn 用错误通带合成。
  // 缺陷记录与规范依据见该头文件；本文件只做委托，
  //   **不再自持任何解析分支**（第二份实现 = 同类缺陷复发的根源）。
  ...
  static std::string extract_qe_curve_name(const std::string& json_content) {
      if (json_content.empty()) return "";
      std::string key = "\"qe_curve\"";
      size_t pos = json_content.find(key);
  ```
  注释 `:1368-1370` 更声称配置形态是 `{ ... "frame": { ... "qe_curve": "GSENSE2020BSI" } ... }`。
- 上位依据：`configs/stage1.schema.json:175,:186` 中 `qe_curve` 属 **`photometric`** 块（不存在 `frame` 块）；`json_config.cpp:705` `config.photometric.qe_curve = resolve_path(root["photometric"]["qe_curve"].get<std::string>(), base_dir)`（即**路径**，且要按配置文件目录解析）；`json_config.h:57` `std::string qe_curve; // 必填, 非空`；模板 `configs/stage1.template.json:33` `"qe_curve": "../responses/camera_qe.json"`；夹具 `configs/stage1_gc_panel1_Red.json:31` 同为一绝对路径。消费侧口径：`lib/algorithms/photometry/cpp/src/filter_curve_json.h:380-410` `find_curve_object(content, curve_name, ...)` 要求 `"curve_name"` 作为**对象键**后跟 `:` 与含 `"wavelength_nm"` 的 `{}`，`:444-456` 找不到即返回 `kCurveNotFound`/`kCurveNameOutsideLibrary`。
- 现状：消费点 `:2664-2684`（属 CR-281 行段）为 `qe_name = extract_qe_curve_name(config_.calib_params_json)`（未解析的原文路径）与 `qe_json = stage1_cfg_->photometric.qe_curve`（解析后的路径），随后 `curve_json::load_curve(qe_json, qe_name, ...)` —— 第二个实参按契约应是曲线名，实际拿到的是路径字符串 ⇒ 必然 `kCurveNotFound`；`:2679-2684` 只 `LOG_WARN` + `fprintf(stderr, ...)` 并**以 `Q(λ)=1` 继续**（不判红）。
- 差在哪：①一个键被同时当作"曲线文件路径"与"曲线名"使用，二者在合同与实现里互斥 ⇒ 配置必填的 QE 曲线在 pipeline 通道**永远不可能生效**；②该提取仍走 `find()` 首次命中的文本扫描（与 `:1353-1359` 记述的缺陷同类；此形态在合法文档中暂不可达，属潜伏）；③注释给出的配置形态（`frame.qe_curve`）在合同里不存在。
- 后果：SCI-A（测光校准到星等坐标系）的 `F_syn = ∫ S(λ)·T(λ)·Q(λ) dλ` 缺 `Q(λ)` 项，而配置与运行证据都表明"已配 QE"；`docs/science/PHOTOMETRY.md:180` 允许 `Q(λ)≡1` 作为**显式未建模项**（并要求显式告警，`grep "PHOTOMETRY.md:180"` 由 `:2681-2683` 注释引用），但本处的告警发生在"用户明明配了曲线"的场景，等于把配置意图静默降级；产品面没有任何字段记录 Q(λ) 是否计入。
- 定级：**S2**（触发路径清晰但消费点 `:2664-2684` 在 CR-281 行段 ⇒ 按派单口径"跨段结论须与 CR-280..283 合并定案"；合并后若确认 pipeline 通道为生产入口，本条应升 S1，因为每个归一化帧的测光通带响应都缺 Q(λ)）
- 怎么算修好：①合同层为 QE 增设 `photometric.qe_curve_name`（或与滤镜同款的 `lookup` 规则），`photometric.qe_curve` 只作路径；②删除 `extract_qe_curve_name`，改读 typed 配置（单一来源，遵守 `docs/standards/CODE_STANDARD.md §MUST` 的"单一实现 + 禁止 silent config fallback 改变科学语义"，`:1349-1351` 已引用）；③订正 `:1368-1370` 的配置形态注释；④把"Q(λ) 未计入"作为可判红事实写进产品/manifest，缺曲线时按 `mode=fail` 判红。防判红：`configs/stage1_gc_panel*_Red.json` 与 `eng/tests/**` 中若以"QE 未生效"为既有期望，需同批复核。
- 置信：PARTIAL（键语义冲突与 `find_curve_object` 契约已逐行读到并复算；"合并后是否升 S1"取决于 CR-281 的 `:2664` 上下文与 CR-283 的入口登记，本批不越段定案）

---

## 本批最高危三条（只挑，不复述）

1. **CR-280-17 · 暗场标度 `dark_k` 恒 1.0、`dark_opt` 恒 0，而合同必填的 `mode/fallback/light_exposure_s/dark_exposure_s` 四项只进日志** —— 唯一一条"每个帧都执行、直接决定像素值"的硬写；与 CR-280-16（曝光门可被绕过）叠加即过扣 3.3 倍。
2. **CR-280-15 · 母版装载失败单个可缺、以 `nullptr` 进内核继续出图，交付状态仍写 `APPLIED`** —— 科学处理步骤可整步消失而产品面自我肯定。
3. **CR-280-02 + CR-280-03 · 叶级 NSIDE：CD 取不到时写死 32768（≈6.44″/px），auto 判据另用"两轴均值 ÷1.5"绕过权威 `compute_auto_nside` 且从不产出 `oversample`** —— 装配点的唯一产品结构参数既无兜底来源，也与同仓另一生产入口异值。
   （紧随其后：CR-280-06 的失败清理无条件 `remove_all` 目标树，属数据丢失形态，但因调用点在 CR-283 段而按 S1/待合并处理。）

---

## 我读不动或需要实跑才能定的

| 待决问题 | 卡在哪 | 要跑什么 |
|---|---|---|
| CR-280-01 的 `catch(...)` 在生产上是否可达 | 需确认 `:5403` 的 `config_json` 字符串是否恒为已通过 `json_config.cpp:545-556` schema 校验的同一文本 | 构造一份语法被破坏但被强制传入的 stage1.json，跑 `astrocs normalize --config`（或 `orchestrator.exe --validate`）看错误出口在何处；只读条件下无法定 |
| CR-280-03 两通道（`orchestrator.calculate_nside` vs `module_adapters → hp_drizzle_compute_auto_nside`）对同一帧是否真给出不同 nside | 两式的输入不同（均值 vs 最细局部尺度、÷1.5 vs ÷1.0），差异幅度依赖 CD/SIP | 用同一帧（`testdata/` 任一已解算帧）分别走两入口，打印 `nside/finest/hp_res/oversample` 对照表 |
| CR-280-09 的"向上 4 级"是否真的让 `orchestrator.exe` 在 Windows 上加载不到 DLL | 需按 `cpp/Makefile` 实际构建并把 exe 放在其声明目录 | 只做一次 `orchestrator.exe --inspect <file.hiss>` 的 Windows 实跑，读 `[dll_loader] DLL 文件不存在: <路径>` 里的路径前缀 |
| CR-280-16 的前置"帧头可无 EXPTIME" | 判据在 `run_stage_read_fits`（CR-283 段）与 AIO 头卡映射 | CR-283 合案后复核；或跑一次剥掉 EXPTIME 卡的 FITS 帧看是否判红 |
| CR-280-18 的 dtype 重解释是否可达 | `run_stage_read_fits` 是否按 `precision` 分配 FLOAT64 块（CR-283 段） | 以 `precision=FP64` 跑一帧 32-bit FITS，检查 `data` 块 `AioBlock::type` 与 `n_pix` 字节宽度 |
| CR-280-21 合并后是否升 S1 | 消费点 `:2664-2684` 在 CR-281 段 | CR-281 合案；实跑一次带 QE 曲线配置的 normalize，在诊断输出里核对 `Q(λ)` 是否进入 `F_syn` |
| 本文件（乃至本模块）是否仍是生产入口 | 三处登记面互斥（见下 M-1） | 由负责人裁；裁前本批所有"生产可达"结论按条件式表述 |

---

## 与同文件其它段（CR-281 1382–2762 / CR-282 2763–4143 / CR-283 4144–5522）的合并待办清单

- **M-1｜本模块的生产性未定，凌驾于全批 S 级判定之上**：`orchestrator.cpp:6`「当前版本: **生产实现**」 vs `cpp/include/orchestrator.h:2`「**legacy Phase1 编排器**」 vs `cpp/CMakeLists.txt:2`「编排层（**legacy Phase1 orchestrator**）模块 CMake」（`:4-7` 并称"在此之前不在任何构建图…改动从未被编译过"） vs `lib/algorithms/drizzle/healpix_drizzle/tests/CMakeLists.txt:307`「**生产管线 orchestrator** → hp_drizzle_run_hips」。四说互斥，须合并定案并上呈负责人；在此之前 CR-280-02/03/06/09/15/16/17 的"后果"均以条件式成立。
- **M-2｜NSIDE 判定链跨段合案**（CR-280-02/03/04）：本批证据在 `:130-219`，调用与登记面在 `:3328-3358`（CR-282）、`:4879-4924`（CR-283）；需由 CR-282/283 确认 `run_stage_drizzle` 是否有任何 CD 守卫、`nside_used_` 与 manifest 记录的关系，以及 `:4877` 注释「auto: 从 WCS/plate scale 推导 (calculate_nside)」是否被当作对外口径。
- **M-3｜清理与原子发布**（CR-280-06）：`AtomicOutputGuard` 两份（`:5097-5106` 无 `allow_partial`、`:5427-5434` 有）→ 由 CR-283 判定两套守卫条件是否有意为之；`IO_003 §4.2` 的"成功对象/残留"二分在 C++ 侧无完成标记可比对（`docs/algorithms/HIPS_WRITER.md:379` DISP-HIPS-004 自陈"manifest 无 COMPLETE 状态字/树哈希"），须与合同侧同批定案。
- **M-4｜`run_stage_nside` 传 `"auto"`**（CR-280-04）：位点 `:4910`（CR-283），要求该段确认每次 auto 运行是否确实打 `未知 strategy='auto'` WARN。
- **M-5｜`data` 块 dtype 与 precision**（CR-280-18）：判据在 `run_stage_read_fits`（`:4030-4236`，CR-283）与 `run_stage_browser_verify` 的"dtype 与 precision 一致"清单（`:4929-4930`，CR-283）。
- **M-6｜`project_root_dir_` 下游路径**（CR-280-09）：`:1499`、`:1510`（CR-281）以 `project_root_dir_ + "/lib/algorithms/..."` 拼 DLL 绝对路径；`:1967`（CR-281）拼 `project_root_dir_ + "/run/logs/plate_solve"` —— 若根推导偏两级，plate-solve 日志会落进 `lib/infrastructure/run/`（仓库树内），触及 `AGENTS.md §6`「不把运行产物散落根目录」，须由 CR-281 落定。
- **M-7｜QE 曲线消费点**（CR-280-21）：`:2664-2684` 落在 **CR-281 段**（1382–2762），请 CR-281 认领该消费点并核 `load_curve` 失败（`kCurveNotFound`）是否影响判红、`photometric.qe_curve` 是否还有第二个读取者。
- **M-8｜`cal_stats` 交付字段无合同**：本批全部 `cal_stats.*` 论断依赖"合同面零命中"这一事实（`git grep -n "cal_stats" -- eng/contracts docs/` 仅 `docs/architecture/execution_inventory.csv` 行 2 提到写点，且其行号锚 `:1218-1243/:4949/:5177/:767` 与现行代码已漂移 40–160 行）⇒ 行号漂移与登记面失效归 CR-28x/机器门面，本批只登记事实。
- **M-9｜跨段"取不到即记 0"总面**：本段已登记 `:938`（EXPTIME）、`:1211-1213/:1222-1224`（母版均值）、`:168-169`（nside 32768）、`:1307/:1320`（RA/DEC）；CR-281/282/283 段内同族位点（各 `fn_kv_get_double(..., 0.0)` 与 `orc_getJsonNum(..., 0.0)`）需按同一口径合并成一张"未测伪装成已测"清单。
- **M-10｜`nside_used_` 与产品记录**：本段确认 `nside_used_`（`orchestrator.h:475`）全仓仅被 `:3346` 读一次，未见任何 manifest/产品写入 ⇒ 若 CR-282/283 段找到记录点，则 CR-280-02 的"兜底值是否被记成用户指定值"须按该处定案。

## 收工 git status --porcelain 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（与开工一致，与基线两项一致；HEAD=`c8f64e9a`；本路对仓库目录零写入，全部临时件与成稿均落在 `产出/`。）

<!-- PROGRESS: 6/6 -->
