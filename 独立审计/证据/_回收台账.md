# 回收台账（各分片回来后逐条登记；防上下文压缩丢信息）

被审基线 `c8f64e9a`。状态口径：`在途` / `已回·未复核` / `已回·复核中` / `已核`。只有两层都过（机器复算＋非原作者独立复核判"确认"）才允许进负责人 P0/P1 清单。

| 分片 | 状态 | 上报要点 | 前台复算 | 第二层 |
|---|---|---|---|---|
| AUD-101 DB-02 | 已回·已复算 | 25 份模块文档上游指针系统性指错节；18/25 带 YAML 头元信息块；rejection 档界在两份模块页各存互斥副本且把不载档界的 §5.5 当来源；两份"同步合同页"行数互斥；`MODULE_MIGRATION_MATRIX.csv` 等三处悬空 | 复算一致：frontmatter 18/25 精确对上；最高设计 §8.4 实为"顶层结构"、"模块与 ABI"是 §8.5，指针错配成立 | 需（档界互斥副本上 D8 前复核） |
| AUD-403 | 已回·部分推翻 | 注释待清 1000 行/455 文件；`additive_mode` 注释与代码默认矛盾（CONFIRMED）；根 README 判 PASS；TROUBLESHOOTING 四条命令路径"全不存在"；版本单源判一致但两份"单源"文档自身带元信息块与 40-hex | 前一项我复核成立（同文件三处两两打架）；路径项更正：三目录**在跟踪集已被 `eaf32aad` 目录等价迁移删除**、本机磁盘仍有残留 ⇒ 文档缺陷成立，判据须用跟踪集而非磁盘存在性（我的首次复算用错了仪器） | 待（覆盖面窄的门与 DOC-003 只覆盖 5/26 需复核） |
| AUD-101 DB-15 | 已回·未复核 | 6 份实验报告七要素齐；两份 *-canon 含 MC 却未声明 seed；`DOCUMENT_INDEX.yaml` 自declare 排除 `实验/` ⇒ 结构性缺口；EXP-06 自列 T1 空背景 vs T2 逐像素总方差稠密权重口径为"最重要未决"；三处与正本口径不符登记 | 机械列误报由它校准（DOI/seed 当 commit） | 待 |
| AUD-203 天光无缝 | 已回·复核中 | 加性口径成立（纯减法语义）；`control_ivar` 常数输入下不归零（约 1e26）；出厂接缝门与归档验收证据用**不同统计量**（0.0073 vs 3e-5，同一背景结构）；四态可达但 stalled=2 无归档证据；`tolerance_relative` 两处默认不同 | 我复算 `tolerance_relative`：头文件默认 0、`upm.cpp` 两处强制归一为 0/1、JSON 读回默认 0 ⇒ 需看它点名的两处（1 与 0）各自在哪层 | 在途（V1..V5） |
| AUD-202 跨帧绝对 SNR | 已回·复核中 | 权重在信号维退化为常数（帧级臂稀疏层未接、逐像素臂取空背景方差面）；稀疏层存的对象与合同冻结非同一物（潜伏）；两篇 FROZEN 文档对"背景逆方差可否当 Phase2 权重"互斥；`snr_path` 死键、dense 生产者零调用、归档实验三臂与主张对象错配；某噪声式量纲不齐、某"重建误差"判据对插值算子恒为 0 | **已亲眼见源码该行为"稀疏 SNR 层尚未接入生产数据面"**；逐像素臂读到的是 ivar 瓦片并有非有限/负值即报红的守卫，其生产侧是否含源散粒项待追写方 | 在途（V1..V6，含替我把两篇打架定死） |
| AUD-201 测光 | 已回·复核中 | 工作包任务书第 1 条的通量式与正本相反（亮度计两次），该错式仍以两个公共函数存活并被测试/工具调用；λ 因子经查正确且量化为色项（黑体峰峰约 0.27 mag 对真实星色轴约 0.011 mag）；现行锚按星等分箱 ⇒ 注入色缺陷只读出约 27% 且源自样本颜色—星等相关 ⇒ 判据无证据资格；正本定量证据 6/6 指向已丢失的 `run/`；6 条常数"已溯源"挂在死键上且 defaults 回链 4/6 指错行；某残差离散度可取 0 而消费门只卡上界 ⇒ 假绿；`lib/infrastructure/gaia/` 跟踪数为 0（AGENTS.md §7 同源笔误）；独立复现稳健尺度常数 4.685 并自我否证一处怀疑 | 待我用 `git ls-files` 复算目录计数 | 在途（V1..V6） |
| AUD-204 面积 | 在途 | — | — | — |
| AUD-401 / 402 / 501 | 在途 | — | — | — |
| AUD-101 DB-06/07 | 已回·复核中 | 合同说明层已越位成第二套事实源，且是"同一字段两处取值不能同时为真"式：同一文档内一处判某权重回退键"不存在、出现即拒绝"、另一处却写"唯一例外=显式给该键即降级"（代码侧记该键已按裁决删除）；像素面积幂次文档与 schema 两侧取值不同；某裁决锚被大量跟踪文件引用而其宿主控制包已按收口规范出库；配置合同的数值快照与事实源全面失配；行号锚普遍漂移 | 我复算：权重回退键两处互斥条款**直接看到原文**（判"例外"那句若被实现即把 fail-closed 翻成 fail-open）；幂次与 A44 宿主交复核 | 在途（W1..W5 含 142 s 与 must_contain） |
| AUD-101 DB-08 | 已回·部分前台确认 | 落盘形态合同的 `must_contain` 逼 11 层字面复述同一组口径词，而上位规范自己明文"本节不复制字段清单"⇒ 按上位收成指针就会被门对上位判红；该门的变更触发面不含三份根规范；性能门合同把"连续低利用窗"阈值写成 142 秒而唯一数值源是 10 秒；裸形态是否必须有索引三方冲突；退出码唯一表不闭合；降级理由词表 3/5 词零使用者且生产写侧用另一个键名自由文本 | **142 秒这条我前台确认**：合同该格写 142 s，`resource_gate_v1.json` 两处是 10，门的自述负例里 142.049 是观测样本读数 ⇒ 按字面落地等于把门松 14 倍 | 在途（同上） |
| AUD-101 DB-13 | 已回·复核中 | 被正本与合同判"禁用"的加权模式仍在验证档案层与其机器源里列为生产模式，且那份合同登记件内部"生产模式集"与它的 FROZEN 条款取值自相矛盾、十余条在册门以该模式为被测对象；追溯映射正向通反向断（缺的是规则不是值）；排异档界三档与五档两篇相反且其署名出处明文"不复制档界"；术语表一处阈值与它所引证据行号无关；6 份档案的机器源整体出库未跟改 | 排异档界与 DB-02 属**同一对象第二次独立命中**；snr=1.0 那条若复核成立即可从待裁清单销案 | 在途（W1..W4） |


| 文献池 P-1 / 旧判盲复算 / 工单对账 | 在途 | — | — | — |

## 第二批回收（后续到达）

| 分片 | 状态 | 要点（前台已复算的标 ★） | 第二层 |
|---|---|---|---|
| AUD-401 架构 | 已回 | 三会话不在生产调用面但仍被产品链接（其一全仓零引用、另两只挂在 ID 写错的注册项上，仓库自家台账已记"永不被调度"）；三阶段调度器只接上 1/3，生产由单一通用调度器跑三阶段；§8.2 命名块管线生产零承载（节点间靠 output_dir 文件约定传 JSON）；唯一执行器池靠 `#include "executor.cpp"` 走私 ＋ 生产 14 处私建线程池 ＋ 第二预算源；模块 ID 两套命名空间交集为 0；§8.4 承诺的加载校验与 CPU provider 不在图但安装布局仍装 5 个 DSO；穷举 621 TU＝生产 262／仅测试 119／独立构建 158／不被任何 target 编译 82 | 在途（W1..W4） |
| AUD-501 门禁 | 已回 | 在册 150 项/370 执行单元；三条最重：agent 那条入口可"零执行报绿"且把平台跳过记 PASS（本机 345 单元登记 linux）、"全门禁 fail-closed 普查"测的是 runner 自己的证据管道而非各门判别力、注册表↔文档映射在关键面断开（79% 执行单元零文档承载）；豁免面如实干净但一条真实数据端到端门 `waivable:true` 且前置缺失即静默 SKIP | 在途（W1..W2） |
| AUD-204 面积 | 已回 | 等面积/轴对齐前提**成立**（我另有独立复算路径）；面积"1e-15 闭合"是代数恒等、在逐格错配 0.84 的例子下照样归零 ⇒ 无证据资格★；`u+v=1` 缝上逐 leaf 误差平台约 8.5e-6 **超 1e-6 冻结门**；`w=a/A_pixel` 写法见最高设计 §2.4 与包文第 6 条、而代码与 FROZEN 正本写 `a/A_drop`（两侧参数化发布同值，属命名之争）；`hips_profile=0/1` 不等价，误差源于 uint8 支撑格、最低 support 处 NaN；为 pixfrac 修复作证的那条门的奇偶断言按构造永不触发 | 在途（W1..W4） |
| AUD-101 DB-01 | 已回 | ★两处**伪造最高设计条款**（以"逐字"引用上位并不存在的句子；我用正反对照复算过其中一条的检索式）；DATA-001 空洞：26 处引用一份已被控制包清理删走的合同、全仓 58 文件引用它（与 A44、§9.41 属同一簇，第三次独立命中）；`acsd verify` 在一份合同里 FROZEN 而协议合同与上位都判它不在命令面，且这份还被登记成机器门判据源；MODULE_MAP 内嵌 190 行缺口台账且其权威指向会被清理的控制包 ⇒ 包一收口那条门必红（"改对会红"型死结） | 待派 |
| AUD-101 DB-09 | 已回 | 对外接口合同面：1 个幻影符号（同文档另一处自陈已删＝文档内自相矛盾）；197 真实导出中 58 个未登记、三整族零登记；17/19 节的头文件行锚全部失效；两份合同一起把上位的"相对容差 1e-12"写成"绝对容差 1e-12"（正本明文禁绝对容差）；TEST_MATRIX 11 行里 8 行的测试 ID 零在册 | 待派 |
| AUD-101 DB-14/16 | 已回 | 恒真门两份且一份与上位倒挂；一处归零门按文档口径应红、靠文档不存在的绝对地板通过；"65 组配置"实测 45；文献对照 RMS 数字零落档却承载关键判定；两份正本与实验正文**互相指认为对方的正本**（循环指认）；四份生成表 median/MAD 两臂逐格恒等 | 待派 |
| 我这边 | — | 子代理 DB-01 曾执行 `git config core.quotePath false`（只写 `.git/config`、不入库、不在 status 体现），我已 `--unset` 还原并在派单里禁用该命令；批次清单文件是 CRLF，会让按行读取的代理拿不到路径，已全部归一为 LF（28 个清单） | — |



- 两处文件域指错：某路任务书把排异算法的文件域写成 `lib/algorithms/rejection/`，该目录只有 `.md/.yaml`、无一个源文件，真实现在 `coverage/src/rejection.cpp` ⇒ 按声明范围结构上做不完。后续派单的文件域一律先跑 `git ls-files` 核过再写。
- 首批 6 路的提示词里"先读工作包 standards/templates"指向了我随后删掉的仓库内解包目录 ⇒ 该 6 路缺证据分级表与产出骨架（判据已内联），回收时看它们是否自行发现缺失。
- 文献那一路我写了"只处理已判态为空的行"，等于把上一轮判定当结论 ⇒ 负责人纠正后补了盲复算路，并把"旧台账只当线索"写进合并规则。
- 派单里给文档批次猜"本批主题是 X"连续多次落空（DB-08/14/16/17/19/20 都报清单与主题不符）⇒ 后续只给清单文件、不描述主题。
- **旧文献判定的盲复算：30/30 完成，回执虽报"撞限"但产物齐**（436 行成稿＋30 行 CSV）。对上一轮"已判确认"的 30 条 P-1 引用盲复算后：**一致 26–27、旧判偏松 2、旧判偏严 1**，另 1 条"状态字段与明细字段不一致"。⇒ 旧文献台账可信度约九成：不是不能用，而是**用前须抽核**；那 2 条偏松的已从论文可用池剔除。
- 该路另给出台账生成器三条校准：`核验态` 无空值（未判档实为"待核验"）、`代表位点` 有 3 例行号不指向该引用件所在行、`DOI ` 与 `DOI:` 前缀变体把同一文献拆成两行并挤占名额；建议把 `核验态` 拆成"状态（三值）＋依据类别＋核验深度"三列（只登记，不自行改共享台账）。

## 第三批回收（要点存档，★＝前台已确定性复算）

- **DB-11 索引地图**：★73 条条目上游写成"§8.4（模块与 ABI）"（真 §8.4＝顶层结构，那标题在 §8.5）；★规范正文的冻结判据表格内残留一个大模型控制 token，且同一行阈值写 10 秒（另一份合同写 142 秒）；★追溯表判别列 67/67 恒为单值；索引缺"角色/主题"两轴 ⇒ "角色标注不符"判据按字面不可执行；负责人面两处总览的源码行锚漂约 9000 行（根因＝行锚门覆盖面只到某一层目录）。
- **DB-12 负责人面**：状态登记私自扩档并被下游与 CI 拿去自证（自造状态词 127 个跟踪件在用；自造"发布结论"词全仓独用）；四投影登记"已实现"与上位"仅 TAN 可用"、代码 RETIRED 标注、同文件另一行三处相互矛盾；称某检查器已退役而它仍是在册 P0 门；一条文献归属错标仍在库；参考条目号 61–67 双重占用。
- **DB-17 结果层**：★选择性转录——极区报告 2 处 PASS、0 处 FAIL、五次提及该判据，而两份跟踪日志明写 `SUMMARY T16: FAIL`；四处值在跟踪日志里不存在；同量两值 20+ 条（M42 帧级 σ 两实验差 4 倍且都不写裁剪口径；SCI-B 双计 34% 对 36.6%）；生成表与门函数经核为实算无写死（问题在手写报告层）；`docs/` 里一份文档是未替换占位符的骨架却被 4 处指向；正面基线＝极区快照 47/47 校验通过、抽查逐位一致。
- **DB-19 测光审稿**：★假阻塞项——控制包 8 处＋一份跟踪证据件称"星数≥100 未落地"，而正本 §16.5 第 1 条明文"星数不构成拒绝条件"，负责人裁的删门槛已兑现（调度侧零命中）；★"四个调度器生产零引用"与代码相反（导出腿已接线，另两路同向）；一条审稿结论在四份文档里三档并存无人裁决，被动摇的正是测光判据的 PASS 资格；README 把预筛口径写错（代码对、文档错）。
- **DB-20 控制包**：第二次独立命中假阻塞项；一条在册冲突其对象已被就地改写、冲突已不存在；三条"待办"在冻结令下不可开工、应改判 DEFERRED 并写解禁条件；★写法门的任务编号前缀表由 `run/` 与 `工程控制/` 的目录名数据驱动派生（`check_doc_hygiene.py:391-393`），而 `run/` 只入库 `.gitkeep` ⇒ 清包后是判红还是失明待判；任务编号被 84 个包外文件、246 处当注释锚 ⇒ 与 GAP_AUDIT／DATA-001 同簇第四次命中：收口清理缺"引用回查"前置。
- **DB-05 架构文档层**：★最高设计 §8 全节对 `docs/architecture/` 的索引指针为 **0** ⇒ 权威链从顶上就断；2 例下层文档自封架构权威（"冲突处以本文件为准"）＋7 例"唯一口径"措辞违规；对外接口登记表 381 行状态**全预写 VERIFIED**（上位要求现场计算）且同目录另一份文档自陈"Windows 侧未验证"；同一 ISA 主题两套互不兼容正本、实测表逐字相同三处承载；三份调用清单为过期件（把已退休项写成生产可达）；两篇性能/线程文档的全部证据锚在不入库的 `run/` ⇒ 干净检出上无一可复算；本批一度据 `ls` 判"§8.4 与实际双向不闭合"，改用跟踪集复算后**自行撤回**（真实缺陷反向：登记表列了不存在的目录）。

## 常数判读（AUD-402 队列，逐侧已前台复算的用 ★）

- **A3 队列 124/124 全判、无缺口**（结构性不适用 97／待确认 18／有出处 9）。三条立案：
  1. ★**`drizzle.pixfrac` 六侧取值表**——设计块 0.8／唯一源 `defaults.json`（标 owner_adjudicated）0.8／出厂模板 0.8／插件文档 0.8，而**配置登记册写 1.0 并附一条可判伪的断言"默认 1.0 已入 defaults.json"**、**CLI 骨架 1.0**、**生产读键兜底 1.0** ⇒ 用户省略该键时生产取的是裁决值的对面，且它是唯一被静默补值的 drizzle 参数（同函数处另一键缺键即显式拒绝）。现有配置门不比对 CLI 骨架，登记册自己就是错的那一侧 ⇒ 分歧今天不可见。性质从"待裁"改判为"**已裁未贯穿**"。
  2. **资源门有一套契约外判据**：契约与实现头文件都禁止实现侧再写字面量阈值，但多组阈值仍是无源字面量；其出处注指向一份文档，**该文档不在跟踪集也无删除历史**，而 lib 内 27 处引这个锚。
  3. **默认字面量冒充测量值**：一组门配置的观测字段在生产无任何填充点 ⇒ 初值即实取值，对应判据恒不触发，"多指标皆低"退化为单腿；另有一处"分母为 0 返回 0.0"伪装成 0% 利用率，与契约写的效应方向相反。
- 判读纪律生效的一例：该路证伪机械层 21 行（把注释与文案里的条款号当"行内浮点字面量"、把契约哨兵值当"现行值"），并主动撤回自己两条判读前提。

# 第②层削掉了我前台已经报给负责人的话（必须回补更正）

- **测光那路复核：确认 2、降级 3、推翻 1，并削掉我转述过的一条论证**。
  - **推翻**："色项裕度 25 倍因此排除备择解释"——0.2708 mag 是**官方 G 通带**的数，生产通带只有 0.0145（差 19×）；样本两臂都走绝对浮点谱、**不经容器解码** ⇒ 对争议的单位约定零灵敏度。可判别的其实是约 7 mag 的绝对平移，而能担事的锚其证据件不在跟踪集。λ 因子本身成立，正本不动。
  - **新 P1（形状比原报更坏）**：零离散度假绿。可达条件不是"样本不足"而是**多数票残差严格相等** ⇒ 中位绝对偏差为 0 ⇒ 生产代码无条件把全部星判为好星 ⇒ 报告 σ=0；我方可构造一颗偏 **11.675 dex** 的星被吞掉，同时产品报"零离散度、离群率 0"并通过唯一那道上界门。另两条原报没有：被引用的"不可估计"分支经 20 万次随机搜索证明**结构上不可达**（注释把 0 归因错了）；报告级状态只看"用没用拟合"，与散度无关。
  - **P1 溯源空转的机理性发现**：锚门**允许 token 前 2 字命中**，于是某个键的前两字正好落在错行的另一个词里 ⇒ 错行被判"已验证"，这就是"已溯源"能长期虚标的机制。历史逐提交判定把 6 条分成两类：3 条**曾真过、后因插章漂移**，1 条**在 8 个登记提交里从未成立＝登记即错** ⇒ 不能同账整改。
  - **降级**：所谓"锁死代码的那条测试"不是产品偏差（生产调用者 0）；分箱锚的问题收窄为"只对绝对量有资格、对色项型无资格"，且它建议的修法按 V2 结论会建成**恒绿门**；质心超容差那条读数虽复现，但产生它的那个脚本被明文降为诊断、且现行两条门晚两天落地 ⇒ 现状既不能判红也不能判绿，只能作复测项。
  - **贯穿性一条**：三处读数的原始证据件全在 gitignore 的 `run/` 下、跟踪面只剩正文引用 ⇒ 建议**合案**整改"证据必须落可跟踪位置"，而不是逐条按数值挂账。

- **我说过"自造状态词有百级跟踪件在用（127）"——不成立**。复核逐字并排后：153 档/517 处用的都是上位合法词；真正的自造词只有"发布结论"那一档（3 处）与另一个不在上位词表里的词（1 档 4 行）。传染面从"百级"降到"个位"。
- **我说过"负责人面两处总览的源码行锚漂约 9000 行，根因是行锚门覆盖面不含负责人面"——两点都错**。实测统一漂移是 +11,205 行；而那个门的文档口径本就含负责人面（其 glob 覆盖 233 篇 md），真正原因是**锚的写法**：门要求路径后紧跟冒号，负责人面写成"路径"后另起冒号数字，于是那 20 条锚一条都没进门；把正则放宽实测只新增 24 条锚、零不可解析 ⇒ "扩面会引入大量既有红"不成立。根因用 git 取到：锚是在目录迁移前对旧路径写的、当时完全正确，迁移改了路径没重导行号。
- **一张"第二张索引地图、建议删除"的处置被推翻**：所谓"53 处引用"里 51 处是索引生成器把那张 CSV 的**数据行**当成引用扫进了自己的 downstream 字段；该 CSV 自身无任何 eng/ 消费者、也不在悬空扫描面上 ⇒ 没有争"唯一地图"的能力；删它反而会让那 51 条 downstream 变成指向不存在文件的死串且无门会红。处置改判"保留并降级为历史快照"。
- 复核补了一条比我原话更硬的事实：上位明文"登记表不预写状态"，而这份登记不仅预写，还把证据钉在一个落后当前 **817 个提交**的旧提交上。
- 一条被判"确认但立案面收窄"：所谓四档并存其实三处口径一致，真正打架的是"验收行被判通过 ＋ 把恒真量当证据字段"；其"下游是桩"这一说被核到更硬的机器面（配置死键台账里在册、且那个"生效路径"字段在 lib 内零命中）。

## 收口前必须做的清理（防遗漏）

- `独立审计/03_小论文/` 有并行代理留下的中间件：`_draft3.md`、`_final3..6.md`、`_refs.part`。**打包前删除**，并核对正式文件名（`论文N_*.md`）下的内容是最终版而非被 `_final*` 覆盖前的旧版；`raw/` 下 `_block*.md` 同理不进交付。
- 底表已刷新为 54 份成稿 → **5,830 条**带被点名对象的条目；聚合时注意 `docs/`、`docs/science/` 这类目录前缀不是对象键（各约 160/56 行是正则吃进的泛指）。

装配侧：底表 `inventory/findings_base.csv`；抽取器 `scripts/extract_base.py` 空输入即非零退出；补派判定用 `scripts/gap_check.py`（带"一个路径都没出现＝判定式失效"守卫）。
- 覆盖读数（严口径＝该路径出现在表格行或标题行里）：文档审计面 303 份中 292 份有条目（96.4%）；残余 13 份已派 `aud101-d1-residual`。DA-01/DA-02 两路后撞 150 轮上限，其严口径覆盖与收尾节另计（见 `inventory/DA-0*-missing.txt` 与 `aud101-da02-tail`）。
- **更正上一条**：那"残余 13 份"里只有 **3 份**真缺条目；另外 10 份控制包任务书在 DB-19 里**已逐份出过条目**，只是小节标题写的是**包内相对路径**（`tasks/ACCEPT-501.md`），我按全路径取交集才判成 0 命中 ⇒ 是**我的合并键不可用**，不是漏审。⇒ 真实覆盖高于 96.4%；D1 装配时的去重键必须**规范化为仓库全路径**，否则这 10 份会被重复计入两批。
- 我自己的一次误编辑：把上句中"（" 连同括号内容首字符替换掉，已复原——记录是为了说明：**台账也会被前台写坏**，所以每条读数都要留可复算命令。
- **更正我报过的"文档审计覆盖 96.4%"**：装配阶段用"审计面全集 ∖ 已登记路径"复算，真实缺口 **13 份**，其中 10 份是同一批控制包任务书用**包内相对路径**登记（按全路径取交集才误判为缺，属合并键不可用），**从未登记的只有 3 份**（`docs/standards/TEST_STANDARD.md` ＋ `docs/**` 下两份 `.py`）。那个百分比还把两个不同计数（303＝某组清单行数、347/360＝命中率）混进一句话 ⇒ 覆盖率今后必须写明分子分母口径。已补派那 3 份。
- **D2（目标文档架构）已交付**，两处值得记：① 独立证实权威链顶断裂——最高设计 §8 五节**零下推指针**，且全 `docs/` 没有任何一份文档的上游指向创新点四那一节；② **方向反转**：模块工作细节的双目录里正本是 `docs/plugins/` 而非页数占优的 `docs/modules/`（注册映射 56 处指向 plugins、指向 modules 0 处，且有一条在册门按 plugins 的索引节解析），已按此改写 49 行应角色与 2 行合并方向。它同时证伪了 D1 给出的一条迁移去向（目标文件根本不在跟踪集）。

## 第二层复核结论（逐条回）

- **复核-架构接线**：W1 确认（P1，术语未串轴："生产调用面零引用"，三档计数已分生产/测试/自身）；W2 确认事实但**降级 P2**（运行时读的是长名面、那份注册表文件运行期根本不被读 ⇒ 非功能缺陷；且成稿说"短名侧无机器门核对"说过头——安装面检查器正核 module_id 且带必败负例，缺的是**跨面比对门**）；W3 确认并修正计数（私建线程池真值 16＝算法内 6／基建内 10，成稿把注释行算了一处），**另立新 P1**：调度器直接起满预算个 worker 而**从不申请**，而它自己的头文件声明"调度器 worker 与节点内部工作共用同一预算、Σ≤预算" ⇒ 稳态存活量可达 2 倍预算、回压判据失真（实测确认则升 P0）；W4 **部分推翻**——"产品内无动态加载"不成立（确有一条真加载路径由基准测试命令活调用，且那份预检逐条实现了上位指名的五项检查），真实形态是"两份加载器实现、图上那份不是上位指名的那份"；装出去的共享库实为 9 份，**无一在产品的链接行上、也无一是活加载器会扫的目录**（它扫的目录不在安装布局里、它要的清单不在仓库里）⇒ 动态 ISA 选择在产品上恒不生效（P1）。近失一条：某个 provider 与活加载器同 ABI 同符号名，只因放错目录而永不被扫。双平台已分开判：Windows 侧属未开工，不判缺陷，只前瞻登记"不得照抄已实现"。
- 待负责人裁两条（复核给成二选一并附推荐）：① 模块形态——同一份最高设计里两句相反（"每个算法模块是独立 DLL/SO" vs "以 DLL/SO 存在**或在可执行程序内部实现**"），推荐承认内联合法、把前者改为可选，否则 W4 在 P1/P2 之间悬着、文档面整治无法排序；② 阶段调度器身份——是"该阶段唯一执行者"还是"节点内执行体、通用调度器保持全局唯一执行者"（现状由导出腿给出可复用范式），推荐后者，否则工单规模差一个量级。
- 更正一条：上面那路成稿自称"三条需裁"，正文只列了两条——按正文计。

- **复核-门禁入口：W1 确认＋W2 确认，并订正 AUD-501 三处数**。最硬的新事实：**两条执行入口都能零执行报绿**——外层 CI 入口自己封死了空集，但注册表被一条 P0 判据强制"聚合项必须经内层入口派发"，实测 71/71 聚合项全派发、**291/370 个执行单元的判定住在那条有洞的入口里**（守卫在外层、洞在被它嵌套的那一层）。文档配套：逐门复跑命令 58 行**全走有洞那条**、0 行走 CI 那条，而验收规范把"这条入口返回 0"写成通过标准。三处订正：① "platform=linux 345 个单元"错，345 是"档位含 linux-main"的数，platform 实为 any 223／linux 143／windows 4；② "在 Windows 上跑 linux-main 会全跳全 PASS"**被推翻**（345 选中、137 跳、208 真可执行），真能整项零执行报绿的是"按名点名"那条路径——23/150 注册项整项全跳即返回 0，其中 8 项是文档点名且标 P0 的；③ "普查表 213 个通过"是错读（213＝声明了 outputs 的单元数），"阈值 50 抄自观测值"也不成立（脚本与归档表同一次提交落地、当时观测值是 230；50 是**无来源魔数**，不改变"该普查无判别力"的结论）。另加两条独立增项：普查的单元枚举不做父项继承 ⇒ **42 个单元的"普查面"与"强制面"不是同一对象**；它的正例拿"我自己零失败"当证明 ⇒ 循环自证。已产出给门禁重设计的 7 条约束。
- **连带纠正本节点自己既往的读数（已写进项目记忆）**：我上一轮"全量扫描红灯分档"用的是快速档，只覆盖 52.4% 的执行单元，且该入口把跳过计绿 ⇒ 那批数不能当全量结论；被归入"平台跳过"的 3 条在整轮汇总面上其实是 PASS。今后红灯计数一律附三件：档位、被选中单元数/总单元数、该入口是否把 skip 计绿。
