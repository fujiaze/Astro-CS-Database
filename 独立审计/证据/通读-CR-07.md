# 代码通读 CR-07 · lib/algorithms/photometry 第 7 段（5 文件）

- 被审基线 HEAD：`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`（`git rev-parse HEAD` 实测一致）
- 派单：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§1／§2／§2.5／§3／§4／§5 照做）
- 清单：`独立审计/批次清单/CR-07.txt`（5 个文件）
- 纪律：只读；零 git 写；不跑构建/ctest/run_checks/`eng/**`；不 import 仓库 Python。

## 开工基线 `git status --porcelain` 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（与派单 §1 预期基线一致。）

## 文件清单与读取状态

- lib/algorithms/photometry/cpp/test/test_p0_pc_api_exception_barrier.cpp —— 已读：是（表 29 项，findings CR-07-01/02/04/05/07/08）
- lib/algorithms/photometry/cpp/test/test_p0_sip_order_guard.cpp —— 已读：是（表 24 项，findings CR-07-09/10/11）
- lib/algorithms/photometry/cpp/test/test_spectrum_integrator.cpp —— 已读：是（表 18 项，findings CR-07-12/13/14）
- lib/algorithms/photometry/cpp/src/wcs_transform.cpp —— 已读：是（表 19 项，findings CR-07-03/06/15/16/17/18）
- lib/algorithms/photometry/cpp/src/wcs_transform.h —— 已读：是（表 8 项，finding CR-07-19）

<!-- PROGRESS: 5/5 -->

## 本批重点核查项（派单指定）

1. 异常屏障／退出码合同是否被断言**真锁住**（还是只锁"调用不发生"）。
2. 测试里写死的期望值与它在合同或科学文档里的口径是否同源。

---

# 文件 1 · lib/algorithms/photometry/cpp/test/test_p0_pc_api_exception_barrier.cpp（158 行，整读）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :5 | 导出入口数 | 6 | 注释断言"全部 6 个 PC_API 导出入口"已包屏障 | 结构性不适用（枚举基数，实测 `photometric_calib.h` 恰有 6 条 `^PC_API`） | `lib/algorithms/photometry/cpp/include/photometric_calib.h:106,158,…`（grep `^PC_API` = 6） | — | CONFIRMED |
| :12,:63（引 pc_api） | 错误码 `-4` | -4 | 内部异常→C 边界错误码 | 合同值（本头文件自订，非科学量）→ 结构性不适用 | `photometric_calib.h:103-105`、`pc_api.cpp:63,167` | 但该码未进对外合同面：`docs/contracts/API_CONTRACTS.csv:284` 只写 "return code 0=OK"；见 CR-07-04 | CONFIRMED |
| :12 | 已占码集合 | 0/-1/-2/-3 | 断言 -4 不与既有码冲突 | 结构性不适用（枚举） | 同上头文件 :102 | -3 对 `pc_calibrate_simple` 实际从不产生（见 CR-07-05） | CONFIRMED |
| :44 | `W`/`H` | 8 / 8 | 夹具图像边长（px） | 结构性不适用（夹具尺寸，不进科学语义） | — | 与 crpix=100 不自洽 ⇒ 见 CR-07-02 的零匹配根因 | CONFIRMED |
| :64 | 像素填充值 | 100.0f / 100.0 | 输入像面常数 ADU | 结构性不适用（夹具内容，不参与被判的量，因 T4 实际零匹配） | — | 若补上真匹配断言，此常数将决定 F_instr | CONFIRMED |
| :65 | `gaia_ra` | 10.0（×2） | Gaia 星 RA(deg)，与 crval1 同值 ⇒ 正切点 | 结构性不适用（夹具自订） | — | 投影后像素=(99,99)，出 8×8 视场 | CONFIRMED（几何手算） |
| :65 | `gaia_dec` | 20.0（×2） | Gaia 星 Dec(deg)，与 crval2 同值 | 结构性不适用（夹具自订） | — | 同上 | CONFIRMED |
| :66 | `gaia_mag` | 15.0 | Gaia 星等 | 结构性不适用（夹具自订） | — | 零匹配 ⇒ 未参与 | CONFIRMED |
| :66 | `gaia_fsyn` | 1000.0 | 合成流量（W·m⁻²） | 结构性不适用（夹具自订） | — | 零匹配 ⇒ 未参与 | CONFIRMED |
| :67 | `psf_cx` | 50.0 / 150.0 | PSF 星 x(px) | 结构性不适用（夹具自订） | — | 与 Gaia 投影点相距 49/51 px ≫ 2 px 匹配半径 ⇒ 必然零匹配 | CONFIRMED |
| :68 | `psf_cy` | 100.0 / 100.0 | PSF 星 y(px) | 结构性不适用（夹具自订） | — | 同上 | CONFIRMED |
| :68 | `psf_flux` | 1000.0 | 仪器流量（ADU） | 结构性不适用（夹具自订） | — | 零匹配 ⇒ 未参与 | CONFIRMED |
| :68 | `psf_status` | 0 | 有效位（0=ok） | 合同值→结构性不适用（枚举） | `star_matcher.cpp:236` `psf_status[i]==0` | — | CONFIRMED |
| :69 | `sip_zero` 长度 | 36 | SIP 系数数组长度 | 公式导出：6×6 行主存 `i*6+j`，order≤5 ⇒ 最大下标 5*6+0=30≤35 | `wcs_transform.h:16-18`、`wcs_transform.cpp:67`（无条件拷 36） | 与 `docs/contracts/DATA_SEMANTICS.md:679` 的 `double [36]（i*6+j）` 同源；但该同行把域写成 ≤2 ⇒ CR-07-03 | CONFIRMED |
| :69 | `sip_zero` 值 | 0.0 | 全零畸变系数 | 结构性不适用（负例夹具只需长度够，值不参与） | — | — | CONFIRMED |
| :77 | `n_matched` 哨兵 | -999 | 未写盘哨兵 | 结构性不适用（哨兵选择） | — | **无任何 CHECK 读它** ⇒ CR-07-01 的另一半证据 | CONFIRMED |
| :78 | `scale`/`sigma` 哨兵 | -999.0 | 同上 | 结构性不适用（哨兵选择） | — | 同上 | CONFIRMED |
| :85,:87,:100… | `(int)size()` | 2 | 星数 n_gaia / n_psa | 结构性不适用（数组长度） | — | — | CONFIRMED |
| :89 | `crval1/crval2` | 10.0 / 20.0 | 参考天球坐标(deg) | 结构性不适用（夹具自订） | — | — | CONFIRMED |
| :89 | `crpix1/crpix2` | 100.0 / 100.0 | 参考像素(1-based) | 结构性不适用（夹具自订） | — | 与 W=H=8 矛盾（参考点落在像外）⇒ CR-07-02 根因 | CONFIRMED |
| :89 | `cd11/cd12/cd21/cd22` | 1.0e-4 / 0.0 / 0.0 / 1.0e-4 | CD 矩阵(deg/px)，即 0.36″/px | 结构性不适用（夹具自订；1 deg=3600″ 属单位定义常数） | — | det=1e-8 ≫ 1e-15 奇异阈值 ⇒ 不触发 CR-07-06 | CONFIRMED |
| :90,:105 | 越界阶 `8` | 8 | 负例：>5 上界外第一档 +3 | 公式导出：合法域 [0,5] 之外任一值；`8` 取的是历史 OOB 证据值（8*6=48>35） | `wcs_transform.cpp:41-43,46` | — | CONFIRMED |
| :120 | 越界阶 `-1` | -1 | 负例：下界外第一档 | 公式导出：[0,5] 下界 −1 | `wcs_transform.cpp:46` | — | CONFIRMED |
| :135,:151 | 合法阶 `0` | 0 | 正例：无 SIP | 合同值→结构性不适用（枚举成员） | `wcs_transform.h:17` | — | CONFIRMED |
| :92,:107,:122 | 期望 rc | -4 | 屏障返回码断言 | 合同值（同上 :12） | `photometric_calib.h:103` | 断言方向成立（能红能绿），缺输出侧锁 ⇒ CR-07-01 | CONFIRMED |
| :137 | 期望 rc | 0 | 正常路径 | 合同值 | `API_CONTRACTS.csv:284` "return code 0=OK" | 由退化路径也可满足 ⇒ CR-07-02 | CONFIRMED |
| :138 | `scale > 0.0` | 0.0 | "输出已填充"回归锁 | **待确认**：判据方向只拦 `≤0` 与 NaN；rc==0 面 scale ∈ {1.0 退化默认, 10^(−location)>0}（`star_matcher.cpp:404,589-590`）⇒ 对"是否真走过一次校准"零判别力 | `star_matcher.cpp:404,411-413,589-590` | 保守方向：改判 `n_matched == 夹具预期颗数` ＋ scale 按导出式取值；影响范围＝本条门对"正常路径回归"无判别力 | CONFIRMED |
| :153 | 期望 rc | -1 | 空输出指针参数校验 | 合同值 | `pc_api.cpp:77-81` | 与 `DATA_SEMANTICS.md:671`（尺寸≤0→rc=−1）与代码（−2）三方不一致 ⇒ CR-07-04 | CONFIRMED |
| :157 | 退出码 | 0 / 1 | 测试程序自身成败 | 结构性不适用（ctest 约定） | `eng/tests/unit/CMakeLists.txt:1633` `add_test` | — | CONFIRMED |

## findings

### CR-07-01 · 屏障测试只锁"返回码是 -4"，不锁"跨界副作用为零"：哨兵值 -999 被声明却从未被断言
- 轴：R（主）/ D
- 位点：`lib/algorithms/photometry/cpp/test/test_p0_pc_api_exception_barrier.cpp:77-78`
  `int n_matched = -999;` / `double scale = -999.0, sigma = -999.0;`
- 上位依据：`docs/plugins/algorithms_phase1/06_photometry.md:93-99` 帧级失败语义要求"不产出 `photoapplied_<base>`、不进 `photscales`"；`lib/algorithms/photometry/cpp/src/pc_api.cpp:60-64` 注释自陈"正常路径与修复前逐行等价"。条文面对"异常路径输出参数是否允许被半写"**零提及**（检索式：`git grep -n "PHOT_WCS_SIP_INVALID"`、`git grep -n "半写\|部分写\|untouched" -- docs/contracts`）。
- 现状：T1/T2/T3 只有 `CHECK(rc == -4, …)`（:92/:107/:122）；三个哨兵 `n_matched=-999`、`scale=-999.0`、`sigma=-999.0` 在全部 5 个用例里没有任何一次被读进 CHECK。同一 `fx.pix_f32.data()` 还被同时当作输入 `pixels` 与输出 `out_pixels`（:83 与 :91）。
- 差在哪：屏障合同的两半（"不外泄异常"+"不留半截产品"）只被判了一半；对输出侧的写保护完全靠实现偶然（当前 `pc_api.cpp` 把输出写在 :158-160、抛点在 :123，故恰好未写）。
- 后果：日后任何把抛点下移到输出之后的改动（例如在 `correctImage` 之后才做入参校验、或 `matchWithKdTree` 内部先写 records 再抛）会留下被半改写的 `out_pixels`/`n_matched` 而测试仍全绿，"帧级失败不产出产品"的对外主张失去机器锁。
- 定级：S2（缺生产触发路径：本条缺陷在测试面，不直接产错值；生产面同族风险见 CR-07-06 的路径）
- 怎么算修好：T1/T2/T3 各补 `CHECK(n_matched == -999)`、`CHECK(scale == -999.0)`、`CHECK(sigma == -999.0)` 与输入缓冲逐字节未变（把输入复制一份做 memcmp）；同时把 `out_pixels` 换成独立缓冲，避免输入/输出别名。同批须在 `docs/contracts/API_CONTRACTS.csv` 或 `DATA_SEMANTICS.md` §14 的返回码面写明"rc<0 ⇒ 输出参数不写"，否则新断言无锚。
- 置信：CONFIRMED（对"断言缺失"逐行读证；对"当前实现恰好未写"读了 pc_api.cpp:77-160 全路径）

### CR-07-02 · T4「合法路径不受屏障影响 → 0」是退化绿：夹具几何上必然零匹配，rc=0 与 scale=1.0 都由"无匹配"退化路径给出
- 轴：R（主，恒真/退化判据）
- 位点：`test_p0_pc_api_exception_barrier.cpp:137-138`
  `CHECK(rc == 0, "T4 pc_calibrate_simple sip_order=0 合法路径 → 0 (不误伤)");` / `CHECK(scale > 0.0, "T4 输出 scale 已填充 (>0)");`
- 上位依据：`docs/plugins/algorithms_phase1/06_photometry.md:93-99`（帧级/全局失败码面）与 `docs/contracts/DATA_SEMANTICS.md:671`「n_psf=0 → 退化 scale=1.0 rc=0」（合同自认退化路径也回 rc=0）。
- 现状：夹具 `W=H=8`（:44）、`crpix=(100,100)`（:89）、Gaia 两点均取 `ra=10.0, dec=20.0`（:65）与 `crval=(10,20)`（:89）同值 ⇒ `skyToPixel` 落在 `(99,99)`（`wcs_transform.cpp:257-258` `x = dx + (crpix1-1)`，正切点 dx=0）；PSF 星在 `(50,100)`/`(150,100)`（:67-68），与 (99,99) 相距 49.02/51.0 px；`match_radius_px` 在 `pc_api.cpp:137` 写死 `2.0`（`DATA_SEMANTICS.md:675`「合同值 2.0」）⇒ `matchWithKdTree` 前/反向命中必为 0（`star_matcher.cpp:277-284,293-300`），`cleanAndScale` 在 `n_in==0` 时直接 `*out_scale_factor = 1.0; return {}`（`star_matcher.cpp:404`、`411-413`），`run_simple_impl` 仍 `return 0`（`pc_api.cpp:164`）。
- 差在哪：名为"合法路径"的正例实际只验证了"构造不抛"，从未走过一次真匹配。**判据方向核对**：`scale > 0.0` 只在 `scale ≤ 0` 或 NaN 时判红，而 rc==0 面 `scale ∈ {1.0（无匹配退化默认，`star_matcher.cpp:404`）, 10^(−location)（:589）}`，二者恒 > 0 ⇒ 该断言对"零匹配退化"与"真校准成功"两种取值不可区分，即对本用例自称的"正常路径"零判别力（不等式方向偏绿，不偏红）。
- 后果：`photometric_calib` 正常路径（投影→KD 匹配→IRLS→scale→校正）在本机门上无任何回归锁；把 scale 算成 1.0、把匹配全丢光、把 out_pixels 原样透传这类错误都不会被 T4 判红。对外"测光校准到星等坐标系（SCI-A）"链条的工程回归面缺一块。
- 定级：S2（测试面缺陷；写不出"由此直接产出错值"的生产路径，但本批文件 2 名下的门面试登记条目可追认"该锁在机器门上是否真跑"这一层）
- 怎么算修好：把夹具改成自洽小视场（例：`crpix=(4,4)`、Gaia 投影落在 (2,3)/(6,5)，PSF 同点位 ±0.3 px），并补 `CHECK(n_matched == 2)` 与 `CHECK(scale == 期望值)`（期望值须由 §2.5 锚给出导出式：`scale = 10^(-median(log10(F_instr/F_syn)))`，本夹具 F_instr=100.0、F_syn=1000.0 ⇒ scale=10^(2.5)=316.227766…，须写成公式导出而非字面量）。改判据时同步补 `sigma_residual`（同夹具下 MAD=0 ⇒ 期望 0.0，注意除 0.6744897501960817 不改变 0）。
- 置信：CONFIRMED（几何与半径逐值手算；对 `findNearest` 的 `max_dist2` 语义读了 `star_matcher.cpp:266,279`）

### CR-07-04 · 尺寸非法的错误码三侧不同源：合同 §14.1 自称"唯一权威"写 rc=−1，本文件被测的两条入口实际返回 −2，−4 从未登记进合同面
- 轴：C（主）/ R
- 位点：`test_p0_pc_api_exception_barrier.cpp:153` `CHECK(rc == -1, "T5 out_pixels=nullptr → -1 (原参数校验语义不变)");`；`:12` `// 修复口径 … 错误码 -4 (内部异常; 0/-1/-2/-3 此前已占用)。`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:657-658`「本节是该模块 单位/dtype/shape/invalid 的**唯一权威**」＋ `:671` `| width/height | int32 标量 | px | ≤0 → rc=−1 |`；`lib/algorithms/photometry/cpp/include/photometric_calib.h:102` `-1: 空指针参数, -2: 尺寸无效`；`docs/contracts/API_CONTRACTS.csv:284-289` 六条 pc_* 入口的 return code 列只写 `return code 0=OK` 且状态 `VERIFIED`。
- 现状：`pc_api.cpp:82-84`（`run_simple_impl`）与 `:543-545`（f64）对 `width<=0||height<=0` 返回 **-2**；`pc_api.cpp:252-254, 671-673, 931-933`（三条 with_gaia impl）对同一条件返回 **-1**。本文件 T5 只测 nullptr→-1 这一条与三侧一致的码，`-2` 分支零断言；`-4` 只存在于头文件注释与本测试断言，合同 CSV/§14 面无任何登记。
- 差在哪：同一条 invalid 语义在"唯一权威"合同表与 6 条实现中的 2 条取不同数值（合同 −1 / 实现 −2），且屏障新增的 −4 从未回写合同面 ⇒ 三侧不同源。
- 后果：按 §14.1 编写的 ctypes/GUI 调用方（正是本测试 :8 自陈的防护对象"Python ctypes/未来 GUI 只面对 C ABI"）对 simple/_f64 的尺寸错误收到未登记的 −2，对内部异常收到未登记的 −4 ⇒ 错误分类落进"未知码"，`docs/plugins/algorithms_phase1/06_photometry.md:93-99` 的帧级/全局失败分流失去可核对的映射依据；对外"合同 VERIFIED"的主张对这 6 行失真。
- 定级：S2（合同面失真可直接判定；生产触发路径止于 `pc_calibrate_simple*` 本身被登记为生产不可达：`CMakeLists.txt:758`、`eng/ci/prod_wiring_baseline.json:207-208`）
- 怎么算修好：先定哪一侧是权威（建议 §14.1），随后同批改三处：① §14.1 补 `-2 尺寸无效 / -4 内部异常` 两行并把 `≤0` 行的取值按各入口分别写（或把实现统一到合同值）；② `API_CONTRACTS.csv` 的 return code 列从 `0=OK` 改为完整码集；③ 本测试补 `CHECK(width<=0 → 期望码)` 的负例，否则改码无锁。**注意防"改对反而判红"**：若把 §14.1 的 `≤0` 改成"分入口"，须同时改 `DATA_SEMANTICS.md:670` 的 `h·w=0/NULL → rc=−1` 行。
- 置信：CONFIRMED（三侧原文逐条读毕）

### CR-07-05 · 本文件 :12 依赖的"−3 已被占用"前提对 `pc_calibrate_simple` 是虚的：该码在此入口从不产生，且与合同"退化 rc=0"相反
- 轴：C
- 位点：`test_p0_pc_api_exception_barrier.cpp:12` `// 错误码 -4 (内部异常; 0/-1/-2/-3 此前已占用)。`
- 上位依据：`photometric_calib.h:102` `-3: 无Gaia星/PSF星(退化scale=1.0)`（该注释块紧接 `:106 PC_API int pc_calibrate_simple(`）；`docs/contracts/DATA_SEMANTICS.md:671` `n_psf=0 → 退化 scale=1.0 **rc=0**`。
- 现状：`pc_api.cpp:86-99` 与 `:100-112` 在无 Gaia / 无 PSF 两支里 `*out_scale_factor = 1.0; … return 0;`，全文件 `return -3` 只出现在 `:309 / :726 / :996`（三条 with_gaia 通道的"锥形搜索失败或无光谱星"，头文件 `:155` 另有其义）。
- 差在哪：头文件为 `pc_calibrate_simple` 登记的 −3 语义（无星退化）在实现里返回 0，本文件又把 −3 当作"已占用"的分配理由 ⇒ 一处注释同时与代码和合同不一致，测试注释继承了 phantom 码。
- 后果：① "无星退化"对外是失败还是成功，三侧各说一套（合同=0、头=-3、代码=0），② 未来按头文件写 `-3 → 帧级 PHOT_FIT_NO_SCALE` 的编排分流永不被触发（静默降级为"成功且 scale=1.0"）。后者是真 fail-open。
- 定级：S2（方向核对：把退化当成功只会让门偏绿；但本条的生产触发链在 `module_adapters.cpp` 侧未成立 ⇒ 缺运行时路径，最高 S2）
- 怎么算修好：删/改 `photometric_calib.h:102` 的 −3 行为"仅 with_gaia 通道：锥形搜索失败"（与 `:155` 对齐），本文件 :12 的分配理由同步改写；若负责人裁定"无星必须是失败"，则改 `pc_api.cpp:98/111` 的返回值，并同批补 `p1phot` 与本合同表的退化语义行，否则会把现有绿门改成红。
- 置信：CONFIRMED

### CR-07-07 · −4 屏障断言全部落在生产不可达的冻结入口；生产可达的 `*_with_gaia_v2/_f64_v2` 的 −4 路径在整个跟踪测试面零断言
- 轴：D（主）/ R
- 位点：`test_p0_pc_api_exception_barrier.cpp:5` `// 背景: lib/algorithms/photometry/cpp/src/pc_api.cpp 全部 6 个 PC_API 导出入口`、`:14-19` 覆盖表（T1–T5 只列 `pc_calibrate_simple` / `_f64`）
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:666`（§14.1 标题）「输入（**生产通道** `pc_calibrate_simple_with_gaia_v2 / _f64_v2`）」；`docs/architecture/execution_inventory.csv:4` 生产链 `main->…->pc_calibrate_simple_with_gaia[_f64]_v2`；`CMakeLists.txt:758` 与 `eng/ci/prod_wiring_baseline.json:207-208` 把 `pc_calibrate_simple`/`_f64` 记为不可达/只在测试 target。
- 现状：全跟踪集里断言 `== -4` 的 pc_* 面共 3 处——本文件 `:92/:107/:122`（simple、simple_f64）、`lib/algorithms/photometry/tests/p1phot/p1phot_tests_negative.cpp:338`（`pc_calibrate_simple`，N8）、`p1phot_tests_units.cpp:488`（`pc_calibrate_simple_f64`，U8）；`pc_api.cpp:1232-1235, 1278-1281, 1331-1334` 三条 v2/qf 入口的 catch→(-4,-4) 无任何测试触达（检索式：`git grep -n "== -4\|rc == -4" -- "*test*" "*Test*"`）。
- 差在哪：文件头声称的"6 个入口全部包屏障"这一 D 轴能力，只在其中 2 个**生产不可达**入口上有机器锁；对外合同点名的生产通道恰好是零锁的那几条。
- 后果：把 v2 入口的 `WcsTransform` 构造或任何可抛调用移出 `try` 块（或新增 wrapper 未包）不会判红，跨 `extern "C"` 展开 = UB，正是本文件 :8 自陈要防的形态；"C ABI 不抛异常"的对外主张对生产通道不成立。
- 定级：S2（测试/门面缺陷本身不产错值；缺生产触发路径）
- 怎么算修好：本文件已自带 `stub::`-式做法的邻件——用 `p1phot` 套件的 `stub::GaiaClient`（`p1phot_tests_negative.cpp:343` 用法）以 `sip_order=6` 调 `pc_calibrate_simple_with_gaia_v2` 与 `_f64_v2` 各断言一次 `rc == -4`，并把 :14-19 覆盖表补成 6 入口逐条；或把这三条 catch 站点纳入 `p1phot_tests_negative`。删除 `:48-53` 的手写 stub 前先确认 CMake 链接面（现由 `astrocs_phase1_photcal` 归档提供符号）。
- 置信：CONFIRMED（三处断言站点 + 7 处 catch 站点逐一读毕）

### CR-07-08 · 文件头给出的编译配方指向仓库中不存在的路径与扩展名（悬空锚）
- 轴：C
- 位点：`test_p0_pc_api_exception_barrier.cpp:25` `//     ../../gaia_client/src/gaia_client.cpp \`
- 上位依据：条文面零提及该路径；实际宿主为 `lib/infrastructure/gaia_xpsd_client/src/gaia_client.c`（`git ls-files "lib/algorithms/gaia_client/*"` → 0 条；`git ls-files "lib/infrastructure/gaia_xpsd_client/*"` 含 `src/gaia_client.c`，无 `.cpp`）。真实构建宿主是 `eng/tests/unit/CMakeLists.txt:1620-1634` 的 `w34_p0_pc_api_exception_barrier`（链接 `astrocs_phase1_photcal`，不编 gaia 客户端）。
- 现状：注释以"编译 (lib/algorithms/photometry/cpp/ 目录)"口吻给出可照抄命令，其中依赖路径不存在、扩展名不符，且与该测试实际链接方式不同。
- 后果：照抄必失败；复现命令是 §5 与宪章要求的交付面之一（"报告须含复现命令"），失真即复现链断。
- 定级：S3（不产错值）
- 怎么算修好：把 :21-27 改写为真实宿主命令 `cmake --build build --target w34_p0_pc_api_exception_barrier && ctest --test-dir build -R w34_p0_pc_api_exception_barrier --output-on-failure`。同批把 `test_p0_sip_order_guard.cpp:26-31` 的同型配方一起改（本批文件 2）。
- 置信：CONFIRMED

---

# 文件 2 · lib/algorithms/photometry/cpp/test/test_p0_sip_order_guard.cpp（136 行，整读）

**小结（对派单核查项①）**：T1/T2/T3（:76/:87/:98 `CHECK(threw, …)`）是**真锁**——判据为"必须抛 `std::invalid_argument`"，若 `wcs_transform.cpp:46` 的守卫被删或放宽则 `threw=false` ⇒ 必红；异常类型若被改成别的（`out_of_range` 等）也必红。方向成立，不属"只锁调用不发生"。失锁发生在正例侧（T5/T6），见 CR-07-09。

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :5,:12 | SIP 合法域 | [0,5] | 守卫域（负例取边界外档） | 公式导出：`m_sip_*_buf[36]` 按 `i*6+j` 存 ⇒ 行标 i≤5 ⇒ order≤5；越界即 member-load OOB | `wcs_transform.h:16-18`、`wcs_transform.cpp:41-46`、`module_adapters.cpp:4353`（同域） | 与 `docs/contracts/DATA_SEMANTICS.md:679` 同行写的 **≤2** 不同源 ⇒ CR-07-03（本批文件 4 名下） | CONFIRMED |
| :7,:9 | 缓冲项数 / 越界式 | 36 / `6*order>35` | 越界读的判定式 | 公式导出：`i*6+j`，i=order,j=0 ⇒ 索引 6·order；6·6=36>35 即出对象 | 复算：order=6→idx36（首个出界档，与 :68 注释"边界外第一档"一致）；order=5→idx30≤35 | — | CONFIRMED |
| :13 | 屏障错误码 | -4 | 越界经 C 边界的落地码 | 合同值（头文件自订，非科学量）→ 结构性不适用 | `photometric_calib.h:103-105` | 该码在 `API_CONTRACTS.csv` / §14 面零登记 ⇒ CR-07-04 | CONFIRMED |
| :16-:22 | 覆盖表 order 档 | 6,8,-1,0,5 | 边界外两档 + 下界外一档 + 两个正例档 | 公式导出（域 [0,5] 的边界邻域取样：-1/6 为紧邻外档） | `wcs_transform.cpp:46` | — | CONFIRMED |
| :22 | T6 声称的判据 | "36 项缓冲内不越界" | 声明要锁的语义 | **待确认**：断言面（不抛 + 0.1 px 往返）不具检出越界读的能力，默认构建无 ASan | `CMakeLists.txt:21` `option(ASTROCS_ENABLE_SANITIZERS … OFF)` | 保守方向：该用例只在 ASan 构建下有资格；影响范围＝T6 现为无锁宣称 ⇒ CR-07-11 | PARTIAL（未确认 `qa-sanitize` 目标是否含本测试） |
| :52,:53 | `a.assign` / `b.assign` | 36, 0.0 | 系数数组长度与初值 | 结构性不适用（数组维度 = `m_sip_*_buf` 同宽；初值为夹具底色） | `wcs_transform.h:44-47` | — | CONFIRMED |
| :55 | `a[3*6+0]` = `scale*1.0e-3` | 1.0e-3 | A_30 系数幅值（1/px³） | **待确认**：无锚（`git grep -n "1.0e-3" -- docs` 该量级在科学/合同面零命中） | — | 保守方向：畸变幅值须由 fixture 登记面给（如 `GATES_AND_TOLERANCES.md:70` 的边缘畸变预算 18.4/91.8/183.6 px 三档），当前值在 dx=1 处仅产生 1e-5 px ⇒ 判据力归零，见 CR-07-09 | CONFIRMED（幅值后果已复算） |
| :56 | `a[2*6+1]` = `scale*2.0e-3` | 2.0e-3 | A_21 系数幅值（1/px³） | **待确认**（同上） | — | 同上 | CONFIRMED |
| :57 | `b[0*6+3]` = `scale*1.5e-3` | 1.5e-3 | B_03 系数幅值（1/px³） | **待确认**（同上） | — | 同上 | CONFIRMED |
| :58 | `b[1*6+2]` = `scale*2.5e-3` | 2.5e-3 | B_12 系数幅值（1/px³） | **待确认**（同上） | — | 同上 | CONFIRMED |
| :55-:58 | 索引位移 `*6` | 6 | 行主存步长 | 结构性不适用（缓冲行宽，非科学量），且与 `wcs_transform.cpp:98` 的 `i*6+j`、`module_adapters.cpp:4347` 的 `c[i*6+j]` 同式 | 同上 | — | CONFIRMED |
| :55-:58 | 守卫 `size() > idx` | 3 项 | 越界写防护 | 结构性不适用（size_t 恒真：`assign(36)` 后 36>18 恒成立） | — | 该条件是死代码（不影响正确性） | CONFIRMED |
| :66 | `zero4` | 36 项 / 0.0 | 负例用的合法长度全零系数 | 结构性不适用（数组维度；值不参与，构造即在 :46 抛出） | `wcs_transform.cpp:67-72`（先抛后拷） | — | CONFIRMED |
| :72,:83,:94,:106,:123 | `crval1/crval2` | 10.0 / 20.0 | 参考天球点(deg) | 结构性不适用（夹具自订） | — | — | CONFIRMED |
| :72,:83,:94,:106,:123 | `crpix1/crpix2` | 100.0 / 100.0 | 参考像素(1-based) | 结构性不适用（夹具自订） | — | 与 :109/:126 的采样点 (100,100) 组合 ⇒ dx=dy=1，见 CR-07-09 根因 | CONFIRMED |
| :73,:84,:95,:107,:124 | CD 对角 | 1.0e-4 / 0.0 | 像素标度(deg/px)=0.36″/px | 结构性不适用（夹具自订；1 deg=3600 arcsec 属单位定义常数） | — | det=1e-8，远大于 `wcs_transform.cpp:54` 的 1e-15 ⇒ 不触发奇异分支 | CONFIRMED |
| :74,:85,:96,:108,:125 | `sip_order` 实参 | 6 / 8 / -1 / 0 / 5 | 五个档位的入参 | 公式导出（同 :16-22） | `wcs_transform.cpp:46` | — | CONFIRMED |
| :109,:126 | 采样像素 | 100.0 / 100.0 | 前向求值点 | **待确认**：与 crpix 同值 ⇒ 归一化偏移恰为 1（`x-(crpix-1)`），任何幂次均为 1 | `wcs_transform.cpp:190-191` | 保守方向：应取远离 crpix 的像素（角点/边缘），见 CR-07-09 | CONFIRMED（手算） |
| :112 | T4 RA/Dec 窗口 | 9.999/10.001、19.999/20.001 | "RA/Dec 合理"的接受域 = ±0.001° = ±3.6″ | **待确认**：±3.6″ 与门表任何 WCS 行都不同源（`GATES_AND_TOLERANCES.md:64` F2=1e-4 px、`:65` RT=1e-4 px、`:69` ITER=1e-8 px；按 0.36″/px 换算 1e-4 px=3.6e-5″） | 复算：dx=1 ⇒ ξ=1e-4°，RA 偏移 ≈1.064e-4°，落在窗口内且窗口余量 ~1000× | 见 CR-07-10 | CONFIRMED |
| :119 | `fill_sip_coeffs(…, 1.0e-2)` | 1.0e-2 | 系数总放大因子 | **待确认**：无锚；与 :55-58 相乘后在 dx=1 处仅 3e-5/4e-5 px | — | 保守方向：与采样点一起重定，使 SIP 修正 ≥ 门表容差的 1e3 倍 | CONFIRMED |
| :121 | `x`/`y` 初值 | -1.0 | 未写盘哨兵 | 结构性不适用（哨兵；被 :130 的窗口断言实质读到） | — | — | CONFIRMED |
| :130 | T5 往返接受域 | 99.9 / 100.1（±0.1 px） | "往返一致"的判据 | **待确认**：±0.1 px 在门表与 `docs/science` 零登记，且比全链冻结门 1e-4 px 松 1e3 倍、比迭代路径门 1e-8 px 松 7e6 倍 | `docs/algorithms/GATES_AND_TOLERANCES.md:65,69`；`docs/science/ASTROMETRY.md:145-148` | 见 CR-07-09/CR-07-10（方向：只会偏绿） | CONFIRMED |
| :135 | 退出码 | 0 / 1 | 测试自身成败 | 结构性不适用（ctest 约定） | `eng/tests/unit/CMakeLists.txt:1634` `add_test(NAME w34_p0_sip_order_guard …)` | — | CONFIRMED |
| :27-:31 | 编译配方 | `g++ … src/wcs_transform.cpp` | 声称的复现命令 | 结构性不适用（无新数值），但配方与真实构建宿主不同源 | 真实宿主 `eng/tests/unit/CMakeLists.txt:1622-1634` | 同 CR-07-08 一并订正 | CONFIRMED |

## findings

### CR-07-09 · T5/T6 的采样点使 SIP 修正量级为 3e-5 px、且 dx=dy=1 时所有幂次恒等 ⇒ "往返一致"断言在系数被完全忽略/索引错置时仍绿
- 轴：R（主，误绿判据）/ D
- 位点：`lib/algorithms/photometry/cpp/test/test_p0_sip_order_guard.cpp:126-127` `wcs.pixelToSky(100.0, 100.0, ra, dec);` / `wcs.skyToPixel(ra, dec, x, y);` 与 `:130` `CHECK(x > 99.9 && x < 100.1 && y > 99.9 && y < 100.1, "T5 order=5 往返 pixelToSky→skyToPixel 收敛到原点");`
- 上位依据：`wcs_transform.h:16` `// sip_a/b/ap/bp: SIP系数数组(长度36, 按i*6+j索引)`；`wcs_transform.cpp:89` `// coeffs按coeffs[i*6+j]存储, 对应dx^i*dy^j, 下三角i+j<=order`；本文件 `:22` 自称锁的就是这条索引约定（"36 项缓冲内不越界 (合法上界边界值)"）。
- 现状：`pixelToSky(100,100)` 与 `crpix=(100,100)` 组合得 `dx = x-(crpix1-1) = 1.0`（`wcs_transform.cpp:190-191`），`evalSip` 在 dx=dy=1 处化为"被读系数之和"（:96-100 `pow(1,i)=pow(1,j)=1`）。系数总幅 `Σ|c| = 1e-2·(1.0+2.0+1.5+2.5)e-3 = 7e-5`，故 A、B 修正 ≤3e-5/4e-5 px。往返偏差上界（含"整套系数读错位置""m_has_sip 恒 false""迭代 0 次直接返回"三种缺陷）≤ Σ|c| + 反演残差 ~ 1e-4 px 量级。
- 差在哪：**不等式方向已定**——上述任一缺陷只会把 |x−100| 从 0 推大到 ≤1e-4 px，永远落在 0.1 px 窗内 ⇒ 判据恒绿（偏绿，不偏红）。该用例自称锁的"i*6+j 约定 / 36 项内不越界 / 往返收敛"三条能力没有任何一条具备判据力。
- 后果：`wcs_transform` 的 SIP 前向索引、`m_has_sip` 开关、无 AP/BP 时的反演近似（`wcs_transform.cpp:236-251`）在生产面（`module_adapters.cpp:5695-5696` 下发 `sip_order/sip_a/b/ap/bp` → `frame_photometry_fit.cpp:232` → `WcsTransform` → `pc_api.cpp:134 matchWithKdTree` 的 2.0 px 匹配半径）一旦错置，唯一声称能抓它的机器门不会红；Gaia 逆投影位置错位 ⇒ 星-图匹配错配 ⇒ 测光 scale 取错星的通量比（SCI-A 创新点的输入面）。
- 定级：S2（测试/门面判据缺陷；错值需另有一处真实代码缺陷才显形，故不给 S1）
- 怎么算修好：① 采样点改到视场边缘（例 `pixelToSky(1000.0, 200.0)` ⇒ dx=901, dy=101）；② 系数幅值按登记面给（`GATES_AND_TOLERANCES.md:70` 边缘畸变预算 18.4/91.8/183.6 px 三档之一），使 SIP 项本身 ≫ 断言容差；③ 断言改成**对拍**：与独立实现（`module_adapters.cpp:4415-4423 p1_sip_poly`（该函数注释自称"独立于 WcsSip … 交叉门用"））逐点比 `|Δ|<1e-12 px`，再对往返用门表值（迭代路径 1e-8 px 或全链 1e-4 px，按 §7 分层取对应那条）；④ 补"系数清零 ⇒ 归零"负例与"索引转置 ⇒ 必红"注入，否则新容差无资格。改判据须同批改 :22 覆盖表与文件头 :5/:12 的档位叙述。
- 置信：CONFIRMED（幂次与幅值界已逐式复算）

### CR-07-10 · 本文件写死的两条接受域（±0.1 px 往返、±3.6″ RA/Dec）在门表与科学合同里都无锚，且与门表分层口径互相冲突
- 轴：S/C
- 位点：`:130` `CHECK(x > 99.9 && x < 100.1 && y > 99.9 && y < 100.1, …)`；`:112` `CHECK(ra > 9.999 && ra < 10.001 && dec > 19.999 && dec < 20.001, "T4 天顶往返 RA/Dec 合理 (CRVAL 处)");`
- 上位依据：`docs/science/ASTROMETRY.md:145-148`「**SIP 逆一致性**：… 判据 `max‖(x,y)−WCS^{-1}(WCS(x,y))‖ ≤ 1e-4 px`（与 ALG-WCS-001 §11.4 F2 同值）……迭代反演路径另设紧门 `G-P1-WCS-RT-ITER = κ_iter·τ = 1e-8 px`……三条路径地板相差 5–9 个量级，**同一个数不能同时服务它们**」；`docs/algorithms/GATES_AND_TOLERANCES.md:65`（1e-4 px 全链冻结门）、`:69`（迭代路径 1e-8 px，"全链门 1e-4 px 对迭代路径过松 4 个量级"）。
- 现状：本文件 T5 走的正是"无 AP/BP ⇒ 迭代反演"路径（`wcs_transform.cpp:228-254`），却取 0.1 px；T4 的 ±0.001°（=±3.6″，在 0.36″/px 标度下 =±10 px）亦无登记；两数在 `docs/**` 门表面零命中（检索式：`git grep -n "G-P1-WCS-RT-ITER" -- docs`、`git grep -n "9.999\|100.1" -- docs`）。另 `:112` 的 msg 自称"天顶往返"，但 T4 只做前向 `pixelToSky`（:109），无反向调用。
- 差在哪：门表明文规定"迭代路径必须另设紧门"，本测试用第三个更松的数；msg 与实际断言的语义不匹配（往返→单向）。
- 后果： photometry 侧的 WCS 往返精度对外无口径（既不在门表也不在合同），门表的"唯一事实源"地位被测试面的自订数绕开；后续以本文件为证据声称"SIP 逆已验"是不成立的对外主张。
- 定级：S3（不直接产错值，失真的是判据口径与对外陈述）
- 怎么算修好：把 T5 容差改为门表适用那条（1e-8 px 迭代路径或 1e-4 px 全链，先由负责人/门表登记 photometry 宿主归哪条），并在 `GATES_AND_TOLERANCES.md` §3 为本测试登记的 ctest 宿主补一行适用域；T4 的 msg 改为"前向"，或补 `skyToPixel` 闭环并把两个数一起挂到同一登记锚。**同批**须一并核对 `wcs_transform.cpp:244` 的收敛阈 `1e-10` 与 `:237` 的迭代上界 3 是否需要按门表 τ=1e-9 登记（见文件 4 的表行）。
- 置信：CONFIRMED（门表三行原文与测试两数逐位比对）

### CR-07-11 · T6 自称锁"36 项缓冲内不越界"，但默认构建无 ASan，断言面（不抛 + 0.1 px）不具备检出越界读的能力
- 轴：R（门只看夹具不看能力）/ D
- 位点：`:22` `//   T6 order=5 越界系数   → 36 项缓冲内不越界 (合法上界边界值)`；实际实现合并在 :116-132 的 T5 块内，无独立断言。
- 上位依据：宪章 §9"判据必须非退化"、§5"实验单元自包含…结论经独立复核"；`CMakeLists.txt:21` `option(ASTROCS_ENABLE_SANITIZERS "Enable ASan/UBSan/LSan (GCC/Clang, production OFF)" OFF)`。
- 现状：越界读只能由 sanitizer 或值异常暴露；T5/T6 块在 order=5 时索引上界 30（:98 `coeffs[i*6+j]`），本身不出界，所以"不越界"在被测点上是**恒真陈述**（守卫已把 order 限到 5，索引必 ≤30）。真正的越界档（order≥6）由 T1/T2 以"必须抛"锁住，那是有效的。
- 差在哪：覆盖表把一个"由构造保证且无独立断言"的条目当作一条锁登记；若日后守卫被放宽到 [0,8] 而缓冲仍 36，本文件的 T1/T2 会红（good），但 T6 仍绿，给出的"SIP 缓冲安全性已覆盖"陈述失真。
- 后果：证据面（覆盖表）与机器锁一一对应关系被夸大；用 ASan 才能锁的东西写成普通断言登记。
- 定级：S3
- 怎么附好：要么删掉 T6 行并把该语义并到 T1/T2 的叙述（"越界档在构造即拒绝 ⇒ 后续求值不可达"），要么在 ASan 预设下把 T5 的求值点推到索引上界并注册一条 `qa-sanitize` 覆盖本测试的 CI 条目。
- 置信：PARTIAL（未确认 `qa-sanitize` 目标的测试清单是否含 `w34_p0_sip_order_guard`——该条决定 T6 是否完全无锁；需实跑 `cmake --build build --target qa-sanitize` 侧的清单文件，只读面见 `CMakeLists.txt:1171-1205`）

---

# 文件 3 · lib/algorithms/photometry/cpp/test/test_spectrum_integrator.cpp（242 行，整读）

**小结（对派单核查项①②）**：本文件**没有任何断言**——它是一个把 5 条合成黑体谱的 `F_syn` 打成 JSON 的**产生器**（唯一判据面是 `return 1..5` 的 I/O 错误码，数值退化一律 rc=0）。因此"退出码合同"在这里根本不存在；而登记面把它当作 SCI-PHOT-001 的 VERIFIED 证据。写死的数值（物理常数、网格 336/2/343）与合同同源且可核，问题不在数值出处，在**"证据"这个词挂在了一个不判定的件上**。

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :47 | `kPlanckH` | 6.62607015e-34 | 普朗克常数 J·s | 文献值（SI 定义值，精确无不确定度） | SI Brochure 第 9 版 §5.2 表 3（h）；`docs/science/PHOTOMETRY.md` 未另立值 | — | CONFIRMED |
| :48 | `kSpeedC` | 2.99792458e8 | 真空光速 m/s | 文献值（SI 定义值） | SI Brochure §5.2 表 2（c） | — | CONFIRMED |
| :49 | `kBoltzK` | 1.380649e-23 | 玻尔兹曼常数 J/K | 文献值（SI 定义值） | SI Brochure §5.2 表 3（k） | — | CONFIRMED |
| :57 | nm→m 换算 | 1e-9 | 单位换算 | 结构性不适用：SI 前缀定义常数，不进科学语义 | — | — | CONFIRMED |
| :60 | exp 溢出阈 | 700.0 | `exp(exponent)` 溢出保护 | **待确认**：double `exp` 溢出点约 709.78，取 700 留 ~14 余量，仓内无登记 | 检索式：`git grep -n "700.0" -- docs` 零命中 | 保守方向：下调更安全（350 以下仍远小于溢出点）；影响范围＝仅 λ≪hc/kT 的短波/低温档被截为 0，本夹具 T≥3500K、λ≥336nm ⇒ `hc/k/(λT) = 1.4388e7 nm·K /(336·3500) = 12.2` ⇒ 分支从不触发 | CONFIRMED（已复算 exponent 上界） |
| :62 | `denom <= 0.0` | 0.0 | 防 0 除 | 结构性不适用：非科学量，是除法守卫 | — | 该分支同样不可达（exponent>0 ⇒ exp>1） | CONFIRMED |
| :63 | Planck 前置系数 | 2.0 | `2hc²/λ⁵` 的 2 | 公式导出：黑体谱辐亮度按波长形式 `B_λ = 2hc²/λ⁵ /(e^{hc/λkT}−1)`（Planck 1901；辐射亮度 per unit **wavelength**，单位 W·sr⁻¹·m⁻³） | 见 CR-07-14（:52 的单位标注与此式不符） | — | CONFIRMED |
| :72 | `n_points` | 343 | 光谱采样点数 | 公式导出：`(1020−336)/2 + 1 = 343` | `docs/contracts/DATA_SEMANTICS.md:678`「spectrum_wl double `[336..1020nm step 2nm]`（XPSD 固定网格）」、`photometric_calib.h:138` | 该网格是否为 Gaia XPSD 出厂网格由 `lib/algorithms/photometry/cpp/test/gate4_dr3sp_gaiaxpy/DR3SP_SCHEMA_AUDIT.md` 一侧核；本文件不承载该主张 | CONFIRMED（式已算） |
| :73 | `wl_start` | 336.0 | 网格起点 nm | 合同值（同上） | `DATA_SEMANTICS.md:678` | 同上 | CONFIRMED |
| :74 | `wl_step` | 2.0 | 网格步长 nm | 合同值（同上） | `DATA_SEMANTICS.md:678` | 同上 | CONFIRMED |
| :91,:93 | uint8 量化上界 | 255.0 | 相对形状归一 | 结构性不适用：uint8 表示域定义（枚举基数） | `DATA_SEMANTICS.md:678`（`F(λ)=byte·flux_mul+flux_min`，byte∈[0,255]） | — | CONFIRMED |
| :111,:120 | 文件格式点数 | `%d`/循环上界 | 输入点数 | 结构性不适用（解析协议） | :25-27 的格式说明 | — | CONFIRMED |
| :148,:158,:170,:198,:214 | 进程退出码 | 1 / 2 / 3 / 4 / 5 | I/O 与前置失败码 | 结构性不适用（退出码枚举）；**但这些码不承载任何数值判据** ⇒ CR-07-12/13 | — | 本程序对"算错了"永远返回 0 | CONFIRMED |
| :180 | `temps[]` | 3500/4500/5800/7500/10000 K | 合成黑体温度档 | 结构性不适用：合成夹具自订参数，真值不进产品（`§4` 豁免） | — | 若要把该档集升格为科学覆盖声明，须在门表登记域 | CONFIRMED |
| :181 | `n_temps` | `sizeof/sizeof` = 5 | 档数 | 结构性不适用：数组维度推导 | — | — | CONFIRMED |
| :186,:187 | `dummy_spec`/`spec_count` | 由 T=3500 档取网格 | cache 与网格绑定 | 结构性不适用（实现选择） | `spectrum_integrator.h` 的 `prepare_filter_cache(..., spec_wl, n)` 合同 | — | CONFIRMED |
| :232-:236 | 序列化精度 | `%.1f` / `%.6f` / `%d` / `%.10e` | JSON 输出位数 | 结构性不适用（十进制打印位数；1e-10 相对位 ≪ 下游比对容差 1%） | 下游 `test_spectrum_integrator_golden.py` 的 `tolerance` | 下游那个 1% 容差本身在本批面外，其锚需另查 | CONFIRMED |
| :2（注释） | 声称的回归档 | "akima n=2 边界回归" | 登记面引用的能力 | **待确认**：本文件不构造 n=2（谱恒 343 点；n=2 只可能来自外部 filter 文件首行），且无任何断言 ⇒ 见 CR-07-13 | 引用面：`docs/TRACEABILITY.csv:22`、`docs/architecture/execution_inventory.csv:4`（"TEST-SPEC-001 (test_spectrum_integrator.cpp:2)"） | 保守方向：该主张目前无机器锁；影响范围＝Simpson/Akima 的小样本分支回归无门 | CONFIRMED |

## findings

### CR-07-12 · 登记为 SCI-PHOT-001「VERIFIED」证据的 TEST-SPEC-001 宿主是一个零断言的产生器，且它调用的两条通道被模块自己的头文件标为「非生产通道」
- 轴：D（主，设计点名的能力只有接口没有判定）/ C
- 位点：`lib/algorithms/photometry/cpp/test/test_spectrum_integrator.cpp:1-2` `// test_spectrum_integrator.cpp - 光谱积分 golden 比对测试程序` `// TEST-SPEC-001: 谱积分器 golden（含 akima n=2 边界回归）`；正文 `:218` `double f_syn_uncached = compute_f_syn(` 与 `:228` `double f_syn_cached = compute_f_syn_cached(cache, spec.data(), spec_count, mag_g);`
- 上位依据：`lib/algorithms/photometry/cpp/src/spectrum_integrator.h:52` `// [非生产通道] 计算单颗星相对合成流量 F_syn = ∫ S(λ)·T(λ)·Q(λ)·λ dλ × 10^(-0.4*mag_g)`、`:65` `// mag_g: … (此通道按历史口径作乘性归一; 生产口径不使用该参数)`、`:104` `// 语义与 compute_f_syn 相同 (含 10^(-0.4*mag_g)); 生产定标用 compute_f_syn_cached_xpsd。`；权威定义 `lib/algorithms/photometry/cpp/include/photometric_calib.h:126-131`「F_syn = ∫ F_λ·T·Q·λ dλ … **不含** 10^(-0.4·magG) 因子 (权威: docs/science/PHOTOMETRY.md §2a, PHOT-FSYN-CANON-001)」。
- 现状：本文件全文无 `CHECK`/`assert`/比较，只做 `std::printf` 出 JSON（:204-239）并以 `return 0` 结束（:241）；被它调用的两个函数带 `10^(-0.4·mag_g)` 历史乘子，而生产链用的是 `compute_f_syn_cached_xpsd`（`docs/architecture/execution_inventory.csv:4` 明写 `…->prepare_filter_cache+compute_f_syn_cached_xpsd->…`）。登记面 `docs/TRACEABILITY.csv:22` 以 `TEST-SPEC-001 → test_spectrum_integrator.cpp` 给 SCI-PHOT-001 记 `VERIFIED`，`docs/contracts/API_CONTRACTS.csv:284-289` 六条 pc_* 入口的 test 列同样写 `TEST-SPEC-001`。
- 差在哪：被指名作为科学门证据的件里**不存在判据**，且它执行的是与冻结口径相反的乘子通道；真正的生产通道断言在另一处（`lib/algorithms/photometry/tests/p1phot/p1phot_tests_units.cpp:361,383`，配独立 oracle `p1phot_oracle.hpp:308`「不调用被测」）却没有回写登记面。
- 后果：对外主张「SCI-PHOT-001 的 F_syn 已由 TEST-SPEC-001 验证」不成立；任何按登记面审阅的发布门（ACCEPTANCE L1/L2）会把它当作已覆盖项跳过。
- 定级：S2（证据/登记面失真，产品字节不受影响；已核生产通道另有断言 ⇒ 不升 S1）
- 怎么算修好：① `TRACEABILITY.csv:22` 与 `API_CONTRACTS.csv` 的 test 列改指真断言宿主（`p1phot_tests_units` 的 xpsd 用例 / golden py 的比对函数），或本文件内加真断言（与 `p1phot_oracle.hpp` 的独立实现逐点对拍，容差按门表登记值）；② 若保留本文件为"产生器"，把它改名/改注释为 `gen_*`，并把 TEST-SPEC-001 的判据宿主写清；③ 同批确认 `compute_f_syn`/`compute_f_syn_cached` 的**非生产**身份在合同面有登记（现在只有头注释），否则 §4 的"已由合同显式声明退役"豁免不成立。
- 置信：CONFIRMED（三处头文件原文 + 两处断言宿主 + 两处登记面逐条读毕）

### CR-07-13 · 「akima n=2 边界回归」的唯一实际驱动者是一个路径已失效、且失败被吞掉的脚本；Linux 侧该测试连构建路径都没有，机器门零采集
- 轴：R（门判不到）/ D
- 位点：`test_spectrum_integrator.cpp:2`（同一声称）与 `:17-23` 的编译/运行配方（只给 `g++ … -o test/test_spectrum_integrator.exe`，Windows 式带 `.exe`）
- 上位依据：宪章 §9「负例注入（能红能绿）」「门禁/判据本身不合理时改进门禁本身」；`docs/TRACEABILITY.csv:22` 记 `VERIFIED`。
- 现状（逐跳）：
  1. 全仓 `*.txt`/`*.cmake` 对 `test_spectrum_integrator` **零引用** ⇒ 不在 CMake 构建图（检索式：`git grep -n "test_spectrum_integrator" -- "*.txt" "*.cmake"` → 0 命中；对比本批文件 1/2 已被 `eng/tests/unit/CMakeLists.txt:1620-1634` 接入）；
  2. `eng/ci/checks.json` 对 `spectrum_integrator` **零命中** ⇒ 无任何阻断采集面（检索式：`grep -n "spectrum_integrator" eng/ci/checks.json` → 0；`grep -n "discover\|pytest" eng/ci/checks.json \| grep -i "lib/"` → 0，即 `lib/**` 的 Python 测试也不在采集面）；
  3. 唯一的 Linux 驱动是 `eng/tools/quality/v19r3_sanitizer.sh:143-148`，其中 `printf '2\n3500 1.0\n9000 1.0\n' > filter.dat` 才是"n=2"档的真身（2 点滤光片 ⇒ 走 Akima 的 `n<2`/小样本分支），但该脚本第 144-146 行用的源路径是 **改名前的 `photometric_calib/cpp/…`**（真实宿主 `lib/algorithms/photometry/cpp/…`，`git ls-files "lib/algorithms/gaia_client/*"` 一类旧名零命中），并以 `|| { echo "…BUILD_FAIL…" >> csv; }` 吞掉编译失败后继续 `run_mod photometric_calib photo_san …`；该脚本自身不在 checks.json；
  4. 真判据在 `test_spectrum_integrator_golden.py`：`_EXE_PATH = cpp/test/test_spectrum_integrator.exe`（:57），exe 缺失即 `raise FileNotFoundError`（:197-201，fail-closed 方向正确），但其唯一给出的编译指引是 Windows 的 `build_test_spectrum_integrator.ps1`（:200），而 `_EXE_PATH` 带 `.exe` 后缀 ⇒ 在 Linux 生产/开发节点上恒不满足，比对恒抛错且无采集面把它算成红。
- 差在哪：**不等式方向已定**——本条各跳都只会让"该回归未跑"这件事**不出红**（构建图外、门采集外、脚本失败被 `||` 吞），故属误绿侧而非误红侧。
- 后果：`spectrum_integrator` 的 Akima/Simpson 小样本边界（n=2、n 为奇数末 3 用 3/8、n==1 退梯形，见 `spectrum_integrator.cpp:2` 注释）在机器面上无锁；对外"golden 比对无回归"的陈述对本仓当前平台组合（Linux amd64 生产）不成立。
- 定级：S2（证据链断点，不产错值；生产入口 `pc_calibrate_simple_with_gaia_v2 → compute_f_syn_cached_xpsd` 不受本文件返回值影响）
- 怎么算修好：把本文件（或其 C++ 判据化版本）接进 `eng/tests/unit/CMakeLists.txt` 的 W34 块并 `add_test`，n=2 档改为**仓内夹具**（自带 2 点 filter/QE，不再依赖 shell heredoc）；比对侧从 `.exe` 后缀改为按平台取名，并把该 ctest 名登记进 `eng/ci/checks.json`；同时订正 `v19r3_sanitizer.sh` 的 `photometric_calib/` 旧路径（属 `eng/**` 改动，须由前台做，本审计不动）。
- 置信：CONFIRMED（四个事实点各自用独立命令核过；仅 `build.ps1` 内容未逐行读，属旁证）

### CR-07-14 · `planck_radiance` 的单位标注与其实现的公式不符（按波长形式被标成按频率单位），并被 Python 双胞胎以"完全一致"名义复制
- 轴：C（主）/ S
- 位点：`test_spectrum_integrator.cpp:52` `// Planck 黑体辐射 B(λ, T) (W·sr^{-1}·m^{-2}·Hz^{-1}, 但用作相对形状)`，实现 `:63` `return (2.0 * kPlanckH * kSpeedC * kSpeedC) / (wl5 * denom);`
- 上位依据：Planck 定律按波长形式 `B_λ(T) = 2hc²/λ⁵ /(e^{hc/λkT}−1)`，单位 `W·sr⁻¹·m⁻²·m⁻¹ = W·sr⁻¹·m⁻³`；按频率形式才是 `2hν³/c² /(e^{hν/kT}−1)`，单位 `W·sr⁻¹·m⁻²·Hz⁻¹`（Planck 1901；二者互为 `B_ν = B_λ·λ²/c`）。仓内单位口径面：`docs/science/PHOTOMETRY.md` 对 F_λ 的单位是 `W·m⁻²·nm⁻¹`（`photometric_calib.h:127`），无任何一处支持 `Hz⁻¹` 标注（检索式：`git grep -n "sr^{-1}\|W·sr" -- docs`）。
- 现状：注释单位 = 频率形式，代码 = 波长形式（`wl5`、`hc/(λkT)`）。:52 末尾自注"但用作相对形状"，且 `:91` 按最大值归一并量化到 uint8 ⇒ 单位错误在本夹具内被归一化吸收，不改变输出。
- 差在哪：注释给出的量纲与式子不符，是把两种 Planck 写法混标的标签错误；同一常数块与 `make_blackbody_spectrum` 被 `test_spectrum_integrator_golden.py:73`「物理常数 (SI, 与 C++ test_spectrum_integrator.cpp 完全一致)」、`:96`「与 C++ … 完全一致」的名义复制过去 ⇒ 错标注随"一致性"主张扩散。
- 后果：后来者按 `Hz⁻¹` 理解该函数会推出错一量纲的绝对流量（若有人去掉归一化直接乘 `flux_mul` 使用）；对外"两语言实现逐位同源"的主张里含一条错单位说明。
- 定级：S3（当前产物不受影响：输出被 `b_max` 归一，见 :85,:91）
- 怎么算修好：:52 改为 `W·sr⁻¹·m⁻²·m⁻¹（按波长形式 B_λ；本夹具只取相对形状后归一到 [0,255]）`，并同步改 `test_spectrum_integrator_golden.py` 的对应注释；若负责人要求"常数须有推导"，把 h/c/k 三个值的出处（SI 定义值）一并写进 `docs/science/PHOTOMETRY.md` 的夹具小节。
- 置信：CONFIRMED（式与单位逐项比对）

---

# 文件 4 · lib/algorithms/photometry/cpp/src/wcs_transform.cpp（261 行，整读）

**小结**：守卫（:46）与其注释、头文件、调度器侧（`module_adapters.cpp:4353`）四处对 [0,5] 一致，是本批最扎实的一块；问题集中在**第二条静默回退**（奇异 CD 置零 :54-57）与**反演近似的三侧口径**（合同"一次"、代码 3 次、门表 τ=1e-9）。

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :13 | `M_PI` 回退定义 | 3.14159265358979323846 | 无 `M_PI` 平台的 π | 结构性不适用：数学常数，double 可表示的最优近似 | — | — | CONFIRMED |
| :18,:19 | `D2R` / `R2D` | π/180 、180/π | 角度↔弧度换算 | 公式导出：弧度定义（180° = π rad） | `docs/science/ASTROMETRY.md:169`（CD deg/px、SIP 1/px 单位口径） | — | CONFIRMED |
| :37 | `m_has_sip(sip_order > 0)` | 0 | 是否施加 SIP 的开关 | 合同值：`wcs_transform.h:17` `0=无SIP, 1-5=SIP阶数` | — | — | CONFIRMED |
| :46 | 上界/下界 | 5 / 0 | 守卫域 [0,5] | 公式导出：行主存 `i*6+j`，i≤5 才可落进 36 项缓冲（5·6+0=30≤35） | `wcs_transform.h:16-18`；`f1cb487c`（提交在库，message 含"SIP order [0,5] 硬校验"）；`module_adapters.cpp:4353` | 与 `docs/contracts/DATA_SEMANTICS.md:679` 的 `sip_order int32(≤2)` 不同源 ⇒ CR-07-03 | CONFIRMED |
| :53 | 行列式 | `m_cd[0]*m_cd[3]-m_cd[1]*m_cd[2]` | 2×2 CD 行列式 | 公式导出：det[[a,b],[c,d]] = ad−bc | — | — | CONFIRMED |
| :54 | 奇异阈值 | 1e-15 | 判 CD 奇异 | **待确认**：无锚；量纲 deg²/px² ⇒ 等效像素标度 `√1e-15 = 3.16e-8 deg/px = 0.114 mas/px`（比任何真实仪器细 4 个量级以上） | 检索式：`git grep -n "1e-15" -- docs/science docs/algorithms docs/contracts` 无对应条文 | 保守方向：应改为**相对**判据（如 `|det| < (s_min)²`）或直接硬失败；影响范围＝只影响病态输入分支 | CONFIRMED（换算已复算） |
| :56-:57 | 奇异时的回退值 | 0.0（逆矩阵四项全零） | 奇异 CD 时的"逆" | **待确认**⇒ CR-07-06：与 `docs/science/ASTROMETRY.md:158`「`trans` 线性奇异 (`det→0`) → 拒 SIP 推导，返回参数错误」不符 | 同上 | 保守方向：硬失败（抛/返回码）；影响范围＝所有 `skyToPixel` 消费者 | CONFIRMED |
| :59-:63 | 2×2 逆 | `[d −b; −c a]/det` | 逆矩阵 | 公式导出：伴随矩阵/行列式 | — | — | CONFIRMED |
| :67 | 拷贝循环上界 | 36 | 四张系数表逐项拷贝 | 结构性不适用（缓冲维度）**但**与 order 无关 ⇒ 见 CR-07-18 | `wcs_transform.h:44-47` | 保守方向：仅在 `m_has_sip` 为真时按 `6·(order+1)` 上界拷贝 | CONFIRMED |
| :94 | `order <= 0` 早退 | 0 | 空多项式 | 结构性不适用（守卫） | — | — | CONFIRMED |
| :96-:98 | 双重循环界 / 步长 | `i<=order`、`j<=order-i`、`i*6+j` | 下三角求值 | 公式导出：SIP 下三角 `i+j ≤ order`（Shupe et al. 2005 §A / FITS Paper IV §2.1，`ASTROMETRY.md:92` 已引） | `docs/science/ASTROMETRY.md:72-73,92` | — | CONFIRMED |
| :115 | 切点短路阈 | 1e-12 | ρ 过小时直接返回 CRVAL（避免 /ρ） | **待确认**：无锚；量纲 deg，1e-12 deg = 3.6e-9 arcsec | 检索式：`git grep -n "1e-12" -- docs/science/ASTROMETRY.md` 零命中 | 保守方向：该值只放大数值稳定性，不改变科学语义；影响范围＝切点邻域 (<1e-12°) | CONFIRMED |
| :129-:131 | `sin_dec` 钳位 | ±1.0 | asin 定义域钳位 | 结构性不适用：`asin` 定义域边界，非科学量 | — | — | CONFIRMED |
| :140-:141 | RA 归一化循环 | `0.0` / `2.0*M_PI` | 把 RA 折回 [0,2π) | 公式导出：RA 周期 2π ⇒ **但实现选择是 while 逐圈累加**（步长 2π，迭代数 ∝ ra_rad）⇒ CR-07-15 | — | 保守方向：`std::fmod` 一次归一；影响范围＝`pixelToSky` 全部调用 | CONFIRMED |
| :168 | 背面点阈 | `fabs(cosc) < 1e-12` | cos(角距)≈0 ⇒ 投影发散 | **待确认**：无锚；等效"距切点 90°∓(1e-12 rad≈2e-7″)" | 检索式：`git grep -n "cosc" -- docs` 零命中 | 保守方向：阈值放大到 1e-9 也仍远小于真实视场；影响范围＝仅半球外点 | CONFIRMED |
| :170-:171 | 发散哨兵值 | 1e6 / 1e6 | 背面点的 (ξ,η) 替身 | **待确认**⇒ CR-07-16：无量纲说明、无状态位、经 CD 逆放大后成为 ~1e10 px 的"合法"坐标 | 检索式：`git grep -n "1e6" -- docs/science docs/contracts` 零命中 | 保守方向：应返回 NaN 或置错误标志；影响范围＝`skyToPixel` 生产路径（`star_matcher.cpp:211`） | CONFIRMED |
| :237 | 反演迭代上界 | 3 | 无 AP/BP 时的定点迭代次数 | **待确认**⇒ CR-07-17：`DATA_SEMANTICS.md:679` 写"牛顿迭代**一次**"，代码是 ≤3 次定点迭代，门表 τ=1e-9 px 属另一模块 | `docs/contracts/DATA_SEMANTICS.md:679`；`docs/algorithms/GATES_AND_TOLERANCES.md:69` | 保守方向：迭代不足 ⇒ 反演残差偏大 ⇒ Gaia 位置偏 ⇒ 匹配偏少（偏红方向），但无残差上报 ⇒ 不可见 | CONFIRMED |
| :244 | 收敛容差 | 1e-10 | 两次迭代位移差判收敛（px） | **待确认**⇒ CR-07-17：门表对迭代路径给 τ=1e-9 px（"τ 写进合同"），本模块自取 1e-10 且未登记 | `docs/algorithms/GATES_AND_TOLERANCES.md:69` | 保守方向：本值更严（1e-10 < 1e-9）⇒ 数值上不致错，但**两侧不同源**本身即失真 | CONFIRMED |
| :257-:258 | 1-based 桥接 | `- 1.0` / `+ (crpix-1)` | CRPIX 1-based ↔ 像素 0-based | 公式导出：`ASTROMETRY.md:169`「CRPIX 1-based 冻结公式（§7）；`xp=x+1`」 | 同上 | — | CONFIRMED |

## findings

### CR-07-03 · SIP 阶数上界四侧不同源：本模块合同写 `≤2`，实现/调度器/drizzle 取 `[0,5]`，drizzle 头注释另写 `0..4`
- 轴：C（主）/ S
- 位点：`lib/algorithms/photometry/cpp/src/wcs_transform.cpp:46` `if (sip_order < 0 || sip_order > 5) {`；对应条文 `docs/contracts/DATA_SEMANTICS.md:679` `| WCS/SIP | crval2/crpix2/CD4 元 double；sip_order int32(**≤2**)；a/b/ap/bp double \`[36]\`（i*6+j） | deg/px | … |`
- 上位依据：§14 表头 `docs/contracts/DATA_SEMANTICS.md:657-658`「本节是该模块 单位/dtype/shape/invalid 的**唯一权威**」；实现侧依据链只有 `wcs_transform.h:19`「对齐 f1cb487c 已入库口径 (healpix_drizzle wcs_sip/hp_drizzle_api)」——即**以另一模块的提交为先例**，非条文；`module_adapters.cpp:4353-4357` 的拒绝语也自陈"out of **drizzle** contract [0,5]"。
- 现状：同一个 `sip_order` 现有四个口径：`≤2`（本模块合同 §14.1）、`[0,5]`（`wcs_transform.cpp:46`、`module_adapters.cpp:4353`、`hp_drizzle_api.cpp:103`、`DRIZZLE_GEOMETRY.md:293`）、`0..4`（`hp_drizzle_api.h:150`，已由 `STANDARDS_REGISTRY.md:179` 登为 DISP-DRZ-001）。**`≤2` 一项在偏差登记面零命中**（检索式：`git grep -n "≤2\|<=2" -- docs`；`git grep -n "sip_order" -- docs`）。
- 差在哪：合同自称唯一权威却与全部实现互斥；`[0,5]` 的取值本身可由缓冲维度公式导出（见 :46 表行），但**没有**任何科学/设计条文为 photometry 模块批准过它；两数之间（order=3/4/5）的合同外输入现在会被合同判非法、被实现判合法。
- 后果：① 按 §14.1 写的调用方/校验器会拒绝合法的 3–5 阶（偏红）；② 按实现写的会向合同报"违规但通过"（判据面失真）；③ 对外"§14 为该模块唯一权威"的句子不成立。
- 定级：S2（触发路径在调用方/门侧，不改产品字节；生产链 `module_adapters.cpp:5695` 与实现同域，故不产错值）
- 怎么算修好：由负责人裁定权威值（缓冲只支持到 5 阶，`≤2` 更像旧文残留），然后**同批**改：§14.1 该格改为 `[0,5]` 并补一句"上界由 6×6 行主存导出"；`hp_drizzle_api.h:150` 的 `0..4` 一并（已挂 DISP-DRZ-001）；若要保留 `≤2` 为产品口径，则 `wcs_transform.cpp:46`、`module_adapters.cpp:4353`、`hp_drizzle_api.cpp:103` 三处一起收，并同步本批文件 2 的档位（T1 用 6 ⇒ 若收域，6 仍是外档，但"边界外第一档"要重算）。
- 置信：CONFIRMED

### CR-07-06 · 奇异 CD 矩阵走"打一行警告 + 把逆矩阵置零"的静默回退，与科学合同 §8 要求的"拒…返回参数错误"相反；后果是把几何故障伪装成"星不够"
- 轴：R（主，静默回退）/ D
- 位点：`wcs_transform.cpp:54-57` `if (std::fabs(m_cdDet) < 1e-15) { std::fprintf(stderr, "[wcs_transform] 警告: CD矩阵行列式接近0 (det=%.3e)\n", m_cdDet); m_cdInv[0] = 0.0; m_cdInv[1] = 0.0; m_cdInv[2] = 0.0; m_cdInv[3] = 0.0; }`
- 上位依据：`docs/science/ASTROMETRY.md:158`（§8 极端/退化条件表）「`trans` 线性奇异 (`det→0`) | **拒 SIP 推导，返回参数错误** | `ipv_wcs.cpp:322 inv_det`」；同一文件 :44-50 的守卫风格自陈"越界一律硬失败抛 std::invalid_argument, **禁止静默截断**"。
- 现状：构造函数对 order 越界选硬失败，对奇异 CD 选**继续构造**（`m_cdInv` 全 0），仅 `fprintf` 警告；随后 `skyToPixel`（:218-219）对任意天球点返回同一像素 `(crpix1-1, crpix2-1)`，无异常、无错误码。生产链逐跳：`module_adapters.cpp:5661` `p1_wcs_astrometry_usable` 只判 `!(|det| > 0.0)`（`module_adapters.cpp:4408-4409`，即 det 严格非 0 即放行，**不查 1e-15**）→ `:5693-5694` 原样下发 `cd11..cd22` → `frame_photometry_fit.cpp:232` → `pc_api.cpp:123` 构造 → `star_matcher.cpp:211` 全部星塌到一点 → 至多 1 对唯一互配（`star_matcher.cpp:300+` 唯一配对）→ `frame_photometry_fit.cpp:332` `n_matched < kMinFitStars`（`frame_photometry_fit.h:89` = 3）→ `degraded_reason="no_data_n_matched_1"`。
- 差在哪：合同要求的"参数错误"被降级为运行期警告；最终虽被外层星数门抓住（**方向：不会产出错值，是偏红侧**），但登记的是"数据不足"而不是"天测矩阵奇异"，`PHOT_WCS_UNUSABLE`/`PHOT_WCS_SIP_INVALID` 都不出现（`docs/plugins/algorithms_phase1/06_photometry.md:94-96` 的码面里也没有对应码）。
- 后果：① 诊断错置——真实原因（WCS 病态）在 `p1_phot.json.frames[]` 里表现为"星不够"，排查方向被引到测光/检出侧；② 任何不走 `frame_photometry_fit` 星数门的 `WcsTransform` 直接消费者（新入口、导出通道）拿到的是有限且看似合理的坐标，无越界、无异常。
- 定级：S2（生产可达输入 `0<|det|<1e-15` 极病态；外层星数门兜住 ⇒ 不产错值，故不升 S1）
- 怎么算修好：与 SIP 守卫同策略——`throw std::invalid_argument`（由 pc_api 屏障转 -4）或给 `WcsTransform` 一个显式 `ok()` 状态位，并在 `docs/plugins/algorithms_phase1/06_photometry.md:94` 的帧级码面登记对应错误码；`p1_wcs_astrometry_usable` 的判据同批改（从 `|det| > 0` 到一个**有相对量纲锚**的下界，例如 `s_min`），否则外层与内层阈值不同源。**同批**须为它加一条负例（构造 det=1e-20 的 CD ⇒ 断言抛/返回码非 0），现在这条分支零断言（检索式：`git grep -n "det" -- lib/algorithms/photometry/cpp/test lib/algorithms/photometry/tests` 无该分支用例）。
- 置信：CONFIRMED（外层判据与内层判据两条原文都读了）

### CR-07-15 · RA 归一化用逐圈 `while` 累加，病态 CRVAL 会使该函数迭代次数与输入成正比（应一次 `fmod` 归一）
- 轴：R（主）/ D
- 位点：`wcs_transform.cpp:140-141`
  `while (ra_rad < 0.0)         ra_rad += 2.0 * M_PI;`
  `while (ra_rad >= 2.0 * M_PI) ra_rad -= 2.0 * M_PI;`
- 上位依据：`wcs_transform.h:12` `// 天球坐标: 度 (degrees)`；`docs/science/ASTROMETRY.md:169`「WCS frame/pixel convention：ICRS/J2000 … CD deg/px」——RA 的定义域是 `[0,360)`，条文面对"任意有限 CRVAL1 都要在 O(1) 时间内归一"零提及（检索式：`git grep -n "normalize\|归一化" -- docs/science/ASTROMETRY.md`）。
- 现状：入参只经 `p1_wcs_astrometry_usable`（`module_adapters.cpp:4396-4399`）的 `isfinite` 检查，无值域检查（`crval1 = 1e300` 合法通过）⇒ `ra_rad ≈ 1.7e298`，第二支 `while` 以 2π 步长递减，实际不可结束（等价单帧挂死）。
- 差在哪：数值定义域（RA 是角度）与实现选择（逐圈加减）不匹配；这不是"单位换算定义常数"类结构性豁免——归一化次数随外部输入无界增长，属可被输入驱动的耗时。
- 后果：**本模块内不可达**（`pc::WcsTransform::pixelToSky` 在本仓零调用者，检索式：`git grep -n "pixelToSky" -- lib/algorithms/photometry` 只命中声明与测试；生产只用 `skyToPixel`，见 `star_matcher.cpp:211`）⇒ 若将来把 `WcsTransform` 接给前向投影/footprint 用途即成立；当前只使对外"健壮性/外部输入面加固（对齐 f1cb487c）"的陈述在本文件上留了一处未收口的输入面。
- 定级：S3（缺运行时触发路径：本模块无 `pixelToSky` 生产者）
- 怎么算修好：改 `ra_rad = std::fmod(ra_rad, 2.0*M_PI); if (ra_rad < 0.0) ra_rad += 2.0*M_PI;`；并在 `p1_wcs_astrometry_usable` 补 `crval1∈[0,360)`、`crval2∈[-90,90]` 的域检查（同批须同步 `DATA_SEMANTICS.md:679` 的 `crval2/crpix2` 格，把域写成合同值），否则外层放行/内层判红不同源；补一条 `crval1=1e300` 的负例（断言 O(1) 返回且 RA 在域内）。
- 置信：CONFIRMED（两支 while 的步长与终止条件逐行读毕；调用者集合用两条独立 grep 定为零）

### CR-07-16 · 投影背面点用 `xi=eta=1e6` 哨兵继续算，经 CD 逆放大成 ~1e10 px 的"看似合法"坐标，且无状态位可供调用方分辨
- 轴：R（主）/ S
- 位点：`wcs_transform.cpp:168-172` `if (std::fabs(cosc) < 1e-12) { // 投影发散 (点在投影背面)  xi = 1e6; eta = 1e6; return; }`
- 上位依据：`docs/science/ASTROMETRY.md:10` 非目标条只豁免"超出 SIP 阶数的畸变残差/大气折射/广视场拼接"，未授权用有限哨兵值冒充投影结果；§8 表（:155-160）为其它退化条件都规定了**具名**返回（`IPV_NO_SOLUTION`/参数错误），本条件无。
- 现状：该路径生产可达（`star_matcher.cpp:211 wcs.skyToPixel(gaia_ra[i], gaia_dec[i], …)`）：非有限坐标经 :218-219 变 `dx≈±1e10 / 1e-4` 量级；`star_matcher.cpp:212-220` 的 `n_in_frame` 因"在框外"不计它，但**仍进 KD-tree**（`gaia_px/gaia_py` 未过滤）。
- 差在哪：把"未定义"编码成"一个很大的有限值"，调用方无法区分（`std::isfinite` 检查通过）；唯一配对靠距离阈 2.0 px 排除，属运气而非合同。
- 后果：目前不产错值（生产查询是视场内锥，背面点极少）；一旦有人复用该函数做视场反查或 footprint 投影，背面星会以 1e10 px 参与几何 ⇒ 面积/包围盒被单个哨兵点劫持。
- 定级：S3（写不出产出错值的生产路径）
- 怎么算修好：改为 `xi = NaN; eta = NaN;` 并在 `skyToPixel` 出口保留 NaN（NaN 与任何比较为假 ⇒ `n_in_frame` 自动排除），或加 `bool ok` 出参；两种做法都须同批在 `matchWithKdTree` 里补 `std::isfinite` 跳过并计入 `out_diag` 的具名字段，否则 KD-tree 里留 NaN 会让 nanoflann 的分裂判据退化。
- 置信：CONFIRMED

### CR-07-17 · 无 AP/BP 的逆映射：合同说"牛顿迭代一次"、代码做"≤3 次定点迭代 + 1e-10 收敛阈"、门表 τ=1e-9 px，三侧不同源且残差不上报
- 轴：C（主）/ R
- 位点：`wcs_transform.cpp:237` `for (int iter = 0; iter < 3; ++iter) {`；`:244` `if (std::fabs(u_new - u) < 1e-10 && std::fabs(v_new - v) < 1e-10) {`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:679` 同行右格「无 AP/BP **前向 SIP 牛顿迭代一次**（wcs_transform.cpp:219）」；代码自述 `:229-235`「无逆向SIP, 用一次迭代近似 … 对于无AP/BP的情况, 用牛顿迭代一次」；`docs/science/ASTROMETRY.md:145-148`「逆映射以**迭代反演**为准 … 迭代反演路径另设紧门 `G-P1-WCS-RT-ITER = κ_iter·τ = 1e-8 px`（τ = 1e-9 px 收敛容差，**写进合同**）」；`docs/algorithms/GATES_AND_TOLERANCES.md:69` 适用域列明写 `lib/algorithms/platesolve/cpp/ipv/src/ipv_wcs.cpp`。
- 现状：实际是**定点**迭代 `u_{k+1} = dx − A(u_k,v_k)`（:242-243），不是牛顿法（无雅可比），上界 3 次，收敛阈 1e-10 px，且不收敛时**静默返回最后一个近似值**（无计数、无残差、无诊断位）；合同引用的行号 `wcs_transform.cpp:219` 实际落在 `:218-219` 的 CD 逆矩阵乘法，不是迭代块。
- 差在哪：三侧对"同一次反演"说的是三种东西（一次牛顿 / ≤3 次定点 / τ=1e-9 的迭代路径门），且行锚漂移；`ASTROMETRY.md:148` 明令"三条路径地板相差 5–9 个量级，同一个数不能同时服务它们"——本模块既没登记自己属哪条路径，也没登记自取的 1e-10 与 3。
- 后果：① 逆映射精度对外无承诺（真实帧 dx 可达数千 px，3 次定点迭代在强畸变下是否收敛到 1e-8 px 无任何证据）；② 按合同"一次"审阅的人会误读代码；③ 测光侧的星位置错位直接进入 `matchWithKdTree` 的 2.0 px 匹配半径（`pc_api.cpp:137`），错位 ≥2 px 即改匹配结果 ⇒ scale/零点用的星集发生变化。
- 定级：S2（错值需畸变足够大才显形，本批证据面不足以断言真实帧是否越界，故不给 S1；缺"真实系数下反演残差 > 2 px"的实测）
- 怎么算修好：① 在 `GATES_AND_TOLERANCES.md` §3 为本模块逆映射登记适用行（含上界次数、收敛阈、残差统计量与测法），或明写"photometry 逆映射由 G-P1-WCS-RT 覆盖"；② 代码侧把收敛与否带出（`out_diag` 加最大残差/迭代数，或 `hasSip()` 旁挂 `sipInverseResidual()`）；③ 合同行改写为与实现一致的"定点迭代，上界 N，收敛阈 τ"，并把 `wcs_transform.cpp:219` 的锚改指 `:236-251`。
- 置信：PARTIAL（缺"真实 SIP 系数下 3 次迭代残差是否 ≤ 1e-8 px"的实测；需跑一次带实测系数的往返，见收口清单）

### CR-07-18 · 系数拷贝与 `sip_order` 无关地读满 36 项：`order=0` 且传入非空短数组时仍越界读
- 轴：R（主）/ C
- 位点：`wcs_transform.cpp:67-72` `for (int i = 0; i < 36; ++i) { m_sip_a_buf[i] = (sip_a != nullptr) ? sip_a[i] : 0.0; … }`
- 上位依据：`wcs_transform.h:16` `// sip_a/b/ap/bp: SIP系数数组(长度36, 按i*6+j索引), 可为nullptr`；`docs/contracts/DATA_SEMANTICS.md:679` `a/b/ap/bp double [36]（i*6+j）`。两处都**没有**写"order=0 时非空数组也必须给满 36 项"，也没写"传 nullptr 是唯一合法的短/缺省方式"。
- 现状：拷贝发生在守卫之后、`m_has_sip` 判定之外——即使 `sip_order==0`（系数根本不会被使用），只要指针非空就读满 36 项；SIP 标准紧凑存储（`(order+1)(order+2)/2` 项，astropy 约定）下 `order=1` 的调用方只分配 3 项也完全合理。
- 差在哪：入参合同按"长度 36"写，但**长度要求与实际是否使用无关**，越界读的触发面比必要面宽；C ABI 的外部调用方（本批文件 1 注释点名的 "Python ctypes/未来 GUI"）最容易在这种情形下越界。
- 后果：ASan 可检的越界读；当前生产链安全（`module_adapters.cpp:4359-4369 rd_arr` 严格要求 `v.size()==36`，且 `sip.present==false` 时 `P1SipCoeffs` 的数组是全零成员），风险在外部入口。
- 定级：S3（生产触发路径不成立：编排层恒给 36 项或 order=0+成员数组）
- 怎么算修好：① 头文件/§14.1 把"必须 36 项或 nullptr（与 order 无关）"写成显式合同；或 ② 代码侧只拷必要项（`n_copy = m_has_sip ? 6*(order+1) : 0`）并在 `order==0` 时完全不解引用；两者都须配一条 `order=0 + 短数组` 的正例（ASan 下跑），否则改哪边都无锁。
- 置信：CONFIRMED

---

# 文件 5 · lib/algorithms/photometry/cpp/src/wcs_transform.h（66 行，整读）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :16 | SIP 数组长度 | 36 | 入参长度合同 | 公式导出：6×6 行主存 `i*6+j` ⇒ 36 槽；与 :17 的 [0,5] 一体 | `wcs_transform.cpp:67,98`、`DATA_SEMANTICS.md:679` | 未写"与 order 无关"⇒ CR-07-18 | CONFIRMED |
| :17 | 合法域 | [0,5]（`0=无SIP, 1-5=SIP阶数`） | 入参值域合同 | 公式导出（同 :46 表行，见文件 4） | `f1cb487c`（在库）；`module_adapters.cpp:4353` | 与 `DATA_SEMANTICS.md:679` 的 `≤2` 不同源 ⇒ CR-07-03 | CONFIRMED |
| :40 | `m_cd[4]` | 4 | CD 矩阵元素数 | 结构性不适用：2×2 矩阵元素基数，不是科学量 | — | — | CONFIRMED |
| :41 | `m_cdInv[4]` | 4 | 逆矩阵元素数 | 结构性不适用（同上） | — | — | CONFIRMED |
| :44,:45,:46,:47 | `m_sip_{a,b,ap,bp}_buf[36]` | 36（×4） | 系数自持缓冲 | 结构性不适用：数组维度（值域语义在 :16/:17 已登记） | `wcs_transform.cpp:67-72` | 4×36×8 B = 1152 B/实例的无条件拷贝：非缺陷，仅记录 | CONFIRMED |
| :35 | `hasSip()` 的语义 | `m_has_sip = (sip_order > 0)` | "是否有 SIP" | **待确认**：值语义是"阶数>0"而非"畸变已施加"（`sip_a==nullptr` 时它仍返回 true 而实际系数恒零，见 `wcs_transform.cpp:73-79`） | 检索式：`git grep -n "hasSip()" -- lib` → 仅本声明，零调用者 | 保守方向：无生产影响（无调用者）；若将来被用于分流，`true` 但零系数会伪装成"已施加畸变" | CONFIRMED |
| :10-12,:29,:32 | 坐标约定 | 1-based CRPIX / 0-based 像素 / deg | 接口约定 | 公式导出：`ASTROMETRY.md:169` 冻结的 `xp = x + 1` 约定 | 同上 | — | CONFIRMED |
| :18-:20 | 异常合同 | `std::invalid_argument` + "禁止静默截断" + "由 pc_api 屏障转错误码" | 对外失败模式声明 | **待确认**⇒ CR-07-19：只声明了这一条失败模式 | `photometric_calib.h:103-105` | 保守方向：见 CR-07-19 | CONFIRMED |

## findings

### CR-07-19 · 头文件把"SIP order 越界"写成该类的唯一失败模式，实现里另有两条静默回退（奇异 CD 置零、order>0 但系数为 nullptr 时按零系数继续）都未进接口合同
- 轴：D（主，接口合同缺项）/ C
- 位点：`lib/algorithms/photometry/cpp/src/wcs_transform.h:18-20` `// 越界(<0 或 >5) 硬失败抛 std::invalid_argument, 禁止静默截断 —— // 对齐 f1cb487c 已入库口径 … // 异常由 pc_api C 边界 try/catch 屏障转错误码 (见 pc_api.cpp)。`
- 上位依据：`docs/plugins/algorithms_phase1/06_photometry.md:93-99` 要求帧级失败各自具名稳定错误码（`PHOT_WCS_UNUSABLE`/`PHOT_WCS_SIP_INVALID`/…）；`docs/science/ASTROMETRY.md:155-160` §8 表为"线性奇异 (det→0)"规定"拒…返回参数错误"。
- 现状：实现中除 :46 的硬失败外还有
  （a）`:54-57` 奇异 CD → 逆矩阵置零继续（→ CR-07-06）；
  （b）`:67-72`/`:73-79` 在 `sip_order>0` 而 `sip_a==nullptr` 时把**全零缓冲**当作系数使用（`m_sip_a = m_sip_a_buf`），即"声称有畸变但一个系数都没给"被当成"系数全为 0"合法通过，`hasSip()` 仍为 true；头文件 :16 只写"可为 nullptr"，未区分"AP/BP 可缺省（走反演近似）"与"A/B 缺省 = 无畸变"。
- 差在哪：对外接口合同声明的失败模式集合 ≠ 实现实际的失败处理集合；(b) 一侧还把"缺参数"与"参数为零"这两种语义不同的情形折叠成同一条路径，无任何返回位可分辨。
- 后果：调用方（含 6 条 PC_API 入口与未来 ctypes 面）无法从合同知道"给定 order 但没给系数"会发生什么；按 §8 表要求应为参数错误，现为静默零畸变 ⇒ 天测畸变未施加却报告成功，进入 `matchWithKdTree` 的 2.0 px 匹配半径时产生系统性错位（真实帧 A/B 项在边缘可达 px 量级）。
- 定级：S2（生产链 `module_adapters.cpp:4359-4376 rd_arr` 要求 `a/b/ap/bp` 各自"缺省即全零"且长度必须 36，`sip.present` 为真时 order>0 必带 36 项数组 ⇒ 现网不产错值；失真在接口合同面与新调用方）
- 怎么算修好：:16-20 补三条显式约束——① `sip_order>0` 时 `sip_a/sip_b` 为 nullptr 属参数错误（硬失败），② `sip_ap/sip_bp` 可为 nullptr（走近似反演，并声明精度归属门表哪一行，见 CR-07-17），③ `m_cdDet` 奇异的处理方式（与 CR-07-06 的修复同批）；同批把 `hasSip()` 的语义改写或在 `p1phot` 加一条"order=3 + nullptr ⇒ 必抛"的负例，否则合同改了无锁。
- 置信：CONFIRMED（:67-79 的指针赋值路径逐行读毕；`rd_arr` 的 36 项强制已读原文）

---

# 本批最高危三条（只挑，不复述）

1. **CR-07-12 / CR-07-13（同一条证据链的两处断裂）**：`docs/TRACEABILITY.csv:22` 与 `docs/contracts/API_CONTRACTS.csv:284-289` 以 `TEST-SPEC-001 → test_spectrum_integrator.cpp` 给 SCI-PHOT-001 记 `VERIFIED`，而该件零断言、且只执行被模块头文件自标"非生产通道"的两条 `10^(-0.4·mag_g)` 路径；它声称的 `akima n=2 边界回归` 唯一驱动脚本路径已失效且不在任何构建图/门采集面。⇒ 对外"科学门已验证"的陈述目前挂在一件不判定的产物上。
2. **CR-07-09**：SIP 守卫的正例锁（`test_p0_sip_order_guard.cpp` T5/T6）在 dx=dy=1 处求值、SIP 幅值 7e-5 px、容差 0.1 px ⇒ 三数量级余量，"系数被整体忽略/索引错置/反演不迭代"全部判绿；本模块对 SIP 索引约定的实际保护为零。
3. **CR-07-03 + CR-07-06 + CR-07-17（同族：一条式子三个说法）**：`sip_order` 值域合同 `≤2` vs 实现 `[0,5]`；奇异 CD 合同要求"返回参数错误" vs 实现静默置零；逆映射合同"牛顿迭代一次" vs 实现"≤3 次定点 + 1e-10" vs 门表"τ=1e-9 写进合同"。三者都属"合同自称唯一权威但与实现不同源"，且都无对应负例锁。

（次高：CR-07-01/02 —— 屏障测试只锁返回码不锁输出侧、T4 正例在夹具几何上必然零匹配，`scale > 0.0` 对退化默认 1.0 无判别力；CR-07-07 —— 生产可达的 `*_with_gaia_v2/_f64_v2` 的 -4 屏障在整个跟踪测试面零断言。）

# 我读不动或需要实跑才能定的

| 待定项 | 为什么读不出来 | 该跑什么 |
|---|---|---|
| `w34_p0_pc_api_exception_barrier` / `w34_p0_sip_order_guard` 现在是否真在 ctest 面上跑绿 | `eng/ci/prod_wiring_baseline.json:73-74` 仍记 `unbuildable_test`（棘轮基线只判新增，不判失效项），而 `eng/tests/unit/CMakeLists.txt:1620-1634` 已 `add_test` —— 两面矛盾，只有构建图能给答案 | `cmake -S . -B build -G Ninja …` 后 `ctest --test-dir build -R "w34_p0_(pc_api_exception_barrier\|sip_order_guard)" --output-on-failure`（套 `mem_guard.py --max-rss-gb 4`），再 `--update-baseline` 前先确认基线条目是否已消失 |
| T6「不越界」是否真无锁（CR-07-11） | 取决于 ASan 预设的测试清单是否含本测试 | 读/跑 `qa-sanitize` 目标的测试清单（`CMakeLists.txt:1171-1205`），或 `ASTROCS_ENABLE_SANITIZERS=ON` 下单跑这两个测试 |
| 真实 SIP 系数下 3 次定点迭代的残差量级（CR-07-17 的定级） | 只读代码无法给出收敛性；需要实测系数与密集域往返 | 用 `实验/` 或 `p1wcs_apbp` 的三档畸变 fixture 对 `pc::WcsTransform::skyToPixel` 跑一次往返，报 `max‖Δx‖` 与迭代数分布 |
| `xi=eta=1e6` 哨兵是否已在真实帧里进过 KD-tree（CR-07-16） | 需要运行期计数（当前 `out_diag` 无该字段） | 在 `matchWithKdTree` 临时统计 `|gaia_px|>1e6` 的星数并跑一轮 M42/E2E 帧集（套内存看门狗，20 GB 档） |
| `planck_radiance` 单位标注错误的扩散面（CR-07-14） | Python 双胞胎是否照抄同一标注需读 `test_spectrum_integrator_golden.py:73-110` 全文（不在本批清单内） | 下一批或本批补读该 py 的常数块与 `make_blackbody_spectrum` 对拍注释 |
| `photometric_calib.h:102` 的 `-3` 与 §14.1 的退化语义谁被运行期真读（CR-07-05 的收口方向） | 需确认是否有调用方按 `-3` 分流 | `git grep -n "== -3\|rc < 0\|rc != 0" -- lib/infrastructure` 之后，若无人分流则改文档面；有人分流则按变更流程改实现 |

---

## 收工基线 `git status --porcelain` 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工输出逐字相同 ⇒ 本节点零改动、零 git 写；未跑任何构建/ctest/`eng/**` 脚本，未 import 仓库 Python。
取证手段：`Read`（5 个被审文件全文）＋ `git grep` / `git ls-files` / `sed -n`（只读原文）＋ `git cat-file`/`git log`（核 `f1cb487c` 锚在库）。

## 派单文字与清单的差异（§5.5 要求指出）

- 派单把本批描述为"含**异常屏障与 API 合同类测试**"；清单 5 项中只有 `test_p0_pc_api_exception_barrier.cpp` 属"API 合同/屏障"类，另两件是 SIP 阶数守卫与谱积分产生器，且含 `wcs_transform.{cpp,h}` 两个实现件。**以清单为准**：5 项全部整读并各交一张处置表。
- 清单文件路径与仓内跟踪面逐字一致（`git ls-files -s` 五条全部命中，blob 与基线 HEAD 相符），无缺件或多件。

