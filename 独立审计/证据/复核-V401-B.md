# 复核-V401-B｜第②层独立复核（批次 V401-B）

## 文件头

- **复核对象**：`独立审计/证据/AUD-401-架构对齐.md`（全文 452 行）之第 123-136、137-149、247-259、260-272、273-285、286-298、299-313 段，条目 7 条（清单 `独立审计/批次清单/V401-B.txt`，7 行 + 1 行表头）。
- **基线**：冻结 HEAD=`c8f64e9a`。
- **只读纪律**：未修改/移动/删除仓库任何文件；git 仅用 `status`/`ls-files`/`grep`/`log`/`show`；未跑构建、`cmake`、`ctest`、`run_checks.py`、任何 `eng/**` 脚本或端到端；未 `import` 仓库内 Python；未写 `整改/`、`独立审计/批次清单/` 共享台账与 `独立审计包/`。工作脚本目录未建（本批纯 `git grep` + 读文本，无大表）。
- **开工 `git status --porcelain` 原文**：
  ```
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```
- **判读纪律声明**：每条先按条目"位置/锚"独立取证并写下结论，再与第①层判定文字比对（§2.1）。S1/P0 判定附从生产入口起的逐跳 `文件:行` 触发路径（§2.5）。

## 逐条判定

## AUD401-008 · 线程池多点私建 + 存在第二套 worker 预算对象
**判定：降级**（机制成立且比成稿更重；订正的是**计数口径**与一处归属，不是严重级）

- **我的独立取证**（先于阅读成稿判定文字）：
  - 确定命令：`git grep -n "std::vector<std::thread>" -- lib | grep -vE "/tests?/|acr/|hips_browser/|/tools/"` → **23 行**。扣除 `executor.cpp:12`（注释）、`:39`、`:217`（设计指定的 CPU 池与 I/O 池）后**生产 TU 私建池 20 处**，非成稿的 14 处。
  - 差额 6 处名册：`lib/infrastructure/benchmark/backend_host/baseline_kernels_impl.inc:72`、`benchmark/cpu/{avx2:266,avx512:265,baseline:445}`（4 处，`benchmark` 命令生产路径）、`lib/algorithms/integration/v6/oracle/weight_chain_selfcheck.cpp:479`（1 处，Oracle 面）、**`lib/infrastructure/cli/commands.cpp:1260` `hash_pool`（1 处， normalize/mosaic 生产哈希链上，成稿名册漏计）**。
  - 成稿 14 的算式可复现：5(upm)+1(sampler)+1(scheduler.cpp)+2(normalize_workflow:462,464)+1(mosaic_window)+1(export_stream)+2(module_adapters:2052,9054)+1(p3_session)=14 ⇒ 其口径实为"排除 benchmark/oracle/CLI 三族"，但条目正文未声明该排除，读者会当成全量。
  - **派单要核的关键事实①：调度器起 worker 不逐项向预算申请，而是把池规模直接建成预算本身**——`lib/infrastructure/scheduler/src/scheduler.cpp:26` `budget_(min(budget, available_cpu))` → `:29-30` `create_thread_budget(budget_)` 持唯一 `budget_obj_` → `:374-375` `pool.reserve(budget_); for i<budget_ emplace_back(worker)`，**无 acquire**。
  - **关键事实②：它自己的头文件确实声明了"共用同一预算、总和不超预算"**——`lib/include/astrocs/core/scheduler.h:30-34`「Scheduler: 唯一拥有全局执行顺序、线程预算与内存回压／禁止第二套全局调度器; 模块只投递 work, 不建私有 pool／Scheduler 创建并持有唯一 ThreadBudget；run() 注入 RunContext，模块经 ctx.acquire_lease/ctx.budget() 原子预留」；`lib/include/astrocs/core/executor.h:7-9`「worker 领取任务前经 ThreadBudget::acquire 预留 → Σ(active) ≤ budget 全局不超卖」、`:59`「唯一共享 executor：worker 数 = ThreadBudget 预算上限」；`lib/infrastructure/scheduler/src/executor_runtime.h:10`「按 ThreadBudget 实例绑定（同一预算对象 → 同一池）」。
  - ⇒ **对负责人那条裁决的事实底座**：常驻调度器 worker **在构造上就等于预算全额**（`scheduler.cpp:26,29,99-100,374`），节点内工作只拿这份预算的**份额提示**（`scheduler.cpp:253-272` `set_dispatch_budget_hint(share=budget_/concurrent)`，复位 `:342`；API 声明 `lib/include/astrocs/core/context.h:163`）——即"账本已计入常驻 worker，节点侧是二次分配而非加额"。因此模块再自建 N 线程池即为**账外并发**，`Σ(active) ≤ budget` 破口来自模块侧，不来自调度器侧。
  - 第二预算对象成立：`lib/algorithms/coverage/include/astro/phase2/execution_options.h:1-4` 头注释自称「CON-002 全局 worker 预算合同…唯一 ExecutionOptions 对象。所有嵌套模块只能从该预算借用，不得各自创建等规模线程池」，而 `:19` `default_cpu_workers() = hardware_concurrency()`、`:22-24,26-30` `effective_*` 回退、`:38-41` `default_execution_options()` 直接把 `cpu_workers` 置为 hardware_concurrency；`stage2_common.h:34` `exec = default_execution_options()` 作结构体默认值 ⇒ 未注入即自决满并发。
- **与成稿差异**：① 计数与口径（14 → 生产 TU 20，须点名 `commands.cpp:1260` 在 normalize/mosaic 链上、benchmark 4 处属 `benchmark` 命令自身产物面）；② 把 `scheduler.cpp:373` 计入"私建池违规"值得商榷——该池即预算本体的实现体（`scheduler.h:30-34` 明定 Scheduler 是唯一预算拥有者），应改列"设计内的唯一池"，与 `executor.cpp` 同档；③ 成稿未取到"头文件已自证不变量"这一加重证据（它只说"lease 是唯一权威"的注入面注释）。④ 残余缺证（超单条 8 命令上限）：`upm.cpp:620` 池规模入参是否即 `exec.cpu_workers`，下一步 `sed -n '600,625p' lib/algorithms/coverage/src/upm.cpp`。
- **影响 D8 的处置**：**进任务书**（改法"退役+接线"方向不变），但条目须按 ①②订正名册与口径；**并须把"回压账本已计入常驻 worker、节点侧为份额二次分配"这一事实单独上一条给负责人**，否则裁决"回压账本是否计入常驻 worker"会建立在不完整事实上。**另需上呈**：benchmark 4 处池是否豁免于统一预算（属 `benchmark` 命令测机阶段，先于 Runtime 存在）。


## AUD401-009 · CLI→阶段调度器：一次调用只驱动一个阶段成立（附全局静态状态偏差）
**判定：确认**（"阶段隔离成立"与"结构可达串阶段"两面均由我独立复算立住；订正一处失效锚点）

- **我的独立取证**：
  - 唯一生产调用点：`lib/infrastructure/cli/commands.cpp:819-821` `run_pipeline({phase.back() - '0'}, cfg_text, budget, …)` —— **单元素初始化列表**，`phase` 由形参 `const std::string& phase`（`:706`）传入，类型上就表达不了多阶段。
  - 该 gate 的全部调用者：`:1228` `"phase2"`、`:1534` `"phase3"`、`:2008` `"phase1"` ⇒ 三命令各一次、各一相，逐处读到。
  - 每次调用新建执行体：`runtime_client.cpp:381` `ModuleRegistry reg;`、`:418` `create_runtime(rrb)`、`:424` `build_pipeline_ir(phases, …)` ⇒ 无跨命令共享 registry/Runtime。
  - 多阶段面确实**结构可达**：`runtime_client.h:37` / `runtime_client.cpp:358` `run_pipeline(const std::vector<int>& phases, …)`；`build_pipeline_ir`（`:104` 起）`:292-314` 以 `want1/want2/want3` 顺序把多条节点链拼进**同一份 IR**；生产与测试侧无多阶段调用者——`git grep -rn "run_pipeline(" -- eng` 命中仅 `{1}` 单元素（`eng/tests/unit/rt008_runtime_client_test.cpp:66`、`rt009_node_trace_test.cpp:26,54`），CLI 命令面无 `phases` 参数（`git grep -n "phases" -- lib/infrastructure/cli/command_tree.h lib/infrastructure/cli/parser.cpp` = **0 命中**，同式在本会话其它对象上有命中，非假阴）。
  - 维持不越界的静态门：`eng/tools/check_cli_run_preset.py:15`（判据 [B] 原文"三个用户命令各自经 run_with_resource_gate(ev,"phaseN") → run_pipeline({N}) 做**单 phase** 调度，互不串接"）与 `:100-105`（`token = 'run_with_resource_gate(ev, "phase%d"' % sid` + `if "run_pipeline({" not in cmds_text`）。
  - 进程级静态：`runtime_client.cpp:338 g_manifests`、`:342 g_trace`、`:343 g_ir_json`，注释自陈 `:336`「最近一次 run_pipeline 的…」、`:340`「RT-009: 最近一次…」；读回 `:350`、`:355`、`:509`。
- **与成稿差异**：① **锚点失效**：成稿写 `runtime_client.cpp:553-556` 为写入点，但 `wc -l lib/infrastructure/cli/runtime_client.cpp` = **529 行** ⇒ 该锚在文件之外；全部写入点是 `:457/:462/:463` 三行（`git grep -n "g_manifests =\|g_trace =\|g_ir_json =" ` 仅 3 命中），都落在同一函数体内。② 静态门的性质须写清：它是**源码文本在场门**（子串匹配 `run_pipeline({`），只要三个 token 仍在，新增一条多阶段调用不会被它判红 ⇒ 成稿"仅靠调用方自律与一个静态门维持不越界"的判断**偏乐观**，门的实际保护力低于其字面。③ "测试会同进程二次 run 覆盖前次"我只核到同 TU 两次调用（`rt009_node_trace_test.cpp:26,54`），未逐 TEST 展开是否同进程——不影响判定，只影响后果强度。④ 严重级：无生产触发路径（命令面无 phases 参数），按 §2.5 此条的缺陷面**最高 S2**，成稿亦未升 S1，无冲突。
- **影响 D8 的处置**：**进任务书**（迁移：`run_pipeline` 形参收为单阶段枚举/删 `want1..3` 拼接分支、`g_*` 三组静态改出参），并把②列为**门加固项**——`check_cli_run_preset.py` 需从"子串在场"改为"断言 `{` 后恰一元素且全仓 `run_pipeline(` 调用者名册可枚举"。"阶段隔离成立"这一合规结论可放心写进 D8 正面清单。


## AUD401-016 · 错型输入静默回退默认值：67 处取值器无报错、无降级声明、无 provenance
**判定：降级**（缺陷机制成立且可举出更重的实例；但成稿的计数口径与两条证据不成立）

- **我的独立取证**：
  - **67 这个数的口径复算**：`git grep -c "p1_num(\|p1_flag(\|p1_int(" -- lib/infrastructure/scheduler/src/module_adapters.cpp` = **67**（这是**含匹配的行数**）；`git grep -o …| wc -l` = **75 次出现**（p1_num 58 / p1_int 12 / p1_flag 5）。75 次里含 3 次函数定义本身（`:1360 p1_flag`、`:1369 p1_num`、`:1374 p1_int`）⇒ 真口径应为**调用 72 次 / 64 行**，成稿把"行数"直接标成"生产调用点 67 处"，名实不符（差 3 行 8 次）。
  - 取值器实际语义（`sed -n '1350,1390p'`）：`p1_flag:1360-1368` 对**数字型静默转 bool**（`is_number_*() → !=0`）；`p1_num:1369-1373` 是**唯一**的"错型→dflt"（`:1371 !it->is_number() → return dflt`，成稿锚 `:1370` 偏一行）；`p1_int:1374-1385` **不做错型回退而是改值**——`is_boolean()→1/0`、`is_number_float()` **截断取整**（注释自陈"截断语义"）。⇒ 成稿把三个取值器一律写成"错型→dflt"不准确：真实失效面比"当作没给"**更宽**（值被悄悄换掉）。该行为在 `:1354-1355` 有登记注释（"number|bool 均合法; 错型回退默认, 不抛"），属**有意的现行约定**，故本案是"约定与合同打架"，不是漏写代码。
  - 无 provenance：`git grep -n "default_used\|config_default\|used_default\|fallback_default" -- lib` = **0 命中**（同式在本会话对 parser.cpp 有 7 命中，非工具假阴）⇒ 回退确实不留痕。
  - **成稿"校验面不覆盖类型"= 假阴，须推翻**：`git grep -n "is_number\|is_boolean\|is_string\|type()" -- lib/infrastructure/cli/parser.cpp` 命中 `:509, :520, :526, :623, :640, :653, :661` —— 全是 `is_string()` **值类型判定**。成稿用了窄模式（只搜 `is_number|is_boolean`）便下"不做值类型判定"的结论。真实表述应为：`validate_config_full`（`lib/infrastructure/cli/cli_common.h:122` 声明、`parser.cpp:267` 自陈"结构可达性唯一实现"、生产调用点 `commands.cpp:1721/1843/1964` normalize、`:1380` mosaic、`:1671` export）**只对结构键与字符串键判型，不对数值旋钮判型**。
  - **派单问的"兜底取负、判据只卡上界"空门形态——复算结论：该形态不存在，但等价空门存在**：
    - 全文件唯一负数兜底 = `module_adapters.cpp:7520 p1_int(dj,"precision_mode",-1)`；其上游 `:7513-7519` 已两次 fail-closed（缺键即拒"no silent default"、非整数即拒"boolean/float/string are rejected (no silent coercion)"），下游 `:7523-7525` 判据是**双边**（`!=0 && !=1` 才拒）⇒ `-1` 不可达，非空门。
    - 真空门形态是**兜底值本身落在合法域内**：`:7505 nested = p1_int(dj,"nested",1)` + `:7521-7522` 双边门、`:7506 pixfrac = p1_num(dj,"pixfrac",1.0)` + `:7526` 门 `pixfrac<=0||>1` 拒 ⇒ 用户写 `"pixfrac":"0.5"`（字符串）时被静默替换为 **1.0** 且**通过全部校验**，run 报告成功。pixfrac 是 drizzle 核足迹科学旋钮（0.5≠1.0 改变输出通量），此例即"判据对缺陷结构性失明"。
- **与成稿差异**：① 计数口径（67 行 ≠ 67 调用点，实为 72 次/64 行）；② "p1_int 错型→dflt"错，实为 bool/float **静默改值**（更严重，须改写主张）；③ "校验面不覆盖类型"为窄模式假阴，须收窄为"不覆盖数值键"；④ 空门形态按派单假设（负兜底＋只卡上界）不成立，改立"域内兜底使双边门失明"并给出 `:7505-7506`/`:7521-7526` 具体件；⑤ 成稿 置信"中高、须实跑注入型负例最终立证"——本阶段禁测，我的判定全部落在静态证据上，**上述 ④ 的实例即静态立证**，不必等实跑。
- **影响 D8 的处置**：**进任务书**，但条目正文须按 ①②③④ 重写（主张从"67 处回退不报错"改为"取值器族 72 个调用点：错型改值/回退两类混合、无 provenance、数值键全域无类型校验，且存在域内兜底使校验门失明的具体件"）。改法方向不变（validate 补数值键型判定 + 错型走 `Result::fail`），**并须新增一条**：对每个 `p1_num/p1_int` 调用点核"兜底值是否落在该键合法域内"，落内者一律改 fail-closed（否则类型校验加了门仍瞎）。S1 尚缺一跳证据：`pixfrac`/`nested` 从用户配置面到 `dj` 的传递路径未逐跳取（`git grep -n '"pixfrac"'` 于 `parser.cpp`/`eng/packaging/config/config_registry.json`）——**在补到该跳前本条按 S2 记账**。


## AUD401-017 · 基建职责家冲突：orchestrator 与 scheduler 两套编排、两套 loader，orchestrator 不在生产链接闭包
**判定：确认**（三问皆独立复算立住；订正两处锚点性质与一条状态轴表述）

- **我的独立取证**：
  - 生产链接闭包不含 orchestrator：`acsd` 的 `target_link_libraries` 在 `CMakeLists.txt:1008-1016`，实读项为 `astrocs_contracts core cli_runtime common cfitsio aio hips p3_projection_wcs calibration phase2 drizzle cpu phase1/2/3_session cli_subcommands ${CMAKE_DL_LIBS}` ⇒ **无 `astrocs_infra_orchestrator`**。
  - 消费面复算：`git grep -n "astrocs_infra_orchestrator"` 全仓命中 = 自身声明 `lib/infrastructure/pipeline/orchestrator/cpp/CMakeLists.txt:50,57,64,83,87`、`orchestrator_legacy_cli` 链接 `:96`、自身测试 `cpp/tests/CMakeLists.txt:19,28`、注释 `CMakeLists.txt:481`、`cpp/include/orchestrator.h:14` ⇒ 与成稿"只被 legacy CLI 与自身测试链接"一致。
  - **补充关键事实：它是 STATIC**（`cpp/CMakeLists.txt:50 add_library(astrocs_infra_orchestrator STATIC`）⇒ 不在链接闭包 = **零字节进产品**，比"发出去没人用"更彻底（对照 AUD401-018 的 SHARED 面）。
  - 冲突在册且未闭合：登记点 4 处 `CMakeLists.txt:480`、`cpp/CMakeLists.txt:17`、`:94`、`cpp/include/orchestrator.h:5,:20`、`docs/modules/orchestrator.md:73`，均写"交前台裁决/待收敛"，`orchestrator.h:20` 把"前台就 ORCH-HOME-01 裁决为「退役」"列为退出条件之一 ⇒ 基线 `c8f64e9a` 无裁决落地痕迹（我只查这 4 处登记点与其自述退出条件，未做全仓裁决记录穷搜）。
  - 两套 loader 复算：`orchestrator/cpp/src/dll_loader.cpp` 真实加载点 `:274-275`、`:302-303`、`:474` 全为 `LoadLibraryExA(..., LOAD_WITH_ALTERED_SEARCH_PATH)`（另有 `:121,:168` 注释指 `LoadLibraryA`），**无 manifest、无 sha256/ELF 校验** ⇒ 与 §8.4:659"只认清单授权的绝对路径，失败只报错、不回退搜索"相反，成立。`module_loader/secure_loader.c:11`（必须绝对路径 + realpath canonical）、`:15`（只从 manifest 给的绝对路径 `dlopen(RTLD_NOW|RTLD_LOCAL)`）、`:17`（ELF64 头校验 + sha256 与 manifest 比对）= 合规那套；`git grep -n "secure_loader\|module_loader" -- '*CMakeLists.txt'` = **0 命中** ⇒ 确实不在构建图（此点属 AUD401-014 主线，此处仅作支撑，不重复立案）。
- **与成稿差异**：① 锚点性质：成稿写"`dll_loader.cpp:22,121`（`LoadLibraryExA/LoadLibraryA` + `RTLD_NOW|RTLD_GLOBAL`）"——`:22` 是**映射注释行**、`:121` 是注释，真实代码在 `:274/:302/:474`；且本基线该文件内**无 dlopen 调用**（`git grep "RTLD_"` 仅此 1 行注释命中），故"RTLD_GLOBAL 行为"是从注释借来的，不能作为已发生的 Linux 行为立案，只能作为"该 loader 的既定语义映射"。② 状态轴须写准：orchestrator = **未接线**（在册、有测试、有 legacy 可执行目标、自declared 非发布），不是"已退役"也不是"文档没写"；`orchestrator.h:14` 已自陈"非发布目标"，故本案的缺失不是"没人发现"而是**裁决未执行**，D8 的任务应是"催裁/执行"而非"新查"。③ 成稿未记 `astrocs_infra_orchestrator` 是 STATIC，导致其"改法：整体退役"的成本被低估（退役需同时删 `orchestrator_legacy_cli` 与其测试注册段，后者成稿已记 `CMakeLists.txt:1128` 一处，我未复核该锚）。
- **影响 D8 的处置**：**进任务书**（迁移+退役方向不变），但改法要按②改成"ORCH-HOME-01 已上呈未裁 ⇒ 本条挂'待裁'，不得由实施方自行选边"；两套 loader 的部分**独立成一条进任务书**（`dll_loader` 退役 + `secure_loader` 进图），因为它与 AUD401-018 的加载面是同一个修复动作，须并入以免两处各改一次。


## AUD401-018 · 交付安装面同时发"静态进 exe 的能力"与"无人加载的模块 DSO"
**判定：确认**（三面证据我自己复算齐；成稿两条支撑证据须订正，且"无人加载"一问有比我方更好的答案）

- **我的独立取证**：
  - 安装面（`eng/cmake/install_layout.cmake`）：`:39-40` 全局属性 `ASTROCS_PLATFORM_SHARED_TARGETS = acsd_runtime acsd_io astrocs_noop astrocs_cpu_baseline`；落点逐行读：**根 `.`** = `:59`、`:68-69`、`:74-75`；**modules/** = `:80-81`（`astrocs_noop`）与 `:104-112` foreach（`astrocs_catalog_gaia`、`astrocs_p1_drizzle`、`astrocs_p1_calibration`、`astrocs_p1_cosmetic`、`astrocs_p1_hips_writer`）；**providers/** = `:86-87`（`astrocs_cpu_baseline`）。foreach 带 `if(TARGET …)` 门卫 ⇒ 5 个 target 逐个核在册：`gaia_xpsd_client/CMakeLists.txt:15`、`drizzle/CMakeLists.txt:61`、`calibration/CMakeLists.txt:30`、`cosmetic/CMakeLists.txt:50`、`drizzle/hips/CMakeLists.txt:50`（第 6 个 `astrocs_p1_noise`（`noise_snr/CMakeLists.txt:40`）**不在安装名单**，且根 `CMakeLists.txt` 无 `add_subdirectory(lib/algorithms/noise_snr)` 命中 ⇒ 门卫自动摘出，与 `:96-103` 注释的 F-CI-002-01 裁决自洽，本条不重复立案）。
  - `astrocs_noop` 来源确为一致性测试骨架：`eng/tests/conformance/noop/CMakeLists.txt:9 add_library(astrocs_noop SHARED`。
  - 链接面：`CMakeLists.txt:1008-1016` 的 `acsd` 闭包**不含**上述任何 SHARED 模块 target（逐项读，见 AUD401-017 取证）⇒ 产品运行确实走静态。
  - **"无人加载"两问的复算答案（派单要点③）**：
    - *活加载器扫的目录*：产品里有三处按硬编码候选目录/裸名加载的活加载器——`lib/infrastructure/acr/backends/cuda/cuda_bridge_loader.cpp:55-69`（exe 同目录 + `run\temp\cuda_bridge\` + `lib\acr\build\bridge\`，`:75 LoadLibraryW` 逐个试，`:79` 最后裸名回退到 OS 搜索路径）、`lib/infrastructure/acr/utilization/system_metrics.cpp:134-139`（`{"nvml.dll", "C:\\Windows\\System32\\nvml.dll"}`）、`lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp:148,167,183`（**裸 DLL 名** `astro_image_io.dll`/`star_detector.dll`/`gaia_client.dll`，零路径钉定）。**三处没有一个扫 `modules/`**。
      - **取证自纠**：我第一把尺子 `git grep -n "dlopen\|LoadLibrary" -- lib | grep -vE "/tests?/|/tools/" | head -30` 被 **`head` 截断**（该命令真实命中 **315 行**），因而漏掉第四处、也是唯一读清单的活加载器 `lib/infrastructure/benchmark/backend_host/backend_loader.cpp:152,156,163,165`（`LoadLibraryExA`/`LoadLibraryA`/`dlopen(RTLD_NOW|RTLD_LOCAL)`/`dlsym("astrocs_backend_get_api_v1")`，经 `astrocs_cpu` 链进 `acsd`）——补到它之后，"成稿称生产无 dlopen 是假阴"这一判定**更强**，且它的清单面细节以 `raw/复核-架构接线.md:294-303` 的先行取证为准（本条不重复立案，见下"与成稿差异"⑤）。这正是前言 §2.4 点名的 `head` 截断假阳来源，我自己踩了一次，记录不删。
    - *它要的清单*：上述三处活加载器**都不读任何清单**；`acsd` 闭包内唯一读清单的加载器是 `backend_loader.cpp`，其清单为 `backends.manifest.json`（安装布局根本没有 `backends/` 目录，取证见 `复核-架构接线.md:296-303`）。产品进程对 `eng/packaging/astrocs.product.json` 的读取 = **0**（`git grep -n "astrocs.product.json" -- lib` 命中仅在 CI 检查器 `lib/infrastructure/acr/ci/check_acr_dormant.py:24,149,190`，其余 `phaseN_product.json` 是无关常量）。⇒ 精确表述应为"**产品清单只被 CI 读、不被产品读；装出去的 `modules/` 没有任何加载器扫它**"，而不是"产品无加载器"。
  - 同一能力两份二进制的实证与原因：`lib/algorithms/drizzle/CMakeLists.txt:1-10` 自陈 STATIC `astrocs_drizzle` 对象非 PIC（TLS local-exec 重定位 `R_X86_64_TPOFF32`，链接 .so 失败实证）⇒ SHARED 目标从**同一批生产源**以 `POSITION_INDEPENDENT_CODE ON` 重编译，`DRZ_PROD_SOURCES` 直指 `${CMAKE_SOURCE_DIR}/lib/algorithms/drizzle/healpix_drizzle/*` ⇒ 同源码、两种重定位模型的两份产物。
  - 加载面自身在册坏证：`lib/algorithms/cosmetic/CMakeLists.txt:18`（"2 TU 裁剪闭包不成立, dlopen undefined symbol"）、`:31`（"DT_NEEDED 丢弃, DLL 内 GOMP 并行区退化为 dlopen 晚装载 → SEGV"）、`lib/algorithms/drizzle/hips/CMakeLists.txt:37` 与 `hips/memory.md:298`（`astrocs_p1_hips_writer.so: undefined symbol aio_hips_tile_ipix`）⇒ 发出去的 DSO 不止"没人加载"，还有在册的"加载即坏"。
  - 安装面自陈的运行面宣称：`install_layout.cmake:96-103`「MOD-001 起随安装树发布到 modules/ 并登记进 product.json + install-tree.contract.json…机器校验 `verify_install_tree.py` + `eng/tests/abi/mod001_install_load_check.py` **经安全 loader 逐 unit 加载验证**，§18.4 只加载签名清单官方模块」，而 `secure_loader.c` 在全仓任何 `CMakeLists.txt` **0 命中**（见 AUD401-017 取证）⇒ 该注释把"测试现场编译的 loader"说成了产品运行面。
- **与成稿差异**：① **成稿的负证据是作用域假阴**：其命令 `git grep … -- lib/infrastructure/scheduler lib/infrastructure/cli lib/infrastructure/aio/src` 只圈 3 个目录，结论却写成"生产代码无任何 dlopen/LoadLibrary"——全仓复算有 3 处活加载器（acr bridge / NVML / platesolve ipv）。结论"这 5 个 DSO 不会被产品载入"仍成立，但**理由必须换成"活加载器不扫 modules/、不读清单"**，否则实施方会照"产品零加载器"去设计修复（选 (a) 时会误判工作量）。② 归属订正：成稿写"`:40-41,83-89` … 安装进根/`providers/`（含 `astrocs_noop`）"——实读 `astrocs_noop` 落 **modules/**（`:80-81`），providers/ 只有 `astrocs_cpu_baseline`（`:86-87`），落根的是 `acsd` 与 `acsd_runtime`/`acsd_io`。③ 我未开 `eng/tests/abi/mod001_install_load_check.py`（单条 8 命令上限），故"唯一加载验证是测试、且现场 `cc` 编 loader"这一句是成稿主张、我方未证，成稿须自留或我批注 `待证`；同条 §2.7 四问里"跑的是产品还是自家夹具"因此未答。④ 我方新增两条成稿没有的加重证据：加载即坏（cosmetic/hips 在册）与"清单只被 CI 读"。⑤ **与成稿批次外的并行判定对齐**："`modules/` 装出去无人加载"这一结论已由 `raw/复核-架构接线.md`（AUD401-014 段，`:280-303`）先行独立判到，且其定性更准——"承诺能力有另一份实现在跑（`backend_loader.cpp` + `backends.manifest.json`），装出去的模块 DSO 属无人加载的死重量"；该段并已记 `modules/` 实收 **6** 个文件（含 `astrocs_noop`，`install_layout.cmake:79-82`）而非成稿的"5 个模块 DSO"，与我本次 `:80-81` 的实读一致。**两批结论同向、无冲突，本处只订正 AUD401-018 自身文字，不重复立案、不另计一条**。
- **影响 D8 的处置**：**进任务书 + 需上呈裁**——机制与名册进任务书；(a) 真做动态模块 / (b) 承认静态交付 的**方向选择属 §8.4:657/659 文档变更，须负责人裁**（成稿已如此标注，无冲突）。上呈时须附两条新事实：选 (a) 则须先解 cosmetic/hips 的 undefined-symbol 与 GOMP 退化在册问题（不是"接上 loader"就完），选 (b) 则须同时订正 `install_layout.cmake:96-103` 的"经安全 loader 逐 unit 加载验证"叙述（现文与产品运行面不符）。


## AUD401-019 · 死码与装饰性入口：三命令"薄入口"零调用、6 个头零引用
**判定：降级**（前半"薄入口零调用 + 三个 INTERFACE target 零消费者"逐点确认；后半"6 个头零引用"复算 6 条中 1 条为假、2 条口径不当，须改写）

- **我的独立取证**：
  - 薄入口确为零调用：`git grep -n "normalize_subcommand\|mosaic_subcommand\|export_subcommand" -- .`（全仓跟踪集，非目录限定）= **恰 3 命中，全是定义行**：`lib/infrastructure/cli/normalize/normalize.h:13`、`mosaic/mosaic.h:13`、`export/export.h:13`，**零调用者**；三头确被 `commands.cpp:92-94` include（实读 `:92 normalize/normalize.h`、`:93 mosaic/mosaic.h`、`:94 export/export.h`），但被 include 的符号无人取用 ⇒ 装饰性 include 成立。
  - 真实命令面有完整触发路径（用于对照，也用于说明本条不可升 S1）：`command_tree.h:90-100` 表登记 `normalize/mosaic/export` 三 `CommandDesc` → `main.cpp:60 return dispatch(p)` → `commands.cpp:2256 int dispatch(const Parsed&)` → `:2280 return sub.dispatch(p, ev)` ⇒ **三子目录不在任何一条生产数据链上**，故"死码"判定成立；反过来也正因无路径，**本条按 §2.5 只能 S2**。
  - 三个每命令 INTERFACE target 零消费者：`git grep -n "astrocs_cli_subcommand_normalize\|_mosaic\|_export" -- .` 命中全部落在 `lib/infrastructure/cli/CMakeLists.txt:28,29,31,33,34,36,38,39,41`（自声明 + 反向 link 到父 `astrocs_cli_subcommands`），**无任何外部 `target_link_libraries` 取用**；父 target 才是在册消费者（`CMakeLists.txt:1015`，实读在 `acsd` 的链接块内）⇒ 与成稿一致。
  - **6 个"零引用头"复算（严格判据 = 有无 `#include` 取用；对照样本 `command_tree.h` 同法命中 commands.cpp/parser.cpp/session_commands.h，证明尺子能真阳）**：
    | 头 | 我的复算 | 成稿主张 |
    |---|---|---|
    | `lib/algorithms/shared/dirent_win.h` | 0 includer | **成立** |
    | `lib/infrastructure/pipeline/orchestrator/cpp/include/spill_manager.h` | 0 includer；但 `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:483` 记它 "production, yes, (2 sites)" ⇒ **死码判定成立 + 另有一处文档与现实打架** | 成立（未记冲突） |
    | `lib/infrastructure/pipeline/typed_dag_contract.h` | 0 includer ⇒ 成立，**但此件历史轮已在册且状态 OPEN**：`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:106`（L28c-E-001，原文"该头全仓零消费者、不在任何构建/测试源列表，被指派的锁不存在"）、`lib/infrastructure/pipeline/PENDING.md:12` 亦在列 | 成稿当新案登记 ⇒ 应改记**重复=历史在册 L28c-E-001**，不另立案 |
    | `lib/infrastructure/aio/include/aio_ahpx_format.h` | **推翻**：被 `lib/infrastructure/aio/src/ahpx/aio_ahpx_reader.h:4` 与 `aio_ahpx_writer.h:4` 以 `#include "../../include/aio_ahpx_format.h"` 取用，而这两头又被 `aio_ahpx_reader.cpp:1`、`aio_ahpx_writer.cpp:1`、`aio_ahpx_api.cpp:14-15` 取用，`astrocs_aio` 在 `acsd` 链接闭包内（`CMakeLists.txt:1008-1016`）⇒ **生产链上的活传递头**，且另有在册测试 `eng/tests/unit/ahpx_hips_format_test.cpp:29` | 成稿列为"零引用" = 假 |
    | `lib/infrastructure/cli/memory_growth.h` | 无 C/C++ TU include，唯一引用者是 `eng/tests/cli/test_memory_growth.py:17-19` 写进临时 .cpp 的内联夹具（测试期编译）⇒ 真实状态 = **仅测试夹具引用 / 生产未接线**，不是"零引用死码" | 口径不当 |
    | `lib/infrastructure/aio/third_party/cfitsio/f77.inc` | 仅出现在 cfitsio 上游 `.f` 的 `C     include f77.inc` 注释横幅（`iter_a.f:12,43,106`）⇒ 无取用**但属第三方 vendored 上游文件**，不适用标准04 §4:40 的"死码直接删除"判据（删它 = 破坏上游完整性） | 口径不当（须标注第三方豁免） |
  - 我未复算成稿的"tracked headers 354、零引用 6"总普查（§4.5 禁写普查脚本），只按名册逐条验；因此**"仅 6 个"这一穷尽性主张我既不确认也不推翻**，成稿若要在 D8 里当"全量普查"用，须由能跑普查的一方复核。
- **与成稿差异**：① 6 头名册中 1 条假（`aio_ahpx_format.h` 为生产活传递头，删它会断 `astrocs_aio`）、2 条口径不当（`memory_growth.h` 应归"未接线"、`f77.inc` 应归"第三方豁免"）、1 条应记为历史在册重复件（`typed_dag_contract.h`）⇒ 实际新增死码头只有 2 个（`dirent_win.h`、`spill_manager.h`）。② 状态轴按 §2.8 须分列：三薄入口与三 INTERFACE target = **未接线/装饰面**（在册有 include 有 target，无消费者），不是"未实现"。③ 严重级：本类无运行时触发路径 ⇒ **最高 S2**（成稿未升 S1，无冲突，但我把它写死在判定里以免下游拔高）。④ 新增一条成稿没有的关联证据：`spill_manager.h` 与 `PRODUCTION_EXECUTION_INVENTORY.csv:483` 的"production=yes/2 sites"互相打架。
- **影响 D8 的处置**：**部分进任务书**——①薄入口与三 target 的"接线或退役二选一"进任务书（与 AUD401-005 阶段分派同一处，成稿已如此标注）；②死码头只留 `dirent_win.h`+`spill_manager.h` 两条可删项，`aio_ahpx_format.h` **必须从删除名单剔除**（若在整改文档包里，须回退该子项），`f77.inc`/`memory_growth.h`/`typed_dag_contract.h` 分别改标第三方豁免/未接线/重复=历史在册；③"354 头普查"不得作为全量结论写进任务书。


## AUD401-020 · 构建脚本里的永假守卫与自相矛盾叙述（使注册面静默退化）
**判定：降级**（恒假守卫与"双重落空"我复算成立、6 处普查数确认；但**影响面主张"使注册面静默退化"推翻**，且三例矛盾叙述之一不成立）

- **我的独立取证**：
  - 恒假守卫：`lib/algorithms/projection/CMakeLists.txt:29-33` 实读——`:29-30` 注释称"共址可复用测试 (ENGINEERING_SPEC §4.6): 仅根图注册"，`:31` `if(ASTROCS_BUILD_TESTS AND EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/eng/tests/CMakeLists.txt)`，`:32` `add_subdirectory(tests)`。判据路径 = `lib/algorithms/projection/eng/tests/CMakeLists.txt`：`git ls-files lib/algorithms/projection` 全列 12 项，**无任何 `eng/` 条目**；`git log --diff-filter=D --oneline -- 'lib/algorithms/projection/eng*' 'lib/algorithms/projection/tests/CMakeLists.txt'` = **空输出** ⇒ 跟踪集＋删除历史两面皆无 ⇒ **从未存在，分支恒假**（按 §1 以跟踪集为准，不用磁盘）。
  - 双重落空亦成立：`git ls-files … lib/algorithms/projection/tests/CMakeLists.txt` **不返回该文件**（在册的只有 `tests/p3wcs/CMakeLists.txt`）⇒ 即便判据为真，`add_subdirectory(tests)` 也无汇总文件可加。
  - 普查 6 处确认（**先差点自伤**）：窄模式 `git grep -n "EXISTS \${CMAKE_CURRENT_SOURCE_DIR}" -- '*CMakeLists.txt'` 只给 **3** 处；改用宽容引号的 `git grep -n "if *( *EXISTS"` = **6** 处，与成稿同数：`CMakeLists.txt:115`（逐文件指纹枚举）、`eng/tests/unit/CMakeLists.txt:653`（noise_snr p1noise）、`lib/algorithms/coverage/CMakeLists.txt:232`（`${_cuda_bridge}` 构建产物门卫）、`lib/algorithms/drizzle/CMakeLists.txt:97`（nanoflann 外部 include）、`lib/infrastructure/acr/CMakeLists.txt:235`（`${subdir}` 循环变量）、`projection/CMakeLists.txt:31`（本案）。⇒ **恒假只有 1 处，与成稿一致**；我第一把尺子若直接用就会误报"6 应为 3"（这正是前言 §2.4 警告的窄模式假阴，先落在自己命令上）。
  - **影响面复算（推翻"注册面静默退化"）**：p3wcs 测试实际在根图在册——`CMakeLists.txt:1125 add_subdirectory(lib/algorithms/projection/tests/p3wcs)`，且 `:1121-1124` 说明它必须先于 `eng/tests/unit` 注册（`eng/tests/unit/p1wcs/CMakeLists.txt` 以 `$<TARGET_FILE:p3_wcs_test>` 交叉引用）；`eng/tests/unit/CMakeLists.txt:832-834` 亦记"W4-A9 批次 1: p3_wcs_test 迁 lib/algorithms/projection/tests/p3wcs/ (共址)… add_subdirectory(...) 承接"。⇒ **没有任何测试因该守卫恒假而失踪**；本案的真实性质是"模块侧留了一个它已不承担注册的死 `if`＋误导注释（注释自称注册钩子，注册实际在根文件）"，不是门全绿下的静默退化。
  - 矛盾叙述两例确认、一例不成立：① `CMakeLists.txt:320-325`（旧文"V7 残留面, 从未入库…目录保持 untracked 锁死不收编"）与 `:327-339`（"⚠ 2026-09-20 订正…上段旧文…已作废…① 该子图已在册…② 本 add_subdirectory 仍保持注释"）**并存** ✓（逐字读到）；② `:1060-1061` 旧入口名叙述与 `:1062-1067` 订正块并存 ✓；③ 成稿所称"`:906-910` GSL 禁令与 `:905` 链接行混排"——实读 `:905 target_link_libraries(astrocs_p1_sdet PUBLIC OpenMP::OpenMP_CXX)`、`:906-909` GSL-REPLACE-01 禁令、`:910 target_link_libraries(astrocs_p1_sdet PUBLIC m)` ⇒ 两条链接行都**不涉及 GSL**，禁令与相邻代码不矛盾，仅注释插在同 target 两条链接语句之间 ⇒ **此例不成立，应删**。
  - **新增一条成稿未记的同族矛盾（且更值得修）**：`eng/tests/unit/CMakeLists.txt:650-652` 注释仍写"lib/algorithms/noise_snr/tests/p1noise/ 为 V7 残留面, 从未入库(untracked 锁死不收编)"，而 `git ls-files lib/algorithms/noise_snr/tests/p1noise/CMakeLists.txt` **返回该路径 = 已在册** ⇒ 根文件的 2026-09-20 订正（`CMakeLists.txt:329-331`）没有回写到测试注册处，读者按 `:651` 会以为那是 untracked 残留而误判 `:653` 门卫恒假。成稿对该守卫给"守卫为真、非缺陷"的结论**正确**，但其取证写的是 `ls`（磁盘存在性，§1 禁用作仓库事实），须换成 `git ls-files` 才立得住。
  - 严重级：注释卫生＋死码类，无运行时触发路径 ⇒ 按 §2.5 **最高 S2**（成稿定级"高置信、路径存在性直接判定"，未升 S1，无冲突）。
- **与成稿差异**：① 标题的"使注册面静默退化"须删或改为"死守卫＋误导注释（注册实际在根图 :1125）"；② GSL 混排一例推翻；③ 补一条 `eng/tests/unit/CMakeLists.txt:650-652` 未随订正回写的在册矛盾；④ 成稿 `ls` 取证须换 `git ls-files`（本批已替它跑成）；⑤ 我确认其 6 处普查与 1 处恒假的判定（并记录窄模式差点造成的假阴）。
- **影响 D8 的处置**：**进任务书（缩小范围后）**——可执行项为：删/修 `projection/CMakeLists.txt:29-33` 死 `if`（要么删块并把注释改为"注册在根 :1125"，要么把判据指向 `tests/p3wcs/CMakeLists.txt` 并同步根文件避免**两处注册同一子图**，后者须防重复注册 target）；删 `CMakeLists.txt:320-325` 作废旧文；`eng/tests/unit/CMakeLists.txt:651` 注释订正为"已在册，门卫为构建产物容错"。**不进**：GSL 混排一项。建议保留的门禁加项（扫 `EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/...` 并断言跟踪集内存在）**要写成正例/负例对**，且判据存在性必须用 `git ls-files` 而非磁盘 —— 否则该门会像我第一把尺子一样自己数漏 6→3。


## 一、本批判定汇总（各态计数，附复算命令）

| 态 | 计数 | 条目 |
|---|---|---|
| 确认 | **3** | AUD401-009、-017、-018（-018 的核心主张确认，两条支撑证据订正） |
| 降级 | **4** | AUD401-008（计数口径）、-016（口径＋两条证据）、-019（6 头名册 1 假 2 口径不当 1 重复）、-020（影响面主张推翻，机制确认） |
| 推翻（整条） | **0** | — |
| 待证（整条） | **0** | 条目内子证据缺口 3 处，已逐条写明该跑什么（见各条末） |
| 重复= | **0**（整条） | 子项 1 处：-019 的 `typed_dag_contract.h` 应记 **重复=历史在册 L28c-E-001**（`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:106`，状态 OPEN） |

复算命令全部内联在各条"我的独立取证"里，可逐条重放；三条本批主干尺子为：
- `git grep -n "std::vector<std::thread>" -- lib | grep -vE "/tests?/|acr/|hips_browser/|/tools/"` → 23 行（生产私池 20）
- `git grep -c "p1_num(\|p1_flag(\|p1_int(" -- lib/infrastructure/scheduler/src/module_adapters.cpp` → 67 行；`-o | wc -l` → 75 次（含 3 处定义）
- `git grep -n "if *( *EXISTS" -- '*CMakeLists.txt'` → 6 处（恒假 1）；对照窄模式 `EXISTS \${CMAKE_CURRENT_SOURCE_DIR}` → 3 处（证明计数对尺子敏感）

## 二、旧判偏松清单（= 上一轮给下游的虚假授权）

1. **AUD401-016 → "全仓校验面不做值类型判定"是假阴**。实为 `validate_config_full` 对字符串类键**有** `is_string()` 判定（`parser.cpp:509,520,526,623,640,653,661`）。下游若据此去"新建类型校验"会重复造；真缺口是**数值旋钮无型判定**。
2. **AUD401-018 → "生产代码无任何 `dlopen`/`LoadLibrary`"是作用域假阴**（命令只圈 3 个目录，且我复核时又被 `head` 二次截断，真命中 315 行）。下游若据此设计"选 (a) 真做动态模块"的最小落地，会误判为"从零加加载器"，而产品里已有 4 处活加载器与一份 `backends.manifest.json` 清单通道。
3. **AUD401-019 → "`aio_ahpx_format.h` 零引用"若进删除名单即断链**：它是 `aio_ahpx_reader.h:4`/`aio_ahpx_writer.h:4` 的传递头，二者被 `aio_ahpx_{reader,writer}.cpp`、`aio_ahpx_api.cpp:14-15` 取用，`astrocs_aio` 在 `acsd` 闭包内。这是本批**唯一一条会导致实删破坏生产编译的授权**。
4. **AUD401-020 → "使注册面静默退化"授权过度**：p3wcs 测试经 `CMakeLists.txt:1125` 在册注册且被 `eng/tests/unit/p1wcs` 交叉引用，无任何测试失踪；若下游据此去"补注册"会造成**同一子图重复注册**。
5. **AUD401-008 → "私建池 14 处"被当成全量**：按同一尺子（生产 TU）真实 20 处，遗漏含 `commands.cpp:1260 hash_pool`（在 normalize/mosaic 生产链上）。下游若按 14 做整改清单会漏改。
6. **AUD401-009 → 静态门 `check_cli_run_preset.py` 的保护力被高估**：它是 `run_pipeline({` 子串在场判定，不阻止新增多阶段调用者。下游若写"该不变量有机器门守着"即为虚假授权。
7. **AUD401-016 的空门形态假设不成立**（负兜底＋只卡上界）：`:7520` 的 `-1` 在 `:7513-7519` 两道 fail-closed 之后不可达；真形态是"域内兜底使双边门失明"（`:7505 nested=1`、`:7506 pixfrac=1.0`）。下游若照"找负兜底"去扫，会一条也扫不到而误判"无空门"。

## 三、覆盖率自报

- **已判 7 / 分配 7**，非空未判名单：**空**。
- 每条均在 8 命令上限内收敛（实耗：008=8、009=5、016=8、017=2、018=8、019=5、020=5）。
- 条目内未穷证子项（不影响判定态，均已写明补法）：① -008 `upm.cpp:620` 池规模入参是否即 `exec.cpu_workers`；② -018 `eng/tests/abi/mod001_install_load_check.py` 是否现场 `cc` 编译 loader（§2.7 四问之"跑的是产品还是自家夹具"因此未答）；③ -016 用户配置键 → `dj` 的键名传递路径（本批唯一可能把该条升到 S1 的缺证）。
- 只读纪律收口：收工 `git status --porcelain` 原文与开工**逐字一致**：
  ```
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```
  全程未跑构建/`ctest`/`run_checks.py`/任何 `eng/**` 脚本（`eng/tools/check_cli_run_preset.py`、`eng/tests/**` 仅**读文件文本**），未 `import` 仓库内 Python，未建 `复算/V401-B/`（无大表需求），未碰 `整改/`、`独立审计/批次清单/`、`独立审计包/`。

## 四、我对第①层判据本身的校准建议

1. **凡"计数"必须同时给尺子与口径，并声明排除了什么**：本批 4 条含计数（14 池、67 取值点、6 头、6 守卫），其中 3 条的数在我换尺子后变化（14→20、67 行≠67 调用点、窄模式 3↔宽容模式 6）。建议第①层判据加一条：**计数句后必附确定命令原文与"排除集"**，否则下游无法复用。
2. **"零引用/无人用"必须给传递链而非同层引用**：`aio_ahpx_format.h` 之错来自只看直接 include 面。建议判据固定为"**从 `acsd` 链接闭包出发的传递可达性**"，并把 `-019` 的六头判定表做成模板（0 includer / 传递活 / 第三方豁免 / 仅测试夹具 四态）。
3. **负证据必须声明目录作用域，且禁止 `head` 截断**：第①层两次把"我搜的 3 个目录内无命中"写成"生产代码无"。建议第①层判据明文：负证据命令不得带 `head`，且作用域必须写进结论句。
4. **"影响面"与"机制"要分栏定级**：`-020` 机制（恒假守卫）确凿而后果（注册退化）不存在；`-008` 机制（私池）确凿且后果比成稿重。同一"高置信"标签盖不住两种方向。建议第①层每条拆 `机制置信` 与 `后果置信` 两栏，后者要求填"若此条不修，哪一次生产运行会变错/不变"。
5. **`ls` 不得作为"路径不存在"的证据**：`-020` 的取证命令用了 `ls`（磁盘），前言 §1 已规定以跟踪集＋删除历史为准。本批我全部改跑 `git ls-files`/`git log --diff-filter=D` 复算，结论侥幸一致，但尺子不合规。建议第①层判据在"取证命令"字段做**机器筛**（出现 `ls `/`test -e` 即打回）。

<!-- PROGRESS: 7/7 -->
