# 复核 — AUDIT-06 第②层：结果层（DB-17）与收口层（DB-20）待判 5 条

独立复核代理件。基线 HEAD=`c8f64e9a`。开工 `git status --porcelain` 原文（两行，与派单给的基线**完全一致**，无新增/消失）：

```text
?? "ACSD\346\225\264\346\262\273\345\267\245\344\270\203_AUDIT-06.zip"
?? site/
```
（非 ASCII 转义由 `git -c core.quotePath=false` 复核为 `ACSD整治工作包_AUDIT-06.zip` 与 `site/`。）
全程只读：零 git 写、零 `git config`、未执行 `eng/**` 任何脚本、未构建、未跑测试。自写复算脚本与输出全部落在
`独立审计/复算件/results-v\`（`r1_ns_prefix_sim.py`、`r1b_d3b_coverage.py`、`r2_polar_snapshot_and_leafmax.py` 及同名 `*_out.txt`）。

---

## R1 清包会让写法门怎么样（来自 DB-20 §0.2 反向咬合条 / 条目 I-14 / P1-5）

**判定：确认（方向 = 判红，不是失明）＋ 补两条成稿未落的独立发现**

**我的独立取证**（只读源码 + 自写复刻脚本，命令：`python r1_ns_prefix_sim.py`，输出 `results-v\r1_out.txt`）

1. 前缀表来源与不等式方向：`eng/tools/doccheck/check_doc_hygiene.py:390-403`
   `namespace_prefixes(root)` 对 `("run","工程控制")` 逐个 `os.path.isdir` ⇒ 不存在则 `continue`（**不抛**），
   存在则 `os.listdir` 取子目录名，`name.startswith(".")` 与非目录被跳过（:398），
   `head = re.split(r"[-_]", name)[0]` 且要求 `^[A-Z][A-Za-z0-9]*$` 且 `len(head)>=2`（:400-401）。
   **① 三种情形判定**：前缀源为空集时 **判红**——`:646-651` 显式零输入守卫
   `if not prefixes_now: v.append({"check":"D3b_namespace_table_empty", … "⇒ D3b 判据不可评估，按 fail-closed 判红"})`；
   既不是崩（:395 已 isdir 保护），也不是空集静默。**不崩。**
   红即门红：`v` 非空 ⇒ `:1130-1131` `verdict:"FAIL"` ⇒ `:1145 return 1`；
   `eng/ci/checks.json` 的 `CHK-DOC-HYGIENE` 为 `"waivable": false`、profiles `fast/linux-main/windows-main`
   ⇒ 该条不可豁免、三档全跑。**成稿主张的 fail-closed 判红 = 成立。**
2. **② 干净克隆上 `run/` 是否存在、贡献什么**：`git ls-files run/` = **1**（只有 `run/.gitkeep`）⇒ 目录**存在**，
   `os.listdir` 得 `['.gitkeep']`，但该名字以 `.` 开头被 :398 跳过 ⇒ **run/ 恒贡献 0 前缀**。
   `git ls-files 工程控制/` = **31** 文件、二级目录只有 `RELEASE-05` ⇒
   **干净克隆上前缀表 = `{"RELEASE"}`，count=1**（我的脚本 B 段实测）。
   本机工作树另有 `run/PROJECT-GOVERNANCE-01` ⇒ 本机 = `{"PROJECT","RELEASE"}`（本机残留不算仓库事实，仅注记）。
3. **③ 删掉最后一个控制包**：`git` 不存空目录 ⇒ `工程控制/` 在干净克隆上消失 ⇒ `prefixes = []`（脚本 C 段实测 count=0）
   ⇒ 命中 :648 ⇒ `D3b_namespace_table_empty` ⇒ exit 1。与 `CONTROL_PACK_SPEC.md:210`
   （"收口即清理……随后把本控制包目录从仓库移除（与清理任务同一提交）"）**正面打架**：
   规范强制的收口动作，必然让一个 `waivable:false` 的门红。
   ⇒ **方向性结论：这条是发布流程阻塞项，不是"又一个洗绿门"。**
4. **成稿未记、但对整改定性有用（已核）**：零输入守卫**已有可执行负例**——
   `check_doc_hygiene.py:1041-1049` 的 W22 `build(root, ns=False)`（不建 `工程控制/`）断言必出
   `D3b_namespace_table_empty`；`build()` 默认分支 `:772-774` 专门造 `工程控制/DOC-SELFTEST-01` 供前缀。
   ⇒ 成稿"最小改法＝输入为 0 即红"**这一半已经在位**，残余缺口只剩另一半（前缀来源换成跟踪清单文件），
   整改面比成稿写的小。
5. **成稿未记、且是真正的失明窗口（我的独立发现）**：`D3B_ID_RE`（`:349`）
   `\b([A-Za-z][A-Za-z0-9]*)((?:-[A-Za-z0-9]+)+)-(\d{1,3})\b` 要求 **head + ≥2 个 dash 段 + 末段 1–3 位数字**。
   实测（脚本 D 段）：`RELEASE-05`、`AUDIT-06`、`P5-SNR`、`SCI-503` **形态均不匹配**；只有
   `DOC-HYGIENE-01`、`GATE-TRIAGE-01`、`MEM-WIRE-01` 这类三段式才进判据面。
   干净克隆上唯一前缀是 `RELEASE`，而仓库里不存在 `RELEASE-x-NNN` 形态的过程编号 ⇒ **该前缀是惰性的**。
   量化（`python r1b_d3b_coverage.py`，输出 `r1b_out.txt`，扫描面与 `:136-155`+`:455-460` 同构：
   4 份根规范 + `docs/**`（剔 `docs/research/`、`docs/archive/`、非 `SCAN_EXTS`）+ 6 份入口文档，
   并复刻 `:372-387` 的代码围栏/行内 code span 豁免）：**正式文档扫描面 191 个文件**；
   **D3b 形态可匹配 token 92 处、分布在 23 个不同前缀**（`CHG`40、`FZ`11、`OBS`9、`ARCH`8、`REL`7、
   `BLD`/`P1`/`CFG`/`PERF`/`SCI`… 各 3–4）；其中 **head==`RELEASE` 的命中 = 0 处 ⇒ 覆盖率 0/92**。
   ⇒ **本条按"命中数=0"的算术只用于说明覆盖面，不等于 92 处漏判**：92 是**形态可匹配总量**，
   我未再减 `registered_ids()`（`:406-426`、`:509-511` 的 `regs` 豁免：`eng/contracts`、`eng/ci`、`docs`、
   `artifacts/evidence` 的 json/yaml/csv 里出现过的 ID）与 D1c 硬表已覆盖的 11 个 token + `SCI-5\d{2}`，
   其中 40 处 `CHG-*` 是变更编号（合法登记面，D1b 有 `CHG_EXEMPT_RE:81`）。⇒ **真实漏判 = 上界 92、下界 0**；
   但"干净克隆上 D3b 的有效命中恒为 0"这一步不依赖这个分类，它只依赖表 = `{RELEASE}` 与正则形态交集为空。
   我另外核过**没有别的门补这个面**：`eng/ci/check_naming_surface.py` 管的是 AstroCS 改名保留面
   （`eng/ci/ledgers/naming_surface.json:purpose`，定义源 `ENGINEERING_SPEC.md §15`），与过程编号命名空间无关；
   除 D3b 外只剩 D1c 的手写 token 表（只减不增棘轮）⇒ 覆盖面确实随在场包数收缩。
   ⇒ 结论要分两段写：**剩最后 1 个包时 D3b 已经是"门在跑、实质不判任何东西"的静默零覆盖（不红不崩＝fail-open 面）**；
   清到 0 个包才跳到判红。所以它**同时**是发布流程阻塞项（0 个包必红）**和**一个部分失明的门（1 个包时零覆盖），
   两头的整改都要做：①前缀表改由跟踪清单（如 `工程控制/**/TASKS*` 登记表或 `run_keep.txt` 式的在册文件）派生；
   ②把 `D3B_ID_RE` 覆盖不到单 dash 编号（`<包>-<NNN>`、`SCI-5xx` 家族已由 D1c 硬表兜）这一事实写进判据说明，
   或补一条"命名空间表当前有效覆盖 = 0"也判红的守卫（与 :648 同构，但按命中数而非表空数）。
   **定级按"交集为 0 未必是功能缺陷"这条自问过**：本门权威（`:22-23`）自称"进程/任务编号"通用面、
   且明确"无手写 token 表"（`:391`），所以覆盖面随在场包数从属于目录布局＝与设计自述矛盾，
   不是"设计如此、按设计它在干活"⇒ 维持 P1 主体（阻塞面）＋ P2 副面（零覆盖），不因交集为 0 升 P0。

**与成稿差异**：成稿（DB-20 §0.2、I-14、P1-5）方向与行号全部正确（:390-403、:648-652、`waivable=False`、
`git ls-files run`=1、self-test 造 `工程控制/DOC-SELFTEST-01`），我全部独立复现 ⇒ **确认**。
差异只有两处补充：成稿把"输入为 0 即红"当作**待加**的最小改法（实际已在位并有 W22 负例）；
成稿未记"1 个包时惰性前缀＝静默零覆盖"这一半，因而把本条只定性为阻塞项，漏了它同时是失明面。

**定级建议**：**P1**（发布流程阻塞项：最后一次清包提交必红一个不可豁免门；修法是"清包与装下一包同提交"＋前缀表换跟踪清单来源）。
不定 P0：门红是**响在正确方向上**（fail-closed），不产生假绿；真实假绿面（第 5 点的零覆盖）在 1 个包时，
不影响发布判定结果，属判据强度缺陷。

**缺什么证据**：无（本条全部为静态源码＋跟踪清单可判定，无需运行）。
可选加强：在临时目录里用 `--root` 跑一次门以观实机读数——本次按派单纪律未执行（属 `eng/**` 脚本）。

---

## R2 极区报告里四处值在跟踪日志中不存在

**判定：降级（4 条中 3 条成立；第 4 条推翻。SNAPSHOT 子核 = 确认 47/47 通过）**

**我的独立取证**（`python r2_polar_snapshot_and_leafmax.py` → `results-v\r2_out.txt`；文档锚为
`实验/healpix-polar/docs/EXP-07-POLAR.md`，日志锚为 `实验/healpix-polar/results/`）

| # | 报告值（路径:行） | 跟踪件实值（路径:行） | 我的复算命令/结果 | 判 |
|---|---|---|---|---|
| ① | 样本面 `441`：`:522` 表行"帧中心 ±10 px / **1 px** \| **441**"；`:298` 正文"破门 **0/441**" | `p4_hst.out:2` 标题＝"帧中心 +-10px, **2px** 栅格"，`:16`＝"汇总 **n=121** : V0 破门=0 最坏=**2.148e-14** \| REC-1 破门=0 最坏=2.635e-14 \| oracle 2.769e-14 \| drop 面积比 3.736e-03" | `grep -n "n=441" results/*` → **0 命中**；`code/p4_hst.cpp:87` 只调 `scan(…, -10.0, 10.0, step=2.0, "…2px 栅格")`，**跟踪探针里不存在 1px/441 扫描**（441=21²，121=11²） | **确认**：样本面标签不实；同行 4 个数值(2.15e-14/2.64e-14/2.77e-14/3.736e-03)与 n=121 汇总**逐位吻合**⇒ 是"值取自跟踪 121 配置、n 与步长写错"，不是另有一份日志 |
| ② | `6.96e-14`（`:298`，"T16 极点扫描里 V0 破门 0/441、最坏 6.96e-14"） | 同实验跟踪实值 `2.148e-14`（2²¹）／`1.896e-14`（2²³，`p4_hst_n23.out:16`） | 字面量 `6.96e-14` 全 `results/` **0 文件**；`grep -lE "6\.9[0-9]*e-14"` 只命中 **`t17_extent.csv:173`** 的 **oracle 列 = 6.937989e-14**（T17 环扫描 r=32px、az=225°，`p5_t17.out:1` 口径＝"T17 极点破门半径依赖 (nside=2²¹, 0.20″/px)"）。范围我按整棵树复核过：全跟踪集中 `6.9?e-14` 形态只出现在 `实验/healpix-polar/results/t17_extent.csv` 与 `eng/tests/validation/release02/c_delta_composition/pair_fits.json`（一批无关的 `c` 系数），字面量 `6.96e-14` 全仓 **仅 `EXP-07-POLAR.md:298` 一处** | **确认**：不是"抄错跟踪日志里的 V0"，而是**本单元跟踪集内唯一同尾数量属另一个实验的另一列（oracle）**⇒ 换件/换列串台高度可疑（未坐实）。且 `:298` 与同文档 `:522`（2.15e-14）**自相矛盾**：同一 T16 扫描两个"最坏" |
| ③ | `1.18e-13`（`:553`，"nside=2²³ 时 … V0 仍给出 1.18e-13"） | `p4_hst_n23.out:16` V0 最坏 = **1.896e-14** | 字面量 `1.18e-13` 全 `results/` **0 命中**；`grep -E "1\.1[0-9]+e-13"` 命中的是 **`p1_t7.out:6` `|sum_prod-sum_A0|/A_drop 最坏=1.106e-13`** 与 **`p9_review.out:47` `|Δ|/A_drop=1.149e-13`** —— 二者量纲是"生产 vs A0 复刻保真度"，**不是 V0 闭合量** | **确认**：不可溯源，且量级家族只存在于**别的口径**（复刻偏差）⇒ 属"换了另一个量的数"，比"日志不存在"更需要订正口径标注 |
| ④ | `leafmax 中位 2.14e-08`（`:229`，同段"最坏 3.159e-07"） | 跟踪件 `t4_pole_full.csv`（1089 行数据）`leafmax_err` 列 | 我自己按列统计：`n=1089 max=3.159e-07 median=2.14e-08` —— **与文档两位值逐位一致**（同段最坏 3.159e-07 另见 `p1_t4_pole_full.out:9` ✓） | **推翻成稿的"未落档"**：字面量确实不在文件里（`grep 2.14e-08` → 0 文件），但它是**跟踪 CSV 上确定性可派生的统计量**，非"无出处"。残余偏差只是"报告没写派生口径（哪个文件、哪一列、n=1089 中位）" |

**SNAPSHOT 子核**：`results/SNAPSHOT.sha256` 47 条，我用 Python 逐条重算 sha256 ⇒ **命中 47 / 不符 0 / 缺件 0 = 47/47 通过**。
覆盖度：本单元跟踪文件 52 个，快照未含的恰是 `README.md`、`docs/EXP-07-POLAR.md`、`docs/EXP-07-POLAR-摘要.md`、
`results/SNAPSHOT.sha256` 自身、`run_all.sh` 五项 ⇒ **`code/` + `results/` 全部数据件与源件均已钉**，
快照是闭合的输入/输出锚（成稿的正面陈述成立）。

**与成稿差异**：成稿（DB-17 :292-294、:298）把四点并列成"4 处数值在跟踪日志中不可回溯"。
我复算后：①②③ 成立（并给出 ① 的"标签错而值对"、② 的"同尾数异口径候选源"、③ 的"异量纲同量级"三条细化，
都比"不存在"更有可修性）；④ **不成立**——`2.14e-08` 由跟踪 CSV 精确派生。
整条按 3/4 成立计 ⇒ **降级**：把"主判据的中位数未落档"从病灶里划掉，剩下的病灶是"报告值与跟踪件之间缺**可追溯标注**
（扫描名/步长/n/列名/派生方式）"，而不是"数字像编的"。

**定级建议**：**P2**（①②③ 三处订正即可，属报告可追溯性；其中 ②③ 因**自相矛盾且涉及 RC3 失明这一关键论据句**，
若报告将对外，建议升 P1——由负责人定；我这一层只把证据摆清）。④ 不列缺陷。
SNAPSHOT 47/47 通过 ⇒ 该单元落档纪律是**本批最强**，成稿也如此评，我确认。

**缺什么证据**：① 是否真跑过 1px/441 扫描（跟踪探针 `p4_hst.cpp` 无该分支 ⇒ 需作者补一次可复现命令行，
或把 `:522` 的 n 改 121、步长改 2px）；② `6.96e-14` 与 `1.18e-13` 的原始出处（本工作树 `run/` 残留里可能有，
但按纪律本机残留不算仓库事实，不能用来给报告背书）；④ 无缺口。

---

## R3 同一物理量两值与"文档低估缺陷量级"

### ① M42 帧级 σ 两份结果差约 4 倍

**判定：确认（差因＝口径未声明，非取数错；两侧数值各自可回溯）**

取证（全部读跟踪件，命令见下）：

| 面板/帧 | `实验/absolute-snr/results/EXP06_TABLES.md`（§C 行 241–243，表头只写"帧级标量 vs 区域 sigma（EXP-03 机理复现）"） | `实验/absolute-snr/results/EXP05_TABLES.md`（表 A2 行 33/35/40，列名 `sigma_frame`） | 比值 |
|---|---|---|---|
| M1 `20251212@012404` | 13.792 | 16.1558 | 1.17× |
| M2 `20251212@020002` | **14.782** | **59.4153** | **4.02×** |
| M4 `20251224@045919` | 13.318 | 17.5785 | 1.32× |

两侧我都能回溯到自己的源 JSON：`exp06_e3_real.json:/meta/crop = 1024`、
`frames[*].C3/sigma_frame_scalar = 13.791913… / 14.782136… / 13.318145…`；
`exp05_e3_real.json:/meta = {"delta_px":64,"crop":2048,…"estimators":{"frame":"fam0 全帧 2 轮裁剪 RMS（生产 recipe 镜像）","patch":"fam0 逐 64px patch 同一 recipe"}}`、
`frames[*].sigma_frame_fam0 = 16.1558 / 59.4153 / …`。
**4 倍差因我复算出来了**：`exp05_e6_mechanism.json` 对同一帧给
`sigma_frame_raw=59.4153 → sigma_frame_struct_removed=17.4041`（`structure_boost=3.4139`）⇒
EXP-05 的 59.4 里 **3.4 倍是星云结构**，去结构后 17.40；再叠加窗口 2048²→1024² 才到 EXP-06 的 14.78（余差 1.18×）。
⇒ 两值都不是"抄错"，是**同一个名字下两套口径**（窗口 × 是否含结构 × 每面板代表帧选择规则）。

**成稿差异**：成稿（DB-17:88）说"两侧源件确有说明差异……**但两份结果表都不写裁剪口径**……只能回到 JSON"。
表内不写口径 = 我实测确认（`git grep -i "crop|2048|1024"` 在两份 `EXP0*_TABLES.md` 中 **0 命中**）；
但"只能回到 JSON"偏重——**实验设计文档里写了**：`docs/EXP-05-ABSOLUTE-SNR.md:363`（臂 C：12 帧、中心 2048² 裁剪）、
`docs/EXP-06-SNR-PHYS.md:319`（M1/M2/M4 4096²、中心 1024² 裁剪），且 EXP-05 自带结构去除对照 ⇒ 差异**仓库内可调和**，
缺的是结果表与对外引用处的口径标注。**⇒ 本条整体确认，但可修性比成稿写的好（降级其严重度，不降级其存在）。**

**要写清什么才算修好**：两表各加列/脚注 = `窗口（中心 1024² / 2048²）+ recipe（fam0 全帧 2 轮裁剪 RMS）+
是否去结构（raw / struct_removed）+ 每面板代表帧选取规则 + 帧数`；
对外只允许一个被命名的量（建议 `σ_frame,prod(2048²,含结构)` 与 `σ_frame,prod(1024²)` 分名，
禁止都叫"帧级 σ"）；并给出 59.4153 = 3.41× 结构 × 1.18× 窗口 的分解链指向 `exp05_e6_mechanism.json`。

### ② SCI-B 读噪双计比例：正式文档 34% vs 实验/任务书 36.6%

**判定：确认（正式文档低估缺陷量级；现行正确值 = 最坏 +36.6%）**

我按两个 JSON 逐键复算：

- `实验/absolute-snr/results/b2_noise_terms.json`：`gates.G4c_max_bias_over_all_points = 0.3395416`（**+34.0%**），
  其来源点是 `scans/read_noise_e[5].bias_empirical_rn = 0.339542`（RN=50 e⁻）；`gates.G4c_bias_at_base_point = 0.1279130`（+12.8%）。
- `实验/absolute-snr/results/b1_sky_scan.json`：`gates_faint.G4c_empirical_rn_max_rel_dev = 0.36619929`（**+36.6%**，
  且 `…rel_dev_at_B0` 同值 ⇒ 最坏点在 B=0 暗源）；`gates_bright.G4c_empirical_rn_max_rel_dev = 0.1581151`。
  ⇒ 跨两个扫描面的最坏 = max(0.3395, **0.3662**) = 0.3662。

引用面清点：正式文档三处用 34%（`docs/science/NOISE_MODEL.md:324`"实测双计使 σ_F 高估 +12.8%~+34.0%"、
`docs/science/CONTROL_WEIGHT_SNR.md:201`"基准点 +12.8%、RN=50 时 +34.0%"、
`docs/plugins/algorithms_phase1/07_noise_snr.md:123`"至 **+34.0%（RN=50 e⁻ 最坏点）**"）；
实验与任务书四处用 36.6%（`实验/absolute-snr/README.md:111`、`REPORT_paper.md:222/244/460`、
`工程控制/RELEASE-05/00_README.md:15`、`GAP_AUDIT.md:20`、`PROMPT.md:16`、`tasks/SCI-501.md:4`）。
且该单元**自己已判过**：`results/REVIEW.md:186` M6「"最坏 +34.0%"低估了自己的最坏值」、`README.md:319` M6 整改项。
⇒ 现行正确值 **36.6%**；34.0% 是 **B2 单扫描面内**最坏，`07_noise_snr.md:123` 把它标成"最坏点"＝把扫描内极值当全局极值。
差因是**口径（扫描面）未声明 + 取数取到弱的一侧**，不是舍入。

**修好标准**：三处正式文档改为"σ_F 高估：基准点 +12.8%；RN 扫描面（B2）内最坏 +34.0%（RN=50）；
**全证据面最坏 +36.6%**（B1 暗源 B=0，`b1_sky_scan.json:gates_faint.G4c_empirical_rn_max_rel_dev`）"，
每值带扫描面 ID + JSON 指针 + 源点（`read_noise_e[5]` / B=0 暗源）；并把它登记进
`eng/tools/doccheck/doc_fact_authority.json`（见 R4 末：该表现在只有 1 条事实，D4b 对这些发散量零覆盖）。

### ③ healpix 摘要同页 1.12e-8 与 2.79e-8

**判定：确认（成稿溯源我逐位复现；两值都真，差在样本面未写）**

- `实验/healpix-polar/docs/EXP-07-POLAR-摘要.md:37`（对照表 REC-1 预算 1e-7 行）= **1.12e-8**、段/叶 1333；
  `:43`（推荐配置正文）= "预算 `1e-7·A_drop` ⇒ 最坏 **2.79e-8**（36× 门禁余量）" ⇒ 同页两值、同一预算、无口径说明。
- 出处复算：`results/p2_t9.out:3` "配置=49"、`:5` "budget=1e-07 x A_drop : 最坏闭合=**1.1220e-08** 平均段数/叶=**1332.77**"；
  `results/p3_t13.out:65` "N4 极点 REC-1(1e-7): 破门=0/**1089** 最坏=**2.789e-08**"。⇒ 1.12e-8 来自 **49 配置**（T9），
  2.789e-8 来自 **1089 配置极点栅格**（T13-N4），**两值皆真、非抄错**。
- 成稿另指"报告 §6.1 行 615 把 N4 的 2.79e-8 与 T9 的 1333 段/叶拼成同一句推荐配置"——我核到原文
  `docs/EXP-07-POLAR.md:615`："预算选择建议：`budget_abs = 1e-7 · A_drop` 给出最坏闭合 2.79e-8（36× 门禁余量），代价 1333 段/叶"
  ⇒ **混源确认**：精度取 1089 配置、代价取 49 配置，同一句里两个不同样本面。

**修好标准**：两处都补 `（T9，49 配置）` / `（T13-N4，1089 配置极点栅格）`，
`:615` 一句内同面配对（1e-7 预算：49 配置 1.12e-8 且 1333 段/叶；1089 配置 2.789e-8、破门 0/1089），
并把"36× 门禁余量"的门禁值同址写出。

---

## R4 手写报告层的字面量与自家 JSON 相矛盾

**判定：确认（三处不一致全部复现），但定级按"是否进判据/对外主张"分档；成稿的 1 处对照口径需订正**

三栏并列（报告值｜JSON 实值｜差多少）：

| # | 报告值（`实验/additive-sky-seamless/README.md`） | 机器实值（跟踪 JSON） | 差多少 / 是否进判据 |
|---|---|---|---|
| 1 | `:369` §9 验收门表："1%@24 px；**PSD k=4（预期 5.33±2）**；来源 = 未施加的 Phase1 归一" | `results/c2_multiplicative.json:hf_component`：`psd_k_peak=26`、`psd_k_expected=21.333333`、`psd_period_peak_px=19.692307`、`bin=4`、`injected_period_px=24.0`、`psd_peak_at_injected=false` | k 差 **22 个 bin**（4 vs 26）、预期差 **16**（5.33 vs 21.33）；同文件 `:151` 已写对（"PSD 峰 k=26 vs 预期 21.33（±6 bin 内）"）⇒ **同文两值**。**进对外主张**：`:369` 是验收门表行（"完成"判定的读数） |
| 2 | `:151` 与 `:289–290`："1% 幅度：未检出（**amp −0.0004 ± 0.038**），3σ 检测限 0.069" | 同一 JSON：`hf_component.injected_amp=0.01`、`detected_at_1pct=false`、`detection_limit_3sigma=0.069056199`；`gates.rows[5].value.amp1=0.6720891171838539 / err1=0.023018733227673917`（属 `hf_matched_filter`，其 `amp_injected=1.0`） | 字面量 `−0.0004`、`0.038` 在 JSON 中 **0 命中** ⇒ 无落档。**但**：该行承载的结论（未检出、3σ 限 0.069）**逐位有落档** ⇒ 属**描述性数字**，不翻转任何门 |
| 3 | `:211` §4.6 E3："按需 64² 块 **11,688 kB** < 稠密物化 512² **14,568 kB**" | `results/c6_sparse_dense.json:upm_memory`：`rss_64block_kb=11744`、`rss_full_grid_kb=14520` | **−56 kB / +48 kB**；E3 门（`gates.rows[2]` `E3_rss_scales_with_block`，value `{blk:11744, full:14520}`, `ok=true`）判据是 **blk < full 的比较式**，用 README 的错值不等式仍成立 ⇒ **不翻转门**；正确值同单元 `REPORT_paper.md:265` 已给 |

**判据/对外主张归属判定（派单要求的那一刀）**：
第 1 处进 §9 验收门表＝对外主张（"该判据完成、读数是它"）⇒ 按派单口径算 **P0 级字面量污染**；
第 2、3 处只是描述性数字（结论另有落档、不翻转门）⇒ **P2**。
成稿说"若干处"并列，未做这层分档 ⇒ 我按"进了判据才算 P0"补分档：**1 处进对外主张、2 处不进**。

**与成稿差异（含一处订正成稿）**：
① 成稿（DB-17:185、表行 195）把第 2 处的"机器值"配成 `0.6721±0.0230` ⇒ **口径错配**：该对是
`hf_matched_filter`（`amp_injected=1.0` 的归一化恢复比），不是"1% 幅度的恢复幅度"；1% 侧的正配是
`hf_component`（injected 0.01 / `detected_at_1pct=false` / limit 0.069）。所以"报告值与机器值差 0.67"这个说法要撤回，
剩下的只是"那两个具体数字无落档"（结论本身有落档）。
② 其余（三处清单、`grep −0.0004|0.038` → 0、RSS 差 56/48、"生成表与门函数是实算、无写死"）我全部独立复现 ⇒ 确认。
③ 补充一条成稿未记、直接决定整改面的事实：**`eng/tools/doccheck/doc_fact_authority.json` 的 `facts` 只有 1 条**
（`drizzle.pixfrac.default`），⇒ 写法门的 **D4b 事实唯一性判据对上面三处同名量发散零覆盖**
（`check_doc_hygiene.py:536-550` 只在登记表内的键上查发散）。这三处该由门自动抓，现在抓不到。

**定级建议**：**P1**（不是 P0）——一处进了对外验收表、一处同文自相矛盾（PSD k）、两处过期字面量与自家 JSON 发散，
但无判据被翻转、无结论被伪造（成稿自述"门函数实算"我也复核：`gates.n/n_pass/n_fail` 由脚本写入，
`c6` 四门 `n=4, n_pass=4, n_fail=0`）。整改：① 三处按 JSON 改正（`REPORT_paper.md` 已全给）；
② 把 `PSD k`、`amp1`、`rss_*_kb` 登记进 `doc_fact_authority.json`，让 D4b 从 1 条扩到覆盖这些跨件同名量。

**缺什么证据**：README 这三个字面量的**当时出处**（旧一轮 run/ 日志或旧 JSON 快照）——跟踪集里没有，
本机 `run/` 残留不作数；找不到就按"无落档字面量删除或改为指针"处理。

---

## R5 一份"文档"是未填充的骨架却占着 `docs/` 位

**判定：确认（占位与 5/7 结论行逐位复现），并补一条比成稿更重的观察**

**先纠路径**：派单与成稿写 `docs/smooth-lambda.md`；仓库根下该路径**不存在**
（`wc -l docs/smooth-lambda.md` → No such file；`git ls-files docs/smooth-lambda.md` → 空）。
真实跟踪件是 **`实验/additive-sky-seamless/docs/smooth-lambda.md`**（156 行）。
成稿沿用引用串里的**单元相对写法**而未展开——整治时要把引用串一并改成仓库根相对路径，
否则 4 处引用里有 3 处从仓库根解析不出来（同样是不自洽锚）。

**我的独立取证**

1. 占位符：**7 处、`grep -c "{{" = 7`**，逐条行锚——
   `:105 {{TABLES}}`（§3 合成实验结果）、`:111 {{REAL}}`（§4 真实 L4 数据实测）、
   `:117 {{OBS}}`（§5 负责人观察的数值验证）、`:123 {{RECO}}`（§6 **定案：推荐生产默认值与配置键**）、
   `:129 {{ADAPT}}`（§7 是否需要自适应 λs）、`:135 {{SURFACE}}`（§8 改动面）、`:141 {{LIMITS}}`（§9 局限与"待定"）
   ⇒ §3–§9 **全部未替换**，成稿主张成立；结果、定案、局限三个核心面零内容。
2. §0 结论速览表共 7 行（`:15–21`），**含未填 X/Y/N 符号的 = 5 行**（成稿"5/7"精确成立）：
   `:16` "C 场空间梯度 RMS 下降 **N×**，天光去除率从 X 掉到 Y"、`:17` "峰区压暗 = **X%**"、
   `:18` "一致 vs 帧间抖动：**X% vs Y%**"、`:19` "台阶比 **X**（λs=0）→ **Y**（推荐 λs）"、
   `:20` "`upm.smoothing_lambda` = **X**（区间 [a, b]）"。另 2 行：`:15` 给了实数（λs≈0.1 上界）、`:21` 值为"—"。
3. **我比成稿多出的那条**：`:15/17/18/19/20/21` 六行的"关键数值"列写的是 **`见 §3.2`／`见 §3.3 / §5`／`见 §3.5`／`见 §4`／`见 §6`／`见 §7`**
   ——而 §3–§7 全是空占位 ⇒ **该表的每一条结论的证据指针都落进不存在的节**。
   这不是"骨架没填完"那么中性：它是一个**看起来已给结论、实则零证据支撑**的速览表，
   且 `:20` 恰是本文件标题声称的目的（"定案：推荐生产默认值"）。
   （唯一实质结论 `:15` 的 λs≈0.1 由 `:38` 解析推导独立撑住：`O(1/n_cov)≈0.1`，锚 `upm.cpp:645-649`。）
4. 引用它的位点：我数到 **6 个跟踪件 / 9 个引用位点**（成稿说"4 处"，低估）——
   `实验/additive-sky-seamless/REPORT_paper.md:373`、`REPORT_paper.md:539`、
   `实验/additive-sky-seamless/code/reverse_verify/smooth_lambda/report.py:2`、
   `实验/additive-sky-seamless/results/REVERSE_VERIFY_CANON.md:4`、
   `实验/shared/REVERSE_VERIFY_MIGRATION.md:29`、`REVERSE_VERIFY_MIGRATION.md:98`、
   `reports/v19r2/file_audit_inventory.csv:3063`、`reports/v19r2/source_manifest.csv:3650`、自身路径。
   **逐处判它被当作什么**：
   - `REPORT_paper.md:373/539` ⇒ 引用"它应有的那份真实件"，且**引用者自己已声明它不作证据**
     （"λs 扫描线在 …§0 结论表中仍是 X/Y/N 占位符（`results/REVERSE_VERIFY_CANON.md` 已如实登记「从未回填」）"）；
   - `report.py:2` ⇒ 生成器 docstring"汇总 λs 扫描指标 → markdown 表 + ASCII 曲线（**供 docs/smooth-lambda.md 引用**）"
     ⇒ 引用的是**它应有而尚无的产物落点**；
   - `REVERSE_VERIFY_CANON.md:4` 与 `REVERSE_VERIFY_MIGRATION.md:29/98` ⇒ 把它的**在册身份**当事实引用
     （迁移来源行、SCI-C 主题的 docs 侧件）；
   - `reports/v19r2/*.csv` 两行 ⇒ 台账登记（`source_manifest.csv` 里状态列就是 `MISSING`）。
   ⇒ **9 个位点里没有一个把"骨架"当骨架引用**：全部把它当证据位/产物落点/在册件——这正是"占位即占位者之名"的病灶。
5. **是否被某条门当在册文档计入分母**：**没有。**
   `eng/tools/doccheck/check_doc_index.py:97-100` 扫描面**显式排除 `实验/`**（注释写明"排除面由 reverse_verify/
   等量替换为 实验/"）⇒ DOC-INDEX 不计它；`check_doc_hygiene.py:67-69` 正式文档面 = `docs/` + 4 份根规范，
   且 `:74-75` `REF_SCAN_SKIP` 含 `"实验/"` ⇒ 写法门既不计它也不查对它的引用；
   `eng/ci/check_doc_unverified_cite.py:53` 与 `check_cite_claim_mark_consistency.py:46` 的
   `DEFAULT_ROOTS` **含 `实验`** ⇒ 它在这两条门的**被扫文件集**里，但该件书目标 **0 条**
   （`grep -c "\[CR\]" → 0`），且两门不出版任何分母/覆盖率读数（`grep "scanned|n_docs|ratio|coverage|total"` → 0 命中）
   ⇒ **删除它不会让任何门的读数变化**；会变的只有一处：**`CHK-DANGLING`（`waivable:false`）**
   ——第 4 点那 9 个引用位点若不同步改，删文件即造悬空引用判红。

**与成稿差异**：成稿主张（§3–§9 占位、5/7 结论行 X/Y/N、4 处指向、未回填）全部独立复现 ⇒ 确认；
差异三处——① 路径要展开到 `实验/additive-sky-seamless/docs/…`；② 引用面是 **6 件 9 位点**不是 4 处；
③ 补第 3 点（结论速览的证据指针 100% 落进空节），这条把它的性质从"未完成的骨架"推进到"看起来给了结论的速览表"。

**处置建议（我的取舍与理由）**：**就地降级身份 + 保留设计内容，不删、不"由生成器补齐"作为当前动作**。
- 不删：`:30–99`（§1 问题与既有事实 + §2 实验设计：物理噪声链、真实 L4 拓扑复用 277234 obs/33472 control/523 tile/49 帧、
  9 场景矩阵含 3 个负例、λs 9 点网格与生产标志）是 λs 判据**先于结果**登记（`:7` 指 `…/smooth_lambda/CRITERIA.md`）的唯一成文件，
  删则设计正本丢失，且要同步 9 个引用位点（含 2 份 `reports/v19r2` 台账行）。
- 不"由生成器补齐"作为当前动作：`report.py` 的输入全在非跟踪面（`run/RELEASE-02/smooth-lambda/`、
  `run/reverse_verify/smooth_lambda/`，见 `:145-156` 复跑方法），补齐需重跑 g++ 编译 + 9 场景 × 9 λs + 49 帧真实数据
  ⇒ 与现行"测试与端到端暂停"裁决冲突，且重跑值须走前台独立验证——不是文档整治能顺带做的事。
- 具体改法（P2 可执行）：标题后加一行身份声明（"本件现只含**设计与判据登记**；§3–§9 未回填，不作证据"）；
  7 个 `{{...}}` 各改为 `（未回填：见 REPORT_paper.md:539 局限 19 / REVERSE_VERIFY_CANON.md 登记）`；
  §0 表 `:15–21` 的"关键数值"列把 `见 §x` 改为 `见 §x（未回填）`，`:20` 推荐值行改为"未定案"；
  4 处引用串改仓库根相对路径。
- 分母核对结论要写进整治单：**删除不会动任何门读数，只会红 `CHK-DANGLING`**（依据：`check_doc_index.py:100` 排除 `实验/`、
  `check_doc_hygiene.py:74-75` 跳过 `实验/`、两条引用门无分母读数）。

**定级建议**：**P2**。理由：下游未据其作主张（`REPORT_paper.md:373/539` 已自行排除出证据面，`source_manifest.csv` 标 `MISSING`）；
但若 §0 结论速览的任何一行被上呈件或索引复读，立即升 **P1**（那时它就是"无证据支撑的结论"）。

**缺什么证据**：`{{TABLES}}`… 的原始填充物是否曾存在（`run/RELEASE-02/smooth-lambda/` 本机残留不作数）；
以及 `REVERSE_VERIFY_CANON.md` 所称"从未回填"是否有对应轮次的日志可反证（跟踪集里没有）。

---

---

## 附：只读性自证与收工状态

- 复核全程未执行 `eng/**` 任何脚本、未构建、未跑 ctest/`run_checks.py`、未 `import` 仓库 Python、零 git 写、零 `git config`。
  自写脚本 3 份与输出 4 份全部在 `独立审计/复算件/results-v\`。
- R5 的"根级同名件不存在"补一条命令：`git ls-files -- docs/smooth-lambda.md | wc -l` → **0**。
- 收工 `git -C "F:\Astro dev\Astro CS Normalization Database" status --porcelain` 原文：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

  与开工原文（转义形式 `?? "ACSD\346\225\264…zip"` / `?? site/`）**同一两条**，`HEAD=c8f64e9a` 未动 ⇒ **与基线差异：无**。

---

## 进 P0/P1（只列"确认"）

- **P1｜R1**（确认）：`CHK-DOC-HYGIENE`（`waivable:false`）的 D3b 命名空间前缀表由 `run/` 与 `工程控制/` 的**目录名**派生
  （`check_doc_hygiene.py:390-403`），干净克隆上 `run/` 恒 0 前缀（只有 `.gitkeep`，`:398` 跳过）、唯一前缀 = `RELEASE`；
  按 `CONTROL_PACK_SPEC.md:210` 删掉最后一个控制包 ⇒ `:648-651` 零输入守卫 ⇒ **fail-closed 判红、门不可豁免**。
  发布流程阻塞项：清包必须与"下一个控制包入库"同提交；并把前缀表来源从目录名换成跟踪清单文件。
  （本条另有 P2 副面：剩 1 个包时 `D3B_ID_RE:349` 与唯一前缀 `RELEASE` 交集为 0 ⇒ 实测 92 处形态可匹配编号命中 0，静默零覆盖。）
- **P1｜R3②**（确认）：SCI-B 读噪双计的最坏缺陷量级，正式文档三处（`docs/science/NOISE_MODEL.md:324`、
  `docs/science/CONTROL_WEIGHT_SNR.md:201`、`docs/plugins/algorithms_phase1/07_noise_snr.md:123`）写 +34.0%（含"最坏点"字样），
  实验与任务书四处写 +36.6%；机器实值 `b1_sky_scan.json:gates_faint.G4c_empirical_rn_max_rel_dev=0.3661993`（全证据面最坏，B=0 暗源）、
  `b2_noise_terms.json:gates.G4c_max_bias_over_all_points=0.3395416`（仅 RN 扫描面）。现行正确值 = **+36.6%**，正式文档低估缺陷量级。
- **P1｜R4**（确认，按"进对外主张"分档）：`实验/additive-sky-seamless/README.md` 三处字面量与自家 JSON 发散，
  其中 §9 验收门表 `:369` 的"PSD k=4（预期 5.33±2）"是对外完成主张的读数（机器值 k=26／预期 21.33，同文 `:151` 已写对）；
  `:211` RSS 与 `:151/289` amp 属描述性数字（不翻转 `E3_rss_scales_with_block` 等门，正确值 `REPORT_paper.md:265` 已给）。
  配套缺陷：`eng/tools/doccheck/doc_fact_authority.json` 仅登记 **1** 条事实 ⇒ 写法门 D4b 对这类跨件同名量**零覆盖**。
- **P2（登记，不进 P1）｜R2**：`EXP-07-POLAR.md` 三处值不可回溯（`:522`/`:298` 的样本面 `441`——跟踪探针 `p4_hst.cpp:87` 只有 2px/121；
  `:298` 的 `6.96e-14`——跟踪集唯一同尾数是 `t17_extent.csv:173` 的 **oracle** 列 6.937989e-14；`:553` 的 `1.18e-13`——
  量级家族只存在于别的口径 `p1_t7.out:6`、`p9_review.out:47` 的复刻偏差），且 `:298` 与 `:522` 同实验两值自相矛盾；
  第 4 处"leafmax 中位 2.14e-08"**推翻**（我按 `t4_pole_full.csv` 1089 行复算：max 3.159e-07、median 2.14e-08 逐位一致）。
  `SNAPSHOT.sha256` 47/47 我自己重算哈希通过，快照覆盖 = code+results 全量（5 个未钉件均为 md/README/run_all.sh/快照自身）。
- **P2（登记）｜R3①**：M42 帧级 σ 两表差 4.02×（`EXP06_TABLES.md:242` 14.782 ↔ `EXP05_TABLES.md:35` 59.4153），
  两表均不写窗口/是否去结构（口径在 `exp0*_e3_real.json:/meta/crop` 与 `docs/EXP-05-ABSOLUTE-SNR.md:363`、
  `docs/EXP-06-SNR-PHYS.md:319`，且 4× 分解已由 `exp05_e6_mechanism.json` 自证：raw 59.4153 → struct_removed 17.4041，boost 3.4139）
  ⇒ 属口径未声明，非取数错；修法是同址标注 + 分名。
- **P2（登记）｜R3③ 与 R5**：healpix 摘要同页 1.12e-8（T9，49 配置）与 2.79e-8（T13-N4，1089 配置）无口径说明，
  且 `EXP-07-POLAR.md:615` 一句里混用两个样本面（精度取 1089、代价取 49）；
  `实验/additive-sky-seamless/docs/smooth-lambda.md` §3–§9 七处 `{{}}` 未填、§0 结论表 5/7 行是 X/Y/N 且 7/7 行证据指针落进空节，
  被 6 件 9 位点按"在册件/证据位"引用；不计入任何门分母，删除只红 `CHK-DANGLING`。

## R1 的方向性结论（一句话）

**清包会让门红**——不是失明：`check_doc_hygiene.py:648-651` 的零输入守卫在 `prefixes_now` 为空时判
`D3b_namespace_table_empty`、`:1130-1145` 转 exit 1、`checks.json` 里该门 `waivable:false`，
而干净克隆上前缀表唯一来源是 `工程控制/<包名>`（`run/` 只有 `.gitkeep`，`:398` 跳过）⇒
删掉最后一个控制包（`CONTROL_PACK_SPEC.md:210` 强制的收口动作）必红；
"失明"发生在**还剩 1 个包**时（唯一前缀 `RELEASE` 与 `D3B_ID_RE:349` 的形态交集为 0，实测 92 处可匹配编号命中 0），那是另一处、同级不同的缺陷。

<!-- PROGRESS: 5/5 -->



