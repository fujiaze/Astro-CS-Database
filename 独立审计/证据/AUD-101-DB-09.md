# AUD-101 · DB-09 文档审计台账（AUDIT-06 只读整治 · 独立审查节点）

范围：`inventory/DB-09.txt` 7 份 —— 对外接口合同面（`docs/contracts/PUBLIC_API.md`、`RT-001.md`、`SCHEDULER_CONTRACT.md`、`TEST_MATRIX.md`、`UNIFIED_OBJECTS.md`、`config_separation_anchors.json`、`unified_object_registry.json`）。
冻结基线 HEAD=`c8f64e9a`。

判据：标准 01 §1–§5（角色表 §2、双向索引 §3、写法 §4、产出 §5）；上位正本 `ASTROCS_DESIGN.md`；命名判定唯一源 `ENGINEERING_SPEC.md §15`。本批特有关注点 = **合同登记的符号/签名/头文件路径与实现真实声明逐条配对** + **挂的测试编号是否真在册**，全部靠读文件比对，未执行任何检查器、构建或测试。

## 0 开工基线（`git status --porcelain` 原文）

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与派单预期一致：恰此两项。收工基线见文末「覆盖率自报」。

## 1 本批共用上位事实（后续判定只引用不重述）

- **命名判定唯一源 = `ENGINEERING_SPEC.md §15`**（246–271 行）：显示名唯一为 `ACSD`；`astrocs`/`AstroCS` 只在 §15 表 1–10 类保留面内合法（类 1 include/符号/`astrocs_*.dll`、类 2 `x-astrocs*` 与 `astrocs.*`/`astrocs-*`/`astrocs/<x>/vN`、类 3 `ASTROCS_*`/`ASTROCS_DESIGN.md`、类 4 target/CLI 名、类 9 `User-Agent`）。本批 7 份里的 `namespace astrocs::core`、`astrocs::healpix`、`astrocs_host_services_v1`、`astrocs.config-separation-anchors/v1`、`https://astrocs.local/schemas/...`、`ASTROCS_REPO`、`ASTROCS_DRIZZLE_TRACE`、`astrocs_phase2`、`astrocs_p1_*.dll`、`astrocs.adaptive_pixel` 全部落在这 10 类内 ⇒ **一律不判为漏改**，不据此要求改名。
- **ASTROCS_DESIGN.md:3**「合同的文档化说明在 `docs/contracts/`，机器校验的 schema 在 `eng/contracts/`，两者双向对应」→ 本批 7 份目录落位**全部正确**，无角色错放到目录级别；但 `docs/contracts/` 与 `eng/contracts/` 是**两面**，任何一方自封「唯一事实源」都触发标准 01 §1「任何文档不声明自己的第二套权威顺序或'唯一口径'」。
- **章节实名**（用于判上游标注）：`ASTROCS_DESIGN.md` §2=「核心科学方法：创新点」、§3=「数据对象与配置」、§3.1=「数据对象」、§3.2=「三类配置」、§7=「CLI 合同」、§8.1=「总原则：唯一 CLI 入口、阶段独立调度器」、§8.3=「三个阶段调度器」、§8.4=「顶层结构」、§8.5=「模块与 ABI」、§9=「CPU 后端与资源」、§12.4=「验证层级与四层验收」。
- **§3.1:223 逐字列出 13 个对象**（signal/variance/ivar/source_snr/depth_m5/frame_snr/point_information/sparse_snr_layer/support/coverage/validity/rejection/provenance），与 07 号 registry、05 号 md 的 13 对象**全等**；`:229` 才是「权重的产生链固定为两步、没有可选择项」的所在行。
- **锚存活口径**：`ENGINEERING_SPEC.md:162`「悬空即缺陷：…文档引用的代码路径必须真实」、`:182`「锚存活…失效时报 `ANCHOR_STALE`」。（`PUBLIC_API.md:289` 把这两条自引成 `:129`/`:133`，那两行实为目录清单条目 —— 订正段自身的锚即错，见 01 号。）

## 2 与机械列（`doc_mechanical.csv`）不符处（本批 7 份逐条校准）

| 列 | 机械判定 | 实读 | 结论 |
|---|---|---|---|
| `meta_block` | 7 份全 `False` | 03 号 `SCHEDULER_CONTRACT.md:6`「`> ID: CONTRACT-501-SCHED  状态: FROZEN  机器 schema: …`」、02 号 `RT-001.md:5-6`「`> 上游: ARCH-001, API-001, DATA-001  下游: RT-002..RT-009 …`＋冻结点声明」、04 号 `TEST_MATRIX.md:5-6` 同形态 —— 三处都在开头 8 行内 | **系统性漏报 3/7**：该列只认 YAML frontmatter，不认引用块式 `> ID:/状态:/下游:` 元信息 |
| `task_ids` | 01 号 `[]`、02 号 `[]`、04 号 `['FIX-406']`、05 号 `[]` | 01 号含 `CLI-001`(92,93)、`CHK-PROD-WIRING`(5)、`WIRING-W16-01`(33)、`CAT-GAIA-DOC`(106)、`P1-CAL-DOC`(137)、`B2-A2/RESCUE-P0-05`(634)、`P1-001`(772)、`W4-A9`(1981)、`SA-P1-S15`(899)、`DOC-203`(2188)、`CHG-2026-09-22-V6-CONTRACT-MERGE`(2163)；02 号含 `RT-002..RT-009`、`CPU-001..004`；04 号含 `DOC-004`、`P3-006`、`CPU-006`、`TEST-001`；05 号含 `DATA-001`、`MOD-001`、`DOC-203`、`GAP_AUDIT` | **全部漏报**：01 号一条未捕获，04/05 号各漏 3–4 类 |
| `commit_sha` | 01 号 `['ac04289d']`；两份 JSON 各 `['ecf6ad6f']`；07 号另含 `808e7a8d` | 实读一致；但 07 号的 `ecf6ad6f` 出现在 :6 与 :307/:351（"基线 INDEX.yaml（ecf6ad6f）"）三处，机械列只报一次 | 计数口径为"存在性"而非"逐处"，不判误报 |
| `dangling_links` | 01 号 17 行、05 号 4 行，其余 `[]` | 01 号真悬空另有 4 处机械列未报：`run/WIRING-W16-01/REPORT.md`(33)、`eng/tests/unit/p3_wcs_test.cpp`(2056)、`eng/tests/backend/p3_wcs_main.cpp`(2057)、`eng/tests/backend/p3_resample_probe_main.cpp`(2058)；07 号机械列 `[]` 但 :647 的 JSON 指针 `#port_contract` 不存在 | **一漏四（01）+ 一漏一（07）** |
| `unregistered_index` | 01 号报 3 个 glob 路径 | glob 三项（`stage1_*.json`/`stage2_*.json`/`unified/*.schema.json`）展开后全部实存，不属"未登记"；真未登记的是 01 号自标的 6 个 API 条款 ID 不在 `docs/contracts/INDEX.yaml` | **误报 3 + 漏报 6** |
| `history_narrative` | 05 号 `[12,99,154,155]`、01 号 `True` | 07 号 JSON 同样密集历史叙事（:265「零命中（旧契约面无定义）」、:553-555「retire_note…不得作为退役条件」、:462「GAP_AUDIT §9.68（2026-09-20）改写为…」）与 06 号（:207「历史登记」）机械列均 `False` | **漏报 06/07 两份** |
| `lines`/`h1` | 7 份与实读一致（2210/57/72/66/165/215/681）；06/07 号无 h1（JSON）故 `h1` 空 | 一致 | 无偏差 |

---

## 3 逐份登记

字段名与派单 §4 的 14 项一一对应（`路径`/`标题`/`行数`/`角色`/`主题`/`上游`/`下游`/`是否正本`/`重复或重叠对象`/`元信息块`/`写法违规`/`悬空引用`/`与上位冲突`/`处置建议`），全部以标签行给出，按标签而非位置合并。**两处对派单顺序的偏离与一处增设，登记在此以便合并方核对**：(a) 各份内 `悬空引用` 段排在 `写法违规` 段之前（与派单列序相反）；(b) 目录落位（角色是否错放）单列为 `目录落位` 行，未并入 `角色`；(c) 按本批特有关注点增设 `符号配对（三态）`（01/02 号）与 `测试编号在册性（三态）`（04 号）两段，位置在 `悬空引用` 之后、`与上位冲突` 之前。

### 01 · docs/contracts/PUBLIC_API.md

- 路径：`docs/contracts/PUBLIC_API.md`｜标题：`Astro Celestial Sphere Database（ACSD） Public / Internal API`｜行数：**2210**（29 个 `##` 节 / 83 个 `###` 子节）｜角色：**合同说明**
- 目录落位：在 `docs/contracts/`（= 应属目录）→ **一致，无角色错放**。
- 主题：全部对外 C ABI / C++ API / CLI 入口 / On-disk 格式的**符号级冻结合同**（每节一条款 ID：`API-GAIA-001` … `API-P3-RSMP-001` + V6 消费面）。
- 上游：`:3`「ASTROCS_DESIGN.md §7（CLI 合同）、§8.4（模块与 ABI）」——§7 名对得上；**§8.4 实为「顶层结构」，「模块与 ABI」是 §8.5** ⇒ 上游条款号与条款名错配一处（同文件 `:8` 又写「§8.4/§8.5」，两说并存）。
- 下游（命令 `git grep -l -F "docs/contracts/PUBLIC_API.md" | grep -v "docs/contracts/PUBLIC_API.md$"`）：**106 个引用者**（含根规范、全部 `docs/modules/registry/*`、`eng/contracts/schemas/**`、`lib/**`）。非孤。
- 是否正本：**是** —— ACSD 唯一 API/ABI 符号合同面。与 `docs/contracts/API-001.md`、`docs/contracts/API_CONTRACTS.csv`、`docs/architecture/api_inventory.csv` 部分重叠（见下条），但后三者是机器投影/档案，不承载签名语义。
- 重复或重叠对象（贴两段）：
  1. **↔ `docs/contracts/API-001.md:7`** —— API-001：「公共 C++ API 只在同一 toolchain 边界；CPU DLL/so 使用 C ABI；exception 不跨 ABI。」｜本文 `:40`：「## C ABI（`extern "C"`，不跨边界抛 C++ exception）」+ `:78-79`「`extern "C"` 边界全部捕获并转换为返回码」。重叠面 = ABI 边界与异常不外跨这一条硬规则，同目录两处承载，另 RT-001.md:49 第三次承载（三处措辞各不相同，无相互引用）。差异 = 本文把它展开成 ownership/nullable/单位逐参数要求，API-001 只给分层表。
  2. **↔ 同文件 §「Phase2 UPM」内部矛盾**（同主题、同文件、互相否定）——`:21-22`：「`p2_upm_normalized_weights` 五条零消费者声明**已删**」；`:1806-1811`：「`int p2_upm_normalized_weights(const P2ControlObservation* obs, …)`（h:139-142，实现 upm.cpp:1325）」并计入「既有 16 导出符号」。这不是分层，是同一份合同对同一符号一次说删、一次说冻结。
- 元信息块：文档开头无（`:1-3` 即标题+上游，合规）；但 22 个条款节各自带 `> ID: …  状态: CONTRACT_READY（<任务号> 冻结，<日期>）` 块（`:106,137,185,247,326,404,479,582,702,814,908,1033,1166,1278,1352,1488,1611,1730,1888,1975,2068`），标准 01 §4 明禁「文档 ID、状态编号、版本锚」。机械列 `meta_block=False` 只看了文件头。
- **符号配对结论（本批核心产出；三态 + `路径:行`）**
  - **A 合同超前（登记符号在实现里不存在）—— 1 例**
    `p2_upm_normalized_weights`：本文 `:1806-1811` 登记为 16 导出之一。实核：`lib/algorithms/coverage/include/astro/phase2/upm.h:181` =「`/* RETIRED 2026-09-25 (CHK-PROD-WIRING W1): p2_upm_normalized_weights 全仓零消费者声明已撤下 … */`」，`lib/algorithms/coverage/src/upm.cpp:1999` 同口径「定义已删」；`grep -nE "^P2_API" upm.h` 无该符号。⇒ **幻影符号**，且其实现锚 `upm.cpp:1325` 现指向另一函数。
  - **B 合同滞后（实现有、合同无）—— 9 个节、共 58 个导出符号未登记**（命令：对各头 `grep -cE "^<EXPORT_MACRO>"` 与本文登记数比对）
    | 节 | 头文件 | 头内导出数 | 本文登记 | 未登记符号（头:行） |
    |---|---|---|---|---|
    | API-CAL-001（`:143`）| `lib/algorithms/calibration/include/astro_calibration.h` | 18 | 12 | `ac_correct_columns:161`、`ac_detect_bad_columns_from_master:184`、`ac_correct_columns_ex:207`、`ac_correct_columns_ex2:248`、`ac_column_variance_inflate:311`、`ac_correct_columns_f64:365` |
    | API-DRZ-001（`:258`）| `lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.h` | 8 | 7 | `hp_drizzle_compute_auto_nside:131` |
    | API-HIPS-001（`:339`）| `lib/infrastructure/aio/include/aio_hips.h` | 13 | 9 | `aio_hips_write_diag_tile:211`、`aio_hips_set_provenance:240`、`aio_hips_verify_product_set:287`、`aio_hips_set_frame_snr:316` |
    | API-NOISE-001（`:422`）| `lib/algorithms/noise_snr/cpp/include/snr_estimator.h` | 27 | 9（边界段 `:416-420` 另点名 5 个：`snr_phot_cal_quality`、`snr_psf_fit_quality`、`snr_extract_model{,_v2,_v3}`）| 13 个既未登记也不在点名内：`snr_free_model`/`_v2`/`_v3`、`snr_frame_depth_f64`、`snr_source_snr_f64`、`snr_moffat4_profile_f64`、`snr_calib_zero_point_standard_error`、`snr_noise_model_v1_abi_check_config`/`_abi_check_model`/`_abi_stamp_config`/`_abi_stamp_model`、`snr_noise_model_v1_bind_variance_floor`、`snr_noise_model_v1_floor_clamp_count` |
    | API-STAR-001（`:829`）| `lib/algorithms/star_detection/include/star_detector.h` | 10 `SDET_EXPORT` | 9 | `sdet_detect_guided_ex_f64:95` |
    | API-COV-001（`:1061`）| `lib/algorithms/coverage/include/astro/phase2/coverage.h` | 6 | 2（且自称「2 个全部当前真实存在」）| `p2_coverage_support_classify:129`、`p2_tile_boundary_owner:142`、`p2_deterministic_reduction_order:149`、`p2_weight_source_token_reject:166` |
    | API-P2-REJ-001（`:1370`）| `.../phase2/rejection.h` | 15 | 7 | `p2_rejection_percentile_band_min_n:287`、`p2_reject_plan_resolve_n:299`、`p2_rejection_applicability:309`、`p2_rejection_class_id:511`、`p2_reject_plan_thresholds_inherited:515`、`p2_rejection_weight_surface_guard:521`、`p2_reject_classify:569`、`p2_reject_calibration:596` |
    | API-P2-SMP-001（`:1506`）| `.../phase2/sampler.h` | 9 | 6 | `p2_spatial_model_eval:209`、`p2_spatial_model_summary:229`、`p2_scalar_degrade_gate:266` |
    | API-P2-UPM-001（`:1733,1748,1865`）| `.../phase2/upm.h` | 28 个不同符号（29 行 `^P2_API`，`p2_upm_materialize_dense` 重复声明 :194/:213）| 16（含 1 幻影 ⇒ 实际 15 真）| `p2_upm_convergence:151`、`p2_upm_identifiability:425`、`p2_upm_warnings_json:434`、`p2_upm_control_variance:443`、`p2_upm_ma_build:340`、`_ma_info:344`、`_ma_solution:348`、`_ma_component_of_frame:353`、`_ma_component_ref_frame:357`、`_ma_param_cov:361`、`_ma_c_out:366`、`_ma_provenance:376`、`_ma_close:436` |
    | 汇总（上表 9 节）| | **134** | **77（含 1 幻影 ⇒ 76 真）** | **58 未登记**（其中 5 个被 API-NOISE-001 边界段点名排除 ⇒ 53 个完全无登记；含 3 个整族：坏列 6、`p2_upm_ma_*` 9、UPM 观测面 4）|
    全文 19 个符号节总账：头内导出 **197**、本文登记 **140**（含 1 幻影 ⇒ 139 真）、未登记 **58**；其余 10 节 1:1 全等（见 C 类）。
    `:1865` 的负向条款「upm.h **既有 16 符号声明**与本节为同一 ABI 的展开冻结」是**可判伪断言**，实测 28。
  - **C 一致（符号面逐条核过，签名也核）**
    `gaia_client.h` 12/12（:82-131）；`photometric_calib.h` 6/6（:106/158/191/207/233/253）；`dynamic_psf.h` 7/7（:44/49/59/64/122/160/200）；`ipv_api.h` 12/12；`integrate.h` 2/2（:74/78）；`p3_output.h` 2/2（:58/70）；`p3_wcs.h` 4/4（:47/54/58/62）；`p3_resample.h` 8/8（:21/28/35/40/48/88/97/100）；`p1_session.h` 5 C API + `last_error`（`:720-725` 登记的 :16/19/23/26/28/36 **与本批唯一逐锚全对的一节**）；`p2_session.h` 5/5（:17/20/23/25/27，`:1633-1665` 锚全对）；`astrocs::healpix`（`lib/algorithms/shared/healpix/healpix_core.h:20-21` 嵌套声明，`ang2pix_nest:26`/`pix2ang_nest:31`/`nested_local↔FITS index:47/49`）✓；`astrocs::crypto`（`sha256.h:9`）✓；`astrocs_host_services_v1` 四通道（`lib/include/astrocs/common_abi_v1.h:110-117`，与 `:733`、`:1675` 的锚**逐字一致**）✓；`acs_handle`/`acs_span_u8`/`ACS_ERR_ABI_MISMATCH|UNSUPPORTED|CANCELLED|STATE|BUDGET`（同头 :37/:52/:55-67）✓。
    签名一致性抽核通过：`ac_correct_frame` 12 参数（`:200-203` 文本 ≡ `astro_calibration.h:124-130`）、`hp_drizzle_run_phase1_hips`、`p2_integrate_pixel`、`p3_wcs_make` 形参序与本文登记式一致。
  - **D 锚漂移系统性（签名对、行号与行数错）—— 除 `p1_session.h`/`p2_session.h` 两节外，其余 17 节全部漂移**
    `:480` 称 `photometric_calib.h`「275 行」实 277；`:505-510` 六锚全偏（+3…+6）。`:327` 九导出锚「:104,121,…,177」实 :157…:378。`:249` 「六导出 :42,62,70,130,139,140」实 :58…:197，且与 `:258`「导出符号（7 个）」自相矛盾。`:422-429` snr 锚「:115…:218」实 :158…:416。`:817` 称 `star_detector.h:1-73` 实 110 行、`:833-841` 九锚全偏。`:910` 称 `ipv_api.h`「238 行」实 315。`:1360` 称 `rejection.h`「595 行」实 604。`:1496` 称 `sampler.h`「288 行」实 273。`:1034` 称 `coverage.h`「59 行」实 171。`:1286` 称 `integrate.h`「74 行」实 83。**同一节内两处行数互斥**：`:1734`「upm.h … 184 行」vs `:1737`「upm.h（384 行）」，实 450。`:1978` 称 `p3_wcs.h`「50 行」实 373（`:1995-2024` 的 h:31-46 锚实为 :47-62）。`:2071` 称 `p3_resample.h`「58 行」vs `:2075`「201 行」，实 202。
- 悬空引用（逐条 + 确认命令，均在 `cd "F:/Astro dev/Astro CS Normalization Database"` 下）
  1. `:33` `run/WIRING-W16-01/REPORT.md` —— `git ls-files run/` → 仅 `run/.gitkeep`；`test -e run/WIRING-W16-01` 假。**W1 口径条款的"逐条抽样复核依据"落在可回收的 gitignore 临时区**，与 AGENTS.md §7 冲突。
  2. `:2056` `eng/tests/unit/p3_wcs_test.cpp`（并称「474 行」）—— 该路径不存在；实体在 `lib/algorithms/projection/tests/p3wcs/p3_wcs_test.cpp`（实测确为 474 行）。
  3. `:2057` `eng/tests/backend/p3_wcs_main.cpp`、`:2058` `eng/tests/backend/p3_resample_probe_main.cpp` —— 均不存在；实体在 `lib/algorithms/projection/tests/p3wcs/` 与 `lib/algorithms/resample/tests/p3rsmp/`（命令：`git ls-files | grep -E "p3_wcs_main|p3_resample_probe"`）。
  4. `:102` `CONFIG_SCHEMA.md`（无目录限定）—— 根目录无该文件；`git ls-files | grep -i config` 唯一命中 `docs/development/CONFIG_SCHEMA.md`（存在）。**不是悬空，但合同页用裸文件名指跨目录正本**，同族 `lib/algorithms/coverage/include/astro/phase2/stage2_common.h:57` 亦引「`CONFIG_SCHEMA.md:3-4`」。
  5. `:289` 自证依据「`ENGINEERING_SPEC.md:129`（锚存活）/ `:133`（无悬空引用）」—— 实读 :129 是「eng/packaging/config/」目录条目、:133 是「artifacts/」条目；两条规则真身在 `:182` 与 `:162`。
- 写法违规（标准 01 §4，逐条行号 + 片段）
  - 日期：`:106`「（CAT-GAIA-DOC 冻结，2026-09-05）」、`:137`、`:185`、`:247`、`:326`、`:404`、`:479`、`:582`、`:702`、`:814`、`:908`、`:1033`、`:1166`、`:1278`、`:1352`、`:1488`、`:1612`「2026-09-10，SA-P2-X24」、`:1730`、`:1888`、`:1975`、`:2068`、**`:286`「（**2026-09-20 订正**：原文锚…）」**、`:310`「**该登记已作废（2026-09-20）**」、`:2163`、`:2190`「变更 claim `CHG-2026-09-20-PSFSW-RETIRE`」。
  - 任务/门/发布编号：`:5` `CHK-PROD-WIRING`、`:33` `WIRING-W16-01`、`:92-93` `CLI-001`、`:177` `RULING-DOC-01`、`:634` `B2-A2（RESCUE-P0-05）`、`:772,781` `P1-001（2026-09-10）`、`:899` `SA-P1-S15`、`:1981` `W4-A9 批次 1`、`:2033` `FIX-406`、`:2163` `SCHEMA-INTEGRATE-001/W6`、`:2188` `DOC-203`、`:2191` `GAP_AUDIT §4.5 C01`、`:2199-2204` `C-004.1/C-004.2/AR-048/SO-01..SO-07`。
  - commit sha：`:2207`「不含被 `ac04289d` 回退的帧级单一 SNR 系数段」。
  - 历史叙事：`:310`「该登记已作废」、`:2163`「V6 合同层已按 … 整体出库，条款去向见 …」、`:2175`「**已按 §9.73 A44 作废**（退役对象 … 的声明 token）」、`:2179`「曾为整数 0/1/2，**该字段已…删除**」、`:2186`「A44 式留痕」、`:2192`「以下原文只作**历史留痕**」、`:2194`「随对象退役」、`:2201`「（**随 `psfsw_robust_weight` 对象退役**…DOC-203 订正）」、`:1206/:1213/:1214/:1339/:1436/:1468` 的「已按 §9.73 A44 作废：该概念不存在」括注群。**`:2163-2210` 整节是"作废留痕容器"**，与标准 01 §2「历史档案…不放在正式文档层」直接冲突。
  - 其他：`:92-93` 两行都以「当前生产入口 =」起头，并列两条"当前"，且行尾「））」重复括号；`§工具/CLI` 只登记 `mosaic`/`normalize` 两命令，**缺 `export`**（三命令在 `lib/infrastructure/cli/commands.cpp:2424-2426` 全部在册）。
- 与上位冲突
  1. `:242-243`「详见 `docs/architecture/api_inventory.csv`（API 机器单源清单，与 `check_api_contracts` 的 `API_CONTRACTS.csv` **一致**）」—— 实读两表 symbol 集合为 444 与 378，后者是前者真子集、差 66 个；「一致」无判据也无定义，指针对象存在但关系陈述不成立。
  2. `:1213`/`:2170-2186` 的权重键判据：「`weight_mode` 键不存在；凡出现在『配置读取/路由/解析』路径上的 legacy 权重域 token（… `weight_mode` 键 …）一律 fail-closed 具名拒绝」。实核解析路径：`lib/algorithms/coverage/src/stage2_common.cpp:193` `cfg->sky_plane_weight_mode = sp.value("weight_mode", …)` **仍接受 `weight_mode` 键**（`stage2_common.h:93` 字段在册），`:164-166` 仍解析 `snr_weight_mode` 键。`integration.weight_mode` 确按 `:447-448` 拒绝 ⇒ 合同的"全接受面为空"陈述只对一节成立。
  3. `:2208`「UPM fit 的 `upm_weight_source`（原 `snr_weight_mode`…）」—— `upm_weight_source` 在 `lib/**` **零命中**（`git grep -l -w upm_weight_source -- lib`），旧名 `snr_weight_mode` 仍是结构体字段与 JSON 键。登记名不存在、被替换名未退役。
  4. `:1187-1233` astrocs-stage2 面：`:1189` 称 usage 在 `stage2.cpp:132`（实 `:140`）、`:1168` 称 main 在 `:112`（实 `:120`）、`:1233` 称 diagnostics 键集在 `:1697-1749`（该文件 2015 行，diagnostics.json 落盘点在 `:2001`）。
  5. `:94-95` `healpix_browser_qt.exe（--hips/--standard-hips/--view/--screenshot/--lod/--exit）` —— `--standard-hips`、`--lod` 在 `lib/infrastructure/hips_browser/**` 零命中；实存选项面为 `--hips --view --screenshot --exit --layer --nside --size --help --preset --sim --diag --frames --queries --refrender --dataset-stats --benchmark --soak --stf-*`（`git grep -o -E "\-\-[a-z][a-z-]*" -- lib/infrastructure/hips_browser/healpix_browser_qt`）。
  6. `:23`「『仓内零消费者且本合同未列名』的符号只能走接线或撤下声明」—— 与本节 B 类 58 个"有实现、合同未列名、多数有仓内消费者"的导出符号同时成立，说明该条出口条件未被本文自己执行。
- 处置建议：**保留**（本主题唯一正本，不可删并），但按 A/B/C/D/E 五类逐条订正；`§V6 消费面`（`:2163-2210`）整节按标准 01 §2「历史台账落 `artifacts/evidence/` 并由正本引用」迁出正式文档层，正文只留现行拒绝条款一句 + 指向证据锚。理由条款：标准 01 §4「正文无日期、无任务编号、无 commit、无 sha256；无旧版/作废/留痕/曾」+ §1「下位文档与上位文档冲突时以上位为准；冲突本身是缺陷」。

<!-- PROGRESS: 已审份数 1/7 -->

### 02 · docs/contracts/RT-001.md

- 路径：`docs/contracts/RT-001.md`｜标题：`RT-001 — Runtime 与模块公共合同（冻结）`｜行数：**57**｜角色：**合同说明**
- 目录落位：在 `docs/contracts/`（= 应属目录）→ **一致**。对象是 `lib/include/astrocs/core/*.h` 的 C++ 接口，标准 01 §2「架构设计」行也覆盖 `docs/interfaces/`，但它同时是头文件的文档化说明面，故按合同说明判，不迁。
- 主题：唯一生产 Runtime + IModule/ModuleDescriptor/ModulePlan + typed Port/DataArtifact + ThreadBudget/ThreadLease + RunContext 服务的接口冻结点。
- 上游：`:3`「ASTROCS_DESIGN.md §8.1（顶层结构）」——**条款号与条款名互换**：§8.1 = 「总原则：唯一 CLI 入口、阶段独立调度器」，「顶层结构」= §8.4。与 01 号的错配方向正好对称（01 号把 §8.4 说成"模块与 ABI"，真身 §8.5）。
- 下游（命令 `git grep -l -F "docs/contracts/RT-001.md" | grep -v "docs/contracts/RT-001.md$"`）：**7 个引用者**。非孤。
- 是否正本：**是**（`lib/include/astrocs/core/**` 四头的唯一合同说明；机器侧 `add_test(NAME rt001_abi …)` 在册）。
- 重复或重叠对象（贴两段）：
  1. **↔ `docs/contracts/ARCH-001.md:14-15`**（同目录，非下钻）——ARCH-001：「**只有 Pipeline Runtime 拥有全局执行顺序与资源预算**」「CLI、I/O、科学模块、计算后端的调度一律走唯一全局执行平面（Pipeline Runtime）」；本文 `:10-11`：「唯一生产 Runtime 的公共接口冻结点。CLI→Runtime→Registry→Module→CPU/I-O 唯一调用链；CLI 只经 Runtime 触达 session/科学内核；调度面只有一套；ACR 不注册不链接」。重叠面 = "唯一执行平面 + CLI 只能经 Runtime"这条结构不变量，两处各自陈述且互不引用。差异 = 本文多"ACR 不注册不链接"，ARCH-001 多组件图。
  2. **↔ `docs/contracts/API-001.md:7`**——API-001：「公共 C++ API 只在同一 toolchain 边界；CPU DLL/so 使用 C ABI；exception 不跨 ABI」；本文 `:49`「C ABI 不抛异常；C++ 边界返回 Result/Error」。同一 ABI 边界规则第三次承载（01 号 `:40` 为第一次），三处措辞不同、无相互指针。
  3. 上游链质量：本文 `:5` 自称「上游: ARCH-001, API-001, DATA-001」，而 `docs/contracts/API-001.md:3` 自标「本文件是**设计档案 / 产品族专用投影（非生产目标态）**」⇒ 一份 ACTIVE_NORMATIVE 合同的上游指向自认非目标态的档案。
- 元信息块：**有**（`:5-6` 两行）：`> 上游: ARCH-001, API-001, DATA-001  下游: RT-002..RT-009, CPU-001..004, P1/P2/P3` + `> 冻结点：… 任何改动须走新任务 + ABI layout 测试`。标题自带状态词「（冻结）」。机械列 `meta_block=False`。
- 悬空引用：**0**（逐条核过：`lib/include/astrocs/core/{runtime,module,artifact,context}.h` 全部在册；`:51` `eng/tests/unit/rt001_abi_test.cpp` 在册且 `eng/tests/unit/CMakeLists.txt:124,127` 注册为 `add_executable(rt001_abi_test …)` + `add_test(NAME rt001_abi …)`；`:56`「全部 core_* 测试」的 9 个 `eng/tests/unit/core_*.cpp` 在册；`ASTROCS_REPO` 在 `eng/tests/**` 实际使用，如 `test_p1004_joint_gate.py:132`）。
- **符号配对（三态）**
  - **一致**：`Runtime::load_pipeline`(`runtime.h:44`)、`run`(`:50`)、`cancel`(`:53`)、`inspect`(`:56`)、`node_statuses`(`:59`)；`:57` 负例「create_runtime(0) 返回 Error(RESOURCE)」≡ `runtime.h:102`/`:129` 注释；`PortDescriptor` 五字段逐字一致（`module.h:19-25`：name/data_schema_id/is_input/unit(UnitId)/coordinate(CoordinateFrame)）；`DataArtifactDescriptor` 十字段一致（`artifact.h:97-108`）；`ThreadLease` 可移动 RAII、析构/异常/取消自动归还、`size()>0`（`context.h:44-91`）；`:44` 登记的 RunContext 七方法全部在册（`context.h:186/191/192/218/220/229/225`）；`:45`「禁全局状态」≡ `context.h:175-176` 注释。
  - **合同超前（登记名不存在）—— 3 个**：`:29-30`「`IModule` 生命周期：describe→validate_config→plan→create→execute→inspect→destroy」。实核 `module.h:65-91` 的 IModule 成员为 `descriptor()`(`:70`)、`validate_config`(`:74`)、`plan`(`:78`)、`execute`(`:83`)、`inspect`(`:86`)、`last_manifest`(`:90`)。⇒ **`describe` 名称不存在**（真实名 `descriptor`）；**`create` 不是 IModule 成员**（只在 `ModuleRegistry::create` `module.h:107`）；**`destroy` 在 `lib/include/astrocs/**` 中零命中**（`git grep -n -E "\bdestroy\s*\(" -- lib/include` 只命中 `abi/lifecycle_v1.h` 的状态机注释与 `core/block_frame.h:132` 的另一语义），生命周期由 `unique_ptr` 承担。
  - **合同滞后（实现有、合同无）—— 4 处**：`Runtime` 另有 5 个纯虚方法未登记（`node_manifests:63`、`node_trace:84`、`trace_jsonl:87`、`trace_violations:89`、`set_run_id:91`，其中 4 个是 RT-006/RT-008/RT-009 的运行时观测面）；`IModule::last_manifest`(`module.h:90`)、`ModulePlan::min_workers/max_workers`(`module.h:58-59`)、`DataArtifactDescriptor::schema_version`(`artifact.h:100`) 未登记；`ThreadBudget::acquire` 真实签名三参 `acquire(min,max,AcquirePolicy=NONBLOCK)`(`context.h:129-130`)，`:39` 只登记两参且未登记 `AcquirePolicy`(`:112-116`) —— BLOCK 档**会阻塞**，与本文「阻塞」列口径无对应；`:30` 说「execute 只用 Runtime 提供的 lease」而租约入口 `RunContext::acquire_lease`(`context.h:214`) 未登记，`GrantedWorkerObservation`(`:99`)、`set_dispatch_budget_hint`(`:163`) 亦未登记。
- 写法违规：无日期、无 commit、无 sha256；但 `:5` 的"下游"写成编号区间（`RT-002..RT-009`、`CPU-001..004`）不可核，`:6`「任何改动须走新任务 + ABI layout 测试」为纪律声明式（标准 01 §4）。标题含任务编号 `RT-001`。
- 与上位冲突：`:19` 单位/schema 列填「pipeline_ir schema v2」—— `eng/contracts/schemas/**` 33 个 schema 中**无 pipeline_ir**，全仓该字面只出现在 `runtime.h:41` 注释与本文（命令：`git grep -n -o -E "pipeline_ir[^ ,\")]*" -- eng/contracts lib/include docs/contracts/RT-001.md`），"v2" 无机器源。⇒ 与 `ENGINEERING_SPEC.md:51`「输入/输出端口引用有效 DATA 合同」的"引用必须有效"口径断链：合同登记的 schema 名解析不到对象。
- 处置建议：**保留**（`lib/include/astrocs/core/**` 的接口合同正本，无可替代），订正三处：`describe/create/destroy` 改真实名与真实归属、补 5 个 Runtime 方法与 `acquire` 的 policy 参数、把「pipeline_ir schema v2」落到真实 schema 或删该列值。理由条款：标准 01 §3「每份下层文档能沿上游标注回到最高设计对应条款」+ §1「冲突本身是缺陷」。注：本文 `astrocs::core`、`ASTROCS_REPO`、`core_*` 属 `ENGINEERING_SPEC §15` 类 1/3 保留面，**不判漏改**。

### 03 · docs/contracts/SCHEDULER_CONTRACT.md

- 路径：`docs/contracts/SCHEDULER_CONTRACT.md`｜标题：`三阶段调度器接口合同（CONTRACT-501 / RELEASE-05）`｜行数：**72**｜角色：**合同说明**（§4 探针 schema）+ **标准与检查**（§2.1 判据、§6 负例）→ 混合角色
- 目录落位：`docs/contracts/` 与主角色一致；§2.1 并行确定性判据的应属目录是 `docs/standards/`（与 04 号争同一正本）。
- 主题：三命令独立进程/独立调度器的统一 entrypoint、DAG 声明、资源声明、探针事件 schema、取消与原子性、负例。
- 上游：`:3-4`「ASTROCS_DESIGN.md §8.1（三命令独立进程、独立调度器）、§8.3（三个阶段调度器）、§9（探针驱动优化）」＋「依据：ENGINEERING_SPEC.md §4.1」——§8.1/§8.3 名实相符；**§9 实名「CPU 后端与资源」**，"探针驱动优化"是内容概括不是条款名（登记不判错）；`ENGINEERING_SPEC §4.1`「管线纪律」在册 ✓。
- 下游：**38 个引用者**。非孤。
- 是否正本：**部分** —— §4 探针 schema 的合同正本（机器源实存且字段全等）；§2.1 容差口径**不是**正本（与 04 号 §2 同题双承载，且两者对上位科学页的引用同错）。
- 重复或重叠对象（两处实帖、均在本批内）：
  1. **↔ `TEST_MATRIX.md:27`** —— 本文 `:32`：「**可满足性下限**：绝对容差只有在被比较量量级使 `atol ≥ 1 ulp(scale)` 时才可判。模块冻结的是绝对容差时，必须同时声明其**适用量级域**；超出该域按同值的相对形式判（只放宽不收紧）。低于 1 ulp 的绝对容差不可满足，门只允许取可满足的容差。」TEST_MATRIX:27：「**绝对容差的可满足性下限**: 任何绝对容差 `atol` 只有在被比较量量级 `scale` 满足 `atol ≥ 1 ulp(scale)` 时才可判；模块若冻结绝对容差（…），必须同时声明**适用量级域**，超出该域按同值的相对形式判（只放宽不收紧）。低于 1 ulp 的绝对容差不可满足，门只允许取可满足的容差。」末句**逐字相同**，中段仅换标点。差异 = TEST_MATRIX 多举 `1e-12` 实例。
  2. **↔ `TEST_MATRIX.md:26`** —— 本文 `:28`「**归约顺序冻结是结构性不变量，判据是事前冻结的浮点容差**——两者不可互换」+「跨 worker **无共享浮点累加器**」；TEST_MATRIX:26「归约顺序冻结是结构性不变量（跨 worker 无共享浮点累加器），与容差判据两者都要满足」。同一不变量两处承载。
- 元信息块：**有**（`:6`）：`> ID: CONTRACT-501-SCHED  状态: FROZEN  机器 schema: eng/contracts/schemas/scheduler_probe_event.schema.json` —— 文档 ID + 状态编号 + 锚三件套。且 `CONTRACT-501-SCHED` **全仓唯一命中即该行**（`git grep -rn "CONTRACT-501-SCHED"`）⇒ 孤立编号，未进 `docs/contracts/INDEX.yaml`。
- 悬空引用：0。`:6`/`:60` 两份 schema、`:30-31`/`:34` 的 `docs/contracts/TEST_MATRIX.md §2`、`:22` 的 `PHASE2_UPM.md` 均实存且章节存在。
- 与上位冲突（两条硬伤）
  1. `:60`「**映射表（与 observability 现有事件流）**：本 schema 与 `eng/contracts/schemas/jsonl_event_v1.schema.json` **兼容**——`ts`/`kind`/`value`/`unit` 复用其字段名；`stage`/`node`/`block`/`worker` 作为 `tags` 的等价展开。」实核该 schema 属性集（47 项）：**无 `ts`、无 `value`、无 `tags`**（时间字段名 `timestamp_utc`，量值为 `rate`/`rss_bytes`/…），而 `stage` 本身就是它的顶层属性（required 十字段之一），不是"tags 的展开"。⇒ 一句里四处对机器 schema 的陈述为假。
  2. `:30`「模块科学页已冻结的三档（`docs/science/PHASE2_UPM.md` §7：… 跨 worker 数（1..N）= **1e-12 绝对容差**）」——正本 `PHASE2_UPM.md:98`「收敛与容差 (SCI-UPM-CONV-001；**容差随观测尺度归一，禁绝对容差**)」、`:154`「跨 worker（1..N）= **相对容差 rtol 1e-12**（判据 `|ΔC| ≤ 1e-12·max(|C|, C_scale)`）」、`:160-166`「绝对量 1e-12 只在 `|C| ≈ 1` 时与 rtol 等价…绝对 1e-12 比 ulp 严 10 个数量级、不可达…引用绝对 1e-12 的前提 = 已声明 C 场标度」。⇒ **本文把上位明令禁止的形态写成上位冻结值**；同错由 04 号 `:27` 复制。处置方向 = 以 science 页改合同页（科学页 FROZEN 不动）。
- 写法违规：`:1` 标题含 `CONTRACT-501 / RELEASE-05` 两个任务/发布编号；`:14`、`:40`「PERF-501」；`:66`「磁盘满：exit 10（**保持已修语义**）」历史叙事；`:40`「本任务只保证机制正确与探针齐全」以任务口吻写规范。机械列 `task_ids=['RELEASE-05']`，漏 `CONTRACT-501`、`PERF-501`。
- 事实核对（正面，供整治时不要误伤）：`:36-40` 资源声明与 `:14`「模块不私建线程池」与 `ENGINEERING_SPEC §4.1` 同向；`:64` SIGTERM→退出码 9 在 `lib/infrastructure/cli/runtime_client.cpp:499`（`ErrorDomain::CANCELLED: return 9`）与 `cancel_token.h:37` 在册 ✓；`:65` 磁盘满→exit 10 在 `lib/infrastructure/cli/commands.cpp:377,845` 在册 ✓；§4 字段表与 `eng/contracts/schemas/scheduler_probe_event.schema.json` **逐项全等**（11 属性、`kind` 8 值、`stage` 3 值、`unit` 3 值、required 5 项）——本批唯一一条"合同↔机器 schema 全等"。
- 处置建议：**保留**，订正 `:60` 映射段与 `:30` 容差档两处；把 §2.1 的容差总则与 04 号 §2 合并为一份正本、另一处只引用。理由条款：标准 01 §2「同一角色内同一主题只有一份正本；多份内容合并为一份」+ §1「下位文档与上位文档冲突时以上位为准」。

### 04 · docs/contracts/TEST_MATRIX.md

- 路径：`docs/contracts/TEST_MATRIX.md`｜标题：`TEST-001 — 科学测试矩阵（V6）`｜行数：**66**｜角色：**合同说明**（§3 映射）+ **标准与检查**（§2 通用容差、§4 影响触发）→ 混合角色
- 目录落位：§2/§4/§5 属"标准与检查"而落在 `docs/contracts/` → **角色错放，应迁往 `docs/standards/`**；§1/§3/§6 属合同映射，留 `docs/contracts/`。
- 主题：测试类型分类、通用容差规则、SCI/ALG→TEST 映射、影响触发条件、验收门。
- 上游：`:3`「ASTROCS_DESIGN.md §12.4（验证层级与四层验收）」——**条款名逐字对上**（`ASTROCS_DESIGN.md:764` = 「### 12.4 验证层级与四层验收」），本批唯一一条完全正确的上游标注。
- 下游：**19 个引用者**。非孤。
- 是否正本：**候选**（SCI/ALG→TEST 映射的合同说明正本候选）；同一映射已由 `docs/TRACEABILITY.csv` 与 `docs/traceability/TRACEABILITY_MATRIX.csv`（含 `test_id/test_path/test_status` 列 + `eng/tools/traceability/check_traceability_matrix.py`）机器承载，本文是第三份人工副本且不声明谁是权威。
- 重复或重叠对象：见 03 号「重复或重叠对象」段 1、2 两段实帖（`TEST_MATRIX.md:26-27` ↔ `SCHEDULER_CONTRACT.md:28,32`）。**不**与 `docs/standards/NUMERIC_STANDARD.md` 重复——该页 `grep -nE "rtol|atol|ulp|归约"` 0 命中，它只管量纲/标度词表，故本文是容差判据唯一承载处（应升为该主题正本，且正本应落在 `docs/standards/`）。
- 元信息块：**有**（`:5-6`）：`> 上游: SCI-001..003, DATA-001  下游: 各模块 P1-*/P2-*/P3-* 测试任务, CPU-006` + `> 规则: 每个 SCI/ALG ID 至少一个独立 TEST; 容差事前冻结; 影响触发按 07 矩阵。`
- 悬空引用（判"不存在"一律用跟踪集 `git ls-files` + 删除历史，不用磁盘存在性）
  1. `:40`「测试文件 `lib/infrastructure/aio/healpix_db/.../candidate_oracle_test.cpp`」——该目录下**无 `tests/`**（跟踪集只含 `README.md/archive/docs/healpix_io/memory.md`）；真实文件 = `lib/algorithms/drizzle/healpix_drizzle/tests/candidate_oracle_test.cpp`。⇒ 省略号掩盖了一个**错目录前缀**。
  2. `:60`「`impact_analysis`（09 规范工具）」—— `git grep -l "impact_analysis"` 全仓唯一命中即本行；`docs/ci/` 跟踪集只有 `01_CHECKS/02_PIPELINE/03_GATES/04_ARTIFACTS/CI_SPEC`，**无 09 号规范**。⇒ 既无工具也无规范落点。
  3. `:6`「影响触发按 **07 矩阵**」—— 同上，无 07 号文档；§4/§5（`:49-62`）自己承载了触发条件。
  4. `:65`「每个 ACTIVE SCI/ALG 在 **TRACEABILITY.csv** 有 TEST 映射」—— 根目录 `TRACEABILITY.csv` 跟踪集 0 命中且无删除历史；候选两个不同文件（`docs/TRACEABILITY.csv`、`docs/traceability/TRACEABILITY_MATRIX.csv`）列结构与内容不同 ⇒ **裸文件名指向不唯一，判据不可执行**。
- **测试编号在册性（本批核心产出；三态）** 命令：`git grep -l -w <ID>` 全仓 + `git grep -l -w <ID> -- eng lib` 可执行面。

| §3 行 | TEST ID | 在册证据 | 三态 |
|---|---|---|---|
| SCI-CAL-001 | TEST-CAL-001 | `eng/tests/unit/CMakeLists.txt`、`lib/algorithms/calibration/tests/test_photometry_apply.cpp`、`docs/TRACEABILITY.csv` | **一致** |
| SCI-WCS-001 | TEST-IPV-001 | `lib/algorithms/platesolve/cpp/ipv/test/test_synthetic.cpp` | **一致** |
| SCI-DRZ-001/014 | TEST-DRZ-CAND-001 / TEST-DRZ-VAR-001 | `lib/algorithms/drizzle/healpix_drizzle/tests/CMakeLists.txt`、`variance_propagation_test.cpp` | **一致**（测试文件列的目录前缀错，见悬空 1） |
| SCI-UPM-001 | TEST-UPMW-001..007 | `lib/algorithms/coverage/tests/synthetic_gate.cpp` 实测门名为**无 TEST- 前缀**的 `UPMW-001,002,003,004,006,007`；`UPMW-005` 与 `TEST-UPMW-*` 形态在 eng/lib 零命中 | **合同超前**（ID 命名空间不一致 + 005 无实体） |
| SCI-PHOT-001 | TEST-PHOT-001 | 全仓唯一命中 = 本文；测试文件列写 `lib/algorithms/photometry/...` 占位 | **合同超前** |
| SCI-PSF-001 | TEST-PSF-001 | 同上，全仓唯一命中 = 本文 | **合同超前** |
| SCI-NOISE-001 | TEST-NOISE-001..015 | 本文 + `artifacts/evidence/**`；`TEST-NOISE-015` 全仓 **0 命中** | **合同超前** |
| SCI-REJ-001 | TEST-REJ-001..008 | 全仓唯一命中 = 本文；`TEST-REJ-008` 0 命中 | **合同超前** |
| SCI-INT-001 | TEST-INT-001 | 本文 + `eng/tests/quality/fixtures/docchk002_claims_fixture.csv`（夹具非测试） | **合同超前** |
| SCI-CW-001 | TEST-CW-001..008 | 本文 + 证据台账；`TEST-CW-008` 0 命中 | **合同超前** |
| SCI-P3-001 | SYN-007 五件套 | `git ls-files eng/tools/validation` **为空**（整个目录无跟踪内容），文中自标「(待建, P3-006)」 | 如实登记未建，但把未建路径写进"测试文件"列仍是**合同超前** |

汇总：11 行中 **3 行在册（1 行目录前缀错）、8 行登记的 TEST 编号在可执行面零在册**。
- 写法违规：`:1` 标题 `TEST-001 — 科学测试矩阵（V6）`（任务编号 + 版本代号）；`:36`「WCS roundtrip 1e-8px（**FIX-406 收紧**）」任务编号 + 历史叙事；`:45`「(待建, **P3-006**)」；`:65`「(**DOC-004** 校验断链)」；`:5` 下游写成 `P1-*/P2-*/P3-*` 通配区间。无日期、无 sha（机械列 `dates=[]`、`commit_sha=[]` ✓ 准）。
- 与上位冲突
  1. `:27` 把 `docs/science/PHASE2_UPM.md` §7 的跨 worker 容差写成「**绝对**容差 `1e-12`」——正本 `:98`「禁绝对容差」、`:154`「= 相对容差 rtol 1e-12」。**与 03 号同错，两份合同一起写反上位**。
  2. `:33-45` 表头自称「每 SCI/ALG 的 TEST 映射（**当前冻结**）」，与两份 TRACEABILITY 机器源构成同一映射的三份承载，本文未声明权威（标准 01 §1「任何文档不声明自己的第二套权威顺序」的反面情形：不声明也造成双头）。
  3. `:41`「k_corr≈1.3883; rtol=1e-9」与 `PHASE2_UPM.md:154` 的 `rtol 1e-12` 非同量级：前者是 UPM 拟合收敛容差、后者是 1/N worker 等价容差，两处同名"容差"未具名区分。
  4. `:5` 自称上游 `SCI-001..003, DATA-001` —— `SCI-001/002/003` 在 `docs/contracts/INDEX.yaml` 的 `id: SCI-*` 清单中不存在（实际形如 `SCI-CAL-001`），是占位而非引用。
- 处置建议：**拆分**——§2（通用容差规则）与 03 号 §2.1 择一为正本、另一处改为引用，判据归属迁 `docs/standards/`；§3 保留为映射的合同说明但以 `docs/traceability/TRACEABILITY_MATRIX.csv` 为机器源、只写指针不重述 ID 区间；§5 的 `impact_analysis`/`07 矩阵` 两条不可执行引用删除或补落点。理由条款：标准 01 §2「同一角色内同一主题只有一份正本」+ §3「追溯不到的先补要点再保留」。

<!-- PROGRESS: 已审份数 4/7 -->

### 05 · docs/contracts/UNIFIED_OBJECTS.md

- 路径：`docs/contracts/UNIFIED_OBJECTS.md`｜标题：`统一数据对象合同（UNIFIED_MODEL §2 的 **13** 个对象）`｜行数：**165**｜角色：**合同说明**
- 目录落位：`docs/contracts/` ✓ 一致（标准 01 §2「JSON/schema/字段语义，对应 `eng/contracts/`」正是本目录职责）。
- 主题：13 个 canonical 数据对象 → schema → schema ID 对照、模糊字段名禁令、旧 DATA ID→统一对象映射、三类配置分离锚点、`sparse_snr_layer` 重建声明面。
- 上游：`:3`「ASTROCS_DESIGN.md §3.1（数据对象）」——**条款号与条款名都对**（`ASTROCS_DESIGN.md:221` = 「### 3.1 数据对象」），本批两条完全正确的上游之一（另一条是 04 号）。
- 下游：**10 个引用者**。非孤。
- 是否正本：**否（候选否）** —— 标准 01 §2 规定「同一主题只有一份正本」，本文与 07 号 JSON、`eng/contracts/schemas/unified/*.schema.json` 三处承载同一事实；机器 const 已在 schema，本文与 07 号都是副本。**正本候选 = `eng/contracts/schemas/unified/`（机器事实源）+ `docs/design/UNIFIED_MODEL.md` §2（语义正本）**，本文应降为"指针页"。
- 重复或重叠对象（贴两段）：
  1. **↔ `docs/contracts/unified_object_registry.json:93`（本批 07 号）** —— 本文 `:47` frame_snr 行「可否作权重」列：「唯一帧级参考；权重由 **Phase 2** 逆方差叠加从 SNR 计算（SNR 本身**不是**权重）」；registry `:93` `"weight_verdict": "唯一帧级参考；权重由 Phase2 逆方差叠加从 SNR 计算"`；语义正本 `docs/design/UNIFIED_MODEL.md:43`：「唯一帧级参考；**Phase2 归一后换算 w=SNR²/F_ref²=1/σ_F²**」；机器 const `eng/contracts/schemas/unified/frame_snr.schema.json:248` = registry 版。⇒ **同一列四处三种措辞**，而本文 `:57` 自称「『可否作权重』列**逐字照抄** `docs/design/UNIFIED_MODEL.md` §2，机器以 `object_weight_verdict`(const) 固化」——照抄陈述为假（与正本不同字），与自家机器 const 也不同字。
  2. **↔ 同件 `unified_object_registry.json:197-370`** —— 本文 §4a（`:122-145`）与 registry `legacy_contract_ids.entries[0..6]`（`:201-353`）承载同一张"旧 ID→统一对象"判定表（7 条、同一 mapped/no_canonical 结论、同一证据清单）。差异处**互斥**：`DATA-P1-SOURCES` 证据本文 `:136` 写 `docs/contracts/PUBLIC_API.md:**636**`、registry `:304` 写 `:634`（实测真身 `PUBLIC_API.md:682`）；`DATA-GAIA-001` 本文 `:140` 写 `DATA_SEMANTICS.md:**87**`、registry `:341` 写 `:86`（实测 `:125`）；`DATA-P2-SMP` 本文 `:138` 写 `registry/astrocs.phase2.sample.md:**63**/:64/:164`、registry `:324` 写 `:62/:64/:164`。⇒ 双承载已产生事实分歧，属标准 01 §2 禁止形态。
- 元信息块：**有**（`:5-13` 八行 `>` 块）：退役与身份三注（含 `DOC-203 / Q2 前置裁决 2026-09-20；GAP_AUDIT §4.1/§4.5`、变更 claim、`CHG-2026-09-22-V6-CONTRACT-MERGE`、`§9.73 A44`），`:15`「性质：**索引与语义登记**」自述纪律声明。机械列 `meta_block=False`。
- 悬空引用
  1. `:74`「`unified_object_registry.json#port_contract` 的 `accepts_object` / `accepts_schema_id` / `connected_object_document`」—— registry 顶层键只有 `port_contracts` 与 `port_contract_ref`，**无 `port_contract`**；示例对象内键为 `connected_object`，**无 `connected_object_document`**。⇒ JSON 指针与字段名双悬空（同一错误在 07 号 `:647` 再出现一次）。
  2. 其余路径全部在册：`:19/:78` 的 `eng/contracts/schemas/unified/` 13 个 canonical schema 逐一存在、`:67-74` 的 n1–n4 负例存在、`:101` 的 n5 存在、`:120` 的 `lib/algorithms/integration/v6/oracle/recon_contract_gate.py` 存在、`:143` 的 `eng/contracts/data/unified_object_compatibility_map_v1.json` 存在、`:145` 的 `test_unified_object_contract.py::TestRegistryIndex::test_legacy_ids_are_decided_not_pending` 与 `::test_legacy_id_map_written_into_index_and_compat_map` **两个方法名都在册**（各 1 命中）。
  3. `:120` 引「`实验/absolute-snr` EXP-04 §2.7/§4.1/§4.3/§4.5」—— 落点 `实验/absolute-snr/docs/EXP-04-RECONSTRUCTION.md`（`:330` §2.7、`:460` §4 存在）✓；但引用面写成"目录 + 编号"而非路径，属可判定性弱（不判悬空）。
- 写法违规（逐条行号 + 片段）
  - 日期：`:6`「Q2 前置裁决 **2026-09-20**」、`:19`「已于 **2026-09-20** 退役」、`:101`「`CHG-2026-09-20-PSFSW-RETIRE`」。
  - 任务/裁决编号：`:5`「DOC-203 / Q2 前置裁决」「GAP_AUDIT §4.1/§4.5」、`:9`「MOD-001」、`:124`「**负责人裁决**」、`:120`「本次新增声明面」、`:83/:87-93`「CHG-2026-09-22-V6-CONTRACT-MERGE」全表重复 7 次。
  - 历史叙事：`:1`「**13** 个对象」以划改对比呈现、`:6`「原 14 个」、`:49`「~~`psfsw_robust_weight`~~ **已退役**…**已删除**…对象已不存在（14→13）」整行删除线、`:99-101`「已按 §9.73 A44 作废（作废键面：删键 / 改写 / 加作废留痕）」、`:154/:155`「原示例 `sci_weight_mode` 已删」。
  - 结构：`:103` `## 4b.` 排在 `:122` `## 4a.` 之前 ⇒ 节号倒置；`:129/:131/:133/:135/:137/:139/:141` 七行以全角空格 + `└` 起头插在表格行之间（`git grep -nP "^\x{3000}"` 命中七次）⇒ **把 §4a 的 Markdown 表切成 8 段碎片**，表格不可渲染也不可机器解析。
  - `:160-164` §6「边界声明」整节是过程性自述（"本任务只写…未改 lib/**…未 commit / push / 建分支 / stash / reset / clean / checkout"）——属控制包回执内容，标准 01 §4「只写现行设计要怎样」不覆盖此类段落，应出库。
- 与上位冲突：本文无直接公式与上位打架；但 `:19/:78` 自称「`eng/contracts/schemas/` 是数据合同的**唯一事实源**（ENGINEERING_SPEC §4.7）」，而被引条款 `ENGINEERING_SPEC.md:51` 原文是「输入/输出端口引用有效 DATA 合同（**`docs/contracts/`** 唯一事实源）」——**引用把上位的主语换了**；同时 `ASTROCS_DESIGN.md:3` 规定两面「双向对应」，未授权任何一面自称唯一。07 号 `:11` 同错。
- 处置建议：**合并**——把本文 §2/§4a 的两张表并入 07 号 JSON 作单一承载（本文只留人读解释 + 指向 schema 的指针），§4b 的算子词表保留（它与 `sparse_snr_layer.schema.json` 的 enum 实测**逐字全等**：4 个 token、`control_point_geometry` 键、`SparseReconstruction.operator_id` 见 `lib/algorithms/integration/v6/include/astrocs/v6/weight_chain.h:165`）；删除元信息块、日期、划改与 §6 过程自述。理由条款：标准 01 §2「同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除」+ §4「正文无日期、无任务编号…无旧版/作废/留痕」。

### 06 · docs/contracts/config_separation_anchors.json

- 路径：`docs/contracts/config_separation_anchors.json`｜标题：无 h1（JSON 文件，`anchor_id = IDX-CONFIG-SEPARATION`）｜行数：**215**｜角色：**合同说明**（机器锚点索引）
- 目录落位：✓ 一致（`docs/contracts/` 承载 `eng/contracts/` 的文档化说明）。
- 主题：三类配置（`phase_config` / `cpu_profile` / `run_manifest`）的字段名命名空间正则、互斥禁入清单、跨类同名登记。
- 上游：**无「上游」条款标注**；`:6-10` 以 `authority` 段代替（`separation`/`design`/`owner` 三键）。标准 01 §3 要求"每份下层文档在开头标明上游最高设计条款"——`authority` 段形式上满足，但 **`:8` 的条款指向错**：「design: ASTROCS_DESIGN.md **§2**（数据对象与配置分离）」，实际 §2 = 「核心科学方法：创新点」，配置分离在 **§3.2 三类配置**（`ASTROCS_DESIGN.md:231`）。同文件 `:7` 指 `docs/design/UNIFIED_MODEL.md §3（三类配置严格分离）` ✓ 正确（该文件 `:64` 标题正是「三类配置严格分离」）。
- 下游：**14 个引用者**。非孤。
- 是否正本：**否** —— 与 05 号 §5 同一张表双承载（见下条）；真权威在 `docs/design/UNIFIED_MODEL.md` §3 + `eng/contracts/schemas/{cpu_profile,run_manifest,phase_config_*}.schema.json`。
- 重复或重叠对象（贴两段）
  1. **↔ `docs/contracts/UNIFIED_OBJECTS.md:153-155`（本批 05 号）** —— 05 号表 `phase_config` 行「混入即 REJECT 的字段：`isa`、`isa_level`、`workers`、`worker_count`、`block`、`block_size`、`cpu_model`、`cpu_vendor`、`thread_budget`、`affinity_mask`」；本件 `:53-64` `"forbidden_field_names"` 数组**十项同名同序**；`cpu_profile`/`run_manifest` 两行同样逐项重复。差异 = 05 号多两条「（原示例 `sci_weight_mode` 已删 …）」留痕，本件对应位置（`:121-125`、`:172-177`）**无该留痕** ⇒ 同一禁入清单两处承载且历史标注不对称，任一处修订都会让另一处失真。
  2. **↔ 本件内部 `:53-64` vs `:181-193`** —— `config_classes[0].forbidden_field_names` 与 `cross_class_forbidden.cpu_profile_keys_in_phase_config` 是同一十项清单在同文件内的第二次承载（字段名不同、内容全等）。
- 元信息块：JSON 无"开头块"形态，但 `:3-5` 的 `anchor_id` / `task: "DATA-001"` / `baseline_head: "ecf6ad6f"` 与 `:9` 的 owner 段「本文件只留锚点与索引，不定义配置 schema」= 文档 ID + 任务编号 + 基线锚 + 纪律声明四合一（标准 01 §4 禁 sha/任务号/纪律声明式元信息）。
- 悬空引用 / 指向不成立（确认命令：`git ls-files -- <path>`）
  - `:16` `"schema_path_reserved": "eng/contracts/schemas/phase_config.schema.json"` —— 跟踪集 0 命中、无删除历史 ⇒ 不存在。**同字段另一语义**：`:70` `cpu_profile.schema.json` 与 `:131` `run_manifest.schema.json` **均已存在**（tracked）。⇒ 同一个键名 `schema_path_reserved` 在三处承载两种互斥事实（"预留待建" vs "已建"），不可判定。
  - 其余全部在册：`:207` `eng/tests/config/test_cfg001_contracts.py`（含类 `TestCrossClassDisjointness`，1 命中）、`eng/tests/config/gate_assertions.py`（`:89` `gate("G4 cpu_profile ∩ phase_config == ∅"` ⇒ 门号 G4 ✓）、`:208` `eng/contracts/schemas/phase_config_normalize.schema.json` 的 `$defs/star_detection_config`（✓ 键存在）、`:212-213` 两条 `machine_assertions`（`TestConfigSeparationAnchors::test_field_name_sets_are_pairwise_disjoint` / `::test_hardware_names_forbidden_in_phase_config` 均在 `eng/tests/contracts/test_unified_object_contract.py` 命中）。
- 写法违规：`:4` `"task": "DATA-001"`；`:5` `"baseline_head": "ecf6ad6f"`（commit sha，机械列已报 ✓）；`:207`「历史登记（本文件为登记面**唯一事实源**…）」历史叙事 + 自封唯一口径；`:208`「登记理由：star_detection 段纳入合法输入面时…出现该同名」过程叙述。
- 与上位冲突
  1. `:207`「本文件为登记面**唯一事实源**，门 = …」——标准 01 §1「任何文档不声明自己的第二套权威顺序或'唯一口径'」；且同句把门写成 `TestCrossClassDisjointness`，与 `:211-214` `machine_assertions` 写的另一对（`TestConfigSeparationAnchors::…`）是**两套不同的门指向同一判据**。
  2. `:208` 声明 `mode`「**不在** cross_class_forbidden 硬件字段名清单内」并把 `star_detection.mode` 与 cpu_profile 的 `mode` 并记为"合法同名"；这与 `:157` 05 号同名句「三类配置的字段名模式两两互斥（**同名即同义**）」在判据方向上相反：一边"同名合法（异义）"、一边"同名即同义"。⇒ 同批两文件对"同名"给出互斥规则。
  3. `:8` 上游 §2 指错（见「上游」）。
- 处置建议：**合并**——并入 07 号 registry 或 `eng/contracts/schemas/` 侧的机器登记，本文与 05 号 §5 各留指针；把 `schema_path_reserved` 拆成 `schema_path`（存在）与 `schema_path_pending`（未建）两个键，删 `task`/`baseline_head` 与"唯一事实源"自封句。理由条款：标准 01 §1「不声明第二套权威顺序或唯一口径」+ §2「同一主题一份正本」+ §4「无 commit、无任务编号」。

### 07 · docs/contracts/unified_object_registry.json

- 路径：`docs/contracts/unified_object_registry.json`｜标题：无 h1（`registry_id = IDX-UNIFIED-OBJECTS`）｜行数：**681**｜角色：**合同说明**（机器归属索引 / 所有权登记表）
- 目录落位：✓ 一致。
- 主题：13 个 canonical 对象类的机器归属登记 + 旧合同 ID 判定 + 端口合同示例 + 其余 schema 文件归属 + 退役与迁移路径 + U-02 判据。
- 上游：**无「上游」条款标注**，`:7-12` 以 `authority` 段承载。其中 `:9`「separation: ASTROCS_DESIGN.md **§2**（数据对象与配置分离）」同 06 号错（真身 §3/§3.2）；`:9`「ENGINEERING_SPEC.md §3（数据对象按 UNIFIED_MODEL 区分）」**✓ 实核成立**（`ENGINEERING_SPEC.md:37` = 「数据对象按 `docs/design/UNIFIED_MODEL.md` 区分，一个字段只承载一个含义」）；`:10` §4.7 ✓（= §4 第 7 条）；`:11`「single_source_of_truth: `eng/contracts/schemas/`（ENGINEERING_SPEC §4.7）」——**被引条款原文的主语是 `docs/contracts/`**（`ENGINEERING_SPEC.md:51`），与 06 号同错。
- 下游：**15 个引用者**。非孤。
- 是否正本：**部分为正本** —— `canonical_object_classes` 与 `other_schema_files` 是"哪个 schema 归哪个对象/谁所有"的**唯一登记面**（U-02 判据的落点，实核 33/33 全覆盖、无幽灵条目）；对象语义（单位/无效值/精度）的正本在 `eng/contracts/schemas/unified/*.schema.json`，本文是索引。
- 重复或重叠对象：见 05 号两段（frame_snr 措辞分歧、legacy 判定表双承载）。本件内部另有自重复：`:543-551` `deprecation.legacy_id_map` 与 `:201-353` `legacy_contract_ids.entries[].canonical_objects` 是同一 7 条映射的两次承载（前者路径字符串、后者结构化），任一处改动另一处不联动；`:671-679` `u02_criterion.statement` 与 05 号 `:29` 的 U-02 结论段**同文重复**。
- 元信息块：`:2-6` `registry_schema` / `registry_id` / `version` / `task` / `baseline_head` 五连 = 文档 ID + 版本锚 + 任务编号 + 基线 sha（标准 01 §4 禁版本锚与 sha）。
- 悬空引用 / 指向不成立（确认命令：`git ls-files -- <path>`；判"不存在"用跟踪集 + 删除历史）
  - `:647` `"new_path": "docs/contracts/unified_object_registry.json#port_contract"` —— 顶层无该键（实为 `port_contract_ref` / `port_contracts`）⇒ **自指指针悬空**，且与 05 号 `:74` 同一处错误。
  - `:564` `"removed": ["eng/contracts/schemas/unified/psfsw_robust_weight.schema.json", "eng/contracts/schemas/unified/examples/psfsw_robust_weight.example.json"]` —— 二者现均不在跟踪集；**删除历史可核**（`git log --all --oneline -- '*psfsw_robust_weight.schema.json'` → `5f034dfc`）。⇒ 登记与事实相符，不判悬空（这是 12 条 `old_path`/`removed` 的正常语义）。
  - `:490` `"权威=ASTROCS_DESIGN.md §5.3 八投影 + docs/algorithms/PHASE3_PROJ_IMPL.md §15"` —— **§5.3 实为「SNR 重建与逆方差叠加」**（`ASTROCS_DESIGN.md:385`）；八投影（TAN/SIN/CAR/AIT/STG/MOL/CEA/ZEA）在 **§6.3 投影算法**（`:453`）。`docs/algorithms/PHASE3_PROJ_IMPL.md:399` 的 §15 标题本身也写「DESIGN §5.3 八投影」⇒ 错误由 ALG 层传入本件（别批路径：`docs/algorithms/PHASE3_PROJ_IMPL.md`）。同句的实现锚 `lib/algorithms/projection/p3_proj_v6.h registry_frozen_set()` ✓ 在册。
  - 其余 72 条路径引用**全部在册**（脚本化核：两 JSON 中 82 个不同路径字面，10 个不存在的均为 `old_path`/`removed`/`schema_path_reserved` 三类的"应不存在"项）。
- 写法违规：`:4` `task: DATA-001`、`:6` `baseline_head: ecf6ad6f`、`:490`「SCI-FIX-PROJ 于 **808e7a8d** 建立，claim SC-001」（commit sha 直写正文，机械列已捕获 ✓）、`:462`「GAP_AUDIT §9.68（**2026-09-20**）改写为多数据块形态：…逐帧 {phase_name, config, inputs[]} **已删除**」、`:532`「变更编号 CHG-2026-09-22-V6-CONTRACT-MERGE」、`:559` `"retired_on": "2026-09-20"`、`:265`「该 ID 在 … 中**零命中**（旧契约面无定义）」、`:553-555`「`retire_after_change`: OWNER_DECISION … **不得**作为退役条件」。机械列把 06/07 两份 `history_narrative` 判 `False` ⇒ **漏报**。
- 与上位冲突
  1. `:11` 的"唯一事实源"主语替换（见「上游」）——与 `ASTROCS_DESIGN.md:3`「两面双向对应」及 `ENGINEERING_SPEC.md:51` 相抵。
  2. `:671-679` U-02 判据要求「`eng/contracts/schemas/**` 下**每个 schema 文件**都必须在 canonical_object_classes 中被登记归属」，而实核口径是"每个 `*.schema.json`"：33/33 ✓ 通过；但 `eng/contracts/schemas/unified/negative/n6_sparse_snr_relative_semantics.schema-violation.json` 存在且在两份合同文档里**零引用**（05 号 §3 只列 n1–n4、`:101` 补 n5）⇒ 判据字面（"每个 schema 文件"）与实现（只校 `.schema.json`）之间有缝，且 n6 为未登记的负例事实。
  3. `:307`/`:351` 两处「基线 `docs/contracts/INDEX.yaml`（ecf6ad6f）对该 ID 零命中，继续作为星表服务合同存在」——与 `:6` 同一 `baseline_head` 锚，但基线已被工作树推进；把"某个历史提交的索引状态"写进现行登记面，标准 01 §4「只写现行设计要怎样」不覆盖此形态。
- 正面事实（整治时不得误删）：13 个 `object_name` ↔ 13 个 canonical schema 文件 ↔ 14 个 `$id`（含 port）逐一核对，**全部等于** 05 号表中写的 `https://astrocs.local/schemas/unified/<对象名>/v1`；`weight_capable` 布尔与 schema const 一致（signal=false、variance=true、ivar=true、point_information=true）；`legacy_contract_ids.summary`（total 7 / mapped 4 / no_canonical 3）与 `entries[]` 逐条结论一致；`:344`「`docs/traceability/TRACEABILITY_MATRIX.csv` 第 2 行（DATA=DATA-GAIA-001, VERIFIED）」实核为真（第 2 行 `data_id=DATA-GAIA-001`、`data_status=VERIFIED`）。
- 处置建议：**保留 + 升为该主题机器登记正本**（它是唯一能承载"每个 schema 归谁"的机器面），同时把 05 号 §2/§4a 的表并入本件、05 号降为指针页；订正 `#port_contract` 指针、`:490` 的 §5.3→§6.3、`:11` 的事实源主语；删 `task`/`baseline_head`/`retired_on` 类日期与 sha。理由条款：标准 01 §2「同一角色内同一主题只有一份正本」+ §3「索引由文档现状重建，与文件树一致」+ §4「无日期、无 commit、无 sha256」。

<!-- PROGRESS: 已审份数 7/7 -->

## 4 同主题组与差异

| 主题组 | 各份路径 | 正本候选 | 主要差异（实测） | 合并建议 |
|---|---|---|---|---|
| 对外 ABI 符号合同 | 本批 `docs/contracts/PUBLIC_API.md`；别批 `docs/contracts/API-001.md`、`docs/contracts/API_CONTRACTS.csv`、`docs/architecture/api_inventory.csv` | PUBLIC_API.md（唯一承载签名/所有权/返回码语义） | API-001 自标「设计档案/非生产目标态」；两 CSV 的 symbol 集为 444 vs 378（真子集、差 66），PUBLIC_API.md:243 却称"一致" | PUBLIC_API 保留为符号正本；两 CSV 由同一生成器产出并各自声明"投影"，删除"一致"陈述改为"包含关系 + 生成源" |
| 并行确定性 / 浮点容差 | 本批 `SCHEDULER_CONTRACT.md §2.1`、`TEST_MATRIX.md §2`；上位 `docs/science/PHASE2_UPM.md §7` | 应立 `docs/standards/`（本文批内两处都不该是正本） | `SCHEDULER:32` 与 `TEST_MATRIX:27` 末句逐字相同；两者对上位同错（都写"绝对容差 1e-12"，上位 `:98` 明禁绝对容差、`:154` 给 rtol） | 择一为唯一判据页并迁 `docs/standards/`，另一处只引用；两处容差档一律改为 rtol 表述 |
| 统一数据对象（13）与旧 ID 判定 | 本批 `UNIFIED_OBJECTS.md §2/§4a`、`unified_object_registry.json`；别批 `eng/contracts/schemas/unified/*.schema.json`、`docs/design/UNIFIED_MODEL.md §2`、`eng/contracts/data/unified_object_compatibility_map_v1.json`、`docs/contracts/INDEX.yaml#legacy_contract_id_map` | registry.json（机器登记面）+ UNIFIED_MODEL §2（语义面） | 同一"可否作权重"列四种措辞；同一 legacy 证据行号两处互斥（636 vs 634、87 vs 86、63 vs 62）；U-02 判据在 md:29 与 json:673 各写一遍 | 表并入 registry、md 留指针；证据行号一律重核后单点登记 |
| 三类配置分离 | 本批 `UNIFIED_OBJECTS.md §5`、`config_separation_anchors.json`；别批 `docs/design/UNIFIED_MODEL.md §3`、`docs/contracts/CONFIG_CONTRACT.md` | UNIFIED_MODEL §3（语义）+ config_separation_anchors.json（机器锚） | 十项禁入清单两处同序重复；"同名即同义"(md:157) 与"同名合法登记"(json:206-209) 规则方向相反；`schema_path_reserved` 一键两义 | 机器锚保留并拆键；md §5 改为引用；同名规则二选一并在全链统一 |
| 唯一执行平面 / ABI 边界 | 本批 `RT-001.md §1/§3`、`SCHEDULER_CONTRACT.md §1`；别批 `ARCH-001.md §1`、`API-001.md:7`、`PUBLIC_API.md:40` | ARCH-001（架构面）+ RT-001（接口面） | "C ABI 不抛异常"三处措辞不同且无互相引用；RT-001 把 ARCH/API 列作上游，而 API-001 自认非目标态 | 各面只留一处条款 + 其余引用；API-001 若为档案应出库到证据层 |

## 5 索引与链接缺陷清单

登记面：`docs/DOCUMENT_INDEX.yaml`（全文档集唯一索引地图，标准 01 §3）与 `docs/contracts/INDEX.yaml`（合同 ID 索引）。命令均为 `git grep -n -A5 -e "path: \"<路径>\"" -- docs/DOCUMENT_INDEX.yaml`。

| 问题类型 | 条目 | 说明（实测） |
|---|---|---|
| 未登记 | 无 | 本批 7 份在 `DOCUMENT_INDEX.yaml` 全部有条目（:588/:622/:628/:633/:638/:644/:649）⇒ 派单前提"未登记即缺陷"在本批 0 命中 |
| 登记了但文件不存在 | 无 | 7 个登记路径逐一对跟踪集核实存在 |
| 上游标注与文档自身不一致 | `PUBLIC_API.md`（doc `:3` vs 索引 `:625`）；两份 JSON（doc `:8`/`:9` vs 索引 `:647`/`:652`） | 索引对 PUBLIC_API 原样复制了错的 §8.4（模块与 ABI）标注；对两份 JSON 登记的上游是「§3.1（数据对象）、§10（I/O 与原子产品）」，而文件内 `authority` 写的是 §2 / §3 / §4.7 ⇒ **同一文档的上游条款有两套** |
| 角色/duty 标注与判定不符 | `RT-001.md`（`:630` duty「运行时合同（**历史合同层**）」而 status=ACTIVE_NORMATIVE）；`config_separation_anchors.json` `:645` / `unified_object_registry.json` `:650` status=**GENERATED** | RT-001 是 `lib/include/astrocs/core/**` 的现行冻结合同，被索引降格为"历史合同层"；两份 JSON 是手写合同锚点，无任何生成器产出它们（`eng/tools/contract_doc_sync.py` 是双向对应校验，不是生成器）⇒ GENERATED 标注为假 |
| duty 自指 | `:646` `"duty": "docs/contracts/config_separation_anchors.json"`、`:651` 同型 | duty 字段填文件自身路径，等于未登记职责 |
| 索引 notes 承载禁止内容 | `:627`「P1-WCS-DOC (**2026-09-07**) 追加 API-WCS-001 … P2-COV-DOC (2026-09-07) 追加 …」 | 唯一索引地图里写任务编号 + 日期 + 过程流水（标准 01 §4）；该 notes 里还固化了 `coverage.h:52/:56` 两个已失效锚（真身 :58/:62）⇒ 索引层同样在扩散锚漂移 |
| 下游登记与实测不符（三套数） | `PUBLIC_API.md` 索引「… 等 **91 处**」/ 实测 **106**；`SCHEDULER_CONTRACT.md` 文档自标下游含 `TEST_MATRIX.md`、索引只登 2 个 schema / 实测 **38**；`RT-001.md` 索引 4 项 / 实测 **7**；`UNIFIED_OBJECTS.md` 索引 4 项 / 实测 **10** | 同一文档"下游"存在文档自标、索引登记、实测三套值 ⇒ 标准 01 §3 双向索引不可核 |
| 合同 ID 未入 `docs/contracts/INDEX.yaml` | `API-GAIA-001`、`API-CAL-001`、`API-COS-001`、`API-DRZ-001`、`API-HIPS-001`、`API-P2-SMP-001`（6 条，均在 PUBLIC_API.md 自标 `> ID:`）、`CONTRACT-501-SCHED`（03 号 `:6`）、`IDX-CONFIG-SEPARATION`（06 号 `:3`）、`IDX-UNIFIED-OBJECTS`（07 号 `:3`） | 该索引现有 `id: API-*` 仅 17 条：`API-001 API-CONTRACTS-001 API-COV-001 API-NOISE-001 API-P1-SESSION API-P2-{HIPS,INT,REJ,SESSION,UPM}-001 API-P3-{FITS,PROJ,RSMP}-001 API-PHOT-001 API-PSF-001 API-STAR-001 API-WCS-001`。同目录同形态的 7 份文档里 6 条 API 条款无登记 ⇒ 登记面不完整（`API-V6-WEIGHTMODE-001` 已作废不计） |
| 悬空（文档→实现） | 01 号 5 条、04 号 4 条、05 号 1 条、06 号 1 条、07 号 1 条（各见该份「悬空引用」段） | 全部以跟踪集 + 删除历史核定，未使用磁盘存在性 |
| 命名保留面（**不判缺陷**） | `astrocs.*`、`x-astrocs*`、`ASTROCS_*`、`namespace astrocs`、`astrocs_*.dll`、`https://astrocs.local/...`、`astrocs.config-separation-anchors/v1`、`astrocs.unified-object-registry/v1` | 依 `ENGINEERING_SPEC.md §15` 类 1/2/3/4 与判定规则「换成 ACSD 会使机器失配 ⇒ 原样保留」。本批无任何一处显示名漏改（`PUBLIC_API.md:1`、`docs/contracts` 各文档正文标题均以 ACSD 起头，仅 01 号 `:1` 用全称 + 括注 ACSD，合规） |

## 6 覆盖率自报

- 已审 **7 / 7**（清单 `独立审计/批次清单/DB-09.txt` 恰 7 行，`wc -l` = 7；派单文字"7 份"与清单一致，**无差异需登记**）。
- **未逐行读名单：空。** `PUBLIC_API.md` 2210 行按 1–560 / 560–1119 / 1119–1679 / 1679–2211 四段读完；`unified_object_registry.json` 681 行、`config_separation_anchors.json` 215 行、其余四份（57/72/66/165 行）整篇读完。
- 取证动作清单（全部只读）：`git status --porcelain`、`git ls-files`、`git grep`、`awk/sed` 定点读、`wc`、`python3 -`（仅 `json.load` 读文本，未 import 仓内任何模块）。**未执行**构建、ctest、`run_checks.py`、任何 `eng/**` 检查器或端到端。
- 收工基线（`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工基线逐字相同 ⇒ 本批取证未污染被审对象（无新增未跟踪项、无被改跟踪件）。

## 7 我证伪了任务书的哪个前提

1. **「docs 当前约 233 份」——未证伪。** 固定口径命令 `git ls-files 'docs/*.md' | wc -l` = **233**（git 默认 pathspec 下 `*` 跨目录，即 docs 全深度 `.md` 数）；同口径 `git ls-files docs | wc -l` = 258（含 23 份 `.json/.yaml/.csv` 与图片等非 md）。故 233 是"仅 md"口径，不是"全部文档"口径——**任务书没错，但口径没写**，两种读法差 25 份。
2. **「派单：清单里每一份都要通读全文，长文件（>600 行）也要读完」——本批无因长度而未读者**；但派单未预见到单份文档内部**自相矛盾**（同一符号一处称已删、一处称冻结），故"三态结论"对本批 01 号必须按节给，不能按文件给一个态。
3. **「对外 API/ABI 合同最易出现的缺陷是登记的符号在实现里不存在」——方向对，量级被低估。** 实测反向才是主患：197 个真实导出里 139 个被登记（70%），**58 个未登记**，而"登记了但不存在"的只有 **1 个**（`p2_upm_normalized_weights`）。把本批缺陷归因为"幻影符号"会导致整治只查一个点而漏掉 58 个面。
4. **「核它挂的测试编号是否真在册」——派单隐含前提"编号大多在册"被证伪**：`TEST_MATRIX.md §3` 的 11 行里 8 行的 TEST ID 在 `eng/**`+`lib/**` 零在册；`docs/contracts/INDEX.yaml` 里 6 条 PUBLIC_API 自标 API 条款无登记；`CONTRACT-501-SCHED` 全仓唯一命中即其自身声明行（孤锚）。
5. **「机器契约保留面判定规则唯一源在 ENGINEERING_SPEC.md」——成立且够用**（§15 判定规则 + 10 类枚举），本批 7 份中显示名漏改为 **0**；但派单说「本项目正在改名（ACSD ↔ astrocs）」的方向与 §15 相反：§15 规定 **ACSD 是显示名、`AstroCS` 是历史名**，即改名方向单一（astrocs→ACSD 只在非机器面），不存在双向改名。

