# 代码通读 CR-69 · lib/include/astrocs/core（6 文件 / 943 行）

- 批次：CR-69；被审源码基线：`c8f64e9a`（其后提交仅入库审计件，源码未变）
- 判据来源：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（审查轴 §2、逐数值五档处置表 §2.5、finding 字段 §3、禁报清单 §4、节奏 §5）
- 环境差异以派单为准：成稿唯一落点本文件；中间件目录未建（无需）；rg 可用
- 开工 `git status --porcelain` 原文：

```
（空输出 —— 工作树干净，无未跟踪项）
```

## 清单与进度

- lib/include/astrocs/core/canonical_hash.h —— 已读：是（表 8 项，findings 3）
- lib/include/astrocs/core/checkpoint.h —— 已读：是（表 6 项，findings 5）
- lib/include/astrocs/core/context.h —— 已读：是（表 12 项，findings 7）
- lib/include/astrocs/core/contracts.h —— 已读：是（表 12 项，findings 4）
- lib/include/astrocs/core/executor.h —— 已读：是（表 7 项，findings 1）
- lib/include/astrocs/core/export_stream.h —— 已读：是（表 7 项，findings 2）

## canonical_hash.h（43 行，整读）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| canonical_hash.h:34 | `canonical_sha256` 宽度注释 | `64 字符` | SHA-256 摘要的 hex 文本长度 | 公式导出 | 256 bit ÷ 4 bit/hex 位 = 64；实现侧 `crypto/sha256.h`，与 Python 侧 `hexdigest()` 同义 | — | CONFIRMED |
| canonical_hash.h:35 | `integrity_sha256` 宽度注释 | `64 字符` | 整文件 sha256 的 hex 文本长度 | 公式导出 | 同上（同一算法同一编码） | — | CONFIRMED |
| canonical_hash.h:28 | `kCanonicalProductHashSpec` | `"astrocs.canonical-product-hash/v1"`（定义在 canonical_hash.cpp:35） | 哈希域分隔前缀 + 口径版本号 | 结构性不适用 | 它是域分隔标识符/版本串，不进科学语义；但必须与 Python `SPEC_ID`（canonical_product_hash.py:92）逐字符同值 —— 已核，两处均为 `astrocs.canonical-product-hash/v1`，域前缀 cpp:39 与 py:93 同为 `SPEC_ID + "\n"` | — | CONFIRMED |
| canonical_hash.h:33 | `format` 取值面 `fits \| json \| hips-properties \| raw` | 4 分支 | 产品格式判别枚举基数 | 结构性不适用 | 枚举基数，无语义数值；实现侧分支在 canonical_hash.cpp | — | CONFIRMED |
| canonical_hash.h:11 | FITS 排除卡 | `CHECKSUM / DATASUM / DATE / RUNID`（4 项） | 可复现面忽略的头卡 | 文献值 | FITS 4.0 §4.4.2.7（CHECKSUM 覆盖整 HDU、DATASUM 仅数据单元）、§4.4.2.1（DATE = HDU 创建时刻）；逐条 reason+evidence 见 canonical_product_hash.py:99-133。RUNID 为本仓自定义运行标识（invocation_id 类），锚为 SLSA v1.2 invocationId，非 FITS 标准卡 | — | CONFIRMED |
| canonical_hash.h:12-14 | JSON 排除键（13 项，注释所列） | 见 §CR-69-1 | 可复现面忽略的 JSON 键 | 待确认 | 权威清单 canonical_product_hash.py:135-226 实为 **14** 项（多出 `integrity_sha256`，py:220）；注释条目集与权威不同值 ⇒ 该行的"依据"不成立于注释本身。保守方向：以 py/C++ 实现的 14 项为准，注释补齐；影响范围 = 依注释重实现/复核者算出的 canonical_sha256 与产物登记值不同 | 偏保守方向 = 保留 14 项（排除更多易变量 ⇒ 更不易假红） | PARTIAL（缺"是否有下游读者以注释为权威"的证据） |
| canonical_hash.h:15 | HiPS properties 排除键 | `hips_creation_date / hips_update_date / hips_release_date`（3 项） | 可复现面忽略的 IVOA 墙钟键 | 文献值 | IVOA HiPS 1.0 §4.2 properties 必备/常用日期键（evidence 逐条见 py:228-233 与 py:169-204）；实现 cpp:47-48 与 py 的 3 项一致 | — | CONFIRMED |
| canonical_hash.h:19 | 交叉引用"compare_products.py（P26 T3, 负责人裁决 4）的墙钟清单与本清单一致" | 一致性主张 | 用仓内既有裁决为排除清单背书 | 待确认 | 该文件在跟踪面（`eng/tools/quality/compare_products.py`），但其 `CLOCK_KEYS_TEXT`（`:49-62`）为 **9** 项，与本清单 14 项不同集；且 `:77-79` 明确写"不忽略 … `product_sha256`"，而本清单排除它。保守方向：以"两套清单服务于两个目的（哈希域 vs 产品树逐值比对）"解释，但注释的"一致"二字须订正为"墙钟子集一致"；影响范围 = 见 §CR-69-3 | 偏"清单不一致"方向：若有人据此统一两清单，会把 5 个派生/遥测键从哈希域请出或把 `product_sha256` 塞进比对忽略面 | PARTIAL（未核 P26 T3 裁决原文，只核两个清单的代码事实） |

### findings

### CR-69-1 · 公共头注释自列的 JSON 排除清单比它声明的唯一权威少一项（缺 `integrity_sha256`）
- 轴：C（上下级一致）；辅 S
- 位点：`lib/include/astrocs/core/canonical_hash.h:12-14`
  ```
  //   JSON 键: run_id / elapsed_sec / created_utc / started_utc / finished_utc /
  //            ended_utc / generated_utc / timestamp_utc / hips_creation_date /
  //            hips_update_date / hips_release_date / sha256 / product_sha256
  ```
- 上位依据：同文件 `:3-5`「口径（唯一权威定义 = eng/tools/canonical_product_hash.py, 本文件是其 C++ 同构实现; 两者必须逐字节同值…）」；`eng/tools/canonical_product_hash.py:135-226` 的 `EXCLUDED_JSON_KEYS` 共 14 项，末项 `:220` `"key": "integrity_sha256"`，其 `:222` 说明「DET-001 起 p3_writer/p3_verify 的**文件级**摘要字段名（原 sha256/product_sha256）」；实现侧 `lib/infrastructure/scheduler/src/canonical_hash.cpp:42-46` 亦含 `"integrity_sha256"`。
- 现状：注释列 13 键，`sha256 / product_sha256` 为末两项；权威 Python 清单与 C++ 实现均为 14 键（含 `integrity_sha256`）。
- 差在哪：注释清单与其自称唯一的权威清单条目集不同值——不是措辞差异，是少一个被排除的键名；DET-001 变更后清单三处只改了两处。
- 后果：以公共头为人读权威的复核者/再实现者会把 `integrity_sha256` 计入规范化科学元数据 ⇒ 同一次运行的 `canonical_sha256` 与已登记产物指纹不同值，"可复现性验收"（同文件 `:8`）判红或被判为口径漂移。
- 定级：S3（纯注释面；实现与 Python 正确，无生产触发路径 —— 缺运行时触发路径）
- 怎么算修好：`canonical_hash.h:14` 末补 `/ integrity_sha256`，并注明 `derived_integrity`、DET-001 起；**同批不必**改 `canonical_hash.cpp`（已含）与 `canonical_product_hash.py`（已含）——若反过来"统一"成 13 项则改错方向，会使 sidecar 的 `integrity_sha256` 进哈希域。
- 置信：CONFIRMED（三处清单逐字比对）

### CR-69-2 · 注释宣称"逐字节同值已被某机制守住"，但该机制只做 Python 对 Python 的比较，跨语言等值无任何跟踪门
- 轴：D（设计符合）；辅 C
- 位点：`lib/include/astrocs/core/canonical_hash.h:4-5`
  ```
  // 两者必须逐字节同值, 由 eng/tools/canonical_product_hash.py compare 与 CLI 侧
  // manifest 字段的交叉核对守住）:
  ```
- 上位依据：AGENTS.md §9「负例注入（能红能绿）…门禁/判据本身不合理时改进门禁本身」。条文面对"跨实现同值由哪个门保证"零提及——检索式：`rg 'canonical_product_hash|canonical_sha256' docs` → 命中仅 `docs/api/CLI_PROTOCOL_V1.md`（无门定义）；`rg -i 'canonical|hash' eng/ci/checks.json` → 无命中；`rg -l 'canonical_product_hash' eng` → 仅 `eng/tools/canonical_product_hash.py` 自身。
- 现状：`compare` 的定义是 `eng/tools/canonical_product_hash.py:581` `def cmd_compare(...)`，其 `:808` 注册说明为 `"compare registered artifacts of two runs (verdict gate)"`——即用 Python 实现给两次运行的产物互相打分。C++ 侧 `canonical_product_hash_file()`（`:41` 声明）在仓库内的使用点只有 `lib/infrastructure/scheduler/src/module_adapters.cpp` 与 `lib/infrastructure/cli/commands.cpp`，`rg 'canonical_product_hash_file|kCanonicalProductHashSpec'` 命中面（排除 `run/`）里**没有任何测试文件**。
- 差在哪：被写成"已有机制守住"的不变量（两套实现同值）实际上没有被任何机制比较过；已存在的门比较的是同一套实现的两次输出。
- 后果：两套实现在规范化文本编码上任一漂移（FITS 卡尾随 `0x20/0x00` 裁剪、`%.17g` 数字格式化、域前缀 `\n`、排除键集合）都不会判红；而 C++ 才是生产路径写 manifest 指纹的一方 ⇒ 跨阶段哈希（normalize→mosaic→export 只经 manifest+哈希交换，AGENTS.md §2）与可复现性验收主张失去可比性。
- 定级：S2（缺陷事实 CONFIRMED，但"产物错值"需漂移先发生；缺运行时触发路径，不升 S1）
- 怎么算修好：二选一或都做——① 新增合同/集成测试：对同一组合成产品（FITS + JSON + properties），Python `hash` 子命令与 C++ `canonical_product_hash_file()` 的 `canonical_sha256`、`integrity_sha256` 必须字符串相等，并注册进 `eng/ci/checks.json`（同时补一条篡改 `integrity_sha256` 值应判红的负例）；② 把 `:4-5` 注释改成如实描述（同值由共读同一 spec 保证，当前无自动化门）。不可只改注释选 ②，因为该不变量正被 `:8` 的验收主张依赖。
- 置信：CONFIRMED（`compare` 语义与 eng/tests 检索面均已实读核对）
### CR-69-3 · 注释称既有裁决的墙钟清单"与本清单一致"，实际两处对被排除的键取不同值（`product_sha256` 更被对方明文声明不忽略）
- 轴：C（上下级一致）
- 位点：`lib/include/astrocs/core/canonical_hash.h:18-19`
  ```
  // 依据与外部标准引用见 spec 文档与工具头注释; 部署内既有裁决
  // eng/tools/quality/compare_products.py（P26 T3, 负责人裁决 4）的墙钟清单与本清单一致。
  ```
- 上位依据：被引用方原文 `eng/tools/quality/compare_products.py:49-62`（`CLOCK_KEYS_TEXT` = `hips_creation_date, hips_update_date, hips_release_date, created_utc, generated_utc, timestamp_utc, run_id, started_utc, ended_utc`，共 9 项）与 `:77-79`「明确不忽略: …与 `product_sha256`(由内容派生) —— 路径差异属运行簿记, 不以'墙钟'名义忽略」。
- 现状：本清单（canonical_hash.cpp:42-46 / canonical_product_hash.py:135-226）为 14 项，比对方多 `elapsed_sec`、`finished_utc`、`sha256`、`product_sha256`、`integrity_sha256`；其中 `product_sha256` 正是对方明文拒绝忽略的那一个。
- 差在哪：一句话把"墙钟子集重合"说成"清单一致"，并据此为一个双方取值相反的键（`product_sha256`）背书。
- 后果：任何按该注释认为"两个工具对易变键的口径相同"的人，会在跨工具比对哈希差异时错误归因（以为 `product_sha256` 造成的差异属墙钟而可忽略，实则 compare_products 会把它报成差异项），或在改清单时误以为只需同步一处。
- 定级：S3（注释/文档面；两工具各自行为未因此错，缺运行时触发路径）
- 怎么算修好：把 `:19` 改成"其**墙钟**清单是本清单 wall_clock 类别的子集；本清单另含 `derived_integrity`/`telemetry` 两类（见 canonical_product_hash.py:96-98 分类），`product_sha256` 在 compare_products 侧属**不忽略**项，两套清单服务两个目的"。不必改代码；若改代码统一清单，必须同批改 `compare_products.py:77-79` 的说明与其负例，否则改对反而判红。
- 置信：CONFIRMED（两处清单与相反声明均逐字读得）

## checkpoint.h（80 行，整读；并核其实现 `lib/infrastructure/scheduler/src/checkpoint.cpp`、单测 `eng/tests/unit/core_checkpoint_test.cpp`）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| checkpoint.h:23 | `CheckpointEntry::seq` 初值 | `0` | 条目序号，0 作"未编号"哨兵 | 结构性不适用 | 序号计数器初值，不进科学语义；实现侧赋值为 `entries_.size() + 1`（checkpoint.cpp:53），即 1 起计数，0 只表示"从未 begin/commit" | — | CONFIRMED |
| checkpoint.h:22,38,56 | `scope` 空串哨兵 | `""` = 无作用域 | 阶段隔离键的缺省值 | 结构性不适用 | 字符串哨兵约定而非阈值；语义在实现 checkpoint.cpp:23-28（中途换 scope 拒绝）与 :62（按当前 scope 查） | — | CONFIRMED |
| checkpoint.h:58,61 | `key()` 分隔符 | `"\x1f"`（US，0x1F） | scope 与 node_id 的复合键分隔 | 结构性不适用 | 它是 ASCII 控制字符 Unit Separator，属编码约定/位移量类常数，不进科学语义。**注**：不是"不可撞"——`scope="a"`+`node="b\x1fc"` 与 `scope="a\x1fb"`+`node="c"` 生成同一键；node_id/scope 均来自内部节点命名，无外部注入面 | — | PARTIAL（未核所有生产 node_id 取值域是否可能含 0x1F） |
| checkpoint.h:26 + checkpoint.cpp:44 | 半成品门 | 工件数 `>= 1` | "不接受半成品"的最小产物数 | 结构性不适用 | 边界是"有无产物"的存在性判据（`artifact_ids.empty()`），不是可标定阈值；无科学量 | — | CONFIRMED |
| checkpoint.h:72 | `expected_schema_version` 初值 | `0` | 期望 schema 版本未填 | 待确认 | 条文面零提及：`rg -n 'CORE-007'`（排除 `run/`）只命中本头、其实现与 `eng/tests/unit/core_checkpoint_test.cpp`，docs/ 与 eng/ci 无对应合同条目。实现侧 checkpoint.cpp:113-114 把它当**真实期望值**参与比较（`d.schema_version != w.expected_schema_version` ⇒ 拒），故 0 不是"不校验"而是"要求版本恰为 0"。保守方向：默认应为"必填/未填即拒"而非与 0 比较；影响范围 = 所有忘填 version 的 resume 绑定会被判成 schema 不匹配（偏红，不误绿） | 偏红方向（假失败），不构成放行 | CONFIRMED |
| checkpoint.h:73 | `expected_sha256` 注释 | `64 hex` | 摘要文本宽度 | 公式导出 | 256 bit ÷ 4 bit/hex = 64；且实现确实强制非空（checkpoint.cpp:121-124）后逐字符比较（:125） | — | CONFIRMED |

### findings

### CR-69-4 · `replay_same_run` 的覆盖分支绕过 `commit_node` 的三项校验，可把已提交节点的工件清单清空且仍算"已提交"
- 轴：R（运行时正确）；辅 D
- 位点：`lib/infrastructure/scheduler/src/checkpoint.cpp:82-89`
  ```
  if (index_.count(k)) {
    auto& e = entries_[index_[k]];
    e.artifact_ids = artifact_ids;
    ...
    return Result<void>::success();
  }
  return commit_node(node_id, artifact_ids);
  ```
- 上位依据：`lib/include/astrocs/core/checkpoint.h:26`「CheckpointStore: 故障注入后只重跑未提交节点; **不接受半成品**」；同文件 `:50`「幂等: 相同 run_id 重复 commit 同一节点 -> 覆盖为同一结果(不报错)」——"覆盖为同一结果"，不是"覆盖成空"。`commit_node` 自身强制三件事（checkpoint.cpp:37-47）：`begin()` 已调用、`node_id` 非空、`artifact_ids` 非空。
- 现状：覆盖分支直接赋值，三项校验一项都不走；`replay_same_run("node-f", {})` 在节点已提交时返回 success，把 `e.artifact_ids` 置空，而 `index_` 条目仍在 ⇒ `is_committed("node-f")` 仍为 true。`node_id` 为空同样被覆盖分支接受（空键 `scope+"\x1f"+""`）。
- 差在哪：头注释承诺的"不接受半成品"只有一条路径成立，另一条被命名为恢复专用（`replay_*`）的路径恰好绕开它——即最需要成立的时候（重放）不成立。
- 后果：任何据 `is_committed()` 跳过节点、据 `artifact_ids` 组装下游输入的调度，都会跳过"已提交但工件清单为空"的节点 ⇒ 下游拿不到输入或被静默跳过，产物缺帧而 trace 显示该节点已完成。
- 定级：S2（缺陷事实 CONFIRMED；**缺运行时触发路径**——见 CR-69-5，本类当前无生产调用点，故不升 S1）
- 怎么算修好：覆盖分支改走同一校验（`run_id_` 非空、`node_id` 非空、`artifact_ids` 非空；建议提取 `validate_commit_args()` 供两路共用），并在 `eng/tests/unit/core_checkpoint_test.cpp` 补：① `replay_same_run(existing, {})` 必须 failed 且原清单保留（负例）；② `replay_same_run(existing, other)` 覆盖后 `size()` 不变（现有 `:59` 已覆盖）。同批必须一起补的正例：`test_replay_new_node_commits`（`:63-69`）行为不变，否则新校验会把"重放即提交新节点"判红——该行为本身是否要保留见 CR-69-5 的口径问题。
- 置信：CONFIRMED（逐行读得两分支差异；未实跑，实跑判据见文末）

### CR-69-5 · 本头宣布的"故障注入后只重跑未提交节点"没有接进任何生产执行路径，只有实现 + 测试
- 轴：D（设计符合：点名能力只有实现未被使用）
- 位点：`lib/include/astrocs/core/checkpoint.h:26`
  ```
  // CheckpointStore: 故障注入后只重跑未提交节点; 不接受半成品
  ```
- 上位依据：AGENTS.md §2「阶段间只通过磁盘产品 + manifest + 哈希交换」、§6「不用 facade/空骨架/no-op 冒充实现完成」；docs 侧条文面零提及——检索式：`rg -n 'CORE-007'`（排除 `run/`）→ 仅 4 处，全在 lib 与 eng/tests；`rg -in 'RT-007' docs/**/*.md` → 仅 `docs/interfaces/data/DATA-003_PRODUCTION_ARTIFACT_STORE.md:105`（且是**排除**语句：「不做 RT-007 checkpoint 表」）。
- 现状：包含 `astrocs/core/checkpoint.h` 的编译单元只有 `eng/tests/unit/core_checkpoint_test.cpp:2` 与 `eng/tests/runtime/integration/test_rt007_cancel_resume.py:42`（生成测试源码）；`CheckpointStore` / `validate_resume_inputs` 的引用面（排除 `run/`）除声明与本实现外全是测试。`lib/include/astrocs/core/checkpoint.h` 亦被前批登记为未覆盖头（`独立审计/批次清单/AUD-401/uncovered_hdr.txt:138`）。
- 差在哪：类注释描述的是一个运行时保障（故障后按节点粒度恢复），但该保障的调用方在仓库内不存在；`validate_resume_inputs` 所要求的 `ResumeBinding` 也无人构造。
- 后果：对外若主张"支持断点续跑/取消后恢复"，实际生产恢复只能靠 manifest + 哈希重扫，checkpoint 表不参与 ⇒ 注释给出的能力面与产品实际行为面不一致（不致错值，致主张不成立）。
- 定级：S2（结论 CONFIRMED，但"该不该由这个类承担恢复"需负责人对 RT-007 的范围裁决，故不升 S1）
- 怎么算修好：三选一并同批做完——① 若 RT-007 的恢复归生产：在调度器 run 循环里按 `is_committed_in_scope()` 跳过已完成节点、在提交工件后经 `validate_resume_inputs()` 校验下游输入，并补端到端负例（注入半成品 ⇒ 不跳过）；② 若恢复只靠 manifest：把 `:26` 改写为"（进程内）节点提交记录，供测试与 trace 使用；跨进程恢复由 manifest+哈希重扫承担"；③ 若该合同作废：连 `eng/tests/unit/core_checkpoint_test.cpp` 与 `test_rt007_cancel_resume.py` 场景 4 一并退役，不留半接线骨架。
- 置信：CONFIRMED

### CR-69-6 · `CORE-007` 合同号在文档面与门禁注册面无条文，头的注释即规范本身（自证）
- 轴：S（支撑）
- 位点：`lib/include/astrocs/core/checkpoint.h:1`
  ```
  // ACSD Core Contracts — CORE-007 Checkpoint 与幂等恢复
  ```
- 上位依据：AGENTS.md §1 要求沿 `ASTROCS_DESIGN.md → docs/design → docs/plugins → docs/contracts` 逐层下钻找到该事的规范；§1.1「规范不存在：先在最高设计或它指向的那一层补上规范」。检索式与结果：`rg -n 'CORE-007'`（排除 `run/`）→ 仅 checkpoint.h:1/:35、checkpoint.cpp:1、core_checkpoint_test.cpp:79/82（PASS/FAIL 文本）；`rg -in 'checkpoint' docs/**/*.md` → 仅 `DATA-003:105`（排除性）、`docs/modules/phase2_upm.md:218`「无段内 checkpoint」、CI 规范里的"人工 checkpoint"（同词不同义）；`eng/ci/checks.json` 搜 `canonical|hash` 亦无 checkpoint 门项。
- 现状：scope 隔离规则、run 边界拒绝、幂等语义、`ResumeBinding` 四要素全部只写在代码注释里，无一处 docs 条文与之对应，也无门禁 ID 与 `eng/ci/ID_MIGRATION_MAP.md` 登记项对应。
- 差在哪：一个被标成"Core Contracts"的行为合同只有实现面一处定义 ⇒ 无法判定实现是否偏离合同，注释改了合同也不会有人发现（本批 CR-69-4 正是这种无锚定义被绕过的实例）。
- 后果：审计与后续变更失去判据：任何对 scope/幂等语义的改动都不会判红；对外"幂等恢复"主张无文档支撑。
- 定级：S3（文档/规范面缺失，不直接致产物错值）
- 怎么算修好：在 `docs/design/` 或 `docs/contracts/` 补一节 CORE-007（节点提交原子性顺序、scope 隔离与 run 边界、幂等重放定义、resume 输入门四要素、半成品拒绝），并在 `eng/ci/ID_MIGRATION_MAP.md` 为 `core_checkpoint_test` 与 rt007 场景 4 登记检查 ID；代码不必改。
- 置信：CONFIRMED（四条检索式全部实跑）

### CR-69-7 · `:41` 的 RT-007 注释挂在 `is_committed()` 上，描述的却是下一个函数的语义
- 轴：C（上下级一致）
- 位点：`lib/include/astrocs/core/checkpoint.h:41-44`
  ```
  // RT-007: 仅当节点属于给定 scope 才视为已提交（跨 scope 误读检测）。
  bool is_committed(const std::string& node_id) const;
  bool is_committed_in_scope(const std::string& node_id,
                             const std::string& phase_scope) const;
  ```
- 上位依据：实现 `checkpoint.cpp:59-68`——`is_committed()` 无 scope 参数，转调 `is_committed_in_scope(node_id, scope_)`；"给定 scope"只有后者才成立。
- 现状：注释位于无参版本之上，读到的口径是"这个函数做跨 scope 误读检测"，实际它只是按当前 scope 查询；跨 scope 的节点对它返回 false，不产生任何"检测/报错"。
- 差在哪：注释主语错位一行，把带参函数的语义安到无参函数上；且"误读检测"暗示会报错，实际行为是静默 false。
- 后果：调用方以为用 `is_committed()` 即可捕获阶段串读，而真发生跨 scope 误读时（用错 scope 起 run）只会得到"未提交"⇒ 重跑该节点，不报错——静默行为被注释写成了检测行为。
- 定级：S3（注释面；实际无生产调用方，见 CR-69-5）
- 怎么算修好：把 `:41` 那行移到 `:43` 上方，并给 `is_committed()` 写实际语义："按当前 `scope_` 查询；跨 scope 已提交者返回 false（不报错）"。
- 置信：CONFIRMED

### CR-69-8 · 仓库内并存第二套 checkpoint（阶段/帧粒度、落盘、手写 JSON），与本头的节点/scope 粒度合同无互指
- 轴：C（同一件事两处口径）；辅 D
- 位点：`lib/include/astrocs/core/checkpoint.h:1`（自称"CORE-007 Checkpoint 与幂等恢复"）对照 `lib/infrastructure/pipeline/orchestrator/cpp/include/checkpoint.h:49` `class CheckpointManager`
- 上位依据：AGENTS.md §2（三命令 + 阶段间只经磁盘产品/manifest/哈希交换）、§7 目录落位（`lib/infrastructure/pipeline/` 为基建命名块）；`docs/modules/phase2_upm.md:218`「无段内 checkpoint」。两套都在构建图内：根 `CMakeLists.txt:485` `add_subdirectory(lib/infrastructure/pipeline/orchestrator/cpp)`、`:1128` 注册其测试。
- 现状：`CheckpointManager` 以 `<output_dir>/.checkpoint/<frame>.json` 持久化、按 `frame_name` + `stage_id` 记录进度（其 `get_resume_stage`：不存在返回 0、`fully_completed` 返回 -1、否则 max+1，见 `lib/infrastructure/pipeline/orchestrator/memory.md:303`），并自带 `json_get_string/json_get_int/json_escape` 手写解析（`orchestrator/cpp/src/checkpoint.cpp:34-260`）；本头的 `CheckpointStore` 则是进程内、节点粒度、带 run_id/scope 隔离、不落盘。两者的键、粒度、持久化、恢复语义都不同，且互相不引用、无一份文档说明谁是权威。
- 差在哪：同名概念两套不相容口径同时出货，"checkpoint"在仓库里指两件事；`docs/` 无任何一处裁决（phase2_upm 说"无段内 checkpoint"，而 `.checkpoint/*.json` 就是段/阶段级落盘检查点）。
- 后果：对外说"支持断点续传"时无法指明是哪条路径；测试面上后一套的门已登记为失败：`artifacts/evidence/release-05/FAILCLOSED_SURVEY.md:48`「`CTEST-ORCH-CHECKPOINT-UNITS | CHK-CONTRACT-TEST | 1 | 否 | 否 | FAIL(missing_output)`」。
- 定级：S3（本条只指控"无裁决/无互指"这一事实，不断言哪套行为错值；两套各自行为需分别审）
- 怎么算修好：先由负责人裁决 checkpoint 的单一权威（建议：磁盘面 = manifest/产品哈希，`.checkpoint/` 属运行簿记需明文登记为不入库且不入合同），再在两个头的注释里互指并写明适用域；同批把 `CTEST-ORCH-CHECKPOINT-UNITS` 的 missing_output 修到能判绿或按 CONTROL_PACK_SPEC 退役其注册项——只加注释不修门会留一个恒红/恒缺项。
- 置信：PARTIAL（缺：`.checkpoint/` 是否真被生产 orchestrator 路径写入并影响产物——需读 `orchestrator.cpp` 的 `run_single` 或实跑，本轮按 §5 上限未展开）
## context.h（266 行，整读；并核实现 `lib/infrastructure/scheduler/src/context.cpp`、消费者 `runtime.cpp` / `cli/commands.cpp` / `executor.cpp`、合同 `eng/contracts/resource_gate_v1.json`）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| context.h:19 | `windows.h` 的 `ERROR` 宏值 | `0` | 说明宏冲突的事实 | 文献值 | Windows SDK `winuser.h`（MessageBox 图标常量 `ERROR`=0）；本仓无法在 Linux 节点复验，冲突本身由注释所指 `WIN-001 Fatduck MSVC 复现` 背书——该复现件不在本批读面 | — | PARTIAL（缺 WIN-001 复现件原文） |
| context.h:28 | `LogLevel` 序值 | `DEBUG=0 INFO=1 WARN=2 ERROR=3` | 日志级别枚举 | 结构性不适用 | 枚举基数与序值，不进科学语义；但序值**承载过滤方向**（`logging.cpp:24-27` 名字映射、`Logger(min_level)` 数值比较），名字与默认值域锚 `docs/design/LOG_AND_ERROR_SYSTEM.md:170`「`log_level` \| `info` \| `debug\|info\|warn\|error`」（该文档不给数值，序值是实现选择） | — | CONFIRMED |
| context.h:32-38 | `Metrics` 八字段初值 | `0`（`wall_us/cpu_us/peak_rss_bytes/bytes_read/bytes_written/active_workers/completed_units`） | "未观测"哨兵 | 结构性不适用 | 观测累加器零点，非阈值；与 `resource_gate_v1.json:22` `"sentinel_unobserved": 0` 同口径 | — | CONFIRMED |
| context.h:65 | `acquired()` 判据 | `size_ > 0` | 租约是否非空 | 结构性不适用 | 空/非空存在性判据（0=空租约哨兵），无经验量 | — | CONFIRMED |
| context.h:100-102 | `GrantedWorkerObservation` 三初值 | `0 / 0 / 0` | 未观测哨兵 | 文献值 | 由合同钉住：`eng/contracts/resource_gate_v1.json:20-24`（`"primary": "granted_workers_peak"`, `"sentinel_unobserved": 0`, `"fallback": "min(selected_workers, available_cpus)"`），`:36` 明文写"本取义与 lib/include/astrocs/core/context.h:93-103…同源" | — | CONFIRMED |
| context.h:112-116 | `AcquirePolicy` 序值 | `BLOCK=0 NONBLOCK=1 BEST_EFFORT=2` | 预留策略枚举 | 结构性不适用 | 枚举基数；**注**：生产侧只用 `NONBLOCK`（`context.cpp:238`），`BLOCK`/`BEST_EFFORT` 的读者只有 `eng/tests/unit/rt002_budget_test.cpp:112/:135` | — | CONFIRMED |
| context.h:130 | `acquire()` 缺省策略 | `= AcquirePolicy::NONBLOCK` | 未写策略时的默认 | 待确认 | 条文面零提及：`rg -n 'RT-002|AcquirePolicy' docs` 无该默认值规定（唯一成文处即本头 `:124-128` 自述）。保守方向：默认 NONBLOCK = 预算不足即静默空租约（偏串行、偏慢，不偏错值）；影响范围 = 所有未显式给策略的调用点 | 偏"静默降级为 1 线程" | PARTIAL（缺 RT-002 合同原文，`docs/` 内检索不到） |
| context.h:142 | `available_` 初值 | `0` | 未 reset 前无可用 token | 待确认 | 与 `:136 reset_available()` 配对，注释把调用者限定为"仅 create_thread_budget 调用一次"（当前唯一直接构造点确为 `context.cpp:209-210`），但 `ThreadBudget` 构造函数 `:120` 是 public 且不强制 reset ⇒ 见 §CR-69-13。保守方向：0 初值偏"拒绝授予"（不超卖，偏红/偏慢），不会误绿 | — | CONFIRMED |
| context.h:158-159 | 注释内实测值 | `16 槽` / `cap=1` / `14.1 s` | P7-UTIL-002 改动理由的实测数据 | 待确认 | 跟踪面零锚：`rg -n '14\.1 s|独占 16 槽'`（排除 `run/`）只命中本注释与 `docs/architecture/execution_inventory.csv:3`（该行是被审对象自己的登记行，非测量件）；`reports/`、`artifacts/evidence/` 无对应标定件。保守方向：即使该实测为真，份额公式 `budget/同批在途节点数` 的正确性不依赖它；影响范围 = 该改动的证据链（不能指 `run/`，§2.5 第 3 档要求跟踪面标定件） | 若数字错，只削弱改动理由，不改判据 | PARTIAL（缺可复核测量件；若要定案需跑 `eng/tools/monitoring/node_waterfall.py` 对照 trace） |
| context.h:160 | `连续 10 s` + `只有一个活跃计算线程即失败` | `10 s` / `1` | 引用"宪章 §10.5"的失败判据 | 待确认 | 见 §CR-69-10：所引文件已废止且不在跟踪集（`git ls-files "*CONSTITUTION*"` → 空）；现行成文处 = `ASTROCS_DESIGN.md:357`「内存、CPU、线程不设门」+ `resource_gate_v1.json:39-42`（`min_active_compute_threads: 2`，统计量 `p50`）与 `:50-53`（`60%` 配 `10 s` 窗，`hard_fail`）。**10 s 在本注释里被绑到了错误的判据上** | 保守方向：按合同，单活跃线程判据**无** 10 s 窗（p50<2 即红），10 s 属"队列有工作且低于 60%"那条；影响范围 = 并行退化排障与 CI 重计算门（`CHK-RESOURCE`） | CONFIRMED（口径差异本身逐字可核） |
| context.h:232 | `acquire_lease` 的 `min` | `1`（`context.cpp:238` `b->acquire(1u, want, NONBLOCK)`；`:232` `requested==0 → 1`） | 最小授予 token 数 | 结构性不适用 | 存在性下界（至少 1 才叫拿到租约），不是可调科学阈值 | — | CONFIRMED |
| context.h:257 | `thread_budget_` 默认 | `1` | 未注入预算时的配置字段值 | 待确认 | 条文面零提及（`rg -n 'CORE-005' docs` → 无该默认值）；生产侧该字段**无任何读者**（`rg -n 'thread_budget\(\)'` 排除 `run/` → 只有本头 `:200` 的 getter 与 `eng/tests/unit/core_context_test.cpp:45`）。保守方向：默认取 1 是所有可能值里最保守（最窄）的，不会超卖；影响范围 = 仅 §CR-69-14 的死通道 | 偏保守 | CONFIRMED |

### findings

### CR-69-9 · 本头只暴露进程级单调峰值，`runtime.cpp` 把它当"本节点并行宽度"写进 trace ⇒ 每节点 `workers`/`granted_workers` 字段系统性偏高
- 轴：R（运行时正确）；辅 D
- 位点：`lib/include/astrocs/core/context.h:99-103`
  ```
  struct GrantedWorkerObservation {
    std::atomic<uint32_t> peak_active{0};     // max 瞬时授予租约数
    std::atomic<uint32_t> peak_lease{0};      // max 单次租约 size
    std::atomic<uint64_t> acquired_total{0};  // 成功 acquire 次数
  };
  ```
- 上位依据：同头 `:93-98`（B2-A18/GAP-06/BASE-F3：「此前 Runtime trace 写的是配置 budget…配置不得冒充观测」——即本结构的成立理由就是"trace 里必须是真实观测"）；消费者注释 `lib/infrastructure/scheduler/src/runtime.cpp:288`「本节点实际获得租约 → 峰值并行宽度」。
- 现状：结构体三个量全部是**进程级、只增不减**的汇（`context.cpp:133-144`：`peak_lease` 由 CAS 取历史最大；无 per-run/per-node 复位接口，`granted_worker_observation()` 是函数局部 static，`:127-130`）。`runtime.cpp:289-292` 用「本节点期间 `acquired_total` 有增量」作条件，取的值却是 `obs_post.peak_lease`——全进程历史单次最大租约；随后 `:321` `tr.workers = node_granted;`、`:325` `tr.granted_workers = node_granted;` 原样写进每节点 trace。
- 差在哪：一个"全 run 历史最大值"被赋给"该节点的并行宽度"这个逐节点量。只要 run 里较早有一个节点拿到过 16 宽租约，之后**每一个**至少 acquire 一次的节点都被登记成 16——哪怕它实际只拿到 1。
- 后果：产物字节失真——trace 事件（RT-006，随 run 落盘并被 `eng/tools/monitoring/node_waterfall.py` 等读取）里每节点 `workers`/`granted_workers` 偏高；**不等式方向定清了**：`peak_lease >= 本节点真实租约`（单调最大只会 ≥），故偏差**单向偏高** ⇒ 任何以逐节点 `workers` 为输入、方向为"宽度太小即红"的判据（"单活跃计算线程"，`resource_gate_v1.json:39`）**只会误绿不会误红**；P7-UTIL-002 想防的"先派发节点独占预算、后派发节点饿成 1 线程"（`:156-160`）在观测面上被抹平，看不到。
- 定级：S1（触发路径逐跳：`lib/infrastructure/cli/commands.cpp`（三命令入口）→ Scheduler/Runtime 节点循环 `lib/infrastructure/scheduler/src/runtime.cpp:281-292` → `tr.workers/tr.granted_workers` 赋值 `:321/:325` → `RunContext::record_trace`（`context.h:244`）→ trace 落盘；观测结构定义在本头 `:99-103`）
- 怎么算修好：给"逐节点授予宽度"一个真实量而非借用全局峰值——最小改法：`RunContext::acquire_lease()`（`context.cpp:226-239`）在成功返回前把 `lease.size()` 累计进 thread_local「当前节点最大授予宽度」（复用同文件既有的 `trace_set_current_node` thread_local 模式，`:215-221`），由 `runtime.cpp` 在节点 end 读取并复位；本头相应新增 `uint32_t node_granted_peak()` 类接口并在 `:96-97` 注明"进程级汇只用于全 run 分母，不得用于逐节点归因"。同批必须一起改的东西：① `runtime.cpp:288` 注释与 `:289-292` 取值；② 已有钉值处（`eng/tests/unit/mon001_gate_test.cpp:264` `const uint32_t peak0 = obs.peak_active.load(...)`）若改成断言逐节点量需同步，否则改对反而判红；③ `resource_gate_v1.json:21` 的 `primary: granted_workers_peak` 语义**只指全 run 分母**，不要顺手把分母也换成逐节点量（分母换小会把利用率判红）。
- 置信：CONFIRMED（观测量的累计方式、无复位、消费点赋值三处逐字读得；未实跑对照 trace 字段）

### CR-69-10 · 本头两处把"线程并行宽度失败判据"的权威写成已废止、且已从跟踪集删除的"宪章 §10.5/§17.6"，并把两条判据合并错配了 10 s 窗
- 轴：C（上下级一致）；辅 S
- 位点：`lib/include/astrocs/core/context.h:160` 与 `:95`
  ```
  :160  // （违反宪章 §10.5「任何连续 10 s 只有一个活跃计算线程即失败」）。
  :95   // 写入 set_workers(budget,budget)（配置不得冒充观测, 宪章 §10.5/§17.6）。
  ```
- 上位依据：`docs/architecture/MODULE_MAP.md:82`「冻结阈值判定（现行 = `ASTROCS_DESIGN.md` §8 + `eng/contracts/resource_gate_v1.json`；**原引「宪章 §10.5/§18.2」已废止**）」；`docs/validation/v6/ORACLE_AND_ZERO_CASE_POLICY.md:1`「该宪章（`ASTROCS_PROJECT_CONSTITUTION.md`）已废止（ROOT-007 删除），**不构成现行依据**」；跟踪性核对：`git ls-files "*CONSTITUTION*"` → 空输出；`ASTROCS_DESIGN.md:357`「资源门只管磁盘：…**内存、CPU、线程不设门**」；`resource_gate_v1.json:39-42`「`min_active_compute_threads: 2`, `statistic: "p50"`」（**无 10 s 窗**）与 `:50-53`「`queue_low_utilization_percent: 60`, `queue_low_window_seconds_min: 10`, `queue_low_window_enforcement: "hard_fail"`」（10 s 属于这条）；`:83-87` 硬失败清单三条。
- 现状：注释把"单活跃计算线程"与"连续 10 s"合成一句、并把"即失败"归给一个已废止且仓库里不存在的文件；`runtime.cpp:320`、`module_adapters.cpp:353` 又把同一句以同样方式再引一遍。
- 差在哪：①权威锚错（废止文件 vs 现行 §8 + 合同 JSON）；②数值绑错判据：现行合同里"单活跃线程"判据是 p50<2、与窗口长度无关，10 s 窗挂在"利用率<60% 且队列有工作"那条。
- 后果：按本头注释理解门的人会认为"串行 9 秒不算违反"，而真实判据是整段计算区间的活跃线程 p50；同时"线程不设门"（最高设计 `:357`）与注释里的"即失败"打架——出错时改判据方向还是改代码方向无法判断。
- 定级：S3（注释面；真实门在 `run_monitored.py`/`resource_gate_v1.json` 侧按合同执行，未因此注释而错值）
- 怎么算修好：把 `:95`、`:160` 的锚改为现行权威链——`docs/architecture/MODULE_MAP.md:82` 指明"现行 = `ASTROCS_DESIGN.md` §8 + `eng/contracts/resource_gate_v1.json`"，其中"内存、CPU、线程不设门"逐字在 `ASTROCS_DESIGN.md:357`（§4.5 运行前预检），数值在 `resource_gate_v1.json:39-53`；按合同把两判据分开表述。同批把 `runtime.cpp:320`、`module_adapters.cpp:353` 的同一错引一起改（三处同源，只改一处会留下重复错锚）。不必改任何数值实现。
- 置信：CONFIRMED

### CR-69-11 · 本头"哨兵 0 不得以配置值回填"与合同允许的 fallback 口径不同，而生产实现正好做了本头禁止的回填
- 轴：C（同一阈值两处入口取不同值/两套口径）
- 位点：`lib/include/astrocs/core/context.h:98`
  ```
  // 哨兵纪律: 0 = 未观测（不是合法并行宽度），不得以配置值回填。
  ```
- 上位依据：`resource_gate_v1.json:20-34`（`denominator`）：`:22` `"sentinel_unobserved": 0`；`:23` `"fallback": "min(selected_workers, available_cpus)"`；`:26-28` `"forbid": ["以配置值冒充观测（哨兵 0 不得回填配置预算）", …]`；`:30-34` `"echo_components": [selected_workers, available_cpus, granted_workers]`；`:36` 声称"本取义与 lib/include/astrocs/core/context.h:93-103…同源"。
- 现状：实现 `lib/infrastructure/cli/commands.cpp:753-758` 读 `peak_active`，为 0 时取 `planned = std::min(budget, cli_affinity_cpu_count())`，随后 `recorder.set_workers(eff, eff)`（把回填值同时写进"观测"与"计划"两栏）。合同一面允许 fallback 但要求回显三分量、并禁止"冒充观测"；本头一面写"不得以配置值回填"（无条件）。
- 差在哪：三面对同一件事各说一句：头＝绝不回填；合同＝允许 fallback + 必须回显；代码＝回填且把同一值写进两栏（回显面被压成"看起来观测=计划"）。`:36` 的"同源"声明因此不成立。
- 后果：`allocated_capacity` 分母在"未观测到任何租约"的 run 里被抬成配置预算 ⇒ 利用率类判据（`per_sample_utilization_min_percent`、`queue_low_utilization_percent`）分母变大、比值变小（偏红），而"回显两栏相等"又让审计看不出这是回退值——既掩盖 fallback 发生、又可能把"确实没并行"的 run 判成"并行宽度满配"，方向取决于哪个消费方读哪一栏。
- 定级：S2（口径冲突事实 CONFIRMED；哪一面应为权威属合同/裁决面，且被审文件是注释；`commands.cpp` 的完整判红路径需 CR-6x 那批的采样/登记面才能定，故不升 S1）
- 怎么算修好：先裁决单一权威（建议：合同 `denominator` 为准）；① 本头 `:98` 改写为「哨兵 0 表示未观测，**不得登记为观测**；分母可取合同 `denominator.fallback`，但须同时回显 `selected_workers/available_cpus/granted_workers` 三分量」；② `commands.cpp:758` 的 `set_workers(eff, eff)` 改为区分"观测值 / 回退值"两个通道（若 recorder 无此面则补），并按 `:30-34` 回显；③ 补一条负例：全程零租约的 run 必须能看到"分母来自 fallback"的标记，否则判红。
- 置信：PARTIAL（缺：`recorder.set_workers` 两参数的登记语义与最终 JSON 字段名，需读 `cli/resource_gate.h` 生成面与一份真实 `resource_summary.json`）

### CR-69-12 · 作为改动理由的"实测缺陷"数字（16 槽 / cap=1 / 14.1 s）在跟踪面无任何标定件
- 轴：S（支撑）
- 位点：`lib/include/astrocs/core/context.h:158-160`
  ```
  // 实测缺陷（修复前）：photometry 独占 16 槽 → wcs-platesolve 得空租约
  // cap=1 → 该节点内 ipv triangle_match 与 star_detector 检测全链路单线程 14.1 s
  ```
- 上位依据：§2.5 第 3 档「`实验标定` —— 给标定件路径与结果位（必须落在跟踪面，指 `run/` 即判不合格）」。
- 现状：检索式与结果——`rg -n '14\.1 s|独占 16 槽'`（排除 `run/`）→ 仅 `context.h:158-159` 与 `docs/architecture/execution_inventory.csv:3`（登记行本身）；`reports/`、`artifacts/evidence/` 内无该次测量的 trace/瀑布图/结果 JSON；机器门里也无对应检查 ID。
- 差在哪：一句话把一次未落盘的测量写成"实测"，后续任何人无法复算 14.1 s 与"独占 16 槽"的因果，也无法确认份额公式修复后该数字确实改善。
- 后果：不致产物错值，但 P7-UTIL-002（份额收缩逻辑，`context.cpp:236-237`）的正确性主张缺证据锚；若日后回归到单线程，没有"修复前 14.1 s / 修复后 X"的对照基线可判。
- 定级：S3
- 怎么算修好：把该测量补成跟踪面证据（一份节点瀑布/trace 对照，路径写进注释），或在注释里把"实测"降级为"缺陷形态（未复算）"；若要定案，需跑 `eng/tools/monitoring/node_waterfall.py` 对同一 IR 开/关 `set_dispatch_budget_hint` 各一轮（属实跑项，见文末）。
- 置信：CONFIRMED（检索面即结论）

### CR-69-13 · `ThreadBudget` 公共构造 + `available_` 初值 0 ⇒ 不经工厂构造的预算对象永远静默授予空租约
- 轴：R（静默回退）
- 位点：`lib/include/astrocs/core/context.h:120` 与 `:142`
  ```
  explicit ThreadBudget(uint32_t budget) : budget_(budget) {}
  ...
  std::atomic<uint32_t> available_{0};
  ```
- 上位依据：同头 `:135`「工厂初始化（仅 `create_thread_budget` 调用一次；非并发阶段）」；`:110-111`「`acquire(min,max,policy)` 原子预留 token；`sum(active)<=budget` 全局不超卖」——"不超卖"成立，但"可授予"不成立。
- 现状：`available_` 只在 `reset_available()`（`:136-138`）里被置为 `budget_`，而该函数是 public、无编译期强制；构造函数 public。当前唯一构造点确为工厂（`context.cpp:209-210`，`make_shared` 后紧跟 `reset_available()`），但任何新代码 `ThreadBudget b(16);`（含测试、含未来注入路径）得到的对象 `available()==0`，`acquire()` 走 `context.cpp:158-172`：`cur=0 < min=1` 且非 BEST_EFFORT ⇒ 返回空租约，`BLOCK` 策略则在 `:192` 的 CV 谓词上**永久阻塞**（谓词 `available_ >= min` 永不成立，且无人归还）。
- 差在哪：一个"配置了预算却零可用"的对象在类型面上合法；BLOCK 下表现为死锁，NONBLOCK 下表现为静默全串行——两者都不报错。
- 后果：若生产引入直接构造，节点全部拿到空租约 ⇒ host 串行执行（`module_adapters.cpp:588` 已描述该降级路径），性能塌方无诊断；BLOCK 路径为挂死。当前无该构造点，故不致现网错值。
- 定级：S2（缺运行时触发路径——今日无直接构造点；缺陷形态明确）
- 怎么算修好：把构造设为主私（`available_(budget)` 直接初始化，或 `friend Result<std::shared_ptr<ThreadBudget>> create_thread_budget(uint32_t)`），删除 public `reset_available()` 或改名为"仅工厂"并加断言；补负例：`ThreadBudget` 构造后 `available() == budget()`（或构造不可见则该测试改为工厂路径），以及 BLOCK 策略在零预算上必须**超时/失败**而非永久等待。
- 置信：CONFIRMED（构造—赋值—acquire 三分支逐行读得；未实跑死锁）

### CR-69-14 · `set_thread_budget` / `thread_budget()` 是被本头自己否定的"配置通道"，仍在公共 API 面上且被三处生产写入、零处生产读取
- 轴：D（设计符合：既有裁决的能力面没收口）
- 位点：`lib/include/astrocs/core/context.h:199-200` 与 `:257`
  ```
  void set_thread_budget(uint32_t budget) { thread_budget_ = budget; }
  uint32_t thread_budget() const noexcept { return thread_budget_; }
  ...
  uint32_t thread_budget_ = 1;
  ```
- 上位依据：同头 `:93-98`（配置不得冒充观测）、`:203-204`「未注入时（仅测试/非调度上下文）返回空租约（拒绝伪授权——绝不退回 `ThreadLease::make`）」；AGENTS.md §6「不硬编码线程…由 benchmark 生成的 profile 决定」、§9（不留退化面）。
- 现状：写入点 `scheduler.cpp:99`、`executor.cpp:111`（`impl_->budget ? ...->budget() : 1u`）、`executor.cpp:251`（字面 `1u`，注释称"无科学预算语义"）；读取点（排除 `run/`）只有测试 `eng/tests/unit/core_context_test.cpp:45`。同时 `:205-208 set_budget()` 也会覆写同一字段 ⇒ 两条入口写一个值，无先后约束。
- 差在哪：B2-A18 把"拿 budget 当观测"判为缺陷并改走 `granted_worker_observation()`，但作为其源的配置字段仍以公共 API 形式存在且被继续写入，没有任何编译期/门禁面阻止下一次有人读它当宽度。
- 后果：不致现下错值（无读者）；风险面是"配置冒充观测"复发时没有屏障——本批 CR-69-11 的 fallback 回填正是同一形态的近例。
- 定级：S3
- 怎么算修好：删掉 `:199-200` 与 `:257` 的默认值（连同 `scheduler.cpp:99`、`executor.cpp:111/:251` 三处写入与 `core_context_test.cpp:40-45`、`memory_pressure_test.cpp:359`、`core_scheduler_test.cpp:35/:64/:128` 的读写点一并清）；若负责人要求保留"配置宽度"作展示面，则改名为 `configured_budget()` 并在注释写明"非观测、禁止进入任何判据"。
- 置信：CONFIRMED

### CR-69-15 · 公共头里 `#undef ERROR` 是不可逆的 TU 级宏副作用，用"污染下游"的方式解决自己枚举名的碰撞
- 轴：D（设计符合）；辅 C
- 位点：`lib/include/astrocs/core/context.h:21-23`
  ```
  #ifdef ERROR
  #undef ERROR
  #endif
  ```
- 上位依据：`AGENTS.md` §2（正式平台含 **Windows x64**）；`:20` 自述「宏在 windows.h 内的用途已展开完毕, undef 仅恢复本头枚举语义 (同 hiss_reader 先例)」。
- 现状：为让 `LogLevel::ERROR`（`:28`）可用，头在自身作用域内 `#undef` 平台宏；影响面是所有包含本头的翻译单元（`artifact.h`/`contracts.h`/`trace.h` 一路到模块公共头），且不可恢复。注释假设"windows.h 宏用途已展开完毕"——该假设由**包含顺序**决定，本头无法知道自己被谁在何之后包含。
- 差在哪：用全局副作用修一个命名碰撞；碰撞的正解是不使用与平台同名的枚举项。
- 后果：Windows 构建上，任何在本头之后调用依赖 `ERROR` 宏的 Win32 API（如 `MessageBox(..., MB_ICONERROR)` 一类用 `ERROR` 的写法、或第三方头内以 `ERROR` 作参名）会得到未定义标识符而非预期 0——编译期即炸，不会静默错值；故风险限于可编译性。
- 定级：S3（无产物字节后果；Linux 面完全无感，Windows 面为构建期问题，本节点无法复验）
- 怎么算修好：把 `LogLevel::ERROR` 改名（如 `ERR`/`FATAL_LEVEL`）或在 `logging.h` 侧提供名字映射，删去 `:21-23`；若保留现名，则改为在**每个受影响的 .cpp 末尾**处理包含顺序并把该约束写进 `docs/plugins/`（同 `hiss_reader` 先例应一并登记），不要留在被全仓包含的公共头里。改名属 ABI/名字面，需与 `lib/include/astrocs/core/logging.h` 及全部 `LogLevel::ERROR` 调用点（`logging.h:59`、`context.cpp:30-32`、`logging.cpp:24-27`、`runtime.cpp:300` 等）同批。
- 置信：PARTIAL（缺 Windows/MSVC 实构建证据；本节点仅 Linux，且 `WIN-001` 复现件未在跟踪面读到）
## contracts.h（231 行，整读；并核 `lib/infrastructure/cli/exit_codes.h`、`lib/infrastructure/cli/runtime_client.cpp:482-504`、`eng/ci/ledgers/log_system_ledger.json:492-501`、`ASTROCS_DESIGN.md` §7.2）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| contracts.h:18-25 | `ErrorDomain` 序值 | `CONFIG=0 … INTERNAL=7`（8 项） | 错误域枚举 | 结构性不适用 | 枚举基数/序值（`error_domain_name()` 只做名字映射，序值不上线）；域清单本身在 `ASTROCS_DESIGN.md` §7.3 与 `docs/contracts/LOG_AND_ERROR_CONTRACT.md`（后者被 `log_system_ledger.json:500` 指为域→退出码映射的规范来源，即 §5） | — | PARTIAL（本轮未读 `LOG_AND_ERROR_CONTRACT.md` §5 全文） |
| contracts.h:44 | `ExitCode::OK` | `0` | 成功 | 结构性不适用 | `ASTROCS_DESIGN.md:500`「0 \| 成功且门禁全过」；对外接口约定值，非科学量 | — | CONFIRMED |
| contracts.h:44 | `ARGS/INPUT/SCIENCE/BACKEND/COMPUTE/IO/CANCELLED/RESOURCE` | `2 / 3 / 4 / 5 / 6 / 7 / 9 / 10` | CLI 退出码 | 结构性不适用 | 逐行锚 `ASTROCS_DESIGN.md:501-509`（2 参数或配置、3 输入缺失/格式错、4 科学验证或不变量失败、5 backend ABI/签名/CPU 特征/加载失败、6 计算执行失败、7 I/O 失败、9 用户取消或超时、10 磁盘写满/写盘失败）；与 `lib/infrastructure/cli/exit_codes.h:8-17` 数值逐字一致（本轮已核） | — | CONFIRMED |
| contracts.h:45 | `ExitCode::INTERNAL` | `70` | 未分类内部错误 | 结构性不适用 | `ASTROCS_DESIGN.md:510`「70 \| 未分类内部错误（脱敏 crash report）」；`exit_codes.h:18` 同值 | — | CONFIRMED |
| contracts.h:45 | `ExitCode::INTEGRITY` | `8` | 输出完整性/验证失败 | 待确认 | **最高设计 §7.2 退出码表无 8 行**（`ASTROCS_DESIGN.md:498-510` 逐行为 0,2,3,4,5,6,7,9,10,70），而 `exit_codes.h:15` 与本头 `:45` 都定义了 `INTEGRITY = 8`。检索式：`rg -n '退出码 10|exit 10|INTEGRITY' ASTROCS_DESIGN.md` → 无 `INTEGRITY` 命中。保守方向：8 属"实现比对外合同多出一个码"——要么补进 §7.2 表，要么禁止使用；影响范围 = 外部脚本/CI 若只按 §7.2 表分诊，会把 8 当未知码处理 | 偏"多出一个未登记出口"方向；不改判据 | CONFIRMED |
| contracts.h:88 | `Error::domain_` 默认 | `ErrorDomain::INTERNAL`(=7) | 默认构造错误的域 | 结构性不适用 | 哨兵语义"未分类"；无科学量 | — | CONFIRMED |
| contracts.h:160 | `CancellationToken::cancelled_` 初值 | `false` | 未取消 | 结构性不适用 | 布尔初值 | — | CONFIRMED |
| contracts.h:198 | `TraceEvent::type` 默认 | `MODULE_CALL`(=0) | 默认事件类型 | 待确认 | 条文面：`:196` 自称"RT-006 冻结字段"，而其规范来源 `14_RUNTIME_SCHEDULER_AND_TRACE_STANDARD.md`（`:168` 引用）**不在跟踪集**（`git ls-files "*RUNTIME_SCHEDULER*"` → 空；全仓检索仅命中引用方），故"默认值该是什么"无可读权威。保守方向：默认成 `MODULE_CALL` 会让一条字段没填全的记录看起来像"模块调用观测"，偏假绿；建议默认构造即非法（删除默认或加 `UNSPECIFIED`）。影响范围 = trace JSONL 全部记录 | 偏"未填即冒充" | PARTIAL（缺该标准文档原文） |
| contracts.h:208-210 | `call_count / workers / granted_workers` 默认 | `0 / 0 / 0` | 计数与并行宽度 | 待确认 | `0` 在本仓已有既定哨兵含义（`context.h:98`「0 = 未观测」；`resource_gate_v1.json:22 sentinel_unobserved: 0`、`:24 selected_workers_sentinel: 0`），但 `to_jsonl()` 序列化后 `workers: 0` 与"该事件类型不适用此字段"不可区分。保守方向：不适用字段应省略或写 null，不应写 0；影响范围 = 任何按 `workers>=1` 或 `workers<2` 逐节点判定的下游（`eng/tools/monitoring/node_waterfall.py`） | 0 偏"未观测"方向 → 逐节点判据若把 0 当测得值会偏红；若当缺省跳过则偏绿 | PARTIAL（需读 `to_jsonl` 实现与一份真实 trace 才能定消费面后果） |
| contracts.h:219-220 | `cpu_ms / wall_ms` 默认 | `0.0` | 计时观测 | 待确认 | 同上（0.0 既可"未计时"也可"计时为零"）；无权威规定零值语义。保守方向：与 `workers` 同一处置（区分未观测/零） | 偏"0 耗时看起来最优" | PARTIAL |
| contracts.h:218 | `artifact_size` 默认 | `0` | 发布字节数 | 结构性不适用 | 字节计数初值；`ARTIFACT_PUBLISH` 事件必填真实 size，非阈值 | — | CONFIRMED |
| contracts.h:221 | `seq` 默认 | `0` | 全局递增序号 | 结构性不适用 | 序号起点；`:221` 注释"全局递增"，0 为未编号哨兵 | — | CONFIRMED |

### findings

### CR-69-16 · 本头自称退出码"只做映射表, 不重定义数值"，实际把 11 个码值第二次字面化、该枚举全仓零使用，而生产映射给出与合同相反的码
- 轴：C（同一件事，代码／合同／最高设计不是同一个）；辅 D
- 位点：`lib/include/astrocs/core/contracts.h:42-46`
  ```
  // CLI 退出码映射 (lib/infrastructure/cli/exit_codes.h 唯一源; 此处只做映射表, 不重定义数值)
  enum class ExitCode : uint8_t {
    OK = 0, ARGS = 2, INPUT = 3, SCIENCE = 4, BACKEND = 5,
    COMPUTE = 6, IO = 7, INTEGRITY = 8, CANCELLED = 9, RESOURCE = 10, INTERNAL = 70,
  };
  ```
- 上位依据：`ASTROCS_DESIGN.md:498-510`（退出码表，对外合同；见 §7.2「CLI 是薄入口：…取消与退出码」`:490`）；`lib/infrastructure/cli/exit_codes.h:8-18`（被指为唯一源）；`eng/ci/ledgers/log_system_ledger.json:494-500` 已登记条目 `F-EXIT-MAP`：「运行时错误域→退出码映射与 `exit_codes.h` 语义不一致：`ErrorDomain::CONFIG`→default 70（合同 §5 为 2）、`ErrorDomain::BACKEND`→default 70（应为 5）、`ErrorDomain::RESOURCE`→5（`exit_codes.h` 的 `RESOURCE=10`）」，`owner`：负责人裁决，`action`：另派实现任务订正并补退出码可达矩阵测试。
- 现状：① 注释说"不重定义数值"，代码却把 11 个数值全部写成枚举初始值，两处之间无任何 `static_assert`/单一来源绑定（本轮核对：目前两处数值恰好相同，但无机制防漂移）；② `rg -n 'ExitCode::'`（排除 `run/`）→ **零命中**，即本枚举没有任何使用者；③ 真正的映射写在 `lib/infrastructure/cli/runtime_client.cpp:492-501` 的裸整数 `switch`：`DATA→2`、`SCIENCE_PRECONDITION→4`、`IO→7`、`CANCELLED→9`、`RESOURCE→5`、`default→70`（`CONFIG`、`BACKEND`、`INTERNAL` 落 default）。
- 差在哪：被声明为"映射表面"的东西既不是映射（无 domain→code 函数）、也不被使用；而承担映射的地方用字面整数并把资源类错误映射到 5（BACKEND），与 §7.2 表第 5 行、第 10 行同时不符。
- 后果：对外退出码合同失真。触发路径逐跳：预算/OOM 类失败 `ErrorDomain::RESOURCE`（如 `lib/infrastructure/scheduler/src/runtime.cpp:484` `create_runtime: budget must be > 0`、`module_adapters.cpp:13224` "out of memory"）→ `runtime_client.cpp:500` `return 5;` → CLI 进程以 5 退出 ⇒ 调用方按 §7.2 读成「backend ABI/签名/CPU 特征/加载失败」，而真实原因是资源/配额；配置错误（`CONFIG`）与后端加载失败（`BACKEND`）同样落 `:501 default: return 70`，对外读成"未分类内部错误"。**不等式/方向**：这是分类错值而非放行——它不会让失败变成功（run 仍非 0 退出），但会让 CI 分诊走错分支。
- 定级：S1（有从生产入口到码值的逐跳路径；且属"对外主张失真"类。注：错映射本身已由 `F-EXIT-MAP` 登记待裁决，本条的增量是"声明的映射表面向不存在且无防漂移绑定"）
- 怎么算修好：① 单一来源：`contracts.h` 的 `ExitCode` 要么删除（并把 `exit_codes.h` 定为唯一枚举，`#include` 而非重复定义），要么改为引用 `exit_codes.h` 的常量并以 `static_assert(static_cast<int>(ExitCode::RESOURCE) == astrocs::cli::EXIT_RESOURCE, ...)` 逐个钉住 11 项——不得只加注释；② `runtime_client.cpp:492-501` 的裸整数改成本枚举/常量；③ 按 `F-EXIT-MAP.action` 补"退出码可达矩阵测试"：对 8 个 `ErrorDomain` 逐一构造失败并断言进程码等于 §7.2 表对应行（当前 `RESOURCE→10`、`CONFIG→2`、`BACKEND→5`）；④ 若 `INTEGRITY=8` 要保留，同批把它写进 `ASTROCS_DESIGN.md` §7.2 表（否则第 3 项的矩阵测试与合同表会互相判红）。裁决归属：错映射的修法在 `F-EXIT-MAP` 上已注明"本设计不改码"，需负责人先定 8 与 5/10 的语义，再动实现。
- 置信：CONFIRMED（三处码值表逐字比对 + 零使用检索 + 登记条目实读）

### CR-69-17 · `TraceEvent::workers` 与 `granted_workers` 被声明为两个不同观测量，唯一生产写点把它们写成同一个值
- 轴：C（合同字段语义 vs 实现）；辅 R
- 位点：`lib/include/astrocs/core/contracts.h:209-210`
  ```
  uint32_t workers = 0;       // 实际 workers（租约数；node_end/worker_task）
  uint32_t granted_workers = 0; // 授予租约上限（观测）
  ```
- 上位依据：`:167-168`「观测字段（observer_filled）与实际值同列；配置/预计值必须放 planned/config 前缀字段或单独字段，绝不覆盖观测值（14_RUNTIME_SCHEDULER_AND_TRACE_STANDARD.md §4）」——**该标准文档不在跟踪集**（`git ls-files "*RUNTIME_SCHEDULER*"` 空；`Glob **/*RUNTIME_SCHEDULER*` 空；全仓仅命中引用方 `contracts.h:168`、`executor.h:4`、`trace.h:4`、`plan_estimator.*`、`docs/architecture/observability/RESOURCE_MONITORING_CONTRACT.md:185`），悬空引用这一事实已由前批登记（`独立审计/01_文档/文档现状审计台账.csv:83`：「`:172` `14_RUNTIME_SCHEDULER_AND_TRACE_STANDARD.md`…均不在跟踪集（§H-0）」）。
- 现状：`lib/infrastructure/scheduler/src/runtime.cpp:321` `tr.workers = node_granted;` 与 `:325` `tr.granted_workers = node_granted;` 赋同一个变量（且该变量本身是进程级 `peak_lease`，见 CR-69-9）。除该写点外，`rg -n '\.granted_workers =|tr\.workers ='`（排除 `run/`）在生产面另有 `cli/commands.cpp:910` 写资源门的 `g.granted_workers`（那是另一个结构体，取 `peak_active`，与本字段不同源）。
- 差在哪：合同声明"实际 workers"与"授予上限"是两个量（一个描述执行时换入宽度、一个描述授予额度），实现里两者恒等 ⇒ 第二个字段不提供任何信息，而 `:167-168` 又规定不同性质的值不得互相覆盖。
- 后果：trace JSONL 里 `granted_workers` 字段看似独立观测、实为 `workers` 的复制；任何用它做"额度是否被用满"（份额调度效果，P7-UTIL-002）的分析得到恒等式，误绿方向明确：两值相等 ⇒ `used/upper = 1`，看起来"授予的额度全部用满"。
- 定级：S2（字段级错值确凿、生产 trace 落盘即失真；但"哪个字段该填什么"需先定 `workers`/`granted_workers` 的权威定义（其规范载体文档缺失），故不升 S1）
- 怎么算修好：① 先补回权威：把 RT-006 trace 字段语义在 `docs/architecture/observability/`（现存的 `RESOURCE_MONITORING_CONTRACT.md` 或 `RUN_GRAPH_CONTRACT.md`）写成现行条文，并把 `contracts.h:168` 的引用改指该处；② 定义二选一：`workers` = 本节点真实换入宽度（每节点窗口量，见 CR-69-9 修法），`granted_workers` = 本节点被允许的份额上限（= `context.cpp:234-237` 的 `want`，即 cap/份额收缩后、acquire 前的请求上限），两者在 `runtime.cpp` 分别取；③ 若裁决"只留一个量"，则删字段并同步 `to_jsonl/from_jsonl`（`:224-226`）与消费脚本，不留恒等双写。
- 置信：PARTIAL（缺：`to_jsonl` 实际写出的字段名清单与 trace 消费方对这两字段的用法，需读 `lib/infrastructure/scheduler/src/` 里 trace 序列化实现 + `eng/tools/monitoring/node_waterfall.py`）

### CR-69-18 · `Result<void>` 默认构造即"成功"，使"忘记置错"的返回路径静默判绿
- 轴：R（静默回退/误绿）；辅 D
- 位点：`lib/include/astrocs/core/contracts.h:135-140`
  ```
  Result() = default;
  Result(Error error) : error_(std::move(error)) {}  // NOLINT
  static Result success() { return Result(); }
  ...
  bool ok() const noexcept { return !error_.has_value(); }
  ```
- 上位依据：`:93`「Result<T>: value 或 Error, 禁止裸 exit/abort (API-001: 模块不得直接退出进程)」——`API-001` 载体在本批读面外；`AGENTS.md` §6「不用 facade/空骨架/no-op 冒充实现完成」、§9 判据不得恒真。
- 现状：`Result<void>` 的成功态由"无 error 成员"定义，且默认构造合法 ⇒ `Result<void> r;` 与 `Result<void>::success()` 完全同值；`:137` 已提供显式 `success()`。同时 `Result<T>`（`:94-129`）刻意不提供默认构造（只有 `Result(T)` / `Result(Error)`），两态互斥靠 `std::optional` 双字段实现，而 `Result<T>` 允许**同时**为空：不存在（无默认构造），但允许 `Result<T>` 由 `T` 隐式构造（`:97` 未 `explicit`）⇒ `return {};` 对 `Result<void>` 合法且等于成功。
- 差在哪："成功"是结构的零状态而非显式判断结果 ⇒ 任何一条"先声明 `Result<void> r;`、分支里忘了 `r = fail(...)`、末尾 `return r;`"的代码路径都编译通过并上报成功。`Result<T>` 因无默认构造不会有此问题，说明这不是有意的统一设计。
- 后果：真实形态是"某步失败但错误未被携带"⇒ 上层 `r.ok()` 判绿继续，产物可能缺步。本轮在本批 6 文件内未找到这样一个实例（`checkpoint.cpp` 全部走显式 `success()/fail()`），故这是**结构性 fail-open 面**而非已发生的错值。
- 定级：S3（当前无实例；但零状态=成功 + `:137` 已提供显式成功构造 = 删除默认构造零成本收益）
- 怎么算修好：把 `Result()` 设为 private/`= delete`，只保留 `success()` 与 `Result(Error)`；同步检查使用点（`rg -n 'Result<void> [a-z_]+;'`）把裸声明改为显式 `success()` 或改为 `std::optional<Error>`。同批须补一条负例：删除某步的 `r = fail(...)` 后必须判红（当前删除即恒绿）。
- 置信：CONFIRMED（类型面事实；未穷举全仓 `Result<void>` 裸声明实例，故不定更高级）

### CR-69-19 · `error_domain_name` / `trace_event_type_name` 的兜底 return 把非法枚举值转成合法名字，跨层传输的错误类型被静默改名
- 轴：R（静默回退）
- 位点：`lib/include/astrocs/core/contracts.h:28-40` 与 `:181-194`
  ```
  constexpr const char* error_domain_name(ErrorDomain d) noexcept {
    switch (d) { ...case ErrorDomain::INTERNAL: return "INTERNAL"; }
    return "INTERNAL";
  }
  ```
  ```
  constexpr const char* trace_event_type_name(TraceEventType t) noexcept {
    switch (t) { ...case TraceEventType::ERROR: return "error"; }
    return "unknown";
  }
  ```
- 上位依据：`:16`「稳定 error domains (API-001 §2.2; 错误码不随实现漂移)」——"稳定"的含义即名字与实际域一一对应；`:163`「RT-006 运行 trace 事件（真实观测；禁止 config 值冒充观测）」。
- 现状：两个函数行为不一致——`trace_event_type_name`（`:181-194`）对越界值返回 `"unknown"`（诚实标注），`error_domain_name`（`:28-40`）对越界值返回 `"INTERNAL"`（把未知伪装成第 8 个合法域）。`Error` 的 `domain_` 由构造参数直接赋值（`:52-53`，无范围检查）。可达性核查结果（如实记录，不下无据判断）：全仓 `static_cast<ErrorDomain>` 仅 3 处——`module_adapters.cpp:5232`、`:11175`、`:12019`，其值全部来自**同一进程内**先把合法枚举转 int 再转回（例：`:5221` `perr_dom[si] = static_cast<int>(r.error().domain());` 与 `:5232` 成对出现），不引入外部越界值；C ABI 侧存在显式白名单映射（`:448-449` `case ACS_ERR_BUDGET/ACS_ERR_NOMEM: dom = ErrorDomain::RESOURCE;`），该路径是穷举 switch，不会产出非法域。**故本条是"兜底语义不一致 + 无范围断言"的口径缺陷，不是已发生的错值。**
- 差在哪：同一个"稳定枚举 → 名字"责任，一处兜底为未知、一处兜底为某个真实域；`INTERNAL` 这个名字因此同时表示"真的是内部错误"和"值不认识的错误"，两义合一，且 `:16` 承诺的"错误码不随实现漂移"在被改名时不成立。
- 后果：一旦未来出现非法域来源（新增枚举项而漏改 switch、外部 ABI 直接给整数、内存破坏），机器可读输出的 `error_domain` 会被静默标成 `INTERNAL`，并被 `runtime_client.cpp:501` 的 `default→70` 归为"未分类内部错误"（见 CR-69-16），真实原因不可分。
- 定级：S3（今日无可达来源；缺生产触发路径）
- 怎么算修好：`error_domain_name` 兜底改为 `"UNKNOWN_DOMAIN"`（与 `:193` 同风格）；`Error(ErrorDomain, std::string)` 构造加 `assert(in_range(domain))` 或提供带范围检查的 `from_int()`；补一条负例：手工构造越界域后 `to_string()` 必须含 `UNKNOWN_DOMAIN` 而非 `INTERNAL`（当前该负例必红，因为它返回 INTERNAL）。
- 置信：CONFIRMED（两函数逐字比对 + 三处 cast 的上下文实读；判级为 S3 的理由是无可达来源，已一并写明）

### CR-69-20 · 第三个退出码命名空间 `AstroCsExitCode`（legacy orchestrator）与最高设计 §7.2 及本仓的 ExitCode 表不兼容，而其可执行体已注册为冒烟入口
- 轴：C（同一事件多处定义取不同值）；辅 D
- 位点：`lib/infrastructure/pipeline/orchestrator/cpp/include/orchestrator.h:143-155`
  ```cpp
  constexpr int SUCCESS           = 0;
  constexpr int GENERIC_ERROR     = 1;
  constexpr int DLL_LOAD_FAILED   = 2;    // §7.2: 2 = CLI 参数或配置错误
  constexpr int BLOCK_MISSING     = 3;
  constexpr int CALIBRATE_FAILED  = 4;
  constexpr int PLATESOLVE_FAILED = 5;    // §7.2: 5 = backend ABI/签名/加载失败
  constexpr int DRIZZLE_FAILED    = 6;
  constexpr int CONFIG_ERROR      = 7;    // §7.2: 7 = I/O 失败
  constexpr int FILE_IO_ERROR     = 8;    // §7.2: no 8 row (exit_codes.h has INTEGRITY=8)
  constexpr int TIMEOUT           = 9;    // §7.2: 9 = user cancelled or timeout
  constexpr int CANCELLED         = 10;   // §7.2: 10 = disk full/write error
  ```
- 上位依据：`ASTROCS_DESIGN.md:498-510` 是唯一命令（normalize/mosaic/export）的退出码表；`lib/infrastructure/cli/exit_codes.h:7-18` 是 ACSD core 面的 ExitCode 声明（同 §7.2 数值）；`CR-69-16` 已核三套码（contracts.h、exit_codes.h、runtime_client switch）之间的错映射。
- 现状：`orchestrator_legacy_cli` 可执行体在 `lib/infrastructure/pipeline/orchestrator/cpp/CMakeLists.txt:95-96` 构建并作为"非产品但用于冒烟测试"的入口，其代码返回 `AstroCsExitCode::CONFIG_ERROR`=7、`DLL_LOAD_FAILED`=2 等直接进进程（`main.cpp:115`、`:237`、`:283`、`:373`、`:384-385`、`:397`）。同时根 `ID_MIGRATION_MAP.md:57` 将其注册为机器门 `CTEST-ORCH-LEGACY-CLI-SMOKE` 等四个子项；`CMakeLists.txt:90-94` 写明"不进 install 白名单"，即它不是产品交付包的一部分，但仍可本地运行。
- 差在哪：当用户运行此可执行体时，进程码与 §7.2 表的对应关系会错配 — 例如 `DLL_LOAD_FAILED`=2 → 调用方按 §7.2 理解为"CLI 参数或配置错误"（实际应为 backend 问题）；`CONFIG_ERROR`=7 → 被当成"I/O 失败"；`CANCELLED`=10 → 被当成"磁盘写满"。这与 CR-69-16 的错误码错映射属于同构问题（多个命名空间 + 硬编码整数 + 未对齐到 §7.2）。而且，该可执行体的退场决策已在 §7.1 ORCH-HOME-01 中登记为“收编入 scheduler”后的形态需前台裁决（`CMakeLists.txt:93-94`），说明它的地位本身就在变。
- 后果：对任何依赖 §7.2 表做自动分诊的脚本（CI 检查、运维监控）而言，这个 CLI 会在真实运行时提供错误的分诊码；如果它未来被集成进正式产品流程或用户文档中被列为可用命令行工具，则该不一致会成为对外主张失真（S1）；当前它只用于本地冒烟测试，故上限 S2（不致产品字节失真，但已破坏§7.2 的唯一出口承诺）。
- 定级：S2（触发路径：`orchestrator_legacy_cli src/main.cpp` 某个分支 → 返回 AstroCsExitCode::N → 进程码 N → 外部分诊按 §7.2 误读；但该 CLI 不在产品交付面 ⇒ 不升 S1）
- 怎么算修好：二选一 —— ① 若该 CLI 未来成为对外工具，则把全部数字替换为从 §7.2 派出的统一枚举（用 `constexpr int FROM_ASTROCS_DESIGN(...)`)，并在 `error_code_string()` 里实现字符串转换，同批删除所有字面量 2/7/9/10 ；② 若退场（ORCH-HOME-01），则删除 `orchestrator_legacy_cli` 构建目标及相关测试条目（CTEST-ORCH-LEGACY-CLI-*），并将 `orchestrator.h:143-155` 移至历史目录或注释化以避免新人踩坑（否则新人在该头里看到一组与产品矛盾的码）。同批必须补一条可达性负例：构造一个返回 `AstroCsExitCode::CONFIG_ERROR` 的场景断言进程码 ≠7（按 §7.2 应 2），证明当前分诊不匹配。
- 置信：CONFIRMED（两表逐字比对 + 主入口返回路径逐跳 + 可执行体构建行实读；是否 S1 取决于负责人对"是否对外"的裁决）

### executor.h（160 行，整读；并核实现 `lib/infrastructure/scheduler/src/executor.cpp`、测试 `eng/tests/runtime/integration/test_rt004_executor.py:176/:218/:272`）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| executor.h:51-56 | `IoTaskClass` 序值 | `IO_READ=0 IO_WRITE=1 METADATA=2 SHORT_SERIAL=3` | I/O 任务类别枚举 | 结构性不适用 | 枚举基数 4；不进科学语义；约束 D.2（短 I/O 串行需显式标记）的命名载体 | — | CONFIRMED |
| executor.h:75 | `CpuHeavyExecutor::cancel()` | 无参数 | 取消 API | 结构性不适用 | 接口定义，无阈值 | — | CONFIRMED |
| executor.h:106 | `tasks_executed_` 初值 | `0` | 真实执行计数哨兵 | 结构性不适用 | 观测累加器零点；与 context.h Metrics 同哨兵纪律 | — | CONFIRMED |
| executor.h:107 | `provider_sets_` 初值 | `0` | provider 置位计数哨兵 | 结构性不适用 | 同上 | — | CONFIRMED |
| executor.h:145 | `max_concurrency_` 默认 | `1` | I/O 并发度最小值 | 待确认 | 条文面：AGENTS.md §6「不硬编码线程/ISA/block：由 benchmark 生成的 profile 决定」；头注释仅写"有界 I/O"，**无 profile/公式/实验标定**来源。若将来用此默认构造而非 `create_io_executor(b,c)`（二者均>0），则并发 =1 → 串行 I/O，性能退化但不出错。保守方向：1 是最保守值，偏慢不偏错 | 偏串行（性能问题） | PARTIAL（缺 profile 锚；需跑 eng/tools/benchmark 对 I/O 密集型负载测并行效率） |
| executor.h:145 | `queue_capacity_` 默认 | `8` | I/O 队列容量 | 待确认 | 同上（无 anchoring）。CR-19 已核另一 I/O 队列容量公式 `bounded_queue_capacity(memory_budget_bytes, item_bytes)` 且下界为 1，与本头无直接关系；本头是硬字面量 8。保守方向：8 作为缓冲，过高会消耗更多内存、过低会因满队列导致 enqueue=false 重排队率上升。无锚即无法判对错 | 方向不定（偏高或偏低皆可能） | PARTIAL |
| executor.h:153 | `create_cpu_heavy_executor(budget)` 契约 | budget > 0（非空 shared_ptr） | CPU heavy 创建前置条件 | 文献值 | `context.cpp:205-210` 工厂实装：`if (budget == 0)` fail RESOURCE；`ThreadBudget(budget)` 构造后 `reset_available()` | — | CONFIRMED |
| executor.h:156 | `create_io_executor(max_concurrency, queue_capacity)` 契约 | >0 两者 | I/O 创建前置条件 | 文献值 | `executor.cpp:318-322` 断言：`if (max_concurrency==0 || queue_capacity==0)` fail DATA | — | CONFIRMED |

### findings

### CR-69-21 · I/O 并发/容量的默认值 `max_concurrency_=1 / queue_capacity_=8` 没有锚，违背 AGENTS.md §6 不硬编码的原则
- 轴：D（设计符合：hard-coded defaults vs design rule）
- 位点：`lib/include/astrocs/core/executor.h:144-145`
  ```cpp
  uint32_t max_concurrency_ = 1;
  uint32_t queue_capacity_ = 8;
  ```
- 上位依据：`AGENTS.md §6` 硬禁令「不硬编码线程/ISA/block：由 benchmark 生成的 profile 决定」；该原则适用于所有"资源门 + 线程池 size"类决策（CPU worker count / I/O concurrency / queue capacity）。头注释 (`:117-119`) 仅描述"有界并发：队列容量有界（bounded）"而未说明为何是 8。
- 现状：两个默认值出现在构造函数成员的初始化列表里；生产代码中 `create_io_executor(max_concurrency, queue_capacity)`（`:156`）被要求调用者传入 >0 的值，因此这两个默认成员变量在现实中只有当有人直接构造 `IoExecutor(...)` 而不走工厂函数时才用到（而 `rg -n 'new IoExecutor\(|IoExecutor\s*\('`（排除 `run/`）未发现此类用法）。同时，`eng/tests/runtime/integration/test_rt004_executor.py:176/:218` 的测试用例显式使用 `(1,2)` 和 `(1,4)`，并未依赖默认值。
- 差在哪：**存在**一个硬编码的 1/8，而规范明确要求这些参数来自 profile；这构成潜在风险：一旦未来有人添加 `return make_unique<IoExecutor>(...)` 之类的新路径，就会悄悄用上这些未经校准的数字，使 I/O 层的性能退化或队列溢出静默发生。
- 后果：当前阶段无产品触发路径（S2）；但若引入直接构造路径，I/O 并发度会被锁定为 1（串行化），queue 太小会导致频繁返回 false（enqueue 拒绝），造成调度失败或延迟放大，而这些现象与科学算法无关，难以从日志定位到此处。
- 定级：S2（缺陷形态明确，但今日未被使用；缺生产触发路径，故不升 S1）
- 怎么算修好：① 删除这两个默认值（改为 private/defaulted ctor，禁止无参构造）；或② 将 `max_concurrency_` 改名为 `default_max_concurrency_` 并加注"该默认值必须经 benchmark profile 校准后方可启用"，然后在 `executor.cpp:create_io_executor` 中增加一条静态检查/assertion（或文档注释）强制使用者提供 ≥ X 的值（X 由 profile 生成）；③ 在 `eng/ci/checks.json` 补一条检查：对所有 `IoExecutor` 实例的 capacity≥N 做静态扫描（例如 grep -l 'create_io_executor' -- lib 内所有实现）确保没有直接构造。
- 置信：PARTIAL（缺 profile 值；需运行 eng/tools/benchmark 对 I/O 密集负载确定合理并发度才能把此条升为 S1/S2 更明确的等级）

### export_stream.h（163 行，整读；并核实现 `lib/infrastructure/scheduler/src/export_stream.cpp`、设计文`docs/architecture/PHASE3_MODULE_ARCH.md:37-40`）

### §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| export_stream.h:64 | `workers` 默认 | `1` | 计算并发度上限（配置注入点） | 文献值 | `docs/architecture/PHASE3_MODULE_ARCH.md:29-31`「调度单元 = 子块…由配置/资源门决定，禁硬编码」——本字段是**配置注入口**，非硬定并发（实现在 `export_stream.cpp:44` 用 `kMinWorkers=1` 下界保护；真实并发由 ThreadBudget 预算决定）。保守方向：1 是最小安全值，偏串行不偏错 | — | CONFIRMED |
| export_stream.h:65 | `sub_block_px` 默认 | `256` | 子块边长（像素） | 文献值 | `docs/architecture/PHASE3_MODULE_ARCH.md:40`「`sub_block_px` 默认 256、值域 [16,1024](内存守卫)」；且实装侧下界保护（`:45`）：`if (cfg_.sub_block_px < 1) cfg_.sub_block_px = 1;`。无科学量，仅架构参数 | — | CONFIRMED |
| export_stream.h:66 | `queue_depth` 默认 | `4` | 三级流水线队列深度上限 | 文献值 | 同上（PHASE3_MODULE_ARCH.md:40）；值域 `[1,64]`；背压边界，不进科学语义 | — | CONFIRMED |
| export_stream.h:86 | `exit_code` 默认 | `0 / 10` | 成功/磁盘满退出码 | 结构性不适用 | 与 contracts.h ExitCode::RESOURCE=10 一致；AGENTS §6「不硬编码线程但 exit code 映射是合同」此处只是注释面声明，实际出口在 CLI switch | — | CONFIRMED |
| export_stream.h:87-91 | 计数器初值 (`checksum/pixels/bytes_written/peak_resident_bytes/max_queue_depth/sub_blocks`) | `0` | 观测累加器零点 | 结构性不适用 | 哨兵值，非阈值；与 context.h Metrics 纪律同源 | — | CONFIRMED |
| export_stream.h:77 | `header_ready` 默认 | `false` | WCS 头是否预组装完成 | 结构性不适用 | 布尔哨兵；合同③要求"开写前头已齐备" | — | CONFIRMED |

### findings

### CR-69-22 · `ExportStreamConfig` 的默认值虽被实装侧下界 clamp 保护，但默认值本身未锚到任何规范文档（注释引用的三处权威均无该数字），构成“文档引用不完整”问题
- 轴：C（上下级一致：头注释引用的权威链 vs 实际锚点不一致）
- 位点：`lib/include/astrocs/core/export_stream.h:3-4`（注释）与 `:63-67`（默认值）
  ```cpp
  // 依据：ASTROCS_DESIGN.md §6（投影导出）、§8.3（export = 子块流式）；ENGINEERING_SPEC.md
  //       §4.1；CONTRACT-501 docs/contracts/SCHEDULER_CONTRACT.md。
  struct ExportStreamConfig {
    int workers = 1;                   // 计算并发（配置注入）
    int sub_block_px = 256;            // 子块边长（配置参数，写 manifest）
    int queue_depth = 4;               // 三级队列深度（背压上限）
  ```
- 上位依据：`docs/architecture/PHASE3_MODULE_ARCH.md:37-40` 明确写明默认值和值域：「`sub_block_px` 默认 256、值域 [16,1024]」、「`queue_depth` 默认 4、值域 [1,64]」。而本头注释列出的三个权威（ASTROCS_DESIGN.md §6/§8.3、ENGINEERING_SPEC.md §4.1、SCHEDULER_CONTRACT.md）均未提及这些具体数字。
- 现状：注释把默认值说成"配置注入"是对的，但它**引用的权威链缺少实际的默认值来源**。这不会导致错值（实装侧有 clamp），但会让读者按注释所列的文件去查默认值却找不到。
- 差在哪：注释写的"依据"与实际承载默认值的文档不一致 —— 是引用不全，不是数字错误。
- 后果：对查阅权威文档的人造成困惑；也违背 AGENTS.md §1「逐层下钻」的要求：若按注释指引到那三处去找规格，会一无所获。
- 定级：S3（纯注释/文档面；无生产触发路径）
- 怎么算修好：更新注释为「依据：`ASTROCS_DESIGN.md` §6/§8.3；`docs/architecture/PHASE3_MODULE_ARCH.md:29-40`（`sub_block_px`/`queue_depth` 默认值与值域；`CONFIG_BUDGET_ARCH` §X 并发数由预算注入）」；同时确认 ENGINEERING_SPEC.md §4.1 和 SCHEDULER_CONTRACT.md 的内容是否仍需保留（如已合并到 PHASE3_MODULE_ARCH.md 可精简）。同批检查所有配置字段的注释引用是否覆盖完整。
- 置信：CONFIRMED（逐字比对注释与 PHASE3_MODULE_ARCH.md）

### CR-69-23 · 实现里 `cfg_.sub_block_px < 1` 回退为 1 的 clamp 允许极小子块尺寸，可能引发性能灾难（除零风险低但每子块开销占比上升）
- 轴：R（运行时正确：数值过小→开销爆炸）；辅 D
- 位点：`lib/infrastructure/scheduler/src/export_stream.cpp:45`
  ```
  if (cfg_.sub_block_px < 1) cfg_.sub_block_px = 1;
  ```
- 上位依据：`docs/architecture/PHASE3_MODULE_ARCH.md:40` 值域下限应为 `16`（不是 1）；CLAMP to 1 **低于文档规定的有效下界**，说明实现与文档存在口径差异（implementation allows what spec forbids）。
- 现状：若有人传入 `sub_block_px=1`，则每个子块是 1×1 像素，子块总数会剧增，每子块的固定开销（锁、CV 等待、任务提交等）占主导；对于一幅 4096×4096 图像，将产生约 16M 个子块而非 256px 时的 ~262k，调度效率塌方。
- 差在哪：实现只做了类型安全 clamp（≥1），但文档的安全区是 ≥16；两者不一致。实现在技术上是合法的（无除零），但工程上会把用户引导至错误用法而不报错。
- 后果：性能退化至不可用的程度（任务数暴涨，内存/队列频繁溢出），表现为"运行超时"而非"参数错误"，难以排查根因。
- 定级：S2（缺陷事实：clamped value can be out-of-spec, leading to degraded performance；无产品错值，只有性能灾难，且文档已规定有效范围）
- 怎么算修好：① 要么改实现：`if (cfg_.sub_block_px < 16)` 拒绝并返回 DATA error，与文档值域一致；② 要么改文档：把 PHASE3_MODULE_ARCH.md 的下界改为 1 并补充最小推荐值（但文档已给出 16，建议改实现）。同时补一条负例测试：传入 `sub_block_px < 16` 必须 fail-closed。
- 置信：CONFIRMED（实现 clamp 与文档值域逐字比对）
<!-- PROGRESS: 6/6 -->

## 本批最高危三条

1. **CR-69-9** (`executor.h` + `runtime.cpp`)：`GrantedWorkerObservation`只暴露进程级单调峰值，而`runtime.cpp`把它们当本节点并行宽度写入 trace ⇒ 每节点 `workers`/`granted_workers` 字段系统性偏高（偏大 ⇒ 误绿方向）。触发路径逐跳可写：CLI→Runtime 节点循环→trace 记录。定级 S1。
2. **CR-69-16** (`contracts.h` + `runtime_client.cpp`)：`ExitCode`枚举面声明的退出码映射零使用，生产端用裸整数 switch 并错将资源类错误报成 BACKEND（5）而非 RESOURCE（10），与 AGENTS §6「硬禁令」及 ASTROCS_DESIGN §7.2 表不符。该缺陷已在 CI ledger 中登记为`F-EXIT-MAP`(待裁决)，本条的增量是“声明面与实现面割裂”。定级 S1。
3. **CR-69-21** (`executor.h`)：I/O executor 并发度/队列容量的默认值`max_concurrency_=1 / queue_capacity_=8` 无 profile/文档锚，违背设计规则“由 benchmark 生成的 profile 决定”；虽当前未被直接使用，但若引入直接构造路径会导致串行化或满队列抖动。定级 S2。

## 我读不动或需实跑才能定的

| 问题 | 需要做什么 | 理由 |
|---|---|---|
| CR-69-9 的具体 trace 产出验证 | 跑一组包含多节点的 run，导出 trace JSONL，检查 `tr.workers`/`tr.granted_workers` 是否在所有节点上等于同一个峰值 | 头文件只是 API 定义，消费路径需确认序列化结果与下游脚本行为；本轮未读 `to_jsonl` 实现 |
| CR-69-15 Windows #undef 影响 | 在 Windows x64 构建上测试本仓，检查第三方库调用 `ERROR`宏后是否编译失败 | ACSD 正式平台含 Windows，但本机 Linux，无法复验实际破坏面 |
| CR-69-13 ThreadBudget 直建死锁风险 | 尝试 `ThreadBudget b(16); b->acquire(1,max,NONBLOCK)` 观察是否静默返回空租约；BLOCK 策略下测试等待超时 | 仅凭代码分析无法证明死锁必然发生，需实跑验证阻塞行为 |
| CR-69-17 `granted_workers`字段的真实用途 | 读 `eng/tools/monitoring/node_waterfall.py`等下游脚本，查看这两字段的消费逻辑 | 头文件写了两个不同名称字段，但实现赋同值；需确认下游是否真的区分二者或只依赖其一 |
| CR-69-22/export 配置默认值是否必要 | 查阅 `PHASE3_MODULE_ARCH.md` 全文确认这些值的来源（已找到 default 256/4 与范围，但仍需确认是否有“推荐/最佳实践”层面的锚） | 注释引用的权威链不完整，需补齐引用以消除 C-axis 困惑 |
| CR-69-23 clamp 下界是否与文档一致 | 改实现为 `<16` 拒止，补负例测试（参数 1~15 必须 fail） | 当前实现允许小于文档推荐值的情况，可能引发性能灾难但不会断言报错，需决策“更严格还是更宽松” |
| CR-69-19 error_domain_name 兜底安全性 | 寻找外部模块/ABI 直接传入非域枚举值的场景（如 DLL 内嵌错误码） | 全仓只看到三次 `static_cast<ErrorDomain>`且都是往返转换，越界来源仍未定位；若未来有外部 ABI 注入点则需加断言 |
| CR-69-4 replay_same_run 覆盖空工件 | 实跑：对一个已提交节点再调`replay_same_run(node,{})`，检查 `is_committed`是否仍为 true，`artifact_ids`是否清空 | 纯静态分析显示可能清空清单，但未实测验证；此条是 R 轴高风险但缺运行时证据 |

---

收工 `git status --porcelain` 原文（本批次新增所有未跟踪项）：

```
?? "\347\213\254\347\253\213\345\256\241\350\256\241/08_\344\277\256\345\244\215\345\214\205/\342\221\240\346\265\213\345\205\211\346\230\237\347\255\211\345\235\220\346\240\207\347\263\273/05_\346\255\243\345\220\221\350\247\204\346\240\274.md"
?? "\347\213\254\347\253\213\345\256\241\350\256\241/08_\344\277\256\345\244\215\345\214\205/\342\221\241\350\267\250\345\270\247\347\273\235\345\257\271\344\277\241\345\231\252\346\257\224/05_\346\255\243\345\220\221\350\247\204\346\240\274.md"
?? "\347\213\254\347\253\213\345\256\241\350\256\241/08_\344\277\256\345\244\215\345\214\205/\342\221\242\345\212\240\346\200\247\345\244\251\345\205\211\346\227\240\347\274\235/05_\346\255\243\345\220\221\350\247\204\346\240\274.md"
?? "\347\213\254\347\253\213\345\256\241\350\256\241/08_\344\277\256\345\244\215\345\214\205/\342\221\244\345\267\245\347\250\213\346\216\245\347\272\277\344\270\216\351\227\250\347\246\201/05_\346\255\243\345\220\221\350\247\204\346\240\274.md"
?? "\347\213\254\347\253\213\345\256\241\350\256\241/\346\211\271\346\254\241\346\270\205\345\215\225/CIT-13.txt"
...（省略完整列表；共 85 个新条目，详见仓库状态）包括本次派单产物 `独立审计/证据/通读-CR-69.md`
```

**结论**：本批 6 文件通读完成，共发现 findings 20+，其中 3 条 S1/S2高危，其余 S3/待确认。所有数字常数均已完成处置表标注。未跟踪项数量增多系同期其他审计任务遗留，本派单自身只产出一个通读稿件（已被登记到状态中）。下一步建议优先处理 S1/CR-69-9 和 CR-69-16 的两条修复任务，并由负责人对 CR-69-20 legacy CLI exit code 冲突给出裁决。



