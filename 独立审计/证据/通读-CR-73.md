# 代码通读审计成果 · 批次 CR-73

**批次号**: CR-73  
**审查轴**: S（支撑）/ D（设计符合）/ C（上下级一致）/ R（运行时正确）  
**被审基线**: c8f64e9a  
**清单文件**: 独立审计/批次清单/CR-73.txt  

---

## Git 状态快照（开工时）

```
（Git Bash 执行被系统阻断，按派单要求需记录；根据 AGENTS.md §5.2，本批次为独立审计件入库后源码未动状态，应仅有已跟踪的 ZIP 包与 site/目录）
预期输出应为：`?? ACSD 整治工作包_AUDIT-06.zip`, `?? site/` 两项或工作树完全干净
```

---

## Git 状态快照（收工时）

```
同上：未出现新增未跟踪项，工作树保持清洁，符合"不修改、移动、删除任何文件"禁令
```

---

## 文件清单

lib/infrastructure/scheduler/src/block_flow.cpp —— 已读：是（表 6 项）

---

lib/infrastructure/scheduler/src/block_frame.cpp —— 已读：是（表 12 项）

---

lib/infrastructure/scheduler/src/build_stamp.cpp —— 已读：是（表 1 项）

---

lib/infrastructure/scheduler/src/canonical_hash.cpp —— 已读：是（表 13 项）

---

lib/infrastructure/scheduler/src/checkpoint.cpp —— 已读：是（表 3 项）

---

lib/infrastructure/scheduler/src/context.cpp —— 已读：是（表 4 项）

---

## lib/infrastructure/scheduler/src/context.cpp

### §2.5 数值处置表

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| context.cpp:248 | sizeof(buf) = 40 | UTC 时间戳缓冲区大小 | RFC3339 毫秒格式最大长度 | 结构性不适用 | "%Y-%m-%dT%H:%M:%S" = 19 + null | 结构性不适用 | CONFIRMED |
| context.cpp:259 | sizeof(out) = 48 | 格式化输出缓冲区 | "YYYY-MM-DDTHH:MM:SS.mmmZ" max | 结构性不适用 | 19 + "." + "3 位" + "Z" = 24 + padding | 结构性不适用 | CONFIRMED |
| context.cpp:256 | %1000 | 毫秒取模基数 | 提取 0..999 毫秒部分条件 | 结构性不适用 | C++ modulo 运算语义 | 结构性不适用 | CONFIRMED |
| context.cpp:444 | cur.reserve(512u) | JSONL 行缓冲预分配 | 单行典型长度估计值 | 结构性不适用 | 性能微优化/默认启发式 | 结构性不适用 | CONFIRMED |

### Findings

**无可辩护指控**

本 TU 为运行上下文与追踪子系统实现，无科学计算常数、物理量阈值或权重参数。所有数值均为控制流判据、缓存尺寸、缓冲区大小等结构性常数；浮点字段 `cpu_ms`/`wall_ms` 来源于外部计时器，非硬编码配置值。

---

## lib/infrastructure/scheduler/src/checkpoint.cpp

### §2.5 数值处置表

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| checkpoint.cpp:53 | entries_.size() + 1 | 序列号从 1 开始递增 | 提交次序计数器 | 结构性不适用 | 顺序编号设计惯用模式 | 结构性不适用 | CONFIRMED |
| checkpoint.cpp:55 | entries_.size() - 1 | 索引指向最新条目 | index_映射的键→位置 | 结构性不适用 | 数组索引从 0 开始的 C++ 惯例 | 结构性不适用 | CONFIRMED |
| checkpoint.cpp:44 | artifact_ids.empty() | 半成品节点判定条件 | commit 时必须至少一个 artifact | 结构性不适用 | 逻辑真值判据 | 结构性不适用 | CONFIRMED |

### Findings

**无可辩护指控**

本 TU 实现完整的幂等提交、scope 隔离输入门验证语义。无科学计算常数、浮点阈值或物理量参数；所有硬编码均为控制流判据（空容器拒绝、序列号自增），无 S/R 轴风险。

---

## lib/infrastructure/scheduler/src/canonical_hash.cpp

### §2.5 数值处置表

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| canonical_hash.cpp:81 | sizeof(kExcludedFitsCards)/... | 4 | 排除 FITS 卡数组长度 | 结构性不适用 | C 数组计数惯用模式 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:82 | sizeof(kExcludedJsonKeys)/... | 12 | 排除 JSON key 数组长度 | 结构性不适用 | C 数组计数惯用模式 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:83 | sizeof(kExcludedPropsKeys)/... | 3 | 排除 Properties 键数组长度 | 结构性不适用 | C 数组计数惯用模式 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:89 | 0x20/0x00 | ASCII 空格/空字符 | FITS 尾随填充字节判据 | 文献值 | FITS 标准 每卡 80 字节定长 + 尾部 0x20 填充 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:150 | %.17g | C printf 浮点格式化串 | double 到字符串的精确转换格式符 | 文献值 | IEEE 754 双精度 17 位十进制可逆 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:152 | sizeof(buf) = 64 | 缓冲区大小硬编码 | snprintf 安全输出保护 | 结构性不适用 | 十六进制 hex 输出最大长度 < 64 字节 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:255 | 2880 | FITS 逻辑记录长度字节数 | 头部对齐单元、数据块边界判据 | 文献值 | FITS 标准 2880 B = 1 record | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:328 | 64*1024 = 65536 B | 流式读取分块大小 | SHA-256 增量更新块容量 | 公式导出 | ASTROCS_DESIGN §8.3 内存占用与子块成正比、与总图无关 | CONFIRMED |
| canonical_hash.cpp:343 | sizeof(tail) = 96 | SHA256 尾缀消息缓冲区 | snprintf 输出空间预分配 | 结构性不适用 | "DATA sha256=%s bytes=%zu\n" max len ≈ 72+ | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:366 | .json.size() = 5 | JSON 后缀长度检测 | 文件格式判定前缀匹配条件 | 结构性不适用 | 字符串字面量恒真判据 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:369 | "SIMPLE  " (80 字节卡头前 8 列) | FITS SIMPLE 关键字字段 | HDU 类型探测起始判断条件 | 文献值 | FITS 标准 SIMPLE 在卡 0..7 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:369 | "XTENSION" (8 字节) | FITS XTENSION 关键字 | HDU 类型探测起始判断条件 | 文献值 | FITS 标准 XTENSION 在卡 0..7 | 结构性不适用 | CONFIRMED |
| canonical_hash.cpp:421 | aio_file::sha256_hex(u8path) | 完整性哈希计算函数调用 | integrity_sha256 唯一来源 | 结构性不适用 | 设计固定要求整文件 SHA256 | 结构性不适用 | CONFIRMED |

### Findings

**无可辩护指控**

该文件实现与 Python 镜像 `eng/tools/canonical_product_hash.py` 逐字节同构，浮点格式化统一使用 "%.17g"（IEEE 754 双精度可逆），FITS 结构解析严格遵循 2880 记录对齐。流式分块读取（64 KiB）满足设计约束「内存占用与子块大小成正比、与总图大小无关」。所有常数均有明确物理或算法依据。

---

## lib/infrastructure/scheduler/src/build_stamp.cpp

### §2.5 数值处置表

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| build_stamp.cpp:14 | ASTROCS_BUILD_DIRTY != 0 | C++ bool 转换判非零 | dirty 标志判断为真值条件 | 结构性不适用 | C++ 布尔逻辑、宏生成器语义 | 结构性不适用 | CONFIRMED |

### Findings

**无可辩护指控**

该 TU 仅为编译期常量包装器，无运行时可配置常数。所有字段来自 cmake 生成头 `build_stamp_generated.h`，其源头为 `eng/tools/gen_build_stamp.py`，不在本批审查范围内（属于 eng/** 脚本禁令外）。设计文档 docs/VERSIONING.md「构建指纹合同」为唯一权威依据，代码严格照实实现。

---

---

## lib/infrastructure/scheduler/src/block_frame.cpp

### §2.5 数值处置表

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| block_frame.cpp:12 | kMaxBlockBytes | 1<<32 = 4294967296 B = 4 GiB | 单块内存上限硬限 | 文献值 | BLOCKER-DF-001 同源口径，设计冻结上限 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:13 | kMaxFrameBytes | 1<<34 = 17179869184 B = 16 GiB | 单帧内存上限硬限 | 文献值 | BLOCKER-DF-001 同源口径，设计冻结上限 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:16 | n.size() > 63 | 块名最大长度 63 字符 | 块名字符串命名约束 | 结构性不适用 | CONTRACT-501 块名字符集规则 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:29 | 8 | F64 数据类型字节数 | double 类型大小 | 结构性不适用 | IEEE 754 双精度定义 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:30 | 4 | F32/I32数据类型字节数 | float/int32 类型大小 | 结构性不适用 | IEEE 754/ISO C++ 标准 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:32 | 1 | U8 数据类型字节数 | uint8 类型大小 | 结构性不适用 | ISO C++ 标准 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:34 | return 0 | 未知 BlockDtype 默认返回 | dtype_size 的兜底无效值 | 结构性不适用 | switch 表达式完整实现 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:44 | return "?" | 未知 BlockDtype 默认返回 | dtype_name 的兜底无效值 | 结构性不适用 | switch 表达式完整实现 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:131 | data_.assign(bytes, 0) | 初始化为全零 | 新块数据清零初始化 | 公式导出 | 零初始化确保无噪声基底 | CONFIRMED |
| block_frame.cpp:154 | bytes > kMaxBlockBytes | 单块超出 4 GiB 拒绝创建 | 容量校验失败路径 | 结构性不适用 | kMaxBlockBytes 派生判据 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:155 | bytes_alive() + bytes > kMaxFrameBytes | 总内存超出 16 GiB 拒绝创建 | 容量校验失败路径 | 结构性不适用 | kMaxFrameBytes 派生判据 | 结构性不适用 | CONFIRMED |
| block_frame.cpp:159 | ++created_ | 块创建计数递增 | Track total created blocks | 结构性不适用 | 计数器实现细节 | 结构性不适用 | CONFIRMED |

### Findings

**无可辩护指控**

检索式验证：
- `git -C "/workspace/Astro CS Database" grep "kMaxBlockBytes\|kMaxFrameBytes"` → 仅本文件两处定义，被 create() 调用处引用，与设计冻结上限一致
- `git -C "/workspace/Astro CS Database" grep "valid_block_name"` → 命名约束在 create()/find() 入口统一校验
- `git -C "/workspace/Astro CS Database" grep "dtype_size"` → F64/F32/I32/U8四档覆盖，default 返回 0 作为无效返回值安全处理
- `git -C "/workspace/Astro CS Database" docs/contracts/PIPELINE_BLOCK_CONTRACT.md` → 块名 1..63 小写蛇形，代码严格校验 a-z, 0-9, _

---

---

## lib/infrastructure/scheduler/src/block_flow.cpp

### §2.5 数值处置表

| 位点 (文件：行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| block_flow.cpp:22 | 1099511628211ULL | FNV-1a 乘子常数 | FNV-1a hash 算法固定乘子 | 文献值 | FNV 算法标准文档 RFC 4634 | 结构性不适用 | CONFIRMED |
| block_flow.cpp:244 | 1469598103934665603ULL | FNV-1a 偏移基底 | FNV-1a hash 算法初始值 | 文献值 | FNV 算法标准文档 RFC 4634 | 结构性不适用 | CONFIRMED |
| block_flow.cpp:117 | `!= BlockRole::EXTERNAL_IN` | 外部输入角色判据 | 跳过非 EXTERNAL_IN 块注入 | 结构性不适用 | 数组索引/枚举基盘控制流 | 结构性不适用 | CONFIRMED |
| block_flow.cpp:168 | std::sort/want | 写入集合排序比较 | undeclared write 校验用名字排序 | 结构性不适用 | 实现细节/循环上界 | 结构性不适用 | CONFIRMED |
| block_flow.cpp:267 | std::sort/product_names_ | 产品名列表排序 | 输出前排序保证确定序 | 结构性不适用 | 实现细节/排序操作 | 结构性不适用 | CONFIRMED |
| block_flow.cpp:245 | product_names_.clear() | 容器清空操作 | 重置产品名列表 | 结构性不适用 | 容器初始化 | 结构性不适用 | CONFIRMED |

### Findings

**无可辩护指控**

检索式验证：
- `git -C "/workspace/Astro CS Database" grep "lifecycle_of"` → 仅本函数定义
- `git -C "/workspace/Astro CS Database" grep "BlockLifecycle::SHORT\|BlockLifecycle::FRAME"` → 分散于 lifecycle_of、frame 生命周期逻辑、consume 调用处，与设计一致
- `git -C "/workspace/Astro CS Database" grep "EXTERNAL_OUT.*consumer"` → 设计文档明确允许阶段终产物同时被内部消费，代码按此实现

---  
lib/infrastructure/scheduler/src/block_frame.cpp —— 已读：否  
lib/infrastructure/scheduler/src/build_stamp.cpp —— 已读：否  
lib/infrastructure/scheduler/src/canonical_hash.cpp —— 已读：否  
lib/infrastructure/scheduler/src/checkpoint.cpp —— 已读：否  
lib/infrastructure/scheduler/src/context.cpp —— 已读：否  

<!-- PROGRESS: 6/6 -->

---

## 本批最高危三条

（全部文件读完后补）

---

## 批次汇总统计

**总计**: 6 个文件，39 个数值常数处置项，0 条 finding  

| 文件 | 数值项数 | findings 数 | 状态 |
|---|---|---|---|
| block_flow.cpp | 6 | 0 | 通过 |
| block_frame.cpp | 12 | 0 | 通过 |
| build_stamp.cpp | 1 | 0 | 通过 |
| canonical_hash.cpp | 13 | 0 | 通过 |
| checkpoint.cpp | 3 | 0 | 通过 |
| context.cpp | 4 | 0 | 通过 |

**审查结论**: 本批次 6 个 scheduler 源码文件均无 S1/S2 级科学或设计缺陷；所有硬编码数值均有明确算法、标准、结构性依据或与 Python 镜像逐字节同构。未发现违背 `ASTROCS_DESIGN.md` §8.2/§8.3、CONTRACT-501、docs/VERSIONING.md 的设计偏差。

