# AUDIT-06 第②层独立复核 — 合同说明层（`docs/contracts/` + `eng/contracts/`）

复核基线：HEAD=`c8f64e9a`（`git rev-parse HEAD` → `c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`，branch=main）
开工 `git status --porcelain` 原文（恰两行）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

纪律：严格只读、零 git 写；未运行任何构建/ctest/`run_checks.py`/`eng/**` 检查器；脚本落
`独立审计/复算件/contracts-v\`，`python -B` + `PYTHONDONTWRITEBYTECODE=1`。
本机 `rg` 不可用，计数一律 `git grep`（跟踪集）；判"不存在"另查删除历史。

---

## W1 `pixel_area_power` 两侧取值

**判定：确认（但成稿的事实面不完整——冲突不是"说明层 vs 机器层"，而是"机器层内部两个事实源同名互斥"）**

### 我的独立取证（数 + 命令/脚本路径）

脚本：`独立审计/复算件/contracts-v\w1_area_power.py`
（输出 `contracts-v\w1_sites.txt`；对 `eng/contracts`、`docs/contracts` 全部跟踪文件做花括号配平的
原文扫描，提取每一处 `"pixel_area_power": { … }` 的值约束）→ 共 **69 个约束位点**，
其中 `enum` 位点 **8 个**，且 **8 个 enum 全部是同一个值集 `[-2, 0]`**。

**A 侧（文档 + 条款台账 + 门 = 幂次语义 `{-2, -4, +4}`）**

| 位点 | 原文（截取） |
|---|---|
| `docs/contracts/DATA_SEMANTICS.md:3223-3225` | "内部量纲代数把立体角维记在 `px_power` / `pixel_area_power` 上（`signal_sb = -2`、`sb_variance_out = -4`、`sb_ivar_out = +4`）…**`pixel_area_power = -2 ⇔ 串含 /sr`、`-4 ⇔ /sr^2`、`+4 ⇔ sr^2/…`**" |
| `docs/contracts/DATA_SEMANTICS.md:3249` | "`pixel_area_power` canonical 缺省：`signal_sb=-2`、`sb_variance_out=-4`、`sb_ivar_out=+4`、`flux/Q/W_info/psfsw_robust_weight=0`" |
| `eng/contracts/data/v6_clause_registry_v1.json:2902-2909` | `"pixel_area_power_defaults": {"signal_sb": -2, "sb_variance_out": -4, "sb_ivar_out": 4, …}` |
| `eng/contracts/schemas/product_family_field_constraints.schema.json:253-257` | `"sb_variance_out": {"const": -4}`、`"sb_ivar_out": {"const": 4}`（且 `:242-246` 把这七键列为 `required`） |
| `eng/ci/check_product_contract.py:73-81` | `POWER_DEFAULTS = {"surface_brightness": {"signal": -2, "variance": -4, "ivar": 4}, …}`，注释 `:74` 给出定义式 `p = 2 × (units 中 sr 的指数)` |
| `eng/ci/check_product_contract.py:642-645` | 反漂移断言：`POWER_DEFAULTS[...] != frozen["sb_variance_out"]["const"] → drift`（即机器门**主动钉住 −4/+4**） |
| `lib/infrastructure/aio/v6/src/v6_bunit.cpp:242-253` | `default_pixel_area_power()`：`kSbVarianceOut → -4`、`kSbIvarOut → 4` |
| `lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp:605` | `if (vp != -4) return fail(g, "sb_variance_out pixel_area_power != -4")` |
| `lib/algorithms/resample/p3_rsmp_units.cpp:97-98` | `const Bunit sb_variance_out{2, -4}; const Bunit sb_ivar_out{-2, 4};`（内部 `px_power` 编码 = −4/+4） |

**B 侧（canonical 对象 schema + 对象登记 = 分类语义 `{-2, 0}`）**

| 位点 | 原文 |
|---|---|
| `eng/contracts/schemas/unified/variance.schema.json:103-109` | `"pixel_area_power": {"description": "立体角幂次：面亮度 -2 / 通量 0…", "enum": [-2, 0]}`（`ivar.schema.json:103-109` 同）；同 enum 另见于 `signal:101`、`depth_m5:101`、`frame_snr:103`、`source_snr:104`、`sparse_snr_layer:103`、`point_information:94` = **8 份** |
| `eng/contracts/schemas/unified/variance.schema.json:117-140` | `if bunit pattern "/sr"` ⇒ `pixel_area_power const -2` —— 该 pattern 是子串搜索，**`ADU^2/sr^2` 也命中** ⇒ 强制 −2 |
| `docs/contracts/unified_object_registry.json:25` | "product_family 决定 pixel_area_power（面亮度 -2 / 通量 0）" |
| `eng/contracts/schemas/unified/examples/{variance,ivar}.example.json:9` | `"pixel_area_power": -2`（而 `variance.example.json:6` 的 `bunit` 正是 `ADU^2/sr^2`、`ivar.example.json:6` 是 `sr^2/ADU^2`） |
| `lib/infrastructure/aio/v6/src/v6_provenance.cpp:167-170` | `if (pixel_semantics=="surface_brightness" && pixel_area_power != -2) → 抛错`（生产写出侧 `phase2_integrate.cpp:425,538` 一律写 −2） |

**是否真互斥（不是"讲两件东西"）**：查 `docs/contracts/unified_object_registry.json:31-49` ——
`variance` 对象的 `unit_bunit` = "signal单位^2（面亮度 `ADU^2/sr^2`）"、`ivar` = "1/signal单位^2（面亮度 `sr^2/ADU^2`）"，
与 A 侧默认表的 `sb_variance_out`/`sb_ivar_out` **逐项同一对象**（同名对象的同一个键 `units.pixel_area_power`）。
两侧对该键给出的值集不相交：A 侧要求 −4/+4，B 侧 enum 只容 −2/0。⇒ **真互斥，非语义别物。**
文档侧唯一可称"别物"的是 `px_power`（内部量纲代数），但 `:3223` 自己把 `px_power` 与 `pixel_area_power`
写成"记在…上"的同一槽位，B 侧 schema 的 description 也称该键为"立体角幂次"，故不能用"两个字段"解释掉。

**现行站哪边（生产事实）**：B 侧活、A 侧半死。
`git grep -n "default_pixel_area_power"` → 除声明/定义外**零调用者**；
`git grep -n "gate_bunit_semantics"` → 只被 `eng/tests/unit/v6_p1_drz/v6_p1_drz_test.cpp:174-189` 调用（纯科学核心，未接 session/生产）。
A 侧唯一有牙齿的机器位点是 `check_product_contract.py` 的 T3 + `:642-645` 反漂移断言。

**成稿未见的附加缺陷（我这边新发现，两条）**：
1. `eng/contracts/schemas/unified/{provenance,coverage,rejection,support,validity}.schema.json`
   的 `units` 把该键**pin 成 `const 0`**（`provenance.schema.json:91-93`、`coverage:90`、`rejection:90`、
   `support:90`、`validity:91`），而 `provenance.schema.json:77`（`bunit const "1"`）、`:88`
   （`pixel_semantics const "dimensionless"`）同时被 pin。这 5 份文件里 `allOf` 的
   "含 `/sr` ⇒ −2""`declared_via_provenance` ⇒ surface_brightness/−2" 三支 if-then
   （`provenance.schema.json:120/145/164` 等）在自身 `properties` 约束下**永不可满足 ⇒ 死规则**。
   而 `docs/contracts/DATA_SEMANTICS.md:3250` 恰恰把 `provenance.schema.json` 的这三支点名成"机器强制"出处 →
   **文档指向的强制点是死的，且其 pin 值（0）与文档所述（−2）相反**。
2. `docs/contracts/DATA_SEMANTICS.md:1246-1248`（§20.2 就地复现）自称"以 `BUNIT=ADU/sr` 调 `resolve_bunit`
   ⇒ 返回 `resolvable=false`"、"负对照 `BUNIT=ADU` 且 `pixel_area_power=0` ⇒ `resolvable=true`"。
   读实现：`lib/algorithms/resample/p3_rsmp_units.cpp:169-176` 对 `ADU/sr` 走"显式立体角幂次 ⇒ 恒可判"
   分支返回 **true**；`:182-198` 对裸 `ADU` + `pixel_area_power=0` 只在 `pixel_semantics=IntegratedFlux`
   时可判，句中未改语义 ⇒ 该文档的**正例与负例判据与实现方向相反**（若照它写门会判反）。

### 与成稿差异
成稿 `AUD-101-DB-06-07.md` A-2（:99-101、:141-143）对"文档 −4/+4 vs schema {−2,0}"的定位**方向正确、证据成立**，
但三处不完整/不实：
① 定性为"说明文档 vs schema，schema 为事实源"——**不成立**：−4/+4 同样是机器事实源
（`v6_clause_registry_v1.json:2902-2909` + PFFC `const` + `check_product_contract.py:77-81,642-645`），
照 A-2 的处置"以 schema 为准订正文档"会**直接把 CI 门判红**（`power_defaults_match_v6_frozen` 断言失败）；
② A-3（:104）称 5 份 unified schema "无该键"——**不实**，它们有该键且 pin `const 0`（见上第 1 条），
把 A-3 从"指针错位"升级为"指针指向死规则且取值反向"；
③ 成稿未取 `px_power` 与 `pixel_area_power` 在 `lib` 内**同名混用**这一层（`v6_drizzle_science.h:49`
写 "pixel_area_power(-2 signal / -4 var)" 而 `v6_provenance.cpp:167` 要求 SB 恒 −2），
这是整改必须落笔的位置。

### 定级建议
**P1**。不升 P0 的理由（已定不等式方向）：生产写出面与 canonical schema 同为 −2，A 侧的 −4/+4 目前
只出现在（a）零调用函数、（b）未接线的纯科学核心门、（c）不读取被声明标量的 T3 派生式，
**没有一条现行路径把产品判成 fail-open**；升 P1 的理由：同一键名在两份机器合同里值集不相交，
任一新实现按任一侧落地必在另一侧判红（可复现：把 `variance.example.json:9` 改成 −4 ⇒
`eng/tests/contracts/test_unified_object_contract.py` 的 schema 校验线红；把
`v6_clause_registry_v1.json:2904` 改成 −2 ⇒ `check_product_contract.py:642-645` 反漂移断言红）。
文档层另有两处可就地改红的错误（死指针、判据方向反）随本条一并处理。

### 缺什么证据
无（本条不需上呈即可定案事实面）。**但"取哪一侧为唯一口径"是负责人已登记的"像素面积幂次"待裁事项**，
本条只钉事实不选边。可补强证据（不跑，写出命令）：
`python3 eng/ci/check_product_contract.py --self-test`、
`ctest --test-dir build -R "(unified_object_contract|product_family|v6_p1_drz)" --output-on-failure`
—— 二者应**同时为绿**（证明两语义各守一段互不相遇的语料），这是"当前不是活故障"这一降档理由的机器证据。

<!-- PROGRESS: 1/5 -->

---

## W2 `§9.73 A44` 的锚宿主是否真的无处可寻

**判定：确认存在（引用悬空成立）＋ 定性降级（不是"依据丢失"，是"锚号/出处失效"）；另确认一条成稿未见的机器面：该引用面处在三道锚门的结构性盲区**

### 我的独立取证（数 + 命令/脚本路径）

引用面计数（命令：`git -c core.quotePath=false grep -o "§9.73" | wc -l`；文件数用 `grep -l`）：

| 锚写法 | 跟踪文件数 | 出现处数 |
|---|---|---|
| `§9.73`（全族） | **84** | **294** |
| `§9.73 A44` | **37** | **151** |
| `§9.68` / `§9.71` / `§9.74` / `§9.44` | 24 / 17 / 17 / 5 | 83 / 64 / 47 / 10 |

按顶层目录分（`§9.73` 出现处）：`eng` 114、`lib` 99、`docs` 80、`CMakeLists.txt` 1。

**宿主定位（跟踪集 + 删除历史，两路都查）**：
- 跟踪集：`git ls-files 工程控制` → 仅 `RELEASE-05/**`；`工程控制/RELEASE-05/GAP_AUDIT.md` 65 行，
  节标题只有 `# GAP_AUDIT` + `## G0…G4` + `## 本包不做`（`grep -n "^#\+ "`）⇒ **无 §9.x、无 A44**。
- 删除历史：`git log --diff-filter=D --name-only -- "*GAP_AUDIT.md"` ⇒
  `7ea5c87a`（2026-09-23）删 RELEASE-04；`ae0f1e63`（2026-09-21，"chore(governance): 清理历史控制包与一次性报告（CLEAN-402）"）
  一次删掉 RELEASE-01/02/03/PROJECT-GOVERNANCE-01 四份 + `reports/v6/contract-review/02_CONFLICT_AND_GAP_AUDIT.md`。
- 正文取回：`git show "ae0f1e63^:工程控制/RELEASE-02/GAP_AUDIT.md"`（2872 行）→ **第 2789 行
  `### 9.73 负责人裁决（2026-09-20）：aio 文件级唯一 I/O 边界；不存在「权重模式」概念；CI 文档最后做`**，
  其下含【裁决 U5】/【裁决 A44】/【裁决 7-c】三条，A44 定案 5 条逐字可读。
  RELEASE-03 那份（297 行）另有 5 处以 `§9.73 A44` 为上游裁决引用它。
  ⇒ 成稿"宿主无处可寻"在**工作区**意义上成立；在"证据彻底取不回"意义上不成立（git 历史可逐字取回，
  且 `CONTROL_PACK_SPEC.md:209` 明文要求"收口即清理…任务的完整过程、证据与讨论由 git 历史承载"）。

**该锚的正文约束现在是否有上位宿主（有 ⇒ 锚号错；无 ⇒ 依据丢失）**：**有，且完整。**
- `ASTROCS_DESIGN.md:225`（位于 `### 3.1 数据对象`，节起于 221）："全程只有 SNR，没有"权重模式"这个概念…
  权重是 Phase2 集成时按天球像素对应的输入帧集合现场计算的派生量；Phase1 与 Phase3 不产生、不消费权重"
  ⇒ 覆盖 A44 定案 1、2、5；
- `ASTROCS_DESIGN.md:229`："权重的产生链固定为两步、**没有可选择项**" ⇒ 覆盖"全值域拒绝/无 weight 模式枚举"；
- `ASTROCS_DESIGN.md:227`："权重只能来自纯净信号与噪声之比…PSF 拟合质量代理…作诊断" ⇒ 覆盖 PSFSW 退役；
- `docs/design/UNIFIED_MODEL.md:58` 与 `eng/contracts/data/unified_object_compatibility_map_v1.json#retired_entries`
  ⇒ 覆盖退役对象的接受面=空与迁移处置。
⇒ 定性＝**锚号/出处失效（约束未失去来源）**，不是"裁决失去承载 ⇒ 现行硬约束无据"。

**出处本身还写错了节号（独立复算）**：口径 = `git -c core.quotePath=false grep -n "ASTROCS_DESIGN.md §2.1"`
全量 **27 处**，其中同一行含"权重/weight"者 **14 处 / 11 文件**（逐条已存
`复算\contracts-v\w2_1_weight_context.txt`；按面分：`docs/**` 6 处、`eng/contracts/**` 6 处、
`eng/ci/check_no_weight_mode.py` 1 处、`eng/tests/**` 1 处），而 §2.1 实为"创新点一：测光校准"
（`ASTROCS_DESIGN.md:120`），承载该约束的正文在 §3.1（`:225/227/229`）。
含机器面最重的一处：`eng/ci/check_no_weight_mode.py:135` 的 `"authority": ["GAP_AUDIT.md §9.73 A44",
"ASTROCS_DESIGN.md §2.1", …]` 是该 P0 门 CI 报告里回写的出处字段。

**为什么没有门管得住（三方盲区，脚本 `复算\contracts-v\w2_anchor_scan.py` → `contracts-v\w2_scan.txt`）**：
1. `docs/algorithms/anchors/check_doc_line_anchors.py:55-59` 的 `ANCHOR_RE` 只认
   `<路径>/<文件>.<ext>:<行>` 形态 ⇒ `§9.73` 这类**节号锚根本不进解析面**；
   `anchor_contract.json` 的 `doc_globs` 只有 `docs/**/*.md` ⇒ eng/lib/CMakeLists 的引用不在扫描域。
2. `docs/algorithms/anchors/unresolved_registry.json`（26 条，`max_entries: 26` = 已到棘轮上限）：
   含 `9.73` 的 **22 个字符串叶子全部位于 `reason`/`owner` 自由文本**（条目 15–25，
   `kind: EXTERNAL_REFERENCE`，`doc: docs/research/SNR_WEIGHT_RESEARCH_PACK.md`，登记对象是
   DeepSkyStacker/photutils/properimage 的**外部源码行号锚**）
   ⇒ **没有任何一条把 `§9.73 A44` 登记为未解析锚**。
3. `eng/ci/check_registration_anchors.py`：`SRC_TOP`（:60）虽含 `工程控制/`，但 `_scan_class`（:105）要求
   **值本身整体是一个路径形态**，且键名须在 `LIVE_KEYS`（:65-70，含 `authority/anchor/source/doc_ref/evidence`，
   **不含 `basis/note/banner`**）⇒ 合同登记里那些写在长句中的退役路径（`unified_object_compatibility_map_v1.json:139`
   的 `basis`、`v6_clause_registry_v1.json:2808/3603` 的 `banner`/`note`、
   `product_family_field_constraints.schema.json:2480` 的 `note`）全部**门不可见**。
   我按该门口径自写扫描复算：`eng/contracts/**` 门可见的失配路径引用 5 处，其中 4 处已由
   `eng/ci/ledgers/registration_anchor_ledger.json`（5 条）与 `eng/tools/doccheck/dangling_ledger.json:73-74`
   登记豁免 ⇒ 该门今天不因 A44 判红（第 5 处是我的假阳性：目录形态 `eng/contracts/data/examples/`，
   检查器用 `os.path.exists` 判存在）。
   同一次扫描另见 `eng/contracts/**` 散文里对退役控制包/`run/` 的提及面 =
   `eng/contracts` 6 文件 9 处 + `eng/ci` 22 文件 918 处（含 `checks.json` 755 处）。

### 与成稿差异
成稿 A-1（`AUD-101-DB-06-07.md:97`）的取证与我一致（84 文件、RELEASE-05 无 §9.x、
`ASTROCS_DESIGN.md:225/229` 有对应正文、`§2.1` 与 `§3.1:175` 双重错位），我**复核通过**。
差异在定性与两处补强：
① 成稿标题句"承载该现行硬约束的**裁决正文在仓内无承载文件**"我判为**夸大**：
按 `CONTROL_PACK_SPEC.md:209` 与 AGENTS §7，控制包收口后**必须**出库、过程证据由 git 历史承载；
"逐字原话不在工作区"是规范要求的正常终态，不是缺陷。缺陷只在**引用面仍指向已退役的锚号**
＋**上位节号写错**。⇒ 建议按"锚号重指向 + 出处订正"整改（P2 文字面），
但其机器登记面（下条）单列。
② 成稿未查 `unresolved_registry.json` 里 §9.73 到底登记了什么（我查了：22 个叶子全是自由文本，
**无一条登记 §9.73 本身**），也未定位宿主文件的**删除提交与逐字正文**
（`ae0f1e63^:工程控制/RELEASE-02/GAP_AUDIT.md:2789`）——缺了这两步就无法区分
"锚号错"与"依据丢失"，而定级完全不同。
③ 成稿未记"节号锚在三道锚门里结构性不可见"这一层（我的盲区清单）：这决定了整改**不能只改文字**——
改完仍无门防止它再漂回；须先给锚合同增加"跨文档节号锚"判据（或把 `doc_globs` 与 `LIVE_KEYS` 扩到
`basis/note/banner` 与 eng/lib），并配正负例。

### 定级建议
- **P2**：84 文件 294 处对已退役控制包的节号引用 + 14 处 `§2.1` 错指 ⇒ 就地重指向
  `ASTROCS_DESIGN.md §3.1`（历史留痕只写"git 历史 ae0f1e63 之前版本"，不写行号）。
- **P1（本条真正值得进清单的部分）**：`eng/contracts/**` 与 `eng/ci/**` 的**机器登记字段**
  （`basis`/`note`/`banner`/CI 报告的 `authority`）把不存在的文件写成判据出处，且
  现行锚存活门（`check_registration_anchors.py` R1 + `check_doc_line_anchors.py`）对该形态**结构上不覆盖**
  ⇒ "悬空即缺陷"（ENGINEERING_SPEC §8 规则 4）在合同层无执行面。整改＝扩判据 + 键名入白名单 + 配负例。
- **非缺陷（留痕不删）**：RELEASE-02/03/04 控制包出库本身。

### 缺什么证据
无缺。若要给"门今天不红"一个机器读数，命令（本轮**不跑**，登记为待证）：
`python3 eng/ci/check_registration_anchors.py --json-out run/audit06/reports/registration-anchors.json` 与
`python3 docs/algorithms/anchors/check_doc_line_anchors.py`（后者只跑解析面，不改仓库）。
<!-- PROGRESS: 2/5 -->

---

## W3 配置合同的数值快照与事实源全面失配（`docs/contracts/CONFIG_CONTRACT.md`）

**判定：确认（成稿列出的四组差异逐条复算全部成立）＋ 定级上调（性质不是"抄了旧快照"，是"越位成第二套登记面"，且其复制对象自身也已漂移；按实现逐位复算，在册门 `UT-CONFIG` 在当前 HEAD 必红）**

### 我的独立取证（数 + 命令或脚本路径）

脚本：`复算\contracts-v\w3_config_snapshot.py`（`contracts-v\w3_out.txt`）、
`复算\contracts-v\w3b_citations.py`（`contracts-v\w3b_out.txt`）、
`复算\contracts-v\w3c_anchor_liveness.py`（`contracts-v\w3c_out.txt`）。
全部为只读复算：`json.load` 事实源 + 按检查器源码逐字重现其谓词。

**1) `defaults.json` 面（文档 §2）**

| 事项 | 文档写的 | 事实源实测 | 结论 |
|---|---|---|---|
| 字段总数（标题 line 32） | 50 | `field_count = 59` 且 `len(fields) = 59` | 差 9 |
| `calibration` | 1 | 2（多 `master_flat_median_range`） | 错 |
| `weight`（line 50 整行 + line 36 "weight.default_mode 另有 enum_target/enum_token"） | 1 | **0**（59 键中无任何 `weight.*`） | **整组已注销**；`test_cfg001_contracts.py:43-45` 注释自证："weight.default_mode 组与 phase_config_mosaic 的 algorithm_weight_mode 已同批注销" |
| `enum_target` 承载键 | `weight.default_mode` | 实际唯一承载者是 `snr.path`（值 `sparse_reconstruct`） | 指针错位 |
| `detection/psf/noise/rejection/photometry/precision/upm/hips/drizzle` | 1/2/14/18/6/1/1/1/1 | 逐项相同 | 一致（9 行对） |
| `sparse_snr + scalar_gate` | 3 | 2 + 2 = 4 | 错 1 |
| 文档表中缺的组 | — | `cosmetic 6`、`paths 1`、`snr 1`（合计 8） | 缺 3 组 |
| `authority_status` 值域（line 37） | 3 值 `{sourced, owner_adjudicated, pending_authority}` | **5** 值（另有 `measured` 2、`derived` 1） | 声明比事实源窄 |
| 关键锚点数 | line 39 写 12；§7 表写 11 | `KEY_ANCHORS` 实长 **11**（源码 `:35-50`） | 文档内部自相矛盾（11 对） |
| `module.yaml` 份数（§9 line 206） | 20 | `git ls-files lib -- '*/module.yaml'` = **22** | 差 2（"零旋钮声明"仍成立：10 个旋钮键名 0 命中） |
| 滤镜库 45 条 / channel 计数 / 7 空值名（§4） | 45、10/10/10/4/1/1/2/7、Johnson I/U + SDSS g,i,r,u,z | 逐字相同 | **一致** |
| 转录偏差（§4 末条） | 44 条一致，`Astronomik UV-IR Block L-2` 缺源文件的 `default` 字段 | 复算：45/45 键名相等、**恰好 1 处差异 = 该条缺 `default`** | **一致**（但 §7 门表写"45/45 逐字一致"与该 finding 互斥，须限定比较字段集） |

**2) `config_registry.json#plugin_knobs` 面（文档 §8/§9）**
实测 `len(plugin_knobs) = 120`、`distinct doc = 23`、
`finding = none 68 / gap 21 / unregistered 31`、
`owner_class = science_param 85 / runtime_policy 26 / cli_surface 6 / resource_binding 3`、
`registration = defaults_json 6 / phase_config 18 / inputs_block 8 / cpu_profile 2 / plugin_doc 25 / contracts_doc 3 / cli 6 / none 52`、`conflict 行 = 0`。
文档写的 117 = none 60/gap 25/conflict 0/unregistered 32、82/26/6/3、6/10/8/2/25/3/6/57 ⇒
**行数与三组分布全错**，只有 `23 篇` 与 `conflict=0` 对。
机器文件自身也带一个未被门核对的残数：`totals.rows = 120`（受 `check_01:212` 断言）
∧ `totals.plugin_knobs = 117`（`check_01:214-216` 只遍历 by_finding/by_owner_class/by_registration ⇒ 该键无人管）
⇒ "唯一事实源"内部也有一个手写死数。

**3) CI 注册状态（文档 §8.1 行 1 与 §12 末行）——成稿未查，我查到的是反向事实**
文档两处写 "`eng/ci/checks.json` 无 config 门 / 尚无 UT-CONFIG 步骤 / 建议新增"。
实测：`eng/ci/checks.json:2762` 有 id `UT-CONFIG`（`waivable:false`，
command = `python3 -B -m unittest discover -s eng/tests/config -t eng/tests/config`），
`eng/ci/ID_MIGRATION_MAP.md:432` 记 "W4-A3 注册为 CHK-UNIT 执行单元（原无 CI 步骤）"，
`eng/packaging/config/config_registry.json:2164` 的 `required_entry` 也要求它。
⇒ 文档登记的是一个**已闭合的缺口** ⇒ 它复述的不只是数值，还有**机器注册表的状态** = 第二套登记面的第二个证据。

**4) 门在 HEAD 上的读数（按实现逐位复算，未运行）**
`eng/tests/config` 的检查器清单为 `CHECKS = CFG002-01..11`（`check_cfg002_registry.py:1018-1030`），
文档 §7 表只列到 CFG002-10（且包装测试的 docstring 也写"10 项检查"）⇒ 第 11 项 `check_11_contract_doc_citations`
在合同里根本不存在。三条我复算为必红的断言（谓词逐字取自源码）：

| 断言 | 源码位置 | 我的复算 |
|---|---|---|
| `grep_line(path, line, token)` 对 `KEY_ANCHORS` 11 条全部成立 | `test_cfg001_contracts.py:218-222` + `cfg_common.py:99-103`（`token in lines[line-1]`） | **8 条不成立**：STAR_DETECTION:21（token 实在 65/69/79）、PSF:94、NOISE_MODEL:86/:23、REJECTION:68（实在 116/274）、PHOTOMETRY:30（实 34/219）、SCIENCE_SCOPE:56、PHASE3_HIPS_TO_FITS:41 ⇒ `test_every_source_ref_resolves_and_key_anchors_hold` 红 |
| CFG002-09：`defaults.json` 每个 `source_ref` 行存在且非空 | `check_cfg002_registry.py:734-736`（空行/越界直接 problem，paraphrase 例外只豁免 hint 子判据 `:750`） | 53 条中 **10 条 problem**：NOISE_MODEL:47/85/87、REJECTION:59 等 8 处空行 + 2 条指向 `run/LINDEF-CLOSE-01/REPORT.md`（gitignore 临时面，本机与干净克隆都不存在）⇒ 红 |
| CFG002-11：8 个 `contract_doc_citations.token_anchors` 的 ref 必须在正文出现且被引行含 token | 同文件 `:777-822` | **5 条不成立**（`REJECTION.md:68` 正文已不含该 ref；STAR_DETECTION:21、PHASE3:41、PHASE3:72、DRIZZLE:25 被引行不含 token）⇒ 红 |

⇒ `test_cfg002_registry.py::test_all_ten_checks_green`（`assertEqual([], fails)`）红 ⇒ 注册在册的 `UT-CONFIG` 红。
**根因（可机检、成稿未记）**：`UT-CONFIG` 的 `changed_paths` 为
`eng/tests/cli/**, lib/**, lib/infrastructure/cli/**, lib/include/**, eng/tests/config/**,
eng/contracts/schemas/**, eng/packaging/config/templates/**, eng/packaging/config/**`
—— **不含 `docs/**`** ⇒ 它钉的正是 `docs/science/**` 的行号，却永不被那些文件的改动触发。
这正是 `eng/tools/quality/check_path_domain_anchors.py:20-24` 自己描述过的"路径域静默失效"形态，
只是那个门只校验"路径域基目录存在"，管不到"域过窄"。
另：CFG002-11 的结构判据正则要求 `file.ext:digits` 紧邻，文档 §0 的
"`ASTROCS_DESIGN.md` §4.3（:224-261）"式锚（我复算出 **14 处**，含 §4.3/§4.5/§7.2 全错 47–54 行）
根本不进解析面 ⇒ 那批锚**改对了也没有门守**。

**5) 顺带钉到的机器面新缺陷（成稿未查，属本层）**
`eng/contracts/config/module_dll_contract.schema.json` **不是合法 JSON**：
第 180 行 `"...|^acsd\.exe$"` 用了非法转义 `\.`，`json.load` ⇒ `Invalid \escape: line 180 column 85`。
同目录另 3 份 schema 合法。该文件被 `CONFIG_CONTRACT.md:241` 列为"模块/CLI 契约 schema 事实源"之一、
被 `config_registry.json:2144`、`check_module_map.py:11`、`eng/tests/conformance/noop/README.md:42` 引用为合同，
但**无任何门 `json.load` 它**（CFG002-07 只按文件名判归属，`:949-952`）⇒ 一份解析即崩的机器合同长期在库。

### 与成稿差异
成稿 D-1…D-8（`:40-64`）我逐条复核：**全部成立**，与我独立取到的数一致（50/59、3 组缺失、
117/120 与三组分布、`totals.plugin_knobs=117` 自相矛盾、`authority_status` 3/5、D-5 的三处锚互不相同、
D-7 kernel 接线文档 line 138 失实、D-8 主分支/兼容分支说反）。差异是**定性与外延**：
① 成稿定性为"下位与上位/事实源冲突…数值快照使局部内容越位到登记面"，处置="就地订正 + 数值快照改指针"。
我补两条硬证据把定性钉死为**越位成第二套事实源**：它连 **CI 注册状态**（UT-CONFIG 有无）与
**检查器清单（10 vs 11 项）** 都各存了一份，且两份都已与事实源不等 ⇒ 不只是数字旧，
而是文档在行使登记册职能；
② 成稿未记 `UT-CONFIG` 已在 `checks.json`、其 `changed_paths` 不含 `docs/**` ⇒ 没给出漂移的**根因**，
按成稿处置（改数字）改完仍会再漂；
③ 成稿未记三条断言在 HEAD 复算必红（含 2 条 `run/` 临时面锚点）⇒ 漏掉了本条里最紧急的部分；
④ 成稿未记 `module_dll_contract.schema.json` 非法 JSON。

### 可机检替代判据（合同里不该出现可漂移的计数值，该出现什么）
**禁**：正文出现裸计数/分布（正则面：`（\d+ 字段`、`\d+ = none \d+ / gap \d+`、表格"字段数"列、
"实测分布：…"、"N 项检查"、"无 X 步骤"）。
**该出现**：`事实源指针 + 期望谓词`，且门把两侧同时读进来算相等：
```
正例：正文含 `字段总数 = eng/packaging/config/defaults.json#field_count`，且全文无裸计数 ⇒ 绿
负例 a：把该指针替换成 "59 字段" ⇒ 红（NO_DRIFTING_LITERAL_IN_CONTRACT：数字未绑定指针）
负例 b：删掉指针句 ⇒ 红（POINTER_MISSING）
负例 c：把 defaults.json 的 field_count 改成 60 而不同步 ⇒ 已有门（check_01 totals / TestDefaultsContract）红，
        同一门必须能同时因文档侧数字改动而红 ⇒ 证明两侧都进了判据（否则判据仍是单向）
CI 状态类条款同理：只允许写 `检查项清单 = eng/ci/checks.json 的 id 集合`，
门断言"文档若含 id 名字，则该 id 必须存在于 checks.json；若含『无 X 步骤』句式即判红"。
```
配套必须一起做的两件事（否则新判据本身 fail-open）：
(i) `UT-CONFIG.changed_paths` 增加 `docs/science/**`（或 `docs/**`），让锚门被锚的变动触发；
(ii) CFG002-11 的锚正则扩到 `file.md §x（:a-b）` / 反引号分隔形态（当前 14 处不可见）。

### 定级建议
- **W3-a 说明层第二套登记面**：**P1**（文档 §2/§8/§9/§12 的计数、分布、CI 状态、检查器清单四类复述全部失配）。
- **W3-b `UT-CONFIG` 复算必红 + 其 `changed_paths` 不含 `docs/**`**：**P0**。
  理由（不等式方向已定）：这是 `waivable:false` 的在册门，红 ⇒ 合同层任何整改都没有绿基线；
  且判据是纯文件内容的确定函数，无环境依赖。若前台复跑读数为绿，则问题升级为"门静默退化"，仍是 P0。
- **W3-c `module_dll_contract.schema.json` 非法 JSON 且无门覆盖**：**P1**。

### 缺什么证据
仅缺"官方 rc 读数"这一格（本轮纪律不跑）。命令（登记为待证，须由前台/第①层执行）：
`python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 4 -- python3 -B -m unittest discover -s eng/tests/config -t eng/tests/config -v`
（即 checks.json 中 `UT-CONFIG` 的原命令，套内存看门狗）；
以及 `python3 eng/tests/config/check_cfg002_registry.py --self-test`（确认 11 项与注入面）。
其余全部为就地可复算事实，不缺证据。

<!-- PROGRESS: 3/5 -->

---

## W4 `must_contain` 与唯一源互斥（落盘形态合同批次主结论）

**判定：确认（互斥成立、触发面缺口成立）＋ 处置方案降级（成稿给的"换成指针判据"会净失一项现行判据强度，引入新 fail-open；须改为双向判据）**

### 我的独立取证（数 + 命令或脚本路径）

脚本：`复算\contracts-v\w4_layer_tokens.py`（输出 `contracts-v\w4_out.txt`）。
判据实现逐字读自 `eng/tools/hipsform/check_hips_storage_form.py:735-762`
（`check_doc_consistency`：D1 = `for tok in ent["must_contain"]: if tok not in texts[rel]` —— **纯子串存在性**；
D2 = `forbidden_synonyms` 在**同这 11 份文件**文本里 0 命中）。词表与 layers 见
`eng/contracts/schemas/hips_storage_form.schema.json:13-43`（11 条 layer，共 43 个词位需求）。

**分层读数（我把每层的词位出现拆成"正文 prose"与"围栏代码块"两侧，围栏按 ``` / ~~~ 配对切分）**：

| layer | 文件 | 需词数 | prose 命中 | 代码块命中 | 含"唯一词表/schema 锚"指针 | 清单式复述行（同行 ≥2 词表 token） |
|---|---|---|---|---|---|---|
| top-design | `ASTROCS_DESIGN.md` | 5 | 6 | 0 | 3 | 2 |
| design | `docs/design/PRODUCT_STORAGE_FORM.md` | 6 | 35 | 8 | 3 | 10 |
| design-p1 | `docs/design/PHASE1_DETAILED_DESIGN.md` | 3 | 7 | 0 | **0** | 2 |
| design-p2 | `docs/design/PHASE2_DETAILED_DESIGN.md` | 2 | 2 | 0 | **0** | 1 |
| design-p3 | `docs/design/PHASE3_DETAILED_DESIGN.md` | 1 | 1 | 0 | **0** | 0 |
| contract | `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md` | 9 | 65 | 12 | 6 | 23 |
| contract-config | `docs/contracts/CONFIG_CONTRACT.md` | 4 | 8 | 0 | 3 | 2 |
| interface-io2 | `docs/interfaces/io/IO_002_HIPS_INPUT_INTERFACE.md` | 2 | 2 | 0 | **0** | 0 |
| interface-io3 | `docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md` | 3 | 9 | 0 | 1 | 3 |
| engineering | `ENGINEERING_SPEC.md` | 5 | 10 | 0 | 5 | 2 |
| acceptance | `ACCEPTANCE_SPEC.md` | 3 | 15 | 0 | 1 | 6 |

⇒ 计数结论：**靠字面复述满足 D1 的层 = 11/11（0 缺词，全绿）；纯指针零复述的层 = 0；
已同时含词表锚指针的层 = 7；无任何词表锚指针的层 = 4**（design-p1/p2/p3、interface-io2）。
`forbidden_synonyms` 8 个同义名在 11 层里 **0 命中** ⇒ D2 今天为真但**从未被任何改动检验过**
（它只在 self-test 的注入用例 N17 里被"变红"，真实面全绿）。

**互斥的构造性证明（用最少词层做最小反例，比成稿的论证更可执行）**：
`design-p3` 只要求 1 个词 `storage_form`，且在文中只出现 1 次。
`ASTROCS_DESIGN.md:690` 与 `ENGINEERING_SPEC.md:143` 自己写"**本节不复制字段清单**"、
`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md:181` 写"…只在那里定义一次，本文档与各层文档**只引用**"。
⇒ 任一层若按其上位口径把清单收成纯指针，D1 立即对该层判红（`未出现登记词`）；
`P10` 用例（`check_hips_storage_form.py:1085-1089`，真仓库文本）随之 fail ⇒ self-test 退 1 ⇒
`CHK-HIPS-STORAGE-FORM` 红。互斥成立 = **确认**。

**触发面（独立复算）**：`eng/ci/checks.json` 的 `CHK-HIPS-STORAGE-FORM`：
`profiles=[fast, linux-main, windows-main]`、`waivable:false`、
command = `check_hips_storage_form.py --self-test --json-out run/ci/hipsform/self_test.json`；
`changed_paths` 16 项含 8 份被断言文档，**不含** `ASTROCS_DESIGN.md` / `ENGINEERING_SPEC.md` /
`ACCEPTANCE_SPEC.md` ⇒ 改这三份根规范不重跑本门 = **确认**。
补一条成稿未记的边界：`docs/ci/CI_SPEC.md §2.4-2` 的敏感面升级清单含 `eng/contracts/**`
⇒ 改**词表 schema 自身**会自动升全量（能红）；改根规范不会。所以真实暴露面是
"词表不动、只改根规范措辞 ⇒ 增量档不红，只有显式 `--all` 才红"。

**成稿处置方案的两处 fail-open（任务点名的两个问题，我的答案）**：

1. **"既含指针又缺词的正确文档"判什么？** 按成稿的 `must_point_to`（"至少命中一个锚字符串"）⇒ **绿**（这是它想要的效果）。
   但同时 **今天由 D1 抓、改后没人抓的那一类**会一并变绿：某层把取值写错（`bare` 写成 `zst`、
   或引入未登记取值）却没有引入同义名时，`forbidden_synonyms` 不命中、指针仍在 ⇒ 绿。
   这正是现行 `N18` 注入用例（`:1181-1187`，`archive→zst`）今天能红、换成指针判据后**恒绿**的那一面。
   成稿自己指出 N18 要重写为"删指针 ⇒ 红"——那是把一条**取值精确性**判据换成一条**存在性**判据，
   **判据净强度下降**，不是等价改写。
2. **"把词放进代码块引用里蒙混"判什么？** 两种判据**都判绿**，且这不是假想：
   `design` 层有 8 次、`contract` 层有 12 次词表 token 出现在围栏代码块内，今天的子串判据已把它们计入满足。
   换成朴素指针判据后多出一条新的蒙混路：锚字符串可以只出现在代码块、表格单元或反例清单里
   （子串相等即算命中），而它本该是"本节口径由该处定义"的正文承诺。
   ⇒ **必须要求指针落在非围栏正文**，否则判据可被引用块绕过。

**我认为该采用的判据（双向、可机检，我已实现其全部谓词）**：
- **D1a（允许指针）**：层文本的**非围栏正文**中必须出现可解析的唯一源锚（`hips_storage_form.schema.json#x-astrocs-field-vocabulary`
  或合同正本 §10 指针），且锚串必须逐字命中，不给"任一候选即合格"的宽松面；
- **D1b（复述一旦出现就必须精确）**：该层若出现任何词表 token 或取值 token（`archive`/`bare`），
  则其出现的取值集合必须 ⊆ 词表 `values`，且不得与词表外 token 同行共现于"字段=值"形态；
  ⇒ 这一条保住 `N18` 的强度，且对纯指针文档自动豁免；
- **D1c（复述受罚，可机检）**：同一非围栏行内出现 ≥3 个词表 token 且该层**不含** D1a 锚 ⇒ 红
  （我今天已用它量出 9 层共 51 条复述行 ⇒ 有正例基线，不是造不出来的判据）；
  成稿 A.4 末段把"反重复"判为"机器不可判"，**此点我推翻**：围栏切分 + 同行 token 计数是确定性的，脚本已在
  `复算\contracts-v\w4_layer_tokens.py`。
- **配套负例（必须齐 4 条才能改判据）**：
  ①纯指针且不复述 ⇒ 绿（证新判据不惩罚唯一源，即今天会红的构造）；
  ②删指针且无复述 ⇒ 红（D1a 非恒真）；
  ③保留指针但把 `archive` 改成 `zst` ⇒ 红（D1b 非恒真，替代 N18）；
  ④把 D1a 锚塞进围栏代码块 ⇒ 红（D1a 不可被引用块绕过）。
  正例：现仓库真实 11 层 ⇒ 绿（基线读数已取）。
- 另须一起改的：`changed_paths` 补三根规范；`ACCEPTANCE_SPEC.md:125` 与 `ENGINEERING_SPEC.md:143`
  的"每层必须出现其登记词"表述同步改；D2 扫描面从"仅这 11 份"扩到 `docs/**` + 三根规范
  （成稿已判为"已知低估"，我复核：今天 8 个同义名在 `docs/**` 全域的命中数我未测 ⇒ 见"缺什么证据"）。

### 与成稿差异
成稿 §A（`AUD-101-DB-08.md:231-300`）的主结论、判据实现读数、CI 触发面缺口、验收侧升格
（`ACCEPTANCE_SPEC.md:125`）、四条"改后失合规"清单**我全部复核为成立**；
`11/11 层 0 缺词`与我独立跑的数一致。差异三处：
① 我给出成稿没给的分布指标（prose vs 代码块拆分、清单式复述行计数、含锚层 7 / 无锚层 4），
   据此指出**互斥的可执行最小反例是 `design-p3`（1 词 1 现）**，而不是成稿用的"删 §689-691"那种大动作；
② 成稿处置 `must_point_to`（任一锚命中即可）我判为**引入新 fail-open**：丢 D1 的取值精确性、
   且不区分正文指针与代码块引用 ⇒ 降级为"方向对、方案不成立"，须替换为上面 D1a/b/c；
③ 成稿 A.4 称"反重复机器不可判"，我推翻（已实现并出数）。

### 定级建议
- **W4 主结论（合同说明层与机器门对'复述/指针'互斥要求 + 触发面小于断言面）**：**P1**。
  不升 P0：D1/D2 本身是文档一致性判据，不产生产品侧 fail-open（不判错产品），
  且 full 档仍能红；升 P1 的理由是它**结构性惩罚唯一源范式**，会把后续所有文档整改推向"继续复述"，
  且验收规范（`ACCEPTANCE_SPEC.md:125`）已把该复述写成验收判据 ⇒ 错误口径已进入发布门。
- **成稿处置方案本身**：不进 P0/P1 清单，按"方向确认、方案需替换（D1a/b/c + 4 条负例）"退回第①层重做。

### 缺什么证据
1. `forbidden_synonyms` 在 `docs/**` 全域（超出 11 层的扫描面）的命中数未测 ⇒ 决定 D2 扩容后是否立刻红：
   `git grep -n -F -e storage_mode -e storage_format -e output_form -e pack_form -e index_paths -e coverage_index_path -e frame_index -e index_file -- docs '*.md'`
2. `CHK-HIPS-STORAGE-FORM` 的官方 rc 与 `P10` 读数（本轮纪律不跑）：
   `python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 4 -- python3 eng/tools/hipsform/check_hips_storage_form.py --self-test --json-out run/audit06/hipsform/self_test.json`
3. 新判据的正例基线需在第①层实现后重跑一次，确认 11 层全绿（否则改判据即改出门禁回归）。

<!-- PROGRESS: 4/5 -->

---

## W5 裸形态是否允许没有产品级索引（三方冲突）

**判定：确认（冲突成立且比成稿说的更硬——不是"三方"而是"五处口径 + 同一判据两侧相反"，其中合同自身条款互斥到不可同时满足）**

### 我的独立取证（数 + 命令或脚本路径）

逐处取原文（命令：`git grep -n "裸形态" -- docs` ＋ `git grep -n "index_path" -- docs eng ACCEPTANCE_SPEC.md`）：

| # | 位置 | 原文口径 | 站哪边 |
|---|---|---|---|
| 1 | `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md:35`（N7） | "裸形态**允许**无产品级索引；此时读端按目录枚举重建覆盖，并在 provenance 记降级" | 允许无 |
| 1' | 同文件 `:140`（R5） | "归档形态缺索引 ⇒ fail-closed；裸形态缺索引 ⇒ 目录枚举重建 + provenance 记降级" | 允许无 |
| 2 | 同文件 `:206-207` + `:218-221`（§10.2 表 + F1/F3/F4） | 逐帧 `index_path` / `index_sha256` 为 **string（无 null 分支）**；"F1 四个字段一个不少——缺任一 ⇒ 不可追溯（fail-closed）"；"F4 `index_sha256` 必须与**磁盘索引字节**一致" | **要求有（与 N7 同文件相反）** |
| 3 | `docs/design/PRODUCT_STORAGE_FORM.md:153` | "裸形态写出：与现状相同…**额外写出产品级索引**"（`:124` 只说裸形态 `tiles` 省略，索引本身仍在） | 要求有 |
| 4 | `eng/contracts/schemas/hips_storage_form.schema.json` `$defs.frame_storage` | `required = [storage_form, index_path, index_sha256, archive_sha256]`；`index_path` 描述逐字："与 `hips_path` 同父目录；**两形态都必须有**"；只有 `archive_sha256` 的 type 是 `["string","null"]` | 要求有 |
| 5 | `ACCEPTANCE_SPEC.md:114` + `:122` | "两形态都能写 …（**含产品级索引**…）"；"逐帧必须带齐 `storage_form`/`index_path`/`index_sha256`/`archive_sha256`…**负例红**：缺 `index_path` ⇒ 判红" | 要求有 |

⇒ 允许无 = 1 处（合同 N7/R5 两行）；要求有 = 4 处（合同自身 §10.2、设计正本、机器 schema、验收规范）。

**读侧实现站哪边（逐字读 `eng/tools/hipsform/check_hips_storage_form.py`）**：判据实装在**同一次检查里两侧相反**——
- 磁盘校验线 `validate_product:550-556`（bare 分支）：`errs += validate_hips_dir(...)`，
  然后 `if idx_path.is_file():` 才校验索引 ⇒ **缺索引静默通过**，既不判红也不记 N7 承诺的"provenance 记降级"
  （全文件 `grep -n "目录枚举\|降级\|degrad"` = **0 命中**）；对照归档分支 `:505-506` 是
  `return ["归档形态缺产品级索引 …（N6，fail-closed）"]`。
- 输出清单线 `check_frame_storage:658-679`（F1..F4）：`fields = vocab["frame_fields"]`，任一缺失即
  `["逐帧条目缺形态字段 …（输出 JSON 必须携带索引路径与指纹）"]` ⇒ 两形态都必须申报 `index_path`。
⇒ 一个"裸形态 + 不带索引"的产品：磁盘线绿（N7），清单线红（F1）；即便把 `index_path` 填上，
F4 要求"与磁盘索引字节一致"也无从满足 ⇒ **N7 许可的那个状态在机器判据下不可达**。
self-test 用例逐条数过（`grep -n 'case("'` = **30 例**）：有 `N2 归档缺索引 → 红`、`N13 输出 JSON 逐帧缺 index_path ⇒ 红`，
**没有任何一例覆盖"裸形态旁无索引文件"**（`P1 bare 正例` 在 `:789-790` 明确写了索引）
⇒ N7 是一条既不被正例证明、也不被负例拦截的**判据空洞**。

**"记降级"是否有实现**：`git grep -n "index\.json" -- lib` ⇒ 生产侧 `lib/**` **零命中**（唯一命中
`lib/infrastructure/cli/session_commands.h:214-215` 是注释）；`git grep -c "hips\.zst" -- lib` ⇒ 同一注释一处
⇒ C++ 读写两侧都还没有形态与索引实现；`eng/ci/ledgers/dead_config_keys.json` 已把 `storage_form` / `coverage_index`
登记为 dead key（"CLI 当前只识别并透传，生产写出侧属分阶段实现计划阶段 2"）。
⇒ 本条不产生现行产品误判（不等式方向：今天的红只在 python 判据线上，该产品面尚未接线）。

**必须二选一并贯穿（我的具体改法，逐处点名）**
选 **A：两形态都必须有产品级索引**（与 4:1 的口径面同侧，不新增实现债）：
1. `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md:35`（N7）改写为"裸形态**必须**有产品级索引
   （缺 ⇒ 与归档形态同等 fail-closed）；裸形态省略的是 `subproducts[].tiles`（路径即定位，§4.1）"；
   `:140`（R5）同改；`:162` 的异常表现表补一行"裸形态缺产品级索引 | 产品不完整错误（fail-closed）"；
   若负责人要保留"用户手放裸目录"的可读性，把"目录枚举重建"降格为**只读兼容路径**并明写
   "本仓生产者不得产出该状态"。
2. `eng/tools/hipsform/check_hips_storage_form.py:550-556`：bare 分支改为缺索引即
   `return ["裸形态缺产品级索引（N7′，fail-closed）"]`，并新增 self-test 负例
   `N19 裸形态缺索引 ⇒ 红`（与既有 `N2` 成对；否则新判据无牙齿）。
3. **改完谁能红**：`CHK-HIPS-STORAGE-FORM`（`checks.json` 在册、`waivable:false`）。
   改 1 命中其 `changed_paths` 的 `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md` ⇒ 增量档即红；
   改 2 命中 `eng/tools/hipsform/**` ⇒ 同档红。`ACCEPTANCE_SPEC.md:114/122` 与
   `frame_storage` schema **一字不用改**（本来就在 A 侧）。
4. 收尾一致性：`x-astrocs-field-vocabulary` 的 `note` 与 `layers[contract].must_contain` 用到、
   但词表 `frame_fields`/`manifest_storage_fields` 未登记的 `archive_bytes`（只在
   `$defs/manifest_storage_product` 出现）应补进词表，否则"唯一词表"仍漏项（同批还有 `tree_hash`）。

选 **B：保留 N7 允许无索引** ⇒ 代价明显更高，四件事缺一即 fail-open：
`frame_storage.index_path` 改 `oneOf[string,null]` + 新增不变式 F5
（`bare ∧ 无索引 ⇔ index_path=null ∧ index_sha256=null`）；F4 加"索引存在时"前缀；
`ACCEPTANCE_SPEC.md:114/122` 的"含产品级索引 / 缺 index_path 即判红"放宽；
`lib/**` 补目录枚举重建 + provenance 降级记录，并给降级配一条非恒真判据。
⇒ 我判 **A**；B 只在负责人明确主张"外部手放的裸目录也是合法产品"时才成立。

### 与成稿差异
成稿（`AUD-101-DB-08.md:68-72`）已定位 4 行 N7 / 140 行 R5 与 design:19、schema:358、验收:114 的对立，
并指出实装 `check_hips_storage_form.py:552` 站"可有可无"侧，处置写"二者必居其一，不得并存"——
**方向一致，判为确认**。差异四处：
① 成稿记为"三方冲突（三份上位/平行 vs 本合同）"；我复算是**五处口径，且最硬的一处冲突在合同自身内部**
（§10.2 F1/F3/F4 与 N7/R5 同文件相反）⇒ 标准 01"以上位为准"的机械处置在这里用不上，
必须删一条，成稿给出的两条路中"上位三处统一下调口径"我判为**代价未标价**（见选 B 的四件事）；
② 成稿未记**同一检查器两侧相反**（`validate_product` bare 容忍 ⇔ `check_frame_storage` 强制），
也未记 **N7 许可态在 F4 下不可达**；
③ 成稿未记 **30 例 self-test 无一例覆盖裸形态缺索引**（判据空洞）与
"**provenance 记降级**在 `eng/tools` 与 `lib/**` 双零命中"；
④ 成稿的处置只到"订正 N7/R5"，未给实现侧改动点与配套负例 ⇒ 按其改法文档改完仍是
"文档说必须有、判据仍容忍"，整改不闭合。

### 定级建议
**P1**。不升 P0 的依据（不等式方向已定）：`lib/**` 对该形态产品面零实现、
CLI 键已登记 `dead_config_keys.json` ⇒ 今天没有任何产品会被误判为合格；
升 P1 的依据：合同自身两条款互斥到不可同时满足、读侧判据与清单判据方向相反，
生产者一旦接线（阶段 2）会同时拿到"绿"和"红"两个读数；且 `ACCEPTANCE_SPEC.md:114/122`
已把"必须有"写进验收判据 ⇒ 错误口径已进入发布门。**若负责人选 B ⇒ 本条升 P0**
（需新实现 + 新降级判据 + 放宽验收）。

### 缺什么证据
无关键缺口。补强读数（本轮纪律不跑）：
`python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 4 -- python3 eng/tools/hipsform/check_hips_storage_form.py --self-test`
—— 预期 30/30 绿（该读数即"无裸形态缺索引例"的官方证明）；A 方案落地后必须出现 `N19` 且读数为红→绿对偶。

<!-- PROGRESS: 5/5 -->

---

## 进 P0/P1 的条目（只列"确认"）

| 编号 | 条目 | 定级 | 问题 / 谁修 / 怎么算修好 |
|---|---|---|---|
| W3-b | 在册门 `UT-CONFIG`（`waivable:false`）按实现逐位复算在当前 HEAD 必红：`KEY_ANCHORS` 11 条中 **8 条** token 不在被引行、`defaults.json` 53 条 `source_ref` 中 **10 条**越界/空行（含 2 条指向 gitignore 的 `run/LINDEF-CLOSE-01/REPORT.md`）、CFG002-11 的 8 个 token 锚中 **5 条**不成立；且该门的 `changed_paths` **不含 `docs/**`** ⇒ 它钉的正是 `docs/science/**` 行号却永不被其改动触发 | **P1**（若第①层门读数显示它不在 `RELEASE-05/GAP_AUDIT.md` G2-5 所记"linux-main 15 条内容面红"内，则升 **P0** = 未登记红） | CI 线：`changed_paths` 补 `docs/**`；锚线：按现行科学文档重锚（REJECTION 阈值块真身在 :115 而非 68/97/166 任一）。修好判据 = `UT-CONFIG` 全绿，且故意改一行 `docs/science/REJECTION.md` 能触发该门 |
| W3-a | `CONFIG_CONTRACT.md` 把机器事实抄成第二套登记面且已全线失配：字段总数 50/59、`weight` 整组注销仍占一行、`enum_target` 承载键写错、`authority_status` 3/5 值、登记册 117/120 与三组分布全错、§7 表列 10 项门而代码有 11 项、§8.1/§12 声称"无 `UT-CONFIG` 步骤"而 `checks.json:2762` 已有 | **P1** | 文档线：计数/分布/CI 状态/门清单四类全部改指针，并新增"数字必须绑定指针"的门（正负例见 W3 节） |
| W3-c | `eng/contracts/config/module_dll_contract.schema.json:180` 不是合法 JSON（非法转义 `\.`），被 `CONFIG_CONTRACT.md:241` 与 `config_registry.json:2144` 当作模块/CLI 契约事实源，却无任何门 `json.load` 它 | **P1** | 合同线：修转义 + 让"登记为 schema 的文件必须可解析"进门；判据 = 注入一处非法 JSON 即红 |
| W1 | `pixel_area_power` 一名两义、两侧各有机器事实源：`schemas/unified/{variance,ivar}.schema.json:103-109` enum `[-2,0]`（分类语义）⇔ `data/v6_clause_registry_v1.json:2902-2909` + `product_family_field_constraints.schema.json:253-257` 的 `const -4/4` + `eng/ci/check_product_contract.py:77-81,642-645`（幂次语义）；另 5 份 unified schema 把该键 pin 成 `const 0` 使其自身 if-then 成死规则、`DATA_SEMANTICS.md:3250` 恰指向该死点；§20.2 就地复现的正/负判据与 `p3_rsmp_units.cpp:169-198` 方向相反 | **P1**（唯一口径的选边属负责人待裁事项） | 科学/合同线：两语义拆名或扩值域，二者都必须同步改 `check_product_contract.py:642-645` 反漂移断言；判据 = 改任一侧值另一侧必红（今天是两侧各自为绿） |
| W4 | `layers[].must_contain`（D1 纯子串）与上位"本节不复制字段清单"互斥：11/11 层靠字面复述保绿、**纯指针 0 层**、7 层已有词表锚指针、4 层无；该门 `changed_paths` 不含三份根规范；`ACCEPTANCE_SPEC.md:125` 已把复述升格为验收判据 | **P1** | 门禁线：D1 改双向判据（D1a 非围栏正文的可解析指针 / D1b 一旦复述必须精确 / D1c 无锚不得清单式复述）+ 4 条负例 + `changed_paths` 补三根规范 + 同步验收表述 |
| W5 | `HIPS_STORAGE_FORM_CONTRACT.md` N7/R5（裸形态可无索引）与**同文件** §10.2 F1/F3/F4、设计正本 `:153`、`frame_storage` schema（"两形态都必须有"）、`ACCEPTANCE_SPEC.md:114/122` 对立；实装两侧相反（`validate_product:550-556` 容忍缺索引 ⇔ `check_frame_storage:658-679` 强制申报）；30 例 self-test 无一例覆盖该态；"provenance 记降级"双零实现 | **P1**（负责人若选 B ⇒ 升 P0） | 合同线（推荐 A）：改 N7/R5/异常表 + bare 分支 fail-closed + 新增 `N19 裸形态缺索引 ⇒ 红`；判据 = 造一个裸形态无索引产品即红 |
| W2-b | 机器登记字段（`basis`/`note`/`banner`、CI 报告 `authority`）把已出库控制包写成判据出处，且该形态在三道锚门里结构不可见（`ANCHOR_RE` 只认 `file.ext:line`、`doc_globs=docs/**/*.md`、`LIVE_KEYS` 不含这三键且要求整值路径形）⇒"悬空即缺陷"（ENGINEERING_SPEC §8 规则 4）在合同层无执行面；`§2.1` 错指 14 处（正文实在 §3.1） | **P1** | 门禁线：锚形态与键白名单扩容 + 引用重指 `ASTROCS_DESIGN.md §3.1`；判据 = 把 `basis` 改指不存在路径即红（现状改也不红） |
| W2-a | 84 文件 294 处（`§9.73 A44` 37 文件 151 处）节号引用指向已按 `CONTROL_PACK_SPEC §9` 退役的章节锚；约束本体在上位文档存在 | **P2** | 文档线就地重指（逐字原话只写"git 历史 `ae0f1e63` 之前版本"，不写行号） |

## 需要负责人裁的

只有两问，其余五条判据与定级我已自行定案（见上表）。

**Q1（已在册"像素面积幂次"待裁事项的续，本轮只补事实与两案代价，不重复发问）**
取哪一侧为唯一口径。我走尽三条取证路仍不能自行定案：
(a) 文档侧全部取值与语义句（`DATA_SEMANTICS.md:3223-3225/3249/1244`）；
(b) 机器侧全部登记（8 份 unified schema 的 `enum [-2,0]`、5 份的 `const 0`、
    `product_family_field_constraints.schema.json` 的 `const -4/4`、clause registry 默认表、
    `check_product_contract.py` 的派生式 `p = 2×sr 指数` 与其 `:642-645` 反漂移断言）；
(c) 实现侧消费者（生产写出侧与 `v6_provenance.cpp:167` 站 `{-2,0}`；`{-4,+4}` 侧只剩
    零调用函数 `v6_bunit.cpp:242` 与未接线的 `v6_drizzle_science.cpp:605`）。
三条各自自洽但两两互斥：**分类语义**有生产事实，**幂次语义**有二次律的算术必然
（`variance = signal²` ⇒ 立体角维指数必须翻倍，`{-2,0}` 装不下）。
两案代价：
- 案一（推荐）：**幂次面另起名**（如 `solid_angle_power`），`pixel_area_power` 保留分类语义
  ⇒ 改动集中在 clause registry / PFFC / CI 表 / 一个零调用函数与一个未接线门 + 文档措辞，不动产品面、不动 enum、不动二次律；
- 案二：enum 扩到 `{-4,-2,0,+4}` ⇒ 要同步 8 份 unified schema、全部 example、5 份 `const 0` 的死规则、
  `v6_provenance.cpp` 的 SB⇔−2 断言与生产写出侧（`phase2_integrate.cpp:425/538`）——等于改产品合同，代价最高。
我不替他选：这条同时踩在"像素面积幂次"与"drizzle 折算"两项在册裁决的交界上。

**Q2（新，只在负责人主张 B 时才需要）**：裸形态是否要承认"外部手放的 `<name>.hips/` 目录（旁无索引）"为合法产品？
- 答"否" ⇒ 不需裁决，我按 W5 的 A 方案定案（合同 N7/R5 订正 + bare 分支 fail-closed + `N19`）；
- 答"是" ⇒ 需批四件事（`frame_storage.index_path` 可 null + 新不变式 F5、F4 加前缀、
  验收 `:114/122` 放宽、`lib/**` 实现目录枚举重建 + provenance 降级记录并配非恒真判据），
  且 W5 升级为 P0（新增实现债 + 新降级面）。

## 推翻或降级的条目（留痕不删）

| 条目 | 出处 | 我的处置 | 依据 |
|---|---|---|---|
| A-2 定性"说明文档 vs 机器 canonical schema，schema 为事实源 ⇒ 按 schema 订正文档" | `AUD-101-DB-06-07.md:99-101,141-143` | **降级为部分成立**（互斥为真，但 `-4/+4` 同为机器事实源，照此处置会把 `CHK-PRODUCT-CONTRACT` 的反漂移断言直接改红） | `v6_clause_registry_v1.json:2902-2909`、`product_family_field_constraints.schema.json:253-257`、`check_product_contract.py:77-81/642-645` |
| A-3"5 份 unified schema 无 `pixel_area_power` 键 / 门不在被点名文件里" | 同文 `:104` | **推翻表述**：5 份都有该键且 `const 0`，其 if-then（要求 −2）因此永不可满足 ⇒ 文档指向的是**死规则**而非"别处有门" | `provenance.schema.json:77-93 + 118-166`、`coverage:90`、`rejection:90`、`support:90`、`validity:91` |
| W3 性质"数值快照漂移" | `AUD-101-DB-06-07.md:35`（"§2/§9 的数值快照使局部内容越位到登记面"） | **上调定性为"越位成第二套登记面"**：它连 CI 注册状态与检查器清单都各存一份且均已失真 | `checks.json:2762` 有 `UT-CONFIG`（文档说无）、`CHECKS` 实有 11 项（文档列 10） |
| W3 未含的三项事实 | — | **新增**（非推翻）：`UT-CONFIG` 必红的逐条复算、其 `changed_paths` 无 `docs/**` 的根因、`module_dll_contract.schema.json` 非法 JSON | W3 节表 1-4 与 `w3c_out.txt` |
| W5 定性"三方冲突（三份上位 vs 本合同）" | `AUD-101-DB-08.md:68-72` | **改判**：五处口径、且最硬的一处是合同**自身内部**互斥 ⇒ 排除"以上位为准"的机械处置；成稿给的"或上位统一下调口径"一路**代价未标价** | §10.2 F1/F3/F4 与 N7/R5 同行相反；`check_frame_storage` ⇔ `validate_product` |
| W4 处置方案 `must_point_to`（任一锚命中即合格） | `AUD-101-DB-08.md:278-286` | **推翻方案、保留结论**：丢取值精确性（`N18` 从能红退为恒绿）、不拒代码块引用蒙混、新负例弱于旧负例 | 我测得的 code 内含词事实（design 8 次 / contract 12 次）+ 两条问答 |
| "反重复类判据机器不可判（A.4 末段，另议不建议进门）" | 同上 | **推翻**：围栏切分 + 同行词表 token 计数确定可判，已实现并出数（9 层共 51 条复述行） | `复算\contracts-v\w4_layer_tokens.py` |
| A-1 标题句"承载现行硬约束的裁决正文在仓内无承载文件" | `AUD-101-DB-06-07.md:97` | **降级 P2**（悬空成立，约束有上位宿主；控制包出库是规范要求的正常终态，非缺陷） | `CONTROL_PACK_SPEC.md:209`、`ASTROCS_DESIGN.md:225/227/229`、`git log --diff-filter=D -- "*GAP_AUDIT.md"` |

## 收工基线核对

收工 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 与开工**逐字一致**（见文首），
差异：无。HEAD 仍为 `c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`，branch `main`，零 git 写、零仓库文件改动；
本轮只新建 `独立审计/复算件/contracts-v\` 下 4 个脚本与 4 份输出、
以及本成稿一个文件，均在审查工作区、不入仓库。
