# AUD-402 判读 · 队列 A3（配置与合同面收尾）

成稿节点：独立只读审查节点。被审基线 HEAD=`c8f64e9a`。
本队列判读对象：`独立审计/批次清单/P402-A-07.csv`…`P402-A-11.csv` 共 **124 行**（全部落表 `AUD-402-判读-A3.csv`）。

## 0 基线自证与纪律遵守

开工 `git status --porcelain` 与收工 `git status --porcelain` 两次输出**逐字相同**，各两行：

```text
?? "ACSD\346\225\264\346\262\273\345\267\245\344\275\234\345\214\205_AUDIT-06.zip"
?? site/
```

未执行构建 / cmake / ctest / run_checks.py / 任何 `eng/**` 脚本 / 端到端；未 `import` 仓库内 Python；
未做任何 git 写操作，未改 git 配置。工作脚本只落 `独立审计/复算件/q402-A3/make_verdicts.py`。
取证一律 `git grep` / `git show HEAD:<path>` / `git ls-files` / `git log --diff-filter=D`（`rg` 本机不可用）。

## 1 判读口径（两列的取值域，供前台合并时对齐）

- `来源现状` 用标准 02 §4 的证据分级四档：**A 一手论文 / A 成熟开源 / B 本仓可复现实验 / C 二手**；
  四档都够不着写 `无出处`（含"引用指针指向跟踪集里不存在也不曾被跟踪的文档"）；
  非科学量写 `不适用（结构性常数）`。
- `处置` 用标准 02 §2 的四选一：`① 登记出处` / `② 可由输入几何或物理关系导出` / `③ 需实验标定` /
  `④ 待确认`；结构性常数写 `不适用（结构性常数）`。
- 本队列 **124 行无一属 A/B 档**：全部落在 `lib/infrastructure/cli/` 的观测面与配置模板面，
  不涉及天文物理量；有实质出处的只有 9 行（见 §3 的 ①/② 类）。

分布（确定命令见 §6）：`不适用（结构性常数）` 97 行、`④ 待确认` 18 行、`①/② 有出处` 9 行。

## 2 立案清单

### 2.1 多侧默认不一致

**L1（本队列最高危）`drizzle.pixfrac` 六侧取值不一致，且生产代码兜底站在错误一侧**

| # | 侧 | 位置:行 | 值 | 状态 |
|---|---|---|---|---|
| ① | 声明的唯一数值源 | `eng/packaging/config/defaults.json:666-667` | **0.8** | authority_status=owner_adjudicated |
| ② | 出厂配置模板 | `eng/packaging/config/templates/normalize.phase_config.json:11,22` | 0.8 | 与①同 |
| ③ | 插件文档默认列 | `docs/plugins/algorithms_phase1/08_drizzle.md:49` | 0.8 | 并写明"数值默认唯一来源=defaults.json" |
| ④ | **CLI `--template` 骨架** | `lib/infrastructure/cli/session_commands.h:181`（本队列 A-10 行 3） | **1.0** | 与①②③矛盾 |
| ⑤ | **生产代码兜底** | `lib/infrastructure/scheduler/src/module_adapters.cpp:7506` `p1_num(dj,"pixfrac",1.0)` | **1.0** | 用户省略 `drizzle.pixfrac` 时的实取值 |
| ⑥ | 编排器代码兜底/模板 | `lib/infrastructure/pipeline/orchestrator/cpp/src/json_config.cpp:713`、`cpp/include/json_config.h:67`、`configs/stage1.template.json:41` | 0.8 | 自称"权威默认，与 schema/template 一致" |
| ⑦ | 登记册（陈旧） | `eng/packaging/config/config_registry.json:725,729` `declared_default:"1.0"`，note 称"默认 1.0 已入 defaults.json（owner_adjudicated）" | 1.0 | **它所指向的文件现在写 0.8** ⇒ 台账与文件互相打脸 |
| ⑧ | schema | `eng/contracts/schemas/phase_config_normalize.schema.json:142-146` | 无 default 关键字 | 只给值域 0<x<=1 |

- 触发路径（可复算，不需跑程序）：`parser.cpp:291` 把 `drizzle` 列入 Phase1 白名单键 →
  配置块原样透传给 `astrocs.phase1.drizzle` 节点（`runtime_client.cpp:217`）→
  `module_adapters.cpp:7506` 读键，缺键取 **1.0** → 直接把该值传进
  `hp_drizzle_run_phase1_hips`（`module_adapters.cpp:8283`）并写 provenance（:8311）。
  即"缺省即 1.0"是生产实取路径，不是假想。
- 科学后果由①自己的 note 写明：`取 1.0 时相邻 drop 仅边界相接，源像素落点错位处出现无覆盖缝隙`；
  而 kcorr 系数表按 `pixfrac=0.8 生产默认` 做 MC 标定（`lib/algorithms/coverage/src/sampler.cpp:81-88`，
  表列只有 {0.5,0.8,1.0}）⇒ 1.0 一侧同时偏离唯一数值源与标定前提。
- **同一函数内的纪律自相矛盾（判 fail-open 的关键证据）**：紧邻的 14 行下面，
  `precision_mode` 缺键即 `ErrorDomain::DATA` 拒绝并写"no silent default"
  （`module_adapters.cpp:7509-7515`），`parser.cpp:289-290` 亦声明 drizzle 参数"非 silent default，
  缺失即节点 DATA 拒绝"。pixfrac 恰是唯一被静默补值的 drizzle 参数。
- 修法判据：④⑤改 0.8（或由契约生成）、⑦重锚到当前 defaults.json；
  并补一条机器门：`config_registry.json` 的 `declared_default` 必须逐字等于它所指向的 defaults 值。
  现有 CFG 系列门不比对 `--template` 骨架与出厂模板（`eng/tests/cli/test_unified_blocks_templates.py`
  只把 `--template` 输出对自己的 schema 跑偏差集门），故该分歧今天不可见。

**L7 资源采样周期四侧，且相差 5×**（本队列 A-09 行 30）

| 侧 | 位置 | 值 |
|---|---|---|
| C++ 头默认参数 | `lib/infrastructure/cli/resource_recorder.h:97` | 0.25（**生产零调用点**，`git grep ResourceRecorder` 仅此 1 处默认 + 2 处测试） |
| C++ 生产实取 | `lib/infrastructure/cli/commands.cpp:720`（另 :714 ProcessMonitor(0.5)、:758 period 0.5、:979 `q_low_run += 0.5`） | 0.5 |
| 外挂 judge | `eng/tools/quality/resource_monitor.py:870 --interval` 默认 | 1.0 |
| 冻结门 | `eng/tools/monitoring/run_monitored.py:917 --poll-interval` 默认 | 0.2 |

三面在 `docs/plugins/infrastructure/21_observability.md §8.6` 被规定为同一判据的等价实现，
但样本周期决定判据⑥（>=70% 样本 U>=0.85）的样本数与判据②（连续 >=10 s 窗）的量化步长；
无一处登记该值（契约、§8、defaults.json、CLI 骨架都没有这个键）。另外 0.5 在 C++ 侧被写成四处字面量，
改周期即令 :979 的窗长累加与实际周期脱钩（该站点属四链路代理域，此处交叉登记）。

**（次级）`io_wait_high_percent=50.0` 两侧同值皆无源**：C++ 结构体默认 `resource_gate.h:207`（不可配置、
且输入未接线）↔ 外挂 judge CLI 默认 `resource_monitor.py:895 --max-io-wait-percent 50.0`（可配置）。
同值不等于同源：契约里没有该键，所以任何一侧单独改动都无门可拦。

### 2.2 该导出却硬编码 / 唯一数值源被绕过

契约 `eng/contracts/resource_gate_v1.json` 的 authority_note 逐字规定：
"本文件是该权威下的**唯一数值源**，实现侧不得再出现字面量阈值"；`resource_gate.h:105` 亦自述
"本文件不再出现字面量阈值：改数值只改契约"。实测违反：

| 立案 | 值 | 位置 | 是否契约/§8.3 判据 |
|---|---|---|---|
| CL-5S | 5.0 s ×3 | `resource_events.h:84`（默认参数）、`resource_gate.h:336`、`resource_gate.h:397` | 否（"无标注 stage 且 wall>5s ⇒ P1"） |
| CL-5S | 5.0 s | `resource_gate.h:363`（wall<5s → NotApplicable） | 否——契约 applicability 只规定"严格 >10 s"，并明记"C++ 侧原有 wall<5s 静默豁免已废止" |
| CL-ALLOWSLOW | 20.0 / 5.0 / 15.0 | `resource_gate.h:368-369` | 否（"CPU/io/mem 皆低"） |
| L2 | 50.0 | `resource_gate.h:207` | 否 |
| L15 | 2（活跃线程下限） | `resource_gate.h:342,345` 写字面量 `< 2` / `>= 2` | 是——`compute.min_active_compute_threads=2` 已由生成的 `kWorkerP50Min` 提供（:344 正确用它，回退分支漏用） |

`0.85` 被抄进面向用户的诊断字符串（`resource_gate.h:492`），属同一收敛失败：契约改值即产生假文案，
应写 `kCpuMeanMinPercent/100`。

### 2.3 阈值抄成观测值 / 陈旧值冒充冻结值

**L4 注释与诊断文本里残留已废止的 D.6 阈值 75%/50%，与契约的 85%/0.70/60% 同时并存**

- `resource_gate.h:125` 自述："旧 D.6 的 75%/50% 已在契约中废止，不得回归"；
- 但 `:211` 注释写 "U>=0.75 样本占比"、`:212` 注释写 "U<0.50"、`:529` 诊断字符串写 "U>=0.75 样本占比"；
- 契约现值：`per_sample_utilization_min_percent=85`、`per_sample_pass_fraction_min=0.7`、
  `queue_low_utilization_percent=60`。⇒ 同一文件同一判据两套数（文本一侧是旧观测档位的残影）。
- 同类：`resource_recorder.h:185` 注释仍写 "(>=70% 样本 U>=0.75)"（该行不在本队列，交叉登记）。

**L16 契约 enforcement_note 里的"实测 65.09%"是观测值入规范**：
`resource_gate_v1.json` compute.enforcement_note 用一次实测读数（16-worker 真负载 65.09%）
作为"85% 均值门不得硬失败"的理由。数值后果是 record_and_justify 分档（可接受），
但该读数无登记复现路径（哪个 run、哪台机、什么 seed），属本项 §4.3 点名的"阈值抄观测值"形态；
`run_monitored.py:728` 又把它复写进代码注释。建议：把该读数移到证据件/UNRESOLVED，
契约里只留"未标定⇒record_and_justify"的理由式表述。（此两行不在本队列 CSV 内，作为本队列证据链一并登记。）

### 2.4 无量纲 / 无单位 / 口径依据缺失

- **L10 除零回退伪装成 0% 利用率**：`resource_gate.h:293-296` 在 `allocated_capacity_cores==0` 时
  返回 0.0。契约 `denominator.zero_denominator_effect` 规定"利用率类判据不成立 → 记入 recorded
  （allocated_capacity_undeclared），不参与裁决"，而 §8.2 第③行也规定分母 0 时判据不成立。
  实现返回 0.0 使 `cpu_p50_percent=0.0` 通过 `>=0.0` 的"已采样"闸门 ⇒ 参与裁决并判红（与契约方向相反）。
  应返回 `kMon001NotSampled`。
- **L11 CPU 阈值跨刻度**：`resource_gate.h:181 cpu_percent` 的单位是 `percent_of_one_core`（100=1 核），
  而 `:106-108` 明令阈值侧比较必须用"已分配容量百分比"。`:368` 的 `cpu_percent < 20.0` 直接比前者 ⇒
  16 核机上 0.2 核即"过线"，"皆低"判据在真实并行下几乎不可触发。同族问题：`resource_recorder.h:369`
  的 worker_balance `utilization_pct` 与 §8.2 定义的"利用率"同名不同义（L12）。
- **L13 键名主张 MB/s、契约禁 MB/s**：`v6_runtime_contract.h:310 rss_growth_mb_per_s` 与
  `:329 required_metric_keys()` 把 `_mb_per_s` 作为对外冻结字段名发布，而契约 memory.unit
  逐字规定"MiB/s（1048576 B/s）；禁止再使用 MB/s（1e6 B/s，口径差 4.858%）"。
  本头不换算 ⇒ 真实单位取决于填充方，键名却已把口径写死。
- **L8 分位数估计器无登记**：`resource_recorder.h:220-227` 用 `idx=round(q*(N-1))` 的最近秩变体，
  空集返回 0.0（"无样本"与"p50=0"不可分）；契约要求 `min_active_compute_threads_fallback_statistic="peak"`
  且回落域为"有效样本 <2"，C++ 侧无该回落实现点（`:343` 的回落是"未采样→用配置值"，域与值两头都不对）。
  统计量定义未登记 ⇒ 同一批样本两实现可给不同分位（判据①⑤的可复核性受影响）。
- **L9 epsilon 无来源**：`resource_recorder.h:265` 的 `dt=max(0.001, Δt)`（RSS 斜率分母下限 1 ms，
  最多把斜率放大 1000×）与 `resource_monitor.py:765` 的 `+5.0` 宽限（非本队列）都属
  标准 02 §3 点名"epsilon 之类要给物理或数值来源"而未给。
- **L17 两个斜率估计器并存**：`resource_recorder.h:266-268` 用首末样本差算 `rss_slope_bytes_per_s`，
  而 `resource_gate.h:448` 明令 alloc 面"整条曲线稳健斜率+回落验证；禁止只看峰值"。
  CLI 把前者的输出送进 `g.rss_slope_mb_per_s`（`commands.cpp:936`）参与 MemoryGrowth 判定 ⇒
  同一判据族两侧用不同估计器（本队列只登记数值面，链路判定交四链路代理）。

### 2.5 权威指针悬空（不是数值问题，但直接决定"出处"这一列能不能填）

- **L5 资源面的"07 文档"不存在**：`lib/infrastructure/cli/` 有 **27 处**（分布在 5 个文件）注称判据来自
  "07 §1/§2/§3/§4/§5"（`resource_gate.h:1,8,15,48,53,174,177,310,328,335,404`、
  `resource_events.h:8`、`commands.cpp:624,726,786,916`、`monitor.h:2,333,394`、`memory_growth.h:1,14,20`）。
  自证：`git ls-files docs | grep "/07[_-]"` 只命中 `docs/plugins/algorithms_phase1/07_noise_snr.md`（SNR 面）；
  `git log --diff-filter=D --name-only -- docs` 里含 07 的路径只有
  `docs/contracts/v6/data/07_provenance.md` 与一批 `2026-07-*` 日期命名的计划文件；
  `git grep -n "MON-003\|拆份\|皆低\|unannotated" -- docs` 全 0 命中；
  现存的 `docs/architecture/observability/RESOURCE_MONITORING_CONTRACT.md`（LOG-002）是 CSV 指纹链合同，
  §1-§10 无任何 5s/20%/15%/50% 或 compute/memory/io/mixed 分类判据。
  ⇒ 该判据族**没有任何跟踪中的文档出处**，也不在 §8 判据表与契约里。
- **L5b 已废止宪章仍被当阈值出处引用**：`docs/DOCUMENT_INDEX.yaml:1065` 记
  "原引「冻结宪章」已废止"；`resource_gate.h:109-110` 亦自述"旧注释引用的「宪章 §10.5/§18.2」已废止
  （旧宪章文件已删）"。但 `resource_gate.h:63,138,191,198,298` 与
  `v6_runtime_contract.h:4,17,22,250,291` 仍用宪章条款号作出处。
  最直白的打架在 `v6_runtime_contract.h:22`："数值阈值不在此发明：门限常量权威在
  `lib/infrastructure/cli/resource_gate.h`（§18.2 冻结 85%/60%）" —— 而 `resource_gate.h:103-105`
  说权威在契约 JSON、§18.2 已废止。两个都自称单一事实源的头文件对"阈值住在哪"给出相反答案。
- **L18 §8 判据表与代码的判据集合不闭合**：§8.3 只有 ①-⑦ 七项；`evaluate_gate` 额外能返回
  `io_wait_high / compute_io_mem_all_low / unannotated_priority / mixed_unsplit /
  memory_bandwidth_low / io_missing_evidence / fast_fail_first_10s / global_lock_degradation`
  八类诊断，全部既不在 §8.3 也不在契约（其中 4 类见 §2.6 恒不触发）。

### 2.6 默认字面量冒充测量值（输入未接线 ⇒ 判据恒真/恒不触发）

生产 `run_with_resource_gate` 的 GateConfig 填充点全部枚举于
`commands.cpp:906-1005`（`g.kind/available_cpus/selected_workers/granted_workers/max_active_threads/
avg_equivalent_cores/wall_seconds/cpu_percent/has_stage_annotation/workers_p50/
active_window_seconds/rss_slope_*/work_core_seconds/monitor_present/util_*/queue_low_run_seconds/
alloc_*`）。**下列本队列字段无任何填充点**，于是其初值就是"生产实取值"：

| 字段:行 | 初值 | 后果 |
|---|---|---|
| `resource_gate.h:182 iowait_percent` | 0.0 | `:326` IoWaitHigh 恒不触发；`:368` 的 `iowait<5` 腿恒真 |
| `resource_gate.h:183 mem_bandwidth_percent` | -1.0 | `:369` 的 `( <0 || <15 )` 腿恒真 ⇒ "皆低"退化为 `cpu<20` 单腿 |
| `:175/:176 one_worker_ns / n_worker_ns` | -1.0 | GlobalLockDegradation 不可达 |
| `:164/:165 achieved/required_memory_bandwidth_frac` | -1.0 / 0.0 | kind 恒 Compute（`commands.cpp:906`）⇒ Memory 分支不可达；即便可达，required 恒 0 使任何实测值都过 |
| `:169 io_await_ms`（连同 io_bytes/io_ops） | 0.0 | Io 分支不可达；且 `<=0` 被当作"无证据"，把"测得 0 等待"与"未测"合一 |
| `:213 heavy_wall_seconds_total` | -1.0 | "<5s 切片规避"的累计墙钟只呈文字面量，判定不咨询 |
| `:206 progress_stalled` | false | ProgressStall 无生产注入点 |

⇒ 这类字面量的正确判读是"结构性占位"，但它们**在事件与诊断里以测量值的面目出现**，
所以按 §3 判据（五件事之"有效有限域"）必须登记：默认值≠未测，需要显式哨兵或字段缺席表达。
（判据本身该不该存在属 §2.5 的悬空权威问题。）

## 3 待确认与影响范围（UNRESOLVED 候选，18 行）

| 位置:行 | 值 | 待确认内容 | 影响范围 |
|---|---|---|---|
| `resource_events.h:84` | 5.0 | 判据是否保留；保留则入契约 | CLI 生产可达（`commands.cpp:675`）；只记录不改退出码 |
| `resource_gate.h:336` / `:397` | 5.0 | 同上（三处重复） | 同上 |
| `resource_gate.h:363` | 5.0 | 契约外的第二判定域下限 | 直调/单元路径；生产路径被 `:358` 的 10 s 域界先拦 |
| `resource_gate.h:368` / `:369` | 20.0 / 5.0 / 15.0 | 三个值与单位口径；跨刻度问题（L11） | Compute 分支记录面 |
| `resource_gate.h:207` | 50.0 | 判据存废 + 与 judge 侧 50.0 的唯一同源 | 记录面（当前恒不触发） |
| `resource_gate.h:529` | 0.75（串内） | 陈旧数须改 0.85 | 用户可见诊断文本正确性 |
| `resource_recorder.h:221` | 0.0 | 空集哨兵语义 | 判据①⑤可复核性 |
| `resource_recorder.h:225` | 0.5（估计器） | 分位数估计器定义登记 | 判据①⑤⑥与 p95 报告 |
| `resource_recorder.h:265` | 0.001 | dt 下限来源 | `resource_summary.json` 的 rss_slope；L17 双估计器 |
| `resource_recorder.h:97` | 0.25 | 采样周期唯一源（L7） | 全部逐样本/滑窗判据 |
| `runtime_client.cpp:118` | "1.0.0" | Pipeline IR 版本串无合同约束点 | 版本治理面（scheduler 只校非空） |
| `session_commands.h:254` | 0.0 | 模板示例值与合法值（ICRS 0,0）不可分 | 用户照抄即提交 |
| `session_commands.h:258` | 0.001 | 示例尺度无推导式（两侧同值，不分歧） | 同上 |
| `v6_runtime_contract.h:310` | 0.0 | 字段名 MB/s vs 契约 MiB/s（L13） | 对外冻结字段名 |
| `v6_runtime_contract.h:321` / `:347` | 0.0 | active 窗"未提供"的两载体两编码（L14） | 完备性判定与判定域前置 |

有出处（①/②）的 9 行：`session_commands.h:181`（①，须改 0.8）、`:269`（①，14_projection.md:38）、
`:272`（②，crpix=(1+W)/2 已按式给值）、`resource_gate.h:492`（②，kCpuMeanMinPercent/100）、
`resource_recorder.h:259`/`:263`（②，契约统计量名 p50）、`:369`（①，但须改名消歧 L12）、
`resource_gate.h:295`（①，现值与契约相反，须回哨兵）、`v6_runtime_contract.h:318`（①，口径须写明）。

## 4 覆盖率自报

- 队列总数 **124 行**（`P402-A-07/08/09/10` 各 30 行 + `P402-A-11` 4 行）；
- 已判 **124 行**，`未判` 0 行；输出 12 列全部齐，每记录 12 字段（`csv.reader` 校验 bad cols = 0）；
- 未做的队列文件与行数：**无**；
- 同簇合并的**机械计数**（按输出 CSV 的 `出处` 串分组，命令见 §6）：
  6 组共用一条出处，覆盖 **79 行** —— 观测字段零初值 46、哨兵/零值守卫 12、fprintf 精度串 11、
  单位换算与除零守卫 4、悬空"07 §"锚 3、"§3.5"文案 3；另有 5 行（`resource_gate.h:84/336/363/368/369`
  与 `:397` 一族）语义同属"契约外阈值"簇，为使逐行判据可读，各自写了加长锚而未合并成同一串。
  合并只在语义确实同源处做（如 11 行精度串同属一次 fprintf 调用的格式表）。
- 队列里 3 行 `备注` 自带"判读交 AUD-205 与四链路代理"（`resource_gate.h:121`、
  `resource_recorder.h:29`、以及 A-10 的机械侧声明），本队列仍给了独立判读，
  与 AUD-205 的口径若冲突以负责人裁决为准。

## 5 我证伪了队列输入里的哪个机械结论

1. **机械层报为"行内浮点字面量"的 19 行其实是注释/文案/字符串里的条款号、值域文本或标识串**，
   不是数值常数：`9.68`（A10#2/#4/#13 → §9.68）、`4.2`（A10#5 → §4.2）、`3.3`（A10#12 → §3.3:256）、
   `3.5`（A10#14-16 → §3.5）、`3.1`（A10#17 → "ASTROCS_DESIGN.md 3.1"）、`-004.1`（A10#18 → 控制项 C-004.1）、
   `10.4`（A10#19 → 已废止宪章 §10.4）、`18.2`（A07#15 → §18.2）、`§10.5`+`0.85`（A08#14 → 枚举注释）、
   `.20000`（A10#7/#8 → 文案 "(1..20000)"，实值 1024）、`.1`（A09#15 recorder:64、A10#28 v6:318 → 注释 "0..1"）、
   `.0;1.0`（A10#1 → 字符串 "1.0.0"）、`1`（A08#28 → 特性宏存在性哨兵）。
   计数口径：其中 18 行在输出 CSV 里逐行写了"作废"字样，`resource_gate.h:191` 写的是"非字面量"⇒ 19。
2. **两行的"现行值"与代码实际初值不符**：`resource_gate.h:211/212` 报 `0.75`/`0.50`，
   实际初值是 `kMon001NotSampled`（契约哨兵），报的数是注释里的废止档位；
   `session_commands.h:256/257` 报 `.20000` 而实值是 1024（该两点已计入上条）。
3. **`resource_recorder.h:29 ASTROCS_RES_HAVE_CFITSIO_LOCK_STATS = 1` 判为"命名常数（纯数值初始化）"错误**：
   它是 `#if defined()` 使用的特性宏存在性哨兵，值不参与任何算术，效果只决定 `lock_wait_ns` 列是否填实测。
4. **`resource_recorder.h:97 interval_seconds=0.25` 判为"生产代码默认"错误**：0.25 一侧在生产零调用点，
   生产实取 0.5（`commands.cpp:720`）⇒ 机械层把"未使用的默认参数"当成了生产值（真缺陷在 L7）。
5. **旁表把 12 行 printf 精度串（`.2f/.3f`）当常数登记**：它们是输出格式，判"无量纲依据"不成立；
   我逐行核了它们对下游判据的分辨率是否够（cpu_pct 0.01 核 ≪ 85% 判据、时间 1 ms ≪ 10 s 域界）⇒ 充分。
6. **"资源判据会影响退出码"这一常见读法被代码路径证否**（旁表 46 行的适用域前提）：
   `gate_enforcement()` 恒 `RecordOnly`（`resource_gate.h:268-270`），契约
   `enforcement.in_process_hard_fail="retired"` ⇒ 本队列所有阈值只影响记录面与外挂裁决面，
   exit 10 只属磁盘门。这不减轻 L1（pixfrac 是真科学路径），但给 L2-L18 定级降一档。

## 6 确定命令（计数可复算）

```bash
# 队列规模（本队列 5 份）
for f in 07 08 09 10 11; do wc -l "独立审计/批次清单/P402-A-$f.csv"; done   # 31/31/31/31/5 → 30×4+4=124
# 判读装配（键 = 路径:行；缺判读行即非零退出并逐条打印）
python "独立审计/复算件/q402-A3/make_verdicts.py"     # rows total=124 judged=124 missing=0
# 输出结构与分布
python - <<'PY'   # records 124 / bad cols 0 / 处置分布 97 不适用·18 ④·9 ①②
import csv,io,collections
rows=list(csv.reader(io.open(r"独立审计/证据/AUD-402-判读-A3.csv",encoding="utf-8-sig",newline="")))
print(len(rows)-1, sum(1 for r in rows if len(r)!=12))
print(collections.Counter(r[9][:2] for r in rows[1:]))
PY
# 悬空权威自证（判"文档不存在"用跟踪集＋删除历史）
git ls-files docs | grep -E "/07[_-]"                                  # 只命中 07_noise_snr.md
git log --diff-filter=D --name-only --pretty=format: -- docs | grep -i "07_"   # 只命中 07_provenance.md
git grep -c "07 §" -- lib                                              # 20+ 处引用（正例自证 grep 有效）
git grep -n "MON-003\|拆份\|皆低" -- docs                              # 0 命中（同一命令在 lib 上可命中）
# L1 六侧
git grep -n "pixfrac" -- eng/packaging/config lib/infrastructure/scheduler/src/module_adapters.cpp \
    lib/infrastructure/cli/session_commands.h lib/infrastructure/pipeline/orchestrator \
    docs/plugins/algorithms_phase1/08_drizzle.md
# L7 四侧
git grep -n "ResourceRecorder\|interval" -- lib/infrastructure/cli eng/tools/quality/resource_monitor.py \
    eng/tools/monitoring/run_monitored.py
```

<!-- PROGRESS: 124/124 -->
