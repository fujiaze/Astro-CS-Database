# 代码通读成稿 · 批次 CR-48

- 基线 HEAD：`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 派单：`DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 全条按硬约束执行）
- 开工 git status --porcelain 原文：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? drz.html
?? photom.rst
?? site/
```

  说明：派单列明的基线三项为 zip／`site/`／`photom.rst`。开工即见到**第四项** `drz.html`，本节点未创建、未修改、未删除任何仓库文件；该项按 §1 的"出现新的第四项"处理，见文末"派单与清单差异"节。

## 独占清单（6 项）

| # | 文件 | 已读 |
|---|---|---|
| 1 | `lib/algorithms/coverage/src/sampler.cpp` | 是（表 62 项） |
| 2 | `lib/algorithms/coverage/src/stage2_common.cpp` | 是（表 44 项） |
| 3 | `lib/algorithms/coverage/tests/rejection_nonfinite_weights_test.cpp` | 是（表 19 项） |
| 4 | `lib/algorithms/coverage/tests/retired_object_reject_negative_test.cpp` | 是（表 8 项＋判据串附表 6 项） |
| 5 | `lib/algorithms/coverage/tests/routing_test.cpp` | 是（表 18 项） |
| 6 | `lib/algorithms/coverage/tests/sampler_parallel_consistency_test.cpp` | 是（表 22 项） |

## 1. `lib/algorithms/coverage/src/sampler.cpp` —— 已读（1503 行全文）

### 1.1 §2.5 数值处置表（62 行；同族合并处已列全部位点）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :76 | kTileWidth | 512 | HiPS tile 边长（所有 tile 寻址与缓冲的基） | 结构性不适用 | 写侧固定 `hips_tile_format/width=512`（aio_hips_writer.cpp:1837）；不是科学量，是产品几何定义 | — | CONFIRMED |
| :77 | kTileShift | 9 | log2(512)，nested 局部寻址位移 | 结构性不适用 | 位移量，随 :76 派生 | — | CONFIRMED |
| :78 | kSnrCatalogMax | 1<<16 = 65536 | 每帧 SNR catalogue 读取上限 | 待确认 | 文档仅登记值不推导（PHASE2_SAMPLER.md:107「SNR catalogue 上限 65536」） | 更小=更早截断；影响 catalog veto(:895)、星掩膜(:1150)、snr_ik(:902)。见 CR-48-04 | CONFIRMED |
| :406 | maxn（p2_frame_id 内） | 1<<20 | 同一 catalogue 在身份哈希里的读取上限 | 待确认 | 无任何文档规定；与 :78 同一数据两处不同值 | 与 :78 的 16× 差 ⇒ 身份覆盖科学忽略的行。见 CR-48-04 | CONFIRMED |
| :83 | kControlCorrDefault | 1.4 | 冻结 k_corr（Drizzle 像素协方差方差放大） | 待确认 | 名义出处=「实验标定」，但 `UPMW-005 control_median_mc_test` 为构建孤儿：PHASE2_SAMPLER.md:307-309 自陈「该测试未注册/不在任何 CMake/ctest/CI 面」＋eng/tests/unit/CMakeLists.txt:1540 同陈 ⇒ §2.5 第 3 档不成立 | 保守方向：独立 MC 在生产尺度给 k≈1（R-2_phase2权重与UPM语义.md:228），1.4 高估方差=权重偏低=保守。见 CR-48-02 | CONFIRMED |
| :84 | kPiHalf | 1.57079632679489661923 | π/2，Var(median) 渐近式的因子 | 公式导出 | Var(median) ≈ π σ²/(2N)（高斯独立样本）；PHASE2_SAMPLER.md:248「control_variance = k_corr × (π/2) × σ_bg² / N_retained # :875」、:345「UPMW-004 独立 MC 基线」；代码 :875 真按式算 | — | CONFIRMED |
| :95 | sc_grid[2] | 300.0 / 600.0 | 标定表的源像素角尺度两档（=查表适用域） | 待确认 | PHASE2_SAMPLER.md:318-321 复述该域；:327-334 实测生产尺度 0.8064″–0.9890″/px ⇒ 域与生产差 300× | 域外一律 1.4（保守侧）。见 CR-48-02 | CONFIRMED |
| :94 | pf_grid[3] | 0.5 / 0.8 / 1.0 | 标定表 pixfrac 三档 | 待确认 | 同上 :318-322；0.8 与生产默认 pixfrac 一致（裁 0.8），0.5/1.0 两端无标定件在构建面 | 域外 clamp 已禁（:103 仅对 pf 保留 clamp） | CONFIRMED |
| :96-99 | k[2][3] | 1.2112, 1.3925, 1.4980, 2.3958, 2.8971, 3.2035 | 六格 k_corr 实测值 | 待确认 | 唯一引用来源 `kcorr_matrix_test.cpp`（tracked-but-unbuilt，CMakeLists:1540）；仓内无跟踪结果位；独立简化 MC 给 1.2966/2.0173（R-2 :222-223）与表值不等 | 生产不可达（in_domain 恒假），故不改产物字节。见 CR-48-02 | CONFIRMED |
| :101-102 | 域判据 `scale>=300 && <=600` | 300/600 | 域外即返回冻结默认（禁 clamp） | 公式导出 | 条文即此规则本身（PHASE2_SAMPLER.md:321「域外一律保留 kcorr=0」）；代码与条文一致 | — | CONFIRMED |
| :103 | std::clamp(pixfrac, 0.5, 1.0) | 0.5 / 1.0 | 表列有界性保留 | 文献值 | 合同域 `pixfrac∈(0,1]`（DATA_SEMANTICS.md:1739、aio_hips.h:300 校验域）＋文档 :322「pixfrac 维仍 clamp [0.5,1.0]（表列有界性保留）」 | — | CONFIRMED |
| :110-113 | fx / w0 / r0 内插权重 | — | 分段线性内插系数 | 公式导出 | 内插式代码真按式算（:107-116 非均匀网格两段各自归一化）；但内插这一行为本身被 SC-ADJ-F05.4 禁止 → 计入 CR-48-02 | — | CONFIRMED |
| :122-123, :137, :140 | provenance 零初始化与值域 | 0.0 / `vf>0&&vf<=1` / `vs>0` | 键缺失即留 0（→ 回退链） | 文献值 | DATA_SEMANTICS.md:1739 键量纲与域（pixfrac (0,1]、scale (0,∞)）；写侧 aio_hips_writer.cpp:1869-1874 | 缺失路径静默。见 CR-48-07 | CONFIRMED |
| :126 | char buf[8192] | 8192 | properties 文本缓冲 | 结构性不适用 | 缓冲区尺寸；写侧 `content.reserve(kv.size()*48+16)`（writer.cpp:676-677），实测属性集约 30 键 ⇒ 远小于 8192 | — | PARTIAL（未逐字符量实测 properties 字节数） |
| :155 | `<< (2*leaf_shift)` | 2 | tile→leaf 的 ipix 位移倍数 | 结构性不适用 | HEALPix Nside 翻倍 ⇒ Npix×4 的位移量 | — | CONFIRMED |
| :202-210 | mid = n/2 等 | 2 | 秩统计量下标 | 结构性不适用 | 实现选择（nth_element 分界） | — | CONFIRMED |
| :255, :281 | 3.14159265358979323846 | π | 度→弧度 | 结构性不适用 | 单位换算定义常数 | — | CONFIRMED |
| :255, :281 | cos_guard floor | 1e-4 | 极点附近 RA 预筛窗保护 | 待确认 | 文档 :402 只复述公式 `cos_guard=max(cos(|dec|+r),1e-4)`，未给该 floor 的依据 | floor 越小⇒窗越宽⇒只多算不漏算（判据仍由 :262 精确距离把关），保守 | CONFIRMED |
| :260 | 180.0 / 360.0 | 180/360 | RA 环绕归一 | 结构性不适用 | 角度定义常数 | — | CONFIRMED |
| :304, :519 | control_grid_per_tile | 8 | 每 tile 8×8=64 个 control | 待确认 | 文档 :120 与 :126 仅列默认值，仓内无「为何 8」的推导/标定 | 更大⇒cell 更小⇒每 cell 样本更少⇒更易 min_samples 拒绝 | CONFIRMED |
| :305, :520 | patch_radius_leaf | 2 | control 附近 leaf 半径 | 待确认 | 文档 :120 仅列值 | 该值在现行 pass1 里未被消费（估窗实由 :308 background_patch_radius 决定）⇒ 口径可疑，核查项见文末「需要实跑/读不动」第 7 条 | CONFIRMED |
| :306, :521 | min_samples | 5 | 有效样本下限 | 待确认 | 文档 :53「域 [min_samples, (2·background_patch_radius+1)²]，默认 [5,289]」＋:274 给 N=5 处基线偏差 −8.5%（保守方向）＝该值处行为已实测，非推导 | 更小⇒退化 cell 更易通过，与 CR-48-01 地板分支相互作用 | CONFIRMED |
| :307, :522 | snr_search_radius_deg | 0.05 | snr_ik 的邻域检索半径（度） | 待确认 | 文档 :120 仅列值，无与 seeing/叶尺寸的关系推导 | 更大⇒邻域均值更平滑（snr_ik 偏保守） | CONFIRMED |
| :308, :503 | background_patch_radius | 8 | 17×17=289 估计窗 | 待确认 | 文档 :121 列值；:573 的 F3 判据把 N=289 与 N=17 两档分别给容差 ⇒ 该值进入判据分母但选择本身无推导 | 更大⇒σ_bg 含更多结构分量（偏大⇒权重偏小，保守），文档 :275/:290 承认「结构主导 patch 上 control_variance 偏大」 | CONFIRMED |
| :309, :504 | background_clip_sigma | 3.0 | 亮端单侧裁剪 σ 倍数 | 待确认 | 文档 :229-231 给「3σ ⇒ 截断点≈99.87 百分位，剪亮尾对 MAD 影响可忽略（实测高斯+8σ 污染…）」＝该值处效应已量化，非推导 | 更小⇒剪得更狠⇒σ_bg 偏小（权重偏大，反保守） | CONFIRMED |
| :310, :505 | background_clip_iters | 3 | 裁剪迭代上界 | 待确认 | 文档 :122/:201 仅述过程；:560（DISP-P2SMP-005）实测收敛门在 m0≈0 时形同虚设 ⇒ 该值即事实迭代上界 | 实现选择；影响收敛充分性（结果仍确定、单调） | CONFIRMED |
| :311, :506 | background_max_contamination | 0.20 | 污染分数上限 | 待确认 | 条文面零提及推导。检索式：`git grep -n "0\.20" -- docs/algorithms/PHASE2_SAMPLER.md`（仅 :122 列默认值一处）、`git grep -n "contamination" -- docs/science` | 更小⇒排异更多（偏红）；影响 accepted_observations 与 UPM 观测集 | CONFIRMED |
| :312, :509 | background_contamination_sigma | 3.0 | 污染计数的判定阈值（与 :309 同族同值，位点 :866） | 待确认 | 文档 :123 列值；与 :309 是两个独立配置键却默认同值，无条文说明二者必须同值 | 同 :309 | CONFIRMED |
| :313, :511 | background_min_retained_fraction | 0.60 | 裁剪后保留比例下限 | 待确认 | 条文面零提及推导。检索式：`git grep -n "0\.60\|min_retained" -- docs/algorithms docs/science` | 更大⇒排异更多（偏红） | CONFIRMED |
| :314, :512 | background_tolerance | 3.0 | Stage C 的 DBE-like 局部容差 σ 倍数 | 待确认 | 文档 :124 列值；无标定件 | 更大⇒局部亮端门更松（偏绿） | CONFIRMED |
| :315, :516 | background_neighbor_radius | 2 | 局部背景邻域 cell 半径 | 待确认 | 文档 :125 列值 | 更大⇒B_local 更平滑（门位置变化方向依结构而定） | CONFIRMED |
| :316 | background_catalog_veto | 1 | Stage E 开关 | 结构性不适用 | 布尔开关，非科学量 | — | CONFIRMED |
| :317, :518 | control_k_corr | 1.4 | 同 :83（合并登记） | 待确认 | 同 :83；**注意 :518 只挡 `<=0`，不挡 `0<k<=1`** ⇒ 见 CR-48-03 | k<1 ⇒ cvar 偏小 ⇒ civar 偏大 ⇒ 过度自信（偏绿） | CONFIRMED |
| :318, :513 | star_mask_snr_factor | 10.0 | 星点判定=帧 SNR 中位数的 10 倍 | 待确认 | 文档 :387/:559 只规定「必须由配置面承载」＋单位口径，未推导 10× | 更大⇒更少星被 veto/掩膜（偏绿） | CONFIRMED |
| :319, :514 | star_mask_radius_deg | 0.012 | 星点掩膜圆帽半径（≈43″） | 待确认 | 同上 :559，无与 PSF/seeing 的推导关系 | 更大⇒掩膜面积与 veto 命中更多（偏红） | CONFIRMED |
| :320, :927 | cpu_workers | 1 | 串行 reference 默认 | 结构性不适用 | 并行度默认；生产由 Runtime lease 覆盖（:924-927 注释＋CON-004） | — | CONFIRMED |
| :546 | leaf_shift（局部） | 9 | 与 kTileShift 同值的第二处字面量 | 结构性不适用 | 位移量；但两处 9 无编译期绑定，`kTileWidth` 改动不会连带此处 ⇒ 静默错位风险（非现行缺陷） | — | CONFIRMED |
| :669 | n_union 上界 | 1e6 | 越界即拒绝（防 cells 溢出） | 待确认 | :668 注释给「714×64=45696，32 帧候选约 1.4M」的规模说明，但 1e6 本身无出处 | 更大⇒允许更大 cells 树（内存风险）；更小⇒合法大 mosaic 被判错 | PARTIAL |
| :679, :1177 | cells 上界 | 200×1000×1000 | 单元数内存护栏 | 待确认 | 纯护栏，无文档值；:1177 那一处在 cells 已 resize 之后 ⇒ 实际不可达（resize 先抛） | 护栏值偏大：CellStat 含 15+ 个 vector（约 ≥500 B/单元），2e8×500 B≈1e11 B，护栏本身不足以防 OOM，靠 :684 的 bad_alloc 兜 | CONFIRMED |
| :694, :739 | `12 << (2*target_order)` | 12 | HEALPix Npix=12·Nside² | 公式导出 | 球面象素定义常数；代码真按式算 | — | CONFIRMED |
| :807 | vals.reserve(400) | 400 | 估计窗样本容量预留 | 结构性不适用 | (2·8+1)²=289 之上的容量预留（性能量，不参与判据） | — | CONFIRMED |
| :853 | 收敛相对阈值 | 1e-12（两处：`1e-12*max(|m0|,1e-12)`） | clipping 迭代停止 | 待确认 | 文档 :203 复述该式、:560（DISP-P2SMP-005）实测 m0≈0 时阈值≈1e-24 过严 | 影响终止精度不影晌正确性（单调收缩＋min_samples 兜底） | CONFIRMED |
| :864 | sigma 地板 | 1e-12 | 把 MAD=0 映射成有限正方差 | 待确认 | 文档 :245「1e-12 只是避免 σ_bg² 归零的数值保护量…**不是**最小可分辨方差、**不是**读出噪声下限」＋:290 同句 ⇒ 条文自身否定其科学含义 | **反保守**：floor 越小⇒cvar 越小⇒civar 越大⇒权重越过度自信。见 CR-48-01 | CONFIRMED |
| :873 | max(n_retained,1.0) | 1.0 | 防零除 | 结构性不适用 | 实现保护 | — | CONFIRMED |
| :875, :877 | cvar / civar | — | `k_corr·(π/2)·σ²/N_ret`、`1/cvar` | 公式导出 | 文档 :248（ALG-UPM-CONTROL-IVAR-001）；代码按式算，唯 :864 使式在退化输入下产出伪值（CR-48-01） | — | CONFIRMED |
| :879 | sup_sum/n_valid | — | support 均值 | 公式导出 | 算术平均定义（诊断量 `o.support`） | — | CONFIRMED |
| :893 | `cfg.star_mask_snr_factor * frame_snr_med` | — | Stage E veto 阈值 | 公式导出 | 文档 :387 同式；代码按式算，唯 `frame_snr_med` 取上中位（:657）与 `median_of` 口径不同 ⇒ CR-48-06 | — | CONFIRMED |
| :1025, :1034 | 邻域最小样本数 | 3 | Stage C 邻域不足即不 gate | 待确认 | 文档 :482 附近只述两级回退，未给 3 的依据 | `neigh<3` ⇒ 该观测不被局部门约束（保守保留＝偏绿一侧） | CONFIRMED |
| :1036, :1040 | `neigh[n/2]`、`dev[n/2]` | 2 | 局部背景 B 与 MAD 的秩取法 | 待确认 | 条文未规定偶数样本口径；与 :199 `median_of`（偶数取两中位均值）实现不同 ⇒ 同一文件两套"中位数" | 上中位 ⇒ B 略偏亮 ⇒ 局部门略偏松（偏绿） | CONFIRMED |
| :470, :844, :859, :1040 | MAD 一致性因子 | 1.482602218505602 | σ̂ = 1.4826×MAD | 文献值 | Rousseeuw & Croux, JASA 88(424) 1993, DOI 10.1080/01621459.1993.10476408；文档 :214-219 已核并给适用域与有限样本偏差实测（:274「N≥65 时相对偏差 <1%，N=5 时 −8.5%」）；四处同值但各写字面量（未共享常量） | — | CONFIRMED |
| :1041, :1045, :1049-1051 | 三道 gate 的式 | — | 局部容差门／污染门／保留率门 | 公式导出 | 文档 Stage C/D 段落式；代码逐条按式算（`>`/`<` 方向与条文一致） | — | CONFIRMED |
| :1068, :1091 | `nclean >= 2` | 2 | 同一 control ≥2 帧 clean 才出观测 | 结构性不适用 | 它不是科学量，而是相对光度方程可解性条件（:621 注释自陈「同一 control ≥2 帧 clean 观测才进入 UPM（相对光度约束）」） | — | CONFIRMED |
| :1087, :1107-1111 | 点 SNR 回退 `\|value\|/unc` | — | 无局部 catalogue SNR 时的点信噪比 | 待确认 | 文档 :460 段承认该口径（`P2_SKY_FLAG_NO_LOCAL_SNR`）但未给「背景估计的 \|m\|/σ 即点 SNR」的推导；unc 命中 :864 地板时该值≈1e13（CR-48-01 第二处污染） | 地板命中⇒snr 字段虚高（偏绿） | CONFIRMED |
| :1122-1131 | leaf ivar 覆盖判据 `v>0.0f` | — | 逐像素 ivar 产品优先于 control_ivar | 文献值 | 合同 DATA_ARTIFACTS.md:26「ivar≤0→rc=2」；代码与该域一致 | — | CONFIRMED |
| :1152-1153, :1162-1163 | 掩膜位置量化 | 1e4 / 1e-4（≈0.36″） | 跨帧去重的键步长 | 待确认 | 文档 :402 只述"跨帧去重"，未给步长依据 | 步长越小⇒去重越少（冗余条目增多，不改判据结果）；RA=0/360 接缝不合并 | CONFIRMED |
| :1188 | cs.frames 异常膨胀阈值 | 10000 | 超限即跳过该 cell 的 candidate 统计 | 待确认 | 无条文依据；跳过使 :1073-1074 声称的 `candidate = accepted + Σrejected` 恒等式在极端情形仍不成立 | 仅统计面；n_frames>10000 在生产不可达 | CONFIRMED |
| :434-439 | SHA 截断 `i<16`、`<<4` | 16 | 256→64 bit 帧身份截断 | 结构性不适用 | 位移/截断量，非科学量 | — | CONFIRMED |
| :1396-1401 | quantile `floor(p*(m-1))` | — | 分位数取法（最近下邻，不内插） | 待确认 | 条文面只点名"必须带 p05/p50/p95"（docs/design/PHASE1_DETAILED_DEIGN 同名条款：PHASE1_DETAILED_DESIGN.md:192），未规定分位数口径 | 下取⇒发布的 p95 偏低、p05 偏低⇒对外散布量偏乐观。见 CR-48-08 | CONFIRMED |
| :1405-1407 | p05/p50/p95 | 0.05/0.50/0.95 | 摘要分位点 | 待确认 | 同上（PHASE1_DETAILED_DESIGN.md:192 点名三档，值与条文一致） | — | CONFIRMED |
| :1416-1418 | max_systematic_deviation / model_error / sampling_coverage | — | 最大偏离 p50／对 p50 的 RMSE／有效节点占比 | 待确认 | 条文点名"必须有"但未定义计算式（PHASE1_DETAILED_DESIGN.md:192）；代码自择 RMSE(about p50) 与 max\|v−p50\| | `max\|v−p50\|` 把随机散布当"系统偏差"⇒对外偏差量偏大；model_error 同理 | CONFIRMED |
| :1440-1441, :1479-1485 | residual_trend_max / power_loss_max | 无默认（须声明） | 降级门阈值 | 结构性不适用 | SO-07 PENDING_OWNER_SIGNOFF（:1436-1437 自陈），未声明即 UNAVAILABLE ⇒ 代码不自造阈值 | — | CONFIRMED |
| :1457-1468 | `sampling_coverage∈[0,1]`、`model_error>=0` 等 | 0 / 1 | 入参值域校验 | 结构性不适用 | 比率/方差值域定义 | — | CONFIRMED |

### 1.2 findings

### CR-48-01 · 零尺度 patch 的 1e-12 地板被平方成伪 control_variance / 伪 control_ivar 并随 obs 发布，违反本仓已冻结的 §5.4 发布口径；下游 per-control 归一化把该观测权重推到 ≈1
- 轴：R（主）＋D＋C
- 位点：`sampler.cpp:864` `const double sigma = (s0 > 0.0) ? s0 : 1e-12;`；`:875` `const double cvar = kcorr_f * kPiHalf * sigma * sigma / n_ret;`；`:877` `cs.civar.push_back(cvar > 0.0 ? 1.0 / cvar : 0.0);`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md` §5.4「零尺度分支的发布口径（正向约束）」逐字：「`σ_bg_raw == 0` …时，control_variance 与 control_ivar **必须**标为「无尺度信息」：`control_ivar = 0`、`control_variance` 取非有限值（或等价地以 `quality_flags` 置位并计数），**发布面 = 无尺度信息标记**（1e-12 只作数值保护量，不生成有限正方差）」；同文 :244-245「1e-12 只是避免 `σ_bg²` 归零的数值保护量，其量纲随 σ_bg 标度而变，**不是**最小可分辨方差、**不是**读出噪声下限」；同文 :296-298「生产侧已具备「无尺度信息即拒」的接口；采样侧当前把地板值送进该接口，绕过了这一语义」。可达性为文档实测：:237「真实 M42 300 s R 帧 4096 个 17×17 patch 中 **2 个**命中（0.049%）…⇒ 该分支在真实数据上可达，不是理论角落」。同文 :562 已把本事实登记为 DISP-P2SMP-007（「禁用」语气的条文＋仍未修的实现并存）。
- 现状（引原文）：代码无「无尺度信息」分支——`s0<=0` 时 `sigma=1e-12`，照常进 `:875` 算 cvar、`:877` 算 civar，第三遍无条件写 `o.control_variance = cs.cvar[fi];`（:1115）与 `o.control_ivar = cs.civar[fi];`（:1116）；`o.quality_flags = cs.qual[fi];`（:1136）只承载 catalogue 位 OR，不含退化标记。
- 复算（本批独立）：N_ret=289、k_corr=1.4 ⇒ `cvar = 1.4×1.5707963267948966×(1e-12)²/289 = 7.609e-27`，`civar = 1/7.609e-27 = 1.314e26` —— 与该文档 :292-293 自陈的本仓实测值逐位吻合，证实现行代码走的确是被禁用分支。
- 下游不拦（方向钉死）：`upm.cpp:2471` `if (!(o.control_ivar > 0.0) || !std::isfinite(o.control_ivar)) { delete m; return 2; }` —— 1.314e26 有限且>0，通过；`upm.cpp:668-671` `if (s > 0.0 && std::isfinite(s)) raw_w[i] = raw_w[i] / s * m->controls[ck].reliability;` —— 同一 control 其它观测的 civar 量级按 `upm.cpp:658` 自陈为「control_ivar 中位 ≈5.6e-22」，故地板观测占权重比 = 1.314e26/(1.314e26+5.6e-22) → **1.0**（f64 下其它帧在该 control 的约束被完全吞掉）。方差被报成比任何 float32 可表示数据所能支持的精度还小 32 个数量级 ⇒ 判据方向是**过度自信（偏绿）**，不是偏红。
- 后果：命中地板的 (frame, control) 独占该 control 的相对光度权重，C 场退化为"单帧单 patch 中位数"（该 patch ≥半数像素同值，即量化平台/填充主导），其它帧在同方向的观测被清零 ⇒ 创新点①的星等坐标系解与 mosaic 接缝产品字节错值；同时 `:1087`/`:1107-1111` 的 `|value|/unc`（unc=√cvar≈8.7e-14）把同一地板写进 `snr` 字段（可达 1e13 量级），对外 SNR 面一并失真。
- 定级：**S1**。生产触发路径（逐跳 `文件:行`）：`lib/algorithms/coverage/tools/stage2.cpp:314` `p2_sample_controls_cached(...)`（另一现役入口 `lib/infrastructure/scheduler/src/module_adapters.cpp:9404`）→ `lib/algorithms/coverage/src/sampler.cpp:1267` `p2_sample_controls_impl` → `:837` `m0 = median_of(vals)` → `:843-844` `dev`／`s0 = 1.482602218505602 * median_of(dev)` → `:864` 地板 → `:875`/`:877` cvar/civar → `:1115-1116` 写入 obs → `stage2.cpp:314` 回填 `obs.data()` → `upm.cpp:644` `p2_upm_raw_weight` → `upm.cpp:668-671` per-control 归一化。
- 有无能红的核对：**零**。`docs/algorithms/PHASE2_SAMPLER.md:575` 的 F5（constant patch / σ_bg_raw=0 分支，要求断言 `control_ivar == 0` 且正/负例各一）自标测试载体「无（新建）」；`phase2_rej_nonfinite_weights`（本批文件 3）只测**非有限**，而 1.314e26 是有限值 ⇒ 结构上抓不到；`phase2_sampler_parallel_consistency`（文件 6）在串行/并行间比较，两侧都犯同一错 ⇒ 恒绿。
- 怎么算修好：①`:864` 之后引入零尺度分支：`s0 <= 0.0` 时不产 cvar/civar 有限值——按条文 `control_ivar = 0` 且 `control_variance` 非有限（或以 quality_flags 置位并计数）；②`:879`/`:887` 与第三遍 `:1107-1111`、`:1087` 对该分支不得再算 `|value|/unc`（unc 来自地板 cvar）；③**同批必改**：`upm.cpp:2471` 与 `upm.cpp:644` 的入参门现在对 `control_ivar<=0` 是整批 rc=2 —— 条文 §5.4 的「或等价地以 quality_flags 置位并计数」要求按观测粒度剔除而非整批失败，否则改 ① 会把**正确修复**判成生产全红；④新增 F5 正/负例门（≥50% 同值 patch 触发、<50% 不触发）并接 `phase2_*` 注册面；⑤`docs/algorithms/PHASE2_SAMPLER.md:562` 的 DISP-P2SMP-007 随之关闭。
- 置信：**CONFIRMED**（算术复算 + 文档自陈实测值逐位对齐 + 下游门与归一化读码）。**可达性一腿降级 PARTIAL**：本条所引「真实 M42 300 s R 帧 4096 个 17×17 patch 中 2 个命中」与「伪方差 7.609e-27 实测」的证据文件 `evidence/r2_m42_raw.json`、`evidence/e3e4_floor_structure.json` **不在跟踪面**（`git ls-files | grep -i "r2_m42_raw\|e3e4_floor_structure"` ⇒ 零命中）⇒ 生产数据命中率属文档转述、本批不可复核；但分支可达条件（≥50% 同值像素）与错值算术由读码自证，不受该降级影响。

### CR-48-02 · 被对外署名为"已标定"的 k_corr 查表：表值在仓内不可复算、标定域与生产尺度差 300×，逐帧标定分支在生产恒不可达，而合同面仍写"逐帧查表优先于该值"
- 轴：S（主）＋C
- 位点：`sampler.cpp:96-99` `static const double k[2][3] = { {1.2112, 1.3925, 1.4980}, {2.3958, 2.8971, 3.2035}, };`；`:574` `const bool in_domain = (sc >= 300.0 && sc <= 600.0);`
- 上位依据：
  - `docs/algorithms/PHASE2_SAMPLER.md:307-309`：「当前生产取值 1.4 的来源是项目自产 MC（`control_median_mc_test`，pixfrac=0.8，2000 次，实证 1.3883，保守上取），而**该测试未注册/不在任何 CMake/ctest/CI 面（构建孤儿）** ⇒ 该常数在仓内**不可复跑**」；`eng/tests/unit/CMakeLists.txt:1540`：「现行事实: control_median_mc_test / kcorr_matrix_test 属 tracked-but-unbuilt，PHASE2_UPM.md 必须显式标 MISSING」。
  - `docs/algorithms/v6/phase2-surface/ALG-P2-SURF-UPM.md:83`（裁决 SC-ADJ-F05.4，另见 `artifacts/evidence/v6/science-adjudication/adjudications.json` ADJ-F-OBS-05 ruling ④）：「可复跑标定落地前，只允许在**已声明适用域内**取 k_corr=1.4，**禁止外推/内插**」。
  - `docs/algorithms/PHASE2_SAMPLER.md:334`：「因此**逐帧标定在所有已知生产数据上恒不生效**，生产实际使用的是未在本尺度标定的冻结常数 1.4；任何「control_variance 已做 Drizzle 相关校正」的表述必须写明这一点」。
- 现状（引原文）：六个表值以字面量写死，唯一出处是 `:87` 注释所称「kcorr_matrix_test 实测」，而该 TU 不被任何构建/采集面编译；`:575` `if (pf > 0.0 && in_domain)` 要求源像素角尺度 ∈[300,600]″/px，而本仓实测生产帧为 0.8064″–0.9890″/px（PHASE2_SAMPLER.md:329-332），provenance 实写值量级更低（R-7 研究 :256-257 记 `lookup(0.8, provenance 实写值 0.009856"/px")`）⇒ `:576` `frames[i].kcorr = kcorr_lookup(pf, sc);` 在生产永不执行。合同面 `docs/contracts/DATA_SEMANTICS.md:1739` 写「**逐帧查表（pixfrac × 角尺度双线性插值）优先于该值**；实现锚 = `lib/algorithms/coverage/src/sampler.cpp` 的 `kcorr_lookup` / `kControlCorrDefault`」，无"该优先在生产恒不生效"的后果句（ALG 面 :334 明确要求任何相关表述必须写明）。
- 差在哪：(i) 表值的处置按 §2.5 不能落 `实验标定`（标定件不在跟踪构建面，结果位仅存在于源码注释字面量），只能 `待确认`；(ii) 现行实现按条文所禁的方式**内插**（`:110-116`），且被测试接缝 `:1500 astrocs_phase2_kcorr_lookup_for_test` 驱动的注册门 `phase2_sampler.kcorr.corner_exact`（文档 :572）把内插/角点值钉成回归期望；(iii) 合同面与 ALG 面对同一机制的生效性口径相反。
- 后果：不改生产产物字节（恒取 1.4），故不升 S1；失真的是对外主张——"k_corr 已按帧标定 / control_variance 已做 Drizzle 相关校正"在合同面不成立；并且后续按 SC-ADJ-F05.4 撤内插时 `phase2_sampler` 门判红（改对反而红）。
- 定级：S2（缺产物字节触发路径；生效性主张不成立面为 S/C）。
- 怎么算修好：①`DATA_SEMANTICS.md:1739` 补 :334 的后果句（生产尺度恒 ∉ 域 ⇒ 恒取冻结默认）；②表值二选一：把 `kcorr_matrix_test` 纳入构建＋CI 采集并把九值结果落跟踪面（`实验/` 或 `artifacts/evidence/`；指 `run/` 按 §2.5 判不合格），或按 SC-ADJ-F05.4 删除内插只留 pixfrac=0.8 单档；③**同批必改** `lib/algorithms/coverage/tests/kcorr_lookup_test.cpp` 的期望（角点 exact／插值 rtol 1e-12），否则 ①② 判红；④`docs/science/PHASE2_UPM.md` 按 CMakeLists:1540 的要求把 MC 证据标 MISSING。
- 置信：**CONFIRMED**（生产不可达性由 :574 判据＋两处独立实测尺度共证；构建孤儿由 CMakeLists:1540 自陈）。

### CR-48-03 · k_corr 的"定义域 1 < k_corr"在采样侧入口不设防：`0 < cfg.control_k_corr <= 1` 被静默接受并乘进 control_variance，而 UPM 侧对同一量显式拒绝
- 轴：C（主）＋R
- 位点：`sampler.cpp:517-518` `if (cfg.control_k_corr <= 0.0) cfg.control_k_corr = kControlCorrDefault;`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:305-308` 逐字：「**定义域 1 < k_corr**（k_corr = 1 ⇔ 忽略相关，`p2_upm_control_variance` 返回 rc=2；k_corr < 1 ⇔ N_eff > N_retained，正相关样本的有效样本量不可能大于样本数，物理不可达，返回 rc=1 —— `lib/algorithms/coverage/src/upm.cpp:2772-2783`）」。
- 现状（引原文）：采样侧只挡 `<= 0`；`0<k<=1` 原样进 `:874-875` `const double kcorr_f = (frames[frame_id].kcorr > 0.0) ? frames[frame_id].kcorr : cfg.control_k_corr; const double cvar = kcorr_f * kPiHalf * sigma * sigma / n_ret;`。而 `DATA_ARTIFACTS.md:26` 规定消费侧「**`k_corr` 放大已含在值内** ⇒ 消费侧取用含 `k_corr` 的值」，即 UPM 的那道域门对采样器自算的 cvar 根本不经过。
- 差在哪：同一物理量的同一条域约束，在 `p2_upm_control_variance`（rc=1/rc=2）与 `p2_sample_controls`（仅 `<=0`）两个入口口径不同；方向钉死：k 变小 ⇒ cvar 变小 ⇒ civar 变大 ⇒ 权重过度自信 ⇒ **偏绿**（不是偏红）。
- 后果：任何显式传 cfg 的调用方把 k 设成 0.9 之类"看似更保守"的值，实际得到低估方差/高估权重，且无 rc、无诊断；对外 `control_variance` 单位与语义面（DATA_ARTIFACTS:26）随输入被静默破坏。
- 定级：S2（**缺运行时触发路径**：已核现役两处入口都不透传该字段——`tools/stage2.cpp:256-280` 的 sccfg 组装清单不含 `control_k_corr`（文档 :434 亦记"control_k_corr 未透传，零初始化经 impl 修补回退默认 1.4"））。
- 怎么算修好：`:517` 改 `if (!(cfg.control_k_corr > 1.0))` → 返回 1 并写 err，与 `upm.cpp:2772-2783` 同口径；同批给 `tools/stage2.cpp` 与 `lib/infrastructure/scheduler/src/module_adapters.cpp` 的 sccfg 组装补该字段或显式保持默认（否则改判后两处对新语义不一致）。
- 置信：**PARTIAL**（缺"存在传 0<k≤1 的实际调用方"证据；检索式 `git grep -n "control_k_corr" -- eng docs lib`，除默认/文档外零赋值点）。

### CR-48-04 · 同一 SNR catalogue 在本文件里被两处不同上限读取：采样侧 65536 行静默截断（不可分辨），身份哈希侧 1048576 行
- 轴：R（主）＋C
- 位点：`sampler.cpp:78` `constexpr int kSnrCatalogMax = 1 << 16;`（消费 `:599-606`）；`:406` `const int maxn = 1 << 20;`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:107` 仅登记「kSnrCatalogMax | :78 | SNR catalogue 上限 65536」——登记值而未给推导，且**未规定目录超上限时的行为**。检索式：`git grep -n "65536" -- docs/algorithms`（仅此一处）、`git grep -n "max_detections\|catalog.*cap\|snr.*上限" -- lib docs`（零命中）。
- 现状（引原文）：`lib/infrastructure/aio/src/hips/aio_hips_reader.cpp` 的 `aio_hips_read_snr_catalog` 实现为 `int n = (int)std::min((size_t)max, d->snr.size()); … return n;` —— 返回值对"正好这么多行"与"被截断"完全不可分辨；采样侧 `:603-606` 直接把 `got` 用于四个 vector 的 resize，无 stats、无 stderr、无 rc。被截掉的尾部参与的是 catalog veto（`:895 snr_idx[frame_id].any_above(thr, …)`）与星点掩膜（`:1150` 逐行扫描 `fr.snr`），且取的是**文件序前缀**（非最暗的 65536 个）。
- 差在哪：静默截断＝"退化时按邻近/前缀取值而不报错"的形态；同时 `p2_frame_id` 用 20 位上限把被科学忽略的行计入帧身份哈希 ⇒ 同一数据在"身份面"与"科学面"的覆盖集不同（换目录尾部内容会改 frame_id 却不改任何归约结果）。
- 后果：宽场/密集场帧若目录行数超 65536，则尾部亮星不被 veto ⇒ 星点污染的 control 以 clean 身份进 UPM ⇒ `y_ik` 偏亮 ⇒ 创新点①的星等坐标系正偏；`sky_samples`/`star_masks` 产品对同一天区不完整而统计面看不出。方向钉死：漏星 ⇒ 排异减少 ⇒ **偏绿**。
- 定级：S2（缺"生产目录实际行数 > 65536"的实测证据）。
- 怎么算修好：读侧返回容量满即判截断 ⇒ 采样器要么 rc=1，要么在 `P2SampleStats` 计 `snr_catalog_truncated` 并把该帧标为降级；`:78` 与 `:406` 统一由一个常量/合同键承载；文档 :107 补该值与哪个上游写侧上限对齐的推导。
- 置信：**PARTIAL**（截断语义与两处上限差由读码钉死；缺生产行数）。

### CR-48-05 · tile 读失败被归因成"支撑不足"且不使本次采样失败，统计面把 I/O 故障报成科学剔除
- 轴：R（主）＋C
- 位点：`sampler.cpp:763-766` `if (rc != 0) { pairs[fi].ok = false; std::fprintf(stderr, "[sampler] read_tile_pair failed rc=%d frame=%llu tile=%llu\n", …); }`；`:786-794` `if (!tp.ok || tp.signal.empty() || …) { ++local_insupp; … cs.reason.push_back(1); … }`；`:997` `stats.rejected_insufficient_support += sum_insufficient_support;`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:137` 把 reason 档归为科学判据（min_samples 拒绝等）；条文面对"读失败该记哪一档"零提及。检索式：`git grep -n "rejected_insufficient_support\|reason" -- docs/algorithms/PHASE2_SAMPLER.md`。
- 现状（引原文）：`read_tile_pair` 的三种错误码（`:174` `return 2`（空指针）、`:180` `return 3`（resize 抛）、`:183/:185` `return 1`（aio 读失败））与"有效样本 < min_samples"（`:828-834`）共用 `reason=1` 与同一个 `insufficient_support` 计数器；三种 rc 都只 fprintf，`pass1_cell` 仍 `return 0`（`:921`），整次采样 rc=0。
- 差在哪：诊断口径把故障当正常天光/掩膜剔除；部分读失败时观测静默减少而命令判绿。方向：偏绿。
- 后果：对外 `P2SampleStats` 统计失真，运维不可分辨；全零观测由 `upm.cpp:258`/`:2450` 的 `n_obs == 0 → return 1` 兜住（不会静默出产品），**部分**丢失无兜底 ⇒ 该 control 只剩平滑先验决定 C 场。
- 定级：S3。
- 怎么算修好：`reason` 增设"读失败"档或新增 `stats.tile_read_failures`；由 cfg 决定是否 fail-closed；文档同步 reason 枚举表与统计恒等式。
- 置信：CONFIRMED（读码即得）。

### CR-48-06 · 同一"帧 SNR 中位数"在本文件里有两种算法：veto 与星掩膜用秩统计量 `cp[n/2]`（偶数取上中位），snr 字段回退用真中位数
- 轴：C（主）＋R
- 位点：`sampler.cpp:657` `frame_snr_med[i] = cp[cp.size() / 2];` vs `:658` `frame_snr_med_exact[i] = median_of(frames[i].snr);`；前者消费于 `:893` 与 `:1149`，后者消费于 `:904`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:387`「thr = cfg.star_mask_snr_factor × frame_snr_med[frame]」——名为 med（中位数）但未定义偶数样本口径；同文 :571 F1 要求 median 判据区分 odd/even 并「逐值 bitwise」。
- 现状（引原文）：`median_of`（:199-212）偶数取两中位均值（`return 0.5 * (a + b);`），而 `frame_snr_med` 直接取排序后上中位；同一 catalogue 在 n 为偶数时两值不同。
- 差在哪：一个物理量两套口径且无条文授权；方向钉死：上中位 ≥ 真中位 ⇒ 阈值偏高 ⇒ veto 偏松 ⇒ 漏排少数污染 control（**偏绿**），幅度为一个秩差。
- 后果：`rejected_catalog_veto` 统计与 star_masks 产品相对"真中位数口径"轻微偏松；两口径同文件并存易被后续修改放大成实错。
- 定级：S3。
- 怎么算修好：:657 改走 `median_of`（或直接删掉 `frame_snr_med`，两处消费改用 `frame_snr_med_exact`）；或在文档明确 veto 用"上中位"并给理由。
- 置信：CONFIRMED。

### CR-48-07 · provenance 键缺失时的 k_corr 回退完全静默，而注释自称"调用方另打 stderr 标"
- 轴：R（主）＋C
- 位点：`sampler.cpp:577` `} else if (pf > 0.0) {` （后接 :578-584 的 `[sampler] k_corr 域外回退` fprintf）；`:92` 注释「调用方 p2_sample_controls 另打 stderr 标」
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md:336-339`（DISP-P2SMP-006 正文）逐字：「`sampler.cpp:575-584` 只在 `pixfrac > 0 且尺度 ∉ [300,600]` 时打 `[sampler] k_corr 域外回退` 标；**provenance 键缺失（pixfrac 不可解析）时回退是静默的**——本仓 probe 产品的 signal properties 内**不含** `ASTROCS_DRIZZLE_*` 键…引用回退状态时**必须**区分「已打标的域外回退」与「未打标的 provenance 缺失回退」」；`docs/algorithms/PHASE2_SAMPLER.md:561` 同一事实登记为 DISP-P2SMP-006。裁决要求见 `ALG-P2-SURF-UPM.md:83` 同族条款（适用域必须显式）。
- 现状（引原文）：`frame_drizzle_provenance` 起手 `*pixfrac = 0.0; *scale_arcsec = 0.0;`（:122-123），键缺失或值 ≤0（被 :137/:140 值域判据丢弃）时返回 0 值；`:575` 与 `:577` 两个分支都要求 `pf > 0.0` ⇒ 该情形既不查表也不打标，`frames[i].kcorr` 保持 `:151` 的 `kcorr = kControlCorrDefault`……**注意**：结构体默认是 `double kcorr = kControlCorrDefault;`（:151）而非 0，而 `:569` 注释与 `:874` 判据 `frames[frame_id].kcorr > 0.0` 都假定"域外留 0 走回退链"——注释（:569「域外/未知一律保留 frames[i].kcorr=0」）与初值（:151 `= kControlCorrDefault`）在文本层互相打架；实际效果由 `:575-577` 不给 kcorr 赋值 ⇒ 保持 1.4 ⇒ `:874` 取 1.4（与回退链同值，产物字节不变），但"0 表示未标定"的语义契约在结构体默认值上已被破坏。
- 差在哪：条文要求的两态可分辨（已打标域外回退 vs 静默 provenance 缺失）在代码里只剩一态；且 `:151` 默认值使"per-frame k_corr 未标定"与"per-frame k_corr 已定=1.4"在 `:874` 判据下不可区分。
- 后果：诊断/provenance 面无法支撑"k_corr 适用域已显式声明"的对外主张（裁决 SC-ADJ-F05.2/③）；数值面为保守方向（无 Drizzle provenance 的帧仍被乘 1.4 放大 ⇒ 方差高估、权重偏低）。
- 定级：S3（产物字节方向保守；已登记项，本批独立取证确认代码仍未修且新增 `:151` 与 `:569` 的语义冲突）。
- 怎么算修好：`:577` 的 `else if` 补 `else`（`pf <= 0`）打标并落 diagnostics 计数；`:151` 初值改 0（真正的"未标定"哨兵）以免 `:874` 把默认当逐帧值，并同步 `:569` 注释。
- 置信：CONFIRMED（读码即得；本批独立核对 `:575` 判据与 `frame_drizzle_provenance` 零值初始化，未沿用邻批结论）。

### CR-48-08 · V6 摘要面的分位数取法、model_error 与 max_systematic_deviation 的定义在条文里只有"必须带"，代码自择口径且方向偏乐观
- 轴：S（主）＋C
- 位点：`sampler.cpp:1396-1401` `auto quantile = [&](double p) -> double { double idx = std::floor(p * (double)(m - 1)); … return vals[i]; };`；`:1411-1417` `const double d = vals[i] - out->p50; … out->model_error = std::sqrt(sse / (double)m);`
- 上位依据：条文面只点名存在性——`docs/design/PHASE1_DETAILED_DESIGN.md:192`「摘要必须带 p05/p50/p95、最大系统偏差、取样覆盖和模型误差」；未规定分位数 scheme、未定义 model_error/max_systematic_deviation 的计算式。检索式：`git grep -n "p05\|p95\|model_error\|max_systematic_deviation" -- docs/algorithms docs/science docs/design`（除该点名句外零命中口径定义）。
- 现状（引原文）：分位取"最近下邻不内插"（`std::floor(p*(m-1))`）；`max_systematic_deviation = max|v − p50|`；`model_error = RMSE(v 关于 p50)`。三者被 `p2_scalar_degrade_gate` 当入参校验（`:1465`/`:1462` 只判有限与非负，不与阈值比较），真正参与门判的只有调用方另给的 `spatial_residual_p95`/`spatial_trend`/`power_loss`。
- 差在哪：方向钉死——下取整分位使发布的 p95 **偏小**、p05 **偏小**（对外散布量偏乐观，偏绿）；`max|v−p50|` 把随机散布与系统偏差混为一谈（对外"系统偏差"量偏大，偏红），两个字段方向相反却都无条文可依据。
- 后果：SO-07 降级门的输入面与对外的"模型误差/系统偏差/散布"三主张口径不可追溯；一旦阈值签收，同一数据在不同 scheme 下的门结果可翻转。
- 定级：S3（当前无阈值生效 ⇒ 尚不改判；缺的是口径唯一性）。
- 怎么算修好：在 `docs/algorithms/PHASE2_SAMPLER.md` 的 V6 节固化分位 scheme（建议线性内插或明确 nearest，并声明 n 下限）与 model_error/max_systematic_deviation 的定义式，代码按式实现并在头注释引条文；阈值签收（SO-07）前完成，避免"先钉数后对口径"。
- 置信：CONFIRMED（口径零规定由检索式共证）。

### 1.3 不报的核过项（附检索式）

- `median_of` 偶数分支取 `*std::max_element(v.begin(), mid_it)`（:210）：`nth_element` 后左半部最大值即下中位，算法正确，且 :208-209 注释明确点名 min_element 是错的（P0-01）。非缺陷。检索式：`git grep -n "nth_element" -- lib/algorithms/coverage/src/sampler.cpp`。
- 属性键解析不做 trim（:131-141 与 :346-353 的 `p2_frame_id` 一处 trim 一处不 trim，看似口径不一）：写侧固定 `key=value\n` 无空格（`aio_hips_writer.cpp:676-682` `content += p.first; content += '='; content += p.second; content += '\n';`），`std::atof` 又能吞 `\r` ⇒ 对本仓产品不会把有效键误判为缺失。不报。
- `out-of-range tile` 在首 cell 硬失败（:695-699 return 1）与后续 cell 打标跳过（:740-748）处理不同：跳过路径把该 tile 的 cells 全置 `tile=-1` 且不产观测，不构成错值，且预检已挡住畸形 coverage 的主症状。不报。
- "串行/并行双份实现漂移"风险不存在：`pass1_cell` 为同一 lambda（:734 定义，:946 与 :967 两处调用），cells 按绝对索引写（:914-916），veto/insuff 累加器为每 worker 私有的独立局部量（:945 注释即为此前复用累加器 bug 的修复）。不报（并行一致性另有专件，见文件 6）。
- `p2_spatial_model_eval`（:1298-1362）的 fail-closed 方向逐条核过：非法模型/越界（"no extrapolation"）/缺节点或非有限节点均 rc=1 且 value=NaN，边界钳格 `std::min(std::floor(fx), xmax - 1.0)`（:1327）使边点精确取边节点而不外插，未发现恒真断言。不报。
- `p2_scalar_degrade_gate` 未自造阈值：`thresholds_declared` 为假即 `P2_SCALAR_UNAVAILABLE`（:1434-1439，引「SO-07 PENDING_OWNER_SIGNOFF」），`max_systematic_deviation`/`sampling_coverage`/`model_error` 只做有限性/值域校验而不参与放行判据 ⇒ 不存在"用未签收阈值放行"。不报。
- 掩膜去重的 RA 接缝（:1152-1153 的 `llround(ra*1e4)` 在 0/360 两侧不合并）：后果只在 star_masks 列表出现冗余圆帽，重复覆盖同一方向不改判据结果。不报（值已入处置表）。
- 默认值两份事实源（`p2_sampler_default_config` :302-321 与 impl 修补 :503-522）：逐项核对 15 个值**全部相同**，:301 注释的"单一来源（null cfg 与显式 cfg 同语义）"指的是语义一致而非字面量复用；现行不构成错误，只构成漂移风险。不报。
- `kTileWidth=512` 与 `leaf_shift=9`、`kSnrCatalogMax` 之类数组/位移/缓冲常数按 §4 属结构性常数，已进处置表但不作 finding。

## 2. `lib/algorithms/coverage/src/stage2_common.cpp` —— 已读（551 行全文）

### 2.1 §2.5 数值处置表（44 行；同族合并处已列全部位点）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :15 | `cfg->hips.size() < 2` | 2 | 最少输入帧数（马赛克定义性下界） | 结构性不适用 | 少于 2 帧不构成"跨帧"；不是科学量 | — | CONFIRMED |
| :29-31 | target_order 域 | 0 / 29 | HEALPix 阶上界放行 | 待确认 | 0..29 = HEALPix 理论上界（写侧 Nside≤2^29，aio_hips_writer.cpp:1200），但 phase2 的 leaf 寻址另加 9 阶预算（sampler.cpp:777）⇒ 两面不同源，见 CR-48-13 | 更大⇒leaf Nside 越界/移位 UB（静默错值而非判红） | CONFIRMED |
| :40 | control_grid_per_tile 默认 | 8 | 每 tile 控制点网格边数 | 待确认（值与 sampler 默认一致） | DATA_SEMANTICS.md:1739 附近未给"为何 8"；CONFIG_SCHEMA.md:46 族内列值；与 sampler.cpp:304/:519 三处同值 | 更大⇒cell 更小更易 min_samples 拒绝 | CONFIRMED |
| :41-42 | control_grid_per_tile 域 | 1 / 64 | 配置允许范围 | 待确认 | **64 以内的非 512 因子（3/5/6/7/9…）被放行**，而 sampler.cpp:630 `cell_side = kTileWidth / grid` 整除截断 ⇒ tile 边缘静默无 control；条文未限定 2 的幂 | 放宽⇒边缘漏采样（偏绿：coverage 看似完整） | CONFIRMED |
| :54 | patch_radius_pixels('auto') | 2 | auto 解析值 | 待确认 | 注释 :46「auto → 2」；sampler.cpp:305/:520 同值；该键在现行 pass1 未被消费（估窗实由 background_patch_radius 决定） | — | PARTIAL（未证"未被消费"的完整性，仅读 sampler pass1） |
| :57-58 | patch_radius_pixels 域 | 0 / 64 | 配置范围 | 结构性不适用 | 半径上界为实现护栏 | 0 被允许（半径 0 ⇒ 单像素窗），与 :81 的 background_patch_radius≥3 不对称 | CONFIRMED |
| :67 | min_samples 默认 | 5 | 有效样本下限 | 待确认 | 同 sampler.cpp:306 行（文档 :53 给域 [5,289]、:274 给 N=5 处偏差 −8.5%） | 更小⇒退化 cell 更易通过 | CONFIRMED |
| :74 | snr_search_radius_deg | 0.05 | snr_ik 检索半径 | 待确认 | CONFIG_SCHEMA.md 列值；无推导 | 更大⇒邻域更平滑（保守） | CONFIRMED |
| :81 | background_patch_radius 下限 | 3 | 最小估计窗（7×7） | 待确认 | 条文未给 3 的依据；与 :57 允许 0 不对称 | 更小⇒窗退化 | CONFIRMED |
| :80 | background_patch_radius 默认 | 8 | 17×17=289 窗 | 待确认 | 同 sampler.cpp:308 行 | 更大⇒σ_bg 含结构分量（偏保守） | CONFIRMED |
| :86 | background_clip_sigma 默认 | 3.0 | 亮端裁剪 σ 倍数 | 待确认 | 同 sampler.cpp:309 行（文档 :229-231 给该值处效应量化） | 更小⇒σ_bg 偏小（反保守） | CONFIRMED |
| :92 | background_clip_iters 默认 | 3 | 迭代上界 | 待确认 | 同 sampler.cpp:310 行 | — | CONFIRMED |
| :98 | background_max_contamination 默认 | 0.20 | 污染分数上限 | 待确认 | 条文面零提及推导 | 更小⇒排异更多（偏红） | CONFIRMED |
| :99-100 | 同键值域 | 0.0 / 1.0 | (0,1) 开区间校验 | 结构性不适用 | 比率值域定义 | 与 :108-109 的 (0,1] **闭开不一致**（同一"分数"两键两域） | CONFIRMED |
| :105 | background_contamination_sigma 默认 | 3.0 | 污染判定阈值 | 待确认 | 同 sampler.cpp:312 行 | — | CONFIRMED |
| :107 | background_min_retained_fraction 默认 | 0.60 | 保留率下限 | 待确认 | 同 sampler.cpp:313 行 | 更大⇒排异更多 | CONFIRMED |
| :114 | background_tolerance 默认 | 3.0 | 局部容差门 σ 倍数 | 待确认 | 同 sampler.cpp:314 行 | 更大⇒门更松（偏绿） | CONFIRMED |
| :120-121 | background_neighbor_radius 默认/下限 | 2 / 1 | 邻域 cell 半径 | 待确认 | 同 sampler.cpp:315 行 | 更小⇒局部背景更不稳 | CONFIRMED |
| :126 | background_catalog_veto 默认 | 1 | Stage E 开关 | 结构性不适用 | 布尔开关 | 0 被允许并直传 sampler（关掉后排异减少，偏绿）——属配置授权范围 | CONFIRMED |
| :127 | huber_delta 默认 | 1.345 | Huber 拐点（95% 效率经典值） | 文献值 | 标准 Huber 常数；合同登记 DATA_SEMANTICS.md:1970「1.345（upm.huber_delta 可覆盖）」＋:2080＋CONFIG_SCHEMA.md:33 | 更小⇒更 aggressively 降权离群（偏红） | CONFIRMED |
| :140 / stage2_common.h:30 | P2_SMOOTHING_LAMBDA_AUTO | 0.1 | smoothing 'auto' 的解析值 | 待确认 | 头 :26-28 已自陈与「冻结面修改须走 SCI/合同变更」冲突并**已上呈**（conform-fix-b.md §009，「本实现不擅自改冻结面」）⇒ 属待裁项而非本批新指控 | 更大⇒C 场更平滑（系统偏差被抹平，偏绿） | CONFIRMED |
| :153 | zero_anchor_weight 默认 | 1e-3 | 弱零校正锚权重 | 文献值（合同登记） | DATA_SEMANTICS.md:2082「1e-3 \| 弱零校正锚权重（h:76；<0→1e-3 :244）」＋CONFIG_SCHEMA.md:33 | 更大⇒零锚更强（C 场被压向 0） | CONFIRMED |
| :155 | max_irls_iterations 默认 | 100 | IRLS 迭代上界 | 文献值（合同登记） | DATA_SEMANTICS.md:1971/:2083「100（upm.max_iterations 可覆盖）」 | 更大⇒收敛更充分 | CONFIRMED |
| :156 | tolerance 默认 | 1e-6 | IRLS 收敛容差 | **合同与实现相反** | 合同明写 DATA_SEMANTICS.md:1972「tolerance \| 1e-6（**无 config 覆盖键**）」，而本行读 `model.tolerance` 并经 :546 透传生效 | 见 CR-48-16 | CONFIRMED |
| :159 | robust_loss 只判 "huber" | 0 | 键校验即弃 | 文献值（合同登记 0=huber） | DATA_SEMANTICS.md:1969「robust_loss \| 0（huber）」；本处从不赋 cfg->robust_loss（struct 默认 0 恰与之一致） | 见 CR-48-12 | CONFIRMED |
| :165 | snr_weight_mode 只判 "snr2_normalized" | 0 | 键校验即弃 | 待确认 | **两面打架**：PUBLIC_API.md:2208 称「原 snr_weight_mode…**已按 §9.73 A44 作废：键不存在**」，而 DATA_SEMANTICS.md:1970 仍以 `upm_weight_source（原 snr_weight_mode）\|0（snr2_normalized）` 列在册 | 见 CR-48-12 | CONFIRMED |
| :169 | sigma_floor 默认 | 1e-3 | UPM 侧 σ 下限 | 文献值（合同登记） | DATA_SEMANTICS.md:1973「sigma_floor \| 1e-3 \| :191」；消费点 upm.cpp:292/:762/:1022/:1244/:2438 全部只作用于 `max(\|uncertainty\|, floor)` 的 robust 尺度 | 与 sampler.cpp:864 的 1e-12 是同一概念两套值 ⇒ 见 CR-48-14 | CONFIRMED |
| :174 | support_power 默认 | 1.0 | legacy 支路 support 指数 | 文献值（合同登记 1.0） | DATA_SEMANTICS.md:1974；但**只**在 use_ivar_weight==0 的 legacy 支路被消费（upm.cpp:1994）⇒ 该键的存在本身依赖 CR-48-09 | — | CONFIRMED |
| :180 | use_ivar_weight 默认 | 1 | 权重口径布尔开关 | 待确认 | DATA_SEMANTICS.md:2089 承认「0=legacy 仅 ablation/诊断（SNR-015）」，但设计面（ASTROCS_DESIGN §3.1:175，本文件 :438 自引）说「没有可选择项」 | 见 CR-48-09 | CONFIRMED |
| :208 | memory_limit_mb 默认 | 24576 | 执行内存预算（MB） | 文献值（合同登记） | DATA_SEMANTICS.md:1194「24576（:51）\| MB \| 执行内存预算（CON-002 全局执行预算域）」＋PUBLIC_API.md:1209；下游 safety_factor=0.75（PHASE2_MOSAIC_WRITE.md:103）⇒ 有效 18.4 GB，低于声明的 23 GiB 物理内存 | — | CONFIRMED |
| :294/:304 | underdetermined_n | 0（缺键→向 resolver 查） | 欠定档参数缺省解析 | 公式导出 | 注释 :284-291（CONFORM-FIX-B-008）：默认值单一来源 = `p2_reject_plan_resolve` 的 profile/request 规则（rejection.h:230-234 冻结）；代码 :300-314 真按该路径调用而非复制字面量 ✓ | 更小⇒更早判定欠定并降级（偏红） | CONFIRMED |
| :333 | normalization_floor 默认 | 1e-12 | MEDIAN_SCALE 的最小 \|median\| | 待确认 | rejection.h:214「（默认 1e-12）」只登记值不给推导；**本行无任何值域校验**（负值/0 可进，:332-333）⇒ 尺度归一化的除零保护形同虚设 | 更小⇒尺度归一化在 \|median\|≈0 时放大噪声（偏绿）；0/负值使保护消失 | CONFIRMED |
| :340/:341 | large_scale min_structure_pixels | 8 / ≥1 | 连通域结构最小像素数 | 待确认 | 条文面零提及推导。检索式：`git grep -n "min_structure_pixels" -- docs` | 更小⇒大尺度排异更易触发（偏红） | CONFIRMED |
| :347/:349 | low/high_grow_radius_pixels | 2 / 2 | 连通域生长半径 | 待确认 | 同上（`git grep -n "grow_radius_pixels" -- docs` 零命中推导） | 更大⇒掩膜膨胀过度（偏红） | CONFIRMED |
| :372-375, :379-382, :386-389, :412-415 | sigma/winsorized/averaged/median_sigma 阈值表 | 4.0, 3.0, 8 | 各档上下侧 σ 与迭代上界 | 文献值 | `docs/algorithms/REJECTION_ALGORITHMS.md:157`「阈值冻结：`sigma/winsorized/averaged/median_sigma 4.0/3.0/8`」＋:181「阈值表…**为 Project-defined 冻结值（本文件 §12），文献不提供门值**」＋CONFIG_SCHEMA.md:46-52 逐档列值；WBPP 2.5.9 对照锚（同文 :65 与 docs/science/REJECTION.md §5） | 上侧 3.0 < 下侧 4.0（非对称，故意偏向保暗端）：方向与 §12 的冻结说明一致 | CONFIRMED |
| :393-396 | linear_fit 阈值 | 5.0 / 3.5 / 8 | WBPP Light 档 | 文献值 | REJECTION_ALGORITHMS.md:65「阈值 5.0 low/3.5 high，8 iter；**不是** MAD 尺度」＋:157；但 `docs/science/REJECTION.md:67` 记该档在生产 profile 下「**改投 winsorized**」⇒ 本处默认值仍按对照档装配 | 见 CR-48-26（路由面），本批文件 5 独立取证 | CONFIRMED |
| :400-401 | generalized_esd | 0.05 / 10 | Rosner α 与最大离群数 | 文献值 | REJECTION_ALGORITHMS.md:66「F5: ESD: Rosner α=0.05 max10」＋:158 | 更小 α⇒更松（偏绿） | CONFIRMED |
| :406-408 | percentile | 0.2 / 0.1 | WBPP Light 分位双侧 | 文献值 | REJECTION_ALGORITHMS.md:53/:91/:158 | 更小⇒剔得更少 | CONFIRMED |
| :420-423 | minmax（**已禁用方法**的参数块） | 1 / 1 / 4 | 双侧删除数与最小保留 | 待确认 | 方法面已按 FZ-REJ-NO-MINMAX 拒（:250-256；DATA_SEMANTICS.md §22 首注、:1657「~~minmax~~（**已禁用**）」），但 CONFIG_SCHEMA.md:54 与 REJECTION.md:120 仍把 1/1/4 列为在册档 | 见 CR-48-11（参数面静默接受） | CONFIRMED |
| :486/:491 | cpu_workers / io_workers 域 | 0 / 1024 | 并发预算范围（0=auto） | 待确认 | 文案自称「0=auto」，但 :487/:492 原样存入且无 0→预算解析；下游 sampler.cpp:927 `workers = (cpu_workers>0)?cpu_workers:1` ⇒ 0 实为串行；合同面 DATA_SEMANTICS.md:1976 却写「cpu_workers = host->budget.max_workers（blocks(budget)，**禁硬编码**）」 | 见 CR-48-15 | CONFIRMED |
| :502 | memory_budget_bytes 默认 | 0 | 0 ⇒ 由 memory_limit_mb 决定 | 文献值（合同登记） | docs/architecture/execution_options_contract.md:26「0 \| 内存预算字节；0 ⇒ 由 memory_limit_mb 决定」 | — | CONFIRMED |
| :542/:543 | quality_mode / control_reliability | 0 / 1.0 | UPM 装配硬编码（无配置面） | 待确认 | DATA_SEMANTICS.md:1975「control_reliability \| 1.0 \| :194」有登记；quality_mode=0 的语义靠 `quality_factor(flags, mode)`（upm.cpp:1980）而未在 :542 处说明为何不可配 | control_reliability 恒 1.0 ⇒ upm.cpp:670 的 per-control 权重乘子退化 | CONFIRMED |
| stage2_common.h:74-96 | sky_plane 族默认：enabled=true、spline_degree=3、node_spacing_deg=0.0、max_nodes=8192、gradient_order=1、gauge_mode=0、weight_mode=0、rank_rtol=1e-10 | 见左 | 天光面链装配默认 | 混合 | node_spacing=0「由输入几何导出」有锚（PHASE2_UPM §7a:185-189，头 :63-66 引）；rank_rtol 有 FZ-AP2S-RANK-RTOL 与 DATA_SEMANTICS.md:2224/:2856（地板 `max(m,n)·eps`）；**spline_degree=3 与 max_nodes=8192 在 docs 面零命中**（检索式 `git grep -n "max_nodes\|spline_degree" -- docs` ⇒ 0 行），且 parser :187/:189 对二者**无任何值域校验** | degree 更大⇒基函数更富 ⇒ 过拟合帧间差异；max_nodes 更小⇒自适应 fail-closed rc=5（头 :72-73 自陈旧默认 2048 会误拒） | CONFIRMED |
| stage2_common.h:53-77 | robust_loss=0、snr_weight_mode=0、huber_delta=1.345、smoothing=0.1、zero_anchor=1e-3、sigma_floor=1e-3、support_power=1.0、use_ivar=1、max_irls=100、tolerance=1e-6 | 见左 | struct 默认（与 parser 默认对齐，CONFORM-FIX-B-009） | 混合 | 头 :60-61 注释立规「struct 默认 == parser 默认 == CONFIG_SCHEMA『auto』解析值（CONFIG_SCHEMA.md:3-4）」；逐项核对**全部一致**（1e-3/1e-3/1.0/1/100/1e-6/1.345/0.1 ✓） | — | CONFIRMED |

### 2.2 findings

### CR-48-09 · `integration.use_ivar_weight` 把"权重口径"留在配置面：置 0 即让 science 权重走设计明文禁止的 support×SNR 式，而同一函数对两个同族退役 token 却按"出现即拒"处理
- 轴：D（主）＋C＋R
- 位点：`stage2_common.cpp:179-180` `cfg->use_ivar_weight = m.value("use_ivar_weight", 1) != 0 ? 1 : 0;`；`:541` `mcfg.use_ivar_weight = cfg.use_ivar_weight;   // 显式透传`
- 上位依据（引原文）：
  - 本文件自己引用的权威：`:437` `ASTROCS_DESIGN.md §3.1:171「全程只有 SNR，没有"权重模式"这个概念」`、`:438` `§3.1:175「权重的产生链固定为两步、**没有可选择项**」`；
  - `docs/science/PSF_SIGNAL_WEIGHT.md §4:62/72`（:439-440 引）「单一权重口径（无模式选择）」「**没有可选择的口径**：不存在口径选择键、口径枚举、口径配置项或口径产物」；
  - `docs/ci/01_CHECKS.md:100` CHK-NO-WEIGHT-MODE-CODE（P0）：「`lib/**` + `eng/tools/**` 无模式选择机制（**枚举 / 解析路由 / 口径门 / 口径集合 API / 生产模式列表成员**）」；
  - 本文件自订退役对象标准 `:445-446`：「该键**既不能被设、也不能被读**：出现即 fail-closed 拒绝（退役对象的拒绝面必须存活，不得静默忽略或静默取默认值）」。
- 现状（引原文）：`weight_mode` 与 `legacy_allow_weight_fallback` 各得一段 fail-closed 拒绝（:447-454、:459-467），但概念本身仍以 `use_ivar_weight` 布尔形态可设、可读、可透传；`lib/algorithms/coverage/src/upm.cpp:1981-1998` 的分支为：
  `if (cfg.use_ivar_weight != 0) { … *out_raw = qf * civ; return 0; }` → 否则 `const double sp = std::clamp(obs->support, 0.0, 1.0); const double snr2 = obs->snr * obs->snr; const double unc = std::max(std::fabs(obs->uncertainty), cfg.sigma_floor); *out_raw = qf * std::pow(sp, cfg.support_power) * (snr2 / (1.0 + snr2)) / (unc * unc);`
  —— 即 `docs/science/PHASE2_UPM.md`（SCI-UPM-WEIGHT-001）点名禁止的「单像素 ivar/support/SNR 乘因子」（该口径另见 `artifacts/evidence/governance-01/research/R-2_phase2权重与UPM语义.md:132` 的条文引文「生产 UPM 权重 = quality × geometric_reliability × control_ivar（SCI-UPM-WEIGHT-001），**禁止再用单像素 ivar/support/SNR 乘因子**」）。合同面把它当在册开关：`docs/contracts/DATA_SEMANTICS.md:2089`「0=legacy 仅 ablation/诊断（SNR-015）」，`docs/contracts/PUBLIC_API.md:2208` 称其为"拟合内部诊断开关"。
- 差在哪：条文说"不存在口径配置项"，实现留了一个配置项并且它改变权重定义式；`upm.cpp:1176` 还把 `use_ivar_weight` 写进几何/配置哈希 payload、`:1397` 写进模型 JSON —— 即系统按"可选口径"来持久化与识别它。"仅 ablation/诊断"的限定**没有任何实现面承载**（无编译期开关、无环境旗标、无 rc 拒绝、无诊断标记），config 写 0 就进生产权重链。
- 后果：mosaic 的观测权重由 `w = qf·support^p·snr²/((1+snr²)·unc²)` 决定而非 `qf·control_ivar`；两式对同一批观测给出不同的相对权重（snr²/(1+snr²) 在高 SNR 饱和到 1、低 SNR 压到 ≈snr²，而 1/方差是另一条曲线）⇒ C 场、corrected 帧与接缝产品字节错值；同时创新点①②③共同的"单一权重口径"对外主张在该配置下不成立。方向钉死：配置可达 ⇒ **误绿/错值**（不是判红）。
- 定级：**S1**。触发路径（逐跳）：生产 config 输入（`lib/algorithms/coverage/configs/stage2_*.json` 同族文件）→ `stage2_common.cpp:179`（解析）→ `tools/stage2.cpp` 组 UPM 配置 → `stage2_common.cpp:541`（透传 mcfg）→ `lib/algorithms/coverage/src/upm.cpp:644`（`p2_upm_raw_weight` 逐观测）→ `upm.cpp:1981` 判假 → `upm.cpp:1992-1997` legacy 权重 → `upm.cpp:668-671` per-control 归一化 → C 场/积分。
- 有无能红的核对：本批文件 4（`retired_object_reject_negative_test.cpp`）与文件 3 独立取证；就本文件面而言，`eng/ci/check_no_weight_mode_code.py` 声称扫 `lib/**` 的"解析路由/口径门"——若它把 `use_ivar_weight` 计入模式选择机制则现树应判红，若不计入则门本身漏判。我不能实跑 CI（§1 禁令），故此项列入"需要实跑才能定"。
- 怎么算修好：①把 `use_ivar_weight` 按 :447 同形态改为"出现即拒"；②ablation 支路（`upm.cpp:1987-1998`）改由测试专用入口（如 `p2_upm_raw_weight_ablation` 或编译期 `ASTROCS_ABLATION`）承载，生产 cfg 结构体字段删除；③**同批必改**：`DATA_SEMANTICS.md:2089`、`PUBLIC_API.md:2208`、`upm.h:89`/`:168-172`、`stage2_common.h:64-66` 注释、`upm.cpp:1176/:1397/:1540-1548` 的序列化面（否则改对反而合同/回归判红）；④给 CHK-NO-WEIGHT-MODE-CODE 补"配置面权重开关"负例。
- 置信：**CONFIRMED**（权重分支与配置链均逐字读码；未实跑）。

### CR-48-10 · `sky_plane.weight_mode` 是第二个权重口径枚举（0=inverse_variance／1=snr2），配置解析连值域都不校验，且该链默认启用
- 轴：D（主）＋R
- 位点：`stage2_common.cpp:193` `cfg->sky_plane_weight_mode = sp.value("weight_mode", cfg->sky_plane_weight_mode);`
- 上位依据：`lib/algorithms/coverage/include/astro/phase2/sky_plane.h:216` `int    weight_mode;           // 0=inverse_variance；1=snr2；默认 0`；同一条禁令见 CR-48-09（ASTROCS_DESIGN §3.1:171/175、PSF_SIGNAL_WEIGHT §4、CHK-NO-WEIGHT-MODE-CODE）；`stage2_common.h:74` `bool sky_plane_enabled = true;`（缺省即在产）。
- 现状（引原文）：:183-196 的整个 `sky_plane` 块是本函数里唯一**零校验**的配置块——七个键（enabled/spline_degree/node_spacing_deg/frame_gradient_order/gauge_mode/weight_mode/rank_rtol/frame_gain）全部原样 `sp.value(...)` 收，无枚举判定、无值域判定；非法值到 `sky_plane.cpp:524` 被静默改写：`if (cfg.weight_mode != 0 && cfg.weight_mode != 1) cfg.weight_mode = 0;`。取 1 时 SNR 域真正进入计算：`sky_plane.cpp:1053` `const double sigma = (cfg.weight_mode == 0) ? std::sqrt(s.variance) : (1.0 / std::max(s.snr, 1e-12));`、`:1169` 同一式用于排异比、`:1688` `if (m->cfg.weight_mode == 0) { … w = 1.0 / s.variance; }`。
- 差在哪：设计禁"口径枚举/配置项"，这里既有枚举又有配置路由；并且非法值静默回退（本仓把它列为缺陷形态：「静默回退＝缺陷」），而同一文件对退役 token 明确要求"出现即拒"。
- 后果：天光面联合拟合（δ_k、g_k，即创新点③的加性天光链）的权重可被配置改成 SNR 域；`rank_rtol`/`spline_degree`/`max_nodes` 无校验 ⇒ 0 或负值进求解器（秩判定阈值 τ=0 ⇒ 可识别性判据恒真，方向＝**偏绿**）。
- 定级：S1（默认启用 + 配置可达 + 判据方向可定）。
- 怎么算修好：删 `weight_mode` 配置键并作废 `sky_plane.h` 枚举的 1=snr2 支路（同 CR-48-09 的处置）；:524 的静默回退改 fail-closed；`sky_plane` 块补与 model 块同级的值域校验（rank_rtol>0 且 ≤1、spline_degree∈{…}、node_spacing_deg≥0、max_nodes≥1）。
- 置信：**CONFIRMED**（消费点与默认值逐字读码；`rank_rtol=0 ⇒ τ=0 恒真`的方向由 DATA_SEMANTICS.md:2224「identifiable ⟺ rank_eff == n_params ⟺ kappa < 1/rank_rtol」直接可推：1/0 → +Inf ⇒ 恒成立）。

### CR-48-11 · 已禁用方法 `minmax` 的参数块仍被静默接受：方法 token 面拒、参数面收
- 轴：C（主）＋D
- 位点：`stage2_common.cpp:417-424` `if (rj.contains("minmax")) { const auto& s = rj["minmax"]; cfg->minmax_low_count = s.value("reject_low_count", 1); cfg->minmax_high_count = s.value("reject_high_count", 1); cfg->minmax_min_kept = s.value("min_kept", 4); }` vs `:250-256` `else if (method == "minmax") { *err = "rejection.method=minmax 不可选 (FZ-REJ-NO-MINMAX)"…; return false; }`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:1657`「~~minmax~~（**已禁用**，见 §22 首注）删后<min_kept → 全栈」；本文件 :443-449 自订标准「该键既不能被设、也不能被读：出现即 fail-closed 拒绝」；:246-249 注释自陈「schema 枚举**不覆盖**这条旁路，必须在此同面 fail-closed 封堵，否则生产可达 reject_minmax_impl」。
- 现状：`P2_REJECT_MINMAX = 9` 仍在枚举（`rejection.h:55`）、kernel 仍在（`rejection.cpp:2169`/`:2566`）、工具入口仍可点名它（`lib/algorithms/coverage/tools/rejection_cli.cpp:145` `{"minmax", P2_REJECT_MINMAX},`）；AUTO 面另有守卫（`rejection.cpp:1290-1292`「AUTO **禁止**产出 min/max / NoRejection…命中 ⇒ fail-closed」）。
- 差在哪：封堵只覆盖 `rejection.method` 一个 token，`rejection.minmax` 子对象（三个参数键）在方法已禁的前提下照样通过校验并被赋值；按本文件自订标准，退役对象的任何面出现即该拒。
- 后果：对外"FZ-REJ-NO-MINMAX 已在配置面封堵"的主张只成立一半；用户留在 config 里的 `rejection.minmax` 不报错（静默接受），而工具面 `rejection_cli` 仍能真跑 minmax ⇒ 同一禁则在两条入口口径不同。
- 定级：S2（生产 mosaic 路径经 :250 挡住 method，参数本身不改产物；失真在禁则完整性与静默接受）。
- 怎么算修好：把 `rj.contains("minmax")` 并入拒绝条件（与 :217-225 的旧别名拒绝同形态）；同批处理 `rejection_cli.cpp:145` 的映射与 `synthetic_gate.cpp:4656/:4675/:4826` 的在册用例（它们仍把 MINMAX 当可规划方法，改判后需同步）。
- 置信：**CONFIRMED**。

### CR-48-12 · `robust_loss` 与 `snr_weight_mode` 两键"校验即弃"，且两本合同对 `snr_weight_mode` 是否在册说法相反
- 轴：C（主）＋S
- 位点：`stage2_common.cpp:157-162`（robust_loss 只判等值 `"huber"`）、`:163-168`（snr_weight_mode 只判 `"snr2_normalized"`）；两键均**从不赋给 `cfg->robust_loss` / `cfg->snr_weight_mode`**（二者由 `stage2_common.h:53-54` 的 struct 默认 0 决定）
- 上位依据：`docs/contracts/PUBLIC_API.md:2208`「UPM fit 的 `upm_weight_source`（原 `snr_weight_mode`，（已按 §9.73 A44 作废：**键不存在**；权重是派生量）…」 vs `docs/contracts/DATA_SEMANTICS.md:1970`「`upm_weight_source（原 snr_weight_mode，）` | 0（snr2_normalized） | :185」（仍列为在册会话冻结值）；DATA_SEMANTICS.md:1969 亦列 `robust_loss | 0（huber）`。
- 现状：本文件把 `weight_mode`/`legacy_allow_weight_fallback` 按"出现即拒"处理（:447/:459），却对同族 `snr_weight_mode` 反其道：键存在且值恰为 `"snr2_normalized"` ⇒ 静默无效果，值不同 ⇒ 整份 config 判错。`upm.cpp:1396/:1540` 还把 `snr_weight_mode` 序列化进模型 JSON 与读回。
- 差在哪：合同一面说键不存在、另一面把它列成冻结字段；实现既不允许它表达任何语义，也不按退役标准拒绝它——"能被设但无效果"是最易误导的第三态。
- 后果：用户按旧模板保留该键可正常运行（以为选择了 SNR 权重口径，实际没选），按 PUBLIC_API 删键也正常 ⇒ 配置面不能反向验证"权重口径唯一"；两本合同的口径不一致会传导到 schema 校验（`eng/contracts/schemas/phase_config_mosaic.schema.json` 未声明该键，而 :247 注释自陈「mosaic_config.additionalProperties=false」⇒ 走 schema 的入口会拒它、走本 parser 的入口接受——两条入口对同一键一拒一收）。
- 定级：S3（不改产物字节）。
- 怎么算修好：与 :447 同形态把两键改为"出现即拒"；或由 parser 真解析并赋 `cfg->snr_weight_mode`（同时删 PUBLIC_API 的"键不存在"声明）；二选一后统一 DATA_SEMANTICS:1970 与 PUBLIC_API:2208；:535 的透传随之处置。
- 置信：**CONFIRMED**。

### CR-48-13 · `inputs.target_order` 放行 0..29，但采样器 leaf 寻址只有 20 阶预算，且 `1u << (order+9)` 在 ≥23 阶是未定义行为
- 轴：R（主）＋C
- 位点：`stage2_common.cpp:28-31` `cfg->target_order = to.get<int>(); if (cfg->target_order < 0 || cfg->target_order > 29) { *err = "target_order 必须在 0..29"; return false; }`
- 上位依据：`sampler.cpp:777` `astrocs::healpix::pix2ang_nest(1u << (unsigned)(coverage->target_order + leaf_shift), center_leaf, ra_deg, dec_deg);`（leaf_shift=9，:546）；`sampler.cpp:781` `cs.tile = (int)tile_ipix;`、`:1211` `out_controls[i].tile_ipix = (std::uint64_t)cs.tile;`；写侧声明的 Nside 域 `aio_hips_writer.cpp:1200`「nside=2^K 且 512<=nside<=2^29」；HEALPix Npix=12·4^K（sampler.cpp:694/:739 自用该式）。
- 现状（引原文）：配置层对阶的唯一约束是 `0..29`；本文件与 sampler 侧都没有把 `leaf_shift` 的 9 阶预算从可配阶数里减出来。
- 差在哪：leaf 层 Nside = 2^(order+9) ⇒ `order ≥ 21` 越出写侧声明的 2^29 上界；`order ≥ 23` 时移位量 ≥32 作用于 32 位 `1u` ⇒ C++ 未定义行为（x64 实践中按 mod 32 回绕 ⇒ Nside 取成 2^(order−23) ⇒ 每个 control 的 ra/dec 静默算到别的天区）；`order ≥ 14` 时 tile ipix 上界 12·4^14 ≈ 3.22e9 > INT32_MAX ⇒ `cs.tile` 截断为负 ⇒ `:1211` 发布的 `tile_ipix` 变成符号扩展的巨大值，且第二遍 `tile_cells` 的 `int` 键在不同 tile 间碰撞 ⇒ Stage C 的局部背景 B/S 跨 tile 混合。
- 后果：`controls[].ra_deg/dec_deg`、`leaf_ipix`、`tile_ipix` 三处对外产品字段错值，且 rc=0（不是判红是静默错）；方向钉死为**偏绿**。
- 定级：S2（域不一致与 UB 已 CONFIRMED，但缺 order≥14 的实际生产用例 ⇒ 不升 S1）。
- 怎么算修好：配置层上限改 `29 − leaf_shift = 20`（或按 ALG 面显式声明"leaf 层 Nside 预算"并在校验里体现）；`sampler.cpp:777` 用 `1ull <<`；`CellStat::tile` 改 `std::uint64_t` 并以 `~0ull` 作哨兵（同时改 `:745`、`:1003-1005` 的 `std::map<int,…>` 键）；文档 DATA_SEMANTICS 的 target_order 域行同步。
- 置信：**CONFIRMED**（域与移位语义可判定；缺生产用例部分已注明）。

### CR-48-14 · 同一个"σ 下限"概念在本仓有两套值（UPM 侧 1e-3、采样侧 1e-12），且 UPM 侧地板拦不住采样侧产出的伪 control_ivar
- 轴：C（主）＋R
- 位点：`stage2_common.cpp:169` `cfg->sigma_floor = m.value("sigma_floor", 1e-3);`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:1973`「sigma_floor | 1e-3 | :191」（有登记）；消费点 `upm.cpp:292-293`、`:762`、`:779`、`:1022`、`:1244`、`:2438` 全为 `std::max(|uncertainty| 或 sqrt(1/ivar), cfg.sigma_floor)`；`docs/algorithms/PHASE2_SAMPLER.md:245`「1e-12 只是避免 σ_bg² 归零的数值保护量，其量纲随 σ_bg 标度而变，**不是**最小可分辨方差、**不是**读出噪声下限」。
- 现状（引原文）：`p2_upm_raw_weight` 的 production 分支（`upm.cpp:1981-1986`）直接取 `obs->control_ivar`，**不经过 sigma_floor**；因此采样侧由 1e-12 得到的 `control_ivar = 1.314e26`（CR-48-01）在本管道里没有任何一道地板能吸收它，而同一"σ 不能为 0"的语义在 UPM 侧被表达成 1e-3。
- 差在哪：两道地板相差 9 个数量级且各自服务同一个"退化尺度"场景；条文未规定二者同源，实现面也确未同源。
- 后果：CR-48-01 的伪权重直达归一化；对外"σ 有下限保护"的说法在权重面不成立（只在 robust 尺度面成立）。
- 定级：S3（本条自身不改产物，是 CR-48-01 的缓解失效说明；与 CR-48-01 同批处置）。
- 怎么算修好：把"零尺度即无信息"表达成 control_ivar=0（而非在下游补第三道地板），或在 `p2_upm_raw_weight` 对 `civar > 1/sigma_floor²` 显式拒绝并 rc=2；同批在 DATA_SEMANTICS §25 把两处地板的关系写明。
- 置信：**CONFIRMED**。

### CR-48-15 · 错误文案称 `cpu_workers` 的 0 为 "auto"，实现把 0 走成串行；合同面则说该值来自 runtime budget
- 轴：C（主）＋R
- 位点：`stage2_common.cpp:486` `if (cw < 0 || cw > 1024) { *err = "cpu_workers 必须在 0..1024 (0=auto)"; return false; }`；`:487` `cfg->exec.cpu_workers = cw;`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:1976`「cpu_workers | =host->budget.max_workers（blocks(budget)，**禁硬编码**） | :195」；`sampler.cpp:924-927` 注释「worker 数：来自 Runtime lease(p2_session 传 cfg.cpu_workers=budget.max_workers)。无 hardware_concurrency(模块不得自行开线程); 1 ⇒ 串行 reference」＋ `:927` `const int workers = (cfg.cpu_workers > 0) ? cfg.cpu_workers : 1;`；AGENTS.md §6「不硬编码线程/ISA/block：由 benchmark 生成的 profile 决定」。
- 现状（引原文）：`tools/stage2.cpp:281` `sccfg.cpu_workers = cfg.exec.cpu_workers;` 把配置值原样送进采样器；本文件与 stage2 工具路径上都没有把 0 解析成 lease/budget 的代码（合同所述的 `host->budget.max_workers` 来源在 scheduler 路径，见 `module_adapters.cpp`）。
- 差在哪：写 "0=auto" 的文案与"0⇒workers=1"的实现不一致；同一字段在两条生产入口（stage2 工具 vs scheduler 节点）的来源不同（config 直取 vs budget），合同只登记后者。
- 后果：对外并行度/性能主张（以及"每 worker 结果逐位一致"的并行一致性门）在用户按文案写 0 时实为串行运行 ⇒ 报告的性能数与用户配置意图不符；无错值风险（串行是参考实现）。
- 定级：S3。
- 怎么算修好：文案改 "0=串行参考实现（生产由 scheduler lease 注入）"或在本函数把 0 解析为 ExecutionOptions 的 auto 值（与 `default_execution_options()` 同源）；DATA_SEMANTICS:1976 补一句两入口的来源差异。
- 置信：**PARTIAL**（缺"stage2 工具路径另有 lease 注入点"的反证；检索式 `git grep -n "cpu_workers" -- lib/algorithms/coverage/tools lib/infrastructure/scheduler/src/module_adapters.cpp`）。

### CR-48-16 · `tolerance` 合同明写"无 config 覆盖键"，实现却读 `model.tolerance` 并透传生效
- 轴：C（主轴，合同 vs 实现）
- 位点：`stage2_common.cpp:156` `cfg->tolerance = m.value("tolerance", 1e-6);` ＋ `:546` `mcfg.tolerance = cfg.tolerance;`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:1972` 逐字「| tolerance | 1e-6（**无 config 覆盖键**） | :188 |」；同表相邻两行却声明可覆盖：`:1970` huber_delta「（upm.huber_delta 可覆盖）」、`:1971` max_iterations「（upm.max_iterations 可覆盖）」⇒ 合同对三个相邻字段刻意区分了可覆盖性，而实现按同一方式统一覆盖。
- 现状（引原文）：`m.value("tolerance", 1e-6)` 取值后无任何拒绝/告警，直接进 `P2UpmBuildConfig.tolerance`；IRLS 收敛判据用它（`upm.cpp:292` 附近及 `max_irls` 循环）。
- 差在哪：合同声明的"不可覆盖"在实现面不成立（反方向的同类已登记缺陷见 DATA_SEMANTICS.md:730 DISP-PSF-003「maxIter/tolerance 模块侧死参数（编排配置 psf.max_iterations/tolerance **不生效**）」——那里是"说能覆盖却不生效"，这里是"说不能覆盖却生效"）；并且该键同样受 AGENTS.md §6「不在 main 之外改默认容差」与"不动默认容差"的纪律约束：配置即可改，无门。
- 后果：两次同 manifest 的运行若 config 文本不同（tolerance 不同）会得到不同收敛点，而合同读者依 :1972 认为该量不可能被配置改变 ⇒ 复现性/口径唯一性主张失真；影响 IRLS 停止位置（迭代提前/推后），不改权重定义。
- 定级：S2。
- 怎么算修好：二选一并同批同步——①parser 删该键（出现即按退役/未知键处理）；②DATA_SEMANTICS:1972 改「1e-6（model.tolerance 可覆盖）」并在 CONFIG_SCHEMA 登记域；同时给 tolerance 加值域校验（现行 :156 无校验，0/负值/1e-30 均可进 ⇒ 判据退化）。
- 置信：**CONFIRMED**。

### 2.3 不报的核过项（附检索式）

- `memory_limit_mb=24576` 我一度怀疑"预算高于物理内存 ⇒ 护栏恒不触发"；核后不报：下游 `p2_block_plan` 带 `safety_factor=0.75`（`docs/algorithms/PHASE2_MOSAIC_WRITE.md:103-104`）⇒ 有效 18.4 GB，且该值有合同登记（DATA_SEMANTICS.md:1194）。检索式：`git grep -n "memory_limit_mb" -- docs lib/algorithms/coverage/configs`。
- `P2_SMOOTHING_LAMBDA_AUTO=0.1`：文件头 :25-28 已自陈与"冻结面改动须走 SCI/合同变更"冲突并已上呈（`reports/RELEASE-02/conform-fix-b.md §009`），属待裁项，本批不重复指控。
- `sky_plane_node_spacing_deg = 0.0`（0=未给出，由输入几何经 `p2_sky_plane_derive_node_spacing` 导出）与 `PHASE2_UPM §7a:185-189`「禁止在配置里留一个『默认节点间距』标定值」一致 ⇒ 合规形态，不报。
- `p2_acr_block_eligible` 恒 `return false`（:511-528）：与 ASTROCS_DESIGN §2「纯 CPU 生产，ACR 生产不可达」一致，注释明确这是唯一权重路径的推论而非"关掉开关"；按 §4（合同已声明退役）不报。
- `exec.gpu_route` 接受 `"cuda"`（:496）而 ACR 块恒不可达 ⇒ 用户意图被静默降级为 CPU。属"未贯穿/文案面"问题而非错值，且我未查完 scheduler 侧是否另有 ACR 实装点 ⇒ 不报为 finding，列入"需要实跑才能定"。
- `:217-225` 的旧 rejection 别名拒绝、`:275-276` 的 `wbpp_current` alias 规范化、`:321-324` 的 normalization alias 规范化：均为显式 fail-closed／规范化，与条文一致，不报。
- `:292-315` 的 underdetermined_n 缺键路径（向 `p2_reject_plan_resolve` 查询而非复制字面量）：正是 CONFORM-FIX-B-008 要求的"默认值单一来源"，实现与注释一致，不报。
- `struct 默认 == parser 默认` 的 15 项逐值核对全部一致（见处置表末行）⇒ 无 CONFORM-FIX-B-009 型分叉，不报。

## 3. `lib/algorithms/coverage/tests/rejection_nonfinite_weights_test.cpp` —— 已读（189 行全文）

### 3.1 §2.5 数值处置表（19 行）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :24-25 | kNaN / kInf | quiet_NaN / +inf | 注入用非有限哨兵 | 结构性不适用 | IEEE 特殊值本身不含科学语义；注入面选值（未含 −Inf，判据式 `!isfinite` 对 ±Inf 同结果 ⇒ 不损失判别力） | — | CONFIRMED |
| :30 | `plan.minimum_n` | 3 | 手工 plan 的最小样本门 | 待确认 | 夹具自订值；权威档界在 `rejection.h:230-234`（profile/request 规则）与 `REJECTION_ALGORITHMS.md` 的档表，本处不经 `p2_reject_plan_resolve` 而是硬填 | 更小⇒更不易判 UNDERDETERMINED（判据更容易落到 status 比较） | CONFIRMED |
| :31 | `plan.underdetermined_n` | 2 | 手工 plan 的欠定档 | 待确认 | 同上；`rejection.h:230-234` 记「wbpp/adaptive=2」⇒ 本值与对照档一致，但生产默认档是 `astrocs_adaptive_pixel`=3（`stage2_common.cpp:266-274`）⇒ 夹具与生产默认档不同值 | 更小⇒更少 stack 被判欠定（偏绿） | CONFIRMED |
| :33 | `plan.normalization` | NONE(0) | RCR 的配置门要求 | 文献值 | 注释 :32 指 `p2_reject_stack_ex` 配置门；`stage2_common.cpp:364-368`「rcr 必须 normalization=none（官方 oracle 原始值域冻结）」同口径 | — | CONFIRMED |
| :34 | `plan.normalization_floor` | 1e-12 | 归一化尺度地板 | 待确认 | `rejection.h:214`「MEDIAN_SCALE 的最小 \|median\|（默认 1e-12）」只登记值不给推导；与 `stage2_common.cpp:333` 同值（两处字面量） | 更小⇒归一化更激进放大（本夹具走 NONE 故不影响判据） | CONFIRMED |
| :35 | `plan.rcr.technique` | 0 | RCR 技术选择 | 结构性不适用 | 枚举基数选择；`stage2_common.cpp:427-432` 记「只支持 ss_median_dl（V15 冻结）」⇒ 0 即唯一在册技术 | — | CONFIRMED |
| :43-44, :63-64, :83-84, :103-104, :133-134, :162-163 | 夹具 values/weights 字面量 | 1.0,1.01,0.99,1.02,0.98 / 1.0… | 夹具样本内容 | 结构性不适用 | 夹具数值不参与判据（判据只看 status 与计数）；按 §4「测试期望值与夹具内容差」不作 finding | — | CONFIRMED |
| :49, :69, :89 | `stack.data_type` | 1 | 声明 fp64 源 | 结构性不适用 | `rejection.h` 注「1=fp64（仅诊断）」⇒ 诊断标记，不改判据 | — | CONFIRMED |
| :114, :144, :176 | `in.support_threshold` | 0.0 | 合格支掌门 | 待确认 | `rejection.h:318`「support > 该值才合格（默认 0.0）」有登记无推导；**本夹具三处全用 0.0 且 `support` 指针留空** ⇒ 该门在本文件恒不被执行（见 CR-48-18） | 传 nullptr 时判据链直接跳过 support 档 | CONFIRMED |
| :110-113, :140-143 | `in.valid` / `in.quality` / `quality_flags_required` | 未设（nullptr / 0） | 其余两条资格链 | 结构性不适用（夹具选择） | `rejection.h:319-321` 注「可空（不检查）」⇒ 本文件只走 finite 链；`invalid_valid`/`invalid_quality` 两诊断从未被断言 | 见 CR-48-18（覆盖面只有条文的一半） | CONFIRMED |
| :56, :76, :96 | `ASSERT_EQ(rc, 0)` | 0 | "调用本身成功" | 文献值（合同登记） | `rejection.cpp:2019-2022` 注释：「rc=0，调用方对非 {OK,UNDERDETERMINED} 一律 hard fail」⇒ 本处 rc=0 是被设计的语义，不是弱化 | — | CONFIRMED |
| :57-58 | `EXPECT_NE(dec.status, P2_STATUS_INVALID_INPUT)` | ≠3 | 正例判据 | **偏松** | 条文本意"全有限即正常受理"，而 `P2RejectStatus` 有 8 个值（`rejection.h:107-117`）：status=7 INTERNAL_ERROR、5 INVALID_CONFIGURATION、6 INVALID_METHOD 都能让本断言通过 | 见 CR-48-19 | CONFIRMED |
| :77, :97 | `EXPECT_EQ(dec.status, P2_STATUS_INVALID_INPUT)` | 3 | 负例判据（结果级） | 文献值 | 条文 `docs/science/REJECTION.md §4(:34)/§8(:87)`（文件头 :4-6 引用）＋实现 `rejection.cpp:2043-2054` 同状态；生产侧确实按 status hard fail（`stage2.cpp:1433-1436`、`acr_kernels.cpp:178-182`） | — | CONFIRMED |
| :123 | `EXPECT_EQ(eligible_count, 3u)` | 3 | 剔除后的合格数 | 公式导出 | 4 样本含 1 个 NaN weight ⇒ `rejection.cpp:1343-1357` 的链式判据给 3；代码真按式算 | — | CONFIRMED |
| :125 | `EXPECT_EQ(eligible[2], 0u)` | 0 | 逐样本资格位 | 公式导出 | 同上（`out_eligible[i] = ok ? 1 : 0`）；位置索引 2 与被注入 NaN 的下标一致 ⇒ 判据与输入耦合 ✓ | — | CONFIRMED |
| :126, :188 | `EXPECT_GE(out.invalid_finite, 1u)` | ≥1 | 诊断计数下界 | **偏松** | 同夹具里非有限样本数恰好是 1（:126）与 1（:188），却断 `>=1` 而非 `==1`；若资格层把同一样本重复计数或连带误计其它样本，本断言不红 | 见 CR-48-19 | CONFIRMED |
| :127-128, :155-156 | 紧凑后 `isfinite(out_weights[i])`／`EXPECT_DOUBLE_EQ(out_weights[i], weights[i])` | — | 输出权重校验 | 公式导出 | 正例逐值等值（:156）✓；**但两例都未校 `out_values` 的紧凑对齐**（见 CR-48-20） | — | CONFIRMED |
| :171-173, :175, :177 | `value_stride`/`weight_stride`=1、`pixel`=0、`value_dtype`=0 | 1/1/0/0 | strided 收集器夹具参数 | 结构性不适用 | 步长/下标/dtype 枚举选择；`value_dtype=0` 对应 fp32 源（`rejection.h` 收集器注释） | 步长恒 1 ⇒ 本文件未覆盖非连续（真跨帧 frame-major）布局，而生产正用它（`stage2.cpp:1358`） | CONFIRMED |
| :186 | `EXPECT_EQ(eligible_count, 2u)` | 2 | 3 样本含 1 个 +Inf weight | 公式导出 | 同 :123 推理 ⇒ 2 ✓ | — | CONFIRMED |

### 3.2 findings

### CR-48-18 · 本文件自称是 REJECTION §4/§8「support/weights 有限性」的共址回归门，但条文的 support 半条在本文件零覆盖，而实现面对 support 做的不是"有限性"判据而是阈值判据（+Inf 支掌照样合格）
- 轴：D（主，条文覆盖声明与实际覆盖面）＋R
- 位点：文件头 `:4-6` `docs/science/REJECTION.md §4（:34）「support/weights 有限性在资格层校验，非有限 ⇒ INVALID_INPUT hard fail」`；`:10-12`「非有限 weights 必须在资格层被判不合格」；夹具侧 `:110-114` `P2EligibilityInput in{}; in.values = values.data(); in.weights = weights.data(); in.count = …; in.support_threshold = 0.0;`（`in.support` 从未赋值 ⇒ nullptr）
- 上位依据：`docs/science/REJECTION.md §4`/`§8` 原文即文件头所引（"support/weights"）；`docs/algorithms/REJECTION_ALGORITHMS.md` 与 `ENGINEERING_SPEC.md §8`（文件头 :8 引「每项检查有正例与负例，能红能绿」）。
- 现状（引实现原文，`rejection.cpp:1349-1355`）：
  ```
  if (!std::isfinite(values[i])) { ++*out_finite; ok = false; }
  else if (valid != nullptr && !valid[i]) { ++*out_valid; ok = false; }
  else if (weights != nullptr && !std::isfinite(weights[i])) { ++*out_finite; ok = false; }
  else if (support != nullptr && !(support[i] > support_threshold)) { ++*out_support; ok = false; }
  ```
  ⇒ support 走的是 `support > threshold` 的**阈值**判据：`support = +Inf` 时 `Inf > 0.0` 为真 ⇒ **判合格并进入候选栈**；`support = NaN` 时因比较为假而被剔除，但归入 `invalid_support` 而非条文所说的"非有限 ⇒ INVALID_INPUT"。本文件三处夹具都把 `support` 留空（nullptr ⇒ 整条跳过）且 `support_threshold` 恒 0.0，因此这条链与 `invalid_support`、`invalid_valid`、`invalid_quality` 三个诊断计数在本门内**从未被执行也从未被断言**。
- 差在哪：条文要求资格层校验 support 的"有限性"，实现校验的是"大于阈值"，门只测了 weights（和 values 顺带被 :56 的有限判覆盖）；结果是一条被条文宣布"hard fail"的路径（+Inf support）在实现面畅通、在门面不可见。方向钉死：+Inf support 被当有效 ⇒ 该样本进入加权积分（support 还用于 `p2_integrate_pixel` 的 canonical max-support reducer，`eng/tests/unit/p2_output_semantics_test.cpp:63-68` 显示 support 会写进产品字段）⇒ 产品 `support` 面可被 Inf 污染，偏绿。
- 后果：对外"非有限输入在资格层被拦"的主张只对 weights/values 成立；support=Inf 的产品面（每像素 support 与依赖它的归一化 `area = sup × A_cell`，见 `lib/algorithms/coverage/hips_p2/memory.md:50`）可携带非有限值。
- 定级：S2（缺"+Inf/+NaN support 在真实生产像素栈中出现"的实测证据；条文与实现的判据不一致本身 CONFIRMED）。
- 怎么算修好：①`rejection.cpp` 的 support 档前加 `!std::isfinite(support[i])` 判（归 `invalid_finite` 或新计数），与条文"有限性"口径对齐；②本门补 support 的正/负例（`in.support` 传 NaN、+Inf、0、>阈值 四档，断言 `invalid_support`/`invalid_finite` 与 `eligible_count` 精确等值）；③同批核对 `docs/science/REJECTION.md §4` 是否需要把 support 的语义写成"有限性 ∧ 阈值"两面，以免改实现被判"与条文不符"。
- 置信：**CONFIRMED**（实现判据与夹具覆盖面逐字读码；`git grep -n "invalid_support" -- lib eng` 除 `rejection.h`/`rejection.cpp` 自身外零测试命中）。

### CR-48-19 · 正例判据取"不等于 INVALID_INPUT"而非"等于 OK"，诊断计数取"≥1"而非"==1"——同一文件的判据在负例侧紧、在正例侧松
- 轴：R（主，判据弱化⇒可绿不可红）
- 位点：`:57-58` `EXPECT_NE(dec.status, P2_STATUS_INVALID_INPUT) << "全有限 weights 被误判为 INVALID_INPUT";`；`:126` `EXPECT_GE(out.invalid_finite, 1u);`；`:188` `EXPECT_GE(out.invalid_finite, 1u);`
- 上位依据：`ENGINEERING_SPEC.md §8`（文件头 :8 引「每项检查有正例与负例，能红能绿」）；`rejection.h:107-117` 定义 8 个 stack status（OK=0…INTERNAL_ERROR=7）。
- 现状：全有限输入的正例只排除 status==3。若资格/kernel 层回归成 `P2_STATUS_INTERNAL_ERROR`(7)、`INVALID_CONFIGURATION`(5) 或 `ALL_REJECTED`(2)，本断言仍然绿——而按生产侧的 hard fail 语义（`stage2.cpp:1433-1436` 对非 {OK,UNDERDETERMINED} 一律 fail），这些 status 全都是**生产会炸**的结果。`invalid_finite` 侧同理：夹具里非有限样本数恰为 1，却断 `>=1`。
- 差在哪：判据方向虽不错（不会把红判成绿的"恒真"），但对"正例被污染"这一类回归不敏感，属于"门只有一半判别力"。
- 后果：非有限权重修复被别的回归顶掉时，本门不报 ⇒ 该 M4-C-06 共址门对"权重有限性回归"的持续保护力被高估。
- 定级：S3。
- 怎么算修好：正例改 `EXPECT_EQ(dec.status, P2_STATUS_OK)`（夹具 n=5、minimum_n=3、underdetermined_n=2 ⇒ 应为 OK），或至少 `EXPECT_TRUE(status==OK || status==UNDERDETERMINED)`；`EXPECT_GE(invalid_finite,1u)` 改 `EXPECT_EQ(invalid_finite,1u)`，并补 `invalid_valid==0`、`invalid_support==0`、`invalid_quality==0` 的等值断言。
- 置信：**CONFIRMED**。

### CR-48-20 · 资格层输出只校权重不校值：紧凑化后 `values↔weights` 的对应关系与 `source_indices` 均无断言（可错位仍全绿）
- 轴：R（主，门判不到）
- 位点：`:117-120` `out.values = out_values.data(); out.weights = out_weights.data(); out.eligible = eligible.data(); out.eligible_count = &eligible_count;` 与判据侧 `:127-128` `for (std::uint32_t i = 0; i < eligible_count; ++i) EXPECT_TRUE(std::isfinite(out_weights[i]));`（`out_values` 从未被断言）；gather 件 `:182` `out.source_indices = source_indices.data();` 与 `:186-188`（`source_indices` 内容从未被断言）
- 上位依据：`rejection.h:327-331` 逐字「double* values; // 输出（容量=count；**合格值紧凑写入**）／double* weights; // 输出（容量=count；输入 weights 为 null 时保持未写，调用方按等权处理）」——紧凑化契约对两个数组同时成立，其正确性即"下标对齐"；实现侧 `rejection.cpp:1360-1364` `if (ok) { out_vals[cnt] = values[i]; if (weights != nullptr && out_weights != nullptr) out_weights[cnt] = weights[i]; ++cnt; }`。
- 现状：本门对 `p2_eligibility_filter` 只断 `eligible_count`、`eligible[2]`、`invalid_finite>=1` 与"输出权重有限"；对 `p2_collect_candidate_stack` 只断 `eligible_count==2` 与 `invalid_finite>=1`。
- 差在哪：可照做的变异体——把 `out_vals[cnt] = values[i]` 写成 `out_vals[i] = values[i]`（或反过来只紧凑 weights），本文件 7 个用例**全部仍绿**（权重侧全有限、计数不变），而生产会把"某帧的值"配"另一帧的权重"送进加权积分 ⇒ 像素值错。`source_indices` 的存在正是为了事后对齐可审计（`hips_p2/memory.md:48` 「p2_collect_candidate_stack :1084-1098/:1330-1353（source_indices …）」），门却把它当未用输出。
- 后果：本门被登记为"资格层非有限判据的共址回归门"（:10-12），但它对资格层最危险的失效形态（紧凑化错位）无判别力；M4-C-06 的持续保护被高估。
- 定级：S2（门的判别力缺口；不直接改产物字节，但属"测不到即绿"的在册形态）。
- 怎么算修好：正例补 `EXPECT_EQ(out_values[i], 期望第 i 个合格值)` 的逐值断言（本夹具期望 `{1.0,1.01,1.02}`）；负例补"错位即红"的配对断言（`out_values[i]` 与 `out_weights[i]` 必须来自同一 `i`）；gather 件补 `EXPECT_EQ(source_indices[0],0u); EXPECT_EQ(source_indices[1],2u);`（跳下标 1 的 Inf 权重）。
- 置信：**CONFIRMED**（判据缺失与变异体存活均由读码推得，未实跑）。

### 3.3 不报的核过项（附检索式）

- 「断言的是不崩还是结果正确」：本文件是**结果级**判据——`dec.status == P2_STATUS_INVALID_INPUT`（:77/:97）、`eligible_count` 精确等值（:123/:153/:186）、`eligible[2]==0`（:125）、逐值 `EXPECT_DOUBLE_EQ`（:156），不是 `EXPECT_NO_THROW`/只看 rc 的"不崩"型断言。不报。
- 「能否红」：静态变异分析——删掉 `rejection.cpp:2043-2054` 的 finite 守卫 ⇒ status 落到 RCR kernel 的常规返回 ⇒ :77/:97 判红；删掉 `:1349-1351` 的 weights 档 ⇒ `eligible_count` 变 4 ⇒ :123 判红。故**在册的两条负例是真门**，不是恒绿。不报。
- 「判据是否读真实对象」：被测对象是生产入口本身（`p2_reject_stack_ex`/`p2_eligibility_filter`/`p2_collect_candidate_stack` 三个 `P2_API` 导出符号，`stage2.cpp:1358/:1428` 与 `acr_kernels.cpp:135/:174` 用的就是它们），不是测试专用替身。不报。
- 「rc=0 却 status=INVALID_INPUT」我一度按 fail-open 立项；核后撤销：`rejection.cpp:2019-2022` 注释与 `stage2.cpp:1433-1436`、`acr_kernels.cpp:178-182` 共同证明生产侧「对非 {OK,UNDERDETERMINED} 一律 hard fail」，status 才是硬失败载体。检索式：`git grep -n "rdec.status != P2_STATUS_OK" -- lib`。不报。
- 手工 plan 不经 `p2_reject_plan_resolve`：RCR 是配置面显式可达的方法（`stage2_common.cpp:238` `else if (method == "rcr")`），且 kernel 入口的 finite 守卫在方法分派**之前**（`rejection.cpp:2043` 早于 `:2057` 的 normalization 门）⇒ 用 RCR 不代表只测了一条非生产支路。不报。
- `:49` `data_type = 1` 与 `:177` `value_dtype = 0`：两个不同枚举的 dtype 标记（stack 侧"仅诊断"、gather 侧"0=fp32 源"），不是同一键的口径冲突。不报。

## 4. `lib/algorithms/coverage/tests/retired_object_reject_negative_test.cpp` —— 已读（151 行全文）

先给本批问题③的直接答案：**被拒的是字符串，不是枚举名**。三侧识别面都是字面 token 比较——`lib/algorithms/resample/p3_rsmp_failclosed.cpp:30` `return token == "psfsw_robust_weight";`、`lib/algorithms/calibration/src/v6_calibration_covariance.cpp:60` 同一式、`lib/algorithms/resample/p3_rsmp_failclosed.cpp:52-55` `std::find(v.begin(), v.end(), token) != v.end()`（黑名单、精确比较）；枚举面已在 PSFSW-RETIRE-03 **物理删除**（本文件 :118-119 自陈「已**物理删除**（UnitId 无其项、冻结单位表无其行；原 is_retired_unit_id 随之删除），识别面只剩字符串面」）。而本门的判据本身也是**文案子串**（见 CR-48-22）。

### 4.1 §2.5 数值处置表（8 行；本文件无数值阈值，判据面由字符串构成，故附判据字面量附表）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :96, :112 | `rec.combination_coefficients` | `{1.0}` | 校准记录夹具的系数向量 | 结构性不适用 | 夹具内容，只为让 `validate_covariance_record` 走到 variance_from 档；不参与退役判据（`v6_calibration_covariance.cpp:697-715` 的判据顺序里系数校验在其后） | — | CONFIRMED |
| :130-134 | `live[]` 现役单位枚举集 | 9 个 UnitId | "退役符号不得等于任何现役行"的遍历域 | 结构性不适用 | 枚举基数（UnitId 现役 9 项，PSFSW-RETIRE-03 删行后）；遍历全部现役项正是为了把"删行"钉住 | — | CONFIRMED |
| :56, :74, :87 | `GateConfig{}` 默认 | epsilon_corr_ratified=false、epsilon_corr=NaN、psf_sum_tol=1e-9、row_sum_tol=1e-9、bunit_tol=1e-12 | 门配置（未签收态） | 待确认（非本文件取值，属继承默认） | `p3_rsmp.h:315-323` 注释：「未签字前不得自定值（fail-closed）」「数值守卫（实现局部，不是科学阈值）」⇒ 夹具用默认即"未签收"态，与本门的语义（声明面拒绝）无关 | 若把 epsilon 签收后端行为变，本门不受影响（不触 epsilon 档） | CONFIRMED |
| :44, :85 | `P3Mode::SurfaceBrightness`、`variance_from` | "actual_combination_coefficients" | 现役方差来源正例 | 文献值 | `p3_rsmp_failclosed.cpp:36-39` 迁移提示与 `v6_calibration_covariance.h:282` 默认值同为 `actual_combination_coefficients`；DATA_SEMANTICS.md:3259 记 FZ-MODE-PRODUCTION = {point_information, surface_gls}（模式面，非来源面） | — | CONFIRMED |
| :95, :111 | `rec.representation` | "diagonal_variance" | 校准表示枚举 | 文献值 | `v6_calibration_covariance.cpp:716-718`「representation not in covariance.v1 enum」；`covariance.v1` 在册表示集 | — | CONFIRMED |
| :114 | `EXPECT_TRUE(validate_covariance_record(...))` 依赖的默认串 | propagation="C_out = R C_in R^T"、input_covariance="declared"、avail="available" | 正例能过的原因 | 结构性不适用 | 头 `v6_calibration_covariance.h:279-284` 的默认值；夹具未显式设 ⇒ 正例其实同时在钉"默认值集合"（若默认被改成非枚举值，:114 判红） | — | CONFIRMED |
| :28-30 | `contains()` 辅助 | — | 子串判据载体 | 结构性不适用（函数本身） | 但它是本门的全部判据形态 ⇒ 见 CR-48-22 | — | CONFIRMED |
| :118-119 | 「物理删除」声称 | — | 枚举面无退役对象身份 | 已由代码核实 | `v6_drizzle_science.cpp:67-76` 冻结单位表内无 psfsw 行，且兜底取表末项而非硬编码下标（:74-75 注释解释了删行会平移下标的风险）✓ | — | CONFIRMED |

判据字面量附表（本门的真正载荷；§2.5 名义上不要求，但本文件的判据 100% 由这些串决定，故列出）：

| 位点 | 断言的子串 | 断言对象 | 处置 | 备注 |
|---|---|---|---|---|
| :60 | `retired_canonical_object=psfsw_robust_weight` | resample `variance_from` 路径的 reason | 待确认（机读前缀，可接受为契约） | 该串由 `p3_rsmp_failclosed.cpp:35` `"retired_canonical_object=" + token` 生成 ⇒ 前缀＋token 是机读的，唯一硬耦合是拼接格式 |
| :62 | `not a current object` | 同一 reason 的**散文**部分 | 待确认 | 纯文案断言，见 CR-48-22 |
| :64 | `migration` | 同一 reason 的散文部分 | 待确认 | 同上 |
| :101-:103 | `retired canonical object` / `psfsw_robust_weight` / `migration` | calibration 的 `*error` 文本 | 待确认 | 对应 `v6_calibration_covariance.cpp:707-710` 的三段字面量；文案与断言双写，任一侧改写即红 |
| :142-:145 | `psfsw_robust_weight` / `not a current object` / `point_information` ∧ `surface_gls` | drizzle 的 reject reason | 待确认 | 允许面集合（point_information/surface_gls）以子串断言，与 DATA_SEMANTICS.md:3259 的 FZ-MODE-PRODUCTION 同值 ✓（这一条是**有实质约束力**的断言，因它钉的是接受集内容） |
| :127 | case-insensitive parity（仅 drizzle 侧） | `is_retired_unit_symbol("PSFSW_ROBUST_WEIGHT")` | **覆盖不对称** | 见 CR-48-21 |

### 4.2 findings

### CR-48-21 · 退役对象识别面三侧口径不一致：drizzle 侧大小写不敏感、resample 与 calibration 侧是精确串比较，而本门只对 drizzle 侧要求 parity，另两侧无任何变体负例
- 轴：C（主）＋R
- 位点：本文件 `:127` `EXPECT_TRUE(is_retired_unit_symbol("PSFSW_ROBUST_WEIGHT")) << "case-insensitive parity";`（唯一一处变体断言）；对照 `:53` `rec.variance_from = "psfsw_robust_weight";`、`:71` `rec.weight_sources = {"psfsw_robust_weight"};`、`:97` `rec.variance_from = "psfsw_robust_weight";` —— 三处负例全部只喂**原样小写串**
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:3259`「~~退役模式~~ ~~`psfsw_robust`~~ **禁用 token**（PSFSW-RETIRE-01/03；§9.73 A44）…产品声明该 token ⇒ **显式拒绝 + 迁移提示**（`FZ-MODE-RETIRED`）；**接受面 = 空**」；`docs/design/UNIFIED_MODEL.md:58`（本文件 :7 引）「旧产品若声明该对象 ⇒ 显式拒绝 + 迁移提示」；`lib/algorithms/resample/p3_rsmp.h:484-485`「旧产品若在 variance_from / weight_sources 声明该对象 ⇒ 显式拒绝 + 迁移提示，**不得静默接受**」。
- 现状（引实现原文）：
  - `p3_rsmp_failclosed.cpp:27-31` `bool is_retired_canonical_weight_object(const std::string& token) { … return token == "psfsw_robust_weight"; }`（**精确、大小写敏感**）
  - `p3_rsmp_failclosed.cpp:52-55` `token_is_forbidden_weight_source` = `std::find(forbidden_weight_source_tokens()…, token)`，列表见 :17-24（含 `"psfsw_robust_weight"`、`"psfsw"`、`"weight"` 等，同样精确比较）；`G-P3-GLB-01` 的触发条件（:95-96）只由这些**命中**或 `rec.uses_relative_weight_as_ivar`/`variance_from_weight` 布尔位构成 ⇒ weight_sources 面是**黑名单**，不在表内的串一律放行；
  - `v6_calibration_covariance.cpp:59-61` `is_retired_canonical_object` 亦 `token ==`；但 calibration 侧另有**白名单**兜底（:712-713 `if (!is_allowed_variance_from(rec.variance_from) || is_forbidden_variance_source(...)) return fail(...)`）⇒ 变体在 calibration 面仍被拒，只是落到通用 reason「variance_from invalid or forbidden」而**丢掉退役对象与迁移提示**；
  - `v6_drizzle_science.cpp:105-113` `is_retired_unit_symbol` 逐字符 `ascii_lower` 比较，且 :106 注释自称「大小写不敏感全等（**与 coverage/resample 的 token 门同口径**）」。
- 差在哪：注释声称的"同口径"为假——resample/calibration 两侧都是 case-sensitive；本文件把 parity 只钉在有 parity 的那一侧。resample 的 `weight_sources = {"PSFSW_ROBUST_WEIGHT"}`（或 `"psfsw_robust_weight "`、`"psfsw-robust-weight"`）经 :88-96 的判据链**不产生任何 Reject**，本门 5 个用例全绿；这与条文"接受面 = 空""不得静默接受"直接冲突（方向钉死：漏拒 = **偏绿**，不是偏红）。
- 后果：对外"退役对象已显式拒绝（FZ-MODE-RETIRED）"的登记主张在 resample 面只对一种拼写成立；旧产品（其 token 大小写由历史写出方决定，例如 FITS/JSON 里的大写形式）可带着退役权重对象声明通过 fail-closed 门进入 Phase3 导出。
- 定级：S2（缺"真实历史产品里存在非小写拼写"的样本证据；实现面不一致与门面无变体负例两点已 CONFIRMED）。
- 怎么算修好：①三侧统一 `ascii_lower` 归一后比较（或统一在入口 trim＋小写化 token）；②本门为**每侧**补变体负例：原样／全大写／混合大小写／带首尾空格／连字符替代下划线，各档都断言对应门产生 Reject（resample 的 variance_from 与 weight_sources 两面分别覆盖）；③同批把 `v6_drizzle_science.cpp:106` 的"同口径"注释改成与实际一致（或改正实现后保留该注释）；④若刻意只认精确拼写，则须在 DATA_SEMANTICS.md:3259 写明"按精确 token 匹配，变体不视为退役对象"，否则条文与实现对不上。
- 置信：**CONFIRMED**（三侧比较式与门条件逐字读码；变体放行由 :88-96 的判据闭包直接可推）。

### CR-48-22 · 门把"退役对象被拒"的判据绑在 reason 的散文子串上（`not a current object`／`migration`），文案与判据双写，行为不变而改文即红
- 轴：R（主，判据耦合面）＋C
- 位点：`:62-63` `EXPECT_TRUE(contains(g->reason, "not a current object")) << "reason must state retirement (not merely a generic forbidden token): " << g->reason;`；`:64-65` `EXPECT_TRUE(contains(g->reason, "migration"))`；`:101-103` `EXPECT_TRUE(contains(err, "retired canonical object")) << err; … EXPECT_TRUE(contains(err, "migration")) << err;`；`:143-144` `EXPECT_TRUE(contains(reason, "not a current object"))`
- 上位依据：这些串对应的是实现里写死的英文散文（`p3_rsmp_failclosed.cpp:35-39`、`v6_calibration_covariance.cpp:707-710`）；条文面（DATA_SEMANTICS.md:3259、UNIFIED_MODEL.md:58）规定的是「显式拒绝 + **迁移提示**」这一**语义**，未规定任何具体英文措辞。
- 现状（引原文）：门同时断 `status == Reject`（好，行为级）与 reason 的三段散文子串（脆弱，文案级）。若负责人或文档侧要求把 reason 里的条款引用从「ASTROCS_DESIGN.md 3.1」改成「§3.1」、或把「not a current object」改成中文/更准确表述，实现行为完全不变而本门判红；反过来，只要实现保留这三段串，门并不检查 reason 里是否给出了**被拒对象的机读身份之外的任何判定依据**（除 `retired_canonical_object=` 前缀与 `point_information`∧`surface_gls` 两处）。
- 差在哪：判据与被测对象**语义**的耦合松、与其**文案**的耦合紧——属本仓已识别的"断言与被测无耦合"形态的镜像（断言耦合在文本表层）。真正需要钉的两件事（状态是 Reject；机读前缀含被拒 token；允许面 = FZ-MODE-PRODUCTION 集合）里，第三件只有 drizzle 侧钉了（:144-145）。
- 后果：门对文案变更过敏 ⇒ 实际执行中会把合规改动判红、逼着改实现的人同步改测试（测试反向牵引实现措辞）；而对"resample 面 reason 里没有允许面集合（`point_information`/`surface_gls`）"这一 drizzle 面已认定必须出现的要素，resample/calibration 两面**没有**同样要求 ⇒ 三侧的"迁移提示"实质内容不等价。
- 定级：S3。
- 怎么算修好：判据只留（a）`status == Reject`、（b）机读段 `retired_canonical_object=<token>`、（c）允许面 token 集合（`point_information`、`surface_gls`）与迁移目标（`actual_combination_coefficients`）三者presence，散文措辞不 assert；并把（c）同样加到 resample/calibration 两侧（现只有 drizzle 有）。
- 置信：**CONFIRMED**。

### CR-48-23 · 本门"能红"的唯一实测证据落在 `run/`（按 AGENTS.md §7 不入库且会被轮次回收），跟踪面上无可复核材料
- 轴：C（主，证据面）＋D
- 位点：`:16` 「能红能绿：把任一模块的退役对象重新变成"静默接受"（不产生 Reject/不返回 false），本文件即失败；**mutation 实测见 `run/PSFSW-RETIRE-01/REPORT.md`**」
- 上位依据：AGENTS.md §7 目录落位速查逐字：「`run/`  临时产物/日志（gitignore，不入库）」；并见 §3「每轮开工走 `eng/tools/round_start.sh <轮次ID>`，它已带 `--prune-products`：回收未登记轮次，并清掉已登记轮次内部的 `run/<轮次>/out/` 产品树（报告/证据/results/logs 保留）」——即 `run/` 内容按设计可被回收。
- 现状：`git ls-files | grep -c "PSFSW-RETIRE-01"` ⇒ **0**（无任何跟踪文件含该轮次名）；本审查节点被禁止执行测试与 `eng/**`，故不能自行复现 mutation ⇒ 该"能红"声明在跟踪面上不可验证。
- 差在哪：不是"门无效"的证据，而是**可复核性**证据缺位：源文件注释把一个只有本机临时目录里存在过的结果当作门的健康证明。对照本仓另两处同类做法（`docs/algorithms/PHASE2_SAMPLER.md:573` 的 F3 域判据也指 `run/SCI-FIX-PHASE2-01/exp/…`，同样不在跟踪面），这是系统性的证据落位问题，不是本文件孤立缺陷——但本文件是**唯一**把 mutation 证据写在源码注释里的一个（其余写在文档），影响是读代码的人会以为有留档。
- 后果：负责人或后续审查无法在不重跑的情况下确认这 5 个用例真能红；若 mutation 从未做（我无法证伪），则 :15-16 是一句无据自评。
- 定级：S3。
- 怎么算修好：把 mutation 记录（注入点、注入前后 status、日期）落 `artifacts/evidence/` 或 `实验/` 并在注释里指过去；`eng/ci/checks.json` 若已有对应 mutation-selftest 门（本文件没有 `--self-test` 面，因为它是 C++ gtest），则补一条"注入静默接受 ⇒ 该 ctest 判红"的可执行负例并登记。
- 置信：**CONFIRMED**（跟踪面为零可判定；mutation 是否真做过属 UNPROVEN，已如实标注）。

### 4.3 不报的核过项（附检索式）

- 「门只抓得住文案、抓不住状态」我一度这样立项；核后撤销：`:59`/`:77` 与 `:99` 分别断 `Status::Reject` 与 `EXPECT_FALSE(validate_…)`，即**状态级**判据在册，文案只是附加耦合（已并入 CR-48-22 而不是另立一条）。
- 「同一生产 TU 被编译两遍，门可能测的是另一份副本」：`lib/algorithms/coverage/CMakeLists.txt:169-170` 直接把 `v6_drizzle_science.cpp`/`v6_calibration_covariance.cpp` 编进本 target，而两文件也分别属 `eng/tests/unit/v6_p1_cal/CMakeLists.txt:16-17`、`eng/tests/unit/v6_p1_drz/CMakeLists.txt:26` 与 `lib/algorithms/integration/v6*/CMakeLists.txt`；核对差异：唯一带 `target_compile_definitions` 的生产面是 `lib/algorithms/calibration/CMakeLists.txt:59-62`（`ASTROCS_ABI_SHARED=1`、`ASTROCS_ABI_EXPORTS=1`、`AC_API=`，纯 ABI/可见性宏，不改 `validate_covariance_record` 行为）⇒ 副本等价，不报。检索式：`git grep -n "target_compile_definitions" -- eng/tests/unit/v6_p1_cal lib/algorithms/calibration lib/algorithms/coverage`。
- 正例（:83-90、:109-115）确为真判据：`find_gate(...)==nullptr` 与 `EXPECT_TRUE(validate_…)` 在实现把"退役对象"判据改成"任何 variance_from 都拒"时都会红 ⇒ 非恒真门。不报。
- `:135-139` 对 9 个现役 UnitId 的 `EXPECT_STRNE`/`EXPECT_FALSE` 双断言，钉的是"删行后无现役项映射到退役符号"，与 `v6_drizzle_science.cpp:74-75` 的表长兜底一致 ⇒ 有判别力。不报。
- `sb_record()` 用 `SurfaceBrightness` 模式，`check_failclosed_all` 在同一次调用里也会因 bunit 二次律等其它条件产生别的 Reject（默认 ProductRecord 的 bunit 字段为空）；本门用 `find_gate` 按门号取自己关心的那条 ⇒ 不受其它门噪声影响。不报。
- 「calibration 侧变体拼写会被白名单兜住」已在 CR-48-21 里作为部分缓解写明（`v6_calibration_covariance.cpp:712-713`），故 calibration 侧的后果只是丢失可诊断 reason，不是静默接受；据此把那侧的定级压低、只对 resample 侧主张"静默接受"。不另立条。

## 5. `lib/algorithms/coverage/tests/routing_test.cpp` —— 已读（108 行全文）

对本批问题⑤的直接回答：**本件不覆盖排异方法的路由表**。件内四处断言分别是 parse+build 共享路径（:25-36）、worker 预算（:38-51）、ACR 块资格（:58-78）、legacy 权重模式 token 拒绝（:84-108）；夹具 :20 用显式 `"method":"sigma"` 从而**绕开 AUTO**，全件对 `p2_reject_plan_resolve` / `astrocs_n_map_method` 零断言（检索式：`git grep -n "plan_resolve\|n_map_method\|WINSORIZED\|PERCENTILE" -- lib/algorithms/coverage/tests/routing_test.cpp` ⇒ 零命中）。真实路由表在 `lib/algorithms/coverage/src/rejection.cpp:1272-1288`，且 `:1282-1284` 注释自陈生产档与对照档「**M3 后不再同表**」⇒ 见 CR-48-26。

### 5.1 §2.5 数值处置表（18 行）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :16 | `inputs.hips` | `["a","b","c"]`（3 帧） | 夹具帧数 | 结构性不适用 | 满足 `stage2_common.cpp:15` 的 ≥2 下界即可；3 也是 `stage2_real_3frame.json` 的在册规模 | — | CONFIRMED |
| :17 | `control_grid_per_tile` | 8 | 网格边数 | 文献值（合同登记） | 与 `stage2_common.cpp:40` 默认、`sampler.cpp:304`、`DATA_SEMANTICS.md` §25 同值；在册 config `stage2_real_3frame.json` 亦为 8 | — | CONFIRMED |
| :17 | `robust_loss` | "huber" | 退役/校验即弃键的在册写法 | **钉住不一致** | 见 CR-48-12 关联；`PUBLIC_API.md:2208` 称键不存在，而此夹具与生产 config 模板都在设它 | 改"出现即拒"⇒ 本件 :28 判红（CR-48-27） | CONFIRMED |
| :17 | `snr_weight_mode` | "snr2_normalized" | 同上 | **钉住不一致** | 同上 | 同上 | CONFIRMED |
| :18 | `memory_limit_mb` | 8192 | 夹具内存预算 | 待确认（夹具选择） | 合同默认 24576（`DATA_SEMANTICS.md:1194`）；在册 config 用 12288/8192/4096 各档 ⇒ 本值属夹具自订，不改判据 | 更小⇒块规划更保守（chunk 更小）；本件不跑块规划 | CONFIRMED |
| :19 | `acr_route` | "cpu" | 夹具路由 | 结构性不适用（夹具选择） | 允许域 auto/cpu（`stage2_common.cpp:469-472`）；**注意 struct 默认是 "auto"**（`stage2_common.h:163`）⇒ 夹具显式避开默认，见 CR-48-25 | 用默认 "auto" 时本件的 ACR 断言仍全绿（谓词恒 false），但 `requested_route` 与 `fallback_reason` 路径不同（`stage2.cpp:990`）| CONFIRMED |
| :20 | `rejection.method` | "sigma" | 显式方法（绕开 AUTO） | 结构性不适用（夹具选择） | 映射 `stage2_common.cpp:229`；选它的后果见开头段与 CR-48-26 | — | CONFIRMED |
| :20 | `underdetermined_n` | 2 | 欠定档 | 文献值（对照档默认） | `rejection.h:230-234` 冻结「wbpp/adaptive=2；astrocs_adaptive_pixel=3」；夹具显式写 2 而生产默认档（`stage2_common.cpp:272-274`）是 astrocs_adaptive_pixel ⇒ 夹具与生产默认档不同值 | 更小⇒更少 stack 判欠定（偏绿） | CONFIRMED |
| :20 | `lower_sigma`/`upper_sigma`/`max_iterations` | 4.0 / 3.0 / 8 | robust_mad_clip 阈值表 | 文献值 | `REJECTION_ALGORITHMS.md:157`「阈值冻结：sigma/…4.0/3.0/8」＋:181「Project-defined 冻结值（§12），文献不提供门值」＋`CONFIG_SCHEMA.md:46` | — | CONFIRMED |
| :30 | `input_manifest_hash` | "deadbeef" | 透传哨兵 | 结构性不适用 | 夹具标记串，:33 断透传不丢 | — | CONFIRMED |
| :31, :32 | `m.robust_loss`/`m.snr_weight_mode` | 0 / 0 | 期望值＝struct 默认 | **钉住"校验即弃"** | `stage2_common.h:53-54`（两者默认 0）＋`stage2_common.cpp:157-168`（parser 从不赋值）⇒ 本断言实际证明"配置串没生效"，而不是"路由正确" | 见 CR-48-27 | CONFIRMED |
| :30 | `p2_stage2_make_upm_cfg(cfg, cfg.target_order, …)` | cfg.target_order（夹具未设 ⇒ **−1**） | 传给 UPM 的阶 | **与合同面不一致** | `DATA_SEMANTICS.md:1977`「target_order = **=cov.target_order（coverage 实测值透传）**」；生产 `stage2.cpp` 传 cov 值；本件传未解析的哨兵 −1，且**不断言** `m.target_order` | 若 make_upm_cfg 丢掉该透传，本件无感（缺断言）；−1 入 UPM 的域校验会判非法，但本件不跑 build | CONFIRMED |
| :42 | `execution` 块 | cpu_workers=4、io_workers=2、gpu_route="cpu"、deterministic=false、memory_budget_bytes=4096 | worker 预算夹具 | 混合 | 显式值（非 0）⇒ 本件**从未测 0=auto 这一被文档承诺的形态**（`execution_options.h:14`「cpu_workers=0 表示 auto」），而 auto 恰是唯一在多处有不同解析口径的取值（CR-48-24）；memory_budget_bytes=4096 与 io/cpu 比亦属夹具自订 | 见 CR-48-24 | CONFIRMED |
| :44-46, :50 | 期望 worker 数 | 4 / 2 / "cpu" / false / 6 | accessor 回读 | 公式导出 | `execution_options.h:28-36`：显式>0 时原样返回 ⇒ 断言与式一致 ✓（但见 CR-48-24：断的是 accessor 不是分发点） | — | CONFIRMED |
| :63, :69, :77 | `p2_acr_block_eligible(cfg, true, P2_REJECT_SIGMA, false)` 等 | acr_registered=true/false、method=SIGMA/AUTO、large=false/true、route∈{cpu,auto,cuda} | 穷举组合 | 结构性不适用（组合穷举本身） | 被测谓词把四参全部 `(void)` 丢弃并 `return false`（`stage2_common.cpp:523-527`）⇒ 24 次断言的信息量＝1（见 CR-48-25） | — | CONFIRMED |
| :86 | legacy token 集合 | auto/ivar/equal/support_x_snr2/snr2/"" | 被拒 token 值域 | 文献值 | 注释 :85「原 auto/ivar/equal/support_x_snr2 四值域」与 `stage2_common.cpp:442-444` 的自述一致；额外两值（"snr2"、""）是加严 ✓ | — | CONFIRMED |
| :96 | 整数形态 | 0/1/2/3 | legacy 整数域 | 文献值 | `stage2_common.cpp:442-443`「映射为整数域 {2,2,1,0}」＋3 为越界加严 ✓ | — | CONFIRMED |
| :93 | `EXPECT_NE(err.find("§9.73"), npos)` | 子串 "§9.73" | 错误文案判据 | 待确认 | 断的是 reason 文本含条款号（`stage2_common.cpp:448` 的实现串含「已按 §9.73 裁决 A44 删除」）⇒ 文案改则红，行为不变；与 CR-48-22 同形态 | — | CONFIRMED |

### 5.2 findings

### CR-48-25 · ACR 资格用例的"穷举组合"零判别力：被测谓词丢弃全部入参恒返回 false，而真正会出事的回归形态（调用点不再咨询该谓词）本门全绿
- 轴：R（主，恒真/恒假断言无证据资格）＋D
- 位点：`:64-74` 「穷举路由与 reject method 的组合：没有任何组合能进 ACR 块」的三重 for（route×method×large，共 12 次 `EXPECT_FALSE`）＋`:75-77` 「ACR 未注册时同样不可进入（**对照组，证明判据非退化**）」；被测 `stage2_common.cpp:523-527` `(void)cfg; (void)acr_registered; (void)reject_method; (void)large_scale_active; return false;`
- 上位依据：本派单与 §0 的要求"恒真门没有证据资格"；`docs/science/PHASE2_UPM.md`／ASTROCS_DESIGN §2「纯 CPU 生产，ACR 生产不可达」（`stage2_common.cpp:519-520` 自引）；`eng/tests/api/test_p2_api.py:14` 把 `p2_acr_block_eligible` 列在导出符号面、`eng/ci/prod_wiring_baseline.json:174` 已登记 `"unreachable:manifest.source_symbols:p2_acr_block_eligible"`。
- 现状（引原文）：谓词与入参无函数关系 ⇒ 12 组输入与 2 组对照（registered=true/false）给出同一值，"对照组"并不对照任何东西。调用点确实在产（我核到 `lib/algorithms/coverage/tools/stage2.cpp:971-973` `const bool use_acr_block = p2_acr_block_eligible(cfg, acr_reg != nullptr, rplan.method, large_scale_active);` —— 我最初的 `git grep … | head` 截断曾让我误判为"无调用点"，已用第二次读码订正，此点如实记录），但本门不对调用点做任何断言。
- 差在哪：两类真实回归本门都抓不到——①`stage2.cpp:971` 被改成 `use_acr_block = (rplan.method == P2_REJECT_SIGMA && !large_scale_active && acr_reg)` 之类"按条件放行"（谓词一字未动 ⇒ 门全绿，而 ACR/CUDA 路径进生产）；②谓词被保留但调用点被删（`use_acr_block` 硬编 true/false 漂移）。门只钉住了"这个函数返回 false"这一条实现细节。
- 后果：对外"ACR 生产不可达"目前由一个未被验证的分发点支撑；`stage2.cpp:1947-1948` 写出的 `acr_requested_route`/`acr_effective_route` 诊断字段是唯一能在产物面看到路由结果的面，而本门不读它（也不跑 stage2）。
- 定级：S2（不报 S1：现行分发点确实咨询了恒 false 谓词，故生产未错值；缺陷在门的判别力与自评）。
- 怎么算修好：①把断言改成"表驱动＋分发点"两面：在册断 `stage2.cpp` 的 `use_acr_block` 由何式给出（可加一条集成面断言：跑一次 mini mosaic，读 diagnostics 的 `acr_effective_route == "cpu"` 且 `ACR block: enabled=0` 日志/字段）；②若坚持恒 false，则删掉 :57 的「能红能绿」自评与 :64 的"穷举"表述（它们暗示了一个不存在的覆盖面），并在 `stage2_common.cpp:515-522` 注明"ACR 不可达由分发点咨询恒 false 谓词保证，验证在集成门"；③`P2_REJECT_SIGMA`/`AUTO`/`cuda` 这些入参既然不参与判定，就别在测试里伪装成维度。
- 置信：**CONFIRMED**。

### CR-48-24 · 本件自称"验证 worker 预算在到达模块前保持正确"（:3），实际只断 accessor 回读，未断任何分发点；`cpu_workers=0`（=auto）这一被文档承诺的形态零覆盖，而采样器恰好在分发点拿原始值
- 轴：C（主）＋R
- 位点：`:42` `j["execution"] = {{"cpu_workers",4},{"io_workers",2},…}`；`:44` `EXPECT_EQ(effective_cpu_workers(cfg.exec), 4);`；`:49-50` `cfg.exec.cpu_workers = 6; EXPECT_EQ(effective_cpu_workers(cfg.exec), 6);`
- 上位依据：`lib/algorithms/coverage/include/astro/phase2/execution_options.h:14`「唯一执行预算对象。cpu_workers=0 表示 auto（=default_cpu_workers）」、`:16`「int cpu_workers = 0; // 0 => max(1, hardware_concurrency)」、`:23-30` `default_cpu_workers()/effective_cpu_workers()`；`docs/contracts/DATA_SEMANTICS.md:1976`「cpu_workers | =host->budget.max_workers（blocks(budget)，禁硬编码）」；`sampler.cpp:925-927`「worker 数：来自 Runtime lease(p2_session 传 cfg.cpu_workers=budget.max_workers)。无 hardware_concurrency(模块不得自行开线程); 1 ⇒ 串行 reference」＋`const int workers = (cfg.cpu_workers > 0) ? cfg.cpu_workers : 1;`
- 现状（引原文）：同一字段的四个消费点里三个过 `effective_cpu_workers`（`stage2.cpp:710`、`:862`、`:1544-1545`），唯独喂给采样器的是原始值：`stage2.cpp:281` `sccfg.cpu_workers = cfg.exec.cpu_workers;` ⇒ 当 config/CLI 用 **0=auto** 时，天光面/UPM/大尺度路径按 `hardware_concurrency()` 并行，而 control 采样按 `sampler.cpp:927` 落到 1（串行）。本件从不喂 0，也从不检查 `sccfg`/`mcfg` 里实际到达模块的值。
- 差在哪：三条口径并存且互不相同（`execution_options.h:16` 的 auto=hardware_concurrency、`sampler.cpp:927` 的 0⇒1、`DATA_SEMANTICS.md:1976` 的 auto=host budget），而名为"预算路由"的门一条都没钉住；方向钉死：不改产物字节（`sampler.cpp:734` 的 pass1_cell 对 worker 数不变，文件 6 亦钉逐位一致），因此本条不升 S1；失真面是对外"并行度由预算唯一来源决定"与"生产 N-worker 采样"两条主张。
- 后果：用户在 config 写 `cpu_workers: 0`（文档承诺的 auto）时得到"串行采样＋并行归约"的混合执行，而诊断与门都看不出；并且下一件（`sampler_parallel_consistency_test`，文件 6）所覆盖的并行档并非生产默认档能取到的值（见 CR-48-28）。
- 定级：S2。
- 怎么算修好：①`stage2.cpp:281` 改 `sccfg.cpu_workers = effective_cpu_workers(cfg.exec);`（或在 `stage2_common.cpp` 的 parse 末尾就把 `exec.cpu_workers` 归一化）；②本件补 0=auto 的用例并断"到达模块的值"——即 `p2_stage2_make_upm_cfg(...)` 后 `mcfg.cpu_workers == effective_cpu_workers(cfg.exec)`，以及 sccfg 侧的等价断言（需要一个可见的接缝或让 stage2 把 sccfg 打印进 diagnostics）；③在 DATA_SEMANTICS.md:1976 与 `execution_options.h` 之间统一 auto 的定义（budget vs hardware_concurrency）。
- 置信：**CONFIRMED**（分发点差异由三处原文直接可推；未实跑）。

### CR-48-26 · 件名与自评所称的"routing"不含排异方法路由表；AUTO 的档界与"生产档/对照档不再同表"这一在册事实，在本共址门内零断言
- 轴：D（主，设计点名的能力只有别的层在做）＋C
- 位点：`:1-3` 「routing_test.cpp — CON-003 production CLI->module routing test / 验证 stage2 CLI 与 gate 测试共享同一生产 parse+build 路径（非 mock），且 worker 预算(CON-002)与 ACR cpu 路由在到达模块前保持正确」；夹具 `:20` `"rejection":{"method":"sigma", …}`
- 上位依据：真实路由表 `rejection.cpp:1272-1288`——`if (method == P2_REJECT_AUTO) { if (pixel_profile) { method = astrocs_n_map_method(n); } else { if (n < 6u) method = P2_REJECT_PERCENTILE; else if (n <= 15u) method = P2_REJECT_WINSORIZED_SIGMA; else method = P2_REJECT_LINEAR_FIT; } }`，且 `:1282-1284` 注释逐字「**M3 后与生产档不再同表**：生产档 astrocs_adaptive_pixel 的 n≥16 已改投 winsorized_sigma，本分支不动，见头文件与 docs/science/REJECTION.md §5」；条文面 `docs/algorithms/REJECTION_ALGORITHMS.md:53`「1≤N≤3 → none（不排异，直接逆方差加权积分）; 4≤N≤5 → percentile 0.2/0.1; …」。
- 现状：本件用显式 `method:"sigma"` 装配 plan，因此 AUTO 分支根本不进；件内对 `p2_reject_plan_resolve`、`astrocs_n_map_method`、`P2_REJECT_PERCENTILE/WINSORIZED_SIGMA/LINEAR_FIT` 的档界零断言（检索式见本节开头）。生产 mosaic 的默认恰是 `method="auto"`＋`profile="astrocs_adaptive_pixel"`（`stage2_common.cpp:227/:272-274`），即"按逐输出像素几何 n 选方法"这条被 §12/REJECTION_ALGORITHMS.md:53 点名的核心路由，在 coverage 模块的共址测试位里唯一叫 routing_test 的文件没覆盖。
- 后果：档界（1≤N≤3 不排异、4..5、6..15、≥16）若被改动（例如把 3 改成 2，或 n≥16 又投回 linear_fit），本门不报；而"两档不再同表"意味着读者/审计无法从门文件名推断该由谁守护 ⇒ 排异路由的守护归属在跟踪面上是空的（我不能实跑其它件去确认别处有覆盖，故此处标 PARTIAL）。
- 定级：S3（覆盖面与命名不符＋守护归属不明；非直接错值）。
- 怎么算修好：本件补一组 AUTO 档界用例，**期望值来自 `p2_reject_plan_resolve`（同源）而非复制表**，并显式钉住两档差异：`profile=astrocs_adaptive_pixel` 下 n=3→none、4→percentile、6→winsorized、16→winsorized；`profile=wbpp_2_9_1` 下 16→linear_fit；或在 `docs/contracts/TEST_MATRIX.md` 明确排异路由表由哪个门守护并把本件改名。
- 置信：**PARTIAL**（缺"别处已覆盖 AUTO 档界"的取证；`eng/tests/api/test_reject_integration_oracle.py`、`synthetic_gate.cpp` 可能覆盖，属他批范围）。

### CR-48-27 · 夹具把"退役键仍可被设且无效果"钉成期望，且 :30 向 UPM 传的是未解析哨兵 `cfg.target_order(=-1)` 而非合同要求的 coverage 实测值
- 轴：C（主）＋D
- 位点：`:17` `"model":{"control_grid_per_tile":8,"robust_loss":"huber","snr_weight_mode":"snr2_normalized"}`；`:31-32` `EXPECT_EQ(m.robust_loss, 0); EXPECT_EQ(m.snr_weight_mode, 0);`；`:30` `P2UpmBuildConfig m = p2_stage2_make_upm_cfg(cfg, cfg.target_order, "deadbeef");`（`production_config()` 未设 `inputs.target_order` ⇒ 取 `stage2_common.h:37` 的 `int target_order = -1`）
- 上位依据：`docs/contracts/PUBLIC_API.md:2208`「`upm_weight_source`（原 `snr_weight_mode`，已按 §9.73 A44 作废：**键不存在**；权重是派生量）」；`docs/contracts/DATA_SEMANTICS.md:1977`「target_order | **=cov.target_order（coverage 实测值透传）** | :189」；对照同文件对退役面对待的方式（`stage2_common.cpp:447-467` 的"出现即拒"）。
- 现状（引原文）：`stage2_common.cpp:157-168` 对这两个键只做等值校验、从不赋值，故 :31-32 的 0 就是 `stage2_common.h:53-54` 的 struct 默认；本件把它写成 EXPECT，等于把"键能被设、设了没用"这一中间态**登记为期望**。另外在册生产 config 模板本身也带着这些键：我读了 `lib/algorithms/coverage/configs/stage2_real_3frame.json` 的键集 —— model = {control_grid_per_tile, huber_delta, min_samples, patch_radius_pixels, robust_loss, sigma_floor, smoothing, snr_search_radius_deg, **snr_weight_mode**, support_power, zero_anchor_weight}，即已发布模板同样依赖"该键被接受"。而 :12 的自评「与 lib/algorithms/coverage/configs/stage2_real_3frame.json **同结构**」实测不成立（夹具无 `target_order`、`patch_radius_pixels`、`smoothing`、`huber_delta`、`sigma_floor`、`support_power`、`zero_anchor_weight` 等键，`integration` 侧也少 `precision` 之外的键差异），"同结构"无任何机制保证——config 模板演变不会让本门变红。
- 差在哪：若按 PUBLIC_API 落实"键不存在⇒出现即拒"，本门 :28 的 `ASSERT_TRUE(p2_stage2_parse_config(...))` 立刻失败（连同 :61、:107 两处对照）⇒ **本门会把正确修复判红**；同时 :30 传的 −1 哨兵与合同的"cov 实测值透传"不同源，且 `m.target_order` 未被断言（若 make_upm_cfg 漏透传，本门无感）。
- 后果：CR-48-12 的修复与本门互相顶；而"routing 件守护 parse+build 共享路径"的自评里，被守护的 build 面漏了 target_order 与 cpu_workers 两个字段（后者见 CR-48-24）。
- 定级：S2（缺"合同两面最终取哪一侧"的裁定 ⇒ 不升 S1；但"门阻碍修复＋夹具依赖"已可照做验证）。
- 怎么算修好：①与 CR-48-12 同批改：`production_config()` 删这两键（或改断 `EXPECT_FALSE` 拒绝路径），并把 :31-32 改成对**在册默认**的显式说明而非"配置结果"；②:30 传一个真实阶（如 7）并补 `EXPECT_EQ(m.target_order, 7)`，另在 `make_upm_cfg` 侧拒绝 <0 的哨兵（fail-closed 而非把 −1 交给 UPM）；③要"同结构"就真同结构——夹具改为 `std::filesystem` 读 `configs/stage2_real_3frame.json`（或加一条比较键集的断言），否则删掉 :12 的声称。
- 置信：**CONFIRMED**（夹具依赖、键集差异、哨兵传递均读码即得；"合同取哪侧"部分待裁）。

### 5.3 不报的核过项（附检索式）

- `LegacyWeightModeDomainRejected`（:84-108）是**有判别力**的门：6 个字符串 token（含空串）＋4 个整数形态全部要求解析失败，并有"无该键时解析成功"的对照（:104-107）；把 `stage2_common.cpp:447` 的拒绝改成静默忽略即红。不报。它未覆盖同族活着的开关（`integration.use_ivar_weight`、`sky_plane.weight_mode`）这一点，我按"属 CR-48-09/10 的修复面"归入那两条的"怎么算修好"，不在本件另立重复条。
- :93 对 `err` 含「§9.73」的断言确实是文案耦合，但同用例已有更强的行为断言（EXPECT_FALSE 解析）⇒ 只算 CR-48-22 同形态的一处，不另立。
- `:47` `EXPECT_FALSE(cfg.exec.deterministic)` 与 `:42` 的 `"deterministic":false` 配对正确（默认是 true，见 `execution_options.h:19`），是对"显式覆盖生效"的真检查。不报。
- 谓词的恒 false 与 ASTROCS_DESIGN §2「纯 CPU 生产，ACR 生产不可达」在语义上相容：按 §4 禁令（已由合同声明退役的东西不报为缺陷），我不把"恒 false 本身"当缺陷报，只报其**判别力与自评**（CR-48-25）。
- `P2_REJECT_SIGMA`/`P2_REJECT_AUTO` 作为入参出现在测试里不构成对 AUTO 路由的覆盖——但我也**不**反过来报"用了 SIGMA 就测错对象"，因为分发点 `stage2.cpp:972` 传的就是 `rplan.method`，取值域一致。不报。

## 6. `lib/algorithms/coverage/tests/sampler_parallel_consistency_test.cpp` —— 已读（464 行全文）

对本批问题④的直接回答：**比的是不同并行度**（1 vs 2；另有 1 vs 4、以及 {1,2} 循环），**比较确实是逐位**（`check_bitwise_same` 用 `EXPECT_EQ` 比 double），但**样本不足以抓到漂移**：三个合成用例里前两个的 fixture 是**常数 patch**（整帧同一 flux），此时中位/MAD/裁剪对任何归约顺序都同值 ⇒ 逐位一致在这些输入上是**平凡真**；唯一含非常数内容的第三件（spike fixture）**不把 obs 交给比较**（`obs` 在 lambda 内局部丢弃，只回 stats）。见 CR-48-28。

### 6.1 §2.5 数值处置表（22 行）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :23 | `ASTROCS_PHASE1_FREEZE_DIR` 缺省根 | `run/temp/phase1_freeze` | 真实 HiPS fixture 默认位置 | 结构性不适用（路径） | AGENTS.md §7「`run/` 临时产物/日志（gitignore，不入库）」⇒ 缺省下该路径不可能存在于跟踪面，:45-46 恒 GTEST_SKIP；件已自陈并用合成件补位（:94-103），故不另立缺陷，但见 CR-48-29 的判据落空 | — | CONFIRMED |
| :29-30 | `mix` 的 `0x9e3779b97f4a7c15ULL`、`h<<6`、`h>>2` | 见左 | 顺序哈希混合常数（SplitMix64/golden-ratio 常数族） | 结构性不适用 | 哈希混合常数，不参与科学语义；仅把 control_id/frame_id 混进 acc（:33-36） | — | CONFIRMED |
| :60-62, :226-229, :305-308, :409-412 | `control_grid_per_tile`=8、`patch_radius_leaf`=2、`min_samples`=5、`snr_search_radius_deg`=0.05 | 见左 | 测试显式重抄的 sampler 默认 | 待确认（值本身继承 sampler 默认） | 与 `sampler.cpp:304-307`、`stage2_common.cpp:40/67/73` 同值；四处重复抄写 ⇒ 默认值改动时本件与实现分叉（门的"同配置"不成立）。同族同值合并登记，位点全部列出 | — | CONFIRMED |
| :62, :68, :246-247, :322-323, :442 | `cpu_workers` 档位 | 1 / 2 / 4 / {1,2} | 被比较的并行度集合 | 待确认 | CON-004（`sampler.cpp:924-928`）只说「>1 走并行分支」，无条文规定"要验到哪一档"；生产实际可达档位取决于 `stage2.cpp:281` 的原始值（见 CR-48-30） | 档位越少⇒越可能漏掉 worker 相关的缺陷（本件的 cell 级切片使缺陷只与"每 worker ≥2 cell"有关，12 cells/4 workers 满足） | CONFIRMED |
| :82-86 | `EXPECT_NEAR(..., 1e-9)` | 1e-9（绝对） | value/uncertainty/snr/control_variance/support 的容差 | 待确认 | 条文面无"并行一致性容差"规定；`docs/algorithms/PHASE2_SAMPLER.md:572` 的 F2 用 `rtol 1e-12`、`:573` 的 F3 用 rtol ⇒ **本件用绝对差、文档口径用相对差**，两处不同源；且被比字段量级跨 1e-27…1e13（见 CR-48-28/31） | 绝对容差对极小量（如 control_variance≈7.6e-27）**过松**（1e-9 比它大 17 个数量级 ⇒ 差异全被吞）；对极大量（snr≈1e13）**过严**（1e-9 绝对差低于 f64 该量级的 ULP ⇒ 只能靠逐位相同才不红） | CONFIRMED |
| :133 | `kW` | 512 | 合成 tile 边长 | 结构性不适用 | 与写侧 `hips_tile_width=512` 一致 | — | CONFIRMED |
| :134 | `kArea` | 1.0e-8f | 合成 support（covered_area） | 待确认（夹具量纲选择） | 夹具自订；与 `o.support = sup_sum/n_valid`（`sampler.cpp:879`）同量级面 ⇒ 生产 support 是球面面积份额，1e-8 属"看起来像小面积"而非从在册产品取值 | 不影响判据（support 只被逐位比较，不与阈值比） | CONFIRMED |
| :142 | 写侧身份参数 | "ivo://astrocs/test"、60.0（exptime）、"2026-09-17T00:00:00Z"、0 | 合成帧元数据 | 结构性不适用（夹具内容） | 进 `p2_frame_id` 的哈希键面（obs_title/filter/exptime/date），两帧同值 ⇒ 只影响 frame_id 是否相同（:187 断逐位等 ⇒ 需要两帧 frame_id 不同才可区分，flux 不同即不同） | — | CONFIRMED |
| :156 | `ipix < 12` | 12 | order-0 tile 数（全球） | 公式导出 | HEALPix Npix=12·Nside²，Nside=1 ⇒ 12；夹具真按全球写 12 tile | — | CONFIRMED |
| :161 | `v.leaf_order` | 9 | tile 内 leaf 阶 | 结构性不适用 | 与 `sampler.cpp:77` kTileShift=9、`:546` leaf_shift=9 同义（三处字面量，见文件 1 处置表 :546 行） | — | CONFIRMED |
| :207-208, :284, :287, :388, :391-392 | flux 值 / support_tiles / spike_value / spike_period | 100.0/125.0；12；6；1000.0；64/8 | 夹具内容 | 结构性不适用（夹具） | :122-128 注释给了两个非平凡设计理由（support_tiles=6 制造 insuff；spike_period=8 保证每个 5×5 patch 必含亮斑）⇒ 夹具意图明确 ✓ | — | CONFIRMED |
| :417 | `background_min_retained_fraction` | **1.0** | 测试把保留率下限顶到 1.0 | **越合同域使用** | `stage2_common.cpp:108-110` 声明该键合同域为 **(0,1]** ⇒ 1.0 合法（闭端）✓；但 :419 的 `background_max_contamination=1.0` 落在合同域 **(0,1) 之外**（`stage2_common.cpp:99-100` 明确 `>=1.0` 判错）⇒ 见 CR-48-32 | 通过 C API 直设结构体即可绕过合同域 | CONFIRMED |
| :418 | `background_tolerance` | 1.0e9 | 把局部容差门推到不可达 | **越合同域使用** | 合同域是 `>0`（`stage2_common.cpp:115`）⇒ 1e9 合法；但其效果是**在测试里关掉一条科学门**，而 `sampler.cpp:503-522` 的入口修补不做上界校验（CR-48-03 同形） | 见 CR-48-32 | CONFIRMED |
| :419 | `background_max_contamination` | 1.0 | 把污染门推到不可达 | **越合同域** | `stage2_common.cpp:99-100`「background_max_contamination 必须在 (0,1)」⇒ 该值经 config 面会被拒，经 C API 面静默接受 | 见 CR-48-32 | CONFIRMED |
| :451 | `grid` | 8 | 期望计数的网格因子 | 结构性不适用 | 循环上界/网格因子（与 :226 的 cfg 同值但**再次独立字面量** ⇒ 三处需同步） | — | CONFIRMED |
| :452-454 | 期望 retained 数 | `n_union_cells × grid²` | 精确计数判据 | 公式导出 | 判据 = "每个 union cell 恰一帧被 reason=2 拒"，与 `sampler.cpp:1049-1054` 的分支一一对应；式正确 ✓ | — | CONFIRMED |
| :326-327 | `ASSERT_GT(rejected_insufficient_support, 0)` | >0 | 非退化自证 | 公式导出 | 正是本派单要求的"判据先钉死方向"：门自证 fixture 真踩到该分支，否则显式判红 ✓ | — | CONFIRMED |
| :347 | `ASSERT_EQ(0u, s1.rejected_insufficient_retained)` | 0 | 隔离 DISP-P2SMP-002 的前提自证 | 待确认（前提依赖） | 注释 :343-345 说明依赖"本 fixture 的 retained 计数为 0"；一旦 fixture 或默认参数变动（例如 spike 生效），该 ASSERT 先红而非恒等式红 ⇒ 门的失败信息会误导（但**不**是错值方向问题） | — | CONFIRMED |
| :349-358, :431-440 | `identity_gap` 的 rej 求和项 | 6 个 rejected_* | 统计恒等式 | 公式导出 | 与 `sampler.cpp:996-997`、`:1044/1048/1054`、`:1092` 的计数分支一一对应；`reason=1`（读失败）也被算进 insufficient_support ⇒ 文件 1 的 CR-48-05 归因缺陷会同样通过本恒等式（恒等式不区分成因） | — | CONFIRMED |
| :270-272 | 「insuff 4214 vs 322」实测证据 | — | 本件的立论数据 | 待确认 | 注释指 `run/RELEASE-03/fix/FIX-210/RECEIPT.md`（`run/` 按 AGENTS.md §7 不入库）⇒ 与 CR-48-23 同一证据落位问题，第二处实例 | — | CONFIRMED |
| :257, :337 | `EXPECT_EQ(rejected_catalog_veto)` 两侧值 | 必为 0 | veto 计数跨 worker 相等断言 | **空断言** | fixture 只写 SIGNAL\|SUPPORT（:140-141）⇒ `aio_hips_open(..., AIO_HIPS_RD_SNR)` 返回空 ⇒ `frames[i].snr` 空 ⇒ `sampler.cpp:883` 条件为假 ⇒ veto 分支根本不执行，0==0 恒真 | 见 CR-48-31 | CONFIRMED |
| :187-198 | `check_bitwise_same` 覆盖字段 | 11 个字段 | 逐位比较的覆盖面 | 待确认（缺口） | 缺 `o.ivar`、`o.ra_deg`、`o.dec_deg`（`sampler.h` 的 `P2ControlObservation` 字段面）⇒ 见 CR-48-31 | — | CONFIRMED |

### 6.2 findings

### CR-48-28 · 并行一致性门用常数 patch 作 fixture，因而把 §5.4 明文**禁止发布**的伪 control_variance/control_ivar 当作逐位基线钉死；同时"并行=串行"在常数输入上是平凡真，门对真实归约漂移判别力近零
- 轴：R（主，恒真断言/判据与缺陷不同源）＋D
- 位点：`:207-208` `ASSERT_TRUE(make_synth_frame(p0, 100.0f)); ASSERT_TRUE(make_synth_frame(p1, 125.0f));` 与 `:149` `std::vector<float> sig((std::size_t)kW * kW, flux);`（**整帧同一常量**，无噪声、无梯度）；判据侧 `:195-197` `EXPECT_EQ(a.control_variance, b.control_variance) << "control_variance 非位精确 @i=" << i;` / `EXPECT_EQ(a.control_ivar, b.control_ivar)`
- 上位依据：`docs/algorithms/PHASE2_SAMPLER.md` §5.4（文件 1 的 CR-48-01 已逐字引）「`σ_bg_raw == 0` …时，control_variance 与 control_ivar **必须**标为「无尺度信息」：`control_ivar = 0`、`control_variance` 取非有限值…**禁用**以 1e-12 之类的占位尺度生成伪有限 control_variance/control_ivar」＋同文 :292-293「本仓实测该伪方差 = 7.609e-27、伪 control_ivar = 1.314e26」；本派单 §2 的 R 轴「判据方向先钉死」与 §9「空断言、SKIP 充数…都算未完成」。
- 现状（推演逐跳，全部读码）：`make_synth_frame` 写出的 signal patch 内所有像素 = flux ⇒ `sampler.cpp:822` `vals.push_back(s)` 全等 ⇒ `:837` `m0 = 100.0` ⇒ `:843-844` `dev` 全 0 ⇒ `s0 = 0` ⇒ `:864` `sigma = 1e-12` ⇒ `:875` `cvar = 1.4×1.5707963267948966×1e-24/289 = 7.609e-27` ⇒ `:877` `civar = 1.314e26`；`check_bitwise_same` 对这两个值做 `EXPECT_EQ` ⇒ **用例现在就是绿的，且绿的原因正是被条文禁止的那条分支**。第二个合成件（:280 起）同样是常数帧（100.0 与 125.0，只是 support_tiles=6），所以它比较的每一个 `control_variance` 都是 7.609e-27。
- 差在哪：两面。(i) **基线钉住缺陷**：一旦按 §5.4 把零尺度分支改成 `control_ivar = 0`／`control_variance` 非有限，本门 `EXPECT_EQ(1.314e26, 0)` 立即判红；若用 NaN 表达"非有限"则 `EXPECT_EQ(NaN, NaN)` 恒假（IEEE 语义），同样判红 ⇒ 本门会把**正确修复判红**（这正是 §3 模板要求"防改对反而判红"要写的同批项）。(ii) **判别力为零**：并行路径按 union cell 切片、每 cell 由单 worker 独占写（:99-102 注释自己说明「无跨 worker 浮点归约」），而常数 patch 下 median/MAD/clip 对任何顺序都得同一值 ⇒ 即使切片/归约真出了顺序相关缺陷，前两个用例也看不见；唯一非常数的 spike 件（:384 起）在 `sample()` 里把 `obs` 作为局部 vector 丢弃（:424-427），只回 stats ⇒ **本文件没有任何"非常数输入 + 逐位 obs 比对"的用例**。
- 后果：对外主张「并行开关不得改变科学数值（ASTROCS_DESIGN §8）」目前主要由"平凡真样本"背书；真正需要守住的归约顺序敏感路径（多帧不同 flux、含结构梯度与噪声的 patch）不在册 ⇒ 若将来引入跨 worker 归约或 tile 级复用改动，本门不能拦。
- 定级：S2（不报 S1：门本身不改产物字节；但它是 CR-48-01 的修复阻碍，且给了"并行已验证"的错误安心）。
- 怎么算修好：①fixture 改为**带噪声/梯度**的合成帧（例如 `flux + 1e-3*sin(x/17)` 或用固定 seed 的 LCG，注释里声明 seed 与分布），并把 spike 件的 obs 也逐位比对；②在 `check_bitwise_same` 里把零尺度分支单独处理（条文修好后：断 `control_ivar == 0` 且 `!std::isfinite(control_variance)`，用 `EXPECT_TRUE(std::isnan(x) || std::isinf(x))` 而不是 `EXPECT_EQ`）；③**同批必改**：CR-48-01 的实现修复与本门的期望必须一起落，否则改对判红；④保留现有三件（其非退化自证 :249/:326/:446 是好的），只是不能把它们当"科学数值正确"的证据。
- 置信：**CONFIRMED**（常数帧 ⇒ σ_bg=0 ⇒ 7.609e-27/1.314e26 的算术与文件 1 的复算同一套；obs 未比对由 :424-427 原文可判）。

### CR-48-29 · 文件头自评的三项判据在 `OneTvsTwoTDeterminism` 里有两项根本没做（stats 与 out_controls 都传 nullptr），并且该件在 Linux 上恒 SKIP
- 轴：R（主）＋C
- 位点：`:2-3` 「同一真实/合成输入分别以 cpu_workers=1 与 =2 运行 p2_sample_controls，**断言 accept/reject 计数、frame_id、control 顺序、obs 值**在容差内一致」；实作 `:63` 与 `:66` `p2_sample_controls(&cov, paths, &cfg, nullptr, 0, &n1, &c1, nullptr, nullptr, 0, err, sizeof(err))`（第 8 参 `out_stats`=nullptr、第 9-10 参 `out_controls`/`ctrl_capacity`=nullptr/0）
- 上位依据：`:45-46` `if (!std::ifstream(base + "/t4_crop_v3.hips/signal/properties").good()) GTEST_SKIP() …`，缺省根 `run/temp/phase1_freeze`（:23）按 AGENTS.md §7 不入库；本件自己的注释 `:95-96` 也已认定「原 OneTvsTwoTDeterminism 依赖 Fatduck 真实 HiPS，在 Linux 恒 GTEST_SKIP，使 sampler 层"并行=串行"实际无门（Skipped ≠ Passed）」——该自我认定是对的，但它只补了合成件，**没有回头修正 :2-3 的自评**。
- 现状（引原文）：件头声称的四类断言里，"accept/reject 计数"与"control 顺序"两处传的是 nullptr；obs 侧则用绝对 `1e-9` 容差（见处置表 :82-86 行），与文档 F2/F3 的 rtol 口径不同源；frame_id/顺序断言在册（:79-81）✓。
- 差在哪：自评把没有做的检查写成做了；且由于该件在 Linux CI 恒 skip，被 skip 的判据不会有任何人发现它本就缺失——而文档 `PHASE2_SAMPLER.md:434` 等处把"控制点节点数组"作为产品面（`out_controls`）来引用，跨 worker 的节点面一致性**在整个 coverage 测试目录里**没有被任何件比过（检索式：`git grep -n "out_controls\|ctrl_capacity" -- lib/algorithms/coverage/tests`）。
- 后果：`controls[]`（tile_ipix/gx/gy/ra/dec/leaf_ipix）跨并行度的一致性无守护；而文件 1 已指出 `tile_ipix` 走 `int` 存储与回转（CR-48-13）——正是这种字段最需要节点级比对。
- 定级：S3。
- 怎么算修好：①:2-3 自评改成实际做到的事；②该件补 `P2SampleStats` 与 `out_controls` 的比对（与合成件共用 `check_bitwise_same`＋新增 `check_control_node_same`）；③若保留 1e-9 绝对容差则注明其适用量级，或统一改 `EXPECT_EQ`（合成件已证逐位可成立）＋相对容差。
- 置信：**CONFIRMED**。

### CR-48-30 · 本门验证的 2/4-worker 分支在生产默认配置下并不执行（`stage2.cpp:281` 把原始 `cpu_workers` 直传采样器），所以"并行一致性"目前是靠"生产默认不并行"实现的
- 轴：D（主，设计点名的"并行开关不得改变科学数值"缺运行时对应）＋C
- 位点：`:246-247` `ASSERT_EQ(sample(1, &n1, &c1, &o1, &s1), 0); ASSERT_EQ(sample(2, &n2, &c2, &o2, &s2), 0);`；`:322-323` `ASSERT_EQ(sample(1, &s1, &o1), 0); ASSERT_EQ(sample(4, &sN, &oN), 0) << "N>1 走并行分支"`
- 上位依据：ASTROCS_DESIGN §8（本件 :265 引「并行开关不得改变科学数值；输出不得依赖线程调度」）；`execution_options.h:16` `int cpu_workers = 0; // 0 => max(1, hardware_concurrency)`；`stage2.cpp:281` `sccfg.cpu_workers = cfg.exec.cpu_workers;`（原始值，未经 `effective_cpu_workers`）；`sampler.cpp:927` `const int workers = (cfg.cpu_workers > 0) ? cfg.cpu_workers : 1;`；`DATA_SEMANTICS.md:1976`「cpu_workers = host->budget.max_workers（blocks(budget)，禁硬编码）」。
- 现状（引原文）：本件自己造并行（显式 `cfg.cpu_workers = workers`），而生产 stage2 路径把 config 的值原样塞给采样器：config 里不写 `execution.cpu_workers`（或写 0=auto，见 `stage2_common.cpp:486` 的文案）时，采样器得到 0 ⇒ `sampler.cpp:927` 判成 1 ⇒ 串行；其余阶段却按 `effective_cpu_workers()` 并行（`stage2.cpp:710`、`:862`、`:1544-1545`）。
- 差在哪：门证明的"并行分支正确"与生产实际走的那条分支（串行）不是同一条；一旦有人按 CR-48-24 把 `:281` 修成 `effective_cpu_workers(...)`，生产才第一次真正走并行——那时本门的保护才开始生效；但反过来说，**本门现在也无法证明生产并行路径已被验证**（对外若主张"Phase2 采样并行已按 CON-004 验证"，其依据是门显式设值，不是配置可达状态）。
- 后果：性能/并行度主张与"科学数值与线程数无关"主张之间的证据链缺一环（配置面到采样器的这一跳没接上）。
- 定级：S3（不改产物字节；属证据链缺口，与 CR-48-24 同批处置）。
- 怎么算修好：与 CR-48-24 同批：`stage2.cpp:281` 改 `effective_cpu_workers(cfg.exec)`；本件补一条"经 config 文本驱动"的用例（`p2_stage2_parse_config` 出 cfg ⇒ 用 `cfg.exec.cpu_workers` 跑 ⇒ 断实际 workers>1），使门的并行分支与生产配置同源。
- 置信：**CONFIRMED**（分发点差异读码即得；"生产默认配置实际取 0"部分依赖在册 config 是否写 `execution` 块——我核对的 `configs/stage2_real_3frame.json` 的 integration 键集里没有 execution 块，故该文件的默认即 0=auto ⇒ 串行采样）。

### CR-48-31 · 逐位比较漏掉 `o.ivar`/`ra_deg`/`dec_deg` 三个产品字段，而 `rejected_catalog_veto` 的跨 worker 相等断言在零 catalogue fixture 上是 0==0 的空断言
- 轴：R（主，空断言）＋D
- 位点：`:187-198` `check_bitwise_same` 的字段清单（frame_id/control_id/leaf_ipix/snr_available/quality_flags/value/uncertainty/control_variance/control_ivar/support —— **无 ivar、无 ra_deg/dec_deg**）；`:257` `EXPECT_EQ(s1.rejected_catalog_veto, s2.rejected_catalog_veto);`、`:337` 同；夹具构造 `:139-141` `aio_hips_product_begin(path.c_str(), kW, kW, AIO_HIPS_FLOAT32, AIO_HIPS_PRODUCT_SIGNAL | AIO_HIPS_PRODUCT_SUPPORT, …)`
- 上位依据：`docs/contracts/DATA_ARTIFACTS.md:26`（control variance/ivar 为对外产品字段，「`k_corr` 放大已含在值内 ⇒ 消费侧取用含 `k_corr` 的值」）；`sampler.cpp:1122-1133` 的 `o.ivar` 有两条来源（civar 回退 vs 帧 ivar 产品的 leaf 读取，后者经 `aio_hips_read_leaf_f32` 逐观测读盘 ⇒ obs 里唯一带独立 I/O 的字段）；`sampler.cpp:883` 的 veto 条件 `if (cfg.background_catalog_veto && !frames[frame_id].snr.empty() && frame_snr_med[frame_id] > 0.0)`。
- 现状（引原文）：fixture 只写 SIGNAL|SUPPORT 两个产品 ⇒ `aio_hips_open(…, AIO_HIPS_RD_IVAR)` 与 `…_RD_SNR` 都返回空 ⇒ `o.ivar` 恒等于 `cs.civar`（与已比的 control_ivar 同源，故漏比不改变现行结论，但**产品字段 ivar 的跨 worker 一致性从未被验证**）；`frames[].snr` 为空 ⇒ veto 分支不执行 ⇒ `rejected_catalog_veto` 两侧恒 0 ⇒ :257/:337 是恒真断言。
- 差在哪：§9 明确「空断言、SKIP 充数…都算未完成」；本件 stats 面比 10 项，其中 1 项在在册 fixture 下不可能非零；obs 面比 10 个字段而 `P2ControlObservation` 是 13 个字段。
- 后果：门的覆盖面被高估（"10 项 stats 全等"含一条空断言）；若将来给 ivar 产品接线或引入 SNR catalogue fixture，`ivar`/veto 两条链的 worker 相关性仍无守护。
- 定级：S3。
- 怎么算修好：`check_bitwise_same` 补 `ivar`/`ra_deg`/`dec_deg` 三项；`make_synth_frame` 增可选 SNR 目录写入（含一颗 >`10×` 帧中位 SNR 的亮星）使 veto 非零，并把 `rejected_catalog_veto > 0` 加入非退化自证（与 :326 同形态）。
- 置信：**CONFIRMED**（字段清单与 fixture 产品面读码即得）。

### CR-48-32 · 门用**合同域之外**的配置值构造非退化输入，暴露"配置域只在 CLI parser 校验、C API 入口不校验"的结构性缺口
- 轴：C（主）＋R
- 位点：`:419` `cfg.background_max_contamination = 1.0;`（合同域 (0,1)，见 `stage2_common.cpp:99-102`「background_max_contamination 必须在 (0,1)」）；`:418` `cfg.background_tolerance = 1.0e9;`（合法但把科学门关掉）
- 上位依据：合同域条文 `docs/contracts/DATA_SEMANTICS.md` §25（parser 面）；实现面 `sampler.cpp:503-522` 的入口修补只做 `<=0 → 默认`、`!(x > 0) → 默认` 一类的**下端**保护，**没有任何上界或值域不变式**；AGENTS.md §6「不动科学公式、默认容差」（此处的"非默认值"只能经 C API 设进去）。
- 现状（引原文）：`sampler.cpp:506-507` `if (cfg.background_max_contamination <= 0.0) cfg.background_max_contamination = 0.20;` ⇒ 1.0 原样进 `:1045` 的 `cs.bfrac[fi] > cfg.background_max_contamination`，污染门被永久关掉；本门靠这个动作把 reason=2 隔离出来（:414-416 注释解释得很清楚）。
- 差在哪：合同声明的域在 C API 面不是不变式（越域值被静默接受并生效）——与文件 2 的 CR-48-03（k_corr 定义域 `1<k` 在采样侧不防）同一形态的两个实例；并且本门的 fixture 构造**依赖**这个缺口，所以"合同域"与"能造出非退化输入的实现面"是矛盾的：修缺口会先把本门判红。
- 后果：任何经 C API（scheduler 的 `module_adapters`、工具 `astrocs-stage2`、以及未来的编排接线）传入的越域参数不会报错；对外"配置域受合同约束"的主张仅在 CLI/JSON 入口成立。
- 定级：S2（门的构造依赖缺口这一事实使修复必须同批协调；不改现有健康数据产物）。
- 怎么算修好：①把合同域在 `p2_sample_controls_impl` 的入口做**双向**校验（越域 ⇒ rc=1＋err，与 `stage2_common.cpp` 同口径，两处共享同一域表而非两份字面量）；②本门改用"域内的极端合法值"达到同样的隔离效果（例如 `background_max_contamination` 取 0.99 并把 spike 亮度控制在该门阈值之下、`background_tolerance` 用域内较大值 1e3 之类），使门的构造不再依赖越域；③同批复核 `sampler.h` 的字段注释是否声明域（若无则补引 DATA_SEMANTICS 行）。
- 置信：**CONFIRMED**。

### 6.3 不报的核过项（附检索式）

- `OneTvsTwoTDeterminism` 的 `GTEST_SKIP`：件自己在 :94-103 承认「Skipped ≠ Passed」并补了 Linux 可跑的合成门 ⇒ 不属于"用 SKIP 充数"，不报（SKIP 面缺判据的问题另由 CR-48-29 承担其"自评不实"部分）。
- `SparseSupportStatsBitwiseIdenticalAcrossWorkers` 的三条自证（:249/:326/:347 与恒等式 :359-360）方向正确、判据非退化，且 FIX-210 的根因（`pass1_cell` 的 cv/ci 复用）与实现侧的修复（`sampler.cpp:943-946` 注释与独立局部量）匹配 ⇒ 这是一条**真能红**的共址门，不报为缺陷。
- `check_bitwise_same` 比 `leaf_ipix`（:189）⇒ 观测**顺序**与 cell 归属被钉住（leaf_ipix 由 `tile_ipix + center_local` 唯一决定），所以我不另立"未比顺序"的指控；节点数组面见 CR-48-29。
- `make_synth_frame` 用 `aio_hips_product_begin(... SIGNAL|SUPPORT ...)`、`aio_hips_tile_view_abi_init(&v)` 的版本化 ABI 初始化（:168-169 注释）符合 CON-006/ABI 规则，不报。
- 件内直接操作 `std::filesystem`（:110/:115/:136-137）而本仓有 AIO I/O 边界门（CHK-AIO-IO-BOUNDARY）：:106-108 注释已登记台账位置并声明「对本文件的命中数**只减不增**」⇒ 属已登记豁免面，不报（我不能实跑该门，故只作事实陈述）。
- 件内 4 处重复抄写 sampler 默认（8/2/5/0.05）与 `grid=8` 的第三处字面量：值与实现全部一致（逐值核对过），只构成漂移风险而非现行不一致；按 §4（不报未构成错误的重复）只入处置表，不立 finding。


### 编号说明

- finding 编号 `CR-48-01 … CR-48-32`，共 **31 条**；`CR-48-17` **未使用**（初稿中该条与"排异路由表不同源"重复，已并入 `CR-48-26`，编号不回收以免与处置表内的引用错位）。
- 归属：文件 1 → 01…08；文件 2 → 09…16；文件 3 → 18…20；文件 4 → 21…23；文件 5 → 24…27（正文顺序为 25、24、26、27）；文件 6 → 28…32。

### 结构自检（本批交付面）

- 六个文件的 §2.5 处置表合计 **179 行**（含文件 4 的判据字面量附表 6 行；分文件 62／44／19／8＋6／18／22），其中 `待确认` 档 **69 行**、`结构性不适用` 42 行、`文献值` 26 行、`公式导出` 18 行，其余为标注不一致形态的行（越域／钉住不一致／偏松等）。
- findings 合计 **31 条**。
- 定级分布：**S1 × 3**（01、09、10，均写明了从生产入口逐跳的触发路径）／**S2 × 14**（02、03、04、11、13、16、18、20、21、24、25、27、28、32）／**S3 × 14**（05、06、07、08、12、14、15、19、22、23、26、29、30、31）。
- 每行处置都给了档位与依据锚；`待确认` 行的"保守方向＋影响范围"列已逐行填写（少量行以「—」表示该值不进入任何科学语义）。
<!-- PROGRESS: 6/6 -->

---

## 本批最高危三条（只挑，不复述）

1. **CR-48-01（S1）零尺度伪方差发布**——`sampler.cpp:864/875/877` 把 `σ_bg_raw=0` 用 `1e-12` 地板平方成 `control_variance=7.609e-27`／`control_ivar=1.314e26` 并随 obs 发布，违反本仓 `PHASE2_SAMPLER.md §5.4` 已冻结的"无尺度信息"发布口径；下游 `upm.cpp:668-671` 的 per-control 归一化使该观测权重占比→1.0，独占该 control 的相对光度解。**三条防御面同时失效**：文件 6 的并行一致性门把这个伪值当逐位基线钉死（CR-48-28，改对即判红）、文件 2 的 `sigma_floor=1e-3` 只作用于 robust 尺度不作用于权重（CR-48-14）、文件 3 的非有限门只判"有限/非有限"抓不到 1.3e26（CR-48-18/§3.3）；而条文 §5.3 自己写明该分支在真实 M42 帧上 4096 个 patch 命中 2 个。
2. **CR-48-09＋CR-48-10（各 S1）"权重模式"概念在配置面活着**——`stage2_common.cpp:179-180` 的 `integration.use_ivar_weight` 取 0 即令 science 权重走 `upm.cpp:1992-1997` 的 `support^p · snr²/(1+snr²) / unc²`（SCI-UPM-WEIGHT-001 点名禁止的 support/SNR 乘因子）；`:193` 的 `sky_plane.weight_mode` 更带 `0=inverse_variance／1=snr2` 枚举且**连值域都不校验**，而 `sky_plane_enabled` 缺省 true。同一函数在 :447-467 却对 `weight_mode`／`legacy_allow_weight_fallback` 两个同族退役 token 立"既不能被设、也不能被读，出现即 fail-closed"的标准——禁则贯穿到键名、没贯穿到概念。
3. **CR-48-13（S2，域不一致 CONFIRMED）高 mosaic 阶下静默错值**——`stage2_common.cpp:29` 放行 `target_order ∈ 0..29`，而采样器 leaf 寻址另加 9 阶（`sampler.cpp:777` `1u << (target_order + leaf_shift)`：阶 ≥23 移位 UB、阶 ≥21 越出写侧声明的 Nside≤2^29），`CellStat::tile` 用 `int` 承载 tile ipix 并在 `:1211` 回转成 `uint64`（阶 ≥14 即截断/符号扩展）⇒ `controls[].ra_deg/dec_deg/leaf_ipix/tile_ipix` 全部错值而 rc=0，第二遍的 `std::map<int,…>` 邻域分组还会跨 tile 碰撞。

## 我读不动或需要实跑才能定的（含该跑什么）

1. **`CHK-NO-WEIGHT-MODE-CODE` 是否把两个活开关计入"模式选择机制"**（决定 CR-48-09/10 是"CI 已红被忽略"还是"CI 本身漏判"）。跑：`python3 eng/ci/check_no_weight_mode_code.py --json-out run/ci/…/no_weight_mode_code.json` ＋ `--self-test`（本批按前言 §1 禁跑 `eng/**`）。
2. **CR-48-01 的修复会把哪些在册用例判红**（NaN 方案 vs +Inf 方案结果不同，见 CR-48-28 的"同批必改"）。跑：`python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 4 -- ctest --test-dir build -R "phase2_sampler_parallel|phase2_rej_nonfinite_weights" --output-on-failure`。
3. **生产 SNR catalogue 是否可能超 65536 行**（CR-48-04 的定级取决于它；`aio_hips_read_snr_catalog` 的静默截断已 CONFIRMED，缺的是行数）。跑法：一次 Phase1 后数 `…/snr/` 目录表行数并对比 `kSnrCatalogMax`；testdata/probe 产品不入库，故无法从跟踪面判定。
4. **`control_grid_per_tile` 取 512 的非因子时的边缘间隙如何被吸收**（grid=7 ⇒ 最右 control 列中心 x=474、patch 半径 8 ⇒ x>482 无控制点）。需通读 `lib/algorithms/coverage/hips_p2/` 的 leaf→control 索引与外插规则（在清单外，本批未展开）。
5. **排异 AUTO 档界是否在别处已有门**（CR-48-26 定为 PARTIAL 的原因）。需读 `eng/tests/api/test_reject_integration_oracle.py`、`eng/tests/api/test_reject_parallel.py` 与 `synthetic_gate.cpp` 的 AUTO 用例（他批文件）。
6. **采样器 P0-08 两支（`sampler.cpp:1077-1090` sky_samples、`:1144-1168` star_masks）在生产是否确实不可达**：两个导出入口 `:1247`/`:1271` 都把 `out_n_sky`/`out_n_mask` 硬编 nullptr，`p2_sample_sky*` 已按 CHK-PROD-WIRING W1 删定义（`:1275`/`:1277`），而 `sky_plane.h:74` 另有同名 `P2SkySample` 供 `p2_sky_plane_build` 消费 ⇒ 需确认生产天光采样点由哪一处装配；若非采样器，则这两支是死代码且 :623/:1143 的"P0-08 输出"表述需订正。
7. **`patch_radius_leaf` 是否有任何消费者**：我只在 `sampler.cpp:305`（默认）/`:520`（修补）与四处测试夹具见到赋值，pass1 估计窗实际用 `background_patch_radius` ⇒ 若 `p2_upm_*`/`sky_plane` 也不消费，则该配置键为"可设不可用"形态（文件 1 处置表 :305 行由此待定）。
8. **`astrocs_adaptive_pixel`（生产档）的 `astrocs_n_map_method(n)` 档界表全文**：`rejection.cpp:1274-1278` 只给了调用点与注释描述的档界（N<6 percentile／6..15 winsorized／≥16 winsorized），我没读到函数本体的分派语句（在 `rejection.cpp` 他处），故文件 2 处置表对 linear_fit 档的"默认值仍按对照档装配"一句以注释为据、未逐行核函数体。
9. **本批未实跑任何构建/ctest/`eng/**`**（前言 §1 硬约束），因此所有"能红"结论均为静态变异推演（文件 3/4/6 各条已标明）；没有任何一条依赖运行结果。

## 派单与清单差异

1. **清单 6 项全部存在、全部逐行读完**（无缺失、无越界）；`wc -l` 与读取行数一致：sampler.cpp 1503、stage2_common.cpp 551、rejection_nonfinite_weights_test.cpp 189、retired_object_reject_negative_test.cpp 151、routing_test.cpp 108、sampler_parallel_consistency_test.cpp 464。
2. **派单重点⑤在清单里没有对应物**：派单问"路由件的判据与真实路由表是否同源"，但 `routing_test.cpp` 是 CON-003 的 CLI→模块 parse/build 共享路径件，**不含排异方法路由表**（夹具显式 `method:"sigma"` 绕开 AUTO；件内对 `p2_reject_plan_resolve`/`astrocs_n_map_method` 零断言）。我按问题句去查了真实路由表（`rejection.cpp:1272-1288`，且 `:1282-1284` 自陈生产档与对照档"不再同表"）并在 §6 开头与 CR-48-26 作答，未擅自扩大或缩小清单。
3. **派单重点③的独立取证与邻批表述不同**：三侧识别面确实全是字符串比较，但 drizzle 侧已做大小写归一（`v6_drizzle_science.cpp:105-113`）并有 parity 断言，resample/calibration 侧是精确 `==`（`p3_rsmp_failclosed.cpp:30`、`v6_calibration_covariance.cpp:60`）且门无变体负例；门的 status 判据（`Status::Reject`／`EXPECT_FALSE`）在册、并非"只匹配消息子串"，只是**额外**绑了散文（CR-48-21/22）。未照抄邻批结论。
4. **工作树基线在收工时发生本节点之外的变化（上呈）**：开工 status 为四行（zip／`drz.html`／`photom.rst`／`site/`），其中 `drz.html` 已是前言所列三项之外的**新第四项**；收工 status 只剩两行，`photom.rst` 与 `drz.html` 已从工作树消失（`ls` 证实文件不存在）。本节点零 git 写、零 `git config` 写、`git diff --stat HEAD` 为空、HEAD 仍为 `c8f64e9a`，全部写入仅 `独立审计/证据/通读-CR-48.md`（在仓库之外）⇒ 这两项的消失由本机其它会话所致，请负责人确认；按前言 §1 我不据此判本节点写入，也不做恢复动作。
5. **环境差异**：`rg` 缺失（Grep 工具报 `spawn rg ENOENT`），全部检索改走 `git grep`/`sed`；Python 输出一律 `sys.stdout.reconfigure(encoding='utf-8')`；未建 `独立审计/复算件/CR-48/`（全程无需中间件，按 §1「能不建就不建」）；两次 `git status` 原文已在文首与下文各自贴出。

- 收工 git status --porcelain 原文：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```
