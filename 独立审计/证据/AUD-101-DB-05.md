# AUD-101 · DB-05 文档审计台账（`docs/architecture/` 为主，25 份）

批次：DB-05 ｜ 清单：`独立审计/批次清单/DB-05.txt`（25 行）｜ 判据：标准 01 §1–§5 ｜ 被审基线 HEAD=`c8f64e9a`
本批特有关注点执行方式：对每份 `docs/architecture/` 文档判「是否在另立架构权威」（标准 01 §1 末条 + §1 第 2 条），
并双向核 §8 机制承接矩阵与孤儿机制（标准 01 §3 第 1、2 条）。三节结论见 §C/§D/§E。

## A 基线自证（开工与收工各一次）

开工（16:1x 本地时间）：

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
---HEAD---
c8f64e9a
```

收工：见本文件末尾 `## L 收工基线`。

本批全程只读：未执行任何构建、`ctest`、`run_checks.py`、`eng/**` 脚本；未 `import` 仓库内任何 Python 模块（无 `__pycache__` 产生）。
所有计数由自己跑的命令得出（命令随判词附在对应条目内）。
取证期间在**审计产出目录**（仓库外）临时落过若干一次性辅助脚本，用完即删；被审工作树未被写入任何字节——
开工与收工的 `git status --porcelain` 逐项一致（§A 与 §L），本文件是 `独立审计/证据/` 下本批唯一留存成稿。
过程记录：一次把 stdout 重定向到 `/tmp` 的尝试在本机不可用，改为直出；批次清单文件是 CRLF，首次 `wc -l` 循环因路径带 `\r` 全数失败，加 `tr -d '\r'` 后重测（§J 行数即重测值）。

## B 逐份登记

列名与顺序按派单 §4 固定。单元格内 `[D-xx]` 指向 §J 的逐份举证（重复段落、违规行号、冲突对证）。
`下游` = 我自己跑的 grep 结果，命令统一为：

```bash
git -C "F:/Astro dev/Astro CS Normalization Database" grep -l -e "<文件名>" \
  -- ':!docs/architecture/<该文件名>' ':!工程控制' ':!run'
```

再剔除过程账目类引用者（`reports/v19r2/**`、`artifacts/evidence/**`、`memory.md`、`docs/audit/**`、
`docs/DOCUMENT_INDEX.yaml` 自身），剩下的才是真消费者；无剩余者记 `孤儿`。

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| docs/architecture/ISA_BIT_MANIP_VARIANTS.md | 整数/位操作 ISA 变体评估 (ISA-005) — NOT_APPLICABLE | 42 | 操作与运营（实测决策台账）；实际在 `docs/architecture/`＝架构设计目录 ⇒ 角色错放，应迁往 `docs/performance/`（标准 01 §2 表行「操作与运营」） | ISA 位操作变体是否发货 | 缺（抬头写「§8（软件架构）」，但该机制条款在 §9；`> ID:` 行另标 `上游: ISA-004/05 §1-3`＝指向不存在的编号文档）[D-01] | 1：`eng/tests/backend/test_isa_bit_manip.py`（+索引） | 否（同主题正本候选＝ISA_VARIANTS.md） | 与 ISA_VARIANTS.md 同主题（ISA 变体评估/发货决策），两处段落 [D-01] | 有：:3–:6（`> ID: ARCH-ISA-005 状态: 结论 NOT_APPLICABLE 上游… 下游… 依据(03 §91)`）——**机械列 meta_block=False 漏报** | `:5` 任务编号 ISA-005/ARCH-ISA-005；`:6` `依据(03 §91)`、`WIN-003`；`:36` 「按 03 §91 → 登记」；`:37` 「测噪验证…不 SHPI/不入 manifest」（过程自述＋错字 SHPI）；`:42` 「与 preflight/ABI-002 关系」 | `:41` `artifacts/evidence/prerelease-v5/ISA-005/MEASUREMENTS.csv` — 不在跟踪集（`git ls-files -- artifacts/evidence/prerelease-v5` 只出 ISA-001/002/003，无删除记录）；`:6/:36` 依据文档 `03`、`05` 不在跟踪集（命令与正例对照见 §H-0：同一 `NN_NAME.md` 式子对 `docs/ci/01–04_*` 命中 6 ⇒ 被引名 0 命中为真 0） | §9（纯 CPU、发货档由 benchmark 台账决定）；§8 无 ISA 变体机制 ⇒ 本文件把结论挂在已消失的控制包编号文档上 | 合并（并入 ISA_VARIANTS 的评估节，证据落 `artifacts/evidence/prerelease-v5/ISA-005/`）——依标准 01 §2 末条「过程台账不放正式文档层」＋§1「不另立权威」 |
| docs/architecture/ISA_VARIANTS.md | ISA 变体决策台账 (ISA-001..004 冻结 — V5) | 85 | 操作与运营（性能实测台账）⇒ 角色错放，应迁往 `docs/performance/` | 各 ISA 档是否发货＋变体注册与污染防线 | 缺（同 ISA-005：抬头 `§8`、`> ID:` 行 `上游: ABI-003/05 §1-2`）[D-02] | 5：`docs/architecture/cpu/CPU_003_AVX2_PROVIDER.md`、`eng/tests/backend/{test_isa_avx,test_isa_avx2_fma,test_isa_avx512,test_isa_variants}.py`＋`lib/.../cpu/avx512/include/astrocs/cpu/avx512_provider_v1.h` | 是（ISA 发货决策台账的正本候选） | 与 CPU_003_AVX2_PROVIDER.md §2 表格逐字重复 [D-02]；与 ISA_BIT_MANIP_VARIANTS.md 同主题 | 有：:3–:6（`> ID: ARCH-ISA-001 状态: FROZEN … 原则(05 §1)`）＋标题内 `(ISA-001..004 冻结 — V5)` 版本锚——**机械列漏报** | `:1` 标题版本锚 V5；`:5` 任务编号＋FROZEN 状态预写；`:6` `原则(05 §1)`、`见 artifacts/…ISA-001/MEASUREMENTS.csv`；`:15–:24` 逐 kernel 实测数值表（ns 级）＋`vm-bj` 机名；`:22` 「决定性判读」过程叙事；`:57` 「按任务规则」；`:69` 「WIN-003/WIN-00x 域…本任务已提供」；`:73` 「sha256 实测入 `backends.manifest.json`」 | `:69` `artifacts/evidence/prerelease-v5/ISA-004/MEASUREMENTS.csv` 不在跟踪集（`git ls-files -- artifacts/evidence/prerelease-v5` 只出 ISA-001/002/003；`git log --diff-filter=D` 对 ISA-004/005 无删除记录 ⇒ 从未入库；机械列已报，复核为真）；`:41`（同主题另一份）ISA-005 证据同样不在跟踪集，**机械列漏报**；`:4/:6/:71/:77` 引用的编号标准 `05 §1/§2/§3/§5` 不在跟踪集（§H-0 命令含正例对照） | 内部自相矛盾：`:53` 「ISA-004(AVX512)…属 Windows 域, 本机不评估」vs `:55–:69` 同一文件在 Linux `vm-bj` 完整评估 ISA-004；`:10` 变体家 `benchmark/backend_host/avx_backend.cpp` vs `:73` `…/avx2_backend.cpp`，而 CPU_001/CPU_003 把变体家写在 `benchmark/cpu/{avx2,avx512}/` 且后者**不在构建图**（根 `CMakeLists.txt:345,702-709` 只列 `backend_host/`）⇒ 同一「ISA 变体」主题两套互不兼容的正本叙述（:73 的 `backends.manifest.json` 是生成件、生产者为 `eng/tools/gen_backends_manifest.py`，仓内不落跟踪副本 ⇒ sha256 断言无法从库内核验，但引用对象真实存在，不记悬空） | 迁移（→`docs/performance/ISA_VARIANTS.md`）＋删实测表副本改指针——依标准 01 §2 表「操作与运营→docs/performance/」＋§1「同一主题只有一份正本」 |
| docs/architecture/MODULE_MAP.md | Module Map | 110 | 模块工作细节（每个生产模块做什么/交付面/证据锚）⇒ 角色错放，应迁往 `docs/modules/`（标准 01 §2 表行 4） | 交付面/构建内执行面/合同目标目录/非交付面 | 有：:3 `> 上游：ASTROCS_DESIGN.md §8（软件架构）` | 7：`README.md`、`docs/DEVELOPER_GUIDE.md`、`eng/ci/ledgers/prod_wiring.json`、`eng/tests/arch/test_single_cli.py`、`eng/tools/check_p2_symbol_map.py`、`lib/infrastructure/pipeline/module_loader/README.md` | 是（模块↔交付面映射的正本） | `docs/modules/*.md`（本文件 §5 自己指向它）：映射面重叠、无逐文件差异说明 [D-03] | 有：:5–:11（状态词口径声明＋「⚠ 本文件不声明第二套状态阶梯」纪律声明）——**机械列 meta_block=False 漏报** | `:27` 「当前为 **Linux 技术预览** 安装面；Windows 侧复验 NOT_VERIFIED」（状态预写）；`:28` `install_layout.cmake（:104-105 …）`；`:74` 「原引「宪章 §F.1」已废止」；`:82` 「原引「宪章 §10.5/§18.2」已废止」；`:91–:96` 「设计冻结 8 种投影…行锚以该注册表为准」；`:105` 「已摘出 add_subdirectory」；`:106` 「ARCH-001 §7 登记，LEG-003 迁移」 | `:76` 及 §2 表首列 `lib/phase1` — **不在跟踪集**（`git ls-files lib \\| cut -d/ -f2 \\| sort -u` = algorithms/include/infrastructure/phase1_session/phase2_session/phase3_session/third_party），本机工作树亦无该名（磁盘只有 `phase1_session`），而本文件 :10 自称「本表按 **`lib/` 实际目录** 登记（不再描述已不存在或尚未建立的产物）」⇒ 自述与该行互斥；`:110` `docs/modules/<module>.md` 为占位符字面量（目录真实存在，非可解析链接） | §12.5「状态由检查与验收现场计算，登记表不预写状态」——本文件 :8 自己复述该条，随后 §1–§4 四张表每行都预写 `INSTALLED/IMPLEMENTED/CONTRACT_READY/DORMANT/DEFERRED` ⇒ 自我违反＋与上位冲突；§8.4 顶层树的五项与**跟踪面一致**（本机磁盘上的 `lib/calibration` 等属未跟踪残留，不算仓库问题，见 §E-1 订正），但 §8.4 要求的「科学算法目录按并联命名、不用 phase」在本文 §2 被登记为 `lib/phase1`（不存在）与 `lib/algorithms/coverage`（Phase2 全链内核所在，名字与阶段无关联但一个目录装五个算法）⇒ 承接面与实际都不闭合 [D-03] | 迁移（→`docs/modules/MODULE_MAP.md`，并删状态列改由 `eng/tools/quality/check_module_map.py` 现场出）——依标准 01 §2 表行 4＋§3 第 3 条 |
| docs/architecture/OWNERSHIP_AND_LIFETIME.md | Ownership & Lifetime | 24 | 架构设计（接口所有权契约）——目录属 §2 允许的架构设计族（`docs/interfaces/` 更贴切） | C API handle/buffer 所有权、RAII、thread-local | 有：:3 `> 上游：ASTROCS_DESIGN.md §8（软件架构）` | 3：`lib/infrastructure/aio/include/aio_upm.h`、`lib/infrastructure/gaia_xpsd_client/src/gaia_client.h`、`eng/tools/quality/v19r3_traceability.py`（经 `docs/TRACEABILITY.csv`） | 是（所有权/生命周期契约仅此一份） | 与 §8.2「命名块生命周期」标题撞名而内容不同（本文讲 C handle，§8.2 讲块消耗），易被误当承接 [D-04] | 无（无 ID 块） | `:18` 「p2_upm_open 失败路径已统一 delete（**V19R2 PR#1 门禁修复**）」＝任务编号＋历史留痕；`:19–:20` 源码行锚（`aio_upm.cpp:~448`、`:283`、`:163`）；`:24` 「ENG-OWN-001..003（S2 注册）」 | `:24` 「S2 注册」的 **S2** 在仓内不解析（`git grep -n "S2 注册" -- docs` 只命中 6 处同样写法的文档，无一处定义登记面）；登记的 `ENG-OWN-001..003` 本身有落点（`docs/TRACEABILITY.csv:11`、`docs/contracts/API_CONTRACTS.csv:272` 行 `API-p2_upm_close`），故记为「口径名不解析」而非悬空；`:19` `~448` 模糊行锚 | §8.2 的「块生命周期＋显式销毁」无人承接：本文 §名 Lifetime 实为 C 句柄规则，未覆盖 PipelineFrame 块的生产者/消费者/销毁声明（唯一承接是 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`，与 §8.2 的命名块生命周期一致）⇒ 本文与 §8.2 无条款对应关系，属**挂在 §8 名下的孤儿文档** | 迁移（→`docs/interfaces/OWNERSHIP_AND_LIFETIME.md`，删「已知审计点」节）——依标准 01 §2 表行 3（架构设计→`docs/interfaces/`）＋§4「无历史叙事」 |
| docs/architecture/PERFORMANCE_MODEL.md | Performance Model | 245 | 操作与运营（性能标定/回归台账）⇒ 角色错放，应迁往 `docs/performance/`＋`artifacts/evidence/` | 编排参数标定、并行宽度推导、内存闸门标定 | 缺（抬头 `§8`；:18 又标 `ASTROCS_DESIGN.md §9（探针驱动优化）` ⇒ 两个上游、条款不同）[D-05] | 2：`docs/modules/astro_image_io.md`、`docs/plugins/infrastructure/19_runtime.md`、`docs/science/ACR_EQUIVALENCE.md`、`lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp`（共 4，均非架构层） | 否（前 13 行是性能模型条文，正文 90% 是标定台账） | 与 THREADING_MODEL.md「并行轴分配」节逐式重复（同一 `in_flight/inner_omp/W_eff` 公式、同一实测表 390.7/421.0/583.7 s）[D-05] | 有：:15 章节标题即任务编号＋日期「PERF-501 编排参数标定（探针驱动，2026-09-23）」；:17–:20 权威声明块 | 日期 `:15,:81,:222`；任务编号 `FIX-01 :106,:113,:192`、`PERF-501`（全文 20+）、`PERF-MEM-FIX-01`、`PERF-PROFILE-01`、`P1-CONCURRENCY-CALIB-01`、`P1-PARALLEL-AXIS-REDESIGN-01`、`RELEASE-05`、`G-RES-01`；commit sha `:85 d7ef4d8b`、`:86 37a09c81`、`:137 f86d60e2…`；历史叙事 `:47` 「原「抬帧并发…」已证伪」、`:64` 「~~该行已作废~~」、`:81` 「该拒收理由已被证伪」、`:99` 「本文件旧版」、`:169` 「原阻塞项已证伪」、`:191` 「已重标定为 116.0…旧值保守 3.59×」、`:222` 「裁决（P1-…，2026-09-23）」、`:229` 「旧值 2 的前提…已被取代」；负责人裁决叙事 `:222` 「裁决…」；删除线表格 `:64,:80,:178,:191` | `:20` `run/PERF-501/EVIDENCE.md`、`:24` `run/RELEASE-05/vis/out/m42_p1_t2/resource_timeseries.csv`、`:28` `run/PERF-501/logs/*.child.stderr`、`:37` `run/PERF-501/evidence/before_w16_2f/`、`:55`、`:56`、`:59`、`:61`、`:83`、`:100`、`:107`、`:207` `run/RELEASE-05/perf/fix3/REPORT-PERF-MEM-FIX-01.md`、`:238` `run/P1-PARALLEL-AXIS-REDESIGN-01/REPORT.md` ⇒ 共 **11 条 `run/…` 证据锚全部不存在**（`run/` 是 gitignore 过程目录，AGENTS §7），即全文结论无一条可在干净检出上复算 | §1「架构问题的唯一权威是 §8」/§9——本文件 :17 把编排参数取值的**权威**判给 `docs/contracts/SCHEDULER_CONTRACT.md` §3，并规定「最终取值由 **PERF-501**（一个任务）确定」⇒ 以任务为权威落点；§10/§7.2 规定资源时序落 `output_dir`，本文件把 `resource_timeseries.csv` 当既成证据面引用（生产实现真实在 `lib/infrastructure/cli/resource_recorder.h:288-293`，列名与该文档口径见 D-18） | 迁移（标定台账→`artifacts/evidence/<轮次>/`，性能模型条文→`docs/performance/PERFORMANCE_MODEL.md`）——依标准 01 §2 末条「过程台账不入正式文档层、留证落 artifacts/evidence 并由正本引用」＋§4 全文 |
| docs/architecture/PHASE3_MODULE_ARCH.md | Phase3 模块架构 (HiPS reader → WCS → resampler → FITS writer) | 64 | 模块工作细节（四单元数据结构/缓存/错误/内存上界）⇒ 角色错放，应迁往 `docs/plugins/algorithms_phase3/`（AGENTS §7 落位速查） | Phase3 模块边界与并发上界 | 缺（抬头 `§8`；:5 又标 `上游: ALG-007/ARCH-002/ARCH-004`）[D-06] | 2：`eng/tests/arch/test_phase3_module_arch.py`、`eng/tests/config/test_cfg004_unified_contract.py`（后者之外无文档消费者） | 否（§6 与 `docs/algorithms/PHASE3_RESAMPLE.md`、`docs/plugins/algorithms_phase3/` 分工未声明） | 默认值/值域与 `eng/packaging/config/defaults.json`＋`docs/contracts/CONFIG_CONTRACT.md` 重叠（§4.3 声明默认值唯一正本在 defaults.json，实测 defaults.json **无** `sub_block_px/queue_depth/max_tiles` 键）[D-06] | 有：:5–:6（`> ID: ARCH-P3-001 状态: FROZEN 上游… 下游…` ＋「原则: 科学选择全部落在 ALG-007 冻结公式…本架构只定义…」自我权威分工声明）——**机械列漏报** | `:5` 任务编号/状态 FROZEN；`:40` 「（内存守卫）」内部代号无定义；`:41` 「(07 资源门联动)」编号文档；`:52` 「ARCH-003 §6-2 同款」 | `:8` 「lib/phase3,四单元单向流」⇒ **`lib/phase3` 既不在跟踪集也不在磁盘上**（`git ls-files lib \| cut -d/ -f2 \| sort -u` 只含 algorithms/include/infrastructure/phase{1,2,3}_session/third_party；实际实现在 `lib/algorithms/resample/p3_resample.cpp`、`lib/algorithms/fits_output/p3_output.cpp`，MODULE_MAP §3 明说「原 `lib/phase3_session/` 路径已随投影/重采样迁出删除」）；`:5/:29/:32/:46/:63` 引用 `ARCH-002/ARCH-003/ARCH-004` ⇒ 仓内无该三份文档（`git ls-files \| grep -E "ARCH-00[2-4]"` 0 命中，同一条正/负例式子对 `ARCH-001` 命中）；`:5` 下游 `API-005/CODE-P3/SYN-007` 同样无落点 | §8.3 export 行「子块流式…内存占用与子块大小成正比、与总图大小无关」本文件 :38–:39 引用正确；但 :40 冻结的默认值与值域属配置合同面（§4.3「默认值与取值域正本 = defaults.json 与 CONFIG_CONTRACT.md」），本文另立一份数值 ⇒ 数值第二正本；:63 把「并发/内存上界」正本判给 `THREAD_BUDGET_ARCH §2/§3`，而该文件自身声明「本文件为线程架构权威」⇒ 上链互指 | 迁移（→`docs/plugins/algorithms_phase3/`）＋删默认值副本——依标准 01 §2 表行 4＋§4「数值表下沉对应下层文档」 |
| docs/architecture/PIPELINE.md | ACSD Pipeline（Phase1 → Phase2） | 63 | 架构设计（数据如何流动）——目录正确 | 三阶段节点序与入口 | 有：:3 `§8`；:5 「本文按最高设计 §3.2 / §4.2 / §4.5 / §6.2 / §8.2 描述」 | 2：`docs/ci/CI_SPEC.md`＋审计账目（`docs/audit/*`）；**无 `docs/` 设计层消费者**（索引也只记 audit） | 否（§4.2/§5.2 的节点序正本在最高设计与 `docs/plugins/algorithms_phase{1,2}/`，本文是复述层） | 与 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`（§8.2 命名块承接）主题重叠且无互指；与 `docs/plugins/algorithms_phase2/12_rejection.md` §9 排异档位表重叠 [D-07] | 有：:5–:8 三行「本文按…描述／入口只有一个／阶段内走内存块管线」的纪律声明块——**机械列漏报** | `:62` 「序列化：V11（外部 oracle 冻结）；sampler/UPM：V13/V14（内部指代）」版本锚；`:42` 「g_k≡1 本期不启用」期次叙事；`:56` 「属**现行设计缺口**：20 个生产节点中命名块管线覆盖率 = 1/20…本条款的满足声明以覆盖率达成为条件」状态/缺口预写（§12.5 状态须现场计算） | `:63` 「最高设计 §7.1/§10.1」⇒ **§10.1 不存在**（§10 无子节）；`:60` 「最高设计 §0.1/§1.1」中 §1.1 是「是什么」，不承载「科学冻结权威」条款（该条在 §0.1）；`:5` 声明覆盖 §6.2（export 流程）但全文无 Phase3 节 ⇒ 上游标注与内容不符 | **§5.5**「逐像素档位表（档界与算法名）的**唯一正本** = `docs/plugins/algorithms_phase2/12_rejection.md` §9（本节不复制档界与取值）」，本文件 :49–:52 一边写「只引用，不复制」一边把三档档界与算法名整表复制，且把档界归因为「取自 WBPP 2.5.9」，而正本 §9 明写生产档「由四档收成三档」的依据是仓内受控评估（`run/REJECT-DOCFIX-01/REPORT.md`）、WBPP 档界只作 `N>15 → linear fit` 的**对照档** ⇒ 归因与上位相反＋数值表第二处；文件内部另含互斥节点序（:13–:21 platesolve 在 star_detection 前 vs :33 `calibrate/cosmetic_correct/detect_sources/plate_solve/…` 顺序相反）[D-07] | 保留（删排异档界复制与 V11/V13/V14、缺口计数段，统一为指针）——依标准 01 §1「冲突本身是缺陷」＋§3「唯一索引地图/不复制数值」 |
| docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv | （无标题，机器清单） | 562 | 标准与检查／操作与运营（生成清单）⇒ 角色错放，应迁往 `eng/` 或 `artifacts/evidence/`，正式文档层不放再生件 | 生产可达性/线程创建/锁/IO 写点逐符号登记 | 缺（CSV 无「上游」位；索引代标 §8；`risk_note` 内有 1 行写「最高设计 §6.2」＝错指）[D-08] | 11（`eng/ci/{INVENTORY_REPORT.md,check_spec_named_impl.py,ci_repair_round.py,spec_named_impls.json}`、`eng/tests/arch/{arch_invariants,test_inventory,test_inventory_generator,test_single_cli}.py`、`eng/tools/arch/build_production_execution_inventory.py`、`docs/owner/PIPELINE_OVERVIEW.md` 等） | 是（生产可达性清单唯一件） | 与 `execution_inventory.csv`（同目录）同主题且结论相反 [D-16] | 无（CSV） | 列 `risk_note` 内：`CLEAN-401 B2 / CONFORM-SWEEP-4 P3X-06 …待 CLEAN-401 退役处置`（任务编号＋退役叙事）、`V5: 禁硬编码线程数→ARCH-003 backend dispatcher 收编`×26、`发布面唯一入口（ARCHITECTURE 不变量 1 / 最高设计 §6.2）` | 自身无文件路径悬空；`:thread_model` 26 行写「thread count via runtime/**orchestrator** set_num_threads」，指向已被本清单自身判 `no`（`exe_target,orchestrator_*,…,tool,no`）的组件；`risk_note` 的补救落点 `ARCH-003` 文档不存在 | ① §9「worker 数只来自 profile 与预算对象」＋THREAD_BUDGET_ARCH §5「`omp_set_num_threads(`/`num_threads(` 字面量=0 容忍」——本清单实测 `thread_creation` 类 99 行中 **production_reachable=yes 63 行**，其中 `std::async` 53 行（`lib/algorithms/coverage\|drizzle\|integration\|…*` 18 行属科学模块），与 §9「科学计算里不用 async/future」正面冲突；② §9「一个进程一个资源预算源」——`lib/infrastructure/benchmark/cpu/{baseline,avx2,avx512}/src/*_provider.cpp` 三行被判 `production/yes`，而这三个源**不在构建图**（`lib/infrastructure/benchmark/cpu/` 下无 `CMakeLists.txt`；根 `CMakeLists.txt:345,702-709` 的 provider 源是 `backend_host/baseline_backend.cpp`）⇒ 「现场计算的可达性」与构建图不一致；③ THREAD_BUDGET_ARCH :12 声称本清单「risk_note 列逐行登记」⇒ 实测 561 行中 **335 行 risk_note 为空** | 迁移（→`artifacts/evidence/` 或 `eng/ci/` 并由正本引用；同步 11 处消费者路径）——依标准 01 §2 末条＋§3「索引与文件树一致」 |
| docs/architecture/THREADING_MODEL.md | Threading Model | 128 | 架构设计（分层与确定性锚点）——目录正确 | 线程分层、并行轴分配、归约序锚点 | 有：:3 `§8`（正文 :42/:53 实际承接 §9 的线程预算与 1/N 等价条款） | 8：`docs/algorithms/{PHASE3_RESAMPLE,PLATESOLVE,STAR_PSF_ALGORITHMS}.md`、`docs/plugins/infrastructure/19_runtime.md`、`docs/standards/CONCURRENCY_STANDARD.md`、`lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp`、`lib/infrastructure/scheduler/src/module_adapters.cpp` 等 | 是（线程分层与确定性锚点正本候选） | 与 THREAD_BUDGET_ARCH.md（:6 声明要取代它）、PERFORMANCE_MODEL.md §5（同公式）三处重叠 [D-09] | 有：:4–:8「生产执行层（唯一）」等条文本属正文，但 :26/:91 两个二级标题＝任务编号＋「冻结口径」⇒ 版本/任务锚混入结构 | `:26` 「### 并行轴分配（PERF-501 冻结口径）」、`:91` 「### 并发度 A/B 对照的可复现性（P1-CONCURRENCY-CALIB-01 冻结口径）」、`:42` 「PERF-501 实测」、`:111` `build_source_digest=f86d60e2…`（sha）、`:61` 「已被实测否决（P1-PARALLEL-AXIS-REDESIGN-01）」、`:76` 「分配式的改动依据 = 实测收益记录」（过程叙事）、`:12`/`:15` 删除线 `~~ACR…~~`/`~~浏览器…~~`＋「DORMANT」「工具分类」留痕（§0.3 撤销设计不出现在正式文档）、`:122–:124` 源码行锚（`upm.cpp:495`、`sampler.cpp:924-954`、`drizzle_engine.cpp:1662/1751/1834/1843`） | `:63` `run/P1-PARALLEL-AXIS-REDESIGN-01/REPORT.md`、`:110` `run/P1-CONCURRENCY-CALIB-01/REPORT.md` ⇒ 两条实测证据锚均不存在（gitignore 过程目录）；`:100` 「`docs/VERSIONING.md` §2.1」文件存在（§2.1 存在性未逐节核对，见 §K-3） | §9「两轴之积即同时真正在算的线程数」vs 本文件 :29「两轴之和必须 ≤ Runtime lease 给出的预算」⇒ 措辞与上位相反（式 :36 又写乘积）；§9「worker 数只来自 profile 与预算对象，模块不取 hardware_concurrency」——本文件 :21 只写「线程数由 run context 配置」，未承接该禁令，而 :123 记录 `sampler` 有 std::thread worker 池，与 THREAD_BUDGET_ARCH §2 表「Phase2 sampler(串行 reference)」互相矛盾 [D-09] | 保留（作为线程分层正本；删两份 `run/` 报告锚与删除线留痕，实测表迁 evidence）——依标准 01 §4「只写现行设计」＋§2 末条 |
| docs/architecture/THREAD_BUDGET_ARCH.md | 全局 Thread Budget 与执行架构 (V5) | 48 | 架构设计（线程预算）——目录正确 | 全局预算、阶段执行画像、异步与取消、静态 checker 合同 | 缺（抬头 `§8` 与标题 `(V5)`；:5 `上游: ARCH-002/ARCH-003` 均不存在）[D-10] | 3：`eng/ci/check_run_manifest_schema.py`、`eng/tools/quality/contracts/check_execution_contracts.py`、`lib/algorithms/coverage/include/astro/phase2/sky_plane.h` | 否（与 THREADING_MODEL.md 同一主题，且自己声明要压过它） | THREADING_MODEL.md（分层/确定性锚点）、PERFORMANCE_MODEL.md（预算与并发）[D-10] | 有：:5–:6 两行（`> ID: ARCH-THREAD-001 状态: FROZEN` ＋「本文件为线程架构权威…冲突处以本文件为准」）——**机械列漏报** | `:1` 标题版本锚 V5；`:5` ID/状态 FROZEN；`:32` 「§4 并发正确性合同(**承接旧锚点**)」；`:47–:48` 「文档: …／任务: ARCH-004(本文件)/BENCH-003/BENCH-004/ABI-001」任务编号列表 | `:5` `ARCH-002`、`:10/:12` `ARCH-003`、`:12` 「ARCH-001 清单」、`:40/:48` `ARCH-004` ⇒ ARCH-002/003/004 不在跟踪集（`git ls-files \| grep -cE "ARCH-00[2-4]"` = 0，同式对 `ARCH-001` = 2 ⇒ 命令可用）；`:47` 「07_RESOURCE_MONITOR」非任何文件路径（实际件名 `observability/RESOURCE_MONITORING_CONTRACT.md`）；`:41` 括号内「当前: orchestrator watchdog/resource_monitor/logger」与实际门 `eng/tools/arch/check_thread_budget.py` 的 `EXEMPT`(4 项)＋`REGISTERED`(~20 文件级带注记)两套登记表不符 | **本批主违规**：:6「本文件为线程架构权威;THREADING_MODEL.md 的分层与确定性锚点保留有效,冲突处以本文件为准」⇒ 标准 01 §1「架构问题的唯一权威是最高设计 §8」「任何文档不声明自己的第二套权威顺序」＋「下位与上位冲突时以上位为准」被反向写成「兄弟文档冲突以下位本文件为准」；§9「一个进程一个资源调度器与线程预算源」——:11 给后台服务（watchdog/资源监控/progress 日志）「恒 1 线程+独立小预算(不入科学预算池)」＝第二个预算池，上位无该要点 ⇒ 孤儿决策（须先补 §9 要点）；:22 Phase2「sampler(串行 reference)」与 THREADING_MODEL :123 的 worker 池实现相反 | 合并（权威声明删除、预算条款并入 THREADING_MODEL.md 单一正本，执行画像表并入 `docs/contracts/SCHEDULER_CONTRACT.md`）——依标准 01 §1 第 2/4 条 |
| docs/architecture/abi/ABI_003_SECURE_LOADER.md | Secure Module Loader (ABI-003) | 113 | 架构设计（加载契约/接口）⇒ 应落 `docs/interfaces/`（现址 `docs/architecture/abi/` 与 §2 表行 3 相容性差） | 动态库加载门、信任边界、错误码、测试清单 | 有（但错）：:4 「上游: `ASTROCS_DESIGN.md` **§7.3（版本化 C ABI/DLL 边界）**」——§7.3 实为「错误传播与运行日志」；真正承接点是 §8.5（模块与 ABI）与 §8.4 末条（加载前固定检查）[D-11] | **0**（除索引与过程账目外无引用者；命令见 §B 前言）⇒ 孤儿文档 | 是（安全 loader 契约仅此一份） | `docs/standards/C_ABI_STANDARD.md`（:4 列为上游之一，存在）、§8.4 末条 | 有：:3–:9 七行块（`ID/状态 FROZEN/上游/下游/实现/测试/权威参考`）——**机械列 meta_block=False 漏报** | `:3` 状态 FROZEN 预写；`:6` 下游「ABI-004、ABI-005、CLI-001 modules/doctor」任务编号；`:50` 标题「Windows 契约(实现由 WIN-* 落地)」；`:63` 「不伪装实现完成」；`:108` 「非目标(不伪装完成)」＋ `:110–:113` WIN-*/ABI-004/ABI-005/CPU-002 任务面；`:83` 标题「测试清单(36 checks)」与表内 17 个场景行不符 | `:4` 上游条款号 §7.3 与标题不符（内容级悬空）；`:40` 「(12 §3: module 不自行开任意路径)」——`12` 编号文档不在跟踪集（§H-0）；`:47` 「FIPS 附录 B 向量自检」未给可核文件 | §7.1 命令树（唯一）只有 `help/--version/doctor/benchmark` 三命令族，本文 :6 把 `CLI-001 modules/doctor` 的 `modules` 子命令当下游消费者 ⇒ 引用命令树之外的入口（MODULE_MAP §1.1 也自述该通道「在本版不由三个命令在运行期 dlopen」）；§8.4「动态库加载前过固定检查（…manifest、哈希、ABI）」为无条件门，本文 :88 测试行 P2「manifest 未登记 sha(SKELETON 兼容) → LOAD_OK」＝对哈希门开豁免，上位无该例外 ⇒ 须先在 §8.4 补要点或改文档 | 迁移（→`docs/interfaces/ABI_SECURE_LOADER.md`，抬头上游改 §8.4/§8.5）＋登记为孤儿待承接——依标准 01 §3 第 1、2 条（追溯不到的机制先补要点再保留） |
| docs/architecture/api_inventory.csv | （无标题，机器清单） | 449 | 标准与检查（符号登记表，供 `check_doc_symbols.py` 消费）⇒ 角色错放，正式文档层不放扫描副产品 | 公开头符号↔签名↔导出宏 | 缺（CSV；索引代标 §8）[D-12] | 4：`docs/contracts/PUBLIC_API.md`、`docs/modules/star_detector.md`、`eng/tools/api_doc_consistency.py`、`eng/tools/gen_v19_evidence.py`＋`check_doc_symbols.py` | 是（符号面清单） | `docs/contracts/PUBLIC_API.md`（148 KB，同一符号面的可读面）＋`docs/contracts/API_CONTRACTS.csv`（381 行逐符号合同）⇒ 三件载同一事实，无互指 [D-12] | 无（CSV） | 无日期/任务编号；文件本身洁净（再生件） | 无路径级悬空 | 448 行中 **4 行为枚举常量**（`ACS_ERR_CANCELLED/ACS_ERR_BUDGET/ACS_ERR_IO/ACS_ERR_UNSUPPORTED`，signature 形如 `enum acs_status { … }`）与 1 行 `#define ACS_CPU_AVX2_REQUIRED_FEATURES …`，与 `doc_symbol_namespaces.json` :18 声称的「api_inventory.csv **只登记函数签名**」相反 ⇒ 下游判据的前提不成立；4 个重复 symbol（`instance`、`get_timestamp`、`set_checkpoint_dir`、`void`），其中 symbol=`void` 是解析残迹 ⇒ 与 API-001 §6「AST 提取与 API index 零漂移」的验收口径不符 | 迁移（→`eng/tools/quality/contracts/` 随其消费者，或 `artifacts/evidence/`）——依标准 01 §2 末条＋§3「与文件树一致」 |
| docs/architecture/cpu/CPU_001_CAPABILITY_PROBE.md | CPU 能力探测与安全矩阵 (CPU-001) | 104 | 架构设计（能力探测契约）——但描述的是未进构建图的子系统，实际角色更像模块工作细节 ⇒ 应落 `docs/modules/`（benchmark/cpu 页） | CPUID/XGETBV 探测、os_safe 平面、schema、判定规则 | 有（但号错）：:4 「`ASTROCS_DESIGN.md` §8（**CPU 后端与资源**）」——§8 标题是「软件架构」，「CPU 后端与资源」是 §9；`docs/DOCUMENT_INDEX.yaml` 同条目已正确写 §9 ⇒ 文档抬头与索引不同源 [D-13] | 5：`docs/traceability/TRACEABILITY_MATRIX.{csv,json}`、`lib/infrastructure/benchmark/cpu/common/README.md`、`…/cpu/common/include/astrocs/cpu/capability_v1.h`＋索引 | 否（`docs/architecture/CPU_BACKEND_ARCH.md`〔别批〕同述后端/provider 面） | `CPU_003_AVX2_PROVIDER.md`（同一探测层）、`capability_v1.h`（位语义数值表）[D-13] | 有：:3–:8 六行（`ID/状态 FROZEN/上游×3/下游/实现/测试`）——**机械列漏报** | `:3` 状态 FROZEN；`:16` 「验收项（04_CPU_RESOURCE_TASKS.md CPU-001）」任务卡引用；`:24` 「（15 §2 原文语义）」；`:36` 「（C §C6 / 15 §2 / CPU-004 验收…）」；`:39` 「本层不读取逻辑核数、affinity、cgroup（CPU-008）」；`:66` 「实机验证由 WIN-* 承担」；`:96` 标题「与相邻任务边界」 | `:5` `03_TARGET_PRODUCT_AND_ARCHITECTURE.md`、`:16` `04_CPU_RESOURCE_TASKS.md`、`:24/:36` 「15」「C §C6」——均不在跟踪集（`git ls-files \\| grep -cE "0[3-5]_[A-Z]*(TARGET\\|CPU\\|RESOURCE)"` = 0）⇒ 本文「验收项」的规范来源整体悬空 | 位语义表 :50–:54 与 `lib/infrastructure/benchmark/cpu/common/include/astrocs/cpu/capability_v1.h:50-62` 逐值一致（已核）**但同表在文档与头文件两处冻结**⇒ 漂移风险；§8/§9 未定义「provider 子系统」这一层（`benchmark/cpu/*` 无 `CMakeLists.txt`，根构建只用 `backend_host/`）⇒ 该文档描述的机制在上位追溯不到要点（孤儿机制），且与 `ISA_VARIANTS.md` 的 backend_host 变体机制构成双正本 | 保留前须先由负责人裁「provider vs backend_host 变体」哪条是生产机制；文档侧：迁移（→`docs/modules/`）＋上游号改 §9——依标准 01 §3 第 2 条＋§1 冲突即缺陷 |
| docs/architecture/cpu/CPU_003_AVX2_PROVIDER.md | CPU AVX2/FMA provider（热点 kernel 后端）(CPU-003) | 158 | 架构设计（provider 契约）＋实测记录混合 ⇒ 实测面应落 `docs/performance/`/`artifacts/evidence/` | AVX2/FMA provider 注册、加载门、容差、编译隔离 | 有（同 CPU_001 号错）：:4 「`ASTROCS_DESIGN.md` §8 · `ENGINEERING_SPEC.md` §10 · docs/architecture/CPU_BACKEND_ARCH.md」[D-14] | 4：`eng/tests/cpu/avx2/run_provider_avx2_checks.py`、`lib/infrastructure/benchmark/backend_host/profile_gen_v2.cpp`、`…/cpu/avx2/include/astrocs/cpu/avx2_provider_v1.h`、`…/cpu/avx2/src/avx2_provider.cpp` | 否（§2 实测表是 ISA_VARIANTS 的副本） | **ISA_VARIANTS.md §1 表**——两处段落见 [D-02]；`CPU_001` 的探测平面；`backend_host/avx2_backend.cpp` 机制 | 有：:3–:13 十一行（`ID/状态 FROZEN/上游×4/下游/实现/测试/profile 台账`）——**机械列漏报** | `:3` FROZEN；`:21` 「验收项（04_CPU_RESOURCE_TASKS.md CPU-003）」；`:29/:31` 「ISA-001/003 实测台账」＋ `vm-bj`；`:49` 「恰 2 个 kernel（v1 冻结）」；`:57` 「（02 §10.1 / 15 §1 / 约束 §C4）」；`:81` 「本机 Xeon Gold 6148」；`:85` 「15 §6 / 约束 §C5」；`:110` 「（15 §6 / 约束 §E3）」；`:124–:129` 本机 objdump 实测计数；`:154` 「WIN-* 域」 | `:4` 引用 `docs/architecture/CPU_BACKEND_ARCH.md`（存在，别批）；`:21/:57/:85/:110` 「02 §10.1／15 §1/§6／约束 §C4/§C5/§E3」全部无落点（不在跟踪集，§H-0）；`:13` 「artifacts/evidence/prerelease-v5/ISA-001/MEASUREMENTS.csv」在跟踪集中存在（非悬空） | §9「支持…是能力上限，发货档由 benchmark 实测台账决定」——本文件 :44–:49 把发货档写成自身冻结（「恰 2 个 kernel（v1 冻结）」），与 ISA_VARIANTS §2「逐 kernel 选路(BENCH-005)」构成两处平行决策记录；实测机器名 `Xeon Gold 6148`（:81,:124）与 ISA_VARIANTS 的 `vm-bj, 2 vCPU`（:26）环境命名不一致，无同一性说明；所描述的 `cpu/avx2` provider 源不在构建图（见 D-08 证据），文档却以「已交付合同」口径陈述 | 合并（实测表删除改指针；provider 契约并入 CPU 后端正本）——依标准 01 §2「同一角色内同一主题只有一份正本」 |
| docs/architecture/doc_symbol_namespaces.json | （无标题，checker 登记表） | 48 | 标准与检查（`check_doc_symbols.py` 的命名空间登记）⇒ 角色错放，应随检查器落 `eng/tools/quality/contracts/` 或 `docs/standards/` | 非 API 符号命名空间豁免登记 | 缺（CSV/JSON 无上游位；索引代标 §8）[D-15] | 1：`eng/tools/quality/contracts/check_doc_symbols.py` | 是（该门的唯一登记表） | `docs/GLOSSARY.md`（:13 自称「唯一术语权威」）、`api_inventory.csv`（:18 声称其只登记函数签名，已证伪见 D-12） | 无（JSON） | `:4` `"task": "W4-A3"` 任务编号入库；`:13` 「docs/GLOSSARY.md = **唯一术语权威**（冻结定义，18 术语，机器门 eng/tools/check_glossary.py）」＝声明唯一口径＋预写术语条数 | 无路径悬空（7 个引用路径全部存在）；`:26/:32` evidence `docs/GLOSSARY.md:13` ⇒ 实测 `snr_v` 在 **`:15`**（:13 是 `ivar` 行）；`:38` evidence `module_adapters.cpp:1040-1057` ⇒ 实测 `p2_write_descriptor` 定义在 **`:1162`**（:1040 为空行、:1057 为 `d.sci_id`），且 :39 自述「行锚 = 同面实测 grep → :1040」为假；`:44` evidence `PHASE2_DETAILED_DESIGN.md:19` ⇒ 实测 **`:21`**；`:45` 「现行承载 PIPELINE_BLOCK_CONTRACT.md:46」⇒ 实测 **`:58`**（`07_noise_snr.md:97` 正确）⇒ 4 条登记 3 条行锚漂移，本表 :3 自称「每条必须在 evidence 所指文件里逐字出现（否则判红）」——门只判文件级出现，行锚不受判据保护 | 上位无「文档符号命名空间门」条款（§8 全节无该机制）⇒ 机制为工程内建，放正式文档层属角色错放；无对最高设计的上游可标 | 迁移（→`eng/tools/quality/contracts/doc_symbol_namespaces.json`，随行锚改指针或改由门计算）——依标准 01 §2 表行 6（标准与检查）＋§4（无任务编号） |
| docs/architecture/execution_inventory.csv | （无标题，标称机器清单） | 15 | 标准与检查／过程台账（调用链与并行度登记）⇒ 角色错放且内容已退役 | Phase1/2 逐阶段入口、循环、线程机制、调用链 | 缺（CSV；索引代标 §8）[D-16] | 1：`docs/science/CONTROL_WEIGHT_SNR.md`（其余为账目类） ⇒ 近乎孤儿 | 否（职能已由 PRODUCTION_EXECUTION_INVENTORY.csv 承担） | **PRODUCTION_EXECUTION_INVENTORY.csv**（同主题：生产可达性与线程机制），结论相反 [D-16] | 无（CSV） | 无日期；每行 `evidence` 列尾「｜ 生产可达性一律由 eng/tools/arch/build_production_execution_inventory.py 现场计算并落 docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv）**）**：yes/no」——括号不配对（模板拼接残迹）＋把「现场计算的结论」手抄进非该脚本产物的文件 | `stage1/stage2` 配置键（见下行冲突）；`orchestrator.cpp:4949/5064/5177-5185/776/1758/2474/3237/4181` 等行锚指向 `lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp`（存在，5522 行，与 ARCH-001 §7「5522 行」一致）⇒ 行锚本身可核；但 :3/:4/:5 引用 `ipv_entry.cpp:524`、`pc_api.cpp:1048/1084` 等未逐个复核（登记为未核，见 §K-3） | ① 本文件对 `Orchestrator::run_stage_calibrate/platesolve/photometric/drizzle/snr` 逐行写「生产可达性…：yes」，而同目录 PRODUCTION_EXECUTION_INVENTORY.csv 对 `exe_target,orchestrator_*` 判 **`tool,no`「非发布目标」**、MODULE_MAP §4 写 `lib/infrastructure/pipeline/orchestrator`「历史保留…无独立 exe」、`production_call_paths_stage1.csv:2` 写「历史登记：main(orchestrator.exe <stage1.json>) / Orchestrator::run 已非入口」⇒ 四件互斥，本文件是其中的过期件；② 其 `config_gate` 面（stage1./stage2.）在生产 schema 中不存在（见 D-21） | 删除——依标准 01 §2 末条「已撤销设计不放正式文档层」＋§3「索引与文件树一致」（它登记为 GENERATED 却无任何 eng/ 工具读写：`git grep -l execution_inventory -- eng` 0 命中） |
| docs/architecture/execution_options_contract.md | CON-002 全局 worker 预算合同 (ExecutionOptions) | 71 | 合同说明（一个结构体的字段合同）⇒ 应落 `docs/contracts/`（与 §8.5/PIPELINE_BLOCK_CONTRACT 同址） | `ExecutionOptions` 四字段、配置块、CLI 覆盖 | 有：:3 `§8`；正文另引 §6.2、§8（错指，见下行）[D-17] | **0**（除索引与过程账目外无引用者）⇒ 孤儿文档 | 否（预算面正本在 §9 与 `docs/contracts/SCHEDULER_CONTRACT.md`） | THREAD_BUDGET_ARCH.md §1（同一「唯一预算源」条款）、`docs/contracts/SCHEDULER_CONTRACT.md` | 有：:5–:7「唯一执行预算对象…以此为唯一来源」＋:9–:15「⚠ **预算唯一来源**…」两节纪律声明——**机械列 meta_block=False 漏报** | `:1` 标题含任务编号 CON-002；`:65` 「**代码侧现状（登记）**：`execution_options.h` 仍含 `gpu_route`（:18/:43）与 `hardware_concurrency` 默认（:16/:24/:38）；文档与合同侧不承认这些键」＝状态/缺口预写（§12.5 状态现场计算；§4 只写现行设计） | `:53–:56` 「**唯一 CLI 入口 = `acsd`**…`mosaic` 子命令接受同名字段的 CLI 覆盖：`--cpu-workers N` / `--io-workers N` / `--deterministic 0\|1`」⇒ 这三个 flag 的实际持有者是**第二个可执行入口** `astrocs-stage2 <stage2.json>`（`lib/algorithms/coverage/tools/stage2.cpp:140,168-169`），`acsd` 侧 0 命中（`git grep -n "cpu-workers" -- lib/infrastructure/cli` 0 命中；同式子对 `workers` 在 `cli/commands.cpp` 命中 25 ⇒ 命令可用，非检索失效）⇒ 本文把退役工具的 flag 面挂到唯一 CLI 上，而同一文件 :13/:64 又声明「第二个可执行入口不在合同面内」（§1.2/§7.1 只认一个入口）；`:49` 「schema 见 `eng/contracts/schemas/phase_config_*.schema.json`」为通配写法（三份实存：normalize/mosaic/export）——**机械列把通配当悬空，属误报** | §7.1「命令树（唯一）」不含这三个 flag，且 flag 的实际持有者是 §1.3/§8.1 禁止出现在产品面的第二入口 ⇒ 文档另立 CLI 面；§9「worker 数只来自 profile 与预算对象」＋AGENTS §6「不硬编码线程」——本文件 :34–:47 允许 `stage2.json` 顶层 `execution.cpu_workers` 与 CLI 覆盖 ⇒ 第二/第三预算来源；:13/:53 两处把机制上游记为「最高设计 §6.2」（§6.2 是 export 流程）⇒ 权威条款指错；§3.2/§4.3 配置文件名口径为 `phase_config.json`＋blocks[]，本文件写 `stage2.json` 顶层块——该文件确为 `astrocs-stage2` 的入参名，即本文整体登记的是**待退役第二入口**的合同面（LEG-003/CLI-002 在册） | 合并（字段表并入 `docs/contracts/` 单一执行合同；删 CLI 覆盖与代码现状登记）——依标准 01 §1「不另立权威」＋§2 表行 5 |
| docs/architecture/observability/RESOURCE_MONITORING_CONTRACT.md | ACSD 资源监控伴随器合同（LOG-002） | 185 | 合同说明（CSV 工件列合同）⇒ 应落 `docs/contracts/`（同域 `LOG_AND_ERROR_CONTRACT.md` 即在此） | `monitor_timeseries.csv` 21 列、指纹链、真实观测接线、双平台分离 | 有但缺承接条款：:3 「§8.1（**顶层结构**）、§9」——§8.1 实为「总原则：唯一 CLI 入口、阶段独立调度器」，「顶层结构」是 §8.4；资源监控与运行图的上位条款在 **§10**（「每次运行生成资源时序…」「资源监控伴随重计算运行，监控数据不可事后编造」），上游行未列 §10 [D-18] | 4：`docs/api/CLI_PROTOCOL_V1.md`、`docs/architecture/observability/RUN_GRAPH_CONTRACT.md`、`docs/design/LOG_AND_ERROR_SYSTEM.md`、`lib/infrastructure/observability/monitoring/windows_pdh_etw.py` | 是（监控伴随器合同正本） | STRUCTURED_LOGGING_CONTRACT.md（同为 LOG 域）、`docs/contracts/LOG_AND_ERROR_CONTRACT.md`、`docs/design/LOG_AND_ERROR_SYSTEM.md` ⇒ LOG 域四件，分工仅靠散文互指 | 有：:5–:9「机器可读事实源…本文档是视图；字段定义与验收以 schema/检查器为权威」纪律声明；:43「⚠ 强制消歧（**唯一口径**）」——**机械列漏报** | `:19` 「本任务不改科学公式（`scientific_change=false`）」；`:26` 「不触碰 LOG-001/RT-006 已冻结语义」；`:158` 「未来 FATDUCK/Windows 控制节点填充」＋「known_limits」；`:167` 「（本任务交付 CSV + 指纹 + 校验闭环）」；`:124` salt 常量 `b"astrocs-log002-v1"` 版本锚 | 无路径悬空（11 条引用路径全部存在）；但 `:47` 行锚「`lib/infrastructure/cli/resource_recorder.h:260-266`（20 列）」**错位**：该区间是统计聚合代码，CSV 列名字面量实在 **:288-293**（实测列数 20，与文档一致，锚号不对） | §1 末条「任何文档不声明自己的『唯一口径』」——:43 原文声明；:52–:53 内部自相矛盾：「本节的 21 列合同只覆盖 **`resource_timeseries.csv`** 上」与 :47–:48 表格（21 列属 `monitor_timeseries.csv`、20 列属 `resource_timeseries.csv`）直接相反；工件名 `monitor_timeseries.csv` 在 `lib/`＋`eng/` **零出现**（`git grep -n monitor_timeseries -- lib eng` 0 命中；`monitor.py` 只按调用方给的路径写文件），文档却称「本合同的 CSV 工件名固定为 `monitor_timeseries.csv`」⇒ 名称无实现落点；上位 §10 只承认「资源时序」一类工件，两份资源时序工件并存（`resource_timeseries.csv` 由 cli 生产、`monitor_timeseries.csv` 由监控伴随器）且一份的列合同「只有一处＝代码行号」⇒ 权威倒置（标准 01 §3 要求下层文档承接、§4 源码行号下沉） | 迁移（→`docs/contracts/RESOURCE_MONITORING_CONTRACT.md`）＋订正上游为 §9/§10＋删「唯一口径」措辞与自相矛盾句——依标准 01 §1＋§2 表行 5 |
| docs/architecture/observability/RUN_GRAPH_CONTRACT.md | ACSD 运行图渲染工具合同（LOG-003） | 173 | 操作与运营（诊断工具合同）⇒ 角色错放，应迁往 `docs/diagnostics/` | plan/trace→DOT/SVG/JSON 渲染与一致性验证 | 有但缺承接：:3 「§8.1（顶层结构）、§9」，而运行图的上位在 **§10**（「运行图（plan 是预期、trace 是实际，分别如实记录）」）[D-19] | 1：`docs/design/LOG_AND_ERROR_SYSTEM.md` ⇒ 近乎孤儿 | 否（工具合同性质，规范来源应在 §10 的运行图条款） | RESOURCE_MONITORING_CONTRACT.md（同 trace 事件面）、`lib/infrastructure/pipeline/trace_replay.py`（聚合语义双写：:103「语义与 `trace_replay.py` 对齐」、:134–:136 又逐条重述聚合规则） | 有：:5–:8「机器可读事实源…本文档是视图；字段定义与验收以工具/标准为权威」——**机械列漏报** | `:17` 「验收（依据=ASTROCS_DESIGN.md §6.3 **+ 本文件**）」；`:24` 「Doxygen/Graphviz 仅生成文档…`scientific_change` 恒 false」；`:26` 「本任务不改科学公式/运行调度」；`:47/:74` 工具版本字面量 `v1.0.0`；`:139` 标题「**旧**手绘图不再作为规范来源」＋`:14` 「旧手绘图/静态架构示意图不再作为规范来源」＋`:142` 「（归档性质，非当前规范来源）」历史叙事 | `:141` 「`evidence/**` 下的 `*.dot`（如 `v6_1_rework` PROD_REACHABILITY）」——跟踪集中既无该相对路径也无该文件名（`git ls-files \| grep -ic v6_1_rework` = 0）；`:172` `14_RUNTIME_SCHEDULER_AND_TRACE_STANDARD.md`、`:173` `23_GRAPH_AND_DOC_TOOL_POLICY.md` 均不在跟踪集（§H-0）；`:31/:44/:64` 「policy §1/§2/§2.5/§4」无落点；`:149` 粗体标记不配对（`作证**（标准 14 §5）。`） | **本批第二严重另立权威**：:144–:147 自行裁定「运行图规范来源 = 本工具+本合同」，并把同层文档判为非规范——「静态架构示意图（`docs/architecture/DATA_FLOW.md` 等 ASCII 流程、ARCH-001 mermaid、历史 evidence DOT）是**信息性视图，不作运行事实规范来源**」⇒ 由一份工具合同对架构文档排定规范等级，违反标准 01 §1「架构问题的唯一权威是 §8」「任何文档不声明自己的第二套权威顺序」；:17/:166 上游写 §6.3（§6.3 是投影算法）⇒ 权威条款指错 | 迁移（→`docs/diagnostics/RUN_GRAPH_TOOL_CONTRACT.md`，删 §8 排等级段与「＋本文件」自证）——依标准 01 §2 表行 8＋§1 |
| docs/architecture/observability/STRUCTURED_LOGGING_CONTRACT.md | ACSD 结构化日志合同（LOG-001） | 169 | 合同说明（JSONL 日志行字段合同）⇒ 应落 `docs/contracts/`（与 `LOG_AND_ERROR_CONTRACT.md` 同址） | `astrocs.log.event.v1` 字段表、seq、脱敏、大小上限 | 有但错：:3 「§8.1（顶层结构）、§9」，日志的上位在 **§7.3**（「机器日志 = 结构化日志合同行（`astrocs.log.event.v1`，实现家 `lib/infrastructure/observability/`）」），本文 :130 自己正确引了 §7.3 ⇒ 抬头与正文互斥 [D-20] | 5：`docs/api/CLI_PROTOCOL_V1.md`、`docs/contracts/API-001.md`、`docs/contracts/LOG_AND_ERROR_CONTRACT.md`、`docs/design/LOG_AND_ERROR_SYSTEM.md`、`lib/infrastructure/observability/logging/README.md`（＋`eng/ci/ledgers/log_system_ledger.json`） | 是（`astrocs.log.event.v1` 字段合同正本；`LOG_AND_ERROR_CONTRACT.md:6/:27/:51/:150` 明确以本文为正本并声明「不另立规则」＝合法分层，不算重复） | STRUCTURED_LOGGING↔API-001 §3（运行事件流字段两面在两份文档各述一遍：本文 :29–:36、API-001 :53） | 有：:5–:7「机器可读事实源…本文档是视图；字段定义与验收以 schema/检查器为权威（16 标准：JSON/JSONL 输出为真相）」＋:24「⚠ 强制消歧（唯一口径）」——**机械列漏报** | `:19` 「边界（**本任务**冻结范围）」；`:123` 「（本任务冻结单行上限）」；`:137` 「LOG-004 **不修改**本合同冻结的字段语义」；`:147–:151` 「既有字段…LOG-002 在 Runtime 集成时以本合同为单一事实源做适配」历史/状态叙事；`:153` 「验收（LOG-001）」；`:163` A7「本任务不产生/不引用 raw testdata；全局 forbidden globs 不含本路径数据」（对检查器行为的自述） | `:168` 「`ASTROCS_DESIGN.md` **§7.1a**」⇒ 该子节不存在（§7.1 无 a 款）；`:29/:36/:167` 「最高设计 §6.3」指运行事件流 ⇒ 错指（§6.3 是投影算法；事件流在 §7.2）；`:81` 「`lib/algorithms/noise_snr/cpp/src/noise_model.cpp`（路径形态示例）」为示例路径，该目录在根构建中被摘出（MODULE_MAP §4），示例选用非活动面 | §1 末条——:24 声明「唯一口径」；§7.1「命令名与 CLI 子目录使用 normalize/mosaic/export；phase1/2/3 仅为内部指代」——本文 :65/:30 把 `phase1/phase2/phase3` 作为**对外落盘工件**（运行日志 JSONL）的字段取值（已核 schema `log_event_v1.schema.json` 的 `phase.enum` 同此）⇒ 内部指代进入外部工件，与上位命名口径存在张力（非硬冲突，登记为待裁）；§12.5 状态现场计算——:19–:20 「本任务冻结范围：不实现生产 logger、不接监控」为状态预写 | 迁移（→`docs/contracts/STRUCTURED_LOGGING_CONTRACT.md`）＋上游行改 §7.3/§7.2——依标准 01 §2 表行 5＋§3 第 1 条 |
| docs/architecture/production_call_paths_stage1.csv | （无标题，机器清单） | 8 | 标准与检查（生产调用路径登记）⇒ 角色错放 | Phase1 入口→目标→符号→执行模式 | 缺（CSV 无上游位；`notes` 列有「最高设计 §6.2/§7.1/§11」＝§6.2 错指）[D-21] | 5：`docs/api/PHASE1_API_V1.md`、`docs/contracts/PUBLIC_API.md`、`eng/tests/api/test_p1_api.py`、`lib/phase1_session/README.md`、`lib/phase1_session/module.yaml` | 否（被 PRODUCTION_EXECUTION_INVENTORY.csv 取代） | `execution_inventory.csv`、PRODUCTION_EXECUTION_INVENTORY.csv（同「Phase1 生产调用面」主题三件）[D-21] | 无（CSV） | `:2` 「历史登记：main(orchestrator.exe <stage1.json>) / Orchestrator::run 已非入口」＝退役留痕；`:3` 「parallel_cpu (OpenMP **16**)」＝硬编码线程数入册（AGENTS §6 禁、§9 禁） | `config_gate` 列 6 个键（`phase_config_normalize.schema.json` 之外的 `stage1.calibrate.enabled`、`stage1.platesolve.enabled`、`stage1.photometric.enabled`、`stage1.drizzle.enabled`、`stage1.snr.enabled`、`stage1.output.hips`）在生产合同与 schema 中**均不存在**（`git grep -l "stage1\.calibrate\\|stage2\.inputs\\|stage2\.model" -- eng docs/contracts` 0 命中）⇒ 全部配置门为悬空键 | `:5` 记 `pc_calibrate_simple`，`execution_inventory.csv:4` 记同节点为 `pc_calibrate_simple_with_gaia_v2`；`:7` 记 `run_stage_snr` 为 `parallel_cpu`，`execution_inventory.csv:6` 记同节点「production path SERIAL…noise_model.cpp has NO #pragma omp」⇒ 同机制两文件结论相反；`:8` 记 `TEST-HIPS-001`，`execution_inventory.csv:7` 记「no explicit TEST-HIPS-### ID」⇒ 第三处相反 | 迁移/删除二选一：其职能已被 PRODUCTION_EXECUTION_INVENTORY.csv 现场计算取代 ⇒ 删除（保留则须改生成器并换 `blocks[]` 现行配置键）——依标准 01 §3「索引与文件树一致」＋§5.6 状态词不串档 |
| docs/architecture/production_call_paths_stage2.csv | （无标题，机器清单） | 11 | 标准与检查（生产调用路径登记）⇒ 角色错放 | Phase2 入口→目标→符号→执行模式 | 缺（同上，`:2` 「最高设计 §6.2/§7.1/§11」错指）[D-22] | **0**（除索引与 `reports/v19r2/**` 账目外无引用者）⇒ 孤儿 | 否（同 stage1） | PRODUCTION_EXECUTION_INVENTORY.csv、`execution_inventory.csv` 的 Phase2 五行 [D-22] | 无（CSV） | `:2` 「历史登记：main(astrocs-stage2 <stage2.json>) 已非入口」退役留痕；`:10` 「DORMANT（保留源码与隔离测试；生产构建/加载/路由/benchmark/发布不含 ACR/CUDA，最高设计 §8）」状态词入册 | `config_gate` 列 8 个 `stage2.*` 键（`stage2.inputs.hips`、`stage2.model.*`、`stage2.output.upm`、`stage2.integration.*`、`stage2.integration.rejection.*`、`stage2.integration.acr_route`、`stage2.output.hips`）全部不存在（同 D-21 命令）；`:9` `test_id` 写 `TEST-REJ-*` 通配（不可解析到具体用例） | `:8` 「p2_reject (**7 methods**)」与 §5.5「生产排异算法集为 none / percentile / winsorized / linear fit；**min/max 极值法不用于生产**」相反（4 种，且 `execution_inventory.csv:13` 把 `minmax 1671` 记在调用链上）⇒ 排异算法集第二口径；`:4` `test_id=TEST-UPMW-004` 挂在 sampler 行（UPM 测试号复用给采样节点）与 §5.2 节点身份唯一性口径不符 | 删除（与 stage1 同批处置；Phase2 调用面正本＝PRODUCTION_EXECUTION_INVENTORY.csv 现场计算面）——依标准 01 §2「同一主题只有一份正本」＋§5.6 |
| docs/contracts/API-001.md | API-001 — 公共 API 与错误模型（V6） | 74 | 合同说明——目录正确；文件自述「设计档案/历史合同层」⇒ 角色与 `status: ACTIVE_NORMATIVE` 冲突 | C ABI/模块接口/退出码/机器输出 | 有但双头：:4 「ASTROCS_DESIGN.md §8.4（**模块与 ABI**）」（§8.4 标题是「顶层结构」，模块与 ABI 是 §8.5）＋ :6 「上游: DATA-001, ARCH-001」＝第二套（合同图）上游；`docs/contracts/INDEX.yaml:775` 亦记 `upstream: [ARCH-001]`，而 `ASTROCS_DESIGN.md` 不在该链上 [D-23] | 2：`docs/contracts/INDEX.yaml`、`docs/contracts/ARCH-001.md`（互为上下游）＋`docs/standards/API_STANDARD.md` 经 CSV | 否（错误模型与 CLI 面均有上位正本） | 退出码表：§7.2 表（`ASTROCS_DESIGN.md:498-510`）＋`docs/api/CLI_PROTOCOL_V1.md:35` §2＋本文 :23-37 ＝**三处**；运行事件流字段：本文 :53 与 STRUCTURED_LOGGING :29-36；两处段落 [D-23] | 有：:3–:9 七行（「本文件是设计档案／变更编号 CHG-2026-09-20-DOC203」＋ID/上游/下游/§12 口径声明）——**机械列 meta_block=False 漏报** | `:1` 标题版本锚 V6；`:3` 日期型变更编号 `CHG-2026-09-20-DOC203`；`:8-9` 「其用途**限于留痕**（生效/退役一律用**日期或变更编号**）」＝与标准 01 §4「正文无日期、无变更编号、无历史叙事」**正面相反**；`:15` 「该命令面表**已作废**（DOC-203 订正）…原文留痕（**只作历史记录**）」＋把作废命令树原文留在正文；`:36` 「原「资源利用率或内存增长门禁失败」措辞**已作废**」；`:54` 「原文留痕（已作废）…」 | `:15` 「§6.2『命令树（唯一）』」×2 ⇒ 命令树在 **§7.1**（`docs/api/CLI_PROTOCOL_V1.md:11` 即写「ASTROCS_DESIGN §7.1 唯一命令树」）；`:36` 「§6.3：一般性资源超限门已取消」⇒ §6.3 是投影算法，该条款在 §4.5/§7.2；`:8` 「见 `ASTROCS_DESIGN.md` §12」⇒ 内部修订号不入产物那条在 **§13**；`:68` 「以 03_TARGET_ARCHITECTURE §6 为准」⇒ 无该文件 | 与上位冲突：`:15` 表行「CPU Provider ABI／I/O ABI」把 `aio_*` 记「保留冻结」，而 §7.2 与 `LOG_AND_ERROR_CONTRACT.md` 口径为「域→码映射唯一数值表 = docs/contracts/LOG_AND_ERROR_CONTRACT.md §5」、退出码唯一源 = `exit_codes.h`——本文件 :23 亦写唯一源，随后 :26-37 又复制一份含义表（措辞与 §7.2 有差：如 code 3「输入缺失、格式或 hash 错误」vs 上位「输入缺失/格式错」）⇒ 数值语义第二处；`:66` 「API_CONTRACTS.csv 登记 **423 行**现有 C ABI（`aio_*`/`p2_*`/`p1_*`/`p3_*`），状态 VERIFIED」⇒ 实测 381 数据行、`p1_*`/`p3_*` 各 **0** 行（见 §H-2 计数命令） | 保留（改指 §7.1/§7.2/§8.5/§13；删留痕块与作废原文；行数改由门出）——依标准 01 §4「无历史叙事」＋§1「冲突即缺陷」 |
| docs/contracts/API_CONTRACTS.csv | （无标题，逐符号 C ABI 合同） | 382 | 合同说明——目录正确 | 381 个 C 符号的units/域/所有权/线程安全/错误模型/状态 | 缺文档内上游（CSV 无上游位）；索引记「§3.1（数据对象）、§10（I/O 与原子产品）」，而内容是 §8.5 模块与 ABI ⇒ 索引代标与内容不符；`docs/contracts/INDEX.yaml:767` 记 `upstream: []` ⇒ 两份索引不一致 [D-24] | 5＋：`docs/contracts/API-001.md`、`docs/standards/API_STANDARD.md`、`eng/ci/known_failures_baseline.json`、`eng/tests/backend/test_public_header_layering.py`、`eng/tools/check_ast_api.py`、`eng/tools/quality/contracts/{check_api_contracts,check_science_units,check_traceability}.py` | 是（逐符号 C ABI 合同面正本） | `api_inventory.csv`（别目录，同符号面）、`docs/contracts/PUBLIC_API.md` | 无（CSV） | `status` 列 **381 行全部预写 `VERIFIED`** ⇒ 违反 §12.5「状态由检查与验收现场计算，登记表不预写状态」＋§0.2「登记表承载定义与映射，状态由检查器现场计算」；`:79`（`API-aio_ahpx_write`）的 `config_keys` 含过程性注记；符号面含退役面（`get_stage1_schema_sha256`、`orchestrator.h` 31 个符号）却整列标 VERIFIED | 无路径悬空（`header` 列路径抽查存在） | 上位 §12.5 定义 VERIFIED = 「正式平台 + 真实数据验收通过」；本表把含 Windows 侧未验的 `orchestrator.h`/`aio_*` 全部标 VERIFIED，与 MODULE_MAP :29「当前为 Linux 技术预览安装面；Windows 侧复验 NOT_VERIFIED」相反 ⇒ 状态词跨轴串用（派单 §5.6 明禁）；且 `eng/ci/known_failures_baseline.json` 内另有该 CSV 的已知失败登记 ⇒ 「全绿 VERIFIED」与门的基线记录并存 | 保留（`status` 列改由 `check_api_contracts.py` 现场出；含退役符号面单独标注）——依标准 01 §3 第 3 条＋§5.6 |
| docs/contracts/ARCH-001.md | ARCH-001 — Runtime 与职责边界（V6 目标架构合同） | 94 | 架构设计——目录**错放**（合同说明目录里放架构条款；标准 01 §2 表行 3 ⇒ `docs/architecture/`） | 唯一执行平面、职责边界、依赖方向、线程预算、ACR 隔离、现状差距 | 有但自相矛盾：:3 「ASTROCS_DESIGN.md §8（软件架构）」vs :9 「架构问题的权威 = `ASTROCS_DESIGN.md` **§7**」⇒ 同一文件内两个架构权威号，且 §7 是 CLI 合同；`docs/contracts/INDEX.yaml:757` 记 `upstream: []`；:5 「上游: 03_TARGET_ARCHITECTURE.md (控制包)」是真实链条顶点 [D-25] | 5＋：`docs/algorithms/CALIBRATION_ALGORITHMS.md`、`docs/architecture/abi/ABI_003_SECURE_LOADER.md`、`docs/architecture/MODULE_MAP.md`（:106 「ARCH-001 §7 登记」）、`docs/contracts/INDEX.yaml`、`docs/contracts/API-001.md` | 否（§8 才是架构权威，本文是自认的「目标态」） | §8.1/§8.3（调度器与执行平面）、§8.4 依赖方向、MODULE_MAP §4（同为差距登记）[D-25] | 有：:5–:10 六行（ID 行＋「⚠ DOC-202 R04 订正（**2026-09-20**）：原句…**已删除**」）——**机械列 meta_block=False 漏报** | `:1` 标题「V6 **目标**架构合同」＝目标态而非现行（§4「只写现行设计要怎样」）；`:6` 日期 2026-09-20＋变更号 DOC-202 R04；`:6-7` 把**已撤销的「本文件是 V6 架构的唯一权威；与 docs/architecture/* 冲突时以本文件为准」原句整段留在正文**（§4 禁「原句/已删除/留痕」，§0.3 禁撤销设计出现在正式文档）；`:80-90` §7「冻结的现状差距」表含 `P0-006/P1-002/P1-003/P1-005/P1-007/P2-002`＋`orchestrator.cpp 5522 行`（源码行数当证据）＋迁移任务号 ⇒ 过程台账 | `:5` `03_TARGET_ARCHITECTURE.md`（控制包）仓内不存在 ⇒ 本合同的上游链顶点悬空；其余引用（`DATA-001`、`BLD-001`、`CORE-*`、`LEG-*`）为合同图编号，无对应可解析文档 | 与上位冲突：:14「**只有 Pipeline Runtime 拥有全局执行顺序与资源预算**」——§8.1 规定「每个阶段实例化自己的调度器与内存管线，互不共享内存、会话或运行时状态」、§8.3「调度器是**阶段内**的唯一执行者」，且「Pipeline Runtime」这一组件名在 §8.4 顶层结构里不存在（实为 `infrastructure/pipeline` ＋ `infrastructure/scheduler` 两个目录，MODULE_MAP §2 把唯一 executor 记在 `lib/infrastructure/scheduler`）⇒ 孤儿组件名＋与上位「阶段内唯一执行者」表述不一致；§7 差距表把「CLI 顺序调用 3 session」判 P0 违例（与 §8.1 一致），但该表预写状态与迁移任务号 ⇒ 与 §12.5/§0.2「状态现场计算、登记表不预写」冲突 | 迁移（→`docs/architecture/`，与 §8 一一对应后重写成现行条款；删留痕块与 §7 差距表，改由 `eng/ci/` 现场出）——依标准 01 §2 表行 3＋§4 |

## C 本批主产出 ①：另立架构权威（标准 01 §1 判定）

判据：标准 01 §1「架构问题的唯一权威是最高设计 §8」「任何文档不声明自己的第二套权威顺序或『唯一口径』」；
最高设计 §0.1 同文重申。逐份判词见台账「与上位冲突」列。分级：

**S1（直接宣称架构权威＋自定冲突顺序）**

1. `docs/architecture/THREAD_BUDGET_ARCH.md:6`
   > 本文件为线程架构权威;THREADING_MODEL.md 的分层与确定性锚点保留有效(§6 引用),**冲突处以本文件为准**。

   线程与预算是顶层决策（上位条款在 §9、§8.3）。该句同时做两件事：把该域的架构权威判给自己，
   并把兄弟文档之间的冲突顺序写成「以下位文件为准」——与 §1「下位与上位冲突时以上位为准」反向。
   其自身上游又写成 `ARCH-002/ARCH-003`（两份均不存在），即：一份自称权威的架构文档，其权威链不落到 §8。

2. `docs/architecture/observability/RUN_GRAPH_CONTRACT.md:144-147`
   > 运行图规范来源 = `eng/tools/graph/render_run_graph.py` 从当前提交可复现生成的 `graph-runtime.{json,dot}`…
   > 静态架构示意图（`docs/architecture/DATA_FLOW.md` 等 ASCII 流程、ARCH-001 mermaid、历史 evidence DOT）是**信息性视图，不作运行事实规范来源**；

   由一份工具合同对**同层架构文档**排定规范等级（谁是来源、谁只是信息性视图）。等级面属 §0.2/§3 的索引地图职责
   （`docs/DOCUMENT_INDEX.yaml` 的 `status` 字段正是为此而设），不属某份下层文档。

**S2（「唯一口径／唯一来源／权威＝本文件＋X」措辞违规）**

3. `docs/architecture/observability/STRUCTURED_LOGGING_CONTRACT.md:24`「⚠ **强制消歧（唯一口径）**」；
   同文件 `:167`「依据：`ASTROCS_DESIGN.md` §6.3 **+ 本文件**」——把自己列进自己的依据。
4. `docs/architecture/observability/RESOURCE_MONITORING_CONTRACT.md:43`「⚠ **强制消歧（唯一口径）**」；
   `:47`「该工件列合同**只有一处** = 生产实现 `lib/infrastructure/cli/resource_recorder.h:260-266`」——
   把列合同权威判给源码行号（实测真列名在 :288-293），属文档向代码让渡权威（标准 01 §1 层级向下）。
5. `docs/architecture/abi/ABI_003_SECURE_LOADER.md:18`「验收（依据=`ASTROCS_DESIGN.md` §7.3/§8 **+ 本文件**）」（同 3 型）。
6. `docs/architecture/observability/RUN_GRAPH_CONTRACT.md:17`「验收（依据=`ASTROCS_DESIGN.md` §6.3 **+ 本文件**）」（同 3 型）。
7. `docs/architecture/execution_options_contract.md:5`「唯一执行预算对象。并行/IO/确定性/内存预算**以此为唯一来源**」＋
   `:9`「⚠ **预算唯一来源**」——同一文件 :34-:56 又给出 `execution` 配置块与三个 CLI flag 作为取值面 ⇒ 声明的「唯一」被自家正文破。
8. `docs/architecture/PERFORMANCE_MODEL.md:17`「**权威**：`docs/contracts/SCHEDULER_CONTRACT.md` §3「编排参数…最终取值由 **PERF-501** 基于探针实测数据确定」」——
   以一份合同的一个小节＋一个任务号作为顶层参数取值的权威落点（§9 规定权威是「benchmark 实测台账＋稳定统计」）。
9. `docs/architecture/doc_symbol_namespaces.json:13`「docs/GLOSSARY.md = **唯一术语权威**」。

**S3（历史留痕型的反向违规：撤销的权威声明仍以引文形式在场）**

10. `docs/contracts/ARCH-001.md:6-7` 把撤销句原文「本文件是 V6 架构的**唯一权威**；与 docs/architecture/* 冲突时以本文件为准」
    连同「已删除」叙述留在正文；该文件 :9 的替代句又把架构权威写成 `§7`（应为 §8）⇒ 全文 grep 「唯一权威」仍命中，
    且正确句本身指错条款。属标准 01 §4「无原句/已删除/留痕」＋§0.3「已撤销设计不出现在正式文档里」双违。

**未越界项（用于对照，避免把下钻当越权）**

- `docs/architecture/MODULE_MAP.md:5`「状态词：**唯一口径 = `ASTROCS_DESIGN.md` §12.5**」——这是把权威**上交**给上位，合规；
  其违规在别处：同文件四张表逐行预写状态，违反它自己复述的那条（见 D-03）。
- `docs/architecture/PIPELINE.md:25`「其输出即唯一权威 WCS」——对象是单次解算结果，非文档权威，合规。
- `docs/contracts/ARCH-001.md` 的 `upstream: []`（contracts INDEX）与 `docs/architecture/ARCHITECTURE.md:67-68`
  （明确「架构问题的唯一权威 = 最高设计 §8，与本文件冲突时以最高设计为准」，**别批文件，仅引作合规样板**）。

## D 本批主产出 ②：§8 机制承接矩阵（缺承接＝权威链断）

先固定一个事实：**`ASTROCS_DESIGN.md` §8 全节（:542–:666）不含任何指向 `docs/architecture/` 的索引指针**——

```bash
awk '/^## 8\. 软件架构/,/^## 9\. CPU/' ASTROCS_DESIGN.md | grep -c architecture   # → 0（正例对照：同式对 "docs/" 命中 1）
```

§8 里唯一含 `docs/` 的一行是 §8.4 目录树中的「`docs/contracts/  合同的文档化说明…`」（目录列举，非索引指针）；
其余外指只有 :656「登记面 = `ENGINEERING_SPEC.md` §7；机器登记 = 根 `CMakeLists.txt` 与 `eng/ci/root_manifest.json`」。
标准 01 §3 第 1 条要求「最高设计的每个机制都有指向下层文档的索引」——§8 逐条缺失 ⇒ 本批 22 份 architecture 文档的
「上游：§8」全是**单向标注**：下层指上，上层不知下层存在。这是本批所有下游判定共用的根因，登记一次即可逐份引用。

按 §8 子节列承接（仅本批 25 份范围内）：

| §8 机制 | 下位承接（本批） | 承接状态 |
|---|---|---|
| §8.1 唯一 CLI 入口＋阶段独立调度器（§1.2 标「唯一正本 = §8.1」） | PIPELINE.md（只写 Phase1/2）、execution_options_contract.md（错指 §6.2） | **部分缺**：export 无人承接；PIPELINE.md 上游行声明覆盖 §6.2 却无 Phase3 节 |
| §8.1 阶段间只磁盘产品＋manifest＋哈希 | 本批无（`lib/.../artifact_store` 只在 MODULE_MAP §2 出现一行） | 缺承接（该条款被 §1.2 称「唯一正本」，下层无对应文档） |
| §8.2 PipelineFrame 命名块、块冻结元数据、显式销毁 | 本批无；OWNERSHIP_AND_LIFETIME.md 标题撞名但讲 C 句柄 | **缺**（真实承接在别批 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`；本批该文件反而无指针） |
| §8.2 provenance 随帧头部 KV 流动 | 本批无 | 缺 |
| §8.3 三阶段调度形态表（异步编排／窗口并行／子块流式） | THREADING_MODEL.md、THREAD_BUDGET_ARCH.md、PERFORMANCE_MODEL.md、execution_options_contract.md、PHASE3_MODULE_ARCH.md（子块流式） | **过承接且互斥**：5 份写同一机制，THREAD_BUDGET 声称压过 THREADING_MODEL，PERFORMANCE_MODEL 把取值权威判给任务号（见 C/S1-1、C/S2-8） |
| §8.3 探针校正／可中断排队／可丢弃重跑 | 本批无（THREAD_BUDGET §2 只有执行画像，未写中断与丢弃策略） | **缺**：三条编排策略无下位展开 |
| §8.4 顶层结构 | MODULE_MAP.md | 有承接但双向不闭合：MODULE_MAP 未列实际存在的 11 个 `lib/` 顶层目录（`calibration/star_detector/dynamic_psf/photometric_calib/plate_solve/snr_estimator/healpix_db/astro_image_io/common/phase2/gaia_xpsd_client`），自身 §2 又登记不存在的 `lib/phase1` ⇒ 见 E-1 |
| §8.4 依赖方向单向 | ARCH-001.md §3（且它把架构权威写成 §7） | 错位承接（写在 `docs/contracts/`） |
| §8.4 动态库加载前固定检查 | abi/ABI_003_SECURE_LOADER.md | 有承接（文档标 §7.3）；但检查面被文档自家 P2 测试行开豁免（见 D-11 行「与上位冲突」） |
| §8.5 模块必备件（README/module.yaml/公开头/单一 entrypoint/CMake target/共址测试） | api_inventory.csv、API-001.md §1/§2.3 | **缺**：无人逐件承接「模块必备件」清单（MODULE_MAP 只给交付面） |
| §8.5 跨 DLL 不传 STL/异常/RTTI | API-001.md :47 | 有承接（同一表另在 §2.3 复制一遍措辞） |
| §8.5 每 DAG 节点映射唯一真实模块 | MODULE_MAP §2、PRODUCTION_EXECUTION_INVENTORY.csv | 有承接，但 §4.2 节点名与生产 IR 8 节点名无映射表（D-07） |

## E 本批主产出 ③：孤儿机制（下层有、§8/§9 追溯不到 ⇒ 按 §3 第 2 条先补要点再保留）

1. **（订正后成立的部分）MODULE_MAP 登记了跟踪集中不存在的 `lib/phase1`**：§8.4 声明的五项顶层目录与
   **跟踪集**逐项一致（`git ls-files lib | cut -d/ -f2 | sort -u` = algorithms / include / infrastructure /
   phase1_session / phase2_session / phase3_session / third_party），故「§8.4 顶层结构与实际目录双向不闭合」一说**不成立，已撤回**；
   真实缺陷反向：MODULE_MAP §2 把「Phase1 科学内核」登记在 `lib/phase1`（跟踪集与工作树都没有该目录，只有 `phase1_session`），
   与该文件 :10 的自我约束「本表按 `lib/` 实际目录登记（不再描述已不存在或尚未建立的产物）」直接互斥。
   本机磁盘上的 `lib/calibration`、`lib/snr_estimator` 等属**未跟踪残留**，按「审查只限 git 跟踪内容」的口径不算仓库问题，
   不进本批任何计数（初稿曾据磁盘判其为孤儿目录，已按跟踪集订正）。
2. **CPU provider 子系统 vs backend_host ISA 变体子系统**：`benchmark/cpu/{baseline,avx2,avx512}/*_provider.cpp`（CPU_001/CPU_003 的正题）
   与 `benchmark/backend_host/{baseline,avx,avx2,avx512}_backend.cpp`（ISA_VARIANTS/ISA_BIT_MANIP 的正题）两套并存，
   根构建只取后者（`CMakeLists.txt:345,702-709`），前者无 `CMakeLists.txt` ⇒ 属**未接线**（源码在位、被 `check_thread_budget.py` 登记、无构建无生产入口）。
   §9 只写「发货档由 benchmark 实测台账决定」，未规定 provider/kernel 分层 ⇒ 该机制需先补 §8/§9 要点，否则两份 FROZEN 合同都在给未接线面立合同。
3. **第二份资源时序工件 `monitor_timeseries.csv`**：§10 只承认「资源时序／资源汇总／worker 均衡／运行图」，
   代码侧唯一生产者工件名是 `resource_timeseries.csv`（`lib/infrastructure/cli/resource_recorder.h:288-293`）；
   `monitor_timeseries.csv` 在 `lib/`＋`eng/` 零出现 ⇒ 工件名由文档造，属孤儿机制。
4. **后台服务独立小预算池**：THREAD_BUDGET_ARCH :11「后台服务(watchdog/资源监控/progress 日志)恒 1 线程+**独立小预算(不入科学预算池)**」，
   与 §9「一个进程一个资源调度器与线程预算源」并置 ⇒ 需要 §9 补「预算池分档」要点，或删该句。
5. **Phase3 四单元目录 `lib/phase3`**：PHASE3_MODULE_ARCH §1 的单向流图整体挂在不存在的路径上（实际实现分散在
   `lib/algorithms/resample/`、`lib/algorithms/fits_output/`、`lib/infrastructure/aio/`）⇒ 目录要点须先由 §8.4 认定，再落文档。
6. **文档符号命名空间门（DOC-SYMBOL-*）**：登记表住 `doc_symbol_namespaces.json`，门的规范面在 `eng/tools/quality/contracts/check_doc_symbols.py`，
   标准 01 §2 属「标准与检查」角色 ⇒ 现址（docs/architecture）与上位（§8 无该机制）两头都无承接。
7. **运行图渲染器本身**：§10 要求「运行图（plan/trace 分别如实记录）」，未要求 DOT/SVG/JSON 渲染器与 `GRAPH_CONSISTENT` 门 ⇒
   工具面机制，需补要点或改登记为工具文档（`docs/diagnostics/`）。

## F 逐份举证（判词所贴的原文对证）

只贴承重的：`上游` 判「缺/错」的实文、`重复或重叠对象` 的两处段落（各带 `路径:行`）、以及支撑「与上位冲突」的上位原句。
未在下列出现的条目，其「重复或重叠对象」列为「无第二处正本」，不需举证。

**D-01 ISA_BIT_MANIP_VARIANTS.md**
- 上游实文：`:3` `> 上游：ASTROCS_DESIGN.md §8（软件架构）`；`:5` 又写 `上游: ISA-004/05 §1-3`；`:6` 写 `依据(03 §91)`。三处并存 ⇒ 判「缺（无唯一上游条款）」。
  被引用的 `03 §91`、`05 §1-3` 不在跟踪集（H-0 命令）。
- 与 ISA_VARIANTS 重叠范围：同一「ISA 变体是否发货」主题的**结论面**（本文 = BMI2/POPCNT 档 NOT_APPLICABLE；ISA_VARIANTS = AVX/AVX2/AVX512 档 SHIP/NOT_SHIPPED），
  两处段落：
  - `docs/architecture/ISA_BIT_MANIP_VARIANTS.md:36`「本 kernel 集**无整数/位操作热点**适用于 BMI2/POPCNT。按 03 §91 → 登记 **NOT_APPLICABLE**, **不写空 DLL**」
  - `docs/architecture/ISA_VARIANTS.md:68`「按 05 §3 "capability 与热点对应; 无机械指令集堆砌" → 登记 **NOT_SHIPPED**(完整测量在案, 非空判定)」
  差异：同一判据被写成两个不同编号文档的两节（`03 §91` vs `05 §3`），措辞互不引用；数值面（逐 kernel 计数/计时）只在 ISA_VARIANTS。⇒ 属同主题**第二份**，非下钻。

**D-02 ISA_VARIANTS.md ↔ CPU_003_AVX2_PROVIDER.md（同主题两份＋数值表逐字复制）**
- `docs/architecture/ISA_VARIANTS.md:28-31`
  ```text
  | kernel | baseline ns | avx2 变体 ns | 提升 | 决策 |
  | calibration-pixel-transform(1M px) | 1 576 742 | 1 250 899 | **+20.7%** | **SHIP(avx2)** |
  | hips-bulk-transform(1M px) | 16 922 530 | 12 145 714 | **+28.2%** | **SHIP(avx2)** |
  | drizzle-accumulate(1M×3) | 2 587 976 | 2 981 571 | −15.2% | NOT_SHIPPED(变体更慢) |
  ```
- `docs/architecture/cpu/CPU_003_AVX2_PROVIDER.md:35-39`
  ```text
  | kernel | baseline ns | avx2 变体 ns | 增益 | 决策 |
  | calibration-pixel-transform (1M px) | 1 576 742 | 1 250 899 | **+20.7%** | **SHIP(avx2)** |
  | hips-bulk-transform (1M px) | 16 922 530 | 12 145 714 | **+28.2%** | **SHIP(avx2)** |
  | drizzle-accumulate (1M×3) | 2 587 976 | 2 981 571 | −15.2% | NOT_SHIPPED（变体更慢） |
  ```
  重叠范围：6 个 kernel 的 ns 级实测值与发货决策**逐字相同**；差异仅列名「提升/增益」与括号间距。
  ISA_VARIANTS :31 另有 ISA-003 复测同 kernel 的另一组数（`+11.7%`），CPU_003 :44 又把该组数复述一遍 ⇒ 同一事实三处承载，无「只引用不复制」声明。
- 上游实文：`:3` `> 上游：ASTROCS_DESIGN.md §8（软件架构）` ＋ `:5` `上游: ABI-003/05 §1-2` ⇒ 同一抬头双头。

**D-03 MODULE_MAP.md ↔ docs/modules/（映射面重叠）**
- `docs/architecture/MODULE_MAP.md:110`「`docs/modules/<module>.md`（L5 模板）；模块清单以本表 §1–§4 为准。」
- `docs/modules/`（跟踪集内实存：`phase2.md`、`orchestrator.md`、`astro_image_io.md`、`star_detector.md`、`benchmark` 等）另按模块逐份写职责/端口/生命周期。
  重叠范围：模块身份 ↔ 路径 ↔ 交付面。差异：MODULE_MAP 只到「目录＋产物＋证据锚」层，`docs/modules/*.md` 到函数/端口层 ⇒ 属**合法分层**，不判重复；
  判的是**角色错放**（标准 01 §2 表行 4「模块工作细节 → `docs/modules/`、`docs/plugins/`」）＋ :76 一行登记了跟踪集中不存在的 `lib/phase1`。
- 自我冲突对证：`:8`「本表**不写状态字段**，状态一律由 `eng/tools/quality/check_module_map.py` **现场计算**」vs `:17`「| conformance (noop) | … | INSTALLED | …」、`:73`「| Runtime / 唯一 executor | … | IMPLEMENTED |」、`:80`「CONTRACT_READY」、`:102`「DORMANT」，
  而 `eng/tools/quality/check_module_map.py` 确在库中（`git ls-files` 命中）⇒ 声明与实际承载互斥。

**D-04 OWNERSHIP_AND_LIFETIME.md ↔ 最高设计 §8.2**
- `docs/architecture/OWNERSHIP_AND_LIFETIME.md:7-9`「C API 返回的 handle（model/cache/reader）由调用方负责 close/free：p2_upm_build → p2_upm_close；aio_upm_open → aio_upm_close…」
- `ASTROCS_DESIGN.md:573-574`「PipelineFrame 承载一组**命名块（block）**…一个块被其声明的消费者全部用完后由管线**显式销毁（消耗）**，内存立即归还。」
  差异：同一「所有权/生命周期」语词指向两种对象（C 句柄 vs 管线命名块）。该文档抬头标 §8，实际承接的 §8.2 条款无人承接（真正承接在 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md`，本批无一份指向它）⇒ 判「挂 §8 名下的孤儿文档」而非重复。

**D-05 PERFORMANCE_MODEL.md ↔ THREADING_MODEL.md（同主题两份）**
- `docs/architecture/PERFORMANCE_MODEL.md:130-132`
  ```text
  F     = min(L, floor(0.75·A / (P·B)))      // p1_frame_workers（内存闸门）
  I     = max(1, L / min(n, F))              // PERF-501 冻结公式
  W_eff = F × min(I, K)                      // drizzle 节点；K 为每帧 scratch 槽上限
  ```
- `docs/architecture/THREADING_MODEL.md:34-36 ＋ :53`
  ```text
  in_flight = min(n, frame_workers)                 // frame_workers = min(__workers, p1_memory_cap)
  inner_omp = max(1, thread_budget / in_flight)     // thread_budget = __workers（lease）
  总并行度  = in_flight × inner_omp ≤ thread_budget
  W_eff = in_flight × min(inner_omp, K)             // 有效宽度（PERFORMANCE_MODEL.md §5）
  ```
  重叠范围：同一分配式（除记号外同构）＋同一批实测数字（`390.7 / 421.0 / 583.7 s`、`W_eff=16`、`CPU p50 98.9%/1321%`、`kP1FrameBytesPerPixel 358→116.0`）
  在两文件各写一遍；差异只有 THREADING_MODEL 多一张四行 A/B 表。⇒ 同主题两份正本，THREADING_MODEL :81 已写「标定值…见 `docs/architecture/PERFORMANCE_MODEL.md`」＝知道正本在别处仍复述数值。

**D-06 PHASE3_MODULE_ARCH.md ↔ 配置默认值唯一正本**
- `docs/architecture/PHASE3_MODULE_ARCH.md:40-42`「`sub_block_px` 默认 256、值域 [16,1024]（内存守卫）; `queue_depth` 默认 4、值域 [1,64]; `max_tiles` 默认 `min(1024, ceil(W_out·H_out/W²)+16)`」
- `ASTROCS_DESIGN.md:295`「默认值与取值域正本 = `eng/packaging/config/defaults.json` 与 `docs/contracts/CONFIG_CONTRACT.md`」
  对证：`grep -nE "sub_block|queue_depth|max_tiles" eng/packaging/config/defaults.json` → **0 命中**（`max_tiles` 只在 `phase_config_{export,mosaic}.schema.json` 的会话键并集说明与测试里出现，无默认值/值域登记）⇒ 该组数值只存在于本文一档，上位指定的正本里为空 ⇒ 数值唯一性落错文件。

**D-07 PIPELINE.md ↔ 12_rejection.md §9（排异档位表）**
- 上位：`ASTROCS_DESIGN.md:422`「逐像素档位表（档界与算法名）的**唯一正本 = `docs/plugins/algorithms_phase2/12_rejection.md` §9**（本节不复制档界与取值；实现路由表须与该正本逐项一致）」
- 正本：`docs/plugins/algorithms_phase2/12_rejection.md:58+`「| **1 ≤ N ≤ 3** | **none（不排异，直接逆方差加权积分）** | / | **4 ≤ N ≤ 5** | percentile clipping | / | **N ≥ 6** | winsorized sigma clipping |
  ＋「即生产档 `astrocs_adaptive_pixel` 的档位表**由四档收成三档**…**依据（生产 kernel 受控评估，`run/REJECT-DOCFIX-01/REPORT.md`）**…对照档 `wbpp_2_9_1`…仍为 `N>15 → linear fit`」
- 本批：`docs/architecture/PIPELINE.md:49-53`「**排异档位映射表（档界取自 WBPP 2.5.9，逐项正本见 §9）**：`1≤N≤3` **none（不排异）** / `4≤N≤5` percentile / `N≥6` winsorized ；
  档位取值限于 percentile/winsorized/linear fit（linear fit 仍为对照档 `wbpp_2_9_1` 的 `N>15` 档）。冻结表落位 = …§9（**只引用，不复制**）」
  差异：档界数值与正本一致（不是错值），但（a）边说「只引用，不复制」边把三档整表复制；（b）把档界**归因**写成「取自 WBPP 2.5.9」，正本写明生产三档是仓内受控评估的结果、WBPP 只是对照档界 ⇒ 归因与上位/正本相反。
- 另：同文件内部两处 Phase1 节点序互斥——`:13-21`（`platesolve → star_detection → psf → photometry`，与 `ASTROCS_DESIGN.md:271`「解算在前、检测与 PSF 建模在后」同）vs `:33`
  「生产 IR 的 normalize 阶段…8 节点（`calibrate/cosmetic_correct/`**`detect_sources/plate_solve`**`/measure_flux/estimate_snr/drizzle_stack/write_hips`）」⇒ 同一文档给出两种先后，且 §4.2 的节点名（`cosmetic/validity`、`background/noise`、`psf`）与 8 节点名无映射表（`background/noise`、`psf` 在 8 节点名中无对应项）。

**D-08 PRODUCTION_EXECUTION_INVENTORY.csv（生成件承载规范断言）**
- 全列枚举（本批对 3 份大 CSV 用此法替代逐行读，见 J）：`category` 7 值 {exe_target 294, thread_creation 99, lock 76, io_writer 39, acr_boundary 21, openmp_kernel 26, queue 6}；
  `classification` {production 224, test 210, tool 127}；`production_reachable` {no 337, yes 224}；`risk_note` 空 335／非空 226。
- 与 THREAD_BUDGET_ARCH :12 的对证：「**全仓线程数由预算派生**（ARCH-001 清单 risk_note 列逐行登记）」vs 实测 `risk_note` 60% 为空 ⇒ 「逐行登记」不成立。
- 与 §9 的对证：`thread_creation` 且 `production_reachable=yes` 共 63 行，其中 `thread_model=std::async` 53 行（`lib/algorithms/` 18 行：coverage 9 / drizzle 6 / integration 3）；
  `ASTROCS_DESIGN.md:674`「串行 I/O 与控制面单线程，异步 I/O 一个专用线程，**科学计算里不用 async/future**，不嵌套并行」⇒ 清单与上位条款并存且相反（清单为现场计算面 ⇒ 需修的是 §9 条款或清单分类口径，二者不能都保留）。
- 与构建图的对证：`lib/infrastructure/benchmark/cpu/{avx2,avx512,baseline}/src/*_provider.cpp:266/265/445` 三行标 `production,yes`；
  而 `git ls-files lib/infrastructure/benchmark | grep -i CMakeLists` → 无 `cpu/` 下任何 CMakeLists；根 `CMakeLists.txt:345,702-709` 的 provider 源是 `backend_host/baseline_backend.cpp`、`backend_loader.cpp` ⇒ 被标「生产可达」的源不在构建图（**未接线**，非未实现）。

**D-09 THREADING_MODEL.md ↔ 最高设计 §9 的乘/和口径**
- `docs/architecture/THREADING_MODEL.md:29-30`「两轴之和必须 ≤ Runtime lease 给出的预算（订阅比例上界为单轴 1×1）」vs 同文件 `:36`「总并行度 = in_flight × inner_omp ≤ thread_budget」（乘积）
  vs `ASTROCS_DESIGN.md:680`「并行度沿两个轴分配，**两轴之积**即同时真正在算的线程数」⇒ 措辞与上位相反（式子本身正确），登记为表述级冲突。
- 与 THREAD_BUDGET_ARCH 的相反陈述：`THREAD_BUDGET_ARCH.md:22`「| Phase2 | UPM 模型读/写(串行) | **sampler(串行 reference)**→rejection(行带)→integration(行带) |」
  vs `THREADING_MODEL.md:123`「Phase2 sampler：`lib/algorithms/coverage/src/sampler.cpp` **std::thread worker 池**（`:924-954`）——worker 数只来自 Runtime lease…`workers == 1` 走同一 `pass1_cells` 的串行 reference 分支」
  ⇒ 同一节点「串行」与「worker 池」两种登记，互不引用。

**D-10 THREAD_BUDGET_ARCH.md（另立权威＋自家门描述失准）**
- `:6`「本文件为线程架构权威;THREADING_MODEL.md 的分层与确定性锚点保留有效(§6 引用),冲突处以本文件为准。」
- `:41`「1. 扫描 lib/ 生产源: `std::thread`/`std::async`/`_beginthread`/`CreateThread` 出现处必须在 `THREAD_BUDGET_EXEMPT` 登记表(**当前: orchestrator watchdog/resource_monitor/logger**;eng/tests/ 全豁免); 2. `omp_set_num_threads(`/`num_threads(` **字面量=0 容忍**」
  vs 实际门 `eng/tools/arch/check_thread_budget.py`：`EXEMPT = {"watchdog","resource_monitor","logger","orchestrator.h","nanoflann.hpp"}`（:148-152）＋ `REGISTERED = {…}`（:156 起，20+ 个**文件级**登记项，含
  `lib/algorithms/{calibration,drizzle,cosmetic}/src/module_entry.cpp` 的 `omp_set_num_threads` 注入、`scheduler/src/{executor,scheduler}.cpp` 的池、`benchmark/cpu/*/src/*_provider.cpp` 三行，
  以及一条 `hardware_concurrency()` 夹紧 [2,8] 的豁免注记）⇒ 文档对自家门的描述（豁免面 3 项、`omp_set_num_threads(` 零容忍）与门的实际登记表不符；
  且 `hardware_concurrency` 与 `THREADING_MODEL.md:123`「模块不取 `hardware_concurrency`」、`execution_options_contract.md:23`「兜底面排除 `hardware_concurrency`」三处口径互斥。

**D-11 ABI_003_SECURE_LOADER.md ↔ 最高设计 §8.4 加载门**
- 上位：`ASTROCS_DESIGN.md:659`「动态库加载前过固定检查（CPU 特征、OS 可安全执行状态、manifest、哈希、ABI），只认清单授权的绝对路径，失败只报错、不回退搜索。」（无例外条款）
- 本批：`:2` 抬头把上游写成「`ASTROCS_DESIGN.md` **§7.3（版本化 C ABI/DLL 边界）**」——`ASTROCS_DESIGN.md:515` 的 §7.3 标题是「错误传播与运行日志」⇒ 条款号与标题不匹配（真承接点 §8.4/§8.5）。
- `:88` 测试行「| P2 | manifest 未登记 sha(SKELETON 兼容) | **LOAD_OK** |」⇒ 对「哈希必检」开了未在上位登记的豁免分支（同文件 :21 第 3 条验收又写「hash mismatch 拒绝」）⇒ 文件内部与上位都需消歧。
- `:6` 下游写「CLI-001 modules/doctor」⇒ `ASTROCS_DESIGN.md:466-485` 唯一命令树无 `modules`；`docs/api/CLI_PROTOCOL_V1.md:15-19` 的命令树亦无 ⇒ 引用命令树外入口（与 MODULE_MAP §1.1 的「本版不由三个命令在运行期 dlopen」并存 ⇒ 该文档是**契约面在位、运行面未接线**）。

**D-12 api_inventory.csv ↔ doc_symbol_namespaces.json 的登记前提**
- `docs/architecture/doc_symbol_namespaces.json:18`「public_headers … lib/**、**lib/include/** 公开头中真实定义的枚举常量/宏（api_inventory.csv **只登记函数签名**）」
- `docs/architecture/api_inventory.csv:2`（首行数据）`ACS_ERR_CANCELLED,lib/include/astrocs/abi/status_codes.h,"enum acs_status { ACS_ERR_CANCELLED = 6 }",`
  ＋同文件另有 `ACS_ERR_BUDGET/ACS_ERR_IO/ACS_ERR_UNSUPPORTED` 三行同形态、`#define ACS_CPU_AVX2_REQUIRED_FEATURES …` 一行
  ⇒ 448 行中 5 行不是函数签名，登记表的判据前提不成立。
- 重复面：`docs/contracts/API_CONTRACTS.csv`（381 行，含 `header` 列同名头文件）与 `docs/contracts/PUBLIC_API.md`（人类可读面）三者载同一符号集，互不设「只引用」声明。
- 解析残迹：重复 symbol 4 个 = `instance`、`get_timestamp`、`set_checkpoint_dir`、**`void`** ⇒ 与 API-001 :74「DOC-004 后：AST 提取与 API index **零漂移**」的验收自述并存冲突。

**D-13 CPU_001_CAPABILITY_PROBE.md（上游号与索引不同源）**
- 文档抬头：`:4`「上游: `ASTROCS_DESIGN.md` **§8**（CPU 后端与资源）」——§8 标题是「软件架构」，「CPU 后端与资源」是 §9。
- 索引登记：`docs/DOCUMENT_INDEX.yaml:441`「upstream: "ASTROCS_DESIGN.md **§9**（CPU 后端与资源）"」⇒ 索引已用正确号，文档抬头未同步；
  而索引文件头 :14 自定规则「upstream 上游条款（与文档抬头「上游」区**同源**）」⇒ 同一份索引违反自家规则（记入 H）。
- 数值表两处冻结：文档 `:50-52` 位语义 vs `lib/infrastructure/benchmark/cpu/common/include/astrocs/cpu/capability_v1.h:50-62`（`SSE2=1u<<0 … AVX512VL=1u<<12`）——本批逐值核对**一致**，登记为漂移风险而非冲突。

**D-14 CPU_003_AVX2_PROVIDER.md**
- 除 D-02 的重复表外：`:4` 抬头上游含「`ENGINEERING_SPEC.md` §10」——`ENGINEERING_SPEC.md` 的 §10 是否即「资源与性能」（CPU_001 :4 的括注）本批未逐节核对，登记为未核（J）。
- `:49`「CPU-003 注册热点 = `calibration-pixel-transform`（ALG-001）+ `hips-bulk-transform`（ALG-P3-002）——恰 2 个 kernel（**v1 冻结**）」
  vs `ISA_VARIANTS.md:74`「逐 kernel 选路(BENCH-005): calibration/hips→avx2 变体; drizzle-accumulate→**保持 baseline**」⇒ 同一选路面两处「冻结」，互不指认正本；
  且二者描述的**载体不同**（`cpu/avx2/src/avx2_provider.cpp` vs `backend_host/avx2_backend.cpp`），构建图只取后者（D-08 证据）。

**D-15 doc_symbol_namespaces.json**
- `:4` `"task": "W4-A3"` ⇒ 任务编号写进机器登记表（标准 01 §4「正文无任务编号」）。
- `:3` 自述判据「每条登记的 token 必须在 evidence 所指文件里**逐字出现**（否则 DOC-SYMBOL-REGISTRY-EVIDENCE 判红）」，
  而 4 条登记里 3 条行锚已漂移（实测：`snr_v` 在 `docs/GLOSSARY.md:15` 非 :13；`p2_write_descriptor` 在 `module_adapters.cpp:1162` 非 :1040-1057；
  `snr_path_effective` 在 `PHASE2_DETAILED_DESIGN.md:21` 非 :19，其 reason 里 `PIPELINE_BLOCK_CONTRACT.md:58` 非 :46）⇒ 判据只判文件级出现，行锚无保护 ⇒ :39「行锚 = 同面实测 grep → :1040」这句自述已是假话。

**D-16 execution_inventory.csv ↔ PRODUCTION_EXECUTION_INVENTORY.csv（同一事实相反结论）**
- `docs/architecture/execution_inventory.csv:2`（Calibration 行 evidence 末）「…｜ 生产可达性一律由 eng/tools/arch/build_production_execution_inventory.py 现场计算并落 docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv）：yes (stages[] orchestrator.cpp:5177; hard-fail if DLL missing :780)」
  同句在 :3/:4/:5/:6/:7/:8-:15 重复（每行都带「）：yes/no」，括号不配对）。
- `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv` 对同一组件的现场计算结论：
  `exe_target,orchestrator_legacy_cli,lib/infrastructure/pipeline/orchestrator/cpp/CMakeLists.txt,tool,no,-,非发布目标,…:95,`（另有 `orchestrator_test_cli`、`orchestrator_saturation_wiring_gate`、`orchestrator_curve_resolve_gate` 等同形态行；`grep -c orchestrator` = 52 行）
  ⇒ 一份手写 CSV 对 orchestrator 各阶段判「生产可达 yes」，另一份生成 CSV 对同组件判 `tool,no`「非发布目标」；
  且生成器 `eng/tools/arch/build_production_execution_inventory.py:25,29` 只写 `PRODUCTION_EXECUTION_INVENTORY.csv` 一个输出 ⇒ execution_inventory 的「由该脚本现场计算」自述为假，`git grep -l execution_inventory -- eng` 0 命中 ⇒ 无任何工具读写它（索引却记 `status: GENERATED`）。

**D-17 execution_options_contract.md**（判词见台账行 + D-08 的 `hardware_concurrency` 三分歧）
- `:5–:7` 抬头「唯一执行预算对象…以此为唯一来源」、`:9` 「⚠ **预算唯一来源**」三处「唯一」；`:34–:47` 又给 `stage2.json` 的 `execution.cpu_workers`、`:53–:56` 又给三个 CLI flag（实际持有者是第二入口 `astrocs-stage2`）⇒ 「唯一」由自家正文破。

**D-18 RESOURCE_MONITORING_CONTRACT.md（两份资源时序工件）**
- `:47` 「| **`resource_timeseries.csv`**（生产运行记录） | `lib/infrastructure/cli/resource_recorder.h` | **唯一列合同声明（Q4）**：该工件列合同**只有一处** = 生产实现 `lib/infrastructure/cli/resource_recorder.h:260-266`（20 列）；**本文件不复写其列名、不重定义其列** |」
  实测：该头文件 :260-266 是 `cpu_pct_p50/p95/peak、workers_mean/p50/peak` 的统计聚合语句；CSV 列名字面量在 **:288-293**，恰 20 列（`elapsed_seconds,stage,cpu_pct,…,io_wait_pct`）⇒ 行锚错位（列数与工件名对，锚号不对）。
- `:52-53` 「本合同的 CSV 工件名**固定为 `monitor_timeseries.csv`**…本节的 21 列合同只覆盖 **`resource_timeseries.csv`** 上。」——同一句内自相矛盾（21 列是 `monitor_timeseries.csv` 的合同，见 :48 表行）；
  且 `monitor_timeseries` 在 `lib`/`eng` 跟踪内容中 0 命中（正例对照：同式对 `resource_timeseries` 在 `lib/infrastructure/cli/*` 命中 13 处）⇒ 文档造名、无实现落点。
- 列合同本身与代码同步：`monitor.py:49-72` 的 `CONTRACT_COLUMNS`＋`row_fingerprint` = 21 列，逐名列出后与本文件 `:60-63` 完全一致（**当前无漂移**，登记为「一致但双写」）。

**D-19 RUN_GRAPH_CONTRACT.md**
- `:144-147` 排等级段原文见 §C/S1-2。
- `:103-104`「`call_count`/`entry`/…全部来自 trace 事件（`aggregate_trace`，**语义与 `trace_replay.py` 对齐**）」＋ `:134-136`「聚合语义与 `lib/infrastructure/pipeline/trace_replay.py` 对齐（同合法类型集、同 call_count 计数、同 provider 最后观测胜出）」
  ⇒ 聚合语义在文档中写了两遍且都声明正本在代码 ⇒ 与 §1「层级向下」的文档职责（说明语义，不复述实现）冲突；重复面在本文件内部。

**D-20 STRUCTURED_LOGGING_CONTRACT.md ↔ 两份日志域合同的分工（合法分层，非重复）**
- `docs/contracts/LOG_AND_ERROR_CONTRACT.md:6`「机器事实源：`lib/infrastructure/observability/logging/log_event_v1.schema.json`（日志行格式正本，LOG-001）」、`:150`「沿用 LOG-001 §5/§6，**不另立规则**」
  ⇒ LOG_AND_ERROR_CONTRACT 明确把行格式正本让给本文，属合法分层，不判重复。
- 但运行事件流面在两处各写一份字段/枚举：本文 `:29-30`（`kind` 枚举 `progress/resource/artifact/backend/final` ＋唯一源 `protocol.h`+`jsonl.h`）与 `docs/contracts/API-001.md:53`
  （「必含字段 = `schema_version/event_id/run_id/timestamp_utc/sequence/kind/severity/phase/stage/message`，事件枚举键名 = **`kind`**（progress/resource/artifact/backend/final/stage_start/stage_end）」）
  ⇒ **枚举集不同**（本文 5 值、API-001 7 值）、字段清单只有一份有 ⇒ 同主题两份且值不一致（判「疑似重叠→已证为冲突」，需以 `protocol.h` 现场计算收口）。

**D-21 production_call_paths_stage1.csv ↔ §4.3/§3.2 配置合同**
- `:2-8` 的 `config_gate` 列 = `phase_config_normalize.schema.json`（存在）、`stage1.calibrate.enabled`、`stage1.platesolve.enabled`、`stage1.photometric.enabled`、`stage1.drizzle.enabled`、`stage1.snr.enabled`、`stage1.output.hips`
  ⇒ 6 个 `stage1.*` 键在跟踪集的 `eng/` 与 `docs/contracts/` 中 **0 命中**（`git grep -l "stage1\.calibrate" -- eng docs/contracts` 无输出；正例对照：同式对 `"blocks"` 在 `eng/contracts/schemas/phase_config_mosaic.schema.json` 命中），
  现行键面是 `blocks[]`＋块内平铺（`ASTROCS_DESIGN.md:282-305`）⇒ 该 CSV 的门列整体登记的是已废配置命名空间。
- 同文件 `:3`「`Orchestrator::run_stage_calibrate … parallel_cpu (OpenMP **16**)`」⇒ 把线程数写死进登记表，与 §9「模块不硬编码线程数」、AGENTS §6「不硬编码线程/ISA/block」相反。
- 与 execution_inventory 的三处相反（符号名/并行度/测试号）见台账行；`TEST-HIPS-001` 在本文件 :8 与 stage2 :11 跨阶段复用，而 `execution_inventory.csv:7` 写「no explicit TEST-HIPS-### ID」⇒ 同一测试号三处不一致。

**D-22 production_call_paths_stage2.csv**
- `:8`「| rejection | stage2.integration.rejection.* | phase2/rejection | **p2_reject (7 methods)** | parallel_cpu per-pixel | … | `TEST-REJ-*` |」
  vs `ASTROCS_DESIGN.md:423`「生产排异算法集为 none / percentile / winsorized / linear fit；min/max 极值法**不用于生产**（WBPP 一手源码明确拒绝）」
  ⇒ 「7 methods」与「4 种且排除极值法」两口径；`execution_inventory.csv:13` 又把 `minmax 1671` 记在调用链上 ⇒ 极值法出现在生产调用登记面。
- `:10`「acr_routing(DORMANT，非生产)…（最高设计 §8）」：ACR 的非目标条款在 §1.3（`ASTROCS_DESIGN.md:98`），§8 未述 ACR ⇒ 上游条款指错。

**D-23 API-001.md ↔ 退出码表三处承载**
- 上位：`ASTROCS_DESIGN.md:498-510` 表（`| 0 | 成功且门禁全过 |` … `| 10 | 磁盘写满/写盘失败 |` `| 70 | 未分类内部错误（脱敏 crash report）|`），且 `:521`「退出码唯一源 = `lib/infrastructure/cli/exit_codes.h`（§7.2 表）」。
- 本批：`docs/contracts/API-001.md:23-37` 再抄一份（`:23` 自己写「`lib/infrastructure/cli/exit_codes.h` 唯一源，冻结」），措辞与上位有差：
  上位 `| 3 | 输入缺失/格式错 |` → 本文 `| 3 | 输入缺失、格式或 hash 错误 |`；上位 `| 0 | 成功且门禁全过 |` → 本文 `| 0 | 成功，且所有请求门禁通过 |`；本文 `:36` 第 10 码追加一整段带（错指）§号的解释。
- 第三处：`docs/api/CLI_PROTOCOL_V1.md:35`「## 2 退出码(全 11 条冻结,唯一源 `lib/infrastructure/cli/exit_codes.h`)」。⇒ 同一 11 码表在三份文档承载，两份逐字复制、一份加解释，唯一源声明相同而副本口径不同 ⇒ 标准 01 §1「冲突即缺陷」＋§4「数值表下沉」。
- 行数对证：`API-001.md:66`「`docs/contracts/API_CONTRACTS.csv` 登记 **423 行**现有 C ABI（`aio_*`/`p2_*`/`p1_*`/`p3_*`），状态 VERIFIED」
  vs 实测 `wc -l` = 382 行（表头 1 ＋ **381** 数据行）、前缀计数 `aio_*` 106 / `p2_*` 35 / **`p1_*` 0** / **`p3_*` 0** ⇒ 行数与符号族两项描述双双不符。

**D-24 API_CONTRACTS.csv**
- 全列枚举（J 口径）：22 列；`linkage` 唯一值 `C`（381/381）；`status` 唯一值 `VERIFIED`（381/381）。
- 与上位 §12.5 对证：`ASTROCS_DESIGN.md:801`「状态由检查与验收现场计算，**登记表不预写状态**」；`docs/DOCUMENT_INDEX.yaml` 头注 :10-11 亦写「status … 由检查器现场校验；**不是** §12.5 交付状态阶梯」⇒ 本 CSV 的 `status` 列即预写状态。
- 与 MODULE_MAP :29「当前为 **Linux 技术预览** 安装面；Windows 侧复验 NOT_VERIFIED」对证：VERIFIED 定义 = 「正式平台 + 真实数据验收通过」（§12.5），381 行含 `orchestrator.h` 31 个符号（待退役组件）⇒ 状态词跨轴串用（派单 §5.6 明禁）。

**D-25 ARCH-001.md ↔ §8.1/§8.3 执行平面归属**
- `docs/contracts/ARCH-001.md:14-15`「**只有 Pipeline Runtime 拥有全局执行顺序与资源预算**。CLI、I/O、科学模块、计算后端的调度一律走唯一全局执行平面（Pipeline Runtime）。」
- `ASTROCS_DESIGN.md:547`「每个阶段实例化自己的调度器与内存管线，**互不共享**内存、会话或运行时状态；一次 CLI 调用只驱动一个阶段。」＋`:599`「调度器是**阶段内**的唯一执行者」
  ⇒ 「Pipeline Runtime」这一具名组件在 §8.4 顶层结构与跟踪集中都不存在（`git grep -l "Pipeline Runtime"` 只命中 ARCH-001、API-001 与 `eng/tools/quality/check_prod_reachability.py`），实际唯一 executor 由 MODULE_MAP §2 记在 `lib/infrastructure/scheduler` ⇒ 孤儿组件名＋「全局」与「阶段内」的归属差。
- `:9`「架构问题的权威 = `ASTROCS_DESIGN.md` **§7**」——同文件 `:3` 写 §8、标准 01 §1 与 `ASTROCS_DESIGN.md:44` 均写 §8 ⇒ 订正后的替代句本身指错条款。
- `:6-7` 留痕原文（撤销的「唯一权威」声明仍以引文在场）见 §C/S3-10。
- `:80-90` §7「冻结的现状差距」6 行（`P0-006/P1-002/P1-003/P1-005/P1-007/P2-002` ＋ 迁移任务号 `CLI-002/LEG-001..004/P2-002`，含「orchestrator.cpp 5522 行」「aio_pipeline_engine 内置 5-stage 调度 + omp16」）
  ⇒ 现状/缺陷登记放在架构合同正文（§12.5 状态应现场计算；MODULE_MAP §4 另有一份等价登记 ⇒ 差距面双载，两处互指但不互校）。
  注：其中「5522 行」经复核为**准确**（`lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp` 实测 5522 行）⇒ 本行的缺陷是形态（预写状态台账），不是数值错。

## G 同主题组与差异（含跨批注明）

| 主题组 | 本批各份 | 别批相关 | 正本候选 | 主要差异 | 合并建议 |
|---|---|---|---|---|---|
| ISA 变体 / CPU 后端发货 | ISA_VARIANTS.md、ISA_BIT_MANIP_VARIANTS.md、cpu/CPU_001、cpu/CPU_003 | `docs/architecture/CPU_BACKEND_ARCH.md`（DB-04）、`docs/standards/`（H 列编号文档缺位） | 待裁：`backend_host` 变体面 or `cpu/*` provider 面 | 载体目录不同、kernel 集合不同（12-kernel vs 恰 2 kernel）、实测表复制三份、其一不在构建图 | 先由负责人裁哪套是生产机制；随后 4 份合 1 份性能/后端页，实测数值迁 `artifacts/evidence/` |
| 线程预算与并行轴 | THREADING_MODEL.md、THREAD_BUDGET_ARCH.md、PERFORMANCE_MODEL.md、execution_options_contract.md | `docs/contracts/SCHEDULER_CONTRACT.md`、`docs/standards/CONCURRENCY_STANDARD.md`、`docs/architecture/EXECUTION_MODEL.md`（DB-04） | THREADING_MODEL.md（分层＋锚点） | 权威声明互斥（BUDGET 自称压过 THREADING）、取值权威判给任务号 PERF-501、CLI flag 归属错、`hardware_concurrency` 三处口径不一 | 保留 THREADING_MODEL 为架构面正本；预算数值/标定迁 `docs/performance/`＋evidence；CON-002 字段表迁 `docs/contracts/` |
| 生产调用/可达性清单 | PRODUCTION_EXECUTION_INVENTORY.csv、execution_inventory.csv、production_call_paths_stage1.csv、production_call_paths_stage2.csv | `eng/ci/INVENTORY_REPORT.md`、`docs/owner/PIPELINE_OVERVIEW.md` | PRODUCTION_EXECUTION_INVENTORY.csv（唯一由生成器产出的那份） | 其余三份：无生成器、退役组件判 yes、`stage1./stage2.` 配置键不存在、SNR 并行度与 min/max 排异集与上位相反 | 保留生成件（迁出 docs/），删除三份手写/过期件 |
| 日志 / 事件流 / 资源观测（LOG-001..004） | observability/{STRUCTURED_LOGGING,RESOURCE_MONITORING,RUN_GRAPH}_CONTRACT.md | `docs/contracts/LOG_AND_ERROR_CONTRACT.md`、`docs/design/LOG_AND_ERROR_SYSTEM.md`、`docs/api/CLI_PROTOCOL_V1.md` §4 | 三份各为本面正本，但都住错目录 | 运行事件流枚举两份不同（5 值 vs 7 值，D-20）；两份资源时序工件（D-18）；「唯一口径」措辞 | 三份迁 `docs/contracts/`（渲染器迁 `docs/diagnostics/`）；事件流字段以 `protocol.h` 现场计算收口 |
| 模块交付面 | MODULE_MAP.md | `docs/modules/**`、`docs/contracts/INDEX.yaml`、`eng/packaging/astrocs.product.json` | MODULE_MAP.md（迁移后） | 状态列预写；1 行路径不在跟踪集；§8.4 的「并联命名」未贯穿 | 迁 `docs/modules/`，状态改现场计算 |
| C ABI / 公共 API 合同 | API-001.md、API_CONTRACTS.csv、api_inventory.csv | `docs/contracts/PUBLIC_API.md`、`docs/api/*.md`、`docs/standards/API_STANDARD.md`、`docs/interfaces/**` | 逐符号：API_CONTRACTS.csv；人类可读：PUBLIC_API.md | 同一符号面三件互不指认；行数/符号族描述与实际不符（D-23）；status 预写 | 合并 API-001 的 ABI 节入 PUBLIC_API/API_STANDARD；两份 CSV 保留但去 status |
| 目标态架构合同（V6 层） | ARCH-001.md | `docs/architecture/ARCHITECTURE.md`（DB-04）、`docs/contracts/RT-001.md` | 最高设计 §8（架构唯一权威） | 上游号自相矛盾（§7 vs §8）；含现状差距台账 | 与 ARCHITECTURE.md 一并重写为 §8 承接页，差距表迁 owner/known-limitations 面 |
| Phase3 内部结构 | PHASE3_MODULE_ARCH.md | `docs/plugins/algorithms_phase3/**`、`docs/algorithms/PHASE3_RESAMPLE.md` | `docs/plugins/algorithms_phase3/`（迁移目标） | 目录名 `lib/phase3` 失效；默认值与 defaults.json 空档（D-06） | 迁 `docs/plugins/algorithms_phase3/` 并把数值改指针 |

## H 索引与链接缺陷清单

**H-0 悬空判定的统一命令（含正例对照，满足派单 §5.2）**

```bash
R="F:/Astro dev/Astro CS Normalization Database"
# 编号文档族（被引：02/03/04/05/07/12/14/15/23_xx + ARCH-002/003/004）
git -C "$R" ls-files | grep -cE "(^|/)(0[2-7]|1[45]|2[0-9])_[A-Z]"      # → 6（正例：docs/ci/01_CHECKS.md…04_ARTIFACTS.md 等被数到）
git -C "$R" ls-files | grep -cE "ARCH-00[2-4]"                          # → 0（同式对 ARCH-001 → 2）
git -C "$R" ls-files | grep -cE "0[3-5]_[A-Z]*(TARGET|CPU|RESOURCE)"    # → 0
git -C "$R" log --diff-filter=D --name-only -- 'artifacts/evidence/prerelease-v5/ISA-00[45]'   # → 空（从未入库，非删除）
git -C "$R" ls-files -- artifacts/evidence/prerelease-v5 | cut -d/ -f4 | sort -u               # → ISA-001/002/003
git -C "$R" grep -cE "stage1\.calibrate|stage2\.inputs|stage2\.model" -- eng docs/contracts    # → 0（同式对 "blocks" 命中 mosaic/normalize schema）
```

结论：以下 5 类引用**判为悬空**——`ARCH-002/003/004`、`03_TARGET_*`/`04_CPU_RESOURCE_TASKS`/`14_*_STANDARD`/`23_*_POLICY`/裸编号「`02 §x`」「`03 §91`」「`05 §x`」「`07`」「`12 §3`」「`15 §x`」「`约束 §Cx/§Ex`」、
`artifacts/evidence/prerelease-v5/ISA-00{4,5}/MEASUREMENTS.csv`、`run/**`（19 条，见 H-3）、`evidence/**`＋`v6_1_rework`。

**H-1 未登记**：**0**。本批 25 份全部在 `docs/DOCUMENT_INDEX.yaml` active 区段登记（逐条命中：`for p in $(tr -d '\r' < inventory/DB-05.txt); do grep -q "$p" docs/DOCUMENT_INDEX.yaml && echo REG $p; done` → 25 REG）。
任务书步骤 5 隐含的「存在未登记项」前提在本批不成立。

**H-2 登记了但文件不存在**：**0**（25 条 path 全部实存）。缺陷形态在别处：见 H-3～H-6。

**H-3 登记内容与实际不符（上游不同源）**——索引文件头 :13 自定规则「upstream…与文档抬头「上游」区**同源**」，以下 5 例违反自家规则：

| 文件 | 文档抬头写的上游 | 索引登记的 upstream | 应属条款 |
|---|---|---|---|
| cpu/CPU_001_CAPABILITY_PROBE.md | §8（括注「CPU 后端与资源」） | §9 | §9 |
| cpu/CPU_003_AVX2_PROVIDER.md | §8 | §9 | §9 |
| abi/ABI_003_SECURE_LOADER.md | §7.3（版本化 C ABI/DLL 边界） | §8.4（模块与 ABI）、§11 | §8.4＋§8.5（§8.4 标题实为「顶层结构」） |
| observability/×3 | §8.1（顶层结构）＋§9 | 同左 | §7.3 / §10（日志与运行图、资源时序的上位条款）＋§9 |
| contracts/ARCH-001.md | 抬头 §8，正文 :9 又写 §7 | §8 | §8 |

**H-4 角色/status 标注与判定不符**（派单 §4「角色」轴＋前言 §6.2）：

| 条目 | 索引标注 | 本批判定 | 依据 |
|---|---|---|---|
| API-001.md | `status: ACTIVE_NORMATIVE`、duty「C ABI 与模块接口合同（**历史合同层**）」 | 合同说明，但**非现行规范面** | 文件 :3 自述「设计档案 / 产品族专用投影（非生产目标态）」；索引 duty 与 status 互斥 |
| ARCH-001.md | `ACTIVE_NORMATIVE`、duty「架构合同（**历史合同层**）」 | 架构设计（目标态） | 标题自述「V6 **目标**架构合同」；与 §4「只写现行设计」冲突 |
| ISA_VARIANTS.md / ISA_BIT_MANIP_VARIANTS.md / PERFORMANCE_MODEL.md | `ACTIVE_NORMATIVE` | 操作与运营（实测/标定台账） | 内容为逐 kernel ns 表、标定推导、A/B 实测 ⇒ 应落 `docs/performance/`（标准 01 §2 表行 8） |
| PRODUCTION_EXECUTION_INVENTORY.csv / api_inventory.csv / doc_symbol_namespaces.json / execution_inventory.csv / production_call_paths_stage{1,2}.csv | `GENERATED` | 标准与检查／过程台账 | 6 份「机器生成清单」住在正式文档层；其中 `execution_inventory.csv` 无任何 eng/ 工具读写 ⇒ 标 GENERATED 却非生成件（D-16） |
| MODULE_MAP.md | `ACTIVE_NORMATIVE`、duty「Module Map」 | 模块工作细节 | 标准 01 §2 表行 4 ⇒ 应落 `docs/modules/` |
| contracts/API_CONTRACTS.csv | `ACTIVE_NORMATIVE`，upstream §3.1＋§10 | 合同说明，upstream 应含 §8.5 | 内容是逐符号 C ABI 合同（§8.5），非数据对象/I-O 面 |
| PHASE3_MODULE_ARCH.md | `ACTIVE_NORMATIVE`、duty「Phase3 模块架构」 | 模块工作细节 | 标准 01 §2 表行 4 ⇒ `docs/plugins/algorithms_phase3/` |

**H-5 两份索引对同一条目登记不一致**：`docs/DOCUMENT_INDEX.yaml` 给 API-001/ARCH-001/API_CONTRACTS 记 §8.x 上游；
`docs/contracts/INDEX.yaml` 给同三件记 `upstream: []`（ARCH-001 :757、API_CONTRACTS :767）或 `[ARCH-001]`（API-001 :775）
⇒ 同一文档两个上游面，且合同图那条链的顶点（ARCH-001 自己的上游）是**不在跟踪集**的 `03_TARGET_ARCHITECTURE.md (控制包)` ⇒ 标准 01 §3「每份下层文档开头标明上游最高设计条款」在合同图层整体不成立。

**H-6 `run/` 锚（实测 14 处引用／去重 12 条唯一路径；按派单 §5.6 用词：`run/` 不入库 ⇒ 干净检出上读者不可复算）**：
命令 `grep -o "run/[A-Za-z0-9._*/-]*" <file> | sort -u`；
PERFORMANCE_MODEL.md **12 条唯一**（引用位 `:20 :24 :28 :37 :55 :56 :59 :61 :83 :100 :107 :207 :238`）、
THREADING_MODEL.md **2 条**（`:63 :110`，二者与前者重合）；
ISA 组 ×2（ISA_VARIANTS `:6 :24 :52 :69`、ISA_BIT_MANIP `:41`，其中 ISA-004/005 两条在跟踪集与 `run/` 都无）、
12_rejection 侧被引用的 `run/REJECT-DOCFIX-01/REPORT.md`（别批，仅注明）。

**H-7 最高设计侧断链（本批共性根因）**：`ASTROCS_DESIGN.md` §8（:542-666）全节 `docs/` 指针数 **0**（`awk '/^## 8\. /,/^## 9\. /' ASTROCS_DESIGN.md | grep -c 'docs/'` → 0）
⇒ 标准 01 §3「最高设计的每个机制都有指向下层文档的索引」在 §8 未落实；22 份 architecture 文档的「上游：§8」全为单向。D 表的 12 条机制逐条都缺上位指针。

## I 机械列校准（`inventory/doc_mechanical.csv`，本批 25 行）

以本批为准的偏差（判据：派单 §5.4「凡你发现的与机械列不一致处，显式写机械列漏报/误报了 X」）：

| 列 | 漏报/误报 | 本批实测 |
|---|---|---|
| `meta_block` | **漏报 16 份（18 份 markdown 中除 OWNERSHIP、THREADING_MODEL 外全部有开头块，工具一律标 `False`）** | ISA_BIT_MANIP `:3-6`、ISA_VARIANTS `:3-6`、MODULE_MAP `:5-11`、PERFORMANCE_MODEL `:15`（标题即任务号＋日期）、PHASE3_MODULE_ARCH `:5-6`、PIPELINE `:5-8`、THREAD_BUDGET `:5-6`、ABI_003 `:3-9`、CPU_001 `:3-8`、CPU_003 `:3-13`、execution_options `:5-15`、observability ×3 `:5-9`、API-001 `:3-9`、ARCH-001 `:5-10`、doc_symbol_namespaces.json `:3`（note 内纪律声明）⇒ 共 **16 份**（工具只标 0 份）；根因：工具只认「紧跟 H1 的 `key: value` 块」，不认 markdown 引用块（`>`）形式的 ID/状态/上游/下游/纪律声明 |
| `dangling_links` | **漏报 ≥18 条 / 误报 3 条** | 漏：`run/**` 14 处＝12 条唯一路径（PERFORMANCE_MODEL 12 ＋ THREADING_MODEL 2，后者与前者重合）、`evidence/**`+`v6_1_rework` 1 条、编号文档 5 类（`ARCH-002/003/004`、`03_TARGET_*`、`04_CPU_RESOURCE_TASKS`、`14_*_STANDARD`、`23_*_POLICY`）＋裸编号节引（「`12 §3`」「`03 §91`」「`05 §1-3`」「`15 §1/§2/§6`」「`02 §10.1`」「`约束 §C*/§E*`」）⇒ 工具只判含 `/` 的相对路径字面量，`run/` 与编号文档均不进判据面；误：`execution_options_contract.md` 的 `phase_config_*.schema.json`（通配，三份实存）、`API_CONTRACTS.csv:79`（`config_keys` 单元格里的 `sha256` 字样）、`execution_inventory.csv` 的 `noise_model_impl`（函数名非路径） |
| `sha256` | **误报 2 份** | `api_inventory.csv`、`API_CONTRACTS.csv` 因符号名 `compute_config_sha256`/`get_stage1_schema_sha256` 被判 `True` |
| `task_ids` | **漏报 20 份＋部分漏报 1 份**（正确为空 3 份：PIPELINE、execution_inventory.csv、api_inventory.csv；不适用 1 份：API_CONTRACTS.csv 的 `id` 列本身即合同号） | 全部为 `[]` 而实含编号：ISA 组（`ISA-001..005`、`WIN-003`、`BENCH-005`）、MODULE_MAP（`ABI-003/004`、`CLI-001`、`LEG-003`、`MOD-001`、`P2-002`、`RT-002`、`W4-A3`）、OWNERSHIP（`V19R2 PR#1`）、PHASE3（`ALG-007`、`ARCH-002/003/004`、`P3-001..004`、`SYN-007`）、THREADING（`PERF-501/401`、`P1-*`×2、`ARC-004`）、THREAD_BUDGET（`ARCH-001..004`、`BENCH-003/004`、`ABI-001`）、ABI_003（`ABI-001..005`、`BLD-003`、`CLI-001`、`CPU-002`、`WIN-*`）、CPU_001/003（`CPU-001..008`、`ALG-001`、`P3-002`）、execution_options（`CON-002/008`）、observability ×3（`LOG-001..004`、`RT-001/006`、`CORE-008`）、call_paths ×2（`TEST-CLI-001/002`、`TEST-HIPS-001`）、API-001（`DOC-004/203`、`CHG-2026-…`、`CORE-001..008`）、ARCH-001（`DOC-202 R04`、`LEG-001..004`、`P0-006`、`P1-00x`、`BAS-002/003`）、PRODUCTION_EXECUTION_INVENTORY.csv（`CLEAN-401`、`CONFORM-SWEEP-4`、`V5:`×26 在 `risk_note` 列，工具不扫该列）；PERFORMANCE_MODEL 只命中 `FIX-01/RELEASE-05`，漏 `PERF-501`、`PERF-MEM-FIX-01`、`P1-CONCURRENCY-CALIB-01`、`P1-PARALLEL-AXIS-REDESIGN-01`、`G-RES-01`、`PERF-PROFILE-01`；根因：正则只覆盖 `EXP-/RELEASE-/FIX-/AUD-` 四前缀，且不扫 CSV 列 |
| `dates` | 一致 | PERFORMANCE_MODEL `[15,81,222]`、API-001 `[3]`、ARCH-001 `[6]` 与本批人工核对相同 ⇒ 该列可用 |
| `lines` | 一致 | 25/25 与 `wc -l` 相同（清单文件本身是 CRLF，需 `tr -d '\r'` 后测，否则路径带 `\r` 全数失败） |
| 整行缺失 | **无** | 本批 25 行在 CSV 中全在（含 `ARCH-001.md`，行 181）；派单 §1 的「机械列可当线索」前提成立 |

## J 覆盖率自报

**已审 25 / 分配 25。未逐行读名单：无。**

- 22 份文本件（含 3 份 JSON/CSV 小件）逐行通读：`ISA_BIT_MANIP 42`、`ISA_VARIANTS 85`、`MODULE_MAP 110`、`OWNERSHIP 24`、`PERFORMANCE_MODEL 245`、`PHASE3 64`、`PIPELINE 63`、`THREADING 128`、`THREAD_BUDGET 48`、`ABI_003 113`、`CPU_001 104`、`CPU_003 158`、`doc_symbol_namespaces 48`、`execution_options 71`、`RESOURCE 185`、`RUN_GRAPH 173`、`STRUCTURED 169`、`call_paths_s1 8`、`call_paths_s2 11`、`API-001 74`、`ARCH-001 94`、`execution_inventory 15`（行数＝`wc -l`）。
- 3 份大 CSV（`PRODUCTION_EXECUTION_INVENTORY 562`、`api_inventory 449`、`API_CONTRACTS 382`）为再生表格，无正文段落可「逐行读」；
  替代动作＝**逐列全值枚举**（`csv.DictReader`＋`Counter`，含空值计数），9/22/4 列全覆盖，命令与结果见 D-08、D-12、D-24 ⇒ 该 3 份按此口径记「已通读（列枚举）」，不以「行数大」为由登记未读。
- 未逐锚核清单（如实登记，不作为结论依据）：`execution_inventory.csv` 内 `ipv_entry.cpp:524`、`ipv_solver.cpp:1389`、`pc_api.cpp:1048`、`sampler.cpp:1115`、`upm.cpp:749` 等约 60 个源码行锚（逐条核对＝改判源码审计，超出本批文档范围）；
  本批只对**被用作冲突判据**的锚做了实测：`module_registry.c:570`、`secure_loader.c:369`、`install_layout.cmake:104-105`、`module_adapters.cpp:1040`（漂移，实测 1162）、`GLOSSARY.md:13`（漂移，实测 15）、`resource_recorder.h:260-266`（错位，实测 288-293）、`orchestrator.cpp` 行数（5522，准确）。
- 判「不存在」全部改用跟踪集＋删除历史（`git ls-files`／`git log --diff-filter=D`）并带正例对照（H-0），不以磁盘存在性为据。

## K 我证伪了任务书的哪个前提

1. **未证伪：「约 233 份」**。固定口径 `git ls-files 'docs/*.md' | wc -l` = **233**（精确）。
   按 `git ls-files docs | wc -l` = 258（含 12 csv / 8 json / 3 yaml / 2 py）。⇒ 派单 §5.3「别人口头汇总的数不得直接写进成稿」与负责人「口径要固定」的裁决在此落地：先前流转的「233 vs 360」不成立，360 是 `doc_mechanical.csv` 的行数（扫面比 `docs/**.md` 宽），非同一口径。
2. **证伪：「30+ 子目录」**。`git ls-files docs | cut -d/ -f2 | sort -u | wc -l` = 30，但其中 7 项是 `docs/` 根散文件
   （`DEVELOPER_GUIDE.md DOCUMENT_INDEX.yaml GLOSSARY.md KNOWN_LIMITATIONS.md TRACEABILITY.csv TROUBLESHOOTING.md VERSIONING.md`）⇒ **跟踪集内 `docs/` 一级子目录 = 23**，「30+」把文件计成了目录。
3. **证伪（本批局部）：任务书步骤 5 的前提「存在未登记项」**。本批 25 份在 `DOCUMENT_INDEX.yaml` 全部登记、全部实存 ⇒ 「未登记 / 登记了但文件不存在」两类缺陷在本批为 0 命中（H-1、H-2 附命令）；真实缺陷形态是**登记字段与文档抬头不同源**（H-3，5 例）与**角色/status 语义冲突**（H-4，8 例）。
4. **指出派单与清单的差异（派单 §3 要求）**：派单称本批「`docs/architecture/` 为主」并给出「逐份判是否在另立一套架构权威」的关注点，
   清单 25 份里有 3 份在 `docs/contracts/`、8 份是 CSV/JSON 再生件。再生件无散文载体 ⇒ 「自称唯一口径/本文件是架构事实源」这一判据对那 8 份不可适用；
   本批对其改用可适用的等价判据：**生成件是否承载规范断言**（结论：是——`risk_note` 列写「唯一发布入口」、`thread_model` 列写「OpenMP 16」、`status` 列预写 VERIFIED），判词见 D-08、D-21、D-24。
5. **未证伪：派单 §1「当前树应恰有两个未跟踪项」**。开工与收工均为 `?? ACSD整治工作包_AUDIT-06.zip` ＋ `?? site/`，本批取证未新增任何项（F 节原文，L 收工基线）。

## L 收工基线

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工一致 ⇒ 取证动作未污染被审对象。本批取证只用了 `git status/ls-files/grep/log`、`sed/cat/wc/grep/ls` 与只读 `python3`（无 `import` 仓库内模块、无写盘）。

<!-- PROGRESS: 已审 25/25 -->

