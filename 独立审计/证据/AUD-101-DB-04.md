# AUD-101 · DB-04 文档审计台账（`docs/plugins/` 工序规范 × `docs/architecture/`）

批次：`DB-04`　清单：`独立审计/批次清单/DB-04.txt`（25 行）　基线 HEAD：`c8f64e9a`
判据：`standards/01_文档权威链整治标准.md`（下称 标准01）全文；`tasks/AUD-101`；`templates/TPL_文档审计台账.md`；上游正本 `ASTROCS_DESIGN.md`（826 行，读 §0/§0.1/§0.2/§0.3/§1.3/§3.2-3.3/§4.5/§5/§6/§7.1-7.3/§8/§9/§10/§12.5/§13）。

## 0 基线与只读自证

开工 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工同命令输出与一次配置类动作的自纠：见本文件末尾 §7。**全程零写跟踪件、零构建、零 `ctest`、零 `run_checks.py`、零 `eng/**` 执行**；对仓库只用 `Read`/`ls`/`find`/`git grep`/`git ls-files`。Python 一律 `-B` 且不落仓库内目录，无 `__pycache__` 新增。

**派单与清单不符（按前言 §3 以清单为准）**：派单把本批描述为「`docs/plugins/` 与 `docs/modules/` 交界」，清单实际是 `docs/plugins/**`（14 份）+ `docs/architecture/**`（11 份），**不含任何 `docs/modules/` 项**。本批只对清单 25 份下结论；为判「工序规范 vs 模块工作细节」的重复，按前言 §3 许可**只读**了 `docs/modules/phase2_samp.md`、`docs/modules/phase2_rej.md` 的相关节，结论只落在清单项上，越界部分只在 §2 主题组里以「别批路径」标注。

存在性判定统一手段：`ls -d <路径>`、`ls <目录>`、`git ls-files | grep`；引用面判定统一手段：`git -C "F:/Astro dev/Astro CS Normalization Database" grep -n "<词>" -- docs lib eng`（本机 `rg` 不可用）。

## 1 逐份登记

### 1.1 `docs/plugins/algorithms_phase2/10_sampling.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase2/10_sampling.md` |
| 标题 | 插件文档：sampling（控制采样） |
| 行数 | 122 |
| 角色 | 模块工作细节；实际所在 `docs/plugins/`＝标准01 §2 应属目录 ⇒ **一致** |
| 主题 | mosaic 控制采样：星点掩膜 + 光度控制点 + 天光背景采样点 + `control_ivar` 定权 |
| 上游 | `:3` 与 `:12` 标 `ASTROCS_DESIGN.md §5.2（固定科学流程）`、`:12` 另标 §5.4 —— 两条均真实存在且语义对得上（§5.2 在 `ASTROCS_DESIGN.md:371`、§5.4 在 `:406`）⇒ **不缺** |
| 下游 | 12 个跟踪引用者（命令：`git grep -l --fixed-strings 10_sampling.md`，剔除自身）。含最高设计 `ASTROCS_DESIGN.md:409`、`docs/science/PHASE2_UPM.md`、`docs/algorithms/PHASE2_SAMPLER.md`、`docs/modules/MODULE_MAP.yaml`、`lib/algorithms/coverage/{include/astro/phase2/sampler.h,include/astro/phase2/sky_plane.h,src/sampler.cpp}` |
| 是否正本 | 采样工序的正本（模块工作细节层唯一一份）。**但它同时充当权重公式的口径载体，而该公式另有科学正本** ⇒ 见下行 |
| 重复或重叠对象 | **A. 权重公式四处承载（同值，非数值分歧）**<br>`:47`「`w_ki = control_ivar_ki = N_retained/(k_corr·(π/2)·σ_bg²)`（= `1/control_variance`，冻结式 `control_variance = k_corr·(π/2)·σ_bg²/N_retained` 见 `docs/modules/phase2_samp.md` §6 / `docs/contracts/DATA_SEMANTICS.md` §23）」<br>对照 `docs/science/PHASE2_UPM.md:20`「| `control_variance` | `k_corr·(π/2)·σ_bg²/N_retained` | `ALG-UPM-CONTROL-IVAR-001` |」；`docs/algorithms/PHASE2_SAMPLER.md:248`「`control_variance = k_corr × (π/2) × σ_bg² / N_retained    # :875`」；`docs/contracts/DATA_SEMANTICS.md:1764`（同一式 + Cramér 1946 §28.5 一手引文 + 适用域）。四处**取值一致**。<br>**B. 与 `docs/modules/phase2_samp.md`（别批）同角色同主题**：标准01 §2 把 `docs/modules/` 与 `docs/plugins/` 定为**同一角色**「模块工作细节」，两处各有一份 sampling 模块页，`:47` 又把「§6」当作权威推给 `docs/modules/phase2_samp.md`（该文件 `:54` 自述「control estimator 方差权威 = k_corr×(π/2)×σ_bg²/N_retained」）⇒ 同角色内同主题两份并存，违反标准01 §2「同一角色内同一主题只有一份正本」 |
| 元信息块 | 无（`:1` 标题、`:3` 上游行，符合标准01 §4）。机械列 `meta_block=False` 正确 |
| 写法违规 | ① `:99`「`## 9 SCI-C 实测结论（RELEASE-04 / SCI-403）`」——任务/实验编号进正文（标准01 §4「正文无任务编号」）＋ `:101` 把实验单元的报告/结果/复跑脚本（`实验/additive-sky-seamless/README.md`、`results/*.json`、`code/run_all.sh`）当本文内容铺陈；② `:103-116` 整段实验过程数据（「伪影漏入 0.0245 e⁻…比 uniform 小 2.7×」「1374 采样点 / 4 帧」「14,001 B vs 8,389,129 B（0.167%）」「χ²_red 0.771」「κ(H_red)=3.16e7」）——标准01 §4「实验过程数据放对应下层文档」的**下层**指科学/算法推导与实验单元，不指模块工作细节页；③ `:7`「本期 `g_k ≡ 1`，不做乘性响应」、`:54`「（本期 `g_k ≡ 1`，不估计乘性响应）」——「本期」是时态限定（标准01 §4 只写现行设计）；④ `:61`「**可辨识性判决只有一条口径**（欠定与病态是同一条不等式的两种读法）」——判决位与全部读数 `:61` 自称「见 `11_upm.md` §4.7」却又在本节把阈值 `τ = rank_rtol`、地板 `max(m,n)·eps`、恒真门论证全部复述一遍 ⇒ 与它自己声明的「只引用不复制」相悖 |
| 悬空引用 | `:24`「参考：`eng/contracts/schemas/control_points.schema.json`」——**该文件不存在**：`ls eng/contracts/schemas/` 实测该目录只有 20 个 `*.schema.json` + `unified/`，无 `control_points.schema.json`；`git grep -l control_points.schema` 只命中本文与 `docs/DOCUMENT_INDEX.yaml`，合同面无此 schema。机械列 `dangling_links=['eng/contracts/schemas/control_points.schema.json']` 已捕获（本条机械列未漏报） |
| 与上位冲突 | **无取值冲突**（`control_variance` 四处同值，`SNR²` 不是有效逆方差代理与 `ASTROCS_DESIGN.md:409` 同口径）。**有一处上游指错**：`:47`「推导与依据见 `docs/science/CONTROL_WEIGHT_SNR.md` 与 `docs/modules/phase2_samp.md` §6」——`docs/science/CONTROL_WEIGHT_SNR.md` 全文 `git grep -n "control_ivar\|control_variance\|k_corr\|N_retained"` **0 命中**，即被指为「推导与依据」的科学文档根本不承载该式；该式的科学正本实际是 `docs/science/PHASE2_UPM.md:20-21`（`docs/TRACEABILITY.csv:2` 的 `authority_doc` 亦为 `docs/science/PHASE2_UPM.md`）⇒ 指错了权威文件。另：`ASTROCS_DESIGN.md:409` 把「权重式与推导」的落点写成本文（`权重式与推导见 docs/plugins/algorithms_phase2/10_sampling.md`），与标准01 §1「科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`」相互矛盾（层级倒置由上层授权造成，见 §5 上呈项） |
| 处置建议 | **保留 + 就地订正**（路径不变，角色与目录一致）。三项动作：① 删 §9 整节，实测结论归 `实验/additive-sky-seamless/` 并由 `docs/science/PHASE2_UPM.md` 引用（标准01 §2「历史档案、过程台账…不放在正式文档层；需要留证的台账落 `artifacts/evidence/` 并由正本引用」）；② `:47` 的「推导与依据」改指 `docs/science/PHASE2_UPM.md` §（`control_variance` 表行）+ `docs/algorithms/PHASE2_SAMPLER.md` §5.4，并删去本文对公式的重述、只留工序语义（标准01 §1 权威链 + §4 公式下沉）；③ `:24` 的 `control_points.schema.json` 改为指向真实合同面（`eng/contracts/schemas/unified/` 下的 `source_snr`/`frame_snr` 族或 `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`），或登记为待补 schema（与 `docs/KNOWN_LIMITATIONS.md` 第 39 条同族，见 §3） |

### 1.2 `docs/plugins/algorithms_phase2/11_upm.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase2/11_upm.md` |
| 标题 | 插件文档：upm（统一相对模型） |
| 行数 | 189 |
| 角色 | 模块工作细节；所在目录一致。**但 §4 实际承载科学判据与公式 ⇒ 内容角色越界到科学定义**，标准01 §2 该部分应属 `docs/science/` |
| 主题 | mosaic UPM：公共天光面 `B_ref` + 逐帧梯度 `δ_k` 的加性拟合、施加、接缝判据、可辨识性判决、配置 |
| 上游 | `:3` 标 §5.2（存在，`ASTROCS_DESIGN.md:371`）。`:12` 标「§4.4（UPM、天光亮度平面）」——**条款号错**：§4.4 实际标题是「输出合同」（`:316`），UPM 条款是 §5.4（`:406`） |
| 下游 | 16 个跟踪引用者（`git grep -l --fixed-strings 11_upm.md`）。含 `ASTROCS_DESIGN.md:411/412/413`、`docs/science/PHASE2_UPM.md`、`docs/contracts/CONFIG_CONTRACT.md`、`lib/algorithms/coverage/{src/upm.cpp,src/sky_plane.cpp,include/astro/phase2/upm.h,...}`、`10_sampling.md:49/61` |
| 是否正本 | **在两处是正本、在两处不是**：工序/配置面 = 正本（保留）；§4.1 模型式、§4.4 目标函数、§4.6 收敛判据、§4.7 可辨识性判决 = **第二份正本**（科学/算法层已有正本，见「与上位冲突」） |
| 重复或重叠对象 | **A. 可辨识性判据四处承载，且科学层把「口径正本」下推给本文**<br>`docs/science/PHASE2_UPM.md:134`「…**口径正本 = `docs/plugins/algorithms_phase2/11_upm.md`**，实现锚 = `lib/algorithms/coverage/include/astro/phase2/identifiability.h`」<br>本文 `:97-99`「**唯一判据**：判在**未正则化**的列均衡数据信息矩阵 `H_red` 上（`H_eq = D⁻¹ H_red D⁻¹`，`D = diag(√H_ii)`），唯一相对阈值 `τ = rank_rtol`（地板 `max(m,n)·eps`）：`identifiable ⟺ r_eff == n_free ⟺ κ(H_red) < 1/τ`」<br>同一科学层自身也写了：`docs/science/PHASE2_UPM.md:199`「阈值**只有一个** `τ = rank_rtol`（**相对**口径，尺度不变），由浮点算术给出（精度地板 `max(m,n)·eps`）」、`:217`、`:381`<br>第三处 `docs/contracts/DATA_SEMANTICS.md:2224`「`rank_rtol`（地板 `max(m,n)·eps`），`identifiable ⟺ rank_eff == n_params ⟺ kappa < 1/rank_rtol`」<br>重叠范围：判据矩阵、阈值、等价式、地板式全部重合；差异只在**符号名**——本文与科学层写作 `n_free`，合同层与产品键写作 `n_params`（本文 `:107` 产品键列 `n_params`）。另 `10_sampling.md:61` 是第四处（引用+复述）。<br>**B. 与 `docs/science/PHASE2_UPM.md` 的模型式重叠**：本文 `:30-32`「`y_k(x) = s(x) + C_k(x) + ε_k(x)` / `C_k(x) = B_ref(x) + δ_k(x)` / `calibrated_k(x) = raw_k(x) − δ_k(x)`」对照 `docs/science/PHASE2_UPM.md:3`「现行归一语义：只施加 `δ_k`，保留公共天光面 `B_ref`；`raw − C_k` 全减属非默认路径」与 `:50` 同名式 —— 同值；本文 `:14` 已声明「本文件只登记插件侧口径，不复制公式」，但 `:30-32` 就是把公式复制了一遍（自述与内容不符）。<br>**C. 与 `docs/science/PHASE2_UPM.md` §17 的接缝门**：本文 `:39`「门 `max|rel_step| ≤ 1e-2`（**门槛的推导与实测标定正本 = `docs/science/PHASE2_UPM.md` §17**：…本节不复制其数值结论）」——同节却把 `1e-2` 这个数值写出，`ASTROCS_DESIGN.md:412` 也写「本节不复制数值」但同句给出 `1e-2`。数值与正本同值（`docs/science/PHASE2_UPM.md:296` 门 `max_e |rel_step(e)| ≤ 1e-2`，`:387` §17 推导）。 |
| 元信息块 | 无。机械列 `meta_block=False` 正确 |
| 写法违规 | ① `:168`「`## 9 SCI-C 实测结论（RELEASE-04 / SCI-403）`」＋ `:172-188` 实验过程数据（「4.80 e⁻ → **0.383 e⁻**（12.5×）」「产品中位 299.17 e⁻ ≈ `B_ref` 297.33 e⁻」「实测 2.799% 是拟合不足」「Pearson 0.896」「max|Δ| = 3.1e-15」「实测峰 k=26 vs 预期 21.33」）——标准01 §4；② `:7`「**本期决议：纯加性模型**」、`:13`「（SCI-UPM-001 冻结模型：**纯加性**…）」、`:35`「（本期决议）」、「本期 `g_k ≡ 1` 不启用」——「本期决议」＝决策过程语域，「决议」指向裁决记录而非现行条款（标准01 §4 无「负责人某日裁决/指令」）；③ `:90`「⚠ **`tol = 1e-6` 不是硬门**：判据 = 拟合正确；**做法参考 PMM**」——「PMM」是全仓无定义的裸缩写（`git grep -n "PMM" -- docs` 只命中本文与 `docs/science/PHASE2_UPM.md:127`，两处都无展开），读者无法追溯 ⇒ 标准01 §4「自然语言把设计讲清楚」不成立；④ `:134` 配置表 `smoothing_lambda` 一行内写「① **阻尼 `α≈0.5`**…② **本键**…③ **堆叠平滑项** = 拟合目标里的新项，**本期不加，做实验验证**」——把未采纳方案与待实验项写进现行配置表（标准01 §4 只写现行设计）；⑤ `:113`「**旧产品**没有该段 ⇒ 具名不可得」——「旧产品」属历史叙事（标准01 §4）；⑥ `:37`/`:39`/`:44` 用 ⚠ 与「依据与理论正本」混排，同一节里三次声明「正本在别处」却又三次就地给出判据式 ⇒ 记为自相矛盾的写法，不另计重复。机械列 `history_narrative=[]` 正确（本文无「旧版/作废」串），但 `task_ids=['RELEASE-04']` 只报了 §9 标题，**漏报了 `:134` 的 `CONFORM-FIX-B-009` 型编号面**（本文未含该串，编号在代码注释；本文对应缺陷是「本期决议」类决策语域，机械列词表不覆盖）。 |
| 悬空引用 | `:23`「参考：`eng/contracts/schemas/upm_output.schema.json`」——**不存在**（`ls eng/contracts/schemas/` 实测无此名；`git grep -l upm_output.schema` 只命中本文 + `docs/DOCUMENT_INDEX.yaml`）。`:91`「`tolerance_relative` 字段与其生产取值登记于 `PHASE2_UPM_IMPL.md` + `DATA_SEMANTICS` 字段表」——两个引用都是**裸简称**：`PHASE2_UPM_IMPL.md` 无目录前缀（实际 `docs/algorithms/PHASE2_UPM_IMPL.md`，存在）、`DATA_SEMANTICS` 未写成路径也未带 § 号（实际 `docs/contracts/DATA_SEMANTICS.md:1980/:2081` 有对应行）⇒ 不可直达，按标准01 §3「双向可追溯」判缺陷（非文件级悬空，是锚点级）。`:119` 指向 `lib/algorithms/coverage/include/astro/phase2/identifiability.h` 的 `p2_identifiability_assess`（头文件存在，符号存在，实测 `git grep -n p2_identifiability_assess`）。`:134` 的两个行锚**均已漂移**：`upm.h:75` 实测该行正是「`double smoothing_lambda;      // 图平滑权重（默认 0=关闭）`」⇒ 对；`stage2_common.h:24` 实测 `constexpr double P2_SMOOTHING_LAMBDA_AUTO = 0.1;` 在 **`:29`**，且该常量的语义权威注释（`:22-23`）指向 `docs/development/CONFIG_SCHEMA.md:19`——该行实测内容是 `output_mode` 段，`smoothing(auto→0.1)` 实际在 **`:33`** ⇒ 两处行锚各偏 5/14 行（机械列 `dangling_links=[]` 未捕获行锚漂移，**漏报**）。 |
| 与上位冲突 | **① 层级倒置（本批最高价值）**：§4.1/§4.4/§4.6/§4.7 承载科学公式、收敛判据与可辨识性判决，而 `docs/science/PHASE2_UPM.md:134` 明确把「口径正本」写给本文、`ASTROCS_DESIGN.md:411` 把「formulation 四要素与收敛判据」写给本文——标准01 §1「科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`」与这两处上层授权**直接打架**（同主题第二份正本 + 权威在下层）。**②「冻结」状态与登记表冲突**：`:129`「`rank_rtol` | 1e-10 | —— | **唯一**判据阈值 τ（相对量，**冻结值 `FZ-AP2S-RANK-RTOL`**）」，而 `eng/contracts/data/v6_clause_registry_v1.json:1590-1602` 该条 `"value": 1e-10, "status": "PENDING_OWNER_SIGNOFF", "owner_signoff": "SO-07"`，`docs/contracts/DATA_SEMANTICS.md:3388` 的 SO-07 行亦写「数值阈值…须 W3/W4 冻结并由负责人确认」⇒ 插件层把一个**待负责人签字**的阈值陈述为已冻结；`docs/contracts/DATA_SEMANTICS.md:2856` 同一键又写「取**产品里的实际值**…不是冻结字面量」。**③ `smoothing_lambda` 默认值跨层不一致，本文未登记分歧**：`:134` 取「键缺省 0.0 + `auto` 解析 0.1，三者并存不冲突」；对照 `docs/algorithms/PHASE2_UPM_IMPL.md:390`「`smoothing_lambda（λs） | 0.0（默认关闭平滑）`」、`docs/contracts/DATA_SEMANTICS.md:2081`「`smoothing_lambda | double | 0.0 | 图平滑权重（默认关，h:75；<0→0.0 :245）`」、`docs/development/CONFIG_SCHEMA.md:33`「`smoothing(auto→0.1)`」，且 `eng/ci/ledgers/config_default_divergences.json` 有 `"id": "watch:smoothing_lambda"` 登记、`eng/tests/cli/test_iso_acr_gpu_isolation.py:31` 记「acr_route 只允许 auto/cpu」的同一防复发面（`eng/ci/check_config_defaults.py:5` 注释点名「同一逻辑键在不同生产代码路径取不同缺省（如 smoothing_lambda）」）⇒ 该键的默认值在**四处文档 + 一个登记册**里并存，本文以「并存不冲突」一句了结，未标自己取哪侧、未指向登记册。**④ 与科学层的符号口径差**：本文判决式用 `n_free`（`:99`），产品键与合同层用 `n_params`（`:107`、`docs/contracts/DATA_SEMANTICS.md:2224`）⇒ 同一量的两名未统一（标准01 §4 自洽）。 |
| 处置建议 | **合并（口径上收）+ 就地订正**：① §4.7 的可辨识性判决与 §4.1/§4.4 的公式块**并入 `docs/science/PHASE2_UPM.md`（该处已有正文，只需删掉 `:134` 的「口径正本 = 11_upm.md」）**，本文改为一句话工序语义 + 指针（依据：标准01 §1 权威链、§2「同一角色内同一主题只有一份正本」）；② `:129` 改「登记值 `FZ-AP2S-RANK-RTOL` = 1e-10，**签字项 SO-07 未闭合**」，删除「冻结值」措辞（依据：`ASTROCS_DESIGN.md` §12.5「登记表不预写状态」+ 标准01 §4）；③ `:134` 的 `smoothing_lambda` 行删掉「③ 堆叠平滑项」与「并存不冲突」结论，改为指向 `eng/ci/ledgers/config_default_divergences.json` 的 `watch:smoothing_lambda`（依据：标准01 §4 只写现行设计）；④ 删 §9、删 `:23` 悬空 schema 或补 schema、订正 `:12` §4.4→§5.4 与两处行锚；⑤ `:90` 的「做法参考 PMM」展开为可追溯条款或删。**本项 ①③ 属跨层归并，触发前言 §5 判据 6「来源冲突」——需上呈负责人裁决（见 §5）。** |

### 1.3 `docs/plugins/algorithms_phase2/12_rejection.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase2/12_rejection.md` |
| 标题 | 插件文档：rejection（排异推断） |
| 行数 | 103 |
| 角色 | 模块工作细节；目录一致。**§9 承载排异算法路由表（档界 + 算法名 + 合法性窗口），该主题按标准01 §1 属科学/算法层** |
| 主题 | 污染状态估计与按逐像素几何可贡献帧数 N 的算法路由 |
| 上游 | `:3` 标 §5.2（存在）；`:12` 标「§5.5（逐像素排异：按几何覆盖帧数 N 自动选择算法）」（存在，`ASTROCS_DESIGN.md:417`，且该节标题与语义对得上）⇒ 不缺，但**同一份文档两处上游条款号不同**（抬头 §5.2、正文 §5.5），标准01 §3 要求开头标明上游条款，抬头标窄了 |
| 下游 | 11 个跟踪引用者（`git grep -l --fixed-strings 12_rejection.md`）：含 `ASTROCS_DESIGN.md:422/425`、`ACCEPTANCE_SPEC.md`、`docs/architecture/DATA_FLOW.md:37/45`、`docs/architecture/PIPELINE.md`、`docs/science/REJECTION.md`、`docs/modules/MODULE_MAP.yaml` |
| 是否正本 | **是——由最高设计指名**：`ASTROCS_DESIGN.md:422`「逐像素档位表（档界与算法名）的**唯一正本 = `docs/plugins/algorithms_phase2/12_rejection.md` §9**（本节不复制档界与取值；实现路由表须与该正本逐项一致）」。该指认本身即层级倒置（见「与上位冲突」） |
| 重复或重叠对象 | **同一档位表五处承载，本文是其中被指名的一份，另四处各自完整或片段承载**：<br>① 本文 `:64-68`「| **1 ≤ N ≤ 3** | **none（不排异，直接逆方差加权积分）** | / | **4 ≤ N ≤ 5** | percentile clipping | / | **N ≥ 6** | winsorized sigma clipping |」<br>② `docs/science/REJECTION.md:62-65`「生产默认 auto + profile = astrocs_adaptive_pixel …`1 ≤ n ≤ 3 → none`…`4 ≤ n ≤ 5 → percentile (low 0.2 / high 0.1, scale=\|median\|)`；`n ≥ 6 → winsorized_sigma (lower 4.0 / upper 3.0 / 8 iter)`」——**同档界，且多带全部方法参数**；`:100-103` 另有对照档表「`n < 6 → percentile` / `6 ≤ n ≤ 15 → winsorized_sigma` / `n > 15 → linear_fit`」<br>③ `docs/architecture/DATA_FLOW.md:36-37`「rejection（**逐像素按几何可贡献帧数 N 自动选择**：`1≤N≤3` none / `4≤N≤5` percentile / `N≥6` winsorized（M3：原 `N≥16` linear fit 档改投）」——架构层第三次复述档界（同批，见 1.16）<br>④ `docs/modules/phase2_rej.md:186-190`「## 排异档位（权威 = `ASTROCS_DESIGN.md` §5.5 / `docs/science/REJECTION.md`）… `1≤N≤3` none（不排异）/ `4≤N≤5` percentile / `N≥6` winsorized sigma clipping」（别批路径，同角色 `docs/modules/`）<br>⑤ `ASTROCS_DESIGN.md:423` 虽称「不复制档界」，仍复述了算法集「none / percentile / winsorized / linear fit」。差异：只有 ② 带参数与逐 profile 差异；① 与 ③④ 无参数，③ 多一句历史。（注：② 与 ④ 的文件不在本批清单，只作重叠证据） |
| 元信息块 | 无 |
| 写法违规 | ① `:70-77` 整段是变更叙事＋过程证据：「> `N ≥ 16` 档取 winsorized sigma clipping，即生产档 `astrocs_adaptive_pixel` 的档位表**由四档收成三档**（…）。**依据（生产 kernel 受控评估，`run/REJECT-DOCFIX-01/REPORT.md`）**：linear fit 在 `N ≥ 16` 档的等效上阈实测仅 **≈2.1–2.4·σ_robust**…导致**干净像素过拒 12.200% → 0.067%**、显著点漏检 **3.92% → 1.44%**、`N≥16` 可测残余 >2.5σ 的像素 **27/1175 → 0/1175**；该档占全图 **13.14%** 像素。对照档 `wbpp_2_9_1` / `astrocs_adaptive` **不随此改动**」——「由四档收成三档」「不随此改动」是历史叙事与变更留痕，`12.200% → 0.067%` 是实验过程数据（标准01 §4 三条同时命中）；② `:79-81`「生产排异算法集 = none / percentile / winsorized / linear fit；**min/max 极值法不用于生产**（WBPP 2.5.9 一手源码明文拒绝…）」与 `:76`「对照档仍为 `N>15 → linear fit`」并用，未区分「生产 AUTO 集」与「合法显式方法集」两个轴 ⇒ 自相矛盾的枚举（linear fit 既在生产集里、又不被 AUTO 产出）；③ `:91` 合法性窗口表出现 `RCR`、`averaged sigma`、`ESD` 三个本文 §4 从未列为可路由方法的算法（`:23` 的分类型清单里没有 RCR/ESD/averaged sigma），无来源标注。机械列 `task_ids=[]`、`history_narrative=[]` **漏报本批最重的一条写法违规**（其词表不含「由…收成…」「不随此改动」型表述，也不识别 `run/**` 型过程证据）。 |
| 悬空引用 | `:11`「`docs/interfaces/io/CON-001_PIPELINE_MODEL.md`」与 `:73-74`「`docs/interfaces/io/CON-001_PIPELINE_MODEL.md` / `CON-002_EXECUTION_OPTIONS.md`」——**三者均不存在**：`ls docs/interfaces/io/` 实测只有 `IO_001_FITS_STREAM_INTERFACE.md`、`IO_002_HIPS_INPUT_INTERFACE.md`、`IO_003_ATOMIC_OUTPUT_PUBLISH.md`（`CON-00x_*` 命名的整族已被改名为 `IO_00x_*`）⇒ 指向**已被改名对象**，且改名后无一对新名（`git grep -l "CON-001_PIPELINE_MODEL\|CON-002_EXECUTION_OPTIONS"` 只命中 docs 侧旧名）。`:72`「`run/REJECT-DOCFIX-01/REPORT.md`」——**不存在且不可再生**：`ls -d run/REJECT-DOCFIX-01` → No such file or directory，`git ls-files \| grep reject-docfix` 0 命中，`find artifacts -iname "*REJECT*"` 0 命中（该路径同时在 `docs/science/REJECTION.md:84` 被当作「这条偏离的依据是实测，不是偏好」的**决定性出处**）⇒ 判据依据的证据未落 `artifacts/evidence/`，违反标准01 §2 留证条款。`:19`「`eng/contracts/schemas/rejection_output.schema.json`」——**不存在**（`ls eng/contracts/schemas/` 无此名；实际同族对象是 `eng/contracts/schemas/unified/rejection.schema.json`）。机械列只报了 `rejection_output.schema.json` 一条，**漏报 `CON-001/CON-002` 与 `run/…REPORT.md` 三条**。 |
| 与上位冲突 | **① 层级倒置 + 唯一正本被四处破坏**：`ASTROCS_DESIGN.md:422` 指名本文为唯一正本，但 `docs/science/REJECTION.md:62-103` 以科学正本身份承载**同一档界表 + 方法参数 + 逐 profile 差异**，`docs/architecture/DATA_FLOW.md:36-37`、`docs/modules/phase2_rej.md:186-190` 再各抄一份。标准01 §1「科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`」与 §2「同一角色内同一主题只有一份正本」不能同时被满足 ⇒ 顶层指认与标准打架。<br>**② 本文表未按 profile 限定，与科学层记载的实测行为冲突**：本文 `:64-68` 的三档表**不区分 profile**；`docs/science/REJECTION.md:51` 实测记「`plan.method` 对 `N ≤ 3` 的取值**随 profile 而定**：生产档 `astrocs_adaptive_pixel` 解析为 `none`，`wbpp_2_9_1`/`wbpp_current`/`astrocs_adaptive` 解析为 **`percentile`**；故「N≤3 ⇒ 不排异」在生产档由路由保证、在对照档由内核闸保证」。本文只在 `:76` 提了 `N>15` 一档的对照差异 ⇒ 按本文表读对照档会得出与实现相反的 `N≤3` 结论。<br>**③ `:13` 上游条款与 `:3` 抬头不一致**（见「上游」）。<br>**④ 无取值冲突**：三档档界、`1≤N≤3 none`、min/max 不入生产，与科学层和 `ASTROCS_DESIGN.md:421/423` 同值——本批的重复是**载体重复**，不是数值分歧（如实记）。 |
| 处置建议 | **保留为正本 + 强制归一**（路径不变）。① 与 `docs/science/REJECTION.md` 二选一：按标准01 §1，档界与算法名的正本应上收科学层，本文只留工序（配置、provenance、fail-closed、Oracle）；若保留现状的顶层指认，则须由负责人改 标准01 §1 或改 `ASTROCS_DESIGN.md:422` —— **二者互斥，上呈**（前言 §5 判据 6）；② 无论谁作正本，`DATA_FLOW.md:36-37` 与 `docs/modules/phase2_rej.md:186-190` 两处复述**删除改为引用**（依据：标准01 §4「只引用」+ §2 一份正本）；③ 删 `:70-77` 的变更叙事与过程数据，把结论以正向条款写入表（「生产档 `astrocs_adaptive_pixel`：…；对照档 `wbpp_2_9_1`：…」），证据落 `artifacts/evidence/` 并由正本引用（标准01 §2/§4）；④ 表头补 `profile` 适用域列，与科学层 `:51` 对齐；⑤ 三条悬空引用（`CON-001/CON-002` → 现名 `IO_00*`/删除、`rejection_output.schema.json` → `unified/rejection.schema.json`、`run/REJECT-DOCFIX-01/REPORT.md` → `artifacts/evidence/`）逐条订正 |

### 1.4 `docs/plugins/algorithms_phase2/13_integration.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase2/13_integration.md` |
| 标题 | 插件文档：integration（科学目标集成） |
| 行数 | 104 |
| 角色 | 模块工作细节；目录一致 |
| 主题 | 按科学目标把归一化+排异帧集成为马赛克：扩展源 GLS、点源 Q/W、`psfsw_integration` 诊断 |
| 上游 | `:3` 标 §5.2（存在）；`:12` 标「§5.3（SNR 重建与逆方差叠加）、§5.6（硬约束：Phase2 信号为面亮度量纲）」——**两条均对得上**（`:385` §5.3、`:428` §5.6）⇒ 本批 plugin 层上游标注最准的一份 |
| 下游 | 8 个跟踪引用者（`git grep -l --fixed-strings 13_integration.md`）：`docs/contracts/CONFIG_CONTRACT.md`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`eng/contracts/schemas/phase_config_mosaic.schema.json`、`eng/packaging/config/config_registry.json`、`eng/tests/contracts/test_unified_object_contract.py`、`lib/algorithms/integration/v6/include/astrocs/v6/weight_chain.h`。**最高设计未指名本文**（`ASTROCS_DESIGN.md` 对 phase2 只给目录级指针 `:430`）⇒ 双向索引只半通 |
| 是否正本 | 集成工序正本（该模块在插件层唯一一份） |
| 重复或重叠对象 | **A. GLS 与点源估计量公式三处承载（同值）**<br>本文 `:50`「`s_hat = (AᵀC⁻¹A)⁻¹AᵀC⁻¹d`」、`:58-59`「`Q_k = a_kP_kᵀC_k⁻¹d_k,    W_k = a_k²P_kᵀC_k⁻¹P_k` / 独立帧: `Q=ΣQ_k, W=ΣW_k, F_hat=Q/W, Var(F_hat)=1/W`」<br>对照 `docs/science/PSF_SIGNAL_WEIGHT.md:22`「`Q_k = a_k P_kᵀ C_k⁻¹ d_k`」、`:93`「扩展源 GLS（`x̂=(AᵀC⁻¹A)⁻¹AᵀC⁻¹d`，§5 与 `docs/science/INTEGRATION.md`）」、`docs/design/PHASE2_DETAILED_DESIGN.md:83/89`「`Q_k = a_k P_kᵀ C_k⁻¹ d_k,    W_k = a_k² P_kᵀ C_k⁻¹ P_k`」——**取值完全一致**，即本文复制了科学层已有推导，未给任何工序侧新增内容。<br>**B. 与 `07_noise_snr.md`（别批）重叠**：`:20-24` 的三条 SNR 重建口径与 `ASTROCS_DESIGN.md:398-404` 逐条同内容（`:20` 甚至复述了设计 `:399` 的「三者都产出同一物理量 `SNR = F_ref/σ_F` 的稠密表示」整句）；`07_noise_snr.md` §4.2/§4.5 是重建算子的正本（本文 `:40` 已正确指过去）。 |
| 元信息块 | 无 |
| 写法违规 | ① `:37`「越界 token 一律 fail-closed（`FZ-WEIGHT-SINGLE-PATH` / `FZ-MODE-RETIRED` / `FZ-FIELD-WEIGHTMODE`）」——三个冻结 ID 在本文是**无锚裸列**（正本 `eng/contracts/data/v6_clause_registry_v1.json`，本文未给路径）；② `:99-103`「### 权重来源受限表的锁定状态」一节整体是**实现现状叙述**：「`coverage.cpp` 的 `kForbiddenWeightSourceTokens`（含 `psfsw_robust_weight`、`psfsw` 等 token）与其同文件内的权重来源检查门、以及 `rejection.cpp` 的同源词表，是…**在役判据的执行面**——**不是死代码**，删除路径 = 变更流程」＋「**解锁条件 = §31.8 受限来源表本身被变更流程废止**；顺序不可颠倒」——「不是死代码」是对既有质疑的答辩语域、`废止`/`旧` 类历史词根（标准01 §4 禁「作废/废止」）；③ `:8`「两者的等价性宣称**以专项证明为前提**；Fisher 最优的宣称同以专项证明为前提」与 `:66` 重复同一句——同一约束写两次；④ `:24` 内嵌「**HST 类高对比域的结论与重建算子绑定**（双线性/默认算子下帧级更优，`..._mesh_median_v1` 下稀疏更优）」——把实验结论写进工序文档（数据在 `实验/absolute-snr/`，标准01 §4）。机械列 `task_ids=[]` 正确；`history_narrative=[102]` **误报**：`:102` 的「废止」出现在「解锁条件 = §31.8 受限来源表本身被变更流程废止」这一**正向流程条款**里，不是历史叙事（本条我在校准工具：`废止` 需带宾语时态才成立）。 |
| 悬空引用 | `:12`「`docs/interfaces/io/CON-003_FITS_IO.md`」——**不存在**（`ls docs/interfaces/io/` 实测只有 `IO_001/002/003_*` 三份；`git grep -l CON-003_FITS_IO` 只命中本文与 `docs/DOCUMENT_INDEX.yaml`）。`:31`「参考：`eng/contracts/schemas/mosaic_product.schema.json`」——**不存在**（`ls eng/contracts/schemas/` 实测无此名）。`:13`「`docs/design/UNIFIED_MODEL.md`（frame_snr、sparse_snr_layer）」、`:14` `docs/science/INTEGRATION.md`、`docs/science/PSF_SIGNAL_WEIGHT.md`、`:15` `docs/design/PHASE2_DETAILED_DESIGN.md §6`——均存在。机械列只报了 `mosaic_product.schema.json`，**漏报 `CON-003_FITS_IO.md`**。 |
| 与上位冲突 | **无取值/无口径冲突**（SNR→逆方差换算 `w = SNR²/F_ref²`、三条重建口径、psfsw 只作诊断，与 `ASTROCS_DESIGN.md:399/400/401` 与 `docs/science/PSF_SIGNAL_WEIGHT.md` 同值）。**一处结构性问题**：`:20` 的口径选择与 `:24` 的适用域结论实际把设计 §5.3 整节内容重述了一遍——按标准01 §1 最高设计只承载顶层决策、细节下沉，这是**正常下钻**；但 `:37` 的「没有可选的权重口径、口径选择键、口径枚举或口径配置项」与设计 `:398`「三种 SNR 重建口径由 JSON 显式指定」在**字面**上互相打脸（真实语义是「权重口径单一、重建口径三选」，两句用同一个词「口径」指两件事）。这不是取值冲突而是**同词两义**，会在实现/审计里被读成矛盾 ⇒ 记为缺陷（标准01 §4 自洽：文档须自洽）。 |
| 处置建议 | **保留 + 就地订正**（路径不变）：① `:12` 悬空 `CON-003_FITS_IO.md` → 改指 `docs/interfaces/io/IO_001_FITS_STREAM_INTERFACE.md`（或该引用原意所在的新对象，需一并核 `17_aio.md`）；② `:31` 的 `mosaic_product.schema.json` 改指真实合同面（`eng/contracts/schemas/unified/` 产品族 + `eng/contracts/data/phase_product_exchange.schema.json`）或登记为待补；③ 删 `:50/:58-59` 公式块改为一句话 + 指针到 `docs/science/PSF_SIGNAL_WEIGHT.md` §2/§5（依据：标准01 §1 科学公式权威、§4 公式不在模块工作细节层）；④ `:37` 与 `:20` 的「口径」用词分层：把「权重口径单一」与「重建口径三选一」写成两个具名条款（依据：标准01 §4 自洽）；⑤ `:99-103` 一节移出到 `docs/ci/` 的门登记或 `docs/algorithms/` 的现状节（依据：标准01 §2 角色表，「标准与检查」不属模块工作细节）；⑥ 三个 FZ- ID 补路径 |

<!-- PROGRESS: 已审 4/25 -->

### 1.5 `docs/plugins/algorithms_phase3/14_projection.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase3/14_projection.md` |
| 标题 | 插件文档：projection（投影 registry） |
| 行数 | 59 |
| 角色 | 模块工作细节；目录一致 |
| 主题 | export 的 WCS 投影 registry：八投影冻结集、registry v3、往返 Oracle |
| 上游 | `:3` 标 §6.2（export 流程，存在 `ASTROCS_DESIGN.md:440`）⇒ 不缺。`:12` 与 `:25` 标「§5.3（投影算法：内置多种）」——**条款号错**：§5.3 是「SNR 重建与逆方差叠加」（`:385`），投影算法是 **§6.3**（`:451`） |
| 下游 | 11 个跟踪引用者（`git grep -l --fixed-strings 14_projection.md`）：`docs/algorithms/PHASE3_PROJ_IMPL.md`、`docs/contracts/CONFIG_CONTRACT.md`、`eng/contracts/schemas/phase_config_export.schema.json`、`eng/contracts/schemas/projection_registry.schema.json`、`eng/ci/ledgers/dead_config_keys.json`、`lib/algorithms/projection/README.md`、`lib/infrastructure/cli/session_commands.h` 等。最高设计未指名本文（§6.4 `:460` 只给目录指针） |
| 是否正本 | 投影工序正本；registry 数值/字段面正本已明确让给 `docs/algorithms/PHASE3_PROJ_IMPL.md`（`:21`）⇒ 分层合法（本文不复制公式，只登记工序） |
| 重复或重叠对象 | `:25`「首批冻结 **TAN / SIN / CAR / AIT / STG / MOL / CEA / ZEA**」与 `ASTROCS_DESIGN.md:453` 同一列举、与 `docs/algorithms/PHASE3_PROJ_IMPL.md:399`（§15 标题「版本化 projection registry v3 与 DESIGN §5.3 八投影冻结集合」）第三处。三处取值一致；差异：只有本文 `:27` 给实现进度、只有 PHASE3_PROJ_IMPL 给逐投影六要素表 ⇒ 属分层下钻，不算第二正本，但**八投影列举被抄三份**（标准01 §4「只引用」） |
| 元信息块 | 无 |
| 写法违规 | ① `:27`「**实现状态**：已实现 TAN/SIN/CAR/AIT（4/8）；STG/MOL/CEA/ZEA 待实现；注册面与会话面分离」——进度状态写进现行设计文档（标准01 §4＋`ASTROCS_DESIGN.md` §12.5「状态由检查与验收现场计算，登记表不预写状态」）；② `:57`「八投影全覆盖测试（已实现四投影**先落**，未实现四投影**补齐时同步补**）」——排期语域；③ `:8`「新增投影必须注册并附独立 Oracle」、`:30`「新增投影经 registry 注册并附独立往返 Oracle」、`:59`「新增投影必须带独立 Oracle 才可注册」——同一约束一页三遍（标准01 §4 自洽）。机械列 `dangling_links=[]`、`task_ids=[]` 正确 |
| 悬空引用 | `:26`「`registry_find` 未命中返回 nullptr = fail-closed」——**全仓无此符号**：`git grep -n "registry_find" -- lib eng docs` 只命中本文与 `docs/algorithms/PHASE3_PROJ_IMPL.md`；代码侧真实导出是 `registry_frozen_set()`（`lib/algorithms/projection/p3_proj_v6.cpp:386`）。`:21`「`lib/algorithms/projection/`（`Spec` 六要素字段与 `registry_frozen_set()` 导出…）」——`registry_frozen_set()` 存在 ✓，但 `Spec` 这个类型名在 `lib/algorithms/projection/` 内 0 命中 ⇒ **指向已被改名对象**。`:20` 的 `projection_registry.schema.json` 存在 ✓（本批唯一直接落到真实 schema 的引用）。机械列只判文件级，**漏报这两处符号级悬空** |
| 与上位冲突 | **与最高设计 §6.3 的可用面冲突**：`ASTROCS_DESIGN.md:453`「注册表中未实现的投影在被选择时显式报"不支持"（**当前仅 TAN 可用**）」；本文 `:27` 写「已实现 TAN/SIN/CAR/AIT（4/8）」、`:36` 把 `projection` 取值列为 `tan/sin/car/ait/stg/mol/cea/zea`、`:49` 只写「未注册投影 → 拒绝」——**缺"注册但未可用"这一档**，读起来像四个可选。算法正本 `docs/algorithms/PHASE3_PROJ_IMPL.md:26`「生产 alpha 路径仍只走 TAN」、`:120`「projection 仅接受 "TAN"…SIN/CAR/AIT 即便已在 registry」、`:283`「P3_WCS_UNSUPPORTED = 2 无产生点」⇒ 本文比上位与算法正本都宽松，会误导按 4 投影可用做验收。另 `:12`/`:25` 的 §5.3 错号与 `PHASE3_PROJ_IMPL.md:399` 的「DESIGN §5.3」同源 ⇒ 最高设计章节重排后未随迁的系统性错号（见 §3） |
| 处置建议 | **保留 + 就地订正**：① `:12`/`:25` §5.3→§6.3；② `:27` 拆两个轴各自具名（`registry 注册面 = 8` / `生产可选面 = 仅 TAN`，锚 `PHASE3_PROJ_IMPL.md:120/:283`），删进度语气（依据：标准01 §4 + 设计 §12.5）；③ `:36` 取值列改「合同取值 / 当前可选取值」两列；④ `:26` `registry_find`、`:21` `Spec` 改代码实名；⑤ 三处「新增投影须附 Oracle」并为一条正向条款 |

### 1.6 `docs/plugins/algorithms_phase3/15_resample.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase3/15_resample.md` |
| 标题 | 插件文档：resample（反向映射与重采样） |
| 行数 | 58 |
| 角色 | 模块工作细节；目录一致 |
| 主题 | 输出像素 WCS inverse 定位 HEALPix、采样核语义、方差/相关传播 |
| 上游 | `:3` 标 §6.2；`:12` 标「§6.2（流程：反向映射 + 重采样）与 §6.3（投影算法：输入语义守卫与输出模式）」——**条款号与标题全对**（`:440`/`:451`）⇒ 本批 plugin 层唯一一处上游完全正确的权威依据块 |
| 下游 | 5 个跟踪引用者：`docs/KNOWN_LIMITATIONS.md`（第 39 条把它当断链证据引用）、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`docs/DOCUMENT_INDEX.yaml`、`eng/packaging/config/config_registry.json`。代码与算法层零引用 ⇒ 引用面最薄的一份 |
| 是否正本 | 重采样工序正本（插件层唯一） |
| 重复或重叠对象 | **同主题在算法层有两份**：`:37` 把 `sampler` 的权威名与取值推给 `docs/algorithms/PHASE3_RESAMPLE.md`，而算法层同时存在 `docs/algorithms/PHASE3_RESAMPLE.md` 与 `docs/algorithms/PHASE3_RSMP_IMPL.md` 两份 resample 文档（后者另有 `docs/modules/phase3_rsmp.md` 对位）⇒ 本批的下游指向了一个有同主题孪生的目标（组详情见 §2 主题组 G-6）。`:29`「线性采样 `y=Rx`：`C_y = RC_xRᵀ`」与 `docs/science/UNCERTAINTY_AND_COVARIANCE.md`、`docs/algorithms/PHASE3_RESAMPLE.md:43` 同值。`:48` 的 NaN/样本级掩膜/强制计数与 `ASTROCS_DESIGN.md:426`（§5.5）、`:694`（§10）逐句同内容，本文已带两处正确指针 ⇒ 合法下钻 + 一次复述 |
| 元信息块 | 无 |
| 写法违规 | ① `:31`「当前"四象限最近中心双线性"**只能在 Oracle 与误差/边界定义后作为注册核，不能因现码存在就成永恒目标**」——对现状代码的评述＋将来时目标（标准01 §4「只写现行设计要怎样」）；② `:24`「科学模式要求球面几何一致，**距离一律取球面量**」、`:30`「coverage 与 variance 各自独立、互不代用」与 `docs/algorithms/PHASE3_RESAMPLE.md` 对应条款近同且无指针 ⇒ 无出处的第二口径 |
| 悬空引用 | `:20`「参考：`eng/contracts/schemas/export_product.schema.json`」——**不存在**（`ls eng/contracts/schemas/` 实测 20 份 schema 无此名）；**已登记**：`docs/KNOWN_LIMITATIONS.md` 第 39 条点名「`docs/plugins/algorithms_phase3/15_resample.md §3` 引 `eng/contracts/schemas/export_product.schema.json`…不存在（实测）」。`:37` 的 `docs/algorithms/PHASE3_RESAMPLE.md` 存在 ✓。机械列已捕获该条 |
| 与上位冲突 | ① 配置面：`:37` `sampler` 在 `phase_config_export.schema.json` 命中 ✓（`grep -c` 实测 4 处），但 `:38` `correlation_output`、`:39` `order_limits` 在三个 phase_config schema 里 **0 命中** ⇒ 登记了合同面不存在的键；② 无取值冲突（NaN/掩膜/方差传播与设计 §5.5/§10 同值） |
| 处置建议 | **保留 + 就地订正**：① `:20` 与同族 9 处断链（`10_sampling:24`、`11_upm:23`、`12_rejection:19`、`13_integration:31`、`16_fits_output:29`、`18_cli:20`×2、`22_gaia:20`、`23_browser:19`）一次处置，按 `docs/KNOWN_LIMITATIONS.md` 第 39 条的「补 schema 与改引用二选一」；② `:38/:39` 标为「实现内部量，非配置键」或补合同面（依据：标准01 §3 双向可追溯）；③ `:31` 改正向条款；④ `:24/:30` 补指针 |

### 1.7 `docs/plugins/algorithms_phase3/16_fits_output.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/algorithms_phase3/16_fits_output.md` |
| 标题 | 插件文档：fits_output（流式 FITS 输出） |
| 行数 | 65 |
| 角色 | 模块工作细节；目录一致 |
| 主题 | 流式写出裸 FITS：HDU 结构、WCS/BUNIT、原子提交与独立验证 |
| 上游 | `:3` 标 §6.2（对）。**`:12` 两条全错**：「§5.1-§5.4（输出不带权重、投影到平面、WCS 直接计算生成）」——§5.1–§5.4 是 mosaic 的使命/流程/SNR/UPM，被引的三件事在 §6.1（`:436`）与 §6.3（`:456`）；「§9（原子提交）」——§9 是「CPU 后端与资源」，原子提交在 **§10**（`:688`） |
| 下游 | 8 个跟踪引用者：`docs/KNOWN_LIMITATIONS.md`、`docs/contracts/CONFIG_CONTRACT.md`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`eng/tools/check_aio_ownership.py`、`eng/contracts/schemas/phase_config_export.schema.json`、`eng/packaging/config/config_registry.json`、`docs/DOCUMENT_INDEX.yaml` |
| 是否正本 | FITS 输出工序正本 |
| 重复或重叠对象 | `:25-26` BUNIT 口径（canonical `ADU/sr`；主 HDU = 输入 HiPS `signal/properties#BUNIT`；`VARIANCE`/`IVAR` = 平方/倒数，`FZ-P3-BUNIT-QUADRATIC`）对照 `docs/contracts/DATA_SEMANTICS.md:30`「`signal`…单位 = **`ADU/sr`**…推导见 §31.1a」与 `:375`「写盘 BUNIT 一律取 §31.1 冻结的 canonical 面亮度族串…守卫 = `hiss_writer.cpp` 的 BUNIT 白名单 fail-closed，只接受 `{ADU/sr, ADU^2/sr^2, sr^2/ADU^2}`」⇒ **同值**（平方/倒数正落在该白名单内），且本文 `:26` 已把「单位口径唯一权威」正确指给 `DATA_SEMANTICS.md` §31.1a ⇒ 合法下钻 + 一次复述，不计第二正本 |
| 元信息块 | 无 |
| 写法违规 | ① `:26`「**BUNIT 语义（已闭合）**」——处置台账语域（标准01 §4；状态现场计算见设计 §12.5）；② `:27`「provenance：源 product/hash、**软件完整 SHA**、配置…」——要求字段但未给字段名与合同锚（唯一词表 = `eng/contracts/schemas/hips_storage_form.schema.json#x-astrocs-field-vocabulary`，设计 `:690` 指名）；③ `:34`「内存 `O(width×band_height + tile_cache)`」是复杂度式，按标准01 §2 属算法推导层，未给指针。机械列 `dates=[]`/`task_ids=[]`/`history_narrative=[]` 正确 |
| 悬空引用 | `:29`「参考：`eng/contracts/schemas/fits_product.schema.json`」——**不存在**且**已登记**（`docs/KNOWN_LIMITATIONS.md` 第 39 条点名本行）。`:12` 的 §9 与 §5.1-§5.4 属条款级悬空（条款存在但内容与所引无关）。机械列只报 schema 一条，**漏报两条条款级悬空** |
| 与上位冲突 | ① `:18`「裸/归档同义」vs `ASTROCS_DESIGN.md:689`「两形态**互斥**且**同身份**（产品哈希取解压后内容，不取压缩包字节）」——「同义」＝可互换、「互斥」＝一次只有一种，措辞把「同身份」滑成「同义」⇒ 口径漂移；② `:12` 的两处条款号错指（与 `IO_AND_ATOMICITY.md` 的 §9 错号同族）；③ 配置面：`:45` `mode` 在合同面命中 ✓（取值与设计 §6.3 `:456` 同值），`:42` `band_height`、`:43` `tile_cache_mb`、`:44` `compression` 在三个 phase_config schema 里 0 命中；④ `:19`「Phase3 不产出 HiPS、不使用 `.hips`/`.hips.zst`」与设计 `:689`「Phase3 平面 FITS 不套壳」一致 ✓ |
| 处置建议 | **保留 + 就地订正**：① `:12` 条款号改 §6.1/§6.3 与 §10；② `:18`「同义」→「互斥且同身份」并指针 §10；③ `:26`「（已闭合）」删；④ `:29` 入同族断链处置；⑤ `:42-44` 三键标注或补合同面；⑥ `:34` 加指针到 `docs/algorithms/PHASE3_FITS_IMPL.md` |

### 1.8 `docs/plugins/infrastructure/17_aio.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/infrastructure/17_aio.md` |
| 标题 | 插件文档：aio（I/O 与原子提交） |
| 行数 | 59 |
| 角色 | 模块工作细节；目录一致 |
| 主题 | 唯一 I/O 边界：读/校验/原子提交/缓存 + HiPS 落盘形态解析 |
| 上游 | `:3` 标 §10（I/O 与原子产品）——**存在且正确**（`ASTROCS_DESIGN.md:684`）。`:13` 另标「§8.1（顶层结构：aio 是文件级唯一 I/O 边界）」——**双重错**：§8.1 标题是「总原则：唯一 CLI 入口、阶段独立调度器」；「aio 是文件级唯一 I/O 边界」实际在 §10 `:686` |
| 下游 | 8 个跟踪引用者：`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`、`docs/design/PRODUCT_STORAGE_FORM.md`、`eng/tools/check_aio_ownership.py`、`lib/infrastructure/aio/src/aio_atomic_file.h`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`eng/packaging/config/config_registry.json`、`docs/DOCUMENT_INDEX.yaml` |
| 是否正本 | aio 工序正本；**打洞机制的常数面唯一载体在本文 `:27`**（上层只写行为），而机制本身另有两处 ⇒ 常数无出处、机制三处 |
| 重复或重叠对象 | **裸形态体积削减：本文 vs 最高设计 §10**<br>本文 `:27`「**只对 4 KiB 对齐的字面全零块**调 `fallocate(FALLOC_FL_PUNCH_HOLE\|FALLOC_FL_KEEP_SIZE)`（Windows：`FSCTL_SET_SPARSE`+`FSCTL_SET_ZERO_DATA`，**64 KiB 对齐**），打洞后丢页缓存**读回复算**整文件 `sha256`；不一致 ⇒ 硬错误、不发布；卷不支持 ⇒ `trim=skipped(reason)` 的 warn 后照常发布…归档容器不做打洞（`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md` §7）」<br>`ASTROCS_DESIGN.md:691`「① **文件系统打洞**：只释放本来就是零字节的区域…（Linux `fallocate(PUNCH_HOLE)`；Windows `FSCTL_SET_SPARSE`+`FSCTL_SET_ZERO_DATA`），失败只跳过并记 `trim=skipped(reason)`、**不 fail-closed**…**归档形态两种都不实施**」<br>重叠：机制、`trim=skipped` 语义、归档不实施全部重合。差异：**本文多出 4 KiB / 64 KiB 两个对齐常数与「读回 sha256 不一致 ⇒ 硬错误」**，最高设计只写行为不写常数。第三处 `docs/design/PRODUCT_STORAGE_FORM.md`（§9 实测收益）。⇒ 常数在标准01 §1 要求「有推导与出处」，本文未给（`git grep -n "4 KiB" -- docs/science docs/algorithms` 0 命中） |
| 元信息块 | 无 |
| 写法违规 | ① `:20`「参考：`eng/contracts/schemas/*.schema.json`（全部数据产品）」——通配代替可核清单，标准01 §3 要求每份可追溯，通配不可追溯；② `:25` 列举字段 `storage_form`/`index_path`/`index_sha256`/`archive_sha256`/`archive_bytes`/`tree_hash`——设计 `:690` 明文「**本节不复制字段清单**，唯一词表 = `eng/contracts/schemas/hips_storage_form.schema.json#x-astrocs-field-vocabulary`」⇒ 本文复制了顶层点名不复制的清单；③ `:9`「调用方（Phase2/Phase3）**不感知形态**」与 `:18/:25` 又要求调用侧读 `storage_form`/`coverage_index` 键（设计 `:690` 也是这个口径）⇒ 同一页两说（记与上位冲突③）。机械列 `dangling_links=['eng/contracts/schemas/*.schema.json']` 的判定不成立（该目录确有文件），但掩盖了「引用不可追溯」这个真实缺陷类型 |
| 悬空引用 | `:20` 通配串（`ls eng/contracts/schemas/` 实测 20 个真实 schema，通配不解析为任何具体文件）。`:27` 的 `aio_sparse_punch.h` 存在 ✓ 但在 `src/` 不在 `include/`（私有头），本文未说明 ⇒ 可直达性缺陷。`:25` 的 `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md` 存在 ✓。无文件级硬悬空 |
| 与上位冲突 | ① `:13` 条款号错指（§8.1 非顶层结构；该句内容在 §10）；② `:25` 复制设计 `:690` 声明「不复制」的字段清单 ⇒ 与禁令对冲，形成字段清单第二载体；③ `:9`「不感知形态」与设计 `:690`「Phase2 输入沿用既有字段结构…额外的总索引引用用加性可选键 `coverage_index`」在字面相反（消费侧其实要感知索引）；④ 无取值冲突：`storage_form` 默认 `archive`、缺失/空 ⇒ 默认＋warn 与 `:690` 逐字一致；`trim=skipped(reason)` 与 `:691` 一致 |
| 处置建议 | **保留 + 就地订正**：① `:13` 的 §8.1 改 §8.4（顶层结构真身），"唯一 I/O 边界"只留 §10；② `:20` 通配改为列真实 schema 名或指针到 `docs/contracts/INDEX.yaml`；③ `:25` 字段清单删除、只留指针到 `hips_storage_form.schema.json#x-astrocs-field-vocabulary`（依据：标准01 §1「同一主题只有一份正本」＋设计 `:690`）；④ `:27` 两个对齐常数补推导/出处，或按标准01 §1 登记为待裁决常数；⑤ `:9` 与 `:18/:25` 的"感知/不感知"用统一措辞（像素读不感知、索引与形态事实要感知）；⑥ 与 `docs/architecture/CACHE_POLICY.md` 的缓存四要素归一（见 1.21） |

<!-- PROGRESS: 已审 8/25 -->

### 1.9 `docs/plugins/infrastructure/18_cli.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/infrastructure/18_cli.md` |
| 标题 | 插件文档：cli（命令行入口） |
| 行数 | 67 |
| 角色 | 模块工作细节；目录一致 |
| 主题 | `normalize`/`mosaic`/`export` 薄入口：解析、预检页面、模板、机器输出、取消、退出码 |
| 上游 | `:3` 标 §7.1/§7.2/§7.3，`:11` 标 §7/§4.5/§7.3 —— **六处条款号全部存在且标题对得上**（`:464`/`:466`/`:493`/`:515`/`:332`）⇒ 与 `15_resample.md` 并列本批上游标注最准的两份 |
| 下游 | 10 个跟踪引用者：`docs/contracts/CONFIG_CONTRACT.md`、`docs/design/LOG_AND_ERROR_SYSTEM.md`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`lib/infrastructure/cli/module.yaml`、`eng/contracts/schemas/phase_config_{normalize,mosaic,export}.schema.json`、`eng/packaging/config/config_registry.json`、`docs/DOCUMENT_INDEX.yaml` |
| 是否正本 | CLI 工序正本。**但退出码面它不是正本**：`:49` 自己写明「退出码唯一源 `lib/infrastructure/cli/exit_codes.h`…域→码映射唯一源 `docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5」⇒ 正确的引用式分层 |
| 重复或重叠对象 | ① `:24` 命令树列举「`help / --version / doctor / benchmark / normalize\|mosaic\|export --json\|--template\|--help`」＝`ASTROCS_DESIGN.md:469-485` 的第三次抄写（第一次是设计本体，第二次是 `docs/architecture/ARCHITECTURE.md:9-11` 的入口段）——取值一致；② `:26` 预检三档 + `yes`/`-y`/`-force` 全套语义＝`ASTROCS_DESIGN.md:353-356` 的整段复述（含「无 error 时（correct 与 warn）都需要用户输入 yes 确认」「存在 error 时 `-y`/`-yes` 不能越过」「`-force` 跳过整个检查步骤」三句逐字级重合）——按标准01 §4 属可接受的下层复述，但本文未标出处行，且 `:11` 已标 §4.5，故记为重叠不记为第二正本；③ `:56` 与 `21_observability.md:35/:169` 的日志落点条款互为副本（同一判据 `CHK-LOG-SYS` R3、同一「不取 CWD/源码树/`run/`/安装目录/家目录」清单）；④ `:31` 与 `21_observability.md:19` 的事件 kind 清单互为副本 |
| 元信息块 | 无 |
| 写法违规 | ① `:24` 与 `:3` 自相矛盾：抬头标 §7.1，正文 `:24` 写「命令树（唯一）见最高设计 **§6.2**」——同一份文档给同一对象两个条款号；② `:37-44` 配置表把 **CLI 旗标**（`json`/`template`/`output`/`help`/`-y`/`-force`）登成「配置项」，与 §3.2 的三类配置划分（`ASTROCS_DESIGN.md:231-235`）不同轴；表内 `:43` 写 `` `-y` ``、`:44` 写 `` `-force` ``，正文 `:26` 写 `` `-y`/`-yes` ``——同一文档内旗标字面量两种写法；③ `:12` 引用「`docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5（错误对象与退出码映射）、§7（日志落点）」——文件存在，但 §7 的标题面需核对（该文件 §5 存在 ✓）。机械列 `task_ids=[]` 正确 |
| 悬空引用 | `:20`「参考：`eng/contracts/schemas/cli_output.schema.json`、`eng/contracts/schemas/events.schema.json`」——**两者均无对应物**：跟踪集判定 `git ls-files eng/contracts \| grep -E "cli_output\|events\.schema"` → 0 命中；进一步 `git log --all --oneline -- eng/contracts/schemas/cli_output.schema.json` 与同 `events.schema.json` 各 0 条 ⇒ **不是被改名/被删，是从未存在**。真实的机器 schema 是 `eng/contracts/schemas/jsonl_event_v1.schema.json`（事件行）与 `eng/contracts/schemas/task_result.schema.json`。`:13` 的 `eng/contracts/schemas/phase_config*.schema.json` 通配可解析为 3 个真实文件 ✓（`phase_config_{normalize,mosaic,export}.schema.json`，实测存在）。机械列只报了 `events.schema.json` 一条（且以通配串形式），**漏报 `cli_output.schema.json`** |
| 与上位冲突 | ① `:24` 的 §6.2 错指（§6.2 是 export 流程 mermaid，不含命令树）；② **旗标字面量与实现不符，且与上位同源**：本文 `:26/:43` 写 `` `-yes` ``，`ASTROCS_DESIGN.md:356` 亦写 `` `-y` / `-yes` ``，而实现白名单是 `"-y", "--yes"`（`lib/infrastructure/cli/command_tree.h:39`、`parser.cpp:40`；`git grep '"\-yes"' -- lib` → 0 命中）⇒ 按文档字面输入的 `-yes` 不被接受；缺陷根因在上位条款，订正须两侧同改；③ `:40` 对 `-force` 的语义「跳过整个检查步骤」与设计 §4.5 一致，但代码侧注释给的是另一套语义（`command_tree.h:40`「`-force` // §6.3: **越过可强制项（缺失校准帧等）**」）⇒ 文档面（设计＋本文）与实现注释两侧不一致，需核实现行为准；④ `:53` 退出码枚举（2/3/4/5/6/7/8/9/10/70）与 `lib/infrastructure/cli/exit_codes.h:8-18` **逐名逐值一致** ✓（无冲突，如实记）；⑤ `:31` 事件 kind 清单与 `eng/contracts/schemas/jsonl_event_v1.schema.json:41-48` 的枚举一致 ✓ |
| 处置建议 | **保留 + 就地订正**：① `:24` §6.2→§7.1（或删除该句，抬头已标）；② `:20` 两条从未存在的 schema 名改为真实文件（`jsonl_event_v1`/`task_result`），并纳入 §3 的断链族；③ `:37-44` 表头改「CLI 旗标」并把 `-yes` 统一为 `--yes`（与设计 §4.5 同步改，属上位订正）；④ `:40` 的 `-force` 语义先以实测行为定案再写（现两侧不一致）；⑤ `:56` 与 `21_observability.md:35/:169` 的日志落点副本合并为一份、另一份引用（依据：标准01 §2 同一主题一份正本、§4 只引用） |

### 1.10 `docs/plugins/infrastructure/19_runtime.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/infrastructure/19_runtime.md` |
| 标题 | 插件文档：scheduler + pipeline（调度与资源） |
| 行数 | 124 |
| 角色 | 模块工作细节；目录一致。**但文件名与模块名不一致**（见「写法违规」①）——本批「编号/命名与实际模块不一致」的重点项 |
| 主题 | scheduler + pipeline：typed DAG 执行、线程预算、locality-aware 编排、流式内存、取消与 checkpoint |
| 上游 | `:3` 标「§8.1（顶层结构）」——**条款号错**（顶层结构 = §8.4 `:618`；§8.1 = 总原则）。`:12` 同错并加 §9（内存极简化/编排连续性，实有 `:675-676`）✓；`:8` 另标「模块名只有一份 = `scheduler` + `pipeline`（最高设计 §7.1）」——§7.1 只说「线程池在 `scheduler`」（`:490`），**未说 pipeline**；「职责名全仓唯一」在 §8.4 `:657`；`:117` 又写「依据最高设计 §8.1（顶层结构）与 §7.1（命令树）」⇒ 同一文档三处错指同一对象 |
| 下游 | 6 个跟踪引用者：`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`docs/plugins/infrastructure/21_observability.md`、`eng/tools/arch/check_thread_budget.py`、`eng/packaging/config/config_registry.json`、`docs/DOCUMENT_INDEX.yaml`。最高设计未指名本文 |
| 是否正本 | 调度与资源工序正本 |
| 重复或重叠对象 | **同一批约束被两份 infra 文档各写一遍**：`:91`「磁盘写满 / 写盘失败 → exit 10（RESOURCE）；内存/CPU/线程不设门（最高设计 §4.5，退出码见 §7.2）」对照 `21_observability.md:103` 的同一规则与 `18_cli.md:53` 的「资源门禁 → 10」——三份文档同一句话；② `:68`「重计算负载受 G-RES-01 **磁盘门**约束…判据与 exit 10 见 21_observability §8 与最高设计 §4.5/§9」对照 `21_observability.md:50-53`「## 8. 重计算负载资源门（G-RES-01）…**本节是 G-RES-01 判据的唯一语义权威**」与 `eng/contracts/resource_gate_v1.json` 的 `"gate_id": "G-RES-01"` / `"title": "重计算负载资源门（利用率 / 并行宽度 / 无界内存增长）"`＋独立的 `"disk_gate"` 段 ⇒ **同一个门 ID 在两份 infra 文档里指两件不同的事**（本文把 G-RES-01 称作磁盘门并把判据外包给 21_observability；21 与合同把 G-RES-01 定义为利用率/并行宽度/内存增长门，磁盘门单列为 `disk_gate`）。这是本批最有价值的两处「同主题两份正本且互不一致」之一（另一处见 1.3） |
| 元信息块 | 无 |
| 写法违规 | ① `:8`「**模块名只有一份 = `scheduler` + `pipeline`（最高设计 §7.1）；`runtime` 不是模块名（禁第二名字）**；本页文件名 `19_runtime.md` 是登记在册的文档路径，仅作路径使用」——文档在一句话里既禁止一个名字又长期保留该名字作为文件名，并靠一段自辩维持；这是「编号/命名与实际模块不一致」的登记面（`docs/plugins/00_INDEX.md:63` 同一形态：`\| 19_runtime.md \| scheduler \| 模块名 = scheduler + pipeline（最高设计 §8.1；runtime 不是模块名）`）；② `:113-124` 整个「## 9. 模块名（全仓唯一）」一节是对命名口径的**再论证**（同一文档已在 `:8` 论证过一次），标准01 §4 简洁；③ `:121-122`「本页文件名 `19_runtime.md` 是 `eng/packaging/config/config_registry.json` 与 `docs/DOCUMENT_INDEX.yaml` 登记在册的文档路径」——把登记面当设计依据写进正文；④ `:31`「**并行轴分配（PERF-501）**」与 `:35`「观测面 `ASTROCS_{LEASE,NODE,P1CAP}_TRACE=1`」——任务编号 PERF-501 进标题级条目（标准01 §4「正文无任务编号」）。机械列 `task_ids=[]` ⇒ **漏报 PERF-501**（其词表不含 PERF- 前缀） |
| 悬空引用 | `:123`「`pipeline` 在 `docs/modules/MODULE_MAP.yaml` 中随 **`index_module_count = 23`** 的登记规模一并维护」——**该字段不在 MODULE_MAP.yaml**：`git grep -n index_module_count` 实测只在 `docs/modules/registry/module_id_migration_baseline.json:6`（值 23）与 `eng/tools/quality/check_module_map.py:324/:332`；`docs/modules/MODULE_MAP.yaml` 内 0 命中 ⇒ 指向了错的文件。`:13`「`docs/design/*_DETAILED_DESIGN.md`（节点与先后关系）」与 `:20`「`eng/contracts/schemas/run_*.schema.json`」两处**通配**：前者可解析为 3 个真实文件 ✓；后者实测只有 `run_manifest.schema.json` 一个，通配掩盖了「其余 run 产物无 schema」这一事实（本文 `:19` 声明输出 run-graph/run-trace/resource_timeseries/resource_summary/worker_balance/artifact-manifest/run-summary 七类，其中只有 manifest 有 schema）。`:14` 指向 `21_observability.md §8` ✓ 存在。机械列报了 `run_*.schema.json` 与 `docs/design/*_DETAILED_DESIGN.md` 两条为悬空——**前者是误报（可解析）、后者是误报（3 个文件都在）**，真实悬空是它没报的 `MODULE_MAP.yaml#index_module_count` |
| 与上位冲突 | ① **G-RES-01 语义与上位/平行文档打架**（见「重复或重叠对象」②）：本文 `:68` 把 G-RES-01 说成「磁盘门」并把判据指针给 21_observability，而 21 与 `eng/contracts/resource_gate_v1.json` 把 G-RES-01 定义为重计算负载资源门、磁盘门是另一段 `disk_gate`；同时本文 `:68` 引「最高设计 §4.5/§9」——§4.5 `:357` 是磁盘门 ✓，**§9 不含任何门与 exit 10 条款**（§9 全文 `:669-681` 实测无 exit/门字样）⇒ 一个错指；② `:3`/`:12`/`:117` 三处 §8.1 错指（顶层结构实为 §8.4）；③ `:26`「科学依赖不可改变（最高设计 §3/4/5）」——§3/4/5 存在 ✓，但该句在 §3（数据对象与配置）里无对应条款，属**过宽引用**（把整章当条款引）；④ 无取值冲突：`:31-32` 的 `in_flight = min(n, frame_workers)`、`inner_omp = max(1, thread_budget / in_flight)` 与设计 §9 `:680` 的两轴分配原则同向，实测 `docs/architecture/THREADING_MODEL.md:26`（「### 并行轴分配（PERF-501 冻结口径）」）与 `docs/architecture/PERFORMANCE_MODEL.md:15`（「## PERF-501 编排参数标定」）两处下游锚**均存在** ✓ |
| 处置建议 | **保留（内容）＋文件名迁移**：① 文件改名 `docs/plugins/infrastructure/19_scheduler_pipeline.md`，同步 `docs/plugins/00_INDEX.md:63`、`docs/DOCUMENT_INDEX.yaml`、`eng/packaging/config/config_registry.json`、`eng/tools/arch/check_thread_budget.py` 四处登记与引用；改名的规范依据＝标准01 §2「目录承载固定角色」＋§3「索引由文档现状重建，与文件树一致」，而文档内自辩段（`:8` 后半、`:113-124`）在改名后整节删除；② `:68` 的 G-RES-01 措辞与 `:3`/`:12`/`:117` 的 §8.1→§8.4、`:68` 的 §9 引用一并订正；③ `:123` 的 `index_module_count` 指针改指 `docs/modules/registry/module_id_migration_baseline.json`；④ `:20` 通配改列真实 schema 名或明写「其余 run 产物无合同 schema（缺口）」；⑤ `:91` 与 `18_cli.md:53`、`21_observability.md:103` 的 exit 10 三副本合一 |

### 1.11 `docs/plugins/infrastructure/20_benchmark.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/infrastructure/20_benchmark.md` |
| 标题 | 插件文档：benchmark（CPU profile） |
| 行数 | 53 |
| 角色 | 模块工作细节；目录一致 |
| 主题 | `benchmark` 子命令：按 kernel 实测生成机器绑定的 `cpu_profile` |
| 上游 | `:3` 标 §9（CPU 后端与资源）✓ 存在且正是本节（`:669`）；`:12` 标「§7.1（命令树：benchmark）、§9（CPU 后端与资源）」——**§7.1 `:484` 确列 `benchmark`，两处全对** ⇒ 本批 plugin 层第二份上游完全正确的文档 |
| 下游 | 5 个跟踪引用者：`ACCEPTANCE_SPEC.md`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`eng/packaging/config/config_registry.json`、`docs/DOCUMENT_INDEX.yaml`。代码与算法层零引用 |
| 是否正本 | benchmark 工序正本 |
| 重复或重叠对象 | `:19`「**缓存位置：程序安装目录**；`benchmark` 直接输出 profile 到安装目录（自动生成/更新），后续运行时自动读取」＝`ASTROCS_DESIGN.md:491`（§7.1）与 `:672`（§9）同一句的第三次抄写（设计两处 + 本文），并与 `docs/algorithms/…`? 无关；另 `:27`「无 profile → 保守运行…不阻塞」＝设计 `:491` 「profile 缺失时按保守参数运行，显示经过但不阻塞」的复述；`:39`「scheduler 读取安装目录 profile 决定线程/ISA」＝设计 `:673` 「worker 数只来自 profile 与预算对象」的复述。三处取值一致 ⇒ 分层复述，不算第二正本 |
| 元信息块 | 无 |
| 写法违规 | `:31-35` 配置表三行 `output`（安装目录固定）/`kernels`（全部）/`repeats`——把「固定不可配」的量（`output`）登成配置项，且与 `:33` 自述「profile 输出路径（程序直接写入）」互相抵消（既然固定，就不是配置项）。标准01 §4 只写现行设计要怎样。机械列 `dangling_links=[]`、`task_ids=[]` 正确 |
| 悬空引用 | `:13`/`:20` 的 `eng/contracts/schemas/cpu_profile.schema.json` **存在** ✓（实测 `git ls-files` 命中）。`:31-35` 三个键在 `eng/contracts/schemas/phase_config_*.schema.json` 里 0 命中（benchmark 不属 phase_config 面，故不计为悬空文件，计与上位冲突①）。无文件级悬空 |
| 与上位冲突 | ① **与最高设计 §7.1 `:491` 的「无参数」冲突**：设计明文「`benchmark` 输出 profile 到安装目录，运行时自动读取，**无参数、不指定输出路径**」，本文 `:34` `kernels`「测量 kernel 选择」＋`:35` `repeats`「重复测量次数」构成两个用户可选输入面 ⇒ 与上位矛盾。实测：`git grep -n "repeats" -- lib/infrastructure/benchmark lib/infrastructure/cli` → 0 命中；`kernels` 只作为 profile **输出结构**的字段存在（`lib/infrastructure/benchmark/backend_host/bench_report.cpp:304` `j["kernels"] = kernels;`、`cpu_routing.cpp:87-90` 读取侧），**不是输入选项** ⇒ 本文的两个配置行既无合同载体也无实现载体，属虚构配置面；② `:19`/`:33` 与设计的安装目录口径一致 ✓；③ `:27` 保守运行与 `:45`「profile 与机器不匹配（哈希不符）→ 拒绝使用」与设计 `:672` 「绑定 CPU 特征/OS/provider 哈希」、`cpu_profile.schema.json` 的 `x-astrocs-writer: "benchmark"` 一致 ✓ |
| 处置建议 | **保留 + 就地订正**：① 删 `:34/:35` 两行（虚构配置面），或按设计 §7.1 走变更流程为 `benchmark` 开参数面——**二者互斥，需先定上位**（依据：标准01 §1「下位文档与上位冲突时以上位为准；冲突本身是缺陷」）；② `:33` `output` 行改为「固定为安装目录（非配置项）」的正向陈述或直接删除；③ 建议补一句 profile 输出面的唯一 schema 指针（现 `:13`/`:20` 重复列了同一个文件两次） |

### 1.12 `docs/plugins/infrastructure/21_observability.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/infrastructure/21_observability.md` |
| 标题 | 插件文档：observability（可观测性） |
| 行数 | 172 |
| 角色 | 模块工作细节；目录一致。**§8 整节承载科学/工程判据（G-RES-01 七条判据、阈值、统计量口径），按标准01 §2 属「标准与检查」角色 ⇒ 该节应属 `docs/standards/` 或 `docs/ci/`**（现目录与角色不一致＝角色错放，局部） |
| 主题 | 结构化日志、JSONL 事件流、运行图、资源监控与 G-RES-01 重计算负载资源门 |
| 上游 | `:3` 标「§7.3（错误传播与运行日志）、§8.1（顶层结构）」——§7.3 ✓（`:515`），**§8.1 错**（顶层结构 = §8.4）；`:12` 标 §7.2/§7.3/§9/§10 四条 ✓ 全部存在且语义对得上（资源记录在 §9 `:678-679`、run 产物在 §10 `:692-697`） |
| 下游 | 21 个跟踪引用者（本批最高）：含 `CMakeLists.txt`、`eng/contracts/resource_gate_v1.json`（**其 `"authority"` 字段直接指向本文 §8**）、`ACCEPTANCE_SPEC.md`、`docs/api/CLI_PROTOCOL_V1.md`、`docs/design/LOG_AND_ERROR_SYSTEM.md`、`docs/owner/PIPELINE_OVERVIEW.md`、`eng/tests/cli/test_cli004_process_protocol.py`、`eng/ci/tests/test_ci001_failclosed.py`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/infrastructure/19_runtime.md` |
| 是否正本 | **是，且有机器登记背书**：`eng/contracts/resource_gate_v1.json` 的 `"authority": "docs/plugins/infrastructure/21_observability.md#8-重计算负载资源门g-res-01"` 与 `"authority_note": "判据语义…以该文档 §8 为唯一权威；本文件是该权威下的**唯一数值源**"` ⇒ 语义正本＝本文 §8，数值正本＝该 JSON。**该指认与标准01 §1 冲突**（见与上位冲突①） |
| 重复或重叠对象 | **七条判据 + 阈值在本文与数值源两处承载（同值，实测逐条一致）**：本文 `:83-89`「① 单活跃计算线程：活跃计算线程统计量 < **2**／② 连续低利用窗：任何连续 **≥10 s** 窗利用率 < **60%**，且队列有工作／③ 无界内存增长：**≥ 32 MiB·s⁻¹**／④ 均值 < **85%**／⑤ p50 < **90%**／⑥ 单样本 ≥ **85%** 的样本占比 < **0.70**／⑦ 线程秒 < **10**」对照 `eng/contracts/resource_gate_v1.json`：`min_active_compute_threads: 2`、`queue_low_window_seconds_min: 10`＋`queue_low_utilization_percent: 60`、`memory.growth_limit_mib_per_s: 32`、`mean_utilization_min_percent: 85`、`p50_utilization_min_percent: 90`、`per_sample_utilization_min_percent: 85`/`per_sample_pass_fraction_min: 0.7`、`workload_floor_core_seconds: 10` ⇒ **全部同值**（本次逐条核过）。差异：JSON 另带 `min_active_compute_threads_statistic: "p50"`、`fallback_statistic: "peak"`、`queued_work_predicate`，本文 `:95-96` 用自然语言重复了同样的统计量与前置。第二处重叠：`:103` 的 exit 10 语义与 `19_runtime.md:91`、`18_cli.md:53` 三份并存 |
| 元信息块 | 无 |
| 写法违规 | ① `:101`「（`ASTROCS_DESIGN.md` §3.5「资源门只管磁盘…内存、CPU、线程不设门」；**§9.74 裁决 10**）」——「裁决 10」是负责人裁决记录编号（标准01 §4「正文无『负责人某日裁决/指令』」），`§9.74` 在最高设计里不是可解析条款（§9 无子节，实测 `grep -n "^###" ASTROCS_DESIGN.md` 无 9.x）；② `:52`「**本节是 G-RES-01 判据的唯一语义权威**」——下层文档自称唯一口径，标准01 §1「任何文档不声明自己的第二套权威顺序或『唯一口径』」（该声明有合同 JSON 背书，但最高设计从未指名本文，见与上位冲突②）；③ `:142-144` 探针表「落点」列写 `module_adapters.cpp` **P10-UTIL2-006** / **P7-UTIL-001** / **PERF-501**——三个内部任务/发现编号进正文；④ `:150`「**两个 I/O 口径不可混用（本任务实测澄清）**」——「本任务」＝过程语域。机械列：`sha256=[1048576]` ⇒ **误报**（`:97` 的 1048576 是 MiB→B 的十进制常数，非哈希）；`task_ids=[]` ⇒ **漏报** ③ 的三个编号（词表无 PERF-/P10-/P7- 前缀）；`history_narrative=[]` 正确 |
| 悬空引用 | `:15`/`:53` 的 `eng/contracts/resource_gate_v1.json` ✓ 存在。`:21`「`run_*.schema.json`」通配（实测只有 `run_manifest.schema.json`）、`lib/infrastructure/observability/logging/log_event_v1.schema.json` ✓ 存在、`eng/contracts/schemas/events.schema.json` ✗ **从未存在**（`git log --all -- eng/contracts/schemas/events.schema.json` 0 条；真实名 `jsonl_event_v1.schema.json`）——同一行里 `:19` 声明的输出（run-graph.json、resource_timeseries.csv、resource_summary.json、worker_balance.csv）**在合同面无一份 schema**，本文未如实登记该缺口。`:48`「脱敏规则唯一源 = **LOG-001 §5**」——LOG-001 不是路径而是合同 ID（实文件 `docs/architecture/observability/STRUCTURED_LOGGING_CONTRACT.md`，其 §5 标题「敏感路径脱敏（强制）」✓ 存在）⇒ 按 ID 引用不可直达。`:119`「口径同 `CLI_PROTOCOL_V1.md` §4 对 `artifact` 的 DET-001 附加字段」——裸文件名（实为 `docs/api/CLI_PROTOCOL_V1.md`，§4 存在 ✓、`:75` 确有「`artifact` 的 DET-001 附加字段不属冻结必含集」）。`:35` 判据 `CHK-LOG-SYS` R3 ✓（`eng/ci/checks.json` 2 处命中） |
| 与上位冲突 | ① **层级倒置（结构性）**：G-RES-01 的判据语义正本落在「模块工作细节」层，依据只是合同 JSON 的 `authority` 字段；标准01 §1 规定「科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`」「架构问题的唯一权威是最高设计 §8」，最高设计 §4.5 只承载「资源门只管磁盘；内存/CPU/线程不设门」一句 ⇒ 一个**决定 exit 10 与发布判定**的门，其语义权威在文档链的第三层，且最高设计没有指向它的索引（`grep -n "docs/plugins" ASTROCS_DESIGN.md` 实测命中的是 phase1/phase2/phase3 的六个指针，**无一条指向 `docs/plugins/infrastructure/`**）；② `:101` 的 §3.5 错指（「资源门只管磁盘」实际在 §4.5 `:357`；§3 只有 3.1/3.2/3.3）——**同一错号被合同 JSON 抄了两遍**（`resource_gate_v1.json` 的 `enforcement.in_process_hard_fail_retired_by` 与 `disk_gate.authority` 都写「ASTROCS_DESIGN.md §3.5/§6.3」，而 §6.3 是投影算法、退出码在 §7.2）⇒ 一个文档错号已污染机器合同；③ `:83-89` 判据表与 `:101`「程序内恒 record_only」在**表内不标注执行面**：表头「违约后果」列写「FAIL → exit 10」，而 §8.4 说 exit 10 只存在于 CI 判定点、程序内不改变退出码 ⇒ 同节两读法需回读才能自洽（标准01 §4 自洽）；④ `:35` 配置表 `log_level` 默认 `info`——该键在 `eng/contracts/schemas/phase_config_*.schema.json` 0 命中（实测），本文未说明日志配置的真实落点 |
| 处置建议 | **保留（唯一语义正本）＋结构订正**：① §8 整节**迁移**到 `docs/standards/`（标准01 §2 角色表：判据与检查方法属「标准与检查」），或由最高设计 §4.5/§9 增一条指向本文的索引并把门的语义上收到 `docs/design/`——二者取一，**属跨层归并，上呈负责人**（前言 §5 判据 6）；② `:101` 的 §3.5→§4.5、删「§9.74 裁决 10」并改为指向 `eng/contracts/resource_gate_v1.json#enforcement`；同步修 `resource_gate_v1.json` 两处同族错号（该文件不在本批清单，只登记为同因缺陷）；③ `:83-89` 表的「违约后果」列改为「FAIL（外部裁决面）／record_only（程序内）」两轴具名，消除与 §8.4 的回读依赖；④ 事件面 `events.schema.json` 悬空与四类 run 产物无 schema 的缺口如实登记；⑤ `:142-144` 三个内部编号删除、只留探针名与输出行；⑥ `:52` 的「唯一语义权威」改为「本合同门语义的现行载体，权威由 `resource_gate_v1.json#authority` 指名」（把自称换成可核登记） |

<!-- PROGRESS: 已审 12/25 -->

### 1.13 `docs/plugins/infrastructure/22_gaia_xpsd_client.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/infrastructure/22_gaia_xpsd_client.md` |
| 标题 | 插件文档：gaia_xpsd_client（外部星表查询） |
| 行数 | 84 |
| 角色 | 模块工作细节；目录一致 |
| 主题 | Gaia/XPSD 星表获取：缓存、查询合并、坐标/历元与版本 |
| 上游 | `:3` 标「§8.1（顶层结构）」——**条款号错**（顶层结构 = §8.4 `:618`）。`:12` 标「§3.3（科学量与星表：星表只解析**本地**星表文件，离线、零网络，科学身份不依赖网络）、§9（CPU 后端与资源：缓存复用、编排连续性）」——两条条款号与引文**都对得上**（§3.3 `:242` 逐字即此句、§9 `:675-676` 确有缓存与编排连续性）⇒ 文档自己把上位读对了，正文却写相反的东西 |
| 下游 | 5 个跟踪引用者：`docs/plugins/algorithms_phase1/05_platesolve.md`、`docs/modules/MODULE_MAP.yaml`、`docs/plugins/00_INDEX.md`、`eng/packaging/config/config_registry.json`、`docs/DOCUMENT_INDEX.yaml`。代码层零引用 |
| 是否正本 | 该基建模块的插件层唯一一份；但 `docs/modules/gaia_xpsd_client.md`（别批）是同主题另一份，且**两份的口径相反**（见与上位冲突①） |
| 重复或重叠对象 | `:27`「两级缓存：**进程内只读共享缓存**（内存，带字节预算与 LRU）+ **磁盘缓存**（跨运行复用，落安装/缓存目录）」与 `19_runtime.md:60`「缓存分层：进程内只读共享缓存（Gaia/星表、PSF、母版，带字节预算 + LRU）+ 磁盘缓存」、`docs/architecture/CACHE_POLICY.md:14`「| Gaia 查询缓存 | 有界/键化 | 查询键精确匹配…|」⇒ 同一缓存三处承载，取值一致（字节预算＋LRU＋磁盘层），但**容量键与失效规则只有 CACHE_POLICY 有、只有 22 有合并语义** ⇒ 三份各持一片，无一份完整（标准01 §3「唯一索引地图/一份正本」落空） |
| 元信息块 | 无 |
| 写法违规 | ① `:7`「查询 Gaia（及 XPSD 等**外部服务**）星表数据，**管理网络**」＋`:8`「外部服务不可用时降级策略显式」＋`:63`「| `timeout` | —— | s | **网络超时** |」＋`:72`「**网络不可用** + 无缓存 → 明确失败」＋`:81`「并发同键请求只发起一次**网络调用**」＋`:84`「**网络失败降级策略测试**」——整篇以在线服务为叙述主体；② `:50`「离线星表输入与在线查询等价合同」——把两种形态并列为现行设计而不标谁在位；③ `:37`「一次外部请求」的 mermaid 节点（`NET["一次外部请求"]`）把不存在的调用面画成流程图。机械列 `dangling_links=['eng/contracts/schemas/catalog_query.schema.json']` 正确 |
| 悬空引用 | `:20`「参考：`eng/contracts/schemas/catalog_query.schema.json`」——**从未存在**：跟踪集 0 命中，且 `git log --all -- eng/contracts/schemas/catalog_query.schema.json` 0 条 ⇒ 非改名非删除。真实星表面合同是 `docs/contracts/DATA_SEMANTICS.md` 的 DATA-GAIA-001 与 `eng/contracts/schemas/unified/` 对象族。`:56-63` 九个配置键在 `phase_config_*.schema.json` 里 0 命中（实测逐个 `grep -c` 全 0）⇒ 无合同载体的配置面 |
| 与上位冲突 | **① 与最高设计 §3.3 逐字相反，且被本文自己引为权威**：设计 `:242`「星表只解析**本地**星表文件，**离线、零网络**；科学身份不依赖网络」；本文 `:7/:8/:50/:63/:72/:81/:84` 通篇设网络查询与在线降级 ⇒ **P0 级口径冲突**。旁证：`docs/modules/gaia_xpsd_client.md:11`（别批）「解析 PixInsight XPSD 格式本地星表（Gaia DR3 / DR3SP，离线、零网络）」、`:63`「"network errors never alter scientific identity" 由零网络 I/O 结构性满足」、`docs/traceability/TRACEABILITY_MATRIX.json:734` 同句 ⇒ 同一模块的另两份文档与上位同为"离线"，本文是孤例。② **实现侧无网络 I/O**：`git grep -in "http\|curl\|socket\|send(" -- lib/infrastructure/gaia_xpsd_client` 只命中 `memory.md:12` 的一个 GitHub 仓库链接（下载来源），生产源 `src/gaia_client.c` 零网络调用 ⇒ 本文 §4.1/§4.2/§7/§8 描述的查询合并、预取、网络超时、降级**均无实现载体**（属"文档没写清现状"而非"未实现"，用词按前言 §5 判据 6 分档）；③ `:3` 的 §8.1 错指（见上游栏） |
| 处置建议 | **合并（并入 `docs/modules/gaia_xpsd_client.md` 的离线口径）＋就地重写**：删去在线服务面（`:7` 的「外部服务/管理网络」、`:8`、`:37` mermaid 的 `NET` 节点、`:50`、`:56` `catalog` 取值里的在线分支、`:63` `timeout`、`:72`、`:81`、`:84` 后半），保留并强化本地 XPSD 解析的缓存/键/版本/历元面（依标准01 §1「下位文档与上位冲突时以上位为准；冲突本身是缺陷」＋§4 只写现行设计）；`:20` 悬空合同指针改为真实锚；`:3` §8.1→§8.4；与 `19_runtime.md:60`、`CACHE_POLICY.md:14` 的三处缓存描述**归一为一份清单**（依据：标准01 §2 同主题一份正本） |

### 1.14 `docs/plugins/infrastructure/23_hips_browser.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/plugins/infrastructure/23_hips_browser.md` |
| 标题 | 插件文档：hips_browser（HiPS 浏览器组件） |
| 行数 | 52 |
| 角色 | 模块工作细节；目录一致（组件在位：`healpix_browser_qt` 在跟踪集中，`docs/architecture/BUILD_GRAPH.md:97-98` 亦登记为工具面 target） |
| 主题 | HiPS 产品可视化浏览（渲染/缩放/导航），工具分类、不进发布 |
| 上游 | `:3` 标「§8.1（顶层结构）」——**错**（= §8.4）。`:12` 标「§1.3（非目标：Alpha 不含 GUI）、§8.1（顶层结构：hips_browser 基建目录）」——§1.3 `:99` 确有「HiPS Browser 是未来可视化组件，不进产品清单」✓；**§8.1 错**（`hips_browser/` 在 §8.4 `:636`）；`:25` 标「§6.3：`visualization` 模式标注「不可测量」」✓ 正确（`:456` 逐字） |
| 下游 | 4 个跟踪引用者：`docs/plugins/00_INDEX.md`、`docs/modules/MODULE_MAP.yaml`、`eng/packaging/config/config_registry.json`、`docs/DOCUMENT_INDEX.yaml`。**代码与算法层零引用**（实测 `git grep -l 23_hips_browser -- lib` 空）⇒ 本批引用面最窄 |
| 是否正本 | 该组件的插件层唯一一份 |
| 重复或重叠对象 | 「不进产品 manifest/清单」这一条约束在**六处**承载：本文 `:8`/`:38`/`:45`（同页三遍）、`docs/plugins/00_INDEX.md:67`、`docs/architecture/CACHE_POLICY.md:5-7`＋`:16`、`docs/architecture/EXECUTION_MODEL.md:10-11`，而规范原文只有两处（`ASTROCS_DESIGN.md:99` 与 `:636`）⇒ 标准01 §4「同一判断在一页里最多出现一次」被整族违反（跨文档六份）。取值一致（全部"不进"），属**复制式一致** |
| 元信息块 | 无 |
| 写法违规 | ① `:8`「是**可选的**、独立于发行清单的可视化工具（**单独文档，地位 = 可选组件**）」——用括号给自己声明地位，属元信息语域（标准01 §4 开头无元信息块的精神延伸：地位由目录与索引承载，不由正文自述）；② `:7`「**未来** GUI 可视化组件」＋`:38`「独立可执行/组件；不进入 `acsd` 命令树」——同一页里既称"未来"又描述现有组件形态，而工具已在位（BUILD_GRAPH 登记 target `healpix_browser_qt`/`browser_cli`）⇒ 时态自相矛盾（标准01 §4 只写现行设计要怎样）；③ `:33-34` `color_map` 默认 `inferno`、`stretch` 默认 `sqrt` 两个**无推导的常数**（标准01 §1：常数须有其所在层的依据）。机械列 `dangling_links=['eng/contracts/schemas/hips_product.schema.json']` 正确 |
| 悬空引用 | `:19`「参考：`eng/contracts/schemas/hips_product.schema.json`（读侧复用）」——**从未存在**：跟踪集 0 命中、`git log --all -- …hips_product.schema.json` 0 条。真实的 HiPS 产品面合同是 `eng/contracts/schemas/unified/`（signal/variance/coverage/validity/support/provenance…）与 `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`。`:3`/`:12` 的 §8.1 属条款级悬空 |
| 与上位冲突 | ① `:33-34` 两个默认值**无实现与合同载体**：`git grep -in "inferno\|color_map" -- lib` 在浏览器实现里 0 命中（唯一命中即本文 `:33`）；`stretch` 在实现里的取值是 `asinh`（`lib/infrastructure/hips_browser/healpix_browser_qt/app/browser_cli.cpp:389` `sky.set_stretch("asinh", true);`）⇒ 本文默认 `sqrt` 与实现不同（工具面取值，非上位冲突，但登记为文档-实现不一致）；② `:12` 的 §8.1 错指；③ 「不进产品清单」与 §1.3/§8.4 一致 ✓ |
| 处置建议 | **保留 + 就地订正**（该组件确有源码与构建 target，属"工具分类"而非不存在，不得删）：① `:3`/`:12` §8.1→§8.4；② `:19` 悬空 schema 改指 `eng/contracts/schemas/unified/` + `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md`；③ `:33-34` 两常数改为「取值由实现侧定义」或补选择依据；④ `:8`/`:38`/`:45` 三处「不进清单」为一，其余引用 `ASTROCS_DESIGN.md` §1.3；⑤ `:7` 的「未来」与工具在位事实统一（写"工具面组件，不进发布安装规则"） |

### 1.15 `docs/architecture/ARCHITECTURE.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/ARCHITECTURE.md` |
| 标题 | Astro Celestial Sphere Database（ACSD） Architecture (V5 单一 CLI 冻结版) |
| 行数 | 68 |
| 角色 | 架构设计；所在 `docs/architecture/` ＝标准01 §2 应属目录 ⇒ **一致** |
| 主题 | 单一入口、分层组件图、三 Phase 数据流、配置/manifest/artifact 生命周期、线程与执行、机器可验不变量 |
| 上游 | `:3` 标 §8（软件架构）——**不缺且正确**（`ASTROCS_DESIGN.md:542`）。但 `:41`/`:57` 两处正文把产物落点归给「最高设计 §9」——§9 是「CPU 后端与资源」，落点在 **§10 `:696`** |
| 下游 | 28 个跟踪引用者（本批最广，`git grep -l --fixed-strings ARCHITECTURE.md`）：`docs/architecture/BUILD_GRAPH.md`、`docs/architecture/cpu/CPU_001_CAPABILITY_PROBE.md`、`docs/architecture/observability/STRUCTURED_LOGGING_CONTRACT.md`、`docs/contracts/ARCH-001.md`、`docs/interfaces/data/DATA-003_PRODUCTION_ARTIFACT_STORE.md`、`docs/interfaces/io/IO_002_HIPS_INPUT_INTERFACE.md`、`eng/cmake/install_layout.cmake`、`eng/tests/arch/arch_invariants.py`、`eng/tests/arch/test_inventory.py` 等 |
| 是否正本 | **架构层总览正本候选**，但与 `docs/owner/ARCHITECTURE_OVERVIEW.md`（117 行，负责人面状态汇总）构成同题两份：后者 `:9` 起「平台与发布形态（验收项）…## 6 架构状态汇总」＝状态汇总面，前者＝结构面 ⇒ 二者**可按角色分工**（架构设计 vs 负责人面，标准01 §2），不需合并；真正的问题是本文 §3 的数据流与同批 `DATA_FLOW.md` 重复（见下行） |
| 重复或重叠对象 | **A. Phase1 节点链两份且顺序矛盾**<br>本文 `:20-21`「Phase1: astro_image_io → calibration → star_detector → dynamic_psf → ipv(plate solve) → photometric_calib → snr_estimator → healpix_drizzle → HiPS」<br>同批 `docs/architecture/DATA_FLOW.md:12-19`「→ calibration（bias/dark/flat） → cosmetic/validity（坏点/宇宙线） → background/noise（背景与噪声） → platesolve（星表匹配 + 稳健迭代精化 WCS = 权威 WCS） → star_detection（星表引导检测：以本帧权威 WCS 逆投影 Gaia…） → psf → photometry → noise_snr → drizzle」<br>上位 `ASTROCS_DESIGN.md:271`「**节点序由真实数据流决定**：`calibration → cosmetic/validity → background/noise → platesolve → star_detection → psf → photometry → noise_snr → drizzle → 产品验证`」<br>重叠＝同一条 Phase1 链；差异＝**本文把 plate solve 排在 star_detector 与 dynamic_psf 之后**，与上位冻结序（platesolve 在 star_detection **之前**，因 `:262` 检测要「以本帧权威 WCS 逆映射 Gaia」）颠倒，且缺 `cosmetic/validity`、`background/noise` 两个节点。<br>**B. 产物落点三份**：本文 `:35`/`:41`/`:57`、`DATA_FLOW.md`（无）、`IO_AND_ATOMICITY.md:8-9/:26-27`、`17_aio.md:36`——同一句「产品只落块级 output_dir；run/ 只放临时产物与日志」在本批出现 6 次，取值一致。<br>**C. 单一入口**：本文 `:9`/`:11`/`:60` 同页三遍＝设计 §7.1 `:487` 与 §8.1 `:546` 的第四次至第六次抄写 |
| 元信息块 | **有**：`:5`「`> ID: ARCH-ARCH-001  状态: FROZEN  上游: ARCH-001  下游: API-001..005/ARCH-003..005/CLI-001`」——文档 ID＋状态编号＋上下游 ID 串（标准01 §4「文档开头无元信息块（文档 ID、状态编号、版本锚…）」）。**机械列 `meta_block=False` 漏报**（其判定只认 `---` YAML 头），这是本批对工具最有价值的一条校准 |
| 写法违规 | ① `:5` 元信息块（同上）；② `:1` 标题含「**V5 单一 CLI 冻结版**」——版本锚＋状态词进标题（标准01 §4；同族见 BUILD_GRAPH/CPU_BACKEND 的 ID 行）；③ `:12`「**被取代的旧入口（迁移冻结）**：`orchestrator.exe`、`astrocs-stage2`、`healpix_browser_qt`/`browser_cli`——**不再构建为发布目标**…」＋`:13`「ACR 不接入（V5）」——已退役实现与"旧"叙事写进正式文档层，直接违反 `ASTROCS_DESIGN.md` §0.3 `:61`「已经撤销的设计与已经退役的实现**不出现在正式文档里**」与标准01 §2「已撤销设计不放正式文档层」；④ `:13`「`acr_route` 仅存配置守卫（**ARCH-001 清单 24 处**，`!=cpu/auto` 显式拒）」——一个未登记出处的计数（`docs/contracts/ARCH-001.md` 全文 94 行、`:73-77` 是「## 6 ACR 隔离」，无"24 处"清单；实测 `git grep -c acr_route` 在跟踪集中散布 12 个文件、无 24 计数面）；⑤ `:52`「执行语义存量证据：`production_call_paths_stage1/2.csv`…（行数由 `eng/tools/arch/build_production_execution_inventory.py` 产出，`wc -l` 实测）」——把生成器命令与行数证据写进架构正文（标准01 §4 实验/过程数据）；⑥ `:64`「任务: ARCH-002(本文件)/ARCH-003(backend ABI)/ARCH-004(thread budget)/ARCH-005(Phase3 模块)/CLI-001(单一 target)/API-001..005」——任务编号一览；⑦ `:58`「（B4-01）」、`:45`「（SCI-P3 §8/§9a …）」、`:46`「ALG 文档 5c 节」——裸 ID 不给登记表路径（不可直达）。机械列 `task_ids=[]` ⇒ **漏报** ⑥⑦（词表不覆盖 ARCH-/CLI-/API-/B4-/SCI-P3 前缀） |
| 悬空引用 | `:20`/`:26`/`:58` 三处 `astro_image_io` 作为「唯一 I/O 层/唯一 I/O 入口」——**该名字在跟踪集中无源码载体**：`git ls-files lib/astro_image_io` → 0 条（磁盘上 `lib/astro_image_io/` 是本机构建残留，不入库，按前言 §5 判据只用跟踪集判定）；唯一命中的源文件是同属 aio 的 `lib/infrastructure/aio/include/astro_image_io.h`。现行名与正本口径＝`ASTROCS_DESIGN.md` §7.1 `:490`「FITS/HiPS 读写在 `infrastructure/aio`」、§8.4 `:631`「`aio/` FITS/HiPS/manifest 唯一 I/O」、本批 `17_aio.md:7` ⇒ **指向已被改名对象且未随迁**。`:65`「文档: MODULE_MAP.md/DATA_FLOW.md/…PERFORMANCE_MODEL.md」六个裸文件名（同目录确有，可解析但未按 §3 写路径）。无文件级硬悬空（机械列 `dangling_links=[]` 正确） |
| 与上位冲突 | ① **Phase1 节点序与最高设计 §4.2 `:271` 的冻结序矛盾**（platesolve 与 star_detector/dynamic_psf 先后颠倒、缺两节点）——本文 `:30` 一节标题即「Phase 数据流」，与同批 `DATA_FLOW.md` 承载同一主题且后者与上位一致 ⇒ **架构层内部两份数据流，一份错一份对**，读者无从判别；② `:41`/`:57` 的「最高设计 §9」错指（落点在 §10）；③ `:26` 把 `astro_image_io` 列为与 Phase1/2/3 平级的层，而设计 §8.4 树里 I/O 归 `infrastructure/aio`——名与位双错；④ `:66-68` 的权威自述本身合规 ✓（「不另立权威链、不作排他性权威自称…架构问题的唯一权威 = 最高设计 §8」与设计 §0.1 `:44` 一致）——本批唯一一处正向的权威声明，记为正面样本 |
| 处置建议 | **保留 + 就地订正（不改名、不迁移）**：① `:20-21` 的 Phase1 链**删除**、只留 `DATA_FLOW.md` 一份并加指针（依据：标准01 §1「下位与上位冲突时以上位为准；冲突本身是缺陷」＋§2 同主题一份正本——数据流的正本按目录职责属 `docs/architecture/DATA_FLOW.md`）；② `:20/:26/:58` 的 `astro_image_io` → `aio`（与设计 §8.4 同步）；③ 删 `:5` 元信息块、标题去「V5…冻结版」；④ `:12-13` 的退役件与 ACR 状态段删除，退役信息由 `docs/owner/` 或 `artifacts/evidence/` 承载（依据：设计 §0.3 `:61`）；⑤ `:41/:57` §9→§10；⑥ `:64` 任务编号段删除或改为指向 `docs/contracts/INDEX.yaml`；⑦ `:13` 的「24 处」计数删除或补登记表出处 |

### 1.16 `docs/architecture/ASYNC_IO_CONTRACT.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/ASYNC_IO_CONTRACT.md` |
| 标题 | Async I/O Contract（CON-008 有界异步 I/O 合同） |
| 行数 | 114 |
| 角色 | 架构设计（接口合同）；目录一致。**但 §8/§9/§10 是测试完成度与实测数据，属「标准与检查」/负责人面** ⇒ 局部角色错放（应迁往 `docs/ci/` 或 `docs/owner/`） |
| 主题 | 有界 producer/consumer 队列：容量推导、buffer 所有权、reader 线程安全、取消/关闭、内存预算 |
| 上游 | `:3` 标 §8（软件架构）✓ 存在且相关（`ASTROCS_DESIGN.md:599` §8.3 明写「处理异步预取」、§9 `:674`「异步 I/O 一个专用线程」）——但本文**未引到这两条具体条款**，只引了章号 ⇒ 上游标注不精确（标准01 §3 要求「章节号 + 标题」） |
| 下游 | 4 个跟踪引用者：`docs/architecture/EXECUTION_MODEL.md`（`:31` 表格「合同见 [ASYNC_IO_CONTRACT.md]」）、`docs/architecture/THREAD_BUDGET_ARCH.md`、`lib/algorithms/coverage/include/astro/phase2/async_io.h`、`docs/DOCUMENT_INDEX.yaml` |
| 是否正本 | 异步 I/O 合同正本 ✓（唯一一份；`docs/architecture/IO_AND_ATOMICITY.md` 只讲原子性，`docs/interfaces/io/` 三份讲接口契约，不重叠） |
| 重复或重叠对象 | `:28`「已有生产结论：cfitsio 同一 `AioHipsDataset*` 不保证并发读安全」与 `:83`「生产结论：cfitsio 同一数据集句柄不保证跨线程并发读安全（详见第 3 节）」——**同一条结论在同一文件里写两遍**（§3 与 §9 各一份，标准01 §4 简洁）；`:64-65` 的容量与 `:15-17` 的容量推导亦重复（`bounded_queue_capacity(memory_budget_bytes, item_bytes)` 两处、`容量下界为 1` 与 `max(1, budget/item_bytes)` 两处）。与 `EXECUTION_MODEL.md:31-32` 重叠：该文表格把「HiPS write async_io／HiPS read 并发只读（无进程级锁）」再述一遍并指回本文 ✓ 属合法引用（带指针） |
| 元信息块 | `:5-7` 三行引用块（关联代码/目标/队列容量推导）——属"合同要点"而非文档 ID，**不判元信息块**；机械列 `meta_block=False` 此处正确 |
| 写法违规 | ① `:75`「**已完成**：`phase2_async_io` 以上全部通过（见 `lib/algorithms/coverage/tests/async_io_test.cpp`）」＋`:99`「状态：PAR-001 **PASS**」——交付状态写进正式文档（设计 §12.5 `:801`「状态由检查与验收现场计算，**登记表不预写状态**」）；② `:105-107`「实测 N=20、Tio=4ms、Tcmp=2ms、2 消费者：串行估计 120ms，实测 **81.5ms**（ratio 0.68，贴近 I/O 下界 80ms）」——实验过程数据进架构合同（标准01 §4）；③ `:90-95`「**待接入点（明确记录，不伪造并发读安全）**：`stage2.cpp`：tile 读取/写入**可使用**…；`sampler.cpp`：`SamplerReader` **可扩展为**…」——将来时＋台账式缺口登记（标准01 §4「只写现行设计要怎样」；此类登记应落 `docs/KNOWN_LIMITATIONS.md` 或 `artifacts/evidence/`）；④ `:1` 标题内嵌合同 ID「CON-008」但未给 ID 登记表路径。机械列 `history_narrative=[]`、`task_ids=[]` ⇒ **漏报** ③（"待接入/可扩展"型未在其词表内）与 CON-/PAR- 前缀 |
| 悬空引用 | `:5`「关联代码：`lib/algorithms/coverage/include/astro/phase2/async_io.h`」✓ 存在（跟踪集命中）；`:75` 的测试文件 ✓；`:100` 的 `eng/tests/cli/test_parallel_queue.py` ✓ 存在。**`:91` 「`stage2.cpp`」与 `:93` 「`sampler.cpp`」为裸文件名**：跟踪集中 `stage2.cpp` 只有 `lib/algorithms/coverage/tools/stage2.cpp`（工具面，非生产链），`sampler.cpp` 只有 `lib/algorithms/coverage/src/sampler.cpp` ⇒ 一个"待接入点"被写成生产文件而实际落在 `tools/`，按 §3 双向可追溯判为**指错落点**。`:33`「cfitsio `_REENTRANT` 构建…`cfileio.c:1544`」——该文件跟踪集内存在 ✓（`lib/infrastructure/aio/third_party/cfitsio/cfileio.c`），但正文写的是裸 `cfileio.c`，无路径 |
| 与上位冲突 | ① **与设计 §9 `:674`「串行 I/O 与控制面单线程，**异步 I/O 一个专用线程**」冲突**：本文 `:30`「异步 IO 池中 **`io_workers` 个 worker** 各自持有自己的 reader 实例」＋`:89`「异步队列并发读的入队项限于已证明线程安全的句柄」⇒ 本文允许一个 **worker 数**的异步读池，上位只授权"一个专用线程"；本文未标这一扩张的依据（标准01 §1「下级文档陈述与本设计一致的细化内容，不另立」）；② `:62-65` 的「容量 = max(1, budget/item_bytes)」与设计 §8.3 `:605`「有界队列 + 背压」一致 ✓；③ `:37`「Worker 捕获读取/写入异常后调用 `cancel`，主线程通过 `error()` 获得第一错误文本」与设计 §7.3 `:519-520`「错误传播口径＝每个失败都产生稳定错误码…只写日志属未传播」一致 ✓（本文 `:38` 亦自证「未取消且未关闭的队列对错误一律显式报告」） |
| 处置建议 | **保留（合同本体）＋分层拆分**：① §8/§10 的完成状态与实测数据**迁出**到 `docs/ci/`（该门的证据登记）或 `docs/owner/`，本文只留"必须覆盖哪些行为"（依据：标准01 §2 角色表「标准与检查」＋设计 §12.5）；② §9 的三条待接入点并入 `docs/KNOWN_LIMITATIONS.md` 或按现状改写为正向条款；③ `:91` 的 `stage2.cpp` 补全路径或删（工具面文件不应当作生产接入点）；④ `:28` 与 `:83` 的重复结论合一；⑤ `:3/:30` 的上游标注补到条款级并说明 `io_workers` 与 §9「一个专用线程」的关系（要么收紧文档、要么走变更流程改上位）——**该点是上位与下位的实质分歧，登记为上呈项**（前言 §5 判据 6） |

<!-- PROGRESS: 已审 16/25 -->

### 1.17 `docs/architecture/BUILD_GRAPH.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/BUILD_GRAPH.md` |
| 标题 | Build Graph |
| 行数 | 121 |
| 角色 | 架构设计（构建面）；目录一致。**但主体是脚本生成的机器块**（`:5` 自述）⇒ 按 DOCUMENT_INDEX.yaml 自己的枚举应标 `GENERATED` |
| 主题 | 生产入口的传递闭包 target 集、非生产/非根 target 面、编译定义与链接面 |
| 上游 | `:3` 标「§8（软件架构）；ARCHITECTURE.md §1（总览与单一入口）、§7 不变量 1」——**两条都在位**：设计 §8 `:542` ✓；`ARCHITECTURE.md:7`「## 1 总览与单一入口」✓、`:54-56`「## 7 不变量（机器可验）」第 1 条「唯一生产入口=acsd」✓ ⇒ 本批唯一一处同时引用上位与同层文档且全部可直达 |
| 下游 | 6 个跟踪引用者：`eng/ci/checks.json`、`eng/ci/cmake_graph.py`、`eng/tools/arch/gen_build_graph_doc.py`、`eng/tools/quality/contracts/check_build_graph.py`、其 fixtures、`docs/DOCUMENT_INDEX.yaml`。**文档层零引用** ⇒ 只被工具消费 |
| 是否正本 | 构建图 target 集正本 ✓（唯一一份；生成器与门双向绑定：`eng/tools/arch/gen_build_graph_doc.py` 写、`eng/tools/quality/contracts/check_build_graph.py` 判） |
| 重复或重叠对象 | 与 `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv`、`docs/architecture/execution_inventory.csv`、`docs/architecture/api_inventory.csv` 三份 CSV 同域（构建/执行/符号登记），本文 `:120` 自己做了分工声明 ✓（合法分层）。真正重叠＝`:82-83` 与 `:97-98` 的「ARCHITECTURE §1 迁移冻结：非入口／工具面」——同一判断在 `ARCHITECTURE.md:12` 已有正本，本文按行重复了 4 次（4 个 target 各一次）；取值一致 |
| 元信息块 | `:5-9` 的生成器/门/判别力自证声明块（`> **本文的构建图机器块由生成器…命令 = …门 = CON-BUILD-GRAPH…**`）——**这是生成物必需的出处声明，不判为元信息块**（它承载可复算命令，符合标准01 §5「可复算」）。机械列 `meta_block=False` 与此判断一致 |
| 写法违规 | ① `:15`「src_fingerprint = 该 target 源集（排序后逐行连接）的 **sha256 前 12 位**，4-4-4 分组书写」＋表内 30 个指纹值（如 `:20` `8a28-c03e-0a23`）——标准01 §4「正文无 sha256」；但这是**生成器的机器块且可复算**（`:113` 给了重跑命令）⇒ 判为"字面违反、实质必需"，处置是加豁免标记而不是删（本批对工具的第二条校准：**机械列 `sha256=[]`、`commit_sha=[]` 全部漏报这 30 个 12 位十六进制串**，因为它按 4-4-4 分组了）；② `:82-83`「Phase2 工具面（ARCHITECTURE §1 迁移冻结：非入口）」与 `:97-98`「浏览器工具（ARCHITECTURE §1 迁移冻结：工具面）」——「迁移冻结」＝退役叙事（标准01 §4）；③ `:99-100` 与 `:108-109`「ACR DORMANT（最高设计 §8：不进生产构建）」——**§8 全文不含 ACR/DORMANT 字样**（实测 `ASTROCS_DESIGN.md` 里 ACR 只出现在 `:98`（§1.3）与 `:813`（§13）），故括号里的上位归因错指。机械列 `dangling_links=[]`、`history_narrative=[]` ⇒ **漏报** ②③ |
| 悬空引用 | `:13`「`eng/ci/spec_named_impls.json` 的 production_entry」、`:105`「`lib/algorithms/coverage/CMakeLists.txt` 的 option(P2_ENABLE_OPENMP … OFF)」、`:109`/`:119`「`eng/cmake/install_layout.cmake`」、`:119`「`eng/packaging/install-tree.contract.json`」、`:120`「`docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv`」、`:121`「`eng/ci/check_spec_named_impl.py`」——**逐个实测全部存在** ✓（含 `--self-test` 的两条命令对应脚本）。`:64` 的 `astrocs_noop` 等 target 名与生成块一致。**无悬空**（本批最干净的一份） |
| 与上位冲突 | ① `:99/:108` 把 ACR 不进生产归给「最高设计 §8」——§8 无此内容，实际依据是 §1.3 `:98`（非目标列 ACR/GPU 生产路由）与 §13 `:813`（发布候选须「ACR 生产不可达」）⇒ **归因条款错**，结论本身与上位一致；② 索引标注冲突：`docs/DOCUMENT_INDEX.yaml` 把本文登记为 `status: ACTIVE_NORMATIVE`，而同目录三份生成物（`PRODUCTION_EXECUTION_INVENTORY.csv`、`api_inventory.csv`、`execution_inventory.csv`）登记为 `status: GENERATED`（该枚举在索引里被用 24 次），本文 `:5` 自述「机器块由生成器导出」⇒ **同一生成物两类标法**，索引与文档实际不符（标准01 §3「索引由文档现状重建，与文件树一致」）；③ 表内容与 CMake 的一致性由 `check_build_graph.py` 现场判，本文不预写状态 ✓ 合规 |
| 处置建议 | **保留**（内容、路径、生成机制均正当）＋两项登记类订正：① 索引 status 改 `GENERATED`（或给生成器块一个显式豁免说明），依据：标准01 §3 唯一索引地图须与现状一致；② `:99/:108` 的上位归因 §8→§1.3/§13；③ `:82-83/:97-98` 的「迁移冻结」改为正向身份描述（「工具面 target，不属发布安装规则」）并在 `ARCHITECTURE.md` 侧删除退役清单（见 1.15）——两侧同改，避免留下半个说法 |

### 1.18 `docs/architecture/CACHE_POLICY.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/CACHE_POLICY.md` |
| 标题 | Cache Policy |
| 行数 | 26 |
| 角色 | 架构设计；目录一致 |
| 主题 | 生产缓存清单（容量/身份/失效/线程四要素）＋缓存规则 |
| 上游 | `:3` 标 §8 ✓ 存在。**但 `:7` 与 `:16` 两处「最高设计 §7.1/§10.1：HiPS Browser 不进产品 manifest」——`§10.1` 不存在**（§10 无子节，实测 `grep -n "^### 10\." ASTROCS_DESIGN.md` 空）；HiPS Browser 的"不进产品清单"实际在 §1.3 `:99` 与 §8.4 `:636`；§7.1 只是**没有** browser 命令（缺席不等于规定） |
| 下游 | 3 个跟踪引用者：`docs/audit/doc_classification.csv`、`lib/infrastructure/gaia_xpsd_client/src/gaia_client.h`、`docs/DOCUMENT_INDEX.yaml` |
| 是否正本 | 缓存清单正本（设计 §9 `:675` 只给四要素要求与举例，不给清单）⇒ 清单面唯一，合规 |
| 重复或重叠对象 | **`:16` 与 `:5-7` 是同一条排除声明的两份**（同页两处：「Browser tile cache 属『工具分类（非发布）』…不是生产缓存」）；`:20`「cache 必须 capacity + identity + invalidation + thread model」＝设计 §9 `:675`「任何缓存具备容量、身份、失效、线程模型四要素」的复述（同值 ✓）；`:14` Gaia 查询缓存与 `22_gaia_xpsd_client.md:27`、`19_runtime.md:60` 构成同一缓存的三处承载（见 1.13 重复栏） |
| 元信息块 | 无（`:5-7` 是 ⚠ 适用范围声明，承载实质信息，不判元信息块） |
| 写法违规 | ① `:16`「**~~Browser tile cache~~** **工具分类（非发布）**」——用删除线在现行文档里表达"这行不算数"（标准01 §4「无历史叙事；需要表达硬约束时用正向条款」）；且同一行既在表里又被前言排除 ⇒ 该行本身是负空间条目；② `:13`「stale=2」——裸数值无出处（哪个枚举、哪段代码给 2）；③ `:26`「ENG-CACHE-001..002（**S2 注册**）」——`S2` 不是可解析的登记表名（实测 `git grep -n "S2"` 在 `eng/ci/checks.json` 与 `docs/standards/` 无该含义；只有 `docs/standards/DOCUMENTATION_STANDARD.md:36` 出现「（S8）」式的编号惯例），⇒ 不可直达。机械列 `history_narrative=[]` ⇒ **漏报**「~~删除线~~」这种非词表型历史标记 |
| 悬空引用 | `:15`「`lib/algorithms/drizzle/healpix_drizzle/spherical_overlap.h:303 TargetGeomCache(capacity=8192) 默认`」——文件存在 ✓ 但**行锚漂移 18–20 行**：实测 `:303` 是注释横幅「// 定点优化（DRIZZLE_TARGETED 优先级 1）」，`class TargetGeomCache` 在 `:321`、`explicit TargetGeomCache(std::size_t capacity = 8192)` 在 `:323`。`:14`「`lib/infrastructure/gaia_xpsd_client/src/gaia_client.c:键精确匹配锚点`」——**用中文短语当行锚**（不是行号也不是符号名），实测该文件无「键精确匹配锚点」字样 ⇒ 不可直达锚。`:26` 的 `S2` 同上。机械列 `dangling_links=[]` ⇒ 行锚级缺陷全部漏报（它只判文件级） |
| 与上位冲突 | ① **§10.1 不存在**（两处）；② **缓存集合与设计 §9 `:675` 的举例不重合**：设计点名「缓存只保留复用收益高于重算成本的对象（**Gaia 查询、PSF 模型、标定母版**）」，本文清单是「UPM dense cache、Gaia 查询缓存、Drizzle geometry cache、(Browser tile cache)」⇒ **PSF 模型缓存与标定母版缓存在清单里缺失**，而 UPM dense / Drizzle geometry 两项不在设计列举内（设计是"举例"式措辞，不构成穷举禁令，但一份自称生产缓存清单的正本与上位举例零交集的两项、漏掉上位点名的两项，属**上下游不对齐**）；③ `:13` 的 UPM dense cache 与 `docs/architecture/IO_AND_ATOMICITY.md:22-23`「dense cache：固定 512B 头部 + 二进制块 + streaming checksum；打开校验 checksum 与 source_hash」——同一缓存的两处描述，本文写「身份 = model_hash + target_order、失效 = 打开时 source_hash 校验、stale=2」、彼文写「打开校验 checksum 与 source_hash」⇒ 校验对象两说（model_hash vs checksum），需定一份 |
| 处置建议 | **保留 + 就地订正**：① 删 `:16` 整行，把"工具分类"的排除收进 `:5-7` 前言一处（正向条款）；② `:7/:16` 的 §7.1/§10.1 → §1.3/§8.4；③ 与 `IO_AND_ATOMICITY.md:22-23` 的 dense cache 描述合并（一处讲协议、一处讲清单，互指）；④ `:15/:14` 行锚改为可直达形式（符号名 + 实测行号或删除）；⑤ `:13` 的 `stale=2` 与 `:26` 的「S2 注册」补出处；⑥ 补设计 §9 点名的 PSF 模型缓存与标定母版缓存两行（或写明它们不在生产缓存面内、由哪份文档承载） |

### 1.19 `docs/architecture/COMPATIBILITY_POLICY.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/COMPATIBILITY_POLICY.md` |
| 标题 | Compatibility Policy |
| 行数 | 26 |
| 角色 | 架构设计；目录一致。**但 §「UPM 持久化」（`:9-16`）是具体数据格式的兼容细则**，按标准01 §2 属「合同说明」⇒ 局部角色错放，应迁往 `docs/contracts/`（该处已有 `DATA_SEMANTICS.md`、`HIPS_STORAGE_FORM_CONTRACT.md` 等同类） |
| 主题 | 科学正确性优先于兼容的总则＋UPM 模型文件与旧签名的兼容策略＋配置/接口冻结后的变更规则 |
| 上游 | `:3` 标 §8 ✓ 存在（相关条款实际在 §8.5 `:661-665`「版本化公开头 `struct_size`/`abi_version`」与 §10 `:688-696`，本文未下钻到条款级 ⇒ 上游标注不精确） |
| 下游 | 4 个跟踪引用者：`docs/TRACEABILITY.csv`、`docs/audit/doc_classification.csv`、`eng/tools/quality/v19r3_traceability.py`、`docs/DOCUMENT_INDEX.yaml`。文档层零引用 |
| 是否正本 | 兼容政策正本（唯一一份）；但 `docs/VERSIONING.md`（别批，根文档）与 `docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md` 也各持一段兼容性口径 ⇒ 主题组，见 §2 G-7 |
| 重复或重叠对象 | `:21`「config default 两处不一致视为缺陷」与 `eng/ci/ledgers/config_default_divergences.json`＋`eng/ci/check_config_defaults.py:5`（注释点名「同一逻辑键在不同生产代码路径取不同缺省（如 `smoothing_lambda`）」）构成"规则—实现—台账"三点，本文只给规则一句、不给台账指针 ⇒ 与 `11_upm.md:134`（本批 1.2）的「并存不冲突」说法互不引用，同一问题两处各说各话 |
| 元信息块 | 无 |
| 写法违规 | ① `:11`「**历史 writer（v1/v2）**frames 列表来自 std::map（升序）…→ **旧文件**自洽，可直接读取（**方案 A 安全迁移**）」——「历史」「旧」＋内部方案编号（标准01 §4 无历史叙事、无任务编号）；② `:16`「**旧签名保留仅为兼容**：`p2_reject_stack` 为 COMPAT adapter，生产用 `p2_reject_stack_ex`」——「旧签名保留」是留痕语域（同一条款可以正向写成「adapter 只服务兼容入口，生产链一律走 `_ex`」）；③ `:20`「接口冻结（**W2/V15/V19**）后行为变化必须 Contract first」——三个开发轮次代号，且 `:25-26` 自己规定「开发轮次版本号只进版本说明/ADR」⇒ **本文违反自己的条款**；④ `:12`「（方案 A 安全迁移）」、`:26`「只进版本说明/ADR」——`ADR` 面在跟踪集中只存在于 `lib/infrastructure/acr/docs/ADR-00*.md`（ACR 隔离实验目录，生产不可达），文档级 ADR 目录不存在 ⇒ 指向不存在的位置。机械列 `dates=[]`/`task_ids=[]`/`history_narrative=[]` **全部为空 ⇒ 三条漏报** |
| 悬空引用 | `:9`「UPM 持久化（**DATA-UPM-MODEL-001**）」——合同 ID；实测 `docs/TRACEABILITY.csv` 与 `docs/contracts/DATA_SEMANTICS.md` 有该族 ID，但本文未给任何路径 ⇒ 不可直达（标准01 §3）。`:14`「畸形文件稳定报错（**ERR-P2-UPM-001**）」——**该码在生产代码里零发出点**：`git grep -c ERR-P2-UPM-001 -- lib` 空，仅 `eng/tools/quality/v19r3_traceability.py`（5 次，登记表性质）与文档/台账命中；`ERROR_MODEL.md:54` 给的实现锚 `upm.cpp:~890`（带"~"的不精确锚）与 `upm.cpp:25-26` 注释（「open 强校验 frames 存在/数组/无重复/类型非法一律拒绝」）描述的是同一校验，但校验返回的是整数 rc，不产出该字符串 ⇒ 「稳定报错（ERR-P2-UPM-001）」为**未接线的文档口径**。`:16` 的两个符号 `p2_reject_stack`/`p2_reject_stack_ex` ✓ 存在（`lib/algorithms/coverage/include/astro/phase2/rejection.h`） |
| 与上位冲突 | ① 上游只到章号（§8），未落到 §8.5/§10 条款 ⇒ 与标准01 §3「章节号 + 标题」不符；② `:7`「科学正确性优先于透明兼容；ambiguous/损坏旧文件显式拒绝」与设计 §4.5/§7.3 的 fail-closed 总则一致 ✓；③ `:20` 的轮次代号与本文 `:25-26` 自订规则冲突（自相矛盾，属内部一致性缺陷＋上位 §13 `:814`「文档与合同的内部修订号不进入程序、代码与产物」的反向情形：内部代号进了正式文档）；④ `:16`「生产用 `p2_reject_stack_ex`」与代码一致 ✓ |
| 处置建议 | **拆分（迁移）**：① `:9-16` 的 UPM 持久化兼容细则迁往 `docs/contracts/`（与 `DATA_SEMANTICS.md` 的 UPM 段合并），依据：标准01 §2 角色表「合同说明→`docs/contracts/`」；② 本文只留政策级三条（原则／变更须 Contract first／版本号出现面），并把 `:20` 的 W2/V15/V19 与 `:12` 的「方案 A」删除；③ `:14` 的 ERR-P2-UPM-001 要么接出实现（返回码→错误 ID 的映射点），要么降格为"文档 ID"并明写无实现——按前言 §5 判据 6 不得把"未接线"写成"稳定报错"；④ `:21` 补 `eng/ci/ledgers/config_default_divergences.json` 指针；⑤ `:26` 的「ADR」改指实际存在的登记面 |

### 1.20 `docs/architecture/CPU_BACKEND_ARCH.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/CPU_BACKEND_ARCH.md` |
| 标题 | CPU Backend 架构 (C ABI v1 · Loader · Per-Kernel Dispatcher) |
| 行数 | 53 |
| 角色 | 架构设计；目录一致。**但 §3 六查、§4 ABI 结构必含字段属"能力探测/ABI 合同"细则，同主题已在 `docs/architecture/cpu/CPU_001_CAPABILITY_PROBE.md`、`docs/architecture/abi/ABI_003_SECURE_LOADER.md`、`docs/standards/C_ABI_STANDARD.md` 各自承载** ⇒ 同层多份 |
| 主题 | CPU 后端变体策略、编译隔离、加载前六查与信任边界、稳定 C ABI v1、kernel 注册粒度、失败与回退、发布检查 |
| 上游 | `:3` 标 §8 ✓；相关上位实为 §9 `:671-673`（纯 CPU、baseline/AVX2/FMA/AVX-512、profile 绑定）与 §8.5 `:661-665`（ABI 与加载检查）。`:6` 另标「权威来源: **控制包 05 全条目(§1–§7)**逐节落成架构; 约束项与 05 完全一致」 |
| 下游 | 5 个跟踪引用者：`docs/architecture/cpu/CPU_003_AVX2_PROVIDER.md`、`eng/tests/arch/test_backend_arch.py`、`lib/infrastructure/benchmark/cpu/baseline/include/astrocs/cpu/baseline_provider_v1.h`、`docs/algorithms/PHASE2_COVERAGE.md`、`docs/DOCUMENT_INDEX.yaml` |
| 是否正本 | **不是**——它把三条不同层的口径（架构决策、ABI 合同、能力探测算法）各抄了一份摘要：ABI 结构必含字段（`:25`）与 `docs/standards/C_ABI_STANDARD.md` 重叠、六查（`:18`）与 `cpu/CPU_001_CAPABILITY_PROBE.md:27` 重叠、kernel 七类（`:30`）与 `docs/standards/BENCHMARK_STANDARD.md`/`PERFORMANCE_MODEL.md` 重叠 |
| 重复或重叠对象 | **六查两处承载**<br>本文 `:18`「加载前六查(全过才执行): ① CPUID feature bits; ② OSXSAVE; ③ XGETBV(XMM/YMM/ZMM 保存); ④ 进程可用 CPU=affinity∩cgroup∩Job Object(非机器总核); ⑤ manifest `required_features`; ⑥ 文件 sha256+ABI 版本。」<br>`docs/architecture/cpu/CPU_001_CAPABILITY_PROBE.md:27`「CPUID feature → OSXSAVE → XGETBV XMM/YMM/ZMM state → (CPU-002+) DLL ABI/hash…」<br>重叠＝前六步中的 ①②③⑥ 完全同名同序；差异＝④ 的进程可用 CPU 与 ⑤ 的 manifest 字段在 CPU_001 里由别的条目承载。⇒ 同一角色目录内两份「加载前检查」清单（标准01 §2 同主题一份正本）。另：`:10`/`:47` 的 `NOT_SHIPPED`/`NOT_APPLICABLE` 决策词与 `docs/architecture/ISA_VARIANTS.md`（18 处）、`ISA_BIT_MANIP_VARIANTS.md`（4 处）同域 |
| 元信息块 | **有**：`:5`「`> ID: ARCH-BACKEND-001  状态: FROZEN  上游: ARCH-002/ALG-001..007  下游: ABI-001/002, ISA-001..005, BENCH-001..005`」＋`:6` 权威来源声明——文档 ID＋状态＋版本锚＋**自设第二套权威来源**（标准01 §1「任何文档不声明自己的第二套权威顺序或『唯一口径』」）。机械列 `meta_block=False` ⇒ **漏报**（同 1.15 的原因） |
| 写法违规 | ① `:5-6` 元信息块＋第二权威来源；② `:8`「## 1 设计结论(**05 §1**)」及 `:12/:16/:21/:28/:32/:39` 同级标题一律挂「(05 §N)」，`:43`「## 8 与 **V5 任务**的映射」＋表（`:47-53` 七行「ISA-001..005 / ABI-001 / ABI-002 / BENCH-004 / BENCH-005 / **09 打包**」）——整篇骨架是一个任务包的条目号，属过程台账进正式文档层（标准01 §2）；③ `:14`「（全局 `-march=native`/`/arch:AVX2`/`-mavx*` 属全局设置）」这类"否定对照"写法与 `:10` 的「AVX-512 无实测收益则不选甚至 NOT_SHIPPED(原因+数据+**代码去留登记**)」——把登记动作写进设计条款；④ 全文用分号连缀的压缩长句（`:14`、`:18`、`:25` 单行 200–400 字），标准01 §4「自然语言把设计讲清楚」不满足。机械列 `commit_sha=[]` 正确，`task_ids=[]` ⇒ **漏报** 05 §N / ISA- / ABI- / BENCH- 全套编号 |
| 悬空引用 | **`:6`「控制包 05 全条目(§1–§7)」不可直达**：跟踪集中 `工程控制/` 只有 `工程控制/RELEASE-05/`（`00_README.md`、`ACCEPTANCE.md`、`GAP_AUDIT.md`、`HANDOFF.md`、`OPEN_QUESTIONS.md`），无「§1–§7」架构条目面；且按 `AGENTS.md` §7，`工程控制/` 是「控制包（收口后按 CONTROL_PACK_SPEC §9 清理）」——**一个会被清理的过程目录被当作权威来源**。`:53`「BENCH-005(doctor)+**09 打包**」的「09 打包」同样只在该控制包内有意义。无文件级硬悬空（机械列 `dangling_links=[]` 正确） |
| 与上位冲突 | ① **权威来源错位**：`:6` 声明约束项与"控制包 05"完全一致并以其为权威，而标准01 §1 规定权威链只有最高设计→详细设计→具体设计，且 `ASTROCS_DESIGN.md:31`「本文档与任何其他文档冲突时，以本文档为准」⇒ 下层文档不得另立来源；② `:18` ④「进程可用 CPU=affinity∩cgroup∩Job Object」与设计 §9 `:673` 逐字一致 ✓；③ `:10` 变体集合与设计 §9 `:671` 一致 ✓；④ `:35` 「计算中失败→安全中止整个 stage,禁静默换 backend 混合结果」与设计 §9 `:677` 「计算后端失败安全中止整个阶段，不混用两种后端结果」一致 ✓；⑤ 索引 status 标 `ACTIVE_NORMATIVE` 与其自述 `状态: FROZEN` 并存——**同一文档两个状态轴**（索引侧字段注释明说「**不是** §12.5 交付状态阶梯」，而正文的 `FROZEN` 也不是），读者会误读 ⇒ 记为状态词跨轴串用（前言 §5 判据 6） |
| 处置建议 | **合并（拆解到各自主本）＋去元信息**：① §3 六查并入 `docs/architecture/cpu/CPU_001_CAPABILITY_PROBE.md`、§4 ABI 面并入 `docs/standards/C_ABI_STANDARD.md` 或 `docs/architecture/abi/`、§5 kernel 七类并入 `docs/standards/BENCHMARK_STANDARD.md`，本文只留"后端架构 = 变体策略＋隔离＋回退"的架构决策一页（依据：标准01 §2 同主题一份正本＋§1 一条权威链）；② 删 `:5-6` 元信息块与「控制包 05 权威来源」声明，全部小节标题去「(05 §N)」；③ `:43-53`「与 V5 任务的映射」表整段删除（任务映射属 `docs/traceability/` 面）；④ 长句改写为分条陈述 |

### 1.21 `docs/architecture/DATA_FLOW.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/DATA_FLOW.md` |
| 标题 | Data Flow |
| 行数 | 49 |
| 角色 | 架构设计；目录一致（数据流按标准01 §2 正属「架构设计：模块如何划分、**数据如何流动**」）⇒ **本主题的应属正本** |
| 主题 | Phase1/Phase2 节点级数据流、阶段间交换与数据契约指针 |
| 上游 | `:3` 标 §8 ✓；`:5` 声明「本文按最高设计 §4.2（normalize 节点流程）/ §5.2（mosaic 固定科学流程）/ §5.5（逐像素排异）/ §8.1-§8.2（阶段内命名块内存管线与块生命周期）描述」——**四条全部正确**（`:254`/`:371`/`:417`/`:544`+`:571`）⇒ 本批上游标注最规范的一份 |
| 下游 | 4 个跟踪引用者：`docs/architecture/ARCHITECTURE.md`（`:65` 文档关联表）、`docs/architecture/observability/RUN_GRAPH_CONTRACT.md`、`docs/audit/doc_classification.csv`、`docs/DOCUMENT_INDEX.yaml` |
| 是否正本 | **是**：三阶段数据流的正本（比 `ARCHITECTURE.md:20-24` 更完整且与上位同序） |
| 重复或重叠对象 | ① 与同批 `ARCHITECTURE.md:20-24` 的 Phase1/2/3 链构成**同层两份数据流**（差异与错向见 1.15 的「重复或重叠对象 A」——那份顺序错、这份对）；② `:36-38` 与 `:44-45` 在同页两处复述排异档位表落位（`:36-37` 还**抄了档界本身**「`1≤N≤3` none / `4≤N≤5` percentile / `N≥6` winsorized」，而 `:45` 自己声明「**只引用，不复制**」）⇒ 自述与内容冲突，并与 `12_rejection.md:64-68`、`docs/science/REJECTION.md:62-65`、`docs/modules/phase2_rej.md:188-189` 合成同一张表的四处承载（详见 1.3） |
| 元信息块 | 无（`:5` 是描述范围声明，承载实质信息） |
| 写法违规 | ① `:25`「当前生产实现仍用磁盘 JSON/FITS 传递，属**现行设计缺口**（生产节点覆盖率 **1/20**、相邻节点传块 **0**）」——缺口台账＋两个未标出处的计数进架构正文（标准01 §2 过程台账不放正式文档层；`ASTROCS_DESIGN.md` §12.5 状态现场计算）；② `:37`「winsorized（**M3**：原 `N≥16` linear fit 档**改投**）」——内部里程碑代号 M3 ＋「原…改投」历史叙事（标准01 §4），机械列 `task_ids=[]`/`history_narrative=[]` ⇒ **漏报**（"改投"与 M3 都不在词表）；③ `:49`「见 docs/contracts/DATA_SEMANTICS.md 与 docs/TRACEABILITY.csv DATA-* 行」——通配行选择器，不可核。 |
| 悬空引用 | `:24`「（最高设计 **§8.1:502** / **§8.2:526-527**）」——**行号锚全部失效**：实测 `ASTROCS_DESIGN.md`（826 行）里 §8.1 起于 `:544`、§8.2 起于 `:571`；`:502` 落在 §7.2 退出码表内（`| 3 | 输入缺失/格式错 |`），`:526-527` 落在 §7.3 的「运行日志（硬要求）」标题附近 ⇒ 两处按行号指向的是**无关条款**。⇒ 下层文档对最高设计使用行号锚（标准01 §4 只允许"公式/行号"放下层，但行号锚指向会随上位增删漂移的对象＝结构性缺陷）。`:31`「admit（兼容性校验）」——`admit` 在设计 §5.2 `:375` 的流程图节点名中存在 ✓（非文件引用）。无文件级悬空（机械列 `dangling_links=[]` 正确） |
| 与上位冲突 | ① `:12-19` 与 `:36-38` 本身与设计 §4.2 `:271`、§5.5 `:422` 取值一致 ✓（这是本批少数"文档与上位同值"的数据流面）；② 但 `:24` 用「§8.1:502 / §8.2:526-527」引用"不落中间文件"——该句实际在 §8.1 `:549`「每个阶段内部是一条内存管线…不落中间文件」与 §8.2 `:573-574` ⇒ **条款号对、行号错**，等价于给了一个会指歪的坐标；③ `:37` 与 `:45` 自相矛盾（一处复制档界、一处声明只引用）；④ `:49` 的 `docs/TRACEABILITY.csv` 存在且确以 `DATA-` 开头的行命中（实测 5 行），但仓库里另有 `docs/traceability/TRACEABILITY_MATRIX.csv`（带 `requirement_type` 列的宽表），两份追踪面的分工本文未写 ⇒ 不是悬空，是**可直达性不精确**（标准01 §3）。 |
| 处置建议 | **保留为数据流正本 ＋ 订正 ＋ 要求 `ARCHITECTURE.md` §3 并入**：① 删行号锚，改「§8.1（总原则）／§8.2（命名块内存管线）」（依据：标准01 §3 双向索引按条款，不按行号）；② `:36-38` 的档界文字删除、只留 `12_rejection.md §9` 指针，与自己 `:45` 的"只引用"一致；③ `:25` 的缺口与两个计数迁到 `docs/KNOWN_LIMITATIONS.md`（该文件正是登记此类缺口的现行载体，见 1.6/1.7 的断链登记先例）；④ 推动 `ARCHITECTURE.md:20-24` 删除并指向本文（见 1.15 处置①）；⑤ `:49` 明确写全两份追踪文件的真实分工 |

<!-- PROGRESS: 已审 21/25 -->

### 1.22 `docs/architecture/DEPENDENCY_RULES.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/DEPENDENCY_RULES.md` |
| 标题 | Dependency Rules |
| 行数 | 16 |
| 角色 | 架构设计；目录一致 |
| 主题 | 模块依赖方向、去重单源、写出位置约束、冻结件、脚本面约束 |
| 上游 | `:3` 标 §8 ✓ 存在。**依赖方向的正本条款是 §8.4 `:658`「依赖方向：命令行 → 阶段调度器/管线 → 注册表 → 模块 → 计算后端，单向」——本文未引到，且给出的是一套不同的中心（astro_image_io）** |
| 下游 | **2 个跟踪引用者**：`docs/audit/doc_classification.csv`、`docs/DOCUMENT_INDEX.yaml` ⇒ 本批唯一接近孤儿的文档（有上游标注、无任何活文档/代码引用）。判定命令：`git grep -l --fixed-strings DEPENDENCY_RULES.md`（剔除 `meta_block=False` 的机械列假阴性） |
| 是否正本 | 名义上是依赖规则正本，实际**不与任何实现或门绑定**（无 `eng/**` 引用它，对比 `BUILD_GRAPH.md` 有门、`EXECUTION_MODEL.md` 有 `check_execution_contracts.py`） |
| 重复或重叠对象 | ① `:10-12` 的 ACR 不进生产与 `ARCHITECTURE.md:13`、`BUILD_GRAPH.md:99-100/:108-109`、`EXECUTION_MODEL.md:7-9` 构成同一约束第五至第八处；② `:8`「实现只有该一份（B4-01 去重，DRZ-01）」与 `ARCHITECTURE.md:58`「healpix_core/sha256 单源（B4-01）」同一条；③ `:5-6`「唯一 I/O 依赖方向…astro_image_io」与 `ARCHITECTURE.md:26/:58` 同词同错（见 1.15 悬空栏）与 `17_aio.md:7`、设计 §10 `:686` 的 `aio` 口径构成**同一角色的两个名字** |
| 元信息块 | 无 |
| 写法违规 | ① `:8`「（**B4-01** 去重，**DRZ-01**）」、`:15`「healpix_stack 为冻结件，改动一律走变更流程（依赖**已归档** healpix_io.dll，不重建）」——内部编号＋"已归档"历史叙事（标准01 §4）；② `:13`「orchestrator 依赖所有模块头文件，通过 DllLoader 动态加载 DLL（不静态链接）」以现在时陈述一个已被上位与同层文档判为「非入口/迁移冻结」的对象（`ARCHITECTURE.md:12`、`BUILD_GRAPH.md:83` 登记 target `orchestrator_legacy_cli` 为工具面），而最高设计全文 **0 次**提到 orchestrator（实测 `grep -c "orchestrator" ASTROCS_DESIGN.md` = 0）⇒ 既非现行设计也未标退役，属**状态词跨轴混用**（前言 §5 判据 6：`未实现`≠`未接线`≠`已退役`）；③ 全文 16 行、8 条无一处指向具体门或代码锚（除 B4-01 类编号）⇒ 不满足标准01 §3「每份文档能追溯到最高设计的一条要点」的实质要求 |
| 悬空引用 | `:5-6` 与 `:9` 的 `astro_image_io` 作为依赖目标——**跟踪集中无该模块源码**（`git ls-files lib/astro_image_io` = 0；磁盘上同名目录是本机残留，不入库），现行名 `lib/infrastructure/aio/`（设计 §8.4 `:631`、本批 `17_aio.md`）⇒ 指向已被改名/迁移对象且未随迁。`:15` 的 `healpix_stack`/`healpix_io.dll`：跟踪集中 `healpix_io` 只以源码与归档说明存在（`lib/infrastructure/aio/healpix_db/healpix_io/ARCHIVED.md`、`include/aio_healpix_io.h`、`src/healpix/aio_healpix_io.cpp`），**无 `.dll` 交付物**⇒ 依赖声明的形态与仓库不符。`:16` 的 `NON_PRODUCTION_TOOL_ONLY` 标记：跟踪集内仅 3 处命中（本文、`docs/modules/star_detector.md`、`eng/tools/assemble_v17_review_pkg.py`），**无门注册**（`eng/ci/checks.json` 0 命中）⇒ 规则不可判红。机械列 `dangling_links=[]` ⇒ 全部漏报 |
| 与上位冲突 | ① **`:14`「模块写出位置限于 testdata/run」与上位直接矛盾**：设计 §10 `:696`「产品与**运行日志**只落块级 `output_dir`…`run/` 只放开发/CI 过程产物与过程日志」＋§7.2 `:513`「运行产物只落配置的 `output_dir`」；本批 `ARCHITECTURE.md:41` 更明文纠正「产品只落块级 `output_dir`（最高设计 §9）；`run/` **只放**临时产物与日志（**不是**「唯一运行输出目录」）」，且同句 `:57` 规定「testdata/ **只读**」⇒ 本文 `:14` 同时把禁写面（run）写成唯一落点、把只读面（testdata）写成可写落点，**与该句自身引用的上位全部相反**。这是本批后果最重的一条与上位冲突（若按本文实现即违反硬禁令）；② `:5-9` 的依赖中心与设计 §8.4 `:658` 的单向链（CLI→调度器/管线→注册表→模块→后端）不是同一套轴 ⇒ 结构口径不一致；③ `:11-12` 的 ACR `DORMANT` 结论与设计 §1.3 `:98`、§13 `:813` 一致 ✓（仅"§8 规定"的归因错，见 1.17 同类）。 |
| 处置建议 | **保留（不删）＋按上位重写约束条目**：① `:14` 立即改为「写出位置 = 块级 `output_dir`（唯一产品落点）；`run/` 只放开发/CI 过程产物与过程日志；`testdata/` 只读」并引设计 §10 `:696`（依据：标准01 §1「下位与上位冲突时以上位为准；冲突本身是缺陷」＋AGENTS §6 硬禁令「不把运行产物散落根目录」）；② `:5-6/:9` 的 `astro_image_io` → `aio`，并把中心从"模块名"改为设计 §8.4 `:658` 的单向链；③ `:13` 的 orchestrator 条目改标"工具面（非入口）"或删除（依设计 §0.3 `:61`）；④ `:15` 的「healpix_io.dll」改为实际的归档源码落点 `lib/infrastructure/aio/healpix_db/healpix_io/ARCHIVED.md`；⑤ `:8` 的 B4-01/DRZ-01、`:16` 的裸标记补登记面或删除；⑥ 因本文件无下游、无门绑定，若 ②③④ 之后只剩上位已有的句子，则按标准01 §2「同一角色内同一主题只有一份正本」并入 `docs/architecture/ARCHITECTURE.md`（其 `:57-59` 不变量节已是同一内容的另一份）并删除本文 |

### 1.23 `docs/architecture/ERROR_MODEL.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/ERROR_MODEL.md` |
| 标题 | Error Model |
| 行数 | 54 |
| 角色 | 架构设计；目录一致（错误模型属"接口契约/架构"面） |
| 主题 | 错误类别、阶段 ID、可恢复状态、退出码唯一源、契约 ID 族 |
| 上游 | `:3` 标 §8 ✓。`:26`/`:34` 两处标「最高设计 **§6.3**」——**错**：§6.3 是「投影算法」（`:451`），退出码表在 §7.2（`:493`，表体 `:498-510`）⇒ 同一错号两处 |
| 下游 | 11 个跟踪引用者：`docs/TRACEABILITY.csv`、`docs/architecture/ARCHITECTURE.md`（`:45`「唯一模型 ERROR_MODEL.md」）、`docs/architecture/EXECUTION_MODEL.md`（`:77`）、`docs/modules/orchestrator.md`、`eng/tools/docs_machine_consistency.py`、`eng/tools/quality/v19r3_traceability.py`、`lib/include/astrocs/abi/status_codes.h`、`lib/infrastructure/gaia_xpsd_client/src/gaia_client.h`、`lib/infrastructure/pipeline/orchestrator/cpp/include/orchestrator.h` 等 |
| 是否正本 | **退出码数值面的引用者，不是正本**：`:26` 自己写明「退出码唯一源 = `lib/infrastructure/cli/exit_codes.h`」——正确的分层 ✓；错误**类别**与**阶段 ID** 面它是唯一载体 |
| 重复或重叠对象 | ① `:24-34` 退出码块与 `18_cli.md:49/:53`、`19_runtime.md:89-92`、`17_aio.md:51`、`21_observability.md:45-46` 构成**同一套 11 码在 6 处承载**；取值全部一致（实测 `lib/infrastructure/cli/exit_codes.h:8-18` 逐名逐值对得上：OK=0/ARGS=2/INPUT=3/SCIENCE=4/BACKEND=5/COMPUTE=6/IO=7/INTEGRITY=8/CANCELLED=9/RESOURCE=10/INTERNAL=70）⇒ 复制式一致，无一份声明"只引用"；② `:10-15` 阶段 ID 与 `:47-48` 阶段 stage IDs **同页两处重复**（后者是前者的子集加 `P2.*`）；③ `:19` 的 C API 0/非 0 语义与 `CPU_BACKEND_ARCH.md:23`、设计 §8.5 `:664` 重叠 |
| 元信息块 | 无 |
| 写法违规 | ① `:8`「…/ INTERNAL（**DIAGNOSTICS_STANDARD**）」——裸文档简称无路径（见悬空栏③）；② `:22`「每个 high-risk error → troubleshooting 条目（**docs/diagnostics/**）」——目录级引用，未给条目命名规则；③ `:52`「ERR-* 族（**S2 注册**，含 ERR-P2-UPM-001 畸形模型）」、`:50` 节标题「契约」——`S2` 不可解析（见 1.18 同类）。机械列 `dates=[]`/`task_ids=[]`/`commit_sha=[]` 正确 |
| 悬空引用 | ① `:54`「ERR-P2-UPM-001 见 `lib/algorithms/coverage/src/upm.cpp:~890`」——**两层失效**：该文件确实存在，但 `upm.cpp:~890` 附近是求解器内部逻辑，真正的「frames 存在/数组/无重复/类型非法一律拒绝」校验在 `upm.cpp:25-26` 的注释与 open 路径里；且 **`ERR-P2-UPM-001` 这个字符串在 `lib/` 内 0 命中**（`git grep -c ERR-P2-UPM-001 -- lib` 空；命中面是 `eng/tools/quality/v19r3_traceability.py`（5 次，登记表）与文档/证据），即"错误码有文档与登记，无发出点"⇒ 带 `~` 的行锚本身就是不精确锚。② `:8` 的 `DIAGNOSTICS_STANDARD`——跟踪集中无此文档名；实测十类（`CONFIG/INPUT_CORRUPT/DEPENDENCY/NUMERIC/NO_DATA/RESOURCE/TIMEOUT/IO/SCIENCE_GATE/INTERNAL`）落在 **`docs/standards/ERROR_HANDLING_STANDARD.md`**（该文件同时含 `INPUT_CORRUPT` 与 `SCIENCE_GATE`），而 `docs/standards/` 下的实际文件名是 `ERROR_HANDLING_STANDARD.md` 与 `LOGGING_DIAGNOSTICS_STANDARD.md` ⇒ 简称指错/已改名未随迁。③ `:36`「JSONL error.numeric_code，20-29」但 `:38-43` 列的是 20–28 共 9 项 + 100 ⇒ 区间与列举不符（`29` 无定义）。机械列 `dangling_links=[]` ⇒ 三条全漏报 |
| 与上位冲突 | ① `:26`/`:34` 的 §6.3 错指（退出码在 §7.2）；且 `:34` 写「（最高设计 §6.3：码值与含义只有一份）」——把一个**不存在的上位表述**当作唯一性来源引用（§6.3 全文是投影算法，无此句）；② `:36` 的 20-29 与 `ASTROCS_DESIGN.md` §7.2 退出码表（0–10、70）**不在同一轴**，本文已正确标为「非进程退出码」✓（未串轴，记为合规）；③ `:20-21` 的可恢复状态枚举与 `docs/contracts/DATA_SEMANTICS.md` 的 status 词表一致性未证（本文不给指针）⇒ 可恢复状态面存在第二载体风险；④ 与 `17_aio.md:51`「I/O 错误 → exit 7」、`18_cli.md:53` 全部同值 ✓ |
| 处置建议 | **保留 + 就地订正**：① `:26/:34` 的 §6.3→§7.2，并把唯一性归因改为设计 §7.2 的实际措辞；② `:8` 的简称改 `docs/standards/ERROR_HANDLING_STANDARD.md`；③ `:54` 改为「错误码文档 ID `ERR-P2-UPM-001`（现行无实现发出点；校验逻辑见 `lib/algorithms/coverage/src/upm.cpp` 的 open 强校验注释 `:25-26`）」——按前言 §5 判据 6 如实分档，不得写"稳定报错"；④ 删 `:47-48`（`:10-15` 的重复）；⑤ `:36` 的区间改 20–28 或补 29 的定义；⑥ `:52` 的「S2 注册」补真实登记表路径；⑦ 其余五处退出码副本（17/18/19/21 各一处）统一改为「码值取 `exit_codes.h`，本文件不复制表」 |

### 1.24 `docs/architecture/EXECUTION_MODEL.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/EXECUTION_MODEL.md` |
| 标题 | Execution & Lifetime Model (ARC-EXEC) |
| 行数 | 102 |
| 角色 | 架构设计；目录一致。**§96-101「在役生产/CI 面」与 §2 的机器判据名属「标准与检查」角色** ⇒ 局部角色错放 |
| 主题 | 串/并行分层、异步 I/O 与 ACR、锁与 I/O 串行、取消/超时、CPU/GPU 驻留与回退、确定性、错误传播、ARC-EXEC 契约映射 |
| 上游 | `:3` 标 §8 ✓。`:9-12` 的括号归因有错（见与上位冲突①）。`:34`/`:62`/`:77`/`:88` 反复写「（最高设计 §8）」——ACR 不进生产的真实条款是 §1.3 `:98` 与 §13 `:813`，**§8 全文不含 ACR** |
| 下游 | 10 个跟踪引用者：`docs/architecture/THREADING_MODEL.md`、`docs/architecture/THREAD_BUDGET_ARCH.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/contracts/PUBLIC_API.md`、`eng/ci/checks.json`、`eng/tools/quality/contracts/check_execution_contracts.py` 及其 fixtures、`memory.md`、`docs/DOCUMENT_INDEX.yaml` ⇒ **有门绑定**（`check_execution_contracts.py`），是本批机器化程度最高的架构文档之一 |
| 是否正本 | 执行/生命周期与 DORMANT 分列面的正本（`check_execution_contracts.py` 以它为判据来源） |
| 重复或重叠对象 | ① `:31` 与 `ASYNC_IO_CONTRACT.md`（本批 1.16）——同一 I/O 合同两处描述，但 `:31` 带指针「合同见 [ASYNC_IO_CONTRACT.md]」⇒ 合法分层 ✓；② `:32`「HiPS read 并发只读（无进程级锁）…每个 `fitsfile*` 为单线程私有…并发安全由 cfitsio `_REENTRANT` 构建保证…机器判据 `check_execution_contracts.py::EXEC-AIO-READ-NO-GLOBAL-LOCK`」与 `ASYNC_IO_CONTRACT.md:28/:83` 的「cfitsio 同一句柄不保证并发读安全」**结论方向相反**（前者：可并发只读、无全局锁；后者：不保证安全、故 per-worker 句柄）⇒ 需按代码/门定案；③ `:23/:33/:60/:61/:62/:77/:88` 七处「~~划掉~~ **DORMANT** …原行：…」＝在同一张表里保留旧内容并划掉——重复＋历史叙事双重（标准01 §4） |
| 元信息块 | **有**：`:5`「`> 关联: ARC-EXEC-001..00N  模块: phase2/acr  状态: FROZEN`」——ID 串＋状态＋"00N"占位（`00N` 是未展开的枚举，形同模板残留）。机械列 `meta_block=False` ⇒ **漏报** |
| 写法违规 | ① `:5` 元信息块（含 `ARC-EXEC-001..00N` 这种未定长占位）；② `:7-12` 以 ⚠ 前言规定"一律标 DORMANT"，随后表内 7 处以删除线保留原内容并加「原行：acr thread / per-tile chunk (px) / auto / Dispatcher::decide / mixed merge / `acr_kernels.cpp`」——**留痕写法**（标准01 §4 禁「留痕/曾/原 xx」，本条正中「原行」二字）；③ `:11`「**orchestrator 标「历史保留」**」——"历史保留"是历史叙事标签；④ `:96-101`「## 在役生产/CI 面（状态词取自 DORMANT 面之外）…⇒ **在役生产**…**在役 CI 面**」——交付状态写进文档（设计 §12.5 `:801`「状态由检查与验收现场计算，登记表不预写状态」）；⑤ `:18`「最大并发 16」「`calibrator.cpp: OpenMP 16`」、`:20`「`sampler.cpp:924-954`」——源码行号进架构正文（标准01 §4 允许行号放"对应下层文档"，本文件是架构层，行号应落 `docs/algorithms/` 或模块 README）；⑥ `:100`「由 `eng/tools/v6/check_v6_runtime_closure.py` 调用其 `selftest`」——把 CI 工具调用链写进架构文档。机械列 `task_ids=[]`、`history_narrative=[]` ⇒ **漏报** ②③（"原行"/"历史保留"不在词表） |
| 悬空引用 | `:10-11`「**§7.1/§10.1**：HiPS Browser 不进产品 manifest」与「orchestrator 标「历史保留」（**最高设计 §7.1：接入后删除**）」——三处均不可达：① §10 无子节；② §7.1 未提 browser；③ **最高设计全文 0 次出现 orchestrator**（实测 `grep -c` = 0），故"§7.1：接入后删除"是被引为一个不存在的上位条款。`:32` 的 `cfileio.c:1544`（裸文件名，实为 `lib/infrastructure/aio/third_party/cfitsio/cfileio.c`，该文件在跟踪集内 ✓ 但行号未核）；`:18` `calibrator.cpp`、`:20` `sampler.cpp:924-954`、`:22` `rejection.cpp/integrate.cpp`、`:23` `acr_kernels.cpp` 全为裸文件名。机械列 `dangling_links=[]` ⇒ §10.1 与"§7.1：接入后删除"两条**虚构上位引文**全部漏报（它只判文件路径） |
| 与上位冲突 | ① `:10-12` 的三处虚构/错指条款（见悬空栏），其中「最高设计 §7.1：接入后删除」是把不存在的条文当权威引用，性质最重；② `:32` 与 `ASYNC_IO_CONTRACT.md:28/:83` 关于 cfitsio 读并发可安全性的**相反结论**同时存在于两份本批文档 ⇒ 二者至少一份错（需以 `check_execution_contracts.py::EXEC-AIO-READ-NO-GLOBAL-LOCK` 与代码定案；本文自述有机器判据，故更可能是 1.16 一侧口径过保守）；③ `:34`/`:62` 的"生产 fallback 只有一条：无 cpu_profile → baseline + 动态 worker"与设计 §7.1 `:491`、§9 `:673` 一致 ✓；④ `:68` 的浮点求和顺序固定与设计 §9 `:677` 一致 ✓ |
| 处置建议 | **保留（有门绑定，不可删）＋三项硬订正**：① `:10-12` 的三处上位归因改为真实条款（§1.3 `:99`、§8.4 `:636`），并删除「最高设计 §7.1：接入后删除」这一不存在引文；② 删除全部「原行：…」留痕，改为「本行为工具面/休眠面，生产表不含此项」的正向条款（标准01 §4）；③ `:96-101` 的在役状态段迁往 `docs/owner/` 或 `docs/ci/`（依 §12.5 状态现场计算）；④ `:32` 与 `ASYNC_IO_CONTRACT.md:28` 的读并发结论二选一，由 `check_execution_contracts.py` 现读现判并让另一侧改引用；⑤ `:5` 元信息块删除，`00N` 占位改实际枚举 |

### 1.25 `docs/architecture/IO_AND_ATOMICITY.md`

| 字段 | 登记 |
|---|---|
| 路径 | `docs/architecture/IO_AND_ATOMICITY.md` |
| 标题 | I/O & Atomicity |
| 行数 | 50 |
| 角色 | 架构设计；目录一致。**§29-46「非生产 / 诊断接口登记」是符号级台账**，属「标准与检查」/`docs/algorithms/` 实现锚面 ⇒ 局部角色错放 |
| 主题 | 写盘原子协议、两个未闭合缺口的登记、dense cache 协议、aio 穷举点之外的块↔文件读写面分级 |
| 上游 | `:3` 标 §8 ✓。**但正文 `:6`、`:9`、`:11`、`:19`、`:27`、`:31` 共 6 处把原子性与落点归给「最高设计 §9」——§9 是「CPU 后端与资源」；实际条款是 §10（`:684`，其中 `:688` 是原子发布链、`:696` 是落点）**⇒ 同一错号在本文件出现 6 次（与 1.15 的 2 次、1.7 的 1 次同族） |
| 下游 | 13 个跟踪引用者：`docs/TRACEABILITY.csv`、`docs/architecture/ARCHITECTURE.md`（`:40`「输出原子落盘（tmp+rename，IO_AND_ATOMICITY.md）」）、`docs/architecture/EXECUTION_MODEL.md`（`:92`）、`docs/modules/astro_image_io.md`、`eng/tools/arch/build_production_execution_inventory.py`、`eng/tools/quality/update_audit_status.py`、`lib/infrastructure/aio/src/hiss_stream_writer.{h,cpp}` 等 |
| 是否正本 | 「块↔文件非生产接口」分级的正本（`:45`「**生产性分级以本表为准**」）；原子协议面不是正本（正本＝设计 §10 `:688`） |
| 重复或重叠对象 | ① 原子协议 `:11`「temp write → validate → atomic promote」＝设计 §10 `:688` 五步链（私有临时区→校验→fsync→算哈希→原子改名→完成清单）的**缩减版**（本文缺 fsync/哈希/完成清单三步，而 `:14` 讨论 tile 时又要求「临时区 → 校验 → 哈希 → 原子改名」）⇒ 同文件内两版协议，均短于上位；② `:22-23` dense cache 描述与 `CACHE_POLICY.md:13` 冲突点已在 1.18 记录（校验对象 model_hash vs checksum 两说）；③ `:26-27` 落点句与 `ARCHITECTURE.md:35/:41/:57`、`17_aio.md:36`、`21_observability.md:35` 同句第六至第九次 |
| 元信息块 | `:5-9` 的 ⚠ 前言（承载实质约束，不判元信息块）。机械列 `meta_block=False` 与此一致 |
| 写法违规 | ① `:12`「aio_upm_write_sparse **已于 V19R6R2** 改为 temp+rename（…，**F-V19R2-IO-001 已修复**）」——轮次号 V19R6R2＋发现编号＋「已修复」完成态，三重历史叙事（标准01 §4）；② `:13`、`:20` 两处「⚠ **待修缺口登记（R13，未闭合）**」——台账条目进正式文档层（标准01 §2「过程台账…需要留证的台账落 `artifacts/evidence/` 并由正本引用」）；③ `:18`「**未闭合期间**本行保持「缺口」语义：HiPS tile 原子发布的文档表述**限于缺口闭合之后**」——文档为自身的不完整表述设定生效条件，属元规则（标准01 §1「任何文档不声明自己的第二套权威顺序」）；④ `:19`「（最高设计 §9「**本期例外（如实登记）**」）」——把带引号的"上位原文"写出来；⑤ `:32`「**已降级**为非生产 / 诊断接口」、`:42`「**旧名**包装」——降级/旧名留痕语域。机械列 `dates=[]`/`task_ids=[]`/`history_narrative=[]` 全空 ⇒ ①②③⑤ **五条漏报** |
| 悬空引用 | ① `:19` 的「最高设计 §9『本期例外（如实登记）』」——**该句在最高设计中不存在**：`grep -c "本期例外" ASTROCS_DESIGN.md` = 0，`grep -c "如实登记" ASTROCS_DESIGN.md` = 0 ⇒ **虚构上位引文**（比错号更重：它伪造了一段权威文本的措辞）；② `:12` 行锚 `lib/infrastructure/aio/src/aio_upm.cpp:60-97`——漂移：函数 `aio_upm_write_sparse` 起于 `:66`，temp 写注释在 `:85`，`std::rename` 提升在 `:101`（重试分支 `:105`）⇒ 所引区间**止于关键动作之前**；③ `:33` 行锚 `lib/infrastructure/aio/include/aio_pipeline.h:205-247`——区间内只有 `aio_frame_save_cache`（`:232`），而表内 `aio_frame_export_all_xml`（`:259`）、`aio_pipeline_export_xml`（`:264`）**落在区间外** ⇒ 区间不足，6 个符号仅 1 个可直达；④ `:21`「`docs/modules/registry/astrocs.phase2.write.md` §已知限制」——该文件在跟踪集内 ✓（`docs/modules/registry/` 有 27 份）；⑤ `:50`「ENG-IO-001..003（S2 注册）」同 1.18/1.19 的 `S2` 不可解析 |
| 与上位冲突 | ① 6 处「§9」错指（原子性与落点均在 §10）＋ `:19` 的**虚构上位原文**；② **`:6-7`「原子性覆盖全部产品（含 HiPS tile）是最高设计 §9 的强制条款」与设计 §10 `:688` 一致 ✓，但本文 `:13-19` 同时声明该强制条款在 tile 上未实现** ⇒ 文档把一个上位强制项的违反登记为"待修缺口"并以「『原子发布已全覆盖』以缺口闭合为条件」自设表述生效期——按标准01 §1「冲突本身是缺陷，需在整治中消除」，此处应上呈而非就地豁免；③ `:11` 的三步协议短于设计 `:688` 的五步链（缺 fsync/哈希/完成清单）；④ `:31`「最高设计 §9 把 aio 定为文件级唯一 I/O 边界，并**穷举**允许的文件读写点」——穷举句实在 §10 `:686`，本文的"穷举"表述与设计一致 ✓（仅条款号错） |
| 处置建议 | **拆分**：① `:13-21` 的两条「待修缺口登记」**迁出**到 `docs/KNOWN_LIMITATIONS.md`（本批已有 `15_resample`/`16_fits_output` 断链在该文件登记的先例，同一处置路径）；② `:29-46` 的符号分级表**迁往** `docs/algorithms/`（模块实现锚面）或与 `docs/architecture/api_inventory.csv` 合并为一张带"生产性分级"列的登记面（该 CSV 现为签名唯一登记处），依标准01 §2 角色表；③ 剩余协议段（`:11`/`:22-27`）只留一句正向条款＋指向设计 §10 `:688/:696`，并把 6 处 §9→§10、删除 `:19` 的虚构引文与 `:12` 的轮次/发现号；④ `:12/:33` 两处行锚按实测修正（`:66-:110`、`:205-:270`）；⑤ 与 `CACHE_POLICY.md:13` 的 dense cache 校验口径合一 |

<!-- PROGRESS: 已审 25/25 -->

## 2 同主题组与差异

| # | 主题 | 本批各份（路径:行） | 别批同题对象 | 差异与后果 | 归并方向 |
|---|---|---|---|---|---|
| G-1 | **逐像素排异档位表（档界＋算法名）** | `12_rejection.md:64-68`；`DATA_FLOW.md:36-37` | `docs/science/REJECTION.md:62-65`（带全部参数与逐 profile 表）、`:100-103`（对照档）；`docs/modules/phase2_rej.md:188-189`；上位 `ASTROCS_DESIGN.md:422-423` | 五份载体、取值一致；**只有科学层带参数与 profile 差异**，插件层与架构层无 profile 轴 ⇒ 读插件/架构层会误判对照档 `N≤3` 行为 | 顶层指认（§5.5→12_rejection）与标准01 §1（科学权威在 `docs/science/`）**互斥**：要么改指认、要么上收；上呈 |
| G-2 | **可辨识性判决（`H_red`/`τ=rank_rtol`）** | `11_upm.md:95-119`；`10_sampling.md:61`（复述＋引用） | `docs/science/PHASE2_UPM.md:199/217/228/381`、`:134`（明文「口径正本 = 11_upm.md」）；`docs/contracts/DATA_SEMANTICS.md:2224/2237/2254` | 判定式同值；**科学层把自己的口径正本写给第三层**＝层级倒置；符号一名两写（`n_free` vs `n_params`） | 上收到 `docs/science/PHASE2_UPM.md`，插件层留工序；`n_params` 为唯一键名 |
| G-3 | **control_ivar 权重式** | `10_sampling.md:40/:47` | `docs/science/PHASE2_UPM.md:20-21`；`docs/algorithms/PHASE2_SAMPLER.md:248`；`docs/contracts/DATA_SEMANTICS.md:1764`（含 Cramér 1946 一手引文）；`docs/modules/phase2_samp.md:54` | 五处同值；但 `10_sampling.md:47` 把推导指给 **`docs/science/CONTROL_WEIGHT_SNR.md`（该文件不含此式，0 命中）** ⇒ 指错权威 | 指针改指 `PHASE2_UPM.md`+`PHASE2_SAMPLER.md`；插件层删公式 |
| G-4 | **CPU 后端 / 加载前六查 / ABI 必含字段 / kernel 分类** | `CPU_BACKEND_ARCH.md:18/:25/:30` | `docs/architecture/cpu/CPU_001_CAPABILITY_PROBE.md:27`、`cpu/CPU_003_AVX2_PROVIDER.md`、`docs/architecture/abi/ABI_003_SECURE_LOADER.md`、`docs/standards/C_ABI_STANDARD.md`、`docs/standards/BENCHMARK_STANDARD.md`、`docs/architecture/ISA_VARIANTS.md`、`ISA_BIT_MANIP_VARIANTS.md` | 同一角色目录内 8 份，六查 ①②③⑥ 两处同名同序 ⇒ 无一份自称正本但各持片段 | `CPU_BACKEND_ARCH.md` 拆回各主本（见 1.20） |
| G-5 | **资源门（G-RES-01）与磁盘门／exit 10** | `21_observability.md:50-149`（语义正本，有合同 JSON 指名）；`19_runtime.md:61/:68/:91`（把 G-RES-01 称作磁盘门） | `docs/standards/`、`docs/ci/`（门登记面）；`eng/contracts/resource_gate_v1.json`（数值源，`authority` 指向本文）；`ASTROCS_DESIGN.md:357`（§4.5 只管磁盘） | **同一门 ID 两份 infra 文档指两件事**；7 条判据在文档与 JSON 两处（值一致）；上位无一条索引指向 `docs/plugins/infrastructure/` | 语义上收 `docs/standards/` 或由上位加索引；两份 infra 文档的门 ID 用词统一 |
| G-6 | **Phase3 resample** | `15_resample.md:37`（把权威名推给 `docs/algorithms/PHASE3_RESAMPLE.md`） | `docs/algorithms/PHASE3_RESAMPLE.md` **与** `docs/algorithms/PHASE3_RSMP_IMPL.md` 并存，另 `docs/modules/phase3_rsmp.md` | 插件层下游指向一个有孪生的目标，不知哪份是推导正本（别批问题，本批只登记受害面） | 待算法层归一后本批指针随之改 |
| G-7 | **兼容性／版本／内部修订号出现面** | `COMPATIBILITY_POLICY.md:11/:16/:20/:25-26`；`ASYNC_IO_CONTRACT.md:1`（CON-008）；`ARCHITECTURE.md:1`（V5）；`CPU_BACKEND_ARCH.md:5/:43`；`EXECUTION_MODEL.md:5` | `docs/VERSIONING.md`、`docs/owner/RELEASE_STATUS.md`、`docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md` | 本批 4 份把 V5/V19R6R2/W2/ARC-EXEC-00N 写进标题或正文，而 `COMPATIBILITY_POLICY.md:25-26` 自己规定轮次号只进版本说明/ADR ⇒ **自相违反** | 轮次/版本锚从正文与标题撤出，统一由 `docs/VERSIONING.md` 承载 |
| G-8 | **架构总览（两份，合法分工）** | `docs/architecture/ARCHITECTURE.md` | `docs/owner/ARCHITECTURE_OVERVIEW.md`（别批，117 行，负责人面状态汇总） | 角色不同（架构设计 vs 负责人面），**不判重复**；仅提示后续目标架构须保留这条分工 | 无需动作 |

## 3 索引与链接缺陷清单（与 `docs/DOCUMENT_INDEX.yaml` 核对）

索引条目实测口径：`docs/DOCUMENT_INDEX.yaml` 的 `status` 枚举为 `{ACTIVE_NORMATIVE, ACTIVE_INFORMATIVE, GENERATED, ARCHIVED_NON_NORMATIVE}`（文件头字段注释），`GENERATED` 在全文被使用 24 次。

| 类型 | 条目 | 说明与命令 |
|---|---|---|
| 未登记 | 无 | 25/25 全部在 active 区段登记（逐条 `path:` 命中，见 1.1–1.25 各栏）。命令：`python3 -B` 扫 `path: "docs/..."` 与清单求交，差集为空 |
| 登记了但文件不存在 | 无（本批范围内） | `git ls-files docs/plugins docs/architecture` 与索引 path 逐条比对，本批 25 条全部对得上 |
| 状态标注与实际生成方式不符 | `docs/architecture/BUILD_GRAPH.md` → `status: ACTIVE_NORMATIVE` | 该文 `:5-9` 自述机器块由 `eng/tools/arch/gen_build_graph_doc.py` 导出、`eng/tools/contracts/.../check_build_graph.py` 判；同目录三份生成物（`PRODUCTION_EXECUTION_INVENTORY.csv`、`api_inventory.csv`、`execution_inventory.csv`）已标 `GENERATED` ⇒ **同一生成物两类标法** |
| 上游标注与文档抬头不一致 | `21_observability.md`（索引 `§8.1`；文档 `:3` 是 `§7.3`＋`§8.1`）、`18_cli.md`（索引 `§7.1、§7.2`；文档 `:3` 多一条 `§7.3`） | 索引漏登条款；且索引照抄了文档的错标题「§8.1（顶层结构）」 |
| 上游标注照抄错误条款号（索引面扩散） | `19_runtime.md`、`21_observability.md`、`22_gaia_xpsd_client.md`、`23_hips_browser.md` 四条索引 upstream 均为「§8.1（顶层结构）」 | 顶层结构是 §8.4；索引作为"唯一索引地图"把四个错号固化成登记值（标准01 §3）。命令：`grep -n "§8.1（顶层结构）" docs/DOCUMENT_INDEX.yaml` |
| **条款级悬空（系统性，非个案）** | 见下 6 类 | 本批 25 份里 **12 份**存在上位条款号/行号错指；共同根因是最高设计章节重排（§5→§6 投影、§9→§10 I/O、§6.3→§7.2 退出码、§8.1→§8.4 顶层结构、§4.5 写成 §3.5）后**只改了部分文档** |
| ①  §9 ≡ I/O 原子产品（实为 §10） | `IO_AND_ATOMICITY.md:6/:9/:11/:19/:27/:31`（6 处）、`ARCHITECTURE.md:41/:57`、`16_fits_output.md:12` | 9 处；`17_aio.md:3` 与 `20_benchmark.md:3` 用的是正确号 ⇒ 同族文档号不一致 |
| ②  §6.3 ≡ 退出码（实为 §7.2） | `ERROR_MODEL.md:26/:34`（含被引为"码值与含义只有一份"的**不存在的上位原句**）；`eng/contracts/resource_gate_v1.json` 的 `disk_gate.authority` 亦写 §6.3 | 文档错号已扩散进**机器合同** |
| ③  §5.3 ≡ 投影算法（实为 §6.3） | `14_projection.md:12/:25`；`docs/algorithms/PHASE3_PROJ_IMPL.md:399`（别批，标题即写「DESIGN §5.3」） | 说明该错号在算法层同样未随迁 |
| ④  §8.1 ≡ 顶层结构（实为 §8.4） | `17_aio.md:13`、`19_runtime.md:3/:12/:117`、`21_observability.md:3`、`22_gaia_xpsd_client.md:3/:12`、`23_hips_browser.md:3/:12` | 9 处；其中 `19_runtime.md:8` 还把「scheduler+pipeline 模块名唯一」归给 §7.1（命令树） |
| ⑤  §3.5 / §9.74 / §10.1 不存在 | `21_observability.md:101`（§3.5、§9.74）；`CACHE_POLICY.md:7/:16`、`EXECUTION_MODEL.md:10-11`（§10.1） | §3 只有 3.1–3.3；§9 无子节；§10 无子节 ⇒ 三个不可解析条款号 |
| ⑥  行号锚指向最高设计 | `DATA_FLOW.md:24`「§8.1:502 / §8.2:526-527」 | 实测 §8.1 在 `:544`、§8.2 在 `:571`；`:502` 落在 §7.2 退出码表、`:526-527` 落在 §7.3 日志节 ⇒ 行号全失效 |
| 虚构上位引文（最重一类） | `IO_AND_ATOMICITY.md:19`「最高设计 §9『本期例外（如实登记）』」；`EXECUTION_MODEL.md:11-12`「最高设计 §7.1：接入后删除」 | 两条被引文字面都不存在：`grep -c "本期例外" ASTROCS_DESIGN.md`=0、`grep -c "如实登记"`=0、`grep -c "orchestrator"`=0（连被引对象都不在设计里） |
| 从未存在的合同 schema（10 处，本批） | `control_points`(10:24)、`upm_output`(11:23)、`rejection_output`(12:19)、`mosaic_product`(13:31)、`export_product`(15:20)、`fits_product`(16:29)、`cli_output`＋`events`(18:20)、`catalog_query`(22:20)、`hips_product`(23:19) | 跟踪集 0 命中**且 `git log --all` 0 条**⇒ 不是改名不是删除，是从未存在；真实文件在 `eng/contracts/schemas/`（20 份，名字不重合）与 `schemas/unified/`（16 份）。其中**仅 2 处已被登记**（`docs/KNOWN_LIMITATIONS.md` 第 39 条点了 `export_product`、`fits_product`）⇒ 其余 8 处未登记 |
| 已改名对象的旧接口名 | `12_rejection.md:11/:73-74` 与 `13_integration.md:12`：`docs/interfaces/io/CON-001_PIPELINE_MODEL.md`、`CON-002_EXECUTION_OPTIONS.md`、`CON-003_FITS_IO.md` | `ls docs/interfaces/io/` 实测现名族为 `IO_001_FITS_STREAM_INTERFACE.md`/`IO_002_HIPS_INPUT_INTERFACE.md`/`IO_003_ATOMIC_OUTPUT_PUBLISH.md` ⇒ `CON-00x`→`IO_00x` 改名未随迁（4 处引用全悬空） |
| 符号级悬空 | `14_projection.md:26` `registry_find`、`:21` `Spec`；`21_observability.md` 未列（其 `missing_required_extension_v1` 实测存在于 `lib/infrastructure/cli/protocol.h:69`） | `git grep` 在 `lib/algorithms/projection/` 内 0 命中；真实导出 `registry_frozen_set()`（`p3_proj_v6.cpp:386`） |
| 源码行锚漂移 | `CACHE_POLICY.md:15`（`spherical_overlap.h:303`→实 `:321/:323`）、`IO_AND_ATOMICITY.md:12`（`aio_upm.cpp:60-97`→实 `:66–:110`）、`IO_AND_ATOMICITY.md:33`（`aio_pipeline.h:205-247`→区间只含表内 6 符号中 1 个）、`11_upm.md:134`（`stage2_common.h:24`→实 `:29`；所附 `CONFIG_SCHEMA.md:19`→实 `:33`） | 逐条 `sed -n '<行>p'` 实测；机械列**全部漏报**（只判文件级存在性） |
| 目录级通配当引用 | `17_aio.md:20`（`eng/contracts/schemas/*.schema.json`）、`19_runtime.md:20`（`run_*.schema.json`）、`18_cli.md:13`（`phase_config*.schema.json`）、`13_integration.md:12`（`docs/interfaces/api/*`）、`19_runtime.md:13`（`docs/design/*_DETAILED_DESIGN.md`） | 其中 `run_*.schema.json` 只解析出 1 份、`docs/interfaces/` 下无 `api/`/`observability/`/`scheduler/`/`execution/` 子目录（`ls docs/interfaces` 只有 `data/`、`io/`）⇒ 通配掩盖缺口 |
| 按 ID 引用不可直达 | `11_upm.md:129` `FZ-AP2S-RANK-RTOL`、`13_integration.md:37` 三个 `FZ-*`、`CPU_BACKEND_ARCH.md:5/:47-53` 全套 ISA-/ABI-/BENCH-、`ERROR_MODEL.md:52` 与 `CACHE_POLICY.md:26` 与 `IO_AND_ATOMICITY.md:50` 的「S2 注册」、`21_observability.md:48`「LOG-001 §5」、`ASYNC_IO_CONTRACT.md:1/:85` CON-004/006/008、`COMPATIBILITY_POLICY.md:9`「DATA-UPM-MODEL-001」 | 这些 ID 族在 `eng/contracts/data/v6_clause_registry_v1.json`、`docs/contracts/INDEX.yaml`、`docs/standards/DOCUMENTATION_STANDARD.md`（S8）等处有登记，但**被引处一律不给登记表路径** ⇒ 违反标准01 §3 双向可追溯。「S2」在跟踪集中无可解析定义面 |

## 4 覆盖率自报

- 已审 **25 / 25**（清单 `独立审计/批次清单/DB-04.txt` 全部 25 行）。**未逐行读名单：空。**
- 读入规模（命令：`python3 -B` 逐份 `len(open(f).read().splitlines())` 求和）：25 份合计 **1990 行**；最长三份依次为 `11_upm.md` 189、`21_observability.md` 172、`19_runtime.md` 124，均逐行读毕。
- 本批 25 份全部同时具备：跟踪集存在、索引登记存在、抬头「上游」标注存在（**无一份缺上游**，11 份 `docs/architecture/**` 与 14 份 `docs/plugins/**` 均在 `:3` 行标注）。
- 孤儿判定：**`docs/architecture/DEPENDENCY_RULES.md` 是最接近孤儿的一份**（仅 2 个跟踪引用者，且都不是设计/文档/代码消费方：`docs/audit/doc_classification.csv` 与 `docs/DOCUMENT_INDEX.yaml`）。它**不是**孤儿（有上游标注），但属"无消费者文档"。其余 24 份引用者数（剔除自身与 DOCUMENT_INDEX 后的跟踪引用者计数，命令同 §1 各栏）：最少 `23_hips_browser.md` 4，其次 `15_resample.md`、`20_benchmark.md`、`22_gaia_xpsd_client.md` 各 5，最多 `ARCHITECTURE.md` 28。

## 5 我证伪了任务书的哪个前提

任务书（`tasks/AUD-101_文档现状全量审计.md`）在本批范围内可核的前提与派单口径，逐条复算：

1. **前提「以 `docs/` 为重点（当前约 233 份、30+ 子目录）」——`.md` 份数未证伪，"30+ 子目录"取决于口径。** 固定命令与实测：`git ls-files docs | wc -l` = **258**（跟踪文件总数）；`git ls-files docs | sed -E 's/.*\.([a-z]+)$/\1/' | sort | uniq -c` = md **233**、csv 12、json 8、yaml 3、py 2 ⇒ 「约 233 份」若指 `*.md` 则**准确**（不是约数，是精确值），若指"文档总份数"则少计 25 份非 md 载体；「30+ 子目录」按**含跟踪文件的最深层目录数**（`git ls-files docs | sed -E 's#/[^/]+$##' | sort -u | wc -l`）= **36** 成立，按**`docs/` 下一层目录数**（`ls -d docs/*/ | wc -l`）= **23** 不成立 ⇒ 任务书未标口径，两种读法差 13。
2. **前提「清单是 `docs/plugins/` 与 `docs/modules/` 交界」——本批派单口径被证伪。** 清单 25 行里 `docs/modules/**` **0 行**，实际交界对是 `docs/plugins/**`（14）×`docs/architecture/**`（11）；我按前言 §3 以清单为准，并把「工序规范 vs 模块工作细节」的真正交界（`docs/plugins/algorithms_phase2/*` 对 `docs/modules/phase2_*.md`）作为**别批证据**处理（只读、只在下钻结论处引用）。
3. **本批内部前提证伪一处（机械列校准，见前言 §4 第 5 条）**：`doc_mechanical.csv` 对 `docs/architecture/**` 的 `meta_block` 全列为 `False`，而 `ARCHITECTURE.md:5`、`CPU_BACKEND_ARCH.md:5`、`EXECUTION_MODEL.md:5` 三份**确有**「`> ID: … 状态: FROZEN 上游: … 下游: …`」型元信息块——该列只认 `---` YAML 头，**漏报 3/3** ⇒ 凡按机械列筛"无元信息块"的下游动作在这三份上会失效。

## 6 需上呈负责人的裁决项（按前言 §5 判据 6 与 AGENTS §10）

1. **G-1 排异档位表的权威层**：`ASTROCS_DESIGN.md:422` 指名插件文档为唯一正本，标准01 §1 指名 `docs/science/` 为公式与算法唯一权威，而 `docs/science/REJECTION.md:62-103` 实际承载更全的表（含参数与逐 profile）。二者不可同真 ⇒ 请裁：改指认，或上收科学层。
2. **G-2/G-3 权重与可辨识性口径**：`docs/science/PHASE2_UPM.md:134` 把「口径正本」写给 `docs/plugins/.../11_upm.md`，构成层级倒置（标准01 §1/§2）。
3. **G-5 资源门语义落插件层**：`eng/contracts/resource_gate_v1.json#authority` 指向 `21_observability.md §8`，但最高设计对 `docs/plugins/infrastructure/` 零索引（实测 `grep -n "docs/plugins" ASTROCS_DESIGN.md` 命中的全是 phase1/2/3 六条）⇒ 一个决定发布判定的门的语义权威落在第三层且无上位索引。
4. **`ASYNC_IO_CONTRACT.md` 与 `EXECUTION_MODEL.md` 的 cfitsio 读并发结论相反**（§28/§83 vs §32），后者自称有机器判据 ⇒ 请定哪份现行；并裁 `io_workers` 与最高设计 §9 `:674`「异步 I/O 一个专用线程」是否构成上位偏差。
5. **`IO_AND_ATOMICITY.md:13-21` 两条未闭合缺口**：一个上位强制条款（§10 `:688` 覆盖含 HiPS tile）在实现上未达，文档自设"表述生效条件"。按标准01 §1「冲突本身是缺陷，需在整治中消除」与 AGENTS §6「不以豁免盖红」⇒ 该缺口的文档承载位置需裁（建议 `docs/KNOWN_LIMITATIONS.md`）。
6. **10 处从未存在的 schema 引用**（本批）：`docs/KNOWN_LIMITATIONS.md` 第 39 条已给出"补 schema 与改引用二选一，且改判据属门禁面变更——待裁决"的口径 ⇒ 请对全部 10 处（不止已登记的 2 处）定一个方向。

## 7 收工核对

`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`（收工，`git rev-parse --short HEAD` = `c8f64e9a` 不变）：

```text
?? "ACSD\346\225\264\346\262\273\345\267\245\344\275\234\345\214\205_AUDIT-06.zip"
?? _measure_copy.py
?? site/
```

前两项与开工是**同一集合**（同一 zip 名，收工时因未再设 `core.quotePath=false` 而以八进制转义显示，非新增项）；`git diff --stat` 空 ⇒ **零跟踪文件改动**。第三条 `_measure_copy.py` 是收工核对期间由**并行进程**落入的新未跟踪项（观测到一次、随后一次核对又消失，见 §7.1），非本审计动作污染；本条正是前言 §1「出现任何新增项即登记」想要抓住的形态，但按 porcelain 单条判据无法区分自己弄脏与并行弄脏 ⇒ 判据缺口见 §7.2。

### 7.1 本会话的一次违规动作与自纠（按前言 §1 末句登记）

- **收工第三次核对出现的第二个新增项（非本会话所造，登记备查）**：末次 `git status --porcelain` 除两项基线项外另出 `?? _measure_copy.py`（`ls -la --time-style=full-iso` 实测 mtime `2026-09-25 15:40:28`，本会话开工与收工前两次核对均未出现该项）。判明归属的证据：该文件正文引用 `site/src/pages/{Home,Guide,Downloads}.tsx` 并 `sys.stdout.reconfigure(encoding="utf-8")`，是官网计量脚本；本会话**从未打开、写入或引用 `site/**`**，也未写过任何 `.py` 到仓库（§0 已列明全部用过的命令，无重定向、无脚本落地）。⇒ 结论：同一工作树内**并行第三方进程**产生，不是本审计动作污染。
- **动作**：为让中文路径在检索结果里可读，中途执行过一次 `git config core.quotePath false`——这写进了 `.git/config`，违反前言 §1「零 git 写」与硬禁令「NEVER update the git config」。**发现后立即停用该动作**并执行 `git config --unset core.quotePath`。
- **复原自证**（porcelain 不足以证明未脏，故另取一命令）：`git config --local --list` 现输出止于 `core.repositoryformatversion/filemode/bare/logallrefupdates/ignorecase`、`remote.origin.*`、`branch.main.*`，**无 `quotepath` 项**；`.git/config` 不在跟踪集内 ⇒ 配置类改动不会出现在 `status --porcelain`。**建议后续批次把 `git config --local --list` 列入开工/收工双查**（本批 §0 只查了 porcelain，是判据面的缺口，不是执行面的豁免）。
- **未使用**：构建、`ctest`、`run_checks.py`（任何参数）、任何 `eng/**` 脚本执行、任何 `import`/`importlib`；对 `eng/**` 只做文本读取（`resource_gate_v1.json`、`dead_config_keys.json`、`schemas/*` 的属性名计数、`checks.json` 计数）。
- **临时件**：分段落盘用的 `_db04_seg2..6.md` 写在审查目录 `独立审计/证据/` 并已逐个 `rm`；本批唯一产出件是 `独立审计/证据/AUD-101-DB-04.md`。

### 7.2 对后续批次的判据面建议（一句话，不展开）

前言 §1 只要求两次 `status --porcelain`，**覆盖不到 `.git/config` 与非跟踪写入**；建议共享派单前言改为：开工/收工各跑 `git status --porcelain` **加** `git config --local --list`，并规定「需要改 git 显示参数时改用命令级 `git -c core.quotePath=false …`，不落配置」。本批的自纠过程已给出可复制的两条命令。

<!-- PROGRESS: 已审 25/25（批次完成） -->
