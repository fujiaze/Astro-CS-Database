# 通读 CR-44 — platesolve IPv 三生产实现文件

- 基线 HEAD：`c8f64e9a`
- 仓库：`F:\Astro dev\Astro CS Normalization Database`
- 派单：DISPATCH-CODE-READ-PREAMBLE.md §0–§5 全条为硬约束
- 开工 `git status --porcelain` 原文：
  ```
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```
- 独占清单（3 个生产实现文件，合计 3072 行）：
  - `lib/algorithms/platesolve/cpp/ipv/src/ipv_sip.cpp`（516 行） —— 已读：否
  - `lib/algorithms/platesolve/cpp/ipv/src/ipv_solver.cpp`（1819 行） —— 已读：否
  - `lib/algorithms/platesolve/cpp/ipv/src/ipv_triangle.cpp`（737 行） —— 已读：否

<!-- PROGRESS: 3/3 -->

---

## 1 · `lib/algorithms/platesolve/cpp/ipv/src/ipv_sip.cpp` —— 已读：是（表 24 项）

全文 516 行已从头读完（1–516）。生产可达性核实：`fit_sip` 的唯一生产侧调用点是
`ipv_wcs.cpp:165`（`build_wcs` 内，固定传 `order=3, logger=nullptr`），而 `build_wcs` 在
跟踪集内的实调用只有 `lib/algorithms/platesolve/cpp/ipv/test/test_synthetic.cpp:277`
（检索式：`git grep -n "build_wcs *(\|build_wcs(" -- 'lib/**' 'eng/tests/**'`）。
`docs/algorithms/PLATESOLVE.md:292-295`（DISP-WCS-003）明文把 `fit_sip` 判为"非生产"。
⇒ **本文件的 finding 一律写不出生产触发路径，按派单前言 §3 硬门槛全部封顶 S2。**

### 1.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| ipv_sip.cpp:45 | `buf[1024]` | 1024 | 日志格式化栈缓冲 | 结构性不适用 | 非科学量：只截断日志文本，`vsnprintf` 带 `sizeof(buf)` 界，不进任何计算 | — | CONFIRMED |
| ipv_sip.cpp:95,110,140 | `PIVOT_EPS` | 1e-12 | 高斯消元主元绝对下界，小于即判奇异 | 待确认 | 注释写"PLATESOLVE.md 数值风险"——**该节在 PLATESOLVE.md 不存在**（`git grep -n "数值风险" -- docs/algorithms/PLATESOLVE.md` 零命中）；真实条文 §9 容差来源(:183-205) 也未列此项 | 保守方向：**调大**（改为相对判据 `max_val < ε·‖A‖`）。现值为绝对界，而矩阵元素量级 ~ n·x^{i+j} 随内点数增长 ⇒ 对大规模设计阵等价于"永不判奇异"，偏绿；影响范围 = 本文件两个 `return false` 奇异出口全部失效 | CONFIRMED |
| ipv_sip.cpp:125 | `factor == 0.0` | 0.0 | 消元跳过零因子行 | 结构性不适用 | 精确零比较是短路优化，不是容差；不影响结果 | — | CONFIRMED |
| ipv_sip.cpp:171 | `w(n, 1.0)` | 1.0 | IRLS 初始权重（首步退化为 OLS） | 结构性不适用 | Huber IRLS 的标准起点，非科学量；由 Huber 1964 方法本身规定 | — | CONFIRMED |
| ipv_sip.cpp:237-240 | `n % 2`、`abs_r[n/2]`、`0.5*(…)` | 2 / 0.5 | 偶数长度取两中间值均值 | 文献值 | MAD 的中位数定义（顺序统计量定义，非标定量）；PLATESOLVE.md:198 已声明适用域 | — | CONFIRMED |
| ipv_sip.cpp:242 | Huber 常数 | 1.345 | δ = 1.345·MAD(\|r\|) 的尺度系数 | 文献值 | Huber 1964, Ann. Math. Statist. 35, 73（DOI 10.1214/aoms/1177703732），95% 高斯渐近效率；本仓登记面 `PLATESOLVE.md:193-199`、`docs/science/ASTROMETRY.md:259`、`docs/references/SCIENTIFIC_REFERENCES.md:119` 同值 | 适用域已由 §9:198 限定："要求残差近似对称"；本文件残差模型不保证对称 | CONFIRMED |
| ipv_sip.cpp:244 | δ 下限 | 1e-9 | 防 δ=0 的保护值 | 待确认 | 条文面零提及（检索式：`git grep -n "1e-9\|delta" -- docs/algorithms/PLATESOLVE.md` 零命中） | 保守方向：**调小**。抬高 δ 只会让更多点权重大于应有值 ⇒ 偏绿（少剔外点）；但实测此支路只在 MAD<7.5e-10 px 触发，该域内 δ 已远小于任何真实外点残差 ⇒ 影响范围限于"零畸变+含粗差"这一少见组合 | PARTIAL（未实跑验证触发域） |
| ipv_sip.cpp:282,483,494 | `36` / 步长 `6` | 36, 6 | `A[36]`、`B[36]` 与 `idx=i*6+j` 存储布局 | 结构性不适用 | 6×6 系数阵 ⇒ 可容纳阶数 ≤5 的二维多项式；`SIPCoeffs` POD 布局由 `ipv_sip.h`/`ipv_api.h:39-61` 固定，不是科学量 | — | CONFIRMED |
| ipv_sip.cpp:292,374 | 最少内点数 | 7（两处同值） | 少于 7 对直接返回 order=0 | 待确认 | 条文面零提及。注释自述"2 阶 6 系数…至少 7 个点留余量"⇒ 由 K+1 推出，但代码同时允许 order=4（K=15）却仍只要求 7 点，最小二乘欠定 | 保守方向：**改为随 eff_order 取 K+1（order=4 时 ≥16）**。现值对 order≥3 允许欠定拟合 ⇒ 偏绿（出"看似成功"的 order）；影响范围 = 本文件全部 order≥2 出口 | CONFIRMED |
| ipv_sip.cpp:302 | 最低请求阶 | 2 | `order<2` 即返回 order=0 | 结构性不适用 | 一阶以下无畸变语义；SIP 拟合从 2 阶起是本文件自设的定义域下界 | — | CONFIRMED |
| ipv_sip.cpp:312 | 降阶门 1 | n_pairs<12 → order 2 | 内点少时降阶 | 待确认 | 条文面零提及（检索式 `git grep -n "n_pairs" -- docs/algorithms/PLATESOLVE.md` 只命中 :317-321 的 **F1 测试门 n_pairs≥12**，属测试判据、非拟合降阶规则） | 保守方向：**调大门槛**（12→按 K+1）。现值与 order=3 的 K=10 只留 2 自由度，稳健性不足 ⇒ 偏绿；影响范围 = 12~29 内点档全部解 | PARTIAL（未验证该档实际拟合质量） |
| ipv_sip.cpp:317 | 降阶门 2 | n_pairs<30 → order 2 | 同上第二档 | 待确认 | 条文面零提及；且与 :312 互斥同结果（`else if`），30 这个数无任何推导 | 保守方向：删除该档或给出 K 自由度依据。影响范围 = 12~29 内点档的降阶语义 | CONFIRMED（该值无出处这件事本身） |
| ipv_sip.cpp:322 | 降阶门 3 | n_pairs<60 → order≤3 | 注释自称"保守" | 待确认 | 条文面零提及；60 与 `ipv_solver.cpp` 的 `n_target=60` 同值但**无任何注释建立关系**，是巧合还是继承不可判 | 保守方向：明确它与 `img_n_target` 的关系后重定。影响范围 = 仅当 caller 传 order≥4 才生效，当前唯一 caller 传 3 ⇒ 实际不触发 | PARTIAL（60 与 n_target 是否同源未证） |
| ipv_sip.cpp:328 | 阶数上界 | 4 | `eff_order` 硬上限 | 待确认 | **与上位条文不一致**：`PLATESOLVE.md:37` 伪代码写 "sip = build_sip(trans) **order 2-3**"。FITS SIP 标准侧本仓只核到 bibcode 级（`ASTROMETRY.md:259`"未逐页核验"），阶数上界无任何一手依据 | 保守方向：**收紧到 3**（与 :37 一致）。放宽一侧的影响：4 阶需 15 系数、要求自由度最多，且 `A[i*6+j]` 布局可容 5 阶 ⇒ 上限写死为 4 而无式导出 | CONFIRMED（与 :37 不一致这件事） |
| ipv_sip.cpp:333-335 | `cx`,`cy` | w/2.0, h/2.0 | 归一化中心 | 公式导出（但代码未按式算） | `ASTROMETRY.md:99-105` §5a 冻结：`u = det_x − w/2 = p − CRPIX` 即 Paper I 的 q ⇒ 归一化中心应取 **u 的原点**，代码却 `+cx` 回到 det_x（见 CR-44-1） | 保守方向：改为 `u.x/scale`。当前写法把拟合变量平移一整幅半宽 ⇒ 低阶系数系统性错值 | CONFIRMED |
| ipv_sip.cpp:335 | `scale` | max(w,h)/2.0 | 坐标归一化尺度 | 待确认 | 条文面零提及归一化尺度取法（检索式：`git grep -n "scale\|归一化" -- docs/science/ASTROMETRY.md docs/algorithms/PLATESOLVE.md` 无归一化尺度条目）；:485 的换算式又完全依赖它 | 保守方向：无原则性偏好，但**必须与 :485 的换算式同基准**并写入条文。影响范围 = 全部归一化系数与换算后系数 | PARTIAL |
| ipv_sip.cpp:336 | `scale <= 0.0` | 0.0 | 除零守卫 | 结构性不适用 | 配合 :297 的 w/h>0 检查，正常输入不触发 | — | CONFIRMED |
| ipv_sip.cpp:361-362 | `+ cx` / `+ cy` | — | 把中心坐标平移回绝对像素再归一化 | **公式导出且代码未按式算** | 应为 `u.x/scale`（`ASTROMETRY.md:72-73`"SIP 自变量 = 像素偏移 dPix = xp−CRPIX"、:99-105 同式）。见 CR-44-1 | 保守方向：删 `+cx/+cy`。影响范围 = A/B 全部非最高阶梯度系数 | CONFIRMED |
| ipv_sip.cpp:408 | `IRLS_MAX_ITER` | 15 | IRLS 迭代上限 | 实验标定→**不合格**；实为待确认 | `PLATESOLVE.md:200-203` 明文："IRLS 迭代上限 15、收敛 ε 1e-6… **阈值来源 UNJUSTIFIED**"，并加"该路径经 DISP-WCS-003 登记为非生产" | 保守方向：**调大**并配收敛证据。上限不足 ⇒ 未收敛即出系数并当成功返回（`irls_huber_fit` 无"未收敛"出口）⇒ 偏绿；影响范围 = 全部出关系数 | CONFIRMED（UNJUSTIFIED 为文档自述） |
| ipv_sip.cpp:409 | `IRLS_CONV_EPS` | 1e-6 | 收敛判据 max\|Δc\| 绝对阈值 | 待确认 | 同上 `PLATESOLVE.md:200-203` UNJUSTIFIED；且判据为**绝对**界而各阶系数量级跨若干数量级（deg=3 项归一化后 ≪ deg=0 项），无相对/尺度不变性 | 保守方向：改相对判据 `max\|Δc\|/\|c\|`。绝对界对高阶小系数偏严（提前 break）⇒ 少迭代；对常数项偏松。影响范围 = 迭代步数与最终权重 | CONFIRMED |
| ipv_sip.cpp:437 | 系数上界 | 100.0 | `max_coeff>100` 判拟合失败 | 待确认 | 条文面零提及（检索式：`git grep -n "max_coeff\|100\.0" -- docs/**` 零命中）。量纲是"像素/(归一化坐标^deg)"，对 deg=0…4 同一阈值无物理意义 | 保守方向：**按阶分别界**或改判"预测残差不超过 RMS 门"。现值对 deg≥2 过松（100 归一化单位在边缘像素上等于天文数字）、对 deg=0 过严；且它检的是**归一化**系数而非发布系数（见 CR-44-4） | CONFIRMED |
| ipv_sip.cpp:455-456 | `sum_w > 0.0 ? … : 0.0` | 0.0 | 权重和为零时 RMS 记 0 | 待确认（R 轴风险） | 条文面零提及。全部权重被 Huber 打到 0 时 RMS=0 ⇒ 小于 :459 的 10px 门 ⇒ **偏绿**：零权重退化的拟合以 rms=0 通过并出关 | 保守方向：`sum_w<=0` 应走 `return order=0` 而非 rms=0。影响范围 = 全部内点被判外点这一极端场景 | CONFIRMED（方向已钉死） |
| ipv_sip.cpp:459 | RMS 门 | 10.0 px | 拟合 RMS>10px 判失败 | 待确认 | 条文面零提及此值；本仓 RMS 类门在 `PLATESOLVE.md:317-318` 是 `rms_arcsec≤0.5″`（角秒量纲、且是 F1 **测试**门），与 10px 无可比关系 | 保守方向：**调小并按 s0 换算成角秒**。10px 在 s0=0.5″/px 时等于 5″，比 F1 门松 10 倍 ⇒ 偏绿；影响范围 = 劣质畸变拟合被放行 | PARTIAL（未与实跑 RMS 分布对比） |
| ipv_sip.cpp:485-487 | `scale_pow` 换算 | `coeff/scale^(i+j)` | 归一化系数 → 像素系数 | 公式导出但基准错 | 换算式本身正确（`coeff·xn^i·yn^j = (coeff/scale^{i+j})·X^i·Y^j`），**但 X 被取成绝对 det 坐标而非 (x−CRPIX)**，:471-477 的注释把两者混为一谈 ⇒ 见 CR-44-1 | 保守方向：见 :361 行 | CONFIRMED |

（本文件数值项已逐条列全：`36/6/1024/2/0.5/1.0/0.0/1e-12/1e-9/1.345/7/12/30/60/4/2/15/1e-6/100.0/10.0/cx/cy/scale/scale_pow`。）

### 1.2 findings

### CR-44-1 · `fit_sip` 把 SIP 自变量取成绝对探测像素坐标 `det_x`，却按 `(x−CRPIX)` 发布系数，等于把畸变场整体平移半幅画面
- 轴：C（代码／科学文档对同一量说的是两件事）；兼 S
- 位点：`lib/algorithms/platesolve/cpp/ipv/src/ipv_sip.cpp:361`
  ```cpp
  xn_arr.push_back((u.x + cx) / scale);
  ```
  与自述换算式 `:473-477`
  ```cpp
  // 但标准 WCS SIP 期望原始像素坐标 (x-CRPIX) 的系数
  // 转换: A_orig[i,j] = coeff_norm[k] / scale^(i+j)
  // 因为 dx_orig^i * dy_orig^j = (xn*scale)^i * (yn*scale)^j
  ```
- 上位依据：`docs/science/ASTROMETRY.md:72-73`「(ξ,η) = CD · [ (xp−CRPIX) + SIP_A/B(xp−CRPIX) ]；# SIP 自变量 = 像素偏移 dPix = xp−CRPIX (px)」；同文 `:99-105` §5a 冻结求解器口径「`u = det_x − w/2`（`ipv_select.cpp:943,947` 取 `cx = img_w/2.0`）…故 **`u = det_x − w/2 = p − CRPIX` 就是 Paper I §2.1.1 的 `q`**」；`PLATESOLVE.md:20` F4 注「SIP 自变量为像素偏移(px)」。一手文献 `Shupe et al. 2005 ASPC 347, 491 §A` 在本仓只到 bibcode 级（`ASTROMETRY.md:259`、`SCIENTIFIC_REFERENCES.md:119`"bibcode 级"）⇒ **我不引该论文原文，只引本仓对它的转述**，本条成立的依据是仓库自己的冻结条文。
- 现状：`u.x` 按 §5a 已是偏移量 `q`；代码先 `+ cx` 还原成绝对 `det_x` 再除以 `scale`，于是拟合变量是 `det_x/scale`。:486 的 `coeff/scale^deg` 只能把归一化绝对坐标换回**绝对坐标**的幂，得到的 `result.A[i*6+j]` 是 `det_x^i·det_y^j` 的系数。注释 `:475` 断言的等式 `dx_orig = xn·scale` 不成立：`dx_orig = p − CRPIX = u.x`，而 `xn·scale = u.x + cx`，两者差整 `cx`。
- 差在哪：发布出的 A/B 与 FITS SIP 模型 `Σ A_ij (x−CRPIX)^i (y−CRPIX)^j` 的自变量基不同，属**基变换缺失**（需要二项式平移重展开，而最高阶系数不变、低阶系数全变），不是"数值容差范围内的差异"。
- 后果：消费 `build_wcs` 输出的任何 SIP 前向畸变场被整体平移约半个画面宽度——在图像中心处取到的是像素 0 处的畸变值，在边缘取到中心的畸变值；`sip.order≥2` 时 A/B 的低阶项全错。对外主张面：`PLATESOLVE.md:210` 把 `ipv_wcs.h: build_wcs` 列为公开 API、`docs/TRACEABILITY.csv:21` 把 `build_wcs` 登记为 SCI-AST-001 实现锚、`docs/architecture/production_call_paths_stage1.csv:4` 更是把 `ipv_solve_from_detections_v1 / build_wcs` 并列写成 stage1.platesolve 的生产操作 ⇒ 若该登记面成立，产品 WCS 的 SIP 头就是错的。
- 定级：**S2**（封顶）。生产入口 `ipv_solve_from_detections_v1 → ipv_solver.cpp:1534 solve_post_select → :1437 extract_wcs_sip` 走的是 `PLATESOLVE.md:244` 的解析路径，**不经过 `fit_sip`**；`fit_sip` 的实际可达调用只有 `test_synthetic.cpp:277`。缺生产运行时触发路径 ⇒ 按前言 §3 硬门槛不判 S1。风险登记给 CR-44-5（登记面自相矛盾）。
- 怎么算修好：① `:361-362` 改为 `u.x/scale`、`u.y/scale`（残差侧 `:369-370` 的 `w.x - x_pred` 同步确认 W 与 pred 同基准）；② 同批必须改 `:471-477` 注释，否则注释继续声称一个不成立的等式；③ 补一条 roundtrip 断言：用发布出的 A/B 在 `(p−CRPIX)` 上求值，与 `W − linear_pred` 逐内点比对（`PLATESOLVE.md:326-328` F2 已有 `|Δ|≤1e-4 px` 口径可借用）——**只改 ①不加 ③ 会判红**，因为现有 `test_synthetic` 只查 CD/CRVAL 不查 A/B。
- 置信：CONFIRMED（等式 `dx_orig = xn·scale` 与 §5a 的 `u = p − CRPIX` 直接代数矛盾，已独立复算：由 :361 得 `xn·scale = u.x + cx`，由 §5a 得 `dx_orig = u.x`）

### CR-44-2 · `eff_order` 上界写死 4，与上位伪代码条文"order 2-3"不一致，且无一手依据
- 轴：C（代码／算法文档不同数）
- 位点：`ipv_sip.cpp:328` `if (eff_order > 4) eff_order = 4;`
- 上位依据：`docs/algorithms/PLATESOLVE.md:37`「sip = build_sip(trans) **order 2-3**, IRLS 15× ε1e-6 Huber 1.345 (ipv_sip.cpp:238-261)」。SIP 标准侧：`docs/standards/STANDARDS_REGISTRY.md:42,54` 只登记「SIP §A（A/B/AP/BP 约定）」，未登记阶数上界；`docs/science/ASTROMETRY.md:259` 标「bibcode 级，未逐页核验」。走过的途径：`git grep -rn "Shupe" -- docs/**` → 5 命中，均为约定引用无阶数；`git grep -n "order" -- docs/science/ASTROMETRY.md` 无阶数上界条文。
- 现状：函数形参 `order` 无校验上界，只有 :328 的钳位；:387 `basis_table[15]` 与 `A[36]`（6×6，容 5 阶）都按 ≥4 阶预留。唯一 caller `ipv_wcs.cpp:165` 固定传 `3`，故 4 阶支路当前不可达。
- 差在哪：条文把该模块的产阶域钉在 2–3，实现把上界钉在 4，两处都无导出式；一旦 caller 改传 ≥4 就静默超出条文域（不报错、不降回 3）。
- 后果：`PLATESOLVE.md §3` 伪代码作为 ALG-WCS-001 的"离散公式"权威面失真；`sip.order` 若真出 4，`docs/algorithms/PLATESOLVE.md:247`（ctype 选择按 order 分档）与下游 drizzle 侧 `sip_order` 域（`lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.h:150` 注释 `0..4` 与 `hp_drizzle_api.cpp:103` 校验 `[0,5]`，属邻批位点，此处只作影响面引用）不同步。
- 定级：**S3**（当前不可达，无生产触发路径；纯条文/实现数值不一致）
- 怎么算修好：把上界与条文钉成一个来源——要么 :328 改成 `> 3`，要么在 `PLATESOLVE.md:37` 与 `STANDARDS_REGISTRY` 补"阶数上界=4 及其依据"；同批把 :387 的注释「最多 4 阶 = 15 项」改成指向该条文的锚，而不是自证。
- 置信：CONFIRMED（两处数值不一致已逐字核对；可达性核对见 :165）

### CR-44-3 · 三处数值注释把出处写成 `PLATESOLVE.md 数值风险`，而 PLATESOLVE.md 根本没有这一节
- 轴：C（依据锚指向不存在的条文）
- 位点：`ipv_sip.cpp:95` `const double PIVOT_EPS = 1e-12; // 奇异判定阈值 (PLATESOLVE.md 数值风险)`；同形态 `:242`、`:408`、`:409`（`:408` 行 `// IRLS 上限 (PLATESOLVE.md 数值风险)`、`:409` 行 `// IRLS 收敛 ε (PLATESOLVE.md 数值风险)`）
- 上位依据：`git grep -n "数值风险" -- docs/algorithms/PLATESOLVE.md` **零命中**（全仓该词只在 `docs/algorithms/HEALPIX_MAPPING.md:31` 是一节标题）。PLATESOLVE.md 的真实容差条文是 `§9 容差来源`（:183-205），其中 :200-203 对 `IRLS_MAX_ITER=15`／`IRLS_CONV_EPS=1e-6` 的原文是「**阈值来源 UNJUSTIFIED**；该路径经 DISP-WCS-003 登记为**非生产**…标定前的引用面 = 待标定项登记」，对 `1.345` 才是「**有文献依据**——Huber (1964)…95%」；对 `PIVOT_EPS=1e-12` **§9 零提及**。
- 现状：四个位点用同一个不存在的小节名充当出处；`ipv_solver.cpp:198-199` 的同款注释（见 §3）说明这不是孤立笔误而是模板。
- 差在哪：注释把"无出处的经验值"包装成"有权威条文背书的值"，并且把唯一一个真有文献的 `1.345` 与三个 UNJUSTIFIED 值混在同一类锚里，读代码者无法区分。
- 后果：AUD-402 的"逐个有处置标注"验收面被这些假锚污染——按注释去查条文会查不到，从而把 4 个值全部误判为"已核"；`PIVOT_EPS` 尤其危险，因为 §9 从未登记它，它是**唯一真正零条文**的那个。
- 定级：**S3**
- 怎么算修好：四处注释改指 `PLATESOLVE.md §9 容差来源` 并**照抄其结论**（15/1e-6 → "UNJUSTIFIED，非生产路径"；1.345 → "Huber 1964"）；`PIVOT_EPS=1e-12` 若保留现锚写法，须先在 §9 增设条目并按上面处置表给出的相对判据重述——**不得只改注释不改判据**。
- 置信：CONFIRMED

### CR-44-4 · "系数过大"兜底检的是归一化系数，发布出去的那组系数算出来了却从不判、也从不打印
- 轴：R（守卫只看错的东西）
- 位点：`ipv_sip.cpp:432-441`（`max_coeff` 由 `coeff_a/coeff_b` 取最大绝对值，`:437` `if (max_coeff > 100.0)`）与 `:493-497`
  ```cpp
  double max_coeff_orig = 0.0;
  for (int i = 0; i < 36; ++i) { … max_coeff_orig … }
  ```
  `max_coeff_orig` 在 :497 之后**再无一次引用**——`:499-501` 的 INFO 日志打印的是 `max_coeff`（转换前）。
- 上位依据：`PLATESOLVE.md:183-205` §9 未登记 `max_coeff` 门；`ipv_sip.cpp:9` 文件头把「5. 失败兜底 (奇异 / 系数过大 / RMS 过大)」列为该实现的第 5 步——条文面声明的兜底对象是**要发布的系数**（:467 起整节标题即"系数填充到 SIPCoeffs"）。
- 现状：兜底在 :437 于归一化空间执行完毕，:479-489 才做 `coeff/scale^deg` 换算；换算后的量只被 :493-497 统计一次然后丢弃。
- 差在哪：被守卫的量与实际交付的量不是同一个；:485 的换算在 `scale<1`（img 尺寸 1×1…2×2 时 `scale=max/2 ∈ [0.5,1)`）时**放大**系数最多 16 倍（deg=4），而这一步发生在守卫之后。
- 后果：`order=4` 且图像极小的组合下可发布超出 :437 意图的系数而无任何告警；对外主张面 = `:9` 文件头自述的"失败兜底"能力在此路径上未覆盖输出。
- 定级：**S3**（`scale<1` 需 img 尺寸 ≤2px，生产 `img_width/img_height` 来自真实帧，不可达；主要是守卫对象错位与死值）
- 怎么算修好：把 :437 的判据搬到 :489 之后按 `result.A/B` 判定（并按阶分别界，见处置表 :437 行），删掉或真正使用 `max_coeff_orig`（同时把它加进 :499 的日志格式串，别只留 `max_coeff`）；同批更新 `:9` 文件头对兜底对象的描述。
- 置信：CONFIRMED

### CR-44-5 · 登记面对 `build_wcs`/`fit_sip` 是否生产自相矛盾：`§9` 据"非生产"豁免了数值依据，`production_call_paths_stage1.csv` 却把它列为生产操作
- 轴：C（两处入口对同一事实说不同的话）
- 位点：`docs/algorithms/PLATESOLVE.md:292-295`「…与非生产 fit_sip IRLS+Huber（`ipv_sip.cpp:268`；irls_huber_fit :161-263…），后者仅被非生产 build_wcs（`ipv_wcs.cpp:157-165`，AP/BP 显式清零）消费」／`:201-203`「该路径经 DISP-WCS-003 登记为**非生产**…**本条只描述非生产实现，生产精度依据只取生产路径的实测值**」／对立面 `docs/architecture/production_call_paths_stage1.csv:4`：`Orchestrator::run_stage_platesolve,stage1.platesolve.enabled,ipv_solver,ipv_solve_from_detections_v1 / build_wcs,serial,platesolve.rms_px,…`
- 上位依据：以上两份皆本仓登记面；另 `docs/TRACEABILITY.csv:21` 把 `build_wcs` 与 `ipv_solve_from_detections_v1` **并列为 SCI-AST-001 的实现锚与"impl"列**，`PLATESOLVE.md:210` 把 `ipv_wcs.h: build_wcs` 列为对外 API。
- 现状：代码事实是 `build_wcs` 在跟踪集内只被 `test_synthetic.cpp:277` 调用（`git grep -n "build_wcs *(" -- 'lib/**' 'eng/tests/**'` ⇒ 命中仅 `ipv_wcs.cpp:117` 定义、`ipv_wcs.h:29` 声明、`test_synthetic.cpp:277` 调用与四处文档/工具），即 §11.3 的说法与代码一致；`production_call_paths_stage1.csv` 的说法与代码不一致。
- 差在哪：一个登记面用"非生产"为全部 UNJUSTIFIED 数值开脱（`§9:204-205`「标记 UNJUSTIFIED 的部分标定前的引用面 = 待标定项登记」），另一个登记面把同一批代码计入生产调用路径 ⇒ 该豁免的证据资格取决于哪份登记面为真，而两份都在跟踪面生效。
- 后果：若按 `production_call_paths_stage1.csv` 读，CR-44-1 的基错就落在生产产品字节上（S1）；若按 PLATESOLVE.md 读，`§9` 的"生产精度依据只取生产路径实测值"才成立。二者不能同时成立，且这个选择直接决定本批最高危条目的定级。
- 定级：**S2**（不改产品字节，但改"哪条主张成立"）
- 怎么算修好：以代码事实为准，把 `production_call_paths_stage1.csv:4` 的第 4 字段改为只列 `ipv_solve_from_detections_v1`；同批在 `TRACEABILITY.csv:21` 的 impl 列把 `build_wcs` 标注 DISP-WCS-003 非生产；**不要反过来把 PLATESOLVE.md 改成"生产"**——那会把 `§9` 全部 UNJUSTIFIED 条目变成欠标定债务，且需要一并给 `fit_sip` 补标定证据。
- 置信：CONFIRMED（代码可达性与两份登记面均已逐字核对）

### CR-44-6 · `fit_sip` 的基含 (1,0)/(0,1) 自由一次项，与仓内"SIP 须剔除单位线性/线性归 CD"的口径是否冲突拿不到一手依据
- 轴：S
- 位点：`ipv_sip.cpp:55-57` 基序表与 `:65-76 get_basis_table`（`deg=0` 与 `deg=1` 两项进设计矩阵），落到 `:486` 发布为 `result.A[1*6+0]`、`A[0*6+1]`
- 上位依据：本仓口径面 = `PLATESOLVE.md:19` F4「AP[1,0]-=1, BP[0,1]-=1」与 `ASTROMETRY.md:84`「约定 AP[1,0]-=1, BP[0,1]-=1（**剔除单位线性**）」——条文只对**逆向** AP/BP 规定剔除线性，对**正向** A/B 的 `A_10/A_01` 是否必须为零：`git grep -n "剔除\|单位线性" -- docs/science/ASTROMETRY.md docs/algorithms/PLATESOLVE.md` 除上述逆向条目外零命中；一手论文 Shupe et al. 2005 本仓只到 bibcode 级（`ASTROMETRY.md:259`），我**不凭记忆断言 §A 是否禁止一次项**。走过的途径：`docs/science/ASTROMETRY.md` §4/§5a、`docs/algorithms/PLATESOLVE.md` §2/§3/§11.1、`docs/standards/STANDARDS_REGISTRY.md:42-66`（SIP 条目）、`docs/references/SCIENTIFIC_REFERENCES.md:119`。
- 现状：线性项由 :365-366 的 `s·R(θ)·U + t` 承担，但相似变换不含剪切/斜，故 :411/:420 拟合出的**一次项承载的是 CD 未覆盖的线性失配**，被作为 SIP A/B 的一次项发布。
- 差在哪：若正向 A/B 的一次项非零被允许，则 `CD` 不再等于全线性项，任何把 CD 当一阶近似使用的消费方（drizzle 的 `cd` 路径）读到的尺度与真解差 `A_10`；若不允许，本实现缺一次项剔除步骤。两种读法都缺一手条文，无法判定实现错在哪一侧。
- 后果：待确认；影响的对外主张 = "CD 给像素→角秒线性比例"这一句话的适用范围。
- 定级：**S3 + UNPROVEN**（不给一手依据不定错）
- 怎么算修好：先在 `docs/science/ASTROMETRY.md` §5（SIP 形式）补一条明文冻结"正向 A/B 的一次项域"，再据此要么在 :479-489 填系数时跳过 deg≤1、要么给 CD 组装加一次项并同步 `extract_wcs_sip`。核对一手：需读 Shupe et al. 2005 §A 全文（本仓现只 bibcode 级），或由负责人裁决。
- 置信：UNPROVEN（缺 Shupe 2005 §A 逐页原文；已列走过的途径）

### 1.3 本文件"核过为合规、不报"的项（附检索式）
- `get_basis_table` 项数与 `basis_table[15]`／`A[36]`：`deg=0..4` 共 15 项，`idx=i*6+j` 上界 24<36 ⇒ 无越界。检索式：手算 Σ(deg+1) 并对 :67-74 逐 deg 复核（注释 `:75` 的 6/10 与 `:387` 的 15 均自洽）。
- 正规矩阵只填上三角再镜像（:193-205）：`Amat[i*(K+1)+j] (j≥i)` 累加、:201-205 镜像下三角，与 :180 的对称性说明一致，无漏项。
- :312 与 :317 的 `else if` 不构成自相矛盾（两档结果同为 order=2，前档覆盖 n_pairs<12、后档覆盖 12≤n_pairs<30，适用域不同）。
- :222-228 收敛判据方向为正（`max_delta < converge_eps ⇒ break`，:258-260），非反向。
- `irls_huber_fit` 若 `max_iter<=0` 会使 `final_w/final_r` 为空并被 :449 越界读——唯一实参是 :408 的 `15`，不可达，故不报为缺陷（已在 :408 处置行登记）。
- 全仓是否存在三角形索引/哈希常数（派单重点②）在本文件的检索式：`git grep -n "sort_triangle_array\|find_ba_triangle\|hash_offset\|0x9e3779b9\|prime\|素数\|扰动" -- 'lib/algorithms/platesolve/**'` ⇒ 该模块零命中（命中的只有无关 `float*`/`a_prime`）。

---

## 2 · `lib/algorithms/platesolve/cpp/ipv/src/ipv_triangle.cpp` —— 已读：是（表 22 项）

全文 737 行已从头读完（1–737）。生产可达性核实：`triangle_match` 由
`ipv_solver.cpp:458`（`solve`）／`:864`（`solve_from_memory`）／`:1213`（`solve_post_select`）调用，
其中 `:1213` 在 `ipv_solve_from_detections_v1` 生产链上
（`ipv_entry.cpp:675 → :711 do_solve_from_detections_v1_impl → :594 s->solve_from_detections_v1 →
ipv_solver.cpp:1534 solve_post_select`）⇒ **本文件的可复现生产触发路径成立**。

派单重点② 的本批取证结论：**本批三个文件不存在三角形索引哈希表、素数表长、扰动量**
——`ipv_triangle.cpp:11-16` 明文记录"投票矩阵改回 2D 数组（原用稀疏 `unordered_map<VoteKey,double>`）"
且"不做 `sort_triangle_array` + `find_ba_triangle` 二分加速"；`git grep -n
"sort_triangle_array\|find_ba_triangle\|hash_offset\|0x9e3779b9\|素数\|扰动" -- 'lib/algorithms/platesolve/**'`
⇒ 零命中。本批"三角形索引面"的实际常数是稠密投票矩阵 + `ba/ca` 描述符剪枝，逐项进下表。

### 2.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| ipv_triangle.cpp:82 | 索引互异判定基数 | 3 | 三点须互不相同 | 结构性不适用 | 组合定义，无科学语义 | — | CONFIRMED |
| ipv_triangle.cpp:89 | `d < 0.0` ×3 | 0.0 | "距离非负"检查 | 结构性不适用 | `std::sqrt` 值域恒 ≥0，三式恒假 ⇒ 死代码但不改结果；不是容差 | — | CONFIRMED |
| ipv_triangle.cpp:94-151 | 排序分支的 `>=`/`>` | — | 三边全序分派 | 结构性不适用 | 无字面常数；分支覆盖性在 :151 行单独核 | — | CONFIRMED |
| ipv_triangle.cpp:151-154 | "不应到达此处"出口 | — | 兜底 return false | 结构性不适用（但注释错） | 实测该支唯一可达条件是**任一距离为 NaN**（三比较式全假）；注释写的"三点距离全相等且为 0"反而**到不了**（全相等时 :94 `d12>=d23 && d12>=d13` 为真走第一支） | 保守方向：NaN 时 fail-closed 已由该支提供 ⇒ 无产品风险，只错在注释语义 | CONFIRMED |
| ipv_triangle.cpp:157-163 | `a > 0.0`；`ba=ca=1.0` | 0.0 / 1.0 | 除零保护 + "让重合点被忽略" | 结构性不适用（1.0 那对是死值） | `a==0` 时 :167 `a < 1e-6` 必返 false ⇒ :161-162 赋值永不被消费；见 CR-44-11 | — | CONFIRMED |
| ipv_triangle.cpp:167 | 退化下界 | 1e-6 | `a_length` 小于此判退化三角形 | **待确认（量纲双关）** | 条文面零提及（`git grep -n "1e-6" -- docs/algorithms/PLATESOLVE.md` 只命中 :339 的 F6 测试门 <1e-6 deg，与三角形无关）。同一常数同时用于 U（像素，:582）与 W（角秒，:583）⇒ 一个 1e-6 服务两种量纲 | 保守方向：**按 s0 归一后取像素侧界**。现值在两侧都远小于真实星距 ⇒ 实际不误杀；风险仅在于它被当成"退化防护"而它并不覆盖共线（CR-44-11） | CONFIRMED |
| ipv_triangle.cpp:182,536,501 | 最少星数 | 3 | 成三角形与 choose3 下界 | 结构性不适用 | 组合基数定义 | — | CONFIRMED |
| ipv_triangle.cpp:185,503 | `C(n,3)` 的除数 | 6 | 组合数公式 | 文献值 | 组合恒等式 n(n−1)(n−2)/3!，按定义常数处理 | — | CONFIRMED |
| ipv_triangle.cpp:194 | reserve 余量 | 1024 | 线程局部 vector 预分配余量 | 结构性不适用 | 纯内存优化，不进结果 | — | CONFIRMED |
| ipv_triangle.cpp:238-245 | `ba` 剪枝上界 | 0.9 | 剔除"细长"三角形 | **待确认＋假锚** | 注释点名的 `AT_MATCH_RATIO` **全仓不存在**（`git grep -n "AT_MATCH_RATIO"` ⇒ 仅此注释一行命中，`ipv_triangle.h` 只有 `AT_MATCH_MINVOTES`）；条文面零提及 ⇒ 见 CR-44-10 | 保守方向：**调小（更严）**。0.9 越大保留越多，细长效应使 (ba,ca) 描述符聚集 ⇒ 虚假匹配增多，偏绿；影响范围 = 三角形集合、投票矩阵、`max_vote`、最终 top_pairs | PARTIAL |
| ipv_triangle.cpp:260,273,280,340 | 描述符匹配容差 | 实参 0.002 | (ba,ca) 空间欧氏距离平方门 `< tol²` | **待确认（且被上位文档错指为另一量）** | 本形参默认值字面在 `ipv_triangle.h:72`「描述符匹配容差 (ba, ca 空间欧氏距离阈值, 默认 0.002)」与 `:88 double tolerance = 0.002, // 默认 0.002 (原 0.001 过严)`；`PLATESOLVE.md:33` 却写作 `build_triangles(...) scale_tol=0.002`、§9:192 写作「**尺度容差 0.002（各向异性 0.2%）**」⇒ 见 CR-44-7 | 保守方向：先改正条文再谈数值。若按"各向异性 0.2%"读，门应为逐三角形 b/a 偏离界；若按描述符半径读，0.002 是 (ba,ca) 单位方框内的半径 | CONFIRMED |
| ipv_triangle.cpp:277-278,283 | `scale_min/scale_max` 哨兵 | −1.0 | "−1=不约束"的禁用哨兵 | 结构性不适用 | 哨兵量，靠 :283 `> 0.0` 判启用；不是科学量 | — | CONFIRMED |
| ipv_triangle.cpp:311,363 | omp chunk / schedule | 64 / static | 并行调度粒度 | 结构性不适用（并行度问题属邻批已立案项） | `PLATESOLVE.md:299-302` DISP-WCS-005 已明文登记 `ipv_triangle.cpp:302/:347` 的 `#pragma omp` 无 `num_threads` 注入；本批不重复立案，只登记行号 | — | CONFIRMED |
| ipv_triangle.cpp:318-321,329-331 | 顶点索引上界 | numA/numB | 越界保护 | 结构性不适用 | 实为恒假死守卫：三角形由 `stars_to_triangles(stars, n_stars)` 用下标 0..n_stars−1 构造，故 `a_index < numA` 恒成立 | — | CONFIRMED |
| ipv_triangle.cpp:343 | 除零保护 | 1e-15 | `a_len_B > 1e-15` 才做 scale 检查 | 结构性不适用 | 除零守卫；且 :167 已保证 `a_len_B ≥ 1e-6` ⇒ 恒真，守卫冗余 | — | CONFIRMED |
| ipv_triangle.cpp:571-573（并 :268/:345/:509/:566） | 尺度带系数 | `/1.2`、`/0.8`（注释称"±20%"） | 允许 `a_len_A/a_len_B` 偏离 1/s0 的带 | **待确认（注释与算式不符）** | 条文面零提及该带（`git grep -n "percent_scale_range\|scale_min" -- docs/**` 无条目）；本文件 :268/:509/:566 与 `ipv_solver.cpp:434/:457` 一致写"±20%" ⇒ 见 CR-44-8 | 保守方向：若本意 ±20% 应写 `[0.8,1.2]×expected`；现为 `[0.8333,1.25]×expected` ⇒ 上侧更宽＝偏绿，下侧更严＝偏红。影响范围 = 进入投票的三角形对集合 | CONFIRMED（标签）／PARTIAL（1/(1±p) 形是否为参考实现口径） |
| ipv_triangle.cpp:432,443-447,613 | `AT_MATCH_MINVOTES` | 2 | 进入候选配对的最小票数 | 待确认 | 定义在 `ipv_triangle.h:28`；**PLATESOLVE.md 与 ASTROMETRY.md 全文零提及**（检索式 `git grep -n "AT_MATCH_MINVOTES\|MINVOTES" -- docs/**` 零命中；仅内部报告 `cpp/ipv/REPORT.md:1038/:1138` 称"添加门槛"／"过严的投票门槛过滤掉真实匹配"） | 保守方向：调小→候选增多（偏绿），调大→更严。它与 :617 `max_v>0`、solver :507 `max_vote<3` 构成三档不同门，无一有锚 | CONFIRMED |
| ipv_triangle.cpp:465 | `top_n > 0` | 0 | top_n 关闭上限的哨兵 | 结构性不适用 | 实参恒为 `n_stars_A`（:605）≥3 ⇒ 恒真 | — | CONFIRMED |
| ipv_triangle.cpp:483 | 调试打印条数 | 10 | 日志截断窗 | 结构性不适用 | 只影响日志可读性 | — | CONFIRMED |
| ipv_triangle.cpp:497-498 | `IPV_TRIANGLE_MATCH_MAX_VOTE_OPS` | 2000000000 | 枚举前工作量预算上界 | 待确认（导出式在注释里但未写成式） | 注释 :490-496 自证：`C(60,3)² = 34220² = 1.171e9`（复核属实）＋"2e9 > 1.171e9 故生产永不触发"＋"仅拦截 n≥66"（复核：`C(66,3)² = 45760² = 2.094e9 > 2e9`、`C(65,3)² = 1.731e9 < 2e9` ⇒ n≥66 正确） | 保守方向：该值随生产 `img_n_target` 变动而失效或误杀 ⇒ **应写成 `k·C(n_target,3)²` 的派生式**。影响范围 = 调用方传 n_target>65 时整帧求解 fail-closed | CONFIRMED |
| ipv_triangle.cpp:554-559 | `fail_reason` 缓冲 | `sizeof(result.fail_reason)` | 失败原因串截断窗 | 结构性不适用 | snprintf 带界，非科学量 | — | CONFIRMED |
| ipv_triangle.cpp:617 | success 判据 | `max_v > 0` | 单票即算匹配成功 | 待确认（与条文一致） | `PLATESOLVE.md:263`「求解器层：三角形匹配 **0 匹配**或 iter_trans_solve 全阶失败 → fail_result」⇒ 条文把失败定义为"0 匹配"，代码与之一致 | 保守方向：无需改；但 `success=1 且 top_pairs 为空` 是合法输出（候选门是 ≥2） | CONFIRMED |
| ipv_triangle.cpp:654,714 | `MIN_VOTES_THRESHOLD` | 3 | 自适应提前停止门 | 待确认（且当前不可达） | 条文面零提及；`ipv_types.h:225` 已登记同族事实「log_only: vote_threshold (仅日志; **判据为字面量 3**)」⇒ 3 是真实硬门但**无出处** | 保守方向：给 3 的漏检率依据。因 :655 只一档，:714 的提前停止与 :721 的"继续扩充"**永不执行**（CR-44-9） | CONFIRMED |
| ipv_triangle.cpp:655-656 | `adapt_seq[]` | {60} | 自适应星数序列（只剩一档） | 待确认 | `PLATESOLVE.md:238` 选星侧给 `n_target = min(60, max(50, …))` ⇒ 60 出现在**选星侧**条文；本文件 :18 文件头仍写"扩充 20 -> 40 -> 60"、:632-636 写"直接用 60 颗" | 保守方向：60 应与 `img_n_target` 同源而非各自字面（solver 侧另有 3 处字面 60） | CONFIRMED |

### 2.2 findings

### CR-44-7 · 被上位文档命名为"尺度容差 0.002／各向异性 0.2%"的量，在实现里是三角形描述符匹配半径；"各向异性"门在 `lib/**` 全仓零实现
- 轴：C（代码／算法文档／测试门三方对同一 0.002 说的是三件事）；兼 S
- 位点：`ipv_triangle.cpp:340`
  ```cpp
  if (dba * dba + dca * dca < tol2) {
  ```
  （`tol2` ← `:280 double tol2 = tolerance * tolerance;` ← `:273 double tolerance` ← 实参 `ipv_solver.cpp:459/:865/:1214` 的 `0.002`）
- 上位依据：`docs/algorithms/PLATESOLVE.md:33`「triangles = build_triangles(detections) **scale_tol=0.002**」；`:34`「matches = kd_match(triangles, gaia_triangles) **tol=5.0"**」；§9 `:192`「**尺度容差 0.002（各向异性 0.2%）**：阈值来源 UNJUSTIFIED，登记为待标定项」；§11.4 `:318`「CD 元素相对误差 ≤2%（**§9 尺度容差 0.002 同源**）」。
- 现状：代码里 0.002 唯一用途是 `make_vote_matrix` 的 (ba,ca) 描述符欧氏半径（形参名就叫 `tolerance`，`ipv_triangle.h:72` 释义"描述符匹配容差"）；三角形**构造**（`set_triangle`/`stars_to_triangles`）内不含任何 0.002，其尺度约束是 :571-573 的 1.2/0.8 带。检索式 `git grep -in "anisotrop\|各向异性\|scale_tol" -- 'lib/**'` ⇒ **零命中**。
- 差在哪：§9 把一个"匹配半径"登记成"尺度/各向异性容差"，§3 伪代码又把它挂到 `build_triangles` 上；于是 §11.4 的 F1 测试门（CD 相对误差 ≤2%）自称与"尺度容差 0.002 同源"，而那个"同源"对象在代码里根本不承担尺度语义。
- 后果：① `PLATESOLVE.md:192` 的待标定项登记指向一个不存在的门 ⇒ 真正需要标定的描述符半径 0.002（决定哪些三角形参与投票）**完全在容差来源面之外**，§9:204-205 的"标定前引用面=待标定项"这条纪律对它不适用；② F1 门 `CD≤2%` 的出处链断裂；③ 后续整改按 §9 找"各向异性门"会找不到而被误判为漏实现。
- 定级：**S2**（不改产品字节，但改两条对外主张的证据资格）
- 怎么算修好：同批三处：① `PLATESOLVE.md:33` 伪代码把 `scale_tol=0.002` 改名并移到 `make_vote_matrix` 所在行；② §9 新增「(ba,ca) 描述符匹配容差 0.002：`ipv_triangle.cpp:340`，比例空间量纲，**阈值来源 UNJUSTIFIED**」，并把 :192 改为"当前无各向异性实现，若需该门另立条文"；③ :318 删掉"§9 尺度容差 0.002 同源"半句。**不要顺手改 0.002 的数值**——它未标定，改数需另出证据。
- 置信：CONFIRMED

### CR-44-8 · 尺度带在六处注释一致自称"±20%"，算式实际是 −16.7%/+25% 的非对称带
- 轴：C（注释与算式）；兼 R
- 位点：`ipv_triangle.cpp:571-573`
  ```cpp
  double expected_ratio = 1.0 / s0;  // 像素/角秒
  scale_min = expected_ratio / 1.2;  // +20% 容差
  scale_max = expected_ratio / 0.8;  // -20% 容差
  ```
- 上位依据：条文面零提及该带。仓库内唯一"参考实现"级依据是 `PLATESOLVE.md:181`「astrometry.net 语义对照 (05 spec)」——它不给项目/版本/文件:行，**故我不能据仓内材料断定 `1/(1±p)` 形是否为参考实现口径**（见置信）。
- 现状：注释自述方向与算式相反（`/1.2` 标"+20%"、`/0.8` 标"−20%"）；"±20%"字样另见 `:268`（`percent_scale_range=20%`）、`:509`、`:566`、`ipv_solver.cpp:434`、`:457`。数值：`1/1.2=0.8333…`、`1/0.8=1.25` ⇒ 实带 `[0.8333,1.25]×expected`。
- 差在哪：若意图为对称 ±20%，式应是 `expected*(1±p)`；现式在两个方向上都与标签不符且不对称（宽侧 +25%）。
- 后果：影响进入投票矩阵的三角形对集合：上侧比声称宽 25%（错误尺度配对得以投票），下侧比声称窄（真配对在 [0.8,0.8333) 被剔）。**不等式方向已钉死**：判据 `ratio < scale_min || ratio > scale_max ⇒ reject`（:345-348），`scale_max` 变大**只让更多配对进入** ⇒ 偏绿；`scale_min` 变大只让更多真配对出局 ⇒ 偏红。两侧同时偏离声称值，所以本条不主张"门失效"，主张**该带与自己的说明书不同**。
- 定级：**S3**（该带数值从未进条文，失真面在注释自述精度）
- 怎么算修好：① 二选一并改齐：注释改写为"非对称带 `[1/(1+p),1/(1−p)]`，p=0.2 ⇒ −16.7%/+25%"，或式改为 `expected*(1±p)`；② 六处注释一次改完（`:268/:509/:566/:571-573` + `ipv_solver.cpp:434/:457`）；③ 若该带要成为对外容差，先在 §9 立条目给 p 的来源（现在连 UNJUSTIFIED 登记都没有）。
- 置信：CONFIRMED（标签 vs 算式）／关于"哪一种才是正确惯例"：UNPROVEN，缺仓内可指的参考实现文件:行

### CR-44-9 · 自适应星数与提前停止整段不可达，而文件头仍把它当现役能力描述；三处字面 `3` 各自独立
- 轴：C（注释宣称的能力与实现不符）；兼 R
- 位点：`ipv_triangle.cpp:654-655`
  ```cpp
  const int    MIN_VOTES_THRESHOLD = 3;
  const int    adapt_seq[] = {60};
  ```
  对立面 `:18`「5. 自适应星数: max_vote < 3 时扩充 20 -> 40 -> 60」
- 上位依据：`PLATESOLVE.md:238` 只给选星侧 `n_target` 公式；条文面无"20→40→60 扩充"（检索式 `git grep -n "自适应" -- docs/**` ⇒ 无该控制流条文）。
- 现状：`n_adapt=1` ⇒ :664 至多一轮，:714 的 `break` 与落到循环尾等价，:721-723 "继续扩充" 日志分支为死支；:669-671 的"若大于 20 以调用方为准"里没有任何 20 参与（`nA` 起点即 60）。同形态见 `ipv_solver.cpp:442`、`:849` 的 `for (int n_target : {60})` 与 `:451/:858`"跳过扩充"、`:476/:882`"扩充星数到下一档"。另外 solver 侧真实终止门是字面量 `< 3`（`:507/:913/:1248`），与本文件 :654 的 `3` 无共享常数。
- 差在哪：四段"多档自适应"控制流与文案是单档实现的残留；文件头把它列为现役特性。
- 后果：按 :18 读代码的人会以为低质量帧会自动加星重试，实际不会。`AGENTS.md §9` 要求"不用 facade 冒充实现完成"——此处是文档面向实现面的 facade。不改产品字节。
- 定级：**S3**
- 怎么算修好：把 :18 改成"固定 60 颗（单档），不足即失败上抛"；同批要么删 `adapt_seq`/`MIN_VOTES_THRESHOLD`/:721 死支，要么真正实现多档；并把三处字面 `3`（本文件 :654 ＋ solver :507/:913/:1248）合并为一个具名常量——**分头改会静默漂移**。
- 置信：CONFIRMED

### CR-44-10 · 细长三角形剪枝上界 0.9 无出处，注释归给的常数名全仓不存在，且注释给的理由方向自相矛盾
- 轴：S（凭什么）＋ C（假锚）
- 位点：`ipv_triangle.cpp:238-245`
  ```cpp
    // 剪枝 ba > AT_MATCH_RATIO(0.9) 的细长三角形:
    // - ba = b/a 越小越接近等边, 描述符越稳定
    ...
            [](const Triangle& t) { return t.ba > 0.9; }),
  ```
- 上位依据：`git grep -n "AT_MATCH_RATIO"` ⇒ **全仓仅此注释一行命中，无任何定义**。条文面零提及：`git grep -n "0\.9" -- docs/algorithms/PLATESOLVE.md docs/science/ASTROMETRY.md` ⇒ 唯一命中是 `ASTROMETRY.md:193` 的 `min_scale_arcsec = 0.9″/px`（与三角形无关）。
- 现状：0.9 以字面量写在 lambda 内（未走命名常量）；注释自述理由是"`ba` 越小越接近等边"，但按 :68/:72 的定义 `ba = b/a`、`a` 为最长边 ⇒ `ba→1` 才是 `a=b` 的等腰，`ba→0` 才是"一根长边 + 两根几乎等短的边"；真正描述符退化的是 `ca→0`（三点近共线），而 `ca` 侧**完全无剪枝**。
- 差在哪：剪枝方向与它自述的理由不匹配，且阈值无数值来源。
- 后果：三角形集合被一个方向可疑、数值无锚的门裁剪；它同时作用于 U 侧与 W 侧（:582/:583 同一函数），漏配与错配两个方向都可能发生。影响的对外主张 = `PLATESOLVE.md:241`「三角形投票」行的可信度，以及 F1 门 `n_pairs≥12`（`PLATESOLVE.md:317`）能否达到。
- 定级：**S2**（生产可达链：`ipv_entry.cpp:675 → :594 → ipv_solver.cpp:1534 → :1213 → :582 → :245`；"造成哪一帧解错"需实跑对比，故不升 S1）
- 怎么算修好：① 定义真常数（`TRIANGLE_BA_MAX` 或补 `AT_MATCH_RATIO` 的定义）替换 :245 字面量；② §9 立条目并给"为何取 b/a 上界而非 c/a 下界、为何 0.9"的依据，或明确标 UNJUSTIFIED＋待标定；③ 同批订正 :239 的方向描述。若 ② 的结论是应改剪枝对象，必须补一条负例：给定共线/等腰星集，验证剪枝前后三角形集合差异。
- 置信：PARTIAL（"无出处＋假常数名＋注释方向自相矛盾"CONFIRMED；"真实帧上是否造成漏配/错配"缺实跑证据）

### CR-44-11 · 退化三角形门只挡"三点重合"，注释与文件头宣称的"三点共线"从未被检测
- 轴：R（声称的守卫不存在）＋ C
- 位点：`ipv_triangle.cpp:166-167`
  ```cpp
    // 退化三角形跳过 (a_length < 1e-6: 三点共线或重合)
    if (a < 1e-6) return false;
  ```
  与文件头 `:64`「返回: true=有效三角形, false=退化 (a_length < 1e-6, **三点共线或重合**)」
- 上位依据：条文面零提及共线判据（`git grep -n "共线\|collinear" -- docs/** 'lib/**'` ⇒ 除本文件 :64/:166 两处注释外零命中；`lib/**` 内无面积/叉积计算）。
- 现状：`a = max(d12,d23,d13)`，只有三边全短（三点几乎重合）时 `a<1e-6` 才成立；长基线共线三点 `a` 很大 ⇒ 全数放行。共线时 `b+c=a` 严格成立 ⇒ `ba+ca=1`，这些描述符全部落在 (ba,ca) 平面的同一条线上，而 :340 的判据是半径 0.002 的圆 ⇒ 共线族彼此聚集。
- 差在哪：唯一被当作"退化防护"的判据管不到它声称的一半对象；而共线恰是描述符退化（比值聚集）的主因。
- 后果：**不等式方向钉死**：少一道剔除判据 ⇒ 保留的三角形集合**只增不减** ⇒ 虚假投票可把 `max_vote` 虚高 ⇒ 偏绿，而 `max_vote` 正是 `ipv_solver.cpp:507/:913/:1248` 三处 `max_vote < 3` 失败门的输入。虚高到 3 票即从"求解失败"变"有解"，产品面上就是多出（或错出）一份 WCS。
- 定级：**S2**（生产可达链成立；"足以虚高过 3 票门"需实跑注入共线星集定案，故不升 S1）
- 怎么算修好：① 加真实共线判据（叉积面积 `|cross|/a² < ε`，或给 `ca` 设下界），或 ② 把 :64/:166 注释改为"仅挡三点重合"并在 §9 登记"共线剪枝：无"。**防误红项**：:233-236/:249-252 的 `valid=`/`pruned=` 计数会随判据改变；凡断言这两个数或断言 `n_triangles_A/B` 的用例必须同批改期望，不得顺测试反向改判据。
- 置信：CONFIRMED（覆盖面差额逐字核对；共线三角形通过全部分支已手算：`d12` 最大且 `d23>d13` 时走 :94 支，`ba+ca=(d23+d13)/d12`，s3 居中共线时该和 = 1）

### 2.3 本文件"核过为合规、不报"的项（附检索式）
- **OpenMP 归约的跨线程确定性**：:190-228 三角形集合的**顺序**随 `schedule(dynamic)` 变化，但 (i) :340-357 累加全是 `int`、:362-373 归并是纯整数加法，结合律成立；(ii) :451-457 排序带（票数, u, w）三级 tie-break ⇒ `top_pairs` 与线程序无关；(iii) :608-616 为整数极值/计数。⇒ 不报（与 `PLATESOLVE.md:164`「确定性」条文不冲突）。
- **NaN 输入行为**：任一 `StarPoint` 含 NaN 时 :94/:113/:132 全假 ⇒ 落 :151 `return false`，fail-closed，不污染投票矩阵 ⇒ 不报（只报 :151 注释措辞，已入 :151 处置行）。
- **稠密投票矩阵内存**：:295 `flat_size=numA·numB`、:296-299 每线程一份；注释 :292 的"60×60×16 = 230KB"复核为 `3600·4·16 = 230400 B` ✓ 属实。
- **`budget_exhausted` 上抛链**：:687-690、:703-706 均"原样赋值后 break"；:550-563 置 `success=false`、`max_vote=0` 并写 `fail_reason`；solver :513-516/:918-921/:1253-1256 三处把 `fail_reason` 原文搬进 `fail_result.error` ⇒ 无静默吞失败。
- **票数三档门（2/1/3）分层**：第三档已被 `ipv_types.h:225` 显式登记为"判据为字面量 3、`vote_threshold` 仅日志"⇒ 属条文已声明的既有口径，按前言 §4 不单独作缺陷报（合并常量的建议已并入 CR-44-9）。
- **`make_vote_matrix` 的 :286 置零与 :363-373 逐元写**：无读写竞争。

---

## 3 · `lib/algorithms/platesolve/cpp/ipv/src/ipv_solver.cpp` —— 已读：是（表 26 项）

全文 1819 行已从头读完（1–1819：1-470 / 470-960 / 960-1450 / 1445-1819 四段）。
生产可达性：`ipv_entry.cpp:675 ipv_solve_from_detections_v1 → :711 → :594 s->solve_from_detections_v1
→ ipv_solver.cpp:1473 → :1534 solve_post_select → :1187`（`PLATESOLVE.md:217` 「生产通道 =
orchestrator PLATESOLVE 阶段直调 `ipv_solve_from_detections_v1`」、`:232` 「**生产入口**」）
⇒ **本文件的 S1 门槛（沿生产路径逐跳写 `文件:行`）可满足。**
`iterative_reproject` 在本文件内是 `static`-级自由函数，被 `:592`（solve）、`:992`（solve_from_memory）、
`:1299`（solve_post_select）三处调用，三条链同形。

### 3.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| ipv_solver.cpp:51 | `IPV_PI` | 3.14159265358979323846 | 圆周率 | 文献值 | 数学常数；double 精度下尾 3 位无意义（可表示到 17 位有效），不改结果 | — | CONFIRMED |
| :52-53, :100-101, :54 | `IPV_DEGTORAD`/`IPV_RADTODEG`/`IPV_ASEC_PER_RAD` | 180.0 / 360.0 / 206264.80624709636 | 度弧度与弧度角秒换算 | 结构性不适用 | 单位换算定义常数（前言 §4 明列不报）；`206264.80624709636 = 180·3600/π` 已复算 | — | CONFIRMED |
| :78 | `rho < 1e-12` | 1e-12 | 切平面原点退化 ⇒ 直接返回 (ra0,dec0) | 待确认 | 条文面零提及（`git grep -n "1e-12" -- docs/algorithms/PLATESOLVE.md` 零命中；`§4 边界/NaN/Inf` 表 :56-63 无此行）。量纲 = 弧度 | 保守方向：**调小**。1e-12 rad = 2.06e-7″ ⇒ `atan(rho)` 与 `rho` 相对差 ~1e-24，该阈远高于数值必要 ⇒ 实际影响 = 在 2e-7″ 内把中心判为"零"并跳过反投影，误差 ≪ 任何门 ⇒ 影响可忽略但仍无锚 | CONFIRMED |
| :123 | `valid = (cosc > 1e-10)` | 1e-10 | TAN 投影半球有效性（cos c 下界） | 待确认 | 条文面：`PLATESOLVE.md:59`「`det(trans.linear)==0` → reject SIP, NO_SOLUTION」不覆盖此判据；§4 表 :56-63 无 `cosc` 条目。`DISP-WCS-001/002`（:279-290）登记了**同族**判据 `wcs_transform.cpp:39-49 det<1e-15` 与 `wcs_tan.cpp:48-51 det<1e-30` 的静默坍缩问题，但那是另两个文件 | 保守方向：**调小**（1e-10 ⇒ 拒掉的星偏多）。1e-10 rad² 对应天顶距 90.0000000029° ⇒ 只挡真正半球外；作用是把边界星写成 1e18 哨兵（:268），拒得多 ⇒ 偏红、拒得少 ⇒ 引入 `1/cosc` 爆炸。现值无锚但保守方向明确 | PARTIAL（未做 1e-10 邻域的 cosc 分布实跑） |
| :124, :130-131 | `cosc_safe = 1.0`；invalid ⇒ `0.0` | 1.0 / 0.0 | 除零替换与无效值 | 结构性不适用 | 哨兵；`valid=false` 时 :130-131 的 0.0 实际不被消费（调用方一律看 `valid`） | — | CONFIRMED |
| :198 | `MAX_ITERS` | 5 | `iterative_reproject` 迭代上限 | 待确认 | `PLATESOLVE.md §9:185-188` 只登记收敛阈 0.01″，**未登记上限 5**；注释写的 `(PLATESOLVE.md 数值风险)` 一节不存在（见 CR-44-3）。`§3:36` 伪代码写 "iterative_reproject(matches) conv 0.01" max5 **(ipv_solver.cpp)** ⇒ 伪代码给了 max5 但无出处 | 保守方向：**调大并加收敛断言**。上限不足 + 不检收敛 ⇒ 未收敛解被当成功下发（CR-44-12）⇒ 偏绿 | CONFIRMED |
| :199 | `CONV_THRESH_ARCSEC` | 0.01 | 收敛阈：TRANS 常数项的模 < 0.01″ | 文献值→**不成立**；按条文应为待确认 | `PLATESOLVE.md §9:185-188`「收敛 0.01" (pixel/3600)：`ipv_solver.cpp:199 CONV_THRESH_ARCSEC = 0.01`，判据 `√(x00²+y00²) < 0.01″`（**`ipv_solver.cpp:19`**）。**该锚的第二处行号指错了对象**：`:19` 是文件头注释行、不是判据行（判据在 :217-219/:320-322/:326）。且 §9 自己限定「该值只约束迭代重投影的自洽收敛，**不是天测精度界**」 | 保守方向：值本身与条文一致（0.01）；要改的是锚的行号。影响范围 = 后续按 :19 复核的任何人 | CONFIRMED |
| :268-269, :634-635, :1033-1034, :1330-1331 | 无效投影哨兵 | 1e18 | 把不可投影星标成极大值 | 待确认（哨兵取值） | 条文面零提及哨兵值；`§11.3 DISP-WCS-002:288-290` 对**同族做法**（`xi=eta=1e6` 哨兵无标志位）已判为缺陷并给出语义要求：「失败-置信度语义契约：…解的取值面 = 显式失败，**不以…坍缩值冒充解**」（:285-287） | 保守方向：应改为标志位而非数值哨兵（与 DISP-WCS-001/002 的契约一致）。现值风险见 :288 行与 CR-44-14 | CONFIRMED |
| :288 | 哨兵过滤阈 | 1e10 | `\|w.x\|<1e10 && \|w.y\|<1e10` 才算有效 | 待确认 | 条文面零提及。与 :268 的 1e18 是**同一约定的两个独立字面量**（写入侧 18 个零、读取侧 10 个零），无任何注释把它们关联 | 保守方向：二者应共名一个常量。当前若写入侧改为 1e9 而读取侧不改，无效星被当有效 ⇒ 偏绿 | CONFIRMED |
| :293, :347 | 最少有效 inliers | 3（两处同值不同语义） | :293 终止迭代 / :347 定义 `success` | 待确认 | 条文面：`§11.2:263-265`「三角形匹配 0 匹配或 iter_trans_solve 全阶失败 → fail_result」**未给 inlier 下限**；`§11.4:317` F1 门是 `n_pairs≥12`（**测试**门）。⇒ 生产下限 3 无出处 | 保守方向：**调大**（3 ⇒ 零/负自由度解合法，见 CR-44-13）；影响范围 = 全链最低信息量解的门槛 | CONFIRMED |
| :442, :849, :1210, :1516, :1592, :1663 | 三角形/选星目标星数 | 60（六个独立字面量） | 强制 `img_n_target=60` 并作为 `triangle_match` 的 n_stars | **公式导出但代码未按式算** | `PLATESOLVE.md §4a.2:96` 为条文：**「`n_target` 由 `rho_img`、查询锥面积与密度比导出，且必须落在闭区间 [50, 60]」**；§11.1:238 给导出式 `min(60, max(50, round(ρ_target·query_area/img_area)))`。代码在六处**无条件取上界 60**（:445/:851 `p_adapt.img_n_target = n_target`）⇒ 导出式在本文件不参与。`ipv_types.h:227` 已登记 `shadowed: img_n_target (=60 硬覆盖)`，但该登记自述范围是"配置可达性"，**未处置 §4a.2 的导出要求**（其尾句"不改变任何默认值/公式/容差"） | 保守方向：**按 §4a.2 式取值**。取上界 = 用更多的暗星进三角形集合 ⇒ 投票噪声增大 ⇒ 偏绿；影响范围 = 六条入口的全部求解 | PARTIAL（`ipv_select.cpp:260-330` 是否另有自己的 n_target 推导未被本批文件覆盖，需读该函数定谁生效） |
| :459, :865, :1214 | 描述符匹配容差 | 0.002（三处字面量） | `triangle_match` 的 (ba,ca) 半径 | 待确认 | 见 CR-44-7（上位文档错把 0.002 登记为"尺度容差/各向异性"）；本文件三处均未具名 | 保守方向：见 CR-44-7 | CONFIRMED |
| :507, :913, :1248 | 三角形票数终止门 | `max_vote < 3`（三处字面量） | 求解终止判据 | 待确认 | `ipv_types.h:225` 登记「log_only: vote_threshold (仅日志; **判据为字面量 3**)」⇒ 已知事实但**无出处**；与 `ipv_triangle.cpp:654` 的 `MIN_VOTES_THRESHOLD=3` 各自独立 | 保守方向：合并为单一具名常量。三处改一处即漂移 | CONFIRMED |
| :534, :936, :1271 | **匹配半径**（`iter_trans_solve` 第 4 实参） | 5.0 | `atMatchLists` 匹配半径（角秒） | **待确认（第二来源已存在）** | `PLATESOLVE.md §9:189-191`「三角形匹配容差 5.0″：**`ipv_solver.cpp:660` 实参**。量纲 arcsec；**阈值来源 UNJUSTIFIED**（无推导、未随像素尺度归一），登记为待标定项」⇒ 条文只点了 9 个位点中的 1 个。第二来源 = `ipv_itertrans.h:99` 与 `:156` 的**默认实参 `= 5.0`**（另 `:84/:144/:149` 三处注释重复"默认 5.0"）。见 CR-44-12 附列的位点全清单 | 保守方向：条文 §9:191 已自判"未随像素尺度归一" ⇒ 保守取值应**按 s0 归一并取更小的秒数**（半径变小 ⇒ 匹配数变少 ⇒ 偏红，安全侧）。影响范围 = 三链全部求解 | CONFIRMED |
| :660, :670, :1056, :1065, :1353, :1362 | **匹配半径**（`at_match_lists` 第 4 实参） | 5.0（六处字面量，仅 :660 带注释 `// 5" tolerance`） | 同上，高阶重匹配阶段 | 同上 | 同上；:660 是本批唯一被 §9 点名的位点，其余 5 处不在任何条文里 | 同上 | CONFIRMED |
| :532, :934, :1269 | 阶数回退上界 | `for (int it_order = 3; it_order >= 1; --it_order)` | TRANS 阶数从 3 逐级回退 | 结构性不适用（迭代上界的实现选择） | 3 = 多项式最高阶，与 `§2:18` F3/`ASTROMETRY.md:77-79` 的 `trans.x_ij` 阶域一致；1 为下限由"线性解必有"保证。`PLATESOLVE.md:242` 登记 `iter_trans_solve … 迭代重投影多项式拟合（order 1→3）` | — | CONFIRMED |
| :535, :937, :1272 | iter_trans 通过门 | `n_inliers >= 3` | 该阶解是否可接受 | 待确认 | 与 :293/:347 同族（见 CR-44-13 的自由度分析：order=2 需 6 系数、order=3 需 10 系数，3 点 ⇒ **严重欠定**） | 保守方向：**至少 K+1**。现值使欠定解通过 ⇒ 偏绿 | CONFIRMED |
| :612, :1012, :1311 | 重投影通过门 | `!success \|\| n_matched < 3` | 唯一消费 `iterative_reproject` 结果的判据 | 待确认（**不含收敛项**） | `PLATESOLVE.md §11.2:263-265` 给失败语义清单，其中**没有"迭代未收敛"这一失败条件**；§9:187 只说 0.01″"约束自洽收敛"，未说谁消费它。⇒ 见 CR-44-12 | 保守方向：判据**增加**收敛项 ⇒ 只会拒绝更多 ⇒ 偏红（安全侧） | CONFIRMED |
| :652, :1049, :1346 | 高阶重匹配触发门 | `trans.order > 1` | order=1 时跳过重匹配与鲁棒精化 | 结构性不适用 | 线性解无畸变项，重匹配无意义（:651 注释）；与 `PLATESOLVE.md:247`「order≤1 → RA---TAN/DEC--TAN（无 -SIP）」一致 | — | CONFIRMED |
| :656-657, :1052-1053, :1349-1350 | `min_pairs_hi` | 10 / 6 / 3（三处同形三元式） | 高阶重匹配最少配对数 | **公式导出，代码写死且存三副本** | 导出式：二维 `order` 阶多项式系数个数 `(order+1)(order+2)/2` ⇒ order1=3、order2=6、order3=10 ✓。真值源 = `ipv_itertrans.cpp:42/:44/:46 static constexpr AT_MATCH_REQUIRE_LINEAR/QUADRATIC/CUBIC = 3/6/10`——**文件内 static，本文件不可引用**，故 :655 注释以"（ipv_itertrans.cpp 内 static 常量）"自述了一份手抄。第三套约定见 `ipv_sip.cpp:292`（order2 用 **7** = K+1）与 `:312`（order3 用 **12** = K+2）⇒ 同仓两族写法（K vs K+1/K+2）并存 | 保守方向：**统一为 K+1 以上**（留自由度）。现值 = 恰好 K ⇒ 零自由度、残差恒机器精度级 ⇒ 见 CR-44-13 | CONFIRMED |
| :668, :1063, :1360 | 高阶重匹配迭代上限 | 5 | `for (int iter = 0; iter < 5; ++iter)` | 待确认 | 条文面零提及（§9 只登记 iterative_reproject 的 0.01″ 与 §3:36 的 max5，未登记此处） | 保守方向：上限不足 ⇒ 未收敛即出解，且**循环耗尽后不改 `success` 标志**（同 CR-44-12 形态） | CONFIRMED |
| :679, :1073, :1370 | 匹配数收敛门 | `std::abs(dn) <= 1` | 两次迭代匹配数变化 ≤1 视为收敛 | 待确认 | 条文面零提及。1 的物理含义 = 允许每轮换掉一颗星 | 保守方向：**取 0** 更保守（现值允许持续漂移 1 颗/轮 × 5 轮）⇒ 现值偏绿 | CONFIRMED |
| :680-681, :1074-1075, :1371-1372 | RMS 相对收敛门 | `0.01`（1%），且 `rms>0` 假 ⇒ **`true`** | 相对 RMS 变化判收敛；RMS 为 0 时无条件判收敛 | 待确认（`else true` 为 fail-open） | 条文面零提及 1%；**`hi_result.rms <= 0 ⇒ conv_rms = true`** 无锚 | 保守方向：**`rms<=0` 应判未收敛**（拒绝），现写法直接判收敛 ⇒ **偏绿**（不等式方向已钉死：`conv_rms=true` 只可能触发 :686 的 break 并让该解作为精化结果继续下游）。与 `min_pairs_hi = K` 同时成立时零自由度解必然"收敛" | CONFIRMED |
| :714, :1104, :1401 | `initial_rms_arcsec` 兜底 | 1.0 | `trans.sig <= 0` 时给鲁棒精化的初始 RMS | 待确认 | 条文面零提及（`ipv_robust_refine.h:76` 只有 `final_rms_tolerance = 1.5` 属邻批位点）。含义 = "1 角秒"这一典型seeing量级 | 保守方向：**调小**（初值偏大 ⇒ 稳健精化的接受域偏宽 ⇒ 偏绿）。影响范围 = 三处 `robust_refine_wcs` 调用 | PARTIAL（未核 `robust_refine_wcs` 内部对该初值的用法） |
| :724, :1114, :1411 | `RobustRefineParams{}` | 全默认构造 | 以编译期默认值调鲁棒精化 | 待确认（**默认值面在本文件不可见**） | `ipv_types.h:220-227` 登记「【全局】生产路径无任何"配置/CLI → IpvParams"注入路径 ⇒ 本结构体 24 字段全部不可由用户配置影响，值恒为编译期默认值」⇒ 同一事实：本行是"默认值即生产值"的发生位。`RobustRefineParams` 各字段属邻批文件，**本批只报本位点** | 保守方向：把实际生效值写进日志或条文。影响范围 = 该次精化全部判据 | PARTIAL（默认字段值在 `ipv_robust_refine.h`，非本批独占面） |
| :1722 | 缓存有效性 | `!matched.empty() && s0 > 0.0` | `last_inliers_.valid` | 结构性不适用 | 空集与除零守卫；`§11.1:252` 登记该缓存角色 | — | CONFIRMED |
| :1741-1750, :1761, :1766 | inlier 缓冲列数 | 9 | 导出行宽与越界填充宽度 | 结构性不适用 | 列数由 `ipv_api.h:240-249` 的字段表钉定（9 列），`§11.1:229` 同 | — | CONFIRMED |
| :1766, :1801-1803 | 越界/无效 ⇒ NaN | NaN | 用 NaN 作对外缓冲的缺失标记 | 待确认（**对外合同无 NaN 声明**） | 条文面零提及：`ipv_api.h:240-249` 逐列给定义但**未声明任何列可取 NaN**；`§11.2:260-262`「error_msg[256] 由 set_error_msg 填充…success=1 时 IpvWcsResult POD 为唯一权威输出」；`PLATESOLVE.md §4:63`「输入含 NaN → skip/fail per-field」是**输入侧**语义，不覆盖输出侧 | 保守方向：要么在 `ipv_api.h` 明列 NaN 语义并让消费方显式处理，要么改为返回可判别的行省略。**风险 = 消费方对 NaN 行取 median/p95 时静默得 NaN 或按 0 计入** ⇒ 见 CR-44-15 | PARTIAL（消费方 NaN 处理在 `eng/tools`/orchestrator 侧，未读） |
| :45, :202, :231, :246, :307, :350, :1761 | 日志/缓冲宽度 | 1024 / 256 / 512 | snprintf 缓冲 | 结构性不适用 | 缓冲区尺寸（前言 §4 明列不报） | — | CONFIRMED |

### 3.2 findings

### CR-44-12 · `iterative_reproject` 的收敛结果既不参与 `success` 也不跨接口传递：未收敛的切点被原样发布为 CRVAL
- 轴：R（判据方向：少一道拒绝门）＋ D（设计点名的收敛未接进失败语义）
- 位点：`ipv_solver.cpp:347`
  ```cpp
  result.success = ((int)inliers_cur.size() >= 3);
  ```
  与循环条件 `:224`
  ```cpp
  while (conv > CONV_THRESH_ARCSEC && trial < MAX_ITERS) {
  ```
  与本地标志 `:222 bool converged = false;` / `:326-331 if (conv <= CONV_THRESH_ARCSEC) { converged = true; … break; }`
- 上位依据：`docs/algorithms/PLATESOLVE.md §9:185-188`「收敛 0.01" (pixel/3600)：`ipv_solver.cpp:199 CONV_THRESH_ARCSEC = 0.01`，判据 `√(x00²+y00²) < 0.01″`」；`§11.2:256-269`「返回码/失败语义」给出的失败清单是：C ABI 层 NULL 参数/C++ 异常、求解器层「三角形匹配 0 匹配或 iter_trans_solve 全阶失败」、编排层 4 个 `BLOCK_MISSING`/`PLATESOLVE_FAILED` ——**其中不存在"迭代未收敛"这一失败条件**。文件头 `:19` 自述「iterative_reproject 收敛阈值: sqrt(x00² + y00²) < 0.01"」。
- 现状：`converged` 只是函数内局部量，**`IterativeReprojectResult` 结构体没有对应字段**（`ipv_solver.h:16-25` 全部成员 = `trans/matched/ra0/dec0/n_matched/n_iterations/convergence/success`）。函数 `:334-337` 在未收敛时只写一行 INFO 日志。三个调用点对结果的唯一判据是 `:612 / :1012 / :1311` 的 `if (!rep_result.success || rep_result.n_matched < 3)`；`result.convergence` 在 `:601/:606-610/:1001/:1006-1010/:1305-1309` 只进日志。`:293-297` 的 `valid_inliers < 3` 出口 `break` 同样不改 `success`（此时 `inliers_cur` 是上一轮的 ≥3 集合）。另外 `MAX_ITERS=5` 耗尽 ⇒ 至多 5 次中心修正，`:320-322` 重算 `conv` 后 `trial++`，循环条件为假而退出，`converged` 保持 false。
- 差在哪：判据方向已钉死——`while` 用 `conv > 0.01` 作继续条件，即"未收敛"是**本应触发拒绝**的状态；把它从 `success` 与结构体两面都拿掉，只会**减少**被拒的解 ⇒ **偏绿 / fail-open**。并且这不是"调用方忘了读"，而是接口**不提供**这个位：外部想判也只能自己重算 `conv = √(x00²+y00²)` 再猜阈值，而 0.01 是 :199 的函数内局部常量。
- 后果：`trans` 的常数项 `x00/y00` 就是"当前切点还差多少角秒"。未收敛即出 ⇒ `result.ra0/dec0`（:342-343）带着最多 5 轮修正后仍未消尽的偏差进入 `extract_wcs_sip(rep_result.trans, rep_result.ra0, rep_result.dec0, …)`（**:758 / :1146 / :1438**），直接成为产品 WCS 的 **CRVAL**，同时 CD（`§2:17` F2 `trans.linear/3600`）与 SIP（F3 `cd_inv·trans.x_ij`）都是在这个偏掉的切点的 gnomonic 切平面上拟合的 ⇒ 失真的对外主张 = "CRVAL/CD/SIP 是天测解"，以及 `PLATESOLVE.md:317-325` F1 的 `|ΔCRVAL|≤1″`。真实场景（初值 `ra0/dec0` 偏离数十角秒）下 5 轮不收敛完全可能：`conv` 的收缩率由 gnomonic 反投影决定，代码没有任何发散/慢收敛检测。
  **本批可指的生产触发路径逐跳**：`ipv_entry.cpp:675`（`PLATESOLVE.md:232` 标"生产入口"）→ `:711 do_solve_from_detections_v1_impl` 调用 → `:594 s->solve_from_detections_v1(…)` → `ipv_solver.cpp:1473` → `:1534 solve_post_select(…)` → `:1187` → `:1299 iterative_reproject(…)` → `:347 success` → `:1311` 门不含收敛 → `:1437-1440 extract_wcs_sip(..., rep_result.ra0, rep_result.dec0, ...)` → `IpvWcsResult.crval`（`§11.1:254`「CTYPE/CRVAL/CRPIX/CD … 写回 :2003-2010」）。
- 定级：**S1**（触发路径逐跳已写出，`§11.1:217/232` 权威指认生产入口）
- 怎么算修好：① `ipv_solver.h:16-25` 的 `IterativeReprojectResult` 增 `bool converged`（并考虑把 `CONV_THRESH_ARCSEC` 提到接口面而非留 :199 局部）；② `:347` 改为 `success = converged && inliers_cur.size() >= 3`；③ 三个调用点 `:612/:1012/:1311` 的失败文案区分"匹配不足"与"未收敛"，并让 `fail_result.error` 携带 `conv` 值；④ **必须同批**：`PLATESOLVE.md §11.2:263-265` 的失败语义清单补"迭代未收敛"这一条（否则文档与代码打架），且 `§9:187` 的"只约束自洽收敛、不是精度界"这句要保留——收紧判据使原本勉强通过的帧改判失败，**F1（`§11.4:317` 要求求解成功）与任何以真实帧为输入的既有用例可能因此从绿变红，这属于把 fail-open 改成 fail-closed 的正确结果，不得反过来放松 0.01 或 5 来"修红"**。
- 置信：**CONFIRMED**（结构体成员、三个门、局部标志的赋值路径均逐行核对；"5 轮不收敛在真实帧上会发生"这一点属机理推断，实测需要一次带偏差初值的求解跑，见"读不动"节）

### CR-44-13 · 三处 `min_pairs_hi` 恰等于多项式系数个数 ⇒ 生产允许零自由度拟合，且其 RMS 收敛门在零自由度下恒真
- 轴：R（恒真判据）＋ C（同仓两套"点数/系数"约定，已发生漂移）
- 位点：`ipv_solver.cpp:656-657`（同形副本 `:1052-1053`、`:1349-1350`）
  ```cpp
  const int min_pairs_hi = (order_hi == 3) ? 10 :
                            (order_hi == 2) ? 6 : 3;
  ```
  与 `:680-681`
  ```cpp
  bool conv_rms = (hi_result.rms > 0) ?
                  (drms < hi_result.rms * 0.01) : true;
  ```
- 上位依据：条文面把这三个数当**实现选择**登记：`PLATESOLVE.md §11.1:242`「`iter_trans_solve` … 迭代重投影多项式拟合（order 1→3）」、§11.3 `:292-293` 描述 fit_sip 路径时未涉及最小点数。`ipv_solver.cpp:655` 注释自述真值源：「AT_MATCH_REQUIRE_CUBIC=10, QUADRATIC=6, LINEAR=3 (ipv_itertrans.cpp 内 static 常量)」——已核实：`ipv_itertrans.cpp:42/:44/:46` 确有 `static constexpr` 三常数，**文件内 static 故本文件不可引用** ⇒ 本文件是三份手抄副本（共 3 处、9 个字面量）。科学侧条文面**无一处**规定"最少点数 = 系数个数"。
- 现状：二维 `order` 阶多项式系数个数 = `(order+1)(order+2)/2` ⇒ order2=6、order3=10，与 `min_pairs_hi` 逐值相等（已复算）。于是 `:664/:1060/:1357` 的 `hi_matches.size() >= min_pairs_hi` 允许**恰好等于系数个数**的样本进入 `at_recalc_trans`；零自由度最小二乘 ⇒ 残差全为机器精度级 ⇒ `hi_result.rms` ≈ 0（若为精确 0 则 `:680` 走 `: true` 分支，**无条件判收敛**）。同时 `:679` 的 `abs(dn)<=1` 在样本已被钉死时也不提供新信息。
- 差在哪：这不是"门太松"，而是**该门在临界点上不携带任何信息**：`rms` 相对变化判据的前提是残差反映模型失配，而零自由度解的残差恒为零（无论模型对错），所以 `drms < 0.01·rms` 必然成立 ⇒ 判据恒真。前言 §2 R 轴列的"恒真断言"形态在此成立。
- 后果：`rep_result.trans = hi_result.trans`（`:695 / :1088 / :1385`）被这条"收敛"确认为精化结果，随后进入 `extract_wcs_sip` 产出 CD/SIP，并进入 `cache_last_inliers_`（`:775 / :1163 / :1457`）成为对外 9 列 inlier 缓冲的来源 ⇒ **`rms_px`/`rms_arcsec` 这两个对外数字可以在只有 6（或 10）颗星时被"证明"极好**，而该解没有任何检验自由度。对应失真主张 = `PLATESOLVE.md §11.4:317-321` F1 的 `rms_arcsec ≤ 0.5″` 与"量测域 = 内点集"的口径（内点数 = 参数个数时该口径退化为自证）。
- 定级：**S1**（路径同 CR-44-12；`:1346-1390` 段整体在 `solve_post_select` 生产链上，`:1437` 出产品）
- 怎么算修好：① 三处 `min_pairs_hi` 改为**派生式** `(o+1)(o+2)/2`，并把下限抬到"至少 K + 冗余度"（与 `ipv_sip.cpp:292` 已有的 `K+1` 约定对齐，或按负责人裁决统一）；② `at_recalc_trans` 出口给 `dof = n - K`，`dof <= 0` 时 `conv_rms` 不得判收敛；③ `:680-681` 的 `: true` 分支改为拒绝方向；④ **同批防误红**：任何以 6 或 10 颗星为 fixture 且期望 `success=1` 的用例会改判红——那是本条要的效果，需一并把 fixture 抬到 K+1 以上，**不得通过把 `min_pairs_hi` 调得更低来消红**；⑤ `PLATESOLVE.md §9` 增条目登记"最小内点数"及其导出式与冗余度要求，`:317` F1 的 `n_pairs≥12` 与之对齐说明。
- 置信：**CONFIRMED**（10/6/3 = 系数个数已复算；三处副本与 static 真值源已核对；"零自由度 ⇒ rms 恒 ~0 ⇒ 相对判据恒真"为代数必然，`rms==0` 的精确零与否只影响走哪一支，两支都判收敛）

### CR-44-14 · 匹配半径 5.0″ 在 9 个生产位点以裸字面量重复，另有 2 处头文件默认实参构成第二来源；上位条文只点名其中 1 个位点
- 轴：C（同一阈值的两处入口/多份副本）＋ S（§9 已自判 UNJUSTIFIED，本条补的是"登记面不完备"）
- 位点（**本批全部取值位点与字面量**，逐条列出）：
  1. `ipv_solver.cpp:534` `U, W, tri_result.top_pairs, 5.0, it_order);`
  2. `ipv_solver.cpp:660` `U, W_final, rep_result.trans, 5.0);  // 5" tolerance`
  3. `ipv_solver.cpp:670` `U, W_final, hi_result.trans, 5.0);`
  4. `ipv_solver.cpp:936` `U, W, tri_result.top_pairs, 5.0, it_order);`
  5. `ipv_solver.cpp:1056` `U, W_final, rep_result.trans, 5.0);`
  6. `ipv_solver.cpp:1065` `U, W_final, hi_result.trans, 5.0);`
  7. `ipv_solver.cpp:1271` `U, W, tri_result.top_pairs, 5.0, it_order);`
  8. `ipv_solver.cpp:1353` `U, W_final, rep_result.trans, 5.0);`
  9. `ipv_solver.cpp:1362` `U, W_final, hi_result.trans, 5.0);`
  检索式：`grep -n "tolerance\|5\.0\|MATCH_RADIUS\|radius" <三个文件>` ⇒ `ipv_sip.cpp` 与 `ipv_triangle.cpp` 本批**不含**该半径的取值位点（triangle 的 `tolerance` 形参是另一个量 0.002，见 CR-44-7）。
  **第二来源**（同值、异载体，改一处不会传导）：`ipv_itertrans.h:99 double tolerance_arcsec = 5.0`（`iter_trans_solve` 默认实参）、`ipv_itertrans.h:156 double tolerance_arcsec = 5.0`（`at_match_lists` 默认实参）；另 `:84`「atMatchLists 匹配半径 (角秒, 默认 5.0)」、`:144`、`:149` 三处注释重述该值。⇒ **同一数字共 11 处实体（9 实参 + 2 默认实参）+ 3 处注释**。
- 上位依据：`PLATESOLVE.md §9:189-191`「三角形匹配容差 5.0″：`ipv_solver.cpp:660` 实参。量纲 arcsec；**阈值来源 UNJUSTIFIED**（无推导、未随像素尺度归一），登记为待标定项，标定前其引用面 = 待标定项登记」；§3 `:34` 伪代码「matches = kd_match(…) tol=5.0"」。
- 现状：9 个实参位点全部等于 5.0、2 个默认实参也等于 5.0 ⇒ **本批内数值漂移尚未发生**（这是本批的独立取证结论，与邻批立案不冲突）。但：①条文只钉 `:660` 一处，其余 8 处不在任何登记面里，"标定前其引用面 = 待标定项登记"这条纪律对那 8 处不成立；②任何一位调用方省略实参即从**另一处**字面量取值；③`AGENTS.md §6` 的"不硬编码…由 profile 决定"虽针对线程/ISA，但 §1 的"常数须有推导"口径在此同样未满足。
- 差在哪：一个被权威条文判定"未随像素尺度归一"的秒级半径，同时以 11 份互不关联的字面量存在 ⇒ 标定（无论定成 1″ 还是 s0 的函数）必须 11 处齐改，且**没有任何机制能发现漏改**。
- 后果：将来的标定动作极可能只改 §9 点名的 `:660`，从而在生产链另外 8 个位点留下与新标定值不同的旧半径 ⇒ 同一帧在 `iter_trans` 阶段与 `hi_order_rematch` 阶段用不同的匹配半径，产生两条互相矛盾的内点集合，且 `rms_arcsec` 与 `n_pairs` 分别来自不同半径下的匹配（对外 F1 门的量测域即被污染）。
- 定级：**S2**（当前 11 处等值 ⇒ **未**产生错值；后果是"标定一发生就异化"的结构性风险，故不升 S1）
- 怎么算修好：① 在 `ipv_itertrans.h` 增 `constexpr double IPV_MATCH_RADIUS_ARCSEC`，9 个实参位点与 2 个默认实参一律引用它，3 处注释改为回指 §9；② §9:190 的行号锚改为"该常量的定义处 + 全部消费位点清单"，并删掉 §3:34 的重复数值（改回指 §9，与 `ASTROMETRY.md:211`「本节…是唯一事实源，重述一律回指本节」同纪律）；③ **别把 5.0 直接换成 1.0**：`ASTROMETRY.md §11a:223` 的"匹配半径 1.0″（唯一值）"属 **G-P1-WCS-CLOSURE 外部闭环指标**（唯一实现 `eng/tools/astrometry/closure_metric.py:51`），与本求解器半径是两个适用域，不可套用（本批明确不主张二者应同值）。
- 置信：**CONFIRMED**（11 处位点逐一贴出并核对取值；条文覆盖范围核对）

### CR-44-15 · 对外 9 列 inlier 缓冲用 NaN 表示缺失，而公开 API 合同未声明 NaN；同处给出的残差定义式与实算式不是同一个量
- 轴：C（合同与实现说的是两个量）＋ R（非数跨对外接口）
- 位点：`ipv_solver.cpp:1801-1802`
  ```cpp
  double res_x = valid ? (xi_actual_asec  - xi_pred_asec)  / s0 : std::nan("");
  double res_y = valid ? (eta_actual_asec - eta_pred_asec) / s0 : std::nan("");
  ```
  与 `:1766`（索引越界 ⇒ `for (int k = 0; k < 9; ++k) row[k] = std::nan("");`）
- 上位依据：`lib/algorithms/platesolve/cpp/ipv/include/ipv_api.h:240-249`「字段约定 (out_buffer 每行 9 个 double…)：**[6] residual_x_px - 残差 x = det_x - pred_x (像素)**；[7] residual_y_px - 残差 y = det_y - pred_y；[8] residual_dist_px - 残差距离 sqrt(res_x² + res_y²)」，**通篇未给 NaN 任何一格语义**；`PLATESOLVE.md §11.2:260-262` 把 `error_msg` 与 POD 结果列为权威输出面；`§11.1:252` 登记 `cache_last_inliers_（WCS Gate v2 双层闭环）` ⇒ 该缓冲的下游是**门**。
- 现状：实算式是 `(实际投影角秒 − 预测角秒)/s0`，行内的 `u_star.x`（即 det_x，见 :1805 写 row[0]）**完全不参与 res_x 的计算**；函数自己的注释 :1774 也承认改了定义——「v3.1 修复: 残差 = (实际W - 预测W) / s0, 与 extract_wcs_sip 的 RMS 计算一致」。两个定义之间差一项 `(det_x − xi_actual/s0)`（像素坐标与"星表位置换算回像素"之差）。NaN 侧：越界与投影无效两种情况写同一种 NaN，调用方无法区分。
- 差在哪：对外合同声明的公式与实现的公式不是同一个表达式，且合同声明的 NaN-free 语义被实现违反（无声明地注入 NaN）。
- 后果：任何按 `ipv_api.h:247` 复算/校验 `[6]` 列的消费方（WCS Gate v2 类诊断，`§11.1:252`）得到的期望与实得不一致；NaN 行若进入 median/p95/求和统计，按语言默认语义会**污染整列统计**（median 含 NaN → NaN，求和含 NaN → NaN），即门读数变 NaN；若消费方用"NaN 视为不参与"的实现，则**静默丢样本**而样本数不报。
- 定级：**S2**（位点在生产链 `:1457` 之后由 `ipv_get_last_inliers` 导出，`ipv_entry.cpp:473-479` 已核；但"造成哪个对外数字失真"需读消费方对 NaN 的处理才能定，故不升 S1）
- 怎么算修好：① `ipv_api.h:240-249` 的 `[6]/[7]` 定义式改为实现式（**或**把实现改回定义式——先由负责人裁决哪个是想要的残差；两者都是"内点集残差"，但一个含中心项一个不含）；② 同批在 `ipv_api.h` 增列 NaN 语义（哪几列可为 NaN、两种 NaN 是否需可分）并让 `PLATESOLVE.md §11.2` 登记之；③ 消费方（`ipv_get_last_inliers` 的每个调用者）必须显式过滤并**同时报告被丢的行数**——否则改了定义仍判不出漏样。
- 置信：**PARTIAL**（缺的那一角 = 消费方对 NaN 行的处理方式；`git grep -n "get_last_inliers\|last_inlier_count" -- 'lib/**'` 只到 `ipv_entry.cpp:473-479` 为止，orchestrator/tools 侧的读数代码未在本批独占面内）

### 3.3 本文件"核过为合规、不报"的项（附检索式）
- **`gnomonic_inverse_proj` 的公式与注释一致**：:59-65 注释式与 :90-94 实现逐项对应（`asin` 项含 `eta·sin c·cos dec0/rho`、`atan2` 分母含 `-eta·sin dec0·sin c`），且 `rho<1e-12` 已在 :78 先返回，除零不可达。
- **`ra` 归一化到 [0,360)**：:100-101 双 while 界宽远大于 360 的输入（`ra0 + atan2` 的偏移 ≤ 半周），不构成无限循环。
- **阶数回退不掩盖失败**：`:532-545` 三次尝试全失败 ⇒ `:571-579` 写 `fail_result.error` 并 return，`§11.2:263-265` 已登记该语义。
- **`robust_refine` 的失败回退**：`:726-738` 仅当 `rob.success && !rob.fallback` 才替换主解，回退时保留 `hi_order_rematch` 结果 ⇒ `PLATESOLVE.md:243`「失败回退不破坏主解」明文认可该行为，按前言 §4（条文已声明的既有口径）不报。
- **`solve_post_select:1213` 在 `:1228 !selection.success` 检查之前先跑 triangle_match**：selection 失败时 U/W 为空 ⇒ `ipv_triangle.cpp:528-531` 立即 warn 返回失败，`:1248` 随即终止 ⇒ 只浪费一次空调用，不改结果、不吞失败。
- **`WcsFitResult fail_result{}` 与 5 处入口的 `last_inliers_ = SolveInlierCache{}`**：`:408/:820/:1204/:1495/:1570/:1642` 逐入口重置陈旧 inlier 诊断，与 `:401-403` 注释声明的 RESCUE-FD-05/B4-P1-5 契约一致；`fail_result.error` 在每处 return 前均被 `snprintf` 写入非空文案（`:402/:489-490/:515-520/:575-576/:616-617` 等）⇒ 无空 error_msg 出口。
- **`1e18` 哨兵在 `at_match_lists` 侧**：`:659/:1055/:1352` 传入的半径 5.0″ 使哨兵星的距离必然超限而落选 ⇒ 该阶段不漏；**残留风险在 `extract_wcs_sip` 不做此过滤**，但 `ipv_wcs.cpp` 非本批独占面 ⇒ 登记进"读不动/需实跑"节，不在本批立 finding。
- **`elapsed_ms` 与各 `[阶段]` 计时日志**：不改数值路径。
- **`img_n_target` 被 60 硬覆盖**这一事实本身已由 `ipv_types.h:227` 显式登记（`shadowed: img_n_target (=60 硬覆盖)`）⇒ 按前言 §4 不报为"死参数"缺陷；本批报的是它与 `PLATESOLVE.md §4a.2:96` 导出要求的差异（已入处置表 :442 行，因缺 `ipv_select.cpp` 一侧的证据，不独立成条）。

### 3.4 ④「异常是否跨对外接口」的本批取证结论
`git grep -n "throw\|try\s*{\|catch\|\.at(\|substr\|std::stoi\|abort\|assert(" -- <本批三文件>` ⇒ **零命中（exit 1）**。
⇒ 三个文件内**无显式抛出、无越界检查型访问器、无 abort**；退出面只有：标志位（`success`/`valid`/`converged`）、
`fail_reason`/`error` 字符串、`order=0` 退化返回、以及 `std::vector` 隐含的 `bad_alloc`（由
`ipv_entry.cpp` 的 try/catch 接住，见 `PLATESOLVE.md §11.2:258-260`「C++ 异常路径填充 set_error_msg」）——
**与 §11.2:265「不抛异常不崩溃」一致，不报为缺陷。**
本批真正跨对外接口的"非正常值"不是异常而是两枚**未声明的哨兵**：`1e18`（无效投影，:268/:634/:1033/:1330，
读取侧另用 1e10 判，:288）与 `NaN`（inlier 缓冲，:1766/:1801-1802，合同 `ipv_api.h:240-249` 未声明）⇒ 已立 CR-44-15。

---

## 4 本批最高危三条（只挑，不复述）

1. **CR-44-12** · `ipv_solver.cpp:347` 的 `success` 不含收敛项、`IterativeReprojectResult`（`ipv_solver.h:16-25`）
   根本没有 `converged` 成员 ⇒ 未收敛的切点直接进入 `extract_wcs_sip` 的 CRVAL/CD/SIP
   （`:1437-1440`）。S1，生产链逐跳已在条内写出。
2. **CR-44-13** · `ipv_solver.cpp:656-657`（另两副本 `:1052-1053`、`:1349-1350`）把最少配对数钉在
   多项式系数个数（10/6/3）⇒ 零自由度拟合合法，而 `:680-681` 的相对 RMS 收敛门在零自由度下恒真。
   S1，对外失真是 `rms_px`/`rms_arcsec`/`n_pairs` 这三个 F1 门读数本身。
3. **CR-44-1** · `ipv_sip.cpp:361` 的 `+cx` 使 SIP 拟合自变量变成绝对探测像素，而 `:473-477` 按
   `(x−CRPIX)` 发布 ⇒ 畸变场整体平移半幅画面。S2（`fit_sip` 生产不可达，见 CR-44-5），
   但它是本批唯一"式子写错"级别的科学面缺陷，且它的定级取决于 CR-44-5 那两份登记面谁作废。

## 5 我读不动或需要实跑才能定的

| 待决事项 | 缺的那一角 | 该跑什么（本批未跑，只读纪律） |
|---|---|---|
| CR-44-12「5 轮内不收敛在真实帧上确实会发生」 | 机理成立但无实测分布：`conv` 的逐轮收缩率取决于初值偏差与 gnomonic 反投影 | 在 `run/<轮次>/` 下取一批 `ra0/dec0` 人为偏 10″/30″/60″ 的帧，读 `ipv_solver.log` 的 `iter %d: … conv=%.4f"` 序列（该日志已由 :334-337 埋点），统计 `converged=false` 占比。**不得**为拿这个数改任何阈值 |
| CR-44-13「`rms` 恰好为 0 还是机器精度级」 | 走 `:681` 的 `: true` 支还是 `drms<0.01·rms` 支 | 对 6 颗星 / order=2 的 fixture 打点 `hi_result.rms`；两支都判收敛，所以这决定的是"要不要连带修 `rms<=0` 分支"，不影响本条成立 |
| CR-44-14 的收敛判据改法 | `converged` 上抛后 F1 与既有真实帧用例的改判红面积 | 需先跑 `PLATESOLVE.md §11.4 TEST-WCS-DESIGN-001`（:315-350）全套，统计有多少帧此前依赖"未收敛仍通过" |
| CR-44-15 的定级（S2→S1？） | `ipv_get_last_inliers`（`ipv_entry.cpp:473-479`）之下的消费方如何对待 NaN 行；不在本批独占面 | 读 orchestrator / WCS Gate v2 的读数侧（属邻批文件），或写一个只喂 NaN 行的读端小夹具 |
| `W_final` 的 `1e18` 哨兵是否会漏进产品 RMS | `extract_wcs_sip`（`ipv_wcs.cpp:229` 起，非本批面）是否对 W 值做有效性过滤 | 需读 `ipv_wcs.cpp:484-516` 的 RMS 统计是否跳过哨兵。本批只在处置表 :268/:288 行登记了"写入侧 1e18 / 读取侧 1e10 两个独立字面量"这件事 |
| CR-44-1/CR-44-2 涉及的 Shupe et al. 2005 §A 原文 | 本仓核验状态停在 bibcode 级（`ASTROMETRY.md:259`、`SCIENTIFIC_REFERENCES.md:119`"未逐页核验"） | 取 ASPC 347, 491 正文核两件事：①SIP 自变量是否为像素偏移；②正向 A/B 的一次项是否被禁止/要求并入 CD。第①点本批靠 `ASTROMETRY.md:72-73/99-105` 仓库自己的冻结条文即可定案，第②点（CR-44-6）只能靠原文或负责人裁决 |
| CR-44-8「`1/(1±p)` 带是否为参考实现口径」 | `PLATESOLVE.md:181` 只写"astrometry.net 语义对照 (05 spec)"，无项目/版本/文件:行 | 按 `AGENTS.md §8` 派子代理核 astrometry.net 的 `percent_scale_range` 实现（给项目+版本+文件:行），再决定是改注释还是改式 |
| `IPV_TRIANGLE_MATCH_MAX_VOTE_OPS = 2e9` 是否会误杀 | 生产 `img_n_target` 被硬覆盖为 60（`:1210` 等），故不触发；但若 §4a.2 的导出式将来生效并可 >65 | 需读 `ipv_select.cpp:260-330` 确认哪个 n_target 最终生效（见下节"派单与清单差异"第 3 条） |

## 6 派单与清单差异

1. **「3 个生产实现文件」对 `ipv_sip.cpp` 不成立。** 本批清单把它列在生产实现文件里，但
   `PLATESOLVE.md §11.3:291-295`（DISP-WCS-003）明文「**非生产** `fit_sip` IRLS+Huber…仅被非生产
   `build_wcs`（`ipv_wcs.cpp:157-165`）消费」，实测其唯一可达调用是
   `lib/algorithms/platesolve/cpp/ipv/test/test_synthetic.cpp:277`。按前言 §5.5「以清单为准」，
   我仍逐行审了它并交全表，但其 finding 依 §3 硬门槛一律封顶 S2。**反向差异更要紧**：
   `docs/architecture/production_call_paths_stage1.csv:4` 与 `docs/TRACEABILITY.csv:21` 却把 `build_wcs`
   写进生产操作/实现锚 ⇒ 两文档面互相矛盾，已立 **CR-44-5**。
2. **派单重点②（三角形索引与哈希常数：表大小、素数、扰动量）在本批清单的三个文件里不存在。**
   `ipv_triangle.cpp:11-16` 自述已把稀疏 `unordered_map<VoteKey,double>` 投票表**换成稠密 2D 数组**，
   并声明"不做 `sort_triangle_array` + `find_ba_triangle` 二分加速"；
   `git grep -n "sort_triangle_array\|find_ba_triangle\|hash_offset\|0x9e3779b9\|素数\|扰动" -- 'lib/algorithms/platesolve/**'` ⇒ 零命中。
   本批按重点② 的要求列了"全位点"，但可列的是稠密矩阵规模、`ba>0.9` 剪枝、票数三档门、
   `2e9` 预算这类（见 §2.1 表），**没有一个哈希/素数/扰动常数可取证**。若确实存在这类常数，
   它在别的模块（不在本批独占面）。
3. **重点①「多个生产位点共用的那个匹配半径」在本批是两件事，需分清。** 数值上：5.0″ 共 11 处实体
   （9 实参 + 2 头文件默认实参），**取值已全部核为一致 ⇒ 未发生数值漂移**（见 CR-44-14）。
   但**语义漂移已经发生且已立案**：`PLATESOLVE.md:33-34` 把 0.002 归给 `build_triangles`（叫 `scale_tol`）、
   把 5.0″ 归给"kd_match 三角形匹配"，而代码里 0.002 是**三角形投票阶段的描述符半径**、
   5.0 是**匹配之后的 `iter_trans`/`at_match_lists` 半径** ⇒ 两个数各自被挂在了错误的阶段上，
   §9:192 更把 0.002 转述为"尺度容差（各向异性 0.2%）"（全仓无该门实现）。此为 CR-44-7，
   与邻批立案的"第二来源/漂移"同源但位点独立。
4. 行数与清单一致：516 + 1819 + 737 = 3072，与派单"合计约 3072 行"吻合；基线 HEAD 与派单一致
   （`git log --oneline -1` ⇒ `c8f64e9a 交接文档按新阶段改写：一切测试与端到端暂停，只读推导找错先行`）。
   注意该提交标题声明"一切测试与端到端暂停" ⇒ 本批**未**跑构建/ctest/`run_checks.py`/任何 `eng/**`，
   与前言 §1 只读纪律一致；因此本批所有 PARTIAL 项都标了"该跑什么"而不是"已跑过"。

## 7 收工核对

开工原文（本节点第一次 `git status --porcelain`）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工原文：

```
?? ACSD整治工作包_AUDIT-06.zip
?? photom.rst
?? site/
```

**两次输出不一致，差一项 `?? photom.rst`，且该项不是本节点写的。** 取证：
`stat -c '%y %s %n' "F:/Astro dev/Astro CS Normalization Database/photom.rst"` ⇒
`2026-09-25 21:20:47 +0800 12896 photom.rst`（本会话期间产生）；首两行是
`.. File Photom.rst` / `.. include:: global.rst`（RST 文档体裁，形似 astropy 侧文档原文）。
本节点在整个 CR-44 过程中的全部写入只有
`独立审计/证据/通读-CR-44.md`（仓库外），
零 `git` 写、零 `git config` 写、未跑构建/ctest/`run_checks.py`/任何 `eng/**`、未 import 仓库 Python。
⇒ 判定：该文件由**同期并行的另一节点/批次**落在仓库根目录。按 `AGENTS.md §7`
「新根目录条目先登记并经负责人确认」，这是一个**未登记的根目录条目**，且按前言 §1 基线
（应为 `ACSD整治工作包_AUDIT-06.zip` + `site/`）属偏差 ⇒ 上呈负责人处置（登记或清除），
本节点不代为移动或删除。

---

## 附：本批 finding 索引

| 编号 | 一句话结论 | 轴 | 定级 | 置信 |
|---|---|---|---|---|
| CR-44-1 | `fit_sip` 把 SIP 自变量取成绝对 `det_x` 却按 `(x−CRPIX)` 发布系数（畸变场平移半幅） | C/S | S2 | CONFIRMED |
| CR-44-2 | `eff_order` 上界 4 与上位伪代码"order 2-3"不一致 | C | S3 | CONFIRMED |
| CR-44-3 | 四处注释把出处写成 PLATESOLVE.md 不存在的"数值风险"节 | C | S3 | CONFIRMED |
| CR-44-4 | "系数过大"兜底检归一化系数；发布系数算出 `max_coeff_orig` 后从不使用 | R | S3 | CONFIRMED |
| CR-44-5 | `build_wcs`/`fit_sip` 是否生产：PLATESOLVE §11.3 与 production_call_paths_stage1.csv 互相矛盾 | C | S2 | CONFIRMED |
| CR-44-6 | 正向 A/B 一次项是否须剔除单位线性：仓内条文只对被逆向 AP/BP 规定 ⇒ 一手依据缺口 | S | S3 | UNPROVEN |
| CR-44-7 | 文档名为"尺度容差 0.002／各向异性 0.2%"的量实为三角形描述符半径；各向异性门 `lib/**` 零实现 | C/S | S2 | CONFIRMED |
| CR-44-8 | 尺度带六处注释自称 ±20%，算式是 [0.8333, 1.25] 非对称带 | C/R | S3 | CONFIRMED／惯例侧 UNPROVEN |
| CR-44-9 | 自适应星数与提前停止整段不可达，文件头仍当现役能力；三处字面 3 互不关联 | C/R | S3 | CONFIRMED |
| CR-44-10 | `ba>0.9` 剪枝无出处、注释点名不存在的 `AT_MATCH_RATIO`、理由方向自相矛盾 | S/C | S2 | PARTIAL |
| CR-44-11 | 退化三角形门只挡"三点重合"，宣称的"三点共线"从未检测 | R/C | S2 | CONFIRMED |
| CR-44-12 | `iterative_reproject` 的收敛既不参与 `success` 也不跨接口 ⇒ 未收敛切点发布为 CRVAL | R/D | **S1** | CONFIRMED |
| CR-44-13 | `min_pairs_hi` = 系数个数 ⇒ 零自由度拟合合法且相对 RMS 收敛门恒真 | R/C | **S1** | CONFIRMED |
| CR-44-14 | 匹配半径 5.0″ 共 11 处实体（含 2 处默认实参＝第二来源），条文只钉 1 处；数值尚未漂移 | C/S | S2 | CONFIRMED |
| CR-44-15 | 对外 9 列 inlier 缓冲注入未声明的 NaN，且合同给的残差式与实算式不是同一个量 | C/R | S2 | PARTIAL |

合计 15 条：S1×2、S2×7、S3×6；CONFIRMED×12、PARTIAL×2、UNPROVEN×1。
§2.5 处置表合计 24 + 24 + 29 = 77 行（三个文件各自逐数值列全，无合并漏项；行数由对本稿
`^| ` 连续块的程序化计数得出）。
