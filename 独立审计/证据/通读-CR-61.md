# 通读 CR-61 —— 创新点②"跨帧绝对 SNR 最优叠加"正本实现（只读代码通读审计）

- 基线 HEAD=`c8f64e9a`；仓库 `F:\Astro dev\Astro CS Normalization Database`
- 派单前言：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 全条已读，作硬约束）
- 本批重点：① 权重链（叠加权重是**什么方差的倒数**、缺失如何表示、支持度/合法性是否参与）；② 方差传播（"排除自身"是否在此、主语是什么、相关性是否被静默丢成对角）；③ 归一化分母与分子是否同源；④ 每个常数五档处置；⑤ 权重模式类键"合同声明不存在、代码仍静默读"形态。

## 清单与仓库实际路径核对（开工即登记）

派单清单原文 6 项，其中 4 项**路径与仓库跟踪面不符**（文件名唯一、可无歧义定位到实际文件），2 项完全一致。按前言 §5.5"以清单为准并指出差异"处理：**差异如实登记，按实际跟踪文件读**，不因为路径写法不同而漏项。

| 清单原文 | 仓库实际跟踪路径 | 差异 |
|---|---|---|
| `lib/algorithms/integration/phase2_integrate.cpp` | `lib/algorithms/integration/v6/src/phase2_integrate.cpp` | 缺 `v6/src/` 一截 |
| `lib/algorithms/integration/variance_propagation.cpp` | `lib/algorithms/integration/v6/src/variance_propagation.cpp` | 缺 `v6/src/` 一截 |
| `lib/algorithms/integration/weight_chain.cpp` | `lib/algorithms/integration/v6/src/weight_chain.cpp` | 缺 `v6/src/` 一截 |
| `lib/algorithms/integration/phase1_product.h` | `lib/algorithms/integration/v6_phase1/include/astrocs/v6/phase1_product.h` | 目录与层级全不同 |
| `lib/algorithms/resample/p3_rsmp.h` | `lib/algorithms/resample/p3_rsmp.h` | 一致 |
| `lib/algorithms/resample/p3_nan_mask_test.cpp` | `lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp` | 缺 `tests/p3rsmp/` 一截 |

开工 `git status --porcelain` 原文：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 逐文件通读进度

- `lib/algorithms/integration/v6/src/weight_chain.cpp` —— 已读：是（表 26 项，findings CR-61-1…61-5）
- `lib/algorithms/integration/v6/src/variance_propagation.cpp` —— 已读：是（表 14 项，findings CR-61-6…61-9）
- `lib/algorithms/integration/v6/src/phase2_integrate.cpp` —— 已读：是（表 41 项，findings CR-61-10…61-18）
- `lib/algorithms/integration/v6_phase1/include/astrocs/v6/phase1_product.h` —— 已读：是（表 17 项，findings CR-61-19…61-22）
- `lib/algorithms/resample/p3_rsmp.h` —— 已读：是（表 21 项，findings CR-61-23、61-25、61-26）
- `lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp` —— 已读：是（表 19 项，findings CR-61-24、61-27）

<!-- PROGRESS: 6/6 -->

## 本批共用事实（先钉住，后面反复引用）

- F1 生产链接事实：`CMakeLists.txt:1047` 原文 `# 不链 astrocs_v6_phase2_integrate: 后者重复编译 upm/rejection/sampler/`；该库唯一消费者是 `eng/tests/integration/v6_p2/CMakeLists.txt:20`。⇒ **phase2_integrate.cpp 全 TU 无生产触发路径**，其 finding 一律封顶 S2 并注明"缺运行时触发路径"。
- F2 生产调用事实：`weight_chain.cpp` / `variance_propagation.cpp` 由 `lib/infrastructure/scheduler/src/module_adapters.cpp` 真实调用（`compute_inverse_variance_weights` @11568、`weight_from_corrected_variance` @11903、`normalized_weight_hat_row`/`corrected_pixel_variance` @10556 区段、`variance_include_self` @10382）。⇒ 这两份文件里的缺陷可 S1。
- F3 本批口径正本条文：`docs/plugins/algorithms_phase2/13_integration.md` §4.0（单一权重口径、逐帧 `F_ref,k`、"没有可选的权重口径、口径选择键、口径枚举或口径配置项"、"Drizzle 相关…纳入 C 或以相关核/低秩近似"）、§7（"相关噪声无描述 → variance 不完备，标记"）。
- F4 权重链条文：同 §4.0 `w_k = SNR_k²/F_ref,k² = 1/σ_F,k²`，"配对性只要求**同一帧内** SNR 与 F_ref 同源（`07_noise_snr.md` §4.1）"。

---

# 一、`lib/algorithms/integration/v6/src/weight_chain.cpp`（821 行，全读）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :17, :545 | `kNodeReproductionTol` | 1e-9 | 控制点复现自检的残差上界 | 待确认 | 零提及（检索式：`git grep -n "node_reproduction\|复现残差\|NodeReproduction" -- docs eng/contracts ASTROCS_DESIGN.md`） | 放松⇒偏绿；实际影响 ≈0（见 CR-61-3：默认/bilinear 算子残差恒为 FP 量级） | CONFIRMED |
| :19 | `positive_finite` 阈值 | >0.0 | "有效"谓词（有限且为正） | 结构性不适用 | 不是科学量，是定义性谓词 | —— | CONFIRMED |
| :91 | `w=(snr/F_ref)²` | 指数 2 | 逆方差权重换算 | 公式导出 | 13_integration.md §4.0 + F4；代码按式算 ⇒ 合规 | —— | CONFIRMED |
| :107 | `1.0/variance` | —— | 1/Var(corrected) | 公式导出 | weight_chain.h:244-249（"P2b 正确权重面"） | —— | CONFIRMED |
| :127 | `frame_snr*intra_snr` | —— | 实际 SNR 合成 | 公式导出 | weight_chain.h:7（DESIGN §3.4「实际 SNR=帧级×帧内」） | —— | CONFIRMED |
| :212-224 | `b0`、`4.0`、`6.0`、`1.0` | 4 / 6 / 1 | 自然三次样条三对角主元与右端 | 公式导出 | 注释 :202-205 给出方程 `M[i-1]+4M[i]+M[i+1]=6Δ²y`（等距 h=1，M[0]=M[n-1]=0），与 07_noise_snr.md:189「可分离自然边界双三次样条」一致；代码按式算 | —— | CONFIRMED |
| :222-226 | `n-2`、`m-1`、`i+2` | —— | 回代索引 | 结构性不适用 | 数组维度/位移量，不进入科学语义 | —— | CONFIRMED |
| :234-239 | `floor(pos)`、钳 `[0,1]`、`n-2` | 0.0 / 1.0 | 节点区间与段内坐标 | 公式导出 | 07_noise_snr.md:189 ②；`h=1` 是等距网格定义 | —— | CONFIRMED |
| :244-245 | `2.0*m0+m1)/6.0`、`/2.0`、`/6.0` | 2/6/2/6 | 三次样条段内求值系数 | 公式导出 | 同 :212-224 的样条积分形式；代码按式算 | —— | CONFIRMED |
| :255-279 | `win[9]`、`-1..1`、`win[4]` | 9 / 3×3 / 4 | 3×3 中值窗与中位下标 | 文献值＋结构性 | 07_noise_snr.md:180/185「3×3 mesh 中值前置滤波（边界 replicate、无条件替换）」；窗宽 3 属该条文，`win[4]`/9 是实现的数组维度 | —— | CONFIRMED |
| :312 | `infinity()` 初值 | —— | 最近邻距离哨兵 | 结构性不适用 | 比较初值，不参与结果 | —— | CONFIRMED |
| :319, :323 | 等距并列判定窗 | 1e-12 | 判定"与最近有效点等距"从而取平均 | 待确认 | 零提及（检索式：`git grep -n "等距并列\|并列取平均" -- docs` ⇒ 07_noise_snr.md:189 只写"等距并列取平均"，无容差数值） | 放宽⇒更多点被判并列⇒填充值偏平滑；影响限于含 NaN 控制点的层 | CONFIRMED |
| :345-346 | `clip_low_/clip_high_` 未钳制取值 | 0.0 | 未启用钳制时的哨兵 | 结构性不适用 | 与 `value_range_clipped=false` 成对出现，不参与换算 | —— | CONFIRMED |
| :379 | `tol_ = max(0.0, grid_tol)` | 0.0 下限 | 负网格容差静默取 0 | 待确认 | 零提及 | 方向：负 tol→0 使 :462 位置校验要求逐位相等 ⇒ **偏红**（不是放行），无错值风险 | CONFIRMED |
| :412, :427 | `nx>=2 && ny>=2`、`max_radius_px>0` | 2 | 插值最少节点数／散点半径必填 | 结构性不适用 | 插值算子的定义域要求（1 点无法构样条），非科学阈值 | —— | CONFIRMED |
| :442 | `points.size() != nx*ny` | —— | 网格完整性格验 | 结构性不适用 | 声明一致性检查 | —— | CONFIRMED |
| :473-474 | `(dx-1)/2` | 0.5 折算 | 节点=cell 中心的几何 | 公式导出 | weight_chain.h:128-137 与 13_integration.md §4.0 层几何（`x0 = grid_origin_x+(dx-1)/2`）；代码按式算并在 :480 设门 | —— | CONFIRMED |
| :479 | `tol_geom = max(tol_, 1e-9)` | 1e-9 | cell 中心门的容差下限 | 待确认 | 零提及 | 只会**放松**调用方给的容差 ⇒ 偏绿，量级 1e-9 px，不构成错值 | CONFIRMED |
| :495-503 | `lo=grid[0]`、min/max | —— | 值域钳制区间取有效控制值 | 公式导出 | 07_noise_snr.md:189 ③「钳到有效控制值值域 [min,max]」、:190「钳制不可省（实测 E 1.007→1.44e4、负 σ −0.2627）」 | —— | CONFIRMED |
| :580 | `r*r*(1+1e-12)` | 1e-12 相对 | 散点覆盖半径的边界放宽 | 待确认 | 零提及 | 放宽⇒边界点多放行；影响限于散点层边缘像素 | CONFIRMED |
| :593-604 | `nx_-1`、`-0.5`、`grid_origin+nx*dx-0.5` | 0.5 | 定义域=cell 并集 | 公式导出 | weight_chain.h:138-141（cell 并集与最外半 cell 常数延拓） | —— | CONFIRMED |
| :619-631 | bilinear `-2`、`1.0-fx` | 1 / 2 | 双线性权重与区间上界 | 结构性不适用 | 双线性定义与数组上界 | —— | CONFIRMED |
| :634 | `kStackCap` | 512 | 栈缓冲上限，超出转堆 | 结构性不适用 | 内存布局选择，越界走 heap 分支，结果与取值无关 | —— | CONFIRMED |
| :682, :714 | `intra_snr` 无层默认 | 1.0 | 无稀疏层⇒帧内因子 1 | 文献值(条款) | 13_integration.md §4.0「frame_reconstruct → 帧级 SNR 直接参与（等权重面）」 | —— | CONFIRMED |
| :748, :763 | `g` 缺省 | 1.0 | 未声明帧增益时按 g=1 | 待确认 | weight_chain.h:273-278 自述"仅加性-only 方案 B 合法"，但 `require_frame_gain` 默认 false ⇒ 默认落在需要额外前提的一侧 | 保守方向：默认应为 true（缺 g 即红）；影响面=所有乘性归一化帧的权重 | CONFIRMED |
| :770 | `f.ref_flux > 0.0` 作"已提供"哨兵 | 0.0 = 未提供 | 逐帧 F_ref 缺失的表示法 | 待确认 ⇒ 见 CR-61-2 | 合同/科学文档零处规定"F_ref 缺失以 0 表示"（检索式：`git grep -n "ref_flux\|reference_flux" -- eng/contracts docs/science` 无此语义） | 静默回退到别的帧的 F_ref ⇒ 权重**非单调**错值 | CONFIRMED |
| :783 | `w *= g*g` | 指数 2 | 乘性归一化的权重修正 | 公式导出 | Var(corrected)=Var(y)/g² ⇒ w×g²（weight_chain.h:274-276；原引 q2-snr-smooth §2/§7 不在跟踪面，见 CR-61-1 附注） | —— | CONFIRMED |
| :810 | 等权基线 | 1.0 | 显式非生产基线 | 文献值(条款) | weight_chain.h:335-337（非闭合、非生产）；`production_allowed=false` ⇒ 不构成放行 | —— | CONFIRMED |

## findings

### CR-61-1 · `weight_from_snr` 的函数文档仍是"组内公共 F_ref"作废前口径，与本头文件自己的作废声明和 §4.0 条文相反
- 轴：C（主轴）＋S
- 位点：`weight_chain.h:235-236` `reference_flux 必须是**定义 snr 时所用的同一参考通量**（组内公共 F_ref，配对性定理；逐帧参考会丢掉 a_f²）`；实际生效点 `weight_chain.cpp:770` `const double f_ref_k = (f.ref_flux > 0.0) ? f.ref_flux : reference_flux;`
- 上位依据：`docs/plugins/algorithms_phase2/13_integration.md` §4.0（F4）"`F_ref,k` 是**逐帧**参考通量…配对性只要求**同一帧内** SNR 与 `F_ref` 同源"；同头 `weight_chain.h:39-45` 已写「本节曾写『F_ref 为组内公共常数…逐帧 F_ref 不得使用』—— 该表述**已作废**」。
- 现状：作废声明只改了「前置条件」一节与 struct 字段注释（:259-269），`weight_from_snr` 的**函数级**文档（实现者读的那一行）原样保留旧要求，并附一条与 :36 推导式（`SNR_k²/F_ref,k² = a_k²/σ_k²`）矛盾的断言"逐帧参考会丢掉 a_f²"。
- 差在哪：同一文件里对同一换算存在两条互斥的规范表述，且被作废的那条挂在**最靠近实现的位点**；这不是"前后各说一遍的两个适用域"——:36 的推导已给出逐帧才正确的结论。
- 后果：维护者按函数文档"修回"组标量闸门即复活 §9.49 定案 2 已否决的行为（多指向/多光学系统拼接全帧被拒）；对外"逐帧同源配对"主张在文档面上不自洽。
- 定级：S2（不改产物字节，改的是唯一可执行的规范面）
- 怎么算修好：把 :235-236 改写为逐帧口径并引用 `13_integration.md §4.0`；同批检查 `weight_chain.cpp:1-50` 与 `module_adapters.cpp:11544-11557` 的注释是否同源；改文不改码 ⇒ 不会判红。附注：本头与 `variance_propagation.h:4-5` 引的 `reports/RELEASE-02/q2-snr-smooth.md`、`reports/RELEASE-02/unc-prop-audit.md` **不在跟踪面**（`git ls-files reports` 只有 v19r2 的 3 个 csv，且无删除历史命中），见 CR-61-9。
- 置信：CONFIRMED

### CR-61-2 · 逐帧参考通量"缺失"用 `>0` 哨兵表示，NaN/负/0 一律静默回退到**别的帧**的 F_ref 且不留标记（生产可达）
- 轴：R（主轴）＋S
- 位点：`weight_chain.cpp:770` `const double f_ref_k = (f.ref_flux > 0.0) ? f.ref_flux : reference_flux;`，随后 :771-776 只校验回退结果有限为正，:779 `weight_from_snr(actual, f_ref_k, &w, &werr)`
- 上位依据：13_integration.md §4.0（F4）＋`weight_chain.h:34-37`「配对性…成立当且仅当**该帧**的分子分母同源」。条文面零提及"缺失以 0 表示"（检索式：`git grep -n "ref_flux" -- eng/contracts docs/science docs/plugins` ⇒ 仅 §4.0 与头注释，无缺失表示法）。
- 现状：`FrameWeightInput::ref_flux` 默认 0.0（.h:270）；NaN 与负值同样落入回退支；回退既不写错误串也不在 `WeightChainResult` 里留痕（结果只有 `reference_flux_k` 的最终值，调用方无从分辨"这帧用自己的"还是"借了别人的"）。与本文件对帧级 SNR 的处置形成对照：SNR 缺失用**显式布尔** `has_frame_snr` 且缺失即 `kUnclosedMissingFrameSnr`（:703-708）。
- 差在哪：同一文件里"缺失"有两种表示法——SNR 用布尔+拒绝，F_ref 用 0 哨兵+静默借用；借用的正是配对性定理由以保证的那个量。
- 后果：`w_k = SNR_k²/F_ref,k²` 中的 `F_ref,k` 与被估计帧不同源 ⇒ 该帧权重不是 `1/σ_k²` ⇒ 创新点②"跨帧绝对 SNR 最优叠加"的权重面对该帧错值；F_ref 借大⇒权重偏大⇒该帧被过度信任（非单调，两个方向都可能）。
- 定级：**S1**。触发路径逐跳：`module_adapters.cpp:11525-11526`（`p2_hips_prop_double(ds,"ASTROCS_REFERENCE_FLUX",&fref) && fref > 0.0` ⇒ 无该头/非正时 `has_ref=false`）→ `:11561` `in.ref_flux = fref`（**只在 `if (has_ref)` 内**，故缺键帧的 `ref_flux` 保持默认 0.0）→ `:11554` 组标量取**第一个有 F_ref 的帧**的值 → `weight_chain.cpp:770` 静默回退 → `:779/:783` → `:789 r.weights[k]` → `module_adapters.cpp:11584 snr_weights = wres.weights` 进积分权重。链路中无一处判红。
- 怎么算修好：① `FrameWeightInput` 增 `bool has_ref_flux`（与 `has_frame_snr` 同构），:770 改为"未声明⇒按 policy 决定：默认 fail-closed `kUnclosedInvalidReferenceFlux`"；② 若保留组标量回退，必须只在"组内 F_ref 全同"时允许并在结果里记 `reference_flux_fallback_count`；③ 同批把 `reference_flux_spread_noncommon`（module_adapters:11558 区段，`rel > 1e-9`）从"只报告"升为回退前置条件，否则改一处即判红另一处。
- 置信：CONFIRMED

### CR-61-3 · 控制点复现自检对默认算子恒为 FP 精度量级，注释宣称的"NaN 会在此放大为红灯"不成立
- 轴：R
- 位点：`weight_chain.cpp:545` `if (!(max_resid <= kNodeReproductionTol))`；注释 :527-528「索引/几何错位或 NaN 会在此放大为红灯」
- 上位依据：13_integration.md §4.0 只要求"实际生效算子标识与重建误差入 manifest"；条文面零规定 1e-9（检索式：`git grep -n "node_reproduction_max_abs" -- docs` ⇒ 只要求入 manifest，无阈值）。
- 现状：残差在节点处求值，:232-245 的样条求值在 `h=0` 时直接返回 `y0`、在末节点 `h=1` 时代数恒等回到 `y_{n-1}`；双线性分支（:618-631）同。NaN 在进入自检前已 :489 `nearest_valid_fill` 全部填充（全 NaN 时 :490 fail-closed），中值与样条均不产生 NaN。
- 差在哪：该门只在列步长/索引错位时才可能红，而比较的 `grid_[idx]` 是**经过 NaN 填充与 mesh 中值之后**的网格，故它既不检验"复现输入控制点"也不检验 NaN ⇒ 注释把恒绿面写成红灯面。
- 后果：`SparseReconstruction.node_reproduction_max_abs` 作为 manifest 里的"重建自证误差"永远是 0 ⇒ 该字段无证据资格（13_integration.md §8 的"控制点自身复现残差 ~0"Oracle 属同一恒真断言）。
- 定级：S3（不改产物值，改的是对外"已自检"主张）
- 怎么算修好：把自检改成对**原始输入控制点值**（填充/滤波前）的复现，或另设"滤波前后保真度"门；把 1e-9 与它真能拦住的偏差种类登记进 13_integration.md §4.0/§8。
- 置信：CONFIRMED

### CR-61-4 · 四个无出处的数值窗（1e-12/1e-12/1e-9/负容差取 0），方向逐条钉死
- 轴：S
- 位点：:319 `if (d2 < best - 1e-12)`、:323 `else if (d2 <= best + 1e-12)`、:580 `if (best_d2 > r * r * (1.0 + 1e-12))`、:479 `const double tol_geom = std::max(tol_, 1e-9);`、:379 `tol_ = std::max(0.0, layer.grid_tol)`
- 上位依据：条文面零提及（检索式见 §2.5 对应行）。
- 现状／差在哪：均为字面量、无量纲说明、无"为什么是这个数量级"的推导；:479 与 :379 都在**改变调用方给的容差**而不报告。方向：:319/:323 与 :580 是放宽（更多点判并列／边缘多放行）⇒ 偏绿；:479 只会放松 cell 中心门 ⇒ 偏绿；:379 把非法负值变 0 ⇒ 位置校验变严 ⇒ 偏红。
- 后果：影响限于含 NaN 控制点的层填充值与层边缘像素；量级（1e-9~1e-12 px）不构成错值风险，但"容差由代码自行抬升"这条一旦被复制到更大量纲的门上就会失效。
- 定级：S3
- 怎么算修好：给每个窗写单位与推导（或改成分区无关的严格比较 `d2 == best`，平方距离为整数和，本就精确可比），并把 :479 的下限改成"仅当 grid_tol 未声明（默认 1e-6）时生效"，不覆盖调用方显式值。
- 置信：CONFIRMED

### CR-61-5 · 设计默认的"帧内稀疏 SNR 面"在生产接线处被写死为空，创新点②只剩帧级一维
- 轴：D
- 位点：`weight_chain.cpp:715` `if (f.sparse != nullptr && f.sparse->present) {`、:796 `r.weight_source = any_sparse ? "frame_snr_x_sparse_snr" : "frame_snr";`
- 上位依据：13_integration.md §4.0「分工固定：阶段一只生产信噪比（稀疏 SNR 控制点）；阶段二在叠加前**先算真实信号面**——用每帧的稀疏控制点重建出稠密 SNR 面，再按逆方差定权后叠加」；§3「SNR 重建路径（三条，配置文件 JSON 显式指定；**默认稀疏**）」。
- 现状：本文件的稀疏路径（:189-664 整段算子词表＋重建器）实现完整且 fail-closed，但生产唯一调用点 `module_adapters.cpp:11534` 原文 `in.sparse = nullptr;   // 稀疏 SNR 层尚未接入生产数据面`，且 `in.x = in.y = 0.0` ⇒ 即使接了层也只是问同一个坐标。
- 差在哪：条文把"逐像素逆方差"写成默认路径与科学主张的一部分，实际生产权重恒为一帧一个常数（`intra=1.0`），"对稀疏重建场逐像素同式 `w(x,y)=…`"（§4.0）在生产中从未发生。
- 后果：对外"跨帧绝对 SNR 最优叠加"的**逐像素**半边无实现；`weight_source` 恒为 `frame_snr`、`sparse_operator_ids` 恒空、`node_reproduction_max_abs` 恒 0 ⇒ 报告面看起来"没有稀疏层"而不是"稀疏层未接线"，与 §7「无稀疏层时实际路径被显式记录」的口径不可区分。
- 定级：**S1**（生产权重面实际取值；触发路径 = module_adapters.cpp:11534→weight_chain.cpp:715→:796→:11584）
- 怎么算修好：要么把 Phase1 稀疏层读进 `FrameWeightInput::sparse` 并按输出像素给 `x,y`（:11535-11536 一起改）；要么由负责人在 §4.0 把"默认稀疏"改成"现行帧级、稀疏未接线"，并在产品 manifest 里区分 `sparse_layer_absent` 与 `sparse_layer_not_wired`。二者不可都不做。
- 置信：CONFIRMED

### 不报的核过项（本文件）
- 语义判别门（:695-702）确实拒绝了相对质量权重与 median 诊断量，token 与 13_integration.md §4.0/FZ-GATE-MEDIAN-SNR 一致 ⇒ 合规。
- `legacy_allow_weight_fallback`（:37-46）虽仍存字段，但置真也只产 `ok=false/production_allowed=false` 与诊断数组 ⇒ 不构成静默降级（合同"键已删除"的落点在配置面，见 CR-61-19 的跨批登记）。检索式：`git grep -n "legacy_allow_weight_fallback" -- lib eng/contracts`。
- 非有限/非正权重的每一支都 fail-closed（:92-95、:784-788）；`make_equal_weight_baseline` 显式 `ok=false`（:802）⇒ 合规。
- 双三次样条与 mesh 中值的每一处常数都可在 07_noise_snr.md §4.5 找到同文（:180/185/189/190/191/192）⇒ 已按文献值/公式导出归类，不报。

---

# 二、`lib/algorithms/integration/v6/src/variance_propagation.cpp`（222 行，全读）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :16, :17 | `finite`/`positive_finite` | >0.0 | 有效性谓词 | 结构性不适用 | 定义性判断，不参与数值 | —— | CONFIRMED |
| :26-27 | `row_diag` 缺列返回 | 0.0 | H 行不含 i ⇒ `H_ii=0` | 结构性不适用 | 稀疏行的定义（include-self 的构造性表达），不是科学阈值 | —— | CONFIRMED |
| :37-48 | `prev/first`、`j <= prev`、`j >= n_frames` | 0 / 1 | 列下标升序唯一与范围检查 | 结构性不适用 | 稀疏行合法性 | —— | CONFIRMED |
| :72, :105 | `sigma2[j] < 0.0` | 0.0 下界 | 负方差拒、**零方差放行** | 待确认 ⇒ CR-61-8 | 条文面零处规定 σ²=0 合法（检索式：`git grep -n "sigma2\|零方差\|variance == 0" -- docs/science docs/plugins/algorithms_phase2`） | 丢一项非负数⇒和变小⇒方差偏小⇒偏绿 | CONFIRMED |
| :78 | `p_ii = 1.0 - row_diag(h,i)` | 1.0 | P=I−H 的对角元 | 公式导出 | variance_propagation.h:11-15「corrected=(I−H)y=P y」；代码按式算 | —— | CONFIRMED |
| :79, :83 | `p_ii²σ_i²`、`coeff²·σ_j²` | 指数 2 | PΣPᵀ 的对角形求和 | 公式导出 | 同上 :15 `Var(corrected_i)=Σ_j P_ij²σ_j²`；**该式仅在 Σ 对角时成立**，见 CR-61-9 | —— | CONFIRMED |
| :85, :116 | `var < 0.0` 拒、有限性 | 0.0 | 结果合法性 | 结构性不适用 | 方差非负是定义 | —— | CONFIRMED |
| :111-114 | `var = sigma2[i] + Σ coeff²σ_j²` | —— | 朴素（文档标注为错误）形式 | 公式导出 | variance_propagation.h:19-22 明写该式漏 −HΣ−ΣHᵀ 交叉项、N=8 高估 1.2857×；代码逐字按该"错误式"算，仅供对照（生产调用点见 CR-61-7 附注） | —— | CONFIRMED |
| :124, :144-155 | `include_self` 布尔 | —— | 校正场估计是否含自身 | 待确认 ⇒ CR-61-7 | docs/contracts 零提及（检索式：`git grep -n "排除自身\|include_self\|W2 含自身\|gauge" -- docs` ⇒ 命中仅 docs/algorithms/PHASE2_REJECTION.md:809/REJECTION.md:375 的卫星帧"留一"z，与本案不同主语；代码面命中见 CR-61-7 位点清单） | 取 true⇒方差偏小（误绿）；取 false⇒偏大（保守）；默认 false 落保守侧但口径无权威登记 | CONFIRMED |
| :146 | `positive_finite(w[i])` | >0.0 | "排除自身"要求自身权重存在 | 公式导出 | variance_propagation.h:64-65；留一统计的定义域要求，非阈值 | —— | CONFIRMED |
| :150-153 | `denom = wsum - w[i]`；单帧栈 | —— | 分母 W₋ᵢ 与 N=1 拒 | 公式导出 | 同 :146；单帧无"其它帧"⇒ :151 fail-closed 合规 | —— | CONFIRMED |
| :132-142 | `w[j] < 0.0` 拒、0 放行、`wsum>0` | 0.0 | 零权重合法⇒该帧在 ĝ 中隐身 | 待确认 | 无条文规定"权重 0 = 合法不贡献"用于本模块（coverage/memory.md:156 有"零权重合同（0 合法不贡献）"字样，但那是 integration 合同面自述、非 docs/science 或合同 schema） | 丢一项⇒ĝ 的分母变小、剩余帧权重占比变大⇒影响该像素的校正场；方向随帧而异，不主张单调 | PARTIAL（缺"零权重合同"的规范化落点：需 `git grep -n "零权重" -- eng/contracts docs` 之外再核合同 schema） |
| :162 | `if (c == 0.0) continue;` | 0.0 | 稀疏化：零系数不入行 | 结构性不适用 | 与 :132 的 0 权重等价的表示压缩，不改变 ĝ | —— | CONFIRMED |
| :178-185 | `gain>0`、`param_var>=0` | 0.0 | 乘性增益与参数方差的合法性 | 结构性不适用 | 谓词 | —— | CONFIRMED |
| :189 | `(data_var+param_var)/(gain*gain)` | 指数 2 | ÷g² 必进方差＋参数不确定度并入 | 公式导出 | variance_propagation.h:24-25（原引 q2-snr-smooth §2 不在跟踪面，见 CR-61-9）＋11_upm.md §4.1「校准参数不确定度必须传播」 | —— | CONFIRMED |
| :199-207 | `1.0/var` | —— | 权重=1/Var(corrected) | 公式导出 | variance_propagation.h:79-80；Var≤0 拒（fail-closed） | —— | CONFIRMED |
| :212-217 | `n<2`、`1±1/dn` | 1 / 2 | 朴素/正确方差比诊断 | 公式导出 | variance_propagation.h:82-84「(1+1/N)/(1−1/N)」，等权均值下 P 与朴素式的解析比 | —— | CONFIRMED |

## findings

### CR-61-6 · 取证结论：本文件的"排除自身"主语是**校正场估计子的留一**，不是"方差中剔除被估计量自身的散粒项"（此前两条待裁若按后者理解则本文件不构成实现）
- 轴：D（取证性，非缺陷本体）
- 位点：`variance_propagation.cpp:143-155`（`double denom = wsum; if (!include_self) { … denom = wsum - w[i]; … }`）＋ `:160` `if (!include_self && j == i) continue;   /* H_ii = 0 */` ＋ `:78-79` `const double p_ii = 1.0 - row_diag(h, i); double var = p_ii * p_ii * sigma2[i];`
- 上位依据：条文面零提及（检索式：`git grep -n "排除自身\|exclude-self\|include_self" -- docs eng/contracts ASTROCS_DESIGN.md` ⇒ **0 命中**；全仓命中仅 variance_propagation.h:16,64-65、.cpp:145-147 与 module_adapters.cpp:10151/10379/10556/10674/10717）。
- 现状：主语逐跳可读——`ĝ_i = Σ_j H_ij y_j`（.h:41），`H_ij = w_j/W₋ᵢ`、`H_ii = 0` ⇒ 第 i 帧不进入**它自己的**校正场；随后 `corrected_i = y_i − ĝ_i`，其方差里 `σ_i²` 以 `(1−H_ii)² = 1` **全额计入**，`σ_j² (j≠i)` 以 `w_j²/W₋ᵢ²` 计入。σ² 向量本身在本模块**从未被改写**：没有任何一处把"被估计量自身的源项散粒方差"从 σ_i² 中减去。
- 差在哪：这不是缺陷判定，是口径钉死：`include_self` 只换"用于求残差的交叉检验模板"，不换噪声模型；两值之差恰为留一引入的膨胀因子（等权 N 帧：exclude-self `σ²·N/(N−1)` vs include-self `σ²(N−1)/N`，N=2 时相差 **3 倍**）。
- 后果：与"绝对 SNR 是否排自身源噪声"相关的待裁事项**不能**以本文件为已实现凭据；引用本文件支撑该待裁会错指。
- 定级：S2（对文档/裁决面的错指，不改字节）
- 怎么算修好：在 13_integration.md §4.0 或 PHASE2_UPM.md 里把"排除自身"的主语、膨胀因子与它**不**处理源项这一条写成条文；把待裁改述为"σ² 是否需要减源项"这一独立问题另立条目。
- 置信：CONFIRMED

### CR-61-7 · `variance_include_self` 是运行时可读的方差口径键：合同零登记，且权重口径键被 §9.73 A44 明令禁止后同形态在方差面复现
- 轴：C（主轴）＋D
- 位点：`variance_propagation.cpp:124-126` `bool normalized_weight_hat_row(const double* w, std::size_t n, std::size_t i, bool include_self, HatRow* out, std::string* err)`；生产读键点 `module_adapters.cpp:10382` `const bool cvar_include_self = doc.value("variance_include_self", false);`
- 上位依据：13_integration.md §4.0「没有可选的权重口径、口径选择键、口径枚举或口径配置项；越界 token 一律 fail-closed（`FZ-WEIGHT-SINGLE-PATH` / `FZ-MODE-RETIRED` / `FZ-FIELD-WEIGHTMODE`）」；`eng/contracts/data/v6_clause_registry_v1.json:2809`「不存在任何被冻结或被选择的权重模式键」。条文面零提及 `variance_include_self`（检索式：`git grep -n "variance_include_self" -- docs eng/contracts` ⇒ 0 命中；全仓命中仅 module_adapters.cpp:10379/10382 与 release02 补丁脚本）。
- 现状：该键**出现即生效、缺失即取默认 false**，schema/合同不认识它；两种取值给出的方差相差 N²/(N−1)² 倍（见 CR-61-6）。
- 差在哪：合同把"口径选择键"本身定为禁止项，代码留了一个可被配置文件静默设定的口径开关，且它的默认值由代码单方面规定（负责人 §9.49/§9.73 系列裁决的对象正是这类"合同说没有、代码还在读"的键）。
- 后果：同一份产品树在不同（未被登记的）配置下给出不同的 variance/ivar 字节，跨帧绝对 SNR 的产品值不再由条文唯一确定；配置审计面（CONFIG_CONTRACT）看不见该键。
- 定级：**S1**。触发路径：CLI 配置 JSON → `module_adapters.cpp:10382` → `normalized_weight_hat_row(..., cvar_include_self, ...)`（:10556 区段「残差制造者方差（(c) 排除自身），加可选帧 Phase1 逐像素方差」）→ `corrected_pixel_variance` → variance/ivar 子产品落盘（`uncertainty_available` 为 true 的分支，:11600 之后）。
- 怎么算修好：二选一并同批改：① 删键，口径由条文唯一确定（推荐，与 A44 一致）；② 保留则在 `eng/contracts/schemas` + `docs/contracts/CONFIG_CONTRACT.md` + `eng/packaging/config/defaults.json` 三侧登记同名同默认，并把取值写进产品 provenance 供事后审计。只改代码侧登记会造成"合同有键、门不认识键"的新分歧。
- 置信：CONFIRMED

### CR-61-8 · σ²=0 被当合法"零噪声"计入传播：缺失以 0 表示时该帧噪声项静默消失，方差单调偏小
- 轴：R
- 位点：`variance_propagation.cpp:71-76` `for (…) { if (!finite(sigma2[j]) || sigma2[j] < 0.0) { … } }`（`< 0.0` 而非 `<= 0.0`），:79 `double var = p_ii * p_ii * sigma2[i];`
- 上位依据：合同对同族量的判据是"非正即拒"：`docs/contracts/DATA_ARTIFACTS.md:26` control_ivar 的 `ivar≤0→rc=2`；`docs/science/PHASE2_UPM.md:24` `k_corr = 1 ⇔ 忽略相关，显式拒`。条文面**没有**任何一处规定"方差为 0 是合法输入"（检索式：`git grep -n "sigma2\|零方差\|variance.*== *0" -- docs/science docs/plugins`）。
- 现状：单帧 σ²=0 通过校验并在求和里贡献 0；只有**全部**项为 0 时才由 :85/:190 的 `positive_finite(var)` 拦下 ⇒ "部分帧缺方差"与"部分帧零噪声"不可区分。NaN 走 :72 拒（合规），但 0 是放行通道。
- 差在哪：丢一项非负数的效果已被钉死为**单调变小**（`var` 是非负项之和）⇒ 被传播的 Var(corrected) 偏小 ⇒ ivar=1/Var 偏大 ⇒ 产品不确定度偏乐观、任何以 σ 为分的判据偏绿。
- 后果：跨帧绝对 SNR 的"绝对"二字失义：一帧产品若 variance 平面含 0（Phase1 掩膜/降级像素常见表示），其噪声不进栈而支持度仍进 ⇒ SNR 高于真值。
- 定级：**S1**。触发路径：Phase1 `VARIANCE` 平面 → `module_adapters.cpp:10556` 区段（"加可选帧 Phase1 逐像素方差"）→ `residual_maker_variance`（本文件 :59）/ `corrected_pixel_variance`（:174）→ variance/ivar 子产品。
- 怎么算修好：:72 与 :105 改 `sigma2[j] <= 0.0` 即 fail-closed，或在入参处要求 `has_variance[]` 位图；同时给"缺失方差"一个不可与 0 混淆的表示（NaN 已在拒侧，推荐 NaN）。补一条负例：单帧 σ²=0 必须让整像素 unavailable 而非把方差压低。
- 置信：CONFIRMED

### CR-61-9 · 协方差在本模块没有载体也没有"不完备"标记，头注释一边写 `Σ=Cov(y)` 一边直接落对角形；两份权威依据的锚指向不在跟踪面的报告
- 轴：D（主轴）＋S
- 位点：`variance_propagation.cpp:59-91`（签名只收 `const double* sigma2`，函数体内 `Σ_j P_ij²σ_j²`）；`variance_propagation.h:13-15`「线性化下（Σ = Cov(y)，对角 = diag(σ²)）: Var(corrected) = P Σ Pᵀ …」；锚 :4-5 `reports/RELEASE-02/unc-prop-audit.md §3`、`reports/RELEASE-02/q2-snr-smooth.md §2/§5/§7`
- 上位依据：13_integration.md §4.0「工程近似**独立样本才**退化到像素 ivar 加权平均；Drizzle 相关、共同 master、UPM 参数、重叠重采样协方差**纳入 C 或以相关核/低秩近似**」；§7「相关噪声无描述 → variance 不完备，标记」；§3 输出面点名列有 `correlation`。
- 现状：无 `Cov` 入参、无非对角项、无 `correlation`/`unavailable` 出参；`param_var` 只并 UPM 参数一项（:189）。锚方面：`git ls-files reports` 只有 `reports/v19r2/` 的 3 个 csv，`git grep -ln "q2-snr-smooth"` 的命中全是 `eng/tests/validation/release02/q2_snr_smoothness/` 脚本与日志——**被引用的两份 .md 不在跟踪面**（判"不存在"按跟踪集＋删除历史，`git log --diff-filter=D -- reports` 未含 RELEASE-02 路径）。
- 差在哪：我**不**主张"丢相关项⇒方差必偏小"的方向：`P_ii>0、P_ij<0`，故 (i,j) 配对的被丢交叉项为负、(j,k) 配对为正，净号不定（按 §3 的硬门槛，方向未定即不得报"误绿"）。成立的是两点：① 设计点名的"相关面纳入 C"在本模块既无实现也无从表达；② §7 要求的"不完备标记"也没有输出面 ⇒ 消费者无法知道自己是走了近似还是走了完备传播。
- 后果：`Var(corrected)` 的对外身份是"产品方差"，而它可被证明只在与条文相反的独立样本假设下成立；正确性/可复现主张缺一角，且缺的那一角连标记都没有。
- 定级：S2（错值不可判定，故不升 S1；但"variance 不完备必须标记"是条文点名且此处零实现 ⇒ 若负责人认定该产品方差直接对外，则升 S1）
- 怎么算修好：给 `residual_maker_variance`/`corrected_pixel_variance` 增可选 `Cov` 入口或 `correlation_represented` 布尔输出；无相关面时由调用方在 variance 子产品上写 `representation="diagonal_only_independent_sample"` + 计数（与 phase2_integrate.cpp:396 的 `covariance_representation` 同族），并在 13_integration.md §4.0 登记本模块的适用域=独立样本近似。补 RELEASE-02 两份报告的跟踪面落点（或改锚到 `eng/tests/validation/release02/`）。
- 置信：PARTIAL（缺"是否存在别处已登记本模块为对角近似"的条文命中；已跑检索式 `git grep -n "对角\|diagonal" -- docs/plugins/algorithms_phase2 docs/science/UNCERTAINTY_AND_COVARIANCE.md`，命中的是 phase2_integrate 的记录面而非本模块适用域声明）

### 不报的核过项（本文件）
- `hat_row_valid`（:32-57）的升序唯一/范围/有限三项检查齐备，越界与重复列都拒 ⇒ 合规。
- `naive_variance` 明确标注"仅红例/对照/审计，生产权重禁用"（.h:57-58）；本 TU 内无自我调用（检索式：`git grep -n "naive_variance(" -- lib` ⇒ 仅 :93 定义与 .h 声明；同名出现在 phase2_integrate.cpp 的是**局部变量** `res.naive_variance`，非本函数）⇒ 不因"库里留了错误式"报缺陷。
- 非有限与负值的每一支都返回 false 并写串（:62-76、:85-88、:190-193）⇒ 无 clamp、无 NaN 静默。

---

# 三、`lib/algorithms/integration/v6/src/phase2_integrate.cpp`（1487 行，全读）

> 定级前提＝本批共用事实 F1：**该 TU 不在生产链接内**（`CMakeLists.txt:1047` 不链 `astrocs_v6_phase2_integrate`，唯一消费者 `eng/tests/integration/v6_p2/CMakeLists.txt:20`，公开入口 `run_point_information` 的调用点全部在 `eng/tests/integration/v6_p2/v6_p2_integrate_test.cpp`）。故本文件所有 finding 封顶 S2，并逐条注明"缺生产触发路径"。

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :43 | `kPi` | 3.14159265358979323846 | π 字面量 | 结构性不适用 | 单位圆常数；**本 TU 零使用**（`grep -c kPi` = 1 即定义行） | —— | CONFIRMED |
| :54, :62, :69 | 求逆主元判据 | `best > 0.0`、`1.0` | Gauss-Jordan 奇异性唯一判据 | 待确认 ⇒ CR-61-16 | 合同有相对秩容差条款 `FZ-AP2S-RANK-RTOL`（registry clauses[68]：value 1e-10，「秩亏 → unavailable，禁止伪逆静默」）但本处未用 | 近奇异⇒W 虚高⇒Var(F_hat) 偏小⇒偏绿 | CONFIRMED |
| :105-107 | `is_hex40` | 40、小写十六进制 | SHA 形态检查 | 结构性不适用 | 摘要长度定义（SHA-1/截断式 40 hex）；大写即判红属实现选择 | —— | CONFIRMED |
| :113-115 | `kBad[]` P33 键表 | 8 个 token | 撤销守卫拒绝表 | 结构性不适用 | 枚举基数（C-004.2 P33 撤销，.h:35-36） | —— | CONFIRMED |
| :128-133 | `kForbiddenWeightSources[]` | 24 个 token | 诊断量不得作权重/方差来源 | 结构性不适用 | DATA_SEMANTICS §31.8 `G-WEIGHT-SOURCES`/`G-DIAGNOSTIC-NOT-WEIGHT`（13_integration.md §8 末段指明其为在役判据词表） | —— | CONFIRMED |
| :144-152, :158 | `sizeof(u64)=8`、`8*i`、`-8` | 8 | 大端 f64 编码 | 结构性不适用 | 字节序定义常数 | —— | CONFIRMED |
| :159 | `bitpix = -64` | -64 | FITS 双精度标识 | 结构性不适用 | FITS 格式定义 | —— | CONFIRMED |
| :167-173 | 百分位最近秩 | `p`、`v.size()-1`、`0.0` | 分位数取法与出界钳制 | 待确认 | **本 TU 零调用**（`git grep -n "nearest_rank_percentile"` 仅命中定义行）；07_noise_snr 与 §31 无该取法条文 | 出界 p 被钳到端点⇒偏绿（不报错） | CONFIRMED |
| :176-181, :845 | off-diagonal 判据 | 1e-12 | 联合 C_in 是否含非对角 | 待确认 | .h:28 登记的是 `FZ-AP2S-EPSF-RTOL 1e-12`（另一件事），本用处未具名（`kEpsfRtol` 零使用） | 阈值越小⇒越多矩阵被判"独立"⇒走朴素支 | CONFIRMED |
| :236, :273, :291-292 | 2880 / 2879 / `1<<40` | FITS 块 | 头块尺寸、补齐、扫描上限 | 结构性不适用 | FITS 物理格式定义常数 | —— | CONFIRMED |
| :240, :245, :281-284 | 36、80、10、30、8 | 卡片数/列宽/值列 | FITS 卡片布局 | 结构性不适用 | 同上 | —— | CONFIRMED |
| :271 | `bitpix<0 ? -bitpix/8 : bitpix/8` | 8 | 每元素字节数 | 结构性不适用 | 位深换算定义 | —— | CONFIRMED |
| :304 | `product_dirs.size() < 2` | 2 | Phase2 组最少帧数 | 待确认 | 合同有 `FZ-AP2S-UPM-MINFRAMES`（PENDING，SO-07）但那是 UPM 侧；本处未引用条款号 | 1 帧组被拒⇒偏红（安全） | CONFIRMED |
| :312 | `n_common < kCommonMin` | 3 | 共同星硬下限 | 文献值(条款) | registry clauses[46] `PSFSW-T-NMIN` value=3，status PENDING_OWNER_SIGNOFF（SO-07）；`grep -c kCommonMin`=1 | —— | CONFIRMED |
| :315 | `n_components != 4` | 4 | PSFSW 四分量必在 | 文献值(条款) | FROZEN 条款 `FZ-FIELD-PSFSW-4COMP`（DATA_SEMANTICS.md:3372 附近 FROZEN 清单） | —— | CONFIRMED |
| :330 | `ext["point_source"].value("a", 1.0)` | 1.0 | 缺 `a` 键时帧光度响应按 1 | 待确认 | 条文面零提及（`git grep -n '"a"' -- eng/contracts/schemas`）；a_k 进 `A=a_k P_k`（:858）与 P_eff（:973） | 缺响应按 1 ⇒ 该帧被按"标度已正确"处理⇒偏绿 | CONFIRMED |
| :342 | BUNIT 字面串 | `ADU/sr`、`ADU^2/sr^2` | 冻结单位律 | 文献值(条款) | `FZ-P3-BUNIT-QUADRATIC`/`FZ-BUNIT-SEMANTICS`（.h:34），重开侧 :1074 同串 | —— | CONFIRMED |
| :346 | `|var·w−1| ≤ 1e-9` | 1e-9 | 磁盘 Q/W 自洽 | 文献值(条款)，但**写死未具名** | 同族条款 `FZ-FORMULA-WINFO`＋.h:66 `kIdentRtol 1e-9`（零使用，`grep -c`=0） | 值对；风险在条款改值时不跟随（CR-61-18） | CONFIRMED |
| :421 | 缺 config_hash 兜底 | `sha256("phase2-config")` | 用固定串的哈希冒充配置哈希 | 待确认 ⇒ CR-61-14 | §31 最小集要求 config_hash 可追溯；无任何条文允许合成假哈希 | 缺失被伪装成存在⇒审计面偏绿 | CONFIRMED |
| :426-427 | `has_target_pixel_area=true` + `target_pixel_area=1.0` | 1.0 | 声明"有目标像素面积"而值为占位 1 | 待确认 ⇒ CR-61-14 | `docs/contracts/DATA_ARTIFACTS.md`/provenance schema 要求面积来自实际采样栅格 | 面积=1 ⇒ 球面像素面积换算恒等（偏差不定号） | CONFIRMED |
| :431-432 | `kernel_id="phase2_combination_operator"`、`has_pixfrac=false` | —— | 采样块占位声明 | 结构性不适用 | 本层不做重采样⇒pixfrac 不适用（与 :459 k_corr 域内 pixfrac=0.8 是两个不同主语：前者是产品采样、后者是标定域） | —— | CONFIRMED |
| :455 | `flux_conservation_factor = 1.0` | 1.0 | 注释自证"Phase2 组合不改变保持因子" | 待确认 | `FZ-COND-FLUX-CONSERV`（PENDING 清单内），条文未给"恒 1"的推导 | 恒 1 ⇒ 通量保持检验被预置为通过 | CONFIRMED |
| :457, :1227, :1278 | `kKCorrFrozen` | 1.4 | Drizzle 相关方差放大因子 | 文献值(条款) | registry clauses[86] `FZ-PROV-KCORR-VALUE` value=1.4（status PENDING）；`DATA_SEMANTICS.md:1739` 给 MC 实测 1.3883 取 1.4 保守侧；`docs/science/PHASE2_UPM.md:24` | —— | CONFIRMED |
| :459 | `k_corr.domain.pixfrac` | 0.8 | 声明的适用域 pixfrac | 文献值(条款) | `DATA_SEMANTICS.md:1739`（域 pixfrac∈[0.5,1.0]，MC 用「pixfrac = 0.8 生产默认」；本处写的是标定时的单值而非域区间） | 写成单点⇒域信息丢失（不构成错值） | CONFIRMED |
| :460, :1227 | `domain.patch_size` / control_variance 的第 3 参 | 8 | 8×8 控制 cell | 文献值(条款) | `DATA_ARTIFACTS.md:26`「tile 内 8×8 网格的 cell」；07_noise_snr.md:214 `Δ = tile_width/8 = 64` | —— | CONFIRMED |
| :461 | `estimator="median"` | —— | k_corr 域内估计器 | 文献值(条款) | `DATA_SEMANTICS.md:1764` 中位数渐近方差 `(π/2)σ²/N` 的定义即建立在中位数上 | —— | CONFIRMED |
| :463-464 | `k_corr.calibration.script` | `eng/contracts/data/v6_clause_registry_v1.json#FZ-PROV-KCORR-VALUE` | 标定"脚本"锚 | 待确认 ⇒ CR-61-14 | §3286「k_corr 缺适用域/**标定脚本**/固定种子 → REJECT」；真实标定实现按 `docs/contracts/TEST_MATRIX.md:41` 是 `lib/algorithms/coverage/tests/synthetic_gate.cpp`（`k_corr≈1.3883`） | 假锚使"缺标定"不可判⇒偏绿 | CONFIRMED |
| :465 | `calibration.seed` | 20260915 | 标定固定种子 | 待确认 ⇒ CR-61-14 | `git grep -n "20260915"` 全仓命中只言日期（控制包名），无任何跟踪件把该数记为 MC 种子 | 种子不可复算⇒偏绿 | CONFIRMED |
| :466 | `calibration_run_id` | `"frozen-in-domain-constant-FZ-PROV-KCORR-VALUE"` | 状态词 | 待确认 ⇒ CR-61-14 | 条款状态实为 `PENDING_OWNER_SIGNOFF`（registry clauses[86] `status`），DATA_SEMANTICS §31.10 明令「生产 artifact 的状态词只写 `PENDING_OWNER_SIGNOFF`」 | 自证"FROZEN"⇒超前签发（偏绿） | CONFIRMED |
| :467 | `lookup_table_ref` | `K_CORR_DOMAIN_B` | 查表名 | 待确认 | 跟踪面零该标识（`git grep -n "K_CORR_DOMAIN_B"` 除本行与 provenance 侧无定义面）| 悬空引用⇒"逐帧查表优先"无法核 | CONFIRMED |
| :500-505 | `ivar[i] = (variance>0) ? 1/variance : 0.0` | 0.0 | IVAR 平面的无效表示 | 待确认 ⇒ CR-61-15 | 同记录 :596-598 自declared `invalid_policy="nan_or_support_le_0"`（两事实源） | 0 权重使下游丢弃该像素（不放大噪声），但与"无覆盖"同值 | CONFIRMED |
| :525, :567-570 | `weight_mode` 记录常量 | `"point_information"` | 产品身份常量 | 文献值(条款) | `v6_clause_registry_v1.json:79` 生产接受集 = point_information \| surface_gls；§2809 声明"权重模式键作废"仅指**输入面**（:97 `not_an_input_path` 同页区分输入面与本表）⇒ 记录面合法 | —— | CONFIRMED |
| :564, :566 | `median_target=1.0`、`constants_version` | 1.0 | psfsw 组内归一目标 | 文献值(条款) | 07_noise/PSF_SIGNAL_WEIGHT 组内中位=1 方案（OI-01，:723-724） | 仅 `has_psfsw` 分支写，本入口恒不写⇒无实际作用 | CONFIRMED |
| :583-584 | `propagation`、`scalar_form` 串 | —— | 传播式声明 | 公式导出 | `FZ-FORMULA-COV-PROP`：`C_out=RC_inRᵀ`、`Var=cᵀC_in c=Σc_ic_j[C_in]_ij`；**声明含非对角的式，落盘字节按对角算**（:994），见 CR-61-13 | —— | CONFIRMED |
| :616-618 | `metric=Var_approx/Var_GLS` + `bound_ref="FZ-AP2S-EPS-PIXIVAR=0.05"` | 0.05 | 近似误差门的界值 | 文献值(条款)，但以**字符串**而非 `kEpsPixivar` 参与判定 | registry `FZ-AP2S-EPS-PIXIVAR`（PENDING，0.05）；该分支 :614 永不进入⇒纯文本 | —— | CONFIRMED |
| :625-627 | `diagonal_approximation.deficit.value` | 0.0 | "对角 vs 精确"缺额 | 待确认 ⇒ CR-61-13 | 该缺额从未被计算（本 TU 无 `exact` 方差计算）；`is_lower_bound=true` 亦无来源 | 恒 0 且宣称下界⇒偏绿 | CONFIRMED |
| :708 | `fwhm_from_effective.value`（注入支） | 4.71 | 负例注入的假 FWHM | 待确认 | .h:148 `force_effective_psf_only_fwhm` 声明为"仅测试"，但字段在**公共** `RunMeta` 里、无生产侧拒绝 | 生产若置真⇒写出假 FWHM（偏绿） | CONFIRMED |
| :712 | `doc["phase2_extensions"][inject_p33_key] = 3.14` | 3.14 | 注入禁止键的哨兵值 | 结构性不适用 | 只为触发重开门的哨兵值，不进入任何计算 | —— | CONFIRMED |
| :757 | `mf["n_modes"]` | 1 | 单口径的"模式数" | 文献值(条款) | FZ-WEIGHT-SINGLE-PATH（.h:16-19）⇒ 恒 1 是口径唯一性的表达 | —— | CONFIRMED |
| :836, :902-904 | `1.0/w_naive : 0.0`、`1.0/wj : 0.0` | 0.0 | 非正信息量时"方差=0" | 待确认 ⇒ CR-61-13/16 | 与 §3286 非正即拒同族；0 在此伪装成"无噪声" | 偏绿（0 方差） | CONFIRMED |
| :909 | `corr_ratio >= kCorrRatioMin` | 1.05 | 相关帧朴素求和拒门 | 文献值(条款) | registry clauses[80] `FZ-AP2PT-CORR-RATIO-MIN` value=1.05（PENDING，SO-07），判据方向＝比值≥1.05 必须走联合 C_in | —— | CONFIRMED |
| :916-917 | `f_hat=q/w`、`var_f=1/w` | —— | 点源组合与方差 | 公式导出 | `FZ-FORMULA-FHAT`/`FZ-FORMULA-WINFO`（.h:24-25） | —— | CONFIRMED |
| :922-923 | `snr_rel > kSnrIdentRtol` | 1e-9 | 独立帧 SNR 恒等式 | 文献值(条款) | registry clauses[79] `FZ-AP2PT-SNR-IDENT-RTOL` value=1e-9（PENDING） | 注意：`joint_used` 时该检查看都不看（:923 短路） | CONFIRMED |
| :986-996 | SB 平面分子/分母 | `P=front().size()`、`i<size` | 加权平均归一 | 待确认 ⇒ CR-61-10 | 13_integration.md §4.0 的 `w(x,y)=SNR²/F_ref²`（本处用的是 α_k/Σα，见 CR-61-11/12） | 分子丢帧、分母不丢⇒SB 偏暗、方差偏小 | CONFIRMED |
| :1222-1224 | `sigma_bg = mean(sqrt(var))`、`max(0.0,·)` | 0.0 | 背景尺度取逐像素 σ 的算术平均 | 待确认 ⇒ CR-61-17 | `DATA_SEMANTICS.md:1764` 定义 σ_bg=「patch 的稳健尺度（MAD×1.482602218505602）」⇒ 同名不同量 | σ 被平均⇒偏小（Jensen）⇒control_variance 偏小⇒偏绿 | CONFIRMED |
| :1263 | `obs.size() < controls*2` | 2 | 每控制点最少观测 | 文献值(条款) | `FZ-AP2S-UPM-MINFRAMES`（PENDING）族 | 偏红侧安全 | CONFIRMED |
| :1267 | `cfg.min_frames` | 2 | 同上求解器侧 | 文献值(条款) | 同 :1263 | —— | CONFIRMED |
| :1269 | `cfg.rank_rtol` | 1e-10 | 秩判定相对容差 | 文献值(条款) | registry clauses[68] `FZ-AP2S-RANK-RTOL` value=1e-10；注释自称"已退休"含糊但值与条款一致 | —— | CONFIRMED |
| :1270-1272 | `gauge_mode=0`、`allow_additive_only_single_frame=1`、`c_in_has_unrepresented_shared_terms=0` | 0/1/0 | 规范固定与共享项声明 | 待确认 | `docs/science/PHASE2_UPM.md:129`（SCI-004 每分量参考帧 gauge）支持 gauge=0；后两键 docs/合同侧未见同名条文（`git grep -n "allow_additive_only_single_frame\|c_in_has_unrepresented_shared_terms" -- docs` ⇒ 见 CR-61-18 检索式） | `c_in_has_unrepresented_shared_terms=0` 恒报"无未表征共享项"⇒与 CR-61-9 同族 | CONFIRMED |
| :1273 | `cfg.huber_delta` | 1.345 | Huber 阈值 | 文献值 | `docs/algorithms/PHASE2_SESSION.md:133-137` 逐字给出：高斯 95% 渐近效率阈值，出处 Huber 1964 (DOI 10.1214/aoms/1177703732) + Holland & Welsch 1977 (DOI 10.1080/03610927708827533) | —— | CONFIRMED |
| :1274 | `cfg.max_iterations` | 100 | IRLS 迭代上界 | 文献值(条款) | PHASE2_SESSION.md:137-138（并规定"达上限必须记 converged=0"，本文件不读 converged） | —— | CONFIRMED |
| :1275 | `cfg.tolerance` | 1e-6 | 收敛相对容差 | 文献值(条款) | PHASE2_SESSION.md:139-142（相对量，`max_dM/max(scale_obs,eps)`；绝对容差在 ADU·sr⁻¹ 上永不收敛） | —— | CONFIRMED |
| :1276 | `cfg.sigma_floor` | 1e-3 | σ 下限（ADU·sr⁻¹） | 文献值(条款) | PHASE2_SESSION.md:142-145（量纲与理由同文） | —— | CONFIRMED |
| :1277 | `zero_anchor_weight` | 0.0 | 零锚权重（不施加） | 文献值(条款) | PHASE2_SESSION.md:145-146 列明该常量**不可经 config 覆盖**（DISP-P2SES-005 登记） | —— | CONFIRMED |
| :1350-1352 | `nominal_contributors=K`、`profile="wbpp_current"`、`underdetermined_n=2` | 2 | 排异计划请求 | 文献值(条款) | PHASE2_REJECTION.md 的 profile 与 underdetermined 面（:137 表） | —— | PARTIAL（未逐字核 `wbpp_current` 与 `underdetermined_n=2` 的登记位） |
| :1363, :1372, :1374-1375 | `n=4`、`+0.3*i`、`sqrt(cv)`、`max(0.0,·)` | 4 / 0.3 | 排异分类的**构造样本** | 待确认 ⇒ CR-61-17 | 无任何条文说明生产库函数内可自造残差；0.3 无量纲出处 | 分类计数/z 全部来自夹具⇒恒不红 | CONFIRMED |
| :1379-1383 | `P2_REJ_PROFILE_*`、`keep_moving_source=1` | 外部常量 | 排异 profile 阈值引用具名常量 | 结构性不适用 | 取自 rejection.h 词表（本文件不重定值⇒与 CR-61-18 相反的正例） | —— | CONFIRMED |
| :1407-1409 | `ra0=0, dec0=0, step_deg=1e-3, nx=P, ny=2` | 1e-3 / 2 | 假空间网格以满足"尺寸≥2" | 待确认 ⇒ CR-61-17 | 注释 :1403-1404 自述"二维网格（两行同值）以满足 eval 的尺寸>=2" | 空间门的输入由构造给出⇒p05/p50/p95 是同一数组的分位数，非天区模型 | CONFIRMED |
| :1434-1436 | `thresholds_declared=1`、`residual_trend_max=0.10`、`power_loss_max=0.05` | 0.10 / 0.05 | 降级门阈值 | 文献值(条款) | `PSFSW-T-TREND`、`PSFSW-T-POWERLOSS`（均 PENDING，SO-07；注释标"已冻结值"与状态词不符） | —— | CONFIRMED |
| :1438-1441 | `summary_complete=1`、`spatial_residual_p95=0.0`、`spatial_trend=0.0`、`power_loss=0.0` | 0.0 / 1 | 降级门的**测量值**输入 | 待确认 ⇒ CR-61-17 | §3286 与 FZ-DEGRADE-SCALAR 要求真实测量；此处为字面量 | 0 恒小于正阈值⇒两支恒不红（误绿，方向已钉死） | CONFIRMED |
| :1445 | `applicability_domain` | `"phase2_mosaic"` | 适用域名 | 待确认 | 与 :458 的 `"phase2_control_cell"` 是同一"域"概念的两个不同字面串，二者谁约束谁无条文 | 域名字符串不一致⇒k_corr 域判定无法机器核 | CONFIRMED |
| :1452-1457 | `cov=1`、`sup=K`、`min_support_frames=2` | 1 / 2 / K | 覆盖与支持度全 1 的夹具 | 待确认 ⇒ CR-61-17 | 真实 support/coverage 由 coverage 面给；此处以"全帧都覆盖该 cell"入分类 | 支持度恒=K⇒永不判"支持不足"⇒偏绿 | CONFIRMED |
| :1471-1473 | `tokens_ok(3)`、`tokens_bad(5)` 且返回值丢弃/`p2_rejection_weight_surface_guard` 返回值不判 | 3 / 5 | "权重来源门"的自检式调用 | 待确认 ⇒ CR-61-17 | 13_integration.md §8 末段称这些词表检查是**在役判据执行面**；本处的操作数是硬编码 token 而非产品字段 | 恒不红⇒门只看夹具 | CONFIRMED |

## findings

### CR-61-10 · 面亮度叠加的分子按帧长截断、分母不截断：短帧在其长度之外仍计入归一化（同一文件另一入口对此 fail-closed）
- 轴：R
- 位点：`phase2_integrate.cpp:987-995`
  `const std::size_t P = fs.frames.front().signal_sb.size();` / `double asum = 0.0; for (double a : alpha) asum += a;` / `for (std::size_t i = 0; i < P && i < fs.frames[k].signal_sb.size(); ++i) { const double wgt = alpha[k] / asum; as.signal[i] += wgt * …; as.variance[i] += wgt*wgt*…; }`
- 上位依据：13_integration.md §4.0「每个天球像素的多个源像素输入…**由各自 SNR 计算对应权重**」＋§7 错误面（退化须明确失败）；条文面零处允许"帧平面长度不等时按首帧裁剪"。
- 现状：`asum` 无条件含全部 K 帧权重；分子对第 k 帧只在 `i < len_k` 时累加。`open_phase2_frame_set`（:302-355）**不校验各帧平面等长**（对照同文件 :1214 `for (const auto& f : fs.frames) if (f.signal_sb.size() != P) { r.error = "signal plane size mismatch"; return r; }`——同一件事在这里是硬失败）。
- 差在哪：分子过滤、分母不过滤 ⇒ `Σ_k wgt_k < 1`（在被截断的像素段上），期望值被按 `Σ_{截断}α_k/asum` 的比例压低；无任何计数、无 error、无 manifest 字段。
- 后果：SIGNAL 平面在超出短帧长度的像素上**系统性偏暗**（面亮度低估），VARIANCE 同段丢项⇒偏小（低估）；两个方向都非保守，且马赛克边缘/局部覆盖差异正是产品对外宣称按支持度处理的情形。
- 定级：S2（缺生产触发路径 F1；在本 TU 内 CONFIRMED）
- 怎么算修好：在 `open_phase2_frame_set` 统一要求所有帧 `signal_sb`/`variance_ivar` 等长（与 :1214 同一判据同一文案），删除内层 `i < …size()` 兜底；若确要支持不等长，则分母改为按像素有效集 `asum_i = Σ_{k: i<len_k} α_k` 并把有效帧数写进支持度平面。
- 置信：CONFIRMED

### CR-61-11 · 本文件的面亮度权重不是 SNR 逆方差链：零 include、零调用，却在注释与迁移文案里宣称由该链强制
- 轴：D（主轴）＋C
- 位点：`phase2_integrate.cpp:184-190` `derive_psf_alpha`（`sum += f.view.w_info; a.push_back(f.view.w_info / sum);`）、:949 `alpha = derive_psf_alpha(fs);`、:992 `const double wgt = alpha[k] / asum;`；宣称面 :1464-1466 与 :202-211
- 上位依据：13_integration.md §4.0（F4）"对稀疏重建场逐像素同式：`w(x,y) = SNR(x,y)²/F_ref,k²`"；`.h:16-19` FZ-WEIGHT-SINGLE-PATH 同式。
- 现状：`git grep -n "p2weight\|weight_chain\|frame_snr\|reference_flux\|F_ref" -- lib/algorithms/integration/v6/src/phase2_integrate.cpp` 的全部命中只有 4 行**注释/字符串**（:202、:208、:1465、:1466）；无 `#include "astrocs/v6/weight_chain.h"`，无 `compute_inverse_variance_weights` 调用。实际权重来源是 Phase1 标量 `W_info`。
- 差在哪：注释说"单一口径由 p2weight 的逆方差权重链与产品校验面强制（见 weight_chain.h）"，实现里那条链根本没进这个 TU；被强制的只有记录串相等性检查（:1049-1061）。
- 后果：读这份"产品链集成"的人会以为 SB 平面按 SNR 场定权；实际按帧级点源信息量定权 ⇒ 创新点②的接线图与本文件不符。
- 定级：S2（缺生产触发路径；文档/实现不符本身可判）
- 怎么算修好：把 `compute_inverse_variance_weights` 真正接进 :986-996 的像素循环（逐输出像素取 `SNR(x,y)²/F_ref,k²`），或把 :1464-1466 与 :202-211 的文案改成实际口径并显式登记"本入口 = 点源信息量权重面，SNR 逆方差链在 scheduler 侧"；两侧不能都不动。
- 置信：CONFIRMED

### CR-61-12 · `W_info`（点源 PSF 拟合域，ADU^-2）被当面亮度像素权重消费，合同要求的三条前置确认一条都没做
- 轴：C（主轴）＋D
- 位点：`phase2_integrate.cpp:974` `as.weight_kind = "W_info"; as.weight_units = "ADU^-2";`、:992-994 用 α 加权 `signal_sb`(ADU/sr) 与 `variance_sb`(ADU²/sr²)、:533 记录同时列 `{"W_info","ADU^-2"}` 与 `{"pixel_semantics","surface_brightness"}`
- 上位依据：`docs/contracts/UNIFIED_OBJECTS.md:48` 原文：「`Var(F_hat)` 是**PSF 拟合域**内的通量估计方差（`F_hat` 单位 ADU），**不含**像素间相关核的贡献；把本对象当权重消费前，消费侧必须确认 ① 估计域与目标一致（点源、非面亮度）、② 是否已含相关核（未含时按 `DATA_SEMANTICS.md` §31.5 的 `k_corr ≠ 1` 条款补核或拒绝）、③ 与 `W_info` 消费面（§28.6）的量纲一致。**三条缺一即 fail-closed，默认代入一律判红**」。
- 现状：三条判据在本文件零实现——① 无目标产品类型检查（本入口 `product_family="phase2_point_source"` 却写 SB 平面 :972/985）；② `correlation_kernel_id` 为空时（:981-984 仅 `joint_used` 才设）不补 k_corr、不拒绝，只在 provenance 写 `representation="full_matrix_unavailable"`（:443）；③ 量纲混用在 :530-538 的单位表里白纸黑字。
- 后果：合同自己规定的 fail-closed 面被默认代入绕过 ⇒ SB 平面的权重与方差都不属于任何已定义的估计域；"绝对 SNR/不确定度"对外主张失去量纲凭据。
- 定级：S2（缺生产触发路径 F1；成立性不依赖运行时）
- 怎么算修好：在 :986 之前加三条判定并逐条 fail-closed（含"未含相关核 ⇒ 拒绝或按 §31.5 补 k_corr"）；把 SB 平面改由 §4.0 的 SNR 逆方差链定权（与 CR-61-11 同批），或另立 `surface_gls` 入口。
- 置信：CONFIRMED

### CR-61-13 · 记录里的门、可用性与交叉引用是字面量：门恒真、`input_covariance="provided"` 无条件、两个 `_ref` 指向从未写出的块
- 轴：R（主轴）＋C
- 位点：:739-743 `ext["gates"] = {{"FZ-MODE-PRODUCTION", true}, {"FZ-FORMULA-COV-PROP", true}, {"FZ-FORMULA-PSFSW-COMPOSITE", as.has_psfsw}, {"FZ-GATE-PIXIVAR-APPROX", !as.pixel_ivar_approx || as.approx_gate_passed}, …}`；:591-593 `cov["input_covariance"] = "provided"; cov["input_covariance_ref"] = "phase2_extensions.input_covariance"; cov["avail"] = "available";`；:648 `{"joint_covariance_ref", as.joint_covariance ? "phase2_extensions.joint_covariance" : ""}`；:1193-1196 重开侧只判 `ref.empty()`；:622-628 `deficit.value=0.0`＋`is_lower_bound=true`；:902-904 非正信息量→方差 0
- 上位依据：DATA_SEMANTICS §31.10 状态语义（`PENDING_OWNER_SIGNOFF` 须 fail-closed 且只写该状态词）；13_integration.md §7「相关噪声无描述 → variance 不完备，标记」；§31 最小集对 `unavailable.{flag,reason,scope}` "取值 = 真实原因"的要求。
- 现状：`as.pixel_ivar_approx` 与 `as.approx_gate_passed` 在本 TU **从未被赋值**（Assembly 默认 false，:614 分支不可达）⇒ `FZ-GATE-PIXIVAR-APPROX` 恒真；`input_covariance/avail` 在 `joint.provided==false`（独立帧路线，此时 SB 方差实为 :994 的对角求和）时仍写 "provided/available"；写出的 ext 键清单（:723-743 全量枚举）**不含** `input_covariance` 与 `joint_covariance` ⇒ 两个引用悬空；`deficit=(exact-diag)/exact` 的 value 恒 0.0 且无 exact 计算。
- 差在哪：把"门通过/输入可用/可重建引用"写成产物自述字面量——判据恒真的方向已钉死（真值常量不可能为假 ⇒ 恒绿）。
- 后果：产品的自证面宣称已消费输入协方差、已通过近似门，而字节面只能证明它没做；下游若按 `_ref` 取数会取到不存在的块。
- 定级：S2（缺生产触发路径 F1）
- 怎么算修好：`gates` 只写**本文件真算过的**判据并把未算的写 `false`+reason；`input_covariance`/`avail` 改为按 `joint.provided` 分支赋值，未提供时写 `not_provided` 并置 `covariance.avail="unavailable"`+reason（与 §31 的 `unavailable` 三键同构）；两个 `_ref` 要么真写块要么不写；重开侧 :1193 由"非空"升级为"能在记录里解析到该 JSON Pointer"。
- 置信：CONFIRMED

### CR-61-14 · 追溯字段用形似值填满：假 config_hash、占位 target_pixel_area、把条款登记表当"标定脚本"、日期当 MC 种子、把 PENDING 条款自证成 FROZEN
- 轴：S（主轴）＋C
- 位点：:421-422（`meta.config_hash.empty() ? ("sha256:" + Sha256::hex_of_string("phase2-config")) : meta.config_hash`）、:426-427、:455、:463-467、:646 状态串 `"frozen-in-domain-constant-FZ-PROV-KCORR-VALUE"`
- 上位依据：`DATA_SEMANTICS.md:3286`「`k_corr` 缺适用域/标定脚本/固定种子或跨域外推 → REJECT」；§31.10 状态语义条；标定实现真实锚 `docs/contracts/TEST_MATRIX.md:41`（`k_corr≈1.3883; rtol=1e-9; lib/algorithms/coverage/tests/synthetic_gate.cpp`）。
- 现状与差在哪：`config_hash` 缺失时不报错而写一个**常数字符串的哈希**（任何一次这样的产品哈希相同 ⇒ 无法区分配置）；`calibration.script` 指向条款登记表 JSON（不是可跑的标定件），`seed=20260915` 全仓无第二处出现（检索式 `git grep -n "20260915"` ⇒ 只命中控制包日期），`lookup_table_ref="K_CORR_DOMAIN_B"` 无定义面；条款状态 `PENDING_OWNER_SIGNOFF` 被写成含 "frozen" 的自述串。
- 后果：ACR/复现与"标定可追"这两项对外主张由这些字段承载，而字段值不可解析；§3286 的 REJECT 判据退化为"键在不在"。
- 定级：S2（不改科学值，改的是可复现主张；且 F1 无生产路径）
- 怎么算修好：`config_hash` 空 ⇒ 直接 fail-closed；标定锚改指真实测试件路径并把 2000 次实现的种子与 `k_corr_empirical` 一并登记；`K_CORR_DOMAIN_B` 要么给出跟踪面查表件要么删；状态词按 §31.10 写 `PENDING_OWNER_SIGNOFF`。
- 置信：CONFIRMED

### CR-61-15 · IVAR 平面把 NaN 方差折成 0，与同记录自declared 的 `invalid_policy="nan_or_support_le_0"` 冲突，且与"无覆盖"不可区分
- 轴：R
- 位点：`phase2_integrate.cpp:502-505` `ivar[i] = (as.variance[i] > 0.0) ? (1.0 / as.variance[i]) : 0.0;`；:595-598 `cov["variance_plane"] = {…{"invalid_policy","nan_or_support_le_0"}}`
- 上位依据：记录自身声明的 invalid_policy（:596）即"无效以 NaN 表示"；13_integration.md §3 输出面列 `variance、correlation、support`；条文面零处允许"无效⇒0 权重"这种折叠（检索式：`git grep -n "invalid_policy\|nan_or_support" -- docs eng/contracts`）。
- 现状：`NaN > 0.0` 为假 ⇒ 落 0.0；`variance==0` 亦落 0.0；写盘前无任何非有限检查，重开门只看 header/位深/长度（:1089-1103），不看数值 ⇒ VARIANCE=NaN、IVAR=0 的产品可带绿灯发布。
- 差在哪：三种语义（无数据 / 方差坏 / 方差为零）压成同一个 0；NaN 的表示由合同侧规定却被代码折叠。方向钉死：0 权重让下游按"该像素无输入"处理 ⇒ **不会**把噪声放大（不偏乐观），但会把"有支持度、方差坏"的像素伪装成无覆盖，使支持度—ivar 一致性判据永远查不出这类像素。
- 后果：产品面值不可信（NaN 直接落 FITS），且"无效像素计数/支持度"类对外指标失真。
- 定级：S2（F1）
- 怎么作修好：写盘前校验 `isfinite(signal)&&isfinite(variance)&&variance>0`，否则按 :596 声明写 NaN 并在 manifest 记 `n_invalid_pixels`；或把 invalid_policy 改成 `zero_ivar` 并同步 :596。两处必须同批改，否则改一侧即与另一侧判红。
- 置信：CONFIRMED

### CR-61-16 · 联合求逆无秩容差、无κ报告，且 :935 丢弃 `invert()` 返回值留越界路径
- 轴：R
- 位点：`phase2_integrate.cpp:62` `if (!(best > 0.0) || !std::isfinite(best)) return false;`；:851（检查返回值）vs :935 `invert(joint.c_in, static_cast<int>(n), &ci);`（丢弃）＋ :941-944 对 `ci[...]` 按下标读；:892 块求逆
- 上位依据：registry clauses[68] `FZ-AP2S-RANK-RTOL` = 1e-10，fail_closed 原文「秩亏 → 该元素/块 unavailable，**禁止伪逆静默**」；本文件 :1269 在 UPM 侧用了它，求逆侧没用。
- 现状与差在哪：主元只要严格为正即通过 ⇒ 相对秩 1e-300 的近奇异阵照样"求逆成功"；`ci` 在失败时不被赋值（:84 `*out = std::move(inv)` 仅在成功路径），:934 声明的空 `Mat ci` 会 :941 起被越界下标访问（实践上因 :851 同阵已成功而不可达，但接口是 UB 形态）。
- 后果：`W = AᵀC⁻¹A` 由近奇异逆给出时虚高 ⇒ `Var(F_hat)=1/W` 偏小 ⇒ 点源 SNR 偏乐观（方向已钉死为误绿侧）；同时 :909 的 corr_ratio 门被同方向推大 ⇒ 更容易放行联合路线。
- 定级：S2（F1）
- 怎么算修好：给 `invert` 加相对秩判据（复用 `FZ-AP2S-RANK-RTOL` 1e-10 的具名常量）并返回秩/κ 供记录；:935 检查返回值，失败即 `res.error`；把 C_in 的 κ 写进 covariance 块。
- 置信：CONFIRMED

### CR-61-17 · UPM/排异/降级"接线"以夹具喂判据：残差构造、支持度全 K、降级门测量值字面量 0，evidence 串仍宣称门已过
- 轴：R（主轴）＋D
- 位点：:1363 `const std::uint32_t n = 4;`；:1372 `residual[i] = (i == 0) ? (y0 - pred) : (y0 - pred + 0.3 * static_cast<double>(i));`；:1374 `sigma_p1[i] = std::sqrt(cv);`；:1452-1453 `std::vector<std::uint8_t> cov(P,1), cov_known(P,1), sup_known(P,1);` `std::vector<std::uint32_t> sup(P, K);`；:1438-1441 `si.summary_complete = 1; si.spatial_residual_p95 = 0.0; si.spatial_trend = 0.0; si.power_loss = 0.0;`；:1471-1473 `const int tok_ok_rc = p2_weight_source_token_reject(tokens_ok, 3, …); (void)tok_ok_rc; p2_rejection_weight_surface_guard(tokens_bad, 5, …);`；:1480 evidence 串 `"FZ-DEGRADE-SCALAR: spatial gate + power-loss gate + p05/p50/p95"`
- 上位依据：13_integration.md §8 末段称这些词表检查与门是"在役判据的执行面——不是死代码"；FZ-DEGRADE-SCALAR 在 DATA_SEMANTICS:3372 附近列于 FROZEN 清单（要求真实降级测量）。
- 现状：`run_upm_rej_samp_wiring` 的前半用真实帧数据（:1247-1262 观测、:1290-1299 解出 g/b/s）；后半的排异分类、覆盖分类、标量降级门的**输入全是硬编码夹具**，且两个 token 门的返回值一个被 `(void)` 丢弃、一个根本不判 ⇒ 无论产品里写了什么权重来源，这段都不会红。
- 差在哪：判据方向钉死——`power_loss=0.0 ≤ 0.05`、`spatial_trend=0.0 ≤ 0.10` 为恒真（0 恒小于正阈值），支持度恒 =K ≥ 2 恒成立 ⇒ 三支全恒绿；evidence 串把恒绿写成"门已过"。
- 后果：`UpmRejSampResult` 的 `reject_status/accepted_count/z/scalar_verdict/n_supported` 等一旦被报告面引用即为无源数值；"σ_eff²/排异继承阈值已接线"的对外主张由夹具支撑。
- 定级：S2（F1；且结果字段未进产品记录，故不升）
- 怎么算修好：把这段整体挪到 `eng/tests/`（它本质是 ABI 冒烟测试），或改喂真实 per-control 残差与产品支持度平面；evidence 串按实际判定来源改写；token 门改为读 `as.weight_sources` 并判定返回值。
- 置信：CONFIRMED

### CR-61-18 · 具名"冻结常数"零使用而同判据处写重复字面量；`sigma_bg` 定义与合同不同名同量
- 轴：C
- 位点：`.h:64-71` 七个 `constexpr`（`kEpsPixivar/kEpsPixivarSup/kIdentRtol/kEpsfRtol` 在本 TU 使用数分别为 0/0/0/0，`grep -c` 实测）；实际判定用字面量 :346/:863/:1191（1e-9）、:845（1e-12）、:618（串 "0.05"）；:1222-1224 `sigma_bg` 的算法
- 上位依据：`.h:62` 自述「冻结常数（逐字来自 docs/contracts/DATA_SEMANTICS.md §31，**不重新定值**）」；`DATA_SEMANTICS.md:1764`「`σ_bg` = patch 的稳健尺度（MAD×1.482602218505602）」
- 差在哪：常量的"唯一事实源"靠**再抄一遍**维持（具名值与字面量当前相等，但没有机制保证）；`sigma_bg` 用"逐像素 σ 的算术平均"（:1223 还先 `sqrt(max(0.0,·))` 吞掉负方差）替换条文的稳健 MAD 尺度，随后进 `control_variance = k_corr·(π/2)·σ_bg²/N`（:1227）——公式同名、输入的 σ_bg 不是同一定义。方向钉死：平均 σ ≤ 稳健尺度的等效量一般不成立，但 `mean(σ)²` 与 `σ_稳健²` 谁大取决于分布形状 ⇒ 我不主张单调偏置，只主张**同名不同量**（按 §3 门槛，这条不报"误绿"）。
- 后果：条款改值时代码不跟随（改一处判红、另一处仍放行）；`control_variance` 与 §25.3 定义的产品量不可比，UPM 权重与排异阈值随之偏离条文。
- 定级：S2
- 怎么算修好：把 :346/:863/:1191/:845 换成 `kIdentRtol/kSnrIdentRtol/kEpsfRtol` 具名常量并删掉未用项（或按用途各立其名）；`sigma_bg` 改按 patch 稳健尺度算或换名并在 §25.3 登记；`allow_additive_only_single_frame` / `c_in_has_unrepresented_shared_terms` / `K_CORR_DOMAIN_B` 三个未见条文的名字一并登记（检索式：`git grep -n "allow_additive_only_single_frame|c_in_has_unrepresented_shared_terms|K_CORR_DOMAIN_B" -- docs eng/contracts`）。
- 置信：CONFIRMED

### 不报的核过项（本文件）
- `has_forbidden_p33_key`（:112-125）与 `weight.kind`/`sources` 的拒绝面（:1126-1171）方向正确、逐 token 全等、命中即 fail ⇒ 合 13_integration.md §4.0；退役 token 的拒绝说明（:204-216）与 `UNIFIED_MODEL.md:58` 同文 ⇒ 不报。
- `quadratic_law_holds`（:1078）、BUNIT 可判性（:1085）、sha 重算与 manifest/provenance 三处一致性（:1111-1117）⇒ 都是"不信任记录、重开重算"的正例，合规。
- FITS 常数（2880/80/36/10/30/8/2879）与 `bitpix=-64`：单位换算与格式定义常数 ⇒ 按 §4 明令不报。
- 负向注入字段（RunMeta `force_*`/`inject_*`）：`.h:140` 自述"生产恒为空"，且注入结果会被重开门拒掉 ⇒ 我**不**报"库里有注入旋钮"；只在 CR-61-19 登记"公共结构体里的测试旋钮无生产侧防御"这一形态供负责人裁。
- `open_phase2_frame_set` 对 `normalization_deferred_to=="phase2"`、record sha256、四分量、n_common≥3 的检查与 OI-01 一致 ⇒ 合规。

# 四、`lib/algorithms/integration/v6_phase1/include/astrocs/v6/phase1_product.h`（244 行，全读）

> 定级前提（F1 的延伸）：本层公开入口 `write_phase1_product` 的调用点全在 `eng/tests/integration/v6_p1/v6_p1_integrate_test.cpp:239,245,413,608` 与 `eng/tests/integration/v6_p2/v6_p2_integrate_test.cpp:201`（`git grep -n write_phase1_product` 全量命中，`lib/` 侧仅本头与 `v6_phase1/` 自身）⇒ 本文件的 finding 亦封顶 S2。

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :70-73 | `kPhase1ProductSchema`/`TypeId`/`ScienceFile`/`RecordFile` | `astrocs.v6.phase1-product/v1` 等 4 串 | 产品身份与文件名常量 | 文献值(条款) | `eng/contracts/schemas` 与 DATA_SEMANTICS §31 的 v6 对象名（.h:12「冻结锚（逐条符合，不得放宽）」） | —— | CONFIRMED |
| :77-85 | `Phase1Units` 十个单位/语义串与一个幂次 | `ADU/sr`、`ADU^2`、`ADU^2/sr^2`、`sr^2/ADU^2`、`ADU^-2`、`ADU^-1`、`ADU`、`1`、`px^2`、`surface_brightness` | 冻结单位律 | 文献值(条款) | 本头 :13-19 的 `FZ-UNIT-*` 逐条同名；`psfsw_robust_weight="1"` 由 :20-24 与 PSFSW-RETIRE-03 声明"仅为旧产品可判而保留、不构成接受依据" ⇒ 按派单 §4 不报 | —— | CONFIRMED |
| :87, :179 | `pixel_area_power` | -2 / 0 | 面亮度二次律指数；重开视图的未判值 | 文献值(条款) | `FZ-P3-BUNIT-QUADRATIC`/`FZ-BUNIT-SEMANTICS`（:25）；0 = "尚未判定"由 `bc.decidable` 消费（phase2_integrate.cpp:1085） | —— | CONFIRMED |
| :93 | `psf_profile` 注释 | `Sum P = 1`、`>=0` | PSF 归一与正值约束 | 文献值(条款) | `FZ-FORMULA-*`/PSF.md（四分量与 P_eff 式，见 phase2_integrate.cpp:973） | —— | CONFIRMED |
| :96 | `PointSourceInput::a` | 1.0 | 缺省乘性光度响应 a_k=1 | 待确认 | 条文面零处规定"a 缺省 1"（`git grep -n '"a"' -- eng/contracts/schemas` ⇒ 无该语义）；与 phase2_integrate.cpp:330 的 `value("a", 1.0)` 同一形态 | a 真值≠1 而按 1 处理 ⇒ `A=a_k P_k` 与 W 同尺度错，偏置方向随 a 而定（不主张单调） | CONFIRMED |
| :108 | `a_nea` | 0.0（`<=0` 复算） | 有效噪声面积缺省 | 结构性不适用 | 面积 px² 的 0 非物理值 ⇒ 可作"未给"哨兵（实现在 phase1_product.cpp:411 `(in.psfsw.a_nea > 0.0) ? … : pst.a_nea`，与本注释一致） | —— | CONFIRMED |
| :109 | `a_ref` | 0.0（`0 => a_nea`） | 参考面积缺省 | 结构性不适用 | 同上（面积 0 非物理） | —— | CONFIRMED |
| :110 | `background_robust_mean` | 0.0（注释「必须 > 0」） | 稳健背景均值入参 | 待确认 | "必须>0"是**注释**而非条款；条文侧 `FZ-CAL-FLOOR`（DATA_SEMANTICS:3372 附近 FROZEN 清单）不覆盖此入参 | 若 .cpp 未校验则 0 通过⇒背景相关量为 0/除零 | PARTIAL（未在本批清单内读 phase1_product.cpp 的校验支） |
| :118-119 | `construction` / `independence_proof` | `external_catalog` / `external_reference_catalog` | 共同星集构造与独立性声明的**默认串** | 待确认 ⇒ CR-61-21 | 13_integration.md §7「PSF 信号权重复合分量缺共同星集/selection function → fail-closed」；接受集校验在 `lib/algorithms/photometry/cpp/src/psfsw.cpp:368-369` | 默认值落在接受集内⇒"缺证明"这一支恒不红 | CONFIRMED |
| :121-122 | `calibration_sample_id` / `acceptance_sample_id` | `phase1_psfsw_calib` / `phase1_psfsw_accept` | 样本 ID 占位默认 | 待确认 | 跟踪面内这两个 ID 无登记样本（`git grep -n "phase1_psfsw_calib\|phase1_psfsw_accept"` 除本行外无第二处） | 占位 ID 进产品⇒样本可追溯主张失真 | CONFIRMED |
| :127 | `DrizzleInput::nside` | 2 | 默认 HEALPix 分辨率 | 待确认 ⇒ CR-61-22 | `docs/science/DRIZZLE.md:31`「`nside, order`: 无量纲（`nside=2^order`）」、:36「`target_order` 由 MOC union 决定」⇒ 默认值应来自数据而非结构体 | nside 偏小⇒覆盖 cell 粗、支持度虚高（偏绿） | CONFIRMED |
| :128-129 | `nx`/`ny` | 0 | 输入栅格尺寸（必填） | 结构性不适用 | 数组维度；0 由下游尺寸检查拒 | —— | CONFIRMED |
| :130 | `DrizzleInput::pixfrac` | **1.0** | drop 收缩因子默认 | 待确认 ⇒ CR-61-20 | 权威侧三处一致给 0.8：`eng/packaging/config/defaults.json:666-667`（`drizzle.pixfrac=0.8`，`authority_status:"owner_adjudicated"`）、`docs/science/DRIZZLE.md:26`「默认 0.8」、`docs/contracts/CONFIG_CONTRACT.md:54`「**数值 0.8**」 | defaults.json:677 原文明写取 1.0 的后果：「取 1.0 时相邻 drop 仅边界相接，源像素落点错位处出现**无覆盖缝隙**」 | CONFIRMED |
| :131 | `closure_rel_tol` | 1e-6 | 球面多边形几何闭合相对容差 | 待确认 ⇒ CR-61-22 | 语义锚 `v6_spherical_overlap.h:73-75`（"判据取绝对值；超出即具名失败"）但**无数值来源**；docs 侧零命中（检索式 `git grep -n "closure_rel\|闭合容差" -- docs` ⇒ 仅 DRIZZLE.md:97 的"几何闭合时"理想条件） | 放松⇒把不闭合的足迹判为闭合⇒通量守恒主张偏绿；影响该帧全部 drizzle 输出 | CONFIRMED |
| :141 | `software_sha` 注释 | 40 hex | 摘要形态 | 结构性不适用 | 长度定义常数 | —— | CONFIRMED |
| :143-146 | `coordinate_frame/epoch/provider/build_id` | `icrs`/`J2000`/`cpu_baseline`/`phase1-v6-integrate` | 默认坐标与来源声明 | 待确认 | 与 phase2_integrate.cpp:428-429 同族：坐标框架/历元由默认串写死，而非由输入 WCS 解析 | 默认值≠真值时产品的天球参考系声明错 | CONFIRMED |
| :181, :189-191, :200, :232 | 视图标量缺省 | 0.0（w_info/q/flux/flux_variance/norm_median_target/weight_value/unnormalized_wt/median_wt） | "未判定"以 0 表示 | 待确认 | 同族形态：`has_weight_value` 布尔与 `weight_value=0.0` 并存（:190-191），说明作者知道两种表示法，但其余七个标量只给 0 | 0 与"信息量为 0"不可区分；消费者若漏判 `has_*` 即把缺值当零（方向=偏红或偏绿随用法） | CONFIRMED |
| :195, :197, :205, :229 | `n_common`/`n_components`/`nside`/`n_frames` | 0 | 计数类缺省 | 结构性不适用 | 计数下界即 0，且下游有 `n_common < kCommonMin`、`n_components != 4` 硬门（phase2_integrate.cpp:312,315）⇒ 0 必被拒 | —— | CONFIRMED |
| :49, :31 | 退役 canonical `median_target=1.0`；`FZ-MODE-PRODUCTION {point_information, surface_gls}` | 1.0 | 组内归一目标与生产接受集 | 文献值(条款) | `eng/contracts/data/v6_clause_registry_v1.json:79` 生产接受集 = `point_information \| surface_gls`；OI-01 组内 median=1 | —— | CONFIRMED |
| :230-231 | `wt_unnormalized` / `w_psfsw` | —— | 退役面的输出容器 | 结构性不适用 | 本面 :220-225 声明"无条件 fail-closed、不产出 w_psfsw"，字段保留仅为可判 ⇒ 按派单 §4（已由条文声明退役）不报 | —— | CONFIRMED |

## findings

### CR-61-19 · 被声明为"Phase2 消费面最小集"的结构里没有帧级 SNR、没有 `F_ref`、没有稀疏 SNR 层 ⇒ 创新点②的输入在本磁盘链上无字段可传
- 轴：D
- 位点：`phase1_product.h:168-207`（`Phase1ProductView` 全量字段枚举：`frame_id/run_id/output_hash`、四个单位串、`signal/variance/ivar/support_bunit`、`w_info/q/flux/flux_variance`、weight 词表 5 串、`retired_*` 2、`group_normalized/norm_median_target/has_weight_value/weight_value`、`valid/reason/n_common/common_star_set_id/selection_function_id/n_components/component_names/unnormalized_wt/normalization_deferred_to`、`science_path/science_bytes/nside/target_ipix`）——**无 `frame_snr`、无 `reference_flux`/`F_ref`、无 `sparse_snr_layer`、无 SNR 单位项**
- 上位依据：13_integration.md §3 输入面原文「归一化产品组、UPM 参数、rejection、PSF/信息层、**帧级 SNR（文件头）**、[**稀疏绝对 SNR 层**]、配置」；§4.0「分工固定：**阶段一只生产信噪比（稀疏 SNR 控制点）**」。
- 现状：Phase2 侧唯一消费入口 `phase2_integrate.cpp:309` 用的就是本视图（`phase1::open_phase1_product(d)` → `r.view`），其权重输入只有 `view.w_info`（CR-61-11）；本头 :1 自述"Phase1 单帧产品装配…Phase2 消费面"，:40-41 的 OI-01 也只谈归一，不提 SNR。
- 差在哪：设计把"阶段二按逐像素 SNR 定权"写成阶段一→阶段二交接的全部理由，而这条交接面的结构体里根本没有那两样输入的载体 ⇒ 这不是接线疏忽（CR-61-5 的 `in.sparse = nullptr`）而是**合同结构缺字段**；v6 链上 SNR 无法跨阶段传递是由本头决定的。
- 后果：v6 产品链（本层＋phase2_integrate）无论怎么接都只能给出帧级 `W_info` 权重；创新点②"逐像素逆方差"在该链上无载体，README/条文的"正本实现"表述与该结构不符。
- 定级：S2（F1：本层调用点全在 eng/tests；生产链走 scheduler/coverage 的 HiPS 头，另见 CR-61-5）
- 怎么算修好：在 `Phase1ProductView` 增 `frame_snr/has_frame_snr/reference_flux/has_reference_flux` 与稀疏层句柄（或明确另立 `Phase1SnrView`），并把 13_integration.md §3 的输入清单逐字段映射到本结构；同步在 `docs/contracts` 登记（否则改了代码改不到合同面）。
- 置信：CONFIRMED

### CR-61-20 · `DrizzleInput::pixfrac` 默认 1.0，落在负责人已裁定值（0.8）的对面，且权威条文把 1.0 的后果写死为"无覆盖缝隙"
- 轴：C（主轴）＋R
- 位点：`phase1_product.h:130` `double pixfrac = 1.0;`
- 上位依据：`eng/packaging/config/defaults.json:666-677`——`"key":"drizzle.pixfrac","value":0.8,"authority_status":"owner_adjudicated"`，note 原文：「默认 0.8：drop 相互重叠，输出网格无未覆盖缝隙；**取 1.0 时相邻 drop 仅边界相接，源像素落点错位处出现无覆盖缝隙**」；`docs/science/DRIZZLE.md:26`「`pixfrac` drop 收缩因子 (0,1]（`drizzle.pixfrac`，**默认 0.8**）」；`docs/contracts/CONFIG_CONTRACT.md:54`「**数值 0.8**（`defaults.json#drizzle.pixfrac`）」；`lib/algorithms/coverage/src/sampler.cpp:81` 注释亦称「pixfrac=0.8 生产默认」。
- 现状：代码侧默认站 1.0；配置面、科学面、合同面三侧都站 0.8 且 0.8 是"owner_adjudicated"。本头无任何注释标示该默认与裁定的关系。
- 差在哪：同一键的默认值在四侧不一致，而代码侧那个值被权威文本自己判为产生覆盖缝隙；调用方不显式赋值即拿到被禁止的那一侧（缺键兜底方向站在裁定反侧）。
- 后果：① 用本层装配的产品其 drop 覆盖语义与生产裁定不同 ⇒ 支持度/覆盖平面与 `k_corr` 适用域（`DATA_SEMANTICS.md:1739` 的 MC 标定即按 pixfrac=0.8 做的）不再是同一个域；② 缝隙像素在下游表现为无覆盖 ⇒ 与 CR-61-15 的"ivar=0 与无覆盖同值"叠加后不可分辨。
- 定级：S2（F1：本层无生产调用点；**但"已裁未贯穿"这一族在本仓已有多侧实例，本条是它在 Phase1 装配面的新落点**）
- 怎么算修好：本头默认改 0.8 并加一行"与 `defaults.json#drizzle.pixfrac` 同源"的引用；更彻底的做法是删掉默认（`pixfrac` 变 `double pixfrac = 0.0 /* 必填 */`，:131 的 0 非物理值可作哨兵）＋ `write_phase1_product` 侧对未设值 fail-closed，与 `defaults.json:669` 的"非法值显式 NO_DATA，不静默夹逼"同口径。改默认的同时必须同步 `eng/contracts/schemas` 的默认一致性门，否则门会判红。
- 置信：CONFIRMED

### CR-61-21 · `independence_proof`/`construction`/两个样本 ID 的默认值就是下游门的合法通行证：缺证明不能判红
- 轴：R（主轴）＋C
- 位点：`phase1_product.h:118-119` `std::string construction = "external_catalog"; std::string independence_proof = "external_reference_catalog";`、:121-122 两个 `*_sample_id` 占位
- 上位依据：13_integration.md §7「PSF 信号权重复合分量缺共同星集/selection function → **fail-closed**」；§8「必须用共同星集/selection-function 门」。校验实现：`lib/algorithms/photometry/cpp/src/psfsw.cpp:368-369` `if (sf.independence_proof != "external_reference_catalog" && sf.independence_proof != "reference_stack_single_threshold") {`
- 现状：结构体默认即接受集第一个 token；`common_star_set_id`、`selection_function_id`（:115-116）虽无默认，但独立性证明这一项有；两个样本 ID 的默认串在跟踪面无第二处登记（检索式见 §2.5）。
- 差在哪：判据方向钉死——把"必须显式给出"的字段做成"不给出也满足"的默认 ⇒ 该门对本层任何调用者恒不红（0 概率触发），条文要求的 fail-closed 面被默认值抹平。
- 后果：psfsw 复合分量的独立性/星集主张可由默认串自证；影响面限于诊断与基线对照（psfsw 不进科学叠加权重），故不升 S1。
- 定级：S3
- 怎么算修好：删 :118-119 的默认（改 `= "unspecified"` 或直接无默认值并令其必填），由 `write_phase1_product` 在缺失时 fail-closed；两个样本 ID 若无登记样本则不给默认串。
- 置信：CONFIRMED

### CR-61-22 · `closure_rel_tol=1e-6` 与 `nside=2` 两个数值零出处，且 `nside` 默认与"阶数由 MOC union 决定"的条文相互排除
- 轴：S
- 位点：`phase1_product.h:131` `double closure_rel_tol = 1e-6;`；:127 `int nside = 2;`
- 上位依据：闭合容差——`v6_spherical_overlap.h:73-75` 只说"判据取绝对值；超出即具名失败"，无数值；docs 侧检索式 `git grep -n "closure_rel\|闭合容差\|几何闭合" -- docs` ⇒ 唯一命中 `docs/science/DRIZZLE.md:97`「Σ_p F_p = … （几何闭合时，§7）」，即把闭合当**理想不变量**而非带容差的判定。阶数——`DRIZZLE.md:36`「`target_order` 由 MOC union 决定」；:35「`nside=2^order` 为 2 的幂」。
- 现状：1e-6 决定"多大面积缺口仍可接受"，直接进入 `v6_spherical_overlap.cpp:121/153/167` 的 `overlap_exceeds_drop / overlap_area_deficit` 判定；nside 默认 2（order=1，96 个像素）与"由数据决定"互斥。
- 差在哪：一个决定通量守恒是否成立的数值窗没有任何推导、实测或条款；一个应由输入决定的量被写成字面量默认。方向：容差放松⇒不闭合被判闭合⇒"通量守恒（严格不变量）"主张偏绿；nside 偏小⇒覆盖过粗。
- 后果：`closure_rel_tol` 是通量守恒门的实际松紧度，无出处即无"严格"二字的可辩护性。
- 定级：S2
- 怎么算修好：为闭合容差给出推导（球面多边形面积相对误差的数值下界，或直接以 `sqrt(eps)` 类可导出式）并登记进 DRIZZLE.md §7 或合同；nside 改成无默认＋必填（或 `target_order` 入参）。
- 置信：CONFIRMED

### 不报的核过项（本文件）
- `a_nea`/`a_ref` 的 0 哨兵：面积量在物理上不可为 0，且 .cpp 侧的复算实现（phase1_product.cpp:411）与头注释逐字一致 ⇒ 与 CR-61-2/15 的"用 0 表示缺失"不同类，不报。
- `psfsw_robust_weight="1"` 与 `Phase1GroupConsumption::w_psfsw` 等退役面字段：:20-24、:220-225 已按 PSFSW-RETIRE-03/FZ-MODE-RETIRED 显式声明"不构成接受依据、无条件 fail-closed" ⇒ 按派单 §4 不报。
- :87/:179 的 `pixel_area_power` 与 :77-85 单位串与 `FZ-UNIT-*` 逐字同名 ⇒ 无分歧。
- `Phase1ProductView::retired_weight_object_declared`（:186）用**显式布尔**登记退役声明而非静默接受，与 13_integration.md §8 末段的解锁顺序一致 ⇒ 正例。

---

# 五、`lib/algorithms/resample/p3_rsmp.h`（501 行，全读）

> 链接事实：本模块 **在生产链接闭包内**——`CMakeLists.txt:961` 列 `astrocs_p3_rsmp`，`module_adapters.cpp:138` 直接 `#include "../../../algorithms/resample/p3_rsmp.h"` 并使用 `p3rsmp::parse_mode`/`P3Mode`（:14121-14137）与 `Bunit::canonical()`（:8461 注释指明按 DATA_SEMANTICS §31.1a 映射）。故本头相关判据可写生产触发链；但本批两条 finding 的最后一跳分别落在 `p3_rsmp_propagation.cpp`（清单外）与"无调用点"，故按 S2 定级并注明缺口。

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :33 | `kNaN` | quiet_NaN | 无效值表示（全模块统一哨兵） | 结构性不适用 | 与 `docs/standards/NUMERIC_STANDARD.md:82`、`PHASE3_RESAMPLE.md:126`（覆盖级 NaN、禁零填）同口径；**这是本批唯一把"缺失"与 0 分开的正面样本** | —— | CONFIRMED |
| :39-43, :47, :94, :105, :111-115, :306-312, :314 | 各 enum 显式值 | 0..4 | 状态/模式/语义/核属性枚举基数 | 结构性不适用 | 枚举基数与判别式，不含科学量 | —— | CONFIRMED |
| :64-65, :98 | `Bunit::adu_power/px_power`、`pixel_area_power` | 0 | 单位幂次未判定值 | 结构性不适用 | 0 = 幂次中性且未判，须由 `resolve_bunit`(:106) 判定后才被消费 | —— | CONFIRMED |
| :79-87 | `units::` 九个外部常量 | `ADU/sr` 等 | 冻结单位表 | 文献值(条款) | 本头 :14-17 逐字对齐 `DATA_SEMANTICS.md §31.1`；`eng/contracts/data/v6_clause_registry_v1.json units_table`（:77 注明） | —— | CONFIRMED |
| :127-132, :197, :318, :338, :379, :397-400, :441, :449 | `= kNaN` 的 14 处标量 | NaN | "未提供"的数值哨兵（含 Oracle 证据 6 项、Q/W/F_hat/var、近似误差） | 结构性不适用 | NaN 与 0 可分＝本仓数值标准的正例；`OracleKind::None`(:115,:122) 与之配对 ⇒ 漏填不可被误读为零 | —— | CONFIRMED |
| :137, :139-148, :174, :214, :217-218, :294, :317, :329-330, :336-337, :365-368, :370-376, :378, :380-386, :407-410, :429-433, :438-440, :444-448, :450-451, :455-457, :462-463, :466-468, :470-471 | `= false` / `RequiresRegistration` / `valid_out` 等 40 余处 | false | "未注册/未声明/未启用"侧默认 | 结构性不适用 | 门要求"缺 Oracle/误差界/边界定义 → REJECT"（:168），默认落未注册侧与条文同向；`epsilon_corr_ratified=false`(:317) 属同族且实装为门（见下） | —— | CONFIRMED |
| :320 | `GateConfig::psf_sum_tol` | 1e-9 | 冻结等式 `Σ pi = 1` 的判定容差 | 待确认 | 条文只给**等式**：本头 :9「pi = S p，Σ pi = 1（ALG-P3-007）」；容差数值在 docs/合同零登记（检索式：`git grep -n "psf_sum_tol\|1e-9" -- docs/algorithms/PHASE3_RESAMPLE.md docs/science`）；使用点 `p3_rsmp_failclosed.cpp:139`、`p3_rsmp_propagation.cpp:209` 用它判 Reject | 放松⇒归一被破坏的 PSF 仍放行⇒通量/面亮度守恒偏绿；影响点源模态全部输出 | CONFIRMED |
| :321 | `GateConfig::row_sum_tol` | 1e-9 | 行归一 `Σ_j R_ij = 1` 的守卫容差 | 待确认（**声明了但全模块零使用**）⇒ CR-61-25 | `git grep -n "row_sum_tol" -- lib eng/tests` ⇒ 唯一命中即本行 | 见 CR-61-25：行归一实际只由自述布尔 `row_normalized` 判 | CONFIRMED |
| :322 | `GateConfig::bunit_tol` | 1e-12 | BUNIT 幂次解析的数值窗 | 待确认 | 本头 :105 的 `resolve_bunit` 语义是**串解析**（幂次为整数），1e-12 在整数幂次上的用途未说明；`git grep -n "bunit_tol" -- lib` 未见科学判定 | 影响面小（幂次判定本不需要浮点容差）；登记为"容差用在整数域"的形态 | PARTIAL（未逐行读 `p3_rsmp_units.cpp` 是否使用） |
| :331, :432 | `uncertainty_available` | true | 不确定度可用性自述 | 待确认 ⇒ CR-61-23 | 条文要求 unavailable 必带 reason（`DATA_SEMANTICS.md:3286` 三键齐备才算完成态） | 漏设⇒宣称有不确定度⇒偏绿 | CONFIRMED |
| :328, :431 | `measurement_capable` | SB 输入 true / 记录 true | 可测量性自述 | 待确认 ⇒ CR-61-23 | 可视化模态 :407 默认 false（安全侧）⇒ 同一文件两侧相反，无条文解释为何科学模态默认"可测量" | 漏设⇒降级面被判可测量⇒偏绿 | CONFIRMED |
| :333, :377, :469 | `covariance` 默认 | `ExactFull` | 协方差表示自述 | 待确认 ⇒ CR-61-23 | :306-312 的五档语义中 `DiagonalOnly` 须相关核否则 REJECT（:310），`WeightDerived` 恒 REJECT（:311）；默认落在**无此义务**的那档 | 传对角阵而漏设枚举⇒"精确完整"宣称无证据⇒偏绿 | CONFIRMED |
| :339, :474 | `provenance_complete` | true | provenance 齐备自述 | 待确认 ⇒ CR-61-23 | `FZ-PROV-MINIMAL-SET` 要求逐键判定；同处 `provenance_missing_keys`(:340,:475) 默认空 ⇒ 两项串起来"齐备且无缺键"永真 | 偏绿 | CONFIRMED |
| :437 | `bunit_quadratic_law_ok` | true | 二次律判定结果被当成入参 | 待确认 ⇒ CR-61-23 | 本头 :90 已有真判据 `is_quadratic_variance(...)`；记录字段却允许调用方直接写"已满足" | 偏绿（绕过 :90） | CONFIRMED |
| :445 | `psf_sum` | 1.0 | PSF 归一"已满足"默认 | 待确认 ⇒ CR-61-23 | 与 :320 的容差配合：`|1.0−1.0|≤1e-9` 恒成立 | 漏设⇒恒真 | CONFIRMED |
| :452 | `effective_psf_normalization_declared` | true | 归一声明"已给"默认 | 待确认 ⇒ CR-61-23 | `FZ-GATE-PSFSW-EPSF`/`FZ-P3-*` 要求声明归一方式；默认 true ⇒ 未声明也判已声明 | 偏绿 | CONFIRMED |
| :183-185, :209-210, :241-243, :256-257, :277 | 索引/维度 `= 0` | 0 | 下标、矩阵维度 | 结构性不适用 | 数组维度与下标初值，不进入科学语义 | —— | CONFIRMED |
| :185, :215 | `overlap_sr=0.0` / `coverage` | 0.0 | 重叠面积与覆盖累积初值 | 结构性不适用 | 几何量累加器初值；真值由 `NeighborhoodSet` 填 | —— | CONFIRMED |
| :244 | `InputGrid2D::at(x,y)` | —— | 无边界检查的下标访问 | 待确认 ⇒ CR-61-26 | 本头同族的 `TileMask::present_at` 在 `p3_rsmp_operator.cpp:11-16` 有完整三重判界；`at()` 无 | 越界读=UB；当前无调用点 ⇒ 现实影响为零 | CONFIRMED |
| :248-250 | `TileMask` 默认 | tile_px=1、tiles_x=y=1 | 单 tile 缺省 | 结构性不适用 | 1×1 是"整幅一个 tile"的中性形状，`present` 空 ⇒ `present_at` 返回 false（fail-closed 侧） | —— | CONFIRMED |
| :259-260 | `GridPlan::out_origin_x/y`、`out_step` | 0.0 / 0.0 / 1.0 | 输出栅格原点与步长（输入像素单位） | 结构性不适用 | 1.0 = 同尺寸重采样的恒等步长；原点是坐标系定义 | —— | CONFIRMED |
| :426 | `ProductRecord::mode` | `SurfaceBrightness` | 未声明模式时的默认枚举 | 待确认 | :425 `mode_declared=false` 同处存在 ⇒ 门若只看 `mode` 即拿到 SB；只看 `mode_declared` 即安全（需核 :494 `check_failclosed` 用法） | 取决于门读哪一个；本批未取证 ⇒ 登记 | PARTIAL |
| :5, :11-12 | 「三输出模式（FZ-P3-MODES）」「三模式 fail-closed **12 门**」 | 3 / 12 | 门数与模式数宣称 | 结构性不适用 | 枚举基数宣称；实际门数需按 `check_failclosed_all` 的 code 计数核对（清单外文件） | 若门数与宣称不符属文档面问题 | PARTIAL |

## findings

### CR-61-23 · 门入参的六个默认值站在"已满足"侧，其中三处是**判定结果本身**被当入参：漏设即恒绿
- 轴：R（主轴）＋C
- 位点：`p3_rsmp.h:437` `bool bunit_quadratic_law_ok = true;`、`:445` `double psf_sum = 1.0;`、`:452` `bool effective_psf_normalization_declared = true;`、`:474` `bool provenance_complete = true;`、`:331/:432` `bool uncertainty_available = true;`、`:328` `bool measurement_capable = true;`、`:333/:377/:469` `CovarianceRepresentation covariance = CovarianceRepresentation::ExactFull;`
- 上位依据：本头 :12 自称「三模式 fail-closed 12 门」；:19-20 自称「本文件不发明任何冻结阈值：OPEN 项以『未签字即 fail-closed』方式实现」。条文档面：`DATA_SEMANTICS.md:3286`「`unavailable.{flag,reason,scope}` 必填；取值 = 真实原因，三键齐备且非空才构成完成态」。
- 现状与差在哪：同一个结构体里，`valid_out`(:214)、`mode_declared`(:425)、`psf_present`(:444)、`epsilon_corr_ratified`(:317)、`KernelStatus::RequiresRegistration`(:137) 等都规矩地落在"未声明/未注册"侧，说明作者掌握该模式；上列七项却把**门的结论**写成入参默认。方向钉死：`|psf_sum−1.0| = 0 ≤ psf_sum_tol` 恒真（`failclosed.cpp:139` 正是这一判定）⇒ 漏设者必过；`covariance` 默认 `ExactFull` 时 `:206` 的 `DiagonalOnly && !correlation_kernel_present → REJECT` 分支根本不进入 ⇒ 传对角 `c_in` 而漏设枚举无任何红灯；`provenance_complete=true` 配 `provenance_missing_keys` 空 ⇒ 同一事实两路自证。
- 后果：`FZ-P3-BUNIT-QUADRATIC`、`FZ-GATE-PSFSW-EPSF`、`FZ-PROV-MINIMAL-SET` 三扇门与相关核要求都可被"忘记设字段"绕过；而 :19-20 的"不发明阈值、未签字即 fail-closed"宣称与这些"未提供即通过"的默认互相排除。
- 定级：S2（生产链接闭包内、`ProductRecord` 由 `p3_rsmp_propagation.cpp:61-63,124-135,133-135` 从输入结构体直填；缺"生产调用方是否确会漏设"的最后一段取证 ⇒ 不升 S1）
- 怎么算修好：把这七项默认改到未声明侧（`*_ok`/`*_complete`/`*_declared` 默认 false、`psf_sum` 默认 `kNaN`、`covariance` 增 `Unspecified` 档并默认之、`uncertainty_available`/`measurement_capable` 默认 false）；`bunit_quadratic_law_ok` 由 `is_quadratic_variance()`(:90) 现算而非由调用方声明。同批必改：`propagation.cpp` 的三处 `rec.* = in.*` 直填点与 `p3_rsmp_core_test.cpp` 中依赖"默认即通过"的用例，否则会出现改对反而判红。
- 置信：CONFIRMED

### CR-61-25 · `row_sum_tol` 声明了却全模块零使用；行归一（面亮度守恒的核心）只判一个自述布尔
- 轴：D（主轴）＋R
- 位点：`p3_rsmp.h:321` `double row_sum_tol = 1e-9;`（自称 :319「数值守卫（实现局部，不是科学阈值）」）；配套 :217 `bool row_normalized = false;`、:223 `bool full_coverage(double tol) const;`、:226-229 两个构造函数
- 上位依据：本头 :7「统一线性模型：y = R x（**R 行归一**）」、:226「R_ij = a_ij / Ω'_i（ALG-P3-002/SPEC §1.3）」；`docs/science/DRIZZLE.md:97` 把"几何闭合"写成通量守恒成立的前提条件。
- 现状与差在哪：`git grep -n "row_sum_tol" -- lib eng/tests` 唯一命中就是声明行本身；`p3_rsmp_propagation.cpp:86-89` 的门只读 `if (!r_op.row_normalized) { out.code = "G-P3-SB-00"; … }`，而该布尔由 `build_row_normalized`/`operator_from_entries_row` **自己置位**（构造即声明）；`row_sums()`/`full_coverage(tol)` 这两个能真正测量行和的接口只在 `eng/tests/unit/v6_p3_rsmp/p3_rsmp_core_test.cpp:39-45,64-65,348-349` 被调用，生产路径零调用。方向钉死：邻域重叠面积不闭合（`Σ_j a_ij < Ω'_i`）时行和 < 1 ⇒ 输出面亮度**系统性偏暗**、方差同向偏小，而门全绿。
- 后果：`G-P3-SB-00` 这条"面亮度语义须行归一"的门不检验它命名的性质；创新点③"加性天光与无接缝叠加"所依赖的重采样守恒在 Phase3 只由构造声明保证。
- 定级：S2（触发链最后一跳需读 `p3_rsmp_operator.cpp` 的置位逻辑，清单外；本头的证据已足以定性"声明的守卫未接"）
- 怎么算修好：在 `propagate_surface_brightness` 进入科学计算前对**每个 valid 行**实测 `|Σ_j R_ij − 1| ≤ row_sum_tol`（复用现成 `row_sums()`），不满足 ⇒ 该行 `valid_out=false`+NaN（与 :214/:194 的 MissingIsNaN 语义同族），并把实测最大偏离写进产品记录；同批给 `row_sum_tol` 一个可导出来源（FP64 行求和的 `n·eps` 界）或改用与 `PHASE3_RESAMPLE.md:174` 同式的相对容差。
- 置信：CONFIRMED

### CR-61-26 · 公共内联访问器 `InputGrid2D::at()` 无判界，与本头自己给出的正例（`present_at` 三重判界）不一致
- 轴：R
- 位点：`p3_rsmp.h:244` `double at(int x, int y) const { return value[static_cast<std::size_t>(y) * width + x]; }`
- 上位依据：`docs/standards/NUMERIC_STANDARD.md §MUST` 的 NaN/Inf 契约（越界读不属"显式不可用"，属未定义行为）；对照实现 `p3_rsmp_operator.cpp:11-16`（`present_at` 先判 `ix<0||iy<0||ix>=tiles_x||iy>=tiles_y`，再判 `idx>=present.size()`）。
- 现状与差在哪：负坐标、`x>=width`、`y>=height`、`value` 为空四种情形都直接下标越界；本头内当前**无调用点**（`git grep -n "\.at(" -- lib/algorithms/resample` 零命中，算子实现按 `value[...]` 或自行处理）。
- 后果：现无产物影响；作为公共头的内联访问器，它是给未来调用者的 UB 陷阱，且与本文件自带的判界范式自相矛盾。
- 定级：S3
- 怎么算修好：与 `present_at` 同形加判界并在越界时返回 `kNaN`（本模块的"缺样本"语言，:114 `MissingIsNaN`），或删除该访问器。
- 置信：CONFIRMED

### 本文件与派单重点的正反对照（如实记录，不报成缺陷）
- **对 CR-61-9（Phase2 协方差静默丢对角）的反证**：本头 :310「`DiagonalOnly` 仅对角（须相关核/近似误差，否则 REJECT）」、:311「`WeightDerived` 由权重标量反推 → **恒 REJECT**」、:299-301「解 C z = b…失败（非正定/奇异）→ fail-closed，**禁伪逆静默**」——同一仓库对"仅对角""权重反推方差""奇异即伪逆"三件事都有明确的 fail-closed 条文级实现。⇒ 我据此**加强** CR-61-9 的定性（Phase2b 不是"尚未做到"，而是与自家后一阶段的既有要求不一致），并**加强** CR-61-16（本头 :299 的"禁伪逆静默"正是 phase2_integrate.cpp:62 所缺的那条）。
- **对 CR-61-2/15（缺失以 0 表示）的反证**：本头把"未提供"一律写 `kNaN`（14 处，见 §2.5），并有 `*_present` 布尔配套 ⇒ 本批三处缺陷不是仓库惯例，而是可避免的偏离。
- **对 CR-61-5（稀疏层未接线）无涉**：本头不消费 SNR 链。
- `parse_mode`(:53-57) 明写 legacy `{auto, support_x_snr2, 0, 1, 2}` 与 `psf_snr_power` 一律拒绝，且 `module_adapters.cpp:14114` 注释确认生产走该函数 ⇒ 权重模式输入面在 Phase3 确实闭合；本批清单内唯一的 `weight_mode` 复现是 `phase2_integrate.cpp:1050-1061` 的**记录面**身份常量（有 `v6_clause_registry_v1.json:97 not_an_input_path` 区分输入面与本表），故我**不**把它报成"合同说没有、代码还在读"。

---

# 六、`lib/algorithms/resample/tests/p3rsmp/p3_nan_mask_test.cpp`（437 行，全读）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 待确认时方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :66-67 | `kW`、`kPx` | 512、512² | 夹具 tile 边长与像素数 | 结构性不适用 | HiPS `hips_tile_width=512`（:95、:129 同值）与数组维度，不含科学语义 | —— | CONFIRMED |
| :68 | `kBase` | 1000.0f | 非角点像素基值（反查映射用） | 结构性不适用 | 夹具自订内容（派单 §4 明列不报项） | —— | CONFIRMED |
| :69, :316 | `kEpsF32` 与 `4.0 * kEpsF32` | 4·2⁻²³ | T2 值判据相对容差 | 文献值 | `docs/algorithms/PHASE3_RESAMPLE.md:174` 逐字：「p3_nan_mask_test.cpp 以剩余合格邻域重归一加权和作解析真值，**相对容差 4·eps_f32**」⇒ 条文与代码同值 | —— | CONFIRMED |
| :83-84 | `kLeafOrder`、`kLeafNside` | 9、512 | 叶级阶与 nside | 文献值(条款) | :81-82 自述推导 `order-0 + log2(512) = 9`，与 `DRIZZLE.md:31`「nside=2^order」一致 | —— | CONFIRMED |
| :94-99 | `hips_order/tile_width/version/frame` | 0 / 512 / 1.0 / equatorial | 夹具 properties 头 | 结构性不适用 | 夹具内容与 IVOA 字段取值（:97 注明 REC-HIPS-1.0 §4.4.1） | —— | CONFIRMED |
| :103 | `ASTROCS_PIXEL_AREA_POWER` | -2 | 面亮度二次律声明 | 文献值(条款) | `FZ-BUNIT-SEMANTICS`/`FZ-P3-BUNIT-QUADRATIC`（与 p3_rsmp.h:17 同串） | —— | CONFIRMED |
| :129-130 | manifest 的 `hips_order/n_leaf_tiles/data_type` | 1(格式版本)/0/1/float32 | 产品清单夹具 | 结构性不适用 | FIX-401 完成清单字段（:125 注明"无清单 ⇒ fail-closed"） | —— | CONFIRMED |
| :159 | 搜索步长上限 | 200 | 方向搜索迭代上界 | 结构性不适用 | 迭代上界的实现选择；找不到即 `CHECK(find_asymmetric_dir(...))`(:243) 判红 ⇒ 不致静默降级 | —— | CONFIRMED |
| :160-161 | 步进与 8 候选方向 | 0.001、8 | 每次偏移的度数与十字加对角 | 结构性不适用 | 搜索策略参数，不进入科学判据 | —— | CONFIRMED |
| :176 | `ww[0] < 0.5` 筛 | 0.5 | 角 0 权重占优门 | 待确认 | :152-154 自述理由（"角 0 权重大时 (1/(1−w0))² ≥ 4 倍"）＋ :18 判据 T7 的 >1%/>5% 要求；0.5 本身是构造选择而非条文 | 更小的门槛会让非退化锚失效⇒误绿方向 | CONFIRMED |
| :259 | 前提校验 `wmax>=0.45 && wmin<=0.15` | 0.45 / 0.15 | 权重不对称前提 | 待确认 | 与 :176 的 0.5 是两个不同门槛（0.45 < 0.5），条文面零提及 | 方向：前提是**判红**侧（不满足即 CHECK 失败）⇒ 安全；但两门槛不一致说明其一是事后凑的 | CONFIRMED |
| :232, :280, :359, :402 | 夹具 `var` 常量 | 0.25f | 方差面基值 | 结构性不适用 | 夹具自订内容 | —— | CONFIRMED |
| :330, :335, :390 | 权重/归一/方差比对容差 | 1e-12 | FP64 侧相对容差 | 待确认 | `PHASE3_RESAMPLE.md:172-174` 只给「恒等 ± 舍入」与 f32 侧的 4·eps，**未给 FP64 的 1e-12** | 放松⇒误绿；收紧⇒可能因 FP64 舍入误红 | CONFIRMED |
| :340, :392 | 非退化锚门限 | >0.01 / >0.05 | "真值必须显著区别于两种错值"的判据 | 文献值(条款) | 本头 :18「T7 非退化锚: … >1% / >5%」与 `PHASE3_RESAMPLE.md:163-165` 的用例要求同族；数值本身在插件文档层未逐字出现 | 方向：门限越大越严⇒现值偏松侧需保持 | CONFIRMED |
| :356 | `u1/u2/u3` | 0.5 / 1.0 / 2.0 | T6 互异方差值（使重归一可判） | 结构性不适用 | 夹具内容；三值之互异性即判别力来源 | —— | CONFIRMED |
| :268-275 | 角点信号值 | 2000/4000/8000/1000f | 掩膜用例注入值 | 结构性不适用 | 夹具内容（派单 §4） | —— | CONFIRMED |
| :392 | 分母地板 | 1e-30 | 防 0 除 | 结构性不适用 | 数值守卫 | —— | CONFIRMED |
| :420 | `u_out` 预置哨兵 | 123.0 | 证明产出被真正写入 | 结构性不适用 | 哨兵值；:423 断言结果必须 NaN ⇒ 若生产者不写即判红（正例形态，值得记） | —— | CONFIRMED |
| :44, :51, :60 | `failures` 计数与宏 | 1（每次 ++） | 失败累计 | 结构性不适用 | 测试骨架 | —— | CONFIRMED |

## findings

### CR-61-24 · 本 Oracle 对"按原因分类、互斥可加"只断言了零值：`n_rejected_nonpositive_weight` 与"信号/方差同时非有限"两类无正例
- 轴：R（判据有效性）
- 位点：`p3_nan_mask_test.cpp:302-303` `CHECK_MSG(rej.n_rejected_nonfinite_variance == 0 && rej.n_rejected_nonpositive_weight == 0, "%s: class counts must be 0", cs.name);`、:345-346 `// 全部合格时计数必须为 0 … if (n_rej_expect == 0) CHECK(rej.n_rejected_nonfinite == 0);`、:378（T6 只断言总数 `n_rejected_nonfinite == 1`）
- 上位依据：`docs/standards/NUMERIC_STANDARD.md:100,105`——「方差面损坏（§4a）⇒ 按不合格样本剔除并计入 `n_rejected_nonfinite_variance`」「**`n_rejected_nonfinite`**（值非有限 / 方差非有限 / 权重非正，分类计数）」；`docs/algorithms/PHASE3_RESAMPLE.md:57,126`——「权威冻结名 `n_rejected_nonfinite`（按原因分类，**互斥可加**）」；:164 对本用例的自我要求「断言…`n_rejected_nonfinite` 分类计数正确」。本文件头 :16 亦自述 T5＝「按原因分类正确暴露 (计数 0 与「字段缺失」可区分)」。
- 现状与差在哪：三类原因中的"方差非有限"类在本文件**只被断言等于 0**（:302），唯一真正出现方差 NaN 的 T6/T6b（:360-364、:403-405 同时把 signal 与 variance 设为 NaN）**不检查任何分类字段**；而 cases[] 表里没有"全合格"对照组 ⇒ :346 那条 `n_rej_expect == 0` 分支**永不执行**。又因 `P3SampleRejection rej{}` 零初始化（:289、:376）＋ :345 注释自述"结构已零初始化"，`== 0` 类断言对"生产者从未写过该字段"这一失败模式**不可分辨**（恒真方向已钉死：该断言不可能红）。
- 后果：「按原因分类、互斥可加」与「计数 0 与字段缺失可区分」（T5 的对外主张）没有证据：生产者把方差损坏一律记成 value 类、或干脆不写 variance/weight 类，本测试全绿；互斥可加性（三类之和等于总数）也无处可查。
- 定级：S2（判据面缺陷，不改产品字节；但它是 Phase3 无效像素语义的**唯一** Oracle 面）
- 怎么算修好：① cases[] 增一个全合格对照用例（令 :346 分支真正执行）；② T6/T6b 增 `n_rejected_nonfinite_value/_variance` 的显式期望与"三者之和 == 总数"的可加断言，并按条文"互斥"给出 signal+variance 同时非有限时的**唯一归类**优先级（该优先级若条文没写，先补条文再改测试）；③ 另补一个 `w_k <= 0` 的正例，使 `n_rejected_nonpositive_weight` 至少被非零断言一次（本文件内确无该类正例；跨文件是否另有覆盖需跑 `git grep -n "n_rejected_nonpositive_weight" -- eng lib` 定案）。
- 置信：CONFIRMED

### CR-61-27 · T4 的 ±Inf 用例测的是产品面到不了的状态：夹具绕开生产 writer，而 writer 自身把 Inf 归成 NaN
- 轴：R（判据覆盖面）
- 位点：`p3_nan_mask_test.cpp:108-109` 注释原文「手写子产品…（逐像素完全可控，含 NaN/±Inf —— **生产 writer 会把非有限 flux 归成 NaN，无法注入 Inf**）」；:272-275 的 T4a/T4b/T4c/T4d 四例即依赖该手写通道
- 上位依据：`docs/algorithms/PHASE3_RESAMPLE.md:128`「bilinear 邻域含 ±Inf | ±Inf 与 NaN 同属不合格样本，按样本级掩膜剔除并重归一」——条文把 ±Inf 当作**产品可能携带**的状态；`docs/standards/NUMERIC_STANDARD.md:82`「不合格样本（`¬isfinite(x_j)`，只看值是否有限）」。
- 现状与差在哪：判据本身正确（同 NaN 处置），但可达性由 :108-109 自证为零——经生产 writer 落盘的方差/通量平面不可能含 Inf ⇒ 四例中三例（T4a/T4b/T4d，纯 Inf 者）检验的是只有绕过 writer 才能构造的输入。测试对此**已如实披露**（:108-109），所以这不算隐瞒；成立的是：**条文与测试对"±Inf 会不会出现在产品里"的说法相反**，而测试的 PASS 报告（:431 的对外文案）把"±Inf 同类"计入已验证清单。
- 后果：创新点②/③的"非有限像素一律剔除且计数"主张，其证据面里 ±Inf 那一支在真实产品上不可达；若未来 writer 改成保留 Inf，本测试也测不到（它绕开 writer）。
- 定级：S3（不改产物值；是覆盖主张的口径问题）
- 怎么算修好：二选一——① 由负责人在 `PHASE3_RESAMPLE.md:128` 把 ±Inf 一条改成"仅对历史/外部产品适用（生产 writer 已归一为 NaN）"，测试文案随之把 T4 标注为"合成注入路径"；② 给 writer 增一条"保留 Inf 的显式模式"或以 `read_leaf` 层直投 Inf 的用例，使该分支在产品可达路径上被走一次。
- 置信：CONFIRMED

### 不报的核过项（本文件）
- 前提自检 T1（:244-253）用 `kBase + fits_index` 的唯一可反查值核对生产 `read_leaf` 映射与本用例索引一致，且 :251 同时要求 `c==1` ⇒ 这是"先验前提、失败即判红"的正例，与 CR-61-24 的"恒真断言"恰成对照。
- Oracle 独立性（:20-21 声明、:186-197 `oracle_value` 自行重归一、:200-204 `wrong_unweighted_mean`、:391 `wrong_geom`）：期望值不复用被测聚合路径，且带两个错值对照锚 ⇒ 按派单 §4「测试期望值与夹具内容差不算缺陷」不报；我只报其**未覆盖的分类学**（CR-61-24）与**不可达的 Inf 支**（CR-61-27）。
- 覆盖语义：`c == 1` 与"值 NaN 不改 C"（:306-307、:310、:419）与 `PHASE3_RESAMPLE.md:126`「C=1（4 tile 均可读）——禁『零填』替代语义」逐字一致 ⇒ 合规，且是本批"缺失不表示成 0"的第二个正例。
- 权重全 0 / 零合格 ⇒ NaN（:308-312、:419-424）方向正确：禁零填、禁哨兵、`u_out` 预置 123 以证明被写。
- 清理与可重复性：`fs::remove_all` 前置（:138）与收尾（:429）、`TMPDIR/TMP//tmp` 退让（:222-224）⇒ 无产物污染风险。

---

# 本批最高危三条

1. **CR-61-2（S1，R/S，weight_chain.cpp:770）** — 逐帧参考通量 `F_ref,k` 的"缺失"用 `>0` 哨兵表示，NaN/负/0 一律**静默借用别的帧**的 F_ref，回退不留痕、无红灯；生产链（`module_adapters.cpp:11525-11526 → 11561 → 11568 → weight_chain.cpp:770 → 779 → 789 → 11584`）里只要有一帧的 HiPS 头缺 `ASTROCS_REFERENCE_FLUX`，该帧权重就不再满足配对性定理的前提，叠加权重面出现非单调错值，直接命中创新点②"跨帧绝对 SNR"的定权环节。
2. **CR-61-5（S1，D，weight_chain.cpp:715/:796）** — 设计默认的帧内稀疏 SNR 面在生产接线处被写死为空（`module_adapters.cpp:11534`），`weight_source` 恒 `frame_snr`；叠加权重退化为一帧一个常数，13_integration.md §4.0 的"对稀疏重建场逐像素同式 `w(x,y)=SNR²/F_ref²`"在生产中从未发生。根因见 CR-61-19：`phase1_product.h:168-207` 的跨阶段消费结构根本没有 SNR/稀疏层字段。
3. **CR-61-8 ＋ CR-61-7（S1，R＋C，variance_propagation.cpp:71-76 / :124-155）** — 方差传播一侧：σ²=0 被当合法"零噪声"，缺失方差从传播中静默消失（丢非负项⇒和单调偏小⇒不确定度偏乐观，方向已钉死）；同时 `variance_include_self` 是**合同零登记、配置文件可静默改**的方差口径键，两种取值给出的方差相差 N²/(N−1)²（N=2 时 3 倍），默认值由代码单方面决定——权重模式键被 §9.73 A44 禁绝后，同一形态在方差面复现。

# 我读不动或需要实跑才能定的

| 项 | 缺哪一角 | 该跑什么（本节点禁跑，交前台/实跑批） |
|---|---|---|
| CR-61-25 的最后一跳 | `build_row_normalized` / `operator_from_entries_row` 是否无条件置 `row_normalized=true`（`p3_rsmp_operator.cpp`，清单外） | 通读 `p3_rsmp_operator.cpp:18-60` 区段；或构造"邻域重叠面积不闭合"夹具，跑 UT-6_P3_RSMP 看是否判红 |
| CR-61-7 的取值影响倍数 | 只做了等权 N 帧的解析推导（exclude-self `σ²N/(N−1)` vs include-self `σ²(N−1)/N`），未实测一帧真实 ivar 面上两口径的方差/ivar 字节差 | 同一产品树以 `variance_include_self` 两值各跑一次 normalize/mosaic，比 variance/ivar 平面哈希与逐像素比（前台实跑，非本节点） |
| CR-61-5 的覆盖面 | Phase1 侧是否有任何跟踪件真正产出稀疏 SNR 控制点（我只读到 `sparse_snr_layer.schema.json` 与条文） | `git grep -n "sparse_snr" -- lib/infrastructure/scheduler` ＋ 一次真实 normalize 端到端后检查产品树是否含稀疏层子产品 |
| `phase1_product.h:110` `background_robust_mean` 的 "必须 > 0" 是否被校验 | 校验落在 `phase1_product.cpp`（清单外），我只读到 :411/:415 的相邻行 | 通读 `phase1_product.cpp` 的 PsfswInput 校验支；或注入 `background_robust_mean=0` 跑 v6_p1 集成测试 |
| CR-61-23 的"生产调用方是否确会漏设" | 未见 Phase3 生产侧构造 `ProductRecord`/`SurfaceBrightnessInput` 的调用点（本批 grep 只到 `p3_rsmp_propagation.cpp` 内部直填） | 通读 `p3_resample.cpp` 与 phase3 session 侧的入参装配点（清单外文件），确认哪几个字段真的由调用方赋值 |
| `n_rejected_nonpositive_weight` 类是否别处有正例 | 我只证明"本文件内无正例" | `git grep -n "n_rejected_nonpositive_weight" -- eng lib` ＋ 读命中的用例 |
| `p3_rsmp.h` 宣称的"12 门"数与实际 code 数 | 门清单在 `p3_rsmp_failclosed.cpp`（清单外） | 枚举 `G-P3-*` code 计数并与 :12 宣称对账 |

# 派单与清单差异

1. **清单 6 项中 4 项路径与仓库跟踪面不符**（原文与逐项对照见本文件开头表格）：`lib/algorithms/integration/` 下三份 `.cpp` 实际在 `v6/src/`、`phase1_product.h` 实际在 `v6_phase1/include/astrocs/v6/`、`p3_nan_mask_test.cpp` 实际在 `tests/p3rsmp/`。文件名均唯一可无歧义定位 ⇒ 按实际文件读，未漏项；派单原文若用于机器分派会造成"文件不存在"误判。
2. **阅读顺序与清单顺序不同**（内容无缺漏）：按派单 §本批重点 的取证依赖顺序读 `weight_chain.cpp → variance_propagation.cpp → phase2_integrate.cpp → phase1_product.h → p3_rsmp.h → p3_nan_mask_test.cpp`（先钉权重与方差口径，再核产品链消费面），成稿节序即此序；清单原文顺序见本文件开头对照表。finding 编号 61-1…61-27 连续，与节序一致。
3. **派单 §本批重点⑤ 假设的形态（"权重模式类键：合同声明不存在、代码仍静默读"）在清单内不成立**：清单内三处 `weight_mode` 全部在**记录/自述面**（`phase2_integrate.cpp:525,1050-1061`），而合同 `v6_clause_registry_v1.json:97 not_an_input_path` 已把"输入面"与"本表描述面"区分开，且 Phase3 `parse_mode` 确实拒绝 legacy 词 ⇒ 我未把它报成缺陷。真正"合同不认识却被静默读"的键出现在**清单外**的生产入口：`module_adapters.cpp:10382 doc.value("variance_include_self", false)`（已按本批重点②立案为 CR-61-7，位点在本批文件 `variance_propagation.cpp:124`）。
4. **需上呈的跨批线索（本节点未立案，因不在独占清单内）**：
   - `lib/algorithms/coverage/include/astro/phase2/sky_plane.h:216` `int weight_mode; // 0=inverse_variance；1=snr2；默认 0` 与 `:355 int weight_mode;`、`src/sky_plane.cpp:460 c.weight_mode = 0;`、`:524 if (cfg.weight_mode != 0 && cfg.weight_mode != 1) cfg.weight_mode = 0;` —— 结构体内仍有整数权重模式域，且**非法取值被静默 clamp 到 0**；与 `v6_clause_registry_v1.json:2809`「该概念不存在」、`CHK-NO-WEIGHT-MODE-CODE`（`docs/ci/01_CHECKS.md:100`，判据为"代码面无模式选择机制"）是否相容，请由负责权重模式的批次立案。
   - `lib/algorithms/coverage/configs/stage2_*.json` 八份配置仍含 `"snr_weight_mode": "snr2_normalized"`（`stage2_common.h:54` `int snr_weight_mode = 0;`、`upm.h:73`）——同名键族在配置面存活。
   - `reports/RELEASE-02/unc-prop-audit.md`、`reports/RELEASE-02/q2-snr-smooth.md` 两份被 `variance_propagation.h:4-5`、`weight_chain.h:14`/`weight_chain.cpp:746` 当作权威依据引用的文档**不在跟踪面**（`git ls-files reports` 仅 3 个 v19r2 csv，`git log --diff-filter=D -- reports` 无 RELEASE-02 命中）；跟踪面最近的是 `eng/tests/validation/release02/` 脚本与日志。 ⇒ 属"权威链断裂/证据落位"族，建议与已记录的阶段 A 结论并案。
   - `lib/algorithms/integration/v6/CMakeLists.txt:2` 自述本库为「V6 Phase2 **三模式**产品链集成库」，与 `13_integration.md §4.0`「单一权重口径，没有可选择项」及同库 `.h:16-19` 的 FZ-WEIGHT-SINGLE-PATH 表述冲突；另 `docs/plugins/algorithms_phase2/13_integration.md §5` 的配置项表未登记 §3/§4.0 点名的 `snr_path`（dense/sparse_reconstruct/frame_reconstruct）键。
5. **收工 `git status --porcelain` 原文**（与开工逐字一致，本节点未在仓库内落任何文件、零 git 写、未跑构建/ctest/`eng/**`；临时件目录 `独立审计/复算件/CR-61/` 未建——本批无中间件需求）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```



