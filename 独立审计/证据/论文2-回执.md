# 论文二（跨帧绝对信噪比）回执

交付物：`独立审计/03_小论文/论文2_跨帧绝对信噪比.md`
工作件：`独立审计/复算件/paper2/`（分部稿 `parts/p1..p4.md` 与合并稿 `论文2_全文.md`）

## 1 长度与体例自检

| 项 | 判据 | 实测 |
|---|---|---|
| 正文字数 | 2200–3500 | **3485 汉字**（`## 参考文献` 之前；命令：`len(re.findall('[一-鿿]', body))`） |
| 结构 | 摘要＋1–7 节＋参考文献 | 齐（3 节含 3.1–3.3，6 节含 6.1–6.7） |
| 末尾常驻进度 | `<!-- PROGRESS: 章节 n/8 -->` | `章节 8/8` |
| 元信息块／正文日期·任务编号·commit·sha | 禁 | 检索 `AUD-\d+&#124;commit&#124;待裁&#124;负责人&#124;流水号&#124;工包&#124;2026-` → 0 命中 |
| 图 | mermaid 或"图位＋说明"，一图一结论，不放假链接与不存在图 | 1 幅 mermaid（图 1，结论＝权重不入库）＋3 张表；未放不存在的图 |

## 2 仓库只读自证

| 时点 | `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` | HEAD |
|---|---|---|
| 开工 | `?? ACSD整治工作包_AUDIT-06.zip`、`?? site/` | `c8f64e9a` |
| 收工 | `?? ACSD整治工作包_AUDIT-06.zip`、`?? site/` | `c8f64e9a` |

**差异 = 空**（无新增/修改/删除）。零 git 写、零 `git config` 写；未构建、未跑 ctest／`run_checks.py`／任何 `eng/**` 脚本、未跑端到端；未 `import` 仓库 Python。对仓库只做读文件、`git grep`/`git ls-files` 与归档结果件（`实验/absolute-snr/results/*.json`）的逐字段回读。

## 3 引用条目（10 条）与其取证的论点

| # | 条目 | 支撑正文哪条论点 | 取证通道与处置 |
|---|---|---|---|
| [1] | Zackay & Ofek 2017, ApJ 836, 187（arXiv:1512.06872） | 权重分母含源项：式 (3) = 与位置无关的背景项 + 源通量与轮廓的卷积项 | 旧判批本轮复算到**式号级**（arXiv 全文逐字命中）；核验另以 Crossref 核 `10.3847/1538-4357/836/2/187` ↔ I ↔ 1512.06872 未绑反 ⇒ 可用 |
| [2] | Zackay & Ofek 2017, ApJ 836, 188（arXiv:1512.06879） | "最优"带**背景主导噪声极限**限定，不外推到源受限像素 | Crossref 标题逐字（核验＋复核两通道一致）；绑定核查证真 |
| [3] | Horne 1986, PASP 98, 609 | 仅作"信号/自身误差"结构与 `A_NEA = 1/ΣP²` 的**结构对照** | 复核 V3 明记 ADS 全文为影像、取不到逐像素方差式文本面；旧判批判"式号层未复现" ⇒ 正文不引式号、不作判据锚（第②层复核优先于核验把它列为 A 级判据的用法） |
| [4] | photutils v3.0.0 `calc_total_error` | 成熟实现把背景误差命名为 background-only、逐像素总误差再加源项 `σ_tot = √(σ_bkg² + I/g_eff)` | 复核 V3 取回文档页并逐字摘录；版本钉到 v3.0.0（总则性 Zenodo 标识在旧判批判"版本错"，故正文声明"引用须钉发行版"） |
| [5] | SWarp（`src/coadd.c`/`src/weight.c`，固定版本快照） | 权重是逐像素通道；逐帧标量只是归一化因子 | 仓内 `NOISE_MODEL.md` §14a.1 在固定 commit 上逐字核对；正文不写 commit 号 |
| [6] | PixInsight《New Image Weighting Algorithms》 | 功率比信噪比与权重是两个量、权重不写进图像 | 仓内 `PSF_SIGNAL_WEIGHT.md` 逐字核对式[16][18][20]；正文只作方法学对照、**不引其标定常数**（该常数存在文章版/PCL 版漂移，部分元数据断言仓内自记"未独立核验"） |
| [7] | Akhlaghi & Ichikawa 2015, ApJS 220, 1 | σ 只在天光已扣、源已剔除的像元上测 | 文献池 P1 判**确认**并附强制限定（检测/分割语境，不背书逆方差加权；`占比足够` 措辞与 §B.1 节号未证 ⇒ 正文两者均不写） |
| [8] | Saydjari & Finkbeiner 2022, ApJ 933, 155 | 控制点重建为稠密面时同时给预测方差是常规要求 | 文献池 P1 判**确认**（摘要逐字 "Local covariance predicts background and uncertainty"）；不引其核函数细节 |
| [9] | Fruchter & Hook 2002, PASP 114, 144（arXiv:astro-ph/9808087v2） | 重采样/drizzle 使输出像素相关；加权均值含 `a`/`w`/`s²` 三因子 | 旧判批本轮取到预印本全文节结构：式(2)–(5) 在 §2、相关噪声在 §7 ⇒ 只引到该层级；`§7.2 式(7)`、`§5 面亮度归一` 判 UNPROVEN ⇒ 不引用 |
| [10] | Melchior et al. 2018, Astron. Comput. 24, 129 | 相关噪声与共加图的处理推导可引用 | 文献池 P1 判**确认**并限定措辞为"applicable to"⇒ 正文写"给出可引用的处理推导"，不写"已证正确" |

**未进正文的在册文献与原因**：Naylor 1998、Howell 1989、Tonry 2012 / Huang 2017 / Ivezić 2019、Newberry 1991、Starck & Murtagh 1998/2006 本轮未取证（Howell 另有"同一 DOI 在代码面与文献面承担互斥断言"的登记）；Bertin & Arnouts 1996 在旧判批判"部分位点错"（FLUXERR 归属不闭合）；Serfling 1980 / Wahba 1990 / Holland & Welsch 1977 的节号 pinpoint 判 UNPROVEN；Rosner 1983、Kelvin 2023、Roellinghoff 2025、Nguyen 2016、Liu & Miller 2026、Schmitt 2010、The Tractor(ASCL) 虽在链路②可用池内，但与本文命题（绝对 SNR 定义与逆方差定权）无直接支撑关系，不为凑数而引。

## 4 按第②层复核撤销或改写的说法（凡冲突以复核为准，被推翻者不复述）

| 核验报告的说法 | 复核判定 | 正文写法 |
|---|---|---|
| 两篇 FROZEN 科学文档相互排斥、须上呈裁决（AGENTS §10），并给 (A)/(B) 二选一 | V3 **推翻**：(B) 一支在科学上不存在、"二选一"是假两难；实质是 `NOISE_MODEL.md` §1/§11 两句话缺适用域限定 | §3.3 只写"两面同向、正本两处句子缺限定语，属措辞订正面，不是两篇权威互斥" |
| "不入库权重"红线被实质破坏（落盘 ivar 即入库权重） | V3：落盘 `ivar` 本身不违反红线（canonical 对象、对象表写明对该估计目标可为权），真正打架在插件层"只存…"的列举不全 | 正文不写"红线被破坏"；改为在 §3.3 用"两个方差面取值互不代用"的正向口径 |
| 两臂捆成一条头条（"各中一次"）、背景 ivar 当权与帧级常数权同判 | V1 **降级拆分**：只有逐像素臂是现行缺陷；帧级常数权是**已声明的降级**（具名标记不确定度不可用），与业界帧权同型 | §6.1 分臂定性，并补复核给出的更硬后果（产品误差棒偏小 1–3 个数量级） |
| 过权倍数 ×1.011/…/×1058（未给 `P_0`、PSF 宽度、σ_sky 取值） | V1 反解出隐含常数并指出对 σ_px/σ_sky 一次方敏感（放宽到 σ_px=2.0 时 ×1058→×398） | 正文改用**复核自算表**（σ_px = 1.4863 px、FWHM = 3.5 px、P₀ = 0.2159、σ_bg = 5 ADU、g = 1.3 ⇒ ×7.6/×67/×665/×6.6e3），并注明是构造性对照；不复述 ×1058 |
| "`sparse_snr_layer` 里写的是逐源 `F_i/σ_F,i`"（把 A 写成 B） | V2 收窄：**合同对象无生产者**；逐源 SNR 是另一在册对象（`source_snr`/HiPS 星表），消费者是相对质量场；整改 = 新增生产＋改消费算子 | §6.2 按"对象缺位 + 算子仍按已作废的乘性分解 + 自检夹具与合同判红样本同形"写 |
| `dense` 口径"稠密 SNR 面生产者零调用" | V4 更强：**两个生产者都不可达**（`hp_drizzle_fits_to_ahpx` 已登记 unreachable；`snr_model` 块从不挂、逐像素 SNR 在累加处被匿名丢弃） | 正文不取"无生产者"这一弱化措辞；把该项落到 §6.6 的待复测（产品在盘证据） |
| AUD202-006：量纲部分 CONFIRMED、41%/347% 判 **UNPROVEN** | V5 改判：两数**可复算** = `1/√(1−φ)`（41.42%/347.21%）⇒ 缺陷真形状是"写出式与其数字不属同一对象"；且该分支条件 `gain ≤ 0` 使带 `1/g` 的式子不可求值 | §6.4 按复核写，整改取 φ 形式；不沿用文档那两个百分数作为所写式的数值，也不写"原文如此" |
| AUD202-006"数值偏 1.03–3.72 倍"归因于缺 `1/g` | V5 订正：只丢 `1/g` 的倍数上界 = `√g`（g=1.3 ⇒ 1.14），3.7 档来自同时丢 `ΣP²` | §6.4 明确"主项亦另在别处：数倍量级那一档要同时丢 `ΣP²` 才解释得通" |
| AUD202-012：同一物理点**两条轴**差 3.1 pp、与解析值偏离至 4.3 pp | V6 订正：实为**四条轴**（12.7913/13.2734/15.9217/17.6277%，极差 4.8364 pp）、全 29 点最大偏离 7.0030 pp（4.3 pp 只是 RN=50 单点）；成因 = 分母样本噪声（重采样 sd 2.9447 pp）；本仓 `REPORT_paper.md:225/:371` 已自登记 ⇒ 属**传导缺陷**非新发现 | §6.5 按四条轴与 4.84 pp 写；正文只引闭式 +14.5009%/+38.2524% |
| AUD202-008 的 Δ=64「够密」论证反转（`Δ/ℓ = 1.1–1.6` 属欠采） | V5 明记"不属工包第③问，本次未核" | 正文**不写** Δ 与相关长度的任何比较结论 |
| §8b 三口径适用域图谱（`Δ* = 32/128 px`、E = 0.1375/0.0530、"差 2.1/2.6 倍"、稠密 vs 稀疏互有胜负） | V4 判**对象错配**：实验臂全是 σ̂ 估计器（两份结果件里 `"snr"` 出现 0 次）、度量对水平信息做了中位数归一、E 的权重形式 `w = 1/σ̂²` 正是被否定的背景权 ⇒ 数值不可迁移 | 正文不引这些数字；§4 以"判据所测对象必须就是所主张的对象"承接该教训 |
| Horne 1986 作为"权重含源项"的 A 级判据锚 | V3 未取到文本面 ⇒ 不引它作判据 | [3] 降为结构对照（见 §3 表） |

## 5 因证据不入库而**剔除**的数字（正文一律不引）

| 被剔除的读数 | 出处（口径） | 为何当前不可重算 |
|---|---|---|
| 实拍帧上"绝对表示电平偏差 ≤ +0.19%""相对表示 −15% ~ −77%"、各帧 `c_k`（1.175/1.206、3.937/4.265、1.199/1.317、2.704/2.940）与"≤ +0.10%~+0.19%"等 | `实验/absolute-snr/docs/EXP-05-ABSOLUTE-SNR.md` 表 C/F | 臂 C/F 的输入是 `testdata/M42_T2T3_mosaic_Flying_dutchman/T2/{M1,M2,M4,M5}` 的 300S-Red 归一化帧；`git ls-files -- testdata` 显示**无任何 .fits 入库**（只跟踪 README/索引/元数据）⇒ 独立读者取不到输入。正文只引自包含、固定 seed、纯 numpy 的臂 A（−44.1%/−45.5% 对 −1.0%/−0.6%，负例 0.037%） |
| HST 真实模板前向仿真的电平读数（如裁剪低偏 1.4279%、1.1672%、`hst_struct_varsky` 各比值） | `EXP-05` 臂 B、`README.md` 表 | 输入为 `testdata/HST_M16/*F657N*.fits`（同上，不入库） |
| 生产实跑**实际走哪条权臂**、`n_variance_tiles > 0` 的磁盘证据、`p2_integrated.json` 的 `weight_basis`/`weight_source`/`uncertainty_unavailable_reason` 三键取值 | 核验需复测 R-1；复核 V1"缺什么证据" | 产品树落 `run/`（gitignore）；本轮禁构建与端到端。且 `07_noise_snr.md` §4.4（调度未挂 variance 块 ⇒ 恒走帧级）与接线读到的默认路径（Phase1 默认挂 variance）**互斥**，只能由实跑裁决 |
| HiPS 文件头两处同名帧级量的实际取值（`ASTROCS_FRAME_SNR = snr_reference.snr_f` 与 `frame["frame_snr"] = F_5/m_5`） | 核验 R-6 | 需读落盘产品头，本机无入库样本 ⇒ 正文只说"两处同名不同物"这一结构事实 |
| 逐帧 `frame_snr` 实测量级 21.4–47.4 与 `×frame_snr² = ×456–2245`；`F_ref` 在 ZP 18/20/22 下的 6.3e4/4.0e5/2.5e6 ADU 及 `×(F_i/F_ref)²` 的具体值 | 复核 V2(c) 传导表 | 前者的帧级取值来自不入库的实跑/台账面（同 §6.6）⇒ 正文只给"权重被乘上 `frame_snr²` 与 `(F_i/F_ref)²`"的代数形态，不给倍数 |
| drizzle 相邻像素相关 `mean&#124;ρ&#124; ≈ 0.19 / max ≈ 0.57`、`k_corr = 1.4` 的 MC 标定值、`SNR-012` 全套 | `docs/science/UNCERTAINTY_AND_COVARIANCE.md`、`PHASE2_UPM.md` | 属测试/仿真读数，本轮禁构建与 ctest ⇒ 正文只写"产品像素相邻不独立"这一结构事实，协方差需求引 [9][10]，不引 ρ 数值 |
| 稀疏重建图谱的 `Δ* = 32/128 px`、E 值、"差 2.1/2.6 倍"、SNR 场相关长度 `ℓ = 40.4–57.9 px` | `CONTROL_WEIGHT_SNR.md` §8b、`eng/packaging/config/defaults.json` 约束文字 | 除对象错配（§4 表）外，`ℓ` 的原始实测产物在跟踪集内检索不到 ⇒ 全部不引 |
| `snr_path_effective` 的实际取值、三口径互差的实跑对拍 | 核验 AUD202-009、R-2 | 代码面证据可用（`snr_path` 为死键、`snr_path_effective` 在 `lib/` 0 命中），但"实跑记录哪个口径"需产品面 ⇒ 正文只写接线事实 |
| 生产 `dense` 臂 IDW 保真性质、`noise_model.cpp`（1482 行）是否在某分支写入源项 | 核验 R-1 附注、R-3 | 本机不能立证（未逐行、未重跑）⇒ 不写入正文 |

## 6 正文数字的可核锚点（供独立复核）

- **帧级定义与判别力**：`实验/absolute-snr/results/b1_sky_scan.json` → `G2_large_sky_slope_def` −0.4879107/−0.4971984、`G2_snr_max_over_min` 0.0210777/0.0107053、`G6_trad_distortion_at_max_sky` 3419.39/102516.88、`G6_raw_frame_distortion_at_max_sky` 8040.05/240914.72、算术归零 `G5_arith_null_max_rel_change` 6.66e-16/4.44e-16、定义式臂 `G3_max_abs_z_sigma` 2.0609/1.7939；"冻住散粒项 ⇒ 斜率归零"来自核验 `aud202_recompute.py::R5_degeneracy_selftest`（比值 1.000000、斜率 1.0e-17）。
- **读噪单次计入与双计闭式值**：`b2_noise_terms.json` → `pred_doublecount_bias = 0.14500914`（基准点在四条轴上逐位相同）、RN=50 行 0.38252383、`bias_empirical_rn` 四行 0.127913/0.132734/0.159217/0.176277、`gates.G4c_pred_vs_measured_mc_max_abs_diff = 0.0700303`、双计臂 `z_empirical_rn_vs_mc` 最坏 15.17；复核 V6 的 20 块重采样 sd 2.9447 pp 与 −1.2186 pp 期望偏移。
- **恒等式与最优功率**：`b4_integration.json` → `part_ab.identity_rel_dev = 2.220446e-16`、`var_ivar/var_equal/var_snr_weight = 1213.0758/8263.4537/1732.5820`（解析 1207.0629/8149.7394/1712.5656）、`part_c` Q/W 1275.1233 vs 1260.4095、`part_d.naive_over_optimal_measured = 1.5226`（解析 1.5044，杠杆区间 [0.079314, 0.206557]）、`part_e` 与 `H5_mismatch_eff_loss_at_max_spread = 0.30184`、`H5_anchored_ratio_by_mag`（12.0 → 1.0362）。核验自有复算的最大相对偏差 3.01e-16 来自 `复算/aud202/aud202_recompute.py::R3`。
- **绝对/相对表示**：`exp05_e1_analytic.json` 臂 A（`abs_snr_dev_R0`/`rel_snr_dev_R0`/`abs_vs_rel_gap`，`meta.data_class` 明写"纯解析代数合成…真值完全已知"，脚本不读 testdata、不跑可执行文件）。
- **复核自算的过权倍数与误差棒比**：`独立审计/复算件/aud202-v/v1_weight_degeneracy.py` → `v1_out.txt`；恒 0 判据的 18 组换源自测：`独立审计/复算件/aud202-rest/v5_units_and_tautology.py` → `v5_out.txt`。
- **接线事实（跟踪集内可复算）**：`module_adapters.cpp` 的 `in.sparse = nullptr`、`w = ivar_v[d][p]`、`weight_basis = "per_sample_ivar"`、`cfg.sigma_sky_source = SNR_SIGMA_SKY_EMPIRICAL_TOTAL_RMS`；`weight_chain.cpp` 的 `double intra = 1.0` 与 `node_residual_`；`git grep -c "sparse_snr" -- lib` = 25 处全在消费/夹具侧；`snr_path_effective`、`predicted_variance_adu2` 在 `lib/` 均 0 命中。

## 7 未写入正文但登记的核验缺口（留给整改与复测）

1. `noise_model.cpp` 1482 行未逐行 ⇒ "背景方差面是否在任何分支被写入源项"未定（核验 R-3）。
2. `p1snr_science_test.cpp` 的 oracle 是同式 long-double 镜像 ⇒ 结构上测不到"分子用 `F_i` 还是 `F_ref`"（核验判据表与 R-5）；论文按复核口径把该事实写成纪律条，不作数字主张。
3. `实验/absolute-snr` 196 个跟踪文件中约 186 个未通读（核验 R-4）；本文引用的数字锚点已逐字段回读并交叉复算。
4. 帧级 SNR 的产品头取值一致性、三口径实跑对拍（R-1/R-2/R-6）需一轮端到端与产品证据入库后才能立数字。
5. `lib/algorithms/coverage/src/rejection.cpp`（排异与权重的相互作用面）不属本分片代码域、本轮未审 ⇒ 论文不涉及排异判据。

## 8 需负责人知晓的两点

1. **论文主张已按证据面收窄**：创新点二的可主张部分限定在"口径与判据已确立"，生产侧的四项缺陷（背景逆方差当科学权、稀疏层无生产者、消费算子按已作废的乘性分解、预测方差未实现）在正文 §6 逐条落定，均可由 §6 的锚点复算。
2. **无需占用裁决额度**：核验报告列为"两篇权威打架、须上呈"的那一条，经第②层复核判为假两难（科学上一侧不存在），因此本文按"两处文档句子缺适用域限定"的订正面书写；相应工单（`NOISE_MODEL.md` §1/§11 措辞、`07_noise_snr.md:121` 的 φ 形式改写、`13_integration.md:39` 的字段改名＋新增预测方差与换源判据）属变更流程，不是方向裁决。
