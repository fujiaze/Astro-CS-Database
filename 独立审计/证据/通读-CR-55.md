# 通读 CR-55 —— master 定标生成与测光施加

- 基线 HEAD=`c8f64e9a`；仓库 `F:\Astro dev\Astro CS Normalization Database`
- 批次：CR-55（只读代码通读审计节点）
- 派单前言：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 全条已读并作为硬约束）
- 开工 `git status --porcelain` 原文：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 清单与阅读状态

| 文件 | 已读 |
|---|---|
| lib/algorithms/calibration/src/master_generator.cpp | 是（§2.5 表 13 项；finding CR-55-6、CR-55-7） |
| lib/algorithms/calibration/src/module_entry.cpp | 是（表 26 项；CR-55-5、8、9、10、11、12、13） |
| lib/algorithms/calibration/src/photometry_apply.cpp | 是（表 8 项；CR-55-3、4） |
| lib/algorithms/calibration/src/photometry_apply.h | 是（表 9 项；CR-55-1、2） |
| lib/algorithms/calibration/src/v6_calibration_covariance.cpp | 是（表 15 项；CR-55-14、15、16、17、18、19） |
| lib/algorithms/calibration/tests/p1cal/bias_influence_harness.cpp | 是（表 14 项；CR-55-20） |

合计 finding 21 条（逐条含 §3 全字段）：S1 0 条、S2 14 条、S3 7 条（S2 的上限原因＝本模块 DLL 与 v6 协方差件生产未接线，见"最高危三条"末段）。

<!-- PROGRESS: 6/6 -->

---

# 1 lib/algorithms/calibration/src/photometry_apply.h —— 已读：是（表 9 项）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| photometry_apply.h:63 | `PhotoSpatialGain::order` 默认 | 0 | 场阶数，0 ⇒ m≡1（关闭/降级） | 公式导出 | PHOTOMETRY.md:13 `I_photo=k_photo·m(x,y)·I_cal`；order=0 即 m≡1 的退化式；装配侧 `module_adapters.cpp:5475-5476` 把请求阶夹到 [0,2] | — | CONFIRMED |
| photometry_apply.h:64 | `coef[5]` / 全部初值 0.0 | 5 / 0.0 | 二阶多项式 5 个基函数的系数槽与初值 | 结构性不适用 | 数组维度＝阶 2 的基函数个数 `{x̃,ỹ,x̃²,x̃ỹ,ỹ²}`，不含科学语义；0.0 是"未拟合"初值 | — | CONFIRMED |
| photometry_apply.h:65 | `center[5]` / 0.0 | 5 / 0.0 | 中心化基函数均值（规范中心）槽位 | 结构性不适用 | 同上（维度）＋photometry_apply.h:59-61 声明其语义 | — | CONFIRMED |
| photometry_apply.h:66 | `x_ref/y_ref` 0.0 | 0.0 | 归一化原点初值（拟合侧取帧几何中心） | 结构性不适用 | 初值不是科学量，真值由 `spatial_gain.cpp` 按帧几何给出 | — | CONFIRMED |
| photometry_apply.h:67 | `x_scale/y_scale` 1.0 | 1.0 | 归一化尺度初值（缺省=不缩放，恒等） | 待确认 | 跟踪面内**无任何条文**规定"缺省尺度=1.0"的含义；1.0 使 `x̃=x−x_ref`（像素制单位直接进多项式）。检索式：`git grep -n "x_scale" -- lib docs eng` | 保守方向：缺省应判非法（如 :118 对 `>0` 的判定那样，再加"必须由拟合侧显式给出"），影响范围＝order>0 的全部施加帧 | PARTIAL（生产侧唯一生产者 `spatial_gain.cpp` 显式赋值，故当前不可达） |
| photometry_apply.h:72 | `(order<=0)?0:(order==1?2:5)` | 0/1/2/5 | 阶→项数映射 | 公式导出 | 阶 1 两项、阶 2 五项＝多项式基函数基数；**但 order≥3 也被映射成 5**，与本行上方 :70 注释"阶 >=3 不启用"不一致（见 CR-55-1） | — | CONFIRMED |
| photometry_apply.h:79 | `if (order >= 2)` | 2 | 是否补二阶三项 | 结构性不适用 | 阶数分支，基数由上行式导出 | — | CONFIRMED |
| photometry_apply.h:87,96 | `return -s;`（`log10 m = −Σ c·B̃`） | −1（取负） | 施加因子是拟合量的**倒数** | 公式导出 | 施加端要抵消测量到的增益 ⇒ 乘 `10^(−Σc·B̃)`；生产端复算核对同式（`p1phot_tests_spatial.cpp:165,202-205` 逐字抄此式）；正本 PHOTOMETRY.md:13 只给 `m(x,y)` 不规定符号，符号唯一锚＝拟合/施加共用本头（:50-52 自述"唯一实现"） | — | CONFIRMED（式一致，见 CR-55-2 关于该式无科学侧冻结条文） |
| photometry_apply.h:102 | `std::pow(10.0, …)` | 10 | 常用对数底 | 文献值 | 星等/通量体系的定义底：PHOTOMETRY.md §2 符号表 `scale = 10^{−location}`；dex 定义 | — | CONFIRMED |

## findings

### CR-55-1 · order≥3 被静默当成二阶求值，与本文件头"阶>=3 不启用"的声明相反
- 轴：R（主）/ C
- 位点：`lib/algorithms/calibration/src/photometry_apply.h:72` `return (order <= 0) ? 0 : (order == 1 ? 2 : 5);`（对照同文件 :70 注释"阶 >=3 不启用（见 spatial_gain.h）"）
- 上位依据：PHOTOMETRY.md:13（`m(x,y)` 为"低阶空间乘法增益"）；ASTROCS_DESIGN.md §4.2:276 要求 `p1_phot.json` 记 `photometry_applied / photscal / 逐帧 k_photo / 施加后产物路径`，"使「k 确实乘进了像素」可由独立读者用「calibrated 面 × k」逐像素复算核对"。求值式本身：条文面零提及（检索式 `git grep -n "photo_spatial_nterm" -- lib docs eng`，命中仅本头＋拟合侧＋测试）。
- 现状：任何 `order>=2` 一律返回 5 项，`photo_spatial_basis`（:79-83）走二阶支路；调用方若给 order=3，本头按二次式求值并**照常返回 0（成功）**，`field.coef[5]` 也装不下三阶的 10 个系数，无越界也无拒绝。
- 差在哪：文件头把"不启用"写成声明，实现写成"降级为二阶并报告成功"——两者不是同一件事；被点名的处置（不启用）在实现面没有对应返回码（:107 的 -6 只覆盖归一化参数与系数非有限）。
- 后果：provenance 记录的 `order` 与实际求值的阶不一致 ⇒ "施加的是所声明的那一个场"这一对外主张不成立（`module_adapters.cpp:5407` 把 `sc.m_order` 原样写进 `spatial_gain` JSON）。
- 定级：S3（当前不可达：两处独立夹逼 `module_adapters.cpp:5476 if (spatial_gain_order > 2) spatial_gain_order = 2;` 与 `spatial_gain.cpp:327 const int order_req = std::min(p.order_requested, 2);`）
- 怎么算修好：在 `photo_spatial_nterm` 或 `apply_photometry_spatial` 的校验段（:118-129 同处）对 `order > 2` 显式返回 -6；同时把 :70 注释与 spatial_gain.h 的阶域写成同一句话。**必须同批改的东西**：`p1phot_tests_spatial.cpp` 里以 order 3 试探的用例（若有）需从"降级"改判为"拒绝"，否则改对反而判红。
- 置信：CONFIRMED（映射与可达性都按原文复算）

### CR-55-2 · 唯一承担 m(x,y) 符号与中心化约定的"权威"是代码注释，科学侧无一条式子
- 轴：S
- 位点：`photometry_apply.h:86` `// log10 m(x,y) = −Σ_j coef[j]·(B_j(x̃,ỹ) − center[j])   ← 中心化在施加端同样生效`
- 上位依据：`docs/science/PHOTOMETRY.md:13` 只写 `I_photo = k_photo·m(x,y)·I_cal` 与"标度类别 = photo_scaled_adu"，**不给出 m 的基函数、中心化的规范式、也不给出取负号**；PHOTOMETRY.md §16.1 在本仓被本头点名为 ④⑤（:43、:106），但检索式 `git grep -n "16.1" -- docs/science/PHOTOMETRY.md` 只命中第 13-14 行对该节的引用，未见被引用节自身的式子定义 `m`。
- 现状：`−Σ`、`B̃ = B − center`、`10^(·)` 三件约定全部只存在于本头与拟合侧头文件；施加侧与拟合侧"共用同一份内联实现"（:50-52）从而**互为验证、无第三方期望**。
- 后果：符号或中心化若写反（例如漏 `center`，:60-61 自己说明后果是"k_photo 的全局语义被暗改"），下游没有任何独立面能判红；受影响主张＝创新点①"测光校准到星等坐标系"的逐帧 k_photo 语义。
- 定级：S2（缺运行时触发路径：科学侧条文面零提及，无法指出"哪一条被违反"，只能指出无锚）
- 怎么算修好：把 `m(x,y) = 10^(−Σ_j c_j (B_j − B̄_j))` 连同规范（参与拟合星集上 `Σ w_j B̄` 加权均值=0）写进 `docs/science/PHOTOMETRY.md` §16.1 本体（走变更流程，因该节被本头引用）；头里改为引用节号。补一条**非代码**期望：拟合→施加→在星点处复算 `−2.5log10(m)` 与拟合的 log10 残差应回零（真值无效应⇒归零的负例）。
- 置信：PARTIAL（缺"§16.1 全文我是否读全"一角——该节在 PHOTOMETRY.md 中的正文位置本次未逐行读完，只核到引用面）

---

# 2 lib/algorithms/calibration/src/photometry_apply.cpp —— 已读：是（表 8 项）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| photometry_apply.cpp:38,42,46,50,55 | 返回码 −1/−2/−3/−4/−5 | 同 | 空指针/尺寸/非有限/非正的具名失败 | 结构性不适用 | 错误码编号不携带科学语义；语义与 photometry_apply.h:30-34 一一对应 | — | CONFIRMED |
| :44 `if (w <= 0 \|\| h <= 0)` | 0 | 0 | 维度下界 | 结构性不适用 | SCI-CAL-001 §4"维度: w>0, h>0" | — | CONFIRMED |
| :53 `if (photscal <= 0.0)` | 0.0 | k_photo 的**定义域下界**（0 与负值拒绝） | 公式导出 | PHOTOMETRY.md §2 符号表 `scale = 10^{−location}` ⇒ 恒正；k=0 会把整帧乘成 0、负值翻极性（:52 注释）。乘性标度取 0 无物理象 | — | CONFIRMED |
| :68 `out[i] = (float)((double)light[i] * photscal)` | double 中间精度 | — | 精度策略（FP32 输入×FP64 标度回 FP32） | 公式导出 | PHOTOMETRY.md:13 `x′=α·x ⇒ Var′=α²·Var`；SCI-CAL §9 FP32/FP64 精度策略 | — | CONFIRMED |
| :120 `!(field.x_scale > 0.0)`（同段 y_scale/x_ref/y_ref 的 isfinite） | 0.0 / finite | 归一化尺度的定义域守卫 | 公式导出 | 除数必须非零且同向为正（归一化尺度是半宽，:75 注释声明 x̃∈[−1,1]）；负/零尺度会翻转场形 | — | CONFIRMED |
| :118-123 | 返回码 −6 | −6 | "场的定标量非有限或 <=0" 的 fail-closed 码 | 结构性不适用 | 错误码编号；**覆盖面不全见 CR-55-3** | — | CONFIRMED |
| :121-134 | `npix = (size_t)w*(size_t)h` | — | 尺寸乘积用 size_t | 结构性不适用 | 与 master_generator.cpp:75 的 `int npix` 相反（那边已登记 DISP-CAL-009） | — | CONFIRMED |
| :138-139 `photoscale * m` | — | 施加顺序（先乘标度再乘场，同为乘法，顺序无关） | 公式导出 | PHOTOMETRY.md:13 同式；两因子皆纯乘 ⇒ 无舍入次序敏感性问题（double 累加，逐像素独立） | — | CONFIRMED |

## findings

### CR-55-3 · -6 声明"场的定标量非有限即拒"，但 `center[]` 一列不判，漏检后整帧变 NaN 且返回成功
- 轴：R（主）/ D
- 位点：`photometry_apply.cpp:124-129`
  ```
      for (int j = 0; j < nterm; ++j) {
          if (!std::isfinite(field.coef[j])) { ... return -6; }
      }
  ```
  （对照 photometry_apply.h:107 的声明："−6: 场的定标量非有限或 <=0"）
- 上位依据：SCI-CAL-001 §8 表"母版与亮场标度不一致…**fail-closed**：消费边界 DATA 拒绝…不静默"；§4"数值: 输入含 NaN 时该像素的 median/MAD 计算跳过 NaN…输出不传播 NaN 为伪有效值"；PHOTOMETRY.md:13 要求施加面与方差面同标度律。本头 :59-61 自己写明 `center` 与 `coef` 同属场的定标量（"必须一起携带…否则引入一个整体乘性常数偏移"）。
- 现状：`x_ref/y_ref/x_scale/y_scale/coef[]` 全部逐列判 `isfinite`，**`center[]` 一列没有任何检查**；`photo_spatial_log10_m`（photometry_apply.h:95）把 `B[j]-f.center[j]` 直接进求和 ⇒ center 为 NaN 时 `m = pow(10,NaN) = NaN`，:139 把每个像素写成 NaN，函数返回 **0（成功）**。
- 差在哪：一个被本文件声明为"定标量"的列被排除在自己的 fail-closed 名单外；产物全 NaN 却报告成功，正是"未施加的校正被登记成已施加"的最坏形态（施加了、但施加出的不是任何校正，而是销毁数据）。
- 后果：`module_adapters.cpp:6077-6088` 只在 `arc != 0` 时报错，arc==0 时把该帧 `p1_write_fits_atomic` 成 `photoapplied_*` 产品并计入 `photometry_applied` ⇒ 整帧 NaN 产品 + 已施加登记；下游 drizzle 的非有限像素跳过会使该帧贡献 0 像素，产品面表现为"该帧消失"而无任何红灯。
- 定级：S2（**缺生产触发路径**：当前唯一生产者 `spatial_gain.cpp:500-536` 在发布前先在整幅帧网格上求 `amax=max|log10 m|` 并 `if (!std::isfinite(amax) …) { last_kind = kHard; continue; }`（:522），center 为 NaN 时该守卫必然命中 ⇒ 现网不可达；本 finding 是"声明的 fail-closed 面自身有洞"，不是"现在会算错"）
- 怎么算修好：把 :124 的循环上界从"只 coef"扩到 `coef[j]` 与 `center[j]` 两列（同一 `nterm` 上界），并把 :118 的 finite 判定同样加到 `center`；**同批**：给 `apply_photometry_spatial` 补一条负例（构造 center=NaN、coef 全有限 ⇒ 必须返回 -6），否则该洞在任何后续改动里都会静默回归。加检查不会把现有合法场判红（合法场 center 由 :535 从 `o.meanB` 导出，且已过 amax 守卫）。
- 置信：CONFIRMED（漏检与 NaN 传播链逐跳复算；不可达性由被引用行的上游守卫确认）

### CR-55-4 · 施加面对"越域坐标"零判定，场的适用域只写在注释里
- 轴：D（主）/ R
- 位点：`photometry_apply.cpp:135-140`
  ```
      for (std::size_t i = 0; i < npix; ++i) {
          const int x = static_cast<int>(i % (size_t)w);
          const int y = static_cast<int>(i / (size_t)w);
          const double m = photo_spatial_m(field, (double)x, (double)y);
  ```
- 上位依据：SCI-CAL-001 §5 为 flat floor 明确写出"适用域 = 平场响应处处 ≥ 0.1·median(flat)…超出该域…必须在 provenance 中标注 floor 生效像素占比"——同一条科学文件对**乘性场的适用域**没有任何条款（检索式：`git grep -n "x̃\|适用域" -- docs/science/PHOTOMETRY.md`；§16.1 被本头点名，但施加面适用域无条文）。photometry_apply.h:75 注释自称"拟合侧取帧几何中心与半宽 ⇒ x̃∈[−1,1]"。
- 现状：函数把 `(x,y)` 取为**输出图像自身**的 0-based 像素下标，与场的 `x_ref/x_scale` 无任何一致性校验：若被施加图像的 `w/h` 与拟合帧几何不同，`|x̃|>1` 的外推完全不受约束（指数的二次项 ⇒ m 可以任意大），而 :522 的上游幅度守卫只在**拟合帧的网格**上评估过。
- 差在哪：一个纯乘性校正场在自身定义域外被当作有效值乘进像素，且没有任何越域计数/返回码；与同仓 SCI-CAL 对 floor 越域"必须登记占比"的既有口径不对称。
- 后果：该路径要求"施加图像尺寸 ≠ 拟合帧尺寸"，现网由 `module_adapters.cpp:6041 find_scale(lp)` 按同一 light 路径取尺度、读同一 `p1_calibrated_path` ⇒ 尺寸同源，暂不可达；不可达时它仍然使"m(x,y) 只在被估计过的视场内有效"这一诚实边界无法被机器守住。
- 定级：S3（缺触发路径＋无被违反的正面条文）
- 怎么算修好：最小改法——在 :135 循环里对 `nterm>0` 的场加一条域判定（`|x̃|≤1+ε && |ỹ|≤1+ε`，ε 需先有推导再登记），越域时返回新码或输出越域像素计数；或在 `PhotoSpatialGain` 里显式携带拟合帧 `w_fit/h_fit` 并在施加入口判"尺寸必须同源"。同时把适用域写进 PHOTOMETRY.md §16.1（变更流程）。
- 置信：PARTIAL（缺"该仓是否允许对已拟合帧之外的像素施加场"的条文一角）

## 不报的核过项（photometry_apply.h / .cpp）

| 核过项 | 为什么不是缺陷 | 检索式 |
|---|---|---|
| 零点是"加性还是乘性" | 正本即乘性：PHOTOMETRY.md:13 `I_photo = k_photo·m(x,y)·I_cal`、:14 "标定因子的绝对值无物理意义…有意义的判据只有一条（尺度无关）：施加后星点星等与 Gaia 的残差"；星等域的加性零点在 SCI-PHOT 里由 `location`/`scale=10^−location` 折算成像素域乘子，本文件只做乘 ⇒ 一致 | `git grep -n "I_photo" -- docs lib` |
| 施加顺序（偏置→暗→平场→测光标度） | 本文件只承担最后一因子；SCI-CAL-001 §3"逐帧测光标度 `x′=α·x`（标度类别 photo_scaled_adu）由下游 photometry 节点施加，**本层不施加**"＋§5 冻结顺序引 ccdproc/ip_isr（§14 第 4/5 条"据此冻结：bias → dark(×K) → flat"）；生产落点 `module_adapters.cpp:6044` 读 `p1_calibrated_path` → :6082 写 `p1_photoapplied_path` ⇒ 顺序与正本一致 | `git grep -n "p1_photoapplied_path" -- lib/infrastructure` |
| 逐像素独立 ⇒ 与线程数无关（:84 声明） | 实现体无任何跨像素归约、无累加器（:135-140 单循环，`i%w / i/w` 只读本像素），声明成立 | 直读 photometry_apply.cpp:135-140 |
| `apply_photometry` 是"冻结路径"、order=0 委托之（:91-93）| 委托而非复制公式，两入口对 order=0 逐位一致（同一乘法），无"多侧实现"风险 | 直读 :86-93 |
| order=0 时 `field` 全 0 也不判 -6 | 走 :91 委托支路，压根不求值 `field`，与头声明（order=0 ⇒ m≡1）一致 | 直读 :90-93 |
| NaN/Inf 输入像素透传 | 头 :37 与 :80 两处一致声明，且 PHOTOMETRY.md:13 的消费律对方差不对称、drizzle 侧以 `isfinite` 跳过 ⇒ 属适用域声明而非漏检（与 CR-55-3 的区别：那是**因子本身**非有限，输入本无问题） | 直读 photometry_apply.h:37,80 |
| `coef[5]`/`center[5]` 是否可能越界 | 全部读点循环上界 = `photo_spatial_nterm(order) ≤ 5`（:88-95、:124），无越界 | 直读 :88-95,124-129 |

---

# 3 lib/algorithms/calibration/src/master_generator.cpp —— 已读：是（表 13 项）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| master_generator.cpp:76 | `k_sigma` | 1.482602218505602f | MAD→高斯 σ 换算 | 文献值 | SCI-CAL-001 §9"系数为唯一全精度写法 1.482602218505602（=1/Φ⁻¹(3/4)，高斯假设；单精度存储时写作 …502f）"＋§14a 归属 Rousseeuw & Croux 1993 JASA 88,1273（并明示"本仓用渐近常数、不做有限样本校正"）；ALG-CAL-001 F1.3（:87-92）逐字复述并给出"击穿点=50%"适用域 | — | CONFIRMED（值与写法与 §9 完全一致） |
| :59 | `* 0.5f` | 0.5 | 偶数样本取双中位均值 | 公式导出 | ALG-CAL-001 文末"中位数定义（全文档共用）：n 偶取 `(v[n/2−1]+v[n/2])/2`（`master_generator.cpp:49-60`）"；代码与该式一致（:53-59） | — | CONFIRMED |
| :52-54 | `mid = n/2`、`n % 2 == 1` | 2 / 1 | 中位数下标与奇偶判定 | 结构性不适用 | 次序统计量定义，不含科学语义 | — | CONFIRMED |
| :50,92,128,155,168,217 | `NAN` | — | "该样本/该像素无有效值"哨兵 | 结构性不适用 | 哨兵语义由 SCI-CAL §4"输出不传播 NaN 为伪有效值"＋ALG F1.4"全 NaN → NaN"承载 | — | CONFIRMED |
| :75,185 | `const int npix = w * h;` | int32 | 像素总数 | 结构性不适用（**但已登记缺陷**） | README §9 已知限制"w·h int31 溢出无防护"＋ALG-CAL §10 DISP-CAL-009；本文件未新增处理 ⇒ 不另立 finding，见"不报的核过项"最后一行 | — | CONFIRMED |
| :78,187 | `n_frames <= 0 \|\| npix <= 0` | 0 | 定义域守卫 | 结构性不适用 | SCI-CAL §4"w>0, h>0, n_frames>=1"；本层 void 静默 return 的补偿在 C 入口（ac_api.cpp:106-107,132-133）⇒ 已登记 DISP-CAL-010 | — | CONFIRMED |
| :84 | `if (n_frames == 1)` | 1 | 单帧直接拷贝、不 clip | 公式导出 | SCI-CAL §7"median 鲁棒性：generate_master 在单帧 n_frames=1 时直接拷贝，不做 sigma-clip"＋ALG F1.1（:74-79，含"调用方须在 manifest 显式登记母版帧数 = 1"的义务 ⇒ 见 CR-55-8） | — | CONFIRMED |
| :104 | `for (iter = 0; iter < max_iter; ++iter)` | 0/`max_iter` | clip 迭代轮数上界 | 待确认（参数无值域门） | `max_iter` 由调用方给：SCI-CAL §2 符号表只写"sigma-clip 阈值/迭代/合并方式"，**全域无一处给 sigma_low/sigma_high/max_iterations 的取值域或默认**（检索式：`git grep -n "sigma_low\|sigma_high" -- docs eng lib/infrastructure`，命中仅 docs/contracts 三处符号说明与配置登记零命中）⇒ 0 与负值的后果无条文可依 | 保守方向：`max_iterations>=1`、`sigma_low/high>0` 强制判 AC_ERR_PARAM（详见 CR-55-5）；影响范围＝master bias/dark/flat 三类母版 | CONFIRMED（缺判定面＝逐行读完 cfg_fill 与两函数） |
| :120 | `if (sigma <= 0.0f) break;` | 0.0f | MAD=0 ⇒ 无离散度、停止剔除 | 公式导出 | SCI-CAL §8 表行"MAD=0 (无离散度) → sigma-clip 提前终止，不再剔除 \| master_generator.cpp: sigma<=0 break" | — | CONFIRMED |
| :127 | `dev < -sigma_low * sigma \|\| dev > sigma_high * sigma` | 负号 | 非对称阈值 | 公式导出 | ALG F1.3（:98）逐字同式 | — | CONFIRMED |
| :132 | `if (rejected == 0) break;` | 0 | 收敛判据（本轮无剔除即收敛） | 公式导出 | ALG F1.3（:99）；本轮内 `int rejected` 声明在循环体内 ⇒ 无"循环外布尔不复位"缺陷（对照 CR 系列已发现的同族形态，此处不成立） | — | CONFIRMED |
| :148-153 | `if (cnt > 0) … sum / cnt` | 0 | 均值分母=非 NaN 样本数 | 公式导出 | ALG F1.4（:102-105）"cnt=非NaN; out=(Σ vals·[!isnan])/cnt，std::accumulate 按帧下标升序 FP32 累加，顺序冻结"——**分子分母同源**（分子 :149-152 也按 `!isnan` 过滤），与 CR 系列发现的"分子分母不同源"母题此处不成立 | — | CONFIRMED |
| :231,273 | `frame_med == 0.0f → 1.0f` / `final_med == 0.0f → 1.0f` | 0.0 / 1.0 | "全零帧中位数为 0 ⇒ 视作 1.0 不归一"的兜底 | **待确认（判红，见 CR-55-6）** | ALG-CAL-002 F2.1（:124）`frame_med == 0.0 → frame_med = 1.0`、F2.3（:133）同款——登记面只**描述**该步、无任何条文论证其科学正当性；:176-177 注释自称"==0 (全零帧) 行为保持不变 (median→1.0 兜底, **最小爆炸半径**)"，而本表下一行的复算证明该断言为假 | 保守方向＝与 `<0` 同一处置（返回 `AC_ERR_PARAM`、out 不写）；影响范围＝master_flat 全部消费方（`cal = num/flat`）| CONFIRMED（纯手算复现，见 CR-55-6） |
| :243,285 | `if (v < 0.1f) v = 0.1f;` | 0.1f | 平场无量纲响应下界（floor） | 文献值（合同冻结值） | SCI-CAL-001 §5 `flat_norm = max(flat/median(flat), 0.1)`＋"floor 0.1 的适用域（冻结，含量纲）…真实响应 f<0.1 的像素…单边偏低（欠校正），且不可由任何后续标度声明恢复…必须在 provenance 中标注 floor 生效像素占比"；§10 不可接受变化第 1 条；机器登记 `eng/contracts/data/v6_clause_registry_v1.json` `FZ-CAL-FLOOR` value 0.1 status FROZEN；ALG F2.1/F2.3 逐字同式 | — | CONFIRMED |

## findings

### CR-55-5 · sigma-clip 的三个输入（sigma_low/sigma_high/max_iterations）在模块入口无任何值域判定，`max_iterations:0` 静默产出不剔除任何离群值的母版
- 轴：R（主）/ C
- 位点：`module_entry.cpp:663-666`
  ```
          if (kv_u64(kvs, nkv, "max_iterations", &u) != 0)
              return efill(err, ACS_ERR_PARAM, ACS_ERR_DOMAIN_CONFIG,
                           "max_iterations positive integer required", 103);
          c->max_iterations = u;
  ```
  同族：`module_entry.cpp:655-662`（sigma_low / sigma_high 只判"能解析为有限数"）、`master_generator.cpp:104`（`for (int iter = 0; iter < max_iter; ++iter)`）、`master_generator.cpp:127`（`dev < -sigma_low * sigma || dev > sigma_high * sigma`）
- 上位依据：SCI-CAL-001 §2 符号表把 `sigma_low/high, max_iter, combine` 列为本合同的输入（"sigma-clip 阈值/迭代/合并方式"），§4 输入有效域只给"维度 w>0,h>0,n_frames>=1；数组指针非空"；README §5"master 生成 = 逐像素非对称 sigma-clip（σ=1.482602218505602·MAD，nth_element 中位数，max_iter 迭代，σ=0/无剔除提前终止）+ median/mean 合并"——操作名即以 sigma-clip 为本质。integration 合同 `astrocs_p1_calibration.integration.json:162` 声明 "non_finite_or_range: ACS_ERR_PARAM + detail=103 (**维度<=0/超界**/非有限数值)"。
- 现状：`kv_u64` 只保证"非负整数可解析"，**`u == 0` 不被拒**（对照 :651 frames 与 :705 max_structure_size 都显式判 `u == 0`，此处独缺）⇒ `max_iterations:0` 合法，:104 循环零轮 ⇒ 一行 clip 都不跑；`sigma_low`/`sigma_high` 经 `kv_f64`（:468-480）只判 `isfinite` ⇒ 负值与 0 合法。负 `sigma_low = -s` 使下界变成 `dev < +s·σ`，与上界合起来把**除中位数外的一切样本**都置 NaN ⇒ mean 支路 `cnt==0` ⇒ 全帧 NaN（`master_generator.cpp:155`），而 `generate_master` 是 void、C 入口 `ac_generate_master_bias`（ac_api.cpp:106-110）在参数合法时**恒返回 AC_OK** ⇒ adapter 映射 `rc == AC_OK` → 出 manifest、记成功。
- 差在哪：同一个 `cfg_fill` 里"必需键存在性"与"类型"都判了、唯独"值域"对这三键整体缺席，而 integration 合同把 103 号错误码的语义写成含"超界"；`max_iterations` 的错误消息自称 "positive integer required" 却放行 0 = **文字在声称一件实现没做的事**（与 CR 系列"登记面在说文件里没发生的事"同族）。
- 后果：母版 bias/dark 在未剔除宇宙线/热像素/瞬态的情况下交付（0 轮）或整帧 NaN（负 sigma）后**仍被登记为一次成功的 master 生成**；失真的是"多帧稳健合并母版"这一对外主张与它所支撑的 §4.2 `calibration` 节点可信性；NaN 母版会进一步把整帧 `cal` 变 NaN（`calibrator.cpp` 直通）。方向：**偏绿**（无任何判据变红，产物看起来"生成了"）。
- 定级：S2（**缺生产触发路径**：本模块 DLL 的 op 面在生产零调用方——README §1/§10 记 `astrocs_module_query_v1` "函数体零调用 ⇒ 机读状态 = NOT_IMPLEMENTED"，且 `git grep -n "sigma_low" -- lib/infrastructure` 零命中 ⇒ 无生产配置生产者；legacy `ac_generate_master_*` 同样零生产调用方，见 README §5"生产调用方：仅 ac_calibrate_frame"）
- 怎么算修好：`cfg_fill` 的 master 段加三条：`max_iterations == 0 → 103`（消息与判据对齐）、`!(sigma_low > 0) || !(sigma_high > 0) → 103`（若确要允许"禁用 clip"，须显式用另一个词表值表达并在 out manifest 记 `sigma_clip_applied=false`，不得靠 0 静默表达）；**必须同批改**：`sci_id/alg_id` 面无变更，但 `docs/contracts/DATA_SEMANTICS.md §9 DATA-P1-CAL` 需补这三键的取值域（当前条文面零提及），否则改完仍是"实现自己定域"。补负例：`{"op":"generate_master_bias",…,"max_iterations":0}` 与 `"sigma_low":-1` 必须判红。
- 置信：CONFIRMED（三条判定链逐行读完；0 轮与负阈值的后果按 :104/:127 手算）

### CR-55-6 · 全零平场 stack 经"median==0 → 1.0"兜底后被产出为**恒等母版 1.0** 且返回 AC_OK——注释自称"最小爆炸半径"实为最大
- 轴：R（主）/ D
- 位点：`master_generator.cpp:231` `if (frame_med == 0.0f) frame_med = 1.0f;` 与 `:273` `if (final_med == 0.0f) final_med = 1.0f;`
- 上位依据：SCI-CAL-001 §5（`flat_norm = max(flat/median(flat), 0.1)`，"median<=0 时不归一，保持原样"）与 §8 表行"`median(flat)<=0` → `normalize_flat` 不归一，保持原样（`calibrator.cpp:91 if(!(med>0)) return`）"；ALG-CAL-002 §3.2 之后的"两侧分工"说明（:138-143）："F2.1/F2.3 是**生产侧**，其合同是产出一个 `median=1.0` 的母版——此时'保持原样'等于交付未归一母版，违反 SCI-CAL-001 §5 输入合同。故负 median 返回 `AC_ERR_PARAM`（fail-closed）**是**该合同下的正确行为"；AGENTS.md §5"判据必须非退化：真值无效应时度量归零或判红；恒真门没有证据资格"。
- 现状（我按原文逐步手算，输入 `flat_stack` 全 0、`bias=NULL`、`n_frames>=1`）：
  1. :205-207 `dst = src`（全 0）；:216-222 tmp 非空（0 不是 NaN）⇒ 不拒；:223 `frame_med = 0.0f`；:226 isfinite ✓；**:231 `frame_med = 1.0f`**；:234 `frame_med < 0` 不成立 ⇒ 不拒；:241-245 `v = 0/1 = 0 < 0.1 ⇒ 0.1` ⇒ 该帧变成**常数 0.1 场**。
  2. :250 generate_master(mean) ⇒ 常数 0.1 ⇒ :120 `sigma<=0 break` ⇒ out ≡ 0.1。
  3. :267 `final_med = 0.1`（非 0、非负、有限）⇒ :284 `out = max(0.1/0.1, 0.1) = 1.0` ⇒ **`out ≡ 1.0`，:294 `return AC_OK`**。
- 差在哪：同一段代码对 `median<0` 判红（"全负/多数负的退化输入, 不可归一化"），对**同一退化类的 `median==0`**（≥50% 样本非正、无任何响应信息）却放行并交付一个 `median=1.0` 的母版——即"完全没有平场信息"的输入产出的正是该函数**声称的产物形态**（§5/§2 符号表 `flat_norm`: "median=1.0"），二者在产品面不可区分；:176-177 注释"最小爆炸半径"与上述事实相反（恒等 1.0 母版 = 等价于根本没做平场校正，且无任何标记）。多帧情形同样成立：一帧真平场 + 一帧全零 ⇒ 该列 mean 结构被 0.1 稀释约一半，母版对比度系统性变浅，仍返回 AC_OK。
- 后果：`master_flat` 产品与"平场已按 §5 归一并 floor 0.1"的对外主张；消费侧 `cal = num/max(flat,0.1)` 得到"平场校正已做"的字节而实际未做 ⇒ 直接落入 AGENTS §5 禁止的"真值无效应⇒度量不归零也不判红"，并使 `p1_op_calibrate` 的"平场归一化判定带 U2"（CALIBRATION.md §11(b)：`median∈[0.5,2.0]` 判绿）被必然满足——**该门对这种母版恒绿**（§11(b) 自己已承认"物理近零的退化平场同样落在带内 ⇒ 判绿"，但未承认"由本函数生产出来的退化平场恰好是 1.0"这一更强的形态）。方向：偏绿。
- 定级：S2（缺生产触发路径：`ac_generate_master_flat` / `generate_master_flat` op 零生产调用方，理由同 CR-55-5；缺陷本身与输入无关地确定，手算即证 ⇒ 不按 S1 报）
- 怎么算修好：把 :231 的 `== 0.0f` 支路删掉并并入 :234 的拒绝（`if (!(frame_med > 0.0f)) return AC_ERR_PARAM;`），:273 同款（`final_med == 0` 亦拒）；这样与 §3.2 的"生产侧必须产出 median=1.0 母版"的自述一致。**必须同批改**：(a) ALG-CAL-002 F2.1:124 与 F2.3:133 两条登记需同步撤销（否则改对反而与登记面冲突、`p1cal` 相关用例判红）；(b) `README §5`（:77"逐帧 median 归一（0/NaN→1.0）"）同批订正；(c) 补一条**非退化负例**：全零 flat stack 必须 `rc==AC_ERR_PARAM` 且 out 未被写（用哨兵填充断言），否则该兜底会在后续改动里回归。
- 置信：CONFIRMED（全链算术自行手算，与被测代码同源输入独立推导；未跑构建）

### CR-55-7 · `generate_master` 对"离群占比 ≥50% 必须显式拒绝该像素列"的登记义务零实现，也无任何样本数输出面
- 轴：D（主）/ R
- 位点：`master_generator.cpp:136-157`（合并段全部）——尤其 :141 `out[idx] = median_of(work);`（`work` 可为 1 个元素）与 :148 `if (cnt > 0)`（1 个样本即出均值）
- 上位依据：`docs/algorithms/CALIBRATION_ALGORITHMS.md` ALG-CAL-001 F1.3（:94-97）原文：
  > `# 适用域: "先估尺度再 clip"成立的充要条件是离群像素占比 < 50%（MAD 的击穿点 = 50%）：占比 < 50% 时 mad 完全不受离群值影响，阈值不被抬高；占比 >= 50% 时 med 本身已落在离群群里，本迭代既不收敛也无科学含义 ⇒ **必须显式拒绝该像素列，不得输出该值**。`
- 现状：函数是 `void`（:67），无返回码、无逐像素有效位、无幸存样本数计数输出；剔除后只要 `cnt>0`（mean）或 `work` 非空（median）就出值。双峰列（如 `n_frames=4, {0,0,1000,1000}`）实测路径：:111 `med=500`、:118 `mad=500`、:119 `σ=741.3`、:127 `|dev|=500 < 3σ` ⇒ **一个都不剔**，out = 500（median 与 mean 同为 500）——该值既不是"好像素电平"也不是"热像素电平"，正是条文说"无科学含义、不得输出"的那一类；另一类 `n_frames=4, {0,0,0,1000}`：`mad=0 ⇒ σ<=0 ⇒ :120 break`，mean 出 250（尖刺被摊进母版），median 出 0，取决于 `combine`。
- 差在哪：被算法层条文点名为"必须显式拒绝"的处置在这份实现里根本没有承载面（连"该列剔除了多少样本"都不出），既不能判红也不能登记。
- 后果：master bias/dark 在"半数以上帧同一像素异常"的位置上拿到一个两不似的中间值 ⇒ `cal = raw − bias − K·dark` 在该处残留系统性偏差，且该偏差**随母版被冻结**、跨帧同形 ⇒ 直接落到 CALIBRATION.md §14a"母版方差/共享系统项"登记为缺口的面上，使"多帧同一物理标度可比较"（§1 目的）在最需要稳健性的像素上不成立。方向：偏绿（母版看起来正常生成、无任何降级记录）。
- 定级：S2（缺生产触发路径，同 CR-55-5）
- 怎么算修好：`generate_master` 改签名 `int`（与 `_flat` 对齐）并新增逐像素输出面——最小改动：`valid_mask[h*w]`（0/1）或 `n_valid[h*w]`（u8），判据 `(幸存数 / n_frames) < 0.5 ⇒ mask=0` 并把该像素写 NaN；C 入口与 adapter 的 out manifest 必须登记 `n_pixels_rejected` 与 `n_frames_used`；**必须同批改**：(a) SCI-CAL-001 §8 表加一行（当前该表对"离群占比≥50%"零条目）；(b) ALG-CAL-001 F1.3 的"必须拒绝"要落进 F1.4 的合并伪码，否则登记面与实现面还会再分叉；(c) 现有 FIX-CAL-C"离群 stack: n_frames=5 其中 1 帧注入尖刺"（ALG §6:433）仍是 <50% 档，需补 ≥50% 档负例，否则新判据不可见。
- 置信：CONFIRMED（两支算术自行手算；"无输出面"以整文件读完＋签名 `void` 为据）

### CR-55-8 · 母版实际帧数与登记面：配置 `frames` 从不与 manifest 实到帧数比对，out manifest 也没有任何帧数字段（违反 ALG F1.1 的显式登记义务）
- 轴：C（主）/ D
- 位点：`module_entry.cpp:962` `const int n = (int)n_frames;`（`n_frames` 来自 :757 `*n_out = (uint64_t)fr->aux_n;` = manifest 数组长度）；对照必需键 `module_entry.cpp:651-654`（`frames` 只判 `u != 0`）与 out manifest 键集 `module_entry.cpp:794-826`（只有 `schema_version/width/height/dtype/out_base64/op`，calibrate 多 `actual_k`，correct 多 `hot/cold`）
- 上位依据：ALG-CAL-001 F1.1 适用域（CALIBRATION_ALGORITHMS.md:76-79）："单帧母版 = 该帧的逐位副本 ⇒ 母版继承该帧全部宇宙线/坏点/瞬态，稳健尺度不可估计……**调用方须在 manifest 显式登记母版帧数 = 1**，下游据此决定是否可用于标度与噪声台账"；SCI-CAL-001 §8 表行（bias 缺位）"运行必须在**预检/manifest 显式登记**"。
- 现状：`c.frames`（配置声明）在 :962 被弃用，实到帧数 `fr->aux_n` 直接进算法；两者不等时**无任何诊断**（配置写 20、manifest 给 3 ⇒ 静默按 3 生成）；`build_out_manifest` 也不写任何帧数/`n_frames_used` ⇒ 下游与审计者既看不到声明值也看不到实到值，F1.1 点名的"母版帧数=1 必须登记"在该模块唯一的输出面上**没有承载键**。另注：manifest 侧硬上限 `ACS_CAL_MAX_STACK 64`（:300，jc_parse_object:334 超限即拒）而配置 `frames` **无上界**（:651 只判 `!=0`）⇒ `frames>=65` 的配置永远不可能满足，且失败消息是 :851 的 "manifest is not a flat JSON object"（与真因无关）。
- 差在哪：三处（配置声明 / 实际使用 / 输出登记）各说各话且互不校验，而被点名的那一项登记在实现里根本没有字段。
- 后果：任何"母版由 N 帧稳健合并产生"的表述都无法从产品 manifest 核对；单帧母版（F1.1 判定的关键前提）在下游不可识别 ⇒ "标度与噪声台账"据此做出的判断失去依据。方向：偏绿（登记缺失，不产生红灯）。
- 定级：S2（缺生产触发路径：本 DLL 零生产调用方）
- 怎么算修好：:962 之前加 `if (n_frames != c.frames) return efill(..., "manifest frames count != config frames", 111);`；`build_out_manifest` 对 OPK_MASTER 增加 `"n_frames_used":<实到>`；对 `n_frames_used==1` 额外写 `"master_single_frame":true`；并给 `frames` 一个与 `ACS_CAL_MAX_STACK` 同源的上界（同批改 :334 的失败消息，改为点名数组键与上界）。**必须同批改**：integration 合同 `astrocs_p1_calibration.integration.json` 的 out manifest 键集（:126 的 keys 列表）需同时加键，否则"键集 = 真实输出"这句自证断言会判红。
- 置信：CONFIRMED（键集与比较缺失均逐行读完；无生产调用方以 README §1/§5 为据）

### CR-55-9 · plan 的 `memory_bytes_estimate` 对 master 类 op 少乘一个整帧面积（work_units×esz 与真实峰值差 w·h 倍）
- 轴：R（主）/ C
- 位点：`module_entry.cpp:1236-1237`
  ```
                        (unsigned long long)(work_units *
                                             (e->is_f64 ? 8u : 4u)));
  ```
  其中 :1219 `const uint64_t work_units = master ? c.frames : (c.width * c.height);`
- 上位依据：`astrocs_p1_calibration.integration.json:127` "memory_bytes_estimate=work_units*sizeof(dtype); io_bytes_estimate=0 是真实值…**禁预测值/禁空转假 plan**"；README §6 资源分类 "cpu_heavy（master 生成/校准为重计算…）"；AGENTS.md §3"重计算必须套内存看门狗…单个计算进程可能涨到 10 GB 以上匿名内存 ⇒ 触发全局 OOM killer"。真实峰值登记面：README §5"峰值额外内存：generate_master_flat **O(n_frames·npix·4B)**（norm 主缓冲）"＋ALG-CAL §5:324"内存 | …generate_master_flat O(n_frames·npix·4B)"。
- 现状：master op 的 `work_units = 帧数` ⇒ 估计值 = `frames×4` 字节；同一 op 在 :740 `stack->resize(fr->aux_n * plane_bytes)` 实分配 `frames×w×h×4` 字节，:957 `out_plane(plane_bytes)` 再一帧。4096×4096、20 帧 ⇒ 报 **80 字节**、真实约 **1.34 GB**（差 w·h 量级）；帧级 op（calibrate）work_units 已是像素数 ⇒ 报 `w·h·4`，而真实是 light+out+至多 3 个母版 ≈ 5 帧（差 5 倍）。
- 差在哪：一个被合同冠以"真实值/禁预测值"的准入量，在 master 分支上把单位（帧 vs 像素）当成了字节基数 ⇒ 不是保守下界而是**量纲错**；同一表达式在两个分支上含义不同（帧级分支至少是同量纲下界）。
- 后果：当前零生产消费者（`git grep -n "memory_bytes_estimate" -- lib` 命中全部为生产者与 `eng/tests`；`lib/infrastructure/scheduler/src/runtime.cpp:187,257` 只调 `plan()` 并检查是否失败，不读该键）⇒ 不改变产品字节，但"plan 给出真实内存估计"这句对外主张不成立；一旦该键被用于准入/看门狗阈值（AGENTS §3 的 OOM 面正是它的用途），master 类 op 会以"80 字节"的价格获得 1.34 GB 的准入。方向：偏绿（低估）。
- 定级：S2（无生产消费者 ⇒ 写不出"产物字节/门禁"通路；量纲错本身 CONFIRMED）
- 怎么算修好：master 分支改 `(work_units * plane_bytes_per_frame)`（即 `frames*w*h*esz`，与 :740 实分配同式）＋ out 平面；帧级分支改为 `(in + out + 已提供母版数) * plane_bytes`；并在 integration 合同里把该键的定义式写成"上界"而不是"work_units×esz"。**必须同批改**：`eng/tests/unit/calibration_integration_test.cpp:788` 与 `cosmetic_integration_test.cpp:807` 正在断言旧式（`pmem == work_units*esz` 类），改式必须同批改这两处的期望，否则改对反而判红；`p1_noise/adapter_test.cpp:526,551` 是另一模块的钉值，不受本改影响（改前先确认 cosmetic/noise 两处是否共用同一 helper）。
- 置信：CONFIRMED（式与真实分配点同文件逐行；消费者面以 git grep 为据）

### CR-55-10 · plan 的 node_id 截断上界（96）不是按缓冲区剩余容量导出，node_id ≳ 46–55 字符时产出**语法非法**的 plan JSON
- 轴：R
- 位点：`module_entry.cpp:1222-1231`
  ```
        char head[192];
        std::snprintf(head, sizeof(head),
                      "{\"plan_version\":%d,\"node_id\":\"%.*s\", ...
                      (int)(node_id.size > 96 ? 96 : node_id.size),
  ```
  拼接点 `:1238 const std::string js = std::string(head) + tail;`
- 上位依据：ASTROCS_DESIGN.md §4.2:271"节点序与依赖边由**注册表端口图唯一确定**，并由机器门断言（注册表 ↔ 管线 IR 的节点序与边保真）"——plan/节点标识是被机器消费的结构化面；integration 合同 :126 声明 plan 键集含 `node_id` 且"plan 键集 = DLL cal_plan 真实输出"。
- 现状（我把格式串的固定开销逐字符加过）：固定字面量 96 + `plan_version` 的 1 位 = 97，加 `work_unit`(5)+`parallel_axis`(5)+`,`min_workers":1,(17) 与 `work_units` 的 D 位数字，再加 op 名（词表最长 `"generate_master_flat_f64"` = 24），得 `总长 ≈ 131 + L(node_id) + D(work_units 位数)`；缓冲 192 ⇒ `L ≤ 60 − D`。4096×4096 帧级 op（D=8）⇒ **L>52 即截断**；master op（D≤3）⇒ L>57 即截断。而 :1228 的截断许可给到 96，且截断后 :1238 仍把 `tail` 直接拼在**被砍断的 head** 之后 ⇒ 产出形如 `…"work_units":16777216,"min_wo"memory_bytes_estimate":…` 的**语法非法 JSON**，并以 ACS_OK 返回（`strbuf_commit` 只看调用方缓冲够不够，不看内容是否闭合）。
- 差在哪：截断保护的上界是拍的（96），没有与同函数内 `head[192]` 的其余开销对账 ⇒ 保护动作本身制造了畸形输出；"不静默"在这里不成立（无任何标记、返回码为成功）。
- 后果：`lib/infrastructure/scheduler/src/runtime.cpp:187/257` 拿到 plan 后按 JSON 解析的调用者会失败或读到残字段 ⇒ 该节点的资源/并行意图丢失；被登记为"真实输出"的 integration 合同键集对产品面不成立。当前校准 DLL 零生产调用方 ⇒ 未成真。方向：偏绿（成功返回畸形串）。
- 定级：S3（缺生产触发路径 + 需要 node_id 长度分布证据才能定档，见"读不动"节）
- 怎么算修好：把 head 缓冲按最坏情形定尺寸（`96 + 24 + 数字位数上界 + 固定串` ≥ 256），或先算 `room = sizeof(head) − 固定开销 − op_len − D`，再按 `room` 截 node_id；截断发生时在 JSON 里加 `"node_id_truncated":true`；补一条用例：60 字符 node_id 的 `plan()` 输出必须能被解析且 `node_id` 前缀可核对。**同批**：`astrocs_p1_cosmetic` 的 `module_entry.cpp:1004` 是同型骨架（同一 head/tail 拼接），改一处要同族同改，否则"同谱系骨架不许外溢结论"的反例再次成立。
- 置信：PARTIAL（长度算式与畸形拼接路径已逐字符复算；缺"现网 node_id 实际长度分布"一角 ⇒ 是否已有节点越界未知）

## 不报的核过项（master_generator.cpp）

| 核过项 | 为什么不是缺陷 | 检索式/依据 |
|---|---|---|
| `generate_master` 是 `void`，`n_frames<=0/npix<=0` 时静默 return 不写 out | 已由 SCI-CAL §8 之外的登记面显式登记：ALG-CAL-001 §7:380"空指针 / n_frames<=0 / w<=0 / h<=0（**C API 入口**）返回 AC_ERR_PARAM，不写 out｜ac_api.cpp:60-61…"＋ALG-CAL §10 **DISP-CAL-010**"`ac::` 层 n_frames<=0 仅日志不返回（C API 层已挡）"；我核对了 C 入口确实挡（`ac_api.cpp:106-107` 与 `:118-119`、flat 的 `:132-133` 三条 `if (!stack \|\| !out \|\| n_frames <= 0 \|\| width <= 0 \|\| height <= 0) return AC_ERR_PARAM;`）⇒ 不另立 finding | 直读 ac_api.cpp:101-139 |
| `int npix = w*h` 溢出 | 已登记 **DISP-CAL-009**（ALG-CAL §10:519）＋README §9"w·h int31 溢出无防护"。本批只补一句新事实：`module_entry.cpp:957 bytes out_plane(plane_bytes)` 是**零初始化**，故该溢出经 DLL 路径命中时后果从"未定义缓冲"变成"静默的全零母版 + rc=AC_OK"——登记条目本身未覆盖这一后果（属登记面需订正，不另立 finding 以免重复计数） | `git grep -n "DISP-CAL-009" -- docs` |
| 饱和帧在母版生成期不做专门判定 | 条文面把饱和归给别处：SCI-CAL §1 非目标＋§9a"saturation：FP32 饱和语义界定，不静默 clamp"＋README §2 不负责"宇宙线剔除（叠加 rejection）"；母版侧唯一机制＝上侧 `sigma_high` 阈值（:127）⇒ 无"设计点名而实现缺失"的饱和剔除义务。（真正的后果落在 CR-55-7 的 ≥50% 档） | `git grep -n "saturat\|饱和" -- docs/science/CALIBRATION.md lib/algorithms/calibration` |
| 合并统计（mean/median）的选择无出处 | 不是"缺出处"而是"**参数化、由调用方显式给**"：SCI-CAL §2 符号表列 `combine`、ALG F1.4 逐式登记两支、flat 侧固定 mean 并在 ALG-CAL-002:114-115 给出出处（"无 combine 参数，合并固定 mean（SCI-CAL-001 §12 ALG-CAL-002 语义一致）"）；本文件对 combine 无默认值 ⇒ 无"多侧默认不一致"面（检索式：`git grep -n "AC_COMBINE_MEAN" -- lib` 命中仅参数传递与默认分支） | 直读 :67-157 |
| `median_of` 偶数取双中位、`max_element` 取上界 | 与登记式逐字一致（ALG 文末中位数定义引 `master_generator.cpp:49-60`），且 nth_element 后 `[begin,mid)` 的最大值确为下中位（注释 :56 的论证成立） | 直读 :49-60 |
| mean 分支"分子分母是否同源" | 同源：分母 `cnt` 数非 NaN（:145-147），分子 `accumulate` 以 `isnan(v)?acc:(acc+v)` 过滤（:149-152）⇒ CR 系列"分子分母不同源"母题此处**不成立** | 直读 :143-153 |
| 全 NaN 帧 / 非有限帧在 flat 路径的处置 | 已判红：:219-221（全 NaN 帧 `AC_ERR_PARAM`）、:226-230（`!isfinite(frame_med)` 拒）、:234-238（`frame_med<0` 拒），且 CALIBRATION_ALGORITHMS.md:138-143 用整段论证"生产侧拒绝是正确行为、与 §4/§5 消费侧'保持原样'不矛盾"——我按两个适用域分别核对，**不是**自相矛盾 | 直读 ALG §3.2 之后说明 |
| 单帧母版直接拷贝（:84-88） | SCI-CAL §7 把它写成"median 鲁棒性"条目并明示"不做 sigma-clip"⇒ 行为有出处；缺的是**登记帧数**那一半 ⇒ 已并入 CR-55-8，不重复立条 | `git grep -n "single frame" -- docs` |

---

# 4 lib/algorithms/calibration/src/module_entry.cpp —— 已读：是（表 26 项）

## §2.5 数值处置表（每个数值一行；同族同值合并并列出全部位点）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :99,100,101 | `needed + 1u` / `cap - 1u` / `n > 0u` | 1 / 0 | strbuf 的 NUL 终结符与可写前缀 | 结构性不适用 | `lifecycle_v1.h`/drizzle·gaia 先例（文件头 :90-92 声明同口径）；NUL 是 C 串定义 | — | CONFIRMED |
| :115-118 | `+26 / +52 / 62 / 63` | 同 | base64 字母表偏移 | 结构性不适用 | RFC 4648 标准字母表定义（:111 注释点名）＝编码表定义常数，非科学量 | — | CONFIRMED |
| :131,137,142,143,144,145,174,182,190,191,197 | `4 / 3 / 2 / 1 / 6 / 8 / 0xFFu / <<16 / >>18 / >>12 / >>6 / 63u` | 同 | base64 四位一组、每字符 6 bit、3 字节还原的位算术 | 结构性不适用 | 同上（编码格式定义常数）；解码严格性由 `b64_decode_len` 与 :745 `dl != plane_bytes` 双重判定 | — | CONFIRMED |
| :131 `pad >= 2` | 2 | '=' 补齐最多 2 个 | 结构性不适用 | RFC 4648 补齐规则 | — | CONFIRMED |
| :300 | `ACS_CAL_MAX_STACK` | 64 | manifest 单键数组的帧条目上限 | 结构性不适用（缓冲区/循环上界） | 无条文给该数（检索式 `git grep -n "ACS_CAL_MAX_STACK" -- docs eng` 零命中）⇒ 实现选择；**但它与"配置 frames 无上界"配对成 CR-55-8 的不可满足域** | 保守方向：`frames` 上界与该常量同源并显式判红 | CONFIRMED |
| :334,361,645,845,1142,1190,1267,1342 | `maxkv = 16`、`jc_keyval mkv[16]` | 16 | 键值数组容量（词表 15 键／manifest 13 键 +1） | 结构性不适用 | CFG_VOCAB 实测 15 项（:387-403 逐行数）；manifest 键集见 types.h:65-78 共 13 项 | — | CONFIRMED |
| :404 | `CFG_VOCAB_N` | 15（由 sizeof 导出） | 词表基数 | 结构性不适用 | 枚举基数；types.h:32-46 逐条对应 15 个键名宏 | — | CONFIRMED |
| :439 | `double d = 0.0` + `sscanf("%lf")` | 0.0 | 非有限判定的初值 | 结构性不适用 | 与 :468-480 `kv_f64` 的判据不同源 ⇒ 见 CR-55-13（弱） | — | CONFIRMED |
| :460 | `(UINT64_MAX - (c-'0'))/10u` | 10 | 十进制累加溢出守卫 | 结构性不适用 | 溢出判据的定义常数 | — | CONFIRMED |
| :471 | `char buf[64]`（kv_f64） | 64 | 数字串拷贝缓冲 | 结构性不适用 | :472 `vlen >= sizeof(buf)` 显式判长（不截断）⇒ fail-closed | — | CONFIRMED |
| :495 | `kvs[i].vlen == 4` | 4 | "true" 的长度 ⇒ 布尔真 | 结构性不适用 | 解析层只允许字面 `true/false`（:348-353 `jc_literal`），长度 4 唯一天然真；不携带科学语义 | — | CONFIRMED |
| :531-532（cfg_fill 内） | `const char* keys[10]; char kinds[10];` | 10 | 必需键数组容量（最长支路 7 项） | 结构性不适用 | `op_required_keys`（:531-554）实际写入 ≤7 ⇒ 容量有余 | — | CONFIRMED |
| :642,646 | `u > 100000000u`（width/height 上界） | 1e8 | 维度上界准入 | **待确认** | 条文面零提及：SCI-CAL §4 只给"w>0,h>0"；`git grep -n "100000000\|1e8" -- docs eng lib/algorithms` 除本文件外零命中 ⇒ 无任何登记面规定该界 | 保守方向：应与产品面真实上限（像素数/字节数预算、`npix` 的 int32 事实）同源，取更小的"能被 npix 与内存准入共同表达"的界；影响范围＝全部 10 个 op 的准入 | PARTIAL（缺"是否有意与某个画幅上限对应"一角） |
| :644,648 | 错误消息 "width must be positive integer" 用于**上界**违规 | — | 诊断文案 | 结构性不适用（但文案与判据不符 ⇒ 并入 CR-55-5 的"文字声称未做的事"一类，不单立） | — | — | CONFIRMED |
| :651 | `frames: u == 0` 判定 | 0 | 帧数下界 | 公式导出 | SCI-CAL §4"n_frames>=1"；**上界缺失** ⇒ CR-55-8 | — | CONFIRMED |
| :655-674 | sigma_low / sigma_high / max_iterations / combine 的判据 | 仅"可解析为有限数/词表值" | clip 参数准入 | **待确认**（见 CR-55-5） | SCI-CAL §2 符号表只列名不给域；integration:162 声称 103 覆盖"超界" | 见 CR-55-5 | CONFIRMED |
| :682 | `!(f >= 0.0)`（dark_scale_factor=K） | 0.0（**下界闭**，容许 K=0） | K 的准入下界 | **待确认（判红，见 CR-55-11）** | DATA_SEMANTICS.md:194"K=t_light/t_dark 由调用方计算（曝光秒，FITS EXPTIME）…**EXPTIME 缺失/非正…→ DATA 拒绝（CLI rc=2）**"；SCI-CAL §7"K=1 不是默认值而是 t_light==t_dark 的特例"；§6a 误差式 `−K·Δb` 亦要求 K 为正曝光比 | 保守方向：`K > 0` 严格判（与 :953 legacy 侧一致由调用方保证）；影响范围＝calibrate_frame ± f64 两 op | CONFIRMED |
| :689-708 | hot_sigma/cold_sigma 仅 `isfinite`；`max_structure_size u == 0`；`method` 词表 | 0 | 坏点参数准入 | 结构性不适用 | README §3 显式声明"sigma 参数无量纲（MAD 倍数，**<=0 禁用对应检测**）"⇒ 负值是合法"禁用"而非越界（与 CR-55-5 的 clip 阈值不同适用域，那里没有"<=0=禁用"的条文） | — | CONFIRMED |
| :711 | `max_workers` 可选，缺键 ⇒ `c.max_workers = 0`（:573 注释"0 = 未给"） | 0 | 租约上限的"未给"哨兵 | 公式导出 | 文件头 :13-16 线程模型；:942-944 `want = ex->max_workers ?: available_cpus`，`if (c.max_workers > 0 && c.max_workers < want)` 才裁剪 ⇒ 0 的语义=不裁剪，与注释同源；非"多侧默认"（只有这一处读它） | — | CONFIRMED |
| :740 | `stack->resize(aux_n * plane_bytes)` | — | 实到帧栈分配 | 结构性不适用 | 真实峰值登记于 README §5 与 ALG §5:324 ⇒ 该值与 CR-55-9 对照 | — | CONFIRMED |
| :754,768,773 | `dl != plane_bytes` / `bl` 长度比对 | — | 平面尺寸严格校验 | 公式导出 | 文件头 :23-27 数据平面 v1 口径（inline base64 严格等长）⇒ 不等长判 111，无静默 | — | CONFIRMED |
| :785 | `char b[40]` + `"%.17g"` | 40 / 17 | double 十进制往返打印 | 结构性不适用 | 17 位有效数字＝IEEE-754 binary64 往返所需的定义位数（无科学语义）；缓冲 40 ≥ 17+符号+指数+`-`+NUL | — | CONFIRMED |
| :794,854 | manifest `schema_version` | 1 | 合同版本号 | 结构性不适用 | 输入侧 :854 严格判 `u != 1` 即拒；输出侧写回 1 ⇒ 单侧一致（types.h:29 config_schema_ver 亦 1） | — | CONFIRMED |
| :813-825 | out manifest 追加键的条件 `op_index >= 0 && kind == …` | — | 键集选择 | 结构性不适用 | 键集登记于 integration:126；**缺 `n_frames_used`/标度施加声明** ⇒ CR-55-8 / CR-55-12 | — | CONFIRMED |
| :837 | `esz = is_f64 ? 8u : 4u`；:957 `out_plane(plane_bytes)` 零初始化 | 8/4 | dtype 元素尺寸 | 结构性不适用 | sizeof 定义；零初始化的后果见 CR-55-7 不报项末行与"不报的核过项" | — | CONFIRMED |
| :906-920 | `planes[3]`、`i < 3`、**`kind != 's' ⇒ continue`** | 3 | 三个可选母版平面 | 结构性不适用（3）＋**判红**（静默降级 ⇒ CR-55-12） | types.h:71-73 声明三个键名 | — | CONFIRMED |
| :942-944 | `want = max_workers ?: available_cpus`、`want < 1 ⇒ 1`、`c.max_workers < want` | 1 | 租约请求核数 | 结构性不适用 | 文件头 :13-16；FORBID-003（不私建线程池） | — | CONFIRMED |
| :959 | `int64_t hot = -1, cold = -1` | −1 | "未运行该 op"哨兵 | 结构性不适用 | 只有 OPK_CORRECT 会改写；:817 起仅 CORRECT 才写键 ⇒ 哨兵不会进产品 | — | CONFIRMED |
| 全文件（:637,644,648,653,657,661,665,673,680,685,691,695,703,707,748,754,765,770,776,856,860,864,871,875,890,939,952,1056） | detail_code 字面量 100/101/102/103/105/110/111/112/120/130 | 同 | 诊断细码 | 结构性不适用（编号无科学语义）⇒ 但**宏零引用** ⇒ CR-55-13 | types.h:97-107 冻结该词表并注明"模块自定义从 100 起为冻结规则" | — | CONFIRMED |
| :1090-1123 | `phase = 1`、`execution_class = 0`、`parallel_ok = 1`、`flags = 0` | 同 | descriptor 常量 | 结构性不适用 | module.yaml（README §10 登记 execution_class=cpu_heavy / parallel_ok=1）；0=cpu_heavy 的词表映射见 :1121 注释 | — | CONFIRMED |
| :1228 | `node_id.size > 96 ? 96 : node_id.size` | 96 | node_id 截断许可 | **待确认（判红 ⇒ CR-55-10）** | 与同函数 `head[192]` 的固定开销（我加到 131+L+D）不对账 | 保守方向：按剩余容量算上界并声明截断位；影响范围＝所有 plan 消费方 | CONFIRMED |
| :1219,1236 | `work_units = master ? frames : w*h`；`mem_est = work_units*esz` | — | plan 工作量与内存估计 | **公式导出→写死且量纲错（CR-55-9）** | integration:127 把该式写成合同规则 | 见 CR-55-9 | CONFIRMED |
| :1294 | `allocator->alloc(..., 16)` | 16 | 实例对齐 | 结构性不适用 | ABI 对齐要求，无科学语义 | — | CONFIRMED |

## findings

> 编号分布说明：§3 名下的 **CR-55-8 / CR-55-9 / CR-55-10** 与 **CR-55-5 的主位点**都在 `module_entry.cpp`（因为它们要论证的是"母版生成链的登记面与参数域"，就近写在 §3 与 §4 之间）；§3 自身位点的 finding 是 CR-55-6、CR-55-7。§4 名下是 CR-55-11/12/13/21。

### CR-55-11 · 模块入口接受 `dark_scale_factor = 0`（含下溢成 0 的写法），等于把暗电流项静默关掉却报告成功
- 轴：C（主）/ R
- 位点：`module_entry.cpp:682-686`
  ```
          if (kv_f64(kvs, nkv, "dark_scale_factor", &f) != 0 || !(f >= 0.0) ||
              !std::isfinite(f))
              return efill(err, ACS_ERR_PARAM, ACS_ERR_DOMAIN_CONFIG,
                           "dark_scale_factor finite >=0 required", 103);
  ```
  消费点 :1010/1020 `c.dark_scale_factor` 直接进 `ac_calibrate_frame(_f64)`。
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:194`（原文）："K（dark_scale_factor）…**=t_light/t_dark 由调用方计算（曝光秒，FITS EXPTIME）**；**标准式（dark_opt=0）与兼容式（dark_opt=1）都施加**（BIAS-001）…**B2-A13 消费边界 fail-closed：P1 校准节点 p1_op_calibrate（module_adapters.cpp:1243-1305）在 K 分支（bias+dark 在位）从 light/dark 的 FITS EXPTIME 推导 K=t_light/t_dark；EXPTIME 缺失/**非正**…**→ DATA 拒绝（CLI rc=2）**"；SCI-CAL-001 §5"K 必须由调用方从 FITS EXPTIME 得出，缺 EXPTIME 时调用方 fail-closed，**模块层不设 K 默认值**"；§7"K 参与不变量：dark≠NULL 时 cal 对 K 的依赖为 −K·dark′/max(flat,0.1)"。
- 现状：准入判据是闭区间 `f >= 0` ⇒ **K=0 合法**；且 `kv_f64`（:476）用 `strtod` 且只判 `isfinite`，`1e-400` 这类下溢会**返回 0.0 并通过**（`end != buf`）。K=0 经 :1010 进 legacy：`dark_opt=1` 时 `cal=(raw−bias−0·(dark−bias))/flat`、`dark_opt=0` 时 `cal=(raw−bias−0·dark)/flat` ⇒ 暗电流扣除整体消失，`actual_k` 回写 0.0，op 返回 AC_OK 并出 manifest。
- 差在哪：**同一键的两个入口取值域不同**——生产节点 `p1_op_calibrate` 按合同把"非正 EXPTIME"判 DATA 拒绝（rc=2），而本模块 C ABI 入口把 0 判为合法；这正是本仓反复出现的"多侧各设一道门、门还不等价"形态，且本侧的门更松。SCI-CAL §5 那句"模块层不设 K 默认值"被实现成"模块层不设 K 定义域"。
- 后果：暗电流未扣除的 `cal` 被当作已校准交付（K=0 使 §7 的"K 参与门"在产品面等价于"bias 参与但 dark 不参与"），并污染其下游全部乘性/加性预算：SCI-B 跨帧绝对 SNR（暗电流噪声项按 dark 电平进入预算而像素里没有）、§6a 的 `|K·Δb|` 残留公式（K=0 时残留恒为 0 ⇒ 该判据自我失效并**判绿**）。方向：偏绿。
- 定级：S2（**缺生产触发路径**：本 DLL 的 `calibrate_frame` op 零生产调用方（README §5"生产调用方：仅 ac_calibrate_frame（lib/phase1_session/p1_session.cpp:243）"，走的是 legacy 直调而非本 adapter；`git grep -n "dark_scale_factor" -- lib/infrastructure` 的命中在 p1_op_calibrate 一侧，不经此处）
- 怎么算修好：:682 改 `!(f > 0.0)`（并同步文案 "finite >0 required"）；`kv_f64` 增加"strtod 后 `end` 必须吃掉整串"与 ERANGE 判定（见 CR-55-13）；**必须同批改**：`op_required_keys` 之外没有任何地方声明 K 的域 ⇒ 需在 SCI-CAL §4"输入有效域"补一句 `K>0`（走变更流程），否则改完仍与"§4 未列该域"打架；另外 `astrometry/` 之外若有把 K 当可 0 关闭开关的用法（`git grep -n "dark_scale_factor" -- eng`）需一并核对，避免把"用 K=0 表达不扣暗"这一非正名用法判红。
- 置信：CONFIRMED（判据行、消费行、上游合同行均逐字读出）

### CR-55-12 · manifest 里已给出的母版平面若 JSON 类型不符被静默当作"未提供"，产出"该标度未施加"的成功结果且无处登记
- 轴：R（主）/ D
- 位点：`module_entry.cpp:911-920`
  ```
      for (int i = 0; i < 3; ++i) {
          const int bi = kv_find(mkv, nmk, planes[i].key);
          if (bi < 0) continue;
          ...
          if (mkv[bi].kind != 's' || !b64) continue;   /* null → 缺席 */
  ```
- 上位依据：SCI-CAL-001 §8 表："bias 未提供而 dark 在位（标准式）｜本底不去除…｜**运行必须在预检/manifest 显式登记（optimize/error 行）**"；同表"母版与亮场标度不一致…fail-closed：消费边界 DATA 拒绝（rc=2），诊断**点名文件 + 观测中位数 + 缺失声明项**"；文件头 :25-28 声明"无 artifacts 服务且 manifest 无 inline 平面时按 legacy NULL 语义（恒等路径, DISP 登记行为保持）"；types.h:71-73 声明三个键名；DATA_SEMANTICS:189/212 的 U3 条款要求"提供 master_dark 而未显式给出 dark_optimization ⇒ 拒绝"。
- 现状：只有"键**不存在**"和"键为 `null`"两种形态被声明为恒等路径；"键存在但类型不是字符串"（`"master_flat": 1.0`、`"master_bias": []`）走 :916 `continue` ⇒ **与"没提供"完全同路**，且 :905 的注释只覆盖 `null`。对照同一函数内其它 manifest 字段的严格度：`frames` 类型不对 ⇒ 判 110（:887-891）、`dtype` 不是串 ⇒ 判 110（:869）、任何平面尺寸不符 ⇒ 判 111——三个母版键是**唯一**"present-but-malformed 被吞掉"的一组。
- 差在哪：把"调用方明确写了但写错"折叠成"调用方什么都没写"，且 :545 强制的 `dark_optimization` 使得"dark 未施加"这一事实连一个否定键都不留 ⇒ 属于派单点名的"未施加的校正不得被登记成已施加"：结果面只有 `op/dtype/out_base64/actual_k`，读方无法知道 flat/bias 到底进没进算术。
- 后果：一次带错类型的 `master_flat` 会产出**未做平场校正**的校准帧并返回 ACS_OK；SCI-CAL §5 对 flat 的整套适用域论证（floor、`median≈1` 判定带、"平场响应已完整校正"成立条件）在产品面上被绕过；同一类错误在 `frames` 上是硬失败 ⇒ 同文件内的处置不自洽。方向：偏绿。
- 定级：S2（缺生产触发路径，理由同 CR-55-11）
- 怎么算修好：:916 拆成两支——`kind == '0'`（null）⇒ 缺席；其余非 `'s'` ⇒ `efill(..., "manifest master plane must be a base64 string or null", 110)`；同时在 `build_out_manifest` 为 OPK_CALIBRATE 增加 `masters_applied`（bit 或字符串数组）与 `flat_applied/bias_applied/dark_applied`，把 §8 点名的登记义务落到键上。**必须同批改**：integration 合同 :126 的 out manifest 键集与 `eng/tests/unit/calibration_integration_test.cpp` 里对 manifest 文本的钉值断言（改键集必同时改期望，否则改对反而判红）。
- 置信：CONFIRMED（同函数内三处严格度对照已逐行读；键集见 :794-826 全文）

### CR-55-13 · 迷你 JSON 词法的数字支路接受非 JSON 语法（`1e`、`1-1`、`+5`），`cfg_check` 用 `sscanf` 而 `kv_f64` 用 `strtod`，两道的判据不同 ⇒ 畸形数字被静默折成前缀值
- 轴：R
- 位点：`module_entry.cpp:267-285`（`jc_number`）与 :437-444（`cfg_check` 的 `std::sscanf(kvs[i].val, "%lf", &d);`）与 :468-480（`kv_f64` 的 `std::strtod`）
- 上位依据：文件头 :209 自述"JSON 配置解析 (**迷你严格解析器**; 15-键词表, 15 §10 冻结禁止)"；:214-215 自述"重复键 → PARAM + CONFIG_SCHEMA。unknown 键 → PARAM + CONFIG_SCHEMA"⇒ 该文件把"严格"作为自我声明的性质。
- 现状：`jc_number` 的字符集把 `. e E - +` 一律当作可继续的 token 字符（:276-279），只要求 `digits>=1 && dots<=1` ⇒ `1e`、`1-1`、`5+2` 都成为合法 token；`cfg_check` 只用 `sscanf("%lf")` 判有限性（`sscanf("1-1")→1.0`、`sscanf("1e")→1.0` 都有限 ⇒ 放行）；`kv_f64` 的 `strtod` 取最长合法前缀（`1-1 → 1.0`、`1e → 1.0`，`end != buf` ⇒ 返回 0 成功）。`kv_u64`（:449-466）反倒逐字符严格 ⇒ 同一解析器对整型严格、对浮点宽松。
- 差在哪："严格解析器"的浮点支路会把语法非法的串解析成另一个数（而不是判红），且两道门用的是两个不同的转换函数 ⇒ 校验面与取值面对同一 token 的结论可以不一致（校验看 `sscanf`、取值看 `strtod`）。
- 后果：手改配置里的一处笔误（`sigma_high: 3-3`、`dark_scale_factor: 1e`）会静默变成一个"看起来对"的值（3.0 / 1.0）并进入像素算术——`dark_scale_factor` 尤重，它正是 CR-55-11 的同一根键。方向：偏绿（不判红且值被改写）。
- 定级：S3（触发需配置文本本身畸形＋本 DLL 零生产调用方；缺陷本身 CONFIRMED）
- 怎么算修好：`jc_number` 按 RFC 8259 文法收严（`int frac exp` 三段，指数段必须至少一位数字，首字符禁 `+`）；`kv_f64` 判 `*end == '\0'` 且拒绝 `errno == ERANGE`；`cfg_check` 与 `kv_f64` 复用同一转换（消除两道门不同源）。**同批**：补一条注入用例 `{"sigma_low":1e}`、`{"dark_scale_factor":1-1}` 必须 103 判红（当前两者均判绿）。
- 置信：CONFIRMED（两个转换函数对同一 token 的行为按 C 标准前缀语义与两行源码推得；未跑构建）

### CR-55-21 · `generate_master_flat` 两个 op 被实现要求 `combine`，而三处登记面都写"flat 无 combine 参数"——按合同写的合法配置被判红
- 轴：C
- 位点：`module_entry.cpp:667-673`
  ```
          const char* cv = NULL;
          uint32_t cl = 0;
          kv_str(kvs, nkv, "combine", &cv, &cl);
          const int cc = parse_combine(cv, cl);
          if (cc < 0)
              return efill(err, ACS_ERR_PARAM, ACS_ERR_DOMAIN_CONFIG,
                           "combine must be \"mean\"|\"median\"", 103);
  ```
  该段整体位于 `if (e->kind == OPK_MASTER) {`（:650）之内，**没有** `if (!e->is_flat)` 排除。
- 上位依据（三处，全部说"flat 不需要 combine"）：
  1. 同文件 :529-530 注释与 :541 代码：`op_required_keys` 里 `if (!e->is_flat) { keys[n] = "combine"; kinds[n++] = 's'; }`，注释原文"flat 的 legacy 签名无 combine → **不要求**"；
  2. `README.md §5.1:102-103`："master 生成：`sigma_low/sigma_high/max_iterations/combine`（combine∈{0=mean,1=median}；**master flat 无 combine 参数**）"；
  3. `docs/algorithms/CALIBRATION_ALGORITHMS.md` ALG-CAL-002 :114-115："源码锚: `master_generator.cpp:178-295`。**无 combine 参数**，合并固定 mean"。
  另加 `astro_calibration.h:80-84` 的 `ac_generate_master_flat` 签名里确实没有 `combine` 形参。
- 现状：`cfg_fill` 的必需键循环（:633-639）按 `op_required_keys` 免除 flat 的 combine，但随后 :669 无条件解析并判红 ⇒ 对 `generate_master_flat` / `generate_master_flat_f64` 两个 op，**声明为可选的键实际是必需键**；若调用方给了 `combine`，它被解析后又在 :986-990 被丢弃（flat 分支不传 combine）＝"必须给但给了没用"。
- 差在哪：同一模块内两份代码（:541 的必需键表 vs :669 的解析）对同一键的必要性各说一种，且三处对外登记面都站在 :541 一侧；合同拒绝的形态（flat 不带 combine 的配置）被实现判红＝两个入口对同一判定取不同值那一族。
- 后果：任何照 README §5.1/ALG-CAL-002 写出的 `generate_master_flat` 配置在 `validate_config`/`create`/`plan` 三步全部判 103，**该 op 在合同规定的写法下完全不可用**；因为判红是 fail-closed，不产出错值，但"10 个科学 op 全部可用"（module.yaml ops 列表 :33,:36 登记 10 op）这句对外主张对其中 2 个不成立。方向：偏红。
- 定级：S3（无生产调用方；且方向是拒绝而非误放）
- 怎么算修好：把 :667-674 整段包进 `if (!e->is_flat) { … }`（与 :541 同源）；**必须同批改**：`eng/tests/unit/calibration_integration_test.cpp` 当前对该键缺失的期望是 101（`ASTROCS_CAL_ECODE_PARAM_MISSING`，:692-699 B5），改后 flat 分支的缺失语义要与 bias/dark 分支区分开——补两条用例：`{"op":"generate_master_flat", …}` 不带 combine ⇒ ACS_OK；带 `"combine":"median"` ⇒ 仍 ACS_OK 且 out manifest 不因此改变（钉住"不发明参数"）。另建议给 :644/:648 的 width/height 上界违规换一条与"range"相符的消息（现文案 "must be positive integer" 描述的是类型而非范围）。
- 置信：CONFIRMED（两份代码路径与三处登记面原文均逐字比对；全仓零用例走过 flat 配置面的检索式：`git grep -n '"op\\":\\"generate_master_flat' -- eng lib` ⇒ 0 命中）

## 不报的核过项（module_entry.cpp）

| 核过项 | 为什么不是缺陷 | 检索式/依据 |
|---|---|---|
| master/calibrate/correct 各键的"缺键兜底值"（派单点名的多侧默认高发面） | **本文件对必需键不设默认**：`op_required_keys`(:531-554) 列必需键、:633-639 缺键即 101 判红；`cfg_fill` 开头 `std::memset(c, 0, sizeof(*c))`(:628) 只在"已过必需键校验"之后发生 ⇒ 兜底 0 值不可能进算术（除 `max_workers`，其 0 有显式"未给"语义，见处置表 :711 行）。检索式：`git grep -n "sigma_low\|sigma_high\|master_column_sigma" -- eng/packaging/config` 与 `git grep -n "\"combine\"" -- eng/packaging/config` ⇒ **defaults.json/config_registry.json 对这四个键零登记**：不是"多侧不一致"而是"只有一侧、且该侧必填" | `git grep -n "sigma_low" -- eng` |
| `generate_master_flat` 的 `combine` 被实现要求而三处登记面说不要求 | **成立，已升为 finding CR-55-21**（列在本节 findings 末）——它不是"两值不一致"而是"合同说可选、实现判必需"，且全仓零用例走过 flat 的配置面，故从未暴露；我最初把它放在本表是错判轻重，改立条以免被"不报项"埋掉 | 见 CR-55-21 |
| `rc != AC_OK` 的 legacy 失败映射 | :1053-1057 一律 `ACS_ERR_PARAM + domain BACKEND + detail 120`，不吞错也不降级 ⇒ 无 fail-open | 直读 :1047-1057 |
| 异常屏障（文件头 :17-20 承诺九回调全包裹） | 九个回调（describe/validate_config/plan/create/execute/inspect/request_cancel/destroy 中除 destroy 外 8 个 `try/catch(...)`；destroy 是 `void` 且内部只调 host free ⇒ 无可抛点）⇒ 承诺与实现相符，无空承诺 | 直读 :1076-1494 |
| out_plane 零初始化 | `bytes out_plane(plane_bytes)`(:957) 值初始化 ⇒ 若 legacy 未写则出全零平面并可能 rc==AC_OK（见处置表 :837 行与 master_generator 不报项末行）——该后果的前提是 DISP-CAL-009（`npix` int32 溢出），已登记 ⇒ 不另立 finding，只登记"零初始化把已登记缺陷的表现改写成'全零母版 + 成功'" | 直读 :957 + ALG-CAL §10 DISP-CAL-009 |
| base64 严格解码（:122 注释"严格解码"） | `b64_decode_len` 对空白/补齐位置/孤立单字符组逐条判 -1，且 :745 与 :768 双判解码长度==平面字节 ⇒ 无静默截断 | 直读 :123-171,744-752 |
| `want < 1 ⇒ want = 1` 的线程租约下界 | :942-944 先取 `ex->max_workers ?: available_cpus`，只在 `c.max_workers>0 且更小` 时才裁剪 ⇒ 0 是"未给"而非"1 核"；且 acquire 失败硬判 BUDGET（:950-952），不降级单线程（文件头 :29-32 与 :929-931 同口径） | 直读 :929-954 |

---

# 5 lib/algorithms/calibration/src/v6_calibration_covariance.cpp —— 已读：是（表 15 项）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| v6_calibration_covariance.cpp:64 | `kFlatFloor` | 0.1 | flat 归一除法下限 | 文献值（合同冻结） | 机器登记 `eng/contracts/data/v6_clause_registry_v1.json` `FZ-CAL-FLOOR` value `0.1` unit `1` status **FROZEN**，anchor "SCI-CAL-001（原样继承）"；本体式 SCI-CAL §5 `max(flat,0.1)` | — | CONFIRMED（注：该 clause 的 `source_binding` 指向 `docs/contracts/DATA_SEMANTICS.md` 子串 `max(f_p, 0.1)`——`git grep -n "max(f_p, 0.1)" -- docs` **零命中** ⇒ 登记锚自身失效，属 registry 面缺陷，不在本批立 finding，供 UNRESOLVED 复算） |
| :66 | `kSharedDetectionEps` | 1e-9 | 共享门 `ratio > 1+eps` 的检出阈 | **待确认** | 条文面零提及：`v6_calibration_covariance.h:258` 与 `V6_CALIBRATION_COVARIANCE.md:32` 都只写"joint/naive>1"，无 eps；registry 检索式 `git grep -n "1e-9\|joint/naive" -- eng/contracts/data/v6_clause_registry_v1.json` 命中 `FZ-AP1-GLS-QW-RTOL`(值 1e-9，另一用途) ⇒ 无本门专属登记 | 方向：**eps 越大越严**（更多判红）⇒ 现值是最松一侧，但没有推导；影响范围＝共享系统项是否被"检出" | PARTIAL（eps 数值无锚，但 1e-9 与浮点噪声同量级 ⇒ 若它只作数值容差用，影响限于 shared_variance 恰好为 0 的退化档；该档的真实后果在 CR-55-16） |
| :117,122,127 | `v_single_frame/N_combined`；`< 0.0 / <= 0` 判定；`-1.0` | — | 母版自方差 σ²/N 与定义域 | 公式导出 | 头 :92-93 "V(master)=V_single_frame/N_combined + 已声明共同模式项"；V6 §1 行 OI-02；CALIBRATION.md §14a"母版由有限帧数合并产生，其自身不确定度（σ²/N_master）"⇒ 均值型估计量的方差律（教科书级）；`-1` 是"不可判"哨兵（:104 声明），非科学量 | — | CONFIRMED |
| :146 | 缺省 `q_adu = 1.0` | 1.0 ADU | 未声明 ADC 量化时的缺省量子 | 文献值（合同冻结） | registry `FZ-CAL-QUANTUM-DEFAULT` value `1` unit `ADU` status **FROZEN**，fail_closed "量化项未声明 q_adu → 缺省 1 ADU 但必须声明"；头 :87-88 与 :14「缺省 1 ADU 并置 quantum_default_applied」同 | — | CONFIRMED |
| :147 | `q_adu * q_adu / 12.0` | 12 | 理想 ADC 均匀量化噪声方差 Δ²/12 | 公式导出 | 均匀分布 `U(−q/2,q/2)` 的二阶矩＝q²/12；本仓式面＝V6 §1 行 `FZ-CAL-QUANTUM-DEFAULT` 的 `V_q=q_adu²/12`；文献池候选 CALIBRATION.md §14a（Janesick 2001 Ch.2 / Howell 2006 Ch.4）**未逐式定位该常数** | — | CONFIRMED（式正确；文献定位仅到"教科书级"） |
| :153-157 | `(read_noise_e/gain)²`；`max(r,0)/gain`；`max(d,0)/gain` | 2 次幂 / 除 gain | 读出噪声与光子噪声的 ADU² 换算 | 公式导出 | 头 :13-15 同式；V6 §1 行 `FZ-UNITS 独立项`；物理：`N_e = ADU·gain`，`Var[e⁻]=N_e` ⇒ `Var[ADU]=gain·N_adu/gain²=N/gain`；本仓 NOISE_MODEL 侧同族式（未逐行核对，见置信列） | — | PARTIAL（未核 docs/science/NOISE_MODEL.md 是否给同一式；若给则升 CONFIRMED） |
| :216 | `px.flat < kFlatFloor` | 见 0.1 行 | floor 触发判定 | 文献值 | 同 :64 行 | — | CONFIRMED |
| :210,266,271 | `px.flat <= 0`；`gain <= 0`；`q_adu <= 0` | 0 | 三条定义域拒绝 | 公式导出 | 头 :20-22 fail-closed 清单；V6 §2 表；`f≤0 → REJECT`＝FZ-CAL-FLOOR 的 fail_closed 列 | — | CONFIRMED |
| :260 | `px.empirical_variance < 0.0` | 0 | 经验方差下界 | 公式导出 | 方差非负（定义）；头 :23 fallback 行 | — | CONFIRMED |
| :283-297 | `y` 的两分支式；`alpha` | — | 校准信号 | **公式导出，且与公共头声明不同一套数 ⇒ CR-55-14** | 正本 SCI-CAL-001 §5（标准式含 `bias·[bias≠NULL]` 与 `K·dark`）＋DATA_SEMANTICS:194（两分支都施加 K，"强制 K=1.0 属缺陷 DISP-CAL-012"）——**代码与正本一致，头与 V6 §1 与正本不一致** | — | CONFIRMED |
| :324,327 | `-(1-α)/f`（折叠）与 `-1/f`、`+α/f`（不折叠） | — | bias 方向导数 | 公式导出 | 头 :11 只给折叠式；不折叠式由 OI-02（registry open_items：`DESIGN-P1 §4.2 的 V_b + α²V_b 与同 master_id 的 (1−α)² 合并关系`）导出 ⇒ **式对，但 4 槽 Jacobian 装不下不折叠支路的第三个系数 ⇒ CR-55-15** | — | CONFIRMED |
| :336-338 | `if (flat_used && !out.flat_floor_applied) j_f = -y/px.flat;` | — | ∂y/∂f（floor 区取 0） | 公式导出 | `y=num/f ⇒ ∂y/∂f=−y/f`；floor 区 `max(f,0.1)` 对 f 导数为 0 ⇒ 与 FZ-CAL-FLOOR 的"floor 区不可由后续标度恢复"（SCI §5）同向。**注意**：floor 区把 flat 的不确定度也一并归零 ⇒ 已在 CALIBRATION.md §5 以"欠校正、单边偏低"登记，属条文承认的已知偏差，不另立案 | — | CONFIRMED |
| :390-420 | `v_cal` 逐项累加；`v_cal_shared_diagonal = v_cal − v_independent` | — | 对角二次型与三分 | 公式导出 | FZ-FORMULA-COV-PROP `C_out = R C_in Rᵀ`（registry FROZEN）＋头 :191-200 的三分声明 | — | CONFIRMED |
| :534,598-601 | `res.naive_variance = (naive<0)?0:naive`；`ratio = naive>0 ? joint/naive : infinity`；`1+eps` | 0 / inf | 取不到值时的缺省读数与比值 | **待确认（判红 ⇒ CR-55-16）** | 头 :257-265 只声明"共享项存在时要求 joint/naive>1+1e-9"，未声明"-1 错误码如何处置"；AGENTS.md §5"判据必须非退化…恒真门没有证据资格" | 保守方向：`naive<0`（尺寸不符的错误哨兵）与 `naive==0` 必须显式判 unavailable，不得折成 0/inf | CONFIRMED |
| :298-303 | `y = 0.0`（clip_negative）；`y += 100.0`（add_pedestal） | 0 / 100 | 故障注入支路的写死值 | 结构性不适用（测试自订注入值，且 :10 注释登记先例 ASTROCS_RT001_FAULT）⇒ 但**注入通道的存在方式本身**见 CR-55-19 | V6 §5 "故障注入钩子仅为测试用；未设置时行为严格正确" | — | CONFIRMED |

## findings

### CR-55-14 · 公共头声明的 `dark_opt=0` 信号式与本实现、与两份正本**不是同一套数**（漏 bias、漏 K），实现侧正确而声明侧是被撤销过的旧缺陷
- 轴：C（主）/ S
- 位点：`lib/algorithms/calibration/include/astrocs/calibration/v6_calibration_covariance.h:8`
  ```
   * 信号:   dark_opt=0  y_p = (r_p - d_p) / max(f_p, 0.1)          [无 flat 不除法]
  ```
  对照 `src/v6_calibration_covariance.cpp:292-297`：
  ```
    } else {
      /* 标准式（BIAS-001）：master_dark 已减 bias -> y = (r - b - alpha*d) / f */
      const double d = px.has_dark ? px.dark : 0.0;
      const double b = px.has_bias_light ? px.bias_light : 0.0;
      y = (px.r - b - cfg.alpha * d) / denom;
  ```
  同一旧式还在 `V6_CALIBRATION_COVARIANCE.md:12` 以"权威锚（冻结，逐条符合）"名义重复一遍，并同段把 J 写成单一 `J=[1/f,−(1−α)/f,−α/f,−y/f]`（头 :11 同），而实现按分支给两套（cpp:306-309 注释＋:322-332 代码）。
- 上位依据（三份，全部站在实现一侧）：
  1. SCI-CAL-001 §5："dark_opt=0 (默认, **标准式**；master_dark 已减 bias): `cal = (raw − bias·[bias≠NULL] − K·dark·[dark≠NULL]) / max(flat, 0.1)`"，并同节："**两个分支都必须施加同一个 K**（`calibrator.cpp:122,144`…），模块层不设 K 默认值"；
  2. `docs/contracts/DATA_SEMANTICS.md:194`："K（dark_scale_factor）…**标准式（dark_opt=0）与兼容式（dark_opt=1）都施加**（BIAS-001）；**标准分支同样施加该 K**（**强制 K=1.0 属缺陷，DISP-CAL-012**）"；
  3. SCI-CAL-001 §7 不变量："bias 参与不变量：`dark==NULL` 时，提供 bias 与不提供 bias 的 cal 逐像素差恒为 bias/max(flat,0.1)；bias≠0 时该差必须非零"＋"K 参与不变量"。
- 现状：实现（cpp）＝正本；公共头＋同模块说明文档＝**订正前的旧式**，且头 :24 写着"权威锚（冻结，不得偏离）"、V6 §1 表格标题写着"（冻结，逐条符合）"。
- 差在哪：公共头是集成者的唯一必读面，它现在冻结的是一条**已被机器条款点名为缺陷**的式子（DISP-CAL-012）；按头实现就会把标准式的 bias 与 K 一起丢掉，而 `calibrate_pixel` 的实现反而是对的 ⇒ 声明面与实现面互为反例。
- 后果：(a) 任何按头 :8 写第二实现/Oracle 的读者会得到"标准式 bias 不参与、K≡1"的产物 ⇒ 与 SCI §11"bias 参与门/K 参与门"直接冲突（该门一判红即销毁支撑度）；(b) V6 §1 表格是这份实现对外署名的"逐条符合"证据件，其中两行（§2.1 信号、§2.2 线性化）与实现不符 ⇒ 该对外主张不成立。
- 定级：S2（**缺生产触发路径**：被点名的一侧是注释/文档面，不进入字节；`lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:367` 调的是实现，故产物按实现走）
- 怎么算修好：把头 :8 改为 `dark_opt=0 → y=(r − b·[has_bias_light] − α·d·[has_dark])/max(f,0.1)`，:11 的 J 改成两分支各一套（照 cpp:307-309 抄），并同步 `V6_CALIBRATION_COVARIANCE.md` §1 前两行；**必须同批改**：`eng/tests/unit/v6_p1_cal/v6_cal_oracle.hpp` 若按头式实现 `analytic_jacobian`，需核对它实际编码的是哪一套（测试 :121-124 用它反断言 `r.jacobian`，若 Oracle 也停在旧式则该用例当前必红——它当前绿，说明 Oracle 用的是新式，则更坐实"只有头/文档落后"）。
- 置信：CONFIRMED（三份条文原文逐字、实现原文逐字、比对完成）

### CR-55-15 · 非同 master 折叠时第 5 个系数（`+α/f` 的暗路 bias）被算进 v_cal 却进不了 4 槽 Jacobian ⇒ 对外的 covariance 记录无法复现它自己声明的方差
- 轴：R（主）/ C
- 位点：`v6_calibration_covariance.cpp:341` `jac[1] = same_bias_master ? j_b_folded : j_b_light;`（`j_b_dark` 在 :327 算出后**再无写入点**）；:403-404
  ```
        v_cal += j_b_light * j_b_light * v_b_light;
        v_cal += j_b_dark * j_b_dark * v_b_dark;
  ```
  发布面 :640 `rec.combination_coefficients.assign(result.jacobian, result.jacobian + 4);`
- 上位依据：registry `FZ-FORMULA-COV-PROP`（**FROZEN**）："value: `C_out = R C_in R^T`; 标量 `c^T C_in c`"、"gate: 禁止从权重标量反推 variance"、"fail_closed: … 或 **C_out≠R C_in R^T → REJECT**"；头 :282 的默认 `variance_from = "actual_combination_coefficients"`（字面主张＝方差来自**实际**组合系数）；OI-02（registry open_items，"DESIGN-P1 §4.2 的 V_b + α²V_b 与同 master_id 的 (1−α)² 合并关系"，status **OPEN**）＝该双项形态被显式承认为要支持的输入。
- 现状：`CalResult::jacobian[4]`（头 :203，语义 `[dr, db, dd, df]`）只有 4 槽；`dark_opt=1` 且两个 bias master 的 `master_id` 不同时，实际参与方差的偏导有 **5 个**（`j_r, j_b_light, j_b_dark, j_d, j_f`），发布的 4 向量丢掉 `j_b_dark`。`diagonal_quadratic_variance`（:187-192，头 :220-223 声明"供 Oracle 交叉校验"）同样只有 4 个方差参数 ⇒ 无法表达该形态。测试面 `v6_cal_covariance_test.cpp:212-229` 的 `test_distinct_master` 把 bias 项写成 **残差**（`v_cal − 已知三项`）再与 `design` 比，从不检查 `jac[1]` 或记录的系数能否复现 `v_cal` ⇒ 该缺陷对现有门不可见。
- 差在哪：记录的 `variance_from=actual_combination_coefficients` 与 `C_out=R C_in Rᵀ` 这条 FROZEN 恒等式在双 master 输入下**不成立**（发布出去的 R 少一项），而 `validate_covariance_record`（:697-741）只做词表与枚举的形状校验，从不比对 `J C Jᵀ` 与 `v_cal`，故它对该记录返回 true（`phase1_product.cpp:376-379` 正是这样放行的）。
- 后果：按记录的 4 系数复算方差的独立读者/下游产品会得到 `v_cal − α²V_b/f²` ⇒ **方差系统性偏小、ivar 偏大、SNR 偏乐观**，正是 FZ-PROV-SHARED-SYSTEMATIC/ADJ-OBS-01 想防的方向；创新点②（跨帧绝对 SNR）的可信度面受损。方向已钉死：丢一项 ⇒ 复算值只会更小 ⇒ 偏绿。
- 定级：S2（触发链两跳已逐跳写出 `phase1_product.cpp:367 → :374 → :376`，但 v6_phase1 集成件本身是否被 `normalize` 生产入口调用我未核 ⇒ 不满足 S1 的"从生产入口"逐跳门槛）
- 怎么算修好：三条同批——(1) `CalResult::jacobian` 扩成 5 槽（或在 `MasterIdRecord`/`CovarianceRecord` 里并列携带 bias_dark 系数），使 R 完备；(2) `make_calibration_covariance_record` 的 `combination_coefficients` 与 `v_cal` 加一条自等断言：`|Σ_j c_j²V_j − v_cal| ≤ tol·v_cal` 否则 `avail=unavailable`；(3) 把 `test_distinct_master` 的残差式断言改成"记录可复现"断言。**必须同批改**：`covariance.v1` 产品 schema 的 `combination_coefficients` 是否有长度/语义约束（`eng/contracts/schemas/product_family_field_constraints.schema.json#/$defs/covariance`，头 :290-291 点名），扩槽位要与该 schema 同批，否则改对反而被产品门判红；OI-02 仍是 OPEN ⇒ 若负责人裁"双 bias master 不支持"，则 (1) 改为显式 `kUnavailable` 而非扩槽——**这条要先问，不能自己选**。
- 置信：CONFIRMED（:341/:403-404/:640/:222-229 四处原文比对；未跑测试）

### CR-55-16 · `evaluate_shared_gate` 把"尺寸不符"的 -1 错误值折成 0、把"独立方差为 0"折成 `ratio=∞` ⇒ 共享门在这两类退化输入上**判绿**
- 轴：R
- 位点：`v6_calibration_covariance.cpp:533-536`
  ```
      const double naive = naive_diagonal_variance(c, diagonal_variance);
      res.naive_variance = (naive < 0.0) ? 0.0 : naive;
  ```
  与 :598-601
  ```
      res.ratio = (res.naive_variance > 0.0)
                      ? res.joint_variance / res.naive_variance
                      : std::numeric_limits<double>::infinity();
      if (!(res.ratio > 1.0 + kSharedDetectionEps)) {
  ```
- 上位依据：头 :239-240 `naive_diagonal_variance` 的契约（同族 :467/:477/:498 三处 `return -1.0` 都是"输入尺寸/符号非法"的**错误哨兵**，见 :479 `if (… != L.size()) return -1.0;`）；AGENTS.md §5"判据必须非退化：真值无效应时度量归零或判红；恒真门没有证据资格"；registry `FZ-PROV-SHARED-SYSTEMATIC`（FROZEN）"gate: 当独立项处理即 REJECT(ratio>1 须检出)"。
- 现状：错误值 −1 被 :534 无条件夹到 0.0（错误信号消失）；随后 `naive_variance==0` 时 :600 直接把 ratio 写成 `+inf`，`inf > 1+eps` 成立 ⇒ `res.ok` 保持 true、`reason` 保持 `kNone`。
- 差在哪："取不到值就落到合法好值一侧"的教科书形态——两件事叠加：① 尺寸不符（本应判不可用）被伪装成"独立方差为 0"；② 独立方差为 0 时"联合比独立大"被自动判真，而此刻 `joint` 可以恰为 0（`c` 全零、或 L 与 c 正交、或 `master_variance=0`）⇒ **0 > 1 的门在 0/0 场景恒真**。
- 后果：FZ-PROV-SHARED-SYSTEMATIC 点名的"共享项按独立处理即 REJECT"这条 fail-closed 在最需要它的退化输入（未填方差/系数向量长度错）上不触发 ⇒ 产品面可携带 `representation=low_rank_factors` 而 `v_cal=0` 的记录被门放行；下游 ivar=1/0 ⇒ 稠密 SNR 重建把该像素当无限可靠。方向：偏绿（已把不等式方向钉死：门要求 ratio>1；ratio 被伪装成 +∞ 而非报错 ⇒ 只会放过、不会误杀）。
- 定级：S2（缺生产触发路径：`git grep -n "evaluate_shared_gate" -- lib` 除本实现与头之外**零调用方**，`phase1_product.cpp` 只调 `calibrate_pixel`/`make_calibration_covariance_record`）
- 怎么算修好：:533-536 改为 `if (naive < 0.0) { res.ok=false; res.reason=CalReason::kInvalidConfig; return res; }`（尺寸/符号错误显式失败，不夹 0）；:598-600 改为 `naive <= 0` 时**不得**判 ok（`res.ok=false; reason=kSharedNotDetected`，或新增 `kNaiveNotAvailable` 并映射到 unavailable）；**必须同批改**：现有单测里以"空 c/空 L"取绿的用例（若有）需改判红——改前先看 `eng/tests/unit/v6_p1_cal/v6_cal_covariance_test.cpp` 共享门段与 8 条负向注入清单，避免把"门更严"误当成回归；`kSharedDetectionEps` 的 1e-9 也要同批登记进 registry（处置表 :66 行），否则改完仍是无锚数。
- 置信：CONFIRMED（两支不等式与 −1 语义按原文与同文件另三处 `-1.0` 约定互证）

### CR-55-17 · 共享通道载荷的非有限值不判：`master_variance=+Inf` 会带着 `status=kOk` 进入方差与记录
- 轴：R
- 位点：`v6_calibration_covariance.cpp:178-181`
  ```
      case SharedRepresentationKind::kCommonMasterId:
        return !common_master.master_id.empty() &&
               common_master.master_variance >= 0.0 && is_finite(common_master.alpha_m);
  ```
  同类漏点：:172-173（`kLowRank` 只判 `rank>0 && !factor.empty()`，不判 `factor` 元素有限性）、:145（`system_error_budget_variance` 全函数无任何判定）、:565-566 与 :587 的消费点。
- 上位依据：头 :20-22 的 fail-closed 清单把 `f<=0 **或非有限** → REJECT` 立为本模块的通则（对 flat 一路）；SCI-CAL-001 §4"数值: 输入含 NaN 时…输出不传播 NaN 为伪有效值"；CALIBRATION.md §8 各行"不静默"。
- 现状：`structurally_representable()` 的三条通道各自只判"字段非空 / rank>0 / scale>0"，**唯一的 isfinite 用在 alpha_m 上**；于是 `master_variance = +Inf`（或 L 含 NaN、预算含 NaN）被判"可表示"，:565/:587 把 Inf/NaN 直接算进 `shared_variance`，:597 得 `joint_variance=Inf`，:601 `Inf > 1+eps` ⇒ 门 ok；`calibrate_pixel` 侧 :418 `out.v_cal = v_cal`（可含 Inf），:448 `status = kOk`，:641 `avail="available"`。
- 差在哪：同一条"非有限即拒"的原则在 flat 上实现了、在共享通道上没实现；声明的可表示性判据只覆盖存在性，不覆盖数值合法性。
- 后果：产品面出现 `variance=ADU²` 名义下取值为 Inf/NaN 且标记 available 的记录 ⇒ ivar=0（像素被无声剔除）或 NaN（整帧传染），且没有任何 unavailable 理由；与头 :198-200 的"三分"承诺（`v_cal = v_cal_independent + v_cal_shared_diagonal`）在 Inf 下退化（Inf−Inf=NaN，:420）。方向：偏绿（available 且过门）。
- 定级：S2（生产触发路径同样止于 `phase1_product.cpp`，该件是否被 normalize 调未核）
- 怎么算修好：`structurally_representable()` 三 case 各加有限性判定（`is_finite(master_variance)`、`std::all_of(factor)`、`is_finite(scale)`、预算 `is_finite(system_error_budget_variance)` 且 `>=0`）；`calibrate_pixel` 在 :418 之前加"结果方差必须有限，否则 kRejected/kNonFiniteInput"；**必须同批改**：新增的 unavailable→记录映射要过 `is_allowed_unavailable_reason` 白名单（:41-46），否则 `validate_covariance_record` 会因白名单不含新理由而判红——这是"改对反而判红"的典型点。
- 置信：CONFIRMED（判定面与消费点逐行读完；Inf 传播按 IEEE 语义）

### CR-55-18 · 缺身份诊断把"哪一路 master 缺"这一参数显式丢弃，unavailable 面不留任何可核对痕迹
- 轴：D
- 位点：`v6_calibration_covariance.cpp:231-239`
  ```
    auto require_identity = [&out](const MasterIdentity& m, const char* role) -> bool {
      if (!m.identity_complete()) {
        out.status = CalStatus::kUnavailable;
        out.reason = CalReason::kMissingMasterIdentity;
        (void)role;
        return false;
  ```
- 上位依据：SCI-CAL-001 §8 表对 fail-closed 的同一仓口径："**结构化诊断点名文件** + 观测中位数 + 缺失声明项"；§11(a)"U1…结构化诊断点名文件"；本结构体自带承载位——头 :179 `MasterIdRecord::role /* bias_light | bias_dark | dark | flat */`；:240-243 四个调用点分别传 `"bias_light" / "bias_dark" / "dark" / "flat"`。
- 现状：`role` 被 `(void)` 丢弃；且该路径在 :424-445 的 `push_master` 之前就 return ⇒ `out.master_ids` 为空，连"哪一路"都不在结果里；`CalReason` 枚举（头 :64-73）也只有一个 `kMissingMasterIdentity` 值，无法区分。`make_calibration_covariance_record` :667-670 把 `kMissingGainOrReadNoise` 与 `kMissingMasterIdentity` **折叠成同一个** `input_covariance_missing`。
- 差在哪：为这条义务准备的参数与结构字段都存在，却在实现里既不落地也不外传；两次折叠（role→无、两 reason→一词）使产品面的 unavailable 理由不指认任何一路 master。
- 后果：读方只能知道"这台机器缺一点身份声明"，不能知道缺的是 bias 还是 flat ⇒ 与 §8 要求的"点名缺失声明项"不符；调查成本落在人工逐帧回放上，且该缺陷不会被任何判据抓到（无断言可比"点名"）。
- 定级：S3（不改变产品字节，改变的是可核对性；无生产触发路径需求）
- 怎么算修好：`CalReason` 增 `kMissingMasterIdentityBias/Dark/Flat`（或 `CalResult` 增 `std::string missing_identity_role` 并填 role），`make_calibration_covariance_record` 的理由白名单同步（:41-46 与 `covariance.v1` schema 的枚举同批，防改对判红）；**别只改一处**：`unavailable_reason` 白名单与产品 schema 是两侧，改一侧必然另一侧红。
- 置信：CONFIRMED

### CR-55-19 · 改变科学语义的环境变量支路编在生产 TU 里，且不留任何产品面痕迹（每像素 4 次 getenv）
- 轴：R（主）/ D
- 位点：`v6_calibration_covariance.cpp:26-30`
  ```
  std::string fault_mode() {
    const char* v = std::getenv("ASTROCS_V6_CAL_FAULT");
    return (v != nullptr) ? std::string(v) : std::string();
  }
  ```
  被 `calibrate_pixel` 每像素调用 4 次：:216 `fault_is("remove_floor")`、:298 `clip_negative`、:301 `add_pedestal`、:347→:148 `quantum_zero`；另有 :546 `drop_shared`、:616 `variance_unit_not_squared`、:658 `drop_representation`。
- 上位依据：V6 §5 "故障注入钩子 `ASTROCS_V6_CAL_FAULT` 仅为测试用；未设置时行为严格正确"（**已声明**，故我不把它当"隐藏后门"报）；AGENTS.md §6 硬禁令"不串三阶段/不硬编码线程/ISA/block：由 benchmark 生成的 profile 决定"——本仓对"运行期改变科学语义"的既有口径是必须可审计（对照 :10 注释引用的先例 `ASTROCS_RT001_FAULT`）。
- 现状：语义反转支路（`clip_negative`＝把负值裁成 0、`remove_floor`＝撤掉 FZ-CAL-FLOOR 的 0.1、`add_pedestal`＝整帧 +100 ADU）**常驻在生产目标里**，开关是一个进程环境变量；`CalResult`/`CovarianceRecord` 没有任何字段记录"本次是否处于故障模式"（:187-211 全字段读完），`calibration_covariance_contract_id()`/`fault_injection_env_var()` 只回字符串。
- 差在哪：一条能整帧改写像素与方差的路径，其激活状态在产物里不可见 ⇒ "未设置时行为严格正确"这句只能靠运行环境担保，不能由产品自证；同一份产品在两种 env 下字节不同而记录相同。附带成本：`getenv`＋`std::string` 构造×4/像素（稠密帧即数十亿次分配）。
- 后果：任何一次 env 泄漏（CI runner、宿主 shell、`ASTROCS_*` 前缀习惯）都会静默产出被裁剪/加了 pedestal 的 `cal` 与方差，而 provenance 显示正常 ⇒ 使"CAL-NO-CLIP：信号与方差通路无 clamp、无 pedestal"（V6 §1 行）这一对外主张失去可核对性。方向：偏绿（无任何红灯）。
- 定级：S2（生产触发路径未通到 CLI 入口：本模块"未接线 Phase session"（V6 :4），但 `phase1_product.cpp:367` 已消费 ⇒ 一旦该件进 normalize，本项即升 S1）
- 怎么算修好：把钩子移出生产编译单元——用 `#ifdef ASTROCS_V6_CAL_FAULT_INJECTION`（仅测试 target 定义）包住 :26-30 与 7 个调用点，或把注入做成**测试侧的子类/回调**；同时（若保留运行期开关）在 `CalResult` 增 `active_fault_mode` 并写进 `CovarianceRecord`＋白名单，使产品面能自证。**必须同批改**：`eng/tests/unit/v6_p1_cal/v6_cal_covariance_test.cpp` 的 8 条负向注入靠 env 驱动，改成编译期开关需同批把该测试挂到定义了开关的 target 上（`eng/tests/unit/v6_p1_cal/CMakeLists.txt`），否则注入用例全部失去效力而仍然判绿——这是本项最容易被改错的地方。
- 置信：CONFIRMED

## 不报的核过项（v6_calibration_covariance.cpp）

| 核过项 | 为什么不是缺陷 | 检索式/依据 |
|---|---|---|
| `is_forbidden_variance_source` 收了 `combination_coefficients`/`linear_combination_coefficients` 两个同义值，比头 :282 的单一 "actual_…" 宽 | 白名单面（:48-52）与禁止名单（:679-695）互不冲突：三个允许值都不在禁止名单里，故不存在"合同拒绝、实现接受"；头的默认值仍是最严的那个。真正需要盯的是 :712-714 之后没有拿产品 schema 的枚举来比对该白名单 ⇒ 属"门缺"而非"值错"，且其载体在 `eng/contracts/schemas/product_family_field_constraints.schema.json`（不在本批清单）。检索式：`git grep -n "variance_from" -- eng/contracts/schemas` | 直读 :48-52,679-714 |
| `is_retired_canonical_object` / `psfsw_robust_weight` 的拒绝面 | 退役对象的**显式拒绝**是负责人裁决项（:54-58 注释登记 CHG-2026-09-20-PSFSW-RETIRE 与 UNIFIED_MODEL.md:58），且实现给出比通用拒绝更可诊断的分支（:705-711 带迁移提示），与 :679-695 的第二道拒绝并存 ⇒ 不是"列而不用" | 直读 :54-61,705-713 |
| `unit_law_consistent` 只认 ADU / ADU/sr / ADU/px 三对（:622-632） | 与 SCI-CAL-001 §3"量纲与标度类别"词表（`calibrated_adu`/`photo_scaled_adu`/HiPS `ADU/sr`）同族；未知单位返回 false＝fail-closed（:632） | `git grep -n "surface_brightness\|ADU/sr" -- docs/science/CALIBRATION.md` |
| `correlation_kernel_variance` 的 O(n²) 双循环与 `scale*scale*qf`（:507-524） | 核函数数值形式属 **DI-03（OPEN）**，实现显式拒绝发明核（:568-575 `if (!correlation_matrix.empty()) … else shared_variance = -1.0;` 走 unavailable）⇒ 与 V6 §3 "生产不发明核，仅接受已物化相关矩阵 K" 一致；性能不是本批轴 | 直读 V6 §3 |
| `same_bias_master` 在只给一路 bias 时取 `bias_present_any`（:312-315） | 单路 bias 无"折叠"可言，取真即按折叠系数 `-(1-α)/f`（cpp:324）——与测试 `v6_cal_covariance_test.cpp:191-207`（"仅提供暗路 bias（光路 bias 缺失）：同样折叠"，期望 `(-(1-α)/f)²·v_b`）同式 ⇒ 是**已断言的行为**，不是漏判 | 直读测试 191-207 |
| `structurally_representable()` 对 `kLowRank` 不校验 `factor.size()==n_pix*rank` | 该一致性校验在求值处 :479 做（不符 ⇒ `return -1.0` → :581 判不可表示 → 预算兜底或 unavailable）⇒ 两跳合起来是 fail-closed，非漏检 | 直读 :475-481,581-594 |

---

# 6 lib/algorithms/calibration/tests/p1cal/bias_influence_harness.cpp —— 已读：是（表 14 项）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :67 | `L` | 1000.0f | 亮场常量场电平 | 实验标定（自订夹具值） | 夹具值，非科学量；选取理由＝float32 精确可表＋与 CALIBRATION.md §3 实测锚"观测中位数 1907 ADU"同量级 | — | CONFIRMED |
| :67 | `B` | 100.5f | bias 电平 | 实验标定（自订） | 同上；.5 保证非整、防"整数可表使两分支逐位相同"的假绿 | — | CONFIRMED |
| :67 | `D` | 200.25f | dark 电平 | 实验标定（自订） | 同上 | — | CONFIRMED |
| :67 | `F` | 2.0f | flat 响应 | 实验标定（自订） | :67 行内注释 `max(F,0.1)=2.0` ⇒ 有意选在 floor 之上，**使本门不触及 floor 路径**（见不报项第 3 行） | — | CONFIRMED |
| :67,:70 等 | `0.1`（注释内） | 0.1 | flat floor | 文献值 | SCI-CAL-001 §5/§8/§10＋registry `FZ-CAL-FLOOR`（FROZEN）；实现位 `calibrator.cpp` | — | CONFIRMED |
| :68 | `std::vector<float> light(16, …)`（同 dark/flat/bias） | 16 | 夹具帧尺寸 4×4 的像素数 | 结构性不适用 | 与 :57-58 传给 `ac_calibrate_frame` 的 `4, 4` 配对（数组长度必须 = w·h）；纯夹具维度 | — | CONFIRMED |
| :57-58,:138-139 | 尺寸实参 `4, 4` | 4 | 夹具宽高 | 结构性不适用 | 同上 | — | CONFIRMED |
| :56,:136 | `-777.0f` / `-777.0` | −777 | "未被写入"哨兵 | 结构性不适用 | 非物理值，使"legacy 没写 out"必然落到 FAIL 分支（这正是 §9 要求的"取不到值不得落到合法好值一侧"） | — | CONFIRMED |
| :73,:130 | `349.625f` | 同 | T1/T7a 期望 (1000−100.5−1·200.25)/2 | 公式导出 | 正本＝SCI-CAL-001 §5 标准式；我独立复算 `699.25/2 = 349.625` ✓（且为二进制可表精确值 ⇒ 逐位比较合法） | — | CONFIRMED |
| :78,:131 | `399.875f` | 同 | T2/T7b 期望 (1000−200.25)/2 与 (1000−100.5−(200.25−100.5))/2 | 公式导出 | §5 标准式（bias NULL）与兼容式 K=1；我复算两式同为 `799.75/2 = 399.875` ✓ | — | CONFIRMED |
| :91 | `899.5f` | 同 | T4 期望（只给 bias、无暗无平场） | 公式导出 | §5＋§7"bias 参与不变量"；`1000−100.5 = 899.5` ✓ | — | CONFIRMED |
| :95,:141 | `249.5`（f32/f64 两处） | 同 | T5/T8 期望 (1000−100.5−2·200.25)/2 | 公式导出 | §5＋DATA_SEMANTICS:194（标准式施加 K）；`499/2 = 249.5` ✓；:141 用 double 版期望同一值 ⇒ 同时钉住 FP64 路径不降级 | — | CONFIRMED |
| :100,:108 | `350.0f` | 同 | T6/T6b 期望 (1000−100.5−2·(200.25−100.5))/2 | 公式导出 | §5 兼容式；`700/2 = 350` ✓ | — | CONFIRMED |
| :105,:109 | `150.5f` / `375.0f` | 同 | T6b 的第二档 bias 与其期望 (1000−150.5−2·49.75)/2 | 实验标定（夹具第二档值）＋公式导出（期望） | 第二档专为"兼容式内 bias 参与"设计（:15 `(∂y/∂bias)=−(1−K)/f ≠ 0, K≠1`）；我复算 `750/2 = 375` ✓ | — | CONFIRMED |
| :74,:96,:142 | `actual_k` 期望 `1.0f / 2.0f / 2.0` | 同 | 实际使用的 K 的回写值 | 公式导出 | 见 **CR-55-20**（与 README §3 的"标准分支=1.0"相冲，与 DATA_SEMANTICS:194 相符） | — | CONFIRMED |
| :37,:44 | `"%.9g"` / `"%.17g"` | 9 / 17 | 读数打印位数 | 结构性不适用 | f32/f64 往返打印的定义位数；判定用的是 `got == want`（:34,:42 逐位等），打印只供人读 ⇒ 不构成"以打印精度掩盖容差" | — | CONFIRMED |
| :37,:44,:85,:113,:127,:145 | `ok ? "PASS" : "FAIL"` / `return g_fail == 0 ? 0 : 1` | 0 / 1 | 退出码约定 | 结构性不适用 | 与配套 `check_bias_influence.py:91,119` 的 `rc != 0 且 RESULT 行含 FAIL` 双条件匹配（两者缺一不算红）⇒ 无"rc 被吞"空间 | — | CONFIRMED |

## findings

### CR-55-20 · 本门用 `actual_k == 2.0` 钉住"标准分支也施加 K"，而同模块 README §3 仍写"标准分支=1.0"——对外合同面与本门相反
- 轴：C（主）/ D
- 位点：`bias_influence_harness.cpp:96` `check_bitwise("T5_actual_k", t5.actual_k, 2.0f);`（`t5` 走的是 `dark_opt=0` 标准式：`run(light, &dark, &flat, &bias, 0, 2.0f)`）
- 上位依据（站在本门一侧的两份）：SCI-CAL-001 §5"`K` 是暗电流的曝光线性缩放因子……**两个分支都必须施加同一个 `K`**（`calibrator.cpp:122,144`：`k = k_init`，两分支同一变量、同一回写值）"；`docs/contracts/DATA_SEMANTICS.md:194`"标准分支同样施加该 K（**强制 K=1.0 属缺陷，DISP-CAL-012**）"。
  相反一侧（原文逐字）：`lib/algorithms/calibration/README.md:52-53`"`ac_calibrate_frame(+_f64)` → 校准帧（可负，不 clamp 不加 pedestal，SCI §9a）+ `actual_k`（**dark_opt=1 生效=入参 K；标准分支=1.0**）"。
- 现状：门 :96 与 :142（f64 同档）要求标准分支回写实参 K；README §3 的输出合同说标准分支恒 1.0。二者对同一产品字段 `actual_k` 各说一种。
- 差在哪：这是"同一约定多处各说一种、其中一处是实现里**根本不成立**的旧约定"形态——README §3 描述的是 BIAS-001/DISP-CAL-012 订正**之前**的行为，且是模块对外合同面（README §4 自称 DATA-P1-CAL/API-CAL-001 的落点）。按 README 实现的读者会与本门冲突，而**本门的期望值是正确的、合同文字是错的**（:11 的"顺测试改引擎"危险在此方向相反：这里改 README 才是要做的动作）。
- 后果：`actual_k` 是 §7"K 参与不变量"与 §11"K 参与门"在 manifest 上的唯一读数；合同面教读者"标准分支读 1.0"，会把"K 未生效"误判成正常 ⇒ 一旦真出现回写恒 1.0 的回归，按 README 审的人不会报警。方向：不改变字节，改变的是可核对性（偏绿）。
- 定级：S3（本条的错侧在 README，本批文件内一侧正确 ⇒ 不构成本文件的实现缺陷，但它是本门作为对外署名证据件的唯一自相矛盾处）
- 怎么算修好：改 `lib/algorithms/calibration/README.md:52-53` 为"`actual_k`＝实际施加的 K（两分支皆＝入参 `dark_scale_factor`；`dark==NULL` 时 K 不进入算术，见 SCI-CAL §5/§7）"；**必须同批改**：`git grep -n "actual_k" -- docs` 的命中面（含 docs/contracts/DATA_SEMANTICS.md §9.2 输出表若有同句）需一并订正，否则改了 README 又与 §9.2 打架；本门**不要动**（它是当前唯一钉住 BIAS-001 的机器断言）。
- 置信：CONFIRMED（三处原文逐字；本门 9 个期望值全部我自行手算复核，无一个与被测实现共享）

## 不报的核过项（bias_influence_harness.cpp）—— 派单点名四项的逐条回答

| 派单点名的问 | 核对结论 | 证据 |
|---|---|---|
| ① 期望值是否独立 | **独立**：9 个期望值全是手算字面量，且我逐个复算吻合（349.625 / 399.875 / 899.5 / 249.5 / 350 / 375 / actual_k 1.0、2.0、2.0）；夹具值都是二进制精确可表的 dyadic 有理数 ⇒ `got == want` 的逐位判据合法，不存在"容差掩盖共享常数"（对照 CR 系列发现的 `1.230310` 六处同抄那型）。文件头 :5-6 自述"期望值用字面量手算而**不复用实现**"成立 | 直读 :65-147 全部断言 |
| ② 能否红 | **能**：`g_fail` 累加 → `exit 1`（:145-146）；哨兵 −777 保证"没写 out"也是 FAIL；T3/T6b/T7 是"必须逐位不同"的非退化判据（bias/K/约定三项若被静默忽略即红）。配套 `check_bias_influence.py` 另加 4 条变异注入并要求**每条必红**，且 :105-109 明令"模式找不到就判 FAIL、不放行"；CTest 注册见 `tests/p1cal/CMakeLists.txt:67-70`（`find_package(Python3 … REQUIRED)`，解释器缺失＝配置期报错而非静默 SKIP）⇒ 不属于"不注册仍称科学门"那型 | 直读 harness＋check_bias_influence.py＋CMakeLists:62-70 |
| ③ 结论是否只落在 `run/` | **是**，且这是本仓 CI 门的一般落位（`eng/ci/checks.json:5039,5045` 的 `result_path = run/ci/ctest/p1cal_bias_influence_gate.json`），门本身已注册进基线 ⇒ 按 §4 不立案。补两条真实边界：(a) ctest 调用**不带** `--json-out`（CMakeLists:68-70），故经 ctest 跑时唯一产物是 stdout；(b) 判据的"红"依赖 g++ 可用（`check_bias_influence.py:60` 硬编码 `g++`），Windows 正式平台上缺 g++ ⇒ 该门在 B1 编译步判 FAIL（fail-closed，不静默），但会在双平台门面上表现为"平台性红"而非科学红（该文件不在本批清单，只登记事实） | `git grep -n "p1cal_bias_influence_gate" -- eng` |
| ④ 是否覆盖 flat floor / NaN 输入 | 未覆盖，但**不是本门的义务**：T1-T8 全部走 `F=2.0 > 0.1`（:67 注释明示），门名与判据都是"bias/K 参与"（SCI-CAL §11"bias 参与门/K 参与门"两条独立门），floor 归 U2 门（`check_master_unit_guard.py --self-test`，§11 末段）、NaN 归 `TST-CAL-FAIL-*`（§11"失败注入"行）⇒ 分工不同，不报 | 直读 :67 与 CALIBRATION.md §11 |
| ⑤ 常量场是否使"逐像素错配/索引序错置"不可见 | 本门判据是"给不给 bias 必须不同"，常量场对该判据充分；像素错配由同目录 `p1cal_tests_core.cpp` 与 I4"确定性（1/2/4 线程 bitwise）"族门承担（README §8:137-138 已登记该分工）⇒ **不报**，但登记一条待查：本 harness 不校验 `out[1..15]`（只判 `out[0]`，:73,:78,:91,:95,:100,:108-109,:130-131,:141），若某实现把 bias 只减在像素 0 上，本门不会红——该形态是否被 `p1cal_tests_core` 的 ramp 场覆盖，我未在本批读（不在清单） | 直读 harness 各 check_bitwise 的 `out[0]` |

---

# 本批最高危三条（只挑，不复述）

1. **CR-55-15**（`v6_calibration_covariance.cpp:341/403-404/640`）——非同 master 折叠时发布的 covariance 记录少一个组合系数，`C_out=R C_in Rᵀ` 这条 FROZEN 恒等式在记录面上不成立，且方向经推导确定为"复算方差只会更小"⇒ 直接打在创新点②（跨帧绝对 SNR）上；它也是本批**唯一已有真实消费点**（`lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:367→374→376`）的一条，现有单测以"残差式"断言使其对门不可见。
2. **CR-55-6**（`master_generator.cpp:231/273`）——"median==0 → 1.0"的兜底使**零信息平场产出恒等母版 1.0 并返回 AC_OK**，与被拒绝的 `median<0` 同属一个退化类；纯手算即证，且代码注释自称的缓解（"最小爆炸半径"）与事实相反。它同时说明 §11(b) 的 U2 判定带对这类母版恒绿。
3. **CR-55-5**（`module_entry.cpp:663-666` 与 master 段全部 clip 参数）——`max_iterations:0` 合法 ⇒ "sigma-clip 母版"可以在**一行剔除都不做**的情况下报告成功；同段 `sigma_low/high` 无值域门（负号会把 clip 变成"全灭"）；错误消息自称 "positive integer required" 却放行 0＝登记面在说实现没做的事。（次高：CR-55-16，共享门在 `naive==-1/==0` 上恒真。）

共性（一句，不复述各条）：本批 21 条里 **14 条的封顶是 S2**，根因只有一个——本模块 DLL 与 v6 协方差件在生产面均未接线（README §1/§5、V6 §4），故所有触发链都断在"入口存在、无调用方"这一跳；一旦 `astrocs_module_query_v1` 或 `v6_phase1` 接入 `normalize`，CR-55-5/6/8/11/12/15/16/17/19 需按 S1 重评。

---

# 我读不动或需要实跑才能定的

| 待定项 | 缺的是哪一角 | 该跑什么（不由本节点跑） |
|---|---|---|
| CR-55-10（plan JSON 畸形）与"现网 node_id 是否已 ≥~50 字符" | 我只算了缓冲预算（131+L+D ≤ 191），未枚举真实节点 id | 在 `eng/tests/unit/calibration_integration_test.cpp` 加一条 `plan()` 用例传 60 字节 node_id 并对返回串做 JSON 解析；或从 `docs/traceability/TRACEABILITY_MATRIX.json` / `module_ports.registry.json` 取最长 node_id |
| CR-55-14/15/16/17/19 的档位是否升 S1 | `lib/algorithms/integration/v6_phase1` 是否被三命令的任一入口调用 | `git grep` 之外需构建期 target 依赖图＋一次端到端跑，确认 `covariance.v1` 记录是否真的落到产品 |
| CR-55-15 的修法是否会撞产品门 | `covariance.v1` schema 对 `combination_coefficients` 有无长度/语义约束（头 :290-291 点名该文件） | 读 `eng/contracts/schemas/product_family_field_constraints.schema.json#/$defs/covariance` 的该字段定义（合同层，不在本批清单，且改它要走变更流程） |
| 处置表中 `read_noise/photon/gain` 三式（:153-157）与 `V6 §1 FZ-UNITS` 行的文献定位 | 未逐行核对 `docs/science/NOISE_MODEL.md` 是否给同一式 | 三行 grep 即可定：`git grep -n "gain" -- docs/science/NOISE_MODEL.md` 后对式；本批未做（超出 20 次/文件取证上限内的优先级排序） |
| DISP-CAL-009（`npix` int32 溢出）经本 adapter 的零初始化变成"全零母版＋成功" | 需要 ≥2.15e9 像素的真实分配才能证；本机内存与 AGENTS §3 看门狗都不允许 | 不跑；只把"零初始化改写了已登记缺陷的表现"这一事实交回登记面（`master_generator` 不报项末行） |
| `check_bias_influence.py` 在 Windows 正式平台上的表现（硬编码 `g++`） | 该文件不在本批清单，我只读到"缺 g++ ⇒ B1 编译 FAIL"（fail-closed） | 由门禁路（AUDIT-06 ⑤/复核-门禁入口）在双平台面处理 |
| FZ-CAL-FLOOR 的 `source_binding` 子串 `max(f_p, 0.1)` 在 `docs/contracts/DATA_SEMANTICS.md` 零命中 | registry 面缺陷，不属本批 6 文件 | 已由 `[[default-value-three-side-divergence]]` 记为"登记面锚基本不可用"族；本批只提供第 N 个实例（`git grep -n "max(f_p, 0.1)" -- docs` ⇒ 0 命中） |

---

# 派单文字与清单的差异（以清单为准）

1. **派单第②问的"截尾"在清单文件里不存在**：`master_generator.cpp` 只有 mean / median 两支（`combine`）＋sigma-clip，**无 winsorized/截尾合并**；`git grep -n "winsoriz" -- lib/algorithms/calibration` 零命中（该统计在本仓活在 rejection 模块，见 `eng/packaging/config/defaults.json:316,358,400` 的 `rejection.*.max_iterations` 三元组）。且无任何上位条文要求母版生成支持截尾合并 ⇒ 该半问落空，不虚构 finding。
2. **派单第①问把"顺序（偏置→暗→平场→测光标度）"与"未施加不得登记成已施加"挂在 `photometry_apply` 上**：这两个问题在清单文件里都没有实现面载体——施加顺序在 `calibrator.cpp`（清单外），"登记"在 scheduler 的 `p1_phot.json`（清单外）与 `module_entry.cpp` 的 out manifest（清单内）。我按清单实际内容作答：乘性/加性＝已核（不报项第 1 行），顺序＝按生产落点核到与正本一致（不报项第 2 行），登记面缺口＝**CR-55-12**（present-but-malformed 母版被当作"未提供"，且 out manifest 无任何"哪些校正进过算术"的键）。
3. **派单第③问假定 `module_entry` 有"缺键兜底值"**：实测本文件**不设任何缺键兜底**（必需键缺失即 101 判红，:633-639；文件头 :529-530 自述"master ops 不发明默认值"），"多侧默认不一致"在这里的表现形态不是兜底值分叉，而是 **值域门缺失**（CR-55-5/11）、**present-but-malformed 被吞**（CR-55-12）与**数字词法把畸形值折成前缀值**（CR-55-13）。已按实际形态立案，未去凑派单设想的形态。
4. **派单第④问把 harness 称作"对外署名的影响度证据件"**：它实际是**注册进 CI 基线的 ctest 门**（`eng/ci/checks.json:4376,5037-5045`、`tests/p1cal/CMakeLists.txt:68-70`、`eng/ci/ID_MIGRATION_MAP.md:297` 登记 `CTEST-P1CAL-BIAS-INFLUENCE-GATE`），不是 `实验/` 下的自包含实验单元；因此"结论只落 run/"是本仓门的一般落位而非缺陷（已在该节表内回答，并指出 ctest 侧未带 `--json-out`）。
5. **派单第⑤问已答，且答案与清单预期相反**：实现与 `include/` 同名声明**不是同一套数**（CR-55-14），但**错的一侧是声明面**（公共头 :8 与 `V6_CALIBRATION_COVARIANCE.md:12` 停在 BIAS-001 订正前），实现与 SCI-CAL §5／DATA_SEMANTICS:194 一致 ⇒ 修法改声明面，不许顺声明改实现。
6. 为完成清单 6 文件的比对，我读了以下**清单外**文件，且未对其立任何 finding：`docs/science/CALIBRATION.md`、`docs/algorithms/CALIBRATION_ALGORITHMS.md`、`docs/contracts/DATA_SEMANTICS.md`、`eng/contracts/data/v6_clause_registry_v1.json`、`ASTROCS_DESIGN.md §4.2-4.3`、模块 `README.md`/`V6_CALIBRATION_COVARIANCE.md`/`module.yaml`/`integration/*.json`、`include/astrocs/calibration/{types.h,v6_calibration_covariance.h}`、`src/{ac_api.cpp}`、`lib/algorithms/photometry/cpp/src/spatial_gain.{h,cpp}`、`lib/infrastructure/scheduler/src/module_adapters.cpp`（施加落点 6036-6119）、`lib/algorithms/integration/v6_phase1/src/phase1_product.cpp`（355-405）、`eng/tests/unit/{calibration_integration_test.cpp,v6_p1_cal/*}`、`tests/p1cal/{check_bias_influence.py,CMakeLists.txt}`。

---

# 只读纪律回执

- 零 git 写、零 `git config` 写；未跑 `cmake`/`ninja`/`ctest`/`run_checks.py`/任何 `eng/**` 脚本；未 import 仓库 Python；未在仓库目录内落任何文件。
- 中途仅对 `eng/contracts/data/v6_clause_registry_v1.json` 做过一次**只读**解析（`python3 -c` + `json.load`，不落盘、不导入仓库模块），用于读 FZ-CAL-FLOOR / FZ-CAL-QUANTUM-DEFAULT / OI-02 / DI-03 的登记原文。
- 全部中间件与本成稿均在仓库外：`独立审计/证据/通读-CR-55.md`（本文件）；未建 `独立审计/复算件/CR-55/`（无复算脚本需求，所有算术在成稿内手算给出）。

开工 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain` 原文：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工同命令原文（与基线两项一致，未增加任何未跟踪项）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

基线 HEAD 核对：`git rev-parse --short HEAD` ⇒ `c8f64e9a`（与被审基线一致）。

<!-- PROGRESS: 6/6 -->
