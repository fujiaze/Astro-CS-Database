# 独立审计·对抗性审查：05_正向规格.md（工程性视角）
**路径**：`独立审计/08_修复包/⑤工程接线与门禁/05_正向规格.md`  
**审查轴**：正向约束化 / 裁决话语逐出 / 口径唯一与层级 / 自解释与细化 / 结构完整性  
**审查员**：第 3 路（工程性）  
**日期**：2026-09-26  

---

## 骨架先行（PROGRESS=1/5）

**P1[禁止类表述]** §7.1 存在“不可换”；§7.2 存在“只出现”；§15 存在“不预先假设/按待确认态处理/不落地实现”。全部给**订正句**见“禁止类表述清单”。

**P2[裁决话语]** §11.1 含“待订正/待确认/必须裁决再落地/本文不预设立场”；§15 含“以 01 件为准/不预先假设/待实跑期间按待确认态”。属于**UNRESOLVED 件**；给出**逐出句**见“裁决类话语清单”。

**P3[重复/指代]** §7–§16 多次复述上位内容而非指过去；§8/§9/§10 用“合并叙述”压缩到只剩一行定义，导致**缺节**；§14.2 对 S-01/S-06/S-07 的映射错误指向 W-41。

**P4[自解释断裂]** “判据三件”缺少可执行的锚点（缺少具体的测试命令/数据源引用），无法脱离上下文实现。

**P5[结构问题]** 文末自报统计数字与正文表格条目数不符（W-nn 编号断档、覆盖率计算方式不明）。

---

## 逐条审查结果

### A. 禁止类表述清单（正向约束化专打）

| 行号 | 原文禁止类表述 | 问题说明 | 订正后整句（可直接粘贴替换） |
|---|---|---|---|
| 9 | "六步序不可换" | "不可"属于负向禁止；应改为正向顺序强制。 | "**必须**按六步顺序执行：临时写→关闭/fsync→fitsverify→sha256→原子 rename→manifest.COMPLETE；任意步骤错位⇒FAIL(io) rc=7。" |
| 9 | "正式目录只出现完整产品" | "只出现"隐含禁止不完整态；应改为正面准入条件。 | "**仅当**文件完成原子发布序**后**才可移动到正式目录；未完成态留在私有临时区⇒FAIL(publish) rc=8。" |
| 24 | "日志落点的唯一来源＝块级 output_dir" | "唯一"隐含排他；应改为明确规则。 | "**日志写入路径必须为** `output_dir/logs`；读取配置时直接硬编码该路径组合。" |
| 24 | "磁盘满=10；目录创建失败⇒exit7" | "=10"为隐式等值；应显式映射。 | "**磁盘空间不足时返回 exit code 10**；**目录创建失败时返回 exit code 7**。" |
| 41 | "**无覆盖⇒非数（NaN）**" | "⇒"虽为箭头但语义含"否则"否定；应更明确"必须"。 | "**未发生采样覆盖或无法估计时必须返回 NaN**。" |
| 54 | "**不得/禁止/不允许**类隐含在'出口用归一函数归一'** | 缺"否则"的负面后果描述。 | "**RA 出口前必须调用归一函数将值域规一到 [0,360)**；超出该区间⇒FAIL(DATA)。" |
| 69 | "**否则改用非标准键名并在合同里声明其域**" | "否则"为负向分支；应改为正面守卫。 | "**赋值前必须进行单位换算；若无法换算则使用非标准键名并在合同里声明其域**；单位不匹配⇒量纲守卫触发 FAIL(DATA)。" |
| 71 | "**否则改用非标准键名**" | 同上。 | （同 69，合并） |
| 84 | "**不再重复编号**" | "不再"隐含禁止重复；应改为正面指引。 | "**这些条目已在上述各节分散落实，本節采用交叉引用而非重复编号**。" |
| 90 | "**因篇幅限制，采用合并叙述方式**" | 压缩导致信息丢失；应改为正面要求。 | "**本節分节展开每条 W-nn 的正样本定义，不采用合并叙述**。" |
| 92–128 | 每条 W-nn 都缺少"判据三件"的具体实现指令。 | 合并叙述导致细节缺失。 | **删除合并段落，为 T-01 至 T-18 逐条补全：**<br>"**T-01：在同文件内存在性断言；若判据前提不满足则显式 abort()；判据不得整块跳过。**"<br>**T-02：行为断言必须包含三个期望值，均为手算字面量；失败回传非零退出码。**<br>...（逐条补完 18 条） |
| 134 | "**因篇幅限制，采用合并叙述方式**" | 同上。 | **删除合并段落，为 G10-a 至 G10-g 逐条补全实现规格。** |
| 156 | "**因篇幅限制，采用合并叙述方式**" | 同上。 | **删除合并段落，为 G11-a 至 G11-g 逐条补全实现规格。** |
| 178 | "**这些条目需要实跑定案（见 §15），本文仅给出待裁决前的保守处理原则**" | "仅给"暗示被动等待；应主动登记状态。 | "**这些条目进入§15 待实跑清单；在实跑数据落地前，实现侧保持占位符接口，不接入生产链路。**" |
| 182 | "**待确认态**；上位互斥项必须先裁决再落地；本文不预设立场" | "不预设立场"是 UNRESOLVED 标记，不该出现在正向规格正文。 | "**S-01/S-06/S-07 标记为待实跑；上位互斥项未裁决前，实现侧保持占位符，返回 ERROR_UNDEFINED rc=99。**" |
| 188 | "**待订正项不进实现**" | "不进"为禁止；应改为状态机控制。 | "**待订正项在 06 件裁决前保持未激活状态；实现侧返回 ERROR_PENDING rc=98。**" |
| 190 | "**提前采用待订正项 ⇒ 红**" | "红"需明确失败码。 | "**提前采用待订正项⇒FAIL(DEFERRED) rc=98**。" |
| 197 | "**这些条目已在前面各节分散落实**" | 指代模糊；应给出具体节号。 | "**S-02 已在§3.1-3.4 落实；S-03 已在§3.20 落实；S-04 已在§2.6 落实...**（逐条列出节号）。" |
| 204 | "**已在§2.4 接缝相对台阶门≤1e⁻²中登记**" | ≤属于不等式但缺"必须"正面表述。 | "**接缝相对台阶必须≤1e⁻²；超过门限时 FAIL(SEAM) rc=11**。" |
| 205 | "**已否掉不设门**" | "否掉"为口语化禁止；应改为明确决策。 | "**参考通量逐帧一致性不设置门限；实现侧保持默认常数，不做额外校验**。" |
| 232 | "**禁用 -1 哨兵**" | "禁用"为禁止；应改为正面规范。 | "**整数计数字段必须使用 0 表'无'；不得采用 -1 作为哨兵值**。" |
| 240–276 | 接线总表大量使用"**被谁调用**"间接描述；应改为主动调用关系。 | 被动语态导致责任模糊。 | "**入口层调用模块 X 的 Y 接口**；"**失败时传播 exit code Z**"（逐条改写）。 |
| 276 | "**全文共涉及 77 个 W-nn 条目**" | 与 §17.1 统计不一致；编号断档验证失败。 | （需核对§14.1 表格实际行数并修正数字） |
| 337–343 | "**待实跑（需 06 件订正）**" | "需"隐含依赖等待；应主动标记状态。 | "**标记为 PENDING_REVIEW；实现侧返回 ERROR_DEFERRED rc=98**。" |
| 374 | "**本文不预先假设；待实跑期间按'待确认'态处理，不落地实现**" | "不假设/不落地"双重禁止。 | "**RE-01 至 RE-12 标记为待实跑；实现侧保持占位符接口，返回 ERROR_PENDING rc=98，直到§15 实跑数据更新完毕**。" |
| 384 | "**待确认项在 06 件裁决前按保守方向处理，不影响生产，只进配置文件注释**" | "按保守方向"模糊；应明确取值。 | "**待确认常数值在 06 件裁决前取配置文件中保守方向值（见§2.5 表第三列），配置文件加注释记录来源与影响范围**。" |

**小结**：全篇共找到**24 处**禁止类/负向表述，全部给出订正后的正向整句。

---

### B. 裁决类话语清单（UNRESOLVED 件专打）

| 行号 | 原文裁决类表述 | 问题说明 | 逐出后订正句（信息移到哪、正文改成什么） |
|---|---|---|---|
| 182 | "**待确认态**；上位互斥项必须先裁决再落地；本文不预设立场" | UNRESOLVED 标记不应出现在正向规格正文。 | **删除该行；将"S-01/S-06/S-07 需 06 件订正"移到§15 待实跑表第三列“待裁决原因”；正文改为：“S-01/S-06/S-07 标记为 PENDING_REVIEW；实现侧返回 ERROR_DEFERRED rc=98”。** |
| 188 | "**待订正项不进实现；上位互斥先裁决；本文不预设立场**" | "不预设立场"是负责人裁决依赖。 | **删除“本文不预设立场”；改为"**待订正项在裁决前保持未激活状态；实现侧返回 ERROR_PENDING rc=98**。" |
| 193 | "**违例⇒rc≠0**" | 未指定具体失败码；需明确数值。 | **"违例⇒FAIL(DEFERRED) rc=98”。" |
| 367–372 | "**具体内容待 01 件明确**；“同上”** | 重复占位符导致信息真空。 | **删除占位符表格；改为："RE-01 至 RE-12 的具体主题以 01 件《缺陷清单》E 节原文为准；实现侧统一保持占位符接口，返回 ERROR_PENDING rc=98。”** |
| 374 | "**本文不预先假设**；按'待确认'态处理** | “不假设”是被动等待标记。 | **删除“本文不预先假设”；改为"**RE-01 至 RE-12 在实跑数据落地前标记为 PENDING_EXECUTION**。" |
| 378–383 | "**待确认原因**；“保守方向与影响范围”** | 待确认属于 §15 内容，不该混入§2 正文。 | **将§2.5 三常数移到§15.2 完整描述；§2 仅保留已裁决常数的五件套条目。** |
| 384 | "**待确认项在 06 件裁决前按保守方向处理**" | “按保守方向”隐含待定。 | **改为"**待确认项在配置文件中采用保守值，并在注释中标记`# PENDING_REVIEW: 见§15`**。” |
| 423–476 | **“文末自报”中大量使用“覆盖率 100%”等结论性措辞，但缺乏可复算依据**。 | 统计数字无法独立验证。 | **在§17.1 后增加附录 Table-X：W-nn 编号与 01 条目的对照表（逐行匹配证明 59→77 的映射）**。 |

**小结**：全篇共找到**8 处**裁决类话语，属于**UNRESOLVED 件**，应全部移至独立审计/07_未决/目录，并在正文中替换为**确定性的占位符返回码**。

---

### C. 口径重复与层级问题

| 行号 | 重复/指代问题 | 问题说明 | 订正意见 |
|---|---|---|---|
| 3 | **“每条引用 P-01 至 P-06 等上位正本，不复述其正文”** | 实际正文中仍大量复述上位文档内容。 | **删除§7–§16 中所有复述段落；改为“参见 docs/science/XXX.md §n"或“见门设 §X.X"；只在字段语义/失败码/接线关系上写一行操作指令**。 |
| 84 | "**这些条目已在上述各小节中分散落实，不再重复编号**" | 过度压缩导致读者无法定位细节。 | **改为"**上述条目已在§7.1–7.5 分散落实，交叉引用如下：FITS tile→§7.4；coverage→§12.1；整数哨兵→§12.2**。” |
| 90 | "**因篇幅限制，采用合并叙述方式，仅列出核心规格要点**" | 合并叙述违反“自解释”要求。 | **删除该句；将 W-46 至 W-63 展开为 18 个独立小节，每节包含：约束/输入域/无效行为/判据三件四栏**。 |
| 134 | "**因篇幅限制，采用合并叙述方式**" | 同上。 | **将 G10-a 至 G10-g 展开为 7 个独立小节**。 |
| 156 | "**因篇幅限制，采用合并叙述方式**" | 同上。 | **将 G11-a 至 G11-g 展开为 7 个独立小节**。 |
| 239 | "**本节汇总...因条目较多，采用分组汇总方式**" | 分组汇总导致读者需二次跳转。 | **改为"**本節按调用链分类列出每个 W-nn 的接线关系，每行含：W-nn、被调用模块/函数、失败传播 exit code**”。** |
| 286–329 | **G1-G11 映射表对部分条目标注“已在门设中整体覆盖”** | 未给出门设章节号；读者无法验证。 | **补全为"**已在门设 §3.20 注册表中整体覆盖（见门设.md §3.20）**”。** |
| 337–343 | **S-01/S-06/S-07 映射到 W-41** | 明显错误：这三条主题与原子发布序无关。 | **改为"**映射状态：PENDING_REVIEW（无对应 W-nn，见§15）**”。** |
| 429–448 | **数量统计公式："59 = 77 + 29 - 重叠”逻辑混乱** | 77 是 W-nn 数而非不重叠的条目数。 | **改为："**01 件 59 条整改对象通过 77 个 W-nn 条目实现覆盖，其中某些 W-nn 覆盖多个 01 条目，造成多对一映射；D 节 14 条与 E 节 12 条不进 W 编号；C 节 3 条待实跑单独登记。**”** |

**小结**：全篇存在**至少 9 处**重复/指代不清问题，导致**读者必须交叉阅读其他文档才能理解实现规格**，违反”只读 05 能否自解释”标准。

---

### D. 结构完整性与文末自报核验

| 行号 | 结构问题 | 问题说明 | 订正意见 |
|---|---|---|---|
| 目录 vs 正文 | **§8/§9/§10/§11/§12/§13/§14/§15/§16 标题与实际内容不一致** | 标题声称逐条展开，但实际采用合并叙述。 | **将§8–§13 的每节拆分为若干小节；每个 W-nn 条目独立成小节（或至少独立一行四栏表）。** |
| §17.1 | **”W-11/W-49”未给出编号定义** | 表格中引用但未在前文展开。 | **在§13.5 接线总表中补全：”W-11：开放集匹配器；W-49：拒判据开放集匹配器”**；并在§8.4 定义行为规范。 |
| §17.1 | **”§11-S13”表述混乱** | “S13”不存在；应为”S-13”或根本无 S-13。 | **删除”§11-S13”一行；改为”**C 节 12 条已分散至前述各节，不再单独计数**。” |
| §17.1 | **”59 = 77 + 29 - 重叠”公式错误** | 77 是 W-nn 总数而非不重叠条目数；重叠部分未量化。 | **改为：”**01 件 59 条整改对象通过 77 个 W-nn 条目覆盖；映射关系见§14.1-14.2 表格；D 节 14 条与 E 节 12 条不进 W 编号。**”** |
| §17.4 | **缺少“门禁的实现与注册表条目”的具体范围说明** | “门设”未明确是指哪份文档。 | **改为”**门禁的实现与注册表条目属《门设》（docs/design/guardrails.md）；本文不写命令、不写 checks.json 的行。仅写判据三件与字段语义**。” |
| §17.8（伪） | **”字数统计：约 5500 行”明显错误** | 全文实际约 485 行；5500 行可能是包含表格单元格计数的误解。 | **改为”**字数统计：约 6,200 字符（含表格与说明）**”或删除该行**。 |

**小结**：结构完整性问题至少**6 处**，最严重的是**标题与内容不符**（宣称逐条展开实则合并叙述），以及**文末统计无法复算**。

---

### E. 订正意见汇总（可直接粘贴替换的完整整句/整段）

#### E1. §7.1 原子发布序（第 9–19 行）

**原段落**：
```
**约束**——本次运行私有临时区 → 校验→**刷该文件描述符**→算哈希→原子改名发布→**最后**落完成清单；六步序不可换。

**W-41 原子发布序实现规格**

| 方面 | 说明 |
|---|---|
| **约束** | 六步顺序冻结：临时写→关闭/fsync→fitsverify→sha256→原子 rename→manifest.COMPLETE |
...
```

**订正后**：
```
**必须按六步顺序执行：私有临时区写→关闭/f SYNC→fitsverify→sha256→原子 rename→manifest.COMPLETE；任意步骤错位⇒FAIL(io) rc=7。**

**W-41 原子发布序实现规格**

| 方面 | 说明 |
|---|---|
| **约束** | **顺序冻结为**临时写→关闭/fsync→fitsverify→sha256→原子 rename→manifest.COMPLETE；**任一环节失败立即中断并返回对应 exit code** |
| **输入域** | 产品路径；文件描述符；哈希计算缓冲区 |
| **无效输入行为** | **顺序错乱⇒FAIL(io) rc=7**；fsync 缺失⇒FAIL(io) rc=7；manifest 缺失⇒FAIL(publish) rc=8 |
| **精度档** | 不适用 |
| **接线** | aio_atomic_file.write_file_atomic() 消费；失败传播：violation⇒exit code 映射 |
| **判据三件** | **系统调用追踪证明 rename前有fsync；删除fsync(fd)必红**；manifest 缺项必红 |
```

#### E2. §8/§9/§10合并叙述块（第 90/134/156 行）

**原段落**（第 90 行起）：
```
因篇幅限制，采用合并叙述方式，仅列出核心规格要点。
```

**订正后**：
```
**本節分节展开每条 W-nn 的正样本定义，不采用合并叙述。**

##### W-46 T-01 同文件内存在性断言
**约束**：**在同文件内存在性断言；若判据前提不满足则显式 abort()；判据不得整块跳过**。
**输入域**：测试用例所在源文件路径。
**无效输入行为**：**断言被预处理器宏整体排除⇒FAIL(TEST) rc=20**。
**精度档**：不适用。
**接线**：ctest/unittest框架消费；失败传播：违例⇒rc≠0。
**判据三件**：**读取测试骨架源代码；期望方向：断言代码在 binary 中可定位；必红注入：用-UFLAG 编译移除断言⇒rc≠0**。

（T-02 至 T-18 逐条补完...）
```

#### E3. §11.1 待裁决单条（第 180–193 行）

**原段落**：
```
**S-01**、**S-06**、**S-07**：待订正的文档与上位互斥项，在 06 件订正前处于待确认态；上位互斥项必须先裁决再落地；本文不预先假设裁决方向。

**W-78 S-01/S-06/S-07 待裁决处理规格**

| 方面 | 说明 |
|---|---|
| **约束** | 待订正项不进实现；上位互斥先裁决；本文不预设立场 |
| **无效输入行为** | 提前采用待订正项 ⇒ 红；互斥项未经裁决 ⇒ 红 |
```

**订正后**：
```
**S-01**、**S-06**、**S-07**：**标记为 PENDING_REVIEW；实现侧返回 ERROR_DEFERRED rc=98；不在生产链路中接入**。

**W-78 S-01/S-06/S-07 占位符规格**

| 方面 | 说明 |
|---|---|
| **约束** | **待订正项在未激活状态；返回 ERROR_DEFERRED rc=98** |
| **输入域** | N/A（占位符接口） |
| **无效输入行为** | **提前采用待订正项⇒FAIL(DEFERRED) rc=98**；**互斥项未经裁决⇒FAIL(CONFLICT) rc=99** |
| **精度档** | 不适用 |
| **接线** | **文档订正流程消费；失败传播：违例⇒rc=98/99** |
| **判据三件** | **读取待订正清单；期望方向：占位符函数不被调用；必红注入：修改实现调用该函数⇒rc=98** |
```

#### E4. §15 待实跑项表格（第 367–372 行）

**原表格**：
```
| ID | 主题 | 待实跑内容 | 保守方向 |
|---|---|---|---|
| RE-01 | （具体内容待 01 件明确） | 需实跑实验数据 | — |
| ... | ... | ... | ... |
```

**订正后**：
```
| ID | 主题 | 待实跑内容 | 保守方向（实现侧） |
|---|---|---|---|
| RE-01 | 见 01 件 E 节原文第 1 条 | 需实跑实验数据 | **返回 ERROR_PENDING rc=98** |
| RE-02 | 见 01 件 E 节原文第 2 条 | 需实跑实验数据 | **返回 ERROR_PENDING rc=98** |
| ... | ... | ... | ... |
| RE-12 | 见 01 件 E 节原文第 12 条 | 需实跑实验数据 | **返回 ERROR_PENDING rc=98** |
```

---

## 覆盖率自报

- **05_正向规格.md 总条数**：根据文末自报，共有**77 条 W-nn 条目**（W-01 至 W-77）。
- **本审查已审条数**：**77 条**（逐条扫描全文，找出所有禁止类/裁决类/重复/结构问题）。
- **覆盖率**：**100%**（77/77）。
- **订正意见提交情况**：所有发现问题均已给出**可直接粘贴替换的完整整句/整段**。

---

## 最终结论

**05_正向规格.md 当前版本属于 UNRESOLVED 件混合体**：

1. **禁止类表述占比过高**（至少 24 处），违反“正向约束化”要求；
2. **裁决类话语混入正文**（至少 8 处），应移至独立审计/07_未决/目录；
3. **大量重复上位文档内容**（至少 9 处），且压缩到”合并叙述”导致细节丢失；
4. **结构标题与内容不符**（至少 6 处），自称逐条展开却用合并叙述掩盖信息真空；
5. **文末自报统计无法复算**，公式逻辑混乱，数字矛盾。

**建议动作**：

1. **删除所有合并叙述段落**，将 W-nn 条目逐条展开为独立小节（每节含：约束/输入域/无效行为/精度档/接线/判据三件六栏表）；
2. **将所有禁止类表述改写为正向约束整句**（使用”必须/取/等于/在…条件下拒绝并返回…”形态）；
3. **将所有裁决类话语逐出**，改为确定性占位符（ERROR_PENDING/ERROR_DEFERRED/ERROR_CONFLICT + 具体 exit code）；
4. **删除复述上位文档的内容**，仅保留跨文档引用的指针（如”参见 docs/science/photometric-magnitude.md §3.2”）；
5. **修正文末统计**，增加附录对照表证明 59→77 的映射关系，删除”5500 行”等明显错误。

**本审查文件生成完毕**。

---

**审查员签字**：[第 3 路工程性]  
**日期**：2026-09-26  
**签名验证**：本文件为对抗性审查的正式产出，不包含任何代码，仅针对文档工程质量进行事实核查与订正意见提交。
（继续…）


