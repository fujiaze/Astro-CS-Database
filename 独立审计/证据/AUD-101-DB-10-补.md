# AUD-101 文档审计台账 · 批次 DB-10（补派）

范围：`独立审计/批次清单/DB-10.txt` 全部 22 行。判据：标准 01 全文 + 标准 05 §5 + AUD-101 任务书 + DISPATCH-DOC-PREAMBLE。
仓库冻结基线 HEAD=`c8f64e9a`。本批全程只读：未构建、未跑 ctest、未跑 `run_checks.py`、未执行任何门禁或 `eng/**` 脚本、未 import 仓库内 Python。

## 0 基线自证（`git status --porcelain` 原文）

开工：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工：见文末 §7。

## 0.1 复算口径（本批所有计数与存在性判定所用命令）

- 行数：`wc -l docs/design/*.md docs/standards/*.md docs/standards/checks/*.py docs/DEVELOPER_GUIDE.md`（逐份取值，与 `doc_mechanical.csv` 的 lines 列 22/22 相符）；
- 下游：`git -c core.quotePath=false grep -l -- "<全路径>"` ∪ `git -c core.quotePath=false grep -l -w -- "<basename>"`，剔除自身与 `artifacts/ reports/ run/`，取 `sort -u | wc -l`；
- 被引路径存在性：内联 python 逐份抽取 `(docs|eng|lib|run|实验|工程控制|testdata|artifacts)/…` 串，先对 `git ls-files` 全集比对、再对磁盘 `os.path.exists` 比对；命中"两侧皆无"才记悬空。此口径会漏掉不以斜杠开头的引用（`findings.csv`、`SCIENCE_CORRECTNESS.md`），这类我逐份手工补查；
- 门禁↔文档映射：`grep -o -E "docs/(standards|design)[A-Za-z0-9_./-]*" eng/ci/checks.json | sort | uniq -c`、`grep -c "_STANDARD" eng/ci/checks.json`（= **0**）。

## 1 逐份登记 · docs/design/

列序与列名按前言 §4。`角色` 用标准 01 §2 表术语。

### 1.1 docs/design/LOG_AND_ERROR_SYSTEM.md（207 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | 日志与错误系统（详细设计） |
| 角色 | 架构设计；实所在 `docs/design/` = 应属目录（标准 01 §2 该行含 `docs/design/`）→ 一致 |
| 主题 | 日志/错误一条链的 L0–L3 层次与不可跨越边界、四类数据对象、落点与 `output_dir` 关系、六步生命周期、observability 五面关系、运行期旋钮、判据 R1–R4、帧级/全局失败作用域 |
| 上游 | :3 标 `ASTROCS_DESIGN.md §7.3、§10` — 两节号与题均对得上（§7.3=ASTROCS_DESIGN.md:515，§10=:684） |
| 下游 | 14 份（含 ASTROCS_DESIGN.md:540、ENGINEERING_SPEC.md:191、docs/contracts/LOG_AND_ERROR_CONTRACT.md、docs/plugins/infrastructure/21_observability.md、eng/ci/ledgers/log_system_ledger.json、实验/photometric-magnitude/docs/p1-spatial-gain.md）；非孤儿 |
| 是否正本 | 是（日志与错误详细设计层唯一正本；字段级合同另有 `docs/contracts/LOG_AND_ERROR_CONTRACT.md`） |
| 重复或重叠对象 | 合法分层（设计↔合同）；**同主题第二处承载在别批**：`docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md:5`「日志统一写 run/logs/<module>/<YYYYMMDD>/」与本文件 :21「不落 `run/`」、:93「`log_dir`，默认 `<output_dir>/logs`」互斥（本批内举证，见 2.10） |
| 元信息块 | 有：:5–:7「机器事实源」三行清单（schema/台账/检查器路径）——登记面内容占用抬头块 |
| 写法违规 | 无日期、无任务编号、无 sha256、无历史叙事词（逐词 grep：`旧版/作废/废止/留痕/曾/原先/上一版` = 0 命中）；:32–:66 两张 mermaid/表格自解释，合规 |
| 悬空引用 | 无。我扫了本文件全部带斜杠的路径引用与 §7 的 5 个合同锚，全部命中跟踪集；扫描曾报 :74 一行，核实为正则把字段名串 `run/task/node/module/...` 误当路径（**误报，不判悬空**） |
| 与上位冲突 | 无实质冲突，但**判据表比门禁少一档**：§9（:182–:187）只列 R1–R4；门禁实装 R5（`eng/tools/quality/check_log_system.py:21,458-469`），且 `docs/ci/01_CHECKS.md:81` 与 `docs/contracts/LOG_AND_ERROR_CONTRACT.md:163` 均已写 R5 ⇒ 标准 05 §5 的"每条门禁对应一条文档口径"在本层缺一档（**本批文档口径落后注册表**） |
| 处置建议 | 保留（标准 01 §2 主题唯一正本）；§9 补 R5 行，:5–:7 的机器事实源清单并入 §9 |

### 1.2 docs/design/PHASE1_DETAILED_DESIGN.md（227 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Phase1 目标态详细设计 |
| 角色 | 架构设计；目录一致 |
| 主题 | normalize 的观测模型、节点序与三条强制语义、校准/方差传播、检测阈、PSF、测光链路单位表、SNR/点源信息量、球面 drizzle、产品与落盘形态、验收 |
| 上游 | :3 标 §4（对得上）。**另有第二套权威声明** :5–:6：把 `docs/owner/PROJECT_SPEC.md` 与最高设计并列为"上位"，且写"冲突时本文件描述目标，**目标只由本文件定义**" ⇒ 直接违标准 01 §1（任何文档不声明第二套权威顺序或"唯一口径"）与 §1"下位与上位冲突时以上位为准" |
| 下游 | 17 份（docs/plugins/algorithms_phase1/01–08、PRODUCT_STORAGE_FORM.md、PHASE2_DETAILED_DESIGN.md、docs/owner/PROJECT_SPEC.md、eng/ci/checks.json 的 CHK-HIPS-STORAGE-FORM changed_paths 等） |
| 是否正本 | 是（Phase1 目标态详细设计正本候选），但 :6 的自封唯一口径与"节点顺序以 §4.2 为唯一权威"（:34）自相矛盾 |
| 重复或重叠对象 | ①:36–:46 节点序与"序的依据"整段 ↔ `ASTROCS_DESIGN.md` §4.2（:254 起）——本文件 :34 自己声明"节点顺序以 §4.2 为唯一权威"却又复述链与理由；②:137–:147 单位/量纲表自称"唯一正本 = `docs/contracts/DATA_SEMANTICS.md` §31.1/§31.1a"，:139–:147 仍把该表的行复制一遍（正本句 `DATA_SEMANTICS.md:375,451` 同口径）；③:154–:156"为什么必须保持线性"整段 ↔ `docs/design/PHASE2_DETAILED_DESIGN.md:11` 同题复述（两份下层文档各写一遍，无互指） |
| 元信息块 | 有：:5–:6 双"上位/下游"块 |
| 写法违规 | :63 任务编号 `RULING-DOC-01` + "裁决 B"引述；:80 审计编号 `P0-21`；:112 源码行号锚 `docs/science/STAR_DETECTION.md:18-19`（实测该两行是 99% 召回阈表，冻结阈 `threshold = median(img) + 5.0·bgnoise` 在 :64–:65，锚位错）；:52–:54 以生产节点计数（"8 个节点"）作论证性叙述（现行条款但绑实现细节） |
| 悬空引用 | :63 `run/RULING-DOC-01/REPORT.md` 不存在（跟踪集仅 `run/.gitkeep`：`git ls-files run/`；磁盘 `ls run` 亦无该目录）⇒ 正式设计文档引用不可再生临时区。:112 属锚位错（见上栏）非悬空 |
| 与上位冲突 | **两处科学口径与权威链冲突**：①:143 表格行"Phase1 HiPS signal … writer 归一 `Σ_j x_j·w_jp / Σ_j a_jp`"——分母取覆盖面积 `Σ_j a_jp = D_p`，而科学正本 `docs/science/DRIZZLE.md:22` 定"面亮度归一分母 `N_p = Σ_j w_jp·A_pixel,j`"、`:159` 明列"将 `S_p` 的分母取覆盖面积 `D_p=Σ_j a_jp`"为**禁止写法**（`pixfrac<1` 偏 `1/pixfrac²`，DISP-DRZ-009 负例控制）⇒ 本文件与科学正本相反，且与本文件 :199 自己写的 `S_p = Σ_j B_j a_jp / Σ_j a_jp`（分子是 `B_j`）也不自洽；②:5 把 `docs/owner/PROJECT_SPEC.md` 列为上位（该文件 :3 自标上游 = 最高设计 §0.1，是最高设计之下的细节总入口）⇒ 层级倒置 |
| 处置建议 | 保留为 Phase1 详细设计正本；删 :5–:6 第二权威块（标准 01 §1）、:63 证据改指 `artifacts/evidence/`（标准 01 §2"需要留证的台账落 artifacts/evidence/"）、:112 锚改 `:64-65`、:143 归一分母按 `docs/science/DRIZZLE.md` 归一——**科学口径不自行改判，按 AGENTS.md §8 查证流程上呈** |

### 1.3 docs/design/PHASE2_DETAILED_DESIGN.md（145 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Phase2 目标态详细设计 |
| 角色 | 架构设计；目录一致 |
| 主题 | mosaic 固定流程十步、输入兼容性与形态、SNR 三口径、coverage、UPM 联合相对模型、排异路由、两类目标产品与权重产生链、压缩、确定性、输出、验收 |
| 上游 | :3 标 §5（对得上，§5=ASTROCS_DESIGN.md:365）；单一抬头，合规 |
| 下游 | 14 份（docs/plugins/algorithms_phase2/09–13、PRODUCT_STORAGE_FORM.md、docs/architecture/doc_symbol_namespaces.json、eng/ci/checks.json changed_paths、实验/absolute-snr/docs/EXP-05-ABSOLUTE-SNR.md 等） |
| 是否正本 | 是（mosaic 详细设计正本；档界与算法名让位 plugins 层） |
| 重复或重叠对象 | ①:21 三条 SNR 重建口径 + `w = SNR²/F_ref²` 整段 ↔ `ASTROCS_DESIGN.md`:397–:398（同句复述，含 `snr_path_effective`）；②:58–:62 排异档界三行表 ↔ 唯一正本 `docs/plugins/algorithms_phase2/12_rejection.md`:63–:67（逐档同值）——**上位明确禁止这次复述**（`ASTROCS_DESIGN.md`:420「逐像素档位表（档界与算法名）的唯一正本 = 12_rejection.md §9，**本节不复制档界与取值**」）；③:100–:114 权重两步产生链 ↔ `ASTROCS_DESIGN.md`:228–:229 |
| 元信息块 | 无（:3 上游单行合规） |
| 写法违规 | 无日期/任务编号/sha256；:54 小标题写"（最终**五档**表）"而其下 :58–:62 只有三行——标题与实际不符（现行陈述错误；正本 :71 记"档位表由四档收成**三档**"）；:19"七个 operation"与 :17 列的十步链数目不衔接，读者无法判定哪七个 |
| 悬空引用 | 无：:11 `docs/contracts/DATA_SEMANTICS.md` §31.1a（该节实测存在于 :31 起）、:64 `docs/science/REJECTION.md`、:145 `ACCEPTANCE_SPEC.md` §6.2（实测 = ACCEPTANCE_SPEC.md:227「### 6.2 视觉验收清单」）、:35 `docs/science/PHASE2_UPM.md` §14a 均可解析 |
| 与上位冲突 | :54 标题"五档" ↔ 正本"收成三档"；:56/:58 用"**几何覆盖帧数**" ↔ 最高设计 :420 与正本 :59 用"**几何可贡献帧数**"（正本 §9 标题本身两名并存，本文件跟随了较弱的一名，未定义衔接）；:58–:62 复述档界 ↔ 最高设计 :420 的"不复制"要求 |
| 处置建议 | 保留；§5.1 删档界表、只留指向 `12_rejection.md` §9 的指针（照最高设计 :420 的"不复制"口径），标题去掉"五档"，N 的定语统一为"几何可贡献帧数" |

### 1.4 docs/design/PHASE3_DETAILED_DESIGN.md（174 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Phase3 目标态详细设计 |
| 角色 | 架构设计；目录一致 |
| 主题 | export 的输入模式与语义守卫、WCS 计划、反向映射与采样核、不确定度传播、FITS 产品、流式与资源、验收、裁剪（§8）、视觉验收判据 V1a/V1b（§9） |
| 上游 | :3 标 §6（对得上，§6=ASTROCS_DESIGN.md:434）；**:79 在同一文件里又立第二块"上游/约束"抬头**（§8 段首），与 :3 同功能不同范围 |
| 下游 | 17 份（docs/plugins/algorithms_phase3/14–16、docs/api/CLI_PROTOCOL_V1.md、docs/contracts/CONFIG_CONTRACT.md、eng/contracts/schemas/phase_config_export.schema.json、lib/algorithms/projection/p3_wcs.h、eng/tests/config/test_export_crop_contract.py 等） |
| 是否正本 | 是（export 详细设计正本；投影算法细则让位 plugins/algorithms_phase3 与 docs/science） |
| 重复或重叠对象 | ①:135–:137 的接缝度量口径（"方差比对电平阶跃原理性失明"）↔ `ACCEPTANCE_SPEC.md`:229 同句（正本已声明"唯一数值来源 = 机器门实现 `eng/tools/e2e/seam_footprint.py`、门限推导正本 = `docs/science/PHASE2_UPM.md` §17，本节与 `docs/ci/01_CHECKS.md` 都不复制 §17 的数值结论"）——本文件复述结论句而不复制数值，判重叠不判重复；②:9–:13 三输出模式 ↔ `ASTROCS_DESIGN.md`:457（§6.3 末"输出模式 surface_brightness/point_source_flux/visualization 显式声明，visualization 标注不可测量"） |
| 元信息块 | 有：:79 的重复抬头块 |
| 写法违规 | 无日期/任务编号/sha256；:171–:173 把一次性实测值写进设计文档正文（"M42 实测 `0.99668` vs `0.96900`，差值 464,263 px"）＝实验过程数据入正式设计层（标准 01 §4：数值表与实验数据放下层文档/证据区） |
| 悬空引用 | 无路径悬空（:131 `eng/contracts/schemas/phase_config_export.schema.json#/$defs/export_crop` 实测该 $defs 存在于 :362；:130 `docs/api/CLI_PROTOCOL_V1.md` §7.1 实测存在 :132；:135 门 `CHK-L4-SEAM-FOOTPRINT` 在 `eng/ci/checks.json` 命中 2 处；:137 `ACCEPTANCE_SPEC.md` §6/§6.2/§8 均可解析）。**一处引用错位**：:107 称判别键取 `crop_form` 是为了避开"`mode` 同名异义与 `docs/design/UNIFIED_MODEL.md` §3 冲突"，实测 UNIFIED_MODEL 全文 `mode` 出现 0 次（`grep -c mode docs/design/UNIFIED_MODEL.md` = 0），其 §3 只讲三类配置分离；真正的同名登记面 = `docs/contracts/config_separation_anchors.json:206-208`（`documented_same_name.mode`）与 `eng/contracts/schemas/phase_config_normalize.schema.json:31` ⇒ 指针指向了不含该内容的文档节 |
| 与上位冲突 | :15 与 :24 把"输入只接受面亮度语义"与"八投影冻结清单"的上游都记为 `ASTROCS_DESIGN.md §5.3`——实测 §5.3 = "SNR 重建与逆方差叠加"（:385），八投影在 §6.3（:453），面亮度输入守卫在 §6.3 末（:457）；§5.6（:428-430）只说"Phase2 信号为面亮度量纲" ⇒ 两处节号指错节；:70 验收把 TAN/SIN/CAR/AIT 四投影列为验收面，:24 与上位 :453 同口径"当前仅 TAN 可用"，验收面未标"实现后生效"的状态档（前言 §5 第 6 条：未实现须如实分档，不得与现行验收面混写） |
| 处置建议 | 保留（export 详细设计正本）；:15/:24 上游节号改 §6.3；:79 重复抬头并入正文；:107 指针改指 `config_separation_anchors.json`；:171–:173 实测值移 `artifacts/evidence/`（标准 01 §2/§4） |

### 1.5 docs/design/PRODUCT_STORAGE_FORM.md（256 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | 产品落盘形态：裸 HiPS 与 zstd 归档包 |
| 角色 | 架构设计；目录一致 |
| 主题 | 两形态定义与互斥、按访问模式定形态的判据、命名与布局、Phase1 完整形态与查询链、两层索引 schema、部分覆盖语义、写路径、properties 与哈希口径、体积削减两机制、形态的输入配置与清单登记 |
| 上游 | :3 标 §10 + §4.4/§5/§6 + 附录 B（节号均实测存在且题对）；**:5–:6 又立"上位/下游"两行清单**（与 :3 同功能） |
| 下游 | 20 份（含 ACCEPTANCE_SPEC.md、ASTROCS_DESIGN.md、ENGINEERING_SPEC.md、docs/contracts/HIPS_STORAGE_FORM_CONTRACT.md、三个 PHASE* 设计、`eng/ci/checks.json` 的 CHK-HIPS-STORAGE-FORM changed_paths、4 份 schema） |
| 是否正本 | 是（落盘形态详细设计正本；:218 已把字段词表让位 `hips_storage_form.schema.json#x-astrocs-field-vocabulary`、细则让位合同文档） |
| 重复或重叠对象 | ①:42–:44 三阶段默认形态表 ↔ `ENGINEERING_SPEC.md`:143（同口径：archive 默认 / Phase2·Phase3 不设该键出现即 REJECT）——两处各写一遍；机器面 `eng/tools/hipsform/check_hips_storage_form.py:735 check_doc_consistency`（词表 `layers`）只断言"每层出现登记词、不出现禁用同义名"，**不断言 ENGINEERING_SPEC 与本文件的默认值一致**；②:230"形态是输入配置项而不是运行期开关" ↔ `docs/design/PHASE1_DETAILED_DESIGN.md:27` 同一句复述；③:151–:153 写路径（临时区→校验→哈希→原子改名）↔ `docs/standards/IO_STANDARD.md:7` 与 `docs/standards/CODE_STANDARD.md:27` 同题三写（见 2.9 与 2.3） |
| 元信息块 | 有：:5–:6 |
| 写法违规 | :46 与 :214 `run/HIPS-PACK-01/REPORT.md` §3/§8（任务编号 + 临时区证据）；:100 `EXP-02` 实验编号；:178"**包围盒 TRIM 纳入可选形态**：本节是它的设计落点"＝变更叙述；:180"**9.1 的整产物百分比是一次性测量值，其原始计时/扫描记录不在当前工作区**（`run/` 受回收策略管理），故只作口径对照、不作门值"＝过程流水；:187"关键区分（**本次前提验证**的核心结论）"＝过程叙述；:184"**Windows 面未实测**""行为面待实测"＝在正文预写状态（最高设计 §0.2/§12.5 要求状态由检查现场计算） |
| 悬空引用 | :46、:214 `run/HIPS-PACK-01/REPORT.md`：跟踪集 `git ls-files run/` = 仅 `run/.gitkeep`，磁盘 `ls run/` 亦无 `HIPS-PACK-01` ⇒ 设计文档引用不可再生的临时区证据。**机械列误报口径**：`doc_mechanical.csv` 本行 `dangling_links=[]`，未判出这两条（其口径只认 `](相对链接)` 形态，不认反引号内路径） |
| 与上位冲突 | 无实质口径冲突：§10.1（:224–:228）与 `ASTROCS_DESIGN.md` §10、`ENGINEERING_SPEC.md` §7、schema 一致，且被 `CHK-HIPS-STORAGE-FORM --doc-consistency` 的词表面覆盖（该文件在 `layers` 内、must_contain 6 词）。**登记一项上位声明与登记面不符（缺陷在上位与 eng 侧）**：`ENGINEERING_SPEC.md`:143 称"默认值来源 = `eng/packaging/config/defaults.json`"，实测 `grep -c storage_form eng/packaging/config/defaults.json` = 0、`config_registry.json` 亦 0，默认值实际声明面 = `eng/contracts/schemas/phase_config_normalize.schema.json:480-492`（enum + description"键缺失/空串/null ⇒ archive + warn"） |
| 处置建议 | 保留（形态主题唯一正本）；:5–:6 抬头只留上游一行；:46/:214 证据迁 `artifacts/evidence/`；删 :178/:180/:187 的过程与变更叙述、:184 的待实测预写（标准 01 §4"只写现行设计要怎样"） |

### 1.6 docs/design/UNIFIED_MODEL.md（76 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | 统一观测模型与数据对象（细节文档） |
| 角色 | 架构设计（数据对象与配置）；目录一致（最高设计 §0.2 指名本文件承载"数据对象与配置"） |
| 主题 | 线性观测模型、13 个 canonical 数据对象及"可否作权重"、SNR 产物边界、三类配置严格分离 |
| 上游 | :3 标 §3.1/§3.2（实测 §3.1=数据对象 :221、§3.2=三类配置 :231，号题相符） |
| 下游 | 79 份（本批最高：README.md、ASTROCS_DESIGN.md、ENGINEERING_SPEC.md、docs/contracts/{CONFIG_CONTRACT,DATA_SEMANTICS,UNIFIED_OBJECTS}.md、docs/plugins/**、eng/packaging/config/config_registry.json 等） |
| 是否正本 | 是（数据对象唯一正本：`ASTROCS_DESIGN.md`:223"全仓只有一份正本定义"指名本层；:56 的 13 项枚举与上位 :223 逐项相符） |
| 重复或重叠对象 | ①:43/:45 的 `frame_snr`/`sparse_snr_layer` 独立性 + `w=SNR²/F_ref²` 长段 ↔ `ASTROCS_DESIGN.md`:225–:226（同一条"两个独立对象、换算对两者一致"在最高设计与本文件各写一遍，本文件 :45 又把算子选择规则让给 `docs/plugins/algorithms_phase1/07_noise_snr.md` §4.5，形成三层承载）；②:64–:76 三类配置 ↔ `docs/contracts/CONFIG_CONTRACT.md`（合同层同类登记表）与 `ASTROCS_DESIGN.md` §3.2；③文件内自我重复：:42 与 :43 两行各写一遍"全链只有一个 `flux` 口径 = PSF 拟合域…孔径仅作显式声明的诊断/交叉验证" |
| 元信息块 | 有（轻）：标题后缀"（细节文档）"＝层级自述；:58 用 `> ` 引用块承载退役件说明 |
| 写法违规 | :58 整段＝退役件留痕（"**不是现行对象**""旧产品若声明该对象 ⇒ 显式拒绝 + 迁移提示"）——最高设计 §0.3（:61）明文"已经撤销的设计与已经退役的实现不出现在正式文档里"；:46"三者精度对比是论文核心实验（判据 SP-0…）"＝计划/论文语域，且 `SP-0` 在本文件无指针（定义面 = `实验/absolute-snr/docs/frame-snr-canon.md`，本文件 :46 只给"完整适用域图谱见 `实验/absolute-snr`"这一粗指针） |
| 悬空引用 | :58 `eng/contracts/data/unified_object_compatibility_map_v1.json#retired_entries`、:45 `docs/contracts/UNIFIED_OBJECTS.md` §4b、:46 `实验/absolute-snr` 实测均可解析（我逐条查节号），**不判悬空**；:43 引 PixInsight"功率比式[18] `(Σf)²/σ_n²`"的编号在本文件无文献表可解析，其承载面 `docs/science/PSF_SIGNAL_WEIGHT.md:43` **自标**"未独立核验…需补一手锚点后才可引用" ⇒ 设计层引用了一条被科学正本判为尚不可引用的依据（越权引用，非路径悬空） |
| 与上位冲突 | :58 退役件段落与最高设计 §0.3 冲突；其余与上位一致（:60 SNR 产物边界 = 上位 :225；:56 的 13 个对象 = 上位 :223） |
| 处置建议 | 保留（数据对象正本）；:58 移出正式文档层（兼容性事实归 `eng/contracts/data/unified_object_compatibility_map_v1.json` 与 `docs/contracts/UNIFIED_OBJECTS.md`），:42/:43 合并去重，:46 的 SP-0/论文语域改指针，:43 的 [18] 编号删除或改指已核验锚（标准 01 §1/§3/§4） |

<!-- PROGRESS: 6/22 -->

## 2 逐份登记 · docs/standards/ 与 docs/DEVELOPER_GUIDE.md

**本目录的系统性事实（一条命令可复算）**：`git -c core.quotePath=false grep -n -- "§8.4（模块与 ABI）" -- docs/standards` 命中 **12 份**（API/BENCHMARK/CODE/COMMENT/CONCURRENCY/C_ABI/DOCUMENTATION/ERROR_HANDLING/IO/LOGGING_DIAGNOSTICS/RELEASE/TEST）。现行最高设计里 **§8.4 = 顶层结构**（ASTROCS_DESIGN.md:618）、**§8.5 = 模块与 ABI**（:661）；同目录只有 `NUMERIC_STANDARD.md:3` 写的是 §8.5 ⇒ **12 份的上游节号集体偏一位**（括注标题对、节号错）。这不是重编号遗留：`git log --format=%h -n 10 -- ASTROCS_DESIGN.md` 逐个修订查 `### 8.4 模块与 ABI` 均为 0 命中。`docs/DOCUMENT_INDEX.yaml` 把这 12 条错号原样登记为 `upstream`（:1373-:1444），而索引检查器 `eng/tools/doccheck/check_doc_index.py:356` 的 `has_upstream_header()` 只判同一行是否同时出现"上游"与"ASTROCS_DESIGN.md"两个子串，**不校验节号是否存在**⇒ 集体错号对 DOC-INDEX 门完全不可见（标准 05 §2 判据判别力缺口）。

**门禁↔文档映射的本批总读数**：`grep -o -E "docs/standards[A-Za-z0-9_./-]*" eng/ci/checks.json | sort | uniq -c` 只有 `docs/standards/`（STD-REG 三步的 command 路径）与 `docs/standards/checks/check_standards_registry.py`；`grep -c "_STANDARD" eng/ci/checks.json` = **0** ⇒ **14 份 `*_STANDARD.md` 无一被任何注册门禁读取**（标准 05 §5 要求"每条门禁对应一条文档口径并登记映射"，此处的映射面是空的）。反方向（文档写了门而注册表没有）逐条见各份"与上位冲突"栏。

### 2.1 docs/standards/API_STANDARD.md（23 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Public API Standard |
| 角色 | 标准与检查；实所在 `docs/standards/` = 应属目录（标准 01 §2）一致 |
| 主题 | 公共 API（C ABI 边界）的注释必备面、状态与所有权口径、API-* 契约 ID、变更纪律 |
| 上游 | :3 标 §8.4（模块与 ABI）→ **节号错**（应 §8.5；见 §2 总述）；主题（公共 API 面）的上位实为 §8.5 + §7（CLI 合同） |
| 下游 | **非索引引用 0 份**（命令：`git grep -l -- docs/standards/API_STANDARD.md` ∪ `git grep -l -w API_STANDARD.md`，剔除自身、`artifacts/ reports/ run/ docs/audit/ docs/DOCUMENT_INDEX.yaml` 后为空）⇒ 只剩索引与审计台账登记 = 近孤儿（标准 01 §3 双向可追溯的下游向断链） |
| 是否正本 | 否（同域已有承载面：`docs/contracts/PUBLIC_API.md`、`docs/interfaces/**`、`docs/api/CLI_PROTOCOL_V1.md`） |
| 重复或重叠对象 | ①:12"public raw pointer 必须注明 borrowed / owned / optional" ↔ `CODE_STANDARD.md:14`"公共指针必须声明所有权（borrowed / owned / optional）与空值语义"——同一条要求两处各写、**互不引用**；②:11"失败时输出参数必须重置或文档化'未写入'" ↔ `C_ABI_STANDARD.md:6-7`"输出在失败时重置：`*out_model = nullptr` 语义由实现保证"；③:7-12 的 ABI 边界清单 ↔ 最高设计 §8.5（:661-667）同一主题分层（分层合法） |
| 元信息块 | 无（:3 上游单行，合规） |
| 写法违规 | :22"接口冻结后（**W2/V15/V19** 冻结）改语义一律走显式变更"＝轮次/工作包代号历史叙事（标准 01 §4 禁任务编号与历史叙事）；机械列 `task_ids=[]` **漏报了这一处**（其口径不含 W/V 代号族） |
| 悬空引用 | 路径不悬空（:16 的 `docs/contracts/API_CONTRACTS.csv`、`docs/traceability/TRACEABILITY_MATRIX.json` 均在跟踪集）；**但举例与所引登记表对不上**：文中三个示例 ID 在被指文件中 0 命中——`git grep -c -w API-AIO-001 docs/contracts/API_CONTRACTS.csv` = 0（API-P2-REJECT-001、API-P2-UPM-001 同），该 CSV 的 id 形态是 `API-<符号>`（实测首行 `API-aio_hcsd_read`）⇒ 读者按本文件写的 ID 去登记面查不到任何行 |
| 与上位冲突 | 除 §8.4 集体错号外无内容冲突；:20-23 变更纪律只说"走显式变更"，不指 `ENGINEERING_SPEC.md` §3 变更流程 ⇒ 权威链缺一段（标准 01 §3） |
| 处置建议 | 合并（并入 `docs/standards/C_ABI_STANDARD.md`，或降为 `docs/interfaces/` 的一节）；若保留须：节号改 §8.5、删 :22 代号叙事、:16-18 示例 ID 换成 CSV 里真实存在的行 ID（标准 01 §2 同主题一份正本 + §4 无历史编号） |

### 2.2 docs/standards/BENCHMARK_STANDARD.md（8 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Benchmark Standard |
| 角色 | 标准与检查；目录一致（主题偏"操作与运营"，但作为 benchmark 判据口径留在 standards 可接受——见处置） |
| 主题 | benchmark 构建档、记录面、性能断言的统计口径、输出落点、fast/reference 成对 |
| 上游 | :3 §8.4（集体错号）；且主题上位错配——性能测量与档位属 §9（CPU 后端与资源，:669）与 §12.4（验证层级，:764），不属"模块与 ABI" |
| 下游 | 非索引引用 2 份：`lib/infrastructure/aio/tests/hiss_experiments.cpp`、`lib/infrastructure/aio/tests/results/performance_report.md` |
| 是否正本 | 否（L2 性能判据的正本在 `docs/contracts/PERF_GATE_CONTRACT.md` §1 与 `eng/contracts/resource_gate_v1.json`） |
| 重复或重叠对象 | :6"性能断言需 variance 报告（多次运行）；结论面 = 多次运行统计" ↔ `docs/contracts/PERF_GATE_CONTRACT.md`:10-15 四条冻结判据（平均利用率 ≥0.85 / p50 ≥0.90 / 达标窗占比 ≥0.70 / 低利用窗）与 :20-:26 测量口径——同一"性能结论怎么算"两处各写、互不引用；本文件无任何门 ID 与之对应（`git grep -c "BENCHMARK_STANDARD" eng/ci/checks.json` = 0） |
| 元信息块 | 无 |
| 写法违规 | 无日期/编号/sha256；但全文仅 4 条 bullet，无档位、无命令、无判据编号、无正负例 ⇒ 不满足标准 01 §3"每份文档能追溯到最高设计一条要点并被下游索引"，也不满足标准 05 §5"每条门禁登记检验对象、命令、判据、正负例与档位" |
| 悬空引用 | :7"输出到 run/ 或 **reports/**"——`reports/` 不在 `ENGINEERING_SPEC.md` §7 仓库根固定条目清单（:103-:134 枚举 README/AGENTS/ASTROCS_DESIGN/…/lib/eng/docs/testdata/实验/工程控制/artifacts/run，无 reports/），而该目录确在跟踪集（`git ls-files reports | wc -l` = 3）⇒ 规范正文指向一个未登记的根目录（AGENTS.md §7"新根目录条目先登记并经负责人确认"） |
| 与上位冲突 | 与 `docs/contracts/PERF_GATE_CONTRACT.md` §1/§2 的四条冻结判据、"采样窗默认 1 s"、"独占资源/回放一致"要求无任何映射；与 `eng/ci/checks.json` 的 `L2-FROZEN-GATE-SELFTEST`（:10493）/`L2-FROZEN-GATE-REPLAY`（:10518）两门零指针 ⇒ 标准层与门层是断开的两端 |
| 处置建议 | 合并（把 :5-:8 四条并入性能口径正本面，或改写为指向 `PERF_GATE_CONTRACT.md` 的门禁口径条）；:7 的 `reports/` 改 `run/` 或 `artifacts/`（标准 01 §2 同主题一份正本） |

### 2.3 docs/standards/CODE_STANDARD.md（42 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Code Standard |
| 角色 | 标准与检查；目录一致 |
| 主题 | C++ 编码 MUST/SHOULD/MAY 三层：工具链、所有权、异常边界、三层状态语义、热路径、atomic promote、关联清单 |
| 上游 | :3 §8.4（集体错号）+ **:5-6 又立"权威来源"第二抬头**（`ENGINEERING_SPEC.md` §1/§4 + `ASTROCS_DESIGN.md` §10.2）⇒ 同一文件两套上游声明（标准 01 §1 禁第二套权威顺序） |
| 下游 | 非索引引用 6 份（`docs/standards/NUMERIC_STANDARD.md:131` + 4 份 lib 源文件 + `lib/infrastructure/pipeline/orchestrator/cpp/tests/CMakeLists.txt`） |
| 是否正本 | 部分（"编码规范"主题正本候选；但工具链与提交纪律的正本是 `ENGINEERING_SPEC.md` §1/§2/§6） |
| 重复或重叠对象 | ①:10-11"C++17（正式 toolchain：Windows = MSVC v143；Linux = GCC 或 Clang）" ↔ `ENGINEERING_SPEC.md:7`"C++17 双平台；MSVC v143（Windows）/ GCC 或 Clang（Linux）"——同一冻结工具链字面量两处各写，无机器门比对（漂移无人判）；②:16"异常边界 = C++ 侧；C ABI 失败时输出重置，单出口 cleanup/RAII" ↔ `C_ABI_STANDARD.md:5-8` 三条同义；③:17-18 三层语义 ↔ `ERROR_HANDLING_STANDARD.md:7-11`；④:27"science product 写盘：temp write → validate → atomic promote" ↔ `IO_STANDARD.md:7` 同句 |
| 元信息块 | 有：:5-6 第二抬头 |
| 写法违规 | 正文无日期/任务编号/sha256/历史叙事（我逐词扫 `旧版/作废/废止/留痕/曾/原先/上一版/订正` = 0 命中）；:12-13 MinGW64 定位是正向条款，不判违规 |
| 悬空引用 | :6 与 :11 两次引用 `ASTROCS_DESIGN.md` **§10.2**——实测 §10（:684）"I/O 与原子产品"**无子节**（下一节直接是 §11，:701），"Windows 官方工具链为 MSVC"实际在 **§11**（:709）⇒ 引用了不存在的节号，两处 |
| 与上位冲突 | :5-6 与 :3 并列＝层级自判（标准 01 §1）；工具链字面量与 `ENGINEERING_SPEC.md` §1 双写（应指而不抄）；除 §10.2 外无内容级冲突 |
| 处置建议 | 保留（编码规范主题）；删 :5-6 第二抬头、:6/:11 的 §10.2 改 §11、:10-11 只指 ENGINEERING_SPEC §1 不复述字面量（标准 01 §1/§4） |

<!-- PROGRESS: 9/22 -->

### 2.4 docs/standards/COMMENT_STANDARD.md（46 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Comment Standard |
| 角色 | 标准与检查；目录一致 |
| 主题 | 注释必写面（8 项）、必须删除/迁移的清理面、叙述性注释、长度上限、推荐写法、审计产物 |
| 上游 | :3 §8.4（集体错号）；主题上位应是 `ENGINEERING_SPEC.md` §2（代码风格与历史实现处置）+ 最高设计 §0.3（写法），不属"模块与 ABI" |
| 下游 | **非索引引用 0 份**（只剩 `docs/DOCUMENT_INDEX.yaml:1388` 与 `docs/audit/doc_classification.csv`）⇒ 近孤儿；且 `CODE_STANDARD.md:41-42` 的"关联"清单列了 NUMERIC/C_ABI/CONCURRENCY/ERROR/IO，**独独不列本文件** ⇒ 同目录规范网的断点 |
| 是否正本 | 否（注释卫生的另一面在 `ENGINEERING_SPEC.md` §2 与在册门步骤 `CON-COMMENTS`） |
| 重复或重叠对象 | 与 `CODE_STANDARD.md` 同属"代码书写规范"一档：CODE 讲写什么（:10-:27 MUST 清单），本文件讲注释写什么与删什么（:10-:24），两份**互不引用**且都无门 ID 指向对方；:12"科学公式：变量/单位 + SCI/ALG ID" ↔ `NUMERIC_STANDARD.md:5-11` 的"每个科学量五件事"同一要求两处承载 |
| 元信息块 | 无 |
| 写法违规 | :23-24 清理面枚举含"**第??号计划**"——占位符未填 ⇒ 该条不可执行（枚举里有一个无法判定的成员）；无日期/sha256/历史叙事 |
| 悬空引用 | 无路径悬空（:23 的枚举是正则族不是文件）；:46 的 `eng/ci/run_checks.py` 实测存在 |
| 与上位冲突 | **文档承诺的门禁产物与注册表实态不符（本批"超前门禁"典型，concern 1）**：:46"每个 production 文件记录 comment_hygiene = PASS/FAIL，由 `eng/ci/run_checks.py` 的**注释卫生检查项**产出并落 `run/ci/`"——① 注册表无此门：`grep -c comment_hygiene eng/ci/checks.json` = 0；② 实际承接面是门 `CHK-STALE-DOC` 的步骤 `CON-COMMENTS`（`eng/ci/checks.json` 实测该步 command = `eng/tools/quality/contracts/check_comments.py`，`outputs: []` ⇒ 不落 `run/ci/` 任何产物）；③ 本文件点名的 `eng/tools/quality/check_comment_hygiene.py` 在 `docs/ci/01_CHECKS.md:171` 被明记为"**未注册**的一次性审计脚本…按本节退役契约处置…其判据面由在册的 `CON-COMMENTS` 承接" ⇒ 文档写的门不存在、写的产物不落、写的脚本已按退役处置 |
| 处置建议 | 合并（"必写面/清理面"两条判据并入 `ENGINEERING_SPEC.md` §2，审计面改指在册的 `CON-COMMENTS`＋`CHK-RETIRED-CODE`）；:23 的"第??号计划"补实或删（标准 01 §4"只写现行设计要怎样"） |

### 2.5 docs/standards/CONCURRENCY_STANDARD.md（26 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Concurrency Standard |
| 角色 | 标准与检查；目录一致 |
| 主题 | 全局并行设置与共享状态所有权、并行模块文档必备要素、确定性与线程数默认、关联指针 |
| 上游 | :3 §8.4（集体错号）；主题上位 = 最高设计 §9（:669 CPU 后端与资源，含 :672 worker 数来源、:675 浮点归约顺序冻结） |
| 下游 | 非索引引用 2 份：`docs/standards/CODE_STANDARD.md:41`、`lib/algorithms/coverage/include/astro/phase2/sky_plane.h` |
| 是否正本 | 否（并行模型正本 = `docs/architecture/THREADING_MODEL.md`，本文件 :25 亦指向它） |
| 重复或重叠对象 | :13-:16"每个并行模块文档必须写 parallel region/shared/thread-local/reduction/determinism/float accumulation order" ↔ `docs/architecture/THREADING_MODEL.md`（本文件 :25 指向的同一文档）与最高设计 §9:675（浮点归约顺序冻结、1 worker 与 N worker 等价判据）——同一"并行文档必备要素"三处承载，两处无互指 |
| 元信息块 | 无 |
| 写法违规 | :26"ENG-THREAD-* 契约（**S2 注册**）"＝波次代号历史叙事（标准 01 §4）；:21 留下裸常数"**16** 不作硬编码值"而无推导或来源（标准 01 §4 要求数值有出处；负责人口径"常数须有推导"） |
| 悬空引用 | 无：:25 `docs/architecture/THREADING_MODEL.md` 实测存在；:26 的 `ENG-THREAD-001` 在 `docs/TRACEABILITY.csv:12` 与 `THREADING_MODEL.md:128` 可解析 |
| 与上位冲突 | ①**条款语义颠倒（不可判定）**：:7"库内随意修改全局 OpenMP setting（omp_set_num_threads 等）— 由 run context 统一控制"、:9"undocumented static mutable cache — 必须容量/身份/失效/线程模型"、:11"shared mutable scratch 无 ownership"——三条都**没有"禁止/不得"动词**，按字面读是允许项，而它们显然想表达硬禁令（标准 01 §4：硬约束用正向条款、硬禁令可直接陈述；此处两者皆无）；②:21"线程数：外部可配置，默认 **min(可用核, 配置上限)**；取值来源 = **配置**" ↔ 最高设计 §9:672"worker 数**只来自 profile 与预算对象**，模块不硬编码线程数、不私建长期线程池"——同一默认值两个来源；③:20"生产并行默认确定性"与 §9:675"等价判据是事前冻结的浮点容差，**不是逐位一致**"未建立衔接（本文件说"相同输入 → 相同输出"，上位说跨 worker 允许 1e-12 级容差） |
| 处置建议 | 保留（并发规范主题）；补规范动词使 :7-:11 可判定、:21 取值来源改"profile + Runtime 预算对象"并给 16 的出处或删、:26 去 S2（标准 01 §4） |

### 2.6 docs/standards/C_ABI_STANDARD.md（13 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） C ABI Standard |
| 角色 | 标准与检查；目录一致 |
| 主题 | C 边界函数写法：异常包裹、失败输出重置、单出口 cleanup、句柄、字符串、checked 整数、rc 与状态字段 |
| 上游 | :3 §8.4 → 节号错（此处括注标题"模块与 ABI"与 §8.5 相符，应改 §8.5） |
| 下游 | 非索引引用 5 份：`docs/architecture/abi/ABI_003_SECURE_LOADER.md`、`docs/modules/orchestrator.md`、`docs/modules/photometric_calib.md`、`docs/standards/CODE_STANDARD.md`、`lib/algorithms/photometry/cpp/include/photometric_calib.h` |
| 是否正本 | 否（与 API_STANDARD、CODE_STANDARD 三份同主题） |
| 重复或重叠对象 | 同一 rc/状态语义在 **4 份文档各写一遍**：本文件 :12-13"0=success，非 0=hard error；recoverable science status 经输出状态字段表达" ↔ `CODE_STANDARD.md:17-18`"稳定 success / recoverable science status / hard error 三层语义；`rc=0 + invalid status` 即双语义违规" ↔ `ERROR_HANDLING_STANDARD.md:7-11`（同三层展开）↔ `API_STANDARD.md:10`"状态取值为稳定 enum/status + 字符串 error"；另有 :6-7 ↔ API:11、:8 ↔ CODE:16 两组同句（各处均无互指） |
| 元信息块 | 无 |
| 写法违规 | :5 以具体符号 `p2_upm_open` 作示例（该符号在 lib 内可解析，不判悬空，但规范正文用单一历史符号作例）；:3-:13 全文**没有一处指回 ABI 合同正本**（`docs/architecture/abi/**`、`docs/contracts/PUBLIC_API.md`）⇒ 标准 01 §3 双向索引只有上、没有下 |
| 悬空引用 | 无 |
| 与上位冲突 | :12-13 的"非 0 = hard error"是收敛口径的唯一表述，但它与域→码唯一表无映射：`docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5（:80-108）给的是 `ErrorDomain`（8 值，`lib/include/astrocs/core/contracts.h:17-26`）→ 退出码 2/3/4/5/7/9/10/70 的唯一映射，本文件不提域 ⇒ 按本文件写出的 rc 无法进唯一表（缺一段映射，非内容对立） |
| 处置建议 | 合并（与 `API_STANDARD.md` 合为一份"C ABI 与公共 API 规范"，三层语义让给 `ERROR_HANDLING_STANDARD.md` 只留指针，并补指 `LOG_AND_ERROR_CONTRACT.md` §5 的域→码表）（标准 01 §1/§2/§3） |

<!-- PROGRESS: 12/22 -->

### 2.7 docs/standards/DOCUMENTATION_STANDARD.md（36 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Documentation Standard |
| 角色 | 标准与检查；目录一致（但它承载的主题本身是"权威链与文档分层"，按标准 01 §1 该主题只由最高设计 §0 承载） |
| 主题 | 权威链声明、L0–L5 文档分层图、活动/归档边界、各类文档必备要素、机器一致性要求 |
| 上游 | :3 §8.4（模块与 ABI）——号错且**题与主题完全不相干**；文档规范的正确上位 = `ASTROCS_DESIGN.md` §0/§0.1（:35）/§0.2（:47）/§0.3（:57） |
| 下游 | 非索引引用 1 份：`README.md`（另有索引与审计台账） |
| 是否正本 | **否——与本批整治标准同主题的第二套口径** |
| 重复或重叠对象 | :5-9"权威链（`ASTROCS_DESIGN.md` §0，唯一）：`ASTROCS_DESIGN.md` → `AGENTS.md` → `ENGINEERING_SPEC.md` → `CONTROL_PACK_SPEC.md` → `docs/ci/` → `docs/plugins/`；另立 `docs/science/`（公式权威）、`docs/algorithms/`（推导权威）、`docs/design/UNIFIED_MODEL.md`" ↔ `ASTROCS_DESIGN.md`:37-42「§0.1 唯一权威链：1 本文档…2 详细设计…3 具体设计…4 代码」＋:52「**只有一条权威链**：每份文档的权威顺序由 §0.1 给出」——本文件把四层链重写成按文件的六段序列，并把 science/algorithms 标为"另立"（链外），是同一主题的第二套权威口径 |
| 元信息块 | 有：:5-9 权威链块 + :11-22 分层代码块（二者皆登记/声明性质，不是"现行设计要怎样"的条款） |
| 写法违规 | :36"机器一致性：docs_machine_consistency.py 必须 PASS（**S8**）"＝波次代号；:21"历史（非权威）docs/**/v6/**（V6 产品族冻结/设计档案，**仍在活动索引**）；其余历史由 git 承载"＝历史叙事（标准 01 §4） |
| 悬空引用 | 无路径悬空：:13 `docs/owner/RELEASE_STATUS.md`、`docs/KNOWN_LIMITATIONS.md`、`memory.md`、:24 `docs/DOCUMENT_INDEX.yaml`、`eng/tools/doccheck/check_doc_index.py`、:21 的 v6 档案实测均在跟踪集。但 :36 只写裸文件名 `docs_machine_consistency.py`（实为 `eng/tools/docs_machine_consistency.py`，`git ls-files` 命中）且**不写门 ID**（实际在册门 = `CHK-DOCS-MACHINE-CONSISTENCY` 与 `CHK-DOCS-MACHINE-CONSISTENCY-SELFTEST`，由 checks.json 反查得）⇒ 读者无法从规范走到判据（标准 01 §3） |
| 与上位冲突 | ①:5-9 第二套权威顺序（见"重复"栏，直接撞标准 01 §1 与最高设计 :52）；②:11-22 的 L0–L5 分层与 `ASTROCS_DESIGN.md` §0.2 的详细层枚举、`docs/DOCUMENT_INDEX.yaml`（唯一索引地图）**三处并存**，且本分层图里 `docs/design/` 只出现 UNIFIED_MODEL（:17），本批同目录另外 5 份详细设计在自己目录的"权威分层"里没有位置；③:35"current authority 只取正式文档集内的现行条目"与 :21 把 v6 历史档案留在分层图里自相矛盾 |
| 处置建议 | 删除或彻底改写（标准 01 §1"权威链只有一条"、§2"同一角色内同一主题只有一份正本"）：权威链与分层两块删，只留 :27-:34"各类文档必备要素"，上游改标 §0；若保留须补实际门 ID |

### 2.8 docs/standards/ERROR_HANDLING_STANDARD.md（24 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Error Handling Standard |
| 角色 | 标准与检查；目录一致 |
| 主题 | 三层错误语义、硬错误类别词表、违规判据四条、error-sensitive 模块必备项 |
| 上游 | :3 §8.4（集体错号）；主题上位 = §7.3（错误传播，:515）与 §7.2（退出码表，:493） |
| 下游 | 非索引引用 1 份：`docs/standards/CODE_STANDARD.md:41`（另 `docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md` 反向被本文件 :22 引用） |
| 是否正本 | 否（错误语义正本 = `docs/design/LOG_AND_ERROR_SYSTEM.md` + `docs/contracts/LOG_AND_ERROR_CONTRACT.md`） |
| 重复或重叠对象 | ①:7-11 三层语义 ↔ `CODE_STANDARD.md:17-18` 同式各写；②:8-9 recoverable status 枚举 ↔ `LOG_AND_ERROR_CONTRACT.md` §5/§6 承载面；③:15-18 四条违规判据 ↔ `ASTROCS_DESIGN.md`:521「空 catch、忽略返回码、只写日志、降级为'警告后继续'四类形态都无错误码，属未传播」与 `docs/design/LOG_AND_ERROR_SYSTEM.md`:184-187 R1/R2——同一"吞错形态"清单三处承载（本文件未指上位，也未指门 R1/R2） |
| 元信息块 | 无 |
| 写法违规 | :22"（P1.* / P2.*，见 `docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md`；**原引** `DIAGNOSTICS_STANDARD` 仓内不存在，**已订正**为实际文件名）"＝"原/订正"类历史叙事（标准 01 §4）；机械列 `history_narrative=False` **漏报了这一处**（其词表不含"订正"） |
| 悬空引用 | 无：:22 指向的 `LOGGING_DIAGNOSTICS_STANDARD.md`、:24 `docs/diagnostics/TROUBLESHOOTING.md` 实测均在跟踪集 |
| 与上位冲突 | **错误类别词表与权威域枚举不符**：:10-11 硬错误类别 = `CONFIG / INPUT_CORRUPT / DEPENDENCY / NUMERIC / NO_DATA / RESOURCE / TIMEOUT / IO / SCIENCE_GATE / INTERNAL`；权威域 = `lib/include/astrocs/core/contracts.h:17-26` 的 `ErrorDomain{CONFIG, DATA, SCIENCE_PRECONDITION, IO, RESOURCE, BACKEND, CANCELLED, INTERNAL}`（8 值）＋ `LOG_AND_ERROR_CONTRACT.md` §5（:96-105）"域→码唯一表，未列出的域一律 70"。实测 `INPUT_CORRUPT`、`DEPENDENCY`、`SCIENCE_GATE` 在 `lib/**`+`eng/**` 零命中（`git grep -l -w` 各 0 份），而 `DATA`/`SCIENCE_PRECONDITION`/`BACKEND`/`CANCELLED` 四个现行域在本文件词表里**缺席** ⇒ 照本文件写出的 status 域进不了唯一映射表（`NO_DATA`/`TIMEOUT` 仅在 eng 的无关上下文命中，非域） |
| 处置建议 | 合并（错误语义主题让位设计+合同正本，本文件只留"C 边界/模块必备项"并把域词表逐字对齐 `contracts.h:17-26`）；:22 去"原引/已订正"叙述（标准 01 §4） |

### 2.9 docs/standards/IO_STANDARD.md（23 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） I/O Standard |
| 角色 | 标准与检查；目录一致 |
| 主题 | science product 写盘原子性、文档必须明确面、已知待改进挂账、只读约束 |
| 上游 | :3 §8.4（集体错号）；主题上位 = §10（I/O 与原子产品，:684） |
| 下游 | 非索引引用 1 份：`docs/standards/CODE_STANDARD.md:42` |
| 是否正本 | 否（原子发布正本 = `docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md` + `docs/design/PRODUCT_STORAGE_FORM.md` §7 + `docs/architecture/IO_AND_ATOMICITY.md`） |
| 重复或重叠对象 | :7"temp write → validate → atomic promote（同目录临时文件 + rename），失败清理 temp" ↔ `CODE_STANDARD.md:27`"science product 写盘：temp write → validate → atomic promote"（同句两处）↔ `docs/design/PRODUCT_STORAGE_FORM.md:151-153`（同一写路径的形态化表述）——三份并存、无互指 |
| 元信息块 | 无 |
| 写法违规 | :16 小节标题即"**已知待改进（FINDING 挂账）**"，正文 :18-:19"当前为直接 trunc 写 + flush，未做 temp+rename：**S6 修复或 backlog + rationale**（见 findings.csv F-V19R2-IO-001）"＝任务代号 + 过程台账进正式文档层（标准 01 §2"过程台账不放正式文档层"、§4 无任务编号） |
| 悬空引用 | :19 `findings.csv` 不在跟踪集（`git ls-files | grep -ci findings.csv` = 0；同类事实实际登记在 `artifacts/evidence/v19r7-quality/audit_findings_io.md`，非 .csv）⇒ 悬空文件引用；该处机械列 `dangling_links=[]` **漏报**（引用不带斜杠，其正则不覆盖） |
| 与上位冲突 | ①**状态词档错（把已闭合的过失当现行）**：:18 称 `aio_upm_write_sparse` 未做 temp+rename，实测 `lib/infrastructure/aio/src/aio_upm.cpp:66`（函数定义）起 :86-108 已是"写 `path.tmp` → 校验 → 失败 `std::remove(tmp)` → `std::rename(tmp, path)`（Windows 覆盖语义补一次）"，:84 注释并自陈"（ENG-IO-001）：science 模型写盘走 temp → validate → atomic promote"⇒ 文档比代码落后一个整改周期；②:23"运行产物统一 run/"与最高设计 §10:696"产品与**运行日志**只落块级 `output_dir`；`run/` 只放开发/CI 过程产物与过程日志，与运行日志不互替"口径相反（与 LOGGING_DIAGNOSTICS_STANDARD:5 同型，二者互为复述） |
| 处置建议 | 删除 :16-:19（挂账已闭合，结论归 `docs/architecture/IO_AND_ATOMICITY.md`）；本文件与 `CODE_STANDARD.md:27` 的 atomic promote 条款二选一保留并互指；:23 改"运行产物落 `output_dir`，过程产物落 `run/`"（标准 01 §1 下位撞上位须消除） |

### 2.10 docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md（11 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Logging & Diagnostics Standard |
| 角色 | 标准与检查；目录一致 |
| 主题 | 日志落点、stage 记录字段清单、敏感信息约束、诊断工具、错误可定位链 |
| 上游 | :3 §8.4（集体错号）；主题上位 = §7.3（错误传播与运行日志，:515） |
| 下游 | 非索引引用 1 份：`docs/standards/ERROR_HANDLING_STANDARD.md:22` |
| 是否正本 | **否——与同域两份正本口径互斥**（正本 = 最高设计 §7.3 + `docs/design/LOG_AND_ERROR_SYSTEM.md` + `docs/contracts/LOG_AND_ERROR_CONTRACT.md`） |
| 重复或重叠对象 | :5-:8"日志统一写 run/logs/<module>/<YYYYMMDD>/…每 stage 记录：stage_id、run_id、frame_id、config_hash、input hash、output hash、wall/cpu/RSS/IO、thread count、status、error category/code、upstream cause" ↔ `docs/design/LOG_AND_ERROR_SYSTEM.md`:93-:100（落点 `log_dir` 默认 `<output_dir>/logs`）＋ :74（`log_event` 字段面 = run/task/node/module/phase/commit/host/level/event/units/elapsed/diagnostic）——同一主题（落点 + 字段清单）两处各写、词表不同、无互指 |
| 元信息块 | 无 |
| 写法违规 | :5 的 `<YYYYMMDD>` 目录分区是与"每次运行一份 `run_<run_id>.jsonl`"（设计 :94-:95）互斥的第二套命名口径（写法层面即与正本不符）；无日期/编号/sha256 |
| 悬空引用 | 无：:10 `eng/tools/astrocs_diagnose.py` 实测存在（`ls` 命中）；:11 指向 troubleshooting 条目链（`docs/diagnostics/TROUBLESHOOTING.md` 存在） |
| 与上位冲突 | **三处口径与上位相反，且没有门禁读取本文件**：①落点——:5「日志统一写 `run/logs/<module>/<YYYYMMDD>/`」↔ `ASTROCS_DESIGN.md:530`「日志目录默认 `<output_dir>/logs`（日志落点的唯一来源 = 块级 `output_dir`）」与 `:696`「`run/` 只放开发/CI 过程产物与过程日志，与运行日志不互替」↔ 设计文档 `:99-:100` 把「`run/` 下的运行日志」列为**判据红**面（`CHK-LOG-SYS` R3，实装在 `eng/tools/quality/check_log_system.py:366`）；②字段面——:6-:8 的 stage 字段清单与设计 §3 的 `log_event` 字段面不同名同域，未指正本；③**映射缺口（concern 1）**：`check_log_system.py:69-70` 的 `DOC_ANCHOR_FILES` 只含设计文档与合同文档 ⇒ 本文件这条与 P0 门判红面相反的落点口径**不被任何门读取**（`grep -o "docs/standards[A-Za-z0-9_./-]*" eng/ci/checks.json` 无本文件），标准 05 §5 要求的"门禁↔文档口径"映射在此为空 |
| 处置建议 | 删除（内容并入 `docs/design/LOG_AND_ERROR_SYSTEM.md` §4/§8）——标准 01 §2"同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除"；若暂留必须先改成 `<output_dir>/logs` 并把自己纳入 `DOC_ANCHOR_FILES` 的扫描面 |

### 2.11 docs/standards/NUMERIC_STANDARD.md（131 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Numeric Standard |
| 角色 | 标准与检查；目录一致——但正文含大量应属科学/算法层的推导（见"处置"） |
| 主题 | 科学量五件事、量纲与**标度类别封闭词表**、线性/面亮度标度律、地板可表示性、NaN/Inf 处置口径、权重与逆方差 |
| 上游 | :3 标 §3.3（科学量与星表）+ §8.5——**§8.5 号题相符（本批 14 份里唯一写对节号的）**；但行内锚 `:185` 错：`ASTROCS_DESIGN.md:185` 实测是 §2.2 的句子"并经 §12.3 的独立审稿"，"五件事"实际在 **:239**（§3.3 首行）；:15 又以"> 依据：…"**把同一条上游重复第二遍**（同文件两处上游声明，且都带同一个错锚） |
| 下游 | 非索引引用 16 份（`docs/contracts/{DATA_SEMANTICS,DATA_ARTIFACTS,UNIFIED_OBJECTS,CONFIG_CONTRACT}.md`、`docs/interfaces/data/DATA-002…`、`docs/science/{CALIBRATION,PHOTOMETRY,SCIENCE_SCOPE,UNCERTAINTY_AND_COVARIANCE}.md`、`lib/algorithms/resample/**`、`eng/tests/unit/p3_coverage_test.cpp` 等） |
| 是否正本 | 是（**标度词表**主题正本：`docs/contracts/DATA_SEMANTICS.md:380`、`:543`、`:2130` 三处写"标度词表 = `docs/standards/NUMERIC_STANDARD.md`"；`docs/science/SCIENCE_SCOPE.md:25` 写"唯一权威 = 本文件「量纲与标度」节"） |
| 重复或重叠对象 | ①:85-:95 分母符号 `D_p`/`W_p` 逐分支表 ↔ `docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`:139-:144（正本表，两处几乎逐字同文，而本文件 :110-:111 自认"三处必须**逐字同口径**"而非指针化）；②:41-:42 GUM 式(10) ↔ `docs/contracts/DATA_SEMANTICS.md`（同引 JCGM 100:2008 各自展开）；③:59 `1/A_cell² = 4.306e21（21.63 dex）` ↔ `DATA-002`/`DATA_SEMANTICS` §4a 同数双写；④:119-:126 权重口径 ↔ 最高设计 §3.1（:225-:229）整段复述（详见"与上位冲突"②） |
| 元信息块 | 有：:3 上游行内嵌被引句原文；:15 第二处上游声明 |
| 写法违规 | :43 `run/SCI-FIX-SEMANTICS-01/evidence/scale_dimension_results.json`、:45 `run/RELEASE-05/vis/out/m42_p1_t3/p1_phot.json`（并附 33 帧、α ∈ [1.1387e-17, 6.0083e-17] 等一次性实验值）、:67 `run/M42-VARIANCE-RCA-01/plane_fixed.f64` **并附 64 位 sha256 字面量** ⇒ 任务编号 ×3 + sha256 + 实验过程数据写进规范正文（标准 01 §4 逐条禁止；§2 要求证据落 `artifacts/evidence/`）。**机械列只报 `sha256=True`、`task_ids=[RELEASE-05]`，漏报了另两个任务编号与三条 `run/**` 悬空** |
| 悬空引用 | :43/:45/:67 三条 `run/**` 均不存在（跟踪集 `git ls-files run/` 仅 `.gitkeep`；磁盘 `ls run` 列出的 17 项里没有 `SCI-FIX-SEMANTICS-01`/`RELEASE-05`/`M42-VARIANCE-RCA-01`）⇒ 规范把结论证据挂在不入库、会被回收的临时区（AGENTS.md §7 规定 run/ 不入库），独立读者不可复算 |
| 与上位冲突 | ①**状态词串档**：:108-:109 称"与此相反的 `DISP-DRZ-004`…为 **TRACKED/OPEN** 偏差"，而它指向的注册表实测记 **CLOSED**（`STANDARDS_REGISTRY.md:182` 与 `:270` 两处），且其"唯一口径"就是本文件的样本级掩膜口径 ⇒ 把已闭合项写成对立面；②**节号错**：:119/:122 把"HiPS 只存 SNR/权重是派生量""阶段一、三不产权重"记为"最高设计 §2.1"（§2.1 是创新点一 测光标定），:121 把 `w=SNR²/F_ref²` 记为 §4.3/§4.4（实测在 §3.1:225-:229 与 §5.3:398），:126 的"四概念分离"记为 §4.4（实测 §4.6 硬约束/§3.1）；③**第二套权威指定**：:111"如有分歧以 DATA-002 §2a 的 `invalid_handling` 块为准"——把一份接口合同文档设成科学处置口径的仲裁者，与最高设计 §0.1:42「科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`」冲突（标准 01 §1 同样禁止） |
| 处置建议 | 保留（标度词表主题正本）；必改：:43/:45/:67 证据迁 `artifacts/evidence/` 并删 sha256（标准 01 §2/§4）、:85-:95 改指针（正本 DATA-002 §2a）、:108-:109 状态词按注册表订正、节号重定位、:111 的仲裁者改为 `docs/science/`。**一项需负责人裁决**：`docs/science/SCIENCE_SCOPE.md:36` 写"一手证据、合成实验与真实数据推导见 `docs/standards/NUMERIC_STANDARD.md`"——科学层把推导反向委托给 standards 层，与标准 01 §2 的角色划分（公式与推导归 science/algorithms）相反，属角色分工冲突，我不自行改判 |

### 2.12 docs/standards/RELEASE_STANDARD.md（16 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Release Standard |
| 角色 | 标准与检查；目录一致（但 :5 的版本与发布状态主题属负责人面，应落 `docs/owner/`） |
| 主题 | 版本唯一源、交付包命名与内容、包内容面、发布门字面量、保留清单 |
| 上游 | :3 §8.4（集体错号）；主题上位 = §13 版本与发布权（:805）与 §11 双平台发行（:701） |
| 下游 | **非索引引用 0 份**（只剩 `docs/DOCUMENT_INDEX.yaml:1428` 与 `docs/audit/doc_classification.csv`）⇒ 现行文档/代码零引用，本批唯一真孤儿 |
| 是否正本 | 否（发布状态正本 = `docs/owner/RELEASE_STATUS.md`，本文件 :5 自己也是这么指的） |
| 重复或重叠对象 | :5"版本唯一源 = 根 `VERSION`（`docs/owner/RELEASE_STATUS.md` §2）" ↔ `ASTROCS_DESIGN.md`:808-:810（§13 同一条款，指针写法，合规）；但 :8-:11 交付包内容与 :12-:15 门字面量 ↔ `docs/owner/RELEASE_STATUS.md` §2（:56 起"版本与发布面"）与 `ACCEPTANCE_SPEC.md` 发布门同域双写 |
| 元信息块 | 无 |
| 写法违规 | :8 交付包名 `AstroCS_Review_<主题>_<YYYYMMDD>.zip` 用**已废弃显示名**（现行显示名唯一 = `ACSD`，`ENGINEERING_SPEC.md` §15:248；`AstroCS` 仅允许出现在该节登记的机器契约保留类内，而 `grep -c AstroCS_Review eng/ci/ledgers/naming_surface.json` = 0 ⇒ 未登记保留面 = 显示名漏改）；:12-:15 的 `=PASS` / `=PENDING` 是**状态字面量预写**（最高设计 §0.2:53"状态由检查器现场计算"、§12.5:790 状态阶梯唯一口径）；机械列 `history_narrative`/`dates` 均为 False——该文件的违规是状态字面量与旧显示名，**机械列无此口径，属漏报面而非漏报条目** |
| 悬空引用 | 无路径悬空（`VERSION`、`docs/owner/RELEASE_STATUS.md`、`AGENTS.md`、`ENGINEERING_SPEC.md` §7 实测均存在） |
| 与上位冲突 | **门禁映射脱钩（"文档有、注册表无"的超前门）**：:12-:15 规定的门字面量不在注册表——`grep -c PRE_RELEASE_ENGINEERING_FOUNDATION eng/ci/checks.json` = **0**；`git grep -rln PRE_RELEASE_ENGINEERING_FOUNDATION` 全仓只命中本文件、`docs/TRACEABILITY.csv`、三份**已自标退役**的打包脚本（`eng/tools/quality/build_v19r2_package.py`、`build_v19r3_package.py`（:93 打印 `BUILD_V19R3_PACKAGE_RETIRED … 任意调用 exit 2`）、`build_v19r4_package.py`）。现行发布面实际门 = `ACCEPTANCE_SPEC.md` 四层验收 + 在册门 `CHK-PACKAGE`/`CHK-PKG-CONSISTENCY`/`WIN-CANDIDATE-VALIDATE`（实测三者 outputs 含 `artifacts/candidate/AstroCS-candidate.zip` 等），本文件对这三个门**零指针** ⇒ 标准 05 §5 的映射在两端都断 |
| 处置建议 | 合并（并入 `docs/owner/RELEASE_STATUS.md` 或 `ACCEPTANCE_SPEC.md` 的发布门条，标准 01 §2 负责人面归 `docs/owner/`）；若保留须删 :12-:15 的字面量门、:8 改现行显示名并去掉 `<YYYYMMDD>` 命名 |

<!-- PROGRESS: 18/22 -->

### 2.13 docs/standards/STANDARDS_REGISTRY.md（404 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | 国际标准冻结注册表（STANDARDS_REGISTRY） |
| 角色 | 标准与检查；目录一致 |
| 主题 | 六域（投影 / HiPS / HEALPix / Drizzle / 星表 / FITS）外部标准版本与条款冻结、符合状态四值、偏差索引与处置归属、机器检查合同 C1–C9 |
| 上游 | :3 自标 `ASTROCS_DESIGN.md` §5.3（冻结八投影）/附录 B + `ENGINEERING_SPEC.md` §8，并自declare"不在 §0 权威链上"；而 `docs/DOCUMENT_INDEX.yaml:1435` 给它登记的 upstream 是"§8.4（模块与 ABI）"⇒ **文档自标与唯一索引登记是两条不同口径**（标准 01 §3 要求同源） |
| 下游 | 非索引引用 11 份（`docs/standards/NUMERIC_STANDARD.md`、`docs/interfaces/data/DATA-002…`、`docs/owner/RELEASE_STATUS.md`、`eng/tools/check_version_consistency.py`、`eng/tests/unit/p1wcs/**`、`lib/algorithms/projection/**` 等）＋自家检查器 |
| 是否正本 | 是（外部标准符合性登记主题唯一，:12 自称"仓库唯一"）；**但它的"不复制公式"自禁（:14）被自己破坏**（见下栏） |
| 重复或重叠对象 | ①同文件内 drizzle §3 条款串三写：:45 与 :156 的 CLAUSES 写"权重 `w_jp=a_jp/A_pixel`"，:169 与 :187 写"核按 drop 归一 `w=a/A_drop` + 面亮度分母 `N_p`"，:267/:268 的 §3 索引行又写回"`w_jp = a_jp / A_pixel`"——同一条款两个互斥写法；②科学口径与正本重复：:169/:187 的 `S_p=Σ_j B_j a_jp/Σ_j a_jp` 与 `docs/science/DRIZZLE.md:50`、`:9` 同式（正本在 science 层，本文件 :14 明写"不复制公式"）；③同一 rule_id `NAN-SAMPLE-MASK-COVERAGE-NAN` 的口径文字在 :160-:164、:182、:270 三处复述，另两处在 `docs/standards/NUMERIC_STANDARD.md:79-:107` 与正本 `DATA-002` §2a（:112-:119）⇒ 一个 rule_id 五处承载 |
| 元信息块 | 有：:3–:6 四行抬头，其中 **:6 把登记状态 `status = ACTIVE_NORMATIVE` 写进文档开头**（标准 01 §4 明禁"文档 ID、状态编号、版本锚"元信息块；本仓文档状态唯一面 = `docs/DOCUMENT_INDEX.yaml`） |
| 写法违规 | :75/:249/:250/:63 把实验过程数据写进规范条目（"实测 astropy 交叉 5.7e-14 deg、九宫格 18 格无 1px 偏移、负向注入必败"）；:80-:81"本机实测 3.3e-10~2.9e-9 px"；:169/:187 的 `DRZ-FLUX-FIX-01（口径订正…）`＝"订正"类历史叙事；:330 的 C9 行标"**[W4-A3]**"＝工作项编号入正文。**机械列校准**：`dates=[47,222]` 是**误报**（:47/:222 的 `2016-07-22` 是 FITS 4.0 的 IAU 批准日期，属外部标准版本标识而非本文档日期）；`task_ids=[FIX-01]` 命中的是 `DRZ-FLUX-FIX-01`（真属任务族代号，判对） |
| 悬空引用 | ①:33"新增/变更域…必须走 `ENGINEERING_SPEC.md` §3 + **`SCIENCE_CORRECTNESS.md`** 变更流程"——不给路径且跟踪集无此件：`git ls-files | grep -i science_correct` 无输出，删除历史 = `git log --diff-filter=D --name-only -- "*SCIENCE_CORRECTNESS*"` → `ae0f1e63` 删 `工程控制/PROJECT-GOVERNANCE-01/SCIENCE_CORRECTNESS.md`；②:282"跨域治理类偏差使用 **05 号 findings 登记册**的 `STD-F<n>` ID"——该登记册已删除（`ls 工程控制/` = RELEASE-05、evidence；checker :68-:73 自记"随旧控制包世代清理删除"且 `EXTERNAL_FINDINGS_REQUIRED = False`），与 :293-:294"本表是这些 ID 的**唯一事实源**"自相矛盾；③:4"资源门 = 同文 §8 + `eng/contracts/resource_gate_v1.json`"——`ASTROCS_DESIGN.md` §8 全节无资源门条款（我扫 :542-:668 只有内存管线/调度），资源门实际在 §4.5:357，而 `resource_gate_v1.json:76` 自称依据 = "§3.5/§6.3"（**§3.5 不存在**，§6.3 是投影算法）⇒ 两侧锚点互不相等且都错。§5 的 `docs/standards/__missing__.md`（:368）是检查器 `--fault-inject`/负例命令的字面量，不判悬空 |
| 与上位冲突 | ①上游节号错：八投影冻结在 §6.3:453（"当前仅 TAN 可用"），非 :3/:383 所写的 §5.3（=SNR 重建与逆方差叠加，:385）；②:65 记"Paper II §5 Table 1（TAN/SIN/CAR/AIT）**四投影** CONFORMANT…registry v1 恰四行"，与上位 §6.3 的**八投影冻结集**不衔接，且"仅 TAN 可用"的实现档**没有登记为偏差**（§D.spherical-projection 偏差表 :73-:78 无对应行）；③:268 把 DISP-DRZ-009 的"保持口径"写成 `w=a/A_pixel`，正是科学正本 `docs/science/DRIZZLE.md:158` 明列的**禁止写法**（"把核权重写回 `a_jp/A_pixel,j` ⇒ `Σ_p F_p = pixfrac²·Σ_j x_j`，FZ-COND-FLUX-CONSERV 判红"），与同文件 :187 的 CLOSED 结论互斥；④**按其自家判据 C7 复算，§3 索引有 2 行不成立**：C7 用"§3 索引行的条款串 ∈ 该域清单首列"做逐字相等（`docs/standards/checks/check_standards_registry.py:513-515`），我逐字比对：`DISP-DRZ-002`、`DISP-DRZ-009` 的条款串"§3（线性重建 w_jp = a_jp / A_pixel 与面亮度语义）"在 drizzle 清单首列里不存在（清单写的是 `A_drop` 版）⇒ `C7_deviation_index_rows_resolve` 应判红 ⇒ 若 CI 台账记 STD-REG 为绿，则台账与该门判据不符（本批禁跑门，此条为推导，需前台独立复验） |
| 处置建议 | 保留（外部标准登记主题唯一、C1–C9 判据面不可废）；必改：删 :6 状态行；§5.3→§6.3、:4 的资源门锚改 §4.5 并连带修 `resource_gate_v1.json` 的 §3.5；:33 的 `SCIENCE_CORRECTNESS.md` 与 :282 的 05 号登记册改指 §3.2；:267/:268 的条款串与 `w=` 分母按 `docs/science/DRIZZLE.md` 归一（科学口径不自行改判，按 AGENTS.md §8 上呈）；四/八投影之差补偏差行（标准 01 §1/§2/§3/§4 全部命中） |

### 2.14 docs/standards/TEST_STANDARD.md（10 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） Test Standard |
| 角色 | 标准与检查；目录一致 |
| 主题 | 科学契约↔测试下限、只用公共生产 API、确定性/固定 seed 与容差来源、gtest 命名与 SKIP、覆盖率分母、变更重跑与 artifact 复用 |
| 上游 | :3 标 §8.4（模块与 ABI）→ 集体错号；主题上位应是 **§12 验证体系（:721）与 §12.4 验证层级（:764）**，以及 `ENGINEERING_SPEC.md` §5（测试规范，:64-:90）——本文件对两者**一律无指针** |
| 下游 | 非索引引用 2 份：`lib/infrastructure/aio/tests/test_checksum.cpp`、`lib/infrastructure/aio/tests/test_transform.cpp`（均为源码注释引用本规范） |
| 是否正本 | 否（测试规范正本是 `ENGINEERING_SPEC.md` §5.1/§5.2/§5.3，契约↔测试映射正本是 `docs/contracts/TEST_MATRIX.md`；本文件 :5 的"每科学契约 ≥1 test/oracle"正是该表的规则复述而不指向它） |
| 重复或重叠对象 | ①:5"每科学契约 ≥1 test/oracle（property/oracle/MC）" ↔ `ENGINEERING_SPEC.md:66-:69`"每模块必备测试：确定性合成数据生成器；**不调用生产实现的独立 Oracle 或解析解**；科学不变量/性质测试"——同一"必须有独立 Oracle"下限两处各写、无互指；②:10"变更模块必须重跑相关 gate" ↔ `ENGINEERING_SPEC.md:83`"普通重构跑模块测试 + 影响分析选出的链路"（同一条变更-重跑规则的第二套措辞，且本句引入 `gate` 词而 ENGINEERING_SPEC 用"链路"）；③:6"测试必须使用公共生产 API；实现面只有生产一份" ↔ `docs/standards/CODE_STANDARD.md:21`"production science implementation 单一（单一实现 + oracle）" |
| 元信息块 | 无（:3 上游单行，但内容错号） |
| 写法违规 | :10"未变更模块引用 **V19** artifact 需 hash 证明"＝轮次代号，且指向**已退役**的交付世代：实测 `eng/tools/quality/build_v19r3_package.py:93` 与 `build_v19r4_package.py:92` 各自打印 `BUILD_V19R*_PACKAGE_RETIRED…已退役；任意调用 exit 2`，`grep -rn "V19 artifact" docs/ ENGINEERING_SPEC.md` 只命中本行 ⇒ 该规程无现行生产者、无消费者、无门；:8"GPU 不可用时 GTEST_SKIP"未标适用域（与"生产仅纯 CPU"的上位关系写在下一栏） |
| 悬空引用 | 无路径悬空（全文不含带斜杠的文件引用）；机械列 `dangling_links=[]` 与此一致 |
| 与上位冲突 | ①:8 的 GPU 分句与上位适用域不对齐：`ASTROCS_DESIGN.md`:671"生产仅纯 CPU"、§1.3:98 把"ACR/GPU/CPU+GPU 混合生产路由"列为**非目标（ACR 源码保留为隔离实验，生产不可达）**——本句把 GPU 当作常规测试面而不写这一限定，读者会以为存在生产 GPU 路径；实测该面的存在形态只有隔离实验与 CUDA 桥（`lib/infrastructure/acr/backends/cuda/**`、`lib/algorithms/coverage/CMakeLists.txt:230` 注释"GPU 不可用时…GTEST_SKIP"）；②:5-:10 六条与 `ENGINEERING_SPEC.md` §5 之间无任何映射或指针，且 14 份 standards 全部不在门禁读取面内（`grep -c "_STANDARD" eng/ci/checks.json` = 0）⇒ 本规范既不被上游索引、也不被下游门读，是"两头空"的一条轴（标准 05 §5 的映射在此为空） |
| 处置建议 | 合并（并入 `ENGINEERING_SPEC.md` §5 或改写为其下钻并显式声明"下限表 = `docs/contracts/TEST_MATRIX.md`、正本 = ENGINEERING_SPEC §5"）；删 :10 的 V19 口径、:8 标"仅 ACR 隔离实验面"、:3 上游改 §12（标准 01 §1 单一权威链 + §3 双向索引 + §4 无轮次代号） |

### 2.15 docs/standards/checks/check_standards_registry.py（702 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | 无 H1（:1 是 shebang、:2 是编码声明）⇒ 机械列 `h1 = "-*- coding: utf-8 -*-"` 是把编码行当标题的**误报**，我按实际登记为"无 H1" |
| 角色 | 标准与检查（**可执行件**）——实所在 `docs/standards/checks/`，应属 `eng/ci/`（门注册表与检查器）或 `eng/tools/doccheck/`（同类文档检查器已有先例 `eng/tools/doccheck/check_doc_index.py`）⇒ 落位与角色不一致，**应迁往 `eng/tools/doccheck/check_standards_registry.py`**（AGENTS.md §7 目录落位表） |
| 主题 | STD-REG 门判据实现：C1–C9 + C5′、9 个 fault-inject 场景、锚存活 fail-closed、注入空转守卫 |
| 上游 | 无上游标注（`check_doc_index.py:356` 的 `has_upstream_header` 只作用 `docs/**/*.md`，.py 不受检），但 `docs/DOCUMENT_INDEX.yaml:1443` 仍给它登记 `upstream: ASTROCS_DESIGN.md §8.4（模块与 ABI）`——号与题都与本检查器无关 ⇒ 索引登记了一个不存在于该件的元信息（标准 01 §3 登记含上游，登记面失真） |
| 下游 | 非索引引用 5 份：`eng/ci/checks.json`（STD-REG 三步 command）、`eng/ci/polarity_evidence.json`、`eng/ci/polarity_probe.py`、`docs/standards/STANDARDS_REGISTRY.md`、`docs/owner/RELEASE_STATUS.md` |
| 是否正本 | 是（STD-REG 判据的唯一实装件；它同时是注册表 §5 的"文档口径"的另一端） |
| 重复或重叠对象 | 与 `docs/standards/STANDARDS_REGISTRY.md` §5 互为"文档口径↔门禁"两端，且把冻结串**二次承载**：:89-:107 `FROZEN_DOMAINS`（六域 STANDARD/VERSION 字面量）与 :116-:118 的列头/词表常量是注册表 :40-:47、:61/:95/:177/:247 的第二处副本；:123-:127 `GOVERNANCE_ROW_STD_F6` 是 §3.2 行的第三处副本 |
| 元信息块 | 不适用（代码件；docstring :43、:46-:47 含"用法/退出码"属正常工程注释） |
| 写法违规 | 注释逐条命中同目录 `COMMENT_STANDARD.md:23-24` 自定的清理面：:68-:70"原 05 号 findings 登记册随旧控制包世代清理（ROOT-007/ARCH-001/RETIRE-001）删除"；:111-:112"**2026-09-21** 根目录整合：tests/tools/ci/contracts → eng/**"；:126"**2026-09-16** 由 git 历史逐字取证迁移"；:44-:46 与 :573-:575"W4-A3 事由…原锚串与注册表正文漂移…假自证"；:14-:15/:30 等 8 处任务号 `STD-REG-001`、`W4-A3` ⇒ 日期 + 工作项编号 + "原/订正/曾"历史叙事。**机械列 `dates=[97,111,127]`**：:111/:127 判对（含 ISO 日期），:97 命中的是 bibcode `2005ApJ...622..759G` 一类版本字面量⇒该行的"日期"是**误报**；`task_ids=[]` **漏报** STD-REG-001/W4-A3 |
| 悬空引用 | 代码内常量 `EXTERNAL_FINDINGS_REL`（:71-:73）指向 `工程控制/AstroCS_CONSTITUTION_ALIGNMENT_CONTROL_V1_20260909/05_FINDINGS_REGISTER_20260911.md`——实测不存在（`ls 工程控制/` = RELEASE-05、evidence）；因 `EXTERNAL_FINDINGS_REQUIRED = False` 走"缺席（可选来源）"分支并显式登记（:313-:320），**不判 ANCHOR_STALE**：这一处置本身合规（fail-closed 由显式登记项兑现） |
| 与上位冲突 | ①**注入锚与注册表现文失配 ⇒ 场景失效**：`GOVERNANCE_ROW_STD_F6`（:123-:127）含逐字串"已闭环（治理级/P1）"与"05_FINDINGS_REGISTER_20260911.md（已删除，仅历史溯源）"，而注册表 §3.2 现文（`STANDARDS_REGISTRY.md:298`）是另一写法——`grep -c "已闭环（治理级/P1）"` 在注册表 = **0**、在检查器 = 1 ⇒ `inject("drop-governance-deviation")`（:621-:623）经 `_replace_once` 命中 0 次 ⇒ 抛 `InjectedNoOp`（:579-:583，:586-:587 声明 exit 3），而注册表 §5:358 写"前 8 场景恒退出 0（判定看 verdict）"⇒ 文档口径与该场景实际行为不符；该场景**未注册**（`eng/ci/checks.json` 的 STD-REG 只有 `STD-REG`/`STD-REG-FI-DANGLING`/`STD-REG-FI-VERSION-DRIFT` 三步，实测），故门不会抓到它；②本件的 C7 逐字相等判据与注册表 §3 现文冲突（详见 2.13"与上位冲突"④），属同一失配的两侧；③它的注册表面（`eng/ci/checks.json`）与注册表 §5 的复现命令（:334、:352-:356）都以 `docs/standards/checks/…` 字面量为锚 ⇒ 迁移本件必须同步改这两处，属门禁改造、需负责人批准 |
| 处置建议 | 迁移（→ `eng/tools/doccheck/`，同步 `eng/ci/checks.json` 的 STD-REG command 与注册表 §5 的复现命令与路径字面量）；不迁移则至少：同步 `GOVERNANCE_ROW_STD_F6` 注入锚与注册表 §3.2 现文、清理注释里的日期与工作项编号（标准 01 §4；AGENTS.md §7 落位） |

### 2.16 docs/DEVELOPER_GUIDE.md（41 行 · 已通读全文）

| 字段 | 判定 |
|---|---|
| 标题 | Astro Celestial Sphere Database（ACSD） 开发者指南 |
| 角色 | 操作与运营；**实所在 `docs/`（根）、应属 `docs/development/`**（标准 01 §2 该行明列 `docs/development/`，且该目录已存在并已有 CODE_STYLE.md/CONFIG_SCHEMA.md/TESTING.md）⇒ 角色错放，**应迁往 `docs/development/DEVELOPER_GUIDE.md`** |
| 主题 | 构建/测试/机器门的标准动作、模块级科学矩阵速览、编码纪律速览、规范与追溯入口 |
| 上游 | **两块**：:3 `ASTROCS_DESIGN.md §8（软件架构）`；:5-7 又一块"上游：`AGENTS.md` §3、`ENGINEERING_SPEC.md` §1/§6、`ASTROCS_DESIGN.md` §12（Alpha 前无版本信息）"⇒ 同文件两处上游声明（标准 01 §1/§3） |
| 下游 | 非索引引用 2 份：`README.md`、`docs/standards/DOCUMENTATION_STANDARD.md:14`（后者把它列进"L0 项目入口"） |
| 是否正本 | 否（构建与检查动作的正本 = `AGENTS.md` §3 与 `docs/ci/CI_SPEC.md`；提交纪律正本 = `ENGINEERING_SPEC.md` §6） |
| 重复或重叠对象 | ①:11-:14 构建命令 + :20-:22 `ctest --test-dir build --output-on-failure` ↔ `AGENTS.md` §3 同一段命令（逐字同形），两处并存无互指，且 AGENTS §3 还带内存看门狗与盘满纪律而本指南不带 ⇒ 速览面比正本少了安全项；②:36"一个 commit = 一个明确目的；只 `main` 原子提交" ↔ `ENGINEERING_SPEC.md` §6 与 `AGENTS.md` §6 同一条三写；③:39"日志统一写 `run/logs/<module>/<YYYYMMDD>/`" ↔ `docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md:5` **逐字同句**（两份同一错误口径互为复述，见 2.10） |
| 元信息块 | 有：:3 与 :5-7 双上游块 |
| 写法违规 | 正文无日期/任务编号/sha256/历史叙事；:27-:31"模块级科学矩阵"里的 `SNR-001..015`、`PR-UPM-001..010`、"phase2_synthetic_gate（**82 项**）"＝契约 ID 枚举 + 未标来源的计数断言：`git grep -n "82 项"` 全仓只命中本行与两份 `artifacts/evidence/v19r7-quality/**` 旧审计件，而实测 `grep -c "^TEST(" lib/algorithms/coverage/tests/synthetic_gate.cpp` = 104 ⇒ 该计数无现行复算面、口径未声明（是"TEST 数"还是"门项数"） |
| 悬空引用 | 无路径悬空：:16 `docs/ci/CI_SPEC.md`、`docs/architecture/MODULE_MAP.md`、:41 `docs/traceability/TRACEABILITY_MATRIX.json` 均在跟踪集；:27-:31 的 5 个测试目标名实测在 `eng/**`+`lib/**` 命中 3–13 份。**但** :41 称追溯矩阵是"机器真相"却不写门 ID，而该矩阵有两个同名检查器在册与应退役并存（`eng/tools/traceability/check_traceability_matrix.py` 在册步骤 `TRACEABILITY-MATRIX`；`eng/tools/check_traceability_matrix.py` 在 `docs/ci/01_CHECKS.md` §2.1.1 列为应退役），本文件不区分两件 ⇒ 读者会被引到退役件 |
| 与上位冲突 | ①:7 把"Alpha 前无版本信息"记为 `ASTROCS_DESIGN.md §12`——§12 是"验证体系"（:721），该条实际在 **§13**（:805-:810"alpha 阶段之前，程序、代码与产物中不出现任何版本信息"）⇒ 节号错；②:39 的日志落点与 §7.3:530、§10:696 相反（同 2.10，且它正是 `CHK-LOG-SYS` R3 判红面）；③:35"仅修改 `lib/` 源码" ↔ `ENGINEERING_SPEC.md` §7 目录规范把 `eng/`、`docs/`、`eng/tests/` 都列为常规改动面（本批多条整改项本身就在 docs/ 与 eng/）⇒ 与上位目录规范冲突 |
| 处置建议 | 迁移（`docs/development/DEVELOPER_GUIDE.md`）＋去重（:11-:22 构建/测试段与 :36 提交段改指针到 `AGENTS.md` §3 与 `ENGINEERING_SPEC.md` §6，标准 01 §1/§2）；并修 §12→§13、:39 落点、:29 计数口径、:41 补门 ID（标准 01 §3/§4） |

<!-- PROGRESS: 22/22 -->

## 3 同主题组与差异

| 组 | 主题 | 本批各份 | 差异一句话 | 正本候选 |
|---|---|---|---|---|
| G1 | 运行日志落点与字段面 | `docs/design/LOG_AND_ERROR_SYSTEM.md` · `docs/standards/LOGGING_DIAGNOSTICS_STANDARD.md` · `docs/DEVELOPER_GUIDE.md` | 前者的 `<output_dir>/logs` + 每运行一份 `run_<run_id>.jsonl` 与后两者的 `run/logs/<module>/<YYYYMMDD>/` 互斥，且后两者的落点正是 `CHK-LOG-SYS` R3 的判红面 | `ASTROCS_DESIGN.md` §7.3:530/§10:696 + 设计层 LOG_AND_ERROR_SYSTEM |
| G2 | C 边界与错误语义 | `API_STANDARD.md` · `C_ABI_STANDARD.md` · `CODE_STANDARD.md` · `ERROR_HANDLING_STANDARD.md` | 同一"success / recoverable status / hard error"三层语义四份各写一遍，域词表只有一份与 `contracts.h` 相符 | `ERROR_HANDLING_STANDARD.md`（域词表须逐字对齐 `lib/include/astrocs/core/contracts.h:17-26`）+ `docs/contracts/LOG_AND_ERROR_CONTRACT.md` §5 |
| G3 | 权威链与文档分层 | `DOCUMENTATION_STANDARD.md` · `docs/design/PHASE1_DETAILED_DESIGN.md` · `docs/design/PRODUCT_STORAGE_FORM.md` · `docs/DEVELOPER_GUIDE.md` | 四处各自声明"上位/唯一口径/双上游块"，与最高设计 §0.1–§0.2 的"只有一条权威链"并存 | `ASTROCS_DESIGN.md` §0.1/§0.2（权威链与层的唯一承载）＋ `docs/DOCUMENT_INDEX.yaml`（索引地图） |
| G4 | drizzle 面亮度归一与 NaN 处置 | `PHASE1_DETAILED_DESIGN.md`（:143/:199）· `STANDARDS_REGISTRY.md`（:45/:156/:169/:187/:267/:268）· `NUMERIC_STANDARD.md`（:79-:107） | 同一核权重的分母在两处写 `A_drop`、四处写 `A_pixel`，其中 `PHASE1:143` 与 `STANDARDS_REGISTRY:268` 属科学正本列为禁止写法的形态；`NUMERIC:108` 还把已 CLOSED 的 `DISP-DRZ-004` 写成 OPEN | `docs/science/DRIZZLE.md`（公式）＋ `docs/interfaces/data/DATA-002…§2a`（rule_id 文字）——**谁是正本需负责人裁**（见 2.11 与 2.13） |
| G5 | 数据对象 / SNR / 权重产生链 | `UNIFIED_MODEL.md` · `PHASE1_DETAILED_DESIGN.md` §8 · `PHASE2_DETAILED_DESIGN.md` §2/§6.3 · `NUMERIC_STANDARD.md` §权重 | 同一条"两独立 SNR 对象 + `w=SNR²/F_ref²` + 阶段一/三不产权重"在最高设计、设计层、标准层各写一遍（三层复述） | `ASTROCS_DESIGN.md` §3.1:223-:229 ＋ `docs/design/UNIFIED_MODEL.md`（对象表） |
| G6 | 落盘形态与索引 | `PRODUCT_STORAGE_FORM.md` · `PHASE1/2/3_DETAILED_DESIGN.md` 的形态段 | 同一"默认 archive / Phase2·3 不设键出现即 REJECT"三处复述；**本批唯一真正接上门禁的文档面**：`CHK-HIPS-STORAGE-FORM --doc-consistency` 的词表 `layers` 覆盖 `PRODUCT_STORAGE_FORM.md` 与三份 PHASE 设计（`eng/contracts/schemas/hips_storage_form.schema.json#x-astrocs-field-vocabulary` 实测 layers=12 文件，含别批 ENGINEERING_SPEC/ACCEPTANCE_SPEC） | `PRODUCT_STORAGE_FORM.md`（设计层）＋ `HIPS_STORAGE_FORM_CONTRACT.md`（合同层）＋ schema 词表（字段词表） |

## 4 索引与链接缺陷清单（对 `docs/DOCUMENT_INDEX.yaml`）

**登记面本身缺列**：`grep -o -E "^      [a-z_]+:" docs/DOCUMENT_INDEX.yaml | sort | uniq -c` ⇒ 只出现 `path/status/duty/upstream/downstream/notes`，`role` **0 次** ⇒ 标准 01 §3 要求索引登记含"角色"，本仓唯一索引地图**没有角色字段**（全批 22 份都无法按索引核对角色；本次角色全部由我按标准 01 §2 表判定）。

1. **未登记**：0 份（22/22 全部在 active 区段有 `path`，行号 91/558/568/603/654/660/666/678/709/732/738/1373-1444/1465）。
2. **登记了但文件不存在**：0 份（本批）。
3. **上游标注与我判定不符（索引自身错号）**：12 份 standards 的 `upstream` 登记值 = 文档抬头的错号串 `ASTROCS_DESIGN.md §8.4（模块与 ABI）` ⇒ 索引复制了错误，索引侧无法纠错。
4. **索引登记与文档自标互相矛盾（2 份）**：`docs/standards/NUMERIC_STANDARD.md`——文档 :3 自标 `§3.3 + §8.5`，索引 :1423 登记 `§8.4`；`docs/standards/STANDARDS_REGISTRY.md`——文档 :3 自标 `§5.3/附录 B + ENGINEERING_SPEC §8`，索引 :1435 登记 `§8.4`。⇒ 标准 01 §3"与文档抬头同源"这一条不成立（且门 `DOC-INDEX` 不比对，见第 6 条）。
5. **给代码件登记文档元信息**：`docs/standards/checks/check_standards_registry.py` 被登记 `upstream: §8.4（模块与 ABI）`，而 `has_upstream_header()` 不作用 `.py` ⇒ 登记了一个该件不可能有的元信息；其 `duty` 字段取到 `-*- coding: utf-8 -*-`（索引生成把编码声明当标题）。
6. **索引门无判别力（映射为何失效）**：`eng/tools/doccheck/check_doc_index.py:356` 的 `has_upstream_header()` 只判同一行出现"上游"+"ASTROCS_DESIGN.md"两个子串，不校验节号存在、不比对登记值与文档抬头 ⇒ 第 3/4 条缺陷对 `DOC-INDEX` 门完全不可见（标准 05 §2"判据必须有判别力"）。
7. **悬空引用（本批 8 条，逐条命令与结论）**

| # | 位置 | 指向 | 判定命令 | 结果 |
|---|---|---|---|---|
| 1 | `docs/design/PHASE1_DETAILED_DESIGN.md:63` | `run/RULING-DOC-01/REPORT.md` | `git ls-files run/`（= `run/.gitkeep`）＋ `ls run/` | 不存在；run/ 由 gitignore 管理 ⇒ 不可再生 |
| 2-3 | `docs/design/PRODUCT_STORAGE_FORM.md:46`、`:214` | `run/HIPS-PACK-01/REPORT.md` | 同上两命令 | 不存在 |
| 4 | `docs/standards/NUMERIC_STANDARD.md:43` | `run/SCI-FIX-SEMANTICS-01/evidence/scale_dimension_results.json` | 同上 | 不存在 |
| 5 | 同上 `:45` | `run/RELEASE-05/vis/out/m42_p1_t3/p1_phot.json` | 同上 | 不存在 |
| 6 | 同上 `:67` | `run/M42-VARIANCE-RCA-01/plane_fixed.f64`（+sha256） | 同上 | 不存在 |
| 7 | `docs/standards/IO_STANDARD.md:19` | `findings.csv` | `git ls-files \| grep -ci findings.csv` = 0 | 不存在（同类事实在 `artifacts/evidence/v19r7-quality/audit_findings_io.md`） |
| 8 | `docs/standards/STANDARDS_REGISTRY.md:33`（另 :282/:293 同族） | `SCIENCE_CORRECTNESS.md`（无路径）／"05 号 findings 登记册" | `git ls-files \| grep -i science_correct` 空；`git log --diff-filter=D --name-only -- "*SCIENCE_CORRECTNESS*"` → `ae0f1e63`；`ls 工程控制/` = RELEASE-05、evidence | 两件均已删除，正文仍把它们当变更流程与 ID 来源 |

（另：`docs/standards/API_STANDARD.md:16-18` 举的三个 API-* ID 在其点名的登记表里 0 命中——路径存在、举例不存在，记在 2.1。）

8. **门禁↔文档两端的缺口（本批特有关注点 1 的总表）**

| 形态 | 具体条目 | 复算命令 |
|---|---|---|
| 文档有门、注册表无（超前） | `COMMENT_STANDARD.md:46` 的 comment_hygiene 产物门；`RELEASE_STANDARD.md:12-15` 的 `PRE_RELEASE_ENGINEERING_FOUNDATION` 与五个 `AUTHORITATIVE_*`；`TEST_STANDARD.md:10` 的"引用 V19 artifact 需 hash 证明" | `grep -c comment_hygiene eng/ci/checks.json` = 0；`grep -c PRE_RELEASE_ENGINEERING_FOUNDATION eng/ci/checks.json` = 0；`grep -rn "V19 artifact" docs/ ENGINEERING_SPEC.md` 仅命中该行 |
| 注册表/实装有档、文档口径落后 | `docs/design/LOG_AND_ERROR_SYSTEM.md` §9 判据表停在 R4，实装有 R5 | `eng/tools/quality/check_log_system.py:21,458-469`；对照 `docs/ci/01_CHECKS.md:81`、`docs/contracts/LOG_AND_ERROR_CONTRACT.md:163` |
| 门与注册表都不读标准文档（映射空） | 14 份 `*_STANDARD.md` 在 `eng/ci/checks.json` 里 0 命中 | `grep -c "_STANDARD" eng/ci/checks.json` = 0；`grep -o -E "docs/standards[A-Za-z0-9_./-]*" eng/ci/checks.json` 只出 `docs/standards/` 与 `…/checks/check_standards_registry.py` |
| 与判红口径相反却无人读 | `LOGGING_DIAGNOSTICS_STANDARD.md:5` 的 `run/logs/…` 落点 | `check_log_system.py:69-70` 的 `DOC_ANCHOR_FILES` 只含设计＋合同两份 |
| 自家门与现文失配（未被注册故不暴露） | `check_standards_registry.py:123-127` 的 `GOVERNANCE_ROW_STD_F6` 注入锚在 `STANDARDS_REGISTRY.md` 现文里 0 命中 ⇒ `drop-governance-deviation` 走 `_replace_once` 抛 `InjectedNoOp`（exit 3），与注册表 §5:358"前 8 场景恒退出 0"不符 | `grep -c "已闭环（治理级/P1）"` 两文件分别 = 0 / 1；STD-REG 在 checks.json 只有 3 步（`STD-REG`/`FI-DANGLING`/`FI-VERSION-DRIFT`） |

## 5 覆盖率自报

已审 **22 / 22**（清单 `独立审计/批次清单/DB-10.txt` 共 22 行：`grep -c "docs/" …` = 22）。**未逐行读的名单：空**——22 份全部通读全文（合计 2641 行，`wc -l` 见 §0.1）。

为判重复与上下游而**读过的别批对象**（只对清单项下结论）：`ASTROCS_DESIGN.md`、`ENGINEERING_SPEC.md`、`ACCEPTANCE_SPEC.md`、`docs/ci/01_CHECKS.md`、`docs/contracts/{LOG_AND_ERROR_CONTRACT,HIPS_STORAGE_FORM_CONTRACT,PERF_GATE_CONTRACT,CONFIG_CONTRACT,DATA_SEMANTICS,API_CONTRACTS.csv}`、`docs/science/{DRIZZLE,STAR_DETECTION,PSF_SIGNAL_WEIGHT,SCIENCE_SCOPE,PHASE2_UPM}`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md`、`docs/plugins/algorithms_phase2/12_rejection.md`、`docs/plugins/infrastructure/21_observability.md`、`docs/development/TESTING.md`、`docs/DOCUMENT_INDEX.yaml`、`eng/ci/checks.json`、`eng/ci/{l2_frozen_gate,check_frozen_gate}.py`、`eng/contracts/resource_gate_v1.json`、`eng/contracts/schemas/{perf_gate_criteria,hips_storage_form,phase_config_normalize}.schema.json`、`eng/tools/{quality/check_log_system,hipsform/check_hips_storage_form,doccheck/check_doc_index,docs_machine_consistency}.py`、`eng/packaging/config/{defaults,config_registry}.json`、`lib/include/astrocs/core/contracts.h`、`lib/infrastructure/aio/src/aio_upm.cpp`、`lib/algorithms/coverage/tests/synthetic_gate.cpp`。

未执行任何门禁、构建、ctest、`run_checks.py` 或 `eng/**` 脚本；未 import 仓库内 Python（本批所有判定来自读文件文本与 `git grep`）。

## 6 我证伪了任务书的哪个前提

1. **AUD-101 任务书"约 233 份"——未证伪（口径固定后成立）**：`git ls-files docs | grep -c '\.md$'` = **233**（跟踪集、递归、只 .md）。
2. **"30+ 子目录"——按一级口径证伪**：`git ls-files docs | awk -F/ 'NF>2{print $2}' | sort -u | wc -l` = **23**（algorithms api architecture audit browser ci contracts design development diagnostics interfaces modules operations owner performance plugins quality references research standards tracing… 见命令输出，共 23 项）；若把口径改成"docs/ 下任意层级目录"则为 35。任务书未固定口径，两种读法差 12 ⇒ 整治包该句应补口径或直接改写为"一级 23 个子目录"。
3. **派单前提"前一路成稿只落下 97 行"——证实；但其 `PROGRESS` 头不可信**：`wc -l 独立审计/证据/AUD-101-DB-10.md` = 97；该文件末尾 `PROGRESS: 已审 20/22`，而按其**路径集差**实测它覆盖了 22 项中的 **21 项**（只缺 `docs/standards/TEST_STANDARD.md`）⇒ 头写 20、实覆盖 21，两者都不等于真值。印证"补派按行集差不看 PROGRESS 头"。旧件另有两处标题与实际 H1 不符（它把 14 份 standards 的标题写成 "ACSD X Standard"，实际 H1 是 `Astro Celestial Sphere Database（ACSD） X Standard`）——我按实际 H1 登记，不沿用其标题串。
4. **本批特有关注点 3（性能门阈值写成一次观测值）——不落在本批 22 份里，但线索本身成立**：22 份全量 `grep -n "142|低利用|利用窗|利用率|queue_low"` 仅命中 `docs/design/LOG_AND_ERROR_SYSTEM.md:24` 与 `docs/standards/STANDARDS_REGISTRY.md:4` 两处"资源门数值不属本文职责/资源门锚点"式声明，无阈值。阈值两侧实际值与位点（供 owning 批使用）：合同 `docs/contracts/PERF_GATE_CONTRACT.md:15` 把判据 4 写作"无「连续 **≥142 s** 低利用窗且无积压」"；唯一数值源 `eng/contracts/resource_gate_v1.json:51` 冻结 `"queue_low_window_seconds_min": 10`（谓词 `>=` :52、`requires_queued_work: true` :54），门实装只从唯一源读阈值（`eng/ci/l2_frozen_gate.py:42,79-80` 读 `queue_low_window_seconds_min`，:262-266 按 `< 阈值` 判红）⇒ 142 是一次观测值（`eng/ci/check_frozen_gate.py:79` 的 `max_low_window_seconds: 142.049`、`eng/ci/run_checks.py:1496` 同一归档违规样本），不是冻结值。**同族第二处**：`PERF_GATE_CONTRACT.md:17` 要求 L2 门从 `record_and_justify` 改 `fail-closed`，而唯一源 :44/:46/:49 现值仍是 `record_and_justify`（"85% 均值门在 16-worker 真负载上实测仅 65.09%，未标定前不得硬失败"）⇒ 合同与数值源两侧现值不一致。两处均在别批文件内，我只移交不代审。

## 7 收工基线（`git status --porcelain` 原文）

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工一致：无新增未跟踪项、无修改项（本批全部动作是读文件、`git grep`/`git ls-files`/`git log` 只读查询与在本审查目录内写这一个成稿文件）。`git rev-parse --short HEAD` = `c8f64e9a`。

<!-- PROGRESS: 22/22 完成 -->
