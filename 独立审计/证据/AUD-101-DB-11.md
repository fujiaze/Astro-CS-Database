# AUD-101 · DB-11 文档审计台账（23 份 + DOCUMENT_INDEX.yaml 全量对账）

批次：DB-11　判据：标准 01 §1–§5　基线 HEAD=`c8f64e9a`　节点：独立只读审查节点

开工基线 `git -C "F:/Astro dev/Astro CS Normalization Database" -c core.quotePath=false status --porcelain` 原文（不带 `-c core.quotePath=false` 时中文件名会以八进制转义显示，同一状态两种写法）：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与派单前言 §1 预期的两项一致。全程只读命令（`ls-files`/`grep`/`status`/读文件），未执行任何构建、测试、检查器、生成器；未 import 仓库内 Python。

## 0 派单与模板的两处不一致（先登记）

1. 前言 §4 规定 14 个字段，`TPL_文档审计台账.md` §1 表头只有 11 列（缺 `行数`、`写法违规`、`悬空引用` 三列，且把 `含日期/编号/历史叙事` 与 `写法违规` 合一）。按前言"顺序与列名固定"执行：每份给全 14 字段，末尾附合并用简表。
2. 前言称本批 23 份，清单 `独立审计/批次清单/DB-11.txt` 实有 23 行（不含空行），与派单一致。清单文件为 **CRLF** 行尾：按行读入时必须先 `tr -d '\r'`，否则 `[ -f ]` 与 `git grep` 一律假阴（本批首轮逐文件 `wc -l` 即因此全部报 MISSING，已纠正后重跑）。

---

## 1 逐份登记

### 1.1 docs/DOCUMENT_INDEX.yaml　（本批主产出：全文档集唯一索引地图全量对账）

| 字段 | 判定 |
|---|---|
| 路径 | `docs/DOCUMENT_INDEX.yaml` |
| 标题 | `ACSD 文档机器索引（DOCUMENT_INDEX）——全文档集唯一索引地图`（第 1 行，注释行伪装 H1） |
| 行数 | 1514 |
| 角色 | 索引地图（标准 01 §3 规定的唯一索引件）。实际目录 `docs/` 根 = 该文件自定的落位，与 §3 一致，**不错放** |
| 主题 | 全文档集登记与双向索引 |
| 上游 | 第 3 行注释自标 `ASTROCS_DESIGN.md §0.2`、`ENGINEERING_SPEC.md §8`。注意：这是**注释**不是 §3 要求的"抬头上游条款区"格式（YAML 无正文层）；对 `.md` 才要求该区，索引自身按 §3 属"现存文档全部登记"对象却**未登记自己**（见 1.1 节 C 组 B-2） |
| 下游 | 255 条 `downstream` 字段把本文件写成首要引用者（自引用），真实外部消费者：`eng/tools/doccheck/check_doc_index.py`、`eng/ci/checks.json`、`docs/audit/*`。命令：`git -C ... grep -l -F docs/DOCUMENT_INDEX.yaml -- ':!docs/DOCUMENT_INDEX.yaml'` |
| 是否正本 | **是**（§3 明定唯一索引地图），但**不是合格的正本**：字段集缺 §3 要求的两项（见下） |
| 重复或重叠对象 | `docs/audit/doc_classification.csv` —— 见 §2 节 G1 |
| 元信息块 | **有，且多重**：1–32 行整段头部注释即 §4 禁的元信息块；35–39 行 `version/authoring_task/base_main_sha/base_product_version/coverage` 五项机器元信息 |
| 写法违规 | 见下条列表（§4 全项命中：日期、任务编号、commit sha、sha256、历史叙事、元信息块） |
| 悬空引用 | 见下条列表 |
| 与上位冲突 | **是**（三处，见下） |
| 处置建议 | **保留 + 就地重建**：`path/status/duty/upstream/downstream/notes` 六字段扩为 §3 要求的 `路径/角色/主题/上游/下游`，删除 35–39 行版本锚与 1–32 行历史叙事（理由：§3"索引由文档现状重建，与文件树一致"；§4"文档开头无元信息块"） |

#### A. 写法违规逐条（行号 + 原文片段）

- `4:  # 重写：DOC-403（2026-09-21，基线 HEAD d1ccfa1bed9c）——双向层级索引闭合` —— 一行同时含任务编号 `DOC-403`、日期 `2026-09-21`、commit sha 前缀 `d1ccfa1bed9c`、历史叙事"重写"。
- `31:  #   （2026-09-21 ROOT-CONSOLIDATION：reverse_verify/ 根条目解散、内容迁入 实验/，` —— 日期 + 任务编号 `ROOT-CONSOLIDATION` + 历史叙事"解散/迁入"。
- `23:  #   4. CLEAN-402 删除 docs/{archive,review,backlog}/** 后遗留的 32 条悬空条目` / `25:  #      docs/backlog/NEXT_STAGE.md）已在本版清除；被删内容由 git 历史承载；` —— 任务编号 `CLEAN-402` + 历史叙事"删除/已在本版清除/由 git 历史承载"。§4 要求无历史叙事，且 §2 第三条明定过程台账不落正式文档层——清除记录属 `artifacts/evidence/`。
- `36:  authoring_task: "DOC-403"` / `37:  base_main_sha: "d1ccfa1bed9c9c4ae3dbf032ae04de24d2618eb8"` / `38:  base_product_version: "0.1.0-alpha.1"` —— 任务编号、40 位 commit sha、版本锚三项 §4 禁载物，被固化进机器字段。
- `notes:` 字段 148 条中大量同类：`137:  notes: "ALG-COS-001..005（P1-COS-DOC 冻结：…）"`、`153:  notes: "SCI-FIX-PSF 第 9 项 (F-2 整改, 2026-09-16): P1 星检测/PSF/WCS 冻结门表 (16 门)…"`、`175:  notes: "P1-NOISE-DOC 冻结（2026-09-07）：§13 CONTRACT_READY 增补…"` —— 冻结叙事 + 日期 + 任务编号。字段数：`grep -c '^      notes: ' docs/DOCUMENT_INDEX.yaml` = 148。
- **142 条 `downstream` 用"等 N 处"截断**（`grep -oE '等 [0-9]+ 处' docs/DOCUMENT_INDEX.yaml | wc -l` = 142，占 265 条的 54%）：例如 `45:  downstream: "AGENTS.md、CONTROL_PACK_SPEC.md、ENGINEERING_SPEC.md、README.md 等 386 处"`。§3 要求索引登记内容含"下游"，而一个只给总数的前三名既不可核对也不可判孤儿——本批 8 份"下游仅索引自引"的文档正是被这类截断掩盖（见 §4 节）。

#### B. 与上位（标准 01）冲突三条

1. **§3 字段缺两项**。§3 规定登记内容含"路径、角色、主题、上游、下游"。本文件实际字段全集（`grep -oE '^[[:space:]]+[a-z_]+:' | sort | uniq -c`）：`upstream/status/duty/downstream/path` 各 265、`notes` 148，**无 `role`/`角色`、无 `topic`/`主题`**（`grep -niE '(role|topic|theme|角色|主题)'` → 0 命中）。`status` 的取值是活动分类 `{ACTIVE_NORMATIVE, ACTIVE_INFORMATIVE, GENERATED}`（第 9–10 行自述，且自注"**不是** §12.5 交付状态阶梯"），`duty` 是一句话职责——两者都不承载 §2 的九类角色，因此"角色与目录一一对应"的机器可核性在索引层**没有落点**：审计要判角色错放只能靠人读目录名。这是索引作为唯一地图的结构性不合格。
2. **§1"任何文档不声明自己的第二套权威顺序或唯一口径"**：第 26 行"本文件是唯一索引地图"属 §3 授权的正向陈述（可保留）；但第 10 行把 `status` 与 §12.5 明确切割、第 21–22 行自定义"活动设计档案"类别（`docs/validation/v6/**` 照实登记在 active 区段"不当作归档"），是在 §12.5 状态阶梯之外另设一套分类语义，且用"不当作归档"的否定式规避 §2 第三条。
3. **§3"未登记即缺陷，登记了但文件不存在同样是缺陷"两侧不对称**：见 §2 节，索引自定规则第 19 行把登记义务缩为"每份**下级文档**（docs/\*\*/\*.md）必须在 active 区段登记"——用 `.md` 后缀过滤替代 §3 的"现存文档全部登记"，直接造成 §2 节 B 组的三个漏登记被判"合规"。

#### C. 全量对账（结论覆盖全部 258 份 `docs/` 跟踪文件，不限本批 23 份）

三个集合并按下列确定命令得出（基线 `c8f64e9a`，工作树与索引一致）：

```bash
# 集合 1：索引登记路径
grep '^[[:space:]]*- path:' docs/DOCUMENT_INDEX.yaml \
  | sed -E 's/^[[:space:]]*- path:[[:space:]]*"(.*)"[[:space:]]*$/\1/' | LC_ALL=C sort
# 集合 2：docs/ 实际跟踪文件
git -c core.quotePath=false ls-files -- docs | LC_ALL=C sort
# 差集
comm -23 <索引∩docs> <实际>   # 登记未存在
comm -13 <索引∩docs> <实际>   # 存在未登记
```

计数（自跑得出）：

| 量 | 值 |
|---|---|
| 索引登记条目总数 | **265**（其中 `docs/` 下 255，仓库根 10） |
| 索引登记条目去重后 | 265 → **无重复条目**（`uniq -d` 空输出） |
| 抽取失败的 path 行 | 0（265 行全部为双引号字面量） |
| `git ls-files -- docs` | **258**（`.md` 233 / `.csv` 12 / `.json` 7 / `.yaml` 3 / `.py` 2 / 其余 1） |
| 登记了但文件不存在（docs/ 侧） | **0** |
| 存在但未登记（docs/ 侧） | **3** |
| 两侧交集 | 255 |
| 仓库根：登记 10 / 跟踪 16，登记未存在 | 0；未登记 6 项均为非文档（`.clang-format .editorconfig .gitattributes .gitignore CMakeLists.txt CMakePresets.json`）→ **不算缺陷** |
| 落盘存在性独立复核 | 逐条 `[ -f ]` 扫 265 条：0 缺失 |

**差集 A（登记未存在）= 空**。头部注释第 23–25 行自述已清除的 32 条悬空条目确实已清，该项声明与现状一致（这是本文件少数可核为真的自述）。

**差集 B（存在未登记）= 3 条，逐条**：

| # | 路径 | 定性 |
|---|---|---|
| B-1 | `docs/research/CCD_LINEAR_DEFECT_LITERATURE.md` | **确定缺陷（S1）**。它是 `docs/**/*.md`，索引自定规则第 19 行与标准 01 §3 都要求登记 → 双重违规；`docs/research/` 其余成员均已登记，非区段性豁免。 |
| B-2 | `docs/DOCUMENT_INDEX.yaml` | 缺陷（S2）。地图不登记自己；§3"现存文档全部登记"无豁免条款。且 200+ 条 `downstream` 把本文件当作引用者列出，等于承认它是被引用件，却不在自己的登记面出现。 |
| B-3 | `docs/algorithms/anchors/unresolved_registry.json` | 缺陷（S3，需裁决）。同目录同类型的 `anchor_contract.json` **已登记**（status GENERATED），说明锚点 JSON 属于登记面；`unresolved_registry.json` 单独漏登 → 索引内自相矛盾，非后缀政策的必然结果。 |

#### D. 索引内部一致性对账（全 255 条 docs 条目）

1. **上游条款标题被批量写错（最高价值发现）**。把 `upstream` 里的 `§N（标题）` 与 `ASTROCS_DESIGN.md` 真实标题表（`grep -E '^#{2,3} ' ASTROCS_DESIGN.md` 归一）逐条比对，58 个去重引用中 20 个对不上，其中 5 类是**条款号指错**而非措辞差异：

   | 索引写法 | 设计实际 | 受影响条目数 |
   |---|---|---|
   | `§8.4（模块与 ABI）` | §8.4 = 顶层结构；"模块与 ABI" = **§8.5** | **73 条**（`docs/modules/**` 全部、`docs/modules/registry/**` 全部、`docs/standards/**` 全部、`docs/api/*` 4 条、`docs/contracts/API-001.md`、`docs/contracts/PUBLIC_API.md`、`docs/architecture/abi/ABI_003*`、`docs/modules/MODULE_MAP.yaml`） |
   | `§8.1（顶层结构）` | §8.1 = 总原则：唯一 CLI 入口、阶段独立调度器；"顶层结构" = §8.4 | **9 条**（含本批 `docs/browser/HIPS_BROWSER.md`、`docs/owner/**` 无、`docs/architecture/observability/**` 3 条、`docs/contracts/RT-001.md`、`docs/plugins/infrastructure/19/21/22/23`） |
   | `§8.2（数据流形态）` | §8.2 = 阶段内：命名块内存管线与块生命周期；"数据流形态" 在设计中无此条款标题 | 2 条（含本批 `docs/owner/PIPELINE_OVERVIEW.md`） |
   | `§8.3（异步与压缩）` | §8.3 = 三个阶段调度器 | 1 条（`docs/research/COMPRESSION_CODEC_RESEARCH_PACK_TABLES.md`） |
   | `ENGINEERING_SPEC.md §4.1（管线纪律）` | 设计/工程规范 §4.1 均无"管线纪律"标题 | 1 条（`docs/contracts/DUAL_LINE_CONTRACT.md`） |
   | 计数命令 | `grep -E '^      upstream: ' docs/DOCUMENT_INDEX.yaml \| grep -cE '§8\.4（模块与 ABI）'` → 73；`§8\.1（顶层结构）` → 9；`§8\.2（数据流形态）` → 2 | |

   后果：§3 要求"从任一文档出发，能沿上游标注回到最高设计对应条款"；按 73+9 条错误条款号回溯会落到**语义不同的那一节**（模块/ABI 文档指到"顶层结构"，浏览器文档指到"总原则"），链条在形式上闭合、在语义上错位——这正是 §5"走得通"判定的反面。
   另有 15 个不匹配属**改写而非引用标题**（`§4.2（Phase1 节点流程）`、`§13（发布决定只属负责人）`、`§9（内存极简化）`、`§10（aio 是文件级唯一 I/O 边界）` 等）。§3 要求"章节号 + 标题"，改写后的括注不是标题，机器不可核 → 同一类缺陷，低一档严重度。
2. **`duty` 字段被写成路径本身（空字段伪装已填）**，4 条：`docs/TRACEABILITY.csv`（第 109 行 `duty: "docs/TRACEABILITY.csv"`）、`docs/audit/doc_classification.csv`（498）、`docs/audit/inventory.csv`（504）、`docs/audit/risk_verification_T012.csv`（510）。头部第 11 行自述 duty = "职责一句话（本文档承担什么）" → 四条全部不符合自定合同。**机械列 `doc_mechanical.csv` 未报此项**。
3. **`archived:` 空悬**（第 1514 行 = 文件末行，其下 0 条）。规则第 17–18 行仍描述"archived 区段全部 ARCHIVED_NON_NORMATIVE"，`status` 取值域也仍保留 `ARCHIVED_NON_NORMATIVE`，但实际取值分布只有 `ACTIVE_NORMATIVE 178 / ACTIVE_INFORMATIVE 63 / GENERATED 24`（自跑 `grep -oE '^[[:space:]]+status: [A-Z_]+' | sort | uniq -c`）→ 空区段 + 死枚举；同时 §2 第三条要求的"历史档案落位"在索引里没有任何承载面。
4. **非文档文件被当作文档登记**：23 条（`grep -E '^    - path: "docs/.*\.(py|csv|json|yaml)"'`），其中 2 条是可执行检查器：`docs/algorithms/anchors/check_doc_line_anchors.py`、`docs/standards/checks/check_standards_registry.py`。§2"目录承载固定角色"的九个角色里没有"脚本"；`docs/standards/checks/` 把 `eng/**` 职责复制进文档树（与 §7 落位速查冲突）。
5. **角色↔目录不符在索引层不可判**（承 D-1.1）。抽样人判：`docs/modules/acr.md`、`docs/modules/MODULE_MAP.yaml` 归"模块工作细节"✓；`docs/standards/**` 归"标准与检查"✓；`docs/research/**` 归"研究与佐证"✓；`docs/traceability/**`、`docs/architecture/*.csv`、`docs/algorithms/anchors/**` 无对应角色 → 索引与目录同时缺角色轴。

### 1.2 docs/GLOSSARY.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/GLOSSARY.md` |
| 标题 | `Astro Celestial Sphere Database（ACSD） 术语与单位词典` |
| 行数 | 29 |
| 角色 | **标准与检查**（术语判据与唯一性登记）。实际目录 `docs/` 根；§2 目标目录表不含 `docs/` 根 → **角色/目录不符**，应迁往 `docs/standards/GLOSSARY.md`（与既有 `docs/standards/DOCUMENTATION_STANDARD.md` 同域） |
| 主题 | 术语 / 单位 / 极性 / 域 / legacy alias |
| 上游 | **有**：`3: > 上游：ASTROCS_DESIGN.md 附录 A（术语）`，附录 A 真实存在（`grep -n '^## 附录 A' ASTROCS_DESIGN.md` = 819 行）。合格 |
| 下游 | 9 个活引用者：`ASTROCS_DESIGN.md`、`README.md`、`docs/algorithms/NOISE_ESTIMATION.md`、`docs/algorithms/anchors/ANCHOR_CONTRACT.md`、`docs/architecture/doc_symbol_namespaces.json`、`eng/ci/checks.json`、`eng/tools/check_agents_gov.py`、`eng/tools/check_glossary.py`、`eng/tools/quality/contracts/check_doc_symbols.py`。**本批全部"活下游"数取自同一条命令**（§4 合并简表首列注释），排除面 = 索引自身＋`docs/audit/*`＋`reports/`＋`artifacts/`＋`run/`＋`工程控制/` |
| 是否正本 | 是（术语轴）；但**它不承载定义**——第 6 行自述"以本文锚点所指的权威文件为准并回改词典"，真正的定义权威在 `docs/contracts/DATA_SEMANTICS.md` 与 `docs/science/**`。即：本文件是**索引件而非正本件** |
| 重复或重叠对象 | **与 `docs/contracts/DATA_SEMANTICS.md` §4a 重叠**（该文件属别批，只登记不结论）。举证：本文件 `13: | ivar | 逆方差=1/variance;variance=0/缺失 → ivar=0(显式不可用,状态显式标注);NaN/负 variance=产品损坏 | ADU⁻² |`；`docs/contracts/DATA_SEMANTICS.md:38` 起即 `## 4a. variance / ivar 产品`，同两条口径。差异：本文件多给单位轴（`ADU⁻²`）与 legacy alias 列，DATA_SEMANTICS 多给产品位与 HDU 语义。**属合法下钻还是重复取决于角色判定**：若 GLOSSARY 定为"索引件"则不算重复，但其第 5 行自称"唯一术语权威"与之冲突（见"与上位冲突"） |
| 元信息块 | 无 |
| 写法违规 | 无日期、无任务编号、无 commit、无 sha256、无历史叙事。**两处其它违规**：`5: 本词典是**唯一术语权威**` 与 `6: …定义只有一套` —— §1 末条"任何文档不声明自己的第二套权威顺序或'唯一口径'"的字面命中（它同时又在第 6 行把权威推给锚点文件，自相矛盾）；`19:` 锚点 `lib/algorithms/calibration/src/cosmetic_corrector.cpp#158` 是**源码行号**，§4 要求源码行号放下层文档，术语词典不承载 |
| 悬空引用 | **5 条锚点不可解析**（目标文件存在、被引章节不存在）：`10:` `docs/science/CALIBRATION.md#28`、`11:` `docs/science/DRIZZLE.md#28`、`24:` `docs/science/DRIZZLE.md`（无锚点，可解析）、`15:` `docs/science/CONTROL_WEIGHT_SNR.md#42`、`28:` `docs/science/DRIZZLE.md#46`、`29:` `docs/science/CALIBRATION.md#28`。命令与读数：`grep -cE '^#+ *28[^0-9]' docs/science/CALIBRATION.md` → **0**；`… '^#+ *46[^0-9]' docs/science/DRIZZLE.md` → **0**；`… '^#+ *42[^0-9]' docs/science/CONTROL_WEIGHT_SNR.md` → **0**；对照正例 `… '^#+ *14[^0-9]' docs/science/ASTROMETRY.md` → **2**（同一条命令形态在一个应命中对象上跑通）。`22:` `docs/science/ASTROMETRY.md#14` 与 `12/13/14/16/17/18/20/21/23/26/27:` 的 `docs/contracts/DATA_SEMANTICS.md §4a/§4/§1/§2/§5` 均可解析（§4a 实测存在） |
| 与上位冲突 | 是（两条）：① §1 末条的第二套"唯一口径"；② `10:` `adu` 行"线性信号单位"、`29:` `calibration_units` 与 §2 角色表把"科学量定义"归 `docs/science/` 不符——本文件实际承载科学量单位口径却不落 `docs/science/` |
| 处置建议 | **迁移 → `docs/standards/GLOSSARY.md`**，并把 5 处 `#NN` 锚改成可解析条款号；理由：§2"角色与目录一一对应"+ §3"每份文档能追溯到最高设计一条要点"（锚不可解析即链条断） |

### 1.3 docs/KNOWN_LIMITATIONS.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/KNOWN_LIMITATIONS.md` |
| 标题 | `Astro Celestial Sphere Database（ACSD） 已知限制（Known Limitations）` |
| 行数 | 157（46 个条目，编号 1–47 缺 14） |
| 角色 | **负责人面**（现状与未决事项的登记面）。实际目录 `docs/` 根 → **角色错放**，应迁往 `docs/owner/KNOWN_LIMITATIONS.md`（§2 负责人面目标目录已存在且已放 `RELEASE_STATUS.md`） |
| 主题 | 现行限制 / 未决缺陷 / 归属去向 |
| 上游 | **有但覆盖不足**：`3: > 上游：ASTROCS_DESIGN.md §1.3（非目标）`。§1.3 真实存在，但只承载 A/B/D 三节（"明确不做"）；**E 节 18 条（30–47）是架构与门禁判据缺口**，其真实上游是 §8.1/§8.3/§9/§12.4 与 `AGENTS.md §5/§6/§9`（本文件自己也是这么引的，如 `77:`、`87:`、`118:`）。→ §3"每份下层文档开头标明上游条款"在实质上不成立（开头条款 ≠ 内容条款） |
| 下游 | 30 个活引用者（含 19 份 `docs/modules/**`、`eng/tests/unit/mem_wire_test.cpp`、`eng/tests/unit/mosaic_window_test.cpp`、`eng/tests/unit/CMakeLists.txt`、`README.md`、`docs/ci/01_CHECKS.md`、`docs/standards/DOCUMENTATION_STANDARD.md`）。命令同 1.2。**实引形式已核**：`eng/tests/unit/mem_wire_test.cpp:3` "docs/KNOWN_LIMITATIONS.md 条目 30"、`eng/tests/unit/mosaic_window_test.cpp:156` "条目 35" |
| 是否正本 | 是（限制登记轴）；`artifacts/evidence/known-limitations-ledger/LEDGER.md`（存在，已核）是其**台账侧**，分工由第 8 行自述 |
| 重复或重叠对象 | **与 `docs/TRACEABILITY.csv` 重叠"科学门状态"轴**（两份均在本批）。举证：本文件 `12:` `1. **真实数据域**：工程/合成验收已完成；BASS + 2×2 + 3×3 与大规模真实数据的终验仍未达成（FINAL_REAL_DATA_VALIDATION 未过）`；`docs/TRACEABILITY.csv:2`（及各数据行第 15 列）`…,PRE_RELEASE_ENGINEERING_FOUNDATION,VERIFIED,V19R3 冻结`。重叠范围：同一批科学门/验收的"是否成立"；差异：本文件判"未过"，TRACEABILITY 67 行全列 `VERIFIED` 且无任何未过值 → **同主题两套状态口径直接互斥**（详见 §2 节 G2） |
| 元信息块 | **有（机械列漏报）**：`5:`–`8:` 四行 `>` 块声明"口径 / 状态词唯一口径 / 过程台账迁出 / 只登记仍成立"——§4 禁的"纪律声明 + 状态编号"元信息块。`doc_mechanical.csv` 本行 `meta_block=False` |
| 写法违规 | **机械列大面积漏报**（其 `dates=[]`、`task_ids=[]`、`history_narrative=[]` 三列对本文件全为空）。逐条：`12:` `FINAL_REAL_DATA_VALIDATION` 状态编号；`22:` `（SNR-012）`；`28:` `（L4 视觉复验，未关）`；`35:` `Phase1 产品标记载荷（D08）`；`36:` `P3-006 2600² … peak_rss≈2.6e8`；`37:` `artifacts/evidence/prerelease-v5/ISA-005/MEASUREMENTS.csv`、`evidence/v6_1_rework/TASK_LEDGER.csv`；`39:` `KI-1`/`KI-2`；`40:` `164 条声明中 53 条路径不存在；…40 条引用不存在路径（真缺口 28 / 假阳性 12）`（复算读数写进正文）；`55:` `原发现编号（如 M-4、F-03、P3-05、N1）与条目号的对照表在…`＝**"原…编号"型历史叙事**；`61:` `（丢弃安全点边界，MEMGOV-01 如实登记）`；`71:` `BFD-A11..A16 即人工补登`；`85:` 标题式 `（已收敛为唯一实现）`；`88:` `design_claim 原引的 ASTROCS_DESIGN.md §11.1 在现行设计中不存在，且 6 条登记产物（run/v6/** 与 eng/tests/contracts/product_family/evidence/mutations.json）的路径已消失`；`111:` 标题 `…的登记面已被反证`；`112:` `**已过期的一半**`；`127:` 标题 `（表述已订正，守卫不动）`；`143:` 标题 `drizzle 两处文档漂移` + `145:` `§6 **曾**写「跨线程数时 leaf 内浮点和顺序不同，不保证 bitwise」——该表述是 P15a/P22 修复前的状态` + `146:` `§7 **曾**写「生产调度路径不挂 variance 块…」`；`148:` `§6 订正为…`。命令：`grep -nE '曾|旧版|作废|废止|留痕|原先|上一版|已过期|订正' docs/KNOWN_LIMITATIONS.md` → 命中 103/104/112/114/127/129/130/145/146/148 行（其中 145/146 是 §4 字面禁词"曾"）。**§4 禁项命中但 0 处真实日期、0 处 40hex、0 处 sha256**（`grep -nE '20[0-9]{2}-[0-9]{2}-[0-9]{2}'` → 空；`grep -oE '\b[0-9a-f]{40}\b'` → 空，机械列这两项判对） |
| 悬空引用 | 4 条：`37:` `artifacts/evidence/prerelease-v5/ISA-005/MEASUREMENTS.csv` → 本节点 `[ -f ]` **ABSENT**（该条目正文明说"证据文件缺位"，属如实登记，但作为路径写入规范页即 §3 不可达指针）；`37:` `evidence/v6_1_rework/TASK_LEDGER.csv` → 非仓库相对路径（`[ -e ]` ABSENT，`git ls-files` 无匹配），前缀缺失；`39:` `eng/tests/contracts/product_family/evidence/mutations.json` → ABSENT（`[ -f ]`；同一路径也被 DOCUMENT_INDEX 第 88 行同款描述引用）；`117:` `ASTROCS_DESIGN.md §11.1` → 设计中**不存在 §11.x 任何子条款**（`grep -E '^#{2,3} ' ASTROCS_DESIGN.md` 归一后无 `11.1`；§11 下无三级标题，`grep -cE '^#{4,6} ' ASTROCS_DESIGN.md` = 0） |
| 与上位冲突 | 是：① 与 `docs/TRACEABILITY.csv` 的 `VERIFIED` 全列值互斥（见重复列）；② 第 55 行"条目号为稳定 ID"与自身编号缺 14 冲突，且 B 节源序号 15/16/17 在 CommonMark 下**渲染为 14/15/16**（列表从首项序号连续递增），稳定 ID 在渲染层不成立；③ 第 62 行大段为 ACR `0.95` 常数不合并作**裁决理由陈述**（"二者不是同一口径…有意原样保留"），属负责人面/台账语域而非"限制"语域 |
| 处置建议 | **迁移 → `docs/owner/KNOWN_LIMITATIONS.md` + 就地瘦身为现行陈述**：保留 46 条的现象/规范依据/去向三段式，把 `曾写/已过期/已被反证/订正为/原发现编号` 与台账性内容移入 `artifacts/evidence/known-limitations-ledger/LEDGER.md`（§2 第三条"过程台账落 artifacts/evidence/ 并由正本引用"）；补齐开头上游条款轴为多条款 |

### 1.4 docs/TRACEABILITY.csv

| 字段 | 判定 |
|---|---|
| 路径 | `docs/TRACEABILITY.csv` |
| 标题 | 无（CSV，仅 16 列表头） |
| 行数 | 68（1 表头 + 67 数据行；`awk -F, '{print NF}'` 全行 =16，结构完好） |
| 角色 | **标准与检查**（追溯登记面）→ 实际目录 `docs/` 根，§2 无该落位；应迁往 `docs/traceability/`（该目录**已存在**且已放 `TRACEABILITY_MATRIX.csv` 等 4 件） |
| 主题 | 需求 ↔ 权威文档 ↔ 实现文件/符号 ↔ 测试 ↔ 门 ↔ 状态 |
| 上游 | **无（CSV 无抬头区）**；索引侧 `110: upstream: "ASTROCS_DESIGN.md §0.2（详细文档层与双向索引）"` 是其唯一上游登记。按索引自定规则第 19 行只要求 `docs/**/*.md` 有上游区，本件"合规"；按 §3"每份下层文档"字面则**缺** |
| 下游 | 19 个活引用者（`docs/science/**` 9 份、`docs/traceability/TRACEABILITY_SPEC.md`、`docs/architecture/DATA_FLOW.md`、`eng/ci/checks.json`、`eng/ci/impact_map.json`、`eng/ci/polarity_evidence.json`、`eng/ci/polarity_probe.py`、`eng/tools/quality/contracts/check_traceability.py`、`eng/tools/quality/v19r3_traceability.py`、`eng/tools/quality/build_v19r2_package.py`、`eng/tools/doccheck/doc_hygiene_exempt.json`）。索引 `111:` 记"等 20 处"，与本批 8 份一样属截断计数 |
| 是否正本 | **否（角色冲突）**：`docs/traceability/TRACEABILITY_MATRIX.csv` 承载同一轴且更细（分层 + 逐层 status），索引把两者都登记为 ACTIVE_NORMATIVE → 同一主题两份 ACTIVE |
| 重复或重叠对象 | **与 `docs/traceability/TRACEABILITY_MATRIX.csv`（别批）同主题两份登记**。举证：本文件 `2: SCI-UPM-WEIGHT-001,science,…,docs/science/PHASE2_UPM.md,…,lib/algorithms/coverage/src/upm.cpp,…,PRE_RELEASE_ENGINEERING_FOUNDATION,VERIFIED,V19R3 冻结`；`docs/traceability/TRACEABILITY_MATRIX.csv:2: MOD-astrocs-catalog-gaia,service,lib/infrastructure/gaia_xpsd_client/src/gaia_client.h,SCI-AST-001,docs/science/ASTROMETRY.md,VERIFIED,ALG-GAIA-001,docs/algorithms/GAIA_QUERY.md,VERIFIED,…`。重叠范围：`权威文档 ↔ 实现路径 ↔ 状态` 三元映射；差异：本件 67 行、ID 前缀 `SCI-/ALG-/DATA-/ENG-/TEST-`、单一 `status` 列；彼件 30 行、ID 前缀 `MOD-/SCI-/ALG-/DATA-/API-/ARCH-/SRC-/TEST-/EVIDENCE-`、每层各一个 `*_status` 列并由 `TRACEABILITY_LAYERS.csv` 规定合法值。两套 ID 空间互不映射 → 追溯一份文档要到两处登记 |
| 元信息块 | 无（CSV） |
| 写法违规 | `65:`–`68:` 四行 `notes` 列含**日期 + 任务编号 + 历史叙事**：`T101 frozen；宿主 2026-09-17 按现树重指（ARCH-001 后 lib/algorithms/psf，原行指向已不存在的 lib/dynamic_psf）`（同型见 66/67/68，含"原行指向已不存在的"）。机械列 `dates=[65,66,67,68]` **判对**，`task_ids=[]` **漏报**（T101/T107/T108/T109/ARCH-001 全未识别）；另有 `2:`–`64:` 的 `V19R3 冻结`/`V19R2 有效`/`PR#1 保留`/`V19R3 展开范围行` 版本批次标记 61 处，机械列同样未识别 |
| 悬空引用 | **0**（正面结论，且需说明工具差异）：`authority_doc` 列 19 个去重路径全部存在（逐条 `[ -f ]`）；`implementation_files` 列、`test_files` 列拆 `;` 后逐条存在性检查 **0 缺失**；抽 4 个符号锚做正例：`git grep -l -w p2_upm_raw_weight/compute_overlap_area_g_ctx_cached/snr_noise_model_v1/dpsf_fit -- lib` 均命中声明文件 |
| 与上位冲突 | **是，两处硬冲突**：① `status` 列 67/67 = `VERIFIED`、`release_gate` 列 67/67 = `PRE_RELEASE_ENGINEERING_FOUNDATION`、`requirement_type` 列 67/67 = `science`（`tail -n +2 … \| awk -F, '{print $2"\|"$14"\|"$15}' \| sort \| uniq -c` → `67 science\|PRE_RELEASE_ENGINEERING_FOUNDATION\|VERIFIED`）。三个"判别列"零信息量，且与 §12.5 状态阶梯（唯一口径，多档）不同构；其中 `requirement_type` 把 `docs/architecture/ERROR_MODEL.md`、`IO_AND_ATOMICITY.md`、`THREADING_MODEL.md`、`OWNERSHIP_AND_LIFETIME.md` 的行也标成 `science` = 与 §2 角色分层冲突。② 与 `docs/KNOWN_LIMITATIONS.md` 第 12 条互斥（后者判终验未达成，本件把同一批门列 VERIFIED）→ §1"下位与上位冲突时以上位为准；冲突本身是缺陷"。③ 权威轴只到 `docs/science/**`，**无一列指回 ASTROCS_DESIGN 条款** → §3"每份文档、每个机制都能追溯到最高设计的一条要点"在本件不可达 |
| 处置建议 | **迁移 → `docs/traceability/TRACEABILITY_ROOT.csv`，并与 `TRACEABILITY_MATRIX.csv` 合并**（合并方向：以 MATRIX 的分层列为骨架，本件的 67 个 requirement 行并入其 `sci/alg/data/api/arch` 轴；补 `design_clause` 列），同时把三常数列换成 §12.5 合法取值；理由：§2"同一角色内同一主题只有一份正本"+ §3 双向索引要求回溯到最高设计 |

### 1.5 docs/TROUBLESHOOTING.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/TROUBLESHOOTING.md` |
| 标题 | `Astro Celestial Sphere Database（ACSD） 排障手册` |
| 行数 | 42 |
| 角色 | **操作与运营**（诊断/排障）→ §2 目标目录含 `docs/diagnostics/`；实际在 `docs/` 根 → **角色错放**，应迁往 `docs/diagnostics/TROUBLESHOOTING.md`——而该路径**已被本批第 19 项占用**（见 §2 节 G3 的同名冲突） |
| 主题 | 症状 → 定位 → 修复命令 |
| 上游 | **有**：`3: > 上游：ASTROCS_DESIGN.md §7.2（配置、事件与退出码）`，§7.2 标题逐字一致 ✓ |
| 下游 | 2 个活引用者：`README.md`、`eng/tools/astrocs_diagnose.py`。命令同 1.2 形态 |
| 是否正本 | 否（与 `docs/diagnostics/TROUBLESHOOTING.md` 同题两份，见重复列） |
| 重复或重叠对象 | **`docs/diagnostics/TROUBLESHOOTING.md`（本批 1.19）**：两处段落对举——本文件 `5:` `## Symptom → 定位 → 修复` + `16:` `| stage2 ZERO_VALID_WEIGHT | INTEGRATE 日志 | 帧无 ivar 产品且 support=0 | 确认 Phase1 输出了 variance/ivar; 重跑 Phase1 生成含 variance/ivar 的帧 |`；`docs/diagnostics/TROUBLESHOOTING.md` 同名同轴（详见 1.19 引文）。重叠范围：症状—错误码—处置对照；差异：本件按 CLI 阶段码列症状表并给可执行命令，彼件按模块/退出码给处置细则。**两份都是现行件 → 同主题两份正本** |
| 元信息块 | 无 |
| 写法违规 | 无日期/编号/sha/历史叙事。两处非 §4 类缺陷：`9:` 在文档里内联 PowerShell 环境改写 `$env:Path="C:\msys64\mingw64\bin;$env:Path"`（机器特定绝对路径写进现行文档，与 §4"只写现行设计要怎样"及 AGENTS.md §3"不硬编码"相悖）；`13:` `确认 SNR 模块为当前版本`（无指向的相对时态表述，"当前版本"在文档层无定义点） |
| 悬空引用 | **4 条命令路径对仓库不存在**（机械列 `dangling_links=[]` → **漏报**）。`29:` `lib\snr_estimator\cpp\test\noise_model_science_test.exe`、`31:` `lib\healpix_db\healpix_drizzle\tests\variance_propagation_test.exe`、`33:` `lib\phase2\build\phase2_synthetic_gate.exe`：三者的宿主目录 `lib/snr_estimator`、`lib/healpix_db`、`lib/phase2` **跟踪文件数 = 0**（`git -c core.quotePath=false ls-files -- <dir> \| wc -l` → 0/0/0），即与 AGENTS.md §7 的 `lib/algorithms/`、`lib/infrastructure/` 落位不符的**本机历史构建残留**；`.exe` 在本节点磁盘上确实存在（`[ -f ]` 三真）→ **这是"用文件存在判存在"的典型假阴**，必须用 `git ls-files` 判；`35:` `py -3.12 tools\astrocs_diagnose.py` → `tools/astrocs_diagnose.py` **不存在**（`[ -f ]` ABSENT），真实件在 `eng/tools/astrocs_diagnose.py`（存在）。正例对照：`25:` `.\eng/build/toolchain.ps1` 存在 ✓ |
| 与上位冲突 | 是：`29/31/33` 三条命令的落位与 AGENTS.md §7 目录落位速查、`ENGINEERING_SPEC.md §7` 的 `lib/` 子目录清单冲突（清单只有 `algorithms/ include/ third_party/ infrastructure/`，`docs/KNOWN_LIMITATIONS.md:92` 亦已如此登记） |
| 处置建议 | **合并**：把本件的命令面与症状表并入 `docs/diagnostics/TROUBLESHOOTING.md`（迁移目标 `docs/diagnostics/`，删除本路径），并先把三条 exe 命令改指现树可再生成的目标名；理由：§2"同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除" |

### 1.6 docs/VERSIONING.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/VERSIONING.md` |
| 标题 | `Astro Celestial Sphere Database（ACSD） 版本合同（VER-001）` |
| 行数 | 68 |
| 角色 | **合同说明**（版本注入链与字段语义 + 机器判据）→ §2 目标目录 `docs/contracts/`；实际在 `docs/` 根 → **角色错放**，应迁往 `docs/contracts/VERSIONING.md` |
| 主题 | 版本号唯一源 / 构建指纹 / 同步矩阵 / 豁免清单 |
| 上游 | **有**：`3: > 上游：ASTROCS_DESIGN.md §13（版本与发布权）`，标题逐字一致 ✓ |
| 下游 | 23 个活引用者（`CMakeLists.txt`、`docs/api/MANIFEST_VERIFY_V1.md`、`docs/architecture/THREADING_MODEL.md`、`docs/ci/01_CHECKS.md`、`docs/owner/RELEASE_STATUS.md`、`eng/ci/check_version.py`、`eng/ci/check_build_provenance.py`、`eng/tools/gen_version.py`、`eng/tools/gen_build_stamp.py`、`lib/include/astrocs/core/build_stamp.h`、`lib/infrastructure/scheduler/src/{build_stamp,module_adapters}.cpp` 等）→ 本批下游最厚的文件之一 |
| 是否正本 | 是（版本轴正本；根 `VERSION` 是数值唯一源、本件是其合同） |
| 重复或重叠对象 | 与 `docs/owner/RELEASE_STATUS.md`（别批）在"版本/发布态"轴相接但不重复：本件规定注入链与判据，彼件登记发布状态。合法分层。**与本件内部第 63 行和其描述的机器门重复**（见冲突列） |
| 元信息块 | 无独立元信息块；但 H1 内嵌 `（VER-001）` 文档编号 = §4 禁的"文档 ID"，只是换了位置 |
| 写法违规 | `1:` `版本合同（VER-001）`（文档 ID，机械列 `task_ids=[]` **漏报**）；`9:` `V6 控制包时代的冻结基线值是历史值，不再在本文件复制（V81-ADOPT-006 起活动文档不把旧版本写成当前值）`＝历史叙事 + 任务编号（机械列 `history_narrative=[9]` 判对，`task_ids` 漏报 `V81-ADOPT-006`）；`19:` `ABI-001 / API-002 冻结时置 1`（冻结叙事 + 编号）；`23:` `**事实（一手证据 run/RUN-PROVENANCE-01/REPORT.md §A）**`（任务编号 + 指向 gitignore 的 `run/`）；`23:`–`47:` 全节以 `RUN-PROVENANCE-01` 编号为纲；`44:` `（干净工作树零读盘，实测 < 50ms）`（实测数值写进合同）；`55:` `CLI-003`、`56:` `CHK-BUILD-PROVENANCE`、`54:` `LNX-005/WIN-009`；`64:` `（eng/tests/version 有固定试金石）`。机械列 `sha256=True` 系**误报**（本文件 35 行的 `sha256` 是构建指纹字段的名称，不是被登记的摘要值） |
| 悬空引用 | 3 条：`18:` `schema 见 schemas/version.schema.json` → `schemas/` 目录不存在（`[ -e ]` ABSENT，`git ls-files` 无匹配）；真实件 `eng/contracts/schemas/version.schema.json` 存在（`git grep -l cli_schema_version` 命中），差一个 `eng/contracts/` 前缀；`63:` 声明扫描面 `docs/ schemas/ eng/tools/ launch/ eng/tests/` 中 `schemas/`、`launch/` **两目录在仓库不存在**；`23:` `run/RUN-PROVENANCE-01/REPORT.md` 属 gitignore 面（`run/` 不入库），正式文档指向不可再生的过程目录 |
| 与上位冲突 | **与它描述的机器门不符（本批最有价值的实测）**：本件 `63:` 写"扫描 `docs/ schemas/ eng/tools/ launch/ eng/tests/` 与根级 README/CHANGELOG/build.sh/toolchain.ps1"；实现（`eng/tools/check_version_consistency.py:39-43`）是 `SCAN_ROOTS = ["docs", "eng/contracts/schemas", "eng/tests"]` + `SCAN_FILES = ["eng/build/build.sh", "eng/build/toolchain.ps1", "README.md", "VERSION", "eng/tools/gen_version.py"]`。四条偏差：① `schemas/` → 实为 `eng/contracts/schemas`；② `launch/` → 完全不在扫描面且目录不存在；③ `eng/tools/` → 实际只扫其中 1 个文件（`gen_version.py`），不是整棵树；④ `CHANGELOG` → 被扫描器注释明确排除（第 40–41 行"CHANGELOG.md 是 history 命名空间驻留点…不进本检查"），本件却列为被扫对象。另 `7:` 把 VERSION 唯一源的上游写成 `ASTROCS_DESIGN.md §12`（§12 = 验证体系；版本与发布权 = §13，与本件第 3 行自标冲突） |
| 处置建议 | **迁移 → `docs/contracts/VERSIONING.md` + 就地订正**：删除 H1 的 `（VER-001）`、把第 63 行改成与 `SCAN_ROOTS/SCAN_FILES` 逐字一致、第 7 行 §12 改 §13、`run/` 证据引用改指 `artifacts/evidence/`；理由：§3"双向可追溯"（文档描述的机器面与机器面不一致即链条断）+ §4 无历史叙事与编号 |

### 1.7 docs/audit/doc_classification.csv

| 字段 | 判定 |
|---|---|
| 路径 | `docs/audit/doc_classification.csv` |
| 标题 | 无（表头 `doc_path,authority,status,notes`） |
| 行数 | 96（1 + 95 条登记） |
| 角色 | 索引/分类件 → 与 `DOCUMENT_INDEX.yaml` 同轴。§2 无"第二索引"角色；实际在 `docs/audit/` |
| 主题 | 文档路径 ↔ authority 分类 ↔ present |
| 上游 | 无（CSV）；索引侧 `499: upstream: "ASTROCS_DESIGN.md §0.2（详细文档层与双向索引）"` |
| 下游 | **0 个活引用者**（命令见下）。仅 `docs/DOCUMENT_INDEX.yaml` 自引 + `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv` + `reports/v19r2/*`，后三者分属索引自述与被排除的过程面。二次检索：basename `doc_classification.csv` 在 `:!reports :!artifacts :!run :!工程控制 :!docs/audit` 面 → **0 命中**；正例对照同型命令查 `02_PIPELINE.md` → 命中 `docs/ci/CI_SPEC.md:140`，说明命令能命中相对链接，0 即真 0 |
| 是否正本 | **否——它是第二份索引地图，应判删除或降级为证据** |
| 重复或重叠对象 | `docs/DOCUMENT_INDEX.yaml`（本批 1.1）。举证：本件 `9: docs/algorithms/CALIBRATION_ALGORITHMS.md,AUTHORITATIVE,present,`；索引 `127:-131: - path: "docs/algorithms/CALIBRATION_ALGORITHMS.md" / status: ACTIVE_NORMATIVE / duty: "定标算法推导与逐符号实现锚定" / upstream: "ASTROCS_DESIGN.md §4.2（Phase1 节点流程）"`。重叠范围：**同一文档路径的分类登记**；差异：本件用 `{AUTHORITATIVE, CANDIDATE, HISTORICAL}` 三值 + 一个恒为 `present` 的 status 列，索引用 `{ACTIVE_NORMATIVE, ACTIVE_INFORMATIVE, GENERATED}` 三值 + duty/upstream/downstream。→ 直接违反 §3"DOCUMENT_INDEX.yaml 是全文档集**唯一**索引地图"与 §1"任何文档不声明自己的第二套权威顺序" |
| 元信息块 | 无（CSV） |
| 写法违规 | 表体本身即"分类台账"：`notes` 列 95 行全空（无信息列）；`authority` 值 `HISTORICAL` 承载历史叙事（§4 要求历史不进正式文档层）。文件名 `doc_classification` 无编号，合规 |
| 悬空引用 | **20/95 条 doc_path 指向不存在的文件，而 `status` 列仍写 `present`**（`while read -r p; do [ -e "$p" ] \|\| echo GONE; done`）：`docs/API_REFERENCE.md`、`docs/ARCHITECTURE.md`、`docs/README-DOCS.md`、`docs/RELEASE_STATUS.md`、`docs/backlog/NEXT_STAGE.md`、`docs/history/README.md`、`docs/history/v19/{ARCHITECTURE,BUILD_RELEASE,CONFIG_REFERENCE,DATA_CONTRACTS,DIAGNOSTICS,DRIZZLE,ERROR_TAXONOMY,MODULE_INDEX,PERFORMANCE,PHASE2,README,SCIENTIFIC_ALGORITHMS,SNR_NOISE_MODEL,TESTING_AND_ACCEPTANCE}.md`（共 15 条 history 件）。75/95 存在。机械列 `dangling_links=[]` → **漏报 20 条**（该列只判 markdown 链接语法，不判 CSV 正文路径，属工具覆盖面缺口） |
| 与上位冲突 | 是（三条）：① §3 唯一索引地图；② 它把 `docs/KNOWN_LIMITATIONS.md`、`docs/TROUBLESHOOTING.md`、`docs/DEVELOPER_GUIDE.md` 判 `CANDIDATE`、索引判 `ACTIVE_INFORMATIVE`，把 `docs/browser/HIPS_BROWSER.md` 判 `AUTHORITATIVE`、索引判 `ACTIVE_INFORMATIVE` → 同一文档两套权威级；③ 自身 `status` 列恒 `present` 且对已删文件仍写 `present`（与 §12.5 状态阶梯无关的第二套状态词，违反 §1"不声明第二套权威顺序"） |
| 处置建议 | **删除**（其信息面被重建后的 `DOCUMENT_INDEX.yaml` 完全覆盖；需要历史分类快照则迁 `artifacts/evidence/`）；理由：§3"全文档集唯一索引地图"+ §2 第三条"过程台账…需要留证的台账落 `artifacts/evidence/` 并由正本引用" |

### 1.8 docs/audit/inventory.csv

| 字段 | 判定 |
|---|---|
| 路径 | `docs/audit/inventory.csv` |
| 标题 | 无（表头 15 列 `module,science_doc,algorithm_doc,arc_doc,public_headers,prod_sources,config,tests,memory,sci_status,alg_status,hdr_check,src_check,tst_exists,memory_exists`） |
| 行数 | 14（1 + 13 模块行；`awk -F, '{print NF}'` 全 15，无断列） |
| 角色 | 过程台账（模块↔文档↔源码↔探针快照）→ 应落 `artifacts/evidence/`；现处 `docs/audit/` = **层级倒置**（正式文档层放检查快照） |
| 主题 | 每模块的科学/算法/架构文档与源码/测试/内存文件对应 |
| 上游 | 无（CSV）；索引 `505: upstream: "ASTROCS_DESIGN.md §0.2（详细文档层与双向索引）"` |
| 下游 | **0 个活引用者**：`git grep -l -F docs/audit/inventory.csv -- ':!docs/DOCUMENT_INDEX.yaml' ':!docs/audit/*' ':!reports/*' ':!artifacts/*'` → 空。二次检索陷阱已做：basename `inventory.csv` 在活面命中 16 文件，但**逐条核对全是同名他件**（`docs/architecture/api_inventory.csv`、`eng/tools/quality/check_source_inventory.py` 等）→ 不能当引用；按全路径检索的 0 才是真值。这正是前言 §5.2 说的"整串检索不到 / 短串误命中"两侧 |
| 是否正本 | 否（过程快照，非现行口径件） |
| 重复或重叠对象 | 与 `docs/modules/MODULE_MAP.yaml`（别批）同轴（模块↔文档↔源码）+ 与 `docs/TRACEABILITY.csv` 同轴（模块↔科学/算法文档↔状态）。举证：本件 `2: Calibration,docs/science/CALIBRATION.md,docs/algorithms/CALIBRATION_ALGORITHMS.md,docs/architecture/THREADING_MODEL.md,lib/algorithms/calibration/include/astro_calibration.h,lib/algorithms/calibration/src,…,AUTHORITATIVE,AUTHORITATIVE,hdr_exists=True,src_files=7,True,True`；`docs/TRACEABILITY.csv:20: SCI-CAL-001,science,校准科学门（bias/dark/flat/cosmetic）,docs/science/CALIBRATION.md,,calibration,…,lib/algorithms/calibration/src/calibrator.cpp;…`。差异：本件按模块列文档与文件计数，TRACEABILITY 按需求行列门与测试 → 两套登记面覆盖同一"模块↔权威文档"关系 |
| 元信息块 | 无（CSV） |
| 写法违规 | 表体把检查器读数原样写进 docs（`hdr_check=hdr_exists=True`、`src_check=src_files=7`、`tst_exists`、`memory_exists`）＝机器门产物驻留正式层；`sci_status/alg_status` 全 13 行 `AUTHORITATIVE` 无信息量 |
| 悬空引用 | 1 条：`7:` 行 `public_headers` 列 `lib/algorithms/drizzle/healpix_drizzle/include` **不存在**（逐条 `[ -e ]` 检查唯一缺失项），同行 `hdr_check=hdr_exists=False` 与 `sci_status=AUTHORITATIVE` 并存 → 快照自证缺陷仍判权威。另有语义错列：`12:` ACR 行 `arc_doc` 列填的是头文件 `lib/infrastructure/acr/include/astro/compute/acr.hpp`（该行 `algorithm_doc` 缺位导致自第 3 列起右移一格，`science_doc` 位填了 `docs/modules/acr.md` 这个"模块工作细节"件），且 `13:` CLI/config 行 `science_doc` 与 `algorithm_doc` 两列同填 `docs/development/CONFIG_SCHEMA.md` |
| 与上位冲突 | 是：`8/9/10/11/13:` 四行至五行 `prod_sources` 明确给了单个 `.cpp` 文件而 `src_check=src_files=0`，与同行 `sci_status=AUTHORITATIVE` 自相矛盾（快照的 src 计数语义为目录 glob，填文件即恒 0）；`memory` 列 13 行全 `True` 但 `7:` 指向与 prod 树不同源的 `lib/infrastructure/aio/healpix_db/memory.md` |
| 处置建议 | **删除或迁 `artifacts/evidence/`**；理由：§2 第三条"历史档案、过程台账不放在正式文档层；需要留证的台账落 `artifacts/evidence/` 并由正本引用" |

### 1.9 docs/audit/risk_verification_T012.csv

| 字段 | 判定 |
|---|---|
| 路径 | `docs/audit/risk_verification_T012.csv` |
| 标题 | 无（表头 `ID,风险,源码位置,构建证据,运行/文档证据,结论,Finding,状态`） |
| 行数 | 8（1 + R1–R7） |
| 角色 | 过程台账（风险验证轮次结论）→ 应落 `artifacts/evidence/`；现处 `docs/audit/` = 层级倒置 |
| 主题 | 7 项风险的取证与结论 |
| 上游 | 无（CSV）；索引 `511: upstream: "ASTROCS_DESIGN.md §0.2（详细文档层与双向索引）"`（与内容无关：本件讲 OpenMP/ACR 接线与内存分配，不涉索引）→ **上游标注与实际主题不符** |
| 下游 | **0 个活引用者**（命令同 1.7/1.8 形态，含 basename `risk_verification_T012.csv` 二次检索 → 0 命中） |
| 是否正本 | 否 |
| 重复或重叠对象 | 与 `docs/architecture/THREADING_MODEL.md`（别批）重叠 R1/R5 的"默认串行/条件并行"口径。举证：本件 `2: R1,Phase2 是否仍串行；OpenMP 接线一致性,lib/algorithms/coverage/src/sampler.cpp:30 #if P2_ENABLE_OPENMP; 620 串行默认,…,默认串行属实，文档与源码一致；并行需显式构建，非静默切换,无,VERIFIED_SERIAL`；`docs/TRACEABILITY.csv:12: ENG-THREAD-001,science,线程模型契约（THREADING_MODEL）,docs/architecture/THREADING_MODEL.md,…,G1ProductionWiringTruth,…,VERIFIED,V19R2 有效`。重叠：同一"线程模型是否已核"的事实由三处登记（本件 R1/R5、TRACEABILITY ENG-THREAD-001、THREADING_MODEL 本体）；差异：结论词各不相同（`VERIFIED_SERIAL` / `VERIFIED` / 文档自述） |
| 元信息块 | 无（CSV）；**但文件名本身含任务编号 `T012`** → §4"正文无任务编号"在路径层即被破 |
| 写法违规 | `3:` 结论列 `VERIFIED_GATING_CORRECT（已按 §9.73 A44 作废：该概念不存在）`＝**"作废"字面禁词** + 编号 `A44` + 悬空条款；`7:` R6 行整行是历史叙事与机器读数：`memory.md:5  16线程+64GB; 12 f:\Astro dev\...`、结论 `属历史描述残留，需后续文档同步但不影响科学`、状态 `OPEN_P2`；`7:` Finding 列 `FIND-INV-001 P2`；`2/4/5:` `需 L7 构建矩阵证明` / `需 L9 四路实测证明` / `需 L9 profile 分段计时证明` / `需 L9 alloc 计量`（验证层级编号 + 未决口）；`6/7/8:` `PASS`/`NEEDS_L9_ALLOC`/`OPEN_P2`。机械列 `history_narrative=[3]` 只抓到"作废"，**漏报第 7 行"历史描述残留"** |
| 悬空引用 | 1 条确定：`3:` `§9.73` —— `ASTROCS_DESIGN.md` §9 无子条款（`grep -E '^#{2,3} ' ASTROCS_DESIGN.md` 无 `9.x`；`grep -cE '^#{4,6} '` = 0）→ 指向不存在章节；`7:` `f:\Astro dev\...` 是**本机绝对路径**写入仓库（违反"在仓库内工作，不写死服务器绝对路径"） |
| 与上位冲突 | 是：状态列用 `VERIFIED_SERIAL / VERIFIED_GATING_CORRECT / NEEDS_L9_PROFILE / NEEDS_L9_ALLOC / VERIFIED_CONDITIONAL / PASS / OPEN_P2` 七种自造值 —— §12.5 状态阶梯之外另立第二套口径（§1 末条禁），并与前言 §5.6 的状态串用同型（`未实现`≠`未接线`≠`待实测`）；`4:` R3 与 `5:` R4 把"待运行时计量"写成结论行，属未决项冒充现行结论 |
| 处置建议 | **迁往 `artifacts/evidence/risk-verification/T012/`（并从 docs/ 删除；文件名去 `T012`）**；理由：§2 第三条过程台账落证据锚 + §4 无编号/无历史叙事 + §1 不另立状态口径 |

### 1.10 docs/browser/HIPS_BROWSER.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/browser/HIPS_BROWSER.md` |
| 标题 | `HiPS Browser` |
| 行数 | 49 |
| 角色 | 内容 = 某生产模块的职责边界/渲染架构/CLI 旗标 → **模块工作细节**（§2 目标目录 `docs/modules/`、`docs/plugins/`）。实际在 `docs/browser/`，该目录**不在 §2 目标目录表内**且只装这一份文件 → **角色错放 + 单文件目录**。应迁往 `docs/modules/hips_browser.md` |
| 主题 | HiPS 浏览器：渲染管线 / DisplayTransformState / Auto STF |
| 上游 | **有但条款号错**：`3: > 上游：ASTROCS_DESIGN.md §8.1（顶层结构）`——§8.1 实际标题是"总原则：唯一 CLI 入口、阶段独立调度器"，"顶层结构"是 **§8.4**。与索引 `516: upstream: "ASTROCS_DESIGN.md §8.1（顶层结构）"` 同源同错（见 1.1 节 D-1）→ 按此回溯会落到错误一节 |
| 下游 | **0 个活引用者**。`git grep -l -F docs/browser/HIPS_BROWSER.md -- ':!docs/DOCUMENT_INDEX.yaml' ':!docs/audit/*' ':!reports/*' ':!artifacts/*'` → 空；二次检索 basename `HIPS_BROWSER.md`（排除同前缀）→ 空；`git grep -n -E 'HIPS_BROWSER|browser/HIPS' -- docs ':!docs/DOCUMENT_INDEX.yaml' ':!docs/audit'` 只命中 `docs/validation/SCIENCE_FREEZE.md:45: HIPS_BROWSER_BASE      = FROZEN`（那是冻结项名，不是对本文件的引用）。正例对照：同型命令查 `02_PIPELINE.md` 命中 `docs/ci/CI_SPEC.md:140` → 命令能命中相对写法，0 即真 0。**孤儿文档** |
| 是否正本 | 否（与 `docs/modules/healpix_browser_qt.md` 同模块两份，见重复列） |
| 重复或重叠对象 | **`docs/modules/healpix_browser_qt.md`（别批，路径在此登记）**：本件 `29:-33: - `Auto Global`（默认）：…首次扫描**全部 leaf tiles**（每 tile 64×64 均匀采样…）后取 **p1/p99** 分位…`；彼件 `39: tile cache + async I/O；STF **0.5%/99.5%** 分位。`。重叠范围：同一模块的 STF 口径；差异：**分位数两套值互斥**（p1/p99 vs 0.5/99.5），且两份件都登记在索引（前者 ACTIVE_INFORMATIVE、后者 ACTIVE_NORMATIVE）→ 同主题两份 + 口径冲突 |
| 元信息块 | 无（但第 5/13 行标题内嵌"（V14 冻结）""V15："版本批次号，语义等同文档 ID 前缀） |
| 写法违规 | `5:` `## 渲染架构（V14 冻结）`；`13:` `## V15：唯一 DisplayTransformState`；`23:` `support 层固定 **linear [0,1]**（V15 移除 sqrt(support)）`＝**"移除"型历史叙事**；`30:` `- V14 v2：Auto Global 首次扫描…`；`31-32:` `与 V13 fixed stretch（1%/99%）视觉一致`＝以旧版本作对照的历史叙事。命令：`grep -nE 'V1[3-5]' docs/browser/HIPS_BROWSER.md` → 5/13/23/30/31/32 六处。**机械列 `task_ids=[]`、`history_narrative=[]` → 全部漏报**（其编号正则只认 `EXP-/RELEASE-/FIX-/AUD-` 前缀族，`V13/V14/V15` 不在内） |
| 悬空引用 | 0（逐项核过，且这是否面结论需记录）：`15:` `core/stf_engine.h` → 实际 `lib/infrastructure/hips_browser/healpix_browser_qt/core/stf_engine.h` 存在；`18:` `HipsSkyView::rasterize` → `git grep -l -e HipsSkyView -- lib` 命中 `.../core/hips_sky_view.cpp`；`16-17:` `MainWindow/STFPanel/STFBar`、`38:` `browser_cli --stf-bench`/`--stf-lock-probe` → `git grep -l -E 'browser_cli|stf-bench|stf-lock-probe'` 命中 `lib/infrastructure/hips_browser/healpix_browser_qt/app/browser_cli.cpp`。**但**：`38:` 把浏览器入口写成 `browser_cli`，与 §11/`docs/api/CLI_PROTOCOL_V1.md:14-20` 的唯一命令树（`acsd normalize/mosaic/export/help/--version/doctor/benchmark`）不同名——本文件未说明它是可选构建的独立程序，读者按"CLI"理解即与唯一入口冲突 |
| 与上位冲突 | 是：① 上游条款号错（§8.1↔§8.4）；② 与 `docs/modules/healpix_browser_qt.md` 的 STF 分位口径互斥（见重复列）；③ `docs/audit/inventory.csv:14` 把本件列在该模块的 `science_doc` 位（`HiPS Browser STF,docs/browser/HIPS_BROWSER.md,docs/algorithms/HEALPIX_MAPPING.md,…`）→ 一份浏览器使用件被当**科学定义**登记，角色串轴 |
| 处置建议 | **迁移 → `docs/modules/hips_browser.md`，并与 `docs/modules/healpix_browser_qt.md` 合并**（保留模块件的端口/生命周期骨架，把本件的 STF/渲染细则并入其"性能特征/显示"节；先由科学侧裁定 p1/p99 与 0.5/99.5 哪个是现行值）；理由：§2"每份文档属于且仅属于一个角色…角色与目录一一对应"+ §1"下位与上位冲突时以上位为准；冲突本身是缺陷" |

### 1.11 docs/ci/01_CHECKS.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/ci/01_CHECKS.md` |
| 标题 | `CI 检查项目录（CI Checks Registry）` |
| 行数 | 273 |
| 角色 | **标准与检查** ✓（§2 目标目录含 `docs/ci/`）→ **目录与角色一致**，本批少数无角色错放者之一 |
| 主题 | 机器检查项注册表的文档侧登记 |
| 上游 | **有**：`3: > 上游：ASTROCS_DESIGN.md §12.4（验证层级与四层验收）`，标题一致 ✓ |
| 下游 | 76 个活引用者（同一条计数命令，见 §4 简表首注），本批最厚：`ACCEPTANCE_SPEC.md`、`ASTROCS_DESIGN.md`、`ENGINEERING_SPEC.md`、`docs/contracts/{CONFIG_CONTRACT,LOG_AND_ERROR_CONTRACT}.md`、`docs/design/LOG_AND_ERROR_SYSTEM.md`、`docs/modules/MODULE_MAP.yaml`、`docs/standards/DOCUMENTATION_STANDARD.md`、`eng/ci/**`（含 `checks.json`、`run_checks.py`、`validate_registry.py`、15+ 个 `check_*.py`）、`eng/tools/**`（40+ 个 checker/生成器）、`lib/algorithms/coverage/src/stage2_common.cpp`、`lib/infrastructure/scheduler/src/module_adapters.cpp` |
| 是否正本 | 是（检查项登记轴正本；`eng/ci/checks.json` 是其机器侧） |
| 重复或重叠对象 | 与 `docs/ci/03_GATES.md §3` **重复给同一批 ID 派门禁级**（见"与上位冲突"给两处段落）；与 `docs/ci/CI_SPEC.md §3` 重复"检查项分类总表"（CI_SPEC `115-124:` 的八类摘要表 ↔ 本件 §2 的 142 行明细，属合法下钻，不计重复） |
| 元信息块 | 无 |
| 写法违规 | 编号密集：`28:` `LEG-002..004`；`34-35:` `D1/D2/PREEXISTING/SCI-5xx`；`38-39:` `FZ-…`；`54:` `（裁决 B）`＝**负责人裁决引述**（§4 明禁）；`55:` `CLI-003`/`CFG-001`；`98-99:` `§9.73 A44`＋`**已按 §9.73 A44 作废**`；`102:` `FIX-202；N1/N2/P1/N3 共 36 断言`；`104:` `CHK-FIX203-PROMOTED-KEYS`；`105:` `FIX-205`；`111-113:` `FIX208`/`FIX-406` 与 `run/FIX-406/WINDOWS_CANCEL_PLATFORM_LIMITS.md §3`（run 面）；`115-116:` `RELEASE-04 归档…（D-10 恒真门已堵）`＝历史叙事 + 编号；`121:` `230 个 / 78 个注册项` 读数；`171:` `（V19R2 注释卫生扫描…产物不再落 reports/**）`＝历史叙事；`180-219` 整节"未注册检查器处置台账"＝过程台账 + `185:` "见对应任务的回执，不在此复述"、`213:` "实际处置（证据见对应任务回执）"、`214-219:` "已删除/保留并上呈/超出本项文件域"；`198:` `instructions 统计量订正为真实指令行计数`＝"订正"叙事；`239:` `（并发轮次占用），故本轮只落地实现 + 自检 + 本行登记说明`＝轮次过程语域。**机械列 dates=[34,40,72,98,99,102,109] 中 40/72/109 为日期误判面需以其自身口径复核，而上述 FIX-202/FIX208/RELEASE-04/V19R2 全未进 `task_ids`（仅列 []）→ 大面积漏报** |
| 悬空引用 | **3 类**：① `98:`/`99:` `§9.73`（设计与工程规范均无 §9.x 子条款，`grep -cE '^#{4,6} ' ASTROCS_DESIGN.md` = 0，§9 无三级标题）→ 与 `docs/audit/risk_verification_T012.csv:3`、`docs/development/CONFIG_SCHEMA.md:35/58` 同一悬空锚被四处复用；② `212:` `eng/tools/quality/check_source_index_v61.py` 与 `214:` 自称**已删除**（机械列把它判为 dangling ✓ 正确，是本批机械列少数判对项）——同一行仍把它列在"应删除（3）"名单里＝对已删对象的现行登记；③ `217:` `lib/phase1_session/README.md`、`215/218:` `eng/tools/doccheck/dangling_ledger.json`（后者存在 ✓）；`113:` `run/FIX-406/…` 属 gitignore 面 |
| 与上位冲突 | 是（三条，均可复核）：① **§2 表自身不可解析**：`21:` 表头与分隔行粘连（`| ID | 类别 | 名称 | 命令/入口 | 门禁 ||---|---|---|---|---|`）→ 142 行主表**无有效表头**；另有 15 行结构破损（`awk -F'\|' '/^\| [A-Z][A-Z0-9-]+ \|/{if(NF!=7) print NR": "NF}'` 读数：36/37/38/39 只有 3 列；54/102/103/139 为 8–9 列；57/84/105/137 为 8 列；155/156 缺 `门禁` 值；157 为 13 列）。破损形态一致：被转义的 `\|` 处被**插入了一条命令**，例 `54:` `…（`max(rtol·\| `python3 eng/tools/quality/check_photometry_apply.py --self-test …` \|, 2·float32 ULP)`）…`、`105:` 尾部 `"^p3_projection_unsupported_cli$" --output-on-failure` \| P0 \|`。本文件 §1 第 16-17 行自证"注册表双向一致…由 `CHK-REGISTRY-DOC-SYNC` 判红"，而它自己承诺的 §2 条目集已不可机器解析 → 该 P0 门的输入面在此文件上失效（属"门全绿仍需人读"的实例）。② **"已注册"与"待写入"并存**：`230:` 判 `CHK-REGISTRATION-ANCHORS` 状态为"实现已落地，注册项待写入"，`239:` 判 `SPEC-POLARITY-CONSIST-01` "注册项待写入…`eng/ci/checks.json` 属本任务文件域之外"——实测两 ID **均已在注册表内**（`grep -c '"SPEC-POLARITY-CONSIST-01"' eng/ci/checks.json` = 3；`grep -c '"CHK-REGISTRATION-ANCHORS"' eng/ci/checks.json` = 1），且 `CHK-REGISTRATION-ANCHORS` 同时已被本文件 `139-140:` 列入 §2 表 → 同文件内自相矛盾且与注册表不符。③ **门禁级两份口径**：`01_CHECKS.md:25` `| CHK-STATIC | 静态 | 静态分析 | … | P1 |` vs `03_GATES.md:26` `| CHK-WARN / STATIC | P0 | 是 | 否 |` → 同一门 P1 与 P0 两值（详见 1.13） |
| 处置建议 | **保留（角色/目录正确）+ 就地修复**：重建 §2 表（补分隔行、把 8 条被命令污染的单元格的命令移回命令列、补齐 4 条缺列行）、删除 §2.1.1 过程台账（迁 `artifacts/evidence/`）、订正两处"待写入"、删 `§9.73 A44` 与 `已按…作废` 表述；理由：§3"索引由文档现状重建，与文件树一致"+ §4 无历史叙事与过程台账 |

### 1.12 docs/ci/02_PIPELINE.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/ci/02_PIPELINE.md` |
| 标题 | `CI 流水线定义（Pipeline）` |
| 行数 | 79 |
| 角色 | **标准与检查** ✓ 目录 `docs/ci/` 一致 |
| 主题 | 事件→档位映射、job 结构、超时、门禁判定、产物 |
| 上游 | **有**：`3: > 上游：ASTROCS_DESIGN.md §12.4（验证层级与四层验收）` ✓ |
| 下游 | 1 个活引用者：`docs/ci/CI_SPEC.md:140` `## 5. 流水线（详见 `02_PIPELINE.md`）`。**必须用 basename 才检得到**：`git grep -l -F docs/ci/02_PIPELINE.md …` → 0 命中（CI_SPEC 写的是裸文件名，前言 §5.2 的"字面量拼串"陷阱在此复现）；改用 `git grep -n -E '02_PIPELINE\.md' -- ':!reports' …` → 命中 `docs/ci/CI_SPEC.md:140`。故**不是孤儿**，但引用是散文提及不是链接 |
| 是否正本 | **否**——第 7 行自认："范围口径以 `CI_SPEC.md §2` 为正本；本节只写'什么事件跑哪个范围'" |
| 重复或重叠对象 | 与 `docs/ci/CI_SPEC.md` **同口径重复承载**（虽自称不重复）。举证：本件 `54:` `- **每 step 的 timeout 上界 = max(60, 3 × 最近一次实测墙钟)**，硬上限 3600 s（`prerelease` 重步骤 10800 s）` ↔ `CI_SPEC.md:83-84:` `- 每个 step 的 `timeout_seconds` ≤ max(60, 3 × 最近一次实测墙钟)，且不超过硬上限（默认 3600 s）；` / `prerelease` 档重步骤另设上限 10808 s…10800 s`；再一处：本件 `56:` `--budget-seconds`（默认 120 s），实际耗时超出即判红` ↔ `CI_SPEC.md:85-86:` `增量档设总预算 `--budget-seconds`（默认 120 s）：实际耗时超出即报告"应拆分"并以 rc=1 结束`。重叠范围：**数值阈值 + 判定后果**被两份件同时写；差异：后果措辞不一（"判红并提示应拆分" vs "报告'应拆分'并以 rc=1 结束"）。→ 违反 §1"公式、数值表…下沉到对应下层文档"与 §4 单层承载；本件第 7 行声明"只写什么事件跑哪个范围"却又把阈值抄了进来 = 自我豁免失效 |
| 元信息块 | 无 |
| 写法违规 | `17:` `真实数据 E2E / L2 性能 / sanitizer / coverage / **nwoker** / invariant`；`45:` 同词复现（`nwoker` 拼写错误，命令：`grep -n 'nwoker' docs/ci/*.md` → 02_PIPELINE.md:17,45 与 CI_SPEC.md:97，共 3 处）；`55:` `逐项改前/改后数值见 `eng/ci/checks.json` 与 `run/CI-INCREMENTAL/EVIDENCE.md``＝指向 gitignore 过程目录的一次性改值台账；`73:` `（amend/force push 在处置面之外）`；`13:` `杜绝"增量假绿"进入 main`（问题史叙事，弱）。机械列 dates/task_ids/sha256 全 [] 与本件相符（**无误报、无漏报**） |
| 悬空引用 | 1 条：`55:` `run/CI-INCREMENTAL/EVIDENCE.md` → `run/` 不入库（AGENTS.md §7"run/ 临时产物/日志（gitignore，不入库）"），`git ls-files -- run` 空 → 正式文档指向不可再生路径。另 `77:` 标题 `## 7. 产物（见 04_ARTIFACTS.md）` 为裸文件名（可解析，但非链接 → §3"索引指针"要求下是弱指针） |
| 与上位冲突 | 是（轻度，抄写型）：§2 job 图与 `CI_SPEC.md:142-151` 的 mermaid 图节点集相同但 `J1/J2` 依赖画法不同（CI_SPEC 用 `J1 --> J2` 串行，本件用 `J1 --> J3` `J2 --> J3` 并行汇入）→ **同一 pipeline 两张图互相矛盾**，且 CI_SPEC 第 153 行自己写"build-linux/build-windows/static 并行"（与本件一致、与其自身图不一致） |
| 处置建议 | **保留 + 瘦身为"事件→档位"单表**：§2.5/§2.6 的阈值、§4 的门禁判定、§7 的产物一律改为指向 `CI_SPEC.md`/`03_GATES.md`/`04_ARTIFACTS.md` 的指针（不复制数值）；修 `nwoker`×2；删 `run/` 引用；理由：§1"每层只由它的上一层推出…每节末尾给出索引指针"+ §4"公式、数值表…放对应下层文档" |

### 1.13 docs/ci/03_GATES.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/ci/03_GATES.md` |
| 标题 | `CI 门禁与状态（Gates & States）` |
| 行数 | 107 |
| 角色 | **标准与检查** ✓ 目录一致 |
| 主题 | 门禁分级、状态词、检查项门禁简表、L2 门与监控字段 |
| 上游 | **有且正确**：`3: > 上游：ASTROCS_DESIGN.md §12.4（验证层级与四层验收）、§12.5（状态阶梯）` 两条标题逐字一致 ✓（本批唯一引了两条且都对的上游行） |
| 下游 | 4 个活引用者：`ACCEPTANCE_SPEC.md`、`eng/ci/check_frozen_gate.py`、`eng/tools/quality/budget_sources.json`、`eng/tools/quality/isa_sites.json`；另有 `docs/ci/CI_SPEC.md:128` 的裸文件名提及 |
| 是否正本 | **状态词与门禁级都不是**（状态词正本 = 设计 §12.5；门禁级正本 = `eng/ci/checks.json`）→ 本件是**转写件**，而转写已失真（见冲突列） |
| 重复或重叠对象 | 与 `docs/ci/CI_SPEC.md §9.2` 重复四条 L2 冻结判据（本文件内部第 58 行还自认"不重复规范正文"）。举证：本件 `62-63:` `四条判据（平均利用率 ≥0.85 / p50 ≥0.90 / 达标样本占比 ≥0.70 / 无连续 ≥10s 低利用窗）在 CI 裁决面由 eng/ci/l2_frozen_gate.py fail-closed 判定，任一违规即红。` ↔ `CI_SPEC.md:210-214:` 同一四条以表格给出（`| 1 | 计算区间平均利用率 ≥ 0.85 | avg_utilization_ge_min |` 起四行）。重叠范围：**阈值数值 + 判据 ID**；差异：本件不带判据 ID，CI_SPEC 带且其表体在第 4 条处断裂（见 1.14 悬空列）。另 §6.2 的 `requires_monitor` 对照表与 `CI_SPEC.md §9.1`（196-202 行）同轴 |
| 元信息块 | 无（但 §6 起有第二段权威声明，见冲突列） |
| 写法违规 | `11:` `## 2. 状态语义（**唯一口径**，与最高设计 §12.5 一致）`＝§1 末条禁"声明自己的唯一口径"字面命中；`19:` 把五个负向状态并成一格（`NOT_IMPLEMENTED / NOT_VERIFIED / DEFERRED / DORMANT / FAIL`）→ 与 §12.5 同形，但漏一项（冲突列）；`38:` `**RESERVED（文档**曾**承诺但无实现，不进本表）**`＝历史叙事"曾"；`40:` `**保留一个不存在的 P0 门 = 把假绿写进本表。**`（过程性警示）；`64-65:` `历史证据回放：`artifacts/acceptance/l2_performance/gates/*_gate.json` 9 份 `verdict=pass` 的违规证据现在全部判红`＝历史叙事 + 实测读数；`92:` `（owner 裁决 F-CI-002-04/06 维持）`＝**负责人裁决编号**（§4 明禁"负责人某日裁决/指令"）；`103-106:` `对 230 个执行单元（78 个注册项）…226 个适用面全部判红、4 个显式无适用面`＝一次性普查读数写进规范件。机械列 task_ids/dates/sha256 全 [] → **漏报** F-CI-002-04/06、DOC-CONV-001、GATE-501 |
| 悬空引用 | 2 条：① `39:` `逐条依据与重新注册前置条件见 `01_CHECKS.md §2.3`` —— `docs/ci/01_CHECKS.md` **无 §2.3**（其标题序列：`## 1`/`## 2`/`### 2.1 检查器退役与预留`/`#### 未注册检查器处置台账（§2.1.1）`/`## 3`/`## 4`/`## 5`；`grep -nE '^#{2,4} ' docs/ci/01_CHECKS.md` 读数），正确落点是 §2.1 → 指向不存在的章节；② `39:` 同句 `eng/ci/id_migration_map.json::reserved_targets` 存在 ✓（本批正例对照）；`64:` `artifacts/acceptance/l2_performance/gates/*_gate.json` 通配实测有对象（该目录含 `fix2f_w1_gate.json` 等）→ 机械列判其为 dangling 属**误报**。③ 本件自身第 57 行的"正本"声明 `docs/ci/CI_SPEC.md §9` 存在 ✓ |
| 与上位冲突 | 是（四条，其中①②为硬冲突）：① **门禁级与 `01_CHECKS.md` 直接互斥**：`03_GATES.md:26` `| CHK-WARN / STATIC | P0 | 是 | 否 |` ↔ `01_CHECKS.md:25` `| CHK-STATIC | 静态 | 静态分析 | python3 eng/ci/run_checks.py --check CHK-STATIC --quiet | P1 |`（命令：逐 ID 对齐两份表后仅此一例不同）。两份 docs + `eng/ci/checks.json` 是三层登记，同一 ID 两个级 → §1"冲突本身是缺陷"；② **状态阶梯少一项**：本件 §2 表列 `CONTRACT_READY/IMPLEMENTED/INSTALLED/VERIFIED` + 一行负向合并格，**缺 `READY_FOR_OWNER_REVIEW`**；上位 `ASTROCS_DESIGN.md` §12.5 表实测 6 行含 `READY_FOR_OWNER_REVIEW | agent 自验完成、已进入裁决面的**上限状态**；不等于 VERIFIED，也不等于发布（§13）`（命令：`sed -n '/^### 12.5/,/^## 13/p' ASTROCS_DESIGN.md`）。而本件标题自称"与最高设计 §12.5 一致" → 自称一致而实际不全；③ **VERIFIED 被单方面收窄**：`03_GATES.md:18` `| VERIFIED | 正式平台（Windows x64）+ 真实数据验收通过 |` ↔ 上位 `ASTROCS_DESIGN.md:797` `| VERIFIED | 正式平台 + 真实数据验收通过 |`，且 §11 表列两个正式平台（`Windows 10+ amd64`、`Linux amd64`）→ 括号限定把 Linux 排除在 VERIFIED 之外，与上位不符（同一收窄已在 `docs/KNOWN_LIMITATIONS.md:13` 复现，见 1.3）；④ 上位 §12.5 的收尾规则"状态由检查与验收现场计算，登记表不预写状态"在本件 §6.2 表中以"是/否"预写 11 行状态而无现场计算声明；⑤ §6 起第二段声明（`57:` `> 正本：docs/ci/CI_SPEC.md §9；判据数值源：eng/contracts/resource_gate_v1.json`）落在文档中部，§3 要求上游/权威标注在**开头位置**，中部第二权威块即 §1 禁的第二套权威顺序 |
| 处置建议 | **保留 + 就地订正**：状态表改为逐字引用 §12.5（补 READY_FOR_OWNER_REVIEW）或整节删去只留指针；删除 §6 全部（其"判据事实"与 CI_SPEC §9 重复，留一行指针）；统一 CHK-STATIC 级别为注册表值；`§2.3`→`§2.1`；删"owner 裁决编号/曾/历史证据回放"表述；理由：§1"下位文档与上位冲突时以上位为准"+ §3"每份下层文档在开头位置标明其上游条款" |

### 1.14 docs/ci/CI_SPEC.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/ci/CI_SPEC.md` |
| 标题 | `Astro Celestial Sphere Database（ACSD） CI 规范（CI Specification）` |
| 行数 | 251 |
| 角色 | **标准与检查** ✓ 目录一致 |
| 主题 | CI 目标、scope 口径、fail-closed 安全网、档位、门禁、流水线、产物、L2 判据 |
| 上游 | **有且正确**：`3: > 上游：ASTROCS_DESIGN.md §12.4（验证层级与四层验收）` |
| 下游 | 17 个活引用者（`AGENTS.md`、`README.md`、`docs/DEVELOPER_GUIDE.md`、`docs/ci/03_GATES.md`、12 个 `eng/ci/**`） |
| 是否正本 | 是（CI 轴总则正本；01/02/03/04 是其下钻件） |
| 重复或重叠对象 | 与 `02_PIPELINE.md`/`03_GATES.md`/`04_ARTIFACTS.md` 互为抄写源（三处举证见各条重复列）。本件 §9 与 `03_GATES.md` §6 是**同一 L2 判据的两份文字**（本件带判据 ID、彼件带对照表）→ §1"同一主题只有一份正本"未成立，尽管 `03_GATES.md:58` 自称"不重复规范正文" |
| 元信息块 | 无；但 `5:` 紧跟分隔线、`185-188` 与 `174-181` 各有一块"与其它文档关系/上游"式元信息表 |
| 写法违规 | `94:` `真起子进程 / 真跑 CLI 的集成型用例：**FIX208** 事件流与磁盘门`；`101:` 同词复现；`106:` `重步骤带**输入指纹缓存**：`commit + 相关配置 SHA256 + 数据清单 SHA256 + worker 数``——**机械列 `sha256=True` 属误报**（此处 SHA256 是被规定的指纹字段名，不是被登记的摘要值，与前言 §5.4"高估"一致）；`188:` `本节是 **RELEASE-04 D-10 / D-12** 的落地口径`；`202:` `（11 个执行单元已逐个核对）`＝一次性核对读数；`224-225:` `**历史证据回放**：`L2-FROZEN-GATE-REPLAY` 断言 RELEASE-04 归档的 9 份 `frozen_gate.verdict=pass` 违规证据现在必须判红（D-10 恒真门已堵）`＝历史叙事 + 编号 + 读数；`247-248:` `普查表落 …（机器可读面 `run/ci/failclosed-survey/survey.json`）`；`174-181:` §8"与其它文档关系"表把 `ENGINEERING_SPEC.md` 写作"定义'检查什么'（本文执行其 §8/§11）"→ 在文档内自排权威次序（§1 禁）。机械列 `task_ids=['RELEASE-04']` 只抓到 1 个，**漏报 FIX208/FIX-208/D-10/D-12/L2-FROZEN-GATE-REPLAY/CHK-* 族**；`history_narrative=[119]` 漏报 224 行的"历史证据回放/现在必须判红" |
| 悬空引用 | **最严重的一条是本件表格已被污染**：`213:` 第 4 条 L2 判据单元格里嵌入了模型特殊 token —— 原文（`od -c` 字节级证据：`<` `357 275 234`×2 `begin` `342 226 201`×2 `of` `342 226 201` `sentence` `357 275 234`×2 `>`）为 `| 4 | 无连续 ≥10s 且利用率 <60% 的低利用窗（**无就绪积压同样<｜｜begin▁of▁sentence｜｜>`，且**该行在此断行**，`214:` 以 `违规**：串行/停顿与 CPU 饥饿同属性能缺陷） | `no_low_utilization_window` |` 起首（`awk 'NR>=213 && NR<=214 {printf "%d [len=%d] %s\n", NR, length($0), substr($0,1,60)}'`）→ ① markdown 表格第 4 行被拆成两物理行、单元格不闭合，四条判据表渲染断裂；② 一个模型聊天模板控制串（用全角 `｜｜` 与 `▁` 转写以避开字面匹配）留在规范正文里。全域唯一：`git -c core.quotePath=false grep -n -e '｜｜'` → **仅这一处命中**，其余 docs 无此形态。其余：`161:` `版本号按最高设计 §12 在可发布 Alpha 时才出现`（条款号错，见冲突列）；`113/128/140/158:` 对 `01_/03_/02_/04_` 的引用一律是裸文件名散文提及（无路径、无链接，§3 双向索引意义上不可机器解析）；`98:` `§9.73`（不存在）；`248:` `run/ci/failclosed-survey/survey.json`（gitignore 面）。正例对照：`136:` `docs/contracts/SCHEDULER_CONTRACT.md §2.1`、`206:` `eng/contracts/resource_gate_v1.json::compute` 均可解析 |
| 与上位冲突 | 是（三条）：① 条款号：`161:` 以 `ASTROCS_DESIGN.md §12` 作为"Alpha 前不出现版本信息"的依据，而上位实际写法是 `482: --version   版本（Alpha 前无版本信息，见 §13）` 且 §13 正文才有"alpha 阶段之前，程序、代码与产物中不出现任何版本信息"→ §12 是验证体系；同一错引在 `docs/ci/04_ARTIFACTS.md:11/12/20/36` 与 `docs/VERSIONING.md:7` 共 6 处复现（见 1.6/1.15）；② §9 中部第二权威块（`187-188:` `> 上游：ENGINEERING_SPEC.md §10（fail-closed）、§12（资源与性能）；数值源 eng/contracts/resource_gate_v1.json。本节是 RELEASE-04 … 的落地口径`）＝§1 禁的第二套权威顺序 + §3 要求开头标注；③ §2.6 把"提交可合"的判据写成 `fast`+`integration` 两档全绿，而 `03_GATES.md §5` 的通过定义是"全部 P0 绿 + P1 绿（或已豁免）→ 门禁通过 → 打包发布候选"——两份件对"门禁通过"给不同充分条件 |
| 处置建议 | **保留为 CI 轴正本 + 立即清除第 213 行的控制 token 污染并修合表**；§9 的判据数值改为唯一落点（保留本件，删 `03_GATES.md §6`）；订正 `§12`→`§13`；删 FIX208/RELEASE-04/D-10/D-12/历史回放语域；理由：§4"只写现行设计要怎样"+ §3"双向索引（引用需可解析）"+ §1 权威链单一条 |

### 1.15 docs/ci/04_ARTIFACTS.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/ci/04_ARTIFACTS.md` |
| 标题 | `CI 产物与留存（Artifacts & Retention）` |
| 行数 | 40 |
| 角色 | **标准与检查** ✓ 目录一致 |
| 主题 | CI 产物清单、命名与哈希、留存策略、发布候选门槛 |
| 上游 | **有且正确**：`3: > 上游：ASTROCS_DESIGN.md §10（I/O 与原子产品）、§12.4（验证层级与四层验收）` 两条标题逐字一致 ✓ |
| 下游 | 2 个活引用者，均为裸文件名散文提及：`docs/ci/02_PIPELINE.md:77` `## 7. 产物（见 04_ARTIFACTS.md）`、`docs/ci/CI_SPEC.md:158` `## 6. 产物与留存（详见 `04_ARTIFACTS.md`）`。全路径检索 0 命中（`git grep -l -F docs/ci/04_ARTIFACTS.md -- …`）→ 同 §5.2 拼串陷阱，须以 basename 复检出。二者都只是"标题里提一句"，无一条真实链接 → 索引 `541: downstream: "docs/DOCUMENT_INDEX.yaml"` 把它的下游**记成只剩索引自身**，与实际不符（机械列 `unregistered_index=False` 正确，但索引自身的 downstream 值不可信） |
| 是否正本 | 否（其 §5"发布候选门槛"抄自上位 §13 且已失真） |
| 重复或重叠对象 | **与最高设计 §13 逐条重叠**（本件自称"与最高设计 §12 一致"）。举证：本件 `38:` `发布候选至少满足：合同冻结无冲突、追踪无断链、模块可独立加载/验证/卸载、ACR 生产不可达、heavy 无硬编码线程/单线程长计算/**持续低利用率**/无界内存增长、双平台 CI 通过、**Linux 真实数据终验 + Windows 复验**、**图像有量化证据+Agent 初审+Owner 终审**、P0/P1=0、发布包白名单/哈希/版本/provenance 通过。` ↔ 上位 `ASTROCS_DESIGN.md` §13 `发布候选至少满足：合同冻结无冲突、追踪无断链、模块可独立加载/验证/卸载、ACR 生产不可达、**无硬编码线程/单线程长计算/无界内存增长**、双平台 CI 通过、**四层验收全部通过（L4 含两组真实数据目检）**、P0/P1=0、发行包白名单与哈希/provenance 通过。`。重叠范围：整条门槛清单；差异：本件**新增**"持续低利用率"限定与 `heavy` 修饰、**替换**"四层验收全部通过"为两句自述、把"发行包"写成"发布包" → 下位复述上位清单并改写其内容，正是 §1"下位与上位冲突时以上位为准；冲突本身是缺陷"与 §4"数值/细节放对应下层"的反面 |
| 元信息块 | 无 |
| 写法违规 | `11:`、`12:` `（Alpha 前无版本号，见最高设计 §12）`；`20:` `（Alpha 前无版本号；可发布 Alpha 后按 §12 加版本）`；`36:` `## 5. 发布候选门槛（与最高设计 §12 一致）` —— 四处同一条款号误引；`40:` `**产物只是材料；最终发布决定只属项目负责人。**`＝把 §13 的发布权条款再抄一遍（同主题第三份复述） |
| 悬空引用 | `22:` `发布候选与普通构建分开存放（`artifacts/release-candidates/` vs `artifacts/builds/`）` → 两目录当前均不存在（`[ -d ]` 检查；`artifacts/` 下只有 `ci/`、`evidence/`）。属"承诺的未来落点"写成现行路径 → §3 不可达指针（非机械列命中项，**机械列漏报**：其 dangling_links 列为 []） |
| 与上位冲突 | 是：① 四处 §12 应为 §13（见写法违规）；② §5 门槛清单与上位文本不一致（见重复列举证）；③ `33-34:` 把 `CHK-MODULE-MANIFEST`、`CHK-PACKAGE` 写作产物验证依据，二者在 `01_CHECKS.md:26/63` 均登记为 P0 ✓（此项一致，无冲突） |
| 处置建议 | **合并**：§1/§2/§3 保留为 CI 产物轴唯一落点（`02_PIPELINE.md §7` 并入本件），**§5 整节删除、改为指向 `ASTROCS_DESIGN.md` §13 的一行指针**；理由：§1"最高设计只承载顶层决策…下位不复述"+ §4"同一主题一份正本" |

### 1.16 docs/development/CODE_STYLE.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/development/CODE_STYLE.md` |
| 标题 | `Code Style` |
| 行数 | 13（本批最短） |
| 角色 | **标准与检查**（编码判据）。实际在 `docs/development/`（§2 该目录对应"操作与运营"），其内容不是构建/运行操作 → **角色错放**，应迁往 `docs/standards/CODE_STYLE.md` 或与 `docs/standards/CODE_STANDARD.md` 合并 |
| 主题 | C++17/工具链/格式/命名/include 顺序/C ABI/警告策略 |
| 上游 | 有但弱：`3: > 上游：ASTROCS_DESIGN.md §8（软件架构）` 标题存在 ✓，但编码风格条款的真实上位是 `ENGINEERING_SPEC.md` §1/§4（`docs/standards/CODE_STANDARD.md:5` 就是这样自标的：`权威来源：ENGINEERING_SPEC.md §1（C++17 双平台工具链…）与 §4（每模块必备项）`）→ 上游指向与同主题正本不一致 |
| 下游 | **0 个活引用者**：`git grep -l -F docs/development/CODE_STYLE.md -- ':!docs/DOCUMENT_INDEX.yaml' ':!docs/audit/*' ':!reports/*' ':!artifacts/*'` → 空；二次检索 `git grep -n -iE 'code_style|code style'`（同一排除面）→ 只命中本文件自己的 `1: # Code Style`。索引 `692: duty: "Code Style"`（照抄 H1，非职责句）+ `693-695` 未给真实下游。**孤儿文档确认** |
| 是否正本 | **否**（同主题已有 `docs/standards/CODE_STANDARD.md`，42 行，ACTIVE_NORMATIVE） |
| 重复或重叠对象 | **与 `docs/standards/CODE_STANDARD.md`（别批）同主题两份，且工具链口径互斥**。举证：本件 `5:` `- C++17；MSYS2 MinGW64 g++ 16.1.0；OpenMP 仅显式并行区。` ↔ `docs/standards/CODE_STANDARD.md:9-11:` `- C++17（**正式 toolchain：Windows = MSVC v143；Linux = GCC 或 Clang**，见 ENGINEERING_SPEC.md §1 与 ASTROCS_DESIGN.md §10.2「官方 Windows 工具链 = MSVC」）；RAII 优先。` / `MinGW64 的定位 = **本地开发/兼容性验证**工具链；正式工具链取上条所列工具链…`。重叠范围：C++ 标准 + 工具链 + 格式/警告/命名；差异：本件把 MinGW64 g++ 16.1.0 写成无条件现行口径（且写死编译器小版本号），彼件把 MinGW64 降为本地开发工具链。**两份件对"正式工具链是什么"给出不同答案** |
| 元信息块 | 无 |
| 写法违规 | `6:` `- `.clang-format`（根目录，**V14 落地**）覆盖 first-party`＝版本批次编号 + 历史叙事（"V14 落地"是过程陈述，非现行规则）；`5:` `g++ 16.1.0` 写死编译器小版本（与 `docs/operations/TOOLCHAIN_AGENT_HOST.md` 的"本机 gcc missing"并读即知会腐坏）。机械列 dates/task_ids/history 全 [] → **漏报 V14 落地** |
| 悬空引用 | `10:` `- include 顺序：本模块 → **acsd 头** → std；self-contained public header。` → 头文件前缀不是 `acsd`：`git grep -l 'include "astrocs' -- lib \| wc -l` = **110** 个文件用 `astrocs/…`；`acsd` 是 CLI 可执行名（`docs/api/CLI_PROTOCOL_V1.md:14` `acsd --version [--json]`）。术语与 `docs/GLOSSARY.md`（自称唯一术语权威）登记面不覆盖 `acsd`/`astrocs` 之分 → 既是用词不准，也是词典覆盖缺口 |
| 与上位冲突 | 是：与 `docs/standards/CODE_STANDARD.md` 的工具链口径冲突（举证见重复列）；上位 `ASTROCS_DESIGN.md §11` 表列 Windows 交付 `acsd.exe` + "Windows 官方工具链为 MSVC"，故本件第 5 行以 MinGW64 为现行工具链的写法与上位不一致 |
| 处置建议 | **合并 → 并入 `docs/standards/CODE_STANDARD.md`**（本件独有的 include 顺序与"self-contained public header"两条并入彼件 MUST 清单，其余删除），删除本路径与 `docs/development/` 下的空位；理由：§2"同一角色内同一主题只有一份正本；多份内容合并为一份，合并后其余删除" |

### 1.17 docs/development/CONFIG_SCHEMA.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/development/CONFIG_SCHEMA.md` |
| 标题 | `Config / Schema（单一事实来源）` |
| 行数 | 115 |
| 角色 | 内容 = JSON/schema/配置键语义 → **合同说明**（§2 目标目录 `docs/contracts/`）。实际在 `docs/development/`（操作与运营）→ **角色错放**，应迁往 `docs/contracts/`；而该目录**已有 `docs/contracts/CONFIG_CONTRACT.md`**（本件第 11 行自认其为"三命令 phase_config 合同的唯一权威"）→ 落位与正本归属双重错位 |
| 主题 | Stage1/Stage2 配置键、默认值、排异档位映射 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §8（软件架构）` 标题存在 ✓，但本件实质内容是 §3.2（三类配置）与 §7.2（配置、事件与退出码）→ 上位条款指向不准（§3 要求"追溯到最高设计的一条要点"） |
| 下游 | 7 个活引用者：`docs/contracts/CONFIG_CONTRACT.md`、`eng/tests/config/check_cfg002_registry.py`、`eng/tools/config_consistency_check.py`、`eng/tools/docs_machine_consistency.py`、`eng/tools/fixtures/config_consistency_known_divergences.json`、`lib/algorithms/coverage/include/astro/phase2/stage2_common.h`、`lib/algorithms/coverage/tests/synthetic_gate.cpp` → 有真实机器消费者（其 fenced 块是 `check_cfg002_registry.py` 与 `config_consistency_check.py` 的输入），**不可轻删** |
| 是否正本 | **对 Stage2 orchestrator 配置是**（第 9-10 行自划范围），**对三命令 phase_config 不是**（让给 `docs/contracts/CONFIG_CONTRACT.md`）→ 一份文件内做了一次正本切分，切分本身合理，但标题"单一事实来源"与切分矛盾 |
| 重复或重叠对象 | 与 `docs/contracts/CONFIG_CONTRACT.md`（别批）同主题（配置合同）。举证：本件 `8-11:` `> **文档范围与 `output_mode` 口径（依 ASTROCS_DESIGN.md §0.2…）** 本文描述的是 **orchestrator 的 Stage2 配置**…**不是**三命令 `phase_config` 合同；后者的语义与索引唯一权威 = `docs/contracts/CONFIG_CONTRACT.md` §3。` ↔ 索引 `docs/contracts/CONFIG_CONTRACT.md` 亦登记为合同件并引用本件（`git grep -l -F docs/development/CONFIG_SCHEMA.md -- docs/contracts` → 命中 `docs/contracts/CONFIG_CONTRACT.md`）。差异：本件带默认值数值表、CONFIG_CONTRACT 带 schema 面 → **属合法分层（下钻）**，但"单一事实来源"这一标题主张使两者关系必须靠一段免责文字维持；另与 `docs/TRACEABILITY.csv:13`（`CLI/config` 行的 `science_doc`/`algorithm_doc` 两列同填本件，见 1.8 行）构成"同一件被当作科学定义 + 算法定义 + 合同面"的三重角色占用 |
| 元信息块 | **有**：`8-19:` 12 行的 `>` 块（范围声明 + 权威让渡 + `output_mode` 口径 + "⇒ 本文与 CONFIG_CONTRACT.md 在此点上**两边同向**"）＝§4 禁的"纪律声明/权威口径"块状元信息。机械列 `meta_block=False` → **漏报** |
| 写法违规 | `35:` `… upm_weight_source(snr2_normalized)  # 原 snr_weight_mode，（已按 **§9.73 A44 作废**：键不存在；权重是派生量）`；`58:` `weight_mode(auto) acr_route(cpu/auto)   # （已按 §9.73 A44 作废：键不存在…）` —— 两处"作废 + 原键名"历史叙事，且 `§9.73` 不存在；`56-57:` `（low/high/max_iterations/min_samples 不是现行键（出现即硬错误），**旧 config 必须** eng/tools/migrate_stage2_config.py 迁移）`＝"旧…"叙事；`93-94:` `- **V17**：旧顶层 low/high/max_iterations/min_samples 已从 parser 删除，出现即硬错误`＝版本编号 + 删除叙事；`63:` `（**M3 裁决 2026-09-25：原 N≥16→linear_fit 档改投**）`＝**负责人裁决 + 日期 + 原方案**（§4 三项同时命中；机械列 dates=[63] ✓ 判对，但未识别"M3 裁决"为裁决引述）；`69-70:` `档界取自 WBPP 2.5.9 `bestRejectionMethod`，`engine.js:1421-1429`，包 **sha1 `712cc7c3…`**`＝外部源码行号 + 摘要值；**机械列 `commit_sha=['712cc7c3']` 属误报**（它是第三方包 sha1 前缀，不是本仓 commit，正是前言 §5.4 说的"高估"实例）；`102-107:` 排异档位映射块又复述一遍 §5.5 路由（与第 62-63 行正文重复）；`114:` `银心三面板 `stage1_gc_panel{1,2,3}_Red.json``、`115:` 括号说明 → 用 shell brace 写法表示三份具体文件（可解析但不可点） |
| 悬空引用 | 5 条：① `35:`/`58:` `§9.73`（`ASTROCS_DESIGN.md` 无 §9.x：`grep -cE '^#{4,6} ' ASTROCS_DESIGN.md`=0 且 §9 下无三级标题）→ 同一悬空锚在本批被 4 份文件复用（1.9、1.11、1.14、1.17）；② `104:` `内核同值见 `lib/algorithms/coverage/src/rejection.cpp:1139` `kPixelSmallNPolicy`` —— 实测 `kPixelSmallNPolicy` 在 **1145 行**（`grep -n 'kPixelSmallNPolicy' lib/algorithms/coverage/src/rejection.cpp` → 1145/1151/1158；文件 2956 行）→ **行号锚偏移 6 行，锚点失效**；③ `56-57:`/`94:` `eng/tools/migrate_stage2_config.py` 存在 ✓（正例对照）；④ `114:` `stage1.schema.json 默认 0.8` 未给路径，真实件 `lib/infrastructure/pipeline/orchestrator/configs/stage1.schema.json` 存在 ✓；⑤ `112:` `见 `lib/infrastructure/pipeline/orchestrator/configs/stage1_*.json` 模板` 该 glob 命中 5 个真实文件（`stage1.schema.json`/`stage1.template.json`/`stage1_gc_panel{1,2,3}_Red.json`）→ 机械列把这条 glob 判为 dangling，**误报**。⑥ `115:` `docs/architecture/ARCHITECTURE.md §6` 存在（文件在，节号未逐字核——不判悬空，登记为待证） |
| 与上位冲突 | 是：① `52:`/`64:` 权重口径与 `docs/owner/PROJECT_SPEC.md:52/64`（"不存在独立的 weight 产品或权重模式"/"权重…不是独立合同对象"）与 `docs/GLOSSARY.md:14-15`（`pixel_weight`、`frame_quality_weight` 作为在册术语并给单位与域）**三处不同向**（详见 §2 节 G4）；② `102:` 自引 `ASTROCS_DESIGN.md §5.5` 为"生产科学路由唯一权威"并在 `63:` 记录"M3 裁决改档"——即本文档的档位表以裁决为来源而非上位条款文本，属 §1"不声明第二套权威"；③ `104:` 行锚失效即 §3"追溯不到"的具体形态；④ `69:` 写 WBPP `n>15` 分支的对照档"本仓该档取 linear_fit = WBPP ≤2.3.x 旧表"——与同段 `63:` 的"N≥6→winsorized、原 N≥16 改投"在同屏并存，对照档与生产档的区分全靠一段括注维持 |
| 处置建议 | **迁移 → `docs/contracts/STAGE2_CONFIG_CONTRACT.md` 并与 `CONFIG_CONTRACT.md` 建立互指**；就地：删标题"（单一事实来源）"、删 12 行元信息块改为一行范围句、删两处"已按 §9.73 A44 作废"注释与 `原 snr_weight_mode`、`M3 裁决 2026-09-25` 去日期与裁决编号（改为正向条款"档位按 §5.5 路由"）、订正 `rejection.cpp:1139`→1145 或改用符号锚；理由：§2 角色-目录一一对应 + §4 无日期/裁决/历史叙事 + §3 锚可达 |

### 1.18 docs/development/TESTING.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/development/TESTING.md` |
| 标题 | `Testing` |
| 行数 | 28 |
| 角色 | **标准与检查**（测试判据）→ 实际在 `docs/development/`（操作与运营）→ 角色错放，应迁往 `docs/standards/`（与既有 `TEST_STANDARD.md` 合并）；若按"怎么跑测试"读则属操作与运营，则目录正确——**本件的实质是"哪些是回归锚"（判据）而非"怎么跑"**，故判错放 |
| 主题 | 测试入口清单 + 关键回归集 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §8（软件架构）` ✓ 存在，但测试的实质上位是 §12（验证体系）/§12.4；与 `docs/standards/TEST_STANDARD.md:3` 自标的 `§8.4（模块与 ABI）` 也不同 → 同主题两份件各指一个不相干的上位条款 |
| 下游 | 1 个活引用者：`docs/contracts/DATA_SEMANTICS.md`（`git grep -l -F docs/development/TESTING.md …` → 仅此）。次检索：basename `TESTING.md` 在活面只命中 `docs/standards/TEST_STANDARD.md` 文件名（不同件）。**近孤儿** |
| 是否正本 | 否（同主题 `docs/standards/TEST_STANDARD.md` 42 行，ACTIVE_NORMATIVE） |
| 重复或重叠对象 | **与 `docs/standards/TEST_STANDARD.md`（别批）同主题两份**。举证：本件 `13-18:` `## 关键回归集` / `- HiPS 序列化：Hipsgen 外部 oracle（205625/205627）` / `- UPM：V13 science 逐位等价（C/M maxdiff=0）作为回归锚。` ↔ `docs/standards/TEST_STANDARD.md:5-8:` `- 每科学契约 ≥1 test/oracle（property/oracle/MC）。` / `- 测试必须使用公共生产 API；实现面只有生产一份。` / `- 确定性：固定 seed；浮点断言用容差 + 说明来源。`。重叠范围："什么算被测/什么锚必须回归"；差异：本件给具体入口与实测值（可执行清单），彼件给普适判据（规范）→ 两者本可分层，但**本件不含"如何跑"的统一入口且与 `docs/ci/01_CHECKS.md` 的 CHK-UNIT/CHK-ORACLE 登记重复列同一批测试目标** |
| 元信息块 | 无 |
| 写法违规 | `7:` `- `phase2_synthetic_gate.exe`（Phase2 合成门，**V2–V13 逐轮累积**）。`＝版本批次编号 + 历史叙事；`9:` `v13_synth_test.exe`（编号入二进制名）；`18:` `- UPM：**V13** science 逐位等价（C/M maxdiff=0）作为回归锚。`；`21:` `- Browser：**V13** 产品几何显示不回归。`；`15:` `（205625/205627）` 无上下文的外部编号（未标项目与版本 → 不可复核）。命令：`grep -nE 'V[0-9]+' docs/development/TESTING.md` → 7/9/18/21 四处。**机械列 task_ids=[]、history_narrative=[] → 全部漏报** |
| 悬空引用 | 2 条：① `7:`/`26:` 的 `phase2_synthetic_gate.exe` —— 真实目标定义在 `lib/algorithms/coverage/CMakeLists.txt:103 add_executable(phase2_synthetic_gate tests/synthetic_gate.cpp)`（即源文件 `lib/algorithms/coverage/tests/synthetic_gate.cpp`），而 `26:` 写的运行路径是 `py -3.12 lib/algorithms/coverage/build/phase2_synthetic_gate.exe`，该 `build/` 子目录**不存在**（`[ -d ]` ABSENT）且不在跟踪集；同一二进制在 `docs/TROUBLESHOOTING.md:33` 被写成 `lib\phase2\build\phase2_synthetic_gate.exe`（`lib/phase2` 跟踪文件数 0）→ **同一目标两份件给出两条互斥路径，且都不可从干净克隆复现**（正确落点按 AGENTS.md §3 是根 `build/`）；② `8:` `lib/infrastructure/aio/tests/*`（存在 ✓，正例对照）。③ `25-27:` 运行块只有 PowerShell + `py -3.12`（Windows 专用启动器），而 Linux amd64 是正式平台（`ASTROCS_DESIGN.md §11` 表）→ 不是悬空而是**适用域缺口** |
| 与上位冲突 | 是：① 第 7 行把合成门写成"V2–V13 逐轮累积"，与上位 §12.4 的四层验收命名（L1–L4）不同轴（另一套 V# 计数）；② 运行入口与 `docs/development/`（其同目录件 `CODE_STYLE.md`）一样只写 Windows 侧，与 §11 双平台现行口径不齐 |
| 处置建议 | **合并 → 并入 `docs/standards/TEST_STANDARD.md`**（本件的具体入口/锚清单作为彼件的"现行回归集"节），或改名为 `docs/operations/TEST_RUNBOOK.md` 并把 exe 路径改成由根构建目录派生；理由：§2 同主题一份正本 + §4 无版本批次叙事 |

### 1.19 docs/diagnostics/TROUBLESHOOTING.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/diagnostics/TROUBLESHOOTING.md` |
| 标题 | `Astro Celestial Sphere Database（ACSD） Troubleshooting` |
| 行数 | 33 |
| 角色 | **操作与运营** ✓（§2 目标目录含 `docs/diagnostics/`）→ **目录与角色一致**；问题不在本件的位置，而在与本件同名的 `docs/TROUBLESHOOTING.md`（1.5） |
| 主题 | 故障场景 → 定位 → 通用顺序 → 诊断工具 |
| 上游 | 有且正确：`3: > 上游：ASTROCS_DESIGN.md §7.2（配置、事件与退出码）` 标题逐字一致 ✓ |
| 下游 | 1 个活引用者：`docs/standards/ERROR_HANDLING_STANDARD.md`；`docs/audit/doc_classification.csv:40` 亦登记（台账件，不计）。检索命令同 1.5 形态 |
| 是否正本 | **是（诊断轴应属正本）**：其 10 行场景表按"阶段+错误码+度量"组织、通用定位顺序五步闭环并指向 `docs/traceability/TRACEABILITY_MATRIX.json`，比 1.5 更接近 §2 的诊断职责边界；1.5 只提供命令与更粗的症状表 |
| 重复或重叠对象 | **与 `docs/TROUBLESHOOTING.md`（1.5，本批）同主题两份**。三处对举：① 本件 `5-6:` `每个 high-risk 错误按固定条目：symptom → likely stage → metrics/log → error code → minimal reproduction → expected invariant → source/doc/test。` ↔ 1.5 `5:` `## Symptom → 定位 → 修复`；② 本件 `17:` `| NaN 候选权重 | P2.INTEGRATE | INVALID_INPUT 状态 | `p2_validate_candidate_weights`（负/NaN/Inf 拒） |` ↔ 1.5 `16:` `| stage2 `ZERO_VALID_WEIGHT` | INTEGRATE 日志 | 帧无 ivar 产品且 support=0 | 确认 Phase1 输出了 variance/ivar; 重跑 Phase1 生成含 variance/ivar 的帧 |`（同一 INTEGRATE 失败族，两份各写一行、判据字段不同）；③ 本件 `33:` `eng/tools/astrocs_diagnose.py <run_dir> 输出小 bundle（stage/error/metrics）。` ↔ 1.5 `35:` `py -3.12 tools\astrocs_diagnose.py run\logs --json diag.json`（同一工具两行用法，**1.5 的路径是错的**，见其悬空列）→ 合并后只应留一行且取本件写法 |
| 元信息块 | 无 |
| 写法违规 | 编号密集但**属引用而非叙事**，逐条列：`13:` `ALG-DRZ-CAND-001 oracle；polar prune 契约`；`15:` `SCI-UPM-PERSIST-001 绑定；G4 seam 测试`；`19:` `IO_AND_ATOMICITY；temp+rename 协议`；`20:` `stale=2 | source_hash/键校验；CACHE_POLICY`；`21:` `NoiseWeightModelV1 矩阵；**SNR-001..015**`。历史叙事/日期/sha：**无**（机械列与本判定一致，dates/task_ids/sha256 全 []） |
| 悬空引用 | 0 条硬悬空（逐条核）：`12:` `docs/modules/gaia_xpsd_client.md` ✓ 存在；`29:` `docs/traceability/TRACEABILITY_MATRIX.json` ✓ 存在；`19/20:` `IO_AND_ATOMICITY`、`CACHE_POLICY` 简称 → `docs/architecture/IO_AND_ATOMICITY.md`、`docs/architecture/CACHE_POLICY.md` ✓ 存在（但写法是简称不是路径，§3 意义上为弱指针）；`17:` 符号 `p2_validate_candidate_weights` ✓ 在 `lib/algorithms/coverage/src/integrate.cpp`（`docs/TRACEABILITY.csv:6` 同锚）。**唯一不可解析**：`21:` `SNR-001..015` —— 登记册里的 15 行 ID 实为 `SCI-NOISE-001..015`（`docs/TRACEABILITY.csv:23-37`）与单一 `TEST-SNR-001`（`grep -c -E 'SNR-0[0-1][0-9]' -- docs/TRACEABILITY.csv` = 16 命中全为 `TEST-SNR-001`/`SCI-NOISE-00x`）→ 该 ID 族按字面不存在，属**编号写错一档**（非文件级悬空） |
| 与上位冲突 | 无实质冲突；`25:` `读 stage 日志（run/logs/<module>/<YYYYMD 形如>/）` 与上位 §7.3/AGENTS.md §3"日志一律落 `run/<task>/logs/`" **顺序不同**（`run/logs/<module>` vs `run/<task>/logs/`）→ 登记为轻微上位不一致 |
| 处置建议 | **保留为诊断轴正本，吸收 1.5（`docs/TROUBLESHOOTING.md`）后删本批同名第二件**；顺带把简称引用（`CACHE_POLICY`、`IO_AND_ATOMICITY`）写成完整路径、`SNR-001..015` 改 `SCI-NOISE-001..015`、日志路径改 `run/<task>/logs/`；理由：§2"同一角色内同一主题只有一份正本；多份内容合并为一份" |

### 1.20 docs/operations/TOOLCHAIN_AGENT_HOST.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/operations/TOOLCHAIN_AGENT_HOST.md` |
| 标题 | `TOOLCHAIN_AGENT_HOST — Agent 主机工具链快照` |
| 行数 | 63 |
| 角色 | 名义 **操作与运营**（目录 `docs/operations/` ✓），实质是**一次性取证台账/快照**（标题自带"快照"，通篇为某台主机的当时状态）→ §2 第三条"历史档案、过程台账不放在正式文档层；需要留证的台账落 `artifacts/evidence/` 并由正本引用" → **层级错置（不是目录名不符，是文件类型不符）** |
| 主题 | Agent 主机工具链实测 + 仓内工具链约束盘点 + 与 hosted CI 策略关系 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §11（双平台发行与安装）` 标题一致 ✓，但本件内容是**某台机器**的 `cmake missing/gcc missing` 盘点，§11 不承载主机环境 → 上位指向与内容不承载（§3"追溯不到一条要点的机制先补要点再保留"） |
| 下游 | **0 个活引用者**：`git grep -l -F docs/operations/TOOLCHAIN_AGENT_HOST.md -- <活面>` → 空；basename 二次检索 `TOOLCHAIN_AGENT_HOST.md` → 空；`grep -i toolchain_agent` 在活面无命中 → **孤儿**。索引 `1046:` 的 downstream 也只有 `"docs/DOCUMENT_INDEX.yaml"` |
| 是否正本 | 否：现行工具链策略的落点是 `eng/ci/toolchain.policy.json` + `CMakePresets.json` + `eng/ci/toolchain.lock.json`（三件都在，本件只是它们的读数快照） |
| 重复或重叠对象 | 与 `AGENTS.md §3（环境与构建）` + `docs/architecture/BUILD_GRAPH.md`/`docs/standards/C_ABI_STANDARD.md` 侧的工具链陈述重叠；最贴的一对是与 `docs/development/CODE_STYLE.md:5`（1.16）：本件 `40:` `Windows 侧统一工具链入口（MSYS2 MinGW64 `C:\msys64\mingw64\bin`、正式 Python 3.12 路径、gh CLI；`check`/`build` 用 g++/make）` ↔ 1.16 `5:` `- C++17；MSYS2 MinGW64 g++ 16.1.0…`。重叠范围：MinGW64 在体系中的定位；差异：本件把它限定为"Windows 主机关注点"并声明"不适用于本 Linux Agent 主机盘点"，1.16 直接当现行编译口径，而 `docs/standards/CODE_STANDARD.md:9` 说正式工具链是 MSVC v143 → **三处三份口径**（详见 §2 节 G5） |
| 元信息块 | **有**：`5-8:` 四条（生成依据 / 证据日志 / 采集方式 / 盘点基准 SHA）＝§4 禁的"版本锚 + 纪律声明"块 |
| 写法违规 | `8:` `- 盘点基准 SHA：`98c2354fcd59423e80ae9592f19b5b9094d20fc1`（main）`（40 位 commit）；`44:` `策略文件 `eng/ci/toolchain.policy.json`（**SHA256 `7c66e1a5ff33c192653b60f2b71ffbd856d1af6821b520f8c301ae98d2881469`**）`（64hex 摘要）→ **机械列 `sha256=False` 漏报**（其 `commit_sha` 列抓到 98c2354f…，却没抓 SHA256 一词旁的 64hex）；`37:` `版本单源 `VERSION`（VER-001）`；`39:` `BLD-001 根级 Linux configure/build/test 入口`；`30:` `详见 `logs/deep_probe.log``；`31:` 标题 `## 2. 仓库既有工具链约束（只读取证，未重新配置）`、`7:` `未安装、未重配服务器`、`60:` `**未安装任何组件、未重新配置服务器**`＝过程自辩（否定式留痕，§4 要求正向条款）；`55:` `意味着 Agent 主机当前**只能执行…**`＝时态现状陈述，注定腐坏；`61:` `验收脚本 `eng/ci/verify_toolchain.py --policy … --scope agent-host` **通过（exit 0）**`＝把一次性运行结果写进文档（前言 §5.6"已核 vs 现行"串轴风险） |
| 悬空引用 | 3 条：`6:` `每项采集命令的 stdout/stderr 原文 + `commands.jsonl``、`30:` `详见 `logs/deep_probe.log``、`61:` `（`evidence/.../logs/*.log` 原文可复核）` → 三者均不在跟踪集（`git ls-files | grep -c commands.jsonl` = 0、`grep -c deep_probe.log` = 0；`evidence/...` 写成省略号伪路径，不可解析）→ **机械列 dangling_links=[] 漏报 3 条**。真实存在的对照（正例）：`5:` `eng/ci/toolchain.lock.json` ✓、`44:` `eng/ci/toolchain.policy.json` ✓、`61:` `eng/ci/verify_toolchain.py` ✓ |
| 与上位冲突 | 是：`38:` 转述 AGENTS 口径为 `Windows x64 = 正式开发/客户端/发布平台，经远程节点执行，离线不阻塞 Linux 任务`，与上位 §11 表把 `Windows 10+ amd64` 与 `Linux amd64` **并列两行交付形态**不同；同一收窄也写在 `docs/owner/ARCHITECTURE_OVERVIEW.md:13-16`（本批 1.21）→ 属同一族上位不一致（记入 §3 节 G6）。另 `24-26:` 把 `zstd 1.5.7`、`xz 5.8.1`、`python3 3.13.5` 当事实登记，而 `AGENTS.md §3` 与 `eng/build/**` 依赖的是另一套（`python3`+`ninja`+`gcc`）→ 本件如实登记"9 项 missing"等于宣告**本仓构建在本控制节点不可执行**，与 §3"构建/测试/检查先于任何'完成'声明"的现行纪律并置时无人裁定谁为准（该冲突应由负责人面处理，不是本件该说的话） |
| 处置建议 | **迁移 → `artifacts/evidence/toolchain/agent-host/<轮次>.md`**（并把 40hex/SHA256/exit 0 读数留在证据件），正式层只保留一句由 `docs/operations/` 指向 lock/policy 的现行说明；理由：§2 第三条过程台账落证据锚 + §4 无 sha/日期/一次性结论 |

### 1.21 docs/owner/ARCHITECTURE_OVERVIEW.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/owner/ARCHITECTURE_OVERVIEW.md` |
| 标题 | `架构总览（Architecture Overview）` |
| 行数 | 117 |
| 角色 | **一份文件承担两个角色**：§1/§2/§4/§5 是**架构设计**（§2 目标目录 `docs/architecture/`），§1"现状核实"、§2/§3 状态表、§6"架构状态汇总"是**负责人面**（`docs/owner/`）。§2"每份文档属于且仅属于一个角色"→ **不合规**；索引 `1050: duty: "L0 架构汇总层"`、status `ACTIVE_NORMATIVE` 亦未区分 |
| 主题 | 平台与发布形态、ACR、Runtime/资源、依赖方向、契约索引、状态汇总 |
| 上游 | 有：`3: > 上游：ASTROCS_DESIGN.md §8（软件架构）` ✓；但 `5-6:` 立刻又列一份自己的权威清单（见冲突列） |
| 下游 | 4 个活引用者：`DEPENDENCIES.md`、`ENGINEERING_SPEC.md`、`eng/ci/check_version.py`、`eng/tools/check_l0_docs.py` |
| 是否正本 | 否——架构轴正本是 `ASTROCS_DESIGN.md §8`，本件自称"汇总层"；而 `docs/architecture/ARCHITECTURE.md` 也在索引登记为 ACTIVE_NORMATIVE（同主题第二份）→ 汇总层与详细层同标 normative 且互不复述关系未定义 |
| 重复或重叠对象 | ① 与 `docs/architecture/ARCHITECTURE.md`（别批）：本件 `74:` `冻结依赖方向：`cli → runtime → registry → modules → cpu_backend`；`io → data_contracts`；`io ⇏ runtime`；`io ⇏ modules`` ↔ `docs/architecture/ARCHITECTURE.md:18` 起 `acsd CLI (唯一入口; parser/JSONL/exit/cancel/crash boundary — API-002)` 的同一分层；② 与 `docs/owner/RELEASE_STATUS.md`（别批）：本件 `7:` `状态词约定同 `docs/owner/RELEASE_STATUS.md` §0（DOC-CONV-001 唯一口径）`＝把状态词正本让渡给彼件，自己仍逐格使用（合法下钻，但"唯一口径"字样归 RELEASE_STATUS，本件再复述即 §1 风险）；③ 与 `docs/owner/PIPELINE_OVERVIEW.md`（同批 1.22）**同一段实测**两处登记：本件 `31:` `**64/64 PASS**（含 5 类负向注入必败）+ `astrocs modules list/verify` units=10 PASS（MOD-001 `59fdeab3`）` ↔ 彼件 `148:` `安全 loader 实测 64/64 PASS` |
| 元信息块 | **两处**：① 头部 `5-7:` 三行"权威：…／状态词约定同…"；② **文件尾部 `112-117:` 一整块裸 YAML 元信息**（`---` 之后）：`authoring_task: GOV-004` / `authoring_owner: SA-GOV-01` / `base_main_sha: caee3e67…`（40hex）/ `convergence_task: DOC-CONV-001` / `convergence_base_sha: da3c4b4a…`（40hex）。机械列 `meta_block=False` → **严重漏报**（其检测只看开头）；`doc_mechanical.csv` 的 `commit_sha` 列抓到 4 个 sha ✓ |
| 写法违规 | `112-117:` 任务编号 ×2 + 40hex ×2；`31:` `（MOD-001 `59fdeab3`）`、`66:`/`67:` `（`91440c16`）`；`32:` `目标安装树定义于控制包 03 §4`＝**控制包编号引用**；`33:` `Fatduck 侧执行（约束 §E.5；FATDUCK_ACCESS.md）；CI WIN-* 检查项已注册（CI-001/CI-001B），本提交未复验`；`28-35:` 全表以"当前提交内可核 + 实测 rc/PASS 读数"为骨架（`64/64 PASS`、`28 步`、`units=10`、`4/4 PASS`、`24/24`）＝一次性读数写进规范；`66:`/`107:` `（…原引「宪章 §F.1」**已废止**，现行 = `ASTROCS_DESIGN.md` §3.2/§4.2/§5.2）`；`70:` `保留中（如实）…不宣称已删除`；`37-40:` 引文块"不写'Windows 已实现/已交付'"＝否定式留痕；`44-45:` `约束 §C.1：ACR 是正式发布后的 CPU/GPU 异构更新；本轮**保留源码和隔离测试**`＝"本轮"过程语域。命令：`grep -nE '本轮|已废止|保留中|不宣称|实测' docs/owner/ARCHITECTURE_OVERVIEW.md` |
| 与上位冲突 | 是（四条）：① `5-6:` 自列权威清单（`ASTROCS_DESIGN.md §8`、`ENGINEERING_SPEC.md §7/§10`、`docs/contracts/ARCH-001.md`、`docs/architecture/*.md`、`docs/api/*`、`eng/contracts/config/module_dll_contract.schema.json`）→ §1 末条"任何文档不声明自己的第二套权威顺序或'唯一口径'"字面命中（还把仓库外类别 `eng/contracts/**` 与文档并列进权威序）；② `13-16:` `**正式开发/客户端/发布平台 = Windows x64**` / `Linux amd64 **仅作**常在线控制…节点；Windows 架构为塑造方向，Linux 便利只作实现面（§B.3）` ↔ 上位 §11 表两行并列（`Windows 10+ amd64`/`Linux amd64`）与 `AGENTS.md §2`"正式平台 Windows x64 与 Linux amd64"→ 下位把一臂降为"仅作"；③ `81:` 自陈 `该方向与 ARCH-001 §3（io 不依赖 runtime/core）存在表述差异，属架构域待审项，本文如实记录`——与合同件冲突写在总览里而不订正（§1"冲突本身是缺陷，需在整治中消除"）；④ `66:` 声明的源码行锚已失效（见悬空列） |
| 悬空引用 | 6 条，其中①为本批最硬：① **源码行锚漂移约 9000 行**：`66:` `lib/infrastructure/scheduler/src/module_adapters.cpp:4257/:4282/:4309 —— P1 八节点 / P2 七节点 / P3 五节点各经 `make_p1/p2/p3_node_module` 绑定` —— 实测 `sed -n '4257p;4282p;4309p'` 三行分别是 `{"median_fwhm_x_px", median(fwhm_xs)},`、PSF-FAST-001 注释、一条注释；真定义点在 **13265 / 13482 / 15364**（`grep -n -E 'make_p1_node_module\|make_p2_node_module\|make_p3_node_module' lib/infrastructure/scheduler/src/module_adapters.cpp`），文件共 15532 行；同一失效锚在 `docs/owner/PIPELINE_OVERVIEW.md:57/91/117` 三处复用（见 1.22）→ 说明"行锚"在本层无任何判据覆盖：`docs/ci/01_CHECKS.md:33` 的 `ALG-LINE-ANCHORS` 自述覆盖面是"扫描面 `docs/**/*.md` 行数锚 + **`docs/algorithms/*.md` 逐符号表行号范围**"，符号级判据不到 `docs/owner/**` → **门禁覆盖面缺口**（本批据此可给出可注入负例：把这三行锚纳入判据即能红能绿）；② `28:` `eng/cmake/toolchain/verify_toolchain.py` ✓ 存在（与 `eng/ci/verify_toolchain.py` 同名两物，本条可解析，登记不判悬空）；③ `64-65:` `cli/runtime_client.cpp`、`runtime/pipeline/typed_dag.py`、`typed_dag.schema.json`、`module_ports.registry.json` 四个**非仓库相对路径**的缩写 → 真实件分别是 `lib/infrastructure/cli/runtime_client.cpp`、`lib/infrastructure/pipeline/typed_dag.py`、`lib/infrastructure/pipeline/typed_dag.schema.json`、`lib/infrastructure/pipeline/module_ports.registry.json`（全部存在，但按原文不可解析）→ 悬空 4 条弱式（路径前缀缺失）；④ `66:`/`107:` 「宪章 §F.1」——仓内无"宪章"件（`git ls-files | grep -c 宪章` = 0）；⑤ `32:` "控制包 03"；⑥ `6:` `docs/architecture/*.md`、`docs/api/*` 通配（机械列据此报的 3 条 dangling 中 `docs/api/PHASE{1,2,3}_API_V1.md` 与 brace 写法属**误报**，brace 展开后三件均在）。正例对照：`92-93:` `docs/contracts/ARCH-001.md`、`RT-001.md`、`DATA_ARTIFACTS.md` 全部存在 ✓ |
| 处置建议 | **拆分**：`§1/§2/§4/§5`（平台与依赖方向陈述）→ 并入 `docs/architecture/`；`"现状核实"` 各表 + `§6 状态汇总` → 并入 `docs/owner/RELEASE_STATUS.md`；**立即删除尾部 `112-117:` 元信息块与全部 40hex/实测读数**，三处行锚改符号锚；理由：§2"每份文档属于且仅属于一个角色"+ §4"开头无元信息块、正文无 commit/读数"（尾部同样不得有）+ §1 冲突须消除 |

### 1.22 docs/owner/PIPELINE_OVERVIEW.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/owner/PIPELINE_OVERVIEW.md` |
| 标题 | `管线总览（Pipeline Overview）` |
| 行数 | 172 |
| 角色 | 与 1.21 同型双角色（§1–§5 装配陈述＝架构设计/模块工作细节；"现状核实/状态汇总"＝负责人面）→ §2 不合"仅一个角色" |
| 主题 | 三 Phase 隔离模型、各阶段内部节点链与装配、执行与资源门、发布形态关系、状态汇总 |
| 上游 | 有但**两处条款号错**：`3: > 上游：ASTROCS_DESIGN.md §1.2（三个命令，三个独立产品）、§8.2（数据流形态）` —— §1.2 ✓ 逐字一致；"数据流形态"**不存在**（§8.2 实为"阶段内：命名块内存管线与块生命周期"；索引 `1057:` 同样写 `§8.2（数据流形态）` → 同一伪标题两份互证）。`5: > 权威：ASTROCS_DESIGN.md §1.2（三命令平级独立）/**§6.2（唯一命令树）**` —— 唯一命令树是 **§7.1**（`### 7.1 命令树（唯一）`），§6.2 是 Phase3 的"流程"；同页第 37 行又正确写 `§1.2/§7.1（唯一命令树…）` → **同文件内对同一要点给出两个条款号** |
| 下游 | 3 个活引用者：`eng/ci/check_version.py`、`eng/tools/check_l0_docs.py`、`eng/tools/doccheck/check_doc_hygiene.py`（全是机器门，无文档级引用者）+ 本批 1.21 第 150 行的文字性提及（"见 ARCHITECTURE_OVERVIEW"，反向） |
| 是否正本 | 否（`ASTROCS_DESIGN.md §4.2/§5.2/§6.2` 与 `docs/contracts/PIPELINE_BLOCK_CONTRACT.md` 才是节点序正本；本件是装配现状） |
| 重复或重叠对象 | ① 与 1.21 同一实测两处登记（`148-150:` vs `ARCHITECTURE_OVERVIEW.md:31:`）；② 与 `docs/modules/MODULE_MAP.yaml` / `lib/infrastructure/pipeline/module_ports.registry.json` 重复节点-端口登记（本件 `72-75:` 自认"绑定表唯一源 = `runtime/pipeline/module_ports.registry.json`"却仍把 20 个节点名逐条复述）；举证：本件 `57-61:` `- 模块注册表：`lib/infrastructure/scheduler/src/module_adapters.cpp`:4257 注册 Phase1 八节点 `astrocs.phase1.{calibration,cosmetic,star-psf,wcs-platesolve,photometry, noise-snr,drizzle,writer}`，逐个经 `make_p1_node_module` 绑定**唯一真实 operation**` ↔ 实测 `grep -oE '"astrocs\.phase1\.[a-z-]+"' lib/infrastructure/pipeline/module_ports.registry.json \| sort -u` 给出同样 8 个 ID → 文档复述登记册内容，二者必腐 |
| 元信息块 | **两处**：头部 `5-12:` "权威 + 状态词约定同 RELEASE_STATUS §0（DOC-CONV-001 唯一口径）"块；尾部 `167-172:` 与 1.21 逐字相同的裸 YAML（`authoring_task: GOV-004`、`base_main_sha: caee3e67…`、`convergence_base_sha: da3c4b4a…`） |
| 写法违规 | 40hex/短 sha **8 处**：`37:` `（CLI-002，`a6c39cc1`）`、`52:` `当前基线的实际装配（BASE=`da3c4b4a`）`、`67:` `（P1-001 `9e09941a`）`、`93:` `（P2-001 `439f9f20`、P2-002 `9e0fa3a8`）`、`119:` `（P3-002 `1a56ffb7`）`、`130:` `（RT-001 `91440c16`）`、`170/172:` 尾部两枚 40hex；日期 `137:` `（2026-09-16 GATE-FIX-RES 订正：旧表述「按最高设计 §8 冻结阈值」是悬空引用）`＝**日期 + 任务编号 + 订正 + 旧表述**四禁同句；历史叙事 `69:`/`110:` `（原引「宪章 §F.1」/「宪章 §7.3/§18.1」**已废止**；现行 = …）`、`109:` `**冻结四投影 TAN/SIN/CAR/AIT registry v1**（BASE 快照；现行 = ASTROCS_DESIGN.md §5.3 八投影、registry v3）`（条款号错 + 版本漂移叙事）、`156:` `已删除 IMPLEMENTED`；裁决/控制包引用 `80:`/`98:`/`144:` `（03 §5）`、`（03 §4）`；一次性读数 `36:` `15/15 PASS`、`66-67:` `7 节点主链…本提交 rc=0`、`111:` `2/2 PASS`、`138:` `ctest rt001_unique_executor 本提交实测 rc=0`。机械列 `dates=[137]` 判对、`task_ids` 抓到 `['GOV-004','GOV-01']`（尾部）却**漏报 GATE-FIX-RES / P1-001 / P2-001 / P2-002 / P3-002 / CLI-001/002 / RT-001 / DOC-CONV-001 / MOD-001**；`commit_sha` 抓到 8 项 ✓ |
| 悬空引用 | ① 三处 `module_adapters.cpp:4257/:4282/:4309`（`57:`/`91:`/`117:`）**全部失效**，真值 13265/13482/15364（同 1.21 ①，命令一致）；② `131:` `module_adapters.cpp:3777-3793（Phase3 resample 行带提交唯一池）` → 实测 3777 行是 `}`、3793 行是一条无关注释 → 区间锚亦失效；③ `39:`/`62:`/`73:`/`115:` `runtime/pipeline/typed_dag.py`、`module_ports.registry.json`、`runtime/artifact_store/phase_product_exchange_validator.py` 为缩写路径（真件在 `lib/infrastructure/pipeline/`）→ 按原文不可解析；④ `55:` `lib/phase1_session/p1_session.cpp`、`85:` `lib/phase2_session/p2_session.cpp`、`104:` `lib/phase3_session/` —— **全部为跟踪件，不构成悬空**（`git -c core.quotePath=false ls-files -- lib/phase1_session lib/phase2_session lib/phase3_session` = 26 个文件，逐条 `ls-files --error-unmatch` 三判 TRACKED）。本条为**审查过程中的自纠项**：这三者与 `docs/TROUBLESHOOTING.md` 里 `lib/snr_estimator`、`lib/healpix_db`、`lib/phase2`（跟踪数 0，见 1.5）目录名前缀相近，若只按前缀直觉判"未入库残留"就会误判；判"不存在"必须逐条用 `ls-files --error-unmatch`，不能按同族类推。它们的真问题是另一类：`docs/KNOWN_LIMITATIONS.md:91-94` 已登记这三目录**不在 `ENGINEERING_SPEC §7` 的 `lib/` 清单、也不在 `MODULE_MAP.yaml` 任何登记面**（无登记面的未声明目录，待裁决），即"文件在、登记缺"，与"登记在、文件缺"相反；⑤ `105:` `p3_wcs.cpp:36`、`112:` `lib/algorithms/projection/module.yaml:79-80 entrypoint: MISSING`（未逐字复核，登记为待证不判悬空）；⑥ `133:` `docs/plugins/infrastructure/21_observability.md §8（G-RES-01）` ✓ 存在 |
| 与上位冲突 | **是，本文件内部即自相矛盾**：`155:` `三 Phase 独立命令:            INSTALLED（**phase1/2/3 × validate\|plan\|inspect\|run**）` ↔ `36-37:` `唯一命令树 `normalize/mosaic/export`…旧 `phase1/2/3` 用户命令与 `validate/plan/inspect` **全部删除**且 rc=2` / `run`/`graph` 不在命令树内…实测 `run --phases` → rc=2 `unknown command 'run'`` ↔ `156:` `遗留 run --phases 连跑:        已删除 IMPLEMENTED（实测 rc=2 unknown command）` → 同一文件第 155 行的状态汇总按**已删除的旧命令树**声明 `INSTALLED`，与本文正文和上位 §7.1（唯一命令树）同时冲突；这是"命令面"级别的错误陈述（CLI 合同轴）。另有 `5:` `§6.2（唯一命令树）` 与 `37:` `§7.1` 的同文件双引（见上游列） |
| 处置建议 | **拆分 + 立即订正**：状态/实测表并入 `docs/owner/RELEASE_STATUS.md`，装配描述并入 `docs/architecture/DATA_FLOW.md` 或 `docs/modules/`；删尾部元信息块与 8 处 sha、把三处行锚改符号锚、把 `§8.2（数据流形态）`/`§6.2（唯一命令树）` 分别改 `§8.2（阶段内…）`/`§7.1`、重写第 155 行为现行命令树；理由：§1"下位与上位冲突时以上位为准；冲突本身是缺陷" + §4 无 commit/日期/历史叙事 |

### 1.23 docs/owner/PROJECT_SPEC.md

| 字段 | 判定 |
|---|---|
| 路径 | `docs/owner/PROJECT_SPEC.md` |
| 标题 | `Astro Celestial Sphere Database（ACSD） 最高设计细节规范` |
| 行数 | 86 |
| 角色 | 内容＝目标态设计总入口（§4–§9 逐 Phase"必须做到什么"、§3 统一观测模型、§7 数据对象不可混淆）＝**架构设计 / 科学定义**；实际在 `docs/owner/`（§2 = 发布状态、验收、裁决记录）→ **角色错放**，应迁往 `docs/design/`（该目录存在，已放 `PHASE1/2/3_DETAILED_DESIGN.md`，本件第 11-14 行正是把三者列为自己的下位件）。索引 `1062: duty: "项目目标态设计细节总入口（最高设计之下）"` 与角色轴自证内容不属负责人面 |
| 主题 | 权威体系、使命、统一观测模型、三 Phase 目标、数据对象、科学门、运行目标、状态与发布、迁移原则 |
| 上游 | 有且正确：`3: > 上游：ASTROCS_DESIGN.md §0.1（唯一权威链）、§0.2（详细文档层与双向索引）` 两条标题逐字一致 ✓（本批唯一双条全对） |
| 下游 | 4 个活引用者：`docs/contracts/DATA_SEMANTICS.md`、`docs/design/PHASE1_DETAILED_DESIGN.md`、`docs/validation/v6/QA_MATRIX.md`、`eng/ci/check_version.py`；另 `artifacts/evidence/v6/review-audit/06_方法与复现.md` 与 `工程控制/RELEASE-05/**`（过程面，不计）。**非孤儿** |
| 是否正本 | 是（目标态总入口轴），但它与 `ASTROCS_DESIGN.md §1/§2/§3/§4–§6` 大面积同题——见冲突列 |
| 重复或重叠对象 | 与**最高设计本体**同题（下位件复述上位顶层决策）。举证：本件 `32:` `所有科学处理都从 `d_k=A_k x+n_k, Cov(n_k)=C_k` 出发。`A_k` 包含光度响应、PSF、像素响应、WCS 和重采样。` ↔ `ASTROCS_DESIGN.md §3.1/§2` 的观测模型陈述（本件第 9 行也自认"最高权威是根 ASTROCS_DESIGN.md（§0 权威链）；本文是其下的项目目标态细节总入口"）；另 `52:` `…权重 w=SNR²/F_ref² 做逆方差集成，**权重是消费时的派生量，不存在独立的 weight 产品或权重模式**` ↔ `docs/GLOSSARY.md:14:` `pixel_weight \| 像素级科学权重=ivar(UPM/integration)；snr² 权重只用于 ablation 对照 \| 无量纲 \|`（GLOSSARY 把 `pixel_weight` 当作在册术语给独立行）与 `docs/TRACEABILITY.csv:5:` `weight_policy=ivar 时 ACR 块禁用（cell-ivar×support 与 CPU 逐像素 ivar 不等价）`（登记册仍有 `weight_policy` 键语义）→ **权重轴三份件三种口径**，见 §2 节 G4 |
| 元信息块 | 无块；但 `5:` `定位：…本文描述 ACSD 必须做到什么，**不描述某次工程修复流水账**。`＝纪律声明型自述（§4 禁项的前向变体，建议改为直接陈述范围） |
| 写法违规 | 日期/sha/任务编号：**无**（机械列三项全 []，与本判定一致 ✓）。命中的是 `86:` §11 整节过程语域：`现有 FROZEN SCI、代码和**旧工程包**凡与本规范冲突，均作为**待迁移基线**而非反证。**本轮**优先完成三阶段设计和统一科学合同，**再做**合同/代码差距审计，**最后实施**；目标规范主体只承载目标态（工程现状属差距审计面）。`——`旧工程包`/`待迁移基线`＝历史叙事，`本轮/再做/最后`＝过程排期（§4"只写现行设计要怎样"）；`60:` `（当前冻结 TAN）`＝现状读数写进目标态件；`107:`/`110:` 类"废止"字样本件无 ✓ |
| 悬空引用 | **0**（本件五条内部指针全部可解析，逐条 `[ -f ]`）：`12-14:` `docs/design/PHASE{1,2,3}_DETAILED_DESIGN.md` ✓✓✓、`15:` `docs/science/UNIFIED_SCIENCE_MODEL.md` ✓、`16:` `docs/references/SCIENTIFIC_REFERENCES.md` ✓ → **本批唯一"全部指针可达"的件**，登记为正面结论 |
| 与上位冲突 | 是（两条，第①条是本批最高价值）：① `86:` `现有 FROZEN SCI、代码和旧工程包凡与本规范冲突，均作为待迁移基线而非反证` —— 在 §1"科学公式与算法推导的唯一权威是 `docs/science/` 与 `docs/algorithms/`"与 §5/AGENTS.md §6"不动科学公式、默认容差、SCI/ALG 冻结定义（除非按变更流程批准）"之外，**由一份 owner 目录下的第二层文档另立了一条权威顺序**（本规范 > FROZEN SCI + 代码），正是 §1 末条禁止的"自己的第二套权威顺序"；同时构成层级倒置（下位件宣布冻结上位为待迁移）。② `82:` `每个 Phase 分别标 CONTRACT_READY、IMPLEMENTED、INSTALLED、VERIFIED 或 unavailable` —— §12.5 唯一口径里没有 `unavailable`（实测阶梯六值含 `READY_FOR_OWNER_REVIEW` 与负向五词），且本件未提 §12.5 → 状态词与上位不同构（与 1.13 的少一项属同族缺陷，此处是**多一项自造词**）。③ `52:`/`64:` 与 GLOSSARY/TRACEABILITY/CONFIG_SCHEMA 的权重口径三分叉（见重复列） |
| 处置建议 | **迁移 → `docs/design/PROJECT_SPEC.md`**，并删第 11 条（`§11 当前迁移原则`）中"凡与本规范冲突均作待迁移基线"的权威排序句与排期句；`unavailable` 改回 §12.5 合法取值；理由：§2 角色-目录一一对应 + §1"权威链只有一条…任何文档不声明自己的第二套权威顺序" |

<!-- PROGRESS: 已审 23/23 -->

---

## 2 同主题组与差异

> 覆盖全 258 份 `docs/` 的索引对账结论见本批 §3"索引与链接缺陷清单"的 D 组；本节只列主题组。

| 组 | 主题 | 成员（路径） | 本批判定 | 主要差异 |
|---|---|---|---|---|
| G1 | **文档索引地图** | `docs/DOCUMENT_INDEX.yaml`（1.1）、`docs/audit/doc_classification.csv`（1.7）、`docs/audit/inventory.csv`（1.8） | **索引只能有一份**：后两份是第二/第三登记面 | 索引有 path/status/duty/upstream/downstream；classification 有 authority+present（20 条指向已删文件仍写 present）；inventory 有模块↔文档↔源码↔门读数 |
| G2 | **科学门/验收状态** | `docs/TRACEABILITY.csv`（1.4）、`docs/KNOWN_LIMITATIONS.md`（1.3）、`docs/audit/risk_verification_T012.csv`（1.9）、`docs/traceability/TRACEABILITY_MATRIX.csv`（别批）、`docs/ci/03_GATES.md`（1.13） | TRACEABILITY 67/67 行 `VERIFIED` 与 KNOWN_LIMITATIONS"终验未达成"**互斥**；risk_verification 自造 7 种状态词；MATRIX 用逐层 `*_status` | 三套 ID 空间（`SCI-/ALG-/DATA-/ENG-/TEST-` vs `MOD-/SRC-/EVIDENCE-` vs `R1..R7`）互不映射；上位 §12.5"登记表不预写状态"被三份同时违反 |
| G3 | **排障/诊断** | `docs/TROUBLESHOOTING.md`（1.5）、`docs/diagnostics/TROUBLESHOOTING.md`（1.9） | 后者为正本，前者合并入后者 | 同名不同目录；前者命令面有错路径与不可再生 exe；后者结构更合 §2 |
| G4 | **权重/weight 口径** | `docs/GLOSSARY.md:14-15`（1.2）、`docs/owner/PROJECT_SPEC.md:52,64`（1.23）、`docs/development/CONFIG_SCHEMA.md:35,58`（1.17）、`docs/TRACEABILITY.csv:5`（1.4） | 四份三种口径，需科学侧裁决 | GLOSSARY 在册 `pixel_weight`/`frame_quality_weight`；PROJECT_SPEC 断言"不存在独立 weight 产品或权重模式"；CONFIG_SCHEMA 把 `weight_mode` 标"作废"却仍列在 config 块里；TRACEABILITY 仍有 `weight_policy=ivar` 的现行门行 |
| G5 | **工具链定位（MinGW64 vs MSVC）** | `docs/development/CODE_STYLE.md:5`（1.16）、`docs/standards/CODE_STANDARD.md:9-11`（别批）、`docs/operations/TOOLCHAIN_AGENT_HOST.md:40`（1.20） | CODE_STYLE 与 CODE_STANDARD 冲突，TOOLCHAIN 件把 MinGW64 归为"Windows 主机关注点" | 上位 §11「Windows 官方工具链为 MSVC」→ CODE_STYLE 需订正后合并入 CODE_STANDARD |
| G6 | **正式平台范围（双平台 vs Windows 优先）** | `docs/owner/ARCHITECTURE_OVERVIEW.md:13-16`（1.21）、`docs/operations/TOOLCHAIN_AGENT_HOST.md:38`（1.20）、`docs/ci/03_GATES.md:18`（1.13 VERIFIED 括号）、`docs/KNOWN_LIMITATIONS.md:13`（1.3） | 四处一致地把 Linux 降为"仅作"、把 VERIFIED 限定 Windows x64，与上位 §11/§12.5 的两平台并列不同 | 属同一族收窄陈述，须一次裁决（不是四份各自微调） |
| G7 | **CI 规范 ↔ 01/02/03/04** | `docs/ci/CI_SPEC.md`（1.14）+ `01_CHECKS.md`（1.11）+ `02_PIPELINE.md`（1.12）+ `03_GATES.md`（1.13）+ `04_ARTIFACTS.md`（1.15） | 五件同域；正本划分未落地：阈值数值同时出现在 CI_SPEC §2.5/§9.2、02 §3、03 §6、04 §1/§5 | CHK-STATIC 在 01 为 P1、03 为 P0；04 §5 与上位 §13 门槛文本不一致；02 图与 CI_SPEC 图并行/串行相反 |
| G8 | **浏览器件** | `docs/browser/HIPS_BROWSER.md`（1.10）、`docs/modules/healpix_browser_qt.md`（别批）、`docs/audit/inventory.csv:14`（1.8） | 两份 docs + 一处登记把浏览器件当 science_doc | STF 分位 p1/p99 vs 0.5/99.5 互斥 |
| G9 | **版本治理** | `docs/VERSIONING.md`（1.6）、根 `VERSION`、`eng/tools/check_version_consistency.py`、`docs/ci/01_CHECKS.md:149-150`（1.11） | 文档描述的扫描面与实现的 `SCAN_ROOTS/SCAN_FILES` 四条不符 | 见 1.6 冲突列 |

## 3 索引与链接缺陷清单（DOCUMENT_INDEX.yaml 全量对账结论，覆盖全部 258 份 docs，不限本批 23 份）

### A 汇总

| 项 | 命令 | 读数 |
|---|---|---|
| 索引登记条目 | `grep '^\s*- path:' docs/DOCUMENT_INDEX.yaml` | 265（docs/ 255 + 仓库根 10） |
| 重复条目 | `sort \| uniq -d` | **0** |
| 登记了但文件不存在（docs 侧 / 根侧） | `comm -23` + 逐条 `[ -f ]` | **0 / 0** |
| 存在但未登记（docs 侧） | `comm -13` | **3**（B-1/B-2/B-3） |
| 索引条目 `duty` 写成路径 | 见 1.1 D-2 | 4 条 |
| `upstream` 括注与设计标题不符 | 见 1.1 D-1 | 58 个去重引用中 20 个不符（5 类条款号指错，波及 86 条条目：73+9+2+1+1） |
| 非文档（.py/.csv/.json/.yaml）当文档登记 | `grep -E '^    - path: "docs/.*\.(py\|csv\|json\|yaml)"'` | 23 条（含 2 个 `.py` 检查器） |
| `archived:` 区段 | 第 1514 行 = 末行 | 空区段 + 死枚举 `ARCHIVED_NON_NORMATIVE`（0 条使用） |
| `downstream` 截断 | `grep -oE '等 [0-9]+ 处' \| wc -l` | 142 条 |
| `status` 分布 | `grep -oE '^\s+status: [A-Z_]+' \| sort \| uniq -c` | ACTIVE_NORMATIVE 178 / ACTIVE_INFORMATIVE 63 / GENERATED 24 |
| 缺 §3 必需字段 | 键全集统计 | **无 `角色`、无 `主题` 两轴**（265/265 缺） |

### B 未登记（存在未登记，逐条）

| # | 路径 | 依据 |
|---|---|---|
| B-1 | `docs/research/CCD_LINEAR_DEFECT_LITERATURE.md` | `docs/research/` 下 6 个文件、索引 5 条 → 唯一缺此一条（`comm -13 <(grep …docs/research) <(ls docs/research)`）。同时违反索引自定规则第 19 行（`docs/**/*.md` 必登记）与 §3"未登记即缺陷" |
| B-2 | `docs/DOCUMENT_INDEX.yaml` | 地图未登记自身；且被 255 条 downstream 当引用者列出 |
| B-3 | `docs/algorithms/anchors/unresolved_registry.json` | 同目录 `anchor_contract.json` 已登记（GENERATED）→ 后缀豁免说不通，索引内部自相矛盾 |

### C 登记了但文件不存在

**docs 侧 0 条**（`comm -23` 空 + 逐条 `[ -f ]` 0 缺失）。反向缺陷在**别的登记件**：`docs/audit/doc_classification.csv` 有 **20 条**指向不存在的文件却仍标 `present`（1.7 悬空列全列），`docs/TRACEABILITY.csv` 与 `docs/owner/PROJECT_SPEC.md` 均为 0。

### D 角色标注与本人判定不符（索引侧）

| 路径 | 索引标注 | 本批判定 | 依据 |
|---|---|---|---|
| `docs/GLOSSARY.md` | ACTIVE_INFORMATIVE，duty"术语唯一性登记"，无角色 | 标准与检查；应落 `docs/standards/` | §2 无 `docs/` 根角色 |
| `docs/KNOWN_LIMITATIONS.md` | ACTIVE_INFORMATIVE，upstream §1.3 | 负责人面；应落 `docs/owner/`；upstream 需扩到 §8/§12.4 | §2 |
| `docs/TRACEABILITY.csv` | **ACTIVE_NORMATIVE**，duty=路径 | 标准与检查；应落 `docs/traceability/`；且其 VERIFIED 全列值使它不配 normative 登记 | §1 + §12.5 |
| `docs/audit/*.csv` 3 条 | ACTIVE_INFORMATIVE，duty=路径 | 过程台账，不属正式文档层 | §2 第三条 |
| `docs/browser/HIPS_BROWSER.md` | ACTIVE_INFORMATIVE，upstream `§8.1（顶层结构）` | 模块工作细节；应落 `docs/modules/`；条款号错（§8.1≠顶层结构） | §1 + §2 |
| `docs/ci/01..04/CI_SPEC` 5 条 | ACTIVE_NORMATIVE ✓ 目录 ✓ | 标准与检查（唯一一致组） | §2 |
| `docs/development/{CODE_STYLE,CONFIG_SCHEMA,TESTING}.md` 3 条 | ACTIVE_NORMATIVE/INFORMATIVE + duty 直接抄 H1（"Code Style"/"Testing"） | 三条角色都非"操作与运营"：应分别落 `docs/standards/`、`docs/contracts/`、`docs/standards/`；duty 无信息 | §2 + §3 |
| `docs/diagnostics/TROUBLESHOOTING.md` | ACTIVE_INFORMATIVE，duty"诊断与故障处置细则" | 操作与运营 ✓ **目录正确**，问题是与 `docs/TROUBLESHOOTING.md` 并存 | §2 |
| `docs/operations/TOOLCHAIN_AGENT_HOST.md` | ACTIVE_INFORMATIVE，duty"工具链与 agent 宿主环境的接管说明" | 目录 ✓ 但文件类型是快照台账 → 应落 `artifacts/evidence/` | §2 第三条 |
| `docs/owner/ARCHITECTURE_OVERVIEW.md`、`PIPELINE_OVERVIEW.md` | ACTIVE_NORMATIVE，duty"L0 架构汇总层"/"L0 流程汇总层" | 各承担两角色（架构 + 负责人面）→ §2"仅一个角色"不成立 | §2 |
| `docs/owner/PROJECT_SPEC.md` | ACTIVE_NORMATIVE，duty"项目目标态设计细节总入口" | 架构设计/科学定义 → 应落 `docs/design/` | §2 |

### E 链接与锚点缺陷（跨全批的可复算清单）

| 类型 | 位置 | 证据 |
|---|---|---|
| **控制 token 污染** | `docs/ci/CI_SPEC.md:213`（并致 `214:` 表格断行） | `od -c` 显示 `<`+`｜｜`(U+FF5C×2)+`begin`+`▁`(U+2581)+`of`+`▁`+`sentence`+`｜｜`+`>`；`git grep -n -e '｜｜'` 全仓仅 1 处 |
| 表格头/分隔粘连 | `docs/ci/01_CHECKS.md:21`；`docs/owner/ARCHITECTURE_OVERVIEW.md:30` | `awk -F'\|' '{if(NF!=7) print NR": "NF}'` |
| 单元格被命令污染 | `01_CHECKS.md:54,57,84,102,103,105,137,139` | 同上（NF=8/9） |
| 缺列 | `01_CHECKS.md:36,37,38,39`（缺 2 列）、`155,156`（缺门禁值） | 同上（NF=5/6） |
| 源码行锚失效 | `docs/development/CONFIG_SCHEMA.md:104`（1139→1145）；`docs/owner/{ARCHITECTURE_OVERVIEW.md:66, PIPELINE_OVERVIEW.md:57,91,117,131}`（4257/4282/4309/3777-3793 → 13265/13482/15364） | `grep -n 'kPixelSmallNPolicy'`；`grep -n -E 'make_p[123]_node_module'`；`sed -n '4257p;4282p;4309p;3777p;3793p'` |
| 悬空条款号 `§9.73` | `docs/audit/risk_verification_T012.csv:3`、`docs/ci/01_CHECKS.md:98,99`、`docs/development/CONFIG_SCHEMA.md:35,58` | 设计 §9 无子条款（`grep -cE '^#{4,6} ' ASTROCS_DESIGN.md` = 0） |
| 版本条款错引 §12↔§13 | `docs/VERSIONING.md:7`、`docs/ci/CI_SPEC.md:161`、`docs/ci/04_ARTIFACTS.md:11,12,20,36` | 设计 §13 正文含"alpha 阶段之前…不出现任何版本信息"；§482 行自证"见 §13" |
| 指向 `run/`（不入库） | `docs/VERSIONING.md:23`、`docs/ci/02_PIPELINE.md:55`、`docs/ci/CI_SPEC.md:248`、`docs/ci/01_CHECKS.md:113` | AGENTS.md §7；`git ls-files -- run` 空 |
| 本机残留被当仓库事实 | `docs/TROUBLESHOOTING.md:29,31,33`（`lib/{snr_estimator,healpix_db,phase2}` 跟踪数 0）；`docs/owner/PIPELINE_OVERVIEW.md:55,85,104`（`lib/phase{1,2,3}_session` 跟踪数 0） | `git -c core.quotePath=false ls-files -- <dir>`；**磁盘 `[ -f ]` 反而为真 → 判"不存在"必须只用跟踪集** |
| 文档内缩写路径不可解析 | `docs/TROUBLESHOOTING.md:35`（`tools\astrocs_diagnose.py`）；`docs/owner/ARCHITECTURE_OVERVIEW.md:64-65`、`PIPELINE_OVERVIEW.md:39,62,73,115`（`cli/…`、`runtime/pipeline/…`） | 真件在 `eng/tools/`、`lib/infrastructure/{cli,pipeline}/`，逐条 `[ -e ]` |
| 尾部元信息块（机械列不可见） | `docs/owner/ARCHITECTURE_OVERVIEW.md:112-117`、`docs/owner/PIPELINE_OVERVIEW.md:167-172` | `---` 之后 5–6 行裸 YAML，含 2 枚 40hex |

### F 对机械列 `doc_mechanical.csv` 的校准（本批 23 行）

| 列 | 判对 | 漏报 | 误报 |
|---|---|---|---|
| `meta_block` | — | `KNOWN_LIMITATIONS`(5–8)、`CONFIG_SCHEMA`(8–19)、两份 owner 尾部块、`DOCUMENT_INDEX`(1–39) 共 **5 例**（该列只扫开头且不含 `authoring_*`） | — |
| `dangling_links` | `01_CHECKS`(check_source_index_v61 ✓)、`VERSIONING` 无、`04_ARTIFACTS` 无 | `TROUBLESHOOTING`(3 条本机残留 + tools\astrocs_diagnose.py)、`VERSIONING`(schemas/、launch/、run/)、`KNOWN_LIMITATIONS`(+2)、`02_PIPELINE`(run/…)、`04_ARTIFACTS`(artifacts 两目录)、`TOOLCHAIN`(commands.jsonl/deep_probe.log/evidence/...)、`CONFIG_SCHEMA`(§9.73、rejection.cpp:1139)、`ARCHITECTURE/PIPELINE_OVERVIEW`(行锚、宪章、控制包 03)、`TESTING`(build 路径) | `01_CHECKS` 的 `docs/**/*.md`、`eng/**/check_*.py`；`CONFIG_SCHEMA` 的 stage1_*.json（glob 实际命中 5 文件）；`03_GATES` 的 `*_gate.json`（实测有对象）；`KNOWN_LIMITATIONS` 的 `eng/contracts/**.schema.json`（glob 形态） |
| `task_ids` | `CI_SPEC`['RELEASE-04'] | 15 份中约 60+ 枚（`DOC-403`、`CLEAN-402`、`ROOT-CONSOLIDATION`、`VER-001`、`V81-ADOPT-006`、`FIX208/FIX-202/203/205/206/406`、`LEG-002..004`、`P15a/P22`、`V13/V14/V15`、`V19R2/R3`、`T101/T107/T108/T109`、`GOV-004/DOC-CONV-001/MOD-001/RT-001/CLI-001..003/BLD-001..002/ARC-001/CFG-001/D-10/D-12/A44/F-CI-002-0x/M3/GATE-501/GATE-FIX-RES/ISA-005/PREEXISTING/FIND-INV-001/SNI-S4-P3X-06/SILENT_OK_UNITS`） | `ARCHITECTURE/PIPELINE` 抓到 GOV 却漏 P1-001 等 |
| `commit_sha`/`sha256` | `DOCUMENT_INDEX`/两份 owner/`risk_T012`/`TOOLCHAIN`(40hex) | `TOOLCHAIN:44` 的 **SHA256 64hex**（该列 `sha256=False`） | `CONFIG_SCHEMA` 的 `712cc7c3`（第三方包 sha1）；`VERSIONING` 的 `sha256=True`（字段名）；`CI_SPEC` 的 `sha256=True`（字段名） |
| `unregistered_index` | 23 份全 False ✓（本批确无漏登） | — | — |
| 结构性列 | `lines`/`h1` 与实测一致；CSV/YAML 无 h1 者按空处理 | `duty`＝路径（**全索引 19 条**，见 G 组；该工具无 duty 列，无从比对） | — |

### G 全索引逐段读完后的补充读数（覆盖全 258 份 docs）

| 项 | 读数 | 命令 |
|---|---|---|
| `duty` 的值就是路径本身（空字段伪装已填） | **19 条**（本批命中 4 条）：`docs/TRACEABILITY.csv`、`docs/algorithms/anchors/anchor_contract.json`、`docs/architecture/{PRODUCTION_EXECUTION_INVENTORY.csv,api_inventory.csv,doc_symbol_namespaces.json,execution_inventory.csv,production_call_paths_stage1.csv,production_call_paths_stage2.csv}`、`docs/audit/{doc_classification,inventory,risk_verification_T012}.csv`、`docs/contracts/{API_CONTRACTS.csv,config_separation_anchors.json,unified_object_registry.json}`、`docs/modules/registry/module_id_migration_baseline.json`、`docs/traceability/{TRACEABILITY_LAYERS.csv,TRACEABILITY_MATRIX.csv,TRACEABILITY_MATRIX.json,traceability_warn_baseline.json}` | `awk '/^    - path: /{p=…} /^      duty: /{d=…; if(d==p) print p}'` → 19 |
| `duty` 是从被登记件正文**截断抓取**（值以连接符/未闭合括号结尾） | 11 条：`754,759,794,800,811,817,943,961,1003,1015,1032`（如 `759: "唯一 I/O 层：FITS/XISF/ahpx 读写、zstd/lz4 压缩、HiPS 读/写（signal/"`、`817: "…`signal = Σ wᵢxᵢ / Σ wᵢ`（仅 eligible ∧"`） | `grep -cE '^      duty: ".*(、\|（\|\(\|→\|∧\|/\|=)"$' docs/DOCUMENT_INDEX.yaml` → 11 |
| `duty` 抓到了源码编码声明 | 2 条：`287`、`1446` 均为 `duty: "-*- coding: utf-8 -*-"`（对象是 `.py` 检查器，即 duty 由抓取正文首行得来） | 同上 grep |
| 模板化 duty（未逐件撰写） | `docs/standards/**` 12 条 duty = `"ACSD X Standard"`（照抄 H1）；`docs/plugins/**` 23 条 notes 同一句 `"插件规范（§0 权威链 ⑥；DOC-001 补登）"`；`docs/modules/registry/**` 9 条 duty 同一句被截断的 `"Registry production 模块(唯一源=module_adapters.cpp descriptor)。职责由"` | `grep -E '^      duty: ' docs/DOCUMENT_INDEX.yaml \| sort \| uniq -c \| sort -rn` |
| **自述"历史/归档/快照/首次测量"却登记在 active 区段、status=ACTIVE_\*** | **10 件**：`docs/audit/{doc_classification,inventory,risk_verification_T012}.csv`（notes "V19R2 文档分类/清单**历史快照**；权威分类见本 DOCUMENT_INDEX.yaml"）、`docs/contracts/{API-001,ARCH-001,RT-001}.md`（duty "**历史合同层**" 而 status **ACTIVE_NORMATIVE**）、`docs/operations/TOOLCHAIN_AGENT_HOST.md`（notes "实测**快照**"）、`docs/quality/{complexity_baseline_v1,coverage_baseline_v1}.md`（duty "deep CI **首次测量**"）、`docs/references/PHOTOMETRY_LITERATURE_REVIEW_ARCHIVE.md`（duty "文献综述**归档**（**历史**评审面）"）、`docs/validation/SCIENCE_FREEZE.md`（notes "**V17 冻结历史快照**"） | `awk '/^    - path: /{p=…} /^      (duty\|notes): /{if(/历史\|归档\|快照\|首次测量/) print p}'` |
| 台账件当活动件 | 根 `memory.md` duty 自述"开发记忆与**历史轮次台账**（history 命名空间）"，status `ACTIVE_INFORMATIVE`，下游 92 处 → 索引正式区段收录历史台账 | `sed -n '81,85p' docs/DOCUMENT_INDEX.yaml` |
| 研究包被标成正本 | `docs/research/SNR_WEIGHT_RESEARCH_PACK.md` 是**唯一** status=`ACTIVE_NORMATIVE` 的 research 件，与 §2"研究与佐证…不重复承载正本口径"直接冲突 | `awk '/^    - path: /{p=…} /^      status: ACTIVE_NORMATIVE/ && p ~ /^docs\/research\//{print p}'` |
| 索引 notes 自身违反 §4 | `832: notes: "P2-SAMP-DOC (**2026-09-09, commit e654d4c2**) 新建…"`（日期＋commit 同句）、`1438: "ASTROCS-CONSTITUTION-ALIGNMENT-V1 **rev18**…承接 05 号 findings 登记册 §STD-F6 处置"`、`1483: "V17 冻结历史快照；当前科学权威以 docs/science 与**约束文件**为准"`（索引自己引入"约束文件"作权威来源）、`1065: "…原引「冻结宪章」**已废止**…"`、`1272:` 压缩结论长文含 `2026-05-01` 型标准日期与实测读数 | `grep -n 'e654d4c2\|rev18\|约束文件\|已废止' docs/DOCUMENT_INDEX.yaml` |
| 索引把代码仓内 README 当文档层落点 | `488: downstream: "…lib/phase1_session/README.md…"`、`807: downstream: "…lib/phase1_session/memory.md"`、`797/808/844/850/856: notes: "合同落位 lib/…/README.md r1 + module.yaml … entrypoint=MISSING"` → 合同正文驻留 `lib/**`，而 `docs/` 索引覆盖声明不含 `lib/**`（见 §6 证伪 #3） | `grep -n 'lib/.*README' docs/DOCUMENT_INDEX.yaml \| head` |

**据 G 组对本清单前文的修订**：D-2 的"duty＝路径 4 条"应读作"全索引 **19** 条，本批命中 4 条"；`docs/audit/*.csv` 三份的处置由"迁出或删除"升格为**删除优先**——索引自身 notes 已写明"权威分类见本 DOCUMENT_INDEX.yaml"并自认三者是"历史快照"，即它们不是待定的正式件，而是已被正本声明作废的残留。

## 4 合并用简表（23 份 × 短字段，供机器并入总台账）

**"活下游"列的唯一取数命令**（对 23 份逐份跑同一条，数字不得另算）：

```bash
git -c core.quotePath=false grep -l -F <path> -- . \
  ':!docs/DOCUMENT_INDEX.yaml' ':!docs/audit/*' ':!reports/*' ':!artifacts/*' \
  ':!run/*' ':!工程控制/*' ':!问题扫描/*' | grep -v -x <path> | wc -l
```

该式只认**全路径字面量**；对 `02_PIPELINE.md`、`04_ARTIFACTS.md` 两个值 0 的件，另用 `git grep -n -E '<basename>'` 复检出散文提及（下表以"0 / +N 裸名"标注），两者不互相顶替。

| 路径 | 角色 | 目录相符 | 上游标注 | 活下游 | 正本 | 处置 |
|---|---|---|---|---|---|---|
| docs/DOCUMENT_INDEX.yaml | 索引地图 | 是（§3 授权） | 注释自标 §0.2/§8 | 机器门 3 | 是（不合格） | 保留＋重建（补角色/主题轴、删版本锚与历史叙事） |
| docs/GLOSSARY.md | 标准与检查 | 否（docs/ 根） | §附录A ✓ | 9 | 是（索引件） | 迁移 → docs/standards/ |
| docs/KNOWN_LIMITATIONS.md | 负责人面 | 否（docs/ 根） | §1.3（覆盖不足） | 30 | 是 | 迁移 → docs/owner/ ＋瘦身台账语域 |
| docs/TRACEABILITY.csv | 标准与检查 | 否（应 docs/traceability/） | 缺（CSV） | 19 | 否 | 迁移＋与 TRACEABILITY_MATRIX.csv 合并 |
| docs/TROUBLESHOOTING.md | 操作与运营 | 否（docs/ 根） | §7.2 ✓ | 2 | 否 | 合并 → docs/diagnostics/TROUBLESHOOTING.md |
| docs/VERSIONING.md | 合同说明 | 否（docs/ 根） | §13 ✓（内文误引 §12） | 23 | 是 | 迁移 → docs/contracts/ ＋订正扫描面/§12→§13 |
| docs/audit/doc_classification.csv | 第二索引 | 否（正式层） | 索引代标 §0.2 | **0** | 否 | **删除** |
| docs/audit/inventory.csv | 过程台账 | 否（正式层） | 索引代标 §0.2 | **0** | 否 | 删除／迁 artifacts/evidence/ |
| docs/audit/risk_verification_T012.csv | 过程台账 | 否（正式层） | 索引代标 §0.2（与内容无关） | **0** | 否 | 迁 artifacts/evidence/risk-verification/ |
| docs/browser/HIPS_BROWSER.md | 模块工作细节 | 否（单文件目录） | §8.1 ✗（应 §8.4） | **0** | 否 | 迁移＋与 healpix_browser_qt.md 合并 |
| docs/ci/01_CHECKS.md | 标准与检查 | **是** | §12.4 ✓ | 76 | 是 | 保留＋修 16 破损表行＋修两处"待写入" |
| docs/ci/02_PIPELINE.md | 标准与检查 | **是** | §12.4 ✓ | **0** / +1 裸名 | 否（自认下钻） | 保留＋去抄写留指针、修 nwoker |
| docs/ci/03_GATES.md | 标准与检查 | **是** | §12.4＋§12.5 ✓ | 4 | 否 | 保留＋订正状态表/门禁级/§2.3 指针 |
| docs/ci/CI_SPEC.md | 标准与检查 | **是** | §12.4 ✓ | 17 | 是 | 保留＋**清除 :213 控制 token 污染并合表** |
| docs/ci/04_ARTIFACTS.md | 标准与检查 | **是** | §10＋§12.4 ✓ | **0** / +2 裸名 | 否 | 保留＋删 §5 复述、订正 §12→§13 |
| docs/development/CODE_STYLE.md | 标准与检查 | 否（应 docs/standards/） | §8 ✓（上位选错） | **0** | 否 | **合并 → docs/standards/CODE_STANDARD.md** |
| docs/development/CONFIG_SCHEMA.md | 合同说明 | 否（应 docs/contracts/） | §8（应 §3.2/§7.2） | 7 | 部分 | 迁移＋删元信息块/裁决句/行锚 |
| docs/development/TESTING.md | 标准与检查 | 否（应 docs/standards/） | §8（应 §12.4） | 1 | 否 | 合并 → docs/standards/TEST_STANDARD.md |
| docs/diagnostics/TROUBLESHOOTING.md | 操作与运营 | **是** | §7.2 ✓ | 1 | **是** | 保留为正本（吸收 docs/TROUBLESHOOTING.md） |
| docs/operations/TOOLCHAIN_AGENT_HOST.md | 快照台账 | 目录 ✓ 类型 ✗ | §11 ✓（不承载内容） | **0** | 否 | 迁 artifacts/evidence/toolchain/ |
| docs/owner/ARCHITECTURE_OVERVIEW.md | 双角色 | 否 | §8 ✓ | 4 | 否 | 拆分＋删尾部元信息与 40hex/读数＋行锚改符号锚 |
| docs/owner/PIPELINE_OVERVIEW.md | 双角色 | 否 | §1.2 ✓ / §8.2 ✗ | 3 | 否 | 拆分＋修 :155 命令树矛盾与三处行锚 |
| docs/owner/PROJECT_SPEC.md | 架构设计/科学定义 | 否（应 docs/design/） | §0.1＋§0.2 ✓ | 4 | 是 | 迁移＋删"待迁移基线"权威排序句、`unavailable` 归 §12.5 |

净效果：23 份中 **13 份角色/目录或文件类型不符**；**6 份真孤儿**（`doc_classification.csv`、`inventory.csv`、`risk_verification_T012.csv`、`HIPS_BROWSER.md`、`CODE_STYLE.md`、`TOOLCHAIN_AGENT_HOST.md`），另 `02_PIPELINE.md`/`04_ARTIFACTS.md` 只靠散文提及存活；`docs/ci/**` 与 `docs/diagnostics/**` 是本批唯一"目录承载角色正确"的 6 份。

## 5 覆盖率自报

- 已审 **23 / 分配 23**（清单 `独立审计/批次清单/DB-11.txt` 23 行，逐行 `tr -d '\r'` 后读入，无缺读项）。
- **未逐行读的名单：空。** `docs/DOCUMENT_INDEX.yaml` 1514 行分六段全量读完（1–180、181–480、481–820、821–1150、1151–1500、1500–1514）；其余 22 份最长 273 行（`docs/ci/01_CHECKS.md`），均整篇读完。
- 为判重复与上下游**读过**的别批文件（只对清单项下结论）：`ASTROCS_DESIGN.md`（§0/§7.1/§11/§12.5/§13/附录 A 的标题与正文）、`docs/standards/{CODE_STANDARD,TEST_STANDARD}.md`、`docs/modules/healpix_browser_qt.md`、`docs/traceability/TRACEABILITY_{MATRIX.csv,LAYERS.csv,MATRIX.json}`、`docs/science/{CALIBRATION,DRIZZLE,INTEGRATION,ASTROMETRY,CONTROL_WEIGHT_SNR,PHASE2_UPM,NOISE_MODEL,PHOTOMETRY,PSF}.md`（只取标题/节号验锚可达）、`docs/contracts/DATA_SEMANTICS.md`（§4a/§27.2 标题）、`eng/tools/check_version_consistency.py`（读 `SCAN_ROOTS/SCAN_FILES` 文本）、`eng/ci/checks.json`（取 `"id"` 字面量集）、`lib/algorithms/coverage/CMakeLists.txt`、`lib/algorithms/coverage/src/rejection.cpp`、`lib/infrastructure/scheduler/src/module_adapters.cpp`、`lib/infrastructure/pipeline/module_ports.registry.json`（锚点行/ID 集）。
- 未执行任何东西：无构建、无 ctest、无 `run_checks.py`（含 `--check`/`--self-test`）、无 `eng/**` 脚本、无 import。全程只 `git status`/`ls-files`/`grep` 类只读命令与 `sed -n`/`awk`/`grep`/`od -c`/`wc`/`comm`/`uniq`。
- 收工 `git -C "F:/Astro dev/Astro CS Normalization Database" -c core.quotePath=false status --porcelain` 原文：

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工逐字相同 → 本批取证未污染被审对象。唯一登记的自我纠正：`1.22` 悬空条目④曾按目录前缀把 `lib/phase{1,2,3}_session/*` 判为"跟踪集不存在"，复查 `ls-files --error-unmatch` 确认三者**均为跟踪件**，正文已就地改正，并把误判写成判据（"判不存在须逐条 `--error-unmatch`，不得按同族前缀类推"）。

## 6 我证伪了任务书的哪个前提

被审对象：`tasks/AUD-101_文档现状全量审计.md`。

| # | 任务书前提 | 实测 | 命令 |
|---|---|---|---|
| 1 | "以 `docs/` 为重点（当前**约 233 份**、**30+ 子目录**）" | **口径不钉死则两项都不成立**：`docs/` 跟踪文件 **258**，其中 `.md` 恰 **233**（另 12 csv / 7 json / 3 yaml / 2 py / 1 其他）→"233 份"只在 `docs/**/*.md` 口径下为真；子目录一级 **23**、二级 **13**（合计 36）→"30+ 子目录"在一级口径下**高估**（23<30），只有含二级才成立（36>30）。本条只给两套可复算口径与命令，不引任何他人汇总数。 | `git -c core.quotePath=false ls-files -- docs \| wc -l`；`… \| grep -c '\.md$'`；`… \| grep -oE '^docs/[^/]+/' \| sort -u \| wc -l`；`… \| awk -F/ 'NF>3' \| grep -oE '^[^/]+/[^/]+/' \| sort -u \| wc -l` |
| 2 | 步骤 5："核对 DOCUMENT_INDEX 与文件树：未登记、登记不存在、**角色标注不符**的条目" | **"角色标注不符"在当前索引上不可按字面执行**：265 条的全部键是 `path/status/duty/upstream/downstream/notes`，**无 role/角色、无 topic/主题**（`grep -niE '(role\|topic\|角色\|主题)' docs/DOCUMENT_INDEX.yaml` → 0 命中），`status` 取值域是活动分类三值而非 §2 九类角色。本批因此把该项改写为"以目录＋duty 文本反推角色再判不符"，并把"补角色轴"列为索引重建必办项——任务书隐含的"索引已含角色标注"前提被证伪。 | 见 1.1 冲突列 B-1 的键统计 |
| 3 | 范围："根目录各 `.md`、`docs/` 下全部文档、`实验/` 与 `工程控制/` 下的说明性文档" | 范围声明漏掉三个承载 markdown 的大目录：`lib/**` **110**、`artifacts/**` **67**、`eng/**` **26**（全仓跟踪 `.md` = **540**）。它们不是无关件——索引 `488` 行把 `lib/phase1_session/README.md` 当 downstream 引用者登记，`797/808/844/850/856` 的 notes 把合同落点写成 `lib/…/README.md r1 + module.yaml`，`docs/ci/01_CHECKS.md:33` 的锚门自述扫描面为"全库文档行号锚"。→ 按 AUD-101 范围做完，唯一索引地图仍只覆盖全仓 markdown 的 **265/540 = 49%**，§3"全文档集唯一索引地图"与审计范围互不闭合。 | `git ls-files -- '*.md' \| wc -l`；`… \| awk -F/ '{print (NF==1)?"(root)":$1}' \| sort \| uniq -c` |
| 4 | 产出格式："格式用 `templates/TPL_文档审计台账.md`" ＋ 前言 §4 的 14 字段 | **模板与前言互斥**：模板 §1 表只有 11 列，缺 `行数`、`写法违规`、`悬空引用`，并把"含日期/编号/历史叙事"与 `写法违规` 合一 → 按模板填写必然丢掉前言要求的三类证据。本成稿以 §4 字段为准（每份 14 字段）另附 §4 合并简表。 | 两份文件对读 |
| 5 | （未证伪项） | 步骤 2/3/4 与验收门"台账覆盖全部现存文档、逐份可核"可按字面完成；`docs/TRACEABILITY.csv` 的 67 行 `authority_doc`/`implementation_files`/`test_files` 三列路径**全部可达**（0 悬空）→ 与"追溯链普遍断链"的隐含担忧相反，该件断的是状态列（67/67 `VERIFIED`、三个判别列零信息量），不是路径。 | 见 1.4 悬空列 |

<!-- PROGRESS: 已审 23/23 · 全批完成（清单 23 份逐份 14 字段齐，DOCUMENT_INDEX.yaml 全量对账完成） -->



