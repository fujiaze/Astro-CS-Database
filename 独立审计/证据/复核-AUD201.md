# 复核-AUD201 —— AUD-201 测光核验的第②层独立复核

复核对象：`独立审计/证据/AUD-201-测光核验.md`（作者为另一路代理，本次复核前未读其正文，逐条先自行复算后按其小节对比）。
基线：仓库 `F:\Astro dev\Astro CS Normalization Database`，HEAD=`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`。

开工工作树核对（`git status --porcelain` 原文，恰两行）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

纪律：全程只读；未构建、未跑 `ctest`、未跑 `eng/**` 检查器、未跑三命令端到端；脚本与输出只落
`独立审计/复算件/aud201-v/`（`python -B` + `PYTHONDONTWRITEBYTECODE=1`）；未 import 仓库内 Python。

---

## V1 —— 被正本否定的通量式仍在库里且"被活跃调用"

**判定：降级**（"存活"确认；"可达产品路径"推翻；"星等计两次"的机理与 0.459 dex 数值判待证）

### 我的独立复算

1. **第一性推导（星等因子进几次）**：探测器计量光子数，`dN_γ = F_λ·T·Q·(λ/hc)·dλ`，故
   `F_syn ≡ hc·N_γ = ∫F_λ·T·Q·λ dλ`，量纲 `W·m⁻²·nm⁻¹·nm·nm = W·m⁻²·nm`。星等是这条积分的**输出**
   （`m = ZP − 2.5log10 F_syn`），不是输入。数值核对（我的脚本 A 项）：
   `F_syn` 与 `hc×∫(F_λλ/hc)TQ dλ` 相对差 **3.02e−16**；同一被积式去掉 `λ` 得能量通量，
   与 `F_syn` 比值 **649.5**（≈带内 λ_eff）⇒ `λ` 恰进一次、不可多也不可无。
   绝对谱情形下星等因子进 **0 次**：同温度、ΔG=1 mag 两颗星，`F_syn` 比值实测
   **0.3981071705534978** vs 理论 `10^−0.4 = 0.3981071705534972`（亮度已在 `F_λ` 里）。
   脚本：`独立审计/复算件/aud201-v/v1_fsyn_double_count.py` → `v1_fsyn_double_count.json`（日志 `v1_run.log`）。

2. **机理判别表（关键分歧）**：该函数喂的是**未解码的 uint8**（`spectrum_integrator.cpp:207`
   `sed_flux[i] = (double)spectrum_uint8[i] * mag_factor;`，全程不乘 `flux_mul`）。所以它是否"把亮度计两次"
   取决于 byte 是否载有亮度，即 `flux_mul` 逐星是否自适应。我按三种 byte 语义各算注入项
   `inj_i = log10(F_syn正确/F_syn错)`（JSON 的 `E_quantization_regime_discriminator`，n=4000，seed 777/778/779）：

   | byte 语义 | MADσ(inj) dex | R² vs G | R² vs 1/T | 斜率 dex/mag(G) |
   |---|---:|---:|---:|---:|
   | PROPORTIONAL（byte ∝ 绝对谱、全局尺度、零下界） | 0.3818 | **1.000** | 0.000 | **0.400** |
   | GLOBAL（全局尺度 + 非零下界） | 0.4072 | 0.013 | 0.976 | 0.076 |
   | ADAPTIVE（逐星 min/mul 自适应＝本仓审计所述语义） | 0.7159 | **0.00035** | 0.985 | 0.018 |

   ⇒ "`+0.4·G_i` dex 加性项"只在 PROPORTIONAL 语义下成立；而 `spectrum_integrator.h:32` 与
   `DR3SP_SCHEMA_AUDIT.md:82` 自己声明 `flux_min/flux_mul` **逐星不同、实测跨 6 个数量级**（＝ADAPTIVE），
   这两条断言互斥：ADAPTIVE 下 `10^(−0.4G)` 是**替代丢失的逐星量化尺度**，其 G 依赖几乎完全抵消，
   残差是**色项**（R²(1/T)=0.985）。0.459 dex 这个数只能来自 `run/SCI-PHOT-FORMULA-01/evidence/`，
   该目录被 `.gitignore:17` 排除、本机不存在、跟踪集里 0 个文件 ⇒ 该数值与机理均不可由仓库材料复算。
   旁证其"自洽但不可核"：`0.459 dex × 2.5 = 1.1475 mag` 与文中"= 1.147 mag"逐位吻合，
   反推该样本 `MADσ(G) = 0.459/0.4 = 1.1475 mag`——即该数字是**由 +0.4G 假设反推**而非独立测得。

3. **调用点实数（生产面）**：命令
   `git grep -n -I -E "compute_f_syn(_cached)?\b" -- "."`（该正则带 `\b`，已排除 `*_xpsd` 变体）：
   - **生产路径调用者 = 0**。生产 src 只调 `compute_f_syn_cached_xpsd`：`pc_api.cpp:386/796/1076`、
     `frame_photometry_fit.cpp:268`；`pc_api.cpp:385` 注释"不再使用 uint8*10^(-0.4G) 猜测公式"。
   - 该两函数不在任何公共头/导出面：`spectrum_integrator.h` 位于 `cpp/src/`（内部头），
     `git grep -n "f_syn" -- lib/include` = 0 命中，`*.def/*.map` 0 命中。
   - 调用者全在测试/工具树：已登记 ctest 1 处（`p1phot_tests_units.cpp:392`，属 `add_test p1phot_units`）；
     **未登记的死测试文件** 2 处（`test_spectrum_integrator.cpp:218/228`，该文件在 CMake 里无目标，
     `git grep "test_spectrum_integrator" -- CMakeLists.txt` 只出现在 CI 盘点/清理脚本里）；
     开发工具 1 处（`fsyn_export.cpp:102`）；Python 孪生 2 处（`fsyn_astrocs.py`、`gate4_gaiaxpy_compare.py:232`）。
   - 反向缺陷（成稿未提）：`spectrum_integrator.cpp:279` 注释仍写"循环内高频日志（被 pc_api.cpp OpenMP
     并行循环调用）"——**虚报生产可达**，与 grep 实数矛盾，会误导后来者。
   - 历史可达性：`git log --oneline -S` 显示生产切换到 XPSD 解码发生在 `70edb842`（2026-08-08，
     "fluxMin/fluxMul 线性解码替代 uint8*10^-0.4G"）⇒ **该错式曾是生产通道**，早于该提交的产物才可能带偏差；
     HEAD 不带。

### 与成稿差异

- 成稿 AUD201-001 的"正本口径正确、该式非正本"我独立复算同向 ⇒ **确认**。
- 成稿标题与结论句写"仍可达 / 被三处活跃调用"并给 CONFIRMED。实数：生产可达 **0**，三处活跃调用里
  `test_spectrum_integrator_golden.py:13` 是**文档字符串不是调用**，而真正的 C++ 调用
  `test_spectrum_integrator.cpp:218/228` 它漏计且该文件未进构建。⇒ "可达产品路径 ⇒ 产品输出可能带该偏差"
  **推翻**；应为"死代码＋1 条登记测试把它锁成 golden"。
- 成稿机理句"×10^(−0.4·G) 会把星等二次计入亮度（XP 绝对谱的幅度本身已含星的亮度）"——括号只对
  **解码后**的绝对谱成立，而该函数不做解码 ⇒ 与我的判别表冲突：ADAPTIVE 语义下不成立（R²(G)=0.00035）。
  成稿照抄了仓库文档里两处互斥断言，未做我这个"三种 byte 语义"的判别，因此把不可复算的机理当成了 CONFIRMED。

### 定级建议

**P2**。理由：HEAD 生产面 0 调用 ⇒ 无产品正确性风险；真实缺陷是三条卫生问题——
①非正本口径的公共函数仍存活并被一条登记测试固化为 golden；
②源码注释虚报"被 pc_api 并行调用"（可达性说谎）；
③文档把该通道的误差机理写成不可复算且与同文档自陈的量化语义互斥的"+0.4·G_i/0.459 dex"。
整改：降为 `[[deprecated]]` 或移入测试夹具；把"生产可达图上不得出现 `compute_f_syn*`（除 `*_xpsd`）调用边"
做成机器门（带正/负例）；文档机理句改为可复算表述（丢 `flux_min/flux_mul` ⇒ 绝对量纲无定义 + 色项），
并把 0.459 dex 标注为"证据在 `run/`、当前不可复核"。

**抬级到 P1 的唯一路径**：若交付物中存在 `70edb842` 之前生成的测光产品（`p1_phot.json` 的
`zero_point_mag`/`ZP_syn`/`sigma_residual`），那批产品带该偏差 ⇒ 属产品面缺陷。

### 缺什么证据

1. `run/SCI-PHOT-FORMULA-01/evidence/{a1,a2,c1,c2,d1}.json` 全部不在跟踪集（`.gitignore:17`，本机也不存在）
   ⇒ 0.459 dex、+16.34 mag、−0.0037 mag 三个锚数字**当前无仓库内证据**。
2. 需一条 Linux 节点上的登记测试实数：`ctest -R "p1phot_units" --output-on-failure`（本次按纪律**未跑**，
   故"该式被登记测试锁为 golden"仅由 `p1phot/CMakeLists.txt:112` + 源码调用点定，未验证其当前为绿）。
3. 需 XPSD `flux_min/flux_mul` 的逐星分布实测（是否 ∝ 亮度、是否跨 6 个数量级）——它决定 V1 机理到底是
   "计两次"还是"替代尺度"，跟踪集里没有任何 per-star 量化参数样本（`git grep flux_mul -- "*.csv" "*.json"` 仅命中台账文字）。
4. 需交付物清单与生成提交日期的对照，才能判定是否有早于 `70edb842` 的产品带偏差。

<!-- PROGRESS: 1/6 -->

---

## V2 —— λ 因子是色项：其"25 倍裕度排除备择假设"的论证

**判定：推翻**（推翻的是"25× 裕度"这条**论证**；λ 因子本身的正确性由别的路径成立，正本无需订正）

### 我的独立复算

脚本 `独立审计/复算件/aud201-v/v2_lambda_color_term.py` → `v2_lambda_color_term.json`；
`独立审计/复算件/aud201-v/v2b_csv_and_discriminator.py` → `v2b_csv_and_discriminator.json`（日志 `v2_run.log`、`v2b_run.log`）。
我自己写的积分核，通带取仓库**数据**：官方 `GaiaEDR3_passband.dat`（列 2/4/6＝能量模式）与生产模型通带
`eng/packaging/config/filters.json` 的 Baader R（73 点，572–716 nm）× `qe_curves.json` 的 GSENSE4040BSI。

1. **色项 = 光子加权平均波长**：`Δm_λ = −2.5log10(∫FRλdλ/∫FRdλ) = −2.5log10⟨λ⟩_{F,R}`。四组**不同源** SED 独立算：

   | SED 族 | 色轴 BP−RP 跨度 | **官方 G 通带** 峰峰 | **生产通带 Baader R×QE** 峰峰 | 生产通带斜率 mag/(BP−RP) |
   |---|---|---:|---:|---:|
   | 黑体 3000–12000 K（成稿同跨度） | −0.55 … 1.62 | **0.2708** | **0.0145** | −0.0066 |
   | 黑体 2500–50000 K（不同网格） | −1.08 … 2.13 | 0.3841 | 0.0212 | −0.0061 |
   | 幂律族 F_λ∝λ^α，α∈[−4,4]（非黑体族） | −1.22 … 2.34 | 0.4197 | 0.0220 | −0.0062 |
   | 黑体+巴尔默跳 35%（结构扰动） | −0.90 … 0.52 | 0.1900 | 0.0084 | −0.0059 |

   ⇒ 成稿的 **0.2708 mag 我逐位复现**（它用的是**官方 G 通带**，黑体族、跨度、积分法都无关，四组里 G 通带
   斜率稳定在 −0.122…−0.134 mag/(BP−RP)）。**但生产 `F_syn` 用的不是 G 通带，是本帧相机通带**：
   同一族 SED 下 λ 色项峰峰只有 **0.0145 mag**，比 G 通带小 **19 倍**。

2. **真实样本的颜色跨度**（跟踪 CSV `gate4_compare_per_star_1050.csv`，n=1050）：
   BP−RP 的 p5–p95 = **0.833 … 4.984**（中位 2.128），G 的 p5–p95 = 11.72…14.94。
   ⇒ 样本主体**比黑体网格 3000–12000 K 能达到的 BP−RP≤1.62 更红**（M42 消光），
   把 G 通带峰峰 0.27 与"该样本"实测色轴量直接相除，是**两个不同色跨**之比。
   我把黑体网格折回样本色跨（只取 BP−RP∈[0.83,4.98] 的 SED，剩 3 个）：峰峰 **0.0718 mag** ⇒ 裕度 **6.7×**；
   换幂律族 0.1515 ⇒ 14.2×；换宽网格黑体 0.1156 ⇒ 10.8×。**没有一个口径给得出 25×。**

3. **成稿 0.0107 mag 我复算**（同 CSV、同两列）：`median(astrocs_mag_G − gaiagx_mag_G) = +0.000584`、
   MAD-σ = 0.001667（与成稿逐位同）；但"按 BP−RP 分 6 箱的箱间 median 互差"这个数**依赖分箱方式**：
   等频 6 箱 = **0.00648**，等宽 6 箱 = **0.02405**，成稿给 0.01070 ⇒ 落在两者之间，其口径不可从成稿复现。
   该样本沿色轴的真实趋势：corr = 0.867、斜率 **0.00222 mag/(BP−RP)**（与成稿 AUD201-009 的 0.0023 一致）。

4. **决定性的一条：这个样本对备择假设零灵敏度。** 备择假设是"**XPSD 容器**给的 `F_λ` 可能是光子谱通量"。
   而 CSV 两臂的谱都来自 `gaiaxpy.calibrate()` 的**绝对浮点谱**（`gate4_gaiaxpy_compare.py:127` 取 `flux`、
   `:144` `astrocs_fsyn(flux,…)`），**从不经过 `byte·flux_mul+flux_min` 的容器解码**
   （容器解码在 `xpsd_production_validation.py` / `xpsd_cpp_crosscheck.py`，其结果件不在跟踪集）。
   ⇒ 两臂同源同约定，它们的差只能检验"我的积分核 vs GaiaXPy 的积分核"，**不可能**检验容器的单位约定。
   这正是任务要我查的"真实星样本的谱是否绕过了某种解码刻度"——**是，绕过了容器解码刻度**。
   所以"绝对刻度未锚定（在跟踪集内不可复核）"这条成立。

5. **该假设真正的可判别量是绝对偏移，不是色项**（我的量级表，`v2b` 的 `part2`）：
   能量谱 vs 光子谱两支约定在同一条积分式下给出的 `m_syn` 差 = `−2.5log10⟨λ⟩` =
   **G 带 −6.86…−7.13 mag、RP 带 −7.19…−7.25 mag、生产 Baader R×QE 带 −7.009…−7.023 mag**
   ——即 **约 7 mag 的整体平移**，色项只是它的 0.0145（生产带）/0.27（G 带）的**变化部分**。
   真正排除备择假设的锚应是 §2a.6"真实数据（生产链）"那条 `median(m_syn−G) = −0.0037 mag`：
   若容器是光子谱，该值会是 ≈−7.0 mag 而非 −0.004 mag ⇒ 裕度 **~1900×（约 3 个数量级）**，
   且**用容器解码的 26211 颗星**。但该锚的证据件 `run/SCI-PHOT-FORMULA-01/evidence/a1_*.json` 不在跟踪集
   ⇒ 只能判"该锚当前不可复核"，不能拿一个零灵敏度的 CSV 统计冒充它。

### 与成稿差异

- 成稿 AUD201-005 的三个数我都能对上或解释：0.2708 逐位复现；median 0.000584/MAD 0.001667 逐位复现；
  0.0107 是 6 箱口径的产物（我两种常规口径给 0.0065 / 0.0241）。
- 差异在推理：成稿把"官方 G 通带 × 黑体全网格"的峰峰（0.27）当作**备择假设在生产口径下应有的信号**，
  再除以"两臂同源、不经容器解码"的样本统计（0.011），得 25× 并宣布"备择假设被排除"。
  我这条链三处断：①通带口径错（生产带峰峰 0.0145，小 19×）；②色跨口径错（样本比网格更红，折回后 0.0718）；
  ③**样本对受检假设零灵敏度**（两臂同一谱源）。③是致命的：前两条即使修正到 2–6×，结论也只能是"不可判"。
- 成稿的"负例：去掉 λ ⇒ 色轴互差跳到 0.226 mag（注入实验）"同样是在**同源两臂**上做注入，
  它检验的是"我的实现 + λ 开关"的自洽，不是"容器的单位约定"。

### 定级建议

- **V2 这条论证本身：非缺陷（不进 P0/P1）**——正本口径无需订正，成稿的"λ 因子成立"结论对，
  但其"25× 裕度"论证**不成立**，不得作为"备择假设已排除"的证据登记。
- **同轴的真实缺陷在另一处（建议单独立案，P1）**：现行 §2a.6 的绝对刻度锚（7 mag 量级杠杆）
  其证据件 6/6 不在跟踪集 ⇒ 该锚当前**不可复核**；而仓库里唯一在跟踪集内的 1050 星 CSV 被成稿（以及
  AUD201-003 的整改建议）当成了色轴判据的数据源，而它对容器约定零灵敏度 ⇒ **按成稿的改法补出来的判据
  会是一条恒绿判据**。这一条比"25× 数字错"更值得进清单。

### 缺什么证据

1. 容器口径的判别锚：需在 XPSD 解码谱上直接量 `median(m_syn − G)`（现应为 ≈0，若容器是光子谱则 ≈−7 mag），
   且证据落跟踪集。本机无 XPSD 数据（`GaiaDR3SP/` 为 gitignore 只读集，本审查未读大文件）。
2. `xpsd_production_validation.py` 的实际结果件（脚本在库、结果不在库）——它才是"容器解码 vs 官方绝对谱"
   的逐点比较，是判该备择假设的正解。要跑的那条：`py -3.12 xpsd_production_validation.py --csv <sample.csv> --out <dir>`（本次按纪律未跑）。
3. 成稿 0.0107 的**分箱定义**（箱数、等频/等宽、边界），否则该数不可复算。

<!-- PROGRESS: 2/6 -->

---

## V3 —— 现行锚的分箱轴使判据失去判别力

**判定：降级**（"色项型缺陷在星等分箱下趋零"我独立复现；但"判据无证据资格"结论过宽，且成稿给的整改路径无效）

### 我的独立复算

脚本 `独立审计/复算件/aud201-v/v3_binning_axis.py` → `v3_binning_axis.json`（日志 `v3_run.log`）。
我自己造两组样本、自己算缺陷函数（黑体族 + 巴尔默跳扰动 → `(BP−RP, −2.5log10⟨λ⟩)` 曲线插值），
判据口径照现行锚：**按轴分 6 个等频箱、取各箱 median、读箱间互差峰峰**，除以缺陷本身峰峰得读出比例。

- 缺陷幅度（在我样本的色跨 BP−RP∈[0.2, 2.6] 上）：官方 G 通带 **0.1999 mag** 峰峰；
  生产相机通带（Baader R × GSENSE4040BSI）**0.0131 mag** 峰峰。
- **A 相关组**（主序式 `G = 12.5 + 3.4·(BP−RP) + N(0,0.6)`，corr = **+0.955**）：
  星等分箱读出 **0.11216 mag = 56.1%**；色轴分箱读出 56.4%。
- **B 解耦组**（把 A 的颜色与星等**逐星打乱配对**——两组的边缘分布逐点相同，corr = **+0.001**）：
  星等分箱读出 **0.00490 mag = 2.45%**；色轴分箱仍是 **56.4%（完全不变）**。
- 相关强度扫描（同一缺陷、同判据，只改 CMD 斜率）：斜率 0.0 → **3.41%**；0.5 → **29.36%**；
  1.5 → 52.43%；3.4 → 56.54%；6.0 → 57.01% ⇒ **读出比例是样本 CMD 结构的单调函数**。
- 加 0.03 mag 逐星噪声：相关组 56.5%、解耦组 2.12%（结论不变）。
- 小样本陷阱（另一条独立发现）：n=200 时解耦组给 **24.55%** —— 打乱后残余 corr=+0.022 + 箱内
  median 抽样噪声就能"读出"四分之一，**与成稿的 27% 同量级** ⇒ 该判据在 n≈200 时的噪声地板
  与它声称测的效应不可分。
- **level 声明不受影响**：解耦组六个星等箱的 median 全为 −7.1568 … −7.1535（箱间 0.0033 mag），
  即缺陷的**常数部分**在任何分箱轴下都被完整读出。

### 与成稿差异

- 成稿的机理我复现：色项型缺陷在星等分箱下，样本解耦即趋零（它称"会趋零"，我实测 **2.45%**）。
- 数字口径差异：成稿给"G 分箱读出 27%、色分箱 84%、3.1×"。我的同构实验给 56%（corr 0.955 的强相关样本）、
  斜率 0.5 时 29%、色分箱 56–65%。**27% 不是判据的属性而是样本的属性**——这恰好是本条要论证的事，
  但成稿把它当成可引用的灵敏度数字登记了；引用时必须连同样本 CMD 结构一起给。
- 结论范围差异（要收窄）：现行锚登记的声明是**绝对 level**
  （`median(m_syn−G) = −0.0037 mag`、`a_absolute_check_by_magbin`）。对"level 是否为 0"这一声明，
  星等分箱与全样本 median **都有完全判别力且与样本 CMD 结构无关**（我的解耦组 level 读数不变）。
  它缺的是对**另一族缺陷（色项）**的灵敏度。所以"判据无证据资格"应写成
  "**该锚不具色项型缺陷的证据资格；其 level 声明仍成立**"——否则会把一条有效的 level 锚误判成无效。
- 整改路径差异（要推翻）：成稿改法 (1) 是"把双轴分箱+注入负例逻辑登记为机器门，**对跟踪 CSV 运行**"。
  按我 V2 的结论，该 CSV 两臂同源（都取 `gaiaxpy.calibrate()` 的绝对浮点谱，不经容器解码），
  在它上面建色轴门将是一条**恒绿门**——它只对"我的积分核 vs GaiaXPy 的积分核"敏感，
  对生产 `F_syn` 的色项缺陷零敏感。正确落点必须是**容器解码谱**上的复测（成稿自己在 AUD201-007 也这么写，
  两条改法互相打架）。

### 定级建议

**P2**（文档与判据设计缺陷，非产品正确性缺陷）：
§2a.6"真实数据"行并列给出色分层表并把"箱间互差 ≤ 阈值"写成正向约束——**但该判据的数据源必须是容器解码谱**；
§16.5 第 7 条的"实证锚定"表述按"level 有效 / 色项无资格"分句订正；
引用 27% 这类灵敏度数字时随附样本 CMD 结构；补一条小样本（n≲300）噪声地板声明。

### 缺什么证据

1. 该锚的实现与结果件都不在跟踪集：`a2_bandpass_and_absolute.json → a_absolute_check_by_magbin`
   在 gitignore 的 `run/` 下（AUD201-004 已立案）⇒ 我只能在**自己造的样本**上验机理，
   无法核对现行锚在真实 26211 星上的 27%/level 读数。
2. 仓库内**没有**任何 ctest/CI 门实现"按星等分箱的 median(m_syn−G)"
   （`git grep -n -I -E "magbin|absolute_check|m_syn" -- eng/` = 0 命中）⇒ 本条是**文档证据口径**问题，
   不是"产品门漏判"问题；定级据此不能上 P1 产品风险。
3. 阈值来源未定：成稿建议"互差 ≤ 0.02 mag"，其依据是 V2 那条已被推翻的 25× 裕度 ⇒ 阈值需重推
   （生产通带色项 0.0131 mag 峰峰 + 通带失配预算）。

<!-- PROGRESS: 3/6 -->

---

## V4 —— 常数"出处"登记为配置键（死键）＋ 权威回链行号大面积指错

**判定：确认**（四条子claim 全部独立复算成立；"造假 vs 漂移"的裁决见下，成稿未做这一层而我做了）

### 我的独立复算

脚本 `独立审计/复算件/aud201-v/v4_provenance_alignment.py` → `v4_provenance_alignment.json`；
`独立审计/复算件/aud201-v/v4b_drift_or_fabrication.py` → `v4b_drift_or_fabrication.json`
（历史逐提交复核，只读 `git show`）。日志 `v4_run.log`、`v4b_run.log`。

1. **三件对齐（登记的键 — 值实际硬编码位置 — 回链行号），6/6 全查**：

   | 键（`eng/packaging/config/defaults.json`） | value | 登记 source_ref | 该行 @HEAD 实际内容 | 值真实陈述行 | 偏差 | 代码里在哪 |
   |---|---|---|---|---|---|---|
   | `photometry.mag_tolerance` | 3.0 | PHOTOMETRY.md:30 | `S`＝`MAD(r)/0.6744…` 符号行 | **34**（另 202/219/248） | **+4** | `pc_api.cpp:145`、`:435` 字面量 `3.0` |
   | `photometry.tukey_c` | 4.685 | :24 | `F_syn` 符号行 | **31**（另 207） | **+7** | `star_matcher.cpp:23` `_TUKEY_C`；`spatial_gain.cpp:40` `kTukeyC`（**重复**） |
   | `photometry.irls_max_iterations` | 50 | :192 | `max_iter=50, tol=1e-6` ✔ | 192 | **0** | `star_matcher.cpp:25` |
   | `photometry.irls_tolerance_dex` | 1e-6 | :192 | ✔ | 192 | **0** | `star_matcher.cpp:27` |
   | `photometry.min_reference_stars` | 3 | :44 | `F_syn = ∫…` 定义式 | **191** | **+147** | `star_matcher.cpp:518` `if (r_consistent.size() < 3)` |
   | `photometry.min_inlier_stars` | 2 | :44 | 同上 | **191** | **+147** | `star_matcher.cpp:616` `if (r_inliers.size() >= 2)` |

   ⇒ **对 2 条 / 错 4 条**，最大偏差 **147 行**（成稿"最多偏上百行"成立；其 −4/−7/−147/−147 四个数我独立算得同值）。
2. **死键 6/6 成立**：`eng/ci/prod_wiring_baseline.json` 自身把六条全列成
   `dead_config_key:photometry.*`；六键在仓库内的"消费者"只有该基线文件与 `test_cfg001_contracts.py`
   （逐键 `git grep -l` 实数，去掉注册表/文档/台账后为空）⇒ 改 `defaults.json` 的任何 `photometry.*` 值
   **不改变任何计算**。
3. **`sourced` 状态字段本身没有说谎**：六个值都在 `docs/science/PHOTOMETRY.md` 里逐字存在
   （上表"值真实陈述行"）⇒ 说谎的是**锚**与"可配"的暗示，不是"有无权威出处"这一项。
   真正互相指认的是同一份文档的两行：`:218` 把锚点写成 `pc_api.cpp:145,435`（代码字面量，正确），
   `:220` 又把"证据出处"写成"配置键 `photometry.mag_tolerance`，`eng/packaging/config/defaults.json`"
   （死键）——出处闭环落在一个没人读的载体上。
4. **"造假 vs 漂移"逐键机器裁决**（把 8 个改动过 defaults.json 的提交逐个取历史版本，
   在同提交的权威文档上测该行是否含该常数陈述）：

   | 键 | 曾成立提交数 / 检查数 | 结论 |
   |---|---|---|
   | `mag_tolerance` | 4/8（最后成立 `5e124efe` 2026-09-23；`7cf94447` 09-24 起失效） | **漂移** |
   | `min_reference_stars` | 4/8（同上） | **漂移** |
   | `min_inlier_stars` | 4/8（同上） | **漂移** |
   | `irls_max_iterations` | 8/8（其间被重锚 `:45 → :192`） | **现在为真** |
   | `irls_tolerance_dex` | 8/8（同上） | **现在为真** |
   | `photometry.tukey_c` | **0/8**（登记面 :24 从未含 4.685；当时真值在 :27/:60） | **登记即错，非漂移** |

   失效原因定位：破坏发生在 `5e124efe → 7cf94447` 之间，该窗口内动过 PHOTOMETRY.md 的提交只有
   `92ede644`（插入 §2a，文件 239 → 418 行）与 `717d2c4b`。同一批重排里**两条 IRLS 键被重锚、三条被漏锚**
   ⇒ 典型的"文档重排 + 登记面部分跟修"。
   `tukey_c` 一条同登记项内三处互斥（source 文本写 `:21`、source_ref 写 `24`、note 写 `:54`，
   真实在 `31/207`）⇒ 不是重排造成的位移，是登记时就没核对过。
   **净结论：整改以"接线＋重锚"为主（3 条漂移），但必须含一条"登记即错"的例外（tukey_c）——
   它与其余四条不同源，不能一起按"漂移"记账。**
5. **"已溯源"为何能长期虚标——两条本应拦住它的门都是空的**（我把它们的判据在自己脚本里重写后逐条喂）：
   - CFG002 `check_09_defaults_anchor_survival`（`eng/tests/config/check_cfg002_registry.py:712-751`）
     的 hint 匹配允许 **token 前 2 字命中**。`photometry.mag_tolerance` 的 hint 含 `mag_tolerance`，
     其前 2 字 `ma` 出现在错行（:30）的 `star_matcher` 里 ⇒ **我的重写版复现出 P2=True**，
     即这条锚"被验证通过"纯属 2 字母巧合（我的 `v4_provenance_alignment.json` 的
     `P2_cfg002_loose_hint`/`P2_matched_token` 字段逐键记录）。paraphrase 豁免清单里也没有它们
     （只有 `noise.max_clip_rounds`、`noise.mask_fwhm_floor_scale`）⇒ 该门对测光四条既没判红也没走豁免，
     是**判据退化**。
   - CFG001 `test_every_source_ref_resolves_and_key_anchors_hold` 的 `KEY_ANCHORS` 只覆盖
     **6 条测光键中的 1 条**，且它自己硬写的期望就是那条已失效的锚
     （`("photometry.mag_tolerance", "docs/science/PHOTOMETRY.md", 30, "3.0 mag")`）
     ⇒ 静态可判：**该断言在 HEAD 必为红**（严格 `grep_line` 谓词我重算 = False）。
   - 没有任何"注册表值 == 代码字面量"的等值核对：只有 `calibration.master_flat_median_range`
     有（`check_master_unit_guard.py`），测光六键无 ⇒ 改 4.685/3.0/50/1e-6 的任何一侧都不报红。

### 与成稿差异

- AUD201-017 的四行偏差表（−4/−7/−147/−147、OK=2/MISMATCH=4）我**独立算得完全一致**；
  AUD201-008 的死键面与硬编码位置也一致（我另补 `star_matcher.cpp:518/616` 两条字面量与
  `spatial_gain.cpp:40` 的 `kTukeyC` **重复常数**，成稿未列重复项）。
- 成稿缺的三件（本次复核新增）：
  ①**逐键"曾真过吗"的历史判定**——成稿停在"虚标"，没区分漂移与登记即错；我给 5/6 漂移、1/6
    （`tukey_c`）0/8 从未成立，这直接决定整改是"重锚"还是"撤回该条的 sourced 状态并重新核对"；
  ②**门为何没拦住**——2 字前缀松匹配导致错行被判"已验证"，是这条"空头账户"能长期挂着的机制原因；
  ③**CFG001 的期望锚自身已失效**且覆盖 1/6 ⇒ 成稿"给机器门补一条判据"的改法其实是**重复登记**：
    判据已在，只是恒真/覆盖率不足，应改为"收紧匹配 + 覆盖全 59 条 + 期望锚从文档生成而非硬写"。

### 定级建议

**P1**（治理/溯源完整性，非产品正确性）。拆两层：
- P1 本体：**溯源验证机制空转**——两条门一条 2 字巧合放行、一条硬写已失效期望且只覆盖 1/6；
  注册表与代码无等值核对；正本 §220 把常数出处指向死键。整改 = 收紧锚判据（整 token 命中 + 全表覆盖 +
  期望从文档导出）、给 6 条补 registry↔code 等值核对（照 `check_master_unit_guard.py` 的形状）、
  正本出处只写活载体或把键真接进配置读取路径（二选一，不并存）。
- P2 伴随：4 条 `source_ref.line` 重锚（+4/+7/+147/+147）、`tukey_c` 一条按"登记即错"单独处理、
  `spatial_gain.cpp:40` 重复常数改为引用单一定义。

### 缺什么证据

1. 两条 Python 门在 HEAD 的真实状态（我按纪律未跑）。要跑的是：
   `python -m pytest eng/tests/config/test_cfg001_contracts.py -k source_ref -v` 与
   `python eng/tests/config/check_cfg002_registry.py`（或其登记的调用入口）。
   我的静态推断：CFG001 该断言红（`"3.0 mag"` 不在 :30）、CFG002 check-09 对 3 条红或靠松匹配放行。
2. 59 条全表的"值在行"比例我只做了粗算（我的 `all_fields_summary`：OK 9 / FAIL 42 / 无 source_ref 6），
   该粗算用裸数值匹配会误判（例如 `3`/`2` 到处命中），**不能当数**；要精确数须按每键语义 token 逐条人工核。
   测光 6 条我用了语义 token，结论可靠。
3. `defaults.json` 是否在别处（运行期）被读进这 6 个常数的通路：我查到 `module_adapters.cpp`、
   `master_unit_guard.h` 等 5 个文件引用 defaults.json，但未逐点确认它们是否覆盖 `photometry.*`
   （现有证据只支持"这 6 条无消费者"）。

<!-- PROGRESS: 4/6 -->

---

## V5 —— 零离散度的假绿通道（`sigma_residual`）

**判定：确认**（我独立构造可达、判据实读放行；并给成稿补三条它没有的事实，其中一条把证据级别从"构造"抬到"库内实跑日志"）

### 我的独立复算

脚本 `独立审计/复算件/aud201-v/v5_sigma_zero_reachability.py` → `v5_sigma_zero_reachability.json`
（日志 `v5_run.log`）：我按 `star_matcher.cpp:165-170`（`medianOf`，奇数取中/偶数取两中平均）、
`:540-552`（`S = MAD/0.6744897501960817`，`MAD==0 ⇒ S=0`）、`:552-585`（IRLS）、
`:592-611`（**`S<=0` 时全部算 inlier**）、`:615-626`（`sigma_residual`）在我自己目录里重写一份等价标量逻辑，
再喂构造样本与随机搜索。**没有编译、没有跑任何测试**。

1. **最小可达零离散度集合**（n=3，两条 `r` 严格相等，第三条任意）：

   | 构造 | n | inliers | σ(dex) | σ(mag) | outlier_rate | 过上界门 |
   |---|---:|---:|---:|---:|---:|---|
   | `{0.30, 0.30, 8.00}`（差 7.7 dex） | 3 | **3** | **0.000000** | 0.00000 | **0.000** | **True** |
   | `{0.30, 0.30, 0.30}` | 3 | 3 | 0.000000 | 0.00000 | 0.000 | True |
   | `{0.30,0.30,0.30,10.2}` | 4 | 4 | 0.000000 | 0.00000 | 0.000 | True |
   | 生产夹具形状 `r=-7.666286`×9 | 9 | 9 | 0.000000 | 0.00000 | 0.000 | True |
   | 随机搜索里"最脏却仍报 0"的一例 `{-5.74007388, 5.935, -5.74007388}` | 3 | 3 | 0.000000 | 0.00000 | 0.000 | True |

   ⇒ **极限量**：一颗偏离 **11.675 dex = 29.2 mag** 的星被计为 inlier，同时产品报
   `σ=0`、`outlier_rate=0`。偏差上界**不受限**——只要多数票严格相等，任意大的错星都被吞掉。
   机理：`MAD(r)=median{0,0,d}=0 ⇒ S=0` ⇒ 走 `:596` 的 `S<=0` 分支**无条件全部入选** ⇒
   `mad_in=0 ⇒ sigma_residual=0`。
2. **成稿引的那条"不可估计"分支不可达**：20 万次随机（n=3…12，含 35% 概率注入重复值）
   **`n_inliers` 最小值 = 2，从未 <2**（`search_for_inliers_lt_2.min_observed_n_inliers = 2`）。
   因 `S>0` 时 Tukey 窗口宽 `c·S = 4.685·S` 而 MAD 的定义保证至少一半星在 `1·S` 内 ⇒
   在 `|r_consistent| ≥ 3` 门后 `|r_inliers| < 2` 结构上不可达。
   ⇒ **`star_matcher.cpp:614` 的注释把 0 归因于"不可估计"，而所有可达的 0 都来自"多数票相等"**；
   即 0 的语义不是"样本不够"，而是"**样本有散度但 MAD 看不见**"——比成稿的说法更糟（成稿接受文档的
   "不可估计"叙事，只批评它没具面值）。
3. **判据实读（不看注释看代码）**：唯一消费该量的科学门是
   `module_adapters.cpp:5747-5748`，谓词 `!isfinite(σ) || σ > P1_PHOT_MAX_SIGMA_DEX`，
   `:5327` `constexpr double P1_PHOT_MAX_SIGMA_DEX = 1.0;` ⇒ **只有上界与有限性**，`σ=0` 判绿。
4. **成稿未列、更直接的一条假绿载体**：`orchestrator.cpp:3030-3031`
   `status = (diag.fit_used > 0) ? "PASS" : "FAIL"`，紧接着 `:3042` 把 `sigma_residual` 原样写进测光报告
   ⇒ **报告级 PASS 只条件于"用了 ≥1 颗星"，与散度完全无关**，`"status":"PASS", "sigma_residual": 0`
   可同时出现，且没有 `estimable` 之类兄弟字段（`eng/contracts/schemas/` 内 `sigma_residual` 0 命中 ⇒ 该标量不受合同约束）。
5. **"是否已有另一条门管这种情况"（任务要我查的降级条件）——查到了两处，但都不构成降级**：
   - `snr_estimator.cpp:148`、`:322`、`:596`：`sigma_residual <= 0.0` ⇒ 返回码 2「退化」，
     旧接口**把整幅 SNR 填 1.0**（一个看起来合法的假 SNR）。
   - `orchestrator.cpp:4493-4494`：`sigma_residual<=0` ⇒ `LOG_WARN` 后**跳过 snr_model 块并 `return true`**
     （`lib/infrastructure/pipeline/orchestrator/memory.md:524` 记同一行为）。
   ⇒ 下游确实"认得"0，但那是**另一个模块的内部退化**，既不使测光帧判红，也不在产品面声明
   "此帧散度不可信"（只落日志）。所以本条按"确认"记账，同时把上面两处登记为**整改可复用的具名退化点**
   （哨兵与 rc 已经存在，缺的是把它们抬到帧级门与产品声明面）。
6. **证据级别抬升（成稿停在"构造"）**：跟踪集里已有一段**生产函数实跑日志**正好是该通道的输出——
   `eng/tests/validation/release02/fix_p1_photometry_apply/qf_oracle_stderr.txt:33-46`：
   `IRLS 初始: location=-7.666286, MAD=0.000000, S=0.000000` → `IRLS: S=0 (所有 r 相同), 跳过迭代` →
   `IRLS inliers: 9 (outliers: 0)` → `sigma_residual = 0.000000` →
   `pc_calibrate_simple_with_gaia (impl) 完成: n_matched=9, scale=4.637520e+07, sigma_residual=0.000000`。
   ⇒ 生产入口在 `σ=0` 下**正常返回并完成标定**（夹具为合成输入，但分支与返回值是生产的）。

### 与成稿差异

- 成稿 AUD201-011 的对象、门谓词、`{2.0,2.0,5.0}` 构造与"0 不可区分"结论，我全部独立复现 ⇒ 确认。
- 我多出的四点：①偏离上界不受限的极限量（29.2 mag 被吞、`outlier_rate=0`）；
  ②`|r_inliers|<2` 分支**不可达** ⇒ "不可估计"叙事本身是错的归因（成稿沿用了它）；
  ③报告级 `status:"PASS"` 与散度无关（比"门只卡上界"更靠前的一道假绿）；
  ④库内已有生产实跑日志坐实 ⇒ 成稿自标的 PARTIAL（"需一条 C++ 构造用例坐实"）可上调，
  我不需要新增用例即可引用该跟踪日志；仍建议补一条 ctest 负例把它钉住。
- 定级分歧：成稿把它与 AUD201-006 同归"判据与缺陷正交空档"。我认为这条比 006 更硬——
  006 是**灵敏度不足**（漏报），这条是**把无散度写进产品**（虚报），且虚报方向是"标定完美"。

### 定级建议

**P1**（假绿声明面，产品可带"零离散度"通过）。整改三件（缺一不可）：
1. **表示**：`sigma_residual` 不可估计/退化时给具面值 —— `null` + `sigma_residual_estimable=false`
   （或 `NaN` + 合同侧显式允许），同步订正 `docs/science/PHOTOMETRY.md:191`/§3/§8（走变更流程，
   因为"否则 `sigma_residual=0`"是**正本自己**规定的）；
2. **门**：`module_adapters.cpp:5747` 加退化侧守卫，形如
   `if (!(σ > σ_floor_eff(n_inliers, n_distinct)) )` 判 `PHOT_FIT_DEGENERATE_SCATTER`，
   其中 `σ_floor_eff` 取"该样本量下不可低于的抽样地板"（`~1.15/√N` 量级，与 `noise.mask_budget_min_sky`
   的推导同族），并显式把 `MAD(r_consistent)==0`（多数票相等）单独判红；
3. **报告**：`orchestrator.cpp:3030` 的 `status` 至少并入 `σ` 可估计条件，不再单靠 `fit_used>0`。
   并补一条 ctest 负例（构造 ≥3 条 `r` 使 `mad_in=0` ⇒ 现判据必须从绿转红），正例 = 正常散度帧仍绿。

### 缺什么证据

1. 真实（非夹具）帧上 `mad_in==0` 的触发概率：`r_i` 需逐位相等才触发，实拍浮点测光下概率极低——
   但重复匹配、float32 落库、小样本窄场是可见路径，需一次真实样本统计（本机无产品树，未查）。
2. 该通道是否已进入**已交付产品**：跟踪集内的 `p1_phot` 只有 CI 夹具（`eng/ci/fixtures/provenance/*.p1_phot.json`），
   其中 `sigma_residual_dex` 出现值为 0.01/0.018/0.019 等非零值 ⇒ 无交付产品带 0 的证据（也不能排除）。
3. 现状是否仍如 HEAD 所示（我未跑测试）：要跑的判定命令 =
   `ctest -R "p1phot_(units|negative|oracle)" --output-on-failure`（负责人下令本阶段测试暂停，故未跑，
   本条结论按源码静态实读给出）。

<!-- PROGRESS: 5/6 -->

---

## V6 —— 一条科学门的归档读数超冻结容差（生产质心 p95）

**判定：降级**（读数、跟踪性、台账措辞我全部复现；但"该读数属一条科学门"这一定性不成立 ⇒ 现状既不能判红也不能判绿）

### 我的独立复算（全部只读跟踪集，未跑任何测试）

命令与文件（逐条自证）：
`git ls-files --error-unmatch artifacts/evidence/audit-2026-01/FIX_LEDGER.csv` → 命中（跟踪）；
`git ls-files --error-unmatch lib/algorithms/photometry/cpp/test/gate4_dr3sp_gaiaxpy/evidence/gate2_result.json` → 命中（跟踪）；
读该 JSON（我自己的 python 解析，输出见 `独立审计/复算件/aud201-v/v6_note.txt`）：

1. **归档读数**：`results.astrocs_production_path` = `n_matched 109`、
   `centroid_median_px = 0.7701382981823749`、`centroid_p95_px = 1.1528986035412487`；
   `gates.centroid_p95_le_0.3px_production = false`；同文件 photutils oracle `0.004719 / 0.039970`；
   同文件 `astrocs_converged_init`（非生产初始化配置）`0.004050 / 0.022788` ⇒ 成稿引的三组数逐位一致。
2. **冻结容差现在是多少、写在哪份正本**：**0.3 px**，三处一致——
   `docs/science/STAR_DETECTION.md:15` 与 `:135`（`|Δc| ≤ 0.3 px @ SNR≥20`，SCI-P1-STAR-001 §1 引用）；
   `docs/algorithms/GATES_AND_TOLERANCES.md:52`（G-P1-CENTROID-SCI，统计量 max，0.3 px）、
   `:54`（G-P1-CENTROID-1，**统计量 p95 ≤ 0.3 px**，`ctest:p1psf_centroid_gate`）；
   门源码 `lib/algorithms/psf/tests/p1psf/p1psf_centroid_gate.cpp:61` `constexpr double kGateP95Px = 0.3;`，
   `:316/:318` 对两条支路各断一次。⇒ 倍数 = **1.1529 / 0.3 = 3.84×**（成稿"数倍/3.8×"成立）。
3. **台账怎么记的**：`FIX_LEDGER.csv:41`（M3b-F-01，P0，F_TEST_GAP）逐字含
   "质心门改按生产路径定义(centroid_p95_le_0.3px_production, 0.3px 未放宽)……**实测生产路径 p95=1.1529px 仍超门 → PSF-001 未闭合**，
   待 M3b-C-01 端到端坐标契约裁决"，处置列 `PARTIAL`→复核 `FIXED`，关联 `c3452d48 / 2026-09-14`。
   ⇒ 成稿"台账当时记未闭合"成立。
4. **我补的时间线（这条决定定级）**：
   `git log --diff-filter=A` 给出——红读数所属提交 `c3452d48` = 2026-09-14；
   而 M3b-C-01 的收口载体 `lib/infrastructure/pipeline/orchestrator/cpp/include/star_coord_contract.h`、
   现行门源 `p1psf_centroid_gate.cpp` 与门表 `docs/algorithms/GATES_AND_TOLERANCES.md`
   **同批新增于 2026-09-16（`b249082f`）**，即**红读数早于收口两天**。
   门表 :54 的标定栏自述"门实测（双支路分离 −0.0000/+0.0001 px、各支路 **p95 ≤0.040 px**）"
   ——即现行登记门**自己的读数方向为绿**（7.5× 余量），但该读数的原始件（`run/ci/ctest/p1psf_*.json`）
   被 `.gitignore` 排除，跟踪集里唯一的原始 gate JSON 就是**改前那份红的**。
5. **关键定性差异**：产生 1.1529 px 的那张门表是 `gate2_psf_oracle.py` 的产物，而
   `git grep -c "gate2" -- eng/ci/checks.json` = **0 命中**（我实跑该计数），且门表
   `docs/algorithms/GATES_AND_TOLERANCES.md:77` 明写该脚本"**未注册进 eng/ci/checks.json** …
   降级为**诊断脚本**：引用面限于诊断"。
   ⇒ 严格说，这条**不是"一条科学门的归档读数超容差"**，而是"**一条已降级诊断脚本的历史读数超容差**"；
   现行两条登记门（`p1psf_prodpath_centroid`、`p1psf_centroid_gate`，均 `add_test` 注册于
   `lib/algorithms/psf/tests/p1psf/CMakeLists.txt:97` 等、`eng/ci/checks.json` 多处）的**当前读数不在跟踪集内**。
6. 成稿附注我也复核：台账把证据路径写成 `lib/photometric_calib/...`，而
   `git ls-files lib/photometric_calib | wc -l` = **0** ⇒ 台账证据指针指向未跟踪旧路径，属实（与 V4 的"锚漂移"同族）。

### 与成稿差异

- 数值、跟踪性、台账措辞、倍数 3.84×、`p1psf_prodpath_centroid` 是活门、台账旧路径失效——成稿全部正确，我独立复现。
- 差异一处（定级相关）：成稿把它登记为"生产质心门的历史红灯 ⇒ 需复测"，但没指出
  **该读数出自已被门表明文降级为非门的诊断脚本**，也没给出 **09-16 收口提交晚于红读数** 这一时间线。
  补上之后结论要改：这不是"某条科学门当前超容差"，而是
  "**跟踪集内唯一的质心原始读数来自已降级脚本且属改前历史值；现行登记门的读数未落档**"。
- 对本分片（测光）的连带影响我按成稿口径复核：1.15 px 质心误差确实会进 `F_instr`→`r_i`→`sigma_residual`；
  但它是历史值且早于收口，所以**不能**据此说当前 `sigma_residual` 预算漏项，只能列为复测观察项。

### 定级建议

**P2 + 待证（现状复测项），不进 P0/P1**：
- 不升 P0/P1 的理由：现行登记门有 0.3 px 的硬判据与配套负例目标（`p1psf_centroid_gate_neg`），
  收口载体在 HEAD 存在，且跟踪集内**没有**任何"现行门超容差"的读数。
- 进 P2 的内容：①门读数落档缺失——`run/ci/ctest/p1psf_*.json` 全部 gitignore，
  导致"门是否绿"在仓库内不可自证（与 V1/V2/V4 的 `run/` 证据丢失同族，建议合案整改：
  门的**汇总读数 JSON**（几百字节）登记进 `artifacts/ci/<sha>/` 而不是只留正文引用）；
  ②台账/正文里的证据路径 `lib/photometric_calib/**` 全部悬空，需批量重锚。

### 缺什么证据

1. 现状读数（本审查按纪律**未跑**）。要跑的正是成稿也指出的那条：
   `ctest --test-dir build -R "p1psf_(prodpath_centroid|centroid_gate|centroid_gate_neg)" --output-on-failure`
   （前置需 `cmake -S . -B build -G Ninja` + `ninja -C build`；负责人已令本阶段构建与测试全停，故留待复测）。
2. 门表 :54 "各支路 p95 ≤0.040 px" 的**原始读数件**（现仅在门表正文与 R-3 正文引用，
   R-3 提到的 `logs/02_oracle_detail.txt` 不在跟踪集：`git ls-files "artifacts/evidence/governance-01/*R-3*"` 只回正文 md 一个文件）。
3. `gate2_result.json` 的产生配置（真实 HST 场还是合成场）与它被降级后是否有等价合成场门覆盖同一条
   `0.3 px` 判据的对应关系。

<!-- PROGRESS: 6/6 -->

---

## 进 P0/P1 的条目（只列"确认"）

| V | 一句话 | 定级 | 两层证据状态 |
|---|---|---|---|
| **V5** | `sigma_residual` 可合法取 0（多数票 `r` 严格相等 ⇒ `S=0` ⇒ 全星入选 ⇒ `MAD=0`），而唯一消费它的门只卡上界（`module_adapters.cpp:5747`，`P1_PHOT_MAX_SIGMA_DEX = 1.0` dex），报告级 `status` 还只看 `fit_used>0`；我构造的极端例：一颗偏 **11.675 dex（29.2 mag）** 的星被吞，产品报 `σ=0`、`outlier_rate=0` 且判绿。库内已有生产实跑日志（跟踪件 `qf_oracle_stderr.txt:33-46`）走到同一分支 | **P1** | 机器复算 ✅（`v5_sigma_zero_reachability.py`）＋ 独立复核 ✅（本件）；负例判定靠源码实读，未跑测试 |
| **V4** | 六条测光常数的"出处"三件不对齐：**4/6 权威回链行号指错（+4/+7/+147/+147）**、6/6 是 CI 自登的死键、值实为代码字面量；正本 `PHOTOMETRY.md:220` 把常数出处指向死键而同文档 `:218` 指向代码字面量；两条本应拦住它的门一条靠 2 字前缀巧合放行、一条只覆盖 1/6 且自身期望已失效 ⇒ "已溯源"是**验证空转** | **P1** | 机器复算 ✅（`v4_provenance_alignment.py` + `v4b_drift_or_fabrication.py`，逐提交历史判定）＋ 独立复核 ✅ |

其余三条（V1 降级→P2、V3 降级→P2、V6 降级→P2+待证）与 V2（推翻）**不进 P0/P1 清单**，落 P2 队列与留痕。

## 推翻或降级的条目（含被推翻理由，留痕不删）

- **V2 —— 推翻（成稿 AUD201-005 的"25× 裕度排除备择假设"论证）**
  推翻理由三条，全部我独立复算支撑：①**通带口径错**：0.2708 mag 是**官方 G 通带**的黑体峰峰（我逐位复现），
  生产 `F_syn` 用本帧相机通带，同一族 SED 下只有 **0.0145 mag**（小 19×）；
  ②**色跨口径错**：真实样本 BP−RP 的 p5–p95 = 0.833…4.984，比黑体网格能达到的 1.62 更红，
  把预测折回样本色跨后是 0.0718 mag ⇒ 裕度 6.7×（换幂律族 14.2×），25× 在任何一致口径下都得不出；
  ③**零灵敏度（致命）**：那 1050 星两臂都取 `gaiaxpy.calibrate()` 的绝对浮点谱，**不经 XPSD 容器解码**，
  而备择假设说的是容器单位约定 ⇒ 该统计对受检假设无判别力，是一台恒绿机器。
  补充：该假设真正可判别量是 **≈7 mag 的绝对平移**（我算得 G 带 −6.86…−7.13、生产带 −7.009…−7.023），
  不是色项；能担事的锚是 `median(m_syn−G)=−0.0037`（裕度 ~1900×），但其证据件不在跟踪集 ⇒ 判"不可复核"。
  λ 因子本身**成立**（`F_syn = hc·N_γ` 我算到相对差 3.0e−16），正本无需订正。
- **V1 —— 降级（成稿 AUD201-001，从"错误写法仍可达/活跃调用"降到 P2 卫生＋文档机理订正）**
  降级理由：①生产路径调用者**实数 0**（`git grep` 口径已给；生产四调用点全是 `compute_f_syn_cached_xpsd`），
  函数不在公共头/导出面 ⇒ 不构成"产品输出可能带该偏差"；"三处活跃调用"里 `test_spectrum_integrator_golden.py:13`
  是文档字符串不是调用，真正的 C++ 调用在**未登记构建**的死测试文件里，成稿既误计也漏计；
  ②"星等计两次 / 注入 +0.4·G_i dex / MAD-σ 0.459 dex"这条**机理**与同一文档"量化参数逐星不同、跨 6 个数量级"
  互斥：我的三档判别（PROPORTIONAL/GLOBAL/ADAPTIVE）显示只有在 byte∝绝对谱且尺度全局恒定时才有
  R²(G)=1、斜率 0.4；在本仓自述的 ADAPTIVE 语义下 R²(G)=0.00035、R²(1/T)=0.985 ⇒ 残差是**色项**；
  该数字只能来自不在跟踪集的 `run/` 证据 ⇒ 判"待证"，不进清单。
  保留的确认部分：该式确非正本口径、绝对量纲无定义，生产不得用；另发现成稿未提的**反向缺陷**——
  `spectrum_integrator.cpp:279` 注释谎称"被 pc_api.cpp OpenMP 并行循环调用"。
- **V3 —— 降级（成稿 AUD201-006，从"判据无证据资格"降到 P2 文档/判据设计，且整改路径需推翻）**
  降级理由：①方向我复现了（自造相关组/解耦组，星等分箱读出从 56.1% 塌到 **2.45%**，色轴分箱不变），
  但 27% 这类"灵敏度"数字是**样本属性**不是判据属性（我 CMD 斜率 0.5→29.4%、3.4→56.5%），
  成稿把它当判据常数登记；②结论过宽：该锚登记的声明是**绝对 level**，而 level 在任何分箱轴、
  任何相关性下都被完整读出（解耦组六箱 median 全为 −7.1568…−7.1535），所以它不是"无证据资格"，
  而是"只对其声明的 level 有资格、对色项型缺陷无资格"；③成稿改法"对跟踪 CSV 建色轴门"按我 V2 的
  结论会建成**恒绿门**（两臂同源、不经容器解码），必须换成容器解码样本；④仓库内没有任何 ctest/CI 门
  实现该分箱锚（`git grep "magbin|absolute_check|m_syn" -- eng/` 0 命中）⇒ 属文档证据口径，不属产品门漏判。
- **V6 —— 降级（成稿 AUD201-014 的定性，从"科学门的归档红灯"降到"已降级诊断脚本的历史读数 + 现状待复测"）**
  降级理由：产生 1.1529 px 的 `gate2_psf_oracle.py` 在 `eng/ci/checks.json` **0 命中**，且门表
  `GATES_AND_TOLERANCES.md:77` 已明文把它**降级为诊断脚本**；现行两条登记门
  （`p1psf_prodpath_centroid`、`p1psf_centroid_gate`）的收口载体晚红读数两天落地
  （`c3452d48` 09-14 → `b249082f` 09-16），门表标定栏自述现门各支路 p95 ≤ 0.040 px。
  ⇒ 现状既不能判红也不能判绿（其原始读数件被 gitignore），只能作 P2 复测项＋证据落档缺陷。
  复测命令（本审查未跑）：
  `ctest --test-dir build -R "p1psf_(prodpath_centroid|centroid_gate|centroid_gate_neg)" --output-on-failure`。
  另需登记：门表正文里"各支路 p95 ≤0.040 px"这类**只活在正文、无入库原始件**的标定读数，
  与本分片 `run/SCI-PHOT-FORMULA-01/**` 证据 6/6 丢失是同一族缺陷（证据落档面），建议合案整改。

---

## 收工工作树核对（与开工基线对比，原文）

开工：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工（同一命令 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

差异说明：**无差异**。收工输出与开工基线逐字相同（恰两行、同为未跟踪项），HEAD 仍为
`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`；本审查未改、未建、未删任何仓库文件，
脚本与输出全部落在 `独立审计/复算件/aud201-v/`（`v1_fsyn_double_count.py`、`v2_lambda_color_term.py`、
`v2b_csv_and_discriminator.py`、`v3_binning_axis.py`、`v4_provenance_alignment.py`、
`v4b_drift_or_fabrication.py`、`v5_sigma_zero_reachability.py` 及各自 JSON/日志、`v6_note.txt`），
未 `import` 仓库内任何 Python，未跑构建/ctest/`run_checks.py`/三命令端到端。





