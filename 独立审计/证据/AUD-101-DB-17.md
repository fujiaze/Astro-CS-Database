# AUD-101 · DB-17 文档审计台账 — 数字层（`实验/*/results/` 结果表与说明 + 两单元报告/判据件，16 份）

批次：`DB-17`。清单：`独立审计/批次清单/DB-17.txt`（16 行，逐行通读全文；无跳读）。判据：标准 01 §2/§3/§4/§5。仓库冻结基线 HEAD=`c8f64e9a`。本节点只读：未构建、未跑 ctest/`run_checks.py`/任何 `eng/**` 检查器、未跑端到端、未 import 仓库内 Python（需要值时读文件文本，含用 `json.load` 读 `results/*.json` 原始件——只读数据，不加载仓库代码）。

派单本批特有关注点的执行方式：

1. **落档支撑**判定 = 该数字能否在同单元 `code/` 脚本 + `results/` 原始件里找到生成处与原始值。三类结论：**有落档**（脚本存在 + 原始 JSON 在跟踪树内且逐值可对）、**无落档**（只有 markdown 文字/表格，生成脚本产物落在 `run/`（gitignore）或根本找不到）、**部分落档**（脚本在、原始件不在）。
2. **同量两值**：逐条列两侧值与 `路径:行`，不概括。
3. **PASS/比率/百分比是实算还是字面量**：一律读生成脚本源码判定（记录 `文件:行`），不执行脚本。
4. **角色归属与处置**：结果表/审稿记录/证据副本按标准 01 §2 末条（过程台账不入正式文档层，留证落 `artifacts/evidence/` 并由正本引用）逐份给迁移或保留建议。
5. **精度与单位**：小数位、量纲、坐标系、截断、格式化产物是否可读、表内能否复算。

## git 基线（开工 / 收工）

开工（原文）：
```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```
（命令：`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`；恰两个未跟踪项，符合前言 §1 预期。HEAD：`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`。）

收工（原文）：见文末「## 收工基线」。

---

## 1. 逐份登记

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 实验/absolute-snr/results/EXP05_TABLES.md | EXP-05 对照表（自动生成，勿手改） | 141 | 研究与佐证（机器生成结果表）；实际目录 `实验/…/results/`，标准 01 §2 角色表无此项（结果表属单元内部数值层），与同单元报告配对，不算角色错放 | EXP-05 绝对/相对表示的 SNR 电平与跨帧偏差、权重定理、机理核对、28 条判据 | 缺（无「上游」条款行；只有行 5–6 口径声明） | 2 份：`docs/EXP-05-ABSOLUTE-SNR.md`、`docs/snr-propagation-design.md` | 否（可再生产物，正本为同单元报告） | `docs/EXP-05-ABSOLUTE-SNR.md` §4.2 表 A 与本件表 A 双承载 12 行同值（见详情块） | 无（首行 h1 内含「自动生成，勿手改」纪律自述） | 多（EXP-05 编号；`<= +0.15%` 类无符号语义；文件名截断） | 出处缺项（非文件悬空）：行 3 只登记 `e1–e5`，表 E 实由 `exp05_e6_mechanism.json` 生成（`make_tables.py:164`）；表 A2/E 的文件名列被 `[:46]/[:44]` 截断 ⇒ 行标识不可解析 | 无条款级冲突 | 保留（单元内可再生结果表）；修生成器三处缺陷 + 补上游指针 |
| 实验/absolute-snr/results/EXP06_TABLES.md | EXP-06 表格（由 results/exp06_*.json 生成） | 331 | 研究与佐证（机器生成结果表）；同上，不算角色错放 | EXP-06 三臂（解析/HST 前向/真实）权重效率损失 E、电平比、拟合诊断、负例、25 门、作用域图谱 | 缺（无「上游」条款行，且无口径/量纲声明） | 2 份：`docs/EXP-06-SNR-PHYS.md:6,580,630`、`docs/EXP-06-SUMMARY.md:3` | 否（可再生） | 与 `docs/EXP-06-SNR-PHYS.md` §7 门表重叠（门清单与 all_pass 两份承载） | 无 | 多（`%%` 渲染成 `-10.0%%`；EXP-03/EXP-04 编号入正文；T1/T2 口径文字被生成器裁掉） | 无（本件除 h1 的通配出处串外不含路径引用；缺陷是**出处声明口径**：`exp06_e6_ablation.json` 在通配覆盖内却不进本件，且缺 e1–e5 与各表的对应关系行） | 无条款级冲突 | 保留；**必须**修生成器（口径被裁、`%%`、缺 seed/量纲）与门表列语义 |
| 实验/absolute-snr/results/REVERSE_VERIFY_CANON.md | reverse_verify 定案结论 · SCI-B（帧级 SNR / 星点通量口径 / SNR 传播设计） | 53 | 研究与佐证（跨文档定案登记台账）；实际目录 `results/`（数值层）却承载定义与结论，与同目录纯结果表角色不同 | 三条定案：帧级 SNR 定义、`F_instr` 口径、SNR 传播设计结论 + 诚实登记 + 复跑 | 有：ASTROCS_DESIGN.md §2.2/§4.4/§5.3/§12.3 + ENGINEERING_SPEC §9（行 3） | 3 份：`REPORT_paper.md:342,424`、`实验/photometric-magnitude/results/REVERSE_VERIFY_CANON.md:17`、`实验/shared/REVERSE_VERIFY_MIGRATION.md:97` | 否（逐字摘自已存在 canon 文档 §0，属二次承载） | `docs/frame-snr-canon.md` §0（行 12–18）、`docs/f-instr-canon.md:14,16`、`docs/snr-propagation-design.md:35–65` | 有（行 3–5 `>` 块：上游 + 来源 + 迁移日期与任务名） | 多（行 4 日期 + `ROOT-CONSOLIDATION`；行 39「未随本次迁移改变状态」、行 30「含 §13 复核订正」、行 34/35「【§13 复核订正】」历史叙事） | 3 条：`run/ROOT-CONSOLIDATION/logs/migration_rerun.md`、`p1_psf.json`、`star_detector.cpp:139-151`（行号错） | 与上位冲突：无条款级；与同主题文档数值口径有 2 处（见同量两值） | 迁移：结论段并入各 canon 文档 §0（正本已在 `docs/`），本件整体作为定案台账落 `artifacts/evidence/absolute-snr/` 由 `README.md` 正本引用（标准 01 §2 末条） |
| 实验/absolute-snr/results/REVIEW.md | SCI-B 独立审稿记录 | 239 | 过程台账（审稿记录），非标准 01 §2 九种角色之一；实际目录 `results/`，应属 `artifacts/evidence/` | 对 b1–b6 的可复现性、判据退化、数值自洽、科学正确性、越权核查与修复清单 | 缺（行 4 只写审稿对象，无最高设计条款） | 3 份：`README.md:277,297,299`、`REPORT_paper.md:5,383,424,485` | 否（对 b1–b6 结果的复核，非现行口径承载者） | 与 `README.md` §9 修正对照表重叠（同一 B1/I1–I6 条目两份承载） | 有（行 3–6 `>` 块：审稿人/对象/时刻/md5 快照声明） | 多（行 5 日期时刻 + CST；行 6–21 13 条 md5；行 4/93/104 等任务编号 SCI-402/FIX-405/EXP-205/DOC-402/CLEAN-401；行 23「13:01 新增…」过程流水；行 192「我至少观察到 README 4 个版本」历史叙事） | 大量（`/tmp/verify*.py`、`/tmp/SCI-B-backup/`、`run/SCI-402/ctest_snr.log` 均非跟踪件，见详情块） | 无条款级冲突；对 b3/README 的结论性推翻在案（B1、I1、I3） | 迁移到 `artifacts/evidence/absolute-snr/REVIEW-SCI-B.md`，由 `README.md` §9 引用；数字复算段（§3）可保留为证据，其余过程流水删除（标准 01 §2 末条、§4） |
| 实验/absolute-snr/results/ctest_evidence.md | SCI-B 仓内测试证据（run/ 不入库，此处为入库副本） | 55 | 过程台账/证据副本（自述「入库副本」）；应属 `artifacts/evidence/` | ctest 两次运行摘要 + 生产 C++ 驱动对拍 gates 字典 | 缺 | 1 份：`README.md:279,293,313,324` | 否（副本） | 与 `README.md` 行 293（同一次运行的 6/6→5/6 叙述）重叠 | 有（标题即元信息：「run/ 不入库，此处为入库副本」+ 行 3 生成时间） | 多（行 3 `2026-09-21T13:37:10+08:00`；行 5「12:4x」；行 48 `FIX-405`；行 10 `run/SCI-402/`；行 49 mtime 时刻） | 有（`/workspace/Astro CS Database/lib/...` 绝对机器路径 4 处；`run/SCI-402/ctest_snr.log` 非跟踪） | 与上位冲突：无；**内部**运行 A 无可核原件（行 8 自承「原始日志已被后续运行覆盖，结论由会话记录支撑」） | 迁移到 `artifacts/evidence/absolute-snr/`（正本引用），并把运行 A 的「无原件」结论撤下——按标准 01 §4 与 AGENTS §9，不能以对照/会话记录充当证据 |

### EXP05_TABLES.md — 详情

**落档支撑：有（三类全）**。生成脚本 `实验/absolute-snr/code/exp05/make_tables.py` 存在且逐表可追：表 A←`exp05_e1_analytic.json`/`e2_hst.json`/`e3_real.json`，表 A2←`e3`，表 B←`e1.cross_frame`/`e2`/`e3`，表 C←`e4_weight.json`，表 E←`e6_mechanism.json`，表 D←`e5_gates.json`。六份原始件均在跟踪树内（`ls 实验/absolute-snr/results/exp05_e*_*.json` = 6 个）。逐值抽查命中：`docs/EXP-05-ABSOLUTE-SNR.md` 与本件表 A 的 `flat_with_struct` 行 1.7897/1.0102/1.7734/−1.01%/−44.12%/43.9x，与 `exp05_e1_analytic.json` 的 `b_frame=1.7897251085512362`、`b_patch_R0=1.0101577598488394`、`c_sigma_R0=1.7733873233153177`、`abs_snr_dev_R0=-0.010051412225691114`、`rel_snr_dev_R0=-0.4411813494937777` 逐项对应（命令：`PYTHONIOENCODING=utf-8 python3 -c "import json;…print(...)"` 只读 JSON）。

**PASS/比率是实算还是字面量：全部实算。** `verdict` 由 `exp05_common.py:449-452` 的 `gate()` 用比较表达式生成（`"PASS" if passed else "FAIL"`），`ALL_GATES_PASS` 取 `exp05_common.py:455-456 all_pass()`，即 `all(g["verdict"]=="PASS")`；表 D 行 111–138 的 28 条与末行 `（28 条）` 均来自 `exp05_e5_gates.json` 的 `meta.n_gates`。判据表达式逐条写在 `code/exp05/e5_gates.py:160-218`（如 G9a `max_d <= 1e-12`、G11 `abs(s["abs_snr_dev_R0"]) < abs(s["rel_snr_dev_R0"])/3.0`），无写死 True。表 A 末三列、比值列同为脚本对 JSON 值的除法（`make_tables.py:65-66`、`81-84`）。

**同量两值（本件与兄弟件/兄弟报告）**

1. 表 A `hst_struct` 比值 `313.5x`（`results/EXP05_TABLES.md:18`）↔ 报告 `docs/EXP-05-ABSOLUTE-SNR.md:374` 同格 `313x`；表 A testdata 四行 `115.8x/772.3x/144.0x/342.5x`（行 20–23）↔ 报告 `116x/772x/144x/342x`（同文行 376–379）。同一比值两处小数位不同、无一处声明「以本件为准」→ 读者无法判定哪个是登记值。
2. 表 A 行 12（`flat_no_struct`，+1.45%/+1.41%/1.0x）在本件是**普通数据行**，报告同格标 `（**负例**）` 并附 `1.0x ⇒ **DEGENERATE**`（`docs/EXP-05-ABSOLUTE-SNR.md:372`）。同一场景的**判据身份**在两份里不一致：结果表未标负例，报告标了；本件行 17（`hst_null`）同样漏标（报告行 375 标 `（**负例**）`）。
3. 表 A `<= +0.15%` 类写法（行 20–23）：`pct()` 对 `max(abs(x))` 取值，符号恒为 `+`，但表 A2 同数据行 33/39 的实值为 `−0.02%/−0.06%`——`<=` 后跟带符号数的组合掩盖了「这是绝对值上界」的语义；无一处说明。

**精度与单位**

- 行 5 口径 `SNR = F_ref/(sigma*sqrt(A_NEA))，报告取 F_ref=1、A_NEA=1`——**无单位**（ADU / e⁻ / px 均未标）；同单元 EXP-06 的原始件里存在 `canon`/`unit` 字段（`exp06_e1_analytic.json → meta.canon = "全部判据为比值口径：F_ref=1 ADU，A_NEA=1 px ⇒ SNR = 1/sigma"`），本件与生成器均未渲染 → 量纲只活在 JSON 里。
- 表 A2「真值自身 SE」列 12 行全为 `0.0110`（源 `truth_se_median=0.01105` ×12，逐值核验：`PYTHONIOENCODING=utf-8 python3 -c "print([round(r['truth_se_median'],5) for r in json.load(...)['frames']])"`）；文档不说明它为何恒等，读者无法区分「常数」与「列坏了」。
- 比值列由**未取整**值算、偏差列取整到 2 位 → 表内不可复算：M1 展示 `16.98/0.15=113.2` vs 印出 `115.8x`；M4 `23.92/0.17=140.7` vs `144.0x`。
- 表 D `实测` 列 3 行为**截断的 JSON 片段**（生成器 `make_tables.py:187` 的 `vs[:80]`）：行 121 止于 `"c_eff`、行 122 止于 `"rel_diff": 4.25`、行 124 止于 `"c1": 1.68407517313714`——门值不可核；同一 `[:80]` 若切到 `|` 会直接破坏表格列。同行内 `%.6f`（+0.000000）与 17 位有效数字 JSON 混用，同列两种精度口径。
- 表 A2/表 E 的「文件」列被截断成两种长度：`r["file"][:46]`（行 33–44）与 `r["file"][:44]`（行 94–105，同一 recipe 同批帧），真名 54 字符（`M42_M1_T2_flying_dutchman-20251212@012404-300S-Red.fts`，源 `exp05_e3_real.json`/`e6_mechanism.json` 的 `file`）→ 表 E 标识被切掉 `0S`，两表同一帧名字不一致，且都不含目录（`data_root` 只在 `e3` 的 `meta.data_root`）→ 结果表自身无法定位数据。
- 表 E 行 94–105 的 `结构倍数` 与表 A2 的 `c_sigma` 是同批帧的两个不同量（1.164 vs 1.1752），本件未说明二者关系（生成器 `make_tables.py:170` 的 recipe 说明只给表 E）。

**悬空引用**：行 3 `原始 JSON：exp05_e{1,2,3,4,5}_*.json` **少登记 e6**——表 E（行 88–105）实由 `exp05_e6_mechanism.json` 生成（`make_tables.py:164`）。该件存在（非文件悬空），但**本件的出处声明不完整**：只看 markdown 会判表 E 无来源。

**写法违规**：h1 与行 3 的 `EXP-05`（标准 01 §4 禁任务编号）；标题括注「自动生成，勿手改」是纪律声明。机械列 `commit_sha=['20251212','20251216','20251224','20251228','20260108']` 为**误报**——它们是 testdata 帧文件名里的观测日期（`exp05_e3_real.json → frames[*].file`），既非 commit 也非正文日期；但按 §4「正文无日期」仍属可议（结果表用文件名作行标识不可避免，建议以帧号替代日期串或明写其为数据标识）。

**下游命令**：`git grep -n -I --fixed-strings EXP05_TABLES`（在冻结基线）→ 命中 `实验/absolute-snr/docs/EXP-05-ABSOLUTE-SNR.md:389,573,584,588,642,661`、`实验/absolute-snr/docs/snr-propagation-design.md:57`（另 `reports/v19r2/*` 两处为审计清点 CSV，不算内容引用）。

**处置建议：保留**（路径不变，标准 01 §1「数值表、实验过程数据下沉到对应下层文档」——本件正是该下层层，且被报告以指针引用）。整治动作：① 补 `上游：ASTROCS_DESIGN.md §2.2/§12.3` 行；② 出处声明补 `e6`；③ 生成器去掉 `[:80]`/`[:44]`/`[:46]` 截断，改为完整相对路径；④ 表 A 补「负例/DEGENERATE」标记，与报告一致；⑤ 比值列与偏差列统一按同一取整值复算（或注明「比值取未取整值」）。

### EXP06_TABLES.md — 详情

**落档支撑：有（e1–e5），但 `exp06_e6_ablation.json` 不出现在本件**。生成器 `code/exp06/make_tables.py:239-275` 只加载 `e1_analytic`/`e2_hst`/`e3_real`/`e4_gates`/`e5_scope`；`results/exp06_e6_ablation.json` 存在但本件无对应表（报告 `docs/EXP-06-SUMMARY.md:3` 却称「25 门 + 12 消融门」——消融门的落档件不进本表）。标题行 1「由 `results/exp06_*.json` 生成」用通配掩盖了这一点。

**生成器 fail-open**：`_load()`（`make_tables.py:39-42`）在原始件缺失时返回 `None`，`main()` 的 `if e1:`/`if e2:`…（行 240/261/265/268/272）静默跳过整节 → 少一份 JSON 就少一节表，表格照样「成功生成」，无任何红。结果表自身无完整性自检。

**PASS/比率是实算还是字面量：全部实算**。`ALL_PASS`（行 247）取 `exp06_e4_gates.json` 的 `all_pass`，由 `exp06_common.py:906-907 all_pass()` 聚合；逐门 `verdict` 由 `exp06_common.py:900-903 gate()` 的 `"PASS" if passed else "FAIL"` 产生，判据表达式在 `code/exp06/e4_gates.py:60-68`（G1a `bool(d_phys <= 1e-2)`、G1b `bool(d_phys <= d_int/5.0)`）、`:95-100`、`:136-140` 等；行内 `实测` 数字是脚本用 `%.3g/%.5f` 把同一批浮点插进 detail 串（`e4_gates.py:61,66-67,97,118,139`），**不是**手写字面量。已用原始件核对：行 251 的 `0.00322` 与 `exp06_e4_gates.json:18` detail 串逐字同；行 268 的 `3347303.22081`/`59348386.3` 与同件 `:253`、`"E_weight_caliberP": 3347303.220811277` 对应。

**表 A-T1/A-T2、Q1-T1/Q1-T2 的 4/8 行逐列相同——不是复制粘贴错，是数据本身相同**（已复算，非推断）：`PYTHONIOENCODING=utf-8 python3 -c` 读 `exp06_e1_analytic.json` 得 `A0_flat`/`A2_gradient`/`A3_smooth_nebula` 的 `vs_T1_var_bg.eff_loss` 与 `vs_T2_var_local.eff_loss` 对 13 个方法**逐位相等**；`exp06_e5_scope.json` 的 `gradient_weak` 同理。原因（源 JSON `meta` 自述）：`truth_T1 = var_bg 空背景口径`、`truth_T2 = var_local 逐像素总方差口径`，无源场景下两口径重合。**但文档未写这一句**，读者只能怀疑表格被复制。

**精度与单位（本件最重的一栏）**

1. **口径被生成器裁掉**：`make_tables.py:65` 用 `tname.split(" ")[0]` 做标题 → `"### A-T1 权重效率损失 E…"`，源串里的 `T1 = var_bg（空背景口径，ACSD 冻结口径）` / `T2 = var_local（逐像素总方差口径）` 全丢。同一文档下方作用域表（行 279/306/315/325）由另一条 `out.append("### Q1 …（%s）" % tn)`（行 185/203/216）**保留**了 `T1 = var_bg（空背景口径）` 却**丢了「ACSD 冻结口径」**。→ 同一文档内 T1/T2 一处有定义一处无定义，两处定义文字还不一致。
2. **量纲缺失**：全件无 `F_ref=1 ADU、A_NEA=1 px ⇒ SNR=1/sigma` 声明（该串在 `exp06_e1_analytic.json → meta.canon`、`meta.unit="EXP-06-SNR-PHYS"` 里，生成器不渲染）；`E`、`电平比`、`r2_var` 均未标是否尺度不变/无量纲（仅表标题括号里写「E=0 最优，尺度不变」）。
3. **`%%` 渲染错**：行 87–94「偏差 vs 真值 1.3」列全部印成 `-10.0%%`、`-41.8%%`、`3.3%%`…——源为 `make_tables.py:103` 的 `fmt(...) + "%%"`（把字面 `%%` 当单位拼进去）。单位列被写坏。
4. **表内不可复算的取整**：行 259 G5 `实测 E_unsep=0.05169 vs E_phys=0.00006（比 938.78）`——`0.05169/0.00006 = 861.5 ≠ 938.78`；生成器 `e4_gates.py:139` 用 `%.5f` 印 E、比值用全精度浮点，故表内自相矛盾（同一形状在行 260 G6a `0.05544 vs 0.00006（比 1006.9）`、行 261、行 266、行 267 重复出现）。
5. **列语义与表头不符**：行 249 表头 `| 判定 | 门 | 预期 | 实测 |`，但生成器 `make_tables.py:173-174` 把 `g["detail"]`（一句判据说明 + 嵌在文字里的数字）放进 `实测` 列；行 263/265 等门**没有任何独立数值**（`实测` 列只有文字），行 271 的 `实测` 列里装的是判据正文（`phys_auto…不得劣于…超过 10%（相对判据；地板场景由 G13d 的绝对容差门覆盖）`）。→ 名为数值表，实际混合了散文，机器/人都无法从该列取值。
6. **seed 缺登记**：A-负例（行 100–106）逐行给 seed（20261927/20262927/20263927/20264927，源 `e1_analytic.py:145`），但门表（行 251–252）的 0.00322/0.0393 来自 base seed `20260927`（`exp06_common.py:61`，`e4_gates.py:51` 的 `_scene(sky_e_per_s=0.5)`）且**不写 seed** → 见「同量两值」。
7. 行 5 「规模：48 帧（8 场景 x 6 seed），[1024, 1024]，Delta=64 px」为实算（`make_tables.py:242-244` 读 JSON），与 `meta.n_seed=6`、8 场景一致 ✓。

**同量两值（本件内部与跨件）**

1. 「平坦真值场 phys 离散度」：`EXP06_TABLES.md:251`（G1a `0.00322`）、`:252`（G1b phys `0.00322` / interp `0.0393`）↔ 同件 `:100–103`（N1 逐 seed `0.0031 / 0.0028 / 0.0031 / 0.0029`，interp `0.0451 / 0.0499 / 0.0419 / 0.0355`）。两侧 phys 值相差 0.00012（3.9%），interp 的 0.0393 **落在 N1 四值区间之外**；差异只由 seed 不同造成，但门表侧不写 seed、负例侧不写「另有 base-seed 版本」→ 一处文档两个数，无从判谁登记。
2. 「M42 帧级 σ」：本件 `:241–243`（M42_M1 `13.792`、M42_M2 `14.782`、M42_M4 `13.318`）↔ `EXP05_TABLES.md:33/35/40`（同三块天区的同名帧 `sigma_frame` `16.1558`（M1 20251212@012404）、`59.4153`（M2 20251212@020002）、`17.5785`（M4 20251224@045919））。M2 相差 **4.0×**。两侧源件确有说明差异（`exp06_e3_real.json → meta.crop=1024`、每面板只取首帧；EXP-05 用中心 `2048²`、12 帧，见 `docs/EXP-05-ABSOLUTE-SNR.md:343` 臂 C 行），**但两份结果表都不写裁剪口径**，本件行 239 表头只写「帧级标量 vs 区域 sigma（EXP-03 机理复现）」→ 从文档层无法调和，只能回到 JSON。
3. 同一帧标识两套：本件行 233–235 只有 `M42_M1`/`M42_M2`/`M42_M4` 面板名（生成器 `make_tables.py:151` 取 `f["tag"]`，帧名在该 JSON 里为 `None`），EXP-05 表 A2 用 46 字符截断文件名 → 两表无法按帧 join。

**悬空引用**：本件内部路径均可解析（`results/exp06_*.json` 五件在跟踪树内）。缺口在**出处声明**：本件无「生成脚本 + 各表对应件」行（EXP05 有、EXP02/03/04 亦有同类行），只有 h1 的通配串；`exp06_e6_ablation.json` 未被本件消费却在通配覆盖内 → 通配口径与事实不符。

**写法违规**：行 9/22/35 表头 `interp_spline（EXP-04 推荐算子）`、行 237 节标题 `（EXP-03 机理复现）`、行 1 `EXP-06`——任务编号进正文（标准 01 §4）。机械列 `commit_sha` 报 `['20261927','20262927','20263927','20264927','3347303','59348386']` 全为**误报**：前四个是 A-负例的 seed（`e1_analytic.py:145` `SEED+1000*(i+1)`），后两个是 G12a 的 `E_wP` 与「比」（`exp06_e4_gates.json:253,256`）。机械列 `task_ids` 报 `['EXP-04','EXP-06']` 为**漏报后的正确捕获**（正文确有）。

**下游命令**：`git grep -n -I --fixed-strings EXP06_TABLES` → `实验/absolute-snr/docs/EXP-06-SNR-PHYS.md:6,580,630`、`实验/absolute-snr/docs/EXP-06-SUMMARY.md:3`。

**处置建议：保留**（同 EXP05，本件是 EXP-06 报告的数值下层层）。整治动作：① 生成器停止裁口径（`make_tables.py:65` 的 `split(" ")[0]`）、修 `%%`；② 门表增加独立 `value` 列（`gate()` 已存 `value` 字段，`make_tables.py:174` 只渲染 `detail`）；③ 每节标源件与 seed、写明 T1/T2 在无源场景重合；④ 帧标识与裁剪口径随行写出；⑤ `_load` 缺件改判红。

### REVERSE_VERIFY_CANON.md（SCI-B） — 详情

**角色与落位**：本件不在数值层——它承载**定义式与定案结论**（§1 帧级 SNR 定义逐字、§2 口径表、§3 六条设计结论），却躺在 `results/`。标准 01 §2 的角色表里，这类内容属科学定义/算法推导（`docs/science/`、`docs/algorithms/`），而它自己声明的「来源」三份文档已在 `实验/absolute-snr/docs/`。→ 现目录与角色不一致（角色错放），且与正本**二次承载**同一口径。

**落档支撑：分节不同（逐节核）**

- §1（帧级 SNR）：与 `docs/frame-snr-canon.md` §0（行 12–18）**逐字相同**（已逐行比对）；该节无数值。
- §2（`F_instr` 口径）：**无落档数字**。生成脚本在（`code/reverse_verify/f_instr/exp0..exp5.py`），但其产物写 `run/reverse_verify/f_instr/`（`exp1_recovery.py:18` `OUT = os.path.join(ROOT,"run/reverse_verify/f_instr")`、`f_instr_lib.py:5`「中间产物落 run/…不入库」）→ 冻结基线内**无原始件**。数字 `−0.72 等 / 0.516× / 0.493/0.672/0.808/0.925/0.985 / 1.353 mag / ≤0.004 mag / 0.998` 在跟踪树里**只以 markdown 形式存在**（命令：`git grep -n -I --fixed-strings 0.516 -- 实验/` → 命中 `docs/f-instr-canon.md:14,16,143,166,329,378`、`docs/surveys/f-instr-survey.md:483-484`、本件 `:21,23`，**无一落在 JSON**）⇒ 判为「无落档支撑的数字」。
- §3（SNR 传播六条）：**有落档**。`code/reverse_verify/snr_design/exp{1,2,3,4,5}_*.json` 与 `audit/audit_{exp1245,exp3_physical,mosaic_shape,sim_validation,sp0}.json` 均在跟踪树内，且 §3.5/§3.6 的订正数字与 audit 件逐值对得上：`1.3127 / 31.27% / 76.8% / 23.8%` ↔ `audit_exp1245.json:544-546`；`1.2857`（§3.3 朴素写法高估）↔ `audit_exp1245.json:33-34`、`exp1_weight_penalty.json:178`；`4.42%（70×）`↔ `audit_exp3_physical.json:84`；`0.175%`↔ 同件 `:298`；`29.4%`↔ 同件 `:688`；`20.5% / 608.6% / 2200%`↔ `audit_exp1245.json:616-617`。§3.4 的 `ℓ ≈ 48 px`、§3.6 的 `20.2%/609%` 未逐值回核到本件外（`snr-propagation-design.md:1435` 亦载）。

**同量两值（本件与别批正本、与本单元别件）**

1. 假增益 `0.516`（本件 `:21`）↔ `0.52`（`code/reverse_verify/f_instr/README.md:15`）——同一测量两种精度，无一方指向另一方。
2. 孔径无关性 `≤ 0.004 mag`（本件 `:23`，同 `docs/f-instr-canon.md:16`）↔ `≤ 0.005 mag`（`code/reverse_verify/f_instr/README.md:17`）——**两个不同的门限值**，非取整差。
3. 「修好后良态 σ 相对误差（含 drizzle）」`≈ 20.5%`（本件 `:35`，同 `docs/snr-propagation-design.md:59`）↔ `修好良态 20.2%（含 drizzle 28.6%）`（`docs/snr-propagation-design.md:1435`）——同一订正量在正本内部即有两个值（20.5 vs 28.6），本件取其一且未标另一侧。
4. 生产链 σ 相对误差：本件 `:35` 写「≈ +31%（不是 131%）」，`docs/snr-propagation-design.md:1435` 写「当前生产 31.3%（含 drizzle 37.2%）」——含 drizzle 分支本件未登。

**悬空引用（逐条 + 确认命令）**

- `:20` 与 `:25` 引 `star_detector.cpp:139-151` 的 `m00`：错。真实位置 `lib/algorithms/star_detection/wrapper_phase1/star_detector.cpp:214-230`（`grep -n "m00" …` → 214/222/225/226…），139–151 处是动态范围/饱和计算（`sed -n '137,153p'`）⇒ **行号引用不实**；同一错引同时存在于 `code/reverse_verify/f_instr/README.md:14`、`docs/f-instr-canon.md`，说明是复制传播。（本单元审稿 M9 已就同类漂移给出「引用带 commit 或改用符号名」的建议：`results/REVIEW.md:189`。）
- `:25` 引 `p1_psf.json`：`git ls-files | grep -i p1_psf` → 只有 `eng/tests/unit/v6_p1_psfw/*`，无 `p1_psf.json` ⇒ **不存在**。
- `:24` 引 `docs/science/PHOTOMETRY.md:95`「星点通量来自 PSF 拟合域」：该句实在 **`docs/science/PHOTOMETRY.md:262`**（`grep -n "星点通量来自 PSF 拟合域"`）；行 95 是 Gaia 通带/`Q(λ)` 正文 ⇒ 引文内容对、**行号不实**。
- `:4` 引 `reverse_verify/docs/{frame-snr-canon,f-instr-canon,snr-propagation-design}.md`：迁移后旧根目录不存在（`ls -d reverse_verify` → `No such file or directory`）⇒ 该路径**悬空**（现位置在同件行 4 后半与 `实验/shared/REVERSE_VERIFY_MIGRATION.md:97` 已给出，属「新旧路径并写」）。
- `:53` 引 `run/ROOT-CONSOLIDATION/logs/migration_rerun.md`：`git ls-tree -r --name-only c8f64e9a | grep -i ROOT-CONSOLIDATION` → 0 命中（`run/` 仅跟踪 `.gitkeep`）⇒ **非跟踪产物指针**，本件所登记的「实测」不可核。
- 正确可达的：`docs/science/PSF.md`、`dpsf_psf.cpp:428`（该行正是 Moffat4 解析积分注释 + `flux = 2πA·sx·sy/3`）、`snr_science.cpp:118`（落在 `snr_moffat4_profile_f64` 内）。

**写法违规**：行 3–5 抬头块（上游 + 来源 + `（2026-09-21 ROOT-CONSOLIDATION 迁入…）`）＝元信息块 + 日期 + 任务名（标准 01 §4）；行 4 与 §4 行 39「未随本次迁移改变状态」、行 34/35「【§13 复核订正】」为历史叙事；行 5「本文件只登记**现行结论**」是机制自述。机械列 `dates=[5]` 命中正确。

**与上位冲突**：无条款级；但 §2 的「`F_instr` 改用 PSF 总通量」与「本仓**无哈勃数据**」（行 26）同 `docs/` 里 HST_M16 数据策略需对表（属别批 `docs/science` 范围，本批只登记不裁）。

**处置建议：迁移**。结论性内容（§1–§3）与 `docs/{frame-snr-canon,f-instr-canon,snr-propagation-design}.md` 的 §0 重复承载，按标准 01 §2「研究与佐证类以引用方式服务正本，不重复承载正本口径」应删并留指针；本件若作为一次整合的定案台账留证，按同条末项落 `artifacts/evidence/absolute-snr/REVERSE_VERIFY_CANON-SCI-B.md` 并由 `实验/absolute-snr/README.md` 与三份 canon 文档引用。迁移前置：把 §2 的六个数字补出落档原始件（或在文中明写「原始件在 run/，不入库」并取消数字复述），并修四处不实/悬空引用。

### REVIEW.md（SCI-B） — 详情

**角色**：独立审稿记录 = 过程台账（AGENTS §5「结论经独立子代理多轮审稿」的产物），标准 01 §2 九角色无一对应「审稿记录」；同条末项明确「历史档案、过程台账、已撤销设计不放在正式文档层；需要留证的台账落 `artifacts/evidence/` 并由正本引用」。本件却在 `实验/absolute-snr/results/`（数值层目录）。

**数字层评价（本件是这批里唯一逐条给复算过程的结果件）**：§2 表 13 行、§3 每条 B/I/M 都带「位置 `文件:行` + 复现命令 + 期望 vs 实际」，且其复算值为独立脚本产出（`/tmp/verify*.py`，不 import 单元代码）。抽检其复算是否自洽：`:85` 的 `0.9130772/43.319582 = 0.0210777` 复乘 = 0.02107764… ✓；`:89` 的 `0.045/(1.44/ln10/√1024) = 2.302585092994046`，与所写「= ln10」✓；`:90` 的 `1+0.75×0.51909103 = 1.389318271478706` ✓；`:168` 的「JSON pred 11494.99、正确 3831.66、实测 3953.29」三者比值自洽（11494.99/3=3831.66）✓。**未发现其复算式自身的算术错**，故本件的数字可作证据使用；但它的**证据文件本身不在跟踪树**（`/tmp/verify*.py`、`/tmp/SCI-B-backup/`），复算不可第三方复核 → 作为「证据」的强度低于 `results/*.json`。

**同量两值**：本件 `:114`「dense RMSE 报 0.0258 → 修正 0.0791（乐观 3.1×）」与 `:184` M4「ℓ=16 的真实值是 dense 0.0258/sparse 0.2304/frame 0.2307」互指同一组数；`:134` 记 `EPS_REF=1.44/√512=0.063640 dex`（代码）↔ `README:63/173` 的 `0.506/√512=0.0224 dex`（文档）——两侧值与位点已列，本件判 2.846×；`:175` 记 `README:269` 称 6/6 而日志 5/6，与 `results/ctest_evidence.md:5,10` 两份摘要相互印证 ✓。

**悬空引用（登记为「证据不可核」而非文件不存在）**：`:33` `/tmp/SCI-B-backup/results`、`:45-55` 与 `:107-115`、`:135`、`:155`、`:161`、`:167` 的 `/tmp/verify*.py`（本节点确认：`ls /tmp/verify*.py` 在本机无对应物，且 `/tmp` 非仓库内容）；`:174`/`:238` `run/SCI-402/ctest_snr.log`（非跟踪）；`:58-63` 的 `lib/...`、`docs/science/NOISE_MODEL.md`、`eng/contracts/...` **存在** ✓；`:204` 引「`07_noise_snr.md`」为裸文件名（无路径），按 §3 扫描口径无法解析——该文件实为 `docs/plugins/algorithms_phase1/07_noise_snr.md`（别批），此处书写不规范。

**写法违规**：`:5` `2026-09-21T05:20Z（CST 13:20）`、`:23` 时间流水（13:01/13:04/13:08/13:09/13:12/13:19）、`:192` M12（README 4 个版本 + 两次 md5 `88d3a1e2…`/`fa1d5f7a…`）、`:6-21` 13 行 md5 表、`:4/93/104/192` 任务编号（SCI-402、FIX-405、DOC-402、CLEAN-401、EXP-205）——标准 01 §4 逐条禁。机械列 `commit_sha` 报的 10 项里 `88d3a1e2` 是真 md5 前缀、其余 `0107053/0210777/0304423/…` 全为复算比值（如 `:85` 的 0.0210777、`:88` 的 1.5225715）⇒ **机械列误报为 commit**。`task_ids=['FIX-405']` 漏报了 SCI-402 / EXP-205 / DOC-402 / CLEAN-401（工具只匹配了带连字符的形态）⇒ 机械列**漏报**。

**下游命令**：`git grep -n -I --fixed-strings "results/REVIEW.md"` → `实验/absolute-snr/README.md:277,297,299`、`实验/absolute-snr/REPORT_paper.md:5,383,424,485`（另 additive 单元两处指向其自身 REVIEW，非本件）。

**处置建议：迁移** → `artifacts/evidence/absolute-snr/REVIEW-SCI-B.md`，由 `实验/absolute-snr/README.md` §9 与 `REPORT_paper.md` 引用（标准 01 §2 末条）。迁移时须一并处理：① §3 的修复项已全部闭环（README 行 313 起有整改对照表），审稿记录的「现行性」为 0，只作证据；② 复算脚本 `/tmp/verify*.py` 如要留证应随件入库（否则「独立复算」不可复核）；③ 删 md5/时刻/流水，保留结论与判据引用。

### ctest_evidence.md — 详情

**角色与落位**：标题即自述「run/ 不入库，此处为入库副本」——**证据副本**，标准 01 §2 末条规定其目标目录为 `artifacts/evidence/`，现躺在 `实验/…/results/`（数值表目录）＝角色错放。

**数字层**：`5/6`、`6/6`、`3.63 s`、`83% tests passed`、`:54` 的 15 项 `GATES` 字典都是**日志原文粘贴**（非本仓生成器产物，也无对应 JSON 落档）；生成方式无脚本可追（`实验/absolute-snr/code/` 下无产出本件的脚本，`grep -rn ctest_evidence 实验/absolute-snr/code/` 0 命中）⇒ 本件是**手工抄录**的证据副本，属「无落档支撑」的一类：其中运行 A（`:5-8`）连原始摘要都没有——`:8` 自承「该次原始日志已被后续运行覆盖（run/ 不入库），结论由会话记录与本文件运行 B 的对照支撑」。以「会话记录」支撑一个 6/6 结论，正是 AGENTS §9 所禁的「以看起来通过代替证据」。

**与上位/同层冲突**：`:7` 的运行 A「6/6 passed（含 p1noise_numpy_oracle）」与其自身 `:23` 运行 B「p1noise_numpy_oracle ***Failed」并存，而 `README.md:293` 把两次都登记为现行事实；审稿件 `REVIEW.md:175`（I6）已判「附录 B 不应保留已被自己推翻的 PASS 句」——本件仍保留「6/6 通过 + 结论由会话记录支撑」的表述 ⇒ 同一事实在三处（本件、README、REVIEW）以三种强度出现。

**悬空/不可核引用**：`:10` `run/SCI-402/ctest_snr.log`（非跟踪）；`:27/31/32/48` 的 `/workspace/Astro CS Database/lib/...` 为**机器绝对路径**（不在被审树内；AGENTS §3 明令「在仓库内工作，不写死服务器绝对路径」）。确认命令：`git ls-files | grep "^run/"` → 仅 `run/.gitkeep`。

**写法违规**：`:3` 时间戳 `2026-09-21T13:37:10+08:00`（机械列 `dates=[3,49]` 命中正确）、`:5` 「运行 A（12:4x，SCI-B 改动前）」、`:10` 「（复跑，当前工作树…）」、`:48` 「并发 FIX-405」、`:49` mtime `2026-09-21 13:14:04`、`:50` 「SCI-B 文件域不含 lib/」——日期、时刻、任务编号、历史叙事四项齐犯。

**下游命令**：`git grep -n -I --fixed-strings ctest_evidence` → 仅 `实验/absolute-snr/README.md:279,293,313,324`。

**处置建议：迁移** → `artifacts/evidence/absolute-snr/ctest-run-summary.md`，由 `README.md` §附录 B 引用；同时**撤下运行 A 的 6/6 结论**（无原件即不得以数字呈现），改为登记「该次运行未保留原件，不作证据」，或重跑并落完整日志（标准 01 §2 末条 + §4 + AGENTS §9）。生产驱动对拍字典（`:52-55`）应改由脚本产出 JSON 落 `results/`，markdown 只留指针。

<!-- PROGRESS: 5/16 -->

（接表 1，续 8 份）

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 实验/additive-sky-seamless/README.md | SCI-C 加性天光与无接缝叠加 —— 实验单元报告 | 458 | 研究与佐证（实验单元报告）；实际在单元根，与标准 01 §2 的 `docs/research/` 不一致但单元自包含（AGENTS §5）要求留此，不算错放 | 加性天光 + 无接缝叠加猜想的成立域与失效边界（C1–C7） | 有（部分）：行 4「标准：最高设计 §12.3」，无条款标题 | 3 处：`docs/science/PHASE2_UPM.md:482`、`工程控制/RELEASE-05/tasks/SCI-502.md:7`、本单元 `REPORT_paper.md`（多处指 §11/§4.x） | 与 `REPORT_paper.md` 并列为同主题两份（须定一份，见详情） | `REPORT_paper.md` 全文为其复核件（附录 A/C 与其数字逐条对表）；`data/README.md` §4 与本文 §2.2 的生产入口清单重叠且不一致 | 有（行 3–5 `>` 块：控制包 `/tmp` 路径 + 文件域 + 零生产码改动声明） | 多（FIX-1/2/3、SCI-402/SCI-403、行 388 日期时刻、§11 整改流水） | 3 处（`results/figs/*.png` 被 `.gitignore:60 *.png` 全局排除；`run/CLEAN-402/...`；`/tmp/astrocs_pkg/...`） | 3 处数字与自身 JSON 冲突（详情块「同量两值」）；与 `docs/science/PHASE2_UPM.md` 的 9 条口径冲突已由 REPORT 附录 C 登记 | 保留为单元报告正本；3 处不可回溯数字按 JSON 改正、图证据撤下或改登记为"本地产物不入库"、§11 整改流水迁 evidence |
| 实验/additive-sky-seamless/REPORT_paper.md | 加性天光与无接缝叠加：猜想在什么条件下成立、何时失效 | 551 | 研究与佐证（论文式精读复核报告）；同上不算错放 | 同上（对 README/JSON/审稿记录的只读复核） | 有：行 8 权威依据（ACCEPTANCE_SPEC §2.3 + 最高设计 §2.3/§12.3 + PHASE2_UPM §7a/§16 + 11_upm §4.6/§9） | 2 处外部：`docs/contracts/DUAL_LINE_CONTRACT.md:19`（把 `实验/**/REPORT_paper.md` 登记为"报告面"、只增不改）、`artifacts/evidence/release-05/AUDIT_PACKAGE.md:15`（SCI-C 行的证据指针）；单元内 `README.md` 与之互为对照 | 与 README 并列（本件数字口径更严） | README §4/§5 全部结果表与本件 §4 表重叠（同一批门值两份承载，本件多附字段映射） | 有（行 3–12 表格式抬头块：报告对象/权威依据/证据口径/判据分级/本轮增量/复现） | 多（commit `d77fd11f` 等 5 个 sha256 短 sha + 大量日期时刻 + FIX-1/2/3、SCI-502/505、"2026-09-20 负责人定案"） | 6 处 `results/figs/*.png`（结构性不存在）+ `run/SCI-403/logs/` | **有**：§4.8 与摘要第 8 条称 FIX-3「κ 自适应 + `roughness_penalty` 已落地」，冻结基线代码已**退休**该键（`sky_plane.h:221`、`sky_plane.cpp:462/1726/1861`、`module_adapters.cpp:9698/9978`） | 与 README **合并**为一份（标准 01 §2 同主题一份正本）；§4.8 的代码事实断言按当前基线重写，附录 C 的 9 条冲突走上位变更流程后从报告删除 |
| 实验/additive-sky-seamless/code/reverse_verify/m16_scene/README.md | experiments/m16_scene —— M16 场景模板的判据实验 | 58 | 过程台账（判据脚本运行记录），标准 01 §2 无该角色，应属 `artifacts/evidence/`；现躺在 `code/` 子目录内 | M16 场景 A6/方差闭合/真实底传输三判据的 stack_n=32 结果 | 缺（无上游条款；行 3 只有任务号 + 日期） | 0 个（见证据块） | 否 | 与 `实验/shared/data/synthetic/before_stackn32/CHANGES.md` 同一批前后对照重叠（后者为跟踪件，本件指向 run/ 版） | 有（行 3–7 `>` 块：任务号 + 日期 + 旧值/新值对照声明） | 多（STACKN32-001、M16-SCENE-FIX-001、P9、"更早修复前"、"修复前全红"、"已订正"、日期 2026-09-20） | 5 条（`run/RELEASE-02/paper/data/STACKN32/STACKN32-001.md`、`…/M16FIX/M16-SCENE-FIX-001.md`、`data/STACKN32/stackn_sensitivity_check.py`、`reports/RELEASE-02/m16-scene.md`；另 `star_detector.cpp:151` 行号不实） | 无条款级 | 迁移到 `artifacts/evidence/additive-sky-seamless/m16-scene/`（标准 01 §2 末条）；本批三判据数字**全部无落档原始件**（脚本产物写 `run/`），须补落档或删数字 |
| 实验/additive-sky-seamless/code/reverse_verify/smooth_lambda/CRITERIA.md | A5（SMOOTH-LAMBDA）判据预登记 | 36 | 标准与检查（判据预登记）；按 §2 应属 `docs/standards/`，但其判据只服务一个探针分片 → 属分片内部件，留 `code/` 可接受 | λs 扫描的 8 条判据（C1–C8）与推荐区间规则 | 缺（行 3 指向不存在的 `reverse_verify/README.md §3.3`） | 1 处：`docs/smooth-lambda.md:7` | 否（判据无落档结果，见详情） | 与 `docs/smooth-lambda.md` §0/§2 的判据叙述重叠（后者是空骨架） | 无（标题即判据登记，非元信息块） | 轻（行 13 引 `FIX-P2a`、行 33「负责人观察」；机械列全空，判定与机械列一致） | 2 条：`reverse_verify/README.md §3.3`（该路径不存在，确认命令见详情）、行 13「Q2 §6.2 口径」（无路径、无法解析） | 无 | 保留（判据先行的登记载体，AGENTS §5 要求），但必须与 λs 分片同一处置：结果从未产出 ⇒ 与 `docs/smooth-lambda.md` 一起定去留 |
| 实验/additive-sky-seamless/data/README.md | SCI-C 数据指针（data/README.md） | 55 | 操作与运营（数据输入与真值口径说明）；单元内留位合理 | 三类数据来源、真值口径、生产入口清单、复跑产物面 | 有：行 6/16/30 逐节标「最高设计 §12.2 第 N 类」 | 2 处：本单元 `README.md:104`、`REPORT_paper.md` 附件段 | 否（是 README §3 的下钻件） | 与 `README.md` §2.1/§2.2 重叠（世界构造公式 + 生产入口清单两份，且清单不一致） | 无 | 轻（行 43 `run/SCI-403/` 任务号目录） | 1 条（行 55 `results/figs/*.png`（入库）与 `.gitignore:60 *.png` 冲突） | 行 10 的 HST 路径为不入库外部件（`testdata/README.md:55-68` 已登记 ⇒ 按项目约定不算悬空）；M42/Galaxy 两数据集只在 `testdata/index.json`、不在 `testdata/README.md`（唯一说明）⇒ 登记面分裂 | 保留；改正 figs 声明、与 README §2.2 的入口清单去重（留一处并互相引用） |
| 实验/additive-sky-seamless/docs/smooth-lambda.md | A5（SMOOTH-LAMBDA）：UPM 控制点平滑参数 λs 的合成 + 真实数据定案 | 156 | 空骨架（§3–§9 全为未替换占位符）——非标准 01 §2 任何角色；实际占 `docs/` 层 | λs 能否去天光、亮区压暗、真实台阶、推荐默认值 | 缺（行 3–7 是任务/执行者/纪律块，无最高设计条款） | 4 处：`REPORT_paper.md:373,539`、`results/REVERSE_VERIFY_CANON.md:4`、`实验/shared/REVERSE_VERIFY_MIGRATION.md:29,98` | 否（无正文结论） | 与 `CRITERIA.md`（判据）+ `code/reverse_verify/smooth_lambda/*.sh`（复跑）重叠：本文的**全部**可执行内容都在这两处，本文自身为空 | 有（行 3–7：分片/执行者/性质/独立构建/中间产物/判据先行） | 多（RELEASE-02、A5、FIX-P2a、SCI-UPM-001、"负责人指出/观察"、行 39 已知实测引用） | 3 条行号不实 + 1 条非跟踪件（`run/RELEASE-02/L4-rebuild/mosaic_out/p2_samples.json`，其 5 个计数只此一处） | **有**：行 39「生产当前 `lambda_s=0`」与 `stage2_common.h:30/58`（`P2_SMOOTHING_LAMBDA_AUTO=0.1`，且注明负责人裁决「不能为 0」）冲突；`upm.h:388` 亦写 0（生产注释自相矛盾，属另线） | **删除**（或降级为 `artifacts/evidence/…/smooth-lambda-SKELETON.md`）。理由：标准 01 §4「只写现行设计」——本件 7/10 节是 `{{TABLES}}`…`{{LIMITS}}` 字面量，不承载任何现行结论；留它只让 4 处引用者指向空文 |
| 实验/additive-sky-seamless/results/REVERSE_VERIFY_CANON.md | reverse_verify 定案结论 · SCI-C（UPM 控制点平滑 λs） | 41 | 过程台账（定案登记 + 状态登记）；实际在 `results/`（数值层目录）却承载结论与状态 | λs 分片的现行结论登记与"未回填"诚实登记 | 有：行 3 上游（最高设计 §2.3/§12.3 + ENGINEERING_SPEC §9） | 2 处：`REPORT_paper.md:373,539`、`实验/shared/REVERSE_VERIFY_MIGRATION.md:98` | 否 | 与 `docs/smooth-lambda.md` §0 表**逐行同文**（本件行 14–20 = 后者行 15–21，含同样的 `X/Y/N` 字面量）——但本件多一列"原文未回填"标注 | 有（行 3–4：上游 + 来源 + 迁移日期与任务名） | 多（行 4 `2026-09-21 ROOT-CONSOLIDATION`、行 29–30「本次迁移只搬位置、不改结论」历史叙事） | 2 条：`reverse_verify/docs/smooth-lambda.md`（旧根路径）、`run/ROOT-CONSOLIDATION/logs/migration_rerun.md`（非跟踪） | 本件把"无数字"如实登记，未与上位冲突；但行 35 复跑命令引用的 4 个脚本均在（已核） | 迁移：与 `docs/smooth-lambda.md` 一并处置——λs 的现行结论正本应是 `docs/science/PHASE2_UPM.md`/`docs/plugins/algorithms_phase2/11_upm.md`（生产默认 0.1 已在代码与合同面），本件落 `artifacts/evidence/` |
| 实验/additive-sky-seamless/results/REVIEW.md | SCI-C 独立对抗审稿记录 | 204 | 过程台账（审稿记录），§2 末条明令不入正式文档层；现占 `results/` 数值层 | 对 README/code/results 的对抗复核：6 条致命 + 9 重要 + 20+ 次要 + 复现性检查 | 缺（行 3 只写审稿对象与对照，无最高设计条款） | 4 处：`README.md:379,400,458`、`REPORT_paper.md:7,161,178,253,466` | 否 | 与 `README.md` §11（判据变更登记）重叠——同一 8 条放松两份承载，README 为整改后的现行表 | 有（行 3–6：审稿对象/对照/方式/立场） | 多（行 4 `/tmp/astrocs_pkg/…SCI-403.md`、FIX-1/2、SCI-402/403、mtime 时刻、sha256 短串 `7aae7212`/`607f93da`） | 6 类：`/tmp/**`（行 4、170、183）、`run/SCI-403/logs/*.log`（行 180）、`build/libastrocs_phase2.a`（行 179，非跟踪）、`工程控制/RELEASE-02/change-claims/CONFORM-FIX-B-001-tolerance-relative.md`（行 142；`工程控制/RELEASE-02` 跟踪数 0，整目录在 `ae0f1e63` CLEAN-402 删除）、`实验/absolute-snr/results/*.json`（行 4，通配可解析） | **本件多条已被整改推翻**（详见「同量两值」第 5 条：README A10 表已按 JSON 改正，本件仍记旧错值） | 迁移到 `artifacts/evidence/additive-sky-seamless/REVIEW-SCI-C.md`，由 README/REPORT 引用；本件数字属**审稿时点快照**，须整件加"时点限定"或在迁移时删除已被改正的对照值 |

### 实验/additive-sky-seamless/README.md — 详情

**落档支撑：有（七件原始 JSON 全在跟踪树）**。`code/c1..c7_*.py` + `code/sci_c_common.py` + `results/c{1..7}_*.json` 均在（`git ls-files 实验/additive-sky-seamless/results/` = 7 结果件 + `evidence_{lit,code}.json` + 2 份 md）。逐值抽查（读 JSON 文本，不执行脚本）：A4 `un_med=4.8023203843169995 / un_max=6.544490559400231 / d_med=0.3829520070123493 / d_max=0.8495001844136709 / b_med=0.27523341269743246` ↔ 本文行 117 的 4.80/6.54/0.383/0.850 与 REPORT 行 175 的 0.275 ✓；A5 `0.845390978109277` ↔ 行 118 ✓；A10 `slope 0.797587650536941 / pearson 0.8964021920950578 / spearman 0.7714285714285715 / ratio 5.07205210713894` ↔ 行 125 ✓；`out_of_basis_sweep` 六点（`oob_rms 0.3125338…/1.347431…/3.070152…/5.491085…/6.514473…/6.643378…`）↔ 行 133 ✓，`seam_max 0.8917056/1.8364130/1.5246187/4.6750095/7.1595203/4.5227773` ↔ 行 134 ✓，`seam_med` ↔ 行 135 ✓；c3 `delta_median 299.1680830296443 / Bref_median 297.33105962109374 / full_neg_frac 0.4839820861816406` ↔ 行 165 的 299.17/297.33 ✓；c5 `margin_vs_uniform 0.08455244978776011` ↔ 行 28「8.5%」与行 412「8.45%」；c7 `R1 level_ratio_med 0.9804535658130475 / slope_med 0.9946445618694196 / bin_span_adu 8.5 / corr −0.9980857436408667` ↔ 行 32/222 ✓；`R2 kappa 31559391.811123226 / chi2_red 1.0036568516732483 / n_used 841` ↔ 行 223 ✓；`R3 un_excess 21.835806704715708 → d_excess 0.5827556076042981`、`un_step 20.885824198649686 → d_step 0.459885352577146` ↔ 行 224 ✓；`slope_uncorrected 0.7681485768759455`（`c1_additive.json:212`）↔ 行 405「slope 0.7681」✓ 有落档。

**三处数字无落档（本批重点：字面量污染）**

1. 行 369（§9 验收门表「不可吸收部分的幅度/空间频率/来源」行）：`PSD k=4（预期 5.33±2）`。`c2_multiplicative.json` 的 M5 门值是 `psd_k_peak10=26 / psd_k_expected=21.333333333333332` ⇒ **5.33/4 是"把分块数组当原始像素"的单位错位值**（本单元 `results/REVIEW.md:37-42` 致命问题 3 已判定，README §11.1 行 419 亦已改），但 §9 验收表这一行未同步，与本文 §4.2 行 151 直接打架（同文两值）。
2. 行 151（§4.2 M5 行）+ 行 289–290（§6 第 12 条）：`1% 幅度：未检出（amp −0.0004 ± 0.038）`。命令 `grep -c "\-0.0004\|0\.038" 实验/additive-sky-seamless/results/c2_multiplicative.json` → **0**；机器值是 `amp1 0.6720891171838539 / err1 0.023018733227673917` ⇒ **无落档支撑的字面量**（`REPORT_paper.md` 附录 C-2 同一判定）。
3. 行 211（§4.6 E3）：`按需 64² 块 11,688 kB < 稠密物化 512² 14,568 kB`。`c6_sparse_dense.json:upm_memory` 实为 `rss_64block_kb 11744 / rss_full_grid_kb 14520` ⇒ 两值皆不可回溯（`REPORT_paper.md:265` 用 JSON 值，附录 C-7 已登记）。

**「PASS」与门计数是否实算**：`gates.rows[].ok` 由各 `c*.py` 的 `g.add(...)` 计算写入，`gates.n/n_pass/n_fail` 为汇总；本文各节标题的「11/11、7/7、7/7、6/6、5/5、4/4、5/5 PASS」与七件 JSON 的 `n/n_pass/n_fail` 逐一对上（命令：`PYTHONIOENCODING=utf-8 python3 -c "import json,glob;[print(f,json.load(open(f))['gates']['n'],json.load(open(f))['gates']['n_pass'],json.load(open(f))['gates']['n_fail']) for f in sorted(glob.glob('results/c[1-7]*.json'))]"`，七件全 `n_pass==n, n_fail==0`）⇒ **非硬编码**。但 `level` 字段与报告声明不一致：本文行 98/164 判 B2 为 `meta`（代数恒等式），`c3_public_plane.json` 的七行 `level` 全为 `data`（只有 c4 的 N1、c5 的 W4 已改 `meta`）⇒ 报告分级与机器分级两份口径，一份未同步（正是 `REVIEW.md:110-112` 问题 12 的残留）。

**同量两值（本件内部/与本批他件）**

| 量 | 值 A（位点） | 值 B（位点） | 判定 |
|---|---|---|---|
| 基外高频 PSD 峰 | k=26 / 预期 21.33（`README.md:151`） | k=4 / 预期 5.33±2（`README.md:369`） | 同一测量，验收表留旧值 |
| 1% 高频分量恢复幅度 | 「未检出 amp −0.0004±0.038」（`README.md:151,289`） | `0.6721±0.0230`（`c2_multiplicative.json`；`REPORT_paper.md:220`） | 报告值无落档 |
| C6 峰值 RSS | 11,688 / 14,568 kB（`README.md:211`） | 11,744 / 14,520 kB（`c6_sparse_dense.json`；`REPORT_paper.md:265`） | README 未跟改 |
| 相对 uniform 的优势 | 8.5%（`:28`） | 8.45%（`:412`）/ 8.46%（`REPORT_paper.md:253`）/ 机器 0.084552 | 三种取整并存 |
| 「PSD 定位在预期空间频率」 | 成立（`:22` 摘要、`:151`） | `psd_peak_at_injected=false`（`c2_multiplicative.json:hf_component`；峰对应周期 `psd_period_peak_px=19.69` px ≠ 注入 24 px） | 结论字段与自身布尔标记相反；`REPORT_paper.md:221` 已改口径为「量级相符，不是精确命中」 |
| 「实验单元是否被 git 跟踪」 | 「整目录未被 git 跟踪」（`README.md:436`） | 基线内 `实验/additive-sky-seamless/**` 已跟踪（`git ls-files` 命中） | 过期事实断言 |

**悬空引用（含确认命令）**：`results/figs/*.png`（行 226 R4 证据、行 457 产物清单「6 张图」）**结构性不存在**——`git check-ignore -v 实验/additive-sky-seamless/results/figs/fig_c7_real_crop.png` → 命中 `.gitignore:60: *.png`（全仓 PNG 不可能入库）；且 R4 门的判据就是"图存在"，其 `value.fig` 还写死为绝对机器路径 `/workspace/Astro CS Database/实验/additive-sky-seamless/results/figs/fig_c7_real_crop.png`（`c7_realdata.json`）。另：行 155 `run/CLEAN-402/backup/reports/RELEASE-02/c-delta-ruling.md`（非跟踪）、行 3/304 `/tmp/astrocs_pkg/**` 与 `/tmp/astrocs_build.lock`（外部）。内部 `code/*.py`、`results/*.json` 清单（行 449–458）均可解析 ✓。

**写法违规（行号 + 片段）**：行 3「任务：`/tmp/astrocs_pkg/extracted/AstroCS工程包_RELEASE-04/tasks/SCI-403.md`」；行 4–5「文件域：本目录 + …（**零生产代码改动**，缺陷只登记）」＝纪律声明入抬头块；行 388「行号在 2026-09-21 由并行任务的合规回退改动后核对于 15:0x」；行 386–390 §10 以 `FIX-1/FIX-2/FIX-3` 作 ID 列；行 414–429 §11.1「审稿后已修（本轮）」整张过程流水（"已按 JSON 逐位改正""本轮所有结果已按最终代码重跑"）；行 436–441 「整目录未被 git 跟踪」「在本轮期间被并行任务改动」＝历史叙事。**机械列校准**：`dates=[388]` 命中正确；`commit_sha=['20260923','48ceaa3']` 中前者是 `SEED_BASE`（**误报**），后者确为 Siril 外部 commit（真 commit 引用，仍属 §4 禁项，应落 `evidence_code.json`）；`history_narrative=[425]` 命中正确；`dangling_links` 六条里 `c1..c7_*.py`、`{upm_probe,sky_probe}.cpp`、`results/*.json`、`evidence_{lit,code}.json`、`实验/absolute-snr/results/*.json` 五条是**通配/花括号写法且可解析** ⇒ **误报为悬空**，只有 `results/figs/*.png` 成立。

**处置建议：保留为单元报告正本**（与 REPORT 合并后唯一）。整治动作：① 行 369/151/289/211 四处按 JSON 改正（正确值 `REPORT_paper.md` 已全给）；② 撤下对 `results/figs/*.png` 的证据依赖（R4 门改判为度量分布本身，或先改 `.gitignore` 再入库）；③ 行 3–5 抬头块与 §11.1 流水迁 evidence；④ `c3` 的 B2 `level` 与报告对齐；⑤ 行 436 的未跟踪断言删除。

### 实验/additive-sky-seamless/REPORT_paper.md — 详情

**数字层：本批最严的一件**。附录 A（行 425–467）是 41 条「数字 → `结果件:字段`」映射，抽样回核 20 条全部命中；行 9 明确「两者冲突时**以 JSON 为准**，冲突清单见附录 C」——本批唯一显式写出"文档数字让位机器数字"口径的结果层文档，**可作为其他件的模板**。附录 A#40 还标注「C1 的 `excess` 汇总 JSON 未存字段，由逐边界复算」，并给复算出处（`REVIEW.md:88-90`）。行 551 自declare 边界「所有数字为只读复核，未重跑任何实验」✓ 与 `code/` 无生成器对应本件（本件非自动产物，属人工复核件）。

**与上位/与代码的实质冲突（本件最高价值项）**：行 29（摘要第 8 条）、§4.8（行 284–306）、行 382 把 FIX-3 写成「代码实现**已落地**（`roughness_penalty` 逐级 ×10、上限 6 次、`KAPPA_EXCEEDED` 分支、provenance 字段），commit `d77fd11f`」。冻结基线 `c8f64e9a` 的跟踪代码显示该机制**已退休**：`sky_plane.h:221`「已退休：原 `kappa_max` 绝对常数（1e8）与 κ(H_solve) 门控口径（§7a 规则 4）」、`sky_plane.cpp:462/1726/1861`（唯一判据阈值 = `FZ-AP2S-RANK-RTOL`，`roughness_penalty`/`kappa_max` 键被忽略）、`module_adapters.cpp:9698/9978`、`upm.h:249/271/291/375`；`git grep -n "KAPPA_EXCEEDED" -- lib/` 只命中 `sky_plane.h:395` 的「已退休名」注释。`d77fd11f`（"SCI-502：UPM 三个生产缺陷修复"）确为 HEAD 祖先（命令：`git merge-base --is-ancestor d77fd11f HEAD` → 真），其后被 `a22b3890`（"可辨识性判决统一为一种自适应方法"）取代 ⇒ **§4.8 是被后续提交推翻的过期代码事实**。同段 FIX-1/FIX-2 的代码事实仍成立但**行号全错**：`uc.tolerance_relative = 1;` 实际在 `module_adapters.cpp:9573`（本件写 `:5994`、README §10 写 `:5948-5949`、本件附录 C-6 写「当前赋值在 `:5989-5994`」）；四态收敛实际在 `upm.cpp:110-111/1041-1061/1350/1837/1894-1903`（本件写 `:96-108/1028-1050/1247-1248`）。⇒ 三处行号 + 一处已退休机制 = 4 条不实引用，且本件自己在 C-6 给出的"修正行号"也是错的。

**结论字段是否实算**：本件不含 `PASS` 字段（只转录 JSON `gates.rows`）。§5.5 判据放松表（行 356–365）与 README §11 同源、属**人工转录**（无生成器），两处逐值一致 ✓（A2 0.7681、A10 0.896/0.771、M2 0.0376/0.107、M3 0.0295、M4 4.334、M6 ρ=0.50、N4 6.975、W1 8.45%）；行 354 说「审稿指出 11 处放松、其中 8 处此前未披露」而 README §11 标题写「8 条原判据 FAIL」⇒ 两份都对但计数口径不同（11 vs 8），文中未并表说明。

**悬空引用**：行 169/209/226/245/259/272 六处 `results/figs/fig_c*.png` ⇒ 同上（`*.png` 全局 ignore，必然不可解析）；本件图引用比 README 更重（6 个内嵌 `![...]()`，README 仅 2 处文字引用）。行 487/416/417 的 `run/SCI-403/logs/`、`实验/absolute-snr/results/*.json` 为非跟踪/通配。其余内部路径（`docs/science/PHASE2_UPM.md`、`docs/plugins/algorithms_phase2/10_sampling.md`、`11_upm.md`、`lib/algorithms/coverage/{src/upm.cpp,src/sky_plane.cpp,tests/synthetic_gate.cpp}`、`docs/algorithms/v6/frozen/01_NUMERIC_THRESHOLD_FREEZE.md`）经 `git ls-files` 确认**均存在** ✓。

**写法违规**：行 3「**SCI-C 实验单元论文式精读报告（SCI-505）**」；行 5–12 抬头表（报告对象/权威依据/证据口径/判据分级/**本轮增量**/复现）＝整块元信息 + 历史；行 11「commit `d77fd11f`，2026-09-22 01:35」；行 86「2026-09-20 负责人定案」；行 290/292/306 大量 `file:line`；行 389「`checked_at=2026-09-21`」；行 409–412 四个外部 commit 短 sha（`2f7e8b62`/`90296de1`/`48ceaa38`/`8103fec5`）；行 545/549/551「归档结果早于 SCI-502 修复」「多次手动分步跑」过程流水。**机械列校准**：`commit_sha` 八项中 `0501460`/`0703454` 是 **arXiv 号**（行 391/393 `astro-ph/0501460`、`astro-ph/0703454`）⇒ **误报**；`20260923` 是 seed ⇒ 误报；`d77fd11f`/`48ceaa38`/`8103fec5`/`90296de1`/`2f7e8b62` 为真 commit ⇒ 命中正确。`dates` 七项（11/86/298/301/389/503/545）全部命中真实日期文本 ✓ 无 overs。

**状态词跨轴（前言 §5 第 6 条）**：行 291/300「FIX-2/FIX-3 **无专项测试**」与行 298「M42 真实样本…**未取证**」用法正确（≠ 未实现）；但行 29/382 在 FIX-3 机制已被退休的情况下写「已落地」，是把**曾经存在**写成**现行** ⇒ 应判 `已退役`，是本批唯一的状态词误档实例。

**处置建议：合并**（并入 README，或保留本件为正本、把 README §4/§5/§11 的数字与登记表迁入后 README 降为入口）。理由：标准 01 §2「同一角色内同一主题只有一份正本；多份内容合并为一份」。**张力须上呈**：`docs/contracts/DUAL_LINE_CONTRACT.md:19` 把 `实验/**/REPORT_paper.md` 登记为 SCI「报告面」且「双线期间**只增不改**」⇒ 本件 §4.8 的过期代码事实**无法就地改正**，与标准 01 §1「冲突本身是缺陷，需在整治中消除」/§4「只写现行设计」直接冲突，须由负责人裁决（AGENTS §10：合同与文档标准打架）。

### m16_scene/README.md — 详情

**落档支撑：无**。三个判据脚本存在，但产物写 `run/`：`exp_a6_seeing_aperture.py:86` `OUT = ROOT / "run" / "reverse_verify" / "m16_scene" / "a6_seeing"`、`:283-284` 落 `result.json`；冻结基线内 `run/` 仅 `run/.gitkeep`（命令：`git ls-files | grep "^run/"`）⇒ 本件的 9/9、9/9、4/4 与全部测量值（0.00381 / 0.00544 / 0.3358 / 0.3315 / 解析预测 0.3878 / 0.00615 / 88.2× / 61× / 0.0127 / 0.3429 / +0.078/+0.028/+0.074% / +0.099/+0.099/+0.220% / +1.92/+0.72/+1.03% / z −0.10/−0.19/−0.06 / ≤4.3e-14 / 0.5606 / 0.8615 / 0.8659 / r 0.887/0.988/0.987 / 0.963 / +137.2/+130.7/+132.1 ADU² / 132.40 / ×1.00/×1.09/×1.18 / 0.88% / 7.6e-5、4.6e-5 e/s / 3.01、0.0196 e/s / 74–81% / 0.0134%）**全部是 markdown 独文、无原始输出**。
**「全绿」是否实算**：判据与 `ok` 由脚本计算（非字面量），但计数只存在于 `run/` 的 `result.json` ⇒ 文档层不可核，属"看起来通过"型陈述（AGENTS §9 禁止形态）。
**同量两值**：行 11 同一单元格并列三个时点的 seeing 跨度（stack_n=32 `0.00381`、stack_n=1 `0.00544`、更早修复前 `0.0127`）与盒域（`0.3358` / `0.3315` / `0.3429`），并且 PSF 域变小（0.00544→0.00381）与盒域变大（0.3315→0.3358）**方向相反**，文中只给「跨度变化主因是饱和重标定」一句归因，未解释反向 ⇒ 读者无法判断哪个是现行值。
**精度与单位**：本件是三份台账里唯一给全量纲的（mag / e⁻/s / ADU² / 无量纲比值都写明 ✓）；缺的是**坐标系**（哪一帧、哪个 seeing 档）与判据窗（行 12 的「≤5%」「[0.5,2]」只出现在括号里，未列判据表）。
**源码引用不实**：行 11「生产实现 `star_detector.cpp:151 s.flux = m00`」——`s.flux = m00` 实际在 `lib/algorithms/star_detection/wrapper_phase1/star_detector.cpp:226`（命令：`grep -n "s.flux = m00" lib/algorithms/star_detection/wrapper_phase1/star_detector.cpp` → 226），151 行是叠加满阱预算注释。这是同一处错引在批内的**第三次复制**（另两次：`实验/absolute-snr/results/REVERSE_VERIFY_CANON.md:20` 的 `:139-151`、`实验/absolute-snr/code/reverse_verify/f_instr/README.md:14`）⇒ 错引在扩散。
**悬空引用（5 条）**：行 7 `run/RELEASE-02/paper/data/STACKN32/STACKN32-001.md`、行 11/27 `run/RELEASE-02/paper/data/M16FIX/M16-SCENE-FIX-001.md`、行 18 `data/STACKN32/stackn_sensitivity_check.py`（相对本 README 目录不存在，`ls` 本目录仅 4 项）、行 38 `reports/RELEASE-02/m16-scene.md` §7。确认命令：`git -c core.quotePath=false ls-files | grep -i "STACKN32\|M16FIX\|reports/RELEASE-02"` → 只命中 `实验/shared/data/synthetic/before_stackn32/**`。
**写法违规**：行 3「**STACKN32-001（2026-09-20）**」、行 4「本目录三个判据脚本走场景 JSON 默认值，故全部已重跑。**阈值/容差一字未改**」、行 11/22/56「更早修复前」「M16-SCENE-FIX-001」「修复前全红」「C2 的预测量子式**已订正**」「旧式用…」＝日期 + 任务号 + 历史叙事三犯。**机械列校准**：`task_ids=['FIX-001','RELEASE-02']` **漏报**了 STACKN32-001 与 M16-SCENE-FIX-001/P9（工具只吃 `FIX-\d+` 与全大写目录名）。
**下游 0 个**：命令 `git -c core.quotePath=false grep -n -I "m16_scene" -- ':!实验/additive-sky-seamless/code/reverse_verify/m16_scene'` → 除 `eng/ci/ledgers/naming_surface.json:482`（那是 `实验/shared/synthetic/m16_scene.py`，非本件）与 `reports/v19r2/*.csv` 清点行外**无内容引用者** ⇒ 孤儿（无上游 + 无下游）。
**处置建议：迁移** → `artifacts/evidence/additive-sky-seamless/m16-scene/`（标准 01 §2 末条）。迁移前置：补 `result.json` 落档（否则撤下全部数字，只留判据与复跑命令），并删除 5 条不可核引用与 `:151` 错行号。

### docs/smooth-lambda.md — 详情

**本批唯一"文档主体为空"件**：§3 `{{TABLES}}`（行 105）、§4 `{{REAL}}`（111）、§5 `{{OBS}}`（117）、§6 `{{RECO}}`（123）、§7 `{{ADAPT}}`（129）、§8 `{{SURFACE}}`（135）、§9 `{{LIMITS}}`（141）——7 个模板槽一字未填；§0 结论表（行 13–21）「关键数值」列 5/7 行是 `N×`、`X%`、`X% vs Y%`、`台阶比 X（λs=0）→ Y（推荐 λs）`、`= **X**（区间 [a, b]）` 字面量 ⇒ **不承载任何现行结论或测量数字**。`results/REVERSE_VERIFY_CANON.md:8-10` 只登记了 §0 的占位符，未登记 §3–§9 整段为空 ⇒ **登记不完整**（该件的"诚实登记"因此仍偏乐观）。
**数字层**：有值的只有 §1/§2 的输入参数与外部事实——`gain=2.0 e-/ADU`、`read_e=5`、`dark_e=3`、`sky_base=1.0e4`（行 62，正向模拟参数，声明不反推 ✓）；`control_variance = k_corr·(π/2)·σ_bg²/N_retained`，`k_corr=1.4`、`N_retained=4096`（行 59）；`lambda_s=1000 时台阶比 0.96`（行 39，来源写作 `FIX-P2a`，无落档件）；行 66–68 的 `277234 obs / 33472 control / 523 tile / 49 帧` 与 `33423 条网格边中 6433 条覆盖突变边 = 19.2%` 来源是 `run/RELEASE-02/L4-rebuild/mosaic_out/p2_samples.json`（非跟踪）⇒ **五个计数无落档**。自洽性可核：6433/33423 = 19.25% ✓，即 19.2% 是实算比例而非独立测量。
**空槽本应由谁填**：行 155 调用 `code/reverse_verify/smooth_lambda/report.py`，其 docstring 即「汇总 λs 扫描指标 → markdown 表 + ASCII 曲线（供 `docs/smooth-lambda.md` 引用）」⇒ 生成器存在、从未回填 ⇒ 空槽是可证的未执行，而非"待定"。
**源码引用不实（3 处）**：行 33「目标函数（`upm.cpp:543,669`）」——`lambda_s` 实际读出于 `upm.cpp:565`、进法方程于 `:691`（命令：`grep -n "lambda_s" lib/algorithms/coverage/src/upm.cpp` → 8/565/691/909/1225）；543 行是 `if (!gseen[v]) {`，669 行是 `raw_w[i] = raw_w[i]/s * m->controls[ck].reliability`（恰是行 36–37 主张的归一性质）⇒ **两条引用的行号互相对调**。行 96「与 `module_adapters.cpp:4991-5040` 一致」——该处是 `const int W = im.w(), H = im.h();`，生产标志默认实际在 `upm.cpp:281-302` / `upm.h:102`（命令：`git grep -n "gs_damping" -- lib/`）。行 36「`upm.cpp:645-649`」标为两级权重归一，实际 645–649 是缺 ivar 的显式报错分支。
**与上位/与实现冲突**：行 39「生产当前 `lambda_s=0`」与 `stage2_common.h:30/58`（`constexpr double P2_SMOOTHING_LAMBDA_AUTO = 0.1;`、`double smoothing_lambda = P2_SMOOTHING_LAMBDA_AUTO;`，注释注明负责人裁决 GAP_AUDIT §9.39「smoothing_lambda 不能为 0」并登记与 `docs/algorithms/PHASE2_UPM.md:382`「0.0（默认关闭平滑）」的冲突）相反；同仓 `upm.h:75/388` 又写「默认 0」⇒ 同一默认值在**文档 0 / 头注释 0 / 实际常量 0.1** 三处打架，且 `REPORT_paper.md:341` 给出第四种说法（"编译期默认 0.0，`auto` 才解析为 0.1"）。本件的 §6 推荐值槽为空 ⇒ λs 生产口径在实验层无任何定案。
**写法违规**：行 3「分片：RELEASE-02 / A5（SMOOTH-LAMBDA）。执行者：SubAgent（UPM 平滑参数实验分片）」+ 行 4「性质：**只做实验，不改生产代码/文档**；零 git 写；未跑 ninja/cmake/ctest」＝元信息块 + 纪律声明；行 33/36/39/59 任务号 `SCI-UPM-001`、`SCI-UPM-WEIGHT-001`、`FIX-P2a`；行 40/83/115「负责人指出 / 负责人观察验证 / 负责人的缓解观察」（§4 禁"负责人…"表述）；行 50/55 与 147/156 的代码块围栏写成**双反引号** ` `` `/` ``bash ` ⇒ 围栏不闭合、markdown 渲染坏，§10 复现命令不可直接复制。机械列 `dangling_links=[]`、`unregistered_index=True`：前者**漏报**（未判 3 处 `file:line` 不实与 run/ 非跟踪件），后者成立。
**处置建议：删除**（或整体降级为 `artifacts/evidence/additive-sky-seamless/smooth-lambda-SKELETON.md`）。理由：标准 01 §4「只写现行设计要怎样」+ §2 末条（过程台账不入正式文档层）——7/10 节为字面量占位符的文档不承载现行设计，却占据 `docs/` 命名空间并被 4 处引用者指向。删除须同步改指：`REPORT_paper.md:373,539`、`results/REVERSE_VERIFY_CANON.md:4`、`实验/shared/REVERSE_VERIFY_MIGRATION.md:29,98`。其 §1/§2 的方法学内容（噪声链、真实 L4 拓扑、场景矩阵）若需保留，应并入 `docs/algorithms/` 或单元 `code/…/CRITERIA.md`。

### results/REVERSE_VERIFY_CANON.md（SCI-C） — 详情

**与 §1 正本的关系（二次承载）**：行 14–20 与 `docs/smooth-lambda.md` 行 15–21 是**同一张 7 问表的逐行复制**，本件仅多每行末「（原文未回填）」与行 8–10 的诚实登记块；行 22–25 的「单位声明（逐字保留）」与 `smooth-lambda.md:23-26` 同文。⇒ 按标准 01 §2「研究与佐证类以引用方式服务正本，不重复承载正本口径」，本件连"空"都复制了一份。
**数字层**：本件**无测量数字**（全部指向原文小节），唯一硬值是行 37 复跑命令里的 `SW_LAM="0,1e-3,0.01,0.03,0.1,0.3,1,10,1000"`，与 `smooth-lambda.md:97` 的 λs 网格同串 ✓ 一致。⇒ 本件的"无落档"风险为 0；真正的风险是**它指向的原文无正文**，整条 λs 线在文档层没有任何可核数字（这与 `REPORT_paper.md:373,539` 把它列为"如实登记"的依据相互一致 ✓）。
**悬空引用**：行 4 `reverse_verify/docs/smooth-lambda.md`（旧根路径，`ls -d reverse_verify` → 不存在；现位置同行后半句已给）；行 41 `run/ROOT-CONSOLIDATION/logs/migration_rerun.md`（非跟踪）。行 35–38 引用的 4 个脚本 `build.sh`/`gen_all.sh`/`sweep_all.sh`/`run_mosaic_ls.sh` **均存在且跟踪** ✓（命令：`git ls-files 实验/additive-sky-seamless/code/reverse_verify/smooth_lambda/`）。
**写法违规**：行 4 日期 + `ROOT-CONSOLIDATION`；行 29–30「**待完成**：…（属 SCI-C 实验单元的后续工作，不在本次根目录整合范围）。本次迁移**只搬位置、不改结论**」＝历史/过程叙事。机械列 `upstream_pointer=True`、`dates=[4]` 均命中正确 ✓。
**处置建议：迁移** → `artifacts/evidence/additive-sky-seamless/`。三条内容里只有"复跑命令"有长期价值（应并入 `CRITERIA.md` 或分片 README）；λs 的**生产取值**（0.1 还是 0）必须由 `docs/science/PHASE2_UPM.md` / `docs/plugins/algorithms_phase2/11_upm.md` 收敛，不属本件，且本件的「待完成」不能替代该收敛。

### results/REVIEW.md（SCI-C） — 详情

**本件与 README 的时点关系（"结论写成字面量"的反向案例：台账把已被改正的旧值当现状登记）**：致命问题 1（行 19–25）以表格记「README 基外 RMS 0.04/0.12/0.40/1.30/4.24/8.20」对「JSON 0.3125/1.3474/3.0702/5.4911/6.5145/6.6434」；现 README 行 133–135 **已等于 JSON 值** ⇒ 本件记录的 README 错值在仓库中已不存在（命令：`git grep -n -I --fixed-strings "4.24" -- 实验/additive-sky-seamless/` 只命中本件行 21）。同类三处：问题 2 的 `spearman = 0.09999999999999999`（行 33，旧值）现为 `spearman_vs_hf 0.49999999999999994`；问题 5 的「mu=−5.012」（行 52）现为 `detector_calibration.mu = −5.101995835997826`（README:179 已用 −5.10）；问题 9 说「c1 JSON 的 `seam_native` 没有 excess 键」而现在**有**（`seam_native.uncorrected[0].excess = 1.4818539216604876`）⇒ 本件是**审稿时点快照**，其所有"README 现状"断言均已过期，留在 `results/` 会被读成现行事实。
**判据是否实算**：本件不产判定，只转录；其"实测"逐值可在 JSON 回核 ✓（`c4_seam_criterion.json`：`separation` N2 `36.44318175990364`、N1 `0.09234725368755825`、`detection_limit_e 6.975350326829212`、`response.slope 0.4491564905529156`、`null_normality.skew 0.07365636704364083`、`shapiro_p 0.858243911726384`；`c5_weights.json` `margin_vs_uniform 0.08455244978776011`）。本件的独立复算（行 166 的 ≈5.5σ/6.2σ、行 158/184–186 的 excess 14.4×/11.0×/37.5×）属"由 JSON 字段另算"，JSON 无对应字段 ⇒ **复算可推但未落机器事实**（37.5× 除外，它可由 `R3` 的 21.8358/0.5828 直接除得）。
**精度/单位**：数字一律带 e⁻ / ADU 单位 ✓；行 40 的 k 轴单位说明（「每 128 个分块样本」）是本批少见的高质量口径澄清 ✓；行 34 的「−47.7…+41.4 e-」跨度与行 41 的 `psd_period_peak_px` 均为**当时**值，现已随 c2 重跑失效。
**悬空引用**：行 4 `/tmp/astrocs_pkg/.../SCI-403.md`（外部）；行 142 `工程控制/RELEASE-02/change-claims/CONFORM-FIX-B-001-tolerance-relative.md` ⇒ `git -c core.quotePath=false ls-files | grep -c "工程控制/RELEASE-02"` = **0**，`git log --diff-filter=D -- 工程控制/RELEASE-02/change-claims/` 命中 `ae0f1e63 chore(governance): 清理历史控制包与一次性报告（CLEAN-402）` ⇒ **确认指向已删除件**；行 179/180/182 的 `build/libastrocs_phase2.a`、`run/SCI-403/logs/*.log`、行 183 的 `/tmp/scic_chk` 均非跟踪；行 196 的「工作树 357 个已跟踪文件被改 + `实验/additive-sky-seamless/` 未跟踪」与冻结基线相反 ⇒ 过期事实断言。
**写法违规**：mtime 时刻群（95/181/182/198/199：14:36:47、14:42:56、14:54:36、14:27:41、14:58:14）、`FIX-1/FIX-2`、`SCI-402/SCI-403`、`VIS-401`、`RELEASE-02`、短 sha `7aae7212`/`607f93da`、行 130「§8.1 文献表 4/9 行的卷页与…不符」＋行 122「Crossref 实时核验」（联网核验记录，无落档件）。机械列 `history_narrative=[94,142,198]` 命中正确；`dangling_links` 两条中 `实验/absolute-snr/results/*.json` 是通配且可解析 ⇒ **误报**，`工程控制/…` 成立。
**独有证据（不得随迁移丢失）**：行 144「κ 的普遍性：C1 3.36e6 / C3 3.00e6 / C7 3.16e7 都接近 `sky_plane.cpp:409` 的默认 `kappa_max=1e8`」、行 146「`make_figures.py:93` 用 `np.random.default_rng(0).normal(...)` 画 null 直方图，不是 120 个真实实现」、行 147「`make_figures.py:165` 第二参数 `d3` 未使用」、行 148「`c4_seam_criterion.py:92-93` 的 `sep` 是死代码」、行 149「C6 E3 只有 20% 余量」——这五条在 README/REPORT 里**均无对应登记**（README:529 只收录了其中的 R4/E3 部分）。注意行 144 的 `sky_plane.cpp:409` 在当前基线已不存在该默认（`kappa_max` 全退休）⇒ 该条也已过期，迁 evidence 时须标注时点。
**处置建议：迁移** → `artifacts/evidence/additive-sky-seamless/REVIEW-SCI-C.md`，标题加"审稿时点"限定；删除已被整改推翻的"README 现状"列（行 19–25、33、52、95），保留次要问题五条与复现性检查（当前唯一记录）。不得删除（标准 01 §2 末条要求留证台账落 `artifacts/evidence/` 并由正本引用，而非销毁）。

（接表 1，续 3 份）

| 路径 | 标题 | 行数 | 角色 | 主题 | 上游 | 下游 | 是否正本 | 重复或重叠对象 | 元信息块 | 写法违规 | 悬空引用 | 与上位冲突 | 处置建议 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 实验/healpix-polar/README.md | 实验单元：healpix-polar（极区面积交叠） | 54 | 研究与佐证（单元入口/索引） | 单元是什么、一句话结论、复现与自查、与生产码关系 | 缺（无最高设计条款；行 5 只述 mosaic 阶段） | 1 处（`eng/ci/ledgers/naming_surface.json:472` 为命名门登记，非内容引用）⇒ 内容层近乎孤儿 | 否（入口件） | 行 13–24「一句话结论」与报告 §0、摘要 §1 三处承载同一批数字（9.97e-2 / 376 / 3.82e-14 / 283/289 / 1.802e-04） | 无 | 轻（行 8 报告名含任务号 EXP-07；行 29/30/36 时长估称与 run/ 路径） | 1 条：行 36 `run/EXP-07-POLAR/verify/check_report2.py`（非跟踪，且其"59 项"与报告"60 项"打架） | 行 22「机制未定位」← 报告 §4.7.2 标题即「机制已定位」；行 22「全天 12 条…实测」← 报告:481/656「只实测 face 0 的北接缝 1 条，其余按对称性推断」 | 保留（单元入口，指向报告与 `results/` ✓）；须把结论强度与"实测/推断"边界改成与报告一致，59/60 定一个 |
| 实验/healpix-polar/docs/EXP-07-POLAR-摘要.md | EXP-07-POLAR 上呈摘要（≤1 页） | 59 | 研究与佐证（上呈摘要，派生件） | 三条根因 + REC-1 + 代价对照 + 全天是否达成 + 待决项 | 缺条款号；行 4 给门禁出处但**行号不实**（`docs/algorithms/DRIZZLE_GEOMETRY.md:236` 实为 §6 的 ThreadLease 段，门禁在 **:304**） | 0 处（`git grep "EXP-07-POLAR-摘要"` 只命中自身标题行；单元 README:8 只指报告不指本件）⇒ **孤儿** | 否（报告 §0 的压缩件） | 与报告 §0（行 12–64）逐段重叠 + 与 §3/§4.7.2/§7 的表格重叠（同一批值两份承载） | 有（行 3–5 粗体项行：课题/门禁/口径） | 轻（标题「（≤1 页）」、T2/T3/T15/T20/T28/T29 探针编号遍布；无日期、无 sha） | 同「门禁出处行号不实」1 条 | 行 37 表列 `1e-7 ⇒ 1.12e-8` 与行 43 正文 `1e-7 ⇒ 最坏 2.79e-8` **同页两值**（无口径说明）；行 22「T16 … 0/121、1.90e-14」与报告:298「T16 … 0/441、6.96e-14」互斥 | **合并**（并回报告 §0 或删本件）：标准 01 §2 同主题一份正本 + §3 双向索引（本件无引用者且 README 不指向它 ⇒ 索引不可达） |
| 实验/healpix-polar/docs/EXP-07-POLAR.md | EXP-07-POLAR — 极区面积交叠：极点破门根因与候选算法对照 | 795 | 研究与佐证（实验报告）+ 含算法推导（§2.1/§6.1 推导与伪代码，按 §2 亦涉 `docs/algorithms/`） | 极点破门三根因、闭合判据失明、接缝绝对地板、REC-1 与 chart 原生路径对照 | 缺（行 3–8 是单元/复现/版本口径块；「最高设计 §12.2」迟至行 150 正文出现，非开头标注） | 5 处**跨单元实引用**：`实验/m42-realdata/README.md:225`、`docs/CRITERIA.md:263`、`code/c4_leaf_allocation.py:17,288`、`results/c4_leaf_allocation.json:620`（均带 `:412/:650-657/:670-673/:677` 行级引用，逐条核对**全部命中** ✓） | **是**（本主题唯一有正文的件） | 摘要为其压缩件（合法派生，但摘要另加两值）；README:15–24 为其结论段复述 | 有（行 3–8：实验单元/复现指向/版本口径 + `results/SNAPSHOT.sha256` 引用） | 中（EXP-07-POLAR、INNOV04-LAND-01、RC1/RC2a/RC3、T0–T29 编号遍布；无日期、无 commit） | 2 条：行 736 `cd "/workspace/Astro CS Database"` + `run/EXP-07-POLAR/verify/check_report2.py`（机器绝对路径 + 非跟踪件）；行 153 HST FITS 为登记过的不入库外部件（`testdata/README.md:55-68` ✓ 不算悬空） | 行 72/571 门禁出处 `DRIZZLE_GEOMETRY.md:236` 不实（应 :304，§9 段号引用 ✓ 正确）；4 处数值在跟踪日志中**不可回溯**（详情块） | 保留为本主题正本（报告位）；改正 4 处不可回溯值 + 门禁行号 + 补开头上游标注 |

### 实验/healpix-polar/README.md — 详情

**落档支撑：有**。本单元是本批唯一把探针原始输出**副本入库**的件群：`results/` 有 25 个 `.out` + 7 个 `.csv` + `SNAPSHOT.sha256`（共 33 项，全部跟踪），且 `sha256sum -c results/SNAPSHOT.sha256`（在 `实验/healpix-polar/` 下）→ **47/47 全部 OK**（命令与输出：`cd 实验/healpix-polar && sha256sum -c results/SNAPSHOT.sha256 | grep -c ": OK$"` → `47`）⇒ 报告头「版本口径…复核快照 SHA256」是可核的现行事实，非装饰。
**数字与日志逐条核对（命中项）**：行 15 `9.97e-2` ↔ `p6_leafmap.out`/报告 §4.1 ✓；行 19–20 `use_fast=0` `3.82e-14`、`full=376` ↔ `p9_review.out:6-7`、`p1_t4_pole_full.out:4` ✓；行 22 `283/289`、`1.802e-04` ↔ `p9_review.out:6` ✓；行 39–40 `≤1.13e-16 rad`、`≤3.4e-10` ↔ `p0_selftest.out:11`「1.130e-16 rad -> PASS」✓。
**同量两值/与上位不一致**：① 行 22「机制未定位」↔ 报告 §4.7.2 标题「机制已定位：**浮点相消型绝对面积地板**」（日志 `p9_t28.out:4` 给出绝对亏空 1.119e-06→实际 1.694e-16 sr 列，见 `p9_review.out:16-17`）⇒ 入口件的结论强度落后报告一版；② 行 22「`u+v = 1` 接缝（全天 12 条）**实测** 283/289」↔ 报告:481「**但只实测了 face 0 的北接缝 1 条**，其余 11 条未实测」+ :656「其余按对称性推断」⇒ 把"1 条实测 + 11 条推断"写成"12 条实测"；③ 行 36「59 项一致性自检」↔ 报告:791「`check_report2.py`（60 项）」⇒ 同一脚本两个计数，且脚本在 `run/` 内**不入库** ⇒ 两个计数都无法核（前言 §5 的"计数须给确定命令"在此无法满足，只能登记冲突）。
**写法违规**：行 8 报告名 `EXP-07-POLAR.md`（任务号，但那是文件名，属可接受的下限形态）；行 11/36 `../../run/EXP-07-POLAR/…`（行 11 已自declare「不入库」✓ 合规声明；行 36 未声明却把它当自查手段）；行 29–30「约 15 分钟 / 约 60 分钟」时长估称（非日期，不违 §4）。机械列 `task_ids=['EXP-07']` 命中、`dates=[]`、`commit_sha=[]`、`dangling_links=[]`、`unregistered_index=True` 全部与人工判定一致 ✓（本件是机械列零误差的一件）。
**处置建议：保留**。整治：① 行 22 与报告 §4.7.2/§7 对齐（"已定位到绝对地板 / 最后一步未定位"、"12 条中实测 1 条"）；② 59/60 择一并给生成计数命令；③ 行 36 的自查脚本入库（`code/` 或 `eng/tools/`）或删除该段——现在读者无法执行单元自己声明的自查。

### 实验/healpix-polar/docs/EXP-07-POLAR-摘要.md — 详情

**落档支撑**：绝大多数值可回溯（`3.16e-07`↔`p1_t4_pole_full.out:9`；`0.516`类无；`2.441e-04/8.644e-17`↔`p1_t3.out:8,10`（摘要行 12「6.1e-5…2.4e-4 / 8e-17」✓）；`1.7489e-04`、`3.317e-03`↔`p9_review.out:44`、`:17`；`283/289`↔`:6`）。**两处例外**：行 22「T16 极点扫描 V0 破门 0/121、最坏 1.90e-14」——121 ✓ 与 `p4_hst_n23.out:16` 的 `n=121` 一致，但 `1.90e-14` 是 **2²³** 档的值，而 `p4_hst.out:16`（2²¹，同为 n=121）是 `2.148e-14`，摘要未标 nside ⇒ 同一段里 2²¹/2²³ 混读；行 43「推荐配置 1e-7 ⇒ 最坏 2.79e-8」与本件行 37 表格「1e-7 ⇒ **1.12e-8**」**同页两值**。
**两值的真实来源（已核，非猜测）**：`1.1220e-08` 出自 `p2_t9.out:5`（T9，日志头 `配置=49`）；`2.789e-08` 出自 `p3_t13.out:65`（T13-N4，同 1089 配置极点栅格）。⇒ 两个值都对，差在**样本数不同**（49 vs 1089），而摘要与报告都没写这层 ⇒ 读者会以为同一实验出了两个矛盾结果；报告 §6.1 行 615 更进一步把 N4 的 `2.79e-8` 与 T9 的 `1333 段/叶` 拼成同一句推荐配置（**混源数字对**：精度取 1089 配置、代价取 49 配置）。
**精度与单位**：本件单位登记最完整（`nside=2²¹`、`0.2″/px`、`pixfrac=1、m=1`、`±8 px / 0.5 px 网格 1089 配置`、`sr`、`rad`、`hp_res`、`e⁻` 均在位 ✓），且行 5 明示「全部为自包含探针，未跑产品二进制、未改生产代码」⇒ 可判定性最好的一件。坐标系：行 20「三角点 2.0e-8、赤道角点 2.5e-10」未给 face/ra/dec 位形（报告 §4.5 表有 ra/dec）⇒ 摘要层丢坐标。
**写法违规**：标题「（≤1 页）」是交付规格自述；行 3–5 粗体抬头块（课题/门禁/口径）＝元信息块；行 9–23 的 T2/T3/T15/T16/T20/T28/T29/T7 探针编号（属实验内部编号，按 §4「无任务编号」的严格口径应指向报告小节而非裸编号）；无日期、无 commit、无 sha ✓。机械列 `dangling_links=[]`：**漏报**行 4 的 `DRIZZLE_GEOMETRY.md:236`（该文件存在 ⇒ 工具判不悬空，但**所指行不是所引内容**，行级不实只有人工能发现）。
**处置建议：合并**（标准 01 §2 同主题一份正本 / §3 索引可达）。本件无任何报告 §0 之外的独有结论，唯一独有信息是行 57–58 的两条开放项（与报告 §7 行 670–676 同文）⇒ 删本件、把开放项留在报告 §7；或保留则必须与报告逐值对齐 + 给 1.12e-8 / 2.79e-8 的口径说明。

### 实验/healpix-polar/docs/EXP-07-POLAR.md — 详情

**落档支撑：有，且本批最强**。§9（行 745–770）给「日志文件 → 支撑结论」的 22 行映射；`results/` 33 项原始件（25 `.out` + 7 `.csv` + `SNAPSHOT.sha256`）全部跟踪；`SNAPSHOT.sha256` 47 条全 OK（命令见 README 详情块）。抽查命中：§4.4 全 17 行表 ↔ `t17_extent.csv` 按 r 聚合的 max/中位逐位一致（例 r=32：v0 max 2.094e-13、med 4.081e-14、rec1 max 1.474e-13、oracle max 1.950e-13 ↔ 报告 2.09e-13/4.08e-14/1.47e-13/1.95e-13 ✓；r=1024 同理 6.681e-10/1.021e-10/1.755e-10/2.177e-10 ✓）；§4.6 `1.589e-3/4.167e-4/1.042e-4/2.606e-5` ↔ `p2_t8b.out:8` ✓；§4.1/§4.5 `9.07e-01` ↔ `p2_t8b.out:5-7` ✓；§4.7.1/§4.7.2 的 `283/289 1.802e-04`、`280/289 1.7489e-04`、`|Δ|/A=4.153e-05`、`3.122e-03 / 3.317e-03`、绝对亏空 `1.694e-16 sr` ↔ `p9_review.out:6,16,17,44` ✓；§4.1 主表五变体行 ↔ `p1_t4_pole_full.out:4-8`、`leafmax 3.159e-07` ↔ `:9` ✓；§7 引用的 `:412/:650-657/:670-673/:677` 被 `实验/m42-realdata/` 四件按行消费，**四条行号全部命中** ✓。
**四处不可回溯数字（本批重点结论：有快照有日志仍不等于数字保真）**
1. §4.9 表 nside=2²¹ 首行「帧中心 ±10 px / **1 px** | **441** | V0 0 / 2.15e-14」：日志头是 `帧中心 +-10px, 2px 栅格 … 汇总 n=121`（`p4_hst.out:2,16`）⇒ 步长与配置数错，值 2.148e-14 ✓ 对得上；同表 2²³ 行写 `2 px / 121` ✓ 正确 ⇒ **两行参数不一致**。
2. §4.3 行 298「T16 极点扫描里 V0 破门 **0/441**、最坏 **6.96e-14**」：`441` 与 `6.96e-14` 在 `p4_hst.out`/`p4_hst_n23.out` 中**均不存在**（命令：`grep -n "441\|6.96e" 实验/healpix-polar/results/p4_hst*.out` → 0 命中）⇒ 该句是"闭合判据对 RC3 失明"这一关键论断的证据句，值不可核；日志的对应值是 2.148e-14（2²¹）与 1.896e-14（2²³）。
3. §4.9 第 2 点行 553「nside=2²³ 时 … V0 仍给出 **1.18e-13**」：同件日志为 `1.896e-14`（`p4_hst_n23.out:16`），1.18e-13 无出处（全 `results/` 检索 `1.18e-13` → 0 命中）。
4. §4.1 行 229 / §0.3 行 39「`leafmax_err` 最坏 3.159e-07、**中位 2.14e-08**」：最坏 ✓ 在 `p1_t4_pole_full.out:9`，**中位 2.14e-08 在跟踪日志与 CSV 中均无**（命令：`grep -rln "2.14e-08" 实验/healpix-polar/results/` → 0 文件）⇒ 主判据的中位数未落档。
**PASS/FAIL 类字段是实算还是字面量**：探针打印（例 `p3_t13.out:65`「N4 极点 REC-1(1e-7): 破门=0/1089 最坏=2.789e-08 -> 绿 OK (须全绿)」；`p1_t4_pole_full.out:9` 带判据文字）⇒ 实算 ✓。但报告**选择性转录总结论行**：报告:411 引 `# SUMMARY T12: PASS`、:511 引 `# SUMMARY T13: PASS`，而两份 HST 日志的 `# SUMMARY T16: FAIL`（`p4_hst.out:56`、`p4_hst_n23.out:62`）**从未出现在报告里**——§4.9 只以文字解释 FAIL 成因（负对照 + REC-1 预算下限），读者从报告无法得知该实验的自动总结论是红。
**与上位冲突**：行 72 与行 571 的门禁出处 `docs/algorithms/DRIZZLE_GEOMETRY.md:236` 不实（该处是 §6 的 ThreadLease 合并序；FP64 闭合 `<1e-6` 在 **:304**，`arc-chord 1e-6·hp_res` 在 :313）；§9 行 795 的 §9 段号引用 ✓ 正确 ⇒ 同一事实在同一文档里一次对一次错。行 153–156 的 HST 头部转录值（`CD1_1=−9.1016831619991e-6`、`NAXIS 8000×8400`、`0.0400″/px`、`ORIENTAT −35°`）与 `testdata/README.md:64-71` 登记值逐项一致 ✓。
**写法违规**：EXP-07-POLAR（标题与全文）、RC1/RC2a/RC3、T0–T29、`INNOV04-LAND-01`（行 79/394/412/569）、`REC-1`、`FZ-AP2S-KAPPA-MAX`；行 3–8 抬头块含「版本口径 …§0.6 记录的探针自身 bug 修复（`rotate_to_z`）之后，§4 全部数字均已重跑」＝历史叙事；行 214「**这张表推翻了**"RC1 直接抬高闭合量 4 个数量级"的因果叙述」＋行 506「**N2 不能作为…（自我更正）**」＋行 252「§0 的表述已按此改写」＋行 394「同时排除了 INNOV04-LAND-01 的假说」＝四段"以前怎么写错、现在改了"的过程留痕（§4 禁）。机械列 `commit_sha=['0409513','0412607','2097152','8388608']` **全部误报**：前两个是 arXiv 号（行 565/566 `astro-ph/0409513`、`astro-ph/0412607`），后两个是 `nside` 值 2097152/8388608；`sha256=True` 命中 `results/SNAPSHOT.sha256` 文件名（不是正文 sha）⇒ **误报**；`dangling_links` 一条（HST f502n）按项目"不入库外部数据"约定不算悬空 ⇒ **误报**（但 `:236` 行级不实被**漏报**）。
**处置建议：保留**（本主题正本）。整治：① 4 处不可回溯值按日志改正（441→121 并统一两行步长、6.96e-14→2.148e-14 或补落该实验的独立日志、1.18e-13→1.896e-14、中位 2.14e-08 补日志或删）；② 转录 `# SUMMARY T16: FAIL`；③ `:236`→`:304`；④ 开头补「上游：ASTROCS_DESIGN.md §…」（现缺，§12.2 迟至行 150）；⑤ 把「推翻/自我更正/已按此改写」四段留痕压成现行结论（标准 01 §4）。

## 2. 同主题组与差异（含跨批）

| 主题组 | 各份路径（本批 / 别批） | 正本候选 | 主要差异（可核事实） | 合并/定案建议 |
|---|---|---|---|---|
| EXP-05 数值层 | `results/EXP05_TABLES.md`（本批）/ `docs/EXP-05-ABSOLUTE-SNR.md`（DB-15） | 报告 | 表 A 12 行双承载；比值列精度不同（313.5x vs 313x 等 4 处）；负例/DEGENERATE 标记只在报告侧 | 报告保表 A，结果件保明细；或结果件加"以报告标注为准" |
| EXP-06 数值层 | `results/EXP06_TABLES.md`（本批）/ `docs/EXP-06-SNR-PHYS.md`、`docs/EXP-06-SUMMARY.md`（DB-15） | 报告 | 结果件无口径/seed 声明、门表列语义混（实测列装 detail 散文）；摘要件称「25 门 + 12 消融门」而消融件 `e6` 不进结果表 | 结果件补 T1/T2 口径与 seed；消融表补进结果件或摘要改述 |
| SCI-B 定案台账 | `results/REVERSE_VERIFY_CANON.md`（本批）/ `docs/{frame-snr-canon,f-instr-canon,snr-propagation-design}.md`（DB-15） | 三份 canon 文档的 §0 | 本件 §1 逐字复制 canon §0；§2 数字无落档（f_instr 产物在 run/）；§3 数字有落档（snr_design/audit JSON） | 台账迁 evidence；f-instr 数字补原始件否则撤 |
| SCI-C 报告面 | `README.md` + `REPORT_paper.md`（均本批）+ `results/REVIEW.md` | 二者之一（须与 `docs/contracts/DUAL_LINE_CONTRACT.md:19` 一并裁） | 附录 A/C 与 9 条冲突登记只在 REPORT；README 有 3 处不可回溯值 + 断句（README:137）；REPORT §4.8 的 FIX-3 已被后续提交退休 | 合并为一份；REPORT 的数字口径 + README 的单元索引职责 |
| λs（SMOOTH-LAMBDA）线 | `docs/smooth-lambda.md` + `code/reverse_verify/smooth_lambda/CRITERIA.md` + `results/REVERSE_VERIFY_CANON.md`（均本批） | 无（三件都不承载结论） | 报告体 7 节为 `{{…}}` 字面量；判据 C1–C8 无一条有落档结果；CANON 已declare「待完成」；生产默认值三处打架（文档 0 / 头注释 0 / 常量 0.1） | 三件同一处置：骨架删除或降 evidence；λs 生产值由 `docs/science/PHASE2_UPM.md` 收敛 |
| 极区/接缝面积交叠 | `README.md` + `docs/EXP-07-POLAR.md` + `docs/EXP-07-POLAR-摘要.md`（本批）/ `实验/m42-realdata/*`（消费方，别批） | 报告 | 入口件结论强度落后（"未定位"）、"12 条实测"过度声明；摘要同页两值；报告 4 值不可回溯 + T16 FAIL 未转录 | 报告为正本；摘要合并；README 与报告逐值对齐（m42-realdata 按行消费报告 :412/:650/:670/:677，改动须连带核对） |
| 独立审稿记录（同型跨批） | `实验/absolute-snr/results/REVIEW.md`、`实验/additive-sky-seamless/results/REVIEW.md`（本批）/ `实验/photometric-magnitude/results/REVIEW.md`（别批） | 无（三份同为过程台账） | 三份均在 `results/`、均含 md5/sha + 时刻 + 已删控制包路径；本批两份的"被审现状"列已被整改推翻 | 三份统一迁 `artifacts/evidence/<unit>/REVIEW-*.md` 并加时点限定 |
| reverse_verify 定案三兄弟（跨批） | `实验/{absolute-snr,additive-sky-seamless,photometric-magnitude}/results/REVERSE_VERIFY_CANON.md` | 各 unit 的 canon 文档 | 同模板：上游块 + 迁移日期 + 「实测与退役登记见 `run/ROOT-CONSOLIDATION/logs/migration_rerun.md`」（该件非跟踪，三份同犯） | 统一处置；把迁移登记归 `实验/shared/REVERSE_VERIFY_MIGRATION.md` 一处 |

## 3. 索引与链接缺陷清单

**登记面与标准 01 §3 冲突（结构性，16/16 命中）**：`docs/DOCUMENT_INDEX.yaml` 扫描口径明写「排除 …`实验/`… 等过程与证据目录」（行 29–30），规则 2 也只要求 `docs/**/*.md` 登记 ⇒ 本批 16 份**全部不在唯一索引地图**（机械列 `unregistered_index=True` 16/16 正确）。标准 01 §3 要求「现存文档全部登记」，两者直接冲突：要么索引扩面（含 `实验/**` 的 62 份 .md），要么标准明确把实验单元划出"文档集"。**这不是本批任何一份文档的缺陷，是索引口径缺陷**，须上呈。索引自身另犯 §4：行 4「重写：DOC-403（2026-09-21，基线 HEAD `d1ccfa1bed9c`）」＝任务号 + 日期 + commit。

**未登记 / 登记了但文件不存在**：本批 16 份均未登记（同上）；`docs/DOCUMENT_INDEX.yaml` 内无指向本批 16 份的条目 ⇒ 无"登记不存在"项。

**悬空引用汇总（逐条已在详情块给命令）**

| 类型 | 位点 | 目标 | 确认 |
|---|---|---|---|
| 结构性不存在（全局 ignore） | `additive README:226,457`、`REPORT_paper:169,209,226,245,259,272`、`data/README:55` | `results/figs/*.png`（声明"入库"） | `git check-ignore -v …fig_c7_real_crop.png` → `.gitignore:60: *.png` |
| 指向已删除控制包 | `additive results/REVIEW.md:142` | `工程控制/RELEASE-02/change-claims/CONFORM-FIX-B-001-tolerance-relative.md` | `ls-files` 计数 0 + 删除提交 `ae0f1e63` |
| 指向不存在的旧根路径 | `absolute results/REVERSE_VERIFY_CANON.md:4`、`additive …:4`、`additive CRITERIA.md:3` | `reverse_verify/docs/…`、`reverse_verify/README.md §3.3` | `ls -d reverse_verify` → 不存在；`ls-files \| grep reverse_verify/README` → 0 |
| 非跟踪 run/ 件 | `absolute CANON:53`、`additive CANON:41`、`m16_scene:7,11,18,27,38`、`healpix README:36`、`healpix 报告:736,791` | `run/ROOT-CONSOLIDATION/…`、`run/RELEASE-02/…`、`run/EXP-07-POLAR/verify/check_report2.py` 等 | `git ls-files \| grep "^run/"` → 仅 `run/.gitkeep` |
| 机器绝对路径 | `ctest_evidence.md:27,31,32`、`healpix 报告:736`、`c7_realdata.json`（R4 value.fig） | `/workspace/Astro CS Database/…` | 非仓库路径；违 AGENTS §3「不写死服务器绝对路径」 |
| 行号不实（文件存在、所指内容不在该处） | `absolute CANON:20,24,25`（`star_detector.cpp:139-151`→实 214–230；`PHOTOMETRY.md:95`→实 :262；`p1_psf.json`→不存在）、`m16_scene:11`（`:151`→实 226）、`smooth-lambda:33,36,96`（`upm.cpp:543,669`↔对调、`module_adapters.cpp:4991-5040`→无关行）、`REPORT_paper:290,292`（`module_adapters.cpp:5994`→实 9573；`upm.cpp:96-108`→实 110-111/1041-1061）、`healpix 摘要:4`/`报告:72`（`DRIZZLE_GEOMETRY.md:236`→实 :304） | — | 逐条 `grep -n` / `sed -n` 于冻结基线核对（命令见详情块） |

**机械列校准（前言 §4 第 4 项：本批 16 份的人工复核结论）**
- `commit_sha` **误报 25 项**：EXP05 五条全为 testdata 帧名日期；EXP06 四条为 seed、两条为 E 值；REVIEW(B) 九条里八条为复算比值；additive README/REPORT 的 `20260923` 为 seed、`0501460`/`0703454`、`90296de1`… 混有 arXiv 号；healpix 报告四条为 arXiv 号 + nside 值。**真 commit** 仅 `88d3a1e2`（md5 前缀，亦非 commit）、`d77fd11f`、`48ceaa38`、`2f7e8b62`、`90296de1`、`8103fec5`（后四为外部仓库）。
- `sha256=True`（healpix 报告）**误报**：命中的是文件名 `results/SNAPSHOT.sha256`，而该文件是**已核 47/47 一致的有效快照**。
- `dangling_links` **误报 8 条**（花括号/通配写法且可解析：`c1..c7_*.py`、`{upm_probe,sky_probe}.cpp`、`results/*.json`、`evidence_{lit,code}.json`、`exp05_e{1..6}_*.json` 等）；**漏报 18 处以上**（以 `..`/相对开头的相对链接本已声明未判，另加全部 `file:line` 级不实与 `:236` 类行号错位——工具只看文件是否存在）。
- `dates` **漏报**：`EXP05_TABLES`（帧名内含 8 位日期）、`EXP-07-POLAR.md` 无日期判定正确 ✓；`REPORT_paper` 七处 dates 全命中 ✓ 无过无漏。
- `task_ids` **漏报**：`m16_scene/README.md` 未捕 `STACKN32-001`/`M16-SCENE-FIX-001`/`P9`；`REVIEW.md`(B) 未捕 `SCI-402`/`EXP-205`/`DOC-402`/`CLEAN-401`；healpix 三件未捕 `RC1/RC2a/RC3`、`T0–T29`、`REC-1`、`INNOV04-LAND-01`。⇒ 工具只吃 `EXP-/FIX-/RELEASE-` 前缀族，**结论：`task_ids` 列只能当"至少这么多"的下界用**。
- `upstream_pointer=True`（6 件）与人工判定一致 ✓；`unregistered_index=True`（16 件）一致 ✓。

## 4. 覆盖率自报

已审 **16 / 16**，清单每份均通读全文（行数：141、331、53、239、55、458、551、58、36、55、156、41、204、54、59、795；合计 3336 行）。**未逐行读名单：空**。最长件 `EXP-07-POLAR.md`（795 行）整篇读完；`REPORT_paper.md`（551 行）、`README.md`（458 行）整篇读完。

清单外为判定而读的文件（不作结论对象，仅取证）：`实验/absolute-snr/code/exp05/{make_tables.py,e5_gates.py,exp05_common.py}`、`code/exp06/{make_tables.py,e4_gates.py,exp06_common.py,e1_analytic.py}`、`results/exp05_e{1,3,6}*.json`、`results/exp06_e{1,3,4,5}*.json`、`实验/additive-sky-seamless/code/sci_c_common.py`、`results/c{1,2,3,4,5,6,7}*.json`、`实验/healpix-polar/results/*`（24 .out + 7 .csv + SNAPSHOT.sha256）、`docs/DOCUMENT_INDEX.yaml`、`docs/contracts/DUAL_LINE_CONTRACT.md`、`docs/science/PHOTOMETRY.md`、`docs/algorithms/DRIZZLE_GEOMETRY.md`、`testdata/README.md`、`testdata/index.json`、`.gitignore`、`lib/algorithms/**`（只读行核对）、`lib/infrastructure/scheduler/src/module_adapters.cpp`（只读行核对）。

**派单与清单差异（前言 §3：以清单为准）**：派单文字把 DB-17 描述为「16 份，`实验/absolute-snr/results/` 下的结果表与说明」，实际清单只有 **5 份**在 `实验/absolute-snr/results/`，另 **8 份**在 `实验/additive-sky-seamless/`（含 `code/reverse_verify/` 两处、`data/`、`docs/`）、**3 份**在 `实验/healpix-polar/`。已按清单全审，未代审清单外任何份。

## 5. 我证伪了任务书的哪个前提

固定命令：`git -c core.quotePath=false ls-files -- 'docs/*.md' | wc -l`（冻结基线 `c8f64e9a`）。

1. **证伪**：AUD-101 任务书范围段的「`docs/` 为重点（当前约 233 份、**30+ 子目录**）」中，**"30+ 子目录"不成立**——`docs/` 下一层子目录实为 **23**（命令：`git -c core.quotePath=false ls-files -- 'docs/*' | sed -n 's|^docs/\([^/]*\)/.*|\1|p' | sort -u | wc -l` → 23）。
2. **未证伪（反而精确命中）**：同一句的「约 233 份」= 上述固定命令的 **233**，一字不差 ⇒ 该前提在本基线下成立，不应作为错报登记。
3. **未证伪**：任务书步骤 3「标记同主题多份（如测光、SNR、UPM、接缝散落在多个目录的情况）」——本批实测支持该前提：UPM/λs 主题在本批内即散落 3 份（`docs/smooth-lambda.md`、`code/reverse_verify/smooth_lambda/CRITERIA.md`、`results/REVERSE_VERIFY_CANON.md`），SNR 主题散落 2 份 results + 3 份 canon，接缝主题跨 `实验/healpix-polar`（球面面积交叠接缝）与 `实验/additive-sky-seamless`（覆盖子集接缝）两单元且互不引用。
4. **新前提（任务书未列，须上呈）**：`docs/DOCUMENT_INDEX.yaml` 的扫描口径主动排除 `实验/`，与标准 01 §3「现存文档全部登记」互斥 ⇒ 本批 16/16「未登记」是**索引口径**后果而非文档失职；同时 `docs/contracts/DUAL_LINE_CONTRACT.md:19` 规定 `实验/**/REPORT_paper.md`「只增不改」，与标准 01 §1「冲突是缺陷须消除」/§4「只写现行设计」互斥（本批已证 `REPORT_paper.md` §4.8 的 FIX-3 代码事实被后续提交推翻却无法就地改正）。两条都属顶层合同层张力，按 AGENTS §10 上呈。

## 收工基线

收工（原文，命令 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`）：
```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```
与开工逐字相同 ⇒ 本批取证**未污染被审对象**：无新增/修改/删除项，未生成 `__pycache__`（未 import 任何仓库内 Python 文件；对 JSON 的读取一律用 `json.load` 读数据文件，不加载仓库代码），未运行构建/`ctest`/`run_checks.py`/任何 `eng/**` 脚本/端到端；`sha256sum -c` 与 `git merge-base --is-ancestor` 均为只读校验。HEAD 仍为 `c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`。

<!-- PROGRESS: 16/16 -->
