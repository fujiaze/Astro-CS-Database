# 通读 CR-21 — 球面面积交叠：可辨识性 / 积分面 / HiPS properties

- 基线 HEAD：`c8f64e9a`
- 批次：CR-21
- 派单前言：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 已完整读）
- 审查轴：S 支撑 / D 设计符合 / C 上下级一致 / R 运行时正确
- 只读纪律：零 git 写、零 `git config` 写、不跑构建/ctest/run_checks/任何 `eng/**`、不 import 仓库 Python、不写仓库内文件。

## 清单（6 个，独占）

1. `lib/algorithms/coverage/tests/execution_options_test.cpp` —— 已读：是（表 13 项，findings CR-21-01…05）
2. `lib/algorithms/coverage/hips_properties.cpp` —— 已读：是（表 21 项，findings CR-21-06…08）
3. `lib/algorithms/coverage/hips_properties.h` —— 已读：是（表 8 项，findings CR-21-09…11）
4. `lib/algorithms/coverage/include/astro/phase2/identifiability.h` —— 已读：是（表 13 项，findings CR-21-12…14）
5. `lib/algorithms/coverage/src/identifiability.cpp` —— 已读：是（表 22 项，findings CR-21-15…17）
6. `lib/algorithms/coverage/include/astro/phase2/integrate.h` —— 已读：是（表 12 项，findings CR-21-18…20）

定位：**无一条判到 S1**（三条候选均卡在"该退化形态有真实输入可达"这一跳，各条 `定级` 字段写明缺哪一跳）；findings 连续编号 CR-21-01…20，每节末带「不报的核过项＋检索式」；文末两节为「本批最高危三条」与「读不动／需实跑」。


---

## 1. `lib/algorithms/coverage/tests/execution_options_test.cpp`（87 行，已全文读完）

### 1.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :12-13 | `h > 0 ? … : 1` | `1` | 测试端 `hw()` 在 `hardware_concurrency()` 返回 0 时的兜底并行度 | 结构性不适用 | 镜像 `include/astro/phase2/execution_options.h:25`；不是科学量——它只是"至少起 1 个 worker"的下界，不进任何公式 | — | 高 |
| :17 | `"version": 1` | `1` | 夹具 JSON 声明的 stage2 配置方言版本号 | 待确认 | 检索式 `git grep -n "version" -- lib/algorithms/coverage/src/stage2_common.cpp` → **零命中**：`p2_stage2_parse_config` 根本不读 `version` 键 | 保守方向：解析器应校验并拒未知主版本。影响范围：版本演进时旧夹具/旧生产配置会被静默按新方言解析，合同测试无法发现 | 中 |
| :28 | `EXPECT_EQ(e.cpu_workers, hw())` | `hw()` | 把"缺省 worker 数 = 机器逻辑核数"钉成合同期望 | 待确认 | 与 `docs/architecture/execution_options_contract.md:50`「`0` = auto ⇒ 由 profile 与预算对象决定」和 AGENTS.md §6「不硬编码线程…由 benchmark 生成的 profile 决定」相冲突（见 CR-21-01） | 保守方向：缺省应取 Runtime 预算/`cpu_profile` 注入值而非 `hardware_concurrency()`。影响范围：本模块并行度来源、性能门 | 高 |
| :29 | `std::max(1, hw())` | `1` | 同一期望的下界写法 | 结构性不适用 | 同 :12-13，仅下界 | — | 高 |
| :30 | `EXPECT_GE(e.io_workers, 1)` | `1` | IO worker 下界 | 结构性不适用 | `execution_options.h:35` `return c > 1 ? c / 2 : 1;` | — | 高 |
| :33 | `EXPECT_EQ(e.memory_budget_bytes, 0u)` | `0` | 内存预算"未设"哨兵 | 待确认 | `execution_options.h:20` 注释「0 => 由配置 `memory_limit_mb` 决定」；但该解析在代码里**零实现**（见 CR-21-05） | 保守方向：哨兵 0 应解析为 `integration.memory_limit_mb × 1048576`。影响范围：异步队列/写缓冲的内存上限完全不受本字段约束 | 高 |
| :40 | `cpu_workers / io_workers / memory_budget_bytes` 覆盖值 | `4` / `2` / `65536` | 显式覆盖夹具内容 | 结构性不适用 | 前言 §4「测试期望值与夹具内容差」不算缺陷；纯 round-trip 探针 | — | 高 |
| :43-:47 | 同上四项的期望回读 | `4`/`2`/`false`/`65536` | 断言写入即读出 | 结构性不适用 | 同上（夹具镜像） | — | 高 |
| :54 | `EXPECT_EQ(cfg.exec.cpu_workers, hw())` | `hw()` | 缺省块缺失时 worker 数仍等于机器核数 | 待确认 | 依赖 `stage2_common.h:34` `exec = default_execution_options()`；与 :28 同一冲突（CR-21-01） | 同 :28 | 高 |
| :62 | `{"cpu_workers", 5000}` | `5000` | 越界拒绝探针 | 结构性不适用 | 镜像 `stage2_common.cpp:486` 的上界 `1024`（该常数不在本批清单，行号已给出） | — | 高 |
| :78-:81 | `cpu_workers=6` ⇒ `effective_io_workers==3` | `6` / `3` | 显式 cpu + io 自动值 | 公式导出 | 式：`io = cpu > 1 ? cpu/2 : 1`（`execution_options.h:33-36`）；测试按式算，代码亦按式 ⇒ 本行合规 | — | 高 |
| :82-:83 | `e.io_workers = 0` ⇒ 再断言 `3` | `0` / `3` | 注释写"auto"，实际与 :81 同一状态 | 待确认 | 见 CR-21-04：`execution_options.h:33` 的 `io_workers > 0` 短路分支在本文件内从未执行 | 保守方向：显式值应改测非零。影响范围：本门对"显式 io 被忽略"这一回归失去抵抗力 | 高 |
| :84-:86 | `cpu_workers=0` ⇒ `hw()`、`hw()/2`、`1` | `0` / `1` | auto 两条分支 | 待确认 | `0=auto` 的语义在本模块下游被 reinterpret 成"1=串行"（CR-21-03）；下界 `1` 本身结构性不适用 | 保守方向：全链对 0 只允许一种生效值 | 高 |

被合并项声明：`1`（下界）出现在 :13、:29、:30、:35(镜像)、:86 五处，均已逐位点列出；`hw()` 出现在 :28、:34、:54、:85、:86 五处，同样逐位点列出，未跨位点合并隐藏。

### 1.2 findings

### CR-21-01 · 本文件把"worker 缺省 = `hardware_concurrency()`"钉成合同期望，而它自己点名的合同说缺省由 benchmark profile 决定
- 轴：C（主轴）/ D
- 位点：`lib/algorithms/coverage/tests/execution_options_test.cpp:28` `EXPECT_EQ(e.cpu_workers, hw());` 与 `:54` `EXPECT_EQ(cfg.exec.cpu_workers, hw());`
- 上位依据：`docs/architecture/execution_options_contract.md:50-51`「约束：`cpu_workers/io_workers` 属于 [0,1024]（`0` = auto ⇒ **由 profile 与预算对象决定**）」；AGENTS.md §6 硬禁令「不硬编码线程/ISA/block：由 `benchmark` 生成的 profile 决定」。检索式 `git grep -n "cpu_profile\|benchmark" -- lib/algorithms/coverage` → **零命中**（整个 coverage 模块不读 profile）。
- 现状：`execution_options.h:23-26` `default_cpu_workers()` 返回 `std::thread::hardware_concurrency()`（为 0 时取 1），`:39-47 default_execution_options()` 用它填 `cpu_workers`；测试断言"这就是合同缺省"。文档 `:71-72` 已把该行为登记为偏差：「代码侧现状（登记）：`execution_options.h` 仍含 `gpu_route`（:18/:43）与 `hardware_concurrency` 默认（:16/:24/:38）；文档与合同侧不承认这些键」。
- 差在哪：测试没有把已登记的偏差标成待修（例如 `EXPECT_EQ` 换成对预算注入的断言或加 `// KNOWN-DEV`），而是用断言把偏差**升级为期望值**——同一"auto"语义在合同侧权威是 profile，在测试侧权威是 CPU 核数，且后者与前者相反。
- 后果：(a) 任何按合同把 auto 改为读 profile/Runtime 预算的修复都会让 `phase2_execution_options.DefaultIsHardwareConcurrency` 与 `ParseWithoutExecutionBlockDefaults` 转红 ⇒ 测试门锁住合同修正案；(b) 对外主张"线程数由 benchmark profile 决定"（AGENTS §6）在本模块零实现，而绿灯清单反而为它提供了反证。
- 定级：S2 —— 生产 `acsd` CLI 不读 stage2.json（见 CR-21-02 的调用者检索），所以不会直接产出错值；缺的是"生产入口读该缺省并据此起线程"这一跳。
- 怎么算修好：两条路只能选一条并同批改：① 走负责人裁决，把 AGENTS §6 的例外与"缺省=hardware_concurrency"写进合同文档 §配置（那就要同时删掉 :71-72 的登记条目）；② `default_execution_options()` 的 auto 分支改为取调用方注入的 Runtime 预算（形如 `astrocs_host_state_set_budget_v1` 的 `max_workers`，`lib/infrastructure/cli/commands.cpp:49`），并把 :28/:34/:54/:85/:86 五处期望从 `hw()` 改成夹具注入的固定预算值（顺带消掉本测试的机器相关性——今天它在 4 核与 64 核机上断言不同）。
- 置信：CONFIRMED（三份条文 + 两条零命中检索式都读到原文）

### CR-21-02 · CON-002 的"生产配置 schema"指针落空：本文件测的 `execution` 块被它文档点名的生产 schema 机器拒绝
- 轴：C（主轴）/ D
- 位点：`execution_options_test.cpp:40` `j["execution"] = {{"cpu_workers", 4}, {"io_workers", 2}, {"gpu_route", "cpu"}, {"deterministic", false}, {"memory_budget_bytes", 65536}};`；语法夹具 `:17-23` `{"version":1,"inputs":{"hips":["a","b"]},"model":{},"integration":{},"output":{"hips":"out"}}`
- 上位依据：`docs/architecture/execution_options_contract.md:52-53`「违反即 `p2_stage2_parse_config` 返回 false（带错误信息）。schema 见 `eng/contracts/schemas/phase_config_*.schema.json` 的 `config` 段（**生产配置 schema**）」；`eng/contracts/schemas/phase_config_mosaic.schema.json:31`「硬件字段禁令：ISA/workers/block/CPU 型号等一律不得出现在 phase_config…**块与 drizzle/wcs 两级用 additionalProperties:false 做机器拒绝**」；同文件 `:5`「只含科学参数、输入路径、output_dir 与算法选择；不含任何硬件字段（ISA/workers/block/CPU 型号）」。
- 现状：`git grep -n "\"execution\"\|cpu_workers\|io_workers\|memory_budget" -- eng/contracts` → **零命中**；`phase_config_mosaic.schema.json` 顶层 properties = `blocks / algorithm_rejection_method / coverage_index / hips_paths / output_dir / reject / snr_path / upm`，`$defs.mosaic_config` 与 `$defs.mosaic_block` 均 `additionalProperties: false`。
- 差在哪：合同文档点名的那份"生产配置 schema"既不含 `execution` 段，还以 closed object 明确**拒绝** workers 类键；本文件断言的却是 `execution.cpu_workers` 被接受并生效，且其 JSON 语法（`version/inputs/model/integration/output`）与 phase_config_* 的任何一层都不同构 ⇒ "合同测试"测的是一份没有合同锚的私有方言。
- 后果：`p2_stage2_parse_config` 在 lib 内的非测试调用者只有 dev 工具 `lib/algorithms/coverage/tools/stage2.cpp:159`（检索式 `git grep -n "p2_stage2_parse_config" -- lib | grep -v "tests/\|stage2_common\|module.yaml\|README"` → 仅 tools 与注释命中）；acsd 生产的 worker 预算另有来源（`commands.cpp:49 astrocs_host_state_set_budget_v1(…, max_workers, …)`，注释 `lib/algorithms/upm/README.md:108`「唯一来源，p2_session.cpp:155/:195 传 `host->budget.max_workers`」）。于是本门全绿**不能支撑**"全局 worker 预算合同在生产上成立"这一对外主张，反而是把读者引向一份会拒该键的 schema。
- 定级：S2 —— 我给出的是"生产 CLI → `execution` 块"这一跳**不存在**的证据，所以不能判 S1（无产物错值路径）。
- 怎么算修好：① 把 `docs/architecture/execution_options_contract.md:52-53` 的 schema 指针改成真实存在的 stage2 方言 schema（新建并登记进 `eng/contracts/schemas/`），或明确改写为"仅 dev 工具面，生产不读"；② 本文件头注释 `:1` 的 `CON-002 global worker budget contract tests` 与合同文档 §测试（:74-75）同步改口径；③ 补一条真正的合同测试：`execution`/`cpu_workers` 出现在 phase_config_mosaic 时被 `additionalProperties:false` 拒（现在能红能绿的门缺这一侧，光改 schema 不会有人发现）。
- 置信：CONFIRMED

### CR-21-03 · `0 = auto` 只在测试与 `effective_*` 里成立，跨到下游 config 时同一个 0 变成"1 = 串行"，本门看不出该分歧
- 轴：R（主轴）/ C
- 位点：`execution_options_test.cpp:84-85` `e.cpu_workers = 0;   // auto` + `EXPECT_EQ(effective_cpu_workers(e), hw());`
- 上位依据：`include/astro/phase2/execution_options.h:16` `int cpu_workers = 0; // 0 => max(1, hardware_concurrency)`；`docs/architecture/execution_options_contract.md:50`「`0` = auto」；实现自身也承认 0 合法——`src/stage2_common.cpp:486` 报错串「cpu_workers 必须在 0..1024 (**0=auto**)」。
- 现状：`src/stage2_common.cpp:548` `mcfg.cpu_workers = cfg.exec.cpu_workers;   // CON-005: UPM build 并行 worker 预算(CON-002 唯一来源)` 传的是**裸字段**而非 `effective_cpu_workers()`；下游 `src/upm.cpp:614` `const int cworkers = (cfg.cpu_workers > 0) ? cfg.cpu_workers : 1;`。同一形态出现在 `tools/stage2.cpp:281` `sccfg.cpu_workers = cfg.exec.cpu_workers;` → `src/sampler.cpp:927` `const int workers = (cfg.cpu_workers > 0) ? cfg.cpu_workers : 1;`。
- 差在哪：对同一个键值 0，`effective_cpu_workers()`（测试断言的那个）解析为机器核数，下游 config 解析为 1；两套生效值共存且都自称来自"CON-002 唯一来源"。注释 `upm.cpp:608`「无 hardware_concurrency; 1 => 串行 reference」说明下游是刻意不同，但那正是与 :16 头注释的冲突点。
- 后果：配置写 `"cpu_workers": 0`（合同广告为 auto 的合法值）时，UPM 构建与采样器实际**单线程**运行；只有墙钟时间/性能门与"资源利用率"主张受影响（数值产物不受，`tests/ivar_wiring_test.cpp:324` 已测 cpu_workers=1/=2 结果一致）。本文件全绿且从不比较"raw 字段 vs effective 值"，所以这条分歧在本门内不可见。
- 定级：S2 —— 触发链完整（JSON → `stage2_common.cpp:486` → `:548` → `upm.cpp:614`），但入口是 dev 工具（CR-21-02），生产 acsd 面走 host budget，故不构成对外产物错值。
- 怎么算修好：`stage2_common.cpp:548` 与 `tools/stage2.cpp:281` 改传 `effective_cpu_workers(cfg.exec)`（或让下游把 0 也解析为 auto，二选一但不要两套）；同批在本文件加 `EXPECT_EQ(p2_stage2_make_upm_cfg(cfg, 0, "x").cpu_workers, effective_cpu_workers(cfg.exec));`——**这条今天会红**，正是它该抓的；注意若同时采纳 CR-21-01 的方案 ①（缺省改 profile 注入），两处必须一起改，否则修好一条另一条判红。
- 置信：CONFIRMED

### CR-21-04 · `EffectiveCounters` 的两条"分支"是同一状态重复断言，`io_workers > 0` 短路分支在本门内不可达
- 轴：R
- 位点：`execution_options_test.cpp:81-83` `EXPECT_EQ(effective_io_workers(e), 3);` / `e.io_workers = 0;    // auto` / `EXPECT_EQ(effective_io_workers(e), 3);`
- 上位依据：`docs/architecture/execution_options_contract.md:74-75`（§测试）把「effective 计数器」列为本文件的职责；`execution_options.h:32-36` 有两条分支。
- 现状：`ExecutionOptions e;`（:78）后 `io_workers` 已是结构体默认 0，:81 已在该状态下取 `6/2=3`；:82 再赋 0 是**无操作**，:83 是重复断言。注释 `// explicit`（:79，指 cpu_workers）与 `// auto`（:82）声称覆盖两条分支，实际 `execution_options.h:33 if (e.io_workers > 0) return e.io_workers;` 从未在本文件执行。
- 差在哪：把该行短路删掉（改成恒返回 `c/2`，即"显式 io 被静默忽略"），本 TEST 仍全绿。缓解事实：`tests/routing_test.cpp:45 EXPECT_EQ(effective_io_workers(cfg.exec), 2)` 经 parse 路径覆盖了该分支，所以整个测试面未失守，只是本文件自称的覆盖面与注释不符。
- 后果：本门对"显式 io_workers 被忽略"失去独立抵抗力；单跑 `phase2_execution_options` 套件（CI 支持按前缀选择，`CMakeLists.txt:186-187 TEST_PREFIX`）时是假绿面。
- 定级：S3（有缓解证据，无产物影响）
- 怎么算修好：:82 改为 `e.io_workers = 5;`、:83 改为 `EXPECT_EQ(effective_io_workers(e), 5);`，再另起一行 `e.io_workers = 0;` 断言 3——三行覆盖两条分支。
- 置信：CONFIRMED

### CR-21-05 · `memory_budget_bytes` 是零消费者字段，本文件只测"存进去读得出来"，把 CON-002 的内存轴写成空壳
- 轴：D（主轴）/ C
- 位点：`execution_options_test.cpp:47` `EXPECT_EQ(cfg.exec.memory_budget_bytes, 65536u);` 与 `:33` `EXPECT_EQ(e.memory_budget_bytes, 0u);`
- 上位依据：`execution_options.h:20` `std::uint64_t memory_budget_bytes = 0; // 0 => 由配置 memory_limit_mb 决定`；`docs/architecture/execution_options_contract.md:63`「异步队列容量由 `memory_budget_bytes` 推导（见 CON-008 异步 I/O 合同）」；`docs/contracts/DATA_SEMANTICS.md:1194`「memory_limit_mb | 24576（:51）| MB | 执行内存预算（CON-002 全局执行预算域）」。
- 现状：`exec.memory_budget_bytes` 在 lib 内**只被写、从未被读**（检索式 `git grep -n "exec\.memory_budget_bytes" -- lib` → 命中 `stage2_common.cpp:502` 赋值与 tests 断言两处，无消费者）；`bounded_queue_capacity`（`async_io.h:27-32`，即 CON-008 声称的推导点）**零生产调用者**（`git grep -n "bounded_queue_capacity" -- lib` → 只有 `async_io.h` 定义与 `tests/async_io_test.cpp:27-30`）；而真正的内存预算消费走的是另一个字段：`docs/algorithms/PHASE2_MOSAIC_WRITE.md:103`「`memory_limit_bytes=cfg.memory_limit_mb :783、safety_factor=0.75 :784`」。
- 差在哪：合同说内存预算的唯一入口是 ExecutionOptions，代码里该入口写进去就没了，实际生效的是 `integration.memory_limit_mb` 直连写通道；本文件用"存进去读得出来"冒充"预算被消费"，所以设计点名的能力（预算→队列容量/写缓冲上限）只有接口没有实现，而测试让它看起来已闭合。
- 后果：`execution.memory_budget_bytes` 设成任意荒谬值（1 字节、2^63-1）都不会被拒也不会影响任何行为——`stage2_common.cpp:501-502` 对它是**无范围守卫**直接赋值（cpu/io 各有 0..1024 守卫，本键没有）；对外"CON-002 全局执行预算含内存"的主张与 AGENTS §3 的内存看门狗前提失真。
- 定级：S3 —— 后果落在"对外主张"层面，产物数值不受影响；写不出错值触发路径故不判 S2 以上。
- 怎么算修好：① 要么让 CON-008 真消费它（有界队列/写缓冲按 `bounded_queue_capacity(exec.memory_budget_bytes, item_bytes)` 计算，并把 `memory_limit_mb` 折进同一入口），② 要么删掉该键并在合同文档 §配置与 `DATA_SEMANTICS.md:1194` 同批标退役；两方案都需在本文件补一条"预算被读到"的断言（例如注入预算后断言队列容量变化），否则仍是空壳；补守卫时同步加 `ParseRejectsInvalidMemoryBudget` 与 `ParseRejectsInvalidIoWorkers`（:491 已实现 0..1024 守卫但本文件无对应负例）。
- 置信：CONFIRMED

---

## 2. `lib/algorithms/coverage/hips_properties.cpp`（177 行，已全文读完）

### 2.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :21,:22 | `trim` 字符集 | `' ' ' ' '\t' '\r'` | properties 行首尾空白剥离（含 CRLF 的 `\r`） | 结构性不适用 | 字符类枚举，非科学量；`\r` 是 CRLF 换行的残留位 | — | 高 |
| :29 | `i = (v[0]=='-'‖'+') ? 1 : 0` | `1` | 符号位偏移量 | 结构性不适用 | 位移量，不进科学语义 | — | 高 |
| :33 | `acc * 10 + (v[i]-'0')` | `10` | 十进制串→整数 | 结构性不适用 | 进制定义常数（前言 §4「单位换算的定义常数」同类） | — | 高 |
| :34 | `if (acc > 100000) return false;` | `100000` | 手写整数解析的防溢出/超界上界 | 待确认 | 全仓检索式 `git grep -n "100000" -- lib/algorithms/coverage` 仅此一处，无文档条文；注释只写「防溢出/超界」，未给该值来源（任何 ≥ 真实上界的数都能用，20/29/1024/2^31 亦然） | 保守方向：应收紧到与 order 合同上界同量级（如 1024）并复用同一命名常数，避免"实际允许到 10 万、值域却只到 20"的两套数。影响范围：仅错误消息可读性（超限被报成 "not integer"），方向偏红不偏绿 | 高 |
| :44 | `p.empty()` | — | 空路径拒绝 | 结构性不适用 | 布尔判据 | — | 高 |
| :45 | `find('\0')` | — | NUL 注入拒绝 | 结构性不适用 | 安全词表 | — | 高 |
| :46 | `find('\\')` | — | 反斜杠/Windows 盘符拒绝 | 结构性不适用 | 安全词表（`eng/tests/unit/p3_assembly_test.cpp:35` 记 Windows TEMP 反斜杠会被本函数拒，属已知约束） | — | 高 |
| :47 | `find("//")` | — | 空段拒绝 | 结构性不适用 | 安全词表 | — | 高 |
| :50-:56 | 段切分 `start = end + 1` | `1` | 分隔符位移 | 结构性不适用 | 位移量 | — | 高 |
| :54 | `seg == ".."` | — | 上溯段拒绝 | 结构性不适用 | 安全词表 | — | 高 |
| :117 | `order < kMinOrder ‖ order > kMaxOrder` | `[0, 20]` | `hips_order` 值域 | **待确认** | 常数声明处 `hips_properties.h:23` 的锚经核不成立，且与 I/O 层合同 `lib/infrastructure/aio/io/include/astrocs/io/hips_input_v1.h:31 ACS_HIPS_ORDER_MAX 29`（注释「order K: 0..29 (NSIDE ≤ 2^38 安全域)」）取不同上界 ⇒ 见 CR-21-09 | 保守方向：两处应统一到同一命名常数并给出可导出的界（64 位 ipix / leaf_nside 溢出界）。影响范围：order 21..29 的产品 P3 门拒、aio 层可开 | 高 |
| :120 | `out->tile_width != kHipsTileWidth` | `512` | tile 边长必须恒 512 | 结构性不适用 | 合同冻结：`docs/contracts/DATA_ARTIFACTS.md:39`「512×512/leaf」、`docs/contracts/DATA_SEMANTICS.md:435`「恒 512（≠512 拒绝 :402；tile_order=leaf_order−9）」、`:1092`「必须 =512」；它不是科学量——唯一进入公式的形式是 `9 = log2 512` 的索引换算位 | 选择理由（512 而非 256/1024）在三份登记面都只写"恒 512"未给导出，但改动会破坏产品字节与 `hips_output_store.py` 布局，属负责人裁决域 | 高 |
| :122 | `tile_format.find("fits") == npos` | — | 声明格式须含 "fits" | 结构性不适用 | 词表匹配。附注（不单独成 finding）：子串 + 大小写敏感 ⇒ `"notfits"`/`"FITS"` 分别误放行/误拒；本仓写盘侧固定写小写 `fits`（`docs/algorithms/HIPS_WRITER.md:206`），方向偏红 | — | 中 |
| :124,:125 | `frame != "equatorial" && frame != "icrs"` | 2 值域 | 坐标系键值域 | 待确认 | `docs/algorithms/HIPS_WRITER.md:206`「hips_frame="equatorial"（IVOA REC-HIPS-1.0 §4.4.1 标准值域 {equatorial, galactic, ecliptic}…）」与 `docs/algorithms/PHASE2_COVERAGE.md:67-68`「同为**产品约定**：IVOA HiPS 1.0 §4.4.1 的标准值域是 {equatorial, galactic, ecliptic}，本模块额外接受 icrs」 | 该放宽**已在 P2 侧登记**（故不报成本文件的缺陷，见"不报的核过项"）；残留待确认项：`icrs` 与 `equatorial` 在本文件里走两条不同字面量但**下游行为零区别**，且从不读 `ext_equinox`/`coosys`（检索式 `git grep -rn "ext_equinox\|coosys" -- docs lib` → 零命中）⇒ 非 ICRS 的 equatorial 产品无从判别 | 高 |
| :126,:127 | `dataproduct_type != "image"` | 1 值域 | 产品类型键 | 结构性不适用 | 词表 token（`docs/contracts/DATA_ARTIFACTS.md:80` 登记 `dataproduct_type=image`），不进科学公式 | — | 高 |
| :141 | `"Norder" + to_string(order)` | — | 叶级目录名 | 结构性不适用 | 布局约定：`lib/infrastructure/aio/io/hips_core.c:14`「tile 路径 Norder{K}/Dir{D}/Npix{N}.fits」 | — | 高 |
| :154,:159 | `kind != 1` / `tkind != 0` | `1` / `0` | 只下钻目录 / 只看常规文件 | 结构性不适用 | 枚举基数，方向已核：`lib/infrastructure/aio/src/aio_atomic_file.h:392`「kind: 0 = 常规文件; 1 = 目录; 2 = 其他（符号链接/fifo/设备等，不跟随）」⇒ 两处判据与语义一致；符号链接 tile（kind=2）被跳过 = 偏红 | — | 高 |
| :155 | `base_name(subdir).rfind("Dir", 0) != 0` | — | Dir 分组段前缀 | 结构性不适用 | 同 `hips_core.c:14` 布局；`lib/infrastructure/aio/io/hips_output_store.py:601` 用同一 `startswith("Dir")` 判据 | — | 高 |
| :161 | `t.size() > 5` | `5` | "= strlen('.fits')"，防前后缀重叠的短名 | 待确认 | 见 CR-21-08：该判据不校验 `Npix` 与 `.fits` 之间的 ipix 索引是否存在/为数字 | 保守方向：要求 `Npix<数字>.fits`。影响范围：`Npix.fits`/`Npixabc.fits` 之类的垃圾文件即可让"至少 1 tile"探测转绿 | 高 |
| :162 | `t.compare(t.size()-5, 5, ".fits")` | `5` | 后缀长度 | 结构性不适用 | 同上位移量 | — | 高 |
| :164,:169 | `return 1` / `return found ? 1 : 0` | `1` | 枚举提前中止哨兵 | 结构性不适用 | 回调协议（`aio_atomic_file.h:395`「fn 返回非 0 ⇒ 立即中止」） | — | 高 |
| :172 | `if (rc != 0) return fail("order directory unreadable")` | `0` | 枚举失败哨兵 | 结构性不适用 | 回调协议；注：:156 对内层 `for_each_child` 的返回值 `(void)` 丢弃 ⇒ 单个 Dir 不可读时静默跳过，最终报成 "no Npix*.fits tile"（消息错位，方向仍偏红） | — | 中 |

被合并项声明：本文件所有路径/词表字面量（`properties` :135、`Npix` :161、`Norder` :141、`Dir` :155、`.fits` :162）与错误串中的数字文本（:118 `[0,20]`、:121 `must be 512`）均由上表对应行覆盖，无未列数值。

### 2.2 findings

### CR-21-06 · `hips_properties_parse` 不读 `hips_ordering`/`hips_version`，与自身「无 silent default」条款矛盾；RING 错读仅剩"tile 头恰有 ORDERING 卡"这一道外部兜底
- 轴：R（主轴）/ C / D
- 位点：`lib/algorithms/coverage/hips_properties.cpp:107` `// 未知键: 保留忽略(HiPS 允许扩展键), 但值仍不得含 NUL`（:79-106 的键表里根本没有 `hips_ordering` 与 `hips_version` 两支）
- 上位依据：
  - 本文件头 `hips_properties.cpp:2` 原则条文「无 silent default —— 必需键缺失/值非法/越界一律拒绝」与 `:28`「必需键缺失/值非法 → false + err。无 silent default」；
  - 同一 properties 方言的必需键集在别处更宽：`docs/contracts/DATA_ARTIFACTS.md:80`「`hips_order/hips_tile_width/hips_frame/dataproduct_type=image`；NESTED」；I/O 合同硬要求 `lib/infrastructure/aio/io/hips_core.c:209`「properties 缺 hips_version」/`:213`「hips_version='%s' 不支持 (合同接受 1.4)」与 `lib/infrastructure/aio/src/hips/aio_hips_reader.cpp:439-440`「properties 缺失 hips_version」；
  - P2 侧对同一键的处置定性：`lib/algorithms/coverage/src/coverage.cpp:122-126`「B2-A8: 读并断言 hips_ordering。本模块的 union/int 父聚合（t >> 2s）与下游 NESTED 消费只在 NESTED 语义下成立；RING 输入必须 fail-closed（**旧实现全仓 0 命中 hips_ordering，AIO reader 直接按 NESTED 解释，使 RING 帧被静默错读**）」（同文 `docs/algorithms/PHASE2_COVERAGE.md:327` 把「(d) `hips_ordering` 非 NESTED 拒绝」列为合同条款）。
- 现状：本文件对 `hips_ordering` 与 `hips_version` 零读取（走 :107 的"未知键忽略"），`hips_product_validate` 因此可返回 true；几何解释随后在 `lib/algorithms/resample/p3_resample.cpp:190-199` 无条件按 NESTED 做：`astrocs::healpix::leaf_to_tile_nest(...)` + `nested_local_to_fits_index(local, 9, kTileWidth)`，且 `p3_resample.cpp:193` 只在 tile 缺失时 `return false`。
- 差在哪：校验项少一项 ⇒ `hips_product_validate` 更容易返回 true ⇒ 更容易绿（不等式方向：门是"键存在且合法才拒"，缺失/未知一律放行）。P2 侧已把同一形态定成缺陷并修（B2-A8），P3 侧同一方言仍是漏项——不是"两种适用域"，两侧读的是同一 `properties` 文件族。
- 后果：一个声明 `hips_ordering = RING`（或 `hips_version` 缺失/为 2.0）的外部 HiPS 输入经 `acsd export` 会被 `hips_product_validate` 判为合格产品。**本批取证同时找到两道下游兜底，故后果比初判窄**：① 版本——`p3_resample.cpp:279` 随后调 `aio_hips_open`，而 `lib/infrastructure/aio/io/hips_core.c:209` 「properties 缺 hips_version」/`:213`「hips_version='%s' 不支持 (合同接受 1.4)」会拒 ⇒ 实际差异只是错误分类（`P3_RS_PARAM` 而非 `P3_RS_IO`）与"门说合格、读者说不合格"的口径分裂；② 序——`hips_core.c:660-669` 对**tile FITS 头**做「头卡一致性 (存在则校验): PIXTYPE/ORDERING/…」「`ORDERING` 非 NESTED ⇒ `tile ORDERING='%s' 非 NESTED (本合同只支持 NESTED)`」。**残留的真实漏洞**是该校验为"存在则校验"：一个按 RING 编号但 tile 头未写 `ORDERING` 卡的输入，会一路通过 properties 门（本文件零读取）+ 头卡门（无卡不查），随后被 `p3_resample.cpp:190-199` 无条件按 NESTED 解释 ⇒ 导出产品每个像素取自错误天区且无一处报错。对外主张失真：`hips_properties.cpp:2` 「无 silent default」与 `DATA_ARTIFACTS.md:39`「NESTED(frame=ICRS)」。
- 定级：**S2** —— 逐跳路径已写全（`module_adapters.cpp:14303 p3_sampler_open_ex` → `p3_resample.cpp:276 hips_product_validate` → 本文件 `:61` → `p3_resample.cpp:190/198` NESTED → `module_adapters.cpp:14353 p3_sample_nearest_ex`），但**判 S1 所需的"该形态可达"未证实**：缺的一跳是"存在一个 `hips_ordering=RING` 且 tile 头无 `ORDERING` 卡的实际输入"（本仓在库数据集与自产产品均非此形态；`lib/algorithms/coverage/tools/controlled_rejection_truth.py:48-51` 的夹具只写 version/frame）。故按前言硬门槛降为 S2。
- 怎么算修好：① 在 :79-106 键表加 `hips_version`（必需，值域按合同 =1.4）与 `hips_ordering`（缺失或 ≠ `NESTED` 一律 fail-closed——注意 `coverage.cpp:129` 现在只在**键存在**时拒，属半修，本文件若照抄会把同一漏项复制过来，必须按"缺失即拒"写，并与 P2 同批改）；② 同步在 `docs/contracts/DATA_ARTIFACTS.md:80` 的必需键清单里登记 `hips_version`/`hips_ordering`；③ 补负例：构造 `hips_ordering=RING` 与 键缺失 两个夹具，断言 `hips_product_validate` 返回 false 且 `err` 载因（现在注入这两项都判绿，正是能红能绿的缺口）。
- 置信：CONFIRMED（两侧键集、下游 NESTED 调用、生产调用者三处均逐字读到）

### CR-21-07 · 头注释承诺的"重复键 = 错误"只覆盖 5 个必需键，BUNIT 值域零校验 ⇒ 同一字段可被后写行静默覆盖成 `Jy/beam`
- 轴：C（主轴）/ R
- 位点：`hips_properties.cpp:104-106` `} else if (key == "BUNIT" || key == "bunit") { out->bunit = val;   // 可缺; 缺省 ADU(SCI 合同), 绝不 Jy/beam 默认 }`
- 上位依据：本文件 `:78` 注释「重复键 = 错误(无 silent override)」与 `:27` 函数文档「重复键=错误；未知键保留忽略」；单位侧权威 `lib/algorithms/resample/p3_resample.h:38-39`「BUNIT 来源输入合同, 无 BUNIT 键时缺省 canonical "ADU/sr"（DATA_SEMANTICS §31.1a FZ-UNIT-SIGNAL-SB；**绝不裸 ADU、绝不 Jy/beam 默认**）」；对照实现已存在的单位等值校验：`lib/infrastructure/aio/io/fits_core.c:847`「unit mismatch: expect BUNIT='%s' file BUNIT='%s'」。
- 现状：`has_order/has_width/has_fmt/has_frame/has_dpt` 五个哨兵（:66-67）只服务于 5 个必需键（:80,:84,:88,:93,:97 各 `return fail("duplicate key …")`）；`creator_did`(:100)、`obs_title`(:102)、`BUNIT|bunit`(:104) 三分支**直接赋值无哨兵**，后写行覆盖先写行。且 `val` 从不与词表比对，`out->bunit = "Jy/beam"` 被照单收下。
- 差在哪：文档说"重复键=错误"，代码对 3/8 个已知键不执行该规则；注释自称"绝不 Jy/beam 默认"，代码既不校验声明值、也无法区分 `BUNIT` 与 `bunit` 两种拼写构成的重复声明（:104 把二者并成一支 ⇒ 一行 `BUNIT = ADU/sr` + 一行 `bunit = Jy/beam` 得到的是"最后一行赢"）。
- 后果：`p3_resample.cpp:293 if (out_bunit) *out_bunit = p.bunit.empty() ? std::string("ADU/sr") : p.bunit;` —— 非空即直传，于是被覆盖成的 `Jy/beam`（每波束流量密度，与面亮度量纲链不同族）随 `module_adapters.cpp:13995` 的 `bunit` 出参写进导出产品的 BUNIT 卡；而重采样本身只是凸组合（`p3_resample.cpp:291`「采样值 = 输入 HiPS tile 值的凸组合 ⇒ 与输入同单位」），不含任何单位换算 ⇒ 产品数值与标注单位脱钩，测光/面积换算链断裂。FITS 侧同族错误有 `fits_core.c:847` 兜住，HiPS properties 侧零防线。
- 定级：S2 —— 触发需要输入 properties 自带矛盾/非法 BUNIT 行（外部输入可达，但我未实测某个真实在库产品含此形态），且错的是**标注**而非逐像素数值，故不上 S1。
- 怎么算修好：① 给 `creator_did`/`obs_title`/`BUNIT` 各加 `has_*` 哨兵，与 :80 同构（重复即 `fail("duplicate key …")`），并在 :27 文档行注明"含可选键"；② 对 `bunit` 加词表校验（至少：拒绝 `Jy/beam`、拒绝空值串，非空时须属 canonical ADU 幂 × sr 幂词表——`lib/algorithms/resample/p3_rsmp.h:62` 已给出该词表口径）；③ 大小写拼写二选一并删除另一支（保留 `BUNIT` 即 FITS 卡名口径）；④ 同批补负例：`BUNIT` 重复、`bunit=Jy/beam` 两个夹具必须转红。
- 置信：CONFIRMED

### CR-21-08 · 「缺 tile 探测 = 至少 1 tile 可读」的"可读"是名义判据：不打开文件、不校验索引，0 字节残tile 即判合格
- 轴：R（主轴）/ D
- 位点：`hips_properties.cpp:161-162` `if (t.rfind("Npix", 0) == 0 && t.size() > 5 && t.compare(t.size() - 5, 5, ".fits") == 0) { found = true;`
- 上位依据：本文件 `:3`「缺 tile 探测 = 声明叶级至少 1 tile **可读**路径」、`:36`「4) 缺 tile 探测: `Norder<order>/Dir*` 下至少 1 个 `Npix*.fits` 存在」、`:2` 原则「无 silent default」；对照合同 `docs/contracts/DATA_ARTIFACTS.md:39`「缺 tile=无覆盖非错误」（即"缺"是有语义的状态，不该与"损坏"混同）。
- 现状：判据只有名字的前缀/长度/后缀三项；不 `stat` 大小、不调 `aio_hips_open`/FITS 头读取、不校验 `Npix` 与 `.fits` 之间是否为合法数字索引（按 `hips_core.c:14` 的布局约定，那里必须是 ipix 编号）。命中即 `found = true` 并中止枚举（:164）。
- 差在哪：文件存在 ⇒ 判"可读"，但代码从未尝试读；把"至少 1 个 tile 存在"写成"至少 1 个 tile 可读"是判据强度被注释夸大（一项校验被省略只会让门更容易绿，方向明确）。
- 后果：AGENTS.md §3 记录的真实场景——盘满导致产品半截（`OSError: [Errno 28]`、产物写到一半）——留下的 0 字节或截断 `Npix0.fits` 会让 `hips_product_validate` 返回 true；随后 `p3_resample.cpp:193 if (!tile) return false;   // 缺 tile` 把"取不到 tile"统一映射为 `p3_sample_*` 的 `*value = nanf(""); *coverage = 0; return P3_RS_OK;`（:309）⇒ 一份被截断的输入被导出成"该天区无覆盖"的合法产品，而不是显式错误。对外"缺失即显式拒绝"（`:2` 无 silent default）与 `DATA_ARTIFACTS.md:39`「properties 必需键非法→显式 P3_RS_PARAM」的主张失真。
- 定级：S2 —— 到 `p3_resample.cpp:276` 的路径清楚（同 CR-21-06 的 :14303 链），但"读失败是否被 fetch_tile 区分损坏/缺失"这一跳我未逐行读完 `fetch_tile`，故不判 S1。
- 怎么算修好：① 探测阶段至少 `stat` 并要求字节数 ≥ 一个最小 FITS 头（2880 B，若嫌硬编码则引 `docs/algorithms/HIPS_WRITER.md` 的卡序条文取实际最小头长），② 校验 `Npix<digits>.fits` 的数字段（复用 :26 `parse_int`），③ 在 `fetch_tile`/`read_leaf` 侧把 TILE_MISSING 与损坏/短读分成两个返回码，导出面只对前者输出 NaN+coverage=0，后者必须显式失败——补一条"0 字节 tile 必须判红"的负例。
- 置信：PARTIAL（缺 `fetch_tile` 对损坏 tile 的返回值这一跳；其余逐字读到）

#### 不报的核过项（本文件）
- `hips_frame` 接受 `icrs`（:124-125）偏离 IVOA §4.4.1 值域：**已在文档面登记为产品约定**，不重复报。检索式：`git grep -rn "hips_frame" -- docs` ⇒ `docs/algorithms/HIPS_WRITER.md:206`、`docs/algorithms/PHASE2_COVERAGE.md:67-68`「本模块额外接受 icrs」、`:92-93`（跨帧必须逐帧相等，混用 rc=1）。
- `path_is_safe` 的安全判据（:42-58）与 `kind/tkind` 极性（:154/:159）：逐字核过 `aio_atomic_file.h:392` 的枚举定义，方向与注释一致，且全部偏差都偏红；检索式 `git grep -rn "kind: 0 = " -- lib/infrastructure/aio`。
- 值域检查的顺序（先解析后判域，:117-127）与"必需键全查"（:111-115）确无 silent default：五个必需键缺失各自 `fail(...)`，无兜底赋值。检索式：本文件 :65 `*out = HipsProperties{};` 之后到 :128 的分支穷举。
- 探测只看声明叶级 `Norder<order>`（:141）而 `hips_core.c:21` 允许 partial tree：这是**更严**的一档（P3 要求叶级确有 tile），偏红不偏绿，不报。

---

## 3. `lib/algorithms/coverage/hips_properties.h`（47 行，已全文读完）

### 3.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :13 | `int order = -1;` | `-1` | "未设"哨兵（合法域是 0..kMaxOrder，-1 在域外，可判） | 结构性不适用 | 哨兵值与值域不冲突：`.cpp:117` 的 `order < kMinOrder` 必然拒绝 -1，故不存在"合法数值冒充未测"；不是科学量 | — | 高 |
| :14 | `int tile_width = 0;` | `0` | 同上哨兵 | 结构性不适用 | `.cpp:120` `tile_width != 512` 必拒 0 | — | 高 |
| :15-:20 | `std::string` 成员默认 | 空串 | 必需串键的"未设"哨兵 | 结构性不适用 | 必需串键由 `.cpp:111-115` 的存在位（`has_*`）判定，不靠空串；`bunit` 是唯一"空串有语义"的（见 :20 行） | — | 高 |
| :20 | `bunit` 缺省语义「按 SCI 合同 = **ADU**」 | `ADU` | BUNIT 键缺失时的单位口径声明 | **待确认** | 与消费侧条文直接相反：`lib/algorithms/resample/p3_resample.h:38-39`「无 BUNIT 键时缺省 canonical "ADU/sr"（DATA_SEMANTICS §31.1a FZ-UNIT-SIGNAL-SB；**绝不裸 ADU**、绝不 Jy/beam 默认）」、`p3_resample.cpp:292`「缺省串取该平面物理单位，禁裸 ADU（每像素口径）」；另 `docs/contracts/DATA_ARTIFACTS.md:39` 站本行（「面亮度(BUNIT 透传, 缺省 ADU; 禁默认 Jy/beam)」）⇒ 两份合同互斥 | 保守方向：缺省必须是带立体角幂次的串（`ADU/sr`），因为重采样只做凸组合不含 Ω_pixel 换算；影响范围：所有 BUNIT 键缺失的输入的导出单位标注与一切由它反推的面积/流量换算 | 高 |
| :23 | `kMaxOrder = 20` | `20` | `hips_order` 上界（超限拒） | **待确认** | 声明处给的锚不成立（见 CR-21-11）；权威链上零处规定该值：`git grep -n "0\.\.20\|hips_order" -- docs/api/PHASE3_API_V1.md docs/algorithms/PHASE3_RESAMPLE.md` → 零命中；`git grep -n "order\|512\|frame" -- docs/modules/registry/astrocs.phase3.properties.md` → 零命中；且与 I/O 合同 `astrocs/io/hips_input_v1.h:31 ACS_HIPS_ORDER_MAX 29` 取不同上界 | 保守方向：统一到单一命名常数并给可导出界（leaf_nside 与 64 位 ipix 不溢出：`nside = 512<<order ≤ 2^30` ⇒ order ≤ 21；或按 I/O 合同的 2^38 安全域）；影响范围：order 21..29 的合法产品在 P3 门被拒、在 plan_estimator 的可行域估计里被当上限用（`lib/include/astrocs/core/plan_estimator.h:18`） | 高 |
| :23 | 注释式 `2^(2*20) leaf 上限` | 表达式 | 为上界给出的"内存守卫"算术 | **待确认** | 式子本身与产品布局不符：本仓 HiPS 叶级 tile 数 = 12·4^K（见 `astrocs/io/hips_core.c:503 base_uniq = 4ULL * (1ULL << (2ULL * order))` 与 12 个底片），故 2^(2·20) 不是 leaf 数；且 ARCH-P3 §3 的内存上界式与 leaf 总数无关（`docs/architecture/PHASE3_MODULE_ARCH.md:37-39`） | 保守方向：要么换成可核对的量纲（tile 数 × W² × 字节 / cache 上限），要么删掉该算式不冒充推导；影响范围：读者据该注释以为存在内存推导 | 高 |
| :24 | `kMinOrder = 0` | `0` | `hips_order` 下界 | 结构性不适用 | 下界由布局定义（order 0 = 12 个底 tile），无自由度；不是科学量 | — | 高 |
| :25 | `kHipsTileWidth = 512` | `512` | tile 边长冻结值 | 结构性不适用 | 与 .cpp:120 同一常数，锚见 §2.1 对应行（`DATA_SEMANTICS.md:435`「恒 512…tile_order=leaf_order−9」）；其科学侧唯一用法是 `9 = log2 512` 的索引位移 | 选择理由（512 而非 256/1024）在三份登记面均只写"恒 512"未给导出；改动牵动产品字节，属负责人裁决域 | 高 |
| :23,:24,:25 | `static constexpr int`（内部链接） | — | 头内常数定义方式 | 结构性不适用 | 无 ODR 问题；注：这也使 §3.2 CR-21-11 的"两个上界各说各话"无法由链接器发现 | — | 高 |

被合并项声明：本文件全部数值即上表 8 行（`-1`、`0`（两处）、空串默认、`ADU`、`20`、`2^(2*20)`、`512`），无遗漏项；`06/TASK`、`P3-001`、`ALG-P3-001`、`ARCH-P3 §3`、`§3.1:173` 类为标识符非数值。

### 3.2 findings

### CR-21-09 · 本文件自报路径与全部下游权威引用停在已迁走的 `lib/phase3_session/`，追溯链断在字面上
- 轴：C
- 位点：`hips_properties.h:1` `// lib/phase3_session/hips_properties.h — HiPS properties 严格解析/校验 (ALG-P3-001) — P3-001`（实际路径为 `lib/algorithms/coverage/hips_properties.h`，`.cpp:1` 同形）
- 上位依据：AGENTS.md §7 目录落位表（`lib/algorithms/` = 科学算法；本模块迁移记录在 `lib/algorithms/coverage/README.md`/`lib/phase3_session/README.md:4`「`hips_properties.h`（本目录，批次 4 → `lib/algorithms/coverage/`）」）；ENGINEERING_SPEC 的追溯要求。
- 现状：`git grep -rn "phase3_session/hips_properties" -- lib docs` 显示 6 处仍指旧路径，其中两处是**把它当数值权威引用**的下游：`lib/include/astrocs/core/plan_estimator.h:19`「tile 宽 W=512（lib/phase3_session/hips_properties.h kHipsTileWidth=512；P2 …）」与 `lib/infrastructure/scheduler/src/plan_estimator.cpp:11`；另 `docs/traceability/TRACEABILITY_MATRIX.csv:24` 的备注「现文件 lib/phase3_session/hips_properties.cpp 存在但核心符号未核对, 不冒认」把 SRC/TEST/EVIDENCE 三列留在 MISSING。
- 差在哪：文件已移动但自述路径与被引用路径未同步，且追溯矩阵据此把本模块的 SRC 一栏挂在 MISSING——按字面检索"这个常数在哪定义的"会指向一个不存在的文件。
- 后果：机器门（追溯一致性）与人工追溯双失效一格：`kHipsTileWidth`/`kMaxOrder` 的引用面找不到定义处；对外"逐条到 ALG-007 的追溯"（`PHASE3_MODULE_ARCH.md:55` §5 标题）在本模块不成立。
- 定级：S3（不产生错值，仅追溯链断裂）
- 怎么算修好：改 `.h:1`/`.cpp:1` 自述路径 + `plan_estimator.h:19`/`plan_estimator.cpp:11` 两处引用为真实路径，并把 `TRACEABILITY_MATRIX` 第 24 行的 SRC/TEST 列按实际符号核对后改状态（同批，否则改路径反而让"核对过"的说法失去依据）。
- 置信：CONFIRMED

### CR-21-10 · 本模块 SCI/ALG 权威在追溯面上标 MISSING，而头注释自称符合 ALG-P3-001：值域（frame/tile_width/order）无任何规定它们的条文
- 轴：S（主轴）/ D
- 位点：`hips_properties.h:2` `// 原则(06/TASK): 无 silent default —— 必需键缺失/值非法/越界一律拒绝;` 与 `:13` `int order = -1;   // hips_order(必需, 0..kMaxOrder)`
- 上位依据：`docs/traceability/TRACEABILITY_MATRIX.csv:24` 行 `MOD-astrocs-phase3-properties`：`SCI-P3-PROPS-001,MISSING,MISSING,ALG-P3-001,MISSING,MISSING` ⇒ 备注「SCI/ALG 显式 MISSING 待独立 authority 文档」；`docs/modules/registry/astrocs.phase3.properties.md:7` `upstream: [SCI-P3-PROPS-001, ALG-P3-001, API-P3-001]` 声称有上游。
- 现状：被引为 ALG-P3-001 的文档真实存在（`docs/algorithms/PHASE3_RESAMPLE.md:5` `> ID: ALG-P3-001 … 状态: DERIVED`），但其对本文件的条文只有流程性两句：`:95`「`props = read_properties(hips_dir)`  # ALG-P3-001: 必需键校验, 非法显式拒」与 `:131`「properties 非法/缺键 | 显式拒(ALG-P3-001), 无 silent default」——**不规定必需键集合、不规定 order 值域、不规定 frame 值域**。检索式：`git grep -n "0\.\.20\|hips_order" -- docs/api/PHASE3_API_V1.md docs/algorithms/PHASE3_RESAMPLE.md` → 零命中；`git grep -n "order\|512\|frame" -- docs/modules/registry/astrocs.phase3.properties.md` → 零命中。
- 差在哪：本文件把"哪些键必需、值域是什么"这两件事**由代码自己定义**（:13-:20 的行内注释即唯一定义面），而上位文档只说"要校验、要显式拒"，等于门的内容项无权威依据；追溯矩阵自己也认定 SCI/ALG 两项 MISSING，与 `.h:1` 自称 (ALG-P3-001) 的合规声明不同步。
- 后果：CR-21-06（不查 `hips_ordering`/`hips_version`）与 CR-21-11（order 上界与 I/O 合同冲突）在制度上无人裁判——"必需键集合"没有权威清单，任何一侧的修改都不违反任何条文；对外"P3 properties 严格校验"的可核验性只剩读代码。
- 定级：S2 —— 后果是判据本身失去权威锚（可致对外主张不成立），但不直接产出错值。
- 怎么算修好：在 `docs/algorithms/PHASE3_RESAMPLE.md`（ALG-P3-001 条目下）或新建的 properties 权威条目里冻结：必需键集合（含 `hips_version`、`hips_ordering`）、各键值域（order 上界及其导出、tile_width、frame 词表、dataproduct_type）、可选键的重复策略；同批把 `TRACEABILITY_MATRIX.csv:24` 的 SCI/ALG 两列从 MISSING 改为指向该条，并让 `.h:13-:20` 的行内注释改为引用条目号而非自证。
- 置信：CONFIRMED

### CR-21-11 · `kMaxOrder = 20` 的"ARCH-P3 §3 内存守卫"锚经核不成立，且与 I/O 合同的 `ACS_HIPS_ORDER_MAX = 29` 并存两套上界
- 轴：S（主轴）/ C
- 位点：`hips_properties.h:23` `static constexpr int kMaxOrder = 20;   // ARCH-P3 §3 内存守卫(2^(2*20) leaf 上限之外拒)`
- 上位依据：被引锚 `docs/architecture/PHASE3_MODULE_ARCH.md:29` §3「并发与内存上界(ARCH-004 合同实例化)」，其冻结式在 `:37-39`：「内存上界(冻结): `M ≤ 2·queue_depth·sub_block_px²·(4|8) + max_tiles·W²·(4|8) + 常数` —— **与输出总图大小 W_out·H_out 无关**(ASTROCS_DESIGN §8.3 export 行:「内存占用与子块大小成正比、与总图大小无关」)」；该节的"内存守卫"落在 `sub_block_px`[16,1024]、`queue_depth`[1,64]、`max_tiles`（`:40-41`）三个参数上，**式中没有 order 项**。
- 现状：代码用 §3 的名义把 order 钉成 20；另一入口对同一个 `hips_order` 键钉成 29——`lib/infrastructure/aio/io/include/astrocs/io/hips_input_v1.h:31`「`#define ACS_HIPS_ORDER_MAX 29   /* order K: 0..29 (NSIDE ≤ 2^38 安全域) */`」，实际执行点 `lib/infrastructure/aio/io/hips_core.c:216-218`。注释里的算术 `2^(2*20)` 也与布局不符（叶级 tile 数含 12 个底片因子，见 `hips_core.c:503 base_uniq = 4ULL * (1ULL << (2ULL * order))` 的 4^K 递推）。
- 差在哪：(a) 引的条文不含该量，注释把"节名匹配"当成"依据"；(b) 同一输入键的两处上界（20 / 29）由两个不同理由支撑，无一处给可核对的溢出推导；(c) 注释给的算式量纲不对（leaf 数不是 2^(2K)）。
- 后果：`acsd export` 读一个 order 21..29 的输入时，I/O 层判定可开（合同 NSIDE ≤ 2^38 安全域），P3 校验层判非法 ⇒ 同一产品"能不能用"有两个答案；另外 `lib/include/astrocs/core/plan_estimator.h:18-26` 把 `kMaxOrder = 20` 当作**全局**可行域上界（`kHipsMaxOrder = 20（p3 hips_properties.h kMaxOrder）`）反哺调度计划，于是 P3 门的一个无锚常数决定了计划估计，而它引用的正是它自己（循环锚）。
- 定级：S2 —— 方向明确偏红（20 比 29 严），不会产出错值，但两处入口对同一阈值取不同值本身即 C 轴成立；缺"因该值导致产物错值"的生产路径故不判 S1。
- 怎么算修好：① 定一个权威上界并给溢出导出式（`leaf_nside = kTileWidth << order`（`p3_resample.cpp:301 s->leaf_nside = kTileWidth << p.order;`）与 `npix = 12·leaf_nside²` 必须在 `uint64_t` 内 ⇒ order ≤ 24；若沿用 I/O 合同的 NSIDE ≤ 2^38 口径则取 29）；② 让 `hips_properties.h:23` 与 `hips_input_v1.h:31` 引用同一个命名常数（或让前者显式取后者并加 static_assert），删掉 `2^(2*20)` 这一伪算式或改成真算式；③ `plan_estimator.h:18` 的引用改为指向 I/O 合同常数；④ 若确要保留 20，须在 §3 或 ALG 条目里写清"20 = 产品约定上限"及其理由，不能挂"内存守卫"。
- 置信：CONFIRMED（两常数、被引条文、下游消费者行号均逐字读到）

#### 不报的核过项（本文件）
- `order = -1` / `tile_width = 0` / 空串默认是否构成"合法数值冒充未测"（前言重点 ④）：**不构成**——三者都落在各自值域之外，且 `.cpp:111-115` 用独立存在位（`has_*`）而非哨兵值判缺失，缺失时返回 false 而不是带着默认继续。检索式：本文件 :13-:20 与 `.cpp:65-66,:111-127` 逐行。
- 「缺 tile 探测」在头里被限定为"至少 1 tile"（:3、:36），未夸大为完整性校验——该弱判据本身已在 CR-21-08 按"可读"二字定性，不另报"没做全树校验"。
- `static constexpr` 放在头里的链接形态：内部链接、无 ODR 风险，不报。

---

## 4. `lib/algorithms/coverage/include/astro/phase2/identifiability.h`（163 行，已全文读完）

### 4.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :21 | `H_eq = D⁻¹ H D⁻¹, D = diag(sqrt(H_ii))` | — | 列均衡式 | 公式导出 | 正本 `docs/science/PHASE2_UPM.md` 规则 4（:196-197）「`H_eq = D⁻¹ H_red D⁻¹`，`D = diag(√H_ii)`（列均衡消除参数单位自由度）」；实现 `identifiability.cpp:224` 逐字按式算 ⇒ 合规 | — | 高 |
| :23 | `r_eff = #{ i : λ_i > τ · λ_1 }` | — | 有效秩定义（λ 域、相对） | 公式导出 | `docs/science/PHASE2_UPM.md:197`「记 `H_eq` 特征值 `λ_1 ≥ … ≥ λ_n`，则 `r_eff = #{λ_i > τ·λ_1}`」；实现在 `.cpp:267-276`（见 §5 的域/常数问题） | — | 高 |
| :25 | `identifiable ⟺ r_eff == n ⟺ λ_n > τ·λ_1 ⟺ κ < 1/τ` | — | 四写法等价链 | 公式导出 | 同正本 :197-198；等价性在 λ_n > 0 时成立（`λ_n > τλ_1 ⟺ λ_1/λ_n < 1/τ`）。**但第四个写法不由实现保证**：判决位由位移 Cholesky 决定、`kappa` 由逆幂迭代独立算出 ⇒ 见 CR-21-14 | — | 高 |
| :37,:150 | 精度地板 `max(m,n)·eps` | — | τ 缺省下界的导出式 | 公式导出 | 代码真按式算：`.cpp:158-162 return static_cast<double>(mn) * kEps;` ⇒ 式与实现一致。**外部对齐论证错域**：见 CR-21-13 | — | 高 |
| :37-:40 | 与 NumPy `matrix_rank` tol / MATLAB `rank` / SciPy `cond` 「同一约定」 | `max(M,N)*eps` | 为地板取法提供外部凭据 | **待确认** | 被引三家约定一律作用在**奇异值** `S` 上（NumPy Notes 原文即本文件 :43-47 所引「…in the calculation of the SVD」），而本仓判据作用在 `H = AᵀWA` 的**特征值比** `λ_i/λ_1 = (σ_i/σ_1)²` 上（正本 `docs/science/PHASE2_UPM.md:197` 亦为 λ 域）⇒ 见 CR-21-13 | 保守方向：σ 域地板平移到 λ 域应取 `(max(m,n)·eps)²`；取大者（现行）方向偏严（偏红） | 高 |
| :49-:50 | 「本仓把 τ 登记为 FZ-AP2S-RANK-RTOL（**冻结值** 1e-10）」 | `1e-10` | 唯一阈值 τ 的实际生效值 | **待确认** | 登记面 `eng/contracts/data/v6_clause_registry_v1.json:1590` 该条目 `"status": "PENDING_OWNER_SIGNOFF", "owner_signoff": "SO-07"`，且其 `source_binding{file: docs/algorithms/UPM_SOLVER.md, contains: "1e-10"}` 的锚文件里 `FZ-AP2S-RANK-RTOL` 与 `1e-10` **均零命中**（检索式：`git grep -n "1e-10\|FZ-AP2S-RANK-RTOL\|rank_rtol" -- docs/algorithms/UPM_SOLVER.md` → 无输出；文件确实存在）⇒ 见 CR-21-12 | 保守方向：值本身比地板严、不会放行错值，但"已冻结"这一状态主张不成立；影响范围：该阈值的改动权（负责人签署）与 CI 的 source_binding 核验 | 高 |
| :50 | 「地板（本产品量级 ≈6e-11）」 | `6e-11` | 论证 1e-10 比地板更保守 | 待确认 | 反推需 `max(m,n) ≈ 270`；全仓无一处登记该量级。实际两侧都远小于 4.5e5 ⇒ 地板几乎从不生效（`1e-10 > max(m,n)·eps` 要求 max(m,n) < 4.5e5），故"取大者"恒等于 1e-10 | 保守方向：把该量级写成可核对式（如夹具/生产实测的 n_free 与 n_obs 上界）；影响范围：仅本行论证的成立性 | 中 |
| :52 | 「不再有 1e6 / 1e8 这类绝对常数」 | `1e6` `1e8` | 历史被退休常数的叙述 | 结构性不适用 | 代码里已无该值（`git grep -rn "kappa_max" -- lib` 无生效实现）；纯历史注释 ⇒ 不报 | — | 高 |
| :60-:61 | 见证 `H=[[1,1],[1,1]]`、`λ=1e-9` ⇒ `κ(H+λI)=2e9 < 1/1e-10` | `1e-9` `2e9` `1e10` | 说明"门设在正则化矩阵上即恒真门"的可执行反例 | 公式导出 | 独立复算：H 谱 = {2, 0}；`κ = (2+1e-9)/1e-9 = 2.000000001e9` ✓，`1/τ = 1e10` ✓，`2e9 < 1e10` ✓ ⇒ 见证成立；代码不实现该式（见证落在 `eng/tests/unit/v6_p2_sky_kappa/`，本批不实跑） | — | 高 |
| :80 | `P_eff = tr(Hat) = rank(X) = rank(A)`, `dof = N − P_eff` | — | 自由度口径 | 文献值 | Andrae, Schulze-Hartung & Melchior 2010, arXiv:1012.3754 式 (8)(9)；正本同引：`docs/science/PHASE2_UPM.md:201`、`docs/contracts/DATA_SEMANTICS.md:2240`；实现 `.cpp:164-166 return n_obs − rank_eff;` 按式（不夹非正，与 :156-157 的声明一致） | — | 高 |
| :89 | `(n_frames−1)·m` | — | Schur 消元后补回被精确消去的 δ 块秩 | 结构性不适用 | 秩的可加项（块对角），非标定常数；消费点 `sky_plane.cpp:1204-1206` | — | 高 |
| :127 | `n_obs`「可为 0」 | `0` | 哨兵语义 | 结构性不适用 | 实现在 `mn == 0` 时退化取 `kEps`（`.cpp:160`）——注意该退化路径头里未述，见 §5 表内同项 | — | 高 |
| :137 | `identifiable` 1/0 | `1` `0` | 判决位 | 结构性不适用 | 枚举基数 | — | 高 |
| :142,:144 | `rank_rtol <= 0 / 非有限 ⇒ 取地板`；`返回 0 = ok（含判红）` | `0` `1` | 返回码约定 | 结构性不适用 | 与 `.cpp:170-176` 一致（逐行核过） | — | 高 |

被合并项声明：本文件数值即上表 13 行；`:114-118`（dllexport 宏）、`:105-108`（测试路径清单）无数值。

### 4.2 findings

### CR-21-12 · 头自称 τ 是「冻结值 FZ-AP2S-RANK-RTOL」，而该登记条目状态为待负责人签署、其 source_binding 锚文件里既无该 ID 也无该值
- 轴：C（主轴）/ S
- 位点：`identifiability.h:49-50` `// 本仓把 τ 登记为 FZ-AP2S-RANK-RTOL（冻结值 1e-10）。`
- 上位依据：`eng/contracts/data/v6_clause_registry_v1.json:1590` 起该条目原文键值：`"value": 1e-10, "unit": "1", "status": "PENDING_OWNER_SIGNOFF", "owner_signoff": "SO-07", "source_binding": {"file": "docs/algorithms/UPM_SOLVER.md", "kind": "substring", "locator": "FZ-AP2S-RANK-RTOL", "contains": "1e-10"}`；治理面：AGENTS.md §6「不在 main 外…」「不宣布发布」＋§10「涉及负责人的决定需上呈」——未签署条款不具"冻结"身份。
- 现状：锚文件 `docs/algorithms/UPM_SOLVER.md` 存在（`ls docs/algorithms | grep -i upm` 命中），但 `git grep -n "1e-10\|FZ-AP2S-RANK-RTOL\|rank_rtol" -- docs/algorithms/UPM_SOLVER.md` → **零命中**；值在代码侧以三处字面量存在（`sky_plane.cpp:463 c.rank_rtol = 1e-10;`、`sky_plane.cpp:519`（非法值兜底再写 1e-10）、`stage2_common.h:97 double sky_plane_rank_rtol = 1e-10;`），无一处从登记表读取。
- 差在哪：注释用"冻结值"表述一个登记为 `PENDING_OWNER_SIGNOFF` 且锚文缺失的条款；同时"两个求解器共用同一个符号、同一个值、同一个函数"（:51-52）的"同一个值"由三处复制的字面量维持，不由任何单一事实源维持。
- 后果：(a) 对外/对内主张"τ 已冻结"不成立——负责人签署（SO-07）一旦改值，三处字面量与登记表面不会同步，门会静默取旧值；(b) 机器侧 source_binding 核验对该条款必为悬空（锚串不在被绑文件里），该阈值的"有出处"实际不可核。
- 定级：S2 —— 可致对外主张（"阈值有唯一登记出处"）不成立；不直接产出错值。
- 怎么算修好：① 把 `docs/algorithms/UPM_SOLVER.md`（或正本 `docs/science/PHASE2_UPM.md` 规则 4）里补出含 `FZ-AP2S-RANK-RTOL` 与 `1e-10` 的条文段，使 binding 不再悬空；② 注释改为与登记面一致的措辞（"登记待签署条款 SO-07"），或在负责人签署后由登记面驱动；③ 三处字面量收敛到一个由合同生成的常数（同批改，否则"改对反而判红"：`v6_p2_sky_node_adapt_test.cpp:164` 正在断言 `default rank_rtol == 1e-10`）。
- 置信：CONFIRMED

### CR-21-13 · 用「与 NumPy/MATLAB/SciPy 同一约定」为 τ 地板背书，但那三家的约定作用在奇异值上、本仓判据作用在法方程特征值比上，两者差一个平方
- 轴：S（主轴）/ C
- 位点：`identifiability.h:37-40` `// p2_identifiability_rank_rtol_floor = max(m,n)·eps，与 NumPy linalg.matrix_rank 的 tol = S.max()*max(M,N)*eps、MATLAB rank 的 max(size(A))*eps(norm(A))、SciPy linalg.lstsq/pinv 的 cond 默认 max(M,N)*eps 同一约定`
- 上位依据：本仓正本对判据量的定义在 **λ 域**：`docs/science/PHASE2_UPM.md:197`「记 `H_eq` 特征值 `λ_1 ≥ … ≥ λ_n`，则 `r_eff = #{λ_i > τ·λ_1}`、`κ = λ_1/λ_n`」；而被引三家的约定按 NumPy Notes 原文（本文件 :43-47 自引）是「deal with floating point roundoff error in the calculation of the **SVD**」，即 `σ_i > tol·σ_max`。同一条款的登记面又写成第三个口径：`eng/contracts/data/v6_clause_registry_v1.json:1593 subject: "秩判定相对容差 sigma_i/sigma_max"`、`lib/algorithms/upm/memory.md:143`「秩：`sigma_i/sigma_max > 1e-10`」。
- 现状：代码在 λ 域执行（`.cpp:267 sigma = tau * lam_max;` 后对 `Heq − σI` 判正定），但论证链同时引用 σ 域的外部约定与 σ 域的登记措辞。
- 差在哪：`H = AᵀWA` 的 `λ_i/λ_1 = (σ_i(A)/σ_1(A))²`，于是同一个 1e-10 在两个口径下相差 5 个数量级：λ 域 1e-10 ⇔ σ 域 1e-5。因此「同一约定」不成立：把 σ 域地板 `max(m,n)·eps` 直接搬到 λ 域，比按约定正确平移（`(max(m,n)·eps)²`）严约 `1/(max·eps)` 倍（n=100 时 ≈ 13 个数量级）。三条面（科学正本 λ / 登记面 subject σ / 头注释借用的外部约定 σ）互不相同 ⇒ 复算者按登记面措辞复算会得出与实现相差 5 个数量级的期望。
- 后果：判据的"出处"被跨域引用支撑 ⇒ §199「阈值只有一个」的可核对性失真；按 σ 口径复核/复算（例如用 LAPACK `gelsd` 的 rcond 直接对比）的人会认为大量已判红样本应判绿，或反之——本批"可辨识性阈值是否有一手出处"这一问的正面回答在此处断裂。方向固定：现行 λ 域执行比 σ 域措辞**更严 ⇒ 偏红**（可能把可解系统判成欠定，触发 `sky_plane.cpp:1126` 的 RED 分支与 `upm.cpp:2742 return 3`）。
- 定级：S2 —— 不产出错值（方向偏严），但"阈值出处"这一对外主张不成立，且存在误红导致产品降级/失败的可达路径。
- 怎么算修好：三处只留一个口径并显式写明域：① 登记条目 `subject` 改为「λ_i/λ_1（列均衡法方程特征值比）＝ (σ_i/σ_max)²」或改值以匹配 σ 措辞；② `lib/algorithms/upm/memory.md:143` 同步；③ 头 :36-47 的 NumPy/MATLAB/SciPy 段改写为「本仓在 λ 域，等价于 σ 域 √τ；引用外部约定仅用于说明'阈值来自算术而非数据集'这一原则」，并把地板改为其平方或注明取大者后由 1e-10 支配；④ 补一条测试/文档断言把 λ↔σ 的换算钉住（否则改一侧忘另一侧必然复发）。
- 置信：CONFIRMED（三处口径原文逐字读到；换算 λ=σ² 自证）

### CR-21-14 · 头宣称「判决位唯一：identifiable ⟺ κ < 1/τ」，实现里 κ 与判决位由两条**互不校验**的路径独立算出，等价链只在逆幂迭代收敛时成立
- 轴：C（主轴）/ R
- 位点：`identifiability.h:25` `//     identifiable ⟺ r_eff == n ⟺ λ_n > τ · λ_1 ⟺ κ < 1/τ` 与 `:132` `double kappa; // λ_1/λ_n；秩亏 ⇒ +inf（**不发布伪值**）`
- 上位依据：正本 `docs/science/PHASE2_UPM.md:198`「`identifiable ⟺ r_eff == n_free ⟺ κ(H_red) < 1/τ`」＋`:217-218`「判据读数必须可观测并写入 provenance：…`r_eff` 与 `κ(H_red)`、唯一阈值 `τ`…以及 `n_unidentified`」——即 κ 是**对外发布的判据读数**（消费点：`upm.h:372`「每分量 ref_frame_id、rank、rank_rtol、rank_rtol_effective、kappa」、`sky_plane.h:369-370`）。
- 现状：判决位走位移 Cholesky/惯性路径（`.cpp:266-284`），`kappa` 走幂迭代 + 逆幂迭代（`.cpp:245/:257`）；两条路径之间没有任何一致性检查或"不一致即降级为不可信"的标志位（`.cpp:279-280` 只调和 `below` 与 `spd_shifted`，**不**调和 `kappa` 与 `identifiable`）。`lambda_min_inverse`（`.cpp:110-124`）**没有收敛判据**：固定 300 步后直接取 Rayleigh 商。
- 差在哪：Rayleigh 商对任意单位向量恒 ≥ λ_n，故逆幂迭代未收敛时发布的是**偏大的 λ_n、偏小的 κ**；于是存在 `identifiable == 0`（位移 Cholesky 判红）而发布 `kappa < 1/τ` 的合法组合同时出现，:25 的等价链在**被发布的字段**上不成立，而下游文档（`sky_plane.cpp:1083`、`upm.h:246`）把它当作定义在写。
- 后果：provenance 里成对出现的 `rank_eff`/`identifiable` 与 `kappa` 可互相矛盾（判红 + κ 看着健康），审计者据 κ 判断病态度会得到偏乐观的读数；对创新点③「天光面可辨识性」的对外读数（κ 入 provenance）失真。判决本身仍偏严（κ 不进判决），故不是产物错值。
- 定级：S2 —— 缺"κ 直接参与判决"这一跳（实现上确实不参与），故不判 S1；但发布读数与判决位互不相容这一条已足够不成立。
- 怎么算修好：① 由**同一批**特征值/主元信息导出 κ（例如以位移惯性价 `below == 0` 时的最小主元比作为 λ_n 的下界读数），或② 保留两条路径但加一致性位：`kappa >= 1/tau` 与 `identifiable == 1` 冲突时把 κ 记为 `+inf`（或新增 `kappa_converged` 旗标）并在文档里改为「κ 为诊断读数，等价链不保证」；③ `lambda_min_inverse` 补收敛检查（复用 :100 的相对判据）并在不收敛时返回 0（→ κ=+inf，与"不发布伪值"一致）；④ 同批改 `sky_plane.cpp:1083`/`upm.h:246` 的"⟺"措辞与 `PHASE2_UPM.md:198`，否则文档侧仍宣称等价。
- 置信：PARTIAL（"κ 与 identifiable 可同时矛盾"我按 Rayleigh 商 ≥ λ_n 的定理与两路径代码逐行核过，但未构造数值夹具实际复现一个样本——缺的是可跑夹具，本批禁止实跑）

## 5. `lib/algorithms/coverage/src/identifiability.cpp`（288 行，已全文读完）

### 5.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :28 | `kEps = numeric_limits<double>::epsilon()` | `2.220446049250313e-16` | 环境浮点精度 | 结构性不适用 | 标准库给出的机器常数，注释复述值与 IEEE754 binary64 一致，非科学量 | — | 高 |
| :40 | `if (!(sum > 0.0) \|\| !std::isfinite(sum)) return false;` | `0.0` | 无前元 Cholesky 的正主元判据 | 结构性不适用 | 数学判据（Sylvester 顺序主子式），无自由参数 | — | 高 |
| :82 | 幂迭代起点 `1.0/sqrt(n)` | `1.0` | 确定性起点（逐位可复现） | 结构性不适用 | 头 :10 注释「确定性起点 1/√n ⇒ 同输入逐位可复现」；起点选择不进语义 | 备注（不单独成 finding）：全 1 起点若与顶特征向量正交则收敛到 λ₂，属该选择的固有局限；极端退化 `A·x₀=0` 被 :96 捕获并走 :247 判红，方向偏红 | 中 |
| :85 | `for (int it = 0; it < 300; ++it)` | `300` | λ₁ 幂迭代步数上限 | 待确认 | 条文面零提及（检索式：`git grep -rn "300" -- docs/science/PHASE2_UPM.md docs/algorithms/UPM_SOLVER.md` → 无该判据条目）；且头的「边角语义」:93-100 只列 4 种不可评估情形，不含"迭代未收敛" | 保守方向：不收敛应显式降级（rc=1 或 κ=+inf）而非静默发布读数；影响范围：`lambda_max` → `sigma = τ·λ₁` → 阈值本身 | 高 |
| :100 | `it > 3` | `3` | 收敛检查预热步数 | 结构性不适用 | 防前几步偶然重合，无科学语义 | — | 高 |
| :100 | `1e-13 * max(1.0, fabs(lam))` | `1e-13` | λ₁ 相对收敛判据 | 待确认 | 零出处；λ₁ 的相对欠估直接进入阈值。**方向钉死**：提前 break ⇒ λ̂₁ < λ₁ ⇒ σ 偏小 ⇒ 门更易过 ⇒ 偏绿（见 CR-21-16） | 保守方向：判据应收紧到 τ 的千分之一以下并带收敛旗标 | 高 |
| :112 | `for (int it = 0; it < 300; ++it)` | `300` | λ_n 逆幂迭代步数上限 | 待确认 | 同 :85；此循环**无任何收敛检查**（:113-121 只做归一化，耗尽后 :123 直接取 Rayleigh 商） | 保守方向：见 CR-21-16（λ̂_n 偏大 ⇒ κ 偏小 ⇒ 读数偏乐观）；影响范围：入 provenance 的 `kappa`/`lambda_min` | 高 |
| :119 | `return 0.0;`（norm 非正/非有限） | `0.0` | λ_n 不可评估 → 记 0 → κ=+inf | 结构性不适用 | 头 :96/:132「κ 记 +inf，不发布伪值」一致；方向偏红 | — | 高 |
| :123 | `return sym_rayleigh(A, n, x);` | — | λ_n 读数 | 公式导出 | 式 `xᵀAx`，对称阵下恒 `≥ λ_n` ⇒ 该读数只高不低（CR-21-16 的方向依据） | — | 高 |
| :138-:141 | `if (!(dj > tol)) { ++neg; dj = tol; }` | `tol` | 修正主元 LDLᵀ（Gill–Murray–Wright） | 公式导出 | 方法有出处（GMW 修正主元 + Sylvester 惯性定律，:12-15/:127-128 已述）；但"主元改写后继续"在浮点下改变了被分解矩阵，`neg` 不是严格惯性数 ⇒ 与 :12-15 把 r_eff 说成惯性计数不符，见 CR-21-17 | — | 高 |
| :160 | `if (mn == 0) return kEps;` | `kEps` | n_obs=n_params=0 时地板退化 | 结构性不适用 | 该路径实际不可达（`n_params==0` 已在 :170 拒），退化值取最小 ⇒ 保守；头 :150 未述该分支 | — | 中 |
| :175-:176 | `tau = (isfinite(rank_rtol) && rank_rtol > 0) ? rank_rtol : floor_tau;` + `if (tau < floor_tau) tau = floor_tau;` | — | 请求值与精度地板取大者 | 公式导出 | 头 :142/:150 逐字一致（「<=0 / 非有限 ⇒ 取地板」），代码真按式算 ⇒ 合规 | — | 高 |
| :189-:194 | `hii > 0.0` / `hii == 0.0` / else `return 1` | `0.0` | 无信息方向 vs 非法输入 | 结构性不适用 | 判据即 Gram 阵对角必要条件，与头 :95-100 一致 | — | 高 |
| :224 | `D[i] = std::sqrt(hii)` | — | 列均衡对角 | 公式导出 | 头 :18 `D = diag(sqrt(H_ii))`，一致 | — | 高 |
| :238 | `Heq[i*nl+j] = 0.5*(a+b)/(di*dj)` | `0.5` | 对称化（输入可能只填一侧） | 结构性不适用 | 平均系数，非科学量；输入对称时结果等于 D⁻¹HD⁻¹ | — | 高 |
| :262-:264 | `kappa = (lam_min > 0.0 && n_dead == 0) ? lam_max/lam_min : inf` | `0.0` | 不发布伪值 | 结构性不适用 | 头 :132 一致 | — | 高 |
| :267 | `sigma = tau * lam_max` | — | 阈值 `τ·λ₁` | 公式导出 | 头 :23 `λ_i > τ·λ_1`，一致（λ₁ 可信度见 :85/:100 与 CR-21-16） | — | 高 |
| :275 | `scale = std::max(1.0, std::fabs(1.0 - sigma))` | `1.0` | 声称的"按位移后矩阵尺度归一" | **待确认** | 可证恒等于 `1.0`：τ = max(1e-10, max(m,n)·eps)、λ₁ ≤ nl（均衡后对角为 1 ⇒ tr = nl）⇒ `0 < sigma ≪ 1` ⇒ `fabs(1−sigma) < 1` ⇒ `max(1.0, …) ≡ 1.0`（见 CR-21-15） | 保守方向：尺度应取 `λ₁ − σ`；退化为绝对容差时**方向 = 计数偏少 = rank_eff 偏大 = 偏绿** | 高 |
| :276 | `count_nonpositive_pivots(shifted, nl, 64.0 * kEps * scale)` | `64.0` | 主元"视为零"的容差系数 | **待确认** | 与正本「阈值**只有一个** `τ = rank_rtol`」（`docs/science/PHASE2_UPM.md:199`）字面冲突（这是第二把数值尺）；检索式 `git grep -rn "64\.0 \* \|64 eps\|64·eps" -- docs lib` ⇒ 仅命中本行（其余命中为测试里的 `12.0*64.0*64.0` 像素面积，无关） | 保守方向：应表为 τ 与 λ₁ 的函数或并入 `rank_rtol_floor` 的同一命名式；影响范围：`rank_eff`/`n_unidentified`/`dof_eff`/`chi2_red` 读数 | 高 |
| :277 | `if (below > nl) below = nl;` | `nl` | 计数上界夹取 | 结构性不适用 | 防越界，非科学量 | — | 高 |
| :279 | `if (spd_shifted && below != 0) below = 0;` | `0` | 两读法不一致时的调和 | 公式导出 | 数学上正确（顺序主子式全正 ⇒ PD ⇒ 所有 λ > σ ⇒ 计数应为 0），**但与 :17/头 :17 承诺的"取更保守者（判红）"方向相反** ⇒ 见 CR-21-17 | — | 高 |
| :280 | `if (!spd_shifted && below == 0) below = 1;` | `1` | 同上另一侧 | 公式导出 | 非 PD ⇒ 至少一个 λ ≤ σ ⇒ 计数 ≥ 1，正确且偏红 | — | 高 |
| :282-:284 | `rank_eff = nl − below;` `n_unidentified = n_dead + below;` `identifiable = (n_unidentified == 0)` | — | 有效秩／未约束方向数／判决位 | 公式导出 | 与头 :23/:134 一致 **除一处域差**：头的 r_eff 定义在 n×n 的 `H_eq` 全体特征值上，实现只在 `live` 子块（nl×nl）上计数并丢弃 dead 行列的全部交叉项；`n_dead > 0` 时被丢项若非零（输入不是 Gram 阵），发布读数与头定义不是同一量（头 :95-96 只讲"无信息方向"，未授权删行删列）。判决位仍为红 ⇒ 记待确认，不单列 finding | 保守方向：dead 行列非零应 rc=1（与 :194 负对角同级） | 中 |

被合并项声明：本文件数值即上表 22 行；哨兵 `0.0`/`1.0` 共 7 处（:40、:82、:119、:160、:189、:191、:262）逐位点列出未隐藏；`300` 两处（:85、:112）分行列出未合并。

### 5.2 findings

### CR-21-15 · 计数路径的第二把尺 `64.0·eps` 零出处，且其"尺度归一因子"`max(1.0,|1−σ|)` 可证恒等于 1 ⇒ 相对容差退化为主 1.4e-14 的绝对容差
- 轴：S（主轴）/ R
- 位点：`identifiability.cpp:275-276` `const double scale = std::max(1.0, std::fabs(1.0 - sigma));` / `std::uint64_t below = count_nonpositive_pivots(shifted, nl, 64.0 * kEps * scale);`
- 上位依据：`docs/science/PHASE2_UPM.md:199`（规则 4 续文）「阈值**只有一个** `τ = rank_rtol`（**相对**口径，尺度不变），由浮点算术给出（精度地板 `max(m,n)·eps`），阈值来源 = 浮点算术（数据集标定与绝对条件数上限（`kappa_max` 类常数）不属阈值面）」；`identifiability.h:15`「判据（唯一，尺度不变，不含按数据集标定的常数）」、`:21`「唯一阈值 τ = rank_rtol」；`docs/science/PHASE2_UPM.md:217-218` 要求 `n_unidentified` 入 provenance。
- 现状：`sigma = tau * lam_max`（:267）。均衡后对角恒 1 ⇒ `1 ≤ λ₁ ≤ nl`；τ = max(1e-10, max(m,n)·eps) ⇒ `σ ≤ nl·1e-10`（nl 为数千量级时 σ ≪ 1）⇒ `fabs(1.0 − sigma) ∈ (0,1)` ⇒ `max(1.0, …)` 恒取 `1.0`：**归一因子是死表达式**，容差因此是与 λ₁ 无关的绝对值 `64·eps ≈ 1.42e-14`；系数 `64.0` 在 `docs/science`、`docs/algorithms`、`eng/contracts` 三面零命中。
- 差在哪：(a) 正本与本文件头都声明"阈值只有一个、尺度不变"，代码在计数路径引入第二个无出处数值旋钮；(b) 意图（按 ‖H_eq−σI‖ ≈ λ₁ 归一）与写法（对 `1−σ` 取 `max(1,·)`）不等价，且可证恒假。
- 后果：λ₁ > 1（近相关参数块，最大可达 nl）时绝对容差比应有的相对容差小 λ₁ 倍 ⇒ 主元"勉强为正"的方向不被计入 below ⇒ 发布的 `rank_eff` 偏大、`n_unidentified` **偏小**（该字段是正本明令入 provenance 的对外读数），并连带 `dof_eff = n_obs − rank_full`（`sky_plane.cpp:1204-1206`）偏小、`chi2_red`（`sky_plane.cpp:1207`）偏大。方向钉死：对外"未被约束方向数"被系统性低估 = **偏绿**。
- 定级：S2 —— 后果在对外发布的判据读数与 χ²_red；判决位由位移 Cholesky 决定（:273/:279），未证产物错值。触发路径：`upm.cpp:2737`、`sky_plane.cpp:1099`、`lib/algorithms/photometry/cpp/src/spatial_gain.cpp:145`。
- 怎么算修好：① 尺度因子改 `std::max(1.0, lam_max - sigma)`（或径取 `lam_max`）并删恒假分支；② `64.0` 提为具名常数并在 `PHASE2_UPM.md` 规则 4 里给导出（例如"c·max(nl,1)·eps 覆盖 nl 次主元累积舍入"，若取 c=64 请写明），使「阈值只有一个」重新成立或明确改写为两个；③ 同批补一条 λ₁ ≫ 1 的近相关块夹具测试，断言 `n_unidentified` 与独立 SVD/QR Oracle 一致（现有对拍在 `eng/tests/unit/v6_p2_identifiability/`，需实跑确认其是否覆盖该形态）。
- 置信：CONFIRMED（`scale ≡ 1` 为可证恒等式；条文与消费点逐字读到；未实跑量具看偏差幅度）

### CR-21-16 · λ_n 逆幂迭代无收敛检查、λ₁ 幂迭代可提前停机，两处偏差同向（都让门与读数"看起来更好"）
- 轴：R（主轴）/ S
- 位点：`identifiability.cpp:112-123` `for (int it = 0; it < 300; ++it) { std::vector<double> w = x; chol_solve(L, n, w); … }` 后 `return sym_rayleigh(A, n, x);`（循环内无残差/位移检查）；`:100` `if (it > 3 && std::fabs(lam - prev) <= 1e-13 * std::max(1.0, std::fabs(lam))) { prev = lam; break; }`
- 上位依据：`identifiability.h:10-11`「λ_1：幂迭代（确定性起点 1/√n ⇒ 同输入逐位可复现）。λ_n：逆幂迭代（Cholesky 解）；H_eq 非正定 ⇒ λ_n = 0（不发布伪值）」；`:93-100` 的「边角语义」穷举式声明「调用方必须知道的两种『不可评估』与两种『未约束』」；正本 `docs/science/PHASE2_UPM.md:217-218`（κ 与 r_eff 必入 provenance）。
- 现状：两个 300 上限与 1e-13 判据都无出处（见 §5.1）；`lambda_max_power` 不满足判据时**静默返回**当前 Rayleigh 商（:106），`lambda_min_inverse` 跑满 300 步后**无条件**返回 Rayleigh 商（:123），二者都不向调用方暴露"是否收敛"，而 `P2Identifiability` 结构体里也没有对应旗标（头 :125-138 无该字段）。
- 差在哪（方向已钉死）：幂迭代对 λ₁ 从下方逼近（Rayleigh 商 ≤ λ₁），未收敛 ⇒ λ̂₁ < λ₁ ⇒ 阈值 `σ = τλ̂₁` 偏小 ⇒ `H_eq − σI` 更正定 ⇒ `spd_shifted` 更易为真 ⇒ **判绿**；逆幂迭代对 λ_n 从上方逼近（任意单位向量 `xᵀAx ≥ λ_n`），未收敛 ⇒ λ̂_n > λ_n ⇒ 发布 `kappa` 偏小 ⇒ 读数偏乐观。头 :93-100 把"不可评估"写成已穷举的两类，实际存在第三类（不收敛）未登记 ⇒ 与"无 silent default"的自家原则冲突。
- 后果：病态临界（λ₂/λ₁ → 1 或小特征值聚簇——正是该判据存在的场景）时，唯一可辨识性门在**低于声明阈值**的情况下判绿，写进 provenance 的 `kappa` 是被低估的病态度；对外主张「`identifiable ⟺ λ_n > τλ_1`」（头 :25）实际执行为「λ_n > τλ̂₁」。创新点③（天光面可辨识性）与创新点②相关的 UPM/`spatial_gain` 三处消费点共用此偏差。
- 定级：S2 —— 触发路径完整（`upm.cpp:2737`、`sky_plane.cpp:1099`、`spatial_gain.cpp:145` 均在生产求解内），但我未构造未收敛夹具实测偏差幅度（本批禁实跑），且判决仍由位移 Cholesky 兜底，故不上 S1。
- 怎么算修好：① 两条迭代各加收敛旗标（如 `|Δλ| ≤ 1e-13·max(1,|λ|)` **且**残差 `‖Ax−λx‖ ≤ √eps·‖A‖`），未收敛时：判决侧走保守（按 λ₁ 的上界复核 σ，或把 κ 记 +inf 并判红），读数侧在 `P2Identifiability` 增 `converged` 列并入 provenance；② `300`/`1e-13` 登记为具名常数并给导出（与 τ、eps、nl 的关系）；③ 补"两特征值重合"负例，断言不静默判绿；④ 头 :93-100 的"两种不可评估"改为可穷举且与实际分支一致。
- 置信：PARTIAL（代码事实与偏差方向 CONFIRMED；偏差量级需实跑夹具，本批禁跑）

### CR-21-17 · 注释承诺"两读法不一致取更保守者（判红）、不静默择一"，实现却在不一致分支折向 Cholesky 读数（方向为判绿），且 r_eff 的对外定义（惯性计数）不是实际判决依据
- 轴：C（主轴）/ R
- 位点：`identifiability.cpp:279` `if (spd_shifted && below != 0) below = 0;`（对照 `:17` `//     两个等价读法不一致时取**更保守者**（判红），并在注释中写明——不静默择一。` 与 `identifiability.h:17` 同句）
- 上位依据：`identifiability.cpp:12-16`「r_eff：位移矩阵 (H_eq − τλ_1 I) 的**惯性计数**（Sylvester 惯性定律）：#负主元(LDLᵀ) = #{λ_i < τλ_1}，用 Gill–Murray–Wright 的**修正主元** LDLᵀ…… 判决位用**直接**判据：chol_spd(H_eq − τλ_1 I) 成功 ⟺ λ_n > τλ_1 ⟺ r_eff == n」；正本 `docs/science/PHASE2_UPM.md:198`「**判决位唯一**」。
- 现状：不一致的两种调和方向相反——`spd_shifted && below != 0` ⇒ `below = 0`（抹平计数，**判绿方向**）；`!spd_shifted && below == 0` ⇒ `below = 1`（抬高计数，判红方向）。由 :284 `identifiable = (out->n_unidentified == 0)` 与 `n_unidentified = n_dead + below` 可直接推出：**判决位恒等于 `spd_shifted`（且 n_dead==0）**，惯性计数对判决零影响，只决定发布的 `rank_eff`/`n_unidentified`。
- 差在哪：无前元 Cholesky 的正主元检验（:40 `sum > 0.0`）在临界近奇异处可被舍入抬正（真 λ_n 略低于 σ 而最后一个 `sum` 舍入为 +1e-17），同一矩阵上 `count_nonpositive_pivots` 的 `dj > 64·eps` 判据会把该主元计入"不高于阈值"——即计数那把尺**更灵敏**；:279 规定不一致时以较不灵敏者为准，注释却称"取更保守者（判红）"，两条陈述不能同时为真。另：:12-13 把 r_eff 说成"惯性计数＝#{λ_i < τλ_1}"，而实际判据是顺序主子式检验，二者仅在精确算术下重合（修正主元已改变被分解矩阵）。
- 后果：(a) 判据在 `λ_n ∈ (σ − O(eps), σ]` 临界带内可判绿，与对外语义「`identifiable ⟺ λ_n > τ·λ_1`」（头 :25、正本 :198、`sky_plane.cpp:1083` 的注释、`upm.h:246`）不符；(b) `spd_shifted == true` 时 `below` 被强制 0 ⇒ 对外 `n_unidentified` 只剩 `n_dead`，即"计数发现的分辨率问题"对外消失；(c) 依赖 :17 承诺的保守性做审计的读者会得出相反结论。
- 定级：S2 —— 生产路径完整（同 CR-21-16 的三个调用点），但临界带宽度 ~eps 量级，且下游 `upm.cpp:2747-2757` 求 `C_theta = H⁻¹` 时另有 `if (!(ek > 0.0) …) inv_ok = false;` 的第二道兜底，未证实产物错值 ⇒ 不判 S1。
- 怎么算修好：三选一但必须同批改注释与正本措辞：① 真按"取更保守者"实现：删除 :279 的清零分支，`below = std::max(below, spd_shifted ? 0u : 1u)`，接受"PD 但主元 ≤ 容差"时判红（此时 `identifiable` 与 `rank_eff` 自洽）；② 或声明"判决位以位移 Cholesky 为唯一准绳，主元计数仅作读数"，则删 :17 与头 :17 的保守承诺、把 :12-15 改写成"读数来自修正主元计数，判决来自顺序主子式检验"；③ 无论哪条，正本 `PHASE2_UPM.md:198` 的"判决位唯一"须指向一个可核对算式，且 `sky_plane.cpp:1083`/`upm.h:246` 的 ⟺ 措辞同步。
- 置信：CONFIRMED（"identifiable ≡ spd_shifted"由 :279/:280/:284 直接推出；临界带内无前元分解被舍入抬正为该算法标准性质，未实跑复现具体样本）

#### 不报的核过项（本文件 + 本头）
- **超域/非法阈值是否降级为放行**（前言重点 ①）：否。`tau = max(请求值, 地板)`（:175-176）⇒ 把 `rank_rtol` 配成 0/负/NaN 时抬到地板（更严），方向偏红；判红不是错误（`return 0` + `identifiable=0`，头 :99/:143 与 `sky_plane.cpp:1126`、`upm.cpp:2742 return 3` 一致）。检索式：`sed -n '168,180p' lib/algorithms/coverage/src/identifiability.cpp`。
- **退化情形是否用合法数值冒充"未测"**（前言重点 ④）：否。κ 在秩亏 / 存在 dead 方向 / λ₁ 非正非有限时一律 `+infinity()`（:206、:249、:262-264），`lambda_min` 归 0（:205、:248、:258）；`dof` 可为非正且**不夹取**（:164-166），头 :156-157 明令"按无自由度处理而不是取 0"，UPM 消费点合规（`upm.cpp:1330-1332`「dof ≤ 0 ⇒ 置 NaN 并置 `chi2_red_defined=0`……**不得**写 0」）。`sky_plane.cpp:1207` 把该情形写成 `0.0`，属 `sky_plane.cpp` 的问题（不在本批清单，已登记为跨批线索）。
- **门是否设在未正则化矩阵上**（该判据最重要的科学口径）：核过成立——判决用 `H_red`（`sky_plane.cpp:1099`），`H_solve = H_red + λ_eff·P` 的 κ 仅作求解稳定性诊断（`sky_plane.cpp:1117-1124`），符合头 :73-74 与正本规则 5（`PHASE2_UPM.md:203-205`）。检索式：`sed -n '1096,1125p' lib/algorithms/coverage/src/sky_plane.cpp`。
- **列均衡是否消除参数单位自由度**：是——:224/:238 严格按 `D=diag(√H_ii)`、`H_eq=D⁻¹HD⁻¹` 实现，与头 :17-20 及正本 :196-197 同式；且 `equilibrate_min/max` 作诊断输出（:241-242）。
- **rc=1（数值未定义）与判红（rc=0）的语义区分**在两个全局调用点被正确保留（`sky_plane.cpp:1100-1106`、`upm.cpp:2741 return 8`）；在 UPM **逐控制块**循环里被折叠（`upm.cpp:1313-1318` 对 rc≠0 执行 `++out.n_blocks_rank_deficient` 并 `out.rank_eff += 0`，而 :1325 判决位 `identifiable = (n_blocks_rank_deficient == 0)` ⇒ "无法评估"并入"判红"同一计数器）。该位点在 `upm.cpp`（非本批独占清单），故登记为跨批线索、不计入本批 finding。
- `chol_solve`（:52-66）原地前代/回代的下标方向逐行核过（回代读 `b[k]`(k>i) 时该位已写入 x_k，标准 in-place 写法，无自污染）；`sym_rayleigh`（:68-78）的 `den > 0` 保护仅在 x 全零时可达，方向保守。
- Golub & Van Loan《Matrix Computations》§2.6.2 的节号归属、Hansen《Regularization Tools》v4.1 §1/§2.2/§2.3.1/§2.7.3 的节号与引文原文，本机无该文献载体可核 ⇒ 移入文末「读不动／需实跑」，不当作 finding。

---

## 6. `lib/algorithms/coverage/include/astro/phase2/integrate.h`（83 行，已全文读完）

**派单文字与清单不符处（按前言 §5.5 以清单为准并指出差异）**：前言重点 ② 要求在本文件核「创新点④球面面积交叠」的**面积精确性**（解析精确 vs 离散近似冒充）。实情：`integrate.h` 通篇**不含任何面积/立体角量**——它是 W8 的逐输出像素加权叠加 C 接口（`values/weights/support/accepted → signal/support/n_used`），声明的量只有加权均值、`max(accepted support)` 归约与五个 status。球面面积在本模块的角色是 `support = covered_area/A_cell`（无量纲比），其精确性口径在**本清单之外**的 `docs/contracts/DATA_SEMANTICS.md:442/:452`（见 CR-21-20 证据链）。据此把重点 ② 改为核「本头声明的口径与实现/生产消费方是否同一口径」，并把面积精确性与域约束落在 `support` 这条真实载体上。本批取证结论：本文件里不存在"面积解析式"，因此不存在"被离散近似冒充的解析实现"；真实存在的形态是**面积被上游 uint8 量化后以无量纲比进入本接口，而本接口不声明其步长与值域**（CR-21-18/20）。

### 6.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :9 | `w = SNR²/F_ref² = 1/σ_F²` | — | 唯一生产权重口径 | 公式导出 | `ASTROCS_DESIGN.md §3.1:173/175`（本文件 :14/:21 自引）＋`docs/science/PSF_SIGNAL_WEIGHT.md §4:72`「不存在口径选择键、口径枚举、口径配置项或口径产物」；生产构造点 `module_adapters.cpp:11897-11900 astrocs::v6::p2weight::weight_from_corrected_variance(vv, &w, &verr)` ⇒ 式与实现同一处 | — | 高 |
| :23 | `NaN/Inf/负权重 → INVALID_INPUT` | — | 权重值域下界 | 结构性不适用 | 值域判据；实现 `integrate.cpp:54-56`；`p2_validate_candidate_weights` 同判据（`integrate.cpp:13`） | — | 高 |
| :24 | `weight==0 → 合法但不贡献（ZERO_VALID_WEIGHT）` | `0` | 零权重哨兵语义 | 结构性不适用 | 合同语义（非标定值）；实现 `integrate.cpp:57`；正本 `docs/contracts/DATA_SEMANTICS.md §30.1`（消费点 `module_adapters.cpp:11998-12000` 依此区分 `NaN` 与 `0` 两态） | — | 高 |
| :25 | `support 非 finite 或 <=0` | `0.0` | support 下界资格 | **待确认** | 合同声明的上界 **[0,1]** 在本头与实现里都不校验：`DATA_SEMANTICS.md:33`「`support`：覆盖/有效支持度 [0,1]」、`:452`「无量纲 [0,1]（covered_area/A_cell，**>1 钳 1.0**）」；本文件 :48 亦只写「提供时须为**正有限**」⇒ 见 CR-21-18 | 保守方向：`support > 1.0 ⇒ INVALID_INPUT`。影响范围：`pr.support` 是 mosaic support 平面的唯一权威值（`module_adapters.cpp:12005`），>1 使 `area = support×A_cell` 超过单像素立体角 | 高 |
| :26-:28 | `output support = max(accepted support)`；「Stage2/ACR 只消费 pr.support，不再自行第二次 max/mean」 | — | canonical 归约器唯一性主张 | 待确认 | 归约式由实现唯一持有（`integrate.cpp:49-50`），B2-A7 订正注释（:42-46）说明作用域含零权 accepted 样本；"不再自行第二次归约"仅覆盖 support 一侧——生产方对**权重和**做了第二次求和（`module_adapters.cpp:11976-11977`），见 CR-21-19 | 保守方向：措辞扩为"不再自行第二次任何归约（含 Σw）"，并让接口发布 Σw | 高 |
| :30 / :55-:59 | status 枚举 `0..4`（OK/NO_CANDIDATES/ALL_REJECTED/ZERO_VALID_WEIGHT/INVALID_INPUT） | `0..4` | 状态基数 | 结构性不适用 | 枚举基数，不进科学语义；实现逐分支穷举（`integrate.cpp:24/:66/:72/:76-77/:88`），无第六态 | — | 高 |
| :47 | `weights 可空=等权` | `1.0` | 缺省权重（`integrate.cpp:51 double w = 1.0;`） | **待确认** | 与 :8-16 的"单一权重口径、equal 已删除"存在张力：等权以"无权重数组"形式重新可达。:20-22 声明生产不可达——实测生产写 `module_adapters.cpp:11954 stk.weights = weights.empty() ? nullptr : weights.data();`，而 `weights` 与 `vals` 同步 push（:11946-11947），故 nullptr 只在 `count==0` 时出现 ⇒ 落 `NO_CANDIDATES`，等权分支确不可触发；风险为语义层面 | 保守方向：`weights == nullptr ∧ count > 0 ⇒ INVALID_INPUT`（fail-closed），并加负例锁住"等权不可从后门进来" | 高 |
| :48 | `support 可空=1.0` | `1.0` | 缺省 support | **待确认** | 无出处：合同说 support = `covered_area/A_cell ∈ [0,1]`（`DATA_SEMANTICS.md:452`），"不提供 support" 与 "support=1.0（满覆盖）" 是两个事实，本头把前者兜底成后者；实现 `integrate.cpp:88/:92 out->support = (in->support != nullptr) ? sup_max : 1.0;` | 保守方向：null ⇒ 无定义（NaN 或 0 + 旗标），禁止兜底为满覆盖。影响范围：任何不传 support 的调用方直接发布 100% 覆盖 | 高 |
| :49 | `accepted 可空=全接受` | — | 缺省掩码 | 结构性不适用 | 逻辑缺省（无数值）；生产恒传（`module_adapters.cpp:11956`）但**无条件填 1**（`:11948 accs.push_back(1);`），资格权威实际在调用方的 `sm` 掩码（`module_adapters.cpp:11863-11869`「像素级 acc 仅作冗余守卫」）——与本头 :25 的措辞分工不一致，已并入 CR-21-19 | — | 高 |
| :50 | `std::uint32_t count` | — | 样本栈长度类型 | 结构性不适用 | 容器宽度 | — | 高 |
| :65,:67-:70 | `n_used / n_candidates / n_accepted / n_finite / n_positive_weight` | — | 显式计数器（「不靠 n_used 猜原因」） | 结构性不适用 | 计数定义与实现逐一对应（`integrate.cpp:23/:33/:36/:47/:58`）；OK 态下零权样本数 = `n_finite − n_positive_weight` 无歧义（非有限/负权会置 INVALID_INPUT 并早返回）⇒ 该主张成立，不报 | — | 高 |
| :74,:78 | 两个导出函数的返回码约定（`0=ok / 1=错误`） | `0` `1` | ABI 约定 | 结构性不适用 | 与 `integrate.cpp:15/:20` 一致；`p2_validate_candidate_weights(nullptr, n)` 返回 0（:12 真空通过）与 :78-79 合同句不冲突 | — | 高 |

被合并项声明：本文件数值即上表 12 行；`1.0` 缺省出现在 :47、:48、:64 三处注释与实现 `integrate.cpp:51/:88/:92`，已分行列出；`0`（零权/非正 support）出现在 :24、:25 与实现 :55/:57，同样逐位点列出。

### 6.2 findings

### CR-21-18 · `support` 的合同值域是 [0,1]（且 >1 钳 1.0），本头与实现只校验 `>0 ∧ finite`，把超域值原样发布为覆盖权威
- 轴：C（主轴）/ R
- 位点：`integrate.h:48` `const double* support;        // 可空=1.0；提供时须为正有限`；判决语义 `:26-27` `// - output support 唯一 canonical reducer：**max(accepted support)**（覆盖并集保守下界， 冻结语义）`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:33`「`support`：覆盖/有效支持度 [0,1]；`support=0` 表示无覆盖」；`:452`「support/…fits | 同上 | **无量纲 [0,1]**（covered_area/A_cell，**>1 钳 1.0**；`A_cell=4π/(12·nside²)`）| 无效像素 0.0」；`docs/algorithms/PHASE2_COVERAGE.md:23`「`support` = 逐像素覆盖支撑 [0,1]（样本级…）」、`:201`「support 样本级 [0,1]（SCI-INT-001 §2）」。
- 现状：唯一判据是 `integrate.cpp:44-47` `if (in->support != nullptr && (!std::isfinite(in->support[i]) || in->support[i] <= 0.0)) { invalid_input = true; continue; }` ⇒ 上界不校验；`sup_max = std::max(sup_max, in->support[i])`（:50）后在 :88/:92 写入 `out->support`。生产消费点 `module_adapters.cpp:12005` `sup_bin[base + p] = pr.support;   // canonical reducer max(accepted support)` ⇒ support 平面逐字节由本接口给出。
- 差在哪：同一件事（support 合法域）在合同面是闭区间 [0,1] 并写明 >1 要钳，在本头/实现面是 `(0, +∞)` 且不钳；本文件自称"唯一 canonical reducer"，因此**没有任何下游环节**会纠正越界值（`DATA_SEMANTICS.md:452` 的钳制发生在 Phase1 写盘侧，不在 mosaic 读入侧）。
- 后果：任一帧 support 平面因上游 bug/单位错误（例如把 `covered_area`[sr] 误当比值传入，或 A_cell 变更后未重钳）出现 >1 时，mosaic 的 support 平面输出 >1，而 `DATA_SEMANTICS.md:453` 的方差口径 `var_num_sum/covered_area²` 与 `:442` 的面积口径 `support×A_cell` 都直接用它 ⇒ 面积超过单像素立体角的物理不可能状态被判为合法产品。方向钉死：少一项上界校验 ⇒ 更易通过 ⇒ **偏绿**。
- 定级：S2 —— 生产路径完整（`module_adapters.cpp:11960 p2_integrate_pixel` → `:12005` 写 support 平面 → `:12759-12788` 下游读回消费），但产出错值需上游已给 >1 的 support（我未在在库夹具/产品中观测到该形态），故不判 S1。
- 怎么算修好：① :48 与 :25 的值域句改为「须 ∈ (0,1]；>1 ⇒ INVALID_INPUT」并注明钳制发生在上游写盘侧；② `integrate.cpp:44` 补 `|| in->support[i] > 1.0`；③ 补负例：`support = {0.5, 1.0000001, 3.0}` 三输入必须判红（现全绿）；④ 若改为"此处钳 1.0"，则 `DATA_SEMANTICS.md:452` 的"上游已钳"须同步删，否则两侧都钳会掩盖两次单位错误。
- 置信：CONFIRMED

### CR-21-19 · 接口不发布归一分母 Σw，迫使生产消费方自行做第二次权重求和，而两次求和的资格域不同（accepted/finite 门被绕过）
- 轴：D（主轴）/ C / R
- 位点：`integrate.h:62-72` 的 `P2PixelResult` 全部字段（`signal / support / n_used / n_candidates / n_accepted / n_finite / n_positive_weight / status`——**无** Σw）；配套主张 `:27-28` `// 不再自行第二次 max/mean。`
- 上位依据：本文件 :19「构造后先经 `p2_validate_candidate_weights`」的单一口径链条；`docs/contracts/DATA_SEMANTICS.md §30.1` 的 `ivar_mosaic = W = Σ ivar_i（正权样本）`（消费点注释原文 `module_adapters.cpp:11377`「ivar_mosaic = Σ ivar_i（帧索引序, **正权样本**）」）；正本对"资格"的定义含 accepted 掩码：本文件 :23-25。
- 现状：`integrate.cpp:52-58` 只在 `accepted ∧ value finite ∧ support∈(0,∞) ∧ w>0` 时累加 `wsum`（:58-60），而 `wsum` 是**局部变量、不出现在结果结构里**；于是 `module_adapters.cpp:11976-11977` 自行重算 `for (size_t i = 0; i < vals.size(); ++i) { if (weights[i] > 0.0 && std::isfinite(weights[i])) wsum += weights[i]; }`，其结果在 `:11981/:12004` 写进 `wsum_bin` = 产品件 `p2_integrated_wsum.bin`（`:12043/:12091` 登记为 artifact）。
- 差在哪：生产确实做了"第二次归约"，与本头 :27-28 的自我约束句冲突（该句字面只点名 max/mean，意图是"归约口径唯一"）；更实的是**两次归约的样本集不同**——本接口的 `wsum` 跳过 `accepted==0`、value 非有限、support 非正的样本，第二次求和只看 `weights[i] > 0 ∧ finite`，**不查 `accs[i]`、不查 `vals[i]` 有限性**（这两门在本头 :23-25 是资格定义的一部分）。当前生产因上游过滤（`module_adapters.cpp:11881-11884 if (!std::isfinite(v) || !std::isfinite(sp) || sp <= 0.0 || !acc || sm == 0) continue;`）恰好等价，且 `accs` 被无条件填 1（`:11948`）使本接口的 accepted 门在生产恒真 ⇒ **等价性由调用方现状维持，不由接口保证**。
- 后果：任何后续改动（复用本接口到别的调用方、给 `accs` 填真实掩码、第二次求和处漏一个条件）都会让 `signal = Σwv/Σw(内部)` 与 `ivar = W = Σw(外部)` 落在不同样本集上 ⇒ 发布的 signal 面与 W/ivar 面对彼此不自洽 ⇒ 创新点②（跨帧绝对 SNR）与 `variance = var_num_sum/covered_area²`（`DATA_SEMANTICS.md:453`）读数错值；这是"接口缺一个字段 ⇒ 调用方必须重算 ⇒ 重算与实现各说各话"的结构型缺陷，正是前言重点 ⑤ 问的"接口声明与实现是否同一口径"的失败形态。
- 定级：S2 —— 两个求和点与产品写盘点逐字定位（缺"某次真实调用中两值不等"的复现，故不判 S1）。
- 怎么算修好：① `P2PixelResult` 增 `double weight_sum;`（reducer 实际使用的分母），规定 `n_positive_weight == 0 ⇒ weight_sum = 0`；② `module_adapters.cpp:11975-11977` 的第二次求和删除、改用 `pr.weight_sum`，`:11981-11983` 的非有限/≤0 降级分支保留；③ 本头 :27-28 扩为"不再自行第二次任何归约（含 Σw、max、mean）"；④ 补断言 `pr.weight_sum == Σ weights[accepted ∧ finite]` 与一个"含 rejected 但权重>0 样本"的夹具（该夹具今天即可让两口径差出一个正数）。
- 置信：CONFIRMED（两处求和代码、上游过滤、产品件登记逐字读到）

### CR-21-20 · 本头把 `support` 称为「覆盖并集保守下界」却不写量纲、分辨率与来源：产品面上的"面积"实为 1/255 量化值，接口层零声明
- 轴：S（主轴）/ C
- 位点：`integrate.h:26-27` `// - output support 唯一 canonical reducer：**max(accepted support)**（覆盖并集保守下界， 冻结语义）；Stage2/ACR 只消费 pr.support，`；`:64` `double support;               // max(accepted support)（canonical reducer）`
- 上位依据：`docs/contracts/DATA_SEMANTICS.md:442`「**B2-A15**：Phase1 末端（`astro_sphere_sink.cpp write_hips_phase1`）把 sumArea 先按 `lround(255·clamp(sumArea/A_cell,0,1))/255·A_cell` 的 **uint8 面积比连续缩放**…（`covered_area_model="support_ratio_x_A_cell"`）」；`:452`「support = covered_area/A_cell ∈ [0,1]」；`docs/algorithms/PHASE2_COVERAGE.md:201`。
- 现状：本头的 support 字段既不给值域（CR-21-18）也不给分辨率/来源；`P2PixelStack::support` 的入参实际是上游 uint8 量化后再乘 A_cell 的面积比，本头与实现（`integrate.cpp:50`）只做 `max` ⇒ `pr.support` 可分辨步长为 **1/255**，而它被当作 mosaic 的覆盖权威写盘（`module_adapters.cpp:12005`）。
- 差在哪：一句话里三个未定义项——"保守下界"未说对哪个测度取下界、"覆盖"未给量纲（sr 还是比值）、"accepted" 的作用域只在实现注释（`integrate.cpp:42-46` B2-A7）里解释。对前言重点 ② 的正面回答：面积链在 Phase1 写盘侧被 uint8 量化（离散近似），mosaic 侧的本接口既不重算面积也不声明步长 ⇒ **不存在"解析精确被离散冒充"的伪装**（无解析实现可冒充），存在的是"离散近似无人声明"。
- 后果：对外主张「加性天光与无接缝叠加」（AGENTS.md §2 / SCI-C）中"覆盖"这一量的精度与语义在产品接口上不可见：读者按 `pr.support` 反推 `area = support × A_cell` 会以为拿到连续量，实际带 0.4% 台阶；tile 级归一分母（`DATA_SEMANTICS.md:442`「归一分母；≤0/非有限 → signal=NaN、support=0」）继承该台阶并进入 `var_num_sum/covered_area²`。方向：量化误差双向，但接口零声明使**判据无从验证**（不是恒真门，而是无门）。
- 定级：S3 —— 属语义/量纲未声明，后果落在对外可读性与精度主张；写不出"因未声明步长而产物错值"的触发路径。
- 怎么算修好：本头补三行（量纲=无量纲面积比 `covered_area/A_cell`、值域 `(0,1]`、有效步长与来源=上游 uint8 量化并指向 `DATA_SEMANTICS.md:442`），并把"保守下界"改写为可核对句（"同像素多帧取 max = 以最大单帧覆盖代替并集，系统性 ≤ 真并集"）；若 mosaic 需要连续面积，须新增以解析球面交叠算面积的路径并登记（本批 6 个文件内不存在这样的实现，见文末）。
- 置信：PARTIAL（量化事实与消费链逐字读到；缺"1/255 台阶对具体判据的实际影响"——需实跑：同一 tile 用连续面积与量化面积各算一次归一，比对 signal/variance 偏差）

#### 不报的核过项（本文件）
- **`count == 0` 与 `values == nullptr` 都映射为 NO_CANDIDATES**（`integrate.cpp:23-26`）：不报为 fail-open——返回码 0 + 显式 status，调用方 `module_adapters.cpp:11961` 检查 rc，且 :11984-12002 对每个 status 分支单独处置（OK/ZERO/其余的 NaN 语义按 `DATA_SEMANTICS.md §30.1` 区分，明确「禁 0/±Inf 伪装」注释在 :11994）；`n_candidates` 仍等于 count，读者可分辨"给了 N 个但无值数组"的调用方 bug。检索式：`sed -n '19,30p' lib/algorithms/coverage/src/integrate.cpp` 与 `sed -n '11960,12005p' lib/infrastructure/scheduler/src/module_adapters.cpp`。
- **单一权重口径**（:8-16）：核过成立——`weight_mode` 与 `legacy_allow_weight_fallback` 在解析侧 fail-closed 拒绝（`stage2_common.cpp:455-468`），生产权重构造只有一条（`module_adapters.cpp:11897-11900`）；`fallback` 恒 false（`module_adapters.cpp:11462 bool fallback = false;   // legacy 等权降级成功路径已删除（恒 false）`）⇒ 等权 `w=1.0` 分支不可达。检索式已附。
- **五态 status 互斥穷举**：`integrate.cpp:63-77` 的分支顺序（先 INVALID，再按 `n_accepted` 分 ALL_REJECTED / ZERO_VALID_WEIGHT，最后 OK）与 :29-30 枚举一致，无第六态、无两态同置。
- **`p2_validate_candidate_weights(nullptr, n)` 真空通过**：与 :78-79 合同句不矛盾，且调用方 `module_adapters.cpp:11972` 传的是与 `stk.weights` 同一个 `weights.empty() ? nullptr` 表达式 ⇒ 不存在"校验一个数组、积分用另一个"的错位。
- **ZERO_VALID_WEIGHT 下仍发布 support**（`integrate.cpp:81-82`）：与 :26-28 的"support 与 signal 解耦"一致，且消费方 `module_adapters.cpp:11996-11998` 明确 `wsum = 0.0`（不写 NaN）并注释理由——"有覆盖但方差不可用"不被误判成"无覆盖"。方向为**避免**误降级，不报。

<!-- PROGRESS: 6/6 -->

---

## 本批最高危三条（只挑，不复述）

1. **CR-21-15 / CR-21-16 / CR-21-17（`identifiability.cpp` 三连）** —— 全仓唯一的可辨识性判据上挂着三个零出处数值旋钮（`64.0·eps` 的第二把尺 + 其"归一因子"可证恒等于 1、`300`/`1e-13` 的迭代上限与判据无收敛旗标、两读法不一致时注释承诺判红而代码折向判绿）。落点：创新点③的门与写入 provenance 的 `kappa`/`rank_eff`/`n_unidentified` **同向偏乐观**，并被 `docs/science/PHASE2_UPM.md:199`「阈值只有一个」直接反证。
2. **CR-21-19（`integrate.h` 接口缺归一分母）** —— `P2PixelResult` 不发布 `Σw`，逼生产消费方 `module_adapters.cpp:11975-11977` 自行第二次求和，且该求和绕过本头 :23-25 定义的 accepted/finite 资格门；signal 面与 ivar/W 面（写入 `p2_integrated_wsum.bin`）可建立在不同样本集上，二者等价性只由调用方现状维持、不由接口保证。落点：创新点②跨帧绝对 SNR 的不确定度面。
3. **CR-21-06 + CR-21-09（`hips_properties.{h,cpp}`）** —— 自称"无 silent default"的严格校验器漏读 `hips_ordering`/`hips_version`（其余三个入口都要求），使 RING 错读只剩"tile 头恰有 `ORDERING` 卡"这一道外部兜底；`kMaxOrder = 20` 挂的"ARCH-P3 §3 内存守卫"锚与守卫公式无关（该节明写"与输出总图大小无关"），并与 I/O 合同 `ACS_HIPS_ORDER_MAX = 29` 并存两套上界，`plan_estimator.h:18-26` 还把 20 当全局权威（循环锚）。落点：`acsd export` 的几何正确性与"哪些 order 合法"的对外答案。

（本批 **无一条判到 S1**：三条 S2 候选的"该退化形态有真实输入可达"这一跳均未取证到，已在各条 `定级` 字段写明缺哪一跳。）

## 我读不动或需要实跑才能定的

**读不动（缺载体，不当作 finding）**
- Golub & Van Loan《Matrix Computations》§2.6.2 是否为列均衡/矩阵缩放的对应节次；Hansen《Regularization Tools》v4.1 手册 §1/§2.2/§2.3.1/§2.7.3 的节号与引文原文（`identifiability.h:28-30/:67-72` 与 `:105` 的引用准确性）——本机无该文献与 PDF，只能核"本仓是否另有一份登记"（`git grep -rn "Regularization Tools" -- docs` 未产出可核对条目）。
- NumPy `matrix_rank`、MATLAB `rank`、SciPy `pinv/lstsq` 的**当前版本**默认阈值语义（影响 CR-21-13 的措辞强弱：域错配是我方能独立证明的，"三家现行为是否仍如注释所述"需在线文档或本地安装核对）。
- `docs/contracts/DATA_SEMANTICS.md:442` 的 uint8 面积量化是否仍与 `astro_sphere_sink.cpp` 现状一致（该文件不在本批清单，我只据条文引用，未逐行读实现）。

**需实跑才能定（本批禁跑构建/ctest/`eng/**`，列出应跑项与判据）**
1. `eng/tests/unit/v6_p2_identifiability`：加 λ₁ ≫ 1 的近相关块夹具与"两特征值重合"夹具，量测 `rank_eff`/`n_unidentified`/`kappa` 与独立 SVD Oracle 的偏差 ⇒ 定 CR-21-15/16 的实际幅度（现在只有方向，没有数）。
2. 同一 H 的 λ 域与 σ 域阈值对照（`κ(H)` vs `κ(A)`）：跑一次 LAPACK/`numpy` 对拍，确认 CR-21-13 的"同一 1e-10 差 5 个数量级"在真实规模（n_free 数千）下的判红率变化 ⇒ 决定是改登记面措辞还是改值。
3. `p2_integrate_pixel` 与 `module_adapters.cpp:11976` 双口径差值：构造含 `accepted==0 但 weight>0` 的样本栈（当前生产 `accs` 恒 1，需临时注入），比对 `pr.signal` 用的分母与 `wsum_bin` ⇒ 把 CR-21-19 从"结构缺陷"升级为"可复现错值"或排除。
4. `support = 3.0` 与 `support = 1.0000001` 的端到端 export：确认 >1 值是否真会落到 `sup_bin`（`module_adapters.cpp:12005`）而无任何下游钳制 ⇒ 定 CR-21-18 是否升 S1。
5. `hips_product_validate` 对 0 字节 / 截断 `Npix0.fits` 与 `Npix.fits`（无数字索引）的判定，以及 export 端到端是否输出"无覆盖"而非报错 ⇒ 定 CR-21-08 的 `fetch_tile` 那一跳（我未逐行读 `fetch_tile`）。
6. RING 编号且 tile 头**无** `ORDERING` 卡的输入产品做一遍 `acsd export` ⇒ 唯一能把 CR-21-06 推到 S1 或彻底排除它的实验。
7. `execution` 块相关：`{"cpu_workers":0}` 经 `p2_stage2_make_upm_cfg`（`stage2_common.cpp:548`）后 `upm.cpp:614` 的实际 worker 数与墙钟对照（CR-21-03）；以及 `acsd mosaic --cpu-workers` 是否存在（合同文档声称，实测 `git grep -- lib/infrastructure/cli` 无该键）⇒ 需跑 CLI 帮助面确认。
8. CI 的 source_binding 核验（`eng/contracts/data/v6_clause_registry_v1.json` 的 24 条绑定是否全部落在被绑文件内）：CR-21-12 我只独立核了 `FZ-AP2S-RANK-RTOL` 一条（`docs/algorithms/UPM_SOLVER.md` 内 `1e-10` 与该 ID 均零命中），其余条目需机器门跑。

---

## 只读纪律回执

开工（原文）：

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
---HEAD---
c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9
```

收工（原文，与本节点开工时**不一致**，差异见下）：

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? drz.html
?? photom.rst
?? site/
```

**工作树漂移登记（非本节点所为）**：开工时基线为前言 §1 预期的两条（`?? ACSD整治工作包_AUDIT-06.zip`、`?? site/`），收工时多出 `?? drz.html`、`?? photom.rst` 两个根级未跟踪文件。本节点全程零仓库写入（唯一写过的文件是 `独立审计/证据/通读-CR-21.md` 与 `独立审计/复算件/CR-21/reorder.py`，均在审查目录内），HEAD 未变（开工/收工均为 `c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`），故该两文件应属同机并行的其它节点或前台会话，登记供负责人核对是否越出 AGENTS.md §7 的根目录固定条目清单（根级新条目需先登记并经负责人确认）。

零 git 写、零 `git config` 写、未跑构建/ctest/`run_checks.py`/任何 `eng/**` 脚本、未 import 仓库 Python、仓库内零写入。中间件仅 `独立审计/复算件/CR-21/reorder.py`（成稿分节顺序校正用，非取证）。节奏合规：骨架先落盘（`PROGRESS: 0/6`）后开读，每读完一个文件即 append 处置表＋findings 并 bump 进度；单文件取证调用未超前言 §5 的 20 次上限，单条 finding 未超派单的 8 条上限。
