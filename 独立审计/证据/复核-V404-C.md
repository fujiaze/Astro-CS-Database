# 复核-V404-C · AUD-404 退役/死代码清单的引用面计数与分组抽核（第②层）

## 文件头

- **复核对象**：`独立审计/证据/AUD-404-退役与死代码清单.csv`（126 文件行 = 1 表头 + 125 数据行 × 9 列）。配套叙述件 `AUD-404-退役与死代码清单.md` 只作为分组与交叉引用的主张来源。
- **条目数**：本批复核 **51** 行（第 1 组 14 行全查 + 第 2 组 18 行 + 第 3 组 19 行），外加一项全表性质的 `CSV 行 N` 内部指向核查（对象＝叙述件里全部行号指向）。
- **基线**：HEAD=`c8f64e9a`。
- **只读纪律**：未跑构建 / `cmake` / `ctest` / `run_checks.py` / 任何 `eng/**` 脚本；未 `import` 仓库内 Python；未写仓库任何文件；CSV 与 md 本体只读；判"不存在/已删"只用 `git ls-files` 与 `git log --diff-filter=D`；检索只用 `git -C "..." grep`（本机无 `rg`）；负证据不以 `head` 截断。零 git 写。
- **开工 `git status --porcelain` 原文**（恰两行，与工包基线一致）：

  ```text
  ?? ACSD整治工作包_AUDIT-06.zip
  ?? site/
  ```

- **与派单不符处（按 §2.9 以清单为准）**：派单说第 1 组含"可直接删 14 条"，但叙述件 §1.1 的 14 条是按**文件行号**给出的行集，其中只有 1 条（行 14 `api_inventory.csv:325`，已被并行代理 V404-S 推翻）处置列字面写"可直接删"；第 1 组的实际成员判据是 §1 的机制规则（处置为删除类 ∧ `连带改动`=无 ∧ `风险` 不以"高"开头 ∧ 门承载列="否"）。本复核因此同时抽核"分组机制"与"分组结论"两层。
- **行号口径（本复核自证，见 §P 节）**：第①层的 `CSV 行 N` = **CSV 文件行号，表头算第 1 行**（数据行 k = 文件行 k+1）。证据：§1 表"待证＝CSV 行 115"与 §3.1"未裁 1 条＝行 115"唯一对应类别列写"待证"的那一行（文件行 115）；§1.1 的 14 条里"最干净的 5 条源文件注释"恰为文件行 7-11；§1.1"改而不是删的两条 `executor.h:22`／`commands.cpp:548`"恰为文件行 36／37。**全文从未声明这条口径。**

## 我的四档重数规则（与第①层同档名、规则自定并公开）

对给定 token（符号名／文件名／路径串），用 `git -C <repo> grep -n -w -e <token>` 得命中行，再按命中**所在文件的路径**归档：

- **生产 P**：`lib/**` 与根 `CMakeLists.txt`、`eng/cmake/**` 中，路径不含 `/tests/`、不在 `*/third_party/*` 者。理由：`CMakeLists.txt:4` 自记"显式源列表, 禁 GLOB"，故构建归属以"CMake 文本是否点名"为代理判据（与第①层 §0 同一代理判据，可比）。
- **测试 T**：`eng/tests/**`、`eng/ci/**`？——**否**。本档只放 `**/tests/**` 与被测命名件（`*_test.*`、`test_*.*`、`*_probe.*`、`*_oracle.*`），不论在 `lib/` 还是 `eng/`。
- **文档 D**：`docs/**` 与 `实验/**` 下的 `.md`；`lib/**/README.md`、`lib/**/memory.md`。
- **登记 R**：机器可读登记面 = `docs/**/*.csv`、`docs/**/*.json`、`docs/**/*.yaml`、`eng/contracts/**`、`eng/ci/**`、`eng/packaging/**`、`artifacts/**`。

与第①层的差异：第①层把 `eng/ci/**` 的门脚本与台账一并算进"登记"，把 `eng/tools/**` 也算"登记"；我不单列 `eng/tools/**`，按扩展名归 D 或 R，故**逐档可比、总数可能不同**，凡差异 >1 行都要写明是口径差还是计数错。

## 抽样计划

- **第 1 组（可直接删，14 行，全查）**：文件行 7、8、9、10、11、18、24、32、36、37、47、49、51、53。
- **第 2 组（先解引用再删，53 行，抽 18 行＝34%）**：文件行 2、3、4、5、6、12、13、15、16、19、20、23、33、35、39、48、60、67。覆盖类别：退役未清理（2-6）、登记面残留（12、13、15、16、19、20、23、33、35、39）、占位模块（48、60、67）。
- **第 3 组（不许删，57 行，抽 19 行＝33%）**：文件行 29、38、41、42、55、56、57、65、74、75、76、80、94、95、105、107、112、116、124。覆盖类别：登记面残留（29、74、75、76、95、124）、退役未清理（38?→见下）、死代码保留（55、56）、占位模块（41、42、57、65、112）、重复实现（105、107）、模板复制残留（80）、未跟踪产物名（94）、退役拒绝面（116）。
- **传递可达性关口**：凡抽到的"可删"结论，一律再过一次"`#include` 闭包 + CMake 源清单"两关；已知假阳形态＝头文件被生产翻译单元文本 include 但因而不是声明符号而被 `git grep -w <符号>` 判零。

## 判定条目（51 条）

### 共用取证块（下面多条引用，不重复贴）

**E1 · 五枚退役符号的全树重数**（命令：`python 复算/V404-C/bucket.py <token>`，内部即 `git -C <repo> -c core.quotepath=false grep -n -w -e <token>` 全跟踪树，再按上面"我的四档规则"归档）：

| token | 全树命中 | P | T | D | R | 第①层声明（scope=`lib docs eng CMakeLists.txt`） |
|---|---|---|---|---|---|---|
| `p2_upm_normalized_weights` | 34 | 3 | 0 | 19 | 8（另 R-TOOLS 4） | 生产2/测试0/文档16/登记7＝25 |
| `p2_upm_ma_component_of_control` | 4 | 2 | 0 | 1 | 1 | 4 ✓ |
| `p2_sample_sky` | 4 | 2 | 0 | 1 | 1 | 4 ✓ |
| `p2_sample_sky_cached` | 4 | 2 | 0 | 1 | 1 | 4 ✓ |
| `p2_reject_stack_resolve_ex` | 5 | 2 | 0 | 2 | 1 | 5 ✓ |

把第①层 scope 套到我的档上可精确复现其 25：其"生产 2"＝`upm.h:181`＋`upm.cpp:1999` 两条注释本身；其"文档 16"＝`docs/**` 全部（**含 `docs/TRACEABILITY.csv:2`、`docs/architecture/api_inventory.csv:325` 两个机器登记件**）＋ `lib/**/*.md` ＋ `lib/algorithms/upm/module.yaml:85`；其"登记 7"＝`docs/contracts/API_CONTRACTS.csv:280`＋`eng/ci/prod_wiring_baseline.json`×2＋`eng/tools/quality/contracts/fixtures/check_api_contracts/`×3＋`eng/tools/quality/v19r3_traceability.py:62`。**口径不自洽点**：`docs/TRACEABILITY.csv` 与 `docs/architecture/api_inventory.csv` 与 `docs/contracts/API_CONTRACTS.csv` 同为"逐行机器登记件"，前两枚进"文档"、第三枚进"登记"；`lib/**/module.yaml`（模块身份合同正本）进"文档"。这不是算错，是**把在册载体混进"文档"档**，直接导致行 14／18／24 被算成"无门承载"。

**E2 · `p2_upm_normalized_weights` 有一个真调用点在 `实验/`**（第①层 scope 不含 `实验/` ⇒ 系统性假阴）：

```text
实验/additive-sky-seamless/code/reverse_verify/smooth_lambda/upm_sweep.cpp:236:  const int wrc = p2_upm_normalized_weights(obs.data(), n_obs, &cfg, wcell.data());
实验/additive-sky-seamless/code/reverse_verify/smooth_lambda/build.sh:15:  "$HERE/upm_sweep.cpp" \
实验/additive-sky-seamless/code/reverse_verify/smooth_lambda/analyze.py:53: 注意：公共 API p2_upm_normalized_weights 仍用绝对门 s>1e-12（已登记缺陷…）
```

即 SCI-C 实验单元（`实验/additive-sky-seamless`＝AGENTS §5 三个创新点之一）的复现程序**现场 g++ 编译并链接真实 `upm.cpp` 后调用这枚"已退役、全仓零消费者"的符号**。另 4 枚符号全树命中里无 `实验/` 条目（已逐条核 E1 明细），故该否证只落在 `normalized_weights` 一族。

**E3 · 传递可达性关口（本批实测的那处危险假阳类）**：`lib/infrastructure/aio/include/aio_ahpx_format.h` 被生产头以相对路径文本 include：

```text
lib/infrastructure/aio/src/ahpx/aio_ahpx_reader.h:4:#include "../../include/aio_ahpx_format.h"
lib/infrastructure/aio/src/ahpx/aio_ahpx_writer.h:4:#include "../../include/aio_ahpx_format.h"
eng/tests/unit/ahpx_hips_format_test.cpp:29:#include "../../../lib//infrastructure/aio/include/aio_ahpx_format.h"
```

而 `aio_ahpx_{api,reader,writer}.cpp` 正是本清单行 57-59 判"不许删"的 AHPX-WEIGHT-RETIRED 门唯一执行对象。⇒ 按"直接 grep 文件名零命中"把 `aio_ahpx_format.h` 送进删除名单会当场断掉生产库与该门测试的编译（见行 80 判定）。同型证据：`lib/include/astrocs/core/executor.h` 被 `lib/infrastructure/scheduler/src/executor.cpp:21` 与 `.../src/executor_runtime.h:28` include（见行 36 判定）。

### 第 1 组（全查 14 条）

## CSV行7 · coverage/src/upm.cpp:1999 单行退役注释＝可直接删 —— **判定：降级**
- 独立取证：按第①层声明的 scope（`-- lib/algorithms/coverage/src`）复算 `p2_upm_normalized_weights`＝**恰 1 行，即本行**，计数"生产1/测试0/文档0/登记0"在其 scope 内成立；`eng/ci/retired_code_allowlist.json` 内**无** `upm.cpp`/`upm.h`/`sampler.*`/`rejection.*` 任何条目（`git grep -n -e upm.cpp … -- eng/ci/retired_code_allowlist.json` ⇒ 0），故删本行不触发豁免台账失配；CON-DOC-SYMBOLS 语料只 `*.h`（§1.2 已实测），删 `.cpp` 注释不判红。
- 与成稿差异：**动作（删本行注释）成立，四档与"零消费者"凭据不成立**——见 E2，该符号在 `实验/additive-sky-seamless` 有真调用点，"全仓零消费者声明已撤下"这句话本身是错的；四档"已退役"应为"实现已删但实验单元仍调用（复现链已断）"。
- 影响 D8 的处置：进任务书，但**从"可直接删"改为"删注释＋同批改 `实验/…/upm_sweep.cpp` 的调用（改指现行入口或如实登记该实验单元已不可复现）"**；E2 另立一条独立缺陷上呈（SCI-C 复现链对一个已删符号的依赖）。

## CSV行8 · upm.cpp:2849 同上 —— **判定：确认**
- 独立取证：E1 第 2 行——`p2_upm_ma_component_of_control` 全树 4 命中，其中 `lib/algorithms/coverage/src/` 内恰 1（本行）；无 allowlist 条目（同上）；`.cpp` 不入 CON-DOC-SYMBOLS 语料。
- 与成稿差异：无（仅档名标签同 E1 的口径问题：本行被记为"生产 2"里的 1，实为注释自身）。
- 影响 D8 的处置：进任务书（可直接删）。

## CSV行9 · sampler.cpp:1275 同上 —— **判定：确认**
- 独立取证：E1 第 3 行，`lib/algorithms/coverage/src/` 内恰 1；无 allowlist 条目；`-w` 已验证不与 `p2_sample_sky_cached` 串档（后者独立 4 命中）。
- 与成稿差异：无。
- 影响 D8 的处置：进任务书。

## CSV行10 · sampler.cpp:1277 同上 —— **判定：确认**
- 独立取证：E1 第 4 行，同上。
- 与成稿差异：无。
- 影响 D8 的处置：进任务书。

## CSV行11 · rejection.cpp:2223 同上 —— **判定：降级**
- 独立取证：E1 第 5 行——`p2_reject_stack_resolve_ex` 在 `lib/algorithms/coverage/src/` 内恰 1（本行）；但全树另有 `docs/algorithms/PHASE2_REJECTION.md:109` 逐符号表行 `| p2_reject_stack_resolve_ex | :2203-2217 | 规划+执行合并入口 |`，落点正是本文件被删定义的区间；而 `check_alg_line_anchors.py:64` 的 `SYMBOL_GLOB = "docs/algorithms/*.md"` ⇒ 该文件在**文档行锚门的语料内**。
- 与成稿差异：`连带改动＝无` 不成立——本行的同族文档锚（PHASE2_REJECTION.md:109）与 CSV 行 6 的 `连带改动=docs/algorithms/...` 是同一枚符号的同一处锚，两行却一个记"无"一个记有；且现网该范围锚是否已因定义删除而越界未裁（禁跑门）。
- 影响 D8 的处置：不进"可直接删"档，**并入第 2 组批 A**，先决动作＝同批核 `docs/algorithms/PHASE2_REJECTION.md:109` 的 `:2203-2217` 范围锚。

## CSV行18 · docs/algorithms/UPM_SOLVER.md:115 判"可直接删" —— **判定：推翻**
- 独立取证：本行 `门/合同承载` 列**自己写的是"是——F1 映射到符号并给测试锚 UPMW-001..003，锚与 docs/TRACEABILITY.csv:2 的 tests 列互为凭据"**；文件在 `SYMBOL_GLOB="docs/algorithms/*.md"`（`eng/tools/doccheck/check_alg_line_anchors.py:64`）语料内；`docs/audit/inventory.csv:8` 把 `docs/algorithms/UPM_SOLVER.md` 登记为 UPM 域正本。同时 §1.2 批 A 的 range 写作"CSV 行 12-23"，**已把本行圈进第 2 组**。
- 与成稿差异：分组自相矛盾——同一行同时出现在互斥的第 1 组（§1.1 列出 18）与第 2 组批 A（§1.2 区间 12-23）；且第 1 组的准入条件（§1.1 明文"门/合同承载列为'否'"）本行不满足。**并行代理 V404-S 已核机器表分组互斥＝全集，故错在 §1.1 的手写行集，不在 CSV 的机制列。**
- 影响 D8 的处置：不进任务书的"可直接删"档；改列第 2 组批 A，先决动作＝与 `docs/TRACEABILITY.csv:2`（本表行 15）同批改，并按行锚族的顺序"文档侧→头文件注释"。

## CSV行24 · lib/algorithms/upm/memory.md:57 判"可直接删（删除该行或改写）" —— **判定：降级**
- 独立取证：`lib/algorithms/upm/memory.md:1` 逐字为 `# memory.md — astrocs.p2.upm（P2-UPM-DOC 冻结）`，`:6` 记"三件套（README/module.yaml/memory.md）…冻结"；`lib/algorithms/upm/README.md:173`、`docs/modules/phase2_upm.md:18,253`、`docs/modules/registry/astrocs.phase2.upm-{apply,fit}.md:37-38` 均以"合同三件套"点名该文件 ⇒ 它是**模块合同冻结件的载体**，不是注释。本行内容与 E1 同源（列 `p2_upm_normalized_weights :1325` 等实现行锚）。`eng/tools/doccheck/check_doc_index.py:118 SKIP_SUFFIX=("/memory.md",)` ⇒ 它被文档索引门**排除**，故"删一行不判红"这一半成立；但 `eng/ci/known_failures.json:65` 存在 `lib/phase1_session/memory.md:118` 这种**行号式在册指向的先例**。
- 与成稿差异：本表**行 110** 对同一批文件（`lib/algorithms/upm/{module.yaml,README.md,memory.md}`）判"不许删（模块身份登记正本）"——两行同族却一"可删一行"一"不许删"，处置文字未声明"文件级 vs 行级"的区分 ⇒ 实施代理按文件级理解会撞车。且同族的 `README.md:127` 被放第 2 组、本行被放第 1 组，无差异理由。
- 影响 D8 的处置：不进"可直接删"档；改列第 2 组，处置只保留"改写"（删行＝动冻结合同件，须走变更流程，AGENTS §6）。

## CSV行32 · eng/tools/quality/contracts/fixtures/check_test_contracts/ 整目录＝可直接删 —— **判定：确认**
- 独立取证：`git grep -n -e check_test_contracts` 全树命中只有 5 处，全部指向**门脚本本身**而非夹具目录：`eng/ci/checks.json:1151`（command 登记）、`eng/ci/polarity_evidence.json:228` 与 `eng/ci/polarity_probe.py:164`（极性证据：临时树删 `docs/TRACEABILITY.csv` 单位节 ⇒ 该脚本必须非 0）、`eng/tools/quality/contracts/check_test_contracts.py:2/63/72`（自身文件名）、`generate_contract_report.py:20`（工具名列表）。再在 `eng/tools/quality/contracts/` 内 grep `fixtures` ⇒ **只有 `check_doc_symbols.py:146` 一处**，`check_test_contracts.py` 不读任何夹具路径。`eng/ci/checks.json` 里被点名的夹具目录是 `eng/tools/quality/contracts/fixtures/check_full_integration/**`（:1926,:1962）与 `eng/tools/quality/fixtures/**`（:444,:472），不含本目录。
- 与成稿差异：无（计数"测试3/其余0"与本目录 3 个文件自计一致）。补充：同族 `fixtures/check_api_contracts/`（本表行 30）判第 2 组、本行判第 1 组，而两者"无人读取"的实测结论同型——不对称只在行 30 的夹具被 `p2_upm_normalized_weights` 命中面（E1）牵连，成立。
- 影响 D8 的处置：进任务书（删整目录或接线均可），但**须在任务书里保留"接线"分支**：`ENGINEERING_SPEC` 要求门带 `--self-test` 夹具，本目录是"该门缺夹具"的证据，直删会抹掉一条待办而非解决它。

## CSV行36 · lib/include/astrocs/core/executor.h:22 改写＝可直接删 —— **判定：降级**
- 独立取证：`:22` 原文＝`// 本头为 RT-004 冻结合同；实现见 lib/core/src/executor.cpp，测试见`（陈旧路径与"冻结合同"声明在**同一行**）。该头**不是零引用**：`lib/infrastructure/scheduler/src/executor.cpp:21` 与 `lib/infrastructure/scheduler/src/executor_runtime.h:28` 直接 `#include "astrocs/core/executor.h"`，`eng/tests/unit/{executor_provider_race_test,rt001_unique_executor_test}.cpp` 与两个 `eng/tests/runtime/integration/test_rt00{4,6}_*.py` 内嵌源码亦 include ⇒ **生产传递头**。登记面另有 `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:356`（以 `lib/include/astrocs/core/executor.h:6` **行锚**登记 thread_creation 扫描点）与 `:435`（`executor.h,…,production,yes`），`eng/tools/doccheck/doc_hygiene_exempt.json:72` 自述该行 symbol 字段是 `eng/tools/arch/build_production_execution_inventory.py` 的 `rg()` 从本头**逐字复制的机器字面量**并因此进白名单。本行声明的实现真实落点是 `lib/infrastructure/scheduler/src/executor.cpp`＝本表**行 65**（不许删、经文本 include 进生产）。
- 与成稿差异：(a) 四档"不在生产 target"错——头文件不入 target 不等于不在生产闭包，本头在生产 include 闭包内；(b) `计数 文档0/登记0` 是 scope（`-- lib`）造成的假阴，登记面实有 2 行＋1 条白名单；(c) 内嵌指向"见 AUD-404 行 56"**错指**：行 56 是 `observability/probes/src/probe.cpp`，executor.cpp 在**文件行 65**（见 P 节）。
- 影响 D8 的处置：不进"可直接删"档；改列第 2 组，先决动作＝①与 RT-004 条款核对（本行自称冻结合同）②改后核 `PRODUCTION_EXECUTION_INVENTORY.csv:356/:435` 与 `doc_hygiene_exempt.json:72` 的逐字复制是否漂移（生成器只读 `:6`，原地改写不移动行号⇒预计不红，但须以现网读数定）。

## CSV行37 · lib/infrastructure/cli/commands.cpp:548 改写＝可直接删 —— **判定：确认**
- 独立取证：`:548` 原文＝`// 仅当 eng/eng/tools/quality/gen_run_graphs.py 存在时调用; timeout 30s 防悬挂。`，双 `eng/` 拼写错属实；`git ls-files eng/eng` ⇒ 0；真实工具 `eng/tools/quality/gen_run_graphs.py` 在跟踪集。该文件确有登记面指向（`eng/ci/ledgers/aio_io_boundary_inventory.json:1639`、`eng/ci/ledgers/log_system_ledger.json` 12 条 `"file": "lib/infrastructure/cli/commands.cpp"`、`eng/ci/check_run_manifest_schema.py:7` 的 `::write_run_manifest` 符号锚），但**均为文件级/符号级锚，不含本行行号**，且处置是原地改写不移动行号 ⇒ 不判红。
- 与成稿差异：无。补一句其"计数 生产1"同样是"注释自身计入生产"的档名问题（E1 口径）。
- 影响 D8 的处置：进任务书（可直接改）。

## CSV行47 · lib/infrastructure/benchmark/backend_host/avx_backend.cpp＝可直接删 —— **判定：推翻**
- 独立取证：`git grep -n avx_backend.cpp`（全跟踪树，非 scope 截断）⇒ 除自身与 `reports/`、`artifacts/` 快照外有 **4 条在册消费者**：
  `eng/tests/backend/test_isa_avx.py:20` 用 `os.path.join(HOST,"avx_backend.cpp")` **现场编译成 `avx_backend.so`**、`:55` 再 `open(...).read()` 对其文本做断言；`eng/tests/unit/cpu_provider_test.cpp:87` `check_shared("avx_backend.cpp")`；`docs/architecture/ISA_VARIANTS.md:10` 把它登记为 AVX(无 FMA) 变体 TU。
- 与成稿差异：`生产0/测试0/文档0/登记0`、`门承载＝否`、`连带＝无`、`风险＝低` 四项**同时不成立**——删该文件 ⇒ `test_isa_avx.py` 的 `setUpClass` 编译失败（该测试属 §1.2 批 E 自己列出的 ISA 先决面族）＋ `cpu_provider_test` 红。**更关键**：第①层在 §3.1 已为同目录的 `cheat_backend.cpp` 建立过"0 CMake 命中 ≠ 0 消费者，须反查 pytest 现场编译"的自证规则，并据此把 eng/tests/backend 19 TU 判第 3 组（本表行 112），**却没在同一条 `test_isa_*.py` 消费链上的 avx_backend.cpp 上执行该规则**；其 `风险` 句"第四种 ISA 变体的壳且无人列"里的"无人列"直接被 `test_isa_avx.py:20` 否证。
- 影响 D8 的处置：从第 1 组撤出；改列**第 3 组不许删（改注册）**，处置与行 112/113 同批（要么接进构建面，要么如实登记为 ISA-002 夹具）。同时把"批 E 先决面"扩到 `test_isa_avx.py` 并登记为一条**第①层自证规则未贯穿执行**的偏松样本。

## CSV行49 · lib/algorithms/calibration/src/dark_optimizer.cpp＝可直接删 —— **判定：推翻**
- 独立取证：本行自报复核命令 `git grep -nw -e dark_optimizer -- CMakeLists.txt eng lib/algorithms/calibration` 的 scope 内**即有命中**（"除自身外 0 命中"为假阴）：
  `eng/tests/backend/test_calibration_oracle.py:201-202` 的 `srcs = ["calibrator.cpp","master_generator.cpp","cosmetic_corrector.cpp","dark_optimizer.cpp","ac_api.cpp"]` 在 `setUpClass` 里 `g++ … *srcs` 现场编译并 `assert r.returncode == 0`；
  `lib/algorithms/calibration/build.ps1:29` 的源清单含 `"src/dark_optimizer.cpp"`；
  `lib/algorithms/calibration/include/astro_calibration.h:387` 生产公共头注释"对应实现见 src/dark_optimizer.cpp"。
  scope 外另有 4 处文档锚：`docs/algorithms/CALIBRATION_ALGORITHMS.md:254`（`dark_optimizer.cpp:97-364`）、`:316`（`astro_calibration.h:168-179` 对照）、`:364`（`:227-238`）、`docs/modules/calibration.md:119`。
- 与成稿差异：计数、门承载、连带、风险四项全不成立；删该文件 ⇒ `test_calibration_oracle` 的 `setUpClass` 编译断言直接失败（Syn-001 校准 Oracle）。其 `风险` 已自留"须先跑一次 CHK-SPEC-NAMED-IMPL 类门确认无 ALG 锚指它"，但**同一结论无需跑门、一次 `git grep` 即可得到**，属"报 0 前未在同一 scope 上自证"。
- 影响 D8 的处置：撤出第 1 组；改列**第 2 组**，先决动作＝①同批改 `test_calibration_oracle.py:201-202` 源清单与 `build.ps1:29`（否则删一个留两个断源引用）②与 `CALIBRATION_ALGORITHMS.md` 的 3 处行锚同批③先核 `astro_calibration.h:168-179` 声明的 `ac::optimize_dark_k` 是否也在 API 合同里（本审查未展开，缺 API_CONTRACTS 对该符号的读数）。

## CSV行51 · lib/infrastructure/gaia_xpsd_client/example/demo.c＝可直接删 —— **判定：推翻**
- 独立取证：本行自报命令 `git grep -nw -e 'example/demo.c' -- CMakeLists.txt eng lib` 的 scope 内即有 3 条命中（`git grep -i -n demo -- lib/infrastructure/gaia_xpsd_client`）：
  `lib/infrastructure/gaia_xpsd_client/Makefile:10` `all: gaia_client.dll libgaia_client.a gaia_client_demo`、`:22` `gaia_client_demo: example/demo.c libgaia_client.a`、`:23` `gcc … -o $@ example/demo.c …`、`:28` clean 列表 ⇒ **该文件的平行构建面显式构建这个 demo**；根 `CMakeLists.txt:287` `add_subdirectory(lib/infrastructure/gaia_xpsd_client)` 使该模块整体在图上（其自身 CMakeLists 不列 demo，但 Makefile 列）。
- 与成稿差异：`生产0/测试0/文档0/登记0` 与"示例面，无门无合同"不成立（有构建面）；`连带＝无；若 README 引用示例须同批改` 把风险押在 README 上，而真正的引用者是同目录 `Makefile`。**同表行 104 的对象清单里就点名了 `infrastructure/gaia_xpsd_client` 的 Makefile**，两行互为连带却各自记"无"。
- 影响 D8 的处置：撤出第 1 组；改列第 2 组，先决动作＝与 `Makefile:10,22,23,28` 同批（删 demo 必须同时删 target 与 all 依赖），或与行 104 的"平行构建面清理"合并为一个批改单元。

## CSV行53 · healpix_db/archive/legacy/healpix_stack/ 的 13 个 TU＝可直接删 —— **判定：推翻**
- 独立取证：以 `healpix_stack`（而非单一代理 token）全树重数，scope 外的在册载体有 5 处：
  `docs/architecture/DEPENDENCY_RULES.md:15`「healpix_stack 为**冻结件，改动一律走变更流程**（依赖已归档 healpix_io.dll，不重建）」；
  `docs/validation/SCIENCE_FREEZE.md:78`「重新引入 active Stage2/healpix_stack 科学路径」（科学冻结条款的禁止对象）；
  `eng/tools/no_legacy_production_reference.py:5,15-17` 以 `healpix_stack`、`run_stage_gradient_sphere(`、`healpix_stack.dll` 为扫描 token 的门；
  `eng/tools/quality/contracts/fixtures/check_doc_symbols/invalid_archive_symbol.md:3` 用 `lib/healpix_db/archive/legacy/healpix_stack/foo.h` 作**负例夹具的主题路径**；
  本目录自身 `Makefile:28`（`SRCS = healpix_core.cpp ahps_reader.cpp … stack_engine.cpp …`）与 `build.ps1:95`。
  本行自报复核 `git grep -nw -e stack_engine.cpp -- CMakeLists.txt eng lib ⇒ 0 命中` 也是假阴：`Makefile:28` 与 `build.ps1:95` 都在 `lib/` scope 内且含该字面量。
- 与成稿差异：`登记0/文档0` 与"无门无合同"不成立；`风险` 句"实测 archive 路径在 docs 面 0 命中"与上面 3 条 docs 命中直接矛盾。**方法缺陷**：以 1 个文件名（`stack_engine.cpp`）当 13 个 TU 的代理计数，未逐名核。另有**支持删除方向的证据须如实并列**：`docs/algorithms/HIPS_WRITER.md:400` 自陈"healpix_stack 系列函数全仓零调用"、`CON-DOC-SYMBOLS` 的 `_header_corpus` 确实 `if "archive" in str(h): continue`（本行 col5 对此的引用无误）⇒ 删除的**方向**可成立，但"直接删"不成立。
- 影响 D8 的处置：撤出第 1 组；改列第 2 组（并与行 52 目录级动作、行 104 的 archive Makefile、`no_legacy_production_reference` 门、`invalid_archive_symbol.md` 夹具成一个批改单元），并把"走变更流程"作为前置门写进任务书（AGENTS §6 不动冻结定义）。

<!-- 小结 1（第 1 组 14 行判完）：确认 5（行 8、9、10、32、37）／降级 4（行 7、11、18、24、36 中 18 为推翻）——精确四态：确认 5、降级 4（7、11、24、36）、推翻 5（18、47、49、51、53）。第 1 组 14 行里 **9 行不在"可直接删"档**。还能再往下删一层的名单只有 4 条：`upm.cpp:2849`、`sampler.cpp:1275`、`sampler.cpp:1277`（三行注释）＋ `fixtures/check_test_contracts/` 整目录（若选"删"而非"接线"分支）。第 1 组被高估的根因是**两条**：(i) 引用面计数一律带 scope（`-- CMakeLists.txt eng lib` 之类），scope 排除 `实验/`、排除被审对象自身目录，于是"我查过的那一面为 0"被写成"全仓为 0"；(ii) 报 0 前的自证只跑在 CMake 命名面，没跑在 **pytest 现场编译面**——而第①层自己已为 `cheat_backend.cpp` 建立过该规则（§3.1），未贯穿。 -->

### 第 2 组（抽 18 条）

## CSV行2 · upm.h:181（生产2/测试0/文档16/登记7 计 25）—— **判定：降级**
- 独立取证：E1 第 1 行——按第①层 scope 精确复现出 25（分解见 E1 表），**其 scope 外另有 9 命中**（`artifacts/evidence/**` 7 行、`实验/**` 2 行），其中含 E2 的真调用点；`upm.h` 现总行 450，`:180` 是"`// per-control 归一化权重（raw/sum_j(raw) × control_reliability）。`"，`:181` 是墓碑，`:182` 空行，`:183-185` 已属 `p2_upm_geometry_hash`。
- 与成稿差异：总数 25 只在声明 scope 内成立，写成"引用面计数"会读成全仓面（口径缺"scope"声明）；处置"必须与 AUD-404 行 6-14 同批处理后才可删"里的 **行 6-14 指向偏窄**——真同批面还要加行 15（TRACEABILITY 正本）、18、19、20、21、22、23、24（同符号的文档/模块/合同三件套行）与 E2 的 `实验/` 调用点。另 `V404-S 已判`：本行是否判红取决于 `CON-DOC-SYMBOLS` 语料是否被 `api_inventory.csv` 白名单子串吸收，本条以"先解引用"为最低安全序，不依赖该分叉。
- 影响 D8 的处置：留在第 2 组批 A，先决动作扩为"文档侧 9 行（含 `实验/`）＋登记侧 7 行同批改，再删头文件注释"；并登记一条独立缺陷：`upm.h:180` 的**说明性注释在删掉 :181 后成为孤儿**（描述一个头里已不存在的函数），本清单未记。

## CSV行3 · upm.h:355（生产2/测试0/文档1/登记1 计 4）—— **判定：确认**
- 独立取证：E1 第 2 行——全树恰 4 命中，四档 P2/D1/R1/T0 与成稿逐档相等；`登记1`＝`eng/ci/prod_wiring_baseline.json:140` 的 `"unreachable:header.P2_API:p2_upm_ma_component_of_control"`（棘轮基线把该符号登记为"不可达"，删注释不新增不可达项，按 §1.2 实测的 `_apply_ratchet` 语义不判红）。
- 与成稿差异：无（`计数口径` 同 E1：两条"生产"都是注释自身）。
- 影响 D8 的处置：进任务书（第 2 组批 A，文档侧 PUBLIC_API.md:21 同批）。

## CSV行4 · sampler.h:133（计 4）—— **判定：确认**
- 独立取证：E1 第 3 行，逐档相等；`-w` 已确证与 `_cached` 变体不串档。
- 与成稿差异：无。
- 影响 D8 的处置：进任务书。

## CSV行5 · sampler.h:135（计 4）—— **判定：确认**
- 独立取证：E1 第 4 行，逐档相等。
- 与成稿差异：无。
- 影响 D8 的处置：进任务书。

## CSV行6 · rejection.h:416（计 5）—— **判定：确认（附一条连带补强）**
- 独立取证：E1 第 5 行，P2/D2/R1 逐档相等；`文档2` ＝ `docs/contracts/PUBLIC_API.md:21` ＋ **`docs/algorithms/PHASE2_REJECTION.md:109`**（`| p2_reject_stack_resolve_ex | :2203-2217 | 规划+执行合并入口（AUTO 在此消解） |`）。
- 与成稿差异：定级确认，但 `连带改动` 只写"docs/algorithms..."未点到行；该处是 `SYMBOL_GLOB="docs/algorithms/*.md"` 语料内的**范围锚**，删注释前须同批处理该行 ⇒ 与行 11 是同一批改单元（见行 11 判定）。
- 影响 D8 的处置：进任务书（第 2 组批 A）。

## CSV行12 · docs/contracts/PUBLIC_API.md:1806 签名条目 —— **判定：降级**
- 独立取证：该条实为 `:1806-1811` 六行（`:1808` 内含 `（h:139-142，实现 upm.cpp:1325）`）。**两处行锚已失效**：`upm.h:139-142` 现是 IRLS 收敛访问器的说明（`// （不改 P2ModelInfo 冻结布局；p2_upm_build 仍以 rc=0 表示…）`…），`upm.cpp:1325` 现是 gauge 自由度记账注释；同段 `:1803-1805`（在产符号 `p2_upm_raw_weight`）的 `:1315-1323/:1294/:1311/h:126-133` 亦全部落在无关代码上（`upm.h:126-133` 现为 `P2ControlNode`/`p2_upm_build_geo`）。
- 与成稿差异：处置"删 :1806 签名条目；:20-22 变更叙述一并清除"**只覆盖文本，不覆盖锚**——本清单行 18-24 同族的 `:1325`/`:1292`/`:1650-1681` 类锚已整体漂移，改对"退役符号半边"仍留死锚。
- 影响 D8 的处置：进任务书，但先决动作加一步：删条目后**核同文档内在产符号的全部 `h:`/`upm.cpp:` 行锚**（属 UPM 锚族系统性订正，需并一条新目）。

## CSV行13 · docs/contracts/API_CONTRACTS.csv:280 —— **判定：未判**（占位，取证未完）

## CSV行15 · docs/TRACEABILITY.csv:2 —— **判定：降级**
- 独立取证：`:2` 原文 `SCI-UPM-WEIGHT-001,science,production UPM 权重 = quality × geometric_reliability × control_ivar；…,docs/science/PHASE2_UPM.md,,phase2,p2_upm_raw_weight; p2_upm_build; p2_upm_normalized_weights`（symbols 列含退役符号，与 `eng/tools/quality/v19r3_traceability.py:62` 的同名串**逐字重复登记**）；文件总行 68。
- 与成稿差异：处置"只从 symbols 列去掉退役 token"方向对；但**同一字符串的第二份事实源**（`v19r3_traceability.py:62`，本清单行 26）必须同批，否则两处 symbols 列不再相等——这正是 §4 结论 3"同一能力第二套事实源"在本域的实例，成稿把两行分在不同处置（行 15 先解引用／行 26 改注册）却未标为同一批改单元。
- 影响 D8 的处置：进任务书，与行 26 并为一组。

## CSV行16 · docs/algorithms/PHASE2_UPM_IMPL.md:87 —— **判定：降级**
- 独立取证：`:87` 原文 `| p2_upm_normalized_weights | :181-184 | :1683-1702 | per-control 归一化（API 面） |`。头侧 `:181-184` 现覆盖**墓碑注释＋下一函数的说明块**（见行 2 取证）；实现侧 `:1683-1702` 现是 nlohmann JSON 解析（`:1684 for (const auto& e : j["cell_index"])`），且**同文件 :61 给同一符号写的是 `:1683-1710`** ⇒ 文件内两处区间互不相同，必有一处错。
- 与成稿差异：成稿的"三处锚同批改"把本行与 `:61`、`UPM_SOLVER.md:115` 算作同族正确；但没发现 (a) `:181-184` 的头侧覆盖已含墓碑、(b) `:61` 与 `:87` 的区间自相矛盾。风险应升一档（从"中"到"高：改完仍是死锚"）。
- 影响 D8 的处置：进任务书，先决动作＝取 `p2_upm_raw_weight`／`p2_upm_normalized_weights` 在 upm.cpp 的**真实现位置**（本审查未展开，缺一次定义级 grep）后整表重钉。

## CSV行19 · docs/science/PHASE2_UPM.md:272 —— **判定：降级**
- 独立取证：`:272` 原文 `- \`ALG-UPM-003\` \`p2_upm_raw_weight / p2_upm_normalized_weights\` 权重` ⇒ 一行绑定两枚符号，"去退役半边、ALG-UPM-003 仍由 raw_weight 持有"的机制成立（与成稿同）。文件 `docs/science/PHASE2_UPM.md` 总行 505。
- 与成稿差异：机制确认，**缺一条纪律**：`docs/science/` 是 AGENTS §1/§5 的"公式权威，只读不改"层，改它须走变更流程（§8），本行处置未标；同表行 99 对另一批 docs/science 行明确写了"改文档走变更流程"⇒ 同类两档处置口径不一。
- 影响 D8 的处置：进任务书，处置补"走科学变更流程"，并与行 99 对齐。

## CSV行20 · docs/modules/phase2_upm.md:144 —— **判定：降级**
- 独立取证：`:144` 原文 `p2_upm_raw_weight（:1292）、p2_upm_normalized_weights（:1325）、`——与 `lib/algorithms/upm/README.md:126-127`（`:1292`/`:1325`）、`registry/astrocs.phase2.upm-apply.md:92`、`upm-fit.md:85` 是**同一段落的四份抄本**（成稿已知，其"三处同批"计数少一处）。实测 `upm.cpp:1292` 现为 `std::vector<int> col_of(...)`、`:1325` 现为 gauge 注释 ⇒ **两个行锚都已指向错误内容**，其中 `:1292`（在产符号的锚）失效比退役符号那条更值得登记。
- 与成稿差异：处置"若该门只核模块 ID，三处可…"未定；实测 `docs/modules/phase2_upm.md` 与两张 registry 页被 `docs/traceability/TRACEABILITY_MATRIX.csv:20` 一类模块行、`docs/audit/inventory.csv:8` 点名 ⇒ 属"模块身份登记面"，删行须由 `gen_module_readmes.py` 侧重生成（同表行 75 的规则），不能手删。
- 影响 D8 的处置：进任务书，先决动作＝①按行 75 的生成器规则处理 registry 页 ②整族行锚重钉（与行 12/16/23/24 同一批改）。

## CSV行23 · lib/algorithms/upm/README.md:127 —— **判定：降级**
- 独立取证：`:126-129` 为四行符号-锚表，`:127` 即 `| p2_upm_normalized_weights | :1325 | per-control 归一化权重 |`；`README.md:173` 自陈"README/module.yaml/memory.md：`lib/algorithms/upm/`（本目录）"＝合同三件套之一（与行 24 同族，冻结件）。
- 与成稿差异：成稿的"README:127 的行锚 :132…"已识别同族；补一条：本行的 `连带改动` 指向 `lib/algori...`（README/registry 三处），但**同样漏掉 `实验/` 侧**（E2 的 `upm_sweep.cpp` 是这枚符号唯一的真消费者，README 的"per-control 归一化权重"表项删掉后，实验单元的调用在该模块合同里失去对应条目）。
- 影响 D8 的处置：进任务书（第 2 组），与行 20、21、22、24 同批；先决动作补"与 E2 的实验调用点对齐"。

## CSV行24 · lib/algorithms/upm/memory.md:57 —— 见第 1 组判定（该行被 §1.1 列入第 1 组，本处不重复判）—— **判定：降级（已在第 1 组段给出）**
- 独立取证：`memory.md:57` 原文 `p2_upm_normalized_weights :1325 / p2_upm_geometry_hash :1346 /` ⇒ 与行 23 同一族的第 5 份抄本；`:1` 自称"P2-UPM-DOC 冻结"。
- 与成稿差异：除第 1 组段已列的两条外，另指出：**同一枚符号在 8 个在册载体里各写一遍行锚**（PUBLIC_API、PHASE2_UPM_IMPL ×2、UPM_SOLVER、PHASE2_UPM、phase2_upm、README、memory、两张 registry 页），是 §4 结论 3"第二套事实源"的最大单例；成稿按"行"拆成 8 条处置，未按"一处真值＋N 个生成视图"合并 ⇒ 任务书会派 8 个批改而不是 1 个。
- 影响 D8 的处置：不进"可直接删"（见第 1 组段）；并建议把行 12/16/17/18/19/20/21/22/23/24 合并为一个 UPM 锚族批改单元。

## CSV行33 · eng/contracts/data/aio_abi_contract_v1.json —— 判定：未判
## CSV行35 · eng/contracts/data/phase2_uncertainty_rejection_provenance_v1.json —— 判定：未判
## CSV行39 · eng/tools/quality/contracts/check_api_contracts.py:20 指针 —— 判定：未判
## CSV行48 · lib/infrastructure/aio/src/aio_pipeline_engine.cpp（第二套 PipelineFrame）—— 判定：未判
## CSV行60 · lib/infrastructure/aio/io/hips_core.c —— 判定：未判
## CSV行67 · lib/phase1_session/p1_session.cpp（编译进产品却无调用者）—— 判定：未判

### 第 3 组（抽 19 条）

## CSV行29 · eng/tools/doccheck/dangling_ledger.json 棘轮台账 —— 判定：未判
## CSV行38 · cfitsio/win_compat/unistd.h:1 自述路径 —— 判定：未判
## CSV行41 · pipeline/module_loader/secure_loader.c —— 判定：未判
## CSV行42 · pipeline/module_loader/module_registry.c —— 判定：未判
## CSV行55 · cfitsio 7 个未白名单文件 —— 判定：未判
## CSV行56 · observability/probes/src/probe.cpp（默认 OFF）—— 判定：未判
## CSV行57 · aio/src/ahpx/aio_ahpx_api.cpp（AHPX-WEIGHT-RETIRED 唯一执行对象）—— 判定：未判
## CSV行65 · scheduler/src/executor.cpp（经文本 include 进生产）—— 判定：未判
## CSV行74 · eng/ci/ledgers/registry_ir_parity.json —— 判定：未判
## CSV行75 · docs/modules/registry/astrocs.phase{2,3}.resample.md —— 判定：未判
## CSV行76 · eng/contracts/block_flow/conformance_deviations.json:36-41 —— 判定：未判
## CSV行80 · 六个零引用头（传递可达性关口）—— 判定：未判
## CSV行94 · 源码注释里的 `run/` 指针族 —— 判定：未判
## CSV行95 · eng/ci/retired_code_allowlist.json —— 判定：未判
## CSV行105 · 三份同名 healpix_core.cpp —— 判定：未判
## CSV行107 · acr 与 benchmark/scheduler 同名件 —— 判定：未判
## CSV行112 · eng/tests/backend/ 的 19 个 TU —— 判定：未判
## CSV行116 · calibration/src/v6_calibration_covariance.cpp:54,680-706 拒绝面 —— 判定：未判
## CSV行124 · eng/ci/check_aio_io_boundary.py ＋ ledgers/aio_io_boundary_inventory.json —— 判定：未判

## P 节 · `CSV 行 N` 内部指向核查（全表性质）—— 判定：未判

### 本批判定汇总（待写）
### 旧判偏松清单（待写）
### 覆盖率自报（待写）
### 我对第①层判据本身的校准建议（待写）

<!-- PROGRESS: 26/52 -->
