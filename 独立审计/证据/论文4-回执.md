# 论文4 回执（AUDIT-06 · D5 · 全天 HEALPix 精确面积交叠分配）

- 交付物：`独立审计/03_小论文/论文4_HEALPix面积交叠.md`
- 篇幅：正文汉字数 **3199**（摘要至第 7 节，含表格文字，不含公式/流程图代码块，不含参考文献）；连同公式代码块为 3259；参考文献另 372 汉字。末尾常驻 `<!-- PROGRESS: 章节 8/8 -->`
- 工作件（全部在仓库外）：`独立审计/复算件/paper4/p4_check.py`、`p4_out.json`（独立自复算件）；`trim_patch*.py`（排版修订脚本）
- 纪律自证：仓库严格只读、零 git 写、**未执行任何 `git config`（含写子命令）**、未构建、未跑 ctest/`run_checks.py`/任何 `eng/**` 脚本/端到端、未 `import` 仓库内 Python。核对代码锚与文档行号只用 `git grep`/只读查看；需要显示非 ASCII 路径时用一次性 `git -c core.quotepath=false` 参数形式。

## 0 开工/收工 git 基线与差异

同一命令 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`，基线 HEAD=`c8f64e9a`。

开工：

    ?? "ACSD\346\225\271\346\262\273\345\224\267\345\267\245\344\275\234\345\214\205_AUDIT-06.zip"
    ?? site/

收工：

    ?? ACSD整治工作包_AUDIT-06.zip
    ?? site/

**条目集合差异 = 无**（同样两项：整治工包 zip、`site/`）。唯一差异是编码呈现：开工那次 git 把非 ASCII 路径按八进制转义输出，收工那次按未转义中文输出（本会话未改任何 git 配置，故该差异来自调用侧环境，不是仓库内容变化）。本回执按要求登记该差异，正文内不出现。

## 1 引用条目清单（论文条目 → 支撑的论点 → 取证来源）

| 编号 | 文献/来源 | 论文里用它支撑什么 | 本轮取证途径 |
|---|---|---|---|
| [1] | Górski et al. 2005, ApJ 622, 759–771, DOI 10.1086/427976 | 等面积基数 `4π/(12N²)`、`N_side=2^k`、NESTED 记法 | 文献池：Crossref + OpenAlex + 预印本 `astro-ph/0409513` 的 journal-ref 三通道，台账已判**确认**；**其 §5.3/§4 小节全文本轮不可得**，正文与参考文献条目都写明"不据其立论" |
| [2] | Calabretta & Roukema 2007, MNRAS 381, 865–872, DOI 10.1111/j.1365-2966.2007.12297.x | "叶边界不是大圆弧"与"卡等面积、属一族投影"的**指定替代来源** | 文献池：Crossref/OpenAlex 书目 + 摘要逐字（"The natural spherical projection associated with… HEALPix… is described and shown to be one of an infinite class…"）；正文逐字引句由核验报告经 arXiv:astro-ph/0412607 取得——该号是 **2004 年单作者预印本**，文献池明令不得与期刊版并列当"同一文同一作者"，故论文在 [2] 与被引处都写了这一限定 |
| [3] | Fruchter & Hook 2002, PASP 114, 144–152, DOI 10.1086/338393（arXiv:astro-ph/9808087**v2**） | 输出为"以累计权重为分母的加权均值"、三因子 `a/w/s²` 的幂次须交代；核按 drop 归一时 `Σ_p w=1` 的类比 | 文献池 rank 29 判**确认**并逐字读到 §2 式(4)(5)；**其 §7.2 式(7)／§5 式号未证 ⇒ 论文不使用**（仓内科学正本引的正是 §7.2 式(7)，本回执第 5 节列为待办） |
| [4] | Greisen & Calabretta 2002（Paper I），A&A 395, 1061–1075, DOI 10.1051/0004-6361:20021326 | drop 的 WCS 线性核 §2.1.1 式(1)；"参考点可非整数/可在图外"为原句；CD 在 §2.1.2 | 文献池 rank 33 **确认（正文已读）**；并采纳其"CD 须挂 §2.1.2"的订正 |
| [5] | Calabretta & Greisen 2002（Paper II），A&A 395, 1077–1122, DOI 10.1051/0004-6361:20021327 | TAN = gnomonic、`R_θ=(180°/π)cot θ` 在 §5.1.3 式(54)；据此"直边成球面大圆弧" | 文献池 rank 34：§2.2 与 §5.1.3 式(54) **确认**；其 `Table 1` 形态被判伪托 ⇒ 论文未用表号 |
| [6] | Van Oosterom & Strackee 1983, IEEE TBME 30, 125, DOI 10.1109/TBME.1983.325207 | 球面多边形面积 = 三角形立体角闭式的扇形剖分 | 文献池交叉提示：台账 seq 25 判该 DOI 三处形态**关联错**，并要链路④作者复核；论文按其指示**只采"三角形立体角闭式"这一真实内容**，未沿用被判错形态。归档实验对该文的补充用法（VOS 前旋转到质心避免相消）本轮未见反证，但论文不引（超出所需） |
| [7] | Sutherland & Hodgman 1974, Comm. ACM 17, 32, DOI 10.1145/360767.360802 | 原文只处理平面多边形/平面窗口，球面逐边裁剪是推广 | 仓内 `docs/algorithms/DRIZZLE_GEOMETRY.md:403` 已逐字登记该适用域；论文以推广口径引用 |
| [8] | Fernique et al. 2015, A&A 578, A114 | HiPS/MOC 层级与"按叶计数取面积" | 文献池：上一轮已判**确认**（HiPS/MOC 面）；论文只取层级面积约定，不引其它结论 |
| [9] | IVOA HiPS 1.0 / MOC 1.0 规范 | 层级 tile 与覆盖记法 | URL 逐字取自仓内 `docs/algorithms/HEALPIX_MAPPING.md:45`、`docs/science/DRIZZLE.md:230`，非本轮新造 |
| [10] | astropy-healpix 2.0.0（BSD-3-Clause） | 异源对拍：像素面积、亚像素偏移、像素归属判定 | 复核件与核验报告均用该版本；本机 `p4_check.py` 亦用 2.0.0 复现（见第 4 节） |

**论文正文中每个关键数字的来源**（全部可追到本轮取证过的来源）：

- 12 面单位正方形、两支 `|J|=π/3`、叶面积 `π/(3N²)`：核验报告 §1 的独立推导（赤道带支与极帽支分别求雅可比）＋复核件 W1"chart 前提自证"；论文第 3.1 节按该推导展开。
- 44652 点逐点差分、最坏 `5.14e-4` 落在南极点、双射 3072/3072 与跨格 0：核验报告 AUD204-02。
- 弦叶全天求和残差 `0.0`、逐叶 `−9.905%/+0.612%`（N=16）与 `−9.964%/+0.543%`（N=64）、`>1e-6` 占 100%/99.72%、保总量三型注入 `≤4.4e-16` 对逐叶 `1.0…321.3`、矢高 `8.094e-2/6.587e-4/1.443e-4`、99.55% 的边超阈：复核件 W1。
- 归档 T12 同表 `6.63e-4 / 2.25e-7 / 8.81e-15`：复核件 W1 与 `实验/healpix-polar/results/p3_t12.out`（角点02 行 `6.626e-4 / 2.247e-7 / 8.807e-15`，二者一致）。
- 极点朴素折线最坏闭合 `9.0734e-1` 且容差 `1e-6→1e-10` 不改善：`实验/healpix-polar/results/p2_t8b.out:5-7`（`9.0734e-01/9.0735e-01/9.0735e-01`）与其报告 §4。
- 跨缝位形对细分段数不敏感（`3.23e-8→4.61e-8`、`2.80e-7→2.70e-7`、近缝对照同量级）、弦/真曲线偏差 `−1.17e-4 / −2.93e-5 / −9.95e-2 / +3.87e-9…e-8`：复核件 W2 的球面精确构造表。
- 生产叶表示范围（`N≥256` 与 `9≤N<256` 都只取四角、生产 `N` 域 `[16,2²²]`、真曲线件被调用点旁路且自述与调用点相反）：核验报告 AUD204-06 的代码锚，本会话另在 `spherical_overlap.cpp:1372-1382` 与 `:543-566` 直读复核一致。
- u8 量化：界 `|r| ≤ 0.5/q`、`1/(510S)` 一阶近似且 `S→1` 处偏小 `1.67e-5`、`S=0.64→+1.2270e-3`、`0.05→−1.9231e-2`、`0.01→−0.150`、最坏 `−0.500`（`S=1/510`）、邻域最坏 `1.9685e-3`、过覆盖 `+0.300`、NaN 门限 `S<1/510`、511 例扫描、f32 比 `4.1e3`：复核件 W3。
- 下游非数处置（两处接缝度量 `nan_to_num` 后进度量且不计数、一处 `nan=med`、Phase2 采样器 fail-closed）与真实帧旁证 `min 0.35948/mean 0.99811/n_zero 0 → 上界 5.4e-3`：复核件 W3；本会话在 `eng/tools/e2e/render_vis.py:458`、`seam_footprint.py:386`、`quality/frame_qc_grid.py:710`、`lib/algorithms/coverage/src/sampler.cpp:820-821` 直读核对。
- 逐叶对照件未注册（蒙特卡洛参照、质心完成度检验）、参照式对照"3 条断言为红"、容差 `1e-6/1e-5/1e-7` 三值并存：复核件 W4 与 W2③；本会话在 `tests/CMakeLists.txt`（"不注册 ctest"段与 `DRZ_EXTRA_TESTS` 段）、`drizzle_acceptance_test.cpp:74/178/250/181`、`DRIZZLE_GEOMETRY.md:303-304/312-313` 直读核对。
- 鞋带须先平移（`5.4e-5` 相对误差）：`实验/healpix-polar/docs/EXP-07-POLAR.md:396-397`。

## 2 按第②层复核撤销/改写的说法清单（正文中一律不出现被推翻版本）

1. **"折线缝上有精度平台、超 1e-6 冻结门"——降级，不复述。**改写为两条：① 换用不依赖基面归属的球面精确构造后，跨缝位形逐叶误差对细分段数不敏感、**缝上没有平台**（第 5.3 节）；② 与细分无关的真实地板是**叶的弦表示**（第 5.3、6.3 节）。另按复核指出：拿逐叶量级的数去比 `1e-6` 那条**求和型**通量闭合门是度量与门的类型错配，现行**逐叶**门是 FP32/FP64 自洽（`1e-5`），没有任何在册几何逐叶门在守该量级（第 6.3 节）。
2. **"构造闭合 1e-15"不得当逐格正确性证据。**复核把核验的"恒真门、无证据资格"精确化为"该门资格范围 = 完备性与全局标度"，正文按此措辞（第 5.2、6.1 节），并保留核验成立的结构事实：归档原型逐格叶面积算完即弃、只累加裁剪格计数（`chart_native.h:352-362`），故该闭合是同一算子与自身相比。
3. **"逐 leaf 分配环节没有任何门"→ 改为"没有任何在册门"。**仓内已有两件能抓逐叶分配的独立可执行件，均被明文不注册（第 6.2 节）；修复动作的性质由"新建判据"改为"定容差＋注册＋收敛红灯"。
4. **"profile=0 在 pixfrac<1 有偏移"→ 判为不成立。**`k=D_p/N_p` 代数相消，profile=0 发布信号 `= S_p`、与 pixfrac 无关；两形态真实差 = profile=1 侧 u8 量化（`0.5/q`）加过覆盖钳制（`S−1`）（第 5.4 节、第 7 节，可据此结案该待裁事项）。
5. **`1/(510·S)` 不是严格界。**严格为 `|r| ≤ 0.5/q`，且 `S=1` 点值为 0、`1.9685e-3` 是邻域最坏（第 5.4 节表格首行注明）。
6. **不沿用 Van Oosterom & Strackee 被判"关联错"的引用形态**，只按"三角形立体角闭式"使用（第 2 节与 [6]）。
7. **不引用不可重算读数的数字**：`reference_overlap` 三条红灯所依 `run/WIRING-W34-01/evidence/reference_overlap.txt` 未入库且当前不存在（本会话核实 `run/` 下无该目录，`git ls-files` 亦无），故正文只写"三条断言为红"这一登记事实、不写其差值。同族处置：`docs/algorithms/DRIZZLE_GEOMETRY.md` 引用的 `run/SCI-FIX-DRZGEOM-01/evidence/exp_*.json` 读数（外接圆上界 1.0442、邻元步长各向异性、δ 实测等）原始件同样不入库、当前无法重算，本文一律不引。
8. **Górski 2005 §5.3（及 §4 基面拓扑节号）不作为断言依据**，"边界非大圆"改挂 [2] 并写明"以该来源为准"；`ASTROCS_DESIGN.md:206` 与 `实验/healpix-polar/code/polar_common.h:15` 的该节号归属仍是上游待订正项（本文未改）。

## 3 缺哪条证据会让哪一节站不住

| 缺的证据 | 直接站不住的节 | 说明 |
|---|---|---|
| 生产侧面积函数（弦表示）与真曲线 oracle 的**逐叶对拍**实跑（需能构建的节点） | 5.3、6.3 的"产品侧读数"部分 | 现有链条只到"表示层地板"（异源构造＋只读代码事实）；不跑 C++ 本体就不能把它说成生产产品读数。本文因此只主张"表示层与几何层的地板" |
| **真实产品 tile 的支撑度直方图**（多少像素 `S<1/510`、多少在 `0.5/q>1e-3` 档） | 5.4 与 6.4 的影响面 | 只能给量化包络与最坏值，给不出实际受损像素占比；现网只有 `release02` 一条 realdata 日志旁证 |
| **逐叶几何门的定值＋注册＋三条红灯的处置**（容差属科学判据面，需裁决） | 6.2 的"尚未纳入机器门，容差待定"能否去掉；第 7 节边界句 | 在该门落地前，论文对"逐格正确性"只能主张到"有可复算对照手段" |
| `sumNorm≤0 ⇒ k=1.0` 回退档与 `S<1/510` 档的正/负例夹具 | 3.3 与 5.4 的口径完整性 | 两档目前无任何件覆盖 |
| HEALPix 定义文献被指名小节的全文（或期刊正文） | 第 2 节"边界非大圆"的归属层级 | 只能继续以 [2] 为准，不能升为"定义文献原文明示" |
| 非数→0 对 `seam_metric` 的响应模型或最小合成例 | 6.4 的方向性判断 | 现在只能说"两处把非数当 0 计入且不计数"，不能说它系统性掩盖或伪造了多大接缝 |
| 三方数据中的物理仿真与真实数据端到端两类 | 第 4 节与全篇证据等级 | 本链路按三重佐证标准未齐，等级低于前三条创新点（正文已如实声明） |

## 4 本会话新增的独立自复算（可复核）

`独立审计/复算件/paper4/p4_check.py`（纯 math ＋ astropy-healpix 2.0.0，未 import 仓库代码），`p4_out.json` 为结果件。与复核读数对照：

- 雅可比：赤道支最大相对偏离 `7.9e-9`、极帽支 `1.2e-8`（各 4000 点，避开极点）⇒ 恒为 `π/3`；`12·(π/3) − 4π = 0.0`（逐位）。
- 等面积基数：`π/(3N²)` 与 astropy `nside_to_pixel_area` 在 `N=16,64,256,512,1024,4096` 全部 **bit_equal = true**。
- 双射/轴对齐：`N=16` 3072 格 ↔ 3072 互异像素、跨格 0；`N=64` 49152 ↔ 49152、跨格 0。
- 弦四边形全天空：`N=16` 逐叶 `−9.9047%…+0.612%`、`>1e-6` 占 100%、中位 `7.56e-4`、Σ−4π `1.24e-14`；`N=64` 逐叶 `−9.9644%…+0.5432%`、`>1e-6` 占 99.72%、中位 `4.71e-5`、Σ−4π `−2.03e-12`；最坏叶（含极点角）弦/真曲线 = `0.9010`（N=16）与 `0.9004`（N=64）。
⇒ 复核的四组关键读数在我的独立实现（不同求和序、自有 chart、自有 Van Oosterom）下同值；论文正文引的是**复核值**，本条只作交叉印证。

## 5 与"面积分配位于创新点①与②之间"这一方案的自洽性

**不自洽之处在仓库文档侧，不在本文。**负责人口径：平面→球面的 HEALPix 面积分配属于创新链本身，位置在①（测光校准到星等坐标系）与②（跨帧绝对信噪比）之间。论文按此写：第 1 节"这一环节在链上，位于测光定标与跨帧信噪比之间——它的输出是后两者的取值域"；第 6.5 节明写设计现行措辞与该口径不同并声明"本文按'链上几何环节'行文，措辞差异不改"。

设计现行文本与此冲突之处（**只指出，不改动**）：

1. `ASTROCS_DESIGN.md:118`：「它**不属于**上面三个科学创新点的链条……因此单列为创新点四」——与"链上环节"直接相反。
2. `ASTROCS_DESIGN.md:752-758`（§12.3）：把创新点四列为"产品级实验单元…随 Phase1 drizzle 模块一并交付"，与"三个创新点互为前提、各自独立实验单元"同表并列——措辞上仍把它放在链外。
3. `ASTROCS_DESIGN.md:207`（创新边界第 2 条）把"Σ_p a_jp = (π/3)·|D∩face| 由代数结构直接成立"列为**创新主张**，而 `:213`（"与冻结门的关系"）称该门"由数值逼近变为构造性满足"——按复核结论，这两句在未加限定语时会被读成逐格正确性已被证明。论文以"资格范围"表述替代，并建议正本补一句限定（复核 W1"缺什么证据"第 2 条同源）。
4. `ASTROCS_DESIGN.md:210`（第 5 条）写 `w_jp = a_jp / A_pixel`，与科学正本 `docs/science/DRIZZLE.md` §5（`w_jp = a_jp/A_drop,j`、`Σ_p w_jp=1`、`N_p=Σ_j w_jp·A_pixel,j`）及 `DRIZZLE_GEOMETRY.md` §1:48-50/DISP-DRZ-009 相反（同一文档体系内 `DRIZZLE_GEOMETRY.md:43` 与 `:337-346` 又各写一侧）。论文采正本口径，并把这处登记为待统一项。
5. `ASTROCS_DESIGN.md:206` 引"Górski et al. 2005 §5.3"、`:211` 引"Górski et al. 2005 §4"——两处节号本轮全文不可得，属第 2 节第 8 条同一待订正项。
6. `ASTROCS_DESIGN.md:211` 的"面积口径不受任何分支影响（`|J|=π/3` 为解析恒等）"这句本身成立，但与同节"全天正确性由此保证"连读会盖过"极点不可逆 ⇒ 必须回退"的条件；论文把可逆性与面积口径分开写，并指出回退后逐叶误差上限与实测数尚缺。

上述 1、2 属科学/顶层文档口径变更，需走变更流程由负责人裁决；本文未改任何仓库文件，也未在正文内复述"产品级旁路/不属于链条"的旧措辞。

## 6 格式与写法自检

- 无元信息块；正文无日期、任务编号、commit、sha、流水号与历史叙述（本回执才登记基线差异与工包语境）。
- 图：第 3.2 节一张 mermaid 流程图（一图一结论：卡路径的近似只在 drop 侧，奇点走回退）；第 5.2 节"图位＋说明"，无假链接、无不存在的图。
- 公式全部给符号与单位；表格只用于并列对照，台账式明细未进正文。
- 主张边界：正面只主张"卡的前提（等面积、轴对齐、双射不重不漏）＋绝对立体角累加、层级面积守恒、覆盖重数守恒"，并声明求和闭合的资格范围；逐格正确性、生产侧读数、加速比、"无需回退"均不主张。
