# 通读 CR-19 · lib/algorithms/coverage 第 1 段

- 派单：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§1/§2/§2.5/§3/§4/§5 全照做）
- 清单：`独立审计/批次清单/CR-19.txt`（6 个文件）
- 被审基线 HEAD：`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`（`git log -1 --format=%H` 原文）

## 开工 `git status --porcelain` 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

（命令：`git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`，运行于读任何被审文件内容之前。）

## 读文件清单与进度

- `lib/algorithms/coverage/include/astro/phase2/acr_kernels.h` —— 已读：是（表 0 项，本文件无数值常数）
- `lib/algorithms/coverage/src/acr_kernels.cpp` —— 已读：是（表 26 行，含同族合并；findings CR-19-01…05）
- `lib/algorithms/coverage/include/astro/phase2/async_io.h` —— 已读：是（表 5 行；findings CR-19-06/07）
- `lib/algorithms/coverage/src/async_io.cpp` —— 已读：是（表 0 项；finding CR-19-08）
- `lib/algorithms/coverage/tests/async_io_test.cpp` —— 已读：是（表 9 行，含同族合并；findings CR-19-09/10）
- `lib/algorithms/coverage/include/astro/phase2/block.h` —— 已读：是（表 7 行；finding CR-19-11）

<!-- PROGRESS: 6/6 -->

---

# 文件 1 · `lib/algorithms/coverage/include/astro/phase2/acr_kernels.h`（12 行，整读）

## §2.5 数值处置表

本文件**不含任何数值常数、阈值、缓冲系数、截断窗、收敛参数或默认配置值**：全文只有一句 `extern const char* kOpMosaicReject;`（:6）、一句 `void register_phase2_acr_kernels();`（:10）与命名空间/注释。逐行核查过的非文字项：`#pragma once`（:2）、`namespace astro::compute::phase2`（:4/:12）皆无数值。表空 = 该文件无数值项，非漏项。

## findings

**无可辩护指控。** 检索式（在本文件与 .cpp 对称性上做的核查）：

- 头文件不声明 buffer/标量布局，故 .cpp 的 10 槽 60 字节 ABI 约定只在实现侧有据（见文件 2 表）；无"注释自称权威默认而权威配置是另一个数"的形态。
- `kOpMosaicReject` 的对外语义主张（"ACR 只加速不改变科学语义"，:8-9）在实现侧的成立范围，作为文件 2 的 CR-19-02/CR-19-03 报，不重复计。

---

# 文件 2 · `lib/algorithms/coverage/src/acr_kernels.cpp`（368 行，整读）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :54,:55,:56,:57-58,:59-60,:61-63,:64-66,:67-69,:71-73,:76-79（CUDA 侧同族 :277-292） | `read_scalar(..., offset)` 偏移族（0/8/16/20/24/32/40/44/52/56） | 见列 | invocation 标量 blob 内各字段的字节偏移 | 结构性不适用 | 不是科学量：由字段宽度拼接出的 ABI 偏移，`2*sizeof(size_t)+…` 全部由 `sizeof` 导出；与 :349-352 注释布局逐项一致 | — | CONFIRMED |
| :356-358 | `r.args.scalar_bytes` | 公式 `2*size_t+2*int+2*double+int+size_t+2*int`（=60） | 声明 invocation 必须带的标量字节数，registry 按精确相等校验 | 公式导出 | 由字段宽度求和；代码确实按式算（非写死数字），`validate_invocation` 精确相等判定见 :353-355 注释所引 `kernel_registry.cpp:21` | — | CONFIRMED |
| :348 | `r.args.buffer_count` | 7 | 该 Operation 绑定的 buffer 槽数（0..6） | 结构性不适用 | 不是科学量：接口基数，与 :44-53 的 7 个 `buffers.find(i)` 一一对应 | — | CONFIRMED |
| :87,:304 | `method_v` 缺省 | `1u` = `P2_REJECT_SIGMA`（`rejection.h:47` alias `astrocs.robust_mad_clip.v1`） | 标量缺失时选的排异方法 | 待确认 | 值本身在合同面有登记（`defaults.json` 的 `rejection.sigma.*` 三元组、`docs/science/REJECTION.md` §5），但"缺省即 sigma"**无条文规定**；生产侧 method 一律来自 `p2_reject_plan_resolve`（profile 路由，`REJECTION.md:48-51`：生产档 `astrocs_adaptive_pixel` 对 N≤3 解析为 `none`=0）⇒ 缺省 1 与生产档不同 | 保守方向：缺省应改为"标量缺失即 throw"（fail-closed），否则漏装一个标量就把不排异档换成排异档，影响整个 ACR/诊断产物像素值 | CONFIRMED（取值面）/PARTIAL（条文面） |
| :89,:301 | `und_n_v` 缺省 | `2u` | 候选数 ≤ 该值判 UNDERDETERMINED（不排异） | 待确认 | `rejection.h:~200` 结构体注释"默认 2"、`configs/stage2_full.example.json:27` = 2；但 `docs/science/REJECTION.md:51` 明写生产档内核闸 `underdetermined_n = 3`、`:63` "1 ≤ n ≤ 3 → none" | 方向：2 比 3 **更激进**（n=3 时本档仍排异、生产档不排异）⇒ 产物字节差；影响域 = 所有 n=3 输出像素 | CONFIRMED |
| :90,:91,:329,:330 | `lo_v`/`hi_v` 缺省 | `-4.0` / `3.0`（CUDA 侧 `4.0`/`3.0` 再取 `±fabs`） | sigma  clipping 的下/上侧 σ 倍数 | 待确认 | 与 `eng/packaging/config/defaults.json:288-316` 登记的 `rejection.sigma.lower_sigma=4.0 / upper_sigma=3.0` 同值（该处 note 记 verbatim='sigma/winsorized/averaged 4.0/3.0/8'）；**本仓无独立推导/标定锚**，来源是 WBPP/SiriL 实践值，`docs/science/REJECTION.md` 未给该三元组的文献式 | 数值与权威配置一致 ⇒ 无当前偏差；风险是"第二处写死"，配置侧改值不联动 | CONFIRMED |
| :92,:302 | `max_it_v` 缺省 | `8` | sigma clipping 迭代上限 | 待确认 | 同族：`defaults.json:316` `rejection.sigma.max_iterations=8`；无推导锚 | 迭代上限只影响收敛精度不上界；写死 ⇒ 与配置侧脱钩 | CONFIRMED |
| :96 | `plan.minimum_n` | `3`（写死，不从标量取） | 方法注册表的最小 N | 公式导出 | 生产由 `method_minimum_n(method)` 导出（`rejection.cpp:982-996`，SIGMA/WINSORIZED/… ⇒ 3），代码**未调用该式而是抄了结果**；对 ACR 支持的两个 method 当前数值相同 | 若注册表变动，ACR 与 CPU 静默异值 ⇒ 等价性主张失效 | CONFIRMED |
| :102 | `plan.normalization_floor` | `1e-12` | **MEDIAN_SCALE 归一的 \|median\| 下限**（数值保护下限，不是零方差保护） | 结构性不适用 | `rejection.h:214` 注释限定其作用域为 MEDIAN_SCALE；`rejection.cpp:2119-2123` 只在 `norm == P2_NORMALIZE_MEDIAN_SCALE` 分支使用 ⇒ 本文件 :101 选的是 `P2_NORMALIZE_MEDIAN_CENTER`，**该值在本 launcher 里完全不被读**（死配置）。同值在 `defaults.json`/`stage2_common.cpp:333`/`rejection.cpp:1245` 有登记 | — （它不进科学语义，且此处不生效） | CONFIRMED |
| :101 | `plan.normalization` | `P2_NORMALIZE_MEDIAN_CENTER` | 判定工作域归一方式 | 待确认 | 注释自称"与 CPU 生产一致"，但生产侧该值是配置项 `cfg.reject_normalization`（`stage2.cpp:840-844` 支持 `astrocs_median_center_v1` / `astrocs_median_scale_v1`），**标量布局里没有 normalization 槽** ⇒ 只有配置选 median_center 时才一致 | 方向：kernel 端恒定 center，配置选 scale 时 CPU/ACR 两臂判定不同 ⇒ 等价主张失真 | CONFIRMED |
| :122,:171 | `gin.value_dtype` / `cstack.data_type` | `0`（fp32） | 声明输入样本 dtype | 结构性不适用 | 枚举基数选择（`rejection.h` dtype 枚举 0=fp32/1=fp64），与 :105-106 的 `float*` 实转一致；非科学量 | — | CONFIRMED |
| :128 | `gin.support_threshold` | `0.0` | support > 该值才算合格候选 | 文献值 | `rejection.h:356` "默认 0.0"，且与生产 CPU 路径 `stage2.cpp:1350` 取同值 | — | CONFIRMED |
| :141 | `*wmode == 2` | `2` = `kAcrWeightModeIvar`（`stage2.cpp:67`） | 声明"本 invocation 的 buffer3 是 ivar 语义"⇒ 显式拒绝 | 结构性不适用 | 模式枚举常量，不是科学量；定义面 `stage2.cpp:67` 与 :138-140 注释 | — | CONFIRMED |
| :146 | `wgt_v` 初值 | `1.0` | 缺 snr/cell 权重面时的乘子 | 待确认 | 无任何条文说"权重乘子缺省 = 1"；效果见 CR-19-04 | 方向：1.0 使 `w = support`（无量纲几何量），比 0 更危险——静默把被排除的量当权重 | CONFIRMED |
| :148 | `grid` | `8` | 每 tile 每边的 cell 数（8×8=64 cell/帧） | 待确认 | 与生产 UPM 侧 `control_grid_per_tile` 同族（`module_adapters.cpp:9583` 从配置读、缺省 8），此处**写死**且 `:39` 注释未声明该耦合 | 方向：配置改 16 时 kernel 仍按 8×8 取 cell ⇒ 权重错位，不报错 | CONFIRMED |
| :150,:151 | `% 512u` / `/ 512u` | `512` | tile 边长（像素），用于把 chunk 内线性序号还原为 (px,py) | 待确认 | 生产侧 tile 边长同为 512（`stage2.cpp:731,753,1086` `512ull*512ull`）但**同样写死**，且 kernel 假设 raster 行优先线性序；`stage2.cpp:413-416` 一侧用的是 HEALPix `nested_local_to_xy` 的 (x,y) | 方向：两侧任一改动（tile 边长、序约定）即错位取权重，无校验 ⇒ 权重张冠李戴 | CONFIRMED |
| :152 | `64` | cell 边长（像素），`(py/64)*grid+(px/64)` | = 512/8 的导出量 | 公式导出 | `tile_side / grid`；**代码写死 64 而非按 :148/:150 两常数导出** ⇒ 三者任改其一即自相矛盾（`512/8=64` 当前成立） | — | CONFIRMED |
| :154-158 | `snr->data[fs*grid*grid + cell]`；`stack_w[s] = stack_sup[s]*wgt_v*wgt_v` | — | 权重构造：本帧本 cell 的面值平方 × support | 待确认 | 设计式是 `w = SNR²/F_ref² = 1/σ_F²`（`ASTROCS_DESIGN.md:399`、`integrate.h:9`）：此处**无 `/F_ref²`**、且多乘 support；见 CR-19-05 | 方向：support≥0 无量纲 ⇒ 权重被几何量缩放，帧间相对权重失真 | CONFIRMED |
| :149,:328 | `tile_p = p0 ? *p0 + p : p`；`p0 ? *p0 : 0` | `0` | tile 内起始偏移 | 结构性不适用 | 位移量，由调用方 `append_scalar(scalars, std::size_t{p0})`（`stage2.cpp:1226`）给出；非科学量 | — | CONFIRMED |
| :161-164,:199-200,:206-209 | 无效像素哨兵 `0.0f`（signal/support/rej/valid 四面） | `0.0f` | 无候选或积分非 OK 时输出全 0 | 结构性不适用 | 不是科学量：无效哨兵，约定与 `integrate.h:18-22` 的 status 面同源；消费端以 `out_sup > 0` 为准（`stage2.cpp:1251`）⇒ 天光为 0 的真像素不会被伪装成有效 | — | CONFIRMED |
| :203 | `rdec.rejected_low + rdec.rejected_high` | — | 写回被拒样本计数 | 结构性不适用 | 计数口径，非阈值 | — | CONFIRMED |
| :213 | `cpu_workers` 缺省 | `1`（串行） | OpenMP 线程预算缺省 | 待确认 | AGENTS §6「不硬编码线程：由 benchmark 生成的 profile 决定」；此处缺省 1 是"无 profile ⇒ 串行"，与 AGENTS 不冲突但条文未规定 | 方向：1 = 确定性串行，保守侧；风险仅在于并行档不可达 | CONFIRMED |
| :218,:228 | `num_threads(cpu_workers)` / `schedule(static)` | — | 线程数与调度 | 结构性不适用 | 实现选择（循环上界与调度策略），不进科学语义；`cpu_workers > 1` 已排除 0/负值入参 | — | CONFIRMED |
| :126,:127,:135,:170,:194 | `(std::uint32_t)` 窄化（`n_depth`、`p`） | — | size_t → uint32 传参 | 结构性不适用 | 由 C API 形参类型决定（`rejection.h` 的 `uint32_t count/pixel`），非科学量；实际 `n_depth` = 覆盖帧数、`p` < chunk_pixels=262144 | — | CONFIRMED |
| :31,:32 | `kOpMosaicReject` 字符串 | `"synthetic.mosaic_reject.fp64acc"` | Operation id | 结构性不适用 | 标识符字面量，非数值；登记面 `docs/science/ACR_EQUIVALENCE.md:17`、`docs/TRACEABILITY.csv:68` 同串 | — | CONFIRMED |
| :81,:83 | `!px/\|*px==0` 缺标量即 throw | — | 入参闸 | 结构性不适用 | 存在性判定，无阈值 | — | CONFIRMED |

（同族合并说明：`4.0/3.0/8/2/1` 五个排异缺省在 CPU(:87-:92) 与 CUDA(:301-:304) 两 launcher 各写一遍，表内已把两侧位点并列；`sizeof` 派生偏移两 launcher 亦同。）

## findings

### CR-19-01 · ①"参考天光平面排除自身"在本批 6 文件零承载；承载点在 v6 方差传播与 scheduler，UPM/天光两求解器与 `11_upm.md` 条文面均无该要素
- 轴：D（附 C）
- 位点：本批唯一权重构造处 `lib/algorithms/coverage/src/acr_kernels.cpp:158` `stack_w[s] = stack_sup[s] * wgt_v * wgt_v;`——全文件 368 行、其余 5 个批内文件均不出现"参考平面 / 其他帧组合 / 排除自身"的任何形态（`acr_kernels.h`、`async_io.*`、`block.h` 与该要素无接口）。
- 上位依据：`ASTROCS_DESIGN.md:411`「参考平面用其他帧的加权组合（**排除自身**）、迭代带阻尼、拟合权重与叠加权重同源、末端用叠加权重计算并扣除残差场」；`eng/ci/ledgers/design_clauses.json:309` 同文登记；该条点名的下钻载体 = `docs/plugins/algorithms_phase2/11_upm.md`。
- 现状（含我这轮实际搜到的东西，不转述）：
  - 检索式与命中数（均 `git -C ... grep`，基线 `c8f64e9a`）：
    - `git grep -n "排除自身" -- docs lib eng *.md` ⇒ 命中 `ASTROCS_DESIGN.md:411`、`eng/ci/ledgers/design_clauses.json:309`、`eng/tests/validation/release02/**`（README:116、oracle_output.txt:21-22、`patch_apply2.py:25/34/40`、`realdata_evidence.py:57`）——**全部不在 `docs/plugins/algorithms_phase2/11_upm.md`、`docs/science/PHASE2_UPM.md`、`docs/algorithms/UPM_SOLVER.md` 里**；
    - `git grep -n "自身\|其他帧\|其余帧\|H_kk\|hat\|include_self" -- docs/plugins/algorithms_phase2/11_upm.md docs/science/PHASE2_UPM.md docs/algorithms/UPM_SOLVER.md` ⇒ 三文件命中的"自身"全是"数据自身分辨率极限/边界自身的局部背景/判据自身"这类语义，**无一条指"参考平面排除被估帧自身"**；
    - `git grep -in "exclude_self\|excludes_self\|leave_one_out\|leave-one-out\|排除自身\|留一" -- lib` ⇒ 代码侧承载点在 **`lib/algorithms/integration/v6/include/astrocs/v6/variance_propagation.h:64-65`「`include_self=false → H_ij = w_j/W_{-i}`（(c) 排除自身；H_ii=0）」** 与 `lib/algorithms/integration/v6/src/variance_propagation.cpp:145`，以及 `lib/infrastructure/scheduler/src/module_adapters.cpp:10151/10379/10556/10674/10717`（`variance_include_self=false`、`residual_maker_PSigmaPt_exclude_self_control`）；**`lib/algorithms/coverage/` 全域 0 命中**（含 `sky_plane.cpp`、`upm.cpp`、`sampler.cpp`、本批 6 文件）。
  - 即：该要素目前**只落在"残差制造者 hat 矩阵的方差传播"一层**（`H_ii=0`），而最高设计那句话的主语是"参考平面用其他帧的加权组合"＝**平面求解/评估本身**要排除自身；两件事不是同一处。本批文件与之无任何接线。
- 差在哪：设计点名的能力在其指定载体（`11_upm.md`）里没有条文，在 coverage 的两个求解器（`sky_plane.cpp`/`upm.cpp`）里没有实现，只有下游方差面把它当成 hat 行的性质实现了——即"要素被写进最高设计，但被实现成了另一层的属性"。
- 后果：对外主张"各帧多退少补使任意覆盖子集上加权阶跃恒为零"（`ASTROCS_DESIGN.md:411` 后半句）的成立依赖"参考平面不含该帧自身"；若求解器实际含自身，`δ_k` 被自身观测拉回 ⇒ 残余台阶，L4 接缝门（`docs/ci/01_CHECKS.md:157`）读到的 `rel_step` 就是系统性偏小的那一类。
- 定级：本批内 **S3**（本批文件不含该要素，无在此修的路径；真正的落点属 `coverage/src/{sky_plane,upm}.cpp` 与 `docs/plugins/algorithms_phase2/11_upm.md`，由对应批次定级）。**若**在那些文件复核后确认求解器含自身 ⇒ 该处 S1，触发路径：`normalize/mosaic 生产入口 → module_adapters upm-fit → p2_upm_build_geo → δ_k(x) 求值 → stage2.cpp:1166-1183 out_v[i] -= dk`。
- 怎么算修好：同批两改缺一不可——(a) 在 `docs/plugins/algorithms_phase2/11_upm.md` formulation 四要素里把"排除自身"写成可判定条款（含 `W_{-i}` 式与单帧覆盖子集的 fail-closed 行为）；(b) 求解器按该式实现或明确声明"该要素的实现位是残差制造者 hat 行，平面本身含自身"，并回改最高设计措辞。**只补文档不补代码 = 判红**（合同/门按 `design_clauses.json:309` 逐条比对）。
- 置信：CONFIRMED（检索式与命中集为本轮实测；"求解器是否含自身"本身属 `sky_plane.cpp`/`upm.cpp` 批次，我未在那两个文件上下结论）。

### CR-19-02 · `ivar 权重必须走 CPU canonical` 的 fail-closed 守卫只装在 CPU launcher，CUDA launcher 根本不读 wmode
- 轴：R（附 D）
- 位点：`acr_kernels.cpp:141-144`
  ```
          if (wmode && *wmode == 2)
              throw std::runtime_error(
                  "mosaic_reject: wmode=2 (cell ivar) 已禁用——ivar science "
  ```
  对照 `mosaic_reject_cuda`（:264-331）：标量只读到 offset 44（`p0`，:290-292），**从不读 offset 52 的 wmode**，随后 :318-331 直接 `api.submit_mosaic_reject(...)` 把 buffer3 原样交给 GPU kernel。
- 上位依据：`docs/science/ACR_EQUIVALENCE.md:43`「逐像素 ivar 权重时 `ACR` 块禁用，走 CPU `canonical p2_integrate_pixel` 路径（`ACR-IVAR-001`）」；`docs/TRACEABILITY.csv:5`（ACR-IVAR-001，状态 `VERIFIED`，验证名 `CPU_ACR_IVAR_EQUIVALENCE`）；主张原文在 `lib/algorithms/coverage/tools/stage2.cpp:1229-1232`「若 ACR 块被重新启用，链路 fail-closed，不会静默回落到 support×snr²」。
- 现状：调用方路由是 `stage2.cpp:1236-1241` `if (gpu_ready && acr_reg->cuda.has_value()) { (*acr_reg->cuda)(inv, nullptr); } else { acr_reg->legacy_parallel(inv, nullptr); }`——即 **`gpu_ready` 为真时先进 CUDA launcher，而它没有该守卫**；异常回退分支（:1242-1246）再次调用 `legacy_parallel`（同一个会 throw 的 launcher），所以 CPU 侧一旦抛就是整块失败，GPU 侧则根本不抛。
- 差在哪："fail-closed"只覆盖了两个执行体中的一个；守卫按 launcher 分置而非按 invocation 语义分置，等于把一条科学禁令实现成了 CPU 分支的副作用。
- 后果：`TRACEABILITY.csv:5` 的 `VERIFIED` 与 `stage2.cpp:1229-1232` 的对外主张不成立——在有 CUDA bridge 的机器上重新启用 ACR 块时，声明为 ivar 的 invocation 会静默按 GPU kernel 的权重口径出产物。
- 定级：**S2（缺运行时生产触发路径）**：`p2_acr_block_eligible`（`stage2_common.cpp:512-528`）无条件 `return false` ⇒ `use_acr_block` 恒假 ⇒ 两个 launcher 在生产均不可达；`acr_kernels.cpp` 也不被生产入口 `register`（`eng/tests/cli/test_iso_acr_gpu_isolation.py:81-90` 断言此事）。触发链只在 ablation/隔离域存在：`stage2.cpp:854 (register) → :1104 if(use_acr_block) → :1236-1237 cuda → :318 submit_mosaic_reject`。
- 怎么算修好：把 wmode 读取与 `==2 → throw` 提到两个 launcher 的公共入口（或 `register_phase2_acr_kernels` 的 wrapper / `validate_invocation` 层），让任何执行体见到 ivar 语义都拒；同批把 `stage2.cpp:1229-1232` 注释与 `TRACEABILITY.csv:5` 的 VERIFIED 依据改写为"CPU/GPU 两臂各自校验"，并给 CUDA 分支补一条负例（wmode=2 + 假 bridge → 必须抛）。**只加 CUDA 分支的 throw 而不改注释/追溯面 = 该条仍判红**（主张与代码不同源）。
- 置信：CONFIRMED（两 launcher 的标量读取范围逐行读过；路由行已逐字引用）。

### CR-19-03 · kernel 缺省权重口径 = 被 §9.73 裁决 A44 删除的 `support×SNR²`，且 buffer3 的语义生产侧已改成 ivar 而消费侧仍按 SNR 平方
- 轴：C（附 S）
- 位点：`acr_kernels.cpp:70` `// weight_mode scalar (offset 7); 缺省 0=legacy support×snr²`、:138-140 注释「kernel 保留 wmode=0（legacy support×snr²，仅 ablation/诊断）」、:158 `stack_w[s] = stack_sup[s] * wgt_v * wgt_v;   // legacy 诊断`；:50 `const BufferBinding* snr = inv.buffers.find(3);`
- 上位依据：`integrate.h:12-17`「§9.73 裁决 A44：原 `stack.support_x_snr2.v1`（weight_mode=0，weights = support × SNR²）与 `stack.equal.v1` 两个**可选口径**及其 weight_mode 选择键**已删除** —— support 是无量纲几何量、equal 是等权，二者都不是信号/噪声之比」，并引 `docs/science/PSF_SIGNAL_WEIGHT.md §4:72`「不存在口径选择键、口径枚举、口径配置项或口径产物」；`ASTROCS_DESIGN.md:227`「进入科学叠加权重的量只有由 SNR 换算的逆方差」。对面一条：`docs/science/ACR_EQUIVALENCE.md:35` 承认「`weights/ivar` 槽只服务 `wmode=0`（legacy `support×snr²`）ablation/诊断口径」。
- 现状：生产供给面已改口径——`stage2.cpp:1106-1107`「§9.73 裁决 A44：唯一权重口径 = compact per-cell ivar (buffer3=ivar)。原 weight_mode=0 的 per-cell SNR 分支（support×snr² 的 ACR 形态）已删除」，`stage2.cpp:414-417` 往 map 里塞的是 `o.ivar`；消费面 :158 仍把该槽的值当 SNR 平方（`wgt_v*wgt_v`），且 :141 的守卫只挡 `wmode==2`，`wmode==0`（含缺省）时同样读这块 buffer。
- 差在哪：同一 buffer 槽在供给面叫 ivar、在消费面被当成 SNR 平方，两篇权威文档对"是否还允许 wmode=0 这条口径"各说一遍（A44 说删除、ACR_EQUIVALENCE:35 说保留为诊断）；本文件的缺省分支落在被删除那一侧。
- 后果：任何以 `wmode=0` 进入该 kernel 的 ablation/回归产物，其权重面 = `support × ivar²`（既不是设计的 `SNR²/F_ref²`，也不是 ivar），诊断结论与"权重同源"主张同时失真；且它给了"配置里再长回一个 weight_mode"一个现成落点。
- 定级：**S2（缺生产触发路径）**——生产 `use_acr_block` 恒假（CR-19-02 已证），现存调用面是 `lib/algorithms/coverage/tests/synthetic_gate.cpp`、`eng/tests/api/test_reject_*.py` 等测试/门。
- 怎么算修好：(a) :141 的判据从"只拒 2"改为"只认合法值、其余一律 throw"，把 `wmode==0` 分支要么删除、要么改名成显式 `ABLATION_SUPPORT_X_SNR2` 并要求调用方自带 `F_ref` 面；(b) 同批订正 `docs/science/ACR_EQUIVALENCE.md:35` 与 `integrate.h:12-17` 二者之一，使"是否存在第二种口径"只有一处说法；(c) 若保留 :158 必须补 `/F_ref²` 或明确该槽 dtype 是 SNR 并把供给面改回 SNR。**只改注释不改判据 = 判红。**
- 置信：CONFIRMED（两侧原文逐字引用）。

### CR-19-04 · buffer2/buffer3 任一缺省时权重静默退化为 `support` 或等权，没有任何拒绝
- 轴：R
- 位点：`acr_kernels.cpp:146` `double wgt_v = 1.0;`（当 :50 `snr` 为空时不变）、:169 `cstack.weights = (sup != nullptr) ? stack_w.data() : nullptr;`、:191 `pi.weights = (sup != nullptr) ? stack_w.data() : nullptr;`、:158 `stack_w[s] = stack_sup[s] * wgt_v * wgt_v;`
- 上位依据：`integrate.h:19-22`「`weights=nullptr` 是**本 C API 的输入合同**（无权重数组 ⇒ 等权），不是可选择口径：生产唯一调用方……恒传 weights」；`ASTROCS_DESIGN.md:227` 禁非"信号/噪声之比"量进入科学叠加权重；`integrate.h:12-17` 已删除 equal 口径。
- 现状：kernel 对可选 buffer 的处理是"没有就当没有"：`snr` 缺 ⇒ `wgt_v=1` ⇒ `w = support`（无量纲几何覆盖量）；`sup` 缺 ⇒ 整个 weights 数组不传 ⇒ 等权；两条路径都不 throw、不进 status，产物字节照出。
- 差在哪：上层文档明确"等权不是可选择口径、生产恒传 weights"，而本 kernel 提供了三条通往"无科学权重"的路且全为静默；与 CR-19-02 同类形态——科学禁令在实现里以"缺省即放行"存在。
- 后果：门/回归里少绑一个 buffer（例如只给 values+support）会得到看起来正常但按几何覆盖加权的 mosaic，与"权重只能来自 SNR 换算的逆方差"的对外主张直接相悖；方向确定：**support 越大的帧权重越大，与噪声无关** ⇒ 低质量帧被抬高、产物偏亮/偏脏（不是偏红）。
- 定级：**S2（缺生产触发路径，同 CR-19-02）**。
- 怎么算修好：把"buffer2/buffer3 是否齐备"升格为 invocation 合同：`wmode` 声明为 ivar/科学时缺任一权重输入即 throw（与 :141 同一入口，见 CR-19-02 修法）；`nullptr` 等权分支保留给 C API 本身但在本 launcher 里禁用。同批给 `synthetic_gate.cpp` 补"只传 values+support ⇒ 必须抛"的负例。
- 置信：CONFIRMED。

### CR-19-05 · ACR 臂的权重是"逐帧×8×8 cell 派生面"，既非设计点名的稠密 SNR 面也无 `F_ref²` 除法 ⇒ 与 `w=SNR²/F_ref²` 不同式
- 轴：D（附 S）
- 位点：`acr_kernels.cpp:148-158`（`grid=8`、`%512u`、`/64`、`fs*grid*grid+cell`、`stack_sup[s]*wgt_v*wgt_v`）
- 上位依据：`ASTROCS_DESIGN.md:399`「叠加权重由 SNR 现场换算为逆方差 `w = 1/σ² = SNR²/F_ref²`（`F_ref` 为逐帧参考通量），这是 point information 最优集成（Zackay & Ofek），**不是直接用 SNR 加权**」；`docs/design/PHASE2_DETAILED_DESIGN.md:21` 三条口径（`dense`/`sparse_reconstruct`/`frame_reconstruct`）"都直接产出同一物理量 `SNR=F_ref/σ_F` 的**稠密表示**"。
- 现状：本 kernel 的权重面是 **帧 × cell（64px 见）粒度**的查表值（:154-156），供给面（`stage2.cpp:412-417`）填的是 UPM 控制点派生的 cell ivar；全文件无 `F_ref`、无逐帧参考通量除法（`git grep -n "F_ref\|f_ref" -- lib/algorithms/coverage/src` 仅命中 `coverage.cpp:437` 的报错文案，kernel 侧 0 命中）。
- 差在哪：设计要求"逐像素/重建为稠密面的 SNR 换算逆方差"，ACR 臂实现成"每帧一个 64px cell 的粗权重且不减参考通量"，二者不是同一个量；仓库自己也承认不等价（`docs/TRACEABILITY.csv:5`、`acr_kernels.cpp:138-140`）并因此禁用——**问题不在禁用，在于禁用之后 kernel 仍保留该式为缺省臂**（见 CR-19-03）。
- 后果：只要有人重新启用 ACR 块并走 `wmode=0`，产物权重面既不含 `F_ref²` 归一又是 cell 级 ⇒ "跨帧绝对 SNR 加权＝point information 最优集成"这条对外科学主张在该产物上不成立。
- 定级：**S2（缺生产触发路径）**。
- 怎么算修好：删除 cell 权重臂或把它改为"逐像素 ivar 面的分块预取"（与 CPU canonical 同式，含 `F_ref²`）；同批在 `docs/science/ACR_EQUIVALENCE.md:35` 明确该臂不产出科学产品、并给出与 CPU 臂的等价性判据（若声称等价）。
- 置信：PARTIAL——逐像素 ivar 臂在生产里的**取值细节**在 `module_adapters.cpp`（`AIO_HIPS_RD_IVAR` 开面在 :11486-11495、:11764-11765，帧级臂的"帧级 SNR 逆方差链"见 `integrate.h:9`/`stage2_common.h:161`），该文件不属本批，我未逐行读；本批内可辩护的部分（kernel 侧无 `F_ref`、cell 粒度）已复算。

---

# 文件 3 · `lib/algorithms/coverage/include/astro/phase2/async_io.h`（116 行，整读）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :30-:31 | `n = memory_budget_bytes / item_bytes; return n > 0 ? n : 1;` | 除式 + 下界 `1` | 队列容量 = 预算 / 单条目字节 | 公式导出 | `docs/architecture/ASYNC_IO_CONTRACT.md:15-17`「`bounded_queue_capacity(memory_budget_bytes, item_bytes)`……容量下界为 1，0 不是合法取值」、:62 `capacity()` = `max(1, budget/item_bytes)`；代码**确实按式算**（整数向下取整，`async_io_test.cpp:28` 锁 `bounded_queue_capacity(1000,256)==3`） | — | CONFIRMED |
| :29 | `if (item_bytes == 0) return 1;` | `1` | 单条目字节数估计为 0 时的容量 | 待确认 | 条文面只规定"下界为 1"，**未规定 `item_bytes==0`（=估计失败/未给）该出什么值**；`async_io_test.cpp:30` 把 `1` 钉成期望行为 | 保守方向：`item_bytes==0` 应判"入参非法"而非返回合法容量——返回 1 会把"条目尺寸没算出来"伪装成"预算只够 1 条"，影响范围 = 一切以该函数为容量源的预取深度（当前生产零调用，见 CR-19-06） | CONFIRMED |
| :31（`memory_budget_bytes==0` 侧） | 同上式的 0 预算分支 | → `1` | 预算为 0 时的容量 | 待确认 | `ASYNC_IO_CONTRACT.md:56-65` §7 只列 `memory_budget_bytes` 含义、未定义 0；同一"预算缺失"条件在分块规划器侧是**显式不可行**（`block.h:40` `int status; // 0=ok, 1=unfeasible`）⇒ 两入口对同一输入一个报错一个放行 | 保守方向：`budget==0` 应 `unfeasible`/抛；影响范围 = 预取深度与峰值内存估算的一致性 | CONFIRMED |
| :38 | `capacity_(capacity > 0 ? capacity : 1)` | `1` | 构造函数对非法容量的钳位 | 待确认 | 同族（与 :29/:31 同一个"下界 1"决定）；条文锚同上，无独立出处 | 方向：钳位使"传 0"不报错——若调用方以 0 表达"禁用异步"，实际得到 1 深度的异步队列 | CONFIRMED |
| :47,:59 | 谓词里的比较（`queue_.size() < capacity_`、`!error_.empty()`） | 无常数值 | 背压/唤醒判据 | 结构性不适用 | 谓词组合，不含阈值；`capacity_` 为唯一上界 | — | CONFIRMED |

## findings

### CR-19-06 · `BoundedAsyncQueue`/`bounded_queue_capacity` 在生产代码里零调用点，合同 §9 已如实记"待接入"，但 §10 仍以该原语的合成测试记 `PAR-001 PASS`
- 轴：C（附 D）
- 位点：`async_io.h:27` `inline std::size_t bounded_queue_capacity(std::size_t memory_budget_bytes,` 与 `:35` `class BoundedAsyncQueue {`
- 上位依据：`docs/architecture/ASYNC_IO_CONTRACT.md:90-95`「**待接入点（明确记录，不伪造并发读安全）**：`stage2.cpp`：tile 读取/写入可使用……`sampler.cpp`：`SamplerReader` 可扩展为每 worker 异步预取队列」；`:99`「状态：PAR-001 PASS（证 `BoundedAsyncQueue` 作 I/O×compute 解耦且非全局串行）」；`docs/api/PHASE2_API_V1.md:22` 把 `async_io 队列(CON-008)` 登记为 session 级 API 行。
- 现状（检索式与命中）：`git grep -n "BoundedAsyncQueue\|bounded_queue_capacity" -- lib eng docs` ⇒ `lib/` 内命中只有 **本 header 自身**、`async_io.cpp`（空 TU，见文件 4）与 `lib/algorithms/coverage/tests/async_io_test.cpp`；生产侧无一处实例化。`git grep -n "std::thread\|std::async\|prefetch" -- lib/algorithms/coverage/tools/stage2.cpp` ⇒ 0 命中（stage2 的 tile 读是同步 `aio_hips_read_tile_f32`，`stage2.cpp:1144-1148`）；调度器的并行来自 `module_adapters.cpp:2052/9054` 的 `std::vector<std::thread> pool`，与该队列无关。`PAR-001` 的命中面只有三处自指：`ASYNC_IO_CONTRACT.md:97/99`、`eng/tests/cli/test_parallel_queue.py:2-3`。
- 差在哪：合同 §9 的诚实登记与 §10 的"PASS"处在同一份文档里，而 §10 的数字（`实测 N=20、Tio=4ms、Tcmp=2ms、81.5ms、ratio 0.68`）来自合成程序里的 `sleep`，不接任何产品；把"原语能 overlap"记成"CON-008 的 I/O×compute 解耦已 PASS"，等于用夹具替代被审对象——而该解耦在生产里根本没接线。
- 后果：对外可读到的主张"有界异步 I/O 已落地并证明无全局串行"（`ASYNC_IO_CONTRACT.md:99` + `PHASE2_API_V1.md:22` 的 API 在面登记）掩盖了事实：真实 mosaic 读盘是同步串行，性能面/预算面的结论（`memory_budget_bytes` 推导预取深度）没有任何生产载体。
- 定级：**S2**（不使产物错值，但使对外主张不成立；无"生产触发路径"可言，因为被审对象在生产无调用点——这本身就是内容）。
- 怎么算修好：(a) §10 标题与状态改为"原语级合成证明（未接入生产）"，并把 `PAR-001` 的验收要求与 `eng/tests/cli/test_parallel_queue.py` 的 `验收(03 PAR-001)` 指向同一条可判定条款；(b) 若要保留 PASS，则把 `stage2.cpp` 或 `sampler.cpp` 真正接上有界预取，并让同一测试读**产品**耗时而非 sleep；(c) `PHASE2_API_V1.md:22` 的 API 行须登记"生产调用点：无"。同批必须同时改，否则"改文档不改登记面"仍判红。
- 置信：CONFIRMED（零调用点由跟踪集检索确定，非本机磁盘）。

### CR-19-07 · `pop()` 用同一个 `nullopt` 表达"流正常结束"与"对端 cancel 出错"，接入方按 EOF 处理即静默截断
- 轴：R
- 位点：`async_io.h:61-62`
  ```
        if (!error_.empty()) return std::nullopt;
        if (queue_.empty()) return std::nullopt;  // closed & drained
  ```
- 上位依据：`ASYNC_IO_CONTRACT.md:36-38`「Worker 捕获读取/写入异常后调用 `cancel`，主线程通过 `error()` 获得第一错误文本」；`:45`「阻塞在 `pop` 的消费者立即返回 `nullopt`」——合同把两态都定为 `nullopt`，但要求消费者另行查 `error()`。
- 现状：`pop()` 无错误出参、不返回 `std::variant`，两行 `return std::nullopt;` 在类型上不可区分；`has_error()`/`error()` 必须被单独调用，而 header 没有任何机制强制消费循环在拿到 `nullopt` 后查一次错误。
- 差在哪：一个"错误必须上达"的合同（§4）配了一个把错误与正常终止压成同一返回值的接口，判定责任完全外推到调用方且无守卫。
- 后果：任何 `while (auto v = q.pop()) consume(*v);` 形态的消费循环在 I/O 失败时会当成正常排空 ⇒ 输出 tile 少写、`valid` 面为 0 而退出码 0（静默截断产物字节）。
- 定级：**S3**——后果链要求"有人接入"，而该队列生产零调用（CR-19-06 已证）；接入即升 S1，因为消费循环的正确写法在此接口下无法由类型保证。
- 怎么算修好：把终止原因显式化（`enum class PopStatus { Item, Closed, Cancelled }` 或 `pop(T& out, std::string* err)`），并让 `async_io_test.cpp` 的 `ReadFailureCancelsAndPropagatesNoDeadlock` 断言"消费者能区分截断与排空"而不是只断言 `error()` 文本；同批更新合同 §4/§5。
- 置信：CONFIRMED（两行原文即全部依据；无需实跑）。

（附带事实核对，非指控：`ASYNC_IO_CONTRACT.md:17`/`:62` 规定"容量下界为 1、0 不是合法取值"，与 :29/:31/:38 的钳位**一致**，故本文件不报"预算不足静默超预算"一条——超预算上限恰为 1 个条目，是合同明示的取舍，要改属合同侧决策。）

---

# 文件 4 · `lib/algorithms/coverage/src/async_io.cpp`（3 行，整读）

## §2.5 数值处置表

**本文件不含任何数值**：全文为一个文件路径注释、一句"当前实现为 header-only……本 TU 预留扩展"的中文注释与一行 `#include "astro/phase2/async_io.h"`。表空 = 无数值项，非漏项。

## findings

### CR-19-08 · 该 TU 只 `#include` 自身 header 且无任何定义，其登记面把它算作模块源文件
- 轴：C（附 S：以骨架充数的形态）
- 位点：`lib/algorithms/coverage/src/async_io.cpp:3` `#include "astro/phase2/async_io.h"`（全文件除注释外仅此一行）
- 上位依据：`ENGINEERING_SPEC.md`/AGENTS §6「不用 facade/空骨架/no-op 冒充实现完成」；`docs/api/PHASE2_API_V1.md:22` 把 `async_io 队列(CON-008)` 作为一条 API/模块面登记。
- 现状：文件自述"预留扩展非模板异步 IO 工厂"——即承认当前**无实现**；全部实现位于 header（模板 + `inline`），故该 .cpp 对产物的唯一作用是编译一个空对象文件。（与 CR-19-06 的"生产零调用"是两件事：那条讲原语没接线，这条讲登记面上存在一个空 TU。）
- 差在哪：单独看它不是"假实现"（header 是真实现），报它的理由是**清单/登记面按文件数与 API 行计数**：一条空 TU + 一条 session 级 API 行，会让"async I/O 已交付"在扫描面上看起来比事实更实。
- 后果：仅影响登记与扫描面（文件/API 计数、覆盖率分母），不影响产物字节 ⇒ 按 §3 规则写不出产物失真后果，故如实降级。
- 定级：**S3**。
- 怎么算修好：要么把非模板工厂真正落到该 TU（与 header 注释一致），要么删掉该 TU 并在 `docs/architecture/`/`PHASE2_API_V1.md` 标 async_io 为 header-only；**不要**只留注释"预留扩展"。（本条不建议为凑数而改科学面。）
- 置信：CONFIRMED。

---

# 文件 5 · `lib/algorithms/coverage/tests/async_io_test.cpp`（232 行，整读）

## §2.5 数值处置表

按前言 §4，测试期望值与夹具内容差**不报缺陷**，但 §2.5 仍要求逐项有处置；下表把"纯夹具量"归 `结构性不适用`（理由统一为：测试自订参数，不进任何科学语义与产品字节），把**被用作同步手段的墙钟量**单列 `待确认`（它们承担的是时序假设，属本文件的实质风险，见 CR-19-09、CR-19-10）。

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :21,:118 | `SimTile::payload` / `assign(512u*512u, i)` | `512*512` float | 模拟一 tile buffer 的所有权对象 | 结构性不适用 | 夹具尺寸，与被测语义无关；数值与生产 tile 边长同族（`sampler.cpp:76 constexpr int kTileWidth = 512`），但此处只造内存体量 | — | CONFIRMED |
| :27,:28 | `bounded_queue_capacity(1024,256)==4u`、`(1000,256)==3u` | 1024/256/4/1000/3 | 容量除式与向下取整的正例 | 结构性不适用 | 夹具期望值，锁 `async_io.h:30-31` 的式（合同 `ASYNC_IO_CONTRACT.md:62` 同式） | — | CONFIRMED |
| :29 | `(0, 256) == 1u` | `0 → 1` | 预算为 0 时的容量 | 待确认 | 该断言把"预算缺失"钉成合法取值；同条件在分块规划器侧判 unfeasible（`block.h:40` + `block.cpp:13-16`）⇒ 两入口异判 | 方向：偏绿（预算缺失不报错）；影响范围 = 一切以该函数为容量源的预取深度 | CONFIRMED |
| :30 | `(100, 0) == 1u` | `item_bytes=0 → 1` | 条目尺寸未估出时的容量 | 待确认 | 同族（`async_io.h:29`），合同 `ASYNC_IO_CONTRACT.md:16` 只要求 `item_bytes` 含全部占用、未规定 0 的处理 | 方向：偏绿；影响范围同上 | CONFIRMED |
| :34,:45,:56,:87,:107,:138,:169,:191,:220 | 队列容量字面量 | 4/4/2/1/2/2/8/16/2 | 各用例的背压强度 | 结构性不适用 | 测试自订参数：小容量用来制造背压，不进科学语义 | — | CONFIRMED |
| :60,:111,:151,:168,:189,:190 | 循环上界/并发数 | 4/10/100/`N=256`/`N=1000`/`Consumers=4` | 条目数与消费者数 | 结构性不适用 | 循环上界与线程数是实现选择（前言 §4 明列不报项） | — | CONFIRMED |
| :67 | `sleep_for(100ms)` 后断 `pushed==2` | 100 ms | 用墙钟代替同步 | 待确认 | 无任何条文/标定；正确写法是事件同步 | 方向：**只会误红不会误绿**（生产者线程未被调度 ⇒ `pushed<2` ⇒ :68 判红）；影响范围 = 高负载 CI 档 | CONFIRMED |
| :72-:73,:77-:78 | `for i<100/200` + `sleep 5ms` 轮询 | 500 ms / 1000 ms 上界 | 等待生产者推进 | 待确认 | 同上，纯时序预算，无出处 | 方向：超时 ⇒ :74/:79 误红；不产生误绿 | CONFIRMED |
| :91,:224 | `sleep_for(20ms)` / `sleep_for(30ms)` 后 `cancel` | 20/30 ms | "假定对端已阻塞"的唯一依据 | 待确认 | 无条文；且该假设不成立时用例仍全绿（见 CR-19-09） | 方向：**误绿**（未阻塞也判过）；影响范围 = 合同 §8 要求的"cancel 唤醒阻塞者"这条覆盖声明 | CONFIRMED |
| :112,:143 | 注入失败的条目号 | 5 / 3 | 模拟读失败/写失败点 | 结构性不适用 | 夹具自订 | — | CONFIRMED |
| :184,:215 | `ASSERT_EQ(consumed.size(), N)` / `EXPECT_EQ(total, N)` | — | 不丢不重的判据 | 结构性不适用 | 判据本身，无阈值 | — | CONFIRMED |

## findings

### CR-19-09 · 两条"唤醒阻塞者"用例只靠 sleep 假定阻塞发生，未发生时同样全绿；合同点名的 `CancelWakesBothBlockedSidesNoDeadlock` 在跟踪集里根本不存在
- 轴：C（附 R）
- 位点：
  - `async_io_test.cpp:90-:94`
    ```
        std::thread producer([&] { pushed = q.push(1); });
        std::this_thread::sleep_for(std::chrono::milliseconds(20));
        q.cancel("read failed");
        producer.join();
        EXPECT_FALSE(pushed.load());
    ```
  - `async_io_test.cpp:223-:227`（消费者侧同形态：起线程 → sleep 30ms → cancel → `EXPECT_TRUE(consumer_returned_null)`）
  - `docs/architecture/ASYNC_IO_CONTRACT.md:78` 点名的用例名
- 上位依据：`ASYNC_IO_CONTRACT.md:69-78` §8「必须覆盖：……cancel 唤醒阻塞者并传播错误……已完成：`phase2_async_io` 以上全部通过，含……`CancelWakesBothBlockedSidesNoDeadlock`」。
- 现状（两条独立事实，均已复算）：
  1. 用例内没有任何"对端确实已进入阻塞"的同步点。若线程启动晚于 20 ms/30 ms，`error_`/`closed_` 已置位，则 `push()`/`pop()` 的等待谓词第一次求值即为真 ⇒ 立即返回 false/nullopt，**从未阻塞过**；此时断言集 `EXPECT_FALSE(pushed)`、`EXPECT_TRUE(consumer_returned_null)`、`has_error()`、`error()` 全部成立。不等式方向定死：**该用例的判定集是"阻塞被唤醒"与"根本未阻塞"两种交织的交集 ⇒ 对"唤醒"这一主张判别力为零**（能红的只有 `pop()/push()` 在取消后仍返回可用值这一类错误）。
  2. `git grep -rn "CancelWakesBothBlockedSidesNoDeadlock" -- .` ⇒ 全仓唯一命中就是 `ASYNC_IO_CONTRACT.md:78` 那一行；本文件实际定义的 10 个 TEST 名（`git grep -n "^TEST"`）为 `…CancelWakesBlockedAndPropagatesError`（:86，只测生产者被阻塞）与 `…CancelWakesBlockedConsumerNoDeadlock`（:219，只测消费者被阻塞），**两侧同时处于阻塞的场景从未被任一用例覆盖**。
- 差在哪：登记面点名一个不存在的验证锚，并且被点名的那个场景（两侧同时阻塞、cancel 必须同时唤醒两个条件变量）恰好是拆分后的两条用例都不覆盖的合取；合同 §8 的"已完成/全部通过"因此没有对应实体。
- 后果：`ASYNC_IO_CONTRACT.md:75-78` 与 `:110-:112`（error/cancel、failure drain 的 PASS 结论）指向不存在的测试；CON-008 的门面在"双侧阻塞"上为零判别力。
- 定级：**S3**——不改变任何产物字节（该队列生产零调用，见 CR-19-06），但属"合同强制点名的测试实体缺失"这一已判形态；按 §3 规则写不出产物失真 ⇒ 不升 S2。
- 怎么算修好：(a) 新增真正的双侧阻塞用例：容量 1、先 push 满，再起两线程（一 push 一 pop），两者各自置位"已进入调用"的原子标志并由主线程等到两个标志后置位后再 `cancel`，最后断言两线程都在有界时间内返回；(b) 两条既有用例补"确已阻塞"的前置判据（例如 cancel 前断言 `q.size()==capacity` 且生产者标志未置位）；(c) `ASYNC_IO_CONTRACT.md:78` 的用例名改为实存名，或让 (a) 用它。**只改文档名不补 (a) = 该覆盖缺口仍在。**
- 置信：CONFIRMED（用例全文读过；不存在性由跟踪集检索确定，非本机磁盘）。

### CR-19-10 · `BackpressureBlocksProducerUntilConsumer` 用 sleep+轮询代替同步，判据在高负载下只会误红
- 轴：R（测试面）
- 位点：`async_io_test.cpp:67-:69` `std::this_thread::sleep_for(std::chrono::milliseconds(100)); EXPECT_GE(pushed.load(), 2); EXPECT_LE(pushed.load(), 2);`
- 上位依据：`ENGINEERING_SPEC.md` 测试规则（本条不引科学面）；`ASYNC_IO_CONTRACT.md:71` 只要求"队列满背压"这一性质，未规定以墙钟判定。
- 现状：`EXPECT_GE(pushed,2)` 需要生产者线程在 100 ms 内完成 2 次 push；`EXPECT_LE(pushed,2)` 需要在同一窗口内没有第三次成功——后者由阻塞保证（成立），前者纯靠调度。`ctest` 与本仓并行档位（AGENTS §3 的重计算并发）下线程启动延迟超 100 ms 不是异常事件。
- 差在哪：一个"确定性"性质（谓词在锁保护下恒成立）被实现成时间窗判据，于是判红条件依赖调度器而非被测代码。
- 后果：CI 抖动（假红），按 AGENTS §9「红灯先自修」的纪律会被反复重跑掩盖；不产生假绿（方向已定：`pushed` 只可能因慢启动偏小，不会因代码错误偏大——第三次 push 在容量满时必须阻塞）。
- 定级：**S3**。
- 怎么算修好：把 :67 的 sleep 换成"等到 `pushed>=2` 或有界超时"的事件等待（文件内 :72-:73/:77-:78 已有同款轮询，把它用于 :68 即可），并把 :69 的上界断言改为"在 pop 之前 `pushed` 恒为 2"——即在 :70 的 `pop()` 之后再取一次 `pushed` 做上界判定，使上下界都不依赖调度。
- 置信：CONFIRMED。

---

# 文件 6 · `lib/algorithms/coverage/include/astro/phase2/block.h`（47 行，整读）

## §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|---|
| :6 | 峰值模型 `≈ P·N_B·B_sample + P·B_scratch + M_AIO + M_UPM + M_ACR` | 公式 | 内存规划器估算峰值的加式模型 | 公式导出 | 实现侧按式算：`block.cpp:39 budget = memory_limit_bytes*sf`，各项由 `P2BlockPlannerInput` 的 `scratch_bytes_per_sample/scratch_bytes_per_pixel/fixed_overhead` 供入；说明：这是工程估算式，不含科学语义，也无文献锚要求（AGENTS §7 把它归基建） | — | CONFIRMED |
| :30 | `safety_factor`（注释自称"默认 0.75"） | `0.75` | 内存上限的折扣：只有 `0.75·limit` 算可用预算 | **待确认** | 目的句唯一：`docs/architecture/PERFORMANCE_MODEL.md:48`「`kP1FrameMemSafetyFrac` 0.75（不变）——留基础占用与运行波动」，**无推导式、无标定件**；`eng/packaging/config/config_registry.json` 与 `defaults.json` 无该键（`git grep -n "safety_factor\|0\.75" -- eng/packaging` 仅命中无关的 `star_detection.limiting_mag_safety`）；代码侧三处各写一遍字面量：`block.cpp:20`（`<=0` 回退 0.75）、`tools/stage2.cpp:735`、`tools/stage2.cpp:1011`；另 `docs/algorithms/PHASE2_MOSAIC_WRITE.md:103`、`docs/modules/registry/astrocs.phase2.write.md:110` 复述 0.75 | 方向：**值越大越激进**（sf→1 预算垫越薄？不：`budget = limit*sf`，sf 越大可用预算越大 ⇒ 越接近真实上限，OOM 风险升；sf 越小越保守但块变小、并行下降）。合法域 (0,1] 在头文件与实现都**无校验**（`block.cpp:20` 只挡 `<=0`），传 `sf=5.0` 会让预算 = 5×limit 且无任何报错 | CONFIRMED |
| :26,:27,:29,:31,:32,:33 | `output_pixels`/`covering_frames`/`memory_limit_bytes`/`scratch_bytes_per_sample`/`scratch_bytes_per_pixel`/`fixed_overhead` | 无默认（调用方填） | 规划器入参 | 结构性不适用 | 结构体字段无内建值，本行不引入常数；`memory_limit_bytes==0` 由实现判 unfeasible（`block.cpp:13-16`） | — | CONFIRMED |
| :28 | `precision; // 0=fp32, 1=fp64` | 0/1 | 精度枚举编码 | 结构性不适用 | 枚举基数/编码，非科学量；与 `integrate.h`/`rejection.h` 的 dtype 编码同族 | — | CONFIRMED |
| :39 | `micro_chunk_required; // 1=需 tile 内 micro-chunk` | 1 | 输出标志 | 结构性不适用 | 布尔标志编码 | — | CONFIRMED |
| :40 | `status; // 0=ok, 1=unfeasible` | 0/1 | 规划结论 | 结构性不适用 | 状态码；与 `coverage.h` 的 rc 约定同族 | — | CONFIRMED |
| :41 | `char error[256]` | 256 | 错误文本缓冲 | 结构性不适用 | 缓冲区尺寸；实现侧以 `sizeof(out->error)-1` 为界的 `strncpy` 写入（`block.cpp:14-15/:60-62`）。已核对同模块 `coverage.h:53 char error[512]` 与 `README.md:112` 的"out->error[512]"说的是 coverage 面而非本结构 ⇒ **两处尺寸不是矛盾，是两个适用域** | — | CONFIRMED |

（:7-:10 的其余文字为语义说明，无数值。表内 :30 是本批唯一带科学/工程语义争议的常数。）

## findings

### CR-19-11 · `safety_factor` 只有注释级默认、无配置键、无合法域校验，`>1` 时内存闸被静默放大
- 轴：S（附 C）
- 位点：`block.h:30` `double safety_factor;             // 默认 0.75`
- 上位依据：`ASTROCS_DESIGN.md` §8 与 AGENTS §7「程序全局配置落 `eng/packaging/config/`（filters.json / defaults.json）」；`docs/architecture/PERFORMANCE_MODEL.md:48` 给出该分数唯一理由句"留基础占用与运行波动"（无推导、无标定件）；同族实测见 `PERFORMANCE_MODEL.md:155`「20.2 GB > 0.75A = 16.13 GB（**超预算 25%**）」——该条属 p1 帧工闸门（`PERFORMANCE_MODEL.md:130 F = min(L, floor(0.75·A/(P·B)))`），**与本结构体是两个适用域，我只把它当"该分数并非充分安全垫"的旁证，不作结论**。
- 现状：头文件把一个直接决定内存预算的乘子的"默认"写成注释，C 结构体本身无默认机制；实现只挡非正值（`block.cpp:20 in->safety_factor > 0.0 ? in->safety_factor : 0.75`），**不设上界**；配置面（`eng/packaging/config/config_registry.json`、`defaults.json`、`runtime_resources.json`）无此键——后者有同族的 `"safety_frac": 0.75`（`runtime_resources.json:48`），即"同一个分数在资源面是配置项、在块规划面是写死字面量"。
- 差在哪：一个安全垫乘子的值既无推导也无标定，且其合法域（应当是 `(0,1]`，>1 就没有"安全"可言）既不在头文件声明、也不被实现拒绝，还绕开了配置面 ⇒ 三处字面量各说各话。
- 后果：任何调用方（含未来的动态分块接入）传 `sf>1` 即可让 `p2_block_plan` 报告"可行"并把块开得比 `memory_limit_bytes` 还大；本机 swap 永久禁用（AGENTS §3），触发的是**全局 OOM killer 连带杀掉同单元服务**，不是本进程报错退出。
- 定级：**S2（缺生产触发路径）**——`p2_block_plan` 已被登记为生产不可达：`eng/ci/ledgers/prod_wiring.json:190-198`「判决证据=调用点仅在非生产面：`eng/tests/unit/p2_block_plan_test.cpp`,`lib/algorithms/coverage/tests/sanitize_driver.cpp`,`synthetic_gate.cpp`」，且 `docs/KNOWN_LIMITATIONS.md:81` 记生产 mosaic 走固定 `kP2TileLeafSpan = 512ULL*512ULL`、无内存驱动分块。若动态分块按 `docs/algorithms/PHASE2_MOSAIC_WRITE.md:221`（"micro-chunk 内存规划（p2_block_plan; unfeasible → rc=6）"）接入生产，此条即 S1。
- 怎么算修好：(1) `block.h:30` 把合法域写进注释并在实现里校验 `(0,1]`，越界 ⇒ `status=1`+`error` 载因，禁止 `<=0 → 0.75` 这种"把未设置当成默认"的静默回退（改为显式判非法）；(2) 值来源单一化：读 `eng/packaging/config/runtime_resources.json#safety_frac`（已存在同值键），删掉 `stage2.cpp:735/:1011` 的字面量；(3) 在 `docs/science`/`docs/algorithms` 侧补一条该乘子的推导或标定（现仅目的句）；(4) 若同时接入生产，须一并更新 `prod_wiring.json` 与 `KNOWN_LIMITATIONS.md:81` 两条登记面，否则"改代码不改登记"仍判红。
- 置信：CONFIRMED（三处代码字面量、配置面零键、登记面不可达证据均已逐字读到）。

---

# 派单与清单差异（前言 §5.5 要求）

派单正文称本批为「`lib/algorithms/coverage` 首段——**含天光平面求解与"背景逆方差"相关实现**」，但清单 `CR-19.txt` 的 6 个文件里**既无天光平面求解器，也无逆方差权重的构造实体**：

- 天光平面求解的实际载体是 `lib/algorithms/coverage/src/sky_plane.cpp`（`p2_sky_plane_eval_delta` 等）与 `lib/algorithms/coverage/src/upm.cpp`——两者不在本批（本批仅 `acr_kernels.*`、`async_io.*`、`async_io_test.cpp`、`block.h`）。
- "背景逆方差/叠加权重"的构造实体在生产侧位于 `lib/infrastructure/scheduler/src/module_adapters.cpp`（逐像素 ivar 面开读在 :11486-:11495、:11764-:11765）与 `lib/algorithms/coverage/src/sampler.cpp:1116-:1131`（`o.control_ivar`/`o.ivar` 取值）——也不在本批。
- 本批能给的、与该主题**直接相关**的代码事实只有一条：ACR kernel 的权重构造 `acr_kernels.cpp:154-:158` 读的是"帧 × 8×8 cell"的派生面并平方，缺 `F_ref²` 除法（见 CR-19-05、CR-19-03）。

**处理：以清单为准**（前言 §5.5），上述两条指派在本批内以"检索式 + 零承载 + 指认真载体所在文件"的方式作答（CR-19-01、本节），未擅自扩读清单外文件作 finding 位点；为定 S 级与触发路径而做的跨文件取证（`stage2.cpp`、`stage2_common.cpp`、`integrate.h`、`rejection.h/cpp`、`sampler.cpp`、`module_adapters.cpp`、`docs/*`）只作依据引用，findings 全部落在本批文件行上。

---

# 本批最高危三条

1. **CR-19-02**（`acr_kernels.cpp:141` vs `:264-:331`）：`ACR-IVAR-001`"ivar 权重必须走 CPU canonical"的 fail-closed 守卫只装在 CPU launcher，CUDA launcher 不读 `wmode`，而调用方路由在有 GPU 时先进 CUDA ⇒ `TRACEABILITY.csv:5` 的 `VERIFIED` 与 `stage2.cpp:1229-1232` 的对外主张在一条臂上不成立。
2. **CR-19-11**（`block.h:30`）：内存安全垫乘子 0.75 无推导/无标定/无配置键/无上界校验，三处字面量各写一遍；`sf>1` 可让规划器报告比 `memory_limit_bytes` 更大的块，在本机（swap 永久禁用）触发的是全局 OOM 而非本进程报错。
3. **CR-19-03**（`acr_kernels.cpp:158`）：buffer3 在供给面已被改成 cell **ivar**、消费面仍按 **SNR** 平方且缺 `F_ref²` 除法，两口径只靠一个 `wmode==2` 的 throw 分隔，缺省臂仍是 §9.73 裁决 A44 宣称"已删除"的 `support×SNR²`。

（三条共同的可达性事实：`stage2_common.cpp:512-528 p2_acr_block_eligible` 无条件 `return false` ⇒ 本批 ACR 路径在生产不可达，故全部按 S2 封顶，注明"缺运行时触发路径"。）

---

# 我读不动或需要实跑才能定的

1. **CUDA kernel 侧的权重口径**：`mosaic_reject_cuda` 把 buffer3 原样交给 `api.submit_mosaic_reject`，真正的 cell 几何与 `snr²` 语义在 GPU kernel 源里。要看的件：`lib/infrastructure/acr/**` 的 `.cu`/bridge 实现（`acr_cuda_bridge.h:209` 起）。该跑什么：本机无 CUDA，只能读 kernel 源码逐行比对 legacy；若已有 ACR 隔离档产物，跑 `eng/tests/cli/test_iso_acr_gpu_isolation.py` 的对应隔离检查确认发布面不含该臂。
2. **逐像素 ivar 臂与帧级臂在生产各自的实际取值路径**（本批只证到"ACR 臂不是二者之一"）：需 `module_adapters.cpp:11486-11850`（`AIO_HIPS_RD_IVAR` 开面、`has_ivar[d]` 分支）＋ `sampler.cpp:1116-:1131` 整读，属别的批次；该跑的是 `phase2_ivar_wiring` 测试目标（`eng/build/build.sh:34` 列名）与 `lib/algorithms/coverage/tests/ivar_wiring_test.cpp`。
3. **"参考天光平面排除自身"是否真在求解器里含自身**：我在本批只能给"零承载 + 指认载体在 `variance_propagation.cpp:145`/`module_adapters.cpp:10151`"。要定案须整读 `coverage/src/sky_plane.cpp` 与 `upm.cpp` 的平面构式（法方程行是否含该帧自身采样点），并跑 `实验/additive-sky-seamless` 单元的负例（真值无效应⇒归零）。
4. **`sf>1` 是否有任何调用面真能构造出来**：`p2_block_plan` 的三处调用点我都读了字面量 0.75，但 `eng/tests/backend/test_p2005_block_io.py` 与 `p2_block_plan_test.cpp` 里是否已有越界取值的用例/门未逐行读（不在本批清单）；要定案须读 `eng/tests/unit/p2_block_plan_test.cpp`。
5. **`plan.normalization_floor = 1e-12` 在 MEDIAN_CENTER 档不生效**这一点由 `rejection.cpp:2117-2123` 的分支结构确定；若要判"注释误导"是否已有门覆盖，需实跑 `phase2_routing`/`rejection` 门并加一条 `median_scale` 的 ACR 等价用例（当前标量布局无该槽，见 CR-19-05 附表 :101 行）。

---

## 收工 `git status --porcelain` 原文

```
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工逐字相同 ⇒ 本批全程只读，仓库工作树零改动（未跑构建/ctest/门禁/`eng/**`，未 `import` 仓库 Python，未写 git config；仅 `status`/`log`/`grep`/`show`/`ls-files`）。

本批中间件未建目录：全部结论直接落本文件，无 `独立审计/复算件/CR-19/` 需求（前言 §1"能不建就不建"）。
