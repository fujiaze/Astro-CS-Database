# 通读 CR-15 · lib/algorithms/noise_snr 第 1 段（6 文件）

- 被审基线 HEAD=`c8f64e9a`
- 清单来源：`独立审计/批次清单/CR-15.txt`
- 派单前言：`DISPATCH-CODE-READ-PREAMBLE.md` §1/§2/§2.5/§3/§4/§5
- 只读纪律：零 git 写；不构建、不 ctest、不跑 `eng/**`、不 import 仓库 Python。

## 开工 status --porcelain 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

## 本批重点核的三条上游结论（待代码事实回填）

1. 设计冻结"控制点层存绝对 SNR"，而合同要求稀疏层携带逐像素**预测方差**——该合同对象在 `lib/**` 实测零生产者。本批文件里谁该写它、实际写了什么？
2. 权重口径上位只允许"用被估计量自身的总方差"——帧级与逐像素两臂的权重来源逐个赋值核。
3. 不可估计情形是否被写成合法数值（本仓已有"零方差被伪装成有限小方差、绕过 fail-closed 门"先例）。

## 文件清单与进度

1. `lib/algorithms/noise_snr/cpp/src/information_weight.cpp` —— 已读：是（表 21 项，findings 5 条：CR-15-01/02/03/04/05）
2. `lib/algorithms/noise_snr/include/astrocs/v6/information_weight.h` —— 已读：是（表 8 项，独有 1 项待确认，其余并入同批 findings）
3. `lib/algorithms/noise_snr/src/module_entry.cpp` —— 已读：是（表 30 项，findings 5 条：CR-15-06/07/08/09/10）
4. `lib/algorithms/noise_snr/cpp/src/noise_model.cpp` —— 已读：是（表 44 项，findings 6 条：CR-15-11/12/13/14/15/16/17 中前 6 项属本文件，CR-15-17 为本批最高危）
5. `lib/algorithms/noise_snr/cpp/test/noise_model_science_test.cpp` —— 已读：是（表 24 项，findings 3 条：CR-15-18/19/20；本批唯一 S1 = CR-15-20）
6. `lib/algorithms/noise_snr/tests/p1noise/p1noise_abi_layout_probe.cpp` —— 已读：是（表 6 项，findings 1 条：CR-15-21）

<!-- PROGRESS: 6/6（成稿；本行为顶部进度位，文末另有收工标记） -->

---

# 逐文件成稿

## 1 · `lib/algorithms/noise_snr/cpp/src/information_weight.cpp`（321 行，整读）

### §2.5 数值处置表（本文件 21 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| cpp:37 | Cholesky 主元界 `!(s>0)` | 0.0 | 判 C 是否正定 | 结构性不适用 | 正定性的数学界，0 不是可调阈值 | — | CONFIRMED |
| cpp:75 | 部分主元界 `!(best>0)` | 0.0 | 判 r×r 系统是否奇异 | 结构性不适用 | 同上（零主元=奇异） | — | CONFIRMED |
| cpp:82 | 消元跳过 `f == 0.0` | 0.0 | 跳过零乘子的行消元 | 结构性不适用 | 纯优化分支，不承载语义 | — | CONFIRMED |
| cpp:105 | `!(e.w_info > 0.0)` | 0.0 | W≤0 即不可估计 ⇒ reject | 公式导出 | registry:480 `W_info=1/Var(F_hat)` ⇒ W>0 是定义域 | 代码确实按式算 | CONFIRMED |
| cpp:118,130,143,206,296 | `m == 0` / `points.empty()` | 0 | 空支持域 ⇒ 拒 | 结构性不适用 | 空集界，非科学量 | — | CONFIRMED |
| cpp:122 | `!(sigma2[i] > 0.0)` | 0.0 | 逐像素方差非正 ⇒ 整点拒 | 公式导出 | 07_noise_snr.md:140–142 两态约束（可用需 variance>0） | 代码 fail-closed，无 clamp | CONFIRMED |
| cpp:145,148,275 | `r > 0` | 0 | 秩 0 = 无低秩相关项 | 结构性不适用 | 低秩分解的维度参数 | — | CONFIRMED |
| cpp:150 | `!(d_diag[i] > 0.0)` | 0.0 | 低秩臂的对角部分需正 | 公式导出 | C=D+LLᵀ 的 D 为正定前提 | 代码按式算 | CONFIRMED |
| cpp:165 | `M[αα] += 1.0` | 1.0 | Woodbury 的 I 项 | 公式导出 | (I+LᵀD⁻¹L)，cpp:155 注释与实现一致（已复核展开式） | 真按式算 | CONFIRMED |
| cpp:227 | `sum2 <= 0.0` | 0.0 | A_NEA=1/ΣP² 的定义域 | 公式导出 | PSF_SIGNAL_WEIGHT.md:32 `A_NEA=1/ΣP²` | — | CONFIRMED |
| cpp:227 | `std::fabs(sum - 1.0)` 的 1.0 | 1.0 | PSF 归一目标 ΣP=1 | 结构性不适用 | 归一化的定义常数 1；条件本身见 CR-15-03（ΣP=1 在正本 §2 零出现） | — | CONFIRMED |
| cpp:227 | `kQwRelTol`（同一行的容差） | 1e-9 | PSF 未归一即整臂 reject 的绝对容差 | **待确认** | registry:2015–2024 该条 subject="Q/W==GLS 与 Var=1/W 相对容差"，**不覆盖** ΣP−1；PSF 侧另有一处同名值常数 `psf_information.h:28` | 保守方向：收紧（更小容差）只会更红、不会误绿；影响面 = 白噪臂的可达帧集合 + `a_nea` 声明的有效性 | CONFIRMED |
| cpp:231 | `1.0 / sum2` | 1.0 | A_NEA 定义 | 公式导出 | registry:528 `A_NEA=1/Sum P_p^2` | 代码按式算 | CONFIRMED |
| cpp:232 | `a*a/(sigma_pix2*a_nea)` | — | 白噪闭式 W | 公式导出 | registry:528 `W=a²/(σ_pix²A_NEA)`；与 `w_info_diagonal` 的 a²ΣP²/σ² 代数恒等（已复算） | — | CONFIRMED |
| cpp:259 | `1.0 / e.w_info` | 1.0 | 理想 1/W（Var_true） | 公式导出 | registry:480 | — | CONFIRMED |
| cpp:283 | `quad <= 0.0` | 0.0 | 二次型正性门 | 公式导出 | C SPD ⇒ c̃ᵀC c̃>0 | — | CONFIRMED |
| cpp:288 | `variance_ratio = quad/ideal_inv_w` | — | 自称"Var_approx/Var_true"(h:101) | **待确认** | 无任何文档定义 `c_tilde` 须满足的归一约定；量纲仅在 c̃ 为**已归一**估计权重时才等于无量纲比 | 保守方向：把未归一入参判红而非产出比值；影响面 = 偏差比声明的可信性（见 CR-15-05） | CONFIRMED |
| cpp:308 | `!(out.sum_w > 0.0)` | 0.0 | ΣW=0 ⇒ 不可估计 | 公式导出 | registry:512 Var=1/ΣW 的定义域 | fail-closed，未见 0 冒充可用 | CONFIRMED |
| cpp:312 | `sum_q / sum_w` | — | F̂ | 公式导出 | registry:512 `F_hat=ΣQ/ΣW` | 按式算 | CONFIRMED |
| cpp:313 | `1.0 / sum_w` | 1.0 | Var(F̂) | 公式导出 | registry:512 `Var(F_hat)=1/ΣW` | 按式算；但产品落盘该值即"理想 1/W"，见 CR-15-01 | CONFIRMED |
| cpp:1–3 头注释 | "冻结 §3.4 列出的路径" | — | 声称三条路径皆冻结所列 | 待确认 | 未在 `docs/` 检索到"§3.4"归属哪份文件的该节列出此三路径（检索式 `git grep -n "§3.4" -- docs/algorithms docs/design` 未做，见"读不动"） | — | UNPROVEN |

单位声明（`ADU^-2 / ADU^-1 / ADU`，cpp 无字面量、h:13 注释）＝ `文献值`/合同值，锚 registry:476–522 与 `FZ-UNIT-*`（DATA_SEMANTICS.md:3295 产品族记录层条目）。

### findings

### CR-15-01 · 头注释自称的冻结合同锚 `FZ-WINFO-DIAG-APPROX` 在合同与登记面零条目，而它禁止的"直接报告理想 1/W"正是唯一产品路径做的事，且被消费者门反向锁死
- 轴：C（主）+ D
- 位点：`information_weight.h:10`「`FZ-WINFO-DIAG-APPROX: 仅用对角近似 C~ 时必须报告 c~^T C c~ (真实 C) 与偏差比,`」+ `:11`「`不得直接报告理想 1/W`」；实现 `information_weight.cpp:241`。
- 上位依据：`git grep -n "WINFO-DIAG-APPROX"` 全仓 3 命中＝本头 2 行 + `eng/tests/unit/v6_p1_psfw/p1psfw_tests_winfo.cpp:123`（注释）；`docs/contracts/DATA_SEMANTICS.md` 与 `eng/contracts/data/v6_clause_registry_v1.json` **零条目**。近邻已登记条款＝`v6_clause_registry_v1.json:556 FZ-GATE-PIXIVAR-APPROX`：`"value": "条件: 同点采样+噪声独立+a_k 一致; 门度量 Var_approx/Var_GLS <= 1+epsilon (epsilon 由 ALG-P2-SURF-001 冻结)"`、`"fail_closed": "像素 ivar 近似无误差门声明，或未报告 R~ C_in R~^T 与 Var_approx/Var_GLS → REJECT"`、`"scope": "Phase2 surface_gls"`。
- 现状：`diag_approx_report`（cpp:241–292）实现了这条未登记的义务；`git grep "diag_approx_report"` 在产品侧零调用（唯一外部命中是单元测 `p1psfw_tests_winfo.cpp:125`）。产品唯一 W_info 路径 `phase1_product.cpp:396` 直调 `w_info_diagonal`（入参 `in.point_source.sigma2` 为逐像素对角数组），落盘 `phase1_product.cpp:672 pi["W_info"]`、`:675 pi["flux_variance"] = {{"value", fe.var_f}}`，而 `fe.var_f` 由本文件 cpp:313 `1.0/out.sum_w` 构造＝理想 1/W。消费者门 `phase1_product.cpp:1173–1175` `std::fabs(var_f * w_info - 1.0) > 1e-9 ⇒ fail("Var(F_hat) != 1/W_info")`。
- 差在哪：义务无登记来源；执行者无人调；且门把"var_f 必须恰等于 1/W"钉成硬要求（对 producer 恒真），真去报近似方差反而判红。
- 后果：`point_information` 的"点源严格权重 1/Var(F_hat)"对外主张（07_noise_snr.md §3）在帧内 C 含非对角项时系统性偏乐观，且产品面上没有任何偏差比字段可查；`flux_variance` 是 Phase2 逆方差权重链的倒数基准。
- 定级：**S2**（缺运行时触发路径：`write_phase1_product` 在 `lib/**` 零调用者，只有 `eng/tests/integration/v6_p1/v6_p1_integrate_test.cpp:239/245/413/608`、`v6_p2_integrate_test.cpp:201` 调它；生产 scheduler 侧接线止于 `module_adapters.cpp:15493 → phase2_integrate.cpp:309 open_phase1_product` 的消费面）。
- 怎么算修好：① 在 registry + DATA_SEMANTICS 正式立此条款（或改引 FZ-GATE-PIXIVAR-APPROX 并把 scope 扩到 point_information 并冻结 epsilon），把偏差比接入 `pi` 产品与 `eng/contracts/schemas/unified/point_information.schema.json`；或 ② 由负责人裁决"生产恒用对角 C、不需偏差报告"，则删头注释里的假合同锚 + 删 `diag_approx_report`。两案都必须同步改 `phase1_product.cpp:1173` 那条恒真门（否则改对反而判红）。
- 置信：CONFIRMED（零命中检索式、调用点、门的不等式方向均逐字核对；"恒真"的论证：producer 侧 `var_f≡1/sum_w`、`w_info≡sum_w`，故 `var_f*w_info−1` 只反映 JSON 往返误差，不含科学信息）。

### CR-15-02 · 科学文档点名"生产 GLS 估计量 `w_info_solve`"，但该算子在产品路径零调用；产品唯一入口写死为对角专用助手
- 轴：D
- 位点：`information_weight.cpp:177`「`PointEstimate w_info_solve(const CovarianceView& cov,`」。
- 上位依据：`docs/science/CONTROL_WEIGHT_SNR.md:102`「`F_hat_i` 为**星点测光**给出的源通量估计（生产 GLS 估计量 `astrocs::v6::p1psfw::w_info_solve`，FZ-FORMULA-WINFO/Q）」；同文件 `:105`「**源稀疏**：`S_src` 由有限个 PSF 模板张成（生产 GLS `w_info_solve` + `snr_moffat4_profile_f64`）」；`docs/plugins/algorithms_phase1/07_noise_snr.md:130`「白噪声时为简单形式；相关噪声时用完整信息核」。
- 现状：`git grep -n "w_info_solve" | grep -v "^lib/algorithms/noise_snr/"` 命中＝上述两条科学文档、`实验/absolute-snr/B7_*`（文档/驱动）、`实验/absolute-snr/code/b7_recon_driver.cpp:564`（且该处 `cov.kind = ...::diagonal`）、以及本文件内部 `:254`（被零调用的 `diag_approx_report` 使用）。`CovarianceView`（h:35）在 `lib/**` 除本模块外零构造点（检索式 `git grep -n "CovarianceView" -- lib`）。
- 差在哪：文档以"生产"名义点名的算子在产品代码里没有生产调用点；`dense_spd`（cpp:128）与 `low_rank`（cpp:140）两条完整信息核路径在产品中不可达。
- 后果：SCI-B"源稀疏 + 完整信息核"的对外主张目前仅由 `实验/` 驱动支撑；相关噪声帧进 Phase1 时产品落盘对角近似，且无机制发现（与 CR-15-01 同因）。
- 定级：**S2**（同上，缺运行时触发路径）。
- 怎么算修好：让 `phase1_product.cpp:396` 改走 `w_info_solve`，且 `CovarianceView.kind` 由 `phase1_product.cpp:373` 的 `CovarianceRecord` 真实决定（不得恒取 diagonal）；或把 CONTROL_WEIGHT_SNR.md:102/105 的"生产"改口径为"实验单元实现"并同步 `实验/absolute-snr/B7`。
- 置信：CONFIRMED。

### CR-15-03 · 冻结阈值 `1e-9` 被挪作 PSF 归一化门；该登记条款管的是另一个量，而 ΣP=1 / P≥0 两个条件在正本 §2 与登记面都不存在
- 轴：S（主）+ C
- 位点：`information_weight.cpp:227`「`if (sum2 <= 0.0 || std::fabs(sum - 1.0) > kQwRelTol) {`」；`information_weight.h:30`「`constexpr double kQwRelTol = 1e-9;`」（h:12 自称「FZ-AP1-GLS-QW-RTOL: Q/W == GLS 与 Var = 1/W 相对容差 1e-9」）。
- 上位依据：`v6_clause_registry_v1.json:2015–2024`：`"subject": "Q/W==GLS 与 Var=1/W 相对容差（沿用）"`、`"value": 1e-09`、`"gate": "FZ-FORMULA-WINFO"` —— 不含 PSF 归一。`registry:524–533 FZ-COND-WHITENOISE` 的 `value` 只有公式，`gate`＝"仅 C 对角且 sigma_pix 声明时可用"、`fail_closed`＝"C 含相关项仍无条件用白噪声近似，或未声明 sigma_pix → REJECT" —— 不含 ΣP=1、不含 P≥0、不含任何数值容差。`docs/science/PSF_SIGNAL_WEIGHT.md:17–35`（§2 全文，已整读）通篇无 "ΣP=1"、无 "P≥0"。
- 现状：两个条件只以代码头转述形式存在（`psf_information.h:5`），并分两处各自定义同值常数执行：`psf_information.h:28 kPsfNormRtol=1e-9`、本头 `kQwRelTol=1e-9`；本文件用后者做**绝对**判据 `|ΣP−1| ≤ 1e-9`。同一数值第三处是字面量：`phase1_product.cpp:1174`。另：数学上 `W=a²ΣP²/σ²` 与 `A_NEA=1/ΣP²` 均**不需要** ΣP=1，代码未区分"公式前提"与"面积可解释性前提"。
- 差在哪：FROZEN 阈值被用于其 subject 未覆盖的判据，且同一冻结数在三个位点无共同事实源（负责人"口径唯一"）。
- 后果：① 产品 `point_information.a_nea` 的"归一"前提只有注释级依据；② 任何一处独立改动该容差都不会被一致性门发现；③ 白噪臂与 `w_info_diagonal` 臂对同一 P 的资格判据不同（前者查 ΣP=1/P≥0，后者不查）。
- 定级：**S2** —— 触发路径可写到 `phase1_product.cpp:382`（先跑 `psf_profile_stats`，同判据、同数值），故生产上两臂不一致当前只在 API 面成立；此点如实压低，不宣称产物错值。
- 怎么算修好：为"ΣP=1 的容差"在 PSF_SIGNAL_WEIGHT.md §2 或 registry 立正式条目（含数值、适用臂、绝对/相对之分），两处常数收敛为单一 `inline constexpr` 引用源，`phase1_product.cpp:1174` 的字面量改引该常数；h:12 停止用 Q/W 条款号给归一化门背书。
- 置信：CONFIRMED（登记面条目、正本节、三处站点均已逐字比对；两臂代数恒等已复算）。

### CR-15-05 · `diag_approx_report` 的 `variance_ratio` 没有入参归一约定，任何 c̃ 都算，唯一测试传的是 `c_tilde = P` ⇒ 名为方差比的量在量纲上就不是方差比
- 轴：S（主）+ R
- 位点：`information_weight.cpp:288`「`rep.variance_ratio = quad / rep.ideal_inv_w;`」＋ `information_weight.h:101`「`double variance_ratio = 0.0;      /* c~^T C c~ / (1/W_true) = Var_approx / Var_true */`」。
- 上位依据：条文面零提及 —— 检索式 `git grep -n "c_tilde\|c~" -- docs eng/contracts` 返回**空**（本次实测，见 CR-15-01 的同一条检索）。即：没有任何权威定义 c̃ 是什么、须满足什么归一。
- 现状：`diag_approx_report` 直接取调用方给的 `c_tilde[i]` 算 `quad = Σᵢⱼ c̃ᵢ Cᵢⱼ c̃ⱼ`（cpp:263–267）再除以 `1/W_true`（cpp:259/288）；函数内**没有**对 c̃ 的任何归一或合法性检查。唯一外部调用：`eng/tests/unit/v6_p1_psfw/p1psfw_tests_winfo.cpp:124`「`std::vector<double> c_tilde = P;   /* 理想匹配滤波系数 */`」，随后 `:133` 只把这个数与同一二次型的独立复算比对（`P1_CHECK_NEAR(rep.c_tilde_C_c_tilde, quad, 1e-12)`）—— 即测试锁的是"我的循环 == 你的循环"，没有锁"这个比值是方差比"。
- 差在哪：`F̃ = c̃ᵀd` 只有当 c̃ 是**已归一**的估计权重时才有 `Var(F̃) = c̃ᵀC c̃`；取 c̃=P 时 `quad` 量纲 ADU²、`ideal_inv_w` 量纲 ADU²，比值虽无量纲，但它不等于任何估计量的方差比（缺 `a²(PᵀC̃⁻¹P)²` 归因子）。头注释把该数断言为 `Var_approx/Var_true` 是**定义性错误**，而测试用同一份实现自证，恒真无判据力。
- 后果：这条注释是唯一登记"对角近似必须带偏差比"的地方；若照它接线，产品会携带一个自称"近似/真值方差比"而实际无此含义的数（并会被 FZ-GATE-PIXIVAR-APPROX 的 `<=1+epsilon` 型门拿去判绿 ⇒ 真误绿）。不等式方向：把 `quad` 换成 `P` 而非 `C̃⁻¹P` 使比值可任意放大（c̃ 缩放的自由度未定），门的形式是"比值 ≤ 1+ε"⇒ 偏小的假比值只会**偏绿**。
- 定级：**S2**（缺运行时触发路径：`diag_approx_report` 产品零调用；一旦接线即升级为 S1）。
- 怎么算修好：函数内部自己算 c̃（从 C̃ 与 P 导出并归一，签名改为收 `C̃` 而非裸数组）、或在入口以门强制 `|a·Pᵀc̃ − 1| ≤ tol` 后拒；同批改 `information_weight.h:99–101` 的字段注释与 `p1psfw_tests_winfo.cpp:124` 的入参，并把断言从"两处同实现"换成"与独立 GLS 复算的 Var(F̃)/Var(F) 对比"。
- 置信：CONFIRMED（量纲与归一条件自行复算；测试位点逐字读）。

### CR-15-04 · 头注释声明的拒绝原因词表含一个实现从不产出的串，实现实际产出的多数串不在声明面
- 轴：C
- 位点：`information_weight.h:58`「` * 拒绝原因: "null_input" | "dimension_mismatch" | "non_spd" | "singular".`」；实际串见 cpp:107、118、120、122、130、132、134、143、145、147、150、167、187、203、207、211、215、221、228、234、256、270、297、302、309（`non_finite`、`non_positive_sigma2`、`non_positive_diagonal`、`non_positive_or_non_finite_w`、`unknown_covariance_kind`、`white_noise_condition_not_met`、`negative_sample`、`psf_not_normalized`、`non_finite_w`、`no_points`、`invalid_point_estimate`、`zero_total_w` 等）。
- 上位依据：`dimension_mismatch` 从未被产出（`git grep -n "dimension_mismatch"` 本次未跑全仓，见置信）。
- 现状：三个 `w_info_*` 入口签名不带长度参数，函数**无法**产出 `dimension_mismatch`；长度一致性被推到调用点（`phase1_product.cpp:392`）。
- 差在哪：注释面是调用方唯一的拒绝语义事实源，既虚报又不穷尽。
- 后果：错误登记/审计面对 reject 串做分类时漏项或永不命中；`dimension_mismatch` 的存在会被误读为"维度不一致已防"。
- 定级：S3（不致产物错值）。
- 怎么算修好：声明面改为穷尽实际串的词表并登记为唯一源（参照 `07_noise_snr.md:169` 的 `variance_audit_required_fields()` 纪律：生产者/校验者/测试引用同一份），或删 `dimension_mismatch`。
- 置信：**PARTIAL** —— 缺"合同是否要求 reject 词表穷尽/登记"的条文（已检索 registry 未见词表条目，未逐节读 P3/`FZ-P3-FAILCLOSED` 面）；`dimension_mismatch` 的全仓命中数未实测。

## 2 · `lib/algorithms/noise_snr/include/astrocs/v6/information_weight.h`（128 行，整读）

### §2.5 数值处置表（本文件 8 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| h:30 | `kQwRelTol` | 1e-9 | 白噪臂的 PSF 归一容差（实现处 cpp:227） | **待确认** | registry:2015–2024 subject 不覆盖该用途（CR-15-03） | 收紧只会更红；影响＝白噪臂可达帧集 + `a_nea` 声明有效性 | CONFIRMED |
| h:5 | `W_info,k = a_k^2 P_k^T C_k^-1 P_k = 1/Var(F_hat_k)` | — | 冻结式转述 | 文献值 | registry:476–489 同串逐字一致；锚 UNIFIED §4 / PSF_SIGNAL_WEIGHT §2 / ADJ-P2-01；Horne 1986 经 PSF_SIGNAL_WEIGHT.md:125 引用 | 转述与登记串一致 | CONFIRMED |
| h:13 / h:51–52 / h:84–85 / h:115–118 | 单位串 `ADU^-2 / ADU^-1 / ADU`、`px^2` | — | 量纲声明 | 文献值 | registry:480–522（同单位面）+ DATA_SEMANTICS.md:3295 `FZ-UNIT-*`；A_NEA 单位 px² 见 07_noise_snr.md:10–11 | — | CONFIRMED |
| h:37 | `Kind kind = Kind::diagonal` | 枚举首元 | 协方差视图默认取对角 | **待确认** | 未见任何文档规定"未声明即对角"；默认值恰好等于产品实际唯一用到的臂（cpp 三臂中 `w_info_diagonal`） | 保守方向：默认应为"未声明"非法态（枚举加 `unspecified`），否则忘设 kind 会静默走对角；影响＝C 结构可表达性（与 CR-15-02 同因） | PARTIAL（未见产品侧构造 `CovarianceView`，故无法判定该默认是否真被触发） |
| h:38 | `m = 0` | 0 | 默认空支持域 | 结构性不适用 | 数组长度，0 ⇒ 各入口 `null_input` 拒 | — | CONFIRMED |
| h:41 | `bool sigma_declared = false` | false | 白噪臂需显式声明 σ_pix 才可用 | 文献值/合同值 | registry:531–532 "或未声明 sigma_pix → REJECT" ⇒ 默认 fail-closed 与条文同向 | — | CONFIRMED |
| h:43 / h:46–47 | `c 为 m*m`、`l 为 m*r`、`r = 0` | — | 布局与秩 | 结构性不适用 | 矩阵维度定义，非科学量；行主序与 cpp:29/64 实现一致（cpp:267 的 `c[i*m+j]` 与 r 步长 `i*r+alpha` 已核对为同一约定） | — | CONFIRMED |
| h:51–52, 84–85, 99–101, 115–118 | 各 `= 0.0` 数值默认 | 0.0 | 未算出时的占位 | 结构性不适用 | 伴随 `ok=false`（h:53/82/97/113），0 不与"已算出的合法 0"混淆；本仓先例担心的"0 当合法值"在此不成立（W、a_nea、var 的定义域都是开区间 (0,∞)，无合法 0） | — | CONFIRMED |

### findings
- CR-15-01 / -03 / -04 / -05 的位点均含本头（假合同锚 h:10–11、常数 h:30、词表 h:58、方差比注释 h:101、生产算子点名的实现面 h:66）。
- 本头独有：`h:37` 的 `Kind::diagonal` 缺省与 `h:5–13` 的"合同锚"清单里含零登记条目（`FZ-WINFO-DIAG-APPROX`）——后者已在 CR-15-01 计；`h:16`「`W_info 唯一权威式即 a^2 P^T C^-1 P, 不从任何诊断量反推`」的纪律，本文件内部合规（cpp 无任何诊断量反推路径），但产品侧是否另有权重实现属 integration 批次，见本批"读不动"第 3 条。

## 3 · `lib/algorithms/noise_snr/src/module_entry.cpp`（1636 行，整读）

事实基座（先立后 used）：本 TU 唯一的编译者是 `lib/algorithms/noise_snr/CMakeLists.txt:40` 的 `astrocs_p1_noise`(SHARED)，而根 `CMakeLists.txt:320–339` 的 `add_subdirectory(lib/algorithms/noise_snr)` **保持注释**（②「本 add_subdirectory 仍保持注释 ⇒ `astrocs_p1_noise`(SHARED) 仍不在根图」）。它的唯一测试是跟踪面内的 `eng/tests/unit/p1_noise/adapter_test.cpp`，注册在 `eng/tests/unit/CMakeLists.txt:1323–1338` 的 `if(TARGET p1noise_under_test AND TARGET astrocs_p1_noise)` 门卫块内——两个前置 target 都不在根图 ⇒ **该 ctest 用例从不注册**（同文件 `:653` 的 p1noise 子图门卫则因 `tests/p1noise/CMakeLists.txt` 已跟踪而为真，故科学/ABI 面在跑；两回事，勿混）。⇒ 本文件的全部数值处置与 findings 都发生在"已入库但不出产、其专属用例不进 ctest"的面上。

### §2.5 数值处置表（本文件 30 项；同族同值已合并并列全位点）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :62 | `kVersion` | "1.0" | 模块描述符版本串 | 结构性不适用 | 版本标识，非科学量 | — | CONFIRMED |
| :69,722,788,850,880,1043 等 | `NOISE_ECODE_EXCEPTION` | 130 | 异常屏障细分诊断码 | 结构性不适用 | 诊断码词表（types.h 定义，注释自述"P1-CAL/COS 同款"） | — | CONFIRMED |
| :82 | `noise_err_msg[1024]` | 1024 | thread_local 错误消息缓冲 | 结构性不适用 | 缓冲区尺寸；vsprintf 截断不越界 | — | CONFIRMED |
| :116,120 | `cap - 1` / `n >= out->cap` | 1 | 留 NUL 位的截断语义 | 文献值/合同值 | `lifecycle_v1.h` 冻结的 strbuf 截断语义（:106–107 注释引用） | — | PARTIAL（未读 lifecycle_v1.h 原文核对截断条款） |
| :142,177,194 | `char pat[128]` | 128 | JSON 键匹配模式缓冲 | 结构性不适用 | 键名长度上界；本文件全部键来自 types.h，均 ≤ 24 字符 | — | CONFIRMED |
| :217 | `if (n == 0) return 1;` | 0 | 空 base64 串=合法空平面 | 结构性不适用 | 尺寸校验由 :565 `n_data != hw*bytes` 与 :577/:660 各平面长度门兜住 | — | CONFIRMED |
| :218,221 | `n % 4 != 0` / `pad > 2` | 4 / 2 | base64 帧与填充约束 | 文献值 | RFC 4648 §4（4 字符→3 字节，pad ≤ 2） | — | CONFIRMED |
| :222–239,274–275 | 解码表 `62/63/0..63`、字母表 | — | base64 字符↔值映射 | 文献值 | RFC 4648 Table 1（标准字母表，非 URL-safe 变体） | — | CONFIRMED |
| :241 | `cap = (body/4)*3 + (pad==1?2:(pad==2?1:0))` | 4/3/2/1 | 解码后字节数上界 | 公式导出 | base64 长度反演式；代码按式算 | — | CONFIRMED |
| :249–251,270,278–289 | `<<6 / bits+=8 / &0xFF / 4ull*((n+2)/3)` | 6/8/255/4/3/2 | 位打包与编码长度 | 结构性不适用 | 位宽与进制换算的定义常数，不承载科学语义 | — | CONFIRMED |
| :313,339 | `snprintf(p, 64, "\"%s\":\"", key)` | 64 | 键前缀写入的局部上界 | **待确认** | 无文档；与 :1093/:1365 预留的 `+64` 同数，实际键最长 24 字符 ⇒ 现安全 | 保守方向：截断只会让 JSON 少写而非越界（snprintf 有界），偏红；影响面=输出 manifest 可读性 | CONFIRMED |
| :354,398 | `dtype 0/1`、`dt > 1ull` | 0/1 | f32/f64 词表与穷尽门 | 结构性不适用 | 枚举基数；>1 显式拒绝（词表扩充需同改此处） | — | CONFIRMED |
| :422,611,643 | `0x7fffffff` | 2^31−1 | h/w/n 的 int 域上界 | 结构性不适用 | 防 :1004/:1013 `(int)c->h` 截断，实现选择 | — | CONFIRMED |
| :558,577,611,649 | `8ull / 4ull` | 8 / 4 | f64/f32 字节宽 | 结构性不适用 | sizeof 定义常数 | — | CONFIRMED |
| :650,657,1093,1158 | 控制点平面数 `5` | 5 | x/y/sig/var/ivar 五面 | 结构性不适用 | 枚举基数，与 types.h:123–127 五键一一对应 | — | CONFIRMED |
| :767–769 | `mem = is_diag?16:(is_build? h·w·(1+8) : h·w·4)` | 16 / 9 / 4 每像素 | plan 的 memory_bytes_estimate | **待确认** | 注释自称"config 元数据推导, 无魔数"，但只计掩膜 u8+兜底样本 f64，**未含**本 op 自身的 malloc（:1045/:1057 out_var/out_ivar 各 4·h·w；:1099 total≈(8/3)·h·w 输出串；输入平面 4–8·h·w） | 方向＝**低估** ⇒ 调度器按小预算放行更多并发＝偏绿；影响面＝峰值内存与 OOM（AGENTS §3 记载本机 swap 永久禁用）；见 CR-15-08 | CONFIRMED |
| :771,1471,1482 | `buf[768] / body[512] / node[256]` | 768/512/256 | 输出 JSON 栈缓冲 | 结构性不适用 | 缓冲区尺寸；:1509 注释给出 768 的构成但无静态断言 | — | PARTIAL（未逐项复算 diag 分支最坏串长） |
| :779,867,1385 | `min_workers:1` / `"floor_fallback":0` / `variance_floor_bound:1` | 1 / 0 / 1 | plan 与输出面的固定标记 | **待确认** | `floor_fallback:0` 与 types.h:165 键注释"1=注册表 miss 回退"和 `adapter_test.cpp:666` 断言的 `"floor_fallback":1` 三方不一致 ⇒ 见 CR-15-06 | — | CONFIRMED |
| :774,1118,1386 | `ASTROCS_NOISE_PLAN_VERSION`、`%.17g` | v1 / 17 | plan 版本、double 十进制往返位数 | 结构性不适用 | 17 = IEEE-754 binary64 最短往返位数（定义常数） | — | CONFIRMED |
| :945,950 | 租约失败 detail `105` | 105 | ACS_ERR_BUDGET 诊断码 | 结构性不适用 | 注释自述"P1-CAL/COS 同款"的码表约定 | — | PARTIAL（未读码表登记面） |
| :699 | `out_desc->phase = 1` | 1 | phase1 | 结构性不适用 | 枚举（module.yaml phase_scope: phase1） | — | CONFIRMED |
| :701 | `execution_class = 0` | 0 | cpu_heavy | 文献值/合同值 | `lib/include/astrocs/abi/module_api_v1.h:50`「`0=cpu_heavy 1=io 2=metadata`」逐字一致 | — | CONFIRMED |
| :702 | `parallel_ok = 0` | 0 | 事务内串行 | 文献值/合同值 | `docs/algorithms/NOISE_ESTIMATION.md:187` 明载本 descriptor 的 parallel_ok 是迁移整改点（现状 0） | — | CONFIRMED |
| :832 | `alloc(..., 16)` | 16 | 实例对齐 | 结构性不适用 | 对齐量 | — | CONFIRMED |
| :1093–1098 | `total = 1024 + 5·(b64(n·8)+64) + fill·(b64(h·w·4)+64) + 256` | 1024/64/256 | estimate 输出堆预算 | 公式导出（缓冲） | 逐项核对：骨架 snprintf ≈ 400 B + scalars ≈ 64 B（含在 1024 内）；5 控制点面与 ≤2 输出面各有独立 +64 前缀余量；256 收口 ⇒ 无越界 | — | CONFIRMED |
| :1365–1367 | `b64_enc_len(plane_bytes + 1ull)` | +1 | fill 输出堆预算里多算 1 字节 | **待确认** | 无出处；与 :1096 同位用途写成 `plane_bytes`（无 +1）不对称，方向偏大＝安全 | 保守方向：取 :1096 的无余量式会**刚好**够；影响面＝fill 输出缓冲（当前 +1 更安全） | CONFIRMED |
| :1132,1391,1480,1528 | `sc[4] / sc[3] / sc[1]` | 4/3/1 | 标量 bitwise 通道元素数 | 结构性不适用 | 数组维度；types.h:173 注 `f64 × 4` 与 :1132 一致 | — | CONFIRMED |
| :1253,1280 | `(uint32_t)rows.n_ctrl` / `(u & 0xffull)` | — | 计数与 u8 截断 | 结构性不适用 | 宽度截断，前置门 :643 已限 ≤0x7fffffff | — | CONFIRMED |
| :474 | `!std::isfinite(d) \|\| d <= 0.0` | 0.0 | variance_floor 必须有限且 >0 | 公式导出 | 见 CR-15-07 的 floor 语义（07_noise_snr.md:141–142 两态约束禁止把 floor 当可用值） | 代码在此处 fail-closed ✓ | CONFIRMED |
| :40 | `_POSIX_C_SOURCE 200809L` | — | 特性宏 | 结构性不适用 | 编译期接口版本 | — | CONFIRMED |

### findings

### CR-15-06 · `floor_fallback` 三张脸：代码写 0、键注释说 1=回退、唯一测试断言 1；同时 inspect 面的 `last_floor_fallback` 仍恒置 1
- 轴：C（主）+ R
- 位点：`module_entry.cpp:1385`「`"floor_fallback":0,"variance_floor_bound":1,`」＋ `:1442`「`inst->last_floor_fallback = 1;`」（fill 成功路径）＋ `:866` inspect 输出该字段。
- 上位依据：`lib/algorithms/noise_snr/include/astrocs/noise/types.h:165`「`#define NOISE_O_KEY_FLOOR_FALLBACK "floor_fallback" /* fill op: 1=注册表 miss 回退 */`」；`eng/tests/unit/p1_noise/adapter_test.cpp:666–667`「`CHECK(fill_out.find("\"floor_fallback\":1") != std::string::npos, "D6 floor_fallback=1 (DISP-NOISE-002 跨 ABI 忠实)");`」；`07_noise_snr.md:141–142` 两态约束（禁止把 floor 当合法可用值发布）。
- 现状：`:1382–1384` 注释自述"原恒写 1（掩盖了 1e-12 静默回退）… 现如实登记 ⇒ floor_fallback=0"，输出面改 0；但（a）键定义面 types.h 的语义注释没跟上，（b）唯一断言它的测试还钉在 1，（c）同一函数返回前 `:1442` 又把实例状态字段置 1，与它自己刚写的输出串 0 相反，而 estimate 路径 `:1202` 置 0。
- 差在哪：同一个布尔事实在输出 JSON、键定义注释、实例 inspect 面、测试断言四处说的是**两个相反的值**；G3-6 声称的"如实登记"只覆盖了四处之一。
- 后果：`last_floor_fallback` 是 inspect/观测面唯一能看到的 floor 事实（`:866`），fill op 每成功一次它就报"发生过静默回退"⇒ 任何按 inspect 计数回退的门将长期假红，从而被训练成"这个字段没用"＝真回退时无人接；测试面若 target 恢复即必红（红在断言旧值的一侧，等于把正确改动算成缺陷）。不等式方向：本条是布尔字段而非阈值，"假红"与"假绿"同时成立（inspect 假红、输出面无判据），不宣称单向误绿。
- 定级：S3 —— 本 TU 不在构建图、其测试不注册，无运行时触发路径。
- 怎么算修好：同批四处对齐：`:1442` 改 0（或删除该字段并改 inspect 为 `variance_floor_bound`）、types.h:165 注释改述新语义（0=已显式绑定，1=注册表 miss）、`adapter_test.cpp:666` 断言改新值并补一条"缺 variance_floor ⇒ 显式错误"的正/负例；若决定保留 1 的历史语义，则 `:1385` 必须回退并由 bind 结果决定该位。
- 置信：CONFIRMED（四处位点均逐字读到）。

### CR-15-07 · G3-6 的"fail-open→fail-closed"整改整落在一个不编译、其测试不注册的 TU 里，生产噪声面走的是另一条 STATIC 路径
- 轴：D（主）+ C
- 位点：`module_entry.cpp:471–481`（config 侧拒绝非法 floor）与 `:1320–1344`（fill 侧显式 `snr_noise_model_v1_bind_variance_floor`，失败 ⇒ `ACS_ERR_PARAM` 不落平面）——这两处就是本文件对外的"钳位 fail-closed"整改。
- 上位依据：`docs/contracts/PUBLIC_API.md:411–412`「MOD: astrocs.p1.noise-snr（迁移目标 astrocs_p1_noise.dll，落码由 P1-NOISE-IMPL 建立）」、`:473–474` 同义；`docs/contracts/DATA_SEMANTICS.md:526–527` 同段；`docs/modules/MODULE_MAP.yaml:234/237` 声明 `target: astrocs_p1_noise_snr` / `module_id: astrocs.p1.noise_snr`，而 `:833` 自登记「合同声明的独立 CMake target 全仓未定义（DESIGN §8.4 独立 target；实现缺口）」；`CMakeLists.txt:332`「本 add_subdirectory 仍保持注释 ⇒ `astrocs_p1_noise`(SHARED) 仍不在根图」；`07_noise_snr.md:136` 生产接入 = 源文件直接编入 STATIC `astrocs_phase1_noise`（根 `CMakeLists.txt:807–810`，其源列表**不含** `src/module_entry.cpp`）。
- 现状：本文件由 `lib/algorithms/noise_snr/CMakeLists.txt:35/40` 唯一引用，而该子目录不被根图纳入；其测试 `eng/tests/unit/p1_noise/adapter_test.cpp` 在 `eng/tests/unit/CMakeLists.txt` 零注册。另外 module_id 也分裂：`types.h:30` `"astrocs.p1.noise"` vs MODULE_MAP.yaml:237 `"astrocs.p1.noise_snr"`（alias 面 :238 把前者降为弃用别名）。
- 差/invoke：登记面把模块 DLL 当合同载体（PUBLIC_API/MODULE_MAP/DATA_SEMANTICS 三处），代码面这条载体不产出；于是"config 层拒绝非法 floor""fill 层必须显式绑定 floor"这类科学纪律级整改，在交付物里**没有对应的可执行体**，而 STATIC 路径（`cpp/src/noise_model.cpp`）的 floor 语义并不被本文件的门保护。
- 后果：对外主张「floor 钳位已 fail-closed（G3-6）」与「p1.noise 是 C ABI 模块面（API-NOISE-001）」目前在构建图上不成立；`07_noise_snr.md:138–139` 登记的"生产调度未挂 variance 块"缺口也失去一个可验证的接入点（本应经此 vtable 的 `estimate_noise_model`+`fill_noise_field` 产逐像素面）。
- 定级：**S2**（缺运行时触发路径——恰恰因为无路径；主张链见上位依据逐跳）。
- 怎么算修好：三选一并在同批登记：① 恢复根 `add_subdirectory` + 注册 `adapter_test`（并修 CR-15-06）；② 明文把 `module_entry.cpp` 标为 unshipped（合同与 07 文档同步降级，且不得再引用它作 floor fail-closed 的证据）；③ 把 floor 的合法性判定下沉到 `noise_model.cpp`（生产 STATIC 路径），使两条入口同一判据（照 07_noise_snr.md:169 的"生产者/校验者/测试引用同一份"纪律）。module_id 与 MODULE_MAP 的 `astrocs.p1.noise` vs `astrocs.p1.noise_snr` 同时定一面。
- 置信：CONFIRMED。

### CR-15-08 · `noise_plan` 的 `memory_bytes_estimate` 漏计本 op 自身的输出平面（对 estimate 档低估约 3 倍），且并发/租约面把它当事实
- 轴：R（主）+ S
- 位点：`module_entry.cpp:767–769`「`uint64_t mem = is_diag ? 16ull : (is_build ? c.h * c.w * (1ull + 8ull) : c.h * c.w * 4ull);`」＋注释 `:765–766`「build: 掩膜 u8 + 全帧兜底样本 f64 上界; fill: 输出 f32 平面」。
- 上位依据：`AGENTS.md §3`（本机物理约 23 GiB、swap 永久禁用，重计算须按峰值预算；调度器预算是内存看门狗之外的第一道）；`lib/include/astrocs/core/plan_estimator.h:69/104`（`execution_class` 与 `is_heavy` 由 descriptor 同源判定，即 plan 面是调度准入依据）。条文面未见对 `memory_bytes_estimate` 的构成规定（检索式 `git grep -n "memory_bytes_estimate" -- docs` 未做，见置信）。
- 现状：estimate op 在同一事务里还 malloc 了 `:1045`/`:1057` 两张 `h·w·4` 输出平面、`:1099` 一张 `total ≈ (4/3)·2·(4·h·w) + 常数` 的输出串缓冲（b64 展开），外加输入 `data` 平面 `h·w·4|8`（`:560`）与可选 `mask` `h·w·4`；申报值只有 `9·h·w`。fill op 申报 `4·h·w` 但同样另 malloc 两张输出串（`:1368`）。
- 差在哪：估式把"科学内核的临时量"当成全部峰值，漏了 adapter 自己的 I/O 面；同一 op 的申报值与自身 malloc 位点集合不闭合。
- 后果：4096² 帧下申报 151 MB 而实际（含 base64 输出串 ≈ 4/3·4·hw·2 ≈ 268 MB + 输入面 + 输出平面）≈ 500 MB 量级 ⇒ 调度器按申报值并发放行的帧数偏多，与 AGENTS §3 记述的 OOM 连带杀 DSH 的失效模式同向（偏绿）。
- 定级：**S2** —— 缺运行时触发路径（本 descriptor 不进构建图；生产 STATIC 路径不经 `noise_plan`）。
- 怎么算修好：估式按"输入平面 + 输出平面 + base64 展开系数 4/3 + 内核临时量"逐项相加并写成具名常量（或改由生产源提供一个 `snr_noise_model_v1_memory_hint`）；补一条 plan-vs-execute 实测峰值的 Oracle（1024²/4096² 两档）；同批核对 STATIC/scheduler 侧对应的自订估算是否同错。
- 置信：**PARTIAL** —— 缺"调度器是否真的按 `memory_bytes_estimate` 做准入"的接线证据（`is_heavy`/`execution_class` 已核，预算消费点未逐跳读）。

### CR-15-09 · 输出串写入失败时 inspect 的 `last_status` 仍报 ACS_OK（execute 已返回错误码）
- 轴：R
- 位点：`module_entry.cpp:1205–1210`「`inst->last_status = ACS_OK;` … `acs_status ret = strbuf_write(out_json, buf, ..., err);` … `if (ret == ACS_OK) inst->last_status = ret;`」；同型：`:1445–1449`（fill）、`:1517–1518` 与 `:1543–1544`（diag）。
- 上位依据：`:1571–1572` 注释「事务起点: 输出面零写入承诺」与 `strbuf_write` 的 `ACS_ERR_PARAM + BUFFER_TOO_SMALL` 分支（`:120–124`）——即"写不下"是本合同面承认的真实失败态。
- 现状：`last_status` 在调用 `strbuf_write` **之前**置 ACS_OK；写失败时函数返回错误，但 `ret != ACS_OK` 分支不更新 `last_status` ⇒ `:863 "last_status":%d` 报 0(OK)。
- 差在哪：状态面把一次真失败记成成功；反向（写成功再赋值）是冗余赋值而非错误，但两行合起来使"仅写失败"这一类错误在观测面上消失。
- 后果：观测/回归面（inspect JSON）低估失败次数 ⇒ 以 `last_status` 为判据的门偏绿。不等式方向：该字段是"失败计数"语义，把失败记成成功＝单向偏绿（本条已定方向）。
- 定级：S3（本 TU 不编译；且 `last_status` 未见产品面消费者，属观测面）。
- 怎么算修好：`inst->last_status = ret;`（无条件），并把 `:1209/1449` 的"仅当 OK 才赋值"删掉；`last_rc`/`exec_count` 同步以"事务结果"为准（当前 `exec_count++` 在写失败前已发生，也算成一次成功执行）。
- 置信：CONFIRMED（四条路径逐行读到）。

### CR-15-10 · 逐星 flux/fwhm 以 `nullptr` 恒传，注释称"合同面未扩展"，但 types.h 的 star 键面确实只有 x/y 两键：降级是真实的，登记只在注释
- 轴：D（主）+ C
- 位点：`module_entry.cpp:1010–1011`/`:1018–1019`（`nullptr, nullptr,` 两个实参）＋ `:1000–1003` 注释（MASK-002：「合同面未扩展 ⇒ 按 SCI-NOISE-001 §5a 回调规则降级 (r=rmax → 天空预算收缩) 并置 MASK_LEGACY 位标。禁止在此凭空造 FWHM/F 冒充逐星半径」）。
- 上位依据：`lib/algorithms/noise_snr/include/astrocs/noise/types.h:113–119` 的输入平面键词表只有 `data/source_mask/star_x/star_y`（`git grep -n "NOISE_M_KEY_" -- .../types.h` 实测，无 `star_f_*` / `star_fwhm_*` 键）；`docs/science/NOISE_MODEL.md §5a`（注释所指）。
- 现状：合同键面确实无逐星流量/半径通道 ⇒ adapter 只能传 NULL；`model.source` 位标（`:1125` 输出 `"source":%u`）承载 MASK_LEGACY。但 `07_noise_snr.md` 与本批文件里没有任何一处把"生产噪声面恒用 rmax 天空预算、掩膜半径未按逐星 FWHM 收缩"登记为**逐帧可审计的产品字段**（输出只有 `source` 位标，无 `n_stars_with_default_radius` 之类计数）。
- 差在哪：一条会改变逐像素方差面数值的降级（天空预算随掩膜半径变化）只以一个 u8 位标登记，且该位标的解释规则不在合同面。
- 后果：逐像素 `variance`/`ivar` 面的数值受"r=rmax"影响而不可复核；当 Phase2 依 `variance` 做逆方差权重时，误差进权重。
- 定级：S2（缺运行时触发路径，同 CR-15-07）。
- 怎么算修好：扩展 types.h 的 star 平面键（flux/fwhm）并让本文件真消费；或把"r=rmax"的方差影响量化后写入 `variance_audit` 必落字段清单（07_noise_snr.md:163–169 的机器唯一源），使降级在产品面可见。
- 置信：**PARTIAL** —— 未读 `docs/science/NOISE_MODEL.md §5a` 全文与 `noise_model.cpp` 的 rmax 实现（属文件 4 取证范围，已在下一节继续）。

## 4 · `lib/algorithms/noise_snr/cpp/src/noise_model.cpp`（1482 行，整读）

本文件是三条上游结论的落点，先给事实基座（逐跳，全部 `git grep`/`Read` 实测）：

- **逐像素预测方差的唯一生产者就在这一个文件里**：`noise_model.cpp:1323 fill_impl` / `:1412 snr_noise_model_v1_fill` 输出 `out_variance[]/out_ivar[]` 两张 h·w 平面，两态编码（`:1385-1396`：`pred>0 ⇒ max(pred,floor)` 且 float32 可表示性守卫；`pred≤0 ⇒ (0,0)`）。**这就是派单①里"实测零生产者"要说的那个对象——它不是"没写"，是"写了但没有生产调用者"**：
  - 调用者全集（`git grep -rn "snr_noise_model_v1_fill" -- .`）＝①本文件；②`src/module_entry.cpp:1069/1345`（该 TU 不在构建图，见 CR-15-07）；③`lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp:4768`（该 target 自述 `orchestrator.cpp/../include/orchestrator.h:14`「**不含 astrocs_infra_orchestrator**；orchestrator_legacy_cli 为**非发布目标**」，且它经 `dll_loader.cpp:59` 取符号的载体名是 `snr_estimator`，同样不在构建图）；④`cpp/test/noise_model_science_test.cpp`（本批文件 5）。
  - 生产 scheduler 侧：`module_adapters.cpp` 无 variance 挂钩（`07_noise_snr.md:138` 已如实登记）。
- **派单①的前提按字面不成立，须订正**：合同 `eng/contracts/schemas/unified/sparse_snr_layer.schema.json:30–51` 用 `propertyNames` + `not.enum` **明文禁止**稀疏层出现 `variance/var/sigma/sigma2/ivar/w_info/…` 键，`:5` 并写"该层是参考层，**不是 variance/ivar**，不承载帧级权重"；`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md:99` 的"返回预测方差"落在**消费侧重建算子**上，不落稀疏层。逐像素方差的合同载体是独立的 `variance`/`ivar` 面（`DATA_SEMANTICS.md:38–86` §4a）。⇒ 结论"零生产者"成立的位置是"生产链调用面"，不是"lib 里无人实现"。
- **派单②在本文件的实测**：全文件**不含加权方差面 §5c**（无任何 `S_src/g` 项与背景面组合的式子；`snr_noise_gain_variance`（:1472）是独立诊断式，本文件内**零调用**）。权重两臂的赋值逐个核：① 平面拟合臂 `w_i = (v_med/v_i)²`（:490-491）用的 `v_i` 是**被估计量自身**（控制点方差，:1099 `max(patch_var, floor)`）⇒ 与 `07_noise_snr.md:162`、`noise_model.cpp:310-313` 的 Aitken 推导一致，**合规**；② 回退臂 `:496 w[i]=1.0`（均匀权）＝被 §5d 排除的"无权最小二乘"，**无降级位**（CR-15-11）；③ 帧级兜底 `ivar_bg_global = 1/max(vmed,floor)`（:1040-1042）＝背景面倒数，被本模块公共头 `snr_estimator.h:49-50` 标注为"(Phase2 科学加权唯一来源)"，与 `NOISE_MODEL.md:12-13/249` 的禁令正面冲突（CR-15-17）。
- **派单③在本文件的实测**：正向合规面——`:805-807` 非法 floor 显式 `SNR_FLOOR_UNBOUND(-10)` 不产模型、`:1362` fill 侧未绑定即拒、`:1385-1396` 预测≤0 与非 float32-可表示一律落 `(0,0)`（正是先例"零方差伪装成有限小方差"的反面做法）。仍有三处"未测得/不可算"被写成合法数：`r_fence`（:999）、`hull_nonpositive_frac`（:1146-1153）、`snr_noise_gain_variance` 的 `return 0.0`（:1475）。

### §2.5 数值处置表（本文件 44 项；同族同值已合并并列全位点）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :28 | `M_PI` | 3.14159265358979323846 | π（掩膜半径式 :652 用） | 结构性不适用 | 数学常数，无科学语义 | — | CONFIRMED |
| :92 | `kLn10` | 2.302585092994045684017991454684 | dex→相对误差换算（:1188） | 结构性不适用 | ln10 定义；与 `NOISE_MODEL.md:100–101`「dex 口径须除以 ln10」同向 | — | CONFIRMED |
| :95 | `kTrimMeanToSigma` | 0.7316727929211932 | 10–90% 截尾均值 \|r\| → Gaussian σ（:1232） | 公式导出 | 闭式 (5/2)[φ(0.12566)−φ(1.64485)]；**我复算＝0.731673** ✓ 与 `snr_estimator.h:86` 一致 | — | PARTIAL（注释给的两个锚 `PSF.md`/`NOISE_MODEL.md robust_residual_sigma` 未逐一回查是否存在） |
| :119 | MAD→σ 因子 | 1.482602218505602 | 稳健尺度换算（:760/:958/:1066 等全走 `robust_sigma`） | 公式导出 | = 1/Φ⁻¹(0.75)；登记面 `NOISE_ESTIMATION.md:13`（列为 SCI-NOISE-001 冻结项） | — | CONFIRMED |
| :103–107 | `n/2`、`n/2-1` | — | 中位数序统计量 | 结构性不适用 | 定义 | — | CONFIRMED |
| :124 | `saturation > 0.0` 的 0.0 | 0 | 0 = "电平未提供" ⇒ 不判饱和 | **待确认** | `NOISE_MODEL.md:346` ②「帧无 `SATURATE`/`DATAMAX` 时必须存在**显式降级声明**」；本文件无任何 saturation 降级位落点（`MaskPlan.flags` 只有 legacy/degraded 两位） | 方向＝漏检饱和平台像素进方差样本 ⇒ 背景方差被平台散布抬偏（影响见 `NOISE_MODEL.md:346` 实测 6.87e8→1.74e8）；影响面＝逐像素权重场 | PARTIAL（门 `ctest -R p1noise_saturation` 的判据落点在 `tests/p1noise/check_saturation_wiring.py`，本批未读） |
| :152,:167,:175 | `min_samples` | 64（默认） | patch 最少可用样本 | **待确认** | 读数在案：`NOISE_MODEL.md:369`（min=5 ⇒ 单 patch −19.2%、5% 门通过率 0.6%；64 ⇒ −1.25%、92.8%）**但正文未指跟踪标定件路径** ⇒ 按 §2.5 第 3 档不合格 | 方向＝更大的 min_samples 更常判 patch 不可用 ⇒ 偏红（丢控制点）；影响面＝合格 patch 数与平面可解性 | CONFIRMED |
| :156,:817 | `max_rounds` | 2（默认），`max(0,·)` | cosmic 裁剪轮数 | 文献值/登记值 | `NOISE_ESTIMATION.md:144`（rounds 2）；静默钳位已登记 `DISP-NOISE-007` | — | CONFIRMED |
| :161–162,:815 | `clip_sigma` | 5.0（默认），`max(1.0,·)` | cosmic 裁剪 k | 文献值/登记值 | `NOISE_ESTIMATION.md:144`；`07_noise_snr.md:116`（5σ 裁剪） | — | CONFIRMED |
| :161–162 | `med ± k·sig`（**双侧对称**） | — | 裁剪窗形状 | **待确认** | 注释自称"cosmic / hot-pixel 稳健裁剪"，但 cosmic/热像素是**单边正偏**，对称窗同时削暗尾；条文面未见对窗形的规定（检索式 `git grep -n "裁剪\|clip" -- docs/science/NOISE_MODEL.md` 只给 5σ 数值） | 方向＝削双侧使样本方差**偏小** ⇒ ivar 偏大＝**偏绿**；影响面＝σ_MAD 与控制点方差 | PARTIAL（未做数值实验量化偏小量） |
| :183 | `kPlaneGeomRatio` | 0.0625（=1/16，κ≤4） | 控制点云是否张成二维 | **待确认** | 登记面 `NOISE_ESTIMATION.md:205/211`（DISP-NOISE-010）给了"为什么废除绝对阈 1e-24"与实测病态外推 ±62%，但 **κ≤4 这个具体切点无实测边界支撑**；其引用的 MC 落在 `run/SCI-VAR-ADAPT-01`（非跟踪面） | 方向＝↑门槛 ⇒ 更多帧退回全局常量场（丢空间分辨、偏保守/可能误红）；↓ ⇒ 病态外推复活（±62% 那类）＝偏绿；影响面＝`has_spatial_field` 与整张逐像素面 | CONFIRMED（值与登记面一致；缺的是该值的推导） |
| :196–198 | `4.0*sxy*sxy`、`0.5*(tr±disc)` | 4 / 0.5 | 2×2 Gram 特征值闭式 | 公式导出 | 特征多项式求根；代码按式算（我复核 λhi+λlo=tr、λhiλlo=det ✓） | — | CONFIRMED |
| :233,:262 | `2 *`（reserve）、`sqrt(2.0)` | 2 | 差分样本数上界 / 白噪 σ_d→σ | 公式导出 | Var(x_{i+1}−x_i)=2σ²（注释 :218–219 与实现一致） | — | CONFIRMED |
| :287,:289 | `n < 3`、`min_keep < 2` | 3 / 2 | 序统计量判据的最小样本 | 结构性不适用 | 极差/跳变需 ≥3 点、主体需 ≥2 点；不承载科学阈值 | — | CONFIRMED |
| :294 | `2.0*r[min_keep-1] - r[0]` | 2.0 | 判据未触发时登记的"最紧栅栏" | 公式导出 | = r_(min_keep) + (r_(min_keep) − r_(1))，与 :298 的 gap>spread 同族；代码按式算 | — | CONFIRMED |
| :298 | `gap > spread`（隐含倍数 1） | 1 | 剔除栅栏 = 跳变超过保留主体极差 | 公式导出 | `07_noise_snr.md:157–158`「比较基准 = 零假设值 1 本身…与写死的绝对倍数（如 R>3）比较属另一口径」＋「恒等倍数 1」；代码正是与 1 倍极差比较，**与条文一致** | — | CONFIRMED |
| :324,:515,:584 | `kFitEpsScale` | 8.0 | 非负可行性的浮点比较余量倍数 | **待确认** | 注释自述"纯浮点比较余量倍数，不是科学阈值"，但"为什么是 8"无出处 | 方向＝↑ ⇒ 更宽容微小负预测入平面；但逐像素侧 `pred>0`（:1385）仍把负预测编码为不可用，净效果＝`hull_nonpositive_frac` 实测非零 ⇒ 按 `07_noise_snr.md:171` 判不可审计＝**偏红** | CONFIRMED（数值行为已按两分支推演） |
| :339,:345,:361 | `norm>0`、`vv>0`、`fabs(d)>0` | 0 | QR 退化主元 | 结构性不适用 | 零主元判据 | — | CONFIRMED |
| :349,:354 | `2.0 * dot / vv` | 2.0 | Householder H=I−2vvᵀ/vᵀv | 公式导出 | 标准反射式 ✓ | — | CONFIRMED |
| :400 | `1e-12 * na * nb` | 1e-12 | 两约束平行的相对退化余量 | **待确认** | 无数值出处（注释仅称"纯数值"） | 方向＝↑ ⇒ 更多 k=2 子集判退化 ⇒ 候选解集变小 ⇒ 偏保守（可能取不到约束最优） | CONFIRMED |
| :471,:1113,:1325 | `n < 4` / `n >= 4` | 4 | 三参数平面的最少控制点 | 结构性不适用 | 参数数+1 的实现要求 | — | CONFIRMED |
| :490–493 | `ratio²`、饱和用 `DBL_MAX` | — | 相对误差加权 w=(v_med/v_i)² | 公式导出 | `07_noise_snr.md:162` 逐字 `w_i = (v_med / v_i)²`；注释给的推导（常数相对误差 ⇒ Aitken 最优 ∝ 1/v²）成立；**代码真按式算** | — | CONFIRMED |
| :496 | `w[i] = 1.0`（回退臂） | 1.0 | 相对加权不可解时改均匀权 | **待确认** | §5d 只冻结相对加权一种；均匀权＝注释 :312–313 自称的"本缺陷的放大器" | 方向＝高方差（结构污染）控制点杠杆回升 ⇒ 平面被亮 patch 拉偏 ⇒ 暗区方差被低估＝**偏绿**；且无任何位标记录用了哪一臂 ⇒ 见 CR-15-11 | CONFIRMED |
| :566 | `round <= n` | n | 割平面轮数上界 | 结构性不适用 | 循环上界的实现选择（每轮至少加 1 约束） | — | CONFIRMED |
| :574 | `rows[j][0] = 1.0` | 1.0 | 设计矩阵常数列 | 公式导出 | 平面 a+b·x+c·y 的截距列 | — | CONFIRMED |
| :642 | `kMaskMoffatBeta` | 2.5 | 掩膜专用保守翼指数 β | 文献值/登记值 | `NOISE_MODEL.md:345`（oracle 用 Moffat β=2.5）、`:368`（导出链第 5 条）；注释 :636 自述"比默认 Moffat4 β=4 翼更宽 ⇒ 半径更大, 保守"⇒ 与 `SCI-PSF-001` 的 β=4 **刻意不同**且方向保守 | — | CONFIRMED |
| :643–644 | `kMaskLegacy` / `kMaskDegraded` | 1 / 2 | 降级位标 | 结构性不适用 | 位标志基数 | — | CONFIRMED |
| :647–657 | `mask_local_radius` 全式（`beta-1`、`M_PI`、`1/beta`、`q>1`） | — | 掩膜边缘面亮度=kσ_bg 的反解半径 | 公式导出 | 约定 I(r)∝(1+(r/α)²)^{−β} 与 :633 注释自洽；**我用 §11 oracle 参数复算（F=10⁵ ADU, FWHM=3, k=0.1, σ_bg=5, β=2.5）得 17.60 px，与 `NOISE_MODEL.md:368` 记载 17.6 px 一致** ⇒ 代码真按式算 | — | CONFIRMED |
| :650 | `alpha = fwhm/(2·sqrt(2^(1/β)−1))` | 2.0 | Moffat α↔FWHM 反解 | 公式导出 | 与 :647 的同一约定互洽（FWHM=2α√(2^{1/β}−1)） | — | CONFIRMED |
| :689,:883,:913 | `max(rmin, s·radii)`、`min(r, rmax)` | — | 半径下界与硬上界 | 公式导出 | `NOISE_MODEL.md:96/99` 逐字（r_min、rmax、收缩 s） | — | CONFIRMED |
| :706–710,:944–949 | `py*h/gy` 等整数分块 | — | patch 边界划分 | 结构性不适用 | 网格索引 | — | CONFIRMED |
| :813–:817 | `max(2,grid)`、`max(1.0,clip)`、`max(1,min_samples)`、`max(0,rounds)` | 2/1.0/1/0 | 参数下限静默钳位 | 结构性不适用 | **已登记 `DISP-NOISE-007`**（"调用方不可知被钳位"），按前言 §4 不作新 finding | — | CONFIRMED |
| :824–:825 | `r0=10`、`scale=6` ⇒ `rmax=60 px` | 10 / 6 | 逐星半径硬上界 | 文献值/登记值 | `NOISE_MODEL.md:368` 末段（"10/6/60px 由 defaults.json 的 source_ref 三段登记"）+ `NOISE_ESTIMATION.md:144/166` | — | CONFIRMED |
| :826 | `k_sigma` 缺省 0.1 | 0.1 | 掩膜边缘残余面亮度系数 | 文献值/登记值 | `NOISE_MODEL.md:37`「默认 0.1（⇒ 残余方差污染 <1%·σ_bg²）」；本文件 :1258 写"σ_bg 偏差 ≤0.5%"——两者换算一致（1% 方差 ≈ 0.5% σ）✓ | — | CONFIRMED |
| :827–:828 | `r_min=1.5`、`fwhm_floor=0.75` | 1.5 / 0.75 | 半径下界 | 文献值/登记值 | `NOISE_MODEL.md:96` 逐字 | — | CONFIRMED |
| :829 | `budget_patches` | 8 | 天空预算最少合格 patch | 文献值/登记值 | `NOISE_MODEL.md:99/287/334`（禁绕过）；同时被 §5d 判据当 `min_keep` 的下界（:996） | — | CONFIRMED |
| :830,:1262 | `budget_sky` | 9216 | 天空预算最少未掩膜像素 | 公式导出 | `NOISE_MODEL.md:99`「SE(σ̂)/σ ≈ 1.44/√N_sky；取 SE ≤ 1.5% ⇒ N_sky ≥ 9216」= (1.44/0.015)² ✓；**但本文件 :1057 把同一常数写成 1.144** ⇒ CR-15-12 | 若真按 1.144 推 ⇒ (1.144/0.015)²=5811，与 9216 不符 | CONFIRMED |
| :858,:879 | `4.0 * wf` | 4 | 种子/缺流量时的掩膜半径 | **待确认** | 注释称"PSF 尺度 4·FWHM 的保守掩膜"；`NOISE_MODEL.md` §5a 检索（`4·FWHM`/seed）未见该数 | 方向＝↑ ⇒ σ_seed 偏小 ⇒ 第二遍 r_local 偏大（保守）；影响面＝σ_bg 种子与最终半径 | PARTIAL（未逐节读 §5a） |
| :900 | `it < 16` | 16 | 收缩因子二分轮数 | 结构性不适用 | 迭代上界（s 分辨率 2⁻¹⁶≈1.5e-5，确定性） | — | CONFIRMED |
| :1040,:1075,:1099,:1386 | `std::max(·, variance_floor)`、`1e-12` | 1e-12 | 可用方差下界 | 文献值/登记值 | `NOISE_MODEL.md:30/289`（1e-12 属冻结项，变更走工程变更；clamp 只作用于可用方差）+ `NOISE_ESTIMATION.md:13/25/109`；代码语义与"只作用于 pred>0"一致（:1385 外层判据）✓ | — | CONFIRMED |
| :1060,:1064,:1070 | rc=1 时 `ivar_bg_global` 保持 0 | 0 | 整帧退化的显式不可用标记 | 文献值/合同值 | `DATA_SEMANTICS.md:550`「完全退化 rc=1 时 `ivar_bg_global==0.0`（显式不可用标记本身——§4a）」逐字 ✓ 合规 | — | CONFIRMED |
| :1086–:1090,:1255 | `malloc(n*sizeof(double))` | — | 控制点数组 | 结构性不适用 | 内存尺寸（`n` 已被 :829/:996 预算约束） | — | CONFIRMED |
| :1146 | `clip_area_nonneg(hx,hy,0.0,0.0,0.0)` | 0 | 以"零平面"取凸包面积 | 公式导出 | a=b=c=0 ⇒ 所有顶点 `di≥0` 全保留 ⇒ 返回凸包面积 ✓（我按 :449 分支逐字推演） | — | CONFIRMED |
| :1148–:1152 | `if (area_hull>0 ∧ isfinite) 才写 frac`，否则保持 memset 0 | 0 | 审计量 `hull_nonpositive_frac` | **待确认** | `07_noise_snr.md:166/171` 把它列为必落审计字段且"`!=0` ⇒ 不可审计"——**"未测得"与"测得为 0"同为 0** ⇒ CR-15-13 | 方向＝把不可审计读成干净＝**偏绿**（判据 `frac != 0 ⇒ 红`，未测得时恒 0 ⇒ 永不红） | CONFIRMED |
| :999 | `out_model->r_fence = std::exp(fence_ln)` 无条件 | 1.0 | 栅栏的 R 域读数 | **待确认** | 判据不武装（:287/:293 早退）与 `r_ln` 为空（:988 块外）时 `fence_ln` 保持 0 ⇒ `r_fence=1.0`＝**恰为零假设值**；`07_noise_snr.md:166` 要求 r_fence 可审计 ⇒ CR-15-13 | 方向＝"未动作"伪装成"栅栏=1（最紧）"＝偏绿 | CONFIRMED |
| :1163 | `ctrl_variance_range = vhi/vlo` | — | 控制点方差动态范围（审计） | 公式导出 | `07_noise_snr.md:166/256`（必落且 `>0`）；vlo≥floor>0 ⇒ 分母安全 ✓ | — | CONFIRMED |
| :1165 | `out_model->source = 0` | 0 | empirical blank-sky 基线 | 文献值/合同值 | `NOISE_MODEL.md:327`「背景方差面（§5）的唯一基线为 empirical MAD（`source==0`）」逐字 ✓ | — | CONFIRMED |
| :1187,:1196 | `2.5 *`（dex→mag） | 2.5 | 星等定义换算 | 公式导出 | m=−2.5log₁₀F ⇒ σ_mag=2.5σ_dex；`snr_estimator.h:57` 同式 ✓（单位换算定义，不属可调值） | — | CONFIRMED |
| :1191 | 注释 `1.253*σ/√N_eff` | 1.253 | 中位数标准误因子 | **待确认** | 因子落在 `snr_calib_zero_point_standard_error`（本文件 :1194 只调用，实现不在此）⇒ 不在本批核；见"读不动" | 方向＝因子偏小 ⇒ SE 偏小＝偏绿 | UNPROVEN（实现未读） |
| :1211–:1219 | `psf + i*9`；row[0]/[1]/[2]/[5]/[6]/[7]/[8] | 9 + 列号 | PSF 块 [N,9] 列语义 | **待确认** | 三处文档各说一种：本模块头 `snr_estimator.h:82–83`（status,B,flux,cx,cy,fwhm,A,mad,ecc，称"冻结"）／生产者 `lib/algorithms/psf/README.md:46`+`dpsf_psf.cpp:927 out_row[2]=result.cx`（B,A,cx,cy,sx,sy,θ,fwhm_x,fwhm_y）／`docs/algorithms/STAR_PSF_ALGORITHMS.md:16`（第三种）。现唯一实际供料方 `orchestrator.cpp:2374–2395` 与本模块约定一致 ⇒ 见 CR-15-14 | 方向＝若改由 psf 模块直供 ⇒ flux↔cx、fwhm↔sy 全错位（确定性错值，非阈值问题） | CONFIRMED |
| :1222 | `status != 0.0`（含 NaN→1） | — | 拟合状态判定 | 结构性不适用 | **已登记 OPEN**（`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:192` M3-D-003 明列"fit_status 把 NaN 状态误判为拒绝"），按 §4 不重报 | — | CONFIRMED |
| :1231–:1234 | `residual_scale > 0` 才算 σ 与 q_psf | — | 质量代理换算 | 待确认 | `docs/algorithms/STAR_PSF_ALGORITHMS.md:112` 说 `MAD==0` 时"`robust_residual_sigma` 不换算，**q_psf 仍计算**"——代码把两者一起留在 0 ⇒ CR-15-15 | — | CONFIRMED |
| :1248–:1262 | default_config 全 14 个值 | 8,8,10,6,5,64,2,1,1e-12,0.1,1.5,0.75,8,9216 | 生产默认面 | 文献值/登记值（逐项） | `NOISE_ESTIMATION.md:144` 逐项列同值（"默认 8×8/r0=10/scale=6/clip 5.0/min 64/rounds 2/spatial 1/floor 1e-12"）＋§5a 的 k/r_min/0.75/8/9216；`min_samples` 与 9216 两项的**标定件路径**仍未指（见上两行） | — | CONFIRMED |
| :1433 | bind floor `<=0/非有限` ⇒ -10 | 0 | floor 合法性门 | 文献值/合同值 | `NOISE_ESTIMATION.md:25`「非有限或 ≤0 一律 SNR_FLOOR_UNBOUND(-10)，不静默回退」逐字 ✓ | — | CONFIRMED |
| :1465–:1468 | `*α²` / `/α²`，`a2>0 ∧ isfinite` | — | 标度传播 | 公式导出（+已登记偏差） | 式与 :12 注释「x'=αx ⇒ var'=α²var, ivar'=ivar/α²」一致；**ivar 仅在 a2>0 时更新**这一不对称已登记 `DISP-NOISE-005`（`NOISE_ESTIMATION.md:200` 原文即含"variance 与 ivar 在非法 alpha 下可失去互倒关系"）⇒ 按 §4 不重报 | — | CONFIRMED |
| :1475 | `gain <= 0 ⇒ return 0.0` | 0.0 | 增益未知时的"方差" | **待确认** | `NOISE_MODEL.md:327/354` 只把该式定为"§5c 源项/常数项来源，兼作交叉验证"，**未见"无增益 ⇒ 返回 0 ADU²"的条文** | 方向＝0 ADU² 是**零方差**（ivar 应为 +∞），函数无状态位可表达"不可算"；若被并进任何面即复现本仓先例形态 ⇒ CR-15-16 | CONFIRMED |
| :1476 | `max(0.0, signal)` | 0 | 负信号截零 | 文献值/合同值 | `NOISE_MODEL.md:327` 逐字 `max(signal,0)/gain` ✓ | — | CONFIRMED |
| :33,:36,:45 | 两张 `unordered_map` + `std::mutex` | — | 按模型指针键控的 floor 注册表 | 结构性不适用 | 进程级容器；PERF-P1 的加锁纪律已注释自证（热路径零锁）；键为指针 ⇒ 生命周期由 `_free` 的 erase 兜住（:1449） | — | CONFIRMED |

### findings

### CR-15-17 · 本模块只产出"背景方差面"，没有任何"加权方差面 §5c"的生产者，而公共头与本文件的 fill 输出被标为 Phase2 科学加权唯一来源——正本明文禁止这一代用并给出偏绿方向
- 轴：D（主）+ S
- 位点：`noise_model.cpp:1380–1398`（fill 逐像素循环，`pred = a + b·x + c·y` 只含背景面，全式无源项）＋ `:1165 out_model->source = 0;  // empirical blank-sky (production 基线)`；口径声明在同模块公共头 `cpp/include/snr_estimator.h:49–50`「`3. NoiseWeightModelV1 — source-masked blank-sky 稳健方差 → ivar`／`(Phase2 科学加权唯一来源)`」。
- 上位依据：`docs/science/NOISE_MODEL.md:12–13`「1. **背景方差面**（本文件 §5 的 `variance`）：**空背景随机分量**，供天光建模、诊断与误差报告；2. **加权方差面**（§5c）：**该像素的总方差**，含源光子散粒项，**供叠加与拟合的最优加权**」；`:208 sigma_w^2(x,y) = sigma_bg^2(x,y) + S_src(x,y)/g`；`:249`「**把背景方差面当作加权方差面——那会系统低估源受限像素的噪声、把权重抬高**」；`:175`「把 `variance` 面（空背景口径）当作逐像素物理总方差场」属"不成立写法"；`docs/science/CONTROL_WEIGHT_SNR.md:99`「两者量纲相同（ADU²）、用途不同，**各自独立、取值互不代用**」；`docs/plugins/algorithms_phase1/07_noise_snr.md:7` 职责面含"逐像素噪声"但全文未见 §5c 面的接口。
- 现状：本文件里 `S_src/g` 只出现在 `snr_noise_gain_variance`（:1472–1480）这一**独立诊断式**中，本文件内**零调用**、任何输出字段也不含它与背景面的和；`NoiseWeightModelV1` 的字段面（`ctrl_variance/ctrl_ivar/plane_*/variance_bg_global`）全部是背景量。⇒ 合同要求"供叠加加权"的那个对象在 `lib/**` 没有生产者；而两篇口径（`snr_estimator.h:49-50`、`orchestrator.cpp:4644`「blank-sky 稳健方差 → 逐像素 variance/ivar 块 (Phase2 science 权重来源)」）已把它当作来源。
- 差在哪：被上位点名的能力（含源项的总方差面）在本模块根本没写，同时模块对外接口用"唯一来源"字样把被禁止代用写成事实。
- 后果：逐像素权重在源受限像素系统性偏高 ⇒ 马赛克在亮源周边过度加权高噪声样本，与创新点②（跨帧绝对 SNR→权重）的"绝对"主张相悖；不等式方向：漏加 `S_src/g ≥ 0` ⇒ σ² 只会偏小 ⇒ ivar/权重只会偏大 ⇒ **偏绿**（本条已按"丢一项"的方向定死）。
- 定级：**S2** —— 缺运行时触发路径：本模块的 fill 在生产 scheduler 无调用者（`07_noise_snr.md:138` 登记未挂 variance 块），唯一挂过的 orchestrator 是 `orchestrator.h:14` 自述的非发布目标。**一旦按 `07_noise_snr.md:139` 的接入义务把该接线搬进 scheduler，即升 S1（产物错值）**，故列本批最高危。
- 怎么算修好：① 在 `noise_model.cpp` 增加 §5c 面的生产者（`sigma_w² = 背景面 + S_src/g`，S_src 用 `CONTROL_WEIGHT_SNR.md:102` 指认的正向式 `Σ_i F_hat_i·P_i`，并要求 gain 已知，否则按 `07_noise_snr.md:4.2a` 决策树 fail-closed），或②明确裁决"产品 `variance` 面就是背景面、Phase2 加权另从 SNR 面派生"，那么必须同批改 `snr_estimator.h:49–50` 与 `orchestrator.cpp:4644` 的"Phase2 科学权重来源"字样，并把 `DATA_SEMANTICS §4a` 的 `variance` 定义与之对齐；③两案都要补一条负例：源受限像素上"背景面当权重面"必须判红（`NOISE_MODEL.md:249` 是禁止项却没有对应门）。
- 置信：CONFIRMED（两臂式子、字段清单、零调用与三份口径均逐字核；方向按"漏非负项"定死）。

### CR-15-11 · fill 的两条拟合都失败时发布全局常量场并 `return 0`，不改 `has_spatial_field`、不置降级位——正是本文件 §5d 注释明文禁止的"退回某个全局常量场"
- 轴：D（主）+ R
- 位点：`noise_model.cpp:1341–1349`「`// 法方程数值不可解（生产配置不可达）: 按 §4/§5 几何退化路径退回全局`／`// 常量场（该场恒正, 不是"用常量掩盖负平面"）`」＋ `const double var = m->variance_bg_global;` 逐像素写常量＋`return 0;`；对照同文件 `:213–214`「**§5d 同时明文禁止「平面不可用就退回某个全局常量场」这类常量兜底**」。
- 上位依据：`noise_model.cpp:214`（本文件自述的禁令，出处为 `NOISE_MODEL.md §5d`）；`07_noise_snr.md:140–142`（逐像素面**两态**约束：可用=正且有限；不可用=`variance=0 ∧ ivar=0`）；`:143`「正本 = `docs/science/NOISE_MODEL.md` §5/§7/§9 与 `DATA_SEMANTICS.md` §4a 三态表」；`:166` 把 `plane_a/b/c` 列为必落审计字段。
- 现状：进入该分支的前置是 `m->has_spatial_field == 1`（:1325）——即 build 侧已经拟出平面并写入 `plane_a/b/c`；fill 侧重算失败 ⇒ 发布常量场，但 `has_spatial_field` 保持 1、`degenerate`/`mask_degraded` 均不动、返回码 0。注释的辩护"该场恒正"只说明它不产生负方差，没有触及"provenance 说这是平面场"。
- 差在哪：一条自述被禁止的兜底写在被禁止它的同一文件里，且降级不可见：产品的 `plane_a/b/c` 与实际发布的像素值来自两个不同模型。
- 后果：①`DATA_SEMANTICS §4a` 的逐像素方差面在梯度大的帧上被抹平为帧常数 ⇒ 暗角方差被抬、亮区方差被压 ⇒ 亮区权重偏高（同 CR-15-17 的偏绿方向）；②`variance_audit` 必落字段（`plane_a/b/c`）如实转录一个从未被使用的平面 ⇒ 审计面与产品面脱钩。方向：常量替代平面在 var 高于常数的区域给出过大 ivar ＝ **偏绿**。
- 定级：**S2** —— 缺运行时触发路径（fill 在生产链无调用者，见本文件事实基座）；注释自述"生产配置不可达"未证（我未构造出可达输入，也未证不可达），故同时标 PARTIAL。
- 怎么算修好：三处同改——该分支改为：`out_variance/out_ivar` 全帧写 `(0,0)`（=显式不可用，符合 §4a 两态）并 `return` 一个非 0 码（或在 model 上置新位 `plane_fit_degraded`）；`has_spatial_field` 置 0 或增 `plane_source` 枚举；同时把 :214 的禁令改成正向表述（"不可解 ⇒ 整帧不可用"）。若保留常量兜底，必须同时改 §5d 条文并补"常量场"的登记态，否则条文与实现二者必居其一被违反。
- 置信：**PARTIAL** —— 缺"该分支在何种输入下可达"的构造或反证（需要跑一遍 build 成功/fill 失败的控制点集，本批禁跑构建）。

### CR-15-13 · 两个 §5d 审计量在"未测得"时保持 memset 的合法值（`hull_nonpositive_frac=0`、`r_min/r_max=0`），而 `r_fence` 被无条件写成 `exp(0)=1.0`＝零假设值
- 轴：R（主）+ C
- 位点：`noise_model.cpp:1148`「`if (area_hull > 0.0 && std::isfinite(area_hull) && std::isfinite(area_pos)) {`」——不成立时整块跳过，`hull_nonpositive_frac` 保持 :797 `memset` 的 0；`:999`「`out_model->r_fence = std::exp(fence_ln);`」在 `if (!r_ln.empty())` **块外**，而 `fence_ln` 在 `:287 if (n < 3) return 0;` 与 `:293 if (min_keep > n - 1) return 0;` 两条早退路径上从未被赋值。
- 上位依据：`07_noise_snr.md:163–169`（必落字段含 `hull_nonpositive_frac`、`r_min/r_median/r_max/r_fence`，且"清单的机器唯一源 = `astrocs::noise::variance_audit_required_fields()`，生产者、校验者与测试**必须**引用同一份"）；`:171`「`hull_nonpositive_frac != 0` ⇒ 该方差面不可审计」；`:166` 与 `:255`（判据不武装 ⇒ `n_r_unavailable_patches` 计入、`r_median ≈ 1` 作负例证据）。
- 现状：三条"没测"的路径（凸包面积非正/非有限、可算 R 的 patch 为空、判据不武装）在产品面上分别表现为 `frac=0`（＝"干净"）与 `r_fence=1.0`（＝"栅栏恰等于零假设值"）。模型结构体里没有一个"是否测得"的标志位。
- 差在哪：判据要求"`!=0` 即不可审计"，而"未测量"给出的正是 0；`r_fence` 更糟——1.0 是这套自校准判据的**基准值**（`07_noise_snr.md:157`「该判据的零假设值恒为 1」），未武装时写 1.0 与"实测栅栏=1"完全同值。
- 后果：审计门（`hull_nonpositive_frac`、`r_fence`）在最该红的"没算出来"上判绿 ⇒ **偏绿方向确定**（判据是 `!=0 ⇒ 红`，未测得恒为 0/1）。这正是派单③问的"不可估计情形被写成合法数值"，且与已登记先例（零方差伪装成有限小方差绕过 fail-closed 门）同形态。
- 定级：**S2** —— 缺运行时触发路径（该面在生产链未挂出，`07_noise_snr.md:138`）；但一旦接线即 S1。
- 怎么算修好：给这两个字段引入显式"未测"表示（`r_fence = NaN`/`-1` 与 `hull_nonpositive_frac = NaN`，并在 §4a/`variance_audit_required_fields()` 的词表里登记该表示与判红方向），或新增 `variance_audit_measured` 位；同时把 `:999` 移进 `if (!r_ln.empty())` 并在 else 分支显式置未测；校验器与测试改读同一份清单。
- 置信：CONFIRMED（三条早退路径与 memset 初值逐行核；未实测凸包面积非正的输入构造）。

### CR-15-12 · 同一预算常数在同文件内有两个值：9216 由 1.44 导出，而 :1057 的注释把它归给 1.144 并称"与 §5a 同一预算常数"
- 轴：C（主）+ S
- 位点：`noise_model.cpp:1055–1059`「`// MASK-002: 全帧兜底阈由 min_samples/2 (=32 px) **收紧**为`／`// max(min_samples, budget_sky): 32 个像素不得为整帧定权重。`／`// 依据 SE(σ̂)/σ ≈ 1.144/√N_sky (MAD 路径) ≤ 1.5% ⇒ N_sky ≥ 9216。`」；对照同文件 `:310`「§5a 的误差预算给出 `SE(σ̂)/σ ≈ 1.44/√N`」与 `:1328` 同值。
- 上位依据：`docs/science/NOISE_MODEL.md:99`「预算推导（a priori）: 单 patch 相对误差 c ≈ **1.152**/√N，中位数效率 1.25 ⇒ SE(σ̂)/σ ≈ **1.44**/√N_sky；取 SE ≤ 1.5% ⇒ N_sky ≥ **9216**」；同文件 `:301`「现按 `SE(σ̂)/σ ≈ **1.144**/√N_sky ≤ 1.5%` 要求 `N_sky ≥ 9216`，**与 §5a 同一预算常数**」。
- 现状：代码注释抄了 `:301` 的 1.144 变体，而实现实际取的 9216 来自 1.44 那一支。三个数（1.144/1.152/1.44）在"同一预算"名下并存；算术可判：(1.44/0.015)²=9216 ✓，(1.144/0.015)²=5811 ✗，(1.152/√N)×1.25 才等于 1.44/√N。
- 差在哪：一条 a priori 推导链上的同一个量在三处书写给出两个互斥值，其中被代码注释引用的那个**算不出**代码使用的门限；而 `:301` 自称与 §5a"同一常数"恰与事实相反。
- 后果：①该阈值的**证据链断裂**——按注释复核 9216 会得 5811，复核人只能重新拍数；②负责人"常数须有推导、口径唯一"（文档口径）不成立；③MAD 路径的理论因子我另算得 1.166（=1.4826×0.7866，见置信），三支都不同 ⇒ 门限的科学依据目前只有 1.44 一支自洽。
- 定级：S3（不直接改产物数值；影响的是可复核性与后续改动方向）。
- 怎么算修好：定一支（推荐保留 §5a 的 1.44=1.152×1.25 并给出 1.152 的推导或蒙特卡洛跟踪件），代码注释、`NOISE_MODEL.md:301`、`:99` 三处同批改；同时按 `AGENTS §7`／负责人"三留一不留"把 MC 读数落到 `实验/` 的跟踪件而非 `run/`。
- 置信：CONFIRMED（三个数与算术均可复算）；MAD 因子 1.166 为我按渐近式独立推导，未与仓库任何写法一致，仅作旁证不作依据。

### CR-15-14 · `[N,9]` PSF 块列序三处各说一种，本模块公共头把自家约定称"冻结"；跨边界列序没有任何守卫（ABI 探针只锁结构体首部，且其 `P1NOISE_OFF` 宏定义了从未使用）
- 轴：C（主）+ D
- 位点：`noise_model.cpp:1211`「`const double* row = psf + i * 9;`」＋ `:1213–1219`（`row[2]=flux`、`row[1]=background`、`row[5]=fwhm`、`row[6]=amplitude_above_bg`、`row[7]=residual_scale`、`row[8]=eccentricity`）与 `:856–859/:872–883`（掩膜用的 `plan.flux`/`plan.fwhm` 同序来自调用方）。
- 上位依据（三种互斥布局，均逐字）：① `lib/algorithms/noise_snr/cpp/include/snr_estimator.h:82–83`「psf 行布局为**冻结的** [N,9]: `status(0) B(1) flux(2) cx(3) cy(4) fwhm(5) A(6) mad(7) eccentricity(8)`」；② 生产者 `lib/algorithms/psf/README.md:46`「`psf_params:FLOAT64[N,9]`：`[0]=B [1]=A [2]=cx [3]=cy [4]=sx [5]=sy [6]=theta [7]=fwhm_x [8]=fwhm_y`（dynamic_psf.h:105）」，实测写盘 `lib/algorithms/psf/src/dpsf_psf.cpp:927`「`out_row[2] = result.cx;`」（:1095 同）；③ `docs/algorithms/STAR_PSF_ALGORITHMS.md:16`「`[N,9]` (B, A, x0, y0, sx, sy, θ, residual_scale, q_psf)」。另注：②把它引到 `dynamic_psf.h:105`，该行实为"旧 uint16 API 保留兼容"注释，**该头 :104–118 内没有列序表** ⇒ 锚点悬空。
- 现状：本模块与之唯一实际供料方一致（`orchestrator.cpp:2374–2395` 自写 `row[2]=r.flux; row[5]=(fwhm_x+fwhm_y)*0.5;`），故**当前不出错**；但 psf 模块的批 API 直供的是另一套列序，而 `07_noise_snr.md:139` 的接入义务正是把 orchestrator 的接线搬进 scheduler（搬的即为 `psf` 块列序）。
- 差在哪：同名 schema 串（`psf_params:FLOAT64[N,9]`）下两套互斥列序都被某一面称"冻结/权威"，第三种写在算法文档；本批的 ABI 守卫（文件 6 `p1noise_abi_layout_probe.cpp`）只锁结构体首部偏移与 sizeof，**没有任何一位锁列序**；探针里 `:15–17` 定义的 `P1NOISE_OFF` 宏全文件从未调用。
- 后果：接线一旦改由 psf 模块直供，掩膜半径吃 `flux=cx / fwhm=sy` ⇒ 逐星半径错到不可解释的量级 ⇒ 天空样本混入源 ⇒ 背景方差面与逐像素 ivar 全错（确定性错值，非阈值问题）。
- 定级：**S2** —— 缺运行时触发路径（当前供料方与消费方同约定；错值只在"按接入义务改供料方"时发生）。
- 怎么算修好：① 在跟踪面（`docs/contracts/DATA_SEMANTICS.md` 或 psf 合同）立唯一列序表并把 schema 串带版本号（如 `psf_params:v2`），② 生产者与消费者各出一位静态/运行时断言（`dpsf_psf.cpp` 侧与 `noise_model.cpp:1211` 侧同引一份枚举常量，禁止字面下标），③ 探针补 `P1NOISE_OFF` 的实际使用或删除，并把 9 列语义纳入 lock JSON 字段；④ 订正 `psf/README.md:46` 的悬空锚点与 `STAR_PSF_ALGORITHMS.md:16` 的第三种表。
- 置信：CONFIRMED（四处位点/写盘点均逐字读到；"dynamic_psf.h:105 无列序表"以 :104–118 全文核过）。

### CR-15-15 · 算法文档规定 `MAD==0` 时"`q_psf` 仍计算"，代码在该分支把 `q_psf` 与 `robust_residual_sigma` 一起留在 0
- 轴：C
- 位点：`noise_model.cpp:1231–1234`「`if (r.residual_scale > 0.0 && std::isfinite(r.residual_scale)) {`／`r.robust_residual_sigma = ...`／`r.q_psf = r.amplitude_above_bg / r.residual_scale;`／`}`」
- 上位依据：`docs/algorithms/STAR_PSF_ALGORITHMS.md:112`「| `MAD==0` | `robust_residual_sigma` 不换算，q_psf仍计算 |」；`:96`「q_psf = A / residual_scale」。
- 现状：`residual_scale == 0` ⇒ 两字段都不算（memset 后保持 0），仅 `fit_status` 在 `row[7] <= 0.0` 时置 3（:1226）。按文档执行则会得 `A/0 = ±inf`。
- 差在哪：同一分支两面说的行为相反（文档要"仍计算"、代码要"留 0"）；谁对未定——文档的字面读法会产出非有限值，代码的读法把"不可算"与"极差质量"压成同一个 0。
- 后果：`q_psf` 是 QA/剔星输入（`snr_estimator.h:102–104` 注 fit_status 用途）；若下游按数值排序剔星，0 会被当成最差星而非"未知"，与 `UNIFIED_MODEL.md:58`／`07_noise_snr.md:4.3`「PSF 拟合质量代理只作诊断」不冲突，但**星点级归因会错**（本应"不可算"的星被当成最差星，反之亦然）。`fit_status=3` 是目前唯一的区分面，是否有消费者读它未证。
- 定级：S3（本函数全仓无代码调用者；诊断字段）。
- 怎么算修好：定一面——推荐保留代码行为并把 `STAR_PSF_ALGORITHMS.md:112` 改为"两字段均不换算，`fit_status=3` 为唯一不可算标记"；若采文档面，则代码需 `q_psf = ±inf` 并在合同面登记非有限 QA 的表示与判据；两种都要给 `q_psf` 的"不可算"表示一个显式标记位。
- 置信：**PARTIAL** —— 未读 `q_psf` 的实际消费者（`snr_psf_fit_quality` 全仓零代码调用者，故消费面尚未建立）。

### CR-15-16 · `snr_noise_gain_variance` 在增益未知时返回 `0.0 ADU²`，函数无"不可算"表示面
- 轴：S（主）+ R
- 位点：`noise_model.cpp:1475`「`if (gain_e_per_adu <= 0.0) return 0.0;`」
- 上位依据：`docs/science/NOISE_MODEL.md:327`「Poisson+read noise：`var_ADU=max(signal,0)/gain+(read_noise_e/gain)²` 是**加权方差面**（§5c）的源项与常数项来源」；`:354` `ALG-NOISE-003` 同；`07_noise_snr.md:4.2a` 决策树第 3 条「`gain_e_per_adu<=0`（gain 未知）⇒ **源泊松项 `F·P_i/g` 同样不可加**……退回天空受限口径……产品必须标 `snr_caliber = upper_bound_no_gain`」。
- 现状：返回类型是裸 `double`，无 out-status、无 NaN 约定；`gain<=0` 与 `gain>0 但方差恰为 0` 同为 0.0。该函数在 `lib/**` 的调用者只有 `module_entry.cpp:1522`（不进构建图），但它是 `ALG-NOISE-003` 的权威实现、并作为模块 `noise_diagnostic` 的 `gain_variance` 子操作对外。
- 差在哪：条文规定"增益未知 ⇒ 该项不可加且必须显式降级"，实现给出的是一个**合法数值 0**；0 在方差语义里是"零噪声 ⇒ ivar=+∞"，正是派单③问的形态（本仓已有一次"零方差被伪装成有限小方差绕过 fail-closed 门"的先例）。
- 后果：任何把它当方差用的调用（含交叉验证路径 `NOISE_MODEL.md:342` 的 Poisson 交叉）在 gain 缺失时得到 var=0 ⇒ 权重无限大；或把"未知"读成"零源噪声"，与 §4a 的 `variance=0 ∧ ivar=0 = 显式不可用` 编码语义**相反**（那里 0 是"不可用"，这里 0 是"无噪声"）。
- 定级：S3（当前无生产调用者；一旦按 CR-15-17 建 §5c 面即升 S1）。
- 怎么算修好：返回类型改带状态（或 `NaN` + 合同登记"NaN=增益未知"），并把 `07_noise_snr.md:4.2a` 第 3 条的 `upper_bound_no_gain` 声明接进该路径；§4a 的"0=显式不可用"与本式的"0=零噪声"必须先消歧再改。
- 置信：CONFIRMED（代码事实与两处条文逐字）。

## 5 · `lib/algorithms/noise_snr/cpp/test/noise_model_science_test.cpp`（603 行，整读）

### §2.5 数值处置表（本文件 24 项；夹具类同族合并并列全位点）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :41,:208,:279,:521 | `W/H = 256`、`W4/H4 = 512`、`W6/H6 = 512` | 256/512 | 夹具帧幅 | 结构性不适用 | 测试自订规模，`§4` 明列"实验单元内的自订参数"为假阳源 | — | CONFIRMED |
| :53–:54 | 星位区间 `30.0, w-30.0`；振幅 `200.0, 4000.0` | 30 / 200 / 4000 | 星点分布夹具 | 结构性不适用 | 夹具内容，不是判据 | — | CONFIRMED |
| :59–:61 | `fwhm = 3.0`、`2.3548200450309493` | 3 / 2√(2ln2) | Gaussian 星像宽度 | 结构性不适用（夹具）＋单位换算定义常数 | 2.3548 = 高斯 FWHM/σ 定义 | — | CONFIRMED |
| :62 | `r0 = 12` | 12 | 星像写入半径（4·FWHM） | 结构性不适用 | 夹具裁剪窗，与被测实现无关 | — | CONFIRMED |
| :115,:145,:182,:428,:485,:523 | seed `20260815..20260823` | — | 固定随机种子 | 结构性不适用 | 确定性要求（TEST_STANDARD："确定性固定 seed"）✓ 合规 | — | CONFIRMED |
| :117 | pedestal `+500.0f` | 500 | 加性背景平移量 | 结构性不适用 | 夹具幅度；判据是比值 | — | CONFIRMED |
| :135 | `rel < 0.01` | 1% | SNR-001  pedestal 不变性容差 | **待确认** | 条文面对"pedestal 不变性"的容差未见数值（`NOISE_MODEL.md:345` 只冻结 ≤2% 的 σ_bg 偏差门）；seed 固定 ⇒ 实际余量未知 | 方向＝收紧会误红、放宽会漏检 ⇒ 影响面＝SNR-001 的证据强度 | PARTIAL（未跑，无法报实测余量） |
| :163,:171 | `rel < 0.02` | 2% | SNR-002 var(αx)=α²var 与 ivar 律容差 | 文献值/登记值 | 与 `NOISE_MODEL.md:345`「`\|σ̂_bg/σ_bg−1\| ≤ 2%`」同值同族（该 2% 是本模块冻结的鲁棒性容差；`NOISE_ESTIMATION.md:85` 亦称"sigma 5%（MAD鲁棒性）"分档） | — | CONFIRMED |
| :199 | `rel < 0.02` | 2% | SNR-003 星族不变性容差 | 文献值/登记值 | 同上（`NOISE_MODEL.md:345` 的 2% 门，其判据是 §11 源污染 oracle） | — | CONFIRMED |
| :220 | `\|bias\| <= 0.02` | 2% | SNR-004 空背景 σ 复现 | 文献值/登记值 | `NOISE_MODEL.md:345` 逐字 ≤2% | — | CONFIRMED |
| :225 | `rp >= 0.95 && rp <= 1.05` | 0.95/1.05 | 逐 patch 尺度带 | **待确认** | 5% 带来自 `NOISE_ESTIMATION.md:85`"sigma 5%"档与 `NOISE_MODEL.md:369` 的"5% 门通过率"，但**通过率的期望值未写进本合同**（测试另要求 ≥95% 的点覆盖，见下行） | 方向＝带越窄越易误红 | CONFIRMED |
| :234 | `frac >= 0.95`（变量名叫 p95 coverage） | 95% | 落带 patch 占比门 | **待确认** | 名为 p95 实为"±5% 带内占比 ≥95%"，与 `07_noise_snr.md:131` 的"摘要带 p05/p50/p95"是不同概念；未见条文给出 95% 这个占比 | 方向＝占比门偏低 ⇒ 漏检 patch 间散布失控＝偏绿 | PARTIAL（条文面未见该占比要求） |
| :243 | `gain=1.5, rn=8.0, mu=2000.0` | — | SNR-005 Poisson 夹具 | 结构性不适用 | 夹具参数（§4 假阳源） | — | CONFIRMED |
| :265 | `rel <= 0.05` | 5% | Poisson 交叉一致门 | 文献值/登记值 | `NOISE_MODEL.md:342`「`μ/gain + rn²/gain²` 与经验 `variance_bg_global` 在 **5%** 内一致（SNR-005, 238-272）」——连行号都对上本测试 ✓ | — | CONFIRMED |
| :270 | `< 1e-9` | 1e-9 | `snr_noise_gain_variance` 与解析式恒等 | 公式导出 | 同函数同式（:1477–1479）⇒ 代数恒等，容差只兜 FP 往返 | 注意：这是**自证门**（被测式与期望式同实现），无独立证据资格 | CONFIRMED |
| :304 | `corr >= 0.98` | 0.98 | SNR-006 空间噪声场相关性 | **待确认（已登记缺陷）** | `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:195` **M3-F-003（P1, OPEN）**已登记："合同 §11:115 要求平面系数 a,b,c 在 10% 内复现，测试用相关性替代"；本条不重报为缺陷，只登记其仍在 OPEN | 方向＝相关性判据弱于系数复现 ⇒ 偏绿（已登记者） | CONFIRMED |
| :307 | `norm_rmse <= 0.05` | 5% | SNR-006 归一 RMSE | 待确认（同 M3-F-003 族） | 同上 | — | CONFIRMED |
| :320,:324 | `1e-9`（σ_mag=0.25）、`1e-6`（σ_cal_rel=0.2302585） | — | dex→mag/rel 换算 | 公式导出 | 2.5×0.1=0.25 ✓；ln10×0.1=0.2302585 ✓（我复算），与 `noise_model.cpp:1187/1188` 一致 | 同 :270 的自证形态（期望值即被测式） | CONFIRMED |
| :354,:359,:368–:370 | `1e-12` | — | q_psf 直通/换算一致 | 公式导出 | `q_psf = A/residual_scale` 与列 7 直通（:1231–1234）✓；注意期望值由被测同一列构造 ⇒ 自证 | — | CONFIRMED |
| :363 | `120.0 / 0.7316727929211932` | 0.7316727929211932 | trimmed-mean→σ 因子（测试内**字面复写**） | 公式导出 | 与 `noise_model.cpp:95` 同值，但测试把常数**重新抄写**而非引用被测头 ⇒ 两处独立可漂移（若源码改因子，测试仍绿/仍红于新值，检测不到"改错方向"） | 方向＝抄写值一旦与实现漂移，判据不再约束实现 | CONFIRMED |
| :389 | `< 1e-4`（legacy (A-B)/residual_scale） | 1e-4 | legacy 通道数值回归 | 结构性不适用 | 退休通道的位级回归锁（注释 :371–373 自述语义 = 不入 Phase2 权重） | — | CONFIRMED |
| :394 | `> 0.5` | 0.5 | legacy 值 ≠ q_psf 的对照 | 结构性不适用 | 夹具量差（(A−B)/mad vs A/mad 必差 B/mad=8.33） | — | CONFIRMED |
| :420 | `< 0.03` | 3% | SNR-009 ivar 加权 = 最优 | **待确认** | 注释称"3% 容差覆盖 MC 估计误差"，但该例是**纯解析**（s1=1,s2=3 无随机数）⇒ 无 MC 误差可言；同段 :412 更是 `var_opt < var_eq` 的恒真比较（1/σ 加权必然更优） | 方向＝容差无来源＝该门的证据强度不可评估；:412 属**恒真断言**（见 CR-15-18） | CONFIRMED |
| :448 | `\|rho\| < 0.02` | 2% | SNR-010 权重与信号独立 | 文献值/登记值（且是冲突点） | `docs/science/INTEGRATION.md:77`「权重与信号独立（**由 SNR-010 噪声模型保证**）」⇒ 该 2% 是被上位点名的保证；与 `NOISE_MODEL.md §5c/:249`（加权面**必须**含 `S_src/g`）互斥 ⇒ 见 CR-15-19 | 方向＝若实现 §5c，本门必红（CR-15-17 的"改对反而判红"实例） | CONFIRMED |
| :466 | `q_psf > q_poor * 5.0` | 5 | 好/差拟合分离度 | 结构性不适用 | 夹具自订（60 vs 600 ⇒ 比值恰 10，门取 5） | — | CONFIRMED |
| :577,:588 | fill `!=` 逐元素 exact、零结构体"≠默认" | — | NOISE-WIRE-001 等价/不等价断言 | 结构性不适用（exact 比较）＋ 见 CR-15-20（"≠默认"那条是恒真） | 合同锚 NOISE-WIRE-001（`NOISE_MODEL.md §生产配置`） | — | CONFIRMED |

### findings

### CR-15-20 · 本测试文件与生产签名差 2 个形参，纳入任何构建必编译失败；其"零结构体≠默认配置"断言靠 ABI 拒绝恒真通过
- 轴：R（主）+ C
- 位点：`noise_model_science_test.cpp:119–121`「`snr_noise_model_v1(img.data(), H, W, nullptr, sx.data(), sy.data(), (int)sx.size(), nullptr, &m1)`」＝**9 实参**；生产声明 `cpp/include/snr_estimator.h:214–220`＝**11 形参**（`… star_x, star_y, star_flux, star_fwhm, n_stars, cfg, out_model`）。同型错点：`:122–124`、`:150–153`、`:185–190`、`:212–213`、`:256–257`、`:282–283`、`:431–435`、`:491–492`、`:502–503`、`:530–531`、`:535–536`、`:543–544`、`:547`。
- 上位依据：`docs/TRACEABILITY.csv:23–37`（SCI-NOISE-001..015 的 `test_files` 全列本文件，状态 **VERIFIED**）；`07_noise_snr.md:144`（门 = `ctest -R p1noise_negative` / `p1noise_selfcheck`）；`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:196` **M3-F-004（P1, OPEN）**"噪声科学验证面未接入构建/CTest：SCI-NOISE 15 行全部锚到一个不被任何目标编译的文件"。
- 现状：本文件不在任何 CMake 目标内（`eng/tests/unit/CMakeLists.txt` 无引用；注册的噪声面是另一套 `lib/algorithms/noise_snr/tests/p1noise/*`，见 `eng/tests/unit/CMakeLists.txt:653–655` 的 EXISTS 门卫，该子文件已跟踪 ⇒ p1noise 系列在跑）；文件头 `:20` 仍写手工编译式 `g++ ... ../src/snr_estimator.cpp ../src/noise_model.cpp`，而 `cpp/src/snr_estimator.cpp` 已随 F-CI-002-01 断链解除（根 `CMakeLists.txt:363`）。
- 差在哪：M3-F-004 的既有诊断是"没被编译"；**新事实是它编译不了**（MASK-002 给 `snr_noise_model_v1/_f64` 加了两形参后本文件未跟进）⇒ 即便把 M3-F-004 的处置（正式入库并无条件注册）照做，噪声科学面依旧零可执行验证，且 15 行 VERIFIED 追溯仍无凭据。第二处：`:588–590` 断言 `mz.has_spatial_field != m0.has_spatial_field || mz.n_control_points != m0.n_control_points`，而 `mz` 来自传**零结构体 cfg** 的调用（`:546–547` 返回值未检查）⇒ `snr_noise_model_v1` 在 `:1301` 就 `SNR_ABI_MISMATCH` 早退，`mz` 是 `:528` 的 memset 零值。该门靠"被拒绝"通过：若哪天放宽 ABI（把 0 头当默认），`mz` 会被真填而断言方向不变，测不出"零结构体被当成默认"这一原始 bug 形态。
- 后果：①以 TEST-SNR-001 为锚的 15 条 SCI-NOISE 追溯（含 §4a/§5d/σ_sky 双计负例等对外主张）无有效验证载体；②噪声模型的正向可跑证据只剩 `tests/p1noise/` 一套（该套是否覆盖 SNR-001..015 全部子项，属本批未读面，见"读不动"），两套的对应关系只在 `TRACEABILITY.csv` 里写向这一套。
- 定级：**S1** —— 触发路径按"主张链"成立且逐跳可核：`docs/TRACEABILITY.csv:23–37 VERIFIED` → `test_files = 本文件` → 本文件 `:119` 与 `snr_estimator.h:214` 形参数不匹配 ⇒ 无目标编译（`eng/tests/unit/CMakeLists.txt` 零引用）⇒ 15 行 VERIFIED 不成立。（不是"产物错值"类，而是"对外主张不成立"类，符合 S1 定义的第二支。）
- 怎么算修好：①本文件全部 `snr_noise_model_v1/_f64` 调用点补 `nullptr, nullptr` 两实参（与 `module_entry.cpp:1010/1018` 同形），并把 `:547` 的返回值纳入断言；②把它或等价的 p1noise 用例注册进构建（`eng/tests/unit/CMakeLists.txt`），并把 `:588` 的恒真断言改成"ABI 拒绝"与"零结构体被误当默认"两种形态各自可红；③同批把 `TRACEABILITY.csv` 的 `test_files` 改指真正注册的目标；④删除或改写 `:20` 的手工编译式（`snr_estimator.cpp` 已退役）。
- 置信：CONFIRMED（形参数逐条数过；头/实现两侧声明逐字读到；`eng/tests/unit/CMakeLists.txt` 与根图逐处 grep 核过）。

### CR-15-19 · SNR-010 把"逐像素权重与信号无关"写成硬判据，而上位 `INTEGRATION.md:77` 正是靠它作跨模块前提——与 `NOISE_MODEL.md §5c` 要求的含源总方差面直接互斥
- 轴：C（主）+ D
- 位点：`noise_model_science_test.cpp:444–448`「`const double rho = pearson(ivx, sig);` … `CHECK(std::fabs(rho) < 0.02, msg);`」（`ivx` = 有源场景的 fill 输出 ivar 面，`sig = |scene − 900|`）。
- 上位依据：`docs/science/INTEGRATION.md:77`「**权重与信号独立（由 SNR-010 噪声模型保证）**」；`docs/science/NOISE_MODEL.md:13`「**加权方差面**（§5c）：**该像素的总方差**，含源光子散粒项，供叠加与拟合的最优加权」；`:208 sigma_w² = sigma_bg² + S_src/g`；`:249`「把背景方差面当作加权方差面——那会系统**低估**源受限像素的噪声、把权重**抬高**」；`:99 07_noise_snr.md` 侧 `CONTROL_WEIGHT_SNR.md:99`「两者…各自独立、取值互不代用」。
- 现状：本测试用**背景面** fill 的 ivar 与信号图求相关并要求 |ρ|<0.02（即"权重不随信号变"）；上位把它读作"§5c 不必含源项"的保证。两篇科学文档在此给出相反方向，且代码与测试站在 INTEGRATION 一侧（`noise_model.cpp` 无 §5c 生产者，见 CR-15-17）。
- 差在哪：同一件事（逐像素权重的方差来源）在 `INTEGRATION.md` / `NOISE_MODEL.md §5c` / 测试判据三处给出互斥口径，且被上位引用的"保证"其实是背景面的构造性后果（掩膜剔除源 ⇒ 与源无相关）。
- 后果：这是"改对反而判红"的实例——若按 CR-15-17 补 §5c（含 `S_src/g`），本测试的 ρ 必然显著为负 ⇒ SNR-010 判红；反之保留本门就等于把被禁止的代用固化成验收判据。不等式方向：含源项后 ivar 随 signal 单调下降 ⇒ |ρ| 由 0 增大 ⇒ 判据方向确定为**红**（不是误绿）。
- 定级：**S2**（无运行时触发路径：fill 未挂生产；但两篇权威打架本身按 `AGENTS §10` 属须上呈负责人的裁决项）。
- 怎么算修好：先由负责人裁一口径（逐像素科学权重面 = 背景面 还是 总方差面）。裁为总方差面 ⇒ 同批改 SNR-010 为"ivar 随信号单调下降且与 `S_src/g` 预测一致"的正向判据 + 改 `INTEGRATION.md:77` 的前提表述；裁为背景面 ⇒ 改 `NOISE_MODEL.md §5c/:249` 的适用域（明确"产品 `variance` 面只作误差报告、不作逐像素权重"），并删 `snr_estimator.h:49-50` 的"(Phase2 科学加权唯一来源)"。**两案都不许只改一处。**
- 置信：CONFIRMED（三处原文逐字；方向单调性由 `S_src/g ≥ 0` 定）。

### CR-15-18 · SNR-009 的"逆方差叠加最优"两条断言都不接触被测实现：一条恒真、一条用被测同一公式自证
- 轴：R（主）
- 位点：`noise_model_science_test.cpp:405–412`「`const double var_opt = 1.0 / (1.0/(s1*s1) + 1.0/(s2*s2));` … `const double var_eq = (s1*s1 + s2*s2) / 4.0;` … `CHECK(var_opt < var_eq, msg);`」与 `:414–420`（`var_w` 由 `w1=1/s1²`、`w2=1/s2²` 手算后与 `var_opt` 比，容差 `< 0.03`）。
- 上位依据：`docs/TRACEABILITY.csv:23–37` 把 SNR-009 归入 TEST-SNR-001（本文件）；`07_noise_snr.md:245`「注入点源：理论 `σ_F=1/√W_psf` 与**实测散度**一致」、`:257`「1 worker vs N worker 一致」——即合同要的是"与实测对拍"。
- 现状：`s1=1, s2=3` 是硬编码正数，`var_opt < var_eq` 对任意 s1≠s2 恒成立（柯西不等式），不引用任何被测符号；`var_w` 与 `var_opt` 是同一代数式的两种写法，差恒为 0，故 3% 容差永不触发。本段既不调 `snr_*`，也不读 `NoiseWeightModelV1`，与 `ivar_bg_global`/fill 面无任何耦合。
- 差在哪：登记为"逆方差叠加效率"科学门的这一段，实际是一条**解析恒等式自检**——判据与被测对象无耦合，任何实现退化都不会让它红；注释"3% 容差覆盖 MC 估计误差"还误称其含蒙特卡洛（本段无随机数）。
- 后果：`SCI-NOISE-009`/TEST-SNR-001 的该项 VERIFIED 状态无证据资格；若叠加权重实现被改坏（例如把 `w ∝ 1/σ²` 写成 `w ∝ σ²`），SNR-009 仍绿。方向：该门对错误**永不红**＝偏绿（不是阈值问题，是零耦合）。
- 定级：S2（无产物错值路径；损害的是 15 行追溯的证据资格，与 CR-15-20 同族）。
- 怎么算修好：把本段改为对被测对象取证——用 `snr_noise_model_v1_fill` 产两张不同方差的 ivar 面 ⇒ 按 `w=ivar` 做加权均值 ⇒ 与等权臂在同 seed 蒙特卡洛下比较散度，断言"加权臂方差 ≤ 最优解析值×(1+ε)"且 ε 有来源；同时保留现解析式作为"实现无关"的 Oracle 侧（照 `tests/p1noise/p1noise_oracle.hpp` 的双侧写法）。
- 置信：CONFIRMED（三段代码逐字读；恒真性由 s1≠s2 的代数论证，无需实跑）。

## 6 · `lib/algorithms/noise_snr/tests/p1noise/p1noise_abi_layout_probe.cpp`（77 行，整读）

### §2.5 数值处置表（本文件 6 项）

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :16 | `offsetof(T,F) == 0 \|\| == 4` | 0 / 4 | "跨边界结构首部字段必在偏移 0 或 4" | 结构性不适用 | 注释指 `ASTROCS_DESIGN 7.3 版本化 C ABI`；是布局断言不是科学量 | — | CONFIRMED |
| :19–:24 | `struct_size`/`abi_version` 类型与偏移 | uint32_t / 0 / 4 | 结构首部纪律 | 结构性不适用 | 同上；与 `noise_model.cpp:1281–1289` 的 `abi_check` 同一约定 ✓ | — | CONFIRMED |
| :31 | `abi_mismatch_rc` | 打印 `SNR_ABI_MISMATCH`（锁值 **-9**） | 唯一被钉的返回码 | 登记值 | `p1noise_abi_layout_lock.json:2` `"abi_mismatch_rc": -9` ✓ | — | CONFIRMED |
| :37,:43 | `SNR_NOISE_CONFIG_ABI_VERSION` / `SNR_NOISE_MODEL_ABI_VERSION` | 锁值 1 / 2 | 两结构版本 | 登记值 | lock `:7` `abi_version:1`、`:13` `abi_version:2` ✓ | — | CONFIRMED |
| :32–:74 | `sizeof`/`offsetof` 全表（config 120、model 200 及 18 个字段偏移） | 见 lock | 布局锁的读数 | 结构性不适用 | 纯布局；lock 内部自洽（末字段 `hull_nonpositive_frac@192` + 8B = `sizeof 200` ✓ 我按此式核过） | — | CONFIRMED |
| :15–:17 | `P1NOISE_OFF` 宏 | — | 声明"字段须在偏移 0 或 4"的断言工具 | 结构性不适用 | **定义了但全文件零调用**（`:26–:27` 只用 `P1NOISE_HEAD`）⇒ 意图未落地，见 CR-15-21 | — | CONFIRMED |

### findings

### CR-15-21 · 探针把"跨边界布局纪律"收成可执行断言，却漏掉本批新引入的 fail-closed 拒绝码与 PSF 列语义；自带的 `P1NOISE_OFF` 从未使用
- 轴：D（主）+ C
- 位点：`p1noise_abi_layout_probe.cpp:31`「`std::printf("  \"abi_mismatch_rc\": %d,\n", (int)SNR_ABI_MISMATCH);`」（锁面唯一的返回码）＋ `:15–17`（`P1NOISE_OFF` 定义）＋ `:26–27`（仅两个结构体调用 `P1NOISE_HEAD`）。
- 上位依据：`07_noise_snr.md:144`「门 = `ctest -R p1noise_negative`（`n7_plane_pred_unavailable` / `n7b_dtype_underflow_pair`）+ `ctest -R p1noise_selfcheck`（证明判据能红，非恒真）」；`NOISE_ESTIMATION.md:25`「非有限或 ≤0 一律 **SNR_FLOOR_UNBOUND(-10)**，不静默回退」；`eng/ci/ID_MIGRATION_MAP.md:300` 把 `CTEST-P1NOISE-ABI-LAYOUT` 登记为 KEPT 的 P1 域门（CHK-ABI）。锁文件 `p1noise_abi_layout_lock.json` 全文只含 `abi_mismatch_rc`，无 `-10`、无 rc 值域、无 fill 输出 dtype/stride、无 `[N,9]` 列表。
- 现状：本批 G3-6 整改把"静默回退 1e-12"改成了新拒绝码 `SNR_FLOOR_UNBOUND = -10`（`noise_model.cpp:805/1362/1433`），并且生产 `rc` 值域本身是 {0,1,3,-9,-10} 的语义约定（`types.h:138` 注 0=成功含兜底、1=完全退化、3=非法）；ABI 锁只钉 `-9` 一个数。探针自带的 `P1NOISE_OFF`（本意是"逐字段落在 0/4"的第二条纪律）从未挂到任何字段上，因此"结构首部之外无裸跨边界标量"这一层从未断言。
- 差在哪：一个被登记为 P1 域门的布局守卫，其覆盖面恰好不含本模块**最新引入的两个跨边界事实**（floor 拒绝码、退化码值域）与**风险最高的裸数组列语义**（`[N,9]`，见 CR-15-14），并且有一段写好的断言从未启用。
- 后果：把 `-10` 改成 `0`（或改 `rc=1` 的含义）不会让 ABI 门锁响；`[N,9]` 列序漂移同样不会——正是 `07_noise_snr.md:144` 要求"判据能红"的反面。
- 定级：**S2**（本探针确实注册在 ctest：`lib/algorithms/noise_snr/tests/p1noise/CMakeLists.txt:102–103`，经 `eng/tests/unit/CMakeLists.txt:653` 的 EXISTS 门卫（该子文件已跟踪 ⇒ 门卫为真）；缺的是"错值如何进产品"的触发路径，因 fill 面未挂生产）。
- 怎么算修好：① 探针增打 `SNR_FLOOR_UNBOUND` 与 `rc` 值域表并进 lock；② `P1NOISE_OFF` 或删除（不留未用断言宏）；③ 把 `[N,9]` 列语义（CR-15-14 定案后的唯一表）纳入同一 lock（生产者与消费者各出一位）；④ fill 输出的 dtype/stride（f32、h·w）与两态编码 (`0∧0`) 作为 ABI 事实入锁——与 `n7/n7b` 负例配对。
- 置信：CONFIRMED（探针、lock、注册点、拒绝码位点均逐字读到）。

---

# 本批最高危三条（只挑，不复述）

1. **CR-15-20（S1）**：`cpp/test/noise_model_science_test.cpp` 是 `docs/TRACEABILITY.csv` 上 SCI-NOISE-001..015 共 15 行"VERIFIED"的唯一指认载体，而它与生产签名差两个形参（`snr_estimator.h:214` 11 形参 vs 测试 9 实参）——不是"没接进构建"，是"接进去也编译不过"；旧台账 M3-F-004 的处置（恢复注册）因此不足以恢复验证效力。
2. **CR-15-17（S2，接线即 S1）**：创新点②要用的逐像素"加权方差面 §5c（含 `S_src/g`）"在 `lib/**` 没有生产者，模块公共头与唯一挂过 variance 块的调用点都把**背景面**标为 Phase2 权重来源，而 `NOISE_MODEL.md:249` 明文禁止该代用并给出偏绿方向；`07_noise_snr.md:139` 的接入义务照搬的正是这条口径。
3. **CR-15-01（S2）＋ CR-15-02（S2）＋ CR-15-07（S2）三件同因**：科学文档点名的"生产 GLS 估计量 `w_info_solve`"在产品路径零调用（产品只走 `w_info_diagonal`），配套的对角近似偏差报告条款 `FZ-WINFO-DIAG-APPROX` 全仓零登记，而消费者门 `phase1_product.cpp:1173` 又把"必须报理想 1/W"锁成恒真；承载这些整改的模块 DLL 面（`src/module_entry.cpp`）整体不在构建图。合起来＝"逐像素/点源不确定度"的三条对外主张目前都只有注释与实验驱动支撑。

# 我读不动或需要实跑才能定的

1. **`fit_variance_plane_nn` 双臂回退是否在生产配置可达**（CR-15-11 的"生产配置不可达"是注释自述，我没构造出反例也没证成）。该跑：给 `tests/p1noise` 补一条正例 fixture——控制点集使相对加权子问题必失败而均匀权成功（例如 D 极大 + 三点共顶点），断言 `plane_a/b/c` 与实际 fill 值一致；并在能跑构建的环境下测 `p1noise_adaptive` 是否已覆盖该形态。
2. **`p1noise` 全套是否覆盖 SNR-001..015 的每个子项**（`tests/p1noise/p1noise_tests_core.cpp` 等共 10 个文件不在本批清单）。该跑：`ctest -N -R p1noise` 列名 + 按 `SNR-0xx` 头注释与 `TRACEABILITY.csv` 的 15 行做双向映射；若覆盖齐，CR-15-20 的处置可改为"把 `TRACEABILITY.csv` 的 `test_files` 改指 p1noise 目标"而非修本文件。
3. **`hull_nonpositive_frac` / `r_fence` 的"未测得"形态能否被现有 §5d 审计门抓到**（我核到 `astrocs::noise::variance_audit_required_fields()` 被 `07_noise_snr.md:169` 定为机器唯一源，但没读它的实现与校验者）。该读：`lib/algorithms/noise_snr/include/astrocs/noise/variance_plane_policy.h`＋ writer 侧 `DATA_SEMANTICS.md:605–650` 的 `skipped_variance_plane_not_auditable` 判据；若唯一源已把 0/1.0 当合法读数，CR-15-13 成立且不依赖任何实跑。
4. **`saturation_level` 缺失时的显式降级声明落点**（`NOISE_MODEL.md:346` ②要求有，本文件无；`tests/p1noise/check_saturation_wiring.py` 与 `p1noise_saturation_test.cpp` 未读）。该读这两个文件即可定案（纯静态）。
5. **`kPlaneGeomRatio=0.0625` 与裁剪窗对称性的数值影响**（前者引用的 20,000 次蒙特卡洛在 `run/SCI-VAR-ADAPT-01`，非跟踪面且按负责人"运行产物一律不留"不可复现）。该做：把 MC 重跑一次并把读数落到 `实验/` 或 `docs/` 跟踪件；对称 5σ 裁剪对 MAD 尺度的偏少量可在同一脚本里顺带量化（我未跑任何计算，本批纪律禁跑构建/测试）。
6. **`snr_calib_zero_point_standard_error` 的 1.253 因子**（本文件只调用，实现在别处）——不在本批文件域，属 photometry/ALG 批次。

# 收工 status --porcelain 原文

```
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工输出逐字相同（基线未动，本节点零 git 写、零仓库改动、未跑构建/ctest/`eng/**`、未 import 仓库 Python）。

# 派单文字与清单的差异（前言 §5.5）

- 清单 6 个文件全部整读，派单文字与清单一致；本批重点三条上游结论均按"代码事实"回答，见各文件节首的事实基座。
- 派单①的表述需要订正：合同 `eng/contracts/schemas/unified/sparse_snr_layer.schema.json:30–51` **明文禁止**稀疏控制点层携带 `variance/sigma2/ivar/w_info` 类键（`:5` 并自述"该层是参考层，不是 variance/ivar"），所以"合同要求稀疏层携带逐像素预测方差"不成立；成立的是两条相邻义务——`DATA-002_PHASE_PRODUCT_EXCHANGE.md:99` 要求**消费侧重建算子**返回预测方差，`DATA_SEMANTICS §4a` 要求独立的 `variance`/`ivar` 产品面。本批的事实是：后者的实现存在于 `noise_model.cpp:1323/1412`，缺的是**生产链调用者**（详见 §4 节首基座与 CR-15-17/07）。
- 取证用量：文件 1 约 21 次工具调用（含为两文件共用的权威链研读），其余各文件均在 20 次内；未出现因到限而截断的未交表文件。

<!-- PROGRESS: 6/6 -->

