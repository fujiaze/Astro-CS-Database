# 复核：负责人面与索引（AUDIT-06 第②层独立复核 · ownerface-v）

复核基线：HEAD=`c8f64e9a`；开工 `git status --porcelain` 原文：

```text
?? "ACSD\346\225\264\346\262\273\345\267\245\344\275\234\345\214\205_AUDIT-06.zip"
?? site/
```

与指定基线一致（仅 zip 与 site/ 未跟踪），零 git 写、未跑构建/ctest/`run_checks.py`/任何 `eng/**` 脚本。
本件所有仓库侧取证为「读文件 + `git grep`/`git show`/`git rev-list` + 我自己工作件里的独立扫描器」，
未 `import` 仓库 Python。工作件：`独立审计/复算件/ownerface-v/anchor_surface.py`。

---

## V1 状态登记面自造档位并向全仓传染

**判定：降级**（"加判据"与"发布结论越界"成立；"自造状态词百级传染"与"闭环"两处口径不成立，需按我实测重写；
另有一条成稿未抓到的更硬依据：`§12.5` 明文禁止登记表预写状态，而本档 §3/§6 预写了状态且证据钉在落后 HEAD 817 个提交的旧 BASE）

### 我的独立取证

**① 两侧定义逐字并排**（"加判据"成立）

- 上位正本 `ASTROCS_DESIGN.md:790` 标题即 `### 12.5 状态阶梯（唯一口径）`；表体只有两列
  （`ASTROCS_DESIGN.md:792` `| 状态 | 语义 |`），10 个词，`ASTROCS_DESIGN.md:802` 收口规则原文：
  > 状态由检查与验收现场计算，登记表不预写状态；合成测试或历史节点不等于真实数据/Windows VERIFIED。

- 下位 `docs/owner/RELEASE_STATUS.md:13` 把表扩成三列（加 `| 判据（当前提交内可核） |`），并逐行改语义：

  | 词 | 上位原文（行） | 本档原文（行） | 差 |
  |---|---|---|---|
  | `CONTRACT_READY` | 权威文档/合同/schema 冻结在位（`:793`） | 权威文档/合同/schema 冻结在位；**文档与合同类对象以此为终态**（`:15`） | 加"终态"判据 |
  | `IMPLEMENTED` | 生产源码在位且当前提交内实际执行通过（`:794`） | 生产**（或注册测试面）**源码在位（`:16`） | **扩大合格对象集** |
  | `READY_FOR_OWNER_REVIEW` | 独立一行，标为上限状态（`:796`） | **§0 表内无此行** | 少一词 |
  | 全部行 | 无判据列 | 各加文件锚/命令/rc 判据列 | 加判据 |

- "（或注册测试面）"这一扩口径在全仓的使用面（`git grep -n "注册测试面"`）：`docs/owner/RELEASE_STATUS.md:16`（定义）、
  `docs/owner/PIPELINE_OVERVIEW.md:111`（应用：Phase3 投影"现状为注册测试面…ctest 2/2 PASS"即按扩口径写 `IMPLEMENTED`）、
  另 1 处在 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:160` 与本议题无关 ⇒ 传染 **1 处**，不是百级。

**② 状态词跟踪件数（我自己的命令与口径）**

```bash
git -c core.quotePath=false grep -c -E "\b(CONTRACT_READY|IMPLEMENTED|INSTALLED|NOT_VERIFIED|NOT_IMPLEMENTED|READY_FOR_OWNER_REVIEW|DEFERRED|DORMANT)\b" -- . \
  | awk -F: '{s+=$2; n++} END {print "files="n" occ="s}'
# files=153 occ=517
```
按目录（`... grep -l ... | sed 's|/[^/]*$||' | awk -F/ '{print $1"/"$2}' | sort | uniq -c`）：
`lib/algorithms 38`、`artifacts/evidence 18`、`eng/tools 16`、`docs/modules 16`、`docs/algorithms 14`、
`eng/tests 13`、`eng/ci 10`、`docs/owner 4`、`docs/architecture 4`、其余 ≤5。

**但这一百级用的是 §12.5 上位词表里的词，不是本档自造的词。** 本档真正自造的只有两个，且传播面是我实测的个位数：

- `NOT_READY_FOR_RELEASE`：`git grep -n` 命中 `docs/owner/RELEASE_STATUS.md:39/:45/:128/:145` **4 行 1 档**，
  外加深挖只在历史证据件 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:69` 出现（登记旧轮次问题，不是在用）。
  该词不在 §12.5 十词之内，也不在 `conclusion_vocabulary.json` 的 `ladder_snapshot` 之内。
- `发布结论`：`git grep -n "发布结论"` → `docs/owner/RELEASE_STATUS.md:42/:45/:145`、
  `docs/KNOWN_LIMITATIONS.md:7`、`docs/research/COMPRESSION_CODEC_RESEARCH_PACK.md:48`，3 档 5 处。

**③ 有没有门会因状态词用错变红 —— 没有（写实）**

会读状态词的门共 3 条，全部登记在 `eng/ci/checks.json`（我按注册表核过 id）：

| 门 id | 检查器 | 实际扫描面对状态词的要求 |
|---|---|---|
| `CHK-TRUTHFUL-CONCLUSION` | `eng/tools/quality/check_conclusion_truth.py` | T1 把 `ASTROCS_DESIGN.md` §12.5 **现场逐字解析**，与 `conclusion_vocabulary.json:ladder_snapshot` 比对（`:133-136`）⇒ 只防"词汇表自己漂移"；随后只遍历 `scan_roots`（`conclusion_vocabulary.json:30-33` = `eng`、`artifacts/evidence`），且只对 **JSON 里 `"status"` 键与 `passed`/`tool` 同现** 的取值判越词（`:141-179`）；造词黑名单只 1 个词 `DELIVERED`，且 `denylist_roots` = `eng`、`docs/ci`（`:181`）|
| `CHK-BUDGET-SINGLE-SOURCE` | `check_budget_single_source.py:18,201` | 只判 `budget_sources.json` 里分母的 status ∈ 阶梯 |
| `CHK-ISA-SAME-SOURCE` | `check_isa_same_source.py:21,343` | 只判 `isa_sites.json` 里站点的 status ∈ 阶梯 |

⇒ **`docs/**` 的正文状态词没有一条门按阶梯判越词**（唯一例外：`denylist_roots` 含 `docs/ci/`，但它只查黑名单里的**一个**字面量 `DELIVERED`，不查阶梯、不含本档造的两个词）；`NOT_READY_FOR_RELEASE`（markdown 叙述位）与 §0 表少一词，
都不会让任何门变红。反向对照：`docs/modules/MODULE_MAP.yaml` 被 `check_module_map.py:14` 明确"本表不含 status 字段——状态一律现场算"，
说明"登记表不预写状态"这条规则在模块面是落地的，只有负责人面这档没落。
另：`conclusion_vocabulary.json:27` 的 `DELIVERED` 造词判据注释，正是仓库自己已确立的"反造词"判例 ⇒ 本档造词与自家判例打架。

**④ "发布结论"是否越界**

- 权威条文：`ASTROCS_DESIGN.md:824`「**最终发布决定只属项目负责人**；agent 至多声明 `READY_FOR_OWNER_REVIEW`」；`AGENTS.md` §6「不宣布发布」。
- 仓库自家对这个词的用法：`docs/research/COMPRESSION_CODEC_RESEARCH_PACK.md:48` 写「发布决定只属负责人（**本文不下发布结论**）」
  ⇒ 在本仓语料里"下发布结论"= 负责人专属动作。
- 而 `docs/owner/RELEASE_STATUS.md:42` 直接以 `## 1a. 发布结论（当前）` 为标题，`:145` 再写 `发布结论: NOT_READY_FOR_RELEASE`，
  并被 `docs/KNOWN_LIMITATIONS.md:7` 当成路由目标（"发布结论见 `docs/owner/RELEASE_STATUS.md`"）向外指。
- 实质内容是否越权：**没有**——结论是"未到 READY_FOR_OWNER_REVIEW"，是负向如实标注，不是宣布发布。
  ⇒ 这是**词格越界（占用了负责人专属动作的名位）+ 造词**，不是发布违规。

**⑤ 成稿未抓、但更硬的一条（我实测）**

`RELEASE_STATUS.md:79` 声明"证据指 BASE=`da3c4b4a`"，`:103-105` 声明本表 IMPLEMENTED/INSTALLED 级证据"全部来自 BASE=`da3c4b4a` 的命令日志"。

```bash
git merge-base --is-ancestor da3c4b4a HEAD   # 是祖先
git rev-list --count da3c4b4a..HEAD          # 817
```
⇒ §3/§6 里那些 `IMPLEMENTED`/`INSTALLED` 是按 §0 自定判据"当前提交内实际执行通过"写的，
但证据链落后当前提交 **817 个提交**，且文档自己也说"未在本提交复跑的面一律不写 IMPLEMENTED/INSTALLED"——两句话互相否掉。
这正面撞 `ASTROCS_DESIGN.md:802`「状态由检查与验收现场计算，**登记表不预写状态**」，
比"加判据"更直接可判红（虽然目前无门扫它，见③）。

### 与成稿差异

| 成稿主张 | 我的复算 |
|---|---|
| 给状态表加判据 | **成立**，逐字已并排（含"（或注册测试面）"扩口径、"终态"加判据、少 `READY_FOR_OWNER_REVIEW` 一行） |
| 另造"发布结论"用词 | **成立**，但定性应为"词格越界 + 造词"，实质内容负向如实，不构成发布违规 |
| 全仓百级跟踪件在使用**它自造的**状态词 | **不成立（改口径）**：百级（153 档/517 处）用的是 §12.5 上位词表；它自造的词只有 2 个、传播 1 档 + 3 档 |
| 存在闭环：上位与 README 把状态单源权威指给它 | **半成立、方向要纠**：`README.md:101` 明确把状态词唯一口径指回 §12.5 并说"本文档不复制"，`RELEASE_STATUS.md:11` 自己也说"唯一登记处 = §12.5"；真正指给它的是同层文档 `docs/owner/ARCHITECTURE_OVERVIEW.md:7`、`docs/owner/PIPELINE_OVERVIEW.md:9`、`docs/owner/SCIENCE_OVERVIEW.md:13`（三处"§0…唯一口径"）与 `memory.md:42`（"状态词阶梯权威 = RELEASE_STATUS.md §0"）——即**平行复制 + 记忆件反向指认**，不是上位授权 |
| 版本门自称与它合同一致 | 成立但是 §2 版本条款：`eng/ci/check_version.py:9`「与 `docs/owner/RELEASE_STATUS.md` §2 GOV-003」；与状态词表无关 |
| 测试拿它当"负责人口径"断言 | 成立但是 §5 资源门口径：`eng/ci/tests/test_ci001_failclosed.py:145,163,169,193,208`；同样不涉状态词 |
| （成稿未提）登记表预写状态 + 证据钉 817 个提交之前 | 我补为本案主判据 |

### 定级建议

- **P1**（一条，合并定性）：负责人面状态档把上位 §12.5 的取值表**复制并改写**（加判据、扩 `IMPLEMENTED` 合格集、漏上限状态一行），
  同时预写了钉在 817 个提交之前证据上的 `IMPLEMENTED`/`INSTALLED`，直接违反 `ASTROCS_DESIGN.md:802` 的"登记表不预写状态"；
  同层 3 份负责人文档 + `memory.md` 已把"唯一口径"指向这份改写过的表 ⇒ 单源已被下位档实质接管。
- **P2**：`发布结论`/`NOT_READY_FOR_RELEASE` 造词与词格越界（占用负责人名位），修法是改用 §12.5 词汇（"当前无面达到 `READY_FOR_OWNER_REVIEW`"）。
- **非缺陷（推翻）**：成稿的"百级自造词传染"。

### 缺什么证据

- "传染无门禁"我已按 3 条门的 `scan_roots`/判据函数逐行读实，但未运行门（本批复禁止跑 `eng/**`）⇒ 若要写"实测不会红"，
  需第③层在 CI 侧做一次注入实验（把 `docs/owner/RELEASE_STATUS.md` 的 `NOT_READY_FOR_RELEASE` 换成任意更荒谬的造词，看 CHK-TRUTHFUL-CONCLUSION 是否仍绿）。
- `memory.md` 是否属"活动规范面"（还是历史轮次记录）需按 `docs/standards/DOCUMENTATION_STANDARD.md` 归档口径确认；
  若它已被判为非规范历史件，则闭环项少一条腿。

---

## V2 负责人面两份总览的源码行锚漂约 9000 行

**判定：降级**（锚确实指到无关内容、真定义点在别处，已我自己定位 3 组；但**成稿给的根因不成立**：
行锚门的文档覆盖面本来就是 `docs/**/*.md`（含负责人面），真正的失效机制是锚正则不认负责人面的书写形态 + 符号绑定只登记 14 份文档；
实测漂量是 11,205 行不是"约 9000 行"；"扩到全 docs 会引入大量既有红"不成立——新增只有 24 条锚、0 条越界）

### 我的独立取证

**① 三个锚我自己定位（两侧 `路径:行` + 该行内容）**

| # | 文档锚（`路径:行`） | 锚指向的实际内容 | 真定义点 | 漂量 |
|---|---|---|---|---|
| A | `docs/owner/RELEASE_STATUS.md:91`、`ARCHITECTURE_OVERVIEW.md:66`、`PIPELINE_OVERVIEW.md:57/91/117`、`SCIENCE_OVERVIEW.md:81/92/102` 均写 `lib/infrastructure/scheduler/src/module_adapters.cpp`:4257/4282/4309（"P1 8 / P2 7 / P3 5 节点"） | `:4257` = `{"median_fwhm_x_px", median(fwhm_xs)},`；`:4282` = `// ── PSF-FAST-001: 拟合星数上限…`；`:4309` = `// （"保留实现但不工作"）; 未来若改口径…` | 同文件 `:15462` `const std::pair<ModuleDescriptor, P1NodeSpec> p1_nodes[] = {`、`:15487` p2_nodes、`:15514` p3_nodes | **统一 +11,205**（文件现 15,532 行） |
| B | `docs/owner/RELEASE_STATUS.md:92`、`ARCHITECTURE_OVERVIEW.md:67`、`PIPELINE_OVERVIEW.md:131` 写 `module_adapters.cpp`:3777-3793（"RT 唯一 executor"） | `:3777` 是单独一个 `}`；`:3793` 是"密集场假源"注释 | `shared_work_executor` 定义在同文件 `:292`（声明 `lib/infrastructure/scheduler/src/executor_runtime.h:36-38`，该文件仅 39 行） | −3,485 |
| C | `docs/owner/RELEASE_STATUS.md:115`、`PIPELINE_OVERVIEW.md:112` 写 `lib/algorithms/projection/module.yaml`:79-80 `entrypoint: MISSING` | `:79-80` 是 scientific_change 说明注释 | 同文件 `:92` `entrypoint: MISSING`（`:66` 有对应注记） | +13 |
| D（对照，正例） | `docs/owner/RELEASE_STATUS.md:94` 写 `eng/cmake/install_layout.cmake`:104-105 | `:104-105` = `foreach(tgt astrocs_catalog_gaia astrocs_p1_drizzle astrocs_p1_calibration` + 续行，`:109-110` 才是 `DESTINATION ${ASTROCS_INSTALL_MODULE_SUBDIR}` | 与"五科学模块入 modules/"主张**吻合** | 0 |
| E | `docs/owner/RELEASE_STATUS.md:120` 写根 `CMakeLists.txt`:17 ACR 默认 OFF | `:17` = `# ── 选项 (03/BLD-001 冻结) ──` | `option(ASTROCS_ENABLE_ACR …)` 在 `:18` | +1 |
| F | `docs/owner/PIPELINE_OVERVIEW.md:105`、`SCIENCE_OVERVIEW.md:101` 写 `p3_wcs.cpp`:36（"会话路径仍 TAN-only"） | `:36` = STD-F1 导出边界桥接注释头 | "未实现投影显式拒绝、不静默回落 TAN" 在 `:63`、`:67`、`:97-98` | +27 且落错语义块 |

**② 那条行锚门的实际覆盖面声明（读门实现与输入清单，未运行）**

- 门：`docs/algorithms/anchors/check_doc_line_anchors.py`，CI 注册 id `ALG-LINE-ANCHORS`（我按 `eng/ci/checks.json` 150 项里查到）。
- 输入清单：`docs/algorithms/anchors/anchor_contract.json` 的 `doc_globs` 实测值 = `["docs/**/*.md"]`（**单一 glob，无按目录白名单**）。
- 我自己用 Python `glob` 复算该模式：命中 **233 篇** md，**含 `docs/owner/` 全部 5 篇**。
  ⇒ 成稿"门的覆盖面只到某一层目录、负责人面不在内"作为**文档扫描面**的说法 **不成立**。
- 真正的两个失效机制（都是我自己正则复算出来的，不是读文档结论）：
  1. **书写形态不被解析**：`ANCHOR_RE`（门 `:56-62`）是 `_FILE` 紧接 `[:#]L?\d+`；负责人面一律写成 `` `路径`:4257 ``（反引号在冒号之前），
     正则匹配不上。我把 `_FILE` 与 `_ONE` 逐字搬进自己的工作件 `anchor_surface.py` 复算：
     门可见 **2,055 条锚 / 105 篇文档**；`docs/owner/` 在门可见锚里是 **0 条**；
     只把正则放宽一个可选 `` [`'"] ``，可见锚变 **2,079 条 / 109 篇**，新增 **24 条**，其中 **20 条正是负责人面那 5 篇的全部数字锚**。
  2. **符号级判据只登记在少数文档**：C4 `symbol_binding` 只吃 `anchor_contract.json:bindings`，实测 **41 条三元组、分布在 14 份文档**，
     全部落在 `docs/algorithms/**` 与 `docs/science/**`，**`docs/owner/**` 一条没有**。
     其余锚只受 C2（可解析）/C3（在界内）/C6（边界行非空）约束 ⇒ 漂 11,205 行仍落在 15,532 行的文件里、且指到的不是空行，三门全过。
- 结论：**根因不是"覆盖面少一层"，而是"负责人面的锚一条都没进门 + 进门也没有符号判据"**。

**③ 真因（我从 git 取到的，成稿没给）**

`:4257` 这串由 `327b6c30 feat(DOC/DOC-CONV-001)…（BASE=da3c4b4a）` 写入 `docs/owner/`。在那一提交上：

```bash
git ls-tree -r --name-only 327b6c30 | grep module_adapters   # lib/core/src/module_adapters.cpp（不存在 lib/infrastructure/scheduler/src/…）
git show 327b6c30:lib/core/src/module_adapters.cpp | wc -l    # 4327
git show 327b6c30:lib/core/src/module_adapters.cpp | sed -n '4257p;4282p;4309p'
#  15462→  const std::pair<ModuleDescriptor, P1NodeSpec> p1_nodes[] = {
#         const std::pair<ModuleDescriptor, P2NodeSpec> p2_nodes[] = {
#         const std::pair<ModuleDescriptor, P3NodeSpec> p3_nodes[] = {
```
⇒ 写的时候是**对的**，对的是旧路径 `lib/core/src/module_adapters.cpp`（4,327 行）；ARCH-001 把文件迁到
`lib/infrastructure/scheduler/src/` 并长成 15,532 行后，文档只改了路径没重导行号
（同提交里 `RELEASE_STATUS.md` 的 `lib/phase3_proj/module.yaml`:79-80 现在也被改写成 `lib/algorithms/projection/module.yaml`:79-80，
行号同样没重导 ⇒ 锚 C 同因）。所以这是**迁移未复核型漂移**，不是"从来没对过"，也不是纯"写手不核"。

**④ 把覆盖面扩到全 `docs/**` 会不会引入大量既有红 —— 我实测的估计**

- 文档扫描面**已经是**全 `docs/**/*.md`（233 篇），扩无可扩。
- 要扩的是"正则放宽一个反引号"：新增 **24 条锚**（其中 20 条在负责人面、4 条在别处）；
  我对这 24 条逐条判 C2/C3：**0 条路径不可解析、0 条行号越界** ⇒ 单按门现有判据，**一条红都不会新增**。
- 要真抓到 A/B/C/F 这类漂移，得给负责人面加 C4 符号绑定；量级 = 5 篇负责人文档里的 8 个去重锚点
  （`module_adapters.cpp` 4257/4282/4309/3777、`install_layout.cmake` 104、`p3_wcs.cpp` 36、`projection/module.yaml` 79、根 `CMakeLists.txt` 17），
  其中我实测已错的是 4 个（A 的 3 个 + B/C/F 各 1），正确 1 个（D）⇒ **修 4~5 条断言 + 加 8 条 binding** 即可，成本有界。
- 一个需要第③层核实的口径差：`ANCHOR_CONTRACT.md:28` 的 C7 声明行写"101 文档 / 1821 锚"，我按同一 `_FILE`/`_ONE` 复算得 105 篇 / 2,055 条。
  我把它记为**我的计数口径与门不同**（我按 `(doc,line,path,num)` 去重、且未合并 `_CONT` 续行），不当成第二起缺陷。

### 与成稿差异

| 成稿主张 | 我的复算 |
|---|---|
| 锚指到无关内容、真定义点在别处 | **成立**，3 组我自己定位（表 A/B/C，另 F 也错，D 正例说明不是"全错"） |
| 漂"约 9000 行" | **量级低估**：P1/P2/P3 三锚统一漂 **11,205** 行（RT executor 那条是反向 −3,485） |
| 根因 = 符号级行锚门覆盖面只到某一层目录、负责人面不在内 | **推翻根因**：`doc_globs=["docs/**/*.md"]`，负责人面 5 篇**在扫描面内**；真因是 (1) 反引号形态锚不被 `ANCHOR_RE` 解析（负责人面 20 条锚 0 条进门）、(2) C4 符号绑定 41 条全部登记在 `docs/algorithms`+`docs/science`，负责人面 0 条 ⇒ 门只判存在/界内/非空 |
| 扩到全 docs 会引入大量既有红 | **不成立**：放宽正则只新增 24 条锚、0 越界 0 不可解析；真红来自"加符号绑定"，量级 8 条 |
| （成稿未提）漂移型态与可修性 | 我补：迁移未复核型（`327b6c30` 写入时对旧路径成立），修法是重导行号 + 给负责人面加 C4 绑定 + 把锚形态统一为门可解析的 `path:line` |

### 定级建议

- **P1**：负责人面 4 篇文档共 20 条源码行锚全部落在行锚门的正则之外（结构性 fail-open），其中至少 4 条指向与主张无关的内容、
  最大漂移 11,205 行；负责人是这套文档的唯一读者，行锚是"证据可核"的唯一机器抓手 ⇒ 负责人看到的"文件锚 + 命令 + rc"证据面当前不可核。
- **P2**（门自身，独立立案）：`check_doc_line_anchors.py:56-62` 的 `ANCHOR_RE` 与仓库主流书写形态（反引号后置冒号）不兼容，
  使 C7"全量复测"名不副实；且 C4 只对 14 份文档生效——覆盖面声明（`doc_globs`）与判据强度（bindings）不是一回事，文档表述易被误读为"全量在管"。
- **非缺陷（推翻）**：成稿"负责人面不在门覆盖内"的字面表述。

### 缺什么证据

- 我没有运行门（本批纪律），"扩面不新增红"是我用自己复算的正则 + 界内检查得出的；
  第③层若要把这条当门改造依据，需在 CI 侧实跑一次 `ALG-LINE-ANCHORS` 前后对照 JSON。
- 其余 3 篇负责人文档以外的 24−20=4 条新增锚的具体位置与真伪，我只做了界内/可解析判定，未逐条判语义。
- A 组锚在 817 提交链上"最后一次仍成立"的提交未定位（我只证明了写入时成立、HEAD 不成立），不影响判级。

---

## V3 是否真存在"第二张索引地图"且该删（`docs/audit/doc_classification.csv`）

**判定：降级**（"争角色"不成立——唯一索引自己已把它判为历史快照并声明权威在本索引；
"零活引用者"在**机器与权威意义上成立**、在 **grep 字面意义上不成立**（前台的存疑方向对但语义读反了）；
"另一套分类词"成立且我给了不可映射的量化证据；处置应为**降级保留（移入索引 archived 区段 + 状态改 `ARCHIVED_NON_NORMATIVE`）**，不删不迁）

### 我的独立取证

**① 引用关系逐条查清（谁引用、什么性质）**

- 该 CSV 在跟踪集里的全部提及（`git -c core.quotePath=false grep -n "doc_classification" -- .`）：
  `docs/DOCUMENT_INDEX.yaml` 53 行、`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:605`、
  `artifacts/evidence/doc-hygiene/baseline.json:928`、`reports/v19r2/{file_audit_inventory,source_manifest}.csv` 各 1 行。
- **53 行的性质**：我逐行看了命中形态——**51 行是别的条目的 `downstream:` 字段**、1 行是它自己的 `- path:`、1 行是它自己的 `duty:`。
  索引抬头 `docs/DOCUMENT_INDEX.yaml:13` 对 `downstream` 的定义是"**机器扫描出的活动引用方**"，扫描口径（`:31-33`）
  只排除 `问题扫描/ reports/ artifacts/ run/ build/ 实验/ 工程控制/ logs/`——**没排除 `docs/` 下的 csv**，
  而该 CSV 是一列 `doc_path` 路径清单 ⇒ 扫描器把"CSV 的数据行里出现某文档路径"记成"该 CSV 引用了该文档"。
- 反过来看它**自己**的登记（`docs/DOCUMENT_INDEX.yaml:496-501`）：

  ```yaml
  - path: "docs/audit/doc_classification.csv"
    status: ACTIVE_INFORMATIVE
    duty: "docs/audit/doc_classification.csv"
    downstream: "docs/DOCUMENT_INDEX.yaml"
    notes: "V19R2 文档分类/清单历史快照；权威分类见本 DOCUMENT_INDEX.yaml"
  ```

  ⇒ 索引自己声明：它的下游只有索引本身，且**权威分类在索引**（`:502`/`:508` 对同目录另两份 csv 用同一句 notes）。
- **有没有程序读它**：`git grep -rn "doc_classification|audit/inventory.csv|risk_verification_T012" -- eng/` → **0 命中**（我工作件 `csv_surface.py` 内调用）。
- **DOC-INDEX 门会不会因它红**：`eng/tools/doccheck/check_doc_index.py:752-753` 与 `:534-539` 同一条规则
  `if p.startswith("docs/"): 只有 .md 入扫描面` ⇒ **`docs/audit/*.csv` 整体不在悬空引用扫描面内**；
  它 20 条死路径在 `eng/tools/doccheck/dangling_ledger.json`（50 条在册）里 **0 命中**（我逐 token grep 台账 JSON 验）
  ⇒ "它既不被门管、也不占台账预算"。所以"争角色"缺少任何机器抓手，它不构成活动竞争者。

**② 两侧分类词表是否互斥（量化）**

- 该 CSV：`doc_path,authority,status,notes` 四列；`authority ∈ {AUTHORITATIVE 71, HISTORICAL 15, CANDIDATE 9}`（95 行）；
  **`status` 列 95/95 恒为 `present`**、**`notes` 列 0 行非空** ⇒ 两列已废。
- 索引：`status ∈ {ACTIVE_NORMATIVE 178, ACTIVE_INFORMATIVE 63, GENERATED 24}`（+archived 区段），且抬头 `:8-9` 明文
  "由检查器现场校验；**不是** §12.5 交付状态阶梯"。
- 同路径交叉表（我算的，重叠 75 条路径）：

  | CSV.authority | 索引 status | 行数 |
  |---|---|---|
  | AUTHORITATIVE | ACTIVE_NORMATIVE | 50 |
  | AUTHORITATIVE | ACTIVE_INFORMATIVE | 21 |
  | CANDIDATE | ACTIVE_INFORMATIVE | 4 |

  ⇒ 不是"两套互斥词表"，是**旧词表把新词表的规范性/知会性区分压平**（71 条 AUTHORITATIVE 里 29% 其实只是知会性）⇒ 不可无损映射，只能当历史判定读。
- 覆盖率与存活率（我实测）：233 篇跟踪 `docs/**.md` 中它只列 75 篇（**32%**），且 **20/95 行（21%）的路径在 HEAD 已不在跟踪集**
  （`docs/API_REFERENCE.md`、`docs/ARCHITECTURE.md`、`docs/README-DOCS.md`、`docs/RELEASE_STATUS.md`、`docs/backlog/NEXT_STAGE.md` 5 条 CANDIDATE + `docs/history/README.md` 1 条 + `docs/history/v19/**` 14 条 = 20 条）。
  ⇒ 它**不具备**索引地图的必要条件（全覆盖 + 路径可达 + 判据可校验）。

**③ 处置与依据**

**保留但降级为快照**（具体：索引条目从 active 区段移到 `archived` 区段、`status` 改 `ARCHIVED_NON_NORMATIVE`，同目录另两份 csv 同办；`duty` 改成一句真职责）。依据三条，均为我实测：

1. **不能删**：删掉后索引里 **51 条 `downstream` 串仍指向不存在的文件**，而 `check_doc_index.py` 里 `downstream` 字段**没有任何判据读它**
   （我 grep 该门源码 `downstream` → **0 命中**，即该字段门不校验、不再生成也不会红）
   ⇒ 删除的后果是把"一个已声明作废的快照"变成"唯一索引里 51 处不可核指针 + 一条需手工同步的索引条目"，净负收益。
2. **不建议迁**：`reports/v19r2/source_manifest.csv:309` 已按路径钉了它的 SHA-256（`7cda2a3d41…`），
   移路径同样断掉那份 V19R2 清单的可核验性；而它现在所在的位置（`docs/audit/`）**不在任何扫描面内**，留着既不产生假红也不产生真红。
3. **必须降级**：它现在的 `status: ACTIVE_INFORMATIVE` 与同一条目自己的 `notes: …历史快照；权威分类见本 DOCUMENT_INDEX.yaml`
   在条目内自相矛盾（"活动知会性" vs "已作废快照"），而这条矛盾**门抓不到**（门不校验 `notes` 与 `status` 的关系）；
   索引抬头 `:26` 已写"本文件是唯一索引地图"——把这三份 csv 从 active 区段挪走，才是把这句话落到登记面上。

**顺带实测到的、属于本批（唯一索引地图）的两条**：

- `duty` 字段在 **19 个条目上等于路径本身**（我逐条比 `path==duty`，全表 265 条），即 19 份机器产物没有职责说明；
  这 19 条含 `docs/TRACEABILITY.csv`、三份 `docs/audit/*.csv`、四份 `docs/architecture/*_inventory/execution/*.csv` 等同族。
- 索引声明的 `downstream` 扫描口径（`:31-33`，含 `docs/` 全部跟踪文件）与门的实际悬空扫描面（`:534-539`，`docs/` 仅 `.md`）**不一致**
  ⇒ 同一个"扫描口径"两个口径，这正是本次前台与成稿各读出 half 的直接原因。

### 与成稿差异

| 成稿主张 | 我的复算 |
|---|---|
| 它与唯一索引争角色 | **不成立**：无职责列/无上游/无判据，覆盖 32%、21% 路径已死、`status` 与 `notes` 两列已废；索引 `:501` 已明文"权威分类见本 DOCUMENT_INDEX.yaml"，且 0 程序消费者、0 门扫描 ⇒ 没有争角色的能力，只有被误读的外表（`ACTIVE_INFORMATIVE` 一档） |
| 用另一套分类词 | **成立但定性要改**：不是互斥词表，是**降维旧词表**（AUTHORITATIVE 50/21 分裂在 normative/informative 两侧），不可映射 ⇒ 只可作历史判定 |
| 三份同目录 csv 零活引用者 | **一半成立**：零**消费者**为真（`eng/` 0 命中、门不扫）；零**提及**为假（索引内 53 行） |
| 建议删除 | **推翻**，改判"降级保留"（理由见③1：删除会在唯一索引里留 51 处不可核 downstream，而门不会因此变红） |
| （前台存疑）索引有数十行提到它 ⇒ 零引用不成立 | **前台的字面观察对、语义结论不对**：51 行是 `downstream` 字段把"CSV 的路径数据行"当成"引用"，属索引生成器口径与门的口径不一致的副产物，不是活引用 |

### 定级建议

- **P2**（合并一条立案，对象是索引不是 CSV）：`docs/DOCUMENT_INDEX.yaml` 对三份已作废快照仍给 `ACTIVE_INFORMATIVE`，
  且 19 条 `duty` 等于路径；`downstream` 声明口径与门实际扫描面不一致、且 `downstream` 无门校验 ⇒ 唯一索引的三处自洽缺陷。
- **非缺陷（推翻）**："第二张索引地图需要删除"。

### 缺什么证据

- `downstream` 字段是哪个生成器产出的我没定位到可执行入口（门只提供 `--dump`，不写回索引），
  所以"重新生成即会清掉 51 条"这一句我没有证据 ⇒ 处置建议按"不依赖再生成"来定，稳妥不受影响。
- `reports/v19r2/source_manifest.csv` 是否被任何门当作校验对象：我只查到它位于 `SKIP_DIRS` 的 `reports/` 下（不在门扫描面），
  未查是否有专门脚本比对 manifest 哈希。若第③层要按"迁到 reports/"处置，需先补这一条。

---

## V4 一条独立审稿结论三档并存无人裁决（SCI-B / I2 fail-closed 对拍）

**判定：确认（缺陷成立、该判据的 PASS 资格现在不成立）**，但**"四档并存"的定性要收窄**：
四处里有三处（审稿、单元 README、论文报告 §5.3/§5.4）事实判断其实**一致**（都是"规范转写自检、实现未落地"），
真正打架的只有**验收判据行被判 PASS** 这一处；且"下游是桩"我核到比审稿更硬的机器面证据。

### 我的独立取证

**① 四处原文并排（含被点名代码行现状）**

| 面 | `路径:行` | 原文要点（逐字取） |
|---|---|---|
| 审稿 | `实验/absolute-snr/results/REVIEW.md:146`＋`:150` | "**I2｜b6 的 fail-closed '对拍'是自指玩具，无被测对象**"；"实际是作者把自己的转写与自己的期望表比对，`H4_all_cases_match` **在任何数据下恒真**（只有改源码才可能失败）……不能支撑 README §3.6 与 H14『语义正确』的强度——应表述为『按规范转写的语义自检，实现侧尚未落地』" |
| 单元 README（按风格项闭环） | `实验/absolute-snr/README.md:309` | 修正动作 = "门名改 `H4_spec_transcription_cases_match`，新增 `H4_scope_note`……README §3.6 同步降级"；复核证据 = "`grep -rn "sparse_reconstruct|snr_path_effective|FZ-SNR" lib/` 无实现命中" ⇒ **动作全是改名/加注/改措辞，无一处逻辑变更**；另 `README.md:329` 保留项②"I2 的实现侧状态机仍未落地（属生产码 FIX 域）" |
| 判据表仍当 PASS 证据 | 判据正本 `ACCEPTANCE_SPEC.md:63`（SCI-B §2.2 第 7 行）= "污染/退化 SNR 输入时**链路 fail-closed 或显式记 `snr_path_effective`**；恒真门不出现"；被判 PASS 的两处 = `实验/absolute-snr/REPORT_paper.md:317`「**判定**：ACCEPTANCE_SPEC §2.2 第 7 行（非退化负例）**PASS（有范围限定）**」与 `REPORT_paper.md:483` 表 A-1 行 38「fail-closed 5/5 + 静默降级判红 ｜ `results/b6_gates_audit.json` ｜ **`fail_closed.all_match`**、`gates.H4_silent_fallback_detected`、`gates.H4_scope_note`」 |
| 论文报告诚实登记为缺陷 | `实验/absolute-snr/REPORT_paper.md:377`（§5.4-8）「**fail-closed 状态机未落地**：b6 的 5/5 用例是规范转写自检，`lib/` 无对应实现（grep 无命中）⇒ 不得据此外推实现行为」；另 `:79`、`:364`（C5 规范严于实现）、`:395`（列为唯一需继续推进的工程项）、`:365`（C6 建议把判据"恒真门不出现"改写成"不得把恒真门计入证据"） |
| 被点名的代码行现状 | `实验/absolute-snr/code/b6_gates_audit.py:100`（`def path_state_machine(...)`＝单元内自建转写）＋ `:119-131`（`cases` 里硬写的期望表）＋ **`:144` `all_match=bool(all(x["matches_expectation"] for x in rows))`** ＋ `:204` `H4_spec_transcription_cases_match=fc["all_match"]` ⇒ 比较两侧同源，**恒真性原样在位，只换了键名**；该文件相关变更止于 `9a2b5d11` |

**② 该判据的 PASS 资格现在成不成立 —— 不成立（三条独立依据，我自己取）**

1. 判据文字要求的是"**链路** fail-closed 或**显式记 `snr_path_effective`**"。实测：`git grep -rn "snr_path_effective" -- lib/` → **0 命中**
   （全仓命中只在 `ACCEPTANCE_SPEC.md`、`ASTROCS_DESIGN.md`、`docs/**` 合同/设计、`eng/ci/ledgers/*`、`eng/contracts/schemas/*` 与实验单元）；
   `sparse_reconstruct` 在 `lib/` 只有 `lib/infrastructure/cli/session_commands.h:226-232` 的 **CLI 配置模板字符串**（默认值文案），不是状态机。
2. **机器台账已在册**：`eng/ci/ledgers/dead_config_keys.json:34-42` 有条目 `dead_config_key:snr_path`，
   `exit_condition` 原文 = "Phase2 叠加现场按 snr_path 消费/重建 SNR（含无稀疏层时显式记录 snr_path_effective、损坏层 fail-closed）出现生产读取点后删除本条"
   ⇒ 仓库自己的机器判定就是"该键无生产读取点"；这条比审稿人的临时 grep 更硬（有登记、有出场条件、有门消费：`eng/ci/check_config_consumed.py`，注册 id `CHK-CONFIG-CONSUMED`）。
3. "恒真门不计证据"是仓库三条并行禁令，不需要新裁决：`ACCEPTANCE_SPEC.md:99`「每个度量有"真值无效应 ⇒ 归零/判红"的负例，**恒真门不计证据**」、
   `ASTROCS_DESIGN.md:740`「恒真门没有证据资格」、`AGENTS.md` §5 同文。
   ⇒ 表 A-1 行 38 把 `fail_closed.all_match` 列为"报告中的数字 → 证据字段"与上述三条直接冲突；
   `REPORT_paper.md:317` 的"范围限定"限的是**强度**（转写层），不是**资格**——按判据原文，该行两个达标条件一个都没满足，
   正确判定是**未达标/待实现**，不是"降格 PASS"。

**③ 影响哪条对外主张**

受影响的是创新点二（`ASTROCS_DESIGN.md` §2.2 跨帧绝对 SNR）在 SCI-B 实验单元验收面上的那条具体主张：
**"退化/污染 SNR 输入时不静默降级"**（`ACCEPTANCE_SPEC.md:63` + §3 L1 面）。现状是该主张只有实验单元内一段 Python 语义转写在自证，
生产侧连 `snr_path` 的读取点都没有（台账在册）。凡引 `README.md:41` H14「成立（范围限定）」或 `REPORT_paper.md:483` 行 38
作为该主张证据的对外面（含 L1 验收汇总、负责人面科学结论）都需落"未落地"限定。
（同一判法在负责人面已有正确对照：`docs/owner/RELEASE_STATUS.md:114` 把资源门执行证据如实判 `NOT_VERIFIED` ⇒ 词是够用的，不是没有档。）

**④ 谁该裁决、按什么现成裁决面（不新发明流程）**

拆两层，避免把作者侧执行误送上呈：

- **不需要负责人裁决**（按已有规则即可判，属作者侧执行）：恒真门不计证据的三条禁令已把表 A-1 行 38 的"证据字段"列法判死；
  未落地事实也有现成登记面 `eng/ci/ledgers/dead_config_keys.json`（带 `exit_condition`）+ `docs/KNOWN_LIMITATIONS.md`
  （L0 已知限制面，`:87-89` 已登记同族"恒真判据"条目）⇒ 修法是**改判定与列法**（表 A-1 行 38 去掉 `fail_closed.all_match`；
  §2.2 行 7 改判"未达标，待生产读取点"），走 `AGENTS.md` §8 变更流程留痕。
- **需要负责人裁决的两点**（都命中 `AGENTS.md` §10）：
  ① `ACCEPTANCE_SPEC.md:63` 判据文字要不要按 `REPORT_paper.md:365`（C6）建议改写（"恒真门不出现"→"不得把恒真门计入证据"）
     —— 顶层验收合同的文字变更（§10 第一条 + §6 硬禁令"不动 SCI/ALG 冻结定义"）；
  ② 该 SCI-B 验收行在完成前对外按 `NOT_IMPLEMENTED`（`ASTROCS_DESIGN.md:790` 阶梯）还是按"范围限定 PASS"表述 —— 涉及对外主张强度。
- **现成裁决面（同型问题已有先例，不需新建）**：`artifacts/evidence/audit-2026-01/OWNER_DECISIONS.md:39` 的 **A-27**
  正是"基线 4 个恒真探针算『在册已知失败』还是判失效"的上呈格式与裁决位 ⇒ 本条按 A-27 同型上呈，
  裁决规则引 `ACCEPTANCE_SPEC §1/§2.2` 正本 + `ASTROCS_DESIGN.md` §12.5 状态阶梯。

### 与成稿差异

| 成稿主张 | 我的复算 |
|---|---|
| 审稿判恒真且下游是桩 | **成立**，并把"桩"升级为机器在册事实：`dead_config_keys.json:34-42` 的 `dead_config_key:snr_path` ＋ `CHK-CONFIG-CONSUMED` 消费它；`snr_path_effective` 在 `lib/` 0 命中 |
| 单元 README 把它归为纯风格项 | **成立**：`README.md:309` 的修正动作与复核证据全是改名/grep/措辞，`b6_gates_audit.py:144` 恒真式原样在位 |
| 判据表仍把它当 PASS 证据 | **成立**，位置我给两处：`REPORT_paper.md:317` 判定行 + `:483` 表 A-1 行 38 的证据字段列法（成稿只给"判据表"一句） |
| 论文报告又诚实登记为缺陷 | **成立**（`:79`/`:364`/`:365`/`:377`/`:395` 五处） |
| 四种口径并存 | **收窄为一种冲突**：审稿、README `:309`+`:329`、REPORT §5.3-C5/§5.4-8 三者的事实判断一致（"转写自检、实现未落地"）；唯一冲突是**验收行被判 PASS** 与**表 A-1 把恒真量列为证据字段**（同一份报告内部 `:377` 说"不得外推"、`:483` 却把它当证据字段） |
| 无人裁决 | **半成立**：证据资格本身已有规则可判（三条禁令），不缺裁决；缺裁决的是"判据文字是否放宽"（C6）与"未落地项对外如何标状态"——这两条自 `REPORT_paper.md:365` 提出后确无裁决记录（我 grep 全仓未见 C6 的后续处置条目） |
| 被点名的代码行未改 | **成立**：`:144` 与 `:204`（仅键名/加注）+ `:100`/`:119-131` 同源比较未动 ⇒ 按"看被点名的行本身"口径，I2 只算**措辞闭环**，不算缺陷闭环 |

### 定级建议

- **P1**：SCI-B 验收面把恒真自检当"非退化负例 PASS"证据（`REPORT_paper.md:317` + 表 A-1 `:483` 行 38），
  而被点名的实现面机器在册为未落地（`dead_config_keys.json:34-42`）⇒ 创新点二"退化输入不静默降级"当前无有效证据。
  修法：撤该行 PASS 与证据字段、改判"未达标/待生产读取点"（作者侧走变更流程）+ 两条文字/状态表述按 A-27 同型上呈。
- **非缺陷（合并）**：成稿的"四档并存、无人裁决"整体框架 —— 实为"三处一致 + 一处验收判定越格"，按上面两处位置立案即可，勿拆成四条。

### 缺什么证据

- `CHK-CONFIG-CONSUMED` 现在是否绿（我只证明该门消费这份台账、台账有这条），未运行（本批禁跑 `eng/**`）
  ⇒ 若它已红，说明工程面已在追，本条应降 P2；需第③层在 CI 读数上核一次。
- 该状态机在 Phase2 生产路径是否已**规划冻结**：`docs/plugins/algorithms_phase2/13_integration.md`、
  `docs/design/PHASE2_DETAILED_DESIGN.md` 均出现 `snr_path_effective` 字样，我只做了命中定位，未逐篇确认是否已列为必做项
  ⇒ 影响"由哪个任务去落地"的归属，不影响资格判定。
- 单元外是否还有面把同一判定当 PASS 二次引用（`docs/traceability/`、`ACCEPTANCE_SPEC §8` 预览版发布门汇总）：我只在 SCI-B 单元目录内做了全量 grep。

---

## 进 P0/P1（只列"确认"）

**本批按"确认"口径进负责人清单的只有 1 条**（V4；且立案主体已按我复算重新定位，不是成稿原样）：

| 编号 | 定级 | 一句话（问题 ／ 谁修 ／ 怎么算修好） |
|---|---|---|
| **V4-R** | **P1** | SCI-B 实验单元的"非退化负例"验收行被判 **PASS（有范围限定）**、附录表 A-1 行 38 仍把恒真量 `fail_closed.all_match` 当证据字段，而被点名的实现面已由机器台账在册为未落地（`dead_config_key:snr_path`，`lib/` 内 `snr_path_effective` 0 命中）⇒ 创新点二"退化/污染 SNR 输入不静默降级"目前**没有有效证据**。谁修：SCI-B 单元作者侧撤 PASS 与证据列、改判"未达标/待生产读取点"（走 §8 变更流程）；两件需上呈的（判据文字是否放宽、未落地项对外状态词）按 `OWNER_DECISIONS.md` **A-27** 同型上呈。怎么算修好：验收行不再以恒真门为证据；`snr_path` 台账条目按 `exit_condition` 出清、或如实挂"未达标"并在负责人面同步限定。 |

**V1、V2 判定为"降级"，不进本轮负责人清单**，但我复算出的缺陷主体比成稿更硬，需前台**按下面两条重新立案**后再走确认程序（不得沿用成稿原条目报上去）：

- **V1-R（建议 P1）**：负责人面状态档把最高设计 §12.5 取值表**复制并改写**（`IMPLEMENTED` 加"（或注册测试面）"、`CONTRACT_READY` 加"终态"、少 `READY_FOR_OWNER_REVIEW` 一行、逐行加判据），
  并在 §3/§6 **预写**了钉在落后 HEAD **817 个提交**的旧 BASE（`da3c4b4a`）上的 `IMPLEMENTED`/`INSTALLED`；同层 3 份负责人文档与 `memory.md` 已把"唯一口径"指向这张改写表
  ⇒ 违反 `ASTROCS_DESIGN.md:802`「状态由检查与验收现场计算，登记表不预写状态」。**没有门会因此红**（三条状态词门的判据面是 `eng/`+`artifacts/evidence`+结构化 JSON 台账；`docs/**` 正文里唯一被扫的是 `docs/ci/` 上黑名单字面量 `DELIVERED`，阶梯成员资格不判 markdown）。
  修好判据：§0 表与 §12.5 逐字同源（或整节删，只留指针）；§3/§6 状态改为现场算并指向当前提交证据；同层三份只指 §12.5。
- **V2-R（建议 P1）**：负责人面 4 篇文档的 **20 条源码行锚 100% 落在行锚门的正则之外**（`` `路径`:4257 `` 形态不被 `ANCHOR_RE` 匹配），其中至少 4 条指向与主张无关的内容、最大漂移 **11,205 行**（真实点在 `module_adapters.cpp:15462/15487/15514`）
  ⇒ 负责人手上唯一的"文件锚＋命令＋rc"证据面当前不可核。门侧真因不是"覆盖面少一层"（`doc_globs=["docs/**/*.md"]`，233 篇含负责人面 5 篇），
  而是**锚形态不匹配 + C4 符号绑定 41 条全在 `docs/algorithms`/`docs/science`、负责人面 0 条**。修好判据：`ALG-LINE-ANCHORS` 放宽后对负责人面命中 0→20 且全绿（实测新增仅 24 条锚、0 越界 0 不可解析）。
- **P2（随批登记，不占负责人清单）**：V1 的"发布结论/`NOT_READY_FOR_RELEASE` 造词与词格越界"（另附：§5 的门禁链 `G-STD→…→G-RELEASE` 与 `02_GATES_AND_EXECUTION.md`/`03_AUDIT_PACKAGE_SPEC.md` 载体**全仓只在 `docs/owner/RELEASE_STATUS.md` 出现**，控制包内 0 命中）；V2 的门侧缺陷（覆盖面声明与判据强度不匹配）；V3 的唯一索引三处自洽缺陷。

**推翻清单（4 条，均不进清单）**：
① V1"百级跟踪件使用它自造的状态词"——自造词只有 2 个（1 档 4 行 + 3 档 5 处）；153 档/517 处用的是 §12.5 上位词；阶梯"表式样"扩散我实测 **15 篇 md**（非百级），且 `docs/ci/03_GATES.md:15-18` 那份拷贝保留的是**上位语义**（无"注册测试面"）⇒ 传染方向不成立。
② V2"符号级行锚门覆盖面只到某一层目录、负责人面不在内"——`doc_globs` 已是全 `docs/**/*.md`。
③ V3"该 CSV 与唯一索引争角色、应删除"——改判"降级保留"。
④ V4"四种口径并存、无人裁决"——实为"三处一致 + 一处验收判定越格"，且证据资格不缺规则、只缺两处上呈。

## 前台存疑的裁定（V3 必给结论：删／迁／降级保留）

**裁定：降级保留。** 文件留在 `docs/audit/` 原地不删不迁；`docs/DOCUMENT_INDEX.yaml` 里把它（连同 `inventory.csv`、`risk_verification_T012.csv`）
的条目从 active 区段移入 `archived` 区段、`status` 由 `ACTIVE_INFORMATIVE` 改 `ARCHIVED_NON_NORMATIVE`，并把 `duty` 由"等于路径本身"改成一句真职责。

对前台那条存疑（"唯一索引有数十行提到这个分类表 ⇒ 零活引用不成立"）的裁定：**观察成立、结论方向要纠**。

- 我逐行数过：`docs/DOCUMENT_INDEX.yaml` 内 53 行提及 = **51 行是别的条目的 `downstream` 字段** + 1 行它自己的 `- path:` + 1 行它自己的 `duty:`；
  而它**自己条目**的 `downstream` 只有 `docs/DOCUMENT_INDEX.yaml`（`:499`）。
- 那 51 行的成因是**索引生成器的扫描口径**（`:31-33`：git 跟踪文件，只排 `问题扫描/ reports/ artifacts/ run/ build/ 实验/ 工程控制/ logs/`，未排 `docs/` 下非 md）
  把一份"`doc_path` 路径清单"的**数据行**当成了引用；DOC-INDEX 门自己的悬空扫描面却**只收 `docs/**/*.md`**（`check_doc_index.py:534-539`、`:752-753`），
  且 `downstream` 字段该门**完全不校验**（同文件 grep `downstream` → 0 命中）。
- ⇒ 它是"索引口径不严谨造出来的自我回声"，不是活引用：`eng/` 内 0 消费者、0 门扫描、0 台账占用（20 条死路径在 `dangling_ledger.json` 50 条在册项里 0 命中）。
- **为什么不是删**：删除会把那 51 条 `downstream` 变成指向不存在文件的死串，而没有任何门会红 ⇒ 唯一索引内部反而多出 51 处不可核指针；
  **为什么不是迁**：`reports/v19r2/source_manifest.csv:309` 已按现路径钉了它的 SHA-256，移路径同样断锚，且它现位置本就不在任何扫描面内；
  **为什么必须降级**：它现在的 `ACTIVE_INFORMATIVE` 与同一条目 `notes` 里的"历史快照；权威分类见本 DOCUMENT_INDEX.yaml"条目内自相矛盾，
  而这条矛盾恰好落在门够不着的字段上（`notes` 与 `status` 的关系无判据）⇒ 只能靠改登记面自洽，这也正是索引抬头 `:26`「本文件是唯一索引地图」的落实动作。

<!-- PROGRESS: 4/4 · 收工 -->
