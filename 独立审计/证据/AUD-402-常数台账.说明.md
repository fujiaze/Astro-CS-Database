# AUD-402 经验参数与硬编码审计 · 说明与自证

成稿节点：独立只读审查节点；基线 HEAD=`c8f64e9a`；未跑任何构建/ctest/门/端到端（负责人裁决：本阶段只读推导）。

主表：`独立审计/证据/AUD-402-常数台账.csv`（12 列固定，UTF-8-BOM，可直接机器合并）
旁表：`独立审计/证据/AUD-402-常数台账.旁表-无名值与测试钉值.csv`（同 12 列；登记不判读）

## 1. 登记面定义（口径先固定，再数数）

| 面 | 内容 | 进主表/旁表 |
|---|---|---|
| A 配置默认值侧 | `eng/packaging/config/defaults.json` 59 字段、`runtime_resources.json` 全键、`config_registry.json` 的 120 个 plugin_knobs、`filters.json` 标量字段、`eng/packaging/config/templates/*.json` | 主表（逐字段） |
| B 合同侧 | `eng/contracts/**` 与 `lib/**/*.json` 的 default / minimum / maximum / exclusiveMinimum / enum 数值 | 主表（逐字段） |
| C CLI 侧 | `--template` 骨架值、`--help` 文案里的值域、退出码/旗标缺省 | 主表（逐键） |
| D 代码兜底侧 | `p1_num(cfg,"key",N)` / 结构体成员默认 / `constexpr`+`#define` 纯数值 | 主表（命名常数逐条） |
| E 科学链路常数族 | 单位换算因子、稳健统计因子、几何/球面常数、epsilon/下界、缓冲与截断窗、迭代上限 | 主表（逐族逐点） |
| F 测试与实验钉值 | `eng/tests/**`、`实验/**` 的期望值与容差 | 旁表（只登记不判对错） |
| G 无名行内字面量 | 生产代码里未命名的浮点字面量 | 旁表（可定位，逐行） |

「多侧默认」判据：同一 leaf 键在 D/A/C/B + 出厂模板中出现且值不等 ⇒ 立案，逐侧给 `路径:行` 与值。可达性一并登记（缺键即 REJECT 的一侧不可达 ⇒ 记为"结构性冗余"，与"可达 fail-open"区分）。

## 2. 确定命令（计数可复算）

```bash
# 扫描器（正负对照 + 空集护栏）
python "独立审计/工具脚本/AUD-402/scan_constants.py" --self-test   # 期望 SELFTEST PASS，exit 0
python "独立审计/工具脚本/AUD-402/scan_constants.py" --empty-check # 期望 exit 3（空宇宙必须非零退出）
python "独立审计/工具脚本/AUD-402/scan_constants.py"               # 写 raw/scan402/{universe,cpp_hits,named,json_hits,counts}.tsv
# 四侧默认
python "独立审计/工具脚本/AUD-402/four_side.py"        # 115 个有默认的键 × lib 命中
python "独立审计/工具脚本/AUD-402/tight_filter.py"     # 收紧到 621 个"键紧邻数值"的站点
python "独立审计/工具脚本/AUD-402/key_consumers.py"    # 59 个 defaults 键的生产消费点数
# 台账装配
python "独立审计/工具脚本/AUD-402/build_ledger.py"     # rows_b*.tsv + 机械行 -> CSV
python "独立审计/工具脚本/AUD-402/check_cols.py"       # 每行 12 列校验（不合格即拒绝装配）
```

<!-- PROGRESS: 已登记 66 判读行 / 预计 ≈1600 判读行（+1678 命名常数机械行 +18884 旁表行） -->
