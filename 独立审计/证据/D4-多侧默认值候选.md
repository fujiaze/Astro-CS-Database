# D4 多侧默认值候选（AUDIT-06 复算·第二路：先抽全表，再只判分叉行）

基线 HEAD `c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`；仓库 `F:\Astro dev\Astro CS Normalization Database`；
本路全程只读仓库、零 git 写、不构建、不跑 ctest/`run_checks.py`/任何 `eng/**` 脚本、不 import 仓库 Python 模块。

## 开工/收工 git status 原文

开工（任务开始时记）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工（本次渲染时实测，命令 `git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain`）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

两次一致：本路未在仓库内留下任何新增/改动条目（中途每次跑脚本后均复查过，未触发停用条件）。

## 抽取口径（第一步产物 = 全量表）

| 侧 | 载体 | 取法 |
|---|---|---|
| S1 defaults.json | `eng/packaging/config/defaults.json` | `fields[].value` ＋ `authority_status` |
| S2 出厂模板 | `eng/packaging/config/templates/*.json` | 现行 `blocks[]` 形态逐块展开叶子（**不**按旧顶层 `config` 取）；缺项显式标「该侧无此项」 |
| S3 CLI 生成骨架 | `lib/infrastructure/cli/session_commands.h` | 先剥注释、按 C++ 聚合初始化项解出 `ConfigField{key,json,doc,scope}`，再把 `json` 里的**字符串内嵌 JSON 解析成对象**逐层取键；`json==nullptr` 记为「不进模板骨架」 |
| S4 机器合同/schema | `eng/contracts/**.json`（含 `schemas/**`）＋ `lib/infrastructure/pipeline/orchestrator/configs/*.schema.json` | 同名属性的 `default`/`enum`/`minimum`/`exclusiveMinimum`/`maximum`/`const` |
| S5 代码兜底 | `lib/**`（跟踪集，排除 `third_party`/`tests`/`test_*`/`*_test.*`/`*.md`） | `value("k",d)`、`obj.k = <字面量>`、初始化列表 `k(<字面量>)`、`contains("k")?x:d`、具名常量与 `#define`；一个键的多处全部收（全位点见 `复算/prescan2/side5_位点.log`） |
| S6 登记册 | `eng/packaging/config/config_registry.json` | `plugin_knobs[].declared_default` ＋ `note` 里的默认值断言文本 |

规范键归一：取语义路径叶子；叶子属泛用词表（mode/name/path/value/type/center/…）时带上父段；
再做 `a.b ↔ a_b` 后缀等值并组（例：`snr.path ↔ snr_path`、`algorithm_drizzle_pixfrac ↔ pixfrac`）。
并组口径的副作用见文末「预筛漏检与误并口径」。

## pixfrac 正例控制（抽取口径的有效性门）

`drizzle.pixfrac` 行六侧实测：

| 侧 | 实测 |
|---|---|
| S1 | `0.8`（authority_status=owner_adjudicated） |
| S2 | `0.8`（normalize 模板两块皆 0.8） |
| S3 | `1.0`（`config_fields(kNormalize).drizzle` 内嵌串 `{"nested":1,"pixfrac":1.0,"precision_mode":1}`） |
| S4 | `(0,1]`：`phase_config_normalize#/drizzle_config/pixfrac`、`#/algorithm_drizzle_pixfrac`、`product_family_field_constraints#/signal/normalization/pixfrac` 三节点均只给 `exclusiveMinimum=0;maximum=1`（**无 default**）；orchestrator `stage1.schema.json#/drizzle/pixfrac` 给 `default=0.8` |
| S5 | `1.0`×11、`0.0`×3、`0.8`×2（0.8 两处＝`json_config.h:67` 成员初值与 `json_config.cpp:712-713` 的 `contains(...)?...:0.8`） |
| S6 | `declared_default="1.0"` ＋ note 断言「默认 1.0 已入 defaults.json」 |

控制判定：**PASS** —— 六侧全部取到值，且同时含 0.8 与 1.0 两组。
抽取脚本 `extract_defaults.py` 内建该校验并以退出码表达（FAIL ⇒ rc=2，禁止以「零分叉」交表）。

## 总量与分叉率

| 指标 | 数值 |
|---|---|
| 全量表数据行（规范键组） | 503 |
| 抽取记录总数 | 1380 |
| 六侧齐全键数 | 1（占 0.2%） |
| 机械筛出分叉行 | 43（分叉率 8.5%） |
| 本件已判分叉行 | 43 |
| 判读中标为「新暴露」的行数（口径见下节：按判读文本，含既判键的新面） | 32 |

各侧覆盖：S1 59 条/49 键｜S2 40/23｜S3 50/41｜S4 909/351｜S5 184/180｜S6 138/123。
**六侧齐全只有 1 个键（就是 pixfrac）** ⇒ 本项目里「同一键在多侧各说各话」不是少数派异常，
而是**默认形态**：绝大多数键只在一到两侧有值，其余侧根本没登记点。这条比率本身就是 D4 的结论性读数。

## 判读用的门覆盖事实（只读 `eng/**` 源码得出，未执行任何门）

- `CHK-CONFIG-DEFAULTS`（`eng/ci/check_config_defaults.py`；profiles fast/linux-main/windows-main；
  豁免台账 `eng/ci/ledgers/config_default_divergences.json`）是唯一声称管「同一配置键的生产默认值必须一致」的门。实测其口径：
  1. 取值形态只认 `\.value/.get/.get_or/.at("k",<字面量>)` 与 **接收者名为** `*cfg|config|settings|opts|params|options|defaults` 的 `recv.k = <字面量>`
     ⇒ `hips_properties.h:14 tile_width = 0`、`v6_drizzle_science.h` 一类成员初值不在形态内；
  2. `config_key_set()` 与 `template_defaults()` 都按 `doc.get("config") or {}` 取模板叶子，
     而现行模板是 `blocks[]` ⇒ **模板侧零键**，门自订的「代码 vs 模板」比较整条空转；
     其 `--self-test` 夹具也写 `{"config": {...}}`，故夹具仍全绿（fail-open 不会被自身发现）；
  3. `defaults.json` 只贡献**键名**不贡献 `value`，CLI 骨架 `config_fields[]` 是聚合初始化（非上述两种形态），
     登记册 `declared_default` 与 schema `default` 均不入比较
     ⇒ 「权威默认 ↔ 代码兜底」「权威默认 ↔ CLI 骨架」「登记册断言 ↔ 权威默认」三条边**无门**。
- `CHK-CONFIG-CONSUMED`（已正确支持 `blocks[]`）与 `CHK-PROD-WIRING` 只管「键有无生产读取点」，**不比值**。
- `CHK-UNIT › UT-CONFIG › CFG002-01/02/03`（仅 `linux-main` 档）比对
  `docs/plugins/**` 表 ↔ 登记册 `declared_default` ↔ `defaults.json` value；
  `eng/tests/test_index.csv` 对该套件记 `verdict=FAIL`（CFG002-02 登记点 2 条，测于 2026-09-23）。
  本路不跑 eng 脚本，现状红灯与否待前台复跑确认；此处按台账引用，不当作已复算证据。
- `CHK-REGISTRY-DOC-SYNC` 是 `checks.json ↔ docs/ci/01_CHECKS.md` 的门，与默认值无关（避免混用门 ID）。
- 键名跨面对齐（`noise.clip_sigma` ↔ 合同 `clip_sigma` ↔ 代码 `cfg.clip_sigma`、
  `snr.path` ↔ `snr_path`）：**无任何门**。

## 预筛漏检与误并口径（正则吃不到的形态）

本路的机械面有已知边界，交表时一并登记，不以「零分叉」冒充「无分叉」：

1. **跨行初始化**：结构体成员默认写在多行初始化列表（`P2SkyPatchConfig c{\n  3.0,\n ...}`）或
   `constexpr X y = \n z;` 时，逐行正则只吃到首行 ⇒ 位置参数式初值全部漏。
2. **宏拼接**：`#define PF_MIN 0.0` / `ASTROCS_DEFINE_LIMIT(name, v)` 一类经预处理才成形的字面量，
   以及 `X(name, value)` 形态的表驱动注册，本路只认 `#define NAME 字面量` 直写形。
3. **运行时计算**：`kcorr_lookup(pixfrac, scale)` 的插值、`1.4826*MAD`、`nside = 1 << tile_order`
   这类「默认值来自函数而非字面量」的取值，机械面只能记到函数名，不能记到数。
4. **形参默认与返回默认**：`double f(double pixfrac = 1.0)`、`Config cfg() { return {}; }`
   以及 `.at("k")` 无兜底 + 外层 try 的隐式默认，均不在两种被认形态内。
5. **跨键名同语义家族**：精度家族 `precision.default("fp64")` / `drizzle.precision_mode(1)` /
   `bitpix(-32)` 三条边按-key 机械口径永远连不上（A1 已手工判出，本路机械面确认其**不可被按键预筛发现**）。
6. **值域断言在散文里**：`docs/contracts/DATA_SEMANTICS.md`、`docs/plugins/**` 表内的「>=1」「默认 64」
   不参与 S4（本路口径 S4 只取 JSON 合同），故「登记即错」只覆盖 JSON 面，散文面靠判读。
7. **误并**（并组口径的反面）：`ra_deg` 因后缀规则吃进 orchestrator 的 `initial_ra_deg`(-999)、
   `block/center/source/median/read/write/checksum/snr` 等泛用叶子吃进同名局部变量与结构体成员。
   这类行在判读里逐条标「同名异义 ⇒ 伪分叉」，**不删行**（主表保持全量、可复算）。
8. **误报形态（本路口径自带，判读时逐行标注）**：`value("k",d)` 形按「带引号键＋逗号」匹配，
   会吃进 `std::set<std::string> k = {"schema_version", "inputs", ...}` 与 `kChain[] = {"cal", ...}`
   这类**键名集合字面量**（实测 parser.cpp:354/578、module_adapters.cpp:1872 三例），
   把邻居键名当成该键的默认值；同时 S3 的内嵌 JSON 串会被 S5 再吃一次（session_commands.h 自身位点），
   属同一事实的自身重复。两类都不删（保持可复算），判读列里逐条点名。
9. **S4 口径边界**：只认 JSON 合同里的 `default/enum/minimum/exclusiveMinimum/maximum/const`，
   因此「默认只写在 `description` 散文里」的键（实测 `export_wcs.rotation_deg`、`wcs_config.init_source`、
   `snr_path` 之外的多数 export 几何键）在 S4 记为「该侧无此项」——这是**合同面缺机读默认**，
   不是该侧与别侧冲突；判读时按「登记面只有散文」单独定性，不计入分叉率的事实依据。


## 与既有四路判读（AUD-402-判读-A1/A2/A3/BD1）的交叉

按规范键名与 `符号/键` 列取交集（命令见 `复算/prescan2/cross_ref.py`）：

- **同键已判 12 行**：`clip_sigma`、`k_corr`、`max_iterations`、`pixfrac`、`tile_width`、
  `saturation_level`、`lower_sigma`、`upper_sigma`、`spatial_field_enabled`（A1）、
  `scale_deg_per_px`、`snr`（BD1）、`block`（A2 判的是别处的 block 常数，本件判 cpu_profile 面 ⇒ 键名同名而对象不同，计入已判以保 12＋31=43 的算术闭合）。
  其中 A1 已在备注里就 `drizzle.pixfrac`（六侧）、`noise.saturation_level`（default 落在自身
  exclusiveMinimum 外）、`noise.spatial_field_enabled`（defaults 整数 1 vs schema boolean）、
  `hips.tile_width`（多侧硬编码同值＋0 的第三种表示法）、`noise.clip_sigma`（Phase2 3.0 无注册侧）、
  `precision.default`（fp64 vs bitpix=-32 vs precision_mode vs 死键四面）
  **立过多侧默认值分叉案** ⇒ 本件对这些键只做补全（记机械读数与门缺口径），不重复立案。
- **新暴露 31 行**：`dark_exposure_s`、`light_exposure_s`、`dec_deg`、`ra_deg`、`nside`、
  `precision_mode`、`storage_form`、`flux_conservation_factor`、`target_pixel_area`、`w_info`、
  `reference_flux`、`median`、`read`、`write`、`workers`、`checksum`、`center`、
  `crpix_px`、`rotation_deg`、`gaia_data_dir`、`height_px`、`width_px`、`hips_paths`、`output_dir`、
  `log_dir`、`max_iter`、`max_stars`、`parity`、`schema_version`、`source`、`wcs`。
  这一批的来源面是 **orchestrator stage1 配置面 / p2-p3 会话读键点 / CLI 键表面**，
  A1 从 `defaults.json`  outward 判，覆盖面天然到不了这里 —— 这正是本路「先抽全表」换来的增量。
- 四路里带「多侧/六侧/四侧/两侧并列」字样的判读条目：A1 16 条、A2/A3/BD1 另有若干；
  计数命令与输出见 `cross_ref.py` 末段（复算可跑）。

## 排序规则与未做行数

分叉行按任务书优先级排序：①生产兜底在对面 ＞ ②值域自相矛盾 ＞ ③哨兵不一致 ＞
④登记面与文档面分歧 ＞ ⑤仅跨侧异值（含同名异义嫌疑）。

本件判读覆盖：已判 43 行 / 共 43 行。各优先档行数（机械筛出口径）：
- ① 5 行
- ② 1 行
- ③ 15 行
- ⑤ 22 行
其中按判读结论再分一刀：
- 构成新案或补强新事实的行数：24
- 判为「同名异义/机械误并 ⇒ 无案」的行数：10（不删行，留作机械口径的可复算证据）
- 与 A1/BD1 既立条目同案、仅补机械读数的行数：9
未做行数：**0**。


---

## T5 专项：「该导出却硬编码」（与多侧分叉正交，另计）

机械候选生成方式：对 `defaults.json` 的全部数值默认做两两 `×`／`+` 匹配第三值（容差 1e-9），
得 **30 个候选键**。逐条判读后：

| 结论 | 键 | 依据 |
|---|---|---|
| **真案（仓内已有导出式，值仍按字面量硬编码）** | `sparse_snr.spacing_px`=64 | 该键自己的 constraint 文本写着「默认 64 = hips.tile_width / 8 = 512 / 8（复用 Phase2 UPM 的 8×8/tile 控制网格）」⇒ 导出式的两个输入（`hips.tile_width`、除数 8）都在仓内，而 64 以字面量同时落在 defaults.json 与 `phase_config_normalize#/sparse_snr_spacing_px` 的 schema default 上（两处各写一遍，任一处改 512 都不会传导） |
| **真案（下界即导出值）** | `cosmetic.bad_column_variance_kappa`=2.0 | authority_status=**derived**；constraint 写 `>= 1/Σw²（单列两邻平均 ⇒ >= 2）` ⇒ 默认值 2.0 恰等于自身下界，是结构导出量而非可选项；登记面同时称「取保守侧」——两说并存 |
| **真案（A1 已立）** | `hips.tile_width`=512 | `leaf_order = tile_order + 9` 的 9 即 log2(512)，A1 已判「应按式算」；本路不重复立案，只记它同时是 spacing_px 的上游 |
| **正面对照（有式且写了式）** | `noise.mask_budget_min_sky`=9216 | note 给完整 a priori 链：SE(σ̂)/σ ≈ 1.44/√N_sky ≤ 1.5% ⇒ N ≥ (1.44/0.015)² = 96² = 9216。这就是「导出常数应怎么写」的样板 |
| **数值巧合（无导出处，判无案）** | 其余 26 键 | 例：`pixfrac` 被判为 0.75+0.05、`moffat_beta` 被判为 5×0.8、`esd.alpha` 被判为 50×0.001 —— 全部是把两个语义无关的量凑成同值。**判据在纯算术面上不可判**，必须回到导出式文本 |

**为什么 T5 不在 43 行分叉表里**：`sparse_snr.spacing_px` 两侧同值（defaults 64 ＝ schema default 64），
模板面自 §9.68 起不再列该可选键 ⇒ 多侧比较面无不等值，机械筛选不吃它。
即：**「该导出却硬编码」是"值哪来的"问题，不是"各侧值不等"问题**，两判据正交，必须像本专节这样单列。

**本路机械口径在此判据上的实证漏检**：候选生成只做 `×` 与 `+`，真式是 **除法**（512/8）——
漏检不是"正则吃不到跨行/宏"那一类，而是**关系算子集不全**。补 `÷`、`log2`、`√`、`²` 之后才可机械检。

---

## 判读表

优先档,键,键族,命中判据,是否已有门覆盖（门 ID 或「无」）,定级建议,保守方向与影响面,与既有四路判读的交叉,六侧实测摘要
①生产兜底在对面,clip_sigma,noise（Phase1 注册面）／coverage·sky_plane（Phase2 实现面）,T1a 生产兜底在权威对面 ＋ 同名跨阶段异值,无（跨 defaults↔代码值比较无门：CHK-CONFIG-DEFAULTS 不读 defaults.json 的 value；CFG002-02 只在登记册有行时比，Phase2 侧无登记行 ⇒ 天然吃不到）,S2（科学参数；且键名碰撞会让按-key 的一致性判定系统性失真）,保守方向＝两套裁剪各自显式命名并各自有唯一数值源：把 Phase2 的 3.0 从 sky_plane.cpp 字面量提升为独立键（如 phase2.background_clip_sigma）入 defaults.json，或反向确认 5.0 一侧为死登记。注意两侧不是同一物理量（Phase1=cosmic/hot 裁剪 k·sigma；Phase2=亮端 MAD 单侧裁剪），任何「统一到 5.0」的粗动作都是错的。影响面＝Phase1 patch 稳健裁剪、Phase2 sky-patch 背景估计与 contamination 判定；另 noise.clip_sigma 已出现在 eng/ci/prod_wiring_baseline.json 的 dead_config_key:noise.clip_sigma，即注册默认是否真进运行路径本身存疑。,已判：A1 行 noise.clip_sigma 备注已确证「同名不同值：Phase1 5.0 进配置键面、Phase2 sky_plane.cpp:230/:251 的 3.0 无任何注册侧（隐藏默认）」。本行不重复立案，只补机械读数与门缺口径。,S1 defaults.json noise.clip_sigma=5.0（sourced，NOISE_MODEL.md 锚）｜S2 模板无此项｜S3 CLI 骨架无此项｜S4 phase_config_normalize#/noise_config/clip_sigma default=5.0;exclusiveMinimum=0;maximum=100｜S5 sky_plane.cpp:230 c.clip_sigma=3.0、:251 cfg.clip_sigma=3.0（缺省兜底），另 noise_model 侧字段名是 cosmic_clip_sigma｜S6 登记册无 Phase2 行
①生产兜底在对面,k_corr,upm／coverage·sky_plane·phase2_integrate,T1b（权威外值 0.0）——实为伪分叉：0.0 是 provenance 输出清零而非默认,无（k_corr 不在 templates/phase_config schema 的键集内 ⇒ 不入 CHK-CONFIG-DEFAULTS；登记册有 defaults_json 行 ⇒ CFG002-02 覆盖 declared_default↔defaults，但不覆盖 defaults↔代码常量）,S3（数值未分叉；缺唯一数值源＋标定不可复算）,保守方向＝保留 1.4（PHASE2_UPM.md 冻结、改动属 §10 不可接受变化，不得动值），把 sky_plane.cpp:233 与 :254 两处独立字面量与 phase2_integrate.cpp:1331 的 kKCorrFrozen 收敛到同一生成常数（同 runtime_resources_generated.h 机制），并补 defaults↔常量的比较面。影响面＝UPM 背景方差放大系数（方差/ivar/SNR 传递链上游）；另 note 自陈 MC 实证 1.3883 与保守 1.4 并存、control_median_mc_test 未注册不可复跑 ⇒ 标定复算属另一条线。,已判：A1 行 upm.k_corr 判「实验标定（证据链断裂，须重跑并落 实验/ 或 artifacts/）」。本行补的是「defaults 之外还有三处独立字面量」这一机械读数。,S1 upm.k_corr=1.4（sourced；PHASE2_UPM.md:24 冻结）｜S2 无此项｜S3 无此项｜S4 无该节点（合同面零登记）｜S5 1.4×2（sky_plane.cpp:233/:254）＋ kKCorrFrozen（phase2_integrate.cpp:1331）＋ 0.0×1（v6_drizzle_science.h:376 输出清零，非默认）｜S6 有行（defaults_json 登记）
①生产兜底在对面,max_iterations,rejection（8）／photometry IRLS（50，键名 irls_max_iterations）／psf·upm·sky_plane（100/30/1）,T1b —— 主因是同名四族被我按叶子并组；真实分叉在同名跨族与 sky_plane 内部两处不同默认,"名义有、实际无：max_iterations 在 defaults.json 叶子里 ⇒ 进 CHK-CONFIG-DEFAULTS 键集，但该门只认 value(""k"",d) 与 cfg 类接收者赋值，rejection.cpp:1246-1256 的 p.sigma.max_iterations=8 一类 p.xxx 接收者、sky_plane.cpp:457/:517 的 c./cfg. 接收者都不在其形态内 ⇒ 门零命中；无门比较 defaults(8/50)↔代码(100/30/1)",S2（收敛判据不唯一 ⇒ 影响可复现性与回归基线；psf 拟合上限 100 在 JSON 合同与 defaults 两侧皆零登记）,保守方向＝每一族各自钉一个数值源并在回执里输出「实际迭代数＋收敛标志」；不得跨族统一（8 是排异、50 是测光 IRLS、100 是 PSF 拟合，语义不同）。sky_plane.cpp:457 的 30 与 :517 的 1 是同一文件不同函数的两处缺省，正属该门立意要抓的「跨权威单元双口径」，因形态漏配而逃逸——这是门缺陷而非代码缺陷，优先修门形态（放开接收者名单）再复算。影响面＝排异/测光/PSF/sky-patch 四处收敛语义与所有以它们为上游的容差判定。,部分已判：A1 行 rejection.sigma.max_iterations 只判出处等级（文献值·本仓冻结档），未做跨族默认值一致性；psf 侧 100 与 sky_plane 侧 1/30 为**新暴露**。,"S1 rejection.{sigma,winsorized,averaged_sigma,linear_fit}.max_iterations=8、photometry.irls_max_iterations=50｜S2 无此项｜S3 无此项｜S4 phase_config_normalize 的 rejection 段未声明 default（本路机械面零 default）｜S5 stage2_common.cpp:375/382/389/396/415 value(...,8)、rejection.cpp:1246-1256 p.*.k=8、upm.cpp:270/:287/:2462=100、json_config.h:50=100、phase2_integrate.cpp:1327=100、module_adapters.cpp:9561=100、p2_session.cpp:203=100、sky_plane.cpp:457=30、:517=1、ipv_robust_refine.h:48=10｜S6 无 psf 行"
①生产兜底在对面,pixfrac（含 algorithm_drizzle_pixfrac 并组）,drizzle／orchestrator.stage1／coverage.kcorr 查表,T1b 生产兜底含权威外值 ＋ T3 哨兵（0 在两处含义不同） ＋ T4 登记即错,部分：CFG002-02（CHK-UNIT›UT-CONFIG，仅 linux-main）覆盖 S6↔S1；CHK-CONFIG-DEFAULTS 覆盖 S5 内部跨单元（11 处 1.0 vs 2 处 0.8 应判红，除已豁免）。**S3 CLI 骨架 1.0 与 S1 的比较：无门**（聚合初始化不在形态内）。台账实测该键未登记差异（config_default_divergences.json 仅 4 条，全为 smoothing_lambda 族）。,S1（负责人已裁 0.8，未贯穿：CLI 骨架、11 处生产兜底、登记册断言三面仍在 1.0；本路确认这是全表唯一六侧齐值的键，即最重母题）,"保守方向＝数值一律向裁决侧 0.8 收敛，并同步订正 S6 的 declared_default 与 note 断言、S3 的骨架内嵌串；S5 的 0.0 三处是输出/入参清零形，不参与收敛。0 的哨兵两义要显式化：合同 (0,1] 禁 0，而 AIO/writer 侧以 0 表「未提供 provenance」（aio_hips_writer.cpp:895、sampler.cpp:122）。影响面＝drop 面积 A_drop=pixfrac²·A_pixel、provenance.pixfrac、kcorr 查表列选择（sampler.cpp:103 按 {0.5,0.8,1.0} 非均匀网格插值，取 1.0 会走错列）。","已判：A1 行 drizzle.pixfrac 标「六侧不一致（P0 母题，本队列唯一集齐全侧的键）」并点出 CLI 骨架违反其自订纪律。本行不重复立案，补三点机械增量：①S5 实测 1.0 出现 11 处（A1 只列 module_entry.cpp:355 一处）；②S4 三节点其实**无 default**，只有 (0,1] 值域，唯一 default=0.8 在 orchestrator stage1.schema.json；③S6 的 note 断言文本本身仍写「默认 1.0 已入 defaults.json」。","见文首「pixfrac 正例控制」表：S1=0.8｜S2=0.8｜S3=1.0｜S4=(0,1] 无 default（stage1.schema 例外 default=0.8）｜S5=1.0×11 / 0.0×3 / 0.8×2｜S6=""1.0""＋note 断言 1.0"
①生产兜底在对面,tile_width,hips／coverage·aio·resample·plan_estimator,T1b（权威外值 0）,无（hips_properties.h:14 的接收者是结构体声明而非 cfg 类名，CHK-CONFIG-DEFAULTS 的 _CONFIG_RECEIVER_RE 不吃；defaults↔代码常量亦无比较面）,S3（同值面占优；0 的第三种表示法已被 A1 记过，本行补机械计数）,保守方向＝按 A1 的改判执行：tile 宽是格式约束而非可默认项（实现只接受 512，hips_properties.h:120-121 非 512 直接 fail），应从 defaults.json 摘掉「default」语义；若保留则 0 必须显式回退 512 并在 properties 留痕，不得静默按 0 参与 leaf_order 推导。影响面＝HiPS tile 几何与 leaf_order = tile_order + log2(W) 的 +9 硬编码。,已判：A1 行 hips.tile_width 备注列「多侧硬编码同值 512 … 另 :97 tile_width_px=0 表示默认 512 ⇒ 第三种表示法」并改判为格式常量。本行只补 S5 计数（512 出现 6 处、0 出现 1 处）。,S1 hips.tile_width=512（sourced）｜S2 无此项｜S3 无此项｜S4 hips_storage_form.schema.json 把 hips_tile_width 列必需键（非 default 形）｜S5 512×6（kTileWidth 常量 sampler.cpp:76/p3_resample.cpp:48/calibrated_pair_diag.cpp:40 ＋ aio_hips_reader.cpp:154/aio_hips_writer.cpp:914/v6_hips_manifest.h:29）＋ 0×1（coverage/hips_properties.h:14）｜S6 无 defaults_json 行
②值域自相矛盾,saturation_level,noise／snr 块,T2 值域自相矛盾（同一 schema 节点 default=0 与 exclusiveMinimum=0 并存） ＋ T3 哨兵两义,无门覆盖该形态。CHK-CONFIG-DEFAULTS 不读 schema default 也不读值域；CFG002-03 只判「defaults 的 enum_token 落进目标 enum」，不判数值 vs exclusiveMinimum。CON-CONFIG-CONTRACTS 与合同结构门不判数值落域。,S2（本路机械面唯一一条 T2 命中；合同自身不可满足——按合同填合同给的默认必被判红）,保守方向＝在合同面把 unset 与合法值分开表达：把 saturation_level 的 unset 由「值为 0」改为「键不出现／显式 null」，并删掉 default=0（保留 exclusiveMinimum=0）；若坚持保留 default，则必须同时给出 0 的合法性（minimum=0 且另立 has_saturation 布尔）。两侧行为不等价已经实测：写 0 ⇒ module_adapters.cpp:6528-6531 判 rc=3 拒绝，不写 ⇒ 走 DISABLED_NO_METADATA 降级声明。影响面＝Phase1 blank-sky 统计与逐像素权重场的饱和剔除是否生效（漏剔除＝污染进背景）。,已判：A1 行 noise.saturation_level 备注明写「本行立案（值域自相矛盾）：同一 schema 对象同时声明 default=0 与 exclusiveMinimum=0」。本行为**同一案的机械确认**（本路全表唯一 T2 自动命中，说明该形态在机械口径下可检，但现无门检）。,"S1 noise.saturation_level=0（authority_status=sourced；note：0/负/非有限 = 未提供，不等于无饱和）｜S2 无此项｜S3 无此项｜S4 phase_config_normalize#/noise_config/saturation_level：default=0 **且** exclusiveMinimum=0 ｜S5 module_adapters.cpp:6384 double saturation_level=0.0、:6648 snr_cfg.value(""saturation_level"",0.0)（0 = unset，随后要求 >0 才启用）｜S6 无行"
③哨兵不一致,dark_exposure_s,orchestrator.stage1.calibration,T3：0 既是该字段声明的 unset 哨兵，又是兄弟字段 light_exposure_s 的非法值,无（orchestrator stage1 配置面整体不在 CHK-CONFIG-DEFAULTS 的键集来源里：该门只看 templates/defaults/phase_config_*schema，stage1.schema.json 不在其 glob 内）,S3（注释自述语义，未构成行为分叉；登记为哨兵词表缺失）,保守方向＝给 stage1 配置面立一张「未设」表示法词表（0 / -999 / 键不出现三选一并写入 schema description），同族的 light_exposure_s(>0) 与 dark_exposure_s(0=null) 用同一表示法。影响面＝暗场/亮场曝光比 K=t_light/t_dark 与 calibration.mode=exposure_ratio 回退路径（dark=0 时比值不可算）。,新暴露（A1 从 defaults.json outward，orchestrator stage1 结构体不在其语料面）。,S1 无此项（defaults.json 的曝光容差是 calibration.dark_light_exposure_tolerance=5 s，另一物）｜S2 无此项｜S3 无此项｜S4 orchestrator stage1.schema.json#/calibration 侧无 default 声明｜S5 json_config.h:37 double dark_exposure_s=0.0; 注释「0 表示 null」｜S6 无行
③哨兵不一致,dec_deg（机械并组吃进 initial_dec_deg）,export.center／orchestrator.stage1.platesolve,T1 ＋ T3：同一物理量（赤纬）在两个入口用两种「未设」表示法,无（跨入口、跨键名的同语义比较无任何门；见文首漏检口径第 5 条）,S3（并组属机械副作用，但被暴露的问题本身成立：-999 与 0.0 二义）,"保守方向＝-999 属 FITS 习惯，但 0.0 对赤纬是合法值（天赤道），对赤经更是合法值：任何把 0.0 当「未设」的路径都会静默把源解算中心放到 (α,δ)=(0,0)。建议 orchestrator 侧改为「键不出现即无初始位置」，或把哨兵抬到值域外（赤纬 |δ|>90 恒非法）并写进 schema description。影响面＝IPV 解算初值命中半径、export 投影中心。",新暴露。,"S1 无此项｜S2 export 模板 center.dec_deg=0.0｜S3 CLI 骨架 center={""ra_deg"":0.0,""dec_deg"":0.0}｜S4 phase_config_export 侧 |dec|<=85 由代码判（schema description 陈述）｜S5 p3_session.cpp:150 doc[""center""][""dec_deg""].get<double>() 无兜底（必出现）＋ json_config.h:46 initial_dec_deg=-999.0「-999 表示 null」｜S6 无行"
③哨兵不一致,flux_conservation_factor,v6 provenance／aio,T3：0 被当「缺因子」哨兵，而 DRZ-FLUX-FIX-01 裁定该因子恒为 1 ⇒ 0 既非合法值也未在合同里声明,部分：product_family_field_constraints.schema.json 有 fail_closed 文案「缺 flux_conservation_factor 或取值 != 1 -> 不可用于绝对通量」，属合同声明面；无门把该文案与代码里的 0 兜底对齐（CHK-PRODUCT-CONTRACT 系判结构不判兜底值）。,S3（方向保守：代码 0 兜底后用于「不可用」判定，未致误绿；但 0 与恒 1 的关系只在注释里）,保守方向＝让 unset 与 0 分离：成员初值改为 std::optional 或 NaN（仓内已有 kMon001NotSampled=-1.0 的显式命名哨兵先例，见 A3 判读），并在 provenance 写出侧强制为 1；影响面＝绝对通量可用性与 FZ-COND-FLUX-CONSERV 门。,新暴露（A1/A3/BD1 未判该字段的哨兵形态；v6_clause_registry 侧的因子恒 1 裁定在 A1 的 pixfrac 行被引用过）。,"S1 无此项｜S2 无此项｜S3 无此项｜S4 product_family_field_constraints#/signal/normalization/flux_conservation_factor（含 fail_closed 文案）｜S5 v6_provenance.h:113 与 v6_drizzle_science.h:374 声明 0.0、v6_provenance.cpp:264 contains(""flux_conservation_factor"")?...:0.0、:58 处 value(""flux_conservation_factor"",...) 形似取 ""k_corr""（并组噪声，见判读）｜S6 无行"
③哨兵不一致,light_exposure_s,orchestrator.stage1.calibration,T3：注释给的是「> 0」，成员初值是 0.0 ⇒ 0 既被判非法又被当默认,无（同上：stage1 配置面不在任何默认值门的键集来源里）,S3,保守方向＝与 dark_exposure_s 同批处理（见该行）。此字段与 dark_exposure_s 是一对：一个注释说「0=null」一个说「>0」，同一 struct 内两行相邻注释自相矛盾，属哨兵词表缺失的直接证据。影响面＝K=t_light/t_dark 与暗场回退模式（fallback=exposure_ratio/fail）。,新暴露。,S1 无此项｜S2 无此项｜S3 无此项｜S4 stage1.schema.json 无 default｜S5 json_config.h:36 double light_exposure_s=0.0; 注释「> 0」｜S6 无行
③哨兵不一致,median,benchmark 后端面（profile_gen_v2 / cpu_routing）与 master_unit_guard,T3：同键一处 -1.0 表未采样、另一处 0.0 表未设，第三种写法 absent；本行兼有同名异义嫌疑,无（benchmark 面不属配置键面，任何默认值门 glob 都不覆盖）,S4（观察级：非科学参数，且 -1 哨兵与 A3 判读记的 kMon001NotSampled 同形同义，方向一致）,保守方向＝把「未采样」哨兵在观测面统一到一个已命名常数（kMon001NotSampled 已有先例），不散写字面量；0 只有在被度量量本身可为 0 时才危险，median_ms/median_service_ms 非负 ⇒ 当前无假绿路径。影响面＝benchmark profile 生成与 CPU 路由建议（不改科学结果）。,新暴露为键；但同族哨兵约定 A3 已判（kMon001NotSampled=-1.0，resource_gate.h:121，并自证「仅用于非负量」）。本行与该判读同向、不另立案。,"S1 无此项｜S2 无此项｜S3 无此项｜S4 无（合同面对 median 无 default 节点；本行 T3 由 S5 触发）｜S5 cpu_routing.cpp:336 value(""median"",-1.0)、profile_gen_v2.cpp:703 value(""median"",0.0)、master_unit_guard.h:62 声明 0.0｜S6 无行"
③哨兵不一致,nside,drizzle／orchestrator.stage1.drizzle.nside／aio HISS 网格,"T3：同一成员在同一 struct 内既是默认 32768 又是「0 ⇒ 触发自动 nside」哨兵；另有 auto/explicit 双形态与 [16,4194304] 合同域",无（drizzle_engine.h:25 是裸声明初值，接收者形态不匹配 CHK-CONFIG-DEFAULTS；defaults.json 无 nside ⇒ 无权威侧可比）,S2（nside 决定输出分辨率与 leaf_order 推导；32768 与「auto」两种缺省语义共存且无登记面）,"保守方向＝把 auto 显式化为 nside.mode=auto（stage1.schema.json 已是这个形态：oneOf{mode:auto | mode:explicit,value∈[16,4194304]}），而引擎层不应再有 32768 与 0 两个数值兜底；保守取值＝**拒绝无模式的裸 nside 缺省**（宁报错不猜分辨率）。影响面＝HEALPix 分辨率、tile 数、内存峰值与 leaf_order=tile_order+log2(W) 链。",新暴露。CLI 侧「nside 缺省即 auto（由最细输入采样派生）」的散文在 A1/BD1 未作为默认值分叉判过。,"S1 无此项｜S2 模板 drizzle 对象不含 nside（缺省即 auto）｜S3 CLI 骨架 drizzle 内嵌串无 nside，--help 文本声明「nside 缺省即 auto」｜S4 stage1.schema.json#/drizzle/nside oneOf(auto | explicit value ∈[16,4194304])，无 default｜S5 drizzle_engine.h:25 int nside=32768（同行注释「nside=0 触发自动 NSIDE 计算」）、0×28 处（含 value(""nside"",0)、各 struct 声明）、1×1、2×1、16×2、4194304×1、NSIDE_MIN/NSIDE_MAX×4｜S6 无行"
③哨兵不一致,precision_mode,drizzle（阶段一精度键）／aio HISS,"T1 ＋ T3：模板与骨架预填 1，而生产缺省是 -1 哨兵（判 !=0/1 即 REJECT）；合同 enum=[0,1] 不含 -1",无（enum 与 -1 哨兵的差集无门判；CHK-CONFIG-DEFAULTS 只比字面量集合，-1 与 1 分属 CLI 表与 scheduler 两处不同形态，且其键集来源不含 S3）,S3（行为上 fail-closed 正确：-1 只作「缺项」内部标记，随即被判红；但模板预填 1 使「必须显式声明精度」的 B2-A12 意图对用户不可见）,保守方向＝保留 -1 内部哨兵，但把模板侧从「预填 1」改为「写 null 或留注释位」以恢复强制显式（或反向：明确承认预填即出厂口径 FP64，并把这条写进 CONFIG_CONTRACT，让两面一致）。不得放宽 module_adapters.cpp:7523-7526 的 REJECT。影响面＝阶段一 FP32/FP64 选择与 SparseEqualsDense 1e-12 等价门；与 precision.default(fp64)/bitpix(-32) 属同一精度家族（该家族四面互异 A1 已立案，见文首漏检第 5 条）。,新暴露为该键；同族的 precision.default↔bitpix=-32 分歧 A1 已判（precision.default 行，四面互不相同）。,"S1 无此项（precision.default=""fp64"" 是另一键）｜S2 模板 drizzle.precision_mode=1｜S3 CLI 骨架内嵌串 precision_mode=1（--help：必须显式 0/1，无 silent 缺省）｜S4 phase_config_normalize#/drizzle_config/precision_mode enum=[0,1]，无 default｜S5 module_adapters.cpp:7523 p1_int(dj,""precision_mode"",-1) 后判 !=0/1 ⇒ REJECT、orchestrator.cpp:3548 orc_getJsonNum(...,-1.0) 作「字段缺失」哨兵、drizzle_engine.h:39 与 hiss_format.h:326 声明 0、main.cpp:144 声明 0｜S6 无行"
③哨兵不一致,read,调度器 mosaic_window 局部计数,T3（机械触发）,不适用（非配置键）,无案——同名异义伪分叉,不需处理。留行的作用是把「泛用叶子必然被 S5 局部变量吃进」这一机械口径边界留下可复算证据（mosaic_window.cpp:153 的 read=0 是计数器初始化，与任何配置默认无关）。,不适用。,仅 S5 命中（mosaic_window.cpp:153 read=0）；S1/S2/S3/S4/S6 无此项 ⇒ 该键无配置身份
③哨兵不一致,scale_deg_per_px,export／p3 会话／scheduler p3 节点,T1 ＋ T3：模板/骨架示值 0.001，生产缺省 0.0（合同要求 >0）,无（S1 无该键 ⇒ 无权威侧；三处读键点分属两模块，CHK-CONFIG-DEFAULTS 因接收者形态与键集来源双重不匹配而不吃）,S3（三处读点均随后显式具名校验，0 不落进几何：p3_session.cpp:108-109 与 module_adapters.cpp:13735 都判 !(scale>0) 即 fail；p3_session_run 的 :152 不内联判而由 p3_wcs_make 统一拒 ⇒ 同一字段三处各写一遍缺省、两处校验一处委托，属重复面而非漏洞）,保守方向＝三处缺省收敛到一处常量并统一「未设 ⇒ 具名报错」；0.001 是模板示例而非登记默认，若要它成为出厂口径就得进 defaults.json/合同 default 面（否则用户删键后拿到的 0.0 与模板给的 0.001 语义不同）。影响面＝导出角尺度与 scale→nside/tile 规划。,已判键名：BD1 有 scale_deg_per_px 行（判的是测试/产品面取值来源）；本行的三处生产缺省与哨兵重复面为新暴露。,"S1 无此项｜S2 export 模板 scale_deg_per_px=0.001｜S3 CLI 骨架 ""0.001""（--help：必须 > 0）｜S4 phase_config_export 侧仅 description（无 default）｜S5 p3_session.cpp:108/:152 value(""scale_deg_per_px"",0.0)、module_adapters.cpp:13727 同、plan_estimator.h:101 声明 0.0｜S6 无行"
③哨兵不一致,snr,normalize snr 科学块／orchestrator stage_timeout_sec／各 wrapper 结构体,T3（0 与 nullptr 两种「未设」并存，且合同对该路径要求 >0）,无（本行的 S4 位点 stage1.schema.json#/execution/stage_timeout_sec/snr 与 S5 的 snr 字段是**两个不同对象**，被同叶子并组；跨对象同名叶子无任何门比较）,无案（同名异义伪分叉），但机械并组暴露的一条真实口径问题需登记：CLI 的 snr 块 json==nullptr（不写模板、--help 只列 gain/read_noise/zero_point_mag），而 phase_config_normalize 的 snr 段与 defaults.json 的 snr.path 是三个不同层次的「snr」,不需按分叉处置。若要收敛观测面：snr 类字段的 unset 表示法应统一为 NaN 或 optional（weight_chain.h:44 与 photometer.h:18 用 0.0、star_detector.h:20 用 0.0、CLI 表用 nullptr 三种），否则「SNR=0」与「SNR 未知」在下游不可区分。影响面＝绝对 SNR 传递链（创新点 SCI-B）的可用性判定。,已判：BD1 有 snr 行（判的是测试激励值 100.0 与机械层并值问题）。本行的三形态 unset 属新暴露的口径登记。,S1 无此项（snr.path 另成组）｜S2 mosaic 模板 snr_path=sparse_reconstruct（不同键）｜S3 CLI 骨架 snr json==nullptr｜S4 stage1.schema#/execution/stage_timeout_sec/snr exclusiveMinimum=0｜S5 0.0×4 ＋ nullptr×3｜S6 无行
③哨兵不一致,storage_form,normalize 落盘形态（HIPS-IDX-01）,"T3：同一合同节点既声明 default=""archive"" 又把 """" 与 null 列进 enum ⇒「未设」与默认值在值域内同时合法",部分：CHK-CONFIG-CONSUMED＋eng/ci/ledgers/dead_config_keys.json#dead_config_key:storage_form 已登记「键由 CLI 认识但生产消费点未落地」⇒「无实效默认」这一面已覆盖；**default 与 null 同域合法**这一形态无任何门。,S3（方向保守：CLI 已自述「缺失/空串 ⇒ 取默认 archive 并报 warn」，不静默；但该处置规则只活在 session_commands.h 注释与 --help 文本里，合同面把三态并列成合法值而未写明三态各自语义）,"保守方向＝合同面把 unset 与取值分开：enum 只留 [archive, bare]，unset 由「键不出现」表达并单写 missing 处置；若保留 """"/null 则须在 description 里写「等价于未设 ⇒ archive + warn」。影响面＝Phase1 产品落盘形态（archive vs bare）与其 tree_hash 不变式声明。",新暴露（四路未判 storage_form 的三态并列；死键事实来自台账而非四路判读）。,"S1 无此项｜S2 模板无此项（CLI 有意不写）｜S3 CLI 骨架 json==nullptr（键名列 --help）｜S4 phase_config_normalize#/storage_form default=""archive"";enum=[""archive"",""bare"","""",null] ＋ hips_storage_form.schema.json#/product_index/storage_form enum=[archive,bare]（**两份合同对同一键值域不一致**）｜S5 无字面量兜底点｜S6 无行"
③哨兵不一致,target_pixel_area,v6 provenance units（阶段一/二/三产品面）,T3 ＋ 值面分叉：同一 units 字段三处生产赋值分别取 1.0（Phase2/Phase3）与 A_pixel（Phase1），另 0.0 作声明初值,无（provenance 面走 CHK-PRODUCT-CONTRACT／v6 条款门，判字段存在与 const，不判上游写它的字面量选择）,S2（单位面自证：合同给 target_pixel_area 声明 exclusiveMinimum=0，写 1.0 意味着「目标像素面积=1」这一无量纲归一，而 Phase1 写实面积 A_pixel ⇒ 同一 provenance 字段在三个阶段语义不同；下游按面积换算的权重/通量口径会跨阶段不一致）,"保守方向＝把「单位面积约定」写成显式枚举字段（如 area_basis ∈ {pixel_index_normalized, physical_arcsec2}），禁止用同一数值 1.0 承载两种基准；Phase1 的 A_pixel 侧保持物理值。影响面＝面亮度/通量归一、A_drop=pixfrac²·A_pixel 链、跨阶段合成可用性（与 FZ-COND-FLUX-CONSERV、DRZ-FLUX-FIX-01 同族）。",新暴露（A1/BD1 未判 units.target_pixel_area 的跨阶段赋值差异）。,"S1 无此项｜S2 无此项｜S3 无此项｜S4 product_family_field_constraints#/provenance/units/target_pixel_area exclusiveMinimum=0 ＋ unified/{coverage,depth_m5,...}#/units/target_pixel_area｜S5 phase1_product.cpp:236 p.units.target_pixel_area=A_pixel、phase2_integrate.cpp:441 与 p3_v6_export.cpp:267 =1.0、v6_provenance.h:28 声明 0.0｜S6 无行"
③哨兵不一致,w_info,information weight（V6 点信息面）／psfsw 源名,T3（0.0 既是「未计算」声明初值又是合法权重？合同 pixel_area_power_defaults 处 const=0）,"无（本行是并组产物：S4 的两个位点一个叫 W_info 的数值字段、一个叫 bunit 幂次默认；S5 的 ""w_psf"" 是来源名字符串。三者在合同里根本是不同对象）",无案（同名异义伪分叉）。真实可登记点只有一条：information_weight.h:45/:82 与 phase2_integrate.h:163 以 0.0 作 w_info 声明初值，而 IVAR/信息权重为 0 语义上等于「零权重＝剔除」⇒ 未计算与剔除不可区分。,保守方向＝w_info 的 unset 用 NaN 或 optional（仓内 -1.0 哨兵先例见 resource_gate，可统一）。影响面＝点信息/信息权重链（SCI-B 绝对 SNR 的权重侧）。,新暴露为键；与 BD1/A1 已判的 ivar/variance 零权重家族相关，属该家族的 unset 表示法补全。,"S1 无此项｜S2 无此项｜S3 无此项｜S4 unified/point_information#/W_info exclusiveMinimum=0 ＋ product_family_field_constraints#/units/bunit_semantics/pixel_area_power_defaults/W_info const=0｜S5 0.0×3（声明）＋ ""w_psf""×2（psfsw.cpp:683、p3_rsmp_failclosed.cpp:46 的来源名）＋ ""ADU^-2""×1｜S6 无行"
③哨兵不一致,workers,cpu_profile kernels／benchmark 后端／调度器与流式管道结构体,T1 ＋ T3：同键三套未设/合法表示法（-1、0、1）并存，而合同 cpu_profile 要求 minimum=1,"部分：resource_binding 类默认值按 UNIFIED_MODEL §3／CONFIG_CONTRACT §3 禁入 phase_config，登记面 declared_default=""由 profile"" 即为该纪律的表达 ⇒ 「无 defaults.json 数值源」是设计意图，不是缺口；**但**生产结构体的 0/1/-1 三套初值与 cpu_profile 的 minimum=1 之间无任何门比较。",S3（资源面不改变科学结果；风险在「0 或 -1 被当合法并行度传下去」的那条路，实测 cpu_routing.cpp:389 用 -1 表未设且随后判 >=0，方向保守）,保守方向＝未设统一用一个已命名常数（现有 kMon001NotSampled=-1.0 先例可复用），禁止 0 兼作「未设」与「零 worker」；对 profile 缺失的降级必须是显式 1 worker 并留痕，不得静默按 CPU 数。影响面＝回压/常驻 worker 账本（与待裁的架构第 3 条同族）与所有并行度相关计时基线。,新暴露。与既有事实「一组内存阈值游离在唯一数值源之外」同属 runtime_resources/cpu_profile 面，但本行是并行度不是内存阈值，不重复立案。,"S1 无此项（设计禁入）｜S2 无此项｜S3 无此项｜S4 cpu_profile.schema.json#/kernels[].workers 与 #/kernel_v1/workers minimum=1｜S5 1×15（含 struct 声明与 a./r. 赋值）、0×7（含 value(""workers"",0)）、-1×1（bench_report.cpp:389）、0u/1u 各 1｜S6 declared_default=""由 profile""（19_runtime.md:67，finding=none）"
③哨兵不一致,write,cpu_profile.memory_bandwidth／psf dpsf 局部计数,T3（机械触发）,不适用（非配置键）,无案——同名异义伪分叉（S4 的 write 是内存带宽读写对的 write，S5 的 write=0 是 dpsf_psf.cpp 里的循环/计数局部量）,不需处理；留行是为可复算地记录「泛用叶子必然被 S5 吃进」的边界。,不适用。,S4 cpu_profile#/profile_v2/memory_bandwidth/write exclusiveMinimum=0｜S5 dpsf_psf.cpp:955/:1120 write=0｜其余四侧无此项
⑤仅跨侧异值（含同名异义嫌疑）,block,cpu_profile kernel_v2.block／benchmark 后端 tile-block 计数,T1（0/1/256 三套值）,"部分：登记面 declared_default=""由 profile"" 已把该键排除在 defaults.json 之外（设计意图）；跨侧值比较无门。",S4（观察）。硬证据是 cuda_backend.hpp:115 同时以 `#define block 256` 与 `block = 256` 出现 ⇒ 宏名与键名撞车，属「宏拼接」这一机械漏检形态的实例，值得单独留一条而不算分叉。,保守方向＝资源面缺省保持「由 profile 决定」（AGENTS §6 禁硬编码线程/ISA/block），把 benchmark 侧的 256 归到 profile 生成物而不是源字面量；影响面＝内核 block 尺寸选择与 ISA profile 一致性。,已判键名：A2 有 block 行（判的是别处的 block 常数）。本行补 cpu_profile 侧登记形态。,"S4 cpu_profile#/kernel_v2/block minimum=1｜S5 0×5、1×3、256×2（含 #define）｜S6 declared_default=""由 profile""（19_runtime.md:68）｜S1/S2/S3 无此项"
⑤仅跨侧异值（含同名异义嫌疑）,center,export.center 对象／snr_science 局部,"T1（S3 给整个对象 {""ra_deg"":0.0,""dec_deg"":0.0}，S5 给一个同名局部量 0.0）",无（对象形与标量形不可比；键表面向量），本行属机械并组产物,无案（伪分叉）。可登记：S3 的 center 是**整对象**写进骨架，故「center.ra_deg 有值」在机械面上永远取不到（要先把内嵌 JSON 解开——本脚本已解，登记面未解）,不需处理；但提醒任何后续一致性门必须以「展开后的叶子」为比较面，否则 CLI 骨架面永远进不了比较（这正是 CHK-CONFIG-DEFAULTS 的现状）。,新暴露为键，判为伪分叉。,"S2 export 模板 center={ra_deg:0.0,dec_deg:0.0}｜S3 CLI 骨架同形（叶子展开后 ra_deg/dec_deg 各自成组）｜S5 snr_science.cpp:76 center=0.0（局部量）｜S1/S4/S6 无此项"
⑤仅跨侧异值（含同名异义嫌疑）,checksum,17_aio 插件选项 checksum（开关）／产品描述符校验和字段／FNV 初值,"T1（S6 ""true"" 对 S5 0 与 2166136261u）",部分：CFG002-01/02 管该登记的 docs↔登记册↔defaults；本行的对立两侧根本不同物 ⇒ 门无从判。,无案（同名异义伪分叉）。登记一条口径：S6 用 field=checksum 登记而 registered_key 缺省 ⇒ 我按 field 归组，把它和一个结构体字段撞在一起；后续门若按 registered_key 归组则本行自动消失。,不需处理。真值语义：aio 的 checksum 开关默认 true（写盘带校验和），与 2166136261u（FNV-1a offset basis）无关。影响面＝无（判为伪分叉）。,新暴露为键，判为伪分叉。,"S5 0×5（desc.checksum/struct 声明）＋ 2166136261u×1（orchestrator.cpp:4603）｜S6 declared_default=""true""＋note 断言默认 true（17_aio.md:39，finding=none）｜S1/S2/S3/S4 无此项"
⑤仅跨侧异值（含同名异义嫌疑）,crpix_px,export WCS 参考像素（FITS 1-based）,"T1（S2/S3 数值 [512.5,512.5] 对 S6 散文「中心」）",部分：dead_config_keys.json#dead_config_key:crpix_px 已登记「合同声明＋CLI 可见，但生产科学消费点为零，参考像素当前恒取输出几何中心」⇒「默认字面量即实取值」这一面已被台账盯住；S6 的散文默认与数值不可自动判等，无门。,"S3（该键当前 no-op；风险在补齐消费点时把 [512.5,512.5] 当成默认而 1024×1024 之外的画幅中心不是它）",保守方向＝登记面把「缺省=中心」写成可展开式（crpix=(W+1)/2 与 (H+1)/2）而非数对；模板侧注明是随 1024×1024 示例派生。影响面＝导出 WCS CRPIX 与 FITS 1-based 约定。,新暴露（四路未判 crpix_px）。,"S2 export 模板 [512.5,512.5]｜S3 CLI 骨架 ""[512.5, 512.5]""｜S4 phase_config_export#/export_wcs/crpix_px 仅 description（无 default）｜S5 session_commands.h:272（即 S3 同一处的字面量被 S5 形态再吃一次，属自身重复非外部冲突）｜S6 declared_default=""中心""（14_projection.md:37）｜S1 无此项"
⑤仅跨侧异值（含同名异义嫌疑）,gaia_data_dir,wcs 段／star_detection 段（两处同名键）,"T1（S2/S3 占位路径 对 S6 declared_default=""无""）","无（「无默认」被登记成字符串 ""无""，与占位路径在机械面上不等值 ⇒ 任何默认值相等判定都会误报；CFG002 的 _value_equal 只在 registration=defaults_json 时比，本行 registration 不是）",S3（登记口径问题，非取值分叉）：路径类键的「无默认」应在登记册写成 null，把散文说明放进 note；否则模板占位串与登记串必然长期假报分叉。,保守方向＝统一登记口径（null=无默认，note 承载说明），并在合同侧把 wcs.gaia_data_dir 与 star_detection.gaia_data_dir 的**两处同名**显式区分（CLI --help 注释已承认 gaia_data_dir 的模板承载走 wcs 段）。影响面＝Gaia 参考星表定位与权威路径回退（catalog_guided 不可用时显式降级）。,新暴露。,"S2 normalize 模板 blocks[].wcs.gaia_data_dir=""path/to/gaia""｜S3 CLI 骨架 wcs 内嵌串同值（另 star_detection 段 json==nullptr）｜S4 phase_config_normalize#/star_detection_config/properties/gaia_data_dir 无 default｜S5 无字面量点（std::string() 空兜底被 lit_only 拒）｜S6 declared_default=""无""（03_star_detection.md:63）｜S1 无此项（paths.gaia_root=""gaia"" 是另一键）"
⑤仅跨侧异值（含同名异义嫌疑）,height_px,export 画幅高（px）,"T1（模板/骨架 1024 对生产缺省 0；合同 [1,20000]）",无（同 scale_deg_per_px 行：三处读点分属 p3_session 与 scheduler，CHK-CONFIG-DEFAULTS 键集来源不含该键的模板面（blocks[] 面失效）⇒ 零命中）,S3（行为保守：0 在两处被具名校验拒、一处委托 p3_wcs_make 拒，不落进几何）,保守方向＝与 scale_deg_per_px/width_px 同批：单点定义「未设 ⇒ 具名报错」，模板的 1024 注明为示例画幅而非出厂默认（它同时决定 crpix_px 的示例值）。影响面＝导出画幅与 crpix 派生、tile 规划。,新暴露（BD1 判过 scale_deg_per_px，本键未判）。,"S2 export 模板 1024｜S3 CLI 骨架 ""1024""（--help 注 1..20000）｜S4 phase_config_export#/export_wcs/height_px minimum=1;maximum=20000（无 default）｜S5 0×8（p3_session.cpp:111/:154、module_adapters.cpp:13729/:13805、p3_proj_v6.h:134、p3_projection.h:79、p3_wcs.h:21、plan_estimator.h:94）｜S1/S6 无此项"
⑤仅跨侧异值（含同名异义嫌疑）,hips_paths,mosaic 输入产品列表,T1（S2 给两条示例路径数组、S3 给空数组 []）,无（数组形：门的标量比较不吃；且 S3 用 [] 表示「必填非空」是**故意留空**，与 S2 给两条示例是两种表达「必填」的写法）,无案（表达法不统一，非取值分叉）,保守方向＝同类「必填列表」在模板里统一写法：要么统一给 path/to/… 占位（让 --template 可直接改），要么统一给 []（让「必填非空」由预检报错），两面混用会让「模板拿来就能跑」这条验收判据不稳定。影响面＝mosaic 输入面与 CLI-002/GAP-034 的「预检与运行期同字段」不变式。,新暴露。,"S2 mosaic 模板 [""path/to/p1_hips_a"",""path/to/p1_hips_b""]｜S3 CLI 骨架 []｜S1/S4/S5/S6 无此项（S4 mosaic schema 对该键无 default）"
⑤仅跨侧异值（含同名异义嫌疑）,log_dir,21_observability 日志目录／orchestrator 配置／ipv 传参,"T1 ＋ 实质属「登记即错」：登记面声明派生值 <output_dir>/logs，生产侧写死字面量 ""run/logs/orchestrator""",无（CFG002 的值相等只跑在 registration=defaults_json；本行 registration 非该类 ⇒ 不比对。orchestrator 侧的配置面整体不在任何默认值门的 glob 内）,S2（登记面承诺的是**派生关系**，生产给的是常量 ⇒ 换 output_dir 不会换日志落点，与 AGENTS §6「不把运行产物散落根目录、一切输出落 output_dir 或 run/」直接相关）,"保守方向＝向登记面收敛：log_dir 由 output_dir 派生（<output_dir>/logs），并把当前硬编码 ""run/logs/orchestrator"" 改为派生＋显式覆盖形；若产品决定不派生，则必须改登记面 note 说明「生产恒写 run/logs，<output_dir>/logs 是历史口径」。影响面＝运行产物落点纪律、日志/证据可定位性与轮次回收（run_gc 的保留判据）。",新暴露。,"S5 ""run/logs/orchestrator""×1（orchestrator.cpp:573 config_.log_dir）＋ nullptr×3（ipv_types.h:281、ipv_entry.cpp:131，另一物）｜S6 declared_default=""<output_dir>/logs""（21_observability.md:35）｜S1/S2/S3/S4 无此项"
⑤仅跨侧异值（含同名异义嫌疑）,lower_sigma,rejection 各档下侧裁剪 σ,T1（S1 同时含 4.0 与 5.0，S5 只被扫到 4.0）,无，且**这里暴露一个机械面系统性盲区**：rejection.cpp:1252 写的是 `p.linear_fit.lower = 5.0`，成员名与其它档的 `lower_sigma` 不一致 ⇒ 任何按键名的比较（本预筛、CFG002、CHK-CONFIG-DEFAULTS）都会把 linear_fit 档读成「无默认」，S1 的 5.0 一侧在代码面上不可见。同类：rejection.percentile.low/high（defaults 名）对 p.percentile.low_fraction/high_fraction（代码名）。,S2（不是取值分叉，而是「键名不贯穿」使已登记默认无法被任何一致性门验证；linear_fit 档 5.0/3.5 只有 defaults.json 与散文可证）,保守方向＝把结构体成员名与注册键名逐字对齐（lower_sigma/upper_sigma、low/high 二选一），或补一张机器可读的「注册键 ↔ 结构体成员」映射表（同 CONFIG_CONTRACT 的 anchor 机制）供门比对。影响面＝四档排异的默认可验证性；不改任何数值（ENGINEERING_SPEC §3 禁随意改默认容差）。,已判：A1 有 rejection.sigma.lower_sigma 行（判出处与两侧值域）。本行的增量是「linear_fit 档成员名不贯穿 ⇒ 门不可见」，A1 未记。,"S1 rejection.{sigma,winsorized,averaged_sigma,median_sigma}.lower_sigma=4.0 ＋ rejection.linear_fit.lower_sigma=5.0｜S2 无此项｜S3 CLI 骨架 reject 段 json==nullptr｜S4 phase_config_normalize 的 rejection_profiles 未声明 default（机械面零 default）｜S5 rejection.cpp:1246/1247/1249/1255/1256 的 *.lower_sigma=4.0（**无 linear_fit**，它在 :1252 以 .lower=5.0 出现）｜S6 无行"
⑤仅跨侧异值（含同名异义嫌疑）,max_iter,11_upm 插件表 max_iter／legacy healpix_stack／acr benchmark／ipv,T1（S6 declared_default=null＋finding=unregistered 对 S5 五组值 5/20/64/100/256）,部分：CFG002-01/02 已把该登记行标 finding=unregistered（登记面自己承认未落配置类）⇒ 「无唯一数值源」已登记；跨侧值比较无门。,S3（登记面为 null，未主张默认，故不构成登记↔实测冲突；风险是插件文档用 max_iter 而合同用 limiting_mag_max_iter，两名字无机器映射）,保守方向＝文档面键名与合同面键名逐字统一（phase_config_normalize#/star_detection_config/propertyNames 已封闭为 limiting_mag_max_iter，插件表 11_upm.md:96 的 max_iter 应改指该实名或标注别名），并把 unregistered 收口进配置类或删列。影响面＝UPM 与星表引导星等迭代的默认可追溯性。,新暴露。与 lower_sigma/upper_sigma 行同属「键名不贯穿」家族（那条是结构体成员名不贯穿，本条是文档名与合同名不贯穿）。,S5 5×3（archive/legacy healpix_stack）、20×1（nls_lm.h:42）、64×1（ipv_wcs.h:81）、100×1（orchestrator.cpp:2293）、256×2（acr benchmark_driver.cpp:457，兼 #define）｜S6 declared_default=null（finding=unregistered）｜S1/S2/S3/S4 无 max_iter 此项（合同实名 limiting_mag_max_iter 另成组）
⑤仅跨侧异值（含同名异义嫌疑）,max_stars,"star_detection（合同域 [20000,50000]）／psf·fit_cfg（生产 5000）／orchestrator platesolve（2000）","T1 ＋ 实质问题在值域与登记：三个生产入口取三套缺省（20000 / 5000 / 2000），其中 5000 与 2000 都落在 star_detection 合同域 [20000,50000] 之外",无有效覆盖：三个读取点分属 scheduler 与 orchestrator 两侧、接收者名（sd/fit_cfg/doc）不匹配 CHK-CONFIG-DEFAULTS 的 _CONFIG_RECEIVER_RE 白名单，且 stage1.schema.json 不在其键集 glob 内；CFG002-02 只比登记册↔defaults（本键 defaults 无项 ⇒ 无比对面）。,S2（与既有事实「同名参数两个生产入口取不同默认值」同族但**第三个入口此前未数出**：module_adapters.cpp:4288 与 :5532 的 psf/fit_cfg 侧 5000、json_config.h:43 的 platesolve 侧 2000、module_adapters.cpp:3346 的 star_detection 侧 20000。风险不是 star_detection 判错，而是 5000/2000 两入口**没有任何合同值域**（stage1 只写 minimum=1），用户与门都无法判其合法性）,"保守方向＝给每处同名键各自一个带命名空间的键名（star_detection.max_stars / psf.fit_max_stars / platesolve.max_stars）并各自登记值域；未登记前取该处现行值作为显式登记值（不改数），禁止把 star_detection 的 [20000,50000] 当作全局约束套用。影响面＝星表引导候选星数、PSF 拟合参考星选取、解算初值星数（三处都直接影响科学输出的稳健性）。",新暴露（四路未判 max_stars 的三入口差异；A1 从 defaults 起，本键不在 defaults.json）。,"S1 无此项｜S2 无此项｜S3 CLI --help 文本写「max_stars(合同域 [20000,50000])」，段本身 json==nullptr｜S4 phase_config_normalize#/star_detection_config/max_stars default=20000;minimum=20000;maximum=50000 ＋ stage1.schema#/platesolve/max_stars minimum=1｜S5 kP1GuidedMaxStarsLo=20000（:3309/:3346）×2、5000×2（:4288/:5532）、2000×1（json_config.h:43）、300×1（ipv_robust_refine.h:44）｜S6 declared_default=""20000"" ＋ **note 断言写「默认0」**（同一登记行内部自相矛盾）"
⑤仅跨侧异值（含同名异义嫌疑）,output_dir,三命令通用运行产物落点,"T1（S2 四套 path/to/out/… 对 S3 mosaic 的 "".""；另 S5 有 ""run"" 与 35 处 "".""）——按实质应归 ①，机械归 ⑤ 只因 defaults.json 无该键","无。CHK-CONFIG-CONSUMED 会判「模板键有无读取点」（有），不判值；CHK-CONFIG-DEFAULTS 的模板侧因 blocks[] 形态而零键，且其接收者白名单不含 doc/dj 一类会话文档名 ⇒ 35 处 value(""output_dir"", std::string(""."")) 全不在门内。","S1 候选（判 S2 保守上报）：parser.cpp:447 的声明是「块级 output_dir 必填非空、**禁 silent default**」，而 lib/** 有 35 处缺键即取 "".""（commands.cpp 8、module_adapters.cpp 26、runtime_client.cpp 1）。声明面与兜底面方向相反，且 ""."" 会把产物写进当前工作目录——正是 AGENTS §6「不把运行产物散落根目录」的反例。","保守方向＝向声明面收敛：缺 output_dir 应 fail-closed 具名报错（宁可不写，不写进 CWD）；若产品决定保留 ""."" 兜底，则改 parser.cpp 的「禁 silent default」措辞并在 --help 里同步，二者不能并存。orchestrator 侧的 ""run""（orchestrator.cpp:576）与 log_dir 行同批处理。影响面＝三命令全部产物落点、run manifest 位置、轮次回收（run_gc 的目录识别）与「产物不入库」不变式。",新暴露。此项与本路已知事实「生产兜底在对面」同类，属补全的第 N 条（此前未有人数过 35 这个量级）。,"S1 无此项（设计：路径不入 defaults）｜S2 三模板各写 path/to/out/{red,ha}/{mosaic,export}｜S3 normalize 块=""path/to/out/red|ha""、mosaic/export="".""｜S4 phase_config_* schema 对 output_dir 无 default（required/description 面）｜S5 "".""×35 ＋ ""run""×1｜S6 登记为 inputs_block 类，不主张默认"
⑤仅跨侧异值（含同名异义嫌疑）,parity,star_detection.parity（pos|neg）／export longitude_parity（east_left|east_right）,T1（两侧各取一套 enum 与默认）,无（两份合同、两个真名；本行由 S5 的裸成员名 parity 并到一起）,S3（同名异义伪分叉，但有一条真实登记点：p3 侧合同真名 longitude_parity，而代码成员与赋值面一律叫 parity（p3_wcs.cpp:102、p3_projection.cpp:328、module_adapters.cpp:13524），star_detection 侧合同真名就叫 parity 且默认 pos（module_adapters.cpp:3315）⇒ 同一裸名 parity 在两个阶段是**互斥词表**，任何跨阶段复用同一解析 helper 都会静默取错）,"保守方向＝代码成员名与合同键名逐字一致（p3 侧用 longitude_parity），或给两词表加不同前缀；同时把 {pos,neg} 与 {east_left,east_right} 的换算（POSITIVE_RA 与 east/west 方向）写成一条显式条款而不是注释。影响面＝WCS 手性/经度方向，错了会得到镜像天图（几何级错误，非数值抖动）。",新暴露。,"S1 无此项｜S2 无此项｜S3 CLI --help 列出 star_detection.parity(pos|neg) 与顶层 longitude_parity（两段皆 json==nullptr）｜S4 phase_config_normalize#/star_detection_config/parity default=""pos"";enum=[pos,neg] ＋ phase_config_export#/longitude_parity enum=[east_left,east_right]｜S5 ""pos""×1、""east_left""×4｜S6 无 defaults_json 行"
⑤仅跨侧异值（含同名异义嫌疑）,ra_deg（机械并组吃进 initial_ra_deg）,export.center.ra_deg／orchestrator.platesolve.initial_ra_deg,T1 ＋ T3（0.0 对 -999.0 两种「未设」；且 0.0 对赤经是合法值）,无（跨键名同语义无任何门；见文首漏检第 5 条）,S2（与 dec_deg 行同案，此处更硬：stage1.schema#/platesolve/initial_ra_deg 值域是 minimum=0;exclusiveMaximum=360 ⇒ 结构体默认 **-999.0 落在自己合同的值域之外**；若 orchestrator 的配置校验按该 schema 走，缺省即非法）,保守方向＝unset 用「键不出现」或 std::optional 表达，初值不再借数值；若必须用数值哨兵，选域外值并把域外写进 schema description（-999 目前只在注释里）。同时注意 export 侧中心赤经由 phase_config_export#/center/ra_deg minimum=0;maximum=360 约束，两处一合法域一含 0 一用负哨兵。影响面＝IPV 解算初值与导出投影中心（错值会让解算从错误的天区出发，属可复现性问题）。,新暴露。,S1 无此项｜S2 export 模板 center.ra_deg=0.0｜S3 CLI 骨架 center 内嵌串 ra_deg=0.0｜S4 phase_config_export#/center/ra_deg minimum=0;maximum=360 ＋ stage1.schema#/platesolve/initial_ra_deg minimum=0;exclusiveMaximum=360｜S5 -999.0×1（json_config.h:44「-999 表示 null」）＋ 0.0×3（healpix_core.cpp:249 等，另一物）｜S6 无行
⑤仅跨侧异值（含同名异义嫌疑）,reference_flux,07_noise_snr 插件表 reference_flux／weight_chain·pc_api 结构体字段,"T1（S6 散文「逐帧 F_ref,k」对 S5 0.0×5）",部分：登记面 finding 已标该键无静态默认（逐帧派生）⇒ 「无唯一数值源」是设计意图；无门判 0.0 兼作「未派生」与「参考通量为 0」。,S3（观测级：weight_chain.h:268 与 pc_api.cpp:966/:1024/:1138 用 0.0 作声明初值，测光度数 α=F/F_ref 在 F_ref=0 处必发散或被夹住，需确认下游有 >0 判）,保守方向＝参考通量 unset 用 NaN/optional，并在消费点显式判 >0 才可用（与 saturation_level 同一处置风格）。影响面＝测光校准到星等坐标系的标度因子（创新点 SCI-A 的直接输入）。,新暴露为键；A1 的 photometry 族判过标定到星等坐标系的相关常数，未判 reference_flux 的 unset 表示。,"S1 无此项｜S2 无此项｜S3 无此项｜S4 无该节点 default｜S5 0.0×5（weight_chain.h:268、pc_api.cpp:966/:1024/:1138、aio_hips_writer.cpp:901）｜S6 declared_default=""逐帧 F_ref,k""（07_noise_snr.md:179）"
⑤仅跨侧异值（含同名异义嫌疑）,rotation_deg,export 顶层（死键台账在册）／star_detection 段（有生产读取点）,"T1（S6 ""0"" 对 S5 0.0 ＋ 一条被并进来的 parser.cpp:354 字面量 ""crpix_px""）",部分：dead_config_keys.json#dead_config_key:rotation_deg 已登记（**其范围是 export 投影链**：p3 不读 crpix/rotation）。本行补的是该台账的粒度问题：同名键在 star_detection 段有真实读取点（module_adapters.cpp:3395-3396），裸键名台账会把「一处 no-op」读成「该键全局 no-op」。,S3（值面无分叉：合同 description 默认 0、S6 断言默认 0、生产兜底 0.0 三面同值）；立案点在**台账/登记粒度**：死键与活键共用一个裸名。,保守方向＝把死键台账条目改成带路径的键（export.rotation_deg vs star_detection.rotation_deg），或按 CONFIG_CONTRACT 的 anchor 机制逐处指向消费点。影响面＝投影旋转角是否真被消费（若把 export 侧的 no-op 误当成全局，会漏掉 star_detection 侧的旋转角回归；反之会误报已实现）。,新暴露（四路未判 rotation_deg）。,"S1 无此项｜S2 export 模板 rotation_deg=0.0（该模板含此键）｜S3 CLI 骨架 ""0.0""（注明合同默认 0、消费点未落地）｜S4 phase_config_export#/export_wcs/rotation_deg 仅 description「默认 0」（无 default/enum 节点 ⇒ 本路口径下 S4 记「该侧无此项」）；star_detection_config.propertyNames 含 rotation_deg 但无 default｜S5 0.0×2（module_adapters.cpp:3313/:3396）＋ 骨架串再被吃 1 次 ＋ parser.cpp:354 的 ""crpix_px""（实为键名集合字面量被误标成 value(""k"",d)，属本路口径边界）｜S6 declared_default=""0""＋note 默认 0"
⑤仅跨侧异值（含同名异义嫌疑）,schema_version,会话配置合同版本／stage1 配置版本／产物 manifest 版本／CLI 工具 profile 版本,"T1（""1"" 对 ""1.1"" 对 0/1 整数 对 ""acr-operation-route-profile-2""）",部分：CHK-VERSION／check_version.py 管版本文件，不管配置面 schema_version 字面量；合同面多处 const=1 由 JSON Schema 结构门保证，但**跨文档不比较**。,"S3（各文档各有其版本是正常设计；要登记的是「同名不同域」使按-key 预筛必然产生假分叉，以及一处真实后果：会话面 CLI 声明「恒 ""1""」而 orchestrator 面 Stage1Config 声明 ""1.1""，两面的版本演进互不通知）","保守方向＝给每处 schema_version 写明它管辖哪份文档（description 里带文档名），并允许一致性门按「键＋文档」而非仅按键比较；同处附带一条：Stage1Config.precision 的成员默认是 FP32（json_config.h:24），而 defaults.json precision.default=""fp64"" ⇒ 精度家族的第四个数面站点（A1 的 precision.default 行只列了 bitpix=-32/precision_mode/死键四面）。影响面＝三命令输入合同与 orchestrator 配置的可前推性；精度家族见漏检第 5 条。",新暴露为键；精度家族的一面与 A1 的 precision.default 行相补（该行已判四面，本行补 Stage1Config 侧 FP32）。,"S1 无此项｜S2 三模板均 ""1""｜S3 CLI 骨架 ""1""（--help：配置合同版本，恒 ""1""）｜S4 多处 const=1（cli_modules_list/cli_selftest/cpu_profile/product_*）｜S5 ""1.1""×1（json_config.h:22）、0×3＋1×6（artifact/v6 产品面）、0ULL×1、""inputs""×1（parser.cpp:578 实为 allowed-key 集合，被本路 value 形误标）、""acr-operation-route-profile-2""×1｜S6 无行"
⑤仅跨侧异值（含同名异义嫌疑）,source,export.source 对象／各处 provenance 来源字段,"T1（S3 给内嵌对象 {""hips_dir"":""""}，S5 给四个互不相干的字符串）",无（对象形与标量形不可比；本行是并组产物）,"无案（同名异义伪分叉）。可登记一条：S3 的 export.source 内嵌串是 {""hips_dir"": """"} 而 S2 是 {""hips_dir"": ""path/to/mosaic_hips""} ⇒ 同一「必填非空」在两处以两种写法表达（空串 vs 占位路径），与 hips_paths 行同族；GAP-034 的注释显示该键曾因形态口径互斥导致 export 无配置可进会话，这类形态分歧是复发点。",保守方向＝模板与骨架对「必填」用同一种写法（统一占位路径或统一空值＋预检报错）。影响面＝三命令进会话的预检放行判定。,新暴露，判为伪分叉（其 GAP-034 历史在 CLI 代码注释里，四路未单列）。,"S2 export 模板 source={hips_dir:""path/to/mosaic_hips""}｜S3 CLI 骨架 source={hips_dir:""""}＋InputContract kExport 的 object_field=""hips_dir""｜S4 phase_config_export 无 default｜S5 ""hips_dir""×1（session_commands.h:91 即 S3 自身）、""reason""×1（protocol.h:90，另一物）、""gaia_star_matcher_tukey_irls""/""measured""/"" + weight_source + "" 各 1（provenance 来源名，另一物）｜S1/S6 无此项"
⑤仅跨侧异值（含同名异义嫌疑）,spatial_field_enabled,noise 段空间场开关,T1（S1 登记整数 1，S4 声明 type=boolean 且 default=true ⇒ 按注册值填配置会被合同判型失败）,无门判「defaults 值类型 ↔ schema type」：CFG002-03 只判 enum token 落域，CHK-CONFIG-DEFAULTS 不读 defaults 的 value。解析层的宽容（flag() 同收 bool 与 0/1）救不回合同面。,S2（与 A1 同案，本路机械确认：五侧里 defaults=1 与 schema=true 是唯一一对**类型级**不匹配，其余都是数值/字符串级）,"保守方向＝把 defaults.json 的 1 改为 true（或把 schema 改为 enum [0,1] 的整数形），二选一并写进 CONFIG_CONTRACT；属默认形不改默认语义，仍走变更流程。影响面＝Phase1 空间噪声场是否启用（1/true 同义，但按合同校验的调用链里会直接被拒）。",已判：A1 行 noise.spatial_field_enabled 标「类型不一致立案」，并记第三个未注册旁路开关 disable_spatial_field。本行不重复立案，只补机械读数（全表里类型级不匹配仅此一对）。,S1 noise.spatial_field_enabled=1（unit=bool，constraint 写 0/1）｜S2 无此项｜S3 无此项｜S4 phase_config_normalize#/noise_config/spatial_field_enabled default=true;type=boolean｜S5 module_adapters.cpp:6466-6467 走 flag()（同收 bool 与整数）、noise_model.cpp:1255 成员名 enable_spatial_field（第三个名字）｜S6 无 defaults_json 行
⑤仅跨侧异值（含同名异义嫌疑）,upper_sigma,rejection 各档上侧裁剪 σ,T1（S1 同时含 3.0 与 3.5，S5 只被扫到 3.0）,无——与 lower_sigma 同一成因：rejection.cpp:1252 写 `p.linear_fit.upper = 3.5`，成员名不是 upper_sigma ⇒ linear_fit 档的 3.5 在按-key 面上不可见。,S2（与 lower_sigma 并案：一条「注册键 ↔ 结构体成员」映射缺失，覆盖 linear_fit 与 percentile 两族四处）,保守方向与 lower_sigma 同：成员名逐字对齐或补机器可读映射表。不改数值。影响面＝排异各档的上侧裁剪阈值可验证性。,已判：A1 有 rejection.sigma.upper_sigma 行（出处档）。本行增量＝成员名不贯穿致门不可见。,"S1 rejection.{sigma,winsorized,averaged_sigma,median_sigma}.upper_sigma=3.0 ＋ rejection.linear_fit.upper_sigma=3.5｜S2/S3/S4/S6 无 default 面命中｜S5 rejection.cpp:1246/1247/1249/1255/1256 的 *.upper_sigma=3.0（linear_fit 在 :1252 以 .upper=3.5 出现，未被本口径认出为同键）"
⑤仅跨侧异值（含同名异义嫌疑）,wcs,normalize wcs 段／P1 DAG 节点名,"T1（S3 给内嵌对象，S5 给一个字符串 ""psf""）","无（本行是并组产物：module_adapters.cpp:1872 的 ""wcs"" 是 p1_dag_stage_index 的节点名数组元素，被 value 形误标）",无案（伪分叉）。可登记两条真实形态：①CLI 明确「json==nullptr ⇒ 不写模板」的理由是「模板不替用户填任何天测值，伪造 WCS 会静默污染真实数据」——这是全表里唯一把「不写默认」写成安全决策的一条，值得进设计说明而不被任何一致性门判成缺失；②wcs.init_source 的合同 default 缺失而 --help 声明「缺省 header_pointing」⇒ 默认只活在散文与 enum 顺序里。,"保守方向＝给 init_source 补合同 default=""header_pointing""（其 enum 首元即该值），使「缺省取哪个」成为机读事实。影响面＝IPV 解算入口选择（header_pointing vs config vs neighbor_crval）。",新暴露（判为伪分叉，附带 wcs.init_source 的默认只在散文一条）。,"S1 无此项｜S2 normalize 模板 blocks[].wcs={gaia_data_dir:""path/to/gaia""}｜S3 CLI 骨架同形＋wcs.init_source json==nullptr｜S4 phase_config_normalize#/wcs_config/init_source enum=[header_pointing,config,neighbor_crval]（无 default）｜S5 ""psf""×1（误标）｜S6 无行"
⑤仅跨侧异值（含同名异义嫌疑）,width_px,export 画幅宽（px）,"T1（模板/骨架 1024 对生产缺省 0；合同 [1,20000]）",无（与 height_px/scale_deg_per_px 同因：模板面在门内零键、读取点接收者名不在白名单）,S3（三键同案：0 为未设哨兵，两处具名校验一处委托 p3_wcs_make，方向保守）,保守方向＝width_px/height_px/scale_deg_per_px 三键合并一处定义「未设 ⇒ 具名报错」，模板 1024×1024 标注为示例画幅（并连带标注 crpix_px 的 512.5 由它派生）。影响面＝导出画幅、CRPIX 派生与 tile 规划。,新暴露。,"S1 无此项｜S2 export 模板 1024｜S3 CLI 骨架 ""1024""｜S4 phase_config_export#/export_wcs/width_px minimum=1;maximum=20000｜S5 0×8（p3_session.cpp:110/:153、module_adapters.cpp:13728/:13804、p3_proj_v6.h:133、p3_projection.h:78、p3_wcs.h:20、plan_estimator.h:93）｜S6 无行"

---

## 附：分叉行机械读数来源

- 全量表：`独立审计/证据/D4-多侧默认值全表.csv`（脚本 `extract_defaults.py`，含 pixfrac 正例控制）
- 分叉筛：`独立审计/复算件/prescan2/D4-分叉行筛选.csv`（脚本 `filter_divergent.py`）
- S5 全位点：`独立审计/复算件/prescan2/side5_位点.log`
- 逐条记录：`独立审计/复算件/prescan2/records.tsv`
- T5 可导出却硬编码候选：`独立审计/复算件/prescan2/T5_可导出却硬编码_候选.csv`

<!-- PROGRESS: 已判分叉行 43 / 分叉行总数 43 -->
