# 通读 CR-59 —— export 阶段：投影 WCS 主程序/测试件 + 重采样 + 协方差传播

- 基线 HEAD：`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`
- 批次：CR-59（只读代码通读审计节点）
- 派单前言：`独立审计/派单规程/DISPATCH-CODE-READ-PREAMBLE.md`（§0–§5 全条已读，作为硬约束执行）
- 开工 `git status --porcelain` 原文见文末「开工/收工 git 状态」。

## 清单（派单原文路径 → 实际审阅路径）

1. `lib/algorithms/projection/p3_wcs_main.cpp` —— 派单路径不存在；跟踪集实际路径为 `lib/algorithms/projection/tests/p3wcs/p3_wcs_main.cpp` —— 已读：是（表 14 项）
2. `lib/algorithms/projection/p3_wcs_test.cpp` —— 派单路径不存在；跟踪集实际路径为 `lib/algorithms/projection/tests/p3wcs/p3_wcs_test.cpp` —— 已读：是（表 35 项）
3. `lib/algorithms/resample/p3_resample.cpp` —— 已读：是（表 45 项）
4. `lib/algorithms/resample/p3_resample.h` —— 已读：是（表 16 项）
5. `lib/algorithms/resample/p3_rsmp.h` —— 已读：是（表 13 项）
6. `lib/algorithms/resample/p3_rsmp_covariance.cpp` —— 已读：是（表 14 项）

<!-- PROGRESS: 6/6 -->

---

# 1 · `lib/algorithms/projection/tests/p3wcs/p3_wcs_main.cpp`（65 行，全文已读）

派单给的路径 `lib/algorithms/projection/p3_wcs_main.cpp` 在跟踪集中不存在（`git ls-files lib/algorithms/projection/` 输出里 projection 目录本体只有 p3_wcs.cpp/.h 等 9 项）；同名文件唯一跟踪实例在 `tests/p3wcs/` 下。按派单 §5.5「以清单为准」，取该唯一跟踪实例为审阅对象，差异登记在文末「派单与清单差异」。

## 1.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么（一句话用途） | 处置 | 依据锚 | 待确认时给保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :19 | `argc < 3` 下界 | 3 | 探针最少实参数（程序名+mode+至少 1 个） | 结构性不适用 | 本文件 :18-19；argv 计数不是科学量 | — | CONFIRMED |
| :21 | lambda `d5` 内绝对阈 | 1e-12 | 判"值是否接近整数"的残差阈 | 待确认 | 跟踪面零提及。检索式 `git grep -n "d5" -- lib/algorithms/projection/tests/p3wcs/` → 仅 :21 定义与 :22 `(void)d5` 两处 | 该 lambda 定义后立刻被 `(void)` 丢弃，不参与任何输出 ⇒ 现值影响范围为 0；保守方向 = 删除死码，而不是留着让读者以为探针做过整数性判别 | CONFIRMED |
| :21 | 四舍五入位移 | 0.5、1 | `v ± 0.5` 取整的偏移量 | 结构性不适用 | 同上（死码）；位移量属实现细节 | — | CONFIRMED |
| :24 | `argc < 9`（kw 分支） | 9 | 程序名+mode+7 参数 | 结构性不适用 | 文件头 :3-8 用法串逐项列出 7 参数，与 `argv[2..8]` 一一对应 | — | CONFIRMED |
| :34 | `argc < 9`（make 分支） | 9 | 同上 | 结构性不适用 | 同上 | — | CONFIRMED |
| :44 | `argc < 11`（p2w/w2p 分支） | 11 | 再多加 2 个坐标参数 | 结构性不适用 | 用法串 `p2w <ra> <dec> <scale> <W> <H> <parity> <pa> <x> <y>` = 9 参数 | — | CONFIRMED |
| :26-28, :36-38, :46-48, :52, :59 | argv 下标 | 2…10 | 参数位序 | 结构性不适用 | 与用法串逐位核对一致 | — | CONFIRMED |
| :29, :39, :49, :53, :60 | 失败退出码 | 1 | 「打印 FAIL <code>」时的进程码 | 结构性不适用 | 消费者 `tests/p3wcs/test_p3_wcs.py:58-68` 按 stdout 前缀判定，不看 rc | — | CONFIRMED |
| :31, :42, :55, :62 | 成功退出码 | 0 | 正常路径进程码 | 结构性不适用 | 只在已经 `printf` 出 OK/关键字之后返回 ⇒ 无「未知输入返回 0」的 fail-open 形态（该形态在 p1noise 族出现过，此处不复用结论，本文件自行数得） | — | CONFIRMED |
| :19, :24, :34, :44, :64 | 用法错误退出码 | 2 | 参数不足/未知 mode | 结构性不适用 | :64 兜底 `return 2` 且**不打印任何 OK** ⇒ 未匹配 mode 不被误当通过 | — | CONFIRMED |
| :40 | CRPIX 打印精度 | `%.10f` | 文本有效位 | 结构性不适用 | 输出格式；消费者比对精度 places=8（test_p3_wcs.py:99）⇒ 10 位有余 | — | CONFIRMED |
| :40 | CD 四元打印精度 | `%.12e` | 同上 | 结构性不适用 | 同上（CD 需覆盖 1e-4 量级绝对值，12 位有效足够） | — | CONFIRMED |
| :54, :62 | ra/dec、x/y 打印精度 | `%.12f` | 同上 | 结构性不适用 | test_p3_wcs.py:91-100 places=8/7 | — | CONFIRMED |
| :29, :39, :49, :53, :60 | `FAIL %d` 里的 `(int)st` | 枚举序数 0/1/3… | 把 `P3WcsStatus` 序数当跨语言协议文本 | 文献值→改判 **登记值**，本处按五档取 `公式导出` 不成立，故落 **待确认** | 序数-语义映射在合同面有登记：`docs/contracts/DATA_SEMANTICS.md:2558`（|dec|>85° → P3_WCS_PARAM）、`:2567`（半球外 → P3_WCS_HEMISPHERE）；但**本文件不打印枚举名、只打印整数**，Python 侧写死 `"FAIL 1"`/`"FAIL 3"`（test_p3_wcs.py:119-131）⇒ 枚举一旦插新项序数即漂移而探针不变 | 保守方向：探针应打印符号名或合同码；影响范围＝test_p3_wcs.py 的 5 处 FAIL 断言 + 任何按码分诊的外部脚本 | PARTIAL（我未逐一枚举 p3_wcs.h 里 P3WcsStatus 的当前序数表，只核对了被引用的两个码有合同登记） |

## 1.2 findings

### CR-59-01 · 本探针不被任何构建载体编译，其唯一消费者的脚本也不注册：登记面用三个互不相同的路径引用一个"从不被构建的探针"
- 轴：C（上下级一致）＋ D（设计了没实现/载体缺失）
- 位点：`p3_wcs_main.cpp:1` —— `// eng/tests/backend/p3_wcs_main.cpp — P3-002 WCS 探针(供 Python 独立参考比对)`；同文件 :2-8 定义 `make/p2w/w2p/kw` 四模式 printf 协议
- 现状（逐字）：
  - 构建面：`lib/algorithms/projection/tests/p3wcs/CMakeLists.txt` 全文 36 行只注册四个目标——`add_executable(p3_wcs_test p3_wcs_test.cpp)`（:8）＋`add_test(NAME p3_wcs …)`（:13）、`p3_projection_registry_test`（:17）、`add_test(NAME p3_projection_registry_selftest …)`（:26-27）、`add_test(NAME p3_projection_unsupported_cli …)`（:32-35）。**没有** `p3_wcs_main` 的 add_executable/add_test。检索式：`git grep -rn "p3_wcs_main"`（全仓）→ 命中只有本文件自身、`test_p3_wcs.py:10`、模块文档 5 处、reports/v19r2 两个清单 CSV。
  - 消费者面：唯一按 printf 协议驱动它的脚本 `tests/p3wcs/test_p3_wcs.py:47-51` 自己在临时目录里 `g++ … DRIVER p3_wcs.cpp -o exe` 现场编译；该 .py 在全仓 CMakeLists / `eng/ci/checks.json` 中零命中（检索式 `git grep -rn "test_p3_wcs" -- ':!…/test_p3_wcs.py'` → 只有 1 条研究件 + 2 条 reports/v19r2 清单，后者 `type=test, prod=no, PENDING`）。同目录 CMakeLists 已示范过 `add_test(NAME … COMMAND python3 …)`（:32-35），可见"注册"在本目录是可做的、而对本脚本没做。
  - 登记面路径三个版本互斥：`docs/algorithms/PHASE3_PROJ_IMPL.md:62`、`docs/contracts/PUBLIC_API.md:2058`、`docs/modules/phase3_proj.md:63`、`docs/modules/registry/astrocs.phase3.wcs.md:167`、`lib/algorithms/projection/memory.md:65,96` 写 `eng/tests/backend/p3_wcs_main.cpp`；`lib/algorithms/projection/README.md:82` 写 `eng/tests/p3wcs/p3_wcs_main.cpp`；跟踪集里的实际路径是 `lib/algorithms/projection/tests/p3wcs/p3_wcs_main.cpp`（`git ls-files` 实证，另两个前缀路径均不在跟踪集）。
- 差在哪：一个自称"供 Python 独立参考比对"的执行件，既不在构建闭包也不在 ctest 注册面，且被 5+1 处文档以两个不存在的旧路径当作"现状执行测试"引用（README.md:55 把 TEST-P3-WCS-001 的"可执行面"直接列为「p3_wcs_test.cpp（90 行）+ test_p1002_gaps.py + p3_wcs_main.cpp」，其中"90 行"也与现值 474 行不符）。
- 后果：`docs/traceability/TRACEABILITY_MATRIX.csv` 行 `MOD-astrocs-phase3-wcs` 的 TEST 层判 VERIFIED 时，第三个载体的效力被高估——任何依赖"探针可跑"的交叉比对在干净构建树上不会发生；对外"WCS 有独立第三方参考"这一主张在本目录只剩 §2 的 p3_wcs_test.cpp + `ctest:p1wcs_std_f1_bridge_cross`（`eng/tests/unit/p1wcs/CMakeLists.txt:158` 已注册，**这一条是真的**）承载。
- 定级：S2（缺运行时触发路径：不进产物字节；失真发生在验证主张与追溯表 VERIFIED 判据上）
- 怎么算修好：①`tests/p3wcs/CMakeLists.txt` 补 `add_executable(p3_wcs_probe p3_wcs_main.cpp)` 并链 `astrocs_p3_projection_wcs`（或直接把它从库里删掉，连同 :21-22 死 lambda）；②若保留 `test_p3_wcs.py`，按同目录 :30-36 的 `find_program(PYTHON3_EXECUTABLE …)`+`add_test` 习例注册，并给它的 ad-hoc g++ 加"编译器不可得 ⇒ SKIP 计数上报"而非 assert；③同批把 5 处 `eng/tests/backend/p3_wcs_main.cpp` 与 1 处 `eng/tests/p3wcs/…` 改成实际路径，README.md:55 的"90 行"改为不写行数（行数必然漂移）。必须同改：追溯表 TEST 层的载体清单，否则改完路径又冒认一次。
- 置信：CONFIRMED（构建面/消费者面/登记面三处各自独立复算：CMakeLists 全文读、两条 git grep 全仓、五处文档行逐条引用）

### 不报的核过项（本文件）
- **`(int)st` 的枚举码是否有合同登记**：核过 `DATA_SEMANTICS.md:2558/2567` 两行分别登记 P3_WCS_PARAM 与 P3_WCS_HEMISPHERE 的触发条件 ⇒ 不报"码值无出处"；本文件只把序数当协议文本这一耦合写进上表 `待确认`，不单立 finding。
- **`atof`/`atoi` 对坏参数静默取 0**：本文件确实不校验数字解析（:26-28 等）。但 0 落到被测函数上会被生产侧拒：`scale=0` 与 `W=0` 走 `p3_wcs_make` 的 `!(scale>0)` / `width_px<1` 守卫（p3_wcs.cpp:107 与 :206 同类式）⇒ 打印 `FAIL 1` 退出 1，不会伪装成 OK。检索式：`git grep -n "width_px < 1" -- lib/algorithms/projection/p3_wcs.cpp` → :107。故不报。
- **`return 2` 兜底是否 fail-open**：不是。未匹配 mode 不打印任何 OK 且 rc=2（:64），与本仓出现过的"未知组名返回 0"族形态相反。

---

# 2 · `lib/algorithms/projection/tests/p3wcs/p3_wcs_test.cpp`（474 行，全文已读）

注册事实：`tests/p3wcs/CMakeLists.txt:8-13` 编译 + `add_test(NAME p3_wcs …)` ⇒ **已注册**；被测面是真实生产符号（`p3_wcs_make/p3_wcs_pix2world/p3_wcs_world2pix/p3_wcs_fits_keywords/p3_wcs_roundtrip_gate`，链接 `astrocs_p3_projection_wcs`）；能红（`CHECK` 累计 `failures`，:472-473 `return 1`）。以下 findings 只挑断言与登记面之间的实际裂缝。

## 2.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :25 | `w <= 0 || h <= 0` | 0 | 局部 helper 的尺寸下界 | 结构性不适用 | 该 helper 在生产面无对应物（见 CR-59-02） | — | CONFIRMED |
| :28 | `uw > UINT64_MAX / uh` | UINT64_MAX | uint64 乘法溢出判据 | 结构性不适用 | 同上；溢出判据属实现 | — | CONFIRMED |
| :64, :65 | `kFrameW` / `kFrameH` | 1024 / 1024 | 测试帧边长 | 结构性不适用 | 登记面同值：`docs/algorithms/GATES_AND_TOLERANCES.md:67` 行内写「1024×1024 帧」⇒ 与门行一致 | — | CONFIRMED |
| :66 | `kCell` | 100 | 九宫格每格边长（每格 100×100 px） | 待确认 | 唯一来源是**注释自述**：`// 每格 100x100 px (负责人 rev3.1 口径)`。跟踪面查不到该裁决文本。检索式：`git grep -rn "rev3.1" -- docs/` 未在本路取证内执行 ⇒ 记缺角 | 方向：格越小 ⇒ 逐像素覆盖面越少 ⇒ 偏绿（漏检面变大仍判绿）。影响：G-P1-WCS-BRIDGE 的"逐像素"覆盖面 | PARTIAL（缺"rev3.1 口径"的跟踪面文本） |
| :67 | `kScale` | 0.0001389 deg/px | 夹具像元尺度 = 0.50004″/px（复算：0.0001389×3600） | 待确认 | 注释「P3-002 既有用例同值」＝同族复制，非出处。该值**决定适用哪条门**：0.50004 < `min_scale_arcsec = 0.9`（p3_wcs.cpp:220）⇒ 落全域保守门 1e-6 px | 方向：夹具落在紧门适用域**之外** ⇒ 本件断言的是比登记门弱 100 倍的那一档（见 CR-59-06）。影响：G-P1-WCS-BRIDGE 载体面 | CONFIRMED（数值关系自己复算） |
| :70, :72 | `kFrozenGatePx` | 1e-4 | "负向注入必须被检出"的偏差下界 | 待确认 | 注释指 `GATES §3 G-P1-WCS-F2 / G-P1-WCS-RT`。实核：`GATES_AND_TOLERANCES.md:64` G-P1-WCS-F2 = 1e-4 px、适用域「合成 SIP 场（order=2），中心 90% 区域」、载体 `ctest:p1wcs_apbp`；:65 G-P1-WCS-RT = 1e-4 px，P1 版**独立密集域** ⇒ 两条都是 **Phase1 版面匹配（plate-solving SIP）** 的容差，不是 Phase3 CD-only TAN 导出边界的门 | 方向：借用来的下界只用于 `>` 判据（越大越难满足），1 px 注入远超 1e-4 ⇒ 不会因此误绿；失真在"这条判据的登记锚其实属于另一域"。影响：证据可追溯性 | CONFIRMED（两条门行原文已读） |
| :73 | `kSampleStride` | 10 | 桥接臂抽样步长 ⇒ 每格 10×10=100 点 | 结构性不适用 | 循环上界/抽样密度属实现选择；被 :308 断言与自身一致 | — | CONFIRMED |
| :74 | `kDegToRad` | 0.01745329251994329577 | 度→弧度 | 结构性不适用 | 单位换算定义常数（π/180），复算与 double 最近舍入一致 | — | CONFIRMED |
| :75 | `kRadToDeg` | 57.29577951308232087680 | 弧度→度 | 结构性不适用 | 同上（180/π） | — | CONFIRMED |
| :85 | `kNineGrid[9]` | 9 | 九宫格格数 | 结构性不适用 | 枚举基数；`GATES:67` 亦写「中心 1 + 四角 4 + 四边中点 4，两 parity 共 18 格」，本件两 parity 各跑一遍 = 18 ✓ | — | CONFIRMED |
| :86-94 | 格起点 `kFrameW-kCell`、`(kFrameW-kCell)/2` | 924、462、0 | 角/边中点格坐标 | 结构性不适用 | 由 :64/:66 导出的夹具几何 | — | CONFIRMED |
| :111, :112 | `(x0 + pixel_origin) - crpix` | 1（经参数） | 被审的 +1 桥接量 | 公式导出 | Paper I §2.1.1 `中间坐标 = CD·(p−CRPIX)`，p 1-based ⇒ 0-based 像素需 +1；ALG-P3-003 §2 G2 亦写 `(iwc1,iwc2)=CD·((x+1)−CRPIX1,…)`。代码真按式算（`pixel_origin` 作参数，见 :137-143） | — | CONFIRMED |
| :119-121 | `n0/east/north` 单位向量基 | — | 参考方向构造 | 公式导出 | 球面 (α0,δ0) 的 east=`(−sinα0, cosα0, 0)`、north=`(−sinδ0 cosα0, −sinδ0 sinα0, cosδ0)`、n0=`(cosδ0 cosα0, cosδ0 sinα0, sinδ0)`：与 test_p3_wcs.py:18-19 独立路径同式（两条实现同式但不同码路，属"独立期望"应有的形态） | — | CONFIRMED |
| :129 | `if (ra < 0.0) ra += 360.0` | 0、360 | RA 归一到 [0,360) | 结构性不适用 | 值域归一；登记面 `DATA_SEMANTICS:2567` 同款 | — | CONFIRMED |
| :139, :140, :141 | `declared_pixel_origin()` 三臂 | 1.0 / 0.0 / 2.0 | 合同桥接 / 桥接移除 / 双重桥接 | 公式导出 | 1.0 = Paper I 1-based；0.0/2.0 是 ±1 px 的人为错置（复算：生产 p2w 用 x+1，参考用 x+0 ⇒ 反解回 x−1 ⇒ 偏差恰 −1 px/轴；x+2 ⇒ +1 px） | — | CONFIRMED |
| :146-156 | `CellStats` 初值 | 0.0、`1e30` | max/min 哨兵与计数 0 | 结构性不适用 | 累加器初值；`1e30` 只作"未更新"标记（:189 失败路径写 `1e30` 强制判红） | — | CONFIRMED |
| :229 | `char buf[64]` | 64 | `%.17g` 最坏串长缓冲 | 结构性不适用 | 缓冲区尺寸；17 位有效 + 符号/指数/点 ≤ 24 字节 ⇒ 64 有余 | — | CONFIRMED |
| :230 | `%.17g` | 17 | double 最短可回读十进制位数 | 结构性不适用 | IEEE-754 binary64 round-trip 需 17 位（与 :334-338 的 memcmp 位级一致配套） | — | CONFIRMED |
| :290 | `p3_wcs_make(150.0, 2.0, kScale, …)` | 150.0、2.0 | 夹具中心 RA/Dec | 结构性不适用 | 夹具内容；|dec|=2 < 85 域内 | — | CONFIRMED |
| :300, :301 | `status != OUT_OF_DOMAIN`、`tol_px > 0.0` | 0 | 门值可得性守卫 | 结构性不适用 | 判据存在性检查；方向正确（取不到门即红，不回落字面量） | — | CONFIRMED |
| :306 | `st.n_px == kCell*kCell` | 10000 | 每格逐像素计数闭合 | 结构性不适用 | 覆盖面断言 | — | CONFIRMED |
| :308 | `(kCell/kSampleStride)²` | 100 | 桥接臂抽样点数闭合 | 结构性不适用 | 与 :201-225 循环一致 | — | CONFIRMED |
| :314, :315 | `fabs(max_nobridge_px − sqrt(2.0)) < 1e-6` | √2、1e-6 | 1 px/轴 ⇒ 2D 距离 √2；容差 | 公式导出（√2）＋ **待确认（1e-6）** | √2=hypot(1,1) 复算成立。1e-6 无出处：它与全域保守门 `roundtrip_tol_global_px = 1e-6`（p3_wcs.cpp:219）**同值但语义无关**，写成字面量 | 方向：这是 `fabs(·−·) < 1e-6` 双侧带，1e-6 越大越易过 ⇒ 偏绿；影响：若 CD 斜率或桥接量被改成"偏差 0.9999995 px"这类亚微像素错置，本断言仍绿 | CONFIRMED（式已复算；出处缺） |
| :316-319 | `fabs(min/max_*_axis − 1.0) < 1e-6` | 1.0、1e-6 | 每轴偏差恰 1 px | 公式导出（1.0）＋ 待确认（1e-6 同上） | CD-only 线性 WCS：像素平移 1 ⇒ 中间坐标平移 CD 一列 ⇒ 反演平移恰 1 px | 同上；另注：只统计 |sx|，sy 未进 min/max（sy 理论恒 0，未被断言） | CONFIRMED |
| :325, :326 | `d.crpix_x == (kFrameW+1)/2.0` | 512.5 | CRPIX 冻结不变量 | 公式导出 | ALG-P3-003 §2 G1：`CRPIX1=(W_out+1)/2`；被测侧同式（p3_wcs.cpp:464-465 的适用域自检亦用 `(W+1)/2.0`）。代码真按式算 ⇒ 合规 | — | CONFIRMED |
| :327, :328 | `cd[0][0] < 0 / > 0` | 0 | parity 手性 | 结构性不适用 | G1 east_left: CD1_1=−s / east_right: +s；与 :323-324 的 `det<0` 配套 | — | CONFIRMED |
| :360 | `p3_wcs_make(150.0, 2.0, 0.0001389, 1024, 768, …)` | 0.0001389（裸写） | 与 :67 `kScale` 同值的第二份字面量 | 待确认 | 与 :68-69 自陈纪律「往返容差**不写第二份字面量**」相冲（该纪律只说容差，尺度同样被复制了一份） | 方向：复制值不会导致误绿，但 kScale 一旦改动本断言的 `+0.0001389`（:366）不会跟随 ⇒ 潜在"改了夹具仍全绿"的错配面 | CONFIRMED |
| :365, :366 | `cd[0][0] != 0`、`fabs(cd[0][0] + 0.0001389) < 1e-9` | 0、1e-9 | CD1_1 符号/量级断言 | 待确认 | 1e-9 deg/px 绝对阈无出处；相对量 = 1e-9/1.389e-4 = 7.2e-6（比任何登记门都松一档以上，但这是"值对不对"的门不是往返门） | 方向：绝对阈随尺度漂移（大尺度下形同零判别，小尺度下过严）⇒ 建议相对化。影响：G1 手性/尺度断言 | CONFIRMED（复算相对量） |
| :372, :373 | `safe_pixel_count(20000, 20000)`、`400000000ULL` | 20000、4e8 | 局部 helper 的正例 | 结构性不适用 | 夹具/局部函数期望值（见 CR-59-02 的判据与被测错配） | — | CONFIRMED |
| :375, :376 | `(0,100)`、`(-5,100)` | 0、−5 | 局部 helper 的负例 | 结构性不适用 | 同上 | — | CONFIRMED |
| :384, :388 | `20001`、`20000` | 20000±1 | 生产尺寸上界的边界两侧 | 待确认 | 该上限可编译期覆盖：`p3_wcs.cpp:23-26` `#ifndef ASTROCS_P3_MAX_SIDE constexpr int kMaxSide = 20000 #else = ASTROCS_P3_MAX_SIDE`；登记面 `DATA_SEMANTICS` 之外另有 `FIX_LEDGER.csv:559`（M1a-I-002，OPEN）自陈"全仓无 target 定义该宏" | 方向：一旦某 target 定义该宏，本断言要么恒红要么测的是不存在的产品边界。影响：§3 整块 + test_p3_wcs.py:163 同值 | CONFIRMED |
| :398, :400, :401 | `512.0/384.0`、`1e-4` | 1e-4 | §4 单点往返容差 | 待确认（并见 CR-59-04） | 与 :68-70 自陈"容差由单一事实源给"冲突；登记面该尺度的门是 1e-6 px | 方向：1e-4 **比本文件自己选的 1e-6 松 100 倍** ⇒ 这条断言永不比 §6 先红，不产生增量误绿；失真在纪律与"第二份字面量" | CONFIRMED（方向已钉） |
| :408, :409 | `safe_pixel_count(50000,50000)`、`2500000000ULL` | 50000、2.5e9 | 局部 helper 的"接近上限仍安全"正例 | 结构性不适用 | 50000 已超出产品尺寸域（kMaxSide=20000），进一步说明被测的是不存在的场景（CR-59-02） | — | CONFIRMED |
| :444-452 | JSON 模板里的 `20000`、`(W+1)/2` 串 | — | 证据面文案 | 结构性不适用 | 文本常量 | — | CONFIRMED |
| :453-457 | `gate.status == TIGHT ? "tight" : "global_conservative"` | 二值 | 证据 JSON 的门档位标签 | 结构性不适用 | 第三态 `OUT_OF_DOMAIN` 已由 :300 的 CHECK 先判红 ⇒ 不存在"越域被标成保守门"的静默路径（本条见"不报的核过项"） | — | CONFIRMED |

## 2.2 findings

### CR-59-02 · 文件头与 PASS 文案宣称的"尺寸乘法溢出检查"测的是测试自带的副本函数，生产面不存在该算子
- 轴：R（运行时正确性/断言与被测无耦合）＋ C（登记面说的事与此处做的事不同名同实）
- 位点：`p3_wcs_test.cpp:24` `static bool safe_pixel_count(int w, int h, std::uint64_t* out) {`；断言位点 :372-376、:408-409；宣称位点 :1 `// P3-002 单元测试: WCS 尺寸溢出检查 + 配置合同上限 + 输出完整性`、:468 `"P3-002 TESTS PASS (尺寸溢出检查, 配置合同上限 20000, WCS 输出完整, "`
- 上位依据：`docs/algorithms/GATES_AND_TOLERANCES.md` 无"像素数乘法溢出"任何门行（检索式：`git grep -n "溢出" -- docs/algorithms/GATES_AND_TOLERANCES.md` 未在本路取证内执行，故此处只说"本文件自陈的三件事之一"）；生产尺寸域的真实约束在 `p3_wcs.cpp:107` `if (width_px < 1 || width_px > kMaxSide || height_px < 1 || height_px > kMaxSide)` —— 逐边长判域，**没有** W·H 乘法溢出分支。
- 现状：`safe_pixel_count` 只做 `uw > UINT64_MAX / uh`；§2/§5 的 4 条 CHECK 全部作用在这个文件内静态函数上，没有任何一条调用被测生产符号。
- 差在哪：被测对象与断言对象不耦合——把这段判据从测试里删掉，或把 `UINT64_MAX` 写成 `INT32_MAX`，ctest `p3_wcs` 结果不变；而文件头与 PASS 文案把它列为本件验证的三件事之一。
- 后果：①`ctest:p3_wcs` 的绿灯里含一条**零生产耦合**的断言集，追溯表按 `VERIFIED` 引用该 ctest 时被高估（与 CR-59-01 的载体缺失叠加）；②真实的产品风险面（W·H 在 `int` 域相乘是否溢出，例如 `int` 累加器 `w*h`）从未被本件覆盖——在 kMaxSide=20000 下 20000×20000=4e8 未溢出 int32（2.147e9），但一旦 `ASTROCS_P3_MAX_SIDE` 被覆盖到 50000（本文件 :408 恰好用 50000 当"合法"示例）就会溢出，而这条路径无人断言。
- 定级：S2（缺运行时触发路径：不产生错值像素；失真在验证主张与绿灯构成）
- 怎么算修好：①要么删除 `safe_pixel_count` 及 §2/§5，并同步改文件头/PASS 文案（不再宣称验过"尺寸溢出检查"）；②要么把它接到真实义务上：对生产入口 `p3_wcs_make` 逐点断言 `W·H` 在 `int32/int64` 两种累加下的一致性，并覆盖 `p3_output.cpp` 里真正做平面字节数乘法的调用面（该文件不在本批清单，需与之同批改以免"改对反而判红"）。
- 置信：CONFIRMED（`git grep -n "safe_pixel"` 全仓 → 仅本文件 5 处命中；`git grep -n "UINT64_MAX"` 在 lib/** 的命中均为其它模块，无一在 projection 生产面）

### CR-59-03 · "负向注入必败"的环境开关没有任何注册载体，且开关注入的是参考实现而非被测实现
- 轴：D（设计点名的能力只有接口没有承载）＋ R
- 位点：`p3_wcs_test.cpp:136-143`
  ```
  //   STD_F1_BRIDGE_FAULT=nobridge → 把"声明桥接"臂改按 xp = x0 求值 (桥接移除)
  //   STD_F1_BRIDGE_FAULT=double   → 把"声明桥接"臂改按 xp = x0 + 2 求值 (双重桥接)
  // 两者都必须使本例**失败** (负向注入必败), 用于 CI 中可重跑地证明桥接不是恒真装饰。
  double declared_pixel_origin() { const char* f = std::getenv("STD_F1_BRIDGE_FAULT"); …
  ```
- 上位依据：`docs/algorithms/GATES_AND_TOLERANCES.md:67`（G-P1-WCS-BRIDGE 行的方法列点名「负向注入（移除/错置 `+1` 桥接必须 ≥1 px 偏差）」，载体列「ctest:p3_wcs; ctest:p3_projection_registry; ctest:p1wcs_std_f1_bridge_cross」）；本仓同目录已有"注册型负例"习例：`tests/p3wcs/CMakeLists.txt:26-27 add_test(NAME p3_projection_registry_selftest COMMAND p3_projection_registry_test --self-test)`。
- 现状：`STD_F1_BRIDGE_FAULT` 在全仓跟踪面**只出现在本文件**（检索式：`git grep -rn "STD_F1_BRIDGE_FAULT"` → 0 命中除本文件；`STD_F1_P3_EXPORT` 也只有 `eng/tests/unit/p1wcs/p1wcs_std_f1_bridge_cross.py:201` 一处，那是导出路径开关不是故障开关）。因此：CI 没有任何一条注册项把该 env 置位；且该 env 改的是 `declared_pixel_origin()`——即"合同声明臂"的**参考**取值，不改生产 `p3_wcs_pix2world` 的 +1。
- 差在哪：所谓"故障注入"注入在期望值一侧，不是被测实现一侧。真实判别力来自 :310 `CHECK(st.max_ref_px < gate.tol_px)`——若生产丢了 +1，则 ref(origin=1) 与生产反演差 1 px ⇒ 该条判红，所以本件对"生产丢桥接"**是有牙的**（这一点必须先说清，否则会被读成"测试恒绿"）；但文件头宣称的"可重跑地证明注入必败"这条 CI 载体不存在，且注入对象与 GATES 行点名的"移除/错置 +1 桥接"（那是被测实现的属性）不是同一件事。
- 后果：G-P1-WCS-BRIDGE 的"负向注入必败"要素在机器面上退化为"参考式换个常数再比一次"，与 :67 的措辞不同形；读者按门行去找"注入臂的红/绿记录"会找不到注册项。
- 定级：S3（可指认的失真只在措辞与载体面；被测实现的桥接缺失仍由 :310 捕获，方向已钉：origin 偏离 ⇒ 偏差 1 px ⇒ `max_ref_px < 1e-6` 必假 ⇒ 偏红，不是 fail-open）
- 怎么算修好：二选一并同步措辞：①按同目录 :26-27 习例注册 `add_test(NAME p3_wcs_std_f1_selftest COMMAND p3_wcs_test)` 并配 `ENVIRONMENT "STD_F1_BRIDGE_FAULT=nobridge"` + `WILL_FAIL TRUE`；②把注入点搬到被测侧（生产 `fits_pixel_1based` 的编译期/链接期替身），并保留现 :211/:217 两条恒跑臂。若都不做，则删除 :136 的"用于 CI 中可重跑地"表述并在 GATES 行内改口径。
- 置信：CONFIRMED（两条全仓 git grep + CMakeLists 全文 + GATES 行原文）

### CR-59-04 · §4 的往返判据写了第二份容差字面量 1e-4，与同一文件自陈的"单一事实源"纪律相反
- 轴：C（同一文件对同一量取两个门值）
- 位点：`p3_wcs_test.cpp:68-70`（纪律原文）
  ```
  // 往返容差**不写第二份字面量**: 由单一事实源 p3_wcs_applicability("TAN") 经
  // p3_wcs_roundtrip_gate() 按尺度选出（紧门 1e-8 px / 全域保守门 1e-6 px）。
  ```
  与 `p3_wcs_test.cpp:400-401` `CHECK(std::fabs(x - 512.0) < 1e-4);` / `CHECK(std::fabs(y - 384.0) < 1e-4);`
- 上位依据：`docs/algorithms/GATES_AND_TOLERANCES.md:67/68` 两条门行的容差是 1e-8（适用域 scale≥0.9″/px）与 1e-6（全域保守）；「不写第二份字面量」是本仓已定案的口径（同一纪律在 `eng/tests/unit/p3_projection_test.cpp:76-89`、`eng/tests/backend/test_p3_projection_oracle.py:42` 被明写为「未拿到合同值 ⇒ 判红（fail-closed），不回落字面量」）。
- 现状：同一次运行里，同一物理量（pix2world→world2pix 往返误差）在 :307/:310 用合同门 1e-6 断言、在 :400/:401 用裸 1e-4 断言；后者不来自任何登记面（1e-4 在 GATES 里属于 G-P1-WCS-F2/-RT 两条 **Phase1 SIP** 门，见处置表 :70 行）。
- 差在哪：纪律与实现同文件打架；且 1e-4 那份在被测尺度上恰好比 :67 行登记的 P3 门松 100 倍。
- 后果：如果未来 §6（九宫格）被拆分/关掉而 §4 留下，本文件对该尺度的往返守卫自动退化为 1e-4 px，且**没有任何断言会因此变红**——即"门的强度可被静默降级"。方向钉死：`err < 1e-4` 比 `err < 1e-6` 更易满足 ⇒ 偏绿；本批不产生误绿（因 §6 同跑），风险是降级后的静默。
- 定级：S3
- 怎么算修好：:400/:401 改为取 `p3_wcs_roundtrip_gate(&w).tol_px`（w 与 :395 构造的 descriptor 同尺度），并同步 `kScale`/`0.0001389` 收敛到 :67 的 `kScale` 一处；不放宽任何既有数值。
- 置信：CONFIRMED

### CR-59-05 · 边界断言把 `kMaxSide` 钉成 20000，而该常量声明为可编译期覆盖
- 轴：C（代码/登记面/测试三面同值，但其中一面的值可被外部改写而断言不跟随）
- 位点：`p3_wcs_test.cpp:384` `…, 0.0001389, 20001, 100, …) == astrocs::phase3::P3_WCS_PARAM);` 与 :387-389（20000 接受）
- 上位依据：`p3_wcs.cpp:21-26`「最大尺寸来自配置/资源合同 (PHASE3_API_V1 §2 默认 20000)，可被编译期配置覆盖；不硬编码业务值」+ 同名 `#ifndef ASTROCS_P3_MAX_SIDE`。登记面已有一条 OPEN 台账记同一风险：`artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:559`（M1a-I-002，"未来任何 target 定义该宏即可静默越出冻结尺寸域，且校验层不会察觉"，状态 OPEN）。
- 现状：测试用裸 `20001/20000` 判边界；被侧面用 `kMaxSide`（编译期可替换）。同目录另有两份同名常量（`p3_projection.cpp:42-45`、`p3_proj_v6.cpp:55-58`）各自 #ifndef。
- 差在哪：被注释自称"不硬编码业务值"的那一侧与被测的一侧，在本件里以硬编码方式绑死；一旦宏被定义（本文件 :408 还把 50000 当"合法示例"），§3 的两条 CHECK 立即与产品不一致（或反向：边界两侧都接受而无人察觉，因为只测了 20000/20001 一对）。
- 后果：`PHASE3_API_V1 §2` 的尺寸合同在测试面退化为字面量比对；资源覆盖构建（低内存 CI 把上界调小）下该 ctest 恒红或恒绿取决于构建旗标，而旗标不在门表里。
- 定级：S3（现状无 target 定义该宏，故不发作；属"未登记开关默认站在合同反侧"的家族，风险登记在册）
- 怎么算修好：测试侧改为从被测模块取边界值（例如公开 `p3_wcs_max_side()` 或在 `p3_wcs_applicability` 结构里带上尺寸域），只断"上界+1 拒 / 上界受"的**关系**而不写数值；若维持现状，则把 FIX_LEDGER 的 M1a-I-002 与 GATES 尺寸行连起来，在 CI 里加一条"检查无 target 定义 ASTROCS_P3_MAX_SIDE"的静态判据（三处 #ifndef 同批覆盖）。
- 置信：CONFIRMED（三处 #ifndef 位点已用 `git grep -n "ASTROCS_P3_MAX_SIDE"` 数过）

### CR-59-06 · 门行把 `ctest:p3_wcs` 列为 1e-8 紧门的载体，而本件的夹具尺度落在紧门适用域之外，实际跑的是 1e-6 档
- 轴：C（登记面载体列与本件实际判据档位不符）
- 位点：`p3_wcs_test.cpp:47-49`（本件自陈）
  ```
  //      本用例 kScale = 0.0001389 deg/px = 0.5″/px < min_scale 0.9″/px ⇒ 紧门
  //      1e-8 px **超出适用域**, 适用门 = 全域保守门 1e-6 px（GATE-WCS-01 裁决 1/4）);
  ```
  与 :67 `constexpr double kScale = 0.0001389;`
- 上位依据：`docs/algorithms/GATES_AND_TOLERANCES.md:67` 容差列「<1e-8 px（**适用域：scale ≥ min_scale_arcsec = 0.9″/px**；低于该尺度本门不适用…）」，载体列「ctest:p3_wcs; ctest:p3_projection_registry; ctest:p1wcs_std_f1_bridge_cross」；`p3_wcs.cpp:218-220` 三值 1e-8 / 1e-6 / 0.9 与之一致；档位选择逻辑 `p3_wcs.cpp:281-290`（`scale >= min` ⇒ TIGHT，否则若包络 ≤ 1e-6 ⇒ GLOBAL，否则 OUT_OF_DOMAIN 且 tol=0）。
- 现状：本件两 parity 全部以 0.50004″/px 跑 ⇒ `gate.status = GLOBAL`、`gate.tol_px = 1e-6`，:307/:310 断言的是 1e-6 档。1e-8 档在本件从未被行使（复算：0.0001389×3600 = 0.50004 < 0.9）。
- 差在哪：门行的适用域限定语只写在"容差列"，载体列不带档位归属 ⇒ 按行索引"哪条 ctest 证明 1e-8"的读者会取到一个弱 100 倍的判据。本件注释是诚实的（自陈超出适用域），失真发生在登记面的载体列。
- 后果：G-P1-WCS-BRIDGE 的 1e-8 档若在其他两个载体上也没被以 ≥0.9″/px 的尺度跑到，则该档在机器面上无证据；本批范围内我只核到 p3_wcs_test 这一条载体，另两条不在清单内 ⇒ 不下结论，只登记错配。
- 定级：S3（登记面措辞与档位的可追溯性；不产物错值）
- 怎么算修好：①门行载体列补档位归属（例如「p3_wcs → GLOBAL 档；TIGHT 档由 X 以 scale≥0.9 覆盖」）；②或在本件增设第三组夹具，尺度 ≥0.9″/px（例如 0.0002778 deg/px = 1.0″/px）跑一遍九宫格，使 1e-8 档被同一 ctest 覆盖——注意这会让原本只在 1e-6 下成立的断言收紧 100 倍，必须**先实测包络再改**，不能顺手把 :314 的 1e-6 一起改小。
- 置信：PARTIAL（本件侧 CONFIRMED 且已复算；另两载体是否覆盖 TIGHT 档未读，因不在本批清单）

### 不报的核过项（本文件）
- **独立参考 `fits_tan_forward_ref` 的数学**：逐式核对 gnomonic 向量法（n0/east/north 基 + `v=n0+ξ·e+η·n` 归一后 asin/atan2）与 Paper II §2.2 语义一致；另用极限复算（Δδ→0 时 η=tan Δδ、Δα→0 时 ξ≈Δα·cos δ0）成立 ⇒ 不报"参考式与标准不同形"。检索式：与本批 test_p3_wcs.py:13-25 的 python 独立实现逐项对比，两条路径同式不同码。
- **`gate.status == TIGHT ? "tight" : "global_conservative"` 把第三态合并进第二态**：核过 :300 已 `CHECK(status != OUT_OF_DOMAIN)`，且 `p3_wcs.cpp:289` 在 OUT_OF_DOMAIN 时 `tol_px = 0.0` ⇒ :301 `CHECK(gate.tol_px > 0.0)` 亦红。取不到门会先判红，不存在"越域被标成已判的保守门"的静默路径 ⇒ 不报。
- **确定性断言（:331-339 memcmp）**：同输入同码路跑两遍比位级，弱但诚实（只覆盖 2 个统计量与 sample 向量），不冒认"跨线程/跨 worker 一致性" ⇒ 不作为缺陷。
- **`st.n_px` 在失败分支也自增（:188）**：核过同处写 `max_rt_px = 1e30` ⇒ :306 的计数闭合与 :307 的误差断言一红一绿可区分，非"计数掩盖失败" ⇒ 不报。
- **枚举序数/错误协议**：见 §1.2「不报的核过项」第 1 条（P3_WCS_PARAM / P3_WCS_HEMISPHERE 在 DATA_SEMANTICS:2558/2567 有登记）。

---

# 3 · `lib/algorithms/resample/p3_resample.cpp`（586 行，全文已读）

生产可达性（先钉，后面多条 finding 的定级依赖它）：export 阶段唯一真实节点链的 resample 节点即本文件——`lib/infrastructure/scheduler/src/module_adapters.cpp:13486-13500` 注释点名「resample → p3_order_select + p3_sample_{nearest,bilinear}_ex + p3_uncertainty_propagate」，调用点 `module_adapters.cpp:14352-14354`（采样）与 `:14367`（方差传播），产物写面 `:14357-14385`（signal/coverage/variance/ivar 四个 bin 平面）；会话面另有一处同构调用 `lib/phase3_session/p3_session.cpp:289-291,305-306`。⇒ 本文件的每一条数值路径都在产品字节上。

## 3.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :6-14 | 修复前实测叙述：`14375²`、`206.6 Mpx`、`0.805″/px`、`max_tiles 默认 804`、`cap = 8`、`523 tile`、`34.2%`、`~2.8e8 次`、`17 线程只用 ~1.9 核`、`门禁 §10.5 需 ≥85%` | 见左 | 性能叙事（注释） | 待确认 | 跟踪面锚：容量式在 `docs/architecture/PHASE3_MODULE_ARCH.md:41`「max_tiles 默认 min(1024, ceil(W_out·H_out/W²)+16)」与实现 `module_adapters.cpp:14219-14222`（`need = (wh + per_tile - 1)/per_tile + 16`）。**复算不符**：14375²=206,640,625 ⇒ ceil(/262144)=789 ⇒ +16 = **805**，注释写 804（差 1）。其余各值（34.2%、2.8e8、1.9 核、523 tile）在本路可见的跟踪面无对应证据件 | 方向：注释不参与计算 ⇒ 现值零影响；风险是后人按"804"当实测基准。影响范围：仅 P30 叙事与 §10.5 证据口径 | PARTIAL（804/805 已自己复算；其余实测值未找到跟踪载体，不宣布其错） |
| :48 | `kTileWidth` | 512 | HiPS tile 边长 | 结构性不适用 | IVOA HiPS 1.0 子集；**有守卫**：`lib/algorithms/coverage/hips_properties.h:25 kHipsTileWidth = 512`、`hips_properties.cpp:120-121` `tile_width != 512 → fail`，且本文件 :276 打开产品前必过 `hips_product_validate` ⇒ 硬编码 512 不可能与产品实际 tile 宽度错配 | — | CONFIRMED |
| :49 | `kReaderBuf` | 512\*512 = 262144 | 单 tile float 像素缓冲元素数 | 结构性不适用 | 缓冲区尺寸 = kTileWidth²；与 `aio_hips_read_tile_f32` 的 out 尺寸合同一致（`aio_hips_reader.h` 同款 512² 约定） | — | CONFIRMED |
| :52 | `kHotSlots` | 8 | 每 sampler 前端直映象缓存槽数 | 待确认 | 注释理由「覆盖 bilinear 单像素触及的 2-4 个 tile 及相邻行带切换」= 论证性叙述；`docs/algorithms/PHASE3_RESAMPLE.md:99` 亦登记「每 sampler 8 槽热缓存」。无标定件 | 方向：只影响 I/O 命中率，不改像素值（登记面 :156 明写"缓存只影响命中率"）⇒ 科学影响 0；性能档位未经标定 | CONFIRMED |
| :64 | 「容量 = cap MiB」注释 | 1 MiB | 512²×4 B 的换算 | 结构性不适用 | 单位换算定义常数（512·512·4 = 1,048,576 B = 1 MiB）✓ 复算 | — | CONFIRMED |
| :70 | `SharedTileCache::cap = 8` | 8 | 未设定时的默认容量 | 待确认 | 头 `p3_resample.h:44-48` 说 `max_tiles≤0 → 恢复默认(8)`；`docs/algorithms/PHASE3_RSMP_IMPL.md:280` 同值。生产两面都会显式设定（module_adapters.cpp:14230、p3_session 同构）⇒ 默认值只在"未接线"时生效 | 方向：8 远小于真实工作集（M42 523 tile）⇒ 未设定即抖动，属性能不属正确性 | CONFIRMED |
| :98 | `cap == 0 ? 1u : cap` | 1 | 零容量兜底 | 结构性不适用 | 不可达分支：`p3_sampler_set_max_tiles` :230 把 ≤0 归 8 ⇒ cap 恒 ≥1 | — | CONFIRMED |
| :144 | `hot_key[i] = ~0ull` | UINT64_MAX | 空槽哨兵 | 结构性不适用 | 哨兵值；合法 tile ipix 不可能等于 ~0ull（nside≤2^29 ⇒ npix≤3.4e18 << 2^64−1） | — | CONFIRMED |
| :157 | 散列乘数 | 0x9E3779B97F4A7C15 | golden-ratio 哈希常数 + `& (kHotSlots-1)`=7 掩码 | 结构性不适用 | 索引散布常数（同值在 `p3_wcs.cpp:412` 亦作 seed 常数使用，非科学量）；掩码要求 kHotSlots 为 2 的幂 | — | CONFIRMED |
| :169 | `TileData(kReaderBuf)` | 262144 | 每次真读的分配元素数 | 结构性不适用 | 缓冲区尺寸 | — | CONFIRMED |
| :189 | `leaf_order = tile_order + 9` | 9 | log2(512)：tile 内 leaf 数对应的阶偏移 | 结构性不适用 | `docs/algorithms/PHASE3_RESAMPLE.md:37,41`「leaf_order = order_sel + log2(W)（支持子集 W=512 ⇒ +9）」＋ :39-41 明确「`>>(2·log2 W)` 是索引位移、order 偏移是 log2(W)」⇒ 与 :190 传"阶"的调用一致 | — | CONFIRMED |
| :199 | `nested_local_to_fits_index(local, 9, kTileWidth)` | 9、512 | tile 内 nested local → 标准 HiPS 行主序 | 文献值 | IVOA HiPS 1.0 tile 排列（`healpix_core.cpp:288-292` 实现 `(tile_width−1−x)·tile_width + y`，其抬头注释「FITS 列 = y, FITS 行 = tile_width-1-x」并点名 CDS Hipsgen MAPTILES 为 Oracle）＋共址测试 `healpix/tests/test_hips_tile_mapping.cpp:37-71` | — | CONFIRMED（本文件只做调用，翻转在被测 helper 内） |
| :206 | `max_order < 0 || max_order > kMaxOrder` | 0、20 | order 值域守卫 | 文献值→登记值：按五档取 **结构性不适用** | `hips_properties.h:23 kMaxOrder = 20`，注释给理由「ARCH-P3 §3 内存守卫(2^(2·20) leaf 上限之外拒)」；`lib/include/astrocs/core/plan_estimator.h:26` 同值二次登记 | 域上限一致（两面同 20）⇒ 无多侧分歧；越界即 P3_RS_PARAM | CONFIRMED |
| :209-210 | `512u << k`、`/3600.0` | 512、3600 | 叶级 nside 与 角秒→度 | 结构性不适用（位移）＋**公式导出**（3600） | `pixel_resolution_arcsec(512·2^k)`＝等面积等效尺度；3600 是 deg/arcsec 定义常数 | — | CONFIRMED |
| :211-214 | `res_deg <= scale_deg_per_px`；兜底 `*out_order = max_order` | — | order 选择判据 | 公式导出 | `PHASE3_RESAMPLE.md:30-34` G3：`order_needed = ceil(log2(sqrt(π/3)/(W·s_out_rad)))`，`order_sel = clamp(order_needed,0,hips_order)`。本处用"从小到大第一个满足 res≤scale 的 k"，与 ceil 式**等价**（复算：`pixel_resolution_arcsec(nside)` = sqrt(4π/(12 nside²))·(180·3600/π) ⇒ res_deg(k) = (sqrt(π/3)/512)·2^(−k)/1 … 解 res_deg≤s 得 k ≥ log2(sqrt(π/3)/(512·s_rad)) ⇒ 最小整数 k = ceil(·) ✓）；无更细层时取 max_order ✓ 与头 :19-20 一致 | — | CONFIRMED（等价性自己推过，不报） |
| :230 | `max_tiles <= 0 ? 8u` | 8、0 | 配置兜底 | 待确认 | 见 :70 行；`PHASE3_RSMP_IMPL.md:201` 明写「负值不报错（按合同恢复默认）」⇒ 属登记行为 | 方向：把"填 0/负"读成"用默认"而非拒 ⇒ 偏静默；影响仅限性能 | CONFIRMED |
| :260 / :141 | `absent_cache` 默认 | 1（开） | 缺失 tile 负缓存开关 | 结构性不适用（工程开关）＋**待确认其默认站哪侧** | 头 :76-80 明写「默认 1=开；enabled=0 仅用于回归阴性对照，生产路径不得关闭」 | 方向：默认开=把"读失败"永久记为"不存在"（见 CR-59-11），故默认值本身是科学语义相关的一次取舍；保守方向=只把"确认文件不存在"入负缓存 | CONFIRMED |
| :286, :511 | `leaf_nside = kTileWidth << p.order` | 512 | 叶级 nside | 结构性不适用 | order≤20 ⇒ 2^29 在 uint32 内 ✓（复算 512·2^20 = 536,870,912 < 2^32） | — | CONFIRMED |
| :293 | 缺 BUNIT 时的缺省串 | "ADU/sr" | 面亮度 canonical 单位 | 文献值 | `docs/algorithms/PHASE3_RESAMPLE.md:64-68` G5 明写「无 BUNIT 键 ⇒ canonical 'ADU/sr'（禁缺省 'ADU'，禁 Jy/beam）」，依据 `DATA_SEMANTICS §31.1/§31.1a FZ-UNIT-SIGNAL-SB FROZEN`；本处与登记串逐字一致 | — | CONFIRMED |
| :309, :394, :407, :443, :530, :536, :552, :575, :579, :580 | `std::nanf("")` | NaN | 无效/缺失输出值 | 结构性不适用（值语义）＋合规 | 头 :82-98 与 ALG-P3-003 §4 表（缺 tile→C=0,S=NaN；零合格样本→覆盖级 NaN；禁零填）——**全部为 NaN，无一处用 0 冒充未测**（这条是派单重点②的直接答案：本文件在边界/外域上给 NaN+coverage=0 或 NaN+C=1，不给合法值） | — | CONFIRMED |
| :352, :356 | `P pts[10]`；`np >= 10` | 10 | 邻域点缓冲上界 | 结构性不适用 | `healpix_core.cpp:374-405 neighbors()` 返回 **8 个邻居不含中心**（复算其实现：`reserve(8)` + 8 次 slot），加本文件 :369 显式 `add_pt(ipix)` ⇒ np 恒 ≤9，10 有余量 | — | CONFIRMED |
| :354, :359 | `* M_PI / 180.0` | 180 | 度→弧度 | 结构性不适用 | 单位换算定义常数 | — | CONFIRMED |
| :360-362 | `den = sinδ0 sinδ + cosδ0 cosδ cos(Δα)`；`den <= 0 → 丢点` | 0 | 角距余弦（>90° 判外） | 结构性不适用（守卫）＋待确认其登记（见 CR-59-12） | gnomonic 分母；`den<=0` ⇔ 该点在切平面半球外 | 方向：丢点后该象限可能空 ⇒ 静默退化为更少象限的插值（见 :396-398 填充），不计数不报 | CONFIRMED（式已复算） |
| :363-365 | ξ、η 式 | — | 球面→切平面 gnomonic | 公式导出 | 标准 gnomonic：ξ = cosδ sin(Δα)/D、η = (sinδ cosδ0 − cosδ sinδ0 cos(Δα))/D；极限复算：Δα=0 ⇒ ξ=0、η=tan(δ−δ0) ✓；δ=δ0 ⇒ ξ≈Δα·cosδ0 ✓ ⇒ 与 `p3_wcs.cpp` 正向、`test_p3_wcs.py:13-25` 独立实现同式 | 注：注释 :351 称「切平面坐标(deg)」，实际为**无量纲弧度正切值**（只作比值用，u/v 不受影响）⇒ 记 S3，不单立 finding | CONFIRMED |
| :373, :387 | `best_d[2][2] = {{1e300,…}}`、`nd = 1e300` | 1e300 | 距离最小值哨兵 | 结构性不适用 | 累加器初值（球面角距 ≪1）；非科学阈值 | — | CONFIRMED |
| :376-377 | `p.x >= 0 ? 1 : 0`、`p.y >= 0 ? 1 : 0` | 0、1 | 象限位（含 0 归正侧） | 结构性不适用 | 象限基数与 tie 规则；:379-380 的并列取"更小 ipix"给确定性 ✓ 与头 :371 注释一致 | — | CONFIRMED |
| :412-414 | `fabs(dx) > 1e-300`、`(0.0 - x0)/dx` | 1e-300、0.0 | 退化防护（除零） | 待确认（守卫阈值） | 全仓/文档对该阈零条文（检索式：`git grep -rn "1e-300" -- docs/ lib/algorithms/resample/` 未在本路取证内执行 ⇒ 记缺角）。语义：dx=0 ⇒ u 保持 0 | 方向：dx 恰为 0 时只取负 x 侧权重，等价退化为一维/点采样 ⇒ 偏保守（不产生伪高值）；影响范围：象限被重复填充的像素（极点/face 角附近） | PARTIAL |
| :415-416 | `min(1.0, max(0.0, u))`、`(0.0, 1.0)` | 0.0、1.0 | 参数钳到 [0,1] | 结构性不适用 | 插值参数域；正常象限构造下 u,v 本已 ∈[0,1]（x0<0≤x1、y0<0≤y1），钳位只对填充退化角起作用 | — | CONFIRMED |
| :417-418 | `wg[4] = {(1-u)(1-v), u(1-v), (1-u)v, u·v}` | — | 四角几何权重 | 公式导出 | **式本身**归一恒等：Σwg = (1-u+u)(1-v+v) = 1 ✓ 与自己复算一致；角序与头 :137-139「[0][0],[1][0],[0][1],[1][1]」一致 ✓。**但其几何含义与登记面不符 ⇒ 见 CR-59-09** | 方向：权重和恒 1 ⇒ 不会整体放大/缩小；偏差是**逐像素重分配**（角点自身坐标不进入其权重） | CONFIRMED |
| :425 | `wsum`（注释区分 Phase1 面积 D_p[sr]） | — | 无量纲合格权重和 | 结构性不适用 | 注释显式否认与 Phase1 `D_p` 同量（`DATA-002` 分母符号表 :137-139 要求逐分支分母，本处符合"信号核分母 = Σ 合格 w"） | — | CONFIRMED |
| :427, :436 | `k < 4`；`n_eligible = 4 - n_rej` | 4 | 四角循环上界与计数 | 结构性不适用 | 数组维度=4（象限基数） | 注：象限退化填充时四角可含**重复 leaf**，此时"样本数 4"与实际不同 leaf 数不等 ⇒ 见 CR-59-10 | CONFIRMED |
| :438 | `(n_rej == 4) || !(wsum > 0.0)` | 4、0.0 | 零合格样本判据 | 文献值 | `DATA-002 §2a` 的 `zero_eligible_samples = "nan_signal_support_le_0"`（:115）＋头 :145 同口径；`!(wsum>0)` 含 NaN（NaN>0 为假 ⇒ 判零合格），方向保守 ✓ | — | CONFIRMED |
| :445 | `weights[0..3] = 0.0`（零合格分支） | 0.0 | 零合格时四权重如实给 0 | 结构性不适用 | 与头 :145、ALG :141「零合格样本时四权重全 0（Σ=1 不适用）」一致 ✓ | — | CONFIRMED |
| :450-452 | `eff = wg[k]/wsum`；仅 `ok[k]` 时写 `weights[k]` | — | 重归一生效权重 | 公式导出 | 与头 :141-142、ALG-P3-003 :54-55 的 `c'_k = w_k/Σ_{合格}w_j` 同式 ✓ 代码真按式算；**但被剔除样本的 weights[k] 未被写入 ⇒ CR-59-08** | 方向：Σc'=1 恒成立 ⇒ 无幅度失真；缺的是"被剔除样本恰为 0"这一合同承诺 | CONFIRMED |
| :454 | `static_cast<float>(val)` | — | FP64 累加后回落 float32 | 结构性不适用 | `DATA_SEMANTICS:2491`「bilinear 权重 FP64 计算后回落 float32 输出」⇒ 与登记一致 | — | CONFIRMED |
| :535 | `d == 0.0 && unc_src == P3_UNC_IVAR` | 0.0 | ivar==0 ⇒ u 无效（NaN 传播态） | 文献值 | `docs/science/UNCERTAINTY_AND_COVARIANCE.md:114-115`「ivar==0 像素 = 零权重 ⇒ u 无效（NaN 传播态），不是硬错误，导出值取 NaN 而非 1/0→Inf」＋`DATA_SEMANTICS §30.4-1/-3` 被其引用 ⇒ 逐字一致 | — | CONFIRMED |
| :538, :540 | `d < 0.0 || !std::isfinite(d)` → `P3_RS_PARAM`；`!isfinite(*out)` | 0.0 | 值域守卫（产品损坏显式拒） | 结构性不适用（守卫）＋合规 | UNCERTAINTY:115「负/Inf = 产品损坏显式错误」＋头 :126「u 值域守卫在逐像素 propagate 时执行」⇒ 不 clamp、不补 0、不静默跳过 ✓ | — | CONFIRMED |
| :539 | `1.0 / d` | 1 | ivar→variance 倒数 | 公式导出 | `ivar = 1/variance` 二次律（`p3_rsmp.h:89 FZ-P3-BUNIT-QUADRATIC` / UNCERTAINTY:92）；FP64 除法 ✓ | — | CONFIRMED |
| :548, :550-551 | `npts != 1 && npts != 4`、null 检查 | 1、4 | 传播入参值域 | 结构性不适用 | 采样核基数（nearest/bilinear）；非法值显式 PARAM ✓ | — | CONFIRMED |
| :558 | `(npts == 1) ? 1.0` | 1.0 | nearest 的单权重 | 公式导出 | 头 :182「npts=1 (nearest): var_out = u_in」= R C_in Rᵀ 的单权重 1 特例（UNCERTAINTY:90）✓ | — | CONFIRMED |
| :567 | `if (npts == 4 && w == 0.0) { ++n_masked; continue; }` | 0.0 | 以"生效权重 0"识别被剔除样本 | 结构性不适用（判据）＋**依赖缺陷** | 头 :188-190 明写该口径；依赖"被剔除样本恰为 0"⇒ 与 CR-59-08/CR-59-10 同源 | 方向：权重若为**未初始化非零**则不 masked ⇒ 该项进 acc（偏大或 NaN）；若退化角 w=0 被 masked 而信号侧记 n_rejected=0 ⇒ 两面计数矛盾 | CONFIRMED |
| :570 | `acc += w * w * u_k;` | — | 方差传播标量特例 Σc_k²·u_k | 公式导出**但适用域被静默假定** ⇒ 见 CR-59-07 | 头 :182-195 与 `UNCERTAINTY_AND_COVARIANCE.md:85-101` 主式 `[R C_in Rᵀ]_ii`；对角特例条件在 :91/:100-101 | 方向：丢非对角项 ⇒ Var 只会**变小** ⇒ `ivar_out = 1/var` 变大 ⇒ 下游按更小的误差加权 ⇒ 偏绿 | CONFIRMED（丢项方向已钉） |
| :572-575 | `n_masked == npts` ⇒ NaN | — | 零合格样本的方差侧判定 | 结构性不适用（判据） | 头 :190「四样本全为 0 ⇒ 覆盖级 NaN（禁静默 0 冒充无效）」✓ | — | CONFIRMED |
| :577-580 | 合成序「损坏 > missing > NaN > 数值」 | — | 状态优先级 | 结构性不适用 | 头 :113-115/:192 三态分解 ✓ 一致 | 注：acc 若因未初始化权重变 NaN，仍走 :580-582 输出 NaN 而 `st = P3_U_OK`（**未走 any_nan**）⇒ 记入 CR-59-08 的后果链 | CONFIRMED |
| :121, :74-77 | 统计计数器 `stat_hits/misses/absent/evict/open_fail` 初值 | 0 | 观测量 | 结构性不适用 | 观测计数；被 `P3CacheStats`（头 :64-73）暴露，回归件 `eng/tests/unit/p3_sampler_cache_test.cpp`（头 :62-63 指认）消费 | 注：`open_failures` 的语义标注见 CR-59-11 | CONFIRMED |

## 3.2 findings

### CR-59-07 · 生产 export 的 VARIANCE/IVAR 走 `Σc_k²·u_k` 对角特例，把输入像素间相关**静默丢弃**；同一仓的冻结门恰好对这种表示判 REJECT
- 轴：S（科学量凭什么）为主；并命中 D（设计点名的一般式未做）与 C（三面说法不一）
- 位点：`p3_resample.cpp:570` `acc += w * w * u_k;               // var_out = Σ c_k²·u_k (nearest: c=1)`；同式在 `:561` 的调用形态里只传 `weights/leaf_ipix`，**没有任何接口能携带 `Cov(u_j,u_k)`**（签名：`p3_resample.h:196-198` `p3_uncertainty_propagate(P3Sampler* u, const double* weights, const uint64_t* leaf_ipix, int npts, double* u_out, P3UncPixelState* st)`）
- 上位依据（引原文短句）：
  - `docs/science/UNCERTAINTY_AND_COVARIANCE.md:86-91`「主式（一般式，允许多个输入像素相关）bilinear: `var_out(i) = [ R C_in Rᵀ ]_ii` … 标量（对角）特例: `C_in = diag(u_k) ⇒ var_out = Σ_k c_k² · u_k`」；:95-99「**为何主式必须带协方差项**：…只写 `Σ c_k²u_k` 等于假设 C_in 对角，**系统性低估**输出方差：偏差因子 ≈ 1+0.75ρ（两像素近邻近似），ρ=0.19 ⇒ 方差低估 36.3%、σ 低估 20.2%」；:100-101「`Σc_k²` 标量式**只在 C_in 对角时**成立，该式的适用域 = C_in 对角」。
  - `docs/contracts/DATA_SEMANTICS.md:2692-2696`（沿用 `FZ-FORMULA-COV-PROP`）「`C_y = R C_x R^T`；`Var(y_i) = Σ_j R_ij²[C_x]_jj + 2Σ_{j<k} R_ij R_ik[C_x]_jk`。现行 `Σ c_k² u_k` 只在输入 `C_x` 对角时严格；Drizzle 输入 `mean|ρ|≈0.19` 下对角省略低估 23.3%」。
  - 输入相关性是本仓自证事实：`UNCERTAINTY_AND_COVARIANCE.md:19-26`「同一源像素贡献多个输出像素 ⇒ Drizzle 后相邻输出**非严格独立**：`Cov(S_p,S_q)=Σ_j c_jp c_jq v_j` … nside=512 合成帧相邻像素 mean|ρ|≈0.19、max|ρ|≈0.57」。export 的输入正是这类 HiPS 子产品（`p3_resample.h:102-111` 的 variance/ivar 子产品面）。
  - 同仓冻结门：`lib/algorithms/resample/p3_rsmp_failclosed.cpp:170-175` `if (rec.variance_diagonal_only && !rec.correlation_kernel_present && !rec.correlation_approx_error_available) add_gate(..., "G-P3-COV-01", "diagonal_only_without_correlation_kernel_or_approximation_error")`，其枚举注释 `p3_rsmp.h:310` 写「DiagonalOnly — 仅对角（须相关核/近似误差，否则 REJECT）」。
- 现状：生产链逐跳——`module_adapters.cpp:14276`（`npts = (g.sampler=="nearest")?1:4`）→ `:14354` `p3_sample_bilinear_ex(..., w, lf)`（w 在 :14351 零初始化）→ `:14367` `p3_uncertainty_propagate(&w_u, w, lf, npts, &u_out, &u_st)` → 本文件 :570 求和 → `:14377-14385` 写 `bvar/bivar`（VARIANCE/IVAR 平面）。全程只有**逐像素标量 u_k**（从 variance/ 或 ivar/ 子产品单点读取，:526-543），无任何相关项、无相关核、无近似误差声明，也没有任何 provenance/manifest 键登记"此处按 C_in 对角假设"。会话面 `p3_session.cpp:305-306` 同构。
- 差在哪：主式要求一般二次型，产品实现取了只在 `C_in` 对角时成立的特例，而其输入类别恰是被文档自己判定为**相关**的 drizzle/mosaic 产物；同时该表示法在本仓已有显式 REJECT 门（G-P3-COV-01），只是那条门住在没有进生产构建的 v6 链里（`eng/ci/ledgers/spec_named_impl_gaps.json` SNI-S4-P3X-06：`p3_v6_export.cpp` 不在任何生产 target）。⇒ 同一个产品面（export 的 VARIANCE/IVAR）由两条实现承担，一条带门一条不带，生产走不带的那条。
- 后果：①对外主张"跨帧绝对 SNR 传递链/不确定度全程可传播"（创新点②SCI-B 面）在产品出口失真——按登记面自报的幅度，方差低估 **23.3%**（DATA_SEMANTICS）～**36.3%**（UNCERTAINTY，两数还互相不一致），σ 低估 ~20%；②`ivar_out = 1/var` 被系统性放大 ⇒ 下游 mosaic 的 ivar 加权（`docs/science/INTEGRATION.md §5` 权重=ivar）对 export 产物给过高的信任；③方向钉死：丢的是 `2Σ_{j<k}R_ij R_ik[C_x]_jk`，在正相关（drizzle 的 ρ>0）下丢掉的是**正项** ⇒ Var 只会偏小 ⇒ 判据一律偏绿，不偏红。
- 定级：**S1**（触发路径已在"现状"逐跳给出，且改的是产品字节与对外不确定度主张）
- 怎么算修好：最小正确面按顺序三步，**三步同批**否则必然判红：①条文侧先定"export 输入面的 C_in 假设"要么成立要么显式声明——把 `UNCERTAINTY_AND_COVARIANCE.md` Phase3 节里"适用域 = C_in 对角"的条件改成本产品的实际口径（相关输入 ⇒ 必须走一般式或带相关核），并与 `DATA_SEMANTICS` 的 23.3%/36.3% 两数**收敛为一数**（两数现在打架：ρ=0.19 时 1+0.75ρ 只能给 14.2% 量级的两像素近似，谁都复算不出 23.3% 与 36.3% 同时成立）；②实现侧：给 `p3_uncertainty_propagate` 增加相邻像素协方差入口（最小可用=相关核 K(Δpix,σ)），或在 `p3_output`/provenance 里写死 `representation="diagonal_variance"` + `correlation_approximation_error=<已签值>`，让 G-P3-COV-01 的语义在生产面可达；③门侧：把 G-P3-COV-01 从 v6 链搬到生产节点（`p3n_*` 里做，节点面已有 p3n_guard_input_units 先例，module_adapters.cpp:11440-11535 同族），补一条**逐格**判据而非求和闭合。**禁止**的修法：把文档改成"输入视为不相关"以自证现状——那条改法会同时把 SNR-012 的 MC 证据与 aperture 边界条款判为假。
- 置信：CONFIRMED（生产调用点、实现式、三条上位原文、门码原文均逐字读过；未实跑，故"低估幅度"沿用两文档自报数且已指出两数互斥）

### CR-59-08 · 被剔除样本的 `weights[k]` 不写入，而合同规定其"恰为 0.0"，方差侧又以 `w == 0.0` 作掩膜判据
- 轴：R（真实输入下得到什么）＋ C（注释/合同与代码不同形）
- 位点：`p3_resample.cpp:447-453`
  ```
  double val = 0.0;
  for (int k = 0; k < 4; ++k) {
      if (!ok[k]) continue;                     // ← 被剔除角：weights[k] 从未赋值
      const double eff = wg[k] / wsum;
      val += eff * static_cast<double>(vv[k]);
      if (weights) weights[k] = eff;
  }
  ```
- 上位依据（三处，逐字）：`p3_resample.h:142`「被剔除样本恰为 0.0」；`p3_resample.h:186-188`「c_k = p3_sample_bilinear_ex 暴露的生效权重（样本级掩膜后重归一; 被剔除样本恰为 0）」；`docs/algorithms/PHASE3_RESAMPLE.md:54-55`「weights[4] 暴露**生效（重归一）权重** c_k = w_k / Σ(合格 w_j)，被剔除样本恰为 0」。另 `DATA-002 §2a`（:114）`aggregation = "sample_level_mask_with_renormalisation"`。
- 现状：只有 `zero_eligible` 分支（:445）把四元素整体写 0；部分剔除分支里，`!ok[k]` 的槽位保持**调用方传入的原值**。生产两处调用点恰好都零初始化（`module_adapters.cpp:14351`、`p3_session.cpp:286` 均 `double w[4] = {0, 0, 0, 0};`）⇒ 现网不发作。
- 差在哪：out-param 的语义承诺由实现方违约，靠调用方的初始化习惯兜住。方差侧的判据是值判据 `w == 0.0`（:567）而非独立的有效性位——一旦某个新调用方写成 `double w[4];`（栈上未初始化）或复用上一像素的数组（本文件对 `leaf_ipix` 是全 4 槽无条件写，:457-460，对比之下 `weights` 是条件写，这个不对称就是缺陷本身），被剔除角带着**非零垃圾**进入 :570。
- 后果：①该像素 variance 得到一个由栈垃圾决定的数（合法值冒充测量值，正是派单重点②要抓的形态）；②`n_masked` 少计 ⇒ :572 的"四样本全被掩 ⇒ 覆盖级 NaN"判据被跳过 ⇒ 一个 signal=NaN 的像素可以给出**有限** variance/ivar（`ivar = 1/var` 在 :14381 处继续放大）；③若垃圾是 NaN，`w==0.0` 为假 ⇒ 不进 masked、`acc` 变 NaN，但 `st` 仍是 :581 的 `P3_U_OK` ⇒ **NaN 值配 OK 状态**，会话侧 :14377 的三分支判定（MISSING/NAN/数值）全部错档。方向：状态从"无效"漂到"OK" ⇒ 偏绿。
- 定级：S2（缺运行时触发路径：现有两处生产调用点均零初始化，写不出"当前构建下产物已错值"的逐跳链）
- 怎么算修好：①:427-430 的循环里对 `!ok[k]` 显式 `if (weights) weights[k] = 0.0;`（与 :445 同口径），并在 :457 之后对 `weights` 也做无条件四写；②把方差侧的掩膜判据从 `w == 0.0` 改为**独立的有效性出参**（例如把 `P3SampleRejection` 或一个 `uint8_t mask` 传进 `p3_uncertainty_propagate`），否则合法几何零权重与被剔除样本永远混淆（见 CR-59-10）；③补一条负例：单角注入 NaN + 调用方把 `weights` 预置为 `{1e9,1e9,1e9,1e9}` ⇒ 断言被掩槽读回 0.0。该负例现在会红，属**新增判据**而非改口径。
- 置信：CONFIRMED（代码路径、两处调用点初始化形态、三处合同原文）

### CR-59-09 · 生产球面 bilinear 的四角权重不是登记面所说的"面积重叠分数"；registry 自己把该核标为非生产默认、另设 `bilinear_area_overlap_exact` 为生产科学默认
- 轴：S（科学式凭什么）为主，命中 D（设计点名的能力未做）与 C
- 位点：`p3_resample.cpp:409-418`
  ```
  // 平面双线性: 以四角平均中心定义局部坐标, 解 (u,v)
  const double x0 = q[0][0]->x, x1 = q[1][0]->x, y0 = q[0][0]->y, y1 = q[0][1]->y;
  // 取每角到原点符号距离的归一权重(双线性一般式, 四角非共线时退化为面积权重)
  ...
  const double wg[4] = {(1 - u) * (1 - v), u * (1 - v), (1 - u) * v, u * v};
  ```
- 上位依据（引原文）：`docs/algorithms/PHASE3_RESAMPLE.md:43`（ALG-P3-003 §2 G4）「bilinear: 邻域 4 leaf 权重 w=**面积重叠分数(投影线性化)**, Σw = 1 ± k·ULP」；`docs/contracts/DATA_SEMANTICS.md:2672`（FZ-P3-KERNEL-REGISTRY 表）「`bilinear_4quad` | 须独立 Oracle + 通量/面亮度语义 + 误差/边界定义后注册 | 面亮度（行归一）或通量（列归一）」；`lib/algorithms/resample/p3_rsmp_kernel_registry.cpp:176-196` 注册 `bilinear_4quad` 为 `normalization = UnitSum`、`error_bound_expression = "(h^2/8)*(max|Fxx| + max|Fyy|)"`、**`production_science_default = false`**；:201-215 另注册 `bilinear_area_overlap_exact` 为 `normalization = ExactOverlap`、`error_bound = "0（解析重叠，无插值近似）"`，注释抬头写「（生产科学默认）」。
- 现状（复算，不凭记忆）：设四角在切平面坐标 `q00=(x0,y0)`、`q10=(x1,y0')`、`q01=(x0,y1)`、`q11=(x1',y1')`，代码给 `u=(0−x0)/(x1−x0)`、`v=(0−y0)/(y1−y0)`，权重 `{(1-u)(1-v), u(1-v), (1-u)v, u·v}`。
  - 归一性：Σwg = (1-u+u)(1-v+v) = **恒等 1**（与登记面 Σw=1±k·ULP 相容）✓。
  - 面积性：**不成立**。四权重只用到 `x0,x1,y0,y1` 四个标量，`q11` 的**自身坐标 (x1',y1') 完全不进入任何表达式**（源码里 `q[1][1]` 只出现在 :404 取值与 :459 输出 ipix）；同理 `q10` 的 y、`q01` 的 x 也不进入。⇒ 把 `q11` 从 (1,1) 移到 (1,0.01)（仍在 (+,+) 象限、仍是该象限"最近中心"）其权重恒为 `u·v = 0.25`，而它所代表的重叠面积元显然变了。真正的面积重叠分数必须是四个顶点坐标的函数（一般四边形下等价于面积坐标/重心广义重心坐标）。故本实现是"**以两条边定出的轴对齐合成矩形上的双线性**"，不是面积重叠分数；`u,v` 也不满足"四角平均中心"（:409 那句注释描述的量在代码里不存在）。
- 差在哪：登记面用面积语言命名并定义该核（并据此声称 `Σc_k²≠1` 的物理与通量语义），实现用的是另一个算子；且本仓 registry 自己已把"面积精确"列为另一个核、并把 `bilinear_4quad` 标为非生产科学默认 ⇒ 生产跑的恰是被登记面标注为"不当生产科学默认"的那一个，而合同条款（`DATA_SEMANTICS:2672`）对二者的区分在生产面不存在（生产按字符串 `"nearest"/"bilinear"` 选路，从不查 registry：`module_adapters.cpp:14352-14354`、`p3_session.cpp:289-291`）。
- 后果：①创新点③的"无接缝叠加/加性天光"在产品出口处依赖重采样核的守恒语义，而该语义（面积重叠）未实现 ⇒ 跨 tile、face 边界与高纬处的逐像素通量重分配与登记式不同（同一输入在 `exact overlap` 与本核下会给不同 signal 字节）；②VARIANCE 面的 `Σ c_k²`（:570）用的正是这套 c_k ⇒ 与 CR-59-07 叠加；③`error_bound "(h²/8)(max|Fxx|+max|Fyy|)"` 是**张量积网格**上的双线性误差界，对本核在斜四边形上的适用性无人证明，却被 `validate_registration` 当作"有误差界"放行。
- 定级：**S1**（signal 与 variance 两个产品平面字节均由该权重直接给出；触发路径：`export` → `module_adapters.cpp:14354` → `p3_resample.cpp:322` → `:417` → `:14357` 写 bsig）
- 怎么算修好：先定口径再改式（顺序反了会把正确实现判红）：①条文明确 export 的球面 bilinear 到底是"面积重叠"还是"四象限最近中心的线性距离"。若要面积精确：实现 `q11` 坐标参与权重（一般四边形面积坐标 / 或直接做输出像素足迹与输入 leaf 的多边形裁剪重叠），并把 `bilinear_area_overlap_exact` 真正接进生产选路；若维持现算子：ALG-P3-003 G4 的「面积重叠分数(投影线性化)」必须改名为"轴对齐合成矩形双线性（UnitSum）"，`PHASE3_RESAMPLE.md:188` 的双线性出处条目要相应改。②同批改 :409/:411 两句与代码不符的注释。③同批给 `p3_rsmp.h:264` 的平面版 `make_bilinear_4quad_neighborhood`（"重叠面积记为 w·Ω'_i 使 R_ij = w_ij"——这是把非面积量**定义**成面积量）一并订正，否则两实现同名同 id 不同式。④误差界条目换成本核的真实界，或在 registry 里把 `error_bound_present` 改 false 让 `validate_registration` 判红。
- 置信：CONFIRMED（权重表达式逐项复算，"q11 坐标不进入权重"是可从 :409-418 直接读出的结构性事实；"是否等于面积分数"未做数值对照 ⇒ 该点为 PARTIAL，但结论不依赖数值对照，只依赖登记面措辞）

### CR-59-10 · 生效权重恰为 0 的**合格**样本：信号侧计入 eligible、方差侧当作 masked、计数侧报 0——同一像素三面对不上
- 轴：C（上下级/相邻面口径不一）＋ R
- 位点：`p3_resample.cpp:424-437`
  ```
  bool ok[4]; double wsum = 0.0; int n_rej = 0;
  for (int k = 0; k < 4; ++k) { ok[k] = std::isfinite(vv[k]); if (ok[k]) wsum += wg[k]; else ++n_rej; }
  if (rejection) { ... rejection->n_rejected_nonpositive_weight = 0;   // 权重由几何唯一确定
                       rejection->n_eligible = 4 - n_rej; }
  ```
  与消费侧 `:567 if (npts == 4 && w == 0.0) { ++n_masked; continue; }`
- 上位依据（原文）：`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md:126`「**合格样本** | 参与聚合的源样本满足：值有限（`isfinite(x_j)`）**且几何上覆盖该输出像素（`w_jp > 0`）**」；:129「该样本按不合格样本剔除并计入 `n_rejected_nonfinite_variance`，类别恒为「损坏」并与「方差不可用」分列」；本文件头 `p3_resample.h:160`「分类为**互斥、可加**三项 (DATA-002 §2a 规则 3): 值非有限 / 方差非有限 / 权重非正」、:168 字段注释「权重非有限或 ≤0」。
- 现状：象限为空时 :396-398 用 `nearest_pt` 填充，被填充的角其 `wg` 可以**恰为 0**（例：`u=0` ⇒ `wg[1]=wg[3]=0`；单点全填充 ⇒ `wg = {1,0,0,0}`）。此时：`ok[k]` 为真（值有限）⇒ 计入 `n_eligible`；`n_rejected_nonpositive_weight` 恒写 0（注释断言"几何权重 ⇒ 恒 0"）；而方差侧 `w == 0.0` 把同一样本**当被剔除样本跳过**。
- 差在哪：按正本定义，`w=0` 的样本**不是合格样本**，应落入三项分类里的"权重非正"类并被强制计数（`rejection_counting = "mandatory"`、"禁静默剔除"，见 `PHASE3_RESAMPLE.md:112`）；实现把它同时算作"合格"（信号侧计数）与"被掩膜"（方差侧），并申报计数 0。
- 后果：①强制计数在产品证据面**低报**被剔除样本数（方向恒为偏小 ⇒ 偏绿：任何"剔除数是否超预算"的判据只会更容易过）；②signal 平面的 eligible 数与 variance 平面的参与样本数不一致，两平面在同一像素上的"用了哪些 leaf"不可比对；③`n_rejected_nonfinite_value` 与合计相等的恒等式（:432-433）使三项分类退化为单项，正是本仓已实证过的"语义列恒定"形态。
- 定级：S2（不改变 signal/variance 数值——w=0 本来不贡献；失真在强制计数与两平面一致性；未找到把 `P3SampleRejection` 送进产品的生产消费者，见"怎么算修好"③）
- 怎么算修好：①:428 的合格判据改为 `ok[k] = std::isfinite(vv[k]) && wg[k] > 0.0`（与 DATA-002:126 逐字同形），并把 `wg[k] <= 0` 的情形计入 `n_rejected_nonpositive_weight`；②`wsum` 随之只累加合格项 ⇒ 现网 `u=0/v=0` 退化像素的 `val` 不变（被剔除项原本权重 0、贡献 0），即**该修法不改任何像素值**，必须先确认这一点再改，否则会误判"修坏了"；③先查清 `P3SampleRejection` 的产品承载面——`PHASE3_RESAMPLE.md:59-60` 自陈「未冻结项: 该计数的产品承载面…登记 ALG-P3-RSMP-006 DISP-P3RSMP-006」，故计数目前可能只在测试里被读；若确实无承载面，本条的第①②步仍要做（合同要求 mandatory），并在修好后把 DISP-P3RSMP-006 一并结案。
- 置信：CONFIRMED（三段代码与两处正本原文逐字对读；"承载面是否存在"标 PARTIAL，其依据是 `PHASE3_RESAMPLE.md:59-60` 自述未冻结）

### CR-59-11 · tile "读取失败"与"文件不存在"合并进同一负缓存并被永久记为缺失；观测计数器名与实计不符
- 轴：R（真实输入下得到什么）＋ C（注释/合同对同一计数的说法不同）
- 位点：`p3_resample.cpp:170-178`
  ```
  if (aio_hips_read_tile_f32(s->ds, tip, tile->data()) != 0) {
      s->last_err = ...; s->cache->note_open_failure();
      if (s->absent_cache) { s->cache->mark_absent(tip); ... hot_state[slot] = 2; }
      return std::shared_ptr<const TileData>();
  }
  ```
  与 `:56-61` 注释「absent = 负缓存: tile 不存在或读失败 → 记一次, 之后直接返回缺失」
- 上位依据：`docs/algorithms/PHASE3_RESAMPLE.md:124`（§4 表）**只登记一种情形**：「缺 tile 文件 | 该足迹 C=0, S=NaN, provenance 记 missing, 不中断」；同表 :131「properties 非法/缺键 | 显式拒(ALG-P3-001), 无 silent default」。本文件的姊妹路径 :492-500 也确有该口径（`p3_uncertainty_open`：文件存在但解析失败 ⇒ `P3_RS_PARAM` 显式拒；不存在 ⇒ 试下一候选）。头 `p3_resample.h:69` 把 `absent_reads` 注释为「写入负缓存的次数 (= 去重后的失败 open)」，:70 把 `open_failures` 注释为「真实失败 open 总次数」，而 :62-63 声称 `open_failures` 是「真实打进"**不存在**的 tile 文件"的 open 次数…未覆盖区域代价的可观测计数器」。
- 现状：`aio_hips_read_tile_f32` 的返回是单一 int：非 0 既可能是文件不存在，也可能是 `BITPIX` 不匹配（其 i32 兄弟的头注释明写「tile BITPIX 必须 = 32, 否则返回 -6」，`aio_hips_reader.h:62-64`）或异常（实现 `aio_hips_reader.cpp:538-550` 把 `catch(...)` 统一折成 `-1`）。本处一律 `!= 0` ⇒ 三者同归"缺失"，且写入**共享** `absent` 表（对所有 worker 生效、进程内不可撤销），此后该 tile 的每个像素恒 `coverage=0 / value=NaN`。
- 差在哪：登记面把"不中断"的宽容只给"文件不存在"；"存在但损坏/读失败"在别处（properties、u 值域）一律要求"显式拒、无 silent default"。同一文件内两处口径相反，且更严重的是**永久性**：一次 `EMFILE` 或压缩流瞬时故障会把一整块 tile 变成"足迹外"。
- 后果：①产品面：一批本应报错的像素以 C=0/NaN 交付，run 正常结束、manifest 无损坏标记 ⇒ 覆盖面被静默缩小，消费方看到的"未覆盖"与实际"读取失败"不可区分；②证据面：`open_failures`/`absent_reads` 被当作"未覆盖区域代价"的可观测计数器用于 §10.5 资源证据与回归用例（头 :62-63 指认 `eng/tests/unit/p3_sampler_cache_test.cpp` 据此断言），而它把损坏与缺失混记 ⇒ 该断言的证据含义被污染；③方向钉死：计数只会被**多计**成"缺失"，任何"缺失率是否可接受"的判据在损坏发生时朝"数据本来就没有"一侧解释 ⇒ 偏绿。
- 定级：S2（有生产路径可达该函数，但"读失败而文件存在"需要瞬时/损坏条件，我写不出当前构建下必然触发的逐跳链；后果一旦发生即产物覆盖面失真）
- 怎么算修好：①让 `fetch_tile` 区分"确认不存在"与"存在但读失败"（reader 需回传可判别的码；`aio_hips_read_tile_f32` 已有 -1/-6 之类码，只需头面登记语义），只把前者入负缓存，后者返回 `P3_RS_IO` 让上层显式拒（与 :495「存在但读取失败 → P3_RS_IO (fail-closed)」同口径）；②同批把 `P3CacheStats::open_failures`/`absent_reads` 拆成 `absent_*` 与 `io_error_*` 两组，避免 §10.5 证据混用；③回归负例：把一个存在的 tile 改成错误 BITPIX ⇒ 现实现会走"缺失"，修后必须 `P3_RS_IO`（这条负例现在必红，属新增判据）。
- 置信：CONFIRMED（读侧与写侧注释、§4 表原文、`p3_uncertainty_open` 对照口径、reader 异常屏障均已读；未实跑）

### CR-59-12 · 半球外邻域点被静默丢弃 + 象限空时以"最近点"重复填充，该退化在登记面无条目、在计面无字段
- 轴：D（设计未登记的行为）＋ R
- 位点：`p3_resample.cpp:362` `if (den <= 0) return;`（add_pt 内）与 `:396-398`
  ```
  for (int i = 0; i < 2; ++i)
      for (int j = 0; j < 2; ++j)
          if (!q[i][j]) q[i][j] = nearest_pt;
  ```
  注释 :385「退化防护: 角点位置某些象限可能无邻域点 → 用最近邻点填充(确定性双线性退化)」
- 上位依据：`docs/algorithms/PHASE3_RESAMPLE.md:120-133`（§4 边界/NaN/Inf 全表）无任何"邻域点投影丢失/象限空/退化填充"行；`p3_resample.h:91-98` 只登记了 tile 缺失、样本非有限、零合格三类；头 :371 只写"确定性: 距离并列时取更小 ipix"。**条文面零提及**。检索式：`git grep -n "退化" -- docs/algorithms/PHASE3_RESAMPLE.md`（本路未执行该条，执行的是 `git grep -n "四象限|quadrant|G4" -- docs/algorithms/PHASE3_RESAMPLE.md` → 命中 :36/:78/:79/:105/:116，均为 G4 锚点无退化条款）。
- 现状：9 个邻域点若某点 `den<=0` 即被丢弃且不留痕迹；若某象限因此（或在 face 角 `neighbors` 少于 8 个方向时，见 `healpix_core.cpp:405`「真角: 该方向无邻居」）为空，则用全局最近点重复填 4 角，权重退化为 `{1,0,0,0}`（即 nearest 语义）。
- 差在哪：一个把双线性静默降级为最近邻的路径，既无登记条款、也无 `P3SampleRejection` 字段承载（该结构只有"值非有限/方差非有限/权重非正/eligible"四项，无"象限退化"项）⇒ 消费者无法区分"我拿到了双线性"与"我在极区/face 角拿到了最近邻"。`den<=0` 在 |dec|<90 的局部邻域里几乎不可达（角距>90°），但 `neighbors` 在真角返回少于 8 个方向是**常态路径**（healpix_core 明写 continue）。
- 后果：①高纬/face 角处的 signal 与 variance 由退化核给出，而登记面承诺的是四角双线性 ⇒ 与 CR-59-09 同源的第二处"实际算子 ≠ 登记算子"；②`Σc_k=1` 不变量在退化下仍成立，所以任何"权重闭合"型门**判不出**这次退化 ⇒ 属"求和闭合掩盖逐格错配"家族。
- 定级：S3（无逐跳证据表明真实产品落在退化域上；若要升档需实跑极区/face 角用例统计退化像素占比——见文末"需要实跑才能定的"）
- 怎么算修好：①在 `P3SampleRejection` 增加 `n_quadrant_filled`（或 `bool degenerate_stencil`）并在 §4 表补一行登记其值域与承载面；②`add_pt` 的 `den<=0` 丢弃要计数（当前完全无痕）；③若结论是"退化即不接受"，则在 export 的适用域守卫处（`|dec|≤85°` 已在 `p3_wcs` 侧，PHASE3_RESAMPLE:130）显式声明 face 角不保证四角核，别只写在注释里。
- 置信：PARTIAL（代码事实 CONFIRMED；"真实产品是否命中退化域"未跑，未定）

---

# 4 · `lib/algorithms/resample/p3_resample.h`（202 行，全文已读）

## 4.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :12-17 | `P3ResampleStatus` | OK=0, PARAM=1, UNSUPPORTED=2, IO=3 | 状态码值域 | 结构性不适用 | 枚举基数；消费侧按名判定（`module_adapters.cpp:14368` 判 `P3_RS_PARAM`，`p3_session.cpp:307` 同），未见跨语言把序数当协议文本（与 §1.2 第 14 行不同，此处无 Python 消费者） | — | CONFIRMED |
| :19-21 | `p3_order_select` 语义注释 | L∈[0,max]；取不到即 max_order | 叶级 order 选择口径 | 文献值 | `docs/algorithms/PHASE3_RESAMPLE.md:30-34` G3（SCI-P3 §5 冻结）；实现等价性已复算（见 §3 表 :211-214 行） | **条文与实现一致，但该 order 未被采样路径使用 ⇒ CR-59-13** | CONFIRMED |
| :23-27 | 输入模式值域 | `surface_brightness` 唯一接受；`weight`/`flux-per-pixel`→UNSUPPORTED；`variance`/`ivar`→PARAM | 显式拒清单 | 文献值 | `DATA-P3-UNC-001 §30.4-4` 的 supersession（本注释自述"上位=宪章 §7.1/§7.3"）＋ `SCI §9a-8`；实现 `p3_resample.cpp:217-226` 与本注释逐字一致 | 该守卫在生产面无调用点（`eng/ci/prod_wiring_baseline.json:203` 标 `unreachable:manifest.source_symbols:p3_resample_check_mode`，模块自登记 `DISP-P3RSMP-003`）⇒ 不另立 finding（已在跟踪面登记），但它是"设计了没接线"的实例 | CONFIRMED |
| :32-34 | `P3Sampler{ impl = nullptr }` | nullptr | 句柄初值 | 结构性不适用 | PImpl 句柄 | — | CONFIRMED |
| :44-48 | `max_tiles≤0 → 恢复默认(8)`；"单位 = 512²f32 tile = 1 MiB" | 8；1 MiB | 容量兜底与 tile 字节数换算 | 待确认（8）／结构性不适用（1 MiB） | `docs/algorithms/PHASE3_RSMP_IMPL.md:201,280` 同值 8；1 MiB = 512·512·4 B 复算成立 | 方向：把非法配置读成"用默认"而非拒 ⇒ 静默；影响仅性能 | CONFIRMED |
| :56 | 「真实 M42: 523 tile」 | 523 | 工作集规模叙事 | 待确认 | 注释自述实测，跟踪面无对应件（本路检索：`git grep -rn "523 tile" -- docs/ artifacts/` 未执行，故记缺角） | 方向：叙事值不参与计算 | PARTIAL |
| :64-73 | `P3CacheStats` 八字段 | 0 | 观测计数初值 | 结构性不适用 | 观测面；语义标注问题见 CR-59-11（`open_failures` 被注释为"不存在文件"却计所有失败） | — | CONFIRMED |
| :76-80 | `absent_cache` 默认 | 1（开） | 负缓存开关 | 待确认 | 注释「默认 1=开…生产路径不得关闭」，无配置键、无合同登记（键面检索：`git grep -rn "absent_cache" -- eng/packaging/ docs/contracts/` 本路未执行 ⇒ 记缺角） | 方向：默认站在"把失败当缺失"一侧 ⇒ 见 CR-59-11 | PARTIAL |
| :82-89 | nearest 的 (value, coverage) 与计数关系 | C=1/C=0；`n_rejected_nonfinite` = 0/1/0 | 单样本计数可推导性论证 | 结构性不适用（论证）＋核过成立 | 头 :84-87 自陈「强制计数在单样本下由 (value, coverage) 唯一确定」——复算：C=0 ⇒ 无候选 ⇒ 0；C=1 且 ¬isfinite ⇒ 1；C=1 且有限 ⇒ 0，三态互斥且可由输出面复原 ⇒ 与 `DATA-002 §2a` `rejection_counting=mandatory` 不冲突（mandatory 要求"可区分"，不要求每路径都有出参） | 不立 finding；但见 CR-59-10（**多样本**路径的 w=0 项不满足同类可推导性） | CONFIRMED |
| :91-98 | bilinear 语义（缺 tile→C=0+NaN；非有限→掩膜+重归一；零合格→NaN+C=1） | — | 采样核口径 | 文献值 | `PHASE3_RESAMPLE.md:43-58,120-133` 逐条对应；实现符合（除 CR-59-08/09/10 三处） | — | CONFIRMED |
| :107-111 / :116-120 | `P3UncertaintySource` / `P3UncPixelState` | NONE=0,VARIANCE=1,IVAR=2 / OK=0,NAN=1,MISSING=2 | 来源与状态三态码 | 结构性不适用 | 枚举基数；`MISSING` 与 `NAN` 分列符合 `DATA-002 §2a` 规则 3 的"分列"要求 | — | CONFIRMED |
| :137-139 | 角序 `[0][0],[1][0],[0][1],[1][1]`；退化角"重复 leaf 计入多次，Σc_k=1 保持" | 4 | 出参数组维度与角序合同 | 结构性不适用 | 与实现 `p3_resample.cpp:417-418,457-460` 一致 ✓ | 注：本行承诺"重复 leaf 计入多次"，与 `DATA-002:126` 的"合格样本 = 值有限 ∧ w>0"在 w=0 重复角上冲突 ⇒ CR-59-10 | CONFIRMED |
| :141-142 | `c'_k = w_k / Σ_{合格} w_j`；"全部合格时与几何权重逐值同量级（重归一为恒等，差异 ≤ k·ULP）" | — | 重归一式 | 公式导出 | 复算：全合格时 `wsum = Σwg = 1 ± k·ULP`（§3 表 :417-418 行已证恒等式）⇒ `c'_k = w_k/(1±k·ULP)`，与该句一致 ✓；ALG-P3-003 :172-173 同口径 | — | CONFIRMED |
| :164-170 | `P3SampleRejection` 五字段 | 0 | 强制计数结构与"三项互斥可加"承诺 | 结构性不适用（字段）＋**内容不成立** | 见 CR-59-10：`n_rejected_nonpositive_weight` 被注释断言"恒 0"，而该断言与 :168 自己的定义「权重非有限或 **≤0**」及 `DATA-002:126` 的 `w>0` 判据互斥 | — | CONFIRMED |
| :181-195 | 不确定度传播块：`npts=1: var=u`；`npts=4: var = Σ c_k²u_k`；"Σc_k²≠1 是正确物理"；`ivar==0→NaN` | 1、4 | 该面的科学权威声明 | **公式导出（特例式本身）＋适用域条件缺失** | 上位为 `docs/science/UNCERTAINTY_AND_COVARIANCE.md:85-101`，但本块只抄了特例、**未抄"该式的适用域 = C_in 对角"这一条限定**（原文 :100-101）；也未登记"输入相关时须带相关核/近似误差"（`G-P3-COV-01`，`p3_rsmp_failclosed.cpp:170-175`） | 后果见 CR-59-07（同一式在产品出口以低估 23.3%–36.3% 的幅度被使用） | CONFIRMED |
| :195 | "FP64 累加" | — | 精度合同 | 文献值 | `DATA_SEMANTICS:2491`「bilinear 权重 FP64 计算后回落 float32 输出」；实现 :447-454 相符 ✓ | — | CONFIRMED |

## 4.2 findings

### CR-59-13 · `p3_order_select` 选出的叶级 order 从不参与采样，采样恒在产品最深阶；provenance 却把它写成 `order_sel_used`——本头文件明文禁止的就是这个形态
- 轴：C（同一件事，代码／provenance／条文说的不是同一个）为主；命中 D（设计点名能力未接线）
- 位点：`p3_resample.h:37-38`
  ```
  /* 打开并暴露输入实际 order 与 BUNIT: order_sel 上限须来自输入实际 order,
   * 禁止仅写 metadata; ... */
  ```
  与 `p3_resample.h:19-21`「选最小 order L∈[0,max] 使 HiPS 叶级分辨率 ≤ 输出 scale；无更细层时取 max_order」
- 现状（实现侧逐跳，全部为 `文件:行`）：
  1. `p3_resample.cpp:285-286` `s->order = p.order; s->leaf_nside = kTileWidth << p.order;` —— 采样层级**只**由产品 properties 的**最深 order** 决定；`p3_sampler_open_ex` 通过 `out_order`（:290）把它交出去。
  2. `module_adapters.cpp:14172-14175` `int input_order = 20; … p3_sampler_open_ex(…, &input_order, …)` —— 拿到的就是产品阶；`:14237-14238` 再算 `p3_order_select(input_order, g.scale, &order_sel)`。
  3. `order_sel` 此后**只出现三次，全是登记**：`:14541` `{"order_sel", order_sel}`（节点 artifact JSON）、`:14580` `(*man)["order_sel"] = order_sel;`（manifest）、`:14707/14711` `prov.order_sel_used = std::to_string(order_sel)`（provenance 字段名直书 "used"）。
  4. 逐像素读取路径 `p3_resample.cpp:306 / :348` `ang2pix_nest(nside=impl->leaf_nside, …)` 与 `:186-201 read_leaf` 全部使用第 1 步的最深阶 ⇒ 与第 2 步算出的 `order_sel` 无任何数据流连接（`git grep -n "order_sel" -- lib/infrastructure lib/phase3_session` 全量 13 处命中，无一处回灌采样层）。
  5. 会话面同构：`p3_session.cpp:199-200` 算、`:366/:379/:413` 只登记。
- 差在哪：本头文件 :38 明写「禁止仅写 metadata」，而两面生产实现恰是"仅写 metadata"；且 `PHASE3_RESAMPLE.md:30-34`（G3）与 :82-89 把 `order_sel` 的科学职责写成"保证**面平均**尺度 ≥ 请求尺度"——该保证不成立。
- 后果：①**provenance 假陈述**：对外字段 `order_sel_used` 给的是一个没有参与计算的数字。按 provenance 复现该产品的消费方会在错误的层级上取 leaf，字节不复现；这是"产品血缘字段"级别的失真，不是注释级别；②请求尺度粗于产品最深阶时，输出像素是**对最深网格的点采样**而非登记的面平均 ⇒ 高频结构混叠进入 signal，方差侧 `Σc_k²`（:570）也随之偏（点采样的 c 全塌到一角时 Σc²=1，等价于不平均，与"bilinear 平均去相关"的登记物理相反）；③性能面：P30 注记（`p3_resample.cpp:7-9`）把"523 tile 工作集 >> cap 8"归因于 worker 未共享缓存并以此修复，而未归因于层级未被选择——即修复处理了症状，登记面（`p3_resample.h:44-57`）把这套解释又抄了一遍。
- 定级：**S1**（触发路径 1→5 逐跳可指认，失真直接落在产品 provenance 字段与 signal/variance 平面的采样层级语义上）
- 怎么算修好：①让 `order_sel` 真正决定 leaf 层级：`p3_sampler_open_ex` 之后按 `order_sel` 重开/配置 sampler（`leaf_nside = 512 << order_sel`），或在 `read_leaf` 里按 `order_sel` 做 `leaf_to_tile_nest`；②**同批**改 `p3_resample.h:82-89` 的 nearest/bilinear 语义与 §4 边界表，因为改层级会改变哪些 leaf 被读到（若不同批，正确的修复会被既有逐像素判据判红）；③provenance 字段改名或改语义：若维持"点采样最深阶"是有意设计，则必须删掉 `order_sel_used` 并在 ALG-P3-003 G3 撤下"面平均尺度"承诺（这属条文变更流程，需走 §8）；④回归：加一条"provenance 里的 order_sel 与实际读取的 tile 阶一致"的逐格判据（现在无任何判据能区分第 2 步与第 1 步不同）。
- 置信：CONFIRMED（五跳全部为实读行号；未实跑，但结论是"数据流不存在"型，静态即可判）

### 不报的核过项（本文件）
- **`p3_resample_check_mode` 生产无调用点**：属"合同强制无实现/未接线"形态，但已在跟踪面两处登记（`eng/ci/prod_wiring_baseline.json:203` 的 `unreachable:manifest.source_symbols` 与模块 `DISP-P3RSMP-003`，`lib/algorithms/resample/module.yaml:142`）⇒ 不作为本批新 finding，仅在处置表注明。
- **头注释里的日期与裁决引用**（:24-27「2026-09-09 冻结」、:102-106 同款）：违反负责人"文档内禁日期与历史"口径属文档纪律问题，且同一形态已在多批重复出现 ⇒ 不占本批 finding 名额，转汇总面。
- **nearest 无 rejection 出参**：见处置表 :82-89 行的复算，三态可由 (value, coverage) 复原 ⇒ 不构成"静默剔除"。

---

# 5 · `lib/algorithms/resample/p3_rsmp.h`（501 行，全文已读）

## 5.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :14-17 | 单位表（signal_sb=ADU/sr、pixel_variance_in=ADU²、sb_variance_out=ADU²/sr²、sb_ivar_out=sr²/ADU²、W_info=ADU⁻²、Q=ADU⁻¹、flux=ADU、psfsw=1） | 见左 | 冻结单位串 | 文献值 | `docs/contracts/DATA_SEMANTICS.md §31.1`（本注释自指）＋ `:3294` 行把 FZ-UNIT-* 归到该节；`§2696` 另登记「Phase3 输出 variance BUNIT = (主 HDU signal BUNIT)²，ivar = 1/variance」⇒ 与 :17 一致 | — | CONFIRMED（未逐字复核 §31.1 全文，八串中 quadratic 律一句已在 :2696 复核） |
| :33 | `kNaN` | quiet NaN | 无效值哨兵 | 结构性不适用 | 无效一律 NaN，无 0 冒充 | — | CONFIRMED |
| :38-44 / :47 / :63-71 / :94 / :111-115 / :121-133 / :135-151 / :182-224 / :240-261 / :276-301 / :306-341 / :358-388 / :424-476 | 各 `enum class` 与结构体成员初值 | 0/1/false/true/0.0/kNaN | 默认状态、基数、哨兵 | 结构性不适用 | 枚举基数与布尔默认（多为 fail-closed 侧：`provenance_complete=true` 一类是**开放侧**默认，见下行） | — | CONFIRMED |
| :329, :331, :339, :444-452 | `measurement_capable = true`、`uncertainty_available = true`、`provenance_complete = true`、`psf_sum = 1.0`、`effective_psf_normalization_declared = true` | true / 1.0 | 输入结构体的**默认站在"合法"一侧** | 待确认 | 全仓无条文规定"缺字段时按可用处理"；相反，`check_failclosed` 的语义是"逐键判定"，默认值即证据 ⇒ 默认 true 会把"调用方忘了填"读成"已声明合格" | 方向：默认 true ⇒ 门偏绿；影响范围：所有走 `propagate_*` 的调用（现仅 v6 shell，未进生产构建） | CONFIRMED（默认值本身）／PARTIAL（未逐个查是否有调用方依赖这些默认） |
| :315-318 | `epsilon_corr_ratified = false`、`epsilon_corr = kNaN` | false / NaN | OPEN 项未签字即 fail-closed | 结构性不适用（守卫）＋合规 | `eng/contracts/data/v6_clause_registry_v1.json:2093,2552` 登记 CF-T-P3-CORR-EPSILON 为 OPEN/SO-07；`p3_rsmp_failclosed.cpp:183-190` 两条 `Unavailable` 分支实装 ⇒ **不自行定值**，符合 §6 硬禁令 | — | CONFIRMED |
| :320 | `psf_sum_tol = 1e-9` | 1e-9 | effective PSF 归一 `|Σπ−1| ≤ tol` 的绝对阈 | 待确认 | 头注释自称「数值守卫（实现局部，不是科学阈值）」；唯一消费者 `p3_rsmp_failclosed.cpp:139` 与 `p3_rsmp_propagation.cpp:209`。全仓无推导/标定件给该值（检索式：`git grep -rn "psf_sum_tol" -- docs/ eng/contracts/` 本路未执行，故记缺角） | 方向：`|Σ−1|≤1e-9` 是**绝对**阈，与 `PHASE3_RESAMPLE.md:141` 的 `1 ± k·ULP` 语言同源但数值未导出；取小⇒易红、取大⇒漏归一缺陷。影响：ALG-P3-007 的 `Σpi=1` 门 | PARTIAL |
| :321 | `row_sum_tol = 1e-9` | 1e-9 | 自称"行归一数值守卫" | **待确认＋零消费者** | 见 CR-59-14。检索式：`git grep -rn "row_sum_tol" -- 'lib/**' 'eng/**'` → 全仓唯一命中即本行 | 方向：无消费者 ⇒ 行归一**无**数值判据 ⇒ 偏绿 | CONFIRMED |
| :322 | `bunit_tol = 1e-12` | 1e-12 | 自称"BUNIT 数值守卫" | **待确认＋零消费者** | 同上：`git grep -rn "bunit_tol" -- 'lib/**' 'eng/**'` → 唯一命中本行 | 同上 | CONFIRMED |
| :264 | `make_bilinear_4quad_neighborhood` 的注释定义 | — | 「权重为线性距离分数 w，Σw=1。**重叠面积记为 w·Ω'_i（使 R_ij = w_ij）**」 | 公式导出（自指定义） | 无外部出处：把"非面积量"通过定义记为面积量 | 方向：与 CR-59-09 同族——定义句直接决定 `R` 的语义，任何后续"面积守恒"推论都建立在被定义而非被证明的等式上 | CONFIRMED |
| :226 / :228 | `R_ij = a_ij/Ω'_i`；`S_ij = a_ij/Ω_j`；`S = R·Ω'/Ω` | — | 行/列归一算子 | 公式导出 | 头 :7-8 自指 ALG-P3-002/SPEC §1.3；`docs/contracts/DATA_SEMANTICS.md:2659-2662` 独立登记「面积元 = 逐像素取值（常数 Ω 近似的 flux 偏差可达 97.5%）」；本式与该登记同形（分母取各自 Ω）✓ | — | CONFIRMED |
| :10-11 | `Q = a·πᵀC_y⁻¹f`、`W = a²·πᵀC_y⁻¹π` | — | 输出帧 Q/W 重算 | 公式导出 | `DATA_SEMANTICS.md:2683-2685`（FZ-P3-QW-RECOMPUTE）逐式同形；实现 `p3_rsmp_propagation.cpp:246-249` 真按式算（Cholesky 解 `C_y z = π`，非对角近似）✓ | — | CONFIRMED |
| :121-133 | `KernelOracleEvidence` 六字段（含 `max_interp_err`/`analytic_bound`/`err_over_bound`/`max_weight_sum_dev`/`boundary_fail_closed`/`zero_fill_error`） | kNaN/false | 核注册的"证据块" | 见 CR-59-15（D 轴缺陷） | 唯一在树填充是字面量：`p3_rsmp_kernel_registry.cpp:189-194`（`0.027395522883651657`、`1.0/36.0`、`0.9862388238114597`、`0.0`、`true`、`1.3223`） | 方向：接口允许调用方自证 ⇒ 门恒可满足 ⇒ 偏绿 | CONFIRMED |
| :8 / :274 | 「covariance 传播 `C_y = R C_x Rᵀ`（FZ-FORMULA-COV-PROP；**唯一来源**，禁权重标量反推）」 | — | 唯一来源主张 | 文献值（式）＋**主张不成立** | 式本身有登记（`DATA_SEMANTICS:2692-2696`）；"唯一来源"在产品面被 CR-59-07 打破（生产出口走另一条标量路径且不经本门） | — | CONFIRMED |

## 5.2 findings

### CR-59-14 · 两个被声明为"数值守卫"的容差（`row_sum_tol`、`bunit_tol`）在全仓零消费者；被它们守住的"R 行归一"属性实际只是 builder 打的一个标签
- 轴：D（点名能力未做）＋ R（判据不存在）
- 位点：`p3_rsmp.h:319-322`
  ```
  // 数值守卫（实现局部，不是科学阈值；不得覆盖任何冻结值）。
  double psf_sum_tol = 1e-9;
  double row_sum_tol = 1e-9;
  double bunit_tol = 1e-12;
  ```
  与 `p3_rsmp.h:226`「R_ij = a_ij / Ω'_i（ALG-P3-002/SPEC §1.3）」、`:217` `bool row_normalized = false;`
- 上位依据：`p3_rsmp.h:7`「统一线性模型：y = R x（**R 行归一**）」与 `docs/contracts/DATA_SEMANTICS.md:2659-2662` 对面积元逐像素取值的要求（行归一是通量/面亮度语义成立的前提）；`docs/algorithms/PHASE3_RSMP_IMPL.md` 未给出这两个 tol 的推导（本路检索式：`git grep -rn "row_sum_tol|bunit_tol" -- 'lib/**' 'eng/**' 'docs/**'`，前两者仅命中声明行）。
- 现状：①`row_sum_tol`、`bunit_tol` 无任何读取点（全仓各 1 处命中＝声明本身）；②`propagate_surface_brightness` 的守卫是 `if (!r_op.row_normalized) REJECT "operator_is_not_row_normalized"`（`p3_rsmp_propagation.cpp:86-91`），而该布尔由 `from_entries(..., bool row)` 无条件写入：`p3_rsmp_operator.cpp:56` `op.row_normalized = row;`（`build_row_normalized` 传 true、`build_column_normalized` 传 false）⇒ 对 `build_row_normalized` 的产物该判据恒不触发；③可用于判决的 `row_sums()`/`col_sums()`/`full_coverage(tol)` 三个方法只在测试里被调（`eng/tests/unit/v6_p3_rsmp/p3_rsmp_core_test.cpp:39-45,64-65,348-349`），生产/库内零调用。
- 差在哪：一条被冻结为语义前提的属性（R 行归一）在本模块里既没有数值判据、也没有可信的布尔凭据；被声明存在的两个守卫是死字段。
- 后果：任何使行和不为 1 的构造（例如重叠面积被后续改动、`missing_contributors>0` 但权重仍被累加、Ω'_i 与 Ω 不一致）都不会被 `propagate_*` 拒绝，而是产出一个"看起来归一"的 y 与 c_y；对外主张"FZ-P3-KERNEL-REGISTRY 要求核带误差界/边界/归一语义"退化为注册表里的字符串。**注**：本条的影响面在 v6 链（未进生产构建，`spec_named_impl_gaps.json#SNI-S4-P3X-06`），故不产生产物错值。
- 定级：S2（缺生产触发路径，已注明）
- 怎么算修好：①`propagate_surface_brightness` / `_point_source_flux` 在数值段开始前逐行断言 `|Σ_j R_ij − 1| ≤ cfg.row_sum_tol`（对 valid_out 为真的行），把标签降级为派生量（`row_normalized` 由该断言算出而非由 builder 声明）；②`bunit_tol` 若确无用途则删除，别留"看起来有守卫"的字段；③同批在 `p3_rsmp_gate_test.cpp` 补一条变异用例（把某行权重乘 1+1e-6 ⇒ 必 REJECT），否则修完仍无判据承载。
- 置信：CONFIRMED（三次 git grep 全仓计数 + 三处实读行号）

### CR-59-15 · 核注册门"独立 Oracle + 误差界 + 边界定义"的判据是调用方自填布尔与字面量，注册面用一组无产生载体的常数放行 `bilinear_4quad`
- 轴：D（设计了没实现——门只看夹具/自述，不看证据件）
- 位点：`p3_rsmp.h:121-133`（`KernelOracleEvidence`：`OracleKind kind; bool ok; bool independent; std::string oracle_id; double max_interp_err; …`）与 `:169` `GateResult validate_registration(const KernelDescriptor& k) const;`
- 上位依据：`p3_rsmp.h:11`「bilinear_4quad 须独立 Oracle+误差界+边界」；`docs/contracts/DATA_SEMANTICS.md:2672`（FZ-P3-KERNEL-REGISTRY 表）「`bilinear_4quad` | 须独立 Oracle + 通量/面亮度语义 + 误差/边界定义后注册」；`:2674`「`status != registered` 用于生产、或 `registered` 而无 `oracle_ref`/`boundary_definition` → REJECT」。
- 现状：`p3_rsmp_kernel_registry.cpp:90-95`（`validate_registration` 的 Oracle 段）判的是 `o.kind == OracleKind::None || !o.ok || !o.independent || o.oracle_id.empty()` —— 三个布尔/字符串，**不复核** `max_interp_err`、`analytic_bound`、`err_over_bound` 是否由任何实验产生，也不与 `error_bound_expression` 比较。唯一的在树填充是硬编码常数：`max_interp_err = 0.027395522883651657`、`analytic_bound = 1.0/36.0`（同行注释给式 `(h^2/8)(max|Fxx|+max|Fyy|)=0.0277777...`）、`err_over_bound = 0.9862388238114597`、`max_weight_sum_dev = 0.0`、`zero_fill_error = 1.3223`。这些数的产生件在跟踪面未被本路找到（`oracle_id = "ALG-P3-001-KERNEL-ORACLE/bilinear_4quad"` 不是路径）。
- 差在哪：合同要求的三件事（独立 Oracle／误差界／边界）被实现为"描述符自述"，注册门对内容的判别力为 0；`err_over_bound = 0.986`（<1 即"界成立"）**没有任何一条代码去比较它与 1**，也就是说连这条自证都没被使用；而 `analytic_bound` 的注释式 `(h²/8)(…)` 与 CR-59-09 已证的生产核（斜四边形上的轴对齐双线性）不是同一个算子的界。
- 后果：对外"采样核经版本化 registry 注册并带独立 Oracle"这一主张（`DATA_SEMANTICS:2665-2674` 整节）目前由一组源码字面量支撑；`0.9862388238114597` 这种 16 位精度的"实验结果"写在生产头/源里而无实验件，属本仓已实证过的"入库快照与产物无载体"家族。
- 定级：S2（该链未进生产构建 ⇒ 不产物错值；失真在 FROZEN 合同的支撑面）
- 怎么算修好：①`validate_registration` 增加数值复核：`err_over_bound == max_interp_err/analytic_bound`（自洽）且 `err_over_bound < 1.0`（界成立）且 `oracle_id` 指向**跟踪面存在**的 Oracle 件/测试名（可判伪：给不存在的 id 即 REJECT）；②把这六个常数从源码搬到 Oracle 结果件（`实验/` 或 `artifacts/evidence/`，不得指 `run/`），源码只留 id 引用；③补一条负例：把 `analytic_bound` 改成 0.03 使 `err_over_bound > 1` ⇒ 注册必红。
- 置信：CONFIRMED（门代码与注册常数均逐字读过；"Oracle 件不存在"以本路可见的 `git grep oracle_id` 为准，标 PARTIAL 的缺口是未穷举 `实验/` 目录）

---

# 6 · `lib/algorithms/resample/p3_rsmp_covariance.cpp`（151 行，全文已读）

## 6.1 §2.5 数值处置表

| 位点(文件:行) | 符号或键 | 现行值 | 它是什么 | 处置 | 依据锚 | 保守方向与影响范围 | 置信 |
|---|---|---|---|---|---|---|---|
| :12 | `m(i, i) = 1.0` | 1.0 | 单位阵对角 | 结构性不适用 | 恒等阵定义 | — | CONFIRMED |
| :18-19 | `DenseMatrix(r,c)` 全零初始化 | 0.0 | 累加器初值 | 结构性不适用 | 累加器 | — | CONFIRMED |
| :26 | `c_in.rows != ni || c_in.cols != ni → DenseMatrix()` | 0 维 | 维度不自洽哨兵 | 结构性不适用 | 调用方按 `rows != n_out` 转 Reject（`p3_rsmp_propagation.cpp:102-107,216-221`）⇒ **fail-closed 成立**（空矩阵不会伪装成合法 C_y） | — | CONFIRMED |
| :28-46 | `M = R C_x`（:33 `k < ni`）、`C_y = M Rᵀ`（:43 `o < no`） | — | 一般式两段乘 | 公式导出 | `C_y = R C_x Rᵀ`（`DATA_SEMANTICS:2693` 逐式，含 `2Σ_{j<k}` 交叉项）。**复算**：第一段 `m(o,k) = Σ_e R_e·c_in(i_e,k)` = Σ_i R_oi C_ik ✓；第二段 `cy(o,p) = Σ_e m(o,i_e)R_ep` = Σ_i M_oi R_pi ✓ ⇒ 与登记式同形、按式计算，未走对角近似。⇒ 本文件的**主式没有错**（派单重点③对本文件的假设不成立，缺陷在生产另一条路径＝CR-59-07） | — | CONFIRMED |
| :51 | `n = min(rows, cols)` | — | 对角提取长度 | 结构性不适用 | 方阵情形下等价 | — | CONFIRMED |
| :63 | `sd[i] = (v > 0.0) ? sqrt(v) : kNaN` | 0.0 | 标准差与"对角非正"归类 | 结构性不适用（守卫方向） | NaN 而非 0，方向保守 ✓ | — | CONFIRMED |
| :68 | `rho(i,i) = (sd[i] > 0.0) ? 1.0 : kNaN` | 1.0 / NaN | 自相关 | 结构性不适用 | 定义 | — | CONFIRMED |
| :71 | `rho(i,j) = (den > 0.0) ? cy(i,j)/den : **0.0**` | 0.0 | 对角非正时的"未知相关" | 见 CR-59-16 | 无条文：`p3_rsmp.h:290-291` 只声明函数，未规定不可知时的返回值；`docs/science/UNCERTAINTY_AND_COVARIANCE.md` 无该规定 | 方向：不可知被写成 0 ⇒ 下游 `max|ρ|` 型判据只会偏小 ⇒ **偏绿** | CONFIRMED |
| :80, :86 | `mx = 0.0`；`if (std::isnan(v)) continue;` | 0.0 | 最大非对角相关的跳过式累加 | 见 CR-59-16 | 同上 | 同上（两条抹掉未知的路径同向） | CONFIRMED |
| :94 | `a.rows != a.cols || a.rows <= 0` | 0 | Cholesky 入口守卫 | 结构性不适用 | 非方阵/空阵显式拒 ✓ | — | CONFIRMED |
| :101-115 | Cholesky 主循环：`sum = a(i,j) − Σ_k L(i,k)L(j,k)`；对角 `!(sum > 0.0) || isnan(sum)` ⇒ 拒；非对角 `sum / L(j,j)` | 0.0 | 标准左下三角分解 | 公式导出 |教科书级 Cholesky–Banachiewicz；复算逐行同式 ✓。守卫方向 `!(sum>0)` 含 NaN 与 ≤0 ⇒ 非正定即拒 ✓（`std::isnan(sum)` 为冗余但无害） | 注：只读**下三角**，不对称的 `c_in` 被静默按下三角处理（见"不报的核过项"） | CONFIRMED |
| :122-147 | `cholesky_solve`：尺寸不符→InvalidArgument；非正定→Reject；前代/回代 `i = n-1 … 0` | 0 | 线性求解 fail-closed 链 | 结构性不适用 | 头 :299「失败（非正定/奇异）→ fail-closed，禁伪逆静默」实装 ✓；被 `p3_rsmp_propagation.cpp:225-239` 用于 `C_y⁻¹π`、`C_y⁻¹f`（Q/W 重算）⇒ 无回退分支 | — | CONFIRMED |
| :135-146 | `y`/`z` 初值 | 0.0 | 累加器 | 结构性不适用 | — | — | CONFIRMED |
| 全文件 | 复杂度 | O(nnz·n_in + nnz·n_out) 时间、O(n²) 空间 | 稠密传播的规模 | 结构性不适用（实现选择）＋见"需要实跑" | 登记面 `docs/algorithms/PHASE3_RSMP_IMPL.md:330`「内存: cache 上限 …」只管 tile 缓存，**无**对 C_y 规模的预算条款 | 方向：一旦接真实帧（10⁶–10⁸ 像素）即不可用；现由"该链未进生产构建"掩盖（`spec_named_impl_gaps.json#SNI-S4-P3X-06`） | PARTIAL |

## 6.2 findings

### CR-59-16 · 相关不可知（对角非正 / NaN）被折叠成 ρ=0 并跳过，使 `max_abs_offdiag_correlation` 单调偏小；同族第二处是 `c_in` 的 NaN 静默传播
- 轴：R（真实输入下得到什么）＋ S（无效值语义未按本仓"取不到值不得落在合法好值一侧"的口径）
- 位点：`p3_rsmp_covariance.cpp:71` `rho(i, j) = (den > 0.0) ? cy(i, j) / den : 0.0;` 与 `:85` `if (std::isnan(v)) continue;`
- 上位依据：`p3_rsmp.h:290-291` 只声明 `DenseMatrix correlation_kernel(const DenseMatrix& cy);` / `double max_abs_offdiag_correlation(const DenseMatrix& cy);`，无值语义条文；本仓已定口径是"取不到值一律显式失败，不得落到合法好值一侧"（`docs/standards/NUMERIC_STANDARD.md §MUST「NaN/Inf 契约」`，被 `p3_resample.h:152-159` 指为三处同口径权威之一）。相关核/近似误差面另有显式 OPEN 条款：`p3_rsmp.h:316-318` 与 `p3_rsmp_failclosed.cpp:183-190`（未签字即 `Unavailable`）。
- 现状：两种"未知"（① `sd_i·sd_j ≤ 0`，含方差为 0、为负、为 NaN；② 算出的 ρ 本身是 NaN）分别被写成 `0.0` 与被 `continue` 跳过，都不进统计量，也都不上抛状态。`max_abs_offdiag_correlation` 的返回类型是 `double`（无 Status 出参），调用方**无法**区分"真的没有相关"与"相关不可知"。
- 差在哪：同一模块对"未签字的近似相关面"的态度是 fail-closed（`Unavailable`），对"不可知的逐对相关"的态度是报 0——两个方向相反的处置；且该统计量正是用来论证"对角近似可接受"的量（`epsilon_corr` 的比较对象）。
- 后果：若 `epsilon_corr` 一旦签字并按 `max|ρ| ≤ ε` 形式判决（这正是 CF-T-P3-CORR-EPSILON 的用途，`v6_clause_registry_v1.json:2093` 主题「Phase3 相关核近似误差阈值 epsilon_corr 具体数值」），则任何把方差算成 0/NaN 的输入（CR-59-11 的缺 tile、零合格像素，以及未刷新的方差行）都会使 `max|ρ|` 归 0 ⇒ 判据**必然**通过，即"最坏情况=最好分数"。方向钉死：跳过项使统计量只减不增 ⇒ 偏绿。当前该函数唯一消费者是测试（`eng/tests/unit/v6_p3_rsmp/p3_rsmp_oracle_test.cpp:78`），故不产生产物错值。
- 定级：S2（缺生产触发路径——函数只被测试调用；一旦 `epsilon_corr` 签字接上门即升 S1）
- 怎么算修好：①`correlation_kernel` 在 `den <= 0` 时置 `kNaN`（与对角线一致），`max_abs_offdiag_correlation` 改为返回 `Status + double`（或在遇到任何 NaN 时直接 `Reject`），禁止把未知折成 0；②补一条负例：给定 `C_y` 某对角为 0 ⇒ 现实现返回 0.0（判绿），修后必须 Reject；③与该量的**用途**同批改：签字 `epsilon_corr` 时判决式必须写成"含不可知即红"，否则第①步会把既有测试判红（`p3_rsmp_oracle_test.cpp:78` 现在只喂合法正定阵，不会因此红——修前先跑该件确认）。
- 置信：CONFIRMED（两条抹除路径与消费面均已实读；判决式方向为推理复算）

### CR-59-17 · `propagate_covariance` 对 `valid_out`（缺 tile/邻域不完整）完全无感知，SB 面只把有效性"记录"而不参与判决
- 轴：R＋D
- 位点：`p3_rsmp_covariance.cpp:22-48`（整个函数体不含 `op.valid_out` 的任何引用；`grep valid_out` 在本文件命中数 0）
- 上位依据：`p3_rsmp.h:193-201`「missing_contributors>0 表示缺 tile/越界，按 BoundaryPolicy::MissingIsNaN 输出 NaN 且**不零填**（C-P3-PROP-6）」；`:214` `std::vector<bool> valid_out; // false → 输出 NaN（fail-closed）`；`p3_rsmp.h:226`「缺 tile → valid_out=false，不零填」。
- 现状：`out.y = apply_operator(r_op, in.x)`（propagation:100）负责把无效行给 NaN（该行为在 operator.cpp 内，本批未审），而 `out.c_y = propagate_covariance(r_op, in.c_in)`（:101）**无条件**为所有行算出有限协方差，`out.variance = matrix_diagonal(out.c_y)`（:108）直接取对角，`:109` 才把 `valid_out` 记进 `out.valid`。对比 `propagate_point_source_flux`：:169-176 逐行检查 `valid_out`，任一为假即 `Reject "output_frame_has_invalid_pixels_missing_tile"` ⇒ 同一模块内两个模式对同一标志的处置不同（一个判决、一个只记录）。
- 差在哪：面亮度模式的 VARIANCE 平面可以在 signal=NaN 的像素上带**有限正**方差；且该行由不完整邻域算出（缺项的权重未被累加，见 `p3_rsmp_operator.cpp:60-63`「非正重叠不贡献」），即 c_y 行本身不代表任何完整测量。合同说"无效即 NaN、禁零填"，实现是"signal NaN、variance 给个有限数"——这是"合法值冒充未测"在产品面的另一种形态（虽然该面当前未进生产构建）。
- 后果：SB 模式的 VARIANCE/IVAR HDU 在缺 tile 区域给出可信外观的方差，与 COVERAGE=0 平面自相矛盾（`p3_v6_export.cpp:632-636` 确实同时写 SIGNAL/VARIANCE/COVERAGE 三面）；任何按 ivar 加权的下游会把 C=0 区当有界噪声处理。
- 定级：S2（生产不可达：`p3_v6_export.cpp` 不在生产 target，已在 `spec_named_impl_gaps.json#SNI-S4-P3X-06` 登记 ⇒ 写不出"当前产物错值"的逐跳链）
- 怎么算修好：①`propagate_covariance` 保持纯数学，但在 `propagate_surface_brightness` 内对 `!valid_out[i]` 的整行/整列置 NaN（对角与非对角一致），与 point_source 侧对齐改为判决；②同批补 v6 e2e 的逐格断言：`C=0 ⇒ variance=NaN ∧ ivar=NaN`（现在只有求和闭合型断言的话会漏）；③若决定 SB 面永远只在完整覆盖下发布，则改为在 `check_failclosed` 里加一条与 point_source 同款的 Reject，并在 `p3_rsmp.h:343-352` 的 `SurfaceBrightnessResult` 注释里订正 `variance` 的语义。
- 置信：CONFIRMED（两模式处置差异与三面写出均实读）

### 不报的核过项（本文件）
- **`propagate_covariance` 与登记式是否同形**（派单重点③对本文件的直接设问）：**同形**，两段稀疏-稠密乘即 `R C_x Rᵀ`，未假设对角、未用权重标量反推 ⇒ 不报；跨阶段"相关性被静默丢弃"确实发生，但发生在 `p3_resample.cpp:570` 那条并行路径上（CR-59-07），不在本文件 ⇒ 派单对本文件的预设不成立，已明写。
- **Cholesky 只读下三角、不校验对称性**：核过调用面——`c_y` 由本文件 :43-45 生成（结构上对称到舍入），`c_d` 由 `p3_rsmp_propagation.cpp:193-199` 的 `diag(Ω)C diag(Ω)` 生成（保持对称），无外部非对称输入通道 ⇒ 不单立 finding；若将来 `C_x` 允许外部注入（合同里 `ExactFull` 允许），此点需回来加一条对称性断言。
- **`DenseMatrix()` 空阵被调用方转 Reject**：核过 propagation:102-107/216-221 两处，方向正确，不报"维度守卫形同虚设"。

---

# 收尾一 · 本批最高危三条（只挑，不复述）

1. **CR-59-07**（`p3_resample.cpp:570`，S1）：生产 export 的 VARIANCE/IVAR 用 `Σc_k²·u_k` 对角特例，把输入像素间相关**静默丢弃**；本仓自己的冻结门 `G-P3-COV-01`（`p3_rsmp_failclosed.cpp:170-175`）对同一表示判 REJECT，两份权威文档对此偏差幅度还各说一个数（低估 23.3% vs 36.3%，且 1+0.75ρ 谁都复算不出这两个数同时成立）。直接打在创新点②（绝对 SNR 传递链）在产品出口的可信度上。
2. **CR-59-13**（`p3_resample.h:37-38` ↔ 实现，S1）：`p3_order_select` 选的叶级 order 只被写进 manifest/provenance 的 `order_sel_used`，采样恒在产品最深阶；本头文件 :38 的原文恰是「禁止仅写 metadata」。后果是一条对外血缘字段为假陈述，且"面平均尺度"承诺（`PHASE3_RESAMPLE.md:89`）不成立。
3. **CR-59-09**（`p3_resample.cpp:409-418`，S1）：四角权重不含 `q[1][1]` 自身坐标 ⇒ 不是登记面所说的"面积重叠分数"；registry 又把 `bilinear_4quad` 标为 `production_science_default=false`、把 `bilinear_area_overlap_exact` 标为生产科学默认 ⇒ 生产跑的是登记面认定"不当生产默认"的那个核，且选路只按字符串从不查 registry。

次高（未入选三条但同批）：CR-59-11（读失败与"文件不存在"合并进永久负缓存）、CR-59-08（生效权重合同承诺未兑现，靠调用方零初始化兜住）、CR-59-16（不可知相关折成 0，为将来的 `epsilon_corr` 判决预留了"最坏=最好分数"的口子）。

# 收尾二 · 我读不动 / 需要实跑才能定的（含该跑什么）

| 未定案项 | 缺的是什么 | 该跑/该读什么（本节点禁跑，故只登记） |
|---|---|---|
| CR-59-12 的"象限退化域"在真实产品上占多少像素 | 静态可判代码事实已定，缺覆盖面统计 | 对 face 角与 \|dec\|→85° 的 export 逐像素统计 `n_quadrant_filled`（先按 CR-59-12①加字段，再跑 `eng/tests/validation/release02/q3_additive_truth/` 同族的高纬用例） |
| CR-59-16 修好后 `p3_rsmp_oracle_test.cpp` 是否仍绿 | 该件只喂合法正定阵，改语义可能连带红 | `ctest -R p3_rsmp_oracle`（经 `eng/tools/monitoring/mem_guard.py --max-rss-gb 4`） |
| `p3_rsmp_kernel_registry.cpp:189-194` 六个 16 位常数的产生件 | 跟踪面未找到 Oracle 载体（`oracle_id` 非路径） | 先 `git grep -rn "ALG-P3-001-KERNEL-ORACLE"` 穷举 `实验/`，若零命中则要求补 `实验/` 内可复现件（按三留一不留口径，产物不落 `run/`） |
| CR-59-07 的偏差幅度到底多少（23.3% 还是 36.3% 还是别的） | 两文档互斥且都给不出可复算链 | 用 `docs/science/UNCERTAINTY_AND_COVARIANCE.md:21-26` 的 `Cov(S_p,S_q)=Σ_j c_jp c_jq v_j` 自己按 export 的 4 角核重算一次 MC，固定 seed 落 `实验/`，然后把两处数字收敛为一个 |
| `C_y` 稠密传播的真实规模上界 | 无内存/规模条款（`PHASE3_RSMP_IMPL.md:330` 只管 tile 缓存） | 把 `p3_v6_export` 接进构建后跑 n_out=10⁴/10⁵ 两档，记录 RSS 与耗时（`mem_guard --max-rss-gb 8`） |
| `test_p3_wcs.py` 的 ad-hoc `g++` 在双平台是否可运行 | 只读面无法判（该件不注册，从未在 CI 跑过） | 注册后跑 `ctest -R p3_wcs_python_oracle`（本批未注册，见 CR-59-01） |
| `p3_resample.cpp:412-414` 的 `1e-300` 退化守卫是否有出处 | 条文面零提及，且不可达性未证 | 需证明"象限空 ⇒ dx≠0 恒成立或 u=0 恒安全"，属数学证明不是实跑 |
| `docs/standards/NUMERIC_STANDARD.md §MUST`「NaN/Inf 契约」全文 | 我只读了 `p3_resample.h:152-159` 对它的转述 | 直接读该标准文件的 §MUST 段，以判定 CR-59-16/CR-59-08 的"取不到值不得落合法好值一侧"是否为逐字条文（我按转述引用，已标 PARTIAL 之处） |

# 收尾三 · 派单与清单差异

1. **两个路径不存在**（以清单为准，实际对象已定位）：派单清单第 1、2 项写 `lib/algorithms/projection/p3_wcs_main.cpp`、`lib/algorithms/projection/p3_wcs_test.cpp`；跟踪集里 `lib/algorithms/projection/` 目录下**没有**这两个文件（`git ls-files lib/algorithms/projection/` 全量 18 项已列，见 §1 开头），同名两件的唯一跟踪实例在 `lib/algorithms/projection/tests/p3wcs/`（W4-A9 批次 1 迁入，`tests/p3wcs/CMakeLists.txt:3-7` 自述）。本批按同名唯一实例审阅。**顺带发现**：登记面对该探针仍用三个不同旧路径引用，其中两个不在跟踪集 ⇒ CR-59-01。
2. **派单重点①（插值核与归一化）**：**部分成立**。归一化侧无缺陷——`Σwg ≡ 1` 与掩膜后 `Σc'_k = 1` 均按式成立且逐字对上 `PHASE3_RESAMPLE.md:43,141`；核的**几何含义**侧不成立 ⇒ CR-59-09（登记"面积重叠分数"、实现是轴对齐合成矩形双线性、registry 另设 area-exact 为生产默认）。
3. **派单重点②（边界/外域/支持度为零给什么值）**：**大部分不成立**。本批 6 件在缺 tile / 零合格 / ivar==0 / u 值域非法四条路径上一律给 NaN（`p3_resample.cpp:309,394,407,443,530,536,575,579-580`），并显式禁零填（`PHASE3_RESAMPLE.md:112`），未以合法值冒充未测。**例外两处**成立并已报：CR-59-08（被剔除槽位 `weights[k]` 不写 ⇒ 垃圾可冒充生效权重）、CR-59-16（不可知相关折成 0.0 冒充"无相关"）；CR-59-17 是同一形态在未进构建那条链上的实例。
4. **派单重点③（协方差是否与冻结式同形、跨阶段是否静默丢相关）**：**对本批文件 `p3_rsmp_covariance.cpp` 不成立**——`propagate_covariance` 与 `C_y = R C_x Rᵀ` 同形且未假设对角（复算两段乘，见 §6.1 表 :28-46 行）。**"跨阶段静默丢弃相关性"成立，但位置在另一条实现**：生产 export 走 `p3_resample.cpp:570` 的标量特例（CR-59-07），带门的那条（`p3_v6_export.cpp` → 本文件）未进生产构建（`eng/ci/ledgers/spec_named_impl_gaps.json#SNI-S4-P3X-06`）。
5. **派单重点④（主程序与测试件）**：`p3_wcs_test.cpp` 已注册（`add_test(NAME p3_wcs)`）、读真实生产符号、能红（含对生产丢 +1 桥接有牙的 :310）；`p3_wcs_main.cpp` 无构建载体且其唯一消费者脚本不注册 ⇒ CR-59-01；测试件内的"尺寸溢出检查"断言的是自带副本 ⇒ CR-59-02；负向注入开关无注册载体且注入在期望侧 ⇒ CR-59-03。
6. **派单重点⑤（每个阈值/阶数/缓冲常数五档处置）**：已按 §2.5 逐文件交表，共 137 行处置项（14+35+45+16+13+14，含合并行内列出的全部位点）。锚未指 `run/`：本批文件里凡依赖"紧门 1e-8 / 全域门 1e-6 / min_scale 0.9″/px 的推导"处，其锚实际在被测库 `p3_wcs.cpp:205-210` 的注释里，写的是 `run/GATE-DERIVE-01/REPORT.md` 与 `evidence/kernel_roundtrip.json` 两件套——**按本派单口径两枚皆不合格**：前者指 `run/`，后者在跟踪集里不存在（`git ls-files | grep -i kernel_roundtrip` → 0 命中）。因该常量所在文件不在本批清单，我只在 §2.1 表 `kScale`/`kFrozenGatePx`/`kDegToRad` 等行以"待确认＋缺角"如实标注，未据其判任何一项合规。
7. **清单未覆盖但强相关的两处，只在证据链里引用、未立 finding**（避免越出独占清单）：`p3_rsmp_propagation.cpp`（`propagate_*` 的门与数值段）与 `p3_rsmp_operator.cpp`（`row_normalized` 标签的来源、`apply_operator` 的 NaN 行为）。前者被 CR-59-14/16/17 直接引用为其判决面所在，若后续批次认领该文件，本批的三条 S2 可能随之升 S1（缺的正是生产触发路径那一跳）。

---

# 开工 / 收工 git 状态（原文）

开工（HEAD=`c8f64e9ab6b867e4f108ba1e0073a8f2a97cfde9`）：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

收工：

```text
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

两次一致，与基线两项相同 ⇒ 本路零改动、零新增未跟踪项。本批全部取证为只读（`git status`/`ls-files`/`grep`/`show`/`Read`），未执行任何构建、ctest、`run_checks.py`、`eng/**` 脚本，未 import 仓库 Python，未下载外部文件；所有临时输出只落在审查工作区 `独立审计/证据/通读-CR-59.md`（本文件）。

<!-- PROGRESS: 6/6 -->
