# D9 — 开发节点工单对账（AUDIT-06 只读整治）

**输入件**：`F:\Astro dev\独立审查\整改\out\开发节点工单.csv`（只读，254 数据行 + 表头；本表键 = `对象` + `条目` 两列原样拼接）
**复核基线**：仓库 `F:\Astro dev\Astro CS Normalization Database`，HEAD=`c8f64e9a`（`git rev-parse --short HEAD`）
**开工基线状态**：`git status --porcelain` ⇒ 恰两行 `?? ACSD整治工作包_AUDIT-06.zip`、`?? site/`（与工包要求一致，无额外脏项）
**状态件**：本机 `run/ONEPAGER/STATUS.md` **不存在**（`ls run/ONEPAGER/` ⇒ No such file or directory）⇒ 按工包规定改以仓库 `eng/tools/HANDOVER.md` §6/§8 的状态表为准（该文件写于 `21c77319`/`9f50ec72`，即 HEAD 前 1–3 个提交）。
**手段限制**：严格只读；不跑构建/ctest/`run_checks.py`/任何 `eng/**` 检查器；不 `import` 仓库 Python 模块；计数一律 `git grep`（本机 `rg` 不可用，`Grep` 工具报 `spawn rg ENOENT`）。
**判态口径**：`已修` 必须看到被点名的那段行本身按正确口径改写；整段删除、措辞规避、提交标题写"收口/订正"均不作证据。

上一轮（`c8f64e9a` 之前）与本工单生成时刻（工单 CSV 落盘 2026-09-24 22:26）之间，仓库有 21 个提交，其中与本对账直接相关的口径改动集中在
`c4af4136`（文档全仓正向化、无来源常数按裁决订正）、`5f8c237b`（检查器可信化，其提交正文逐条点名"两个并存分母"等）、
`139a2bc4`（排异路由改投/坏列修复/接线面收敛）、`822b9c53`（通量折算断言钉值）、`a6f602fe`（项目标识 ACSD 贯穿，**行号普遍漂移**）。
⇒ 对"已修"的判定必须看内容，不能只看提交标题；对行号锚一律重定位。

---

## 逐条判定（前 90 行）

### ROW 1 — `core/gl_renderer.cpp` :: `R5-18.md::C-03`
- **问题**：球面网格 leaf 键把 nside 硬编码为 64，与 backend 的 `tile_nside`（B19）不同源。
- **原证据位点**：`core/gl_renderer.cpp`（复核依据钉 `:738`/`:888`）、`core/browser_backend.cpp`、`core/gl_renderer.h`
- **现在位点**：`lib/infrastructure/hips_browser/healpix_browser_qt/core/gl_renderer.cpp:738`、`:888`
- **四态判定**：**仍在**
- **取证**：`git grep -n "ang2pix_nest(64" -- lib` ⇒ 恰好 2 命中 `gl_renderer.cpp:738` 与 `:888`，两行内容均为 `vc.leaf_ipix = HealpixMath::ang2pix_nest(64, ra, dec);`。
  同文件 `:1230` 注释仍写"先找 nside=64 子叶"。对照 `browser_backend.cpp:373-379`（`// B19: 用 hiss_header_.tile_nside 替代硬编码 64`）与 `:454-458`（`nside_min = hiss_header_.tile_nside`）⇒ backend 已按 tile_nside 走、renderer 的 leaf 键仍是常量 64，**两条不同源**这一主诉原样成立。
- **置信**：高（同一符号两侧现读，行号与发现时一致）

### ROW 2 — `core/gl_renderer.cpp` :: `R5-18.md::C-06`
- **问题**：数据 bbox 的 RA 跨度不做 0/360 归一，跨零数据得到接近全天的宽度。
- **原证据位点**：`core/gl_renderer.cpp`、`core/gl_renderer.h`、`widgets/sphere_view.cpp`、`core/browser_backend.cpp`
- **现在位点**：`lib/infrastructure/hips_browser/healpix_browser_qt/core/gl_renderer.cpp:1382-1474`（bbox 估算/保存）、`:1627-1649`（未设 bbox 时采样估算）
- **四态判定**：**仍在**（原判级 S1→S3 降级后仍成立；本轮只复核存续性，不重开定级）
- **取证**：`git grep -n "bbox" -- .../core/gl_renderer.cpp` ⇒ 命中 12 处，`1382` 起为"bbox 估算（仅统计有效像素）"、`1408` "更新 bbox (仅有效像素)"、`1469` "保存 bbox"；`git grep -n "0/360\|fmod\|360.0" -- .../gl_renderer.cpp` 在该 bbox 段内未出现任何 RA 归一/跨零处理（对比 `docs/GLOSSARY.md:14` `ra_dec` 条目仍要求 `RA∈[0,360)`）。
- **置信**：中（主证为 bbox 计算段无归一分支，属"读代码未见即无"的否定式取证；行号已重定位）

### ROW 3 — `core/gl_renderer.cpp` :: `R5-18.md::C-12`
- **问题**：显示传递函数三处各自定义，曲线与 MTF 的先后次序相反、asinh 强度参数化不同。
- **原证据位点**：`core/gl_renderer.cpp`、`core/hips_sky_view.cpp`、`core/stf_engine.h`、`core/stf_engine.cpp`
- **现在位点**：`gl_renderer.cpp:243/251/269-271`（球面片元着色器）、`gl_renderer.cpp:302/310/329-331`（四边形片元着色器，逐字复制）、`hips_sky_view.cpp:272-283`（`display_tone`）、`stf_engine.cpp:90`
- **四态判定**：**仍在**
- **取证**：`git grep -n "mtf(\|asinhCompress(" -- lib/infrastructure/hips_browser` ⇒ 两份 GLSL 内各自独立定义 `mtf`/`asinhCompress`（243/251 与 302/310 两处函数体逐字相同），次序均为 `result = mtf(x, uMidtones)` 之后才 `asinhCompress(result, uCompression)`；
  `hips_sky_view.cpp:272-283 display_tone()` 次序相反——先按预设做曲线 `sqrt / log1p(c*x)/log1p(c) / asinh(c*x)/asinh(c)`，`return STFEngine::mtf(y, m)` 后压 MTF；asinh 参数化亦为两套（GLSL 用 `scale=(1-c)/c` 的 `log(x/scale+…)/log(1/scale+…)`，CPU 用 `asinh(c*x)/asinh(c)`）。⇒ "三处定义 + 次序相反 + 参数化不同"逐条现读成立。
- **置信**：高

### ROW 4 — `docs/GLOSSARY.md` :: `L8-术语与死词.md::L8-04`
- **问题**：词典自带 A44 已废的权重式 `frame_quality_weight = support×snr_v²` 且被检查器列为必备术语。
- **原证据位点**：`docs/GLOSSARY.md:15`、`docs/GLOSSARY.md:14`、`eng/tools/check_glossary.py:12`
- **现在位点**：`docs/GLOSSARY.md:15`、`eng/tools/check_glossary.py:12`
- **四态判定**：**仍在**
- **取证**：`git grep -n "frame_quality_weight"` ⇒ 3 文件命中，正文行为
  `docs/GLOSSARY.md:15:| frame_quality_weight | 帧质量权重=support×snr_v²（SCI-CW 域专用，非生产；…）；生产=逐样本 ivar；snr=1.0 一律按 unknown 显式标注 | … |`
  `eng/tools/check_glossary.py:12:REQUIRED = [... "frame_quality_weight", ...]`（仍列为必备核心术语，G1 要求"恰出现一次"）。
  历史核查：`git log -S "SCI-CW 域专用，非生产" -- docs/GLOSSARY.md` ⇒ 该限定语由 `d1ccfa1b`(2026-09-21) 引入，**早于工单生成时刻**，不是本轮整改；`c4af4136` 只把同句尾部"禁止 snr=1.0 伪装 unknown"改为"snr=1.0 一律按 unknown 显式标注"（`git show c4af4136 -- docs/GLOSSARY.md` 的 ±行对照），公式主体与 REQUIRED 项均未动 ⇒ 不构成本条被点名行的改写。
- **置信**：高

### ROW 5 — `docs/GLOSSARY.md` :: `L8-术语与死词.md::L8-08`
- **问题**：`coverage` 在词典里被禁为 `support` 的 legacy alias，在对象模型里是独立 canonical 对象。
- **原证据位点**：`docs/GLOSSARY.md:16`、`docs/design/UNIFIED_MODEL.md:50-51`、`eng/contracts/schemas/unified/*.schema.json`
- **现在位点**：`docs/GLOSSARY.md:16`、`docs/design/UNIFIED_MODEL.md:30/51/56`、`eng/contracts/schemas/unified/coverage.schema.json` + `support.schema.json`
- **四态判定**：**仍在**
- **取证**：`git grep -n "coverage → support" -- docs/GLOSSARY.md` ⇒ `:16`（support 行的 legacy alias 列）。
  `git grep -n "coverage" -- docs/design/UNIFIED_MODEL.md` ⇒ `:30` 对象图节点 `COV["coverage<br/>几何有效域"]`、`:51` 表行 `| coverage | 几何/数据有效域 | 否 |`、`:56`「canonical 数据对象 = 13 个（… / support / coverage / validity / …）」。
  `git ls-files eng/contracts/schemas/unified/` ⇒ `coverage.schema.json` 与 `support.schema.json` **同时存在**（另有 `examples/coverage.example.json`、`negative/n3_coverage_as_rejection.schema-violation.json`）。⇒ 词典禁用面与模型/合同 canonical 面依旧打架。
- **置信**：高
- **备注**：`git ls-files eng/contracts/schemas/unified/ | grep -E "coverage|support"` 原样跑通（工单"复验命令"列的那条命令有效，只是要求以 `^(.*/)` 锚 `schema.json`，实测两文件双双命中）。

### ROW 6 — `docs/GLOSSARY.md` :: `L8-术语与死词.md::L8-14`
- **问题**：`frame_id` 一名两义（canonical=uint64 载荷哈希；P2 coverage 面=`char[64]` 文件名），合同声称的"全仓校验"实际未注册。
- **原证据位点**：`docs/GLOSSARY.md:26`、`docs/contracts/DATA_SEMANTICS.md:98-103`、`lib/algorithms/coverage/include/astro/phase2/sampler.h:94-99`、`coverage.h:34`
- **现在位点**：`docs/GLOSSARY.md:26`、`docs/contracts/DATA_SEMANTICS.md:93-104` 与 `:1117`、`lib/algorithms/coverage/include/astro/phase2/coverage.h:34`、`sampler.h:92-99`、`eng/tools/docs_machine_consistency.py:331`、`lib/algorithms/coverage/module.yaml:90`
- **四态判定**：**仍在**（两个子指控一存一失，主诉"一名两义"仍成立）
- **取证**：
  - 一名两义：`git grep -n "frame_id" -- docs/GLOSSARY.md` ⇒ `:26` 定义 `uint64 = truncated-64(SHA-256…), 与路径/重命名无关`；`git grep -n "frame_id" -- docs/contracts/DATA_SEMANTICS.md` ⇒ `:1117: | frame_id | char[64] | 无量纲标识 | 路径基名截断（:113-118，DISP-COV-002 唯一性风险）；不保证全局唯一 |`，与同文 `:93-100`（§5 载荷哈希、"与路径/重命名无关"）在同一份合同里并存；实现面 `coverage.h:34: char frame_id[64]; // 唯一帧标识（文件名/哈希）` 仍同时挂着"文件名/哈希"两种口径。
  - 子指控"全仓校验未注册"**已不成立**：`git grep -n "frame_id_contract_exact"` ⇒ 合同声明在 `DATA_SEMANTICS.md:104`，实现在 `eng/tools/docs_machine_consistency.py:331`（`results.append(check("frame_id_contract_exact", frame_id_ok, …))`，判据体 `:322-330`）。⇒ 该检查项**在册**。但它只断言 sampler.h/DATA_SEMANTICS 里"无 FNV-1a / 无由输入路径派生"字样并存在 truncated-64/SHA-256，**不覆盖** `char[64]`/基名截断这一面 ⇒ 门恒绿而两名并存，属"门与缺陷正交"。
  - 偏差登记面在：`git grep -n "DISP-COV-002"` ⇒ `docs/algorithms/PHASE2_COVERAGE.md:279/:316`、`DATA_SEMANTICS.md:1117`、`lib/algorithms/coverage/module.yaml:90`。
- **置信**：高（名两侧均现读原文）

### ROW 7 — `lib/infrastructure/scheduler/src/module_adapters.cpp` :: `L3-并发与资源模型.md::L3-05`
- **问题**：帧级内存闸门与调度器内存预算构成两个并存的内存预算源，且文档把 95% 登记为"单一来源"。
- **原证据位点**：`module_adapters.cpp:1970-1971`、`eng/packaging/config/runtime_resources.json`、`CMakeLists.txt`、`memory_budget.cpp`
- **现在位点**：`eng/packaging/config/runtime_resources.json:46-50`、`eng/tools/quality/budget_sources.json`、`eng/tools/quality/check_budget_single_source.py`、`module_adapters.cpp:2235-2236/2253-2270`
- **四态判定**：**已修**
- **取证**：被点名的"两个分母"现在都落在唯一源并逐位比对：
  `runtime_resources.json:46` 新增 `"frame_memory_gate": { … }`，`:50` 的 note 原文为"原先它们只以 constexpr 字面量存在于 module_adapters.cpp，与 memory_budget_percent 构成两个并存分母而文档把本文件登记为唯一预算来源；**现两个分母都登记在本唯一源，并由 CHK-BUDGET-SINGLE-SOURCE 与实现侧字面量逐位比对**"；
  `budget_sources.json` 三条 `denominators`：`scheduler_memory_budget_percent`（source_key `memory_budget_percent`，consumer `memory_budget.cpp` 必须引用 `runtime_resources_generated.h`）、`frame_gate_bytes_per_pixel`、`frame_gate_safety_frac`（consumer 用 `literal_regex "kP1FrameBytesPerPixel\s*=\s*([0-9.]+)"` / `kP1FrameMemSafetyFrac` 从实现侧取字面量比对）；两条帧闸门分母的 `status` 是 `READY_FOR_OWNER_REVIEW`（不是"已签"，如实登记）。
  实现侧 `module_adapters.cpp:2235-2236` 仍是 `constexpr double kP1FrameBytesPerPixel = 116.0; / kP1FrameMemSafetyFrac = 0.75;` ⇒ 与唯一源登记值一致（policy 允许编译期字面量，条件是逐位相等 + 门比对）。
  提交 `5f8c237b` 正文点名"帧级内存闸门与调度器预算原是两个并存分母而文档登记为单一来源 ⇒ 两个分母都进唯一源并逐位比对，且每个分母必须通过扰动敏感性实测" ⇒ 与仓库现状互相印证（不是只看提交标题）。
- **置信**：中高。**未验证项**：按本阶段禁令未跑 `check_budget_single_source.py`，故"该门真能判红（扰动敏感性实测）"这条只有代码与提交正文作证，无本轮实跑证据。
- **对 D8 的影响**：不再进实施任务书；若要收口，缺的是**负责人对两条 `READY_FOR_OWNER_REVIEW` 的签字**，不是新代码。

### ROW 8 — `lib/infrastructure/scheduler/src/module_adapters.cpp` :: `R6-01.md::E6R6-04`
- **问题**：某门守护的承载面在生产产品里不存在：`p3_rejection.bin` 无生产者、`p3_resampled.json` 无 `diagnostic_planes`。
- **原证据位点**：`module_adapters.cpp:12355-12391`、`p3_resampled.json`、`docs/contracts/DATA_ARTIFACTS.md`、`DATA_SEMANTICS.md`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXC…`
- **现在位点**：`module_adapters.cpp:14537-14577`（p3_resampled.json 写出面，无 `diagnostic_planes`）、`docs/contracts/DATA_ARTIFACTS.md:38`（DATA-P3-REJ-001 要求该载体）、`eng/tools/quality/check_p3_rejection_count.py:52`（`CARRIER = "p3_rejection.bin"`）
- **四态判定**：**仍在**
- **取证**：
  - 生产者零：`git grep -n "p3_rejection" -- lib eng docs` ⇒ `lib/**` **零命中**，命中全在合同/判据/测试/交接文档（含 `eng/tools/HANDOVER.md:218`）。
    同法对 `diagnostic_planes`：`git grep -n "diagnostic_planes" -- lib` ⇒ 零命中。
    **否定式取证的阳性对照**：同一命令形态 `git grep -c "diagnostic" -- lib` 正常返回逐文件计数（`dark_optimizer.cpp:15` 等）、`git grep -n "n_rejected_nonfinite" -- lib` 返回 10+ 命中（`drizzle_engine.cpp:2241-2245`、`hp_drizzle_api.cpp:1292-1293`）⇒ 命令可用，零命中是真零命中。
  - 产品侧写出面现读：`module_adapters.cpp:14573` 写 JSON 时只登记 `{"bin", "p3_resampled.bin"}`，`14537-14577` 段无 rejection/diagnostic 键。
  - 门仍未登记：`git grep -n "p3_rejection" -- eng/ci` ⇒ 零命中 ⇒ `check_p3_rejection_count.py` 依旧不在 CI 注册表内，与 HANDOVER §8.6（HEAD 前 1 个提交写入）一致："载体不存在，既存未登记门 `check_p3_rejection_count.py` 返回 rc=2"。
  - 合同侧要求原文：`DATA_ARTIFACTS.md:38` 定义 DATA-P3-REJ-001 载体 `p3_rejection.bin` 且声明"字段缺失 ≠ 全 0（缺 `diagnostic_planes.n_rejected_nonfinite` 的产品判不满足 §2a 规则 3，判据具名 `COUNT_FIELD_MISSING`）"。
- **置信**：高
- **锚漂移说明**：发现时钉的 `module_adapters.cpp:12355-12391` 现在指向 `p2_sky_plane_audit_from_product()`（天光面审计抽取），与本条无关 ⇒ 行号因 `a6f602fe` 起的改名/插入整体漂移，判态以内容重定位为准。

### ROW 9 — `lib/infrastructure/scheduler/src/module_adapters.cpp` :: `复核新增-20260924.md::新增-54`
- **问题**：生产决策注释把四个无在册来源的百分数当 `additive_mode` 默认取值的"实测依据"，且同段对"默认"的自陈互斥。
- **原证据位点**：`module_adapters.cpp:8443`、`eng/tests/validation/release02/README.md:96-103`、`which_correction.py:17`、`delta_seam_cause.py:22`、`c_delta_comp.py:24`、`delta_vs_C.py:60`
- **现在位点**：`module_adapters.cpp:10293-10308`（决策注释）、`:10698`（同一组数值的第二处引用）、`eng/tests/validation/release02/README.md:96-103`
- **四态判定**：**仍在**（"无在册来源"半边仍在且**新增一条硬证**；"默认自陈互斥"半边已收敛）
- **取证**：
  - 四个百分数原样在位：`10293-10296`：`实测帧间失配 raw−C = 0.131% / raw−δ = 2.799% / raw−C−δ = 13.974%（比不校正的 13.454% 还差）`，出处只写 `（c-delta-ruling §2）`；`10307-10308` 又复述一遍；`10698` 第三次引用 `证有害：13.974% vs 0.131%，c-delta-ruling §2`。
  - **新增硬证（本轮实测到的"无在册"）**：`git ls-files | grep -i "c-delta-ruling"` ⇒ **零命中**，仓库根本不跟踪 `reports/RELEASE-02/c-delta-ruling.md`（本机 `ls reports/` ⇒ 只有 `v19r2`，无 `RELEASE-02` 目录）。⇒ 生产决策注释与 `lib/algorithms/coverage/include/astro/phase2/upm.h:98`（`依据：reports/RELEASE-02/{q2-snr-smooth,c-delta-ruling}.md`）都挂在**未入库**的外部报告上。
    阳性对照：`git grep -n "c-delta-ruling"` ⇒ 6 命中（`upm.h:98/:104`、`upm.cpp:808`、`module_adapters.cpp:9648/:10297/:10698`）⇒ 命令有效，`git ls-files` 的零命中是真的"未跟踪"。
  - 四个数值在跟踪的实测产物里也找不到：`grep -o "13\.974\|0\.131\|2\.799\|13\.454" eng/tests/validation/release02/c_delta_composition/*.json` ⇒ 零命中（`c_delta_comp.py`/`which_correction.py`/`delta_vs_C.py`/`delta_seam_cause.py` 这些脚本本身在库，但其 JSON 结果里没有 README 与注释所引的那四个数）。⇒ "脚本在册、数值不在册"，README:96-103 的表同样是孤值。
  - "默认"自陈现状：`10298` 与 `9834-9836`（`// §9.67 定案 1：默认 "delta"`）、`docs/plugins/algorithms_phase2/11_upm.md:132`（默认 delta，`c` 标"非默认"、`both` 标"仅对照/回归"）、`eng/packaging/config/config_registry.json:1856`（负责人 §9.67 定案 1 定默认 delta）四处一致 ⇒ **该半边已按正确口径改写，不再计为缺陷**。
  - 附带事实（供 D8 参考，不改本条判态）：`config_registry.json:1856` 自陈 `seam.additive_mode` "不在 defaults.json / CLI 白名单 / 任何 phase_config schema 中 ⇒ 未登记"，与 `11_upm.md:132` 的默认值登记并存。
- **置信**：高

---

### ROW 10 — `QA-V19R7-A2-07/EVIDENCE_INDEX.md` :: `R4-10.md::Q-10-04`
- **问题**：该归档件自带的"grep 可复现"校验命令在 BRE 里写 PCRE 的 `\d`，原样执行恒 0 命中 ⇒ 样例不可复核；其中一条样例锚点内容已消失。
- **原证据位点**：`QA-V19R7-A2-07/EVIDENCE_INDEX.md:42`、`lib/algorithms/coverage/tests/synthetic_gate.cpp:5791`、`lib/algorithms/drizzle/healpix_drizzle/tests/test_spherical_overlap.cpp:1025`、`representative_probe.cpp:2`
- **现在位点**：`artifacts/evidence/v19r7-quality/evidence/QA-V19R7-A2-07/EVIDENCE_INDEX.md:42`
- **四态判定**：**仍在**
- **取证**：
  - 被点名行逐字未改：`git grep -nF '\d' -- …/QA-V19R7-A2-07/EVIDENCE_INDEX.md` ⇒ 唯一命中 `:42`，行内容为「文件存在且非空；grep 可复现：`grep -rn "V19R\\d" lib --include="*.cpp" --include="*.h" | grep -v third_party`」。
  - 恒 0 命中实证：GNU grep 的 BRE/ERE 无 `\d` 字符类 ⇒ 该模式退化为字面 `V19Rd`；`git grep -nE "V19Rd" -- lib` ⇒ 0 命中。**阳性对照**：同命令换正确写法 `git grep -nE "V19R[0-9]" -- lib` ⇒ 命中 `synthetic_gate.cpp:5846`（`// F-V19R2-UPM-002…`）、`test_spherical_overlap.cpp:1025`（`// F-V19R2-DRZ-001…`）⇒ 命令可用，0 命中是真 0。
  - 样例锚点现状：`test_spherical_overlap.cpp:1025` 行内容与发现时一致（仍在位）；`synthetic_gate.cpp:5791` 现为 `ASSERT_TRUE(j0["C"].is_array());`（该文件现 6078 行，V19R 标记已漂到 `:5846`，而 EVIDENCE_INDEX 自己的发现表 STD-001 写的是 `:5322`）⇒ 三处锚互不一致；`representative_probe.cpp:2` 现为 `// representative_probe.cpp — DRIZZLE_REALISTIC_SINGLE_FRAME_PROBE`，且 `git grep -n "V19R4" -- …/representative_probe.cpp` rc=1、`git grep -c "V19R4" -- lib` 无任何文件命中 ⇒ **"V19R4" 样例内容在全仓已消失**，该子指控现成立。
- **置信**：高

### ROW 11 — `QA-V19R7-A2-07/EVIDENCE_INDEX.md` :: `R5-56.md::E-56-05`
- **问题**：A2-07 的"可复现"校验命令恒 0 命中 ⇒ 校验面恒绿。
- **原证据位点**：同 ROW 10（`EVIDENCE_INDEX.md:42` 等）+ `QA-V19R7-A1-02/EVIDENCE_INDEX.md`
- **现在位点**：同 ROW 10
- **四态判定**：**仍在**（与 ROW 10 同因同证，属同一修复面的重复条目）
- **取证**：同 ROW 10 的三条命令与对照。补充：该证据件的"校验"小节只有这一条命令（现读 `## 校验` 段仅 1 条 grep），命令恒 0 ⇒ 该域无任何可执行复核面。
- **置信**：高
- **对 D8 的建议**：与 ROW 10 合并为一条（同一文件同一行同一根因），避免整改文档包把两个条目呈成两个缺陷。

### ROW 12 — `artifacts/acceptance/l2_performance/README.md` :: `R4-06.md::D-R4-06-09`
- **问题**：本批归档里的 `*_alloc_report.json` 在全仓零消费方；自述 `schema:"astrocs.memory-report/v2"` 在合同面没有登记；唯一能校验它的 `validate_alloc_report()` 因缺原始曲线无法对任一归档实例运行。
- **原证据位点**：`ab2f_ab.json`、`README.md:59`、`eng/ci/checks.json`、`eng/ci/check_frozen_gate.py:39`、`eng/ci/check_worker_balance.py`
- **现在位点**：`artifacts/acceptance/l2_performance/alloc_reports/*_alloc_report.json`（11 份）、`lib/infrastructure/cli/memory_report.h:541-543`、`eng/ci/check_frozen_gate.py:39`、`eng/ci/check_worker_balance.py:46`
- **四态判定**：**仍在**（且**规模比工单更大**：11 份而非 5 份）
- **取证**：
  - 零消费方：`git grep -n "alloc_reports" -- eng docs lib` ⇒ **0 命中**（阳性对照：`git grep -n "l2_performance" -- eng docs` ⇒ `check_frozen_gate.py:39 DEFAULT_REPLAY_GLOB = "artifacts/acceptance/l2_performance/gates/*_gate.json"`、`check_worker_balance.py:46 L2_DIR = …`、`eng/ci/checks.json:10541/10593` 等命中 ⇒ 别的子目录有消费者，`alloc_reports/` 一个都没有）。
  - 数量订正：`git ls-files artifacts/acceptance/l2_performance` ⇒ `alloc_reports/` 下 `*_alloc_report.json` **11 份**（fix2f×3、real16_lock1、real16×3、real4、real8、v1b2f、v1locked2f），每份第 1 键均为 `"schema":"astrocs.memory-report/v2"`。
  - schema 未登记：`git grep -n "memory-report" -- eng/contracts docs/contracts` ⇒ **0 命中**（阳性对照：`git grep -n "astrocs.memory-report" -- eng lib` ⇒ `lib/infrastructure/cli/memory_report.h:210`（kSchema）与 `eng/tests/unit/mon002_gate_test.cpp:303` 命中 ⇒ 只有生产者与自测断言，无合同面登记）。
  - 校验器跑不了归档实例：`memory_report.h:541-543` 的 `validate_alloc_report(dir)` 第一步就是 `fopen(dir + "/alloc_samples.csv")`，取不到即 `return false`；`:559` 又要求 `dir + "/alloc_report.json"` —— 归档树里既无 `alloc_samples.csv` 也无裸名 `alloc_report.json`（`git ls-files artifacts/acceptance/l2_performance | grep -i "sample\|curve"` ⇒ 0 命中，实际文件名一律带 `<run>_alloc_report.json` 前缀）。
- **置信**：高

### ROW 13 — `artifacts/acceptance/l2_performance/README.md` :: `R5-49.md::H-49-05`
- **问题**：L2 归档只存产物：采集脚本 0 份入库、11 个 manifest 哈希无对象、strace 指针指向已回收目录。
- **原证据位点**：`README.md:82-104`、`measure.py`、`run_rest.py`、`bitwise.py`、`bitwise_classify.py`、`gaia_count.py`、`summarize.py`、`archive_l2.py`、`make_v1.py`、`use_variant.sh`
- **现在位点**：`artifacts/acceptance/l2_performance/README.md:86-104`（§8 复现命令）、`artifacts/acceptance/l2_performance/gates/*_gate.json`（11 份各带 1 个 manifest sha256）、`gaia_external_requests.json`
- **四态判定**：**仍在**（三个子项全部复算成立）
- **取证**：
  - 采集脚本 0 份入库：`git ls-files artifacts/acceptance/l2_performance | grep -E "\.py$|\.sh$"` ⇒ **0 命中**；`git ls-files | grep -c "PERF-401"` ⇒ **0**。README §8 的复现命令逐条指向 `run/PERF-401/{run_rest,measure,make_v1,bitwise,bitwise_classify,gaia_count,summarize,archive_l2}.py` 与 `use_variant.sh`（现读 `:86-104`），本机 `ls run/` ⇒ 无 `PERF-401` 目录。阳性对照：`git ls-files eng/tools/monitoring | grep -E "\.py$|\.sh$"` ⇒ 5+ 命中 ⇒ 该 grep 形态有效。
  - 11 个 manifest 哈希无对象：`git ls-files artifacts/acceptance/l2_performance/gates | wc -l` ⇒ 11；每份 gate JSON 内嵌一条 `"kind":"artifact","message":"run manifest written",…"sha256":"b3857a…1162d","size_bytes":39105` 事件（现读 `real16_w16_gate.json:18635`），其 `path` 为 `/workspace/Astro CS Database/run/PERF-401/out/real16_w16/astrocs_run_62ae40479b79.json`；`git ls-files | grep -c "astrocs_run_"` ⇒ **0** ⇒ 11 个哈希在全仓无被哈希对象。
  - 指针指向回收目录：`gaia_external_requests.json` 的 `per_file` 键全部是 `/workspace/Astro CS Database/gaia/GaiaDR3SP/gdr3sp-1.0.0-NN.xpsd` 绝对路径；`real16_bitwise_cmp/compare_report.json` 的 `a`/`b` 亦为 `…/run/PERF-401/out/real16_w{1,16}`。本节点（Windows 只读）不存在该前缀，`run/` 本身不入库 ⇒ 证据不可回放。
- **置信**：高

### ROW 14 — `artifacts/acceptance/l2_performance/ab2f_ab.json` :: `R4-06.md::E-R4-06-08`
- **问题**：`io_wait_pct_mean=144.39` 超出字段注释定义的"占区间比例×100"上界，且该口径与门禁侧同名判据分母互斥、生产侧从不喂值。
- **原证据位点**：`ab2f_ab.json`、`lib/infrastructure/cli/resource_recorder.h:70`、`monitor.h:189-207`、`resource_gate.h:182`
- **现在位点**：`ab2f_ab.json:113`（值 144.39，属 `ab2f_lock1_r2`）、`lib/infrastructure/cli/resource_recorder.h:70`
- **四态判定**：**仍在**（且**面比工单宽**：同批 `resource_summaries/` 另有 4 个 >100 的同类值）
- **取证**：现读 `resource_recorder.h:70: double io_wait_pct = 0.0;  // 系统级 iowait 占区间比例 × 100`（定义上界即 100），`:256` 的 `st_.io_wait_pct_mean = sum_iowait / recs.size()` 口径未变。
  本轮自跑取数（python 标准库读 JSON，未 import 仓库模块）：`ab2f_ab.json` 6 臂的 `io_wait_pct_mean` = 38.14 / 57.9 / 78.34 / 77.51 / 80.92 / **144.39**；
  `resource_summaries/*.json` 的 `active` 段另有 `fix2f_w1=110.06`、`real16_w16=126.72`、`real4_w16=184.16`、`real8_w16=171.45` ⇒ 同一越界形态在本批归档里共 5 处，不止工单点名的一个值。
- **置信**：高（数值为逐文件现读，非引用工单）

### ROW 15 — `artifacts/acceptance/l2_performance/ab2f_ab.json` :: `R5-51.md::C-51-03`
- **问题**：README §4 的「同二进制交错 A/B」处理臂在自身证据里零取锁，该对照不成立。
- **原证据位点**：`ab2f_ab.json`、`resource_summaries/v1locked2f_w16.json`、`README.md:57`
- **现在位点**：`ab2f_ab.json`（6 臂全部 `"cfitsio_lock":{"acquisitions":0}`）、`artifacts/acceptance/l2_performance/README.md:57`
- **四态判定**：**仍在**
- **取证**：现读 6 臂：`ab2f_lock0_r0/r1/r2` 与 `ab2f_lock1_r0/r1/r2` 的 `cfitsio_lock` 均为 `{wait_ns:0, hold_ns:0, acquisitions:0, contended:0}` ⇒ 标称 `lock=1` 的三臂同样零取锁。
  README §4 结论行原文（`:57`）：「同二进制交错 A/B（2 帧 probe，16 worker，3 轮中位数）：lock=off 48.42 s / lock=on 49.53 s（±2.2%，噪声带内）」，未加任何"两臂均未取锁 ⇒ 不构成锁开/锁关对照"的限定。
  真正有取锁的是另一次运行：`resource_summaries/real16_lock1_w16.json` 的 `acquisitions:205962, wait_ns:455230429305, hold_ns:89483532519`（README §4 表体用的是它），`v1b2f_w16.json` 为 25839 次 ⇒ 与 ab2f 零取锁并存，"对照不成立"的判据不变。
- **置信**：高

### ROW 16 — `docs/algorithms/DRIZZLE_GEOMETRY.md` :: `C2-01-科学算法源码.md::S-CCV-C2-01`
- **问题**：IDW 重建出的稠密 SNR 面无任何消费者；引擎侧形参被丢弃，"SNR 面参与有效性判定"是空头登记。
- **原证据位点**：`drizzle_engine.cpp:1409`、`:1339-1340`、`:2029`、`hp_drizzle_api.cpp:709/:899`、`docs/algorithms/DRIZZLE_GEOMETRY.md:184`
- **现在位点**：`docs/algorithms/DRIZZLE_GEOMETRY.md:197`（空头登记表行）与 `:280`（IDW 重建说明）、`drizzle_engine.cpp:1417`（丢弃形参）、`:2045-2047`（读面无判据）、`:1347`（具名但体内未用）
- **四态判定**：**仍在**
- **取证**：
  - 文档侧原文：`DRIZZLE_GEOMETRY.md:197`「| SNR 面非有限 | 计入 `rejected_nonfinite_value` 同族掩膜路径（SNR 面参与权重/有效性判定，剔除项逐条计数登记） | :2000-2005 |」。
  - 实现侧：`git grep -n "snrValue" -- …/drizzle_engine.cpp` ⇒ 7 命中 `1347/1405/1417/2045/2047/2089/2100`；`:1417` 为 `Scalar pixelValue, float /*snrValue*/, float /*weightValue*/,`（形参显式匿名丢弃），`:1347` 具名但函数体内再无该标识符；`:2045-2047` 只做 `float snrValue = 1.0f; if (snrData) snrValue = snrData[…];`，无 `isfinite` 判定。
  - `git grep -n "isfinite" -- …/drizzle_engine.cpp` ⇒ 唯一与 snr 有关的是 `:1214`（`snr_model->points[i].snr_psf`，**稀疏控制点**路径），不是稠密面 ⇒ "参与有效性判定"无承载。
  - 文档锚本身已失效：`:197` 指向的 `:2000-2005` 现在是 stripe 切块与 G3-5/DISP-DRZ-004 的 NaN 合同注释（现读 `sed -n '1995,2050p'`），不含 SNR 判定 ⇒ 锚与内容不对应。
  - 阳性对照：`git grep -n "sumSnrSq" -- lib eng` ⇒ 仅 `drizzle_engine.cpp:877/996`（赋 0）与 `drizzle_engine.h:51/65`（`// sumWeight/sumSnrSq 为诊断字段, 已移除.`）⇒ 历史聚合通道确已移除，无第三方消费者。
- **置信**：高

### ROW 17 — `docs/algorithms/DRIZZLE_GEOMETRY.md` :: `R5-88.md::G-88-05`
- **问题**：TEST 11 记录的是被禁止的第二套 NSIDE 数值表（上限 2^20 + 魔数 210960）。
- **原证据位点**：`test_output.txt:73`、`hiss_correctness_test.cpp:498`、`drizzle_engine.cpp:642-643`、`docs/algorithms/DRIZZLE_GEOMETRY.md:146`
- **现在位点**：`lib/infrastructure/aio/tests/hiss_correctness_test.cpp:498/501/502/511/522/534/537/548/563/564/570/573`（12 处）
- **四态判定**：**仍在**
- **取证**：`git grep -n -e "1048576" -e "210960" -- …/hiss_correctness_test.cpp` ⇒ 12 命中，与上一轮计数订正一致；关键行现读：`:498 // 返回: 推荐 NSIDE (2 的幂, 钳位 [16, 1048576])`、`:501 // HEALPix 线性像素尺度 ≈ 210960/nside 角秒`、`:537 ASSERT_TRUE(nside <= 1048576, "NSIDE <= 上限 1048576")`、`:573 ASSERT_TRUE(nside == 1048576, "极细尺度 (0.1\"/px) 触发 NSIDE 上限 1048576")`。
  正本口径（唯一源）`docs/algorithms/DRIZZLE_GEOMETRY.md:145/157-158`：`HEALPIX_SCALE_PER_NSIDE_ARCSEC = sqrt(π/3)·(180/π)·3600 = 211076.28514206142″`、`钳位 [NSIDE_MIN, NSIDE_MAX] = [16, 4194304=2^22]` ⇒ 测试内私有表两处常数都与正本不同（210960 vs 211076.285；2^20 vs 2^22），第二套数值表原样在位。
- **置信**：高

### ROW 18 — `docs/contracts/API_CONTRACTS.csv` :: `前台复验-20260924.md::V-11`
- **问题**：零实现、零消费者的 orphan 头，在合同登记表里被标 `VERIFIED`（§12.5 最干净的一次违反）。
- **原证据位点**：`docs/contracts/API_CONTRACTS.c`（即 `.csv`）、`lib/infrastructure/pipeline/orchestrator/cpp/include/spill_manager.h`、`admission_controller.h`、`resource_monitor.h`、`ASTROCS_DESIGN.md`
- **现在位点**：`docs/contracts/API_CONTRACTS.csv`（spill 18 行 / admission 10 行 / resource_monitor 8 行，合计 36 行，全部以 `,VERIFIED` 结尾）
- **四态判定**：**仍在**（规模数与发现时逐位相同 ⇒ 该面未动）
- **取证**：
  - 三个 orphan 头仍被跟踪：`git ls-files | grep -i "spill_manager|admission_controller|resource_monitor"` ⇒ `lib/infrastructure/pipeline/orchestrator/cpp/include/{spill_manager.h,admission_controller.h,resource_monitor.h}`。
  - 零实现/零消费者：对三个头名分别 `git grep -l "<头名>" -- 'lib/**/*.cpp' 'lib/**/*.c'` ⇒ 各 0 个文件；全仓提及者只有文档/清单/合同/夹具（`docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv`、`docs/architecture/api_inventory.csv`、`docs/contracts/API_CONTRACTS.csv`、`docs/modules/orchestrator.md`、`eng/tools/quality/extract_cpp_api.py`、`eng/tools/quality/contracts/fixtures/check_api_contracts/*.csv`）与两份旧审计台账。
  - 逐头行数：`git grep -c "spill_manager.h" -- docs/contracts/API_CONTRACTS.csv` ⇒ **18**；`admission_controller.h` ⇒ **10**；`resource_monitor.h` ⇒ **8**（合计 36 = 工单"18 行/头、36 行同族"）。
  - 全表单一状态：`git grep -c "VERIFIED$" -- docs/contracts/API_CONTRACTS.csv` ⇒ **381**；`git grep -c "^API-" …` ⇒ **381** ⇒ 381/381 恒 VERIFIED（工单复核依据里的那条计数在 HEAD 逐位复现）。
  - `TST-GEN-001` 仍无跟踪测试承载：`git grep -n "TST-GEN-001"` ⇒ 命中面只有 `docs/contracts/API_CONTRACTS.csv` 自身（355 行共用）与 `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv:57`（后者把同一问题登记为 **OPEN**，条目 M5b-C-01"API 合同表全部标 VERIFIED、共用未定义占位 test_ids"）⇒ 无 eng/tests 侧在册测试。
- **置信**：高

### ROW 19 — `docs/contracts/API_CONTRACTS.csv` :: `质量-合同与接口.md::Q-合同与接口-01`
- **问题**：`units / valid_range / nullable / determinism / precision` 五列 381/381 行填同一占位串，合同字段实际无值。
- **原证据位点**：`docs/contracts/API_CONTRACTS.c`（即 `.csv`）
- **现在位点**：`docs/contracts/API_CONTRACTS.csv:2-382`（全表）
- **四态判定**：**仍在**（自跑复算，与发现时逐值相同）
- **取证**：本轮自跑 `PYTHONIOENCODING=utf-8 python -c "import csv,io; …csv.DictReader('docs/contracts/API_CONTRACTS.csv')…"`（只用标准库，未 import 仓库模块）⇒ 行数 381；工单点名的五列 distinct 全为 **1**：`units='ADU/pixel/deg per header'`、`valid_range='per SCI valid_range'`、`nullable='per header nullable'`、`determinism='deterministic per SCI'`、`precision='fp32/fp64 per header'`（各 ×381）。
  同表另有 `linkage / lifetime / mutability / blocking / reentrancy / async_completion / error_model / status` 亦 distinct=1 ⇒ "坏列"面比工单点名的五列更宽。
  反向核对：`139a2bc4`「坏列修复收口」与本表无关（本表被点名五列一字未改），不得据提交标题判已修。
- **置信**：高（命令与取值均为本轮自跑）

---

### ROW 20 — `docs/contracts/DATA_SEMANTICS.md` :: `L8-术语与死词.md::L8-06`
- **问题**：`FZ-MODE-PRODUCTION` 把"模式可选"留在输入面，与 §3.1「没有可选择项」在两份合同正文里并存。
- **原证据位点**：`docs/contracts/DATA_SEMANTICS.md:3102`、`docs/contracts/PUBLIC_API.md:2139/:2150`、`DATA_SEMANTICS.md:3108`
- **现在位点**：`eng/contracts/data/v6_clause_registry_v1.json:684-692`（FZ-MODE-PRODUCTION 条目）、`docs/contracts/PUBLIC_API.md:2174`、`docs/contracts/DATA_SEMANTICS.md:3252/:3259/:3262`、`ASTROCS_DESIGN.md:229`
- **四态判定**：**仍在**
- **取证**：机器登记面一字未改：注册表 `FZ-MODE-PRODUCTION` 仍 `"kind":"mode"`、`"value": "point_information | surface_gls | psfsw_robust"`、`"scope": "Phase2 配置/路由"`、`"status": "FROZEN"`，只在 `subject` 后追加了括注"（已按 §9.73 A44 作废：该概念不存在；权重是阶段二按该天球像素对应帧集合现场算出的派生量）"⇒ **subject 宣布作废、value 仍给三个可选模式**，同一对象内互斥。
  合同正文面：`PUBLIC_API.md:2174` 仍以表格行写「合法值（生产）| `point_information` / `surface_gls`（**显式选择，切换只经配置**）」，与紧随其后的 `:2184/:2186`（A44 订正段：`weight_mode` 键不存在、出现即 fail-closed 具名拒绝）并存；`DATA_SEMANTICS.md:3252` 已把 weight_mode 三分整条划废（`### 31.3 ~~weight_mode 三分~~（该概念不存在…）`）、`:3259` 写 `FZ-MODE-PRODUCTION = {point_information, surface_gls}` 且判 psfsw_robust「**接受面 = 空**」——这与注册表 `value` 里的三值列表直接冲突。
  上层：`ASTROCS_DESIGN.md:229` 现文为「权重的产生链固定为两步、**没有可选择项**」并新增一段把三条 SNR 重建口径（dense/sparse_reconstruct/frame_reconstruct）解释为"重建方式的选择，不是权重口径的选择"⇒ 上层已自洽，冲突留在注册表 value 与 PUBLIC_API 的"合法值（生产）…显式选择"表述上。
- **置信**：中高（四处正文/登记面均现读；`subject` 括注属"改写措辞"而非把 value 改为不可选，不判已修）

### ROW 21 — `docs/contracts/DATA_SEMANTICS.md` :: `质量-合同与接口.md::Q-合同与接口-41`
- **问题**：NaN/无效样本处置在两份自称权威的合同里写成相反 MUST：drizzle「不掩膜、直接传播」↔ 交换合同「对 Phase 1 drizzle drop 做样本级掩膜并重新归一」。
- **原证据位点**：`docs/contracts/DATA_SEMANTICS.md:340`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md:151-158`
- **现在位点**：`docs/contracts/DATA_SEMANTICS.md:371`、`docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md:151-159`、`docs/contracts/PUBLIC_API.md:311`、`docs/modules/healpix_drizzle.md:57`、`docs/modules/registry/astrocs.phase1.drizzle.md:52`、`docs/standards/NUMERIC_STANDARD.md:108`、`lib/algorithms/drizzle/README.md:96`
- **四态判定**：**仍在**（但"谁对"已定：DATA-002 §2a + `rule_id NAN-SAMPLE-MASK-COVERAGE-NAN` 为正确侧，陈旧「不掩膜」句为错误侧）
- **取证**：正确侧已就位并进了实现：`DATA-002:151-159` 的「处置规则（样本级掩膜 + 重归一 + 覆盖级 NaN + 强制计数）」对 Phase1 drizzle drop 明确「从分子、分母、方差三项一并剔除并重新归一」；`docs/algorithms/DRIZZLE_GEOMETRY.md:196/:327` 同 rule_id；实现 `drizzle_engine.cpp:2040-2043`（`!std::isfinite((double)pixelValue) → ++tc.rejected_nonfinite_value; continue`，本轮现读）与 `:2203-2208` 分类计数聚合。
  错误侧仍在四处以上：`DATA_SEMANTICS.md:371` 仍写「值 NaN/Inf 经 `F_p=Σx_j·w_jp` **直接传播、drizzle 层不掩膜**」并把 `isfinite(...)+continue` 称"即反例"（同一行还自称锚 `docs/science/DRIZZLE.md:116`），`PUBLIC_API.md:311`、`healpix_drizzle.md:57`、`registry/astrocs.phase1.drizzle.md:52`、`NUMERIC_STANDARD.md:108`（把「不掩膜」记为 TRACKED/OPEN 偏差）、`lib/algorithms/drizzle/README.md:96` 同样保留旧句 ⇒ 同一 rule_id 下两处合同正文仍是相反 MUST。
  计数取证：`git grep -n "不掩膜"` ⇒ 上述 6 个合同/模块/标准面命中（另 2 命中为 `drizzle_engine.cpp:2037` 与 `drizzle_nonfinite_test.cpp:3/:12`，这两处是"已按同一 rule_id 反转/原断言已作废"的说明文字，不算缺陷）。
- **置信**：高

### ROW 22 — `docs/science/DRIZZLE.md` :: `C2-01gap2-科学算法源码.md::A-C2gap2-16`
- **问题**：`FZ-COND-FLUX-CONSERV` 落地不齐——`v6_spherical_overlap.h` 把该条款引成被禁恒等式；生产 HiPS 面整条不落 `flux_conservation_factor`。
- **原证据位点**：`docs/science/DRIZZLE.md:101-104`、`eng/contracts/schemas/product_family_field_constraints.schema.json:425-433`、`lib/algorithms/drizzle/healpix_drizzle/v6_spherical_overlap.h:13`
- **现在位点**：(a) `v6_spherical_overlap.h:13`；(b) 已修面见下
- **四态判定**：**仍在**（只剩 (a) 半边；**(b) 半边判已修，不得再上报**）
- **取证**：
  - (a) 仍在：`v6_spherical_overlap.h:12-14` 现读为「冻结锚：`FZ-FORMULA-DRIZZLE-SB  D_p = Sum_j a_jp；w_SB_jp = a_jp / A_pixel_j` / `FZ-COND-FLUX-CONSERV   Sum_p a_jp = A_drop_j = pixfrac^2 * A_pixel_j`」——而注册表里该条款现文（`eng/contracts/data/v6_clause_registry_v1.json` 内 `FZ-COND-FLUX-CONSERV`）已按负责人裁决 `DRZ-FLUX-FIX-01` 订正为「`w_jp=a_jp/A_drop_j` 为 drop 面积归一核 … provenance `flux_conservation_factor` 恒 1（口径标记）」，且其 `signoff_reason` 明写"由『A_pixel 归一 + 条件因子 pixfrac^2』订正为『drop 面积归一 + 因子恒 1』"⇒ 头文件仍按被订正掉的旧口径引用同一条款号，且 `w_SB_jp = a_jp/A_pixel_j` 与注册表的 `a_jp/A_drop_j` 直接互斥。
  - (b) 已修：`git grep -c flux_conservation_factor -- lib` ⇒ 命中面已含产品/provenance 与 AIO 层：`lib/algorithms/integration/v6_phase1/src/phase1_product.cpp:299-300`（`has_flux_conservation_factor = true; flux_conservation_factor = 1.0;`）与 `:1188-1200`（缺键/非正/`!=1` 一律 `fail("flux_conservation_factor != 1 (drop-area normalized kernel)")`）、`lib/infrastructure/aio/v6/src/v6_provenance.cpp:57`（列入 provenance 必备键表）与 `:122-125`（序列化）、`lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.cpp:534-539/:630-634`、`phase2_integrate.cpp:454-455`（Phase2 置 1.0）。工单原文"生产 HiPS 面整条不落该键""仅靠默认 pixfrac=1.0 不显形"已不成立；HANDOVER §6 第 4 条（provenance 通量折算键无数值断言）以 `822b9c53` 收口，与本处现读一致。
- **置信**：高

### ROW 23 — `docs/science/DRIZZLE.md` :: `复核新增-科学-20260924.md::K-19`
- **问题**：`docs/science/DRIZZLE.md §7` 把中间累加量 `F_p` 的 `pixfrac²` 恒等式当成产品绝对通量的兜底，与同节常量场不变量「`S_p=B0` 与 pixfrac 无关」互斥，且"乘 pixfrac²"与其自称的"1/pixfrac² 偏差"反号。
- **原证据位点**：`docs/science/DRIZZLE.md:101-104`、`eng/contracts/data/v6_clause_registry_v1.json:448`、`DRIZZLE.md:47`
- **现在位点**：`docs/science/DRIZZLE.md:6/:19-22`（§1 目的、§2 符号表）与 `:112-118`（§7 独立不变量）
- **四态判定**：**已修**
- **取证**：被点名的三段行本身按正确口径改写（`git log -S "把 \`pixfrac²\` 当归一因子" -- docs/science/DRIZZLE.md` ⇒ 唯一改动提交为 `c4af4136`(2026-09-25 01:16:59，晚于工单落盘 09-24 22:26)；`git show c4af4136 -- docs/science/DRIZZLE.md` 的 ± 行现读）：
  - §1：`− …按条件不变量守恒通量（Σ_p F_p=pixfrac²·Σ_j x_j）` → `+ …**严格守恒总通量**（`Σ_p F_p=Σ_j x_j`，与 `pixfrac` 无关；核按 drop 面积归一）`；
  - §2：`− | w_jp | a_jp/A_pixel,j 面亮度保持权重（= pixfrac²·a_jp/A_drop,j） |` → `+ | w_jp | a_jp/A_drop,j **核权重**（drop 的分数交叠，`Σ_p w_jp=1`） |` 并新增 `+ | N_p | Σ_j w_jp·A_pixel,j **面亮度归一分母** |`；
  - §7 现读（`:112-118`）：`provenance.flux_conservation_factor 因此恒为 **1**（口径标记，正确取值为 1）；把 pixfrac² 当归一因子会把输出总通量**压低** pixfrac² 倍（pf=0.8 → 0.64，即 −0.48 mag 光度零点偏差）` ⇒ 偏差方向与"压低"同侧，反号问题消失；同节常量场不变量现读为 `S_p=F_p/N_p=B0 对全部 pixfrac∈(0,1] 成立（drop 面积在分子分母相消）` ⇒ 与通量守恒式不再互斥（分母改用新具名 `N_p`）。
  与注册表 `FZ-COND-FLUX-CONSERV`（因子恒 1）同向 ⇒ 规模/能力未回退（文档只加不减，§2 多出一行）。
- **置信**：高
- **对 D8 的影响**：本条不得再作为缺陷上报；剩余唯一未合流面是 ROW 22(a) 那条头文件旧口径引文。

### ROW 24 — `docs/science/NOISE_MODEL.md` :: `R5-64.md::C64-02`
- **问题**：kernel 表 5 行声明 `ACS_PRECISION_F64`，但参数合同只有 f32 通道 ⇒ 精度档位不可能成立；且与 `docs/science/**` 的 FP64 条款双向对撞；该列无消费者且不进 profile。
- **原证据位点**：`backend_table.in`、`baseline_kernels.h:33-42`、`baseline_kernels_impl.in`、`docs/science/NOISE_MODEL.md:283`、`docs/science/PHASE2_UPM.md:218`、`docs/science/INTEGRATION.md:110`
- **现在位点**：`lib/infrastructure/benchmark/backend_host/backend_table.inc:21/22/26/27/28/31`（6 行 F64）、`baseline_kernels.h:29-42`、`backend_table.inc:88`、`profile_gen.cpp:134`、`cpu/baseline/src/baseline_provider.cpp:83-85`、`docs/science/NOISE_MODEL.md:312`
- **四态判定**：**仍在**（行数订正：6 行而非 5 行）
- **取证**：
  - `git grep -rn "ACS_PRECISION_F64"` ⇒ 除 `lib/include/astrocs/common_abi_v1.h:122` 的枚举定义外，全部命中在 `backend_table.inc`：`:21 ALG-004 noise-snr-reductions`、`:22 ALG-002 wcs-psf-batch`、`:26/27/28 ALG-006 upm-spmv / upm-residual / upm-weight-update`、`:31 ALG-P3-002 hips-bulk-transform`，每行均带 `ACS_DET_BITWISE` ⇒ 声明面 6 行 F64 原样在位。
  - 通道只有 f32：`baseline_kernels.h:29-42` 现读 `acs_baseline_params_v1` 为 `float k; uint32_t aux0/aux1; acs_span_f32 in0..in3, out0, out1;` ⇒ 无 f64 数据面/参数面。
  - 同族两处自证矛盾：`backend_table.inc:88 g_api.precision_class = ACS_PRECISION_F32;`（后端整体声明 F32）与 `profile_gen.cpp:134 {"precision", "fp32"}`（profile 逐 kernel 写死 fp32）⇒ "F64 档被 ISA 基准覆盖"的宣称在 profile 面根本不成立（工单原判断复现）。
  - 已登记但未修：`cpu/baseline/src/baseline_provider.cpp:83-85` 注释现读「`desc.precision` 以实际 f32 数据面为准 — legacy 部分条目 F64 标注与其 f32 实现不一致，见 README §4」，其 `kKernels[]` 表把同名 kernel 一律写 `ACS_CPU_BASELINE_PREC_F32`；该注释由 `9a2b5d11`(2026-09-21，**早于工单**) 引入 ⇒ 属既存登记，不是本轮整改。
  - 科学正本对撞仍在：`docs/science/NOISE_MODEL.md:312` 现读「FP64 全链路；MAD 常数唯一权威写法 `1.482602218505602`…」⇒ 与 ALG-004（noise-snr-reductions）声明的 F64 档在"是否按 FP64 被基准覆盖"上仍两说法。
- **置信**：高

### ROW 25 — `docs/science/NOISE_MODEL.md` :: `复核新增-科学-20260924.md::K-35`
- **问题**：`var_ADU = max(signal,0)/gain + (rn/gain)²` 与 Horne Eq.(12) 的 `|D|` 口径方向相反，且"入不入生产"在 `NOISE_MODEL.md:320` 与 `NOISE_ESTIMATION.md`/`DATA_SEMANTICS.md` 两正本互斥（须上呈）。
- **原证据位点**：`docs/science/NOISE_MODEL.md:80`、`docs/algorithms/NOISE_ESTIMATION.md:27`、`docs/contracts/DATA_SEMANTICS.md:530`
- **现在位点**：`docs/science/NOISE_MODEL.md:80/:291/:327`、`docs/science/SCIENCE_SCOPE.md:84`、`docs/algorithms/NOISE_ESTIMATION.md:27/:149`、`docs/contracts/DATA_SEMANTICS.md:561/:3140`、实现 `lib/algorithms/noise_snr/cpp/include/snr_estimator.h:283`、`src/module_entry.cpp:1521`、`tests/p1noise/p1noise_oracle.hpp:472`
- **四态判定**：**仍在**（两个子项均在位，"须上呈"状态不变）
- **取证**：
  - 公式侧未动：`NOISE_MODEL.md:80` 现读 `var_ADU = max(signal,0)/gain + (read_noise_e / gain)²   # signal: ADU, gain: e-/ADU（冻结换算）, rn: e-`（行号与发现时相同）；`git grep -n "var_ADU"` 共 12 命中，实现侧 `snr_estimator.h:283`、`module_entry.cpp:1521 (ALG-NOISE-003)`、oracle `p1noise_oracle.hpp:472` 全部仍是 `max(signal,0)` 截断，仓库内**没有任何一处**讨论 Horne Eq.(12) 的 `|D|`（带符号探测量取绝对值）口径：`git grep -n "Horne"` 的 8 处命中全在 `snr_phot`/`PᵀC⁻¹P`/文献登记语境，无一方差式与 `|D|` 的比对。⇒ "方向相反"仍是未核实的开放项。
  - 入不入生产互斥仍在：`NOISE_MODEL.md:327` 现读「Poisson+read noise：`var_ADU=max(signal,0)/gain+(read_noise_e/gain)²` 是**加权方差面（§5c）的源项与常数项来源**」＝进生产；`NOISE_ESTIMATION.md:27`「F6: 诊断: …（仅 SNR-005, **不入生产**）」、`:149`「诊断不入生产」、`DATA_SEMANTICS.md:561`「仅诊断（SNR-005，不入生产）」、`:3140`「（**仅诊断、不入生产**）」＝不进生产 ⇒ 同一式子在两份自称权威的正本里取值语义相反，未收口。
- **置信**：高

### ROW 26 — `docs/validation/v6/QA_MATRIX.md` :: `D5b-验证与追溯.md::D5b-04`
- **问题**："零用例即红"由常量喂数：P0-01…06 与 G-RD-03/04 的 `executed_cases` 是硬编码字面量，P0-01 的定义恰是"机器门不得看着在跑"。
- **原证据位点**：`docs/validation/v6/QA_MATRIX.md:69-74`、`docs/validation/v6/README.md:13`、`ORACLE_AND_ZERO_CASE_POLICY.md`、`QA_MATRIX.md:150`
- **现在位点**：`artifacts/evidence/v6/qa-design/case_ledger.json`（`entries[]`，44 门）、`docs/validation/v6/QA_MATRIX.md:71-73/:150`、`docs/validation/v6/ORACLE_AND_ZERO_CASE_POLICY.md:22`
- **四态判定**：**仍在**
- **取证**：本轮自跑读账本（python 标准库）：`case_ledger.json` 共 44 条 entry，字段只有 `gate_id / required_cases / executed_cases / skipped_cases / pends / counts_as_pass / scheduled_wave / implementation_cases_scheduled / owner / note` ⇒ **没有任何指向真实用例/ctest 目标/夹具的引用字段**。被点名 8 条现值：`P0-01..04/06` `required=2/executed=2`、`P0-05` `5/5`、`G-RD-03` `8/8`、`G-RD-04` `1/1`，全部 `counts_as_pass=true`，note 为"结构校验 + mutation 覆盖计数"或"qa_oracle 独立 check 计数"⇒ executed 恒等于 required，是常量喂数而非计数。
  不接 CI 复算：`git grep -rn "executed_cases" -- eng lib` ⇒ eng/lib 侧只有 1 处注释（`eng/tests/unit/v6_p1_cal/v6_cal_test_support.hpp:5`），无 runner 消费该账本；`git grep -n "case_ledger" -- eng` ⇒ 0 命中 ⇒ 账本只被 `artifacts/evidence/v6/qa-design/SUMMARY.md`（自跑 `validate_spec.py` 的登记）引用，`validate_spec.py` 亦不在 eng/** 注册面。
  文本面同页互斥仍在：`QA_MATRIX.md:72` 定义"executed_cases==0 → rc=2"，`:150` 的 P0-01 行仍写"结构性规则（冻结）…min=2；executed=0 或 skip-only -> rc=2"，而喂数来自上面那本账本。
- **置信**：高（判定不依赖跑门；本阶段禁止跑检查器，故 `validate_spec.py` 实际行为未验）

### ROW 27 — `docs/validation/v6/QA_MATRIX.md` :: `质量-标准CI与验证.md::Q-D5-24`
- **问题**：`QA_MATRIX.md §1` 把已废止的"冻结宪章"列为权威链第一条并加"原样继承，不得矛盾"，而同一页第 3 行的溯源注记宣布该宪章已废止、不构成现行依据（同页互斥）；上一轮复验把主证换成"两份 frozen 机器合同把废止宪章写成 `constitution_ref` 权威 + 在册门恒绿 + 一个测试把已废止文件名当运行时锚"。
- **原证据位点**：`docs/validation/v6/QA_MATRIX.md:28`、`ASTROCS_PROJECT_CONSTITUTION.md`、`docs/ci/03_GATES.md`、`docs/DOCUMENT_INDEX.yaml:1438`
- **现在位点**：`docs/validation/v6/QA_MATRIX.md:3` 与 `:32`、`eng/contracts/data/aio_abi_contract_v1.json:9-12`、`eng/contracts/data/phase2_uncertainty_rejection_provenance_v1.json:9-13`、`eng/tests/unit/p1001_real_nodes_test.cpp:195-206`、`eng/tools/check_agents_gov.py:74/:308`
- **四态判定**：**仍在**（三个机制一个未动）
- **取证**：
  - 同页互斥：`:32` 现读「上位锚：冻结宪章 `ASTROCS_PROJECT_CONSTITUTION-001` §4.1（量不混名）/§5.3/§6.3/§14.2（零用例即红）」列为权威链；`:3` 现读「…该宪章（`ASTROCS_PROJECT_CONSTITUTION.md`）**已废止**（ROOT-007 删除），不构成现行依据」。`git ls-files | grep -c ASTROCS_PROJECT_CONSTITUTION` ⇒ **0** ⇒ 被引权威体在仓内不存在。
  - 两份 frozen 机器合同仍把它写成权威：`aio_abi_contract_v1.json:9-12` `"frozen": true` + `"constitution_ref": ["ASTROCS-CONSTITUTION-001 §8.3 …","§8.6 …"]`；`phase2_uncertainty_rejection_provenance_v1.json:9-13` 同构（§6.1/§6.3/§16.2/§18.3）。
  - 运行时锚失效：`eng/tests/unit/p1001_real_nodes_test.cpp:195-206` 的 `find_repo_gaia_dir()` 以 `fs::exists(probe / "ASTROCS_PROJECT_CONSTITUTION.md")` 作仓库根判据 ⇒ 该文件已不存在 ⇒ 函数恒返回空串，Gaia 真实数据臂静默失效（同一失效被 `eng/tests/cli/test_cli001_vpi.py:9` 的注释承认："原权威 ASTROCS_PROJECT_CONSTITUTION.md 已不存在（仓库零命中）"）。
  - 门的双盲区仍在：`eng/tools/check_agents_gov.py:74 LEGACY_TOKENS = ["ASTROCS_PROJECT_CONSTITUTION.md", …]` 而 `:308` 的扫描面是 `os.walk(os.path.join(base, "docs"))` ⇒ 只看 `docs/`，不覆盖 `eng/contracts/**` 与 `eng/tests/**`，所以上述三处命中该门全部看不见（本阶段禁跑门，故以扫描面代码 + 现读命中集合作证）。
- **置信**：高

### ROW 28 — `lib/algorithms/calibration/V6_CALIBRATION_COVARIANCE.md` :: `R5-84.md::A-84-01`
- **问题**：校准协方差传播式在实现说明里丢了转置，与科学正本和实现字符串都不一致。
- **原证据位点**：`lib/algorithms/calibration/V6_CALIBRATION_COVARIANCE.md:13`、`docs/science/PSF_SIGNAL_WEIGHT.md:100`、`docs/contracts/DATA_SEMANTICS.md:2748`、`src/v6_calibration_covariance.cpp:6xx`
- **现在位点**：`lib/algorithms/calibration/V6_CALIBRATION_COVARIANCE.md:13`（未改）
- **四态判定**：**仍在**
- **取证**：被点名行现读：`:13 | ALG-P1-001 §2.2 线性化 | J=[1/f,−(1−α)/f,−α/f,−y/f]；**C_cal=J C_in J**；逐像素 v_cal=J C_in Jᵀ |` —— 同一行内矩阵式 `C_cal = J C_in J` 缺右乘转置，紧跟的逐像素标元式却带 `Jᵀ` ⇒ 与正本/实现字符串（`v6_calibration_covariance.h:279 propagation = "C_out = R C_in R^T"`、`src/:637/:702/:703/:710` 四处均带 `R^T`）不一致。行号与发现时同为 `:13`，内容一字未改 ⇒ 非"整段删除/措辞规避"，是原样在位。
  注：上一轮据此把 S1 降为 S2（实现面正确），本条只复核存续性。
- **置信**：高

### ROW 29 — `lib/algorithms/calibration/V6_CALIBRATION_COVARIANCE.md` :: `R5-84.md::D-84-01`
- **问题**：条款登记表里状态为 `OPEN` 的 `OI-02` 被列进"权威锚（冻结，逐条符合）"表并已锁进实现。
- **原证据位点**：`V6_CALIBRATION_COVARIANCE.md:8`、`eng/contracts/data/v6_clause_registry_v1.json:2495-2502`、`v6_cal_covariance_test.cpp:144`、`src/v6_calibration_covariance.cpp`
- **现在位点**：`eng/contracts/data/v6_clause_registry_v1.json:2495-2501`（status 仍 OPEN）、`V6_CALIBRATION_COVARIANCE.md:1`节标题 + `:16`（同节表行）+ `:53`、`include/…/v6_calibration_covariance.h:17`、`src/v6_calibration_covariance.cpp:282`、`eng/tests/integration/v6_p1/v6_p1_integrate_test.cpp:133`
- **四态判定**：**仍在**
- **取证**：注册表现读 `:2495-2501`：`"id": "OI-02", "topic": "DESIGN-P1 §4.2 的 V_b + α²V_b 与同 master_id 的 (1−α)² 合并关系", "cls": "finding", "owner": "CONTRACT-FREEZE-001(W4)/负责人", **"status": "OPEN"`, "anchor": "ADJ-OBS-01; ALG-P1-001 §2.2"`（行号与发现时逐位一致）；同文件 `:3388` 起仍把它列在未决清单里。
  文档面：`V6_CALIBRATION_COVARIANCE.md:1` 节标题为「## 1. 权威锚（冻结，逐条符合）」，其表内 `:16` 行即「同一 bias master 只计一次（`OI-02` 精确形式）| 光/暗路 bias 的 master_id 相同 → 系数折叠 −(1−α)/f…」⇒ OPEN 条目被当成"冻结逐条符合"的权威锚；同文 `:53` 又写「`OI-02`（同 master 折叠 vs DESIGN §4.2）…只登记，归 W4/负责人」⇒ 同文档两处对同一 id 的定位互斥。
  实现锁定：`git grep -n "OI-02" -- lib` ⇒ 头 `v6_calibration_covariance.h:17`（冻结注释「不同 master_id 时为 -1/f 与 +alpha/f 两个独立项（OI-02 精确形式）」）、`src:282` 同句；测试 `eng/tests/integration/v6_p1/v6_p1_integrate_test.cpp:133` 以「共享 master -> 系数折叠 (OI-02 精确形式)」为断言前提。
- **置信**：高

### ROW 30 — `lib/algorithms/platesolve/IPV_PIPELINE.md` :: `R5-85.md::A-85-15`
- **问题**：`IPV_PIPELINE.md` 把代码已判定"高估 4–5 个数量级"并已被替换的密度公式当作现行方法，且参数表与生产实况相反。
- **原证据位点**：`lib/algorithms/platesolve/IPV_PIPELINE.md:113-128`、`cpp/ipv/src/ipv_select.cpp:358-372`、`cpp/ipv/include/ipv_select.h:69-71`、`docs/algorithms/PLATESOLVE.md`
- **现在位点**：`lib/algorithms/platesolve/IPV_PIPELINE.md:108-127`（Step 5 整段未改）、`ipv_select.cpp:357-372`、`ipv_select.h:69-73`
- **四态判定**：**仍在**
- **取证**：
  - 文档面原样在位：`:113` 标题仍是「### Step 5: V4.9 密度公式估算极限星等 ⭐ 新增」，`:114` 仍写「函数: `estimate_mag_lim_by_density()` (ipv_select.cpp:315)」，`:117-119` 仍给模型 `ρ(G) = 5 × 10^(1.3×(G-10)) 颗/平方度 (G=10 → 5, G=12 → 30, G=14 → 150, G=16 → 800, G=18 → 4000)`，`:127` 仍写「替代 V4.8 的 6 次迭代查询，一次估算 + 一次查询搞定」。
  - **新增硬证**：该"现行函数"在代码里**已不存在**——`git grep -n "estimate_mag_lim_by_density" -- 'lib/algorithms/platesolve/cpp/**'` ⇒ 3 命中全为注释（`ipv_select.cpp:360`「替换原一次性密度公式 …」、`:689`、`ipv_select.h:69`），无定义也无声明 ⇒ 文档链接指向的 `ipv_select.cpp:315` 现为 `n_target` 计算式（现读 `:313-316`），与该函数无关。
  - 代码判定与文档相反：`ipv_select.cpp:360-366` 现读「实测真实 alpha = 0.243–0.456（中位 0.2885，R²>0.986）；**原代码/文档写死的 1.3 使密度模型高估 4–5 个数量级**（该偏差尚未在 docs/algorithms 登记为 DISP 条目）」⇒ 现行实现是 `estimate_mag_lim_iterative`（割线迭代），文档仍以旧公式为 Step 5。
  - 参数表与生产相反：文档 `:108-111` 仍写「n_target 上限：宽 FOV(>3°) **150**、窄/中 300、下限 50」，而 `ipv_select.cpp:313-314` 现读注释「宽窄 FOV 统一为 60（与 img_n_target 一致，减少形状碰撞）… 60 颗足够三角形匹配，不需要更大候选集」⇒ 同一参数两处取值互斥。
  - 文档内自证：模型 `5×10^(1.3(G−10))` 在 G=12/14/16/18 的实际取值约为 1990/7.9e5/3.2e8/1.3e11，与其自列例值 30/150/800/4000 相差 66×…1.3e7× ⇒ 该段即使按其自身口径也不成立（上一轮复核已算出，本轮逐字复现在位）。
- **置信**：高

---

### ROW 31 — `lib/algorithms/platesolve/IPV_PIPELINE.md` :: `R7-04.md::P-R7-04-01`
- **问题**：IPV_PIPELINE 把已被否决的"U=角秒"写成现行坐标变换。
- **原证据位点**：`IPV_PIPELINE.md:88-93`、`cpp/ipv/src/ipv_select.cpp:1117-1125`、`src/ipv_solver.cpp:12-14`
- **现在位点**：`IPV_PIPELINE.md:27-28`、`:88-93`；`cpp/ipv/src/ipv_select.cpp:1117-1126`
- **四态判定**：**仍在**
- **取证**：文档面两处未改：`:27-28`「FITS ─► [1 StarSelector] ─► U (图像侧星点, **角秒坐标**) / W (星表侧星点, 角秒坐标)」、`:88-93`「坐标转换 (角秒, 原点图像中心, Y 轴向上): `U[i].x = (det_x - cx) * s0` … 其中 `s0 = 206.265 × pixel_size_um / focal_length_mm`」。实现面恰相反：`ipv_select.cpp:1117-1119` 现读「Step 4: 构建 U 向量组 (**像素坐标**, 原点在图像中心, Y 轴向上) / 用户指导: "角秒不可靠, 应该用像素" / 像素坐标直接用, **不乘 s0**, 避免 FOCALLEN 标称误差影响匹配」，`:1125-1126` 的赋值确为 `output.U[i].x = (det_x[idx] - cx);  // 像素, 不乘 s0`（同文件 `s0` 仍计算于 `:1120` 但不进 U）。工单复验命令 `git grep -n "不乘 s0" -- lib/algorithms/platesolve` 在 HEAD 仍命中多处 ⇒ 现行口径未写进文档。
- **置信**：高

### ROW 32 — `lib/algorithms/star_detection/src/sdet_detector.cpp` :: `R5-25.md::A-R525-10`
- **问题**：包围盒生产端闭区间、所有消费端半开区间 ⇒ 星参数丢最后一行列、右下边框过滤永不触发、背景环混入源像素。
- **原证据位点**：`sdet_detector.cpp:88`、`sdet_api.cpp:308-309`、`lib/algorithms/psf/src/dpsf_psf.cpp:268-269`、`docs/algorithms/STAR_PSF_ALGORITHMS.md`
- **现在位点**：`sdet_detector.cpp:111-114`（生产端闭区间）、`:180-183`/`:206-210`/`:222-224`（消费端半开）、`:281-284`（边框过滤）、`dpsf_psf.cpp:268/:275/:288`
- **四态判定**：**仍在**
- **取证**：生产端 `:111-114` 现读 `if (xi < cc.x0) cc.x0 = xi; … if (xi > cc.x1) cc.x1 = xi; if (s.y > cc.y1) cc.y1 = s.y;` ⇒ x1/y1 = 被包含像素的最大下标（闭区间）。同一变量的三处消费全是半开：
  - 背景环 `:191-199`：`for (yy=oy0; yy<oy1; yy++) for (xx=ox0; xx<ox1; xx++) { if (xx >= cc->x0 && xx < cc->x1 && yy >= cc->y0 && yy < cc->y1) continue; annulus.push_back(...); }` ⇒ 真正属于该星的 `x=cc->x1` 列与 `y=cc->y1` 行未被排除 ⇒ **混入背景环**；
  - 质心 `:222-224`：`for (yy=cc->y0; yy<cc->y1; yy++) for (xx=cc->x0; xx<cc->x1; xx++)` ⇒ **丢最右列/最下行**（而 `flux` 用 `cc->px/py` 全列表，两者不同源）；
  - 边框过滤 `:281-284`：`if (s.rect_x1 >= w || s.rect_y1 >= h) deleted` ⇒ 闭区间语义下贴右/下边的星其 `x1 = w-1`，条件永假 ⇒ **右下边框过滤永不触发**。
  另一消费端同型：`dpsf_psf.cpp:268 int rw = rect_x1 - rect_x0;`、`:275` 越界判据 `rect_x1 > width`、`:288 for (x = rect_x0; x < rect_x1; x++)` ⇒ 半开读法贯穿。
- **置信**：高

### ROW 33 — `lib/algorithms/star_detection/src/sdet_detector.cpp` :: `前台复验-20260924.md::V-14`
- **问题**：与 ROW 32 同一缺陷（复验表述：每星丢最右列/最下行，且这些像素被当背景）。
- **原证据位点**：`sdet_detector.cpp`、`ASTROCS_DESIGN.md`、`AGENTS.md`
- **现在位点**：同 ROW 32
- **四态判定**：**仍在**（同一修复面，登记为重复条目）
- **取证**：同 ROW 32 的全部现读；两条工单的取证对象、行集与后果链完全一致（闭区间生产 + 半开消费 + 背景环）。
- **置信**：高
- **对 D8 的建议**：与 ROW 32 合并为一条实施任务（区间语义统一为半开并同步边框判据与 `rw/rh`）。

### ROW 34 — `lib/infrastructure/aio/src/aio_fits.cpp` :: `C1-01-平台基建源码.md::F-C1-02`
- **问题**：生产 I/O 库内 `#pragma omp parallel for` 用环境线程数，且 `-fopenmp` 只在一侧平台开。
- **原证据位点**：`aio_fits.cpp:1154-1158`、`CMakeLists.txt:504`、`aio_api.cpp:232`、`CMakeLists.txt:527-530/525-526`
- **现在位点**：`lib/infrastructure/aio/src/aio_fits.cpp:1154-1158`、`CMakeLists.txt:560-563`（另 `:605-608`、`:693-695` 同型）
- **四态判定**：**仍在**（两条代码事实原样在位；上一轮把后果论断推翻、只留门缺陷，本轮只判存续）
- **取证**：`aio_fits.cpp:1154` 现读 `#pragma omp parallel for schedule(static)`（无 `num_threads` 子句 ⇒ 取环境/ICV 线程数，不经 Runtime lease 与 benchmark profile），其保护范围 `:1155-1157` 是 int16 转换写缓冲循环。构建面 `CMakeLists.txt:560-563` 现读「# QA-001: aio_fits.cpp 含 omp pragma → -fopenmp 编译消除 unknown-pragmas / `if(UNIX AND OpenMP_CXX_FOUND)` / `target_compile_options(astrocs_aio PRIVATE -fopenmp)`」⇒ 仅 UNIX 一侧启用，Windows 侧同一段 pragma 被静默忽略（同一模式在 `:605-608` calibration、`:693-695` drizzle 重复）。与 AGENTS §6「不硬编码线程…由 benchmark 生成的 profile 决定」相互对立的事实未变。
- **置信**：高

### ROW 35 — `lib/infrastructure/aio/src/aio_fits.cpp` :: `C1-01-平台基建源码.md::F-C1-13`
- **问题**：`aio_write_fits` 就地截断写正式路径（非原子、无 fsync、无校验、无完成清单），已知缺陷只在调用侧绕行。
- **原证据位点**：`aio_fits.cpp:1090`、`aio_api.cpp:232`、`include/astro_image_io.h:106`、`CMakeLists.txt:504`
- **现在位点**：`aio_fits.cpp:1090-1098`；调用侧绕行 `module_adapters.cpp:1446/:1488/:2992`；库内已有原子原语 `aio_atomic_file.h:5`
- **四态判定**：**仍在**
- **取证**：函数体现读 `:1090 FILE *fp = aio_fopen_utf8(path, "wb");` + `:1096-1097 setvbuf(1 MiB)` + 直接 `std::fwrite(...)` ⇒ 正式路径截断写；`git grep -c "aio_atomic" -- lib/infrastructure/aio/src/aio_fits.cpp` ⇒ **0 命中**（阳性对照：同型命令在 `aio_sparse_punch.h` 命中 `:36/#include "aio_atomic_file.h"`、`:394 make_tmp_path`、`:404 fsync_path`），而同库 `aio_atomic_file.h:5` 自述为「AIO 统一"临时文件 + fsync + 原子 rename"落盘原语」⇒ 原语在库内可用却不被 writer 使用。
  调用侧绕行仍在自陈缺陷：`module_adapters.cpp:1446`「…而 `aio_write_fits` 为 `fopen("wb")` 截断 + 增量写（非原子）⇒ 并发消费者…」（其对策是写到 `staging` 再 `:1488 aio_write_fits(im.p, staging.c_str())`），`:2992`「读同一路径竞争, `aio_write_fits` 非原子 ⇒ 撕裂读（P1 数据完整性缺陷）」。⇒ "本体未修、只在调用侧绕行"这一表述在 HEAD 逐字成立。
- **置信**：高

### ROW 36 — `lib/infrastructure/benchmark/backend_host/avx512_backend.cpp` :: `R5-21.md::F-21-05`
- **问题**：AVX512 变体按 `-mavx512f/bw/vl/dq` 编译，但"要求特性"只声明且只检测 AVX512F ⇒ F-only 主机放行后执行非法指令。
- **原证据位点**：`avx512_backend.cpp:2`、`CMakeLists.txt:694`、`cpu_features.h:12-17`、`cpu_features.cpp:44`、`eng/tools/gen_provider_manifests.py:57-58`
- **现在位点**：`avx512_backend.cpp:9-16`（声明已改为同源宏）、`cpu_features.h:26-31`、`CMakeLists.txt:740-744`
- **四态判定**：**已修**
- **取证**：被点名的声明行本身按正确口径改写并给出理由：`avx512_backend.cpp:9-15` 现读「CPU-001 / TRUTHFUL-CONCLUSION-01: 声明必须与**编译旗标**逐位同源 —— 本 TU 由根 CMakeLists.txt 以 `-mavx512f -mavx512bw -mavx512vl -mavx512dq` 编译，故 required = F|BW|DQ|VL。**原声明只写 ACS_FEAT_AVX512F**，理由"硬件上 F 与 DQ/BW/VL 共存"——该前提是错的：AVX-512F 不蕴含 BW/DQ/VL…于是"声明通过、加载放行、执行撞非法指令"…三者（声明／编译／检测）现由 CHK-ISA-SAME-SOURCE 逐位把关」，`:16 #define ASTROCS_BACKEND_REQUIRED_FEATURES (ACS_FEAT_AVX512_PROVIDER_REQUIRED)`。
  同源唯一拼写点：`cpu_features.h:26-31`（`ACS_FEAT_AVX512_PROVIDER_REQUIRED = F|BW|DQ|VL`，注释明写与 backend 声明、`cpu_routing.cpp` 的 provider_supported 共用）；检测面 `cpu_features.cpp:44-52`（gcc builtin 路径）与 `:86-94`（CPUID 路径）均以该组位做 `avx512_group`；编译面 `CMakeLists.txt:740-744` 注释自证"检测面 = ACS_FEAT_AVX512_PROVIDER_REQUIRED（原注释称与 cpu_features.h 一致而该头当时只有 AVX512F 一位 ⇒ 注释与事实相反；TRUTHFUL-CONCLUSION-01 已补位并改为同源宏）"。
  改动提交：`git log -S "ACS_FEAT_AVX512_PROVIDER_REQUIRED" -- lib/infrastructure/benchmark` ⇒ 唯一命中 `5f8c237b`(2026-09-25 13:30:41，晚于工单落盘)。能力未回退（要求位由 1 位增至 4 位）。
- **置信**：高。**未验证项**：`CHK-ISA-SAME-SOURCE` 本阶段禁跑，未取实跑证据。

### ROW 37 — `lib/infrastructure/benchmark/backend_host/avx512_backend.cpp` :: `前台复验-20260924.md::V-06`
- **问题**：AVX512 backend DSO 的加载门只声明 F，而同一仓库的 provider 路径明写"不能只看 AVX512F"。
- **原证据位点**：`CMakeLists.txt:711`、`avx512_backend.cpp:11`、`cpu_features.cpp:44`、`cpu_features.h:16`、`benchmark/cpu/avx512/include/astrocs/cpu/avx512_provider_v1.h`
- **现在位点**：同 ROW 36
- **四态判定**：**已修**（与 ROW 36 同一提交同一修复面；"两条路径互不一致"现已被同源宏消除）
- **取证**：同 ROW 36 全套现读；额外一条：`avx512_backend.cpp:13-15` 的订正理由逐字引用同仓 provider 规则（「同仓另一处早已写明这条规则：…「不能只看一个 AVX512F=true」」）⇒ 工单点名的"两处口径互斥"现已在文本与实现上合流为一处。
- **置信**：高
- **对 D8 的建议**：ROW 36/37 同为已修，合并登记为一条作废项，不再进实施任务书。

### ROW 38 — `lib/infrastructure/benchmark/cpu/avx512/src/avx512_provider.cpp` :: `R5-22.md::Q-22-11`
- **问题**：provider 串行兜底路径释放未取得的 worker 租约，可把全局预算计数压成负值。
- **原证据位点**：`avx512_provider.cpp:243-258`、`backend_host/host_services.cpp:69-84`、`cpu/baseline/src/baseline_provider.cpp:412-457`
- **现在位点**：`avx512_provider.cpp:244-258`（acquire 在条件块内）、`:273-275`（release 在块外）
- **四态判定**：**仍在**
- **取证**：现读结构：`:244 if (cap > 1 && N > 1) {` ⇒ 块内 `:248-255` 才判 `host->executor->acquire/release != nullptr` 并 `acquire(...workers)`，`:256 workers = 1;`（executor 缺失分支）；块外无条件收尾 `:273-275 if (host != nullptr && host->executor != nullptr && host->executor->release != nullptr && workers > 0) host->executor->release(host->executor->user_data, workers);`。⇒ 走串行路径（`cap<=1` 或 `N<=1`，或 acquire 失败降到 1 之外的分支）时 `workers==1` 从未 acquire，却仍调用 `release(…, 1)` ⇒ 全局计数可被压负。工单点名的行集与判据链在 HEAD 未变。
- **置信**：高

### ROW 39 — `lib/infrastructure/benchmark/cpu/avx512/src/avx512_provider.cpp` :: `复核新增-20260924.md::新增-40`
- **问题**：AVX-512 provider 的源码级缓解只护住一个符号：`astrocs_provider_query_v1` 无 `target("no-avx512vl")`，其 TU 级 `-mavx512vl` 仍在 ⇒ "query 路径零 EVEX"既无判据级也无源码级保证。
- **原证据位点**：`avx512_provider.cpp:82-88`、`eng/tests/cpu/avx512/check_avx512_illegal_instr.py:46-53`、`run_provider_avx512_checks.py:69-70`
- **现在位点**：`avx512_provider.cpp:82-88`（宏只挂 cap_gate）、`:385-386`（query 无属性）；判据面 `eng/tests/cpu/avx512/check_avx512_illegal_instr.py:13-15/:31-45`
- **四态判定**：**仍在**（"只护一个符号"未变；"既无判据级"一句需收窄）
- **取证**：`git grep -n "ACS_CPU_AVX512_CAP_GATE_NOEVEX" -- lib` ⇒ 3 命中：`:82` 宏定义（`target("no-avx512f,no-avx512cd,no-avx512bw,no-avx512dq,no-avx512vl")`）、`:85` 非 GCC/Clang 空替换、`:88` 唯一使用者 `extern "C" … int acs_cpu_avx512_cap_gate(...)`。query 入口现读 `:385-387 ASTROCS_EXPORT acs_status ASTROCS_CALL / astrocs_provider_query_v1(uint32_t host_abi, …) ACS_TRY {` ⇒ 前面无任何 target 属性，与文件头 `:75-80`「query + cap_gate) 在任何不支持 CPU 上可安全执行…」的自述不完全对齐（源码级只覆盖一半）。
  需订正工单表述的一处：判据级断言存在——`check_avx512_illegal_instr.py:13-15`「query 期符号函数体 **零** %zmm → 加载/握手路径 (dlopen 静态 init + astrocs_provider_query_v1 + acs_cpu_avx512_cap_gate)…」与 `:31-32/:44-45` 的符号清单已含 query ⇒ "既无判据级保证"不成立；进任务书时应收窄为"源码级缓解只护住一个符号，query 的零 EVEX 只靠反汇编断言，编译器换内联策略即翻红/翻绿"。
- **置信**：中高（判据断言的实际判别力未验：本阶段禁跑该脚本）

### ROW 40 — `qualification/benchmarks/benchmark_common.h` :: `R5-14.md::C-R514-06`
- **问题**：`benchmark_common.hpp` 的 ISA 安全门禁 helper 零调用点，AVX-512 基准会在无 AVX-512 的机器上执行非法指令。
- **原证据位点**：`qualification/benchmarks/benchmark_common.h`、`benchmarks/arithmetic_benchmark.cpp`、`backends/cpu/isa/dispatch.cpp:9-12`
- **现在位点**：`lib/infrastructure/acr/qualification/benchmarks/benchmark_common.hpp:109-115`（helper）、`lib/infrastructure/acr/qualification/benchmarks/arithmetic_benchmark.cpp:65-132/:205-296`
- **四态判定**：**仍在**（承载面为 ACR，`DORMANT` 且不参与生产）
- **取证**：helper 现读 `benchmark_common.hpp:109 inline std::vector<IsaLabel> available_isa_labels(const CpuIsaCaps& caps)`（`:115` 注释"AVX-512 子集要求 F/CD/BW/DQ/VL（与 isa_kernels.hpp 一致）"）；`git grep -n "available_isa_labels"` 全仓 ⇒ **1 命中 = 其自身定义** ⇒ 零调用点（阳性对照：`git grep -n "fill_dynamic_range"` ⇒ 定义 `:273` + 调用 `reduction_benchmark.cpp:56` 两命中，说明该 grep 能抓到调用点）。执行面无门禁：`arithmetic_benchmark.cpp:65/87/109/131` 一带 AVX-512 kernel 均带 `__attribute__((target("avx512f,avx512cd,avx512bw,avx512dq,avx512vl")))`，`:205-296` 的 `case IsaLabel::AVX512: return add_avx512;` 等直接按标签派发，文件内 `git grep "__builtin_cpu_supports\|cpuid"` ⇒ 零命中 ⇒ 无运行期特性判定。
  范围限定（进任务书要带）：该树属 ACR，`docs/KNOWN_LIMITATIONS.md:62` 记 ACR 状态 `DORMANT`（`ASTROCS_ENABLE_ACR=OFF` + `lib/infrastructure/acr/CMakeLists.txt` 的 FATAL_ERROR 守卫）⇒ 非生产路径；工单原对象名写作 `…/benchmark_common.h`，实际跟踪文件为 `.hpp`（路径/后缀订正）。
- **置信**：高

### ROW 41 — `qualification/benchmarks/benchmark_common.h` :: `R5-14.md::C-R514-07`
- **问题**：`fill_dynamic_range` 实际产出 1e-9..1e3，与声明的 1e-3..1e3 相反向。
- **原证据位点**：`qualification/benchmarks/benchmark_common.h`、`benchmarks/reduction_benchmark.cpp`
- **现在位点**：`lib/infrastructure/acr/qualification/benchmarks/benchmark_common.hpp:273-282`
- **四态判定**：**锚失效（该数值事实在 HEAD 不成立）**
- **取证**：现读函数体 `:273-282`：`// 动态范围（1e-3 到 1e3，对数分布）` / `double mag = std::pow(10.0, -3.0 + 6.0 * v);  // 1e-3 ~ 1e3` ⇒ v∈[0,1) 时取值区间正是 `1e-3..1e3`，与自述一致；全文件 `git grep -n "1e-9\|1e-3"` ⇒ 只有 `:257 (+1e-3)`、`:274/:278 (1e-3~1e3)`、`:227 (1e-30)` 一类，无 1e-9。历史核对：`git log -S "-9.0 + 12.0" -- lib/infrastructure/acr` ⇒ 0 命中；`git log -- …/benchmark_common.hpp` ⇒ 该文件自 `eaf32aad`(2026-09-16 目录等价迁移) 起未被改动 ⇒ 不是"本轮被修好"，而是**HEAD 上不存在该事实**，工单所引数值形态与当前树不符（连同 `.h`/`.hpp` 路径差，判锚失效）。
  注：与 ROW 40 同一文件，另一条（helper 零调用点）仍成立，二者不得互推。
- **置信**：中高（"从未有此写法"基于 `-S` 计数与文件历史，未做全历史逐行 diff）

### ROW 42 — `release02/README.md` :: `R5-30.md::A-R5-30-14`
- **问题**：`synth_q2_v3.py` 的 (c)/(d) 臂每轮 gauge 由真值 `B_TRUE` 供数，README §2.2 拿它与 truth-free 的 (a)(b) 比大小。
- **原证据位点**：`synth_q2_v3.py:114`、`release02/README.md:116`
- **现在位点**：`eng/tests/validation/release02/q2_snr_smoothness/synth_q2_v3.py:114`（调用点）与 `:128-131`（`resid_field` 用 `B_TRUE`）、`eng/tests/validation/release02/README.md:112-118`
- **四态判定**：**仍在**
- **取证**：`git grep -n "B_TRUE" -- …/synth_q2_v3.py` ⇒ 5 命中：`:40 B_TRUE = {n: peval(BCOEF[n])…}`、`:50 return {n: S + B_TRUE[n] + scale*NOISE[n]…}`、`:130 m=MASK[n]; num[m]+=wref[n]*(B_TRUE[n][m]-c[n][m]); den[m]+=wref[n]`（在 `def resid_field` 内）、`:146`、`:157 gk={n: B_TRUE[n]-d[n] …}`；迭代主体 `:114 R,gm=resid_field(c,wref,Zin)` ⇒ 每轮 gauge 由真值背景供数，非 truth-free。
  README 侧原文未改（§2.2 表 `:112-118`）：`(a) 参考帧 gauge（旧生产） 0.209/0.260`、`(b) 全局均值零 gauge 0.209/0.260（与 a 逐位相同）`、**`(c) 排除自身 + 阻尼 0.035/0.060（小 4–6×）`**、`(d) 全帧平均自反馈 4.91/4.93（发散）` ⇒ 真值喂出的 (c) 与 truth-free 的 (a)(b) 同表比大小，"小 4–6×"结论仍在。
  文件历史：`git log -- README.md synth_q2_v3.py` 最新为 `ee99ca26`(2026-09-21) ⇒ 工单之后无改动。
- **置信**：高

### ROW 43 — `release02/README.md` :: `R5-30.md::A-R5-30-15`
- **问题**：`weight_consistency_q2.py` 的 "fully self-consistent" 臂是恒真式：gauge 与度量取同一个加权量。
- **原证据位点**：`weight_consistency_q2.py:32-38`、`synth_q2_v3.py:133-138`、`weight_consistency.json`、`release02/README.md:123-124`
- **现在位点**：`eng/tests/validation/release02/q2_snr_smoothness/weight_consistency_q2.py:28-40`、`eng/tests/validation/release02/README.md:121-126`
- **四态判定**：**仍在**
- **取证**：脚本现读：`:28 print('-- final gauge recomputed WITH the mapped weights (fully self-consistent) --')`、`:29-31 wsb=W_NORM; dA,_=joint_ref_fit(wsb,Z,B_order=2,d_order=1)`、`:33-39 def gauge_with(cf, ws): … num[m]+=wv*(Z[n][m]-cf[n][m]); den[m]+=wv … return {n:cf[n]+R}`、`:40-41 rep('a_ref+gauge(mappedW),mappedW', gauge_with(d, ws5), ws5)` ⇒ 施加的 gauge 与评判度量用同一组 `ws5` 权重与同一残差场 ⇒ `Σ w_k·g_k/W ≡ 0` 按构造成立。
  README 侧把恒等标为实测（`:121-126` 现读）：「拟合权重与叠加权重**同源** → … ⇒ `Σ w_k·g_k/W ≡ 0`，**任意覆盖子集 / 任意权重面，阶跃恒 0.0000**（**实测**）」⇒ 与 AGENTS §5"判据必须非退化、恒真门没有证据资格"直接冲突；同段"若 gauge 权重与叠加权重解耦，阶跃重现（0.4285）"未改变恒真臂的登记性质。文件在工单后无改动（同 ROW 42 的 `ee99ca26`）。
- **置信**：高

---

### ROW 44 — `文献 :: DOI 10.1109/TBME.1983.325207` :: `文献::DOI 10.1109/TBME.1983.325207`（判定：关联错／张冠李戴）
- **问题**：van Oosterom & Strackee 1983（平面三角形立体角闭式）被写成"Girard 定理/球面三角面积"用途。
- **原证据位点**：`docs/algorithms/DRIZZLE_GEOMETRY.md:390`、`docs/references/SCIENTIFIC_REFERENCES.md:115`、`:123`
- **现在位点**：`docs/references/SCIENTIFIC_REFERENCES.md:115`（第 53 条，错误用途原样在位）与 `:123`（第 66 条，订正版）
- **四态判定**：**仍在**（并新增一条更硬的形态：同一 DOI 在同文件两条登记、用途互斥）
- **取证**：`git grep -n "325207"` ⇒ 8 处命中，其中 `SCIENTIFIC_REFERENCES.md:115` 现读「53. Van Oosterom & Strackee 1983 … 用途：**Girard 定理**/球面三角面积（DRIZZLE.md §5 Sutherland–Hodgman + **Girard**）」——与被上一轮判为张冠李戴的原文逐字相同；同文件 `:123` 第 66 条却写「用途：球面多边形面积（S-H 裁剪后的扇形三角剖分；单位向量下 Ω = 2·atan2(…)）；ACSD DRIZZLE.md §5 **误记为"Girard 定理"**，实现实为 Van Oosterom 扇形剖分。**核验状态**：Crossref 已核验」⇒ 一条自认误记、一条仍按误记登记，同一 DOI 双登记互斥。
  正确侧已就位：`docs/science/DRIZZLE.md:227`（该式实现归该文献）、`docs/algorithms/DRIZZLE_GEOMETRY.md:132`「**禁用** "Girard 定理"（内角和式）这一命名——本模块无该实现」、`:325` DISP-DRZ-002 登记源码注释侧的旧命名、`lib/algorithms/drizzle/healpix_drizzle/spherical_overlap.cpp:187` 引文已带正确题注。
- **置信**：高

### ROW 45 — `文献 :: DOI 10.5281/zenodo.596036` :: `文献::DOI 10.5281/zenodo.596036`（判定：版本错）
- **问题**：photutils 的 Zenodo 记录 DOI 关联正确但被用错层级（版本错）。
- **原证据位点**：`实验/additive-sky-seamless/results/evidence_code.json:376/:389/:402`
- **现在位点**：`实验/additive-sky-seamless/results/evidence_code.json:376/389/402/415/428/441/454/493`（同一串文本 8 处）
- **四态判定**：**仍在**（且比工单点名的 3 处更多：同一 ref 文本重复 8 处）
- **取证**：`git grep -n "596036"` ⇒ 命中 8 行，`ref` 字段现读为「Bradley, L. et al. 2024, "astropy/photutils", Zenodo, DOI 10.5281/zenodo.596036 (**已核验**: photutils 文档 citation 页与 README "Citing Photutils" 均指向该 DOI)…」；同一 entry 的 `repo` 字段现读「astropy/photutils @ main 8103fec5ae97ed11398357c93e542bfbfea452fc (2026-09-19), **最新发行 tag v0.7.2**」⇒ 把"版本化 Zenodo 记录"当"该 commit/该发行版"的引用层级用，且自标"已核验"。
  与 HANDOVER §8.3 相互印证：仓库侧状态件仍记「引用状态自相矛盾 5 条、越界引用 6 条」为未修（§6 G 组：「**5 + 6 条真实红灯未修**」）⇒ 本类条目在 HEAD 确未收口。
- **置信**：高

### ROW 46 — `CMakeLists.txt` :: `R5-09.md::C-R5-09-03`
- **问题**：vendored 树被"机械路径改写器"改写 3 个文件并留下悬空引用，而哈希锁的读集看不见它们。
- **原证据位点**：`…/CMakeLists.txt`、`cmake/cfitsioConfig.c`、`eng/cmake/cfitsioConfig.c`、`lib/infrastructure/aio/third_party/cfitsio/cmake/cfitsioConfig.c`、`Makefile.in`、`config/ltmain.sh`
- **现在位点**：`lib/infrastructure/aio/third_party/cfitsio/CMakeLists.txt:317`、`:333`；锁文件 `reports/v19r2/source_manifest.csv:2601/:2605`
- **四态判定**：**仍在**
- **取证**：悬空引用现读：`cfitsio/CMakeLists.txt:317 configure_file(eng/cmake/cfitsioConfig.cmake "${CMAKE_CURRENT_BINARY_DIR}/cfitsio/cfitsioConfig.cmake" COPYONLY)`、`:333` 的 install FILES 列亦写 `eng/cmake/cfitsioConfig.cmake`；而 `git grep -ln "eng/cmake" -- lib/infrastructure/aio/third_party` ⇒ 唯一命中该 CMakeLists.txt；被引对象实际在 `lib/infrastructure/aio/third_party/cfitsio/cmake/cfitsioConfig.cmake`（`git ls-files … | grep "cmake/"` 现读），且 `git ls-files eng/cmake | grep -i cfitsio` ⇒ 只有 `cfitsio_sources.cmake`，全仓无 `eng/cmake/cfitsioConfig.cmake` ⇒ 改写器把上游 `cmake/` 前缀误写成 `eng/cmake/`，形成两处悬空。
  哈希锁看不见：`git grep -n "third_party/cfitsio/CMakeLists.txt" -- reports/v19r2/source_manifest.csv` ⇒ `:2601` 行存在（大小 13439 + sha256 `4667ff13…`）但第 4 列为 **`excluded`**；`Makefile.in` 同样在 `:2605` 标 `excluded` ⇒ 被改写文件在清单里有行、在**校验读集**外（与"excluded ⇒ 不核"的语义一致），故锁不会因这两处悬空翻红。
- **置信**：中高（"3 个文件被改写"本轮只逐字核到 CMakeLists.txt 一处 + Makefile.in 在锁内的状态；未逐文件 diff 上游原树）

### ROW 47 — `RELEASE-05/OPEN_QUESTIONS.md` :: `L6-发布与验收证据链.md::L6-08`（判定：确认，S2）
- **问题**：`ASTROCS_DESIGN.md` §13 发布候选条件逐条判定：9 条中仅 1 条"已满足（静态）"，3 条"无证据"。
- **原证据位点**：`ASTROCS_DESIGN.md:758`、`ACCEPTANCE_SPEC.md`、`RELEASE-05/OPEN_QUESTIONS.md:74-152`、`ACCEPTANCE.md:20`、`docs/KNOWN_LIMITATIONS.md:99-107`
- **现在位点**：`ASTROCS_DESIGN.md:805-816`（§13 正文，发布候选条件清单在位）、`工程控制/RELEASE-05/OPEN_QUESTIONS.md`（Q-1 起，:6/:15/:52/:74/:156）
- **四态判定**：**仍在**（属发布就绪判定，不是代码缺陷；本阶段禁测 ⇒ "无证据"三项无法闭合）
- **取证**：§13 现读（`ASTROCS_DESIGN.md:805` 起）：条件清单仍以「发布候选至少满足：合同冻结无冲突、追踪无断链、模块可独立加载/验证/卸载、ACR 生产不可达、无硬编码线程/单线程长计算/无界内存增长、**双平台 CI 通过**、**四层验收全部通过（L4 含两组真实数据目检）**、P0/P1=0、发行包白名单与哈希/provenance 通过」在位，且「最终发布决定只属项目负责人」不变。
  仓内现状逐条对着看仍未达标（且比工单时更明确）：`eng/tools/HANDOVER.md` §7「现有 M42 产品不能作为证据」（端到端须重跑）+ §4.1「当前阶段禁止任何测试与端到端」⇒ L2/L3/L4 与 P0/P1=0 三条在本基线**不可取证**；§6 表「1 | 全量扫描非绿 | 部分：crash 1→0；内容真红仍有约 30 条」；§8.1「51/51 个 M42 落盘产品不携带任何交换对象文档」；Windows 侧构建未开工 ⇒ "双平台 CI 通过"无证据（按双平台裁决属"没做"，不计为"已坏"，但发布条件面仍缺）。
- **置信**：中（判据清单与仓内状态均为现读；"9 条中 1 条满足"的逐条计数来自上一轮成稿，本轮未重做全量判定——禁测使其中 3 条无法判）

### ROW 48 — `_work/D5b/p10_entries.py` :: `D5b-验证与追溯.md::D5b-15`
- **问题**：文献核验器与全部核验证据在未跟踪的 `run/DOC-404/` 下；两篇文档对同一 `[V]/[S]/[U]` 词表给出不同定义；`B1–B90` 实为 87 条 ⇒ 引用"已核验"陈述对下游不可复核，且没有门扫这两层。
- **原证据位点**：`_work/D5b/p10_entries.py`、`docs/research/PHOTOMETRY_RESEARCH_PACK.md:18`、`run/DOC-404/verify_refs.py`、`verify_oss*.py`、`check_pack_refs.py`、`evidence/verified_refs_table.txt`
- **现在位点**：仓库侧成立面：`run/DOC-404/**` 全未跟踪；`docs/research/PHOTOMETRY_RESEARCH_PACK.md`（词表定义处）
- **四态判定**：**锚失效 + 仍在（分两半）**
  - 原锚 `_work/D5b/p10_entries.py` 与 `run/DOC-404/*` 均不在仓库跟踪集（`git ls-files | grep -c "DOC-404"` ⇒ 0；本机 `ls run/` 亦无 `DOC-404`）⇒ 这些位点来自**审查节点/开发节点的未入库临时快照**，在 HEAD 无对应对象，按工包口径判**锚失效并显式说明**。
  - 该条的仓库侧结论（核验证据不可复核）仍在：核验器与其输出从未入库 ⇒ "已核验"陈述对下游不可复核这一事实成立。
- **取证**：`git ls-files | grep -c "DOC-404"` ⇒ 0（阳性对照：同型命令 `git ls-files | grep -c "l2_performance/gates"` ⇒ 11）；`ls run/` ⇒ 只有 PROJECT-GOVERNANCE-01/ci/configs/v6/win006 等，无 DOC-404。词表两处定义与 `B1–B90=87 条` 的计数本轮**未复核到位**（需在 `docs/references/PHOTOMETRY_LITERATURE_REVIEW_ARCHIVE.md` 内逐条数 `| B<nn>` 行，本轮未跑该计数）。
- **置信**：中（不可复核面为高置信；"两篇文档词表不同""87 条"两项未复算）

### ROW 49 — `_work/l1/graph_scan.py` :: `L1-构建与可重现性.md::L1-02-1`
- **问题**：根图可达子树里有 12 处 `project()` 与一处 C++20/MinGW 默认，与"唯一 project()、C++17、禁 MinGW"三份权威同时互斥，且无任何门。
- **原证据位点**：`_work/l1/graph_scan.py`、`CMakeLists.txt`、`lib/algorithms/coverage/CMakeLists.txt:12/:15`、`lib/algorithms/integration/v6/CMakeLists.txt:3`、`ENGINEERING_SPEC.md`
- **现在位点**：`lib/algorithms/coverage/CMakeLists.txt:11`（project）与 `:14`（C++20）、根 `CMakeLists.txt:28`（C++17）；跟踪树内 `project(` 共 17 处
- **四态判定**：**仍在**（计数订正：跟踪树内 17 处 `project(`，非 12；"MinGW 默认"一项在 HEAD 不成立）
- **取证**：`git grep -inE "^[[:space:]]*project\(" -- '*CMakeLists.txt' | wc -l` ⇒ **17**，样本：根 `CMakeLists.txt:15 project(acsd VERSION 0.1.0 …)`、`lib/algorithms/coverage/CMakeLists.txt:12`（实为 `:11`，行内 `project(astro_phase2 LANGUAGES CXX)`）、`lib/algorithms/integration/v6/CMakeLists.txt:9`、`eng/tests/unit/v6_*/CMakeLists.txt` 共 9 处、`eng/tests/{integration,system}/v6_*/CMakeLists.txt` 4 处 ⇒ 与"唯一 project()"权威互斥仍在位。
  C++20 冲突现读：`lib/algorithms/coverage/CMakeLists.txt:14 set(CMAKE_CXX_STANDARD 20)` ↔ 根 `CMakeLists.txt:28 set(CMAKE_CXX_STANDARD 17)`；同文件 `:8-10` 注释自陈该模块以 `EXCLUDE_FROM_ALL` 方式保留自测分支 ⇒ 仍在根图可达子树内。
  MinGW 子项订正：`git grep -in "mingw" -- CMakeLists.txt` ⇒ 仅 `:495` 一处注释「不可用 MSYS2 mingw64 include（gcc 头无法被 MSVC 编译）」，是对 MSVC 的说明而非 MinGW 默认；`CMakePresets.json:16/:31/:60` 的正式 Windows 生成器是 `Visual Studio 17 2022` ⇒ "C++20/MinGW 默认"中的 MinGW 半边**在 HEAD 不成立**，进任务书时删去。
  原锚 `_work/l1/graph_scan.py` 为审查节点临时件，不在仓库跟踪集（同 ROW 48 说明）。
- **置信**：高（project/C++20 两侧均现读逐字）

### ROW 50 — `_work/q209_census2.py` :: `Q2-09.md::Q9-03`
- **问题**：对 HEAD 的在册只读门全量普查：63 个 `fast` 档只读门中 18 个 rc=1、4 个 rc=2、1 个 rc=3，而三张豁免台账共 1 条且与它们零交集 ⇒ 台账与红灯不可对账。
- **原证据位点**：`../_work/q209_census2.py`、`../_work/q209/census/census.json`、`eng/ci/checks.json`、`eng/ci/run_checks.py`
- **现在位点**：**未复核**
- **四态判定**：**未复核（不适用本轮手段）** — 判该条必须在 HEAD 跑 `eng/ci/run_checks.py` 与各门，负责人已裁决本阶段一切测试与端到端停止、且工包明令不得跑任何 `eng/**` 检查器；台账/豁免面（`eng/ci/**`、`waivers`）虽在树内可读，但"18 rc=1 / 4 rc=2 / 1 rc=3 + 零交集"是纯运行时读数，静态读不出。
- **置信**：不适用
- **另**：原锚 `_work/q209*` 为审查节点临时件，不在仓库跟踪集（判锚失效的同型说明）。

### ROW 51 — `artifacts/acceptance/L3/EVIDENCE.json` :: `L6-发布与验收证据链.md::L6-03`
- **问题**：L3 层入库证据自记"SNR 权重链被阻塞、靠 legacy 回退跑完"，且证据载体全在 `run/` 不可复核。
- **原证据位点**：`artifacts/acceptance/L3/EVIDENCE.json:62`、`ACCEPTANCE_SPEC.md:191/:177`
- **现在位点**：`artifacts/acceptance/L3/EVIDENCE.json:62`（`weight_chain` 字段）与 `:64`（`evidence_dir`）
- **四态判定**：**仍在**
- **取证**：现读 `:62`「`"weight_chain": "BLOCKED by PRE-F-01 (no variance/ivar from Phase1 CLI); chain completed via explicit legacy_allow_weight_fallback=true"`」——行号与发现时逐位一致；`:64`「`"evidence_dir": "run/PROJECT-GOVERNANCE-02/PRE-REL/"`」⇒ 证据载体全在未跟踪的 `run/`（本轮实跑 `git ls-files | grep -c "PROJECT-GOVERNANCE-02/PRE-REL"` ⇒ **0**；`run/` 整体不入库由 AGENTS §7 与 `.gitignore` 规定）。
  新增硬证（本条比工单更重）：该字段自陈跑通生产所用的键 `legacy_allow_weight_fallback` 正是 A44 订正后**必须 fail-closed 具名拒绝**的 token 之一——`docs/contracts/PUBLIC_API.md:2186` 现读「凡出现在配置读取/路由/解析路径上的 legacy 权重域 token（auto/ivar/equal/support_x_snr2/整数 0|2/weight_mode 键/**legacy_allow_weight_fallback 键**）一律 fail-closed 具名拒绝」⇒ 入库证据记录的路径与现行合同互斥。
- **置信**：高

### ROW 52 — `artifacts/acceptance/L4/STATUS.json` :: `L6-发布与验收证据链.md::L6-02`
- **问题**：L4 视觉验收唯一入库证据自证"未跑"，而验收包必需的 PNG 被全仓 ignore 规则永久排除。
- **原证据位点**：`artifacts/acceptance/L4/STATUS.json:14`、`工程控制/RELEASE-05/SUMMARY.md:80-82`、`artifacts/evidence/release-05/AUDIT_PACKAGE.md:52-53`、`eng/tools/e2e/render_vis.py`
- **现在位点**：`artifacts/acceptance/L4/STATUS.json:13-14`、`.gitignore:60`
- **四态判定**：**仍在**
- **取证**：现读 `STATUS.json:14`「`"full_run_status": "NOT RUN on this node: M42 ~196 lights ~106GB Phase1, GC ~157 lights ~85GB; available disk ~108GB"`」、`:13` 的 `demo` 指向 `run/PROJECT-GOVERNANCE-02/PRE-REL/L4_l3demo/`（未跟踪）⇒ 唯一入库 L4 证据仍自证未跑。
  PNG 排除仍在：`git grep -n "png" -- .gitignore` ⇒ `.gitignore:60: *.png` ⇒ L4 的目检载体（`full.png` 与 4×4 分块）按规则永久不能入库 ⇒ "验收包必需 PNG 被永久排除"这一机制性冲突未解。
- **置信**：高

### ROW 53 — `artifacts/acceptance/l2_performance/real16_bitwise_bitwise.json` :: `R5-50.md::PERF-38`
- **问题**：逐位一致证据三件套互相给出互斥判据，README 只引用其中被推翻的那一个。
- **原证据位点**：`real16_bitwise_bitwise.json:34`、`real16_bitwise_cmp/compare_summary.md:6`、`real16_bitwise_verdict.json`
- **现在位点**：同三件（均在位）+ `artifacts/acceptance/l2_performance/README.md:63-66`
- **四态判定**：**仍在**
- **取证**：`git grep -n "\"verdict\"" -- …/real16_bitwise_bitwise.json …/real16_bitwise_verdict.json` ⇒ 两条互斥读数原样在位：`real16_bitwise_bitwise.json:34 "verdict": "FAIL"`、`real16_bitwise_verdict.json:2 "verdict": "PASS"`；第三件 `real16_bitwise_cmp/compare_summary.md:6` 现读「结论: **DER**」（byte 模式 rel=0 abs=0，A/B 各 3094 文件）。
  README 侧仍只引被推翻的那一个：§5（`:63-66`）现读「`real16_bitwise_verdict.json`：verdict = **PASS**。3094 个文件中 3077 个 sha256 逐字节相同…唯一被点名的非科学差异是 `p2_samples.json:/sampler_config/cpu_workers`」，未提 `bitwise.json` 的 FAIL 与 compare_summary 的 DIFFER ⇒ 三件套互斥 + 单侧引用未变。
- **置信**：高

---

### ROW 54 — `artifacts/evidence/audit-2026-01/FIX_LEDGER.csv` :: `R5-80.md::E-80-10`
- **问题**：`check_full_integration` 自造状态词 `DELIVERED` 并把 P1 挂债从 JUnit 计数里摘除；其"waivers []"检查无对象。
- **原证据位点**：`check_full_integration.py:107-108`、`FIX_LEDGER.csv`、`ASTROCS_DESIGN.md:792-800`、`repo/waivers.json`
- **现在位点**：`eng/tools/quality/contracts/check_full_integration.py:4/:8-9/:136`、`eng/tools/quality/conclusion_vocabulary.json:27`、`eng/tools/quality/check_conclusion_truth.py:12/:503`、`eng/ci/exemptions.json`
- **四态判定**：**已修**
- **取证**：被点名的造词与无对象豁免检查都在**同一文件里按正确口径重写**（不是整段删除）：
  `check_full_integration.py:4`「Checks: 全生产运行；豁免面为空（**正本 = `eng/ci/exemptions.json`**）；P0/P1=0」；`:8-9`「原 status 有一个**造词**取值（ASTROCS_DESIGN.md §12.5 唯一状态阶梯里不存在该词），其语义是「存在挂账 P1 债务也算通过」。现改为只输出…」；`:98`「Check P0/P1: 豁免必须条件化, 否则「有债务也算通过」永远可达」、`:136`「挂账债务如实登记在 debt 字段；**只要存在任何 P1 finding 就是 FAIL**」。
  `git grep -n "waivers" -- …/check_full_integration.py` ⇒ **0 命中**（原悬空 `waivers.json` 引用改指实存件，`git ls-files eng/ci/exemptions.json` ⇒ 该文件在库）；全仓 `git grep -n "DELIVERED"` ⇒ 命中只剩两处**反面对象**：新增判据 `check_conclusion_truth.py:12`「DELIVERED 是造词：最高设计的唯一状态阶梯里没有这个词」与其 `:503` 的 `denylist {"DELIVERED": "造词"}` + 夹具 `:529/:630`，以及 `conclusion_vocabulary.json:27` 的词条说明（"系 check_full_integration.py 自造…越词即红"）。
  互证：`eng/tools/HANDOVER.md:161` 把「`DELIVERED` 造词 / `waivers.json` 悬空 / 分母 713 无定义 / 快照失配」列为 B 组已由 `5f8c237b` 修，且提交正文同句点名「用于让「有债务」也算通过的自造状态词取消，状态词表改为实时解析最高设计的验收章节生成；悬空的豁免件引用与无定义的分母改为同一条命令产出」⇒ 内容与标题互证，非只信标题。
- **置信**：高。**未验证项**：新判据 `check_conclusion_truth.py` 的判别力本阶段禁跑，未取实跑证据。

### ROW 55 — `artifacts/evidence/compress-01/code/build.sh` :: `R5-54.md::C-54-10`
- **问题**：`build.sh` 只构建 5 个 C 工具中的 2 个，研究包 §7 步骤 5 却直接调用未构建的二进制。
- **原证据位点**：`build.sh:6-9`、`rt_diff.c`、`fits_loss.c`、`diag_cfitsio.c`、`COMPRESSION_CODEC_RESEARCH_PACK.md:603-605`
- **现在位点**：`artifacts/evidence/compress-01/code/build.sh:6-10`；调用方 `docs/research/COMPRESSION_CODEC_RESEARCH_PACK.md:598-599`
- **四态判定**：**仍在**
- **取证**：`build.sh` 现读全文只有两条 gcc 行：`:6-7 echo "== gcc zstd_bench ==" / gcc -O2 -o bin/zstd_bench code/zstd_bench.c -lzstd`、`:8-9 echo "== gcc fits_bench ==" / gcc -O2 -o bin/fits_bench code/fits_bench.c -lcfitsio -lm`；而 `git ls-files …/code | grep \.c$` ⇒ 5 个 C 工具（`diag_cfitsio.c`、`fits_bench.c`、`fits_loss.c`、`rt_diff.c`、`zstd_bench.c`）⇒ 三个未构建。
  调用侧未改：研究包 `:598-599` 现读 `COMPRESS01_TMP=/tmp ./bin/rt_diff data/signal_leaf_1371567.bin GZIP_2_q0` 与 `./bin/fits_loss data/signal_leaf_1371567.bin RICE_1` ⇒ 步骤 5 直接执行 build.sh 从未产出的两个二进制 ⇒ "按 §7 全跑"不可复现。
- **置信**：高

### ROW 56 — `artifacts/evidence/compress-01/code/run_bench.sh` :: `R4-09.md::RC-02`
- **问题**：`run_bench.sh / run_bench2.sh / run_bench3.sh` 全步骤失败仍 `exit 0`："能红能绿"只存在于打印，不存在于退出码。
- **原证据位点**：`run_bench.sh:9-32`、`run_bench2.sh:8-26`、`run_bench3.sh:8-21`、`zstd_bench.c:101-102`
- **现在位点**：`artifacts/evidence/compress-01/code/run_bench{,2,3}.sh`（三份同型）
- **四态判定**：**仍在**
- **取证**：三份脚本头部现读均为 `#!/bin/bash` + `set -uo pipefail`（**无 `-e`**）；步内形如 `timeout 3600 ./bin/zstd_bench … 2> logs/….err` 后仅 `echo "  rc=$? rows=$(wc -l < evidence/….csv)"`（把退出码打印成文本、不判不中止），三份脚本结尾都是 `echo "ALL DONE"` ⇒ 最后一条命令是 echo ⇒ 无论各步 rc 多少，脚本整体 rc=0。
  对照：同目录只有 `build.sh:3` 用 `set -euo pipefail` ⇒ 判据缺失是这三份基准脚本特有的。
- **置信**：高

### ROW 57 — `artifacts/evidence/compress-01/code/zstd_bench.c` :: `R5-55.md::C-55-07`
- **问题**：`zstd_bench.c:43-52` 命令行文件数 `nf` 无边界检查即写入 `files[4096]`。
- **原证据位点**：`zstd_bench.c:43`
- **现在位点**：`artifacts/evidence/compress-01/code/zstd_bench.c:43-45/:52`
- **四态判定**：**仍在**
- **取证**：现读 `:43 const char* files[4096];`、`:44 int nf = 0;`、`:52 else files[nf++] = argv[i];` ⇒ 参数循环内对 `nf` 无任何上限判据 ⇒ 超过 4096 个文件实参即栈内越界写。
  同函数内的对照写法证明这是遗漏而非有意：`:53-55` 的 levels 解析带界 `int levels[64]; … while (tok && nl < 64) { levels[nl++] = …; }`。
- **置信**：高（证据件为实验/基准小工具，非生产路径；风险等级按原判定 S2）

### ROW 58 — `artifacts/evidence/compress-01/verify/final_numbers.py` :: `R4-09.md::RC-01`
- **问题**：`final_numbers.py` 把"无损逐位一致瓦片数"硬编为常量，报告声称的"重算汇总"段对该字段是假复算。
- **原证据位点**：`final_numbers.py:87-89`、`final_numbers.json`、`COMPRESSION_CODEC_RESEARCH_PACK.md:617-619`
- **现在位点**：`artifacts/evidence/compress-01/verify/final_numbers.py:87-91`（订正说明）+ `:102-113`（同源计数实现）
- **四态判定**：**已修**
- **取证**：被点名的行本身改为同源计数并自陈原缺陷：`:87-91` 现读「TRUTHFUL-CONCLUSION-01（一页纸 S2-B）：本循环原先把"位相等样本数"硬编为 20/103，与同一 dict 里的 `corpus_n=len(rows)` 不是同一来源（103 与 188 无法相互解释），且硬编值无法随证据更新 —— 属"不可核的结论"。现改为从**同一批 rows 的 bitwise 列**计数：分子与分母同源；缺列/空行集时置 None 并写明原因（fail-closed，不猜数）」；实现 `:102-105 if rows and "bitwise" in rows[0]: _bw = [int(float(r["bitwise"])) for r in rows if r.get("bitwise") not in (None, "")] … bw_src = os.path.join(EV, "fits_codecs.csv") + "#bitwise"`、`:109 bw_why = "fits_codecs.csv 缺 bitwise 列或行集为空（fail-closed：不给数）"`、`:112-113` 输出 `bitwise_eq_corpus / corpus_n / bitwise_source / bitwise_unavailable_reason` ⇒ 常量已换成读数 + 出处 + 缺数具名，规模未回退。
  与 `5f8c237b` 提交正文「检查器可信化：判据改读真实对象，判据值可推导，声明输入缺失即红」同族，且以文件内容为准（不只信标题）。
- **置信**：高。**未验证项**：未实跑该脚本（禁测），只读判据实现与自述。

### ROW 59 — `artifacts/evidence/governance-01/research/R-4_资源门与观测权威落点.md` :: `R5-59.md::C-R5-59-13`
- **问题**：A1 段"保留 `--strict-resource-gate` 可复现旧语义"与同批 R-4 的逐字探针矛盾。
- **原证据位点**：`02_待裁决清单.md:12`、`R-4_资源门与观测权威落点.md:420-429`、`/nonexistent.json`、`lib/infrastructure/cli/command_tree.h:75-82`
- **现在位点**：`artifacts/evidence/review-package/02_待裁决清单.md:12`、`R-4_…md:393`（未订正的表格行）、`:420-429`（探针）、`:730`（订正结论）
- **四态判定**：**仍在**（同文档内已有订正段，但被点名的两处未改）
- **取证**：`git grep -n "strict-resource-gate"` ⇒ 10 命中，三处构成互斥：① `02_待裁决清单.md:12` 现读「P26 已按你的决定落地——程序只记录…；**保留 `--strict-resource-gate` 可复现旧语义**」（行号与发现时一致，未改）；② `R-4_…md:393` 表格行仍把"裁决权"写为 `record_only`（默认）/ `Enforced`（**仅 `--strict-resource-gate`**）；③ 同文 `:420-429` 的逐字探针现读「`$ ./build/astrocs normalize --json /nonexistent.json --strict-resource-gate` → `astrocs: unknown flag '--strict-resource-gate' rc=2`」并给出白名单依据（`command_tree.h:75-82` 不含该旗标）⇒ ①②宣称"可复现旧语义"的能力在真 CLI 上不可达。
  同文档 `:730` 已写「`--strict-resource-gate` 的定位已订正：它不在命令树白名单，真 CLI rc=2…故「程序内永不 enforce」是当前事实」⇒ 订正只落在结论段，表格行与对外待裁决清单未同步（本条剩这一半）。
  文件历史：`git log -- R-4_…md` 最新为 `9a2b5d11`(2026-09-21) ⇒ 工单之后无改动。
- **置信**：高

### ROW 60 — `artifacts/evidence/release-05/D-14_STALE_PATH_CLEANUP.md` :: `L6-发布与验收证据链.md::L6-08`
- **问题**：本包把"测试现场覆写入库证据"正当化，并给该覆写开了 dirty 豁免面，使发布门"零 waiver"不可判。
- **原证据位点**：`D-14_STALE_PATH_CLEANUP.md:33-43`、`MEASUREMENTS.c`、`eng/ci/checks.json`、`eng/ci/run.py:763-764`、`docs/ci/03_GATES.md:97`
- **现在位点**：`D-14_STALE_PATH_CLEANUP.md:33-45`、`eng/ci/checks.json`（16 处 `dirty_ignore_prefixes`）、`eng/ci/run.py:917-923`
- **四态判定**：**仍在**（豁免面反而扩大）
- **取证**：证据件原文未改（`git log -- …/D-14_STALE_PATH_CLEANUP.md` 最新 `25c8227d`(2026-09-22)，早于工单）：`:34-38` 现读「端到端验证：ISA 后端测试现在写**已跟踪证据树** … ISA-001/002/003 三份已跟踪 `MEASUREMENTS.csv` 被真实刷新（`git status` 显示 `M artifacts/evidence/prerelease-v5/ISA-00{1,2,3}/MEASUREMENTS.csv`）」+ `:41-45`「写入面已在注册表显式登记：`eng/ci/checks.json` 的 `CHK-UNIT` 新增 `dirty_ignore_prefixes: ["artifacts/evidence/prerelease-v5/"]`…故必须显式登记，否则工作区对比会判红」。
  豁免面现状：`git grep -c "dirty_ignore_prefixes" -- eng/ci/checks.json` ⇒ **16** 处登记（消费实现 `eng/ci/run.py:917-923`「per-check dirty 豁免（显式登记制）… `ignore_exact |= set(check.get("dirty_ignore_exact", []))`；`ignore_prefixes += list(check.get("dirty_ignore_prefixes", []))`」）。
  "零 waiver 不可判"仍在：`docs/ci/03_GATES.md:7-8` 的豁免正本只谈"检查项"级豁免（`eng/ci/exemptions.json`，只减不增），未把 `dirty_ignore_*` 纳入同一正本 ⇒ 工作区脏面豁免与发布门的豁免账本两套分离，无法由一条命令判"零 waiver"。
- **置信**：高

---

<!-- PROGRESS: 60/90 -->
