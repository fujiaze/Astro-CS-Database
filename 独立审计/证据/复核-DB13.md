# AUDIT-06 第②层独立复核 · 批次 DB-13（研究与佐证／追溯／验证档案）

被复核成稿：`独立审计/证据/AUD-101-DB-13.md`（384 行，16 件）
基线：`F:\Astro dev\Astro CS Normalization Database` @ HEAD=`c8f64e9a`
复核方式：**四件全部先自行取证并写下结论，之后才打开成稿对比**；取证面 = git 跟踪集（`git ls-files` 3896 件），本机磁盘残留不作为仓库事实。全程只读，未构建、未 ctest、未跑 `run_checks.py` 与任何 `eng/**` 检查器（含 `--self-test`），未 import 仓库 Python。
脚本与读数：`独立审计/复算件/db13-v/`（`w2_traceability.py`、`w2b_traceability.py`、`w2c_c8.py`、`w2e_recon.py`、`W1_*.txt`、`tracked_files.txt`）。

开工工作树（原文）：

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
$ git -C "F:/Astro dev/Astro CS Normalization Database" rev-parse --short HEAD
c8f64e9a
```

---

## W1

**W编号**：W1（成稿 §1.8 / §1.12 / §1.13 / P3）

**判定**：确认（成稿事实面成立；其中"需负责人裁决"这一处置判断降级，见"与成稿差异"）

**我的独立取证（数＋命令）**

四件事各自取证如下。被点名的模式 = **`psfsw_robust`**（其对象身份 `psfsw_robust_weight`）。

① 科学正本与合同的原文判定（逐字）

- `ASTROCS_DESIGN.md:225`：`**全程只有 SNR，没有"权重模式"这个概念**：…权重是 Phase2 集成时按天球像素对应的输入帧集合现场计算的派生量`；`:227`：`进入科学叠加权重的量只有由 SNR 换算的逆方差；使偏差随帧而变的量（含 PSF 拟合质量代理）作诊断`；`:228`：`PSF 拟合质量代理（FWHM、残差尺度等）只作诊断。`
- `docs/design/UNIFIED_MODEL.md:58`：`**\`psfsw_robust_weight\` 不是现行对象**…旧产品若声明该对象 ⇒ **显式拒绝 + 迁移提示**（一律显式处置）`
- `docs/science/PSF_SIGNAL_WEIGHT.md:58`：`对象身份 \`psfsw_robust_weight\` **已退役**…接受面 = 具名拒绝`；`:91`：`已退役对象 token（\`psfsw_robust\`）…**没有任何 token 属于合法口径**`
- `docs/contracts/DATA_SEMANTICS.md:3259`：`**禁用 token**（PSFSW-RETIRE-01/03；§9.73 A44）…**不在**生产接受集（\`FZ-MODE-PRODUCTION = {point_information, surface_gls}\`）…接受面 = 空`；`:3305`：`\`W_psfsw\` 的**对象身份** \`psfsw_robust_weight\` 不在生产接受集…\`W_psfsw\` **的落点 = 诊断面**`
- 命令：`git grep -n "psfsw_robust" -- ASTROCS_DESIGN.md docs/design/UNIFIED_MODEL.md docs/science/PSF_SIGNAL_WEIGHT.md docs/contracts/DATA_SEMANTICS.md`

② 验证档案层与它的机器源各自列成什么

档案层（`docs/validation/v6/`，6 件，全部被 `docs/DOCUMENT_INDEX.yaml:1484-1513` 登记为 `status: ACTIVE_NORMATIVE`；同文件 `:21-22` 注释「活动设计档案…照实登记在 active 区段，**不当作归档**」）：

- `BASELINE_COMPARISON_MATRIX.md:31`：`| \`psfsw_robust\` | production | psfsw_robust_weight | …`
- `QA_MATRIX.md:44`：`冻结 mode（原样继承）：生产 = \`point_information | surface_gls | psfsw_robust\``；`QA_MATRIX.md:26`：`3 生产模式 × 2 文档基线 + 1 延迟模式`；`README.md:11`：`三生产模式 × 文档基线 + 延迟模式`

机器源 JSON：

- 文档自述的机器源 `BASELINE_COMPARISON_MATRIX.md:7` = `reports/v6/qa-design/data/baseline_matrix.json` —— 该路径跟踪集 **0 件**：`git ls-files -- "reports/v6" | wc -l` → `0`（`reports/` 下只有 `v19r2`，`git ls-files reports | wc -l` → 3）。
- 实存机器源 `artifacts/evidence/v6/qa-design/data/baseline_matrix.json:54-55`：`"mode": "psfsw_robust", "class": "production"`；其 `production` 类行 = `['point_information','surface_gls','psfsw_robust']`（3 行）。
- `artifacts/evidence/v6/qa-design/qa_matrix.json:10-13`：`"production_modes": ["point_information","surface_gls","psfsw_robust"]`；`:42-45`：`"forbidden_production_modes": ["psf_snr_power","0","auto","support_x_snr2"]` —— **禁产名单里没有 `psfsw_robust`**，即为拦本次违例而设的负例面自身不拦它。
- 命令：`PYTHONDONTWRITEBYTECODE=1 python -B -c "import json;q=json.load(open('artifacts/evidence/v6/qa-design/qa_matrix.json',encoding='utf-8'));print(q['production_modes'],q['forbidden_production_modes'])"`

③ 合同机器登记件内部"生产模式集合"字段 vs 它自己的 FROZEN 条款取值 —— 确为两个不同集合（逐字）

同一份 `eng/contracts/data/v6_clause_registry_v1.json`：

- 字段侧：`#weight_modes.production` = `["point_information", "surface_gls"]`（**2 元**）；`#weight_modes.retired` = `["psfsw_robust"]`；`#weight_modes.mode_details` 的 psfsw 行 `status = "RETIRED_NOT_PRODUCTION"`
- 条款侧（文件 `:683-698`）：`{"id":"FZ-MODE-PRODUCTION", "value": "point_information | surface_gls | psfsw_robust"（**3 元**）, "status": "FROZEN", "required_freeze_id": true}`
- 自我否证：同文件 `#a44_deprecation.psfsw_retire_03_correction`（`:2811`）逐字写「生产模式接受集收窄（**FZ-MODE-PRODUCTION {point_information, surface_gls}** / FZ-MODE-RETIRED）」——它宣称的 `FZ-MODE-PRODUCTION` 取值与该条款自己的 `value` 字段不相等；`#weight_modes._psfsw_retirement_note` 同向
- 连带第 3 处：`#fail_closed` 21 条中的 `G-EPSF-PRESENT` 条件仍写「**三生产模式**缺 effective PSF / 只给 FWHM 标量」
- 命令：`PYTHONDONTWRITEBYTECODE=1 python -B -c "import json;d=json.load(open('eng/contracts/data/v6_clause_registry_v1.json',encoding='utf-8'));print(d['weight_modes']['production'],[c['value'] for c in d['clauses'] if c['id']=='FZ-MODE-PRODUCTION'],[c['status'] for c in d['clauses'] if c['id']=='FZ-MODE-PRODUCTION'])"`

④ 以该模式为被测对象的在册门禁数（我数的，四条口径）

| 口径 | 在册总数 | 以 psfsw 为对象 | 其中被测对象**即该模式**（标题级） |
|---|---|---|---|
| `qa_matrix.json` 科学门 `gates[]` | 44 | 12（`G-ANA-02/06/07`、`G-MC-04/05`、`G-INJ-03/04/05/06`、`G-BASE-01/03/05`） | **4**（`G-ANA-07`、`G-MC-04`、`G-INJ-06`、`G-BASE-03`） |
| 登记件 `#fail_closed` | 21 | **7**（`G-PSFSW-UNIT/-NORMALIZATION/-4COMP/-4COMP-UNITS/-FAILCLOSED/-COV` + `G-EPSF-PRESENT`） | 6 |
| 登记件 `#clauses[]`（id 含 `PSFSW`） | 96 | **31**（7 条 `FZ-*PSFSW*` + 24 条 `PSFSW-T-*`/`PSFSW-COMPOSITE-*`/`PSFSW-BASELINE-NONINFERIOR`） | — |
| `eng/ci/checks.json` 在册检查 | 150 | 2（`CHK-PSFSW-RETIRED-STATIC`、`CHK-PSFSW-RETIRED-NEGATIVE`） | — |

命令（读数存 `复算/db13-v/W1_counts.txt`、`W1_gatecount_qa.txt`、`W1_registry_psfsw.txt`、`W1_ci_checks.txt`）：

```text
PYTHONDONTWRITEBYTECODE=1 python -B -c "
import json,re
q=json.load(open('artifacts/evidence/v6/qa-design/qa_matrix.json',encoding='utf-8'))
print(len(q['gates']),[g['gate_id'] for g in q['gates'] if re.search('psfsw',json.dumps(g,ensure_ascii=False),re.I)])
print([g['gate_id'] for g in q['gates'] if re.search('psfsw',g.get('title',''),re.I)])
r=json.load(open('eng/contracts/data/v6_clause_registry_v1.json',encoding='utf-8'))
print(len(r['clauses']),len([c for c in r['clauses'] if 'PSFSW' in c['id']]),len([x for x in r['fail_closed'] if 'PSFSW' in json.dumps(x)]))"
```

判"门为何不红"（只读实现）：退役门 `eng/ci/check_psfsw_retired.py:27` `DEFAULT_ROOT = "eng/contracts/schemas/unified"`（同件 `:10` 判据自述同范围）—— 扫描面**不含** `docs/validation/v6/**` 与 `artifacts/evidence/v6/**`；文档层门 `eng/ci/check_no_weight_mode.py` 的判据 R1/R2 只查 `weight_mode` 裸键（`:40`）与「权重模式」中文 token（`:49`）是否同行带 A44 留痕，扫描根 `SCAN_DIR = "docs"`（`:51`），**不判"某退役 token 被列为 production"** ⇒ 档案层的 production 行不在任何在册门的判据面内。命令：`grep -n "^SCAN_DIR\|^ASCII_TOKEN\|^ZH_TOKEN" eng/ci/check_no_weight_mode.py`；`grep -n "DEFAULT_ROOT" eng/ci/check_psfsw_retired.py`（读数 `复算/db13-v/W1_gateroot.txt`）。另：条款 `FZ-MODE-RETIRED` 被 8+ 处现行文档与 `lib/infrastructure/cli/v6_runtime_contract.h:113-119`、`eng/ci/check_no_weight_mode_code.py:80` 作为 operative 条款引用，但登记件 `#clauses[]` 内**无该条**（`FZ-MODE-*` 只有 PRODUCTION/BASELINE/DEFERRED 三条，`required_freeze_ids` 19 条内亦无）——这是同一处矛盾的第二形态。

**我的定性（在看成稿之前已写定）**：这是**档案层过期未跟改 + 机器登记件一处字段未跟改**，不是现行生产口径有二义。理由：正向口径在六面完全一致并且可执行——最高设计 §3.1、设计层 UNIFIED_MODEL:58、科学层 PSF_SIGNAL_WEIGHT §4、合同层 DATA_SEMANTICS §31.3/§31.7、登记件 `weight_modes`/`mode_details`、实现 `v6_runtime_contract.h:113-119`（`--mode psfsw_robust` 显式拒绝 + 迁移提示）与 `lib/algorithms/coverage/tests/weight_mode_retire_negative_test.cpp:105-106`（该拒绝被负例钉住）。按负责人口径「层级向下·口径唯一」，下层与上位相反即下层错，无需裁决即可定案。**需要负责人拍的只剩整改方向**（二选一，均不涉科学口径）：(a) 就地改成 2 模式并给 v6 六件补 A44 留痕；(b) 把 v6 六件从 `ACTIVE_NORMATIVE` 降级出正式文档层。我的推荐 = (b) 为主 + 登记件 `FZ-MODE-PRODUCTION.value` 与 `G-EPSF-PRESENT` 条件按 (a) 就地订正。

**与成稿差异**

- 一致：`BASELINE_COMPARISON_MATRIX.md:31`、`QA_MATRIX.md:44`、机器源 `class=production`、登记件两处相反（`weight_modes.production` 2 值 vs `clauses[FZ-MODE-PRODUCTION]` 3 值且 `status=FROZEN`）、44 门中 12 门含 psfsw、`README.md:19-21` 把该自相矛盾件指定为"现行机器登记表" —— 我全部独立复算命中，含成稿列举的门 id 名单。
- 成稿缺（我补）：`QA_MATRIX.md:26` 与 `README.md:11` 的"3/三生产模式"计数；`forbidden_production_modes` 不含 `psfsw_robust`（负例面自身漏项）；`#fail_closed.G-EPSF-PRESENT` 仍写"三生产模式"（登记件内**第三处**相反面）；`FZ-MODE-RETIRED` 被广泛引用却未在册；**门不能红的机制**（`check_psfsw_retired.py` 扫描面只及 `eng/contracts/schemas/unified`、`check_no_weight_mode.py` 判据不含 production 分类）；文档自述机器源路径 `reports/v6/...` 跟踪数 0 与实存 `artifacts/evidence/v6/...` 的分裂；"被测对象即该模式"的窄口径 = 4 门（成稿只给宽口径 12）。
- 成稿说 `BASELINE_COMPARISON_MATRIX.md`「没有同目录其余 4 份的 A44 作废横幅」——复算成立（该件 `:1` 只有 DOC-001 溯源注记，无 A44 横幅）。
- 判断分歧：成稿 §8.3 落「需负责人裁决」。我判**不需**（口径可自决），需裁的只剩整改方向。

**定级建议**：**P1**（现行 `ACTIVE_NORMATIVE` 层与两份现行合同、科学正本、实现相互否证；受影响在册门 12 宽 / 4 窄；无门能拦 ⇒ 会持续再生产）。若负责人选择继续把 v6 六件留在 active 层，则改值前该层任一门读数不可采信，应升 **P0**。

**缺什么证据**：无（四问全部可静态定案）。唯一需实跑才能定的是"改后是否全绿"，命令：`python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 8 -- python3 eng/ci/run_checks.py --check CHK-PSFSW-RETIRED-STATIC`（本批复核禁跑，未执行）。

---

## W2

**W编号**：W2（成稿 §1.2 / §1.4 举证 F、§1.5 举证 G、§1.6 举证 H、P10、P11、§5 表"派单关注点 3"）

**判定**：确认（四个子项全部独立复算成立）；其中**头条数字降级**——成稿的 137/64/53 三口径均不可复算，且举例中 4 个 id 的层状态实为 `MISSING` 非 `VERIFIED`

**我的独立取证（数＋命令）**

对象规模：矩阵 `TRACEABILITY_MATRIX.json` = 30 行 × 24 列（9 层组）；`TRACEABILITY_MATRIX.csv` = 1 表头 + 30 行；`docs/TRACEABILITY.csv` = 1 表头 + 67 行；`TRACEABILITY_LAYERS.csv` = 1 表头 + 9 层；`docs/contracts/INDEX.yaml` 注册 id = 116（type 分布 DATA 38 / ALG 36 / API 17 / SCI 14 / TEST 9 / MOD 1 / ARCH 1）。

**子项①「一批标 VERIFIED 的层 ID 在指定合同索引里零命中」——成立，但按口径给三个数**

| 口径（我的定义） | 数 |
|---|---|
| 矩阵 8 个 id 列中，单元格状态 `VERIFIED` 的**去重**非占位 id | 140 |
| 其中在 `docs/contracts/INDEX.yaml` 注册 id 集里零命中 | **75** |
| 再限定到 spec §3 自己圈定的"合同层"（SCI/ALG/DATA/API/ARCH/TEST，排除 SRC/EVID） | 111 中零命中 **46** |
| 全部非占位去重 id（含 MISSING 状态单元格） | 159 中零命中 91（全文包含口径 = 86） |

零命中实例：SCI `SCI-P1-PSF-001`/`SCI-P1-STAR-001`；ALG `ALG-COS-001`；DATA 11 条（含 `DATA-P1-CAL/-COS/-DRZ/-FITS`、`DATA-GAIA-001`、`DATA-HIPS-001`、`DATA-P2-RES/-SMP`、`DATA-P3-PROPS/-VER`、`DATA-TILE-001`）；API 12 条（含 `API-ABI-001`、`API-P1-001/-003/-008`、`API-P2-001`、`API-P3-001`、`API-GAIA-001`、`API-CPU-001`、`API-IO-STREAM-V1`）；ARCH `ARCH-CPU-001`；TEST 19 条；EVID 7 条。
命令：`python -B 复算/db13-v/w2_traceability.py`（读数 `W2_out.txt`）、`w2e_recon.py`（`W2e_out.txt`）、`w2b_traceability.py`（`W2b_out.txt`）。明细 JSON：`W2_zero_hits_by_layer.json`、`W1…`。

**子项②「两表没有任何可连接键」——成立（并且是缺规则）**

- `docs/TRACEABILITY.csv` 无 `module_id` 列；矩阵无 `requirement_id` 列（逐列名核对）。
- `csv.module`（去重 10：`astro_image_io/calibration/coverage/healpix_drizzle/orchestrator/phase2/photometric_calib/plate_solve/…`）∩（矩阵 `module_id` ∪ `module_anchor`）= **0**；加 `MOD-` 前缀后 = **0**；去 `MOD-astrocs-` 前缀后 = **0**。
- `csv.algorithm_id`（4 值：`ALG-ACR-EQUIV-001`、`ALG-INT-001`、`ALG-REJ-001`、`ALG-STAR-PSF-001`）∩ 矩阵全部 id 列 = **0**。
- 我另数到成稿未提的一点：`csv.requirement_id` ∩ 矩阵 id 全域 = **8**，全部是 `SCI-*`（`SCI-AST-001/CAL-001/DRZ-001/INT-001/NOISE-001/PHOT-001/REJ-001/UPM-001`）——值层面偶然重合，但 spec 从未把它定为连接键，故"无**声明**键"成立。
- **缺规则的确证**：`TRACEABILITY_SPEC.md:94` 写「本矩阵是模块化事实源（**第 5 节给出两表关系**）」，而 §5 实为 `## 5. SOURCE SYMBOL 表达`（`:120-126`），§5 内出现 `TRACEABILITY.csv` **0 次**、出现「两表/关系/连接键/requirement_id」**0 次**；spec 全文出现 `requirement_id` **0 次**。⇒ 承诺了一节、规则不存在。
- 命令：`python -B 复算/db13-v/w2b_traceability.py`（末段 `§5 实际内容`）。

**子项③「自设 ID 正则与它自己的示例及矩阵取值互斥」——成立，且成漏了机制**

- `TRACEABILITY_SPEC.md:70`：`ID 一律 ASCII 大写，段分隔符取 -；空格、点、下划线之外的字符与空段都判红`（该句字面即自相反：照字面读，"…之外的字符"判红 ⇒ 大写反不判红）
- `TRACEABILITY_SPEC.md:78`：`MOD      ^MOD-[A-Z0-9]+(-[A-Z0-9]+)*$          例如 MOD-astrocs-phase1-calibration` —— 同行示例**不匹配**同行正则（13 个示例中唯一不匹配者）
- 矩阵取值：30 行 `module_id` 对 spec MOD 正则 **30/30 不匹配**；其余 7 层 id 列（SCI/ALG/DATA/API/ARCH/SRC/TEST/EVID）不匹配数 = **0**
- 机制（成稿未取）：检查器 `eng/tools/traceability/check_traceability_matrix.py:78` 用的是 `^MOD-[A-Za-z0-9]+(-[A-Za-z0-9]+)*$`（放宽 `a-z`），对 30/30 放行 ⇒ **规范与机器合同是两套互斥判据**，门不红纯属实现自行放宽。该差异不在 spec 任何一节里声明。
- 规则缺口：spec §3 的 8 条正则（`:73-:80`）**不含 SRC**，而 `:42` 要求 SRC 必填 `src_id`、`:84-85` 把「SRC 层 id（非占位）全矩阵唯一」列为机器 ERROR 强制域；检查器 `:79` 却有 SRC 正则 ⇒ 被审对象有判据、规范无规则。
- 计数面：spec §6 `:134`「`docs/modules/registry/astrocs.phase*.md` 声明的 **22 个** registry 生产模块」vs `docs/modules/registry/` 跟踪 27 件（26 个 `.md` 模块页 + `module_id_migration_baseline.json`）⇒ 自述计数与登记面差 4。
- 命令：`python -B 复算/db13-v/w2_traceability.py`（`== J3 ==`）、`git grep -n '^MOD-' -- eng/tools/traceability/check_traceability_matrix.py docs/traceability/TRACEABILITY_SPEC.md`、`git ls-files docs/modules/registry | wc -l` → 27

**子项④「必填层数两处不一致」——成立，且是"三处 vs 三处"**

- 8 的说法：`TRACEABILITY_SPEC.md:13`「八层追溯合同」、`:31`「展成 **8 个必填层**」、`:138`「每行 8 层全部显式」；`eng/contracts/schemas/traceability_matrix.schema.json` `title` = 「ACSD 模块**八层**追溯矩阵」、`description`「…八层全显式」
- 9 的说法：同文件 `:36-44` 状态表 **9 行**（SCI/ALG/DATA/API/ARCH/MOD/SRC/TEST/EVIDENCE，MOD 自占一行 `:41`）、`TRACEABILITY_LAYERS.csv:2-10` **9 层且 `required_for_module` 全为 `every_module`**、schema `properties.modules.items.required` = **24 列**（正合 9 层组）、矩阵每行 24 列
- 命令：`git grep -n "八层\|8 个必填层\|每行 8 层" -- docs/traceability eng/contracts/schemas/traceability_matrix.schema.json`；`python -B -c "import json;print(len(json.load(open('eng/contracts/schemas/traceability_matrix.schema.json',encoding='utf-8'))['properties']['modules']['items']['required']))"` → 24

**关键核（成稿要我辨的"缺规则 vs 缺值"）——主体是缺规则，缺值面只有 10 条**

- 缺规则（整改 = 补/改规则文本，不动数据）：①子项②（§5 空承诺 + 全文无 `requirement_id`）；②子项③（MOD 正则与示例/取值互斥、SRC 层无格式规则、规范与检查器两套正则）；③子项④（8 vs 9）；④子项①的**机制**本身——`spec:88-89` 把合同层的"真实唯一性"交给 `docs/contracts/INDEX.yaml`，而该文件头部自述生成方式=「自动化提取 **docs/science 与 docs/algorithms** 的 ID/状态」，对 DATA(38)/API(17)/TEST(9)/ARCH(1) 覆盖极薄，即被指定的反向索引按其自述口径根本不能承载这些层 ⇒ 指错索引；且**两个在册门都不做"矩阵 → 索引"这条反向连线**：`eng/tools/check_contract_graph.py` 只判 `INDEX.yaml` 自身（ID 唯一/path 存在/owner 非空/上下游双向/ACTIVE 不依赖 OBSOLETE），全文对 `traceability|matrix` **0 命中**；`eng/tools/traceability/check_traceability_matrix.py` 的反向判据 C8 走的是 `AUTHORITY_DIRS` 目录文本搜索，全文对 `INDEX.yaml` **0 命中**。⇒ 规范把"真实唯一性"交给一个在册门，但那个门不管这件事；反向连线在机器面不存在。命令：`grep -c -i "traceability\|matrix" eng/tools/check_contract_graph.py` → `0`；`git grep -n "INDEX.yaml" -- eng/tools/traceability/` → 无命中；`grep -n "\"id\": \"CONTRACT-GRAPH\"" -A12 eng/ci/checks.json` → `:753` 起，command = `python3 eng/tools/check_contract_graph.py`（在册、跑在 fast/linux-main/windows-main 三档）。
- 缺值（整改 = 补数据/补锚）：按检查器**自己**的反向判据 C8 忠实复刻（跟踪集 + `pathhint` + 同一扩展名表），VERIFIED 而"无 authority 文件可见"= **10** 条；`traceability_warn_baseline.json` 在册 **6** 条；**4 条超册未登记**：`EVID-BLD-003`(conformance-noop)、`EVID-P1-PSF-001`(phase1-star-psf)、`EVID-CPU-001`(providers-cpu)、`EVID-IO-001`(services-io)。另 `docs/TRACEABILITY.csv` 与矩阵无键属结构缺规则，不是坏值。命令：`python -B 复算/db13-v/w2c_c8.py`（`W2c_out.txt`）。
- 门红不红（只读实现判定）：C8 一律先记 WARN（`:506-508`），只有 `--strict` 才升级 ERROR（`:945-951`）；CI 在册命令 `TRACEABILITY-MATRIX` = `python3 eng/tools/traceability/check_traceability_matrix.py`，**无 `--strict`**（`eng/ci/checks.json` 内该步原文）；基线 6 条在我的复刻里全部匹配 ⇒ 不触发 `BASELINE_STALE`。⇒ 反向面今天是 **fail-open**（4 条超册静默容忍），"缺值"不会自己变红。
- 与成稿"举证 E"相关：`git ls-files | grep -c "^returns/"` → **0**，而 `TRACEABILITY_SPEC.md:109-110` 仍把 `returns/` 列为合法证据面（检查器 `:99` 注释已把 `evidence/ reports/ returns/` 收正为 `artifacts/evidence`）⇒ 又一处 spec 文本死锚。

**与成稿差异**

- 四个子项的方向与"缺规则 vs 缺值"的归类，我独立得出与成稿 §5 表「不是部分断链，是缺规则」**同向**；SPEC §5 空承诺、`returns/`、MOD 正则 30/30、8 vs 9 层、SRC 无格式规则、两表无键 —— 全部命中。
- 数字不可复算：成稿「非占位 ID 共 137 个 / 零命中 64 / 其中 VERIFIED 53」。我按两种自然口径都跑不到：去重 159（注册 id 集零命中 91 / 全文包含 86）、token 出现次数 204；VERIFIED 去重 140（零命中 75；合同层 46）。
- 证据误标：成稿 §1.4 举证 F 把 `ALG-P1-WR-001`、`ALG-P2-RES-001`、`ALG-P2-SMP-001`、`ALG-P3-005` 列为 VERIFIED 零命中例，实测这四个 id 只出现在 `algorithm_status = MISSING` 的单元格里 ⇒ 该例不成立（把 MISSING 计入 VERIFIED）。
- 成稿未取、我补的机制：检查器 MOD 正则已放宽小写（故 30/30 对门合法、对规范非法）；`INDEX.yaml` 与追溯矩阵之间**没有任何在册门做反向连线**（`CONTRACT-GRAPH` 与 `TRACEABILITY-MATRIX` 两门各自成立、互不相读，见上条命令）；C8 的 10/6/4 与"默认 WARN + CI 不带 `--strict`"⇒ 反向面 fail-open 的确证；SRC 层检查器有正则而规范无；§6「22 个 registry 生产模块」应为 26 页；`INDEX.yaml` 的 type 覆盖分布（解释零命中为何集中在 DATA/API/TEST）。
- 自我订正（本复核内的口径事故，留痕不删）：本节初稿曾写「`check_contract_graph.py` 在 `checks.json` 里只作为 `changed_paths` 出现、不是任何 command」。该判断**错**——它来自一次 `git grep` 只回一行就把行号读成 `changed_paths`。实测该文件在 `eng/ci/checks.json:753` 的在册步 `CONTRACT-GRAPH` 里就是 command，跑在 fast/linux-main/windows-main 三档。已按上条改为正确机制：**门在册、跑，但它与追溯矩阵互不相读**（跨文件连线不存在），结论方向不变、依据换掉。
- 成稿 §1.4「正向 10/10 行文件与符号锚解析成功」我只复核了它举的 `lib/algorithms/projection/p3_wcs.h::p3_wcs_make` 一例（命中），未独立重跑 10 行全量；正向不是本次争点，不影响定性。

**定级建议**：**P1**（追溯合同与其检查器是两套互斥判据；规范承诺的两表关系与反向权威均不存在 ⇒ 合同不可执行；反向面 fail-open 使真缺锚不再自曝）。其中"缺值"的 10 条与 4 条超册单列 **P2**；`returns/` 死锚、22 vs 26 计数 **P2**。

**缺什么证据**：只欠"实跑读数"一条——`python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 4 -- python3 eng/tools/traceability/check_traceability_matrix.py --root . --json-out run/audit06/trace.json` 与加 `--strict` 的一次对照，用来把我复刻的 10/6/4 变成门侧读数并确认 `errors=0 / warns=10`。本批按派单禁跑 `eng/**`，未执行，故该三数以"复刻值"登记。另：成稿的 137/64/53 需作者补出命令与口径，否则不采信。

<!-- PROGRESS: 2/4 -->

---

## W3

**W编号**：W3（成稿 §1.3 / §1.4 / P2 / §1.7 末段"同主题一致性"）

**判定**：确认（"档界在两处相互排斥"成立），并把范围与严重度**上修**：我方独立数到对侧共 **7 处**（成稿 2 处），且发现**两条在册单测对同一 profile 同一入口断言相反**（成稿未涉）

**我的独立取证（数＋命令）**

A 侧（正本，三档）逐字

- `docs/plugins/algorithms_phase2/12_rejection.md` §9（`:58-70`，标题「排异算法自动路由：按几何覆盖帧数 N 的档位表」）表体三行：`1 ≤ N ≤ 3 → none（不排异，直接逆方差加权积分）` / `4 ≤ N ≤ 5 → percentile clipping` / `N ≥ 6 → winsorized sigma clipping`；同节 `:70-75` 注：「`N ≥ 16` 档取 winsorized sigma clipping，即生产档 `astrocs_adaptive_pixel` 的档位表**由四档收成三档**」「对照档 `wbpp_2_9_1` / `astrocs_adaptive` **不随此改动**（仍为 `N>15 → linear fit`）」「`linear_fit` 仍是合法显式方法…只是**不再由 AUTO 在生产档产出**」
- 唯一源规则逐字：`ASTROCS_DESIGN.md:422`「按 N 自动选择排异算法，**逐像素档位表（档界与算法名）的唯一正本 = `docs/plugins/algorithms_phase2/12_rejection.md` §9**（**本节不复制档界与取值**；实现路由表须与该正本逐项一致）」
- 被署名的那一节到底写不写档界：**不写**。§5.5（`:417-427`）内无档位表，只有 `:422` 的指认与 `:423` 的算法名集合；`git grep -n "N ≥ 16\|N≥16" -- ASTROCS_DESIGN.md` → **0 命中**
- 同向其余：`docs/science/REJECTION.md:60-70`（§5 表体）与 `:275`；`docs/algorithms/PHASE2_REJECTION.md:210-213`；`docs/algorithms/REJECTION_ALGORITHMS.md:52-57`；`docs/design/PHASE2_DETAILED_DESIGN.md:56-60`；`docs/architecture/PIPELINE.md:50`、`DATA_FLOW.md:37`；`docs/development/CONFIG_SCHEMA.md:63`；`docs/validation/SCIENCE_FREEZE.md:18-19`

B 侧（作废档序被写成现行生产）逐字 + 计数 = 7 处

| # | 位置 | 原文关键片段 | 写成什么 |
|---|---|---|---|
| 1 | `docs/traceability/TRACEABILITY_MATRIX.json:396` | 「AUTO 路由按 **ASTROCS_DESIGN.md §5.5 五档表**(1≤N≤3 none / 4≤N≤5 percentile / 6≤N≤15 winsorized sigma / **N≥16 linear fit**)」 | 署名 §5.5；列 4 档却称"五档"；被引那一节不写档界 |
| 2 | `docs/traceability/TRACEABILITY_MATRIX.csv:17` | 同上（CSV 视图同行同字） | 双载体，改一处即漂移 |
| 3 | `docs/contracts/PUBLIC_API.md:1376-1378` | 「**生产默认 profile = `astrocs_adaptive_pixel`**（自研：n≤3 → NONE、4..7 → PERCENTILE、8..15 → WINSORIZED、**≥16 → LINEAR_FIT**）」 | 现行合同写着被 `rejection.cpp:1142` 明标"作废"的**原四档表**（比 #1 更旧一版） |
| 4 | `docs/modules/phase2_rej.md:46-47` | 「生产默认 profile astrocs_adaptive_pixel（自研）：n≤3→NONE、4..7→PERCENTILE、8..15→WINSORIZED、**≥16→LINEAR_FIT**」 | 同原四档表 |
| 5 | `docs/modules/registry/astrocs.phase2.reject.md:43-44` | 同句 | 同原四档表 |
| 6 | `docs/algorithms/PHASE2_REJECTION.md:90` | 「`astrocs_n_map_method` \| `:1148-1158` \| 生产档逐几何 N 路由（1≤N≤3 none / N<6 percentile / N≤15 winsorized / **else linear_fit**）」 | 同文件 `:210-213` 已是三档 ⇒ 文件内自相矛盾；行号锚 `:1148-1158` 与实际 `1154-1176` 不符 |
| 7 | `lib/algorithms/coverage/src/rejection.cpp:1129-1131` | 「最终映射表（逐像素按几何 N）：… 6 ≤ N ≤ 15…→ winsorized_sigma / **N ≥ 16 → linear_fit**」 | 同函数体 `:1163-1170` 已 `return P2_REJECT_WINSORIZED_SIGMA` ⇒ 决策点注释与决策点相互否证 |

标签级错（值已对、标题未改）另 1 处：`docs/design/PHASE2_DETAILED_DESIGN.md:54` 标题「### 5.1 逐像素排异路由（**最终五档表**）」下是三行表。
计数命令（读数 `复算/db13-v/W3_stale7.txt`，13 命中）：`git grep -n -o -e "≥16→LINEAR_FIT" -e "≥16 → LINEAR_FIT" -e "N ≥ 16 → linear_fit" -e "N≥16 linear" -e "else linear_fit" -e "五档表" -- docs lib`
读数归类（我逐条读过命中行本身）：13 命中 = 对侧 **7 处**（#1–#7 表；其中 csv/json 各含"五档表 + N≥16"两行命中、`rejection.cpp:1131` 一行）+ 标题级 1 处（`PHASE2_DETAILED_DESIGN.md:54`）+ 正确侧的历史提及 3 处（`rejection.cpp:1109` 讲 WBPP 分支差异、`PHASE2_REJECTION.md:569` 与 `:211` 均为"原 N≥16 档改投"的登记语）+ 同词不同主题 2 处（`STAR_DETECTION_ALGORITHMS.md` / `p1star_fixtures.hpp` 的"σ 五档"，与排异无关）。宽口径首版命令与读数另存 `W3_stale_places.txt`（13 行，含被上述窄口径剔掉的同词异物）。

现行代码实现走哪一档序（读实现，未跑）

`lib/algorithms/coverage/src/rejection.cpp:1154`（`astrocs_n_map_method` 首行实测）：`kPixelSmallNPolicy == kConservativeNone && 1≤n≤3 → P2_REJECT_NONE`（常量 `:1147-1148`）→ `n<6 → PERCENTILE` → `n≤15 → WINSORIZED_SIGMA` → **else → `WINSORIZED_SIGMA`**（M3 注释与"对照档未随此改动"偏离登记在 `:1163-1169`）。接线：`p2_reject_plan_resolve:1271-1286` 仅 `pixel_profile` 走该函数，非 pixel 分支保留 `n>15 → LINEAR_FIT`（合法例外）；`p2_reject_plan_resolve_n:1310-1321` 只覆写 `nominal_contributors` 后转调同一函数；`req->profile` 为空时缺省 = `P2_PROFILE_WBPP_2_9_1`（`:1214`），生产由 scheduler 侧显式给 `astrocs_adaptive_pixel`（`eng/contracts/data/phase2_uncertainty_rejection_provenance_v1.json:67`、`eng/tests/unit/p2002_unc_rej_prov_test.cpp:1423-1425`）。
⇒ 代码 = 三档，与唯一正本逐项一致；正本 = `12_rejection.md` §9；口径本身已由负责人 M3 裁决（2026-09-25）定案，不需再裁。

新发现（比文档打架更硬）：两条在册单测互斥

- `eng/tests/unit/p2_rejection_test.cpp:212` `CHECK(resolve_pixel_method(16) == P2_REJECT_WINSORIZED_SIGMA)`；`:216-218` 显式负例 `!= P2_REJECT_LINEAR_FIT`（16 与 20）—— 注释自述依据 = `run/REJECT-DOCFIX-01/REPORT.md §2/§4`
- `eng/tests/unit/p2002_unc_rej_prov_test.cpp:544-551` 同一 `astrocs_adaptive_pixel`、同一 AUTO 入口（`:416-425` helper → `p2_reject_plan_resolve_n` → `astrocs_n_map_method`）却断言 `{16u, P2_REJECT_LINEAR_FIT}`、`{64u, P2_REJECT_LINEAR_FIT}`；所在 `test_s302_kernel_semantics()` 在 `main:2001` **无条件调用**，`CHECK_MSG`（`:94-101`）失败即 `++failures`
- 时序：`git log -1 --date=iso -- eng/tests/unit/p2002_unc_rej_prov_test.cpp` → `a6f602fe 2026-09-24 23:49`；`git log -1 --date=iso -- lib/algorithms/coverage/src/rejection.cpp` → `139a2bc4 2026-09-25 10:11`「排异路由改投…」⇒ M3 改了路由与 `p2_rejection_test.cpp`，**漏改 `p2002_unc_rej_prov_test.cpp`**
- 两条断言同输入互斥 ⇒ 不可能同时通过。判哪条红须实跑（本批禁跑）⇒ 记 **待证**，命令：`python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 4 -- ctest --test-dir build -R "^(p2002_unc_rej_prov|p2_rejection)$" --output-on-failure`
- 歧义放大器（成稿未涉）：`docs/algorithms/PHASE2_REJECTION.md:634-636` F2 门描述「V15AutoPlanResolvesByNominal（n=2/5→PERCENTILE、6/15→WINSORIZED、**16/20→LINEAR_FIT**）」**未点名 profile**；实测该测试 `lib/algorithms/coverage/tests/synthetic_gate.cpp:4574` 用 `req.profile = "wbpp_2_9_1"` ⇒ 断言合法，但文档这行会被读成生产档序

**与成稿差异**

- 一致：三档 vs「N≥16 linear fit」的对立、署名出处 §5.5、该节"不复制档界与取值"、唯一正本 `12_rejection.md` §9、`SCIENCE_FREEZE.md:18-19` 属正确侧、CSV/JSON 双视图同字 —— 全部独立命中。
- 锚号差：成稿 P2 写「`ASTROCS_DESIGN.md:420` 明文'本节不复制档界与取值'」，实测在 **`:422`**（`:420` 是「路由依据 N = …」那条）。在一条以"引用出处是否存在"为争点的条目上自带 2 行锚漂，须订正。
- 对侧规模：成稿 2 处 vs 我 7 处，其中 4 处成稿未列且更严重——现行合同 `PUBLIC_API.md`（还写着更旧的原四档表）、两份模块页、算法文档同文件自我否定、以及 `rejection.cpp` 决策点注释与代码互否。
- 成稿称 B 侧"五档"却未指出它自列 4 档（引文与标签不闭合）。
- 成稿完全未涉两条单测互斥与 M3 漏改测试文件 —— 这是本条唯一的机器面后果。
- 关于"另有独立一路在同一对象上打出第二次命中"：我未与它对齐，上述 7 处、锚 `:422`、函数首行 `:1154` 全为自数。

**定级建议**：**P1**；若 ctest 实跑确认 `p2002_unc_rej_prov` 红，则升 **P0**（红门压在冻结科学路由上）。口径不需裁决。

**缺什么证据**：仅"门侧读数"一项（上面 ctest 命令，两目标同跑）；其余（正本归属、七处对侧逐字、代码档序、提交时序）已全部静态定案。

---

## W4

**W编号**：W4（成稿 P1 / §8.1）

**判定**：确认（"术语表条目错"成立），并给出可结案的正确表述与唯一源 ⇒ **该条不必再上呈**

**我的独立取证（数＋命令）**

该锚在该文件里到底说什么

`docs/GLOSSARY.md:15` 整行：`| frame_quality_weight | 帧质量权重=support×snr_v²（SCI-CW 域专用，非生产；…）；生产=逐样本 ivar；`**`snr=1.0 一律按 unknown 显式标注`**` | 无量纲 | - | docs/science/CONTROL_WEIGHT_SNR.md#42 |`
锚所指实文（`sed -n '42p' docs/science/CONTROL_WEIGHT_SNR.md`）：`| \`quality_weight\` | \`frame_quality_scalar × local_quality_proxy/median\`，无量纲相对质量权重（S4 重定义的规范名；\`snr_v\` 为其别名） | 本文件 §4 |`
⇒ 该行在 §2 符号表（`:36-49`）内，只定义符号身份与别名，**不含任何 snr=1.0 / unknown / 缺失回退语义**。"锚与说法无关"复算成立。

阈值正确的口径与唯一源（同一文件三处一致，逐字）

- §5「规则 / 显式行为」`:153`：「**snr=1.0 的语义面 = 合法测量值**：缺失走整帧 median 回退并计数 `local_snr_unavailable`（stage2.cpp:394-420）」
- §6「独立不变量」`:168`：「**无伪 unknown**：`snr=1.0` 不作为缺失标记（缺失→回退整帧 median 而非伪装 1.0）」
- §7「不可接受变化（部分）」`:175`：「**以 `snr=1.0` 替代缺失回退（伪装 unknown）**」← 列为不可接受变化
- §4 代码块内 `:134`：「`# 禁止 snr=1.0 伪装 unknown：缺失必须走回退并计数 local_snr_unavailable`」
⇒ 唯一源 = **`docs/science/CONTROL_WEIGHT_SNR.md` §5（`:153`）**，配套 §6 `:168` / §7 `:175` 为不可接受变化面。命令：`git grep -n "snr=1.0" -- docs lib`

三方旁证（成稿所引，我逐字复核全部命中）+ 我另取的两处机器锚

- `docs/algorithms/PHASE2_SAMPLER.md:398`：「半径内无星 → `snr_val=frame_snr_med_exact`（整帧精确中位）且 `snr_available=0`（**该档标记 = 0；1.0 只属可用档**，upm.h:51-53 冻结）」
- `docs/contracts/DATA_SEMANTICS.md:1762`：「`snr_available=0` 时整帧回退值（**不以 1.0 伪装**，upm.h:51-55）」
- `docs/contracts/PUBLIC_API.md:1562`：「`snr_available` | int 0/1（0=回退整帧中位，**禁以 1.0 伪装 unknown**，upm.h:51-55）」
- 实现锚 `lib/algorithms/coverage/include/astro/phase2/upm.h:51-54`：「1=该 control cell 邻域确有 catalogue 星点（snr 为真实局部中位数，**可为 1.0**）；0=无局部星点（… **禁止以 1.0 伪装 unknown**）」
- 测试锚 `lib/algorithms/coverage/tests/synthetic_gate.cpp:3938`：`EXPECT_NE(o.snr, 1.0) << "无局部星点不得以 snr=1.0 伪装";`
⇒ 六面同向（SCI 正本三条 + 两份现行合同 + 算法文档 + 实现头 + 测试），对侧只有术语表一行。

判"术语表条目错"是否成立 —— 成立

术语表是派生表、不作口径源；对手是 SCI 正本 + 两份现行合同 + 实现锚。按「层级向下·口径唯一」，低层与上位逐字相反即低层错；该条把"合法测量值"当缺失标记使用，正是 §7 `:175` 明列的不可接受变化本身。
正确表述（建议逐字替换该行第三段与其锚）：

```text
；`snr=1.0` 是**合法测量值**、不作缺失标记：无局部星点 ⇒ `snr_available=0` + 回退整帧精确中位并计数 `local_snr_unavailable`
| docs/science/CONTROL_WEIGHT_SNR.md#153 |
```

同行第二处错（成稿未涉）：该行把 `frame_quality_weight` 定义为 `support×snr_v²` 并注「SCI-CW 域专用，非生产」，而同一被引文件 §4 `:130` 逐字写「本文件的量只作采样与诊断，**不存在权重枚举、不存在 support×snr² 通道**」⇒ 术语表仍把被否定的通道当作"域专用"活定义。
门为何不能拦（只读实现）：`eng/ci/check_no_weight_mode.py` 的 R1 判裸键 `weight_mode`、R2 判中文「权重模式」且要求同行带 A44 留痕；`GLOSSARY.md:15` 两类 token 都不含 ⇒ 不在判据面内。在册 `GLOSSARY-DOCS`（`eng/ci/checks.json:8840-8858` → `eng/tools/check_glossary.py`）判术语表自身结构，不判与正本的语义一致。

**与成稿差异**

- 一致（全部独立命中）：错侧 = `docs/GLOSSARY.md:15`；正确侧 = SCI-CW `:153/:168/:175`；锚 `#42` 指向 `quality_weight` 符号行、与阈值说法无关 ⇒ "锚错 + 口径反"成立。
- 成稿未取、我补：`:153` 的**节号归属**（唯一源应写作 §5，不能只给行号）、§4 `:134` 代码块内同一禁令、**实现锚 `upm.h:51-54`** 与**测试锚 `synthetic_gate.cpp:3938`**（把"哪侧对"从文档多数票升级为代码+测试事实）、正确替换表述与改锚建议（`#42 → #153`）、同行 `support×snr_v²` 的第二处错、以及"门为何不能拦"的机制。
- 判断分歧：成稿把 P1 停在"未解决"冲突表内、§8.1 只说"定位到行"，未撤负责人先前登记的"两篇打架"。我判：**可用一手证据定案，不必再上呈**——一侧是 SCI 正本 §5/§6/§7 + 两份现行合同 + 算法文档 + 实现头文件 + 测试，另一侧是术语表一行，不是对等打架。

**定级建议**：**P1**（`docs/GLOSSARY.md` 是 CLI/配置/合同文档链的读入面；一行反向定义直接把实现者引导到正本明列的不可接受变化）。同批订正 `support×snr_v²` 半句可一并结掉待裁清单里"snr=1.0 两篇相反"一项。

**缺什么证据**：无。已用一手证据闭合（正本三条 + 两份现行合同 + 算法文档 + `upm.h` + `synthetic_gate.cpp`）。

---

## 进 P0/P1（只列"确认"）

| W | 判定 | 问题／谁修／怎么算修好 |
|---|---|---|
| W1 | 确认 | 被科学正本与两份现行合同判为"禁用 token、接受面 = 空"的 `psfsw_robust`，在验证档案层 6 件（在册 `ACTIVE_NORMATIVE`）与其机器源里仍列为**生产模式**；合同机器登记件自身三处相反（`weight_modes.production` 2 值 vs 同名 FROZEN 条款 3 值 vs `G-EPSF-PRESENT` 条件仍写"三生产模式"），且退役门扫描面只及 `eng/contracts/schemas/unified` ⇒ 拦不住。谁修：文档+登记面按正本跟改或出层，退役门扩面。怎么算修好：跟踪集内 `psfsw_robust` 不再出现在任何 production 分类；登记件三处同值；`FZ-MODE-RETIRED` 入册。 |
| W2 | 确认（头条数字降级） | 追溯合同的 ID 正则与被审对象、与其检查器互斥（30/30 行 `module_id` 对规范非法、对门合法）；规范承诺的"两表关系"一节内容不存在（全文 `requirement_id` 0 次）；"八层 vs 九层"同文档两处；被指定的反向权威 `INDEX.yaml` 承载不了它被委派的那几层，而且它与追溯矩阵之间没有任何在册门做反向连线（`CONTRACT-GRAPH`、`TRACEABILITY-MATRIX` 各自成立、互不相读）。定性＝**主体缺规则、缺值只 10 条**；反向面默认只 WARN、CI 不带 `--strict` ⇒ fail-open。谁修：改规范（正则/层数/SRC 规则/§5 关系/反向索引指认）+ 4 条超册入基线或补锚 + INDEX.yaml 连线进门。怎么算修好：规范正则 == 检查器正则 == 30/30 取值；`--strict` 跑通且基线条目数 == 实际 WARN 数。 |
| W3 | 确认（范围与严重度上修） | 排异档界对侧 **7 处**（成稿 2 处），含现行合同 `PUBLIC_API.md:1376-1378`、两份模块页、`PHASE2_REJECTION.md:90` 同文件自我否定、`rejection.cpp:1129-1131` 注释与代码互否。唯一正本 = `12_rejection.md` §9 三档（`ASTROCS_DESIGN.md:422` 点名并自证"本节不复制档界与取值"），现行代码走三档 ⇒ 口径不需裁决。机器后果：**`p2_rejection_test.cpp:212-218` 与 `p2002_unc_rej_prov_test.cpp:550-551` 对同一 profile 同一入口断言相反**（后者系 M3 提交 `139a2bc4` 漏改）。谁修：按正本跟改 7 处 + 修 1 处测试断言 + 订正成稿 `:420→:422` 锚。怎么算修好：`git grep` 对侧 0 命中；两条 ctest 同跑全绿。实跑确认前 **P1**，确认为红则 **P0**。 |
| W4 | 确认（可结案，不必上呈） | 术语表 `docs/GLOSSARY.md:15` 的「snr=1.0 一律按 unknown 显式标注」与正本 §5/§6/§7、两份现行合同、算法文档、实现头 `upm.h:51-54`、测试 `synthetic_gate.cpp:3938` 六面逐字相反（把合法测量值当缺失标记 = 正本明列的不可接受变化），其自证锚 `#42` 只定义符号别名；同行还把被否定的 `support×snr²` 通道当活定义。谁修：文档面一行改口径 + 改锚 `#153`。怎么算修好：全仓 `snr=1.0` 语义单向；负责人待裁清单"snr=1.0 两篇相反"一项可撤。 |

## 必须负责人裁的

**四条都不需要在科学口径上裁决**——每条我都用一手证据定案了。真正属于负责人的只有一个方向选择（非口径）：

W1 的整改路线：`docs/validation/v6/**` 6 件是"就地改成 2 模式"还是"降级出正式文档层并扩退役门扫描面"。我已穷尽三条取证路仍不能替他拍的这一条：① 正本与代码六面单向（口径无可裁）；② `docs/DOCUMENT_INDEX.yaml:21-22` 明文「活动设计档案…照实登记在 active 区段，**不当作归档**」＝当年是有意留在 active 层，直接"降级"会推翻一次既有登记判断；③ `docs/validation/v6/README.md:19-21` 又把该登记件指定为条款 id 的"现行机器登记表"，降级会切断该指针的读者面。三条互斥、需文档治理取向定夺。我的推荐：降级为主 + 登记件 `FZ-MODE-PRODUCTION.value` 与 `G-EPSF-PRESENT` 条件就地订正。

W3 的 P1/P0 边界不是裁决，是缺一次实跑：`python3 eng/tools/monitoring/mem_guard.py --max-rss-gb 4 -- ctest --test-dir build -R "^(p2002_unc_rej_prov|p2_rejection)$" --output-on-failure`。本批禁跑，我不预判红门，按"待证 + 门侧复跑"登记。

## 推翻或降级（留痕不删）

| 对象 | 处置 | 依据 |
|---|---|---|
| 成稿 §1.4 举证 F / §5 表「非占位 ID 137 / 零命中 64 / 其中 VERIFIED 53」 | **降级**（数字不采信，换复核口径） | 我按口径复算：去重非占位 159（对 `INDEX.yaml` 注册 id 集零命中 91；对全文包含 86）；VERIFIED 单元格去重 140（零命中 75；限 spec §3 自定合同层 111/46）；token 出现次数 204。三种口径都得不到 137/64/53。命令：`python -B 复算/db13-v/w2e_recon.py`、`w2_traceability.py` |
| 成稿举例 `ALG-P1-WR-001`、`ALG-P2-RES-001`、`ALG-P2-SMP-001`、`ALG-P3-005` 列为"VERIFIED 零命中" | **推翻**（这 4 例不成立） | 实测四个 id 只出现在 `algorithm_status = MISSING` 的单元格。命令：`git grep -n "ALG-P1-WR-001\|ALG-P2-RES-001\|ALG-P2-SMP-001\|ALG-P3-005" -- docs/traceability/TRACEABILITY_MATRIX.json` 后逐条核状态 |
| 成稿 §5 表「反向不可走（不是部分断链，是缺规则）」的**量** | **部分成立** | 方向我独立同向；但按检查器自身判据 C8，"VERIFIED 而无 authority 可见"只 **10** 条（6 在册 + 4 超册），非 53 条 ⇒ 缺值面小一个量级，缺规则面成稿未量化（我补了 5 类） |
| 成稿 P2 的 `ASTROCS_DESIGN.md:420` 锚 | **订正** | 「本节不复制档界与取值」实测在 **`:422`**；`:420` 是「路由依据 N = 该输出像素的几何可贡献帧数」。命令：`git grep -n "本节不复制档界与取值" -- ASTROCS_DESIGN.md` |
| 成稿 P3 的处置「需负责人裁决」 | **降级** | 现行生产口径六面单向、代码显式拒绝且有负例钉住；"打架"只存在于下层未跟改面。需裁的是整改路线，不是口径 |
| 成稿 §1.4「正向 10/10 行文件与符号锚解析成功」 | **未复核**（登记为成稿自述） | 我只抽查其举例 `lib/algorithms/projection/p3_wcs.h::p3_wcs_make`（命中），未独立重跑 10 行全量；正向非本批争点，不影响四项判定 |

---

## 收工工作树核对

```text
$ git -C "F:/Astro dev/Astro CS Normalization Database" status --porcelain
?? ACSD整治工作包_AUDIT-06.zip
?? site/
```

与开工输出**逐字相同**（两项均为开工前既有）。本批复核未污染被审对象：未写入仓库任何路径、未生成 `__pycache__`（全部脚本以 `python -B` + `PYTHONDONTWRITEBYTECODE=1` 运行，落在 `独立审计/复算件/db13-v/`）、未执行构建 / ctest / `run_checks.py` / 任何 `eng/**` 检查器（含 `--self-test`）、未 import 仓库 Python、零 git 写。

<!-- PROGRESS: 4/4（完成） -->
