# W2 · 科学量一致性与量纲（第二轮）复核档案

- 轴：W2（第二轮全仓静态审查）
- 时点：HEAD `a3a343a44080d917089e1f8d548ed2d0400b0c61`（本轮开始时 `git --no-optional-locks rev-parse HEAD` 实测）
- 纪律：纯静态（read/glob/grep + `python3 -B` 只读复算 + `git --no-optional-locks` 只读子命令）；未运行任何编译器/ctest/二进制/仓内脚本；判缺失一律走三级复核并贴命令与输出。
- 免重报核对（已逐条读）：`问题扫描/SUMMARY.md` 簇表（14 机制）、`问题扫描/INDEX.md` §三跨域主题唯一总述表、`V1-N-12`、`V2-N-03..07`、`V7 全族`（N-01..07）、`V10-N-04`、`V12 全族`、`V5-N-04`；并按关键词回查 `账本/FIX_LEDGER.jsonl` 682 条中命中「单位/量纲/ADU/3600/角秒/variance/ivar/归一/像素中心/CRPIX/CDELT/CROTA/像素尺度」的全部行（见 §附A）。
- 权威顺序：①负责人裁决/SCI 内「负责人授权」订正块 ②现行冻结文本 ③代码测试自洽。

## 复算脚本（本目录，纯 stdlib，可复跑）

| 脚本 | 用途 |
|---|---|
| _w2_r2_math.py | 角度/像素尺度/归一化/方差链路复算 |

## 一、条目

（逐条填写；每条含 两侧站点 / 冻结文本 / 可复算数值 / 静态或需运行期）

## 二、判否清单（本轮查过但不立条）

## 三、需运行期未判清单

## 附A · 免重报核对：账本关键词命中行（本轮逐条比对）
