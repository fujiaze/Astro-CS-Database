# V12 · 科学常数与阈值「单一权威点」普查

- 轴：V12（从 A-12 / M3-A-008 / V6-N-08c / A-39 外推）
- 执行者：V12 子代理
- 模式：**纯静态**（read / grep / glob / write / edit + `问题扫描/` 内 python3·node 只读脚本 + `git --no-optional-locks` 只读查询）；无编译器 / 无 cmake·ctest / 不跑测试或二进制 / 不跑 ci/run.py / 不跑无 --dry-run 的仓内脚本 / 不连 Fatduck / 不删改名。
- 纪律：每条结论附「命令 + 输出摘录」；裸数字带口径与时点；判「缺失」走三级复核；不能静态定的写「需运行期，未判」。
- 免重报：先读 `问题扫描/SUMMARY.md` 簇表与 `INDEX.md` §三；同义主题挂 related 不另立条。

## 0. 准绳与已知输入

（待填）

## 1. 常数权威表（普查主产物）

（待填）

## 2. 新缺陷条目（上限 15，宁窄而实）

（待填）

## 3. §建议：A-12 一类裁决的一次性收敛

（待填）

## 4. 未覆盖面与需运行期项

（待填）
