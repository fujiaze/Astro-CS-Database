# V12 · 科学常数与阈值「单一权威点」普查

- 轴：V12（从 A-12 / M3-A-008 / V6-N-08c / A-39 外推）
- 时点：2026-09-15 00:5x（HEAD 521095b8518a0db65c6c3cdeec10c26f719329f7，工作树 69 处 dirty；本轴全程只读，未写 findings/ 与 账本/）
- 模式：纯静态（read / grep / glob / write / edit + 问题扫描/ 内 python3 只读脚本 + git --no-optional-locks 只读查询）；无编译器、无 cmake/ctest（含 -N）、不跑测试或二进制、不跑 ci/run.py、不跑无 --dry-run 的仓内脚本、不连 Fatduck、不删改名。
- 取证口径：所有"宿主计数"以 git --no-optional-locks ls-files 的 4840 个被跟踪文件为唯一基数（缓存 问题扫描/_cache/v12_tracked.txt），排除 archive/、third_party/、docs/archive/；生产面 SCOPE = lib/ tests/ providers/ cli/ runtime/ modules/ include/ tools/ scripts/ ci/ docs/ contracts/ engineering/。
- 复算口径：python3 statistics.NormalDist 与 math，FP64；相对差按 (x-ref)/ref 计；时点同上。
- 纪律：每条结论附「命令 + 输出摘录」；判「缺失」走三级复核（L1 精确串 → L2 放宽同义 → L3 定点读疑似宿主）；不能静态定的写「需运行期，未判」。
- 免重报：先读 问题扫描/SUMMARY.md 簇表、INDEX.md §三、40_OWNER_DECISIONS.md、findings/** 与 _verify/V1..V11.md；与在册条目同义的部分只挂 related，不另立条（见每条 related 行）。

## 0. 准绳与前置声明

准绳顺序（负责人明令）：① 负责人设计意图（设计大纲/、memory.md、根 AGENTS.md 十要素、问题扫描/40_OWNER_DECISIONS.md 已裁条）② 现行冻结文本（改后）③ 代码/测试自洽。本轴不把"与旧文档不一致"当缺陷；但专查"把旧错误口径当权威改回去"（见 N-02 / N-08 / N-12 与 §3 S-5）。

权威源缺失说明（贯穿性根因，三级复核）：

    $ git --no-optional-locks ls-files | grep -icE 'constant|sci_const|numeric'   # 生产/合同层 0 命中
    $ git --no-optional-locks ls-files | grep -iE 'constant|sci_const|numeric'
    docs/standards/NUMERIC_STANDARD.md
    reports/REAUDIT_V3/scripts/constants_oracle.py        ← 仅审计脚本，非生产承载
    tests/testkit/examples/check_constant.py              ← 仅示例
    问题扫描/findings/H_NUMERIC/...                        ← 本组档案

即：仓内不存在任何集中承载科学常数的头文件 / 登记表 / 生成源（include/ 下 0 命中）。现存"具名点"只有模块私有常量（lib/phase1/noise/noise_model.h:15、lib/snr_estimator/cpp/src/noise_model.cpp:37、snr_science.cpp:38、cli/resource_gate.h:96-111、cli/memory_report.h:47-57），彼此不引用、跨语言（C++ 与 Python）无值比对。因此 §1 表中「权威点数 = 0」不是"漏改一个符号"，而是无处可指；§3 建议据此收敛为一条总闸（S-1），而不是逐常数立规则。

## 1. 常数权威表（普查主产物）

列义：「可复算数值差」= 相对差（无量纲，带符号），以最可信值（解析值或宪章冻结值）为基准；「钉」= 是否被测试正向钉死该常量的值（只钉文案/字符串不算）。

| # | 常数 | 语义（影响哪个科学量/门禁） | 全部宿主（文件::符号 + 位数） | 权威点数 | 文档口径 | 可复算数值差 | 出处/DISP | 钉 | 判 |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 1.482602218505602 | MAD→σ；σ_bg、σ_res、sky_sigma | 15 位：noise_model.h:15 kMadToSigma（唯一具名，消费者仅 noise_model.cpp:189 一处）；snr noise_model.cpp:61；sdet_api.cpp:227；sdet_image.cpp:495(f)/524；tests 侧 p1noise_oracle.hpp、p1star_mad_check.cpp:43-44；tracked 生产面 15 文件 24 处 | 1（私有头，跨模块不可见） | NOISE_MODEL.md:17/:46/:91/:135 同时含 11 位与 15 位；:91 自述"与 1.4826022185 差 <1e-12" | 复算 1/Φ⁻¹(3/4)=1.482602218505602（0 ulp）⇒ 15 位正确；文档 11 位 rel -3.778467e-12（绝对 5.602e-12，大于自述 1e-12） | A-12 待裁；M3-A-008；无 DISP | 部分（钉字符串与"两常数不等"） | 并入 N-01 |
| 2 | 1.4826 | 同上（4 位截断式） | tracked 生产面 66 文件 99 处：sdet_api.cpp:590、phase1/photometry/photometer.cpp:90、phase1/stars/star_detector.cpp:51、providers/cpu/baseline/src/baseline_provider.cpp:320/335、lib/backend_host/src/baseline_kernels_impl.inc:129/137、tools/quality/frame_qc_grid.py:35/224/500/592 | 0 | NOISE_MODEL.md:91 称"不可互换"却仍把 1.4826 留在 SCI 正文 | rel -1.496359e-06；FP32 1.4826f=1.4825999736785889 → rel -1.514e-06 | A-12 域 | 否 | N-01 / N-02 |
| 3 | 0.6745（倒数式） | σ_residual = MAD/0.6745（进公共交付字段） | star_matcher.cpp:21 _MAD_SCALE（:539/:612 用）；photometric_calib.h:41/:72/:96/:146 四处注释；p1phot_oracle.hpp:68；生产 17 处 / 文档 27 处 | 1（模块私有） | PHOTOMETRY.md §5/§14；NOISE_MODEL.md:135 称"与 SCI-PHOT 的 0.6745 同源" | 1/0.6745=1.4825796886582654 → rel -1.519615e-05（比 #2 大 10.2 倍）；Φ⁻¹(3/4)=0.6744897501960817 与 0.6745 rel +1.52e-05 | 无 DISP；A-12 未覆盖倒数式 | 否（oracle 与被测同式=共源） | N-03 |
| 4 | 0.7316727929211932 | trimmed-mean→σ；robust_residual_sigma、sigma_sky_adu | snr noise_model.cpp:37 kTrimMeanToSigma（:331 用）；snr_science.cpp:38 第二份（注释自述"noise_model.cpp:37 同源"）；snr_estimator.cpp:53/:74/:99 裸字面量 | 2 + 裸码 3 处 | PSF.md:23/:81/:88/:119=0.7316728（7 位）；STAR_PSF_ALGORITHMS.md:56-57 同一行内三套（0.7316728 / 16 位 / 0.731673） | 解析 0.731673095280613；16 位 rel -4.132439e-07；7 位 rel -4.035690e-07；16 位↔7 位 +9.674826e-09 | PSF.md:119 称"由高斯分位积分确定"，仓内无该积分记录；无 DISP | 否（noise_model_science_test.cpp:363 用同值） | N-04 |
| 5 | 1.230310 | Moffat4 FWHM/σ；σ=FWHM/系数，同时作 fwhm 兜底值 | snr_science.cpp:36 kMoffat4FwhmFactor；dpsf_psf.cpp:25 MOFFAT4_FWHM_FACTOR；snr_estimator.cpp:63/:97（兜底语义）；oracle/fixture 4 处；生产 19 处 / 文档 21 处 | 2（无共享头） | PSF.md §2；STAR_PSF_ALGORITHMS.md:52 F2 以等号写 =1.230310σ | 解析 2*sqrt(2*(2^0.25-1))=1.2303076525901024 → rel +1.907986e-06；FWHM=3px 时 σ 偏 -4.65e-06 px | SCI-PSF 冻结该 6 位值（M7-A-118 在册 P1） | 否 | 挂 M7-A-118；语义面入 N-05 附注 |
| 6 | 6.0 / 1.5 / 2.0 与 13.0 与 -4.0 | 曝光→初始极限星等 m_cut → 选星深度 | ipv_select.cpp:342-344（compute_initial_mag_cut）与 :408-409（内联第二份）；13.0 仅 :394 m0_hi；-4.0 仅 ipv_types.h:237 | 0（全裸字面量；13.0 无 params 字段） | docs 面 0 登记；ipv_select.h:97 与 :102 对同一夹取给两种口径 | 两函数对同一 (f,t) 差 4.000 mag（f=1877mm,t=300s：16.8642 对 12.8642） | 无 DISP；登记锚不存在（N-06） | 否 | N-05 / N-06 |
| 7 | 0.2885 / 3.0 / 4 / 0.1 / ±6.0 | 割线迭代收敛 → 选星星等 | 默认 ipv_types.h:228/:233/:240/:245/:246；兜底 ipv_select.cpp:395-400；±6.0 仅 :492 | 2（默认与兜底各写一遍） | 依据列 run/perf-fix/P4-magiter/REPORT.md（gitignored）；docs 0 登记 | 默认与兜底今日同值（0 ulp）⇒ 无当前差，改默认即分叉；±6.0 无任何承载 | 锚不存在 | 否 | N-07 |
| 8 | 200000（每文件触顶上限） | 截断检测（科学有偏防御）+ 查询缓存预算 | gaia_client.c:31 MAX_STARS_RESULT（.c 私有宏，12 处用）；ipv_types.h:244 独立字面量 200000.0；module_entry.c:546 手写 64LL*200000LL*24；gaia_client.c:128-129 宏派生；gaia_client.h:3 注释写 307,200,000 | 1 具名 + 3 复制 | GAIA_QUERY.md:147（依据列"源码 27, 1146, 1215"，现值 31/1482/1611）、:222；PUBLIC_API.md:93；模块 README.md:50/:144、memory.md:36 | 四处今日同值；307,200,000 B=293.05 MiB；分叉后果=检测静默失效（非当前数值差） | 无 DISP；V2-D7 已报 fmod 语义 | 否（stub 自造） | N-11 |
| 9 | 343 与波长网格 | 光谱网格 → f_syn | gaia_client.c:27 WL_COUNT 343（:28 派生 STAR_STRIDE_SP，12 处用）；fixture gaia_xpsd_fixture_gen.c:342 与 xpsd_spectrum_count_bounds_test.c:97-113 用 step=1；gaia_cat_test.c:282-283 钉 (1,336,1,343) | 1 | 真库口径唯一在册处 = DR3SP_SCHEMA_AUDIT.md:29（start=336, step=2, count=343 ⇒ λ∈[336,1020]）；GAIA_QUERY.md:131 只给公式不给值 | 生产 343 点跨 684 nm；fixture 跨 342 nm = 真库 1/2 | DISP 无（该审计文档不属 docs/ 权威层） | 是（钉的是与生产不同的合成值） | N-12 |
| 10 | 0.25（星等 shard 剪枝裕量） | 剪枝 → 是否漏星 | gaia_client.c:2054 唯一 | 1 | docs 0 行（GAIA_QUERY.md 无该行） | 不适用 | 权威文件在 run/（不版本化） | 否 | V5-N-04 已挂账，related |
| 11 | 1.2（bbox RA 裕量） | 赤道带剪枝 → 漏星 | gaia_client.c:923（唯一），守卫 :922 cos_dec<0.01 | 1 | 三处互相矛盾：gaia_client.h:9"保持无假阴性"；GAIA_QUERY.md:95-96"非数学证明"；:166"不漏星" | 复算存在漏星窗口（临界 ρ：dec0=50→37.0°、60→27.0°、70→17.5°、80→8.75°）；若赤道树限于 |dec|≤45 则 ρ≤20° 全域 0 反例 | 有登记行但无适用域数值 | 否 | N-10 |
| 12 | 0.85 / 0.60 / 10.0 / 2 / 2（§10.5 冻结门） | CPU 利用率发布门 | C++ resource_gate.h:96-111/:184-187；Python run_monitored.py:451-456；文本 宪章 §10.5 + §18.2 裁决 2、docs/owner/PIPELINE_OVERVIEW.md:133 | 2 套代码 + 1 文本（无生成关系） | 宪章原文只给：有效 CPU≥2、区间"超过"10s、平均≥85%、连续 10s<60%、单线程 FAIL | 数值同值；但 10s 边界开闭相反：C++ >=10.0 判、Python <=10.0 免判 ⇒ wall=10.000s 两侧结论可相反；宪章文本支持 Python 侧 | 宪章在册 | 是（各钉自实现） | N-08 |
| 13 | 90.0 与 0.70 | FAIL 级门禁判据 | resource_gate.h:97 与 :109（判定 :266/:342，文案 :436）；Python 侧无对应判据 | 1 + 1 | 宪章 §10.5/§18.2 无 90%、无 70%（grep -c '90%' 宪章 = 0）；docs/contracts/ci 0 命中 | 判据集合不同 ⇒ 同一 heavy run 两门可相反（p50=88%、达标占比 0.65、均值 86%：C++ FAIL，Python PASS） | 出处仅控制包 04_TASK_SPECIFICATIONS（不在 docs/） | 是（mon001_gate_test.cpp:85-92/158/171；test_p2007_joint_gate.py:259 裸 90.0） | N-08 |
| 14 | UtilizationP75Low / "utilization_p75_low" | 进 verdict JSON 的诊断码 | resource_gate.h:56/:82/:331/:342-343/:431-436；ci/ci_repair_round.py:110 已按该名归因 | 1 | 名=P75；实现="≥70% 样本 ≥0.85"（:109）⇒ 名值不符 | 无数值差，有语义差 | 无 | 是：mon001_gate_test.cpp:179-180 钉成合同字面量 | N-08(b) |
| 15 | 32（MB/s 与 MiB 三义） | 内存增长 FAIL 线 + 回收残差容差 | resource_gate.h:164 memory_growth_limit_mb_per_s=32.0（可覆盖，:233 用 >）；memory_report.h:48 kAllocGrowthUnboundedMbPerS=32.0（编译期，:370 用 >=）；memory_report.h:53 kAllocReclaimResidualTolBytes=32MiB | 3（同数字不同量纲/语义） | 宪章无 32 登记；A-42 已挂"0.5/32MiB 阈值无出处" | 边界可复算相反：RSS 斜率恰 32.0 不 FAIL；alloc 斜率恰 32.0 FAIL；覆盖后两条线差 2 倍，:423 与 :452 两处文案各印一个 | A-42 在册（不重报"无出处"） | 部分 | N-09 |
| 16 | 0.5 / 32MiB / 10.0 / 2.0 / 4（F-14 回收门） | 内存回收充分性 | memory_report.h:48-57 六条全部具名（本轴唯一"好样板"） | 1（集中） | 无 docs 登记（A-42 已挂） | 不适用 | A-42 在册 | 部分 | 不重报；作 S-2 模板 |
| 17 | 8（grid） | 控制网格 → 权重/UPM | upm.h 默认 8；硬门 upm.cpp:245（!=8 → rc 3）、upm.cpp:414、sampler.cpp:1449（→ rc 1）；配置域 stage2_common.cpp 允许 1..64；SCI PHASE2_UPM.md:43/:65 | 4 处字面量，0 具名 | SCI 在册 | 不适用（512 整除前置未编码） | V3-C1.6 已报 | 否 | related V3，不另立 |
| 18 | 511 / 512（manifest 缓冲） | 路径长度校验 | gaia_client.c:839 buf+511、:998 head+511（两处手写 512-1），缓冲 buf[512]/head[512] 在 4 个函数各复制 | 0 具名 | ACS_FIO_PATH_MAX=512 在册但这些点不用它 | 不适用（V5 已判 FIXED 残余） | V5-C1 在册 | 否 | related V5，不另立 |

表读法小结：18 行中「权威点数=0」6 行、「有 2 处以上独立复制」7 行、「文档与代码位数不同」5 行；「被测试正向钉死该常量的数值」只有 2 行，且其中 1 行（#9）钉的是与生产不同的合成值。⇒ 本轴结论不是"某常数写错"，而是常数无承载层。

## 2. 新缺陷条目（13 条；上限 15，宁窄而实）

### V12-N-01（P1 · 簇 10 同源断链 · 总述条）科学常数无承载层：唯一具名点跨模块不可见，同族三套位数并存
- 位置 见 §0；noise_model.h:15 kMadToSigma（全仓唯一具名 MAD 点，消费者仅 noise_model.cpp:189 一处）；15 位字面量仍散写 15 文件 24 处、4 位 66 文件 99 处、倒数式 17 处（口径：git ls-files 4840 文件，排除 archive/third_party/docs-archive，时点 HEAD 521095b）。
- 权威依据 宪章 §12.3-3（同一科学常数须单一权威值 + 可机器核对的定义点）、§4.1 量与单位分离、§5.3 单位闭合。
- 证据

      $ git --no-optional-locks ls-files | grep -iE 'constant|sci_const|numeric'   # 生产/合同层 0 命中（见 §0）
      $ git --no-optional-locks grep -c '1.482602218505602' -- 'lib/*' 'tests/*' 'providers/*' 'tools/*'
        → 15 文件 24 处；python3 复算 1/Φ⁻¹(3/4) = 1.482602218505602（0 ulp）⇒ 15 位是权威值，其余为截断/倒数近似

- 问题说明 具名点存在，但 24 个 15 位宿主里只有 1 个引用它 ⇒ "权威点"在结构上不承担权威；因此 A-12 / M3-A-008 / V6-N-08c 每一次"订正某文件位数"都不可能收敛，新代码继续抄字面量（V6-N-08c 的 frame_qc_grid.py 即同型事件，本轴复核其 :35/:224/:500/:592 仍是 4 位）。
- 影响 位数分叉可无限再生；任何"统一位数"的修复都只是局部修复。
- 建议处置 走 §3 S-1 总闸（一次性收敛 A-12 一类全部裁决）。本条为该总述的唯一立条；A-12 / M3-A-008 / V6-N-08c 归并为其实例（related），不再各自要求"改某文件"。
- 置信度 高 ｜ related A-12、M3-A-008、M7-G-103（缺四元表；本条补"缺代码承载点"这一不同事实）、V6-N-08c、findings/H_NUMERIC/p2/V2.md、M7-A-211

### V12-N-02（P2 · 簇 1 机器门）snr_constants 门只比"字面在场"、比对面不含任一 4 位/倒数式宿主，且把截断串固化为过门必要条件
- 位置 tools/docs_machine_consistency.py:139-144；被绕过面：sdet_api.cpp:227（15 位）与 :590（4 位）同文件同运算；star_matcher.cpp（倒数式）；providers/cpu/baseline（FP32 4 位）。
- 证据

      $ sed -n '139,144p' tools/docs_machine_consistency.py
              "1.4826022185" in snr_doc and "0.7316728" in psf_doc and
              "0.7316727929211932" in nm and
              "1.482602218505602" in nm,
      # 比对文件集只有 NOISE_MODEL.md / PSF.md / noise_model.cpp 三个
      $ python3 -c "print('0.7316728' in '0.7316727929211932')"   → False
      $ python3 -c "print('1.4826022185' in '1.482602218505602')" → True

- 问题说明 三点叠加：① 子串判据对 1.4826022185 恒真（M3-A-008 已记该点，本条不重报）；② 覆盖面不含任何分叉宿主 ⇒ "同文件两套位数"结构上不可能变红；③ 关键新事实：'0.7316728' 不是 16 位值的子串 ⇒ 若按 S-1 把 PSF.md 统一到全精度，门反而变红 ⇒ 该门把"文档必须保留 7 位截断串"固化为过门必要条件（反向钉死）。
- 影响 位数统一工作被一个永不失败、又反向锁死截断串的门阻塞。
- 建议处置 门改为比对常量的值集合（读代码具名常量的字面值，与 docs 表格逐值比对），删掉子串在场判据。本轴不判该门当前是否绿（该工具不在 ci/checks.json 126 项内，属 M6b-G-003，见 §4）。
- 置信度 高 ｜ related M3-A-008（子串恒真面）、M6b-G-003（工具未接线，不重报）、V4-N-16（钉文案家族）

### V12-N-03（P1 · 科学口径）冻结 SCI 文本声明 0.6745 与 1.482602218505602「同源」，复算相对差 -1.5196e-05，且该式产出跨模块交付量
- 位置 docs/science/NOISE_MODEL.md:135（"与 SCI-PHOT 的 0.6745 同源"）；宿主 lib/photometric_calib/cpp/src/star_matcher.cpp:21 _MAD_SCALE = 0.6745，:539/:612 产出 sigma_residual，进公共头 photometric_calib.h:41（"供 SNR 模块使用"，:72/:96/:146 三处重复同式）。
- 复算

      1/0.6745  = 1.4825796886582654 ; 对 1.482602218505602 rel -1.519615e-05
      Φ⁻¹(3/4)  = 0.6744897501960817 ; 与 0.6745 rel +1.519615e-05
      对照 4 位式 1.4826 rel -1.496359e-06 ⇒ 倒数式是三套中偏差最大的（10.2 倍）

- 问题说明 A-12 争的是 4 位截断是否违反 SCI-CAL §9 精度约束；本条报另一侧：SCI 文本用"同源"把 1.5e-5 的相对分歧写成同一件事，且分歧落在交付给 SNR 的 sigma_residual 上（不是内部诊断量）。M3-A-008 已记"三套字面并存 + 倒数式 1.53e-5 偏差"，但未判「:135 那句同源陈述本身为假」，也未提交付面（findings/H_NUMERIC/p2/V2.md 明确写"不判新错"）⇒ 本条只补这两点，不重报"三套并存"。
- 影响 按 :135 施工者会认为两条链等价；实测该等价性差 1.5e-5 相对。
- 建议处置 ① :135 的"同源"改为"同源但截断，截断引入 +1.52e-5 相对，登记 DISP"；② _MAD_SCALE 升全精度 0.6744897501960817，或由权威点提供倒数；③ 若负责人裁决"SCI-PHOT 侧容许 4 位"，须在 SCI 文本写明容许与由此引入的相对偏差（这正是 A-12 的可执行答案形态）。
- 置信度 高 ｜ related A-12、M3-A-008、M7-A-211、findings/H_NUMERIC/p2/V2.md (c)、M3-A-004

### V12-N-04（P2 · 反向钉死 + 伪精度）kTrimMeanToSigma 的 16 位值不等于它自称的解析值（第 7 位起分叉），且同族两份互不引用
- 位置 lib/snr_estimator/cpp/src/noise_model.cpp:37、snr_science.cpp:38（第二份，注释自述"noise_model.cpp:37 同源常数"）、snr_estimator.cpp:53/:74/:99（裸字面量 3 处）；文档 PSF.md:23/:81/:88/:119（7 位）、STAR_PSF_ALGORITHMS.md:56-57（同一行内三套位数）。
- 复算（闭式 + 蒙特卡洛双口径；脚本 scripts_v12_trim.py / scripts_v12_trim2.py）

      解析（对 |r| 的 10-90% 截尾均值, r~N(0,1)）
        = 2*(φ(q0.1) - φ(q0.9))/0.8 ,  q0.1=Φ⁻¹(0.55)=0.12566, q0.9=Φ⁻¹(0.95)=1.64485
        = 0.731673095280613
      MC(N=2e6, seed=12345, 对 |r| 去 10%/10%) = 0.7323830（差 +9.7e-4 ≈ 2σ 采样噪声 ⇒ 口径确认）
      在册 16 位 0.7316727929211932 → rel -4.132439e-07
      docs 7 位  0.7316728            → rel -4.035690e-07 ；16 位↔7 位 rel +9.674826e-09
      换算影响：sigma = residual_scale / c ；rs=1000 → 1366.731153153（16 位）对 1366.730588360（解析）
                 ⇒ σ_sky 系统性高估 +4.13e-07 相对
      另一自然口径（带号残差 ±1.2816σ 截尾后取 |r| 均值）= 0.5586098711723649 ⇒ 与在册值差 24%

- 问题说明 ① 16 位字面量暗示解析精确，实际第 7 位就与解析值分叉（与 #1 的 1.4826 族不同：那里 15 位值本身正确），属伪精度；② PSF.md:119 声明"数值由高斯分位积分确定"，但仓内无该积分的脚本或结果登记、无 DISP ⇒ 不可核对；③ 文档未声明截尾的分母域，而两种自然读法差 24% ⇒ 常数在文本上不可复算；④ 同族两份 kTrimMeanToSigma（:37/:38）互不引用，注释称"同源"却各写一遍。
- 影响 σ_sky / robust_residual_sigma 带 +4.1e-7 相对系统偏移（不足以翻转判定），但"16 位=权威"的宣称被否证 ⇒ 任何据 :119 复算的人第 7 位起得到不同数字。
- 建议处置 在 S-1 权威表登记该常数的口径（截尾对象 = |r| 的 10%/90% 分位）+ 推导脚本 + 值；代码归一到单一具名点；文档位数由权威表决定（7 位或全位二选一，不得同文三套）。
- 置信度 高 ｜ related _merge/M3.md:189（已建议归一但未判值错）、M3-D-003、V1-N-09（同型"抄来的常数"）

### V12-N-05（P1 · 科学公式双头 + 死码占合同位）曝光→初始极限星等公式两份字面实现，"唯一权威函数"零调用者却登记 VERIFIED
- 位置 lib/plate_solve/cpp/ipv/src/ipv_select.cpp:336-354 compute_initial_mag_cut（6.0/1.5/2.0，无 offset、无 13 上界）与 :408-409（同一式内联第二份 + m_lim_m0_offset）；:394 m0_hi = std::min(clamp_hi, 13.0)（13.0 无 params 字段）；声明 ipv_select.h:64-66；合同 docs/contracts/API_CONTRACTS.csv:369（末列 VERIFIED）；清单 docs/architecture/api_inventory.csv:374。
- 证据

      $ git --no-optional-locks grep -n 'compute_initial_mag_cut' -- 'lib/' 'tests/' 'cli/' 'runtime/' 'modules/' 'providers/'
        lib/plate_solve/cpp/ipv/src/ipv_select.cpp:333/:336   ← 定义
        lib/plate_solve/cpp/ipv/include/ipv_select.h:64        ← 声明
        （无任何调用点；仅 docs/contracts 与 api_inventory 两行登记）
      $ python3  f=1877.0, t=300.0 : a = 6+1.5*log10(f)+2*log10(t)
        a = 16.8642 ; a + m_lim_m0_offset(-4.0) = 12.8642   ⇒ 两路径对同一输入差 4.000 mag
      $ grep -c 'ADU/pixel/deg per header' docs/contracts/API_CONTRACTS.csv   → 422
        （该函数行的"单位"列即此占位串，而 m_cut 量纲是星等）

- 权威依据 宪章 §12.3-3、§10.4（禁硬编码；:372/:946 注释自称"参数全部来自 IPVSolverParams"）、§14.4 fail-fast、SCI-AST-001。
- 问题说明 ① 公共 API 的权威函数零调用者，生效的是内联复制 ⇒ 两式差 4 mag，只改函数版对生产无效；② 13.0 是该族唯一不进 params 的常数，与自述矛盾，且与 ipv_select.h:102"m 全程夹取 [clamp_lo, clamp_hi]"冲突：clamp_lo=15 时 min(max(m,15),13)=13 < clamp_lo（可复算的初值越界）；③ 合同行单位列填占位串、状态列 VERIFIED，而该函数测试面 0 命中。
- 影响 极限星等 = 选星深度 = 定标零点的科学上游；公式两份 ⇒ 五处同源中"代码/接口"分叉、"测试"缺失。
- 建议处置 ① 内联处改调 compute_initial_mag_cut（或删函数、把式子提为文件级具名常数）；② 13.0 进 params（m_lim_m0_clamp_hi）或删；③ 补一条两路径一致性测试；④ 合同行单位列改星等，状态按"零调用者"重判。
- 置信度 高 ｜ related M1a-L01-012（n_target 绕链同族）、V2-D2（同函数残余，本条不重报其 NaN 面）、M3-C-011

### V12-N-06（P1 · 依据虚构）6 处注释把 α=1.3→0.2885 的偏差登记指向不存在的 docs/algorithms/IPV_PIPELINE.md ⇒ 该科学口径变更实际无登记宿主
- 位置 lib/plate_solve/cpp/ipv/include/ipv_types.h:225；lib/plate_solve/cpp/ipv/src/ipv_select.cpp:364 / :946 / :1244 / :1514 / :1841。
- 三级复核（判「缺失」）

      L1  $ git --no-optional-locks grep -n 'docs/algorithms/IPV_PIPELINE.md' -- 'lib/*'   → 上述 6 命中
      L2  $ git --no-optional-locks ls-files | grep -i ipv_pipeline
           lib/plate_solve/IPV_PIPELINE.md                                               → 唯一，且不在 docs/
      L3  $ ls docs/algorithms/IPV_PIPELINE.md    → No such file or directory（exit 2）
          $ grep -c 'DISP' lib/plate_solve/IPV_PIPELINE.md  → 0
          该文件 :113-123 与 :327 仍以 ρ(G)=5×10^(1.3(G-10)) 作现行第 11 步描述（V2 §195 已记）

- 问题说明 注释句式是"（…已在 docs/algorithms/IPV_PIPELINE.md 走偏差登记，本次不改冻结科学文档）"——以"已登记"为不动冻结文档的理由；但登记锚不存在，真实同名文档还在全职描述退休公式 ⇒ 既没登记也没订正，口径变更无痕。按准绳①这是"设计意图已改、权威链无痕"，不是"与旧文档不一致"。
- 影响 α 先验 0.2885 决定割线迭代步长 → 选星星等；其依据唯一在 run/perf-fix/P4-magiter/REPORT.md（gitignored，V6-N-04 同型）。
- 建议处置 ① 在 docs/algorithms/ 建该件，或把 6 处锚改指真实路径；② 在 docs/standards/STANDARDS_REGISTRY.md 登记 DISP-IPV-MAGLIM（旧 α=1.3 退休、新值出处 = 10 帧实测中位 0.243–0.456）；③ 真实文件里的 α=1.3 段标 ARCHIVED 并指向现口径。
- 置信度 高 ｜ related V2 §195（同一文档旧公式，本条只补"锚不存在"）、V6-N-04（权威被搬 gitignored）、M7-G-105（引用不存在的依据）

### V12-N-07（P2 · 双写无锁）割线迭代 5 个兜底字面量与结构默认值一一同值但各写一遍；±6.0 步长夹全仓无承载
- 位置 兜底 ipv_select.cpp:395 safety=3.0 / :396 tol=0.1 / :397 max_q=4 / :398 zero_step=3.0 / :400 alpha=0.2885；默认 ipv_types.h:228 0.2885 / :233 3.0 / :240 3.0 / :245 4 / :246 0.1；:492 step = std::min(std::max(step, -6.0), 6.0)。
- 证据

      逐值比对（本轴 python 脚本）⇒ 今日五对默认↔兜底全部同值（0 ulp）
      $ grep -rnE 'm_lim_step_max' lib/plate_solve/cpp/ipv/include/   → 0（±6.0 无字段）；docs/ 0 登记
      $ grep -rn 'static_assert' lib/plate_solve/cpp/ipv/             → 0（无一致性锁）
      $ grep -n '可覆盖' lib/plate_solve/cpp/ipv/test/test_mag_iter.cpp → 只测参数可覆盖，不比对兜底==默认

- 问题说明 兜底路径生产可达（ipv_api.h 允许外部填 params，配置写 0/负即触发），两值相同纯属抄写巧合，无任何静态或测试锁。±6.0 是迭代步长上限（决定 4 次查询内能否从 m0≈12.9 走到目标星等），却无 params 字段、无文档、无 DISP。
- 影响 改默认 → 兜底静默留旧值；改兜底 → 与文档口径分叉；步长夹无出处属"科学量被硬编码"（§10.4 精神）。
- 建议处置 兜底改为引用 IPVSolverParams 默认构造的字段而非重写字面量；±6.0 进 params（m_lim_step_max）并登记。
- 置信度 高 ｜ related V2-D2 ③（NaN 落 13.0，不重报）、A-39/A-43 家族（判据与文案分叉）

### V12-N-08（P1 · 门禁判定）§10.5 冻结门在两个实现里判据集合不同、10s 边界开闭相反，且两个 FAIL 级阈值（90.0 / 0.70）不在冻结文本内
- 位置 C++ cli/resource_gate.h:96-97 / :108-111 / :184-187（kWorkerP50Min=2.0、kCpuP50MinPercent=90.0、kCpuMeanMinPercent=85.0、kMon001UtilSampleMinPercent=85.0、kMon001UtilSampleFrac=0.70、kMon001QueueUtilMinPercent=60.0、kMon002MinWindowSeconds=10.0）；Python tools/monitoring/run_monitored.py:451-456（5 个常量，无 p50 门、无样本占比门）、:515 interval <= FROZEN_GATE_MIN_INTERVAL_SECONDS → not_applicable。
- 权威依据（读宪章原文）

      §10.5：「有效 CPU 数不少于 2 且计算区间超过 10 秒时，计算区间平均 CPU 利用率不低于已分配容量的 85%；
              任何连续 10 秒低于 60% 或只有一个活跃计算线程均失败。」（§18.2 裁决 2 同文，并明废旧 D.6 的 80/75/50）
      $ grep -c '90%' ASTROCS_PROJECT_CONSTITUTION.md   → 0
      $ grep -rn '90\.0|0\.70|UtilSampleFrac' docs/contracts/ docs/owner/ docs/science/ ci/  → 0 命中

- 问题说明（三件均可静态判定）
  (a) 判据集合不同：p50≥90% 与 ≥70% 样本达标只在 C++ 侧 ⇒ 反例输入「均值 86%、p50=88%、达标样本占比 0.65、wall=12s」：C++ evaluate_gate 给 CpuP50Low / UtilizationP75Low FAIL，Python evaluate_frozen_gate 只看均值 → PASS。同一次 heavy run 两门结论相反，且 C++ 加严的两个阈值不在任何冻结文本内（实现自造门禁；§1.2 修改权在负责人）。
  (b) 名值不符：枚举名 UtilizationP75Low 与进 verdict JSON 的码 "utilization_p75_low"（:82）对应的是 ":109 kMon001UtilSampleFrac=0.70 ⇒ ≥70% 样本"判据；P75 应为 75%。该串被 tests/unit/mon001_gate_test.cpp:179-180 钉成合同字面量，并被 ci/ci_repair_round.py:110 用于失败归因。
  (c) 10.0s 边界开闭相反：宪章"超过 10 秒"= 严格大于；Python <=10.0 → 不适用（合文本），C++ :186-187 active_window_seconds >= 10.0 才判（把恰 10.000s 纳入判定域）⇒ wall=10.000s 时 Python 必 NOT_APPLICABLE、C++ 可 FAIL。
- 影响 发布门禁（§17.6）的 FAIL 级判据有一部分无权威出处，且两实现不等价 ⇒ "门会不会红"取决于走哪条路径，而非取决于宪章。
- 建议处置 ① 负责人裁 90.0/0.70 是否属 §10.5 的合法细化（否则删判据；若是则登记进 docs/owner/ 或 §18 并让 Python 侧同步）；② 枚举与诊断码改 utilization_sample_pass_frac_low（保留旧码须登记映射）；③ 两侧统一为 > 10.0（与宪章文本一致）；④ 加"判据集合逐条对齐"测试（S-4③）。
- 置信度 高 ｜ related A-39（allocated_capacity_cores 取义，不重报）、A-43（0.75/0.50 文案滞留——本条是另一组值 90/0.70）、M5a-G-002、_merge/00_COORDINATION.md:57-60（只记"90 存在"，未判无出处与两实现不等价）

### V12-N-09（P2 · 边界可复算相反）同一个「32」在两条内存增长门里量纲不同、比较号不同、可覆盖性不同
- 位置 resource_gate.h:164 memory_growth_limit_mb_per_s = 32.0（注释"调用方可覆盖"）→ :233 判据用 >；memory_report.h:48 kAllocGrowthUnboundedMbPerS = 32.0（编译期）→ resource_gate.h:370 判据用 >=；memory_report.h:53 kAllocReclaimResidualTolBytes = 32ull*1024*1024（字节，第三义）；文案 :423 印可覆盖字段、:452 印常量。
- 复算

      RSS 斜率 = 32.0 整   → :233 (32.0 > 32.0) 假  → 不判 MemoryGrowth
      alloc 斜率 = 32.0 整 → :370 (32.0 >= 32.0) 真 → AllocGrowthUnbounded(FAIL)   ⇒ 同值两侧相反
      调用方把 limit 覆盖成 16.0 → RSS 门线 16、alloc 门线仍 32（差 2 倍），
      而同一 verdict 的 :423 文案印 16、:452 文案印 32

- 权威依据 宪章 §10.5/§17.6 对"无界内存增长"未给任何数值（A-42 已就 0.5/32MiB 挂"阈值无出处"，本条不重报该面）；§12.3-3 单一权威值。
- 问题说明 三处 32 只有 kAllocGrowthUnboundedMbPerS 具名；边界结论相反；覆盖通道只作用于其中一条 ⇒ 判据面与配置面分叉，且诊断文案在同一份报告里给出两个"阈值"。
- 建议处置 ① 统一比较号并写明边界语义；② alloc 门也读 g.memory_growth_limit_mb_per_s（或两门合并为一条具名阈值表）；③ 32MiB 与 32.0 MB/s 用不同具名，避免同数字双义。
- 置信度 高 ｜ related A-42、簇 6

### V12-N-10（P2 · 常数安全域未声明）1.2 bbox 裕量在三处文本里的保证强度互相矛盾，其成立与否取决于未登记的树赤纬覆盖域
- 位置 lib/gaia_xpsd_client/src/gaia_client.c:914-925（唯一宿主；:922 cos_dec<0.01 守卫、:923 d_ra*cos_dec > radius_deg*1.2）；文本 gaia_client.h:9「裕量 1.2 保持无假阴性」、docs/algorithms/GAIA_QUERY.md:95-96「false_negative=0 是工程保守性结论…非数学证明」、:166「多查不少查，不漏星」。
- 复算（逐字复刻该谓词；脚本 scripts_v12_fneg.py / scripts_v12_band.py / scripts_v12_bbox12b.py）

      反例：q=(ra 0, dec 70°, ρ=20°)，叶 bbox ra 80..85 / dec 88..89
        d_ra=80 → 80·cos70 = 27.3616 > 1.2ρ = 24.0000 ⇒ 谓词返回 0（剪掉）
        叶内最近点 (80.000, 88.000) 与查询心角距 = 19.7471° < 20° ⇒ 该叶含锥内星 ⇒ 漏星
      第二反例：q=(0,70,20) 叶 ra 76..80 / dec 87..89 → 剪；叶内最近 19.4839° < 20°
      使 1.2 失效的最小 ρ：dec0=50→37.0°、60→27.0°、70→17.5°、80→8.75°（二分解 40 次）
      但设赤道带 Equirectangular 树严格限于 |dec|<=45 时：dec0=10..80、ρ 扫到 90-|dec0| → 0 反例
      对照 AE 分支裕量：1.2 / C45 = 1.2/1.1107207345395915 = 1.0804 ⇒ 两支裕量量级不同源

- 问题说明 头注释是无条件断言，ALG 同族文档写"非数学证明"⇒ 同一常数两处保证强度不同；而真正决定 1.2 是否足够的"赤道树赤纬覆盖域"既无文档数值也无代码断言（GAIA_QUERY.md:52 只写"赤道带 4 棵"；AE_CAP_BOUNDARY_DEG 45.0 只用于 AE 分支整树拒绝，不约束赤道节点）。且 :922 守卫只看查询中心的 cos（acos(0.01) ⇒ |dec₀|>89.427° 才免剪），不覆盖"中心不在极区但锥触极"（dec0=70、ρ≥20°）；AE 分支有对应守卫（:984 θ_q+ρ>90 → 保守不剪枝），赤道分支没有。
- 影响 可达性取决于 ρ=query_radius_deg（由 compute_fov_density 给出）是否 ≥17.5° 及赤道树是否触极；FOV≤20° 是 SCI-AST 口径但全仓无强制点（A-08 系）⇒ 本轴判"文本矛盾 + 守卫缺失 + 安全域未编码"，漏星是否实际发生标「需运行期 + 真实 XPSD 树布局，未判」。
- 建议处置 ① 赤道分支补同形守卫：dec0 + radius >= 90 → 保守返回相交（或按 d_ra_max = asin(sinρ / cos(dec0+ρ)) 判，ρ≥15° 退化为不剪枝）；② gaia_client.h:9 的绝对措辞改与 ALG 一致并写明适用域，登记进 GAIA_QUERY.md §2.7；③ 补一条球面网格属性测试（仿极冠 80270 例做法）。
- 置信度 高（几何/数值），可达性中 ｜ related V5-N-04（0.25 同型裕量无权威文件）、A-08（FOV 上限无强制点）、V6（GAIA 域）

### V12-N-11（P1 · 跨模块复制无锁）200000 触顶上限一具名（.c 私有）加三处独立复制；缓存预算乘积在两个 TU 各算一遍
- 位置 gaia_client.c:31 #define MAX_STARS_RESULT 200000（不在任何头，12 处引用）；ipv_types.h:244 m_lim_gaia_cap_per_file = 200000.0（:241 注释"该常量由 P1 持有, 本模块只读"，实为抄值）；module_entry.c:546 kQueryCacheCap = 64LL*200000LL*(sizeof(double)*3)；gaia_client.c:128-129 QUERY_CACHE_MAX_BYTES（宏派生）；gaia_client.h:3 注释"= 307,200,000 B（模块 plan 合同值 kQueryCacheCap）"。
- 复算与取证

      64 × 200000 × 24 = 307,200,000 B = 293.05 MiB（两条算式今日同值，0 ulp）
      若 MAX_STARS_RESULT→150000：QUERY_CACHE_MAX_BYTES 自动变 230,400,000，而 plan 合同值仍 307,200,000
        （高估 33%），且 plate_solve 的 fmod(N, 200000)==0 触顶检测永不成立
        ⇒ 被截断的 N 继续进 alpha 差分与达标判据（科学有偏，且静默）
      $ git --no-optional-locks grep -n 'm_lim_gaia_cap_per_file|MAX_STARS_RESULT' -- 'tests/*'
        → 仅 test_mag_iter.cpp:161 s.cap_value = 200000.0（stub 自造，不引用生产宏）；:166 钉 n_returned==200000
      $ git --no-optional-locks grep -n '307200000|QUERY_CACHE_MAX_BYTES|kQueryCacheCap' -- 'tests/*' 'docs/*' → 0
      $ sed -n '147p' docs/algorithms/GAIA_QUERY.md
        | 结果上限 | 单文件 collector 200000 星静默截断（MAX_STARS_RESULT） | 源码 27, 1146, 1215 |   ← 现值 31/1482/1611，三锚全漂

- 问题说明 触顶检测要求两处 200000 相等，而这个等式没有编译期、生成期或测试期任何承载，唯一具名点还在 .c 内跨模块不可见。（行锚漂移主题挂 M6b-E-002 总述，本条只记"该常数的依据列不可定位"这一具体后果。）
- 建议处置 ① MAX_STARS_RESULT（与 WL_COUNT）提到 gaia_client.h 或 S-1 常数头，plate_solve 侧引用符号而非字面量（若分层禁依赖，则由 plan/inspect 接口读实际上限）；② module_entry.c:546 改用 QUERY_CACHE_MAX_BYTES；③ 加 S-4① 的跨语言值比对门。
- 置信度 高 ｜ related V2-D7（fmod 判据语义，本条只判常量复制无锁）、M6b-E-002、M7-T-102

### V12-N-12（P2 · 正向钉死合成口径）波长网格 step 在测试面被钉成 1，真库口径 2 只存在于非 docs 层的一份审计文档
- 位置 生产 gaia_client.c:1224（atoi "spectrumStep="）→ :2620 出参；消费者 orchestrator.cpp:1485 out_wl[i] = start_nm + i*step_nm（同式注释 :1460）、module_entry.c:763/:820；测试面 gaia_xpsd_fixture_gen.c:342（spectrumStep=1）、xpsd_spectrum_count_bounds_test.c:97-113（全部 step=1）、gaia_cat_test.c:282-283 CHECK(rc==1 and s1==336 and s2==1 and s3==343)；真库口径唯一在册处 lib/photometric_calib/cpp/test/gate4_dr3sp_gaiaxpy/DR3SP_SCHEMA_AUDIT.md:29（start=336, step=2, count=343）与 :33（λ∈[336,1020]）。
- 权威依据 docs/algorithms/GAIA_QUERY.md:131 只写公式 λ_j = spectrum_start + j×spectrum_step，不登记数值三元组；lib/gaia_xpsd_client/README.md:48 同；无 DISP。
- 复算

      生产网格跨度 = 336 + 342×2 = 1020 nm（区间 684 nm）
      测试网格跨度 = 336 + 342×1 = 678 nm（区间 342 nm）= 真库的 1/2
      ⇒ 凡经 fixture/stub 验证的 λ 轴链路（get_spectrum_params → spectrum_wl → 积分/F_syn）覆盖波长域与真库不同；
        把 fixture 改成 step=2 会让 gaia_cat_test.c:282 立即变红（钉的是合成值，不是生产值）

- 问题说明 与"位数值不一致"不同型：这里是被测试正向钉死的网格常数与真实数据口径不同，而真库口径不在 docs 权威层 ⇒ 五处同源中"测试"与"科学/算法"脱钩且方向不可见（测试绿）。附：积分侧第三个步长 spectrum_integrator.cpp:247 const double wl_step = 1.0（注释称"与 python _DEFAULT_WL_STEP 一致"，该名在仓内 0 命中）；其 0.1nm 对 1.0nm 之争已由 M3-I-003（DISP-PHOT-002）在册，本条不重报，只记"三处 step 无共同承载"。
- 建议处置 ① GAIA_QUERY.md 增一行登记 DR3SP 波长网格三元组（336 / 2 / 343）+ 出处；② fixture 的 step 由生产值派生，或在 gaia_cat_test.c:282 注明"合成网格，非真库口径"并补一条真库 step 用例；③ wl_step 归入 S-1 权威表。
- 置信度 高 ｜ related M3-I-003（0.1 对 1.0 nm 已挂账）、簇 9（反向钉死）、M7-A-211 族

### V12-N-13（P3 · 口径与自述不符）n_target 密度自适应被夹进 [50,60] 的 20% 带宽，注释却写"统一为 60"，两常数无承载
- 位置 ipv_select.cpp:314-318：n_target_cap = 60; n_target = std::min(n_target_cap, std::max(50, std::lround(n_target_dbl)))；注释 :306-313（记录 150→300→150→60 的反复与"宽窄 FOV 统一为 60（与 img_n_target 一致）"）；对照 ipv_types.h img_n_target = 20、docs/algorithms/PLATESOLVE.md §5:115「默认 20，自适应 20→40→60」。
- 复算

      n_target_dbl < 50 → 50 ；> 60 → 60 ⇒ 实际取值域只有 {50..60}
      ⇒ 密度估计 rho_target 仅在 50..60 区间有意义（20% 带宽），其余被夹
      ⇒ 三套口径互斥：注释"统一为 60" / 实现 [50,60] / 文档"20→40→60"

- 问题说明 60 与 50 两处裸字面量无 params 字段、无文档数值、无 DISP。本条不重报 M1a-L01-012（那条讲默认 20 被硬编码 60 绕链，_merge/M1a.md §二 记该半边随并发提交消失），只记当前夹逼域与两套文本口径互不相符。
- 影响 选星池大小（注释自陈该值反复调整是因为宽 FOV 识别率退化，但全部调整无登记锚）。
- 置信度 高（算术），归因弱 ｜ related M1a-L01-012、簇 8（被算被记不参与执行）、V2 §132

（本轴止于 13 条；上限 15，留 2 名额不凑数。）

## 3. §建议：把 A-12 一类裁决收敛成单一规则

建议条款 S-1（总闸，替代逐常数裁决；措辞可直接进 docs/contracts/DATA_SEMANTICS.md 或 docs/standards/）：

    S-1 科学/门禁数值常数单一权威点（SINGLE NUMERIC AUTHORITY）
    1. 凡进入科学量计算或门禁判定的数值常数，必须且只能有一个可机器定位的定义点：
       docs/algorithms/NUMERIC_CONSTANTS.md 为登记面（值 / 单位 / 适用域 / 推导源 / DISP 号 / 退休口径），
       生产侧由一个常数头（建议 include/astrocs/sci_constants.h）承载具名 constexpr，
       二者由机器门逐值比对（逐值，不得用子串在场判据）。
    2. 一个数值一个名字：同一换算系数禁止以"倒数式"（x/0.6745）与"正数式"（x*1.4826）两种形式并存；
       需要倒数时由权威点定义 kInv = 1/k，或同时登记两值并写明舍入方向与引入的相对偏差。
    3. 位数即合同：登记位数 = 代码位数 = 文档位数。文档允许截断展示时必须显式写
       "截断展示，权威值见 X"并给出截断引入的相对偏差（复算值，非"≈"）。
       截断串不得被任何机器门当作过门必要条件（灭 N-02）。
    4. 量纲与适用域同行：登记单位（含 无量纲 / 星等 / MB·s⁻¹ / B / px / nm / deg）与适用域
       （分布假设、样本量下限、FOV/半径上限、边界开闭 > 与 >=）（灭 N-08(c)、N-09、N-10）。
    5. 退休即登记：口径被替换时（α 1.3→0.2885、1.4826→15 位、0.7316728→全精度等），
       退休文本必须标 ARCHIVED，替换必须落 DISP-，且登记锚必须是 git 跟踪的真实路径（禁 run/、禁不存在路径）
       （灭 N-06、V6-N-04 同族）。
    6. 本条吸收 A-12、M3-A-008、V6-N-08c、A-39 一类"某常数位数/取义是否违规"的裁决：
       此类争议一律按 S-1 判——无权威点 = 缺陷；有权威点则一切引用它。不再逐值立案。

配套（S-2…S-5 是 S-1 的机器化，不新增裁量）：

- S-2 权威点样板已存在，不新造风格：cli/memory_report.h:47-57（六条 inline constexpr + 注释给单位与判据）与 gaia_client.c:26-31（WL_COUNT 派生 STAR_STRIDE_SP、QUERY_CACHE_MAX_BYTES 由宏派生）是本仓可复制的正例；S-1 落地时以这两处为模板。
- S-3 跨语言禁手抄：tools/monitoring/run_monitored.py:451-456 与 cli/resource_gate.h:96-111 的 85%/60%/10.0/2/2 必须同源（Python 侧读同一登记表/生成头，或两侧都读 contracts/ 的数值清单）。当前 5 个常量在 Python 侧手抄、C++ 侧具名，无比对（N-08、N-11 同一根因）。
- S-4 三条机器门（建议作为 ci/checks.json 的真检查项，而非现在这个未接线的 docs_machine_consistency.py）：
  ① check_numeric_constants：代码具名常数集合 == NUMERIC_CONSTANTS.md 表格值集合（逐值，FP 比较写明 ulp），并拒绝同一语义出现多个位数字面量（族扫描 1.4826* / 0.6745 / 0.73167* / 1.23031*）；
  ② check_comment_anchor_exists：注释里出现的 docs/** 与 contracts/** 路径、DISP-*/§ 锚必须存在（直接灭 N-06，顺带覆盖 M6b-E-002 的行锚漂移面）；
  ③ check_gate_threshold_parity：门禁阈值在 C++ 与 Python 两侧逐条对齐，且比较号与边界开闭一致（灭 N-08(a)(c)、N-09）。
- S-5 反向钉死禁令：测试或门若钉住"字面串 / 文档措辞 / 诊断码名"，必须在同处注释给出该串的权威值来源；禁止把合成 fixture 值（如 N-12 的 step=1）钉成期望值而不标注。此条直接约束本轴 4 个实例（N-02 截断串、N-08(b) 诊断码串、N-12 网格、p1star_mad_check.cpp:84 的"两常数不相等"断言）。

一句话给前台的收敛建议：**A-12 不再单独裁"1.4826 是否违规"，而是裁"是否接受 S-1"**；接受后 A-12 / M3-A-008 / V6-N-08c / 本轴 N-01~N-04 全部由 S-1 + S-4① 自动判定，负责人无需再逐值裁决。

## 4. 未判与未覆盖面

需运行期 / 需负责人，本轴未判：

1. 各常数位数差是否越黄金基准或回归容差：需跑基准比对（禁跑），未判。N-03（1.52e-5）与 N-04（4.13e-7）是否越限，须由负责人按 §12.3 裁定。
2. snr_constants（及 docs_machine_consistency 全部 9 项）当前是否绿：需运行期。已复核该工具不在 ci/checks.json 的 126 项内（python3 json.load 实测，检查项 id 列表无 DOCS-CONSISTENCY/machine 类），属 M6b-G-003 挂账面，不重报。
3. N-10 的漏星在生产是否可达：需真实 XPSD 树的赤道带赤纬覆盖域 + query_radius_deg 实测分布；本轴只判"两处文本保证强度矛盾 + 赤道分支缺大半径回退守卫 + 安全域未编码"。
4. N-11 的 capped 实际触发频率：需真实查询（V2-D7 同判）。
5. N-08 的 90.0 / 0.70 是否属 §10.5 的合法细化：裁决权在负责人（§1.2）；本轴只记"冻结文本无此二值 + 两实现判据集合不同"。
6. DISP-MAG-001 / α=1.3 退休为何未建档：需查控制包流程史，未判。另注：40_OWNER_DECISIONS.md 内无 OWNER-06 / OWNER-10 编号条目（三级复核：git ls-files + grep docs|contracts|ci|tools + grep engineering|reports|evidence 均无 DISP-MAG / DISP-IPV / "偏差登记表"），按权威源缺失记，不判为"不一致"。
7. 1.4826f 在 provider/ABI kernel（baseline_provider.cpp:320/335、baseline_kernels_impl.inc:129/137）是否与模块级实现逐位一致：需真机跑 provider oracle 比对（禁跑），未判；本轴只判"同一语义 FP64 用 15 位、FP32 生产 kernel 用 4 位（rel -1.514e-6）"这一静态事实。

本轴明确不重报（挂 related）：0.85/0.60 口径与 RSS 回退（SUMMARY 簇表）、allocated_capacity_cores 取义（A-39）、resource_gate.h 文案滞留 0.75/0.50（A-43）、1.230310 与解析值第 6 位分歧（M7-A-118）、0.25 裕量权威文件不版本化（V5-N-04）、grid 8 的 rc=3 门（V3-C1.6）、511/512 契约缝（V5-C1）、fmod 触顶语义（V2-D7）、行锚系统性漂移（M6b-E-002）、0.1nm 对 1.0nm wl_step（M3-I-003 / DISP-PHOT-002）。

纪律自证：
- 未跑编译器 / cmake / ctest（含 -N）/ 任何测试或二进制 / ci/run.py / 任何无 --dry-run 的仓内脚本；本轴执行的非只读命令为 0。
- git 仅用 --no-optional-locks 的 ls-files / grep / rev-parse / status --porcelain（只读）。
- 产物全部落 问题扫描/_verify/（本档 + scripts_v12_*.py 共 22 个只读探针脚本）与 问题扫描/_cache/（v12_tracked.txt 4840 行、v12_mad.json、v12_scan2.json、v12_fam.json）；未动 findings/、账本/、_merge/；未新增根目录条目。
- 结构化文件一律用解释器解析（ci/checks.json 用 json.load 读出 126 项，非 grep 计数）。

本轴命令清单（可复跑）：

    git --no-optional-locks ls-files > 问题扫描/_cache/v12_tracked.txt                      # 4840 文件
    python3 问题扫描/_verify/scripts_v12_mad.py    / v12_scan2.py / v12_fam.py / v12_named.py # 位数族普查与宿主计数
    python3 问题扫描/_verify/scripts_v12_math.py   / v12_k.py / v12_d2.py / v12_d3.py / v12_sum.py
                                                                                              # 15/11/4 位与倒数式相对差
    python3 问题扫描/_verify/scripts_v12_trim.py   / v12_trim2.py / v12_g.py                 # 0.73167 族解析值 + MC
    python3 问题扫描/_verify/scripts_v12_ae.py                                                 # AE_C45=π/(2√2) 逐位核对
    python3 问题扫描/_verify/scripts_v12_bbox12.py / v12_bbox12b.py / v12_band.py            # 1.2 裕量失效临界半径
    python3 问题扫描/_verify/scripts_v12_fneg.py  / v12_fneg2.py                             # 复刻 bbox_intersects 的假阴性反例
    python3 问题扫描/_verify/scripts_v12_h.py / v12_i.py / v12_tf.py / v12_top.py / v12_cpu.py # 门常量宿主与测试钉死面
    python3 问题扫描/_verify/scripts_v12_dup.py / v12_dup2.py                                # 免重报交叉检索
    python3 问题扫描/_verify/scripts_v12_chk_ipv.py                                          # IPV_PIPELINE 锚三级复核
