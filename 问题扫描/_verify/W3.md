# 轴档案 W3 · 并发与资源生命周期（第二轮全仓静态审查）

- 轴名: W3（锁的粒度与顺序/原子性发布/共享累加器与缓存复用/线程预算唯一源(§10.4)/fd·mmap·句柄泄漏/RAII/析构顺序/Windows-Linux 文件占用差异/超时与取消路径/任务被 kill 后中间态清理）
- 时点: HEAD a3a343a44080d917089e1f8d548ed2d0400b0c61；本档案内所有行号/计数口径=该时点工作区真源，grep/find/git grep 复算（收编时建议转 path::symbol）
- 模式: 纯静态（read/glob/grep + 问题扫描/ 内只读 python3 -B 未用 + git --no-optional-locks 只读子命令：rev-parse/log/show/grep --no-optional-locks）；零构建、零 ctest、零二进制/脚本运行、零仓内 git 写、零删除改名、未连 Fatduck
- 权威顺序: ①负责人设计意图=docs/architecture/THREAD_BUDGET_ARCH.md（ARCH-THREAD-001）、THREADING_MODEL.md、IO_AND_ATOMICITY.md、docs/standards/CONCURRENCY_STANDARD.md ②现行冻结文本=宪章 §10.4/§10.5/§10.6/§11/§12.3-8、ALG-P3-003 §5c ③代码测试自洽

## 一、扫描面（本轴实际读过的文件/检索）
- 通读: lib/core/src/executor.cpp（全文）、scheduler.cpp（全文）、runtime.cpp（全文）、context.cpp（全文）、include/astrocs/core/scheduler.h/context.h（相关节）、cli/runtime_client.cpp（全文）、cli/cancel_token.h（全文）、cli/process.cpp（全文）、cli/commands.cpp:288-318、628-670、747-895、1262-1330、模块节选
- 定点读: lib/healpix_db/healpix_drizzle/drizzle_engine.cpp:60-190、1225-1299、1400-1590（grep 定位）、1725-2072（P22 池+待归约槽全循环逐行）、lib/core/src/module_adapters.cpp:140-220、440-520、2225-2444（含 2320-2390 共享观测注）、5100-5145、5280-5325、5700-5966（resample2 行带全闭包）、5969-6048、6230-6300（P3 execute 显式归还租约）、lib/phase3_session/p3_session.cpp:205-354、lib/phase2/src/upm.cpp:505-651、lib/phase2/include/astro/phase2/async_io.h（全文）、lib/astro_image_io/src/aio_cfitsio_mutex.h（全文）、aio_fits.cpp:505-560、hips/aio_hips_reader.cpp:90-210、hiss_writer.cpp:290-350、1060-1099、hiss_stream_writer.cpp:168-202、hiss_reader.cpp:460-528、1765-1789、aio_upm.cpp:70-113、aio_pipeline.cpp:78-95、870-929、lib/phase1_session/p1_session.cpp:200-320、providers/cpu/{baseline,avx2,avx512}/…provider.cpp 租约段、lib/calibration/src/{ac_api.cpp:128-134,module_entry.cpp:925-955}、lib/drizzle/src/module_entry.cpp:895-935、cli/monitor.h:244-320
- 合同/文档: ASTROCS_PROJECT_CONSTITUTION.md §10.3-10.6/§11、docs/architecture/{THREAD_BUDGET_ARCH,IO_AND_ATOMICITY}.md 全文、docs/standards/CONCURRENCY_STANDARD.md 全文、docs/algorithms/{PHASE3_RESAMPLE,PHASE3_RSMP_IMPL,INTEGRATION_ALGORITHMS,PHASE2_INTEGRATION,PHASE2_SESSION,NOISE_ESTIMATION}取消/线程节、docs/contracts/PUBLIC_API.md:129/179/413/449/555/620/687/849
- 全仓检索面: std::thread|pthread_create|mutex|lock|atomic|condition_variable|omp_set|hardware_concurrency|rename|MoveFile|unlink|remove|fopen|tmp|cancel|SIGKILL|dlopen|join|detach 等（lib/ runtime/ cli/ providers/ modules/ include/ tests/ tools/ scripts/ ci/ docs/）

## 二、排除清单
见本文件 git 历史版本首部（协议 §0.4 全量）：./run/ ./build/ ./out/ ./worktrees/ ./Testing/ ./logs/ ./artifacts/ ./reports/ ./工程控制/ ./GaiaDR3/ ./GaiaDR3SP/ ./BASS DR3/ ./AstroCS.wiki/ **/__pycache__/** 根目录 ./astrocs_run_*.json ./alloc_* ./resource_* ./worker_balance.csv ./CS/ ./Database/ ./graph/ ./astrocs_p1sess_*/ 与 ./问题扫描/ 自身。另按第一轮未覆盖声明与宪章跳过: ./lib/acr/**（§10.1 生产不可达）、./lib/healpix_db/archive/**、./lib/healpix_db/healpix_browser_qt/**、./third_party/** 与 vendored cfitsio 内部。git status 显示并发 agent 正改 lib/plate_solve/cpp/ipv/*、ci/checks.json 等——本轴未引用其未提交态。

## 三、三级复核台账（判「缺失/0 命中」逐条：命令 + 输出）
1. W3-R2-001/009（Windows 语义）：判「UCRT std::rename 不替换」的同仓逐字依据 = lib/io/src/io_adapter.cpp:125-127 注释 + lib/astro_image_io/src/aio_upm.cpp:102 注释自认「Windows rename 不覆盖已存在目标」。两文件同树互证，属现行文本口径，非外部知识。
2. W3-R2-002（set_cancel 零调用）：命令 A = `git --no-optional-locks grep -n 'astrocs_host_state_set_cancel' HEAD` → 恰 3 命中（host_services.cpp:128 定义、evidence/…/c07_link_map.map:6942 历史链接图、问题扫描/_cache/v20_defs.json 第一轮自产），**零调用点**；命令 B = `grep -rn 'astrocs_host_state_set_cancel' lib/ cli/ tests/ tools/ scripts/ providers/ modules/ include/ runtime/` → 仅定义行。两向复核一致。
3. W3-R2-003（登记面查重）：`grep -rn 'node_granted|peak_lease' 问题扫描/findings/ 问题扫描/_merge/ 问题扫描/_verify/` → 0 命中；peak_active 面命中=M5a-G-004（已在条内声明不重报）。
4. W3-R2-004（登记面复核）：`grep -rn '迁移整改点' docs/` → PUBLIC_API.md:687 命中 p1_session 锚注（据此由 P1 降 P2）；`grep -rn 'DISP-P1SESS' docs/ 问题扫描/findings/` → 0（p1_session 无独立 DISP ID）。
5. W3-R2-005/007/010 可达性与缺失判定：`grep -rn 'run_kernel' cli/ lib/ --include='*.cpp' | grep -v lib/acr` → 0；`grep -rn 'setsid|setpgid|CREATE_NEW_PROCESS_GROUP|CreateJobObject' lib/ cli/ runtime/ modules/ providers/` → 0；`grep -rn 'aio_frame_save_cache' lib/ cli/ modules/ runtime/ | grep -v aio_pipeline.cpp` → 仅 2 个 tests 命中。ci/run.py:691-696 正对照 = 直接读取。
6. W3-R2-006（monitor 零锁）：`grep -cE 'std::(atomic|mutex)|lock_guard|unique_lock' cli/monitor.h` → 0；`grep -nE 'lock_guard|unique_lock|std::mutex' cli/monitor.h | wc -l` → 0。
7. W3-R2-009（悬空合同锚）：`find . -name '00_COMMON_CONTRACTS*' -not -path './run/*' -not -path './build/*' -not -path './out/*'` → 空输出。
8. W3-R2-011（队列消费者缺失）：`grep -rln 'BoundedAsyncQueue' lib/ cli/ runtime/ modules/ providers/ | grep -v 'phase2/tests|async_io'` → 0。
9. W3-R2-012：scheduler.cpp 内 min_workers/max_workers 唯一读点 = :231-233（全文通读复核）；租约申请链 :5113-5116/:466-468/:5289+ 三处同源复核。

## 四、finding 汇总（9 条本轴新立 + 3 条已由前台收编）
- P1: W3-R2-001（已收编）、002（已收编）、003（已收编）、005、006（见 ../findings_r2/W3/p1/W3-b.md）
- P2: 004（自 P1 降档，登记面复核）、007、008、009、010、011、012（见 ../findings_r2/W3/p2/W3-b.md）
- 查重声明: 全部对照 SUMMARY.md 14 机制与 INDEX §三总述逐条挂 related；M5a-G-004/005/006、M5b、M3-I-002、M6b-E-002/004、DISP-*-004 族、B13-R13-3、B2-A18、B4-22、W4-R2-08、V21-N-15/N-17 无一条被本轴重复立总述。

## 五、需运行期、未判（未强行定档项）
1. provider 负预算漂移后的实际超额订阅幅度（W3-R2-005）——需并发压测/TSan 实跑（B-09 边界）。
2. 孤儿孙进程与后续 run 的资源冲突是否真实发生（W3-R2-007）——需 Windows/Linux 真机超时路径。
3. 非 ASCII 输出路径下 MoveFileExA 的两种致错分支在 Fatduck 上的实际表现（W3-R2-010）——Windows-only，静态只判口径矛盾。
4. p22 归约流水线（drizzle_engine.cpp:1744-1993）的锁纪律逐路径推演未见缺陷，但 cv 唤醒风暴/活锁类结论需 TSan 实跑——未报。
5. drizzle HissWriter finalize 失败后 stream 内部 tmp 是否残留（RAII 兜底未见显式 unlink 失败分支）——需运行期。
6. 行锚漂移类（W3-R2-004 的 :162-165、本档案全部行号）按 M6b-E-002 既有总述处理，收编时转 path::symbol。

## 六、现场冲突备注（不作为 finding）
- 并发 agent 工作树正改 lib/plate_solve/cpp/ipv/*、ci/checks.json、docs/contracts/PUBLIC_API.md（git status M）；本轴引用一律以 a3a343a4 时点行号，PUBLIC_API.md 行号若前台收编时已漂移按 symbol 锚为准。
- 本轴 findings_r2 首次落盘后被前台收编动作删除，已按新规（-b 文件名 + W3-R2 前缀）重建，未动 findings/ 与 账本/ 与 _merge/。
