# V2 · 测光 / PSF / 极限星等 三处性能与科学改动独立复核

- 复核层：V2（独立验证，纯静态；无编译、无测试、无 ctest、无 git 写操作）
- 开档时点 HEAD = dbaa2e18；复核中前台并发推进至 **bc8f3893**（35c85f53 science(P8) 已入库）。本档所有「工作树」结论以 **bc8f3893** 为准（时点：2026-09-14，V2 会话）
- 对象：23a7f665（perf P2 PSF FAST）、6d74046d（perf P4 极限星等割线迭代）、c3452d48（fix RQS/B4 测光/PSF 部分）
- 准绳顺序：①负责人设计意图（设计大纲/、memory.md、根 AGENTS.md 十要素、问题扫描/40_OWNER_DECISIONS.md）②现行（改后）冻结文本 ③代码与测试自洽。不把「与旧审计条目不一致」当缺陷；文档改动已获 §1.2 授权，不以「动了冻结文档」为理由。

## §0 范围与命令

| # | 命令（仓库根执行；git 一律 --no-optional-locks） | 用途 |
|---|---|---|
| K1 | git --no-optional-locks show --stat 23a7f665 / 6d74046d / c3452d48 | 三批改动面 |
| K2 | read lib/core/src/module_adapters.cpp:1563-1789 / 2300-2576 / 2630-2737 | FAST 节点链、P8-SNR 消费、precision_mode 门 |
| K3 | grep -rn "max_stars / psf_mode" lib include cli docs contracts runtime tools ci | 开关归属与可达性（三级复核见 C1） |
| K4 | read lib/plate_solve/cpp/ipv/{src/ipv_select.cpp:356-575, include/ipv_select.h, include/ipv_api.h} + lib/plate_solve/tools/diag_gaia_psf_projection.py | 割线迭代 + C ABI + Python 镜像 |
| K5 | python3 问题扫描/_verify/scripts_v2_secant.py（逐字复刻迭代算法） | 独立复算金标准与步进 |
| K6 | read lib/star_detector/src/{sdet_api.cpp:70-246,415-450,575-600, sdet_image.cpp:485-525} + docs/science/NOISE_MODEL.md:46/91/135 + docs/algorithms/STAR_DETECTION_ALGORITHMS.md:105-120 | 真 MAD 定义与同源 |
| K7 | read lib/photometric_calib/cpp/src/{star_matcher.cpp:380-660, pc_api.cpp:119-160,1040-1112} + docs/science/PHOTOMETRY.md:28-90 | 参考星门与失败码面 |
| K8 | read lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp:241-360 + test/test_extract_wcs_sip_failclosed.cpp + src/ipv_solver.cpp 调用点 | SIP 奇异 fail-closed |
| K9 | python3 问题扫描/_verify/scripts_v2_api_clean.py（离线复刻 CON-API-CONTRACTS） | 合同清单 ↔ 头文件一致性 |
| K10 | read lib/dynamic_psf/tests/p1psf/p1psf_prodpath_check.cpp + lib/photometric_calib/cpp/test/gate4_dr3sp_gaiaxpy/gate2_psf_oracle.py + evidence/gate2_result.json + ci/checks.json | 质心门是否被绕过、残余是否真实 |
| K11 | python3 解析 问题扫描/账本/FIX_LEDGER.csv（encoding=utf-8-sig） | 处置列与被修条目原文 |

结构性文件（CSV/JSON/长行 md）一律用解释器解析，未以行文本 read 判定「缺失/0 命中」。两处 0 命中按三级复核（整句 grep → 放宽关键词 → 定点 read）：m_lim 配置面（放宽为 m_lim → 命中仅 cli/memory_growth.h 的 mem_limit 与 DATA_SEMANTICS.md:769 锚点）、gate2/psf_oracle 在 ci/checks.json（放宽为 gate → 定点 walk 全部 121 项 id）。排除前缀写作 ./run/、./build/、./artifacts/。

## §1 逐条结论

### C1（T1）FAST 默认值与可达性 —— 机制成立，但口径有三处不实；交付数值现已会静默变化

默认值与分发（逐字，lib/core/src/module_adapters.cpp）：

    1761: int p1_psf_fit_limit(const Json& doc) {
    1762:   const Json psf = (p1_has(doc, "psf") && doc["psf"].is_object())
    1763:                        ? doc["psf"] : Json::object();
    1764:   return p1_int(psf, "max_stars", 5000);
    1765: }
    1772: constexpr bool kPrecisePsfEnabled = false;
    1785:   if constexpr (kPrecisePsfEnabled) { return p1_op_star_psf_precise(doc, man); }
    1788:   return p1_op_star_psf_impl(doc, man, p1_psf_fit_limit(doc));

- 默认 = 每帧只把最亮 **5000** 颗送进 Moffat4 拟合；注释 1760 明写「0 = 不截断（全量精确路径）」。
- 谁能改：仅 P1 节点配置 JSON 的 psf.max_stars。三级复核：① 整句 grep max_stars → 命中仅 lib/dynamic_psf/module.yaml:159、README.md:208/225、module_adapters.cpp:1764、lib/phase1_session/README.md:49，另有 platesolve 侧 sdet_params.maxStars 与 orchestrator 的 platesolve.max_stars（**同名不同物**，属 lib/orchestrator/configs/stage1.schema.json）；② 放宽为 psf 配置块 → cli/runtime_client.cpp:32-45 的 phase_config 用 pdoc = doc **整份直通**，无 CLI 旗标；③ 定点 read contracts/schemas（7 份 schema，无 P1 run-plan schema）与 runtime/pipeline/module_ports.registry.json（max_stars 0 命中）。
- ⇒ 开关可达但只能手改 run-config JSON；不在 CLI 面、不在任何 schema/校验面、无配置键登记点（见 D-3）。

**D-1（provenance 失真 / 字段分离，P1）**：psf_mode 是无条件字面量，不反映实际执行路径。

    1737:                      {"psf_mode", "fast"},
    1750:   (*man)["psf_mode"] = "fast";              // PSF-FAST-001
    1778:   return p1_op_star_psf_impl(doc, man, /*n_fit_limit=*/0);   // 精确路径共用同一实现

⇒ 两种情形下产物与 manifest 与真实路径不一致：(a) psf.max_stars=0 时生产入口跑**全量精确**拟合仍记 "fast"；(b) 直调钩子 p1_op_star_psf_precise_json（n_fit_limit=0，tests/unit/p1001_real_nodes_test.cpp:2199 一带）产出的 p1_psf.json 也记 "fast"。宪章 §4.3 要求 manifest 记录「实际执行路径」、§4.1 要求 provenance/validity 不混用。回归锁挡不住：该测试只断言 psf2.contains("psf_mode")，不校验值。与 M2a-H-1 同族但方向相反（那边「被计算被记录不参与执行」，这边「被记录不反映执行」）。

**D-2（FAST 裁决前提已被同批推翻 ⇒ 交付数值静默变化，P0/P1 需负责人裁）**

- 前提原文：module_adapters.cpp:1768-1769 与 lib/dynamic_psf/README.md §12「psf 输出端口为死边、psf_params 在仓库内零消费者」。
- 现况（HEAD=bc8f3893）：p1_op_noise 已以 psf_params 为逐源 SNR 目录 —— module_adapters.cpp:2505 起「for (const auto& p : src_frame->value("psf_params", Json::array()))」，lib/phase1/noise/README.md:50 逐字「**SNR 目录成员 = 上游 p1_sources.json 的 psf_params（PSF 有效星，p1_psf.json::n_valid 同源）∩ sources**」。
- 而 psf_params 行集在 FAST 下 = 最亮 5000 颗中的拟合成功星（module_adapters.cpp:1686-1704 经 fit_idx[k] 子集映射）。
- ⇒ 静态可推的后果链：一个性能旋钮 psf.max_stars 现直接决定 p1_snr.json 的 n_snr_input / median_snr / snr_phot / frame_depth_flux5_adu / frame_depth_m5_mag；真实帧 n_detected=145884（23a7f665 提交信息），目录被截到 ≤5000 且是**最亮**子集 ⇒ 帧级 SNR 与 5σ 深度系统性偏乐观。P2 的「p1_snr 逐位不变」验收测的是当时不含该消费的 SNR 实现；P8 的 p1snr 锁用手工 fixture，不覆盖 FAST 截断交互 ⇒ 无 parity 锁。

**D-3（同批两套开关严格度不一致，P2）**：precision_mode 一侧（dce8abd4/B2-A12）已按宪章收紧并**真接执行**（⇒ M2a-H-1 修复属实），逐字：

    2641:   if (!p1_has(dj, "precision_mode")) ... fail(... "no silent default")
    2645:   if (!dj.at("precision_mode").is_number_integer()) ... "boolean/float/string are rejected (no silent coercion)"
    2710:   if (precision_mode == 1) { ... blk_type = AIO_BLOCK_FLOAT64; }
    2730:   const AIO_BLOCK_TYPE want = (precision_mode == 1) ? FLOAT64 : FLOAT32;  // 不一致即 DATA 失败

而 psf.max_stars 走 p1_int（1117-1136）：boolean→1/0、float→静默截断、超 int32 范围→回退 5000、负数与 0 同义（不截断）。即 "psf":{"max_stars":true} ⇒ 只拟合 1 颗星；-1 ⇒ 全量精确。两开关作用于不同节点（drizzle vs star-psf），**不存在互相绕过**，但同批内的纪律标准不一致。建议：按 precision_mode 同款收（必须整数、≥0、越界即 DATA 失败），并让 psf_mode 由有效配置推导（n_fit_limit==0 ⇒ precise(full)）。

### C2（T2）精度门判定路径：FAST 与精确是否同一套 —— 未放宽任何容差；但 M3b-F-01 的残余比账本描述更大

- **无第二条判定路径**：逐星拟合门与质心门都直接调 dpsf_fit_batch_*（lib/dynamic_psf），FAST 只减少「被拟合的星数」，不改任何容差常量；未发现 if(fast) 形式的门分支。lib/dynamic_psf/README.md §12 亦明写「同批纯性能改动（公式与默认容差零改动）」。
- **真实变化在帧级统计口径**：p1_psf.json 的 median_fwhm_x_px / median_fwhm_y_px / median_ellipticity / n_valid 样本集由「全量检测的成功拟合星」变为「最亮 5000 颗里的成功星」（module_adapters.cpp:1663-1677 收集、:1731-1741 写出并注释自认口径变化）。新口径在科学上更对（旧口径把失败星混进中位数），但**跨帧/跨基线不可比**；与提交自述「906 帧基线须重跑」一致。
- **该口径变化未进任何接口层**：grep 现行 docs/contracts/DATA_SEMANTICS.md §15（DATA-P1-PSF，491-572 行）→ 无 psf_mode / n_fit_input / n_sources 三个新字段、无 median_* 口径变更条目（解析块内仅命中既有 out_n_valid 与行语义说明）；ci/checks.json grep median_fwhm|fwhm → 0 命中 ⇒ 没有任何门消费该口径。
- **M3b-F-01 的 PARTIAL 残余真实存在、且理由需更正**（任务书转述的「生产路径无质量位数据源」是 M3-C-002 的理由；账本 K11 解析原文：M3b-F-01 = 「实测生产路径 p95=1.1529px 仍超门 → PSF-001 未闭合（待 M3b-C-01 坐标契约裁决）」）。三点静态证据：
  1. 0.3px 门**未放宽**：新 ctest lib/dynamic_psf/tests/p1psf/p1psf_prodpath_check.cpp 硬常数 0.3；gate2 脚本门名由 centroid_p95_le_0.01px_converged 改为 centroid_p95_le_0.3px_production，并把 converged 版改名钉为「非验收」；evidence/gate2_result.json 两项均 false（生产 p95=1.1529px，收敛初值 p95=0.0228px）。⇒ 判定门未被顺手放宽，残余未被抹平。
  2. 但唯一测到「超门」的载体仍是**未登记脚本**：grep -rn "gate2|gate4|psf_oracle|dr3sp" ci/checks.json → 0 命中（三级复核：整句 → 放宽 gate → 定点 walk 全部 121 项 id）。⇒ 与原 finding「生产路径质心无门、脚本零登记」同型，本批只改了脚本里的门名与分类。
  3. 新登记的 CTEST-P1PSF-PRODPATH-CENTROID（waivable=false）**构型弱于其命名**：该测试自述「本测试场无噪声，σ 仅用于标定 A」+ 单星 + det=truth+0.5，即只测「初值偏 0.5px 时 LM 能否收敛回真值」（同文件 T3 已证明拟合器能拉回 0.25px 偏移），**不覆盖** gate2 记录的 sdet→dpsf 端到端坐标契约失败（M3b-C-01 0.5px 错位，账本仍 OPEN）。
- 内部张力（不强行定档）：该测试头注释宣称「T1 断言在 0.5px 级偏差下必红」，而 c3452d48 提交信息宣称「串行全量 ctest 301/301 passed…4 个新目标全 Passed」。二者不可能同时为真 ⇒ **需运行期，未判**（我未跑任何测试）。静态可判的部分：若 T1 绿，则新增 ctest 不构成对该残余的门禁覆盖（属假安心面）。
- 建议残余改写（供前台/账本采用）：「p1psf 新增 ctest 不覆盖端到端坐标契约；gate2 生产路径质心门仍未接任何 CI 检查项；M3b-C-01 裁决未落」。

### C3（T3）极限星等割线迭代数值健壮性 —— 主体正确、有界、金标准可独立复算；发现 1 个 P0 内存安全 + 多处同源缺口

逐字核心（lib/plate_solve/cpp/ipv/src/ipv_select.cpp::estimate_mag_lim_iterative，:374-510）：

    406:  const double f_safe = std::max(focal_length_mm, 1.0);
    407:  const double t_safe = std::max(exposure_s, 0.1);
    408:  double m = 6.0 + 1.5*std::log10(f_safe) + 2.0*std::log10(t_safe) + params.m_lim_m0_offset;
    410:  m = std::min(std::max(m, clamp_lo), m0_hi);            // m0_hi = min(clamp_hi, 13.0)
    426:  for (int it = 0; it < max_q; ++it) {                   // max_q = params.m_lim_max_iter>0 ?: 4
    428:      const int rc = query_func(m, n_ret);
    430:      if (rc != 0) { out.query_failed = true; ...; break; }   // 用末次成功结果（fail-open）
    447:      const bool capped = (n_ret > 0 && cap_unit > 0.0 &&
    448:                           std::fmod((double)n_ret, cap_unit) == 0.0);
    457:      const double rel = std::fabs(n_ret - N_target) / N_target;      // N_target = n_target*safety
    463:      if (n_ret == 0) { m = std::min(std::max(m + zero_step, clamp_lo), clamp_hi); continue; }
    467:      const double logN = std::log10((double)n_ret + 0.5);
    471:          const double a_new = (logN - logN_prev) / (m - m_prev);
    472:          if (a_new > params.m_lim_alpha_min && a_new < params.m_lim_alpha_max) { alpha = a_new; ... }
    477:      double step = (std::log10(N_target) - logN) / alpha;
    478:      if (!std::isfinite(step)) step = 0.0;
    479:      step = std::min(std::max(step, -6.0), 6.0);
    484:      m = std::min(std::max(m + step, clamp_lo), clamp_hi);

**我的独立复算**（问题扫描/_verify/scripts_v2_secant.py，逐字复刻同一迭代，输入 = test_mag_iter.cpp 测试 2 的同一 stub）：

    trace: [(0, 11.864439, 85), (1, 12.985085, 178)]
    result: m_final=12.9850849855, query_count=2, n_returned=178, converged=True, capped=False, alpha=0.2885

⇒ 与测试锚 12.9850849855 / 178 / 2 次**逐位一致**（时点：本会话）⇒ 金标准非拍脑袋值，代码↔测试自洽 ✓。逐项判定：

- **初值/端点来源**：12 个 m_lim_* 全部走 IPVSolverParams（ipv_types.h 与公共头 ipv_api.h:64-91 双份镜像；ipv_entry.cpp 的 to_solver_params 与 ipv_get_default_params 双向填充）✓。仅 6.0/1.5/2.0 曝光式、±6.0 步长夹、13.0 初值上界为函数内字面量 ⇒ **D-9**：与提交「参数全部走 IPVSolverParams…无硬编码」的自述不完全一致（宪章 §10.4 口径面属轻微，但自述失实要登记）。
- **符号变化 / 括号保证**：本实现**不是**教科书割线法（两点差分只用于估 alpha），步进符号由 sign(log10(N_target) − log10 N) 给出，alpha 默认路径下恒正（先验 >0 或 0.2885；更新受 alpha_min=1e-3 开区间约束）⇒ 方向自纠正、无需括号 ⇒ **不判为缺陷**（属设计选择，且比带括号法更省查询）。但：alpha 下界保护仅在「配置 alpha_min>0」时成立；若 run-config 把 m_lim_alpha_min 写成 ≤0，则 a_new=0 可被接受 → step=±inf → 被 :478 归 0 → 同一 m 空转满 4 次（不崩、不 NaN，但白耗 Gaia 查询）⇒ **D-10**：缺 alpha_min>0 的入参 fail-fast。负斜率（天区异常/截断所致）同理只能靠 (1e-3,100) 夹取拒掉，无独立告警。
- **最大迭代数与终止**：max_q 硬上界 4，循环内恰一次查询 ⇒ 必终止 ✓；test_mag_iter.cpp 用「恒返回 1 颗」stub 锁 query_count == m_lim_max_iter 且 !converged ✓（我的复刻在恒欠查情形亦停在 4 次）。
- **不收敛返回码 = fail-open**：调用侧 gaia_query_mag_iterative（:518-574）只在「无任一次成功查询或 FOV 内 <2 颗」时 return -1；否则**采用末次结果**并仅 logger->warn。且 :974-981 只把 m_lim_iterations / n_gaia_final / gaia_query_calls / gaia_query_ms / m_lim_capped / m_lim_alpha_final 写入 StarSelection，**mi.converged 与 mi.query_failed 被丢弃** ⇒ 下游与交付面无法区分「收敛达标」与「迭代 4 次仍差得远」= **D-4（可观测性缺口）**。capped 亦被记成 converged=true（:452 一带），语义上「截断」≠「达标」。
- **NaN/Inf 传播**：focal_length_mm / exposure_s 为 NaN 时 std::max(NaN,1.0)=NaN（无 isnan 守卫）→ m=NaN → :410 的双侧夹取因「与 NaN 比较恒假」而**静默返回 m0_hi=13.0**，既无 error 也无标记 ⇒ **D-5**（宪章 §14.4 fail-fast 面）。:478 的 isfinite 守卫只护 step，不护 m。
- **除零**：log10(n_ret+0.5) 用 +0.5 防 log10(0) ✓（但 :457 判据用未加 0.5 的 n_ret ⇒ 两套口径，**D-6**）；N_target = n_target*safety 若配 0 → rel 除零 → 该情形下 step 的 isfinite 守卫兜住，但无入参校验。
- **触顶检测是启发式且会漏（与本批动机直接冲突）**：capped 用「N 恰为 cap_unit(200000) 整数倍」判定，仅当**所有被查文件同时饱和**才成立；跨多文件锥形查询中若只有部分文件被 MAX_STARS_RESULT 截断（例 200000+3000=203000），既不判 capped，又把**按文件遍历序截断的 N** 喂进 alpha 差分与 rel 判据 ⇒ P4 声称修正的「FOV 供星缺陷/按文件序截断的科学有偏」在这条路上仍在 ⇒ **D-7**：需 Gaia 客户端回传「本查询是否截断」而非由星数猜。
- **新旧算法差异是否被测试固定：没有。** lib/plate_solve/cpp/ipv/test/test_mag_iter.cpp（target ipv_mag_iter，49 checks，已在 ci/checks.json 以 CTEST-IPV-MAG-ITER 非豁免登记）全部断言只针对新函数自身（收敛、α 更新、capped 防御、N=0 步长、查询次数上界、参数覆盖、错误路径）；旧一次性密度公式函数已被删除，无任何用例固定「新旧输出差」。真实差异证据在 run/perf-fix/P4-magiter/（run/* 全 gitignore，不入库）⇒ 新旧结果差属**需运行期，未判**；静态可判的是下列同源缺口。


### C4（T4）真 MAD —— sdet 侧定义修对了；「统一」只在单模块成立，且 ALG 伪代码仍钉着旧错误口径

改后实现（逐字，lib/star_detector/src/sdet_api.cpp）：

    441:  double median_res = sdet_median_of(pdata.residuals);
    444:  for (double r : pdata.residuals) deviations.push_back(std::fabs(r - median_res));
    445:  result->mad = sdet_median_of(deviations);
    226:  double rmse_ratio = fit.mad * 1.482602218505602 / fit.A;   if (rmse_ratio > 0.2) return SF_RMSE_TOO_LARGE;

- **与文档一致性**：mad 现在是 median(|res − median(res)|)（未换算 σ），系数单独在门里乘 1.482602218505602 ⇒ 与 docs/science/NOISE_MODEL.md:46/:91/:135 冻结的 1/Φ⁻¹(3/4)=1.482602218505602 语义与位数一致 ✓。
- **median 同源与样本下界**：sdet_median_of(:92-101) 我逐样例核算——奇数取第 m、偶数取 0.5*(v[m-1]+v[m])（第二次 nth_element 只重排 [0,m)，v[m] 不被破坏）⇒ 与 sdet_robust_median 同式 ✓；空输入返回 0 ✓；n<=p(=7) 直接 SDET_FIT_INVALID_PARAMS ⇒ 进统计的样本 n>=8 ✓。
- **门的实际松紧（我自己的算术，不引旧文档）**：旧门实际执行 mad*1.4826/A>0.2，而旧 mad 实为 RMSE ⇒ 等价 **RMSE/A > 0.2/1.4826 = 0.13490**；残差近高斯时 RMSE 是 σ 的一致估计。新门 1.482602218505602*MAD/A>0.2，1.4826·MAD 正是 σ 的稳健估计 ⇒ 等价 **σ_res/A > 0.2**。阈值比 0.2/0.13490 = **1.4826（放宽 +48.26%）**。⇒ 判定：这是**恢复文档名义门的量纲正确性**（旧代码把 σ 型量又换算了一次），不是改回错误；但必须同时登记两件事：① 星表成员门被放宽 48%，会改变 n_valid/星表可用性，本批无真实数据计数对照；② 名义门 0.2 在 SCI/ALG **至今无出处**（fix_note 自述「门限 0.2 未改（出处待登记）」）。另 MAD 在高度对称/量化残差下可为 0 ⇒ 门恒不触发（fail-open），RMSE 版无此退化 = **D-12**。
- **跨模块统一性：未统一（现测清单）**。15 位冻结值仅落 lib/star_detector/sdet_image.cpp:495,524 与 sdet_api.cpp:226；同一文件 **sdet_api.cpp:585 仍是 2.0 * 1.4826 * mad_lh**（同模块内并存），:455/:206 注释仍称「返回值已含 *1.4826 / 系数从 mad*3.0 改为 mad*1.4826」。生产 Phase1 链三处未动：lib/phase1/stars/star_detector.cpp:51（σ-clip 背景，**23a7f665 当日刚重写过该函数**）、lib/phase1/photometry/photometer.cpp:90（sky_sigma）、lib/calibration/{cosmetic_corrector.cpp:125,146, dark_optimizer.cpp:131,276, master_generator.cpp}。photometric_calib 侧仍用 _MAD_SCALE=0.6745（MAD/0.6745），lib/acr/qualification/profile_generator.cpp:396 反向用 σ*0.6745。我的复算：1/0.6745 = **1.4825796886582654**，对冻结 15 位值相对差 **1.5196e-5**；而 1.4826 对冻结值相对差仅 **1.4964e-6** ⇒ **未修的 photometric_calib 侧截断误差比被修掉的 sdet 旧值大 10 倍**。另 docs/science/NOISE_MODEL.md:135「与 SCI-PHOT 的 0.6745 同源」这句算术上不成立（0.6745 != 1/1.482602218505602 = 0.6744897501960817）。⇒ 判定：**不是新引入的错误**，但本批「统一到冻结值」的说法只在 sdet 一个模块内成立，A-12 与账本 M3-A-008（同一常数三套字面值、无单一权威定义点，仍 OPEN）未被消解。
- **回归锁自述与实现不符（D-13）**：lib/star_detector/tests/p1star/p1star_mad_check.cpp:21-22 宣称「本锁以生产常量来源唯一性覆盖（断言 4：生产源不含 4 位截断常数）」，但正文只有 3 个断言块（常数位数 / MAD != RMSE*1.4826 / 对称样本），**无断言 4**；若真有，上表 5 处会立即让它红。锁的覆盖面被高估。
- **反向钉死风险（本轮 §3(a) 最典型一条）**：docs/algorithms/STAR_DETECTION_ALGORITHMS.md:111 现行文本仍是「#   RMSE=mad·1.4826/A<=0.2、FWHM 上限」——把 RMSE 与 mad·1.4826 画等号（正是被修的错口径）且用 4 位常数；lib/star_detector/README.md:132 仍称「饱和星豁免 RMSE」。代码已改真 MAD、文档未随批订正 ⇒ 后来者按 ALG 伪代码复现会**把 RMSE 塞回 mad 字段**。该文件属 L2 算法规范、负责人已授权 §1.2 订正 ⇒ 判定为**同源缺口（应改）**，与「agent 不该动冻结文档」无关。

### C5（T5）参考星门 |r_consistent|>=3 → NO_DATA —— 是恢复冻结门的 fail-closed（方向正确），但只做了库层一半

- 实现（lib/photometric_calib/cpp/src/star_matcher.cpp）：

      513-536:  if (r_consistent.size() < 3) { ...fit_used = 0; scale_factor = 1.0; sigma_residual = 0.0; return {}; }
      613-625:  if (r_inliers.size() >= 2) { ... sigma_residual = MAD(r_inliers)*1.482...; } else sigma_residual = 0.0;

- **现行科学文本早已写明该门**（docs/science/PHOTOMETRY.md:38 逐字）：「参考星数 |r_consistent|>=3 才进 IRLS，否则 NO_DATA；|r_inliers|>=2 才估计 sigma_residual」；§8 异常表 :82「无星/星数不足 | 返回 NO_DATA」。⇒ 本批是**把实现漏掉的冻结门装回去**（旧代码仅判 r_consistent.empty()，1 颗星即可定标整帧），属新增 fail-closed、非放宽 ⇒ 符合负责人 SP-5.5「测光 fail-closed」意图（设计大纲/暂存/分册稿/AstroCS终态规格SPEC.md:75）。回归锁 lib/photometric_calib/tests/p1phot/p1phot_fixgates.cpp:114-140 用哨兵值（scale=123.0/sigma=456.0）断言「不写 scale」⇒ 判别力真，非恒真断言 ✓（该 target 已以 CTEST-P1PHOT-FIXGATES 非豁免登记）。
- **缺口（D-14）：产品面没有失败码。** ① 库层退化只表现为「返回空集 + diag.fit_used=0」，而 **pc_api.cpp:1112 仍 return 0（成功码）**，调用方无法从 rc 区分 NO_DATA；② 编排层 lib/orchestrator/cpp/src/orchestrator.cpp:2976 **无条件**写 photo_stats/STATUS="OK"（N_MATCHED=0 另记），即「星数不足、未定标」的帧在交付头里仍显示 OK；③ cli/core 侧无对应错误码：grep -rn "NO_DATA" lib/core cli include 无测光相关命中（NO_DATA 只存在于散文与 fit_used 数值里）。④ PHOTOMETRY.md:82 的证据锚「star_matcher.cpp:478 前校验」与 §4:38 的「(552-559)」在本批插入约 70 行后**全部漂移**（门实在 :513-536，sigma 实在 :613-625；:552 现已落在 IRLS 循环内）⇒ 与账本 M3-E-001（行级失真，本批记 FIXED）同一件事被本批重新制造 = D-16。
- 提示前台：本门与 M3-C-002 的「生产路径无 quality_flags 数据源」是**两条不同残余**，不得合并转述。我核到 pc_api.cpp 五个调用点均以 nullptr 传 quality_flags ⇒ M3-C-002 的 PARTIAL 理由属实。

### C6（T6）SIP 奇异失败码（c3452d48 的 M1a-C-002 部分）—— 真 fail-closed，判定与证据一致

- 改后（lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp:343-352）三条件齐全：

      if (!trans.valid || !std::isfinite(det_lin) || std::abs(det_lin) < 1e-15) {
          result->success = false;
          std::snprintf(result->error, sizeof(result->error), "WCS 拒绝: TRANS 线性项奇异/无效 (det_lin=%.3e < 1e-15), ...");
          if (logger) logger->error(result->error);
          return;
      }

  旧行为确为「仅 logger->warn 且函数尾 :648 无条件 result->success = true」⇒ 修复对症。补 isfinite 尤其正确：旧判据 |NaN|<1e-15 为假，NaN 会**静默放行**。
- 错误语义在 C ABI 可观测：IpvWcsResult.success + error_msg[256]（ipv_api.h:52,60）；调用侧以 success 决定是否写权威 inlier 缓存（ipv_solver.cpp:764,1146,1431 三处 if (result->success) cache_last_inliers_），非只落日志 ✓。
- 回归锁 test_extract_wcs_sip_failclosed.cpp 有非奇异对照断言（自述「判别力：断言 3 用同一函数同一装配路径证明正常路径仍 true」），且以 CTEST-IPV-EXTRACT-WCS-SIP-FAILCLOSED 非豁免登记 ✓。
- 小残余（D-15）：函数尾部仍留无条件 success=true，若日后新增分支绕回尾部会静默转成功；建议把「唯一置真点」收敛为显式成功出口。


## §2 新发现缺陷（V2 新发现，非旧账本条目；均未改 findings/ 与 账本/）

| id | 建议级别 | 一句话 | 证据锚 |
|---|---|---|---|
| **V2-D8** | **P0（内存安全 / ABI 漂移）** | 改公共 C 结构体 IpvParams 布局但仓内 Python ctypes 镜像未同步：ipv_get_default_params 以 sizeof(IpvParams)=424 memset/写进 352 字节对象 ⇒ **72 字节越界写** + 字段错位（density_tolerance 88→160、log_dir 96→168、m_lim_max_iter 80→152）；且 IpvParams 无 struct_size/abi_version（宪章 §8.6 明文要求） | include: lib/plate_solve/cpp/ipv/include/ipv_api.h:64-91；消费者: lib/plate_solve/tools/diag_gaia_psf_projection.py:61-80（:73 仍含 m_lim_step）+ :190-192；C 侧 lib/plate_solve/cpp/ipv/src/ipv_entry.cpp:333-335；尺寸由 python ctypes 复算（352 vs 424，Δ72）；该工具在 ci/tools/docs/tests/scripts 零引用 ⇒ 机器门不可见 |
| **V2-D2** | **P0/P1（需负责人裁）** | FAST 的裁决前提（psf_params 零消费者）已被同批 35c85f53 推翻 ⇒ psf.max_stars 现静默改变 p1_snr.json 的 median_snr / snr_phot / 5σ 深度，且无 parity 锁 | module_adapters.cpp:1768-1769 vs :2505；lib/phase1/noise/README.md:50；module_adapters.cpp:1686-1704 |
| **V2-D11** | P1（合同/清单同源） | 被删函数仍以 VERIFIED 留在 API_CONTRACTS.csv:371（另 docs/architecture/api_inventory.csv:376），新公共函数 estimate_mag_lim_iterative 未登记 ⇒ 干净检出口径离线复刻 CON-API-CONTRACTS 得**恰 1 条 API-MISSING-AST**，而该门 waivable=false 且在 fast/linux-main/windows-main 三 profile 均跑 ⇒ 静态判定该门必红 | docs/contracts/API_CONTRACTS.csv:371；问题扫描/_verify/scripts_v2_api_clean.py 输出（headers 60 / symbols 439 / rows 422 / missing 1）；tools/quality/contracts/check_api_contracts.py:91-97；ci/checks.json（CON-API-CONTRACTS 条目） |
| **V2-D1** | P1（provenance / 字段分离） | psf_mode 恒写字面量 "fast"，精确路径与 psf.max_stars=0 均被记成 fast；锁只查 contains() 不查值 | module_adapters.cpp:1737,1750,1778；tests/unit/p1001_real_nodes_test.cpp:2199 |
| **V2-D7** | P1（科学有偏残留） | 触顶检测用 fmod(N,200000)==0 启发式 ⇒ 部分文件截断时漏判，被文件遍历序截断的 N 仍进 alpha 差分与终止判据 | ipv_select.cpp:447-455,471-478 |
| **V2-D4** | P1（可观测性） | 未收敛/查询失败不落 StarSelection（mi.converged、mi.query_failed 被丢），capped 还记成 converged=true ⇒ 交付面分不清「达标」与「迭代耗尽仍欠查」；与 §4.3「manifest 记实际执行路径」同族 | ipv_select.cpp:499-506,518-574 vs :974-981 |
| **V2-D14** | P1（接口层缺失败码） | 参考星门 NO_DATA 无机器可判码：pc_api.cpp:1112 仍 return 0，orchestrator 无条件写 photo_stats/STATUS="OK"，cli/core 无对应错误码 | pc_api.cpp:1040-1112；orchestrator.cpp:2976；grep NO_DATA（lib/core/cli/include 无测光命中） |
| **V2-D3** | P2（配置纪律不一致） | psf.max_stars 静默强制（bool→1、float→截断、越界→回退 5000、负数=不截断）且无配置键/schema 登记；与同文件同批 precision_mode 的 fail-closed 严格度不一致 | module_adapters.cpp:1117-1136,1761-1765 vs 2641-2655,2710-2730 |
| **V2-D5** | P2（数值健壮） | focal/exposure 为 NaN 时静默落到 13.0，无 error/标记（isfinite 只护 step） | ipv_select.cpp:406-410 + scripts_v2_secant.py 复刻 |
| **V2-D9** | P2（自述失实/禁硬编码口径） | 「参数全部走 IPVSolverParams…无硬编码」不实：±6.0 步长夹、13.0 初值上界、6.0/1.5/2.0 曝光式系数仍是函数内字面量 | ipv_select.cpp:394,408,410,479 |
| **V2-D10** | P2 | 无 alpha_min>0 入参校验：配置面可致 alpha=0 → step 非有限 → 归 0 → 同一点空转满 4 次查询 | ipv_select.cpp:400,471-478 |
| **V2-D6** | P3 | 收敛判据用 n_ret、步进用 log10(n_ret+0.5)，两套口径未注明且不可互换（n 小时 rel 与 step 方向可反） | :457 vs :467 |
| **V2-D12** | P3 | 真 MAD 在高度对称/量化残差下可为 0 ⇒ 稳健门 fail-open（RMSE 版无此退化） | sdet_api.cpp:440-446,226 |
| **V2-D13** | P3（锁自述失实） | p1star_mad_check 头注释宣称「断言 4：生产源不含 4 位截断常数」，正文无该断言 ⇒ 覆盖面被高估 | p1star_mad_check.cpp:21-22 vs :74-113 |
| **V2-D15** | P3 | extract_wcs_sip 函数尾仍留无条件 success=true，新分支绕回即静默转成功 | ipv_wcs.cpp:345 vs :648 |
| **V2-D16** | P3（追溯锚漂移） | 本批在 PHOTOMETRY.md 引用的行锚（:478、552-559）随代码插入失效；DATA_SEMANTICS.md:769 的「ipv_select.cpp:429 m_lim」同样落在循环内（该处现是查询计数/失败分支） | docs/science/PHOTOMETRY.md:38,82；docs/contracts/DATA_SEMANTICS.md:769；ipv_select.cpp:425-445 实读 |

**同批自披露、不需我另判的待办（仅登记核对结果）**：23a7f665 的 R1（ulp 致 N7 负例翻绿、锚 B=100.061/A=484.7967 重 bake）、R2（906 帧基线不可复现须重跑）、R3（资源门禁反向激励）；6d74046d 的 α=1.3 文档订正待签。我核到：N7 锚与 dpsf_psf.cpp:240-243 的 t2*t2 改写一致 ✓；R2/R3 属运行面，静态无从复算。

## §3 反向钉死与五处同源缺口

### (a) 有没有把「旧的错误定义」当权威重新钉回去？—— 有 1 处实质风险 + 4 处文本残余

1. **docs/algorithms/STAR_DETECTION_ALGORITHMS.md:111** 仍写「RMSE=mad·1.4826/A<=0.2」：把 RMSE 与 mad·1.4826 画等号，**正是本次被修掉的错口径**；代码改了、该行未改 ⇒ 「读规范即得错误实现」的路径仍开着。**本轮最该立即订正的一行**（负责人已授权 §1.2）。
2. lib/star_detector/README.md:132 仍以「豁免 RMSE」描述该门（现豁免量已是 sigma_res）；sdet_api.cpp:206/:455 注释仍称 mad 已含 *1.4826 且系数「从 3.0 改为 1.4826」（三处注释彼此+与代码不一致）。
3. docs/contracts/DATA_SEMANTICS.md:201 仍以 sigma=1.4826·mad 表述 cosmetic 域（4 位常数未统一，且 mad=0 时阈值=±med 的 invalid 语义仍在）。
4. ipv_select.cpp:578 注释「保留作为兜底 (estimate_mag_lim_by_density 失败时)」指向**已删函数**；lib/plate_solve/IPV_PIPELINE.md:113-123,327 仍以 α=1.3 作现行步骤描述（该文件头自署「版本 V4.9 (2026-07-04)」，且未在 docs/DOCUMENT_INDEX.yaml 登记 ⇒ 属模块历史文档，但代码注释指向它形成规范错觉）。
5. 反向未发现：sdet/phase1-noise/snr_estimator 三侧都朝 NOISE_MODEL 冻结 15 位值收敛，没有把 FAST/inactive 钉成「新要求」，也没有把新算法反向写回旧审计条目 ⇒ 该项**未发现违规**。

### (b) 新定义是否五处同步（科学=算法=接口=代码=测试）

| 改动 | 科学 | 算法 | 接口/合同 | 代码 | 测试 | 缺口 |
|---|---|---|---|---|---|---|
| PSF FAST（23a7f665） | 未涉公式 | lib/dynamic_psf/README §12 + module.yaml:157-161 已登 ✓ | **缺**：DATA_SEMANTICS §15 未登记 psf_mode/n_fit_input/n_sources 与 median_* 口径变更；registry/module_ports.registry.json 无 psf 配置键；KNOWN_LIMITATIONS.md 无 FAST 条目（grep 0 命中） | ✓ | 有锁但只查 contains(psf_mode) | 接口/合同 + provenance（D-1/D-2/D-3） |
| 极限星等割线（6d74046d） | IPV_PIPELINE α=1.3 待签，proposed 补丁落 run/（gitignore ⇒ 仓库内不可定位） | 现行算法文档仍写旧公式 | **缺**：API_CONTRACTS/api_inventory 双向失配（D-11）；StarSelection 新 5 字段未进任何 DATA 条目；公共结构体无 abi 版本 | ✓（金标准我独立复算逐位一致） | ✓ ipv_mag_iter 非豁免登记 | 接口+清单+证据可定位性（D-8/D-11） |
| 真 MAD（c3452d48） | NOISE_MODEL:46/:135 ✓ | **STAR_DETECTION_ALGORITHMS.md:111 仍旧口径** | DATA-P1-STAR §17 未补 sdet mad/extras 真实定义（M3b-H-01 建议处置第⑤项未做） | sdet 内 15 位与 4 位并存（:585）；跨模块 5 处未统一 | ✓ 但宣称的断言 4 不存在（D-13） | 算法/接口/锁三处（+ p1phot_fixgates 注释自承「MAD oracle 复刻实现式」= 循环 oracle） |
| 参考星门 | PHOTOMETRY.md:38 ✓（先于本批） | PHOTOMETRIC_FIT.md 一致 | **缺**：无可判失败码（D-14）；文档行锚漂移（D-16） | ✓ | ✓ 哨兵断言判别力真 | 接口层 |
| SIP 奇异门 | ASTROMETRY/PLATESOLVE §一致 | ✓ | 复用既有 success+error_msg ✓ | ✓ | ✓ | 仅 D-15 提示 |

## §4 需运行期或未决（静态不可判，不强行定档）

1. p1psf_prodpath_centroid 的 T1 是否真绿（决定新增 ctest 是否等于生产质心门被覆盖）——测试自述「0.5px 级偏差下必红」vs 提交自述「4 个新目标全 Passed」。
2. FAST 截断对 p1_snr.json 的实际幅度（median_snr/m_5 偏移量）与 906/140 帧基线差 —— 需真实数据；我仅静态确立「耦合存在 + 无锁」。
3. 新旧极限星等选星结果差（欠查/过查比例、匹配星数分布、platesolve 成功率）：P4 的 A/B 证据在 run/（gitignore），仓库内无可复核件。
4. P2/P4 全部性能与位一致验收（302/302 ctest、p1_flux.json sha bc18d11f…、CLI user CPU 2102→258.75 s、maxrss 4.07→1.68 GB）—— 按纪律不跑构建/测试，**未判**。
5. MAD→σ 常数（1.482602218505602 / 1.4826 / 0.6745）最终归一属 A-12 与 M3-A-008 负责人裁决面；我只给算术与清单，不定档。
6. SF_RMSE_TOO_LARGE 放宽 48% 后真实帧拒绝计数变化：需重跑基线（提交自述 PLATESOLVE_ALL.csv 基线陈旧）。
7. CON-API-CONTRACTS 在真实 CI 是否红：我用同 glob/同正则/排除影子树的离线复刻静态判「干净检出必红」，未跑 CI（禁 ci/run.py 非 --plan-only）。

### 附：本档使用的只读脚本（均在 问题扫描/_verify/，未动仓库其他区域）
- scripts_v2_secant.py：逐字复刻 estimate_mag_lim_iterative，独立复算金标准（12.9850849855 / 2 次 / 178 颗，与测试逐位一致）与退化场景（NaN 输入、恒欠查）。
- scripts_v2_api_clean.py / scripts_v2_api_contracts_replica.py / scripts_v2_api_debug.py：离线复刻 check_api_contracts.py + extract_cpp_api.py（含「本机树被 ./run/ 影子头污染 → 假绿」的定位）。

