# V5 · gaia 客户端内存安全与"逐位等价"剪枝复核（独立验证层）

> 复核对象：`07eb229b fix(RQS/B1): 内存安全 P0 —— M9-H-1 gaia manifest 栈越界写、M9-H-2 XPSD 自报 spectrumCount 无界（堆越界读+OOM）`
> 与 `bd4bc23b perf(P1): gaia 客户端 —— 星等 shard 剪枝（逐位等价）+ 查询缓存上限 + 解块缓存客户端总预算`
> 纪律：纯静态；不跑测试/ctest/构建；判"缺失"走三级复核。

## §0 范围与命令

- 仓库根 = 当前工作目录；git 查询一律 `--no-optional-locks`。
- 覆盖：`lib/gaia_xpsd_client/`（module_entry.c / gaia_client.c 及头）、`tests/**/CMakeLists.txt` 中两个回归锁、相关冻结文本、`aio_publish.cpp`/`fits_core.c` 发布面（仅新增不一致）。
- 判定顺序：①负责人设计意图（设计大纲/、memory.md、根 AGENTS.md、40_OWNER_DECISIONS.md）②现行冻结文本（改后）③代码与测试自洽。

## §1 逐条结论

（待填：越界修复闭合性 / 回归锁真实性 / 剪枝等价性 / 缓存确定性）

## §2 新发现缺陷

（待填）

## §3 反向钉死与同源同源缺口

（待填）

## §4 需运行期或未决

（待填）
