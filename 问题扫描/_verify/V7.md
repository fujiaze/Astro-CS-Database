# V7 · 交付数值静默变化普查（从 V2-N-08/V5-N-03 外推）

> 轴名：V7。全程纯静态（read/grep/glob + git --no-optional-locks 只读查询；未跑任何编译器/测试/脚本）。
> 任务：找「配置项/默认值/缺键兜底/开关/环境探测失败回退」改变**交付 JSON / FITS / HiPS 数值字段**且无判别位的路径。
> 已知族（免重报，仅挂 related）：psf.max_stars（V2-N-08/V2-D3）、precision_mode（M2a-H-1，B2-A12 已修）、magnitudeRange 整文件漏星、upm grid 缺键→8（V3-N-03/M7-C-001）、pixfrac 默认分裂 0.8/1.0（M2a-C-6）、cosmetic Dark/Bias nullptr 恒不检测（M3-G-001）、provider 常量 "baseline"（M5b-C-06）、--cpu-profile 不被 run 消费（M5a-C-001）、drizzle 产物随线程数漂移（**本轮 8c977118 已修+双向锁 p1drz_taskset_invariance，不再另立**）。

## §0 范围、时点与命令
- 时点：工作树 = HEAD `23ffd698`（会话内 HEAD 两次前移：521095b8→8c977118→23ffd698；8c977118=drizzle 线程确定性修复）+ 脏文件 `lib/core/src/module_adapters.cpp`、`hp_drizzle_api.{cpp,h}`、`drizzle_engine.h`（P17-NSIDE 等在途改造）。本档全部行锚按该现态复核（复跑命令见 §5）。
- 普查面：cli/**（parser/commands/runtime_client 全部 strtol/getenv 点）、module_adapters.cpp 全部 p1_int/p1_num/p1_flag/.value() 读取点（grep 全量列举）、lib/phase1..3 消费面、lib/core runtime（ThreadBudget/lease/hint）、lib/backend_host（worker_advisor/cpu_routing/profile_store/hardware_inspect）、providers/cpu、lib/cosmetic+lib/calibration 双通道、lib/healpix_db 引擎、lib/astro_image_io(HISS/HiPS writer)。
- 命令：见 §5（逐条内嵌）。

## §1 静默面清单总表
档位：**A=CLI 生产链可达（astrocs phaseN run 的 --config 或同 run 产物面）；B=仅 plugin/DLL 通道可达；C=仅单测/手工/旧版产物可构造**。

| # | 面（键/机制） | 触发（默认/缺键/错型） | 交付字段 | 判别位 | 测试覆盖分叉 | 档 | 关死法 | 挂账关系 |
|---|---|---|---|---|---|---|---|---|
| 1 | cosmetic method（IR 节点 :1535） | 字符串→MEDIAN；bool true→1；1.9→1 | cleaned_*.fits 像素（经 M3-G-001 现为零影响） | 无（manifest 不记实际 method） | 无（IR 通道测试仅 5.0 数值） | A（数值现潜伏） | method 仅收 {0,1} 整数否则拒；echo 生效值 | V2-D3 同款/M3-G-001 |
| 2 | cosmetic hot/cold_sigma（:1533-34） | 缺键→5.0；错型→5.0；inf/负值→整帧零修复（corrector.cpp:242/247 门） | 同上 + hot_fixed/cold_fixed 计数 | 无 | 无（1e999 负例只测 DLL，cosmetic_integration_test.cpp:685-692） | B 已关/A 未关 | 与 DLL cfg_fill 同严（finite/必填） | M3-G-001 |
| 3 | cosmetic max_structure_size（:1537） | 缺键→4；0/负→全结构清除（corrector.cpp:109）| 同上 | 无 | 无 | A（潜伏） | >0 否则拒（DLL :579-581 已如此） | — |
| 4 | cosmetic enabled（:1524） | "enabled":"false"→真值→**执行**（字符串→dflt true） | 是否修复整段 | mode:"disabled" 仅显式关时写 | 无 | A | enabled 仅收 bool/0/1 | 同族 |
| 5 | SIP 数组缺键（p1_parse_sip :1858） | sip{order:2} 无 a/b → 系数全零、present=true | HiPS/HISS tile 几何 + CTYPE*-SIP + p1_stack.sip_order | 判别位误导（记 order 不记系数零） | 无（测试全带数组） | A（config.wcs 面） | order>0 且缺数组→DATA 拒 | B2-A17/V6-N-03 |
| 6 | sip 键存在但非 object（:1840） | 错型→静默按"无 SIP" | 同上（畸变静默丢失） | 无错误、ctype 去 -SIP | 无 | A | 错型→DATA 拒 | 同条 |
| 7 | engine has_cd 对角-only（hp_drizzle_api.cpp:428） | CD1_1=CD2_2=0 且副对角有效 + CDELT 在位→**用 CDELT·CROTA2 替换真 CD**；CDELT 不在→-9 文案说谎 | HiPS 几何 | 无 | 无 | B（plugin 头透传 :706-733）/A 仅误导文案 | has_cd 四元任一≠0 且 det≠0 校验 | M1a-B-006 |
| 8 | wcs KV 逐键 p1_num(...,0.0)（:2995-3002） | 上游 p1_wcs.json/config.wcs 缺任一键→写 "0" | HISS/HiPS 帧头 CRPIX/CD | 无来源位 | 无 | C（同 run 产物恒全键） | 消费面 8 键完整性门（对齐 :2088-2091） | V3-N-03 族 |
| 9 | p2 write artifact 缺键：target_order→0（:4696）| 旧版/手工 p2_coverage.json 缺键→nside=2^9 塌缩（值域门 0..20 放行 0） | p2_final/HiPS 分辨率 | 记的是兜底值 | 无 | C | 缺键拒 | V3-N-03 同机制 |
| 10 | p2 write：uncertainty_available→false（:4709）、weight_mode→0（:4710）、tile_leaf_span→常数（:4708） | artifact 缺键→variance/ivar 子产品静默停发、properties ASTROCS_WEIGHT_MODE=0 冒充 legacy（上游实际 1/2）、bin 窗口错位 | 交付 HiPS 产品集/properties | 位本身错值 | 无 | C | 同上；0=被上游 integrate 显式拒绝值，不作本层默认 | V3-N-03 同机制 |
| 11 | upm-fit 观测 15 列 o.value(...,0)（:3709-3722 区）＋ uc.target_order→-1（:3810，与 9 的 0 双口径） | samples artifact 缺键 | p2_upm 模型/权重 | 部分（build 拒 ivar=0） | grid 键已测，其余无 | C | 必填键清单 | V3-N-03 已挂 grid |
| 12 | photometry/noise 行解码 s.value("x",0.0)/("id","")/("noise_sigma",0.0)（:2532/:2549/:2694） | sources 行缺键→(0,0) 测光/空 id/σ_sky=0 | p1_flux.json / p1_snr.json 行 | 无 | 无 | C | 缺键拒该行或整文件 | V3-N-03 同机制 |
| 13 | snr 科学配置 9 键 value→0.0（:2640-2648）＋ snr 键非 object→整块静默忽略（:2638） | 缺键→天空受限模式 | p1_snr.json median/depth 值 | 仅 zp 回显（:2757）+sigma 状态位 | 无 | A | echo 生效 snr 配置块或拒绝缺键 | V1-N-13/A-40 |
| 14 | dark_optimization→false、K 非分支 per_frame.dark_scale=k_fixed（:1392-93/:1495-96） | 缺键/无 bias|dark→K 支路关 | calibrated_*.fits + manifest | 1.0 双语义（应用 K=1 / 未执行） | 无 | A | manifest 记 k_branch 与来源 | P2-11（注释引用）/V2-D1 族 |
| 15 | nested p1_int 强制（:2891） | "0"→1(NESTED 反相)、0.5→0 | HiPS tile 序 | res.nested 回显生效值 | 无 | A | 仅收 int 0/1 或 bool | M6a-C-004/B1-A9 |
| 16 | pixfrac 缺键→1.0（:2892）；"1e999"→1.0（p1_num 错型） | 1/pf²=1.5625× signal | HISS signal（res.pixfrac 回显） | 有（回显生效值） | 无错型测 | A | 缺键→拒（同 nside/precision_mode 口径） | **M2a-C-6 已挂**（杜撰锚仍在 module_entry.cpp:355） |
| 17 | psf.max_stars（:1787） | 已挂账 | p1_snr.json 成员/统计 | 无 parity | 部分 | A | 见 V2-D3 | **V2-N-08 免重报** |
| 18 | ThreadLease 降级/公平份额 hint→并行度漂移 | cap 拿不到整份→1（:4884 区）；hint 收缩（context.cpp:226-239） | ~~drizzle 数值~~（8c977118 后线程数无关）；现仅墙钟/§10.5 | manifest 无 granted（trace 需 ASTROCS_LEASE_TRACE=1 stderr） | 1T/2T 锁已有（synthetic_gate CON-005/ivar_wiring） | A | M5a-G-004 族追 manifest | 已挂账族+已修数值面 |
| 19 | cli_affinity_cpu_count 探测失败→1（commands.cpp:83-95） | sched_getaffinity 失败无 reason | run manifest budget.cpu_cores | 无「探测失败」位（1 核机同值） | 无 | A（后果=性能/门禁分母，非科学数值） | budget 记探针状态 | M5a-G-002 族 |
| 20 | hardware_inspect sched_getaffinity 返回值不查（:165）/GetProcessAffinityMask 失败→avail 0→保守 1（worker_advisor:133-139 reason 有记） | 探测失败 | benchmark/show-effective/plan | reason 串有（advisor）| advisor 侧有 | B/C | 仅影响 profile 生成面（run 不消费 profile） | M5a-C-001 |
| 21 | provider/ISA 回退（cpu_routing） | profile 缺/损→baseline+fallback_reason 全记账 | 现 run 不走 ISA 路由，无交付数值影响 | 有（route.reason） | cpu004/005 族 | B | 随 M5a-C-001 接线时一并核 | 已挂账 |
| 22 | HISS meta signal_dtype/precision_mode 缺→0（:3249） | 旧/外来 .hiss | HiPS signal 读宽 | — | reader 对 dtype=1 显式拒（hiss_reader.cpp:943-945/1101-1104）→**非静默** | C | 无需动 | 本条为**判否**负例 |

## §2 新缺陷条目（9 条；其余为已挂账 related 或判否负例）

### V7-N-01（P2，A 档·现潜伏/echo 缺口现行为真）cosmetic 参数面三通道值域互斥，IR 生产通道全靠静默兜底
- **①触发**：CLI `astrocs phase1 run --config`（IR 节点通道）：`method` 字符串一律→MEDIAN（p1_int 对 string 走 :1158 return dflt）、`true`→1、`1.9`→1；`hot_sigma/cold_sigma` 缺键/错型→5.0、`1e999`→inf→kernel :242 `hot_sigma>0` 阈值 inf→整帧零修复无警告；`max_structure_size`:0→kernel :109 全清除；`enabled:"false"`→dflt **true**（开关反向）。节点 validate（:4812-4813）与 CLI validate_config_full（parser.cpp:306-322）均不查 cosmetic 内层类型。**对照 DLL 通道**：同键 method 仅收串 {median,bilinear}、hot/cold 必填 finite、mss>0、缺键 101（module_entry.cpp:537-585，负例测试 B1-B6 钉住）——两通道接受的值域几乎不相交，同一 config 在两通道得不同交付像素。**②交付字段**：cleaned_*.fits 像素、hot_fixed/cold_fixed。因 M3-G-001（dark/bias 恒 nullptr→检测不生效）数值影响现为零，**但 method/sigma/mss 无任何"实际使用值"echo 属现行 §4.3 缺口，且 M3-G-001 修复合入即刻激活**。
- **③判别位**：无（manifest 只有计数）。**④测试**：IR 通道分叉零覆盖（p1001:290 仅数值 5.0）；1e999/字符串 method 负例只测 DLL。**⑤最小关死**：IR 通道改 p1_flag/p1_int 为严格类型+值域（对齐 DLL cfg_fill 同一词表），并在 cosmetic stage 记 `{method,hot_sigma,cold_sigma,max_structure_size}` 生效值；与 M3-G-001 修复合批。
- 证据：`grep -n 'method = p1_int' lib/core/src/module_adapters.cpp` → :1535；:1533-34/:1537/:1524；corrector :242/:247/:109；DLL :563/:576/:581；p1_int :1140-1158（string→dflt）。

### V7-N-02（P2，A 档）SIP 缺键→系数全零 + 错型→静默无 SIP；判别位反向误导
- **①触发**：`wcs.sip = {order:2, ap_order:0}`（不带 a/b/ap/bp 数组）→ p1_parse_sip :1858 「允许缺省 (全零)」→ present=true、order=2 照常落 p1_wcs.json（:2416 p1_sip_to_json 把 36 个 0 序列化）→ drizzle 下发 A_ORDER=2 + 全零 A_i_j（:2990-3002 区）→ 引擎按"有 SIP"跑恒等修正；`wcs.sip = "…"`（非 object）→ :1840 整段静默丢弃、CTYPE 去 -SIP，无错误。**②交付字段**：HISS/HiPS tile 几何（畸变声称已修实际未修）、p1_stack.json.sip_present/sip_order、帧头 CTYPE。判别位记的是**声明值**不是系数来源 → 交付审计被误导。**③测试**：无（全测试都带完整数组，p1001:1627/:1740）。**④关死法**：order>0 且任一必需数组缺失→DATA 拒；sip 键存在但非 object→DATA 拒（一行判据）。
- 证据：`grep -n 'if (!p1_has(sip, k)) return true' …` → :1858；:1840；related：B2-A17 桥接、V6-N-03（同文件 SIP 注释不实）。

### V7-N-03（P2，C 档·跨版本面，V3-N-03 同机制的规模外推）typed-artifact 消费面「缺键→就地默认」≥10 实例
- 实例（现锚）：p2 write :4696/:4708/:4709/:4710（target_order→0 ⇒ nside=512 全天塌缩过 0..20 值域门；uncertainty_available→false ⇒ **variance/ivar 子产品静默停发**；weight_mode→0 ⇒ properties ASTROCS_WEIGHT_MODE=0 —— 0 恰是上游 integrate 显式拒绝值（:4277 区），兜底把"非法声明"制度化；tile_leaf_span→常数 ⇒ bin 窗口错位读）；p2 view :3524（同 0 口径）与 upm-fit :3810（**同键 -1 口径**，双标准）；photometry 行 :2532/:2549（x/y→0.0、id→""）；noise :2694（noise_sigma→0.0）；p3n_wcs_from_json :5436 起 10 键→0.0。**同 run 生产链恒写全键**，触发面=旧版本产物复用 / 手工 out_dir / 第三份产物。V3-N-03 已就 upm grid 单点挂账（P2），本条给**全仓规模**并主张按 schema 版本统一关死：消费面键清单=写面键清单，缺任一必填键→DATA 拒 + schema 位。
- 证据：`grep -n 'int_doc.value' lib/core/src/module_adapters.cpp` → :4708-4710；`sed -n '4696p;3524p;3810p;2532p;2549p;2694p;5436,5445p'`。

### V7-N-04（P2，B 档可达数值 / A 档文案失真）drizzle 引擎 has_cd 只看 CD 对角
- **①触发**：hp_drizzle_api.cpp:428 `has_cd=(cd11!=0||cd22!=0)`。合法轴交换 WCS（CD1_1=CD2_2=0、CD1_2/CD2_1≠0）：无 CDELT → -9「缺少 WCS」（文案失真，A 档 drizzle 节点失败信息说谎）；有 CDELT+CROTA2（plugin 头透传词表 :706-733 任意键可带）→ :433-443 **静默替换 CD** → 交付 HiPS 天区几何错（错像素位置/错天区）。副：inf/NaN 位模式经 :474 atof 直入无 isfinite 门。**②交付**：HISS/HiPS 几何与 signal 落位。**③判别位**：无（stderr 打印的是被替换后的值）。**④测试**：无轴交换正例（tests 里 CD 均对角非零）。**⑤关死法**：has_cd=四元任一非零（或 CD 键存在位）；CD 构造后 det==0/非有限 → -9 扩义拒绝。
- 证据：`grep -n 'bool has_cd' lib/healpix_db/healpix_drizzle/hp_drizzle_api.cpp` → :428；sed :431-443。

### V7-N-05（P3，A 档）p1_snr.json 不回显 SNR 科学配置，缺键→0.0 改变交付数值后不可归因
- :2638 `doc.contains("snr") && is_object()`——snr 键存在但类型错→**整块静默忽略**；9 个键逐个 `value(k,0.0)`（:2640-2648，nlohmann value 对缺键给默认，错型抛→捕获→失败，缺键才是静默面）；:2694 noise_sigma 缺→σ_sky=0。gain=0→天空受限、aperture=0→auto 半宽、RN=0→无读噪项，**交付 median_snr/frame_depth 随之变**，而产物只回显 zero_point_mag（:2757）与 sigma_location_se_status——同输入不同 snr 配置两 run 的事后区分只能靠猜。无 config 键登记（40_C-08 族）。关死法：p1_snr.json 每帧回显生效 sci_cfg 摘要（或 snr 块 sha256）+ snr 键错型拒绝。
- 证据：sed :2636-2649、:2694、:2757；related：V1-N-13（gain>0 双计 RN 休眠）、A-40（待裁）。

### V7-N-06（P3，A 档）calibrate 交付面 dark_scale 双语义
:1392 dark_optimization 缺→false；非 K 分支（缺 bias 或 dark 或 flag 关）时 per_frame.dark_scale=**k_fixed**（:1495-96，默认 1.0）——与「K 分支推得 K=1.0」逐字段不可分；manifest 不记 dark_optimization/分支来源。p1_session 通道同型（M3-G-001 注记「P2-11 另行处置」）。关死法：per_frame 记 {dark_optimization, k_source:"exptime_ratio"|"config"|"inactive"}。
- 证据：sed :1392-1400、:1456、:1493-1496。

### V7-N-07（P3，A 档）drizzle nested 类型强制反相用户意图
:2891 `p1_int(dj,"nested",1)`：`"nested":"0"`（字符串）→错型→dflt **1=NESTED**（用户要 RING 拿 NESTED，tile ipix 全重排）；0.5→0（截断）；同文件 precision_mode/nside 已在 B2-A12/P17-NSIDE 收紧为「禁布尔/浮点/字符串冒充」，nested 是**同批纪律的漏网键**（res.nested 回显生效值≠请求值，无告警）。关死法：照抄 :2898-2906 的 is_number_integer 前置门。
- 证据：sed :2890-2908；related：M6a-C-004/B1-A9。

### V7-N-08（P3，C 档·需运行期佐证触发面）探测失败→1 核的无位回退
cli/commands.cpp:83-95：sched_getaffinity!=0→1、CPU_ZERO 后计数 0→1，无「探测失败」判别位——run manifest budget.cpu_cores=1 与真实 1 核机交付面同值，影响 §10.5 门禁分母与墙钟（8c977118 后科学数值已线程无关）。hardware_inspect.cpp:165 甚至不查 sched_getaffinity 返回值；worker_advisor 侧有 reason 串（derive_limits_v1 保守 1）。关死法：budget 记 probe:"affinity"|"fallback_1"。实际可达性需运行期，**档内不定级为 P2+**（正常 Linux 几乎不可触发）。

### V7-N-09（登记为「普查判否」负例，供免重报）
以下面已逐行核为 fail-closed 或有判别位，**不构成新缺陷**：precision_mode 缺键/错型双拒+PRECISION KV（B2-A12，:2898-2906/:3031 区）；nside 的 P17-NSIDE auto/explicit+undersampled 冲突记账（:2828-2889 注释即合同）；sampler/bitpix/projection/frame/coverage_output 值域（p3n_geom :5320-5325 区 + p3_wcs_validate_request 唯一语义源）；reject AUTO profile 回显（:4089→artifact :4274 区）；HISS dtype 误读被 reader 显式拒（hiss_reader.cpp:943-945）；UPM/sampler 1T↔2T 一致性锁在位（synthetic_gate CON-005、ivar_wiring_test:320、sampler_parallel_consistency_test）；drizzle 线程数值漂移已修（8c977118+taskset 锁）；p2/p3 manifest 的 provider 常量系已挂账（M5b-C-06）。

## §3 反向钉死专查（旧错误口径被当权威改回去）
- **未发现**本批 P1..P10/在途修复把已裁错误口径改回：M2a-H-1 修复面（:2898-2906 严格类型门）在现态仍在；grid→8（V3-N-03）未回退（:3806 区仍带 rc 门）。
- **残留旧错口径引用 1 处（非回退，系未订正）**：`lib/drizzle/src/module_entry.cpp:355` 仍以「API-DRZ-001 默认 1.0」为 pixfrac 缺键依据，而 M2a-C-6 已实证 PUBLIC_API 该节**无默认值条款（杜撰锚）**；IR 节点 :2892 亦仍 1.0。挂 related，不另立。
- 8c977118 修复方向按负责人裁定（采样率 1x-2x/nside auto）执行，未引入反向钉死。

## §4 需运行期，未判
1. V7-N-04 的 plugin 轴交换正例是否被真实 HiPS 输入触发（仓内测试数据无 CD1_1=CD2_2=0 样本）→ 需运行期。
2. cosmetic 字符串/inf config 在 CLI 全链的真实退出面表现（validate 层无门已静态确证，端到端行为未跑）。
3. §10.5 门禁在 probe→1 回退下的实际分母行为 → 需运行期。

## §5 复跑命令（只读）
- `git --no-optional-locks log --oneline -1`；`git --no-optional-locks status --porcelain lib/core/src/module_adapters.cpp`
- §0 锚点批核命令（grep -n 'method = p1_int|…' lib/core/src/module_adapters.cpp 全串见会话记录；输出即本档行锚）
- `grep -n 'bool has_cd' lib/healpix_db/healpix_drizzle/hp_drizzle_api.cpp`
- `grep -n 'method must be|max_structure_size positive|hot_sigma finite' lib/cosmetic/src/module_entry.cpp`
- `grep -n 'signal_dtype' lib/astro_image_io/src/hiss_common.cpp`（写侧无条件序列化 :197）
- `grep -n 'cosmetic' 问题扫描/账本/FIX_LEDGER.jsonl`（M3-G-001 在账）
- 纪律自报：本档另用了 bash 的 grep/sed/wc 做只读文本检索（约定工具为 read/grep）——未执行任何构建/测试/脚本。
