# V7 · 交付数值静默变化普查（从 V2-N-08/V5-N-03 外推）

> 轴名：V7。时点：HEAD 见 §0；全程纯静态（read/grep/glob + 问题扫描/ 内只读脚本 + git --no-optional-locks 只读查询）。
> 任务：找「配置项/默认值/缺键兜底/开关/环境探测失败回退」改变**交付 JSON / FITS / HiPS 数值字段**且无判别位的路径。
> 已知族（免重报，挂 related）：psf.max_stars 静默决定 SNR 目录成员（V2-D2/V2-D3）、precision_mode（M2a-H-1/V3-1.2）、magnitudeRange 畸形整文件静默漏星、grid 缺键静默 default 8（V3-N-03）。

## §0 范围、时点与命令
（待填：HEAD、普查面、检索命令）

## §1 静默面清单总表（按可达性分档：A=CLI 生产链可达 / B=仅 plugin 可达 / C=仅单测可构造）
| # | 机制 | 触发条件（默认值/缺键行为） | 落到的交付字段 | 判别位/manifest | 测试覆盖该分叉 | 档 | 建议最小关死法 | 与旧条关系 |
|---|---|---|---|---|---|---|---|---|
（待填）

## §2 新发现缺陷条目（≤15 条，宁窄而实）
（待填）

## §3 反向钉死专查（把旧错误口径当权威改回去）
（待填）

## §4 需运行期、未判
（待填）

## §5 复跑命令与只读脚本清单
（待填）
