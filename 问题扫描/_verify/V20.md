# V20 · 「声明了但实际不被消费的参数」四面全仓静态审（骨架 · 进行中）

## §0 时点与口径
- 时点：HEAD `a3a343a44080d917089e1f8d548ed2d0400b0c61`（2026-09-15 06:12:47 +0800），工作区脏文件 60 条（`git --no-optional-locks status --porcelain | wc -l`）。
- 口径：纯静态（read/glob/grep + 只在 `问题扫描/` 内跑只读 `python3 -B` + `git --no-optional-locks` 只读子命令）；禁编译器/cmake/ctest/跑测试或二进制/`ci/run.py`/连 Fatduck/删除改名/git 写。
- 三态定义：**消费** = 该键/选项/形参/字段的值进入 kernel 计算、写出产物内容、或参与任一判定（分支/门禁/退出码）；**仅回显** = 只被写进 manifest/日志/JSON 输出或原样透传，值的变化不影响任何计算或判定；**零消费** = 解析/声明后既无回显也无计算（含 `(void)x` 刻意未用、分支永不成立）。
- 「若该参数被用户设置，行为是否有任何差别」判据：逐值改动 → 是否存在任一可达分支/写出字段/计算项随之改变；若唯一改变是回显文本本身，判「无差别」。
- 反查面纪律：每条「零消费」结论至少两条检索（basename 全树 + providers/runtime/modules/cli 面 + 测试面），并说明为何非别名/函数指针/序列化宏消费。
- 免重报：已读 `问题扫描/SUMMARY.md` 簇表（簇 8 六成员、机制⑪、`C-20`/`C-21`）与 `问题扫描/INDEX.md` §三；已知实例一律挂 related 不重报。

## §1 四面三态表
### 面 1 · 配置与 IR 键
（待填）

### 面 2 · CLI 与选项
（待填）

### 面 3 · 结构体字段与 C ABI 形参
（待填）

### 面 4 · 默认值与哨兵
（待填）

## §2 结论条
（待填）

## §3 判否负清单（核过确为真消费，供其它轴免重报）
（待填）

## §4 需运行期未判
（待填）

## §5 机器门建议
（待填）
