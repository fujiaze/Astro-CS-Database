# V19 · CI 依赖声明完整性普查（检查项 × 依赖类型 × 静默 error/skip）

> 轴：V19 | 派生自 V15-N-16 / M8-F-004 / C-12 外推 | 权威序：①负责人设计意图 ②现行冻结文本（改后）③代码/测试自洽
> 纪律：纯静态（read/glob/grep + 问题扫描/ 内 python3 只读解析 + git --no-optional-locks 只读查询）；
> 未运行编译器/cmake/ctest（含 -N）/测试/二进制/ci/run.py；未运行任何无 --dry-run 的仓内脚本；无 git 写；未连 Fatduck；无删除改名。
> 免重报：已先读 SUMMARY.md 簇表 + INDEX.md §三；同义主题挂 related，不另立条。
> 口径基线：`ci/checks.json` 经 `json.load` 解析，checks 数组长度 **130**（时点 HEAD f5c790bd，登记面为活动靶，见 V9 附表同样数）。

## 0. 骨架（先建后填）
- [ ] §1 规模与口径（json.load 计数 + 字段频次 + 排除清单）
- [ ] §2 依赖轴①：command 引用的可执行/脚本/数据文件是否存在于 tracked 树（L1/L2/L3）
- [ ] §3 依赖轴②：构建产物依赖（build/**、*.so/*.dll、evidence/*.json、run/**）而该 job 无构建步骤
- [ ] §4 依赖轴③：网络 / 真机（GaiaDR3、Fatduck、外部 endpoint）
- [ ] §5 依赖轴④：前序检查项产物 vs 声明顺序 / changed_paths
- [ ] §6 依赖轴⑤：读 gitignored 路径（触 C-15）
- [ ] §7 矩阵：检查项 × 依赖类型 × 是否可能静默 error/skip
- [ ] §8 新缺陷条目（≤15 条，宁窄而实；每条附命令+输出摘录 + 最小修复建议）
- [ ] §9 需运行期、未判项
- [ ] §10 排除清单与工具输出物

## 1. 规模与口径
（待填）

## 2. 依赖轴①
（待填）

## 3. 依赖轴②
（待填）

## 4. 依赖轴③
（待填）

## 5. 依赖轴④
（待填）

## 6. 依赖轴⑤
（待填）

## 7. 矩阵
（待填）

## 8. 新缺陷条目
（待填）

## 9. 需运行期未判
（待填）

## 10. 排除清单
（待填）
