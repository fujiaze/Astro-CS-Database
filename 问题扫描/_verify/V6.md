# V6 — 修复层独立复核（验证层子代理）

## 0. 范围、时点与命令口径

**被复核对象**：隔壁 Agent 的 16 个提交 / 58 个 lib 文件 / 7 份冻结科学文档，以及 `账本/FIX_LEDGER.csv` 中 20 条已处置（FIXED/PARTIAL）条目。
**判据顺序**（负责人裁定优先于本层任何推断）：① 负责人设计意图（`设计大纲/`、根 `memory.md`、AGENTS.md 十要素、`40_OWNER_DECISIONS.md`）② 当前冻结文本（订正后）③ 代码/测试自洽。与旧审计条目或旧文档不一致**一律不算缺陷**；反过来专查：(a) 旧的错误表述是否被重新钉成权威；(b) 新表述是否只改文档不改代码，或只改代码而文档/接口/测试未同步（§「科学定义=算法=接口=代码=测试」五处同源）。
**纪律**：纯静态；git 查询一律 `--no-optional-locks`；结构化文件（csv/json/长行 md）用解释器解析，不作行文本读（>2000 字符行会被截断）；「缺失/0 命中」结论一律走三级复核（全句 grep → 放宽关键词 → 定点读），排除前缀写作 `./x`；不判运行期事项，写「需运行期，未判」。

### 0.3 时点漂移（本层期间 HEAD 移动 5 次，全部计数按标注时点复算）

| 测量 id | 时点 HEAD | 用途 |
|---|---|---|
| M-V6-01 | `dbaa2e18` | 初次 SHA/锁普查 |
| M-V6-02 | `35c85f53` | 门判定器内存复算 |
| M-V6-03 | `bc8f3893` | 分片 1 全部结论（C3=10、CON-TRACE、BOM） |
| M-V6-04 | `b858b76d` | 分片 1 之后的账本重对齐、P11 两提交普查 |
| M-V6-05 | `5db3c905` | 终片 C3 计数（=12）、新工具/新注释、锚漂移 |

### 0.4 一处自我违规登记（必读）

为复算 M3b-F-02 的自报量，我在 M-V6-05 时点运行了 `tools/quality/v19r3_traceability.py`。该脚本**无 `--dry-run`**，会重写 `docs/TRACEABILITY.csv` 与 `reports/v19r3/evidence/**`——属本层「只读脚本」边界之外的写路径，主动登记：

```
$ timeout 120 python3 tools/quality/v19r3_traceability.py
{ "contract_inventory_rows": 63, "traceability_rows": 63, "authority_path_broken": 0,
  "traceability_broken": 0, "contract_coverage": true, ... }
exit=0
$ git --no-optional-locks diff --stat -- docs/TRACEABILITY.csv     # 空
$ python3 -c '...按 id 逐行比较...'
committed rows 64 worktree rows 64 / only in committed: [] / only in in worktree: [] / rows whose CONTENT changed: [] 0
```

⇒ 内容逐行等值（仅 `CRLF→LF` 换行归一化警告），未产生语义改动；`reports/v19r3/evidence/quality/contract_inventory.csv` 在我运行前已是 `M` 态，无法归因。请前台按需 `git checkout -- docs/TRACEABILITY.csv`（本层不执行 git 写操作）。副带确证：**M3b-F-02 自报的 `authority_path_broken=0` 可复算为真**。

---

## 1. 账本 20 条逐条复核

### 1.0 结论汇总（M-V6-04/M-V6-05 时点）

| 项 | 判据 | 结果 |
|---|---|---|
| ① `fix_commit` SHA 存在性 | `git cat-file -e <sha>^{commit}` | **20/20 通过**（去重后 4 个 SHA：`07eb229b`/`adaeb531`/`dce8abd4`/`c3452d48`，均在 main 祖先链） |
| ② `--stat` 覆盖 position | position 内 path ∩ `show --name-only <fix_commit>` | **19/20 条代码落点全覆盖**；`M7-C-001` position 仍空（前台生成器未覆盖该条），无法核 |
| ③ `regression_test` 锁静态可解析 | `add_test(NAME` / `add_executable` / 源内符号 | 15 条 `ADDED:` 中 **13 条可解析，2 条不可解析**（见 1.3） |
| ④ `fix_note` × diff 一致 | 抽 diff 逐句比对 | **20/20 未见夸大或反向**（其中 2 条我做了独立数值复算，见 1.4） |
| ⑤ PARTIAL 残余真实性 | 残余陈述 vs 当前代码 | **2/2 属实**；但 M3b-F-01 的残余门**无机器载体**（见 V6-N-08b） |
| 账本失真（簇 10） | ①–④ 任一不符 | **0 条隔壁越权**；判定列改动全部可归因到前台自身提交（见 1.6） |

### 1.1 SHA 存在性

```
$ git --no-optional-locks cat-file -e 07eb229b^{commit}; echo rc=$?   # rc=0
$ git --no-optional-locks cat-file -e adaeb531^{commit}; echo rc=$?   # rc=0
$ git --no-optional-locks cat-file -e dce8abd4^{commit}; echo rc=$?   # rc=0
$ git --no-optional-locks cat-file -e c3452d48^{commit}; echo rc=$?   # rc=0
# 按 id 对齐 20 条：fix_commit 全部落在上述 4 个 SHA，无空值、无短于 8 位的伪 SHA
```

### 1.2 `--stat` × position 覆盖（按 id 重对齐后）

注：`position` 列在 `9777193d`（前台账本生成器自修）被重写 509 行，本层**不**把该改动算作隔壁越权；本表用的是重对齐后的值。

```
$ python3 (逐条 position 抽 path → 与 fix_commit --name-only 求差)
HEAD b858b76d | FIXED/PARTIAL 20
  M3-C-001     FIXED   pos-files= 1 untested-by-commit= 0
  M3b-F-01     PARTIAL pos-files= 2 untested-by-commit= 0
  M4-C-01/02/03 FIXED  untested-by-commit= 2/1/1  (差集均为 .md/.csv 合同锚，非代码落点)
  M8-F-002     FIXED   pos-files= 4 untested-by-commit= 4  ← 见 1.2a
  M9-H-1 / M9-H-2       pos-files= 0（position 为纯符号描述，不含 path）
  M7-C-001     PARTIAL position 仍空 → 无法核
```

**1.2a M8-F-002 的 position 自身失实（审计侧缺陷，非账本失真）**

```
$ git --no-optional-locks show HEAD:tools/check_aio_ownership.py | wc -l   → 55
position 却写：`tools/check_aio_ownership.py::main`（:459 附近 `return 0` 恒零，`return 1` 仅在 :455）
⇒ 55 行文件不存在 :455/:459，锚指向不存在的行
```

（前台已自判为 F00-13；本层附确证：修复实体真实——`tests/unit/io_ownership_test.cpp` 现 138 行，:80-83 注入失败分支 `return failures == 0 ? 0 : 1`，:126-127 子进程断言 `CHECK(rc != 0)`，:137 主路径 `return 1`。）

### 1.3 锁可解析性

```
$ git --no-optional-locks grep -n 'add_test\s*(\s*NAME\s+([^\s()#]+)' -- '*CMakeLists.txt'   # 201 个名字
$ git --no-optional-locks grep -c ci_missing_prereq_fails -- .        → 0 命中（全仓）
   真名：tests/cli/test_cli_single_install.py::test_01_install_succeeds … test_05_install_tree_only_bin_and_astroc_data
$ git --no-optional-locks grep -n 'case_avg_085_boundary\|case_alloc4_observed_0p9_core_fails' -- tests
   → 仅出现在 p2_workers_test.cpp 的注释行（:93/:109），不是可解析锁名
```

判定：**3 个锁名不可静态解析**（`ci_missing_prereq_fails`、`case_avg_085_boundary`、`case_alloc4_observed_0p9_core_fails`）。其中前 1 个前台已在 `FRONT_LEDGER_CHECK.md` 自判；本层复核同意：断言实体真实在位（`test_cli_single_install.py` :35-39 `raise AssertionError`、:64/:81/:90/:100 `self.fail`；`p2_workers_test.cpp` 4/4b/4c/4d 段有真断言），缺陷形态是**锁名与可发现实体不同名**，属 F_TEST_GAP 书写问题而非假门。

### 1.4 `fix_note` × diff（20/20 一致；摘两条我独立复算的）

**M4-C-01（kcorr）**：note 称「表值与默认 1.4 零改动，角点 bitwise exact；旧式 pf=0.8 偏差 +1.515%/+2.115%」。

```
$ python3 (按 dce8abd4^ 旧式与 dce8abd4 新式各自独立重算 pixfrac∈{0.5,0.8,1.0}×scale∈{300,600} 六格)
新式角点：1.2112 1.3925 1.4980 / 2.3958 2.8971 3.2035   ← 逐位等于表值
旧式对角点偏差：pf=0.8,sc=300 → 1.515260% ; pf=0.8,sc=600 → 2.115219%
默认 kControlCorrDefault = 1.4 未变（lib/phase2/.../sampler.cpp）
```

⇒ note 的两个百分数与我独立算的 1.515260%/2.115219% 一致，**非与旧文档比附，算法方向正确**。

**M3b-H-01（真 MAD）**：note 称「mad 改真 MAD；常数改冻结 15 位 1.482602218505602；门限 0.2 未改（出处待登记）」。

```
$ git --no-optional-locks show c3452d48 -- lib/star_detector/src/sdet_api.cpp
-    result->mad = pdata.rmse;
+    double median_res = sdet_median_of(pdata.residuals);
+    for (double r : pdata.residuals) deviations.push_back(std::fabs(r - median_res));
+    result->mad = sdet_median_of(deviations);          ← median(|res − median(res)|) 定义正确
-        double rmse_ratio = fit.mad * 1.4826 / fit.A;
+        double rmse_ratio = fit.mad * 1.482602218505602 / fit.A;
$ python3 -c 'from math import erfinv,sqrt; print(1/(sqrt(2)*erfinv(2*0.75-1)))'
1.482602218505602   ← 与我独立算的 1/Φ⁻¹(3/4) 逐位一致
$ grep -n '1.482602218505602' docs/science/NOISE_MODEL.md   → :46, :135（引用的冻结行真实存在）
```

⇒ 改法与自述一致。遗留：门限 `0.2` 到 HEAD 仍无出处登记（note 已自陈），属 §3 同源缺口而非失真。

### 1.5 PARTIAL 残余真实性

**M3-C-002**（残余：饱和星门只在库内，生产未喂 qf）

```
$ grep -n 'cleanAndScale(' lib/photometric_calib/cpp/src/pc_api.cpp
:137 :396 :524 :767 :1046  →  全部形如 cleanAndScale(raw_matches, &match_rows, nullptr, 3.0, ...)
```

⇒ 5 个生产调用点第 3 参一律 `nullptr`，残余陈述**属实**，PARTIAL 判得准。

**M3b-F-01**（残余：生产路径 centroid p95 未达 0.3px）

```
$ python3 -c "import json;d=json.load(open('lib/photometric_calib/cpp/test/gate4_dr3sp_gaiaxpy/evidence/gate2_result.json'));..."
centroid_p95_px            = 1.1528986035412487
centroid_p95_le_0.3px_production = False
centroid_p95_le_0.01px_converged_not_acceptance （已从验收项改名）
```

⇒ 残余陈述 p95=1.1529px **与证据逐字相符**，PARTIAL 判得准。**但**：

```
$ git --no-optional-locks grep -ln 'gate2_psf_oracle' HEAD -- ci
  （ci/checks.json 121 项 command 中零命中；仅 inventory/audit/账本类文件提及）
```

⇒ 这个「生产门为 false」的判定没有任何机器载体：红门不进 CI，无人会看到。归 V6-N-08b。

### 1.6 判定列是否被改动（任务 2）

```
$ git --no-optional-locks log --format=%h ccc7a933..HEAD -- 问题扫描/账本/FIX_LEDGER.csv   （逐提交 diff 列计数）
35e531e3  判定列改动: 无      处置列: fix_state/fix_commit/… 各 2
023246c9  判定列改动: 无      处置列: 各 11-12
b61ca877  判定列改动: 无      处置列: 各 7
bc8f3893  判定列改动: 无      verified_* 各 5
9777193d  判定列改动: {position:509, clause:109, impact:61, suggested_disposition:219, related:82, priority:59, release_blocker:40, evidence:16, source_line_state:2}
e03800bc  判定列改动: 无      verified_* 3-4
c3ff6c42  判定列改动: {owner_decision:1}
```

⇒ **隔壁（B1–B4 挂账）三次提交只动处置列，判定列零改动，无越权。** 判定列的大规模改动全部来自前台自身 `9777193d`（生成器自修），本层不计为被复核对象的缺陷。
**提醒前台**：`9777193d` 的提交说明是「生成器自修三处」，但实际同时改写了 `priority` 59 行、`release_blocker` 40 行、`clause` 109 行、`suggested_disposition` 219 行——改动面远大于自述范围，建议前台单独确认这批判定值不是生成器副作用。

---

## 2. 新发现缺陷（本层新增，编号 V6-N-01 … V6-N-12）

### V6-N-01（P1·簇 10 门破族）本批修复自己把三道非豁免门改红

时点 M-V6-03（bc8f3893）与 M-V6-05（5db3c905）。三道门的判定器我都在内存内复算（不建树、不跑 ctest、不写 run/）：

**(a) CON-TRACEABILITY 由 PASS 转 FAIL —— 因删行**

    $ python3 问题扫描/_verify/scripts_v6_con_traceability.py   # 本层按 tools/quality/contracts/check_traceability.py 判据重写
    REV=ccc7a933 rows=67 status=PASS exit=0
    REV=c3452d48 rows=63 status=FAIL exit=1  TRACE-CORE-MISSING PSF / REJ
    REV=5db3c905 rows=63 status=FAIL exit=1  TRACE-CORE-MISSING PSF / REJ

被 c3452d48 删掉的 4 行（均原为 VERIFIED）：SCI-PSF-001（docs/science/PSF.md）、SCI-REJ-001（REJECTION.md）、SCI-INT-001（INTEGRATION.md）、SCI-ACR-EQUIV-001（ACR_EQUIVALENCE.md）。四份文档在 HEAD 仍是 FROZEN (T101/T107/T108/T109)，其 ID 全仓仍分别被引用 31/83/86/10 次 ⇒ **合同行被删而权威文档仍在被消费**；核心关键词 PSF/REJ 从全部 63 行的 requirement_id 中消失，门即红。

**(b) TRACEABILITY-MATRIX 早退（BOM 杀头）**

    $ python3 -c "print(open('docs/traceability/TRACEABILITY_MATRIX.csv','rb').read()[:12])"
    b'\xef\xbb\xbfmodule_id'          # ccc7a933 版为 b'module_id,mo'；BOM 由 c3452d48 引入
    $ python3 -c "…import check_traceability_matrix as cm; cm._check_csv_parity(root,out)…"
    ERROR SCHEMA_VIOLATION docs/traceability/TRACEABILITY_MATRIX.csv 表头与 JSON 列不一致: 缺={'module_id'} 多={'\ufeffmodule_id'}

该检查器以 encoding="utf-8"（非 utf-8-sig）打开 CSV ⇒ 首列名带 \ufeff → 判 schema 违规并 return，JSON/CSV 奇偶校验（:374-411）**从未被执行**。检查项 TRACEABILITY-MATRIX 在 fast/linux-main/windows-main，waivable:false。

**(c) CTEST-REGISTRATION C3 未登记目标 0 → 10 → 12**

    $ python3 -c "…q=check_ctest_registration; q.evaluate(parse_targets(HEAD), ci/checks.json, ci/ctest_baseline.json)…"
    C3 未注册的 add_test 目标 12 个：
      p1snr_science_{units,oracle,negative,production,determinism}   ← b0353303
      p1snr_linux_{units,oracle,contract,negative,determinism}       ← 35c85f53
      p1star_angle_guard                                             ← 8dc0220e
      ipv_triangle_budget                                            ← b858b76d
    时点对照：ccc7a933 UNREG=0 / bc8f3893 UNREG=10 / 5db3c905 UNREG=12

登记规则原文（tools/quality/check_ctest_registration.py C3）：「新增/改名测试必须**同提交**在 ci/checks.json 登记显式检查项，或经 ci/ctest_baseline.json 冻结收编」——四个提交均未做。讽刺点：M3b-F-02 的 fix_note 自称「ci/checks.json 相应 profile 增列」，而它新登记的 3 个脚本反倒成了**唯一没被红门发现的**那一处（见 V6-N-08b）。

### V6-N-02（P1·簇 10）MATRIX 的 CSV 视图与 JSON 权威分叉 5 行

    $ python3 (CSV 行 vs JSON 派生行逐列求差)
    5 行分叉：MOD-astrocs-phase1-{calibration,noise-snr,photometry,star,star-psf}
      分叉列：test_id, test_path, evidence_id, evidence_status, notes
    $ git --no-optional-locks show c3452d48 -- …/TRACEABILITY_MATRIX.json | 语义 diff → 仅 3 个模块变（calibration/noise-snr/photometry）
    $ git --no-optional-locks show c3452d48 -- …/TRACEABILITY_MATRIX.csv  → 5 行变（另含 star / star-psf）

即：CSV 被**手工**改 5 行，JSON 只改 3 行；gen_traceability_csv.py（JSON→CSV，JSON 为权威）一旦重跑，star/star-psf 两行订正即蒸发、evidence_status 被打回 MISSING。→ 该订正**不在权威面上生效**（见 V6-N-06）。

### V6-N-03（P1·簇 7 注释可靠性）两条新注释自述不存在的 ABI

    $ grep -c quality_flags lib/photometric_calib/cpp/include/photometric_calib.h   → 0
    $ grep -c -e PC_QF -e SNR_QF  lib/photometric_calib/cpp/include/photometric_calib.h → 0
    $ grep -n 'int rejected_quality' …/photometric_calib.h                          → 仅 :36
    $ git --no-optional-locks diff c3452d48^..c3452d48 -- …/photometric_calib.h     → 空（头文件确未动，ABI 回退属实）
    c3452d48 新增的 lib/photometric_calib/cpp/src/star_matcher.h 头部却写：
      「与 photometric_calib.h 的 PC_QF_*/SNR_QF_* 同值 … 见 photometric_calib.h 头部注释」

    $ grep -c '^PC_API int pc_' lib/photometric_calib/cpp/include/photometric_calib.h → 6（:106 :156 :189 :205 :231 :251）
    $ grep -n quality_flags   lib/photometric_calib/cpp/include/photometric_calib.h   → 无
    $ sed -n '258,262p' lib/photometric_calib/tests/p1phot/p1phot_fixgates.cpp
      // 编译期即证明 6 个导出均有 quality_flags 形参

⇒ 新锁 p1phot_fixgates 的注释把自己（库内私有枚举）说成公共头的既有合同，并声明了一个**不成立的编译期证明**（该文件实际断言只有两条宏值 CHECK）。属 R-6c「注释被当权威引用而不回写」。

### V6-N-04（P1·簇 10 证据不可复现）冻结权威与 oracle 锚被搬到 gitignored 的 run/

    $ git --no-optional-locks log -S 'PHOTOMETRY_LITERATURE_REVIEW' ccc7a933..HEAD -- docs/science/CONTROL_WEIGHT_SNR.md
      b0353303 science(P5): SNR 改为正确的逐源最优提取模型（含 S1–S10 冻结文档订正，负责人授权 §1.2）
    $ git --no-optional-locks show ccc7a933:docs/science/CONTROL_WEIGHT_SNR.md | grep -c PHOTOMETRY_LITERATURE_REVIEW
      0                                                    ← 该引用系本轮新写入
    $ sed -n '51p' docs/science/CONTROL_WEIGHT_SNR.md
      - 推导与文献锚见 `run/release-rescue/science-phot/PHOTOMETRY_LITERATURE_REVIEW.md` §C.3.1
    $ git --no-optional-locks ls-files run/       → run/.gitkeep（唯一被跟踪项；.gitignore:19 为 run/*）
    $ ls run/perf-fix/P5-snr/harness/snr_oracle.py …   → 磁盘在、仓外无 ⇒ 干净 checkout 不可复现

量化（逐文件 added-line 扫描 `run/[A-Za-z0-9_./-]+`）：**50 处新引用 / 14 个被改文件**，含：P4 割线迭代常数的「实测依据」（ipv_select.h:74、ipv_types.h:223、ipv_select.cpp:360）、P5/P8 两个新锁的 oracle 与真实源 fixture（snr_frame_science.cpp:11、p1snr_science_test.cpp:7、tests/unit/p1snr/p1snr_linux_test.cpp:12-14/121、lib/snr_estimator/README.md:234、tests/p1noise/CMakeLists.txt:101）、P1 gaia REPORT（gaia_client.c:2053）、以及 CHANGELOG/SCI 文档。判据不是「与旧文档不一致」，而是 R-6b「锚必须有宿主」：新 FROZEN SCI 的推导宿主在仓外。

### V6-N-05（P1·R-6a 自报量为反）README 与同一提交的 diff 互相否定

    $ git --no-optional-locks show --stat --format= 35c85f53 | grep module_adapters
      lib/core/src/module_adapters.cpp          | 162 +++++++++++-
    $ sed -n '54,58p' lib/phase1/noise/README.md      ← 这 4 行由 35c85f53 自己新增
      lib/core/src/module_adapters.cpp（P7 独占）。本批**未改仓库树中的 lib/core**，
      改动以补丁交付：run/perf-fix/P8-snr-linux/patches/out-of-scope/lib-core-snr-wiring.patch
      …bind-mount 这些副本完成（仓库文件逐字节未变，见 REPORT §6）。

⇒ 同一提交既把 lib/core 改了 162 行入库、又在同提交改写的 README 里声明「未改仓库树 / 逐字节未变 / 以补丁交付」。HEAD 至今未订正（:54/:58 仍在）。补丁文件本身也确实存在于 run/（`lib-core-snr-wiring.patch`、`module_adapters.cpp.patched`），说明这是**流程中途改变而文案没回改**。

### V6-N-06（P1·簇 10 ID 无宿主，L28e-E-001 机制复发）

    $ for id in EVID-P1-PSF-001 EVID-P1-STAR-001; do echo "$id refs=$(git --no-optional-locks grep -c -e $id HEAD -- docs ci tools lib tests | awk -F: '{s+=$3} END{print s+0}')"; done
      EVID-P1-PSF-001  refs=1
      EVID-P1-STAR-001 refs=1     ← 唯一命中就是它自己所在的那一行 CSV
    对照：TEST-P1-PSF-001 refs=22 / TEST-P1-STAR-001 refs=14（既有占位，文档明写「由 P1-*-INT 对齐后更新本行」）

c3452d48 在两行里把 evidence_status 由 MISSING 抬成 VERIFIED，凭据是**全仓无宿主的自铸 ID**。
**通过项（明确不报）**：本轮被改的 registry 页与 module.yaml（docs/modules/registry/astrocs.phase1.photometry.md、lib/dynamic_psf/module.yaml）的 path::symbol 锚我逐条解析 → **0 断链**；同法扫 173 个锚，与基线相比**新增破损 0 条**（存量 156 处系他因，不在本层范围）。

### V6-N-07（P2·簇 10 锚漂移在门盲区）PUBLIC_API.md 九个 snr 行锚 9/9 漂移

    $ python3 (把门的 C3/C4 判据套到作用域外的 docs/contracts/*.md)
    symbol                             docAnchor actual delta
    snr_noise_model_v1                 :143     153     +10
    snr_noise_model_v1_f64             :151     161     +10
    snr_noise_model_v1_default_config  :115     125     +10
    snr_noise_model_v1_fill            :162     172     +10
    snr_noise_model_v1_free            :167     177     +10
    snr_noise_scale_law                :173     183     +10
    snr_noise_gain_variance            :178     188     +10
    snr_estimate                       :200     280     +80
    snr_estimate_f64                   :215     295     +80
    DRIFTED: 9 / 9
    $ python3 -c "print(json.load(open('docs/algorithms/anchors/anchor_contract.json'))['doc_globs'])"
    ['docs/science/*.md', 'docs/algorithms/*.md']      ← docs/contracts/** 不在扫描范围

基线对照：ccc7a933 时同 9 锚偏移为 +5（小漂移已存在）；b0353303 在 snr_estimator.h 的 :39 与 :185 各插 5/68 行后扩大到 +10/+80，而**同一批 b0353303 恰恰改了 PUBLIC_API.md（6 行）**却没同步这 9 个锚。因作用域不含 contracts，无机器门可发现。同法另扫出 out-of-scope 侧 5 处 C4 类不符：DATA_ARTIFACTS.md:94、DATA_SEMANTICS.md:150/:154/:310、PUBLIC_API.md:779（后三处指向被 dce8abd4/35c85f53/dbaa2e18 大幅改动的 module_adapters.cpp 区域）。

### V6-N-08（P2·簇 11 落盘与孤儿 + 反向钉死）新工具 tools/quality/frame_qc_grid.py（80c32b19，1093 行）

    (a) 孤儿：$ git --no-optional-locks grep -ln frame_qc_grid -- .  → 仅命中它自己；
        121 项 ci/checks.json 的 command 中零命中；无 registry/文档引用 ⇒ --selftest 也无机器载体
    (b) 落盘：$ grep -n 'add_argument("--outdir"' tools/quality/frame_qc_grid.py
        :1029  default="."   （另 :777 os.makedirs(outdir)、:884 open(path,"a") 累积写 frame_qc_grid_summary.csv）
        docstring 的用法示例一律显式 --outdir /tmp/qc，默认值却是 "." ⇒ 从仓库根直接调用即在**项目根**产出 PNG/JSON/CSV，
        与 AGENTS.md 目录规范「禁止将运行产物产出到项目根目录」冲突。80c32b19 自身 --stat 仅 1 文件，未新增根产物；
        但当前工作树根目录有未跟踪、且未被 .gitignore 命中的 p8-files.patch（P8 流程落盘违规，与本提交无关）。
    (c) 反向钉死退休口径：$ grep -n '1.4826' tools/quality/frame_qc_grid.py → :35/:224/:500/:592（4 位）
        而 docs/science/NOISE_MODEL.md:46/:135 冻结为 1.482602218505602，同一轮 c3452d48 刚以 M3b-H-01 把 sdet 的 1.4826 判为缺陷并订正；
        新代码把已退休的旧口径写进**机器可读指标**（tile_metrics.mad）。我未据此判科学错（4 位与 15 位差 5.6e-12，不改结论），判的是 §(a) 专查项。
    (d) 边界校验一半：--percentiles 有 try/except→rc=2，但 --grid/--tile 无正数校验（--grid 0 → canvas 0×0 → fromarray 抛 → rc=3；--tile 0 → 空切片 np.percentile 抛 → rc=3）。非静默坏图，轻。
    (e) 自报量结构成立：--selftest 的 "9/9 synthetic stars detected and 9/9 catalog entries re-projected onto them"
        由 assert 从 s["tiles"]/overlay_matched_total 实算（:997-1011），非硬编码 ⇒ 结构可复算（是否真绿属 §4）。

### V6-N-09（P3·新代码卫生）P7（dbaa2e18）

    $ grep -n '#include <mutex>' lib/core/src/module_adapters.cpp → 105: 与 108:（:108 系本提交新增，同文件重复包含）
    lib/core/src/scheduler.cpp:308  set_dispatch_budget_hint(0)  ← 复位非 RAII；
      :247-248 heavy_gate（std::unique_lock）或 :292-306 取消收尾若抛出即不复位，而行内注释自述「异常路径亦到达」
      仅对 :272-290 那层 try/catch 成立 ⇒ 注释夸界 + 池化线程 TLS 残留风险（是否真漏复位需运行期，§4）。

**明确不报为缺陷（防误伤）**：同提交用 ScopedOmpWorkerInjection 的 omp_set_num_threads 取代 ac_set_num_threads —— 我核 lib/calibration/src/ac_api.cpp:128-134，后者除 omp_set_num_threads(n) 外无附加状态 ⇒ 语义等价；ac_set_num_threads 仍在 lib/calibration/src/module_entry.cpp:949 被使用，未成死码。

### V6-N-10（P2·治理）有修无账 / 修复无锁

    $ python3 (ccc7a933..HEAD 的 fix*/perf*/science*/tools* 提交逐个查账本引用)
      35c85f53 ref=0   8dc0220e ref=0   b858b76d ref=0   80c32b19 ref=0   b0353303 ref=0
      23a7f665 ref=0   6d74046d ref=0   bd4bc23b ref=0   c1959436 ref=0
      c3452d48 ref=7   dce8abd4 ref=6   adaeb531 ref=5   07eb229b ref=2

账本 20 条的 fix_commit 全集只有 4 个 RQS 批次提交，与本轮 9 个真改代码的 fix/perf/science 提交**完全不相交**。其中改生产代码且**零测试文件**：bd4bc23b（3 文件/0 测试）、dbaa2e18（4 文件/0 测试）、80c32b19（1/0）；有测试但锁未登记：8dc0220e、b858b76d（正是 C3 的最后两个）；有测试且已登记：6d74046d→ipv_mag_iter ✓、c1959436→mon002_gate ✓。

### V6-N-11（P2）新失败码 -14 无任何文档面

    $ grep -n 'return -14' lib/healpix_db/healpix_drizzle/hp_drizzle_api.cpp
      :997   （precision_mode==1 && !data_is_f64）
    三级复核 docs 命中：L1 '-14' 全句 → 0；L2 放宽"精度不一致/precision_mode 失败码" → 0；
      L3 定点读 PUBLIC_API.md / DRIZZLE_GEOMETRY.md 返回码表 → 仅 -1..-13 在册

⇒ 新码只存在于代码，属「改代码不同步接口文档」。**未决**：dce8abd4 亦使 -12/-13 的行号从 1117/1139 移到 1127/1149；落在 docs/algorithms/** 锚作用域内的部分应由 DOC-LINE-ANCHORS 复测，本层不判其红（需 CI 实跑，§4）。

### V6-N-12（P1·五处同源缺「接口」）四个新科学导出未进接口合同

    $ grep -c '^SNR_API' lib/snr_estimator/cpp/include/snr_estimator.h → 21
    $ grep -n '本节只冻结 noise 路径 9 个导出符号' docs/contracts/PUBLIC_API.md → 命中（合同仍声明 9 个）
    三级复核 4 个新导出（snr_source_snr_f64 / snr_moffat4_profile_f64 / snr_frame_depth_f64 / snr_calib_zero_point_standard_error）：
      L1 全仓精确 grep → 只命中 lib/（header/impl/模块 README/测试），docs 零命中
      L2 放宽 snr_frame_depth* / *zero_point_standard* -- docs → 0 命中
      L3 定点读 PUBLIC_API.md:351-400 与 API_REFERENCE.md 的 snr 行 → 均未列（后者列举的 21 个 snr_* 串中无这 4 个）

⇒ 新公共 C ABI（连 SnrSourceParams / SnrSourceResult 两个新 struct）只落「代码+测试+模块 README」，接口合同层零登记。AGENTS.md「科学定义=算法=接口=代码=测试」缺一处。

---

## 3. 反向钉死与同源缺口

**专查项 (a)：旧的错误表述是否被重新钉成权威 —— 命中 2 处**

1. frame_qc_grid.py 重钉 4 位 1.4826（V6-N-08c）：同一轮 c3452d48 刚以 M3b-H-01 判该口径为缺陷并订正为冻结 15 位，其后的新工具又写回旧口径并让它进机器可读指标。
2. MATRIX 的 **CSV 视图被当作权威来改**（V6-N-02 + V6-N-06）：同批 notes 自己声明「现行权威 = TRACEABILITY_MATRIX.json」，而 star/star-psf 两行订正只落在 CSV ⇒ 把非权威视图钉成权威，效果是「文档改了、权威没改、重跑生成器即蒸发」，并用自铸 EVID ID 把 evidence 抬成 VERIFIED。

**专查项 (b)：新表述是否只改文档 / 只改代码 —— 逐条给结论，含我自己的独立计算**

| 新口径 | 我的独立复算 | 判定 |
|---|---|---|
| kcorr 两段各自归一化分段线性（dce8abd4） | 自行重建旧式与新式，六格（pixfrac {0.5,0.8,1.0} × scale {300,600}）重算：新式角点逐位 = 表值 {1.2112,1.3925,1.4980 / 2.3958,2.8971,3.2035}；旧式在 pf=0.8 偏差 1.515260%(sc=300)、2.115219%(sc=600) | **方向正确**（非均匀 pixfrac 网格不应按均匀网格归一），与自报 +1.515%/+2.115% 相符；不属「与旧文档不一致即错」 |
| 真 MAD + 1.482602218505602（c3452d48） | python: `1/(sqrt(2)*erfinv(2*0.75-1)) = 1.482602218505602`（与代码逐位同）；定义 `median(|res − median(res)|)` 与 NOISE_MODEL.md:17/:46/:135 一致；注释引用的锚行真实存在 | **成立**。遗留：reject_star 门限 `0.2` 至 HEAD 仍无出处登记（note 已自陈「待登记」，属 §3 同源缺口） |
| 逐源 Horne 最优提取 SNR（b0353303/35c85f53） | 手核代码内公式：离散归一化 Moffat4 `P=I/ΣI`、`σ_i²=σ_sky²+max(F·P_i,0)/gain+(RN/gain)²`、`Var(F)=1/Σ(P_i²/σ_i²)`、`f_in(r)=1−(1+r²/(2σ²))^{-3}`、`kMoffat4FwhmFactor=1.230310`；`kTrimMeanToSigma` 与 noise_model.cpp:37、PSF.md:81/:119 同源 | **未判科学错**（我给不出反例）。缺的是接口合同登记（V6-N-12）与仓内可复现 oracle（V6-N-04） |
| 资源门 85/60 恢复（adaeb531） | cli/resource_gate.h：kCpuMeanMinPercent=85.0、kMon001UtilSampleMinPercent=85.0、kMon001QueueUtilMinPercent=60.0，与宪章 §18.2 负责人裁决逐字对齐；tests/unit/p2_workers_test.cpp 4/4b/4c/4d 有真断言 | **成立**（值对；实测是否达标属 §4） |
| drizzle precision_mode 一致性（dce8abd4） | lib/core/src/module_adapters.cpp::p1_op_drizzle 选 AIO_BLOCK_FLOAT64 + f32→f64（aio_get_dtype(im.p)==1）+ 不一致即 ErrorDomain::DATA 失败 | **成立**，但新 -14 未同步接口文档（V6-N-11） |
| gaia 缓存预算常量（bd4bc23b） | 独立重算 QUERY_CACHE_MAX_BYTES = 64×200000×3×8 = 307,200,000 B；BLOCK_CACHE_MAX_MEMORY = 4GiB；两者在 gaia_client.h:3 头注释与 docs/algorithms/GAIA_QUERY.md / docs/architecture/CACHE_POLICY.md 的交叉一致性 —— **未判**（文档侧我未逐句比对，属簇 8/科学层） | 未决（见 §4-6） |

**通过项（明确不报，防误伤）**：
· `gate2_psf_oracle.py` 把已收敛项改名 `centroid_p95_le_0.01px_converged_not_acceptance` 并保留 blocker 文案 —— 这是**正确**的防「以收敛值冒充验收」写法；
· 隔壁未改 photometric_calib.h 的公共 ABI（ABI 回退真实，diff 为空）；
· registry/module.yaml 锚 0 断链（V6-N-06 通过项）。

---

## 4. 需运行期或未决（不强行定级）

1. **p1psf_prodpath_centroid 是否红**：静态推演 T1 用检测约定 CX+0.5/CY+0.5 与 `constexpr double kSciCentroidAbsPx = 0.3;` 矛盾（0.5 > 0.3 ⇒ 必红），即该锁把未闭阻塞钉成失败用例；但 PASS/FAIL 只在运行期可知 → **需运行期，未判**。
2. **12 个未登记 ctest 目标的完整红灯集合**：C3 判据我已用仓库自带判定器在内存内复算为红（等价于脚本主体），但完整脚本另有与 ctest 实列表比对的 S1–S6 场景 → 精确红灯集需 CI 实跑。
3. **frame_qc_grid.py --selftest 是否真绿**：其计数由 assert 实算（结构成立，V6-N-08e），执行结果需运行期。
4. **CON-COMMENTS / CON-DOC-SYMBOLS 是否抓到 V6-N-03 两条不实注释**：静态只确证 DOC-LINE-ANCHORS 作用域不含 contracts（故 V6-N-07 必不被抓）；CON-* 系需实跑 → **需运行期，未判**。
5. **§18.2 85%/60% 实测达标**、以及 P7 TLS 是否真的漏复位（V6-N-09）→ 需运行期。
6. **gaia 缓存预算与 GAIA_QUERY.md / CACHE_POLICY.md 的跨文档一致性**（V6-N-11 同族但不同域）→ 本层未逐句比对，交簇 8。
7. **工作树当前有 6 处未提交的生产改动**（CMakeLists.txt、include/astrocs/core/module.h、lib/core/{module_adapters,runtime}.cpp、lib/phase1/noise/{noise_model,snr_frame_science}.cpp）与根目录未跟踪 p8-files.patch → 归属与去向属前台提交纪律，本层只登记事实，不判。

---

## 5. 一句话总判

20 条账本**本身不失真**（SHA 20/20、覆盖 19/20、锁名 3 处不可解析、fix_note 20/20 一致、两条 PARTIAL 残余均属实），隔壁**未越权改判定列**（判定列大改全在 9777193d，系前台生成器自修）。真正的缺陷集中在修复动作的**外围同源面**：三道非豁免门被本批改红（CON-TRACE / MATRIX / CTEST-REG）、科学与 oracle 权威被搬到 gitignored 的 run/、接口合同（PUBLIC_API 9 锚 + 4 新导出）与生成器权威（JSON）未随代码同步，另有两处注释自述不存在的 ABI / 自报量与同提交 diff 相反，以及一处「同轮刚退休的旧常数被新代码重新钉成口径」。
