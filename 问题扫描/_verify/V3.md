# V3 · Phase2/3 六项 + kcorr + ivar + 坐标序 复核档案

- 复核对象：`dce8abd4 fix(RQS/B3): Phase2/3 六项 —— 坐标序、精度一致性、kcorr 网格、零锚、ivar fail-closed、UPM grid`（对应账本 6 条 FIXED：M2a-C-1 / M2a-H-1 / M4-C-01 / M4-C-02 / M4-C-03 / M7-C-001）
- 复核层：V3（独立静态验证；判定列/verified 列由前台统一填）
- 时点：工作区 HEAD = `dbaa2e18`（git log 2026-09-14 批次顶端）；本档案所引行号均为 **HEAD 时点行号**（dce8abd4 时点行号在有分歧处另行标注；HEAD 与其后 c3452d48/b0353303 等提交造成 ±数行漂移处已注明）。
- 纪律：纯静态；findings/ 与 账本/ 未改动；冻结文档修正按负责人授权（§1.2）口径处理，不以"改了冻结文档"判违规。

## §0 范围与命令

| # | 命令（节选） | 用途 |
|---|---|---|
| C1 | `git --no-optional-locks show --stat dce8abd4` | 20 文件 / +527-69 清单 |
| C2 | `python3 csv.DictReader(问题扫描/账本/FIX_LEDGER.csv)` 过滤 fix_commit=dce8abd4 与 M2a-C-1/M2a-H-1/M4-C-01..03/M7-C-001 | 六条账本原文+处置结构化解析（未用行文本解析） |
| C3 | `git --no-optional-locks show dce8abd4 -- <逐文件>` | 六项 diff 逐字核对 |
| C4 | `grep match_idx 全仓`（72 命中，排除 ./问题扫描 ./docs 后逐条归类） | 载荷路径覆盖性三级复核 |
| C5 | `grep -n "aio_get_dtype\|data_is_f64\|use_f64\|precision_mode" hp_drizzle_api.cpp / astro_image_io` | 精度链语义核对（含 dtype=1==FP64 反向风险排除） |
| C6 | `git --no-optional-locks log -L 1525,1545:drizzle_engine.cpp` | variance 计数区归因（86777131 [B4-21]，非 dce8abd4） |
| C7 | `grep -rn "P2_INTEGRATE_INVALID_INPUT" lib/phase2/src/integrate.cpp; sed 40-80` | ivar NaN/0/负权重的唯一内核语义 |
| C8 | `python3 tools/check_data_artifacts.py 逻辑阅读（100 行全读）` | DATA-ARTIFACTS 门能力面（前台 V3-Q3） |
| C9 | `sed -n '224,245p' docs/contracts/PUBLIC_API.md` | 帧通道返回码清单（-14 缺席实证） |
| C10 | `grep -rn "零锚\|zero_anchor" docs/science/PHASE2_UPM.md; sed 128,138 / 40,48` | SCI 冻结面未被反向篡改核对（0 命中假象经三级复核排除：首跑 grep 输出丢失，重跑命中 :58/:133/:138/:141） |

## §1 逐条结论

### 1.1 坐标序 n_coords —— M2a-C-1：改的是同一件事，判 **修复属实**（VERIFIED 候选）
- 账本缺陷 = DLL 通道 b64 载荷按 matched_count 截断（审计锚 module_entry.c:732/:876）。本提交 :777（容量）与 :936（编码）两处同改 `c.n_coords`，:794 头 JSON 增 `"n_coords"`；合同 DATA_SEMANTICS.md:113 补"长度=n_coords"。
- **载荷路径覆盖**（任务指令特别核）：全仓 `match_idx` 仅由 by_coords 分支产生（module_entry.c:716-724；gaia_client.c:2486 `calloc(n_coords)`、:2503/:2506 坐标序回填）；其余 op 恒 NULL、由 :933 `if (match_idx)` 跳过；`b64_idx_cap`（容量申报）与 `b64_encode`（实写）成对同步，无第二处 count 口径残留 → **无漏改路径**。n_coords≥1 由 cfg 校验保证（module_entry.c:235/:321，ra/dec 长度相等），编码不越分配。
- 回归锁 D6（gaia_adapter_test.c:648-727）：部分未命中 {0,1,-1,-1}，断言长度=NC*4、与直连 bitwise、JSON n_coords/count 双字段。✓ 名实相符。
- 弱点（登记，不判缺陷）：D5 :625 `wantb[2]=dn*sizeof(int)` 仅因全命中（:607 dn==NC）与合同巧合一致，本身不具鉴别力；C9（gaia_integration_test.c:726）只断键存在。

### 1.2 precision_mode —— M2a-H-1：**主体已修（引擎累加域逐字核实）**；两残余一同步缺口
- 库边界：hp_drizzle_api.cpp:992-997 `pm==1 && !data_is_f64 → -14`，位于 `run_drizzle_internal`（:396）——`hp_drizzle_run`（:1244 薄壳 :1255 委托）与 `hp_drizzle_run_hips` 共享唯一实现体（:1222-1223 注释自证），plugin 面同受覆盖。
- 装配面：module_adapters.cpp:2701-2733 pm=1 时 `aio_get_dtype(im.p)==1` → `aio_get_pixel_data_f64`，否则 f32→double 上采样，块型 FLOAT64 + add 后自检。已排雷：**aio dtype 语义 0=FP32,1=FP64**（astro_image_io.h:110-112 注释 + aio_fits.cpp:616/:877 赋 1=FP64），分支方向**未反**。
- **引擎累加变量是否 double（前台转问 V6，逐字）**：`drizzle_engine.h:65-69 struct TileLeafAccumulatorT { Scalar sumFlux; Scalar sumArea; Scalar sumVarNum; uint32_t nContrib; }`；f64 生产路由 hp_drizzle_api.cpp:1043-1047 `img.use_f64 → engine.drizzleTiled_f64` → drizzle_engine.cpp:926-929 注释+实例化 `std::vector<TileAccumulatorT<double>> tiles; drizzleTiledImpl<double>(...)`；核心循环 :1508 `Scalar weight = overlap_area / drop_area`、:1529 `acc.sumFlux += Scalar(pixelValue * weight)`、:1536 `acc.sumVarNum += Scalar((double)varianceValue*(double)w2)`。**结论：Scalar=double 实例化下三个累加变量即 double，累加域真修，"适配器层拒绝到位、累加域未修"的担忧不成立**。限定语：variance **输入面**仍 `const float* varianceData`（:1559 区签名；方差图本身 f32 供数），属关联项 L04-019（账本关联已注明"同一根因的另一半"），不并入本条。
- 测试语义倒置正当：旧 T7 `max_rel<1e-5` 把缺陷（FP64 请求实际 binary32 累加）钉为期望；新 T7 (0,1e-3)+T9（`(double)(float)s[i]!=s[i]` 计数>0）——f32 量化值转 double 恒等，计数必 0，签名可区分，非为过测试而调。
- 残余①（fix_note 自认）：**pm=0+FLOAT64 反向面 = 静默升级而非元数据虚假**（转问答复：writer `add_tile_f64` hiss_writer.cpp:774-777 强制 `metadata.precision_mode=1; signal_dtype=1`，故产物声明与实算一致，但 config.precision_mode=0 请求被静默忽略、无拒无警）。
- 残余②（新）：-14 未登记 PUBLIC_API.md:233-239（逐码有锚的清单）；plugin `drz_legacy_status`（lib/drizzle/src/module_entry.cpp:862-866）把 -14 落入默认域 SCIENCE_PRECONDITION，DATA 语义丢失。

### 1.3 kcorr 网格 —— M4-C-01：改的是同一件事、**无值篡改、独立验算吻合**（VERIFIED 候选）
- 任务书口径纠偏：本提交处置 M4-C-01（非均匀 pixfrac 网格按均匀取列），**非**"两档 300/600 反保守"（那是 M7-A-112，账本 OPEN，本提交未动，`git show --stat` 无该文件面）。
- 独立重算（逐字引 sampler.cpp 旧式）：旧 `wi=(0.8-0.5)/(1.0-0.5)=0.6; i0=1.2; c0=1,c1=2,fx=0.2` → 300 档 1.3925+(1.4980-1.3925)·0.2=1.41360（+1.515%），600 档 2.8971+(3.2035-2.8971)·0.2=2.95838（+2.115%），与提交注吻合。**表值 :91-93 与默认 1.4（:82）零改动**——新口径不是"调值过门"。
- 新式 :99-108 两段各自归一：pf=0.8 → lo 段 fx=1 → w0=0 → 命中表列 bitwise；pf=0.5/1.0、sc=300/600 同法端点 exact（`x*0.0+y*1.0==y` 对正有限 double 成立）。生产默认 pixfrac=0.8 从此返回**冻结表值本身**——科学方向正确（旧行为产出表外值）。
- 锁：kcorr_lookup_test.cpp（corner EXPECT_EQ、中点独立算式 rtol 1e-12、clamp）经 :1161-1163 extern"C" 接缝驱动生产 TU 同一实现；CMake phase2_sampler target + TEST_PREFIX 注册与账本 regression 名一致；接缝符号不在任何 .dll 导出面（全仓 grep 仅 sampler.cpp+test）。
- 遗留（账本有主）：PHASE2_SAMPLER.md:189-191 与 sampler.cpp:554 注释仍称"scale 未知→300" 档**保守**"——独立推导：300 档 k(1.2112/1.3925/1.4980) < 600 档 k(2.3958/…)（:92-93 表），var=k·(π/2)σ²/N ⇒ 300 档 variance 更小 ⇒ ivar 更大 ⇒ **反保守**。此即 M7-A-112 本体，OPEN；sampler.cpp:86 内部注释"双线性插值"与新文档措辞并存（语义可容）。M9-A-2（clamp 静默饱和域）OPEN。**本提交未把它们当权威改回去，也未顺手订正——合规留单**。

### 1.4 零锚 λ0 —— M4-C-02：三入口同源达成；p2001 面有隔离裁量；收敛性留观察
- 机制核对：`P2UpmBuildConfig uc{}` 零初始化 → λ0=0，而 upm.cpp:244 只修 `<0`——账本"声明 1e-3 实际 0"成立；修复在两装配点显式 `zero_anchor_weight=1e-3`（p2_session.cpp:191；module_adapters.cpp:3496）+ 显式 config 覆盖面（:3509-3510）；stage2 链 header 默认 1e-3（stage2_common.h:41）+ JSON 默认 1e-3（stage2_common.cpp:152-153）+ 透传（:455）。三入口同值 ✓。
- SCI 反向篡改检查（任务红线）：docs/science/PHASE2_UPM.md **不在本提交文件清单**；:58/:133 仍 `zero_anchor_weight=0.001`、:133 "弱 Tikhonov/岭型锚定向全局零"——代码向冻结文档对齐，非文档向旧代码对齐。PHASE2_UPM_IMPL.md:361-365 仅订正"同值生产装配"失实句（授权面内的文字订正）。
- 锁：P2EntryParity.zero_anchor_default——stage2 透传 EXPECT_DOUBLE_EQ(1e-3) + λ0=0 vs 1e-3 的 model_hash 必异（λ0 确入 model hash payload，upm.cpp:892 `fmt(cfg.zero_anchor_weight)+"|"` 实证）✓。
- p2001_real_nodes_test.cpp:390-395 转用 `"upm":{"zero_anchor_weight":0.0}` 隔离接线：fixture 注释自陈 λ0=1e-3 于 126obs/128 槽近秩亏面达 max_iterations（objective 4235）。核对内核语义：upm.cpp:602 循环耗尽 max_iterations 后**不作失败返回**（:927 一带 return 0，objective 如实入档）——该行为系旧有，非本提交引入；其科学危害性（未收敛模型被持久化/使用）需运行期量化，fix_note 已建议另立 finding。**p2001 面副作用登记**：唯一走真实节点链的用例从此不再验证生产缺省 λ0——覆盖面净减一（有注释理由，判"可容留痕"，非红线）。
- 转前台注意：M3-C-010（**zero_point** 字段声明/结构体/截断三源，photometry 域）与本轮"零锚"**不是同一件事**；M3-C-010 账本仍 OPEN、dce8abd4 未处置；HEAD 实证 PHOTOMETRIC_FIT.md:9 已由 **b0353303（P5 批，负责人授权）** 以"删除无定义式的 zero_point 声明"订正（行内 HTML 注释自证）——文档-结构体两源已闭合，aio 截断锚（audit 时点 libaio:372-380）HEAD 已是 source_path 访问器，锚位漂移。前台应核对该条是否该转 FIXED（由 P5 批关账）而非挂 dce8abd4。

### 1.5 ivar fail-closed —— M4-C-03：与节点链归一达成；无"无 ivar→0 权重/NaN/静默跳"新路径
- 判据链（HEAD stage2.cpp）：
  - 产品缺失（`ivr[f]==nullptr`）：开档即 gate（:566-578，缺省 legacy=false → **rc=7**；true → 放行+日志标红）——该 gate 系 1752e8ca 旧有；
  - **tile 读失败（本提交新增分支）**：`ivar_valid[s]=0`（:1259）且 `ivr[frames[orig]]!=nullptr` → 并行 fail=2→join 后 rc=7（:1114-1118/:1323-1327，cpu_fail 为 std::atomic<int> :1292，归因唯一——:1117 是全文件唯一 `fail = 2` 写入点）/ 串行 :1381-1390 直接 rc=7；legacy=true 时 `++ivar_tile_fallback_px` 逐像素计数并入 diagnostics（:1752-1753）。
  - "无 ivar 当 0 权重 / NaN"：唯一消费内核 integrate.cpp:52-58——非有限权重→INVALID_INPUT、负权重→INVALID_INPUT、w==0→合法零贡献（ZERO_VALID_WEIGHT 语义）；NaN ivar 不会静默成 0 也不会污染输出（:1546-1560 非 OK 像素 valid=0/flux=area=0，:1604 同构）。**未发现"把无 ivar 当 0/NaN/静默跳过"的路径**。
- drizzle_engine 历史项核（任务指令点 2）：当前 :1529-1537 累加 `acc.sumFlux/sumArea` 无条件、`acc.nContrib++` 无条件，variance 仅 `varianceValue>0.0f` 有界加项；`continue` 前置版本早于本提交已由 **86777131（B4-21）** 改掉（git log -L 归因），**dce8abd4 未触碰 engine**，也无"把正确行为改回错误行为"。残余观察（旧有，非新增）：NaN varianceValue 在 :1533 被静默当作无方差贡献（不告警不计入 sumVarNum），属 L04-019 方差面家族。
- 锁：Phase2IvarWiring.IvarTileMissingFailClosed——真实破坏 B 帧 variance/ivar tile 文件（dataset/properties 保留 → 打开成功、读失败），断言默认 rc=7、legacy 放行 rc=0 且 diagnostics 含 `ivar_tile_read_fallback_pixels`+`legacy_allow_weight_fallback: true`；经 `std::system` 端到端跑 stage2 二进制（**测试本体属运行期证据，静态仅核其断言逻辑与夹具构造成立性**）。
- 文档面：MOSAIC_WRITE.md:143/:281/:322 现文本与 HEAD 代码行为一致（"像素级缺 ivar 仅显式 fallback 可达并计数"修复后才为真）；其锚 :1372-1375 因本提交插入 +7 行而漂移（现回落点 :1391），**轻锚漂**；KNOWN_LIMITATIONS.md:13 / TROUBLESHOOTING"接受 support 回退"句已过时——**已有主 M6b-C-001（OPEN）**覆盖该文档面，不另立。
- 宪章 §6.3 红线（support/coverage 入权重）：本提交**收紧**了 support 冒充 ivar 的唯一静默路径，未引入新权重语义；红线不触发。

### 1.6 UPM grid —— M7-C-001：rc=3 门+hash+持久化闭环达成；两处按裁决单未落地
- 门：upm.cpp:245 `if (cfg.grid != 8) return 3;` 位于 `build_impl`（:212），`p2_upm_build`（:932）与 `p2_upm_build_geo`（:938）唯一实现体双覆盖；:259 `m->grid = cfg.grid`（已校验==8）；:951 save 写 `j["grid"]`；:1061-1065 open 读回并 `!=8 → rc=1`（老文件无键 default 8 与历史恒 8 行为一致，非静默错格架）；:1354 `p2_upm_geometry_hash` payload `"grid="+std::to_string(m->grid)+";"` ——建议处置④（实际 G 入几何 hash）✓。
- 采样器贯通：stage2_common.cpp:466 `mcfg.grid = cfg.control_grid_per_tile`（config 界 1..64 :40-44）；p2_session.cpp:191 `uc.grid = sc.control_grid_per_tile`；module_adapters.cpp:3402 采样 artifact 增写 `control_grid_per_tile`、:3496 upm-fit 读回 `value("control_grid_per_tile", 8)`。
- **缺口语义（登记）**：artifact 缺键时 default-8 继续——若采样器由**旧版**（不写该键）产出且实际 G≠8，upm-fit 会静默按 8 拟合，正是 M7-C-001 要禁的"静默错位"在**跨版本读取面**残留一个洞；现行 DATA-P2-SMP artifact 无"键必存在"schema 门。判 PARTIAL 候选因素之一（前台定）。
- 建议处置①③已落地（①加显式 rc、③负面回归 ControlGridMismatchRejected：bad.grid=4→build≠0+model==nullptr、ok 默认 8→成功+几何 hash sha256 长 64）；②「PHASE2_SAMPLER.md §9 禁改清单补 512 可整除前置」**未做**（本提交未改 §9；采样器仍可配 3 等非整除值——生产面因 UPM rc=3 兜底不出科学污染，但采样器本身仍可产非整除网格 artifact）；④已落地。
- SCI §5（A-21b 裁决挂靠面）：docs/science/PHASE2_UPM.md 不在本提交文件集；:43 `C_f(p) = 双线性(8×8 control cell, θ_f)` 与 :126 "8×8 双线性网格" 原文未动——**无单方面改动裁决面**。G==8 强约束恰以该冻结文本为依据（宪章 §6.3/§12.3-1 同向）。
- 锁名相符：账本 regression `phase2_synthetic_gate.Phase2Upm.ControlGridMismatchRejected` ✓。

## §2 新发现缺陷（本提交引入或直接相关）

| 候选 | 内容 | 级别建议 | 依据 |
|---|---|---|---|
| V3-N-01 | -14 未登记 docs/contracts/PUBLIC_API.md:233-239 帧通道返回码清单（清单为逐码带锚风格，新码即缺口） | P2 | C3/C9 |
| V3-N-02 | plugin drz_legacy_status 将 -14 归默认域 SCIENCE_PRECONDITION，库内 DATA 语义在 DLL 面丢失 | P3 | lib/drizzle/src/module_entry.cpp:862-866 |
| V3-N-03 | upm-fit 读旧版采样 artifact 缺 control_grid_per_tile 键时静默 default 8（跨版本静默面，M7-C-001 ①的镜像） | P2-P3 | module_adapters.cpp:3496 |
| V3-N-04（轻） | MOSAIC_WRITE.md:143/:322 锚 :1372-1375 因 stage2 插入漂移 ~+7 行；DATA_ARTIFACTS.md:29 shape 列未随 DATA_SEMANTICS:113 补 match_idx 长度口径（机器门不查语义，实证见 §4-Q3） | P3/P4 | C3/C8 |

## §3 反向钉死与同源缺口

- **反向钉死检查：未发现**。六项均为"代码/装配向冻结文本靠拢"：SCI 零锚 0.001、8×8 FROZEN 文本、kcorr 表值与 1.4、DATA_SEMANTICS 坐标序合同均未向旧错误口径回改；测试语义倒置仅 T7（旧断言=缺陷本体，判正当，见 §1.2）。
- 同源总表（docs/science ↔ docs/algorithms ↔ 接口/合同 ↔ 实现 ↔ 测试；✗=未同步/漂移）：

| 项 | science | algorithms | 接口 | 实现 | 测试 |
|---|---|---|---|---|---|
| M2a-C-1 | n/a | n/a | docs/contracts/DATA_SEMANTICS.md::§8.2:113 ✓；DATA_ARTIFACTS.md::DATA-GAIA-001:29 **✗（轻）** | lib/gaia_xpsd_client/src/module_entry.c::gaia_execute :777/:794/:936 + gaia_client.c::gaia_client_query_spectrum_by_coords :2486 | tests/unit/gaia_adapter_test.c::D6 ✓（D5 弱、C9 存在性） |
| M2a-H-1 | （SCI-DRZ §9 冻结文本未动） | （ALG-DRZ B2-A12 口径同向） | docs/contracts/PUBLIC_API.md::drizzle 返回码 **✗（-14 缺）** | lib/healpix_db/healpix_drizzle/hp_drizzle_api.cpp::run_drizzle_internal :992 + drizzle_engine.h::TileLeafAccumulatorT<double> + lib/core/src/module_adapters.cpp::p1_op_drizzle :2701-2733 | tests/unit/drizzle_precision_default_test.cpp::T1-T9 ✓ |
| M4-C-01 | docs/science/PHASE2_UPM.md:::94/:100/:142（1.4 冻结，未动）✓ | docs/algorithms/PHASE2_SAMPLER.md::§5.4:189-196 + §11.3 F2:405 ✓（:189"保守"方向词遗留→M7-A-112） | 内部函数（无外部接口）n/a | lib/phase2/src/sampler.cpp::kcorr_lookup :88-109 + 接缝 :1161-1163 | lib/phase2/tests/kcorr_lookup_test.cpp ✓（phase2_sampler.*） |
| M4-C-02 | docs/science/PHASE2_UPM.md::§9a:133 ✓未动 | docs/algorithms/PHASE2_UPM_IMPL.md::§13:361-365 ✓（锚位 HEAD 漂 →§4-Q2） | lib/phase2/include/astro/phase2/upm.h::P2UpmBuildConfig:76 注释 ✓ | p2_session.cpp:191 + module_adapters.cpp:3496 + stage2_common.cpp:152-153/:455 | synthetic_gate.cpp::P2EntryParity.zero_anchor_default ✓；p2001 λ0=0 隔离（留痕） |
| M4-C-03 | （SCI-INT §3/§5 权重单位口径未动）✓ | docs/algorithms/PHASE2_MOSAIC_WRITE.md:::143/:281/:322 ✓（锚 :1372-1375 漂 **✗轻**）；docs/KNOWN_LIMITATIONS.md:13 + TROUBLESHOOTING **✗（旧，已有主 M6b-C-001）** | 配置面 stage2_common.h::legacy_allow_weight_fallback=false ✓（DATA_SEMANTICS:925/PUBLIC_API:1160 已载） | lib/phase2/tools/stage2.cpp:: :566-578/:1114-1118/:1323-1327/:1381-1390/:1752-1753 | lib/phase2/tests/ivar_wiring_test.cpp::IvarTileMissingFailClosed ✓（需运行期执行） |
| M7-C-001 | docs/science/PHASE2_UPM.md::§5:43（8×8）✓未动 | docs/algorithms/PHASE2_UPM_IMPL.md（:30 区引用不变）；**§9 禁改清单补 512 整除句未做 ✗（M7-C-001②）** | lib/phase2/include/astro/phase2/upm.h::P2UpmBuildConfig::grid=8 ✓（rc=3 语义在头注释） | lib/phase2/src/upm.cpp:: :245/:259/:951/:1061-1065/:1354 | synthetic_gate.cpp::Phase2Upm.ControlGridMismatchRejected ✓ |

## §4 需运行期或未决

1. **需运行期，未判**：λ0=1e-3 在 p2001 近秩亏面 IRLS 达 max_iterations 后仍以 rc=0 产出模型（upm.cpp:602→927 静态可读，但"未收敛模型被下游使用是否构成科学危害"须数值实验）——fix_note 已建议另立 finding，前台裁决是否立条。
2. 297/297 ctest 与"六项注入回退必红"为提交自述，V 层不跑 ctest（纪律②），未独立验证。
3. **V3-Q3 答（DATA_ARTIFACTS 门能力）**：`tools/check_data_artifacts.py`（100 行全读）只查两件事——DATA_SEMANTICS 声明的 DATA-* ID 全部登记 + 每行 9 列非空（REQUIRED_COLS 结构性检查），**不比对 shape 文本与代码/§8.2 语义** → :29 的 match_idx 长度缺写**机器门抓不到**，证实前台预判，可并入 L28e-E-002。
4. **V3-Q2 答（pm=0+FLOAT64 可达性）**：CLI 生产链（p1_op_drizzle）**不可达**——pm 必须显式 0|1（module_adapters.cpp:2641-2655），pm=0 恒建 FLOAT32 块；反向组合仅能经 astrocs_drizzle.dll plugin 构造（rows.dtype=f64 + precision_mode=0 或 KV PRECISION=fp32；drz_execute_drizzle:929→同 internal），而 plugin 当前不在 CLI 生产链接闭包（cli/CMakeLists.txt:154 注释），属"typed DAG 启用即达"面；效果=静默升级（writer :774-777 强制声明 FP64，与实算一致），非虚假声明。
5. 锚位漂移订正项（module_adapters.cpp:3152-3176 → HEAD 实际 :3496；MOSAIC_WRITE :1372-1375 → HEAD :1391）系 b0353303/c3452d48 等后续提交的行漂移所致，建议随 DOC-LINE-ANCHORS 刷新批统一处理，不单立。
6. 归批提醒（前台已认，§1.3）：PHASE2_SAMPLER.md:189-191 / sampler.cpp:554 "300 档保守"方向词与 M7-A-112 一并处置。
