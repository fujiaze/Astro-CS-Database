# V8 — 本批新增注释与文档文本的可核验性普查（发现新缺陷层）

> 轴名：V8（对应 `df16f008` 派发的「新增注释可核验性」轴）。
> 方法基线：`git --no-optional-locks diff --unified=0 ccc7a933..HEAD -- lib cli include tools docs` 取**新增行**，只审新增注释 / docstring / README / 合同文本行。
> 判据（L28d 的 R-6a/b/c 升级版，逐条复算）：
>  ① 自报计数（"N 个导出""N 处""全部""逐字节相同"）能否复算 → 复算为假即登记；
>  ② 锚是否有宿主（path 存在、`::` 符号存在、行号在文件内）；
>  ③ 宣称"权威 / 已验证 / 不可变"者是否与代码或合同互斥；
>  ④ "无硬编码 / 全部走参数 / 已同步 CI"类纪律宣称是否就地为假；
>  ⑤ 引用 `run/**` 或未版本化文件作依据者（与 V6-N-04 同族，计数并给该族完整清单）。
> 免重报：已立条（V6-N-03 / V6-N-04 / V6-N-05 / V2-D-9 / V2-D-13 / V2-N-06 等）站点只挂 related，**只报新命中**。

## 0. 范围、时点与命令口径（骨架，待填）

- 被审语料：
- 抽取命令与新增行计数（含口径与时点）：
- 时点漂移表：

## 1. 判据 ① 自报计数复算（骨架，待填）

## 2. 判据 ② 锚宿主（骨架，待填）

## 3. 判据 ③ 权威/已验证/不可变宣称互斥（骨架，待填）

## 4. 判据 ④ 纪律宣称就地真伪（骨架，待填）

## 5. 判据 ⑤ `run/**` 与未版本化依据（该族完整清单，骨架，待填）

## 6. 新立缺陷条目（V8-N-01 …）

## 7. 明确不报为缺陷（防误伤）

## 8. 需运行期，未判

## 9. 一句话总判
