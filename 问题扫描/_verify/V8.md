# V8 · 本批新增注释与文档文本的可核验性普查（从 V6-N-03 / D-9 / D-13 / N-05 外推）

- 轴主：本 subagent（V8）｜派发人：session-9db01ebd（前台）
- 语料口径：git --no-optional-locks diff --unified=0 ccc7a933..HEAD -- lib cli include tools docs，**只取新增行**（^+），只审注释 / docstring / README / 合同文本行。M-V8-01 = 7912 条新增行 / 93 文件（建库时点 HEAD 521095b8，40 提交中 16 提交落在本范围）。
- 正文抽取口径：scripts_v8_extract_comments.py 得 3313 条注释候选，剔除 .json/.csv 数据行后 2146 条散文行（_v8_comment_lines.json）。
- 判据（升级 L28d R-6a/b/c，逐条重算）：① 自报量（"N 个 / N 处 / 全部 / 逐字节"）必须可复算，复算为假即登记；② 锚必须有宿主（路径存在、:: 符号存在、行号在文件内、被引章节名真实存在）；③ "权威 / 已验证 / 负责人裁决"宣称不得与代码或合同互斥；④ 纪律宣称（"零副本 / 全部走参数 / 已同步"）不得就地为假；⑤ 以未版本化文件为权威 —— 计数并给该族完整清单。
- 纪律：纯静态（read/grep/glob/write/edit + 问题扫描/ 内只读 python3 + git --no-optional-locks 查询）。未运行编译器 / ctest / 任何二进制 / ci/run.py / 任何 in-repo 生产脚本；未连 Fatduck；未写 findings/、账本/。
- 只读复算脚本（全部可复跑，均在本目录）：scripts_v8_parse_diff.py、scripts_v8_extract_comments.py、scripts_v8_passA.py、scripts_v8_passB_anchors.py、scripts_v8_passB2.py、scripts_v8_passD_symbols.py、scripts_v8_passC_ci.py、scripts_v8_checkids.py、scripts_v8_runrefs.py、scripts_v8_passE_absolute.py、scripts_v8_passF_owner.py、scripts_v8_snr_sites.py、scripts_v8_p9_frames.py、scripts_v8_p9_dev2.py、scripts_v8_p9_idx.py。
- 免重报核对：已先读 SUMMARY.md 簇表、INDEX.md 三、以及 V1/V2/V4/V5/V6/V12/V15 归档。发现 V12-N-06 已立"6 处注释指向不存在的 docs/algorithms/IPV_PIPELINE.md" —— 本轴独立复算一致，不另立条，只在 七 记交叉印证。

---

## 一、结论一览（新立 8 条 + 1 项计量交付）

| 条目 | 级 | 判据 | 一句话 |
|---|---|---|---|
| V8-N-01 | P1 | ② | 新注释把"防止死代码被清理"的凭据写成不存在的测试符号 ::test_starpsf_precise_inactive_direct_call |
| V8-N-02 | P1 | ①④ | snr_frame_science 自称"零本地副本 / 全部数值调 C ABI"，复算：4 个权威函数只调 3 个，本 TU 仍存 3 处本地算式（含 2.5 dex→mag 的第二 / 第三实现） |
| V8-N-03 | P2 | ① | README 自称"4 处计算"，括号里列 5 个名字，直调点实为 1 处 —— 三种数互相否定 |
| V8-N-04 | P1 | ②⑤ | 证据外置的新形态：15 处裸 REPORT.md 章节锚，仓内可定位宿主 0（连 run/ 里都没有"越域残余"） |
| V8-N-05 | 计量 | ⑤ | 本批新增行内 run/ 直引 17 真引用 / 14 文件（19 命中扣 2 假阳性），完整清单 + 与前台 573/194 口径区分 |
| V8-N-06 | P2 | ② | gaia_client.h:3 的契约锚指向 GAIA_QUERY.md 三个不存在的节名（同句其余 6 项自报量复算全真） |
| V8-N-07 | P1（需确认） | ③ | "负责人裁决 / 裁定 2026-09-14" 新增 39 行援引，三族中两族在权威登记面 0 命中（对照：P5-SNR 授权在 CHANGELOG 有登记） |
| V8-N-08 | P3 | ② | sdet_angle_guard.h:39 契约区间写 (-90, 90]，实现允许输出恰为 -90（可达端点被文本排除） |

---

## 二、逐条证据

### V8-N-01（P1·判据②）死码保留的"防清理凭据"是一条不存在的测试符号

    $ sed -n '1796,1800p' lib/core/src/module_adapters.cpp      # 新增提交 23a7f665
    // 直调测试: tests/unit/p1001_real_nodes_test.cpp::test_starpsf_precise_inactive_
    // direct_call（证明本路径仍可编译且能跑出结果, 防止被当作死代码清理）。

    $ git --no-optional-locks grep -n "precise_inactive" -- .
    （仅 lib/core/src/module_adapters.cpp:1798 自身 + 本轴 _verify dump，无任何测试面命中）
    $ grep -n "test_psf_fast_cap_and_inactive_precise" tests/unit/p1001_real_nodes_test.cpp
    2188:static void test_psf_fast_cap_and_inactive_precise() {
    2486:  test_psf_fast_cap_and_inactive_precise();

- 问题说明：注释给出的 path::symbol 锚，路径在册、符号在仓内零宿主；真实符号是同文件的 test_psf_fast_cap_and_inactive_precise（不是大小写或前缀差异，是另一条断言面）。该锚的唯一功能是给"按负责人裁决保留但不启用"的 inactive PSF 路径提供"有直调测试守着"的理由 —— module_adapters.cpp:6162 同句式："防止 inactive 代码被当作死代码清理（负责人裁决 2026-09-14）"。理由的证据面虚构，等于把"保留死码"变成无凭据决定。
- 判据：②（锚无宿主）。定级 P1 而非 P3：行号漂移动摇的是定位成本，符号整体不存在动摇的是保留决定本身。
- related（不另立）：V2-D1（同一测试真实覆盖面只有 contains 断言）、V12-N-06（同族依据虚构）、V7-N-10（同族杜撰锚 module_entry.cpp:355）。

### V8-N-02（P1·判据①④）「科学公式零本地副本 / 全部数值调 C ABI」复算为假

    $ sed -n '3,8p' lib/phase1/noise/snr_frame_science.cpp     # 35c85f53 建、57c04256 改，均为新增行
    // 科学公式零本地副本: 本 TU 的全部数值都来自 P5-SNR 唯一权威实现
    // lib/snr_estimator/cpp/src/snr_science.cpp:
    //   snr_source_snr_f64 / snr_moffat4_profile_f64 / snr_frame_depth_f64
    //   / snr_calib_zero_point_standard_error

    $ sed -n '27p' lib/phase1/noise/README.md                   # 35c85f53 新增
    | lib/phase1/noise/snr_frame_science.cpp | 聚合实现；**零公式副本**，全部数值调 snr_science.cpp 的 C ABI |

复算：

    $ grep -n "snr_moffat4_profile_f64" lib/phase1/noise/snr_frame_science.cpp lib/phase1/noise/snr_frame_science.h
    snr_frame_science.cpp:6      ← 只有注释自身；本 TU 零调用（全仓调用点在 snr_estimator.cpp:106 与 p1snr_science_test.cpp）
    $ grep -n "snr_source_snr_f64\|snr_frame_depth_f64\|snr_calib_zero_point_standard_error" lib/phase1/noise/snr_frame_science.cpp
    58 / 85 / 190 附近            ← 4 个只调 3 个
    $ sed -n '30p;86p;155p' lib/phase1/noise/snr_frame_science.cpp
    30:  double median_of(std::vector<double> v) {                    ← 本地中位数实现
    86:  out.sigma_location_se_mag = 2.5 * out.sigma_location_se_dex;  ← 本地 dex→mag 换算
    155: out.local_snr[i] = out.snr_f[i] / med;                       ← 本地 quality_weight 定义式
    $ grep -n "2.5 \*" lib/snr_estimator/cpp/src/snr_science.cpp lib/snr_estimator/cpp/src/noise_model.cpp
    snr_science.cpp:207 / :227（权威侧 -2.5*log10 内嵌）；noise_model.cpp:295（第二份同式）

- 问题说明：两处可判定的不符。(a) 枚举不实：以"本 TU 的全部数值都来自"引出 4 个权威函数，其中 snr_moffat4_profile_f64 在本 TU 零调用。(b) 纪律宣称就地为假：同一 TU 有 2.5·dex→mag 换算、本地 median_of、local_snr = snr_f/med 三处手写算式；而 2.5 换算在 noise_model.cpp:295 有第二份、权威侧 snr_science.cpp:207/:227 是第三处 —— "零公式副本 / 唯一权威"在同批交付里没有发生。宪章 §12.2 要求注释与实现一致，本条不是"注释旧了"，是宣称的收敛性未落地。
- 判据：①（清单 / 计数复算不符）+ ④（"零副本 / 全部走 ABI"就地假）。P1：它直接动摇"改公式只需改一处"的复核前提。
- related：V1 二(d) 曾把 snr_frame_science.cpp:84-86 记为站点并判"一致（零公式副本）" —— 本轴给该判定的反例，属前层复算不充分，不是重报；V6-N-04（:11 的 run/ oracle 宿主）；V12-N-05（同族"公式双头 / 唯一权威"）。

### V8-N-03（P2·判据①）README 的"4 处计算"与它自己括号里的 5 个名字互相否定

    $ sed -n '212,214p' lib/snr_estimator/README.md            # b0353303 新增（已核为 added 行）
    → **逐源最优提取 SNR_F**（无量纲）。生产路径 snr_estimator.cpp 的 4 处计算
    （snr_estimate / snr_estimate_f64 / snr_extract_model / _v2 / _v3）统一改为调用 snr_source_snr_f64

复算：

    $ grep -n "snr_source_snr_f64" lib/snr_estimator/cpp/src/snr_estimator.cpp
    76:  if (snr_source_snr_f64(&p, &res) != 0) return 0.0;     ← 唯一直调点（在本地 helper 内）
    $ python3 scripts_v8_snr_sites.py
    sourceSnrFromPsfRow call sites: [188, 357, 616, 740, 851]   ← 5 个导出函数各 1 处
    enclosing exported functions: snr_estimate / snr_estimate_f64 / snr_extract_model / _v2 / _v3 (distinct = 5)

- 问题说明：括号自列 5 个入口、helper 调用点 5 处、权威函数直调 1 处，三处口径里没有一个是"4"。在册 README 的计数句是复核者核对覆盖面的入口，写 4 会让人以为有一处漏改。
- 判据：①。P2。对应前台工单 8baf1f1f 的 E7（在册计数禁字面量钉死）。
- related：V12-N-13（同 README 族"口径与自述不符"）、前台 a44b159f（工单自纠"写六条实为七条"同错）。

### V8-N-04（P1·判据②⑤）证据外置的新形态：裸 REPORT 章节锚，仓内连"去哪找"都没写

    $ python3 scripts_v8_tmp_q3.py        # 在 7912 条新增行里筛 REPORT
    REPORT mentions in added lines: 15   files: 15
      lib/phase1/stars/star_detector.cpp:19            逐字节不变(REPORT.md §3 给 bit-pattern 对照)
      lib/core/src/module_adapters.cpp:1605            最亮 5000 颗 ~4 s（REPORT.md §4）
      lib/dynamic_psf/src/dpsf_psf.cpp:112             仅末位 ulp 可能与 std::pow 不同 (REPORT.md §2 量化)
      lib/dynamic_psf/README.md:233                    末位 ulp 可能不同，见 P2 REPORT §2
      lib/phase1/stars/README.md:23                    bg 估计 7.69 s → 见 P2 REPORT §3
      lib/phase1/noise/README.md:58                    仓库文件逐字节未变，见 REPORT §6
      lib/plate_solve/cpp/ipv/test/CMakeLists.txt:13   见 REPORT "越域残余"
      lib/plate_solve/cpp/ipv/test/test_extract_wcs_sip_failclosed.cpp:25   注册钩子见 REPORT "越域残余"
      lib/star_detector/test/sdet_angle_guard_test.cpp:10   见本批 REPORT §同类缺陷排查
      lib/photometric_calib/tests/p1phot/p1phot_fixgates.cpp:17   新工作项, 见 REPORT
      （另 5 处为显式 run/ 路径，归 V8-N-05 清单）

宿主核对（三级复核）：

    $ ls REPORT.md                                  → No such file or directory（根目录无该文件）
    $ git --no-optional-locks ls-files | grep -E "^lib/.*REPORT"
      lib/plate_solve/cpp/ipv/REPORT.md              ← 仓内唯一在册同名文件
    $ grep -n "^## " lib/plate_solve/cpp/ipv/REPORT.md
      ## 2. 算法实现 / ## 3. 测试结果 / ## 4. 与设计文档的差异 / ## 6. 后续 Phase 规划
      ← §3 是 k-vector 单测，与"P2 的 p1_sources / p1_flux bit-pattern 对照"无关
    $ git --no-optional-locks grep -c "bit-pattern" -- .
      命中文件只有 star_detector.cpp 自身（宣称者 = 被宣称物）
    $ grep -rl "越域残余" run/                       → 空（该章节名在仓内与工作区都不存在）
    $ git --no-optional-locks grep -c "越域残余" -- . → 只有引用它自己的 3 处注释 + 本轴 dump

- 问题说明：V6-N-04 立的是"显式 run/ 路径被当权威"；本批还有一族连路径都不给的裸 REPORT 锚。它比前者更弱：显式 run/ 路径至少能判定"宿主在仓外"，裸名锚在干净检出后无任何可解析目标，而仓内唯一同名文件是 plate-solve 的历史报告（章节语义完全不对应），会把复核者直接指向错文件。承载的恰恰是最需要凭证的宣称：p1_sources.json / p1_flux.json 逐字节不变、末位 ulp 量化、最亮 5000 颗 ~4 s、注册钩子归属。
- 判据：②（锚无宿主）+ ⑤（未版本化依据）。P1（与 V6-N-04 同级，但属未登记的新形态）。
- related：V6-N-04、V6-N-05（README:58 那一处已随"同提交自相否定"入账，本条只补其余 14 处的族量）；前台 8baf1f1f 工单 E5（run/ 作权威即红）—— 该门抓不到本形态，因为串里没有 run/。

### V8-N-05（计量交付·判据⑤）本批新增行内 run/ 引用完整清单

    $ python3 scripts_v8_runrefs.py    # 口径：新增行 × 范围 lib cli include tools docs × 正则 run/[A-Za-z0-9_./-]+
    TOTAL run/ mentions in added lines: 19   files: 15
      c3452d48 docs/traceability/TRACEABILITY_MATRIX.json:448 与 :760 → "run/inspect/destroy"  ← 假阳性
    扣除 2 处假阳性后：真引用 17 处 / 14 文件（逐条清单见 _v8_runrefs.txt）

| 提交 | 站点 | 被引宿主 |
|---|---|---|
| bd4bc23b | lib/gaia_xpsd_client/src/gaia_client.c:2053 | run/perf-fix/P1-gaia/REPORT.md |
| 6d74046d | ipv_select.h:74；ipv_types.h:223；ipv_select.cpp:360（×2） | run/perf-fix/P4-magiter/REPORT.md；run/release-rescue/ |
| 23a7f665 | lib/dynamic_psf/tests/p1psf/p1psf_tests_core.cpp:852 | run/perf-fix/P2-psf/logs/15_iter_limit_* |
| b0353303 | docs/science/CONTROL_WEIGHT_SNR.md:51；snr_estimator/README.md:195,234；snr_estimator.h:195；snr_estimator.cpp:10；snr_science.cpp:3；p1snr_science_test.cpp:7,10；tests/p1noise/CMakeLists.txt:101 | run/release-rescue/science-phot/PHOTOMETRY_LITERATURE_REVIEW.md；run/perf-fix/P5-snr/harness/snr_oracle.py |
| 35c85f53 | lib/phase1/noise/README.md:55；snr_frame_science.cpp:11 | run/perf-fix/P8-snr-linux/patches/out-of-scope/lib-core-snr-wiring.patch；P5-snr/harness/snr_oracle.py |

- 宿主存在性：ls run/perf-fix/P4-magiter/REPORT.md、run/perf-fix/P5-snr/harness/snr_oracle.py、run/release-rescue/science-phot/PHOTOMETRY_LITERATURE_REVIEW.md 三个都在本机盘上；但 git ls-files run/ 只有 run/.gitkeep（.gitignore 收 run/ 全目录）⇒ 盘上可看、仓内无物，干净 checkout 全部失锚。V6-N-04 已就此立条，本条只交族清单与计数（轴工单⑤要求的"完整清单"）。
- 口径区分（避免与前台数字混用）：前台 56b5e662 给的是"全仓 run/ 引用 573 处 / 194 文件（分类后仅 A 类计入 C-15）"，本条是"本批新增行 × 5 路范围 × 注释 / 文档面"，两者不可相减。另注：run/ 命中里 2 处是 C API 符号列举（p2_session_create/validate/run/inspect/destroy）的假阳性，E5 门若按串匹配同样会吃进，落地时需排除该上下文。

### V8-N-06（P2·判据②）gaia_client.h 头注释的契约锚指向三个不存在的节名

    $ sed -n '3p' lib/gaia_xpsd_client/src/gaia_client.h       # bd4bc23b 新增整行
     * - 容量/生命周期: QueryCache 64条 … 契约见 docs/algorithms/GAIA_QUERY.md
       Postconditions/Invariants/并行模型 与 docs/architecture/CACHE_POLICY.md Gaia查询缓存行

    $ grep -n -i "postcond\|invariant\|并行模型\|并发模型" docs/algorithms/GAIA_QUERY.md
    216:- **不变量（properties）**：            ← 无 "Invariants" 节名（中文，且属 §5）
    191:## 4. 并发模型（现状，与源码一致）      ← 节名是"并发模型"，不是"并行模型"
    （"Postconditions" 全文件 0 命中）
    $ grep -n "Gaia 查询缓存" docs/architecture/CACHE_POLICY.md   → 命中（该锚为真）

同句其余自报量复算全真（防误伤一并记录）：QUERY_CACHE_CAPACITY=64、64×200000×3×sizeof(double)=307,200,000（与 module_entry.c:546 的 kQueryCacheCap 同式）、QUERY_CACHE_TTL_SEC=60、BLOCK_CACHE_CAPACITY=8192（=2 的 13 次方）、BLOCK_CACHE_MAX_MEMORY=4GiB、内存压力淘汰 to_evict = bc->count/4 + 1、gaia_client.c:732 "事务式替换——先全部分配成功再释放旧条目"。

- 问题说明：公共头是 C ABI 的消费入口，注释给的三个锚名里两个在目标文档无同名节、一个与实际节名语义相反（"并行模型" 对 文档"并发模型（现状，与源码一致）"）。按名检索必失败；CACHE_POLICY 那一半是对的，故本条只登记 GAIA_QUERY 侧。
- 判据：②。P2。related：V6-N-07（snr 行锚 9/9 漂移，同族"门盲区锚"）、V5 四（GAIA_QUERY 与 CACHE_POLICY 跨文档一致性当时记"未判"，本条即该未判项的锚面局部结论）。

### V8-N-07（P1·判据③，需负责人确认）"负责人裁决 2026-09-14" 39 行援引中两族在权威登记面无条目

    $ python3 scripts_v8_passF_owner.py
    负责人裁决类新增行: 49            其中同含 2026-09-1x 日期: 39
    $ grep -n "09-14" memory.md                                   → 空
    $ grep -n "2026-09-14" 问题扫描/40_OWNER_DECISIONS.md         → 空
    $ grep -n "PSF-FAST\|只留 fast\|max_stars" CHANGELOG.md     → 空
    $ grep -n "帧头\|CRVAL\|init_source" memory.md 问题扫描/40_OWNER_DECISIONS.md → 空
    正对照（同批已登记的那族）：
    $ grep -n "负责人授权" CHANGELOG.md | head -2
    9:### P5-SNR 逐源 SNR 科学修正（2026-09-14，负责人授权，scientific_change=YES）
    11:> **负责人授权**：2026-09-14 负责人明示批准修改冻结/科学文档（…）

三族拆解：

| 族 | 新增援引站点 | 登记面 |
|---|---|---|
| P5-SNR 授权（改冻结科学文档） | 约 20 处（docs/science 四份、PUBLIC_API、PHOTOMETRY、algorithm.md、snr 族源码与锁…） | CHANGELOG.md:9-11 有登记 ✓ |
| PSF-FAST-001「只留 FAST + 精确路径保留」 | 12 处：include/astrocs/core/module_adapters.h:30、module_adapters.cpp:1599 / 1754 / 1791 / 6162、lib/dynamic_psf/README.md:204 / 218、module.yaml:157、lib/phase1_session/README.md:49、p1phot_fixgates.cpp:13 / 90 | 40 / memory / CHANGELOG 0 命中 |
| P9「帧头 WCS 未授权」 | 6 处：DATA_SEMANTICS.md:788 与 791-792（含"裁定原文"引号）、lib/core/README.md:15、module_adapters.cpp:1966 / 2207 | 0 命中（"裁定原文"的唯一仓内宿主就是合同自己那一行，自指成环） |

- 问题说明：准绳①把"负责人设计意图"列为最高权威，其载体是 40_OWNER_DECISIONS.md / memory.md / 根 AGENTS.md 十要素 / 宪章 §18。同批的 P5-SNR 走了 CHANGELOG 登记（说明作者知道该怎么做），而 PSF-FAST 与 P9 两族把裁决直接写进代码注释、模块 README、module.yaml，甚至在冻结合同里以引号形式引用"裁定原文"，登记面 0 条 ⇒ 复核者无法区分"确有其裁决"与"以裁决名义自证"。宪章 §1.2 明确 Agent 无权解释或新设裁决条款，这条链必须补登记。
- 判据：③（权威宣称与在册登记互斥）。P1，但定性需负责人确认：若确有口头裁决，缺的是登记；若无，则 P9 合同正文是在铸权威。
- related：V4 三 A-42（memory_report.h:10 的 OWNER-07 未落档，同族，不重报）、V13-N-04（VERIFIED 自指）、前台 C-18（编号生命周期：新编号须先登记才可引用 —— 本条是其"裁决引用"侧实例）。

### V8-N-08（P3·判据②）归一化契约区间端点写错（可达值被文本排除）

    $ sed -n '38,39p' lib/star_detector/src/sdet_angle_guard.h      # 8dc0220e 新增
    // 将 angle_deg 归一化到 (-90, 90], 行为与冻结迭代式对有限输入逐位一致。
    $ sed -n '56,63p' lib/star_detector/src/sdet_angle_guard.h
        while (std::fabs(angle_deg) > 90.0) { angle_deg += (angle_deg > 0.0) ? -180.0 : 180.0; ++steps; }
    $ sed -n '96,104p' lib/star_detector/test/sdet_angle_guard_test.cpp
    CHECK(std::fabs(out) <= 90.0, "归一化结果落在 [-90, 90]");   ← 测试用闭区间，头注释用左开区间

- 问题说明：输入恰为 -90 时循环条件不成立，函数返回 -90.0（可达），而头注释声明的值域排除 -90。同文件 :34 的"64 次 (≥ 11610°)"与 :24 的"有限值逐位不变"复算为真（90+180×64=11610 恰被接受，11790 才拒），故本条只登记端点一处。
- 判据：②（文本自述与实现互斥，行内即判）。P3。

---

## 三、五条判据的覆盖率（口径交代）

| 判据 | 扫描动作 | 结果 |
|---|---|---|
| ① 自报量 | passA（87 命中）+ 逐条复算 | 新立 V8-N-02 / N-03；复算为真的 9 条入 七 |
| ② 锚有宿主 | passB（32 路径可疑）+ passB2（精修 9）+ passD（path::symbol）+ 节名核 | 新立 V8-N-01 / N-04 / N-06 / N-08；行号越界 0 |
| ③ 权威互斥 | passE 绝对化宣称 156 行 + passF 裁决族普查 | 新立 V8-N-07 |
| ④ 纪律宣称 | passE（零副本 / 全部走参数 / 无硬编码） | 新立 V8-N-02；"无硬编码"族属 V2-D9（不重报） |
| ⑤ 未版本化权威 | scripts_v8_runrefs.py 全清单 | V8-N-05（计量）+ V8-N-04（新形态） |

---

## 四、需运行期或未判（不强行定级）

1. p1phot_fixgates.cpp:25「GREEN(修复后): 全部断言通过 → rc=0」与各锁 PASS/FAIL —— 需运行期，未判。
2. ipv_types.h:232「safety=3 是 10 帧样本上全部通过的最小值（余量 +0.36..+1.07 mag）」、ipv_select.h:72「4/10 真实帧」「密度实测差 4–5 个数量级」—— 宿主在未版本化 P4 REPORT，属真实数据运行结果 ⇒ 需运行期，未判。
3. star_detector.cpp:18-19「median/MAD/bg 逐位等于旧实现 / p1_sources.json 逐字节不变」—— 需同帧双跑比对 ⇒ 需运行期，未判（本轴只判其凭证锚无宿主，见 V8-N-04）。
4. snr_frame_science.cpp:99-106（P10）「无共享可变状态、无归约、无锁 ⇒ 与线程数无关」—— 被调侧是否含函数级 static 属并发正确性（同类已由 V6-N-08 记），逐位性需双线程数实跑 ⇒ 需运行期，未判。
5. DATA_SEMANTICS §18.5 的 906 帧可推导性 —— 本轴已静态复算（见 五），不再列未判；但"NGC55 idx561 是 CRVAL 错、OBJCTRA 对"这一归因需真解算 ⇒ 未判。

---

## 五、反向取证：P9 新增合同文本的实测宣称复算为真（不报为缺陷）

    $ python3 scripts_v8_p9_frames.py        # 只读解析 testdata 下全部 .fts 的 FITS 头
    scanned 906 err 0
    missing header-WCS keys (FOCALLEN/XPIXSZ/OBJCTRA/OBJCTDEC): 0     ← 对合同 :808-809「906/906 均有」
    frames WITHOUT CRVAL1/2: 31  per dataset: {Galaxy_Center_T4: 1, M42_T2T3_mosaic: 30}
                                                                       ← 对合同 :806-807「31 帧（1×Galaxy_Center_T4 + 30×M42）」
    $ python3 scripts_v8_p9_dev2.py
    TOP |CRVAL - OBJCT|: 0.88247°  NGC55_T3 20250705@075716-1200S-H-alpha（数据集内 idx34）
    $ python3 scripts_v8_p9_idx.py
    global-path-sorted idx: 561               ← 对合同 :807「NGC55 idx561 … ~0.88°」
    906 帧总数另由在册 testdata/index.json 的 lights_count 求和独立复算 = 906 ✓

⇒ §18.5 的四项计数（906 / 31 / 1+30 / 0.88247° @ idx561）与 lib/core/README.md:19-22 的板尺度量纲说明（206.265 = (180×3600)/π × 1e-3；206264.806 仅当 XPIXSZ 以 mm 记；常量与 ipv_select.cpp:57 同值）全部复算为真；README:21 的 ipv_select.cpp:57 行锚在册命中。本轴据此给 P9 出通过项，同时留一句口径：合同用"全库路径排序下标 561"当唯一标识却在文本里未写明排序口径（数据集内是 idx34），后续复现需按同一排序 —— 属改进，不计缺陷。

---

## 六、其余"权威 / 合同"宣称的核对（简）

| 宣称 | 结论 |
|---|---|
| cli/resource_gate.h:107「75%/50% 已被 §18.2 第 2 项明文废止，不得回归」 | 宪章 §18 第 2 项 verbatim 含"替代旧 D.6 的 80%/75%/50%" ⇒ 真 |
| lib/phase1/noise/README.md:29 的 include/link | 原文是"未改 include/link/目标结构"，指 CMake include 与 link 语义，非路径 ⇒ 扫描假阳性 |
| module_adapters.cpp:1969-1971「206.265 与求解器唯一权威常量逐位一致」 | 两处同为字面量 206.265 ⇒ 真；但存量 ipv_select.cpp:56 把该值写作 (180×3600)/π（差 1e3，非本批新增行，已核 added 表 False），属 V12-N-01 常数承载族 |
| ipv_select.cpp:512「4 个 ipv_select 路径共用的迭代查询封装」 | gaia_query_mag_iterative 调用点 :957 / :1255 / :1525 / :1852 = 4 ⇒ 真 |
| test_mag_iter.cpp:10「safety / max_iter / alpha_prior / clamp 全部来自 IPVSolverParams」 | :221 / :222 / :230 / :236 逐项覆盖，但只测 m_lim_clamp_hi，clamp_lo 无断言 ⇒ 部分真；生产侧字面量问题属 V2-D9（不重报） |

---

## 七、明确不报为缺陷 / 交叉印证（防误伤）

1. docs/algorithms/IPV_PIPELINE.md 幻影宿主（6 站点）= V12-N-06 已立条。本轴独立复算一致：git cat-file -e ccc7a933:docs/algorithms/IPV_PIPELINE.md 返回 rc=128、git log --all 该路径为空、全仓命中仅 lib/plate_solve/IPV_PIPELINE.md（且全文 safety 0 命中、偏差 / 登记 / DISP 0 命中、:136 仍写"最终兜底: mag=22"）⇒ 不另立条，仅交叉印证。
2. LINUX-BUILD-ROOT-GRAPH（cli/CMakeLists.txt:212）：宿主存在（ci/workflow_binding.json:195、ci/checks.json:1260）⇒ 非断锚。
3. tests/kcorr_lookup_test.cpp、tests/p1noise/p1snr_science_test.cpp：模块相对路径，解析成立。
4. module_entry.c:386 / :780「全部拼接 helper 显式带容量」：4 个 json_append 系 helper 全部带 end 参数，且全文件无裸 sprintf / strcat / strcpy ⇒ 真。
5. memory_report.h:171「RSS 口径回退与 F-14 前逐字一致」：逐条件比对 c1959436 前的旧判定链（peak==0 → Insufficient；时间窗 → PeakInWindow；frac≥0.5 或 retained≤32MiB → Reclaimed；否则 Unexplained）与新版一致 ⇒ 真（其非单调与 retained 语义后果属 V4-N-10，不重报）。
6. snr_estimator/README.md:203-207 公式表逐条与 snr_science.cpp 实现比对（f_in = 1 − (1 + r²/2σ²) 的 -3 次方、F_5 = 5σ_F、1.253 = sqrt(π/2)）⇒ 表达式一致（位数问题属 V12-N-04）。
7. p1psf_tests_core.cpp:844「README §6 码 3 行为锚」⇒ lib/dynamic_psf/README.md:107 / :124 确有 §6 错误码表且码 3 = DPSF_FIT_ITERATION_LIMIT，真。
8. sdet_angle_guard.h:34「64 次 (≥ 11610°)」边界算术复算真（11610 接受、11790 拒）。
9. TRACEABILITY_MATRIX.json:448 / :760 的 "run/inspect/destroy" 是 C API 符号列举，非 run/ 路径 ⇒ 扫描假阳性，已从 ⑤ 计数扣除。
10. snr_frame_science.cpp:3 的"零副本"若只看 Horne 主式确为单一实现 ⇒ 本轴 V8-N-02 判的是枚举不实 + dex→mag 换算三处并存，不判"科学公式重复实现错"。

---

## 八、一句话总判

本批新增文本自己制造的可核验性缺陷集中在三处：给决定找凭据时把锚写到了不存在的东西上（一条杜撰测试符号、15 处裸 REPORT 章节锚、3 个不存在的契约节名）；把收敛性写成事实（"零公式副本 / 全部数值调 C ABI / 4 处计算"三句均就地复算不符）；以及把负责人裁决只写进注释与合同、不写进登记面（39 行援引中两族 0 登记，同批另一族却登了 CHANGELOG）。反过来，最被怀疑的 P9 合同实测数字（906 / 31 / 1+30 / 0.88247° @ idx561）经真实帧头只读复算全部为真 —— 本轴净结论：数字这一批没撒谎，撒谎的是锚和归属。
