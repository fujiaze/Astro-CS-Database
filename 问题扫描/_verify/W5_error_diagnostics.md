# W5 · 错误处理与诊断完备性（第二轮）HEAD a3a343a4 / 纯静态

> 轴：W5（错误处理与诊断完备性，第二轮全仓静态审查）| 时点 HEAD `a3a343a44080d917089e1f8d548ed2d0400b0c61`（简称 a3a343a4）
> 权威序：①负责人设计意图 ②现行冻结文本（宪章 §14.4 fail-fast / §8 退出码 / §12.3 机器门）③代码/测试自洽
> 纪律：纯静态（read/glob/grep + 本目录内 python3 -B 只读解析 + git --no-optional-locks 只读子命令）；
> 未执行任何编译器 / cmake / ctest（含 `-N`）/ 测试 / 二进制 / ci/run.py / Fatduck 连接 / git 写 / 删除改名；
> 未运行任何无 `--dry-run` 的仓内脚本。静态不能定的，一律标「需运行期，未判」。

## 〇、口径与分母（先锁，后文裸数字均带此口径号）

- **口径 W5-A（生产 C/C++ 语料，时点 a3a343a4，`git ls-files`）**：`lib/**`+`cli/**`+`include/**`+`runtime/**`+`providers/**`+`modules/**` 中 `*.cpp|*.h|*.hpp|*.c` 文件集合，实测数见 §一。
- **口径 W5-B（门面 Python 语料）**：`tools/**`+`ci/**`+`scripts/**` 中 `*.py`，实测数见 §一。
- **口径 W5-C（吞错分类）**：S1 空 catch/except 体；S2 catch 后仅记日志继续（无重投/无退出码）；S3 忽略返回值（不检查 status/rc）；S4 默认值兜底（`value_or`/`get(default)`/`or {}`）；S5 `(void)` 显式吞；S6 裸 except 吞 KeyboardInterrupt/SystemExit。分类计数见 §一。
- 本轴条目号：**W5-N-01 …**（宁窄而实，上限 15 条）。

## 一、吞错点分类计数（全量）

（待填）

## 二、最危险 15 处抽取

（待填）

## 三、错误码空间：复用 / 冲突 / 含义漂移

（待填）

## 四、fail-fast 边界（§14.4）是否被降级成警告

（待填）

## 五、重试与幂等

（待填）

## 六、诊断可定位性（文件::符号 + 实际值 + 阈值）

（待填）

## 七、跨语言错误码名值表

（待填）

## 八、条目总表

| 编号 | 优先级 | 一句话 | 证据锚 | related |
|---|---|---|---|---|
|（待填）| | | | |

## 九、三级复核记录（判"缺失"必贴命令与输出）

（待填）

## 十、负结果（查过没找到的，防"没扫"被读成"没问题"）

（待填）
