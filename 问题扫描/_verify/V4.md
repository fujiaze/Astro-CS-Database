# V4 · 门禁硬化与线程预算复核（性能域，涉及宪章 §10.4 / §10.5 / §17.6）

> 复核组：V 层 V4。全程中文。纯静态（read/grep/glob + `问题扫描/**` 内只读脚本 + `git --no-optional-locks` 只读查询）。
> 准绳顺序：①负责人设计意图（`设计大纲/`、`memory.md`、根 AGENTS.md 十要素、`40_OWNER_DECISIONS.md` 已裁条）②现行冻结文本（改后）③代码与测试自洽。
> 纪律：不把"与本审计旧条目不一致"当缺陷；反向查 (a) 旧错误口径被反向钉死、(b) 五处同源缺口。
> 时点：本次复核基于 HEAD（工作树状态见 §0 命令记录）。

## §0 范围与命令

- 对象提交：
  - `adaeb531` fix(RQS/B2): 门禁硬化 —— 恢复冻结资源门、假门转真门、UT-ABI 采集补齐
  - `dbaa2e18` perf(P7): 修复节点租约污染 OpenMP ICV + 预算份额均分
  - `c1959436` fix(RQS/B7): F-14 内存回收门被测量修正
- 复核任务项（6 项）：门能否红 / 资源门分母三件套 / OpenMP ICV 与份额均分新风险 / UT-ABI 是否真用例 + FD-F-003 真实状态 / F-14 测量式可欺骗性与阈值依据 / 账本与回归锁一致性。
- 命令清单（逐条结论处贴完整输出摘录）：
  - `git --no-optional-locks show --stat <sha>`
  - `git --no-optional-locks show <sha> -- <path>`
  - `python3`（在 `问题扫描/**` 内解析 FIX_LEDGER.csv、静态解析 add_test）

## §1 逐条结论

### 1.1 「假门转真门」清单与"能否红"（M8-F-002 根因：failures 不进退出码）

待填。

### 1.2 资源门分母（M5a-G-002）三件套：分母口径 / 字面量是否被真实计算取代 / 子串断言是否换成值断言

待填。

### 1.3 OpenMP ICV 污染修复（P7）机理、修复点、份额均分的新风险与"单一线程预算源"

待填。

### 1.4 UT-ABI 采集补齐是否真用例；FD-F-003（WIN-TEST-UNIT 0 用例仍 PASS）真实状态

待填。

### 1.5 F-14 内存回收门：测量式可否被 RSS/虚拟内存假象欺骗；阈值是否调整及依据

待填。

### 1.6 账本核对：三提交声称覆盖条目 vs `git show --stat`；回归锁名 vs `tests/**/CMakeLists.txt` 的 `add_test`

待填。

## §2 新发现缺陷

待填。

## §3 反向钉死与同源缺口

待填。

## §4 需运行期或未决

待填。
