# V11 · 跨语言结构体镜像与 ABI 自描述普查（从 V2-D8 外推）

- 复核层：V11（发现新缺陷，纯静态；无编译、无 cmake/ctest、不跑测试或二进制、不跑仓内脚本、无 git 写、不连 Fatduck）
- 仓库根 = 当前目录。开档时点 HEAD = `<待填>`（时点：2026-09，V11 会话）
- 准绳顺序：①负责人设计意图（设计大纲/、memory.md、根 AGENTS.md 十要素、问题扫描/40_OWNER_DECISIONS.md 已裁条）②现行冻结文本（改后）③代码/测试自洽。**不把「与旧文档不一致」当缺陷**；专查「把旧错误口径当权威改回去」。
- 宪章 §8.6 自描述要求 = 本轴主判据（逐字见 §1）。
- 免重报：已读 问题扫描/SUMMARY.md 簇表与 问题扫描/INDEX.md §三；V2-D8（IpvParams ctypes 镜像 352 vs 424，72 字节越界写）为**已知实例**，本轴只作外推基准，不重报。

## §0 命令清单（全部只读）

| # | 命令 | 用途 |
|---|---|---|
| K1 | git --no-optional-locks rev-parse --short HEAD | 时点 |
| K2 | grep ctypes.Structure / cffi / struct.pack / pybind / DllImport / StructLayout | 跨语言镜像点全量 |
| K3 | python3 问题扫描/_verify/_v11_census.py | 解释器解析：镜像点 ↔ C 头配对与 sizeof 推导 |
| K4 | 定点 read 公共 C 头 | 字段序/类型/对齐/数组长度/位域 |
| K5 | grep struct_size / abi_version | 自描述面 |
| K6 | 解释器解析 ci/checks.json | 消费方是否被任何 CI 面采集 |
| K7 | grep dlopen / LoadLibrary / 函数指针 typedef / extern "C" | 跨二进制同族 |

## §1 宪章 §8.6 逐字（判据源）

`<待填>`

## §2 普查总表（镜像点清单）

`<待填>`

## §3 逐条结论（新缺陷）

`<待填>`

## §4 系统性统计：公共 C 头中「无 struct_size 而被跨语言消费」的结构体

`<待填>`

## §5 需运行期、未判

`<待填>`
