# V13 — 自造 ID 与循环证据普查（验证层子代理）

## 0. 范围、时点与命令口径

**轴源**：任务书 V13，种子 = V6-N-06（EVID-P1-PSF-001/EVID-P1-STAR-001 全仓各仅 1 处引用=自己那行）+ L28e-E-001（v19r3_traceability.py f-string 铸 TEST ID 再自证）。
**对象**：全仓登记面（docs/TRACEABILITY.csv、docs/traceability/TRACEABILITY_MATRIX.{json,csv}、reports/v19r{2,3}/**、contracts/schemas/**、ci/checks.json）与其生成器（tools/**、ci/**）。
**判据顺序**（负责人明令）：① 负责人设计意图 ② 现行冻结文本（改后）③ 代码/测试自洽。不把「与旧文档不一致」当缺陷；专查「把旧错误口径当权威改回去」（结果见 §7）。
**纪律**：纯静态；git 一律 --no-optional-locks；结构化文件全部解释器解析；「缺失/0 命中」结论走三级复核（全句 → 放宽 → 定点读）；排除前缀 ./run ./build ./worktrees ./GaiaDR3 ./GaiaDR3SP './BASS DR3' ./AstroCS.wiki ./Testing ./out；未运行任何无 --dry-run 的仓内脚本（门判定器全部「读源码 + 内存复算」）。

### 0.3 时点表（本层期间 HEAD 两次移动，计数按标注时点）

| 测量 id | 时点 HEAD | 用途 |
|---|---|---|
| M-V13-01 | `521095b8`（2026-09-14T15:13Z） | 建骨架；机制A扫描、EVID/编号普查、矩阵双向 diff |
| M-V13-02 | `8c977118` | 必查1/2 收口；HEAD 面复验（TRACEABILITY 64 行、PSF|REJ 0 行命中，df16f008..8c977118 共 60 文件无追溯面改动） |

**工作树态**：MATRIX csv/json 与 HEAD 逐字节一致（`git show HEAD:f == open(f).read()` → True/True）；docs/TRACEABILITY.csv 与 HEAD 内容一致（仅换行归一化 M 态，`git diff HEAD --` 空）。

### 0.4 引用计数口径（本层统一）

对每条编号：python3 逐 tracked 文件读入，`(?<![A-Za-z0-9-])ID(?![A-Za-z0-9-])` 全字边界计数；宿主分类：
REG=三张追溯面自身；GEN=`tools/quality/v19r3_traceability.py`；SELFREP=`reports/v19r3|v19r2/`；DOCS=docs/contracts；TRUTH=lib/include/cli/runtime/providers/modules/tests/ci/tools/scripts 等真源面。
「非登记面命中」= 除 REG+GEN+生成器自带报告外的全部命中；0 即"编号无宿主"。三级复核：字面边界计数 → 放宽子串（含范围锚 X-001..015 命中确认）→ 定点读宿主行。

---

## 1. 机制A · 生成器以字符串模板铸造编号（穷举结果）

全 tracked py/js/sh/ps1/cmake/txt（4,840 文件，去 ./run 等 8 前缀）扫描 f-string/拼接/%/JS模板 四形态，命中并分类：

| 铸造点 | 编号族 | 枚数 | 先报关系 |
|---|---|---|---|
| `tools/quality/v19r3_traceability.py:263` `f"SCI-NOISE-{i:03d}"` | SCI-NOISE-001..015 | 15 | **L28e-E-001 只报 TEST 侧 16 枚；SCI 侧本层新计数（见 §3）** |
| 同 :274 `f"SCI-UPM-{i:03d}"` | SCI-UPM-001..010 | 10 | 同上 |
| 同 :294 `f"TEST-PR-UPM-{i:03d}"` | TEST-PR-UPM-001..010 | 10 | L28e-E-001 在册；本层补 2 对重复载体（§3-C） |
| 同 :311 `f"TEST-UPMW-{i:03d}"` | TEST-UPMW-001..007 | 7 | L28e-E-001 在册 |
| （负结果）DISP- 铸造脚本 | 0 | — | DISP-* 只存在于 docs/algorithms 文本，无任何脚本铸造（全仓 grep 脚本面 0 命中） |
| （负结果）f-string 反序形/百分号形/JS 模板形铸造 | 0 | — | 详见 `问题扫描/_verify/scripts_v13_mint.py`（本层落盘） |

非铸造噪声（明确不算）：`runtime/artifact_store/provenance.py:438/:591` 与 `tools/science_contract_lint.py:46` 的 f-string 是**报错文案**里的正则说明串；`tests/cli/test_cli00*.py` 的 `b"SCI-DATA-" + os.urandom(...)` 是负样本填充；`tests/traceability/fixtures/*.json` 是校验器负样本（故意坏 ID）。

**机制A总判**：全仓「以模板铸编号」仅 v19r3_traceability.py 一器四族 42 枚；但**同类循环不依赖 f-string 也成立**——c3452d48 手工把 5 枚自铸 EVID 直接写进两张矩阵（§2-N-01/N-02），说明「铸 ID」的实现形态不重要，**登记面对 ID 零真实性要求**才是根因（§9）。

## 2. 机制B · VERIFIED 行的指针四形态

### 2.1 登记面清单与判定器消费关系（本层实测）

| 表 | VERIFIED 计数 | 读取该列的门 | 门的判定 |
|---|---|---|---|
| docs/TRACEABILITY.csv（63 行） | status=VERIFIED **63/63** | **无**：`tools/quality/check_traceability.py`、`tools/check_traceability.py`、`tools/quality/contracts/check_traceability.py` 三消费方 `grep -n VERIFIED` 全 0 命中；仅读 requirement_id/authority_doc/路径存在性 | status 列**恒不被消费、恒不为假而红** |
| MATRIX JSON（30 行） | evidence_status=VERIFIED 6 行 | `check_traceability_matrix.py`：C7 对 EVID 层**无路径列**；C5 只查唯一；C8（authority 文本命中）**非 --strict 仅 WARN**，ci/checks.json 实测 command 无 --strict | 6/6 行 EVID 在自身 authority 目录零承载也全绿（§V13-N-01） |
| reports/v19r2/file_audit_inventory.csv | review_status 由 update_audit_status.py 按**行类别**批量铸 VERIFIED | 无门（三工具均未在 ci/checks.json 登记） | 见 §V13-N-03 |

### 2.2 逐行四形态判定（自指/指文档/不存在路径/三列皆空）

**TRACEABILITY.csv**：63 行 implementation_files/test_files 路径全部存在（worktree 实测 0 IMPL_MISS / 0 TF_MISS / 0 TF_DOC / 0 ALL_EMPTY）——**行级指针全解析**，问题不在指针形态，在 42/63 行的 requirement_id 本身无宿主（§3）与 status 列无消费（§2.1）。

**MATRIX（HEAD==worktree，M-V13-02）**：CSV-JSON 分叉 5 行（`python3 逐列比较`输出）：
```
MOD-astrocs-phase1-{calibration,noise-snr,photometry}: JSON 有自铸 EVID-P1-{CAL,NOISE,PHOT}-001+VERIFIED，CSV 侧仍 EVID-MISSING
MOD-astrocs-phase1-star-psf, MOD-astrocs-phase1-star:   CSV 有自铸 EVID-P1-{PSF,STAR}-001+VERIFIED，JSON 侧仍 MISSING
```
即**双视图共 5 行「自铸 ID + VERIFIED + 全仓唯一宿主=自己那行」**（V6-N-06 只报 CSV 侧 2 行；JSON 权威侧 3 行为本层新案，P1 更重——JSON 才是现行权威面）。

**EVID 引用计数（全 tracked 语料，逐枚，三级复核）**：
```
EVID-P1-CAL-001   refs=1  唯一宿主 docs/traceability/TRACEABILITY_MATRIX.json   （evidence_status=VERIFIED）
EVID-P1-NOISE-001 refs=1  同上
EVID-P1-PHOT-001  refs=1  同上
EVID-P1-PSF-001   refs=1  唯一宿主 ..._MATRIX.csv（V6-N-06 在册，此处复核一致）
EVID-P1-STAR-001  refs=1  同上
EVID-BLD-003      refs=5  {REG:4, TRUTH:1}   TRUTH 宿主=lib/phase1_session/memory.md（跨模块备忘，非证据件）
EVID-IO-001       refs=3  {REG:2, TRUTH:1}   同上
EVID-CPU-001      refs=5  {REG:4, TRUTH:1}   同上
```
**且 EVID 层自身 authority 目录全空**：`git grep -l 'EVID-' -- evidence reports` → **0 命中**；AUTHORITY_DIRS["EVID"] 第三目录 `returns/` **在仓库不存在**。⇒ 6/6 VERIFIED evidence 行无一有证据件宿主；3/3 自铸行连文本提及都没有。

**MATRIX test_path 五行为「不存在路径」形态**（详 §V13-N-02）。

## 3. 机制C · 编号已铸、实体消费者为零（登记面口径）

**A. 42 枚铸造行逐枚非登记面计数**（scripts 复跑见 `问题扫描/_verify/`；REG=登记三表+生成器+v19r3 报告 排除）：
```
SCI-UPM-001     nonreg=178(多系登记快照/文档互指) | SCI-UPM-002..010  9/9 全为 0
SCI-NOISE-004/006/009/010/012  0 宿主；其余 sporadic（reports/v19r7_quality 审计文案、
               docs/science/UNCERTAINTY_AND_COVARIANCE.md、p1noise_oracle.hpp 注释）
TEST-PR-UPM-002..010  9/9 全为 0（-001 仅 reports/v19r2 + v19r7 审计文案）
TEST-UPMW-002..007 中 002/003/005/006/007 = 0；001 有 5（含 PHASE2_UPM.md:…），004 有 1
```
**合计 42 枚中 28 枚「登记面之外零宿主」**（SCI 侧 14 + TEST 侧 14+1 sporadic）。TEST 侧 16 枚已由 L28e-E-001 定稿，**不重报**；本条增量=SCI 侧 14 枚（SCI-UPM 9 + SCI-NOISE 5）与「登记面行集」口径的合并计数。related：L28e-E-001、簇 4 补二（八级阶梯第 7 级 19.2% 系注释面口径，本口径为**表行面**口径，两者不可相加混用）。

**B. VERIFIED 语义 ≠ 要求被验**：15 行 SCI-NOISE-i 的 test_ids 列一律 `TEST-SNR-001`（1 个实体挂 15 个"独立要求"），10 行 SCI-UPM-i 一律同 4 个 gtest 名；铸造行 status=VERIFIED 的实算判据只是「路径存在+符号在文件+函数名在测试文件」（读 main() 判定式确证）。

**C. TEST-PR-UPM 两对重复载体（新实例）**：pr_tests 列表字面重复（:283-292）→
```
TEST-PR-UPM-005 与 -008 → 同一实体 TEST(Phase2Upm, UpmPersistRoundtripChainNoDrift) @ synthetic_gate.cpp:5214
TEST-PR-UPM-006 与 -009 → 同一实体 TEST(Phase2Upm, UpmPersistInsertionOrderIndependent) @ :5260
```
10 行 VERIFIED 实际仅 8 个不同测试实体；行↔实体唯一性无任何机器检查（C5 只管 module_id/src_id/evidence_id 唯一）。

## 4. 必查1 · dce8abd4 / c3452d48 新铸编号登记核对

净新增 ID 集（added−removed 行级 diff，正则 (TEST|EVID|SCI|ALG|DATA|DISP)-…）：
```
dce8abd4: +3 = ALG-DRZ-001 / ALG-P2-SMP-001 / SCI-UPM-001 —— 三枚均为既有编号的正文引用
          （docs/algorithms、INDEX.yaml、PUBLIC_API.md 多宿主，逐一定点读确认）⇒ **无新造未登记**
c3452d48: +11 = 自铸 EVID×5（2 枚 CSV 面 V6-N-06 在册 + 3 枚 JSON 面 = 本层 V13-N-01）
          + TEST-CAL-001（既有 ID：docs/TRACEABILITY.csv:20 行在用、TEST_MATRIX.md 在册 ⇒ 合法并入）
          + "SCI-PHOT"（title 文案切片噪声，非编号）
          + TEST-{P1PHOT-FIXGATES, P1STAR-MAD, P1PSF-PRODPATH-CENTROID, IPV-EXTRACT-WCS-SIP-FAILCLOSED}
            ×4：系 ci/checks.json 检查名 **CTEST-\*** 的子串切片（各恰好 1 处登记，目标真实存在）⇒ 非铸造问题
```
**登记面核对**：contracts/schemas/traceability_matrix.schema.json 对 EVID 只有格式正则 `^EVID-[A-Z0-9]+(-[A-Z0-9]+)*$`；docs/standards 与 docs/traceability/TRACEABILITY_SPEC.md 均无「EVID 必须对应落盘证据件 / 编号须先登记后使用」条款（SPEC grep 生命周期/登记 → 仅 :88 只登记不判重、:112 豁免行）。⇒ **「新造编号未登记」在制度面上不存在可违之规**；实际缺陷是登记面自身缺承载校验（§9 总判）。
**SUPERSEDED_BY 落点核对**（fix_note 自述「非权威 CSV 加 SUPERSEDED_BY」）：全仓 `grep -c SUPERSEDED_BY` = **0**（三个 csv/json 皆 0）；实际落点=TRACEABILITY_MATRIX.csv 仅 :11/:12 两行 notes 内 `SUPERSEDED: 现行权威=...json` 字样；第二张「非权威 CSV」docs/TRACEABILITY.csv **零标注**；且该字样不在 JSON notes 里 → `gen_traceability_csv.py`（JSON→CSV 逐列 `str(row.get(c,''))`）一旦重跑即蒸发（V6-N-02「重跑蒸发」判词的追加实证：连 SUPERSEDED 声明本身也蒸发）。

## 5. 必查2 · V6-N-01 删行的对应登记动作

c3452d48 自 docs/TRACEABILITY.csv 净删 4 行（added=∅）：SCI-PSF-001 / SCI-REJ-001 / SCI-INT-001 / SCI-ACR-EQUIV-001。逐面核查登记动作（M-V13-02 时点）：
```
CHANGELOG.md / memory.md / docs/backlog/           → grep 0 命中（三级复核：全句→放宽"删.*合同"→定点读）
问题扫描/40_OWNER_DECISIONS.md                     → 0 命中（非负责人裁决的删行）
账本 M3b-F-02/M3-E-001 fix_note/verified_note      → 通篇未提删 4 行（只述"三域订正"）；verified_state 却已回填 VERIFIED（df16f008 批）
生成器 v19r3_traceability.py CONTRACTS             → 四 ID grep 0 命中 ⇒ 重跑即永久缺失
docs/contracts/INDEX.yaml                          → SCI-PSF-001(:42)/REJ-001(:74)/INT-001(:82) status=ACTIVE，ACR-EQUIV-001(:122) DORMANT
CONTRACT-GRAPH 门(waivable:false)                  → 只验 INDEX.yaml 内部（ID 唯一/格式/path 存在/up-downstream 双向一致），与 TRACEABILITY.csv 无 ID 集对账 ⇒ 两表互斥恒不红
CON-TRACEABILITY @8c977118                         → 仍红：63 行、requirement_id 拼接串含 "PSF"/"REJ" 的行数=0（判据逐行读源码复算；V6-N-01a 之红未修复）
```
**判定**：删行**无任何对应登记动作**（无 CHANGELOG、无 INDEX.yaml 状态联动、无负责人裁决、无生成器同步），且删除使合同面（INDEX.yaml ACTIVE）与追溯面（行消失）**永久分叉**；V6-N-01a 报「门红」，本条报「删行无登记 + 双面无对账 + 再生成必失」的登记面全貌。related：V6-N-01a、V6-N-10（有修无账同族）、L28c（废止 ID 填充对偶）。

---
ZZZ_MARKER

**轴源**：任务书 V13（自造 ID 与循环证据普查），种子 = V6-N-06（EVID-P1-PSF-001/EVID-P1-STAR-001 全仓各仅 1 处引用=自己那行）+ L28e-E-001（v19r3_traceability.py f-string 铸 TEST ID 再自证）。
**判据顺序**（负责人明令）：① 负责人设计意图（设计大纲/、memory.md、根 AGENTS.md 十要素、问题扫描/40_OWNER_DECISIONS.md 已裁条）② 现行冻结文本（改后）③ 代码/测试自洽。不把「与旧文档不一致」当缺陷；专查「把旧错误口径当权威改回去」。
**纪律**：纯静态；git 一律 --no-optional-locks；结构化文件用解释器解析；缺失结论走三级复核；排除前缀写 ./x；禁跑无 --dry-run 的仓内脚本。
**免重报基线**：SUMMARY.md 簇 4 补二（八级阶梯/74 枚候选/L28e-E-001 机制已升第七种无据 VERIFIED）、INDEX.md §三（无据 VERIFIED=M6b-G-001、追溯双头=M6b-E-001、机器门结构性不红=M8-G-001+M6b-G-001）。同义主题挂 related。

### 0.3 时点表

| 测量 id | 时点 HEAD | 用途 |
|---|---|---|
| M-V13-01 | 521095b8 (main, 2026-09-14T15:13Z) | 建骨架；工作树含未提交 docs/TRACEABILITY.csv 等 M 态 |

## 1. 机制A · 生成器以字符串模板铸造编号（待填）

## 2. 机制B · VERIFIED 行的指针四形态（自指/指文档/不存在路径/三列皆空）（待填）

## 3. 机制C · 补编号即完事、无实体消费者的编号（待填）

## 4. 必查1 · dce8abd4/c3452d48 锚订正是否新铸编号未登记（对照 docs/standards 登记面与 contracts/schemas）（待填）

## 5. 必查2 · V6-N-01 删行（SCI-PSF/REJ/INT/ACR-EQUIV-001）有无对应登记动作（待填）

## 6. 假 VERIFIED 清单（汇总表）（待填）

## 7. 通过项（明确不报，防误伤）（待填）

## 8. 需运行期或未决（待填）

## 9. 一句话总判（待填）
