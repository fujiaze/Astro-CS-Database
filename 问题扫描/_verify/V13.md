# V13 — 自造 ID 与循环证据普查（验证层子代理）

## 0. 范围、时点与命令口径

**轴源**：任务书 V13，种子 = V6-N-06（EVID-P1-PSF-001/EVID-P1-STAR-001 全仓各仅 1 处引用=自己那行）+ L28e-E-001（v19r3_traceability.py f-string 铸 TEST ID 再自证）。
**对象**：全仓登记面（docs/TRACEABILITY.csv、docs/traceability/TRACEABILITY_MATRIX.{json,csv}、reports/v19r{2,3}/**、contracts/schemas/**、ci/checks.json）与其生成器（tools/**、ci/**）。
**判据顺序**（负责人明令）：① 负责人设计意图 ② 现行冻结文本（改后）③ 代码/测试自洽。不把「与旧文档不一致」当缺陷；专查「把旧错误口径当权威改回去」（结果见 §7）。
**纪律**：纯静态；git 一律 --no-optional-locks；结构化文件全部解释器解析；「缺失/0 命中」结论走三级复核（全句 → 放宽 → 定点读）；排除前缀 ./run ./build ./worktrees ./GaiaDR3 ./GaiaDR3SP './BASS DR3' ./AstroCS.wiki ./Testing ./out；未运行任何无 --dry-run 的仓内脚本（门判定器全部「读源码 + 内存复算」，V6 §0.2 同法）。

### 0.3 时点表（本层期间 HEAD 移动 1 次，计数按标注时点）

| 测量 id | 时点 HEAD | 用途 |
|---|---|---|
| M-V13-01 | `521095b8`（2026-09-14T15:13Z） | 建骨架；机制A扫描、EVID/编号普查、矩阵双向 diff |
| M-V13-02 | `8c977118` | 必查1/2 收口复验：HEAD:docs/TRACEABILITY.csv=64 行、含 PSF/REJ 的行数=0；df16f008..8c977118 共 60 文件全部为审计层，无追溯面改动 |

**工作树态**：MATRIX csv/json 与 HEAD 逐字节一致（python 比较 `git show HEAD:f` == 磁盘文件 → True/True）；docs/TRACEABILITY.csv 与 HEAD 内容一致（M 态仅换行归一化，`git diff HEAD --` 空输出）。

### 0.4 引用计数口径（本层统一，裸数字均引此口径）

对每条编号：python3 读入全部 tracked 文件（4,840 个，去 §0 的 8 个 ./排除前缀），`(?<![A-Za-z0-9-])ID(?![A-Za-z0-9-])` 全字边界计数；宿主分类 REG=三张追溯面、GEN=v19r3_traceability.py、SELFREP=reports/v19r2|v19r3、TRUTH=lib/include/cli/runtime/providers/modules/tests/ci/tools/scripts 真源面。「nonreg」=除 REG+GEN+SELFREP 外命中。0 即「编号无宿主」。复算脚本：`问题扫描/_verify/scripts_v13_mint.py`（机制A）+ /tmp/v13_{evid,rows,census,diff,led}.py（本层工作脚本，输出摘录均随条目内嵌）。

---

## 1. 机制A · 生成器以字符串模板铸造编号（穷举结果）

全 tracked py/js/sh/ps1/cmake/txt 扫 f-string/拼接/%/JS模板 四形态：

| 铸造点 | 编号族 | 枚数 | 先报关系 |
|---|---|---|---|
| `tools/quality/v19r3_traceability.py:263` `f"SCI-NOISE-{i:03d}"` | SCI-NOISE-001..015 | 15 | L28e-E-001 只报 TEST 侧；**SCI 侧本层新计数（§3-A）** |
| 同 :274 `f"SCI-UPM-{i:03d}"` | SCI-UPM-001..010 | 10 | 同上 |
| 同 :294 `f"TEST-PR-UPM-{i:03d}"` | TEST-PR-UPM-001..010 | 10 | L28e-E-001 在册；本层补 2 对重复载体（§3-C） |
| 同 :311 `f"TEST-UPMW-{i:03d}"` | TEST-UPMW-001..007 | 7 | L28e-E-001 在册 |

负结果（穷举为 0）：**DISP- 无任何脚本铸造**（只存在于 docs/algorithms 文本）；f-string 反序形/百分号形/JS 模板形/拼接形铸造=0。非铸造噪声（不报）：provenance.py:438/:591 与 science_contract_lint.py:46 的 f-string 是报错文案；test_cli00* 的 `b"SCI-DATA-"+os.urandom` 是负样本填充；tests/traceability/fixtures/** 是校验器故意坏 ID 的负样本。

**机制A总判**：全仓「模板铸编号」仅 v19r3_traceability.py 一器四族 42 枚。但铸 ID 不依赖脚本——c3452d48 手工把 5 枚自铸 EVID 直接写进两张矩阵（V13-N-01/02）⇒「铸」的实现形态不重要，**登记面对 ID 零承载要求 + status 零消费**（V13-N-04）才是循环得以闭合的机器面根因。

## 2. 机制B · VERIFIED 行的指针四形态（自指/指文档/不存在路径/三列皆空）

### 2.1 三张表的 VERIFIED 与门消费关系（实测）

| 表 | VERIFIED 行（M-V13-01 时点） | 读该列的门 | 判定 |
|---|---|---|---|
| docs/TRACEABILITY.csv | status=VERIFIED 63/63 | **无**。三消费方 tools/quality/check_traceability.py、tools/quality/contracts/check_traceability.py `grep -n VERIFIED`=0 命中；tools/check_traceability.py（TRACEABILITY-CODE）读的是**另一张表**（claim_id/status∈{ACTIVE,RETIRED} 词汇表），不读本文件 | status 列恒不被消费、恒不会因假而红（⇒V13-N-04） |
| MATRIX JSON | evidence_status=VERIFIED 6 行 | check_traceability_matrix.py：EVID 层无路径列；C5 只查唯一；C8 authority=在 evidence/reports(/returns) 目录**文本命中**即可，且 ci/checks.json 的 command **无 --strict** ⇒ WARN-only | 6/6 行 EVID 在自身 authority 目录零承载也结构上不可红（⇒V13-N-01） |
| MATRIX CSV | evidence_status=VERIFIED 2 行（自铸，JSON 侧 MISSING） | 仅 C 系奇偶校验消费，且被 BOM 挡住从未执行到（V6-N-01b 在册） | 分叉 5 行现存（V6-N-02 延续） |
| reports/v19r2/file_audit_inventory.csv | review_status=VERIFIED **713/713（100%）** | 无门；生成器 update_audit_status.py 未注册 CI | ⇒V13-N-03 |

### 2.2 逐行形态判定

- **TRACEABILITY.csv 63 行**：implementation_files/test_files 路径 worktree 实测全部存在（0 IMPL_MISS / 0 TF_MISS / 0 TF_DOC / 0 ALL_EMPTY）——行级指针无假；假在 ID 宿主与 status 语义（§3、V13-N-04）。
- **MATRIX 双视图分叉 5 行**（python 逐列比较输出，HEAD==worktree）：calibration/noise-snr/photometry 三行 JSON 侧=自铸 `EVID-P1-{CAL,NOISE,PHOT}-001`+VERIFIED 而 CSV 侧=MISSING；star/star-psf 两行 CSV 侧=自铸 `EVID-P1-{PSF,STAR}-001`+VERIFIED 而 JSON 侧=MISSING。**「自指」形态：自铸 ID 唯一宿主=自己那行**（refs 表见 V13-N-01）。
- **「指向不存在路径」形态：5 行 test_path 被写成散文**（详 V13-N-02）。
- **「指向文档」形态**：MATRIX 其余 TEST-VERIFIED 行 test_path=docs/algorithms/*.md(::TEST-*-DESIGN-001)（cosmetic/drizzle/hips/wcs/gaia/integrate/reject/session/write/resample2/wcs/writer/star 等）——**归 M6b-G-001 既有总述（TEST 层 18/22 行锚 docs/）不重报**；机制注脚：C7 对 `path::SYMBOL` 只要求 SYMBOL 以词边界出现在 path 指向的文件文本里（:286-296），文档提一下 ID 即"可解析"，故这类行**恒绿**——这是 M6b-G-001 得以长存的机器原因，作为其判词的机器根因补强挂 related。
- 三列皆空形态：本批登记面内 0 命中（TRACEABILITY.csv 无空列；MATRIX schema C3 禁空单元格）。

## 3. 机制C · 编号已铸、实体消费者为零（登记面行集口径）

**A. 42 枚铸造行逐枚 nonreg 计数**（口径 §0.4；输出摘录）：
```
SCI-UPM-002..010 : 9/9 枚 nonreg=0（SCI-UPM-001 nonreg=178，多系登记快照/审计档案互指）
SCI-NOISE-004/006/009/010/012 : nonreg=0；002/005 有真源注释命中（p1noise_oracle.hpp 等），
              003/007/008/011/013 仅 reports/v19r7_quality 审计文案，014 docs/standards，015 docs/modules
TEST-PR-UPM-002..010 : 9/9 枚 nonreg=0（-001 仅 v19r2 自报 + v19r7 审计文案）
TEST-UPMW-002/003/005/006/007 : nonreg=0；-001 nonreg=5（含 docs/science/PHASE2_UPM.md），-004=1（production_call_paths_stage2.csv）
```
**42 枚中 28 枚登记面之外零宿主**（SCI 侧 14 + TEST 侧 14；TEST 侧 16 枚由 L28e-E-001 定稿，不重报，SCI 侧 14 枚与「表行集」口径为本层增量）。related：L28e-E-001、簇 4 补二（八级阶梯第 7 级 19.2% 系注释面口径，与本表行面口径**不可相加混用**）。

**B. VERIFIED 语义 ≠ 要求被验**：15 行 SCI-NOISE-i 的 test_ids 一律 `TEST-SNR-001`（15 个"独立要求"共享 1 个测试锚），10 行 SCI-UPM-i 一律同 4 个 gtest 名（S0…/S1…/S2…/SaveOpenRoundtripAndHash）。铸造行 VERIFIED 的实算判据仅为「路径存在+符号在实现文件+函数名在测试文件」（main() 逐句确证）。

**C. TEST-PR-UPM 两对重复载体（新实例）**：生成器 pr_tests 列表字面重复（:283-292）→ `TEST-PR-UPM-005`≡`-008` 同指 `TEST(Phase2Upm, UpmPersistRoundtripChainNoDrift)`@synthetic_gate.cpp:5214，`-006`≡`-009` 同指 `InsertionOrderIndependent`@:5260；10 行 VERIFIED 实际只有 8 个不同测试实体；行↔实体唯一性无机器检查（C5 只管 module_id/src_id/evidence_id）。

---

## 4. 必查1 · dce8abd4 / c3452d48 新铸编号登记核对

净新增 ID 集（added−removed 行级 diff，正则 (TEST|EVID|SCI|ALG|DATA|DISP)-…，摘 /tmp/v13_newids.py）：
```
dce8abd4  +3 = ALG-DRZ-001 / ALG-P2-SMP-001 / SCI-UPM-001 —— 均为既有编号正文引用（多宿主定点读确认）⇒ 无新造未登记
c3452d48 +11 = 5 枚自铸 EVID（2 CSV 面=V6-N-06 在册复核一致；3 JSON 面=V13-N-01 新案）
              + TEST-CAL-001（既有 ID：docs/TRACEABILITY.csv:20 行在用、TEST_MATRIX.md 在册 ⇒ 合法并入）
              + "SCI-PHOT"（title 文案切片噪声，非编号）
              + TEST-{P1PHOT-FIXGATES,P1STAR-MAD,P1PSF-PRODPATH-CENTROID,IPV-EXTRACT-WCS-SIP-FAILCLOSED} ×4
                ＝ ci/checks.json 检查名 CTEST-* 的子串切片（各恰 1 处登记、目标真实）⇒ 非铸造问题
```
**登记面核对结论**：contracts/schemas/traceability_matrix.schema.json 对 EVID 仅格式正则；docs/standards、TRACEABILITY_SPEC.md、LAYERS.csv 均无「EVID 须对应落盘证据件 / 编号先登记后使用」条款（SPEC grep 生命周期/登记 → 仅 :88「只登记不判重」、:112 豁免行）。⇒ **本批 5 枚自铸编号在制度面上"未违反任何登记条文"，因为不存在承载校验条文可违**——这本身即交付缺陷（假 VERIFIED 无门可拦），入 V13-N-01/04 判词。
**SUPERSEDED_BY 落点**：详见 V13-N-07。

## 5. 必查2 · V6-N-01 删行的对应登记动作

c3452d48 自 docs/TRACEABILITY.csv 净删 4 行（unified=0 diff：+0/−4）：SCI-PSF-001 / SCI-REJ-001 / SCI-INT-001 / SCI-ACR-EQUIV-001。逐面核登记（M-V13-02）：
```
CHANGELOG.md / memory.md / docs/backlog/       → 三级复核 0 命中
问题扫描/40_OWNER_DECISIONS.md                 → 0 命中（非负责人裁决删行）
账本 M3b-F-02/M3-E-001 fix_note/verified_note  → 未提删 4 行（只述"三域订正"），而 verified_state 已回填 VERIFIED（df16f008）
生成器 v19r3_traceability.py CONTRACTS         → 四 ID 0 命中 ⇒ 重跑即永久缺失
docs/contracts/INDEX.yaml :42/:74/:82/:122     → SCI-PSF/REJ/INT-001 status=ACTIVE、SCI-ACR-EQUIV-001=DORMANT（合同注册表仍当它们在用）
CONTRACT-GRAPH 门(waivable:false)              → 只验 INDEX.yaml 内部一致（唯一/格式/path 存在/up-downstream 双向），与 TRACEABILITY.csv 无 ID 集对账 ⇒ 两表互斥恒不红
CON-TRACEABILITY @8c977118                     → 判据内存复算：63 行 requirement_id 拼接串含 "PSF"/"REJ" 者 =0 ⇒ 仍 TRACE-CORE-MISSING×2 红（V6-N-01a 之红至今未修）
```
**判定：删行无任何对应登记动作**；且删除使合同面（INDEX.yaml ACTIVE）与追溯面（行消失）永久分叉、无门对账。**入 V13-N-06。**

---

## 6. 假 VERIFIED 清单（汇总表；机制注：全部"结构上不可红"）

| # | 编号/行 | 宿主面 | refs(§0.4) | VERIFIED 的实际依据 | 门消费 | 本层条目 |
|---|---|---|---|---|---|---|
| 1 | EVID-P1-CAL-001 | MATRIX **JSON**（MOD-…-calibration，evidence_status=VERIFIED） | 1（唯一宿主=自己那行） | 无（EVID 层无路径列；C8 非 strict WARN；authority 目录零命中） | 不可红 | V13-N-01 |
| 2 | EVID-P1-NOISE-001 | MATRIX JSON（noise-snr 行） | 1 | 同上 | 不可红 | V13-N-01 |
| 3 | EVID-P1-PHOT-001 | MATRIX JSON（photometry 行） | 1 | 同上 | 不可红 | V13-N-01 |
| 4 | EVID-P1-PSF-001 | MATRIX CSV（star-psf 行） | 1 | 同上（且 JSON 侧仍 MISSING，CSV 手工态） | 奇偶校验被 BOM 挡住未达 | V6-N-06 在册，本层复核 |
| 5 | EVID-P1-STAR-001 | MATRIX CSV（star 行） | 1 | 同上 | 同上 | V6-N-06 在册，本层复核 |
| 6 | EVID-BLD-003 / IO-001 / CPU-001 | MATRIX JSON 3 行 VERIFIED | 5/3/5，非登记面仅 lib/phase1_session/memory.md 文本提及 | EVID 自身 authority 目录 evidence/reports/returns **零命中**（returns/ 不存在） | C8 WARN-only | V13-N-01 附带 |
| 7 | SCI-UPM-002..010（9 行） | docs/TRACEABILITY.csv | nonreg=0 | 生成器行内锚存在性（4 个共享函数名） | status 列零消费 | V13-N-04/05 |
| 8 | SCI-NOISE-004/006/009/010/012（5 行） | 同上 | nonreg=0 | 15 行共享 1 个 TEST-SNR-001 锚 | 同上 | V13-N-04/05 |
| 9 | TEST-PR-UPM-002..010（9 行）+ TEST-UPMW-002/003/005/006/007（5 行） | 同上 | nonreg=0 | 函数名在测试文件即 VERIFIED；-005≡-008、-006≡-009 双行同实体 | 同上（ID 侧 L28e-E-001） | V13-N-05-C |
| 10 | file_audit_inventory.csv review_status | reports/v19r2/** | —（713/713=100% VERIFIED） | 行类别查表（shipping_src→R1-v19-carry 等 5 轮字符串） | 无门、生成器未注册 CI | V13-N-03 |
| 11 | MATRIX calibration/noise-snr/photometry 三行 test_status=VERIFIED | MATRIX JSON | test_path=散文串，isfile=False | c3452d48「锚订正」把路径列写成非路径 | C7 内存复算=DANGLING_REF（门当前红于 BOM，两红叠加） | V13-N-02 |

合计：登记面「VERIFIED 声明」**27 行（TRACEABILITY.csv 铸造行）+ 5 行 evidence + 3 行 dangling test + 713 文件行** 依赖自证或零消费；全部有 §1–§5 机器面根因。

## 7. 反向钉死专查 + 通过项（明确不报，防误伤）

**专查「把旧错误口径当权威改回去」——本层 0 命中**：CSV 视图 3 行滞留的 docs 锚旧写法是「未跟进」而非「回改」；生成器 CONTRACTS 未恢复任何被 c3452d48 判废的行；SUPERSEDED 字样只落 CSV 视图（把废止视图当落点）已由 V6-N-02 定性，本层仅追加"重跑蒸发"实证（N-07）。V6-N-08c frame_qc_grid 重钉 4 位 1.4826 在册不重报。
**通过项**：① dce8abd4 净新增 3 ID 全有宿主（§4）；② 4 个 CTEST-* 检查名各恰 1 处登记、目标真实存在；③ TEST-P1-{NOISE,PHOT,STAR,PSF}-001 多宿主真源在册（V6-N-06 对照一致）；④ TRACEABILITY.csv 63 行文件级指针全解析（无指不存在路径/无三列皆空）；⑤ tests/traceability/fixtures 坏 ID 为负样本设计；⑥ DISP- 无脚本铸造。

## 8. 需运行期或未决

1. TRACEABILITY-MATRIX / CON-TRACEABILITY 的完整红灯集合须 CI 实跑定案（本层判据内存复算；BOM+3×DANGLING+parity5行+TRACE-CORE2键 各红属并集）。
2. file_audit_inventory/batch_summary 进入 v19r2~r4 发布审核包后的对外效力——包构建链逐跳核（build_v19r3_package.py:191、build_v19r4_package.py:282/:311 已核引用，包内容未逐件开）。
3. SCI-UPM-001 nonreg=178 的宿主质量分层（登记快照 vs 活性引用）未拆——artifacts/prerelease_v5/tables/TRACEABILITY.csv 等系历史快照。
4. ci/reconcile_state.py（控制包 TASK_STATE/STATE_RECONCILIATION 面）与 LAYERS.csv 未入本轴——非 TEST/EVID/SCI/ALG/DATA/DISP 登记面。

## 9. 一句话总判

全仓自造 ID 机制唯一实体是 v19r3_traceability.py（4 族 42 枚，28 枚登记面外零宿主），但循环证据的真正机器面根因有三：① EVID 层无路径列+C8 非 strict ⇒ **evidence 状态不可红**（5 行自铸 VERIFIED 横贯两张视图，6/6 VERIFIED 行在自身 authority 目录零承载，returns/ 不存在）；② status/review_status 列在全部三张登记表中**零门消费**（docs/TRACEABILITY.csv 63/63 VERIFIED 无人读；file_audit_inventory 713/713 VERIFIED 由类别查表铸造，且随 build_v19r3/r4 进审核包）；③ 合同面与追溯面无 ID 集对账（4 行被无声删除，INDEX.yaml 仍 ACTIVE，生成器已失记，重跑永久丢）。c3452d48 的「锚订正」以自铸编号+散文路径列+错面回归引用交付——它修的是"锚指向"，没有修"VERIFIED 何以为真"。修复最小集（写门）：VERIFIED 行的 ID 必须有非登记面宿主 ∧ EVID 层加证据件路径列并入 C7 ∧ status 列至少被一个 ERROR 级判据消费 ∧ INDEX.yaml↔TRACEABILITY 双向对账。

## 附：本层新缺陷条目（V13-N-01 … 07，判词全文见对应 §）

- **V13-N-01（P1·无据 VERIFIED 第八机制）**：MATRIX JSON 权威侧 3 行自铸 EVID（CAL/NOISE/PHOT-001）+ EVID 层结构性不可红。related：V6-N-06（CSV 侧 2 枚同机制）、M6b-G-001、L28e-E-001、簇 4 补二。
- **V13-N-02（P1）**：c3452d48 把 3 行 MATRIX JSON test_path 写成散文串（isfile=False）→ C7 必 DANGLING_REF；fix_note「改指真实可执行载体」与判定器口径互斥；regression_test 引用的 check_traceability.py(rows=63) 不读 MATRIX test_path。related：V6-N-01b（双红叠加）、M3b-F-02。
- **V13-N-03（P1）**：update_audit_status.py 按类别批量铸 review_status=VERIFIED（713/713）+7 列 *_ok=PASS+findings=0；batch_summary 数字列硬编码 0/"PASS"；三铸造工具（update_audit_status/check_source_inventory/v19r3_traceability）均无 ci/checks.json 登记；inventory 经 build_v19r3/v19r4_package.py 进审核包。related：簇 2（M3-G-002）、M8-G-001。
- **V13-N-04（P1）**：docs/TRACEABILITY.csv 63/63 VERIFIED 的 status 列全仓零门消费；42/63 行系生成器范围铸造（生成器不在 CI）；VERIFIED 判据=锚存在≠要求被验（15 行共享 1 TEST-SNR-001）。related：L28e-E-001（TEST 侧 16 枚定稿）、M6b-G-001、§2.1。
- **V13-N-05（P2）**：登记面行集口径 28/42 枚非登记面零宿主（SCI 侧 14 枚为本层新案）+ TEST-PR-UPM 两对重复载体（4 行→2 实体）。related：L28e-E-001、簇 4 补二（口径不可加）。
- **V13-N-06（P1·必查2）**：删 4 行无任何登记动作（CHANGELOG/负责人决策/账本 fix_note 零提及；生成器永久失记；INDEX.yaml ACTIVE 悬挂；双面零对账门；CON-TRACEABILITY 至 8c977118 仍红）。related：V6-N-01a、V6-N-10、L28c（废止 ID 填充对偶）。
- **V13-N-07（P2·必查1）**：自述「两个非权威 CSV 加 SUPERSEDED_BY」——全仓 `SUPERSEDED_BY`=0 命中；实际=MATRIX CSV :11/:12 两行 notes 的 `SUPERSEDED:` 字样，docs/TRACEABILITY.csv 零标注；且字样不在 JSON notes，gen_traceability_csv.py（JSON→CSV 逐列）重跑即蒸发——声明的宿主在被废止视图上。related：V6-N-05（同提交文案自斥族）、V6-N-02。

（第 0 步骨架于 M-V13-01 建立并回读 32 行；本档案分片进度：骨架→N-01..03→N-04..07→终稿。）
