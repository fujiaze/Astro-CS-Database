# V17 · fail-open 前置缺失全仓普查（从 V15-N-07 外推）

- 轴主：本 subagent（V17）｜派发人：session-9db01ebd（前台）
- 普查时点：HEAD 417775b94cd404688d8ab23ec3dca366dd899907（"git --no-optional-locks rev-parse HEAD"，2026-09-14T17:4xZ；扫描期间前台并发推进，条目行锚以本时点现文为准，均同时给函数名锚）
- 纪律：纯静态（read/glob/grep + 本目录只读 python3；未运行任何测试/编译/cmake/ctest/"ci/run.py"/仓内脚本）。结构化文件（ci/checks.json、CMakePresets.json）一律 json 解析器读取。
- 统计脚本（只读，可复跑，均在 问题扫描/_verify/）：scripts_v17_censusA.py（三形态站点普查）、scripts_v17_censusB.py（except-pass 与 exists-guard-return 普查）、scripts_v17_envnames.py（env 变量名清点）、scripts_v17_gates.py（checks.json 门→脚本映射）、scripts_v17_negpos.py（C++ 读文件+npos 交集）、scripts_v17_selfcheck.py（selfcheck 家族 fork/rc 分类）、scripts_v17_checkfields.py、scripts_v17_linuxbuild.py、scripts_v17_ipv.py；中间结果 scripts_v17_censusA.json、scripts_v17_censusB.json。

## §0.1 准绳与种子

1. 准绳顺序：①负责人设计意图（40_OWNER_DECISIONS.md；C-16 禁文本命中代行为断言）②现行冻结文本（改后）③代码/测试自洽。「与旧文档不一致」不当缺陷；本卷专查「把旧错误口径当权威改回去」⇒ 结论：**未发现新站点**（负结果，见 §3）。
2. 种子（不重报，只挂 related）：**V15-N-07**（io_adapter_test.cpp:214-221 无 is_open ⇒ 5 条 "== npos" 假绿）、**test_fits_stream_contract.py:31-32**（"if LIB_SO.exists(): return"，本机 .so mtime=2026-09-03）、**V4-N-05**（CHECK(rc!=0) 对子进程 127/126/信号同样成立）、M8-F-002/M8-F-005/M8-F-007/M8-F-008/M8-F-014、FD-F-001/FD-F-002/FD-G-004、M5a-G-008、M5b-G-01、M5b-G-11、L23-001/002/003、V15-N-01/02/05/15、V9-N-17、M6b-L18-003、M3-F-001（类级 skipUnless(g++)）、簇 1 机制①⑤。
3. 免重报核对：已读 SUMMARY.md 簇表、INDEX.md §三；账本 FIX_LEDGER.jsonl 655 行（2026-09-14T17:4xZ，"grep -c '' 账本"）逐词 grep 复核：lib/cpu、test_p1003、mon003_synthetic、child_rc、iso_acr、skip_compile、compile_commands 命中数均 **0**（输出见 §2 各条 L3）。

## §0.4 排除清单（口径 ./x，普查脚本 EXCLUDE 逐字）

普查 A/B 语料排除：./问题扫描/ ./run/ ./build/ ./out/ ./Testing/ ./logs/ ./__pycache__/ ./.git/ ./AstroCS.wiki/ ./GaiaDR3/ ./GaiaDR3SP/ ./BASS DR3/ ./astrocs_p1sess_neg/ ./astrocs_p1sess_perf/ ./astrocs_p1sess_props/ ./astrocs_p1sess_test/ ./third_party/。
条目选址另排除（历史/登记面，非现行执行面）：./engineering/control/archive/（普查 A 未排该目录；其命中 test_wcs_closure.py 三条已在选址时排除，不作条目）。
语料根：tests/ ci/ tools/ lib/ runtime/ providers/ cli/ modules/ cmake/ scripts/ packaging/ engineering/；排除后 **2578** 个文件（.py/.c/.cpp/.h/.hpp）；shell/yaml/CMake 面另用 grep 直扫，不计入 2578 口径。

## §1 规模计数（口径：上述语料与时点）

- py：exists/skipUnless/skipIf 行 **310**；assertNotIn/assertIsNone 行 **196**；getenv/environ.get 行 **110**；"except…: 下一行 pass" **53** 处；"if exists 守卫 → return/continue" **78** 处。
- cpp：ifstream/fopen 行 **323**；"==/!= std::string::npos" 行 **575**；getenv 行 **129**。
- 命中数≠缺陷数：逐条读上下文后本卷只登记蕴含不成立的 **8** 条（宁窄而实）。C++ 侧「纯负向 == npos + 读仓内源文件 + 无 is_open」组合全仓仅 io_adapter_test 一站（=种子，不重报；负结果见 §3-2）。

## §2 条目（8 条）

### V17-N-01 ｜①（子进程成功性前置缺失 ⇒ 必败验证接受"没起来"）｜P1 ｜selfcheck 家族 10 TU / 16 门
- 位置：11 个 fork 式 selfcheck TU 中的 **10 个**：tests/unit/p1wcs/p1wcs_tests_selfcheck.cpp（2 门）、tests/unit/aio_abi_selfcheck.cpp（3）、lib/calibration/tests/p1cal/p1cal_tests_selfcheck.cpp（1，:58-70）、lib/cosmetic/tests/p1cos/…（1）、lib/dynamic_psf/tests/p1psf/…（1）、lib/snr_estimator/tests/p1noise/…（1，:62-78）、lib/phase1_session/tests/p1sess/…（2）、lib/photometric_calib/tests/p1phot/…（2）、lib/star_detector/tests/p1star/…（2）、lib/astro_image_io/tests/p1hips/p1hips_tests_selfcheck.cpp（2，:81-90 与 :94-102）。注册面 **13** 个 add_test(NAME *selfcheck)（全仓 CMakeLists grep），其中 p2hips_selfcheck 与 ipv_abi_layout_lock_selfcheck 不属本形态。
- 断言原文（p1noise 为例，逐字）：
  > "waitpid(pid, &status, 0);" / "const int child_rc = WIFEXITED(status) ? WEXITSTATUS(status) : -1;" / "if (child_rc == 0) { … 注入机制失效 … return 1; }" / "SELFCHECK phase2: fault-inject '%s' → child rc=%d (必败验证通过)"
- 缺的前置：**「子进程正常退出且失败可归因于注入」**。判据只断 child_rc != 0，而下列四类全部非零 ⇒ 全被记成"必败验证通过"：①execve 失败 _exit(127)（10/10 TU 同型）；②信号终止 → -1；③fork 失败：变体 A（p1hips/p1phot/p1star/p1sess/p1wcs/aio_abi 的 helper run_injected_child "return 127"）被调用方当注入成功（p1drz 的 127 在主函数级=红，另计）；④组名漂移 → 子进程 run_injected_group 兜底 "return 127"。
- 失败偏向：**绿**。127 同时是"子进程没起来"哨兵码与被期望的"非零失败"证据——**码值碰撞**使"注入机制失效"（selfcheck 本应抓的缺陷）恰好表现为通过。脚本 scripts_v17_selfcheck.py 逐文件输出：10 TU 的 child_rc==0 门合计 **16** 处、"rc == 127" 分类命中 **0**（"127-classify=0 gates=N" 全行）。
- 默认 CI 触发：selfcheck ctest 目标在册随 CTEST 面执行；假绿不需要特殊环境——注入通路任何损坏即静默通过。三 profile 无门控 env 差异。
- 同仓正对照（改法对齐对象）：p1drz_tests_selfcheck.cpp:88-99（同族唯一 "} else if (rc == 127) { fprintf(…execve 失败…); ++failed; }"）；cli/commands.cpp:306-316（spawn_failed/未退出/exit_code 三分类，注释自述"非静默"）；tests/unit/cli_process_test.cpp:113（CHECK(r.spawn_failed || (r.exited && r.exit_code == 127))）。
- 改法：必败门统一改 "WIFEXITED && WEXITSTATUS==1（组失败码单源）&& 子进程 stdout 含 FAULT_APPLIED:<注入名> 标记"；fork 失败返回与组失败码不碰撞的哨兵（如 -2/126）；照 p1drz 写法回灌全部 10 TU（并入 M8-F-008 的"回灌公共头"处置）。
- 同族：related **V4-N-05**（同一 rc!=0 形态的账本实证）、**M8-F-008**（同 harness 未知组 fail-open，不同函数）、V15 乙档（其判"注入宏不求值 cond"与本条"必败判定接受没起来"为同族两个独立断点，不并条）。

### V17-N-02 ｜②（门扫描面无存在性前置 ⇒ 死路径静默零扫）｜P1 ｜SERIAL-HARDCODE 门（含同型两站）
- 位置：tools/check_serial_hardcode.py::main（PROD_FILES :17-21；静默跳过 :41；恒绿出口 :79-82）。注册 ci/checks.json::SERIAL-HARDCODE（checks.json json 解析实测：profiles=fast/linux-main/windows-main，waivable=False）。
- 断言原文：
  > "files = [base] if base.is_file() else (base.rglob("*.cpp") if base.is_dir() else [])"
  > "print(f"QA-002_PASS: 无 workers=1/nside=2048/OMP 线程数硬编码…")" / "return 0"
- 缺的前置：PROD_FILES 每条的**存在性/非空**判定（无 isdir 断、无实扫计数>0 断）。
- 当前实证（非假设）："lib/cpu" **不存在**："ls: 无法访问 'lib/cpu'"；"git --no-optional-locks log --all --oneline -- lib/cpu" 零命中（本仓 2031 commit）⇒ 该条目自登记起静默零扫；其余 7 条现存（DIR/FILE 逐条实测）。真实 CPU 选路域 lib/backend_host 不在扫描面：cpu_routing.cpp:180/187、host_services.cpp:116（max_workers = 1）、worker_advisor.cpp:239 均不被 WORKER_ONE_RE 扫到。
- 失败偏向：**绿**（目录改名/删除 ⇒ files=[] ⇒ errors 不增 ⇒ QA-002_PASS 照打，且 PASS 行不披露实扫数）。
- 默认 CI 触发：**是**——三 profile 恒执行、非豁免；lib/cpu 死条目在每次 CI 静默。
- 改法：逐条存在性断（"if not (REPO/d).exists(): errors.append("PROD_FILES 死路径 "+d)"）+ 收尾"实扫文件数 ≥ 声明下限"；同仓正对照 tools/check_module_readmes.py:24-26（"if not p.is_file(): errors.append(f"{rel} missing")" ⇒ 红）。扫描面并 lib/backend_host（与 M5b-G-11 的 providers 缺口一并处置）。
- 同型站点（潜在，当前路径尚在）：tools/check_unit_closure.py::check_docs（:78-79 "if not isdir(d): continue"——docs/science、docs/api 漂移即零扫；UNIT-CLOSURE 三 profile 在册非豁免）；tools/check_api_docs.py:184-186（"if not isfile(doc): return []" 函数登记表空⇒对账零项）与 :246-248（continue）——API-DOCS "双空输出制度化"（簇 1⑤/E4）与此同根。
- 同族：related **M5b-G-11**（扫描面不含 provider 已在册；本条新事实=**死路径已实际发生 + 无存在性前置**）、M5a-G-005、INDEX §3「边界校验做一半」（修复=对齐既有写法：check_module_readmes）。

### V17-N-03 ｜②/③（产物门控恒假 ⇒ 可达性验证在默认 CI 永不执行）｜P1 ｜test_p1003.test_04
- 位置：tests/cli/test_p1003_drizzle_path.py::test_04_prod_callgraph_no_hp_drizzle（:53-59 三层 skipTest）；宿主 UT-CLI 注册 linux-main、waivable=False（checks.json json 解析）。
- 断言原文：
  > "cc = os.path.join(REPO, "build", "compile_commands.json")" / "if not os.path.isfile(cc): self.skipTest("compile_commands.json 缺失(checker 依赖)")"
  > 文件内注释自证："纯 CMake 构建产物不含 CMAKE_EXPORT_COMPILE_COMMANDS 时该环境证据缺失, skip 而非 fail"
- 缺的前置：checker 输入（compile_commands.json）没有生成方前置。核链（L3）：①CMakePresets.json::linux-control 的 cacheVariables 仅 CMAKE_BUILD_TYPE/ASTROCS_ENABLE_ACR/ASTROCS_BUILD_TESTS（json 解析器读取）；②ci/steps/linux_build_root_graph.sh 全文（cat）只有 preset/cmake -S cli/cp 二进制，无该开关、无 compile_commands 落 build/ 的命令；③grep -rn EXPORT_COMPILE_COMMANDS build.sh CMakeLists.txt cmake/ 等 ⇒ 仅 tools/quality/extract_cpp_api.py:5 一条注释命中 ⇒ **CI 默认无任何生成方** ⇒ test_04 在每次 linux-main 恒走 skipTest ⇒ TU 级可达性校验（BANNED_CLI_INCLUDES 全集、Runtime owner 可达、ACR 零可达、cc 源完整性）**从未在 CI 执行，账面记 OK（skipped 不红）**。本机 build/compile_commands.json 存在=控制节点本地构建产物，恰证"本地能跑≠CI 在跑"。
- 失败偏向：**绿**（checker 自身 fail-closed（缺 binary/缺 cc ⇒ rc=2，:79-88 实测），但它永不被叫到）。
- 默认 CI 触发：**恒触发弱化路径**（结构性，非偶发）。
- 改法：preset 加 "CMAKE_EXPORT_COMPILE_COMMANDS": "ON" 并在 build 步 cp 到 build/（或测试直读 build/linux-control/ 树）；更稳=照 tests/cli/test_cli_single_install.py:38-44 范式："CI/GITHUB_ACTIONS 置位而前置缺 ⇒ raise AssertionError（该门恒 SKIP=未执行）；仅本地放行 SkipTest"。
- 同族：related **M5a-G-008**（"可达性只跑 selftest"在册——本条补第二层：UT 面真扫调用亦恒 skip ⇒ 可达性语义在 CI 无任何执行点）、**L23-003**（CLI-008 恒 SKIP 同型）、**V9-N-17**（三层静默同向）、簇 1 机制①⑤。
### V17-N-04 ｜②（编译失败→SKIP：自家输入坏了不红反隐）｜P1 ｜MON-003 合成资源门
- 位置：tests/backend/test_mon003_synthetic.py::setUpClass（:27-41）+ test_01/test_02（:50-51、:63-64）；宿主 UT-BACKEND（linux-main，waivable=False）。
- 断言原文：
  > "cls.skip_compile = r.returncode != 0"（:38，g++ 编译 mon003_synthetic_main.cpp + baseline_backend.cpp + host_services.cpp）
  > "if self.skip_compile: self.skipTest("编译失败")"（:50-51、:63-64）
- 缺的前置：把「被测合成 harness 编译失败」正确归为**失败**的前置——现把 rc!=0 转成 skip，"python3 -m unittest discover" 对 skip 退 0 记 OK。
- 失败偏向：**绿**。该门守 §10.5 资源利用合成判定（MULTI/SINGLE 五 kernel、workers=2、无 MULTI_FAIL）；当它链接的三个生产源与 harness 接口漂移（编译断）正是最需要红的时候，账面却是 skipped。
- 默认 CI 触发：正常 linux-main 有 g++（workflow 恒装依赖）⇒ 两测均执行；弱化路径在"接口漂移/编译失败/缺 g++"时触发——**触发即目标场景**。
- 改法：setUpClass 编译非零 ⇒ raise RuntimeError（类级 ERROR=红），仅保留 @skipUnless(which(g++)) 作环境类前置；同仓正对照：tests/backend/test_isa_bit_manip.py:32（setUpClass 内 "assert r.returncode == 0, r.stderr"——同为编译型夹具，编译失败=红）；tests/unit/p1wcs/p1wcs_astropy_cross.py 头注（"未设则判定 FAIL, 不静默跳过"）。
- 同族：related 簇 1 机制⑤（自宣跳过记 PASS）、FD-G-004（SKIP 制度化）、V9-N-17（exit77→SKIPPED 不看 waivable——本条是其测试侧对应物）。

### V17-N-05 ｜①/②（生产源清单逐条 if-exists 包裹负向断言 ⇒ 源漂移即静默脱扫）｜P1 ｜iso_acr 隔离测试
- 位置：tests/cli/test_iso_acr_gpu_isolation.py::test_01_production_sources_no_acr_gpu（:83-94）与 ::test_02_production_has_no_acr_register_call（:97-107）；类门 :41；宿主 UT-CLI（linux-main）。
- 断言原文：
  > test_01："for rel in PRODUCTION_SOURCES: p = ...; if not os.path.isfile(p): continue"（:85-86）→ 逐行扫禁词 violations，末尾 "assertEqual(violations, [])"
  > test_02："prod_cpp = [5 个路径]; if os.path.isfile(p): ... assertNotIn("register_phase2_acr_kernels", content)"（:102-106）
- 缺的前置：PRODUCTION_SOURCES 六条与 prod_cpp 五条的**存在性断**。文件改名/拆分/迁移 ⇒ 该文件整体脱扫 ⇒ violations 恒空=**绿**；违规符号若随重构搬到新文件名，双重绿（旧路径消失 + 新路径不在清单）。
- 失败偏向：**绿**（正向对照：若文件在而内容违例=红；缺的不是判据是前置）。
- 默认 CI 触发：当前 11 条路径全部存在（选址时逐条实测）⇒ 默认 CI 走完整扫描面；弱化为**结构缺陷+潜在**，与 V17-N-02（死路径**已**发生）同型不同度。另注意类门 :41 把 test_01/02 两个**纯静态文本检查**挂在 skipUnless(cmake ∧ EXE=build/cli/astrocs) 上——静态面被运行期产物门连坐：CLI 未建/后改路径 ⇒ ACR 文本隔离检查整体消失（该 shape 已由 M5b-G-01 证过"CLI 门验兼容树"）。
- 改法：清单逐条 isfile 缺失 ⇒ 计 violation（"源清单与实际树漂移"本身即 §10.1 隔离面失效信号）；test_01/02 拆到类级无 EXE 门的 TestCase（静态检查不需要任何产物）；清单改为由 git ls-files 动态派生+下限断言。
- 同族：related V17-N-02（同型）、M5a-G-008（ACR 休眠机器证明在册薄弱面）、M5b-G-01（EXE 兼容树连坐）。

### V17-N-06 ｜②（陈旧构件复用：if-exists 早退 + 无源↔产物新鲜度前置）｜P1 ｜.so 夹具四站家族（种子 fits 的三孪生）
- 位置（种子=tests/io/test_fits_stream_contract.py:29-32 不重报，其余三站新登）：
  > tests/io/test_hips_input_contract.py:54-56："def _build_lib(): if LIB_SO.exists(): return"（LIB_SO=runtime/io/libhips_core_test.so，:37）
  > tests/io/make_hips_fixture.py:43（"if not LIB_SO.exists(): 编译"——存在即复用，同无新鲜度判定）
  > tests/io/hips_output_fixture.py:48（同型）
- 缺的前置：**产物不早于源**的新鲜度判定（mtime/sha 均无）。实证（本控制节点，2026-09-14T17:3xZ，"ls -la runtime/io/"）：libfits_core_test.so 与 libhips_core_test.so mtime=**9月3日 03:05**，而 runtime/io/fits_core.c mtime=**9月7日 01:29**（其最近提交 6825cb53 @2026-09-07 01:36 +0800）⇒ 契约测试正对着比源旧 4 天的 .so 执行；.so 在 .gitignore:31（"*.so"）托管，无 CI 生成步 ⇒ 宿主上永远"存在即复用"。
- 失败偏向：**绿**（源改坏 ⇒ 测试仍对旧二进制绿）。宪章 §15.4 把"Linux 单元测试/真实数据终验"压在控制节点本机 ⇒ 弱化路径**在终验节点默认触发**（本机即活例）；hosted CI 全新检出无 .so ⇒ 现编现测（该面不弱化）。
- 需运行期、未判：旧 .so 与新源的行为差异是否存在（不跑不判）——本条只判**前置缺失**本身。
- 改法：去掉 exists 早退，改为无条件重编（单文件 gcc <1s）或 "max(源 mtime) > 产物 mtime ⇒ 重编"；最稳=编译期把源 sha256 写入符号、测试断 sha 一致（配 M8 系列"注入式反向锁"惯例）。
- 同族：related **种子 fits 站**（V17 轴已知实例）、L23-003/C-12（"对不存在的交付物下判"的镜像形态——对**陈旧**交付物下判）。

### V17-N-07 ｜③（环境变量门控核查 = 负结果 + 校准）｜结论条 ｜非缺陷登记
- 任务轴③要求"核默认 CI 是否真设该变量"。逐变量核链（消费者 grep 全集 110 行普查 + workflows/steps/preset 对照）：
  1. **ASTROCS_REPO**（C++ 消费 6 TU：cpu_provider:61/cpu_bench:74/core_pipeline:370/io_adapter:212/p2_ir_facade:24/p1_ir_facade:27）：**CI 不设，但 ctest 面已注**——tests/unit/CMakeLists.txt:730-734 "set_tests_properties(core_pipeline cpu_provider cpu_bench io_adapter p1_ir_facade p2_ir_facade PROPERTIES ENVIRONMENT ASTROCS_REPO=CMAKE_SOURCE_DIR)"，消费集与注入集**恰好相等**（6=6，L3 名单互证）；Python 侧由调用方自带（tests/backend 5 文件 env=dict(os.environ, ASTROCS_REPO=REPO)；tests/cli/cli_test_hygiene.py:33 setdefault）。⇒ **校准 V15-N-07**："ASTROCS_REPO 未设⇒恒绿"仅在**非 ctest 直跑**成立；ctest 下 io_adapter 的残余风险是"头文件迁名 ⇒ content='' ⇒ 5 条负断言仍绿"（缺 is_open，非缺 env）。修复重点应是 is_open+结构判据，**不是**再补 env 注入（已存在，勿重复改回旧错误口径——本卷 §3-1 专项）。
  2. **ASTROCS_TEST_BIN_DIR**：ci-linux.yml:45-49 job env 恒设 build/linux-control/tests/unit；唯一消费者 cmd_test_synthetic（commands.cpp:283）；windows 不设 ⇒ 其消费门（UT-BACKEND P2006 族）profiles=linux-main only ⇒ 无未触发缺口。fail-closed 面：spawn_failed⇒rc=127 计入 failed（commands.cpp:308-316）——此即"子进程成功性前置"的仓内金标准写法。
  3. **ASTROCS_CI_ALLOW_NO_GIT**（ci/run.py:1191）：默认**收紧方向正确**（未设且无 .git ⇒ EXIT_RUNNER_ERROR），无 workflow 设值 ⇒ 无弱化路径。
  4. **GAIA_ZLIB_CONTRACT_SOURCE/EVIDENCE_DIR**（test_gaia_zlib_configure_contract.py:211-216）：SOURCE 默认仓内 cmake 且 "if not is_file: raise AssertionError"、cmake 缺 ⇒ raise——fail-closed；EVIDENCE_DIR 只控证据落盘不控判定 ⇒ 不构成"有设才严"。
  5. **IPV_LAYOUT_PROBE**（ipv_abi_layout_lock.py）：probe 缺 ⇒ rc=2 硬失败；注册面走 ctest 目标（CTEST-IPV-ABI-LAYOUT-LOCK/-SELFCHECK，checks.json 解析 index 125/126）⇒ 前置由构建系统满足。
  6. **ASTROCS_CLI_BIN / ASTROCS_AIO_DIR / ASTROCS_MINGW_BIN / KEEP_ABI00X_WORK / TMPDIR 族**：均"env 优先+默认回退"，无"仅设置才检查"的语义。
- 轴③总结论：**本仓 env 门控无一处造成"未设⇒检查不存在"**；真实弱化载体统一表现为**文件/产物存在性门控**（N-02/N-03/N-04/N-05/N-06 五站）。跨条综合：**该仓库的 fail-open 不在变量面，在存在性面**——挂 INDEX §3「机器门结构性不红」簇 1 家族新机制：**"前置=被检物自身存在性"的门在源清单/产物清单漂移时静默缩面，且 PASS 文案不披露实扫数**。
- 需运行期、未判：windows hosted runner 是否真有 g++（skipUnless(which("g++")) 12 处类级门；M3-F-001 已在册，不新报）。

### V17-N-08 ｜①（负向普查校准注记：C++ "== npos" 全仓仅一站为纯负向假绿）｜结论条 ｜防邻站误修
- 普查：cpp 侧 "==/!= std::string::npos" 575 行 → 交集「读仓内源文件」= 52 文件（scripts_v17_negpos.py 名单）→ 逐站读上下文后：
  - **纯负向（读不到⇒全绿）且读仓内源**：仅 io_adapter_test.cpp:217-221（=V15-N-07 种子）。
  - 方向为**红**的同族（读不到⇒假红，缺 is_open 但非 fail-open）：p1_ir_facade_test.cpp（:47-49 "find("enabled")!=npos" 等）、p2_ir_facade_test.cpp（:29-38 "CHECK(n_nodes == 4)"）、p2002_unc_rej_prov_test.cpp（ASTROCS_REPO_ROOT 编译期锚，CMakeLists:357-359 目标正确）——**改法若统一"补 is_open"，这三站是诊断面修复，不能按假绿门报功**。
  - 有前置正例（勿动）：cpu_bench_test.cpp:78 "CHECK(fp != nullptr); if (fp) {…}"；mon001_recorder_test.cpp（先 CHECK(rec.write_all(...)) 再读产物）；core_pipeline_test.cpp:373-381（f.good() 探测 + CHECK(!base.empty()) + 缺⇒return 前已计 failures）；test_logger/test_orchestrator_cli 的负 npos 全部与正向 ASSERT_CONTAINS 同函数成对（读不到⇒对侧红）。
- 结论：**不存在"批量把 npos 站当 fail-open 改"的空间**；逐站方向判定以本条名单为准。related：V15-N-01/02/07、M8-F-002（修复本体真、锁不锁缺陷站点的教训反向适用——改前置时要红站别误报成绿站）。

## §3 专项负结果（按负责人指令登记"查过而无"）

1. **「把旧错误口径当权威改回去」**：0 命中。核查法：对 §2 全部 8 条涉及的现行口径（workers 选路域、MON-003 判据、.so 夹具、selfcheck rc、PROD_FILES）对照 40_OWNER_DECISIONS.md 现行裁决（R-14/§18.2 口径）与测试现文，未发现任何站点以废止口径为期望值。V17-N-07-1 的"ASTROCS_REPO 注入已存在"同时防了一类潜在回退式修复（再往 CI 加 env = 对已修点重复施工）。
2. **C++ 纯负向假绿全仓普查**：仅种子 1 站（见 V17-N-08）。
3. **③"有设才严"门控**：0 站（见 V17-N-07）。

## §4 汇总索引（条目→类别→去向）

| 条目 | 类别 | 一句话 | 登记建议 | related（唯一归属挂点） |
|---|---|---|---|---|
| V17-N-01 | ① | selfcheck 10 TU/16 门接受 127/-1=必败验证通过 | F_TEST_GAP/G_GOV_GATE p1 | V4-N-05、M8-F-008 |
| V17-N-02 | ② | SERIAL-HARDCODE 扫描面死条目 lib/cpu 静默零扫 | G_GOV_GATE p1 | M5b-G-11（同门不同事实）、簇 6/INDEX §3 |
| V17-N-03 | ②③ | test_p1003.test_04 compile_commands 恒缺⇒CI 永 skip | F_TEST_GAP p1 | M5a-G-008、L23-003、V9-N-17 |
| V17-N-04 | ② | mon003 编译失败→skipTest（要红时隐） | F_TEST_GAP p1 | 簇 1⑤、FD-G-004 |
| V17-N-05 | ①② | iso_acr 源清单 if-exists 包负断言⇒漂移脱扫 | F_TEST_GAP p1 | V17-N-02 同型、M5b-G-01 |
| V17-N-06 | ② | 陈旧 .so 复用三孪生站（fits 种子外扩） | F_TEST_GAP p1 | 种子 fits 站、C-12 |
| V17-N-07 | ③ | 变量门控负结果+V15-N-07 校准（注入已在 CMake 面） | 结论/校准 | V15-N-07（校准其修复方向） |
| V17-N-08 | ① | npos 全仓方向判定名单（防批量误修） | 结论/校准 | V15-N-01/02 |

## §5 需运行期、未判清单

- 陈旧 .so 与现源行为是否已分歧（N-06）；windows hosted g++ 存在性（N-07-6/M3-F-001）；13 个 selfcheck ctest 目标在真实 CI 日志中的 rc 分布（N-01 只判结构，不判现网红绿）；test_p1003 三 skip 在 hosted 日志中是否曾以 skipped 记账（结构推导恒 skip，未取日志=未判）。
