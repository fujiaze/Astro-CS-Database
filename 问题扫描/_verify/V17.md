# V17 · fail-open 前置缺失全仓普查（从 V15-N-07 外推）

- 生成时点：2026-09-14T17:35:46.299Z（UTC）；纯静态（read/glob/grep + 本目录只读 python3 + git --no-pager 只读查询），无编译器/cmake/ctest/测试运行。
- 语料：tests/**、ci/**、tools/**、lib/** selftest；排除清单见 §0.4。
- 准绳顺序：①负责人设计意图（40_OWNER_DECISIONS.md）②现行冻结文本（改后）③代码/测试自洽。
- 种子（不重报，挂 related）：V15-N-07、test_fits_stream_contract 实例（V15 系或前台已知）、V4-N-05（CHECK(rc!=0) 对 127/126/信号同样成立）、M8-F-002/M8-F-005、FD-F-001/FD-F-002、M6b-L18-003、M8-F-007、V15-N-05 等（见 V15.md §前置-2 种子清单）。

## 0.1 三类形态定义
- ①读文件/取子进程/取环境后缺存在性或成功性前置 ⇒ 否定式断言（== npos / assertNotIn / 空集合遍历）假绿；
- ②if os.path.exists(...): return / skipUnless / try-except-pass 式静默跳过；
- ③环境变量门控增强检查 ⇒ 核默认 CI 是否真设该变量。

## 0.2 站点清单（占位，随条目回填）

## 0.3 三级复核记录（每条 L1 粗筛 grep / L2 上下文阅读 / L3 交叉验证+门控环境核对）

## 0.4 排除清单
（见下，随扫描回填）

## 1. 条目正文
（待填）
