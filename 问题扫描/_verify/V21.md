# V21 · 「算了写了但无人据此行动」与「返回值/错误码被丢弃」全仓静态审

> 轴：V21（第 21 轴，与 V20 平行但判据对象不同：V20=参数不被消费；本轴=**产出侧不被消费**）
> 权威序（负责人明令）：①负责人设计意图 ②现行冻结文本（改后）③代码/测试自洽。
> 纪律声明：纯静态（read/glob/grep + 在 `问题扫描/` 内跑只读 `python3 -B` + `git --no-optional-locks` 只读子命令）；
> 未运行编译器/cmake/ctest（含 `-N`）/测试/二进制/`ci/run.py`；未运行任何无 `--dry-run` 的仓内脚本；
> 无 git 写；未连 Fatduck/Windows；无删除改名；档案只写本文件。
> 免重报：已先读 `问题扫描/SUMMARY.md` 簇表 + `问题扫描/INDEX.md` §三。已知实例一律挂 related 不重报：
> V10-N-05（bad_snr_rows 死码）、V9-N-08/N-16、V11-N-05/N-08（enum_fingerprint 无比较点）、
> V7-N-01/N-06/N-10、V15 全族（测文本不测行为）、C-14/C-15/C-16/C-20/C-21、E1..E14。

## 0. 时点口径

（待填：HEAD、checks 数、语料规模、排除前缀全量清单）

## 1. 四类站点表与规模数

### 1.1 类 1 · 返回值与错误码被丢弃

（待填）

### 1.2 类 2 · 状态位与统计量写了没人读

（待填）

### 1.3 类 3 · 产物面只写不读 / 两侧 schema 不一致

（待填）

### 1.4 类 4 · 判定输入恒为哨兵值

（待填）

## 2. 结论条（≤15 条，宁窄而实）

（待填）

## 3. 判否负清单（核过确为真消费，供其它轴免重报）

（待填）

## 4. 需运行期、未判

（待填）

## 5. 机器门建议：字段级"只写不读"与调用点"返回值被丢"能否同一 AST pass

（待填）
