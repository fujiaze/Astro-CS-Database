# W1 · 隔夜新代码零扫描面全审（复核组第二轮）

- 时点：HEAD `a3a343a44080d917089e1f8d548ed2d0400b0c61`（收工时 HEAD 漂至 `a20a1db8`，docs-only 不触代码锚；扫描期间工作区被并行编辑：`lib/plate_solve/cpp/ipv/{include/ipv_api.h,include/ipv_types.h,test/CMakeLists.txt}`、`ci/checks.json`、`lib/snr_estimator/include/astrocs/noise/types.h` 于 10:0x-10:3x 出现未提交改动/新增，涉到处均注"工作树态"）。区间 base `521095b8`→head 共 63 commit。
- 范围：① `问题扫描/_cache/recheck_round1.json`.changed 35 文件逐 hunk（审新逻辑本身，不审其修的旧 bug）；② `git status --porcelain -uall` 未跟踪新增源码全量（清单见 §A）；③ 新增注释可核验性抽检 12 处（配额 10）。
- 方法：纯静态（read/glob/grep + python3 -B 只读解析 + git --no-optional-locks 只读子命令）；零构建/零 ctest（含 -N）/零二进制/零脚本执行/零 git 写。
- 判词上限纪律：**13 条（6×P1 + 7×P2），宁窄而实**；全部挂 related 到 SUMMARY 簇表 14 机制或 INDEX §三主题，无新机制条目；另记 1 处自发正向（selfcheck 反向锁）。
- 准绳顺序声明：与负责人裁决冲突处按①判（W1-N-03）；"登记不了≠登记漏"之 V19 区分不适用于本轴 4 处"登记面零命中的裁决引用"（本轴裁决在 control-pack 之外亦零出现，按机制⑫ 立条并挂 A-44）。

## §A 覆盖清单与排除

**排除前缀（全量，未跟踪源码扫描时剔除）**：`./build/` `./run/` `./artifacts/` `./out/` `./问题扫描/` `./设计大纲/` `./工程控制/` `./third_party/` `./GaiaDR3/` `./GaiaDR3SP/` `./BASS DR3/` `./AstroCS.wiki/`

**tracked 35 文件 hunk 覆盖**：ci/checks.json(12 新门逐名对账)；DATA_SEMANTICS §13.4(与实现逐字段对表✓)；module_adapters.cpp(P14-N-08 块+P17 块+P21 建桶块，等价性静态核)；dynamic_psf/README✓；gaia_client.c/.h(有界解析全链)；drizzle_engine.cpp/.h+hp_drizzle_api.cpp/.h(P15a 常量提取/P17 新 ABI/P21/P22 流水线全 hunk)；p1drz 5 文件(CMake/两 shell 锁/tests_core 确定性块/thread_probe)；phase1/noise/README(发现 N-02)；ipv 头×3+源×2+测试×6+diag 工具+mirror(逐处)；tests/unit/CMakeLists+gaia 边界测试+p1001+p1snr C++/CMake。**未逐行深审（诚实声明）**：lib/phase1/tests/p1phot/ 四未跟踪件（判"双生分叉待收编"未评，见 §D）；module_entry.cpp 中段输出装配（抽查释放链完整）。

**未跟踪新增源码（10 文件，全量列出）**：
1. lib/snr_estimator/CMakeLists.txt（W1-N-03）
2. lib/snr_estimator/include/astrocs/noise/types.h
3. lib/snr_estimator/src/module_entry.cpp（W1-N-04）
4. lib/snr_estimator/src/astrocs_p1_noise.def
5. lib/snr_estimator/src/module_exports.map
6. tests/unit/p1_noise/adapter_test.cpp（W1-N-03③）
7. tests/unit/p1_noise/adapter_entry_impl.cpp（同）
8. lib/plate_solve/cpp/ipv/test/ipv_dead_params_lock.py（邻站在途，不判，见 §D）
9. lib/plate_solve/cpp/ipv/test/ipv_dead_params_manifest.json（同上，10:10 出现）
10. lib/plate_solve/cpp/ipv/test/ipv_dead_params_selfcheck.py（同上）
（另 4 件 lib/phase1/tests/p1phot/* 判"待收编双生面"入 §D；根目录 p1x-files.patch×5 与 reports/ 新 md 属他轴产物不判。）

## §B 新缺陷清单（13）

### W1-N-01 (P1) ipv 触顶判据重写基于错误前提，错误口径扩散到交付头并被测试钉死
- 新代码：ipv_select.cpp:482-491 弃 fmod 改 `n_ret >= cap_unit`，注释断言"gaia_client 每文件顺序截断使任一被截断文件恰返回 cap_unit ⇒ n_ret>=cap_unit 必有一文件触顶（k*cap 与 cap+partial 均被捕获，无漏判）"。
- 反证：gaia_client.c:1538/1607/1667/1736/1807/1875 —— MAX_STARS_RESULT=200000 是 **collector 全局（跨文件、每查询）硬顶**（add 即 return），非"每文件"；饱和时 n_ret 恒 ==200000，"cap+partial / k*cap"两态经真实客户端不可达 ⇒ V2-N-10 缺陷2 前提为假，旧 fmod==0 在可达集内不漏判；新判据默认参数下与旧等值（未变好），且反向引入新失败模式：`m_lim_gaia_cap_per_file` 配置 < 200000 时合法 n_ret≥cap 被误判触顶 → 首查询即 break、converged=false、m_lim_final 停初值（下游按过深星等投影）。
- 扩散：ipv_types.h:101 交付头注释同步改"n_ret >= 每文件上限"（错口径入公共头）；test_mag_iter.cpp 3b(600000)/3d(250000) 把不可达态钉成期望（若将来真改 per-file 语义，这两例的反向语义又会被锁死）。
- 挂 related：V12-N-11（200000 一具名三复制）、INDEX §三"反向钉死"。判据③：实现自洽性被注释虚构的机制破坏。

### W1-N-02 (P1) P14 新增注释虚构接口符号 p1_op_star_psf_json / p1_op_noise_json
- 声明处：lib/phase1/noise/README.md:71-73（"P14 同批新增两个测试专用直调钩子…供 parity 锁直调"）；tests/unit/p1snr/CMakeLists.txt:36（"端到端直调生产 op (p1_op_star_psf_json / p1_op_noise_json)"）。
- 三级复核缺失判定：L1 `git --no-optional-locks grep -n 'p1_op_star_psf_json\|p1_op_noise_json' -- . ':!问题扫描' ':!设计大纲'` → 仅 README/CMake 两处注释自身命中，零定义零声明；L2 工作树 raw grep 同 0；L3 同族检索：真实 op 为 `p1_op_star_psf`(:1828)/`p1_op_noise`(:2630)，无 _json 变体。且 p1snr_frame_parity_test.cpp 文件头自述"经**生产注册表** register_phase_modules 端到端跑真实节点"——与"直调钩子/生产注册表不变"互相矛盾。
- 挂 related：V8-N-01（凭据为不存在符号）、簇1 机制③（验证对象错位）。

### W1-N-03 (P1) 未跟踪 snr 模块迁移面整块零编译 + 其 CMake 注释与在案负责人裁决相反
- 事实链：① lib/snr_estimator/CMakeLists.txt:28 注"本子目录由根 CMakeLists.txt (hips 之后) 注册"——根 CMakeLists.txt:210 实为**注释掉的** `add_subdirectory(lib/snr_estimator)` + 明文 F-CI-002-01 裁决（f74fc20f：owner 裁决执行，"目录保持 untracked 锁死不收编"；:239 RPATH 摘除）⇒ 注释失实且与裁决冲突；② `add_library(astrocs_p1_noise SHARED)` 全仓唯一命中在该未跟踪文件 ⇒ target 不在任何挂载图，module_entry.cpp(≈1400 行)与 types.h 从未进编译器；③ tests/unit/CMakeLists.txt:1155 门卫 `if(TARGET p1noise_under_test AND TARGET astrocs_p1_noise)` 恒假 ⇒ tests/unit/p1_noise/{adapter_test,adapter_entry_impl}.cpp 不编译。
- 判词：按准绳①，该面当前形态与在案裁决相反；若邻站拟提交收编，须先走裁决变更（注释"由根注册"必须改掉，否则登记面继续自相矛盾）。
- 挂 related：机制⑪（零采集⇒必漂）、簇7（与裁决相反）。

### W1-N-04 (P1) module_entry.cpp JSON 整数字段无 isfinite/值域防护 → (int)NaN/Inf UB
- lib/snr_estimator/src/module_entry.cpp:447/449/463/465：`patch_grid_x/y、min_patch_samples、max_clip_rounds = (int)d`，d 来自 json_get_f64（:137-143 明确放行 nan/inf 字面量，理由仅为 scale_law alpha 直传 DISP-NOISE-005）。同文件 :429-434 对 h/w 已有 `<=0 || >0x7fffffff` 拒绝的正确示范 ⇒ 边界校验做一半；NaN/1e300 配置直转 int 属未定义行为，可静默进科学 cfg。dtype :411 走 u64 界内路径除外。
- 挂 related：簇6（新边界未继承同仓老纪律，12 行正对照就在同一文件）。静态可判形态，触发面需配置畸形 JSON（运行期未判实跑）。

### W1-N-05 (P1) 新跨 DLL 边界结构 HpAutoNsideResult 不带 struct_size/abi_version
- hp_drizzle_api.h:104-111（HP_DRIZZLE_API 导出，被 module_adapters.cpp:3148 栈上构造并由 DLL 写入含 char[512] 的整结构）。同区间 P18 刚按宪章 §8.6 给 IpvParams 立"双 uint32 自描述 + 入口 fail-closed + 布局锁 + selfcheck"四件套；V11-N-10 定稿"唯一稳定解=边界结构补 struct_size 运行期自拒"并点名向 aio_hips/gaia/dynamic_psf 扩散——新增边界结构未继承。新旧 DLL 错配时调用方按错误布局读 nside/finest/error_msg 无自拒（对照：同文件 HpDrizzleResult 属存量）。
- 挂 related：机制⑪、V2-N-01/V11-N-01..10。

### W1-N-06 (P1) "负责人裁定（1x-2x 采样率）"零登记（机制⑫ 新实例）
- 引用处：hp_drizzle_api.h:77"语义 (负责人裁定)"、module_adapters.cpp P17 块注释、p1001_real_nodes_test.cpp:2334"合同 (负责人裁定, 采样率等价 drizzle 1x-2x)"。
- 三级复核登记面：`grep -n '1x_to_2x\|1x-2x\|采样率等价' CHANGELOG.md memory.md` 0 命中；`问题扫描/40_OWNER_DECISIONS.md` 无本项；`grep -rn ... 工程控制/ docs/ contracts/` 0 命中（命令原文见分片）。裁决文本只活在代码注释与测试里，不可证伪。
- 挂 related：机制⑫、V8-N-07、A-44、E10。

### W1-N-07 (P2) 对"永不编译"的测试 TU 施加本区间修复（修复零采集）
- lib/plate_solve/cpp/ipv/test/test_last_inlier_reset.cpp:114-119（+5：IpvParams 改经 ipv_get_default_params 填充以适配新 fail-closed）。三级复核：L1 `grep -rn 'test_last_inlier' --include='CMakeLists.txt'`（排除 build/run）0 命中；L2 全量 53 CMakeLists 构建面清单 0；L3 无 GLOB 收集 ⇒ 该修复动作无任何编译/采集回报，文件属存量孤儿（同目录 test_kvector/test_synthetic 同形态）。
- 挂 related：L22-005（构建孤儿）、机制⑪。

### W1-N-08 (P2) gaia 新注释实测包络口径错报（36 片宣称 vs 16 片口径）
- gaia_client.c:31-40 注"仓库内 36 个真实 shard…包络实测 low ∈ [-2.00, 21.05], high ∈ [16.59, 25.59]"。python3 解析其自引证据 shard_magranges.tsv 复算：36 片合并 high ∈ **[13.62**, 25.59]（DR3SP 前 9 片 high 13.62-16.52 全部低于 16.59）；16.59/25.59 恰为 GaiaDR3-only 16 片口径；low 与 16+20 计数正确。结论（[-10,40] 完全包含）不受影响，但"实测登记"数字与其宣称口径不符。证据文件本身存在（工作树态）但在 gitignored run/ 内——CI 干净树不可复核（E4 裸锚家族，登记于 §C 抽检#4）。
- 挂 related：V8-N-04（证据在 gitignored 区）、簇6。

### W1-N-09 (P2) parse_attr_after 新 docstring 与 unquoted 畸形行为矛盾
- gaia_client.c:852-856 注"magnitudeRange 需区分'属性缺失'(正常, 不告警) 与'属性存在但值为空/畸形'(必须告警并放弃剪枝)"。实现：值未加引号时函数 return 0（:887 落尾），调用方 mag_present=0 → 按"缺失"静默路径处理——不告警、不计 magnitude_range_reject_count。空引号 `""` 形才进告警分支。注释承诺覆盖不到自己点名的畸形形（无引号 XML 属性确属畸形声明）。可达性依赖外部 XPSD 头，仓内 writer 恒带引号——静态判"注释≠行为"。
- 挂 related：簇6、S-4①（静默形态）。

### W1-N-10 (P2) probe 新写 <prefix>.order 无任何消费者（新增只写不读）
- p1drz_thread_probe.cpp:110-115 写 .order（tiles 顺序，自述"用于逐位比对不同线程预算"），而 p1drz_merge_pipeline_lock.sh 只比 .canon+.norm.hiss、p1drz_taskset_invariance.sh 只比 .norm.hiss（两脚本全文 grep 'order' 0 消费）。恰好漏掉本区间新逻辑 `std::sort(tiles, parent_ipix)`（drizzle_engine.cpp:2077-2080 输出序规范化）的唯一可观测面。
- 挂 related：机制⑪（只写不读新边）、簇9。

### W1-N-11 (P2) 欠采样告警在 auto 分支失实归因
- module_adapters.cpp:3170-3178：告警条件不含 `nside_source`，当 auto 决策被引擎钳位到 2^22 而 finest<0.0503"（hp_res>finest）时，auto 运行会打出"**显式** drizzle.nside=%d … 本次按用户**显式值**执行 (nside_source=explicit)"的 stderr，且"合规应取 nside=auto_res.nside"之建议值==本次采用值（自相矛盾）。产物字段（nside_source=auto/conflict）仍如实——诊断面失实，数据面不错。
- 挂 related：簇1 机制①族（自述与事实脱节）在日志面。

### W1-N-12 (P2) HEALPIX_SCALE_PER_NSIDE_ARCSEC 双实现无单一承载层
- drizzle_engine.cpp:683-684（权威注释"禁魔数 210960/1186.18"处）与 module_adapters.cpp:3146-3147（P17 块同式 `sqrt(pi/3)*deg2arcsec` 重推导）。两处独立推导同值，无共享常量/头承载；V12-N-16 已判过同形态（sqrt(10) 两处硬编码）、V12-N-01 常数无承载根因。
- 挂 related：V12-N-01/N-16（S-1 单一权威面）。

### W1-N-13 (P2) p22 自报观测面含恒零伪字段
- drizzle_engine.cpp:2113-2120 `[drizzle_engine][p22]` 输出行字段 `out_sort=%.3f` 实参为字面量 `0.0`（排序耗时从未计时）。运维读表会把伪零当实测。
- 挂 related：簇1 记账面（自宣指标不可信）。

### 正向控制（不计缺陷，建议入账本亮点）
- ipv P18 三件套：布局锁 + **反向破坏镜像 selfcheck 门**（证明锁非恒真，正是前轮 E 类建议的自发落实）+ 五入口运行时 fail-closed 锁；5 个公共入口校验全覆盖、调用方（orchestrator/module_adapters/绑定测试/diag 工具）全部改走 ipv_get_default_params 或 import 唯一镜像——本区间质量最高的修复闭环。
- P22 归约流水线静态复核无竞态：全部共享状态变更在 merge_mu 下、谓词 (a)/(b)/(c) 互斥穷尽、池槽所有权单线程、stripe 升序左折叠 + parent_ipix 排序输出 ⇒ "逐位与线程数无关"的确定性论证成立；in-process 1..16 预算逐位锁（p1drz_tests_core (a)-(d)）为真采集。
- gaia 有界解析 + 414 行边界回归锁（含逐位==atof 不变性断言与 baseline 红/绿自述）设计良好。
- p1snr_frame_parity 含非恒真负例（snr.max_sources 人为截断必须数值不同）。

## §C 新增注释可核验性抽检（12 处；✗=不实 △=存疑/不完整 ✓=属实）

| # | 注释锚 | 判 | 复核证据 |
|---|---|---|---|
| 1 | ipv_api.h:71"所有公共入口在 params != NULL 时校验二者" | ✓ | ipv_entry.cpp 恰 5 个 const IpvParams* 入口，:455/:499/:649(约)/:694/:741 全调 validate_params_abi；to_solver_params(nullptr)=默认放行 |
| 2 | ipv_select.h:110-112"4 条生产路径都经 gaia_query_mag_iterative 调它" | ✓ | ipv_select.cpp:1016/1313/1582/1908 四处传 &output；唯一映射点 :554 |
| 3 | P18 CMake"Python 352 vs C 424" | △ | 424-352=72 与 962c47d4"历史 72 字节结论勿再改"自洽；但同段"log_dir 96->168"两时点面（base git show :90 / head :108）均 256，不可复算 |
| 4 | gaia_client.c:33-37"36 片包络 + 登记于 run/…/shard_magranges.tsv" | ✗ | 包络 high 下界实报 13.62≠16.59（W1-N-08）；tsv 存在但落 gitignored run/（CI 不可复核） |
| 5 | parse_attr_after:853"存在但畸形必须告警" | ✗ | unquoted 形静默（W1-N-09） |
| 6 | hp_drizzle_api.h:77"负责人裁定 1x-2x" | ✗ | 四登记面 0 命中（W1-N-06） |
| 7 | snr CMakeLists:28"由根 CMakeLists 注册" | ✗ | 根 :210 注释化 + F-CI-002-01"不收编"裁决（W1-N-03） |
| 8 | module_entry.cpp:7-9 三先例路径 | ✓ | calibration/cosmetic/hips 三个 module_entry.cpp 均存在 |
| 9 | noise README:71-73 两钩子"供 parity 锁直调" | ✗ | 符号不存在且锁走生产注册表（W1-N-02） |
| 10 | gaia_client.h:139-141"非导出面 (无 GAIA_EXPORT)" | ✓ | 新 getter 无宏；legacy 导出清单未增改 |
| 11 | probe 头:3-7"输出 .hiss/.canon/.order"（CMake 同款） | △ | 实际还产 .norm.hiss（:75，两锁真正消费物），清单漏列；.order 反而无人消费（W1-N-10） |
| 12 | module_adapters:1613"精确直调路径传 0" | ✓ | :1828-1833 if constexpr(kPrecisePsfEnabled=false) 分发核验 |

统计：✓5 / ✗4 / △3（抽检 12 ≥ 配额 10）。

## §D 需运行期未判项（不设判词）
1. P22 提交语"并行效率 93.5%→95.1%、T4 合并 4.4s 移出关键路径"——需 benchmark 复跑；静态仅判结构无竞态。
2. W1-N-01 新触顶判据在真实 Gaia 密场的实际触发率（默认 cap_unit==全局帽时与旧等值，不改变现有产物）。
3. 两 shell 锁的逐位一致结论本身（taskset 在 CI 机是否可用；不可用即 [SKIP] exit 0——V9-N-17 已判机制，新站 merge_pipeline_lock 同形态第二站，登记不重报）。
4. lib/phase1/tests/p1phot/ 未跟踪四件：与 lib/photometric_calib/tests/p1phot/ 同名文件 sha256/行数已分叉（fixtures 252 vs 264、oracle 461 vs 322、test_main 175 vs 133，core 单侧独有），零构建引用——判"待收编双生面"，内容质量不评。
5. ipv_dead_params_lock.py + manifest + selfcheck + 两 gates（工作区态新增，邻站 10:0x-10:3x 在途编辑）：按纪律不作判；若提交时 CMakeLists/checks.json（均为跟踪文件）先行入库而三脚本未收编，干净树两门 command 所指不存在——**提交原子性风险预告**。

## §E 与第一轮防重报核对
逐条对照 SUMMARY 簇表 14 机制与 INDEX §三 15 主题：无一条构成新机制；W1-N-01(簇6+反向钉死)、02(簇1③+V8-N-01)、03(机制⑪+簇7)、04(簇6)、05(机制⑪)、06(机制⑫)、07(机制⑪)、08(簇6+E4 裸锚)、09(簇6)、10(机制⑪)、11(簇1)、12(V12-S-1 族)、13(簇1 记账面)。新代码中"修复正确且带负例"的面（P18 四件套、P15a in-process 逐位锁、P14 parity 非恒真、P17 fail-closed 六类拒绝）不立条。
