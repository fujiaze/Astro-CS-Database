# V18 · 自证与旁路求值普查（从 V15-N-05 乙档外推）

- 轴主：本 subagent（V18）｜派发人：session-9db01ebd（前台）
- 普查时点：HEAD `d8726fd78bd6742bdbb3baa572757ab0b2bec47d`（`git --no-optional-locks rev-parse HEAD`），2026-09-15
- 纪律：纯静态（read/glob/grep + `问题扫描/` 内只读 python3 -B + `git --no-optional-locks` 只读查询）；**未运行**任何编译器/cmake/ctest（含 `-N`）/测试/二进制/`ci/run.py`/仓内无 `--dry-run` 脚本；未写 `findings/`、`账本/`
- 准绳顺序（负责人明令）：①负责人设计意图（`40_OWNER_DECISIONS.md` **C-21**：注入式反向锁三条合格判据；C-16：禁以文本命中代替行为断言）②现行冻结文本（改后）③代码/测试自洽
- 排除前缀清单（本轴全仓口径，逐字）：
  `./run/ ./build/ ./out/ ./worktrees/ ./Testing/ ./logs/ ./artifacts/ ./evidence/ ./reports/ ./工程控制/ ./GaiaDR3/ ./GaiaDR3SP/ ./BASS DR3/ ./AstroCS.wiki/ ./__pycache__/ ./问题扫描/ ./third_party/ ./testdata/ ./CS/ ./Database/ ./graph/ ./.git/ ./astrocs_p1sess_neg/ ./astrocs_p1sess_perf/ ./astrocs_p1sess_props/ ./astrocs_p1sess_test/ ./设计大纲/`
- 统计脚本（只读、可复跑，落 `问题扫描/_verify/`）：`scripts_v18_*.py`
- 种子条目（不重报，挂 related）：V15-N-05 / V15-N-06（乙档与恒真白名单）、V15-N-14（`|| true`）、FD-F-001 / FD-F-002（生产者与守卫同处）、M8-F-007、M8-F-014、M8-F-015、M2b-F-02、V4-N-05、V1-N-09（同构 Oracle）、L23-001 / L23-002（failures 不进退出码）、M5a-G-005（门自测检查器输出串）、M6b-G-001（无据 VERIFIED）、L28e-E-001（生成器自造 ID 再自证）

## §0 已知实测与本轮目标

## §1 普查规模与口径

## §2 ①族：cond 与结果计数解耦 / failures 旁路自增 / CHECK 无求值形态（逐条）

## §3 ②族：注入名全集与三态（CI 驱动 / 测试驱动 / 无人驱动）

## §4 ③族：生产者与守卫同处的其余形态

## §5 机器门可覆盖性判定

## §6 新条目登记建议表

## §7 未覆盖面与需运行期项
