# V15 · 「测文本不测行为」弱断言全仓普查（骨架）

> 轴：V15 ｜ 执行者：V15 子代理 ｜ 生成时点：2026-09-14（HEAD 见 §0）
> 权威序（负责人明令）：①负责人设计意图（设计大纲/、memory.md、根 AGENTS.md 十要素、问题扫描/40_OWNER_DECISIONS.md 已裁条）②现行冻结文本（改后）③代码/测试自洽
> 纪律：纯静态（read/grep/glob/write/edit + 问题扫描/ 内只读 python3·node + git --no-optional-locks 只读查询）；未运行编译器/cmake/ctest/测试/二进制/ci/run.py；未运行任何无 --dry-run 的仓内脚本；结构化文件一律解释器解析；判缺失走三级复核；排除前缀按 ./x 写。
> 免重报：已先读 SUMMARY.md 簇表与 INDEX.md §三；同义主题挂 related 不另立条。

## 0. 准绳与已知输入
- 机制源（外推起点，均已定稿，不重报）：
  - FD-F-001 `tests/unit/cpu_provider_test.cpp::check_shared`：`CHECK(s.find("backend_table.inc") != std::string::npos)` 守「禁复制漂移」
  - FD-F-002 `cli/resource_recorder.h:243`（生产者字面量）+ `tests/unit/mon001_recorder_test.cpp:98`（`js.find("\"…\":true")`）
  - V4-N-16 `tests/quality/test_known_failures_baseline_ci.py`：`assertGreaterEqual(entries,1)` + `assertIn("p1_noise_adapter", units)` 对版本化在册文件正向钉
  - M8-F-009 资源门 0.80 系数钉为期望 + 被测函数自造通过输入 + `ci/tests/test_ci001_failclosed.py` assertNotIn 反向钉死不被采集
  - 已裁条：`问题扫描/40_OWNER_DECISIONS.md` C-16（禁止以文本命中代替行为断言）
- HEAD：（待填）

## 1. 普查规模与口径（四类判据的机器扫描）
（待填：扫描范围、命中数、口径定义）

## 2. 逐条判定表（四类 ①②③④）
（待填：文件::行 ｜ 断言原文 ｜ 被守护不变量 ｜ 有无逻辑蕴含 ｜ 改成什么才有效 ｜ related）

## 3. 特别检查：P5/P8 新写测试文件 与 backend 三文件
（待填）

## 4. 「注入式反向锁」实际数量统计
（待填：黄金手段在全仓的真实落地数）

## 5. 新缺陷条目（上限 15，宁窄而实）
（待填）

## 6. 未覆盖面与需运行期项
（待填）
