# V15 · 「测文本不测行为」断言全仓普查（从 FD-F-001/F-002、V4-N-16、M8-F-009 外推）

- 轴主：本 subagent（V15）｜派发人：session-9db01ebd（前台）
- 普查时点：HEAD `56b5e6629e4c5ee1cfedd9d31964ebc6e97116c3`（`git --no-optional-locks rev-parse HEAD`），工作树含大量 `?? artifacts/**` 未跟踪项，不影响本轴读取面
- 纪律：纯静态（read/grep/glob + `问题扫描/` 内只读 python3 -B）；未运行任何测试/编译/ctest/`ci/run.py`；未写 `findings/`、`账本/`
- 本档统计脚本（只读，可复跑）：`问题扫描/_verify/scripts_v15_cat.py`、`scripts_v15_pytext.py`、`scripts_v15_cpp.py`、`scripts_v15_py.py`、`scripts_v15_inject.py`、`scripts_v15_injectB.py`、`scripts_v15_inject_final.py`、`scripts_v15_selfcheck.py`、`scripts_v15_ctest_reg.py`、`scripts_v15_verify2.py`、`scripts_v15_verify3.py`（结果 JSON 同名 `.json`）

## §0 准绳与已知输入

1. 负责人裁决 **C-16｜禁止以文本命中代替行为断言**（`问题扫描/40_OWNER_DECISIONS.md:132`）：find/contains/正则命中字面量即通过的"守卫"一律视为无效锁，须改值断言/结构断言/行为断言或注入式反向锁。本轴交付=**规模 + 逐条判定**，不重开机制之争。
2. 种子条目（不重报，作 related）：FD-F-001（`cpu_provider_test` 读 .inc 文件名）、FD-F-002（生产者与守卫同处）、V4-N-16（known_failures 基线反向钉死残留）、M8-F-009（钉阈值文案）、M8-F-007（`|| true` 短路恒真 2 处）、M8-F-014（ISA-002 自写自验）、M2b-F-02（`||` 析取 + 同文件读两次）、V4-N-05（注入分支不锁缺陷站点）、V2-N-06（锁自述与判据不符）、M6b-L18-003（`test_single_cli` 字面量黑名单恒绿）、V1-N-09（P5 Oracle 同源 + 锚值不可复算）、M8-F-004（注册≠执行）。
3. 权威依据：宪章 §3.1/§8.1（单入口）、§10.3（稳定统计）、§10.4（唯一线程预算源）、§10.5/§18.2（85%/60%）、§12.3-4（可证伪）、§13.1（Oracle 独立）、§14.4（fail-fast）、§17.6；设计大纲/memory.md 的「科学定义=算法=接口=代码=测试」同构条。
4. 「与旧文档不同」不作缺陷；本轴专查「把旧错误口径当权威改回去」的站点，结论：**未发现新站点**。命中 2 处「把历史呈现钉成契约」的相邻事实——`tests/backend/test_isa_variants.py:90-96` 钉 `+28.2%`（一次历史实测数字，V15-N-11）与 `tests/arch/test_single_cli.py:15-16` 的旧入口字面量黑名单（M6b-L18-003 已在册，本轴只在 V15-N-15 挂 related）；`tests/backend/test_p2007_joint_gate.py:19/:231` 的 `0.80*N` 属 V4-N-01 在册残留（现行冻结口径为 §18.2 的 85/60），本轴不重复登记。

## §1 普查规模与口径

**口径**：`tests/**` + `ci/**` + `tools/**` + `lib/**` 中路径含 `/tests/` 或文件名含 `_test.`/`selftest` 的 `.py/.cpp/.c/.h/.hpp`；排除前缀 `./run/ ./build/ ./out/ ./worktrees/ ./Testing/ ./logs/ ./artifacts/ ./evidence/ ./reports/ ./工程控制/ ./GaiaDR3 ./GaiaDR3SP ./BASS DR3 ./AstroCS.wiki ./__pycache__/ ./问题扫描/ ./third_party/ ./testdata/ ./CS ./Database ./graph ./.git ./astrocs_p1sess_*`。
**时点**：HEAD 56b5e662，2026-09-14。

`python3 -B scripts_v15_py.py` / `scripts_v15_cpp.py` / `scripts_v15_pytext.py` 输出摘录：

- 扫描文件 **717**
- C++ 侧含 `std::string::npos` 的行 **643**；其中 `find(...)!=npos` 明细 **121** 行
- C++ "读入文本后再 find" 命中 **122** 行，其中**读仓内源文件/配置正文当证据**的 **28 行 / 6 个测试文件**：
  `./tests/unit/cpu_bench_test.cpp`(4)、`./tests/unit/cpu_provider_test.cpp`(2，=FD-F-001)、`./tests/unit/io_adapter_test.cpp`(5)、`./tests/unit/p1_ir_facade_test.cpp`(6)、`./tests/unit/p2002_unc_rej_prov_test.cpp`(2)、`./tests/unit/p2_ir_facade_test.cpp`(9)
- Python 侧：①类「读文件/取文本后做子串或正则命中」= **419** 行；③类「对 stderr/stdout/msg/reason/detail/message 做文案子串断言」= **204** 行；④类「正向存在/计数断言」= **277** 行
- ③族中 `reason`/`message` 子串命中全仓 = **45** 处，分布：cpu008_worker_advisor_test 15、cpu005_route_decision_test 10、cpu007_profile_store_test 5、cpu004_routing_test 5、`lib/backend_host/profile_store.cpp`（生产码内同类自测）5、p1_noise_test 2、cpu001_negative_test 2、p1_wcs_phot_test 1
- 判据要点：命中数≠缺陷数。下面 §2 只登记"蕴含不成立"的站点；蕴含成立者（例如对**被测函数刚产出的内存对象**做值比较、或 `parse` 后按字段判值）不进表。


## §2 逐条判定表（四类；15 条，宁窄而实）

> 判据口径：**蕴含=有** ⇔ 断言为假时，被守护不变量必为假（同方向）。仅"字面量在文本里出现"者一律判 **无**。
> 全部断言原文为逐字抄录，行号=HEAD 56b5e662 现文（M6b-E-002 行锚漂移风险已知，故同时给函数名锚）。

### V15-N-01 ｜①｜P1
- 位置：`tests/unit/p2_ir_facade_test.cpp::main 第1/3/4段`（:29-38、:56-62、:68-69）
- 断言原文：
  > `std::string src = read_file(base + "/lib/phase2_session/p2_session.cpp");` …
  > `for (const char* node : {"coverage", "sample", "upm_build", "persist"}) { if (src.find(std::string("stage(\"") + node + "\"") != std::string::npos) ++n_nodes; … }` `CHECK(n_nodes == 4);   // canonical IR 4 节点全链`
  > `std::size_t p1 = src.find("p2_coverage_build"); … CHECK(p1 < p2 && p2 < p3);   // 顺序正确`
  > `CHECK(src.find("stages") != std::string::npos); CHECK(src.find("kind") != std::string::npos);`
- 被守护不变量：Phase2 canonical IR 恒 4 节点、阶段序 coverage→sample→upm_build→persist 与静态 IR 一致、manifest trace 记录 stages（宪章 §8.2 节点/§12.3-4 可证伪）
- 蕴含判定：**无**。源文本里出现 `stage("coverage")` 字面量与"运行时 IR 有四节点且按此序执行"无因果；把 `p2_coverage_build` 的调用挪到函数定义之前（或放进一个永不执行的分支）文本序不变，断言仍绿；`find("stages")`/`find("kind")` 命中任意注释或字符串即绿。且 :31 的节点名列表与 :44 的委托名列表都由**测试自己抄写**，被测源改名而语义不变即假红。
- 有效改法：跑 `astrocs phase2 plan/run` 拿真实 manifest 与 trace，断 `len(ir["nodes"])==4` 且 `[n["stage"] for n in …]==["coverage","sample","upm_build","persist"]`（值/结构断言）；节点名单一事实源改取 `contracts/schemas` 或 IR 产物，不在测试里手抄。
- 同族：有 → related **FD-F-001**（同机制，本条为 Phase2 侧独立站点，非同一文件故不并档）

### V15-N-02 ｜①｜P1
- 位置：`tests/unit/p1_ir_facade_test.cpp::main 第1/2/3段`（:33-40、:47-49、:54-56）
- 断言原文：
  > `if (src.find(std::string("\"name\", \"") + node + "\"") == std::string::npos && src.find(node) == std::string::npos) { … ++failures; }`
  > `CHECK(src.find("\"enabled\"") != std::string::npos);         // 开关键`
  > `CHECK(src.find("cosmetic_flag(doc[\"cosmetic\"], \"enabled\", true)") != std::string::npos);  // 默认开`
  > `CHECK(src.find("ac_calibrate_frame") != std::string::npos);  // 委托校准`
- 被守护不变量：Phase1 canonical 节点集 io_read→calibrate→cosmetic→io_write；`cosmetic.enabled` 缺省为真；facade 只委托不内联科学
- 蕴含判定：**无**。:35-36 是"精确串不中则退化为裸词命中"的**放宽析取**——`io_read` 出现在任何注释/字符串/变量名即通过；:49 把一整行 C++ 表达式（含实参写法与 `, true)`）钉成契约，任何格式化或把默认值移到 `cosmetic_flag` 内部即假红，而真正的"缺省开"运行期行为无断言；:54-56 委托符号命中只说明该名字符串在文件里。
- 有效改法：validate/plan 产物的结构断言（节点名集合+顺序）；"默认开"改**行为断言**：不给 cosmetic 配置跑一次，断言美容实际被执行（trace call_count / 输出像素被改写）；"facade 不内联科学"改由域边界门（`tools/check_abi_boundary.py`/架构门）判 include 与符号，而非文本。
- 同族：有 → related **FD-F-001**、V15-N-01（同族两文件宜同批整改）

### V15-N-03 ｜①｜P1
- 位置：`tests/backend/test_p3002_properties_order_unit.py::TestP3002PropertiesOrderUnit`（:24-57，整文件）
- 断言原文（节选）：
  > `s = open(SESS, encoding="utf-8").read()` / `self.assertIn("input_order", s, "session 未读取输入实际 order")` / `m = re.search(r"const int max_order = \(input_order[^;]+;", s)` / `self.assertIsNotNone(m, "order 上限公式缺失")` / `self.assertIn("*out_order = p.order", r)` / `self.assertNotIn('"Jy/beam"', s, "session 不得硬编码 Jy/beam")` / `self.assertIn("bunit", h, "HipsProperties 缺 bunit 字段")` / `self.assertIn("bitpix != -32 && bitpix != -64", s)`
- 被守护不变量：P3-002 四条合同——order_sel 上限取自输入实际 order、BUNIT 源自输入合同且缺省 ADU、BITPIX 仅 -32/-64、不支持输入显式错误（`docs/algorithms/HIPS_GENERATION_ALGORITHMS.md` G6 段 + 宪章 §7.3/§14.4）
- 蕴含判定：**无**，且整文件**零执行**（不编译、不跑 `astrocs phase3`、不回读任何 properties/FITS）。:51 钉的是逐字源码表达式，等价改写（换变量名/拆两个 if/`!= -32 && != -64` 写成 switch）即假红；:40 `assertNotIn('"Jy/beam"')` 只保证这个字面量不在该文件，把默认单位挪到 `p3_resample.cpp` 或头文件即假绿；:44 `assertIn("bunit", h)` 三行里最弱——任何含该子串的注释都满足。
- 有效改法：改为**值断言**——用在册 HiPS fixture（`tests/backend/fixture_common.py::ensure_f1f2_hips` 已可在 UT-BACKEND 面制备）跑一次 phase3 输出，读 properties/FITS 头断 `NORDER`、`BUNIT=="ADU"`、`BITPIX` 实际值与非法输入的退出码；:53-57 的"不支持输入显式错误"改断退出码 + 诊断码（不钉文案）。
- 同族：无同名条目；机制同 FD-F-001 → related FD-F-001、**V4-N-01**（同一 backend 域、同一批 G6 联合门）

### V15-N-04 ｜①｜P1
- 位置：`tests/backend/test_p3003_parallel_resampler.py::test_02_no_hardware_concurrency/test_03_worker_local_sampler`（:100-116）
- 断言原文：
  > `code_lines = [l for l in s.splitlines() if not l.strip().startswith("//")]` / `self.assertNotIn("hardware_concurrency", "\n".join(code_lines), "代码不得读 hardware_concurrency")` / `self.assertIn("budget.max_workers", s, "worker 数应来自 budget.max_workers")` / `self.assertIn("std::thread", s, "应使用 std::thread 并行")` / `self.assertIn("rows_per_worker", s, "应按 row-band 分 work units")` / `self.assertIn("P3Sampler w_samp", s, "worker 应独立 sampler")`
- 被守护不变量：宪章 §10.4/§17.6（禁私有线程预算源、heavy 路径无硬编码线程数）+ P3-003 合同"每 worker 独立 sampler、输出行带不重叠"
- 蕴含判定：**无**。源文本出现 `std::thread`/`rows_per_worker` 与"worker 数来自 budget"无因果（把 `budget.max_workers` 换成 `min(budget.max_workers, hardware_concurrency())` 时 :107 仍绿、:105 因写了别的调用形式也可能仍绿）；:115 钉死 `P3Sampler w_samp` 这一**变量名**，重命名即假红。
- 有效改法：跑两次（budget 2 核 / 4 核）断 `worker_balance.csv` 的实测并发 worker 数等于预算（值断言，产物在本文件 test_01 已解析）；"无硬编码线程"改由已存在的 `tools/arch/check_thread_budget.py` 门覆盖（THREAD-BUDGET），本文件不再自造文本判据。
- 同族：机制同 FD-F-001；与在册 THREAD-BUDGET 门重叠 → related FD-F-001、**M5a-G-005**（THREAD-BUDGET 门自身只断检查器输出串）

### V15-N-05 ｜①＋④｜P1
- 位置：`tests/runtime/test_rt005_plan_estimator.py::test_frozen_constants_pinned`（:463-468）
- 断言原文：
  > `h = _HDR.read_text(encoding="utf-8")` / `self.assertIn("kHipsTileWidthPx = 512", h)` / `self.assertIn("kHipsMaxOrder = 20", h)` / `self.assertIn("kHipsTileF32Bytes", h)` / `self.assertNotIn("hardware_concurrency", _SRC.read_text(encoding="utf-8"))`
- 被守护不变量：RT-005 冻结常数（tile 512px / maxOrder 20 / tile F32 字节数）与"不读 hardware_concurrency"（`docs/algorithms/…` + 宪章 §10.4）
- 蕴含判定：**无**。常数由头文件声明、被生产码消费，但断言只命中**声明文本**；`constexpr uint64_t kHipsTileWidthPx = 512;` 改成 `512u` 或换行折行即假红，而在 estimator 实现里另写死 `<< 9`/9`*512` 绕过该常数则全绿；第三行 `assertIn("kHipsTileF32Bytes", h)` 是纯符号存在断言（④），不改任何行为也过。
- 有效改法：把常数当**值**测——调用被测估算函数喂一个 1-tile 场景，断估算字节数 == `4*4*512*512` 的算术结果（独立算式，非同一常数）；"不读 hardware_concurrency"交给 THREAD-BUDGET 门。
- 同族：有 → related V15-N-06（同文件的 kernel 恒真条）

### V15-N-06 ｜②（恒真，判别力为零）｜P1
- 位置：`tests/runtime/test_rt005_plan_estimator.py::test_kernel_ids_authoritative`（:470-479）
- 断言原文：
  > """实现中出现的 kernel id 必须都在 backend_table.inc 权威清单内。"""
  > `for m in re.finditer(r'"(calibration-pixel-transform|noise-snr-reductions|wcs-psf-batch|drizzle-overlap|drizzle-accumulate|drizzle-normalize|upm-spmv|upm-residual|upm-weight-update|rejection-statistics|integration-accumulate|hips-bulk-transform)"', text): self.assertIn(m.group(1), _KNOWN_KERNELS, m.group(1))`
  > （:432-438）`_KNOWN_KERNELS = { "calibration-pixel-transform", … "hips-bulk-transform", }  # 权威清单 lib/backend_host/backend_table.inc（12 kernel）`
- 被守护不变量：实现侧 kernel id ⊆ 权威清单 `lib/backend_host/backend_table.inc`（宪章 §12.3-4 可证伪；ALG 台账 12 kernel）
- 蕴含判定：**无——该断言永不可能失败**（数学恒真）。只读复算：正则交替集与 `_KNOWN_KERNELS` 实测同为 12 项且集合相等（脚本 `scripts_v15_verify2.py` 输出：`两集合相同? True`），故 `m.group(1)` 是交替式的捕获组，取值必属该 12 项 ⇒ `assertIn` 恒真。真实权威 `backend_table.inc` **从未被本测试读取**（本条经 python 解析实测：`.inc` 内 `ACS_KERNEL_ENTRY("ALG-00x","<kernel>",…)` 恰 12 条、与白名单同集，但那是巧合一致，非被测出的事实）。
- 有效改法：**读权威再做集合差**——解析 `lib/backend_host/backend_table.inc` 的 `ACS_KERNEL_ENTRY` 第二参得权威集合，解析 `plan_estimator.cpp` 出现的 kernel 引用得实现集合，断 `实现集合 ⊆ 权威集合` 且 `权威集合 − 实现集合 == ∅`（新增 kernel 未登记即红）；配注入式反向锁：临时加一个未登记 kernel id → 断言必须红。
- 同族：无同文条目；机制与 FD-F-002"生产者与守卫同处"同型（此处期望值与被匹配模式同处一个测试文件）→ related FD-F-002、M8-F-015

### V15-N-07 ｜①（fail-open 型负向文本锁）｜P1
- 位置：`tests/unit/io_adapter_test.cpp::test_io_adapter_no_scheduler_include`（:210-221）
- 断言原文：
  > `const char* repo = std::getenv("ASTROCS_REPO");` / `std::string hp = (repo ? repo : "..") + std::string("/include/astrocs/io/io_adapter.h");` / `std::ifstream h(hp);` / `std::string content((std::istreambuf_iterator<char>(h)), std::istreambuf_iterator<char>());` / `CHECK(content.find("scheduler.h") == std::string::npos);`（+`phase1`/`phase2`/`phase3`/`calibration` 共 5 条）
- 被守护不变量：IO 公共头不得 include Runtime scheduler / Phase 实现（宪章 §3.2 三 Phase 相互隔离、§8.1 不串接）
- 蕴含判定：**无，且方向反了**。(a) 文件读不到时 `content` 为空串，5 条 `find(...)==npos` **全部通过** ⇒ 门在非 ctest 环境（`ASTROCS_REPO` 未设 + CWD 无 `../include`）下**恒绿 fail-open**；(b) 反过来，在头文件注释里合法写一句"本头不得依赖 phase1"即**假红**；(c) 真实违规（经 `astrocs/core/something.h` 间接传递 include）测不到。
- 有效改法：先 `CHECK(h.is_open())` 再判；判据从文本改**编译/符号面**——用 `tools/check_abi_boundary.py` 同族门（该门已在仓内解析 `#include`）判 io_adapter.h 的 include 闭包不含 `astrocs/runtime/**` 与 `astrocs/phase*/**`，或加一个"只 include io_adapter.h 的 TU 必须能编译并链接、且不得需要 scheduler 符号"的行为探针。
- 同族：无同文条目 → related FD-F-001（同族反向）、**M8-F-002/M8-F-005**（fail-open 假门形态）

### V15-N-08 ｜①/③（名不副实：注释声称的不变量无对应断言）｜P1
- 位置：`tests/unit/p1snr/p1snr_linux_test.cpp::group_contract C6`（:294-295；组注册见 `tests/unit/p1snr/CMakeLists.txt` 的 `p1snr_linux_contract`）
- 断言原文：
  > `// C6: 未参与行必须是 NaN (不得静默回填 1.0 伪装 unknown)`
  > `check(std::isnan(out.local_snr[0]) == false, "C6 used rows finite");`
- 被守护不变量：P8 合同——未参与（valid=false）行的 `local_snr` 必须为 NaN，禁止回填 1.0 伪装 unknown（`docs/science/NOISE_MODEL.md` 与本文件头"否认旧构造"段；宪章 §11 禁吞错后产出、§13.1 可证伪）
- 蕴含判定：**无——断言与注释声明的不变量不是同一命题**。断言只证"**第 0 行是参与行且有限**"，而 C6 要证的"未参与行 = NaN"在本文件从未被断言（全文件 `isnan` 只出现在 :194/:238（m5）、:312（snr_phot）、:333/:345（snr_f），无一处针对 `local_snr` 的未参与行）。把 `local_snr` 未参与行改成 1.0 回填，本锁仍绿。
- 有效改法：在 contract 组里造一个含 valid=false 行的输入，断 `std::isnan(out.local_snr[k])` 为真（值断言）；并把"参与行有限/未参与行 NaN"写成两条独立断言，避免同一条 check 承担两个命题。
- 同族：无同文条目（P8 为该文件首条）→ related **V2-N-06**（锁自述与判据不符同族）、V1-N-09（P5/P8 同族测试的 Oracle 独立性）

### V15-N-09 ｜③（钉诊断文案/呈现格式）｜P1
- 位置：`tests/unit/mon001_gate_test.cpp::14)`（:183-190）与 `tests/unit/mon002_gate_test.cpp::17)`（:209-216）
- 断言原文：
  > mon001：`const std::string m1 = astrocs::diag_message(astrocs::GateDiag::UtilizationP75Low, g);` / `CHECK(m1.find("unsampled") != std::string::npos);` / `CHECK(m2.find("0.300000") != std::string::npos);` / `CHECK(m2.find("0.700000") != std::string::npos);` / `CHECK(m3.find("10.000000") != std::string::npos);`
  > mon002：`CHECK(m1.find("60") != std::string::npos && m1.find("90") != std::string::npos);` / `CHECK(m3.find("32.0") != std::string::npos);`
- 被守护不变量：MON-001/002 合同——gate 失败必须输出**含实测值与阈值**的具体诊断（宪章 §10.5/§14.4；诊断串会进 verdict JSON 与 stderr，属交付面）
- 蕴含判定：**部分有、形式无**。断的是 `printf("%f")` 的六位小数呈现与两位裸数字子串：改格式（`%.3f`、加百分号、加单位）即假红；`find("60")`/`find("90")` 更弱——"1600"、"0.96"、任何长串里的 `90` 都命中，故对"阈值真的是 90"无判别力。
- 有效改法：让 `diag_message` 的**结构化前置**成为断言对象——同段 :177-182 已经在断 `gate_diag_name(enum)==字面量`（那是枚举的机器名，属可断契约）；数值侧改为断 config 字段与诊断中同一来源的**数值**（例如把诊断模板拆成 `diag_kv()` 返回 {阈值, 实测值}，断 `kv.thr==90.0 && kv.obs==60.0`），文案串只保留非空与含单位两项存在性检查。
- 同族：有 → related **M8-F-009**（0.85 文案钉死同族）、**V4-N-02**（`resource_gate.h` 诊断文案滞留 0.75/0.50——正是"文案与实现不同源"的实现侧根因）

### V15-N-10 ｜③（析取式 reason 命中）｜P2
- 位置：`tests/cpu/dispatch/cpu005_route_decision_test.cpp`（:199-200、:211-212；同族全仓 45 处，分布 cpu008_worker_advisor_test 15 / cpu005 10 / cpu007_profile_store_test 5 / cpu004_routing_test 5 / `lib/backend_host/profile_store.cpp` 自测 5 / p1_noise_test 2 / cpu001_negative_test 2 / p1_wcs_phot_test 1）
- 断言原文：
  > `CHECK(d2.fallback_reason.find("raised") != std::string::npos || d2.fallback_reason.find("verify") != std::string::npos);`
  > `CHECK(d3.fallback_reason.find("source_commit") != std::string::npos || d3.fallback_reason.find("invalid") != std::string::npos);`
  > （同族强例对照：`tests/unit/p1_noise_test.cpp:105` `CHECK(r0.value().reason.find("gain") != std::string::npos);`）
- 被守护不变量：CPU-005 合同——回退必须给出**可区分的**原因（profile 抬升 / 校验失败 / 无 profile 等，供 §10.3 决策审计）
- 蕴含判定：**无**。析取把"原因必须是 X"降为"原因里含 X 或 Y 之一"，`invalid` 一词几乎任何错误串都会含；:199-200 在"因 hash 不符而回退"的错误实现下仍可绿（只要串里带 verify 字样）。
- 有效改法：`reason` 改**诊断码枚举**（仓内 `gate_diag_name` 已有此形态），断 `d2.reason_code == kReasonProfileVerifyFail`；若必须留文案，则改成精确前缀匹配 + 一条"另一析取支不得同时成立"的互斥断言。
- 同族：有 → related M8-F-009（同族）、V15-N-09

### V15-N-11 ｜②（自建自证）｜P2
- 位置：`tests/backend/test_isa_variants.py::test_02_shared_contract_single_source`（:53-58）、`::test_03`（:79-88）、`::test_04_decision_ledger_frozen`（:90-96）
- 断言原文：
  > `self.assertIn('#include "baseline_kernels_impl.inc"', v, "变体不得复制 impl")` / `self.assertIn('#include "backend_table.inc"', v)` / `self.assertNotIn("float calibration_impl", v, "impl 不得复制")`
  > （:81-88）测试自己 `csv.writer` 写出 `artifacts/prerelease_v5/ISA-001/MEASUREMENTS.csv`，紧接 `self.assertTrue(os.path.isfile(out))`
  > （:90-96）`for k in ("SHIP(avx2)", "NOT_SHIPPED", "drizzle-accumulate", "hips-bulk-transform", "calibration", "+28.2%", "共享", "FMA"): self.assertIn(k, doc, f"决策台账缺 {k}")`
- 被守护不变量：ISA 变体共享单一 impl 源（禁复制漂移）；测量工件可审计；决策台账冻结（宪章 §10.3、ISA-001 合同）
- 蕴含判定：**无**（三点）：(a) `assertIn('#include "backend_table.inc"')` 与 **FD-F-001 同一机制、同一不变量**的 Python 侧第二实例（本条不并档因文件与门不同：FD-F-001 在 ctest `cpu_provider`，本条在 `UT-BACKEND`）；(b) `MEASUREMENTS.csv` 由本测试**自己**用 :76 算出的 `imp_hips` 写出，再断"文件在"——生产者与守卫同一处，且该工件是测试产物不是被测产物；(c) `+28.2%` 是**一次历史测量的呈现**，被钉成文档契约（重新 benchmark 合法变化即红，文档改了而测量没改也绿）。
- 有效改法：(a) 并入 FD-F-001 的建议机器门（符号/编译面锁重复实例化）；(b) 若需要工件审计面，应断"工件由 `astrocs benchmark cpu` 产出且 schema 合法 + 数值与本次运行日志一致"，删自写自证；(c) 文档侧断结构化字段（`decision: SHIP(avx2)` 行存在）而非百分比字面量。
- 同族：有 → related **FD-F-001**（.inc 文本锁）、**M8-F-014**（`tests/backend/test_isa_avx.py:109 assertTrue(True)` 同文件族自证）、L23-012

### V15-N-12 ｜④（正向在册/键存在）｜P2
- 位置：`tests/backend/test_p3006_production_pipeline.py::test_05_registry_implemented`（:133-139）
- 断言原文：
  > """SCI/ALG/MOD 状态 IMPLEMENTED(控制包台账标记)。"""
  > `ledger = os.path.join(REPO, "evidence", "v6_1_rework", "TASK_LEDGER.csv")` / `ids = [r[0] for r in rows]` / `self.assertIn("P3-006", ids, "P3-006 应在台账")`
- 被守护不变量：P3-006 交付状态（测试名与 docstring 自称验"SCI/ALG/MOD 状态 IMPLEMENTED"）
- 蕴含判定：**无**。只查 `task_id` 列是否含字符串 `P3-006`，**不查状态**——三级复核实测该行 `status` 列值为 `PASS`、`acceptance` 列内文含"status IMPLEMENTED"字样（脚本 `scripts_v15_verify2.py` 输出，表头 `task_id,gate,title,depends_on,platform,change_class,heavy_compute,commit_required,scope,acceptance,status`）；同时该台账在 `evidence/**`（免报区/一次性并发现场，非受控状态源），归档即假红。⇒ 名不副实 + 正向在册双重。
- 有效改法：改断 `rows["P3-006"]["status"] == "PASS"`（值断言）并把权威指向受控状态源（`docs/TRACEABILITY.csv` 或台账门 T6 的产物），或整条删除（该事实由在册台账门覆盖）；不得以 `assertIn(id)` 充当"已实现"。
- 同族：有 → related **V4-N-16**（正向在册钉同族）、M6b-G-001（无据 VERIFIED 同族）

### V15-N-13 ｜④（枚举白名单 + 键在册 + 产物存在）｜P2
- 位置：`tests/backend/test_p2007_joint_gate.py::test_04`（:234-250、:281-284）
- 断言原文：
  > `self.assertIn(g["verdict"], ("ok","low_avg_cores","single_threaded","cpu_p50_low","cpu_mean_low","memory_growth","progress_stall","fast_fail_first_10s","compute_io_mem_all_low"), f"verdict 非已知诊断枚举: {g['verdict']}")`
  > `self.assertIn("avg_equivalent_cores", g, "缺 avg_equivalent_cores 证据")`（+`cpu_p50_percent`、`cpu_mean_percent`）
  > `for name in ("resource_samples.csv","worker_balance.csv"): self.assertTrue(os.path.isfile(os.path.join(self.out, name)), f"{name} 缺失")`
  > （对照同段 :248-250 的 `assertNotIn` 与 :276-281 的数值断言）
- 被守护不变量：MON-002/§18.2——heavy 运行平均 CPU ≥85%、任何连续 10s <60% 即失败；每 heavy 运行自动记录 worker 均衡与采样
- 蕴含判定：**部分有**。:237-241 是**枚举白名单**：新增合法诊断枚举（诊断面扩容）即假红，而真正的失败（`cpu_p50_low`）也在白名单内 ⇒ 该条本身不判失败；:242-244 只断键存在（值为 `-1.0` 未采样哨兵也过，哨兵豁免逻辑在 :251-260，但那是"跳过判定"）；:281-284 只断文件存在，不判行数/覆盖窗口。同文件 :276-281（`n_samples>0`、`rss_peak_bytes<512MiB`、`workers_p50>=2`）是有效的值断言，本条只登记其弱半边。
- 有效改法：verdict 断"等值于 ok"（而非属九元组）；证据字段断`>=0`（哨兵即视为未采集→按 §10.5 应判失败或显式 skip 标记）；产物存在改断"行数 == 期望采样窗口且列头含八字段"。
- 同族：有 → related **V4-N-01**（本文件 docstring :19/:231 的 `0.80*N` 未同步 §18.2 85/60，本条不重复登记该事实）、**M5a-G-002**（同一测试文件的自证字段条）

### V15-N-14 ｜②/③（恒真短路残站）｜P1
- 位置：`tests/unit/p3002_uncertainty_test.cpp::read_fits_hdus`（:275）
- 断言原文：
  > `fits_close_file(f, &status);` / `return status == 0 || true;`
- 被守护不变量：P3-002 不确定度输出必须真实回读（`read_fits_hdus` 供 :325/:351/:575 三处 `CHECK(read_fits_hdus(...))` 当"写后读回"证据）
- 蕴含判定：**无**。助手函数恒返回成功 ⇒ 三处 `CHECK(read_fits_hdus(fits,&hi))` 永绿，HDU 数/`extname`/`bunit` 的后续断言建立在**打开或读取已失败**的未初始化 `hi` 上（cfitsio 失败时 `out->hdus` 保持 0，测试可能因"HDU 数不符"以无关原因红，或更糟：`status=0` 复位后 :272 掩盖逐 HDU 失败）。
- 有效改法：`return status == 0;` 并把逐 HDU 读取失败显式计数；配注入式反向锁：`ASTROCS_P3002_FAULT=skip_hdu`（本文件 :790-793 已有该注入名）应使 `read_fits_hdus` 返回 false，而当前该注入只影响下游断言、不影响读回助手。
- 同族：有 → related **M8-F-007**（`|| true` 同型；该条的 grep 口径是 `CHECK(.*|| true)`，本站点是 `return` 语句故未被覆盖）、**M8-F-015**（恒真面未被机器门覆盖）

### V15-N-15 ｜①＋④（文档措辞当架构门 + 反向钉死未达成状态）｜P1
- 位置：`tests/arch/test_single_cli.py::test_01/test_02/test_04/test_05`（:11-16、:18-20、:28-32、:34-38）
- 断言原文：
  > `hits = re.findall(r"唯一(?:生产)?(?:用户)?入口", s)` / `self.assertGreaterEqual(len(hits), 1, "必须有唯一入口声明")`
  > `self.assertNotIn("orchestrator.exe", s) if os.path.exists(MODULE) else None`（:20，三元表达式当语句用）
  > `prod = [r for r in rows if r["category"] == "exe_target" and r["classification"] == "production"]` / `self.assertEqual(len(prod), 0, "单一 CLI 未建前清单不得含 production exe")`（:31-32）
  > `for k in ("Phase1", "Phase2", "Phase3"): self.assertIn(k, s)` / `self.assertIn("in-process", s, "三 Phase 必须声明 CLI 进程内调用")`（:36-38）
- 被守护不变量：宪章 §3.1/§8.1（每平台唯一用户入口 = `astrocs`，CLI 不实现科学公式）＋ §3.2（三 Phase 严禁隐式串接）；`docs/architecture/ARCHITECTURE.md:9`（"唯一生产 exe：astrocs"）
- 蕴含判定：**(a) 无蕴含**——:13-14/:36-38 对 ARCHITECTURE.md 的中文措辞做正则/子串命中，文档同义改写即假红；真出现第二个入口（多 `add_executable`/`install(TARGETS)`）而文档仍留一句"唯一入口"则全绿——门与交付对象无关。**(b) 反向钉死**——:32 把"清单里 production exe 数 = 0"钉成期望，而 0 的原因是**单一 CLI 尚未在清单登记**：三级复核实测 `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv` 共 291 行、`category=exe_target` 74 行、其中 `classification=production` **0 行**、`symbol` 恰为 `astrocs` 的行 **0 行**（解释器解析，命令见下），而 ARCHITECTURE.md:9 已声明 astrocs 为"唯一生产 exe"⇒ 二者互斥，且 **CLI-001 建成并把 astrocs 登记为 production 的那一刻本测试必红**（真整改假红）。**(c) 死条件**——:20 的三元表达式永不走 else（:19 已无条件 `open(MODULE)`，缺文件先 error），是"看起来像守卫、实际是表达式语句"的零作用写法。
- 有效改法：①不变量改**交付面结构断言**——从根 CMakeLists 的 `install(TARGETS …)`/`add_executable` 闭包（或 `tools/check_release_layout.py` 产物）断"RUNTIME 用户入口数 == 1 且名为 astrocs"；②:32 改 `assertEqual(len(prod), 1)` + `assertIn("astrocs", [r["symbol"] for r in prod])`，与①同步修正登记面，消除反向钉死；③:20 改回普通断言并补 `assertTrue(os.path.exists(MODULE))`；④文档措辞类降级为 DOC-L0 文档门，不再充当架构门。
- 复核命令（只读）：`python3 - <<PY … csv.DictReader("docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv") PY` → 输出 `总行: 291 / category=exe_target: 74 / 其中 classification=production: 0 / symbol 恰为 astrocs 的行: 0`
- 同族：有 → related **M6b-L18-003**（"只比对两条固定字面量⇒门恒绿"已在册；本条新增 :32 反向钉死与 :20 死条件两个不同事实，故不并档）、**M9.md §7**（反向钉死六站点 → 本为第七站）、M5b-G-08（AGENTS-GOV 同型关键词全含门）

## §3 特别检查：P5/P8 新写测试文件 与 backend 三文件

**口径**：P5 = `lib/snr_estimator/tests/p1noise/p1snr_science_test.cpp`（提交 31ca57c3，注册 `p1snr_science_{units,oracle,negative,production,determinism}`，见 `lib/snr_estimator/tests/p1noise/CMakeLists.txt:117-123`）；P8 = `tests/unit/p1snr/p1snr_linux_test.cpp`（提交 35c85f53，注册 `p1snr_linux_{units,oracle,contract,negative,determinism}`，见 `tests/unit/p1snr/CMakeLists.txt`）；backend 三文件 = `test_p2007_joint_gate.py`、`test_p3003_parallel_resampler.py`、`test_p3006_production_pipeline.py`（提交 23a91351/48ceee59 触碰面）。

| 文件 | 四类命中 | 判定 |
|---|---|---|
| P5 `p1snr_science_test.cpp` | ①②③④ **0 命中**（`grep -n 'find(\|read_file\|assertIn' = 0 hits`，实测读该文件 :1-360 全文） | **形态合格**：全为 `checkClose` 值断言 + 独立 long-double 参考 + 冻结 NumPy 锚值 + `failures` 计退出码（:324-329）。唯一遗留：锚值来源 `run/perf-fix/P5-snr/harness/snr_oracle.py` 属 gitignored 现场（三级复核：`git --no-optional-locks ls-files \| grep snr_oracle` 只命中 `engineering/control/archive/**` 3 个 txt；`ls run/perf-fix/P5-snr/harness/` 现**无该 py**，只剩 oracle_compare.json/make_manifest.py）→ 不另立，挂 related **V1-N-09**（同文件同事实已在册） |
| P8 `p1snr_linux_test.cpp` | 值断言为主，**1 条命中** | :294-295 C6 注释声称"未参与行必须 NaN"但代码只断参与行有限 → 登记 **V15-N-08** |
| `test_p2007_joint_gate.py` | ④ + ① | 弱半边登记 **V15-N-13**；`0.80*N` 口径残留挂 V4-N-01 不重报 |
| `test_p3003_parallel_resampler.py` | ① | 登记 **V15-N-04**（test_01 的真字节断言是同文件正对照） |
| `test_p3006_production_pipeline.py` | ④ + ① | 登记 **V15-N-12**；该文件 test_01/03/04 是**行为+值断言的正确形态**（rc、CTYPE1、BUNIT、verdict 等值） |
| backend 三文件的共同弱形态 | ③ | `assertIn("stage coverage ok", self.res.stderr)`（p2007:295、p3006:118）与 `assertIn("avail=2 backend=baseline workers=2 reason=no_valid_profile", out)`（test_cpu_profile.py:181）——**整行日志文案钉死**；建议同批改为解析 `--output jsonl` 的 `type=resource` 事件字段（值断言），不另立条目（与 V15-N-09/10 同机制，已计入 §1 的 ③ 族 204 处） |

**backend 三文件的共同弱形态补记**：p3003:23 `EXE = os.path.join(REPO,"build","astrocs")` 且 `test_01` 直接 `subprocess.run([EXE,…])`——该路径不在 UT-BACKEND 的构建前置里（`ci/checks.json::UT-BACKEND` 的 `command` 只是 `unittest discover`，无 build 依赖声明），故干净 checkout 上这些"真行为断言"会因 EXE 缺失整体 error；挂 related **M8-F-004**（在册≠执行族），不另立。

## §4 「注入式反向锁」实际数量

三档口径并列（**判据与口径先行**，避免裸数）：

| 档 | 口径定义 | 数量 | 复核命令（只读） |
|---|---|---|---|
| **甲｜真·改回缺陷型**（生产码里留具名缺陷分支 → 测试设 env → 断言 rc 必红） | 生产源文件内 `getenv("ASTROCS_*FAULT")\|fault_injected(...)` 且翻转的是**生产行为**（不是测试宏） | **5 个生产文件 / 14 个注入名**：`lib/astro_image_io/src/aio_abi.cpp`（n1_hash_value_flip / n2_verify_mismatch_shortcut / n3_file_size_skip）、`lib/astro_image_io/src/hips/aio_hips_writer.cpp`（HIPS_DIAG: sentinel,skip_write；HIPS_PROV: missing_key,value_drift；HIPS_VERIFY: shortcut）、`lib/hips/src/aio_publish.cpp`（p1_stage_slow_write / p1_stage_create_fail / p1_discard_noop / p1_fsync_fail / p1_promote_fail）、`lib/core/src/module_adapters.cpp:5739`（resample_drop_band）、`lib/phase3_session/p3_output.cpp:103-104`（`#ifdef astrocs_hash_fail_inject` + `ASTROCS_HASH_FAIL_INJECT`，**唯一带编译期剔除的一条**） | `grep -rn 'fault_injected("\|getenv("ASTROCS_[A-Z0-9_]*FAULT' lib cli include` |
| **乙｜两阶段 selfcheck 族**（fork+execve 子进程注入 → 基线必 PASS 排除恒 FAIL、注入必 FAIL 排除恒 PASS） | 同文件内既有 `baseline … PASS` 又有 `child_rc==0 → return 1` | **11 个 selfcheck TU**（p1cal/p1cos/p1drz/p1hips/p1noise/p1phot/p1psf/p1sess/p1star/p1wcs/aio_abi），注册为 **12 个 ctest selfcheck 目标**（含 p2hips_selfcheck）；但注意：注入宏 `P1XX_CHECK(cs,cond,faultname)` 的语义是"命中即 `++failures`、**不执行 cond**"（实测 `lib/snr_estimator/tests/p1noise/p1noise_test_main.hpp:53-67`、p1cal/p1cos/p1psf/p1drz/p1sess 同型）⇒ 甲档语义下它**不锁生产缺陷，只锁"报告通道能变红"** | `python3 -B scripts_v15_inject2.py`；`grep -rn 'add_test\\(NAME .*selfcheck' --include=CMakeLists.txt .` |
| **丙｜Python 篡改/构造缺陷变体 → 断言红** | 同函数体内构造缺陷变体（篡改真实工件副本或合成 fixture）+ 期望失败断言 | 粗口径 **163 处 / 77 文件**；其中"复制**真实在册工件**→篡改→跑**真门**→断言红"（甲档语义在 Python 侧的对应物）**16 处 / 9 文件**（清单：`scripts_v15_inject_final.py` 输出：ci/tests/test_negative_guards.py×7、test_workflow_lock.py×1、test_ci001b_wf_step.py×1、tests/quality/test_known_failures_baseline_ci.py×2、tests/version/test_adopt006_version_gate.py×1、tests/backend/test_cpu_profile.py×1、tests/cli/test_iso_acr_gpu_isolation.py×1、test_phase123_pipeline.py×1、tests/glossary/test_glossary.py×1） | `python3 -B scripts_v15_injectB.py`、`python3 -B scripts_v15_inject_final.py` |

**结论（回答"到底几把"）**：
1. 若按负责人的黄金口径「把缺陷改回生产实现里，看门会不会红」——**真实落地 = 甲档 14 个注入名（5 个生产文件）+ 丙档 16 处篡改真实工件的门级反向锁**，合计量级为「三十」而非「上百」。
2. 被当作"有注入锁"的多数（乙档 11 TU / 12 ctest 目标）实际锁的是**测试框架的报告通路**（`CHECK` 命中注入名即计失败，`cond` 根本不参与求值），不构成缺陷回归锁；且只有 `p1star`/`p2hips`/`p3proj` 三个目标在 CMake 里用 `ENVIRONMENT` 显式驱动注入，其余 selfcheck 走内建默认名。
3. 甲档 14 名里 **13 个没有编译期剔除**（只有 `p3_output.cpp` 的 `#ifdef astrocs_hash_fail_inject` 一条有），生产库内常驻 `getenv` 分支——本轴只登记事实，是否违规属 G_GOV_GATE/§14.4 轴，**不立危害条**。
4. 与 C-16 的对应：C-16 要求"无效锁须改值/结构/行为断言或注入式反向锁"。本轴量化 ⇒ 仓内**可复用为反向锁模板的站点只有甲/丙两档 30 处**，其余为镜像/说明性断言，不能作为整改依据。
## §5 新缺陷条目（登记建议表；本轴不写 `findings/`，交前台/整理员落盘）

| 编号 | 类别 | 建议优先级 | 一句话危害 | 建议落点 | related |
|---|---|---|---|---|---|
| V15-N-01 | F_TEST_GAP | P1 | Phase2 IR 四节点/阶段序/trace 全用源文本 find 守，源序≠执行序 | `findings/F_TEST_GAP/p1/V15_test_textual.md` | FD-F-001 |
| V15-N-02 | F_TEST_GAP | P1 | Phase1 裸词析取 + 钉整行 C++ 表达式，"默认开"无运行期证据 | 同上 | FD-F-001、V15-N-01 |
| V15-N-03 | F_TEST_GAP | P1 | P3-002 四条合同全部零执行验证（文件名却叫 unit test） | 同上 | FD-F-001、V4-N-01 |
| V15-N-04 | F_TEST_GAP | P1 | §10.4「禁 hardware_concurrency」用源文本禁词守，改写法即绕 | 同上 | FD-F-001、M5a-G-005 |
| V15-N-05 | F_TEST_GAP | P1 | 冻结常数钉的是声明文本，绕常数写死仍绿 | 同上 | V15-N-06 |
| V15-N-06 | F_TEST_GAP | P1（判别力为零，建议升 P0 由负责人裁） | 白名单与正则交替集实测同集 ⇒ 恒真；权威 .inc 从未被读 | 同上 | FD-F-002、M8-F-015 |
| V15-N-07 | F_TEST_GAP | P1 | 头文件读不到即 5 条断言全绿（fail-open 假绿） | 同上 | M8-F-002、M8-F-005 |
| V15-N-08 | F_TEST_GAP | P1 | P8 注释声称"未参与行 NaN"，代码只断参与行有限（名不副实） | 同上 | V2-N-06、V1-N-09 |
| V15-N-09 | B_STD_MISMATCH | P2 | 诊断呈现被 %f 字面量钉死 + 两位裸数字子串判别力≈0 | `findings/B_STD_MISMATCH/p2/V15_diag_text_pin.md` | M8-F-009、V4-N-02 |
| V15-N-10 | B_STD_MISMATCH | P2 | 析取式 reason 命中（全仓 45 处族），原因可区分性未锁 | 同上 | M8-F-009、V15-N-09 |
| V15-N-11 | F_TEST_GAP | P2 | ISA 侧 .inc 文本锁（FD-F-001 第二实例）+ 自写 CSV 再断存在 + 钉 "+28.2%" 历史值 | `findings/F_TEST_GAP/p2/V15_isa_textual.md` | FD-F-001、M8-F-014、L23-012 |
| V15-N-12 | E_TRACE_BREAK | P2 | 台账在册 ID 断言冒充"已实现"，权威取自 `evidence/**` 免报区 | `findings/E_TRACE_BREAK/p2/V15_ledger_pin.md` | V4-N-16、M6b-G-001 |
| V15-N-13 | F_TEST_GAP | P2 | verdict 枚举白名单 + 证据键存在 + 产物 isfile 三处弱半边 | 同 V15-N-12 档 | V4-N-01、M5a-G-002 |
| V15-N-14 | F_TEST_GAP | P1 | `return status == 0 || true;` 使读回助手恒成功（M8-F-007 口径外新站） | `findings/F_TEST_GAP/p1/V15_tautology.md` | M8-F-007、M8-F-015 |
| V15-N-15 | G_GOV_GATE | P1 | 单入口门只测文档措辞；:32 把"production exe 数=0"钉成期望 ⇒ CLI 建成即红 | `findings/G_GOV_GATE/p1/V15_single_cli.md` | M6b-L18-003、M9.md §7、M5b-G-08 |

- 规模条（不占 15 名额，仅供看量级）：①类 Python **419** 行 / ③类 **204** 行（其中 `reason\|message` 族 45 处）/ ④类 **277** 行；C++ 读仓内源文件当证据 **28** 行 / 6 文件。逐条判定无法在本轴穷尽，本轴按「蕴含不成立 + 危害可陈述」筛出 15 条。
- 权威序专查结论：**未发现**新的"把旧错误口径当权威改回去"站点；相邻唯一事实是 `tests/backend/test_p2007_joint_gate.py:19/:231` 的 `0.80*N` 与冻结 §18.2 的 85/60 不同源，该事实**已在 V4-N-01 在册**，本轴只挂 related 不重报。

## §6 未覆盖面与需运行期项（不得判「无」）

1. **未覆盖面（诚实口径）**：
   - C++ 侧 122 处「读文本后 find」只精读 6 个源文本守卫站 + 11 个 selfcheck；余 ~94 处多为「读被测函数刚产出的串/文件」，属弱形式而非零蕴含，未逐条判定。
   - Python ①类 419 行只精读 15 个文件；`tests/realdata`、`tests/api`、`tests/contracts`、`tools/**` 的 checker 自测面**未精读**（`tools/**` 在「注入锁」口径下命中 710 行，多为 checker 自带 fixture，须另轴判，本轴不据以立条）。
   - ④类 277 行未逐条；`assertGreaterEqual(count, N)` 型正向计数只精读 V4-N-16 + V15-N-12/13 三处。
2. **需运行期才能定论**（本轴禁止运行，一律标注）：
   - V15-N-07：需在 `ASTROCS_REPO` 未设的 CWD 实跑 `io_adapter` 才能确认"读不到"（静态依据：:214-216 `std::ifstream h(hp)` 后无 `is_open()` 判定）。
   - V15-N-08：需实跑 `p1snr_linux_contract` 并用 valid=false 行确认现实现是否真产出 NaN（静态依据：全文件 `isnan` 命中清单里无 `local_snr` 未参与行）。
   - V15-N-06 的**恒真**结论纯静态成立（解释器复算两集合相等）；但"新增 kernel 会不会红"仍须注入实验。
   - 甲档 14 个注入名是否真的"注入必败"：须 `ctest -R` 带 env 实跑；本轴只静态确认注入点在生产源 + ctest 目标存在。
   - `build/astrocs` 缺失时 UT-BACKEND 的 p3003/p3006 真行为断言是 error 还是 skip —— 须运行期（影响其可信度评级）。
3. **交前台的机器门建议（C-16 已授权方向，不新增裁决）**：把本轴普查升级为门——对 `tests/**` 静态扫「对 `src/s/h/text/content/code` 变量做 `assertIn\|find\|re.search` 且串内含 `.cpp/.h/.inc/.md` 或 JSON 键值字面量」者，登记为「源文本守卫」清单；不直接 FAIL，而是要求每条挂一个不变量 ID + 同文件至少一条行为断言。现成挂点：`tools/quality/check_ctest_registration.py` 的 C1-C6 结构，新增检查项 `TEST-TEXTUAL-GUARD`（profiles=fast，waivable=false）。该建议与 FD-F-001 建议的「任何 `CHECK(*.find("…")` 且参数含 JSON 键值对/文件名字面量 → 登记可疑断言」同一条门，应合并实现。

