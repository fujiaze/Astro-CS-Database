# V9 · 全部门当前状态静态判定（CI 现状清单）

> 轴：V9 | 生产时间：2026-09-15（终版时点见 §1）| 权威序：①负责人设计意图 ②现行冻结文本（改后）③代码/测试自洽
> 纪律：纯静态（read/grep/glob + 问题扫描/ 内 python3 只读解析 + git --no-optional-locks 只读查询）；未运行任何编译器/cmake/ctest/测试/二进制/ci/run.py；未运行任何无 --dry-run 的仓内脚本。所有"某门当前判 X"结论一律来自**在 问题扫描/_verify/scripts_v9_*.py 内逐字复刻该门 checker 逻辑**后的复算，或仓内既有 CI 证据文件；未把"跑了门"当证据。
> 免重报：已先读 SUMMARY.md 簇表 + INDEX.md §三；同义主题挂 related，不另立条。
> 本轴的"必红/永不红"是**静态推导**，不等于"CI 已红"：本仓最近一次完整 linux-main 跑是 48ceee59（2026-09-13T21:00:03Z），其后邻站已提交 20+ 个 commit，多数门无新证据（见 §5）。

## 0. 骨架
- [x] §1 门清单规模与口径
- [x] §2 逐门五判据表（附表 问题扫描/_verify/V9_gates_table.md，130 行机器生成）
- [x] §3 【A 表】静态必红非豁免门完整清单
- [x] §4 【B 表】静态永不红门（按机制分组）
- [x] §5 【C 表】引用悬空 / 覆盖面缺口
- [x] §6 新缺陷条目（12 条）
- [x] §7 需运行期、未判项

## 1. 规模与口径

**终版时点：HEAD `d7f46dd0`，UTC 2026-09-14T17:39:54Z（北京 2026-09-15 01:39:54）。**

本会话期间 HEAD 移动 7 次（521095b8 → 5617a512 → 8c977118 → efde9ef6 → 23ffd698 → 2f03dd89 → f5c790bd → d7f46dd0），门数 121 → 130；§3 的四项必红推导与 CTEST-REGISTRATION 转绿结论，都在 f5c790bd 与 d7f46dd0 两个时点各复跑一次，结论一致（`问题扫描/_verify/scripts_v9_contrace.py` → FAIL、`scripts_v9_anchors_final.py` → errors=13 FAIL、`scripts_v9_apicontracts2.py` → findings=1、`scripts_v9_ctestreg.py` → UNREGISTERED 0 PASS、`docs/TRACEABILITY.csv` rows=63 / PSF=0 / REJ=0）。

口径：`python3` 解析 `ci/checks.json`（`json.load` → `d['checks']`），非 grep 计数。生成脚本 `问题扫描/_verify/scripts_v9_stats_final.py`（另存逐门表 `scripts_v9_table.py`），输出摘录：

```
total 130 unique 130
waivable ['TESTKIT-LIST','WORKSPACE-ADOPTION','RECONCILE-STATE','DEEP-CLANG-BUILD','DEEP-SAN-TSAN','DEEP-COV-PY','DEEP-COMPLEXITY']
profiles {'fast': 63, 'linux-main': 120, 'windows-main': 68, 'linux-deep': 7}
platform {'any': 71, 'linux': 55, 'windows': 4}
mutates_workspace ['TRACEABILITY','LINUX-MAIN-FIXTURES','LINUX-MAIN-BUILD-TREE','UT-ARCH']
heavy 6 monitor 6 | outputs non-empty 60 | prerequisite_tools 18
ctest_targets 38 | ctest-target 命令门 39 | UT-* 门 21
missing .py targets: []
KF failures: [('p1_noise_adapter','ctest','conditional')] removals 1
```

会话期间口径变化（必须写清，否则数字对不上）：本轴起步时 `checks.json` 为 **121 门 / HEAD 521095b8**，终版 **130 门 / f5c790bd**——前台在本时段连续新增 9 门（CTEST-P1SNR-SCIENCE-ALL、CTEST-P1SNR-LINUX-ALL、CTEST-P1DRZ-TASKSET-INVARIANCE、CTEST-P1STAR-ANGLE-GUARD、CTEST-IPV-TRIANGLE-BUDGET、CTEST-IPV-ABI-LAYOUT-LOCK、CTEST-IPV-ABI-LAYOUT-LOCK-SELFCHECK、CTEST-IPV-PARAMS-ABI-FAILCLOSED、CTEST-GAIA-MAGNITUDE-RANGE-BOUNDS）。
`ci/known_failures.json` 的键是 **`failures`**（现仅 1 条），`UT-CLI` 已于 2026-09-14T04:31:22Z 移入 `removals`（FD-G-002 已修，不再计入豁免面）。

## 2. 逐门五判据表

完整 130 行见附表 **`问题扫描/_verify/V9_gates_table.md`**（列：门 id / profiles / platform / 可豁免 / 是否在基线 / paths 覆盖自身 checker / paths 覆盖 ci/checks.json / 静态判定 / 失效机制）。机器生成，可复跑。

五判据的批量结论：

| 判据 | 结果 | 依据 |
|---|---|---|
| ① command 目标文件存在 | 130/130 通过（`missing .py targets: []`） | scripts_v9_stats_final.py |
| ② changed_paths 覆盖自身 checker | **99/130 不覆盖** | scripts_v9_table.py（把 changed_paths 翻成正则后匹配 checker 路径） |
| ②' changed_paths 覆盖 ci/checks.json | **124/130 不覆盖**（仅 6 门覆盖：WIN-CANDIDATE-VALIDATE、WORKFLOW-REGISTRY-BINDING、CI-BINDING-TESTS、CTEST-REGISTRATION、KNOWN-FAILURES-BASELINE-VERIFY、KNOWN-FAILURES-BASELINE-CHECK） | 同上 |
| ③ waivable / 在基线 | 可豁免 7 门；基线 `failures` 仅 1 条（`p1_noise_adapter`，kind=ctest，expected=conditional） ⇒ **check 面豁免数 = 0** | scripts_v9_kf.py / kf.json |
| ④ 能否红 | 必红 6 + 连带必红 2（§3）；永不红 20（§4）；其余需运行期或未逐一复算（§7） | §3/§4 各行 |
| ⑤ 归属五种失守机制 | 附表"失效机制"列逐行标注（A1/A2/E4/E5 编号见 §4） | — |

## 3. A 表 · 当前静态必红的非豁免门（完整清单 + 推导依据）

> 与前台工单 `FRONT_ACTION_LIST.md` §A 的 4 行对照：**A1/A2 成立但根因需订正；A3 已不成立（隔壁本时段已修）；A4 需运行期；本轴新增 3 行（A5/A6/A7）+ 1 条连带门（A8）**。

| # | 门 id | profiles | 豁免 | 判据 | 复算依据（命令 → 输出摘录） | 工单状态 |
|---|---|---|---|---|---|---|
| A5 | **CON-API-CONTRACTS** | P/L/W | 否 | `docs/contracts/API_CONTRACTS.csv`:371 登记 `estimate_mag_lim_by_density` 为 VERIFIED，但该声明已被 6d74046d（P4 割线迭代替换密度公式）从 `lib/plate_solve/cpp/ipv/include/ipv_select.h` 删除 → checker 判 `API-MISSING-AST` → exit 1 | `python3 问题扫描/_verify/scripts_v9_apicontracts2.py` → 本机工作树与"仅 tracked 文件"两种口径同判 `findings=1 (371,'estimate_mag_lim_by_density',...,'API-MISSING-AST')`；`git show HEAD:.../ipv_select.h \| grep -c estimate_mag_lim_by_density` = 1（仅存注释行 69），48ceee59 时为 2 | **工单未列**（related V2-D11 / 簇4） |
| A6 | **DOC-LINE-ANCHORS** | P/L/W | 否 | `anchor_contract.json` 的 41 条 `bindings` 有 13 条 `C4_symbol_binding`：符号在目标文件中存在但已落在任何锚范围之外（锚漂移） | `python3 问题扫描/_verify/scripts_v9_anchors_final.py` → `CLEAN-CHECKOUT DOC-LINE-ANCHORS: docs=41 anchors=808 errors=13 => FAIL; by code {'C4': 13}`（PLATESOLVE-* 11 条：文档写 `ipv_entry.cpp:275` 等，符号实测在 317/329/340/353/407/421/473/529/623/668；另 P3RSMP-DESCRIPTOR、NOISE-MINPATCH、NOISE-CMAKE）。本机工作树口径另加 35 条 C2（`设计大纲/_evidence` 影子树未跟踪副本进入 basename 索引，48 errors 总） | **工单未列**（related M6b-E-002 行锚系统性失效；本条是"该门当前必红"的新事实） |
| A7 | **CON-TRACEABILITY** | P/L/W | 否 | 核心关键词 `PSF` 与 `REJ` 在 requirement_id 拼接串中零命中 → 2 条 `TRACE-CORE-MISSING` → status=FAIL → exit 1 | `python3 问题扫描/_verify/scripts_v9_contrace.py` → `STATIC VERDICT: FAIL`，`CORE KEYWORDS MISSING: ['PSF','REJ']`；`grep -ci psf docs/TRACEABILITY.csv`=0；rows 63（48ceee59 时 67） | 工单 A1 已列，但**根因订正：不止 PSF**，肇因是 c3452d48（2026-09-14 16:19:36）净删 4 行，含 `SCI-PSF-001`、`SCI-REJ-001`、`SCI-ACR-EQUIV-001` 三条 `VERIFIED/frozen` 科学门行 |
| A2 | **TRACEABILITY-MATRIX** | P/L/W | 否 | `docs/traceability/TRACEABILITY_MATRIX.csv` **在 HEAD blob 里就带 UTF-8 BOM**；checker 以 `encoding="utf-8"` 打开 → `header[0]=='\ufeffmodule_id'\ != CSV_COLS[0]` → `SCHEMA_VIOLATION` ERROR → exit 1；且该分支直接 `return`，**后面 5 行的 JSON/CSV 发散被完全遮蔽**（修 BOM 后仍红） | `python3 问题扫描/_verify/scripts_v9_bomhead.py` → `HEAD blob first 12 bytes: b'\xef\xbb\xbfmodule_id'`、`blob == worktree: True`；`scripts_v9_matrix2.py` → `header == CSV_COLS ? False` + `POST-BOM-FIX PARITY DIFFS: 5`（MOD-astrocs-phase1-calibration / phase1-noise-snr / phase1-photometry / phase1-star / phase1-star-psf，差异列 test_id,test_path,evidence_id,evidence_status,notes） | 工单 A2 已列，**补：BOM 已入库（不是本机脏），且修 BOM 后仍有 5 行红** |
| A1 | CON-TRACEABILITY 之"删行"根因 | — | — | 见 A7 | 同 A7 | 工单 A1 |
| — | ~~CTEST-REGISTRATION~~ | P/L/W | 否 | **已不成立**：本时段前台补登记 5 门 + 重算基线，复算转绿 | `python3 问题扫描/_verify/scripts_v9_ctestreg.py` → `TARGETS_TOTAL 206 BASELINE_TOTAL 160 EXPLICIT 46 STRUCTURAL 0 UNREGISTERED 0 ... VERDICT: PASS`（同脚本在 521095b8 时为 `UNREGISTERED 12 → FAIL`） | **工单 A3 应关闭**（related STD-F7/CI-REG-002） |
| A4 | UT-BACKEND | L | 否 | 上一次真机 run 实测 FAIL(exit 1)：`tests/backend/test_p2007_joint_gate.py:117 AssertionError: 必须发出 resource gate 事件(kind=resource)`，且基线已无 UT-* 条目 ⇒ 若未修则必红（非豁免、无兜底） | `artifacts/ci/48ceee59fe52/20260913T210003Z-766d84ce/checks/UT-BACKEND.json`（exit 1，"Ran 209 tests in 946.119s"）；`问题扫描/_verify/scripts_v9_evidence.py` 汇总 `latest-verdict: {'PASS':101,'FAIL':2,...}` | 工单 A4 成立，**当前是否仍红需运行期**（本轴禁跑） |
| A8 | KNOWN-FAILURES-BASELINE-CHECK | L | 否 | **连带必红**：基线 `failures` 只剩 1 条 kind=ctest 的条件项，任何 check 面红都算"新增失败"→ fail-closed。A5/A6/A7/A2 四门只要任一未修，本门必红 | `问题扫描/_verify/scripts_v9_kf.py` → `failures: 1 条 (p1_noise_adapter, ctest, conditional)`；上一 run 直证：该门 FAIL，errors=`新增失败 1 项不在基线（fail-closed）：['check:UT-BACKEND']` | **工单未列**（属 A 表派生行，修好 A5-A7 即自动消） |
| A9 | CON-FULL-INTEGRATION | P/L/W | 否 | **连带必红**：`check_full_integration.py` 用子进程跑 `generate_contract_report.py`（TOOLS 10 个 checker），任一 `passed=false` 且不是 FORBID-HARDCODE-THREADS 白名单 → `INTEG-P1-FAIL` → exit 1。A5（api）与 A7（traceability）都在 TOOLS 名单内 ⇒ 必红 | 读码 `tools/quality/contracts/generate_contract_report.py:10-20` + `check_full_integration.py:60-77`；`问题扫描/_verify/scripts_v9_cons.py` 复算的 CON-* 判定 | **工单未列**（新缺陷 §6-3） |

**A 表合计（本轴终版）**：静态必红非豁免门 = **6 个实体红（CON-TRACEABILITY、TRACEABILITY-MATRIX、CON-API-CONTRACTS、DOC-LINE-ANCHORS、UT-BACKEND(实测) ）+ 2 个连带红（CON-FULL-INTEGRATION、KNOWN-FAILURES-BASELINE-CHECK）**；工单 A3（CTEST-REGISTRATION）应关闭。注：其中 4 门（A5/A6/A7/A2）同在 `fast` profile ⇒ 本地 `--profile fast` 复跑即红，不需要构建树。

## 4. B 表 · 静态永不红的门（按机制分组）

机制编号沿用 SUMMARY.md 簇1 的五类，这里细化为 A1/A2/E4/E5 标签（见附表列）。

### B-1「结构上不可能红」——checker 自身无失败出口
| 门 | 直证 |
|---|---|
| **TRACEABILITY**（P/L/W，非豁免） | `tools/quality/check_traceability.py` 末行 **`return 0` 无条件**（读码 :159）；坏行/缺引用只写进 JSON 报告，不影响退出码 ⇒ 永不对交付面发红。且它是 4 个 `mutates_workspace=true` 门之一，写的目标 `reports/v19r2/evidence/quality/traceability_check.json` 是 **受跟踪文件** ⇒ 每跑一次工作区即脏一次，而红又被结构性封死。（related M6b-E-001；本条补"当前 HEAD 仍如此"） |
| **CON-COMMENTS**（P/L/W，非豁免） | 注释提取用 `re.findall(r'//.*\|/\*.*?\*/', text, re.S)`：**`re.S` 使 `//.*` 一路吞到文件尾**，"一条注释"= 文件剩余全文。实测 `lib/phase2/tests/synthetic_gate.cpp` 首条"注释"跨度 **222,679 字符**，其内含"冻结"字样 ⇒ `if "冻结" not in c` 抑制生效，`COMMENT-STALE` 永不登记。逐字复刻 `问题扫描/_verify/scripts_v9_comments_exact.py` → 638 文件、`COMMENT-STALE: 0`、`=> CON-COMMENTS verdict: PASS`；而按"逐行注释"正确口径复算（`scripts_v9_cons.py`）该文件确有 1 条 V19R2 违规 → **判据被 re.S 缺陷吞掉，非"真无违规"**。（related M6a-G-001 空壳家族；本条是新根因） |
| **5 道自检壳门**：PRODUCTION-GRAPH / ISA-LEAK-SELFTEST / SERIAL-HEAVY-SELFTEST / PROD-REACH-SELFTEST / LOG-CONTRACT-SELFCHECK（全 P/L/W，全非豁免） | 登记的 command 只带 `--selftest`/`--selfcheck`：`check_pipeline_graph.py --selftest`（内存 fixture）、`check_isa_leak.py --selftest`（写死的假 objdump 文本 4 串）、`check_prod_reachability.py --selftest`（tempdir 里造一个假 CLI）、`check_log_contract.py --selfcheck`（docstring 自述"无样本文件也可独立验证检查器可运行（PASS 语义）"）。真实判定模式在 130 门里**零载体**（`grep` 复算：`check_isa_leak.py`/`check_prod_reachability.py`/`check_pipeline_graph.py` 各只出现 1 次，且都是 selftest 那次；`check_isa_leak.main` 真实模式需 `--binary`，`check_prod_reachability.main` 真实模式需 binary+`compile_commands.json`，缺则 exit 2）⇒ **交付二进制的 ISA 泄漏、生产可达性、生产图一致性、日志契约在 CI 中无人验证**，产品回归永不让这 5 门红。 |
| **CI-BINDING-TESTS**（P/L/W，非豁免） | command 为 `python3 -B -m unittest discover -s ci/tests -t ci/tests -p test_ci001b_*.py`：`ci/tests` 21 个 test 文件、379 个 `test_` 函数，只有 `test_ci001b_binding.py`(23) + `test_ci001b_wf_step.py`(10) 被匹配 ⇒ **9% 覆盖**；登记面自校验（`test_ctest_registration.py`、`test_runner_selection.py`、`test_impact_map.py`、`test_workflow_lock.py`、`test_negative_guards.py` 等 346 个 test-def）**在 CI 内零载体**。复算：`问题扫描/_verify/scripts_v9_citests.py` → `total test defs: 379  covered: 33 (9%)`。 |

### B-2「红被制度化吸收」
| 门 | 直证 |
|---|---|
| **7 个 waivable 门**（TESTKIT-LIST、WORKSPACE-ADOPTION、RECONCILE-STATE、DEEP-CLANG-BUILD、DEEP-SAN-TSAN、DEEP-COV-PY、DEEP-COMPLEXITY） | `ci/run.py` 对 waivable：prerequisite 未满足 → `SKIPPED(waivable)`；exit 77 → 同；非零退出 → waiver 语义。TESTKIT-LIST 的命令还是 `check_testkit.py --list`（纯清单，无断言）。 |
| **UT-CPU-AVX512**（waivable=false，但…） | `ci/run.py:898`：**任何**检查（含非豁免）只要以 `SKIP_EXIT_CODE=77` 结束即记 `SKIPPED(waivable)`，且 `1040-1066` 把它计入 `skipped_waivable` 而**不影响"全部非 waivable 检查 PASS"总结论** ⇒ "宿主缺能力"是登记面之外的第二豁免通道。上一 run 直证：`verdict=SKIPPED(waivable), exit=77`。风险：任一 checker/测试自行 `exit 77` 即从门禁面上隐身，而登记面显示"非豁免"。 |
| **WIN-* 4 门 + 16 个新 CTEST-* 门**（零执行证据） | `问题扫描/_verify/scripts_v9_evidence.py` → `GATES WITH NO EXECUTION EVIDENCE EVER: 20`。红不红无从谈起；`WIN-TEST-UNIT` 更已有真机直证是"0 用例记 PASS"（`run/ci/ci001b/artifact_34678549894/windows-ci-.../run/ci/win-test-summary.json`：`exit_code: 0` + `output_tail: "... No tests were found!!!"`）。（related FD-F-003） |

### B-3「0 输出 / 0 用例记 PASS」
| 门 / 机制 | 直证 |
|---|---|
| **API-DOCS、UNIT-CLOSURE** | `ci/run.py:103 EMPTY_OUTPUT_SILENCE_EXEMPT = frozenset({"API-DOCS","UNIT-CLOSURE"})` + `silent_failure()` 提前豁免 ⇒ 这两门 `exit 0` 且 stdout/stderr 全空仍记 PASS（其余 55 个空 outputs 门会 `FAIL(empty_outputs)`）。 |
| **39 道 ctest-target 门（含新增 5 门）** | `tools/quality/deep_ci_driver.py::_ctest_argv` 只在 `--junit` 时追加参数，argv 为 `ctest -R "^<target>$" --output-on-failure`，**不带 `--fail-if-no-tests`**；本仓自带直证 ctest 在"无匹配用例"时 `exit_code: 0`（上文 win-test-summary.json）⇒ 目标改名/被 `if()` 守卫关闭/未构建时，这类门记 PASS 而非红。 `CTEST-PHASE2-GATES` 还额外缺 `ctest_targets` 字段（`scripts_v9_finalgap.py` → `NO ctest_targets field: CTEST-PHASE2-GATES target= phase2_.*`），使 STD-F7"逐目标登记"对它不成立。 |
| **CTEST-P1DRZ-TASKSET-INVARIANCE**（新增门，非豁免） | 被测脚本 `lib/healpix_db/healpix_drizzle/tests/p1drz/p1drz_taskset_invariance.sh` 自带两处自宣跳过：`if ! command -v taskset ...; then echo "[SKIP] ..."; exit 0; fi` 与 `if [ -z "${aff:-}" ]; then echo "[SKIP] 无法读取 affinity"; exit 0; fi` ⇒ 宿主无 util-linux 或读不到 affinity 时 ctest 记 PASS，宪章 §10.4/§17.6 的"线程预算不变式"在该环境**从未被检查**。该门亦无任何执行证据（`_P1DRZ 在 20 个零证据门名单内`）。 |

### B-4「字面量/子串断言」（能红，但红的条件与事实脱钩；见附表逐行 E4 标注）
代表：CON-CONFIG-CONTRACTS（`'weight_mode = 2' in 源码`、错误消息原文子串）、CON-BUILD-GRAPH（`'add_library(phase2'` 等 4 个字面量 + `upm.cpp` 存在性）、CON-SCIENCE-UNITS（`unit_refs>=20`、`precision>=5` 计数阈值；实测 229/42）、CON-TEST-CONTRACTS（`unique TST>=5` 实测 36；`test_files` 缺失时只要同行有 `TST-` 即兜底放行）、CON-DOC-SYMBOLS（符号三向 substring + 40 余个人工白名单 + `repo.rglob("*")` 未跟踪影子树）、CON-TRACEABILITY（关键词 substring，见 A7）、DOC-LINE-ANCHORS（锚表与 `设计大纲/` 影子树歧义，见 A6）。⇒ 这类门"红"多由字面漂移触发，"绿"不能证明事实成立。

## 5. C 表 · 引用悬空与覆盖面缺口

1. **20 门有登记、无任何执行证据**（`scripts_v9_evidence.py`）：WIN-BUILD-RELEASE、WIN-TEST-UNIT、WIN-PACKAGE-CANDIDATE、WIN-CANDIDATE-VALIDATE、CTEST-GAIA-MANIFEST-BOUNDS、CTEST-XPSD-SPECTRUM-COUNT-BOUNDS、CTEST-P1PHOT-FIXGATES、CTEST-P1STAR-MAD、CTEST-P1PSF-PRODPATH-CENTROID、CTEST-IPV-EXTRACT-WCS-SIP-FAILCLOSED、CTEST-IPV-MAG-ITER、CTEST-P1SNR-SCIENCE-ALL、CTEST-P1SNR-LINUX-ALL、CTEST-P1DRZ-TASKSET-INVARIANCE、CTEST-P1STAR-ANGLE-GUARD、CTEST-IPV-TRIANGLE-BUDGET、CTEST-IPV-ABI-LAYOUT-LOCK、CTEST-IPV-ABI-LAYOUT-LOCK-SELFCHECK、CTEST-IPV-PARAMS-ABI-FAILCLOSED、CTEST-GAIA-MAGNITUDE-RANGE-BOUNDS。其中 **16 个是非豁免 linux-main 门** ⇒ 一旦真跑，红/绿未知（尤其 §4 B-3 的"0 用例记 PASS"路径下大概率是绿）。
2. **`ci/impact_map.json` 覆盖面缺口**：24 条 rules + 15 条 fallback 只触达 56+15 个 id；**74/130 门既不在任何 rule 也不在 fallback**（`scripts_v9_impact.py`），其中 **7 个属 fast**：THREAD-BUDGET、WORKFLOW-REGISTRY-BINDING、CI-BINDING-TESTS、CTEST-REGISTRATION、KNOWN-FAILURES-BASELINE-VERIFY、UT-GAIA-ZLIB、**DOC-LINE-ANCHORS（本轴判定的必红门之一）**。后果：按 `工程控制/.../04_FOREGROUND_AGENT_RUNBOOK.md:54` 的标准定向复跑 `python3 ci/run.py --profile fast --changed-from HEAD` **永远看不到这些门** ⇒ "定向复跑全绿"不等于 fast 全绿（本次 DOC-LINE-ANCHORS 就是被漏掉的红门）。
3. **登记面自校验工具在册无载体**：`ci/validate_registry.py`（checks.json schema 校验）在 `ci/checks.json` 命令里 0 次、在 `.github/workflows/` 0 次，只有 `ci/tests` 15 处引用——而 `ci/tests` 只有 9% 进 CI（B-1）⇒ 门数从 121 涨到 130、`waivable` 翻转（DEEP-SAN-ASAN、DEEP-COV-CPP 由可豁免变非豁免）这类登记面变更**没有 schema/一致性门把关**。同口径：`tools/quality/check_registration_timeouts.py` 在 checks.json/workflows/ci-tests 全 0 载体（它存在的目的正是校验 `timeout_seconds` 与 ctest `TIMEOUT` 一致性；新增 `CTEST-P1DRZ-TASKSET-INVARIANCE` 的 `timeout_seconds=900` 与内层 `step-timeout` 缺省 600 的冲突因此无人校验）。
4. **changed_paths 覆盖面**：99/130 门的 changed_paths 不含自身 checker；124/130 不含 `ci/checks.json`（附表逐行）。即"改坏 checker 本身"或"改登记面"按 `--changed-from`/`--focus` 定向口径不会触发该门。注：workflow 目前跑的是 `ci/run.py --profile X`（`.github/workflows/ci-linux.yml:141`、`ci-windows.yml:110`，不带 `--changed-from`）⇒ 全 profile 无裁剪，changed_paths 只在定向复跑口径下起作用（正是第 2 条的漏门场景）。
5. **旧证据不可用于判新态**：6 个 DEEP 门最新证据时间戳停在 **2026-09-05T20:48:42Z**（`scripts_v9_evidence.py` "oldest 15"），其中 DEEP-SAN-ASAN/DEEP-COV-CPP 现已非豁免，9-05 的 `SKIPPED(waivable)` 证据已失效。
6. **工作区洁净性豁免被用于掩盖规范违规**：UT-CLI 的 `dirty_ignore_prefixes: ["astrocs_run_","run/"]` + `dirty_ignore_exact: [resource_samples.csv, resource_summary.json, worker_balance.csv]`，而 AGENTS.md 目录规范明确"CLI 运行产物 `astrocs_run_*.json` 一律落 `run/cli_runs/``、运行产物禁落根目录"；实测根目录 `astrocs_run_*.json` **56 个**（`ls -1 astrocs_run_*.json | wc -l`）。⇒ 违规被登记成"该门看不见"，而不是被修掉。

## 6. 新缺陷条目（12 条，宁窄而实）

> 编号沿用 V9-N-`x`；每条给"命令 + 输出摘录"。related 指向既有台账，避免重报。

1. **V9-N-01（P1，A 表补项）DOC-LINE-ANCHORS 当前静态必红，且前台工单 §A 未列。** 13 条 `C4_symbol_binding` BINDING_VIOLATION（PLATESOLVE.md 的 12 行函数表锚整体漂移 ~42 行 + P3RSMP-DESCRIPTOR + NOISE-MINPATCH + NOISE-CMAKE），肇因是 P8/P11 改 `ipv_entry.cpp`/`module_adapters.cpp` 未同提交订正锚。命令+输出：`python3 问题扫描/_verify/scripts_v9_anchors_final.py` → `errors=13 => FAIL; by code {'C4': 13}`；手工核对 `docs/algorithms/PLATESOLVE.md:101` 写 `ipv_entry.cpp:275`，而 `ipv_solve_create` 实测在 `ipv_entry.cpp:317`。**该门在 fast profile ⇒ 邻站本地 `--profile fast` 即红，且被 impact_map 漏门（见 N-09）。** related：M6b-E-002（行锚系统性失效）。
2. **V9-N-02（P1，A 表补项）CON-API-CONTRACTS 当前静态必红：合同表把已删除的公开 API 仍登记为 VERIFIED。** `API_CONTRACTS.csv:371 estimate_mag_lim_by_density` ↔ `ipv_select.h` 中该声明已被 6d74046d 删除（仅存注释）。两种口径复算同判：`scripts_v9_apicontracts2.py` → 工作树与 tracked-only 各 `findings=1`。related：V2-D11（该条已挂账，但**未进工单 §A**，本轴补入 A 表）。
3. **V9-N-03（P1）CON-FULL-INTEGRATION 与 KNOWN-FAILURES-BASELINE-CHECK 是"放大器"门：任一实体红都会把它们一起拖红，工单必须按根因合并而非按门派单。** 读码 `generate_contract_report.py:10-20`（TOOLS 含 check_traceability/check_api_contracts/check_comments）+`check_full_integration.py:60-77`（非白名单 FAIL → INTEG-P1-FAIL → exit 1）；`known_failures_baseline.py` check 模式对"新增失败"fail-closed（上一 run 直证 errors=`新增失败 1 项不在基线（fail-closed）：['check:UT-BACKEND']`）。⇒ 当前 1 个 UT-BACKEND 红 + 4 个静态红 = 实体 5 红 + 连带 2 红。
4. **V9-N-04（P1）工单 A3（CTEST-REGISTRATION 必红）已失效，A1 根因不完整——隔壁照单施工会修错对象。** 复算 `scripts_v9_ctestreg.py` → `UNREGISTERED 0 / VERDICT: PASS`（521095b8 同脚本为 `UNREGISTERED 12 → FAIL`）；A1 关键词缺失实测为 **PSF + REJ 两个**（`scripts_v9_contrace.py`），且直接肇因是 c3452d48 一次净删 4 行（`git diff 535e7387 c3452d48 -- docs/TRACEABILITY.csv` 显示删 `SCI-PSF-001`/`SCI-REJ-001`/`SCI-ACR-EQUIV-001` 三条 VERIFIED/frozen 行）。⇒ 修复动作应是**回滚删行**，不是补文档。related：A1、簇4。
5. **V9-N-05（P1）TRACEABILITY-MATRIX 的 UTF-8 BOM 已进 HEAD blob（非本机脏），且 checker 在"表头不等"处 return，把其后 5 行 JSON/CSV 发散完全遮蔽。** `scripts_v9_bomhead.py` → `HEAD blob first 12 bytes: b'\xef\xbb\xbfmodule_id'`；`scripts_v9_matrix2.py` → `header == CSV_COLS ? False` + `POST-BOM-FIX PARITY DIFFS: 5`（phase1-calibration/noise-snr/photometry/star/star-psf）。⇒ 只去 BOM 不重跑 `gen_traceability_csv.py` 仍红；只重跑不查 `_check_csv_parity` 的早退设计则看不到全貌。related：工单 A2（补强）。
6. **V9-N-06（P1）5 道非豁免门只跑 `--selftest/--selfcheck`，真实判定模式在登记面零载体 ⇒ 交付二进制 ISA 泄漏、生产可达性、生产图一致性、日志契约、串行重计算自检在 CI 中实际无人验证。** 复算：`grep` checks.json 中 `check_isa_leak.py`/`check_prod_reachability.py`/`check_pipeline_graph.py` 各 1 次命中且均为 selftest；读码 `check_isa_leak.main` 真实模式需 `--binary`（缺则 exit 1）、`check_prod_reachability.main` 缺 binary/compile_commands 则 exit 2。命令：`python3 问题扫描/_verify/scripts_v9_real2.py`、`scripts_v9_cmds.py`。related：簇1 机制①（新实例，且属"门名与执行面不符"）。
7. **V9-N-07（P2）CON-COMMENTS 的 `COMMENT-STALE` 子句被自身正则缺陷永久抑制：`re.findall(r'//.*\|...', text, re.S)` 中 `re.S` 让 `//.*` 吞到文件尾，只要文件任意位置出现"冻结"二字，该文件全部 `V19R2/V19R3` 违规一并消失。** 逐字复刻 `scripts_v9_comments_exact.py` → 638 文件 `COMMENT-STALE: 0 / PASS`；同仓按逐行口径（`scripts_v9_cons.py`）实有 1 处 `lib/phase2/tests/synthetic_gate.cpp:5488 // F-V19R2-UPM-002：…`；且 `lib` 内 181/638 文件含"冻结"字样、含 V19R2/V19R3 的 2 个文件 100% 被抑制。⇒ 该门"绿"是假绿，隔壁若只看 verdict 会误认为注释卫生已达标。
8. **V9-N-08（P1）39 道 ctest-target 门不带 `--fail-if-no-tests` ⇒ 目标未注册/被守卫关闭时记 PASS；本仓自带真机直证 ctest 无匹配用例退出码为 0。** `run/ci/ci001b/artifact_34678549894/windows-ci-.../run/ci/win-test-summary.json`：`"exit_code": 0` + `"output_tail": "... No tests were found!!!"`。叠加：新增 5 门全部无执行证据（§5-1）+`CTEST-PHASE2-GATES` 缺 `ctest_targets` 字段（`scripts_v9_finalgap.py`）。⇒ STD-F7"逐目标显式不可豁免登记"目前只完成登记面，执行面仍可静默绿。related：FD-F-003 / E4（同机制，本条把面从 WIN-TEST-UNIT 扩到全部 39 门）。
9. **V9-N-09（P1）`ci/impact_map.json` 漏 74/130 门，其中 7 个在 fast（含本轴必红门 DOC-LINE-ANCHORS）⇒ 工单/runbook 推荐的 `--changed-from HEAD` 定向复跑会系统性看不到这些门，"定向全绿"不能当交付证据。** 复算：`scripts_v9_impact2.py` → `fast gates NOT reachable: 7 [..., 'DOC-LINE-ANCHORS']`、`linux-main NOT reachable: 64`；runbook 依据 `工程控制/AstroCS_ALPHA0.11.0_EXISTING_WORKSPACE_CI_CONTROL_V8_1_20260905/04_FOREGROUND_AGENT_RUNBOOK.md:54`。
10. **V9-N-10（P2）CI 登记面的自校验链条整体无载体：`validate_registry.py` 零门、`check_registration_timeouts.py` 零载体、`CI-BINDING-TESTS` 只跑 ci/tests 的 9%。** 命令：`scripts_v9_pin3.py` → `validate_registry.py gates=0 wf-hits=0 ci/tests-hits=15`、`check_registration_timeouts.py gates=0 wf-hits=0 ci/tests-hits=0`；`scripts_v9_citests.py` → `379 defs, covered 33 (9%)`。后果实证：本时段门数 121→130、DEEP-SAN-ASAN/DEEP-COV-CPP 的 `waivable` 由 true→false 翻转，均无一致性命门拦截（`scripts_v9_stats_final.py` 的 waivable 名单里两者已缺席；9-05 旧证据仍记 SKIPPED(waivable)）。
11. **V9-N-11（P1）豁免的第二通道：`ci/run.py:898` 把 exit 77 无条件判为 `SKIPPED(waivable)`，不看 `waivable` 字段，且 `1040-1066` 使总结论仍为"全部非 waivable 检查 PASS"。** 直证：UT-CPU-AVX512 登记 `waivable=false`（`scripts_v9_waiv.py` 输出）却在上一 run 得 `SKIPPED(waivable) exit=77`。⇒ 任何 checker 自行 `exit 77` 即可从门禁面隐身；建议把 77 语义限定为"仅 waivable=true 的门可用"，否则 `waivable` 字段与 CI 总判定都可被绕过。
12. **V9-N-12（P2）工作区洁净性检查被用来固定化目录规范违规：UT-CLI 的 `dirty_ignore_prefixes=["astrocs_run_"]` 正对 AGENTS.md 明令禁止落根目录的运行产物。** 直证：`ls -1 astrocs_run_*.json | wc -l` → 56（全部未跟踪、`git ls-files` 命中 0）；登记面见 `问题扫描/_verify/scripts_v9_waiv.py` 输出的 UT-CLI 条目。⇒ 与 AGENTS.md"CI 资源产物路径…落 `run/resource/`""禁止将运行产物产出到项目根目录"冲突：门不再为违规发红，而是把违规写进 ignore 名单。related：FD-G-002 家族（UT-CLI 基线已删是另一回事）。

## 7. 需运行期、未判

| 项 | 为什么静态定不了 |
|---|---|
| UT-BACKEND 当前是否仍红 | 上次实测 FAIL（48ceee59），其后 P7-P10 大量改 CPU 利用率/门禁口径（宪章 §10.5/§17.6），`test_p2007_joint_gate` 是否已随 §18.2 口径修复只能跑测试看 |
| UT-CLI 是否已真绿（基线条目已删） | `removals` 的 reason 称"干净检出 174 OK"，但本轴禁跑测试；且根目录 `astrocs_run_*` 仍在（N-12），dirty 判定需实跑 |
| 20 个零证据门（含 4 个 WIN-*、16 个新 CTEST-*）的红/绿 | 需要构建树与 Windows 真机；本轴禁 cmake/ctest/二进制 |
| 39 道 ctest-target 门"目标是否真存在" | 需 configure 后的 CTest 清单；静态只能证 `add_test` 名存在于 cmake 源（`scripts_v9_ctestnames.py` → `MISSING_TARGETS 0`），不能证其在 `run/ci/build-gcc-release` 中被注册（尤其 `tests/unit/CMakeLists.txt:1138` 的 `if(TARGET p1noise_under_test AND TARGET astrocs_p1_noise)` 守卫、`lib/healpix_db/healpix_drizzle/tests/p1drz/CMakeLists.txt:94` 的 Linux-only 守卫） |
| CON-DOC-SYMBOLS / CON-EXECUTION-CONTRACTS / CON-FORBIDDEN-PATTERNS 等 43 个静态门的当前判定 | 未逐一复算（附表标"未逐一复算"）；其中 CON-DOC-SYMBOLS 需先决定"本机工作树含 `设计大纲/`、`run/` 影子"与"干净检出"两种口径，二者结论可能不同 |
| 129/130 道门在本次登记变更后的 CI 总判定 | 需 `ci/run.py` 真跑（本轴禁） |
| `p1_noise_adapter`（基线唯一存活项）是否真"条件未激活" | 静态看 `add_subdirectory(lib/snr_estimator)` 在根 `CMakeLists.txt:210` 仍是注释 ⇒ 干净树 guard 恒假；但需真机构建确认 |
