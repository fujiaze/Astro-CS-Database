# V10 · 外部输入边界校验"做一半"普查（从 V5-N-03 / V5-N-01 外推）

> 轴定位：**发现新缺陷**，不复核挂账。判定顺序（负责人明令）＝①负责人设计意图（设计大纲/、memory.md、根 AGENTS.md 十要素、问题扫描/40_OWNER_DECISIONS.md 已裁条）②现行冻结文本（改后）③代码/测试自洽。
> **不把"与旧文档不一致"当缺陷**；专查"把旧错误口径当权威改回去"。
> 纪律：纯静态（read/grep/glob/write/edit + 在 `问题扫描/` 内跑 python3 只读脚本 + `git --no-optional-locks` 只读查询）。**不跑编译器/cmake/ctest/测试/二进制/ci/run.py；不运行任何无 --dry-run 的仓内脚本**；不删改名；不动 `findings/` 与 `账本/`。
> 免重报基线：`SUMMARY.md` 簇 6「边界校验做一半」（总述归 `_merge/M9.md` §3 同仓正对照 12 行）+ `INDEX.md` §三 + `_verify/V5.md`（V5-N-01 magnitudeRange 裸 atof、V5-N-03 已登记的 spectrumStart/Step/Bits atoi）。同义主题一律挂 `related`，不另立条。
> 时点：工作区 HEAD = 待填（本档案书写于 V10 复核会话）。

## §0 普查口径与命令

（待填：解析函数清单、目录排除前缀、命中计数口径、外部输入六类定义）

## §1 结论清单（按"最坏后果"排序：静默丢数据 → 越界 → 崩溃 → 科学偏差 → 仅诊断）

（待填）

## §2 已知正确先例对照表（同仓/同文件正对照）

（待填）

## §3 反向钉死检查（是否把旧错误口径当权威改回）

（待填）

## §4 测试面（每站点有无在库回归锁）

（待填）

## §5 需运行期、未判项

（待填）

## §6 与在册条目的 related 映射（不重报清单）

（待填）
