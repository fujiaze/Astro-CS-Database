# V10 轴：外部输入数值解析（裸解析站点）

- 层：V10（任务=发现新缺陷，不复核挂账）
- 时点：清点脚本跑于 HEAD 521095b8；结论锚点复核于 HEAD 8b6e9c5f。期间工作树被并发修改（lib/healpix_db/healpix_drizzle/{drizzle_engine.h,hp_drizzle_api.cpp,hp_drizzle_api.h}、lib/core/src/module_adapters.cpp 等为脏）；本文行号一律按复核时点现文，并已在脏树上重新 grep 确认。
- 免重报基线：问题扫描/SUMMARY.md 簇表 + INDEX.md 第三节。已挂账的同类站点（V5-N-01 magnitudeRange、M9-H-2 spectrumStart/Step/Bits、M9-H-3 data_position）不另立条，仅在第六节挂 related。
- 纪律：全程静态（read / glob / bash 只读 grep 与 sed / 本目录内 python3 只读脚本 / git --no-optional-locks 只读查询）；未跑编译器、cmake、ctest、测试、二进制、ci/run.py；档案只写本目录，未触 findings/ 与 账本/。

## 第 0 节 口径（清点数字与时点）

脚本：python3 问题扫描/_verify/scripts_v10_census2.py（权威版；v1 因把 vendored cfitsio 计入而作废）。站点明细：问题扫描/_verify/_v10_sites.txt（214 行，由 scripts_v10_dump.py 生成）。

口径定义：
1. 文件集 = git -c core.quotepath=off --no-optional-locks ls-files 加模式 *.c *.cpp *.cc *.h *.hpp（必须关 quotepath，否则非 ASCII 路径返回带转义的名字导致 READFAIL）
2. 排除任意路径段属于 {third_party, build, run, out, Testing_archive}（SUMMARY 已声明 third_party 不展开），并排除 astrocs_p1sess_* 与 engineering/
3. 函数集 = atof atoi atol strtol strtoll strtoul strtoull strtod scanf fscanf sscanf stoi stol stoul stoll stoull stof stod，加上自定义解析器 parse_* / json_get_* / get_num / geti / parseInt / parseDouble / classifySipKey
4. 剔除纯注释行命中

结果（HEAD 521095b8 时点）：

| 指标 | 值 |
|---|---|
| 候选文件 | 890 |
| 命中站点（去注释后） | 263（另剔除纯注释 28） |
| 生产面（lib/cli/runtime/modules/providers/include） | 233 |
| 测试/工具面 | 30 |

生产面按函数：atoi 69 / sscanf 41 / atof 32 / strtod 21 / strtol 12 / atol 11 / stod 10 / fscanf 9 / stoull 7 / stoi 6 / strtoull 6 / strtoul 4 / stoul 2 / strtoll 2 / scanf 1。

## 第 1 节 结论清单（按最坏后果排序）

### N-01【越界读 + 静默丢数据】readFits 短读后 img.pixels 与 img.width/height 不一致且仍返回成功，引擎按声明尺寸索引

- 输入面：FITS 文件（外部帧）
- 站点 lib/healpix_db/healpix_drizzle/fits_reader.cpp：
    - :418-422 bytes_per_pixel = std::abs(bitpix)/8; n_pixels = (size_t)width*(size_t)height*(size_t)naxis3; data_size = n_pixels*(size_t)bytes_per_pixel; std::vector raw(data_size);
    - :425-427 短读只告警并按实读量缩像素数：if (got < data_size) { fprintf(stderr, "[fits_reader] 警告: 数据读取不完整 (%zu/%zu 字节)\n", got, data_size); n_pixels = got / (size_t)bytes_per_pixel;  // 按实际读取量处理
    - :431 img.pixels.resize(n_pixels);   ← 缓冲按实读量
    - :514-515 img.width = width; img.height = height;   ← 尺寸仍为头声明值；函数 return true（fail-open）
- 消费式（越界可证）：drizzle_engine.cpp:1808 for (int y = y0; y < y1; y++)（条带上界由 img.height 决定，见 :1597 drizzle_deterministic_stripe_count(int height)）、:1826 for (int x = 0; x < img.width; x++)、:1833 Scalar pixelValue = pixels[(size_t)y * (size_t)img.width + (size_t)x];（pixels 即 img.pixels.data()，:835/:953/:2056/:2070 传 pointer）⇒ 最大索引 width*height-1 大于 pixels.size()-1。:1837/:1842/:1848 对 snrData/weightData/varianceData 同型（各自另一次 readFits，同样可短读）。
- 判①：NAXIS 有 <=0 拒（:315），无上限（见 N-06）；截断量不参与任何一致性判定。判②：fail-open。判③：是（vector 长度对上标基准）。判④：短读行按界外内存值参与累加。判⑤：无锁。
- 命令 + 输出摘录：

      $ grep -n 'got < data_size\|n_pixels = got\|img.pixels.resize\|img.width    = width\|std::vector<uint8_t> raw' lib/healpix_db/healpix_drizzle/fits_reader.cpp
      422:    std::vector<uint8_t> raw(data_size);
      425:    if (got < data_size) {
      427:        n_pixels = got / (size_t)bytes_per_pixel;  // 按实际读取量处理
      431:    img.pixels.resize(n_pixels);
      514:    img.width    = width;

      $ grep -n 'pixels.size()' lib/healpix_db/healpix_drizzle/drizzle_engine.cpp
      846:            if (local >= tile.pixels.size()) continue;
      964:            if (local >= tile.pixels.size()) continue;
      1622:            if (local >= d.pixels.size()) d.pixels.resize((size_t)local + 1);
      2239:            if (local >= tile.pixels.size()) continue;
      （全部是输出侧 tile 累加器，无一条「输入 pixels 长度 vs img.width*img.height」校验）

- 同仓正对照（同族字段已做对）：lib/astro_image_io/src/aio_xisf.cpp:500-521（W1-AIO-001：data_size 小于 expected_bytes 即拒，注释逐字 truncated attachment must hard-fail before pixel conversion）、lib/astro_image_io/src/hiss_reader.cpp:290-302（offset/compressed_size 对照 filesize 硬拒）、同文件 :503 RGB 分支自己用了 if (n_pixels >= plane_size * 3)（作者知道 n_pixels 与 w*h 可不一致，主路径却未夹）。
- 修复（对齐既有写法）：短读即 return false（与 xisf/hiss 同口径），或按 got 同步反推 width/height；禁「警告后继续」。

### N-02【越界读 → 崩溃 + 静默 WCS 偏差】readFits 侧 A_ORDER/AP_ORDER 未做 [0,5] 值域，SIP 求值按 order 索引 36 元素数组

- 输入面：FITS 文件头 KV
- 站点：fits_reader.cpp:289-292 else if (k == "A_ORDER") { a_order = parseInt(v); has_a_order = true; }（AP/BP 同）→ :76 return (int)std::strtol(t.c_str(), nullptr, 10);（无 endptr、无界）→ :395-396 img.wcs.sip.order = has_a_order ? a_order : 0; img.wcs.sip.ap_order = has_ap_order ? ap_order : 0;；:378-381 仅做 A==B 一致性 warning
- 缺口实证（脏树现文 grep 零命中）：

      $ grep -n 'order > 5\|order < 0\|\[0,5\]\|SIP_MAX\|clamp' lib/healpix_db/healpix_drizzle/fits_reader.cpp lib/healpix_db/healpix_drizzle/wcs_sip.cpp
      （无输出：readFits 与 WcsSip 构造都不夹 order）

- 消费式：wcs_sip.cpp:85-88 for (int i = 0; i <= order; i++) 内 result += T(coeffs[i * 6 + j]) * pdx * pdy;，调用 :253-255（m_wcs.sip.order）与 :304-306（m_wcs.sip.ap_order）；系数数组 fits_reader.h:12-13 double a[36]、double b[36]（ap/bp 同）⇒ 合法下标最大 35。order=6 时 i=6,j=0 → 索引 36 越入相邻数组（静默错值）；order=1000 → 索引 6000 且循环约 5e5 次（界外读/挂起/崩）。
- 同库另两条边界已夹，唯此边漏（做一半）：
    - hp_drizzle_api.cpp:461-465（frame-KV 边）if (a_order < 0 || a_order > 5 || b_order < 0 || b_order > 5 || ap_order < 0 || ap_order > 5 || bp_order < 0 || bp_order > 5) { err = "SIP order 非法: 正式支持 [0,5] (A_ORDER/B_ORDER/AP_ORDER/BP_ORDER 之一越界)"; return -10; }
    - hp_drizzle_api.cpp:99-101（C-ABI 入参边）if (in->sip_order < 0 || in->sip_order > 5 || in->sip_ap_order < 0 || in->sip_ap_order > 5) {（→ return 6）
    - 同文件 fits_reader.cpp:163（写侧系数下标）if (i < 0 || i > 5 || j < 0 || j > 5) return 0;  // 6x6 数组边界 ⇒ 存系数夹了界，order 本身没夹
    - 生产写侧同口径：lib/core/src/module_adapters.cpp:1853 报 "sip order out of drizzle contract [0,5] (order="（DATA 拒；注释 :1825-1826 自述「逐项有限性校验…禁静默截断」）
- 可达性：fits_reader.cpp 与 wcs_sip.cpp 同时进三张生产清单 —— cli/CMakeLists.txt:157（DRIZZLE_SRCS → astrocs 可执行）、CMakeLists.txt:409-410、lib/drizzle/CMakeLists.txt:21；入口 hp_drizzle_api.cpp:185 hp_drizzle_fits_to_ahpx（:232/:249/:280 三次 readFits），该入口只夹 nside（nside <= 0 或非 2 的幂）、pixfrac（超出 [0,1]）与 photscal，无 order 边。现产主链（module_adapters.cpp:3104 调 hp_drizzle_run(frame)）走已夹的 KV 边 ⇒ 本条定「导出 API + CLI 静态链入面可达」，不判 P0。
- ⑤ 测试面（半锁）：grep -rln 'A_ORDER' tests lib 仅得 lib/healpix_db/healpix_drizzle/tests/{mini_sip1000.cpp, p0_sip_order_guard_test.cpp, test_pipeline_adapter.py}，三者全部经 aio_frame_kv_set（KV 边）⇒ 真文件边零锁。p0_sip_order_guard_test.cpp T1-T7 与 mini_sip1000.cpp（A_ORDER=1000 期望 rc==-10）只证明 KV 边已修，反而给出「该字段已全面防护」的错觉。
- 修复：在 readFits 落 sip.order/sip.ap_order 处复用同一 [0,5] 判定并硬失败（同码 -10、同错误文），并补一条 readFits 面锁。

### N-03【静默数据损坏 + 挂账口径与代码不符】gaia XPSD itemSize 仍裸 atoi，而它正是字节逆置换的步长与循环界

- 输入面：XPSD 文件自报 XML 属性（客户端载荷）
- 站点：lib/gaia_xpsd_client/src/gaia_client.c:1213 xf->item_size = atoi(item_sz);（同一 if (data_tag) 块内 :1201-1202 atof、:1208 strtoull、:1223/:1224/:1233 atoi 均已另案，见第六节）
- 消费式（参与寻址，非仅透传）：:1106 与 :1127 if (xf->use_byte_shuffle && xf->item_size > 1) { if (byte_unshuffle(scratch, block_size, xf->item_size) != 0) return NULL; } → :1056-1071

      static int byte_unshuffle(uint8_t *data, size_t data_len, int item_size) {
          if (item_size <= 1 || data_len == 0) return 0;
          size_t n = data_len / item_size;
          if (n == 0) return 0;
          ...
          for (int i = 0; i < item_size; i++) {
              size_t src_start = (size_t)i * n;
              for (size_t j = 0; j < n; j++) { tmp[j * item_size + i] = data[src_start + j]; }

  界内性我核过：写最大下标 n*item_size-1 不超过 data_len-1，读最大 item_size*n-1 亦然 ⇒ 无越界（不夸大）；缺陷在语义：item_size 与实际记录步长不符时结果静默错序。
- 三条 fail-open（全部 rc=0 成功）：① item_size 小于等于 1（属性缺失、非数字致 atoi=0、负数、atoi 溢出回绕）⇒ 文件声明 +sh 却整块跳过逆置换；② block_size 小于 item_size ⇒ n==0 跳过；③ block_size 不能被 item_size 整除 ⇒ 尾部余数字节不重排（半置换）。
- 同函数自述纪律与该口子的冲突：B13-R13-5 注释逐字「修复前 malloc 失败静默 return, 调用方把未逆置换的数据当合法科学数据继续解析 (静默数据损坏)」⇒ 作者已认定该后果不可接受，但只关了 malloc 分支。
- 反向钉死检查（本条重点）：问题扫描/账本/FIX_LEDGER.csv 的 M9-H-2 行 verified_by 逐字为「残余: spectrumStart/Step/Bits 仍 atoi 但仅元数据透传不参与寻址(已自登记)」。我核：该口径只对这三值成立（spectrum_start/step/bits 确实只在 :1216-1219/:1223-1224/:1233 赋值、:2619-2620 出参透传）；itemSize 不在三值内，且参与下标与循环界。风险 = 后续把「三值仍 atoi 无害」扩读成「这一片 atoi 都无害」，从而把 item_size 按旧口径免修或改回。请负责人裁：M9-H-2 行补注「itemSize 另属寻址面，不在本条免检范围」。
- ⑤ 测试：tests/unit/gaia_unshuffle_test.c:43-63 覆盖 item_size 小于等于 1、n==0、data_len==0、OOM 注入、整倍数（helper 单元锁，且把 no-op 钉成契约）；tests/unit/gaia_xpsd_fixture_gen.c:233/282/305 只用一致 item_size ⇒ 无一条「声明 +sh 但 itemSize 非法 ⇒ 整文件拒绝」的边级锁。
- 修复：与同文件 parse_bounded_int（:1150-1160，首字符、尾字符、上限、溢出四重校验；全文件现仅 :1228 一处使用）同口径收口 itemSize；item_size 小于 2 或 block_size 不整除即拒（非 0 返回）。

### N-04【静默科学偏差】aio_fits 的 BSCALE/BZERO 解析 catch(...) 吞错取默认，且不吃 FITS 的 D 指数记法

- 输入面：FITS 文件头（定标科学量）
- 站点 lib/astro_image_io/src/aio_fits.cpp:161-162

      else if (key == "BSCALE") { try { hdr.bscale = std::stod(kw.value); } catch (...) {} }
      else if (key == "BZERO")  { try { hdr.bzero  = std::stod(kw.value); } catch (...) {} }

  默认见 :117-118（hdr.bscale = 1.0; hdr.bzero = 0.0;）⇒ "abc"（invalid_argument）与 "1e999"（out_of_range）静默保留默认；"1.5xyz" 前缀合法后缀垃圾 ⇒ 静默取 1.5（无 endptr）；无 isfinite ⇒ "nan"/"inf" 可过。
- 消费式（进交付像素）：:853-856 pixel_data_f64[i] = hdr.bscale * pixel_data_f64[i] + hdr.bzero;、:886-889 pixel_data[i] = (float)(hdr.bscale * (double)pixel_data[i] + hdr.bzero);（应用前只判 bscale 不等于 1 或 bzero 不等于 0）
- 与同仓 D 记法口径冲突：lib/healpix_db/healpix_drizzle/fits_reader.cpp:64-71（parseDouble）自述「兼容 FITS 的 D 指数记法，如 1.5D-3」并做 D→E 归一；aio_fits.cpp 无该归一 ⇒ 同一合法 FITS 头在两个读面得到 0.0015 与 1.5（差三个数量级，不抛不报，属静默测光错）。
- ① 无 endptr、无 isfinite、无上界；② fail-open；③ 不参与寻址；④ 正是测光标量；⑤ 测试：grep -rn BSCALE tests 的三处命中均为「键存在/类型合法」的正向断言（tests/backend/test_p3005_fits_output.py:74、tests/backend/test_p3_output.py:173、tests/cli/test_phase3_inprocess.py:286-288），无畸形值锁。
- 同函数已做对的一半：:168-187 对 NAXIS 有 <=0 拒 + P0-4 的 65535 上限（注释自述「与 aio_xisf read_file 既有校验口径一致」）⇒ 同一 read_file 里几何维度夹了值域，定标系数没有。
- 修复：stod 带 endptr + 尾字符判 + isfinite + BSCALE 大于 0；D 记法按 fits_reader 同法归一；解析失败硬失败（勿吞）。
- 同型站点（一并修）：aio_fits.cpp:386-388（kw_float 的 try/stod/catch 取默认）；drizzle_engine.cpp:1147-1149 与 :2152-2154（get_meta("GAIN") 后 try { hmeta.gain = std::stod(gain_str); } catch (...) {} ⇒ gain 写进 HISS 头 JSON（hiss_common.cpp:194），并可被 hiss_common.cpp:273 的 get_num("gain") 读回 ⇒ 下游标定静默用错增益）。该文件另有 :332/:493-512/:557-561/:1165/:1322/:2038 多处 isfinite，说明这是漏而不是口径。

### N-05【静默丢数据】HiPS SNR TSV 坏行只计数，而计数器全仓无消费者（死码）

- 输入面：HiPS 产品集 *_snr.tsv（客户端可读交付物）
- 站点 lib/astro_image_io/src/hips/aio_hips_reader.cpp:332-339

      if (std::sscanf(line.c_str(), "%lld %lf %lf %lf %u %u", &sid, &ra, &dec, &snr, &qf, &ps) == 6) {
          ... d->snr.push_back(pt);
      } else {
          ++d->bad_snr_rows;
      }

- 死码实证：

      $ grep -rn 'bad_snr_rows' --include='*.c' --include='*.cpp' --include='*.h' . | grep -v '^./run/'
      ./lib/astro_image_io/src/hips/aio_hips_reader.cpp:98:    size_t bad_snr_rows = 0;          // 损坏 TSV 行计数 (G6 robustness)
      ./lib/astro_image_io/src/hips/aio_hips_reader.cpp:339:                        ++d->bad_snr_rows;

  无 getter、无 set_err、无门禁引用；:344 return d.release(); 仍按成功返回 ⇒ 交付星表少行而完全不可观测。
- 另一半缺口：== 6 只挡字段个数，%lf 仍接受 nan / inf / 1e999 ⇒ NaN/Inf 的 ra、dec、snr 直接入表（下游匹配半径与显著性选择受影响）。
- 同文件正对照：:55-57 valid_product_params（tile_width == 512 且 hips_order 在 [0,29]）+ :300-307 硬失败，注释逐字写明历史危害（恶意 hips_tile_width=2048 致 4x 堆越界写；恶意 hips_order=32+ 触发 1ULL 左移 2*order 的移位 UB）⇒ 同一 TU 对 properties 用 atoi 后立刻硬校验，对 TSV 行却是吞加计数。
- 修复：坏行数大于 0 即经既有错误面暴露，或让 SNR 计数与实际行数不一致时硬失败（与 module_entry.cpp manifest 平面 ln != want 同口径）；行值补 isfinite。

### N-06【崩溃/资源面】readFits 无 NAXIS 上限，声明尺寸直乘成分配量

- 站点：fits_reader.cpp:315-321 只有 if (naxis1 <= 0 || naxis2 <= 0) 拒；:333-342 对 naxis3 只归一小于 1 的情况并把非 1/3 按 1（无上限）；:419-422 直接相乘成 data_size 并分配 vector
- 同仓两处正对照都设 65535：aio_fits.cpp:176-187（P0-4，注释「与 aio_xisf read_file 既有校验口径一致」）、aio_xisf.cpp:32-33 定义 XISF_MAX_DIM=65535（消费处 :514-521 硬拒）⇒ 同族字段（NAXISn）的第三个读面没跟。
- 后果：NAXIS1=NAXIS2=100000 且 BITPIX=-64 时 data_size 约 8e10 ⇒ 分配抛 bad_alloc 或直接占坑（hp_drizzle_api.cpp 入口未见 try 包裹；phase2/sampler.cpp:168-173 的 catch(...) 只护 resize）⇒ 判「崩溃/资源」，是否必崩属运行期（第五节第 1 项）。
- ⑤ 测试：65535 类锁只在 aio_fits/xisf 面，drizzle 侧无。

### N-07【仅诊断/资源】模块 JSON 自定义解析器：数组元素数无上限、double 转 uint64 无 isfinite

- 站点（三处同族）：lib/hips/src/module_entry.cpp:174-226、lib/drizzle/src/module_entry.cpp:151-206、lib/gaia_xpsd_client/src/module_entry.c:152-201
    - json_get_u64_array / json_get_f64_array：第一遍扫只数 count，随后 malloc((size_t)(count > 0 ? count : 1) * sizeof(...)) ⇒ count 无上限（hips :218、drizzle :206、gaia :190 附近）；调用方随后才比对 an != n_tiles（hips :462）⇒ 巨量元素先分配再拒。
    - json_get_f64（三处同文）有首字符挡（*p 不是 '-' 也不是数字则返回 0，挡掉 nan/inf 词法）加 end == p 挡，但 "1e999" 经 strtod 得 HUGE_VAL 且 end 不等于 p ⇒ inf 通过；json_get_u64 就是把该 double 转 uint64 且只挡 d 小于 0 ⇒ inf 或超 2^64 转整为 UB、小数静默截断。
- 现产影响判（不夸大）：hips 边所有参与内存的 u64 都有后续硬校验（:342 nside 须 2 的幂且不大于 2^24、:349 tw 必须 512、:436 n_tiles 大于 65535 拒、:443/:450 等值比对、:523 snr 计数大于 1e8 拒、:482-492 平面长度不等即拒）⇒ 值错面已被夹；未夹的只有 moc_order（:385，被 aio_hips_writer.cpp:501 的 std::min(moc_order, tile_order) 夹住）与 max_workers（:387，:307 注释自述「0 = 无 config 上限（事务恒串行, 仅作回显）」）两个透传字段 ⇒ 本条归「仅诊断 + 巨量分配」，不判越界。
- 同族正对照：lib/drizzle/src/module_entry.cpp:354-475 逐字段 isfinite 加值域（pixfrac [0,1]、precision_mode 取 -1|0|1、nside u32 且 2 的幂、reverse width/height 为 i32 且大于等于 1、pixel_scale_arcsec 大于 0、ra0/dec0 有限）⇒ 三兄弟里唯一每字段都夹；gaia_xpsd_client/src/module_entry.c:223-257 gaia_cfg_check_params 对 ra/dec/radius/mag/match_radius 逐个 isfinite 并给专用码 ACS_GAIA_ECODE_PARAM_NOT_FINITE，但数组版 ra_list/dec_list（:319-328）只判 nr<0 或 nd<0 或 nr!=nd，无逐元素 isfinite、无元素数上限 ⇒ 标量做对、数组没做（同文件相邻代码）。
- 修复：数组解析「先判上限后 malloc」（与 snr_count 同口径）；json_get_u64 补 isfinite、上界、非整判；ra_list/dec_list 逐元素 isfinite（复用既有错误码）。

### N-08【仅诊断，现产不可达】hips_order 的第四站点无界，1u 左移 leaf_order_ 属移位 UB；orchestrator 同字段整段静默跳过

- lib/healpix_db/healpix_browser_qt/core/hips_browser_backend.cpp:157/:168 order_ = std::atoi(...)（properties 与 hips_order= 子串两路）→ :162/:169 leaf_order_ = order_ + kTileShift → :200/:216 uint32_t nside = uint32_t(1) << (uint32_t)leaf_order_; ⇒ order 大于等于 23 即移位 UB/回绕，正是 aio_hips_reader.cpp:50-54 注释里列为「已修危害」的那一类。
- lib/orchestrator/cpp/src/orchestrator.cpp:4452-4457：atoi(A_ORDER) 后越界时整段静默跳过 SIP 加载（只在 a_order 大于 0 且不大于 SNR_SIP_MAX_ORDER 时加载；b_order 则超上限夹住、负数归 0），与 hp_drizzle_api.cpp:461 的 return -10 同字段不同命；:3982/:4949 用 std::atoi(s.c_str()+p+11) 取 hips_order → :3985 处 1u 左移 (hips_order+9) 未夹；:1525 又有 int idx = i*6+j; if (idx < 36) 守卫 ⇒ 同文件第三种写法。
- 可达性判（两者一律降档）：lib/healpix_db/healpix_browser_qt/CMakeLists.txt 与 lib/orchestrator/cpp 均不在构建图 —— 根图 add_subdirectory 只有 CMakeLists.txt:172/177/184/191/199/208/694/701（无 lib/healpix_db、无 lib/orchestrator），grep -rn 'healpix_browser' --include=CMakeLists.txt 仅命中其自身与 run/ 影子树；orchestrator.cpp 无任何 CMakeLists 命中 ⇒ 判「补注册即红」（related M9-F-1），不判现产可达。
- 正对照（同字段两处已夹，供修复抄）：aio_hips_reader.cpp:55-57、hips/src/module_entry.cpp:342/:436。

### N-09【仅诊断】CLI 与环境变量面的裸解析（同文件已有带 endptr 的写法未推广）

- cli/commands.cpp:204/:1081/:1286/:1672 std::strtol(sleep_ms, nullptr, 10)（无 endptr、无界、可负；:204 上下文是测试钩子 ASTROCS_TEST_SLEEP_MS），而 :292 同文件已用 strtoul(tmo, &end, 10) 带尾字符判 ⇒ 同 TU 两种口径。
- 环境变量三处同族：lib/astro_image_io/src/aio_log.cpp:22、lib/dynamic_psf/src/dpsf_log.cpp:22、lib/star_detector/src/sdet_log.cpp:22 的 atoi(getenv(...)) ⇒ 非数字或负数静默得 0（日志静默），不参与寻址、不触科学量。
- cli/monitor.h:150-167、cli/memory_report.h:306/:540 对 /proc 与自采样的 sscanf 有 ==1 或 ==N 判 ⇒ 不立案（仅诊断）。
- lib/phase2/src/sampler.cpp:127-132：atof 之后有 if (vf > 0.0 && vf <= 1.0) 与 if (vs > 0.0) 值域挡，越界时静默保留 0 ⇒ :551 的 if (pf > 0.0) 走不进 ⇒ frames[i].kcorr 留 kControlCorrDefault（:143），:839-840 用默认 k_corr（k_corr 直接乘进 cvar，是控制域方差输入）⇒ 属「值域有挡、失败无诊断」档，不判越界。

## 第 2 节 同仓正对照表（做对了的站点，按可信度排序）

| 序号 | 站点 | 做对什么 | 被引用于 |
|---|---|---|---|
| 1 | gaia_client.c:1150-1160 parse_bounded_int（唯一调用 :1228） | 首字符数字、*end 只能是 NUL 或逗号、值域与溢出四重校验，失败整文件拒（:1229-1232 close_xpsd_file 后 return -1） | N-03 修复口径 |
| 2 | hp_drizzle_api.cpp:461-465（KV 边）与 :99-101（C-ABI 边） | SIP order 在 [0,5] 外硬失败（-10 与 6） | N-02 |
| 3 | fits_reader.cpp:163 classifySipKey | 系数下标 i、j 均在 [0,5]（注释 6x6 数组边界） | N-02（写侧有界、读侧无界的证据） |
| 4 | aio_xisf.cpp:32-33/500-521、aio_fits.cpp:168-187 | 截断硬拒（W1-AIO-001）加 NAXIS 的 <=0 与 65535 双挡（P0-4） | N-01、N-04、N-06 |
| 5 | aio_hips_reader.cpp:55-57/300-307 | tile_width 必须 512、hips_order 在 [0,29]，硬失败并留历史危害注释 | N-05、N-08 |
| 6 | drizzle/src/module_entry.cpp:354-475 | 每字段 isfinite 加值域 | N-07 |
| 7 | gaia_xpsd_client/src/module_entry.c:223-257 | 逐标量 isfinite 加专用错误码 PARAM_NOT_FINITE | N-07（标量对、数组漏） |
| 8 | hips/src/module_entry.cpp:436/443/450/462/482-492/523 | manifest 全平面「等长校验 + 元素数上限 + 计数一致」三重 | N-01、N-05 应达到的口径 |
| 9 | runtime/io/fits_core.c:429-445 与 :381-383 与 :855 | strtol 后紧跟消费侧校验（每字节数非法、naxis 越界一律 UNSUPPORTED）、INT64_MAX/n 溢出防护、fio_scan_naninf | 全轴严格面参照（IO-001 未接线属既有在册） |
| 10 | gaia_client.c:1261 while (xf->tree_count < 16) | 自报重复结构有硬上界 | N-03、N-07 写法参照 |
| 11 | lib/core/src/artifact.cpp:140-156 | stoull 配 try/catch 后 return false（fail-closed） | 反例对照：吞错与吞错的差别就在返回码 |

## 第 3 节 反向钉死检查（把旧错误口径当权威）

1. 命中一条：FIX_LEDGER.csv 的 M9-H-2 verified_by「仍 atoi 但仅元数据透传不参与寻址」只对 Start/Step/Bits 成立，item_size 在同文件参与 byte_unshuffle 下标（见 N-03）。同表另有 impact 自述「后续重构（把 atoi 换成别的解析、改默认值）无护栏」⇒ 本轴后续改动确需行为锁。
2. 未发现「把 findings/ 旧判词当依据改回代码」的站点：grep -rn 'R9-A\|P0-4\|W1-AIO-001' --include='*.c' --include='*.cpp' lib runtime cli 的命中全部是现行修复注释自述口径（方向正确，不立案）。
3. 「取默认」在本仓是既有允许口径（sampler.cpp:551-554 注释「scale 未知：300 档保守」、module_entry.cpp:380 if (!f) c->exposure_s = 0.0;）⇒ N-09 只判「失败无诊断」，不判口径错，也不建议改成硬失败（避免与冻结口径冲突）。

## 第 4 节 测试面

- 有锁：tests/unit/gaia_unshuffle_test.c（byte_unshuffle 边界）、tests/unit/gaia_module_manifest_bounds_test.c（magnitudeRange）、lib/phase2/tests/unit/kcorr_matrix_test.cpp（k_corr 查表网格）
- 半锁（只覆盖已修那条边）：p0_sip_order_guard_test.cpp T1-T7、mini_sip1000.cpp（A_ORDER=1000 期望 rc==-10）、tests/backend/test_p0_drizzle_sip_wiring.py:209（kv_set header A_ORDER 2）⇒ 全走 frame-KV，readFits 文件面零覆盖（N-02）
- 无锁：截断 FITS 消费（N-01）、itemSize 非法到整文件拒绝（N-03）、畸形 BSCALE/BZERO 与 D 记法（N-04）、畸形 TSV 行与 NaN 值（N-05）、readFits 侧 NAXIS 巨值（N-06）、ra_list/dec_list 逐元素有限性（N-07）
- 可造样本的既有工具：tests/unit/gaia_xpsd_fixture_gen.c:233/282/305（列 shuffle 生成器，可产异常 item_size 样本）

## 第 5 节 需运行期，未判

1. N-01 与 N-06 的实际崩溃取决于分配行为与 bad_alloc 捕获面（sampler.cpp:168-173 有 catch(...)，drizzle 入口未全覆盖）⇒ 静态只判「界外索引可达」，不判「必崩」。
2. hp_drizzle_fits_to_ahpx 是否出现在 Fatduck 真机数据流经（仓内生产代码零调用者，仅 tests/orchestrator/test_dll_loader.c 按符号名探测）⇒ 判「导出 API 面」，不判「CLI 主路径」。
3. mmap_size 与各自报偏移的真实大小关系：grep 确认 mmap_size 仅出现在 gaia_client.c:1171/:1181/:1182/:1186/:1344，均不参与查询期校验 ⇒ 属 M9-H-3 判定域，本轴不重复下判。
4. /proc 读在容器或新内核上是否可能返回非数字行（monitor.h、memory_report.h）⇒ 未判，且不触科学量。

## 第 6 节 related 映射（同义主题不另立条）

| 本轴结论 | 已挂账条 | 关系 |
|---|---|---|
| N-02 | P0-1（SIP 接线修复）与其回归锁 | 同字段不同边界：已修方只修 frame-KV 与 C-ABI 边 |
| N-03 | M9-H-2（残余登记） | 三值残余属实；itemSize 不在三值内且参与寻址 ⇒ 需补注，非撤账 |
| N-03 同块偏移面 | M9-H-3（OPEN） | data_position（:1208 strtoull 无 endptr）→ :1102 comp = mmap_data + data_position + block_offset → :1105 memcpy(scratch, comp, block_size)，无 mmap_size 约束 ⇒ 不重报 |
| 同块 atof magnitudeRange（:1201-1202） | V5-N-01 | 已挂账 |
| 同块 spectrumStart/Step/Bits（:1223/:1224/:1233） | M9-H-2 自登记残余 | 已挂账（我核确不参与寻址） |
| N-08 两站点 | M9-F-1（build-orphan 族） | 不可达性同源 |
| N-08 不可达判据写法 | E4-实测（hips_core.c 不属任何构建目标） | 同一判据 |
| N-05 后果族 | M9-G-2（吞错族）、账本 impact「未测光 ADU 数据可能被静默当作已标定产物进入下游」 | 同类后果、不同站点 |
| N-01 短读族 | W1-AIO-001（已修，AIO 面） | 同族不同读面 |
