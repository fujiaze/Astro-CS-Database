### M6a-I-002 部分修复残余：合同字段 result.coverage_ok 仍被写路径无条件置 1（真值只在 verify 路径计算），头注释/DATA §27.2 称其为「coverage 头/数据一致」的验证结论，ALG 把常量当规格写死，三处自测把常量当断言（来源 L14-008）

- 类别码: I_DOC_HYGIENE
- 优先级: P1（原报 P1；复算后：门禁后果已收窄——verify 节点上报的是真值，写路径的常量当前不外泄，故按「合同字段语义割裂 + 常量断言」定 P1）
- 位置:
  - `lib/phase3_session/p3_output.cpp::p3_output_write_atomic_ex 结果段`（复核时点 :360-379：:374 `result->coverage_ok = 1;`，随后 :376-379 调 `p3_output_verify_ex` 但只回传 `reopen_ok`，丢弃 `v.reopen_ok` 之外的真值）
  - `lib/phase3_session/p3_output.cpp::p3_output_verify_ex`（复核时点 :481-493 真算 covok、:537-538 `result->reopen_ok = (ok&&covok&&uncok&&wcsok); result->coverage_ok = covok;`）
  - `lib/phase3_session/p3_output.h::P3OutputResult`（:33 `int coverage_ok;  // 1=coverage 头/数据一致`）
  - `docs/contracts/DATA_SEMANTICS.md::§27.2`（复核时点 :1978「result.coverage_ok / reopen_ok | int 0/1 | coverage 头/数据一致；独立 reader 重开回环一致」）
  - `docs/algorithms/PHASE3_FITS_IMPL.md::§4 伪代码`（:142 写路径 `coverage_ok=1   # :284-289` vs :154 verify 路径 `coverage_ok=covok`）
  - 消费面（说明后果收窄）：`lib/phase3_session/p3_session.cpp::run 结果 JSON`（:400-411 只发 `coverage_stats`，不发 coverage_ok）、`lib/core/src/module_adapters.cpp::p3_op_writer`（:5120-5124 同上）、`::p3_op_verify`（:5198-5212 发 `vres.coverage_ok`＝真值）
  - 常量断言面：`tests/unit/p3_output_test.cpp`（:75 `CHECK(res.coverage_ok == 1);` 打在写路径）、`tests/unit/p3_assembly_test.cpp:121`、`tests/backend/test_p3_output.py:160`、`tests/backend/p3_output_probe_main.cpp:78`
- 证据摘录:
  > （p3_output.cpp:374-379）result->coverage_ok = 1; / // 独立重开读回验证 … / P3OutputStatus vst = p3_output_verify_ex(output_path, wcs, signal, coverage, variance, ivar, width, height, &v); / result->reopen_ok = (vst == P3_OUT_OK) ? v.reopen_ok : 0;
  > （p3_output.h:33）int coverage_ok;                // 1=coverage 头/数据一致
  > （PHASE3_FITS_IMPL.md:142）result.total_px = W*H; result.covered_px = #(coverage>0.5f); coverage_ok=1   # :284-289
  > （DATA_SEMANTICS.md:1978）| result.coverage_ok / reopen_ok | int 0/1 | coverage 头/数据一致；独立 reader 重开回环一致 |
- 权威依据: DATA-P3 §27.2（字段语义即合同）；宪章 §11（半成品禁发布、验证结论须有证据）、§16.2、§12.3-5
- 问题说明: 同一字段两条通道两套语义（写路径常量 / verify 路径真值），合同与头注释只描述真值语义，ALG 却把常量写进规格伪代码；结果是三个自测对写路径 `coverage_ok==1` 的断言恒真（无判别力，等价于无断言）。残余风险：任何新消费者按头注释取 write 结果的 coverage_ok 即得到无条件 PASS；`lib/phase3_fits/memory.md:57/:121` 仍把它当验收项列记。
- 建议处置: ①写路径改用 `v.coverage_ok` 回写（一次真值），或删除该字段并在 DATA/头注明「仅 verify 通道提供」；②ALG §4 与 §4 verify 段统一为 `coverage_ok=covok`；③三条自测改断言真值语义（构造一个 coverage 不一致的负例，令 covok=0），否则登记为无效断言。
- 置信度: 高
- 来源: L14-008（部分修复；修复面与残余的边界见 _merge §8）
- related: M6b-G-001（无据 VERIFIED 的根因定稿点，本条只登 coverage_ok 字段语义实例）、M6a-I-001、M4（L08/L09 域 verify 面）、M6a-G-001
