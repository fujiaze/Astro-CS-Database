### M5b-I-07 活动文档中的版本命名空间自述与占位说明缺失（governance/VERSION_NAMESPACES 及若干合同页）
- 类别: I_DOC_HYGIENE · 建议优先级: P2
- 位置: `docs/governance/VERSION_NAMESPACES.md::（history 豁免说明与 known_limits）`（现 :53-57）；`docs/contracts/RT-001.md::DOC-004`（现 :55）；`tools/quality/gen_module_readmes.py::（同 ID）`（现 :3）；`docs/standards/API_STANDARD.md::（检查器字段清单与实际 required 的差异）`（现 :13-14）；`tools/quality/contracts/check_api_contracts.py::required`（现 :49）
- 证据摘录:
  > | DOC-004 | 模块 README | 26 registry 页存在且非空 |
  > （`tools/quality/gen_module_readmes.py`:3 亦声明 `DOC-004`）
  > 至少含 symbol/full_signature/header/linkage/units/valid_range/nullable/ownership/thread_safety/test_ids/status
- 权威依据: 宪章 §12.3-9/-10、§15.4、§1.1（术语与 ID 唯一性）
- 问题说明: `DOC-004` 被至少三个不同对象占用（AST-API、gen_module_readmes、RT-001 表的"26 registry 页"）；`API_STANDARD.md:13-14` 声明的检查器字段清单与 `check_api_contracts.py:49` 实际 `required` 4 项不同（余 7 项靠注释推给 AST-API），且该标准把追溯链交给 `docs/TRACEABILITY.csv`（SPEC 权威已是矩阵）；`VERSION_NAMESPACES.md` 的 known_limits 只登记 legacy 面，未登记 `packaging/**` 与 `ci/checks.json --expected` 这两类实际漂移点（见 M5b-G-04）。
- 影响: 文档 ID 与字段口径混用导致跨文档引用不可定位；门声明与门实现差距被"至少含"式措辞掩盖。
- 建议处置: 统一 `DOC-00x` 编号表并在 DOCUMENT_INDEX 登记；`API_STANDARD` 的检查器字段清单与代码 required 一致或标 MISSING；known_limits 增补真实未覆盖面。
- 置信度: 高 · related: M5b-G-03、M5b-C-01、M5b-G-04
