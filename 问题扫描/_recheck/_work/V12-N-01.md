### V12-N-01（P1·**总述根因条**）科学常数**无承载层**：`include/` 下无任何 constants 头，权威点数 = 0
- 宿主实测：15 位 `1.482602218505602` 在 tracked 生产面 **15 文件 24 处**——`noise_model.h:15 kMadToSigma` 是**全仓唯一具名点**，而其消费者只有 `noise_model.cpp:189` **一处**（`snr noise_model.cpp:61`、`sdet_api.cpp:227`、`sdet_image.cpp:495(f)/524` 等**全是裸字面量**）；4 位 `1.4826` 达 **66 文件 99 处**（含 `photometer.cpp:90`、`phase1/stars/star_detector.cpp:51`、`providers/cpu/baseline/src/baseline_provider.cpp:320/335`、`backend_host/baseline_kernels_impl.inc:129/137`、`frame_qc_grid.py:35/224/500/592`）；倒数式 `0.6745` 生产 17 处 / 文档 27 处。
- **权威点数 = 0**：三级复核 `git ls-files | grep -iE "constant|sci_const|numeric"` ⇒ 生产/合同/文档层 **0 命中**（`include/` 下没有 constants 头）。⇒ 这不是"某处写错"，是**架构上无处可写对**。
- **文档自述不实**：`NOISE_MODEL.md:91` 称与 `1.4826022185` 差 `<1e-12`，V12 复算绝对差 **5.602e-12** ⇒ 不成立。可复算数值差：`1.4826` rel **−1.496359e-06**；FP32 `1.4826f`=`1.4825999736785889` rel −1.514e-06。出处/DISP：**无**。钉死：`p1star_mad_check.cpp:43/44/84` **只钉字符串与"两常数不相等"，不钉生产值**。
- **处置（本条一次性收敛 `A-12`/`M3-A-008`）**：新建 `include/astrocs/science/constants.h`（`inline constexpr double`，每个常数带 `static_assert` 精度界 + 出处注释 + `DISP-*`），把上述 24+99+17 处**全部替换为具名常数**；`kMadToSigma` 现成，缺的是消费者。**这条做完，`A-12` 与 `M3-A-008` 可一并关闭**（我已把两者挂 related）。
- related：**A-12**、`M3-A-008`、`M7-G-103`、`V6-N-08c`、**V12-N-02/03/04**
