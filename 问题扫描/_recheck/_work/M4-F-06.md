### M4-F-06 backend「每节点执行证据」测试断言依赖 p2_session 日志串——当前生产 IR 链不发这些日志；5/7 节点无逐节点证据
- 类别: F_TEST_GAP
- 定稿优先级: P1（来源 L09-008 原议 P1，成立；CI 实际红绿需执行 → §6 待复核注）
- 位置: tests/backend/test_p2006_canonical_pipeline.py::test_01（复核时点 :84-96，断言句 :91-92；自述降级句 :88-90/:113）；tests/backend/test_p2007_joint_gate.py:73,:227-228；日志串唯一出处 lib/phase2_session/p2_session.cpp:116,:148,:177；生产路由 cli/commands.cpp::cmd_phase2_run（:1026-1027）与 cli/runtime_client.cpp:173-205；执行面 ci/checks.json::UT-BACKEND（:1345-1377，waivable:false，linux-main）
- 证据摘录:
  >         for frag in ("stage coverage ok", "stage sample ok"):
  >             self.assertIn(frag, self.res.stderr, ...)
  >             w = _session_budget_workers(res.stderr)；self.assertIsNotNone(w, "session 日志缺 budget workers 注入证据")
  > stderr 不含 “session run: budget workers”（p2_session 未产出预算证据）
  （p2_session.cpp:116/:148/:177；grep「stage coverage」于 lib/core 与 lib/phase2/src 零命中——节点链无此日志）
- 权威依据: 宪章 §12.3-7（Pipeline 静态图与实际 trace 一致）、§13.2-4；§6.2（七节点=不同算法符号的验收锚）
- 问题说明: M4 独立复证：astrocs phase2 run → cmd_phase2_run → run_pipeline（Runtime IR 子图，7 节点 bind 于 module_adapters 的 p2_op_*），全程不调 p2_session_run（:1027 的 ev.stage("phase2_session") 仅是事件标签）；被测二进制 build/astrocs 必含节点链。故 p2006:91-92/p2007:73,:227 所断言的三条 session 日志串在现路由下必不出现——静态证据指向「必红」；但 UT-BACKEND 为非豁免门且 ci/known_failures.json 无 p2006/p2007 挂账、ci/ci_repair_round.py:110 历史登记其失败归因于 resource gate——红/绿终态须执行定夺（§6 待复核，M5/L12 同域）。若其当前为绿，则必存在另一条使日志成立的路由（同样违 RT-008 注释）——两情形均构成追溯断裂。叠加测试自述：「Registry 注册 + final ok 承载」五节点（upm_fit/upm_apply/reject/integrate/write）无逐节点执行证据、observed_trace 无落盘载体。
- 影响: P2-006/007 VERIFIED 的证据链与真实执行链脱钩；§6.2 节点独立性验收失去测试锚；节点级取消/预算/资源联动验证虚化。
- 建议处置: 以节点 manifest（p2_op_* 已在 man 写 operation/entry 标记）或 observed_trace 落盘为断言源重写；补 IR trace 文件载体；同步 p2007。
- 置信度: 中高（静态完整；CI 终态待执行核实）
- 来源: L09-008
- related: M4-C-07、M4-G-02；M5（UT-BACKEND/CI 挂账域）、L12
