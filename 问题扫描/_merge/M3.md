# M3 合并验证报告（域：单帧校准 + 测光标定 + 噪声模型 / SNR / 控制权重）

- 代理：M3（第二层合并验证）｜输入：`_cache/L05.md`（19 条：P0:3/P1:10/P2:6）+ `_cache/L07.md`（25 条：P0:6/P1:14/P2:5），共 **44 条**
- 纪律：全程只用 `read`/`grep`/`glob` 取证；**未执行任何 shell / 构建 / 测试 / 脚本 / git 命令**；只写 `问题扫描/findings/**` 与本文件；未触碰他人 `_cache` 或他人产出；未修改任何目录 README.md
- 定稿产出：16 个「类别×优先级」合并件，文件名一律 `findings/<类别>/p<N>/M3_L05_L07.md`；共 **43 条**定稿条目（44 条原文经根因合并对应 42 条 + 本轮新增 1 条）
- 分布：**P0 7 / P1 26 / P2 10**（A_SCI_DEF 9、C_DOC_CODE_GAP 14、D_COMMENT 6、E_TRACE_BREAK 2、F_TEST_GAP 5、G_GOV_GATE 1、H_NUMERIC 2、I_DOC_HYGIENE 4）
- 复核时点结论：本域真源仍被并发修改（协调记录记为 dirty 17→8），故全部条目一律标注「复核时点成立」，位置全部按符号/关键语句定位

## 0. 前台两条前置裁定的执行结果

1. **L05-001（flat_norm 公式不等价）→ 已按裁定降级并重写**：重新逐字核对后确认，生产 `generate_master_flat` 步骤 3（`master_generator.cpp:::233-252`）已把母版整体除以帧中位数并夹 0.1 地板，故 `calibrator.cpp:122/:132` 的 `max(flat,0.1)` 在「母版已归一」前提下与 SCI 式数值近似等价——叶子原判「相差数量级」不成立，已剔除。定稿为 **M3-A-003（P1）**，改写为三个仍成立事实：SCI §2 点名的归一实现 `normalize_flat` 全仓零调用方（README:75 自证 DISP-CAL-007）；`median≈1` 前置条件只写在 ALG（L3）「约定」里、未进冻结 SCI；两条通道均不校验该前提（IR 的 `p1_master_flat_valid` 只拒退化，median=30000 的原始 ADU 平场合法通过）。升 P0 的条件已写入条目：负责人认定「按 SCI 字面直调交付 DLL 面即属可达生产路径」。
2. **L07-001（≥3/≥2 门缺失）→ 已按裁定拆成三个可分别证明的事实**，定稿 **M3-C-001（P0）**：A 事实门缺失（`r_consistent.empty()` 为唯一判据 → 单星 MAD=0 → `location`=该星 r → `scale=10^(−location)` 整帧乘算，真实可达）；B 阈值不符（`|r_inliers|>=2` 实现为 `!empty()`）；C 合同证据锚指向不含任何阈值的代码区。

## 1. 逐条处置表（四态）

四态 = 仍成立 / 已被修复 / 部分修复 / 无法判定。本轮 44 条：**仍成立 42**（其中 2 条降级、3 条按当前树改写证据、3 条并入根条目）、**部分修复 2**（L05-004、L05-013）、**整体已被修复 0**、**无法判定 0**（4 项子事实列入 §6 待复核）。定稿条目 43 = 44 − 2（根因合并）+ 1（本轮新增）。

### L05（19 条）

| 来源 | 叶子级别 | 四态 | M3 定稿 | 处置要点 |
|---|---|---|---|---|
| L05-001 | P0 | 仍成立（**改判 P1**，公式差异剔除） | M3-A-003 (P1) | 见 §0-1：三事实保留，升 P0 条件写明 |
| L05-002 | P0 | 仍成立（**改判 P1**） | M3-C-004 (P1) | 四处冲突逐条复算（K 搜索 / Light 帧 5×5 检测 / bilinear+NaN 回填 / 3σ）；实现不跟随文档、README:147 已注历史 → 当前输出不变，按规程降 P1；残余事实是文件自身无归档标记 |
| L05-003 | P0 | 仍成立（**根因并入**） | M3-E-001 (P0) | 与 L07-015 并为「被引对象不存在/挂错」根条目；矩阵 :20 逐列复算（test_files 只有 apply_photometry 测试、符号列缺 `ac_correct_frame`、`TST-CAL-*` 违反 SPEC:78 的 `^TEST-` 文法） |
| L05-004 | P0 | **部分修复** | M3-C-005 (P1，残余事实) | B2-A6 已在 IR 通道落地并有 CLI 回归测试；`p1_session.cpp::calibrate` 复核时点仍只有尺寸校验（isfinite/median/all_zero grep = 0）→ 标题写实为「只覆盖 IR 通道」 |
| L05-005 | P1 | 仍成立 | M3-D-001 (P1) | 头注 :5/:14-16/:25 三处失实逐字复现（黄金分割搜索、`optimize_dark_scale`、固定 16 线程）；`lib/astro_calibration` 路径 glob 零命中 |
| L05-006 | P1 | 仍成立 | M3-F-002 (P1) | 1e-3/2e-3/3e-3 与 40 点抽样逐行复现；订正：该文件确由 `ci/checks.json::UT-BACKEND` 的 discover 执行（非「从不运行」） |
| L05-007 | P1 | 仍成立 | M3-C-006 (P1) | 头文件实测 12 个 `AC_API`（逐行清点），ALG:16/:207/:415 与 README:63 写 14 |
| L05-008 | P1 | 仍成立 | M3-I-001 (P1) | 六处过期现状断言逐条按当前树复算（根 :191/:199、`lib/cosmetic/src/module_entry.cpp`、`cli/CMakeLists.txt:175` 含 `dark_optimizer.cpp`、`tests/unit/CMakeLists.txt:744-755` 已 `add_test(cal_photometry_apply)`、master_generator :240-244 已防护负 median） |
| L05-009 | P1 | 仍成立（**改类**） | M3-C-007 (P1) | `SCI-P1-CAL-001`/`ALG-P1-CAL-001` 全 `docs/` 仅出现在该 registry 页；版本/状态三口径（1.0.0·ACTIVE / 0.11.0-alpha.2·CONTRACT_READY / `ac_version()` v1.0.0）；「引用未定义 ID」的断链部分并入 M3-E-001 |
| L05-010 | P1 | 仍成立 | M3-D-002 (P1) | 头 :112 总括 vs :115-116 细则 vs `ac_api.cpp:140`「输出降级 float」vs PUBLIC_API:123-124 登记口径，四措辞三口径 |
| L05-011 | P1 | 仍成立（**剔除一条子证据**） | M3-G-001 (P1) | 两通道 `ac_correct_frame(..., nullptr, nullptr, ...)`（:401 / :1401）与 `availability cosmetic=available`（:445-447）复现；剔除 `if (rc==AC_OK && (n_hot>0||n_cold>0))` 形态（已不存在）；新增论据：config 层无任何母版键 → 检测在配置层不可达 |
| L05-012 | P1 | 仍成立 | M3-C-008 (P1) | `astrocs.phase1.*`（:467/:624）vs `astrocs.p1.*`（module.yaml/types.h/integration.json）双词表；与 F00-04 并档 |
| L05-013 | P1 | **部分修复 + 证据重写** | M3-C-009 (P1) | 叶子四条引文（`dark_scale_mode`、`"optimized"`、`CONFIG_SCHEMA.md:264-265`、`fallback:dark_floor`）复核时点均不存在（B2-A13 改造 + 该文件仅 85 行）→ 全部重锚；仍成立：A 线 `dark_opt=0`/`dark_k=1.0` 常量（:957-958）、mode 仅决定必填母版集、曝光四键仅日志/告警、PROCESS ±10s 无实现 |
| L05-014 | P2 | 仍成立 | M3-C-014 (P2) | `generate_master_flat` 步骤1 循环无 `#pragma`（同函数 :246 有）；`correct_frame` 合并/计数 :252-258 串行，ALG-COS:138/:141-143 称 `reduction(+:hot,cold)` |
| L05-015 | P2 | 仍成立 | M3-D-004 (P2) | `§flat_norm` 锚不存在（SCI 只有 §1-§15，`flat_norm` 是 §5 公式内符号）；任务号流水复现 |
| L05-016 | P2 | 仍成立 | M3-D-005 (P2) | label-0「size 设为 max_size」机制不存在（实际 `labels[i]>0` 跳过）；:159「8邻居双线性」实为 4 正交 `1/dist` IDW；ALG 与 `lib/cosmetic/README.md:74-82` 已正确登记 → 本条收窄到源码注释层 |
| L05-017 | P2 | 仍成立（**并入根条目 + 2 子项剔除**） | M3-E-002 (P1) | 复现 SCI:53/75 与 ALG-COS/ALG-CAL 锚漂移、`:321-333` 实为 `astrocs_aio` 且错锚被复制到模块 CMake 两处；**剔除** SCI:74 的 `calibrator.cpp:86`（精确命中）与 `master_generator:222-234`（已并发订正为 :243-255） |
| L05-018 | P2 | 仍成立 | M3-A-007 (P2) | 复算反例：>50% 像素被 0.1 地板抬高时 `median(once)=0.1`，二次归一 → ×10，破坏 §7 幂等不变量；I3 fixture 不含该形态 |
| L05-019 | P2 | 仍成立 | M3-A-009 (P2) | 「FP32 饱和语义」与 IEEE-754 溢出（→±∞）相反；:118/:122 无饱和处理，inf/NaN 直传，与 §4:37 口径自相冲突 |

### L07（25 条）

| 来源 | 叶子级别 | 四态 | M3 定稿 | 处置要点 |
|---|---|---|---|---|
| L07-001 | P0 | 仍成立（按裁定拆三事实） | M3-C-001 (P0) | 见 §0-2；单星整帧缩放链复核时点复算 |
| L07-002 | P0 | 仍成立（可达性已补证） | M3-C-002 (P0) | `quality_flags/SATURATED` 在 `lib/photometric_calib/cpp/src` grep = 0；六个 `pc_calibrate_*` 无 qf 入参；**新增可达链**：`dynamic_psf.h:117-120` 状态码无饱和态、`dynamic_psf/README.md:45`「[4]/[5] 不消费」、`p1psf_tests_core.cpp:1048` B3 断言满量程饱和星 `status==DPSF_FIT_OK` → 饱和星确以 status=0 进匹配 |
| L07-003 | P0 | 仍成立 | M3-A-001 (P0) | SCI 唯一授权 `signal/√variance`；被消费的是 `(A−B)/residual_scale`（:544/:663/:768 → `snr_psf` → drizzle :1162/:2037 → sampler → stage2 :1136 平方），公共头自述「已降级为诊断 / SNR-008 已退休路径」 |
| L07-004 | P0 | 仍成立（给出代码语义唯一结论） | M3-A-002 (P0) | 结论：生产默认 `weight_mode=2` = 逐像素 ivar（`stage2_common.cpp:378`；ivar 缺失时 rc=7 fail-closed），`support×snr²` 只在 `mode 0`（:1136/:1396 双站点）；SCI-CW:38/:42/:27 写成 mode 2，与自身 :65-66/:72/:78、SCI-UPM-WEIGHT-001:54、UPMW-001 测试全部互斥；机器门 `config_weight_mode_ivar` 不做子串外的语义核对 |
| L07-005 | P0 | 仍成立 | M3-C-003 (P0) | 5 vs 64（12.8 倍）；ALG:151-153「不改 SCI，以代码为准登记」= 权威层级倒置 |
| L07-006 | P0 | 仍成立（框架订正，级别不变） | M3-F-001 (P0) | 四事实：无 numpy 导入；复算助手 `median()/rsig()/KSIG` 为死代码；类级 `skipUnless(g++)` 可整类静默跳过；5% / `0.40×(1/16)` 容差且用非生产 cfg。**订正叶子框架**：`ci/checks.json::UT-BACKEND` 确实以 discover 执行该文件 → 问题不是「从不运行」，而是「§15 承诺的验收手段（NumPy rtol 1e-9）没有对应实现」= 无证据声明 |
| L07-007 | P1 | 仍成立 | M3-A-004 (P1) | :16 `pixel⁻²·ADU⁻²` vs :29/:76/:99 与 GLOSSARY:11 的 `ADU⁻²` |
| L07-008 | P1 | 仍成立 | M3-C-010 (P1) | ALG:9 声明 `zero_point`；两个结构体逐字段复算均无该成员，实现只有未导出的局部 `location` |
| L07-009 | P1 | 仍成立 | M3-H-001 (P1) | `fill_impl` 非空间分支 :410-416 原样写出、不 apply floor（空间分支 :402-403 有）；与 §7「任何输出 variance ≥ floor」不符 |
| L07-010 | P1 | 仍成立 | M3-A-005 (P1) | `|det|>1e-24` 无 SCI 出处、量纲 px⁸ 不闭合；无 else → 静默 `b=c=0`，`has_spatial_field` 不回退 |
| L07-011 | P1 | 仍成立 | M3-H-002 (P1) | :449 无条件缩放 variance、:451-453 仅 `α²>0` 且有限时缩放 ivar → 可产出 `var>0, ivar=0`，破坏 :76 互倒不变量 |
| L07-012 | P1 | 仍成立 | M3-F-003 (P1) | 测试注入 `σ=3+3u` 的 σ 梯度（真值场为二次面），断言为 `corr>=0.98` + `norm_rmse<=0.05`，从不校验 a,b,c（公共 API 亦不导出系数） |
| L07-013 | P1 | 仍成立（加强 + 1 项待复核） | M3-F-004 (P1) | 15 行全锚 `noise_model_science_test.cpp`，而该文件不被任何 CMake/CTest 目标编译（`tests/` grep = 0，仅 `tools/redteam_v19*` 手工 g++），且其 :18 自述不覆盖 SNR-011/012/015；**新增**：p1noise 的 `if(EXISTS)` 门卫化 +「从未入库」注释 与 `ci/ctest_baseline.json:15,123-130` 冻结八目标清单不可同真（追踪状态需 git，见 §6-1） |
| L07-014 | P1 | 仍成立 | M3-A-006 (P1) | `noise_model.h:33` 漏 `/gain²`；`p1_noise_test.cpp:44/:56/:63-65` 把 ADU 写成 `e⁻×gain`（方向倒置）且 ±15% vs SCI §11 的 5%；`photometer.cpp:69/:77-79` 用 1.4826 |
| L07-015 | P1 | 仍成立（**并入根条目**） | M3-E-001 (P0) | 与 L05-003 并档；六类被引对象 glob 零命中 |
| L07-016 | P1 | 仍成立（**并入根条目**） | M3-E-002 (P1) | ALG-PHOT:95 的 `:396-403`；ALG-NOISE:132-140 三组锚互相错位；「466 行 / 431 行」实测 467 / **438** |
| L07-017 | P1 | 仍成立 | M3-C-012 (P1) | 三份 `pc_calibrate_*`（:172/:544/:800）丢弃 `mag_max`；硬编码 `{12..16}` + `n_gaia>=2000` 无合同出处；ALG 锚 `:836-866` 只覆盖三副本之一 |
| L07-018 | P1 | 仍成立 | M3-D-003 (P1) | 头 :82 承诺 `star_ids`「空则填 0」而 `PsfFitQualityRow`（:68-79）**无 star_id 成员**、实现 `(void)star_ids;`（:325）；NaN status 落入「拒绝」码 1；线程安全承诺（:259-262）无实现支撑 |
| L07-019 | P1 | 仍成立 | M3-C-011 (P1) | `sigma_residual=0` 三义（完美 / IRLS 未启动 / 一致集为空）；`fit_status=2` 两处同码不同义；**新增**：`use_gain_model` 经 `types.h:98` + `module_entry.cpp:467` 真实从 config 填入而 `noise_model.cpp` 读取 0 次 |
| L07-020 | P1 | 仍成立 | M3-F-005 (P1) | TRACEABILITY 无 SCI-CW 行；`TEST-CW-*` 仅 TEST_MATRIX:41 且测试路径为 `lib/phase2/tests/...` 占位串；`INDEX.yaml:106-113` 的 ALG-CW-001 `path` 指回 SCI 文件自身；SCI-CW 缺 §9/§11/§14/§15（其余四份 SCI 均有） |
| L07-021 | P2 | 仍成立 | M3-D-006 (P2) | `:26` 收敛判据写成 `|Δscale|` 实为 location；`:173`/`pc_api:162` 的「MAD 清洗」与 ALG 订正相反；「OpenMP 16 线程」三处而本文件 `num_threads` = 0（该单一事实以 M5a-C-002 为定稿点）；任务号流水 13 处 |
| L07-022 | P2 | 仍成立（**改类**） | M3-A-008 (P2) | 复算 `1.482602218505602 − 1.4826022185 = 5.602e-12` > 合同自述的 `<1e-12`；「15 位」实为 16 位；三套字面（0.6745 / 长常数 / 1.4826）；`docs_machine_consistency.py:140-143` 为子串命中（长常数含短常数，恒真） |
| L07-023 | P2 | 仍成立 | M3-C-013 (P2) | 头 :102 承诺 `-3`，`pc_calibrate_simple` 全文无该返回；同条件尺寸非法在 :78 返 `-2`、在 :208 返 `-1` |
| L07-024 | P2 | 仍成立 | M3-I-003 (P2) | `algorithm.md:4/:6`「文档版本 1.0 / 状态: 已实现」、:14 梯度曲面目标、:107/:346 的 0.1nm vs `spectrum_integrator.cpp:247` `wl_step = 1.0`；ALG 已判其失实（DISP-PHOT-002） |
| L07-025 | P2 | 仍成立 | M3-I-004 (P2) | 两份同名四文件全部异体（实测 265/134/323/417 行 vs 252/462/176/411 行）；未注册副本 `p1phot_fixtures.hpp:12-13` 自称「共址于 lib/phase1/tests/p1phot」；注册方为 `lib/photometric_calib/tests/p1phot` |

### 本轮新增（非叶子来源）

| 新条目 | 级别 | 来源 | 说明 |
|---|---|---|---|
| M3-I-002 | P2 | 复核中从 L05-007 的附带事实独立化 | `PHASE1_API.md:8` 把 `ac_set_num_threads` 归入「不在本契约」的遗留 ABI，但该符号在公共头 :161 与 `.def`/`.map` 导出表在位，且无任何文档约束其私有线程预算风险（§10.4）；不与「符号计数错误」混件 |

## 2. 已被修复（并发提交期间）——不进 findings/

本域**没有一条叶子 finding 整体落入「已被修复」**（即复核时点已完全不可复现且无需定稿）。但下列**子判据**在复核时点已不成立（多数因并发提交带走），按规程登记于此，并标注是否存在回归保护：

| 原判据（叶子内） | 当下树事实（本轮重读） | 回归保护 |
|---|---|---|
| L05-004：IR 通道 cal 节点把全零 / median≤0 的 flat 静默降级为「无平场」，产出 ×10 帧且 manifest 报 ok | `module_adapters.cpp::p1_master_flat_valid`（:1153-1179）+ `p1_op_calibrate` 的 `degenerate_flat_artifact` / `error_kind=input` fail-closed（:1230-1240），B2-A6 引入 | **有**：`tests/cli/test_phase1_inprocess.py::test_10_invalid_master_flat_fail_closed`（:255-290）+ fixture `tests/backend/phase1_fixture_main.cpp --make-bad-flat`（:119）。残余（session 通道无门）→ M3-C-005 |
| L05-013：`dark_scale_mode=optimized` 暴露于 config 却从未实现（全仓无 K 推导） | 该枚举与字符串比较已从 `orchestrator.cpp` 移除；IR 通道改为 `dark_optimization` 布尔并由 `k_expo = t_light / dark_exptime` 推导 K + 跨检 `dark_scale_k`（:1280-1306），B2-A13 引入 | **有**：`tests/unit/p1001_real_nodes_test.cpp` 的 13a-13d 四例（:446-515）+ `lib/calibration/tests/calibration_adapter_test.cpp:169`。残余（A 线 mode/曝光键仍不生效）→ M3-C-009 |
| L05-001：`generate_master_flat` 未做 `/median(flat)`，据此推出「相差数量级」 | 步骤 3（:233-252）已做 `out[i]=v/final_med` + floor 0.1，并含 `final_med < 0 → AC_ERR_PARAM`（:240-244，B13-R13-7） | **有**：`tests/unit/master_flat_median_test.cpp::RejectsNegativeMedianMaster`（ctest `master_flat_median`，`tests/unit/CMakeLists.txt:444-448`）+ p1cal I1/I3。子判据剔除，条目降级为「合同前提缺失」→ M3-A-003 |
| L05-011 侧证：`if (rc==AC_OK && (n_hot>0||n_cold>0))` 才置 available | 复核时点两通道均无此形态（`:419` / `:1425` 一律 `status="ok"`，availability 恒 available） | 无（属形态消失而非修复）；子证据已剔除，M3-G-001 改建立在「config 层不可达 + availability 声明」上 |
| L05-017 侧证：SCI:74 锚 `calibrator.cpp:86`；SCI:53 锚 `master_generator.cpp:222-234` | `:86` 精确命中 `if (!(med > 0.0f)) return;`；master_generator 锚已被并发订正为 `:243-255`（与步骤 3 实际范围一致） | 无（文档侧）；两条子判据剔除，其余锚漂移仍成立 → M3-E-002 |
| L05-013 侧证：`docs/development/CONFIG_SCHEMA.md:264-265` 的 mode / exposure_ratio / fallback 条款 | 该文件复核时点仅 **85 行**，无 mode / exposure_ratio / fallback / optimized 任何一项 | 无；引文与行号整体作废，M3-C-009 已全部重锚 |

> **修复未固化（F_TEST_GAP）判定**：本域被并发修复触及的三处科学面（B2-A6 flat 退化门、B2-A13 K 推导门、B13-R13-7 负 median 拒绝）**均有可定位回归测试钉住**（见上表），故本轮不另立 F_TEST_GAP。唯一「单边修复」型残余——修复只覆盖 IR 节点通道、`p1_session` 通道无门且无对称测试——已在 M3-C-005 的「建议处置」中以「新增判定必须同时钉住两通道」的形式登记，避免重复计件。

## 3. 行号漂移处置

规程 R1 在本域持续生效：扫描期间真源仍被修改（`stage2.cpp`、`drizzle_engine.cpp`、`master_generator.cpp`、`orchestrator.cpp`、`module_adapters.cpp`、`ci/checks.json`、`test_photometry_apply.cpp` 等）。处置原则与结果：

1. **一律按符号 / 关键语句重锚**：43 条定稿的「位置」全部写成 `<path>::<符号或关键语句>`；行号仅作「复核时点参考值」，16 个合并件的文件头均已声明这一点。
2. **漂移不降级、不剔除**：本轮未因任何行号不符而剔除或降级一条 finding。
3. **实测漂移与分叉样本**（均已在条目正文按符号重锚）：
   - `drizzle_engine.cpp` 的 `snr_val = snr_model->points[i].snr_psf`：1159 → **1162**；另有第二站点（`snr_from_file` 分支）:2037，叶子只给一处。
   - `stage2.cpp` 权重分支：叶子记 1106-1140，复核时点**同一分支存在两份副本**（:1106-1140 与 :1359-1400），关键语句分别在 :1115/:1374（support 回退）与 :1136/:1396（`support×snr²`）→ M3-A-002 双站点登记，并提示整改必须两处同改。
   - `master_generator.cpp`：ALG-CAL:81 的 [194-225] vs 实际步骤1 为 193-225（偏 1 行）；SCI:53 的母版锚在并发中由 222-234 改为 243-255。
   - `p1_session.cpp`：`lib/calibration/README.md:91` 称 `ac_calibrate_frame` 调用点在 :243，复核时点在 :339-344（偏 96 行）；`ac_correct_frame` 在 :401-404；ALG-COS:150 称生产调用点「:294-301」，实际 cosmetic 段为 :390-420。
   - `cosmetic_corrector.cpp`：ALG-COS 锚表整体 +10/+8/+8 漂移（8 邻接 74-86 → 84-96；hot 95-101 → 103-109；cold 111-117 → 119-127）；ALG-COS:141-143 的 `:258-264` 实际对应串行段 :252-258；ALG-COS:70 在复核时点是无关键语句的行。
   - `noise_model.cpp`：ALG-NOISE:132-140 的三组锚不但漂移且**互相错位**（门面 :411-422 → 实际 `snr_noise_model_v1_fill` :422-429；LS :340-370 → 实际 `fit_plane_ls` :376-398）；:140 的 `snr_phot_cal_quality :67-79` 未标文件，两处候选位置皆不命中（实际 :276-290）。
   - 行数事实：ALG-NOISE:121 与 `lib/snr_estimator/README.md:7` 称 `noise_model.cpp` 466 行（实测 467）、`snr_estimator.h` 431 行（实测 **438**）。
   - `orchestrator.cpp`：`dark_opt` / `dark_k` 声明在 :957-958（叶子记 :951-957），调用点在 :1157-1176（叶子记 :1155-1168）。
   - `CMakeLists.txt` 目标锚三处互斥：ALG-CAL:20 写 `:321-333`（该区间实为 `astrocs_aio`）、`lib/calibration/CMakeLists.txt:12/:19` 写 `:333`、`docs/algorithms/COSMETIC_ALGORITHMS.md:6` 写 `:373-380`（此处正确）；本轮按目标名 `add_library(astrocs_calibration STATIC)`（复核时点 :373-377）重锚。
4. **锚点失效 ≠ 事实失效**：凡引文在复核时点已不存在者（L05-013 的 `optimized` 词表、L05-011 的 `n_hot>0` 判定式、L05-017 的两处锚），**只作废引文本身**，再把该事实按当前代码重述或明确剔除；全部剔除项见 §4.3，未静默丢弃。

## 4. 剔除与降级显式清单（不得静默丢弃）

### 4.1 降级（2 条）与「维持级别但改写框架」（1 条）

| 条目 | 变更 | 理由与保留部分 |
|---|---|---|
| L05-001 → M3-A-003 | P0 → **P1** | 生产母版生产者自带归一，未找到仓库内可达的「未归一 flat → 错误输出」路径（IR 通道只拒退化，median=30000 的原始平场仍合法通过但无仓库内生产者）。保留三项独立事实（SCI 点名的归一实现是死代码 / 前提未进冻结 SCI / 消费边界不校验），并在条目内写明升回 P0 的唯一条件 |
| L05-002 → M3-C-004 | P0 → **P1** | 该文档不驱动代码，实现与 ALG/SCI 一致，当前数值输出不变（叶子自身「影响」段亦承认未改变输出）。保留全部四处冲突与「文件无归档标记、README 单句不足以阻断误读」的残余事实 |
| L07-006 → M3-F-001 | **维持 P0**（框架改写） | 叶子的「从不运行 / 静默跳过即门禁失效」被本轮证据部分推翻（`ci/checks.json::UT-BACKEND` 在 linux-main profile 以 discover 确实执行该文件，`waivable:false`）。成立且足以支撑 P0 的是：FROZEN 合同 §15 承诺的验收手段（NumPy rtol 1e-9 复算）在仓库内**没有任何对应实现**（该文件不 import numpy，复算助手是死代码），属无证据声明；`skipUnless(g++)` 与 5%/0.40 容差为次要事实 |

### 4.2 改类 / 拆件（3 条）

| 条目 | 变更 | 理由 |
|---|---|---|
| L05-009 | E_TRACE_BREAK → **C_DOC_CODE_GAP**（M3-C-007） | 主体是 registry 页陈述与 `module.yaml` / 构建事实不符；「引用未定义合同 ID」的断链部分并入 M3-E-001，两个事实均未丢失 |
| L07-022 | 叶子在 A/H 间混用 → **A_SCI_DEF**（M3-A-008, P2） | 主危害是冻结合同内一条可复算陈述为假 + 常数无单一权威定义点，不是数值缺陷本身 |
| L05-007 附带事实 | 抽出为 **M3-I-002**（I_DOC_HYGIENE, P2） | `ac_set_num_threads` 的 ABI 范围声明问题与「符号计数 14/12」性质不同，不混件；两处互挂 related |

### 4.3 剔除的子判据（7 项，逐条给因）

1. **L05-001**「实现少一次 `/median(flat)`，两者相差 `median(flat)` 数量级」→ 母版生成步骤 3 已归一，前提成立时数值近似等价（并发事实 + 本轮复算）。
2. **L05-011**「`if (rc==AC_OK && (n_hot>0||n_cold>0))` 才置 available」→ 复核时点两通道均无此形态，改以「config 层不可达 + availability 恒 available」立论。
3. **L05-013** 五条引文（`dark_scale_mode`、`"optimized"`、`CONFIG_SCHEMA.md:264-265`、`fallback:"dark_floor"`、`dark_optimizer 有完整实现未接线`）→ 均不可复现；实现已由 B2-A13 改造，残余另立。
4. **L05-015**「注释与代码逐字符等价」的推定 → 锚点失效成立、等价性不成立，两事实拆入 M3-D-004 与 M3-A-003 分别表述。
5. **L05-017** 两处：SCI:74 的 `calibrator.cpp:86` 锚（精确命中）、SCI:53 的 `master_generator:222-234` 锚（已并发订正为 :243-255）。
6. **L07-006** 把「`_approx_list` 抽样缺陷」归到噪声 oracle → 抽样与 40 点比对在 `test_calibration_oracle.py`（已定稿 M3-F-002）；噪声侧的真实助手问题是「死代码 + 无 numpy」。
7. **L07-003** 的 `snr_phot`「被 Stage2/6 消费」强度 → 该字段无 HISS 写出点，降为次要事实，主链改为 `snr_psf`（可逐跳追到 stage2）。

### 4.4 未发布项

**0 条**。44 条全部完成复核：定稿、部分修复改写、或按 §4.1-§4.3 显式降级 / 改类 / 剔除；无一条因「行号对不上」「原文难找」或「工作量大」被静默丢弃。

## 5. 根因合并（本域实例归位）

| 根因 | 归并条目 | 本域实例 |
|---|---|---|
| ① 权威文本引用的测试 ID / 被引文档 / Oracle 不存在或挂错 | **M3-E-001（P0，根条目）** | L05-003 + L07-015（8 组锚点：`TST-CAL-*`、`TST-PHOT-*`、`TST-NOISE-*`、`02_FROZEN_STAGE1_HISS_SPEC`、`SNR_SCIENCE_DERIVATION.md`、`SNR_REDESIGN_CONTRACT.md`、`SCIENCE_ACCEPTANCE_MATRIX.md`、`flux_calibrator/**`、`contracts/schemas/test_ids.schema.json`）+ L07-013① 部分；与 F00-01 双头矩阵并档（主落 M6） |
| ② 冻结常量 / 参数被代码或 ALG 改写（权威层级倒置） | **M3-C-003（P0）** + M3-A-003、M3-A-008 | `min_samples` 5↔64 且 ALG 明写「不改 SCI，以代码为准」；flat 的 `median≈1` 前提沉到 ALG「约定」；MAD 常数三套字面 + 合同内算术断言为假 |
| ③ 公共头 / ALG 的总括句越权 | M3-D-002、M3-D-003、M3-C-006、M3-I-001 | 「全链路 double 不降级」；「star_ids 可空（空则填 0）」而结构体无该成员；「14 AC_API」；六处过期现状断言 |
| ④ 模块 ID / 版本 / 状态多词表且无映射登记 | M3-C-008 + M3-C-007（与 **F00-04** 同根） | `astrocs.phase1.*` vs `astrocs.p1.*`；1.0.0·ACTIVE vs 0.11.0-alpha.2·CONTRACT_READY vs `ac_version()` 的 `v1.0.0` |
| ⑤ 「现状」断言与行锚随并发提交整体过期 | M3-E-002（锚点 / 行数）+ M3-I-001（现状） | 12 组锚点 10 组不命中（其中 3 组指向无关符号）；466/431/438 行数；三处互斥 CMake 目标锚 |

**未合并的相邻项**（保持独立，避免吞掉独立事实）：M3-A-001（SNR 定义）与 M3-A-002（权重口径）虽同处 SNR/权重语域但判据与整改动作不同，分别定档为 P0 根条目的两个实例并互挂 related；M3-C-001 与 M3-C-002 同为 SCI-PHOT §4 有效域失效，但一是「有门未实现」一是「接口无法实现」，故各自成条。

## 6. 待复核（只读手段无法判定，需更高权限或运行验证）

| 项 | 卡点 | 需要的权限 / 动作 | 影响条目 |
|---|---|---|---|
| 1 | `lib/snr_estimator/tests/p1noise/**` 与 `astrocs_p1_noise` 目标是否真在仓库（tracked） | `git ls-files`（本轮禁 git）；或一次干净树 `cmake configure` 看目标是否生成 | M3-F-004（若确认未入库，M3-F-004 与 M3-F-001 应合并升级为「CI 上 0 条噪声科学测试」级 P0）；亦解 `L07 §6-3` 遗留问题 |
| 2 | `snr_estimator.dll` / `astrocs_p1_calibration.dll` 的**导出符号面**是否仍含 `(A−B)/mad` 旧口径与 `ac_set_num_threads` 等 | `nm` / `dumpbin` 或构建产物符号核对（本轮不构建） | M3-A-001（退休路径可否被外部调用的强度）、M3-I-002 |
| 3 | A 线把 `sigma_residual=0` 转成 `inf` 并写出 HISS 是否在运行期真发生 | 需实跑 `astrocs_orchestrator`（该线不在根构建内，`lib/orchestrator/cpp/Makefile` 独立） | M3-C-011（数值后果部分；静态三义事实已足以定 P1）；L07 §6-8 原待复核项 |
| 4 | 交付面（`packaging/`、`astrocs.exe` 导出）是否暴露 `p1_session_run`，即 M3-C-005 残余可否经生产入口可达 | packaging 清单 + 导出表核对 | M3-C-005、M3-G-001（两者是否升 P0 取决于此） |

## 7. 移交清单（跨域事实，本域不重复登记）

| 交给 | 需接走的事实（已在本域证据中定位） | 本域挂账条目 |
|---|---|---|
| **M4**（L08 / L09：Phase2 权重、积分、写出） | `SCI-CW:42` 的 `support×snr²` 与 `stage2.cpp` 的 mode 2=ivar 互斥；`synthetic_gate.cpp:3915-3944`（UPMW-001）判据方向与 SCI-CW 相反；`ACR_EQUIVALENCE:19`、`PHASE2_REJECTION:52-56`、`DATA_SEMANTICS:919`、`GLOSSARY:12-13` 四处一致站 ivar 侧；权重分支已复制为两份（:1106-1140 与 :1359-1400），任何整改必须两处同改 | M3-A-002、M3-A-001（请与 L09-003 / L09-013 / L18-002 并档，勿重复登记同一条） |
| **M5a / M5b**（L11 / L12 / L17：路由、CI、治理） | CLI 路由事实本轮独立复算并可与 L12 结论互证：`cli/runtime_client.cpp:101/328 build_pipeline_ir` 建 8 节点 IR，`module_adapters.cpp:5472` 把 `astrocs.phase1.calibration` 绑到 `p1_op_calibrate`；`make_session_module<P1Api>` 全仓 0 命中（仅 P2/P3 注册）→ `p1_session` 非 CLI 生产入口，故 M3-C-005 / M3-G-001 未升 P0；另 `cli/commands.cpp:628` 注释仍写「phase1 run: 进程内调用 p1_session」与实际路由不符（注释级，属 M5 域） | M3-C-005、M3-C-009、M3-G-001；建议与 L12-017（同型参数无效）并档 |
| **M5a**（L11：线程预算与并行断言） | `pc_api.cpp:331/:695/:969` 的「OpenMP 16 线程」注释（该事实以 **M5a-C-002 为定稿点**，本域仅在 M3-D-006 挂 related）；`ac_set_num_threads` 作为导出符号不受任何预算登记约束；`astro_calibration.h:133` 的头注释描述了一个签名里没有的参数（`psf_status` 类注释错位，若属 F00-03 范围请一并核） | M3-D-001、M3-D-006、M3-I-002 |
| **M6**（L13 / L14 / L16 / L18：文档体系与追溯层） | 双头追溯矩阵并存；`TST-*` 前缀违反 `TRACEABILITY_SPEC.md:78` 的 `^TEST-` 文法（三域同犯）；六类被引文档 / 路径不存在；`docs/TRACEABILITY.md:24/:50/:56` 与 `docs/README.md:55` 指向 `SCIENCE_ACCEPTANCE_MATRIX.md` | M3-E-001、M3-E-002（请与 F00-01 / F00-05 并档） |
| **M2 / L08 域** | `kcorr`、drizzle 侧 variance 传播与本域 `ivar` 产出语义的衔接（本域只登记产出侧缺陷，不判传播侧） | M3-A-004、M3-H-001 |
| **PSF / L06 域** | `0.7316727929211932`（`robust_residual_sigma` 换算）与 `kTrimMeanToSigma` 的同族常数登记，需与 M3-A-008 的三套 MAD 常数一并归一到单一定义点 | M3-A-008、M3-D-003 |
| **前台 / 负责人裁决（5 项）** | ① flat `median≈1` 前提走「补 SCI」还是「真归一」；② SCI-CW 与 SCI-UPM-WEIGHT-001 的权重口径唯一化（两份 FROZEN SCI 互斥，Agent 无权择一）；③ `min_samples` 5 与 64 的唯一值及处方；④ SNR canon 定义（`signal/√variance` vs 质量比代理）；⑤ p1noise 追踪状态（§6-1），据此决定 M3-F-001/004 是否合并升 P0 | M3-A-003、M3-A-001、M3-A-002、M3-C-003、M3-F-004 |

## 8. 本代理产出清单

- `问题扫描/_merge/M3.md`（本文件）
- `findings/A_SCI_DEF/p0|M3_L05_L07.md`（2）、`p1`（4）、`p2`（3）
- `findings/C_DOC_CODE_GAP/p0|M3_L05_L07.md`（3）、`p1`（9）、`p2`（2）
- `findings/D_COMMENT/p1|M3_L05_L07.md`（3）、`p2`（3）
- `findings/E_TRACE_BREAK/p0|M3_L05_L07.md`（1）、`p1`（1）
- `findings/F_TEST_GAP/p0|M3_L05_L07.md`（1）、`p1`（4）
- `findings/G_GOV_GATE/p1|M3_L05_L07.md`（1）
- `findings/H_NUMERIC/p1|M3_L05_L07.md`（2）
- `findings/I_DOC_HYGIENE/p1|M3_L05_L07.md`（2）、`p2`（2）
- 合计 **16 件 / 43 条**；未创建任何「一条 finding 一个文件」的散件；未改动任何 README 或他人产出；未执行任何 shell / 构建 / git 命令。
