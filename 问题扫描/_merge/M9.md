# M9 合并验证档案 — 健壮性与交付安全（L24）+ 角度单位换算链（L26）

- 合并代理码: M9 · 状态: **已定稿**
- 输入: `_cache/L24.md`（前台口径 15 条 P0:3/P1:10/P2:2）+ `_cache/L26.md`（12 条 P0:1/P1:7/P2:4）= 名义 27 条；另有 **M2b 改道 4 件**（IO_003 合同侧、L15-004/006/007 的 AIO/HiPS 合同面、write_properties 返回 void、原子发布四层分布并档）
- 纪律: 纯只读（read/grep/glob + web 仅取标准原文，URL 见 §11）；锚一律 `path::符号`；四态判定；已修复者不进 `findings/`
- 基准消费: `10_PROTOCOL.md`、`_merge/00_COORDINATION.md`（L24 R-1/R-2/R-3/R-4 + 前台对 F00-07b 的自纠）、`_cache/F00_FRONT_SPOTCHECKS.md`、`_merge/ANCHOR_VERIFY_REPORT.md`、`_merge/M2a.md`（§D 移交 4 条）、`_merge/M8.md`

## 0. 取证口径声明（本档所有数字的自述口径，供前台直接引用）

1. **结构化文件比对不用行文本作等值判据**（read 对 >2000 字符单行截断的伪影规避）。本档三处结构化比对各自声明口径：
   - `docs/contracts/API_CONTRACTS.csv` → **按 CSV 逐列解析 + 去重计数**（数据行 102、units 去重值 **1**）；未用行文本比较。
   - `docs/standards/STANDARDS_REGISTRY.md` → **按 `|` 分列取第 3 列为状态值**（条款行 22：CONFORMANT 15 / PARTIAL 6 / PROJECT_DEFINED 1）；偏差号存在性用整文件 grep 计数（DISP-HIPS-013=0）。
   - `_cache/L26.md` §2 矩阵 → **以 `| N |` 开头的行计数 + 取末列前缀**（50 行：✓25 / ✗22 / ⚠1 / 混合 2）。
   - 唯一例外：M9-F-2 判 O4「同构」是**两处 C++ 代码短行**的公式与格式串逐字比较（非结构化文件），且辅以本代理独立数值归约（§4 口径段），不受截断伪影影响。
2. **一切转述数字视为待复核线索，定稿数字一律本代理重算**。本档出现的 410MB / 1900 字节 / 343·384 / 58.6323 / 211076.285 / 3.220769 / 22 行 / 68 项 等，全部在 §2、§4、§5、§8、§11 标出算式或计数命令口径；其中 **两处转述被推翻**（见 §10 剔除表：L24 的"15 条 P2:2"、L26-007 的"3.220769 断言"）。
3. **系统性判词的归属按前台指定**：平台盲区=M9（本档 §4 为唯一主落）；行锚系统性失效=M6b-E-002、无据 VERIFIED=M6b-G-001、追溯双头=M6b-E-001、符合性证据链三断点=M8、不可达门=M7 —— 本档涉此四处一律 `related` 指回，不重复写判词。

## 1. 定稿清单与分布（类别 × 优先级）

**终数：26 条定稿 finding，13 个合并文件**（口径：`findings/**/M9_L24_L26.md` 内 `## M9-` 标题计数，字段完整性已逐条机检：11 字段齐、优先级与目录一致、无缺项）。

| 类别 | P0 | P1 | P2 | 条目 |
|---|---|---|---|---|
| H_NUMERIC | 2 | 4 | 0 | M9-H-1 … M9-H-6 |
| G_GOV_GATE | 0 | 6 | 0 | M9-G-1 … M9-G-6 |
| C_DOC_CODE_GAP | 0 | 3 | 0 | M9-C-1 … M9-C-3 |
| A_SCI_DEF | 0 | 2 | 1 | M9-A-1、M9-A-2、M9-A-3 |
| B_STD_MISMATCH | 1 | 1 | 0 | M9-B-1、M9-B-2 |
| F_TEST_GAP | 1 | 1 | 1 | M9-F-1、M9-F-2、M9-F-3 |
| D_COMMENT | 0 | 0 | 2 | M9-D-1、M9-D-2 |
| E_TRACE_BREAK | 0 | 1 | 0 | M9-E-1 |
| **合计** | **4** | **18** | **4** | **26** |

来源映射（口径：逐条 `- 来源:` 行的 Lxx-nnn 计数）：**L24-001…012 全部落地（12）+ L24-013 由 related 指针重建（1）= L24 域 13 条（P0:3/P1:10/P2:0）**；**L26-001…012 全部落地（12，P0:1/P1:7/P2:4）**；**M2b 改道新增 1 条（M9-G-6）**。
→ 与前台口径的差异一处：L24 名义 15 条中 **L24-014/L24-015 无正文**（`_cache/L24.md:7` 自述「finding 数（当前累计）: 12…未定稿」，§3/§5/§6 均为「待后续分片」占位），本代理不代为臆造，列为**未落地缺口**（§10、§13）。

## 2. 三条 P0 的独立复验结论（前台必问项 1）

**结论：L24-001 / L24-002 / L24-003 三条 P0 全部独立复验成立**，且本代理各补一项叶子未记的加重事实；另对本域新增 P0（L26-001）做量纲复算。

1. **L24-001 → M9-H-1（H_NUMERIC/P0）成立并加重。** 复验：`json_append_escaped`（module_entry.c:384）确无容量入参；`head[512]`（:738）三处 `hw += snprintf(hw, (size_t)(head+sizeof(head)-hw), …)`（:741/:770/:774）用应写长度推进；`memcpy(hw, schema, schema_len)`（:766-768）无界；`json_append_escaped(&hw, c.catalog_dir)`（:776）配 `char catalog_dir[1024]`（:211）。第二站点 `gaia_inspect`（:930-:940）在指针越界后 `buf+sizeof(buf)-w` 变负 → size_t 巨量，逐字命中。**本代理补充两项**：①越界长度会被回读进产品——`total = (uint64_t)(hw - head)`（:808）+ `memcpy(big, head, (size_t)(hw - head))`（:857），而 :816-817 作者自述「head 是栈缓冲…严禁作为 total 长度源直接拷贝」恰被该行违反 → 产品 manifest 携带栈相邻字节（信息外泄），不只溢出；②触发点前移——`cap==0` 的"仅报尺寸"分支（:830-841）同样执行整段拼装，故**两阶段协议的第一次调用即已溢出**。越界面独立复算：前缀+schema(127)+spec 三段 ≈ 300–390 字节，余量 120–210，catalog_dir 1023 字符全转义膨胀 2046 → 越界上限约 1.9KB（与叶子同量级）。正对照（同文件同族）：`strbuf_write`（:101-105）容量不足即 `ACS_ERR_PARAM` + `BUFFER_TOO_SMALL`；`json_copy_str`（:144）`c = n < dst_cap-1 ? n : dst_cap-1`。
2. **L24-002 → M9-H-2（H_NUMERIC/P0）成立并精确化。** 复验：`xf->spectrum_count = atoi(p + 14)`（gaia_client.c:1068）无上限；`malloc((size_t)capacity * spectrum_count)`（:1208）+ 扩容 `realloc(…, new_cap * sc->spectrum_count)`（:1250）；`memcpy(sc->spectra + …, spectrum, sc->spectrum_count)`（:1266）；源侧 `spectrum = p + 40`（:1549）。**本代理独立算术**：`WL_COUNT 343` + `STAR_STRIDE_SP = 40+(343+(343&1)) = 384`（:27-28）→ 每条星记录在光谱起点后仅 **344 字节可用（384−40）**，故 memcpy 长度为自报值时**每星越界读 spectrum_count−343 字节**；分配侧实测初值 `spec_collector_init(&sc_arr[i], 4096, spec_count)`（:2052）→ 4096×100000 = **4.096e8 ≈ 410MB/文件**（复算与叶子一致），导出侧 :2106 再按 `total×global_spec_count` 且 `MAX_STARS_RESULT=200000`（:31）→ **数十 GB 级 OOM 放大**。全文件仅 `==0` 兜底 4 处（:2050/:2090/:2241/:2432）。**新增正对照**：同文件合并路径 :2130-2131 与 :2274-2275 **已写** `if (copy_count > global_spec_count) copy_count = global_spec_count;` → 作者知道这类字段要夹，源侧却漏 → 修复成本=照抄同文件现成写法。
3. **L24-003 → M9-F-1（F_TEST_GAP/P0）成立并补全。** 复验口径=**命中计数 + 文件归属**（不用行文本）：对 8 个孤儿 TU 名在 `*.{txt,json,yml,yaml,cmake}` 全仓联合 grep、排除 run/build/out/Testing/artifacts 后，**8 个名字在 build/CI 声明文件的命中数 = 0**；同一次查询中 `tests/unit/CMakeLists.txt` 命中 11 次（p1_io_hardening :761-762、hips_publish_atomic :566-579）、`ci/ctest_baseline.json:90`、`ci/checks.json` 8 次（均为**已注册**目标）→ 孤立面与在册面在同一口径下可分。门本身非豁免：`CTEST-REGISTRATION`（checks.json:2412-2442）挂 fast/linux-main/**windows-main**、`"waivable": false`；门实现自证单向：`check_ctest_registration.py` 的 universe 只来自 `add_test(NAME …)`，C1..C6 无一条判「含 main() 的测试源未进任何 add_executable/add_test」。**本代理补充**：①站点由 7 补到 **8**（`modules/services/io/tests/fits_core_selftest.c`，与 hips_core_selftest.c 同目录同性质）；②该 TU 是 **IO-001 写出边界在 C 侧的唯一自检驱动**（全仓 grep `acs_fio_writer_*` 的调用者归属：fits_core_selftest.c + tests/io 三个 py + runtime/io 自身，**lib/、cli/ 命中 0**）→ 本域 M9-G-4/G-5 的任何修复都没有 C 侧机器验证面；③文档把它当证据的站点共 5 处（DEVELOPER_GUIDE:27、IO_002:46、PRODUCTION_EXECUTION_INVENTORY:270/:276 标 `production,yes`、TRACEABILITY_MATRIX csv:31+json:54）；④`lib/orchestrator/cpp` 整树确无 CMakeLists.txt（该目录只有 Makefile，另残留 `orchestrator.exe`/`nul`）。
4. **L26-001 → M9-B-1（B_STD_MISMATCH/P0，本域唯一科学口径类 P0）**：成立。本代理的独立支撑不依赖外部标准原文——同一函数内 CD 做了 `/3600.0`（ipv_wcs.cpp:274）而 A/B 没做（:357-373）；量纲复算：像素域 A 满足 `[A_px]=1/px^(i+j-1)`、标准域要求 `[A_std]=deg/px^(i+j)=A_px·s`，故按标准键语义读取者把畸变放大 **1/s ≈ 1e3–1e4 倍**（s 为本仓生产尺度 deg/px）。

## 3. 同仓正对照表（低成本修复论证；前台必问项 2）

口径：每行「正确写法」均为**本代理在当前树内逐字定位到的既有代码**（非设想、非外部范例）。

| # | 缺陷面（本域条目） | 同仓已有正确写法（path::symbol） | 成本判 |
|---|---|---|---|
| 1 | 转义/拼接不夹紧（M9-H-1） | gaia module_entry.c::strbuf_write :101-105（容量不足即 ACS_ERR_PARAM + BUFFER_TOO_SMALL）、::json_copy_str :144（min 夹紧）；跨模块 lib/drizzle/src/module_entry.cpp:724-726 | **同文件即有** |
| 2 | 自报计数不夹紧（M9-H-2） | gaia_client.c:2130-2131、:2274-2275 已写 copy_count 对 global_spec_count 的夹紧 | **同文件即有**，照抄一行 |
| 3 | 映射区寻址不与映射长度对照（M9-H-3） | hiss_reader.cpp:290-302（offset/compressed_size 对 filesize → HISS_ERR_FORMAT_VIOLATION）；ahpx/aio_ahpx_reader.cpp::readPixels :546-551 | 同库现成模板 |
| 4 | 溢出静默截断（M9-G-4、M9-H-1） | runtime/io/hips_core.c::hips_join_path :533-536（溢出即返回 ACS_HIPS_ERR_PARAM，**同目录**）；aio_publish.cpp:299、:102（对 snprintf 返回值判截断即拒） | 提为共用小函数 |
| 5 | 偏移窄化用 (long)（M9-H-6） | hiss_reader.cpp:307-310（_WIN32 → _fseeki64 / else → fseeko，注释引 02_FROZEN §14） | 同库现成分支 |
| 6 | 路径不做 UTF-8→UTF-16（M9-C-1、M9-G-6） | aio_util.h:10-23 ::aio_fopen_utf8（MultiByteToWideChar + _wfopen）；ahpx::ahpx_fopen_utf8 :174；**hiss_stream_writer.cpp:185 的 MoveFileExW + MOVEFILE_WRITE_THROUGH** | 已有 helper，三边界统一接 |
| 7 | rename 前无耐久性屏障（M9-G-5） | aio_publish.cpp:34（PUBLISH_FSYNC_FD=_commit）+ POSIX fsync + 父目录 fsync :369-384；p3_output.cpp（fsync 命中 15）、profile_store.cpp（11） | 宏即成，两处调用 |
| 8 | 元数据写出吞错（M9-G-6） | hiss_stream_writer.cpp:313-324、:384-399（注释「fflush 返回值必须检查, 失败时传播错误」+ fclose 硬失败） | 改返回类型并传播 |
| 9 | 入参不判负（M9-H-4） | 同文件姊妹入口 aio_hips_reader.cpp:394（i<0 或 >=size 即 return -1） | 一行 |
| 10 | 校验失败不回收资源（M9-G-1） | 同一函数上游 5 个失败分支 :1016/:1021/:1023/:1026/:1031 均就地回收 | 加 goto fail |
| 11 | 负向用例从不存在（M9-F-1、M9-F-3） | tests/unit/CMakeLists.txt:761-769 p1_io_hardening、:566-580 hips_publish_atomic（注册样板）；p1hips_tests_negative.cpp:237-240（域断言样板） | 注册模板即有 |
| 12 | 单位/域不声明（M9-C-2、M9-D-2、M9-A-3） | astro_image_io.h:126 aio_wcs_rotation_deg（名内带单位）；GAIA_QUERY.md:75-77（2 µas/LSB 已推导清楚，只是未回写代码与审计表） | 改名 + 一行注释 |

**本域判词（限本域）**：26 条中没有一条需要新算法或新架构——12 个缺陷面全部能在同仓（甚至同文件、同函数上游）找到已经写对的兄弟实现。故「修复成本高」不成立；真实成因是**新边界（runtime/io 的 IO-001/IO-002、lib/hips 的 AIO-002 原语、gaia_xpsd_client、astro_image_io 的 AHPX/SNR 读面与 properties 面）在建立时未继承同仓老边界（hiss_reader / readPixels / hips_join_path / strbuf / aio_publish / hiss_stream_writer）已确立的纪律**。该结论与 §4 平台盲区、§2 第 3 项注册门失守叠加，才解释这些遗漏为何长期存活。

## 4. 平台盲区主题清单（M9 主落；本档为该主题唯一总述处，他处一律 related 指回）

**聚合结论**：本域存在一个闭环的 Windows 交付盲区 = 「只有 Windows 会坏的缺陷」×「windows-main 上关键子检查恒零断言」×「畸形输入守卫 TU 从不进构建」。

| 编号 | 条目 | Windows-only 机理（本代理逐字复核） | 机器门为何看不见 |
|---|---|---|---|
| A | **M9-H-6**（L24-010）AHPX 块偏移窄化 | ahpx/aio_ahpx_reader.cpp:441 的 std::fseek(m_fp, (long)offset, SEEK_SET) —— LLP64 下 long=32 位，≥2GiB 偏移折叠；折叠后若落在合法位置则 fseek 返回 0 → **读到另一块数据** | Linux long=64 位同偏移「读对」→ CI 恒绿；hiss_reader:307-310 已有 _fseeki64 分支而 ahpx 无 |
| B | **M9-G-4**（L24-011）target/tmp 511 塌缩 | fits_core.c:1077/:1078 两个 ACS_FIO_PATH_MAX(=512，定义于 fits_stream_v1.h:33) 缓冲；:1228 静默截断 + :1229 tmp 同尺寸 → strlen≥511 时 tmp==target → "w+b" 原地覆写正式路径 | 塌缩需长路径（Windows 深目录/中文目录常态）；且 IO-001 在 C 侧唯一自检驱动 fits_core_selftest.c **从不进构建**（§2 第 3 项第 8 站点） |
| C | **M9-C-1**（L24-012）「路径均为 UTF-8」无适配层 | aio_publish.cpp:33 _mkdir、:34 _commit、:37 _unlink、:128/:169 FindFirstFileA、**:365 MoveFileExA**；fits_core.c:137/:1216 fopen；hips_core.c:122/:168/:415 fopen；gaia_client.c:1014/:1020 CreateFileA/CreateFileMappingA（对 _wfone|MultiByteToWideChar 的命中数：runtime/io 两文件均 **0**） | UCRT 按 ANSI 代码页解释 → 非 ASCII 用户目录被曲解；promote 在事务最后一步失败且只留 errno（GetLastError 丢弃）；Linux UTF-8 直通 |
| D | **M9-G-6**（M2b 改道）properties 非原子 + 吞错 | aio_hips_writer.cpp:335-342 —— 返回 void 的 write_properties 直写正式键文件，fopen 失败即 return，fprintf/fclose 返回值不查 | 无状态码即无断言对象；同库 hiss_stream_writer 的 W 系 + 硬失败写法未被继承 |
| E | 配套事实 1：windows-main 声明量 | 口径=对 ci/checks.json 用正则 ^\s*"windows-main",?\s*$ 计数**裸值行** → **68 项**（对照 "id": 行 113、"profiles" 键 113 → 约六成检查项声明在 Windows 执行）；另 .github/workflows 内该字面量命中 0（由 runner 侧脚本消费） | 声明量足够 ≠ 有效断言，见 F |
| F | 配套事实 2（**与 M5b 会签**）：windows-main 上的恒零子检查 | tools/check_api_docs.py:132-133 —— exe 路径拼的是无扩展名的 astrocs，随后 if os.path.isfile(exe): 且**无 else、无 fail** | Windows 交付物是 astrocs.exe → isfile 为假 → 「CLI 命令树 ↔ 文档一致性」子检查整段静默跳过；检查项 PASS 被误读为 Windows 已验（本代理读码复现，未执行脚本） |

→ **本域判词（限本域）**：平台盲区的真身不是「缺 Windows 机器」，而是**「68 项 Windows 声明」与「其中关键项在 Windows 上零断言」之间的差额无人核算**。建议把「windows-main profile 下的**有效断言数**（非检查项数）」提为独立度量项；度量本身属 M7（不可达门，他域主落），本档只交付 A–F 六项输入与锚点。

## 4.5 原子发布在四层的实现分布（**M2b 改道并档**；只定事实，DISP-HIPS-004 的路线二选一不选）

合同事实源：IO_003:23-24（`临时写（run 私有 stage）→ 关闭/fsync → fitsverify（结构 + DATASUM）→ sha256 → 原子 rename → 最后原子落 manifest.json(COMPLETE) = 完成标记`）、IO_003:44-45（`Windows 正式 DLL 交付（astrocs_io.dll）由 IO-003 同语义 C 接线复刻同一发布状态机；manifest/tree hash/错误码公式不变`）；归属事实源：HIPS_WRITER.md:214（`**无 COMPLETE 状态字、无 sha256 树哈希**——发布侧语义在 IO-003`）、:84（`原子发布属 IO-003 层，见 DISP-HIPS-004`）、lib/hips/memory.md:262（`sha256 tree_hash 归 IO-003 Python 层`）。

| 层 | 实现站点 | tmp→rename | rename 前同步文件 | rename 后同步目录 | sha256 / tree hash / manifest COMPLETE | Win 路径 API | 错误传播 | 生产接线 | 本域条目 |
|---|---|---|---|---|---|---|---|---|---|
| 1 IO-001 FITS 写 | runtime/io/fits_core.c::acs_fio_writer_begin_v1 / ::acs_fio_writer_end_v1（:1228/:1229/:1464-1480） | **有** | **无**（fsync/_commit/FlushFileBuffers 命中 0；仅 fflush :176） | 无 | **无**（tree_hash 命中 0） | **fopen（ANSI）**（:137/:1216） | 有（状态码 + err） | **零生产调用点** | M9-G-4、M9-G-5、M9-C-1 |
| 1b IO-002 HiPS 读 | runtime/io/hips_core.c | 不适用 | 无（命中 0） | 无 | 无 | fopen（ANSI） | 有（::hips_join_path 溢出即拒 = 本域正对照） | 读路径在用 | §3 表第 4 行 |
| 2 IO-003 执行形态 | runtime/io/hips_output_store.py（+ fits_verify.py） | 有 | **有**（合同 :23） | 有（同链） | **有**（:22-23 定义；:65 sha256_bytes、:69 sha256_file、:87 tree_hash） | Python 路径语义 | 有（异常） | 仅 Python 交付形态 | 事实源（非缺陷） |
| 3 AIO-002 发布原语 | lib/hips/src/aio_publish.cpp（:99 mkdir 链、:128/:169 枚举、:299 截断拒、:365 promote、:369-384 POSIX+父目录 fsync） | 有（stage→promote） | **有**（PUBLISH_FSYNC_FD=_commit :34 / fsync :45） | **有**（后序 tree_fsync，publish.h:20） | **无**（归 IO-003 层，已在册 DISP-HIPS-004） | **_mkdir/_unlink/FindFirstFileA/MoveFileExA 全 ANSI** | 有（错误码枚举） | **已接线**（p1.hips 事务面） | M9-C-1 |
| 4 AIO 产品内文件 | lib/astro_image_io/src/hips/aio_hips_writer.cpp::write_properties（:335-342，调用 :1023/:1242） | **无（直写正式路径）** | 无（命中 0） | 无 | 无；**但 tile 的 CHECKSUM/DATASUM 是算出来的真值**（::write_chksum_deterministic :178-212） | **fopen（ANSI）** | **无（返回 void，if (!f) return）** | **已接线** | **M9-G-6** |
| 4b 同库最强范例 | lib/astro_image_io/src/hiss_stream_writer.cpp::atomic_replace（:178-197；:185 MoveFileExW + MOVEFILE_REPLACE_EXISTING + MOVEFILE_WRITE_THROUGH；:313-324/:384-399 fflush/fclose 硬失败） | 有 | **有（WRITE_THROUGH，仓内唯一）** | 无 | 不适用 | **宽字符 W 系（仓内唯一）** | 有 | 已接线（HISS 流式写） | §3 表第 6/7/8 行 |
| 4c 同库次范例 | lib/astro_image_io/src/aio_pipeline.cpp（:876-921：".tmp" → 查 fflush :904 → MoveFileExA :911 / rename :917，错误码 1/2/4/5） | 有 | 无（仅 fflush） | 无 | 不适用 | MoveFileExA（ANSI） | 有 | 已接线（帧缓存） | 补强 M9-C-1 的同库分叉 |
| 5 他层（旁证，不立案） | lib/orchestrator/cpp/src/checkpoint.cpp:526（MoveFileExA）、lib/backend_host/profile_store.cpp:117（MoveFileExA，同文件 fsync 命中 11）、lib/phase3_session/p3_output.cpp（fsync 命中 15）、tests/backend/p3_output_fsync_interposer.cpp（**已存在的 fsync 注错测试件**） | 有 | 部分层有 | profile_store 有 | 不适用 | 混用 A/W | 有 | checkpoint 树零注册（§2 第 3 项） | related M9-F-1 |

**四条事实结论（只定事实）**：
1. 「原子发布」在本仓**不是有无问题，而是四层四样**：rename 人人有；文件耐久屏障只有第 2、3 层齐；目录同步只有第 3 层；宽字符路径只有第 4b 层一处；sha256/manifest 只有第 2 层。
2. **最弱的一步恰好是元数据那一步**——第 4 层 properties 既非原子又吞错，而它是 IVOA 键与 AstroCS provenance 的唯一载体；与 L26-002（`hips_pixel_scale` 角秒口径）同文件叠加 = 「键值口径已错 + 键文件可静默缺失」。
3. **新边界最弱、老边界最强**：第 1 层同时缺耐久屏障、宽字符与长路径纪律，而这三项在同仓都有现成写法（第 3 层的 fsync 宏、第 4b 层的 MoveFileExW|WRITE_THROUGH、同目录 hips_core.c::hips_join_path 的溢出即拒）→ 与 §3 判词咬合。
4. **合同与归属叙述在 C 侧不闭合**：IO_003:44-45 要求「Windows 正式 DLL 交付由 IO-003 同语义 C 接线复刻同一发布状态机，manifest/tree hash 公式不变」，但第 3、4 层的 C 实现都没有 manifest/tree hash；而 lib/hips/memory.md:262 与 HIPS_WRITER.md:214 反过来把该职责归 Python 层。可判事实即：**归属文字两处一致（归 IO-003 层），且与 IO_003:44-45 的"C 侧复刻"要求互斥**。DISP-HIPS-004 已覆盖 writer 侧无 COMPLETE/树哈希这一格 → 本域不重报偏差本体，只登记此互斥与 properties 吞错两条新事实。整改路线二选一由负责人裁决（M2b 已提 A-22），**M9 不选路线**。

## 5. 会签与跨域咬合

| 对手方 | 事项 | M9 处置 | 本代理自证 |
|---|---|---|---|
| **前台 / M2a** | 必问项 4：M2a 是否已按 R-2 重判 L10-008 | **已执行，会签** | findings/C_DOC_CODE_GAP/p1/M2a_L04_L10.md:142-160（:146 来源行写「按前台改判令 R-2 重写定档」、:158 结论为"静默接受"而非"崩溃/必败"、:159 定 P1）；M2a 改判表第 7 行重挂为 M2a-C-9 |
| **M2a** | 其 §D 移交本域的 4 条 XPSD 事实 | **全部落地**：M9-H-2 / M9-H-3 / M9-G-2 / M9-G-3（对应 L24-002/005/006/007）；M2a:194 把 512 截断站点归 L24-011 → 已落 M9-G-4 | 逐条与 M2a 无重复计口 |
| **M8（前台指定）** | 孤儿测试根因主落 M8-F-004 | **已改挂**：M9-F-1 新增「归属」行，根因（注册门单向闭包、CI 永久隐形）让给 M8-F-004；本条只保留实例面（8 个 TU 的名字、**各自钉住什么修复**（逐字取自各 TU 文件头，已补进定稿）、sanitize_wsl_v4/v5 分叉） | 反向复核：8 TU 全部实际存在且路径如列（lib/astro_image_io/tests 6 个 + modules/services/io/tests 2 个），build/CI 命中计数 0 |
| **M8 的反证（第 3 件）** | lib/gaia_xpsd_client/tests 的 UT-GAIA-ZLIB 已注册 | **采纳并改述**：M9-F-1 已把"gaia 面无注册"改为「**部分在册**——UT-GAIA-ZLIB（checks.json:3105 起）+ CAT-GAIA-TEST 族（tests/unit/CMakeLists.txt:787-915：cat 七模式/polar_ref/oracle/adapter/integration）+ gaia_unshuffle_test（:121-129）在册；本域主张的只是**恶意输入/健壮性负例**零注册」 | 三处均本代理现场读证 |
| **前台/M8（第 3 件后半）** | checks.json 的 "id" 计数（前台 112，此前有 111 一说） | **两种转述都不成立，本代理定稿为 113**。口径三条互证：①python json 解析顶层 "checks" 数组长度 = **113**；②全文件 "id" 字符串总数 = 113，且无嵌套 id；③grep 裸 "id": 行 = 113（六空格缩进 113）。同口径另得 **profiles 含 windows-main 的检查项 = 68** | 见 §12 前言；请前台把 111/112 一律订正为 113 |
| **M8（第 4 件）** | 不可达门家族须含"基线恒真探针"一例 | **已纳入家族清单**（总述仍归 M7，本域只提交站点）：tests/unit/p2_seam_gate_test.cpp:71 `g.avg_equivalent_cores = compute_cores_threshold(g) * 1.2;  // 超阈值` —— 用被测函数自身产出的阈值再乘 1.2 造"超阈"输入 ⇒ 资源门断言与被测实现同源，恒可自证。本代理现场读证该行确为此文 | 该行 :58-:59 的数值门（seam 8→0）与 :71 的资源门同处一测，故"两门同时通过"的表述中只有数值门是独立判据 |
| **M5b** | windows-main 上的恒零子检查 | **会签并已独立复现**（§4 行 F）：tools/check_api_docs.py:132-133 —— exe 拼的是无扩展名的 astrocs，if os.path.isfile(exe): 之后**无 else、无 fail** → Windows 交付物名为 astrocs.exe → CLI 命令树↔文档一致性子检查整段静默跳过 | 另附 §4 行 E 的 68/113 声明量对照 |
| **M6b** | 行锚系统性失效 / 无据 VERIFIED / 追溯双头 | **一律 related 指回，本域不写总述**；本域提交给 M6b 的站点：TRACEABILITY_MATRIX.csv:31 + .json:54（把零注册的 fits_core_selftest.c 当 io 模块证据）、PRODUCTION_EXECUTION_INVENTORY.csv:270/:276（把孤儿标 production,yes）、STANDARDS_REGISTRY.md:98（§6.3.1 仍 CONFORMANT） | 见 M9-F-1、M9-E-1 |
| **M8（符合性证据链三断点）** | 总述归 M8 §0.2 | **本域只作实例**：M9-F-1（证据文件从不进构建=断点②本域样本）、M9-E-1（注册表账实不符=断点③本域样本）；M9.md 不重复三断点判词 | — |
| **M2b（改道四件）** | IO_003 合同侧 + L15-004/006/007 的 AIO/HiPS 面 + write_properties void + 四层分布并档 | **全部处置**：①新条目 **M9-G-6**（properties 吞错/非原子，且界定了 DISP-HIPS-004 已在册故不重报偏差本体）；②§4.5 原子发布四层分布表（只定事实、路线二选一不选）；③IO-001 的 CHECKSUM 恒占位**不重复定稿**（M2b 已在 B_STD_MISMATCH/p0 落，本域仅在 §4.5 引用并补 HiPS tile 侧「有真值」的反向澄清，防止过度概括）；④**L15-004（Gaia 历元 J2016.0 vs ICRS/J2000）与 L15-007（D.fits 条款锚错位）经核不属 M9 输入切片**（本域 L24/L26 无相关记录；gaia 域条目只主张边界与单位事实）→ **退回前台定归属**，本域不接、不并档（§13 第 1 项） | M2b 转来的三处「同库已在用 rename」本代理逐处复核为成立，但其中 aio_publish.cpp:285 是**路径长度校验分支**而非 promote 点，真正的 promote 在 :365（Win）/:369-384（POSIX）→ 定稿按后者引用 |

## 6. 待接线升级清单（前台指令 R-4；只挂触发判据，不改当前档）

| 条目 | 当前档 | 升档的**唯一触发判据** | 升后 |
|---|---|---|---|
| **M9-G-4**（L24-011 target/tmp 511 塌缩） | P1 | 任一生产写出路径改走 acs_fio_writer_*（即 §8.3 的 IO 写出收敛批次落地，或 lib/drizzle、lib/hips、p1_* 的 FITS 写出经 IO-001） | **P0** |
| **M9-G-5**（L24-013 rename 前无 fsync） | P1 | 同上（同一提交点，必须与 M9-G-4 同批处置） | **P0** |
| **M9-C-1**（L24-012 无 UTF-8→UTF-16）之 **runtime/io 半边** | P1 | 同 G-4 判据 | **P0** |
| — 以下三项**已接线**，其升档不取决于"接线"，不得挂本清单（R-4 要求避免的条件误挂） — | | | |
| M9-C-1 的 **lib/hips/AIO-002 半边** | P1（平台/需真机） | 负责人认定「Windows + 非 ASCII 目录为发布阻断场景」即升 | P0（**升档待裁决**） |
| M9-G-6（properties 吞错） | P1 | 认定「无 properties 的产品被当成功交付」属发布阻断即升 | P0（**升档待裁决**） |
| M9-H-6（AHPX (long) 折叠） | P1 | Fatduck 真机出现 >2GiB 容器读错/失败 | P0（**待实测**，见 §12） |
| **反例声明**：M9-H-1、M9-H-2、M9-F-1、M9-B-1 | **已是 P0** | 无"待接线"缓冲，属现产可达 | — |

**本域接线现状的自证口径**：对 acs_fio_writer_begin_v1 / end_v1 / write_v1 全仓 grep 后按文件归属计数 → 调用者仅 modules/services/io/tests/fits_core_selftest.c（13 处）、tests/io/{make_hips_fixture,test_fits_stream_contract,hips_output_fixture}.py、以及 runtime/io/fits_core.c 自身声明；**lib/ 与 cli/ 命中 0**。故"IO-001 尚未接线"在本域是可复核事实而非转述。

## 7. 「修复即扩散」子清单（前台必问项 6，挂在 M9-B-1 之下）

**定义（本域用法）**：某次修复/新特性把**尚未固化的错误口径**复制到一个新的、更可交付的面上，使错误从"内部计算"升级为"随产品交付"。以下站点均本代理现场读证。

| # | 扩散站点 | 被复制的错误口径 | 为什么算扩散 |
|---|---|---|---|
| 1 | lib/core/src/module_adapters.cpp::**p1_sip_write_header_frame**（复核时 :1678，键写 :1688/:1693-1694，B/AP/BP 同型至 :1712） | 像素域 A/B/AP/BP 数组以 **%.17g 原样写入 FITS 标准键** A_i_j/B_i_j/AP_i_j/BP_i_j | B2-A17 是"补齐写出面"的**并发修复批次**（module_adapters.cpp:2319 与 :2361-2371 注释、DATA_SEMANTICS:247 的 B2-A17 段已把它登记为正式行为）→ 键名进入交付合同，而**没有任何一处做 px→deg 换算** |
| 2 | 同文件 :1648 注释 `SIP 前向修正 (FITS paper IV §2.1: U = dx + A(dx,dy), V = dy + B(dx,dy))` | 以**不存在的文献编号**（本仓仅登记 Paper I/II，无 Paper IV）为像素域用法背书 | 修复顺带**固化了错误依据**，比无依据更难纠正（宪章 §12.3 违例） |
| 3 | 同文件 :1672-1673 `p1_tan_forward_reference(…, crpix1 + (dx + A), crpix2 + (dy + B), …)` | 把 A/B **加在像素上**（自证其域为像素） | 消费面与写出面同域 → 错误口径获得"端到端自洽"的外观 |
| 4 | lib/orchestrator/cpp/src/orchestrator.cpp :2085/:2092-2093 写、:4444-4467 回读（第二处 A_%d_%d 在 :4467） | 同一批 px 域数组的**第二条写入路径** | 两条独立写出路径都无换算 → 修复若只改一处即出现两制 |
| 5 | lib/drizzle/src/module_entry.cpp::kHeaderKeys（复核时 :706-717，含 A_ORDER/B_ORDER/AP_ORDER/BP_ORDER 与 CROTA1/CROTA2）+ :725-733 的 kv_set 循环 | 键词汇表被第三层模块**原样透传**（且 CROTA2 口径亦未固定，见 M9-B-2） | 扩散到 drizzle 交付面的 header 透传；**本代理新增站点**（叶子未记） |
| 6 | lib/astro_image_io/src/hips/aio_hips_writer.cpp:943 + :984（hips_pixel_scale 写角秒）+ docs/algorithms/HIPS_WRITER.md:186（把角秒公式写进算法文档）+ p1hips_tests_oracle.cpp:183-191（O4 用同式同格式串断言） | 单位口径（度制的标准键写角秒） | **文档 + 回归面同时追认**错误口径 → 与本表 #1 同族（本体主落 M2b-L15-002，回归面本域 M9-F-2，此处只记"扩散通道"） |

**处置口径**：#1 与 #2 必须并入 **B2-A17 批次的验收清单**（该批次正在改这块代码，属唯一便宜的收口时机）；若该批次只做"补齐键名"而不做"域修正 + 文献订正"，则本清单视为**已发生扩散**。#5 是新增扩散面，请前台转 drizzle 域。

## 8. 「回归保护钉死错误口径」的反向形态（前台必问项 7；与 M8 的"钉死已废除值"族对偶）

M8 域的症状是**测试钉住已废除的旧值**；本域症状相反——**测试钉住从未正确过的口径**，两者都让"修复"表现为测试红，故必须分开登记。本域样本：

| 条目 | 被钉住的错误 | 现场证据 | 修复时的连锁 |
|---|---|---|---|
| **M9-F-2**（L26-002） | hips_pixel_scale 的**角秒值**被当期望 | p1hips_tests_oracle.cpp:183-191：O4 与产品用同一 `3600·180/π·√(π/3)/nside` 公式 + `%.6f` 字符串等值 | 改产品为度制 → O4 红；且 O4 本身不能证伪单位错（同构） |
| **M9-A-2 / M9-F-3**（L26-006/007） | 亚弧秒 scale（0.35/0.1）被钉为合法基线 | p1hips_tests_units.cpp:183、p1hips_tests_properties.cpp:328-329（=="0.3500"）、p1hips_tests_negative.cpp:237-240（`(0.5,0.1)→0`） | 若按 M9-A-2 改为"越标定域即拒绝" → 这三条断言**假失败** |
| **M9-G-2 / M9-G-1**（L24-006/004） | **部分加载与坏文件被钉为"接受"** | tests/unit/gaia_cat_test.c:645-710 mode_negative：:661 坏魔数断言「应静默跳过」且 file_count==0、:688 八字节头同判、:694-700 「只要部分树可加载即接受」 | 实现"要么完整要么拒绝" → 该在册测试直接红，且它会在评审中被当成"行为回归"证据反对修复 |
| **M9-B-2**（L26-005） | CROTA2 的 sin 项符号**不可观测**（无 0 值外样本） | 全仓 CROTA2 真值仅 module_adapters.cpp:2324 的字面量 "0"；无 CROTA2≠0 往返 fixture | 未来若定口径，无测试能守住 |
| **M9-B-1**（L26-001） | SIP 像素域被端到端自洽断言（写与读同域） | module_adapters.cpp:1672-1673（加在像素上）、orchestrator.cpp:4444-4467（同域回读） | 改为标准域后，**所有同域自洽断言都会同时红** → 必须配一条异源比对（astropy/wcslib）才能推进 |

**判词（限本域）**：本域 4 条 P0/P1 的**真正阻塞不是代码，而是"错误行为已在册测试里获得期望值地位"**。因此每条处置都附了"须同批订正的在册断言"清单。机制侧（哪些测试在册、哪些不在册）主落 M8-F-004，本域不复写。

