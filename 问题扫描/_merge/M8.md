| 16 | 前台 09-xx 转述的 \`io_ownership_test.cpp:64 aio_write_bytes(..,NULL,0)==AIO_OK\`、\`:77/:79 无条件 printf\` | 前台转述 | **该锚点在当前树不存在** | **不采**：全仓 \`aio_write_bytes\` 仅命中本审计文件自身（唯一 1 处），\`tests\/**\` 与 \`runtime/**\`、\`lib/**\` 全 0 命中；真源与其 \`run/**\` 影子树副本同为 32 行 ⇒ 判为跨条误挂，M8-F-002 一律用真源 \`path::符号\`（:9/:14/:25/:31） |






























































































































































| 16 | 前台 09-xx 转述的 `io_ownership_test.cpp:64 aio_write_bytes(..,NULL,0)==AIO_OK`、`:77/:79 无条件 printf` | 前台转述 | **该锚点在当前树不存在** | **不采**：全仓 `aio_write_bytes` 仅命中本审计文件自身（唯一 1 处），`tests/**`、`runtime/**`、`lib/**` 均 0 命中；真源与其 `run/**` 影子树副本同为 32 行 ⇒ 判为跨条误挂（疑与 `tests/unit/aio_abi_selfcheck.cpp:64/:77-79` 的 rc/failures 判定段混线，该处逻辑本身正常）。M8-F-002 一律用真源 `path::符号`（::failures :9/:14、唯一 CHECK 站点 :25、::main 的 return :31） |