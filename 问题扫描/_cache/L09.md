# 扫描缓存 L09 — Phase2 排异、ivar 加权积分、马赛克写出与会话编排

- 代理码: L09
- 覆盖文件: docs/science/{REJECTION,INTEGRATION,SCIENCE_SCOPE}.md；docs/algorithms/{REJECTION_ALGORITHMS,PHASE2_REJECTION,INTEGRATION_ALGORITHMS,PHASE2_INTEGRATION,PHASE2_MOSAIC_WRITE,PHASE2_SESSION}.md；ASTROCS_PROJECT_CONSTITUTION.md（全文）；问题扫描/10_PROTOCOL.md、20_AGENT_PLAN.md；lib/phase2/src/rejection.cpp（1-130、900-1240、1240-1620、1619-1999、1998-2076 全量分块）、integrate.cpp（全文）、lib/phase2/include/astro/phase2/{rejection.h,integrate.h}（全文）、lib/phase2_session/{p2_session.h,p2_session.cpp}（全文）、lib/phase2/tools/stage2.cpp（525-645、1060-1620 关键区 + 定向 grep）、lib/core/src/module_adapters.cpp（1-40、270-330、484-502、880-960、2860-3480、3981-3987、5010-5130 及定向 grep）、lib/phase2_rej/{module.yaml,README.md}、lib/phase2_int/module.yaml、lib/phase2/README.md（头 40 行）、lib/phase2/src/stage2_common.cpp（方法名解析 grep）、lib/phase2/src/acr_kernels.cpp（100-250）、lib/phase2/tools/rejection_oracle_compare.py（全文）、lib/phase2/tests/synthetic_gate.cpp（头部+锚点 grep 核对）、cli/{commands.cpp(724-1115),runtime_client.cpp(1-290),parser.cpp(grep)}、docs/development/CONFIG_SCHEMA.md:40-79、docs/contracts/DATA_SEMANTICS.md:2228-2461(grep)、docs/contracts/PUBLIC_API.md(grep)、docs/science/UNCERTAINTY_AND_COVARIANCE.md(grep)、docs/modules/registry/astrocs.phase2.reject.md(grep)、runtime/pipeline/{module_ports.registry.json,fixtures/phase2_typed_dag.json}(grep)、tests/unit/{p2_rejection_test.cpp,p2_output_semantics_test.cpp,p2_seam_gate_test.cpp,p2_ir_facade_test.cpp,p2_block_plan_test.cpp(前50行)}、tests/api/{test_reject_parallel.py,test_reject_integration_oracle.py,test_seam_metric_gate.py}、tests/backend/{test_p2003_seam_oracle.py,test_p2004_reject_integrate.py,test_p2006_canonical_pipeline.py,test_p2007_joint_gate.py}；外部核对：NIST/SEMATECH e-Handbook §1.3.5.17.3（Generalized ESD 公式原文，web_fetch）。
- 未覆盖(原因): lib/phase2/tests/synthetic_gate.cpp 5452 行仅做锚点/名称核对与定向 grep，未通读全部断言体（体量）；lib/core/src/module_adapters.cpp 仅通读 Phase2 节点区与注册区（其余为 Phase1/Phase3 域）；lib/phase2/tools/stage2.cpp 未通读全 1762 行（coverage/sampler/block 区属 L08）；rejection.cpp:130-900 RCR 官方表未逐值与 rcr 2.4.7 源码比对（需外部源码）；tests/backend/test_p2005_block_io.py（块 IO，L08 边界）；docs/contracts/DATA_SEMANTICS.md §20-22 与 PUBLIC_API.md 全文（L10/L12 slice）；WBPP 2.9.1 bestRejectionMethod 原始源码（本机安装源不在仓库，外部不可达，见 §6）。
- 判定基线: 宪章 §1.1/§4.1/§6.1/§6.2/§6.3/§8.3/§8.4/§12.2/§12.3/§13.1/§13.2/§13.4/§14.3/§14.4/§16.2/§17；SCI-REJ-001..008；SCI-INT-001/002/004/008；SCI-SCOPE-001；ALG-REJ-001..008；ALG-INT-001..003；ALG-P2-REJ-001；ALG-P2-INT-001；ALG-P2-HIPS-001..004；ALG-P2-SESSION-001；DATA-P2-VAR-001（DATA_SEMANTICS §30.1，科学公式权威=UNCERTAINTY_AND_COVARIANCE.md）；NIST e-Handbook Generalized ESD 公式。
- finding 数: 16（P0:3 / P1:10 / P2:3）

---

### L09-001 N≤4 全拒降级为“全接受”是未入 SCI 的临场启发式且无测试
- 类别: G_GOV_GATE
- 建议优先级: P0
- 位置: lib/phase2/src/rejection.cpp:1835-1849；docs/science/REJECTION.md:79,86-88；docs/algorithms/PHASE2_REJECTION.md:375-386(§5 F14),508-509(§10.9)
- 证据摘录:
> // 容错：小栈全拒回退为 UNDERDETERMINED（不 hard fail）
> // 根因：wbpp_2_9_1 percentile (low 0.2 / high 0.1, scale=|median|)
> // 在 N=4 时阈值过严可导致全 rejected（tile 116446 N_B=4），而
> // n=2 走 underdetermined_n 白名单绕过；该状态按 SCIENCE_FREEZE
> // 仍 hard fail。仅调容错路径、阈值冻结不变：N<=4 且全拒时降级为
> // 全接受 UNDERDETERMINED（保留中位数/放宽阈值的等价可继续语义），
> // 保证 32 帧 mosaic 可落盘且不破坏 2 帧语义。
（rejection.cpp:1835-1841；随后 :1842-1849 把全部 reasons 改写为 P2_REASON_UNDERDETERMINED、accepted_count=n、status=UNDERDETERMINED）
> | `n ≤2` 或 `n < minimum_n` | `UNDERDETERMINED` 全接受 | `rejection.h:86` |
> | 全拒 | `ALL_REJECTED` | `rejection.h:84` |
（SCI REJECTION.md §8 表 :86-88；§7 :79 “UNDERDETERMINED 单调性：n ≤2 恒 UNDERDETERMINED，不做剔除”）
- 权威依据: 宪章 §6.3（“排异算法及自动选择策略必须在 SCI/ALG 中版本化，有合成污染实验、边界条件和确定性验证；不得靠临场启发式静默改变方法”）；SCI-REJ-001 §7/§8（FROZEN）；宪章 §1.1
- 问题说明: 3–4 候选像素在“全部样本被判拒”（即检出污染）时，实现把该像素降级为 UNDERDETERMINED 并全部接受，被污染样本直接进入积分。该行为①与冻结 SCI §8“全拒→ALL_REJECTED”、§7 单调性(n≤2)直接冲突，SCI 无任何 N≤4 容错条款；②注释自认“该状态按 SCIENCE_FREEZE 仍 hard fail”却仍然放行，触发根因是一次具体运行的 tile 116446，属“临场启发式静默改变方法”；③仓库内找不到任何执行/断言该 fallback 的合成污染测试（lib/phase2/tests 全量 grep “116446” 无命中；PHASE2_REJECTION §11.4 F1-F8 冻结测试面亦不含此路径），宪章要求的边界+污染实验证据缺失。
- 影响: 所有经 stage2/节点链生产的 3–4 深度 tile 像素；卫星线/宇宙线整栈污染的小深度像素带污染入 mosaic（signal/support/flux 均改变）。真实数据低深度区直接受影响。
- 建议处置: 负责人裁决：按 §1.2 把“N≤4 全拒容错”正式入册 SCI/ALG 并补合成污染注入、边界与确定性门（现无测试），或删除 fallback 恢复 ALL_REJECTED 语义。禁止维持“代码注释自认偏离冻结科学约定”的现状。
- 置信度: 高
- related: L09-005、L09-013；宪章 §6.3

### L09-002 冻结 SCI 内 support reducer 两种口径互斥且代码只满足其一
- 类别: A_SCI_DEF
- 建议优先级: P0
- 位置: docs/science/INTEGRATION.md:21,58,63,75；lib/phase2/src/integrate.cpp:44-50,76；docs/algorithms/INTEGRATION_ALGORITHMS.md:20-21,53；docs/algorithms/PHASE2_INTEGRATION.md:42,64,166-174
- 证据摘录:
> support = (support 空) ? 1.0 : max_{valid,W>0} support[i]   # canonical reducer, 覆盖并集保守下界
（INTEGRATION.md §5 :58）
> - **支撑单调性**：sup_max = max(accepted support)，增样本不减 support。
（INTEGRATION.md §7 :75）
> // B2-A7: canonical reducer 契约 = max(**accepted** support)，作用域是
> // 通过资格门 (accepted ∧ value/支持 finite) 的全部样本，**不含**权重
> // 正性要求（w==0 合法但不贡献 signal）。故 sup_max 必须在权重分支
> // 之前更新；
（integrate.cpp:44-48，代码 :49-50 于权重分支前更新 sup_max）
- 权威依据: 宪章 §6.3（integration 必须明确归一化/缺失值/不确定度）、§1.1（不得选择方便的一层）、§12.3-4；SCI-INT-001（FROZEN T108）
- 问题说明: 同一冻结 SCI 内 §5 公式把 support 限定在 W>0 样本，§7 不变量与 §2 符号表又定义为 max(accepted support)——“零权重 accepted 样本的 support 是否计入”两种读法给出不同数值输出。实现（B2-A7 修复，tests/unit/p2_output_semantics_test.cpp:85-107 回归门）选择 §7 口径，直接违背 §5:58 字面公式；共享 ALG（INTEGRATION_ALGORITHMS.md:20-21 F6a）把修复后口径写为离散公式，而 ALG-P2（PHASE2_INTEGRATION.md §3/§7/§11.3，2026-09-09 冻结）仍声称“实现现状=max_{valid,W>0}、sup_max 位于 :49 continue 之后（DISP-P2INT-001 待整改）”。SCI 内部、两代 ALG、ALG↔代码三方割裂，且代码修复未走 SCI 修订。
- 影响: support 输出改变 → stage2 area=support×A_cell、flux=signal×area 与写盘 covered_area 随之改变（凡存在零权重/ivar=0 accepted 样本的像素，生产 mode=2 可达）。
- 建议处置: 以 §1.2 修订 SCI 统一唯一公式并重冻结；同步订正 PHASE2_INTEGRATION.md 的 §3 锚表与 §11.3 DISP 状态（其现状描述已随 B2-A7 落地而失效）。
- 置信度: 高
- related: L09-007；INTEGRATION_ZERO_WEIGHT_CONTRACT

### L09-003 stage2 ivar 缺 tile 时静默降级 support 作权重，违宪且文档失实
- 类别: G_GOV_GATE
- 建议优先级: P0
- 位置: lib/phase2/tools/stage2.cpp:1106-1116（并行 lambda）、:1359-1375（串行）、:1254-1263（ivar_valid 置 0）；docs/algorithms/PHASE2_MOSAIC_WRITE.md:321-322
- 证据摘录:
>                         if (orig < depth && ivar_valid[orig]) {
>                             const double iv = (double)ivarv[
>                                 (std::size_t)orig * chunk_pixels + i];
>                             weights[s] = iv;
>                             ++tl_local_ivar_used;
>                         } else {
>                             weights[s] = support_v[s];
>                         }
（stage2.cpp:1109-1116；串行路径 :1363-1375 同构，其 :1368 注释自称“禁止静默换 support”）
> 像素级 ivar 缺失仅显式 fallback 路径可达（开时已门控 :565-577），降级 support 并计数
（PHASE2_MOSAIC_WRITE.md §7 :322 大意；实际该 fallback 分支无任何计数器）
- 权威依据: 宪章 §6.3（“support/coverage 不能作为 inverse-variance 或 SNR 权重”）；宪章 §14.4（禁止为未发生场景静默 fallback/改默认值）；SCI-INT §3（weights 单位=信号⁻²，support 无量纲）；PHASE2_MOSAIC_WRITE §7 自身“禁 support 冒充 ivar”红线
- 问题说明: :565-577 的 ivar 门只 gate 产品级打开失败；当帧的 ivar 产品存在但某 tile 读取失败（:1256-1262 → ivar_valid[s]=0）时，权重被替换为该像素 support（无量纲 [0,1] 冒充 ADU⁻²），无须任何 legacy 开关、无任何计数器（文档“降级 support 并计数”失实：fallback 分支无 ++ 统计，diagnostics 字段面无对应项），运行照常产出名义 ivar coadd 的 mosaic（:567 log 自警“非逆方差语义”）。weight_mode=0 的 support×snr²（:1136/:1396）同属宪章禁语，且仍是 phase_config 可达键。
- 影响: stage2 路径全部真实数据 mosaic 的权重语义与数值；同输入下 ivar 缺 tile 与否改变输出且不可检出；节点链对照实现（module_adapters.cpp:3362-3373 tile 读失败即 fail-closed）证明该差异是行为分叉而非统一合同。
- 建议处置: 与生产节点链归一：tile 级 ivar 缺失同样 fail-closed，或至少显式开关+逐像素计数+diagnostics 标红并登记偏差；legacy mode=0 如需保留须在 SCI/DATA 登记偏差 ID。
- 置信度: 高
- related: L09-012；宪章 §6.1/§6.3

### L09-004 SCI-REJ 宣称 7 方法，实现暴露 10 方法且 median_sigma 无科学定义
- 类别: A_SCI_DEF
- 建议优先级: P1
- 位置: docs/science/REJECTION.md:20,40；lib/phase2/include/astro/phase2/rejection.h:45-57；lib/phase2/src/rejection.cpp:1061-1062,1603-1645；lib/phase2/src/stage2_common.cpp:225-226,347-352；docs/algorithms/PHASE2_REJECTION.md:25-26,323
- 证据摘录:
> | `method` | `None/Sigma/Winsorized/AveragedSigma/LinearFit/GeneralizedESD/RCR` | `P2RejectionMethod` |
（REJECTION.md §2 :20）；§5 :40 “逐像素候选栈经 planning 方法过滤，7 种方法 (SCI-REJ):”；而 rejection.h:45-57 枚举 10 方法+AUTO，PHASE2_REJECTION §1 自称“显式方法核（10 方法...）”，rejection.cpp:1061-1062 给出 median_sigma=(4.0,3.0,8)（SCI 全文无 median_sigma），stage2_common.cpp:225-226 生产 config 可直接选 median_sigma。
- 权威依据: 宪章 §1.1、§6.3、§12.3-3；SCI-REJ-001..008（FROZEN）
- 问题说明: 冻结 SCI 声明方法域 7 种；实现为 10 显式方法，MEDIAN_SIGMA 的语义/阈值/假设在 SCI 完全缺席（percentile/minmax 至少在 SCI §5 阈值锚点表内出现），PHASE2_REJECTION §7 :323 却声称“SCI §5 ...与本文档 §5 逐项同构”。config 面允许生产选择无 SCI 背书的方法，“科学定义=算法=接口=代码=测试”不闭合；ALG-REJ §2 又写“7 方法”、§3 switch 列 9 名——三份文档对方法计数互不一致。
- 影响: 以 method=median_sigma（或 percentile/minmax）配置的任何排异结果失去 SCI 背书；SCI §15 Acceptance 的“7 方法”验收面无法覆盖实现面。
- 建议处置: SCI 修订扩列方法全集（含各方法统计假设与阈值出处）或从 config 白名单剔除无 SCI 方法；两代 ALG 方法计数表述归一。
- 置信度: 高
- related: L09-005、L09-006

### L09-005 非有限 weights/support 的无效值语义：SCI/共享 ALG 与实现相反
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 位置: docs/science/REJECTION.md:34,87；docs/algorithms/REJECTION_ALGORITHMS.md:50；lib/phase2/src/rejection.cpp:1188,1192-1198,1710-1718；docs/algorithms/PHASE2_REJECTION.md:199-204；lib/phase2/tools/rejection_oracle_compare.py:250-262
- 证据摘录:
> - `support/weights` 有限性在资格层校验，非有限 ⇒ `INVALID_INPUT` hard fail。
（REJECTION.md §4 :34；§8 :87 同）
> if (!std::isfinite(v)) { ++out->invalid_finite; ok = false; }
（rejection.cpp:1188——gather 对非有限仅计数剔除、返回 rc=0；support 非有限 :1192-1197 同样剔除；p2_reject_stack_ex 的 INVALID_INPUT 判据 :1710-1718 仅覆盖 stack->values，全程不检查 weights）
- 权威依据: 宪章 §4.1（无效值语义必须明确）、§6.3；SCI-REJ-001 §4/§8；PHASE2_REJECTION §5 F2/§9（实现权威锚）
- 问题说明: 冻结 SCI 与共享 ALG 把“非有限 weights/support”定义为 INVALID_INPUT hard fail；实现是静默剔除（values/support 资格层）或不检查（weights——仅 RCR 核消费 weights，NaN/负权重进入 rcr 加权分支无防护，:1783-1796 直传）。PHASE2_REJECTION §7 :323 反而声称“SCI §5 ...逐项同构；阈值表逐值一致”；rejection_oracle_compare 的 edge_matrix 在 compat 路径（:1968-1974 has_nonfinite 覆盖）验证 INVALID_INPUT，恰掩盖生产 ex 路径的真实语义。
- 影响: NaN support/ivar 样本被静默丢出候选栈 → n_eff 与排异决策改变而不自知；与 SCI §10“不得使 INVALID_* 变为可继续集合”的表述相悖。
- 建议处置: 归一唯一语义（建议：eligibility 层非有限→计数+可判状态；kernel 拒绝带非有限权重的 RCR 输入），据 §1.2 修 SCI 措辞或改实现；补 ex 路径 INVALID 权重注入门。
- 置信度: 高
- related: L09-004、L09-009

### L09-006 rejection.h 公共头三处注释与冻结常数/默认值矛盾
- 类别: D_COMMENT
- 建议优先级: P1
- 位置: lib/phase2/include/astro/phase2/rejection.h:106-110,118,178；对照 lib/phase2/src/rejection.cpp:1057,1060,1039-1041
- 证据摘录:
> typedef struct {
>     double lower;          // 低侧因子（默认 4.0）
>     double upper;          // 高侧因子（默认 3.0）
>     int max_iterations;    // 默认 8
> } P2LinearFitParams;
（rejection.h:106-110；实现冻结 linear_fit=5.0/3.5：p.linear_fit.lower = 5.0; p.linear_fit.upper = 3.5;（rejection.cpp:1057），SCI §5:48 同）
> double low_fraction;   // 相对 median 低侧小数（默认 0.1 = 10%）
（:118；实现 :1060 为 0.2；已登记 DISP-P2REJ-001 未整改）
> const char* profile;         // “wbpp_current”（nullptr=默认）| “astrocs_adaptive”
（:178；实际 nullptr 解析为 wbpp_2_9_1（:1039），且该注释未列 wbpp_2_9_1）
- 权威依据: 宪章 §12.2（注释须与实现一致、承载正确语义/单位/默认值）；SCI-REJ §5 阈值冻结锚点
- 问题说明: linear_fit 默认阈注释 4.0/3.0 与冻结表 5.0/3.5 不符——公共头是接口复现方唯一阅读面，直接误导科学常数（未在 DISP 清单登记）；percentile 注释错误已登记未改；profile 注释与 canonical 名漂移（与 L09-014 的 SCI profile 混乱同源）。
- 影响: 不改变当前输出，但破坏“接口注释↔SCI↔实现”一致性；新 provider/绑定按注释写死即引入阈值偏差。
- 建议处置: 注释对齐实现默认值并把 :107-108、:178 并入 DISP-P2REJ 登记面整改。
- 置信度: 高
- related: L09-001、L09-014

### L09-007 PHASE2_INTEGRATION/PHASE2_SESSION 冻结文档与当前实现整体脱节
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 位置: docs/algorithms/PHASE2_INTEGRATION.md:4-6,64,170-181；docs/algorithms/PHASE2_SESSION.md:21-22,83,254；lib/phase2/src/integrate.cpp（81 行实测）；lib/phase2_session/p2_session.cpp（298 行实测，:245-261）
- 证据摘录:
> 模块: lib/phase2/src/integrate.cpp（76 行，astrocs_phase2 静态库成员...
（PHASE2_INTEGRATION.md:4；现文件 81 行）
> ├ sup_max 更新 | :54-55 | support 非空时 sup_max=max(sup_max,sup)——**位于 :49 continue 之后**（DISP-P2INT-001，§11.3）
（PHASE2_INTEGRATION.md:64；现状 sup_max 在 :49-50、continue 在 :56——缺陷已被 B2-A7 修复）
> run 成功  → status=“complete” + n_inputs/n_obs [+ artifacts]   :243-246
（PHASE2_SESSION.md §11.2 :254；对照 p2_session.cpp:252 s->manifest[“status”] = “partial”; 与 :253-261 availability 门，文档全篇无 partial/complete-gate 语义）
- 权威依据: 宪章 §12.3（文档—源码符号一致、状态字面量一致）；两文档自带的“锚=实测；本文件行号权威”声明
- 问题说明: B2-A7 修复与 session complete 门（P2-001 整改）落地后，两份 CONTRACT_READY ALG 未订正：PHASE2_INTEGRATION §3 锚表/§7 公式映射/§11.3 DISP 描述与代码相反（其 §11.4 F5 甚至规定“现状实现该门 FAIL”，现 PASS——p2_output_semantics_test.cpp:85-107）；PHASE2_SESSION §11.2 状态机与 run 返回值行号（282 行时点）整体漂移。
- 影响: 下游整改任务（P2-INT-IMPL/TEST、P2-SESSION-IMPL）按已消失缺陷/过期状态机设计回归；审计方以 DISP 清单判断“缺陷仍在”将得出相反结论。
- 建议处置: 按文档自设流程重跑逐符号锚定版，关闭已整改 DISP 条目，订正 §11.2 状态机（partial+availability）。
- 置信度: 高
- related: L09-002、L09-015

### L09-008 生产链“每节点执行”测试断言依赖不再被调用的 session 日志
- 类别: F_TEST_GAP
- 建议优先级: P1
- 位置: tests/backend/test_p2006_canonical_pipeline.py:88-92,113；tests/backend/test_p2007_joint_gate.py:123-131,225-229；lib/phase2_session/p2_session.cpp:116,148,177；cli/commands.cpp:1010；cli/runtime_client.cpp:173-205
- 证据摘录:
>         for frag in (“stage coverage ok”, “stage sample ok”):
>             self.assertIn(frag, self.res.stderr, ...)
（test_p2006:91-92 大意）
>         workers, cpus = _session_budget_workers(self.res.stderr)
>         self.assertIsNotNone(workers, “session 日志缺 budget workers 注入证据”)
（test_p2007:128-129）
（“stage coverage ok”/“session run: budget workers=” 全仓唯一实现出处 = p2_session.cpp:148/:116；而 cmd_phase2_run :1010 注释“RT-008: phase2 走 Runtime 单 phase IR 子图”，IR 由 runtime_client.cpp:176-205 生成 7 节点链，不经过 p2_session_run；节点链代码内 grep fprintf(stderr/cells= 无命中）
- 权威依据: 宪章 §12.3-7（Pipeline 静态图与实际 trace 一致）、§13.2-4（Phase 内 Pipeline 测试用真实模块）
- 问题说明: 两个 backend“生产链执行证据”测试以已不在该路径上的 p2_session 日志行作节点执行判据——按当前代码路径（节点链无此日志）断言应红或测试恒失败/被豁免；叠加其自述降级：“upm_fit→write 五节点由 Registry 注册 + final ok 承载”（p2006:89-90）、“observed_trace 逐节点 COMPLETED 断言无落盘载体”（:113）——即 7 节点链中 reject/integrate/write 等 5 节点无任何逐节点执行证据。宪章 §6.2“实际不同的算法执行符号”验收失去测试锚。
- 影响: P2-006/P2-007 的 VERIFIED 结论证据链断裂；节点级取消/预算/资源联动验证虚化。
- 建议处置: 以节点 manifest（p2_op_* 已在 man 写 operation/entry 标记）或 observed_trace 落盘为断言源重写两测试；补 IR trace 文件载体。
- 置信度: 中（静态证据完整，未见 CI 结果文件，先入 §6 复核）
- related: L09-012、L09-015

### L09-009 p2_rejection/p2_output_semantics 单元测试含空断言与名不副实节
- 类别: F_TEST_GAP
- 建议优先级: P1
- 位置: tests/unit/p2_rejection_test.cpp:60-72,74-83,85-90；tests/unit/p2_output_semantics_test.cpp:19-32,132-137
- 证据摘录:
>   // 6) integration 语义: mean/weighted mean/variance/support + frame identity
>   {
>     // frame identity 不丢失: 拒绝只作用于像素值, 不重编号 frame
>     // 语义验证: reason 输出与 frame_id 解耦 (kernel 不知 frame_id)
>     CHECK(true);
>   }
（p2_rejection_test.cpp:85-90）
>     // 1) 权重类型语义: ivar 是实际权重 (weight_mode=2); 名字不含模糊 “weight”
>     for (const char* name : {“signal”, “support”, “ivar”, “variance”}) {
>         CHECK(name[0] != ‘\0’);
（p2_output_semantics_test.cpp:23-25——对本地字面量数组判非空字符，不触及任何 writer/产品目录）
- 权威依据: 宪章 §12.3-4、§13.1；PHASE2_REJECTION §13 等把这类文件列为 TEST 证据
- 问题说明: p2_rejection_test §4 构造 20 样本 fixture 却从不送入任何 kernel（vals/rng 未使用），仅断言枚举常数值大小序；§5 对全零数组求和判 0；§6 与 p2_output_semantics §5（CHECK(true)）为占位；p2_output_semantics 标题宣称“writer 输出语义（mosaic/support/实际权重类型/UPM surface/diagnostics）”但全文未链接 writer。成功打印语（“P2-005/P2-006 TESTS PASS”）为追溯表引用面背书。
- 影响: 单元测试层“已验证”结论虚增；integration/frame identity/writer 名义覆盖实际不存在（真正有效的仅 §2/§3/4b/4c 的 reducer 面断言）。
- 建议处置: 删除或坐实空节（接入产品目录断言与 kernel 注入门）；追溯表只登记真断言面。
- 置信度: 高
- related: L09-010

### L09-010 test_p2004 “生产 Oracle” 实为 compat 弃用接口 + 伪独立断言
- 类别: F_TEST_GAP
- 建议优先级: P1
- 位置: tests/backend/test_p2004_reject_integrate.py:3-8,41-52,157-172；lib/phase2/include/astro/phase2/rejection.h:299-300；lib/phase2/src/rejection.cpp:1888-1889
- 证据摘录:
> “““test_p2004_reject_integrate.py — P2-004 (G5) 排异/积分生产 Oracle。
> C++ driver 调正式 kernel(p2_reject_stack / p2_reject_plan_resolve / p2_integrate_pixel)执行合成场景:
>   cosmic ray / hot pixel / streak / 星核 / 低样本 / NaN-Inf-zero-negative ivar;
>   ... integration 加权均值/variance/ivar/support/frame identity / all-rejected; ...
（:3-8；driver :41-52 实际走 P2SampleStackView+p2_reject_stack——compat 路径，rejection.h:299-300 冻结注释“兼容入口（仅测试/旧调用；生产 Stage2 不再调用）”；sv.frame_ids=nullptr（:43），“frame identity”无载体；“variance/ivar”在 integration 面根本无输出）
>     CHECK(p2_reject_stack(&sv, &res) == 0);
（:48）
- 权威依据: 宪章 §13.1、§12.3-4；PHASE2_REJECTION/PHASE2_INTEGRATION 把 test_p2004 列为生产 Oracle 证据
- 问题说明: ①名义“生产 Oracle”用 compat 接口且绕开生产 strided gather/source_indices 面，无法暴露生产路径缺陷（如 L09-005 的权重不检查）；②compat 内 frame_ids 映射为 fids[i]=in->frame_ids[i]（rejection.cpp:1888-1889，compact 下标直取原始数组，未经 eligibility 掩码映射）——一旦传入非空 frame_ids 且发生剔除即错绑；③Python 层 test_02..test_05 只是复读 driver 退出码/“P2-004 DRIVER PASS”字符串，无独立判定。
- 影响: TEST-P2-REJ/TEST-P2-INT 设计面引用该文件为“生产等价”证据失去效力；compat 错映射缺陷无暴露门。
- 建议处置: 迁到 ex+gather 生产签名；Python 侧解析结构化输出逐项断言；修 rejection.cpp:1888-1889 或给 compat 补显式映射与回归门。
- 置信度: 高
- related: L09-005、L09-009

### L09-011 接缝验收四项缺二，已有门槛无 SCI 出处
- 类别: A_SCI_DEF
- 建议优先级: P1
- 位置: ASTROCS_PROJECT_CONSTITUTION.md:194；tests/api/test_seam_metric_gate.py:10,23,54-60；docs/science/{REJECTION,INTEGRATION}.md、docs/algorithms/{PHASE2_MOSAIC_WRITE,PHASE2_SESSION}.md（全文无接缝节）
- 证据摘录:
> - 接缝验收同时包含重叠区背景差、法向梯度跳变、support/weight 连续性和固定显示参数下的人工图像审核。
（宪章 §6.3 :194）
>   - 门限预冻结: cross-frame seam p95 <= 3σ, max <= 5σ(σ=kNoiseRms=0.05)
（test_seam_metric_gate.py:10；SIGMA=0.05 注明“同步 synthetic_gate kNoiseRms” :23）
（全仓 grep “法向”仅命中宪章本文与 engineering/control/archive/.../09_WINDOWS_AND_FATDUCK_VALIDATION.md:35（archive 非活动规范）；“support/weight 连续性”在 docs/science、docs/algorithms、tests 均无定义/判据）
- 权威依据: 宪章 §6.3、§13.2-8（接缝必须有量化证据）、§13.4；SCI-REJ/SCI-INT（未登记接缝条款）
- 问题说明: 本 slice 权威文档对宪章强制的接缝四项只由测试侧覆盖“重叠区背景差”（cross-frame 差 p95/max）一项；“法向梯度跳变”“support/weight 连续性”两项在活动文档与测试中无定义、无阈值、无证据；“固定显示参数人工审核”在 docs 层无登记链接（发布侧以 NOT_VERIFIED 挂账）。且 3σ/5σ 门槛冻结于测试文件内、σ 取合成噪声常数——宪章 §4.1/§12.3 要求的容差出处（SCI 登记）缺失（类别 A“默认容差无出处”）。
- 影响: “Phase2 接缝有量化验收证据”结论不成立；真实 mosaic 的梯度跳变/支撑不连续缺陷无机器门拦截。
- 建议处置: 新增/扩列 SCI（建议 SCI-INT 或新 SCI-SEAM）登记四项判据的数学定义、单位与冻结容差出处；测试引用同一 SCI/SYN ID；人工审核面链接 §13.4 预览产物 ID。
- 置信度: 高
- related: L09-016（p2_seam_gate_test 自满足问题）

### L09-012 双份马赛克实现语义互斥，slice 权威文档整体指向已退出生产的 stage2
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 位置: docs/algorithms/PHASE2_MOSAIC_WRITE.md:9,15；docs/algorithms/PHASE2_REJECTION.md:390-417(§6)；docs/algorithms/PHASE2_INTEGRATION.md:136-148(§6)；lib/core/src/module_adapters.cpp:13-30,2878,2896,3116,3143-3146,3476；docs/contracts/PUBLIC_API.md:54
- 证据摘录:
> 实现源（唯一权威生产源）: lib/phase2/tools/stage2.cpp（1762 行实测；入口 main :112）。
（PHASE2_MOSAIC_WRITE.md:9）
> - `astrocs-stage2.exe <config.json>`（**V5 遗留, LEG-004 已退出生产**；当前生产入口 = `astrocs phase2 run` / `astrocs run --phases` preset）。
（docs/contracts/PUBLIC_API.md:54——而 phase2 run 走 module_adapters 节点链：p2_op_reject :2878 / p2_op_integrate :3116 / p2_op_write :3476；节点链产品含 variance/ivar（:23-25 头注、:3629-3642 产品计数校验）、禁 weight_mode=0 与 support fallback（:3143-3146），reject 默认 profile 为 “wbpp_current”（:2896，SCI canonical 为 wbpp_2_9_1）；stage2 反之仅 signal|support（:594，DISP-P2HIPS-001）且存在 tile 级 support fallback（L09-003））
- 权威依据: 宪章 §6.1（Phase2 输出含 variance/ivar、rejection 产品）、§8.3（禁各自复制 reader/writer）、§12.3-3；PHASE2_MOSAIC_WRITE §11.5
- 问题说明: 生产入口实际执行 module_adapters.cpp 内约 600 行 reject/integrate/write op，与本 slice 四份 ALG 锚定的 stage2.cpp 实现互为分叉（权重资格、NaN/零权产品语义、产品集、默认 profile）；节点链的权重构造/掩码传递/§30.1 方差合成消费面无 ALG 逐符号锚（散在代码注释；公式 SCI 侧在 UNCERTAINTY_AND_COVARIANCE.md:49-63 已登记）；stage2 侧缺 variance/ivar 违反宪章 §6.1 却仍被文档称“唯一权威生产源”。PHASE2_SESSION §7 的反断言测试与 4 段会话面都不覆盖真实生产链。
- 影响: 排异/积分/写出全链 ALG 权威与发布路径代码脱钩；同一 config 在两路径产出产品结构与数值不同；追溯矩阵按 stage2 锚登记的结论对生产路径失效。
- 建议处置: 指定唯一生产源（建议节点链），为 p2_op_reject/integrate/write 建 ALG 页（或扩入现有 PHASE2_* 文档）逐符号锚定；stage2 保留则全文档改口为 legacy 并登记宪章 §6.1 偏差处置。
- 置信度: 高
- related: L09-003、L09-008、L09-015

### L09-013 SCI §8“空栈→NO_CANDIDATES”与实现相悖且 ALG 自称覆盖 FROZEN SCI
- 类别: G_GOV_GATE
- 建议优先级: P1
- 位置: docs/science/REJECTION.md:89,133；docs/algorithms/REJECTION_ALGORITHMS.md:53；docs/algorithms/PHASE2_REJECTION.md:433-437,474,689-691
- 证据摘录:
> | 无候选 | `NO_CANDIDATES` | 同上 |
（REJECTION.md §8 :89；§9a :133 又写“单帧无排异（n=1 不进核）”——实现 n=1 进 plan/eligibility 层、走 UNDERDETERMINED :1739-1745）
> SCI FROZEN 禁改；语义权威=本文件 §4.1；澄清归 SCI 修订流程（不在本任务）。
（PHASE2_REJECTION §7 DISP-P2REJ-002 :436-437——而同文档 §11.5 :655-657 声明“两处冲突时以 docs/science/ 为准...（禁止反向）”）
- 权威依据: 宪章 §1.1（权威分层，不得选择方便的一层）
- 问题说明: 实现八态无 NO_CANDIDATES（空栈=MIN_SAMPLES），SCI 条文失实；ALG 一面承认 SCI 权威优先，一面宣布“语义权威=本文件”，同文档内权威自相矛盾并事实上以低层覆盖 FROZEN SCI。虽登记为 DISP-P2REJ-002，但“谁有权改语义”的治理冲突未解。
- 影响: SCI 验收文本不可判真；“ALG 覆盖 SCI”先例可能被复制。
- 建议处置: 负责人裁决走 §1.2 修订 SCI（NO_CANDIDATES→MIN_SAMPLES、n=1 措辞），删除 ALG 越权句。
- 置信度: 高
- related: L09-001、L09-014

### L09-014 SCI §4 profile 合法集与同文档 §5 及实现互斥
- 类别: A_SCI_DEF
- 建议优先级: P2
- 位置: docs/science/REJECTION.md:21,35,49-50；lib/phase2/src/rejection.cpp:1039-1046；docs/algorithms/PHASE2_REJECTION.md:187-192
- 证据摘录:
> - `profile` 仅 `wbpp_2_9_1`（及 `wbpp_current` alias）合法，否则 `INVALID_CONFIGURATION`。
（REJECTION.md §4 :35）
> astrocs_adaptive (tunable):
>     同阈但可配置 large_scale 等
（同文档 §5 :49-50；§2 符号表 :21 亦只列 wbpp_2_9_1/wbpp_current；实现 :1040-1041 白名单 {wbpp_2_9_1, wbpp_current, astrocs_adaptive}；非法 profile 返回 rc=1+err 文本，并非 P2_STATUS_INVALID_CONFIGURATION——INVALID_CONFIGURATION 专指 method×normalization 组合 :1730-1734）
- 权威依据: SCI-REJ-001 §4/§5；宪章 §1.1；PHASE2_REJECTION §4.3（rc/status 正交通道）
- 问题说明: 冻结 SCI 的 §2/§4 与 §5 内部对 profile 集自相矛盾；“非法⇒INVALID_CONFIGURATION”与实现错误通道不符；PHASE2_REJECTION §7 仍称逐项同构。
- 影响: SCI 有效域条款不可据以判真；配置校验测试（非法 profile rc≠0）与 SCI 字面语义互斥。
- 建议处置: SCI 修订统一 profile 集与错误通道（rc vs status），两代 ALG 同步。
- 置信度: 高
- related: L09-004、L09-006

### L09-015 整阶段 Session 以 astrocs.phase2.resample 假身份驻留模块注册表
- 类别: G_GOV_GATE
- 建议优先级: P2
- 位置: lib/core/src/module_adapters.cpp:484-495,5027-5033；docs/algorithms/PHASE2_SESSION.md:83-89,288
- 证据摘录:
>   auto d2 = phase2_descriptor();
>   auto r2 = registry.register_module(d2);
>   auto f2 = registry.register_factory(
>       d2.module_id, [d2]() { return make_session_module<P2Api>(d2); });
（module_adapters.cpp:5028-5033；phase2_descriptor() :486 d.module_id = “astrocs.phase2.resample”;，端口 calibrated→resampled 为虚构，sci/alg/test 挂 SCI-P2-RES-001/TEST-P2-RES-001 占位 ID——无 docs/science 权威页）
- 权威依据: 宪章 §8.4（每算法模块“单一模块 entrypoint，不隐藏整阶段 Session”）、§9（端口须引用有效 DATA 合同）、§14.3（禁 facade 冒充迁移）、§12.3-1/2
- 问题说明: 注册表中的 astrocs.phase2.resample 实为 4 段整阶段会话（现自报 status=partial、reject/integrate/write=unavailable，p2_session.cpp:252-261），且当前无任何 IR 节点引用该 module_id（cli/runtime_client.cpp phase2 链不含）——既违 §8.4 字面又无机器门拦截。文档侧 §4/§11.4-7 登记为“占位、对齐归 P2-XX-INT”，但它在生产二进制注册面常驻。
- 影响: 模块注册表/registry 一致性机器门可信度；外部工具枚举模块时会把会话当 resample 算法模块调度。
- 建议处置: 迁移完成前从注册表移除或降格为非可调度诊断模块；恢复时以 astrocs.p2.session 真名+有效端口注册。
- 置信度: 高
- related: L09-007、L09-012

### L09-016 注释/追溯卫生违例簇（rejection.cpp 流水、SCIENCE_SCOPE 任务语、seam_gate_test 自满足、粘连错误串）
- 类别: D_COMMENT
- 建议优先级: P2
- 位置: lib/phase2/src/rejection.cpp:3-7,1042-1044；docs/science/SCIENCE_SCOPE.md:61-62；tests/unit/p2_seam_gate_test.cpp:24-59
- 证据摘录:
> // W7（ 34A532A2...B2EB308 + wiki Phase2_Rejection）：
> // - 首版实现：None、Sigma、WinsorizedSigma（确定性，Oracle 对照）；
> // - AveragedSigma/LinearFit/ESD/RCR 接口冻结，后续子任务按论文/Oracle 独立实现；
（rejection.cpp:3-7——任务号+残断哈希堆积，且“首版实现/后续子任务”与现文件已实现 10 方法的现实矛盾）
> SCI-SCOPE-001（本文件范围/假设/失效域入口）；... # B5-06 同步在 TRACEABILITY.csv 增 SCI-SCOPE-001 行（doc=SCIENCE_SCOPE.md, status=VERIFIED）但本任务不改csv，仅文档。
（SCIENCE_SCOPE.md:61-62——任务流水与未经核对的 status=VERIFIED 混入活动科学文档）
> “p2_reject_plan_resolve: profile 仅支持 wbpp_2_9_1(wbpp_currentastrocs_adaptive”
（rejection.cpp:1043-1044——拼接缺分隔符，输出粘连词）
>   // 数值门: 合成 seam 校准前后对比 (复用 P2-003 三块重叠语义)
>   const double C_A = 0.0, C_B = -8.0;
>   CHECK(std::fabs(before - 8.0) < 1.5);   // 校准前显著 seam
>   CHECK(std::fabs(after) < 0.5);          // 数值门: 校准后 seam 消失
（p2_seam_gate_test.cpp:24,54-59——偏移 +5/-3 由测试自己构造、C_B=-8 由测试自己设定，再“验证”差为 8→0；全程不调用任何生产 UPM/rejection/writer 符号，注释却称“复用 P2-003 三块重叠语义”；§2/§3 的资源门只是 evaluate_gate 纯函数单测，与 Phase2 接缝无涉）
- 权威依据: 宪章 §12.2（禁任务编号/审计流水堆积、注释承载正确语义）、§13.1、§19
- 问题说明: 多处注释/测试叙述与事实矛盾或堆积过程语；p2_seam_gate_test 属“以自满足算术冒充生产数值门”的代表（与其标题“synthetic seam 数值门”名不副实），会误导追溯表把 seam 数值验证记为已覆盖。
- 影响: 不改变数值；污染审计与复现。
- 建议处置: 按 §12.2 清理注释；SCIENCE_SCOPE 尾部过程语移入追溯台账；p2_seam_gate_test 改为消费 production apply 产物（对齐 test_p2003 口径）或删除该名义；修错误串。
- 置信度: 高
- related: L09-011、L09-009

---

## 覆盖声明（重点条目核对结论）

- 重点①（σ 阈/多帧稳健统计/自动策略版本化、临场启发式）：阈值 4.0/3.0/8、5.0/3.5/8、ESD 0.05/10、percentile 0.2/0.1、minmax 1/1/4、large_scale 8/2/2、underdetermined 2 经 SCI §5 ↔ ALG F1 ↔ rejection.cpp:1048-1069 ↔ V15AutoPlanResolvesByNominal 四方逐项一致；ESD λ/p/ν 公式经与 NIST e-Handbook §1.3.5.17.3 原文逐符号核对字面一致（λ=(n−i)t/sqrt((n−i−1+t²)(n−i+1))、p=1−α/(2(n−i+1))，非 finding）；1.4826、√(π/2)、1.134/1.5/5e-4/64 等核内常数在 ALG F4-F8 有登记。违例集中于 L09-001（N≤4 容错）、L09-004/005。
- 重点②（rejection mask/count 并入 coverage/support）：未见问题——rejected 样本在 integrate.cpp:34-36 于计数/sup_max 之前 continue 排除；stage2 valid/area 由 reducer 输出生成；节点链 nrej 面独立成 artifact（module_adapters.cpp:3051-3063,3225），§30.2 恒等式以逐样本掩码 fail-closed（:3419-3430）。依据文件：integrate.cpp:33-62、stage2.cpp:1440-1541、module_adapters.cpp:2920-3063。
- 重点③（权重/归一/缺失/空集/误差传播、support 误用）：见 L09-002/003/005/012；归一化 signal=Σwx/Σw 与实现一致（integrate.cpp:75）；空集/全拒/零权三态齐备且有门（synthetic_gate V17StatusesExplicit、p2_output_semantics 4b/4c）；马赛克方差合成公式权威在 UNCERTAINTY_AND_COVARIANCE.md:49-63（ivar_mosaic=Σivar、variance=1/W），与节点链实现（module_adapters.cpp:3397-3416,3480-3483）一致；stage2 的 tile 级 support fallback 违宪已列 L09-003。
- 重点④（排异-积分自我实现偏置）：排异判定域=逐像素 MEDIAN_CENTER 工作域（rejection.cpp:1749-1768），与 UPM 加性背景模型（control 级，L08 域）解耦；未见“同一背景估计既判异又积分且未文档化”路径。依据：SCI-REJ §3a/9a、rejection.cpp:1756-1768、INTEGRATION.md §6。
- 重点⑤：见 L09-011、L09-016 与 §6（人工审核证据挂账）。
- 重点⑥（节点独立执行）：7 节点链工厂绑定唯一真实 op（module_adapters.cpp:5065-5087；头注 :13-14 记录“原工厂委托 P2Api session adapter=子节点调用完整 p2_session_run 违规”已整改）；p2_ir_facade_test 仍只测 4 段旧面（其“canonical IR 全链”标题名不副实——断言的是 4 节点，且为源码文本 grep 而非运行时验证，并入 L09-008 处置）。未见新违规，但执行证据断裂如 L09-008。
- 重点⑦（PHASE2_SESSION/MOSAIC_WRITE 符号一致性与注释质量）：见 L09-007/012/015/016；PHASE2_MOSAIC_WRITE 的 §12 锚漂移自记表（其自述 8 处 ±1-8 行漂移）在并发下继续扩大（如 §5 的 :288 引用现注释在 :1368）。

## §6 待复核清单

- test_p2006:91/test_p2007:128,227 断言的 p2_session 日志在当前节点链下是否真红（还是被 skip/waiver） | tests/backend/test_p2006_canonical_pipeline.py、test_p2007_joint_gate.py | 静态证据指向必红，但本代理禁执行、未见 CI 工件 | 运行两测试并核对 ci/known_failures.json 挂账
- lib/astro_image_io/astro_image_io.dll（Windows PE）被 Linux g++ 当链接输入（test_reject_parallel.py:87、test_reject_integration_oracle.py:87、test_seam_metric_gate.py:37） | 上述三文件 | 链接成败/是否恒 skip 需实测；skipUnless 仅检查 g++ 与 build/linux-openmp-on/libphase2.a（本地构建产物路径） | 在 Linux 节点执行并查 skip 原因；这些 API 测试硬编码本地构建目录，CI 上很可能从未运行（F_TEST_GAP 潜在升级）
- “NIST ESD 120/120”（SCI §11 :106、ALG-REJ §8 :74）无当前可复现载体 | docs/science/REJECTION.md:106；rejection_oracle_compare.py 仅跑 Rosner 54 值单数据集+1 masking 例（:193-217）；120/120 只见于 engineering/control/archive 的 EXECUTION_LOG（archive 非证据面）与 docs/archive 历史日志 | 数字声称与现存工件不符，且 SCI §15 以 §11 全过为验收条件 | 负责人补 evidence 或按现状（单数据集 oracle）改写 SCI §11 措辞
- WBPP 2.9.1 bestRejectionMethod 路由阈值（n<6/6≤n≤15/>15）与 percentile 0.2/0.1 的原始源码比对 | REJECTION.md §14.3、rejection_oracle_compare.py:220-237（auto_vs_wbpp_policy 以硬编码期望表自校验，非外部源比对） | 本机安装源不在仓库，本次外部检索未获原始脚本 | 将 WBPP 2.9.1 源码摘录（引用定位）录入 evidence/docs，或把“采纳自 WBPP”声称降级为“项目自定阈值、命名沿用”
- “synthetic_gate 74/74”（SCI §13、ALG §10）与现文件 91 个 TEST 计数 | docs/science/REJECTION.md:122 | 计数为历史快照且无可定位工件 | 以当前 SHA 运行取计数并订正文档或补 evidence 指针
- stage2 large_scale 两遍缓冲索引一致性：一遍 buf 按 fid_stack（frame_seq 值）索引（:1472-1476），二遍按 frames[s] 索引（:1550-1553） | lib/phase2/tools/stage2.cpp:1468-1596 | 需确认 frame_seq[s]==frames[s] 恒成立（构造区 :640-860 未展开读），否则 grow/积分错位 | 通读 stage2 :640-860 或以双顺序对照实验验证
- DISP-P2SES-002（nlohmann 类型异常穿越 extern C）是否已被现行 try/catch 完全消除 | lib/phase2_session/p2_session.cpp:103-112（parse 有 try；:112 之后 doc[...] 访问仍裸，依赖前置 validate 的键形约束） | 静态审查不能穷尽调用序 | 构造“validate 通过但 run 内类型异常”最小用例（需执行）
- V15SatelliteTrail20Frames 是否字面支撑 SCI §11 “结构注入 recall=1.0” | synthetic_gate.cpp:4259-（ALG 摘要口径为 accepted≥15、false-reject≤4） | 未读断言体细节，无法确认 recall 计算口径 | 读测试体并按注入帧数核算 recall

## §7 现场冲突备注

- module_adapters.cpp、stage2.cpp、p2_session.cpp 行数在本代理读取期间即与各文档“实测”值漂移（并发 agent 作业中），本文全部行锚以读取时点为准；run/**、engineering/**、evidence/**、reports/** 等并发现场一律未作为问题登记。
- lib/phase2/README.md 已被 P2-COV 合同占用（一目录一套三件套），rejection/integrate 的 README 落位 lib/phase2_rej、lib/phase2_int——该“目录合同占用”是文档侧权宜，不计入 finding。
