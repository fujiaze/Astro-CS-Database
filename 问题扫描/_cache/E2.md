# 执行层缓存 E2 — 构建拓扑与门有效性（实测）

- 代理码: E2（第四态落地层，负责人已授权 B-14）
- 模式: 只读实测（git 不改索引查询 / nm / objdump / readelf / strings / ctest -N / 读既有 CMakeCache 与 CTestTestfile / find / grep / wc / python3 纯文本分析与本地重算）
- 工作目录: run/审计执行层/E2/
- 覆盖任务:
  - 任务 1: CI 第二二进制终判（M5b-G-01，P0）
  - 任务 2: 构建孤儿三处（L22-005 / M4-F-01 / M2a-F-1）
  - 任务 3: B-06 增量选择集
  - 任务 4: CPU 门禁分母（M5a-G-002，P0）
  - 任务 5: B-04 packaging 产物是否暴露 p1_session_run
- 未覆盖(原因): 待补
- 判定基线: 宪章 §3.1 / §8.1 / §10.5 / §12.3 / §14.5 / §15.3 / §17.6；10_PROTOCOL.md §8 E 层边界
- 纪律声明: 每条结论附「命令 + 输出摘录（原样几行）」；无证据即写「未获证据」；与静态推演不符者标「改判」；不新增总述条目、不改级别定义；对既有 findings 只做追加式修订（末尾加一行 `- **【E2-实测 2026-09】** …`）
- finding 数: 0（E 层不新增 finding，只做四态落地）

## 任务 1 · CI 第二二进制终判（M5b-G-01，P0）

### 实测 1.1 对象存在性与元数据 —— 证实
```
$ ls -l build/cli/astrocs
-rwxr-xr-x 1 dsh dsh 7178472  9月14日 05:07 build/cli/astrocs
$ stat build/cli/astrocs
  大小：7178472   ...  修改时间：2026-09-14 05:07:55.082782976 +0800
$ file build/cli/astrocs
build/cli/astrocs: ELF 64-bit LSB pie executable, x86-64, ... BuildID[sha1]=83142ab2aadfcbac00b781264954d9a92fac6a97, for GNU/Linux 3.2.0, not stripped
```

### 实测 1.2 归属判定：是 cmake -S cli 兼容 shim，非根图产物 —— 证实
```
$ grep -E "^(CMAKE_HOME_DIRECTORY|CMAKE_PROJECT_NAME|CMAKE_GENERATOR|CMAKE_BUILD_TYPE)" build/cli/CMakeCache.txt
CMAKE_BUILD_TYPE:STRING=Release
CMAKE_PROJECT_NAME:STATIC=astrocs_cli
CMAKE_GENERATOR:INTERNAL=Unix Makefiles
CMAKE_HOME_DIRECTORY:INTERNAL=/workspace/Astro CS Database/cli

$ grep -E "^(CMAKE_HOME_DIRECTORY|CMAKE_PROJECT_NAME|CMAKE_GENERATOR)" build/CMakeCache.txt
CMAKE_PROJECT_NAME:STATIC=astrocs
CMAKE_GENERATOR:INTERNAL=Ninja
CMAKE_HOME_DIRECTORY:INTERNAL=/workspace/Astro CS Database
```
即：build/cli/ 是**以 cli/ 为源目录、独立 project(astrocs_cli) 的第二棵 configure**；根图是 project(astrocs)+Ninja。
```
$ readelf -n ... | grep "Build ID"
-- build/astrocs               Build ID: 96339b419159080a3e7190e62b8201830b8b7ace  size=6695984
-- build/cli/astrocs           Build ID: 83142ab2aadfcbac00b781264954d9a92fac6a97  size=7178472
-- build/linux-control/astrocs Build ID: 5e87fdedeb3c30748b653d890d1f5a6ebfd0971b  size=6687440
$ md5sum build/astrocs build/linux-control/astrocs build/cli/astrocs
7515489e11935ac6bacd5a07a73d1c28  build/astrocs
a5f7bba738dc19a5d25a3c823d6844b8  build/linux-control/astrocs
9e3c1a700fa2cecdc9db664437053cc9  build/cli/astrocs
```
三个 BuildID/MD5 互不相同 ⇒ 同名的 astrocs 确为**不同二进制**。

### 实测 1.3 源码集差异（M5b-G-01 ③）—— 逐字证实
```
$ grep -n "aio_pipeline" CMakeLists.txt | head
328:  lib/astro_image_io/src/aio_pipeline.cpp
$ grep -n "aio_pipeline" cli/CMakeLists.txt | head
164:  .../src/aio_pipeline.cpp .../src/aio_pipeline_engine.cpp)

$ nm --defined-only build/astrocs          | grep -c "aio_pipeline_engine"  -> 0
$ nm --defined-only build/cli/astrocs      | grep -c "aio_pipeline_engine"  -> 16
$ nm --defined-only build/libastrocs_aio.a | grep -c "aio_pipeline_engine"  -> 0
```
⇒ ARCH-001 §7 判为违例的 aio_pipeline_engine.cpp **只存在于兼容树**，根图（含其归档库）编译单元层面为 0。
符号集对称差（过滤 C.NNNN/CSWTCH 噪声后）：only_root=95 / only_cli=385 / common=4944。两图 `nm -D --defined-only` 动态导出面 diff 为空（各 31 条，DYN_EXPORTS_IDENTICAL），差异全在 exe 静态符号表内。

### 实测 1.4 交付面对象 = 根图 astrocs —— 证实
```
$ grep -rn "install(" CMakeLists.txt cmake/ packaging/ | grep -v uninstall
cmake/install_layout.cmake:34:# 白名单原则: 本文件是唯一 install() 集合; 其余文件绝不 install。
cmake/install_layout.cmake:58:install(TARGETS astrocs
$ sed -n '58,59p' cmake/install_layout.cmake
install(TARGETS astrocs
  RUNTIME DESTINATION . COMPONENT astrocs_runtime)
```
+ cli/CMakeLists.txt:1-8 自述「COMPATIBILITY 声明 (BLD-002, 非产品事实源)…产品二进制一律由根 CMakeLists.txt 产出」；既有 install 记录 build/install_manifest.txt 首行 = /tmp/cli008_*/prefix/./astrocs（由 test_cli_single_install 对根 build/ 跑 cmake --install 产生）。
⇒ **被 CI 的 CLI 门验收的 build/cli/astrocs 不是随包交付的那个；交付物 = 根图 target astrocs。**

### 实测 1.5 tests/cli 路径字面量是否「恒指」build/cli/astrocs —— 改判（精确化）
```
$ grep -rnE "build., .cli.|build/cli|build/astrocs" tests/cli/*.py
BUILD = os.path.join(REPO, "build", "cli")  ->  test_bench_cli.py:7, test_cli001_vpi.py:35,
    test_cli003_semantics.py:26, test_cli_protocol.py:7, test_cli_v7_surface.py:38,
    test_iso_acr_gpu_isolation.py:9, test_monitor_events.py:8        （共 7 个，EXE=BUILD/astrocs）
EXE = os.path.join(REPO, "build", "cli", "astrocs")  ->  test_cli004_process_protocol.py:17,
    test_phase2_inprocess.py:6, test_phase3_inprocess.py:6           （共 3 个）
EXE = os.environ.get("ASTROCS_CLI_BIN", os.path.join(REPO, "build", "cli", "astrocs"))
    ->  test_phase1_inprocess.py:6, test_phase123_pipeline.py:22      （共 2 个，默认仍为兼容树）
例外：test_p1003_drizzle_path.py:14 = build/astrocs（根图）
      test_cli_single_install.py:24 cls.bdir = build/（根图 install）
      test_cli_build.py:38 自行 cmake -S cli -B <tmpdir>（现场建兼容树，非 build/astrocs）
```
⇒ 精确表述：**12 个模块的 EXE 指向兼容树 build/cli/astrocs（含 test_cli_build 现场自建同图），仅 2 个指向根图产物**。不是「恒指」，但主面（命令树/help/退出码/phaseN in-process）确全落在兼容树上。
CI 接线：UT-CLI（ci/checks.json，profiles=[linux-main]，waivable=false，command=unittest discover -s tests/cli）之前由 LINUX-MAIN-BUILD-TREE → ci/wf_step.py --step LINUX-BUILD-ROOT-GRAPH 执行 ci/steps/linux_build_root_graph.sh:22-24 的 cmake -S cli + --target astrocs；同文件 :25 才 cp -f build/linux-control/astrocs build/astrocs。

### 实测 1.6 fail-open（M5b-G-01 ②）—— 证实
```
$ sed -n '132,142p' tools/check_api_docs.py
        exe = os.path.join(self.repo, "build", "cli", "astrocs")
        if os.path.isfile(exe):
            ...
            missing = sorted(c for c in doc_cmds if c not in help_cmds)
            if missing:
                self.fail("命令树 doc 有但 CLI --help 缺(或文本差): %s" % "; ".join(missing[:6]))
```
if 后确无 else ⇒ exe 缺失时零检查、零告警（API-DOCS 挂 fast/linux-main/windows-main 三 profile 且 waivable=false）。

### 实测 1.7 【E2 新事实，静态推演未覆盖】两图版本串格式互不相同，golden 断言在交付物上必红
```
$ cat build/cli/version_generated.h
#define ASTROCS_VERSION_STRING "0.11.0-alpha.2+g48ceee59fe52.dirty"
$ cat build/version_generated.h
#define ASTROCS_VERSION_STRING "0.11.0-alpha.2+g48ceee59fe52e0881fc4b925846fdf37800810d2"
```
成因（源码）：cli/CMakeLists.txt:38 取 `git rev-parse --short=12` 并在 :44-52 追加 `.dirty`；根 CMakeLists.txt:58 为 `VERSION+g<40位 HEAD>` 且无 dirty 位。
本地重算（python3，未执行任何二进制）复刻 tests/cli/test_cli_build.py:55-56 的正向断言：
```
VERSION 文件 = '0.11.0-alpha.2'
cli 图 version 串 = 0.11.0-alpha.2+g48ceee59fe52.dirty
  --version 文本断言 :55-56 => PASS
根图 version 串 = 0.11.0-alpha.2+g48ceee59fe52e0881fc4b925846fdf37800810d2
  --version 文本断言 :55-56 => FAIL
```
⇒ 该门**不是「恰好没验交付物」，而是「一旦改验交付物即 FAIL」**——错位被 golden 正则锁死。（:66 的 json 前缀式两图皆 PASS，故只有全串文本式暴露该锁。）

### 回答负责人那句「被验收的那个二进制，是不是随包交付的那个」
**不是。** 交付面只有根图 `install(TARGETS astrocs)`（cmake/install_layout.cmake:58，唯一 install 集合）；CI 的 CLI 门（API-DOCS + UT-CLI 的 12/14 模块 + test_cli_build 自建）全部打在 `cmake -S cli` 的 astrocs_cli 兼容产物上。两者 BuildID / MD5 / 大小 / 静态符号集（only_root=95、only_cli=385）/ 版本串格式五重不同；且兼容树独家编入 ARCH-001 §7 判为违例的 aio_pipeline_engine.cpp（根图 0 符号、兼容树 16 符号）。M5b-G-01 三点静态推演 ①②③ **全部证实**，另补 §1.7 新事实（改验交付物即 FAIL，属**加重**）。

---

## 任务 2 · 构建孤儿三处（L22-005 / M4-F-01 / M2a-F-1）

**分档原则（按要求把两件事分开）**：A = 根本不在图里；B = 在图里但从没被 CI 采集；C = 在图里且被 CI 采集。

### 实测 2.1 七个孤儿 TU 的存在性与登记面 —— 全部落在 **A 档：根本不在图里**
```
$ ls -la lib/healpix_db/healpix_drizzle/tests/*.cpp
-rw-r--r-- 11364 candidate_oracle_test.cpp   -rw-r--r-- 13237 variance_propagation_test.cpp
-rw-r--r-- 22627 drizzle_freeze_test.cpp     -rw------- 9339 drizzle_nonfinite_test.cpp
-rw-r--r-- 8581  control_median_mc_test.cpp  -rw-r--r-- 6295 kcorr_matrix_test.cpp
-rw-r--r-- 4864  concurrency_cache_test.cpp
$ git ls-files lib/healpix_db/healpix_drizzle/tests/ | wc -l
42        （七个文件均在 tracked 列表内）
```
```
$ for n in candidate_oracle_test ... concurrency_cache_test; do grep -rn "$n" --include=CMakeLists.txt . ; done
candidate_oracle_test        CMake命中=0 非注释命中=0
variance_propagation_test    CMake命中=0 非注释命中=0
drizzle_freeze_test          CMake命中=0 非注释命中=0
drizzle_nonfinite_test       CMake命中=2 非注释命中=0      ← 两处均为禁令式注释，见下
control_median_mc_test       CMake命中=0 非注释命中=0
kcorr_matrix_test            CMake命中=0 非注释命中=0
concurrency_cache_test       CMake命中=0 非注释命中=0

$ grep -rn "drizzle_nonfinite" --include=CMakeLists.txt . | grep -v "^./run\|^./build"
./tests/unit/CMakeLists.txt:476:# 域基线 09ee5363 (NaN/Inf 传播), 与 drizzle_nonfinite_test 互补不重复。
./lib/healpix_db/healpix_drizzle/tests/p1drz/CMakeLists.txt:31:# 与 drizzle_nonfinite_test (负面值合同) 互补不重复。
```
```
$ grep -rn "file(GLOB" --include=CMakeLists.txt . | grep -vE "^\./(run|build|out)/"
（无输出 ⇒ 真源 CMakeLists 内 file(GLOB) 命中 0，不存在「被 GLOB 扫进去」的可能）
$ grep -rn "add_subdirectory" tests/unit/CMakeLists.txt | grep -iE "drizzle|p1drz|healpix"
478:add_subdirectory(../../lib/healpix_db/healpix_drizzle/tests/p1drz p1drz)   ← 唯一被登记的 drizzle 测试目录
```
产物与运行面（既有 build 树 + ctest -N）：
```
$ find build -type f \( -name "candidate_oracle_test*" -o ... -o -name "concurrency_cache_test*" \)
（无输出 ⇒ 任何既有构建树下均无这七个 target 的产物）
$ grep -rl "<七名>" build/ run/ci/ --include="*CTestTestfile*" --include="*_tests.cmake" --include="*_include.cmake"
每个名字 build注册命中=0
$ ctest --test-dir run/ci/build-gcc-release -N | grep -icE "candidate_oracle|variance_propagation|drizzle_freeze|drizzle_nonfinite|control_median_mc|kcorr_matrix|concurrency_cache"
0
$ grep -rn "<七名>" ci/ .github/
（空=零命中 ⇒ 无任何旁路执行）
```
唯一「构建方式」仍是手敲注释（原文逐字）：
```
$ head -17 lib/healpix_db/healpix_drizzle/tests/concurrency_cache_test.cpp
// 编译（tests/）：
// g++ -O2 -std=c++17 -fopenmp -I.. -I..\..\..\astro_image_io\include
// -o concurrency_cache_test.exe concurrency_cache_test.cpp
```
⇒ **B4-22 回归守护（concurrency_cache_test.cpp）与 k_corr=1.4 的 MC 证据（control_median_mc_test.cpp / kcorr_matrix_test.cpp）、五个 drizzle Oracle，三处全部为「根本不在图里」**，与被测修复的关系实测在位：
```
$ grep -n "atomic\|B4-22" lib/healpix_db/healpix_drizzle/drizzle_engine.cpp
1661:    // 在同进程并发 run 下是 C++ data race（UB）——改 atomic fetch_add。
1662:    static std::atomic<std::uint64_t> s_target_cache_gen{0}; // B4-22 atomic run-gen
1663:    rctx.target_cache_run_gen = s_target_cache_gen.fetch_add(1) + 1;
```

### 实测 2.2 【改判】M4-F-01 (a) 类：lib/phase2 六门**已进根图且已被 CI 采集**
静态定稿原话（findings/F_TEST_GAP/p0/M4_L08_L09.md::M4-F-01 问题说明 (a)）：
> 而根构建 add_subdirectory 全集实测**不含 lib/phase2**（仅 modules/conformance/noop、lib/gaia_xpsd_client、lib/drizzle、lib/calibration、lib/cosmetic、lib/hips、tests/unit），该文件整体在拓扑外；即便按需构建，链接路径写死 Windows 制品 astro_image_io.dll（Linux 产品树不可能构建）。

实测（当前 HEAD）：
```
$ grep -n "add_subdirectory" CMakeLists.txt
673:  add_subdirectory(tests/unit)
680:  add_subdirectory(lib/phase2)            ← 在 if(ASTROCS_BUILD_TESTS) 内，:671

$ file build/lib/phase2/phase2_synthetic_gate
ELF 64-bit LSB pie executable, x86-64, ... BuildID[sha1]=44cacae3bdf60db2e5c0383b4dd0d778ebde986d   size=2217992  mtime 2026-09-14 09:41
$ file lib/astro_image_io/astro_image_io.dll
lib/astro_image_io/astro_image_io.dll: ELF 64-bit LSB shared object, x86-64, ...        ← 名为 .dll 实为 Linux .so，Linux 下可链
```
CI 采集面（既有证据，未新跑）：
```
$ python3（解析 run/ci/build-gcc-release/ctest-full.junit.xml，timestamp="2026-09-14T05:41:09"）
junit testcase 总数= 288  phase2_* 数= 113     phase2_* 状态分布: Counter({'run': 102, 'notrun': 11})
   run phase2_synthetic_gate.Phase2Upm.S0IdentityCalibrationNoChange
   run phase2_synthetic_gate.Phase2Upm.S1KnownAdditiveFieldRecovered  ...
$ ctest --test-dir run/ci/build-gcc-release -N
Total: 288   phase2_*: 113   p1drz_*: 6   p2_upm*: 1   drizzle 孤儿七名: 0
```
入图时点（git 历史，不改索引查询）：
```
$ git --no-optional-locks log -S "add_subdirectory(lib/phase2)" --format="%h %ad %s" --date=short -- CMakeLists.txt
c21eec50 2026-09-13 fix(CI/B3): Batch 3 真启用（Windows GSL/接缝门/Phase2 门进 CI/并发归因/known-failures/FD-01）
```
⇒ **改判：M4-F-01 的 (a) 类事实（UPMW 硬门/持久化门「不挂产品构建」）在 HEAD=adfdda7e 已被修复**，且不是「在图未被采集」——113 个 phase2_* 用例在 CI 注册命令 CTEST-LINUX-FULL 的产物 junit 里逐条有名有状态。**残余（仍成立）**为 (b) 类：control_median_mc_test / kcorr_matrix_test 等仍零注册（见 2.1），以及 SCI §15「Oracle 全过」引用的 MC 支柱仍不可复算。
注意其性质：这属 **C 档（在图 + 被采集）**，与 A 档七名同被 M8-F-013/M4-F-01 并列为「构建孤儿」，实为两件事——定稿文本未区分，实测必须区分。

### 实测 2.3 B 档（在图但从未被 CI 采集）本轮**未获证据存在**
```
$ python3（读 run/ci/ctest-registration/ctest_registration.json，generated_utc=2026-09-14T01:49:03Z）
targets_total=182  sources_total=15  registered_explicit len=22  registered_baseline_only_count=160
unregistered len=0  stale_baseline len=0  error_count=0  verdict=PASS
```
⇒ 在 add_test 层面 CTE 注册门当前**闭合**（unregistered=0），本轮在七个孤儿名与 phase2 六门上都未找到「在图但从未被 CI 采集」的实例。
但实测到一个**新的第四形态（登记为线索，不新增 finding）**：`phase2_*` 六门是 `gtest_discover_tests` 注册的，而 check_ctest_registration 的 targets_total=182 只统计源码里的 `add_test(NAME ...)`；`grep -c "phase2_" ci/ctest_baseline.json` = **0**、`checks.json` 里 `ctest_targets` 含 phase2 的项 = **0**。⇒ 这 113 个用例的「被采集」只靠 CTEST-LINUX-FULL 的全量 ctest 兜住，任何按目标名做采集/基线比对的机器门都看不见它们。此点归入 §待复核，交前台决定是否与 C-02 三向门并案。

### 2.4 ctest -N 的执行边界与一次副作用自报（纪律透明）
本轮确实用了 `ctest -N`（协议 §8 授权），但仅对 `run/ci/build-gcc-release` 这一棵树跑，依据是逐文件核过它不会触发的重发现：
```
$ python3（比较 *_tests.cmake 与 exe / *_include.cmake 的 mtime）
run/ci/build-gcc-release: include_cmake=6 需重跑发现的=0
build/linux-control: include_cmake=6 需重跑发现的=6   ← 全部 no_tests_file，ctest -N 会启动 gate 可执行并重写文件，故**未在该树跑**
```
（build/lib/phase2 的 _tests.cmake 09:41:45 比 exe 09:41:34 与 include 05:00:13 均新 ⇒ 条件为假，理论上亦安全；但为最小化写盘，统一只用了 CI 树。）
**副作用自报**：`ctest -N` 会更新 CTest 自身的日志文件，实测：
```
$ find run/ci/build-gcc-release -type f -newermt "2026-09-14 10:00" -printf "%p %TH:%TM:%TS\n"
run/ci/build-gcc-release/Testing/Temporary/LastTest.log 10:49:43 / 10:50:05
```
即：该目录（run/**，gitignore，免报区）内既有的 LastTest.log 时间戳被我更新（内容是 -N 的清单，不是任何测试执行）。未新增/删除任何文件，未写项目根目录，未运行任何测试用例或 CLI/业务二进制。

---

## 任务 3 · B-06 增量选择集（ci/run.py）

### 实测 3.1 有无真正不写盘的干跑 —— **有：`--plan-only`，且已真跑一次**
源码判定：
```
ci/run.py:1164    parser.add_argument("--plan-only", action="store_true",
ci/run.py:1165                        help="只打印选中检查清单 JSON，不执行（hosted 预检/windows profile 验证）")
ci/run.py:1213-1240   if args.plan_only: plan = {...}; print(json.dumps(plan,...)); return EXIT_OK
ci/run.py:1242-1250   ← 出 root.mkdir / logs / checks 三个写盘动作全在 return 之后
```
真跑（输出重定向到本层目录，未落项目根）：
```
$ python3 ci/run.py --plan-only --profile linux-main > run/审计执行层/E2/plan_only_linux_main.json 2> run/审计执行层/E2/plan_only_linux_main.err
exit=0
44694 run/审计执行层/E2/plan_only_linux_main.json
{
  "schema_version": 1,
  "mode": "plan-only",
  "profile": "linux-main",
  "source_sha": "db2c02f326f7139d449b06ce0b3e825526e4f306",
  "platform_runtime": "linux",
  "changed_from": null,
  "focus": null,
  "selection_mode": "profile",
  "selected_count": 104,
```
（产物已留在 run/审计执行层/E2/plan_only_linux_main.json）

### 实测 3.2 但 `--changed-from` **不能**用真 CLI 跑 —— 边界自报与替代方案
```
$ grep -c "no-optional-locks" ci/run.py
0
ci/run.py:227    ["git", "-c", "core.quotepath=off", "-C", str(repo), *args]        ← git() 助手不加 --no-optional-locks
ci/run.py:460    status = git(repo, "status", "--porcelain=v1", "--untracked-files=all")   ← changed_paths_from 内
```
`git status` 无 `--no-optional-locks` 会刷新并可能重写 `.git/index`，而本树正被另一 agent 并发提交（实测期间 HEAD 由 adfdda7e → db2c02f3）。纪律 ④ 要求我的每一条 git 查询都带 `--no-optional-locks` ⇒ **不以真 CLI 传 --changed-from**。
替代（**这不是复刻，是跑真代码**）：importlib 载入 ci/run.py 为模块，仅把 `changed_paths_from` 替换为「我用 `git --no-optional-locks diff/ls-files` 预取的历史改动集」，其余 `select_checks`/`_match_prefix`/impact_map 全部走 run.py 原函数。改动集来源命令：
```
$ git --no-optional-locks rev-parse --verify --quiet b32246c4^{commit}
b32246c42907a6d789aca1e454b96fa6cffbbc28
$ git --no-optional-locks diff --name-only b32246c4 HEAD | wc -l
357
$ git --no-optional-locks ls-files runtime/io
runtime/io/fits_core.c   runtime/io/fits_verify.py
runtime/io/hips_core.c   runtime/io/hips_output_store.py
```
结果文件：run/审计执行层/E2/b06_selection_realcode.json。

### 实测 3.3 选择集结果（profile=linux-main 全量 104 项为基准）
```
### only_runtime_io: 改动文件=4  选中检查=21  / 全量104 → 未选 83
### only_runtime_all: 改动文件=31 选中检查=23 / 全量104 → 未选 81
### b32246c4..HEAD:   改动文件=357 选中检查=51 / 全量104 → 未选 53
```
只改 runtime/io 的 21 项（逐条）：VERSION-CONSISTENCY, TRACEABILITY-CODE, PRODUCTION-GRAPH, NO-SERIAL-HEAVY, ACR-DORMANT, AIO-OWNERSHIP, CONTRACT-GRAPH, DUPLICATION, SERIAL-HARDCODE, UNIT-CLOSURE, WARNING-SUPPRESSION, TASK-RESULT-SCHEMA, PROD-REACH-SELFTEST, CON-COMMENTS, CON-EXECUTION-CONTRACTS, CON-TRACEABILITY, VERSION-NAMESPACES, TRACEABILITY-MATRIX, UT-MONITORING, UT-PIPELINE, UT-RUNTIME。

### 实测 3.4 【决定性事实】两平台 CI 都不传 `--changed-from` ⇒ 增量选择集在 CI 路径上根本不生效
```
$ grep -rn "changed-from\|changed_from" .github/workflows/ ci/ --include=*.yml --include=*.json
（无输出）
$ grep -rn "ci/run.py" .github/workflows/*.yml
.github/workflows/ci-linux.yml:141:        run: python3 ci/run.py --profile "${{ steps.profile.outputs.profile }}"
.github/workflows/ci-windows.yml:110:        run: python ci/run.py --profile windows-main
```
⇒ 「只改 runtime/io 时增量 CI 实际选择集」这个问题在**当前 CI 接线下不存在运行期实例**：workflow 永远跑整 profile（linux-main 104 项 / windows-main），21 项那个集合只有在有人显式传 --changed-from 时才出现（现仅 --plan-only 的 hosted 预检用途）。这条把 B-06 从「需实测」直接转为「**已由接线证伪：无缺口可暴露**」，属**改判**方向（不是修好了 impact_map，而是 impact_map 不在 CI 关键路径上）。

### 实测 3.5 impact_map 缺口的可量化形态（即便启用 --changed-from 也在）
```
ci/impact_map.json: ['runtime/**'] -> [ ...13 个 BASE..., 'AIO-OWNERSHIP','DUPLICATION','WARNING-SUPPRESSION','CON-EXECUTION-CONTRACTS','UNIT-CLOSURE','UT-RUNTIME','UT-PIPELINE','UT-MONITORING']
（该列表内**无** UT-IO / UT-BACKEND / UT-CLI / 任何 CTEST-*）
$ grep -n "runtime/io" ci/checks.json ci/impact_map.json → (空=无显式 runtime/io)
UT-IO profiles=['linux-main'] changed_paths=['tests/io/**','lib/**','cli/**','include/**']   ← 不含 runtime/**
```
而 tests/io 确实直接吃 runtime/io 的 C 源（逐字）：
```
tests/io/test_fits_stream_contract.py:24:FITS_CORE = REPO / "runtime" / "io" / "fits_core.c"
tests/io/test_hips_input_contract.py:36:  HIPS_CORE = REPO / "runtime" / "io" / "hips_core.c"
tests/io/test_hips_output_contract.py:32:sys.path.insert(0, str(REPO / "runtime" / "io"))
$ wc -l runtime/io/*.c → fits_core.c 1709 行, hips_core.c 956 行
```
⇒ 量化结论：改 runtime/io 的 2 个 C 文件（2665 行生产 I/O 实现）时，真代码选择集给出 21 项，**恰好把唯一直接编译/装载它们的 UT-IO 与全部 15 个 CTEST-* 门排除在外**（83 项未选）。这是「无反向闭包」的可复算代价，与 L24 §6B 的静态指认同向，本轮给出精确数 21/104 与逐条清单。
另注（源码级）：`mapped = matched_ids | set(fallback if unmatched else [])`（ci/run.py:500）⇒ 只要改动文件全部命中规则，15 项 fallback 不会加入；上表「未选」里的 WORKSPACE-ADOPTION/RECONCILE-STATE 等正是 fallback 成员，实测未选。

---

## 任务 4 · CPU 门禁分母（M5a-G-002）

（待实测填充）

## 任务 5 · B-04 packaging 产物 p1_session_run 暴露面

（待实测填充）

## 追加式修订清单（对既有 findings）

（待实测填充）

## 汇报摘要

（待实测填充）