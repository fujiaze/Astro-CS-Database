# 扫描缓存 E3 — 既有 sanitizer 与真实运行证据挖掘（只读旧证据，禁止新构建）

- 代理码: E3（执行层，B-14 一次性授权）
- 授权边界: 问题扫描/10_PROTOCOL.md §8 E 层（白名单命令：git 不改索引查询 / nm / objdump / readelf / strings / ctest -N / find / grep / wc / python3 纯文本分析；只读既有产物；禁止任何构建与运行）
- 工作目录: run/审计执行层/E3/
- 复验时点: HEAD = **fa5581a0**（`git rev-parse HEAD` 2026-09-14 实测；WATCH 基线 f4f88d54 已过期，工作树脏 68 项——并发 agent 在场）
- 覆盖范围: build/asan/**（CMakeCache/flags/nm/ninja_log/Testing 痕迹）、run/**、reports/**、evidence/**、logs/、artifacts/**、ci/**、engineering/control/archive/**、docs/archive/**、真源 tests/lib/modules 注册面
- 未覆盖(原因): 新 ASan 构建与实跑（B-14 未授权）；Windows/UCRT 行为；Fatduck 侧记录
- 判定基线: 宪章 §14.4；10_PROTOCOL §8（无输出即写「未获证据」）
- 追加式修订已落 6 条定稿条目（H_NUMERIC p0 M9-H-1/H-2、p1 M9-H-5/H-6、F_TEST_GAP p0 M4-F-01、p1 M3-F-004、G_GOV_GATE p0 M5b-G-06），格式统一 `- **【E3-实测 2026-09】** …`

---

## §1 B-09 四条（L24-001/002/009/010）运行期证据检索结果

**总结论：四条全部「无针对该缺陷触发面的 sanitizer 运行记录」。既有 sanitizer 运行记录共 11 批（§4 表），无一命中这四条的触发语料。**

### L24-001 → 定稿 M9-H-1（gaia_execute/gaia_inspect 栈越界）
- 签名扫描：全仓（排除影子树与源码注释）grep `stack-buffer-overflow` = **0 命中**。命令：`grep -rIn 'stack-buffer-overflow' run/ logs/ reports/ evidence/ artifacts/ engineering/ build/`。
- 被测对象在 sanitizer 树中的状态：`build/asan/lib/gaia_xpsd_client/CMakeFiles/astrocs_catalog_gaia.dir/src/module_entry.c.o` 存在且被插桩——`nm` 命中 `__asan_register_globals` 等 29 个 sanitizer 符号；`grep -A12 'build lib/gaia_xpsd_client/CMakeFiles/astrocs_catalog_gaia.dir/src/module_entry.c.o' build/asan/build.ninja` → `FLAGS = -g -fPIC -fvisibility=hidden -fsanitize=address,undefined -fno-sanitize-recover=all -fno-omit-frame-pointer -g -fvisibility=hidden -fopenmp`。**构建条件已具备**（§4）。
- 已跑过什么：asan 树注册并跑过 `gaia_cat_*`×10 + `gaia_integration`（ctest -N #81–#91；CTestCostData 09-11 实测 gaia_cat_* 各 7 轮）。触发面缺失：既有 fixture 的 catalog_dir 全部短路径；`gaia_cat_test.c` 全文无 `catalog_dir` 长串/转义注入（grep 仅 :59 一个无关 define）。
- 唯一 gaia sanitizer 失败记录是 **LSan 泄漏**而非越界：`run/local/agent_p1cal_int/gaia_fail.log`（2026-09-09 06:45，"Test project .../build/asan … 85: gaia_integration ***Failed"）尾部 `ERROR: LeakSanitizer: detected memory leaks … SUMMARY: AddressSanitizer: 294912 byte(s) leaked in 3 allocation(s)`（帧含 GOMP_parallel）；`build/asan/Testing/Temporary/LastTestsFailed.log`（09-11 19:06）`90:gaia_cat_adapter / 91:gaia_integration`。59fdeab3 提交文本自证「gaia asan LSan 预存泄漏 bisect 同败证明与本次无关」→ 该失败与本条缺陷无因果，且**预存至今**。
- 判定：**静态推演维持；无运行期确证，亦无运行期反证**。

### L24-002 → 定稿 M9-H-2（spectrumCount 堆越界读）
- **gaia 恶意输入语料执行记录检索（任务点名项）**：7-8 个回归 TU（M9-F-1 定稿为 8 个：test_p0_io_hardening.cpp / hips_robust_sanitize_driver.cpp / gaia_sanitize_driver.c / dataflow_fuzz.cpp / hiss_writer_smoke.cpp / pipeline_frame_contract_test.cpp / hips_core_selftest.c / fits_core_selftest.c）中——
  - `gaia_sanitize_driver.c` **确有 sanitizer 跑批记录**：`engineering/control/archive/…/evidence/P13-003/sanitizer/`（mtime 2026-08-02/08-09 报告）`sanitize_wsl_v4_full.log` 阶段 [4]：`gcc $FLAGS …gaia_client.c …gaia_sanitize_driver.c -lz -lm` 后对真实 `GaiaDR3SP` 目录运行 → `files=20 sources=219165266 / GAIA_SANITIZE_OK matched=1 flux_probe=4.515788e-17`；G06 报告判词「阶段 4 DR3SP parser 对 GaiaDR3SP 目录**实际解析** PASS」。V5 复跑由 V18 运行日志（docs/archive/history/memory_V18R2-V19_operational_log_2026-08-21.md:2114）「G7 (5f35c92): WSL ASan/UBSan/LSan ALL_SANITIZE_V5_PASS」与 v19r2 coverage JSON 转述。
  - **但该语料喂的是真实数据**：真实文件 spectrumCount=343（合法值）→ 本条触发面（撒谎 count）**从未被执行**；`build/asan/tests/unit/gaia_xpsd_fixture_gen` 的源 `tests/unit/gaia_xpsd_fixture_gen.c`:343 硬编码 `spectrumCount=343`，负向面只有 `truncate.xpsd`（截断，:433-465），无 344/-1/65535 注入。
  - `hips_robust_sanitize_driver.cpp` 亦跑过（v4 阶段 [3]，FP32/FP64 双过 + corrupt TSV/properties/MOC 注入），且该轮 ASan 当场抓出并修复 1 个 driver 自身 heap-buffer-overflow（G06 报告 §「G6 过程中发现」2：aio_hips_writer.cpp:398 FP64 误用 float 缓冲）——**证明 ASan 能力真实有效**，但检测对象是 lib/hips 而非 AHPX。
  - 其余 6 个孤儿 TU：全树运行记录 **0 命中**（注册面由 E1 实测=8/8 不构建；本轮补执行面=无日志）。
- 相邻不同缺陷命中：`run/local/bughunt_p1_batchA/asan_before_gaia_p13.log`（2026-09-08 07:36）`ERROR: AddressSanitizer: attempting double-free … #2 spec_collector_push origtest/../orig_gaia2.c:1083`——同一函数族的 realloc 失败路径缺陷（/tmp/b4p1 的 orig 副本构建），**不是**本条的 memcpy 越界读，不可移用。
- 判定：**静态推演维持；撒谎 spectrumCount 从未在任何 sanitizer 下被喂入**。

### L24-009 / L24-010 → 定稿 M9-H-5 / M9-H-6（AHPX memcpy 越界写 + (long) 窄化）
- **被测 TU 从未进入任何 sanitizer 构建树**：`aio_ahpx_reader.cpp`（2026-07-12 入库 9d6b200a…51b8c7b6）在真源所有 CMakeLists.txt / cmake / ci / workflow grep **0 接线**；`grep -c ahpx build/asan/build.ninja` = **0**；`find build/asan -iname '*ahpx*'` 空。常规树/asan 树均无该 target → **无运行记录在物理上不可能存在**。
- 历史唯一编译记录：`tools/quality/v19r3_sanitizer.sh`（2026-08-25）把 ahpx 三源编进 `aio_san`（-fsanitize=address,undefined），`reports/v19r3/evidence/quality/aio.log` 跑 13/13 PASS——但 `grep -ci ahpx aio.log` = **0**：驱动 test_wph_cli_browser 不调用 AHPX 读路径 → **编译在场、执行缺位**。
- UBSan `runtime error:` 与 `(long)` 窄化相关运行记录：全树 **0 命中**。L24-010 缺陷③（LLP64 ≥2GiB 折叠）按规程 §8 属 **Fatduck/Windows 侧无法判定**，Linux 不可确证。
- 新查事实（非运行证据，但影响「是否值得修+跑」）：`lib/astro_image_io/src/ahpx/DEPRECATED.md`（git tracked，废弃日期 2026-07-13）：「.ahpx 格式已废弃，由 .hiss 替代……代码保留供参考，**不用于生产路径**」。若成立，M9-H-5/6 影响面措辞（Phase3 tile 容器必经）需负责人按「死代码面」复核——E 层不改判，仅登记此实测。
- 判定：**无据（构建面缺失，比「无运行记录」更前置）；009/010 的「ASan 条件已具备」不成立**。

## §2 SUMMARY/40-B11 §6 其余项既有证据对账（有据 / 无据 / 证据与声称不符）

| # | 项 | 判定 | 既有证据（命令+输出摘录见 §7） |
|---|---|---|---|
| 1 | **mod001 64/64** | **有据（历史）+ 计数版本漂移** | `run/mod001/fix_zlib/install_load_check_final{,2}.log`（mtime 09-11 23:59）尾行 `MOD001_INSTALL_LOAD_CHECK PASS: 64/64 checks`（units=10）；同日 `green_install_load_check_r1..r3.log` = **72/72**（units=11，19:11）；`red_install_load_check.log` = FAIL 15/46（18:46，RED 留档）。59fdeab3 与 e6254d4d 提交文本相互印证 72→64 系摘除 MOD-P1-NOISE（units 11→10）所致。⇒ 「64/64」曾真跑、有日志；对**当前 HEAD 未复核**（复核=跑脚本，本轮禁）。 |
| 2 | **两棵 astrocs 命令面差异** | **无据** | 全树未找到任何两棵树 `--help`/`version --json` 实测 diff 产物（run/cli_runs/** 仅运行 JSON，无 help 文本；run/arch_audit_p1/cli_probe 目录现已空）。M5b-G-01 维持静态结构判词。 |
| 3 | **SparseEqualsDense 实跑** | **注册面有据 / 用例执行记录无据 / ASan 下明确未跑完（证据与声称部分不符）** | 常规树 `ctest -N` 实测 **288** 项在册含 `#194: phase2_synthetic_gate.Phase2Upm.SparseEqualsDense`（⇒ B3-A4「已修注册面」属实）；但任何既有日志 0 命中该用例名；build/asan 无 phase2_synthetic_gate target（build.ninja 0）；TST003（08-27）：`phase2_synthetic_gate **超时**（内部 60s 看门狗）| ASan 较慢超时；Release 通过`。⇒「实跑过且 PASS」的逐用例运行日志至今**不存在**；SCI「Oracle 全过」仍无运行时证据（M4-F-01 残余维持）。 |
| 4 | **FOV 出片形态** | **无据** | `grep -l -i fov run/cli_runs/*.json` = 0；无 phase3 大 FOV 产品读回记录。维持无法判定（需真跑 astrocs phase3）。 |
| 5 | **p1noise 追踪（B-01）** | **有据 + 改判（追踪矛盾已闭合，另有 sanitizer 运行记录）** | `git ls-files`：`lib/snr_estimator/tests/p1noise/` **8/8 已入库**（`git log --diff-filter=A` → e54cd740，2026-09-09 16:44）；`tests/unit/CMakeLists.txt`:525-526 注释「从未入库（untracked 锁死不收编）」**与 git 事实不符（过时文本，现树仍在）**。E1 已在 M3-F-004 落行；E3 补运行面：e54cd740 提交文本自证 `asan+ubsan树-fno-sanitize-recover 8/8`，`build/asan/Testing/Temporary/CTestCostData.txt`（09-11 19:08）p1noise_* 八项各 5 轮通过在册。 |
| 6 | **avx512 exit-77 记账（L23 R3）** | **有据（合同化已落地；记账表述与 checks.json 有一处用词冲突）** | 时间线四件：①09-06 CI round4 hosted：DEEP-SAN-ASAN.log 尾 `The following tests FAILED: 35 - cpu001_selftest_avx512 (Failed)`（当时未合同化）；②09-07 11:01 **16867c25**「avx512 selftest 合同化 SKIP」→ `tests/unit/CMakeLists.txt:411 set_tests_properties(cpu001_selftest_avx512 PROPERTIES SKIP_RETURN_CODE 77)`（实测现文命中）+ `run_provider_avx512_checks.py:158 return 77`；③09-11 mod001 常规树 `ctest_all_j4.log`：`did not run: 49 - cpu001_selftest_avx512 (Skipped)`（SKIP 语义生效实证）；④审计转述（run/release-rescue/audits/aud-ci-live/raw/runs_all.json 内嵌）：`UT-CPU-AVX512=SKIPPED(waivable)（宿主缺AVX-512F，非红）` + 修复决定「verdict 以 SKIP 开头即归入 skipped（显式计数 skipped_waivable_internal + 逐项留名）」。⚠ 与 `ci/checks.json` UT-CPU-AV512 `"waivable": false`（实测 :71 号 check 全量 JSON）用词冲突：checks 定义不可豁免，CI 叙述记为 "SKIPPED(waivable)"——属**记账标签失真**而非门禁失效，登记供负责人引用。 |
| 6b | （赠）fault-injection WILL_FAIL（L23 R6） | **实测：仍无** | `grep -rn WILL_FAIL tests/ lib/ modules/ CMakeLists.txt --include='CMakeLists.txt'` = **0**（唯一命中为 p1star 注释「无 WILL_FAIL 依赖」）；ci/ctest_baseline.json 无 should_fail 字段。⇒ R6 问「是否已在近期批次补」答：没有。相邻新事实：09-09 有故障注入在 ASan 下跑过的产物目录 `run/t_fi_asan/`（atomic fsync_fail/stage_fail/kill/promote_fail 等，目录存在，无日志留存）。 |
| 6c | （赠）A-19「36/36 是否曾真跑」 | **有据（Linux 本机面首次）** | `run/mod001/abi_test_secure_loader.py.log`（09-11 19:11 副本）尾 `results: 0 FAIL / 36 PASS (total 36 checks)`；`abi_test_module_registry.py.log` `0 FAIL / 28 PASS`。注意：这是脚本手跑记录，非 UT-ABI discover 面——M6a/L23-001 的「discover 收 0 用例仍 OK」结构判词不受影响。 |

## §3 build/asan 影子树本体证据（它到底是什么）

命令与输出见 §7-A 节。要点：

1. **性质**：本地直配的插桩树，非 CI qa-sanitize 树。CMakeCache（mtime **2026-09-09 20:16:16**）：`ASTROCS_ENABLE_SANITIZERS:BOOL=ON`、`ASTROCS_SANITIZE_RUNTIME:BOOL=OFF`、`ASTROCS_ENABLE_TSAN:OFF`、`CMAKE_BUILD_TYPE:STRING=Debug`、`CMAKE_GENERATOR:INTERNAL=Ninja`、`CMAKE_HOME_DIRECTORY:INTERNAL=/workspace/Astro CS Database`、编译器 `/usr/bin/cc`、`/usr/bin/c++`（全局 FLAGS 由根 CMakeLists :36-38 `add_compile_options(-fsanitize=address,undefined -fno-sanitize-recover=all -fno-omit-frame-pointer -g)` 注入——现树实测）。
2. **规模远超任务简报的 6 库**：27 个 .a + `libastrocs_runtime.so`/`libastrocs_io.so` + 185MB `astrocs` 主程序 + `tests/unit/` 下 ~90 个测试二进制（含 gaia_cat_test、gaia_adapter_test、aio_abi_*、p1noise_*、p1wcs_* 等）+ `lib/gaia_xpsd_client/astrocs_catalog_gaia.so`。
3. **插桩直证**：`readelf -d astrocs_catalog_gaia.so` NEEDED 含 `libasan.so.8` 与 `libubsan.so.1`；`nm -C` 命中 `gaia_execute`/`json_append_escaped`/`load_xpsd_file`/`spec_collector_push` 四符号；对象文件 nm 有 `__asan_report_load*/store*` 全套（001/002 被测函数逐函数在场）。
4. **确实跑过**（非只建不跑）：`Testing/Temporary/CTestCostData.txt`（**09-11 19:08**，169 项全部有历史轮次记录，gaia_cat_* ×7、p1noise_* ×5、aio_abi_* ×11）；`LastTestsFailed.log`（**09-11 19:06**：90/91 gaia 两败）；`tests/unit/*_trace.jsonl`（09-11 19:03–19:06 写出）；外部日志 `run/mod001/asan_affected.log`（09-11，asan 受影响域 38/38 通过）、`run/PSF-001/ctest_asan.log`（09-11 08:54，p1psf 7/7）、`build/tmp_wcs001_reval_ctest_asan.log`（09-11 05:26，p1wcs 6/6）、`run/p2003_fix/asan_p200{1,2}_after.log`（09-10，P2-001 PASS）、`run/p2002_evidence/asan_heap_overflow_p2002.log`（09-10 22:45，**asan 树二进制 `build/asan/tests/unit/p2002_unc_rej_prov_test` 真抓出 `rejection.cpp:1185` heap-buffer-overflow READ**——该树运行期抓 bug 能力的正面样本）。
5. **新鲜度风险**：`.ninja_log` 末条 **09-11 19:06:39**；module_entry.c 最后一次提交 59fdeab3（09-11 19:14:45，+8/-2，describe 空 ID 语义）比 .so 新 **8 分钟**；lib/gaia_xpsd_client 在 09-12/09-13 又有 27909ddf、e6254d4d、c21eec50 三次提交（zlib/CI 接线域）。⇒ 若要拿它当「当前树」证据，须先重编 gaia 段（本轮禁）。
6. `build/asan/tsan/` 仅 1 个 cfitsio.supp（09-09），无 TSan 运行痕迹；`Testing/Temporary/LastTest.log` 见 §6 冲突备注。

## §4 专节（单列）：ASan 构建条件已具备 × sanitizer 运行记录缺失 —— 供负责人裁「下次是否授权真跑」

**核心事实链（三条都有逐字输出，§7-A/§1/§2）：**

1. **构建选项层面：已具备（对 L24-001/002）。** build/asan 就是 `ASTROCS_ENABLE_SANITIZERS=ON` + `-fno-sanitize-recover=all` 的 Debug 插桩树；001/002 的全部涉事函数（module_entry.c 的 gaia_execute/gaia_inspect/json_append_escaped，gaia_client.c 的 load_xpsd_file/spec_collector_*）**已在其中编译为插桩对象并链成 .so**，且有配套 ctest 注册（#81–#91）与 fixture 生成器。跑一次 `ctest -R 'gaia' --output-on-failure` 的门槛是「授权真跑」，不是「再建一棵树」。
2. **语料层面：不具备（四条全部）。** 没有任何既有或在册语料能触发：①catalog_dir ≥~121 字符 + 引号/反斜杠转义（001）；②spectrumCount ∉ {0..343} 的恶意 XPSD（002，fixture 硬编码 343）；③块长非 4 倍数 / 撒谎 offset/size 的 AHPX（009/010——且 AHPX 整 TU 零构建接线，须先过「废弃代码是否纳建」的裁决门）。P13-003 的 gaia ASan 跑批（08-09）与 V5（08-21）喂的是**真实 DR3SP 全量**（sources=219165266），合法数据 ⇒ 通过 ≠ 缺陷不存在，只说明未触边界。
3. **运行记录层面：四条全部空白，且各层历史上都有 sanitizer 执行**（证明空白≠能力空白）：V4/V5（08-09/08-21 WSL）、v19r3 十模块矩阵（08-25，抓到 akima heap-overflow 并修）、TST003（08-27，phase2 0 error 但 synthetic_gate 超时）、QA-002/LNX-003（09-01/09-02，56/56 全过，覆盖 core/rt/cpu/p1-p3 但**不含 gaia/AIO 边界**）、CI deep linux-deep 首轮 d416fc9d（09-06，F-D1：**选项错配 `-DASTROCS_ENABLE_SANITIZERS` vs qa-sanitize 要求 `-DASTROCS_SANITIZE_RUNTIME`，target 从未定义，CI ASan 检查首轮从未真正跑过测试**；round4 52e71804 修正后 57 用例 96%，0 sanitizer 报告）、09-09~09-11 本地 agent 系列（LSan 抓 gaia 泄漏、ASan 抓 rejection.cpp 真越界、SEGV×2）。
4. **既有拦路石**：asan 树下 gaia_cat_adapter/gaia_integration 目前被 **LSan 预存泄漏**钉败（09-09 与 09-11 两次复现，59fdeab3 自证预存）——不修泄漏或不调 detect_leaks 语义，asan 全量绿不可达；这是「授权真跑」前必须知悉的一条。
5. **登记结论（一句话）**：**「ASan 构建条件对 L24-001/002 已具备但无该两条越界的任何 sanitizer 运行记录；对 L24-009/010 连构建接线都不存在（且源自述 DEPRECATED）；全仓不存在任何 stack-buffer-overflow 运行记录」**——若负责人下次授权真跑，最小动作面 = 在既有 build/asan 上增量重编 gaia 段 + 造两个恶意语料（长目录 / 撒谎 count）+ 泄漏豁免策略，三件合一即可把 B-09 从「无法判定」推到「确证或反证」。

## §5 汇总对照表：静态推演 → 是否已有运行期证据（供并入 SUMMARY 三态表）

| 条目 | 静态结论 | 运行期证据状态 | 四态落点建议 |
|---|---|---|---|
| M9-H-1（L24-001） | P0 栈越界 | 无运行记录；构建条件已具备；全树 stack-buffer-overflow 0 命中 | 无法判定→**仍无法判定（但可判定化**：授权跑恶意语料即可） |
| M9-H-2（L24-002） | P0 堆越界读 | 真实数据 ASan 跑过 2 次（08-09/08-21）未触发；恶意语料不存在 | 同上（语料需新建） |
| M9-H-5（L24-009） | P1 memcpy 越界写 | TU 零构建接线；v19r3 编而未跑；另 DEPRECATED.md 在案 | **无法判定（前置缺口是构建裁决，不是跑不跑）** |
| M9-H-6（L24-010） | P1 (long) 窄化 | 同上；缺陷③属 Windows/UCRT 边界（规程 §8 维持不可判） | ①②同 H-5；③**无法判定（需 Fatduck）** |
| B-11 mod001 64/64 | 是否过期不可判 | 有据：09-11 实测 64/64 与 72/72 双版本日志 + 提交文本 | **有据（历史确证）**；当前 HEAD 未复核 |
| B-11 两棵命令面 | 差异不可判 | 无据（无任何实测 diff） | 维持无法判定 |
| B-11 SparseEqualsDense | 实跑不可判 | 注册面有据（#194 在册）；用例日志 0；ASan 下 60s 超时在案 | **部分有据/执行仍无据** |
| B-11 FOV 出片 | 形态不可判 | 无据 | 维持无法判定 |
| B-11/B-01 p1noise | 两陈述互斥 | 改判：8/8 已入库（e54cd740）；asan 5 轮通过记录在案；CMake 注释过时 | **有据→已判定，矛盾文本按「注释过时」收敛** |
| B-11 avx512 exit-77 | 记账不可判 | 有据：FAIL(09-06)→合同化(09-07 16867c25)→Skipped 实测(09-11)+checks 用词冲突登记 | **有据** |
| A-19 36/36 | 历史上不可证伪 | 有据（09-11 本机 36/36+28/28 日志）；但非 discover 面 | **部分有据**（脚本面曾真跑；discover 结构缺陷不受影响） |
| L23 R6 WILL_FAIL | 现状待读 | 实测：现树仍 0 WILL_FAIL；asan 故障注入产物目录存在（09-09） | 有据（「未补」确证） |

## §6 现场冲突备注（免报区观察，不作 finding）

1. **build/asan 的 LastTest.log 正文已不可恢复**：首次观测（09-14 10:29）时其 mtime 已是当日 10:29（早于本人第一条 ctest 调用）；本人此后按白名单执行 `ctest -N`（asan 树 4 次 + 常规 build 树 2 次），该命令会把 LastTest.log 写成空 Start/End 记录（现内容 `Start testing: Sep 14 10:33 CST`）。09-11 19:06 那次 gaia 两败的**逐条 stdout 因此缺失**，幸有 LastTestsFailed.log + gaia_fail.log + 提交文本三点旁证。纪律偏差自报：ctest -N 的该副作用事前未知。
2. 并发 agent 在场：工作树脏 68 项（含 问题扫描/findings 两文件），HEAD fa5581a0 与 WATCH 基线 f4f88d54 已错位；`run/arch_audit_p1/cli_probe` 等目录观测期间被清空。一切 git 查询均带 --no-optional-locks，未遇 lock 冲突。
3. `run/release-rescue/**`、`run/arch_audit_p1/shadow*/` 为整仓克隆，检索命中去重后均归并到真源文件；未据此登记任何 finding。

## §7 命令与输出摘录附录

完整逐条「命令 + 原样输出」台账已存 `run/审计执行层/E3/appendix_commands.md`（A 节=build/asan 取证；B 节=十一批 sanitizer 运行记录；C 节=六项对账；D 节=签名扫描）。正文各节结论处均已内嵌关键输出原样行；此处不重复。
