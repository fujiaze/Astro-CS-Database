# 扫描缓存 L24 — 健壮性与交付安全（畸形输入、越界、窄化、路径、原子发布、日志）

- 代理码: L24（横向第二波）
- **本档案由前台代录**：L24 两次宣布「即将整档写入」但文件仍为 40 行骨架，判定其单次 write 规模超限；
  已改为分片回传（每条消息 3 条 finding），由前台逐片落盘并在每片前复验锚点。
- 判定基线: 宪章 §14.4（信任边界必须校验 / INPUT_CORRUPT 显式化）、§8.6、§11、§13、§17.6/§17.11、§6.3、§5.3
- finding 数（终计）: **15（P0:3 / P1:10 / P2:2）** — 分片 1-6 全部收齐，本轴收工
- 形态分布: 缺陷 12（L24-001…013 除 013 为跨平台合同缺陷外均为代码/文档缺陷）+ 横向扩展 3（L24-014/015/016）
- 与 §2 小结表对账: P0 3 · P1 10 · P2 2 = 15 ✓（M8 要求的计数一致性已核）

## 0. 方法自校验
- 穷举清单：①外部输入边界 ②错误路径资源 ③路径与文件名 ④原子发布 ⑤日志与诊断 ⑥未定义行为面
- 子代理分工：S1（AIO/XPSD 解析与缓冲）、S3（构建注册与 orchestrator 树）；主代理逐条二次复验后才登记

## 1. findings

### L24-001 Gaia 模块 manifest 拼接：snprintf 返回值推进指针 + 转义无容量参数 → 栈缓冲越界写
- 类别码: H_NUMERIC（主）；次挂 C_DOC_CODE_GAP、G_GOV_GATE
- 优先级: P0（前台已逐字复验锚点）
- 位置:
  - lib/gaia_xpsd_client/src/module_entry.c::gaia_execute（char head[512]，:738；hw += snprintf(...) :741/:770/:774；json_append_escaped(&hw, c.catalog_dir) :776）
  - 同文件::gaia_inspect（char buf[512] :930；json_append_escaped(&w, inst->cfg.catalog_dir) :939）
  - 同文件::json_append_escaped（:384，签名为 (char** w, const char* s)，无剩余容量入参）
  - 站点参数来源：同文件 gaia_cfg::catalog_dir 为 char catalog_dir[1024]（:211）
- 权威依据: 宪章 §14.4（信任边界含 CLI/配置输入与用户可控数据，须 fail-fast 返回明确状态码）、§8.6（跨 DLL 边界返回稳定状态码）、
  §11（不得吞错后继续生成看似成功的产品）；同文件 :24-27 自述的输出事务合同（尺寸查询→写入，不足→PARAM+BUFFER_TOO_SMALL）
  **只覆盖出参 strbuf，未覆盖这两个内部栈缓冲**
- 问题说明: 三重缺陷叠加。①转义函数逐字节无界写入；②snprintf 返回「本应写入的字符数」而非实际写入数，代码用它直接推进 hw，
  一旦头部（module_id+op+schema+spec 三字段与键名，实测约 330-390 字节）+ catalog_dir 超过 512，hw 越过数组末尾继续写；
  ③静态算术：head 剩余约 121 字节，而 catalog_dir 容量 1024、转义后最坏 2046 → 越界写最多约 1900 字节；
  gaia_inspect 更严重——w 越过 buf+512 后 (size_t)(buf + sizeof(buf) - w) 变成负数的 size_t 巨值，
  随后 snprintf 以近无限 max 继续格式化写出。不是"截断显示"级问题，是缓冲区溢出。
- 影响: astrocs_catalog_gaia 是随产品清单发布的官方模块；execute 是每次星表查询必经路径、inspect 是 host 诊断必经路径。
  触发条件仅要求 catalog_dir 超过约 121 字符——Windows 深路径、网络盘、含空格目录都会命中。后果为栈破坏：
  随机崩溃或被构造的目录字符串操控返回地址；Phase1/Phase2 均以该模块取参考星与光谱，属发布阻断级。
- 建议处置: ①json_append_escaped 增加剩余容量入参并返回是否溢出；②所有推进点对 snprintf 返回值取 min(返回, 剩余) 夹紧，
  越界即返回 ACS_ERR_PARAM/INTERNAL 并停止产品提交（只在边界校验一次，不在 CLI/AIO/算法层重复）；
  ③head/buf 改为「先精确计算所需长度再一次性分配」，与同文件出参两阶段合同同口径；
  ④负向用例：catalog_dir 120/121/1023 字符 + 含引号反斜杠 + 控制字符，断言不越界且状态码正确（ASan 版 ctest 注册）
- 置信度: 高（前台复验）
- related: S1-001（同族堆版：lib/hips/src/module_entry.cpp::hips_execute_write_product，预算 384u+strlen+64u 且同用无容量转义函数）

### L24-002 XPSD 自报 spectrumCount 无上限即用作每星 memcpy 长度 → 堆越界读 + OOM 放大
- 类别码: H_NUMERIC（主）；次挂 G_GOV_GATE（§14.4 外部文件解析边界）、B_STD_MISMATCH（Gaia DR3 光谱 343 列固定面）
- 优先级: P0
- 位置: lib/gaia_xpsd_client/src/gaia_client.c::load_xpsd_file（:1068 xf->spectrum_count = atoi(p + 14)）；
  消费点 ::spec_collector_push（:1266 memcpy(sc->spectra + count*spectrum_count, spectrum, spectrum_count)）；
  ::spec_collector_init（按 capacity*spectrum_count 分配）；布局常量 WL_COUNT=343、STAR_STRIDE_SP=40+(343+(343&1))=384；
  唯一处置点为「等于 0 才兜底」（:2049-2050、:2089-2090、:2240-2241、:2431-2432）
- 权威依据: 宪章 §14.4（外部文件解析属真信任边界；明文要求对"会造成数据破坏、安全问题或 ABI 未定义行为"的输入校验）、
  §5.3/§7.3（科学路径数值语义不得静默漂移）；D.catalog（Gaia DR3 光谱波段数固定 343）
- 问题说明: spectrum_count 来自文件内 XML parameters 字符串的 atoi，全文件**从未校验其是否 ≤343、是否为负、
  是否超过记录可用长度**。星记录在块内可用字节由 STAR_STRIDE_SP=384 固定，块长度又受限于文件自报的 block_size，
  于是 memcpy 的源长度由撒谎字段独立决定、与源缓冲真实长度无任何约束：声明 spectrumCount=100000 时每颗星从 p+40
  起读 100000 字节，越过 scratch 末尾约 99.6KB；同时按 capacity*spectrum_count 分配（4096*100000 ≈ 410MB/文件）。
  同文件其它自报计数都有上限（tree_count<16、file_count<MAX_FILES、magnitude 判 NaN/Inf 回落），说明作者知道这道防线，唯独此处漏。
- 影响: XPSD 是从外部拉取的持久化产品（GaiaDR3/、GaiaDR3SP/），属不受信输入。后果 ①堆越界读（崩溃或把相邻堆内容当光谱字节）；
  ②数百 MB 至数 GB 分配（OOM 放大）；③若侥幸不崩，越界字节经 base64 交给 lib/photometric_calib（pc_api.cpp 调
  gaia_client_cone_search_with_spectrum），即**污染测光定标所用光谱数值且不报错**（宪章 §11 禁止的吞错继续）。
- 建议处置: ①建立"文件自报计数"统一上限策略：1 ≤ spectrum_count ≤ WL_COUNT，越界即整文件拒绝并给显式损坏状态码
  （边界之后信任 typed contract，不在查询路径重复校验）；②spectrum_start/step/bits 同样校验；
  ③spec_collector_push 的 memcpy 长度改为 min(spectrum_count, 记录剩余可读字节)，源不足返回错误；
  ④负向用例注入 spectrumCount=0/1/343/344/65535/-1 六个 XPSD，断言拒绝与不越界（ASan 注册）
- 置信度: 高
- related: L10-008（同一 load_xpsd_file 的 header_len 弃用与 root_pos/child 不设限——本条是该族第四个未校验字段）

### L24-003 七个恶意输入回归测试从不进构建：注册门只做单向闭包，孤儿测试 TU 对机器门完全隐形
- 类别码: F_TEST_GAP（主）；次挂 G_GOV_GATE（门禁形同虚设）
- 优先级: P0
- 位置: 孤儿测试 TU（真源）lib/astro_image_io/tests/{test_p0_io_hardening.cpp, hips_robust_sanitize_driver.cpp,
  gaia_sanitize_driver.c, dataflow_fuzz.cpp, hiss_writer_smoke.cpp, pipeline_frame_contract_test.cpp} 与
  modules/services/io/tests/hips_core_selftest.c；门实现 tools/quality/check_ctest_registration.py
  （C1 universe 定义 :9-17、C3 判定 :99-120/:167-174，ADD_TEST_NAME_RE 只匹配 add_test(NAME …)）；
  正面对照 tests/unit/CMakeLists.txt::p1_io_hardening_test（:761-769 add_executable+add_test）、::hips_publish_atomic_test（:566-580）
- 权威依据: 宪章 §13.1/§13.2、§12.3-4、§14.4 末段（对已观察到的故障建立最小复现与机器测试）、§17.2、§17.10；
  本仓 CI-REG-002 立规目的自述（STD-F7：新增测试目标须被 CI 显式登记）
- 问题说明: CI-REG-002 的判定 universe 是「add_test 名字集合」，因此只能发现「已 add_test 但未登记 CI」，
  **无法发现"测试源文件从不进 add_executable/add_test"**——后者对门完全不可见，是该门的结构性单向闭包缺陷。
  漏网的恰是本轴最重要的加固成果：26 个 AIO extern "C" 入口异常屏障、HISS/AHPX 恶意 fixture 四连、
  XPSD 真实解析 ASan/UBSan 驱动、HiPS MOC 与 SNR TSV 行数上限、dataflow 模糊器、HISS 写出烟测、管线帧合同、IO-002 全套自测。
  多数文件头还自述为「P0 修复的复现测试」；test_p0_io_hardening.cpp 的唯一"运行方式"是文件头的 g++ 注释行。
  grep 实证：7 个文件名在真源 CMakeLists.txt / ci/checks.json / .github/workflows 均 0 命中；
  两个 sanitize driver 仅被 lib/astro_image_io/tests/sanitize_wsl_v4.sh、v5.sh 引用。
- 影响: 影响的是"机器门可发现性"本身——约 6000 行验证代码上 §13 必备测试与 §17.2 追溯无断链不成立，且 CI 全绿不可证伪；
  直接后果是任何后续改动（含正在进行的 RESCUE-V3 批次）把边界校验改回去都不会被发现。
  同域 p1_io_hardening / hips_publish_atomic 均已注册，说明这是遗漏而非风格差异。
  另 lib/orchestrator/cpp 整树无 CMakeLists.txt（靠该目录 Makefile 手工构建），其原子写出与 checkpoint 修复同样无保护。
- 建议处置: ①给 CI-REG-002 补反向闭包 C7：扫描 tests/ 与 lib/**/tests/ 下含 main() 或自述为 test 的 C/C++/py TU，
  凡未出现在任何 add_executable/add_test/注册脚本即 FAIL（并补负例自检）；②7 个 TU 逐个定归属：能进 ctest 的进
  tests/unit/CMakeLists.txt 并入 ci/checks.json 的 UT-AIO/UT-IO（sanitize 类可单列非默认 profile 但必须在册），
  确认废弃的**删除**而不是留在树里充当证据；③lib/orchestrator/cpp 要么入构建，要么按 §14.1 明示非交付状态并停止在交付文档中引用其行为
- 置信度: 高
- related: S3-017（orchestrator 整树不注册）、F00-03（同类"门对不可见对象失灵"）、L22-005（修复未固化）、L16-001（TEST 层锚在文档）


### L24-004 XPSD 魔数/长度校验失败不解除映射，句柄随下一次 memset 永久失联（P1）
- 类别码: G_GOV_GATE（主，违 §14.4 fail-fast 应带资源回收）；次挂 H_NUMERIC
- 位置: lib/gaia_xpsd_client/src/gaia_client.c::load_xpsd_file 失败返回点 :1034（前台已复验该行原文：
  `if (xf->mmap_size < 16 || memcmp(xf->mmap_data, XPSD_MAGIC, 8) != 0) return -1;`）；唯一回收处 ::close_xpsd_file；
  调用方 ::gaia_client_create_ex 目录扫描循环两条
- 问题说明: 三重叠加。①映射建立后的第一个校验直接 return -1，跳过本文件自己写明的两条回收路径；
  而上游三个失败分支（打开失败 / hMap 失败 / mmap 失败）都就地回收 → 是漏项不是风格。
  ②调用方只在 load 返回 0 的分支调用 close_xpsd_file，返回 -1 时无补偿。③放大：file_count 不自增，
  下一个候选文件用**同一下标**进 load_xpsd_file，函数首行 memset(xf,0,...) 把上次的 mmap_data/fd/hMap/hFile 清零 →
  映射与句柄在本进程内永久不可达，destroy 也救不回。另该路径不区分「太短」「魔数不符」「读取截断」，
  与 §14.4 要求显式损坏状态码不符。
- 影响: 目录中每有一个损坏或非 XPSD 的 *.xpsd，每次 create 永久泄漏 1 映射 + 1 fd（或 2 内核句柄）；
  MAX_FILES=32 使单次有界，但 plan/create 在一次 run 中可多次调用 → 长跑表现为 fd 耗尽与地址空间持续增长。修复面极小。
- 建议处置: 该 return -1 前统一走已有回收（或引入单一 fail 标签）；调用方对非 0 返回值也调用 close_xpsd_file
  （该函数对 mmap_data==NULL 幂等）；注入「长度 8 的文件」与「魔数为 XPSD0000 的文件」并断言 fd 数不增。
- 置信度: 高（代理本轮一度误判为假，按符号重读两处调用点后确认为真 —— 该自我纠错过程保留为方法证据）

### L24-005 read_leaf_block 用文件自报偏移与长度直接寻址映射区，无映射长度约束（P1）
- 类别码: H_NUMERIC（主，越界访问面）；次挂 G_GOV_GATE
- 位置: 同文件 ::read_leaf_block :964/:967（前台已复验：`const uint8_t *comp = xf->mmap_data + xf->data_position + block_offset;`
  与 `memcpy(scratch, comp, block_size);`）；字段来自 ::load_xpsd_file 树节点解析（block_offset/block_size/compressed_size、data_position）；
  消费点 ::search_recursive 及另三条查询路径
- 问题说明: block_offset 与 data_position 全部来自文件，相加后与 mmap_size **无任何关系校验**；
  compressed_size 与 block_size 亦互相独立，唯一校验是解压后 dest_len==block_size（事后）。字段撒谎或文件截断时：
  ①非压缩分支从映射区外最多读 block_size（uint32，可达 4GB）；②zlib/lz4 分支把映射外指针交给解压函数。
  scratch 目标侧安全（按全局 max block 分配），风险全在源侧。作者知道该字段可含任意位型（64 位偏移被刻意清最高位当叶子标志），
  却没对映射长度取模。同族其它三点已由 L10-008 登记，本条是第四站点（不同字段/函数/越界倍数）。
- 正对照: lib/astro_image_io/src/hiss_reader.cpp 对同类「自报偏移+长度」显式写了 offset+comp_size > file_size →
  HISS_ERR_FORMAT_VIOLATION —— 本仓既有且可照抄的口径。
- 影响: 畸形/截断 XPSD → 段错误；或把映射外兄弟数据当星块解压 → ra/dec/mag/光谱全部错位，
  而调用方无法区分「正确解压」与「读错区域解压出垃圾」，属静默错误科学数据。
- 建议处置: 在 load_xpsd_file 末尾（解析完全部树、返回 0 之前）做一次性块区间模型校验：
  每叶节点要求 data_position+block_offset+max(compressed_size,block_size) ≤ mmap_size，任一不满足即整文件拒绝
  （显式损坏状态码，与 hiss 同口径）；读取热路径不重复校验，只留 assert/不变量注释指回该校验；
  负向用例补「只截断数据段而保留头」的 XPSD（现有 negative 模式覆盖不到字段撒谎）。

### L24-006 树节点数组分配失败被 break 吞掉，仍返回成功并少注册若干棵树（P1）
- 类别码: G_GOV_GATE（主，违 §14.4 禁「宽泛处置后继续」与 §11 禁吞错）；次挂 C_DOC_CODE_GAP
- 位置: 同文件 :1106 `if (!ti->nodes) break;` 与 :1137 `xf->tree_count++;`（两行前台均已复验），函数出口 return 0 在树循环之后
- 问题说明: break 跳出的是「解析所有 Tree」的循环，不是「本文件加载失败」路径：tree_count 保持已解析棵数（可能为 0），
  函数继续初始化块缓存并**返回 0（成功）**，调用方据此 file_count++ 并把该文件当完整数据集用。
  同一函数对更早的 open/mmap 失败都正确 return -1 → 此处属漏判而非有意降级。
- 影响: 「部分加载被当成功」——树与树覆盖不同天区，被丢掉的树所在**整片天区在锥查询中静默为空**，
  返回 0 颗星与「该天区确实没有亮星」在接口上不可区分。下游 plate_solve 可能因参考星不足而失败或退化、
  测光定标的参考星集合被静默裁剪 → 直接改变科学数值。触发条件为内存压力或节点区撒谎造成超大分配。
- 建议处置: malloc 失败改为资源类失败返回（与 open/mmap 失败同状态码语义）且不注册该文件；
  同函数其它静默点（字段缺失、find_max_block_size 返回 0）按「要么完整要么拒绝」复核；
  若确实希望「部分树可用」，必须变成显式契约：plan 上报完整性、execute 携带 PARTIAL 状态、禁止下游把 PARTIAL 当成功。

### L24 分片 1/2 小结（当前累计 6 条：P0:3 / P1:3）
- 全部 6 条位于两个模块：gaia_xpsd_client（5 条，含 3 条 P0）与 astro_image_io 测试注册面（1 条 P0）
- 共同根因：**外部输入与内存边界的校验只做了「一半」** —— 同文件其它分支都做对了，漏的那一处恰好是可达路径；
  且**守护这些边界的 6000 行回归测试从不进构建**（L24-003），修复可被无声回退
- 与前台已验证据同源：F00-07a（畸形关键字被测试钉成期望输出）、F00-07b（PATH_MAX 边界用常量致 509~511 静默截断）


### L24-007 Gaia 配置数组计数循环可无限自增（strtod 不推进时不消费字符）（P1）
- 类别码: G_GOV_GATE（主，违 §14.4 配置输入边界 fail-fast）；次挂 H_NUMERIC（活性 + CWE-190）
- 位置: lib/gaia_xpsd_client/src/module_entry.c::json_get_f64_array :191-192（前台复验原文：
  `while (*q && *q != "]") {` / `if (*q == '-' || (*q >= '0' && *q <= '9')) { count++; strtod(q, (char**)&q); }`），
  消费点 ::gaia_cfg_parse 的 ra_list/dec_list
- 问题说明: 设计意图是「数字起头则 strtod 消费，否则手动 q++」，但 '-' 属数字起头而 strtod 在无法转换时**不推进 endptr**
  （输入如 `[-]`、`[1,-,2]`、`[1e]`）→ *q 仍是 '-'、循环条件恒真 → **count 无限自增且永不推进**，进程挂死；
  count 为 int，持续自增另构成有符号溢出 UB。第二遍 :199-202 用 `i < count` 夹紧，故**不会**越界写数组 —— 本条只登记活性与 UB。
  另 count 无上限，超大数组 → malloc(count*8) 与 plan 阶段内存预算（§10.5/§14.4）脱钩。
  该解析器被 validate_config / plan / create 三处调用 → 模块生命周期每一步都可被一次配置挂死。
- 建议处置: ①计数循环记录 prev=q，strtod 后若 q==prev 即判语法错误（*out_n=-1 返回 NULL）——把「零推进」显式判为非法；
  ②对 count 设登记常量上限，超限返回 ACS_ERR_PARAM 而非分配；③负例 `[-]`、`[1,-,2]`、`[]`、`[1e]`、超大元素数各一，
  断言不挂死且状态码明确，挂在已注册的 gaia ABI 测试面
- 置信度: 高

### L24-008 HiPS SNR 目录读出的容量入参为 int 且未判负，(size_t) 强转使负值变巨值（P1）
- 类别码: H_NUMERIC（主）；次挂 C_DOC_CODE_GAP（头合同未声明该入参域与负值语义）
- 位置: lib/astro_image_io/src/hips/aio_hips_reader.cpp::aio_hips_read_snr_catalog :519/:526（前台复验：
  `int n = (int)std::min((size_t)max, d->snr.size());`）；合同面 include/aio_hips_reader.h 该声明只写
  「调用方分配; 返回实际点数 (0=无)」，未写负值处理
- 问题说明: 调用方以 int 承载「失败/无数据」的 -1 哨兵并透传时，`(size_t)max` ≈ 1.8e19 → min 取 snr.size() →
  n 被赋为产品内实际点数（可远大于调用方缓冲），随后按 n 逐条写 ra/dec/snr/star_id/quality_flags/photometric_status
  **六个调用方缓冲** → 越界写最多 (snr.size()-max) 条 × 约 36 字节。
- 正对照（同文件即有正确写法）: `aio_hips_tile_ipix` 首行 `if (!d || !out_ipix || i < 0 || (size_t)i >= d->tiles.size()) return -1;`
  → 说明这是漏项而非有意设计。
- 可达性: 该 API 是 Phase2 采样与 stage2 读 SNR 的唯一入口（sampler.cpp:399/569、stage2.cpp:82、
  orchestrator.cpp:3906/4922 经 dlsym、healpix_browser_qt:350）。当前五处调用点均传正数（sampler 传 1<<20），
  故定性是「边界不设防」而非「已知必崩」；但 orchestrator 侧按函数指针动态调用，签名不匹配或未来任一调用方
  以 -1 表示「无缓存」即造成越界写。
- 建议处置: ①首行加 `if (max < 0) return -1; if (max == 0) return 0;`（与姊妹入口同口径）；
  ②头文件与 contracts/API_CONTRACTS.csv 该 API 行补写 max 定义域与负值语义；
  ③负例并入 hips_robust_sanitize_driver.cpp —— 但该 TU 目前只测 max=8 正例**且自身从不进构建**（见 L24-003）
- 置信度: 高

### L24-009 AHPX 权重/SNR 块按字节数 memcpy 进按 float 个数 resize 的缓冲（P1）
- 类别码: H_NUMERIC（主，堆越界写）；次挂 G_GOV_GATE（§14.4 外部文件解析）
- 位置: lib/astro_image_io/src/ahpx/aio_ahpx_reader.cpp::readSnr :572/:574 与 ::readWeight :633/:635；
  **正对照同文件 ::readPixels :546-549**（前台逐字复验，三处 memcpy 对比）
- 逐字对照: readPixels 先 `size_t expectedSize = (size_t)w*h*c*sizeof(float); if (rawData.size() != expectedSize) {…拒绝…}`；
  而 readSnr/readWeight 直接 `size_t floatCount = rawData.size()/sizeof(float); result.resize(floatCount);
  std::memcpy(result.data(), rawData.data(), rawData.size());`
- 问题说明: **分配 N*4 字节却拷 rawData.size() 字节**。块长度非 4 的整数倍（畸形或截断的 AHPX 尾部）时
  memcpy 越界写 1~3 字节到 vector 堆缓冲之外。同路径的 readPixels 明确做了等值校验 → 作者知道需要这一步，两处属漏项。
  次级：两函数也不校验块长度与 imageInfo 的 W*H*C 一致，故「块只覆盖 1/10 像素」的撒谎头会被静默当完整平面交下游
  （返回 vector 长度即 floatCount，消费方无从知晓它与几何不符）。
- 影响: AHPX 是 Phase3 投影/重采样的 tile 容器；readSnr/readWeight 的输出作为 SNR 平面与 drizzle 权重平面参与归一化与排异。
  ①堆元数据/相邻对象被 1~3 字节覆写（分配器相关崩溃或静默破坏）；②几何不符的短平面静默进入积分 →
  **科学数值被污染且不报错**（§11 禁止形态）。本仓自产 AHPX 恒为 4 的倍数，故外部产品是主要暴露面。
- 建议处置: ①与 readPixels 对齐（snr 至少校验 %4==0 且 ==w*h*c*4，不符返回空并打印期望/实际）；
  ②memcpy 长度改为 result.size()*sizeof(float) 或 min(dst_bytes, src_bytes)，杜绝任何源长于目的的拷贝形态；
  ③负例：块长度 4k+1 / 4k+3 / 截半，断言拒绝且不越界（注册 TU，见 L24-003）
- 置信度: 高（正反对照逐字复验）

### L24 分片 1/2/3 小结（累计 9 条：P0:3 / P1:6）
- 分布：gaia_xpsd_client 6 条（含 3 条 P0）、astro_image_io（AIO/AHPX/HiPS 读侧 + 测试注册面）3 条
- **统一根因（前台据此主题化）**：外部输入与内存边界的校验**只做了「一半」** —— 同一函数、同一文件里其它分支都做对了，
  漏的那一处恰好位于可达路径；而守护这些边界的约 6000 行回归测试**从不进构建**（L24-003），故修复可被无声回退。
  正对照三例（readPixels、aio_hips_tile_ipix、load_xpsd_file 的上游三个回收分支）证明这是**遗漏而非风格**，
  因此「按同文件既有正确写法补齐」是低成本高确定性的处置建议。
- 与前台已验同源：F00-07a（测试把缺陷钉成规格）、F00-07b（PATH_MAX 边界用常量 → 509~511 静默截断）


### L24-010 AHPX 块描述符把 JSON 浮点强转 uint64 作偏移/长度，不校验落在文件内，读取用 (long) 窄化（P1）
- 类别码: H_NUMERIC（主，CWE-681/686 窄化与越界转换 UB）；次挂 G_GOV_GATE
- 位置（前台复验，行号为当前树实测）: lib/astro_image_io/src/ahpx/aio_ahpx_reader.cpp::parseBlockIndex :350/:356
  `blk.offset = (uint64_t)extractNumber(...)`、`blk.size = (uint64_t)extractNumber(...)`；::readRaw :441
  `if (std::fseek(m_fp, (long)offset, SEEK_SET) != 0)`；::readBlockByIndex :473/:491 `size_t estSize = blk->size * 8;`
- 问题说明（四缺陷同链）: ①extractNumber 返回 double，被直接强转 uint64 —— JSON 写 `"offset": -1` / `1e300` / `2^70` 时
  **浮点到无符号整型的越界转换是 UB**（结果不确定，不是截断），而该值随后当文件偏移用。②offset/size 从不与文件实际长度对照，
  `offset+size ≤ 文件大小` 这一最低约束缺失；size 撒谎超大 → `vector<uint8_t> compData(blk->size)` 直接巨额分配，
  或走 `size*8` 乘法回绕（size > SIZE_MAX/8 时回绕成小值，**恰好绕过 1<<30 的降级分支**），最终靠 decompressBlock 事后长度校验兜底
  ——即防线是"事后解压失败"而非"入参拒绝"。③`(long)` 窄化在 **LLP64/Windows 把 ≥2GiB 偏移折成 32 位**，
  回绕命中文件内合法位置时 fseek 返回 0、读到的是**另一块的数据**，codec=NONE 分支不校验即原样返回 → 静默取错数据；
  Linux（long=64 位）同文件同偏移却读对。④getImageInfo 把 JSON 数字 (int) 强转赋给 w/h/c 亦无范围校验。
- 权威依据: §14.4（外部文件解析须校验，且"会造成数据破坏/安全问题/ABI 未定义行为"正是必须加生产分支的情形）、
  §3.1（Windows amd64 是正式交付入口 → long=32 是交付事实）、§8.3（AIO 负责必要边界校验）
- 正对照: lib/astro_image_io/src/hiss_reader.cpp 用 `#ifdef _WIN32 _fseeki64 / #else fseeko` 处理大文件定位，ahpx 侧无此分支
- 影响: AHPX tile 超 2GiB（HiPS 多通道高层级容器可达）或头字段畸形 → Windows 正式平台读到错块或失败，
  **Linux CI 与 Fatduck 行为分叉**；本仓 HiPS 结构校验（§17.9）可能"Linux 通过、Windows 产物不同"。
- 建议处置: ①增 extractUint64（拒绝负值/小数/超阈值），open 期记下 fsize，对每个 block 校验
  `offset ≤ fsize && size ≤ fsize-offset`（先防溢出再相加）；②readRaw 改用 hiss_reader 同款 _fseeki64/fseeko 分支；
  ③估算分配上限改为 min(必要上限, 剩余文件字节×k)，blk->size 超剩余长度直接拒绝；
  ④负例：offset 为 -1/1e300/2^70、size 为 0/2^40、>2GiB 真实块，断言拒绝或读对**且两平台一致**
- 置信度: 高

### L24-011 FITS 原子写 target/tmp 均 512 字节且静默截断：长路径下二者塌缩为同一路径（P1）
- 类别码: G_GOV_GATE（主，违 §11 原子提交条款）；次挂 C_DOC_CODE_GAP
- 位置（前台复验）: runtime/io/fits_core.c 结构体 `char target[ACS_FIO_PATH_MAX];` :1077 与 `char tmp[...];` :1078；
  ::acs_fio_writer_begin_v1 :1228 `snprintf(wr->target, sizeof(wr->target), "%s", path_utf8);`、
  :1229 `fio_writer_make_tmp_name(path_utf8, wr->tmp, sizeof(wr->tmp));`（内部 `snprintf(out, cap, "%s.tmp.%ld.%lu", ...)`）；
  常量 ACS_FIO_PATH_MAX 定义于 modules/services/io/include/astrocs/io/fits_stream_v1.h；提交点 ::acs_fio_writer_end_v1 的 rename
- 问题说明: begin_v1 只校验 path_utf8 非 NULL、**不校验长度**。target 用 snprintf 拷贝（超长静默截为 511 字符），
  tmp 由「path_utf8 + .tmp.<pid>.<seq>」再 snprintf 到同样 512 字节。当 `strlen(path_utf8) ≥ 511` 时
  **tmp 的截断结果恰等于 target → tmp == target**：随后 `fopen(wr->tmp,"w+b")` 以截断模式**原地打开并清空最终产品路径**，
  写入过程直接暴露在正式路径上（崩溃/取消/EPIPE 都留下半截正式文件），最后 `rename(wr->tmp, wr->target)` 变成自己改自己、永远"成功"。
  两个后果同时发生：①「临时文件+原子提交」被静默替换为「原地覆写」，且 overwrite=0 的"目标已存在则失败"保护也失效
  （探测用未截断的 path_utf8，实际写的是截断路径）；②**不同目标只要前 511 字符相同即塌缩到同一文件，两帧/两 tile 互相覆写而无任何错误**。
- 正对照: 姊妹边界 runtime/io/hips_core.c::hips_join_path :536 `if (dn + 1 + rn + 1 > path_cap) return ACS_HIPS_ERR_PARAM;`
  —— 同仓已有"溢出即报错"的正确写法（前台复验命中）。
- 影响: 该 writer 是 IO-001 唯一 FITS 原子写实现（astrocs_io 导出）。当前 lib/cli 内无生产调用点（L10-015 与 S2-007 已证），
  故**当前生产写出路径不受影响**；但 tests/io 的 HiPS/FITS 合同测试与 fixture 生成器都走它，一旦按 §8.3 把三个 Phase 的写出
  收敛到本边界（正在进行的 IO/RESCUE 批次方向），深输出目录 + 中文目录 + 长 run 目录场景即为产品破坏。
  Linux 无 PATH_MAX 限制 → **问题只在 Windows 正式平台暴露**，与"CI 主跑 Linux"叠加后极难发现。
- 建议处置: ①begin 入口校验 `strlen(path_utf8) < ACS_FIO_PATH_MAX - 48`（为 tmp 后缀留确定余量），超限返回 PARAM 并在 err 里
  写清上限与实测长度；②ACS_FIO_PATH_MAX 提到 OS 上限（Linux 4096 / Windows 长路径 32767）或动态分配；
  ③长度回归用例 511/512/508 三档 + "两目标仅尾部不同"的碰撞用例，断言拒绝且不留半成品；④确认发布物是否声明 longPathAware（§6）
- 置信度: 高 | related: F00-07b（同族不同站点，勿重复定稿）、L24-013（同一提交点的 rename/fsync 缺陷）

### L24-012 「路径为 UTF-8」合同下 Windows 侧无 UTF-8→UTF-16 适配层，发布原语直接用 *A ANSI API（P1）
- 类别码: C_DOC_CODE_GAP（主）；次挂 G_GOV_GATE（§8.3 唯一 I/O 边界）
- 位置（前台复验）: lib/hips/src/aio_publish.cpp :33 `#define PUBLISH_MKDIR(p) _mkdir(p)`、:37 PUBLISH_UNLINK 用 _unlink、
  :128/:169 `FindFirstFileA`（L24 另引 :364-367 的 MoveFileExA，前台本次未在截断窗口内命中，交 M6 复核）；
  合同 lib/hips/include/astrocs/hips/publish.h :73「原语 v1 (4 函数; **路径均为 UTF-8**, 与 IO-001 同语义)」，
  参数名 out_dir_utf8 / stage_path_utf8；runtime/io/fits_core.c::fio_file_open 与 begin_v1 的探测 fopen、
  runtime/io/hips_core.c 的 fopen 在 _WIN32 下均无宽字符分支
- 正对照: lib/astro_image_io/src/aio_util.h::aio_fopen_utf8（MultiByteToWideChar(CP_UTF8)+_wfopen）与
  ahpx/aio_ahpx_reader.cpp::ahpx_fopen_utf8 —— 全仓只有 astro_image_io 实现了该适配层
- 问题说明: `IO_001` 把 UTF-16 转换显式指派给「上层适配层」，但 runtime/io（IO-001/IO-002 新基线边界）与 lib/hips（AIO-002 原语）
  都没有实现它 —— **没人兑现该合同**。①UCRT 的 fopen/_mkdir/_unlink 与 *A 系 API 按进程 ANSI 代码页解释字节，
  UTF-8 中文/带重音目录变成乱码路径 → 建目录失败或写到非预期目录；而同产品的 astro_image_io 老边界能正常处理，
  形成「同一次运行里一个 I/O 层能写、另一个不能写」的行为分叉，直接违反 §8.3 的单一边界要求。
  ②更硬：promote 用 MoveFileExA 重命名整个 staging 目录 → 中文目录下 HiPS 发布在**事务最后一步**失败，
  此时 stage 树已 fsync 但未 promote，调用方走 discard 分支 → 整次发布被丢弃，而原因只剩一个 errno（GetLastError 被丢弃）。
  参数名后缀 `_utf8` 与实际按 ANSI 解释，构成"名字即合同"的误导。
- 影响: 交付面是 Windows 正式产品（§3.1），§15.3 指定 Fatduck 为 Windows 复验节点。非 ASCII 用户目录是常态。
  runtime/io 侧当前无生产调用点，但 **lib/hips 的 AIO-002 原语已被 astrocs.p1.hips 模块事务面接线**（stage_create/
  tree_fsync/promote）→ 生产可达。Linux 完全无此问题，CI 主跑 Linux → **该缺陷对机器门不可见，只能靠 Fatduck 人工复验发现**。
- 建议处置: ①按 §8.3 把 UTF-8→UTF-16 收口为**一个**跨层 helper（把 aio_fopen_utf8 提为公共基建，或 runtime/io 内建等价物），
  IO-001/IO-002/AIO-002 三边界统一使用；②aio_publish Windows 分支改 CreateDirectoryW/MoveFileExW/FindFirstFileW/
  _wunlink/_wrmdir，失败处把 GetLastError+FormatMessage 写进诊断（不要只留 errno）；③IO_001/002/003 文档把「上层适配层」
  的具体归属写实（哪个 target、哪个函数），否则该句属悬空引用（§12.3 一致性）；
  ④Windows profile 增一条含中文与非 ASCII 输出目录的 HiPS 发布端到端用例
- 置信度: 高（代码 + grep 实证）；影响面 中（禁执行下未实测 Fatduck 行为，已如实分栏）

### L24 分片 1-4 小结（累计 12 条：P0:3 / P1:9）
- 分布：gaia_xpsd_client 7 条、astro_image_io（AHPX/HiPS 读侧 + 测试注册面）4 条、runtime+io 与 lib/hips 边界 2 条（部分交叉）
- **本轴最硬的结论**：这些边界缺陷**全部有同仓正对照**（readPixels 已等长校验、tile_ipix 已判负、hips_join_path 已拒溢出、
  load_xpsd_file 上游三分支已回收、hiss_reader 已用 _fseeki64/fseeko、aio_util 已做 UTF-8→UTF-16）→
  **不是不知道怎么写对，而是同一仓库内新边界没有继承老边界的纪律**。这比"发现 12 个 bug"更有行动价值：
  修复方向是"对齐同仓既有正确写法"，成本极低、争议极小。
- 第二结论：其中 **3 条只在 Windows 正式平台暴露**（L24-010 的 long 窄化、L24-011 的 511 塌缩、L24-012 的 ANSI 路径），
  而 CI 主跑 Linux → 与 M5b 的「windows-main 恒零检查」和 L12 的 Windows 覆盖缺口直接咬合，构成**平台盲区闭环**。

### L24-013 IO-001 提交点缺 Windows 替换语义与 rename 前 fsync（P1）【前台补录·分片 6】
- 类别码: C_DOC_CODE_GAP（主）；次挂 G_GOV_GATE（§11 原子提交）
- 位置: runtime/io/fits_core.c::acs_fio_writer_end_v1（rename 提交点）、::fio_file_flush（仅 fflush）、
  合同 modules/services/io/include/astrocs/io/fits_stream_v1.h 的 writer 段（自述「同目录临时文件 → 全部写完后 rename」、
  「overwrite: 1=允许覆盖已存在目标 (原子替换); 0=目标已存在则失败」）
- 问题说明: ①UCRT 的 rename() 在目标已存在时**失败**（POSIX rename 才替换）⇒ overwrite=1 承诺的"原子替换"在 Windows 必走 fail 分支，
  同一份代码 Linux 能覆盖、Windows 报 IO 错误 = **平台行为分叉**（§3.1 正式入口）；②rename 前只有 fflush，**无 fsync(fd)、更无 rename 后对父目录 fsync**
  ⇒ 掉电后可能"rename 已生效而数据页未落盘"，产生名义完整、实际缺失的产品；而 §17.11 的 sha256 是在 rename 之后读回算的 → **把这种不一致永久写进 provenance**；
  ③tmp 名是 "%s.tmp.%ld.%lu"（pid + 进程内 seq），两进程同时写同一 target 时各自 tmp 不冲突、但 POSIX 下**两次 rename 都成功**，后完成者静默覆盖前者且无冲突检测。
- 正对照（同仓三处已有补偿写法）: lib/hips/src/aio_publish.cpp 的 Windows 分支用 MoveFileExA(..., MOVEFILE_REPLACE_EXISTING)
  + ERROR_NOT_SAME_DEVICE 分类、promote 前 publish_tree_fsync；aio_upm.cpp:101-108 与 module_adapters::p1_atomic_publish 都有 rename 失败重试。
- 影响: 当前生产写出未接 IO-001 → P1；**按 §8.3 收口写出即升 P0**（M9 已挂「接线即升级」条件标签）。Windows 侧表现为「重跑同一输出目录必失败」，
  与同仓其它写者的 overwrite 语义不一致，会被上层误判成 IO 故障。
- 建议处置: ①Windows 分支改 _wrename / MoveFileExW(REPLACE_EXISTING)（**与 L24-012 的 UTF-8 适配一并收口**，避免先改 ANSI 再改宽字符两遍）；
  ②提交前 fsync(fd)、rename 后 fsync 父目录（照抄 publish_fsync_one/publish_tree_fsync 口径）；③rename 返回 EEXIST 且 overwrite=1 时显式走替换路径；
  ④两平台一致性用例（同 target 二次写入 + 并发写）
- 置信度: 高（①②③均代码实证）｜related: L24-011/L24-012（同一 writer 的三个缺陷面）、S3-006（先删后 rename 可双灭，待 B-05 Windows rename 语义裁决）
- **本条由 M9 独立取证重建并标注重建来源**（M9.md §13-1）；前台代录遗漏已补齐。

### L24-014 HiPS tile DATASUM 读取接口头文档与实现返回码不符，且全仓零调用零测试（P2）【前台补录·分片 6】
- 类别码: C_DOC_CODE_GAP
- 位置: lib/astro_image_io/include/aio_hips_reader.h::aio_hips_read_tile_datasum 声明段；实现同符号（缺 DATASUM 分支）
- 问题说明: 三条同时成立：①头文档写「tile 无 DATASUM 时返回 1 并置 err」而实现该分支是 `*out = 0; return -3;` → 按文档写判断（==1 视为缺 DATASUM）
  会把 -3 当通用失败，或反之把真失败当缺字段；②**无任何现存调用者**（除声明与定义外全仓 0 命中），其码值约定从未被执行检验；
  ③它是本模块唯一以"数字含义"承载存在性的出口，与同文件其它出口用 err 串承载详情的风格不一致。
- 建议处置: 三选一并写明理由 —— 订正头文档为 -3=缺 DATASUM 并补注册用例 / 改实现返回 1 与文档一致（风险：未知外部依赖）/
  确认无规划用途则按 §14.4「禁止为不存在的调用者保留接口」从接口表与实现中移除，避免**僵尸接口充当合同**。
  定稿前须查 PUBLIC_API/API_CONTRACTS.csv 是否收录该符号。
- 置信度: 高（文档/实现差异与零调用均 grep 实证）｜related: §14.4 僵尸接口条款、L24-015（同族「对外承诺与实现对不上」）

### L24-015 SNR 目录坏行计数只自增不外露，损坏被静默当部分集（P2）【前台补录·分片 6】
- 类别码: G_GOV_GATE（主，违 §11 禁吞错）；次挂 H_NUMERIC（NO_DATA 与部分数据不可区分）
- 位置: lib/astro_image_io/src/hips/aio_hips_reader.cpp::parse_snr_catalog（`++d->bad_snr_rows;`）；读取面 aio_hips_read_snr_catalog
- 问题说明: TSV 行被判非法（列数不足/解析失败）时只累加内部计数器并 continue，返回值仍是「成功 + 实际点数」；
  `bad_snr_rows` **在全仓读取侧 0 命中**（无导出、无诊断结构、不进 status/inspect），既不改变返回码也不进任何诊断
  → 「SNR 目录坏了 40% 的行」与「这产品本来只有 60% 的 SNR 源」在接口上**完全不可区分**（与 L24-006 同一形态、不同模块不同字段）。
  另：行数上限 10,000,000 钳制生效时同样只走截断路径而无状态码。
- 影响: SNR 目录被 Phase2 采样与 stage2 用作选源依据（本会话复验调用方为**六处**），部分缺失会改变选源统计而无告警；
  属科学输入静默降级，读路径可达。
- 建议处置: ①把 bad_snr_rows 经既有 inspect/diag 出口外露，或 open 时按占比超阈值置输入损坏状态码（与 §14.4 损坏输入一致）；
  ②对"行数被上限钳制"补显式 PARTIAL/TRUNCATED 诊断码；③用例并入注册面（8 列合法 + 4 列非法混合 TSV，断言计数可见）
- 置信度: 高（代码实证）｜影响面 中（读路径可达性依 Phase2 接线）｜related: L24-006、L24-008（同一 API 的容量入参缺陷）

### L24 补录后终计：**15 条（P0:3 / P1:10 / P2:2）**，本轴收工
- 与分片 6 的计数订正一致（代理自报「18 条」为误，正确 15）；M9 已按此定档并把 L24-003 根因改挂 related: M8-F-004。
- 待复核 13 条（分片 5），其中 7 条需执行权限已由 M9 续排为 **R7–R14**。

### L24-014 tile DATASUM 接口的成功码语义与文档相反，且全仓零调用者（P2）【前台补录·代理分片】
- 类别码: C_DOC_CODE_GAP（主）；次挂 I_DOC_HYGIENE（僵尸接口）
- 位置: lib/astro_image_io/src/hips/aio_hips_reader.cpp::aio_hips_read_tile_datasum；头文件 `aio_hips_reader.h` 文档段
- 问题说明: 文档段称「`out_datasum` 接收实际计算值并返回 `1`（成功）」，实现是 `if (out) *out = 0; return 0;` ——
  `return 0` 在本模块其他接口里一律是「失败/缺件」码位，故此接口把**计算成功**编码成**其他接口的失败码**；
  同时 `aio_hips_read_tile_datasum` **全仓 0 个调用者**（除声明与定义外无第三处命中），因此该语义分叉从未被执行检验过。
- 影响: 当前是**潜在缺陷**（无调用者）；但按 §8.3 HiPS 读侧将统一收敛到 AIO 边界，接线后按文档语义写判断的调用方
  会把成功当成缺件、或把缺件当成成功，且两种误判都是**静默**的。
- 建议处置: ①改文档与实现为同一成功码（建议 `1`，与本文件 `read_hips_tile`/`read_ext_catalog` 一致）；②或**删除该零调用接口**；
  ③禁止新增第三种码位约定。建议走②，宪章 §14.4「禁止为不存在的调用者保留接口」。
- 置信度: 高（头文档原文与实现返回语句同时直读命中，零调用经 grep 全仓确认）
- related: M6b-E-002（注释溯源）、宪章 §14.4；M9 的僵尸接口面

### L24-015 HiPS tile 读取路径零尺寸上限，单张恶意 tile 即可耗尽内存（P2）【前台补录·代理分片】
- 类别码: G_GOV_GATE（主，§14.4 边界信任）；次挂 H_NUMERIC
- 位置: lib/astro_image_io/src/hips/aio_hips_reader.cpp::read_hips_tile
- 问题说明: `int64_t npix = (int64_t)nside * nside * 12;` 之后**无条件** `std::vector<uint16_t> buffer(npix * 4);`，
  不校验 npix 与文件实际 `file_size` 的关系。NSIDE 合法上限 8192 时 `npix*4 = 805,306,368` 字节（约 0.75 GiB，uint16 路径），
  与真实 tile 文件大小无关；NUNIQ 路径 `nside²/2` 的 `std::vector<int64_t>` 同样无上限。
  对比：**同文件上游对 NUNIQ 已有校验**（`:553-556` 要求 `npix == nside²/2` 且与 `file_size` 匹配）——即同一文件里
  「一半路径有边界检查、一半没有」，与 L24-004/005/006 的成因完全一致（同仓正对照）。
- 影响: 本地或 HiPS 服务上的畸形 tile（NSIDE 字段被改大但文件很小）可让读侧立即分配数百 MB，构成 DoS 面；
  `read_hips_tile` 被 `aio_hips_read_catalog` 与 `aio_hips_read_snr_catalog` 调用（Phase2 消费路径），**读路径可达**。
- 建议处置: 分配前加 `if (npix * 4 > file_size) return 0;`（照抄 NUNIQ 分支写法）；并给 npix 一个 NSIDE≤8192 的硬上限。
- 置信度: 高（代码直读，两处对照同时命中）｜影响面 中（可达性已确认，危害为可用性而非科学错误）
- related: L24-004/L24-005/L24-006（同一「新边界未继承老边界纪律」根因）、M9 §4 平台盲区、宪章 §14.4

### ✅ M9 指出的缺口已闭合（前台补录，非臆造）
- `_merge/M9.md` §13-1 报「`_cache/L24.md` 自述仅 12 条、缺 L24-014/015」→ 实为**前台分片流水遗漏**：代理分片到达时我正在转录，
  未做「头部计数 vs 正文小节数 vs 代理自报数」三方对账就先把头部写成 12。两条 P2 现已按代理原文补录，**本轴终计 15 条（P0:3/P1:10/P2:2）**。
- L24-013 由 M9 独立取证重建（其标注的重建来源有效）；014/015 为代理原文，两者不混。
## 2. 本轴扩展实例（与第一波同事实，只挂 related 不重复定稿）
- 与前台 F00-07a 同源：properties 畸形关键字被 tests/io/test_fits_stream_contract.py 钉成期望输出（测试固化缺陷）
- 与前台 F00-07b 同源：aio_hips_writer 路径边界用 PATH_MAX 常量而非调用方缓冲区长度 → 509~511 字符区间静默截断

## 3. 反证 / 改判建议
（待后续分片）

## 4. 与其他轴的挂接
- L23（测试能失败性）：L24-003 是该轴的一个强实例（孤儿 TU + 单向闭包门）
- L26（单位链）：Phase2 采样域未见 3600 因子证据（M4 已交付该负结果）

## 5. 覆盖声明
（分片 1 未含覆盖面数字，待 L24 末片补：已扫文件数 / 命中数 / 中止点）

## 6. 待复核
（待后续分片）

## 7. 现场冲突备注
- 并发 agent 正在改 lib/core、cli、lib/hips 等，本档案锚点一律用 path::符号 + 「复核时 N」
- 真源正被 RESCUE-V3 批次持续修复，每条定稿前需按当前树重读