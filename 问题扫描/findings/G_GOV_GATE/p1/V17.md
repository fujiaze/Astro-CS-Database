# V17 · fail-open 前置缺失（分片 1：N-01..N-03，全 P1；HEAD `417775b9`，纯静态）

### V17-N-01（P1·①）**selfcheck 家族 10 TU / 16 门的"必败验证"接受"子进程没起来"** ⇒ 核心是 **127 码值碰撞**
- 断言只判 `child_rc != 0`（逐字：`const int child_rc = WIFEXITED(status) ? WEXITSTATUS(status) : -1; if (child_rc == 0) {…return 1;}`），而**四种非缺陷路径全部非零**：`execve` 失败 `_exit(127)`、信号终止（`-1`）、**fork 失败（helper 变体 A 直接 `return 127`：`p1hips`/`p1phot`/`p1star`/`p1sess`/`p1wcs`/`aio_abi`）**、组名漂移兜底（127）⇒ 一律记「必败验证通过」＝**假绿**。
- 实证：10 TU 中 `"rc == 127"` 分类命中 **0**；**同仓正对照两处**：`p1drz_tests_selfcheck.cpp:88-99`（**唯一**有 `127→failed++` 分类者）与 `cli/commands.cpp:306-316`（`spawn_failed` 三分类）⇒ 判"漏"不判"口径"。
- 改法：对齐 `p1drz`（`WIFEXITED && WEXITSTATUS==1` **且**子进程打 `FAULT_APPLIED:<名>` 标记）；**fork 哨兵码与组失败码解碰撞**。
- **related** `V4-N-05`（种子同型，不并条）、`M8-F-008`、**`V15` 乙档**（`cond` 不参求值＝同一族的另一种失效）、`C-21`、`V18`

### V17-N-02（P1·②）`SERIAL-HARDCODE` 门（三 profile、非豁免、在册）扫描面**无存在性前置** ⇒ 一个自登记起从未被扫过的死路径，门仍打 `QA-002_PASS`
- `tools/check_serial_hardcode.py:41` `files = [base] if base.is_file() else (rglob if is_dir else [])` ⇒ **目录不存在即静默空集合**；其 `PROD_FILES:17-21` 含 **`lib/cpu`**，**实测该目录不存在**（`ls` 报无法访问；`git log --all` 于 **2031 个提交零命中** ⇒ **自登记起从未扫过任何东西**），且**无实扫计数断**。
- **真实 `workers` 选路域 `lib/backend_host` 不在扫描面**（`host_services.cpp:116` `max_workers = 1` 等不被扫）。⇒ **偏向=绿、默认 CI 恒触发**。
- 同型两站（**同一判据缺陷的复发，说明需要一个门而不是三条补丁**）：`check_unit_closure.py:78-79`（docs 目录缺 ⇒ `continue` 零扫）、`check_api_docs.py:184-186`/`:246-248`（doc 缺 ⇒ `return []`/`continue` 零项对账）。**同仓正对照**：`check_module_readmes.py:24-26`（逐条存在性 `errors.append`）。
- 改法：逐条存在性即 `errors.append` + **实扫文件数 ≥ 下限断言** + 并入 `lib/backend_host`。**related** `M5b-G-11`（providers 缺口在册；本条新事实是**死路径已实际发生**）、`INDEX §3`「边界校验做一半」、`V5-N-04`、**新门 `E13`：`PROD_FILES`/清单类扫描面的每一项必须存在，且实扫数 > 0，否则红**

### V17-N-03（P1·②③）`test_p1003_drizzle_path.py::test_04` **恒 SKIP** ⇒ 可达性校验在默认 CI **零执行点**（三层叠加）
- 链条：`:58-59` 要求 `build/compile_commands.json` 否则 `skipTest` ⇒ 核链三级：①`linux-control` preset 的 `cacheVariables` **无 `CMAKE_EXPORT_COMPILE_COMMANDS`**（json 解析）；②`ci/steps/linux_build_root_graph.sh` 全文**无该开关、无 `cp`**；③全仓 grep 仅注释命中 ⇒ **CI 无生成方** ⇒ **每次 `linux-main` 走 skip、账面 OK**。
- ⇒ checker 自身**是 fail-closed 的，但永不被叫到**；再叠加 `PROD-REACH` 只注册 `--selftest`（`M5a-G-008` 在册）⇒ **「生产可达性」这一整条语义在 CI 面没有任何执行点**。本机 `build/compile_commands.json` 是本地构建产物，**恰证"本地≠CI"**。
- 改法：preset 开导出 + `cp`；或照 `test_cli_single_install.py:38-44` 的形态「**CI 环境下缺前置 ⇒ `raise`**」。**related** `M5a-G-008`（补第二层）、`L23-003`、**`V9-N-17`**、簇 1 机制①⑤、`V15-N-16`（同属"backend/arch 测试缺构建前置"）
