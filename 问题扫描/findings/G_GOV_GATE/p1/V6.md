# V6 复核新发现 · P1（本批修复打破非豁免门 / 追溯收缩）

## V6-N-01 「追溯锚订正」实际删掉 TRACEABILITY.csv 四行，直接砍断两条 FROZEN SCI 的追溯端点并使 CON-TRACEABILITY 变红
- **ID**：V6-N-01 ｜ **类别**：G_GOV_GATE ｜ **优先级**：**P1**
- **位置**：`docs/TRACEABILITY.csv`（`c3452d48` 的 4 处 `-` 行）；判据 `tools/quality/contracts/check_traceability.py:26-67`（门 `CON-TRACEABILITY`，`fast`+`linux-main`+`windows-main` 三 profile、**非豁免**）；被砍端点的权威面 `docs/science/{PSF,REJECTION,INTEGRATION,ACR_EQUIVALENCE}.md`（HEAD 仍 FROZEN T101/T107/T108/T109，ID 在 HEAD 仍被引用 31/83/86/10 处，`docs/contracts/INDEX.yaml` 仍列 downstream）
- **证据**（V6 把 checker 判据**逐行照抄成只读复算脚本**、输入换成 `git show <rev>:<path>` 的 blob）：`REV=ccc7a933 rows=67 status=PASS exit=0` → `REV=c3452d48 rows=63 status=FAIL exit=1`（`TRACE-CORE-MISSING PSF`、`TRACE-CORE-MISSING REJ`）→ `REV=HEAD` 同 FAIL。被删四行 `SCI-PSF-001`/`SCI-REJ-001`/`SCI-INT-001`/`SCI-ACR-EQUIV-001` 原本 status 均为 VERIFIED。
- **为什么是缺陷不是订正**：核心覆盖判据要求 `requirement_id` 拼串里必须含 CAL/PSF/PHOT/NOISE/DRZ/UPM/REJ/INT/ACR **九关键词**；删行后 PSF/REJ 在 63 个 id 中**零命中** ⇒ 门红。即 `fix_note` 写「CSV 补 ac_correct_frame」，**实际动作含删行**，删掉的正是两条 FROZEN SCI 的唯一追溯端点（§17.2）。
- **建议处置**：`SCI-PSF-001`/`SCI-REJ-001` **改锚恢复**（`test_ids`/`test_files` 改指 `p1psf`/`p1star`/`p1phot` 真目标即可修好门），`SCI-INT-001`/`SCI-ACR-EQUIV-001` 一并恢复；**不得以"从 CSV 删除"作为处理不可追溯项的手段**。
- **取证口径**：V6 复算脚本 `问题扫描/_verify/scripts_v6_con_traceability.py`（逐 rev 可复跑）｜ **状态**：OPEN ｜ **置信度**：高 ｜ **related**：**V6-N-04**（同批门破）、L28e-E-001（自造 ID 自证的镜像：这次是删 ID）、M8-F-004、簇 9/簇 10
- **附一条 R-6a 判例**：隔壁自报 `rows=63 ok=63` 复算**吻合**，但 **63 是缩水后的分母**（67→63）⇒ 自报计数与真实覆盖不同源，属"数字真、口径假"。

## V6-N-02 给 MATRIX 的 CSV 注入 UTF-8 BOM 令整条同构校验提前 return，且只改视图未改被自己声明为权威的 JSON
- **ID**：V6-N-02 ｜ **类别**：G_GOV_GATE ｜ **优先级**：**P1**
- **位置**：`docs/TRACEABILITY_MATRIX.csv`（BOM）；判据 `check_traceability_matrix.py::_check_csv_parity:380-390`（以 `encoding="utf-8"` 打开、**不剥 BOM**）；门 `TRACEABILITY-MATRIX`（`fast`+三 profile、**非豁免**）；权威面 `docs/TRACEABILITY_MATRIX.json`
- **证据**：`git show` 取前三字节 —— `ccc7a933: b'module_id,mo'`；`c3452d48: b'\xef\xbb\xbfmodule_id'`；HEAD 同。**BOM 是本批引入**。⇒ `header[0] = '\ufeffmodule_id' != 'module_id'` → `SCHEMA_VIOLATION` 并 **return**，**从此 JSON↔CSV 同构校验整条不再执行**（门确定 FAIL）。V6 剥 BOM 后逐列比，仍有 **5 行发散**（`MOD-astrocs-phase1-{calibration,noise-snr,photometry,star,star-psf}`，字段 `test_id/test_path/evidence_id/evidence_status/notes`）：CSV 已改指 `p1star_units/p1star_oracle/p1star_mad` 与 `EVID-P1-STAR-001`，而 **JSON 三版一字未动**仍是 `test_path=docs/algorithms/…::TEST-STAR-DESIGN-001`、`evidence_id=EVID-MISSING`、`evidence_status=MISSING`。
- **问题说明（五处同源的反向缺口）**：改的是 CSV **视图**，而该视图自己的 `notes` 里声明 JSON 为现行权威（CSV 标 SUPERSEDED）⇒ **视图与权威互斥**；且 `gen_traceability_csv.py` 下次重跑会**用 JSON 覆写 CSV**，把这次订正**静默冲回 `EVID-MISSING`**。修复会随生成器回退 = 簇 9 的又一型。
- **建议处置**：①删 BOM；②**同一订正回写 `TRACEABILITY_MATRIX.json`** 并按 `gen_traceability_csv.py` 的口径重生成 CSV（二者必须由同一命令派生）；③顺手把"CSV 是 JSON 的生成物、不得手工编辑"写进 CSV 头注或 CI 检查。
- **取证口径**：V6 逐 rev 字节比对 + 剥 BOM 后逐列比 ｜ **状态**：OPEN ｜ **置信度**：高 ｜ **related**：**V6-N-01**、C-12（证据产物新鲜度）、M8-F-004、FD-F-001（子串/结构断言族）

## V6-N-04 P5/P8 两提交新增 10 枚 ctest 目标未登记，令 CTEST-REGISTRATION（fast、非豁免）现下必红
- **ID**：V6-N-04 ｜ **类别**：G_GOV_GATE ｜ **优先级**：**P1**
- **位置**：`tests/unit/p1snr/CMakeLists.txt`（新目标 `p1snr_science_{units,oracle,negative,production,determinism}` + `p1snr_linux_{units,oracle,contract,negative,determinism}` 共 10 枚）；未登记于 `ci/checks.json`，亦不在 `ci/ctest_baseline.json` 的源清单（该清单含 `lib/snr_estimator/tests/p1noise/CMakeLists.txt` 但**不含** `tests/unit/p1snr/CMakeLists.txt`）；判据 `check_ctest_registration.py::parse_targets+evaluate`
- **证据**：V6 用 checker **原函数**（非复刻）逐 rev 复算未注册数：`ccc7a933 UNREG=0` → `dbaa2e18 UNREG=5` → `bc8f3893 UNREG=10` ⇒ **该门当前必红**。
- **问题说明**：新增测试目标本身是好事（SNR 新模型终于有锁），但**没同步 CI 面** ⇒ 按 AGENTS.md「修改代码、测试后应同步订正 ci」与 §12.3，这既是门破也是**新锁不进门**——10 枚 SNR 用例在 CI 里等于不存在，与审计簇 9「修复即扩散」同型。**两提交都不在账本 20 条内**，属全新引入。
- **建议处置**：把这 10 枚登记进 `ci/ctest_baseline.json` 源清单并补 `ci/checks.json` 的对应 `CTEST-*` 项（`waivable=false`），与 V1 正在复核的 SNR 新模型同源检查一并做。
- **取证口径**：V6 逐 rev 复算（原函数）｜ **状态**：OPEN ｜ **置信度**：高 ｜ **related**：**V6-N-01/02**（同批三连门破）、C-02（第四向：注册校验器与被校验对象生成方式不匹配）、L28e-E-001
