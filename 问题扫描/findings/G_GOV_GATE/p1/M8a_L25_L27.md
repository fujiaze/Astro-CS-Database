# 定稿 G_GOV_GATE / P1 — M8a（L25+L27）

> 合并代理 M8a｜来源 L25+L27｜验证方式：纯只读（read/grep/glob）按当前树逐条重读；锚以 `path::符号` 为准，行号仅作复核时点记录。
> 结构化比对一律采用「元素计数 + 键集合 + 逐列 grep」口径，不以 read 读到的行文本作等值判据（规避单行 >2000 字符截断）。

---

## M8a-G-003 第三方/派生代码的识别与豁免按「目录名字面」切分，登记面与真实闭包互不覆盖

- 类别: G_GOV_GATE
- 定稿优先级: P1（本轴根因，直接导致 M8a-G-001/-002/-005 无法被任何机器门看见）
- 位置（复核时点全部仍成立）:
  - `tools/quality/gen_source_index_v61.py::VENDORED_PARTS`（复核 35）、`::is_third_party`（95）、`::third_party_sources.csv 输出`（277-279）
  - `tools/gen_v19_evidence.py::is_excluded`（复核 54-59）、`::third 标记`（78-82）
  - `tools/make_rev2_capsule.py::排除段`（复核 49-50）
  - `tools/arch/check_thread_budget.py::目录剪枝`（复核 81）
  - `tools/quality/contracts/check_comments.py::排除`（复核 28-29）、`check_doc_symbols.py`（14/101）、`check_forbidden_patterns.py`（30/60）
  - `docs/algorithms/anchors/check_doc_line_anchors.py::ARCHIVE_MARKERS`（复核 45）、`ANCHOR_CONTRACT.md`（40）
  - `tools/quality/check_source_inventory.py::排除集`（复核 49）、`tools/no_legacy_production_reference.py`（32）、`tools/pack_audit_package.py`（23）、`tools/arch/build_production_execution_inventory.py`（23/29）
  - 反证（派生/拷贝的真实落点）：`lib/plate_solve/cpp/ipv/src`（13 TU，见 M8a-G-001）、`lib/healpix_db/healpix_drizzle/nanoflann.hpp::NANOFLANN_VERSION`（89=0x190）、`lib/common/healpix/healpix_core.cpp::来源头注`（4-9）
- 证据摘录:
  > gen_source_index_v61.py:35 `VENDORED_PARTS = {"third_party"}`；:277 `if path.is_file() and "third_party" in path.parts:`
  > gen_v19_evidence.py:57 `if p in ("third_party", "archive", "build", "__pycache__", ".git", "tests", …)`
  > check_thread_budget.py:81 `dirs[:] = [d for d in dirs if d not in ("archive", "third_party", "tests")]`
  > check_doc_line_anchors.py:45 `ARCHIVE_MARKERS = ("/archive/", "/legacy/", "/.git/", "/third_party/", …)`
  - 独立复算（口径：读 `evidence/v6_1_rework/tasks/R0-002/third_party_sources.csv` 全量 grep 键列）：该 vendored 清单 **1 列 4 键**（path/size_bytes/sha256/license_hint），命中的 `license_hint` 值域只有 `cfitsio (NASA)`；对 `nanoflann|ipv|healpix|plate_solve` 的检索 **0 命中**（仅命中表头行）。即清单里的"第三方"= cfitsio 一族 + 名字恰为 third_party 的目录，与本仓真实第三方/派生代码集合不交。
- 权威依据: 宪章 §16.3（审核包白名单只含"当前源码基线"，第三方许可属必含项）、§12.3-1（依赖/构建/清单一致）、§17.11（白名单/哈希/provenance 全过）；BSD-2（nanoflann 二进制再分发须复现版权声明）、BSD-3（healpix_core 源分发须保留上游版权行）、MIT（json-schema-validator）
- 问题说明: 全仓所有"第三方"判定 —— 无论豁免方向（跳过注释/行锚/线程/禁词检查）还是登记方向（第三方源清单、许可收集、审核胶囊收录）—— 都用**路径段字面量** `third_party` 作唯一判据。后果双向且互斥：①名字恰为 `third_party` 的目录被整段豁免出多数检查面与审核胶囊，其许可文本、上游一致性、"是否被改过"对审核侧不可见；②真实 vendored/派生代码只要落在别的目录（`lib/plate_solve/cpp/ipv`、`lib/healpix_db/healpix_drizzle/nanoflann.hpp`、`lib/common/healpix/healpix_core.cpp`）就不被识别为第三方，于是既没有许可登记、没有哈希锚、没有上游版本锚，也不被 `packaging/gen_sbom_input.py` 的 lock↔文档检查覆盖（见 M8a-G-004）。**判据的正确形态是"存在 LICENSE/NOTICE/SPDX/派生登记"这类出处事实，不是目录叫什么。**本仓已有正面样本可作范式：`lib/common/healpix/THIRD_PARTY_NOTICE.md`（31 行，含来源/许可/差异声明）与 cfitsio 的 lock 登记面。
- 影响: 发布审核链对第三方合规给出的"通过"结论不覆盖真实 vendored/派生集合；M8a-G-001/-002/-005 三类缺口结构性不可见；任何后续新增的 vendored 拷贝仍会以同样方式逃逸。
- 建议处置（机器门建议，供负责人落地）: ①新增 `tools/quality/check_third_party_registry.py`：以"文件出处事实"为判据 —— 出现 LICENSE/COPYING/THIRD_PARTY_NOTICE/上游 SPDX 头/上游署名行 → 必须在 `packaging/dependency-lock.json` 增 `vendored_copies` 或 `derived_code` 条目（含上游项目、版本或 tag、仓内路径、许可 SPDX id、聚合哈希、派生差异说明），缺条目即 FAIL；②豁免类脚本改为读该登记面取豁免集，禁止再写目录名字面量；③反向门：`dependency-lock` 里的 `vendored_copies[].path` 必须真实存在且许可文本可寻址（§14.2），否则 FAIL；④审核胶囊/发布打包脚本收录集与登记面同源。
- 置信度: 高（豁免判据、清单键集合、反例路径全部仓内字面可核）
- 来源: L25-005
- related: M8a-G-001、M8a-G-002、M8a-G-004、M8a-G-005（本条是三者共同根因）；L13（派生 .cpp 归类）、L11（nanoflann EXEMPT 合理，缺的是登记）

---

## M8a-G-004 「依赖锁的机器校验入口」与「preset/actions 复验」三处现时态声称在 CI 注册表零接线，且已有校验为单向

- 类别: G_GOV_GATE
- 定稿优先级: P1
- 位置: `DEPENDENCIES.md::依赖锁定 (BLD-004)`（复核 58-63）、`::工具链表`（4、26-27）、`::机器路径政策`（81-83）；`packaging/gen_sbom_input.py::verify_dependencies_md`（复核 93-107）、`::main`（145-163）；`ci/checks.json`（全文 grep，复核 415-437/1200-1227 为对照项）；`cmake/toolchain/verify_toolchain.py`（存在性）；`ci/verify_toolchain.py::docstring`（6-21）；`ci/verify_actions_lock.py`、`ci/WORKFLOW_BINDING_AUDIT.md`（17-18）
- 证据摘录:
  > DEPENDENCIES.md:61-63 「机器校验入口：`packaging/gen_sbom_input.py --root .`（lock <-> 本文一致性 + fresh configure 无机器绝对路径扫描 + SBOM 输入生成）」
  > gen_sbom_input.py:100-105 仅遍历 `lock["production_dependencies"]` / `lock["test_only_oracles"]` 断言"名称已在 DEPENDENCIES.md 提及"
  > ci/checks.json 全文 grep：`gen_sbom` **0 命中**、`BLD-004` **0 命中**、`SBOM` **0 命中**、`dependency-lock` **0 命中**、`verify_actions_lock` **0 命中**、`cmake/toolchain/verify_toolchain` **0 命中**（`.github/` 同 grep 亦 0 命中）；注册表实跑的只有 `ci/verify_toolchain.py`（:1209）
  > ci/verify_toolchain.py:6-21 自述校验对象 = policy ↔ lock 静态比对 + missing_tools 自洽 + 版本行格式
- 权威依据: 宪章 §12.3-1、§12.3-12（L0 结论可追溯到当前 SHA 的机器证据）、§17.11；§14.4（fail-fast 语义要如实）
- 问题说明: 三处独立缺口：①**无接线** —— 活动文档以现在时态把 `gen_sbom_input.py` 称为"机器校验入口"，但它既不在 `ci/checks.json` 70 项注册表内，也不被任何 workflow 调用，等于不存在；②**判据方向不足** —— 即便接线，`verify_dependencies_md` 只做 `lock ⊆ DEPENDENCIES.md` 的单向提及检查，既不做 `DEPENDENCIES.md/CMake → lock` 的反向完整性检查，也从不读构建图的 `target_link_libraries`，因此"M8a-G-002 的 GSL 被产品闭包链接却完全不在锁里"这一类缺口在数学上不可能被它发现；③**声称的 verifier 与实跑的 verifier 不同文件** —— 表格 :27 指 `cmake/toolchain/verify_toolchain.py`（存在但无人调用），注册表跑的是 `ci/verify_toolchain.py`，且后者只比 policy↔lock（不比 lock↔实况）。同族还有 `ci/verify_actions_lock.py`（在线 tag→SHA 复验，`ci/WORKFLOW_BINDING_AUDIT.md:17-18` 以完成时态声称其"维持并复验"，但注册表零命中，其 fail-closed 退出码无挂载点）。
- 影响: 依赖/许可/provenance 面（§17.11、§16.2、§16.3）实际处于无门状态；锁与文档一旦漂移不会变红；本轴 P0 两条（G-001/G-002）正是该无门状态的具体产物。
- 建议处置: ①`gen_sbom_input.py` 补反向检查（构建图外部库名集合 → lock；仓内 vendored/派生 → lock，见 M8a-G-003），并以新 check id 注册进 `ci/checks.json`（profile 至少 linux-main/windows-main，`waivable:false`）；②`cmake/toolchain/verify_toolchain.py` 与 `ci/verify_toolchain.py` 合并或订正 DEPENDENCIES.md:26-27 的指针与措辞；③`verify_actions_lock.py --offline` 注册进注册表；④同步更新 `DEPENDENCIES.md` 时态（"已接线" vs "候选入口"）。注册即生效：任一检查器接线后若判据仍恒真，另立 fail-open 条（比照 R-4 纪律）。
- 置信度: 高
- 来源: L25-006
- related: M8a-G-002、M8a-G-003、M8a-G-005；F00-03（"门禁打在假对象"同型根因，本条为依赖/许可面实例）

---

## M8a-G-005 BSD 第三方源（nanoflann / astrometry.net healpix）编入交付 DLL，许可与哈希零采集

- 类别: G_GOV_GATE
- 定稿优先级: P1
- 位置: `CMakeLists.txt::astrocs_aio`（复核 267 healpix_core.cpp）、`::astrocs_drizzle`（408-417，含 snr_evaluator.cpp）、`::QA-001 注`（425/431）；`lib/healpix_db/healpix_drizzle/nanoflann.hpp::BSD 头 + NANOFLANN_VERSION`（2-9 / 89）；`lib/healpix_db/healpix_drizzle/snr_evaluator.cpp::include`（17）；`lib/common/healpix/healpix_core.cpp::来源头注`（4-9）、`lib/common/healpix/THIRD_PARTY_NOTICE.md`（全 31 行）；`lib/hips/CMakeLists.txt::astrocs_p1_hips_writer`（41-43）；`lib/drizzle/CMakeLists.txt::nanoflann 幻影 include`（95-98）；`packaging/licenses/`（实树 3 文件）、`packaging/dependency-lock.json`（全文 grep）
- 证据摘录:
  > nanoflann.hpp:2-9 BSD License Agreement（上游署名在头内保留）；:89 `#define NANOFLANN_VERSION 0x190`
  > snr_evaluator.cpp:17 `#include "nanoflann.hpp"` → 经 `astrocs_drizzle`/`astrocs_p1_drizzle`（install_layout.cmake:104-105 交付）编入产品
  > healpix_core.cpp:4-9 「ang2pix/pix2ang 算法来源: astrometry.net healpix.c（BSD-3-Clause…）逐文件审核后迁移」+ :6 出处 URL `https:// github.com/astrometry/astrometry.net healpix.c`（"https://" 后含空格，不可定位，另见 M8a-I-004）
  > lib/drizzle/CMakeLists.txt:96 `if(EXISTS "${CMAKE_SOURCE_DIR}/third_party/nanoflann/include")` —— `third_party/` 实树仅 `third_party/nlohmann/json.hpp`（glob 复核），该守卫恒假、静默跳过
  > dependency-lock.json 全文 grep `nanoflann|healpix|astrometry` → 仅 `astrometry.net` 一条 test-only oracle 条目（:124），无 nanoflann、无 vendored healpix；`packaging/licenses/` = LICENSE-INDEX.txt + CFITSIO_LICENSE.txt + nlohmann_json.MIT.txt
- 权威依据: 宪章 §16.2（发布包至少含许可证）、§14.2（公共许可证可位于 lib/ 或根，须可寻址）、§12.3-1/-9；BSD-2 条款（二进制再分发须复现上游版权声明，nanoflann.hpp:15-19 原文）、BSD-3 条款 1
- 问题说明: 两份第三方源（BSD-2 nanoflann 1.9.0、BSD-3 astrometry.net healpix 迁移）经**非规范路径**编入交付 DLL/SO，四处登记面（`DEPENDENCIES.md`、`dependency-lock.json`、SBOM 输入、安装树许可收集）全部缺失；上游版本仅存在于头文件宏（0x190）或转述文字中，无哈希锚，日后升级无从比对。同目录的 `THIRD_PARTY_NOTICE.md` 证明本仓具备登记能力，缺的只是把它接进锁与许可白名单（根因即 M8a-G-003）。另 `lib/drizzle/CMakeLists.txt` 的 EXISTS 守卫指向一个不存在的 vendored 路径，属"幻影 vendored 目录"：既不 fail，也不命中，读者会误以为存在受管的 nanoflann 拷贝。
- 影响: 交付包许可合规与 provenance 不完整；BSD 署名义务未闭合（虽轻于 copyleft，仍属 §16.2 必含项缺失）；上游漂移无锚。
- 建议处置: lock 增 `vendored_copies`（path/上游/版本/哈希/许可 SPDX）并把这些拷贝的许可文本入 `packaging/licenses/`；幻影 include 删除或改 `message(FATAL_ERROR)`（fail-closed）；与 M8a-G-003 的登记门一并实现。
- 置信度: 高
- 来源: L25-003
- related: M8a-G-003、M8a-G-004、M8a-I-004、M8a-C-004；L03-007（同目录 GPL 冲突定性由他轴，本条只落登记与许可收集增量）

---

## M8a-G-006 MODULE-READMES 门确数覆盖 5/43、要素核验 0 项，且实测放行错锚（裁决：维持 P1，附与 fail-open 三家的实质差别）

- 类别: G_GOV_GATE
- 定稿优先级: P1（前台建议升 P0，本代理裁决维持 P1，理由见"问题说明"末段）
- 位置: `tools/check_module_readmes.py::MODULES`（复核 8-18）、`::合同 ID 正则`（29）、`::引用文件存在性`（38-44）、`::"L2" 字样`（45-46）；`ci/checks.json::MODULE-READMES`（复核 415-437）；`docs/standards/DOCUMENTATION_STANDARD.md::module 文档要素`（复核 8-10）；被检 5 份：`lib/phase1/{stars,wcs,photometry,noise}/README.md`、`lib/phase3_session/README.md`；锚核对：`docs/contracts/INDEX.yaml`
- 证据摘录:
  > ci/checks.json:415-437 `"id": "MODULE-READMES"`，command `python3 tools/check_module_readmes.py`，profiles `fast/linux-main/windows-main`，`"waivable": false`
  > check_module_readmes.py:29 `if not re.search(r"(P\d-\d{3}|SCI-|ALG-)", t)`；:45 仅判 `"L2" in t`
  > lib/phase1/wcs/README.md（全文 10 行）「合同: `P1-004` / `SCI-WCS-001` (docs/contracts/INDEX.yaml)」；stars=「P1-003」、photometry=「P1-004」、noise=「P1-005」
  - 独立复算（口径：对 `docs/contracts/INDEX.yaml` 单文件 grep，键 = `- id:`）：`P1-003|P1-004|P1-005|P1-006` **0 命中**；`SCI-WCS-001` 等 `SCI-` 形式的 id **存在**。即被检 4 份 README 的"合同"锚里，任务号那一半在它所声称的注册表中不存在，门仍 PASS（正则只看字样）。
  - 覆盖面复算（口径：glob `**/README.md`，剔除 规程 §0.4 免报区 + `.pytest_cache/**` 自动生成 + `docs/archive/**` + `engineering/control/**` + vendored cfitsio + 根 README 自身）= **42** 份（L27 原报 43，差 1 源于是否计入根 README；不影响结论量级）。门实覆 **5** 份 = 11.9%；对 92 个"应具文档"目录单元 = 5.4%。
  - module.yaml 侧复算（口径：glob `lib/**/module.yaml`）= **21** 份；`ci/checks.json` 全文 grep `module\.yaml|module_status` **0 命中**（唯一命中是 :1209 的 `ci/verify_toolchain.py` 参数行，非 manifest 语义）。
- 权威依据: 宪章 §12.3-1（manifest/注册表/构建/产品清单一致）、§8.4（每算法模块必备 README.md + module.yaml + SCI/ALG/API/TEST 链接）、§12.3-3（实现引用有效 SCI/ALG）；负责人要求「每子库 README：功能/用途/相关文档/接口代码」
- 问题说明: 该门是"名字承诺 ≠ 实际判据"的确数实例：覆盖面 5/42，21 份 manifest 语义 0 校验，负责人 8 要素（功能/定位/相关文档/接口/构建安装/测试/DISP 偏差/状态词）核验 0 项，且它核验的方式只是"文件名以子串出现 + 该文件在 `lib/` 或 `tests/unit/` 下存在"，对锚是否可解析到注册表不作判定 —— 已实证放行 4 份 README 的错锚。**裁决维持 P1 的实质差别**（与三家 P0 fail-open 对比）：①与注释门空壳、线程预算门 0/0 自证、AST-API 恒真三条的区别在于**判据是否恒真**。那三条的断言在结构上永不可能为假（空集/自指/只比字样），任何输入都绿灯，属"检查器不存在"级别；本门的断言是可假的 —— 删掉 `lib/phase1/wcs/wcs_tan.cpp` 或改 README 不含 "L2"，它会变红。②那三条守护的是 §12.3 明列的机器一致性项（-5 AST/API 一致、-8 并发文档一致），条文存在且要求机器门，门却恒真 → 直接使"已验证声明"失去证据，属发布阻断；本门守护的"模块 README 8 要素完备性"在宪章/标准中**没有对应条文**（§8.4 只要求文件存在 + ID 链接；`DOCUMENTATION_STANDARD.md:8-10` 列 12 要素但不规定落点与最小断言，负责人口头要求无文本支撑，见 M8a-I-001）→ 缺的是"更强的门"，不是"恒真的门"。③后果面：本门失效让文档类问题不变红，但产品不合规/科学错误的证据链由别的门承担，不构成交付阻断。④**条件升级条款（比照 R-4）**：若负责人把 §12.3-1/-3 判为要求"README 声明的合同锚必须可解析到 INDEX.yaml"，则本门在已检 5 份上给出绿灯即为对现存交付物的假验证声明，届时应升 P0 并与 M8a-E-001 并档。
- 影响: README 完备性/正确性整类问题（本合并件其余全部 L27 条目）在该门下不可能变红；§12.3-1 在 L2 面事实失效；错锚继续以"合同"名义被下游任务引用。
- 建议处置: ①门改目录驱动枚举（`lib/**/README.md` + `lib/**/module.yaml` + `modules/**/README.md`，豁免须显式登记）；②判据升级为可解析断言：每个 `P\d-\d{3}`/`SCI-`/`ALG-`/`DATA-`/`API-`/`TEST-` 引用必须能在 `docs/contracts/INDEX.yaml` 的 `- id:` 键集合中找到，否则 FAIL；③最小 8 要素：文件须含"状态词"（取自 `docs/owner/RELEASE_STATUS.md` §0 阶梯）、"构建 target"、"测试锚"三类可 grep 段落；④`ci/checks.json` 增 `changed_paths` 覆盖 `lib/**/README.md`、`lib/**/module.yaml`；⑤同步为 `module.yaml` 建语义门（module_status/entrypoint 与产品清单三方比对，见 M8a-C-002）。
- 置信度: 高（覆盖面、判据、错锚零命中均为可复算确数）
- 来源: L27-002
- related: L12-022（同一门的事实面，本条补确数与错锚实证）、M8a-G-007、M8a-C-002、M8a-E-001、M8a-I-002、M8a-I-001；判词层级：行锚系统性失效主落 M6b-E-002，本条不重复"系统性"判词

---

## M8a-G-007 生成器自述「checker 以本生成器输出为源」不实：registry 无 diff 门，手改页与 descriptor 双向漂移

- 类别: G_GOV_GATE
- 定稿优先级: P1（前台建议升 P0，本代理裁决维持 P1）
- 位置: `tools/quality/gen_module_readmes.py::docstring`（复核 1-8）、`::OUT_DIR`（17）、`::MODULE_README_GEN_PASS`（143）、`::git rev-parse HEAD`（20）；`ci/checks.json`（全文 grep `gen_module_readmes` 0 命中）；`docs/DOCUMENT_INDEX.yaml::registry 条目 notes`（复核 383-458）；`docs/modules/registry/astrocs.phase2.coverage.md::front matter`（复核 1-9）；`lib/core/src/module_adapters.cpp::p2_coverage_descriptor`（复核 762-779）；`docs/modules/registry/astrocs.phase1.session.md:14-18`、`astrocs.phase1.star-detection.md:17`、`astrocs.phase2.reject.md:13-16`、`astrocs.phase2.integrate.md:13-16`
- 证据摘录:
  > gen_module_readmes.py:4-7 「以 lib/core/src/module_adapters.cpp 为唯一源(registry 生产模块)…」「**非手工清单, checker 以本生成器输出为源**」
  > ci/checks.json 全文 grep：`gen_module_readmes` **0 命中**（对照：同目录 `tools/arch/check_thread_budget.py`:564、`tools/quality/contracts/check_comments.py`:782 均有注册项）
  > 手写页与生成页共存同一目录且互认：DOCUMENT_INDEX.yaml:392 「P1-HIPS-DOC **手写合同页**（registry 无 hips_writer descriptor，生成器不覆盖；gen_module_readmes.py 以 module_adapters.cpp 为源——**重生成时须排除/保留本页**）」；:401/:407/:410/:416/:419/:428/:431… 同类注记共 **19** 条含"手写"
  > 漂移实证（键集合比对）：registry 页 front matter `upstream: [SCI-P2-COV-001, ALG-COV-001, API-COV-001, API-P2-001]` vs descriptor 现值 `d.alg_id = "ALG-P2-COV-001"; d.data_id = "DATA-P2-COV"; d.api_id = "API-P2-001"`（module_adapters.cpp:773-777）—— `ALG-P2-COV-001` 与页面写的 `ALG-COV-001` 两名并存，前者在 `docs/` 全域 grep **0 命中**
  - 分母复算（口径：glob `docs/modules/registry/*.md` vs grep `d\.module_id = \"`）：registry 页 **26**，descriptor module_id **22**；集合差 = 页面多 4（`astrocs.phase2.session`、`astrocs.phase1.session`、`astrocs.phase1.star-detection`、`astrocs.phase1.hips-writer`），descriptor 无缺页 → 差集全为手写页，与 DOCUMENT_INDEX 的"生成器不覆盖"注记一致。
- 权威依据: 宪章 §12.3-1/-9、§12.2（L2 与实现一致）、§12.3-12（结论可追溯到机器证据）
- 问题说明: ①生成器声明自己是 checker 的证据源，但注册表里没有以它为源的项 —— 声明为假，registry 26 页与 22 个 descriptor 之间不存在任何 diff 门；②由于无门，"手写页与生成页共存"只能靠 `DOCUMENT_INDEX.yaml` 的 notes 用自然语言叮嘱"重生成时须保留"（19 条），一旦有人跑重生成，覆盖行为无任何机器保护；③已经漂移：coverage 页的 `ALG-COV-001` 与 descriptor 的 `ALG-P2-COV-001` 两名并存，而后者在 docs 面不存在（详见 M8a-E-001），生成面/代码面/docs 面各说各话；④模板对全部模块写死 blanket 断言（"确定性=固定顺序输出"等）并对 `module_status=CONTRACT_READY`/`entrypoint=MISSING` 的模块也用现状口吻，front matter status 一律 `ACTIVE`，与 `docs/owner/RELEASE_STATUS.md` §0 状态词阶梯无映射（26 页 grep `^status: ACTIVE` = 26/26）。
- 影响: 模块端口/DATA/单位查询落点（registry 页）不可信；"已验"类 blanket 断言与 §13.1 每模块必备项脱钩；重生成即静默毁档的风险无门拦截。
- 建议处置: 二选一并写进文档：①把生成器接成真门 —— 生成到临时区与仓内页 diff，非空即 FAIL（手写页进显式豁免清单，豁免须带 owner+理由）；②放弃"checker 以本生成器输出为源"表述，把手写合同页迁出 `docs/modules/registry/` 到独立目录，模板去掉 blanket 已验断言、status 词表改取 §0 阶梯。
- 置信度: 高（0 接线、页/描述符集合差、漂移键名均为确数）
- 来源: L27-003
- related: M8a-G-006（裁决同一族，一并维持 P1）、M8a-E-001、M8a-G-008（L27-001 分域矛盾）；判词层级：矩阵双头/无据 VERIFIED 主落 M6b-E-001/G-001，本条只落 README/manifest 一致性面

---

## M8a-G-008 分层标准把宪章 §12.2 的 L2 定义改写，并把外部 Wiki 置于权威链首位

- 类别: G_GOV_GATE
- 定稿优先级: P1
- 位置: `docs/README-DOCS.md::分层表/权威链`（复核 1-22）；`ASTROCS_PROJECT_CONSTITUTION.md §12.2`（复核 423-429）、`§1.1`（17-28）、`§1.2`（30-32）；`docs/standards/DOCUMENTATION_STANDARD.md:3`（把分层权威让给 README-DOCS）
- 证据摘录:
  > README-DOCS.md:8 「L2 算法规范 `docs/algorithms/*.md`」（并 L1=science、L5=docs/modules）；:16-20 「权威链：Wiki(核心约束) → … 矛盾以 Wiki 为准」；:13 「追溯 `docs/TRACEABILITY.csv`（唯一矩阵）」
  > 宪章 §12.2:425-427 「L1：SCI、ALG、DATA、ARCH、API/ABI 规范；L2：**模块 README、manifest、函数合同、实现和测试说明**」
  > 宪章 §1.1:17 「本文是 AstroCS 的最高工程约束…规范优先级为：1. 本文 …7. 模块 README、源码、测试和机器生成证据」
  > DOCUMENTATION_STANDARD.md:3 「权威文档分层（L0-L5）见 docs/README-DOCS.md。」
- 权威依据: 宪章 §1.1（唯一最高工程约束 + 八层顺序）、§1.2（修改权仅在负责人）、§12.2
- 问题说明: 一份被 DOCUMENTATION_STANDARD 指定为分层权威的 L 级文档，把宪章定义的 L2（模块 README/manifest）改写成 `docs/algorithms`，并把"模块 README"挤出各层（`docs/modules` 记为 L5）；同时以"矛盾以 Wiki 为准"把仓库外的 wiki 本地克隆（`AstroCS.wiki/`，属用户/资料区）置于权威链首位 —— 与 §1.1 第 1 位是宪章、第 7 位才是模块 README 的排序直接冲突，且 §1.2 明定 Agent 无权重排。其 :13 的"唯一矩阵"表述与 F00-01 同事实，不在此重复计数。
- 影响: 所有"模块文档该写哪、按哪层模板查、与谁冲突以谁为准"的判定与门（DOC-INDEX、docs_machine_consistency）建在这份与宪章相悖的分日上；负责人点名的"每子库 README"义务因此在本仓规范里无稳定归属层，是 README 域长期无人追责的同根问题（见 M8a-I-001）。
- 建议处置: README-DOCS 按 §12.2 重标（L1=SCI/ALG/DATA/ARCH/API，L2=模块 README/manifest/函数合同/实现与测试说明，L3=实现细节），`docs/modules/**` 定位为 L2 生成视图；权威链首改宪章，删除或改写"矛盾以 Wiki 为准"为"wiki 为镜像、以仓内宪章为准"；DOCUMENTATION_STANDARD 的分层指针随订正复述 §12.2 而不外链。
- 置信度: 高
- 来源: L27-001
- related: M8a-I-001（规范缺口）、F00-01（唯一矩阵同源）、L17-012（README 权威栏反向覆盖同族）

---
