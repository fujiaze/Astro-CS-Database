# 定稿 G_GOV_GATE / P1 — M8a（L25+L27）

> 合并代理 M8a｜来源 L25+L27｜验证方式：纯只读（read/grep/glob）按当前树逐条重读；锚以 `path::符号` 为准，行号仅作复核时点记录。
> **0 命中判据三级复核声明（前台新令）**：本文件所有"0 命中/不存在"判据均经 ①整句 grep ②放宽字符集与大小写关键词 grep ③定点 read 三级复核；放宽级结果记于 `_merge/M8a.md §12`。未据任何"报告称未命中"撤证据。
> 结构化比对一律采用「元素计数 + 键集合 + 逐列 grep」口径，不以 read 读到的行文本作等值判据（规避单行 >2000 字符截断）。
> 发布阻断级的许可/依赖面已按前台指令**并为一条**（`p0/M8a_L25_L27.md` 的 M8a-G-001，一条四面）；本文件只放 P1 级的门与治理条目。

---

## M8a-G-003 第三方/派生代码的识别与豁免按「目录名字面」切分，登记面与真实闭包互不覆盖

- 类别: G_GOV_GATE
- 定稿优先级: P1（本轴根因，直接导致 M8a-G-001、-005、-009 无法被任何机器门看见）
- 位置（复核时点全部仍成立）:
  - `tools/quality/gen_source_index_v61.py::VENDORED_PARTS`（复核 35）、`::is_third_party`（95）、`::third_party_sources.csv 输出`（277-279）
  - `tools/gen_v19_evidence.py::is_excluded`（54-59）、`::third 标记`（78-82）；`tools/make_rev2_capsule.py::排除段`（49-50）
  - `tools/arch/check_thread_budget.py::目录剪枝`（81）；`tools/quality/contracts/check_comments.py::排除`（28-29）；`check_doc_symbols.py`（14/101）；`check_forbidden_patterns.py`（30/60）
  - `docs/algorithms/anchors/check_doc_line_anchors.py::ARCHIVE_MARKERS`（45）、`ANCHOR_CONTRACT.md`（40）
  - `tools/quality/check_source_inventory.py::排除集`（49）；`tools/no_legacy_production_reference.py`（32）；`tools/pack_audit_package.py`（23）；`tools/arch/build_production_execution_inventory.py`（23/29）
  - 反证（派生/拷贝的真实落点）：`lib/plate_solve/cpp/ipv/src`（13 TU，见 M8a-G-001）、`lib/healpix_db/healpix_drizzle/nanoflann.hpp::NANOFLANN_VERSION`（89 = 0x190）、`lib/common/healpix/healpix_core.cpp::来源头注`（4-9、337-341）
- 证据摘录:
  > gen_source_index_v61.py:35 `VENDORED_PARTS = {"third_party"}`；:277 `if path.is_file() and "third_party" in path.parts:`（同一判据用于生成 `third_party_sources.csv`）
  > gen_v19_evidence.py:57 `if p in ("third_party", "archive", "build", "__pycache__", ".git", "tests", …)`；check_thread_budget.py:81 `dirs[:] = [d for d in dirs if d not in ("archive", "third_party", "tests")]`；check_doc_line_anchors.py:45 `ARCHIVE_MARKERS = ("/archive/", "/legacy/", "/.git/", "/third_party/", …)`
  - 独立复算（口径：读 `evidence/v6_1_rework/tasks/R0-002/third_party_sources.csv` 全量并按列 grep 键值）：表头 = **path,size_bytes,sha256,license_hint 四列**；数据行 `license_hint` 值域只有 `cfitsio (NASA)` 一族；对 `nanoflann|ipv|healpix|plate_solve` 四词 grep = **0 数据命中** → 清单里的"第三方" = cfitsio 一族 + 名字恰为 `third_party` 的目录，与本仓真实第三方/派生代码集合不交。
- 权威依据: 宪章 §16.3（审核包白名单只含"当前源码基线"，第三方许可属必含项）、§12.3-1（依赖/构建/清单一致）、§17.11（白名单/哈希/provenance 全过）；BSD-2（nanoflann 二进制再分发须复现版权声明）、BSD-3（healpix_core 源分发须保留上游版权行）、MIT（json-schema-validator）
- 问题说明: 全仓所有"第三方"判定 —— 无论豁免方向（跳过注释/行锚/线程/禁词检查、审核胶囊排除）还是登记方向（第三方源清单、许可收集、SBOM 输入）—— 都用**路径段字面量** `third_party` 作唯一判据。后果双向且互斥：①名字恰为 `third_party` 的目录被整段豁免出多数检查面与审核胶囊，其许可文本、上游一致性、"是否被改过"对审核侧不可见；②真实 vendored/派生代码只要落在别的目录（`lib/plate_solve/cpp/ipv`、`lib/healpix_db/healpix_drizzle/nanoflann.hpp`、`lib/common/healpix/healpix_core.cpp`）就不被识别为第三方，于是既没有许可登记、没有哈希锚、没有上游版本锚，也不被 `packaging/gen_sbom_input.py` 的 lock↔文档检查覆盖（见 M8a-G-004）。**判据的正确形态是"是否存在 LICENSE/NOTICE/SPDX/派生登记"这类出处事实，不是目录叫什么。**本仓已有正面范式可抄：`lib/common/healpix/THIRD_PARTY_NOTICE.md`（来源/许可/逐条差异/外部验证依赖四节齐备）与 cfitsio 的 lock 登记面。
- 影响: 发布审核链对第三方合规给出的"通过"结论不覆盖真实 vendored/派生集合；M8a-G-001（含其面③两例）、-005、-009 三类缺口结构性不可见；任何后续新增的 vendored 拷贝仍会以同样方式逃逸。
- 建议处置（**机器门建议**，供负责人落地）: ①新增 `tools/quality/check_third_party_registry.py`：以"文件出处事实"为判据 —— 目录或文件出现 LICENSE/COPYING/THIRD_PARTY_NOTICE/上游 SPDX 头/上游署名行/"移植自|参考"字样 → 必须在 `packaging/dependency-lock.json` 有 `vendored_copies` 或 `derived_code` 条目（上游项目、版本或 tag/commit、仓内路径、许可 SPDX id、聚合哈希、派生差异说明），缺条目即 FAIL；②各豁免脚本改为从该登记面取豁免集，**禁止再写目录名字面量**；③反向门：登记面每条 `path` 必须真实存在且许可文本可寻址（§14.2），否则 FAIL；④审核胶囊/发布打包收录集与登记面同源；⑤该门与 M8a-G-004 的双向 lock 检查合并为一个 check id 注册进 `ci/checks.json`（`waivable:false`）。
- 置信度: 高（豁免判据、清单键值域、反例路径全部仓内字面可核）
- 来源: L25-005
- related: M8a-G-001（该根因造成的 P0 后果）、M8a-G-005、M8a-G-009、M8a-G-004（门缺失面）；L13（派生 .cpp 归类）、L11（nanoflann 豁免本身合理，缺的是登记）

---

## M8a-G-004 「依赖锁的机器校验入口」「preset/actions 复验」等现时态声称在 CI 注册表零接线，且已接的检查是单向

- 类别: G_GOV_GATE
- 定稿优先级: P1
- 位置: `DEPENDENCIES.md::依赖锁定 (BLD-004)`（复核 58-63）、`::工具链表`（4、26-27）、`::机器路径政策`（81-83）；`packaging/gen_sbom_input.py::verify_dependencies_md`（93-107）、`::main 输出`（145-163）、`::SCAN_SCOPE`（29）；`ci/checks.json`（全文 grep）；`cmake/toolchain/verify_toolchain.py`（存在性）；`ci/verify_toolchain.py::docstring`（6-21）；`ci/verify_actions_lock.py` 与 `ci/WORKFLOW_BINDING_AUDIT.md`（17-18）
- 证据摘录:
  > DEPENDENCIES.md:61-63 「机器校验入口：`packaging/gen_sbom_input.py --root .`（lock <-> 本文一致性 + fresh configure 无机器绝对路径扫描 + SBOM 输入生成）」
  > gen_sbom_input.py:100-105 的判据实体只遍历 `lock["production_dependencies"]` 与 `lock["test_only_oracles"]` 断言"名称已在 DEPENDENCIES.md 提及"（**单向：lock → 文档**）；:163 打印 BLD-004 PASS 字样；:29 `SCAN_SCOPE` = 构建输入清单，**不读构建图**
  > `ci/checks.json` 全文 grep（放宽级亦 0，见 §12-3）：`gen_sbom`、`SBOM`、`BLD-004`、`dependency-lock`、`verify_actions_lock`、`gen_module_readmes` 各 **0 命中**；唯一含 lock 字样的两行（:1213/:1224）是 TOOLCHAIN-VERIFY 的 inputs 指向 `ci/toolchain.lock.json`，与依赖锁无关。`.github/` 两域同模式亦 0 命中。
  > 对照：同文件 :564 `check_thread_budget.py`、:782 `check_comments.py`、:1209 `ci/verify_toolchain.py` 均正常注册 → "该面完全没进注册表"不是机制问题，是这些检查未接。
  > `DEPENDENCIES.md:26-27` 把 `cmake/toolchain/verify_toolchain.py` 称为 preset/.vsconfig 漂移的校验入口；该文件**存在但全仓 0 引用**（`ci`、`.github` 两域 grep 文件名 = 0）；实跑的是**另一个同名文件** `ci/verify_toolchain.py`，其 docstring 6-21 自述判据 = policy ↔ lock 静态比对 + missing_tools 自洽 + 版本行格式（**不校验 lock ↔ 实况**）。
- 权威依据: 宪章 §12.3-1、§12.3-12（L0 结论须可追溯到当前 SHA 的机器证据）、§17.11；§14.4（fail-fast 语义要如实）
- 问题说明: 三层缺口叠加：①**无接线** —— 活动文档以现在时态把 `gen_sbom_input.py` 称为"机器校验入口"，但它既不在 70 项注册表内、也不被任何 workflow 调用，等于不存在；②**判据方向不足** —— 即便接线，`verify_dependencies_md` 只做 lock ⊆ DEPENDENCIES.md 的单向提及检查，既不做 文档/CMake → lock 的反向完整性检查，也不读构建图的 `target_link_libraries`，所以「M8a-G-001 的 GSL 被产品闭包链接却完全不在锁里」这一类缺口在数学上不可能被它发现；③**声称的 verifier 与实跑的 verifier 是两个文件** —— 读者按文档找 `cmake/toolchain/verify_toolchain.py` 会以为有门，实际门在别处且判据不同。同族还有 `ci/verify_actions_lock.py`（在线 tag→SHA 复验；`ci/WORKFLOW_BINDING_AUDIT.md:17-18` 以完成时态声称其"维持并复验"，但注册表 0 命中，其 fail-closed 退出码无挂载点）。
- 影响: 依赖/许可/provenance 面（§17.11、§16.2、§16.3）实际处于无门状态；锁与文档一旦漂移不会变红；本域 P0 条（M8a-G-001）正是该无门状态的直接产物。
- 建议处置: ①`gen_sbom_input.py` 补反向检查（构建图外部库名集合 → lock；M8a-G-003 的出处事实集合 → lock），并以新 check id 注册进 `ci/checks.json`（profile 至少 linux-main/windows-main，`waivable:false`）；②`cmake/toolchain/verify_toolchain.py` 与 `ci/verify_toolchain.py` 合并或订正 DEPENDENCIES.md 的指针与措辞；③`verify_actions_lock.py --offline` 注册进表；④同步把 `DEPENDENCIES.md` 的"机器校验入口"改为如实时态（候选入口/已接线）。**注册即生效条款**：任一检查器接线后若判据仍恒真（例如只比"名称被提及"），另立 fail-open 条，比照 R-4。
- 置信度: 高
- 来源: L25-006
- related: M8a-G-001、M8a-G-003、M8a-G-005、M8a-C-006；F00-03（"门禁打在假对象"同型根因，本条是依赖/许可面实例）；判词层级：不可达门主落 M7，本条不写系统性判词

---

## M8a-G-005 BSD 第三方源（nanoflann / astrometry.net healpix）编入交付 DLL，许可与哈希零采集

- 类别: G_GOV_GATE
- 定稿优先级: P1
- 位置: `CMakeLists.txt::astrocs_aio`（复核 267 healpix_core.cpp）、`::astrocs_drizzle`（408-417，含 snr_evaluator.cpp）、`::QA-001 注`（425/431）；`lib/healpix_db/healpix_drizzle/nanoflann.hpp::BSD 头 + NANOFLANN_VERSION`（2-9 / 89）；`lib/healpix_db/healpix_drizzle/snr_evaluator.cpp::include`（17）；`lib/common/healpix/healpix_core.cpp::来源头注`（4-9）、`lib/common/healpix/THIRD_PARTY_NOTICE.md`（全 31 行）；`lib/hips/CMakeLists.txt::astrocs_p1_hips_writer`（41-43）；`lib/drizzle/CMakeLists.txt::nanoflann 幻影 include`（95-98）；`packaging/licenses/`（实树 3 文件）、`packaging/dependency-lock.json`（全文）
- 证据摘录:
  > nanoflann.hpp:2-9 BSD License Agreement（上游署名在头内保留）；:89 `#define NANOFLANN_VERSION 0x190`（上游版本只存在于宏里）
  > snr_evaluator.cpp:17 `#include "nanoflann.hpp"` → 经 `astrocs_drizzle`/`astrocs_p1_drizzle`（`cmake/install_layout.cmake:104-105` 列入交付）编入产品；根 CMakeLists.txt:425 注释自认 QA-001: nanoflann.hpp (BSD-3, vendored 第三方) 经本 target 引号 include 引入
  > healpix_core.cpp:4-9 「ang2pix/pix2ang 算法来源: astrometry.net healpix.c（BSD-3-Clause…）逐文件审核后迁移」；同 TU :338-339 又写「邻居算法移植自官方 HEALPix C++ (Healpix_3.83 … **GPL-2+ 参考**)」→ 该 TU 直编进 `astrocs_p1_hips_writer`（`lib/hips/CMakeLists.txt:41`）与 `astrocs_aio`（根 :267）
  > `lib/drizzle/CMakeLists.txt:95-98` `if(EXISTS "${CMAKE_SOURCE_DIR}/third_party/nanoflann/include")` —— `third_party/**` 实树经 glob 复核**仅 `third_party/nlohmann/json.hpp` 一个文件**，该守卫恒假且静默跳过（既不 FAIL 也不告警）
  > 登记面复算：`packaging/licenses/` = `LICENSE-INDEX.txt` + `nlohmann_json.MIT.txt` + `CFITSIO_LICENSE.txt`（**无 GSL / Siril / HEALPix / nanoflann 文本**）；`DEPENDENCIES.md` 对 `nanoflann|healpix|astrometry` 的命中只有 test-only oracle 一栏；`dependency-lock.json` 无 nanoflann、无 vendored healpix 条目
- 权威依据: 宪章 §16.2（发布包至少含许可证）、§14.2（公共许可证可位于 `lib/` 或仓库根，须可寻址）、§12.3-1/-9；BSD-2 条款（二进制再分发须复现上游版权声明，`nanoflann.hpp:15-19` 原文即此义务）、BSD-3 条款 1
- 问题说明: 两份第三方源（BSD-2 nanoflann 1.9.0、BSD-3 astrometry.net healpix 迁移 + GPL-2+ 官方 HEALPix 参考的邻居表）经**非规范路径**编入交付 DLL/SO，四处登记面（`DEPENDENCIES.md`、`dependency-lock.json`、SBOM 输入、安装树许可收集）全部缺失；上游版本仅存在于头文件宏或转述文字，无哈希锚，日后升级无从比对。同目录的 `THIRD_PARTY_NOTICE.md` 证明本仓**具备**登记能力，缺的只是把它接进锁与许可白名单（根因即 M8a-G-003）。另 `lib/drizzle/CMakeLists.txt` 的 EXISTS 守卫指向不存在的 vendored 路径，属"幻影 vendored 目录"：读者会误以为存在受管的 nanoflann 拷贝。
- 影响: 交付包许可合规与 provenance 不完整；BSD 署名义务未闭合（虽轻于 copyleft，仍属 §16.2 必含项缺失）；上游漂移无锚。
- 建议处置: lock 增 `vendored_copies`（path/上游/版本/哈希/许可 SPDX）并把许可文本入 `packaging/licenses/`；幻影 include 删除或改 `message(FATAL_ERROR)`（fail-closed）；与 M8a-G-003 的登记门一并实现（同一 check id）。
- 置信度: 高
- 来源: L25-003
- related: M8a-G-001（许可传染与派生署名主条）、M8a-G-003、M8a-G-004、M8a-I-004、M8a-C-004；L03-007（同目录 GPL 冲突定性由他轴）

---

## M8a-G-006 MODULE-READMES 门确数覆盖 5/43、要素核验 0 项且实测放行错锚（裁决：维持 P1，附与三家 P0 的实质差别）

- 类别: G_GOV_GATE
- 定稿优先级: P1（前台建议升 P0，本代理裁决**维持 P1**，理由见"问题说明"末段与 `_merge/M8a.md §4.2`）
- 位置: `tools/check_module_readmes.py::MODULES`（复核 8-18）、`::合同 ID 正则`（29）、`::引用文件存在性`（38-44）、`::"L2" 字样`（45-46）；`ci/checks.json::MODULE-READMES`（复核 415-437）；`docs/standards/DOCUMENTATION_STANDARD.md::module 文档要素`（复核 5-12）；被检 5 份：`lib/phase1/stars/README.md`、`lib/phase1/wcs/README.md`、`lib/phase1/photometry/README.md`、`lib/phase1/noise/README.md`、`lib/phase3_session/README.md`；锚核对：`docs/contracts/INDEX.yaml`
- 证据摘录:
  > ci/checks.json:415-437 `"id": "MODULE-READMES"`，command `python3 tools/check_module_readmes.py`，profiles `fast/linux-main/windows-main`，`"waivable": false`，`changed_paths: ["modules/**","lib/**","README.md"]`
  > check_module_readmes.py:29 `if not re.search(r"(P\d-\d{3}|SCI-|ALG-)", t)`；:38-44 只检查三个列出的文件名以子串出现且文件在 `lib/` 或 `tests/unit/` 下存在；:45-46 只判 `"L2" in t` —— **`DOCUMENTATION_STANDARD.md:5-12` 列的 12 项 module 要素，本门核验 0 项**
  > `lib/phase1/wcs/README.md:3` 「- 合同: `P1-004` / SCI-WCS-001 (docs/contracts/INDEX.yaml)」；stars = 「P1-003」、photometry = 「P1-004」（与 wcs 同号）、noise = 「P1-005」
  - 错锚复算（口径：对 `docs/contracts/INDEX.yaml` 单文件 grep `- id:` 得键列，再放宽连字符族 grep）：该文件 `- id:` 键 100+ 个，全部形如 `SCI-CAL-001`/`ALG-*`/`API-*`；**`P1-003`/`P1-004`/`P1-005` 精确 grep = 0，放宽半角/全角/各类连字符变体仍 = 0**（同模式在 `docs` 全域 106 命中，但零命中恰好落在 INDEX.yaml，故非假阴；见 §12-4）。即被检 4 份 README 的"合同"锚里任务号那一半在它所声称的注册表中不存在，而门仍 PASS。
  - 覆盖面复算（口径：glob `**/README.md` = 全仓 **951** 路径，按 规程 §0.4 免报区 + 非真源逐条剔除：`run/**` 847（影子树）、**`问题扫描/**` 37（本扫描自写档案，依 M2b 通则不计真源）**、`.pytest_cache/**` 5、`engineering/control/**` 9、`docs/archive/**` 2、`工程控制/**` 4、`reports/**` 1、`BASS DR3/**` 1、vendored cfitsio 1、根 README 1、`lib/healpix_db/archive/legacy/` 1 → **在范围标准命名 README = 42 份**，另 1 份异形命名 `runtime/core/phase_lifecycle.README.md`，合计 **43 份文档面**（与 L27 的 43 同值但口径已显式化）。门实覆 **5** 份 = 11.6%（对 L27 的 92 目录单元分母 = 5.4%）。
  - manifest 侧复算：glob `lib/**/module.yaml` = **21**；`ci/checks.json` 全文 grep `module\.yaml|module_status` = **0 命中**（放宽大小写亦 0）→ manifest 语义面无门。
- 权威依据: 宪章 §12.3-1（manifest/注册表/构建/产品清单一致）、§8.4（算法模块必备 README + module.yaml + SCI/ALG/API/TEST 链接）、§12.3-3（实现引用有效 SCI/ALG）；负责人要求「每子库 README：功能/用途/相关文档/接口代码」
- 问题说明: 该门是"名字承诺 ≠ 实际判据"的确数实例：覆盖面 **5/43**，21 份 manifest 语义 0 校验，负责人 8 要素（功能/定位/相关文档/接口/构建安装/测试/DISP 偏差/状态词）核验 0 项，且核验方式只是"文件名以子串出现 + 该文件在 `lib/` 或 `tests/unit/` 下存在"，对锚能否解析到注册表不作判定 —— 已实证放行 4 份 README 的错锚。**裁决维持 P1 的实质差别**（与 M5b 三家 P0 fail-open 对比）：①**可否为假**：注释门空壳、线程预算门 0/0 自证、AST-API 恒真三条的断言在结构上永不可能为假（空集/自指/只比字样），任何输入都绿灯，属"检查器不存在"级；本门断言**可为假**（删 `lib/phase1/wcs/wcs_tan.cpp` 或让 README 不含 L2 字样即变红），是"窄门 + 弱判据"，不是"恒真门"。②**条文有无**：那三条守护的是 §12.3 **明列**的机器一致性项（-5 AST↔API、-8 并发文档一致），条文要求机器门而门恒真 → "已验证声明"直接失据 = 发布阻断；本门守护的"模块 README 8 要素完备性"在 §12.3 十二项中无对应条文（§8.4 只要求文件存在 + ID 链接），且负责人口头要求**无文本支撑**（见 M8a-I-001）→ 缺的是"更强的门 + 先立条文"。③**后果面**：本门失效只让文档类问题不变红，产品不合规与科学错误的证据链由其他门承担，不构成交付阻断。④**条件升级条款（比照 R-4）**：若负责人把 §12.3-1/-3 判为"README 声明的合同锚必须可解析到 INDEX.yaml"，则本门对现存 5 份给出绿灯即构成对交付物的假验证声明 → **即升 P0** 并与 M8a-E-001 并档。
- 影响: README 完备性/正确性整类问题（本合并件其余全部 L27 条目）在该门下不可能变红；§12.3-1 在 L2 面事实失效；错锚继续以"合同"名义被下游任务引用。
- 建议处置: ①门改**目录驱动枚举**（`lib/**/README.md` + `lib/**/module.yaml` + `modules/**/README.md`，豁免须显式登记 owner + 理由）；②判据升级为**可解析断言**：每个 `P\d-\d{3}|SCI-|ALG-|DATA-|API-|TEST-` 引用必须能在 `docs/contracts/INDEX.yaml` 的 `- id:` 键集合中找到，否则 FAIL；③最小要素门：README 须含"状态词（取 `docs/owner/RELEASE_STATUS.md` §0 阶梯）""构建 target""测试锚"三类可 grep 段落；④`ci/checks.json` 的 `changed_paths` 补 `lib/**/README.md` 与 `lib/**/module.yaml`；⑤为 `module.yaml` 建语义门（module_status/entrypoint ↔ 构建 ↔ 产品清单三方比对，见 M8a-C-002；注意 entrypoint 须先裁术语，见 M8a-I-006）。
- 置信度: 高（覆盖面、判据、错锚零命中、注册表缺口均为可复算确数，三级复核留痕于 §12）
- 来源: L27-002
- related: L12-022（同一门的事实面，本条补确数与错锚实证）、M8a-G-007、M8a-C-002、M8a-E-001、M8a-I-001（规范缺口）、M8a-I-002（薄 README）、M8a-G-001（许可面 P0）；判词层级：行锚系统性失效主落 **M6b-E-002**，本条不写"系统性"判词

---

## M8a-G-007 生成器自述「checker 以本生成器输出为源」不实：registry 无 diff 门，手写页与 descriptor 双向漂移（裁决：维持 P1）

- 类别: G_GOV_GATE
- 定稿优先级: P1（前台建议升 P0，本代理裁决**维持 P1**，理由见"问题说明"与 `_merge/M8a.md §4.2`）
- 位置: `tools/quality/gen_module_readmes.py::docstring`（复核 1-8）、`::OUT_DIR`（17）、`::git rev-parse`（20）、`::末尾输出`（143）；`ci/checks.json`（全文 grep `gen_module_readmes` = 0）；`docs/DOCUMENT_INDEX.yaml::registry 条目 notes`（复核 383-458）；`docs/modules/registry/astrocs.phase2.coverage.md::front matter`（1-9）；`lib/core/src/module_adapters.cpp::p2_coverage_descriptor`（复核 762-779）；`docs/owner/RELEASE_STATUS.md::§0 状态词阶梯`
- 证据摘录:
  > gen_module_readmes.py:4-7 「以 `lib/core/src/module_adapters.cpp` 为唯一源(registry 生产模块)…」「**非手工清单, checker 以本生成器输出为源**」
  > `ci/checks.json` 全文 grep：`gen_module_readmes` **0 命中**（放宽级亦 0，见 §12-3）→ 不存在任何以其输出为源的 checker 项
  > `docs/DOCUMENT_INDEX.yaml:392` 「P1-HIPS-DOC **手写合同页**（registry 无 hips_writer descriptor，生成器不覆盖；gen_module_readmes.py 以 module_adapters.cpp 为源——**重生成时须排除/保留本页**）」；同文件含"手写"注记共 **19** 条（:383/:386/:389/:395/:398/:401/:404/:407/:410/:416/:419/:428/:431/:434/:437/:440/:449/:455/:458）
  - 集合差复算（口径：glob `docs/modules/registry/*.md` = **26** vs grep `d.module_id = "` 于 `module_adapters.cpp` = **22**，两侧值集合取差）：页面多 4 = `astrocs.phase2.session`、`astrocs.phase1.session`、`astrocs.phase1.star-detection`、`astrocs.phase1.hips-writer`，descriptor 无缺页 → 多出的 4 页全为手写页，与"生成器不覆盖"注记一致，但**没有任何机器豁免清单**记录这一点。
  - 漂移实证（逐列 grep）：registry 页 front matter `upstream: [SCI-P2-COV-001, ALG-COV-001, API-COV-001, API-P2-001]` vs descriptor 现值 `d.alg_id = "ALG-P2-COV-001"`（:774）—— 两名并存，且 `ALG-P2-COV-001` 在 `docs/` **0 命中**（另立 M8a-E-001）
  - 状态词复算（口径：glob 26 页后逐文件 grep front matter 的 `status:`）：`status: ACTIVE` = **26/26**；而 `docs/owner/RELEASE_STATUS.md` §0 阶梯（`CONTRACT_READY/IMPLEMENTED/INSTALLED/VERIFIED/NOT_IMPLEMENTED/NOT_VERIFIED`）不含 `ACTIVE` → registry 页状态词与唯一口径**完全脱钩**（无一页用阶梯词）。
- 权威依据: 宪章 §12.3-1/-9、§12.2（L2 与实现一致）、§12.3-12（结论须可追溯到机器证据）
- 问题说明: ①生成器声明自己是 checker 的证据源，但注册表里没有以它为源的项 —— **声明为假**，26 页与 22 descriptor 之间不存在任何 diff 门；②由于无门，"手写页与生成页共存同一目录"只能靠 `DOCUMENT_INDEX.yaml` 的 notes 用自然语言叮嘱"重生成时须保留"（19 条），一旦有人跑重生成，覆盖行为无任何机器保护；③已漂移：coverage 页 `ALG-COV-001` 与 descriptor `ALG-P2-COV-001` 两名并存，代码面/docs 面/生成面各说各话；④模板对全部模块写死 blanket 断言（"确定性=固定顺序输出"等）并对 `module_status: CONTRACT_READY`/`entrypoint: MISSING` 的模块同样用现状口吻，front matter status 一律 `ACTIVE` 与 §0 阶梯无映射。**裁决维持 P1 的理由**：本条不是"恒真门"而是"根本没接进门 + 生成面/手写面双轨"，其后果是文档漂移与覆盖风险，属一致性失效而非交付物被假验证；且其判据要成立须先解决 §12.3-1 的落点（R-01/R-03 建议条款）与 `entrypoint` 术语裁决（M8a-I-006），条文未定即无从判 P0。**条件升级条款（比照 R-4）**：一旦负责人把"registry 页必须与 descriptor diff 为空（豁免须登记）"判为 §12.3-1 的要求，则现状 26 vs 22 的无门双轨即为假验证声明 → 升 P0。
- 影响: 模块端口/DATA/单位查询落点（registry 页）不可信；"已验"类 blanket 断言与 §13.1 每模块必备项脱钩；重生成即静默毁档的风险无门拦截。
- 建议处置: 二选一并写进文档：①把生成器接成**真门** —— 生成到临时区与仓内页 diff，非空即 FAIL；手写页进显式豁免清单（带 owner + 理由 + 复查日期），豁免清单本身进门；②放弃"checker 以本生成器输出为源"表述，把手写合同页迁出 `docs/modules/registry/` 到独立目录；模板去掉 blanket 已验断言，status 词表改取 §0 阶梯（见 R-03）。
- 置信度: 高（0 接线、26/22 集合差、19 条手写注记、26/26 状态词均为确数）
- 来源: L27-003
- related: M8a-G-006（同族裁决，一并维持 P1）、M8a-E-001（漂移的 ID 面）、M8a-C-002（滞后簇）、M8a-G-008（分层矛盾使模块文档归属层无定见）；判词层级：追溯双头/无据 VERIFIED 主落 **M6b-E-001 / M6b-G-001**，本条只落 README/manifest 一致性面

---

## M8a-G-008 分层标准把宪章 §12.2 的 L2 定义改写，并把仓库外 Wiki 置于权威链首位

- 类别: G_GOV_GATE
- 定稿优先级: P1
- 位置: `docs/README-DOCS.md::分层表/权威链`（复核 1-22，关键 :8/:11/:13/:16-20）；`ASTROCS_PROJECT_CONSTITUTION.md §12.2`（423-429）、`§1.1`（17-28）、`§1.2`（30-32）；`docs/standards/DOCUMENTATION_STANDARD.md:3`（把分层权威让给 README-DOCS）
- 证据摘录:
  > README-DOCS.md:8 「L2 算法规范 `docs/algorithms/*.md`」（并把 L5 记为 `docs/modules`）；:16-20 「权威链：**Wiki(核心约束) → …** 矛盾以 Wiki 为准」；:13 「追溯 `docs/TRACEABILITY.csv`（唯一矩阵）」
  > 宪章 §12.2:425-427 「L1：SCI、ALG、DATA、ARCH、API/ABI 规范；L2：**模块 README、manifest、函数合同、实现和测试说明**」
  > 宪章 §1.1:17 「本文是 AstroCS 的最高工程约束…规范优先级为：1. 本文 … 7. 模块 README、源码、测试和机器生成证据」；§1.2:30-32（修改权仅在负责人，Agent 无权重排）
  > DOCUMENTATION_STANDARD.md:3 「权威文档分层（L0-L5）见 docs/README-DOCS.md。」
- 权威依据: 宪章 §1.1（唯一最高工程约束 + 八层顺序）、§1.2、§12.2
- 问题说明: 一份被 DOCUMENTATION_STANDARD 指定为分层权威的 L 级文档，把宪章定义的 L2（模块 README/manifest）改写成 `docs/algorithms`，并把"模块 README"挤出各层（`docs/modules` 记为 L5）；同时以"矛盾以 Wiki 为准"把仓库外的 wiki 本地克隆（`AstroCS.wiki/`，AGENTS.md 列为用户/资料区、gitignore）置于权威链首位 —— 与 §1.1 第 1 位是宪章、第 7 位才是模块 README 的排序直接冲突，且 §1.2 明定 Agent 无权重排。其 :13 的"唯一矩阵"表述与 F00-01 同事实，不在此重复计数。
- 影响: 所有"模块文档该写哪、按哪层模板查、与谁冲突以谁为准"的判定与门（DOC-INDEX、docs_machine_consistency）建在这份与宪章相悖的分日上；负责人点名的"每子库 README"义务因此在本仓规范里无稳定归属层，是 README 域长期无人追责的同根问题（见 M8a-I-001）。
- 建议处置: README-DOCS 按 §12.2 重标（L1 = SCI/ALG/DATA/ARCH/API，L2 = 模块 README/manifest/函数合同/实现与测试说明，L3 = 实现细节），`docs/modules/**` 定位为 L2 生成视图；权威链首改宪章，删除或改写"矛盾以 Wiki 为准"为「wiki 为镜像，以仓内宪章为准」；DOCUMENTATION_STANDARD 的分层指针随订正复述 §12.2 而不外链。
- 置信度: 高
- 来源: L27-001
- related: M8a-I-001（规范缺口：无落点的直接后果）、F00-01（唯一矩阵同源，主落 M6b）、L17-012（README 权威栏反向覆盖同族）
