# M5b 定稿 · G_GOV_GATE · P1

> 位置一律写「path::符号」，行号仅辅助（随并发漂移）。逐条按定稿时点工作树重读复验。

### M5b-G-07 DOC-L0 门校验 docs/review 而非宪章 §12.1 指定的 docs/owner；点名的 PHASE_OVERVIEW.md 全仓不存在
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `tools/check_l0_docs.py::DOCS/main`（现 :11-29）；`ci/checks.json::DOC-L0`（现 :369-389）；`ASTROCS_PROJECT_CONSTITUTION.md::§12.1`（现 :410-421，PHASE_OVERVIEW 在 :416）；`docs/DOCUMENT_INDEX.yaml::docs/review 区段`（现 :323）；`REVIEW.md::2 顶层文档`（现 :26-34）；`docs/owner/RELEASE_STATUS.md::L0 负责人入口`（现 :80）
- 证据摘录:
  > 规则: REVIEW.md + docs/review/{SCIENCE,PIPELINE,ARCHITECTURE,RELEASE_STATUS,CHANGE_REVIEW}_OVERVIEW.md
  >         p = REPO / "docs" / "review" / d
  >         elif len(p.read_text(encoding="utf-8", errors="ignore")) < 200:
  > - `docs/owner/PHASE_OVERVIEW.md`；
  > 更写明「DOC-L0 收敛：登记 docs/review/ …（check_l0_docs.py 检查面）」
- 权威依据: 宪章 §12.1（负责人默认只阅读本文 + `docs/owner/{SCIENCE,PHASE,ARCHITECTURE}_OVERVIEW.md` + `docs/owner/RELEASE_STATUS.md` + 每轮 `CHANGE_REVIEW.md`）、§12.3-12、§17-2
- 问题说明: 门的存在判据、非空判据（≥200 字节）、"文件名出现在 REVIEW.md"判据全部落在 `docs/review/`；宪章点名的 `docs/owner/` 零覆盖。glob `docs/owner/**` 实测只有 5 份（RELEASE_STATUS/PIPELINE_OVERVIEW/CHANGE_REVIEW/ARCHITECTURE_OVERVIEW/SCIENCE_OVERVIEW），**无 PHASE_OVERVIEW.md**，而全仓该名的唯一命中就是宪章第 416 行 ⇒ 宪章指定的 L0 文件不存在且无归档说明。门能失败吗：**能**——但只会因 docs/review 面被删/截短/改名而红，永远不会因"owner 面缺文档"或"结论无当前 SHA 证据"而红；`DOCUMENT_INDEX.yaml:323` 把这一错层写成有意收敛，`RELEASE_STATUS.md:80` 又据 `check_l0_docs.py` 的 PASS 支撑 `CONTRACT_READY`。
- 影响: §12.1/§12.3-12 无实现；负责人入口的完整性由错误目录保证。
- 建议处置: 按 §1.2 二选一（订正 §12.1 文件清单，或补建/归档 PHASE_OVERVIEW）；DOC-L0 检查面改指 `docs/owner`；为 §12.3-12 增设"每条 L0 结论携带 base SHA + 可定位证据且该证据由本次 run 生成"的证据门。
- 置信度: 高 · related: L18 域（M6）、M5b-G-18

### M5b-G-08 AGENTS-GOV 只做关键词全含：强制 AGENTS.md 保留台账验证器判为非法的状态字面量，且不校验条款号真实性
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `tools/check_agents_gov.py::REQUIRED/FORBIDDEN/main`（现 :2、:6-18、:20-38）；`AGENTS.md::治理要素映射表`（现 :16 单入口行、:28 状态机行）；`ASTROCS_PROJECT_CONSTITUTION.md::§14.4/§14.5/§3.1`；`tools/quality/validate_task_ledger.py::STATUSES`（现 :42-43）与 `::SELFTEST 负例`（现 :208、:229）；`ci/checks.json::AGENTS-GOV`（现 :184-204）
- 证据摘录:
  > """GOV-001 checker: 核验 AGENTS.md 含 V5 全部治理要素且无冲突旧条款。exit 0=PASS。"""
  >     ("状态机",       ["NOT_STARTED -> IN_PROGRESS", "REVIEW_PENDING", "waiver"]),
  > FORBIDDEN = []
  > STATUSES = {"NOT_STARTED", "IN_PROGRESS", "PASS", "FAIL", "BLOCKED",
  >             "WAITING_WINDOWS", "WAITING_DATA"}
  > 任务级状态取 `NOT_STARTED -> IN_PROGRESS -> PASS/FAIL/BLOCKED/REVIEW_PENDING`
- 权威依据: 宪章 §12.3（机器一致性检查）、§1.1、§14.5；AGENTS.md 自述「本表 10 项与 check_agents_gov.py 的 REQUIRED 一一对应…若某项检查器断言的措辞与宪章表述不一致，以宪章为准并登记 finding，不得为过检查写入与宪章相悖的规则」
- 问题说明: ①判据是 10 组字符串在全文的"全含"（`all(n in text for n in needles)`），`FORBIDDEN=[]` 使 docstring 承诺的「无冲突旧条款」完全未实现 ⇒ 关键词齐备即 PASS，与十项治理要素的语义是否被正确承载无关（宪章 §3.1 实为"Windows 为主交付、Linux 为日常开发执行面"，而门只要求出现"仅支持 amd64"七字）。②`REQUIRED` 内不含任何宪章条款号 ⇒ 表内条款号全部手工维护，写错号不会变红。③**反向锁定**：AGENTS.md 现依检查器要求保留 `REVIEW_PENDING` 字面量，而 `tools/quality/validate_task_ledger.py:42-43` 的 STATUSES 不含该值、:208/:229 的 selftest 负例名即 `illegal_state`（其 :238/:280 亦确用该值作非法样例）⇒ 治理文本被机器门强制含一个台账工具判为非法的状态；且 `validate_task_ledger.py` 在 `ci/checks.json` 与 workflow 中零注册（全仓引用只有其自身与 evidence/历史），其 12 条负例与 R6/R7 依赖语义 CI 永不执行。④宪章全文对 `REVIEW_PENDING\|NOT_STARTED\|台账\|waiver\|§14.4 状态机` 的命中数为 0 ⇒ 十项映射中 `状态机` 项声称的"以冻结宪章条款为语义源"没有文本源。
- 影响: 项目自述的"机器门 AGENTS-GOV"只保证字面量在场；AGENTS.md 与工具链对状态字面量的冲突被机器门固化为不可修复。
- 建议处置: 条款号纳入校验；把 `tools/quality/validate_task_ledger.py --selftest` 注册为 CI 项；把`状态机`要素改为字面引用宪章 §14.5/§14.4 文本；冲突的 REVIEW_PENDING 走 finding + 负责人裁决。
- 置信度: 高 · related: M5b-G-07、M5b-G-18、L17-003

### M5b-G-09 ARCHIVED 约束文件被 L0 与 README 列为 ACTIVE_NORMATIVE 根权威，字母条款号在宪章中不存在
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `docs/owner/ARCHITECTURE_OVERVIEW.md::权威表`（现 :93）；`::文件头 权威`（现 :8）；`docs/owner/PIPELINE_OVERVIEW.md::文件头 权威`（现 :8）；`README.md::权威与当前状态`（现 :9、:26、:31、:35-36、:64）；`AstroCS_ENGINEERING_CONSTRAINTS.md::文件头/§D.6`（现 :10、:21、:76、:82-84）；对照 `ASTROCS_PROJECT_CONSTITUTION.md::§1.1/§18 supersession`（现 :15-28、:660）与宪章章节清单（§1-§19 全为数字节，无字母节）
- 证据摘录:
  > | 工程约束（根权威） | `AstroCS_ENGINEERING_CONSTRAINTS.md` | ACTIVE_NORMATIVE |
  > > 权威：`AstroCS_ENGINEERING_CONSTRAINTS.md` §B/C/D/F、`docs/contracts/ARCH-001.md`、
  > > **AstroCS 项目唯一根冻结约束**（Agent 与人工提交的硬门）。GOV-001 状态：**ACTIVE_NORMATIVE**（根权威，全部 Agent 入口必读）。
  > supersession 与冲突消解：自本文冻结起，`AstroCS_ENGINEERING_CONSTRAINTS.md` 降级为 `ARCHIVED_NON_NORMATIVE` 历史参照
- 权威依据: 宪章 §1.1（唯一最高工程约束）、§1.2、§18 末段 supersession、§17-1
- 问题说明: 宪章 :660 明写该文件降级为 ARCHIVED_NON_NORMATIVE，其文件头 :10 亦自写 ARCHIVED，但被宪章指定的负责人入口把同一文件登记为 `ACTIVE_NORMATIVE`（ARCHITECTURE_OVERVIEW:93 表头明确写着"根权威"列），并在文件头以 `§A/§B/C/D/F` 指称现行权威；README 更称其为"根冻结约束"。宪章只有 §1-§19 数字节，无 §A-§K/§H/§F.1 ⇒ 这些条款引用只能解析到已废止文件。门侧：`tools/doccheck/check_engineering_constraints.py` 只判"ARCHIVED 字面量在约束文件文本中出现"，不反向要求 L0 权威栏与 README 用词一致，且 AGENTS-GOV（G-08）不做条款号核验 ⇒ 无门可发现该倒挂。
- 影响: 后续 Agent/负责人按 L0 路由会读到被 §18.2 废止的阈值与 §14 职责口径；§17-1「本文及相关合同已冻结且无冲突」不成立。（数值后果本身由 L17-004 → M5a 的资源门条目承载，本条只定权威倒挂。）
- 建议处置: 五份 owner 文档与 README 的权威栏改指宪章条款号；约束文件行改标 ARCHIVED_NON_NORMATIVE + superseded_by；新增「L0 权威栏不得把 ARCHIVED 文档列为活动权威」机器项。
- 置信度: 高 · related: M5b-G-08、L17-004（移交 M5a）、M5b-E-04

### M5b-G-10 ACR-DORMANT 门在缺二进制时静默 PASS，源码判据只匹配单一符号且豁免自相矛盾，nm 无 timeout
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `tools/check_legacy_exit.py::check/bin_path`（现 :19-24、:46-57、:63-76）、`::main ACR 源码判据`（现 :73-76）；`ci/checks.json::ACR-DORMANT`（现 :137 起，三 profile、waivable=false）；`ci/steps/linux_build_root_graph.sh::构建树`（现 :13-14、:22-25）；`CMakeLists.txt::option(ASTROCS_ENABLE_ACR)`（现 :17、:610-616）；对照 `docs/owner/ARCHITECTURE_OVERVIEW.md::ACR dormant`（现 :53-57）
- 证据摘录:
  >     bin_path = REPO / "build" / "root-cmake" / "astrocs"
  >     if bin_path.exists():
  >         out = subprocess.run(["nm", str(bin_path)], capture_output=True, text=True).stdout.lower()
  >         if "acr" in out:
  >             errors.append("生产二进制含 ACR 符号 (LEG-004)")
  >     for f in (REPO / "lib" / "phase2" / "src").glob("*.cpp"):
  >         if "acr" in txt.lower() and "register_phase2_acr" in txt:
- 权威依据: 宪章 §17-5（ACR 在生产不可达）、§10.1、§14.5（所有外部命令带 timeout 并保存日志）、§12.3
- 问题说明: ①二进制级判据整体被 `if bin_path.exists()` 包住且**无 else** ⇒ 路径写死为 `build/root-cmake/astrocs`，而 CI 实际构建树是 `build/linux-control` 与 `build/cli`（步骤脚本现 :13-14、:22-24，并把根图 cp 成 `build/astrocs`，从不产生 `build/root-cmake`；全仓 `build/root-cmake` 的现存引用只有注释与 `tools/check_duplication.py` 的候选探测）⇒ 该门在 CI 中恒走"无二进制"分支并输出 PASS。②源码判据只 glob `lib/phase2/src/*.cpp` 且必须同时命中 `register_phase2_acr` ⇒ ACR 注册若落在该文件之外（providers/、modules/、lib/core、cli/）永不命中；且 `acr_kernels.cpp` 自身被排除，而同树该文件内注释（现 :88）写「实际注册由 module_adapters.cpp 的 register_phase2_acr() 在 ASTROCS_ENABLE_ACR 下完成」⇒ 门豁免的文件把注册指向门不扫的文件，按豁免规则 ACR 接线不可发现，按语义规则被豁免文件自证存在。③两处 `subprocess.run(["nm", …])` 无 timeout（宪章 §14.5 明文要求）。
- 影响: §17-5 的符号级证明恒为"跳过即通过"；`docs/owner/ARCHITECTURE_OVERVIEW.md:53-57` 的 dormant 结论只有构建面（option OFF / 不链 lib/acr / 不安装 libacr*）支撑，缺产品二进制级与注册面证据。
- 建议处置: 二进制判据改读 CI 实构建树并补 else 硬失败；`nm` 加 timeout + 日志；源码判据扩展到 `cli/lib/providers/modules` 且以符号集合而非单符号匹配。
- 置信度: 高 · related: L11 域（M5a）、M5b-G-01

### M5b-G-11 heavy 分类无正向判据、资源门在 CI 内零调用点，且两个静态门的扫描面不含 provider/模块 DLL
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `ci/validate_registry.py::R7/R9`（现 :105-110、:132-138）；`ci/run.py::requires_monitor 判定`（现 :59-66、:110-119）；`ci/checks.json::UT-CTEST/UT-ABI（heavy=false, requires_monitor=false）`（现 :1643-1671 等）；`tools/check_serial_hardcode.py::PROD_FILES/SCAN`（现 :17、:39）；`tools/arch/check_thread_budget.py::SCAN_ROOTS`（现 :26-27）；`providers/cpu/{baseline,avx2,avx512}/src/*_provider.cpp::workers = 1`（现 :418、:244、:243）
- 证据摘录:
  >             errors.append(f"{cid}: heavy=false 但 requires_monitor=true")
  >     "lib/astro_image_io/src/aio_fits.cpp", "lib/astro_image_io/src/astro_image_io.cpp",
  >     "lib/phase1/photometry/photometer.cpp", "lib/calibration/src/master_generator.cpp",
  >     REPO / "include", REPO / "lib", REPO / "runtime", REPO / "modules", REPO / "cli", REPO / "providers",
  > uint32_t workers = 1;
- 权威依据: 宪章 §10.5、§17-6、§10.4、§14.2（稳定目录职责）、§12.3-4
- 问题说明: ①注册表只有反向规则（heavy=false 且 requires_monitor=true 才报错），无「kernel/节点级重计算必须标 heavy」的正向规则；实测 `ci/checks.json` 的 `--gate-required` 命中数为 0（`ci/run.py` 现 :72 自述「当前零 --gate-required」），反向不变量由 `ci/tests/test_ci001_failclosed.py:41` 断言但该测试不被 CI 执行（见 M5b-F-02）⇒ `UT-CTEST`/`UT-ABI` 等执行真实 kernel 的项 `requires_monitor=false` 恒合法，§10.5 的"每个 heavy 运行自动记录"在此面不成立。②`SERIAL-HARDCODE` 的 docstring 称"扫描生产 C/C++ 文件"，实际只判 `PROD_FILES` 4 个固定文件；`THREAD-BUDGET` 的 `SCAN_ROOTS` 含 providers 但 `SKIP_DIRS` 不含 `third_party`/`examples`，而真正的 `providers/cpu/*/src` 三处 `uint32_t workers = 1;`（现 :418/:244/:243）两个静态门都不抓（前者不在 PROD_FILES，后者只匹配 omp_set_num_threads/硬编码 num_threads(N)/std::thread）。
- 影响: §17.6 静态门覆盖面被高估；新增 heavy 模块或 provider 侧串行/自取并行不会被这两个门抓到。
- 建议处置: 注册表增正向 heavy 判据（按 module_ports/节点表 cpu_heavy 逐节点核对）；两门扫描面并入 `providers/**` 并按 `execution_class` 驱动；删除或真正启用 `check_serial_heavy.ALLOWLIST` 死配置。
- 置信度: 高 · related: M5b-G-12、M5b-F-02、F00-03（M5a）

### M5b-G-12 SERIAL-HEAVY 门的核心判定为「四关键词全缺才报错」的结构式 fail-open，且声称的覆盖面与实扫四文件不符
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `tools/quality/check_serial_heavy.py::docstring/scan/ALLOWLIST`（现 :3-13、:27-34、:44-60、:55-58、:82-95、:96-113、:130-133）；`ci/checks.json::SERIAL-HEAVY`（现 :555 前邻项）；`CMakeLists.txt::（-fopenmp/std::thread 关键词落点）`（现 :346、:384、:434）
- 证据摘录:
  > 从生产调用图定位每个 cpu_heavy 节点实现与编译 target，核对：
  >     if ("P2_ENABLE_OPENMP" not in cmake and "P2_PARALLEL" not in cmake and
  >             "-fopenmp" not in cmake and "std::thread" not in sampler + upm):
  > ALLOWLIST: dict[str, dict[str, str]] = {
- 权威依据: 宪章 §17.6、§10.4、§10.5、§12.3-8
- 问题说明: ①docstring 称"从生产调用图定位每个 cpu_heavy 节点"，实际 `scan()` 只读死写的 4 个文件（sampler/upm/p3_session/p3_resample），从不读注册表或 module_ports 节点表 ⇒ `lib/photometric_calib/cpp/src/pc_api.cpp` 的 19 处裸 `#pragma omp parallel for`（其中 `num_threads` 命中数 0，前台 F00-03 同事实）不在扫描面，`master_generator.cpp`/`aio_fits.cpp` 的同类 pragma 也不判。②Phase2 开关检查是四条件的 AND：根 CMake 现含 `-fopenmp`（:346/:384/:434）⇒ 该项**永不为真**，即使 `P2_ENABLE_OPENMP` 被从 ON 改回默认 OFF、并行整体退回串行也不会红（结构式 fail-open）。③`ALLOWLIST` 定义后 `scan()` 从不引用（全文件命中数 = 1，仅定义处），docstring 承诺的 allowlist 语义不存在。④:101 自述"V6.1 语义：内联判定等价视为生产 caller 已接线"，把自身 docstring 的第 3 条负面 fixture（resource gate 无生产 caller）永久豁免。
- 影响: §17.6 的串行/低利用静态门被高估为逐节点覆盖，实际是四个文件的定点 grep；回归（关并行、去 lease）不会被发现。
- 建议处置: docstring 改真实覆盖面；判据改为按 `module_ports.registry.json` + ctest 节点表逐 cpu_heavy 节点核对；启用或删 ALLOWLIST；AND 结构拆为独立判据。
- 置信度: 高 · related: M5b-G-11、F00-03（M5a）

### M5b-G-13 ABI-BOUNDARY 门实际只扫 legacy 单头，PASS 文案虚报覆盖头数并含死分支
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `tools/check_abi_boundary.py::HEADERS/BOUNDARY_ONLY/check/死分支`（现 :13-19、:24-43、:30-34、:46-53、:58）；`ci/checks.json::ABI-BOUNDARY`（现 :161 起）；对照 `include/astrocs/abi/{status_codes,module_api_v1,lifecycle_v1,host_api_v1,artifact_api_v1}.h`、`contracts/artifact_abi_v1.h`、`io/aio_abi_v1.h`、`modules/*/include/**`、`providers/cpu/*/include/**`
- 证据摘录:
  > BOUNDARY_ONLY = [REPO / "include/astrocs/common_abi_v1.h"]
  >     for h in BOUNDARY_ONLY:
  > print(f"CPU-001_PASS: ABI 结构 {len(blocks)} 个均带 size/version; {len(HEADERS)} 头无 STL/异常跨边界")
  >         if f"struct_size" not in abi:      # 取整文件文本 → 恒不触发
- 权威依据: 宪章 §8.6、§12.3-6；`docs/standards/C_ABI_STANDARD.md`
- 问题说明: 收集进 `HEADERS` 的 `lib/backend_host/*.h` 从未被扫描（STL/异常循环只跑 `BOUNDARY_ONLY` 单元素），PASS 文案却报 `len(HEADERS)` 个"头无 STL/异常跨边界"⇒ 覆盖声明与实际不符；真正承载 5 个科学模块 DLL 与 provider 边界的模块化 ABI 头族全部不在扫描范围。另 :30-33 的"每结构必须带 struct_size"取整文件文本判断（任何 struct_size 出现即全体通过），是紧随其后 :36-43 有效块检查之前的死分支。
- 影响: 新增 STL/异常跨界只会在 legacy 头被拦，§8.6/§12.3-6 在真实边界上失守。
- 建议处置: 扫描集改 `include/astrocs/**` + `modules/*/include/**` + `providers/cpu/*/include/**`；删死分支；PASS 文案只报实际扫描集合。
- 置信度: 高 · related: M5b-G-14、M5b-E-06

### M5b-G-14 导出符号与 ABI 合同一致性（§12.3-6）实际无有效检查：门只判符号表非空，Linux 侧零覆盖
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `tools/quality/ci_windows_driver.py::（exported_abi_dumpbin 登记 + dumpbin 段）`（现 :586-590、:605-626）；`include/astrocs/abi/module_api_v1.h::（唯一导出合同）`（现 :7-12）；`cmake/install_layout.cmake::（modules/providers 安装）`（现 :91-96）；`CMakeLists.txt::WINDOWS_EXPORT_ALL_SYMBOLS`（现 :146-153、:217-236）
- 证据摘录:
  > ("exported_abi_dumpbin", "dumpbin /EXPORTS … 非空符号表，且 astrocs_runtime 导出 acs_artifact_*、astrocs_io 导出 acs_fio_*（头文件冻结 C ABI）")
  >     exports = [ln.strip() for ln in res["output_tail"].splitlines()
  >                if re.match(r"^\s+\d+\s+[0-9A-Fa-f]{1,8}\s+[0-9A-Fa-f]{1,8}\s+\w", ln)]
  >         ok = res["exit_code"] == 0 and bool(exports)
- 权威依据: 宪章 §12.3-6、§8.4、§17-3；`docs/standards/API_STANDARD.md`
- 问题说明: 唯一触及导出符号的门只做"符号表非空"判断，不比较任何名字集合（docstring 承诺的 `acs_artifact_*\|acs_fio_*` 未实现）；循环只覆盖 4 个平台/示例单元，**5 个科学模块 DLL/SO 与 catalog 模块完全不在范围**（而 `module_api_v1.h:12` 与 `install_layout.cmake:93` 要求"唯一导出 astrocs_module_query_v1"）；Linux 主机或非 Windows 分支下该项以 `executed=False` 登记为 hosted-only 预期，即本轮证据为空。Linux 侧完全没有导出符号检查（`tools/` 内 `nm\|readelf\|version-script` 无命中），而 Linux 是真实数据终验执行面。另 `WINDOWS_EXPORT_ALL_SYMBOLS ON` 自述"默认全导出"，与"唯一导出符号"合同在机器上不可核对。
- 影响: 多余/缺失导出符号不会被发现；§17-3"可独立加载、验证和卸载"缺最直接的证据。
- 建议处置: 以 api_inventory/ABI-006 合同为期望集做双向名字比对（Linux `nm --dynamic`、Windows `dumpbin /exports`），纳入 `modules/astrocs_p1_*.so`；平台库加 version-script 或显式 dllexport 白名单。
- 置信度: 高 · related: M5b-G-06、M5b-G-13

### M5b-G-15 CLI_PROTOCOL §6 承诺的 tools/check_cli_protocol.py 不存在；CLI-COMMAND-LAYER 只做字符串存在性判断
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `docs/api/CLI_PROTOCOL_V1.md::§6`（现 :47-56）；`tools/check_cli_command_layer.py::main 规则1/4`（现 :2-10、:19-40）；`cli/commands.cpp::dispatch`（现 :2433-2466 区段，含现 :2451 `if (joined == "drizzle")`）；`cli/parser.cpp::kRules`（现 :36-64）；`tools/check_api_docs.py::EXIT_NAMES`（现 :27）；`cli/exit_codes.h::COMPUTE`（现 :13）
- 证据摘录:
  > ## 6 机器化一致性检查器合同(04 §6,API-002 建立 `tools/check_cli_protocol.py`)
  > 1. `--help` golden 树与 §1 逐行一致; … 3. 退出码常量唯一源(include/astrocs/exit_codes.h, grep 无第二处数值表);
  >     if "CmdRule kRules[]" not in PARSER:
  >         errors.append("no CmdRule kRules[] table")
  > EXIT_NAMES = {0: "OK", 2: "ARGS", … 6: "EXEC", …}
- 权威依据: 宪章 §8.1、§12.3-1/-5/-9/-11
- 问题说明: `tools/check_cli_protocol.py` 全仓不存在（glob 0 命中），文档承诺的 6 项里只有第 1 项被 `check_api_docs.py::check_command_tree` 部分替代，而该替代是单向 doc⊆help 且二进制缺失即零检查（M5b-G-01）；第 3/5 项无任何实现。现存 CLI-COMMAND-LAYER 规则 1 声称"parser 用 kRules 表驱动分发（无 argv 硬编码 if 链）"，实现只判两个字符串是否出现，而 `dispatch()` 正是 30+ 条 `if (joined == …)` 硬编码链与 kRules 并存（两份命令面登记表，漂移不可见）；规则 4 声称退出码单一来源，实现只判 `exit_codes.h` 字样是否出现。检查器族内部还自带冲突：`check_api_docs.py:27` 把 6 命名为 EXEC，唯一源 `cli/exit_codes.h:13` 为 `COMPUTE = 6`。
- 影响: CLI 命令树、退出码唯一源、发布 manifest 不含旧 Phase exe 三条治理约束处于无门禁状态。
- 建议处置: 补齐 §6 六项实现，或把文档中不存在的检查标 MISSING；CLI-COMMAND-LAYER 改为比对 kRules ↔ dispatch ↔ kHelp 三集合相等。
- 置信度: 高 · related: M5b-G-01、M5b-C-04、M5b-E-01

### M5b-G-16 CLI 配置模板把产物写到仓库根，AGENTS.md 禁而 CLI_PROTOCOL 认，门只豁免不拦
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `cli/commands.cpp::kConfigTemplate`（现 :97-101，`\"output_dir\": \".\"`）；`cli/commands.cpp::（output_dir 回退默认，多处）`（现 :842、:1042、:1218、:1483）；`cli/commands.cpp::cmd_bench_cpu 默认输出`（现 :2286）；`docs/api/CLI_PROTOCOL_V1.md::§7`（现 :63、:74）；`ci/checks.json::UT-CLI.dirty_ignore_prefixes`（现 :1407-1415）；`ci/known_failures.json::failures[UT-CLI]`（现 :44-66）
- 证据摘录:
  >   \"output_dir\": \".\"
  > CLI 不得以进程 CWD(\".\")作为隐式缺省写出,否则在工作区根散落产物并触发
  >     "dirty_ignore_prefixes": [ "astrocs_run_", "run/" ]
- 权威依据: AGENTS.md 目录规范「禁止将运行产物产出到项目根目录」「CLI 运行产物 `astrocs_run_*.json` 一律落 `run/cli_runs/`」；宪章 §14.2、§12.3-11
- 问题说明: `config init` 产出的模板即把产物目录设为 CWD，CLI 各路径的 `output_dir` 缺省回退同样是 `"."`，`benchmark cpu` 未给 `--output` 时默认写 CWD `cpu_profile.json` ⇒ 合规调用 `astrocs phaseN run --config <config init 产物>` 必然在仓库根生成运行产物；`CLI_PROTOCOL_V1.md:74` 已记载该缺陷未解（且 :63 自身与 :59 的 `run\|graph --out-dir <dir>` 允许值表述互斥），治理侧的处置是把 `astrocs_run_`/`alloc_`/`resource_`/`worker_balance.csv` 加进 UT-CLI 的 dirty 豁免集（现 :1407-1415）并把 UT-CLI 以 `expected=fail` 挂账到 2026-12-31（known_failures 现 :44-66，其 reason 字段自认"根因在 cli/commands.cpp 产物默认落点"）⇒ **以豁免替代修复**，目录规范机器面失效。
- 影响: 工作区根持续被运行产物污染（本会话工作树内即可观察到）；豁免集使"CLI 运行是否写根目录"永久不可判定。
- 建议处置: 模板与所有回退默认改 `run/cli_runs/`（或强制 `--output`）；撤 dirty 豁免，把"无根目录残留"做成 `no CWD residue` 负向判据；解除 UT-CLI 挂账。
- 置信度: 高 · related: M5b-G-18、M5b-C-04

### M5b-G-17 L0 顶层文档自述「当前提交实测绿」，同仓库 CI 基线自证七项以上非豁免门禁仍为红灯
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `REVIEW.md::1 一句话结论`（现 :20）；`REVIEW.md::6 机器验证入口`（现 :115-118）；`docs/owner/RELEASE_STATUS.md::1 一句话结论`（现 :40）、`::6 发布 Gate`（现 :78-84）；`ci/known_failures.json::contract.excluded_by_policy`（现 :39）与 `::base_commit/generated_utc/failures[UT-CLI]`（现 :41-42、:44-66）；`ci/checks.json`（各红灯项 `waivable:false`）
- 证据摘录:
  > 薄命令面均已落地（源码 + 当前提交实测绿）；遗留 run --phases 连跑已删除。
  >   `tools/check_l0_docs.py`（DOC-002）；本轮全部 PASS（日志 `run/docconv001/logs/`）。
  > "excluded_by_policy": "……本轮 linux-main 其余红灯（AGENTS-GOV/DATA-ARTIFACTS/THREAD-BUDGET/CON-COMMENTS/CON-FULL-INTEGRATION/UT-ARCH/UT-BACKEND/WIN-*/DEEP-*）均为在册 P1（FD-R1-010），不得登记进基线，须由域主修复；……"
  >       "check_id": "UT-CLI", … "expected": "fail", "expiry": "2026-12-31T00:00:00Z"
- 权威依据: 宪章 §17-7（同 SHA CI 通过）、§17-10（P0/P1 为零）、§12.3-12、§14.5
- 问题说明: 两份文件互相矛盾且**无法用 git 定序**（本审计禁执行 git）：L0 侧自述 BASE=`da3c4b4a`（REVIEW.md:7-9）并称"当前提交实测绿/本轮全部 PASS"，CI 基线侧自述 base=`f8778bbb`、generated 2026-09-12（现 :41-42）并在 `excluded_by_policy` 中点名一批 `waivable=false` 的红灯项。按文件自述，二者谁更新**不可判定** ⇒ 本条不定性为"时间线矛盾 P0"，只定"两份活动文档对 CI 现状的陈述互斥且均无当前 SHA 证据指针"。可确证的次生事实：L0 的绿证据全部指向 `run/docconv001/logs/`（gitignore 现场，仓库内不可核），因此无论基线新旧，§12.3-12 都不成立。
- 影响: 负责人据 L0 判断"CI 绿、可进终验"可能得出错误结论；§17-7/§17-10 的可达成性无法从仓库内核验。
- 建议处置: L0 增设「CI 现状」行，按 `ci/known_failures.json` 或最近一次 `CI_RESULT.json` 如实列红灯项与在册 finding ID；把「L0 声称绿 ⇔ 注册表/结果无 FAIL」做成机器门。
- 置信度: 中（矛盾本身确凿；基线新鲜度不可判） · related: M5b-G-07、M5b-G-08、F00-05

### M5b-G-18 注册表合同页对未接入产品链路的模块给出 production 身份与"1/N 等价已验"完成度声明
- 类别: G_GOV_GATE · 建议优先级: P1
- 位置: `docs/modules/registry/astrocs.phase3.resample.md::职责/端口/并行/验证`（现 :15、:20-24、:34、:40、:52）；`lib/core/src/module_adapters.cpp::phase3_descriptor`（现 :503-513）与 `::register_phase_modules`（现 :5447、:5460-5465）与 `::p3_nodes 表`（现 :5521-5528）；`runtime/pipeline/module_ports.registry.json`（现 :268，只有 resample2）；`cli/runtime_client.cpp::P3 节点表`（现 :215）；`docs/traceability/TRACEABILITY_MATRIX.csv::MOD-astrocs-phase3-resample`（现 :25）
- 证据摘录:
  > Registry production 模块(唯一源=module_adapters.cpp descriptor)。
  > 确定性=固定顺序输出(1/N 等价已验)。
  > `TEST-P3-RES-001` 对应测试(逐任务 TASK_RESULT 证据); 容差=验收冻结。
  > // 顶层 astrocs.phase3.resample 占位 descriptor（P2 模板复制残留）不在本
  > // 任务触碰, 由 P3-RSMP-INT 处理 (lib/phase3_rsmp/README.md 实测登记)。
- 权威依据: 宪章 §9、§12.3-1/-2/-4、§13.1、§16.2、§17-4
- 问题说明: **修正叶子 L12-018 的"未进入注册表"说法**——`phase3_descriptor()` 确在现 :5460-5465 被 `register_module`，但它以 `make_session_module<P3Api>`（整阶段 Session 包装）注册，与宪章 §8.2/§8.4"每节点唯一实际 entrypoint、不得隐藏整阶段 Session"的方向相悖，且代码注释自认它是"P2 模板复制残留/占位"（现 :5521-5522）。真正的产品链路（`module_ports.registry.json:268` 与 `cli/runtime_client.cpp:215`）用的是 `astrocs.phase3.resample2`。在此前提下，该模块的 L2 合同页仍自称"Registry production 模块"，并给出 `parallel_ok=True`、"确定性=固定顺序输出(**1/N 等价已验**)"与"TEST-P3-RES-001 对应测试"；而八层矩阵同一模块行（现 :25）把 SRC/TEST/EVIDENCE 明确记为 MISSING、notes 记为"待迁移任务" ⇒ 页面上的"已验"在矩阵与仓库内都没有工件（§13.1 要求的 1/N worker 一致对该模块无任何证据）。同族：`astrocs.phase1.session`、`astrocs.phase1.star-detection` 两页登记了注册表不存在的 module_id（前者页面自述"registry 无 astrocs.p1.star_detection descriptor"）。实测 registry 合同页 26 份 vs `lib/*/module.yaml` 21 份，非一一对应。
- 影响: 端口/DATA 合同与实现割裂（按此页实现会把重采样接到未冻结的端口模型）；"已验"污染 §17 门禁的证据基础。
- 建议处置: 未接入生产链路的模块页强制 `STATUS: NOT_REGISTERED` + `TEST: MISSING`（禁用"已验"字样）或删占位 descriptor；新增 MODULE-REGISTRY 机器项比对 registry 页 ↔ register_phase_modules ↔ module_ports ↔ CLI IR 节点集。
- 置信度: 高 · related: M5b-G-06、M5b-E-02、F00-04

### M5b-G-19 §17-9 的 Agent 图像初审与 Owner 终审在机器上门禁面为零，视觉检查文档指向非产品浏览器
- 类别: F_TEST_GAP · 建议优先级: P1
- 位置: `docs/owner/RELEASE_STATUS.md::发布 Gate`（现 :90-91）；`docs/owner/CHANGE_REVIEW.md::已知限制`（现 :129）；`ci/checks.json`（112 项中无图像/视觉审核项）；`tools/gen_visual_views.py`（全仓仅 evidence/ 历史引用，无 CI 接线）；`VISUAL_CHECK_README.md::启动路径`（现 :8、:17、:35）；`docs/standards/RELEASE_STANDARD.md::版本记录指向`（现 :3、:8-11）；`AGENTS.md::目录规范`（launch→packaging/launch）
- 证据摘录:
  > Agent 图像初审：**未执行**（缺 `evidence/<phase>_sample_review.md` 与 `run/visual_review` 产物，见 §4）。
  > 在项目根目录打开 PowerShell 7，运行：`pwsh -File .\launch\start_browser.ps1`
  > - 版本记录：CHANGELOG.md + docs/RELEASE_STATUS.md 同步。
- 权威依据: 宪章 §17-9（接缝、黑洞、条纹、排异和 HiPS 结构有量化证据、Agent 初审及 Owner 图像终审）、§13.4、§15.4
- 问题说明: L0 自认该门未过，且 `ci/checks.json` 112 项里没有任何图像预览生成或视觉审核项（`gen_visual_views` 零接线）⇒ §17-9 无可执行证据链；`CHANGE_REVIEW.md:129` 进一步自认"跨 L0 文档状态词一致性目前无 CI 检查项"。面向 Owner 的唯一操作文档 `VISUAL_CHECK_README.md` 指向仓库根 `launch/\*`（该目录不存在，实际产物在 `packaging/launch/`，见 `AGENTS.md` 目录规范与 HANDOVER 的迁移记录），且 `launch/js/`、`launch/css/` 已作为跟踪文件留在仓库（其运行副本在 gitignore 的 `run/` 下），等于把非产品影子树当验收路径。
- 影响: 发布门禁第 9 条不可判定；Owner 视觉验收按不存在的路径操作。
- 建议处置: 把图像初审做成可定位证据（生成命令 + 产物路径 + 审阅记录 ID 入 L0 与证据门）；订正或删除 `VISUAL_CHECK_README.md` 指向的 launch 路径；把 `docs/RELEASE_STATUS.md` 从 RELEASE_STANDARD 的版本记录指向中移除（改指 `docs/owner/`）。
- 置信度: 高 · related: M5b-I-01、M5b-G-17

---
