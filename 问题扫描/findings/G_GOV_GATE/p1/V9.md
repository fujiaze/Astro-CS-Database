# V9 · 全门现状态清单（分片 1-2 立条；时点 HEAD `d7f46dd0`，`ci/checks.json` 经解析 = **130 门**，会话开始时 121）

### V9-N-01（P1·簇1 机制③+④的合成实例，且**直接推翻我工单 A1 的写法**）`c3452d48` 把三行「冻结科学门」从 `docs/TRACEABILITY.csv` **净删 4 行 / 0 新增**，而 CON-TRACEABILITY 的正确修法是回滚删除、**不是补关键词凑子串**
- 逐关键词命中（`scripts_v9_contrace.py` → `STATIC VERDICT: FAIL`）：`CAL 1 / PSF **0** / PHOT 1 / NOISE 6 / DRZ 3 / UPM 6 / REJ **0** / INT 1 / ACR 1`。⇒ 我工单原文只写「PSF 缺」，**漏了 REJ 同样零命中**。
- **肇因定位到提交**：`c3452d48`（`fix(RQS/B4)`，2026-09-14 16:19:36+08）对 `docs/TRACEABILITY.csv` 是**净删 4 行、0 新增**，被删行含 **`SCI-PSF-001`(VERIFIED,T101 frozen)、`SCI-REJ-001`(VERIFIED,T107 frozen)、`SCI-ACR-EQUIV-001`(VERIFIED,T109 frozen)** ⇒ **三条标注 `frozen` 的科学门行被一次 RQS 修复顺手删掉**。（命令：`git diff 535e7387 c3452d48 -- docs/TRACEABILITY.csv | grep "^[-+]SCI-" | grep -iE "psf|rej|acr"`）
- **为什么这是本轮最该修的一条**：门是按"关键词是否在文档里出现"判的（机制④子串判据），而删权威行（机制③校验对象错位）会让门**永远红**，于是**修门最省力的路径就是往文档塞词**——这正是 `A-38`/`E1` 想拦的组合。**工单已改写为"回滚这三行"，删掉"补 PSF 段落"的表述。**
- **related** `V2-N-02`（合同表与实况脱节）、`V4-N-16`、`V13-N-07`（派生视图订正而权威源未动）、`E1`、`A-38`、**`C-18`**（编号被删无检测 ⇒ 这正是 ID 集对账门要拦的）

### V9-N-02（P1·机制③的放大器）`check_traceability_matrix.py:387-390` 在表头不等处**直接 return** ⇒ 其后的行级发散**全部被遮蔽**，每次只暴露一类问题
- 事实：`_check_csv_parity` 遇 `header[0]='\ufeffmodule_id' != CSV_COLS[0]` 即 ERROR + 早退。**把 BOM 剥掉再跑同一条 parity 逻辑，仍有 5 行与 JSON 不一致**（`MOD-astrocs-phase1-calibration`、`phase1-noise-snr`、`phase1-photometry`、`phase1-star`、`phase1-star-psf`；差异列 `test_id,test_path,evidence_id,evidence_status,notes`）。
- 后果：施工方按红项修完 BOM 会看到"换了个新红"，**误以为没修对**或反复返工；而"一次只报一类"的门结构对 §12.3 的可证伪性是实质削弱。⇒ **改法：去掉早退，表头与行级差异同批报全**。
- **related** `M8-F-002`（CHECK(rc!=0) 判不出肇因）、簇 1 机制③、`V4-N-05`（双向锁测错对象）

### V9-N-03（P1）`TRACEABILITY_MATRIX.csv` 的 **BOM 在 HEAD blob 里**（非本机脏），而 checker 用 `encoding="utf-8"` 打开 ⇒ 干净检出即红
- 实证：`git show HEAD:docs/traceability/TRACEABILITY_MATRIX.csv` 前三字节 = `b'\xef\xbb\xbf' + 'module_id'`，**blob 与工作树字节全等（56580B）** ⇒ 不是"本机编码脏"这一类。⇒ **修法必须落到 git 内容层**（去 BOM + 重跑 `tools/traceability/gen_traceability_csv.py` 一并提交），并连带 `V9-N-02` 一起修，**只去 BOM 不够**（V9 明言）。
- **related** `V4-N-13`（读带 BOM、写不带，我本人的同类缺陷）、`C-19`（跨平台 manifest 须由一源生成）

### V9-N-04（P1·新增必红门 A5）`CON-API-CONTRACTS`（非豁免，三 profile）：`API_CONTRACTS.csv:371` 仍把 `estimate_mag_lim_by_density` 记 `VERIFIED`，而该声明已被 `6d74046d` 从 `ipv_select.h` 删除 ⇒ checker 判 `API-MISSING-AST` → exit 1
- 复算：HEAD blob `grep` 命中 1 处且那 1 处是**注释行 `:69`**（`48ceee59` 时为 2 处）；`scripts_v9_apicontracts2.py` 在**「本机工作树」与「仅 tracked 文件（干净检出）」两口径下同判 `findings=1`** ⇒ 非本机脏。
- ⇒ **这是 `V2-N-02` 的活体后果**：我在 `V2-N-02` 报「死符号仍以 VERIFIED 占合同表 ⇒ 门可红」，如今隔壁删了代码没删合同行，**门真的红了**。修法：**删该行或改 `RETIRED` 并登记**，不得为过门把注释当声明。（**related** `V2-N-02`、`V12-N-05`、`C-18` ID 生命周期）

### V9-N-05（P1·新增必红门 A6 + 一条口径地雷）`DOC-LINE-ANCHORS`：41 条 bindings 里 **13 条 `C4 BINDING_VIOLATION`**；而**本机口径另有 35 条 `C2 ambiguous`，两口径 13 vs 48 不一致**
- 13 条构成：**`PLATESOLVE-*` 11 条**（`docs/algorithms/PLATESOLVE.md:101-113` 的表锚停在 `ipv_entry.cpp:275/287/298…`，符号实测已移到 `317/329/340/353/407/421/473/529/623/668`）+ `P3RSMP-DESCRIPTOR` + `NOISE-MINPATCH` + `NOISE-CMAKE`。肇因：**P8/P11 改码未同提交订正锚**（根 AGENTS.md「修改代码测试后应同步订正 ci」的同类义务）。复算 `scripts_v9_anchors_final.py` → `docs=41 anchors=808 errors=13 => FAIL by code {'C4': 13}`（已按「仅 tracked + 同一 `ARCHIVE_MARKERS` 剪枝」模拟干净检出）。
- **口径地雷（对任何锚普查都成立，我记为自己的工具约束）**：本机工作树口径另有 **35 条 `C2 ambiguous`**，因**未跟踪的 `设计大纲/_evidence` 影子树进入 basename 索引** ⇒ 本机 48 errors vs CI 13。**"按 basename 索引"的门在脏树本机跑会得出与 CI 不同的结论** ⇒ 我的 `verify_anchors.js` 与 `_front_*` 脚本须显式排除未跟踪根目录，否则我会把 CI 不会报的东西报给隔壁。
- **related** `M6b-E-002`（行锚系统性漂移）、`V12` 附注（`gaia_client.c` 同文件 +1..+60 ⇒ **禁行号锚**）、`V13-N-08`（根目录未跟踪产物）、`S-4②`（`check_comment_anchor_exists`）

### V9-N-06（P1·连带放大，非独立缺陷但决定工单量）实体红 4-5 个会被**放大成 6-7 个红门**
- `CON-FULL-INTEGRATION` 用子进程聚合 `generate_contract_report.py` 的 **10 个 checker**；`KNOWN-FAILURES-BASELINE-CHECK` 对任何未登记新红 **fail-closed** ⇒ 单点红会扇出。⇒ 施工顺序须按 V9 档案 §3 的依赖图走，否则"修一个冒出两个"。
- 基线现状（我据此更新工单 D 节）：`ci/known_failures.json` **只剩 1 条**（`p1_noise_adapter`, ctest, conditional），**check 面豁免数 = 0**（`json.load(...)["failures"]` 实测 `1`）⇒ 我此前记的「35 条里 29 条过期」**已被吸收，须改数**。
