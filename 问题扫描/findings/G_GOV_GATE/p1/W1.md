# W1 片1 · G_GOV_GATE｜P1 一条

### W1-N-03（P1）未跟踪模块迁移面**整块零编译**，且新注释与负责人在案裁决相反（约 1400 行新适配器代码从未进过编译器）
- 事实链：① 新 `lib/snr_estimator/CMakeLists.txt:28` 注释称本子目录由根 CMakeLists 在 hips 之后注册 —— **为假**：根 `:210` 实为**被注释掉的 add_subdirectory** 且旁有明文裁决 `F-CI-002-01`（commit f74fc20f，owner 裁「目录保持 untracked 锁死不收编」；`:239` RPATH 摘除）。② target `astrocs_p1_noise` **不在任何挂载图**（全仓 add_library 唯一命中该未跟踪文件）⇒ `module_entry.cpp` 与 `include/astrocs/noise/types.h` **从未进任何编译器**。③ `tests/unit/CMakeLists.txt:1155` 门卫 `if(TARGET p1noise_under_test AND TARGET astrocs_p1_noise)` **恒假** ⇒ `tests/unit/p1_noise/` 的对拍测试**不编译**。
- 未跟踪新增面：`lib/snr_estimator/{CMakeLists.txt, include/astrocs/noise/types.h, src/module_entry.cpp, src/astrocs_p1_noise.def, src/module_exports.map}` 与 `tests/unit/p1_noise/{adapter_test.cpp, adapter_entry_impl.cpp}`。
- **判据说明**：本条按「隔夜未跟踪新代码」判，不要求入库；若邻站在途，请在提交时一并消解**注释与裁决的冲突**（要么改注释承认未挂载，要么按 §1.2 走裁决变更）。**后果**：1400 行的科学适配器在无编译、无测试、无 CI 采集的状态下增长 ⇒ 正是机制⑪「零采集 ⇒ 必漂」的最大单站。related 机制⑪、簇 7（与在案裁决相反）、`V11` S-4、`C-14`（新 ABI 面须带 struct_size）、`A-44`（裁决登记面）
