# V18 · 自证与旁路求值（分片 1；HEAD `d8726fd7`，纯静态，27 条排除前缀见 `_verify/V18.md`）

### V18-N-01（P1·`C-21` 判据②在全仓现状不成立）12 套 selfcheck harness 并存**三种 `cond` 语义**，且新增一种归因失真机制
- **10 套「不判即翻」**：注入分支排在 `!(cond)` **之前**，C++ `else if` 短路使 **`cond` 连求值都不发生**。逐站：`p1hips:64(+80)`/`p1cal:48(+60)`/`p1cos:53(+69)`/`p1psf:84(+110)`/`p1drz:64(+78)`/`p1sess:83(+99)`/`p1phot:60(+76,+85)`/`p1noise:53(+69)`/`p1wcs:150(+166)`/`aio_abi:66(+80)`。
- **1 套「判后翻」**：`P1STAR_CHECK:70/:72` → `record():53-58` `if (fault_hit(name)) ok = !ok;` —— **`cond` 求值了但结论被覆盖 ⇒ 改法与前一种不同**。
- **1 套无注入（正对照）**：`P2H_CHECK:34` → `check_impl:24-32`，`cond` 真求值。⇒ **三种语义并存、8 套有 `fault_reported` 支而 2 套没有、零一致性门**。
- **新机制（V15 未记，直接破坏归因）**：8 套宏体第一支 `if ((cs).fault_reported && faultname!=nullptr) ++failures;` ⇒ **首个命中后同组其后每条带名 CHECK 无条件计数**，而 `note_injected()` 只打印**首个**名字 ⇒ **`failures` 数 ≠ 被破坏的不变量数**，且报告里的 `FAULT-INJECT` 名与实际制造 `rc=1` 的那批**不是同一集合**。`p1psf`/`p1drz` 无此支 ⇒ **同名注入在不同家族得不同 `failures`**。
- **负结果（省实现成本，我已采纳进 `E11` 范围收窄）**：全仓**不存在** `#define CHECK(c)` 丢弃 `cond` 或整体编空的实例（候选 370 全是 cfitsio token-pasting/字符串化；`ACS_STATIC_ASSERT` 两支都 `static_assert((cond))` 真参与，V18 第一轮粗分类误判后两轮自纠）⇒ **旁路唯一形态是"求值次序"，机器门不必覆盖"空断言宏"这一支**。
- **related** **`C-21`（三条判据的实证核心，已按本条改数）**、`V15` 乙档、`V17-N-01`（127 码值碰撞，同族另一种失效）、`M8-F-015`、`E11`

### V18-N-02（P0·**补强 `V9-N-07`，不另立机制**）四道 `waivable=false` 门的"自检形态"里，`check_prod_reachability.py::_selftest:219-226` **把 `:100-106` 的规则重写了一遍而非调用主判定函数** ⇒ 两份可各自漂移、自检照绿
- 四门：`PRODUCTION-GRAPH`→`check_pipeline_graph.py --selftest`、`ISA-LEAK-SELFTEST`、`PROD-REACH-SELFTEST`、`LOG-CONTRACT-SELFCHECK`（唯一有真形态配对的是 `check_serial_heavy.py`：`NO-SERIAL-HEAVY` 真跑 + `SERIAL-HEAVY-SELFTEST`）。命令：`scripts_v18_selfonly2.py`。
- **三重失守（比 `V9-N-07` 更具体的三处机制）**：①**判定对象≠交付物**——ISA 门看的是脚本里硬编码的 `fake_cli_avx2="…vpaddd…"`（`check_isa_leak.py:123-128`），**交付二进制从未被它看过**；②**自检重写规则**（上句，`_selftest` 不调主判定）；③**路由误导**——`PRODUCTION-GRAPH` 名为"生产图"却声明 `changed_paths: graph/**` ⇒ **改 graph 会路由进一条不看任何 graph 产物的门**；`ci/INVENTORY_REPORT.md:57` 自认「登记 selftest 形态」，而 `:24` 仍把它与真门并列计 PASS。
- 另：`gates with --jsonl/--stdin = NONE`（`scripts_v18_logwire.py`）⇒ **日志合同的真形态无任何门消费**（`LOG-CONTRACT` 两头都空）。
- **可一条机器门覆盖，且性价比最高**（`E11` 定稿判据）：**`checks.json` 门若 `changed_paths` 含生产面（`lib/`/`cli/`/`include/`/`runtime/`/`graph/`/`providers/`/`modules/`）而 `command` 只含 `--selftest|--selfcheck`、且同 checker 无真形态配对 ⇒ FAIL**。纯 JSON+命令解析，误报成本最低。
- **related** **`V9-N-07`（同一 P0 的机制补强，不重复计数）**、`V11-N-05`、`V15-N-06`、`M8-G-001`、`E11`、`A-38`

## 三条对派单前提的订正（改我自己的 `C-21`，不改即误导施工）
1. **注入名全集不是 25 个**：arity 感知解析（`*_CHECK`/`*_CHECK_MSG` 取第 3 参、`*_CHECK_NEAR` 取第 5 参、`P1STAR_CHECK_EQ` 取第 4 参）→ **501 个不同名 / 787 个 CHECK 站点**；另 **226 个站点的注入名是变量而非法字面量**（⇒ 静态枚举注入名这件事本身在本仓不可完备，门须按调用点而非名字集）。命令：`scripts_v18_census3.py`。
2. **CI 用 `ENVIRONMENT` 驱动的是 3 处不是 2 处**：`lib/astro_image_io/tests/p2hips/CMakeLists.txt:51` `ASTROCS_HIPS_PROV_FAULT=missing_key`（V15 漏）。但 `ASTROCS_P1STAR_FAULT=`（`p1star:161`）**值为空**，语义是「**清除继承的注入位**」而非驱动 ⇒ **真驱动仅 2 处、各覆盖 1 个名**（2/501 ≈ 0.4%）。
3. **selfcheck ctest 目标是 13 个不是 12**：第 13 个是 `lib/plate_solve/cpp/ipv/test/CMakeLists.txt:91` `ipv_abi_layout_lock_selfcheck`（Python 侧，且是**全仓唯一合格的负例自检形态**：`make_mutated_mirror()` 破坏真镜像后调**同一个** `check_all()` ⇒ **这就是 `C-21` 判据的标准答案样板**，隔壁照它写即可）。
- **related** `V11`（隔壁 00:47 的 ipv 修复，其锁自带合格自检 ⇒ 我此前的"是否真会红未判"现可判 **合格**）。
