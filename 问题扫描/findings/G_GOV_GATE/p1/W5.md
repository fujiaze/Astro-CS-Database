# W5 片4 · P1 三条（错误码空间与名值表）｜HEAD a3a343a4

### W5-N-07（P1）`include/astrocs/abi/status_codes.h:210-211` 声明 `acs_status_name_v1`/`acs_status_domain_name_v1`，注释承诺稳定枚举→冻结字符串名（非法值 NULL），**但全仓 0 定义**
- 三级复核：L1 git grep（排 run/ 影子与 问题扫描/）只剩 2 声明 + 1 注释；L2 文件系统级覆盖 lib cli include runtime providers modules tests tools ci scripts cmake 同样只剩声明；L3 按定义形态（排除以分号结尾）搜无定义体。
- **同族三处一致性不齐（这是真缺口而非风格差异）**：`acs_artifact_status_name_v1` 有生产实现（`artifact_abi_v1.c:624`）、`acs_lc_state_name_v1`/`acs_module_op_name_v1`（`lifecycle_v1.h:237-238`）**只在测试探针里由测试自己实现**（`abi002_lifecycle_probe.c:101/117`）⇒ status 无实现、lifecycle 仅测试实现、artifact 生产实现。**后果**：生产 TU 采用声明即链接失败；Python 侧无任何 `ACS_ERR_*`/`AIO_ERR_*`/`detail_code` 名值映射。related `M5b-E-04`、`V11-N-05`（lifecycle 六函数零实现同族）

### W5-N-08（P1）`module_adapters.cpp:300-315` 的 `status_str` 是**第 4 份私有名值表**，且**缺 `ACS_ERR_EXCEPTION=10`** ⇒ 打 UNKNOWN(10)、域折叠落 INTERNAL
- case 覆盖 0-9 与 70，缺 10；`:317-329 to_result()` 也无 10 ⇒ default INTERNAL。**产生侧真实存在**：`status_codes.h:164` 定义 10 为「DLL 边界捕获内部 C++ 异常并转换（禁异常外泄）」，`lib/calibration`、`lib/cosmetic` 的 module_entry 按该族头使用；而**宿主 TU 只 include legacy `common_abi_v1.h`（该族无 10）** ⇒ **同名枚举两套成员，语义由 TU 选哪份头决定**。第二处折叠：`ACS_ERR_STATE=7`、`ACS_ERR_SELFTEST=9` 也 default INTERNAL，经 `runtime_client.cpp:395-400` 变 CLI 70，而 `exit_codes.h` 里「签名/加载/自检失败」属 BACKEND(5)；SELFTEST 返回点真实存在（`backend_table.inc:50`）。可达性需运行期未判；**表缺项 + 域折叠 + 两族同名不同集为静态可判**。related `M5b-E-04`、`M5b-G-13`、`M5b-C-05`、`W5-N-01`（异常被记 COMPLETED 是同一条链的两端）

### W5-N-09（P1）错误码登记事实源**文件不存在**：`lib/orchestrator/cpp/include/orchestrator.h:111` 与 `tools/gen_audit_pack.py:74` 都引 `engineering/contracts/error_code_registry.csv`
- 三级复核：`git ls-files` 检索 rc=1；`engineering/contracts/` 下无 error/code 命名文件；仅 `设计大纲/_evidence/packs/history/` 有历史副本 ⇒ **悬空权威引用**（另见 `SA-N-04` 同形态）。**后果**：`AstroCsExitCode`（0-10、20-28、100 基址）与 CLI 11 码段之间**无分配表载体** ⇒ `LOGGING_DIAGNOSTICS_STANDARD.md:9` 要求的「error code → troubleshooting → source/test」链断。免重报：账本内该关键词 0 命中。related `SA-N-04`、`M5b-E-04`、`E4`
