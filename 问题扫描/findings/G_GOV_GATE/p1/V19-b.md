# V19 片2-4（P1 五条 + P2 三条 + 负结果）HEAD 4ea24d07 / 132 门 / 纯静态

### V19-N-04（P1）UT-QUALITY 的门禁对象 build/lnx_v5_clean_rel/astrocs 在 CI 与 CMakePresets 与 workflows 三面零生产者 ⇒ LNX-005 发布包合同 6 用例恒 skip 而非豁免门仍记 PASS
- :20 的 skipUnless(isfile(BUILD)) 让 :109/:120/:131/:139/:149/:159 全 profile 隐身。改法：生产侧补 Release 构建树并给 wf_step 加 require_outputs，或守卫改 fail-closed（同仓正对照 test_seam_metric_gate.py:24 的 _require_prerequisites 与 check_cli_protocol.py:439 的 require_binary 退 77）。**明写不得做的方向**：不可把 BUILD 指回 build/linux-control，那会把发布包判据降级成任意构建树。related V15-N-16、M8-F-003、L23-003、V17-N-03

### V19-N-05（P1·即负责人裁决 A-28 的施工面）21 道门真调用 g++/gcc/nm/objdump/tar/bash/taskset，而这些名字在 prerequisite_tools 出现 0 次
- 值集合只有 ctest/cmake/clang++/clang/llvm-profdata/llvm-cov/python3 四项；姊妹门成对反差（两门 p1wcs 类声明 astropy+numpy，而 UT-BACKEND 有 5 numpy 4 astropy 裸 import、UT-IO 有 4 numpy，声明均空）。后果两并存：skipUnless(which) 型静默 skip；裸 subprocess 型 rc=127 判 FAIL 但归因成「门坏了」而非「宿主缺工具」，绕过 probe_prerequisite 的 FAIL(prerequisite) 留痕（与 V17-N-01 的 127 码值碰撞同类）。改法：逐门补声明（纯登记面一次做完）+ validate_registry R12（AST 扫 command 体的 argv 首位与顶层 import 求差集即 FAIL）。related A-28、L15.md:623、V9-N-10、V17-N-01

### V19-N-06（P1）环境守卫让 152/674 用例（22.6%）静默隐身，而 R11 与 runner 两侧都只数采集数 ⇒ 整类被 skip 与整类通过在结果面上完全同形
- 分布 UT-CLI 71/174=41%、UT-API 33%、UT-BACKEND 21%、UT-RUNTIME 13%、UT-QUALITY 10%、UT-ARCH 8%、UT-MONITORING 4%（其守卫是 isdir /proc 而该门 platform=any 且在 windows-main 内）。R11（validate_registry:99-138）纯 AST 数 test_ 方法且 total==0 才红；run.py 对 unittest 的 OK (skipped=N) 零解析。改法：守卫率 >20% 须登记新字段 env_guarded_cases 否则 FAIL；runner 解析并披露 skipped_cases 一轮后升门。related V17 轴总结论、L23-001、M8-F-001、A-38

### V19-N-07（P1）跨门输入依赖写不下也查不到：schema 无 inputs/depends_on 字段，而 run.py:616-623 对 run/ 前缀无条件豁免存在性探测
- 消费 run/ci 的门 41 道、消费 build 的 13 道；**fast（63 门）与 windows-main（68 门）结构性零生产步骤**，全仓唯一根 build 生产者只挂 linux-main。豁免注释自证是为免误判产物缺失，但 --build-dir 指向的是输入不是产物 ⇒ 登记面无法区分；顺序契约只有 R10 一条被机器守。实测 --focus 选消费者不选生产者（producers=NONE），--changed-from 侧 consumers 42/42 不可达、六生产者全不可达 ⇒ 定向复跑整条构建加 ctest 链不可选。改法：schema 增 requires_artifacts + probe 探其在位 + R13 双向闭包 + impact_map 补 deep_ci_driver.py（现 40 道 CTEST 门无一把它列进 changed_paths）。**正对照（列为模板非缺陷）**：known_failures_baseline.py 的 parse_ctest_junit 缺文件与零 testcase 皆 error、compare_failures:452-457 明写不得静默跳过。related V15-N-16、V9-N-12、G-D、C-12

### V19-N-08（P1）dirty_ignore_prefixes 被用来遮蔽非豁免门无条件覆写 5 份**受跟踪** ISA 测量 CSV 并读回当判据（工作树现见 3 条 M）
- UT-BACKEND 声明 mutates_workspace=false 加 dirty_ignore_prefixes 含 artifacts/prerelease_v5/；test_isa_avx512.py:75-82 与 test_isa_variants.py:79-85 无条件覆写其下 MEASUREMENTS.csv，而这些 CSV 在 git ls-files 且被 docs/architecture/ISA_VARIANTS.md 当权威引用；run.py:777 的 ignore 集等于 outputs 加 dirty_ignore ⇒ 该事实对 DIRTY 判定永久失明；同一 CSV 又被 test_isa_avx.py:101-103 与 test_isa_bit_manip.py:66-68 读回当判据 ⇒ 自写自读。⇒ **同一 SHA 在两宿主可写出不同的权威台账数值**（触 §12.3/§17.6）。改法二选一不得维持现状：写出面改 run/ci/isa/ 并删豁免；或裁台账留仓内则改 mutates_workspace=true 让 dirty 如实红。related V15-N-11、V9-N-15、C-15

### V19-N-09（P2）7 道门 changed_paths 与 5 条 impact rule 是零 tracked 命中的死模式（3 条正落 gitignored 区）⇒ 这些路径上的变更永不触发对应门
- 清单：graph/**（且 .gitignore:130 忽略）、legacy/**、docs/glossary/**、schemas/**（真源在 contracts/schemas/）、schemas/traceability_matrix.schema.json、.git/HEAD（永不进 diff 面）、CONTROL_TASK_LEDGER.csv；impact 死规则含 AstroCS.wiki 与 BASS DR3 两资料区。改法：逐条订正 + validate_registry R13（每模式须 ≥1 tracked 命中，白名单 run/**）。related M6b-E-005、V9-N-13、**V17-N-02（同族：那是 PROD_FILES 侧、本条是触发面侧）**、C-15

### V19-N-10（P2）outputs 与 dirty_ignore 与 .gitignore 三面零联动：两条半登记产物既不能提交也不被忽略
- KNOWN-FAILURES-BASELINE 的 artifacts/KNOWN_FAILURES_BASELINE.json 与 WIN-PACKAGE-CANDIDATE 的 artifacts/candidate/AstroCS-candidate.zip：实测 git ls-files 零命中且 git check-ignore 零命中，而 run.py:777 恰让 outputs 自豁免 dirty 检测 ⇒ 每次跑都留下一个既不能提交也不被忽略的文件（违目录规范），前者还在三个 profile 上跑。改法：归位 run/ci 或显式 gitignore + R14 断言。related M5b-G-16、L12-021、V9-N-15、C-15

### V19-N-11（P2）glob 型输入零校验：CI-BINDING-TESTS 的 -p 模式若被改到匹配 0 文件，门永久绿且两个守卫都不触发
- run.py:614-615 对不含斜杠的参数直接 continue 不探测而 -p 的值恰无斜杠；R4/R6 只核文件路径存在，glob 不属之 ⇒ 全链无处校验命中数。discover 零采集时 stdout 打 Ran 0 tests 与 OK、stderr 非空 ⇒ FAIL(missing_output) 与 FAIL(empty_outputs) 均不触发。改法：probe 对含星号与问号的相对 pattern 探命中数 >0；_collect_test_files 从目录总用例为零升级为逐 -p 命中文件数大于零。related V9-N-14（数值面已报，本条只补机制）、V9-N-11、L23-001

### V19-N-12（负结果 · 不立条，供邻站定性与防误报）
- 轴③：132 门静态**零真实数据依赖、零外网依赖**（GaiaDR3 与 BASS DR3 在 cmake 与 CTest 注册面零命中；http(s) 命中全是 example.com 与 127.0.0.1:1 与 [::1] 等脱敏 fixture）⇒ 任何「CI 依赖 GaiaDR3/Fatduck/网络」的表述在本轴口径下不成立，真机耦合全在宿主能力面（N-05/N-06）与空 profile（N-02）。轴①：132 门 argv 首位全为 python3、command 指向的仓库文件 token 全部存在于 tracked 树 ⇒ 文件缺失型 error 不成立 ⇒ **本轴结论全部落在「引用得到但校验不到」这一层**。轴④：KNOWN-FAILURES-BASELINE-CHECK 的 fail-closed 加 R10 末位契约是**推荐给全部依赖门对齐的模板**。
- 根事实（八条共同根）：**ci/checks.schema.json 是 additionalProperties false，可选字段只有 changed_paths 与 requires_monitor 与 prerequisite_tools 与 ctest_targets 与 dirty_ignore_exact 与 dirty_ignore_prefixes ⇒ 没有 inputs/depends_on/needs/produced_by**，即「我需要谁的产物」在登记面上无处可写。实测频次 prerequisite_tools 18、ctest_targets 40、dirty_ignore_prefixes 2、dirty_ignore_exact 1。
- 需运行期未判 6 项：hosted 是否真装七工具；avx512f 与 os.cpu_count 实值；lnx_v5_clean_rel 是否曾在 vm-bj 存在；41 道消费者在生产者未跑时退 127 还是 8；PRE_TEST 发现模式在目标未构建时的 ctest 行为；两条 artifacts 产物有无仓外消费者。