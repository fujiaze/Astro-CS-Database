# M2a 定稿 · G_GOV_GATE · P1（来源切片 L10）

> 合并代理: M2a · 复核时点: 全部证据按**当前树**重新定位（path::符号）。

## M2a-G-1 标准注册表的机器检查器存在但未登记进 ci/checks.json，D.catalog/D.fits 的失真 CONFORMANT 判定无门可拦（注册表自身已自陈该事实）

- 类别: G_GOV_GATE
- 优先级: P1
- 来源: L10-013（与 M2a-B-1/M2a-B-2/M2a-B-4 同根：三处判定失真都发生在无门禁的面上）
- 位置: docs/standards/STANDARDS_REGISTRY.md::§5 机器检查段；.github/workflows/build.yml::standards-registry 步骤；ci/checks.json（缺项）；tools/check_domain_interfaces.py::STANDARDS_PATH；docs/standards/checks/check_standards_registry.py
- 证据摘录（逐字，复核时点现文）:
  > （STANDARDS_REGISTRY.md §5）- `docs/standards/checks/check_standards_registry.py` 校验：每个 ID 的必填小节存在；…；**该检查器当前尚未登记进 `ci/checks.json`（缺 CI 检查项 ID）**——已作为 finding 登记，由 CI 域原子任务承接（宪章 §12.3 机器一致性检查清单须与 CI 一致）。
  > （ci/checks.json）grep `standards|registry` → NO HIT（当下树仍无该项）
  > （docs/standards/checks/check_standards_registry.py 存在；tools/check_domain_interfaces.py 引用 STANDARDS_PATH）
  > （宪章 §12.3 区段）机器一致性检查清单…CI 必须逐步覆盖；清单与 CI 不一致须登记 finding
- 权威依据: 宪章 §12.3（机器一致性检查清单须与 CI 一致、CI 逐步覆盖）、§17.1（发布门须由机器证据支撑）；STD-REG-001 §5 自述
- 问题说明: 检查器已在树内、注册表也诚实自陈「尚未登记 CI」，但承接任务未落地：`ci/checks.json` 里没有该项，CI 与追溯/接口清单因此都读不到它。结果是注册表的结构性断言（ID 唯一、必填小节、偏差 ID 闭合、域/状态取值域合法）只在有人手动跑脚本时成立。本轮在该域实测到的三处失真（历元判定式丢维、D.fits §6 的 CONFORMANT、DISP-DRZ-004 的过期偏差）全部落在该门的盲区——不是巧合：无门时，判定行只靠人工维护，条款面与判定式必然分叉。
- 影响: §12.3 与 §17.1 不成立；CONFORMANT 判定可静默失真而 CI 全绿；已登记的 finding 长期挂空（自陈≠修复）。
- 建议处置: ① 以 `non_exempt` 项登记 check_standards_registry.py 并纳入 ci/checks.json（含 profile）；② 把 M2a-B-2 的「判定式须逐项覆盖条款面」写成脚本断言；③ 把 DISP 条目状态与真实处置任务 ID 的绑定关系纳入同一脚本；④ 同步订正 STANDARDS_REGISTRY §5 的自陈（改为已登记 + 项 ID）。
- 置信度: 高
- related: M2a-B-1、M2a-B-2、M2a-B-3、M2a-B-4；L10-013
