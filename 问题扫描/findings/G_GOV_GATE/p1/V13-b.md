# V13 定稿 · 追溯主表的「假 VERIFIED」结构与制度缺口

### V13-N-06 `c3452d48` 删掉的 4 行 VERIFIED 合同行**无任何对应登记动作** ⇒ 合同面与追溯面永久分叉且恒不红
- **ID** V13-N-06 ｜ **类别** G_GOV_GATE ｜ **优先级** **P1** ｜ **状态** OPEN
- **逐面核**（三级复核）：净删 4 行（SCI-PSF-001/SCI-REJ-001/SCI-INT-001/SCI-ACR-EQUIV-001，+0/−4）；`CHANGELOG.md`、`memory.md`、`docs/backlog` **0 命中**；`40_OWNER_DECISIONS.md` **0 命中**（**不是负责人裁决的删除**）；账本 `M3b-F-02`/`M3-E-001` 的 `fix_note` 通篇未提删行，而 `verified_state` 却已回填 VERIFIED（**该回填是我做的，判词需据此订正**）；生成器 `v19r3_traceability.py` 的 `CONTRACTS` 已无这四 ID ⇒ **重跑即永久缺失**。
- **最关键一层（结构性不可红）**：`docs/contracts/INDEX.yaml:42/:74/:82` 三 ID 仍 `status=ACTIVE`（`:122` 为 DORMANT），而 `CONTRACT-GRAPH` 门（`waivable:false`）**只验 INDEX.yaml 内部一致性、与 `TRACEABILITY.csv` 无 ID 集对账** ⇒ 合同面说四条活着、追溯面说没这四行，**两者永久分叉且无门可发现**。叠加 `CON-TRACEABILITY` 在 HEAD `8c977118` 仍 `TRACE-CORE-MISSING PSF/REJ` 红（`V6-N-01` 至今未修）。
- **建议处置**：①恢复四行（`V6-N-01` 处方）；②给 `CONTRACT-GRAPH` 或 `CON-TRACEABILITY` 加一条 **ID 集对账**：INDEX.yaml 中 `status=ACTIVE` 的合同 ID 必须出现在 TRACEABILITY.csv，缺失即红（**本轮发现的成本最低、拦面最大的一条**）。
- **取证口径** V13 三面 grep + 判定器内存复算 ｜ **置信度** 高 ｜ **related** **V6-N-01**、`M3b-F-02`、`L28e-E-001`、`D-06`、`A-37`

### V13-N-04 追溯主表的 status 列 63/63 全 VERIFIED 却**全仓零门消费** ⇒ 「假 VERIFIED 恒不红」是结构事实
- **ID** V13-N-04 ｜ **优先级** **P1** ｜ **状态** OPEN
- **实测**：读 `docs/TRACEABILITY.csv` 的共 3 个检查器（`tools/quality/check_traceability.py`、`tools/quality/contracts/check_traceability.py`＝CON-TRACEABILITY、另 `tools/check_traceability.py` 读的是另一张 `claim_id` 表），**前两者 grep -n VERIFIED 全 0 命中**，只读 `requirement_id`/`authority_doc`/路径存在性；V13 内存复算：任意行改 VERIFIED 或 BROKEN **不改变任何门退出码**。
- **铸造与生成器不在 CI**：42/63 行（**66.7%**）由 `v19r3_traceability.py` 范围循环铸造（四族），该生成器 `ci/checks.json` **零登记** ⇒ **交付表是私器输出**。
- **VERIFIED 语义实=「锚存在」而非「要求被验」**：15 行 SCI-NOISE-i 的 test_ids 一律 TEST-SNR-001（**15 要求 : 1 测试锚**）、10 行 SCI-UPM-i 一律同 4 个 gtest 名；main() 判定式＝路径存在 + 符号在实现文件 + 函数名在测试文件。
- **建议处置**：①让 CON-TRACEABILITY **消费 status 列**（VERIFIED 行须给可解析证据锚且 refs>=2，否则 MISSING）；②加「要求 : 测试锚」覆盖率下限（15:1 即红）；③生成器登记进 CI 并加 `--dry-run`。
- **related** **V13-N-01**（EVID 层同型）、`L28e-E-001`、`A-37`、`A-38`、簇 2

### V13-N-05 登记面零宿主新口径计数：**28 枚非登记面零宿主**（SCI 侧 14 为本层新案；此口径不与八级阶梯引用度相加）
- **ID** V13-N-05 ｜ **优先级** **P2** ｜ **状态** OPEN
- **计数**：42 枚铸造行逐枚去登记面（3 表 + 生成器 + v19r3 报告）后计数——SCI-UPM-002..010 **9/9 枚 0**；SCI-NOISE-004/006/009/010/012 五枚 0（其余有 sporadic 真宿主：002/005 有 `p1noise_oracle.hpp` 注释、014 有 `NUMERIC_STANDARD.md`、003/007/008/011/013 仅审计文案）；TEST-PR-UPM-002..010 9/9 枚 0；TEST-UPMW 002/003/005/006/007 五枚 0。
- **重复载体**：`v19r3_traceability.py:283-292` 的 `pr_tests` 列表字面重复 ⇒ TEST-PR-UPM-005 与 -008 同指 `TEST(Phase2Upm, UpmPersistRoundtripChainNoDrift)@synthetic_gate.cpp:5214`、-006 与 -009 同指 `InsertionOrderIndependent@:5260` ⇒ **10 行 VERIFIED 实为 8 个测试实体**；**行↔实体唯一性无机器检查**（C5 只管 module_id/src_id/evidence_id 全表唯一）。
- **related** `L28e-E-001`（TEST 侧 14 枚归它，只计数不重报）、`V13-N-04`、簇 4 补二（注释面口径 66.3%/19.2% 与本表行面口径**不可混用相加**）

### V13-N-07 制度缺口：登记面对新编号**零承载要求** ⇒ 「新造未登记」在制度上无可违之规（附一条 SUPERSEDED 自毁）
- **ID** V13-N-07 ｜ **优先级** **P2** ｜ **状态** OPEN
- **净新增 ID 核对**：`dce8abd4` +3（ALG-DRZ-001/ALG-P2-SMP-001/SCI-UPM-001，均既有编号正文引用，定点读确认宿主）⇒ **干净无新造**；`c3452d48` +11 = 3 JSON 自铸 EVID（`V13-N-01`）+ 2 CSV 自铸 EVID（`V6-N-06`）+ TEST-CAL-001（既有 ID 合法并入，多宿主）+ 字符串 SCI-PHOT（title 切片噪声）+ 4 枚 CTEST-* 检查名子串（各恰 1 处登记、目标真实，**非铸造问题**）⇒ **其全部新造都落在无承载校验的面上**。
- **制度层面（根因而非单点）**：`contracts/schemas/traceability_matrix.schema.json` 对 EVID **仅格式正则**；`SPEC`/`docs/standards` **无「编号先登记后使用」或「EVID 须对应证据件」条文** ⇒ 铸造行为本身不违规。V13 结论我完全认同：**制度缺口即交付缺陷**，须在 `docs/standards/` 补一条**编号生命周期**规则（登记→引用→承载→退役）。建议并 **C-18**。
- **一条自毁细节**：`M3b-F-02` 的 `fix_note` 自述「非权威 CSV 加 SUPERSEDED_BY」——全仓 SUPERSEDED_BY **0 命中**；实际是 `TRACEABILITY_MATRIX.csv:11/:12` 两行 `notes` 内的 SUPERSEDED 字样（称现行权威为 json），且**字样不在 JSON notes** ⇒ `gen_traceability_csv.py`（JSON→CSV 逐列生成）重跑即蒸发 ⇒ **「本视图已被废止」的声明，宿主恰在被废止的那个视图上**。
- **related** `V6-N-05`（同提交文案自斥族）、`V6-N-02`（CSV 是生成物）、`A-37`

### V13-N-08 工作树根目录遗留 p8..p11-files.patch（归批提醒，非本层所为）
- **ID** V13-N-08 ｜ **类别** I_DOC_HYGIENE ｜ **优先级** **P2** ｜ **状态** OPEN
- **事实**：仓库根目录现有 `p8-files.patch`、`p9-files.patch`、`p10-files.patch`、`p11-files.patch` 等未跟踪补丁（V13 只登记事实并声明非本层所为；前台此前已就 p8 记过一次）。
- **依据条款**：AGENTS.md 目录规范（根目录只允许固定条目，任何新产物必须落位对应目录；`run/` 是临时操作与 agent 工作区落点）。若补丁被充当权威依据即触 **C-15**。
- **建议处置**：归位 `run/patches/`（已入库则删）；把「根目录出现未跟踪 `*.patch` 即红」并入既有根目录纪律检查（我这侧的 `changed_watch` 可承接）。

> **V13 终判摘录（假 VERIFIED 共 11 组）**：MATRIX 双视图 5 行自铸 evidence + 3 行散文 test_path + 6/6 VERIFIED evidence 在自身 authority 目录零承载（`returns/` 目录根本不存在，C8 非 strict ⇒ WARN-only）+ TRACEABILITY.csv 42/63 生成器铸造行（28 枚登记面外零宿主、TEST-PR-UPM 两对重复载体、status 列零门消费）+ file_audit_inventory 713/713 类别查表铸 VERIFIED（batch_summary 硬编码 PASS，三铸造工具零 CI 登记，经 build_v19r3/r4 进审核包）。**通过项（防误伤）**：TEST-P1-{NOISE,PHOT,STAR,PSF}-001 多宿主真源在册；4 个 CTEST-* 登记齐；fixtures 负样本；DISP-* 无脚本铸造；本批 CSV 旧 doc 锚滞留属「未跟进」非「回改」（**反向钉死专查 0 命中**）。**根因三件**：登记面对 ID 零承载要求 + status 列零消费 + 合同↔追溯无对账门。
