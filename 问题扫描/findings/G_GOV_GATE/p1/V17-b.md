# V17 片2 · N-04..N-06（全 P1）

### V17-N-04 `test_mon003_synthetic.py`：**编译失败 → `skipTest`**（该红时静默），而它守的是 §10.5 资源合成判定
- `:38` `cls.skip_compile = r.returncode != 0`（`g++` 编 `mon003_synthetic_main.cpp`+`baseline_backend.cpp`+`host_services.cpp`）⇒ `:50-51`/`:63-64` `if self.skip_compile: self.skipTest("编译失败")`；**`UT-BACKEND` discover 对 skip 退 0 记 OK**。⇒ **harness 接口漂移正是最需要红的场景，却是静默跳过**。偏向=绿；正常有 `g++` 时走全执行，**弱化仅在编译断时触发——而触发即目标场景**。
- 改法：`setUpClass` 编译非零 ⇒ `raise`（类级 ERROR=红）。**同仓正对照两处**：`test_isa_bit_manip.py:32`（同类编译夹具用 `assert r.returncode==0`）+ `p1wcs_astropy_cross` 头注「未设则判定 FAIL 不静默跳过」。
- **related** `V9-N-17`（门的测试侧对应物）、`FD-G-004`、簇 1 机制⑤、`V17-N-01`

### V17-N-05（潜在缺陷）`test_iso_acr_gpu_isolation.py`：**生产源清单逐条 `if not isfile: continue` 包住负向断言** ⇒ 任一改名即整体脱扫恒空
- `test_01:83-94`（`PRODUCTION_SOURCES` 六条）逐行禁词后 `assertEqual(violations,[])`；`test_02:97-107` `if isfile(p): assertNotIn(register_phase2_acr_kernels)` ⇒ **违规符号随重构迁名 = 双重绿**。当前 11 条路径实测全在 ⇒ 走完整扫描（**结构缺陷，非现行**）。
- 另一层：类门 `:41 skipUnless(cmake ∧ EXE)` 把 **两个纯静态文本检查连坐到 `build/cli/astrocs` 运行期产物上**（EXE 面本身＝`M5b-G-01` 兼容树薄弱的延伸；与 `V15-N-16` 同一根因）。
- 改法：`isfile` 缺失计 violation；静态检查拆到无 EXE 依赖的门类；清单由 `git ls-files` 派生 + 下限断言。**related** `V17-N-02`（**同型但那条死路径已发生、这条仅结构 ⇒ 分条不并**）、`M5a-G-008`、`E13`

### V17-N-06 陈旧 `.so` 复用家族：种子 fits 站**外的三孪生站**，四站全部无源↔产物新鲜度前置
- 同款 `if LIB_SO.exists(): return` 在 `tests/io/test_hips_input_contract.py:54-56`（`runtime/io/libhips_core_test.so`）；`tests/io/make_hips_fixture.py:43`、`hips_output_fixture.py:48` 为"存在即复用"镜像写法。
- **实证**：两 `.so` mtime=**9月3日 03:05**，而 `runtime/io/fits_core.c` mtime=**9月7日 01:29**（最近提交 `6825cb53` @09-07 01:36）⇒ **契约测试正对比源旧 4 天的二进制执行**；`.gitignore:31 "*.so"` 且 CI 无生成步 ⇒ **宿主机上永不重编**。偏向=绿（源改坏仍对旧 `.so` 绿）。触发面：**控制节点/终验面默认触发**（§15.4 终验在 vm-bj，本机即活例）；hosted 全新检出则现编现测 ⇒ **这解释了为何同一测试在两处结论可相反**。
- 改法：删 `exists` 早退 ⇒ 无条件重编（<1s），或 `max(源mtime)>产物mtime` 重编，或编译期 `sha256` 符号自证。**行为是否已分歧＝需运行期，未判**（只判前置缺失）。
- **related** `V11`（`test_fits_stream_contract.py:29-32` 种子站）、`C-12`（对陈旧交付物下判）、`V19`（依赖声明矩阵应把此类"陈旧产物依赖"列为一类）
