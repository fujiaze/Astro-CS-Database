# V14 分片 3 · N-07/N-08 + 已核为一致的面（负面清单，供免重报）

### V14-N-08 registry 页的**生成面与修订面同盘互踩**：重跑生成器即蒸发 10 页人工修订，并把占位 ID 钉回 docs 面
- **ID** V14-N-08 ｜ **类别** G_GOV_GATE ｜ **优先级** **P1** ｜ **状态** OPEN
- **机制**：`tools/quality/gen_module_readmes.py:134-143` 以 `module_adapters.cpp` 的 descriptor 为**唯一源**、对 `docs/modules/registry/` **无条件 `open(p,"w")` 覆盖**（把 `execution_class`/`parallel_ok`/`alg_id` 占位直接渲染进页面，连「已知限制」行都是字面抄 `{d[alg_id]}`）。
- **实况**：26 页中 **10 页自报「事实修订：将派生页占位 ID 更正为真实冻结 ID」**（cosmetic / noise-snr / photometry / star-psf / wcs-platesolve / coverage / integrate / reject / sample / write），**但修订只落在生成物、descriptor 未动** ⇒ 任何人重跑生成器：**10 页修订蒸发 + `V14-N-04` 的占位 ID 重新出现在 docs 面**。生成器在 `ci/checks.json` 与 `.github` **均 0 接线**（既不进 CI 也不进 pre-commit ⇒ 无人知道自己会踩）。
- **同机制族**（这是本轮反复出现的同一个形状）：`V6-N-02`（改 MATRIX 的 CSV 视图不改 JSON 权威）、`V6-N-06`（SUPERSEDED 声明宿主在被废止视图）、`V13-N-07`（同上）。⇒ **"权威源未改、改的是派生视图"的修复一律会随生成器回退**，这是 `C-13`/`C-19` 需要覆盖的机器判据：**修订必须落在权威源，派生视图不得手工编辑**（生成器应检测目标文件被人工改动并拒绝覆盖或报警）。
- **建议处置**：①把 10 页的修订**回写 descriptor**（真实冻结 ID 与 `execution_class`），生成器只作视图；②生成器加"目标已被人工修订 ⇒ 拒写并报错"（或写前 diff 报警）；③把生成器接进 CI（`--check` 模式：生成物与仓内不一致即红），否则它永远是一个没人跑的抹除器。
- **related** **V6-N-02**、`V6-N-06`、`V13-N-07`、`V14-N-04`、`C-13`、`C-19`、簇 9

### V14-N-07 四个新 `SNR_API` 导出在**第三登记面**（`module.yaml` 的 exports/source_symbols）同样零登记，而该字段头注自述「源码核对清单…禁止手抄他版」
- **ID** V14-N-07 ｜ **优先级** **P2** ｜ **状态** OPEN
- **事实**：`snr_source_snr_f64`/`snr_moffat4_profile_f64`/`snr_frame_depth_f64`/`snr_calib_zero_point_standard_error` 在 `snr_estimator.h:240-256` **真实存在**，但 `lib/snr_estimator/module.yaml` 的 `exports`/`source_symbols` **0 登记**；该字段头注自述「源码核对清单…禁止手抄他版」⇒ **自称"源码核对"者已与源码不符**。docs 面 0 登记归 **`V6-N-12`** 父条不重报。
- **为什么值得单独一句**：同一批导出**三面（header / yaml / PUBLIC_API）互不校验、且无任何门比对**（`tools/quality` grep 0 消费者）⇒ 这是 `V14-N-08` 的通用形状：**多副本登记面无生成关系、无一致性门**。建议并进 **E6/E4** 的实现：一次 AST/正则提取 header 导出集合，比对 yaml 与合同视图，差集即红。
- **related** `V6-N-12`（父）、**V14-N-08**、`E6`、`A-36`、`C-18`

## V14 已核为**一致**的面（负面清单：后文与后续轴**不得重报**，也防止我误报）
1. `module_ports.registry.json` ↔ C++ 绑定表：**20/20** 的 `operation`/`entry`/`resource_class` 逐行一致（`entry` 为声明名由 JSON note 自认、归 RT-002，**不判缺陷**）。
2. `packaging/astrocs.product.json`(10) ↔ `install-tree.contract` ↔ `install_layout` 的 `install(TARGETS)`：当前**三方一一对应**，`kind`/`abi_version` 全符。
3. providers 头宏 `KERNEL_COUNT`（12u/2u/1u）= `kKernels` 行数；`determinism` 列 **12/12 一致**（漂移只在 precision 列，即 `V14-N-03`）。
4. **22 个 descriptor 全部在 `register_phase_modules` 注册，无孤枚**。
5. `noise-snr` 生产路径（`lib/phase1/noise`）**实际有 omp** ⇒ 其 `cpu_heavy` 属实；`cpu_heavy` 的失真实例仅在 `p3_properties`/`p3_wcs`（`V14-N-06`）。
