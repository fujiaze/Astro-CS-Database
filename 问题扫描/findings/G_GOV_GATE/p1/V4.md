# V4 与 V6 终片 · P1 汇总（V4 时点为 adaeb531/dbaa2e18/c1959436，未含 P10=57c04256）

### V4-N-04 我把 M8-F-001 自降为 PARTIAL：新采集 R11 无任何 CI 执行面
checks.json 引用 validate_registry 的检查项为 0；唯一调用者是 ci/tests/test_workflow_lock.py:624，而 CI-BINDING-TESTS 的命令只 discover 匹配 test_ci001b 前缀的文件，不收它；ci/run.py 也不 import。⇒ 「discover 目录 0 用例、直跑脚本 0 采集即 strict FAIL」这条新规则本身正是它所描述的失守。**前台自纠**：我先前只核到「R11 在位 + 18→22 采集」就填了 VERIFIED；「校验器里有条目」不等于「条目会红」，必须追到退出码路径——这正是我给隔壁立的簇 1 第一机制，我自己踩了。

### V6-N-10 有修无账：账本 20 条的 fix_commit 全集与本轮 9 个真改代码的提交完全不相交
V6 逐提交查引用：35c85f53、8dc0220e、b858b76d、80c32b19、b0353303、23a7f665、6d74046d、bd4bc23b、c1959436 全部 ref=0，只有 c3452d48(7)、dce8abd4(6)、adaeb531(5)、07eb229b(2) 有账 ⇒ B7 整批与 P1/P2/P4/P5/P7/P8/P9 全在账本外。分三类：改生产代码且零测试文件的是 bd4bc23b（3/0）、dbaa2e18（4/0）、80c32b19（1/0）；有测试但锁未登记 ci/checks.json 的是 8dc0220e 的 p1star_angle_guard 与 b858b76d 的 ipv_triangle_budget；已登记的是 6d74046d 与 c1959436。⇒ **这是账本机制自身的失效面**：隔壁确实在修，却只挂 4 个批次，其余按「性能/科学提交」绕过。建议规则补一条：**每个改生产代码的提交必须在账本有行，regression_test 留空即自动降 PARTIAL**。

### V4 其余三条 P1 与 V6-N-12 的结论位置
V4-N-01（门口径收紧未同步 tests/backend，静态推导非豁免门 UT-BACKEND 必红，需一次运行期定性）、V4-N-07（heavy 判据 max_workers 大于等于 budget_ 因 runtime.cpp:101-102 恒真 ⇒ 全部 cpu_heavy 两两互斥＝被否决的无条件串行化实际生效；CORE-006 走聚合初始化故测试仍绿＝测试面不等于生产装配面；P10 若只加编译 flags 则本条仍成立，因根因在 runtime 不在 scheduler）、V4-N-10 与 V4-N-11（回收门回退分支非单调：peak==last 时 retained=0 判 PASS 而正常释放 40% 反而 FAIL ⇒ 单调增长的泄漏判不出，且该分支在 Windows/MSVC 恒生效；本批新锁③还把旧判定逐条不变写成期望＝反向钉死。新被测量在 validate_alloc_report 的 :583-584 被显式 void 丢弃，samples 可重算合同对新主被测量失效）、V6-N-12（4 个新 SNR_API 导出与两个新 struct 在 docs 全域 0 登记，合同仍写 9 个）。**全文与逐条命令输出见 问题扫描/_verify/V4.md 与 V6.md（384 行，0 处待填）**，本条为账本可见性索引，避免我在低额度下重复抄写导致失真。
