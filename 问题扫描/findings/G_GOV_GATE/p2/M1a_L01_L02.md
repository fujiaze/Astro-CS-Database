# M1a 定稿 · G_GOV_GATE · P2（来源切片 L02）

## M1a-G-003 同一校验条件在请求层/会话层/描述符层内联重复，未接单一机器源

- 类别: G_GOV_GATE · 优先级: P2 · 来源: L02-019
- 位置: lib/phase3_session/p3_session.cpp::（`\|dec\| > 85` 内联）；lib/phase3_session/p3_wcs.cpp::p3_wcs_validate_descriptor（同条件第二处）；lib/core/src/module_adapters.cpp::（节点参数第三处）；contracts/schemas/::（应为单一机器源而缺）
- 证据摘录（逐字）:
  > （p3_session.cpp:107）if (std::fabs(dec) > 85.0) { s->last_error = "abs(center.dec_deg) must be <= 85° (TAN pole excluded)"; return ACS_ERR_PARAM; }
  > （p3_wcs.cpp 校验段）同一 |dec|>85 判定 + proj!="TAN" 判定（独立第二处）
  > （registry/SCI）\|dec\|<=85 与 FOV<=20 的权威源为 SCI-P3 §4/§9a-12（合同文本，非机器源）
- 权威依据: 宪章 §12.3-3（单一实现）、§13.1；AGENTS.md（一处权威、他处引用）
- 问题说明: 三项参数约束（极点、投影族、尺寸）在三个层各自以硬编码字面量重复实现，边界值与错误语义无共享定义；无机器可读校验源（schema 缺失，见 M1a-G-002）⇒ 修订约束时须逐处同步，漏改即产生"外层放行、内层拒绝"或反之。当前实测三层判定一致（无数值错），故定 P2。
- 影响: 治理/维护面：约束漂移风险与错误语义不一致。
- 建议处置: 抽出单一校验模块或由 schema 驱动生成，三层引用同一函数/常量与同一错误码表。
- 置信度: 高 · related: M1a-G-002、M1a-C-004
