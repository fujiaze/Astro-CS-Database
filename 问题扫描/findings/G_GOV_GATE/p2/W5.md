# W5 片5-6 · P2 六条

### W5-N-10（P2）退出码面三缺：`commands.cpp:306 rc=124`（超时）与 `:309 rc=127`（spawn 失败）**不在冻结码集 {0,2..10,70} 也不在 jsonl schema 的 exit_code enum**；`contracts.h:41-46` 的 `enum class ExitCode` **重列全部字面量且其注释自称「唯一源、只做映射不重定义」**，与 `cli/exit_codes.h:2`「其他文件只 include，不得重定义数值表」**直接冲突**（生产无消费者）
- 机器门盲区：`test_cli_protocol.py::test_10_exit_codes_single_source` 扫描范围是 `for fn in os.listdir(CLI)`（**只 cli/**）⇒ include/ 的重复表与 124/127 都在判据外；`check_api_docs.py::check_exit_codes` 用自带 EXIT_NAMES 过滤且**只做 doc→头单向** ⇒ 头里新增/改名不报。related `M5b-C-05`、`M5b-E-03`、`V18-N-05`、`C-18`

### W5-N-11（P2）诊断可定位性：`LOGGING_DIAGNOSTICS_STANDARD.md:9` 要求的「source」一环**无任何机制承载**——`__FILE__`/`__LINE__` 在生产 706 文件的日志调用中 **0 命中**；`[[nodiscard]]` 0、`std::ignore` 0、非 ACR 生产 `assert()` 0
- **判否限定（不过判）**：该标准未强制「文件::符号」字面量 ⇒ 不判为标准违反，判为**机制缺位 + 状态返回值无编译期不可忽略约束**。「实测值+阈值」是否同现逐条不齐：正例 `resource_gate.h:393-395`（值与 `0.85*min(workers,cpus)` 同现）、反例 `aio_ahpx_reader.cpp:484`/`:502`（只报块 id，不给声明大小与实际解压值）。related `W5-N-07/08`、`E4`

### W5-N-12（P2）门面 Python 吞错的危险子集在**门自身**：`tools/quality/check_complexity.py:65` 的 `except OSError: continue` 使**读不到的文件既不计入 `out[files]` 也不进任何跳过桶 ⇒ 被检分母静默缩水而门仍 PASS**；裸 `except:` 3 处（`check_doc_symbols.py:51`、`check_full_integration.py:34`、`generate_contract_report.py:45`）**连 SystemExit/KeyboardInterrupt 一起吞**
- 统计：except 298、体只有 pass/continue 64。非危险面（列出不报）：`check_source_inventory.py:38`、`check_comment_hygiene.py:36`、`docs_machine_consistency.py:32` 吞错后仍有两档确定回退。**定级理由**：§14.4 针对生产路径，本条落点是**门禁可信度**。related `V19-N-06`（实扫数须披露）、`M8-F-008`/`F-010`、`V20` 轴、簇「采集面静默缩水」

### W5-N-13（P2）解压重试**无字节封顶**：`aio_ahpx_reader.cpp:476`(ZSTD)/`:494`(LZ4) 以 `estSize=blk->size*8` 起、失败则 `*=4`、**最多 8 轮** ⇒ 第 k 轮目标缓冲 = 声明大小 × 8 × 4^k，**k=7 时峰值 131072 倍**；除尝试次数外无字节上限，全败后 `result.clear()` + fprintf 返回空容器、调用面以「空块」继续
- 宪章对照：**§14.4 禁重复重试、§17.6 把无界内存增长列为发布门禁禁止项**。是否真分配到该量级取决于 resize 抛 bad_alloc 的时机 ⇒ **需运行期未判**；**上限倍数与无字节封顶静态可判**。related `V10-N-05`（自报 max_block_size 决定 scratch 的同族）、`V18-N-02`、§17.6

### W5-N-14（P2）`drizzle_engine.cpp:1149`（直写）与 `:2275`（分块）同构 `try { hmeta.gain = stod(gain_str); } catch (...) {}` ⇒ **产品头写出「看起来是产品事实、实为结构体初值」的增益**，无日志、无计数、不影响 rc
- 与 `V10-N-04`（`aio_fits.cpp:161/162` 的 BSCALE/BZERO）同机制不同站点，**本条只报未登记的新站**；GAIN 是否进入下游科学链路需运行期未判。related `V10-N-04`、`C-20`、`V7-N-05`

### W5-N-15（P2）`aio_fits.cpp:389` 的通用数值关键字助手 `try{return stod(v);}catch{return def;}` **把「解析失败」与「键不存在/空值」折叠为同一默认值**；`:491 catch(...){ cal.ccd_temp = 0.0; }` 更把 CCD-TEMP 解析失败静默变 **0.0，而 0.0 是物理合法温度** ⇒ 事后无法辨别「没有温度」与「温度解析失败」
- 同文件 `:161`/`:162` 属已报 `V10-N-04`，本条只报 `:389`/`:491` 两站点。**轴内负结果**：早期筛查假设的「BITPIX 静默按 8bit」**经复核不成立**（`bytes_per_pixel==0` 前有 `return -1` 校验）⇒ 已列 §十 负结果。related `V10-N-04`、`V7-N-05`（缺键给默认）、`C-20`（哨兵不得与合法值同值）

## W5 轴级结论摘要（三条跨条事实）
- **错误码空间共 10 个命名空间表且互不对齐**；碰撞点：**值 8/9/10 在 acs 与 aio 两族含义完全不同**；loader/registry 复用同一 `acs_error_info_v1.detail_code`（`status_codes.h:195` 称模块自定义细分码）**无命名空间标记无登记表**；`ci/prepare_linux_fixtures.py:125` **已出现数值误引**（写 `ACS_ERR_BUDGET(5)`，实际 BUDGET=8、5=UNSUPPORTED）。⇒ **跨语言名值表：没有**。
- **§14.4 fail-fast 边界四处合规**（CLI/配置输入、C ABI 屏障、远程进程、原子替换）；违例集中在「**边界之后重复解析 + 静默改默认值**」（N-05）与「**外部文件解析吞错**」（N-04/06/13/14/15）。真实形态不是降级为警告，而是**连警告都不发**（N-01/02/03）。
- 重试与幂等：非 ACR 仅 3 处重试语义（N-13 + EINTR）；**ACR 164 文件是重试集中区但按 §10.1 运行时不可达 ⇒ 全轴不判，标需运行期+需接入裁决**。幂等正证据齐（`p1cos check_i3_idempotent`、`aio_publish:355-374` 可重放、calibration integration.json:150/158 由 RT-006 巡检）。
