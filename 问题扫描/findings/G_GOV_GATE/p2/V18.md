# V18 收尾 · 门可覆盖性汇总 + **机制⑭ 命名建议**

## 一、机器门可覆盖性（回答"能否一条门覆盖全族"：**不能一条，能一条主门 + 一条门面门**）
| 建议门 | 覆盖 | 可行性 | 挂点 |
|---|---|---|---|
| **`E14` TEST-ASSERT-BYPASS**（＝V18 的 G-A）| `V18-N-01/02/08/11/12`（①族全部 5 条）| 全静态可判（需白名单）| `tools/quality/check_ctest_registration.py` 的 C1–C6 旁新增，`profiles=fast`、`waivable=false` |
| **`E11` GATE-REALMODE-REQUIRED**（＝V18 的 G-B，我上轮已立号）| `V18-N-04/05` | **全静态**（纯 `checks.json` + 命令解析）| 同上；与 `AGENTS-GOV` 同族 |
| `E15` FAULT-REGISTRY-DRIVEN（＝V18 的 G-C）| `N-06/07/09/10` | **半静态**：`N-09`（名经变量传递）改造前**必误报** | 须先做 `N-09` 改造 ⇒ **短期不可实现** |
| **`G-D` 前提件**：`checks.json` 加 `env`/`variants` 维度 | 使 `C-21①` 可表达 | **合同改造**（schema + `ci/run.py` 消费侧）| 不是门，是前置件 |
- **为什么不合并成一条**：`E14` 与 `E11` 的**判据对象不同**（源文件 vs `checks.json`），强行合并会让新门自己变成 `N-04` 那种"多形态一把抓"的门。⇒ **我认可，并已在工单分列。**

## 二、机制⑭（V18 建议命名，我采纳并写入簇表）**「元门（证明门会红的门）自身不具判别力」**
- 依据三条合一：`V18-N-04`（4 道非豁免门只跑自检形态）+ `V18-N-05`（自检样本由**被检生产实现**生成、函数体内负例数 0）+ `V18-N-11`（元门自身 `execve` 换光环境、`child_rc!=0` 即算"必败验证通过"、`argc>=1` 恒真可被空值清位绕过）。
- ⇒ **本轮新失守点不在测试层，而在"用来证明测试有效的那层"**。这是簇 1 的**第 6 种机制**（前五种见 SUMMARY 簇 1 ①②③④⑤ + `E4` 记账面）。
- **它同时解释了 `C-21` 判据②不成立的根因**：若元门无判别力，则"注入锁存在"这一事实**不能**支撑"该锁能变红"的结论 ⇒ 我此前凡以"已带双向锁"为由判 `VERIFIED` 的条目**都要重看一遍**（我已核：受影响的是 `DRIZZLE-DET-001` 族，已按 `V9-N-17` 压在 PARTIAL，其余 `VERIFIED` 均另有独立复算依据）。

## 三、V18 的四次公开自纠（**与 V11/V12 同档，是本轮取证质量的正样本**）
① 首轮 `os.walk` 使 `lib/astro_image_io/third_party/cfitsio/**`（**不在其 27 条排除前缀内**）涌入 370 条"无求值宏"候选 ⇒ 加 vendored 正则剔除并在 `N-03` 明示口径；② 粗分类曾把 `ACS_STATIC_ASSERT`/`P1STAR_CHECK`/`P2H_CHECK` 误判为"`cond` 不参评"（**只把最后一个参数当 `cond`**）⇒ 按宏体逐个复核后改判为"p1star 求值但覆盖结论 / p2hips 真求值"；③ 一次 `grep -v "^./build/"` 过滤失效、输出全是 `build/asan/**` ⇒ **那批中间输出未用作任何结论**，计数全改由 `git ls-files` 底座重算；④ `selfcheck` 负例数 0 的结论**先**用了字符串检索，**补** `tests/monitoring` 存在性复核后才定档为"本门不跑那些负例"（避免把"用词不同"判成"缺失"）。
- **需运行期未判五项**（一律未推断为结论）：注入命中时 `failures` 实际数值；子进程是否**真的**因缺 `PATH`/`TMPDIR`/`*_DLL_PATH` 而失败；四条门接上真产物会不会立刻红；`recovery` 是否确在 `p1psf` 注册表内；`ipv_abi_layout_lock --selfcheck` 覆盖面（本轴仅静态认定它是**全仓唯一合格负例自检形态**）。
