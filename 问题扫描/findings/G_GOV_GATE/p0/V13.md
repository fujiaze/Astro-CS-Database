# V13 定稿 · 自造 ID 与循环证据普查（发布门槛被机器铸造）

> 时点 M-V13-01=`521095b8` / M-V13-02=`8c977118`；refs 口径 = tracked 4840 文件全字边界计数，REG/GEN/SELFREP 除外（脚本 `问题扫描/_verify/scripts_v13_mint.py`，三级复核：字边界 → 放宽 → 逐宿主定点读）。

### V13-N-03 审计状态铸造面：713/713 行机械铸 `VERIFIED` 并铸 `findings_p0..3=0`、7 个 `*_ok=PASS`，而这份 inventory **被打进审核包（交付面）**
- **ID** V13-N-03 ｜ **类别** G_GOV_GATE ｜ **优先级** **P0**（V13 原判 P1，我升：它直接把 §17.10 的发布前置条件从"事实"变成"输出"）｜ **状态** OPEN
- **位置与实测**：`tools/quality/update_audit_status.py:94-121` 五个分支**只按 `typ ∈ 类别` 赋值** ⇒ 对 `reports/v19r2/file_audit_inventory.csv` 的 `review_status` 铸 **VERIFIED 713/713 = 100%**（`reviewer_round` 分布 carry 279 / tests 207 / docs 113 / tools 113 / pr-audit 1；`changed` 与 `carry` 两分支仅字符串不同）。同分支还铸 `findings_p0..3 = 0`、`dead/duplicate/legacy = no`、**7 个 `*_ok = PASS`**；`check_source_inventory.py` 播种的 `PENDING` **全部被翻转**；`batch_summary.csv` 数字列**硬编码 0** + 判决 `"PASS"`（`:135-138`）。
- **为什么是 P0 而不是"文档卫生"**：宪章 **§17.10 要求 P0/P1 清零才可发布**，本工具把 `findings_p0=0/p1=0/p2=0/p3=0` 与 7 个 `*_ok=PASS` **无条件写进产物** ⇒ 发布前置条件成为**该脚本的输出**而非被审计事实；且该 inventory 经 `build_v19r3_package.py:191` 与 `build_v19r4_package.py:282/:311` **打进审核包** ⇒ 审核者拿到的是"100% VERIFIED + 零缺陷"的**铸造成果**。这正是簇 1 第五机制（0 输出记 PASS）的**规模化、批量、且面向审核包**的版本，也是簇 2「符合性由生成逻辑决定」的最重实例。
- **门可见性**：`update_audit_status.py`、`check_source_inventory.py`、`v19r3_traceability.py` 三工具在 `ci/checks.json` 的 **121 项 command 中零登记** ⇒ **整个"审计状态铸造面"不在 CI**（与 V6 那次越界跑 `v19r3_traceability.py` 同源——无 `--dry-run`、无采集）。
- **依据条款**：宪章 §17.10（P0/P1 清零方可发布）、§12.3（机器检查须可复核）、§14.4（fail-closed）、§17.2；AGENTS.md 状态机要素（严禁用 waiver/登记掩盖红灯）。
- **建议处置（按顺序）**：①**审核包停止引用该铸造产物**（改由判定器直接生成，或至少在包内标注"铸造视图，非审计结论"）；②`findings_pN` 与 `*_ok` 一律由**实际扫描器输出**填充，禁止常量赋值；③`review_status` 的 VERIFIED 必须有**逐行可复算依据**（证据路径存在 + 用例名可注册定位 + 判定器 rc），否则保持 `PENDING`；④三个铸造工具登记进 `ci/checks.json` 非豁免项，并加 `--dry-run`（V1 轴同类问题的通用要求）。
- **取证口径** V13 实测计数（713/713、五个分支逐字、播种值被翻转的前后对照、两处打包行号）｜ **置信度** 高 ｜ **related** **簇1 第五机制**、**簇2（`M3-G-002`）**、`M8-G-001`、**V13-N-01/02**、`V6-N-04`（同类"铸造面不在 CI"）、`A-38`（0 用例算不算绿）、`C-16`

### V13-N-01 MATRIX 权威侧自铸 3 枚 `EVID`，其 VERIFIED 在结构上不可被证伪 ⇒ 双视图共 5 行假 evidence
- **ID** V13-N-01 ｜ **优先级** **P1** ｜ **状态** OPEN
- **编号与宿主**：`EVID-P1-CAL-001`/`EVID-P1-NOISE-001`/`EVID-P1-PHOT-001`，宿主 `docs/traceability/TRACEABILITY_MATRIX.json`（phase1-calibration / noise-snr / photometry 行，`evidence_status=VERIFIED`），由 `c3452d48` 铸入（JSON 净新增 4 ID 中的 3 枚）；**refs 各=1，唯一宿主就是自己那行**。
- **"VERIFIED"的实际依据：无**——判定器 `check_traceability_matrix.py` 的 `EVID` 层在 `LAYERS` 表 `:66` 处 **`pathk=None`（无路径列）**，`C5` 只查唯一性、`C8` 虽要求 ID 在 `evidence`/`reports`/`returns` 目录文本命中，但 `ci/checks.json` 的 command **无 `--strict` ⇒ WARN-only** ⇒ **结构上不可红**。另：JSON 全部 6 行 VERIFIED evidence（含 `EVID-BLD-003`/`IO-001`/`CPU-001`）在自身 EVID authority 目录 `git grep EVID- -- evidence reports` **0 命中**，而第三目录 `returns/` **仓库不存在**。CSV 侧 2 枚（PSF/STAR）与已立条 `V6-N-06` 一致，不重报。
- **建议处置**：给 `EVID` 层补路径列并令 `C8` 进 `--strict`；禁止"仅在自身行内出现的 ID"取得 VERIFIED（一条断言即可：refs 计数 `<= 1` 即判 MISSING）。
- **related** **V6-N-06**、`M6b-G-001`、`L28e-E-001`、`A-37`（`DISP-*` 是否一等键）、`C-16`

### V13-N-02 同批把 MATRIX 三行 `test_path` 写成**散文串**（非路径）⇒ 修完 BOM 后仍 DANGLING_REF，且回归依据用错了检查器
- **ID** V13-N-02 ｜ **优先级** **P1** ｜ **状态** OPEN
- **实测**：`c3452d48` 把 3 行写成 `"lib/calibration/tests/p1cal (ctest p1cal_units/...); tests/backend/test_calibration_oracle.py; tests/unit/CMakeLists.txt:441 master_flat_median + ..."`（noise-snr/photometry 同形）。判定器 `C7 _check_anchor` 只认 `path` 或 `path::symbol`，V13 用 python 复算 `isfile=False` ⇒ **`DANGLING_REF` ERROR×3**——**与已立条 `V6-N-01/02` 的 BOM 红叠加**，即修好 BOM 该门仍红。
- **更要紧的一层（循环依据）**：账本 `regression_test` 列引用的 `tools/quality/check_traceability.py（rows=63 ok=63）` 读的是 `docs/TRACEABILITY.csv`——**另一文件、另一列，根本不读 MATRIX 的 `test_path`** ⇒ **用"不查该列的检查器"充当该列的回归依据**，是簇 1 第三机制（门验错对象）在"回归锁登记"上的新实例。
- **建议处置**：`test_path` 一律 `path::symbol` 或纯路径（ctest 目标名放独立列）；`regression_test` 列只允许填**确实会读该字段的**检查项/用例名（机器判据：该 command 的脚本源码里出现被改列名）。
- **related** **V6-N-01/V6-N-02**（同一门的另一半）、`M3b-F-02`（已回填 VERIFIED）、`D-06`（归因须过三级复核）、`C-16`
