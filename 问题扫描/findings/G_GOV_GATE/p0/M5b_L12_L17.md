# M5b 定稿 · G_GOV_GATE · P0（CLI 单入口 / 构建安装面 / 治理与发布门禁）

> 合并代理 M5b（第二层）。输入：`_cache/L12.md`（24 条）+ `_cache/L17.md`（22 条）。
> 全部条目均按**定稿时点的工作树**重读原文复验（禁 shell/构建/脚本/git；仅 read/grep/glob）。
> 位置一律写「path::符号」；行号只作辅助，见 `_merge/M5b.md` §5「行号漂移处置」。

---

### M5b-G-01 CLI 命令树与协议门禁全部打在 COMPATIBILITY 二进制上；二进制缺失时门静默零检查
- 类别: G_GOV_GATE
- 建议优先级: P0
- 位置:
  - `ci/steps/linux_build_root_graph.sh::（cli 子图构建步）`（现 :21-25）——`cmake -S cli -B build/cli` + `cmake --build build/cli --target astrocs`
  - `tools/check_api_docs.py::Checker.check_command_tree`（现 :132-142）
  - `tests/cli/test_cli_protocol.py::EXE`（现 :7-8）与 `::built`（现 :46-50）
  - `cli/CMakeLists.txt::（文件头 COMPATIBILITY 声明 + add_executable(astrocs)）`（现 :1-8、:121）；`cli/CMakeLists.txt::（AIO 源集）`（现 :164-165）
  - `CMakeLists.txt::astrocs`（现 :328、:588-609）
  - `ci/checks.json::API-DOCS`（现 :232-247）
- 证据摘录:
  > exe = os.path.join(self.repo, "build", "cli", "astrocs")
  > if os.path.isfile(exe):
  >     missing = sorted(c for c in doc_cmds if c not in help_cmds)
  > （该 if 之后无 else；文件不存在时不产生任何 failure）
  > mkdir -p build/cli && cmake -S cli -B build/cli -DCMAKE_BUILD_TYPE=Release
  > timeout 2400 cmake --build build/cli -j 2 --target astrocs
  > ${AIO_DIR}/src/aio_pipeline.cpp ${AIO_DIR}/src/aio_pipeline_engine.cpp)
  > # cli/CMakeLists.txt — COMPATIBILITY 声明 (BLD-002, 非产品事实源)
- 权威依据: 宪章 §3.1（每平台一个用户入口）、§8.1（CLI 薄入口）、§12.3-1（manifest/注册表/构建 target/产品清单一致）、§14.4（最小充分校验）；`docs/contracts/ARCH-001.md::§7 LEG-003`（现 :80）「aio_pipeline_engine 内置 5-stage 调度 + omp16 | I/O 越权编排 (P1-003) | LEG-003」
- 问题说明: 三层叠加。①**门验的不是交付物**：产品 `astrocs` 由根 CMake 产出（唯一 add_executable，:588），而 CI 又另跑 `cmake -S cli` 产出**同名** `astrocs`；`API-DOCS` 命令树比对（`check_api_docs.py::check_command_tree`）与 CLI golden 族（`test_cli_protocol.py::EXE`、`test_cli001_vpi.py::BUILD`、`test_cli003_semantics.py`、`test_cli_v7_surface.py`、`test_phase123_pipeline.py` 等）全部指向 `build/cli/astrocs`，即 `cli/CMakeLists.txt` 第 1 行自述的「COMPATIBILITY 声明（BLD-002，**非产品事实源**）」二进制；该步骤内根图产物只被执行 `version --json`。②**缺失即零检查**：`check_command_tree` 只有 `if os.path.isfile(exe):`，**无 else** ⇒ 文件不存在时既不 fail 也不告警；`API-DOCS` 挂在 fast/linux-main/windows-main 三个 profile 且 `waivable=false`，而路径写死为无扩展名的 `build/cli/astrocs`，Windows profile 下该路径永不成立 ⇒ 该 profile 中这道门恒为零检查。③**两个同名二进制的源码集不同**：兼容树把 ARCH-001 §7 判为违例的 `aio_pipeline_engine.cpp` 编入并用 `ACS_WARN_SUPPRESS="-w"` 静默（`cli/CMakeLists.txt:18、:165`），根构建面只编 `aio_pipeline.cpp`（`CMakeLists.txt:328`）⇒「命令树/help/退出码均通过」不能被引用为产品二进制的符合性证据。另 `tests/cli/test_cli_protocol.py::built` 在二进制缺失时**自行** `cmake -S cli` 现场构建，故本地跑测永不暴露①的错位。（对 L12-001 的两处修正：`tests/cli` 并非「全部」用兼容树——`test_p1003_drizzle_path.py:13` 用 `build/astrocs`（根图 cp 副本）；`check_api_docs` 的退出码面是双候选路径回退。）
- 影响: §12.3-1/-5 与 §14.4 的全部 CLI 级证据（命令树、JSONL、退出码映射、cancel/crash、run-manifest verify）建立在非交付对象上；交付 `astrocs` 的命令面可自由漂移而三门恒绿；真实数据终验（§13.3）报告的 CLI 行为同属兼容树行为。
- 建议处置: 门禁目标改为安装/发布树中的产品二进制（先 install 再对 `<prefix>/astrocs` 跑 golden），或让 CI 对两棵产物做命令面双向 diff 并声明唯一权威；`check_command_tree` 补 else 硬失败并按平台解析 exe 扩展名；从 `cli/CMakeLists.txt` 源集剔除 `aio_pipeline_engine.cpp` 或删该兼容工程。
- 置信度: 高
- related: M5b-G-16（L12-011）、M5b-C-04（L12-012）、F00-04、M5a「验证对象 ≠ 交付对象」根因族
- **【E2-实测 2026-09】** 三推演全证实并加重：交付面唯一 install 集合 = `cmake/install_layout.cmake:58 install(TARGETS astrocs RUNTIME DESTINATION .)`，实测 `build/astrocs` BuildID 96339b41…/MD5 7515489e…/6695984 B，`build/cli/astrocs` BuildID 83142ab2…/MD5 9e3c1a70…/7178472 B（`build/cli/CMakeCache.txt`: CMAKE_HOME_DIRECTORY=…/cli、project=astrocs_cli、Unix Makefiles）⇒ 确为 `cmake -S cli` 兼容图产物。③源码集差异实测：`aio_pipeline_engine` 定义符号 根图 exe=**0**、兼容图 exe=**16**、`libastrocs_aio.a`=**0**。②fail-open 实测 `tools/check_api_docs.py:132-142` 的 `if os.path.isfile(exe):` 后确无 else。①的 tests/cli 覆盖面精确化为 12 模块指兼容树 / 仅 `test_p1003_drizzle_path.py:14`+`test_cli_single_install.py:24` 指根图。新事实（加重）：两图版本串格式互斥（`build/cli/version_generated.h` = `0.11.0-alpha.2+g48ceee59fe52.dirty`，根图 = `…+g<40位SHA>`），本地用 python3 复刻 `tests/cli/test_cli_build.py:55-56` 正则重算 ⇒ 兼容图串 PASS、**根图串 FAIL**，即该门一旦改验交付物即红；详见 `问题扫描/_cache/E2.md` §1.7。
- **【E4-实测 2026-09】** 同类族计数（只解析注册表与检查器文本，未跑任何检查器）：与本条同形「拼无后缀产物名 + 缺失即静默」者在 windows-main 的 57 个脚本项里共 **3 项 / 4 处站点** = `API-DOCS`（本条锚点）、`DUPLICATION`（`tools/check_duplication.py:24` 三候选 `build/root-cmake/astrocs`、`build/astrocs`、`build/cli/astrocs`，`:29 if bin_path is not None:` 后无 else）、`ACR-DORMANT`（`tools/check_legacy_exit.py:19,67` 无后缀 `astrocs` + `:60 astrocs-stage2` + `:20,:68 if bin_path.exists():` 后无 else）。**锚点订正**：派发包所写 `tools/quality/check_api_docs.py` 不存在（`sed -n 100,160p` 空输出），真身为 `tools/check_api_docs.py:132-133`，行号正确，计数按实测路径。两项不计入本族（属缺席而非静默）：`PROD-REACH-SELFTEST`、`ISA-LEAK-SELFTEST` 在 windows-main 只注册 `--selftest`，真实产物模式未注册到该 profile；而 `tools/quality/check_prod_reachability.py:74-80` 本体是 fail-closed（探测不到即 return 2 并打印 REACH_FAIL）。POSIX 工具依赖另计 **6/57** 项（nm: ACR-DORMANT、DUPLICATION、PROD-REACH-SELFTEST；objdump: ISA-LEAK-SELFTEST；bash: WARNING-SUPPRESSION（`tools/check_warning_suppression.py:79`）、TESTKIT-LIST）。
- **【E4-实测 2026-09】** 真机留痕与本条互证 + 全 profile 算术：现树注册表 total=**114**（`_merge/M9.md` 记 113 ⇒ **改判**）、windows-main=**68**（platform any 64 + windows 4；waivable 3；57 脚本 + 11 unittest discover）、linux-main=104、fast=63、linux-deep=7；`linux-main − windows-main = 40 项`（含 `UT-CLI`、`UT-IO`、`UT-ABI`、`UT-BACKEND`、`UT-ARCH`、`BUILD-GCC-RELEASE`、`CTEST-LINUX-FULL` + 24 个 `CTEST-*`）⇒ 本条涉及的 CLI 协议族与 IO 合同族对 Windows 交付物**根本没有声明**，不只是恒零。hosted Windows 真实运行留痕 `run/ci_domain_merge/gh_artifact_327b6c30/windows/artifacts/ci/327b6c309b37/20260912T072034Z-d0e06d0a/checks/API-DOCS.json`（`platform_runtime: windows`）记 verdict=PASS、exit=0、duration=7.906、stdout_tail 与 stderr_tail **双空**，且该 id 正在 `ci/run.py:103-105 EMPTY_OUTPUT_SILENCE_EXEMPT` 白名单内（其注释理由即 rc=0 静默成功）⇒ 静默被制度化为合法。同窗口 `WARNING-SUPPRESSION` 自宣 QA-001_SKIP 无可用构建树 后 return 0（其 `detect_build_tree()` :59-65 只探 `build/root-cmake` 与 `build/`，而 Windows binaryDir 实为 `build/win-msvc-17.14.39-x64`）；`DUPLICATION`/`ACR-DORMANT` 均以 nm 面从未执行的形态报出 PASS 文案。更重的一档：windows-main 唯一读 Windows 测试产物的 `WIN-TEST-UNIT` 在 `run/**` 里 28 次真机运行的最近 10 次连续判 PASS，而其 stage 日志为 `ctest --preset win-rel` 输出 No tests were found!!! 且 exit 0、junit 文件 184 字节 tests 属性为 0、`<testcase>` 计数 0 ⇒ `ci_windows_driver.py::stage_plan`(:344-356) 无「用例数>0」断言；同仓相反说法见 `tools/quality/deep_ci_driver.py:248`（称未构建时 ctest 非零），与真机 exit 0 冲突。三档有效断言下界 = **63 项 / 716 方法 / 产物级 0**（口径见 `问题扫描/_cache/E4.md` §3.5）。

---

### M5b-G-02 追溯门 fail-open：`tools/quality/check_traceability.py` 无条件 return 0；TRACEABILITY-CODE 默认读预发布快照
- 类别: G_GOV_GATE
- 建议优先级: P0
- 位置:
  - `tools/quality/check_traceability.py::main`（现 :142-159，`return 0` 在 :159）
  - `tools/check_traceability.py::DEFAULT_TABLES`（现 :16）与 `::main`（现 :110）
  - `ci/checks.json::TRACEABILITY`（现 :36-62）、`::TRACEABILITY-CODE`（现 :63-87）、`::TRACEABILITY-MATRIX`（现 :1103-1126）
  - `docs/traceability/TRACEABILITY_SPEC.md::检查器`（现 :23-24）；`docs/DOCUMENT_INDEX.yaml::docs/TRACEABILITY.csv`（现 :68-69）
- 证据摘录:
  >     print(f"traceability: rows={len(trace)} ok={rows_ok} broken={len(broken)} "
  >           f"symbols={sym_ok}/{len(symbols)}")
  >     return 0
  > DEFAULT_TABLES = [os.path.join(REPO, "artifacts", "prerelease_v5", "tables", "TRACEABILITY.csv")]
  >     tables = sys.argv[1:] or DEFAULT_TABLES
- 权威依据: 宪章 §17-2「文档—模块—源码符号—测试追踪无断链」、§12.3-3/-4/-9、§1.1；`docs/traceability/TRACEABILITY_SPEC.md:23-24`（SPEC 指定正式检查器并规定「任何断链…以非 0 退出」）
- 问题说明: ①`tools/quality/check_traceability.py::main` 算出 `rows_broken`/`broken_rows` 写入 `evidence/quality/traceability_check.json` 后**无条件 `return 0`**（全文件 `return 1` 命中数 = 0），`__main__` 用 `raise SystemExit(main())` ⇒ CI 项 `TRACEABILITY`（`waivable=false`，changed_paths 覆盖 `docs/** lib/** cli/** tests/**`）**不可能变红**；它读的正是旧表 `docs/TRACEABILITY.csv`。②`TRACEABILITY-CODE` 无参运行 ⇒ 校验对象是 `artifacts/prerelease_v5/tables/TRACEABILITY.csv` 快照副本，而其触发条件写的是 `docs/**`。③SPEC 指定的正式检查器 `tools/traceability/check_traceability_matrix.py` **存在**且**真 fail-closed**（`::main` 现 :281 与 :415 `return 1 if errors else 0`；未捕获异常 → `SystemExit(3)`，现 :458-463），并已注册为 `TRACEABILITY-MATRIX`。⇒ 根因**不是"接错检查器"**，而是「三套矩阵并存无权威 + 名为 `TRACEABILITY` 的门验非权威旧表且恒绿」：`TRACEABILITY-MATRIX` 的 changed_paths 只有 `schemas/traceability_matrix.schema.json docs/** evidence/**`，**不含 `lib/** cli/** tests/**`** ⇒ 代码侧断链（实现/测试文件被删改名）在三道追溯门里没有任何一道会红。
- 影响: 宪章 §17-2 在 CI 上对代码侧不可执行；断链可长期存活并被 `ok=` 计数报告当作"已检查"引用。
- 建议处置: `check_traceability.py::main` 以 `rows_broken or sym_broken` 决定退出码；三道门收敛为单一权威（矩阵）并把 `lib/** cli/** tests/**` 纳入其 changed_paths；TRACEABILITY-CODE 的 DEFAULT_TABLES 改指真源或删门；旧表在 DOCUMENT_INDEX 降级为 ARCHIVED。
- 置信度: 高
- related: L17-018（并入本条）、L17-020、F00-01（M6 主落旧表双头事实）、M5b-G-03

---

### M5b-G-03 AST-API 门为同一头文件自反比对（恒真），参数数提取后弃用，且不读任何 API 文档
- 类别: G_GOV_GATE
- 建议优先级: P0
- 位置: `tools/check_ast_api.py::main`（现 :40-68）；`ci/checks.json::AST-API`（现 :577-598）；`tools/quality/gen_module_readmes.py`（现 :3）；`docs/contracts/TEST_MATRIX.md`（现 :62）
- 证据摘录:
  > HEADERS = [
  >     "include/astrocs/core/contracts.h",
  >     "include/astrocs/core/artifact.h",
  >     "include/astrocs/io/io_adapter.h",
  >         decls = re.findall(r"\b([a-zA-Z_]\w*)\s*\(([^)]*)\)\s*(?:const)?\s*;", txt)
  >             if name not in ast_names:
  > print(f"DOC-004_PASS: {len(HEADERS)} 头 AST public 符号与声明一致 (参数/cv/noexcept 基线)")
- 权威依据: 宪章 §12.3-5「AST 提取的函数名、参数、类型、可见性与 **API 文档**一致」；`docs/standards/API_STANDARD.md`
- 问题说明: 判据是「同一头文件的正则声明名集合 ⊆ 同一头文件 AST 的 name 集合」——对任何可被 clang 解析的自包含 C/C++ 头**恒真**；`nparams` 在 `declared[name]=n` 处算出后仅作循环变量出现，从未参与比较（docstring 承诺的「参数个数一致」「visibility/ownership 对比」全未实现）；脚本从不读 `docs/contracts/PUBLIC_API.md` 或 `API_CONTRACTS.csv` ⇒「AST ↔ API 文档」这一环在仓库内无实现（`check_api_docs.py::_count_params_in` 走正则数逗号，非 AST）。门可失败的唯一路径是环境级（header 缺失、clang 失败、正则零命中），不属合同判定。覆盖面另只有 3 个 C++ 头，`include/astrocs/abi/*.h` 全不在内；profile 仅 `linux-main`、`waivable=false`。ID 冲突：`DOC-004` 同时是 `tools/check_ast_api.py` 与 `tools/quality/gen_module_readmes.py` 的文档 ID，且 `TEST_MATRIX.md:62` 把「TRACEABILITY.csv 断链校验」也归给 DOC-004 ⇒ 三个对象共用一个 ID。
- 影响: §12.3-5 形同虚设；签名/参数/可见性与 API 文档不一致无法被发现；`AST-API`/`DOC-004_PASS` 文案被活动文档当作机器证据引用。
- 建议处置: 改为「AST 集合 ↔ API 文档声明集合」双向差集并比较参数个数/类型/可见性；扫描面并入 abi 头族；ID 与 `gen_module_readmes`、`TEST_MATRIX` 解冲突。
- 置信度: 高
- related: M5b-C-01（L12-003）、M5b-G-05、M5b-G-02

---

### M5b-G-04 版本单源簇：交付清单/安装树合同/依赖锁/schema 并存 4 份手工版本副本，两道版本门的扫描面都不覆盖它们
- 类别: G_GOV_GATE
- 建议优先级: P0
- 位置（根因合并，保留全部证据）:
  - `VERSION`（当前值 0.11.0-alpha.2）
  - `packaging/astrocs.product.json::product_version`（现 :3，0.11.0-alpha.1）
  - `packaging/install-tree.contract.json::target_version`（现 :5）与 `::root_layout`（现 :6，字面含 `AstroCS-0.11.0-alpha.1-win-x64/`）
  - `packaging/dependency-lock.json::target_version`（现 :5）
  - `packaging/schemas/install-tree-contract.schema.json::properties.target_version.const`（现 :12）
  - `packaging/schemas/astrocs-product.schema.json::properties.product_version.pattern`（现 :10）
  - `ci/checks.json::VERSION-CONSISTENCY.command`（现 :12-17，`--expected 0.11.0-alpha.2` 硬编码 = 第 5 份副本）
  - 门扫描面：`ci/check_version.py::main` 的 `scan_targets`（现 :182-213）与 `DOC_SET_FILES`/`DOC_SET_DIRS`（现 :62-66）
  - 门扫描面：`tools/doccheck/check_version_namespaces.py::SCAN_FILES/SCAN_DIRS/LOG_FILES`（现 :36-48）、`::NAMESPACE_EXEMPT`（现 :61-70）
  - 命名空间借用：`lib/calibration/include/astrocs/calibration/types.h::ASTROCS_CAL_VERSION`（现 :20）与 `::ASTROCS_CAL_BUILD_ID`（:22）、`lib/calibration/module.yaml::module_version`（:16）vs `lib/star_detector/module.yaml::module_version`（:45，alpha.1）、`include/astrocs/abi/module_api_v1.h::acs_module_descriptor_v1.version`（:43 注释「语义版本 "1.x"」）
  - 结论面：`docs/owner/RELEASE_STATUS.md::版本单源`（现 :66）判 CONTRACT_READY
- 证据摘录:
  > "product_version": "0.11.0-alpha.1",
  > "target_version": { "const": "0.11.0-alpha.1" },
  >     "product_version": { "type": "string", "pattern": "^0\\.11\\.0-alpha\\.1" },
  > "command": ["python3", "ci/check_version.py", "--expected", "0.11.0-alpha.2"]
  > module_version: 0.11.0-alpha.1
  >     "module_version", "module.yaml",                                # module 命名空间
- 权威依据: 宪章 §16.1「根 `VERSION` 是产品版本唯一输入；CMake、CLI、产品 manifest 和活动顶层文档由机器生成或检查一致；module version、ABI version、schema version 不与产品版本混用」、§16.2、§12.3-1/-10、§17-11；GOV-003 `docs/governance/VERSION_NAMESPACES.md` §1/§3
- 问题说明: **根因 = 产品版本存在多份手工副本，且两道版本门的扫描面在设计上都不覆盖这些副本 ⇒「门无法发现本条漂移」成立**。`ci/check_version.py` 只扫 `cli/**` + 根 `CMakeLists.txt`（[4e]）与 `README/REVIEW/HANDOVER/DOCUMENT_INDEX/VERSIONING + docs/governance + docs/owner`（[5]），`packaging/**`、`lib/**`、`ci/checks.json` 均不在内；`check_version_namespaces.py` 的 SCAN_FILES/SCAN_DIRS 同样不含 `packaging` 与 `lib`，且 `module_version\|module.yaml` 被整行列入豁免集 ⇒ 跨命名空间借用与漂移双双不可见。四份 alpha.1 副本中 `install-tree-contract.schema.json:12` 用 JSON-Schema `const`、`astrocs-product.schema.json:10` 用 `pattern` 把旧值**钉成第二事实源**（两份 schema 在全仓内无任何消费者：仅 `packaging/README.md` 目录清单与合同自身的安装白名单条目提到文件名，`packaging/verify_install_tree.py` 不加载 schema）⇒ 一旦接 validator 即锁死旧版本。另 `ci/checks.json` 把当前值硬编码成第 5 份 `--expected` 手工副本（版本提升须同提交改 6+ 处，且无门校验该副本本身）。附带修正：`cmake/astrocs.product.windows.json.in::product_version` 用 `@ASTROCS_BASE_VERSION@` 生成 ⇒ 非漂移点（L12-006 该子主张不成立），漂移只发生在 Linux 交付清单与两份合同/锁文件。module 命名空间借用另使 `ASTROCS_CAL_BUILD_ID "P1-CAL-IMPL"`（任务号）与 `module_api_v1.h:44`「编译指纹」注释矛盾。
- 影响: 交付安装树的 `astrocs.product.json` 与安装树合同声明的产品版本 ≠ 根 VERSION（§16.1 直接违反，§17-11「版本及 provenance 全部通过」不可成立）；module 版本面失去接口指纹鉴别力；下次 alpha 提升会再产生不一致而门恒绿。
- 建议处置: 版本由 VERSION 在 configure 期生成到 packaging；schema 的 `const/pattern` 改为格式约束；两门的扫描面加 `packaging/**`、`lib/*/module.yaml`、`ci/checks.json`；`module_version` 移出 NAMESPACE_EXEMPT 并改独立递增。
- 置信度: 高
- related: L12-006、L12-007（并入本条）、M5b-I-04（L17-017）、M5b-C-06（L12-020）、M5b-G-05

---

### M5b-G-05 CON-BUILD-GRAPH 只做子串存在性检查：订正 BUILD_GRAPH.md 反而变红，产品构建面无任何机器校验
- 类别: G_GOV_GATE
- 建议优先级: P0
- 位置: `tools/quality/contracts/check_build_graph.py::main`（现 :18-46）；`docs/architecture/BUILD_GRAPH.md::§1 顶层 targets`（现 :5-17）；`CMakeLists.txt::add_executable(astrocs)`（现 :588）；`cmake/install_layout.cmake::（install 白名单）`（现 :133-136）；`ci/checks.json::CON-BUILD-GRAPH`（现 :746-770）
- 证据摘录:
  >     for tgt in ["phase2","astrocs-stage2","calibrated_pair_diag","rejection_cli"]:
  >         if tgt not in text:
  >             findings.append({"id":"BUILD-MISSING-TARGET", ...
  >             status="FAIL"
  >     cmake = repo / "lib/phase2/CMakeLists.txt"
  > | astrocs-stage2 | EXEC | tools/stage2.cpp | lib/phase2/CMakeLists.txt |
- 权威依据: 宪章 §12.3-1（构建 target 与产品清单一致）、§12.3-9（无悬空引用）、§14.2、§14.3-2（不得保留第二套架构表述）；`cmake/install_layout.cmake` 自述「唯一 install 规则源」
- 问题说明: 检查器判据 = 四个字符串是否**出现在 BUILD_GRAPH.md 文本中**（`tgt in text`）加 `lib/phase2/CMakeLists.txt` 里四个 `add_*` 前缀是否存在；从不读根 `CMakeLists.txt`、从不读 `cmake/install_layout.cmake`、从不读 File API/`compile_commands.json`（docstring 承诺「与 CMake file-api/compile DB 一致」未实现）。后果双向：①**文档订正会变红**——把已不存在的 `astrocs-stage2\|calibrated_pair_diag\|rejection_cli\|orchestrator.exe\|astro_image_io.dll\|hepix_drizzle` 从表里删掉即 `BUILD-MISSING-TARGET → status=FAIL → exit 1`（三 profile、`waivable=false`）；②**真实构建面无覆盖**——按产品目标名集合（`astrocs_runtime\|astrocs_io\|astrocs_noop\|astrocs_cpu_baseline\|astrocs_p1_*\|astrocs_catalog_gaia\|add_executable(astrocs`）grep `BUILD_GRAPH.md` 命中数 = 0，而这些恰是安装/发布对象；文档同时把 `acr_kernels.cpp / acr/backends/cuda/cuda_bridge_loader.cpp` 列为生产源集、把 `orchestrator.exe` 列为顶层 target（ARCH-001 §7 已登记为 LEG-001/002/003 退出项）⇒ 门把过期事实固化。
- 影响: 宪章 §12.3-1 的构建面一致性检查实际不存在；「唯一产品入口」的实际 target/source/link/install 关系无可校验权威文档，按 BUILD_GRAPH.md 施工会重新引入 LEG-001/002/003；订正文档的门禁成本为红。
- 建议处置: 检查器改读 CMake File API codemodel + `install_layout.cmake` 安装白名单，与产品 manifest units 三方集合对账；BUILD_GRAPH.md 按真源订正并与 `MODULE_MAP.md\|PHASE3_MODULE_ARCH.md` 收敛为单一表述。
- 置信度: 高
- related: M5b-E-002（L12-019）、L18 域（M6）、M5b-G-04

---

### M5b-G-06 §18.4「只加载随产品签名清单发布的官方模块」在机器上无实现：产品不加载、hash 全 null、唯一闭环测试不进 CI
- 类别: G_GOV_GATE
- 建议优先级: P0
- 位置:
  - `CMakeLists.txt::target_link_libraries(astrocs ...)`（现 :605-609，静态链全部科学库）
  - `CMakeLists.txt::astrocs_runtime / astrocs_io`（现 :133、:146，Linux SHARED 面各只 1 个 C 源）
  - `cli/commands.cpp::unit_file_present`（现 :1889-1892）、`::cmd_modules_verify`（现 :2017+）、`::cmd_selftest`（现 :2096+）
  - `runtime/registry/module_registry.c::（acs_secure_loader_load_v1 调用）`（现 :502）——根构建面对 `runtime/registry`、`runtime/module_loader` 引用命中数 = 0
  - `packaging/astrocs.product.json::units[*].sha256`（现 :8-17，10 个 unit 全部 null）
  - `packaging/verify_install_tree.py::main`（现 :56-114，无 hash 比较、无白名单外判据）
  - `tests/abi/mod001_install_load_check.py::（模块清单 docstring）`（现 :13-18）vs `::SCIENCE_MODULES`（现 :67-73）
  - `ci/checks.json::UT-ABI`（现 :1643-1671，`unittest discover -s tests/abi`）
  - 完成度声明：`docs/owner/RELEASE_STATUS.md`（现 :52、:73）、`docs/owner/ARCHITECTURE_OVERVIEW.md`（现 :34、:38、:106）、`docs/owner/PIPELINE_OVERVIEW.md`（现 :145）、`docs/owner/CHANGE_REVIEW.md`（现 :123）、`REVIEW.md`（现 :85、:110-111）、`docs/architecture/MODULE_MAP.md`（现 :14）
- 证据摘录:
  > 4. **插件开放范围**：明确禁止第三方模块目录；只加载随产品签名清单发布的官方模块（§3.3）。任何未经产品清单授权路径的模块加载一律拒绝。
  > S4 产品清单完整性: ... 6 个科学模块 DLL（astrocs.catalog.gaia / ... / astrocs.p1.noise）
  > 产品清单 10 units，安全 loader 实测 64/64 PASS
  > "python3", "-B", "-m", "unittest", "discover", "-s", "tests/abi", "-t", "tests/abi"
- 权威依据: 宪章 §18.4（负责人裁决四·插件开放范围）、§17-3（所有发布模块可独立加载、验证和卸载）、§16.2（不得用命令占位、空输出或文档声明冒充完成）、§12.3-4/-6/-12、§13.2
- 问题说明: ①交付 CLI 以静态库方式把全部科学模块链进单一 `astrocs`，`cli/**` 内对 `dlopen\|acs_secure_loader\|module_registry` 零命中 ⇒ 产品运行时不存在「按清单加载模块」这条路径；安全 loader 的真实调用者 `runtime/registry/module_registry.c` 与 `runtime/module_loader/secure_loader.c` 都不在根构建面（`astrocs_runtime` 只编 `runtime/artifact_store/artifact_abi_v1.c`，`astrocs_io` 只编 `runtime/io/fits_core.c`）⇒ §18.4 的白名单强制在产品侧不可达。（对 L12-014 的措辞修正：secure_loader 并非「仅测试引用」，`runtime/registry` 亦引用，但两者都不入产品图，结论不变。）②`modules list/verify` 与 `selftest` 实际只做 `is_regular_file` 存在性判断。③10 个 unit 的 `sha256` 全 null，`verify_install_tree.py::main` 完全不做 hash 比较，也不判「白名单外条目」（docstring 承诺的「clean install 仅白名单」未实现：非 required 缺失不报错、多余文件零告警）⇒ §18.4 所要求的"清单授权"没有内容绑定。④唯一逐 unit 加载闭环 `tests/abi/mod001_install_load_check.py` 文件名不以 `test` 开头，`unittest discover` 默认模式不收集，`ci/checks.json`/workflow 内亦无显式调用 ⇒「64/64 PASS」在 CI 内永不重跑；且该脚本文档与其常量互斥（docstring 说 6 个科学模块含 `astrocs.p1.noise`，`SCIENCE_MODULES` 只有 5 项）。⑤该 64/64 被 L0 四份文档 + REVIEW.md + MODULE_MAP 作为「已实测」证据引用，日志仅在 `run/docconv001/logs/`（gitignore 现场，仓库内不可核）。
- 影响: §17-3 与 §18.4 在交付形态上无机器实现亦无 CI 证据；清单/安装面一致性被 L0 当作已完成度引用，直接影响发布 Gate 判断。
- 建议处置: 二选一并写进 §12.3 门——(A) 交付面接真实 loader（把 `runtime/registry + module_loader` 编入 runtime 库并在 `modules verify/selftest` 调用），打包期填 `sha256` 并由 loader 强制比对；(B) 显式登记「当前为单二进制静态交付，§18.4 白名单延至 X 里程碑」并撤销 L0 的 64/64 完成度引用；mod001 改名 `test_*` 或单列 CI 项；`verify_install_tree.py` 增「白名单外即 FAIL」+ hash 比对。
- 置信度: 高
- related: M5b-C-07（L12-018）、M5b-C-02（L12-005）、M5b-G-15（L12-010）、F00-05、M5b-I-02
- **【E3-实测 2026-09】** 「64/64」获运行证据确证（历史面）：run/mod001/fix_zlib/install_load_check_final.log 与 final2.log（mtime 2026-09-11 23:59:50）尾行 `MOD001_INSTALL_LOAD_CHECK PASS: 64/64 checks`（units=10 版）；同日 19:11 `green_install_load_check_r1..r3.log` = **72/72**（units=11 版）、18:46 `red_install_load_check.log` = FAIL 15/46（RED 留档）——72→64 系 e6254d4d（09-12）摘除 MOD-P1-NOISE 清单行所致，59fdeab3/e6254d4d 提交文本互证。同批赠证：tests/abi 实跑 `run/mod001/abi_test_secure_loader.py.log` `results: 0 FAIL / 36 PASS` + `abi_test_module_registry.py.log` `28/28`（09-11 本机）。⇒ 本条「唯一闭环测试不进 CI / 交付面不加载 / hash 全 null」三主判词不受影响，仅「64/64 是否曾真跑」由无法判定改判为**曾真跑（对 09-11 树）**；当前 HEAD 未复核（复核需执行，E 层白名单外）。详见 _cache/E3.md §2。

---
