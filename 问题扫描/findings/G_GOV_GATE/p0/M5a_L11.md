# M5a 定稿 · findings · G_GOV_GATE（来源切片 L11；M5a-G-001 含 L17-004 并档，M5b 勿重复登记）
> 代理：M5a（第二层合并验证）。全部条目已于复核时点按当前树重读；位置一律 path::符号，行号为复核时点参考值。详见 ../../_merge/M5a.md。


### M5a-G-001 生产利用率门沿用 §18.2 已废止的 80%/75%/50%，宪章冻结的 85%/60% 无实现
- 类别: G_GOV_GATE
- 建议优先级: P0
- 来源: L11-002（与 L17-004 并档为一条根因；本条由 M5a 主落，M5b 不得重复登记）
- 位置（复核时点行号；锚以符号/语句为准）:
  - `cli/resource_gate.h::compute_cores_threshold`（现 :181-186，0.80 系数在 :185）
  - `cli/resource_gate.h::kMon001UtilSampleMinPercent / kMon001QueueUtilMinPercent`（:104-107，全仓仅定义行）
  - `cli/resource_gate.h::GateConfig::active_window_seconds` 上方注释（:153）
  - `cli/commands.cpp::run_with_resource_gate`（:909 `>= 0.75`、:912 `< 0.50` 字面量）
  - `tests/unit/p2_workers_test.cpp::main`（:85）
  - 反面对照：`tools/monitoring/run_monitored.py::FROZEN_GATE_*`（:451-456，0.85/0.60 正确值）
- 证据摘录:
  > return (m >= 1 ? 0.80 * static_cast<double>(m) : 0.0);
  >     // 注意: 阈值(85%/60%/32MB/s/10s)与判定式不动, 只加窗口/样本量前置。
  > inline constexpr double kMon001UtilSampleMinPercent = 75.0;   // 单样本 >=0.75
  > inline constexpr double kMon001QueueUtilMinPercent = 50.0;    // 窗口内利用率下限 0.50
  > FROZEN_GATE_MIN_AVG_UTILIZATION = 0.85    # 平均利用率 ≥ 已分配容量的 85%
  > FROZEN_GATE_WINDOW_MIN_UTILIZATION = 0.60  # 窗口内利用率下限 60%
  >         CHECK(t1 == 0.80 * 2.0);         // 0.80*min(4,2)
- 权威依据: 宪章 §10.5（「计算区间平均 CPU 利用率不低于已分配容量的 85%；任何连续 10 秒低于 60% 或只有一个活跃计算线程均失败」）；**§18.2 负责人裁决明文**：「本裁决替代旧 `AstroCS_ENGINEERING_CONSTRAINTS.md` D.6 的 80%/75%/50% 表述」；§17.6
- 问题说明: 生产 C++ 门（`evaluate_gate` + `evaluate_mon001`）的实际判据是「均值 ≥ 0.80·min(selected,available) 核」「逐样本 U≥0.75 占比≥70%」「连续 10 s U<0.50」——三个数字恰是 §18.2 点名废止的旧 D.6 值。宪章冻结的两个数（85% 平均、60% 连续窗）在 `cli/**` 内无任何实现点：`kCpuMeanMinPercent=85.0` 虽存在，但其比较对象是「单核=100%」标度的 cpu_pct（见 M5a-G-002），故该常量不构成 §10.5 的「已分配容量 85%」。另外 `kMon001UtilSampleMinPercent`(75.0) 与 `kMon001QueueUtilMinPercent`(50.0) 复算 grep 结果：**除定义行外零引用**，判定处写 0.75/0.50 字面量，与同文件 :91-92「判定集中本头文件, 不散落硬编码」自相矛盾。收口一处叶子/前台表述：:233-234 的「active window <10 s 或 wall<5 s → Ok」与 §10.5 自身前提「计算区间超过 10 秒」相容，本条不将其列为 fail-open。
- 影响: 发布门禁（§17.6 资源门，RESOURCE exit 10）按低于宪章的标准放行；「连续 10 秒低于 60% 即失败」在生产运行中永不被检出（对应窗口用的是 50%）；任何按 :153 注释或 L0 口径审阅阈值的人都会被误导为「85%/60% 不动」。
- 建议处置: 以 §10.5/§18.2 字面量重写生产判据（0.85 容量平均 + 0.60×10 s 窗口 + 单活跃线程），或按 §1.2 走宪章变更；以常量替换 0.75/0.50 字面量；订正 :153 注释；`tests/unit/p2_workers_test.cpp` 的 0.80 断言随判据一并改。
- 置信度: 高
- related: M5a-G-002（口径）、M5a-G-003（合规实现零应用点）、L17-004（并档）、F00-03

### M5a-G-002 CPU 利用率门的分母是 1 核：85%/90% 实为 0.85/0.90 核下限，四处注释自述相反口径
- 类别: G_GOV_GATE
- 建议优先级: P0
- 来源: L11-003
- 位置:
  - `cli/resource_recorder.h::ResourceRecorder::record`（:101 采集式）与文件头口径声明（:5、`ResRecord::cpu_pct` :38）
  - `cli/resource_gate.h::GateConfig::cpu_p50_percent`（:145-146 注释）、`::utilization_value`（:286-294 另一套分母）、`::evaluate_gate` CPU p50/mean 判定（:246-249）、`::kMon001UtilSampleMinPercent` 上方注释（:102-103）与 :281-285 段落
  - `cli/resource_recorder.h::ResourceRecorder::write_all`（:242-243 写进运行工件的自证字段）
  - `cli/monitor.h::ProcessMonitor::Summary`（:318 `avg_cpu_percent = avg_equivalent_cores * 100.0`）
  - `cli/commands.cpp::run_with_resource_gate`（:879-880 直接喂 act.cpu_pct_p50/mean；:907-908 口径注释）
  - `tests/unit/mon002_gate_test.cpp::main` 用例 1（:23-31）；`tests/backend/test_p2007_joint_gate.py` 实测注记（:33-34）
- 证据摘录:
  >         r.cpu_pct = interval_ > 0 ? (s.d_cpu_seconds / interval_) * 100.0 : 0.0;
  > // Windows/Linux 统一 "100%=全部分配核用满" normalized 口径。
  >     double cpu_p50_percent = -1.0;    // CPU p50%(normalized: 100%=全部分配核)
  >   return m >= 1 ? cpu_pct / (100.0 * static_cast<double>(m)) : 0.0;
  >         if (g.cpu_mean_percent >= 0.0 && g.cpu_mean_percent < kCpuMeanMinPercent)
  >       * MON-002 CPU 指标实测达标并恢复断言(test_03): 16c 全核语境 cpu_p50
  >         ≈114% ≥ 90、cpu_mean ≈107% ≥ 85(kCpuP50MinPercent/kCpuMeanMinPercent);
- 权威依据: 宪章 §10.5（平均利用率不低于**已分配容量**的 85%）+ §18.2 同句；ARCH-LOG-MONITOR-002 / `docs/architecture/observability/RESOURCE_MONITORING_CONTRACT.md` 字段口径
- 问题说明: 复算给出的唯一口径结论——采集端 `cpu_pct = 进程 ΔCPU秒/墙钟 × 100`，即 **100% = 1 核满载**（4 核满载 = 400；`monitor.h:318` 的「等价核数 × 100」自证同一事实）。因此 :246-249 的 90/85 比较是单核标度门槛：只要用到 ≥0.90/≥0.85 核即判通过，与分配核数无关；仓库自身实测注记（16 核语境 cpu_mean≈107%，即约 6.7% 已分配容量）直接证明该门在深度低利用率下仍报达标。同文件内 `utilization_value()` 又按 `cpu_pct/(100·min(selected,available))` 归一（MON-001 路径是容量标度，阈 0.75/0.50），两套 CPU 判据分母不同。四处文字声明（recorder 头 :5、`ResRecord::cpu_pct` :38、gate :102-103、gate :145 与 :281-285）均写成「100%=全部（分配）核用满」，与它们自己的公式互斥；`resource_summary.json` 更把 `"normalized_cpu_100pct_all_allocated_cores":true` 写进运行工件（:243），对下游构成错误口径断言。
- 影响: 宪章 §10.5/§18.2 的 85%/90% 利用率门在数学上无效（仍在起作用的容量判据只有 M5a-G-001 那条被废止的 0.80·min 核数门）；`resource_summary.json` 自证字段向审核方虚假声明归一口径；跨核数/跨平台不可比。
- 建议处置: 统一为「已分配容量」口径（采集端归一或门内换算，二选一），同步订正 `resource_recorder.h:5/:38`、`resource_gate.h:102-103/:145/:281-283`、`commands.cpp:907-908` 与 `normalized_cpu_100pct_all_allocated_cores` 字段语义；`mon002_gate_test.cpp` 用例 1 改按容量口径构造，并补「分配 4 核、实测 0.9 核必须 FAIL」负例。
- 置信度: 高
- related: M5a-G-001、M5a-G-004、M5a-G-003、F00-03

### M5a-G-003 唯一逐字实现 §10.5 的门禁零应用点，L0 口径却称「缺失即 fail-closed」
- 类别: G_GOV_GATE
- 建议优先级: P0
- 来源: L11-004
- 位置:
  - `tools/monitoring/run_monitored.py::evaluate_frozen_gate`（常量 :451-456；函数 :459-602；CLI 旗标 :681,714-726）
  - `ci/checks.json`（111 项注册检查全文）；`ci/run.py::monitor_gate_requested` / `monitor_gate_evidence_gap`（:70-77、:109-127）
  - `ci/tests/test_ci001_failclosed.py`（:128-152）；`ci/tests/test_windows_ci.py`（:81-82）
  - `docs/owner/RELEASE_STATUS.md::§5 发布 Gate 口径`（:108-110）；`docs/architecture/MODULE_MAP.md::监控与资源门`（:41）；`docs/owner/PIPELINE_OVERVIEW.md::§5`（:130-135）
  - `ci/resource_monitor.py`（:7,:12-16,:28,:34 桥接自述）
- 证据摘录:
  > # F-CI-002-04/06（owner 裁决原则一致化应用, 2026-09-11）：资源门冻结语义
  > # （§10.5/§18.2）针对重计算区间——构建/打包/单测为非重计算面，CI 注册表
  > # 当前零 --gate-required（WIN-* 另按裁决 requires_monitor=false）；本判定
  - 资源门口径（负责人既定原则，CI-001/CI-REPAIR-001 裁决）：构建/打包/单测为
  **非重计算面**，冻结阈值语义针对重计算区间；重计算面必须显式请求
  `--gate-required` 并附判定证据，缺失即 fail-closed（§10.5/§18.2 阈值不被放宽）。
  > | 监控与资源门 | `tools/monitoring` | IMPLEMENTED | 冻结阈值判定（§10.5/§18.2） |
  - `ci/tests/test_ci001_failclosed.py:128`：`"""裁决一致化：全部 requires_monitor 检查命令均不含 --gate-required。"""` + `:137` `self.assertNotIn("--gate-required", head, ...)`
- 权威依据: 宪章 §10.5/§18.2、§17.6、§12.3-12（L0 结论可追溯到当前 SHA 的机器证据）、§12.1
- 问题说明: 复算结论（含对叶子措辞的两处订正）。① `ci/checks.json` 全文 `--gate-required`/`--gate-workers` 零命中（0/111）；`requires_monitor: true` 的 6 项全是 DEEP-CLANG-BUILD / DEEP-SAN-ASAN / DEEP-SAN-TSAN / DEEP-COV-CPP / DEEP-COV-PY 等构建·sanitizer·覆盖率检查，无一为科学重计算面。② 订正叶子：`run_monitored.py` 并非「只在合成字典上被跑过」——9 处注册项经 `ci/resource_monitor.py`（自述 "bridge shim -> tools/monitoring/run_monitored.py"）真实调用它，但因不含判定旗标，按收窄后的 `ci/run.py` 逻辑只「采样留证」，`evaluate_frozen_gate()` 仍不在任何 CI 面上执行。③ `ci/tests` 把「任何检查都不得请求门禁判定」写成反向断言（两文件三处），缺口被固化为规格。④ 叶子未报的新事实：`ci/run.py:75-76` 把资源门应用面指向 **REAL-001**，而 `ci/checks.json` 内不存在该 ID 的注册项（该 ID 在真源中仅出现在 ci 注释文本），故「未来注册的重计算检查」当前不存在。⑤ RELEASE_STATUS:109-110 的「重计算面必须显式请求…缺失即 fail-closed」与 ci/run.py:74-77 的「未请求判定的监控检查不强制 frozen_gate」互斥：按 L0 口径「未请求」本身就应是缺失，实现却按不适用处理。
- 影响: §10.5 利用率门在整个自动化链条（CI/打包/真实数据终验注册表）没有执行点；唯一合规实现与产品在跑的错误口径门（M5a-G-001/002）互不相干；L0 的 fail-closed/IMPLEMENTED 结论无当前机器证据。
- 建议处置: 在真实重计算注册面（REAL-001 或等价项）至少一处请求 `--gate-required` 并保留判定证据；反向不变量测试改为「重计算面必须请求判定」；按接线现状订正 MODULE_MAP:41 与 RELEASE_STATUS:108-110，或按 §1.2 显式记录裁决边界。
- 置信度: 高
- related: M5a-G-001、M5a-G-002、M5a-F-001、L17-007（M5b：heavy 分类无机器判据）

### M5a-G-005 THREAD-BUDGET 机器门空转：扫描面、正则与行级豁免三重漏检并被单测固化
- 类别: G_GOV_GATE
- 建议优先级: P0
- 来源: L11-005；与前台 F00-03 并档为一条根因（M5a 主落，他域 related 指过去）
- 位置:
  - `tools/arch/check_thread_budget.py::SCAN_ROOTS`（:6）、`::EXEMPT`（:7-11）、`::PATTERNS`（:69-75）、`::scan` 行级豁免（:92-94）、`::main` 输出串（:118）
  - `ci/checks.json::THREAD-BUDGET`（:555-576，`changed_paths` = `lib/**`、`include/**`）
  - `tests/arch/test_thread_budget.py::test_01_real_repo_passes`（:11-16）
  - 不可见实例（真源且在根构建内）：`lib/core/src/executor.cpp`（:39、:217）、`lib/core/src/scheduler.cpp`（:310）、`lib/phase2/src/sampler.cpp`（:890）、`lib/phase2/src/upm.cpp`（:521,617,661,778,1484）、`lib/phase3_session/p3_session.cpp`（:327）、`providers/cpu/baseline/src/baseline_provider.cpp::run_banded`（:445）、`providers/cpu/avx2/src/avx2_provider.cpp`（:266）、`providers/cpu/avx512/src/avx512_provider.cpp`（:265）
  - 裸 OpenMP 实例：`lib/calibration/src/calibrator.cpp`（:89,119,128,162,171）、`lib/photometric_calib/cpp/src/pc_api.cpp`（现 19 处：:87,100,232,284,320,335,464,477,512,603,651,684,699,750,867,925,958,973,1067）
- 证据摘录:
  > SCAN_ROOTS = [os.path.join(REPO, "lib")]
  >     "std::thread": re.compile(r"std::thread\s*\(|std::thread\s+\w+"),
  >                     row_exempt = is_exempt or "watchdog" in line
  >         self.assertIn("未登记线程创建=0 硬编码线程数=0", r.stdout)
  - `docs/architecture/THREAD_BUDGET_ARCH.md:39`（ARCH-THREAD-001 §5-1）：「扫描 lib/ 生产源: `std::thread`/`std::async`/`_beginthread`/`CreateThread` **出现处**必须在 `THREAD_BUDGET_EXEMPT` 登记表」
- 权威依据: 宪章 §10.4（一个进程一个线程预算源；不得硬编码 workers；不得建不受 Runtime 管理的长期私有线程池）、§12.3-1、§17.6；ARCH-THREAD-001 §5-1/§5-3（未登记即 FAIL）
- 问题说明: 对当下树做只读重放（read/grep，未执行脚本）得到的量化结果。① 正则与仓库主流写法不匹配：`std::thread\s*\(|std::thread\s+\w+` 无法匹配 `std::vector<std::thread> workers/pool;`（`std::thread` 后紧跟 `>`），`lib/` 生产源（.c/.cpp/.h/.hpp，排除 archive/third_party/tests 与 `/tests/`）内该形态 **12 处命中、11 处代码行，全部不可见**，其中含 Runtime 自身的 `lib/core/src/executor.cpp:39/:217`；检查器自身正则在该集合内只命中 5 行（4 代码行），而这 4 行分别被文件名豁免（`resource_monitor.h`、`orchestrator.h`）与行级 `watchdog` 豁免（`orchestrator.cpp:5167/:5174`）吃掉 → 输出恒为「未登记线程创建=0」。② OpenMP 完全不设防：`lib/` 生产源 `#pragma omp parallel` **99 处**（叶子报 85；本条按「仅源文件 + 排除 tests/archive/third_party」重算得 99），其中带数字 `num_threads(\d)` 子句的生产源命中 **0**，故 `hardcoded_num_threads` 与 `omp_set_num_threads` 两条均无从命中。③ PATTERNS 无 `pthread_create`（`lib/gaia_xpsd_client/test/test_gaia_race.c:239` 在扫描目录内但不可见）、无 `hardware_concurrency`（`lib/` 生产源 20 处）。④ 扫描面缺 `providers/**`（3 处建池）与 `cli/`，且 `changed_paths` 只含 `lib/**`、`include/**` → 改 providers/cli/runtime 不触发该项。⑤ 行级 `watchdog` 子串允许任何含该词的行自我豁免。⑥ 实现低于其冻结合同文字（§5-1 要求登记「出现处」）。⑦ 模块自证（F00-03 同源）：`lib/photometric_calib/module.yaml:33-36` 明写「ThreadLease/omp_set_num_threads 无接线（迁移整改点）」——该模块自知不在预算面上，而门看不见它；同时该模块当前不在根 CMake 交付图（根构建的 phase1 测光目标是 `CMakeLists.txt:484 astrocs_phase1_phot` → `lib/phase1/photometry/photometer.cpp`），故本条只把它作为门盲区的佐证实例，不据此宣称生产数值受影响。
- 影响: §10.4「无未登记线程创建/无私有线程池」的机器保证实际不存在——新增 `std::vector<std::thread>` 池、OpenMP 区域或 provider 内并行都不会被拦；THREAD-BUDGET 与 NO-SERIAL-HEAVY 的绿勾不具备宣称的覆盖力；`test_01` 把空转结果固化为通过条件（负例 test_02/03 只证明注入假源码时正则会响，不证明对真实形态有效）。
- 建议处置: 扩扫描根到 `cli/providers/runtime/modules`；正则覆盖 `std::thread` 出现处（含 `vector<std::thread>`、`emplace_back` 建池）、`#pragma omp parallel`、`pthread_create`、`hardware_concurrency`；豁免改「文件+符号」登记制并去掉行级 `watchdog` 旁路；`test_01` 改为对真实树必须给出非零命中集合，或注入 `std::vector<std::thread> pool;` 形态的正例必须 FAIL；补齐 `changed_paths`。
- 置信度: 高
- related: M5a-G-006（第二预算源）、M5a-G-001/002（利用率门）、M5a-D-001（头文件语义相反）、F00-03、L13 注释层实例（M6 落，勿重复定稿）
