# 定稿 G_GOV_GATE / P0 — M8a（L25+L27）

> 合并代理 M8a｜来源 L25｜验证方式：纯只读（read/grep/glob），按当前树逐条重读；锚以 `path::符号` 优先，行号仅记复核时点。
> **合并说明（依负责人/前台指令）**：本条为**一条**定稿「依赖与许可登记面三处失真」＝ 面①许可传染 + 面②清单零登记 + 面③派生署名；面③含两例（Siril 派生无署名 / HEALPix 措辞与自著声明互斥），后者按前台第二道令由本条主落，不另立条。
> **别名规则**：其余文件中出现的 `M8a-G-002` 一律读作 `M8a-G-001②`（同一条的第二个面），不另立条、不重复计数。
> **引用建议条款**：`问题扫描/40_OWNER_DECISIONS.md:59` 的 **C-07（依赖与许可登记门）** —— 「PUBLIC 链接的非 vendored 库（如 GSL）必须出现在 DEPENDENCIES.md 与 dependency-lock.json 并带许可证；派生目录的 LICENSE 必须覆盖上游著作权」，本条即该条款的事实基础与阻断依据。
> **0 命中判据三级复核声明（依前台新令）**：本条所有"零登记/零命中"证据均走过 ①整句 grep ②放宽字符集与大小写的关键词 grep（含 U+2212/全角连字符/上下标变体）③定点 read 三级；放宽级复核结果一并记录，未因任何"报告称未命中"撤证据。

---

## M8a-G-001 依赖与许可登记面三处失真：GPL 组件进入产品链接闭包、五处登记面零覆盖、派生代码挂自著作权 MIT

- 类别: G_GOV_GATE
- 定稿优先级: P0（发布阻断；宪章 §17.10 要求 P0/P1=0，本条同时命中 §16.2/§17.11，故不具备"降级为表述问题"的空间）

### 面①：许可传染 —— GPL 族组件位于产品可执行文件的链接闭包内

- 位置: `CMakeLists.txt::astrocs_p1_sdet`（复核 535-546）→ `::astrocs_module_adapters`（646-649）→ `::astrocs_cli_runtime`（665-666）→ `::add_executable(astrocs)`（588-609）；`lib/star_detector/src/sdet_api.cpp::gsl includes`（28-31）；`lib/star_detector/Makefile::LDLIBS`（27）；`lib/star_detector/tests/p1star/CMakeLists.txt::自探注`（55-66）
- 证据摘录:
  > CMakeLists.txt:545 「# sdet PSF 拟合 trust-region LM 求解器依赖 GSL（star_detector Makefile 同款 -lgsl -lgslcblas）」:546 `target_link_libraries(astrocs_p1_sdet PUBLIC gsl gslcblas m)`
  > :646-649 `target_link_libraries(astrocs_module_adapters PUBLIC … astrocs_p1_ipv astrocs_p1_sdet …)`；:665-666 `astrocs_cli_runtime PUBLIC astrocs_core astrocs_module_adapters`
  > p1star/CMakeLists.txt:55-66 「主树根 CMake 无 GSL target，本域自探」→ 域内已意识到根图缺该依赖的受管表达
  - 派生侧（同一 STATIC 闭包）：`lib/plate_solve/cpp/ipv/SIRIL_COMPARISON.md`:1 「# Siril atpmatch.c + astrometry_solver.c vs IPV 完整逐项对比文档 (V3)」、:3 「基于 Siril 1.4.3 源码全面重新分析」、:18 「本文档中的所有 Siril 对齐方案已全部实施完成」；生产源沿用 Siril atpmatch 内部符号族 —— 计数口径：grep `AT_MATCH_` 于 `lib/plate_solve/cpp/ipv/src` = **50 命中 / 4 个生产 .cpp**（ipv_ransac / ipv_itertrans / ipv_solver / ipv_triangle）；`lib/plate_solve/cpp/ipv/src/*.cpp` = **13**（与 CMakeLists.txt:503 「ipv 13 TU」自述一致）。
  - 上游许可事实（附 URL，本代理独立抓取）：Siril = **GPL-3.0-or-later**（https://gitlab.com/free-astro/siril/-/raw/master/README.md 「## License」段）；GSL = **GNU GPL**（https://www.gnu.org/software/gsl/doc/html/gpl.html ， GSL 2.8 文档基线）。
- 判定边界（不夸大）: 本代理不判定法律意义上的"衍生作品"，也不断言 GSL 一定以静态方式并入（`-lgsl` 的实际解析取决于发行版提供 .so 还是 .a）。本面定的是**结构事实**：产品唯一用户入口 `astrocs` 的可执行链接闭包含一个 GPL 族库与一组以"对齐 Siril 源码"自述的 TU，而仓内既无 copyleft 义务履行面，也无派生登记面。

### 面②：清单零登记 —— 自称权威的登记面对该闭包内的组件全盲

- 位置: `DEPENDENCIES.md::依赖锁定 (BLD-004)`（复核 3、58-63）、`::生产依赖`（65-73）、`::test-only oracle`（75-79）、`::机器路径政策`（81-83）；`packaging/dependency-lock.json::production_dependencies/system_dependencies/test_only_oracles`（复核 14-133，条目名全量列举）；`packaging/licenses/`（实树 3 文件）、`::LICENSE-INDEX.txt`（7-10、22-23）；`packaging/gen_sbom_input.py::verify_dependencies_md`（93-107）
- 证据摘录:
  > DEPENDENCIES.md:3 自称「版本锁定权威」；:65-73 生产依赖只有 cfitsio 4.6.4 / nlohmann-json 3.12.0 / zlib / zstd / lz4 / OpenMP / Threads / libm / libdl
  > 计数口径（单文件 grep，大小写不敏感）：`DEPENDENCIES.md` 内 `gsl` **0 命中**、`siril|plate_solve|派生|derived` **0 命中**；`packaging/dependency-lock.json` 内 `gsl` **0 命中**、`siril|ipv|plate_solve` **0 命中**；`dependency-lock.json` 的 `test_only_oracles` 实有条目 **4**（astropy/numpy/pytest/astrometry.net），无 rcr/astropy-healpix/scipy
  > `packaging/licenses/` 实树 = `LICENSE-INDEX.txt` + `CFITSIO_LICENSE.txt` + `nlohmann_json.MIT.txt`（3 文件，无 GSL/Siril/HEALPix/nanoflann 文本）；LICENSE-INDEX.txt:7-10、:22-23 自认「仓库根无独立 LICENSE 文件（历史缺口）」
  > 交付末端：`tools/make_windows_release.py:101-106` 写入包内 `LICENSES/NOTICE.txt` 的是**脚本内字面量**（「静态自带的第三方: cfitsio(BSD), zlib, nlohmann/json(MIT), HEALPix(见 lib/plate_solve/LICENSE 等), OpenMP/VC 运行时」），既不含 GSL/Siril，又把 cfitsio 标为 BSD（实际 `licenses/License.txt:1-8` 为 NASA/USG 版权）并把 HEALPix 归属指到 plate_solve 的 MIT 文件 —— 详见 M8a-C-006
  > 反证（CI 面知道该依赖，锁面不知道）：`ci/toolchain.policy.json:28` apt 允许清单含 `"libgsl-dev"`；`ci/workflow_binding.json:49/76/129` 三次登记；`.github/workflows/ci-linux.yml` 两个 job 安装 `libgsl-dev` 并有 `test -f /usr/include/gsl/gsl_multifit_nlinear.h` 断言步
- 权威依据: 宪章 §16.2（发布包至少含「许可证」「product manifest」「build provenance」）、§17.11（发布包与审核包白名单/哈希/版本/provenance 全部通过）、§12.3-1（manifest/注册表/构建/产品清单一致）、§14.2（公共许可证须可寻址）；C-07 建议条款

### 面③：派生署名 —— 唯一许可声明是整模块 MIT，逐文件出处为零

- 位置: `lib/plate_solve/LICENSE::全文`（复核 1-3）；`lib/plate_solve/README.md::文档版本 r1 段`（复核 3-6）、`::唯一权威签名头`（13-14）；`lib/plate_solve/module.yaml::module_id/module_status/entrypoint`（49、57-58）
- 证据摘录:
  > LICENSE:1-3 「MIT License / Copyright (c) 2026 fujiaze」
  - 计数口径（对整个 `lib/plate_solve` 目录）：grep `SPDX|Copyright|GPL|MIT` = **1 命中**（即 LICENSE 自身），`lib/plate_solve/cpp/ipv` 子树 = **0 命中** → 13 个生产 TU + 15 个头文件零逐文件署名、零出处头注。
  > README:5-6 「本 README 重写取代 V4.30 营销式旧 README——旧文成功率/性能**对比等叙述保留于 GitHub 上游仓库与 memory.md 存档**」→ 派生叙述被移出活动文档树，而承载派生指征的代码与本仓唯一许可声明留在原处
  > 同批 STATIC 里的另一处派生：`CMakeLists.txt:515` 把 `lib/gaia_xpsd_client/src/gaia_client.c` 编入 `astrocs_p1_ipv`；该模块有出处登记（`lib/gaia_xpsd_client/README.md:17` 「上游来源 | PixInsight XPSD 格式客户端（历史上游 Gaia-DR3-DR3SP-Client-C，MIT；仅来源说明，非本合同权威）」、:151-153 「## 许可 / MIT…」）—— 同一 target 内一半有出处注记、一半零注记，对比即是标准（本仓 `lib/common/healpix/THIRD_PARTY_NOTICE.md` 为范式样本）
### 面③续（派生署名第二例）：HEALPix 措辞与自著许可声明互斥（并入本条主落，M2b 承诺不重复定稿）

- 位置: `lib/common/healpix/healpix_core.cpp::neighbors/query_disc 段注`（复核 337-341、344-352、418）；`lib/common/healpix/THIRD_PARTY_NOTICE.md::本实现与上游差异`（复核 19、22）；可达性：`CMakeLists.txt::astrocs_aio`（267）与 `lib/hips/CMakeLists.txt::astrocs_p1_hips_writer`（41 直编该 TU、43 SHARED）→ 进交付 DLL。
- 证据摘录:
  > healpix_core.cpp:338-339 「邻居算法移植自官方 HEALPix C++ (**Healpix_3.83 healpix_base.cc / healpix_tables.cc, GPL-2+ 参考**)」；:418 「移植自 Healpix_3.83 query_disc（cone_search, :1320）…max_pixrad 取自官方实现 (healpix_base.cc L1314)」
  > 同文件 :344-352 已把上游表以常量入库：`kNbXOffset[8]` / `kNbYOffset[8]` / `kNbFaceArray[9][12]`（注释自署「官方 Healpix_Tables::nb_xoffset/nb_yoffset」）
  > THIRD_PARTY_NOTICE.md:19 「仅迁移 NESTED 排序所需路径, **未迁移 RING 排序与邻居查询**;」；:22 「**未复制任何 GPL (Healpix_cxx / RELION) 代码进入生产树。**」
  - 三级复核记录（放宽级）：`docs/` 全域 grep "(?i)healpix.{0,80}(gpl|licen|许可)" = **0 命中**；"healpix_cxx|官方 ?healpix|healpix_base" 于 `docs/` = **0**；"3[ ._-]?8[0-9]" 于 `docs/science/` 无许可语义命中 → **"Healpix_3.83 GPL-2+" 措辞不在 docs 的 DISP-HIPS 文档里，而在生产源码注释**（`lib/hips/README.md`、`docs/algorithms/HIPS_WRITER.md` 全文亦无许可字样），故转述中"文档写成 GPL"的定位需按此订正；互斥事实本身（源码声明 vs 自著 NOTICE 断言）两级复核均命中。
- 权威依据: 宪章 §16.2（发布包须含许可证）、§14.2（许可可寻址）、§12.3-1/-9；GPL-2+（若成立为复制/衍生则随交付传染 copyleft 义务，与本域 BSD-3 自述互斥）；HEALPix 官方实现版权归 EA/ESA/CNRS（GPL-2+）这一上游事实与 `THIRD_PARTY_NOTICE.md:22` 的"未复制任何 GPL"是同一命题的两侧
- 问题说明: 同目录内两句话不可能同时为真：源码注释写明邻居查询与 cone-search 算法**移植自 GPL-2+ 的官方 HEALPix C++**（且把上游邻面表作为常量表入库），而 NOTive 断言**未迁移邻居查询**、**未复制任何 GPL 代码进入生产树**。该 TU 确实编入交付（`astrocs_p1_hips_writer` 直编 + `astrocs_aio`），因此这不是措辞问题而是交付许可面的实质矛盾：若"移植"成立 → copyleft 义务与面①同族；若仅"参考算法、表为独立重写" → NOTICE 与源码注释须同时改写为可辩护的表述并给出 clean-room 依据。另记：位交错表为 8 元素小表，独立重写可能性不低，但**仓内没有任何登记说明这一点**（无 derived_code 条目、无 clean-room 声明、无逐文件出处），故按 §16.2 阻断族处理，定性交负责人/法务。
- 建议处置: 与面①②③同批裁决：①负责人裁决"参考 vs 复制"性质；②`dependency-lock.json` 增 `derived_code` 条目（上游 Healpix_3.83 / 文件 / 段落 / 许可 SPDX GPL-2+ / 差异说明），或补 clean-room 声明；③NOTICE 与源码注释统一为同一事实口径；④随 C-07 门一并覆盖"注释声明与登记面条目一致"的比对。

- 四态判定: **仍成立**（三面均在当前树复核命中，无一处被并发修复消除）。对照记录：`lib/common/healpix/healpix_core.cpp:338-339` 的「Healpix_3.83 … GPL-2+ 参考」与同目录 `THIRD_PARTY_NOTICE.md:22`「未复制任何 GPL (Healpix_cxx / RELION) 代码进入生产树」互斥这一事实，**本轮由 M8a 主落（前台指令：M2b 承诺不重复定稿，其原条目 M2b-G-04 与本面并档、以本条为准）**（前台转述称其为「L15-002 许可证面」，经复核：L15-002 在 `_cache/L15.md:56` 与 `_merge/M2b.md:34` 的定身是 `hips_pixel_scale` 单位错值面，由 M2b-B-03 主落；且 `Healpix_3.83` 字样出现在生产源注释而非 `docs/` 的 DISP-HIPS 文档 —— 全 docs 树 grep `GPL-2|Healpix_3\.83|healpix_cxx` = 0 命中）。本条**已将该面并入为面④**，不另立条。
- 影响: Phase1 天测解算核心路径（ipv 13 TU + sdet）与最终交付 exe 的许可证面、SBOM 面、审核包白名单面同时失真；§16.2/§17.11 在现状下不可能通过，§17.10（P0/P1=0）连带不成立；GSL 大版本 ABI 漂移与上游改动可追溯性均无锚。
- 建议处置: ①负责人裁决派生性质与 copyleft 履行方式（随包 GPL 文本 + 源码提供承诺，或取得兼容授权，或替换求解器/改链接策略）；②按 **C-07 建议条款**落门：`dependency-lock.json` 增 `system_dependencies[gsl]`（版本 floor + 哈希策略 + 许可 SPDX + 来源）与 `derived_code`/`vendored_copies` 登记面（上游项目/版本或 tag/commit/仓内路径/许可/逐文件映射/聚合哈希），`DEPENDENCIES.md` 同步；③派生成立则订正 `lib/plate_solve/LICENSE` 的覆盖范围与义务，不成立则补 clean-room 声明 + 逐文件出处头注（比照 `lib/common/healpix/` 范式）；④机器门按 **出处事实**判定而非目录名（见 M8a-G-003），并把 `gen_sbom_input.py` 由单向改双向后接进 `ci/checks.json`（见 M8a-G-004）；⑤交付 NOTICE/SBOM 改由 lock 派生，删除脚本内许可字面量（见 M8a-C-006）。
- 置信度: 高（三面均可仓内字面复算：链接闭包、五处登记面零命中、许可文件与署名计数、上游许可 URL；唯一保留项是"是否法律意义派生"，已交负责人终裁）
- 来源: L25-001（面①③派生侧）+ L25-002（面①②GSL 侧），按前台指令并成一条
- related: **C-07（40_OWNER_DECISIONS.md:59，本条的处置落点）**；M8a-G-003（三面的共同根因：豁免与登记按目录名字面切分）、M8a-G-004（三面的共同门缺失：依赖锁无机器门且判据单向）、M8a-G-005（同族的 BSD 侧未登记）、M8a-C-006（交付 NOTICE/SBOM 硬编码）、M8a-C-009（oracle 侧零登记）、M2b-G-04（HEALPix_3.83 面主落，本条不重复计）、M5b 版本簇 G-04（事实不同源：依赖锁缺失 vs 版本号复写，仅交叉引用不合并）、L01-020（同目录残留事实）、F00-01
