# V11 分片 2 · 两条 P0 + 两条 P1（ABI 镜像与合同面）

### V11-N-04（P0）ctypes **少传第 9 个参数** `out_status` ⇒ C 侧把未初始化栈槽当指针，**既往垃圾地址写、又据其垃圾值决定"哪些星算成功"**；现行 gate2 PSF 证据产生于这条不匹配 ABI 之后
- **普查口径**：脚本 `_v11_arity2.py` 自动比对「C 头原型参数个数 ↔ Python `argtypes` 个数」：**82 处 argtypes 赋值 / 76 处可配到原型 / 不一致 2 处**，两处都是 `dpsf_fit_batch_f32`：**PY=8 vs C=9**（未配的 6 处是 opaque 句柄与跨行原型，不作判据）。
- **权威与时序**：`lib/dynamic_psf/include/dynamic_psf.h:122-132` 第 9 参 `int *out_status` 由 **`01eaba36`（2026-09-13 17:23，"fix(SCI/B2-A2) 星 ID ↔ PSF compact 错位修复 + 逐星 status（RESCUE-V3 P0-05）"）** 追加；`git log -L` 逐字显示 `- int *out_n_valid` → `+ int *out_n_valid, + int *out_status`。**同提交的 C++ 侧全部改齐**（`p1psf_tests_core.cpp:241` 等调用都带第 9 参 `NULL`），**只有两份 Python 镜像停在 8 参**：`lib/plate_solve/tools/diag_gaia_psf_projection.py:210-216`、`lib/photometric_calib/cpp/test/gate4_dr3sp_gaiaxpy/gate2_psf_oracle.py:158-166`。
- **为什么比 `V2-N-01` 更危险**：C 侧不只写它——`dpsf_psf.cpp:960` `const bool ok = out_status ? (out_status[i] == DPSF_PSF_STATUS_OK) : isfinite(out_psf_params[i*9+1]);` **读它来决定哪些星成功并据此 compact**（另 `:864`/`:895`/`:934`/`:945`/`:949` 写）。x64 下第 9 个标量参走**栈槽**，ctypes 只压 8 个 ⇒ 被调方读到**未初始化栈内容**：非 NULL 即按 `n_detections` 个 `int` **往任意地址写**，且"成功星集合"由垃圾决定。
- **打到挂账证据（时间戳实测）**：`gate2_psf_oracle.py` 正是 **PSF 质心门的 AstroCS 侧量具**，其产物 `evidence/gate2_result.json` 的 mtime = **2026-09-14 14:40，晚于签名变更 09-13 17:23** ⇒ 簇 5 引用的「生产 p95=1.1529px / photutils p95=1.5795」产生于这条不匹配 ABI 之上。**静态只能判"该证据不可复现"**；是否恰好读到 NULL、实际有没有写坏 ⇒ **需运行期，未判**（V11 不跑，我也不跑）。
- **建议处置**：①两份镜像补第 9 参（传 `None`/`NULL` 即可命中 `:960` 的 `isfinite` 退路）；②**把「C 原型参数数 ↔ argtypes 参数数」做成机器门**（V11 已给出可跑的 `_v11_arity2.py`，76/82 可自动配，误报面可控）；③**请负责人裁**：修好后 `gate2` 证据是否**必须重算**（我的建议：必须，且重算前后差值本身是这条 P0 的唯一量化答案）。
- **related** **`V2-N-01`**（同族：镜像落后于结构/签名变更）、**`M3b-F-01`/`M3b-F-02`**（PSF 质心门证据链）、簇 1「校验对象≠交付对象」、`M9-§7`（修复即扩散：C++ 全覆盖、Python 验证侧整体漏掉）、`C-14`/E6

### V11-N-05（P0，结构性）ABI 协商与自检的 6 个合同函数**生产面零实现、零调用**，唯一实现住在**测试自己的探针副本**里 ⇒ 那道门在自证
- **声明面**：`include/astrocs/abi/lifecycle_v1.h` 声明 6 个判定函数（`acs_lc_transition_allowed_v1:158`、`acs_lc_op_error_v1:163`、`acs_abi_compat_v1:169`、`acs_struct_ext_ok_v1:177`、`acs_negotiate_v1:194`、`acs_module_selftest_v1:234`），头 `:141` 还写明「host 可预检，module 自检**同源使用**」。
- **三级复核定义面**：①五个函数名在 `lib`/`runtime`/`cli`/`modules`/`providers` 的 `*.c/*.cpp` **零命中**；②放宽 `ext_ok` → 只剩该头 + `tests/abi/abi002_lifecycle_probe.c`；③定点读确认全仓**唯一定义**在 `abi002_lifecycle_probe.c:24/50/55/60/68/84`。
- **自证结构坐实**：`tests/abi/test_abi002_lifecycle.py:67-79` 的 `compile_probe` 命令 = `[cc, -std=c11|c++17, -Wall, -Wextra, -pedantic, -I include, PROBE, -o, exe]` —— **只编探针一个 TU，不链任何生产对象** ⇒ 18 个 test 方法断言的是"测试自己写的那份 reference"。该测试 `:94` **自带白注释**「只编译（链接语义由探针测试覆盖；避免未定义引用）」⇒ **仓库自己已经知道真链接会撞 undefined reference**。
- **第二条硬后果**：头声明了这些符号，任何真模块按头注释去调**必然链接失败** ⇒ "同源使用"在本仓事实上不可能 ⇒ 每个消费点只能各自抄一份 ⇒ **直接生出下面的 `V11-N-06`**。
- **顺带订正一条挂账措辞（不撤条，只补数）**：簇 1 / `L23-001` 称「`tests/abi` 三个脚本无 TestCase ⇒ discover 收 0 用例仍 OK」。V11 AST 实测现 6 个 py：`test_abi002_lifecycle.py` **TestCase=4 / test 方法=18**、`test_abi005_echo.py` 1/1、`test_module_registry.py` 1/1、`test_secure_loader.py` 1/1、`test_mod001_install_load_check.py` 1/1，**仍为 0 的只有 `mod001_install_load_check.py`** ⇒ 成因已从"没类"变成「有类但 `UT-ABI` 仅 `linux-main` + 探针不在构建面」（`grep -rn abi002 --include=CMakeLists.txt --include=*.cmake` 全仓**零命中**）。**转告 `L23-001` 归属域更新措辞。**
- **related** **`V11-N-06`**（后果）、`FD-F-003`（0 用例记 PASS）、`M8-F-004`（在册≠执行）、`A-38`、簇 1、`C-16`

### V11-N-06（P1）同一个 `struct_size` 判据在仓内有**三套互斥语义**，头规定的那套几乎没人用
- **A 尾部扩展兼容（头规定：`status_codes.h:12`「扩展只允许追加尾部字段」+ `lifecycle_v1.h:61-65` + `:147` 静态断言）= `peer >= self` 兼容**：只有头文字与测试副本（`abi002_lifecycle_probe.c:60-66`）；实现侧 `providers/cpu/{baseline.cpp:465,515,607; avx2.cpp:285,333,391; avx512.cpp:140}` 与 `runtime/module_loader/secure_loader.c:572/593/626` 用 `<` 拒绝式（**=A 的正确实现**）。
- **B 严格相等 `!= sizeof(X)` 即拒**：`p1_session.cpp:155`、`p2_session.cpp:57`、`p3_session.cpp:53`、`aio_abi.cpp:101`、`secure_loader.c:347/382/388/658`、`module_registry.c:579` ⇒ **与头相反方向**：一次合法的尾字段追加（**正是 `V2-N-01`/`V11-N-04` 的成因方向**）会让**三个 Phase 会话入口 + 装载器 4 处同时判 `ABI_MISMATCH`** ⇒ 宪章承诺的尾扩展兼容在这 7 个点不可用。
- **C `struct_size==0` 放行**：`runtime/io/fits_core.c:83`（`fio_check_head`）、`:94`（`fio_check_hooks`）、`runtime/io/hips_core.c:679` ⇒ 与头相反且**方向是放宽**：零初始化即通过握手，而 **ctypes 结构体默认就是零初始化** ⇒ 自描述对"忘填"这一种**最常见漂移完全失效**。今天三份 Python Fio 镜像确实主动填了（`test_fits_stream_contract.py:143-144`、`make_hips_fixture.py:103-104`、`hips_output_fixture.py:100-101`）⇒ 门**不是恒真**，而是「**对未来某个忘填的调用方恒真**」（V11 这句限定我原样保留）。
- **判词**：A/B/C 三者不可能同时对；权威在头 ⇒ **B、C 是实现偏离自订合同**，不属"与旧文档不一致"。
- **related** **`C-14`**（本条是它的实证核心）、`V11-N-04/05`、簇 8（装饰性合规的 ABI 版）、`E6`

### V11-N-07（P1）唯一的 `struct_size` 机器门 `ABI-BOUNDARY`：**语料与被保护面 8/8 零交集**，且"扩语料即假绿"的陷阱已在门内
- **复刻**：V11 逐字复刻 `tools/check_abi_boundary.py`（`_v11_gate_corpus.py`）——其 `HEADERS` = `include/astrocs/common_abi_v1.h` + `lib/backend_host/*.h`（排除名含 `impl`/`inc`）⇒ **共 12 个头**；规则 1 原文「所有 C ABI 结构必须带 `struct_size` + `abi_version`」。
- **交叉比对**：被跨语言/跨 DLL 消费的结构所在 **8 个头**（`ipv_api.h`、`aio_hips.h`、`aio_pipeline.h`、`star_detector.h`、`dynamic_psf.h`、`gaia_client.h`、`fits_stream_v1.h`、`aio_abi_v1.h`）**8/8 全部不在语料内**（脚本逐行输出「不在 <<<」）⇒ 本轴全部镜像漂移与那 70 个无自描述结构，**都在这道三 profile、`waivable=false` 的"非豁免真门"之外**。
- **关键限定（防止把门误判成已假绿）**：该门对现有 12 个头**是有效的**（`common_abi_v1.h` 全用 tag 名），只是**覆盖面被高估**。但陷阱已埋好：两个正则都是 `typedef\s+struct\s+(\w+)\s*\{`（**要求 tag 名**），复刻实测在 `common_abi_v1.h` 扫到 11 个、按 typedef 名实际 12 个（差集 `[acs_status]`）；而本仓绝大多数公共结构写作**匿名** `typedef struct { ... } Name;`（`IpvParams`/`AioHipsSnrPoint`/`GaiaSpectrumStar` 全是）⇒ **只扩语料不换正则，门会一个结构都扫不到并继续报 PASS**。
- **建议处置**：正则改「`typedef struct` 块 + 其后 `} Name;`」提取；语料以**「被 ctypes/跨 DLL 消费的结构」为驱动**（可由 `_v11_exports.py` 的采集结果自动生成），而非固定头清单。
- **related** `M8-G-001`、簇 1（在册非豁免 ≠ 有效）、**`C-14`/E6**、`V11-N-01`
