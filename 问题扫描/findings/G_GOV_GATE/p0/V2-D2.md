# V2 定稿 · 本批新引入的门禁与静默降级缺陷（P0/P1）

### V2-N-08 FAST 模式的裁决前提已被同批另一提交推翻 ⇒ `psf.max_stars` 静默决定交付的 SNR／极限星等，且无任何 parity 锁
- **ID** V2-N-08 ｜ **类别** G_GOV_GATE ｜ **优先级** **P0**（我裁定：V2 原判「P0/P1 需前台裁」）
- **位置**：截断源 `lib/core/src/module_adapters.cpp:1761-1765`（`psf.max_stars` 默认 **5000**）与 `:1772` `constexpr bool kPrecisePsfEnabled=false` + `:1785 if constexpr`（⇒ **生产永不进精确分支**）；新消费者 `module_adapters.cpp:2505` 起（SNR 节点读 `src_frame->value("psf_params")`）；语义 `lib/phase1/noise/README.md:50`「**SNR 目录成员 = `p1_sources.json` 的 `psf_params` ∩ sources**」。
- **证据链（这是关键）**：`23a7f665`（P2 PSF FAST）的正当性建立在「`psf_params` 在仓库内**零消费者**，截断只影响诊断」之上；而**同批 `35c85f53`（P8）恰好造出了这个消费者** ⇒ 前提被自己推翻。现况：`n_detected=145884` 被截到**最亮 ≤5000 颗**，**这 5000 颗直接决定 `p1_snr.json` 的 `median_snr`/`snr_phot`/`frame_depth_m5_mag`** ⇒ **SNR 与极限星等系统性偏乐观**（截最亮 ⇒ 残差与深度都偏好）。
- **为什么不是 P1**：①交付 JSON 里的科学量被一个**诊断性性能开关**静默决定；②**无 parity 锁**——P2 当初自证的「`p1_snr` 逐位不变」测的是**当时还没有该消费者**的实现，P8 的新锁用手工 fixture，两头都不覆盖这个耦合；③`psf_mode` 又恒标 `"fast"`（见 V2-N-09）⇒ **下游连"被截断过"都读不出来**。合起来正是宪章 §4.3「manifest 记实际执行路径」+ §17.6「禁静默降级」双违，且落在交付数值上。
- **建议处置（V2 原案，三选一或组合）**：①把 SNR 目录改回**全量成功星**（精确 PSF 重新有价值，FAST 只用于形态诊断）；②或在 `p1_snr.json` 显式记 `psf_mode`/`n_fit_input`/`n_sources` + **目录被截断标记**，并把该耦合写进 `DATA-P1-SNR` 与 `SCI-CW`；③无论哪种都补一条 parity 锁（同一输入在 `max_stars=0` 与 `5000` 下 `median_snr` 差异必须**可见且被断言**）。
- **取证口径** V2 静态逐字（三处锚 + `--stat` 时序对照）；**偏移幅度需真实帧实测，V2 与我均未跑** ｜ **置信度** 高（耦合存在性），影响量级未知 ｜ **related** **V2-N-09**（同一事的可观测面）、`V1-N-12`（同属交付 SNR 数值正确性）、`M2a-H-1`（同族反向：那条是请求被忽略，这条是截断被静默传播）、`A-40`

### V2-N-09 `psf_mode` 是无条件字面量 `"fast"` ⇒ 走精确路径也标 fast，锁只查键名不查值
- **ID** V2-N-09 ｜ **优先级** **P1** ｜ **类别** G_GOV_GATE ｜ **状态** OPEN
- **位置**：`module_adapters.cpp:1737`（产物）与 `:1750`（manifest）写死 `"fast"`；而精确路径 `:1778` **共用同一实现** ⇒ `psf.max_stars=0`（文档义「0=不截断=全量精确」）与直调钩子**都仍标 fast**；锁 `p1001_real_nodes_test.cpp:2199` 只 `contains("psf_mode")` **不查值** ⇒ 挡不住。**与 `FD-F-001/F-002`（子串断言守卫）同机制**、与 `V2-N-08` 同因。
- **建议处置**：`psf_mode` 由实际分支决定（含截断时 `"fast:<n_fit_input>"` 或加 `psf_truncated` 位）；锁改值断言并覆盖 `max_stars=0` 一侧。

### V2-N-10 极限星等迭代：不收敛为 fail-open、`capped` 靠 `fmod` 猜、`converged`/`query_failed` 不落交付面
- **ID** V2-N-10 ｜ **优先级** **P1** ｜ **类别** H_NUMERIC ｜ **状态** OPEN
- **主体先记正分**：V2 用 `scripts_v2_secant.py` **逐字复刻**复算出金标准 `m_final=12.9850849855`、2 次查询、178 颗，与 `test_mag_iter.cpp` 锚**逐位一致** ⇒ 代码↔测试自洽、非恒真断言；且它**不是教科书割线法**（差分只估 `alpha`，步进符号由 `sign(log10(N_target)-log10 N)` 给出、`alpha` 默认恒正 ⇒ **方向自纠正，不需符号变化/括号保证**）⇒ 不判缺陷；`max_q=4` 硬上界必停 ✓。
- **三条残余**：①**不收敛 fail-open**（`:518-574` 只在 `have_ok&&valid&&n>=2` 时返 -1）且 `mi.converged`/`mi.query_failed` **未落 `StarSelection`**（`:974-981` 只写 iterations/n_returned/capped/alpha_final）⇒ 交付面分不清「达标」与「4 次耗尽仍欠查」，**`capped` 还被记成 `converged=true`**；②**触顶判定用 `fmod(N,200000)==0` 启发式**：只在**所有被查文件同时饱和**时成立 ⇒ 部分文件截断（`200000+3000`）漏判，且**被文件遍历序截断的 `N` 仍进 `alpha` 差分与 `rel` 判据** ⇒ **P4 声称修正的「按文件序截断的科学有偏」仍在**（与本批动机直接冲突）；③**NaN 静默**：`focal/exposure=NaN` 时 `std::max(NaN,1.0)=NaN` 与 `:410` 比较恒假 ⇒ **静默落到 13.0**，无 error 无标记；另缺 `alpha_min>0` 校验（配 ≤0 则同点空转满 4 次）。**无任何测试固定新旧差异**（旧函数已删、A/B 证据在未版本化的 `run/`）⇒ 数值差异未判。
- **related** `A-39`（同属「交付面看不出退化」族）、`V2-N-09`、`M3-E-001`
