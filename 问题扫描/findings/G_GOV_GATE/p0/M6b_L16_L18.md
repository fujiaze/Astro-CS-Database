# M6b · G_GOV_GATE 定稿（来源切片：L16 / L18 / F00-02 / F00-04 / F00-05）

> 合并代理 M6b。行号仅为辅助定位，锚一律以 `path::符号/字段` 为准（并发修复导致漂移，处置见 `_merge/M6b.md` §7）。

---

### M6b-G-001 未经证据支撑的「已验证」声明（单一根因，四组实例）：八层矩阵在 TEST / EVIDENCE / SRC / 整行四个层面给出 VERIFIED，而门的结构只可能判「文件存在 + 符号 token 可见」

- 类别码: G_GOV_GATE
- 优先级: **P0**
- 位置:
  - `tools/traceability/check_traceability_matrix.py::_check_refs`（现 :258-274，:262-265 `if code in ("MOD","SRC","TEST"): continue  # C7 已校验文件存在`）、`::_check_anchor`（现 :284-315）、`::C7 调用点`（现 :241-256）、`::STATUS_OK`（现 :55）、`::AUTHORITY_DIRS`（现 :88）、`::main/--strict 判定`（现 :272-273、:436-452）
  - `ci/checks.json::TRACEABILITY-MATRIX/command`（现 :1111-1114，**无 `--strict`**）
  - `docs/traceability/TRACEABILITY_MATRIX.json::modules[*].{test_status,test_path,evidence_status,src_status,notes}`、`docs/traceability/TRACEABILITY_MATRIX.csv`（同构视图）
  - 实例 A（TEST 层）：JSON 全部 `test_status=VERIFIED` 行；实例 B（EVIDENCE 层）：`::EVID-BLD-003`（现 :9）、`::EVID-IO-001`（现 :35）、`::EVID-CPU-001`（现 :61）；实例 C（未构建却整行 VERIFIED）：14 行（现 :87,:113,:139,:165,:191,:217,:347,:373,:425,:607,:659,:685,:711,:763）；实例 D（SRC 锚在声明）：22 个 VERIFIED src 行中 11 行锚 `.h/.hpp`
  - `docs/algorithms/STAR_PSF_ALGORITHMS.md::§11.4/§11.5 状态声明`（现 :201、:203）
  - `docs/modules/registry/astrocs.phase2.resample.md::§Execution class`（现 :39-40）
  - `ASTROCS_PROJECT_CONSTITUTION.md::§12.3-1 / §12.3-4 / §13.1 / §17.2`
- 证据摘录（全部为本代理按当前树复算，未沿用二手数）:
  - 层分布（30 行矩阵）：`test_status` VERIFIED **22** / MISSING 8；其中 **18/22 的 `test_path` 以 `docs/` 开头**（锚在文档而非测试），仅 4 行指向真实测试文件（`modules/conformance/noop/tests/unit/noop_handshake_test.c`、`modules/services/io/tests/fits_core_selftest.c`、`tests/cpu/dispatch/cpu_capability_matrix_test.c`、`tests/unit/p1_ir_facade_test.cpp`）；**11** 行的 TEST ID 为 `*-DESIGN-001` 形态（自述设计态；口径：对矩阵 JSON 的 `"test_id": "…DESIGN…"` 整行 grep 计数 = 11，不依赖 read 行文本）。
  > （checker :262-265）if code in ("MOD", "SRC", "TEST"):
  > （checker :263）    continue  # C7 已校验文件存在
  > （checker :308-315）若锚携带 ::symbol：仅判 symbol 是否出现在文件文本中（`sym not in text → SYMBOL_MISMATCH`）
  > （SPEC:124）任一检查项 FAIL 即追溯链断（宪章 §17-2 口径）。
  - `evidence_status` VERIFIED **3** / MISSING **27**（90%）；schema 无 `evidence_path` 列（`contracts/schemas/traceability_matrix.schema.json` 仅 `evidence_id/evidence_status`），故 VERIFIED 的证据层在真源里**没有任何路径字段可供机器复核**；C8 判据是"ID token 出现在 `evidence/`/`reports/`/`returns/` 任一文件内"，而 EVID-BLD-003 / EVID-IO-001 / EVID-CPU-001 三个 ID 在 `evidence/**` 与 `reports/**` 全仓 **0 命中**（实测）。
  > （JSON:9 行 notes）evidence 锚 returns/BLD-003（…）/（JSON:61 行 notes）证据 returns/CPU-001
  - `returns/` 实测为 **0 个文件**（`glob returns/**` = 空）；`AUTHORITY_DIRS`（:88）却把 `returns` 列为 EVIDENCE 合法权威根。
  - 14 行的 `notes` 自述「entrypoint=MISSING / 未编入根 CMake / dll 未建」，而 7 个合同层状态全为 VERIFIED：实测 **19 行**为七层全 VERIFIED，其中 **14 行** notes 自述未落地（其余 5 行为 conformance/service/provider 类，按规则豁免）。典型两行：
  > （JSON:217-225）"src_status": "VERIFIED", "test_status": "VERIFIED", "evidence_status": "MISSING", … "notes": "…phase1-photometry: CONFORMS-TO ASTROCS-AIO-001。生产实现 lib/photometric_calib/src/photometry.c … 依赖注入，未编入构建。…"
  - 独立复核 `photometric_calib`：`grep photometric_calib CMakeLists.txt` **0 命中**（实测）→ 该行 SRC/TEST 层 VERIFIED 无任何构建/测试承载。
  - 声明即缺陷仍 VERIFIED：
  > （STAR_PSF_ALGORITHMS.md:201）- 状态：VERIFIED（设计冻结，dpsf 套件建立后 TEST-P1-PSF-001 落 EVIDENCE）。
  > （registry/astrocs.phase2.resample.md:40）确定性=固定顺序输出(1/N 等价已验)。 —— 同页 :13-20 自述 sci_id/data_id/api_id/test_id 为「占位 ID——descriptor 使用，docs/science 未定义」
- 权威依据: 宪章 §12.3-1（manifest/注册表/构建 target/产品清单四源一致）、§12.3-4（核心合同必须至少存在一个独立测试）、§13.1（每模块必备九项）、§17.2（文档—模块—源码符号—测试追踪无断链，发布门禁）、TRACEABILITY_SPEC §5「SOURCE SYMBOL：锚必须是 path::symbol」、§7「任一检查项 FAIL 即追溯链断」。
- 问题说明: 四组实例同源——矩阵把「存在一个同名文件/同一 token 可见」当成「已验证」，而 §12.3-4 与 §13.1 要求的是独立测试与产品清单一致；C7 的弱判据 + C8 显式跳过 SRC/TEST + CI 不带 `--strict`（越界 REF_OUT_OF_SCOPE 降级为 WARN 后连 WARN 都不影响 exit 0）+ `STATUS_OK` 用全局并集而非逐层取值域，使 VERIFIED 的语义在机器上被偷换。EVIDENCE 层更是**结构上不可核**（无路径列 + 权威根含空目录 `returns/`）。
- 影响: ①追溯链的"已验证"结论对 22/30 行不成立，§17.2 发布门禁实际无判据；②27/30 行无 EVIDENCE 支撑，宪章 §12.3-12「结论可追溯到机器证据」在矩阵层被批量违反；③按矩阵做影响面/回归选择（ci/impact_map.json 把 TRACEABILITY-MATRIX 列为大量变更路径的必跑门）会漏掉真实测试缺口；④"1/N 等价已验""VERIFIED（设计冻结）"这类措辞会在负责人审核包中被读成已达成。
- 建议处置: ①C7 增逐层判据：TEST 层 VERIFIED 要求 `test_path` 落在 `tests/` 或 `modules/*/tests/` 且为构建内目标；②C8 不再跳过 SRC/TEST，SRC VERIFIED 要求锚目标为定义（`.cpp/`.c）或经 `module.yaml` 声明为 header-only；③schema 增 `evidence_path`（或 `authority_ref`）必填列，并把 `returns` 从 `AUTHORITY_DIRS` 移除或补建该目录；④`ci/checks.json::TRACEABILITY-MATRIX/command` 加 `--strict`；⑤`notes` 含 `entrypoint=MISSING`/`未编入构建` 的行禁止 SRC/TEST=VERIFIED（新增 CROSS 判据）；⑥把 `*-DESIGN-001` 行的 TEST 状态改 `NONE`/MISSING，另立 `verification_level` 列表达"设计冻结"。
- 置信度: 高（全部数值复算；判定式来自源码逐字）
- 来源: F00-04（前台基准）、L16-001、L16-002、L16-004（本条即三者的共同根因；同类实例全部保留各自 path:line）
- related: M6b-G-004、M6b-G-005、M6b-E-001、M6b-E-003、M6b-I-002、M6b-C-001、L16-011、L16-012、L16-015

---

### M6b-G-002 发布与完成状态在仓库内无单一事实源：四份 RELEASE_STATUS 并存、状态词三套、AGENTS.md 状态行被自家校验器判非法却被另一道门强制保留，且活动文档描述的执行面已被取代

- 类别码: G_GOV_GATE
- 优先级: **P0**
- 位置:
  - `docs/owner/RELEASE_STATUS.md::§0 状态词阶梯 + §3`（现 :18-27、:70-:71、:82-84）
  - `docs/RELEASE_STATUS.md::概述`（现 :6、:16）
  - `docs/review/RELEASE_STATUS.md::§3 标题`（现 :29）
  - `docs/archive/review/RELEASE_STATUS.md::状态字面量`（现 :12）
  - `AGENTS.md::治理要素映射表 状态机行`；`tools/check_agents_gov.py::REQUIRED`（现 :15）
  - `tools/quality/validate_task_ledger.py::STATUSES`（现 :42-43）与 ::非法值负例（现 :208、:229）
  - `docs/TROUBLESHOOTING.md::全篇命令面`（现 :5-:33）；`docs/DOCUMENT_INDEX.yaml::（docs/TROUBLESHOOTING.md 条目）`（现 :70-71）
  - `docs/ARCHITECTURE.md::§2 数据流 / §5 模块表`（现 :16、:18、:51、:52）；`docs/API_REFERENCE.md::§12/§13`（现 :112、:127）；`docs/modules/phase2.md::概述`（现 :6）
  - 旁路构建面并档（域外实例，只登记不重复定稿）：`cmake -S cli`、`cmake -S lib/phase2`、根 `Makefile`/`build.ps1`（L12-001/002/003 → M5a；L16-004 → 本条实例 C）
- 证据摘录:
  > （docs/RELEASE_STATUS.md:6）PRE_RELEASE_ENGINEERING_FOUNDATION = **PASS**   ／  （:16）FINAL_REAL_DATA_VALIDATION = **PENDING**
  > （docs/review/RELEASE_STATUS.md:29）## 3 冻结面（PASS，当前提交静态可核）
  > （owner/RELEASE_STATUS.md §0，现 :18-27 摘要）九级状态词（含 NOT_READY_FOR_RELEASE / IMPLEMENTED 需"文件锚+命令+rc=0"）取代 PASS 口径；:82-84 「已实现未接入……不得声称 IMPLEMENTED/可用」
  > （archive/review/RELEASE_STATUS.md:12）… 1 REVIEW_PENDING …
  > （AGENTS.md 状态机行）`REVIEW_PENDING` 系 V5 遗留状态，当前台账验证器（`tools/quality/validate_task_ledger.py`）已将其判为**非法状态**，本条仅作历史映射、不得据此新增状态
  > （tools/check_agents_gov.py:15）"状态机": ["状态机", "任务级状态取", "REVIEW_PENDING", "waiver"]   ← 门强制该字面量必须出现在 AGENTS.md
  > （validate_task_ledger.py:42-43）STATUSES = {NOT_STARTED, IN_PROGRESS, PASS, FAIL, BLOCKED, WAITING_WINDOWS, WAITING_DATA}
  - 实测：`docs/DOCUMENT_INDEX.yaml` 中同路径系状态声明文档 4 份并存（owner=ACTIVE_NORMATIVE、review=ACTIVE_INFORMATIVE、docs/ 版=ACTIVE_INFORMATIVE（现 :66-67）、archive 版=ARCHIVED）；`grep -c "status: ACTIVE_NORMATIVE" docs/DOCUMENT_INDEX.yaml` = **123**、`ACTIVE_INFORMATIVE` = **65**、`ARCHIVED_NON_NORMATIVE` = **31**、`GENERATED` = **17**（索引共 205 active + 31 archived 条）。
  - `docs/TROUBLESHOOTING.md` 通篇命令面为 `toolchain.ps1` / `lib\snr_estimator\cpp\test\*.exe` / `phase2_synthetic_gate.exe` / orchestrator 启动，`astrocs` CLI **0 次出现**（实测全文 40 行），而其 DOCUMENT_INDEX 状态为 ACTIVE_INFORMATIVE；`docs/ARCHITECTURE.md:16/:18/:51/:52` 仍以 `orchestrator.exe <stage1.json>`、`astrocs-stage2 <stage2.json>` 描述生产数据流，而 `docs/architecture/ARCHITECTURE.md`（ARCH-002，FROZEN）:10 明确二者为"被取代的旧入口（迁移冻结）"。
- 权威依据: 宪章 §1.1（权威顺序）、§12.3-10（不得以陈旧版本号或历史状态冒充当前状态）、§13.2（验证等级不得混用）、§16.2/§16.3（状态与发布口径）、§17.10（P0/P1 清零方可发布）、§17.12（仅负责人可作发布决定）；`AGENTS.md::治理要素映射（alpha/发布、状态机）`；`docs/architecture/ARCHITECTURE.md::§1 被取代的旧入口 + §7 不变量（机器可验）第 1 条`（现 :10、:54）。
- 问题说明: 同一个"当前状态"问题在仓库内有四个答案、三套词表（owner 九级 / 旧 V19R8 的 PRE_RELEASE_*+PASS / 台账七状态），且**互相判对方非法**：AGENTS.md 因门强制而保留 `REVIEW_PENDING`，台账验证器判其非法；`docs/review/RELEASE_STATUS.md` 用 owner §0 明令废止的 PASS 口径；`docs/ARCHITECTURE.md`/`docs/TROUBLESHOOTING.md` 描述的执行面按 ARCH-002 已退役。再叠加旁路构建面（`cmake -S cli`、`cmake -S lib/phase2`、根 Makefile/build.ps1）与 `docs/modules/phase2.md:6` 的"生产入口 astrocs-stage2"，读者在仓库内**无法判定哪一份是当前权威状态**，也无法判定"哪套命令才是当前构建/运行面"。§12.3-10 的机器门（DOC-L0 / DOC-INDEX / P3-STATUS / AGENTS-GOV）全部只做存在性与关键词集合检查，无任何一致性判据（门侧根因见 M6b-G-004/M6b-G-005）。
- 影响: 发布判断（§17.10/§17.12 的输入）与审核包证据面不一致：负责人若读 `docs/RELEASE_STATUS.md` 或 `docs/ARCHITECTURE.md` §7 会得到"工程基础面已 PASS"的结论，而 L0 权威是 NOT_READY_FOR_RELEASE；对 Agent 而言，"当前权威状态"不可判定即等价于任何状态声明都可被选择性引用，宪章 §1.1 权威分层失效。
- 建议处置: ①`docs/RELEASE_STATUS.md`、`docs/review/RELEASE_STATUS.md` 标 SUPERSEDED_BY 并移入 `docs/archive/`（同步 DOCUMENT_INDEX）；②`docs/review/RELEASE_STATUS.md:29` 的 "PASS" 改 owner §0 词表；③AGENTS.md 状态机行改为"引用 `tools/quality/validate_task_ledger.py::STATUSES` 为唯一合法值集"，并同步把 `check_agents_gov.py:15` 的 needle 由 `REVIEW_PENDING` 改为 `WAITING_WINDOWS`；④`docs/TROUBLESHOOTING.md`/`docs/ARCHITECTURE.md`/`docs/API_REFERENCE.md`/`docs/modules/phase2.md` 的入口叙述按 ARCH-002 §1 订正或整体归档；⑤增一道"完成度词表一致性"门（声明词只允许 owner §0 九级 + 台账七状态，且禁止 ACTIVE 文档出现已废止字面量）。
- 置信度: 高
- 来源: F00-02（前台基准）、L18-003、L18-009、L18-010（部分）、L16-010（REVIEW_PENDING 环部分）
- related: M5a（L12-001/002/003 旁路构建面）、M5b-G-008（AGENTS-GOV 门侧）、M5b-G-017、M6b-G-003、M6b-G-005、M6b-I-001

---

### M6b-G-003 「本版实测」类无据证据：ACTIVE 文档以完成时宣称机器门 PASS 9/9 与 mismatches=[]，既无 SHA/命令/时间/日志指针，其所引检查器又根本不在 CI 注册表内

- 类别码: G_GOV_GATE
- 优先级: **P0**
- 位置: `docs/ARCHITECTURE.md::§7 Machine Consistency (S8 gate, 本版实测)`（现 :114-125）、`::文首机检声明`（现 :3）、`::§6 工具清单`（现 :112）、`::§5 模块表 orchestrator 行`（现 :106）、`docs/README-DOCS.md::机器一致性行`（现 :22）、`docs/standards/DOCUMENTATION_STANDARD.md::S8 声明`、`tools/docs_machine_consistency.py::docstring 检查清单`（现 :5-13）、`ci/checks.json::checks[*].id/command`（实测 75 个 id 无该项）、`ci/INVENTORY_REPORT.md::S8 段`（现 :73）
- 证据摘录:
  > （ARCHITECTURE.md:3）> 工程架构权威（机检见 `tools/docs_machine_consistency.py` 9/9 + `tests/arch/`）
  > （ARCHITECTURE.md:114-124）## 7. Machine Consistency (S8 gate, **本版实测**) / ``` / tools/docs_machine_consistency.py PASS 9/9 / …… / tools/config_consistency_check.py PASS mismatches=[]
  > （ARCHITECTURE.md:112）- 工具: `tools/docs_machine_consistency.py`(9 checks，见 §7)，`config_consistency_check.py`，`api_doc_consistency.py`，`no_legacy_production_reference.py` 等。
  > （README-DOCS.md:22）机器一致性：tools/docs_machine_consistency.py（S8 gate）
  - 实测：`docs/ARCHITECTURE.md` 全文（133 行）**不含任何 SHA、命令行、时间戳或 evidence/reports 指针**；`grep -rn "docs_machine_consistency|config_consistency|api_doc_consistency|no_legacy" ci/checks.json` = **0 命中**（本代理逐字复核 ci/checks.json 全部 75 个 `"id":`），即所谓 "S8 gate" 从未登记为 CI 检查项；`tests/` 侧亦无任何引用（`grep docs_machine_consistency tests` = 0 命中）。
  - 该工具自身的比对基面：`tools/docs_machine_consistency.py:6-11` 声明比对 `stage2_common.cpp`、`orchestrator AstroCsExitCode`、`orchestrator stage_name_v2`、`noise_model.cpp`、`aio_hips.h`、`drizzle_engine.h`；而 `ARCH-002`（`docs/architecture/ARCHITECTURE.md::§1`）已把 orchestrator.exe 列为被取代旧入口，根 `CMakeLists.txt`:586 一带明文禁止 `lib/orchestrator/cpp` 进入生产构建（本代理实测该路径唯一存在处为 `lib/orchestrator/cpp/include/orchestrator.h`）。
  - `ci/INVENTORY_REPORT.md:73` 对该工具的记录是「无视 --help 无条件覆盖写 `reports/v19r3/evidence/quality/`…」——即它既是**未接线的旁路脚本**，又会**无条件写仓库内产物路径**。
- 权威依据: 宪章 §12.3-12（L0 结论必须可追溯到**当前 SHA** 的机器证据）、§12.3-10（禁止历史状态冒充当前状态）、§13.2（验证等级：静态检查≠当前提交实测）、§14.5（所有外部命令须带 timeout 并保存日志）、§16.2（状态声明与证据绑定）；`docs/architecture/ARCHITECTURE.md::§7 不变量（机器可验）第 1 条`（现 :54：入口表述唯一由测试断言）；`docs/owner/RELEASE_STATUS.md::§0`（现 :19：IMPLEMENTED 需"文件锚+命令+rc=0"）。
- 问题说明: 宪章 §12.3-12 要的是"当前 SHA 的机器证据"，此处给出的是**无边界的完成时陈述**：无 SHA、无命令行、无时间、无日志路径，读者无法复算也无法证伪；更糟的是被引用的检查器未登记进 `ci/checks.json`，所以"9/9 PASS"既不可复核，也不会在任何提交变更后重跑；其比对基面（orchestrator）还指向被生产构建禁止的退役模块，意味着即便真跑，PASS 也不描述当前生产面。同族的"文档内以完成时宣称机器门结果"还出现在 `docs/validation/SCIENCE_FREEZE.md:3-5`（详见 M6b-I-003）与 registry 页「1/N 等价已验」（M6b-G-001 实例）。
- 影响: ①§12.3-12 与 §13.2 被绕过，"实测"成为无需证据的形容词，是审核包中最易被引用的假绿来源；②负责人据 ACTIVE 文档的 PASS 陈述做完成度判断（§17.10 输入）；③文档宣称的门与 ci/checks.json 实际面长期背离，掩盖真正的门缺口。
- 建议处置: ①§7 结论改为「未接线（缺口登记）」或直接删除"本版实测"字样，如需保留则附 `sha + 完整命令行 + UTC 时间 + 日志落 `run/resource/` 路径`；②`tools/docs_machine_consistency.py` 若仍是规范要求的 S8，则登记为 CI 检查项（`mutates_workspace:false` + 产物改落 `run/resource/`），否则从 `README-DOCS.md:22` 与 `DOCUMENTATION_STANDARD.md` 的规范声明中删除；③其 orchestrator 基面按 ARCH-002 §1 重指向现行 CLI/编排实现；④增门：ACTIVE 文档内出现 `PASS`/`已验`/`实测` 字面量的行，必须同段落包含 `sha=` 与 `command=`（可正则机检）。
- 置信度: 高
- 来源: F00-05（前台基准）、L18（本版实测类条目）、L16-010（门未接线同族）
- related: M6b-G-001、M6b-G-004、M6b-G-005、M6b-I-001、M6b-I-003、M5b-G-015（宣称的门不存在）、M5b-G-017

