# V12 · 常数单一权威点（P2 两条）

### V12-N-02（P2·**反向钉死的机器门**）`snr_constants` 把**截断串在场**固化为过门必要条件 ⇒ 按 `S-1` 统一精度反而会让门变红
- 宿主：`tools/docs_machine_consistency.py:139-144`。权威点数：1（**且未接线**——不在 `ci/checks.json` 126 项内）。
- 判据原文为四条目串命中：`"1.4826022185" in snr_doc and "0.7316728" in psf_doc and "0.7316727929211932" in nm and "1.482602218505602" in nm`。**python3 实测**：`"0.7316728" in "0.7316727929211932"` = **False**，而 `"1.4826022185" in "1.482602218505602"` = True ⇒ ①对 11 位串**恒真**（`M3-A-008` 已记，不重报）；②比对面只有 `NOISE_MODEL.md`/`PSF.md`/`noise_model.cpp` 三文件，同文件两套位数的 `sdet_api.cpp:227`(15 位)与 `:590`(4 位)、`star_matcher.cpp` 倒数式、providers FP32 4 位**全不在面内** ⇒ **结构上不可能变红**；③**新事实**：若 `PSF.md` 按 `S-1` 统一到全精度，`"0.7316728"` 消失 ⇒ **门反而变红** ⇒ 门把「文档必须保留 7 位截断串」固化成过门必要条件（**反向钉死**，与 `V4-N-16` 同族但更隐蔽：钉的是**字面串**而非计数）。
- 处置：门改为「两侧数值相等（解析到 double 比较）」而非字符串在场；接线进 CI；与 `V12-N-01` 的 constants 头合并后，让门比较"文档值 ↔ `constants.h` 值"。
- related：`M3-A-008`、`M6b-G-003`、**`V4-N-16`**、`V12-N-01/04`、`C-16`

### V12-N-04（P2·伪精度）`kTrimMeanToSigma` 的 16 位值**不等于它自称的解析值**：第 7 位起就错（rel −4.13e-07），且文档未声明分母域 ⇒ 常数不可复算
- 宿主：`lib/snr_estimator/cpp/src/noise_model.cpp:37 kTrimMeanToSigma=0.7316727929211932`（`:331` 用）；**第二份同名** `snr_science.cpp:38`（注释自述「`noise_model.cpp:37` 同源常数」而**互不引用**）；`snr_estimator.cpp:53`/`:74`/`:99` **裸字面量 3 处**（`p.sigma_sky_adu = residual_scale / 0.7316727929211932`）。文档面：`PSF.md:23/:81/:88/:119` 用 7 位；**`STAR_PSF_ALGORITHMS.md:56-57` 同一行内三套**（`0.7316728` / `0.7316727929211932` / `0.731673`）；`API_REFERENCE.md:83` 7 位；`ARCHITECTURE.md:96`、`NOISE_MODEL.md:91` 16 位。权威点数：2 + 裸码 3。
- **V12 的独立复算（闭式 + 蒙特卡洛双口径，`_verify/scripts_v12_trim2.py`）**：对 `|r|` 的 10–90% 截尾均值解析值 = `2(φ(q0.1)−φ(q0.9))/0.8` = **0.731673095280613**（`q0.1=Φ⁻¹(0.55)=0.12566`、`q0.9=Φ⁻¹(0.95)=1.64485`）；MC（N=2e6, seed=12345）=0.7323830（+9.7e-4 ≈ 2σ 采样噪声，口径确认）。⇒ **在册 16 位 rel −4.132439e-07**（第 7 位起错）；docs 7 位 rel −4.035690e-07；16↔7 位 rel +9.674826e-09。换算影响：`residual_scale=1000` 时 σ = 1366.731153153（16 位）vs 1366.730588360（解析）⇒ **`σ_sky` 系统性高估 +4.13e-07 相对**。
- **还有一层"不可复算"**：另一自然口径（带号残差 ±1.2816σ 截尾后取 `|r|` 均值）= **0.5586098711723649**，与在册差 **24%** ⇒ **文档未声明分母域**，所以"16 位精度"是**伪精度**——位数多不等于定义清。`PSF.md:119` 声明「数值由高斯分位积分确定」，而**仓内无该积分脚本/结果登记**（触 `C-15`/`C-18`）。
- 钉死：`noise_model_science_test.cpp:363` 与 `p1snr_science_test.cpp:45` 用**同一个 16 位串** ⇒ 共源，抓不到值错；`docs_machine_consistency.py:141` 把 7 位串钉成在场条件（见 `V12-N-02`）。
- related：`_merge/M3.md:189`、`M3-D-003`、`V1-N-09`、`V12-N-01/02`
