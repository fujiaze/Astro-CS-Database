# W2 片4-5 · P2 四条 + 判否与未判清单
（`W2-N-14` 为编号占位，其事实已并入 `W2-N-06` 第二站点 ⇒ **本轴实立 14 条**）

### W2-N-11（P2）交付帧头**同时写 CD 与由 CD 伪造的 `CDELT1/2` + `CROTA2=0`**（不可逆且不等价）
- 甲：`module_adapters.cpp:3085-3118` 的 KV 表中 `:3087` 列出 CDELT1/2 与 CROTA1/2=0，`:3106` 把 CDELT1 返回为 `b5=CD1_1`、CDELT2 取 `b8=CD2_2` ⇒ **与同表 CD 四元并存**。乙（等价性反证）：`fits_reader.cpp:359-368` 的反推式 ⇒ `CD′=diag(CD1_1,CD2_2) ≠ 原 CD`；**同仓正确等价式在 `third_party/cfitsio/wcssub.c:250-254`**（`phia=atan2(cd21,cd11)`）。
- 复算：PA=30°、s=1.1e-5 deg/px ⇒ 重构后 `|det′|/|det| = 0.7500`（**像素尺度低 13.40%**）且旋转丢 30°，r=2048 处合成位移 ≈42″；**等价 CROTA2 应为 150.0° 而非 0**。可达性：自产帧恒带 CD ⇒ 本仓走 CD 支路不受影响，**危害面是按 CDELT/CROTA2 优先解释的外部读者**与 `V7-N-04` 已登记的「CD 对角双零 → 退 CDELT」分支。
- related `M9-B-2`（其内证段引用本写侧但判「生产不可达」⇒ 本条给出可达面与量化）、`M1a-B-006`、`V7-N-04`

### W2-N-12（P2）`WcsTan` 交付 RA 值域 **[−180,180)** 与合同 **[0,360)** 互斥，而交叉门用同一 wrap ⇒ 恒不可见
- `wcs_tan.cpp:35-36` 的两次 ±2π 归一使 RA 落 [−180,180)，负值经 `module_adapters.cpp:2162-2176` 原样进 `p1_wcs.json` 的 samples；合同 `ASTROMETRY.md:29` 定 RA 为 deg [0,360)，**同仓另四处（aio_wcs/ipv/p3_wcs/gaia 匹配）都归一到 [0,360)**；交叉参考门 `module_adapters.cpp:375-377` 用同一 wrap 且 `p1_angular_sep_deg` 本身 wrap-safe ⇒ **门在数值上恒不可见**。
- 复算：CRVAL1=210° 的帧中心像素交付 RA=−149.99967（应 +210.00033），**偏差恒为 360° 整数倍；影响面为 RA≥180° 的整个半天区样本**。related `M2a-B-2`（可复用模式）、`M1a-D-001`

### W2-N-13（P2）Phase3 投影退化的「**四角同半球**」守卫对跨极恒真 ⇒ 冻结文本这一判据本身不能表达其意图
- `p3_wcs.cpp:15` 的 `kMaxAbsDec=85.0` **仅作用中心**、`:140-148` 四角只要求 `pix2world` 返回成功、`:163` 唯一硬失败是 `r >= π/2`；文本要求见 `PHASE3_HIPS_TO_FITS.md:48`「输出四角与中心同半球（越界显式拒）」+ §9a-12（FOV≤20°、中心距极≥5°）。
- 复算（标准 TAN 逆式）：中心 dec=85°、15000² px @1e-3 deg/px（**边长 15°<20° 上限、距极 5°=下限，即完全合规**）时四角全部可解：左上 (71.034,75.543)、左下 (148.280,82.210)、右上 (8.966,75.543)、右下 (291.720,82.210)、顶边中点 (220.012,87.543) ⇒ **同一顶边两端 RA 相差 143°，天球已绕过极点回折，交付图为折叠像**。
- ⇒ **即使日后补齐 `M1a-C-004` 所缺的 FOV/|dec| 强制点，该守卫仍语义上无法覆盖跨极**。related `M1a-C-001`（TAN 奇点无防护）、`M1a-C-004`（本条为其残余面）

### W2-N-15（P2）「独立参考实现」测试**硬写本仓明令禁止的魔数 210960**，且私有上限与生产不同制
- `hiss_correctness_test.cpp:500-511` 注释称「参考实现 `compute_auto_nside`（与 `drizzle_engine.cpp` 算法一致）」却写 `210960.0/finest_arcsec` 并 `if (nside>1048576) nside=1048576`；`:563-573` 断言 `nside==1048576`（finest=0.1）。对照生产：`drizzle_engine.cpp:680` 明文「**禁止魔数 210960**/1186.18」、`:684-685` 是 √(π/3)·(180/π)·3600、`:703-706` 钳位 `NSIDE_MAX=4194304`；另有 `memory.md:121` 记 210960、`lib/drizzle/README.md:87` 记 211034.6、`module_adapters.cpp:3146-3147` 同式重推导（＝`W1-N-12` 双实现）。
- 复算：公式真值 **211076.285**；测试值 210960 偏 **−0.0551%**、注释值 211034.6 偏 −0.0197%；而断言用 `hp_res <= finest*1.01`（1% 容差）**吸收该偏置 ⇒ 此差在测试里永不可见**；决策翻转窗存在于**每个 2 的幂边界**（宽 0.055%），且 finest<0.201″ 区间测试私有上限 2^20 与生产 2^22 **直接不同制**（测试只自证其私有上限）。related `M2a-D-1`（注释面本条是测试面兄弟）、`M7-G-104`、`V12` 族

## W2 判否清单（逐条读过并判定不是缺陷，防后续轮次重复上报）
- J-1 `CDELT·CROTA2→CD` 构造与 Paper I 相反？**否**（与 `third_party/cfitsio/wcssub.c:253-291` 逐元素 boolean 对拍 exit 0）⇒ **维持 `M9-B-2` 对「符号相反」子主张的撤回**。J-2 `crpix=(W+1)/2` 对 `W/2+0.5`：算式恒等。J-3 `p3_projection.cpp`/`wcs_sip.cpp` 的 0/1-based：逐处 `x+1` 显式桥接自洽。J-4 drizzle 单像素方差归一：两像元 v=100 复算得 50，与 SCI-DRZ α²v 一致。J-5/J-6 面积单位 sr 与 tile 无 BUNIT：已由 `M2a-A-2` 立条，本轴只补量化因子（0.2″/px ⇒ 1/A_pix=1.0636e12，等效 30.067 mag）。J-7 FP32 累加：归 `M2a-H-1`(P0) 与 `DISP-HIPS-009`。J-8 解析归一对离散样本和 ±1.2%：归 `M1a-A-003`/`V1-N-12`(a)。J-9 读噪双计：`V1-N-12`(c) 在册，本轴只确认其 gain 是 1.0 伪值（`W2-N-02`）。J-10 Phase2 降级权重两制：属 §6.3 锁定簇（`M3-A-002`+`M4-A-003`，**两份 FROZEN SCI 互斥须负责人裁决**）。**J-11 drizzle 输入 weight 面数值不进 sumFlux/sumVarNum**（`hp_drizzle_api.cpp:276-303` → `drizzle_engine.cpp:1903-1906`、`:1362` 形参被注释掉）：冻结式 SCI-DRZ §5 只定义 `w_jp=a_jp/A_drop`、**未定义输入权重乘子 ⇒ 定义真空，需负责人钉死语义**。J-12 sup 与 A_cell 与 hp_res：与 `DRIZZLE.md:145` 一致，缓冲 1.25×>实测 1.044 保守。J-13 `gain_variance` 诊断式方向：与 `NOISE_MODEL.md:58` 一致（注释/测试侧失实归 `M3-A-006`）。
- **需运行期未判五项**：R-1 FITS 通道 `exptime=0` 进 HISS `EXPTIME=0.0`（`meta` 空载，`hp_drizzle_api.cpp:1143-1150`/`:346`）生产是否经 `drizzle_fits` 入口；R-2 IPv 0.5 px 坐标域偏置是否被 ra0/dec0 收敛吸收；R-3 k_corr 越界与层级方差低估的**发生频率**；**R-4 `zero_point_mag` 是积分 ADU 零点还是 ADU/s 零点——全仓 `ADU/s`/`adu/s`/`flux_rate`/`/exptime`/`electron/s` 在生产码与 science 文档零命中、EXPTIME 全链从未被除出**（若按 ADU/s 供零点则 t=30/300/1800 s 时 m_5 偏 −3.693/−6.193/−8.138 mag，30 s 与 300 s 混合深度互不可比；**属合同真空需裁决而非判代码错**）；R-5 HISS signal（原始 sumFlux）与 HiPS signal（flux/area 面亮度）**同名两制**需产品裁决。
