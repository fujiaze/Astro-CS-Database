# V12 片3-4 · P2（双写无锁 / 边界相反 / 常数安全域 / 合成口径钉死）

### V12-N-07（P2）割线迭代 5 个兜底字面量与结构默认值**各写一遍**；`±6.0` 全仓无承载
- 兜底 `ipv_select.cpp:395 safety=3.0`/`:396 tol=0.1`/`:397 max_q=4`/`:398 zero_step=3.0`/`:400 alpha=0.2885`；默认 `ipv_types.h:228 0.2885`/`:233 3.0`/`:240 3.0`/`:245 4`/`:246 0.1`；步长夹 `:492 min(max(step,-6.0),6.0)`。**权威点数：2**（默认与兜底各一处，互不引用）。
- 复算：今日五对默认↔兜底**全部同值（0 ulp）**⇒ 无当前数值差，但**改 `ipv_types.h` 默认即静默分叉**，而兜底路径**生产可达**（`ipv_api.h` 允许外部填 params，配置写 0/负即触发）。`±6.0` 无 params 字段（`grep m_lim_step_max` → 0）、docs 0 登记、无 static_assert；`test_mag_iter.cpp` 只测"参数可覆盖"、**不比对兜底==默认**。
- 处置：兜底一律引用结构默认（同源）+ 加一条 `static_assert`/测试比对五对值；`±6.0` 入 params 并登记。**related** `V2-D2③`、`V12-N-05/11`。

### V12-N-09（P2）同一个「32」**三义**、比较号不同、可覆盖通道只作用于其中一条 ⇒ **边界点两侧结论相反**
- 宿主：`resource_gate.h:164 memory_growth_limit_mb_per_s=32.0`（注释"调用方可覆盖"）→ `:233` 判据用 **`>`**；`memory_report.h:48 kAllocGrowthUnboundedMbPerS=32.0`（编译期 constexpr）→ `resource_gate.h:370` 判据用 **`>=`**；`memory_report.h:53 kAllocReclaimResidualTolBytes=32ull*1024*1024`（字节，第三义）；文案 `:423` 印可覆盖字段、`:452` 印常量。**权威点数：3**。
- 复算：RSS 斜率恰 32.0 ⇒ `(32.0>32.0)` 假 ⇒ 不判 MemoryGrowth；alloc 斜率恰 32.0 ⇒ `(32.0>=32.0)` 真 ⇒ `AllocGrowthUnbounded`(FAIL) ⇒ **同一"无界增长"语义在边界点两侧相反**。调用方把 limit 覆盖成 16.0 ⇒ RSS 门线 16、alloc 门线仍 32（**差 2 倍**），同一 verdict 两条文案各印一个阈值。宪章 §10.5/§17.6 对"无界内存增长"**未给任何数值**。
- 处置：一处常量 + 一个比较号 + 覆盖通道作用于两条（或明确禁止覆盖）。**不重报 `A-42` 的"0.5/32MiB 无出处"面**，本条只判边界相反。**related** `A-42`、`V4-N-10`、簇 6。

### V12-N-10（P2）1.2 bbox 裕量：三处文本保证强度互斥 + **赤道分支缺"锥触极"回退守卫**；V12 造出可复算漏星反例
- 宿主：`lib/gaia_xpsd_client/src/gaia_client.c:923`（唯一，`d_ra*cos_dec > radius_deg*1.2`），守卫 `:922 cos_dec<0.01`。**权威点数：1**。文本三处互斥：`gaia_client.h:9`「裕量 1.2 **保持无假阴性**」（无条件）vs `docs/algorithms/GAIA_QUERY.md:95-96`「`false_negative=0` 是工程保守性结论…**非数学证明**」vs `:166`「多查不少查，不漏星」。
- **反例（逐字复刻该谓词；`_verify/scripts_v12_fneg.py`、`_v12_band.py`、`_v12_bbox12b.py`）**：`q=(ra0, dec=70°, ρ=20°)`、叶 bbox `ra 80..85 / dec 88..89` ⇒ `d_ra·cos70 = 27.3616 > 1.2ρ = 24.0000` ⇒ **谓词返回 0（剪掉）**；而叶内点 `(80.000, 88.000)` 与查询心角距 **19.7471° < 20°** ⇒ **该叶含锥内星 ⇒ 漏星**。第二反例 `ra 76..80 / dec 87..89` 同样被剪、叶内最近 19.4839°。使 1.2 失效的最小半径：`dec0=50→37.0°`、`60→27.0°`、`70→17.5°`、`80→8.75°`。对照组：**若赤道带树严格限于 |dec|≤45，则 dec0=10..80、ρ 扫到 90−|dec0| 全域 0 反例**。
- **要害（不是"1.2 不够大"，是安全域没编码）**：决定该常数是否足够的「赤道树赤纬覆盖域」**既无文档数值也无代码断言**（`GAIA_QUERY.md:52` 只写"赤道带 4 棵"；`AE_CAP_BOUNDARY_DEG 45.0` **只用于 AE 分支整树拒绝**）；`:922` 守卫只看**查询中心**（`acos(0.01)=89.427°`），**不覆盖"中心不在极区但锥触极"**；而 AE 分支有对应守卫 `:984`（`θ_q+ρ>90 → 保守不剪枝`），**赤道分支没有**。对照裕量量级 `1.2/1.1107207345395915 = 1.0804`。
- **可达性如实限定**：`ρ=query_radius_deg` 由 `compute_fov_density` 给出；FOV≤20° 属 SCI-AST 口径但**全仓无强制点**（`A-08` 系）⇒ **漏星是否实际发生：需运行期 + 真实 XPSD 树布局，未判**；静态可判的是**文本矛盾 + 守卫缺失 + 安全域未编码**。
- 处置：赤道分支补 `θ_q+ρ>90 → 不剪枝`；把赤道带覆盖域写成常量 + `static_assert` + 文档数值；三处文本统一为"工程保守性结论"。**related** `V5-N-04`（0.25 裕量同型）、`A-08`、`V5-N-03`（同函数族的静默漏星）、`C-15`

### V12-N-12（P2）波长网格 `step`：测试面钉 **1**、真库是 **2** ⇒ 凡经 fixture 验证的 λ 轴只覆盖真库一半波长域，且"把 fixture 改对"会立刻让锁变红
- 生产 `gaia_client.c:1224`（`atoi "spectrumStep="`）→ `:2620` 出参 → `orchestrator.cpp:1485 out_wl[i]=start_nm+i*step_nm`（同式注释 `:1460`）→ `module_entry.c:763`/`:820`。测试面 `gaia_xpsd_fixture_gen.c:342` 写 `spectrumStep=1`、`xpsd_spectrum_count_bounds_test.c:97-113` 七例全 step=1、**`gaia_cat_test.c:282-283 CHECK(rc==1 and s1==336 and s2==1 and s3==343)`**。真库口径唯一在册处：`gate4_dr3sp_gaiaxpy/DR3SP_SCHEMA_AUDIT.md:29`（`start=336, step=2, count=343`）与 `:33`（λ∈[336,1020] @2nm）。文档 `GAIA_QUERY.md:131` **只写公式不登记数值三元组**、`README.md:48` 同。
- 复算：生产 `336+342×2 = 1020 nm`（跨 684 nm）；fixture `336+342×1 = 678 nm`（跨 342 nm）= **真库的 1/2**。**免重报核对**：`git grep spectrumStep|spectrum_step` 在 `问题扫描/` 命中 **0** ⇒ 全仓首次登记。
- 处置：把 `(start, step, count)` 三元组登记到 docs 权威层（一处），fixture 与生产同源生成；`gaia_cat_test.c:282` 改断言"与登记三元组一致"而非钉合成值。**不重报 `M3-I-003`/`DISP-PHOT-002` 的 0.1 vs 1.0 nm 面**。**related** `M3-I-003`、簇 9（fixture 与生产分叉）、`V1-N-09`（合成 oracle 同族）

### V12-N-13（P3）`n_target` 被夹进 `[50,60]`，注释写"统一为 60"，文档写"20→40→60" ⇒ 三套口径互斥，两常数无承载
- 宿主：`ipv_select.cpp:317-318` `n_target_cap=60; n_target=min(60, max(50, lround(n_target_dbl)))`；注释 `:306-313` 记录 150→300→150→60 的反复并写"宽窄 FOV 统一为 60（与 `img_n_target` 一致）"；对照 `ipv_types.h img_n_target=20`、`docs/algorithms/PLATESOLVE.md §5:115`「默认 20，自适应 20→40→60」。**权威点数：0**（60/50 皆裸，无 params 字段、无文档数值、无 DISP）。复算：实际取值域仅 `{50..60}`（20% 带宽），其余被夹。
- 归因弱故判 P3：**不重报 `M1a-L01-012`** 的"默认 20 被 60 绕链"（`_merge/M1a.md §二` 记该半边随并发提交消失）。**related** `M1a-L01-012`、簇 8、`V12-N-07`
