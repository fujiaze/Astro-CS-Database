# M8 定稿（L23）— A_SCI_DEF / P2

> 合并代理 M8｜来源 _cache/L23.md + _cache/L22.md｜纯只读复核（read/grep/glob），锚点在当前树按符号重新定位；行号为「复核时点」参考。

### M8-A-001 provider oracle 判定器把 2e-4 相对容差与截断到 1.4826 的 MAD 常数写成默认口径，与被判定文档（rtol=1e-6/atol=1e-7、1.482602218505602）不一致，且自带一条永不进入的失败分支

- 类别码: A_SCI_DEF
- 优先级: **P2**（判定器不在任何注册执行面 ⇒ 无现行判定后果；若接入则升 P1）
- 位置: `tools/monitoring/run_provider_oracle_checks.py`（复核时点 :7/:78 引用 docs/science 口径、:111 `1.4826`、:186 `tol = 2e-4`、:221 死分支、:223 `if FAILURES:`）；对照 `docs/science/NOISE_MODEL.md:46/:91/:135`、`docs/science/CALIBRATION.md:107`；被指向的不存在文件 `tools/monitoring/run_cpu_baseline_checks.py`
- 证据摘录:
  > （run_provider_oracle_checks.py:111）1.4826   ← MAD→σ 常数（截断 5 位）
  > （同文件:186）tol = 2e-4
  > （同文件:221）if not any(n.startswith("noise") and "mad" in f for f in FAILURES):   ← 与 :223 组合后该分支永不进入
  > （NOISE_MODEL.md:91）FP64 全链路；MAD 常数 `1.482602218505602`（15 位截断，与 `1.4826022185` 差 <1e-12）…与本节 `1.4826` 不可互换
  > （CALIBRATION.md:107）NumPy 对同一 raw/bias/dark/flat/K 的双分支公式逐像素比对，FP32 `rtol=1e-6, atol=1e-7`
- 权威依据: 宪章 §5.3（科学定义单一真源，判定器不得自设容差）、§6.3（默认容差冻结）、§12.3-4、§13.1
- 问题说明: ①容差：判定器用 2e-4，被判定文档给 FP32 逐像素比对定 rtol=1e-6/atol=1e-7 ⇒ 判定面比合同松约 200 倍（口径差异未在任何文档登记，与 M8-F-006 的 golden 1% 同族）；②常数：判定器用 `1.4826`，而 NOISE_MODEL.md:91 **明文声明** `1.4826` 与 15 位常数「不可互换」⇒ 判定器与被判定文档在同一常数上取互斥口径（**订正 L23 措辞**：这不是"抄错"，而是文档已登记的不可互换口径被判定器采用为默认）；③逻辑缺陷：:221 的条件在 :223 之前，任何 noise-mad 失败已先被 :223 计入 FAILURES 汇总，:221 的"宽容豁免"分支实际不可达 ⇒ 死代码；④:12-13 注释把 ABI 冒烟推给 `run_cpu_baseline_checks.py`，该文件全仓不存在（同 M8-F-005 的子事实）。宿主 `lib/photometric_calib` 不在根构建图（既有登记事实），故判定器本身也在非执行面。
- 影响: 若该 oracle 被接入 §13.1 的 NumPy/解析解对账门，会以松 200 倍的容差放行回归，并用「不可互换」的常数口径判 noise 路径；死分支掩盖作者以为存在的豁免逻辑。
- 建议处置: ①容差与常数一律从 docs/science 取值（或由负责人显式冻结 provider 侧独立容差并在 STANDARDS_REGISTRY 登记）；②删/修 :221 死分支并补一条负例自检（注入失败必红，参照 `p1noise_tests_selfcheck.cpp` 的正例写法）；③修文档指向（把 `run_cpu_baseline_checks.py` 改为真实存在的 runner 或删除该句）；④接入执行面前先做本条 ①②③。
- 置信度: 高（判定器逐字；两侧文档口径逐字；死分支为静态判定，无运行验证需求）
- 来源: L23-013（定稿；措辞按本代理实测收窄）
- related: M8-F-005、M8-F-004、M8-F-006
