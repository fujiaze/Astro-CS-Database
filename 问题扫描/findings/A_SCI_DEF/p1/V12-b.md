# V12 片3-4 · P1（门禁双实现与常量复制无锁）

### V12-N-08（P1·**门禁判定**）§10.5 冻结门有**两套实现、判据集合不同、10 秒边界开闭相反**；`90.0` 与 `0.70` **不在冻结文本内**
- **宿主**：C++ `cli/resource_gate.h:96 kWorkerP50Min=2.0`、`:97 kCpuP50MinPercent=**90.0**`、`:98 kCpuMeanMinPercent=85.0`、`:108 kMon001UtilSampleMinPercent=85.0`、`:109 kMon001UtilSampleFrac=**0.70**`、`:110 kMon001QueueWindowSeconds=10.0`、`:111 kMon001QueueUtilMinPercent=60.0`、`:184 kMon002MinWindowSeconds=10.0`；Python `tools/monitoring/run_monitored.py:451-456`（`FROZEN_GATE_MIN_EFFECTIVE_CPUS=2`/`MIN_INTERVAL_SECONDS=10.0`/`MIN_AVG_UTILIZATION=0.85`/`WINDOW_SECONDS=10.0`/`WINDOW_MIN_UTILIZATION=0.60`/`MIN_ACTIVE_THREADS=2`）；文本＝宪章 §10.5 + §18.2 裁决 2 + `docs/owner/PIPELINE_OVERVIEW.md:133`。⇒ **权威点数：2 套代码 + 1 文本，三者无生成关系**。
- **冻结文本原文（V12 读 `:375`）只给四条**：有效 CPU≥2、计算区间「**超过** 10 秒」、平均≥已分配容量 85%、任何连续 10 秒 <60% 或单活跃线程 FAIL。`grep -c "90%" ASTROCS_PROJECT_CONSTITUTION.md` → **0**；`docs/contracts`、`docs/owner`、`docs/science`、`ci/` 对 `90.0`/`0.70` 亦 **0 命中**（出处只在控制包 `04_TASK_SPECIFICATIONS`）⇒ **两个非冻结值被当成冻结门在用**。
- **(a) 可复算反例（同一 run 结论相反）**：均值 86%、p50=88%、达标样本占比 0.65、wall=12s ⇒ C++ `evaluate_gate` 返回 `CpuP50Low`/`UtilizationP75Low`（**FAIL**），而 Python `evaluate_frozen_gate` 只看均值 ⇒ **PASS**。
- **(b) 名值不符**：枚举 `UtilizationP75Low` 与进 verdict JSON 的码 `"utilization_p75_low"`（`:82`）对应的实现是「≥**70%** 样本 ≥0.85」（`:109`/`:342`），P75 应为 75%；该串被 `tests/unit/mon001_gate_test.cpp:179-180` **钉成合同字面量**、并被 `ci/ci_repair_round.py:110` 用于**失败归因** ⇒ 名字错、值也错、还被下游当分类依据。
- **(c) 边界开闭相反**：Python `:515 interval <= 10.0 → not_applicable`（严格大于才判，**合宪章文本**）；C++ `:186-187 active_window_seconds >= 10.0 才判` ⇒ **wall 恰 10.000s 时 Python 必 NOT_APPLICABLE、C++ 可 FAIL**。
- **处置**：①`90.0`/`0.70` 要么删、要么按 §1.2 流程走宪章修订并登记 `DISP-*`；②两实现合并为**一份常量源 + 生成关系**（Python 侧读同一份 CSV/头），或明确"仅 C++ 是门、Python 只是监控"并改其退出语义；③边界统一为宪章的"超过"；④枚举与 JSON 码名同步修正（连带 `mon001_gate_test.cpp:179-180` 与 `ci_repair_round.py:110`）。
- **related** **`A-39`**（`allocated_capacity_cores` 取义，同门另一面）、`A-43`（0.75/0.50 文案滞留）、`M5a-G-002`、`V4-N-01`（该门口径收紧致 backend 疑红）、`_merge/00_COORDINATION.md:57-60`、`V12-N-09`
# V12 片3-4 · P1 续（常量复制无锁）

### V12-N-11（P1）`200000` 一具名（`.c` 私有）+ **三处独立复制**；缓存预算乘积在两个 TU 各算一遍
- 宿主：`gaia_client.c:31 #define MAX_STARS_RESULT 200000`（**不在任何头**，12 处引用 `:1482/:1551/:1611/:1680/:1751/:1819` 等）；`ipv_types.h:244 m_lim_gaia_cap_per_file = 200000.0`（`:241` 注释称"该常量由 P1 持有，本模块只读"，**实为抄值**）；`module_entry.c:546 const long long kQueryCacheCap = 64LL*200000LL*(sizeof(double)*3)`；派生宏 `gaia_client.c:128-129 QUERY_CACHE_MAX_BYTES`；注释 `gaia_client.h:3`「= 307,200,000 B（模块 plan 合同值 kQueryCacheCap）」；文档 `GAIA_QUERY.md:147/:222`、`PUBLIC_API.md:93`、模块 `README.md:50/:144`、`memory.md:36`。**权威点数：1 具名 + 3 复制**。
- 复算：`64×200000×24 = 307,200,000 B = 293.05 MiB`（两条算式今日同值 0 ulp）。**若 `MAX_STARS_RESULT→150000`**：`QUERY_CACHE_MAX_BYTES` 自动变 230,400,000，而 plan 合同值仍 307,200,000（**高估 33%**）；且 plate_solve 的 **`fmod(N,200000)==0` 触顶检测永不成立** ⇒ 被截断的 `N` 继续进 alpha 差分与达标判据 ⇒ **科学有偏且静默**。
- 无锁证据：`git grep m_lim_gaia_cap_per_file|MAX_STARS_RESULT -- tests/*` → 只有 `test_mag_iter.cpp:161 s.cap_value=200000.0`（**stub 自造，不引用生产宏**）与 `:166` 钉 `n_returned==200000`；`git grep 307200000|QUERY_CACHE_MAX_BYTES|kQueryCacheCap -- tests/* docs/*` → **0 命中**。文档依据列「源码 27, 1146, 1215」**现值 31/1482/1611，三锚全漂**。
- 处置：常量入公共头一处具名（含 `static_assert` 与文档同步）、`kQueryCacheCap` 与派生预算由**同一表达式**生成、`m_lim_gaia_cap_per_file` 默认值引用该宏而非抄值；触顶检测改为**服务端返回的截断标志**而非 `fmod`（连带 `V2-N-10`）。
- **related** **`V2-N-10`（D-7 `fmod` 触顶语义）**、`M6b-E-002`、`M7-T-102`、`V12-N-07`、`C-18`
