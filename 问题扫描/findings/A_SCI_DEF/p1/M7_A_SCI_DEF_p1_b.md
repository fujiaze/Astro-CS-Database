# M7 · A_SCI_DEF · P1（第二层合并定稿：L19+L20+L21；b 档，条目 M7-A-118 ～ M7-A-139）

> **档位声明**：本文件内全部条目的 **类别与优先级由本行标题承载**（协议 §3 的「一类别×优先级一档」）；条目正文只在**偏离本档级别**时显式标注改档及理由（如 M7-A-101 记 P0→P1、M7-A-201 记 P1→P2 并撤核心结论、M7-A-001 记 P1→P0、M7-I-202 记 P1→P2 且改类 A_SCI_DEF→I_DOC_HYGIENE）。逐条四态判定与编号映射见 `问题扫描/_merge/M7.md` §2；每条均含 位置/权威依据/证据或证据出处/问题说明/影响/建议处置/置信度/related/四态判定 九项。

口径同 a 档：锚为 `path::符号`，行号注「复核时 N」；证据逐字原文见 `问题扫描/_cache/L19.md`／`L20.md`／`L21.md` 对应条，本档不重复粘贴长引文。

## M7-A-118 SCI-PSF 的 FWHM 冻结常数 1.230310 与解析值 1.2303076 在第 6 位分歧，三条合同语句不能同真（L19-025）
- 位置 `docs/science/PSF.md::§2/§5/§7/§10/§11`（复核时 :20/:51/:63/:86/:118，grep 1.230310 五处命中）
- 问题说明 本代理独立复算半高条件 (1+Q)^4=2 ⇒ Q=2^{1/4}−1=0.1892071，各向同性 Q=r²/2σ² ⇒ FWHM/σ=√(8·0.1892071)=**1.2303076**；§7 称比值「恒为 1.230310」、§11 要求「解析一致性 max_abs==0」、§10 禁止改常数 ⇒ 三语句在数学上不可同真（位精确门只能靠把解析侧也硬编码同一错值来「过」）。数值影响 2e-6·px 可忽略，毁的是该 Oracle 的可证伪性。
- 处置 常数订正为 1.2303077 或改为公式定义（§10 自锁 ⇒ 交负责人）。
- related M7-G-001（不可达门家族）、M7-A-130（L20-010 FWHM=1.230310·sx/sy 量纲错式）、L19-005、L04-012；判定 **仍成立**；置信度 高

## M7-A-119 星点通量维度链 PSI→PHOT 三套标签互斥（ADU·px² / ADU·px / ADU），px² 在 ∫I dA=Σpixel 处被默默抵消（L19-026）
- 位置 `docs/science/PSF.md::§2 flux 行/§3/§5`（复核时 :21/:28）、`docs/science/PHOTOMETRY.md::§2 F_instr 行/§3`（:14/:29）
- 问题说明 模型 I 在 §3 标 ADU（每像素计数），则 ∫I d²x 数值等于 Σ_pixel I，单位应为 ADU；§5 积分式写成 ADU·px² 是把 px² 当量纲保留；SCI-PHOT 再给第三标签 ADU·px，且换算因子全文隐含。
- 处置 在 DATA_SEMANTICS 钉死「星点流量=ADU（每像素 counts 求和）」并两文档同文订正。
- related L04-010（同型 ADU/面亮度混写，M2a 已定稿）、L07-007、L21-E5（附录 A 扩展实例）、M7-A-120；判定 **仍成立**；置信度 高

## M7-A-120 GLOSSARY 自称唯一术语权威，却缺宪章 §4.1 十量中 6/10 条（L21-002）
- 位置 `docs/GLOSSARY.md::表体`（18 行）、`ASTROCS_PROJECT_CONSTITUTION.md::§4.1 表`
- 问题说明 snr/quality/coverage/validity/rejection_mask/provenance 六条无词条；coverage 被反向登记为 support 的 legacy alias 而活使用域仍在（附录 A 第 7 行）⇒ 「恰一个含义」在词典层不可判定，正是 SNR/quality/coverage 多口径并存的辞典出口缺口。
- 处置 按 §4.1 逐行补条（snr 须先由负责人裁决唯一定义=A-01）；每条给权威锚点+单位+有效域。
- related L07-003、L18-017、M7-A-121、M7-A-001；判定 **仍成立**；置信度 高

## M7-A-121 「quality」在两份 FROZEN SCI 中类型互斥（[0,1] 浮点因子 vs uint32 位掩码），flags→因子映射无定义（L21-005）
- 位置 `docs/science/PHASE2_UPM.md::§3/§5`、`docs/science/CONTROL_WEIGHT_SNR.md::§/quality 行`（grep uint32 命中 :25/:32）
- 问题说明 `quality_factor(quality_flags, mode)` 的映射函数全文无定义，而 §5 的唯一冻结式把它当无量纲 [0,1] 因子 ⇒ 同名量在两文档中是两种类型、其乘积进入唯一冻结式（与 M7-A-001 同族但不同事实）。
- 处置 给 flags→[0,1] 映射表并声明 mode 的合法域；GLOSSARY 补 quality 条。
- related L07-004、A-02、M7-A-001、M7-A-120；判定 **仍成立**；置信度 高

## M7-A-122 NOISE_MODEL 符号表给 ivar 记「pixel⁻²·ADU⁻²」，同篇 §3 与全部合同记 ADU⁻²（L21-006）
- 位置 `docs/science/NOISE_MODEL.md::§2 符号表`（:16）对照 `::§3`（:29）、`docs/contracts/DATA_SEMANTICS.md::§13.2/§21.1`
- 问题说明 两读法相差像元面积因子的平方，且被用于 ivar 加权的方差传播；属「单位被默默抵消」形态。
- 处置 删 pixel⁻²；若确有像元面积归一，须在 §5 的 ivar 式中显式写出。
- related L04-010、L15-002、M7-A-119；判定 **仍成立**；置信度 高

## M7-A-123 交换合同要求三 Phase 最小平面集含「mask」，而 mask 在三个 DATA 节都被判「非产品输出」且语义三方不一（L21-007）
- 位置 `docs/contracts/DATA_SEMANTICS.md::§mask/rejection 相关节`（复核时 :174/:214/:314-315）、`docs/interfaces/data/DATA-002`、`docs/science/PHASE3_HIPS_TO_FITS.md::§7`
- 问题说明 同一 plane 名在 P1/P2/P3 三处分别是「拒绝掩膜/有效域/覆盖」，而交换校验器要求它存在 ⇒ 要么校验器验的是一个语义未定义的平面，要么三处有一处不该要求（**属运行时行为，需校验器读码定强弱**：已列待会签 M2b）。
- 处置 先定 mask 的权威定义域，再定平面集是否要求；本条按前台「同法自查：凡涉运行时行为必须找实际校验点」的指令保留 P1 并挂校验点待核。
- related L18-017、L21-009（前台已降级，见 `findings/I_DOC_HYGIENE/p2`）、M2b；判定 **仍成立（文档面）/无法判定（运行时阻断面）**；置信度 中

## M7-A-124 GLOSSARY variance/ivar 条目未随 DATA-UNC-001 双值消解同步，仍写单值口径（L21-010）
- 位置 `docs/GLOSSARY.md::variance/ivar 行`（:10/:11）对照 `docs/contracts/DATA_SEMANTICS.md::§4a/§30`
- 问题说明 唯一词典落后于已生效的双值（模型方差/经验方差）口径 ⇒ 「不得混淆」失效。
- 处置 同步两值定义并给各自权威锚点。
- related F-UNC-001、M7-A-120、M7-A-125；判定 **仍成立**；置信度 高

## M7-A-125 ALG 层把 ivar 缺失回退 support 写成 weight_mode=2 的唯一语义（L21-001 的 ALG 侧独立事实）
- 见 `findings/A_SCI_DEF/p0/M7_A_SCI_DEF_p0.md::M7-A-002`（本代理判其与 M3-A-002 的「两 SCI 互斥」是不同事实面，ALG 越权登记面单独定 P1）
- 判定 **仍成立**

## M7-A-126 ALG 中的 σ 未逐法定义：linear_fit 以平均绝对残差充当 σ 且未乘 √(π/2)（L20-016）
- 位置 `docs/algorithms/REJECTION_ALGORITHMS.md::§/linear_fit 块`（:14/:17/:98/:106）
- 问题说明 本代理独立复算 √(π/2)=1.2533 ⇒ 名义 3σ 阈实为 2.39σ，拒收率随法漂移；与「SNR 无定义」族（L07-003）同根式。
- 处置 逐法给 σ 的闭式（含折算因子）并声明与 SCI 阈的对应。
- related L07-003、L09-004、M7-A-108；判定 **仍成立**；置信度 高

## M7-A-127 Generalized ESD 的 α=0.05 被当冻结统计判据引用，却未纳入 NIST 自述的近似有效性域（n≥15/25）（L20-017）
- 位置 `docs/algorithms/REJECTION_ALGORITHMS.md::§/ESD 块`（:18/:37/:57/:61）
- 问题说明 λ_i 与 p 与 NIST 原文逐项一致（本代理未复算 λ_i，按 L20 记录接受），但有效性域条件被丢；mosaic 像素栈 n 的实际分布决定违规比例 ⇒ 已列 R 层待执行。
- 处置 在条款内写 n≥15 前置并显式给 n<15 的降级路径；出处改 `path::节 + 标准名`（附录 A 口径矩阵保留其出处行，见 `_merge/M7.md`）。
- related M7-A-126、M7-A-108、R 层；判定 **仍成立（文档面）/无法判定（发生率）**；置信度 中高

## M7-A-128 冻结完备性门「SNR≥10 召回≥99%」在本文阈值统计下数学不可达（L20-008 的 SCI/ALG 侧定档；家族成员）
- 见 `findings/F_TEST_GAP/p1/M7_F_TEST_GAP_p1.md::M7-T-101`（本代理按协议把「判决域与噪声域不同源」这一根因归 A_SCI_DEF 侧讨论，测试面归 T-101，两处交叉 related，不重复计条数——计数以 T-101 为准）
- 判定 **仍成立**

## M7-A-129 bilinear「Σw=1 精确成立」在跳象限、FP64 舍入、bitpix=−64 三处不成立，而它是「常数场 max_abs=0」的唯一依据（L20-007）
- 位置 `docs/algorithms/PHASE3_RSMP_IMPL.md::§6.5 p3_sample_bilinear`（:193 附近）、`::§6.6 值语义表`；`docs/algorithms/PHASE3_RESAMPLE.md::§5c/§8/§9`；判据支撑 `docs/science/PHASE3_HIPS_TO_FITS.md::§9a-7`
- 问题说明 三条独立破口按本代理分级：①**跳象限未重归一 = 真实缺陷，成立**（只累加保留象限 ⇒ Σ(kept w)<1，常数场在该分支输出 (Σkept w)·C，把「面积欠覆盖」实现成「能级欠定」，正是 §9a-7 要防的事；且文档未声明是否重归一）；②**FP64 四项和逐位≠1 = 成立但仅表述级**（IEEE 标准结论；被 float32 输出舍入掩盖，故不改数值，只毁「精确成立」这一措辞）；③**bitpix=−64 路径 = 依赖 L20-025 的前件**：上游恒 float32（L20-025 定稿），故 −64 不增有效位，②的残差在 −64 路径下**并不会真的可见** ⇒ ③从「独立破口」降为「若将来接入真 FP64 源则成立」的潜在项。综合定档：**P1，主事实是①**；②③改述为「容差口径缺陷」并入 M7-F-201（L20-026 构造保证非逐位）族。
- 影响 覆盖率边界/背面退化处的逐像素能级（①）；Phase3 唯一能量语义门的可证伪性（②③）。
- 处置 明确跳象限时按 Σkept w 归一并写入 SCI 口径；「Σw=1 精确成立」改记「±k·ULP，不作逐位断言」；「常数场 max_abs=0」改成分 dtype 的相对容差；声明双线性累加 dtype（现 `nanf` 与 FP64 权重混用）。
- related L02-007/L02-008、L04-010、M7-F-201、M7-C-201（bitpix=−64，L20-025）、L19-018、SCI-P3 §9a-7/§9a-12、宪章 §7.3:224；判定 **仍成立（①）＋部分成立（③，前件被 L20-025 改写）**；置信度 高
## M7-A-130 FWHM=1.230310·sx/sy 是量纲错误式（乘/除轴向宽度），自反于本文 F2 与 SCI-PSF 不变量（L20-010）
- 位置 `docs/algorithms/STAR_PSF_ALGORITHMS.md::§/FWHM 行`（复核时 :7/:54/:75/:151）；实现侧 `dpsf_psf.cpp` 的 FWHM 计算（L20 §4-6 记录为「乘法」）
- 问题说明 各向异性下 FWHM 是方向泛函，单一标量式必给 sx/sy 各一个 FWHM；「×sy」读法使 FWHM 随 sy 增大而增大而随 sx 增大而减小，物理不对称；且与 SCI 的 FWHM/σ 比值不变量（M7-A-118）在代数上不可同时成立。
- 处置 改 `fwhm_x=1.230310·sx`、`fwhm_y=1.230310·sy` 两式，并删「×sy」读法；同步 DATA 合同侧引用。
- related M7-A-118、L06-001、L06-005；判定 **仍成立**；置信度 高

## M7-A-131 MAD=0 时热/冷检测阈值退化为「高于中位即热、低于中位即冷」，文档两次判为良性（L20-011）
- 位置 `docs/algorithms/CALIBRATION_ALGORITHMS.md::§/MAD 分支`（:177/:181/:403）
- 问题说明 MAD=0 ⇒ σ=0 ⇒ 阈≡中位，任何正偏差成热、负偏差成冷；在常量母版/大欠曝区（M3 真实数据证据：master 出现 ≥50% 同值像素）必然发生，与 §「良性/无影响」声明互斥；属「绝对阈不随电平定标」同族（L07-010）。
- 处置 MAD=0 时该像素降级为「不可判」并计数暴露，禁按 ±0 阈分类。
- related M7-H-104（同族的负方差地板）、L08-008、M3 的 ≥50% 同值证据（转 M3/负责人）、宪章 §13.1；判定 **仍成立**；置信度 高

## M7-A-134 ALG 对同一代码给出互斥的冷/热分支符号描述（L20-012）
- 位置 `docs/algorithms/CALIBRATION_ALGORITHMS.md::§/冷像素块` 对照 `docs/algorithms/COSMETIC_ALGORITHMS.md::§/冷分支`（:33/:36/:47/:135）
- 问题说明 冷分支复用热分支的 med/σ 符号 ⇒ 两篇文档对同一实现给出互斥判据（一侧必错）；属「同域两文档对同一代码」的镜像型割裂。
- 处置 以代码符号为准回写其中一篇（**代码行随 RESCUE-V3 漂移，本条锚用符号，不写行号**）。
- related L05-003、L14-002（注释镜像族，M6b）、L02-006；判定 **无法判定（哪侧为真需读码，本代理未定点读该两文件）**；置信度 中

## M7-A-135 peaker 在非极大值分支固定 x+=5 跳步可漏检；§3 与 §6 对跳步时机描述互斥（L20-009）
- 位置 `docs/algorithms/STAR_DETECTION_ALGORITHMS.md::§3 阶段 4`（:93）、`::§6`（:148）
- 问题说明 两处在文档中互斥（本代理 grep 双命中，文本未变）；按 §3 字面实现在数学上不封闭（单调上升后 +3 达峰会被跳过）⇒ 系统性漏检，与 §11.5 的完备性验收面冲突。R2 待复核项（以实现为准取哪侧）本代理**未读 sdet_api.cpp 主循环**，故运行时侧判「无法判定」。
- 处置 以 §3 或 §6 之一为准并写明跳步不变量（跳到已知更亮点的列）＋补双峰可分性性质测试。
- related L06-016（检测阈三套）、M7-T-101；判定 **仍成立（文档互斥）／无法判定（实现取哪侧）**；置信度 中高

## M7-A-136 PERCENTILE 方法既无分位数也不含噪声尺度，拒绝强度随背景绝对电平线性变化；「全接受」兜底是其症状（L20-015）
- 位置 `docs/algorithms/PHASE2_REJECTION.md::§/percentile 块`（:14/:99）、`docs/algorithms/REJECTION_ALGORITHMS.md::§/F14`
- 问题说明 阈按像元值的绝对分位取 ⇒ 背景电平抬升即改变拒收强度，与 SCI-REJ 的方法语义无关化。
- 处置 分位改为对「标准化残差」取；F14 兜底改为显式状态。
- related M7-A-108、M7-A-110、L09-004、L09-007；判定 **仍成立**；置信度 高

## M7-A-137 「CAR 无投影奇点」为假（极点整行塌缩未排除）；§15.3 中 θ 在两族投影间承担两个互斥几何量（L20-023）
- 位置 `docs/algorithms/PHASE3_PROJ_IMPL.md::§/CAR 行与 §15.3`（复核时 grep CAR 命中 :20/:107/:190/:348）
- 问题说明 本代理按标准式独立复核：AIT 前向 `ρ=√(2(1−sinφ₀))`、`λ=λ₀+π/ρ` 与给定逆变换精确互逆、`λ→0` 极限成立（即「ρ→0 单点分支」为真，L20 §5-A 记录一致），而 CAR 在 φ=±90° 整行塌缩为一点被写成「无奇点」；AIT 的退化点被文档写成 (φ=0/2,λ=0)，实为北极 (φ=π/2)。⇒ 有效域/奇点维在两处均不闭合（与 M1a-A-003 的 CRVAL2 事实不同，勿并）。
- 处置 补「极点整行 NaN」排除；AIT 退化点订正为北极；θ 分符号（θ_colat / φ_lat）。
- related L02-006（|dec|≤85° 与四角同半球守卫两侧互斥，本代理按 L20 §4-2 确认）、A-07/A-08、M7-A-117；判定 **仍成立**；置信度 高

## M7-A-138 HEALPIX_MAPPING 仅 35 行却承担 ALG-HEALPIX-* 全域：无公式、无单位/坐标系、无无效值语义（L20-021）
- 位置 `docs/algorithms/HEALPIX_MAPPING.md::全篇`（:23「单调」等）
- 问题说明 该篇是注册表 D.healpix 五行的证据指针（STANDARDS_REGISTRY :133-:137 的证据列点名本文），但其内容不含任何可判公式 ⇒ 域的 CONFORMANT 判定无 ALG 落点（与「判定式 ⊂ 条款面」家族并案，见 `_merge/M7.md` §横向核）。
- 处置 补 ang2nest/nest2ang 闭式、面号/朝向、order 上界推导、无效值语义。
- related M7-G-001、M3-G-002（registry 判定式家族，主落）、L03-004；判定 **仍成立**；置信度 高

## M7-A-139 Phase3 重采样按全图平面 + 逐 HDU 全帧临时缓冲分配（L20-022）
- 位置 `docs/algorithms/PHASE3_RESAMPLE.md::§10/内存块`（:32 等）
- 问题说明 宪章 §5.3/§7.3 禁止无界整图中间缓存；文档自述的分配面按「全图平面」计，且与 `L20-004 第 5 项`（coverage O(Σ|MOC_f|) 缓冲被写成 max+K）同型：资源门禁失去基准。
- 处置 改分块/流式并给上界；在 §10 登记绝对上限与显式拒绝。
- related L09-010、L08-012、M7-I-101（L20-004 复杂度面）、宪章 §17.6；判定 **仍成立**；置信度 中高（分配实现未读码）

