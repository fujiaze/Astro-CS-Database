# W2（第二轮 · 量纲与科学量一致性）片1-2 · P1 六条｜工作树 931ab42f（任务钉 a3a343a4，期间并行提交推进，锚已全量重锚）

### W2-N-01（P1）孔径通量**误差式与同域冻结 CCD 方程三方互斥** ⇒ σ_F 结构性低 12.86%、SNR 恒虚高
- 甲：`lib/phase1/photometry/photometer.cpp:92` 的 `flux_error = sqrt(max(sum,0)+n_in*sky_sigma²)`（`sum`=ADU 通量、`n_in`=整格计数像元数；`photometer.h:12-16` 声明 flux/flux_error 皆 ADU；**Photometer 全类无 gain 入参 ⇒ 隐式 gain≡1**）。乙：`lib/snr_estimator/cpp/src/snr_science.cpp:190,194-195` 的 `n_pix=πr²`、`var_ap=n_pix·σ_sky²·(1+n_pix/n_sky) + s_ap/gain`；`PHOTOMETRIC_FIT.md:134` 逐字记录甲式，而 `NOISE_MODEL.md:58` 记 var_ADU=max(signal,0)/gain+(rn/gain)²。
- **三处独立偏差**：①**缺 `(1+n_ap/n_sky)` 项** ⇒ r=4.0/天环6-10/sky=5 时甲 49σ² 对乙 62.41σ²，**σ_F 低 12.86% ⇒ SNR 恒虚高 12.86%，与 gain 无关（结构性缺项）**；②面积口径甲 49/50/52（随亚像素相位跳）对乙 50.27 ⇒ ±6% 量化跳变；③**泊松项 gain=2 高估 2×、gain=0.5 低估 2× ⇒ 符号随 gain 翻转，不可常数标定**。
- 判定：静态可判；生产 `measure_flux` 节点（`module_adapters.cpp:2507/2556`）在链上。related `M3-A-006`③（那条只登 1.4826 截断，未登这三缺项）、`V1-N-12`(a)(c) 不重报

### W2-N-02（P1）e⁻↔ADU 换算因子在读取边界被**实例化为 1.0 且无判别位** ⇒ 合同层「不可得」被实现成产品层「已知=1」
- 甲：`aio_fits.cpp:483` `cal.gain = kw_float(「GAIN」, 1.0)`，而 `kw_float:386-390` 对「缺键」与「解析失败」**同一出口**；`include/astro_image_io.h` 的 `AIOCalibrationMetadata` 对同批可选量**都有判别位**（`has_ccd_temp`/`has_cdelt1`/`has_cdelt2`/`has_equinox`/`has_lonpole`）**唯独没有 `has_gain`** ⇒「增益未知」**在类型上不可表达**。
- **后果链（伪值回写成产品事实）**：`orchestrator.cpp:4247-4248` 的 `if (gain > 0)` **恒真** ⇒ 伪 1.0 回写帧头 GAIN，`:4719` 再读回 `ncfg.gain_e_per_adu`，经 `drizzle_engine.cpp:1147`→`hiss_common.cpp:194` 以「已测仪器常数」交付进 HISS 元数据。**`READNOI` 全仓零写入点**（仅 `:4720` 读取）⇒ A 线读噪恒 0。
- 乙（双通道分叉）：`module_adapters.cpp:2667` `gain_e_per_adu = sc.value(「gain_e_per_adu」, 0.0)`，而 `snr_science.cpp:166`/`snr_frame_science.h:43` 明写 **<=0 = 未知** ⇒ **同一帧两条入口 gain 不同（1.0 已知 对 0 未知）**。权威面 `PHOTOMETRY.md:14`/`:29`（负责人已授权）称「e⁻ 需 gain，当前不可得」。
- 复算：真值 g 下方差错 g²、σ 错 g、**星等偏 2.5log10(g)**（g=2 ⇒ σ_F×2、m 偏 0.752 mag）。related `V10-N-04`（本条增量＝判别位缺失＋恒真回写＋双通道分叉＋与已授权面冲突）、`C-20`

### W2-N-03（P1）`DATA-P1-FLUX` 端口**声明 ELECTRON 而载荷为 ADU**，而单位门因**两端同标恒不红（自我印证）**
- 甲：`module_adapters.cpp:786` 与 `:804` 把生产测光输出口与 noise-snr 输入口**同标** `UnitId::ELECTRON`（同文件其余 **26 个端口一律 ADU**）。乙：registry 侧 `astrocs.phase1.photometry.md:43` 已订正为 ADU（内嵌 P5-SNR 订正注释、负责人授权），而 `astrocs.phase1.noise-snr.md:33` **仍 ELECTRON**；生产者 `photometer.h:15-16` 声明 ADU。
- **门失效机理（这是本条的价值）**：`pipeline.cpp:247-298` 只在 `prod_port.unit != p.unit` 且双方非 UNKNOWN 时触发 ⇒ **两侧同标时门恒不红** ⇒ 标签经 `artifact.cpp:101 unit_name()` 进 manifest、`artifact_store.cpp:35` 进 JSON **外销**。复算：信任 ELECTRON 的下游做换算即错 gain 倍、泊松方差错 gain²（g=2 ⇒ 方差×4、σ_F×2、m 偏 0.752 mag）。
- 判定：静态可判；属**负责人已裁口径未传导**。related `V1-N-05`（本条增量＝门恒不判别的结构成因＋外销＋数值倍率）、`M7-A-119`、`M8a-G-007`、簇 1 机制③

### W2-N-04（P1）`PHOTAPPL=1` **无条件硬编码** ⇒ 退化（恒等拷贝未定标）帧被声明为已应用测光，违冻结合同明文「禁硬编码」
- 甲：`orchestrator.cpp:3007-3008` 在 `ret==0` 分支出口同时写 `PHOTSCAL=out_scale` 与 `PHOTAPPL=「1」`——**既不 gate `out_n_matched>0` 也不 gate `out_scale!=1.0`**。乙（退化侧）：`pc_api.cpp:84-92`/`:97-105`/`:238-245` 三处（无 Gaia 星、无 PSF 星、星数不足）`*out_scale_factor=1.0; *out_n_matched=0;` 恒等拷贝像素后 `return 0`。
- 后果侧：`drizzle_engine.cpp:2258-2262` 据 `photometry_applied_upstream` 置 `photappl` 并切 `bunit=ASTROCS_RELATIVE_FLUX`；守卫不全：`hiss_writer.cpp:322-331` 只拒「RELATIVE_FLUX 且 PHOTAPPL=FALSE」（`return -2`），**反例 PHOTAPPL=TRUE + BUNIT=ADU 仅在 `:340` WARN**。
- **冻结合同逐字禁止**：`DATA_SEMANTICS.md:249`「PHOTAPPL=1 仅当 provenance 声明已应用（**禁硬编码**）」、`:497`「已定标 = scale≠1 且 n_matched>0…禁止在已定标帧标 BUNIT=ADU」。复算：恒等拷贝帧被标相对流量刻度 ⇒ **绝对通量错整个仪器零点 2.5·|location| mag**（location=2/4/6 dex → 5/10/15 mag，通量 ×1e2/1e4/1e6）。
- 判定：写侧无判据静态可判；命中取决于该帧是否退化（运行期）⇒ **定 P1 不定 P0**。related `M3-E-001`、`M3b-A-07`、`W4-R2-04`（同族「完成标记」）

### W2-N-05（P1）`photscal` **只乘像素、不乘 variance/ivar**；唯一 α² 实现**生产零调用** ⇒ 跨帧 ivar 求和失同单位前提
- 甲：`image_corrector.cpp:76` `out_pixels[i]=pixels[i]*(float)scale`（f64 内联同式 `pc_api.cpp:536`/`:779`/`:1099`），而方差块描述仍写旧单位（`orchestrator.cpp:4758`/`:4777`「variance (ADU², blank-sky)」）。乙：`noise_model.cpp:456-463` 的 `snr_noise_scale_law`（`var'=α²var`、`ivar'=ivar/α²`）**生产链零调用**——全量命中仅公共头、实现、测试与 `module_entry.cpp` 的 `subop==「scale_law」` 诊断分支；而求和点 `module_adapters.cpp:4490` 直接 `w=ivar_v[d][p]`（`DATA_SEMANTICS.md:2311` 的 `ivar_mosaic=Σ ivar_i` **隐含同单位前提**）。
- 复算：photscal 取 1.5/2/3 ⇒ 正确权重比应为 0.4444/0.2500/0.1111，现按等权比 1 计入 ⇒ **被高标定帧分别少计 55.6%/75.0%/88.9%**；且同帧并存两标度（`psf_flux` 取缩放前阶段 `orchestrator.cpp:2656`，`sigma_sky` 取缩放后）⇒ **相差 photscal 倍**。
- 权威：`NOISE_MODEL.md:12` 与 `DRIZZLE.md:57` 均规定 `x'=αx → var'=α²var`。related `M3-H-002`（内部 var/ivar 不对称，另一事实）、`M2a-A-2`、`W3-R2-012`（同族：算了没人用）

### W2-N-06（P1）方差面 NaN 像素「**半累加**」：面积进分母、方差不进分子 ⇒ 交付方差系统性偏低
- 甲：`drizzle_engine.cpp:1912` 的 `if (varianceValue <= 0.0f) continue;`——**NaN 不满足 <=0**，且注释 `:1893-1895` 自认「NaN…自然落入传播路径」；`:1553-1559` 中 `sumFlux`/`sumArea` 无条件累加、`sumVarNum` 仅当 `varianceValue > 0.0f` 才加 ⇒ **同一像元对 F_p/D_p 有贡献、对方差分子无贡献，而 `var=sumVarNum/D_p²` 的分母仍含其面积**。
- 乙：`DATA_SEMANTICS.md:254` 明文要求 variance 面「**非有限或 ≤0 → 跳过该像素**」（原锚 `:1727-1729` 已漂移，现读 `:1912`/`:1557`）。同链另一站点门不同集：`aio_hips_writer.cpp:569` 的 signal 门含 `isfinite(flux)`，而 `:721` 的 variance 门**无 flux 条件** ⇒ **可交付 signal=NaN 而 ivar=有限**，违反 `:2336`「signal=NaN 同态」。
- 复算：一滴水跨两像元 a1=0.6A(v=100)、a2=0.4A(v=NaN) ⇒ 代码 var=36/A²，契约（整颗剔除、D=0.6A）var=100/A² ⇒ **低估 2.78×**。related `M2a-H-2`（同行另一分支）、`M2a-C-3`、`V7` 全族不重报
