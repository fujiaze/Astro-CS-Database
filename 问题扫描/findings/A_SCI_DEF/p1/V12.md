# V12 · 常数单一权威点（P1 四条，时点见 V12.md §0）

### V12-N-01（P1·**总述根因条**）科学常数**无承载层**：`include/` 下无任何 constants 头，权威点数 = 0
- 宿主实测：15 位 `1.482602218505602` 在 tracked 生产面 **15 文件 24 处**——`noise_model.h:15 kMadToSigma` 是**全仓唯一具名点**，而其消费者只有 `noise_model.cpp:189` **一处**（`snr noise_model.cpp:61`、`sdet_api.cpp:227`、`sdet_image.cpp:495(f)/524` 等**全是裸字面量**）；4 位 `1.4826` 达 **66 文件 99 处**（含 `photometer.cpp:90`、`phase1/stars/star_detector.cpp:51`、`providers/cpu/baseline/src/baseline_provider.cpp:320/335`、`backend_host/baseline_kernels_impl.inc:129/137`、`frame_qc_grid.py:35/224/500/592`）；倒数式 `0.6745` 生产 17 处 / 文档 27 处。
- **权威点数 = 0**：三级复核 `git ls-files | grep -iE "constant|sci_const|numeric"` ⇒ 生产/合同/文档层 **0 命中**（`include/` 下没有 constants 头）。⇒ 这不是"某处写错"，是**架构上无处可写对**。
- **文档自述不实**：`NOISE_MODEL.md:91` 称与 `1.4826022185` 差 `<1e-12`，V12 复算绝对差 **5.602e-12** ⇒ 不成立。可复算数值差：`1.4826` rel **−1.496359e-06**；FP32 `1.4826f`=`1.4825999736785889` rel −1.514e-06。出处/DISP：**无**。钉死：`p1star_mad_check.cpp:43/44/84` **只钉字符串与"两常数不相等"，不钉生产值**。
- **处置（本条一次性收敛 `A-12`/`M3-A-008`）**：新建 `include/astrocs/science/constants.h`（`inline constexpr double`，每个常数带 `static_assert` 精度界 + 出处注释 + `DISP-*`），把上述 24+99+17 处**全部替换为具名常数**；`kMadToSigma` 现成，缺的是消费者。**这条做完，`A-12` 与 `M3-A-008` 可一并关闭**（我已把两者挂 related）。
- related：**A-12**、`M3-A-008`、`M7-G-103`、`V6-N-08c`、**V12-N-02/03/04**

### V12-N-03（P1·科学口径为假且落在交付量上）`NOISE_MODEL.md:135` 声明「0.6745 与 1.482602218505602 同源」——**倒数式是三套中偏差最大的，大 10.2 倍**
- 宿主：`docs/science/NOISE_MODEL.md:135`；实装 `lib/photometric_calib/cpp/src/star_matcher.cpp:21 _MAD_SCALE=0.6745`（`:539`/`:612` 用），产出 `sigma_residual` **进公共头** `photometric_calib.h:41`（注释「供 SNR 模块使用」，`:72`/`:96`/`:146` 三处重复同式）。权威点数：1（模块私有）。
- **复算**：`1/0.6745 = 1.4825796886582654`，对冻结值 rel **−1.519615e-05**；真值 `Φ⁻¹(3/4)=0.6744897501960817`，与 `0.6745` rel +1.52e-05；而 `1.4826` 只有 −1.496359e-06 ⇒ **被 `A-12` 追问的那个 1.4826 反而比 0.6745 准 10 倍**（与我先前给的 `V2-N-07(c)` 同数，互证）。
- **钉死失效**：`p1phot_oracle.hpp:68` 用**同一个** `0.6745` ⇒ **oracle 与被测共源，抓不到**（`V1-N-09` 同型）。本条不重报"三套并存"，只判「**同源陈述为假 + 落在交付面**」。
- related：**A-12**、`M3-A-008`、`M7-A-211`、`V2-N-07`、`V1-N-09`、`V12-N-01`

### V12-N-05（P1·公式双头 + **死码占合同位**）曝光→初始极限星等公式两份字面实现，"权威函数"零调用者，两路径对同一输入差 **4.000 mag**
- 宿主：`lib/plate_solve/cpp/ipv/src/ipv_select.cpp:342-344`（`compute_initial_mag_cut`：`m_cut = 6.0 + 1.5·log10(f_safe) + 2.0·log10(t_safe)`）与 `:408-409`（**同一式的内联第二份**，额外 `+ params.m_lim_m0_offset`）；`:394` `m0_hi = std::min(clamp_hi, 13.0)`；声明 `ipv_select.h:64-66`；**合同位** `docs/contracts/API_CONTRACTS.csv:369`（末列 **VERIFIED**）、清单 `api_inventory.csv:374`。权威点数：**0**（`6.0`/`1.5`/`2.0`/`13.0` 全裸；**`13.0` 在 `IPVSolverParams` 里没有字段**）。
- **死码证据**：`git grep compute_initial_mag_cut -- lib/ tests/ cli/ runtime/ modules/ providers/` → 只命中定义 `:336` 与声明 `h:64`，**零调用者**。复算 `f=1877mm`、`t=300s`：权威函数 **16.8642**、活路径 **12.8642** ⇒ **同一输入差 4.000 mag**。
- **自相矛盾（两处）**：`:372`/`:946` 注释自称「参数全部来自 `IPVSolverParams`（宪章 §10.4 禁硬编码）」而 `13.0` 无字段；`ipv_select.h:97` 写「clamp(…, clamp_lo, 13)」而 `:102` 写「`m` 全程夹取 `[clamp_lo, clamp_hi]`」⇒ 当 `clamp_lo>13` 时 `min(max(m,15),13)=13 < clamp_lo`，**可复算的初值越界**。docs 面 **0 登记**该式。钉死：无（`test_mag_iter.cpp` 只测割线迭代与参数可覆盖，**不比对两路径**）。
- related：**`V2-N-02`**（该死符号仍以 VERIFIED 占合同表 ⇒ 同一条门红）、`V2-D2`、`M1a-L01-012`、`M3-C-011`、`V12-N-06`

### V12-N-06（P1·依据虚构）6 处注释把「α=1.3→0.2885」的偏差登记指向**不存在的路径** ⇒ 「已登记故不动冻结文档」的理由不成立
- 宿主：`lib/plate_solve/cpp/ipv/include/ipv_types.h:225`；`ipv_select.cpp:364`/`:946`/`:1244`/`:1514`/`:1841`，句式「偏差登记：`docs/algorithms/IPV_PIPELINE.md` proposed patch / 已在该处走偏差登记，本次不改冻结科学文档」。
- **三级复核判缺失**：L1 `git grep -n docs/algorithms/IPV_PIPELINE.md -- lib/*` → 6 命中（全是这些注释自己）；L2 `git ls-files | grep -i ipv_pipeline` → 唯一 `lib/plate_solve/IPV_PIPELINE.md`（**不在 docs/**）；L3 `ls docs/algorithms/IPV_PIPELINE.md` → **No such file（exit 2）**；且 `grep -c DISP lib/plate_solve/IPV_PIPELINE.md` → **0**，该文件 `:113-123`/`:327` 仍以 `ρ(G)=5×10^(1.3(G−10))` 作现行第 11 步描述。
- **判新**：`V2 §195` 只报了「该文档仍写旧公式 α=1.3」；本条报的是**「被 6 处注释当登记锚的路径根本不存在」⇒ 以"已登记"为由不改冻结文档的论证前提失效**。影响：α 先验 `0.2885` 决定割线迭代步长→选星星等，其依据唯一在 `run/perf-fix/P4-magiter/REPORT.md`（gitignored，**`V6-N-04` 同族**）。
- related：**`V6-N-04`**（锚无宿主）、`V2 §195`、`M7-G-105`、`V12-N-05`、`C-18`（编号/锚生命周期）、`C-15`
