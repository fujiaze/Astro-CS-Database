# M9 定稿 · A_SCI_DEF · P1（来源切片 L26）

> 合并代理: M9 · 复核时点: 全部证据按**当前树**重新 read/grep 定位；位置一律 path::symbol +「复核时 N」。

## M9-A-1 「60″ 双路径切换阈值无数学依据」的缺陷已被本仓注释自证并在面积路径修复，但同一无依据阈值仍以 use_adaptive 判据存活于引擎——修复未固化（判据未收口、偏差未登记）

- 类别: A_SCI_DEF（标题义：缺陷已认定但修正口径未全量落地/文档未回写）
- 优先级: P1
- 来源: L26-004（复验成立；锚点全部**保留**，并按当前树补行号；本代理另补一处判据方向的语义疑点，见「问题说明」末）
- 位置: lib/healpix_db/healpix_drizzle/spherical_overlap.cpp（Eriksson 统一段注释，自证旧阈值无依据）、lib/healpix_db/healpix_drizzle/drizzle_engine.cpp::DrizzleRunContext::cos_thresh_60 + ::run_drizzle_internal（THRESH_60ARCSEC 与 use_adaptive 切换处）、docs/algorithms/DRIZZLE_GEOMETRY.md（阈值去向未记）
- 证据摘录（逐字，复核时点现文）:
  > 自证（spherical_overlap.cpp 头部注释）`// 3. 双路径切换阈值 60" 无数学依据, 1° 像素 (3600") 远超阈值走 Girard 失效` 与 `// 统一使用球面三角剖分 (fan triangulation) + Eriksson (2018) 稳定公式:`
  > 存活面（drizzle_engine.cpp:243）`double cos_thresh_60 = 0.0;   // cos(60 角秒)：边跨度自适应阈值`；（:1652）`const double THRESH_60ARCSEC = 60.0 * (M_PI / 180.0) / 3600.0;`；（:1656）`rctx.cos_thresh_60 = std::cos(THRESH_60ARCSEC);`；（:1362/:1370）`bool use_adaptive = false;` / `if (d <= rctx.cos_thresh_60) use_adaptive = true;`，随后 :1373/:1396/:1421-1436 两条路径分岔（drop 角点提升 vs drop_corners_d）。
  > 文档面：DRIZZLE_GEOMETRY.md:72-74 只记 `Eriksson 扇形三角剖分` 与 DISP-DRZ-002（面积口径），:116-117 只有输入校验行；对全文 grep `60`/`use_adaptive`/`cos_thresh` → **无该阈值去向的任何陈述**（该文档 :231 另有 DISP-DRZ-005 记录 `max_angle < 1e-3` ≈ 206″ 的切平面分支，属另一阈值）。
  > 边界测试：对 tests 面 grep `59`/`61` 弧秒档或 THRESH_60 → 0 命中（无跨阈值一致性用例）。
- 权威依据: 宪章 §5.3（Drizzle 单位与误差传播必须明确）、§4.1；STD-REG 纪律（无科学依据的阈值须经 SCI 裁决或 DISP 登记）；宪章 §12.3（缺陷认定后须回写文档）。
- 问题说明: 同一 60″ 阈值，面积路径已按「无数学依据」自证重构为统一 Eriksson（DISP-DRZ-002 已回写），但引擎的快速/自适应**路径切换判据**仍是同一无依据阈值。单位换算本身无误（60·(π/180)/3600 = 2.909e-4 rad ✓，本代理独立复算）。科学判据面另有一处需负责人核的方向性疑点：代码注释把该量读作「边跨度**大于**阈值时走 adaptive」（:243「边跨度自适应阈值」），而实现是 `d <= cos_thresh_60 → use_adaptive = true`；若 `d` 是两点**夹角余弦**（越大越近），则该不等式实际选中的是跨度**不小于** 60″ 的 drop（与注释一致）；若 `d` 为其他量（如平方距离或法向点积的补），则不等式方向与注释相反——本条不臆断，标注为核证点。无论方向如何，"以 60″ 为界"本身无科学依据这一点已由本仓自己的注释承认，且旧论证（1° 像素即失效）未迁移进 ALG/SCI、无偏差编号、无 59″/61″ 边界测试。
- 影响: 边跨度≈60″ 的产品帧在两条路径间无科学理由跳变（两条路径的 drop 覆盖与权重展开不同 → 边缘像素 weight/flux 不连续）；后人据 spherical_overlap 的注释会误以为全链已消除该阈值；宪章 §5.3 的误差传播可解释性不成立。
- 建议处置: ①负责人裁决阈值去向：保留 → 挂 DISP 编号并在 SCI/ALG 给出依据（含 `d` 的确切定义与不等式方向的书面说明）；或并入统一路径（与面积路径同判据）；②DRIZZLE_GEOMETRY.md 的切换判据段补写实际判据与来源，禁只写"面积已统一"；③补边界一致性测试（59″/60″/61″ 三档同帧，断言两路径的 weight 与 flux 差在合同容差内）；④若裁决为"保留但无依据"，须按 §6.3 与 DISP-DRZ-002 同级登记，不得仅存代码注释。
- 置信度: 高（代码/文档/测试三面自证；`d` 语义方向为中，已标核证点）
- related: L04-012（同函数群 1.25/1.15 矛盾论证——不同事实）、DISP-DRZ-002（面积半边已闭）、L23-009（同族：测试把无依据系数钉成基线）、M9 §8（回归钉死错误口径族）

## M9-A-2 k_corr 标定表的像素尺度轴（300–600″）与生产源尺度（在册用例最大 3.22″）脱节约两个数量级：std::clamp 使"按帧尺度选择"恒饱和、静默且不可见

- 类别: A_SCI_DEF
- 优先级: P1
- 来源: L26-006（复验成立；锚点**保留**；本代理**订正一处**：标定域并非"文档未声明"——PHASE2_SAMPLER.md 已声明 [300,600]，缺的是"生产尺度恒在域外"这一后果的声明）
- 位置: lib/phase2/src/sampler.cpp::kcorr_lookup（含 scale 未知回退）、lib/healpix_db/healpix_drizzle/hp_drizzle_api.cpp（src_pixel_scale_arcsec 产出）、lib/astro_image_io/src/hips/aio_hips_writer.cpp::aio_hips_set_drizzle_provenance（透传与校验）、docs/algorithms/PHASE2_SAMPLER.md（标定段）
- 证据摘录（逐字，复核时点现文）:
  > （sampler.cpp:90）`static const double sc_grid[2] = {300.0, 600.0};`；（:95-96）`const double pf = std::clamp(pixfrac, 0.5, 1.0);` / `const double sc = std::clamp(scale_arcsec, 300.0, 600.0);`；（:554）`: kcorr_lookup(pf, 300.0);   // scale 未知：300" 档保守`
  > 上游贯通（hp_drizzle_api.cpp:1130-1133）`// （K_CORR_DOMAIN）：源帧像素角尺度（deg→arcsec）写入` / `meta.fits_meta["src_pixel_scale_arcsec"] = std::to_string(` / `    std::fabs(img.wcs.cd[0]) * 3600.0);`，消费侧 astro_sphere_sink.cpp:68 `meta.fits_meta.find("src_pixel_scale_arcsec")` → properties → sampler；声明面 aio_hips.h:238-241「Phase2 sampler 按帧读取以选择 control-ivar 的 k_corr 标定值」；写出面（aio_hips_writer.cpp:1276-1285）`if (scale_arcsec < 0.0) return 2;` → `ps->drizzle_scale_arcsec = scale_arcsec;`，:975-978 原样写 `ASTROCS_DRIZZLE_SCALE_ARCSEC`（`%.4f`）。
  > 生产/测试侧实际取值（全为在册用例）：p1hips_tests_units.cpp:183 `(0.7, 0.35)`；p1hips_tests_negative.cpp:237-240 `(1.5,1.0)→2`、`(0.0,1.0)→2`、`(0.5,-1.0)→2`、`(0.5,0.1)→0`；drizzle_science_matrix_test.cpp:227 的合成扫描为 `const double scales[] = {0.1, 1.0, 10.0, 60.0, 3600.0};`（:346 另用 6.3）—— 即 **>300″ 的档位只在该合成 sweep 里出现，provenance 通道的实际取值全为亚弧秒至个位弧秒**。（订正：叶子 L26-007 所称「drizzle_science_matrix_test.cpp 断言 ASTROCS_DRIZZLE_SCALE_ARCSEC == "3.220769"」在当前树 **不可复现**——对该文件 grep `DRIZZLE_SCALE_ARCSEC|3\.220769` 命中 0；本条不采用该证据，改为上面这一条自证事实。）
  > 文档声明（PHASE2_SAMPLER.md:191-193）`{1.2112,1.3925,1.4980 | 2.3958,2.8971,3.2035}（300"/600" × …）`、`[0.5,1.0]×[300,600]（:95-96）` —— 域已写明，**但未写"实际源尺度全落在域外，故 sc 恒被夹到 300"**。
- 权威依据: 宪章 §5.3（误差传播必须明确、系数须可解释）、§4.1（单位与域）、§11（禁静默）；本仓自述口径 = sampler.cpp:82 `constexpr double kControlCorrDefault = 1.4;` 与 PHASE2_SAMPLER.md:186-187「k_corr 默认 1.4（:82，UPMW-005 MC 实证 1.3883 @pixfrac=0.8，保守上取）」、:188-189「K_CORR_DOMAIN 选项 B（逐帧标定）…scale 未知→300" 档」、:193「[0.5,1.0]×[300,600]（:95-96）」、:51 变量表「k_corr … 无量纲 … sampler.cpp:82/:547-555」。
- 问题说明: 换算链本身正确（×3600 deg→arcsec ✓）。缺陷在于**标定轴与生产域不相交**：全部在册/生产值 <300″ → clamp 后恒等 300.0，scale 数值对 k_corr 结果**无影响且不可见**（无日志、无状态码、无 provenance 标记）；因此「按帧尺度选择标定系数」名不副实。次级矛盾：未知 scale 的回退档取 300″（pf=0.8 → 1.3925），而文档所称保守默认是 1.4 —— 两者数值不同，"保守"声明与该档实际取值不一致（本代理复算：1.3925 < 1.4，即回退档比声明的保守值更**不**保守约 0.5%；叶子称"系统性偏置低 ~13%"来自 pf=0.5/1.0 端的极值比较，此处按逐档数值给出更精确口径）。
- 影响: 方差控制量 control_variance = k_corr·(π/2)·σ_bg²/N_retained 的系数选择失去帧间差异 → 宽窄尺度帧共用同一 k，SNR/ivar 组合的系统误差被静默吸收；同时文档"按帧标定"与"保守取 1.4"两处以不同方式高估了该机制的实际作用。健康数据的像素值不变（系数属不确定度面，不改科学数值），故不升 P0。
- 建议处置: ①负责人裁决：扩表至生产域（补 0.05/0.1/0.5/1/2/5″ 等档并给出来源或重算依据）／或显式 DATA 拒绝越域（禁 clamp 静默）／或承认"scale 轴当前无效"并从表结构与文档中删除该维（连带删除"按帧尺度"表述）；②PHASE2_SAMPLER.md 的标定段补写「当前生产尺度恒 <300″ → 恒取 300 档」与回退档与 1.4 的差异说明；③补域界回归测试（299.9/300/600/600.1 + 生产典型 0.35″/3.22″，断言拒绝或按裁决生效，且不再有静默饱和）；④与 M9-F-3（provenance 域断言）同步处置，否则修 ①时在册断言会假失败。
- 置信度: 高
- related: L08-001（同函数 pixfrac 非均匀网格插值 bug——不同事实）、M9-F-3（本条修复会被其钉住的 0.1–3.22″ 断言假失败）、M9 §8（回归钉死族）、M9-A-1（同为"有表无依据/有表无域"的不确定度系数面）
