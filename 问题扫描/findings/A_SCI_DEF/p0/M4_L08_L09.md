# 定稿 L08_L09 — Phase2 全链域（UPM/控制采样/覆盖 + 排异/积分/写出/会话）

> 合并代理 M4｜来源 L08+L09｜验证方式：逐条重读原文（当前树即时 read/grep；行锚以符号/关键语句重定位，标注「复核时点行号」，并发修复中可能漂移）

### M4-A-01 N≤4 全拒像素被静默全接受为 UNDERDETERMINED，FROZEN SCI 未订正且零测试
- 类别: A_SCI_DEF
- 定稿优先级: P0（来源 L09-001 原议 P0，复核时点成立；类别由 G_GOV_GATE 改判——根因在排异语义与宪章 §1.1 权威链，治理红线为次面并入影响段）
- 位置: lib/phase2/src/rejection.cpp::「容错：小栈全拒回退为 UNDERDETERMINED」分支（函数 p2_reject_stack_ex，复核时点 :1835-1852）；docs/science/REJECTION.md:79,86-88；docs/algorithms/PHASE2_REJECTION.md:375-386(§5 F14),508-509(§10.9)
- 证据摘录:
  > // 仍 hard fail。仅调容错路径、阈值冻结不变：N<=4 且全拒时降级为
  > // 全接受 UNDERDETERMINED（保留中位数/放宽阈值的等价可继续语义），
  > // 保证 32 帧 mosaic 可落盘且不破坏 2 帧语义。
  > if (accepted_count == 0) {
  >     if (n <= 4) { … reasons_out[i] = P2_REASON_UNDERDETERMINED; out->accepted_count = n; … out->status = P2_STATUS_UNDERDETERMINED; }
  >     else { out->status = P2_STATUS_ALL_REJECTED; }
  （rejection.cpp:1839-1852 逐字重读吻合；rejected_low/high 同时清零）
  > | `n ≤2` 或 `n < minimum_n` | `UNDERDETERMINED` 全接受 | `rejection.h:86` |
  > | 全拒 | `ALL_REJECTED` | `rejection.h:84` |
  （REJECTION.md §8 表 :86/:88 逐字吻合；§7 :79「UNDERDETERMINED 单调性：n ≤2 恒 UNDERDETERMINED，不做剔除」）
- 权威依据: 宪章 §6.3（排异算法及自动选择策略必须在 SCI/ALG 版本化，有合成污染实验、边界条件和确定性验证；不得靠临场启发式静默改变方法）；SCI-REJ-001 §7/§8（FROZEN）；宪章 §1.1（ALG/DATA 无权覆盖 SCI）
- 问题说明: 复核确认：3-4 样本栈全部被判拒（即检出污染）时实现改写为全接受 UNDERDETERMINED，被污染样本直接进入积分；注释自认「该状态按 SCIENCE_FREEZE 仍 hard fail」，触发根因是一次具体运行的 tile 116446。定稿精确化：该行为已被 ALG 承接（PHASE2_REJECTION §5 F14/:375-386、§10.9/:508-509）并已登记 DATA 合同（DATA_SEMANTICS.md:1267「P2_STATUS_ALL_REJECTED | 2 | accepted_count==0 且 n>4（全拒非小栈）」）——但 FROZEN SCI §8「全拒→ALL_REJECTED」无 n≤4 例外、§7 单调性 n≤2，从未按 §1.2 修订；SCI↔(ALG/DATA/代码) 三方不一致。测试面：全仓 grep「116446」在 tests/** 与 lib/phase2/tests 零命中；无任何执行该 fallback 的合成污染/边界用例；连 n>4 的排异域 P2_STATUS_ALL_REJECTED 分支在产品可构建测试面也无人断言（test_p2004:73-79 断言的是积分域 P2_INTEGRATE_ALL_REJECTED，非本枚举）。
- 影响: 生产 IR 节点链（module_adapters.cpp p2_op_reject 调用 p2_reject_stack_ex，复核时点 :3418）与 stage2 工具（:1184/:1449）同源可达——astrocs phase2 run 真实数据中 3-4 帧重叠区被污染的像素全量入积分，直接改变科学值；属 SCI 未登记的排异策略变更（§6.3 红线），产品合规不成立。
- 建议处置: 负责人裁决二选一：按 §1.2 修订 SCI 登记 N≤4 容错语义并补合成污染/边界/确定性测试；或撤分支恢复 ALL_REJECTED hard fail。禁止维持现状。
- 置信度: 高
- 来源: L09-001
- related: M4-A-02（决策/修复不回写 SCI 同模式）、M4-G-01（ALG 越权承接 SCI 语义）、M4-F-08（排异测试名不副实）；宪章 §6.3/§1.1


### M4-A-02 冻结 SCI 内 support reducer 两口径互斥：代码取 accepted 口径并有门，§5 公式未订正、旧 ALG 仍称现状=W>0
- 类别: A_SCI_DEF
- 定稿优先级: P0（来源 L09-002 原议 P0，成立；按「部分修复」规则改写为残余事实——实现侧缺陷已修，SCI/ALG 未回写）
- 位置: docs/science/INTEGRATION.md:58（vs :21,63,75）；lib/phase2/src/integrate.cpp::「B2-A7: canonical reducer」注释与 sup_max 前置更新（复核时点 :44-50，输出 :76）；docs/algorithms/INTEGRATION_ALGORITHMS.md:19-21(F6/F6a),53；docs/algorithms/PHASE2_INTEGRATION.md:42,64,166-174（含 :170-171「实现现状=SCI §5:58 表述」句与 :266-268「F5 现状实现该门 FAIL」句）；tests/unit/p2_output_semantics_test.cpp:85-107（回归门，4b/4c）
- 证据摘录:
  >         support = (support 空) ? 1.0 : max_{valid,W>0} support[i]   # canonical reducer, 覆盖并集保守下界
  （INTEGRATION.md :58 逐字）
  > - **支撑单调性**：`sup_max = max(accepted support)`，增样本不减 `support`。
  （同文件 :75 逐字；:21 符号表与 :63 一致句亦为 accepted 口径）
  > // B2-A7: canonical reducer 契约 = max(**accepted** support)，作用域是
  > // 通过资格门 (accepted ∧ value/支持 finite) 的全部样本，**不含**权重
  > // 正性要求（w==0 合法但不贡献 signal）。故 sup_max 必须在权重分支
  > // 之前更新；
  （integrate.cpp :44-47 逐字；代码 :49-50 于权重分支前更新 sup_max）
- 权威依据: SCI-INT-001（FROZEN T108）内部自洽；宪章 §6.3（integration 必须明确归一化/缺失值/不确定度传播）、§1.2（变更冻结面唯一通道）、§12.3-3
- 问题说明: 两口径确证互斥而非等价：零权重 accepted 样本（生产 mode=2 下 ivar==0 合法产生：integrate.cpp:56「零权重=合法但不贡献」）是两集合差集，其 support 为全栈最大者时 :58 不计、:75 必计，support 输出不同。实现现取 :75 口径（B2-A7 修复，含回归门 p2_output_semantics 4b：零权样本 support=0.9 必计入且链接 astrocs_phase2、注册 ctest「p2_output_semantics」——**实现侧已修且有保护**）。残余事实三条：①FROZEN SCI §5:58 公式从未按 §1.2 订正，同一 SCI 内 §2:21/§5:58/§5:63/§7:75 两套互斥定义并存，「科学定义」本体不自洽；:63 自称「与 integrate.cpp 一致」而 :58 与实现不一致——SCI 自称一致为不实；②共享 ALG（INTEGRATION_ALGORITHMS F6a）已改为 accepted 口径，而模块级 ALG-P2（PHASE2_INTEGRATION.md，2026-09-09 冻结）仍载「实现现状=sup_max 位于 :49 continue 之后=max_{valid,W>0}（DISP-P2INT-001 待整改）」「F5 现状实现该门 FAIL」——随 B2-A7 落地整体失效（另立 M4-E-02 的锚表漂移中合并陈述）；③DISP-P2INT-002（:166-169）当时登记了该矛盾并裁定「整改归 P2-INT-IMPL（实现对齐 header :17）」，整改完成后 SCI 归一义务无人承接。
- 影响: support 输出改变 → area=support×A_cell、flux=signal×area、写盘 covered_area 随之改变（凡存在「零权重 accepted 样本且其 support 为栈内最大」的像素，生产 ivar 模式可达）。当下代码行为唯一且可复算，但按 :58 字面实现者会得到不同产品——追溯链不能据以唯一复现。
- 建议处置: 走 §1.2 修订 SCI 保留唯一公式（同步 :58/:63/:21/:75 四处）并重冻结；订正 PHASE2_INTEGRATION.md 锚表与 DISP-P2INT-001/002/F5 状态；回归门已存在无需另建。
- 置信度: 高
- 来源: L09-002
- related: M4-E-02、M4-A-01、M4-C-07；INTEGRATION_ZERO_WEIGHT_CONTRACT

