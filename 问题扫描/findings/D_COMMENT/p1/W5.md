# W5（第二轮 · 吞错与诊断完备性）片2-3 · P1 六条｜HEAD a3a343a4，语料 C/C++ 850（去 vendored 核心 706；lib/acr 164、非 ACR 542），Python 门面 162

### W5-N-01（P1）**抛异常的任务在观测面被记成 COMPLETED**：`lib/core/src/executor.cpp:137`（CpuHeavyExecutor）与 `:254`（IoExecutor 同形）
- `try{task();}catch(...){}` **之后照常 `tasks_executed_.fetch_add(1)`**，并在 `:151` 写 `TraceEvent e.status=「COMPLETED」`（`TraceEvent` 本有 `status`/`error`/`error_domain` 三字段，`contracts.h:213-215`，此处 error 留空）。
- 污染面：`observed_trace.json`（`commands.cpp:567`）、RUN_GRAPH 渲染（`gen_run_graphs.py:48` 把 COMPLETED 涂绿）、**CHK-002 静态/观测双向比较**。同仓对照：`runtime.cpp:245` 用 `r.failed()?FAILED:COMPLETED`、`scheduler.cpp:285` catch 后 `node_ok=false`+`record_failure`。命中频率需运行期未判。related `M5a-G-005`、`V21-N-10`（RunContext 面）、簇 1 机制⑤

### W5-N-02（P1）`cpu_routing.cpp:412` 的 `catch (...) { /* handled */ }`——**注释声明 handled 而正文为空** ⇒ 退 baseline 不留痕
- `prof` 保持空对象 → `kernel_row` 取不到行 → `want_provider` 落字面量 baseline（`:416` 的 `jget_ref(*kp,「provider」,「baseline」)`）。退 baseline 本身合 §10.3；**缺陷在处置不留痕且注释与行为相反**：同族条件下 `worker_advisor.cpp:208` 记 `chain += profile_unreadable|`、`profile_store.cpp:363`/`:390` 有显式回落记录 ⇒ **同仓同一条件两种处置**。触 §14.4「禁静默改默认值」。related `M5a-G-010`、`C-20`、`V2-N-10`

### W5-N-03（P1）`worker_advisor.cpp:72` 的 cgroup 限额解析：`catch (...) { return false; }` 使**解析抛错与键不存在共用同一出口** ⇒ 按「无 cgroup 约束」算 workers 上限（**fail-open**）
- 同文件 `:146`（字段类型篡改）与 `:208`（profile 不可读）**都显式记录回落**，`:72` 不记录也不区分 ⇒ 容器文本形态稍有出入即**失去 OS 约束**，直接抬升 §10.3 并行度与 §10.5 已分配容量口径。触发需运行期未判。related `M5a-G-006`、`V7-N-08`、`A-39`/`A-41`（分母面已由 `M5a-G-002` 修复）

### W5-N-04（P1）`cli/runtime_client.cpp:389` `catch (...) {}` 与 `:416` `catch (...) { continue; }`：退出码判据「是否 INPUT(3)」的两个面都坏
- (a) manifest 不可解析时该节点**静默不参与判定** → 落 DATA→2 / IO→7（吞错直接影响退出码与 JSONL final 事件）；(b) 归因主体是「**任一节点**」而非「失败节点」⇒ 多节点 run 存在**串扰**。其上方 `:382-384` 注释自述「失败 manifest 的 error_kind==input → 3」⇒ **实现与自述不一致**（准绳③）。related `M5b-C-05`（该条只管 RESOURCE→5，不覆盖本面）

### W5-N-05（P1）`cli/commands.cpp:867`/`:1051`/`:1149`/`:1377`/`:1727` + `:1214` 共六处同构 lambda：`try { json::parse(cfg_text).value(「output_dir」,「.」) } catch (...) { return 「.」; }` ⇒ **配置解析失败时产品写到当前工作目录，run 仍返回 0、无 warning 事件、无 stderr**
- 受影响产物：run manifest、`resource_samples.csv`/`resource_summary.json`/`worker_balance.csv`、provenance、graph 产物。**§14.4 双重命中**：同一 `cfg_text` 已在 `:1071`/`:1204`/`:1659` 过 `validate_config_full` 信任边界，此处属「**边界后重复解析同一文本并各自就地默认**」+「静默改默认值」。related `V7-N-03`（机制级；本条差异是默认值来自 catch 而非缺键、落点是写出目录）、`M5b-C-06`、`M5b-G-16`、`W4-R2-03`

### W5-N-06（P1）`cli/commands.cpp:120`/`:138`/`:466`/`:542`/`:553`/`:579` 六处 `bool ok=false;` 后**从不读取 `file_sha256(&ok)` 的出参**（全仓 21 个调用点，其中 12 处是读了 ok 的对照面）
- `parser.cpp:238` 读失败返回空串并置 `*ok=false` ⇒ 空串被直写 JSON 字段 `sha256`/`ir_sha256`/`output_artifacts[].sha256`，而 `jsonl_event_v1.schema.json` 的形状是 [string,null] + 64 位十六进制 pattern ⇒ **空串两者都不符**；provenance/verify 面无从区分「没算」与「算错」。附带站点：`aio_publish.cpp:382` 的 `(void)fsync(fd)`。related `W4-R2-04`（同一事实的门侧归因，本条是 CLI 六站点计数）、`M8a-G-010`、`M6a-G-002`、`V21-N-12`
