# V8 · 本批新增文本可核验性（P1 两条；口径 `git diff --unified=0 ccc7a933..HEAD -- lib cli include tools docs` 取新增行，M-V8-01 = 7912 行 / 93 文件；建库 `521095b8`、锚点复核 `01f94996`）

### V8-N-01（P1·判据②锚无宿主）死码保留的"防清理凭据"是一条**不存在的测试符号** ⇒ 保留决定无凭据
- 新增注释（`23a7f665`）`lib/core/src/module_adapters.cpp:1796-1800`：`// 直调测试: tests/unit/p1001_real_nodes_test.cpp::test_starpsf_precise_inactive_direct_call（证明本路径仍可编译且能跑出结果, 防止被当作死代码清理）。`
- 复算：`git grep precise_inactive -- .` → **仅该注释自身**，测试面 **0 命中**；真实存在的符号是 `test_psf_fast_cap_and_inactive_precise`（`p1001_real_nodes_test.cpp:2188`，`:2486` 注册）。⇒ **path 在册、`::` 符号整体不存在**（非行号漂移，是符号虚构）。
- 后果：`module_adapters.cpp:6162` 同句式援引"负责人裁决 2026-09-14 防止被当死代码清理" ⇒ **inactive 精确 PSF 路径"按裁决保留"的唯一技术凭据是虚构的**。（另 `V2-N-09` 已记：该测试的真实覆盖面只有 `contains("psf_mode")` 不查值。）
- 改法：注释改指真实符号 + 若要"防清理"须补一条真直调锁（现无）。**related** `V2-N-09`、`V12-N-06`、`V7-N-10`、`C-18`、`C-15`

### V8-N-02（P1·判据①④，**并推翻 V1 一项判定**）「科学公式零本地副本 / 全部数值调 C ABI」复算为假：4 个宣称函数只调 3 个，且本 TU 内有三处同式副本
- 新增宣称：`lib/phase1/noise/snr_frame_science.cpp:3-8`（`35c85f53` 建、`57c04256` 改）「**科学公式零本地副本**: 本 TU 的全部数值都来自 P5-SNR 唯一权威实现」（列 4 个函数）；`README.md:27`「**零公式副本**，全部数值调 `snr_science.cpp` 的 C ABI」。
- **复算**：`grep snr_moffat4_profile_f64` → **只有 `:6` 注释自身，本 TU 零调用**（4 个只调 3 个：`snr_source_snr_f64 :58`、`zero_point_standard_error :85`、`frame_depth_f64 :190`）；且本 TU 内有 **本地 `median_of(:30)`、本地 `2.5*dex→mag(:86)`、本地 `local_snr = snr_f/med(:155)`**；`2.5 *` 同式在 `snr_science.cpp:207`/`:227` 与 `noise_model.cpp:295` **各有一份** ⇒ 宣称的收敛性（改一处即可）**就地不成立**，"唯一权威实现"在同批内已有第二/第三份。
- **★前台订正（我据此改自己的记录）**：`V1.md §1 二(d)` 曾把 `snr_frame_science.cpp:84-86` 记为站点并判「**一致（零公式副本）**」——该判定与 V8 的逐调用点复算冲突，**予以撤销**，改判为"宣称与实况不符"（同段号在 `gaia_client.c` 已漂移，锚一律改 `文件::符号`）。已同步登记于 `V1.md` 末尾「前台订正」段与账本 `V1-N-12` 行的 `verified_by`。
- 改法：①要么真调 `snr_moffat4_profile_f64` 并删本地副本，要么把注释/README 改为如实描述（哪些量本地算）；②`2.5·dex→mag` 收敛到 `S-1` 权威点（配 `check_numeric_constants` 的 `NC-DEX-to-MAG` 族）。
- **related** **V1-N-12**、`V12-N-05`（同族公式双头）、`V6-N-04`、`S-1`、簇 8
