# L28c findings — D_COMMENT P1（tier2 头文件外围 · 注释溯源补轴）

- 代理码: L28c ｜ 判据: 复用 L28b D-TRACE-1 R-1..R-6（见 问题扫描/_cache/L28c.md §0.3）
- 本类 finding: 1 —— **现行分级 P1（前台定档改回；L28c 自检降 P2 的理由仅对 ①「两 ISA 头休眠」成立，②③经当场二进制实测成立）**。原自检文本保留在本文件末尾ings/D_COMMENT/p2/L28c.md

## L28c-D-001 avx2/avx512 provider 合同头把 op 的入出槽位语义指派为「另一头的注释」权威，而该权威自身携带 docs 零登记 ID；两 ISA 头自身的唯一科学锚是已废止的编排层占位编号

- 类别: D_COMMENT
- 建议优先级: **P1**（原判 P1；合并阶段曾降为 P2，前台按 C-06 恢复为 P1，目录 p1/ 为定级源。注：原此处引用的「分级自检」段在本文件不存在，属悬空引用，已删。）
- 位置: providers/cpu/avx2/include/astrocs/cpu/avx2_provider_v1.h:47、:24、:80、:88；providers/cpu/avx512/include/astrocs/cpu/avx512_provider_v1.h:59；（被指向的权威）providers/cpu/baseline/include/astrocs/cpu/baseline_provider_v1.h:102-121、:88-:99；（同机制的 .cpp 侧）providers/cpu/avx2/src/avx2_provider.cpp:110
- 证据摘录:
  > avx2_provider_v1.h:47 " *   - 槽位语义逐 op 同 baseline_provider_v1.h 头注释 (calibration: 4 入 1 出;"
  > avx512_provider_v1.h:59 " *   - 槽位语义逐 op 同 baseline_provider_v1.h 头注释 (hips: 1 入 1 出;"
  > avx2_provider_v1.h:24 " *   - ALG-001 calibration-pixel-transform 与 ALG-P3-002 hips-bulk-transform"
  > avx2_provider_v1.h:88 "ACS_CPU_AVX2_KIDX_CALIBRATION = 0,   /* ALG-001 calibration-pixel-transform */"
  > baseline_provider_v1.h:102-103 "/* ── kernel 参数 POD (前两字段 acs_head; 只读输入; 无指针字段, 跨边界安全) ──\n * 字段语义逐 op (w*h=N=输出元素数; k/aux0/aux1 按 op):"
  > baseline_provider_v1.h:97-98 "ACS_CPU_KIDX_REJECTION_STATS = 9,  /* ALG-008 rejection-statistics */" / "ACS_CPU_KIDX_INTEGRATION_ACCUM = 10,/* ALG-009 integration-accumulate */"
- 权威依据: 宪章 §12.2（注释只解释…非显然决定；禁堆积历史编号）、§12.3-3（实现须引用**有效** SCI/ALG）；docs/modules/registry/astrocs.phase1.{star-psf,photometry,noise-snr,wcs-platesolve}.md:14-16 明文把 `ALG-0NN` 称「占位 ID…保留为编排层词汇」且「合同 ID 由占位更正为真实」；L28b R-3（废止编号形不计溯源）与 R-6c（注释被指派为权威 = 必须已回写 L1）
- 问题说明: ①**权威外包给注释**：两个 ISA provider 头（**注：本域原判「已交付」不成立，经复验改判为「接线面/计划中」——见本条「影响」与「分级自检」两段**）对「每个 op 的输入/输出槽位」这一 ABI 语义不自行给出，而是写「同 baseline_provider_v1.h **头注释**」；被指向的权威是 baseline_provider_v1.h:102-121 的手写散文表，docs 登记面无对应条款（该表的 ALG-008/ALG-009 两行为 L28b-D-002 已实测的 docs 全域零登记，本域以 `\\bALG-008\\b`/`\\bALG-009\\b` 对 docs 与 contracts 复核复算 = 0/0 命中）。三处消费者（avx2.h:47、avx512.h:59、avx2.cpp:110）同指这一处注释 ⇒ **一份未回写 L1 的注释同时充当三个交付头的算子语义权威**，任一处漂移无门可拦（宪章 §12.3-3 的「有效」条件与 §12.2 的「非显然决定须解释」同时落空）。②**废止编号当科学锚**：avx2 头的 calibration kernel 在 :24/:80/:88 三处以 `ALG-001` 为唯一算法锚，而 ALG-001 在 docs 只出现在 `docs/architecture/CPU_BACKEND_ARCH.md:3` 的「上游: ARCH-002/ALG-001..007」范围转述与 docs/modules/registry 的「占位/编排层词汇」自述中，即它同时是 V5 控制包任务编号与 `science_contract_id` 字面量（runtime 侧实测：providers/cpu/baseline/src/baseline_provider.cpp:89、avx2/src/avx2_provider.cpp:102、lib/backend_host/backend_table.inc:20 都把字符串 "ALG-001" 写进 kernel 描述符）⇒ 按 R-3 属废止编号形，strict 档下这三个锚全部不可解析，跨边界交付的 kernel 与科学合同之间的连接因此只剩任务编号。③两头的自报计数本身可复算且**相符**（ACS_CPU_AVX2_KERNEL_COUNT 2u 对 KIDX 两项、AVX512 1u 对一项、BASELINE 12u 对 0..11 共十二项），故本条不涉 R-6a 失配，只涉 R-6c 与 R-3。
- 影响: **接线面/计划中**的 CPU provider kernel 合同（ISA 路由与 provider 数值自测的预期输入），非现行交付面。**本域复验订正**：`providers/` 在全部 CMakeLists.txt 中**只出现在注释行**（grep "providers/" 命中 3 处 = CMakeLists.txt:125/:219/:232，三条皆为注释；`*.cmake` 内 set() 命中仅 cmake/install_layout.cmake:53 一个子目录名字面量）；根 CMakeLists.txt:220-221 的 `add_library(astrocs_cpu_baseline SHARED lib/backend_host/baseline_backend.cpp)` 源在 backend_host，且 :118-125 自述「正式 provider ABI…与 Windows DLL 由 CPU-002 (providers/cpu/baseline) 交付，本任务不伪装 provider 完成」⇒ **providers/cpu/{baseline,avx2,avx512}/src/*.cpp 不进任何 target**，L28b §4-3「根 :220 编入生产 provider」不成立（前台已裁定采信 L28d 订正，本域据此自纠）。**但同一失效锚串的现网一支仍在编译面**：`lib/backend_host/backend_table.inc:20-26` 把 "ALG-001"/"ALG-002"/"ALG-004"/"ALG-005"/"ALG-006" 写进 `astrocs_kernel_entry_v1 kKernels[]` 的 science_contract_id，而该 .inc 由 CMakeLists.txt:441-452 编入 astrocs_core、:462-465 由 `astrocs_cpu_avx2`/`avx512` STATIC 变体共享（注释自述「变体与 baseline 共享 backend_table.inc 同一源(零复制漂移)」）⇒ ②的编号占位并非休眠，①的注释权威链休眠。当有人按注释给 avx2/avx512 增删槽位或改 op 语义时，唯一「依据」是一份未在 L1 登记的注释表；而 provider manifest 里对 host 暴露的 science_contract_id 字面量指向编排层词汇，导致「科学定义=算法=接口=代码=测试」的链条在 CPU provider 边界上不可机器解析。当前不改变已产出数值（kernel 实现本身与 baseline 同式）。
- 建议处置: ①把槽位语义上收到 L1（docs/architecture/cpu/CPU_00{3,4,5}_*.md 或新建 ABI 章节给逐 op 槽位表并登记 ID），三个消费者改为引用该条款而非头注释；②把 kernel 描述符与注释中的 ALG-0NN 全部替换为现行 ALG-*-nnn（与 docs/algorithms/ 实际条款对齐），或由登记面正式声明 ALG-0NN 为合法别名并给出映射表；③给「头注释被指为权威」的句式加机器判据（L28b R-6c 现网命中面即含本域新增 3 处）。
- **分级自检（前台 M8a 三准则）**：①断言可为假 = 成立（「槽位语义…同某头注释」与「ALG-001 为科学合同锚」均可判为假/不可解析）；②条文在位 = 成立但**只到 L2 层**（docs/standards/COMMENT_STANDARD.md:10 要求科学公式给 SCI/ALG ID；R-6c 属 L28b §1.5 草案条文，宪章 §12.2 现有文本未含溯源义务，此点即 L28b §1.1 的判定）；③后果达交付面 = **部分成立**（①一支因 providers 不入编译而休眠，②一支经 backend_table.inc 达编译面）⇒ 三条中「③」降为部分，本条按 P2 交，**升级条件**：CPU-002 落地（providers 源进 target 或产品以 provider 形态装载）⇒ ①随之激活，建议按 P1 复判；若负责人以②为主落点，可按其现网性自定 P1（本域不代签）。
- 置信度: 高（①②均经本域当场 grep 复算，含前台转述三处事实的独立复核；②的「废止」定性依据 docs/modules/registry 的自述措辞，若负责人裁 ALG-0NN 为合法别名则本条降为① alone）
- related: L28b-D-002（同一 baseline 注释表主定稿点；本条为**被外包的两个新头 + .cpp 共三处新载体**，若合并方判为同族请并案而非另计）；M6a-D-009（§12.2 禁止形态族）；M8a-E-001（未注册 ID 被引用，同型不同命名空间）；M6a-E-001（不可解析引用族）

- **【前台定档 2026-09 · ②③经当场实测成立，P2 改回 P1】** 你的「ALG-0NN 一支并不休眠」我逐层复验通过，且**比你写的更进一层——它已经在交付单元里**：
  ① **包含关系**：`#include "backend_table.inc"` 命中 **4 个编译单元** —— `lib/backend_host/baseline_backend.cpp:27`、`avx_backend.cpp:22`、`avx2_backend.cpp:26`、`avx512_backend.cpp:27`（`.inc` 自身 `:1-6` 声明「表与 self_test 来自同一 .inc，变体仅编译旗标不同，禁复制漂移」+ `ASTROCS_BACKEND_ID` 宏切换）。
  ② **二进制级证据**：`strings` 实测 `build/astrocs_cpu_baseline.so` = **11 处** `ALG-0NN` 字面量（`build/libastrocs_cpu.a` 同 11；`.o` 级 `baseline_backend.cpp.o` 亦 11），
     命中集合 = `ALG-001 / 002 / 004 / 005 / 006 / 008 / 009` + `ALG-P3-002`。其中 **`ALG-008`、`ALG-009` 正是 L28b-D-002 三级复核定为「docs 登记面零命中」的两枚**。
  ③ **交付面归属**：`packaging/astrocs.product.json` 该单元逐字 = `PROV-CPU-BASELINE / kind=provider / status=IMPLEMENTED / rel_path=providers/astrocs_cpu_baseline.so`
     ⇒ **未登记且属废止占位词汇的编号，正作为运行时 `science_contract_id` 随一个 IMPLEMENTED 交付单元出厂**；而消费侧 `providers/cpu/baseline/src/baseline_provider.cpp:490-493` 按 L28b-D-005 **只判非空**，不做注册面校验
     ⇒ 追溯链在运行时被"看起来有值"的废止 ID 填充，**任何字面存在性检查都会通过**（与 L28e-E-001「生成器自造 ID 再自证」是同一失守的两侧：那边造，这边不验）。
  **分级依据变更**：①断言可为假 ✓（若门展开 `kKernels[]` 逐条比对 INDEX.yaml 即被推翻）；②条文在位 ✓（§12.3-3「实现须引用**有效** SCI/ALG」——注意本条主语**就是实现**，不像范围锚那条有主语争议）；
  ③后果达交付面 ✓（**已从"接线面"升级为二进制内含 + product.json IMPLEMENTED**）。三条全成立 ⇒ **P1**；仍不抬 P0（不改科学数值、不改产品结构，§17.10 意义上无产出污染）。
  **你 ①「两 ISA 头休眠」的判定我保留**（`providers/cpu/**` 确不编译），本条升级只针对 `.inc` 这一支；两侧不要混读。
