# L28d 定稿 · D_COMMENT × P1（生产 .cpp/.c 实现体「注释撒谎」面 · 第三轮补轴）

> 代理：L28d。纯只读（read/grep/glob/write/edit），未用 shell/构建/测试/git/联网。
> 判据逐字沿用 问题扫描/_cache/L28b.md §1.3 的 R-6a/R-6b/R-6c（本轴加 R-6c-unit 收紧：注释声明的单位/量纲/阈值必须能由同函数算式复算），不换判据。
> 语料口径、25 件取件理由、R-6a/b/c 三条各自的命中数（含 0 命中）、与 L28b/M6a/L01/L04/L26 的分界与撞车清单：全部在 问题扫描/_cache/L28d.md §0/§2/§3/§4。

### L28d-D-001 生产求解器的绝对剔除阈被注释与对比文档写成「角秒」，实现按像素比较：有效阈是宣称值的 2 倍，诊断日志把像素值印成角秒
- 类别: D_COMMENT
- 建议优先级: P1（涉及科学量的误导性注释：10_PROTOCOL §2「涉及科学量的误导性注释」为 P1；本条不改代码行为，行为后果见"影响"）
- 位置:
  - lib/plate_solve/cpp/ipv/src/ipv_ransac.cpp:1016（常量定义 + 单位宣称）
  - 同文件 :942（被减量的真实量纲自述「残差 (像素)」）、:943-954（residual_px 实现：dx/dy 为 W−pred 的平面坐标差，无任何角秒换算）、:978（算法清单里写「dist2 > MAX_DIST²」）、:1050（max_dist2 = MAX_DIST*MAX_DIST）、:1066-1071（dist2[k]=r*r，r=residual_px）、:1077-1081（与 max_dist2 比较）
  - 同文件 :1091-1092（运行时诊断串把该像素值印成「角秒」）
  - 同族第二载体 lib/plate_solve/cpp/ipv/SIRIL_COMPARISON.md:222（对比表把该行标为「单位换算 ✓」）
- 证据摘录:
  > （:1016）    const double MAX_DIST          = 100.0;   // AT_MATCH_MAXDIST (原实现 50px, IPV 100 角秒 ≈ 50px * 2"/px)
  > （:942-954）// 计算一对匹配的残差 (像素) ... return std::sqrt(dx * dx + dy * dy);
  > （:1078）            if (dist2[k] > max_dist2) {
  > （:1091）                g_ransac_logger.warnf("iter_trans_inner iter=%d: working_set 所有对残差 > %.1f 角秒, 失败",
  > （SIRIL_COMPARISON.md:222）| 7 | **绝对阈值** | 50px (dist²>2500) | 100 角秒 (≈50px×2"/px) | 单位换算 | ✓ |
- 权威依据: 宪章 §12.2（注释只解释单位/数学原因/边界条件，须与实现一致）、§4.1（每个量必须写明单位且不得名实不符）、§12.3-1（文档-代码一致）；docs/algorithms/PLATESOLVE.md §5/§9（TRANS 与残差在像素域）
- 问题说明: 三点各自成立。①**量纲错**：MAX_DIST 与 dist2 同域比较，而 dist2 来自 residual_px（其函数头 :942 自述单位为像素），故 MAX_DIST 的有效量纲是 **px**；注释却断言「IPV 100 角秒」，并按「50px×2″/px=100 角秒」把它说成与原实现 50px 等价的换算是**已完成的**——代码里没有 ÷2″/px、也没有 ×3600 的任何一步。②**后果是可复算的 2 倍松动**：按注释自述的标称像元尺度 2″/px，100 px = 200″，即当前绝对阈等于原实现 50 px 的 **2 倍**（在 σ-clip 首轮即放松剔除，影响进入 inlier 集与 rms 的星对）。③**诊断面同错**：:1091 的告警串把该阈值印成「角秒」，运维/复算者据日志读到的数值单位与真实域不符。④该错还以「✓」形态进入对比文档 ⇒ 与 M1a-D-002（StarPoint 注「角秒」而实为像素）同族同方向缺陷，但**位置与断言不同**（那条是结构体字段注释与 SIP 值域，本条是剔除阈常量 + 对比表结论 + 日志串），不与之并计。
- 影响: astrocs_p1_ipv STATIC 与 astrocs_catalog_gaia/astrocs_p1_* 模块的板解算路径（根 CMakeLists.txt:507-515 编入交付图）；按注释/文档判定「IPV 与 Siril 绝对阈等价」的评审结论失真，匹配内点集偏松；真实数据（Gaia DR3 路径）受影响的幅度需一次实测（本轴禁执行，未判）。
- 建议处置: ①三处（:1016 注释、:1091 日志串、SIRIL_COMPARISON.md:222 该行）统一到实现事实：写明「MAX_DIST 单位 = px，当前 100 px；相对原实现 50px 的放宽须登记 DISP 或改回 50」；②若要保留角秒口径，则在比较前显式换算（唯一换算点 + 该常量的单位断言随之成立）；③机器门（与 L28b R-6c-unit 同一条）：注释/文档中出现「N 角秒/N″」而该常量的消费点与 residual_px 同域 ⇒ 报警；日志格式串里的单位词必须与被印变量的量纲同源。
- 置信度: 高（比较链 :1066→:1078 与 residual_px 定义逐行 read 复现；2 倍结论取注释自述的 2″/px 作标度）
- related: M1a-D-002（同族单位注释自矛盾，不同位置不同断言）、M6a-D-009（§12.2 禁止形态与"原实现"叙述）、L28d §0.4 重算台账第 3 行

