# M1a 定稿 · D_COMMENT · P1（来源切片 L01）

## M1a-D-001 inlier 权威缓冲的坐标域声明三处互斥：ALG/README 称"+0.5 契约解读"，C ABI 字段注释称"图像中心原点、Y 轴向上"

- 类别: D_COMMENT · 优先级: P1 · 来源: L01-017
- 位置: lib/plate_solve/cpp/ipv/include/ipv_api.h::ipv_get_last_inliers 字段表；docs/algorithms/PLATESOLVE.md::§5/§9（+0.5 契约句）
- 证据摘录（逐字）:
  > （ipv_api.h:203-219 字段表）// [0] det_x_px - 检测器 x (像素, **图像中心原点, Y 轴向上**)
  > （PLATESOLVE.md:128）… orchestrator.cpp 统一 **+0.5 契约**（index-is-center）解读 …
  > （PLATESOLVE.md:147-148）inlier 缓冲 det_x_px/det_y_px 为…（+0.5 契约解读）
- 权威依据: 宪章 §12.3-15（像素语义显式）、§13.1；ALG-WCS-001 §5
- 问题说明: 同一缓冲的 x/y 语义在两处各说一种：C ABI 头注称"图像中心原点、Y 轴向上"（既非数组下标也非 Y-down），文档称"数组下标 +0.5 中心、Y-down"。二者相差一次原点平移与一次 Y 轴翻转；消费方（orchestrator 回写、Phase2 配对、任何 ctypes 调用者）只能择一，选错即亚像素到像素级的系统偏差（Y 翻转量级为图像高度）。本轮复核未在实现处找到统一裁决的注释或断言。
- 影响: 跨语言 ABI 的语义歧义面（不可由类型系统发现）；若消费方按头注实现，产生整幅镜像偏移。
- 建议处置: ①在 ipv_api.h 字段表给出唯一定义（原点、轴向、是否 +0.5、单位 px）并与生产写入处逐字对齐；②加一条断言/测试：写入值域与图像尺寸关系（0..W-1 或 -W/2..W/2）；③文档侧删除"解读"式措辞，改为引用同一符号。
- 置信度: 中-高（两处措辞互斥为确定事实；孰为实际生效语义未运行验证） · related: M1a-C-003、M1a-D-002

# 附：本目录未立案的候选（已并入他处）
- "StarPoint 角秒 vs 像素"族 → M1a-D-002；"CD/CDELT 头面混写" → M1a-B-006。
