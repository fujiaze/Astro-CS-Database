# M6a 定稿 · D_COMMENT × P1（来源切片 L13 + L14；注释质量横向 / Phase1-3）

> 代理：M6a。只读取证；位置一律 path::符号，行号为复核时点参考值（并发提交在改源码，行号不符不作剔除理由）。四态处置、并档与移交见 问题扫描/_merge/M6a.md。
> 并档说明（避免双计）：L13-002（不存在文档当合同锚）已由 **M6b-E-004** 定稿；L13-004（StarRecord.mag 注释）与 L13-005（PSF [N,9] 两份 L1 文档 A/B 倒置）已由 **M3b** 定稿；L13-006/L13-017（dpsf/sdet 锚漂移与残句流水）已由 **M3b-E-01/M3b-D-01** 定稿；L14-007（第二线程预算源）已由 **M5a-G-006/M5a-D-001** 定稿 —— 本文件不重复登记上述事实，只登记各条与本域相邻的注释层残余。

### M6a-D-001 生产注释承担「未登记的裁决请求」角色：SCI 冻结 1e-4 px 门被同文件注释自证一步法数学不可达；裁决与偏差 ID 只活在注释与控制包台账里，docs 权威登记面（STANDARDS_REGISTRY / PLATESOLVE §11.3）对 DISP-WCS-007、DISP-WCS-008 零命中（来源 L13-001 的注释层残余；科学定档在 M1a）

- 类别码: D_COMMENT
- 优先级: P1（原 L13-001 报 P0；科学侧的 P0 已在 M1a 定稿，本条只落注释层 ⇒ 降为 P1）
- 位置:
  - `lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp::extract_wcs_sip 可达性注记`（复核时点 :397-400）
  - `lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp::SIP 段标题注释`（复核时点 :384，写「WCS-002/DISP-WCS-008 整改」）
  - 登记面反证：`docs/standards/STANDARDS_REGISTRY.md::wcs 域`（复核时点 :66/:76/:241 只出现 DISP-WCS-001/006）、`docs/algorithms/PLATESOLVE.md::§11.3 偏差表`（登记至 DISP-WCS-006，无 007/008 行）；grep `DISP-WCS-00[78]` 在 **docs/ 全域 0 命中**
  - 两个 ID 的实际存在处（均非现行权威登记面，本域按免报区只作旁证）：`工程控制/ACTIVITY_STATE.md:44`「DISP 未修候选：DISP-WCS-007（deg/rad 成对抵消）、DISP-WCS-008（AP/BP 逆…」与 `evidence/v8_1_ci_control/TASK_STATE.json:1563`「重大发现登记:DISP-WCS-007候选…;DISP-WCS-008候选…;均锚锁现状」
  - 另一侧事实：`tests/unit/p1wcs/p1wcs_tests_negative.cpp:255`「// B2-A1 翻锚（原为"缺陷行为锚"，锁定 DISP-WCS-007 现状: 偏差 >1e-6）」⇒ 007 在测试面已翻锚，但翻锚结论同样未回写 docs 偏差登记
- 证据摘录:
  > （ipv_wcs.cpp:397-400）… 边缘畸变 ~117px 时 5 阶最优 center90 roundtrip ~7.6 px, 为逆映射最近奇点决定的逼近极限 (order 6..15 → 5.3..0.26 px, 收敛率~0.73); 冻结 1e-4 px 在该 fixture 下数学不可达, 需负责人裁决 (finding)。
  > （ipv_wcs.cpp:384）// 5.3 SIP AP/BP: 网格反变换法 (WCS-002/DISP-WCS-008 整改: 网格加密 7×7→41×41、拟合阶 trans.order→AP 布局上限 5、各向归一化正规方程)
  > （STANDARDS_REGISTRY.md:66 区段）AP/BP 以 7×7 网格拟合、SCI 层已显式冻结（与实现 41×41/阶 5 互斥）
- 权威依据: 宪章 §12.2（注释只解释单位/数学原因/前后置/所有权/线程安全/生命周期，禁堆积任务编号与审计流水）、§1.1（层间冲突须建 finding 并回写权威层）、§12.3-3（实现引用有效 SCI/ALG/偏差 ID）、§17.2（追溯无断链）
- 问题说明: 两个可分别核对的事实。①**未决科学冲突以注释形式挂起**：注释自己写明「需负责人裁决 (finding)」，但仓内不存在该 finding 的登记面（STANDARDS_REGISTRY 的 wcs 域与 PLATESOLVE §11.3 都没有承载它的行，docs 内 DISP-WCS-007/008 零命中）⇒ 裁决请求没有落点、不可被任何门或人接手，注释成为唯一载体。②**注释引用只存在于控制包/证据台账的偏差 ID**：即便整改事实本身（网格 7×7→41×41、阶 →5）为真，其引用的 DISP-WCS-008（及同族 007）在 `docs/**` 无任何登记行，只以「候选」形态活在 `工程控制/ACTIVITY_STATE.md` 与 `evidence/v8_1_ci_control/TASK_STATE.json`（两者都不是宪章 §1.1 的现行权威层，且 evidence/、工程控制/ 为免报区）⇒ 按 §12.3-3 属「未经定义即被引用」；而 STANDARDS_REGISTRY:66 对现行实现仍写 7×7（该注册表失实已由 M1a 定稿，本条只补注释侧断链与「翻锚结论不回写登记面」）。
- 影响: 后续整改者按注释找 DISP-WCS-008 会在权威登记面找不到登记（只能翻控制包台账/证据 JSON），无法判断该偏差是被接受、驳回还是待裁；007 已在测试面翻锚却仍无登记行，说明「翻锚/裁决」与「登记面」之间没有回写义务；SCI 的 1e-4 px 不变量与生产实现的对立在治理面上不可见（该对立的科学后果见 M1a）。
- 建议处置: ①把「需负责人裁决」从注释迁出：在 PLATESOLVE §11.3 与 STANDARDS_REGISTRY 的 wcs 域新增 DISP-WCS-007/008 两行（状态=待裁决），注释只留「登记号 + 一句结论 + 指针」；②注释里禁止承载未登记的裁决/整改请求（§12.2 强化），可加机器门：注释中出现的 `DISP-[A-Z]+-\d+` token 必须在 docs/ 有登记行。
- 置信度: 高（注释文本、行号与 grep 零命中均复算）
- 来源: L13-001（注释层残余）
- related: M1a-A-001/M1a-C-001（L01-004 与 L13-001 的三点互锁 P0 主定稿点）、M6a-D-003（同文件 NB_GRID=7 残留）、M6a-E-001（不可解析引用族）、M6a-G-001（此类注释违例无门可拦）

### M6a-D-002 同一 host_lease 合同在四个适配器里被注释成三种互斥语义，calibration 文件头与其自身实现直接相反（写「acquire 失败→单线程降级、非致命」，实现是硬 BUDGET 拒绝）（来源 L13-003）

- 类别码: D_COMMENT
- 优先级: P1
- 位置:
  - `lib/calibration/src/module_entry.cpp::文件头 注入途径/降级策略`（复核时点 :13-15）vs 同文件 `::heavy 路径 acquire`（复核时点 :929-931 与 :937-940/:950-953）
  - `lib/cosmetic/src/module_entry.cpp::文件头`（复核时点 :16-20「禁单线程降级（cpu_heavy；CAL 先例同款）」）
  - `lib/drizzle/src/module_entry.cpp::acquire`（复核时点 :916-922「acquire 失败 → 单线程降级执行 (BUDGET 缺额非致命; workers=0)」）
  - `lib/snr_estimator/src/module_entry.cpp::文件头`（复核时点 :30-32 引 CAL 先例）
- 证据摘录:
  > （CAL 头 :15）acquire 失败 → 单线程降级 (drizzle 先例, 非致命); 不私建线程池
  > （CAL 实现 :929-931）"重计算禁止单线程" → acquire 失败/executor 缺失均为硬 BUDGET, 不取 drizzle 降级单线程路径
  > （COS 头 :19）ACS_ERR_BUDGET detail 105, 禁单线程降级 (cpu_heavy; CAL 先例同款, …)
  > （DRZ :922）/* acquire 失败 → 单线程降级执行 (BUDGET 缺额非致命; workers=0) */
- 权威依据: 宪章 §10.4（一个进程一个线程预算源；禁私有降级路径绕过 Runtime）、§10.5（「只有一个活跃计算线程均失败」）、§12.2（线程安全与前后置条件必须写对）、AGENTS 执行纪律「重计算禁止单线程」
- 问题说明: 三个事实：①CAL 文件头与其实现（同一文件内相距约 900 行）语义相反——头称「降级、非致命」，实现「硬 BUDGET、不降级」；实现侧注释还点名「不取 drizzle 先例」，等于自证文件头错。②COS/NOISE 头称「与 CAL 先例同款」，其代码正确但**指向了一份写错的 CAL 头**。③DRZ 真的降级单线程，并把该行为写成先例 ⇒ 与 §10.5「只有一个活跃计算线程即失败」冲突（drizzle 降级是否属被允许的 BUDGET 缺额行为，仓内无登记）。
- 影响: 评审/接线者按 CAL 头注释推断并发语义会得出「cpu_heavy 单线程是可接受的降级路径」，恰是发布门禁禁止项；四个适配器互不复制事实，任何一处订正都不会自动传导。
- 建议处置: ①按实现订正 CAL 头（acquire 失败=硬 BUDGET）；②DRZ 的单线程降级若保留，登记偏差 ID 并在 §17.6/§10.5 面说明例外边界，否则改硬失败；③把四适配器的降级策略收敛为单处合同文本（例如 docs/architecture/THREAD_BUDGET_ARCH.md），注释仅引用条款号，禁在四个头文件里各自复述；④机器门：同一 ACQUIRE 策略词（降级/硬失败）在 lib/*/module_entry.cpp 头注释中只允许一种表述。
- 置信度: 高
- 来源: L13-003
- related: M5a-G-006（第二线程预算源）、M5a-D-001（cpu_workers=0 释义与实现相反）、M6a-G-001（该形态无门可拦）

### M6a-D-003 同一文件的顶部「设计」注释块保留整改前参数（SIP AP/BP 仍写 NB_GRID=7），与同文件实现（41×41、阶 5；RA 侧 81×81、阶 7）互斥（来源 L13-007 的注释残余）

- 类别码: D_COMMENT
- 优先级: P1
- 位置: `lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp::文件头 设计清单`（复核时点 :17「// - SIP AP/BP: 网格反变换法 (NB_GRID=7)」）与 `::函数内前向说明`（复核时点 :238 同口径）；实现 `::extract_wcs_sip`（复核时点 :402-403 `const int NB_GRID = 41; const int AP_FIT_ORDER = 5;`）、`::APx 通道`（复核时点 :526-527 `constexpr int NB_GRID_X = 81; … APX_ORDER = 7;`）
- 证据摘录:
  > （:17）// - SIP AP/BP: 网格反变换法 (NB_GRID=7)
  > （:402-403）const int NB_GRID = 41;       // 每轴网格点数 (整改: 7 -> 41)
  > （:238）…（文件头同口径的第二处 7×7 陈述，与 :402 的 41 并存）
- 权威依据: 宪章 §12.2（注释只写非显然决定且须与实现一致）、§12.3-1（文档-代码一致）
- 问题说明: 文件顶部是新人读到的第一段设计说明，其参数在 WCS-002 整改后未回写；同一文件里同时存在「7×7」的两处陈述与「41×41/阶 5、81×81/阶 7」的两处实现。M1a 已在 SCI/ALG/注册表面定稿该冲突（其位置列的是 ASTROMETRY:57/:125 与实现常量），本条只定稿**同文件头注释**这一残余载体（未被 M1a 位置列覆盖）。
- 影响: 按顶部注释推理逆向 SIP 精度前提会低估畸变场反拟合误差量级，并与 M1a 定稿的 1e-4 px 门冲突面叠加，形成「读注释即得错误口径」的路径。
- 建议处置: 删除文件头里的实现细节参数（只留 SCI/ALG 条款指针与 DISP 登记号），或按实现同步为 41×41/阶 5 与 81×81/阶 7，并登记 AP 与 BP 通道参数不同的事实（若为有意分叉须写理由）。
- 置信度: 高
- 来源: L13-007
- related: M1a-C-001（SCI/ALG/注册表面定稿点）、M6a-D-001（同文件的未登记裁决请求）

### M6a-D-004 公共头把「不影响 FP64 全链路精度」写成结论性宣称，其依据（仅 orchestrator 旧通道不调用）已被两条已接线的 FP64 生产路径推翻；同头另用控制包简写锚指节（来源 L13-009）

- 类别码: D_COMMENT
- 优先级: P1
- 位置:
  - `lib/calibration/include/astro_calibration.h::FP64 ABI 段`（复核时点 :110-119，含「(这些函数用于 master 帧预生成与坏点修复, orchestrator 的 run_stage_calibrate 不调用它们, 因此不影响 FP64 全链路精度。)」与 :112 的「R10」历史任务号；:8 同类）
  - 反证一：`lib/cosmetic/src/module_entry.cpp::is_f64 分支`（复核时点 :800-808 实调 `ac_correct_frame_f64`，同文件 :13 自述 1:1 转发 legacy AC_API）
  - 反证二：`lib/calibration/src/module_entry.cpp`（复核时点 :970/:975/:979/:1004/:1026 实调 `ac_generate_master_flat_f64/bias_f64/dark_f64` 与 `ac_calibrate_frame_f64`、`ac_correct_frame_f64`）
  - 实现面：`lib/calibration/src/ac_api.cpp`（复核时点 :141-147、:232-267；:241 注释「坏点修复 (mask 操作): 转 float 调用 f32 实现, 输出转回 double」）
  - 登记面：`docs/standards/STANDARDS_REGISTRY.md::DISP-COS-004`（已登记「非真双精度、统计/mask/插值全程 f32」）
- 证据摘录:
  > （astro_calibration.h:112-118）双精度 ABI 改造 (R10): FP64 模式下全链路使用 double, 不降级到 float32。…统计/mask 操作, 内部将 double 输入转 float 调用 f32 实现, 输出转回 double。(这些函数用于 master 帧预生成与坏点修复, orchestrator 的 run_stage_calibrate 不调用它们, 因此不影响 FP64 全链路精度。)
  > （cosmetic/src/module_entry.cpp:802）rc = ac_correct_frame_f64(
  > （ac_api.cpp:241）// 坏点修复 (mask 操作): 转 float 调用 f32 实现, 输出转回 double
- 权威依据: 宪章 §5.3（校准/母版精度合同）、§4.1/§4.3（dtype 与量纲不得以措辞掩盖）、§12.2（注释不得与实现/合同矛盾）、§12.3-10（禁历史任务号 R10 入活动注释）
- 问题说明: 降级执行本身**已登记**（DISP-COS-004），未登记的是这段头注释给出的结论——「不影响 FP64 全链路精度」。该结论的论证前提是「只有 orchestrator 的 run_stage_calibrate 调用方」，而 `astrocs_p1_cosmetic`/`astrocs_p1_calibration` 两个生产 SHARED 模块的 FP64 op 都直接调用这些函数（根 CMakeLists.txt :191/:199 编入交付图，节点表亦经 cosmetic_correct/ calibrate 的 f64 分支）⇒ 前提失效，结论变成对全部 FP64 通道的错误担保。同段 :112 的「R10」是历史任务号（§12.2 禁止形态）。
- 影响: 以该公共头为依据做精度判断的人会系统性高估坏点修复/母版通道的位精度（整帧实际经 float32）；「不影响全链路精度」这类总括句在合同头里等价于未登记的偏差豁免。
- 建议处置: 删总括结论，逐函数写事实（内部转 f32、位精度损失范围、指向 DISP-COS-004）；把「哪些调用方不调用」改成路径限定并显式列出已接线调用方；删 R10 等历史轮次号。
- 置信度: 高
- 来源: L13-009
- related: M6a-C-003（现状宣称三源对账）、M6a-D-005（同族：C 头承诺与实现语义不符）、M3a（L05 域主定稿点）

### M6a-D-005 C 合同头把 fill 写成纯函数并承诺「clamp 到 variance_floor」，真实语义依赖进程级指针键侧表并在未登记时静默退 1e-12；禁拷贝/线程/生命周期约束只在内部适配头披露（来源 L13-010）

- 类别码: D_COMMENT
- 优先级: P1
- 位置:
  - `lib/snr_estimator/cpp/include/snr_estimator.h::snr_noise_model_v1_fill 段`（复核时点 :163-165「* 像素 (像素中心语义)。预测 <0 clamp 到 variance_floor。」）
  - `lib/snr_estimator/cpp/src/noise_model.cpp::g_model_floor`（复核时点 :32 `static std::unordered_map<const NoiseWeightModelV1*, double> g_model_floor;`；:126 写入；:401-407 读取与回退 `double floor = 1e-12; auto it = g_model_floor.find(m); …`；:431-433 erase）
  - `lib/snr_estimator/include/astrocs/noise/types.h::op 词表注释`（复核时点 :52-61：build→fill→free 单事务、影子实例不在注册表 → floor 回退 1e-12、DISP-NOISE-002「如实登记不消化」）
- 证据摘录:
  > （snr_estimator.h:165）* 像素 (像素中心语义)。预测 <0 clamp 到 variance_floor。
  > （noise_model.cpp:401-404）// W1-NOISE-002: floor propagation via internal registry (model ptr keyed), fallback 1e-12 → double floor = 1e-12; auto it = g_model_floor.find(m); if (it != g_model_floor.end() && it->second > 0.0) floor = it->second;
  > （types.h:59-61）影子实例不在 g_model_floor 注册表 → floor 回退 1e-12, DISP-NOISE-002 build/fill floor 语义不一致…如实登记不消化
- 权威依据: 宪章 §4.1（variance/ivar 的无效值与下界语义不得含糊）、§12.2（公共头须写所有权/生命周期/线程安全，且注释须与实现一致）、§12.3-8
- 问题说明: C ABI 用户头的措辞让调用方以为 fill 只依赖入参模型对象；实际 floor 取自**进程级、以指针为键**的侧表，于是：①模型按值拷贝、经 manifest round-trip 重建、或跨 build/free 事务外使用时，floor 静默变成 1e-12（改变 variance/ivar 输出数值）；②侧表在 build/free 时被写/删（fill 读全局态、并发边界未写）、且结构体不可拷贝这一约束在用户头零字未提，只在 `types.h`（适配器内部头）里以「如实登记不消化」的方式承认；③DISP-NOISE-002 登记的是**跨 ABI 现状**，不是用户头的义务，登记不能替代合同头的义务说明；④像素循环内每像素做一次哈希查找（热路径开销，注释未声明为已知代价）。
- 影响: Phase2 的科学权重输入（variance/ivar 平面）的 floor 语义随调用形态漂移且无诊断；以该头为唯一依据的外部调用方无法自知落入 1e-12 分支。
- 建议处置: ①把 floor 移进模型结构体字段（消灭侧表），或至少在用户头写明「fill 依赖 build 期建立的进程级侧表；模型不得拷贝/重建；未登记时退 1e-12」；②把 1e-12 回退本身登记为偏差（当前只有 DISP-NOISE-002 覆盖语义不一致，未覆盖回退值来源）；③侧表查找移出像素循环。
- 置信度: 高
- 来源: L13-010
- related: M3a（L07 噪声域主定稿点）、M6a-D-004（同族：合同头承诺与实现语义不符）

### M6a-D-006 「Phase1/2/3 节点唯一真实 operation 委托」的权威总述与同文件实现和根 CMake 归属四处不符（不接 dpsf_fit_batch / 求解器未接线 / drizzle 宿主库写错 / writer 写成 aio_write_fits），且与同文件后段的取代注释并存无标记（来源 L13-014，含本次对账加强）

- 类别码: D_COMMENT
- 优先级: P1
- 位置: `lib/core/src/module_adapters.cpp::文件头 委托总述`（复核时点 :2-12，逐项 :5-6 star-psf、:7-8 wcs-platesolve、:11 drizzle、:12 writer、:23-25 write/原子发布）vs 同文件实现 `::p1_op_star_psf`（:1474-1494，:1485 实调 `dpsf_fit_batch_f64`，manifest :1563 entry 串）、`::p1_op_wcs_platesolve`（:1905 `sdet_create`、:1918 `ipv_solve_create`、:1933 `ipv_solve_from_memory_with_callback_d`）、`::p1_op_writer`（:2476/:2605/:2618 `aio_hips_product_begin/write_signal_support_tile/finalize`）、`::descriptor 组注释`（:524「工厂委托 P3Api session adapter(一站式执行)」、:759 同口径 for P2）与注册段（:5489-5522 已改 `make_p2/p3_node_module`）
- 反证（根构建面）: 根 `CMakeLists.txt::star-psf 口径`（:643-645 已写「口径更新: star-psf→dpsf_fit_batch_f64」）、`::p1_ipv 源集`（:502-520 把 ipv 13 TU + gaia_client 编入 `astrocs_p1_ipv` 并 PUBLIC 链入 `astrocs_module_adapters` :646-649）、`::drizzle 归属`（`hp_drizzle_run` 在 `lib/healpix_db/healpix_drizzle/hp_drizzle_api.cpp`，编入 `astrocs_drizzle` STATIC :408-417；`lib/drizzle` 是 `astrocs_p1_drizzle` SHARED :179-184）
- 证据摘录:
  > （:5-6）star-psf → StarDetector::detect (lib/phase1/stars) + StarSource / fwhm_px/ellipticity 作 PSF 特性输出（不接 dpsf_fit_batch）
  > （:44-45）// P1-001 口径更新: 真实求解器/拟合器/HiPS writer 生产头（模块库零 diff 只读调用） #include "dynamic_psf.h"     // lib/dynamic_psf: dpsf_fit_batch_f64
  > （:11）drizzle → hp_drizzle_run (lib/drizzle 静态库)
  > （:12）writer → aio_write_fits (lib/astro_image_io)
  > （:524）工厂委托 P3Api session adapter(一站式执行)
- 权威依据: 宪章 §12.2（注释须与实现一致）、§12.3-1/-5（模块注册表/委托关系一致）、§14.2（唯一权威实现面）
- 问题说明: 该文件头被当作「节点→真实实现」的权威映射表，但它停留在上一代：①star-psf 明确写「不接 dpsf_fit_batch」，同文件 20 余行之后就在接（且根 CMake 注释已改口径）；②wcs-platesolve 写「真实求解器接线归各 IMPL 任务」，而 ipv 链已编入并直调；③drizzle 的宿主库名把 SHARED 适配层与 STATIC 内核弄反（符号与调用相符，库归属错）；④writer 写成 `aio_write_fits`，实际是 HIPS product/tile/finalize 三步（另 :23-25 的「AIO-002 原子发布内建」由 M6a-C-003 定稿）；⑤descriptor 组注释仍称「委托 session adapter 一站式执行」，而同文件注册段明文说明该形态正是被整改的违规。同文件两代注释叠加、无取代标记，是本域「行锚与现状句各自漂移」的典型。
- 影响: 以头注释为语义源的评审/机器一致性检查（§12.3-1）对模块-符号委托关系产生系统性误差；新人据 :5-6 会认为 PSF 通道未接生产拟合器，反向误导接线整改。
- 建议处置: 重写委托总述与实现一致，并加「取代」标记或改由 descriptor/节点表单点生成（禁手写第二份映射）；库归属写 target 名而非目录名。
- 置信度: 高
- 来源: L13-014
- related: M6a-C-003（现状宣称三源对账族）、M3b-E-01（sdet/dpsf 面锚漂移定稿点）、M5b（模块注册表/构建面对账门）
### M6a-D-010 横向抽查「注释三要素」齐备性：被消费者/生产者标识缺失率最高（55%），算法来源次之（15%）；缺口最重的头恰是合同头本身（astro_calibration.h、star_detector.h），另含 photometry_apply 返回码表缺实现中的 -5 与 ac_calibrate_frame 无单位/生命周期说明（来源 L13-018 的未覆盖部分 + 本次 20 样本抽查；矩阵全量定稿归 L28，本条只落种子复核）

- 类别码: D_COMMENT
- 优先级: P1（三要素是宪章 §12.2 的必写项，且缺失集中在对外 ABI 合同头 ⇒ 高于一般卫生）
- 位置:
  - 抽查样本 20 个（跨 Phase1/2/3 + 公共基建）：`lib/calibration/include/astro_calibration.h::ac_calibrate_frame` / `::ac_correct_frame`；`lib/star_detector/include/star_detector.h::SDetParams` / `::sdet_create` / `::sdet_detect` / `::sdet_detect_ex_f64`；`lib/dynamic_psf/include/dynamic_psf.h::dpsf_fit_batch_f32` / `::dpsf_fit_batch_f64`；`lib/plate_solve/cpp/ipv/include/ipv_api.h::ipv_solve_from_memory` / `::ipv_solve_from_memory_with_callback_d`；`lib/photometric_calib/cpp/include/PhotometryCalculator.h::measure`；`lib/drizzle/include/astrocs/drizzle/types.h::DrizzleConfig`；`lib/common/healpix/healpix_core.h::ang2pix_nest` / `::pix2ang_nest` / `::neighbors`；`lib/snr_estimator/cpp/include/snr_estimator.h::snr_extract_model_v2`；`include/astrocs/common_abi_v1.h::acs_module_descriptor_v1`；`lib/phase2/include/astro/phase2/coverage.h::p2_coverage_build` / `::sampler.h::p2_sample_controls` / `::upm.h::p2_upm_build_geo`；`lib/phase3_session/p3_wcs.h::p3_wcs_make` / `::p3_resample.h::p3_sample_bilinear_ex` / `::p3_output.h::p3_output_write_atomic_ex` / `::hips/aio_hips_reader.h::aio_hips_read_tile_f32`
  - 判定结果（A＝算法来源 SCI/ALG/DISP 锚；B＝输入来源/单位；C＝被消费者/生产者标识）：A 缺 3/20（15%）、B 缺 2/20（10%）、C 缺 11/20（55%）；三项全缺 2/20，为 `astro_calibration.h::ac_calibrate_frame` 与 `ac_correct_frame`（同文件另一符号 `ac_generate_master_dark_f64` 亦三项全缺）；`star_detector.h` 的 9 个导出函数整体无 A/C、无单位/值域/前后置（与 M3b-D-01④ 同一位置，本条只补量化事实）
  - 零命中样本（作为对照）：`PhotometryCalculator::measure`、`ipv_solve_from_memory*`、`p3_wcs_make`、`p3_sample_bilinear_ex`、`p3_output_write_atomic_ex`、`aio_hips_read_tile_f32`、`p2_coverage_build`、`p2_sample_controls`、`p2_upm_build_geo`、`healpix_core 三符号`、`acs_module_descriptor_v1` 的 A 项均命中真实存在的 SCI-/ALG-/DATA- token（零悬空）
  - 未披露的实现分支：`lib/calibration/src/photometry_apply.h::返回码表`（:24-31 止于 -4）vs `photometry_apply.cpp`（:49-51 另有 `return -5;` 即 photscal≤0）；`astro_calibration.h::ac_calibrate_frame` 无输入单位/母版可用性/dtype 前提
- 证据摘录:
  > （photometry_apply.h:27-31）// 返回: 0=成功, <0=失败 -1: light == nullptr -2: out == nullptr -3: w/h 非法 (<=0) -4: photscal 非有限值 (NaN/Inf)
  > （photometry_apply.cpp:49-51）if (photscal <= 0.0) { fprintf(stderr, …); return -5; }
  > （star_detector.h:31-36）SDET_EXPORT StarDetectorHandle sdet_create(const SDetParams *params); …（SDetParams 九字段无单位/默认值/值域，函数无返回语义/所有权/线程安全说明）
- 权威依据: 宪章 §12.2（注释只解释单位、数学原因、前后置条件、所有权、线程安全、生命周期）、§4.1（每个量必须写明单位）、§12.3-1/-3；`docs/standards/COMMENT_STANDARD.md:10-17`（八类必写要素）
- 问题说明: 抽查是种子（20 样本），结论有三点稳：①**「被消费者」标识最缺**（55%）——即 §12.2 的前后置/生命周期面，导致同一 ABI 头的语义只能靠读实现获得，本域多条 finding（D-004/D-005/D-007）都是该缺口的外溢；②算法来源面整体健康（SCI/ALG ID 零悬空），说明问题不是「不写 ID」而是「写错/写旧 ID 与散文当结论」；③返回码表与实现分支不同步（缺 -5）是典型的「合同头不可据以写错误处理」。另：样本中发现一处 ALG ID 交叉误标（`lib/phase3_session/p3_wcs.h::p3_wcs_make 注释` :41 引 ALG-P3-003，而该文件头 :1 自称 ALG-P3-002）⇒ 已入 _merge §10 待复核（不臆断谁错）。
- 影响: 合同头的可评审性；调用方按头写错误分支/单位假设会系统性偏；机器门无法用注释核对一致性（§12.3 的语义源缺失）。
- 建议处置: ①按 COMMENT_STANDARD:10-17 的八类要素给 `star_detector.h`、`astro_calibration.h` 补逐参数注释（从 README/ALG 指锚，禁复制公式）；②返回码枚举化并加门：头内声明的码值集合必须覆盖实现 return 语句集合；③三要素全仓矩阵以本条为种子交 L28 定稿（本条不抢定稿）。
- 置信度: 高（20 样本逐条 read；缺项与零命中项均给出符号级定位）
- 来源: L13-018（残余）+ 本域横向抽查
- related: L28（全仓三要素矩阵定稿点，本条为种子复核）、M3b-D-01（star_detector.h/dynamic_psf.h 面定稿点）、M6a-D-004、M6a-E-001
