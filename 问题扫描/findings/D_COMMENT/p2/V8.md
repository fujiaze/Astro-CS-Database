# V8 · P2 一条

### V8-N-03（P2·判据①自报计数）README「4 处计算」与它自己括号里列的 5 个名字互相否定
- `lib/snr_estimator/README.md:212-214`（`b0353303` 新增）：「生产路径 `snr_estimator.cpp` 的 **4 处**计算（`snr_estimate`/`snr_estimate_f64`/`snr_extract_model`/`_v2`/`_v3`）统一改为调用 `snr_source_snr_f64`」——括号里列的是 **5 个名字**。
- 复算：`grep snr_source_snr_f64 snr_estimator.cpp` → **唯一直调点 `:76`（在本地 helper 内）**；`scripts_v8_snr_sites.py` 实测 helper 调用点 **5 处 `[188,357,616,740,851]`，分属 5 个导出函数（distinct=5）**。⇒ **三处口径（5 个名字 / 5 个调用点 / 1 个直调点）无一等于「4」**。
- 这正是工单 **`E7`（在册计数禁字面量钉死）** 的文档侧对应物：计数应写成"由 5 个导出函数经单一 helper 收敛"或直接引用可复算来源。
- **related** `V12-N-13`、`E7`、`V4-N-16`（同类自报计数与实况不同源）；亦记我自身同一错误两次（工单「写六条实为七条」`a44b159f`）。
