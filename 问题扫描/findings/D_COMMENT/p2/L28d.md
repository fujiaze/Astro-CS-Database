# L28d 定稿 · D_COMMENT × P2（生产 .cpp/.c 实现体「注释撒谎」面 · 第三轮补轴）

> 代理：L28d。判据沿用 L28b §1.3（R-6a/b/c）+ 本轴 R-6c-unit 收紧；语料与命中计数见 问题扫描/_cache/L28d.md §0。

### L28d-D-002 边细分阈值块注释仍写整改前口径（×1e-12 / 1.6e-14 rad / 每边 ~256 段），与同函数 20 行后的「新阈值 1e-6」与代码相反
- 类别: D_COMMENT
- 建议优先级: P2（同文件两代注释互斥，代码侧行为正确；先例同形状 M6a-D-014 定 P2）
- 位置: lib/healpix_db/healpix_drizzle/spherical_overlap.cpp:550-555（函数上方主说明块）；被否证的实现 :576-577；同文件已订正口径 :565、:572-573；常量 :550 HP_ADAPTIVE_MAX_DEPTH=8
- 证据摘录:
  > （:552-553）// hp_epsilon = hp_res_rad * 1e-12, 其中 hp_res_rad = sqrt(π/(3·Ns²))
  > // NSIDE=64 时 hp_epsilon ≈ 1.6e-14 rad, 每条边细分到 ~256 段
  > （:565）    // 修复: HEALPix 边细分阈值从 1e-12 改为 1e-6
  > （:572-573）    // 新阈值 1e-6 (相对值): NSIDE=65536→1.56e-11, 1-2次二分收敛 (2-4段/边);
  > （:577）    double hp_epsilon = hp_res_rad * 1e-6;
- 权威依据: 宪章 §12.2（注释须与实现一致；禁两代口径并存无取代标记）、§12.3-1；docs/algorithms/DRIZZLE_GEOMETRY.md（overlap 面积路径合同）
- 问题说明: 主说明块（紧贴函数体、读者先看到的那一段）把**已被本文件自己宣布废止**的口径当作现行事实陈述三件事：ε=hp_res×1e-12、NSIDE=64 时 ε≈1.6e-14 rad、每条边细分到 ~256 段。①代码 :577 用 1e-6，相差 10⁶；②按块注释自己的公式复算，nside=64 的 hp_res_rad = √(π/(3·64²)) = 1.5989e-2 rad，×1e-12 才得 1.6e-14 —— 该数字只在旧口径下成立；③「~256 段/边」= 2^8（MAX_DEPTH 截断），正是 :566「永远不收敛」与 :570-571 列出的 1024 顶点/像素、332B 次操作爆炸场景，而 :572 明确新口径收敛到「1-2 次二分（2-4 段/边）」。同一函数内两套互斥数值口径、无取代标记，且旧口径出现在权威位置。
- 影响: 现状数值正确（代码走 1e-6）；危害在维护面——按主说明块「恢复/复核」该阈值即回到永不收敛路径（性能爆炸 3~4 个数量级，:571 已量化），且该块同时被 :551「改进2」当作现行描述；本文件另有多处 ≈ 断言复算通过（见 §0.4），故此块是唯一失真块。
- 建议处置: 把 :550-555 改写为现行口径（ε=hp_res_rad×1e-6；给出 NSIDE=64/65536 两档实算值；删除「~256 段」或改「2-4 段/边」），整改史只留 DISP 指针不入活动注释（§12.2）；机器门同 R-6c-unit：注释中的乘子/数量级与同函数常量表达式复算比对。
- 置信度: 高（:577 与 :552 逐字互斥；1.6e-14/256 段两个数字均由块注释自身公式复算）
- related: M6a-D-014（同文件两代口径并存形态定稿点）、L04-012/M2a-D-1（同文件 1.25 vs 1.15 常数群，本条不重复该事实）

### L28d-D-003 CD 阻尼过渡带的上限注释「1e-4 → ~22」与实现的对数插值上限 1e6 相差约 4.5 个数量级，同函数两处注释互斥
- 类别: D_COMMENT
- 建议优先级: P2
- 位置: lib/plate_solve/cpp/ipv/src/ipv_robust_refine.cpp:613-617（函数上方说明块）；被否证实现 :618-629（log10 插值）；同函数内已订正口径 :623；参数默认值 lib/plate_solve/cpp/ipv/include/ipv_robust_refine.h:49-52
- 证据摘录:
  > （:615）// 1%-3% → 指数过渡 (1e-4 → ~22)
  > （:623）    // 1%-3% 指数过渡: 从 cd_damp_base (1e-4) 到 cd_damp_freeze (1e6) 的对数插值
  > （:626-629）    double log_lo = std::log10(params.cd_damp_base); double log_hi = std::log10(params.cd_damp_freeze); double log_damp = log_lo + t * (log_hi - log_lo); return std::pow(10.0, log_damp);
  > （robust_refine.h:51-52）double cd_damp_base = 1e-4; // 1% 时的阻尼    double cd_damp_freeze = 1e6;  // > 3% 时的阻尼
- 权威依据: 宪章 §12.2（边界条件与非显然决定须写对；注释须与实现一致）、§12.3-1
- 问题说明: 过渡带是 t∈(0,1) 上 10^(-4+10t)：t=0.5 时约 3.2e1（≈"22" 所在的那个量级，对应 rel_change≈2.07%），t→1⁻ 时趋于 1e6；块注释把**中段某值**写成过渡带的**上限**，与 8 行之后 :623 写的「到 cd_damp_freeze (1e6)」直接互斥。同 :616「> 3% → 1e6 (冻结)」与 :615 的上限 ~22 也接不上（若上限真为 22，则 3% 处跳变 4.5 个数量级）。该阻尼项直接进入正规方程 (J^T W J + D)Δp = −J^T W r 的线性项对角，读者按 ~22 估阻尼强度会低估 4~5 个数量级。
- 影响: 无行为后果（代码自洽），危害在按注释调参/复核 CD 冻结逻辑的场合；属 L28b R-6c-unit 判据「注释声明的阈值与代码算式不符」的实现体内新例。
- 建议处置: :615 改为「1%→1e-4、t 线性对数插值、3%⁻→1e6（与 :616 的硬冻结同值）」，或直接把过渡带公式 ε(t)=10^(-4+10t) 写进注释；禁以单个中段样本冒充端点值。
- 置信度: 高（插值式与默认值逐字复现；"~22" 对应 t=0.53 由本域重算）
- related: L28b-D-003（同一「注释承载的断言不可复算」族，不同文件不同判据子式）、M6a-D-012（声明与强制点脱节形态，本条不重复）

