# M3 定稿 · findings · D_COMMENT · P2（来源切片 L05 + L07）
> 合并代理 M3｜来源 L05 + L07｜逐条重读原文复核；处置表见 `../../_merge/M3.md`。

### M3-D-004 `master_generator.cpp` 注释引用不存在的合同小节，并以任务号流水充当契约依据
- 类别: D_COMMENT ｜ 优先级: P2 ｜ 来源: L05-015
- 位置: `lib/calibration/src/master_generator.cpp::generate_master_flat 步骤3 前注释`（复核时点 :171-175）；`::generate_master 上方注释`（:89-91）；`lib/phase1_session/p1_session.cpp::阶段注释块`（复核时点 :290、:323、:328-331、:353-356、:392-394 等，含 `PROD-P0-001`/`CORE-DEG-001`/`P1-R14-3`/`P1-001`/`CORE-R12-01`）
- 证据摘录（复核时点重读）:
  > // 与 docs/science/CALIBRATION.md §flat_norm 定义完全一致：（master_generator.cpp:172）
  > //   flat_norm = max(flat / median(flat), 0.1)  (median→1.0, 逐像素 floor 0.1)（:173）
  > // PROD-P0-001 修复: master_bias 是 dark 的基底…（p1_session.cpp:290）
  > // P1-001 (attempt 2): complete 门 fail-closed（PROD-P0-001 Phase1 侧; 宪章 §16.2/§18.3 不冒充完成）…（p1_session.cpp:438-443）
- 权威依据: 宪章 §12.2（禁止任务号/版本/控制包流水叙述充当规范依据）、§12.3-1（锚点必须可定位）
- 问题说明: ① `docs/science/CALIBRATION.md` 只有 §1-§15 编号小节，**不存在名为 `flat_norm` 的小节**（`flat_norm` 是 §5 公式块内的符号），故该「与 §flat_norm 完全一致」锚点不可定位（真实可核对的是 §5 公式块 :44-51，而它并不与 `generate_master_flat` 一致——见 M3-A-003）；② 两文件的注释以 `PROD-P0-001 / CORE-DEG-001 / B13-R13-7 / P12-001 / W1-NOISE-002` 一类任务号流水作为「依据」表述，宪章 §12.2 明禁；③ 注释声称的「完全一致」正是本域最高价值的不一致点（M3-A-003），误导优先级高。
- 影响: 注释级；但 `:172` 是 M3-A-003 的入口线索，修正合同后须同步改写本注释。
- 建议处置: 锚点改指 `docs/science/CALIBRATION.md §5` 并把「完全一致」改为「归一发生在母版生成期（见 ALG-CAL-002 步骤 3）」；删除全部任务号流水（保留必要处改指 memory.md 与 DEViations 登记）。
- 置信度: 高 ｜ related: M3-A-003、M3-E-002、M3-D-006

---

### M3-D-005 cosmetic 注释与 ALG 描述一个不存在的 label-0 防误清机制，并把 method=1 写成 8 邻居双线性
- 类别: D_COMMENT ｜ 优先级: P2 ｜ 来源: L05-016
- 位置: `lib/calibration/src/cosmetic_corrector.cpp::文件头与 detect_hot/cold 注释`（复核时点 :14、:59-60；实际守卫 `labels[i] > 0` 在 :104/:109）；`::interpolate_pixels 上方注释`（:157-159）与实现（:169-218）；`docs/algorithms/COSMETIC_ALGORITHMS.md::§3 步 4 与 §13.1 锚表`（复核时点 :65-66、:47）；对照 `lib/cosmetic/README.md:::74-82`（已正确登记）
- 证据摘录（复核时点重读）:
  > // 背景 label 0 的 size 设为 max_size，防止全图被误标记（cosmetic_corrector.cpp:14；:59-60 重述）
  >         if (labels[i] > 0) { …（:104/:109 —— 实际机制：label 0 直接跳过，不参与 size 统计）
  > // method=1 (bilinear): 用8邻居双线性插值替换坏像素（:159）
  >     for (int k = 0; k < 4; ++k)（:199 —— 仅 4 正交方向；权重为 1/dist 反距离加权，非双线性）
  > - `interpolate_bilinear`（:95-101）为死代码…生产修复用 `interpolate_pixels`（:169-218，4 邻域 IDW）…（ALG-COS:47）
- 权威依据: 宪章 §12.2、§12.3-1；SCI-CAL-001 §8（修复策略行只登记 median 5×5，未登记 bilinear/IDW）
- 问题说明: ① 头注与函数注释描述「把背景 label 0 的 size 设为 max_size」这一保护并不存在：实现是把 label 0 视为背景直接跳过（`labels[i] > 0`），效果相近但机制不同，读注释者会去找不存在的赋值；② `method=1` 注释写「8 邻居双线性插值」，实际只做 4 正交方向的 `1/dist` 反距离加权（`dirs[8]` 数组只填了 ±1 四个方向），既非 8 邻居也非双线性；ALG-COS 与 `lib/cosmetic/README.md:74-82` 已把该事实正确登记（"4 邻域 IDW"、"bilinear 实为 4 方向 IDW"），故本条只剩源码注释层未订正。
- 影响: 阅读实现会误判修复邻域与算法形式（权重差异会影响局部结构）；与 ALG 已登记事实不一致的注释会持续误导。
- 建议处置: 注释按实现改写（「label 0 视作背景跳过」「method=1 = 4 正交方向 1/dist 反距离加权」），并把 `interpolate_pixels` 的分支数（`<2`/`<3` 直接保原值）一并写入 ALG。
- 置信度: 高 ｜ related: M3-D-004、M3-E-002、M3-I-001

---

### M3-D-006 `star_matcher.cpp`/`pc_api.cpp` 以任务号流水作注释主体，且三处科学表述与实现相反
- 类别: D_COMMENT ｜ 优先级: P2 ｜ 来源: L07-021
- 位置: `lib/photometric_calib/cpp/src/star_matcher.cpp::常量与阶段注释`（复核时点 :21-27、:173、:197/:209/:222/:240、:300/:345、:429/:471/:487/:591）；`lib/photometric_calib/cpp/src/pc_api.cpp::阶段注释`（:162、:331、:695、:969）；`lib/snr_estimator/cpp/src/noise_model.cpp::401`；`lib/snr_estimator/cpp/include/snr_estimator.h::279-283`
- 证据摘录（复核时点重读）:
  > // IRLS 收敛阈值 (|scale_new - scale_old| < 1e-6)（star_matcher.cpp:26）
  >             if (std::fabs(location - prev_location) < _IRLS_CONVERGE …（:516-519 —— 实际比较量是 location(dex)，不是 scale）
  > // GAP-013: robust_location 的 MAD 清洗语义是 3σ 截断（非 Tukey biweight）（star_matcher.cpp:173）
  > // MAD 清洗 (sigma clip)：逐通道独立，阈值为 ±3σ …（pc_api.cpp:162）
  > 主清洗已升级为星等一致性预过滤 + IRLS/Tukey 鲁棒估计…旧 robust_location 的 MAD 清洗仅用于其独立接口（ALG-PHOT:139-142）
  > // OpenMP 16 线程并行校正（pc_api.cpp:331；:695、:969 同）
  > #pragma omp parallel for schedule(static)（本文件内 `num_threads` grep = 0 命中）
  > // 修复: … 1609 个越界点…消除 1609 个越界点（snr_estimator.h:279-283 / noise_model.cpp:401「W1-NOISE-002」）
- 权威依据: 宪章 §12.2（禁任务号/审计流水注释；注释必须写不变量、前置条件、返回码语义，不得写完成过程）、§12.3-1、§10.4
- 问题说明: ① `star_matcher.cpp` 通篇以「阶段1/阶段1a/阶段2/阶段4/阶段5」「P12-001 修复: …」为注释主体（十余处），`pc_api.cpp` 同型（「P0-2: …」「P1-PHOT-004: …」），`snr_estimator` 侧把「1609 个越界点」等**审计结论**写进结构体注释——宪章明禁此类完成流水；② 三处内容错误：`:26` 的收敛判据把 `location` 写成 `scale`；`:173` 与 `pc_api.cpp:162` 仍把「MAD 3σ 截断」描述为生产清洗路径（ALG 已订正为「I 域 σ-clip 为清洗、生产测光清洗已升级为 IRLS/Tukey」），会引导后续维护者把测光清洗改回 3σ；`pc_api.cpp:331/:695/:969` 的「OpenMP 16 线程」在本文件内不存在 `num_threads` 参数（线程数由 OpenMP ICV/Runtime 预算决定），属未实现承诺。
- 影响: 注释层；「MAD 清洗」与「16 线程」两条与 M3-D-001/M5a-C-002 同族（后者已把 `pc_api.cpp:331` 事实并入其定稿，本条不重复登记该单一事实，只登记 star_matcher/流水面）。
- 建议处置: 按 §12.2 全量清理任务号流水（去向 memory.md/台账）；`:26` 改为「|Δlocation|<1e-6」；`:173`/:162 改指 ALG-PHOT 现行清洗描述；「16 线程」改「并行度由 OpenMP ICV（Runtime 预算）决定」。
- 置信度: 高 ｜ related: M3-D-001、M3-D-004、M3-A-008、M5a-C-002（`pc_api.cpp:331` 定稿点）
