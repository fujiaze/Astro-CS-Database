# V6 复核新发现 · P2（注释自述不实）

## V6-N-03 新写注释两处不实自述：把符号宿主指向根本没有该符号的文件，并把两行宏值比较说成 ABI 形参证明
- **ID**：V6-N-03 ｜ **类别**：D_COMMENT ｜ **优先级**：**P2**
- **位置与复算**：
  ① `lib/photometric_calib/cpp/src/star_matcher.h:6-7`（本批新增）称 quality_flags「与 `photometric_calib.h` 的 `PC_QF_*`/`SNR_QF_*` 同值，见其头部注释」。复算：该公共头 `grep -c` → `quality_flags` **0**、`PC_QF` **0**、`SNR_QF` **0**（全仓 `git ls-files '*photometric_calib.h'` 仅一枚，且本批未改它）。真宿主：`PC_QF_*` 唯一在 `star_matcher.h` 自身，`SNR_QF_*` 在 `lib/snr_estimator/cpp/include/snr_estimator.h:411-417`。**值确实同**（`1<<1`/`1<<2`，V6 逐位比过），但**指向错了**。
  ② `lib/photometric_calib/tests/p1phot/p1phot_fixgates.cpp:258-260`（本批新增）称"编译期即证明 6 个导出均有 `quality_flags` 形参"。复算：公共头 `PC_API int pc_` 导出**恰 6 枚（数目对）**，但 6 枚形参列表**一个都没有 `quality_flags`**（`:106/:156/:189/:205/:231/:251` 全部止于 `PhotometricDiag* out_diag`）；该组实际断言只有两行宏值 `CHECK`。⇒ **既非形参存在性、也非 6 个导出**。
- **问题说明**：②尤其要紧——它与 `M3-C-002`「ABI 按裁决完整回退」的处置**本身一致（回退是对的）**，但**注释没跟着回退**，留下一句"读起来像 ABI 已带质量位"的假陈述 ⇒ 正是簇 1「注释与合同互斥」的新实例，会让下一个读者据此认为质量位已进 ABI。
- **依据条款**：宪章 §12.3（注释/声明须可机器核验、R-6a 自报可复算、R-6b 锚须有宿主、R-6c 被指为权威须回写 L1）、§17.2。
- **建议处置**：①改指 `snr_estimator.h:411-417` 或直接以 `DATA_SEMANTICS` 的 quality_flags 位集为准；②把注释改为实际覆盖（"宏值与 `1<<1`/`1<<2` 一致"），或在公共 ABI 真带质量位前**不得**保留"6 个导出均有该形参"字样。
- **取证口径**：V6 逐位/逐形参复算 ｜ **状态**：OPEN ｜ **置信度**：高 ｜ **related**：**M3-C-002**（其 PARTIAL 残余描述真实）、L28d（注释自述不实族）、簇 1
