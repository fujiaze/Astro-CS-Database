# M6a 定稿 · D_COMMENT × P2（来源切片 L13 + L14；注释质量横向 / Phase1-3）

> 代理：M6a。只读取证；位置一律 path::符号。四态处置、并档与移交见 问题扫描/_merge/M6a.md。

### M6a-D-007 同一公共头内 PSF 拟合状态被两套互斥口径注释（质量位「status==0 或 3 有效」vs 逐星剔除「status!=0 全拒」），宽松口径把「迭代不收敛」当 OK 位（来源 L13-011 的注释层残余；科学归属移交 M3a）

- 类别码: D_COMMENT
- 优先级: P2（原 L13-011 报 P1；复算后改判：现行两条生产 SNR 通道一律 `status != 0 → 剔除`，宽松口径只出现在遗留 `snr_extract_model*` 族与不在根交付图的 `lib/orchestrator` ⇒ 无数值后果证据，按规程 §2 降 P2；口径孰为合同意图仍待裁，见 _merge §10）
- 位置: `lib/snr_estimator/cpp/include/snr_estimator.h::SnrQualityFlagBits`（复核时点 :337 `SNR_QF_PSF_OK = 1u << 0,  // PSF 拟合状态有效 (status==0 或 3)`）、`::PsfFitQualityRow.fit_status`（:56-57 布局 + :77「0=ok, 1=rejected(status!=0)」）、`::snr_extract_model 前言`（:398「有效星条件: status==0, A>B, …」）、`::snr_extract_model_v3 前言`（:417「有效星条件与 v2 一致」）；消费侧 `lib/orchestrator/cpp/src/orchestrator.cpp`（复核时点 :4392 `if (psf_status == 0.0 || psf_status == 3.0) qf |= SNR_QF_PSF_OK;`）；实现侧 `lib/snr_estimator/cpp/src/snr_estimator.cpp`（复核时点 :104/:114、:274/:281、:533/:540、:657/:659、:761/:763 一律 `status != 0 → 跳过`）
- 证据摘录:
  > （:337）SNR_QF_PSF_OK          = 1u << 0,  // PSF 拟合状态有效 (status==0 或 3)
  > （:77）uint32_t fit_status;        // 0=ok, 1=rejected(status!=0), …
  > （orchestrator:4392）if (psf_status == 0.0 || psf_status == 3.0) qf |= SNR_QF_PSF_OK;
  > （snr_estimator.cpp:281）if (status != 0.0) { ++n_skip_status; continue; }
- 权威依据: DATA-P1-PSF §15.2（status 值域 0..3；PHOTOMETRIC 仅 status=0 入匹配）；`lib/dynamic_psf/include/dynamic_psf.h::DPSF_PSF_STATUS`（3 = 分配失败/迭代不收敛类失败码）；宪章 §4.1（质量/支持度不得混淆）、§12.2
- 问题说明: 一个头里三处写「status!=0 即 rejected/剔除」，另一处把 3 解释为「有效」；两口径无一句说明分叉理由或适用范围。状态 3 在生产枚举里是失败码，若质量位被下游当剔星依据，则收敛失败星带 OK 位进入 Phase2 采样/权重判定。
- 影响: 现状生产路径（NoiseModel::estimate / snr_noise_model_v1 族，一律剔除非 0）不产错值；风险面在按 :337 实现新消费者的场合 + 文档口径混乱使 §12.3-1 一致性检查失去语义源。
- 建议处置: 头内显式写出 status 的完整值域与两种口径各自的适用面（或统一位定义为一处），并把该口径冲突登记偏差或按 DATA §15.2 收敛为「仅 0 为有效」；遗留 `snr_extract_model*` 族若保留「0 或 3」须注明理由与出处。
- 置信度: 高（四处文本与两处实现口径均已复算）
- 来源: L13-011（部分移交：科学权威归属裁决 → M3a/L07）
- related: M3a（L05/L07 噪声与质量域主定稿点）、M6a-D-008、M6a-G-001
### M6a-D-008 同一科学合同（SCI-NOISE-001）在两个数值不同的实现上各挂「唯一真实入口 / 现状唯一生产实现」宣称，注释层互斥；本条为该族的注释主登记点（来源 L13-013 的注释层残余）

- 类别码: D_COMMENT
- 优先级: P2（原 L13-011/013 报 P1；科学权威归属为 M3a 主落，本条只定稿注释互斥事实，故按「注释层残余」定 P2）
- 位置: `lib/core/src/module_adapters.cpp::op: estimate_snr 前言`（复核时点 :2142 区段「唯一真实入口 NoiseModel::estimate; SCI-NOISE-001 公式」）、`lib/phase1/noise/noise_model.cpp`（:1 头、:20 与 :38-42 `median + MAD -> sigma (SCI 公式)`）、`lib/snr_estimator/memory.md`（:35）与 `lib/snr_estimator/README.md`（:5）「现状唯一生产实现 `lib/snr_estimator/cpp/src/noise_model.cpp`」、`lib/snr_estimator/tests/p1noise/p1noise_tests_core.cpp`（:21「被测面: 现状唯一生产实现 snr_noise_model_v1(+_f64)/fill/free/scale_law/…」）
- 反证: 根 `CMakeLists.txt:210-215`（明文解除 `lib/snr_estimator` 子图，根图无 `astrocs_p1_noise`）、`:490-493`（`astrocs_phase1_noise` 源集为 `lib/phase1/noise/*`）、`lib/snr_estimator/tests/p1noise/CMakeLists.txt:37-40`（自述「根构建无 snr_estimator CMake 目标」，且把测试目标名写成 `astrocs_p1_noise_prod` 而实名 `p1noise_under_test`）
- 证据摘录:
  > （module_adapters:2142 区段）// ── op: estimate_snr（唯一真实入口 NoiseModel::estimate; SCI-NOISE-001 公式）──
  > （memory.md:35）现状唯一生产实现 lib/snr_estimator/cpp/src/noise_model.cpp
  > （p1noise/CMakeLists.txt:37-40）根构建无 snr_estimator CMake 目标 … 测试目标 astrocs_p1_noise_prod
- 权威依据: 宪章 §14.3（单一入口/禁双源冒充）、§12.2、§12.3-1（与注册表/构建面一致）；TRACEABILITY 矩阵的 SCI-NOISE-001 权威实现面字段
- 问题说明: 全帧标量 median+MAD（无星掩膜/patch/截尾）与 patch 级稳健三层模型是两个数值不同的实现，各自被一条注释宣布为唯一，且后者所在的子图已被根构建解除 ⇒ 「现状唯一生产实现」在交付面上为假；同文件的测试目标名也与实名不符。权威归属应由矩阵裁决（M3a 域），本条只定稿注释与构建面不符的这一半。
- 建议处置: 两处「唯一」注释按矩阵裁决改写（一处为权威、一处显式标注为参考/遗留实现并给 DISP 指针）；测试目标名按实名写。
- 置信度: 高
- 来源: L13-013（注释层残余；主事实移交 M3a/L07）
- related: M3a（L05/L07 主定稿点）、M6a-D-007、M6a-C-003（实例⑤同一事实，勿双计）
### M6a-D-009 §12.2 禁止形态（任务号/审计轮次/commit 哈希/修复流水）成为注释主干：Phase2/3 侧六目录实测 257 处命中 + Phase1 侧抽样清单，被登记偏差条目自身的注释也在复述历史（来源 L13-015 全域 + L14-003 量化，两切片并档）

- 类别码: D_COMMENT
- 优先级: P2
- 位置与量化（本代理复核时点重测，正则命中行计）:
  - Phase1 侧（L13）：`lib/plate_solve/cpp/ipv/src/ipv_sip.cpp`（:29/:171/:386 修复流水）、`ipv_solver.cpp`（:225-227「缺陷 (P0-1 修复前)…v1.0 曾误设为 6」、:1687）、`ipv_entry.cpp`（:31/:44-48/:74）、`lib/calibration/src/master_generator.cpp`（:171-175/:211/:238）、`ac_api.cpp`（:25/:89/:207）、`astro_calibration.h`（:8/:112 的「R10」）、`lib/cosmetic/include/astrocs/cosmetic/types.h`（:3-6 对齐先例含 commit 短哈希 `1959dc89`/`948dfcba`/`adf820ac`）、`lib/snr_estimator/include/astrocs/noise/types.h`（:3-5 同款）、`noise_model.cpp`（:401 「W1-NOISE-002:」）、`lib/common/crypto/sha256.cpp:35`、`lib/common/healpix/healpix_core.h`（:2/:16）、`lib/core/src/module_adapters.cpp`（:1-30/:189/:362/:1025/:1411）、`lib/star_detector/tests/p1star/p1star_test_main.hpp:5-6`、`lib/photometric_calib/cpp/test/test_p0_sip_order_guard.cpp:64`
  - Phase2/3 侧（L14）：`lib/phase2/src/sampler.cpp`（:157 CON-010、:629「hotfix：串行」、:881）、`execution_options.h:3`（CON-002）、`lib/phase3_session/hips_properties.h:1-2`（「06/TASK」）、`p3_resample.h:43`（「P3-006/DOC-003」）、`p3_output.cpp`（:141/:203-207/:241/:363「R10-C」）、`cli/cli_common.h`（:21/:38）、`runtime/monitoring/trace_feed.py`（:1/:3/:6）
  - 命中计数（同一正则族 `CON-0[0-9]|B2-A[0-9]+|P3-0[0-9][0-9]|RT-0[0-9][0-9]|V1[0-9]|B[0-9]+-P[0-9]|B13-R[0-9]+|AIO-0[0-9][0-9]|R10-C|R18|06/TASK|commit短哈希`）：`lib/phase2` 67、`lib/phase3_session` 39、`runtime` 43、`cli` 63、`lib/hips` 27、`providers` 18、`modules` 0（Phase1 侧本代理未做全量计数，按 L13 抽样清单登记）
- 证据摘录:
  > （ipv_sip.cpp:386）// 修复: 步长从 1e-10 改为 5e-6 (约 1.8 角秒), 避开 double 浮点噪声
  > （ipv_solver.cpp:225-227）// 缺陷 (P0-1 修复前): 无阶数上界检查… // 修复: 入口校验 order 范围 [0, SIP_MAX_ORDER], 越界抛异常。 // 修复: 默认设为 5。v1.0 曾误设为 6, 导致 6阶SIP匹配失败 (Siril 原值为 5)
  > （types.h:3-5）* 对齐先例: lib/hips/include/astrocs/hips/types.h (P1-HIPS-IMPL, 1959dc89)、… (P1-COS-IMPL 948dfcba / P1-CAL-IMPL adf820ac)。
- 权威依据: 宪章 §12.2（禁堆积历史版本号、任务编号、审计流水、代码复述）；`docs/standards/COMMENT_STANDARD.md:19-24`（禁 V[0-9]+/R[0-9]+/控制包/审计轮次/"本次修复"）；§12.3-10
- 问题说明: 多数命中在「解释 WHY」上是零信息（:629「hotfix：串行」、:363「R10-C:」），少数是**科学量变更史实留在注释**（SIP 步长 1e-10→5e-6、默认阶 6→5），读者无法分辨哪是现行口径、哪是被废止口径；commit 短哈希被写进合同头当「对齐先例」依据（不可核、且属 §12.2 明禁）。本条同时登记一个治理后果：该形态在全仓数百处存在却无任何门可拦（机制见 M6a-G-001）。
- 影响: 注释的信噪比与「读注释即知现行合同」的前提失效；机器一致性检查（§12.3-10）无实现 ⇒ 只能靠人工，且人工扫描的结论（本域 finding）也无法被防回归。
- 建议处置: ①一次性瘦身：只留单位/前后置/不变量/偏差 ID，过程史迁 `memory.md`/`CHANGELOG.md`；②把 CON-COMMENTS 门补齐（M6a-G-001 建议处置）后再做，否则同类堆积会立即复发；③对「科学量变更史」类注释，强制改为 DISP 指针而非散文。
- 计数口径: 六目录命中数为**同一正则族的 grep 命中行计数**（逐目录跑，含代码与注释行）；Phase1 侧未做全量计数，只给逐条 read 复现的抽样清单 ⇒ 「数百处」不作规模结论（全仓计数归 L28/前台）。「任务号入注释」的根因判词归 **M6b-G-001 / M6b-E-002** 与 **M7**，本条只登注释维实例与防线缺失的因果。
- 置信度: 高（命中计数为本代理实测；清单条目逐条 read 复现）
- 来源: L13-015 + L14-003（两切片同一违例形态并档；与 M4-D-03 的 Phase2 实例、M5a-D-002 的 runtime/ARCH 实例互补，勿双计）
- related: M6a-G-001（本形态无机检防线）、M6b-G-001（无据 VERIFIED 根因定稿点）、M7（不可达门归口）、M4-D-03、M5a-D-002、M3b-D-01
### M6a-D-011 cosmetic 修复核注释三说并存：函数头写「8 邻居双线性」、体内写「4 方向距离反比」、权威合同为 4 正交方向 IDW（来源 L13-016）

- 类别码: D_COMMENT
- 优先级: P2
- 位置: `lib/calibration/src/cosmetic_corrector.cpp::interpolate_pixels 前言`（复核时点 :157-159「method=0 (median): 用5x5中值滤波结果替换坏像素 / method=1 (bilinear): 用8邻居双线性插值替换坏像素」）vs 同函数实现（复核时点 :202-206「双线性插值：用4个方向最近的好像素做距离反比加权 / int dirs[4][2] = {{-1,0},{1,0},{0,-1},{0,1}}」）；权威 `docs/algorithms/COSMETIC_ALGORITHMS.md::§8`（复核时点 :106-117 登记 DISP-COS-003：名义 bilinear 实为 4 正交方向 1/dist 加权）
- 证据摘录:
  > （:159）// method=1 (bilinear): 用8邻居双线性插值替换坏像素
  > （:202-204）// 双线性插值：用4个方向最近的好像素做距离反比加权 / int dirs[4][2] = {{-1,0},{1,0},{0,-1},{0,1}};
- 权威依据: ALG-COS-003 §8（DISP-COS-003 已登记实现≠名义）；宪章 §12.2、§12.3-1
- 问题说明: 同一方法在相距 40 行的两处注释里分别是 8 邻居与 4 方向，且都自称「双线性」。「bilinear/IDW」名实不符已由 DISP-COS-003 登记（M2a 域），**新增**的错文是「8邻居」——它既不符合实现也不符合已登记口径，属注释二次劣化。
- 影响: 修复核语义误导（按 8 邻居改判邻域集即偏离 ALG，且不收敛到 DISP 记录）；坏像素修复是母版链路，邻域集变化会影响像素级结果。
- 建议处置: :159 改为「非 0 method：取 4 正交方向最近好像素做 1/dist 加权（名义 bilinear，实为 IDW；见 DISP-COS-003）」，并在 :202 保留唯一表述、删重复。
- 置信度: 高
- 来源: L13-016
- related: M2a（L04/L10 域 DISP-COS-003 定稿点）、M6a-C-003
### M6a-D-012 registry 声明的 per-projection 守卫字段无任何强制点：注释称「中心 |dec| 守卫/合法 FOV 声明」，实现读文件级常量且 FOV 字段零消费，selfcheck 只断言「>0」（来源 L14-010）

- 类别码: D_COMMENT
- 优先级: P2
- 位置: `lib/phase3_proj/p3_projection.h::P3ProjectionSpec`（复核时点 :44-47 六要素声明「max_abs_crval_dec_deg（适用天区/极点奇点守卫）、max_fov_deg（合法 FOV 声明…）」、:55-56 字段注释「中心 |dec| 守卫（单一条件，四投影统一 85.0 保守冻结）」、:87-88 make 校验序注释）；`lib/phase3_proj/p3_projection.cpp::kMaxAbsDec`（:38）、`::p3_projection_make`（:328 `if (std::fabs(centre_dec_deg) > kMaxAbsDec) return P3_PROJ_PARAM;`）、`::kRegistry`（:266-275 四行的 85.0 与 max_fov_deg=20/60/180/360）、`::p3_projection_registry_selfcheck`（:306-307 仅 `> 0.0`）
- 证据摘录:
  > （h:55）double max_abs_crval_dec_deg; // 中心 |dec| 守卫（单一条件，四投影统一 85.0 保守冻结）
  > （cpp:328）if (std::fabs(centre_dec_deg) > kMaxAbsDec) return P3ProjectionStatus::P3_PROJ_PARAM;   ← 读文件级常量，不读 spec 字段
  > （cpp:306-307）if (!(kRegistry[i].max_abs_crval_dec_deg > 0.0) || !(kRegistry[i].max_fov_deg > 0.0)) return i + 1;
- 权威依据: 宪章 §7.3（每投影须声明适用天区/合法 FOV/奇点，声明须可执行）、§14.2（registry 为唯一权威实现面）、§12.2
- 问题说明: 全仓 grep 复算（口径＝键名 grep 命中集合，不以整行 read 文本判存在性）：`max_abs_crval_dec_deg` 与 `max_fov_deg` 两个字段的**唯一读取点**是 `selfcheck` 的「`> 0.0`」断言（:306-307），此外 `make` 的中心纬度守卫读的是文件级 `kMaxAbsDec`（:38）而非 spec 字段，FOV 更无任何入参校验 ⇒ 守卫实为装饰：今天四行同为 85.0 故行为等价，将来给某投影登记 60° 或不同 FOV 时，注册表值与生效行为会静默背离，而注释已把它写成守卫。
- 影响: registry 的「声明即权威」承诺不成立（当前无行为后果，属 latent 一致性缺陷）；按 §7.3 做审计会误认为天区/FOV 约束已在机器面强制。
- 建议处置: `make` 改读 `spec->max_abs_crval_dec_deg`；selfcheck 增「注册值与 ALG/SCI 冻结值逐行一致」断言；FOV 若无强制点则把字段名与注释改为「诊断性声明」并在 §7.3 面登记。
- 置信度: 高
- 来源: L14-010
- related: M6a-B-001（AIT 合法域口径）、M1a（phase3_proj 主定稿域）
### M6a-D-013 热路径死代码被 ALG 当「实测」固化：pix2ang 结果只被 (void) 压制、(void)y1 压制的是实际被使用的变量，合同文本据以写成事实（来源 L14-011 的死代码部分；其 stderr 探针部分已并 M6a-G-002）

- 类别码: D_COMMENT
- 优先级: P2
- 位置: `lib/phase3_session/p3_resample.cpp::p3_sample_bilinear_ex`（复核时点 :174-176 计算 `ipix` 后 `pix2ang_nest(nside, ipix, c_ra, c_dec)`，:229/:232/:234 使用 y1，:251 `(void)y1; (void)c_ra; (void)c_dec;`）；`docs/algorithms/PHASE3_RSMP_IMPL.md::§6.5`（复核时点 :194 把「(void)y1 压 unused」写进冻结叙述）
- 证据摘录:
  > （:175-176）double c_ra = 0, c_dec = 0; astrocs::healpix::pix2ang_nest(nside, ipix, c_ra, c_dec);
  > （:229/:232/:234）const double x0 = q[0][0]->x, x1 = q[1][0]->x, y0 = q[0][0]->y, y1 = q[0][1]->y; … const double dx = x1 - x0, dy = y1 - y0; … if (std::fabs(dy) > 1e-300) v = (0.0 - y0) / dy;
  > （:251）(void)y1; (void)c_ra; (void)c_dec;
  > （ALG §6.5:194）…(void)y1 压 unused）⑤任一输入 NaN → 输出 nanf("")；
- 权威依据: 宪章 §12.2（注释/文档须与实现一致）、§10.5（重计算路径的无谓开销影响资源测量）、COMMENT_STANDARD:16/:28
- 问题说明: 每采样一次都做一次 `pix2ang_nest`（反三角），其两个输出除 :251 的 `(void)` 外无任何消费 ⇒ 死计算；`(void)y1` 压制的是在 :229/:232/:234 实际参与权重求解的变量，说明该行是历史遗留而非当前事实。合同文档把这条 `(void)y1` 当「实测」写进冻结叙述，使后续清理失去依据（读者以为它是合同的一部分）。
- 影响: CPU 重路径的每像素无谓反三角开销（双线性是 Phase3 全帧主循环）；ALG 记录失效实现细节 ⇒ 合同与代码互相固化错误。
- 建议处置: 删无消费的 pix2ang_nest 与三条 `(void)`（或补一句保留理由），并同步订正 ALG §6.5 的叙述。
- 置信度: 高
- 来源: L14-011（残余，stderr 子事实见 M6a-G-002）
- related: M6a-G-002、M6a-C-002（同模块并发/缓存口径失真）
### M6a-D-014 同一文件内的并发注释互斥：一处要求「所有 aio open/read 经单一全局 mutex 串行化」并在实现里这么写，另一处写「禁全局 critical(aio_read)」，另有「hotfix：串行」与并行池语义并存（来源 L14-013）

- 类别码: D_COMMENT
- 优先级: P2
- 位置: `lib/phase2/src/sampler.cpp::CON-010 复归注释`（复核时点 :157-160「cfitsio / aio_hips 并发文件读在本机 gcc 14 上非线程安全…故将所有 aio_hips 的 open/read 用单一全局 mutex 串行化（计算仍并行）」、:161 `static std::mutex g_aio_mu;`、:166 加锁）、`::SamplerReader 前言与实现`（复核时点 :681-683「cfitsio 同句柄并发读非线程安全 => 禁跨线程共享；**禁全局 critical(aio_read)**」、:693-694 `sig/sup` 两个 getter 内部 `std::lock_guard<std::mutex> lk(g_aio_mu)`）、`::第一遍注释`（:629-631「hotfix：串行；…并行由 std::thread + Runtime lease 驱动」）、`::worker 数注释`（:880-883）
- 证据摘录:
  > （:157-160）CON-010 复归：… 故将所有 aio_hips 的 open/read 用单一全局 mutex 串行化（计算仍并行），消除崩溃且不改变每 cell 结果。
  > （:683）（cfitsio 同句柄并发读非线程安全 => 禁跨线程共享；禁全局 critical(aio_read)）。
  > （:693）AioHipsDataset* sig(std::size_t f) { … if (!csig[f]) { std::lock_guard<std::mutex> lk(g_aio_mu); csig[f] = aio_hips_open(...); } … }
- 权威依据: 宪章 §12.2、§12.3-8（并发模型须一致）、§10.4/§10.5（I/O 串行化的扩展性是资源结论前提）；CONCURRENCY_STANDARD
- 问题说明: :683 的禁止项与 :161/:166/:693-694 的实际实现直接对立（实现就是全局 critical）；:629 的「hotfix：串行」与 :880-883 的 lease 驱动并行是两代叙述并存。读者无法判断该锁是必须保留的约束还是待清除的临时物 ⇒ 误删即 cfitsio 并发崩溃（真实回归风险），保留则 §10.5 的 CPU/I/O 讨论失去正确前提。
- 影响: 一条锁的删除/保留决定生产稳定性，而其合同依据互斥；文档层面的并行度与 I/O 扩展性结论不可信。
- 建议处置: 收敛到一处权威说明（锁保护对象、范围、为何不能靠句柄复制规避、是否登记为偏差），删互斥的历史叙述；若要并行读则改 per-worker 句柄并在 ALG/ARCH 登记（本条与 `docs/algorithms/PHASE2_SAMPLER.md` 的「并行路径不经此锁」失真同根，后者已由 M4-C-05 定稿 ⇒ 勿双计，本条只落代码内注释）。
- 置信度: 高
- 来源: L14-013
- related: M4-C-05（文档侧定稿点）、M6a-C-002、M6a-G-002
### M6a-D-015 浏览器后端头注释把已废弃的私有 tile 线性约定 [y*512+x] 写成现行契约，与唯一权威映射及自身实现相反；同头另把二进制文件名写进「正式数据源」表述（来源 L14-015）

- 类别码: D_COMMENT
- 优先级: P2（原报 P2；该目录不在根交付图 ⇒ 无现行产品后果，但注释错文会直接产出镜像/转置天图）
- 位置: `lib/healpix_db/healpix_browser_qt/core/hips_browser_backend.h::文件头`（复核时点 :2/:4-5「正式 Browser 数据源…仅通过 astro_image_io.dll 的 AIO HiPS Reader API 读取」、:8-9「全分辨率查询直接按 NESTED leaf ipix 定位叶级 tile (512×512), FITS 行主序 **[y*512+x]**」）；`::实现`（`core/hips_browser_backend.cpp` :9-10「fits_index = (511-x)*512 + y …（不再使用 z % 512 / z / 512 私有线性约定）」、:220 `nested_local_to_fits_index(z, 9u, 512u)`）；权威 `lib/common/healpix/healpix_core.h::映射声明`（复核时点 :43-49「FITS 列(NAXIS1)=y、FITS 行(NAXIS2)=tile_width-1-x，行主序 fits_index=(tile_width-1-x)*tile_width+y」）；同式实现 `lib/phase3_session/p3_resample.cpp:62-63/:77-78`
- 证据摘录:
  > （backend.h:8-9）LOD: … 全分辨率查询直接按 NESTED leaf ipix 定位叶级 tile (512×512), FITS 行主序 [y*512+x]。
  > （backend.cpp:9-10）// fits_index = (511-x)*512 + y, (x,y) = nested_local_to_xy(z, 9) // （不再使用 z % 512 / z / 512 私有线性约定）
  > （healpix_core.h:46）// 行主序 fits_index = (tile_width-1-x)*tile_width + y
- 权威依据: 宪章 §14.2（`lib/common/healpix` 为唯一权威实现，禁第二套映射）；IVOA HiPS 1.0 tile 像元排列；§12.3-1
- 问题说明: 头注释与权威式/自身实现是「转置 + 上下翻转」关系；实现侧注释明确「不再使用私有线性约定」，而头注释仍在契约位写它。附带：:4-5 把「astro_image_io.dll」这一**二进制文件名**写进数据源契约句（该文件以构建产物形态存在于源目录 `lib/astro_image_io/astro_image_io.dll`），而该 Qt 浏览器是独立 `project()`、根 CMake 对其零引用（「正式」口径的构建面事实由 M6a-C-003 实例⑥登记，勿双计）。
- 影响: 按头注释重排 tile 像素即显示镜像/转置天图；产物名进入契约表述易被当交付接口。
- 建议处置: 头注释引用 `healpix_core.h`/ALG ID 而非复制公式；数据源表述改「AIO HiPS Reader C ABI（aio_hips_reader.h）」。
- 置信度: 高
- 来源: L14-015
- related: M6a-C-003（实例⑥「正式数据源」口径）、M2b（HiPS/NESTED 映射主定稿域）
