# W1 片3 · P2｜D_COMMENT

### W1-N-08（P2）`gaia_client.c:31-40` 新注释的实测包络**口径错报**：称 high 属于 [16.59, 25.59]，复算为 [13.62, 25.59]
- 其宣称覆盖 36 个真实 shard（GaiaDR3 16 + DR3SP 20）的包络 high 下界 16.59；解析其**自引证据** `run/perf-fix/P1-gaia/evidence/shard_magranges.tsv` 复算 ⇒ 36 片合并 high 为 [13.62, 25.59]（**16.59 只是 GaiaDR3-only 口径**；DR3SP 前 9 片 high 全小于 16.59），low 与片数计数正确。
- **影响限定**：「[-10,40] 完全包含包络」的结论**不受影响** ⇒ P2。但要记的是**形态**：注释把子集口径写成全集口径，且证据文件在 `run/`（gitignored）**仓内不可复核** ⇒ 数值可核性只在我方能重算的那批里存在。related 簇 6 量纲口径族、`E4`、`C-18`、`A-42`/`A-43`（阈值与陈旧文本同族）

### W1-N-09（P2）`gaia_client.c` 的 `parse_attr_after` 新 docstring（`:853-856`）**自述承诺与实现相反**
- docstring 称「属性存在但值为空或畸形 ⇒ **必须告警并放弃剪枝**」；实际**无引号形** `magnitudeRange=10,20` 走非引号分支 `return 0`，被当**属性缺失**静默处理（**不告警、不计 reject_count**）——只有空引号形 `""` 才覆盖告警路径。
- **可达性限定**：依赖外部 XPSD 头格式，**仓内 writer 恒带引号** ⇒ 静态判「行为不等于注释」，不判生产可达。改法：非引号分支也走告警与计数，或把 docstring 订正为真实三分支语义。related 簇 6、`C-20`（禁止静默兜底）、`V15`（测文本不测行为）
