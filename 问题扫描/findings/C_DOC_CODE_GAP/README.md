# C_DOC_CODE_GAP — 科学/算法文档 ↔ 代码 ↔ 接口割裂

覆盖范围：文档与实现行为不一致、签名与默认值不一致、README/module.yaml/registry 与实现不符、错误码与状态字面量不一致。

本目录下按优先级分档：

- `p0/`：P0（发布阻断：科学结果错误或产品不合规）
- `p1/`：P1（一致性/可验证性破坏，须尽快修）
- `p2/`：P2（表述与可维护性）

判定标准与证据要求见 `../../10_PROTOCOL.md` §1、§2、§3。定稿文件由第二层合并代理写入，命名为 `<slice码>.md`。
