# M2a 定稿 · C_DOC_CODE_GAP · P2（来源切片 L10）

> 合并代理: M2a · 复核时点: 全部证据按**当前树**重新定位（path::符号）。

## M2a-C-13 gaia_client_get_spectrum_params 缺句柄空指针防护，是同族查询符号里唯一不设防者，且合同未声明该差异

- 类别: C_DOC_CODE_GAP
- 优先级: P2
- 来源: L10-024
- 位置: lib/gaia_xpsd_client/src/gaia_client.c::gaia_client_get_spectrum_params；对照 ::gaia_client_get_db_type / ::gaia_client_get_file_count / ::gaia_client_get_total_sources / ::gaia_client_query_spectrum_by_coords；lib/gaia_xpsd_client/README.md::§6 inspect 面；docs/algorithms/GAIA_QUERY.md::§3.1 inspect 段
- 证据摘录（逐字，复核时点现文）:
  > （gaia_client.c:2425-2427）int gaia_client_get_spectrum_params(GaiaClient *client, int *out_start_nm, int *out_step_nm, int *out_count) { for (int i = 0; i < client->file_count; i++) {   // 出参全部判空、句柄不判空
  > （对照 :2001/:2006/:2011/:1722/:1785）if (!client) return GAIA_DB_AUTO; / if (!client) return 0; / if (!client) return NULL; / if (!client) return;   // 同族其余符号均先判句柄
  > （GAIA_QUERY.md:188-189）- `inspect()`：`gaia_client_get_db_type / get_file_count / get_total_sources / get_spectrum_params`（真实存在，详见 API-GAIA-001）。
- 权威依据: API-GAIA-001（家族一致错误语义）；宪章 §11（稳定状态码、边界行为可预期）、§14.4（最小充分校验）
- 问题说明: inspect 合同把该符号列为可调用项，实现却直接解引用 client（NULL 句柄=崩溃），与同族 4 个 inspect/查询符号一致设防的现状形成孤例；合同与 README 均未声明"句柄不得为 NULL"这一差异前置条件。
- 影响: 仅误用/负向面（正常路径不变），但崩溃而非错误码违反 §11 的稳定状态码要求，且负向测试无法据合同预期结果。
- 建议处置: 补 `if (!client) return 0;`（与 get_db_type 同语义）或在 API-GAIA-001 显式登记前置条件与状态码；并在 gaia_cat_test negative 模式补 NULL 句柄用例。
- 置信度: 高
- related: M2a-C-5（同模块文档滞后）、M2a-D-4

## M2a-C-14 exchange/manifest schema 与执行校验器在三处口径分叉，"与 schema 一一对应"声明不成立

- 类别: C_DOC_CODE_GAP
- 优先级: P2
- 来源: L10-026
- 位置: contracts/data/phase_product_exchange.schema.json::type_id/storage_uri；contracts/data/artifact_manifest.schema.json::storage_uri/size；runtime/artifact_store/phase_product_exchange_validator.py::_TYPE_ID_RE、::min_planes 断言段；runtime/artifact_store/artifact_manifest_validator.py::_is_nonneg_int
- 证据摘录（逐字，复核时点现文）:
  > （phase_product_exchange.schema.json:54）"storage_uri": { "type": "string", "minLength": 1 },   // 对照 artifact_manifest.schema.json:53-56 要求 URI 形态 pattern 且「禁止裸路径」
  > （artifact_manifest_validator.py:72-73）def _is_nonneg_int(v: Any) -> bool: return isinstance(v, int) and not isinstance(v, bool) and v >= 0   // 未实现 manifest schema:71 的 int64 上限
  > （两校验器 type_id 正则分叉）artifact_manifest_validator.py:40 = r"^astrocs\.[a-z0-9]+\.[a-z0-9_]+\.v[0-9]+$" ｜ phase_product_exchange_validator.py:54 = r"^astrocs\.[a-z0-9]+\.[a-z0-9_]+\.[v][0-9]+$"（等价但字面分叉，两处独立维护）
  > （schema 文件头/两文件自述）声明与 validator「一一对应…须同步修改」；而 min_planes/role↔format 断言只存在于 validator 执行形态
- 权威依据: 宪章 §12.3-5（AST/schema 与执行形态一致）、§4.3；DATA-002/DATA-001 合同自述
- 问题说明: 执行面比 schema 更严或不同（storage_uri 词法、size 的 int64 上限缺失方向相反、min_planes 只在执行形态存在），且同一条 type_id 词法在两个校验器里以两种字面维护（一旦按 M2a-C-10 需要放宽，极易只改一处）——单读 schema 的第三方无法复现拒绝行为。
- 影响: 合同可移植性与独立复核效率；CI 现用 validator 故运行结果自洽（不产生错误 PASS）。
- 建议处置: schema 补齐 if/then min_planes 与 maximum；type_id 词法抽为单一真源（如 contracts/data/artifact_types.registry.json 里登记 pattern，两校验器共读）；或把声明改为"validator 为 schema 超集"。
- 置信度: 高
- related: M2a-C-10、M2a-C-11
