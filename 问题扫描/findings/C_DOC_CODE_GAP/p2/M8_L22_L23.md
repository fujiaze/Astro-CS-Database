# M8 定稿（L22）— C_DOC_CODE_GAP / P2

> 合并代理 M8｜来源 _cache/L22.md｜纯只读复核（read/grep/glob），锚点在当前树按符号重新定位；行号为「复核时点」参考。

### M8-C-003 `lib/plate_solve/README.md` 称投票/选星为「整数归并 + 静态调度，输出 bitwise 与线程数无关」，实现为 `schedule(dynamic)` / `schedule(dynamic, 64)`

- 类别码: C_DOC_CODE_GAP
- 优先级: **P2**（整数归并下 dynamic 不破坏可交换性 ⇒ 无科学后果；但文档口径与代码相反）
- 位置: `lib/plate_solve/README.md::算法要点`（复核时点 :156「投票/选星（整数归并+静态调度，输出 bitwise 与线程数无关…」）；`lib/plate_solve/src/ipv_triangle.cpp`（:202 `#pragma omp parallel for schedule(dynamic)`、:309 `schedule(dynamic, 64)`、:288 注释「实测 ~50ms (16 线程)」）
- 证据摘录:
  > （README:156）投票/选星（整数归并+静态调度，输出 bitwise 与线程数无关,
  > （ipv_triangle.cpp:202）#pragma omp parallel for schedule(dynamic)
  > （ipv_triangle.cpp:309）#pragma omp parallel for schedule(dynamic, 64)
  > （ipv_triangle.cpp:288）// 实测 ~50ms (16 线程)
- 权威依据: 宪章 §12.3-12（文档与实现同步）、§10.4（不得硬编码线程数口径）、§17.2（声明须与已验证事实一致）
- 问题说明: 文档的"静态调度"与同模块两处 `schedule(dynamic)` 直接矛盾。实测归并对象为整数计数（可交换）⇒「与线程数无关」这一结论在本例成立，但被写成"静态调度"的后果，属把"恰好成立"写成"设计保证"。另 :288 把墙钟数字与「16 线程」绑定，是 §10.4 禁止的硬编码线程数口径（同族的 `pc_api.cpp:331/:695/:969`、`image_corrector.h:23`、`aio_pipeline_engine.cpp:7`「16 线程」注释面**主落 M5a-C-002 ⑤**，本条只登 plate_solve 域）。
- 影响: 读者据文档推断调度模型/外推性能会失真；文档不能作为 §10.4 审查输入。
- 建议处置: README 改述为「整数计数归并 ⇒ 结果与调度/线程数无关（可交换）；当前 schedule(dynamic, 64)」；性能注释删除具体线程数或改为「按 Runtime 线程预算」。
- 置信度: 高（两处代码与一句文档逐字对照）
- 来源: L22-008（定稿；范围收窄到 plate_solve 域，注释面让位 M5a）
- related: **M5a-C-002 ⑤**（「16 线程」注释面主落）、M8-C-001
