# M6a 定稿 · C_DOC_CODE_GAP × P2（来源切片 L13 + L14）

> 代理：M6a。只读取证；位置一律 path::符号，行号为复核时点参考值。四态处置与移交见 问题扫描/_merge/M6a.md。

### M6a-C-004 部分修复残余：drizzle 合同词表仍把 nested 写成「0=RING 1=NESTED」并列、DLL 面缺省仍取 0，与引擎「仅 NESTED」和已订正的 README 互斥（来源 L13-012）

- 类别码: C_DOC_CODE_GAP
- 优先级: P2（原 L13-012 报 P1；README 面已由并发提交订正 ⇒ 只报残余，且后果为 fail-closed 的产品不可达而非错值，按规程 §2 降 P2）
- 来源: L13-012（已修复面与残余的边界见 问题扫描/_merge/M6a.md「已修复（并发提交期间）」表）
- 位置:
  - `lib/drizzle/include/astrocs/drizzle/types.h::DRZ_CFG_KEY_NESTED`（复核时点 :55 词表注释 `/* 0=RING 1=NESTED */`，未标「仅 1 可用/缺键即拒」）
  - `lib/drizzle/src/module_entry.cpp::config 解析`（复核时点 :417-418 `double nested = json_get_f64(json, DRZ_CFG_KEY_NESTED, &found); c->nested = (found && nested != 0.0) ? 1 : 0;` ← 缺键即 0）
  - 反证合同：`lib/healpix_db/healpix_drizzle/drizzle_engine.cpp::validate/execute`（复核时点 :745-747 `if (!config.nested) { error_msg = "HISS requires NESTED ordering, RING not supported";`，另 :870、:1578 同族）
  - 已订正面：`lib/drizzle/README.md`（:52「nested（仅 1=NESTED，0=RING 硬拒绝）」）；core 侧 `lib/core/src/module_adapters.cpp::p1_op_drizzle`（:2232-2233 「B1-A9: HiPS NESTED 合同缺省 nested=1」）
- 证据摘录:
  > （types.h:55）#define DRZ_CFG_KEY_NESTED         "nested"             /* 0=RING 1=NESTED */
  > （module_entry.cpp:417-418）double nested = json_get_f64(json, DRZ_CFG_KEY_NESTED, &found); c->nested = (found && nested != 0.0) ? 1 : 0;
  > （drizzle_engine.cpp:745-746）if (!config.nested) { error_msg = "HISS requires NESTED ordering, RING not supported";
- 权威依据: 宪章 §5.3（HiPS 仅 NESTED 语义冻结）、§12.3-1（词表/注释与实现一致）、§14.3（同一合同不得两处不同缺省）
- 问题说明: core 侧已按 B1-A9 把缺省改 1 并在注释里写明「旧缺省 0 被引擎直接拒绝、链不可达」，但 p1_drizzle DLL 面原样保留旧缺陷：配置缺 nested 键即取 0，validate 放行、execute 必被引擎拒（fail-closed：不产错值，但该通道不可达）；合同词表 `types.h` 仍把 0/1 并列，暗示 RING 可选，与 README 已订正后的表述相反。
- 影响: 走模块/ DLL 面的任何未显式给 nested 的配置运行必败（错误信息指向引擎而非配置缺省，定位成本高）；词表注释继续误导配置编写。
- 建议处置: `types.h:55` 注释改「仅 1=NESTED 合法；0=RING 被引擎硬拒（HISS 无 RING 语义）」，并把缺省改为「缺键即 PARAM 拒绝」（与 §7.1 禁 silent default 一致），与 core 侧 B1-A9 同步。
- 置信度: 高
- related: M6a-C-001（同一「控制参数被记录不参与执行」族）、M2a（drizzle/ACR 主定稿域）
