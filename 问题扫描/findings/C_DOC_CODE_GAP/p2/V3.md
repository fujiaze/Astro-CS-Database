# V3 复核新发现 · C_DOC_CODE_GAP（本批修复引入）

## V3-N-01 新增的失败码 -14 未登记进公共 API 返回码合同（本批修复引入的同源缺口）
- **ID**：V3-N-01 ｜ **类别**：C_DOC_CODE_GAP ｜ **优先级**：**P2**
- **位置**：`docs/contracts/PUBLIC_API.md::233-239`（帧通道返回码清单，逐码带锚 -1..-8 / -9 / -12 / -13）；生产者 `lib/drizzle/**hp_drizzle_api.cpp:992-997`（`pm==1 && 块 dtype==FLOAT32 → return -14`）
- **证据摘录**：V3 逐字核：清单里逐码列到 `-13`，**无 `-14`**；而库边界新增的拒绝分支置于累加之前、显式返回 -14。
- **问题说明**：修复本身方向正确（fail-closed，宪章 §5.3 float64 累加），但它**在交付合同面上开了一个新码而没登记**。AGENTS.md 五处同源（科学=算法=接口=代码=测试）与 §17.2 追溯完整性要求新增对外可见语义同提交入合同；下游（CLI、provider、外部消费者）按清单处理返回码时 -14 会落进"未知错误"路径。
- **依据条款**：AGENTS.md「科学定义=算法=接口=代码=测试」；宪章 §12.3 机器一致性、§17.2 追溯完整。
- **影响**：不改数值、不改产品结构，但**新增对外语义未登记**，属本批新引入的小缺口（不是旧缺陷延续）。
- **建议处置**：`PUBLIC_API.md` 补 -14 行（语义「请求 FP64 精度但块为 FP32 → 显式拒绝」+ 锚 `hp_drizzle_api.cpp:992-997`）；与 V3-N-02 同批。
- **取证口径**：V3 静态逐字（read/grep）｜ **状态**：OPEN（待隔壁补登）｜ **置信度**：高
- **related**：**M2a-H-1**（本条是其修复的副产物）、V3-N-02

## V3-N-02 plugin 面把 -14 折叠进默认域，DATA 语义在第二出口丢失
- **ID**：V3-N-02 ｜ **类别**：C_DOC_CODE_GAP ｜ **优先级**：**P2**
- **位置**：`lib/drizzle/**`（plugin 出口）`drz_legacy_status:862-866`；语义源 `hp_drizzle_api.cpp:992-997`
- **证据摘录**：V3 逐字核：legacy 映射函数对 -14 落入 default 分支 → 归为 `SCIENCE_PRECONDITION`，而该码的语义是数据/精度不匹配（DATA 域）。
- **问题说明**：同一个失败码在两个出口给出不同域归类 ⇒ 运维按域分流时，plugin 路径会把"精度不匹配"当成"科学前置未满足"，掩盖真实原因。宪章 §4.1 要求失败语义不混域。
- **依据条款**：宪章 §4.1 失败语义分离、§12.3；AGENTS.md 五处同源。
- **影响**：诊断质量；不改数值。
- **建议处置**：`drz_legacy_status` 为 -14 显式给 DATA/精度域（新增 case），并与 V3-N-01 的合同登记同提交，避免"补了文档、第二出口仍错"。
- **取证口径**：V3 静态逐字｜ **状态**：OPEN｜ **置信度**：高｜ **related**：V3-N-01、M2a-H-1
