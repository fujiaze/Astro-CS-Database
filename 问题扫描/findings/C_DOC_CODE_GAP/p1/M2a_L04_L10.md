# M2a 定稿 · C_DOC_CODE_GAP · P1（来源切片 L04+L10）

> 合并代理: M2a · 复核时点: 全部证据按**当前树**重新 read/grep 定位（不信叶子行号）；位置一律 path::符号。叶子引用的 commit 哈希不作事实使用。

## M2a-C-2 产品元数据的 precision_mode/signal_dtype 记录"请求值"而非实际累加域，帧头 PRECISION 与产品精度脱钩

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L04-001（manifest/元数据失真半边单列）
- 位置: lib/healpix_db/healpix_drizzle/drizzle_engine.cpp::writeHis（hmeta.precision_mode/signal_dtype 赋值处）、::writeHisTilesT（同一赋值 + 落盘选路）；docs/contracts/DATA_SEMANTICS.md::§11.1 PRECISION 行；docs/algorithms/DRIZZLE_GEOMETRY.md::§8 B2-A12 段
- 证据摘录（逐字，复核时点现文）:
  > （drizzle_engine.cpp:1137-1142）// 精度模式写入 metadata (precision_mode + signal_dtype) … hmeta.precision_mode = config.precision_mode; hmeta.signal_dtype   = config.precision_mode;
  > （drizzle_engine.cpp:2022-2023）同一赋值（tiled 生产路径）；（:2099）acc.pixels[local].sum_flux = static_cast<double>(tile.pixels[local].sumFlux);  ← 来源是 float 累加器；（:2133-2140）if (config.precision_mode == 1) { ... writer.add_tile_f64(...) }
  > （DATA_SEMANTICS.md:248）| "header" KV: "PRECISION" | 字符串 "fp32"/"fp64" | — | precision_mode=-1 时读取（API 兼容缺省 FP32）…**B2-A12**：P1 drizzle 节点不再写死 "0"，按 `drizzle.precision_mode` 写实际精度
  > （DATA_SEMANTICS.md:261）| sumFlux（tile 累加量，HiPS SIGNAL 底数） | float32 或 float64（**随 precision_mode**）`[512][512]`/tile |
- 权威依据: 宪章 §4.3（manifest/元数据必须如实记录精度与像素语义）、§5.3、§12.3-1/-10；DATA-P1-DRZ（DATA_SEMANTICS §11）
- 问题说明: 累加域由输入块 dtype 决定（见 M2a-H-1），但元数据三项（HISS `precision_mode`/`signal_dtype`、p1_stack.json `precision_mode`、帧头 `PRECISION`）全部按 `config.precision_mode` 写出。于是"请求 FP64 + 上游给 FP32 块"这一最常见组合产出的产品：值域是 binary32 累加结果、落盘被加宽成 float64、元数据声明 signal_dtype=FP64/PRECISION=fp64 —— 声明与实际精度无因果链。DATA_SEMANTICS:261「随 precision_mode」与 :248「写实际精度」两句都以 precision_mode 为精度真源，属合同层面的同源错误。
- 影响: 消费方（Phase2 ivar 加权、Phase3 导出、审核面）按元数据判定的数值精度不可信；跨精度对拍/回归比较在错误前提下进行；宪章 §4.3 的"精度可追溯"不成立。
- 建议处置: 元数据三项改为写**实际累加域**（由 Scalar/块 dtype 派生，例如 `actual_accum_dtype` 与 `requested_precision` 分列）；请求与实际不一致时显式拒绝（同 M2a-H-1 建议①）；DATA_SEMANTICS:248/:261 与 ALG §8 B2-A12 段同步订正。
- 置信度: 高
- related: M2a-H-1（同一根因的数值半边）、L04-019（variance 面降精度，M2a-H-4）

## M2a-C-3 值像素 NaN/Inf 的"静默掩膜"登记在六处合同/注册面仍存在，而代码现状已是"不掩膜、直接传播"——登记本身失实

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L04-005（按协调指令改判为「登记失实」，不是「已登记未修复」；不使用 commit 哈希）
- 位置: docs/algorithms/DRIZZLE_GEOMETRY.md::§5 输入校验表（值像素行）、::§10 DISP 表（DISP-DRZ-004 行）；docs/contracts/DATA_SEMANTICS.md::§11.1 "data" 块行；lib/drizzle/README.md::§5 输入校验段；docs/modules/registry/astrocs.phase1.drizzle.md::invalid 段；docs/standards/STANDARDS_REGISTRY.md::D.drizzle §3 判定行 + 偏差表 + 偏差索引；对照 lib/healpix_db/healpix_drizzle/drizzle_engine.cpp::drizzleTiled 主循环
- 证据摘录（逐字，复核时点现文）:
  > （drizzle_engine.cpp:1714-1719）// (P1-DRZ-NONFINITE) 冻结合同 docs/science/DRIZZLE.md §8 :96: 源像素 NaN/Inf 经 F_p=Σx_j·w_jp 直接传播, drizzle 层**不掩膜**; … 旧 isfinite(...)+continue 静默吞像素已删除
  > （DRIZZLE_GEOMETRY.md:118）| **值像素 NaN/Inf** | **主循环静默 continue（等效掩膜），不进累加器，无计数暴露** | :1715（DISP-DRZ-004） |
  > （DRIZZLE_GEOMETRY.md:217）| DISP-DRZ-004 | 值像素 NaN 经 F_p 传播、不掩膜（DRIZZLE.md:96） | 主循环 !isfinite→continue 静默跳过（不进累加器），无计数暴露 | DRIZZLE.md:96 vs drizzle_engine.cpp:1715 |
  > （DATA_SEMANTICS.md:245）… 值 NaN/Inf 静默跳过（等效掩膜，不进累加器，无计数暴露——DISP-DRZ-004，drizzle_engine.cpp:1712）
  > （lib/drizzle/README.md:89-90）- **NaN/Inf**：值像素非有限 → 主循环静默 continue（:1712），不进累加器、无计数暴露（与 DRIZZLE.md:96 "不掩膜传播 NaN" 不符——DISP-DRZ-004）
  > （docs/modules/registry/astrocs.phase1.drizzle.md:43）invalid：值像素 NaN/Inf 静默跳过（DISP-DRZ-004）；pixfrac∈(0,1]
  > （STANDARDS_REGISTRY.md:163 / :173 / :259）DISP-DRZ-004（值像素 NaN 静默 continue，无计数暴露 …）｜ DISP-DRZ-004 | 中 | … | P1-DRZ-IMPL（NaN 值像素静默跳过）｜ … | TRACKED | P1-DRZ-IMPL |
- 权威依据: SCI-DRZ-001 §8:96（现行为准）；宪章 §12.3-1/-2/-10（登记与实现一致、活动文档禁陈旧状态）、§1.1（权威分层：SCI > ALG > 注册表转录）
- 问题说明: 当前树的代码已按 SCI §8:96 传播（值侧无 isfinite 掩膜），且**已有注册回归钉住**：`tests/p1drz/p1drz_tests_core.cpp:423-443`（p1drz_negative，注册于 p1drz/CMakeLists.txt:48-72）断言"NaN 面引擎层成功（传播合同）+ 无有限伪输出 leaf"。因此残余事实全部在文档/登记侧：六处合同仍把"已不存在的掩膜行为"作为**当前事实**登记，DISP-DRZ-004 在注册表里仍是 TRACKED 待 P1-DRZ-IMPL 处置的偏差，其中三处还引用同一行号（:1712/:1715）而该行现在的语义已反转。按 ALG §10 编写反向实现或复用该偏差登记的任务会拿到与代码相反的口径。
- 影响: 标准符合性登记失真（D.drizzle §3 的偏差列）；ALG/DATA/README/registry 与 SCI/代码四方互斥；追溯机器门读到的偏差实为已闭。
- 建议处置: 一次性订正六处（ALG §5 行 + §10 DISP-DRZ-004 行、DATA_SEMANTICS:245、README:89-90、registry 页:43、STANDARDS_REGISTRY:163/:173/:259），把 DISP-DRZ-004 改记为「已闭（值侧掩膜已删，传播合同 + p1drz_negative 钉住）」并把 var/weight 的 ≤0 语义差异另立登记（见 M2a-H-2）；同批把这三处引用行号改为 path::符号 锚。
- 置信度: 高
- related: M2a-H-2（≤0 分支仍在）、M2a-E-1（锚订正同批）、L04-005

## M2a-C-4 lib/drizzle 模块 README/module.yaml 的"当前事实"仍写无源码/DLL 未建/未接节点，与构建接线事实相反

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L04-008（前台指令：与 F00-04 方向相反，**不自我合并**，单独立条并把模式名移交）
- 位置: lib/drizzle/README.md::§1 身份表、::§9 构建段；lib/drizzle/module.yaml::状态注释与 entrypoint/dll 字段；对照 根 CMakeLists.txt::add_subdirectory(lib/drizzle)、lib/drizzle/CMakeLists.txt::add_library(astrocs_p1_drizzle SHARED)、lib/core/src/module_adapters.cpp::descriptor
- 证据摘录（逐字，复核时点现文）:
  > （lib/drizzle/README.md:9）> 由 P1-DRZ-IMPL 建立，当前本目录仅合同文件、无源码；落位依据
  > （lib/drizzle/README.md:21）| MOD ID / DLL target | `MOD-astrocs-phase1-drizzle` / 现状实现编入 CMake 静态库 `astrocs_drizzle`（CMakeLists.txt:356-366，无独立 DLL 产物）；迁移目标 `astrocs_p…
  > （lib/drizzle/module.yaml:21）# entrypoint=MISSING：registry descriptor（lib/core/src/module_adapters.cpp:508-525 …
  > （lib/drizzle/module.yaml:24）# 序列=io_read→calibrate→cosmetic→io_write）；astrocs_p1_drizzle.dll 未建；
  > （lib/drizzle/module.yaml:34）entrypoint: MISSING
  > 现状：CMakeLists.txt:184 `add_subdirectory(lib/drizzle)`；lib/drizzle/CMakeLists.txt:60 `add_library(astrocs_p1_drizzle SHARED …)`（:11 注释「唯一导出 astrocs_module_query_v1」，:109 version-script/def 兜底）
- 权威依据: 宪章 §12.3-1（manifest↔注册表↔构建 target↔产品清单一致）、§12.3-9/-10（禁悬空引用、禁历史状态冒充当前）、§16.2（不得以文档声明冒充完成）
- 问题说明: 模块已落码（lib/drizzle/src/module_entry.cpp）、已建 SHARED target、已以唯一导出符号 `astrocs_module_query_v1` 接 registry descriptor，但 README/module.yaml 的"当前事实"章节仍是迁移前的合同冻结稿陈述。任何以 README/module.yaml 为准的调度、验收、manifest available 判定与 §12.3-1 机器门都会拿到相反事实（且 module.yaml 的 entrypoint=MISSING 与 lib/drizzle/CMakeLists.txt 的导出面直接冲突）。
- 影响: 模块状态声明不可信；机器一致性门建立在过期事实上；重复实现风险（后续任务以为需要"建立 adapter"）。
- 建议处置: 按现源码逐函数复核 README §1/§5/§9/§10 与 module.yaml（module_status、entrypoint、dll_name、node_operations）；订正 README:21 的 CMake 锚（现 astrocs_drizzle 静态库在 CMakeLists.txt:408 起，非 356-366）；同批并入 M2a-E-1 的锚点订正提交。
- 置信度: 高
- related: L04-006、M2a-E-1、M2a-C-5（Gaia 侧同型，方向相反）；移交项「状态声明无单一事实源」

## M2a-C-5 Gaia 模块 README 与 ALG-GAIA-001 的现状陈述整体滞后于迁移落地（无测试/无 target/无 plan-cancel-lease 三条"缺口"均已不成立）

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L10-003 + L10-004（同根因合并：迁移落地后文档未回登）
- 位置: lib/gaia_xpsd_client/README.md::§1 身份表、::§8 测试、::§9 构建与已知限制；lib/gaia_xpsd_client/module.yaml::状态注释；docs/algorithms/GAIA_QUERY.md::§3.1、::§4；对照 lib/gaia_xpsd_client/CMakeLists.txt::add_library(astrocs_catalog_gaia SHARED)、::src/gaia_client.c::gaia_set_worker_lease / gaia_set_cancel_checkpoint / gaia_client_collect_plan_stats、::src/module_entry.c、tests/unit/CMakeLists.txt::gaia_cat_* add_test
- 证据摘录（逐字，复核时点现文）:
  > （README.md:13）迁移目标 `astrocs_catalog_gaia.dll`（CAT-GAIA-IMPL 建立，尚未存在）
  > （README.md:130）- **当前无可执行测试、无 PASS 声明**（CAT-GAIA-TEST 建立；…）
  > （README.md:137）**无 CMake target**（BLD-001 显式 target 缺口，CAT-GAIA-IMPL 建立 `astrocs_catalog_gaia` DLL + adapter）
  > （README.md:140）1. C ABI adapter / 版本化 query 入口 / plan()/cancel() 不存在；
  > （GAIA_QUERY.md:182-183）当前实现未提供 plan 入口（迁移缺口，CAT-GAIA-IMPL 建立 adapter）。
  > （GAIA_QUERY.md:186）`cancel()`：当前 C API 无取消检查点（合同缺口）；迁移后取消检查点=文件循环边界…
  > （GAIA_QUERY.md:200）- 线程来源：OpenMP 默认 team（无 host ThreadLease）——迁移缺口…
  > 现状：gaia_client.c:83 `void gaia_set_worker_lease(int threads)`、:85 `gaia_set_cancel_checkpoint(...)`、:102 `static int gaia_cancel_hit(void)`、:237 `int gaia_client_collect_plan_stats(GaiaClient*, GaiaPlanStats*)`（:76-78 注释「供 module_entry.c plan() 真实推导…禁空转假 plan」）、:1862/:2057 文件循环边界取消检查点；CMakeLists.txt:15 `add_library(astrocs_catalog_gaia SHARED …)`；tests/unit/CMakeLists.txt:838+ `add_test(NAME gaia_cat_unit …)` 等七项
- 权威依据: 宪章 §9（模块注册合同 plan/execute/cancel）、§12.3-2/-10、§16.2；ALG-GAIA-001 §4 自述「现状，与源码一致」+ :6「全部离散公式、常量、行为边界均从该文件逐函数核对」
- 问题说明: 四条"缺口/不存在"陈述对应的实现面在当下树内全部存在（lease/cancel 注入桥段、plan 元数据统计、DLL target、七模式 ctest 注册），而 ALG 头部还自述"与源码一致"、README 还写"尚未存在/无测试/无 target"，module.yaml:16-19 一面写 `module_status: CONTRACT_READY` 一面在注释里记 entrypoint 已修正（:7-9）——三份活动文档互不同步。注意其方向与 lib/drizzle 相反：drizzle 侧是"说没建、其实建了"（M2a-C-4 说的是"说有源码"，两者都是状态声明失实，但错位方向不同），故两案各立。
- 影响: 后续接线（SA-RT-05）可能重复实现 plan/cancel 或漏接取消/预算语义；产品 manifest available 判定与 §12.3 机器门依据失真；「逐函数核对」类可追溯声明不可复核。
- 建议处置: 以一次 DOC 复审统一 README/module.yaml/ALG §3.1/§4 至迁移落地后现状（含 lease/cancel 注入为**进程级 static** 的限制，见 M2a-C-8），并重判 module_status；ALG 的"与源码一致"改为符号锚清单。
- 置信度: 高
- related: L10-005（行锚漂移，M2a-E-2）、L10-007（M2a-C-8）；移交项「状态声明无单一事实源」

## M2a-C-6 同名科学参数 pixfrac 的两条生产入口默认值分裂（0.8 vs 1.0），且"API-DRZ-001 默认 1.0"是被杜撰的锚

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L04-011
- 位置: lib/orchestrator/cpp/include/json_config.h::DrizzleConfig::pixfrac；configs/stage1.template.json::drizzle；lib/core/src/module_adapters.cpp::p1_op_drizzle（pixfrac 读取处）；lib/drizzle/src/module_entry.cpp::config 解析（默认与校验）；docs/contracts/PUBLIC_API.md::API-DRZ-001；docs/science/DRIZZLE.md::§4/§9a
- 证据摘录（逐字，复核时点现文）:
  > （json_config.h:67）double pixfrac = 0.8;                // (0, 1]; 生产默认 0.8
  > （configs/stage1.template.json:41）"pixfrac": 0.8
  > （module_adapters.cpp:2234）const double pixfrac = p1_num(dj, "pixfrac", 1.0);   // 缺键即 1.0
  > （module_entry.cpp:355）if (!found) pixfrac = 1.0;                     /* API-DRZ-001 默认 1.0 */
  > （PUBLIC_API.md:228/:234/:259）该节只列参数与返回码语义（3=pixfrac 越界）…全节无 pixfrac 默认值条款（grep "pixfrac" 命中项均非默认值声明）
- 权威依据: 宪章 §9（配置默认值及其科学/工程分类须登记）、§4.2（phase_config 可跨机器复现）、§12.3-2（禁杜撰锚）；SCI-DRZ-001 §4:31/§9a:143 只冻结定义域 (0,1]，未冻结默认值
- 问题说明: 同一 `pixfrac` 缺键语义在两条生产入口取不同值：编排 Stage1 侧 0.8，核心 IR 节点与迁移 DLL 侧 1.0；迁移面注释把 1.0 归因于"API-DRZ-001 默认 1.0"，而 PUBLIC_API 的 API-DRZ-001 全节没有任何默认值条款（该引用不成立）。结合 M2a-A-1 的 1/pixfrac² 因子，缺省路径直接决定产品亮度：0.8 与 1.0 相差 1.5625 倍。SCI 未冻结默认值，等于把该选择留给两个互不相同的代码字面量。
- 影响: 跨入口/跨配置不可复现；同域产品的面亮度基准不一致；虚假锚掩盖了真实决策点。
- 建议处置: ① Owner 裁决唯一默认 pixfrac 并写入 SCI §4/§9a（含理由：缝隙抑制 vs 平滑）；② 两入口按合同默认值对齐（缺键即读合同，不在代码里各写一份）；③ 删除 module_entry.cpp:355 的虚假锚注释或补 PUBLIC_API 条款。
- 置信度: 高
- related: M2a-A-1（1/pf²）、M2a-F-4（默认路径无回归）；移交项（两默认值分裂转 M5/M7）

## M2a-C-7 块缓存 4GB 上限在文档按全局声明、实现按每文件私有（MAX_FILES=32），资源验收断言与实现不同径

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L10-006
- 位置: docs/algorithms/GAIA_QUERY.md::§7 内存与资源段、::§9 测试门资源行；lib/gaia_xpsd_client/README.md::§7 资源、::§8 测试；lib/gaia_xpsd_client/src/gaia_client.c::BLOCK_CACHE_MAX_MEMORY、::XPSDFileInternal（内嵌 block_cache）、::block_cache_put（判据处）、::MAX_FILES
- 证据摘录（逐字，复核时点现文）:
  > （GAIA_QUERY.md:157）- 内存：mmap 全部 XPSD（只读）+ 块缓存 ≤4GB + 每线程 scratch…；（:232）- **资源**：RSS 结束回落有解释高水位；块缓存 total_memory ≤ 4GB
  > （README.md:107）- 内存：mmap 全部 XPSD 只读 + 解压块缓存 ≤4GB（8192 槽 LRU 1/4 淘汰）…
  > （gaia_client.c:115）#define BLOCK_CACHE_MAX_MEMORY (4ULL * 1024 * 1024 * 1024) /* 解压块缓存最大4GB */
  > （gaia_client.c:434）if (bc->total_memory + data_size > BLOCK_CACHE_MAX_MEMORY || check_memory_pressure()) {   // bc 为 per-file
  > （gaia_client.c:212）BlockCache block_cache;  /* 解压块缓存 (保留到关闭) */（在 XPSDFileInternal 内）；（:30/:222）#define MAX_FILES 32 … XPSDFileInternal files[MAX_FILES];
- 权威依据: 宪章 §10.4（一个进程一个资源预算源）、§10.5（资源记录）、§17.6（无界内存增长属发布门禁禁止项）；TEST-GAIA-DESIGN-001 资源门面（README:127「块缓存 ≤4GB」）
- 问题说明: 判据落在每文件私有 BlockCache 上，client 最多 32 个文件 ⇒ 名义上限 32×4GB=128GB，与文档三处「≤4GB」全局陈述和测试资源门的断言口径不同；唯一兜底是启发式 `check_memory_pressure()`（README:109 自述"可用内存 <4GB 时自适应停止缓存"），不构成预算上限。GaiaDR3+DR3SP 同开即多份缓存并存。
- 影响: 资源验收（§10.5 记录 + §17.6 增长禁止项）与实现不同径，高内存占用不可见；不改单线程数值。
- 建议处置: 二选一并同步验收：实现进程级共享预算（client 级 BlockCache 或全局令牌桶），或合同/README/测试门改述"每文件 ≤4GB、总量按 file_count 上界"并给绝对上限。
- 置信度: 高
- related: M2a-C-8（同模块并发/资源面）、L10-006

## M2a-C-8 并发模型合同仍写"单写者"，实现自陈该前提对 match 类查询失效；lease/cancel 注入是进程级全局

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L10-007
- 位置: docs/algorithms/GAIA_QUERY.md::§4 并发模型；lib/gaia_xpsd_client/src/gaia_client.c::块缓存锁封装注释、::返回缓存指针处的所有权注释、::gaia_leased_workers/gaia_cancel_poll_fn；lib/gaia_xpsd_client/integration/astrocs_catalog_gaia.integration.json
- 证据摘录（逐字，复核时点现文）:
  > （GAIA_QUERY.md:191）## 4. 并发模型（现状，与源码一致）；（:193-194）- 并行：`#pragma omp parallel for schedule(dynamic)` 按文件分片；每文件独立 collector 与 scratch，块缓存仅被处理该文件的线程访问（单写者）。
  > （gaia_client.c:300-306）/* B4-P1-2: block_cache 锁封装。合同 docs/algorithms/GAIA_QUERY.md §4 单写者约定原本依赖 "并行轴=文件…"; 但 match 类查询（gaia_client_query_spectrum_by_coords 等）并行轴=坐标，多线程并发读写同一文件的 block_cache … = 无同步数据竞争 (UAF)。修复: block_cache 读写全部持 per-file 锁… */
  > （gaia_client.c:947-949）注: 持锁只消除缓存结构性竞争…并发淘汰仍可能释放该块 —— 这是缓存所有权设计的固有语义, 与单写者时代一致 (命中返回的指针同样可被淘汰), …
  > （gaia_client.c:79-81）static int gaia_leased_workers = 0; static int (*gaia_cancel_poll_fn)(void *user_data) = NULL; static void *gaia_cancel_poll_ud = NULL;
- 权威依据: 宪章 §10.4（进程一个资源/线程预算源）、§10.6、§14.4（fail-fast）、§12.3-8（文档并发模型与真实线程入口一致）；ALG §4 自述「现状，与源码一致」
- 问题说明: (a) ALG 的"单写者"结论与源码自述直接冲突（by_coords 并行轴是坐标，多池线程共享同一文件缓存），实现靠 per-file 锁补救但**合同未更新**；(b) 代码明确保留"返回缓存指针后并发淘汰可释放该块"的悬垂读窗口并自称固有语义——该语义下调用方拿到的指针生命周期未在任何合同里定义（违 §7.3/§8.5 语义须显式）；(c) lease/cancel 注入面是三个文件级 `static`：同 DLL 被两个实例并发 execute 时互相覆盖轮询回调与线程租约（A 的取消作用到 B 的循环；一侧清空使另一侧取消失效；一侧 destroy 后 poll_fn 悬垂），并发合同不闭合。
- 影响: 多实例编排下取消/预算语义错误（静默失效或误取消），缓存返回指针存在悬垂读风险；不改单线程数值。竞态回归缺位另立 F_TEST_GAP（M2a-F-2）。
- 建议处置: ALG §4 重写为"多写者 + per-file 锁 + 缓存所有权契约（返回指针的有效期/拷贝义务）"；注入面改为 client/execute 上下文携带；把 test_gaia_race.c 纳入注册面（见 M2a-F-2）。
- 置信度: 高
- related: M2a-C-5、M2a-F-2、L10-006

## M2a-C-9 XPSD 文件头/XML 解析无区间校验：header_len 读后弃用、root_pos 与 child 索引不设限，负向合同"确定性拒绝"缺实现支撑

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L10-008
- 位置: lib/gaia_xpsd_client/src/gaia_client.c::load_xpsd 头解析段、::树 XML 解析段（rootPosition/nodeCount/child 索引）、::search_recursive（child 递归）；docs/algorithms/GAIA_QUERY.md::§9 负面期望
- 证据摘录（逐字，复核时点现文）:
  > （gaia_client.c:1036-1037）uint32_t header_len;  memcpy(&header_len, xf->mmap_data + 8, 4);   // 全文件再无第三处使用（grep 实证：仅 1036/1037 两命中）
  > （gaia_client.c:1100-1107）int root_pos = parse_int_after(tree_tag, "rootPosition", 0); … if (node_count > 0 && root_pos > 0) { const uint8_t *node_data = xf->mmap_data + root_pos;   // 无 root_pos + node_count*48 ≤ mmap_size 校验
  > （gaia_client.c:1127）memcpy(&n->child_nw, p + 32, 4); …（:1415/:1555/:1692）if (node->child_nw) search_recursive(xf, tree, node->child_nw, …)   // child 索引未校验 < node_count
  > （GAIA_QUERY.md:222-227 负面期望）… 坏魔数/截断文件 → create 失败或 0 结果 …
- 权威依据: 宪章 §14.4（信任边界输入校验一次做足、fail-fast）、§7.3（不支持语义须显式拒绝，不得静默/崩溃）；ALG-GAIA-001 §5 负面合同
- 问题说明: 声明式长度/偏移字段一律不校验：`header_len` 读出即弃（对"声明头长 vs 实际文件长"零一致性检查）；`rootPosition` 只做 >0 判定即以裸指针偏移进 mmap；节点 child 索引未与 node_count 上界比较即递归下钻；XML 以 strstr/strlen 在 mmap 上扫（无 NUL 终止保证）。负向测试面只覆盖粗粒度截断（gaia_cat_test negative 模式），字段撒谎组合零覆盖 ⇒ 合同承诺的"确定性拒绝"没有实现支撑。
- 影响: 星表文件损坏/被替换时行为是越界读或崩溃，而非确定性错误码；真实数据终验（§13.3「实际 PlateSolve/测光所需的代表性联调」）遇到坏文件不可预期。健康数据数值不变。
- 建议处置: create 期建立只读区间模型（magic + header_len + 各树节点区边界），越界即返回确定错误码；child 索引入栈前校验 < node_count；negative 用例补"字段撒谎"注入（rootPosition 超出、nodeCount×48 越界、child 指向自身/越界）。
- 置信度: 高
- related: M2a-F-3（深树 fixture 缺，L10-009）

## M2a-C-10 registry 登记的 `astrocs.calibrated_frame.v1` 不满足自家 type_id 词法，该类型一经使用必被词法拒绝（登记即合法 vs 未登记即拒 互斥）

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L10-012
- 位置: contracts/data/artifact_types.registry.json::types[3]；contracts/data/artifact_manifest.schema.json::properties.type_id.pattern；contracts/data/phase_product_exchange.schema.json::type_id.pattern；runtime/artifact_store/artifact_manifest_validator.py::_TYPE_ID_RE/校验顺序；runtime/artifact_store/phase_product_exchange_validator.py::_TYPE_ID_RE；docs/interfaces/data/DATA-002_PHASE_PRODUCT_EXCHANGE.md::沿用类型声明段
- 证据摘录（逐字，复核时点现文）:
  > （artifact_types.registry.json:34）"type_id": "astrocs.calibrated_frame.v1",（:7 note「单一真源：全部已知 artifact type_id。manifest 的 type_id 必须登记于本表，未登记即拒绝」）
  > （artifact_manifest.schema.json:43）"pattern": "^astrocs\\.[a-z0-9]+\\.[a-z0-9_]+\\.v[0-9]+$"
  > （artifact_manifest_validator.py:40）_TYPE_ID_RE = re.compile(r"^astrocs\.[a-z0-9]+\.[a-z0-9_]+\.v[0-9]+$")；（:131-137）# type_id 词法 + 注册表 … if not _TYPE_ID_RE.match(tid): errors.append(f"type_id lexical invalid: {tid!r}") … else: unknown type_id … not in artifact_types.registry.json
  > （DATA-002_PHASE_PRODUCT_EXCHANGE.md:184）不新增 registry type（沿用 DATA-001 三产品 type + calibrated_frame 内部类型）。
- 权威依据: 宪章 §4.3、§12.3-1（注册表/schema/校验器一致）、§7.3（语义须闭合）；DATA-001/DATA-002 合同自述
- 问题说明: 正则要求四段（astrocs.<无下划线段>.<段>.v<n>），而注册登记的第四个类型只有三段，且第二段含下划线；校验器先跑词法、后跑注册表闭合（:131-137），因此 `astrocs.calibrated_frame.v1` 永远落在"词法 invalid"分支——注册表宣称它是合法登记的内部交换类型、DATA-002 又声明"沿用该类型"不新增，但任何携带它的 manifest 都被拒。这是**登记类型死锁**，不是漏登记。
- 影响: Phase1 内部 calibrated_frame typed artifact 一旦经 DATA-003 生产 Store 通道发布即被拒；RT-001/RT-005 接线时最可能诱发的处置是"改走非 typed 通道或塞 waiver"，即绕门。
- 建议处置: ① 二选一：把 type_id 改为符合词法的 `astrocs.phase1.calibrated_frame.v1`（并在 DATA-001/13 号标准同步），或放宽词法允许三段带下划线形态（需同时改两处正则 + 两份 schema，且 `_TYPE_ID_RE` 在两文件已分叉，见 M2a-C-15）；② 给该类型补一条机器检查：注册表内每个 type_id 必须能过 schema pattern（可并入 check_standards_registry/DATA 检查器）。
- 置信度: 高（正则两侧与注册表取值逐项核过；无生产端发射该 type_id 的证据，故按"一经使用必拒"而非"已在产线被拒"定档 P1）
- related: M2a-C-11、M2a-C-15、M2a-E-1（DATA-001 锚全仓缺失）

## M2a-C-11 §30 合同 JSON 仍把两个 AIO 通道标 PENDING，与已落地的 AIO writer 能力直接矛盾

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L10-016
- 位置: contracts/data/phase2_uncertainty_rejection_provenance_v1.json::pending_aio_channels；lib/astro_image_io/include/aio_hips.h::产品 flags 枚举；lib/astro_image_io/src/hips/aio_hips_writer.cpp::properties 键写入段、::诊断平面写段
- 证据摘录（逐字，复核时点现文）:
  > （contract JSON:81-93）"pending_aio_channels": { "writer_int32_tile": { "status": "PENDING_AIO_DOMAIN", "gap": "aio_hips writer 无 int32 子产品写通道（flags 位 32/64 未定义…" … "properties_key_channel": { … "gap": "…ASTROCS_* 五键无法写 HiPS properties" } }
  > （aio_hips.h:46-47）AIO_HIPS_PRODUCT_NREJ = 32, AIO_HIPS_PRODUCT_NUSED = 64,
  > （aio_hips_writer.cpp:943-945）// DATA-UNC-001 §30.2: 诊断统计平面固定 int32 (无 precision 开关) … kv.push_back({"astrocs_diag_dtype", value_dtype ? value_dtype : "int32"});
  > （aio_hips_writer.cpp:946-957）// DATA-UNC-001 §30.3 (DATA-P2-PROV-001) provenance 五键。… kv.push_back({"ASTROCS_INPUT_MANIFEST_HASH", …}); … ASTROCS_MODEL_HASH / ASTROCS_UNCERTAINTY_AVAILABLE / ASTROCS_WEIGHT_MODE / ASTROCS_REJECT_PROFILE
- 权威依据: 宪章 §12.3-1/-2（合同声明与实现一致）、§16.2；该 JSON 自任「通道语义唯一事实源输入」
- 问题说明: 两个 pending 通道都已在 AIO 域实现（int32 诊断平面 flags 位与写路径、ASTROCS_* 五键 properties 写入，且带缺键故障注入面证明其被断言），合同 JSON 无 resolution 回登——同文件其它条目（findings_transfer）本来就有 status=IMPLEMENTED 的回登惯例，说明这是漏更而非有意保留。
- 影响: 后续任务可能在错误前提下重复实现或改走旁路；声明与证据割裂，跨域消费（Phase2/Phase3 uncertainty）判定通道可用性时会误判。
- 建议处置: 按同文件既有惯例补 resolution 字段（实现任务 ID + 证据指针：aio_hips.h flags 位、writer 段落、测试），或显式声明由 AIO 域文档接管并删除 pending 块。
- 置信度: 高
- related: M2a-C-10、M2a-C-12

## M2a-C-12 恒零天测列以 published 行 schema 下发，而合同写"不输出/未初始化"；0 落在合法值域内，无 invalid 表达

- 类别: C_DOC_CODE_GAP（含**部分修复**残余事实）
- 优先级: P1
- 来源: L10-017（部分修复：代码侧已显式置 0；残余为"未初始化"文本失实 + schema 发布恒零列 + sentinel 落合法域）
- 位置: docs/contracts/DATA_SEMANTICS.md::§8.2 不输出段；lib/gaia_xpsd_client/README.md::§9 已知限制 3；lib/gaia_xpsd_client/src/module_entry.c::cone 行 schema；lib/gaia_xpsd_client/src/gaia_client.c::calloc 装配处；lib/gaia_xpsd_client/include/astrocs/gaia/types.h::GaiaStar / GaiaSpectrumStar
- 证据摘录（逐字，复核时点现文）:
  > （DATA_SEMANTICS.md:121-123）明确**不输出**：`source_id`（恒 0 占位）、parallax/pmra/pmdec（输出结构体中**未初始化**，调用方不得使用——现状契约，CAT-GAIA-IMPL 迁移时需显式置 0 或剔除）、误差列、proper motion 消化坐标。
  > （README.md:142）3. `GaiaStar.parallax/pmra/pmdec` 输出未初始化；`source_id` 恒 0；
  > （gaia_client.c:1888）*out_stars = (GaiaStar *)calloc((size_t)total, sizeof(GaiaStar)); /* CAT-GAIA-IMPL: 未初始化字段显式置 0 */（缓存命中路径 :1829 同注释）
  > （module_entry.c:751-753）schema = {"ra":"f64",…,"parallax":"f32","pmra":"f32","pmdec":"f32","source_id":"i64"}
  > （tests/unit/** grep "parallax"）NO HIT（恒零语义无任何测试钉住）
- 权威依据: DATA-GAIA-001；宪章 §4.1（validity 是独立状态，不得用值域内数字充当哨兵）、§4.3、§12.3-2
- 问题说明: 三分状态：**(a) 已被修复**——"未初始化"（UB 面）在当下树已改为 calloc 显式置 0，两处注释即自认；**(b) 残余失实**——DATA_SEMANTICS:121-123 与 README:142 仍写"未初始化"，读者会以为存在 UB；**(c) 残余设计缺陷**——DLL 行的 published schema（module_entry.c:751-753）仍把 parallax/pmra/pmdec/source_id 作为**正式输出列**下发，其值为恒零常量，而 0 对 parallax（0 mas）与 pmra/pmdec（0 mas/yr）都是**合法天测值**，无 NaN/缺列/位标记表达"未提供"。合同一面说"不输出、调用方不得使用"，一面在 ABI schema 里把它列为产品列。
- 影响: 下游按 schema 消费即得语义错误的天测约束量（当前消费面 pc_api 只用 magG/flux 未受害，属潜伏面）；恒零值与真零值不可区分，可能被当作"无自行/无视差"参与统计。
- 建议处置: ① 行 schema 剔除这四列（或改由合同定义列级 invalid，如 NaN/负值域外哨兵 + `has_astrometry` 位）；② DATA_SEMANTICS:121-123 与 README:142 按现状改写（"显式置 0 占位，非真值，不得用于天测"）；③ 补一条注册测试断言恒零语义（并入 M2a-F-2 测试缺口簇）。
- 置信度: 高
- related: M2a-B-1（无天测列 ⇒ 历元无法传播）、M2a-C-5
