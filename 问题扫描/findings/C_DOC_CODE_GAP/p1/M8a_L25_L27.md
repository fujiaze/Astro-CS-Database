# 定稿 C_DOC_CODE_GAP / P1 — M8a（L25+L27）

> 合并代理 M8a｜来源 L25+L27｜纯只读复验（read/grep/glob），未跑 shell/构建/git。
> **0 命中判据三级复核声明（前台新令）**：本文件所有"0 命中/不存在"判据均经 ①整句 grep ②放宽字符集与大小写关键词 grep ③定点 read 三级复核；放宽级结果记于 `_merge/M8a.md §12`。未据任何"报告称未命中"撤证据。
> 结构化比对用「元素计数 + 键集合 + 逐列 grep」口径；凡涉运行期行为的结论均标注为静态读入判断。

---

## M8a-C-001 已废弃第二调度器被 README 写成「正式科学运行只有一条命令」，并整套冻结第二套 `ASTROCS_*` 退出码

- 类别: C_DOC_CODE_GAP
- 定稿优先级: P1
- 位置: `lib/orchestrator/README.md::题头/唯一运行方式/退出码表/Stage1 流水线`（复核 1、3、5-13、60-75、76）；对照 `CMakeLists.txt::add_subdirectory 全集`（复核 172/177/184/191/199/208 + 670/677）、`cmake/install_layout.cmake::交付单元表`（14-31、40-41、104-110）、`packaging/astrocs.product.json::units`（8-17）；宪章 §8.1、§3.1；`docs/contracts/ARCH-001.md::§7`（复核 75/88）
- 证据摘录:
  > README:3 「版本：AstroCS Orchestrator 2.0（Phase1 JSON 唯一入口）」；:5-7 「## 唯一运行方式 / **正式科学运行只有一条命令**：`orchestrator.exe <stage1.json>`」
  > README:60-75 完整 0-10 十级 `ASTROCS_*` 码表（含 `ASTROCS_MODULE_MISSING`）；:76 「## Stage1 流水线（**Phase1 V4 生产链**）」
  > ARCH-001.md:88 「G7 后：canonical run **不链接/调用旧 scheduler**/ACR；CLI 薄化；静态图=trace」；:75 「§7 冻结的现状差距」
  - 可达性复算（口径：grep `add_subdirectory(` 于根 `CMakeLists.txt` 得 8 个入口 + 2 个测试/工具域）：**无 `lib/orchestrator`**；`cmake/install_layout.cmake` 交付单元表与 `astrocs.product.json` units 亦无该二进制。
  - 码表实现复算（三级）：①整句 grep `ASTROCS_MODULE_MISSING` 于 `cli` = 0；②放宽 grep `ASTROCS_[A-Z_]{3,}` 于 `cli` = **43 命中，逐条读后全为 `ASTROCS_VERSION_STRING`/`ASTROCS_COMMIT_SHA`/`ASTROCS_CFITSIO_n` 等构建宏，无一是退出码枚举成员**（见 §12-5）；③定点读 `cli/cli_common.h:108`、`cli/CMakeLists.txt:53-58` → 该十级码表在现行入口**无实现对应物**。
- 权威依据: 宪章 §8.1（唯一薄入口 `astrocs`）、§3.1（每平台一个用户可见入口）、§12.3-10（活动文档不得以历史状态冒充当前）
- 问题说明: 一个不进根构建图、不进安装树、不进产品清单的遗留二进制，其 README 以现在时态自称"正式科学运行唯一方式"和"生产链"，且无 legacy/DORMANT 总标注；同时把第二套退出码连同语义（0-10）整套冻结在文档里，与现行 CLI 码表构成互斥口径。按 AGENTS.md「模块文档必读」路由，新 Agent 打开本子库即被引导去扩写遗留通道而非收敛。三套退出码互斥本身由 L12-016 主落，本条只落该 README 的失实叙述与第二码表固化。
- 影响: 单一入口是宪章级架构约束（§3.1/§8.1）；文档面给它造成了第二个"正式"入口。后续任务按此文件施工会直接与 §12.3-10 冲突。
- 建议处置: README 顶部加 ARCHIVED/DORMANT 标注并改指「生产入口 = astrocs CLI（宪章 §8.1）」；退出码表标注 legacy 并注明与 `cli` 现行码表的关系；或由负责人裁决整目录（连同 `cpp/` 遗留 Makefile 与 vendored jsv）迁档 —— 注意迁档须与 M8a-G-009 的许可处置同批，不得让无 LICENSE 全文的 vendored 库静默消失。
- 置信度: 高
- 来源: L27-004
- related: L12-016（三套退出码主落）、M8a-G-009（该目录 vendored 许可）、M8a-I-003（tools/README 的 `run_orchestrator` 指向本二进制）、L05（遗留构建通道定性）；判词层级：不可达门归 M7、平台盲区归 M9，本条不写系统性判词

---

## M8a-C-002 五源状态滞后簇：同一模块的 README / module.yaml / registry 页 / 构建安装 / 产品清单对「有没有源码、DLL 建没建、entrypoint 接没接」给出互斥答案

- 类别: C_DOC_CODE_GAP
- 定稿优先级: P1
- 位置（四模块 × 五类源，本轮全部按当前树重读仍成立）:
  - `lib/cosmetic/README.md::现状段`（复核 7-10、19、23）vs `lib/cosmetic/src/module_entry.cpp`（实存）、`lib/cosmetic/CMakeLists.txt::astrocs_p1_cosmetic`（1-14）、`CMakeLists.txt::add_subdirectory`（199）、`cmake/install_layout.cmake`（104-110）、`packaging/astrocs.product.json`（15）
  - `lib/hips/README.md::身份表`（复核 24、27、28）vs `lib/hips/CMakeLists.txt::add_library(astrocs_p1_hips_writer SHARED)`（43）、install_layout（104-105 交付清单）、product.json（16）
  - `lib/calibration/README.md::§10 迁移段`（复核 159-163）vs `lib/calibration/module.yaml`（2-6、20、21）
  - `lib/drizzle/module.yaml::dll_name/module_status/entrypoint`（32-34；:21 陈旧 descriptor 行锚 `module_adapters.cpp:508-525`）vs 根图 `astrocs_p1_drizzle` 已建已装
- 证据摘录:
  > cosmetic README:9-10 「…`astrocs_p1_cosmetic.dll` 落码由 P1-COS-IMPL 建立，**当前本目录仅合同文件、无源码**」
  > lib/cosmetic/CMakeLists.txt:1 「CMakeLists.txt - astrocs_p1_cosmetic 模块 DLL (P1-COS-IMPL 迁移面)」；install_layout.cmake:104-105 `foreach(tgt … astrocs_p1_cosmetic astrocs_p1_hips_writer)`；product.json:15 `"unit_id": "MOD-P1-COS" … "status": "IMPLEMENTED"`
  > hips README:24 「现状实现编入 CMake 静态库 astrocs_hips（**无独立 DLL 产物，全仓库无 astrocs_p1_hips_writer 目标**）；迁移目标 astrocs_p1_hips_writer.dll（P1-HIPS-IMPL 建立，**尚未存在**）」；:27 「不声明 IMPLEMENTED」——vs `lib/hips/CMakeLists.txt:43 add_library(astrocs_p1_hips_writer SHARED` + :57 `OUTPUT_NAME "astrocs_p1_hips_writer"` + install + product.json:16 IMPLEMENTED
  > calibration README:161-163 「module.yaml…登记 manifest：…**entrypoint=MISSING（registry 入口未接）**」——vs 同目录 module.yaml:21 `entrypoint: astrocs_module_query_v1` 且 :2-6 「已由 P1-CAL-IMPL 交付：astrocs_p1_calibration DLL…entrypoint astrocs_module_query_v1，唯一导出 ABI-006」
- 权威依据: 宪章 §12.3-1（manifest/注册表/构建 target/产品清单一致）、§12.3-10、§16.2（只有通过的 Phase 才能在 manifest 标 available）；`docs/owner/RELEASE_STATUS.md` §0 状态词阶梯（唯一口径）
- 问题说明: 本簇是"谁最新无机制保证"的确证：四份 README 均自署 2026-09-07 冻结，而 DLL 落码在其后（P1-*-IMPL），且没有任何门比对 manifest ↔ 构建 ↔ 清单 ↔ README 四源（`ci/checks.json` grep `module\.yaml|module_status` = 0，见 M8a-G-006），所以文档滞后不可能变红。calibration 一例最能说明性质：**README 抄的是自家 yaml 字段却抄反了**（yaml 写 `astrocs_module_query_v1`，README 写 MISSING 并加解释性括注「registry 入口未接」），说明两义术语（M8a-I-006）已在同一模块内造成事实冲突。另：`lib/drizzle/module.yaml:21` 的 descriptor 行区间（508-525）按当前树已不成立（`p1_drizzle_descriptor` 的 `d.module_id` 实在 :721 附近），行号漂移按纪律不作移除理由，此处以符号锚登记。
- 影响: 「科学模块交付度」这一发布核心事实在 L2 陈述面失真；按 README 行事的后续迁移任务可能重复落码或覆盖已安装产物；负责人"每子库可核查交付状态"的要求在该面无法落地。
- 建议处置: ①建三方机器比对门（`product.json units` ↔ `module.yaml module_status/entrypoint` ↔ README 状态行），任一不一致 FAIL，注册进 `ci/checks.json`；②四源订正为单一事实源派生（README 状态行由 manifest 生成，禁手抄）；③本簇现状文字按构建事实订正，状态词统一取 §0 阶梯（R-03）；④`entrypoint` 语义先裁（M8a-I-006），否则本门会因两义而必然 FAIL。
- 置信度: 高
- 来源: L27-007
- related: L03-008、L04-008（hips/drizzle 两实例已登记，本条并入矩阵不重复计数）、M8a-G-006（无门）、M8a-I-006（entrypoint 术语缺口）、M6b（索引/registry 断链主落，本条 related 指回）

---

## M8a-C-003 lib/astro_image_io README 停在 2026-07-12 快照：「零外部依赖」与自身目录、根构建图同时矛盾，且把外部 GitHub 仓当「当前版本」权威

- 类别: C_DOC_CODE_GAP
- 定稿优先级: P1
- 位置: `lib/astro_image_io/README.md::题头/特性/编译/目录结构/尾注`（复核 3、5、7、9-12、19、28、43-54、106-131、166-167、180、190-193）；对照 `CMakeLists.txt::astrocs_cfitsio`（275 区段，`${ASTROCS_CFITSIO_SOURCES}`）、`::astrocs_aio`（322-348）、`::astrocs_hips`（351-370）、`cmake/cfitsio_sources.cmake`（60 源全在 `lib/astro_image_io/third_party/cfitsio/`）
- 证据摘录:
  > README:3 「版本：v1.0 ｜C++原生实现｜2026-07-12」；:5/:7/:19/:180 「**零外部依赖**（不依赖 cfitsio / XML 库）」「仅需 MinGW-w64 g++」
  > README:106-131 目录结构树**无 `src/hips`、无 `aio_abi.cpp`**，并把 `astro_image_io.dll` 列为目录条目（树内确有该产物文件）
  > README:9-12 「GitHub 仓库…最新 commit `a33d167`」；:190-193 「详细文档 → GitHub 仓库（**当前版本**）」；:43-54 构建段只给 `g++ -shared -o astro_image_io.dll` 与 Makefile
  > README:28/:166-167 「批量并行（**OpenMP 16 线程**）…`n_threads=16`」
  > 构建事实：`cmake/cfitsio_sources.cmake` 60 项源全部位于 `lib/astro_image_io/third_party/cfitsio/`（含 :14 fits_hcompress.c、:61 zcompress.c），`astrocs_aio` 显式链 `astrocs_cfitsio`
- 权威依据: 宪章 §12.3-1、§12.3-10、§14.1（只用现有仓库原地演进，不建额外 clone/第二权威）、§10.4（一个进程一个线程预算源，禁硬编码并行度）、§8.3（AIO 收敛方向）；`DEPENDENCIES.md:31/:83`（MinGW/MSYS2 禁为正式工具链）
- 问题说明: 四重脱钩同时存在 —— ①「零外部依赖（不依赖 cfitsio）」对**整个目录**不成立：该目录下就是 vendored cfitsio 60 源并编入交付；②构建说明只给 `g++ -shared` 与 Makefile，得到的产物与产品三 target（cfitsio/aio/hips）无关，且指向被禁作正式发布工具链的 MinGW；③目录清单漏掉本目录最大的产品面（HiPS/HISS 源），并把预编译 DLL 当源树内容；④「当前版本」权威指向外部 GitHub 仓与一个仓内不可核的 commit 号。另「OpenMP 16 线程」是 §10.4 违例叙述在 README 的新实例（该面计票主落权在 M5a，本条只登记 README 面）。
- 影响: 唯一 I/O 层（Phase1/2/3 共用）的入口文档整体失真；按它施工得到的产物与交付无关；外部仓"当前版本"口径直接抵消 AGENTS.md 的真源路由。
- 建议处置: README 按 L2 模板重写（现网 target 清单 + 安装面 + 合同链 + 状态词）；「零外部依赖」限定到 `aio_fits.cpp` 原生解析路径并显式登记 vendored cfitsio 与 zlib 关系（与 M8a-C-007 同批）；外部仓库链接降为历史出处；预编译 DLL 出树。
- 置信度: 高
- 来源: L27-005
- related: F00-03（「16 线程」同模式，主落权在 M5a）、L12-001（aio_pipeline_engine 兼容面）、M8a-C-002、M8a-C-007、L03/L04（HiPS 域）

---

## M8a-C-004 lib/healpix_db README 系列把生产真源定性为「独立仓库本地副本、.gitignore 忽略」，与根构建图、安装清单和 .gitignore 三重冲突

- 类别: C_DOC_CODE_GAP
- 定稿优先级: P1
- 位置: `lib/healpix_db/README.md::GitHub 仓库/模块表/关联仓库表/依赖`（复核 7-10、14-21、31-37、45）；`lib/healpix_db/healpix_drizzle/README.md::GitHub 仓库地址/OpenMP 16 线程`（复核 11、58-59）；对照 `CMakeLists.txt::astrocs_drizzle`（408-417）、`::astrocs_p1_drizzle`（179-184 区段）、`cmake/install_layout.cmake`（104-105）、`.gitignore`（全文 132 行）
- 证据摘录:
  > README:18-19 「`healpix_stack/` … 稀疏 HEALPix 堆栈存储（**独立仓库本地副本，.gitignore 忽略**）| C++ | 活跃（独立仓库）」「`healpix_drizzle/` …（独立仓库本地副本，.gitignore 忽略）| 活跃（独立仓库）」
  > README:7-10 「仓库地址：https://github.com/fujiaze/Healpix-Database」；:31-37 关联仓库表把三个 GitHub 仓列为职责主体；:45 依赖指向另一外部仓
  - .gitignore 复算（三级）：①整句 grep `healpix` = **0**；②放宽 `(?i)healpix|drizzle` = **0**（该文件命中的只有 `GaiaDR3/`、`BASS DR3/` 与 build/log/产物模式族，:1-10、:18-36）；③定点通读全文 → 「.gitignore 忽略」这一关键定性**无任何文件支撑**（见 §12-8）。
  > 根构建事实：`CMakeLists.txt:408-417` `add_library(astrocs_drizzle STATIC … lib/healpix_db/healpix_drizzle/*.cpp)`，:417 含 `snr_evaluator.cpp`；install_layout:104-105 把 `astrocs_p1_drizzle` 列入交付
  > 另：README:18 把 `healpix_stack` 列为"活跃"模块行，但该目录实际在 `lib/healpix_db/archive/legacy/healpix_stack/`（glob 复核，其 README 亦在该路径下）；README:17 称 `healpix_io` 已归档但该目录仍含活文件
- 权威依据: 宪章 §14.1（原地演进，不建额外 clone/第二仓库）、§12.3-1/-10；AGENTS.md 目录规范（代码与合同真源 = `lib/`）
- 问题说明: 同一目录的真源归属被写成两种互斥答案：README 说"权威在外部仓、本目录只是被 gitignore 的副本"，而 .gitignore 无对应条目、根构建图直接编译它、安装清单交付它。后果最重的是 Drizzle —— Phase1 产品核心科学路径（球面重投影、通量守恒）的修改者据 README 会认为"改这里没用，去 GitHub 改"。附带登记：`lib/acr/README.md:41` 残留「本支线 feature/astrocompute-runtime 只开发底层」的分支口吻（§14.1 禁新建分支；该 README 其余 DORMANT 口径与 §10.1 一致，属正面样本，不另立条）。
- 影响: 科学重投影路径的真源判定与追溯链（外部 commit 不可仓内核）失效；§12.3-1/-10 在该面无门。
- 建议处置: 订正为「生产真源 = 本仓 `lib/healpix_db/…`，根 CMake 直接编译并安装」；三个 GitHub 仓链接降为"历史出处"注记；`healpix_stack` 行改指 archive 实路径；`healpix_io` 归档事实与目录实况同步；acr README 删分支口吻句。
- 置信度: 高
- 来源: L27-006
- related: M8a-C-003（同模式）、M8a-G-005（同目录 nanoflann 未登记）、M8a-C-002、L04（healpix_drizzle 代码域）、L12（构建面）

---

## M8a-C-005 module_id 多套词汇并存：21 份 manifest 与 22 个生产 descriptor 的 module_id **精确交集为 0**

- 类别: C_DOC_CODE_GAP
- 定稿优先级: P1
- 位置: `lib/snr_estimator/README.md::题头`（1）、`lib/snr_estimator/module.yaml::module_id`（32）、`lib/core/src/module_adapters.cpp::p1_noise_snr_descriptor`（702）、`docs/modules/registry/astrocs.phase1.noise-snr.md`；全量：`lib/**/module.yaml`（21 份）
- 证据摘录:
  > README:1 「# lib/snr_estimator — astrocs.p1.noise-snr（P1-NOISE）」；module.yaml:32 `module_id: astrocs.p1.noise`；descriptor:702 `d.module_id = "astrocs.phase1.noise-snr";` —— 同一模块三个不同名
  - **全量值集合比对（口径：grep `^module_id:` 于 `lib` 得 21 值；grep `d\.module_id = "` 于 `module_adapters.cpp` 得 22 值；两侧做字符串精确比对，不用行文本）**：manifest 侧值域为 `astrocs.p1.*`/`astrocs.p2.*`/`astrocs.p3.*`/`astrocs.catalog.gaia`/`astrocs.phase1.session`；descriptor 侧全为 `astrocs.phaseN.*`（22/22）→ **精确交集 = 0/21**（唯一的 `astrocs.phase1.session` 出现在 manifest 侧，而 registry 无该 descriptor、只有手写页）。此结论**强于** L27-008 原报的"三向一致 1 + 双轨 19/21"。
  > 多份 manifest 自述双向不权威：`lib/plate_solve/module.yaml:41-49` 「descriptor 现状占位 ID…不得反向作为冻结依据」；`lib/star_detector/module.yaml` 同类注
- 权威依据: 宪章 §12.3-1；`docs/traceability/TRACEABILITY_SPEC`（ID 唯一性）
- 问题说明: README 的「相关文档」要素只能靠人工超链接跨词汇指认 registry 页；任何以 module_id 为键的自动比对（矩阵、install manifest、docs 链接、后续可能的 README 门）跨源必然失效 —— 这是 M8a-C-002 那类矛盾无法被机器捕捉的**前置原因**。0/21 精确交集意味着"两轨"不是个别模块的毛病，而是命名体系整体分裂。
- 影响: 模块身份在 L2 三源无一可自动对齐；交付度/追溯类门的可行性被词汇层卡死。
- 建议处置: 负责人裁决唯一 module_id 词表（建议取 registry/descriptor 侧 `astrocs.phaseN.<name>` 为唯一，或反向）；`module.yaml` 增 `registry_page`/`descriptor_module_id` 字段并由门校验映射；批量订正 21 份 manifest 与 README 题头。
- 置信度: 高（0/21 的结论来自值集合精确比对而非抽样；口径已写明）
- 来源: L27-008（本代理复算加重）
- related: L05-012（模块 ID 双口径主条，本条为 README/manifest 层扩量）、M8a-C-002、M8a-G-006（该差异无门）、M8a-G-007（26/22 页面对差）

---

## M8a-C-006 发布包 NOTICE/SBOM 由脚本内硬编码字符串生成，与实际链接闭包脱钩且含三处错标（M8a 新立）

- 类别: C_DOC_CODE_GAP
- 定稿优先级: P1
- 位置: `tools/make_windows_release.py::license_text/SBOM 段`（复核 100-127）；`tools/make_linux_release.py::license_text 段`（复核 86-93）；`tools/check_release_layout.py::必含文件`（复核 6、23）；`packaging/licenses/LICENSE-INDEX.txt`（7-10、22-23）；对照 M8a-G-001（GSL/Siril/HEALPix）、`lib/astro_image_io/third_party/cfitsio/licenses/License.txt`（1-8）
- 证据摘录:
  > make_windows_release.py:101-108 `license_text` 为**脚本内字面量**，写入包内 `LICENSES/NOTICE.txt`：「仓库内第三方许可见控制包 PACKAGE_MANIFEST.md 与各 third_party LICENSE」/「静态自带的第三方: cfitsio(**BSD**), zlib(zlib license), nlohmann/json(MIT), **HEALPix(见 lib/plate_solve/LICENSE 等)**, OpenMP/VC 运行时」
  > 同脚本 :109-127 SBOM 的 `packages` 只有**一条** `SPDXRef-Package-AstroCS`，`licenseConcluded: "NOASSERTION"`、`downloadLocation: "NOASSERTION"`，且不由 `dependency-lock.json` 派生
  > make_linux_release.py:87-93 同型字面量，且把许可权威指向「**工程控制/RELEASE_V5/…/PACKAGE_MANIFEST.md**」（免报区归档路径，非交付可寻址对象）
  > 反证一：`lib/astro_image_io/third_party/cfitsio/licenses/License.txt:1-8` 为 NASA/USG 版权文本（「…rights reserved under the copyright laws of the United States…U.S. Government…」），非 BSD；`dependency-lock.json:26` 亦按 cfitsio 自身许可登记
  > 反证二：交付 exe 的链接闭包含 GSL 与 Siril 派生 ipv（M8a-G-001 面①），NOTICE 两条均未提；`packaging/licenses/` 无 GSL/Siril/HEALPix/nanoflann 文本
  > 反证三：`tools/check_release_layout.py:6/:23` 要求包根必含 `LICENSE` 文件，而 `LICENSE-INDEX.txt:7-10` 自认「仓库根无独立 LICENSE 文件（历史缺口）」→ 必含项的满足方式未在仓内定档
- 权威依据: 宪章 §16.2（发布包至少含「许可证」「product manifest」「build provenance」）、§17.11（发布包/审核包白名单、哈希、版本及 provenance 全部通过）、§12.3-1；NASA/USG 与 GPL/MIT/BSD 各自的署名义务
- 问题说明: 交付物的许可证面不是从 `dependency-lock.json` 派生的，而是两个手工字符串 —— 因此锁里有没有条目、构建图里链了什么，都不影响包内 NOTICE 内容。现状后果三连：①NOTICE 漏列实际链接进产品的 GPL 组件与两处派生声明（M8a-G-001 的交付末端）；②NOTICE 把 HEALPix 归属指向 `lib/plate_solve/LICENSE`（MIT 自著），而真正的 HEALPix 派生实现与其 BSD-3/GPL-2+ 措辞在 `lib/common/healpix/` —— **指错文件，且这一误指会把 plate_solve 的 MIT 声明"背书"给本非它的代码**；③NOTICE 把 cfitsio 标为 BSD 与仓内实际许可文本（NASA/USG）不符。SBOM 单一容器且 `NOASSERTION` 等于未对第三方建档。
- 影响: §16.2「随包许可证」在内容层面即为不实；§17.11 的门即便接线（M8a-G-004）也会因 NOTICE 非派生而给出错误绿灯 —— 这是发布阻断项的交付末端。
- 建议处置: NOTICE/SBOM 改由 `dependency-lock.json`（含 `vendored_copies`/`derived_code`/`system_dependencies` + 许可 SPDX + 哈希）生成，删除脚本内许可字面量；许可文本从 `packaging/licenses/` 按 lock 条目拷入包内 `LICENSES/`；订正 cfitsio 许可标注与 HEALPix 归属指向；`check_release_layout.py` 的 `LICENSE` 必含项与"根 LICENSE 缺失"事实同批闭合（负责人裁决根 LICENSE 落地）。
- 置信度: 高（三处脚本字面量、许可文本、锁条目均仓内可核）
- 来源: L25-001/L25-002 的交付末端（**M8a 新立，L25 未单列**）
- related: M8a-G-001（一条四面主条）、M8a-G-004、M8a-G-005、M8a-I-004；M5b 版本簇（G-04）仅交叉引用不合并；建议条款 R-04（见 _merge §6.2）
