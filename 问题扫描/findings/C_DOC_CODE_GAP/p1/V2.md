# V2 定稿 · C_DOC_CODE_GAP × P1

### V2-N-03 算法文档把被修掉的错误口径钉成规范：`RMSE = mad·1.4826 / A <= 0.2` 与 MAD 语义画等号，代码已改而该行未改
- **ID** V2-N-03 ｜ **类别** C_DOC_CODE_GAP ｜ **优先级** **P1** ｜ **状态** OPEN
- **位置**：`docs/algorithms/STAR_DETECTION_ALGORITHMS.md:111`（`#   RMSE=mad·1.4826/A<=0.2`）；同病 `lib/star_detector/README.md:132`；代码侧 `lib/star_detector/src/sdet_api.cpp:226` 现按 `1.4826·MAD/A>0.2` 判、`sdet_api.cpp:441-445` 的 `result->mad` 存的是**未换算的 MAD**。
- **为什么是本轮最该改的一行**（负责人准绳）：文档把 **RMSE 与 `mad·1.4826` 划等号**，正是本批代码刚刚脱离的那个错口径。⇒ **按现行算法文档复现的人会直接把 RMSE 塞回 `mad` 字段**，等于用文档把代码拉回缺陷。这不是"不该动冻结文档"，而是**你已授权 §1.2 修改、而这处漏改**。
- **V2 的独立算术（不引旧文档）**：旧门实执行 `1.4826·RMSE/A>0.2` ⇔ `RMSE/A>0.13490`；新门 `1.4826·MAD/A>0.2` ⇔ `σ̂_res/A>0.2` ⇒ **阈值放宽比 0.2/0.134898 = 1.4826（+48.26%）**。判定为**恢复量纲正确性**（旧代码对 σ 型量又做了一次 MAD→σ 换算），**不是改回错误**；但必须同时登记两件事：①星表成员门一次性放宽 48%，`n_valid`/可用性会变，而本批**无真实数据计数对照**（`PLATESOLVE_ALL` 基线自述陈旧）；②**0.2 的出处至今无**（`fix_note` 自承「出处待登记」）。
- **依据条款**：AGENTS.md「科学定义=算法=接口=代码=测试」五处同源；宪章 §12.3、§17.2。
- **建议处置**：①改 `:111` 与 `README.md:132` 两行为「`σ̂_res = 1.4826·MAD`（MAD 为残差绝对值中位数，未换算字段），判据 `σ̂_res/A > 0.2`」；②把 48% 放宽与 `0.2` 出处补登为 `DISP-*`；③补一次真实数据的 `n_valid` 前后计数对照。
- **取证口径** V2 逐字 + 独立算术复算 ｜ **置信度** 高 ｜ **related** **A-12**、`M3-A-008`（同一常数三套字面值、无单一权威定义点，仍 OPEN）、**V2-N-05**、L28d（注释自述不实族）

### V2-N-04 参考星门只做了库层一半：`NO_DATA` 之后仍返回成功码、编排层无条件写 `STATUS=OK` ⇒ 未定标的恒等拷贝在交付头里显示成功
- **ID** V2-N-04 ｜ **类别** C_DOC_CODE_GAP ｜ **优先级** **P1** ｜ **状态** OPEN
- **位置**：`lib/photometric_calib/cpp/src/star_matcher.cpp:513-536`（`size<3` ⇒ `fit_used=0`/`scale_factor=1.0`/`sigma=0`/`return {}`，**门本身正确**）→ `pc_api.cpp:1112`（NO_DATA 后**仍 `return 0`**）→ `orchestrator.cpp:2976`（**无条件** `fn_kv_set("photo_stats","STATUS","OK")`，`N_MATCHED=0` 另记）。
- **问题说明**：C5 的判定是**正面**的——这是**恢复冻结门**（现行 `docs/science/PHOTOMETRY.md:38` 早已逐字写明 `|r_consistent|>=3` 才进 IRLS、`|r_inliers|>=2` 才估 `sigma_residual`，旧实现只判 `empty` ⇒ **一颗星即可定标整帧**），方向是**收紧**、符合 SP-5.5「测光 fail-closed」，锁 `p1phot_fixgates.cpp:114-140` 用哨兵（`scale=123.0`/`sigma=456.0`）断言"不写 scale"，判别力真且已非豁免登记 ✓。**缺的是接口层**：调用方无法从 `rc` 区分"定标成功"与"未定标恒等拷贝"，而交付 manifest 里两者都写 `STATUS=OK`。
- **依据条款**：宪章 §11（不得留下看似成功的产品）、§8.6（失败须返回稳定状态码）、§4.1 字段语义不混。
- **影响与如实限定**：稀疏星帧从此不定标（正确），但 **Phase1 孔径测光链不经此门**（`p1_op_photometry` 只读 `p1_sources.json` 的 `sources`，`:2347`），受影响的是 `orchestrator`/`with_gaia` 通路 ⇒ **两路不可混谈**（V2 明确提示，我采纳）。
- **建议处置**：①`pc_api` 给可判别的 `NO_DATA` 码；②`orchestrator` 按门结果写 `STATUS` 并在 manifest 记退化原因；③`cli`/`core` 侧补对应错误码（V2 放宽关键词复核：`NO_DATA` 在 `lib/core`、`cli`、`include` **无测光相关命中**）。
- **取证口径** V2 逐字 + 三级复核 ｜ **置信度** 高 ｜ **related** `M3-C-001`（本门主体，隔壁标 FIXED ⇒ **建议改 PARTIAL，残余=接口层三处**）、`M3-C-002`（**另一条**残余：`quality_flags` 无生产数据源，V2 核到 `pc_api.cpp` 五个调用点全部传 `nullptr` ⇒ 其 PARTIAL 理由属实。**两条不可合并转述**）、`V2-N-06`
