# M8 定稿（L22）— C_DOC_CODE_GAP / P1

> 合并代理 M8｜来源 _cache/L23.md + _cache/L22.md｜纯只读复核（read/grep/glob），锚点在当前树按符号重新定位；行号为「复核时点」参考。

> 合并代理 M8｜来源 _cache/L23.md + _cache/L22.md｜纯只读复核（read/grep/glob），锚点在当前树按符号重新定位；行号为「复核时点」参考。

### M8-C-001 THREAD_BUDGET_ARCH.md §4 把「与线程数无关 / 无裸 data race」列为契约，却与同文 §4 自己的计数器条款、与实际实现、与同仓测试三方互斥；四个行锚已漂移

- 类别码: C_DOC_CODE_GAP
- 优先级: **P1**（科学确定性口径文档失实；无直接数值后果故不升 P0）
- 位置:
  - 文档：`docs/architecture/THREAD_BUDGET_ARCH.md::§4`（复核时点 :32「旧 THREADING_MODEL.md §确定性锚点全部有效: upm.cpp:495 / sampler 串行 / drizzle_engine.cpp:1662, 1751, 1834, 1843」、:33「计数器: atomic 或 thread-local 聚合，由 owner 线程定序合并，无裸 data race」）
  - 实现反证（sampler 非串行）：`lib/phase2/src/sampler.cpp::`（:882「生产默认 N-worker 并行(std::thread …)」注释、:890 `std::vector<std::thread> pool`）
  - 实现反证（drizzle 跨 worker 合并）：`lib/healpix_db/healpix_drizzle/drizzle_engine.cpp::threadTiles`（reduction 站点现 :1674 对应文档 :1662；合并站点现 :1770-1773 对应文档 :1751；累加循环 :1648/:1681）
  - 测试反证：`lib/healpix_db/healpix_drizzle/tests/p1drz/p1drz_tests_core.cpp`（:209-213、:277 跨 1/2/4 线程比对用 `< 1e-12` 容差而非位比较）、`p1drz_oracle.hpp:348-349`
  - 行锚漂移：`lib/phase2/src/upm.cpp` 的 D1 契约注释现 :514（文档写 :495）
- 证据摘录:
  > （THREAD_BUDGET_ARCH.md:32）旧 THREADING_MODEL.md §确定性锚点全部有效：upm.cpp:495（…）、sampler 串行、drizzle_engine.cpp:1662, 1751, 1834, 1843
  > （同文件:33）计数器: atomic 或 thread-local 聚合，由 owner 线程定序合并，无裸 data race
  > （sampler.cpp:882/:890）生产默认 N-worker 并行(std::thread …) / std::vector<std::thread> pool
  > （p1drz_tests_core.cpp:209-213）跨线程一致性用 fabs(a-b) < 1e-12 判定（非 bitwise）
- 权威依据: 宪章 §5.3/§6.3（确定性/科学等价须文档-代码-测试一致）、§12.3-12（文档与实现同步）、§10.4（单一线程预算源）
- 问题说明: 该节宣称旧模型的确定性锚「全部有效」，但三向互斥：①文内 :33 承认存在跨线程合并与计数器归并（这正是可能不可交换的来源），:32 却把同一批位置列为「与线程数无关」的锚；②"sampler 串行"被 sampler.cpp:882/:890 直接反证；③同仓 p1drz 测试对跨 1/2/4 线程的比对使用 1e-12 **容差** ⇒ 文档口径（与线程数无关，隐含位级）强于测试口径（容差级）。此外已核对的三处行锚全部漂移（495→514、1662→1674、1751→1770-1773），:1834/:1843 两处本代理未逐一定位（留待复核，见 _merge §7 R1）——即便如此，"锚点全部有效"的字面意义已不成立。（**定档纪律**：本条不按「浮点归约危害」定级——实测 `reduction(+:...)` 16 处全为整数计数或计时 double，无科学浮点归约，见 _merge §6 反证节。）
- 影响: 后续按该节做确定性/性能决策（例如据此删掉 thread-local 合并、或据此声称跨 worker 位精确）会踩空；ARCH-004 §4（M8-F-012 的被引依据）继承同一错误期望。
- 建议处置: ①:32 改为按站点分述「位精确（整数/索引/计数器）/ 容差等价（浮点累加与 tile 合并）」两级口径，全部行锚一律以符号重锚；②"sampler 串行"改述为 N-worker 并行 + 其归约形态；③把 p1drz 的 1e-12 容差登记为 docs/science 的正式等价判据（与测试同批）。
- 置信度: 高（三向反证逐字；reduction 类型逐一核）
- 来源: L22-001（定稿）
- related: **M8-F-012**（引用 ARCH-004 §4 的测试）、M8-C-002、M5a-C-002 ⑤、L22 §8 反证①（本代理采纳并复算）

### M8-C-002 `upm.cpp` D1 契约注释声称「worker 数无关，同一 worker 数下位精确」，而同合同在 synthetic_gate 里是 1e-6 容差、在文档里是「标定值等价」

- 类别码: C_DOC_CODE_GAP
- 优先级: **P1**
- 位置: `lib/phase2/src/upm.cpp::D1 注释`（复核时点 :514「worker 数无关, 同一 worker 数下位精确」）；`lib/phase2/tests/synthetic_gate.cpp::Phase2UpmParallel.OneTvsTwoTDetermine`（:220/:244 `EXPECT_NEAR(out1[0], out2[0], 1e-6)`，:259 同配置重复用 1e-12）；`docs/science/PHASE2_UPM.md`（:78「max_abs==0（dense/sparse 1e-12 等价门）」、:94）
- 证据摘录:
  > （upm.cpp:514）D1: 结果与 worker 数无关, 同一 worker 数下位精确
  > （synthetic_gate.cpp:244）EXPECT_NEAR(out1[0], out2[0], 1e-6) << "1T/2T calibrate 差";
  > （synthetic_gate.cpp:259）EXPECT_NEAR(out3[0], out2[0], 1e-12) << "repeat-2T calibrate 差";
- 权威依据: 宪章 §5.3/§6.3、§13.1；§12.3-12
- 问题说明: 注释把两件事混成一句：跨 worker 等价（实际被测的是 1e-6 容差）与同配置重复（1e-12，也不是位比较）。三处口径互不相同（注释「位精确」/测试 1e-6 与 1e-12/文档 max_abs==0 与 1e-12 并列）。此外该 gtest 面在 GTest 缺失时**configure 级静默零注册**（`lib/phase2/CMakeLists.txt:75 find_package(GTest QUIET)`、:76 `if(GTest_FOUND)`、:109 `endif()` 无 else fail-closed；根 `CMakeLists.txt:675-677` 注释自认「GTest 缺失时这些目标零注册」），⇒ 唯一能证伪注释的门有静默消失路径。
- 影响: 读代码的人据「位精确」做等价/回归判断（例如把 1e-6 收紧或放宽）会误判；确定性合同无单一真源。
- 建议处置: ①注释改述为「同配置重复=位精确；跨 worker=1e-6 容差等价」并给出门禁位置；②把容差数值登记进 docs/science（§6.3 冻结容差），代码/文档/测试同源；③给 GTest 缺失路径加 fail-closed 或显式 SKIP 记录（与 M8-G-001 方向②同批）。
- 置信度: 高（三处口径逐字；GTest 守卫逐行）
- 来源: L22-007（定稿；行锚 495→514 重锚）
- related: M8-C-001、M8-F-013（同族构建孤儿）、L22 §8 反证②（SparseEqualsDense 面已被 V3 B3-A4 修复，本代理采纳，见 _merge §3）