# M3b 定稿 — C_DOC_CODE_GAP × P1（域 L06 星点检测与 PSF）

### M3b-C-03 饱和平台星中心输出与合同相反、整数除法两处不同式、拟合失败静默丢星无痕迹
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 来源: L06-014（仍成立，补一条合同互斥）
- 位置: lib/star_detector/src/sdet_api.cpp::rec.cx(:2266)/::饱和局部分支(:1943-1952)/::edge_walking_center(:667)/::失败 continue(:2253)/::去重注释(:827) vs 实现(:847-866)/::A>dynrange 覆盖注释(:2271-2272)；docs/science/STAR_DETECTION.md::§1(:19-22)；docs/algorithms/STAR_DETECTION_ALGORITHMS.md::§2(:45-47)/§4(:129-130)；docs/contracts/DATA_SEMANTICS.md::§17.3(:684 d²≤1.0)
- 证据摘录:
  > - saturation / blend / edge: 饱和判定=3×3 邻域双条件……饱和平台中心=edge-walking 几何中心……
  （STAR_DETECTION.md:19-21）
  >         rec.cx = fit_results[i].cx;
  （sdet_api.cpp:2266——平台星中心亦来自拟合外推）
  >                 xx += (xr + xl) / 2;        （:1943，整数除法）
  >     center_x = (double)(xr + xl) / 2.0;    （:667，浮点除法）
  > - 输出全序：……dedup（饱和优先保、正常星 d²≤1.0，:822-939）……
  （DATA_SEMANTICS:684——实现与 SCI 均为 d²<4.0 :863）
- 权威依据: SCI-P1-STAR-001 §1（亚像素「不引入 0.5px 网格量化损失」、平台中心=edge-walking 几何中心、d²<4.0 保饱和星）；宪章 §12.2、§12.3
- 问题说明: ①主路径输出 rec.cx=拟合外推值(:2266)，edge-walking 结果(:1943-1946)只用于把局部中心移入平台内继续估 Sr/Sc，从不作为输出中心 → SCI §1「平台中心=edge-walking 几何中心」与「平台面积用于等效半径/流量」在现行主路径无实现（独立实现 edge_walking_center :627-670 仅 debug/旧路径）。②同一几何中心公式两处定义不同数值：:1943 整数除法（向零截断、xl 常为负 → 截断方向不对称）vs :667 double 除法，可差最多 1px 舍入方向，正违反 SCI §1。③拟合失败候选 :2253 静默 continue，不产出行也不产状态，与 ALG §4:129-130「饱和星拟合失败→全 0.0f 哨兵+mag=NaN」及 DATA §17.2 invalid 列（0.0f 哨兵，其锚 :1519-1531 实指旧路径）不符——哨兵现仅存在于非生产 debug 路径。④:827 注释「丢弃饱和星」与 :847-866 实现（丢弃正常星保饱和星，符合 SCI/ALG F2）相反，误导性注释。⑤:2271-2272 注释自认候选 3×3 双条件「最终被 A>dynrange 覆盖」，SCI §1 双条件只影响 sat 字段选择与拟合掩膜、不决定最终饱和标志——此点 SCI/ALG 未写明。⑥新增残余：DATA §17.3 :684 把去重阈写成 d²≤1.0，与 SCI:21 与实现 :863 的 d²<4.0 直接矛盾（合同汇总节错值）。
- 影响: 饱和星位置与流量语义与冻结合同不同；主路径丢星无痕迹（不可审计）；调试路径与生产路径不可对照；按 DATA §17.3 重实现者会得到不同星集合。
- 建议处置: owner 裁决平台星中心权威定义（采外推则改写 SCI 并登记偏差；采几何中心则输出 :667 式并把 :1943 统一为 double 除法）；失败候选产显式状态或按 ALG 产哨兵；订正 :827/:238 注释、DATA §17.2 invalid 列与 §17.3 去重阈。
- 置信度: 高
- related: M3b-A-01、M3b-A-07、M3b-C-01、M3b-D-01、M3b-E-01

### M3b-C-04 PSF [N,9] 双布局「布局 A」两列无对应缓冲，两份 L1 合同的 A/B 标签相反
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1（与 M3a 的 L13-005 会签，同一事实只在一处定稿——本条为 L06 侧登记点）
- 来源: L06-015（仍成立）
- 位置: docs/algorithms/STAR_PSF_ALGORITHMS.md::§1.1(:12-23)/§1.2(:25-45)；docs/contracts/DATA_SEMANTICS.md::§15.2(:513-546，A=编排块/B=批 API)；lib/star_detector/module.yaml:155-162（复述 ALG 版标签）；lib/dynamic_psf/include/dynamic_psf.h(:84-98 批缓冲列)/::注释(:55-57)；lib/dynamic_psf/src/dpsf_psf.cpp(:901-911,:1069-1079 写入 fwhm_x/fwhm_y)；lib/orchestrator/cpp/src/orchestrator.cpp(:2440-2458)
- 证据摘录:
  > [N,9] (B, A, x0, y0, sx, sy, θ, residual_scale, q_psf)
  （ALG §1.1:14）；而真实批缓冲 :910-911/:1078-1079 为 out_row[7]=result.fwhm_x; out_row[8]=result.fwhm_y;
  > **布局 A：编排层 psf 块** / **布局 B：批 API out_psf_params**
  （DATA §15.2:513/:530——与 ALG/module.yaml 的 A/B 恰好相反）
- 权威依据: 宪章 §12.3（同一语义在两处文档须字面一致或显式互引）、§17.2；DATA-P1-PSF §15.2；ALG-STARPSF §1.1/§1.2
- 问题说明: ①标签冲突：ALG §1.1/§1.2 与 module.yaml:156-158 称「A=科学参数序、B=生产编排块序」，DATA §15.2 称「A=编排 psf 块、B=批 API out_psf_params」——两份 L1 对同名标签给相反定义；ALG:23 让读者「详见 DATA §15 生产表布局 B 对照」而 ALG-B 自己就是生产表，交叉引用互相指错。②ALG「布局 A」的后两列 residual_scale/q_psf 在任何真实缓冲中不存在（批缓冲 8/9 列=fwhm_x/fwhm_y；q_psf 在 lib/dynamic_psf 全模块无定义无计算，出处仅 ALG:58/78 与 snr_estimator）；residual_scale 只在 DATA:524-537 作为「mad 列的历史语义」存在。前两项 x0/y0 为相对偏移，与 DATA 表标注绝对 cx/cy（0-based 全图）也不同。③ALG:19 单位串「B/A/flux/ADU 域」把不存在的 flux 列与 px 列混在一格。④dynamic_psf.h:55-57 给 _f 通道的注释描述的列序（status/B/flux/cx/cy/fwhm/A/mad/eccentricity）是编排序列化布局，而 _f 实际返回 DPSFFitResult 结构体——注释-接口再一层错位。
- 影响: 任何据 ALG §1.1 解析批缓冲 [N,9] 的消费者把 fwhm_x/fwhm_y 当 residual_scale/q_psf（数值量级完全不同，静默）；PSF 块单位/域声明在接口处不可判定。
- 建议处置: 以 DATA §15.2 为唯一布局表并删/改 ALG §1.1 与 module.yaml:155-162 的不存在列（或改名为「LM 参数向量内部序」并锚 dpsf_psf.cpp:347）；A/B 标签统一；补 q_psf 产出位置声明；订正 dynamic_psf.h:55-57 注释。
- 置信度: 高
- related: L13-005（M3a 并档）、M3b-A-03、M3b-E-01、M3b-D-01

### M3b-C-05 SDetParams 五字段中四个全仓零读取而文档称有消费面；检测阈三套并存；伪代码与实现相反
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 来源: L06-016（仍成立）
- 位置: lib/star_detector/include/star_detector.h::SDetParams(:17-27)；lib/star_detector/src/sdet_detector.cpp::sdet_get_structure_map(:14-56，仅读 hotPixelFilterRadius :22-23；:31 硬编码 clip 3.0f/3)；lib/star_detector/src/sdet_image.h(:38-46)；lib/star_detector/src/sdet_api.cpp::sdet_create 默认值(:968-978)；docs/algorithms/STAR_DETECTION_ALGORITHMS.md::§11.1(:212-218)/§3(:93)；docs/algorithms/STAR_PSF_ALGORITHMS.md::§3(:67)；lib/star_detector/README.md:37/:178-180
- 证据摘录:
  > structureLayers/hotPixelFilterRadius/iterativeClipSigma/iterativeMaxRounds/medianFilterDetail 仅旧 sdet_get_structure_map 路径消费（sdet_detector.cpp:14-56），生产 impl 不调用结构图。
  （ALG §11.1:216-218）——实测该函数只读 hotPixelFilterRadius，其余 4 字段全仓无读取点（M3b grep：仅头文件/默认值/测试赋值）
  >     sdet_dynamic_regional_background(s1, smap.data(), w, h, 100, 20, 20, 4, 3.0f, 3, 16);
  （sdet_detector.cpp:31——clip_sigma/max_rounds 硬编码 3.0f/3，而默认 iterativeClipSigma=9.0f/iterativeMaxRounds=5，同参数两套值按调用路径随机生效）
  >     float threshold = bkg + 1.5f * sigma; // centroid 1.5 sigma (STAR_PSF_ALGORITHMS.md)
  （sdet_detector.cpp:222）
  >     if not local_max_11x11(smooth,y,x): x+=5; continue   # :1715-1734
  （ALG §3:93——实现在未命中分支只 continue，命中后才 :1843 x += r，方向相反）
- 权威依据: 宪章 §4.2（参数走配置、无隐藏常量）、§14.4（禁死参数）、§12.3；ALG-STARDET-001 §2/§11.1；DATA-P1-STAR §17.1（SDetParams 消费面缺口=DISP-STAR-003）
- 问题说明: ①消费面登记不实：ALG/DATA/module.yaml 声称 5 字段由旧结构图路径消费，实际其中 4 个（structureLayers/iterativeClipSigma/iterativeMaxRounds/medianFilterDetail）全仓零读取——DISP-STAR-003 的登记内容本身失真（「已登记偏差的描述与代码事实不符」形态，与前台 DISP-WCS-006 同模式）。②检测阈三套并存互指：生产 peaker=median+5.0·bgnoise（:1754）、sdet_detector 质心路径=1.5σ(:222)、README:179 称桥接层「3σ 阈值」（实际节点构造 StarDetector(5.0)）、ALG-STARPSF §3:67 把 1.5σ 当 PSF 候选生成算法登记——同一「检测阈」四个数字且文档互相指认。③ALG §3 伪代码 :93 与实现相反（见证据），按文档重实现即偏离，且与 ALG §5/§6 自身「同行右移 x+=r」叙述冲突。
- 影响: 配置面 4 字段无效但被视为有效（§4.2 违背）；审计者无法判定产品实际检测阈；伪代码错误直接误导再实现。
- 建议处置: 删除或接线死字段（owner 裁决），把 ALG §11.1/module.yaml 的消费面描述改为如实「仅 hotPixelFilterRadius 消费，其余无读取点（DISP-STAR-003 更新）」；三处阈值各归其位并在 DATA/GLOSSARY 声明唯一生产阈；订正 ALG §3:93 与 README:179。
- 置信度: 高
- related: M3b-C-02、M3b-A-04、M3b-E-01、DISP-STAR-003、前台「偏差登记表可信度」主题
