# V1 复核新发现 · P1（SNR 新模型的同源断裂，本批引入）

> 对象 = `b0353303` P5（SNR 改逐源最优提取，含 S1–S10 冻结文档订正，负责人授权 §1.2）+ `35c85f53` P8（交付接线）。
> **交付闭合只存在于 P8**：P5 单独看**不在根交付图**（`CMakeLists.txt:210-215` 已按 F-CI-002-01 解除 `add_subdirectory(lib/snr_estimator)`），且 P5 commit 自陈"Linux 二进制改前/改后 sha256 完全相同" ⇒ **两半必须一起复核**（V1 的时点警告，已采纳）。
> 新模型口径（V1 全文抄录于 `_verify/V1.md §1.1`）：Horne 1986 最优提取 `sigma_F^-2 = SUM_i P_i^2/sigma_i^2`、`SNR_F = F/sigma_F`；Moffat4（beta=4）解析孔径改正 `f_in = 1-(1+r^2/(2 sigma^2))^-3`；`m_5 = ZP - 2.5*log10(5 sigma_F)`；零点标准误 `1.253*sigma/sqrt(N)`。

## V1-N-04 授权订正后的 SCI 文本一落地即与代码相反：宣称「当前实现不产出 sigma_F、不把任何标量称为科学 SNR」，而 P8 正把 `snr_f/sigma_f_adu` 逐源写进交付 JSON
- **ID**：V1-N-04 ｜ **类别**：C_DOC_CODE_GAP ｜ **优先级**：**P1**
- **位置**：`docs/science/CONTROL_WEIGHT_SNR.md:::44`（新写句）与 `::§7`（新红线句）；生产者 `lib/snr_estimator/**snr_science.cpp:178-181`（产出 `sigma_f_optimal_adu`）→ `lib/core/src/module_adapters.cpp:2544-2546`（逐源写 `snr_f`/`sigma_f_adu` 进交付 JSON）、`:2520`（`snr_phot = median(SNR_F)`）
- **证据摘录**：文本「当前实现**不产出** `sigma_F`，故本合同不把任何现有标量称为科学 SNR」；而同一批的接线代码把 `sigma_f_adu` 与 `snr_f` 作为**交付字段**输出、并把 `snr_phot` 定义成 `median(SNR_F)`。`§7` 新写的「不得把 `local_snr`/`frame_snr` 解释为科学信噪比」同样与 `:2520` 的命名冲突。
- **问题说明**：这是**授权编辑自身的产物**，不是旧债：P5 改文档时按"实现尚未产出"的旧状态写否定句，P8 紧接着在同批接线产出该量 ⇒ **两份文档在数小时内同时失实**。宪章 §12.3 与 AGENTS「科学定义=算法=接口=代码=测试」要求同提交同源；文本一旦与交付字段矛盾，下游（HiPS 消费者、审核包、精度门）无从判断哪面是权威。
- **依据条款**：宪章 §12.3、§17.2 追溯完整、§4.1 字段语义不混。
- **建议处置**：把 `:44` 与 `§7` 改写为**现行事实**（`sigma_F`/`SNR_F` 已产出并逐源交付；`snr_phot` 是 `median(SNR_F)` 的帧聚合，二者关系写明），并把"哪个标量可称科学 SNR"与 `snr_schema` 版本号绑定；顺带把该结论回写 `docs/algorithms/NOISE_ESTIMATION.md:234-237`（那里仍称新通道为 legacy heuristic，且锚因头文件 +116 行漂移）。
- **取证口径**：V1 静态逐字 + 我复核锚点与生产者三处 ｜ **状态**：OPEN ｜ **置信度**：高 ｜ **related**：**V1-N-02**（同一语义的持久化面）、`M3-A-002`/A-02（§6.3 红线，见下）、`V1-N-03`、`L28e-E-003`（ALG 层缺描述同族）

## V1-N-02 SNR 物理量改了但二进制版本位没改 ⇒ 新旧 `.hiss` 同标签承载不同物理量；JSON 面加了 `snr_schema` 而二进制面没加
- **ID**：V1-N-02 ｜ **类别**：C_DOC_CODE_GAP ｜ **优先级**：**P1**（数据可追溯，§4.1/§16.2）
- **位置**：`lib/astro_image_io/src/healpix/aio_healpix_io.h:52/58/64`（仍注 `snr_psf=(A-B)/mad`、`snr_phot=1/(ln10*sigma_residual)`）、`snr_format=1` **未升版**、读写侧 `aio_healpix_io.cpp:1890/2140` 原样搬运；对照 JSON 面 P8 新增 `snr_schema="DATA-P1-SNR/2"`；`docs/contracts/DATA_SEMANTICS.md` 对 `DATA-P1-SNR` 与 `p1_snr` **零命中**（三级复核），`"/2"` 无登记
- **问题说明**：**同一列名/同一格式版本下换了物理量定义** ⇒ 已发布的 `.hiss` 与新产物**无法区分**，任何下游按 `snr_format=1` 解读新文件都会用旧公式反演（旧：`(A-B)/mad` 与 `1/(ln10*sigma)`；新：`F/sigma_F`）。这是 §4.1 字段分离与 §16.2 清单可判别性的直接违背；而 JSON 面**已经**加了 schema 标签，说明作者知道该怎么做，只是**半同步**。
- **依据条款**：宪章 §4.1、§16.2 发布清单、§17.2。
- **影响**：不改当前数值计算，但**破坏跨版本可追溯**：新旧产物混读会产生系统性 SNR 偏差且**无任何机器面能发现**（旧文件不会报错，只会静默错解读）。
- **建议处置**：①`snr_format` 升 **2**（或在头部加 `snr_schema` 同名位），读侧对 `1` 保留旧语义解释、对 `2` 用新语义；②`aio_healpix_io.h` 三处注释改指新定义并标版本；③把 `DATA-P1-SNR/2` 登记进 `DATA_SEMANTICS.md`（现在整条零命中）；④`SnrModel`/`SnrModelV2/V3`/`PhotometricCalibrationQuality` 尾部追加 2~3 个 `double` 使 `sizeof` 已变，而全仓**无任何 `sizeof` 断言** ⇒ 补一条（当前该 DLL 不在根图故无跨二进制暴露，属"声明失实 + 潜在越界写"）。
- **取证口径**：V1 静态（三级复核确认零命中）｜ **状态**：OPEN ｜ **置信度**：高 ｜ **related**：**V1-N-04**、`C-12`（证据/产物新鲜度）、`M9-H-2`（同族：格式位与自报值不校验）、簇 1 第三机制
