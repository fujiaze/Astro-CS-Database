# M3 定稿 · findings · C_DOC_CODE_GAP · P1（来源切片 L05 + L07）

> 合并代理 M3（第二层合并验证）｜来源 L05 + L07｜逐条重读原文复核；位置一律 `<path>::<符号/关键语句>`，行号为复核时点参考值。逐条处置表与降级清单见 `../../_merge/M3.md`。

---

### M3-C-004 `lib/calibration/CALIBRATION_PROCESS.md` 与冻结 SCI/现行实现冲突，文件自身无历史/非规范标记（P0→P1 改判）
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1（原判 P0；降级理由见「问题说明」末段）
- 来源: L05-002
- 位置:
  - `lib/calibration/CALIBRATION_PROCESS.md::§2 算法流程 / §4 Dark 优化 / §5 技术细节`（复核时点 :52-62、:66-79、:81-84、:12/:19/:31/:35）
  - `docs/science/CALIBRATION.md::§5:46-47、§10:100-101、§8:73-78、§9:83`
  - `docs/algorithms/CALIBRATION_ALGORITHMS.md::§13 F2.1/F2.3/F2.4`（:100-136）
  - `lib/calibration/src/dark_optimizer.cpp::optimize_dark_k`（复核时点 :97-200）
  - `lib/calibration/src/cosmetic_corrector.cpp::detect_hot_pixels / interpolate_pixels`（:119-134、:169-218）
  - `lib/calibration/README.md:::147`（仅 README 侧称其为「历史流程参考」）
- 证据摘录（复核时点重读）:
  > 残差最小化优化：… 通过残差最小化（背景区域 MAD 最小）搜索最优 K 值（PROCESS:56）
  > - 搜索范围：K_nominal ± 10s / t_dark（PROCESS:62）
  > - 基于 Light 帧构建检测模板：… 计算 5x5 局部中值滤波与全局中值的偏差 …（PROCESS:73-75）
  > - 双线性插值：对坏点取 8 邻域中有效像素值做距离加权插值；若邻域全为坏点则保持原值；若存在 NaN 则退化为最近邻原始值（PROCESS:84）
  > 默认 3.0（PROCESS:12/:19/:31）
  > - 将 `K` 改为优化搜索值而非 `t_light/t_dark` 比值；（SCI §10:100）
  > 冷像素检测：候选 = (bias < median − sigma_cold×sigma) AND (dark < median_dark)（ALG-CAL:136）
  > - 搜索范围 `max(0.8, k_init−delta_max) ~ min(1.25, k_init+delta_max)`，delta_max 默认 0.05（dark_optimizer.cpp:108-110 区域）
- 权威依据: 宪章 §1.1（模块级 README/流程文档属 L7，与 L2/L3 冲突时无效）、§12.3-10、§11；SCI-CAL-001 §5/§8/§10
- 问题说明: 四处独立冲突：① K 搜索（PROCESS:56/:62）与 §10:100 的禁止项直接冲突；② cosmetic 检测改为「基于 Light 帧 5×5 模板 + 全局偏差阈值 + `max_neighbor_candidates`」，与实现的「从 Dark 全局 MAD 阈 + 8 邻接连通域大小过滤」（cosmetic_corrector.cpp:119-134/:84-96）和 ALG 的冷像素双条件（:136）三套不同；③ 回填算法（双线性、NaN→最近邻、邻域全坏保原值）与 `interpolate_pixels` 的「4 正交方向 1/dist IDW、权重和为 0 时保原值」（:194/:205-213）不符；④ sigma-clip 系数写 3.0（:12/:19/:31），实现与 ALG/SCI 均为 5.0（`kSigmaClip=5.0f`、SCI:83）。
  降级理由（复核时点）：该文件不驱动代码，实现全部与 ALG/SCI 一致，不改变当前科学输出；且 `lib/calibration/README.md:147` 已把它标注为「历史流程参考（含已被现行实现取代的旧描述，禁止作为实现输入）」。仍然成立的事实是：**文件自身没有历史/非规范标记，也不在 `docs/archive/`**，任何直接读到它的 Agent/维护者会拿到与 FROZEN 合同相反的规范；README 的一句话不足以阻断误读（宪章 §12.3-10 要求活动文档不得以历史状态冒充当前状态）。
- 影响: 误导后续实现与真实数据终验的判据来源；K 优化、cosmetic 检测与回填三处都可能被"照文档实现"从而引入 §10 禁止项。属规范性文本层缺陷。
- 建议处置: 在文件首行加与 `AstroCS_ENGINEERING_CONSTRAINTS.md` 同款的 `ARCHIVED_NON_NORMATIVE`/「历史流程参考，禁作实现输入」标记（或移入 `docs/archive/`），并把 §4/§5/§8 三节按现行 SCI/ALG 重写为现状描述。
- 置信度: 高
- related: M3-C-009（orchestrator 侧 mode/K 不生效）、DISP-CAL-005、L12（跨域）

---

### M3-C-005 退化 master flat 的 fail-closed 只覆盖 IR 节点通道（并发修复已落地并有回归测试），p1_session 会话通道仍无等价校验
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1（原判 P0；**部分修复**，本条为残余事实）
- 来源: L05-004
- 位置:
  - 已修复面：`lib/core/src/module_adapters.cpp::p1_master_flat_valid`（复核时点 :1153-1179）与 `::p1_op_calibrate` 的 `degenerate_flat_artifact` fail-closed 分支（:1230-1240，`error_kind="input"` + rc=5）
  - 残余面：`lib/phase1_session/p1_session.cpp::calibrate 阶段`（flat 读取 :303-309，仅尺寸校验 :316-319，直传 `ac_calibrate_frame` :339-345）
  - 同型缺失：`lib/cosmetic/src/module_entry.cpp::calibrate_frame`（内部库通道）与 `lib/cosmetic/src/real_ingest.cpp:165-168`
  - 回归测试：`tests/cli/test_phase1_inprocess.py::TestPhase1InProcess::test_10_invalid_master_flat_fail_closed`（:255-290，断言 rc≠0 + `degenerate_flat_artifact` + `error_kind=input`）；`tests/backend/phase1_fixture_main.cpp::--make-bad-flat`（:119）
- 证据摘录（复核时点重读）:
  > // B2-A6 修复: 退化 flat 不得静默降级为 "无平场" …（module_adapters.cpp:1148-1152 注释块）
  >         if (!p1_master_flat_valid(fm.w(), fm.h(), fm.px())) {（:1231）
  >         // 残留全零风险已消除(见 flat 退化检查)。（p1_session.cpp:318，flat 尺寸不符分支的注释）
  >         if (flat_w != w || flat_h != h) { s->last_error = … }（p1_session.cpp:317 —— 全函数仅此一处尺寸判据；grep `isfinite|median|all_zero|degenerate` 于该 calibrate 段 = 0 命中）
- 权威依据: 宪章 §14.4（退化输入不得被静默解释为正常）、§11、§17.2；SCI-CAL-001 §8（`flat 全零` 行为须显式）
- 问题说明: 并发提交期间 B2-A6 已在 IR 节点通道补上门（非有限/median≤0/全零 → fail-closed），并有 CLI 级回归测试钉住，故叶子原判「legacy 与 IR 两通道都无校验」在复核时点只对**一半**成立。残余事实：会话通道 `p1_session.cpp` 的 calibrate 段对 flat 只做尺寸校验，退化 flat（全零/median≤0）仍按 `ac_calibrate_frame` 的 floor 0.1 语义产出约 ×10 放大帧，manifest 无 `error_kind`；该文件仍经 `cli/CMakeLists.txt:201` 编入 `astrocs`（p1_session.cpp:115 注释自述 "astrocs.exe 内嵌 p1_session_run"），即两条同域实现的有效域判定不一致。CLI `astrocs phase1 run` 的实际路由为 IR 节点通道（路由结论由 L12 §跨域线索回执①提供：`cli/runtime_client.cpp:117` 建 IR 图 + `make_session_module<P1Api>` 全仓零命中），故本条不升 P0；但 `astrocs_p1_calibration.dll`/会话 API 直调方仍可命中残余面。
- 影响: 同一退化输入在两条通道得到「拒绝」与「产出 ×10 伪产品并报 stage ok」两种结果；直接调用会话/legacy API 的用户与真实数据终验的 legacy 组不受保护。无跨通道一致性证据。
- 建议处置: 把 `p1_master_flat_valid` 提为共用判定（或由 `ac_calibrate_frame` 边界返回 `AC_ERR_PARAM`，见 M3-A-003 建议 b），会话通道与 `lib/cosmetic/src/calibrate_frame` 同型补齐；为会话通道补一条与 test_10 对称的回归测试，并在回归测试中同时钉住两通道（否则修复会再次单边化）。
- 置信度: 高
- related: M3-A-003、L12（路由证据，主责 M5）、F00-04、M3-G-001

---

### M3-C-006 公共符号数量在 ALG/README 写 14、头文件与构建注释写 12（实测 12 个 `AC_API`）
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 来源: L05-007
- 位置: `docs/algorithms/CALIBRATION_ALGORITHMS.md::§1 / §13 / 附录`（复核时点 :16、:207、:415）、`lib/calibration/README.md::§API 面`（:63）对照 `lib/calibration/include/astro_calibration.h`（AC_API 声明实测 12 处：:38/:47/:58/:79/:102/:122/:128/:134/:140/:147/:158/:161；文件头注释 :9「12 个 legacy 符号」）、`lib/calibration/CMakeLists.txt:::8`（「legacy 12 个 AC_API 符号」）
- 证据摘录:
  > 唯一公共头，14 个 …（ALG-CAL:16）；14 个 `AC_API`（ALG-CAL:207）；14 符号（ALG-CAL:415）
  > （14 AC_API 符号）（README:63）
  > 头 :9 注释「…12 个 legacy 符号」；构建注释「legacy 12 个 AC_API 符号」
- 权威依据: 宪章 §12.3-1（活动文档的事实性数字须可机器核对）、§11（证据可验证）、§1.1
- 问题说明: ALG 自己在 :207 的清单逐项数下来恰为 12，公共头与构建清单均为 12，三处「14」为失实计数。该数字是「公开符号面完整」类断言的锚，一旦有新增/删除，14 会掩盖真实差异（本次复核正是靠逐行 grep 才发现口径冲突，而非靠任何机器门）。
- 影响: 合同与 README 的 API 面计数与真源不符；任何按 14 做的符号面核对会得出「少两个符号」的假结论。
- 建议处置: 改三处「14」为 12，或（更好）由 `tools/` 机器门从公共头统计注入，禁止手写计数。
- 置信度: 高
- related: M3-E-001（15 符号/12 符号第三种口径）、M3-I-001、M3-A-004

---

### M3-C-007 registry 页引用两个全库未定义的合同 ID，并与 `module.yaml`/`ac_version()` 形成版本与状态三口径
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 来源: L05-009（模块 ID 词表部分另立 M3-C-008）
- 位置: `docs/modules/registry/astrocs.phase1.calibration.md:::7 upstream 行 / :56`；对照 `lib/calibration/module.yaml:::15-20`、`docs/contracts/INDEX.yaml`、`lib/calibration/src/ac_api.cpp::ac_version`（:136-138）
- 证据摘录（复核时点重读）:
  > upstream: [SCI-P1-CAL-001, ALG-P1-CAL-001, API-P1-001]（registry:7；:56 再次出现 ALG-P1-CAL-001）
  > # 合同 ID 溯源（全部 ID 均在 docs/contracts/INDEX.yaml 定义：SCI-P1-CAL-001、ALG-P1-CAL-001、API-P1-001、DATA-P1-CAL/…（registry 正文，复核时点 :62 区）
  >   id: astrocs.p1.calibration / version: 0.11.0-alpha.2 / status: CONTRACT_READY（module.yaml:15-20）
  > const char* ac_version() { return "Astro Calibration C++ v1.0.0"; }（ac_api.cpp:137）
- 权威依据: 宪章 §12.3-1/-4（追溯锚点可验证）、§11；`docs/traceability/TRACEABILITY_SPEC.md:78` 的 ID 文法；`docs/contracts/INDEX.yaml` 为合同 ID 唯一事实源
- 问题说明: `SCI-P1-CAL-001` 与 `ALG-P1-CAL-001` 在整个 `docs/` 树中**只出现在这一张 registry 页**（INDEX.yaml、SCI/ALG 文件、TRACEABILITY.csv 内均无），即 registry 的「合同 ID 溯源」声明与 INDEX 事实源冲突；同页与 module.yaml 的版本（1.0.0 vs 0.11.0-alpha.2）与状态（ACTIVE vs CONTRACT_READY）不一致，另加 `ac_version()` 字符串 `v1.0.0`，构成三口径。
- 影响: 门禁证据链在模块级断裂：按 registry 页追合同会落到不存在的 ID；版本/状态不一致使「模块是否 ACTIVE」不可判定（同域另一实例见 F00-04）。
- 建议处置: 把 registry 的 upstream 改指真实 ID（`SCI-CAL-001`/`ALG-CAL-001..006`），或由负责人正式登记 `*-P1-*` 词表到 INDEX；版本与状态以 module.yaml 为单一事实源，registry 页改为引用而非复述。
- 置信度: 高
- related: M3-E-001、M3-C-008、M3-I-001、F00-04

---

### M3-C-008 模块 ID 存在 `astrocs.phase1.*` 与 `astrocs.p1.*` 两套词表，同一模块在 descriptor/registry 与 module.yaml/types.h 中不同名（与 F00-04 同根因）
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 来源: L05-012
- 位置: `lib/core/src/module_adapters.cpp::descriptor 赋值`（复核时点 :467 `d.module_id = "astrocs.phase1.calibration";`、:624 同型 cosmetic）对照 `lib/calibration/module.yaml:15`（`astrocs.p1.calibration`）、`lib/cosmetic/include/astrocs/cosmetic/types.h:19`（`inline constexpr const char* kModuleId = "astrocs.p1.cosmetic";`）、`lib/cosmetic/module.yaml`、`docs/pipeline/integration.json:5`；registry 目录名与页面亦用 phase1 拼写
- 证据摘录:
  > d.module_id = "astrocs.phase1.calibration";（module_adapters.cpp:467）
  >   id: astrocs.p1.calibration（module.yaml:15）
  > inline constexpr const char* kModuleId = "astrocs.p1.cosmetic";（cosmetic types.h:19）
- 权威依据: 宪章 §12.3-2（同一标识符不得存在两套有效词表）、§8（模块标识须唯一）、§12.3-1
- 问题说明: 两套词表在仓库内同时活跃使用：descriptor/registry/GRAPHIR 用 `astrocs.phase1.*`，module.yaml/types.h/模块 manifest 用 `astrocs.p1.*`，二者无归一化登记。按任一拼写做机器核对都会漏掉另一套，导致 manifest 与 descriptor 的模块身份无法直接对齐（前台 F00-04 在测光模块上发现同根问题：模块登记与构建/安装/打包面不一致）。
- 影响: 追溯与装配核对失效（自动化脚本按 ID 匹配时会静默漏项），属跨模块一致性问题；不改当前数值。
- 建议处置: 提请选定唯一词表并在 `docs/GLOSSARY.md`/`docs/contracts/INDEX.yaml` 登记；对存量两套做一次全仓替换并加机器门（禁止 `astrocs.p1.*` 与 `astrocs.phase1.*` 混用）。与 F00-04 并档同根因，不重复登记测光实例。
- 置信度: 高
- related: M3-C-007、F00-04（前台基准）、M5（测光双实现实例）

---

### M3-C-009 编排器（A 线）的 `calibration.mode/fallback/light_exposure_s/dark_exposure_s` 被 config 接受但无科学效果：`dark_opt` 恒 0、`dark_k` 恒 1.0，PROCESS 的 ±10s 校验不存在
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1（原判 P0；**部分修复 + 叶子证据失效后按当前树重写**）
- 来源: L05-013
- 位置:
  - `lib/orchestrator/cpp/src/orchestrator.cpp::阶段 2 校准设置`（复核时点 :945-975）、`::run_stage_calibration` 的 dark 曝光处理与调用点（:1069-1088、:1157-1176）
  - `lib/orchestrator/cpp/include/json_config.h::Stage1Config.calibration`（:34-39）与 `lib/orchestrator/cpp/src/json_config.cpp::calibration 解析`（:119-123、:476-477）
  - 对照 `lib/core/src/module_adapters.cpp::p1_op_calibrate 的 B2-A13 分支`（:1241-1312）
  - `docs/development/CONFIG_SCHEMA.md`（全文 85 行，无 `mode`/`exposure_ratio`/`fallback` 任何一项）
  - `lib/calibration/CALIBRATION_PROCESS.md:62`（±10s/FILTER 规则）
- 证据摘录（复核时点重读）:
  >     int dark_opt = 0;（orchestrator.cpp:957）
  >     double dark_k = 1.0;（:958 —— 复核时点全函数内两变量在 :1162/:1170/:1174/:1268 只被读取，无任何赋值/推导）
  >     if (calib_mode == "optimal" || calib_mode == "exposure_ratio") { … 额外要求 master_dark/master_bias … }（:962-974 —— mode 只改变「必须存在哪些母版文件」）
  >     // B2-A14 修复: 不再静默回退出光时间 …（:1069-1088 —— 从 dark 文件名解析 t_dark，仅用于日志/告警）
  >         const std::string fb = light_exp_src == "exposure_time" ? "none" : "light_path_fallback";（:1158）
  >         config.calibration.light_exposure_s > 0.0 && fabs(t_light - …) > tol …（:1082-1088 —— 告警）
  > 搜索范围：K_nominal ± 10s / t_dark（PROCESS:62）
  > **B2-A13**: `dark_scale_mode=optimized` 在 config 中暴露却从未实现；现改为显式使用 `dark_optimization` 布尔 …（module_adapters.cpp:1243 注释）
  > `calibration.mode=optimal/exposure_ratio` 仅当 `light_exposure_s` 与 `dark_exposure_s` 均提供时执行 ±10s/`FILTER` 校验 …（旧 CONFIG_SCHEMA 引用见「问题说明」）
- 权威依据: 宪章 §14.4（参数无效不得静默接受）、§12.3-1/-10、§11、§1.1；SCI-CAL-001 §5/§10:100；PROCESS:62
- 问题说明: 叶子原文（`dark_scale_mode`/`"optimized"` 词表 + `CONFIG_SCHEMA.md:264-265` 的 exposure_ratio 规则）**在复核时点已不存在**：该枚举已被 B2-A13 改造为 `dark_optimization` 布尔（module_adapters.cpp:1243），`docs/development/CONFIG_SCHEMA.md` 现仅 85 行且不含任何 mode/exposure 校验条款。按 R1 以符号重定位后，仍然成立的事实是：A 线 `Stage1Config` 仍解析并接受 `mode`（枚举 `standard|optimal|exposure_ratio`，json_config.cpp:119-123）、`fallback`、`light_exposure_s`、`dark_exposure_s`、`exposure_time_tol_s` 五个键，其中：`dark_opt`/`dark_k` 是常量（永不调用 K 优化，输出等价 standard 路径），`fallback` 只写入 manifest 的 `degradations` 字符串，曝光键只做「解析值 vs 配置值」的告警比较；`optimal/exposure_ratio` 的唯一作用是要求额外母版存在（bias 被要求加载但不参与实际计算，因 `dark_opt=0`）。因此「按曝光比自动选 dark 尺度」这一被 config 暴露的能力，在 A 线仍然不存在。
  与 IR 通道的差异（部分修复）：IR `p1_op_calibrate` 已实现 `dark_optimization` + `dark_exptime > 0` 校验 + `k_expo = t_light/dark_exptime` 并跨检 `dark_scale_k`（:1280-1306），故本条不再是「全仓无 K 实现」，而是「同一 config 语义族在两条通道实现程度不同 + A 线仍有 5 个无效果键」。
- 影响: A 线（`lib/orchestrator/cpp` 自构建产物 `astrocs_orchestrator`，未在根 CMake 出现，交付性由 M5 判）用户配置 mode/曝光相关键得到「成功」回执与 ok manifest，但校准公式仍是 `dark_opt=0` 分支；PROCESS 的 ±10s/`FILTER` 规则在两通道皆无（只有一处非阻断告警），合同文本层三套规则并存。
- 建议处置: (a) 删除 `fallback/light_exposure_s/dark_exposure_s/mode` 中确无实现意图的键，或 (b) 把 mode 语义对齐 IR 通道的 `dark_optimization` 实现（含 `k=t_light/t_dark` 推导与校验），并在 PROCESS 之外只留一份规范文本；无论哪条，config 层对未生效键必须报错或 WARN（宪章 §14.4）。
- 置信度: 高（结论限定于 `lib/orchestrator/cpp` 这一路；IR 通道已有真实 K 推导与校验）
- related: M3-C-004、M3-C-005、L12-017（同型参数无效，主责 M5）、M3-A-003

---

### M3-C-010 ALG-PHOT 声明输出含 `zero_point` 字段，接口与实现均无该字段
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 来源: L07-008
- 位置: `docs/algorithms/PHOTOMETRIC_FIT.md:::9 输出行`；对照 `lib/snr_estimator/cpp/include/snr_estimator.h::PhotometricCalibrationQuality`（:36-47）、`lib/photometric_calib/cpp/include/photometric_calib.h::PhotometricCalibration`（复核时点 :54 区）
- 证据摘录（复核时点重读）:
  > 输出: PhotometricCalibrationQuality (sigma_mag, sigma_cal_rel, **zero_point**) + scale（ALG-PHOT:9）
  > typedef struct { int n_matches; double sigma_logflux_dex; double sigma_mag; double sigma_cal_rel; uint32_t fit_status; } PhotometricCalibrationQuality;（snr_estimator.h:36-47，无 zero_point）
  > PhotometricCalibration 成员：scale/fit_used/robust_iterations/sigma_logflux_dex/sigma_mag/sigma_cal_rel/outlier_rate/n_gaia_used/n_psf_used（photometric_calib.h:54-66，无零点项）
- 权威依据: 宪章 §12.3-1（活动文档对代码状态的事实性陈述须逐符号命中）、§11、§4.1
- 问题说明: ALG 与 README（:7）宣称的 `zero_point` 字段在两个结构体中都不存在；实现只有 `location`（局部变量，未输出，经 `scale=10^(−location)` 隐式携带）。同一术语在 `lib/astrometry/…/plate解算器::apply_zero_point_correction` 里另指天测零点修正，跨域同名不同概念。
- 影响: 下游按 ALG 消费零点（做时变零点或跨帧比对）时拿不到字段，只能自行反解 `−log10(scale)`；QA 面与文档语义不闭合。
- 建议处置: 从 ALG:9 与 README:7 删除该字段声明，或新增真实输出 `double zero_point`（`= −location`，单位 dex）并加对应单测；术语条目在 `docs/GLOSSARY.md` 与天测侧做显式区分。
- 置信度: 高
- related: M3-C-011、M3-E-002、L18 域同名术语（本条不重复登记）

---

### M3-C-011 `sigma_residual=0` 一值三义、`fit_status` 编码冲突，且暴露一个实现完全不读取的配置字段
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 来源: L07-019
- 位置:
  - `lib/photometric_calib/cpp/src/star_matcher.cpp::cleanAndScale`（初值 :552；S==0 跳过 IRLS 的分支 :488-491；`mad_in>0` 才换算 :558）
  - `lib/photometric_calib/cpp/src/pc_api.cpp`（一致集为空 → `sigma=0.0` :91、:104）
  - `docs/algorithms/PHOTOMETRIC_FIT.md::F2.4/F2.5`（:36、:45 把 `sigma=0` 同时写成「完美」与「退化」）
  - `lib/snr_estimator/cpp/src/noise_model.cpp::snr_phot_cal_quality`（`sigma<=0 → fit_status=2`，复核时点 :281-284）
  - `lib/snr_estimator/cpp/include/snr_estimator.h::SnrNoiseModelConfig.use_gain_model`（:122）与 `lib/snr_estimator/include/astrocs/noise/types.h:98`（`NOISE_CFG_KEY_USE_GAIN_MODEL "use_gain_model"`）、`lib/snr_estimator/src/module_entry.cpp:467`（确实从 config 填值）；对照 `noise_model.cpp`（全文件对该字段 **0 次读取**）
- 证据摘录（复核时点重读）:
  >         double sigma_residual = 0.0;（star_matcher.cpp:552）
  >     if (S > 0.0) { …IRLS… }（:488 —— S==0 时整段跳过，sigma_residual 保持 0）
  >     sigma_residual = mad_in > 0 ? mad_in / _MAD_SCALE : 0.0;（:558）
  >     if (!std::isfinite(sigma_logflux_dex) || sigma_logflux_dex <= 0.0) { out->fit_status = 2;（noise_model.cpp:281-282）
  > （ALG-PHOT:36/:45）sigma=0 → 完美 / sigma=0 → 退化（两义并用）
  > `use_gain_model`：配置字段存在但 `NoiseModel::estimate` 未读取（生产主路径 empirical）；… **不实现假融合逻辑**（ALG-NOISE DISP-NOISE-003:175）
- 权威依据: 宪章 §4.1（validity 必须独立于 value 可表达）、§14.4（暴露而无效的配置字段属静默降级）、§11；SCI-PHOT-001 §5/§8；SCI-NOISE-001 §4/§9
- 问题说明: ① `sigma_residual=0` 同时表示「完美一致（`mad_in=0`）」「IRLS 未启动（`S=0`）」「一致集为空（pc_api 退化路径）」三种状态，QA 面无法区分，而 `snr_phot=1/(ln10·sigma_residual)` 会把它变成 +Inf；② `snr_phot_cal_quality` 用 `fit_status=2` 表示「sigma 非正/非有限」，但 `SnrControlPointV3.fit_status` 的 2 又表示「n_eff<3」（头 :327 注释），同码不同义，公共头未登记 `2` 的含义；③ `use_gain_model` 作为公共配置字段被解析、被 descriptor 声明、被 module_entry 填值，但生产实现从不读取（DISP-NOISE-003 已诚实登记「不实现假融合」）——登记不消除「字段对用户有效」的事实：配置里 `use_gain_model=true` 不产生任何效果。
- 影响: 帧级测光 QA 的可解释性（同一数值三义）；用户以为已启用/关闭增益融合而实际未变。
- 建议处置: 给 `sigma_residual` 增加来源枚举（或把 `fit_status` 接入 `PhotometricDiag`），在 ALG/头文件登记每个码值唯一含义；`use_gain_model` 删除或显式报错「字段未实现」（禁止静默接受）。
- 置信度: 高
- related: M3-C-001、M3-D-003、M3-A-001、DISP-NOISE-003
