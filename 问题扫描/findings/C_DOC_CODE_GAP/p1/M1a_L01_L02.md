# M1a 定稿 · C_DOC_CODE_GAP · P1（来源切片 L01+L02）

## M1a-C-005 ALG §3 施工伪代码与生产链步骤错位：容差挂错步骤、生产 build_sip 不含 IRLS/Huber 却按 legacy 路径描述且未注明

- 类别: C_DOC_CODE_GAP · 优先级: P1 · 来源: L01-009
- 位置: docs/algorithms/PLATESOLVE.md::§3 流程伪代码 与::§11.1 DISP-WCS-003；lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp::extract_wcs_sip；lib/plate_solve/cpp/ipv/src/ipv_sip.cpp::irls_huber_fit（legacy）
- 证据摘录（逐字）:
  > （PLATESOLVE.md:29-33）matches = kd_match(gaia, detected, tol=5.0") / trans = iter_trans_solve(...) / sip = build_sip(trans) order 2-3, **IRLS 15× ε1e-6 Huber 1.345 (ipv_sip.cpp:238-262)**
  > （PLATESOLVE.md:166-170）DISP-WCS-003 双 SIP 拟合路径并存：**生产 extract_wcs_sip（TRANS 解析 A/B + 7×7 网格反变换 AP/BP…）与 legacy fit_sip IRLS+Huber…仅被 legacy build_wcs 消费**；两实现行为漂移
  > （ipv_wcs.cpp:441-446）// c. UV = cd_inv · IWC (无畸变像素) … double uv_x = cd_inv_00 * wx + cd_inv_01 * wy;
  > （ipv_wcs.cpp:456-463）// 累加正规方程 (拟合 UV → (u, v), 归一化目标) … bx[p] += bv[p] * (u / R_u);
  > （ipv_sip.cpp:230-241）// δ = 1.345×MAD …（IRLS/Huber，legacy）
- 权威依据: 宪章 §12.3-3、§13.1；ALG-WCS-001 §3/§11.1
- 问题说明: §3 把生产链的 SIP 步骤写成 legacy 的 IRLS+Huber 迭代（引用 ipv_sip.cpp），而 §11.1 自己登记"生产走 extract_wcs_sip 的解析+正规方程最小二乘"（代码复核确认：无 IRLS/Huber 权重、单次高斯消元）→ 同一文档两处描述互斥，伪代码会误导施工者把鲁棒迭代接回生产路径。另 §3 的 `tol=5.0"` 挂在 kd_match 步，实测该阈值属 RANSAC 内点判据域（与 §9 容差族同源问题，另见 M1a-A-005）。
- 影响: 施工依据与生产实现不一致；§3 自称"公式零改动承接"使错述被固化为合同。
- 建议处置: ①§3 的 build_sip 步改述为"解析前向 A/B + 网格最小二乘反演 AP/BP（ipv_wcs.cpp::extract_wcs_sip），legacy IRLS 路径不入生产"；②各步阈值挂到 §9 容差表符号；③锚点改 path::symbol。
- 置信度: 高 · related: M1a-A-005、M1a-C-001、L01-013

## M1a-C-006 选星口径三处互斥（残余事实）：ALG/头注称"flux 降序、默认 20、自适应 20→40→60"，实现为 box-mag 升序且被固定 60 覆盖

- 类别: C_DOC_CODE_GAP · 优先级: P1（**部分修复后的残余**；叶子原述含"节点硬编码 gaia_n_target=60 绕链"半边，该半边随并发提交消失，见 _merge/M1a.md §二）· 来源: L01-012
- 位置: docs/algorithms/PLATESOLVE.md::§5 选星；lib/plate_solve/cpp/ipv/src/ipv_select.cpp::（文件头口径 + 排序实现）；lib/plate_solve/cpp/ipv/include/ipv_types.h::IPVSelectParams；lib/plate_solve/cpp/ipv/src/ipv_solver.cpp::（内部 n_target 与参数装配）
- 证据摘录（逐字）:
  > （PLATESOLVE.md §5:115）统一按 flux(A) 降序取前 img_n_target 颗…（默认 20，自适应 20→40→60）
  > （ipv_select.cpp:16）统一按 flux(A) 降序取前 img_n_target 颗
  > （ipv_select.cpp:462-470）// 按 mag(box积分) **升序**取前 N … 注: flux 参数（来自检测器）**当前未使用**
  > （ipv_types.h:206-208）img_n_target = 20 …（自适应扩充 20→40→60）
  > （ipv_solver.cpp:1159）const int n_target = 60;
  > （ipv_solver.cpp:438-439/:821-822/:1442）p_adapt.img_n_target = n_target;
- 权威依据: 宪章 §12.3-3（公式/规则唯一实现）、§13.1、§14.4；ALG-WCS-001 §5
- 问题说明: 三处口径互斥：排序键（flux 降序 vs box-mag 升序）、默认值（20）、实际生效值（60，且由 solver 内部常量覆盖外部传入的 img_n_target）。"自适应扩充"仅存在于注释。公共 C ABI 的 img_n_target 参数在文档面是可调项，实现面被常量覆盖（用户不可观测地失效）。节点侧的重复实现（p1_pick_stars/gaia_n_target）本轮复核已不存在（全仓 grep 0 命中）。
- 影响: 选星结果不可由合同复算；参数失效属静默降级（§6.3）；文档/代码/注释三源各自表述。
- 建议处置: ①单一权威（裁决排序键与 N 值），其余两源改为引用；②要么实现"自适应 20→40→60"，要么删除该宣称；③若保留常量覆盖，须在 ipv_api.h 参数表标注"当前实现固定 60"。
- 置信度: 高 · related: M1a-C-005、F00-06

## M1a-C-007 Phase3 重采样实现级合同与现行 p3_resample.{h,cpp} 全面脱节：非目标已被实现推翻、10 符号清单与签名/锚双重失真、行数陈述过期

- 类别: C_DOC_CODE_GAP · 优先级: P1 · 来源: L02-003
- 位置: docs/algorithms/PHASE3_RSMP_IMPL.md::§1 非目标、::§4 符号表、::§6.5 coverage 台账；lib/phase3_session/p3_resample.h::（文件整体）；lib/phase3_rsmp/（目录现状）
- 证据摘录（逐字）:
  > （PHASE3_RSMP_IMPL.md:13-14）p3_resample.h **58 行** / p3_resample.cpp … 239 行 …（实测 2026-09-12）
  > （PHASE3_RSMP_IMPL.md:24）非目标：**不实现 variance/weight/ivar/support 输入**
  > （p3_resample.h:23-27）/* 已废止的"不实现 variance/ivar"非目标（§30.4-4 supersession）… 现实现为 **四平面产品 (signal/coverage/variance/ivar)** */
  > （p3_resample.h:39-41 / :97-99 / :110-112）p3_sampler_open_ex(…, std::string* out_bunit, std::string* err) / p3_sample_bilinear_ex(…, double weights[4], uint64_t leaf_ipix[4]) / p3_uncertainty_propagate(…)
  > （PHASE3_RSMP_IMPL.md:75-89 符号表）p3_sample_nearest(const P3Sampler*, const P3WcsDescriptor*, int x, int y, …)（**签名与实现不符**）
  > （PHASE3_RSMP_IMPL.md:194-196）`*coverage` 恒 1（DISP-P3RSMP-001）
  > （实测目录）lib/phase3_rsmp/ 仅含 README.md / module.yaml / memory.md；头文件实位于 **lib/phase3_session/p3_resample.h（116 行）**
- 权威依据: 宪章 §12.3-3、§12.3-9（无断链）、§13.1、§17.1；ALG-P3-003/RSMP 实现合同
- 问题说明: 四项独立失真：①头文件行数陈述 58 → 实测 116；②合同文件所在模块目录与实现目录不同名（合同描述 lib/phase3_rsmp/，实现与头文件在 lib/phase3_session/）；③"非目标：不实现 variance/weight/ivar/support"已被实现整体推翻（四平面产品 + uncertainty 传播面），头文件自己登记 supersession 而合同 §1 未订正；④§4 符号表 10 项与现行 15 个公共符号不符，且`p3_sample_nearest` 等签名与实现参数完全不同（合同签名含 WcsDescriptor 与 int 像素参数，实现为 ra/dec 界外接口 + _ex 族）；⑤§6.5 仍以"coverage 恒 1"登记 DISP-P3RSMP-001，而实现已产出逐像素 0/1 覆盖（节点 :4856）→ 台账与代码互斥，且该失真经 README/module.yaml 向下游传导。
- 影响: 实现级合同对施工者不可信；追溯链（合同→符号→测试）断；台账"恒 1"若被上层当作真值使用将导致覆盖统计失效。
- 建议处置: ①按现行头文件逐符号重写 §4（签名 + path::symbol 锚）；②§1 非目标改为"已实现，见 §30.4-4 supersession"；③行数陈述改"实测"或删除（不复制行数）；④DISP-P3RSMP-001 结案或改述；⑤合同与实现的模块目录归属差异在 §2 明确登记。
- 置信度: 高 · related: M1a-E-002、M1a-C-008、L02-004

## M1a-C-008 HISTORY provenance 残余（部分修复定稿）：节点面 manifest 哈希已真实化，会话面仍恒 nullptr、hips_id 两面仍恒占位，合同仍以"已闭合"整体登记并引用不存在的「SCI-P3 §96」

- 类别: C_DOC_CODE_GAP · 优先级: P1 · 来源: L02-012（残余事实；**节点面半边已修复，见 _merge/M1a.md §二**）
- 位置: lib/phase3_session/p3_session.cpp::（provenance 装配）；lib/phase3_session/p3_output.cpp::HISTORY 写出；lib/core/src/module_adapters.cpp::p3_node（已修侧）；docs/algorithms/PHASE3_FITS_IMPL.md::§14；docs/modules/…/p3 侧 README
- 证据摘录（逐字）:
  > （p3_session.cpp:365-366）prov.hips_id = "ivo://astrocs/phase3"; prov.manifest_hash = nullptr;
  > （p3_output.cpp:224-227）snprintf(hist, sizeof(hist), "HISTORY phase3 source=%s manifest=%s", prov->hips_id, prov->manifest_hash ? prov->manifest_hash : "");
  > （module_adapters.cpp:5065-5081，已修侧）prov.hips_id = "ivo://astrocs/phase3";  … p3n_input_manifest_hash（run_context.json 缺失即 fail-closed）
  > （PHASE3_FITS_IMPL.md:344）- **整改项（B2-A9/A10 已闭合）**: (a) `prov.manifest_hash` 不再恒 nullptr——
  > （README 侧引用）… 见 SCI-P3 §96（**该文件共 143 行，无 §96**）
- 权威依据: 宪章 §4.3（产品须记录完整 provenance：输入哈希、实际 provider）、§12.3-9（无断链）、§12.3-4；SCI-P3-001 §9a-11
- 问题说明: 复核时点两条调用面行为不同：Runtime 节点面已写真实输入 manifest 哈希并对缺失 fail-closed；**会话面（p3_session）仍把 manifest_hash 置 nullptr**，p3_output 的 HISTORY 于是输出 `manifest=`（空值）——即同一合同"已闭合"的断言只对一半路径成立。hips_id 在两面均为常量占位（非实际 HiPS provider URI），"实际 provider"这一 §4.3 强制项未满足。文档面另有一处指向不存在的「SCI-P3 §96」的断链。
- 影响: 会话面产品缺输入溯源锚（发布门 §17.1 的 provenance 证据项不实）；provider 身份不可判别；合同"已闭合"整体登记掩盖残余。
- 建议处置: ①会话面同节点面接入 manifest 哈希（缺失即失败）；②hips_id 取实际 properties 的 HIPS_ORDER/识别 URI 或显式标注为占位；③合同按路径分别登记（节点已闭合 / 会话未闭合）并删除悬空 §96 引用；④补一条 HISTORY 面断言测试（见 M1a-F-005）。
- 置信度: 高 · related: M1a-G-001、M1a-E-002、L02-003
