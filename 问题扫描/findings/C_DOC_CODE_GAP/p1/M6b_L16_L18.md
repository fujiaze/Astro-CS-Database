# M6b · C_DOC_CODE_GAP 定稿 P1（来源切片：L16 / L18）

> 合并代理 M6b。三条同属「文档口径与树内实现互斥」，但根因不同，故分条：C-001=限制层把红线禁止的行为写成既有行为；C-002=已修复缺陷仍登记为现行缺陷；C-003=同一文档内新旧冻结集并存。
> 行号仅辅助定位（并发修复期，锚以 `path::符号` 为准）。

---

### M6b-C-001 限制层与排障层把宪章 §6.3 明令禁止的「support 当权重回退」写成既有行为，而实现默认是硬错误：三处文档口径互斥

- 类别码: C_DOC_CODE_GAP
- 优先级: **P1**
- 位置:
  - `ASTROCS_PROJECT_CONSTITUTION.md::§6.3 科学硬约束`（现 :191）
  - `docs/KNOWN_LIMITATIONS.md::第 9 条`（现 :13-14）
  - `docs/TROUBLESHOOTING.md::Symptom 表 stage2 两行`（现 :14、:16）
  - 实现侧对照：`lib/phase2/include/astro/phase2/stage2_common.h::legacy_allow_weight_fallback`（现 :94）、`lib/phase2/src/integrate.cpp::权重与 INVALID_INPUT 分支`（现 :54-56、:66-69）、`lib/phase2/memory.md::（回退口径两处）`（现 :132、:145）、`lib/phase2/src/sampler.cpp::ivar 缺失降级注释`（现 :580）
  - 合同侧对照：`docs/contracts/DATA_SEMANTICS.md::（禁静默回退 support/SNR 段）`（现 :1702）、`docs/modules/phase2_upm.md::（rc=2 显式 INVALID）`（现 :113）
- 证据摘录:
  > （宪章 §6.3:191）- `support/coverage` 不能作为 inverse-variance 或 SNR 权重。
  > （KNOWN_LIMITATIONS.md:13-14）9. **UPM ivar 回退**: 输入帧无 ivar 产品时积分权重回退 support（`ivar_product_missing` 计数如实记录）
  > （TROUBLESHOOTING.md:16）| 权重全等权 (模式 ivar 失效) | diag `ivar_product_missing>0` | 输入帧无 ivar 产品 (V19 前产物) | 重跑 Phase1 V19 链; **或接受 support 回退** |
  > （TROUBLESHOOTING.md:14）| stage2 `ZERO_VALID_WEIGHT` | INTEGRATE 日志 | 帧无 ivar 产品且 support=0 | …
  > （stage2_common.h:94）bool legacy_allow_weight_fallback = false;
  > （lib/phase2/memory.md:145）…（policy/reducer 分离）、ivar 产品缺失默认硬科学错误（显式 fallback 开关）
  > （lib/phase2/memory.md:132）缺产品 -> support 回退 + ivar_product_missing 计数
  > （DATA_SEMANTICS.md:1702）… h:126-133 "禁止静默回退 support/SNR"；权重语义红线 §23.4 镜像）。
- 权威依据: 宪章 §6.3:191（科学硬约束）、§1.1（权威顺序：限制/排障文档不得改变硬约束）、§12.3-10、§17.4（科学正确性门禁）。
- 问题说明: 三份 ACTIVE 文档（L0 侧的 KNOWN_LIMITATIONS 第 9 条、排障手册的处置建议、模块 memory 的 :132 行）把"缺 ivar → 用 support 当积分权重"表述为**既有可接受行为**，而实现里 `legacy_allow_weight_fallback` 默认 `false`、权重非法即 `P2_INTEGRATE_INVALID_INPUT`，同一份 memory.md 的 :145 行又写"默认硬科学错误"——同一事实在仓库内有两种相反口径。宪章 §6.3 只允许一种：不得作为权重。本条只定**文档口径互斥**这一面；"生产路径实际是否发生回退、回退后产品是否可发布"属实现/科学侧，由 M4 与 `L09-003` 定稿（协调记录指定两域并档、互挂 related）。
- 影响: 运维/审核者按 TROUBLESHOOTING 的"或接受 support 回退"处置，即按文档走上一条宪章禁止的路径；KNOWN_LIMITATIONS 作为 L0 清单会把这个未证实的行为当成已接受的限制，影响 §17.10 的 P0/P1 清零判断。
- 建议处置: ①KNOWN_LIMITATIONS 第 9 条改写为"缺 ivar → 显式 INVALID（`legacy_allow_weight_fallback=false`）；开启开关即违反 §6.3，需负责人裁决"；②TROUBLESHOOTING:16 删除"或接受 support 回退"分支，改为"重跑 Phase1 产出 ivar；否则任务失败"；③memory.md 两处口径统一，并把过程性描述移出（与 M6a 的 memory 治理条并档）。
- 置信度: 高（宪章逐字 + 代码默认值逐字）
- 来源: L18-002（前台判定高价值候选；本条只定文档侧）
- related: L09-003（M4，实现侧回退）、L07-004（M3，weight_mode=2 定义层）、M6b-G-002、M6b-C-003

---

### M6b-C-002 冻结 ALG 权威把已在代码修复并有回归门的缺陷登记为"现行缺陷语义"，逐符号锚表随之整体错位

- 类别码: C_DOC_CODE_GAP
- 优先级: **P1**
- 位置:
  - `docs/algorithms/PHASE2_INTEGRATION.md::§2 sup_max 行 / §3 门序表 / §11.3 现状缺陷清单`（现 :42、:64、:175-179、:234-236）
  - `docs/algorithms/PHASE2_INTEGRATION.md::§11.3 附注（锚漂移）`（现 :247）
  - 代码真源：`lib/phase2/src/integrate.cpp::canonical reducer 分支`（现 :44-50、:54-56）
  - 回归门：`tests/unit/p2_output_semantics_test.cpp::B2-A7 回归门`（现 :64、:85、:126）
  - 转述面：`docs/science/INTEGRATION.md::§5 canonical reducer 伪码与自述`（现 :58、:63）、`docs/algorithms/anchors/anchor_contract.json::exemptions[2..6]`（现 :107-145）
- 证据摘录:
  > （PHASE2_INTEGRATION.md:42）| `sup_max` | max(accepted support)（**现状=R3-A 缺陷语义**，§11.3） | 无量纲 | integrate.cpp:61-61 |
  > （PHASE2_INTEGRATION.md:234）### 11.3 现状缺陷清单（DISP-P2INT-001..002，登记不改码，整改归 P2-INT-IMPL/TEST）
  > （PHASE2_INTEGRATION.md:236）- **DISP-P2INT-001**（bughunt R3-A）: sup_max 在 `if (w==0) continue` …
  > （integrate.cpp:44-50）// B2-A7: canonical reducer 契约 = max(**accepted** support)，… 故 sup_max 必须在权重分支之前更新；旧实现置于 w==0 continue 之后会让零权 accepted 样本的 support 被静默丢弃… ／ if (in->support != nullptr) sup_max = std::max(sup_max, in->support[i]);
  > （tests/unit/p2_output_semantics_test.cpp:85）// 4b) B2-A7 回归门: canonical reducer = max(accepted support)，且零权…
  > （INTEGRATION.md:58）       support = (support 空) ? 1.0 : max_{valid,W>0} support[i]   # canonical reducer, 覆盖并集保守下界
  > （INTEGRATION.md:63）… `support` 为 `max(accepted support)` 且仅消费 `pr.support`，调用方不二次 max/mean。
  > （anchor_contract.json:110）"reason": "SCI §5:63 冻结声明原文（**实际 76 行**）…"   ← 实测 integrate.cpp 现 81 行，reason 内嵌的规模陈述已过期
- 权威依据: 宪章 §12.2（L1 算法权威须与实现一致）、§12.3-10、§13.1（缺陷须有性质/边界测试）、§1.1（历史账本≠现状）；`docs/standards/COMMENT_STANDARD.md`/`DOC-GOV` 的"现状与历史分离"要求。
- 问题说明: 代码侧 `B2-A7` 已把 reducer 移到资格门之后并配了回归门（`tests/unit/p2_output_semantics_test.cpp:85`），但冻结 ALG 的 §2/§3/§11.3 仍以"现状=缺陷语义/现状缺陷清单/整改不改码"表述，且其逐符号锚（`integrate.cpp:61-61`、`:54-55`）随整改整体错位；SCI §5 的伪码 `max_{valid,W>0}` 与其下一行自述的 `max(accepted support)` 作用域也不等价（w=0 的 accepted 样本是否参与）。豁免登记把该 SCI 声明冻结为 `FROZEN_SCI_CLAIM` 并在 reason 里写"实际 76 行"，等于把过期规模陈述也焊进门里。
- 影响: 后续读者/实现者按 ALG §11.3 会以为仍存在"零权样本 support 被丢弃"的缺陷（并可能重复整改或反向改码）；SCI 伪码与语义行的不等价在 B2-A7 语境下会引出两种 reducer 结果；锚表错位使"文档↔源码"核对失效（与 M6b-E-002 同族）。
- 建议处置: ①§11.3 的 `DISP-P2INT-001` 关闭并记录整改 SHA + 回归门（`tests/unit/p2_output_semantics_test.cpp`），§2/§3 的"现状=缺陷"改"已整改（SHA…）"，逐符号锚按 `path::symbol` 重取；②`docs/science/INTEGRATION.md:58` 伪码改为与 :63 等价的 accepted 域写法（属 SCI 变更，按 §1.2 走负责人裁决）；③`anchor_contract.json` 三条 `FROZEN_SCI_CLAIM` 的 reason 去掉内嵌行数，或改由脚本生成。
- 置信度: 高（代码/测试/文档三方逐字对照）
- 来源: L16-005
- related: M6b-E-002、M6b-C-001、M6a（DISP 登记与注释治理侧）

---

### M6b-C-003 同一 ACTIVE 文档内冻结投影集新旧两版并存：§0 非目标仍写「不实现 SIN/ZEA/CAR/AIT」，§15 已按负责人裁决冻结 TAN/SIN/CAR/AIT

- 类别码: C_DOC_CODE_GAP
- 优先级: **P1**
- 位置:
  - `docs/algorithms/PHASE3_PROJ_IMPL.md::§0 非目标 / §11 注 / §15 冻结四投影表`（现 :20、:35、:107、:190、:287、:348-:414）
  - `ASTROCS_PROJECT_CONSTITUTION.md::§18.1 负责人裁决`、`::§7.2/§7.3`
  - 状态面：`docs/owner/RELEASE_STATUS.md::§4 Phase3 四投影行`（现 :94）、`docs/modules/registry/astrocs.phase3.wcs.md::（TAN-only 现状行）`（现 :151 一带）、`README.md::当前状态`（现 :33）、`memory.md::§5-4`（现 :94-100）
  - 实现面：`lib/phase3_session/p3_wcs.cpp::投影白名单校验`（现 :58-64、:110、:207-209）、`lib/phase3_proj/p3_projection.cpp::registry`（现 :267-273）、`lib/phase3_proj/module.yaml::entrypoint`（现 :79-80）
- 证据摘录:
  > （PHASE3_PROJ_IMPL.md:20）- 非目标: 不实现 SIN/ZEA/CAR/AIT（矩阵 notes 列为显式扩展清单，当前…
  > （PHASE3_PROJ_IMPL.md:348）## 15 P3-001 增补：版本化 projection registry 与冻结四投影（TAN/SIN/CAR/AIT）
  > （PHASE3_PROJ_IMPL.md:107）projection 仅接受 "TAN"（缺省即 TAN）；SIN/CAR/AIT 即便在…
  > （README.md:33）… Phase3 SIN/ZEA/CAR/AIT、`healpix_interp4`、流式 FITS 接入 未实现（NOT_VERIFIED）
  > （memory.md:94-98）负责人裁决 §18.1 冻结四投影为 **TAN+SIN+CAR+AIT**（旧 `SIN/ZEA/CAR/AIT` 表述有误）——registry v1 已在 `lib/phase3_proj/p3_projection.cpp`（:267-273）IMPLEMENTED…**生产会话/DLL 挂载未切换**（`lib/phase3_proj/module.yaml` `entrypoint: MISSING`），故未 INSTALLED
  > （owner/RELEASE_STATUS.md:94）… registry 实现已 IMPLEMENTED，但 … `entrypoint: MISSING` …（引 `p3_wcs.cpp:36`，实测该行为坐标推导注释）
  - 实测：`p3_projection.cpp` 的 registry（宪章 §18.1 要求的 TAN/SIN/CAR/AIT 四投影 + 独立往返 Oracle）已在位并有 ctest 名（`p3_projection_units`/`p3_projection_fault`，memory.md:96-97 记录），但 `module.yaml:79-80` 仍 `entrypoint: MISSING` → "已实现未接入"。
- 权威依据: 宪章 §18.1（负责人裁决优先于历史表述）、§7.2/§7.3（Phase3 投影与首批域）、§12.3-10、§13.2（验证层级）；`docs/owner/RELEASE_STATUS.md::§0`（现 :82-84「已实现未接入…不得声称 IMPLEMENTED/可用」——同页 :94 却给 IMPLEMENTED）。
- 问题说明: 两个方向的失真叠加：①裁决前的旧集合（含 ZEA）仍写在 ALG §0 与 README 状态段，与同文 §15/宪章 §18.1 直接冲突；②L0 与 ALG 对"registry 已实现但生产未接入"的给词不一致（owner:94 用 IMPLEMENTED，同文件 §0 判据又禁止未接入即 IMPLEMENTED），矩阵对应行则整行 VERIFIED（M6b-G-001 实例）。按锚看代码时又因 :94 的行锚漂移而落不到证据行。
- 影响: 投影集合决定 FITS-WCS 的 CType 与有效域，属接口级口径；新旧两版并存使"当前允许哪些投影"不可判定，并可能被后续实现按旧集合（含 ZEA）继续开发；L0 的 IMPLEMENTED 用词与自家判据冲突，直接进入审核包。
- 建议处置: ①一次性订正 `PHASE3_PROJ_IMPL.md:20/:190` 与 `README.md:33` 为 TAN/SIN/CAR/AIT，并标注裁决来源 §18.1；②owner:94 的完成度词按 §0 阶梯改 `IMPLEMENTED_NOT_WIRED`（或 NOT_VERIFIED），并把锚改 `path::symbol`；③矩阵 phase3-wcs/resample2 行的 SRC/TEST VERIFIED 按 M6b-G-001⑤ 降级；④在 §17.10 口径下把该缺口作为 P1 登记（当前 README 已如此登记，需保持三处一致）。
- 置信度: 高
- 来源: L18-011、L18-018（Phase3 状态互斥部分）、L18-008（:94 锚漂移部分）
- related: M6b-G-001、M6b-G-002、M6b-E-002、M2（若其定稿投影实现/数值侧，本条只保留文档面）
