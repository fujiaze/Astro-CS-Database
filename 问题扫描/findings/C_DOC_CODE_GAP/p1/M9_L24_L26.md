# M9 定稿 · C_DOC_CODE_GAP · P1（来源切片 L24 + L26）

> 合并代理: M9 · 复核时点: 全部证据按**当前树**重新 read/grep 定位；位置一律 path::符号 +「复核时 N」。结构化文件比对一律用「按列解析 + 计数」口径，不用 read 的行文本作等值判据。

## M9-C-1 「路径均为 UTF-8」合同下 Windows 侧无 UTF-8→UTF-16 适配层，发布原语与 IO-001/IO-002 直接用 *A / fopen（ANSI 代码页）

- 类别: C_DOC_CODE_GAP（主）；次挂 G_GOV_GATE（§8.3 唯一 I/O 边界）
- 优先级: P1（含条件升级与升档待裁决项，见下）
- 来源: L24-012（复验成立；锚点**保留并补全**——叶子引的 :364-367 MoveFileExA 由前台记为「窗口内未命中」，本代理按当前树定位为 **aio_publish.cpp:365**，逐字命中，故该锚不需作废）
- 位置: lib/hips/src/aio_publish.cpp（平台宏与枚举/提升原语）、合同 lib/hips/include/astrocs/hips/publish.h::原语 v1 段、runtime/io/fits_core.c::fio_file_open、::acs_fio_writer_begin_v1（探测）、runtime/io/hips_core.c（三处 fopen）、lib/gaia_xpsd_client/src/gaia_client.c::load_xpsd_file（Win 分支）；正对照 lib/astro_image_io/src/aio_util.h::aio_fopen_utf8、lib/astro_image_io/src/ahpx/aio_ahpx_reader.cpp::ahpx_fopen_utf8
- 证据摘录（逐字，复核时点现文）:
  > 合同（publish.h:73）`/* ───────── 原语 v1 (4 函数; 路径均为 UTF-8, 与 IO-001 同语义) ───────── */`；参数名 out_dir_utf8 / stage_path_utf8。
  > Win 宏（aio_publish.cpp:33）`#define PUBLISH_MKDIR(p) _mkdir(p)`、（:34）`#define PUBLISH_FSYNC_FD(fd) _commit(fd)`、（:37）PUBLISH_UNLINK 用 _unlink；（:128）`HANDLE h = FindFirstFileA(pat, &fd);`（:169 同型）、（:365）`if (!MoveFileExA(stage_path_utf8, out_dir_utf8, MOVEFILE_REPLACE_EXISTING))`。
  > 文件头自述（:8-9）`平台: Linux/macOS 全功能 (fsync/O_DIRECTORY/dirfd); Windows 编译保持 (_commit 落盘 / FindFirstFile 递归 / MoveFileEx 原子替换; …)` —— 未提任何 UTF-8→UTF-16 处理。
  > IO-001/IO-002 侧同样无宽字符分支：runtime/io/fits_core.c:137 `f->fp = fopen(path, mode);`、:1216 `FILE* probe = fopen(path_utf8, "rb");`；runtime/io/hips_core.c:122/:168/:415 三处 `fopen(path, …)`；计数口径：两文件对 `_wfopen|MultiByteToWideChar` 的命中数均为 **0**。
  > 第三处同族：lib/gaia_xpsd_client/src/gaia_client.c:1014 `xf->hFile = CreateFileA(path, …)`（Win 分支直接用 A 系 API）。
  > 同仓正对照（唯一实现了该适配层的模块）：lib/astro_image_io/src/aio_util.h:10-23 `aio_fopen_utf8` → `MultiByteToWideChar(CP_UTF8, …)` + `_wfopen`，POSIX 回退在 :23；ahpx 侧 `ahpx_fopen_utf8`（aio_ahpx_reader.cpp:174 邻域）。
  > 溢出即拒的同文件正对照（证明纪律可照抄）：aio_publish.cpp:102 `if (n == 0 || n >= sizeof(tmp)) return AIO_PUBLISH_ERR_PARAM;`、:299 同型。
- 权威依据: 宪章 §8.3（全仓唯一 I/O 边界；边界行为不得分叉）、§3.1（Windows amd64 为正式交付入口）、§15.3（Fatduck 为 Windows 复验节点）、§12.3（文档与实现一致；「上层适配层」若无实体即悬空引用）、§11（失败须显式）。
- 问题说明: IO_001 把 UTF-16 转换显式指派给「上层适配层」，但 runtime/io（IO-001/IO-002 新基线边界）与 lib/hips（AIO-002 原语）都没有实现它——**没人兑现该合同**。①UCRT 的 fopen/_mkdir/_unlink 与 *A 系 API 按进程 ANSI 代码页解释字节，UTF-8 中文/带重音目录变成乱码路径 → 建目录失败或写到非预期目录；而同产品的 astro_image_io 老边界能正常处理，形成「同一次运行里一个 I/O 层能写、另一个不能写」的行为分叉。②更硬：promote 用 MoveFileExA 重命名整个 staging 目录 → 中文目录下 HiPS 发布在**事务最后一步**失败，此时 stage 树已 fsync 但未 promote，调用方走 discard 分支 → 整次发布被丢弃，而原因只剩一个 errno（GetLastError 被丢弃）。③参数名后缀 `_utf8` 与实际按 ANSI 解释，构成"名字即合同"的误导。
- 影响: 交付面是 Windows 正式产品（§3.1），非 ASCII 用户目录是常态。runtime/io 侧当前无生产调用点（实测 acs_fio_writer_* 调用者仅 tests/io 与 selftest，见 M9-G-4）；但 **lib/hips 的 AIO-002 原语已被 astrocs.p1.hips 模块事务面接线**（stage_create / tree_fsync / promote）→ 生产可达。Linux 完全无此问题，CI 主跑 Linux → **该缺陷对机器门不可见，只能靠 Fatduck 人工复验发现**。
- 建议处置: ①按 §8.3 把 UTF-8→UTF-16 收口为**一个**跨层 helper（把 aio_util.h::aio_fopen_utf8 提为公共基建，或在 runtime/io 内建等价物），IO-001/IO-002/AIO-002 三边界统一使用；②aio_publish 的 Windows 分支改 CreateDirectoryW/MoveFileExW/FindFirstFileW/_wunlink/_wrmdir，失败处把 GetLastError+FormatMessage 写进诊断（不要只留 errno）；③IO_001/002/003 文档把「上层适配层」的具体归属写实（哪个 target、哪个函数），否则该句属悬空引用；④Windows profile 增一条含中文与非 ASCII 输出目录的 HiPS 发布端到端用例；⑤gaia_client 的 CreateFileA 一并纳入同一 helper（勿只改两处）。
- 置信度: 高（代码 + 命中计数实证）；影响面 中（禁执行下未实测 Fatduck 行为，已如实分栏）
- 条件升级: **runtime/io 半边**待接线（判据同 M9-G-4）；**lib/hips/AIO-002 半边已接线且生产可达** → 严格按「接线即升级」该半边应升 P0；本代理不擅自升档，标为**升档待负责人裁决**（裁决输入=是否接受「Windows + 非 ASCII 目录」为发布阻断场景）。
- 平台盲区: **是**（Linux 侧不可能出现；需 Fatduck 实测）
- related: M9-G-4 / M9-G-5（同一 IO-001 边界的另两事实，三条应同批处置）、M9-H-6（第三条 Windows-only 站点）、M9 §4 平台盲区清单、M9 §6 待接线升级清单、M9 §12 需执行权限清单
- **【E4-实测 2026-09】** 三态确认：合同有（`docs/interfaces/io/IO_001_FITS_STREAM_INTERFACE.md:45` 原文「文本公共格式 UTF-8；Windows 内部路径 UTF-16 属上层适配层，不进入本头。」+ `publish.h:73`「路径均为 UTF-8, 与 IO-001 同语义」）成立；Linux 无此问题成立（`aio_util.h` 的 `#else` 分支即裸 `std::fopen`）；第三态精确化为「**出向**实现者只存在于 `lib/astro_image_io/src/` 私有目录」——`aio_fopen_utf8` 消费者 `aio_fits.cpp:514,788,945,1090`、`aio_xisf.cpp:440,654`、`hiss_stream_writer.cpp:265,527,572`、`hiss_reader.cpp:469`（`grep -rn include.*aio_util.h` → 5 命中，全在该 src 目录 + 1 个 drizzle 探针），另有 8 处平行私有转换器（`aio_pipeline.cpp:81-89` 自带 `_wfopen`、`hiss_stream_writer.cpp:158-168 widen()`、`healpix_drizzle/fits_reader.cpp:25-31`、`ahpx/aio_ahpx_reader.cpp`+`ahpx_writer.cpp`、`healpix/aio_healpix_io.cpp:80`、`acr/backends/cuda/cuda_bridge_loader.cpp:48`、`healpix_db/archive/legacy/healpix_stack/` 三处）；该头不在任何 PUBLIC include 面 ⇒ 跨层只能像 `tests/unit/CMakeLists.txt:751,:764` 那样把 `.../src` 塞进 include 路径，即建议处置①「提为公共基建」确有一步真实成本（可静态定，不受实测阻）。`runtime/io/**` 与 `lib/hips/**` 对 `MultiByteToWideChar` 命中 **0**（写出边界确无实现者）。新登记限定：仓内另有**方向相反**的适配器 `cli/main.cpp:52 int wmain` + `cli/cli_common.h:44-53 utf8_from_wide_*` + `tests/unit/cli_wideconv_test.cpp`（ctest `cli_wideconv`，`tests/unit/CMakeLists.txt:112-117`）⇒ 入向 UTF-16→UTF-8 有实现者与用例，出向 UTF-8→UTF-16 缺。A 系按 ACP 误解释 UTF-8 已由既有 Windows 运行实证过一次（commit `da0cf578` 正文「/utf-8 已全局存在(CMakeLists:45), 真因是运行期窄字符 CreateFileA 按 ACP 解释 UTF-8 路径字节」；现文 `tests/unit/core_pipeline_test.cpp:25-40`）。接线复核：`grep -rln acs_fio_writer lib cli` → 0 ⇒ runtime/io 半边判据**未触发**（P1 维持）；`aio_publish_*` 仍接在 `lib/hips/src/module_entry.cpp:862,896,988,993,1000,1003` ⇒ AIO-002 半边「已接线、升档待负责人裁决」仍准确。补充限定：`runtime/io/hips_core.c` 在现树不属任何构建目标（`grep -rn hips_core --include=CMakeLists.txt --include=*.cmake .` 命中 0；`build/compile_commands.json` 610 TU 命中 0；仅 `IO_002_HIPS_INPUT_INTERFACE.md:44` 声称其在 DLL 内）⇒ 本条引用的 :122/:168/:415 三处 fopen 现属**不可达站点**（属 B-10 数据点，本条不因此降级）。

## M9-C-2 公开 C-ABI aio_wcs_pixel_scale 返回角秒/像素而单位零声明；同头兄弟 aio_wcs_rotation_deg 以名带单位；API_CONTRACTS 的 units 列对全表 102 行是同一句样板串

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L26-003（复验成立；**改写一处事实**：叶子称「CSV 无单位字段」不确——该表**有** units 列，缺陷形态更严重：该列对全部数据行同值，等价于未声明）
- 位置: lib/astro_image_io/include/astro_image_io.h（两兄弟声明）、lib/astro_image_io/src/aio_api.cpp::aio_wcs_pixel_scale、docs/contracts/API_CONTRACTS.csv（API-aio_wcs_pixel_scale 行 :124）、docs/contracts/PUBLIC_API.md（无 aio_wcs 条目=缺席确认）
- 证据摘录（逐字，复核时点现文）:
  > （astro_image_io.h:125）`AIO_EXPORT double aio_wcs_pixel_scale(const AIOWCSKeywords *wcs);` 紧邻（:126）`AIO_EXPORT double aio_wcs_rotation_deg(const AIOWCSKeywords *wcs);` —— 前者名内无单位、头内无注释，后者名内带 deg。
  > （aio_api.cpp:381-391）`if (!wcs) return 0.0;` / `double det = wcs->cd1_1 * wcs->cd2_2 - wcs->cd1_2 * wcs->cd2_1;` / `if (std::abs(det) < 1e-30) return 0.0;` / `return std::sqrt(std::abs(det)) * 3600.0;` —— 返回 **角秒/像素**；0.0 同时充当 NULL 入参、退化行列式与异常三个不同语义的哨兵。
  > （aio_api.cpp:393-399 兄弟实现）`return std::atan2(wcs->cd2_1, wcs->cd1_1) * 180.0 / 3.14159265358979323846;` —— 同名族、不同单位族。
  > 列解析口径（非行文本比较）：API_CONTRACTS.csv 表头第 6 列即 `units`；对文件按 CSV 解析后计数 → 数据行 **102** 行，units 去重值 **1** 个，即 `ADU/pixel/deg per header`；:124（pixel_scale）与 :125（rotation_deg）两行的 units 与 valid_range 单元格逐字相同（`per SCI valid_range`）。
  > PUBLIC_API.md：对 `aio_wcs` 的命中数 0（缺席确认）。
- 权威依据: 宪章 §4.1（DATA/公共面必须写明单位、有效域）、§12.2（注释须解释单位）、§12.3（登记与实现一致；登记列不得以样板串充数）。
- 问题说明: 接口名不带单位后缀、头无注释、契约 units 列为全表同值的样板串、PUBLIC_API.md 不收容——四道声明面同时失守；0.0 哨兵与「真 0 尺度」歧义混用。当前无生产消费者（仅清单/CSV 行），属公共面欠账而非现产错误；但种子 L15-002（hips_pixel_scale 以角秒写入 IVOA 度制键）已证明 deg/arcsec 歧义在本仓真实致错。
- 影响: 外部集成方按 deg/px 直觉消费即 3600× 错；同头文件 deg-in-name / arcsec-not-in-name 双风格并存诱导误读。units 列的样板串使「有契约登记」的机器门与人工审查双双获得虚假安心。
- 建议处置: ①改名 `aio_wcs_pixel_scale_arcsec`（与兄弟一致），或在头/实现/契约三处同时注明「arcsec/px」；②0.0 哨兵语义文档化并改为「返回码 + 出参」两态（同 M9-H-4 的判负口径建议）；③PUBLIC_API.md 补 aio_wcs_* 条目；④**把 API_CONTRACTS.csv 的 units/valid_range 样板串改为按符号实填，并加机器门断言「units 列不得为全表同值」**（该断言成本极低、判别力即时可见）。
- 置信度: 高
- related: L15-002（种子）、M9-D-2（同族：公共面单位靠命名不靠合同，L26-010）、M9-H-4（同族：入参域未声明且哨兵复用）、M9 §3 正对照表、M7/M5（契约治理面建议）

## M9-C-3 ipv_polygon 视场参数以角秒命名声明、生产选择器已全面改度口径字段；两函数无生产调用方，宽视场路径为 3600 族复发点（含只存在于报告的公式）

- 类别: C_DOC_CODE_GAP
- 优先级: P1
- 来源: L26-012（复验成立；**两处锚点重锚**：①叶子写 `lib/plate_solve/cpp/ipv/ipv_polygon.h` → 实为 `lib/plate_solve/cpp/ipv/include/ipv_polygon.h`；②叶子写 `lib/plate_solve/cpp/ipv/test_synthetic.cpp` → 实为 `lib/plate_solve/cpp/ipv/test/test_synthetic.cpp`）
- 位置: lib/plate_solve/cpp/ipv/include/ipv_polygon.h::build_hex_descriptor/::geometric_vote 族（fov_diag 三签名）、lib/plate_solve/cpp/ipv/src/ipv_polygon.cpp（r_local/is_wide_fov/日志换算）、lib/plate_solve/cpp/ipv/include/ipv_types.h::fov_diag_deg、lib/plate_solve/cpp/ipv/src/ipv_select.cpp::estimate 段、lib/plate_solve/cpp/ipv/test/test_synthetic.cpp（唯一调用者）、lib/plate_solve/cpp/ipv/REPORT.md（sigma_d_adaptive 公式）
- 证据摘录（逐字，复核时点现文）:
  > （include/ipv_polygon.h:42）`double fov_diag = 0.0,            // FOV 对角线 (角秒, 0=禁用形状剪枝)`；（:98 与 :114）同注两次 → 三签名族同口径声明为角秒。
  > （src/ipv_polygon.cpp:475）`double r_local = 0.15 * fov_diag;`；（:633）`bool is_wide_fov = (fov_diag > 3.0 * 3600.0);  // >3°`；（:658）`bool  use_window = (N_U_eff > 100) || is_wide_fov;`；（:478/:480）日志 `"sigma_d=%.3f, r_local=%.1\" (%.2f deg)"` 配 `r_local / 3600.0` —— 该 TU 内三处换算彼此自洽，均按**角秒**入参。
  > 生产主链已改度口径：（include/ipv_types.h:91）`double fov_diag_deg;     // FOV 对角线(度)`；（src/ipv_select.cpp:257-258）`double fov_diag_asec = diag_pix * s0;` / `fov_diag_deg = fov_diag_asec / 3600.0;` →（:296）向下游传 fov_diag_deg。
  > 唯一调用者：（test/test_synthetic.cpp:235-236）`double fov_diag = 4000.0 * std::sqrt(2.0);` / `geometric_vote(U, W, kv, params, fov_diag, sigma_d, poly_result.votes);` —— 角秒口径。
  > 报告公式无实现：（REPORT.md:335）`sigma_d_adaptive = max(2.0, 0.0015 × fov_diag_arcsec)`；计数口径：对 `sigma_d_adaptive|0\.0015` 在 `*.{c,cpp,h,hpp}` 全仓 grep → 真源命中仅 1 处且属他模块（healpix_browser_qt/widgets/sphere_view.h:106），ipv 侧 0 命中。
  > 接线现状：`geometric_vote`/`polygon_match_adaptive` 在生产树内除 test_synthetic.cpp 外无调用者（命中数口径）。
- 权威依据: 宪章 §4.1（同名量必须同单位或改名）、§12.2（注释须正确解释单位）、§12.3（报告/文档中的公式若声称算法须在代码可定位，否则属失实）。
- 问题说明: 同名参数 fov_diag 在 polygon/vote 模块按角秒自洽（阈值 3·3600、日志 /3600、r_local 系数），而选择器/求解器主链已全面改为「度」口径字段（fov_diag_deg）；若按头文件语境把度值传进 geometric_vote，则 is_wide_fov 永假（3° 被当 3 角秒与 10800″ 比较）、r_local 缩 3600× 致局部检索全空 → 投票全零而接口无错误。REPORT.md 的 sigma_d_adaptive 公式在代码中不存在。
- 影响: 当前无生产调用方，故非现产错误；但该模块名带 ADAPTIVE/polygon 且被报告当算法陈述，**一旦接线即复发型 3600× 族**（与 L15-002、M9-C-2 同族）。审计与教学成本高：报告里有一条不存在的公式。
- 建议处置: ①统一命名与口径：参数改名 fov_diag_arcsec（并把 :633 阈值与 :480 日志读同一单位），或全面改 *_deg 并在入口显式换算；②负责人裁决 polygon_match_adaptive / geometric_vote 去留——接线则补「度/角秒双口径」负例回归，删除则同时清 REPORT.md:335/:379 的公式与相关叙述；③若保留，把 wide-FOV 阈值提为带单位后缀的登记常量（禁裸 3.0*3600.0 字面量）。
- 置信度: 高
- 条件升级: 无（当前无调用点即 P1；若按 ①②接线则按实际致错面上调）
- related: L01-016（取代登记域）、L13 §0（明示未覆盖 ipv_polygon，本条为该缺口补报）、L15-002 / M9-C-2（3600 族同型）、M2a §D（跨域移交惯例）
