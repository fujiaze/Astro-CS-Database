# M2b 定稿 — C_DOC_CODE_GAP（文档与实现不一致）P1

> 合并代理：M2b。来源切片 L03（19）+ L15（22）。证据锚一律「path::符号」。

---

### M2b-C-02 lib/hips README 与 module.yaml / 构建 / 打包面互相否定（DLL 目标是否存在、迁移是否开始、C ABI 是否已建立）
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 位置: lib/hips/README.md::§1 身份表 + ::§0 前言状态块；lib/hips/module.yaml::模块定位段 + entrypoint 字段；lib/hips/CMakeLists.txt::add_library(astrocs_p1_hips_writer SHARED)；根 CMakeLists.txt::add_subdirectory(lib/hips) 与 install 段；packaging/astrocs.product.json::required_units；docs/contracts/PUBLIC_API.md::API-HIPS-001 尾段；docs/modules/registry/astrocs.phase1.hips-writer.md
- 证据摘录:
  - README 前言（原文）:「> 状态: CONTRACT_READY（P1-HIPS-DOC 冻结，2026-09-07）｜doc revision: r1」+「以现行唯一生产实现 lib/astro_image_io/src/hips/aio_hips_writer.cpp（合同头 lib/astro_image_io/include/aio_hips.h；根 CMakeLists.txt:298-309 编入静态库 astrocs_hips…）为准；lib/hips/ 是 P1-HIPS 迁移目标目录（astrocs_p1_hips_writer.dll 落码由 P1-HIPS-IMPL 建立，**当前本目录仅合同文件、无源码**…）」
  - README §1（原文）:「MOD ID / DLL target | … 现状实现编入 CMake 静态库 astrocs_hips（CMakeLists.txt:298-309，**无独立 DLL 产物，全仓库无 astrocs_p1_hips_writer 目标**）；迁移目标 astrocs_p1_hips_writer.dll（P1-HIPS-IMPL 建立，**尚未存在**）」、「文档状态 | CONTRACT_READY（实现存在于 lib/astro_image_io/src/hips，**模块化迁移未开始**；不声明 IMPLEMENTED）」
  - README §2（原文）:「负责：IVOa HiPS 1.4 产品集写入（aio_hips.h:6-9 标准：…NorderK/DirD/NpixN.fits 万进制分片…）」——同一 README 把错误的万进制算式当作标准转述（见 M2b-B-01）
  - module.yaml（原文）:「模块化迁移已交付: 本目录是迁移目标面（module.yaml + astrocs/hips/*.h 合同 + C ABI adapter）, 产出 astrocs_p1_hips_writer.dll」+「entrypoint: astrocs_module_query_v1」（并在同文件另处标 descriptor 未注册）
  - 构建与打包实况（原文）:「add_library(astrocs_p1_hips_writer SHARED)」；根 CMakeLists「add_subdirectory(lib/hips)」+「## Required install units (packaging/astrocs.product.json required_units)」；packaging/astrocs.product.json「"modules/astrocs_p1_hips_writer.so"」；packaging/install-tree.contract.json 同步列该 .so
  - 合同面停在旧态（原文，PUBLIC_API.md API-HIPS-001）:「astrocs_p1_hips_writer / astrocs_p1_hips_writer.dll，**entrypoint=MISSING——registry 无 descriptor**；C ABI adapter 由 P1-HIPS-IMPL 建立；本节描述现状 API，不声明 DLL 化完成」
- 权威依据: 宪章 §12.3-1/-5/-6（manifest ↔ 构建目标 ↔ 注册表 ↔ ABI 一致）、§12.3-10（活动文档不得以历史状态冒充当前）、§12.2
- 问题说明: 同一模块的四处现行自述互斥——README 说「无 DLL 目标 / 本目录无源码 / 迁移未开始」；module.yaml 说「已交付 C ABI adapter / 产出 DLL」；构建与打包事实是 DLL 目标存在、被根 CMake 构建、被 install 规则安装、被 packaging 列为 required unit；PUBLIC_API 又说 entrypoint=MISSING、registry 无 descriptor。四句中最多两句为真。此外 README 引用的「根 CMakeLists.txt:298-309 编入静态库 astrocs_hips」行位已漂移（现 astrocs_hips STATIC 在 :351 附近），属 M2b-E-01 的又一实例。
- 影响: 读者无法判定 HiPS 写出的实际交付物与 ABI 面（决定接线、打包、审计三个动作的前提）；「AIO 唯一写出路径」的边界在此模糊，直接支撑 M2b-G-01 的归属争议。
- 建议处置: 以 module.yaml + 当前构建事实为唯一源，一次性订正 README（含删去「万进制分片 = 标准」的错误转述）、registry 页与 PUBLIC_API 的状态字面量与行锚；若 entrypoint 确已存在则改 PUBLIC_API 的 MISSING，若未存在则改 module.yaml 的「已交付」。
- 置信度: 高
- related: M2b-G-01、M2b-E-01、M2b-B-01、L03-008、L12（p1_session legacy 通道并行）
- 来源: L03-008

---

### M2b-C-03 lib/hips 构建注释仍宣称「aio_hips_reader 全部不进本 DLL（grep 0 命中）」，与被 RESCUE-V3 当场推翻的同文件更正段并存（部分修复：链接闭包已修，注释残留）
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 位置: lib/hips/CMakeLists.txt::头注释「零依赖剔除」段；同文件 ::[RESCUE-V3 FD-01] 更正注释与 target_sources 列表；lib/astro_image_io/src/hips/aio_hips_writer.cpp::aio_hips_verify_product（跨 TU 依赖来源）
- 证据摘录:
  - 残留的错误注释（原文）:「# 零依赖剔除: lib/hips/src 下无 CFITSIO/c++/POSIX 调用, aio_hips_writer.cpp 无 aio::cfitsio_guard、无 CreateThread（grep 实证）；aio_fits/aio_log/aio_api/aio_healpix_io/hiss_*/**aio_hips_reader**/aio_upm **全部不进本 DLL**（grep 0 命中; g++ -fsyntax-only 实证通过）。」
  - 同文件下方的自我否定（原文）:「# [RESCUE-V3 FD-01 2026-09-08] **上述「writer 零依赖剔除 reader」注释失真**: aio_hips_writer.cpp aio_hips_verify_product 调用 aio_hips_tile_ipix（定义在 aio_hips_reader.cpp）→ 只编 writer 会 dlopen undefined symbol（hips_writer_adapter 基线红）。」+「target_sources(astrocs_p1_hips_writer PRIVATE "…/aio_hips_reader.cpp")」
  - 依赖实况（原文，writer 头）:「#include "aio_hips_reader.h"」；verify 面调用 reader 内定义的 aio_hips_tile_ipix / aio_hips_tile_count / aio_hips_open / aio_hips_read_tile_f32 等符号
- 权威依据: 宪章 §12.2（注释=实现）、§12.3-1/-6、§7.2（依赖闭包须可验证）
- 问题说明: 本次复核确认**构建闭包已被并发修复**（reader TU 已进本 DLL，见 _merge 已修复表），故本条只登记**未修部分**：错误注释原文照留，与下方更正段同页同文件并存；且原注释的实证方式（grep 0 命中 + -fsyntax-only）本身不足以证明链接闭包——单文件语法检查不检验跨 TU 符号，这是该类假证据可复现的原因（前台已把「判定式丢维度却判通过」列为可复用模式）。
- 影响: 后来者若按上半段注释做「精简源清单」重构，会再次产出携带未解析符号的 .so（Linux 允许，装载期才失败），且 hips_writer_adapter 基线红会再次出现；无构建源集合↔注释一致性的独立钉测。
- 建议处置: ① 直接改写上段为当前事实（reader TU 在内），删除失效的 grep/-fsyntax-only 实证宣称；② 若「writer 应零依赖 reader」是架构意图，登记为 DISP 而非写成已完成事实；③ 把 dlopen 冒烟（现有 hips_writer_adapter ctest）显式标注为该依赖的回归钉，并在更正注释里指回该 ctest 名。
- 置信度: 高
- related: M2b-C-02、M2b-F-02、L03-009（**部分修复**：见 _merge/M2b.md 已修复表）
- 来源: L03-009（残余部分）

---

### M2b-C-04 hips_version 口径四层并存（必须含 1.4 / 只要存在 / 完全不要求 / 夹具写 1.0），注册表基线又引用被自身取代的 PR 文档
- 类别: C_DOC_CODE_GAP
- 建议优先级: P1
- 位置: docs/standards/STANDARDS_REGISTRY.md::D.hips VERSION 字段 + D.hips 清单行 3；lib/astro_image_io/src/hips/aio_hips_writer.cpp::finalize_image_product；runtime/io/hips_core.c::hips_parse_properties；lib/astro_image_io/src/hips/aio_hips_reader.cpp::valid_product_params/properties 取值面；lib/phase3_session/hips_properties.cpp::hips_properties_parse_file；tests/unit/p3002_real_nodes_test.cpp::write_props（另两处夹具同值）；tools/orchestrator/run_evidence_orchestrator.ps1::HIPS_VERIFY；lib/photometric_calib/cpp/test/gate4_dr3sp_gaiaxpy/gate7_hips_validate.py
- 证据摘录:
  - 注册表基线（原文）:「- VERSION: HiPS 1.0 (**PR-HiPS-1.0-20161122**) + properties hips_version="1.4"」+ 清单行 3「| §4.2.1（properties 必需键集） | … CONFORMANT … |」（该清单行的键集与条款号问题另见 M2b-B-04）
  - 写侧:「kv.push_back({"hips_version", "1.4"});」
  - 四个读者四种口径（原文）:IO_002「必需，值须为 1.4（按前缀 "1.4" 判定）」→ runtime 执行「if (strncmp(v, "1.4", 3) != 0 … ACS_HIPS_ERR_PROPERTIES」；AIO reader「只要求该键存在（不校验值）」；Phase3「static const char* kRequired[] = {"order", "width", "format", "frame", "dataproduct_type"};」（**无 hips_version**）；夹具「s += "hips_version = 1.0\\n";」（三处同值）
  - 门侧要求字面量:run_evidence_orchestrator.ps1「hips_version\\s*=\\s*1\\.4」与 gate7_hips_validate.py「!= "1.4"」
  - 标准原文（实测取回，§4.4.1 第 4 条）:「hips_version : HiPS version number – Format: word **"1.4" corresponds to this document specification**」→ 写 1.4 本身合标，问题不在取值
  - 取代关系（实测取回，两页并存）:https://www.ivoa.net/documents/HiPS/ 「HiPS 1.0 – **IVOA Recommendation 19 May 2017**」；https://www.ivoa.net/documents/HiPS/20161122/ 「HiPS 1.0 – IVOA **Proposed Recommendation** 22 November 2016 … 本文件是一个建议标准…**may be changed before it is accepted** as a full recommendation」
- 权威依据: IVOA HiPS 1.0 §4.4.1；STD-REG-001 §1.6（域/版本变更须走宪章 §1.2 并在注册表写明版本）、§2（注册表面冻结「标准/域/版本」三元）；宪章 §7.2、§12.3-9
- 问题说明: 与 L15-003 分档（那条判键集与条款号，本条判 version 口径与基线文档标识，按前台裁决**保持两条**并交叉 related）。三层事实：① 同名键跨读者语义不同（值域校验 / 存在性校验 / 不校验），是「同一目录被一个读者判合法、另一个判非法」的直接根源；② 三份自产夹具写 1.0，与写侧口径永不同时成立，因走 AIO/Phase3 两个宽松口径才未爆红 → 掩盖真实不兼容；③ 注册表把判定基线挂在 superseded PR 上（该 PR 自我声明「评审中可能修改」），而判定实际按 REC 的 1.4 口径执行 → 版本面与判定面用两份文档。L03 在案「1.4 是否为该键正确取值」的待复核问题，**已由标准原文闭环**（1.4 正确），故本条剔除该项疑问、只保留上述三层事实。
- 影响: 跨阶段版本语义不可判定；测试夹具与产品口径背离；注册表 §2 的版本声明不满足自身 §1.6 的「写明版本」要求。
- 建议处置: ① D.hips VERSION 改指 REC-HIPS-1.0-20170519（PR 只作沿革注记）；② 存在性 + 值域校验收敛为单一实现，两侧读者共用；③ 三份夹具 hips_version 改 1.4；④ IO_002 的 hips_version 行改引标准 §4.4.1 原文口径。
- 置信度: 高
- related: M2b-B-04、M2b-A-02、L03-016、L15-003（两条并立、互为 related）
- 来源: L03-016
