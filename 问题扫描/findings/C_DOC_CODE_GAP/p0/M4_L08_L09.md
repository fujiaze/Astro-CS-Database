# 定稿 L08_L09 — Phase2 全链域（UPM/控制采样/覆盖 + 排异/积分/写出/会话）

> 合并代理 M4｜来源 L08+L09｜验证方式：逐条重读原文（当前树即时 read/grep；行锚以符号/关键语句重定位，标注「复核时点行号」，并发修复中可能漂移）
### M4-C-01 kcorr_lookup 把非均匀 pixfrac 网格当均匀插值：生产默认 0.8 列返回表外值，偏差 +1.5%/+2.1%
- 类别: C_DOC_CODE_GAP
- 定稿优先级: P0（来源 L08-001 原议 P0，**经独立重算成立**）
- 位置: lib/phase2/src/sampler.cpp::kcorr_lookup（复核时点 :88-109；冻结表值 :91-94；k_corr 冻结注释 :78-82）；消费点 sampler.cpp pass1_cell cvar 链（复核时点 :839-842）；docs/science/PHASE2_UPM.md:94,100,142；docs/algorithms/PHASE2_SAMPLER.md:405（§11.3 F2）、:188-196（§5.4）；docs/algorithms/UPM_SOLVER.md:98
- 证据摘录:
  >     static const double pf_grid[3] = {0.5, 0.8, 1.0};
  >     const double wi = (pf - pf_grid[0]) / (pf_grid[2] - pf_grid[0]);
  >     const double i0 = wi * 2.0, i1 = i0 + 1.0;   // 网格列
  （:89/:98/:99 逐字——「i0=wi*2」把 [0.5,1.0] 线性映到列区间 [0,2]，隐含三节点均匀假设）
  > | F2 | kcorr_lookup 边界与角点：pf∈{0.5,0.8,1.0}×sc∈{300,600} 九值、域外 clamp、provenance 缺失回退 1.4 | 角点值 exact；插值点 rtol 1e-12 | 无（新建；表值 :91-94） |
  （PHASE2_SAMPLER.md:405 逐字）
- 权威依据: ALG-P2-SMP-001 §11.3 F2「角点值 exact」（冻结容差，「每个 threshold 引用本节，禁止事后改」:399-400）；SCI-UPM-001 §9:94、§10:100（k_corr=1.4 属不可接受变化面）、§14:142（1.4=MC 实测 1.3883 @pixfrac=0.8 保守上取）；宪章 §6.3（常数冻结可追溯）
- 问题说明: M4 独立按 :95-108 逐语句重算：pf=0.8 → wi=(0.8−0.5)/(1.0−0.5)=0.6 → i0=1.2 → c0=(size_t)min(1.2,2.0)=1、c1=2、fx=0.2 → 表格节点被替换为「列1 与列2 的 0.2 线性混叠」。sc=300：1.3925+0.2×(1.4980−1.3925)=**1.4136**（冻结表值 1.3925，**+1.52%**）；sc=600：2.8971+0.2×(3.2035−2.8971)=**2.9584**（冻结表值 2.8971，**+2.12%**）。pf=0.5/1.0 两端恰 exact（wi=0/1、fx=0），掩盖中段系统性错位；scale 轴 r0=(600−sc)/300 经复核无错。角点语义结论：「表节点 pf=0.8 × sc∈{300,600} 返回表外值」成立；「九角点」中两角失真。正确形式=两段分段线性（0.5–0.8、0.8–1.0 各自归一）或网格节点直接命中表值。生产可达性独立核实：①Phase1 写侧 provenance 落 properties（lib/core/src/astrocs_phase2_writer.cpp 复核时点 :927-928 ASTROCS_DRIZZLE_PIXFRAC，%.6f）；②采样侧读回（sampler.cpp:112-135,:547-555）pf>0 即 kcorr_lookup(pf,sc)，**scale 未知时固定 300 档（:554）恰命中失真值 1.4136**，比冻结保守值 1.4 还高；③「0.8 生产默认」独立于 :80 注释另有两处：lib/orchestrator/cpp/src/json_config.cpp:711-713（CFG-001，pixfrac 省略→0.8；其测试 test_orchestrator_cli.cpp:905-924 断言同值）、lib/drizzle/README.md:114；④生产链：astrocs phase2 run 的 sample 节点唯一入口 p2_sample_controls_cached（module_adapters.cpp :2774/:2784 → sampler.cpp:1138 → p2_sample_controls_impl :463 → :552-554 → cvar :840）。
- 影响: 凡帧 provenance pixfrac=0.8（生产默认）的真实数据：control_variance=k_corr·(π/2)σ²/N_ret 系统性放大 +1.5–2.1% → control_ivar 相应缩小 → UPM 生产权重（quality×control_ivar）与 Huber sigma_eff 全链偏移；违背 F2 冻结设计与 SCI §10 不可接受变化面；全仓 tests/**、lib/phase2/tests grep「kcorr」零命中——无任何可执行测试暴露（F2 现状「无（新建）」）。
- 建议处置: 科学负责人裁决修正段映射；按 F2 设计落地九值角点门并接入产品构建（联动 M4-F-01）；修正前不得引用 §14:142 作为「冻结值已生效」证据。
- 置信度: 高（推导链全文可复算）
- 来源: L08-001
- related: M4-F-01、M4-F-05（[0.98,2.0] 宽区间门抓不到 ±2%）；ALG-UPM-CONTROL-IVAR-001

### M4-C-02 SCI 冻结弱零锚 1e-3 在两条生产装配均为 0；ALG「同值生产装配」为不实陈述
- 类别: C_DOC_CODE_GAP
- 定稿优先级: P0（来源 L08-002 原议 P0，成立；module_adapters 位置由叶子错锚 :2750-2774 订正为 fit_upm 节点，见 _merge/M4.md 漂移处置表）
- 位置: lib/phase2_session/p2_session.cpp::「P2UpmBuildConfig uc{}」装配段（复核时点 :183-202）；lib/core/src/module_adapters.cpp::「── op: fit_upm」内「P2UpmBuildConfig uc{}」（**复核时点 :3032-3056**）；lib/phase2/src/upm.cpp::入参修补（复核时点 :244）；lib/phase2/src/stage2_common.cpp:152-153；docs/algorithms/PHASE2_UPM_IMPL.md:364-365,373；docs/science/PHASE2_UPM.md:133
- 证据摘录:
  > if (cfg.zero_anchor_weight < 0.0) cfg.zero_anchor_weight = 1e-3;
  （upm.cpp:244 逐字——只拦 <0，0.0 原样生效）
  > **P2UpmBuildConfig 默认值**（cfg_in=null 缺省面 ：218-245；生产组装 p2_session.cpp:187-195 同值）：
  > | zero_anchor_weight（λ0） | 1e-3 | :226/:244 |
  （IMPL:364-365 + 表 :373）
  > **正则化**：Huber IRLS 鲁棒求解 + control-ivar 感知权重 + **弱零锚** `zero_anchor_weight=0.001`（弱 Tikhonov/岭型锚定向全局零）。
  （SCI:133 逐字）
- 权威依据: SCI-UPM-001 §9a:133（FROZEN 常数）；ALG-P2-UPM-IMPL §13 默认值表；宪章 §5.3/§6.3/§12.3-3（默认值不得静默改、文档↔实现一致）
- 问题说明: 两条生产装配均自 uc{} 零初始化出发，显式设置 robust_loss/snr_weight_mode/huber_delta/max_iterations/tolerance/target_order/sigma_floor/support_power/use_ivar_weight/control_reliability/input_manifest_hash，config 覆盖键仅 {max_iterations,huber_delta,smoothing_lambda}——**均从不赋 zero_anchor_weight**，有效值 0.0（逐行重读 :183-202 与 :3032-3056 双处确认）。build 修补只拦 <0；stage2 工具面回填 1e-3（stage2_common.cpp:152-153，且 lib/phase2/configs/ 8 个生产示例 JSON 全部显式 "zero_anchor_weight": 0.001）——同一冻结常数在三入口得 0/0/1e-3。ALG §13「生产组装 p2_session.cpp:187-195 同值」不实（该区间无 zero_anchor 字样）。零锚进 model_hash 序列化 payload（upm.cpp 复核时点 :890-891），故 CLI 产品与 stage2 工具对同一输入产出不同 model_hash 的模型。DISP-P2UPM-003（lib/phase2_upm/README.md:193）只登记「覆盖键仅三、不可配」，未登记「有效值≠SCI」。
- 影响: astrocs phase2 run（宪章 §3.1 正式入口）拟合 C 场缺 SCI 声明的定向全局零微调项；退化/欠定分量下 θ 分布改变；SCI §7 常量场不变量「弱零锚微调除外」的前提在 CLI 路径不成立；三入口产物不可比且 model_hash 差异被持久化面放大。
- 建议处置: 负责人裁决统一生产装配默认（两装配补 zero_anchor_weight=1e-3，或按 §1.2 修订 SCI），订正 ALG §13「同值」行与 DISP-P2UPM-003 登记文本；补「三入口组装等值」回归门（现无——见 M4-F-01）。
- 置信度: 高
- 来源: L08-002
- related: M4-C-04（同装配面 smoothing_lambda 的 SCI 断言失效）、M4-F-01；DISP-P2UPM-003

### M4-C-03 stage2 ivar tile 读失败逐像素静默换成 support 作权重：无开关、无计数；文档「显式 fallback 并计数」失实；节点链同场景 fail-closed 证系行为分叉
- 类别: C_DOC_CODE_GAP（宪章 §6.3 红线 + §14.4 双挂；主类别终裁归前台）
- 定稿优先级: P0（来源 L09-003 原议 P0，**复核时点在当前树仍成立**）
- 位置: lib/phase2/tools/stage2.cpp::并行权重构造（复核时点 :1106-1116，回退行 :1115）、串行同构（复核时点 :1359-1375，回退行 :1374；其 :1368 注释自称「禁止静默换 support」）、ivar_valid 置 0 处（复核时点 :1254-1263）、产品级 gate（复核时点 :549-578）、mode0 support×snr²（:1136/:1396）；对照链 lib/core/src/module_adapters.cpp::p2_op_integrate「ivar tile read failed where corrected data exists」（复核时点 :3786-3789）与 mode0 拒入（:3567-3570）；docs/algorithms/PHASE2_MOSAIC_WRITE.md:320-322；lib/phase2/src/stage2_common.cpp:372-385
- 证据摘录:
  >                         if (orig < depth && ivar_valid[orig]) {
  >                             const double iv = (double)ivarv[
  >                                 (std::size_t)orig * chunk_pixels + i];
  >                             weights[s] = iv;
  >                             ++tl_local_ivar_used;
  >                         } else {
  >                             weights[s] = support_v[s];
  >                         }
  （stage2.cpp:1109-1116 逐字；串行 :1363-1375 同构。ivar_valid 只在 aio_hips_read_tile_f32 成功时置 1（:1256-1258），tile 读失败即留 0——无须任何开关命中回退；回退分支无 ++ 计数，diag 仅 local_ivar_used 一项（:1732））
  > **禁止 support 当科学权重冒充 ivar**——weight_mode=2 缺 ivar 产品 → rc=7 或显式标红 fallback（:565-577），像素级缺 ivar 仅显式 fallback 路径可达并降级 support 计数（:1372-1375）；
  （MOSAIC_WRITE.md §7 :320-322 逐字）
  >           if (!has_ivar[d])
  >             return Result<void>::fail(Error(ErrorDomain::DATA,
  >                 "ivar tile read failed where corrected data exists (frame " + …
  （节点链 :3786-3788 逐字）
- 权威依据: 宪章 §6.3「support/coverage 不能作为 inverse-variance 或 SNR 权重」；§14.4（禁止无已知需求的 fallback/静默改默认）；SCI-INT §3:27（weights 单位=信号⁻²，support 无量纲 [0,1]）；SCI-UPM §10:98
- 问题说明: 三条子事实全部复核成立：①:565-577 的 gate 只拦「产品级打开失败」（整帧 ivar 产品 aio_hips_open 失败 → rc=7，除非 legacy_allow_weight_fallback=true），**帧 ivar 产品存在但个别 tile 读取失败**时 :1254-1263 → ivar_valid=0 → 逐像素权重=该像素 support（无量纲冒充 ADU⁻²），运行照常产出名义 ivar coadd；②文档失实——MOSAIC_WRITE:322 称「仅显式 fallback 路径可达并降级 support 计数」：tile 级路径不经 legacy 开关即达、且全文件无该回退的计数器/标红字段；串行分支 :1368 注释「禁止静默换 support」与紧邻 :1371-1375 else 分支自相矛盾；③行为分叉有对照实证：同一场景节点链 fail-closed（:3786-3789），证明「tile 级 ivar 缺失必须 hard fail」是既定合同而非设计两可。**可达性裁定（前台 D 问）**: astrocs phase2 run 生产路径=Runtime 7 节点 IR 链（cli/commands.cpp cmd_phase2_run 复核时点 :1008-1054「RT-008: phase2 走 Runtime 单 phase IR 子图」；cli/runtime_client.cpp:173-205；docs/contracts/PUBLIC_API.md:54 明载 stage2「V5 遗留, LEG-004 已退出生产」）——故本分支不在产品出口上；但 stage2 仍是可构建执行面、且 PHASE2_MOSAIC_WRITE.md:9 仍称其「实现源（唯一权威生产源）」，legacy mode0（support×snr²，宪章禁语）在 stage2 配置面仍可达（stage2_common.cpp:372-385），节点链则显式拒绝 mode0（:3567-3570）——同输入两链产物不同。
- 影响: 按工具文档执行 mosaic 的用户/验证面：ivar 个别 tile 缺失时权重语义被静默替换，输出改变且无计数可检出；文档以虚假的「显式+计数」描述为回退背书，削弱 §6.3 红线可执行性；「唯一写入路径」声称不成立。
- 建议处置: 与节点链归一（stage2 tile 级 ivar 缺失同样 fail-closed）；或至少显式开关 + 逐像素计数 + diagnostics 字段并登记偏差 ID；MOSAIC_WRITE §7 相应句订正；legacy mode0 若保留须 SCI/DATA 登记偏差。
- 置信度: 高
- 来源: L09-003
- related: **L18-002**（KNOWN_LIMITATIONS.md:13 + TROUBLESHOOTING.md:14,16 把「回退 support」当既有行为——同一根因的文档面，前台终裁并档）、**L07-004**（CONTROL_WEIGHT_SNR.md 把 support×snr² 标为 weight_mode=2 生产权重——权重定义冲突归 M3；本域代码执行口径裁定见 M4-C-07）、宪章 §6.3/§14.4

