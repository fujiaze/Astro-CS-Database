# M1a 定稿 · C_DOC_CODE_GAP · P0（来源切片 L01+L02）

## M1a-C-001 SIP 逆向冻结口径三方割裂（SCI/ALG 说 7×7、代码是 41×41 阶5 + 81×81 阶7）；SCI 冻结 1e-4 px 门在生产导出的 36 项一步逆上自证"数学不可达"，验收门实际钉在不可导出的迭代/APx 路径上

- 类别: C_DOC_CODE_GAP · 优先级: P0 · 来源: L01-004（与 M1a-B-001 同根，登记面另立）
- 位置: docs/science/ASTROMETRY.md::§5（自述）与::§7 不变量与::§9 实测口径、::§11 验收门；docs/algorithms/PLATESOLVE.md::4 与::符号表；lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp::extract_wcs_sip（"5.3"/"5.4"）与::solve_pix2world_iterative；tests/unit/p1wcs/p1wcs_tests_apbp.cpp::冻结门段；tests/unit/p1wcs/p1wcs_std_f1_bridge_cross.py::§2
- 证据摘录（逐字）:
  > （ASTROMETRY.md:57）AP/BP computed by least-squares inversion of forward SIP residuals **on 7×7 grid** (NB_GRID=7)
  > （ASTROMETRY.md:125）| `AP/BP` 逆向拟合 | `NB_GRID=7`（7×7 网格最小二乘反演）…
  > （ASTROMETRY.md §15:172）… **§11 Oracle 全过**（astropy 第三方交叉 `<1e-4 px`、往返 `<1e-6 px`）…
  > （ipv_wcs.cpp:402-403 / :526-527）const int NB_GRID = 41; … const int AP_FIT_ORDER = 5; / constexpr int NB_GRID_X = 81; … constexpr int APX_ORDER = 7;
  > （ipv_wcs.cpp:514-521 结论段）… **1e-4 px 冻结门: 由迭代反演达成**（AP/BP 为初值, 亚像素线性残差修正; 一步法不达标）
  > （ipv_wcs.cpp:397-400）… 冻结 1e-4 px 在该 fixture 下**数学不可达, 需负责人裁决 (finding)**。
  > （p1wcs_tests_apbp.cpp:256-257）// 布局扩展防退化观察线 (**非验收线**)   CHECK(ap36_onestep_max[f] < 50.0);
  > （p1wcs_std_f1_bridge_cross.py:304-305）build_sip_matrix(fx["APx"], fx["apx_order"], 10) …（喂给 astropy 的是 **APx/BPx 阶 7、10×10 布局**）
  > （lib/plate_solve/cpp/ipv/include/ipv_types.h:64-72）double A[36]; … double AP[36]; … double APx[100]; … int apx_order;  // 扩展逆向 SIP 阶数 (0=无, 上限 9)
  > （lib/photometric_calib/cpp/src/wcs_transform.cpp:46-50）sip_order 仅允许 **[0,5]**，越界即抛
- 权威依据: 宪章 §12.3-3（公式唯一实现）、§12.3-4（claim↔evidence）、§13.1、§17.1；SIP（Shupe 2005）AP/BP 为 A/B 的逐级反演；SCI-WCS-001 §7/§9/§11
- 问题说明（复核时点，四段独立可验）:
  (1) **口径割裂**：SCI §5:57、§9:125 与 ALG §4/符号表均把逆向拟合钉为"7×7 网格"，实现无任何 7×7 网格（41×41 阶5；WCS-003 另加 81×81 阶7）→ 冻结文本描述的对象不存在。
  (2) **门与路径错配**：SCI §11 的 astropy 第三方交叉门（1e-4 px）与 §15:172 的"Oracle 全过"宣称，本轮复核确认其达成者是**迭代反演**（:514-521 自述"由迭代反演达成；一步法不达标"），而测试正是走该路径（apbp:267-313 用 `closure_rt`/`invert_point`；bridge 交叉测试用 `x_it` 与 **APx 阶 7** 系数）。
  (3) **导出能力缺**：C ABI 可导出的逆向面是 `AP[36]`（阶 ≤5、36 项）；APx/BPx[100]（阶 7）**不经该结构导出**，下游唯一 WCS 消费者 `wcs_transform.cpp:46-50` 硬要求 `sip_order ∈ [0,5]` → 被用于达标的那组系数在链路上不可达。
  (4) **残余一步精度**：真正可导出的一步法 AP/BP 误差在 F2 病态 fixture 上 ~7.6 px（代码自证），测试侧只设 50 px"非验收观察线"（apbp:256-257）。
  → 综合：SCI 的 1e-4 px 不变量在**产品可消费的导出口径上不成立**，且该"数学不可达"事实以注释形式挂起、未升为偏差登记。
- 影响: 天测逆向精度在产品消费面失真（一步逆变换残差可达像素级）；验收门给出与消费面不同的口径的"全过"结论；负责人裁决项未入台账（另见 M1a-B-001）。
- 建议处置: ①订正 SCI §5:57/§9:125 与 ALG 为"41×41 阶5（APx 81×81 阶7）"；②明确 1e-4 px 门的**适用路径**（若只约束迭代反演，须在合同写明"导出的一步 AP/BP 不受该门约束"并给一步门数值）；③把"需负责人裁决"升为正式偏差登记（DISP-WCS-006 描述订正）；④若要求导出面达标，则扩 C ABI 到 APx 并同步放宽 wcs_transform 的 [0,5] 校验（须裁决）。
- 置信度: 高 · related: M1a-B-001、M1a-A-001、L13-001（M6 共签）、L01-013

## M1a-C-002 trans 线性奇异：SCI 承诺"返回参数错误"，实现仅 warn 并恒设 success=true，可回写冒称 TAN-SIP 的退化头

- 类别: C_DOC_CODE_GAP · 优先级: P0 · 来源: L01-005
- 位置: docs/science/ASTROMETRY.md::§8 退化与错误语义表（奇异 trans 行）；docs/algorithms/PLATESOLVE.md::§11.1（DISP-WCS-001/004 两条）；lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp::extract_wcs_sip；lib/orchestrator/cpp/src/orchestrator.cpp::run_stage_platesolve（success 门与头写回）；tests/unit/p1wcs/p1wcs_tests_negative.cpp::child_collinear
- 证据摘录（逐字）:
  > （ASTROMETRY.md:118）| trans 线性奇异（共线/退化配置） | **拒 SIP 推导，返回参数错误** | ipv_wcs.cpp:322 inv_det |
  > （PLATESOLVE.md:160-162）失败-置信度语义契约：**CD det 退化必须视为求解失败（success=0），禁止以 CRPIX 坍缩值冒充解**——本语义由 P1-WCS-IMPL 落地…
  > （ipv_wcs.cpp:340-343）if (std::abs(det_lin) < 1e-15) { logger->warn("…SIP 推导跳过（退化）"); } else { … 前向 A/B 拟合 … }
  > （ipv_wcs.cpp:637-639）result->success = true;   （`void extract_wcs_sip(` 全程无失败路径）
  > （orchestrator.cpp:2042）if (ret != 1 || wcs_result.success != 1) { … PLATESOLVE_FAILED … }
  > （registry:68）| DISP-WCS-001 … | 已由负面用例覆盖 |
  > （p1wcs_tests_negative.cpp:151-161）child_collinear() … 断言对象为 **iter_trans_solve** 返回 0（不覆盖 extract_wcs_sip 分支）
- 权威依据: 宪章 §6.3（禁静默降级）、§14.4（fail-fast）、§12.3-4；SCI-WCS-001 §8；ALG-WCS-001 §11.1
- 问题说明: 三条冻结文本一致要求"退化即失败"，实现只在日志 warn，函数返回 void 且 `success` 无条件 true → orchestrator 的失败门结构上不可能触发；随后的头写回块按 `order>=2`（:322）之外的路径仍写 CTYPE=RA---TAN 与 CRPIX/CD（order<2 时不写 SIP 键），于是"跳过 SIP"这一退化被作为**正常解**发布，即合同明文禁止的"以 CRPIX 坍缩值冒充解"。注册表以"已由负面用例覆盖"结案，而该用例的断言对象是另一函数。
- 影响: 退化/病态星场下产品头静默降级（无 SIP 修正或带未修正畸变的线性 TAN），下游按合格解消费；错误语义在 C ABI 上不可观测（IpvWcsResult 无失败标志位）；负面用例覆盖声明不实。
- 建议处置: ①`extract_wcs_sip` 增返回码/failure flag 并在 det<1e-15 分支置 `success=0`（与 ALG 契约对齐）；②orchestrator 侧保留现门即自然生效；③负面用例改为直接对 `extract_wcs_sip` 注入共线匹配点并断言失败码；④registry:68 在①③落地前不得标"已覆盖"。
- 置信度: 高 · related: L01-004、M1a-C-004、M1a-H-001（同族 fail-open 形态）、F00-06

## M1a-C-003 p1_op_wcs 自检以 0-based 数组下标喂 1-based 契约的 WcsTan；两条门与"独立参考解"共用同一原点 ⇒ 对原点平移零判别力；发布工件 p1_wcs.json 的 samples 无像素原点声明

- 类别: C_DOC_CODE_GAP · 优先级: P0 · 来源: L01-003
- 位置: lib/core/src/module_adapters.cpp::p1_op_wcs（显式配置分支自检环 + 求解分支自检环）与::p1_tan_forward_reference；lib/phase1/wcs/wcs_tan.h::WcsTan::crpix1 与::合同行；lib/phase1/wcs/wcs_tan.cpp::WcsTan::pix2sky
- 证据摘录（逐字）:
  > （wcs_tan.h:11）double crpix1, crpix2;   // 参考像素 (**1-based**)
  > （wcs_tan.h:2）合同: 已知 WCS 参数往返误差 **< 1e-6 deg**
  > （wcs_tan.cpp:9-10）double dx = x - crpix1, dy = y - crpix2;
  > （module_adapters.cpp:1785-1787）for (int y = 0; y < H; y += step) { for (int x = 0; x < W; x += step) { pts.emplace_back(x, y); …
  > （module_adapters.cpp:1799）wcs.pix2sky(static_cast<double>(x) + A, static_cast<double>(y) + B, &ra, &dec);
  > （module_adapters.cpp:1803-1805）double bx = 0, by = 0; wcs.sky2pix(ra, dec, &bx, &by); const double rt = std::hypot(bx - x, by - y);
  > （module_adapters.cpp:1809-1811 / :254-255）p1_tan_forward_reference_sip(sip, wcs.crpix1, wcs.crpix2, …, x, y, &ref_ra, &ref_dec);   ——  其中 `const double u = x - crpix1; const double v = y - crpix2;`
  > （module_adapters.cpp:1823/:1832）"explicit WcsTan roundtrip … px exceeds 1e-6 contract" / "exceeds 1e-9 deg contract"（求解分支 :1977/:1982 同型）
  > （ASTROMETRY.md §10:137）… 在 Phase3 导出边界（§5a）之外再施加一次、或在该边界处**缺失** +1 桥接——两种情形都产生恒定 1px 系统偏移
- 权威依据: FITS WCS Paper I §2.1.1（xp = x + 1）；SCI-WCS-001 §5a/§10:137；宪章 §12.3-15（像素语义显式）、§12.3-4
- 问题说明: `WcsTan` 的头注释钉死 CRPIX 为 1-based、映射按 `dx = x − crpix1`，即其"x"必须是 1-based 像素值；调用方喂的是 **0-based 数组下标**（x∈[0,W)）。后果两条：①该环产出的 `p1_wcs.json` samples 的像素列与天球列配对**相对标准口径整体平移 1px**，而该 JSON 内 `wcs` 块由 `r.crpix`（1-based）写出、其**生产消费端**（wcs_transform.cpp:190-191 `dx = x-(crpix1-1)`）按 0-based 解读 → 同一工件内部两套原点；②两条验收门（往返 rt 与"独立前向参考解"交叉）都建立在**同一个被平移的原点**上（参考解同样做 `u = x − crpix1`），因此任何常数原点平移在两门上恒为零 —— 门对该缺陷类的判别力为零，而 :1832 的门面文字宣称"绝对正确性"。
- 影响: 天测验证工件的像素-天球配对带恒定 1px 系统偏差（按 s_out≈0.5″/px 即 0.5″），且被两道"全过"的门掩盖；下游按 samples 做星表配对/残差统计者继承该偏差。SCI §10 明文禁止的第二种情形（边界之外缺失 +1 桥接）正在发生。
- 建议处置: ①`pts.emplace_back(x + 1, y + 1)`（或在 pix2sky/sky2pix 调用处桥接），与 Phase3 单点桥接同一裁决；②p1_wcs.json 增显式像素语义键（`pixel_origin: "0-based array index"` 或 "FITS 1-based"）；③为原点平移加**必败负向用例**（注入 ±1 必须使门失败，参照 p3_wcs_test.cpp:289-298 的九宫格+双桥必败模式）；④参考解改用真正独立的 Paper I 形式（xp = x+1）以恢复交叉门的判别力。
- 置信度: 高 · related: M1a-B-003、M1a-A-001、L01-002、L02-009

## M1a-C-004 alpha 适用 FOV ≤ 20° 为冻结约束但全仓零强制点；三处合同对"是否硬门"互斥；超限/半球外像素静默留 NaN 仍计数发布

- 类别: C_DOC_CODE_GAP · 优先级: P0 · 来源: L02-002
- 位置: docs/science/PHASE3_HIPS_TO_FITS.md::§9a-12 与::§15；docs/standards/STANDARDS_REGISTRY.md::DISP-WCS-001/偏差表；docs/algorithms/PHASE3_PROJ_IMPL.md::§13（非硬门表述）；lib/phase3_session/p3_wcs.cpp::p3_wcs_validate_descriptor / ::p3_wcs_pix2world；lib/core/src/module_adapters.cpp::p3_op_resample（worker）
- 证据摘录（逐字）:
  > （SCI-P3 §9a-12:109）…**alpha 适用 FOV ≤20°** 冻结（中心距极点 ≥5°）；容差：WCS roundtrip 1e-6 px…
  > （SCI-P3 §15:115）本条款集合为 alpha 适用上限，放宽须修订本文件；**产品实现不得自行放宽**（§15 收窄不扩大）
  > （p3_wcs.cpp:46-64）validate：仅校 CRPIX 有限、CD 非奇异、`proj != "TAN"` 拒绝、`|dec|>85` 拒绝（**无 FOV/尺寸/s_out 联合上限**）
  > （PHASE3_PROJ_IMPL.md:382-384）… registry map 为**非 make 硬门**…（FOV 约束由合同层承担，实现不设门）
  > （registry:68 附近）DISP-WCS-001 …（FOV/畸变适用域相关偏差）
  > （module_adapters.cpp:4842-4844）if (p3_wcs_pix2world(&w_wcs, x, y, &px_ra, &px_dec) != P3_WCS_OK) continue;   // **半球外像素保持 NaN/0**
- 权威依据: 宪章 §7.3（逐投影须声明合法 FOV/奇点/方向，禁静默 fallback）、§12.3-13、§6.3、§17.1；SCI-P3-001 §9a-12/§15
- 问题说明: 复核时点确认：FOV 上限仅存在于 SCI 文字，**生产链四处校验面（请求层、p3_wcs_validate、p3_projection_make 的四角守卫、节点参数校验）无一实现它**（p3_projection_make:315-352 的守卫为 parity/|dec|≤85/scale/size + 四角可映射）。ALG §13 明确写"非 make 硬门"，与 SCI"不得自行放宽"互斥 → 三合同互斥（另见 registry 偏差登记）。可达性复算：TAN 前向半球界以 r≥π/2 判定，r=tan d ⇒ 有效半视角 d<57.519°，即 kMaxSide=20000、s_out 极小时可出**对角 FOV 约 115°** 的头与产品；worker 对界外像素 `continue`（NaN/0）但该行带仍被计数并发布（详见 M1a-H-001 同族形态）。
- 影响: 违反 §17.1（SCI 冻结约束与实现无冲突）；超限帧不被拒绝而产出"中心正确、外围静默缺值/严重投影畸变"的产品，使用者无从判别；TAN-only 的现行控制使该缺口当前不落到已发布产品（前瞻风险 + 门禁不实）。
- 建议处置: ①在 p3_wcs_validate_descriptor 或请求层补 FOV 硬门（对角半角 atan(r_max) 与 20° 比较，返回 P3_WCS_FOV/UNSUPPORTED）；②三处合同订正为单一口径；③把 FOV 门列入 §17.1 发布门证据（含负向用例）；④若裁决为非硬门，须修订 SCI §9a-12/§15 并登记偏差。
- 置信度: 高 · related: M1a-A-003、M1a-A-010、M1a-H-001、L02-001
