# V10 · 外部输入数值解析（分片 2：N-04..N-06）

### V10-N-04 `BSCALE`/`BZERO` 解析 `catch(...)` 吞错取默认、**不吃 FITS 的 D 指数记法** ⇒ 同一合法 FITS 头两个读面差三个数量级，直进交付像素
- **ID** V10-N-04 ｜ **类别** C_ALG_IMPL ｜ **优先级** **P1** ｜ **状态** OPEN
- **站点**：`lib/astro_image_io/src/aio_fits.cpp:161-162` `try { hdr.bscale = std::stod(kw.value); } catch (...) {}`（`BZERO` 同型）；默认 `:117-118`（1.0/0.0）⇒ `"abc"`（invalid_argument）与 `"1e999"`（out_of_range）**静默保留默认**；`"1.5xyz"` 静默取 1.5（**无 endptr**）；无 `isfinite` ⇒ `nan`/`inf` 可过。
- **消费式（进交付像素）**：`:853-856` `pixel_data_f64[i] = hdr.bscale*pixel_data_f64[i] + hdr.bzero`（`:886-889` float 路同型）。
- **最硬处：与同仓另一读面口径冲突**：`fits_reader.cpp:64-71` 的 `parseDouble` 自述「兼容 FITS 的 D 指数记法，如 `1.5D-3`」并做 D→E 归一，而 `aio_fits.cpp` 无 ⇒ **同一合法 FITS 头两处得到 0.0015 与 1.5**（差 1e3），不抛不报。
- **"做对的一半"（本轴反复出现的形状）**：同函数 `:168-187` 的 NAXIS 有 `<=0` 拒 + P0-4 的 **65535 上限**，注释还自述「与 `aio_xisf read_file` 既有校验口径一致」⇒ **几何维度夹了、定标系数没夹**。
- **测试全是正向断言**：`tests/backend/test_p3005_fits_output.py:74`、`test_p3_output.py:173`、`tests/cli/test_phase3_inprocess.py:286-288` 只查「键存在/类型合法」⇒ **无畸形值锁**。
- **同型站点一并修（V10 已列）**：`aio_fits.cpp:386-388`（`kw_float`）；`drizzle_engine.cpp:1147-1149` 与 `:2152-2154`（`get_meta("GAIN")` → `try{...stod...}catch(...){}`，gain 写进 HISS 头 JSON `hiss_common.cpp:194` 并可被 `:273` `get_num("gain")` 读回 ⇒ **下游标定静默用错增益**）；该文件 `:332`/`:493-512`/`:557-561`/`:1165`/`:1322`/`:2038` 多处有 `isfinite` ⇒ **属漏、不属口径**。
- **建议**：统一到一个 FITS 数值解析器（D→E 归一 + endptr 全消费 + `isfinite` + 失败即拒），禁各读面手写 `stod`+`catch(...)`。
- **related** **`V5-N-04`**（值域校验做一半）、`V10-N-06`（同文件同族字段）、`V7-N-04`（drizzle 读面另一处）、`C-20`、簇 1 第三机制

### V10-N-05 HiPS SNR TSV 坏行**只 ++ 一个全仓无消费者的计数器** ⇒ 交付星表少行完全不可观测；`%lf` 还接受 NaN/inf
- **ID** V10-N-05 ｜ **优先级** **P1** ｜ **状态** OPEN
- **站点**：`lib/astro_image_io/src/hips/aio_hips_reader.cpp:332-339`：`sscanf(... "%lld %lf %lf %lf %u %u") == 6` 才入表，`else ++d->bad_snr_rows`；`:344` `return d.release()` **仍按成功返回**。
- **死码实证**：全仓（排除 `./run/`）grep `bad_snr_rows` → **仅 `:98` 声明 + `:339` 自增**（无 getter、无 `set_err`、无门引用）⇒ **交付星表少行完全不可观测**。另一半：`==6` 只挡字段**个数**，`%lf` 仍接受 `nan`/`inf`/`1e999` ⇒ **NaN 坐标与 NaN SNR 入表**（影响匹配半径与显著性选择）。
- **同一 TU 的正对照（判"漏"不判"口径"）**：`:55-57` `valid_product_params`（`tile_width==512` 且 `hips_order∈[0,29]`）+ `:300-307` 硬失败，注释**逐字写明历史危害**（4x 堆越界写、`1ULL<<(2*order)` 移位 UB）⇒ 对 properties 立刻硬校验、对 TSV 行却吞错加计数。
- **建议**：坏行数经既有错误面暴露，或「SNR 计数与实际行数不一致即硬失败」（与 `module_entry.cpp` 的 `ln != want` 同口径）；行值补 `isfinite`。
- **related** **`V11-N-01`（同一 SNR 目录的另一半：镜像字段数落后）**、`V5-N-03`（静默整批丢数据）、簇 1 第五机制（0 输出记 PASS）、`M9-H-2`

### V10-N-06（P2）`readFits` 无 NAXIS 上限：声明尺寸直乘成分配量，而同族两个读面都设 65535
- **位置**：`fits_reader.cpp:315-321` 仅 `if (naxis1 <= 0 || naxis2 <= 0)`；`:333-342` 对 `naxis3` 只做归一（非 1/3 按 1，**无上限**）；`:419-422` 直接相乘为 `data_size` 并 `std::vector<uint8_t> raw(data_size)`。
- **正对照**：`aio_fits.cpp:176-187`（P0-4 的 65535）与 `aio_xisf.cpp:32-33` `XISF_MAX_DIM` + 消费处 `:514-521` 硬拒 ⇒ **同族字段的第三个读面没跟**。
- **量级与如实限定**：`NAXIS1=NAXIS2=100000`、`BITPIX=-64` ⇒ `data_size≈8e10` ⇒ `bad_alloc` 或占坑；`hp_drizzle_api.cpp` 入口**未见 try 包裹**（`phase2/sampler.cpp:168-173` 的 `catch(...)` 只护 resize）⇒ **是否必崩属运行期**（V10 已在§5 标「需运行期未判」，我不改写为事实）。测试：65535 类锁只在 `aio_fits`/`xisf` 面，**drizzle 侧无**。
- **related** `V10-N-01`（同文件短读）、`V10-N-04`（同族字段两处已夹一处漏）、簇 9
