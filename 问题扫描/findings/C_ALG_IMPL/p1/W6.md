# W6（第二轮 · 构建与可移植）片1 · P1 三条｜扫描发起 a3a343a4、收口 1d2f84fc（构建面六文件两时点同 blob，逐文件 rev-parse 已核）

### W6-N-01（P1）三面对账：CTest 登记面把 **6 个「结构上永不可生成」的用例名当存量在册**，门无「可达性」维度
- 口径：复刻 CI-REG-002 采集面 + 本轴**独立算根图 `add_subdirectory` 可达闭包** ⇒ 活动 CMake 源 52 个、`add_test(NAME)` 名 211 个，**根图可达面只有 205 个** ⇒ 6 名来自不在任何构建图里的子图：`healpix_math`/`stf_engine`/`browser_backend`/`hips_browser_backend`/`geometry_truth`（`lib/healpix_db/healpix_browser_qt/CMakeLists.txt`）+ `module:astrocs.conformance.echo`（`modules/conformance/echo/`）。
- **双向都不红的机理**：这 6 名全在 `ci/ctest_baseline.json` 的 160 条冻结清单内 ⇒ `check_ctest_registration.py` 的 **C2(b) 判「已覆盖」**、**C5 只比「名还在源文本里」** ⇒ 门能证明「源里有 + 基线里有」，**证明不了「该名在 CI 任一构建目录里真注册过」**。
- 二级复核已排掉「另有入口」：全仓受跟踪文件对这两个子图的引用只有 baseline 自身、其自身 CMakeLists、若干 `tools/*.py` 清单脚本，**0 处 `add_subdirectory`**。另账：门 `ctest_targets` 显式覆盖 **51/205 ≈ 25%**，其余 154 名全靠一次性基线兜住（设计选择，不单列）。related `M8-F-005`（在册≠有效）、`V19-N-06`、`V17-N-03`、簇 1 机制①

### W6-N-02（P1）`V19-N-05` 复验：**21 道门的宿主工具声明一条未补**；按更宽口径本轴另得 **74 门**（两口径不可混用）
- 实测：`prerequisite_tools` 非空的门 **18/135**；声明值全集为 cmake×10、clang×3、llvm-profdata、llvm-cov、pytest、dumpbin、python3:yaml、python3:astropy×2、python3:numpy×6 ⇒ `g++`/`gcc`/`nm`/`objdump`/`tar`/`bash`/`taskset` **以及 `ctest` 被声明次数全为 0** ⇒ **`V19-N-05` 点名的 21 道门一条都没补**（含 `ctest` 也从未声明，属新子项）。
- 新站点（口径 W6-E：门入口脚本 + 一级 import 内 argv 首位字面量）：`nm` → `check_legacy_exit.py:21,69`、`check_duplication.py:30`、`check_prod_reachability.py:111`；`objdump` → `check_isa_leak.py:49`、`check_baseline_opcodes.py:18`、`tests/backend/test_isa_*.py`（5 份）；`tar` → `make_linux_release.py` + `tests/quality/test_linux_release.py`；`taskset` → `p1drz_taskset_invariance.sh` + `tests/backend/test_hardware_inspect.py`。
- 姊妹门反差仍在：`UT-BACKEND` 与 `UT-IO` 与 `UT-CLI` 的 declared 均为空，而其加载体分别裸 import numpy 5/astropy 4、numpy 4/astropy 4、astropy 1。**机器面建议也未落地**：`ci/validate_registry.py`（blob `48e7eee0`，两时点同）实测**只有 R10/R11，V19 建议的 R12 无实现**。related **`V19-N-05`（本体，未修）**、`V19-N-06`、`A-28`（裁决未施工）、簇 1 机制⑤

### W6-N-03（P1）安装面白名单**不闭合**：实装文件超出合同 2 个，且唯一白名单校验器**三面零注册**
- ① `cmake/install_layout.cmake` 的 `install(DIRECTORY packaging/schemas/ DESTINATION schemas FILES_MATCHING PATTERN 「*.json」)` 会把 `packaging/schemas/` 下**全部 4 个 json**装进 `<prefix>/schemas/`，而 `packaging/install-tree.contract.json` 的 units 只登记 **2 条 SCHEMA** ⇒ `schemas/dependency-lock.schema.json` 与 `schemas/preset-contract.json` 是**白名单外载荷**，与该文件自写「clean install 仅白名单」冲突（对照：`licenses/` 面 3 文件 = 3 unit，一致）。
- ② 它自指为机器校验的 `packaging/verify_install_tree.py`（118 行）在 **135 道门里 command/args 引用 = 0**；且其实现只查 required 与 manifest `rel_path` 存在性，**没有「白名单外多余文件」的闭合判据**（docstring 第 1 条承诺了，实现里没有）⇒ **即便接上也不抓超装**。
- ③ `packaging/**` 出现在 `changed_paths` 的门只有 `WIN-PACKAGE-CANDIDATE`/`WIN-CANDIDATE-VALIDATE` 两道（均 windows-main）⇒ **Linux 安装面 0 门**。是否真在交付树多出两文件需真机 `cmake --install` 后清点，**未判**（静态判据：install 规则是 DIRECTORY + PATTERN，无逐文件白名单）。
- related `M5b-G-05`（建议改读 install 白名单，未做）、`M5b-G-04`、`M8-F-003`/`L23-003`（install 门恒 SKIP）、`V14-N-05`（「收编即装」同族反向）、`W4-R2-05`（发布面对账验幽灵布局）、§12.3
