# V7 · 交付数值静默变化（分片 2：N-04..N-07，时点 HEAD `23ffd698`+脏树）

### V7-N-04 drizzle 引擎 `has_cd` 只看 CD 对角 ⇒ 合法「对角双零」WCS 被**静默替换**为 CDELT·CROTA2，交付天区几何错（**前台由 P2 升 P1**）
- **ID** V7-N-04 ｜ **类别** C_ALG_IMPL ｜ **优先级** **P1** ｜ **状态** OPEN
- **位置**：`lib/dynamic_psf/src/../hp_drizzle_api.cpp:428` `has_cd = (cd11 != 0 || cd22 != 0)`；退化分支 `:433-443`；CD 系数经裸 `atof`（`:474`）**无 `isfinite` 门** ⇒ inf 位模式直入。
- **两条后果**：①**轴交换/纯旋转 WCS**（`CD1_1=CD2_2=0`、`CD1_2`/`CD2_1≠0`，`det≠0`）**完全合法**：无 CDELT 时报 `-9`「header 缺少 WCS 信息」——**错误文案说谎**（WCS 存在且合法）；有 `CDELT`+`CROTA2` 时 `:433-443` **静默用 CDELT·CROTA2 替换真 CD** ⇒ 交付 HiPS 的天区几何与像素落位皆错。②`atof` 无有限性门 ⇒ 畸形头不报错、直接把 inf 带进投影。
- **为何升 P1（我的判据，V7 原判 P2/B 档）**：`CD1_1=CD2_2=0` 正是**旋转 90°（或轴交换）**的形态，在真实巡天/行星空产品与多数 FITS 工具链改写过的头里**常见**，不是构造场景；且**无任何判别位**（stderr 打的是替换后的值），落点是 HISS/HiPS 几何与 signal 落位 ⇒ 属"静默几何错"，严重度与 `V2-N-08`（静默改变科学量）同级。
- **测试**：仓内**无 CD 对角双零正例**（⇒ 双向锁不存在，V7 明确记录）。
- **建议处置**：①`has_cd` 改「四元任一非零」或**以 CD 键存在位**判定；②CD 构造完成后校验 `det != 0` 且全元素 `isfinite`，否则 `DATA` 拒；③补一条轴交换 WCS 正例 + 一条 inf 头负例。
- **related** `M1a-B-006`（CD/CDELT 混写并族）、**V5-N-03**（同属"输入解析无有限性门"）、`V5-N-04`（值域校验做一半同族）、`C-20`、`V4-N-10`（同属"判别位记声明不记生效"）

### V7-N-05（P3）`p1_snr.json` 不回显生效 SNR 科学配置，九键缺键→0.0 后**改了什么数值不可归因**
- `module_adapters.cpp:2638`：`snr` 键存在但非 object ⇒ **整块静默忽略**（连错型都不报）；九键逐个 `value(k, 0.0)`（`:2640-2648`，`gain`/`read_noise`/`zero_point`/`aperture`/`n_sky`/`profile_half`/`sigma_logflux`/`n_matches`/`reference_flux`——**nlohmann `value()` 缺键给默认＝静默面，错型抛＝响面**）；`:2694` `noise_sigma` 缺 ⇒ `σ_sky=0`。`gain=0` ⇒ 天空受限 `σ_i²` 无 RN 项；`aperture=0` ⇒ auto 半宽 ⇒ 交付 `median_snr`/`frame_depth` 全变。判别位仅 zp 回显（`:2657`）+ sigma 状态位（`:2724-2727`）⇒ **同输入不同 snr 配置的两次 run 事后不可分**。测试无该回显锚；九键亦未登记配置键权威清单（`C-08` 族）。
- 建议：每帧回显生效 `sci_cfg` 摘要（或 snr 块 sha256）+ `snr` 键错型即拒 + 九键入清单。**并 `V1-N-13`**（`gain>0` 双计 RN 的休眠态正可由这条回显被发现）、`A-40`。

### V7-N-06（P3）`calibrate` 交付面 `dark_scale` 双语义：「应用了 K=1.0」与「暗优化未执行」逐字段不可分
- `:1392` `dark_optimization` 缺→false；非 K 分支（缺 bias 或 dark 或 flag 关，`:1399 k_branch`）时 `per_frame.dark_scale = k_fixed`（`:1493-1496`，默认 `1.0f`，`:1393`）；manifest **不记** `dark_optimization`/分支来源。⇒ K 分支本身由 EXPTIME 推导并强比对（**fail-closed 正例，值得记一笔**），**唯关闭态无痕**。
- 建议：`per_frame` 记 `{dark_optimization, k_source: "exptime_ratio" | "config" | "inactive"}`。**related `V2-N-09`**（`psf_mode` 恒写 fast＝同属 §4.3「记实际执行路径」），注释自引「P2-11 另行处置」需一并挂账。

### V7-N-07（P3）`nested` 的类型强制与同文件同批纪律相反 ⇒ 用户 `"0"` 得到 NESTED，**tile ipix 全重排**
- `:2891` `nested = p1_int(dj, "nested", 1)`：字符串 `"0"` ⇒ 错型 ⇒ 取默认 **1 = NESTED**（**用户要 RING 拿到 NESTED**）；`0.5` ⇒ 截断为 0（合法值但非用户所求）。**同文件同参数面的 `nside`（`:2855-2876` 拒字符串/浮点）与 `precision_mode`（`:2898-2906` `is_number_integer` 前置门）已收紧**，注内自述属 B1-A9/B2-A12 同批纪律 ⇒ **`nested` 是该批的漏网键**。落点 `p1_stack.hiss`/HiPS tile 序；`res.nested` 回显生效值但**无请求位对照** ⇒ 不可发现。
- 建议：照抄 `precision_mode` 的 `is_number_integer` 前置门（一行）+ 补错型测试。**related `M6a-C-004`**（DLL 缺省 0 已挂）、`C-20`（一键一缺省）、`V7-N-01`。
