# W1（第二轮 · 隔夜新代码全审）片1｜时点 HEAD a3a343a4｜P1 三条

### W1-N-01（P1）ipv_select.cpp 触顶判据的**重写基于错误前提**：新注释断言每文件顺序截断，实为跨文件全局硬顶
- 新注释称 gaia_client 每文件顺序截断使被截断文件恰返回 cap_unit，故 n_ret 大于等于 cap_unit 必有一文件触顶，k 乘 cap 与 cap 加 partial 均被捕获。三级复核：`gaia_client.c:1538/1607/1667/1736/1807/1875` 的 `MAX_STARS_RESULT=200000` 是 **collector 全局（跨文件、每查询）硬顶**而非每文件 ⇒ 饱和时 n_ret 恒恰等于 200000，**cap 加 partial 与 k 乘 cap 两态在真实客户端不可达** ⇒ `V2-N-10` 缺陷2 的部分截断漏判前提为假，**旧 fmod 判据在可达集内并不漏判**。
- 后果：默认值下新判据与旧等值（**不变好**）；若 `m_lim_gaia_cap_per_file` 被配置为小于 200000 ⇒ **把合法 n_ret 大于等于 cap 误判触顶并提前 break、记 converged=false** ⇒ 新失败模式。且 `ipv_types.h:101` 交付头注释同步改为每文件上限（**错误口径扩散到公共头**），`test_mag_iter.cpp` 3b（600000）与 3d（250000）把**不可达态钉成必须判触顶**的期望 ⇒ 反向钉死第七站候选。
- 处置建议：要么恢复等价的旧语义并删除错误注释，要么把 per-file 上限做成真每文件语义（改客户端）并同步测试；**不得保留注释-客户端-测试三者互斥**。related `V12-N-11`（200000 一具名三复制）、`V2-N-10`、INDEX §三反向钉死、机制⑫
