# V21 片3-4 · P1 五条 + W3 收编说明｜HEAD a3a343a4

### V21-N-12（P1）产品 manifest 的完整性两面在整条链上被「**期望为空**」短路 ⇒ 换掉 `modules/*.dll` 只要文件存在即放行
- **写侧**：`packaging/astrocs.product.json` 与 `cmake/astrocs.product.windows.json.in` 的 `units[].sha256` **两平台 10/10 恒 null**（json.load 复算 sha 值集合 = {None}；`.in` 内 10 处 sha256 为 null）。
- **读侧 1**：`runtime/registry/module_registry.c:305-308` 注释「null 时字段保持空」→ `:735` 的 `if (e->sha_registered[0] && e->sha_actual[0] && strcmp(...))` 比对被短路（**registry 仍无条件 `file_sha256_hex` 算了实际值，成本已付**）。
- **读侧 2**：`:780` 调 `load_dll(..., e->sha_registered, 「空字面量」, e->abi_version, ...)` ⇒ `expected_build_id` 位**硬传空** → `secure_loader.c:601` 的 `BUILD_ID_MISMATCH` 判据**形同失效**；`secure_loader.c:492-497` 仍无条件读全文件算 sha256，而**结论在 `:499` 的 `size>0` 处被丢掉**。
- **承诺无实现 + 反向钉死**：`packaging/verify_install_tree.py:14-15` 自述「sha256 为 null 时跳过（ABI-004 填充后启用）」，但全文 `sha256` **只出现在该注释行**——**非空分支根本不存在**；`tests/abi/mod001_install_load_check.py:266` 更**断言 sha256 must be null** ⇒ 反向钉死第八站候选。
- ⇒ 宪章 §18.4「只加载签名清单官方模块」的**完整性维度在产品面上是空操作**。ABI-004 是否真填＝需运行期未判；**「读端无非空分支」为静态定论**。related `M5b_L12_L17`（product.json sha256 全 null 面，本条给出读端机理）、`W4-R2-04`（sha256 弃 ok）、`V11-N-05`、`C-21`、簇 1 机制③

### V21-N-13（P1）`module_adapters.cpp` 节点 manifest 有 **14 个键写出后全仓零回读**（含 docs/tests/tools）
- 清单：`band_executed`、`max_forward_cross_deg`、`mosaic_root`、`nside_clamped`、`photometry_provenance`、`photometry_provenance_artifact`、`product_sha256`、`props_artifact`、`resampled_artifact`、`snr_schema`、`verified_artifact`、`wcs_artifact`、`wcs_plan_artifact`、`writer_artifact`（写侧共 234 次 `(*man)[...]`、100 个不同键，`scripts_v21_c3b.py`）。
- **两条是状态位/版本声明型，危害高于路径型**：`nside_clamped`（`:3211`/`:3232`，语义＝nside 被 [16, 2^22] 钳位、`drizzle_engine.h:99` 自述「决策不自由」）**无人据此告警**；`snr_schema`（`:2845` 写单键 `DATA-P1-SNR/2`，而产物内是 `schema` + `schema_version` 两键 `:2838-2840`）⇒ **两种表意并存且无人比对**。
- 限定：`max_forward_cross_deg` 的**数值本身在 `:2195` 有 1e-9 deg 绝对门判定**，本条只报 manifest 副本无人读。related `V18`（只写不读族）、机制⑪、`C-12`

### V21-N-15（P1·**§10.5 门禁的第一根因**）`cli/commands.cpp:912` `g.has_stage_annotation = true;` 与 `:946` `g.monitor_present = true;` —— 两个判据输入**唯一生产写点即硬编码常量 true**
- ⇒ `resource_gate.h:247`/`:300` 的 `UnannotatedPriority`（07 §1「无 stage 标注且 wall 大于 5s 判 P1」）与 `monitoring_effective():323` 的 monitor_present 半边**恒不触发**。
- **旁证（同族判据整体失活）**：`is_stage_priority()` 被标 `[[maybe_unused]]`（`:722`）且零调用；`classify_stage`/`res_kind_name`/`is_unannotated_priority` **生产零调用**（读者仅 `tests/cli/test_monitor_events.py:156-167` 用**合成入参自证**）。⇒ 与 `V21-N-03`（monitor 的 ok 被丢弃）构成同一失效链。related `M5a-C-003`、`V15`（测文本不测行为）、§10.5、`V21-N-16/17`

### V21-N-16（P1）`cli/commands.cpp:901` `g.kind = astrocs::ResKind::Compute;` 是**生产唯一 kind 写点** ⇒ 四条分类判据分支在生产不可达
- 不可达的分支：`resource_gate.h:240`(Mixed)、`:284`(Memory)、`:291`(Io，含需 `io_bytes`/`io_ops`/`io_await_ms` 证据的 IoMissingEvidence)、`:298`(Unknown)；且 `utilization_value():215` **对非 Compute 直接 return 0.0**（未知分类＝零利用率）。⇒ **07 §3 四分类门在 CLI 面只剩一类**。
- **这是 `M5a-C-003`「GateConfig 字段未赋值」的上游根因**，事实不同故单列挂 related。related `M5a-C-003`、`V21-N-09`（`read_ops` 装字节却挂 ops 名 ⇒ 即便接线也算错量纲）、§10.5、`V21-N-15`

### V21-N-17（P1）内存回压**双哨兵恒假** ⇒ §17.6「无界内存增长属发布禁止项」在产品面**失去执行者**
- `runtime/runtime.cpp:121` `spec.estimated_memory_bytes = 0;`（**上一行 `:113-116` 刚从 `plan()` 取了真实 min/max_workers 却没取内存**）+ `:96` `make_unique<Scheduler>(budget_, budget_)` **不传 `memory_limit_bytes`** ⇒ `scheduler.cpp:172` 的 `if (memory_limit_bytes_ > 0 && ...)` **永不进入**。
- 复算强度：全仓 `Scheduler(` **仅 2 命中**（定义 `:24`、生产构造 `:96`），**三参构造只出现在** `tests/unit/core_scheduler_test.cpp:140,167` ⇒ 测试测的是生产中不存在的构造形态。
- 同族：`ModulePlan.{work_units, parallel_axes, kernel_ids, estimated_memory_bytes}` 由 `module_adapters.cpp:448-6236` 写出而宿主**只读 min/max_workers**；`RT-005 estimate_plan()`/`plan_estimator.cpp` **生产零调用者**（唯一读者 `tests/runtime/test_rt005_plan_estimator.py`）。
- **判否同批记录（防邻站误修）**：`cli/commands.cpp:821` `f10.first10s_mem_not_saturated = true;` 也是硬编码，但**方向是 fail-closed**（让三条件门更易触发失败），注释自述 07 §4 保守口径 ⇒ **属负责人设计意图内，不判缺陷**。related §17.6、§10.5、`V21-N-15/16`、`A-41`（`allocated_capacity_cores` 分母项已由 `M5a-G-002` 修复，本条是其姊妹缺口）
