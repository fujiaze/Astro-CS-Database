# V21（第二轮 · 算了写了无人据此行动）片1-2 · P1 五条 + P2 六条｜HEAD a3a343a4，133 门，语料 3095 文件（C/C++ 903 去 vendored、生产 745；Python 真源 388）

### V21-N-01（P1）`acs_cap_detect_v1` 的**四码值域被塌缩成两码**：6 站点全写成「非 ACS_CAP_OK → ACS_ERR_UNSUPPORTED」⇒ 把装配缺陷归因成硬件事实
- 值域 4 码见 `capability_v1.h:110-114`（含 `ERR_ABI_MISMATCH=2`，return 点 `capability_detect.c:156,158,175,179`）；塌缩站点 `providers/cpu/{baseline:63,avx2:76,avx512:103}` 与 `{baseline:614,avx2:398,avx512:398}`。
- **后果**：provider 与探测库**不同构建**（`struct_size`/abi 失配）被报成「这台 CPU 不支持 AVX2」⇒ 宿主**静默退 baseline**，ISA 面失效无从区分。**反证这不是全局约定**：同函数 `avx2_provider.cpp:391-395` 对 `host_abi` **仍正确保留** `ACS_ERR_ABI_MISMATCH`。
- **related** `V11-N-01..10`（跨语言 ABI 面）、`W1-N-05`（新结构不带 struct_size）、`M5a-G-009`、`C-14`

### V21-N-02（P1）`aio_healpix_io.cpp:732,759,786,813,846`（+`:493-498`）：HISS 的 **8 码错误域全塌成 `HIO_ERR_FILE(-2)`**，而 HIO 本有 -1..-7 七个可用码
- 被吞的码含 `CHECKSUM=-4`、`UNSUPPORTED=-6`、`UNKNOWN_REQUIRED=-7`（**规范 §13 要求拒绝**）、`FORMAT_VIOLATION=-8`（`hiss_format.h:242-249`）。
- **后果**：**产物位翻转/篡改**与「文件打不开」在 HISS_VERIFY 面上**不可区分**（而 `orchestrator.cpp:3689` 明写依赖该码）。⇒ 与 `W4-R2-04`（sha256 弃 ok）同一失效族的下游侧。**related** `W4-R2-04`、`M2b-B-09`（CHECKSUM 恒零卡）、§12.3、`C-16`

### V21-N-05（P1）`tools/check_warning_suppression.py:79-81`（判定 `:102-104`）：**`grep -c` 的 returncode 恒为 0 且不读退出码**，判定把「构建没跑 / `cd || exit 9` / cmake 缺失被 `|| true` 抹平」与「零警告」**并列通过** ⇒ `QA-001_PASS「生产警告=0」` 可 exit 0
- 这是簇 1 机制③（门校验错对象/空对象）的又一独立实例，且与 `V9-N-17`、`V17-N-01`（127 与 exec 失败被当必败通过）构成**同一类误归因的第三站**。**related** `M8-F-007`（同门两处 `|| true`）、`V9-N-07`、`V17-N-01`、簇 1 机制③、`E11`/`E13`

### V21-N-06（P1）`tools/quality/contracts/check_full_integration.py:20,29-34`：子报告 **exit 码不读**（同文件头自述 0/1/2/3 四态）+ `except: pass` 吞掉 `waivers.json` 解析失败；**三级复核：全仓只有本文件读 waivers.json、无任何生产者** ⇒「waivers 必须为空」静默失效仍可 PASS
- ⇒ 直接对抗 AGENTS 的**「严禁用 waiver 掩盖红灯」（R-05/R-13）**：这道防掩盖的门自己**看不见 waiver 文件是否存在**。**related** `M8a-C-006`、`V18-N-13`（额度与豁免同源）、`C-19`、`R-05`/`R-13`、簇 1 机制⑤

### V21-N-07（P1）打包链 rc 全丢：`scripts/package_audit.py:29-30,34,62` + `tools/quality/build_v19r{2,3,4}_package.py`（`r2:52-56,69,197-198`／`r3:54-58,71,290`／`r4:54-58,78,400`）+ `tools/assemble_audit.py:243-249` vs `260-262`
- 三文件 `returncode` 计数均为 **0** ⇒ `git()`/子进程失败后**空清单仍 return 0**，最终包校验器 `rc2` **只打印不判**。**后果：交外部审阅的包可以是空源码快照且无提交锚，而显示 `PACKAGE_OK`/exit 0**（触 §16.3 与审核包白名单义务）。
- **related** `W4-R2-07`（审核包生成器自宣 PASS）、`W4-R2-05`（对账验幽灵布局）、簇 1 机制⑤、`M8-F-004`

### V21-N-03（P2）`cli/monitor.h:144,157,179-180` + 唯一调用点 `:230`：Linux 分支自算的 `bool ok` 被 `(void)ok;` **丢弃并无条件 `return true;`**，调用者又丢弃 bool 返回值 ⇒ 失败通道不可达
- 后果：`/proc` 不可读时产出「全 0 但成功」样本，叠加 `monitor_present=true` ⇒「**监控没跑**」被归因成「利用率低」（正是 §10.5 门禁依赖的信号源）。**related** `V12-N-09`（32 三义）、`M5a-G-002`（已 FIXED 的分母面）、`V21-N-08/09`

### V21-N-04（P2）`modules/conformance/echo/src/echo_module.c:350,362,371`：出参 `acs_error_info_v1 aerr/rerr` 被 `artifact_open`/`read_all` **填后从不读**（全仓对 `aerr` 的引用只有 memset 与传址两态），真实 `detail_code` 被 `efill` 写死的静态消息覆盖
- **related** `M5b-C-05`（错误码表假声明）、`V21-N-01/02`（错误码塌缩族）

### V21-N-08（P2）`cli/resource_recorder.h:43` 的 `ResRecord::system_cpu_pct`：**表头有该列（`:229-231`）、序列化 fprintf 有该参数（`:235-243`），而全仓赋值点 = 0** ⇒ 该列恒 0.00
- ⇒ 读曲线者据「系统 CPU=0」判「机器空转」。`M5a-C-003` 未列的**新站点**（本轴 L1 检索 findings 与 `_verify` 对该符号零命中）。同族 `pss_bytes`/`queue_depth`/`lock_wait_ns`/`progress` 挂 related 不重报。**related** `M5a-C-003`、`V21-N-09/10`

### V21-N-09（P2）`cli/monitor.h:76-79`（填点 `:124-126,:138-140,:166-167,:175-176`）+ `:245`：`ProcSample` 五字段 **prodW>0 而 prodR=0**（`read_ops`、`write_ops`、`sys_mem_avail`、`sys_swap_free`、`d_rss_bytes`）；`record()` 只搬 `d_read_bytes`/`d_write_bytes`
- ⇒ `GateConfig.io_ops`/`io_bytes` 类判据需要的原料**已在采样层存在却在映射层被丢**。**另记一个量纲事实**：Linux 侧 `read_ops`/`write_ops` 装的其实是 `rchar`/`wchar`（**字节数）却挂 ops 名** ⇒ 会诱导后续当 ops 用。**related** `V12-N-09`、`V21-N-08`、`M5a-G-002`

### V21-N-10（P2）`include/astrocs/core/context.h:31-39,170-200` + `lib/core/src/context.cpp:28-52` + `logging.cpp:52-112`：**RunContext 合同服务面生产空转**
- `ctx.log()` 有 6 个生产写入点（`runtime.cpp:151,197`；`module_adapters.cpp:521,5182,5373,6315`），而 `log_entries()` 的读者**只有两个单测**；`add_metric`/`record_tick`/`metrics()`/`ticks()`/`mark_checkpoint`/`checkpoints()`/`store_artifact()` 与 CORE-008 的 `Logger`/`LogSink`/`InMemorySink`/`MetricsAggregator` **全族零生产实例化**。
- **`Metrics::completed_units` 是 §10.5「work units」在 C++ 侧的唯一载体，恒 0** ⇒ 节点级 INFO/ERROR 随 ctx 析构丢弃，「统一日志 + work units 面」只剩 `docs/contracts/RT-001.md:43` 的文档事实。**related** 机制⑪、`V21-N-01`（同族「接口存在但无消费者」= `V11-N-05`）、§10.5、`C-12`

### V21-N-11（P2）`lib/astro_image_io/include/hiss_format.h:352` 的 `uint32_t n_contrib`（注释自述「诊断用」）：生产写 2 点（`drizzle_engine.cpp:1251,2363`）、**全仓读 0**（51 处同名命中经 L3 通读全为测试自建 fixture）
- **related** `V10-N-05`（`bad_snr_rows` 同族死码）、机制⑪
