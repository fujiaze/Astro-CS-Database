# W3（第二轮 · 并发与资源生命周期）· P1 三条｜HEAD a3a343a4｜由 findings_r2 收编并规范 ID

### W3-R2-001（P1）`aio_upm` 原子晋升在 Windows 正常路径＝**先删旧再 rename**，双失败时**新旧模型同灭**
- `aio_upm.cpp:86`/`:101-111`：UCRT `std::rename` **不替换已存在目标**（同仓 `io_adapter.cpp:125-127` 逐字承认）⇒ Windows 覆写既有 UPM 模型每次都走 `remove(path)` → rename 的删除窗口；二次 rename 失败**再删 tmp** ⇒ 旧新同灭，而注释称「失败可恢复」**为假**。Linux 分支不可达 ⇒ CI 零暴露。
- **同仓正对照**：`hiss_stream_writer.cpp:172-193` 注释明令禁止 delete-first 并用 `MoveFileExW(REPLACE_EXISTING)` ⇒ 作者知道该怎么写。related `B4-22`（原子写出族）、INDEX §三「边界做一半」、§11、`IO_AND_ATOMICITY.md:3`、`W4-R2-01/02`

### W3-R2-002（P1）**取消通道整体断线**：`astrocs_host_state_set_cancel` 全仓零调用 ⇒ `cancel.is_cancelled` 恒 false
- 定义在 `host_services.cpp:128`，**无任何调用方**；`executor.cpp:108` 为每个 work unit 新建**裸 RunContext（只注入 budget）**；adapter resample band worker（`module_adapters.cpp:5768` 起）**零取消检查且丢弃 `RunContext&` 形参**（`:5850`）。
- ⇒ `ALG-P3-003` §5c（`PHASE3_RESAMPLE.md:87` 行带粒度取消 + 取消不落盘）与 `THREAD_BUDGET_ARCH.md:27` 在 CLI/IR 生产路径**不可达**；SIGINT/first-10s 快速失败只在「当前节点返回后」生效（`scheduler.cpp:292`）。**`p3_session` 通道有行级检查（`:269`）** ⇒ `M3-C-005` 两通道纪律的**逆不对称新实例**。related 簇 8、`M3-C-005`、`V21-N-10`（同一裸 RunContext 的合同服务面空转）

### W3-R2-003（P1）进程级峰值观测单例**无 epoch 复位** ⇒ node trace 的 `workers` 被记成**全进程历史峰值**
- `context.h:99-103` 的 `peak_lease`/`peak_active` 单调不减、无 reset；`runtime.cpp:177-189` 用 `acquired_total` 差分判「本节点有没有 acquire」，**但写入 `tr.workers`/`tr.granted_workers` 的是全局 `peak_lease`** ⇒ 非最大节点一律虚高、并发节点互相归属、跨 run 泄漏。
- ⇒ §11「trace 记实际观测」语义失守。`M5a-G-004` ③ 已覆盖 `commands.cpp` 消费 `peak_active` 面，**本条是同单例的第二累加器 + 第二消费点**，挂 related 不重报。related `M5a-G-004`、`V21-N-08/09`（只写不读与恒值面）、§11、§10.5
