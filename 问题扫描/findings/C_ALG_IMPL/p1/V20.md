# V20（声明了但不被消费 · 四面）片1｜HEAD b84454f7（工作区脏 174 路径，锚按当前工作区并附文件 sha256 前 12 位）

### V20-N-01（P1｜面1+面3）`lib/drizzle/src/module_entry.cpp`（sha `ca6c28c885d0`）`op=reverse` 的**四个 config 键解析并校验后零读取** ⇒ 用户改这四键产物逐位不变
- 键：`pixel_scale_arcsec`/`ra_center_deg`/`dec_center_deg`/`projection` 在 `:448-481` 做值域校验（`types.h:65-68` 自称 reverse op 专属）后写入 `rev_scale`/`rev_ra0`/`rev_dec0`/`rev_proj` + 三个 `have_*` 共 7 字段，**全树零读取点**；reverse 的实际几何**只从 input manifest 的 crval/crpix/cd 取（`:1049-1063`）**，plan 只 echo nside/pixfrac。
- **唯一效果是「非法值被拒」** ⇒ 语义上等价于「四个用户输入只做输入验证，不做配置」。反查命令与命中数：对 7 个字段名全树 grep **仅 11 命中且全为该文件的声明与写侧**；键名字面量全树仅 `types.h` 定义 + 本文件错误文案。
- 处置二选一（不可留现状）：让 reverse 真用这四值（并以 manifest 为冲突时的权威），或**删除解析与注释并在 API 面声明它们对 reverse 无效**。related `V2-N-09`/`V10-N-07`（回显即消费族）、`W2-N-03`（标签与载荷分叉）、`C-20`
