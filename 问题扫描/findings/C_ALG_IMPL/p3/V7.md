# V7 分片 3 · N-08 与**判否负清单**（供其余各轴免重报）

### V7-N-08（P3）亲和性探测失败 ⇒ 静默按 1 核，**无「探测失败」判别位** ⇒ run manifest 的 `budget=1` 与"真 1 核机"同值
- **位置**：`cli/commands.cpp:83-95`（`sched_getaffinity != 0` → 1；计数为 0 → 1）；`hardware_inspect.cpp:165` **甚至不查 `sched_getaffinity` 返回值**（`CPU_ZERO` 后空集 → avail=0 → `worker_advisor` 的 `derive_limits_v1` 保守取 1；advisor 侧有 reason 串＝**半记账**）。
- **后果（口径要说清）**：影响的是 §10.5 门禁**分母**与墙钟；`8c977118` 之后**科学数值已线程无关** ⇒ 不构成交付数值缺陷，但构成"绿/红取决于一次静默探测失败"（与 `A-39`、`V4-N-01` 同一根问题域）。
- **建议**：budget 记 `probe: "affinity" | "fallback_1"`，并让 `hardware_inspect` 检查返回值；探测失败时应拒绝跑门禁而非按 1 核跑。
- **related** **A-39**、`V4-N-01`、`M5a-G-004`、`C-20` 第③条（判别位须记生效值+来源）

### V7-N-10（P2）杜撰锚残留：`module_entry.cpp:355` 仍以「`API-DRZ-001` 默认 1.0」为 pixfrac 缺键依据，而该节**无默认条款**（`M2a-C-6` 已实证）
- IR 节点 `module_adapters.cpp:2892` 亦仍取 1.0。⇒ 一条**已被证伪的合同引用**继续充当缺省值的依据（注释被指为权威却无宿主条文）。**挂 `M2a-C-6` related，不另立机制**。
- 建议：删该锚或改指真实条文；若 1.0 是刻意缺省，按 `DISP-*` 登记偏差（`A-37` 会决定 `DISP-*` 是否一等键）。

## V7-N-09 **判否负清单**（V7 逐行核为 fail-closed / 非缺陷，各轴与后续扫描**不得重报**）
1. `precision_mode`：缺键与错型**双拒** + 写 `PRECISION` KV（B2-A12 现行实现正确）。
2. `nside`：auto/explicit/undersampled 三态冲突**全记账**（`module_adapters.cpp:2828-2889`，注释即合同）。
3. p3 的 `sampler`/`bitpix`/`projection`/`frame`/`coverage_output`：值域**硬拒**（`p3n_geom :5316-5325` 区）；连 `"Nearest"` 拼写错误都会被拒（V7 自认最初候选、核后判否）。
4. HISS：meta **恒无条件写 `signal_dtype`**（`hiss_common.cpp:197`），reader 对 `dtype=1` 文件用 f32 读**显式拒**（`hiss_reader.cpp:943-945`、`:1101-1104`）。
5. UPM/sampler：**1T↔2T 一致性锁在位**（`synthetic_gate` CON-005、`ivar_wiring_test:320`、`sampler_parallel_consistency_test`）。
6. `reject` AUTO profile：回显进 `p2_rejection.json`。
7. provider/ISA 路由：`fallback_reason` 全可见但 run 不消费 ⇒ **已挂账**（`M5a-C-001`、`M5b-C-06`），不重报。
8. **全生产 FP 归约站点普查结论**：`snr_estimator` 全部 reduction 为 int/min/max 计数（线程无关）；phot/noise 有 P10-UTIL2 逐行独立确定性论证注释；**唯一 FP 归约站点是 drizzle，且已被 `8c977118`（DRIZZLE-DET-001）修复并带双向锁 `p1drz_taskset_invariance`** ⇒ V7 的头号候选按免重报纪律降为表格行。
9. `cli/**` 参数面：`strtol` 仅出现在 `ASTROCS_TEST_*` 钩子（非生产数值）⇒ 无新增静默强制。
- **覆盖度自述**：`module_adapters` 全部 `p1_*`/`.value` 读取点**逐点列表 22 行**并按可达性分 A/B/C 档（档案 §1）；runtime 租约/预算、benchmark/profile、cgroup/affinity 探测＝表 #19-21。
- **需运行期未判三条**：①轴交换 CD 正例在真实输入上的触发（仓内无样本，`V7-N-04`）；②cosmetic 错型 config 端到端表现；③probe→1 回退下 §10.5 实际分母。
