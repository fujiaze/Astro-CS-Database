# V21 片3 · P2 一条 + W3 收编

### V21-N-14（P2）安装树合同与产品清单的版本/归因字段：`packaging/install-tree.contract.json:5` 的 `target_version = 0.11.0-alpha.1` 对根 `VERSION = 0.11.0-alpha.2` **已实证漂移**
- `schema/install-tree-contract.schema.json:12` 的 `const` **同钉旧值**；同批升版已把 `module_dll_contract.schema` 的 `const` 升到 alpha.2 ⇒ **证明是被遗漏而非有意**。
- `product_version`/`source_commit`/`root_layout` **读端零判定**（`verify_install_tree.py:58-80` 只读 install_path/required/rel_path；`cli/commands.cpp:2030-2045` 只读 kind/unit_id/module_id/abi_version/status/sha256）。
- **两平台新鲜度不对称**：Windows 走 `configure_file` 生成新鲜值（`.in:3-4`），Linux **直装仓库静态文件**（`install_layout.cmake:124-138`）⇒ 按 manifest 归因一次 Linux 构建会得**错版本错 commit**（审核取证错锚）。related `M5b` 版本簇、`W4-R2-06`（provenance 时间戳硬编码）、`C-17`、`A-42`/`A-43`

## W3 轴收编说明（我的派发失误，非轴主的错）
- 我给 W3 的任务书写错一句（「档案只写 `问题扫描/findings_r2/` 之外的 `问题扫描/_verify/`…」语义不通），致其把三条 P1 直接落进 `问题扫描/findings_r2/W3/p1/W3.md` 且用 `W3-001` 三段式 ID（**抽取器要求「前缀-类别-序号」**）。
- 处置：由我把该档案**搬入 `findings/C_ALG_IMPL/p1/W3.md` 并把 ID 规范为 `W3-R2-001..003`**，`findings_r2/` 作废目录随后删除。教训并入 `E4` 类：**派发模板里的落盘路径必须是一句无歧义的话**。

### V21-N-03（P2）`cli/monitor.h:144,157,179-180` + 唯一调用点 `:230`：Linux 分支自算的 `bool ok` 被 `(void)ok;` **丢弃并无条件 `return true;`**，调用者又丢弃 bool 返回值 ⇒ 失败通道不可达
- 后果：`/proc` 不可读时产出「全 0 但成功」样本，叠加 `monitor_present=true` ⇒「**监控没跑**」被归因成「利用率低」（正是 §10.5 门禁依赖的信号源）。**related** `V12-N-09`（32 三义）、`M5a-G-002`（已 FIXED 的分母面）、`V21-N-08/09`

### V21-N-04（P2）`modules/conformance/echo/src/echo_module.c:350,362,371`：出参 `acs_error_info_v1 aerr/rerr` 被 `artifact_open`/`read_all` **填后从不读**（全仓对 `aerr` 的引用只有 memset 与传址两态），真实 `detail_code` 被 `efill` 写死的静态消息覆盖
- **related** `M5b-C-05`（错误码表假声明）、`V21-N-01/02`（错误码塌缩族）

### V21-N-08（P2）`cli/resource_recorder.h:43` 的 `ResRecord::system_cpu_pct`：**表头有该列（`:229-231`）、序列化 fprintf 有该参数（`:235-243`），而全仓赋值点 = 0** ⇒ 该列恒 0.00
- ⇒ 读曲线者据「系统 CPU=0」判「机器空转」。`M5a-C-003` 未列的**新站点**（本轴 L1 检索 findings 与 `_verify` 对该符号零命中）。同族 `pss_bytes`/`queue_depth`/`lock_wait_ns`/`progress` 挂 related 不重报。**related** `M5a-C-003`、`V21-N-09/10`

### V21-N-09（P2）`cli/monitor.h:76-79`（填点 `:124-126,:138-140,:166-167,:175-176`）+ `:245`：`ProcSample` 五字段 **prodW>0 而 prodR=0**（`read_ops`、`write_ops`、`sys_mem_avail`、`sys_swap_free`、`d_rss_bytes`）；`record()` 只搬 `d_read_bytes`/`d_write_bytes`
- ⇒ `GateConfig.io_ops`/`io_bytes` 类判据需要的原料**已在采样层存在却在映射层被丢**。**另记一个量纲事实**：Linux 侧 `read_ops`/`write_ops` 装的其实是 `rchar`/`wchar`（**字节数）却挂 ops 名** ⇒ 会诱导后续当 ops 用。**related** `V12-N-09`、`V21-N-08`、`M5a-G-002`

### V21-N-10（P2）`include/astrocs/core/context.h:31-39,170-200` + `lib/core/src/context.cpp:28-52` + `logging.cpp:52-112`：**RunContext 合同服务面生产空转**
- `ctx.log()` 有 6 个生产写入点（`runtime.cpp:151,197`；`module_adapters.cpp:521,5182,5373,6315`），而 `log_entries()` 的读者**只有两个单测**；`add_metric`/`record_tick`/`metrics()`/`ticks()`/`mark_checkpoint`/`checkpoints()`/`store_artifact()` 与 CORE-008 的 `Logger`/`LogSink`/`InMemorySink`/`MetricsAggregator` **全族零生产实例化**。
- **`Metrics::completed_units` 是 §10.5「work units」在 C++ 侧的唯一载体，恒 0** ⇒ 节点级 INFO/ERROR 随 ctx 析构丢弃，「统一日志 + work units 面」只剩 `docs/contracts/RT-001.md:43` 的文档事实。**related** 机制⑪、`V21-N-01`（同族「接口存在但无消费者」= `V11-N-05`）、§10.5、`C-12`

### V21-N-11（P2）`lib/astro_image_io/include/hiss_format.h:352` 的 `uint32_t n_contrib`（注释自述「诊断用」）：生产写 2 点（`drizzle_engine.cpp:1251,2363`）、**全仓读 0**（51 处同名命中经 L3 通读全为测试自建 fixture）
- **related** `V10-N-05`（`bad_snr_rows` 同族死码）、机制⑪
