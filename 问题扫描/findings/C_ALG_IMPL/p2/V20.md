# V20 片1 · P2 两条


### V20-N-02（P2｜面1）`max_workers` 六模块解析、四个生效、**两个零消费**，其中 hips 连「仅作回显」的承诺都不成立
- 生效四方：calibration `:942-943`、cosmetic `:760-761`、drizzle `:912-914`、gaia `:661-662`（真参与租约 want 收缩）。零消费两方：hips（sha `04102ff20a48`）只写 `:387-388`、租约恒 `acquire(...,1)`（`:844`），**且 plan JSON 硬写字面量 min/max_workers = 1**（`:666`）⇒ **用户设 4 而产物报 1，是回显失真而非回显**（`types.h:74` 与模块注释 `:307` 的「仅作回显」表述本身也不实）；snr（sha `29e329af3b56`，未跟踪件）`:473` 写、全树零读、**plan 根本不输出该键**。
- related `W1-N-03`（snr 整块零编译）、`V2-N-09`、`M5a-C-001`、`V21-N-15/16`（同一族：判据输入与被控对象不同源）

### V20-N-04（P2｜面2）`cli/commands.cpp`（sha `b85aec30ec7c`）`--resource-detail` 两档**唯一行为差别是多两个字段** ⇒ 「timeseries 详略」从未产出任何曲线点
- `curve_points` **恒为 `nlohmann::json::array()`**（`:657`）、`downsample_max` 为常量 121；全仓唯一曲线构造器 `astrocs::build_curve()`（`resource_events.h:96`）**生产零调用**（唯一调用是同文件 `:115` 与 acr 测试里的同名局部函数），raw 曲线由 `recorder.write_all` 无条件落盘（`:869`）与该开关无关。
- **反向钉死第七站候选**：守卫 `tests/cli/test_monitor_events.py:119-127` **只断言 `downsample_max`/`raw_dir` 两键存在** ⇒ 恒空数组也过。已知面不重报：`--cpu-profile` run 路径（`M5a-C-001`）、drizzle `--nside`/`--pixfrac` 零读取（`M5b_L12_L17`，仅登记于三态表）。related `V15`（测文本不测行为）、`V21-N-13`、机制⑪
