# V20（声明却未消费的参数）片2 · P2 四条｜时点 HEAD 9ebffdd4（147 脏；module_adapters 锚口径 sha256 92bd0b21b68e，本轴两次复核、26 条漂移锚已逐条改正）

### V20-N-05（P2·面1）phase3 平铺「特征键」`output_fits_path`/`sampler_used`/`mode` 的**值全仓零消费，但存在却改判**
- 三键只被 `cli/parser.cpp:336` 列入 `kSessionKeys`、被 `cli/runtime_client.cpp:42-43` 用 `contains()` 判存在；**真实输出路径由生产者写死**（`module_adapters.cpp:5882` 的 `fits_path = g.out_dir + "/output_phase3.fits"`；`p3_session.cpp:376-380` 同），`sampler_used` 是输出字段（`p3_output.h:23`）。
- **要害**：`parser.cpp:346-362` 的 `flat_session` 判定会因这些键的**存在**而**跳过 `schema_version`/`inputs` 两项必填校验** ⇒「值不被读、存在却改判」＝一个不携带任何语义的键集合成了合同的旁路开关。反查：`value("mode")` 生产面 0 命中。
- related `V7-N-03`（缺键给默认）、`M5b-G-16`、`V19-N-06`（采集与校验分离）、簇 1 机制②

### V20-N-07（P2·面3）导出面形参三态：`(void)` 刻意未用 41 处 对 **真·零引用未标注 4 处**
- 四处：`cal:790`/`cos:623` 的 `build_out_manifest(..., err)`——**函数唯一出口 `return ACS_OK`，出参错误对象永不填充**；`cal:1076`/`cos:852` 的 `describe(..., self)`——**同族其余 4 模块都写了 `(void)self`**。另 `decode_plane(cal:761-780)` 的 `what` 被 `(void)` 丢弃，而调用点传入了 `"data"`/`master_bias`/`master_dark`/`master_flat` ⇒ **四个平面的失败共用同一句话**（诊断丢平面名）。
- **编译器面无兜底**：根 `CMakeLists.txt:646-657` 的 `-Wall -Wextra` 白名单**不含 cal/cos/drizzle/hips**（其 CMakeLists 只加 `-fopenmp`）⇒ 未用参数在此四模块连告警都不会有。related `W5-N-11`（生产零 `[[nodiscard]]`）、`M2a-C-12`（恒零列同族）、`V21-N-13`

### V20-N-08（P2·面3+面4）`P3nGeom.frame` 与 `coverage_output`：**唯一合法值就是缺省值**，且写入后全仓零读者
- `p3_session.cpp:99-102` 只接 `icrs|ICRS`、`:128-129` 只接 `mask` ⇒ **可配面是单点域**；`module_adapters.cpp:5305`/`:5306` 写入后**零读者**。
- **同 struct 正对照**（证明不是整块死码）：`projection` 在 `:5454` 有读、`sampler` 在 `:5584`/`:5623`、`bitpix` 在 `:5887` **都真消费**。
- 与 `V7-N-09` 第 3 条分工明确：V7 判「值域硬拒非静默」，本条判「合法域单点 + 字段零读」，**不推翻 V7**。related `V7-N-09`、`V20-N-05`、`C-20`

### V20-N-09（P2·面3）**只写不读字段已成簇**：6 个结构体 / 18 个字段
- `drz_cfg`×7、`hips_cfg`×1、`noise_cfg`×4（含 `cfg_present`：**13 处写、0 处读**）、`drz_rows.n_bytes`、**`gaia_inst.cancel_active`（`:443` 声明、`:650` 置 1、`:666`/`:730` 清 0，全仓零读 ⇒ 生命周期标志记而不用）**、`P3nGeom`×2。
- ⇒ 与 `W3-R2-002`（取消通道整体断线）直接互证：`cancel_active` 正是那条断线的**本地残留端**。related `W3-R2-002`、`V21-N-08/09/13`、机制⑪

## 判重与负结果（本片的免报部分，必须记录）
- **`V20-N-06` 与已入账的 `V20-N-04` 为同一事实**（`--resource-detail` 两档实质等价、`curve_points` 恒空、`build_curve` 生产零调用、守卫只断言键存在）⇒ **不重复入账**，仅补一条新证：守卫注释自认 `only a marker array (empty)` ⇒ **空壳被钉成规格**（比「断言太弱」更重，属反向钉死候选，已并入 `V20-N-04` 语境）。
- **未初始化读：本轴该面零确证**（任务书列为最高优先）。`include/astrocs` 473 struct / 2386 字段扫出 **28 个「有读无写」候选，逐条通读后全部**是宏位置初始化（`ACS_KERNEL_ENTRY`、`AIO_ABI_INFO_V1_INIT`、`backend_table.inc`）或 memset 后赋值 ⇒ **不立条**。
- 登记脆弱点一条（未成缺陷）：`upm.cpp:1296-1306` 的 else 分支只赋 4/16 字段，**当前读者恰只这 4 个** ⇒ 一旦有新增读者即成未初始化读。related `M9-H-*` 族
