# V10 分片 3 · N-07..N-09（各档严重度经 V10 主动收窄，我保留其口径）

### V10-N-07（P2）三个 module_entry 的**自定义 JSON 解析器**：数组元素数无上限（先巨额分配再拒）、`double→uint64` 无 `isfinite`（`1e999`→inf→转整 UB）
- **站点（同族三处）**：`lib/hips/src/module_entry.cpp:174-226`、`lib/drizzle/src/module_entry.cpp:151-206`、`lib/gaia_xpsd_client/src/module_entry.c:152-201`。①`json_get_u64_array`/`json_get_f64_array` 第一遍只数 `count`（由外部字符串决定），随后 `malloc(count*sizeof)`（hips `:218`、drizzle `:206`、gaia `:190` 附近）⇒ **count 无上限**；调用方**随后**才比对 `an != n_tiles`（hips `:462`）⇒ **巨量元素先分配再拒**。②`json_get_f64` 有首字符挡 + `end==p` 挡，但 `"1e999"` 经 `strtod` 得 `HUGE_VAL` 且 `end!=p` ⇒ **inf 通过**；`json_get_u64` 即 `(uint64_t)d` 且只挡 `d<0` ⇒ **inf/超 2^64 转整为 UB、小数静默截断**。
- **现产影响不夸大（V10 自限，我认可）**：hips 边所有参与内存的 u64 **都有后续硬校验**（`:342` nside 为 2 的幂且 ≤2^24、`:349` `tw==512`、`:436` `n_tiles<=65535`、`:443`/`:450` 等值比对、`:523` `snr<=1e8`、`:482-492` 平面等长）⇒ 值错面已夹；未夹只有 `moc_order`（`:385`，被 `aio_hips_writer.cpp:501` `std::min(moc_order,tile_order)` 夹住）与 `max_workers`（`:387`，`:307` 注释自述**仅作回显**）⇒ **判「仅诊断 + 巨量分配」**。
- **同族正对照（关键差异）**：`drizzle/src/module_entry.cpp:354-475` **逐字段** `isfinite`+值域（pixfrac [0,1]、precision −1|0|1、nside u32 且 2 的幂、rev w/h i32≥1、scale>0、ra0/dec0 finite）⇒ **三兄弟里唯一每字段都夹**；`gaia module_entry.c:223-257` `gaia_cfg_check_params` 对标量逐个 `isfinite` 并给专用码 `ACS_GAIA_ECODE_PARAM_NOT_FINITE`，**但数组版 `ra_list`/`dec_list`（`:319-328`）只判 `nr<0|nd<0|nr!=nd`** ⇒ **标量做对、数组没做（同文件相邻代码），且无逐元素 `isfinite`、无元素数上限**。
- **建议**：count 先与调用方期望值比对再分配（两遍法改单遍带界）；`(uint64_t)d` 前加 `isfinite` + `[0, UINT64_MAX)` 界；数组版对齐标量版。
- **related** `V5-N-04`、`V10-N-02`、`C-20`、`M9-H-2`

### V10-N-08（P2，**现产不可达**）`hips_order` 第四站点无界 ⇒ `1u << leaf_order_` 移位 UB；orchestrator 同字段整段静默跳过 SIP
- **站点**：①`lib/healpix_db/healpix_browser_qt/core/hips_browser_backend.cpp:157`/`:168` `order_ = std::atoi(...)` → `:162`/`:169` `leaf_order_ = order_ + kTileShift` → `:200`/`:216` `uint32_t nside = uint32_t(1) << (uint32_t)leaf_order_` ⇒ **`order>=23` 即移位 UB，正是 `aio_hips_reader.cpp:50-54` 注释列为「已修危害」的那一类**。②`lib/orchestrator/cpp/src/orchestrator.cpp:4452-4457` `atoi(A_ORDER)` 越界时**整段静默跳过 SIP**（与 `hp_drizzle_api.cpp:461` 的 `return -10` **同字段不同命**）；`:3982`/`:4949` → `:3985` `1u << (hips_order+9)` 未夹；`:1525` 又有 `if (idx<36)` 守卫 ⇒ **同文件第三种写法**。
- **可达性判据（这条判法我要留给后续轴复用）**：两目录**都不在构建图**（根图 `add_subdirectory` 仅 `CMakeLists.txt:172/177/184/191/199/208/694/701`；`grep -rn healpix_browser --include=CMakeLists.txt` 只命中其自身与 `run/` 影子树；`orchestrator.cpp` 零 CMakeLists 命中）⇒ **判「补注册即红」，不判现产可达**。
- **related** **`M9-F-1`**（注册面）、`V10-N-02`（同字段另一站点）、`P0-1`、`E4`（不可达判据写法）

### V10-N-09（P3）CLI/环境变量面裸解析：同 TU 已有带 `endptr` 的写法未推广
- `cli/commands.cpp:204`/`:1081`/`:1286`/`:1672` `strtol(sleep_ms, nullptr, 10)` 无 endptr/无界/可负，而 **`:292` 同文件已用 `strtoul(tmo,&end,10)`** ⇒ 同 TU 两种口径；`aio_log.cpp:22`、`dpsf_log.cpp:22`、`sdet_log.cpp:22` 的 `atoi(getenv(...))` 非数字/负 ⇒ 静默 0 级（**日志静默**）；`cli/monitor.h:150-167`、`memory_report.h:306`/`:540` 的 sscanf 有 `==1`/`==N` 判 ⇒ **V10 不立案（我记为正样本）**。
- **一条"不建议改硬失败"的诚实判断（我特别保留）**：`lib/phase2/src/sampler.cpp:127-132` `atof` 后有 `if (vf>0.0 && vf<=1.0)` 挡，越界静默保留 0 ⇒ `:551` `if (pf>0.0)` 走不进 ⇒ kcorr 留 `kControlCorrDefault`（`:143`）并在 `:839-840` 乘进 `cvar`（控制域方差输入）⇒ 属「**值域有挡、失败无诊断**」档；因「取默认」在本仓是既有允许口径（`:551-554` 注释「scale 未知：300 档保守」）⇒ **只建议加诊断，不建议改硬失败**。
- **related** `V5-N-01`（execute-only 绑定合同缝）、`C-20` 第③条、`A-15`/`A-35`

## V10 收尾三节（摘要，全文在 `_verify/V10.md`）
- **§2 同仓正对照 11 条**：`parse_bounded_int`、`hp_drizzle_api` 两边、`classifySipKey`、`aio_xisf`/`aio_fits` 尺寸与截断、`valid_product_params`、drizzle module 逐字段 `isfinite`、`gaia_cfg_check_params`、hips manifest 三重校验、`fits_core.c` 严格面、`tree_count<16` 硬上界、`artifact.cpp` try/catch 后 `return false` 的 fail-closed 反例对照。⇒ **本轴的判据不是"该仓不校验"，而是"同一仓已有正确口径却没推广"**。
- **§4 测试面**：有锁 3 处；**半锁**（只锁已修那条边）`p0_sip_order_guard_test` T1–T7 + `mini_sip1000` + `test_p0_drizzle_sip_wiring` **全走 frame-KV**；**无锁 6 类**（N-01/03/04/05/06/07 所列）。
- **§5 需运行期未判**：崩溃是否必现（分配与 `bad_alloc` 捕获面）、`hp_drizzle_fits_to_ahpx` 是否在 Fatduck 真机流经（**仓内生产零调用者**，仅 `tests/orchestrator/test_dll_loader.c` 按符号名探测）、`mmap_size` 与自报偏移的实际关系（属 `M9-H-3` 判定域）、`/proc` 行是否可能非数字。
