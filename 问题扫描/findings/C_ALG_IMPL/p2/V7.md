# V7 · 交付数值静默变化（分片 1：N-01..N-03，时点 HEAD `23ffd698`+脏树，锚点已终验）

> 免重报说明：V7 明确记录其「头号候选」已被隔壁 `8c977118`（修 drizzle 线程数值漂移）消除并带双向锁 `p1drz_taskset_invariance` ⇒ 不另立条。此判断我核过与 `V11-N-01` 不同面（那条是 ctypes 镜像，非线程数值）。

### V7-N-03（P2，**机制级**，本卷最值一条）typed-artifact 消费面「缺键 ⇒ 就地默认」全仓规模：兜底把**上游显式拒绝的非法值**制度化
- **ID** V7-N-03 ｜ **类别** C_ALG_IMPL ｜ **优先级** **P2**（覆盖面大，建议并 `C-20`）｜ **状态** OPEN
- **四连（p2 write 读上游产物）**：`target_order` 缺→**0** ⇒ `nside=2^9` **全天塌缩**且**通过 0..20 值域门**（`module_adapters.cpp:4696-4723`）；`uncertainty_available` 缺→**false** ⇒ variance/ivar 子产品**静默停发**（`:4709` + `:4625` 区 flags）；**`weight_mode` 缺→0** ⇒ properties 写 `ASTROCS_WEIGHT_MODE=0`，而 **0 恰是上游 integrate 显式拒绝值**（`:4710`/`:4858`）⇒ **兜底路径把"非法声明"变成合法缺省**；`tile_leaf_span` 缺→**编译期常数** ⇒ bin 窗口错位读（`:4708`/`:4083` 区）。
- **同键双口径**：`target_order` 在 p2 view 缺→**0**（`:3524`）而 upm-fit 缺→**−1**（`:3810`）⇒ 同一物理键两处兜底值不同。另有 photometry 行 `x/y`→0.0、`id`→空串（`:2532`/`:2549`）、noise `σ_sky`→0.0（`:2694`）、`p3n_wcs_from_json` 十键→0.0（`:5436` 区）。
- **触发面（如实限定）**：同一次 run 的生产链**恒写全键** ⇒ 现网不触发；触发条件是**旧版产物复用 / 手工指定 `out_dir` / 跨版本输入**（与 `V3-N-03` 的 `control_grid_per_tile` 缺→8 同族，本条是**外推到全消费面**的新档）。
- **建议处置（写成一条可执行规则）**：①**消费面必填键清单 = 写面清单**，缺任一即 `DATA` 拒 + 要求 schema 位；②**兜底默认值禁止与"显式拒绝值"重合**（`weight_mode=0`、`target_order=0`、`σ_sky=0.0` 全部违规——0 在物理上常是合法值，正是它最坏的地方）；③同一键在全仓只允许一个缺省（禁 `0` 与 `-1` 并存）。并入建议条款 **C-20**。
- **related** **V3-N-03**（grid→8 单点已挂）、`V1-N-02`（同属跨版本可判别性）、`M3-A-002`/`A-02`（`weight_mode` 语义即 §6.3 红线）、`B2-A17`、簇 1 第三机制

### V7-N-01（P2）cosmetic 参数面**两通道值域几乎不相交**：同一 config 在 IR 生产通道与 DLL 通道交付**不同像素**，且 IR 侧全静默兜底
- **ID** V7-N-01 ｜ **类别** C_ALG_IMPL ｜ **优先级** **P2** ｜ **状态** OPEN
- **IR 生产通道实况**（`module_adapters.cpp:1140-1158`、`:1524-1535`，节点 validate 只查 `cosmetic` 是 object（`:4812-4813`）、CLI 只查顶层键 `parser.cpp:306-322`）：`method` **任何字符串→MEDIAN**（string→dflt），`true`→1、`1.9`→1；`hot/cold_sigma` 缺键/错型→5.0，而 **`1e999`→inf→过 kernel 的 `hot_sigma>0` 阈值门→整帧零修复且无警告**（`cosmetic_corrector.cpp:242/247`）；`max_structure_size:0`→**全结构清除**（`:109`）；**`enabled:"false"` 按真值执行 ⇒ 开关反向**（`:1524`）。
- **对照 DLL 通道同键**：`method` 仅收 `{median,bilinear}`、sigma 必填且 finite、`mss>0`、缺键报 101（`cosmetic module_entry.cpp:537-585`）⇒ **两通道接受值域几乎不相交**，同一份 config 走两条路会得到不同像素，而落点是 `cleaned_*.fits` 的 `hot_fixed`/`cold_fixed`。
- **判别位/测试**：manifest **不记生效的 method/sigma/mss**（无判别位）；**IR 分叉零覆盖**（`p1001:290` 只测数值 5.0；`1e999` 负例只测 DLL，`cosmetic_integration_test.cpp:685-692`）。
- **建议处置**：IR 侧对齐 DLL 的 `cfg_fill` 严格度（同严），stage 记录生效参数；**与 `M3-G-001`（nullptr）修复合批**——否则一修 nullptr 就"激活"这条静默路径。
- **related** `V2-N-03`(D-3 同款 `p1_int` 静默强制)、**`M3-G-001`（在账，须合批）**、`C-20`、簇 1 第五机制

### V7-N-02（P2）SIP 缺键→36 个系数全零但 `present=true` ⇒ 交付**声明"畸变已修"而实际未修**；判别位记声明值不记生效值
- **ID** V7-N-02 ｜ **类别** C_ALG_IMPL ｜ **优先级** **P2** ｜ **状态** OPEN
- **链**：`config.wcs.sip={order:2,ap_order:0}` 不带数组 ⇒ `p1_parse_sip`「允许缺省（全零）」（`:1858`）置 `present=true`、**36 个 0** 落 `p1_wcs.json`（`:2416` 区）⇒ drizzle 下发 `A_ORDER=2` + 全零系数（`:2990` 区）⇒ 引擎按"有 SIP"跑**恒等修正**。`sip` 键存在但非 object ⇒ 整段静默丢弃（`:1840`）、`CTYPE` 去掉 `-SIP` 且**无错误**。
- **要害**：`p1_stack.sip_present`/`sip_order` 与帧头 `CTYPE` 都是**判别位**，但它们记的是**声明值而非生效值** ⇒ 判别位在此**反向误导**（读起来像"已校正"）。测试无该分叉（现有测试全带完整数组，`p1001:1627`/`:1740`）。
- **建议处置**：`order>0` 而缺任一系数数组 ⇒ `DATA` 拒；`sip` 非 object ⇒ 拒；`sip_present` 改为「实际载入非零系数」而非「键存在」。
- **related** `B2-A17`、`V6-N-03`（同属"声明位与实况脱钩"）、`C-20`、`V7-N-01`（同为缺键兜底族）
