# 定稿 B_STD_MISMATCH / P2 — M8a（L25+L27）

> 合并代理 M8a｜来源 L25-007（**复验后缩窄定稿**）｜纯只读复验。本条原报的两个核心断言在当前树**均不成立**（并发修复与转述有误所致），只保留结构面残留；原报 P1 → 本代理定 **P2**。
> **0 命中判据三级复核声明（前台新令）**：本文件所有"0 命中/不存在"判据均经 ①整句 grep ②放宽字符集与大小写关键词 grep ③定点 read 三级复核；放宽级结果记于 `_merge/M8a.md §12`。未据任何"报告称未命中"撤证据。

---

## M8a-B-001 SCI 把 RCR 方法的语义权威锚定为「官方 rcr 2.4.7 软件参考」，引用未落实到推导位

- 类别: B_STD_MISMATCH
- 定稿优先级: **P2**（原报 P1。降级理由：①原报的两个硬事实经三级复核**都不成立**（见"四态判定"），已无相互矛盾的版本口径；②残留为引用形式与可复现性结构问题，不属"成批一致性失效"；③现产 oracle 逐元素对齐有测试锚（`lib/phase2/tests/synthetic_gate.cpp:2721` 明示由 `rcr_oracle_compare.py` 覆盖）。
- 位置（本代理复算口径：grep "2\.4\.7" 分域命中 = **13 命中 / 11 文件**）: `docs/science/REJECTION.md::§9a 统计假设行`（130）与 `::§14 Primary literature 第 4 项`（141）、`docs/algorithms/PHASE2_REJECTION.md`（103、308）、`docs/DOCUMENT_INDEX.yaml:120`、`lib/phase2/include/astro/phase2/rejection.h::注释 Oracle 段`（27）、`lib/phase2/src/rejection.cpp::来源注`（100）、`lib/phase2/tests/synthetic_gate.cpp`（2721）、`lib/phase2/tools/rcr_oracle_compare.py`（4、205）、`lib/phase2/tools/rejection_oracle_compare.py`（278）、`lib/phase2/memory.md`（102）、`tools/assemble_v17_review_pkg.py::文献映射`（171）
- 证据摘录:
  > REJECTION.md:141 「4. RCR：官方 RCR 2.4.7 软件参考（项目 oracle 锚定 official rcr 2.4.7，见 tools/assemble_v17_review_pkg.py 文献映射），**非论文引用**。」
  > REJECTION.md:130 「…；RCR=reject-clean-refine 迭代（**官方 RCR 2.4.7 oracle 锚定**）。」——同段其余方法（ESD/winsorization）都给到文章级/书籍级文献（:138/:139），唯独 RCR 给的是软件版本
  > rejection.h:25-27 「Oracle：Astropy sigma_clip(median+mad_std)、NIST ESD、AveragedSigma（公式定义；IRAF exact = NOT_CLAIMED）、Siril 1.4.3（GPL ORACLE ONLY）、RCR 2.4.7 **官方固定版本**（ORACLE ONLY）」
  > rcr_oracle_compare.py:14 `from rcr import RCR, SS_MEDIAN_DL`（无守卫 import）；:205 "python_pkg" = "rcr 2.4.7"；:4 注「nickk124/robust-outlier-rejection commit a8a29a6」
- 权威依据: 宪章 §19 尾注 :674 「详细科学文档必须把引用落实到具体 SCI claim 和**推导位置**，不能只在项目总纲末尾罗列论文」；§13.1（独立 Oracle 须可复验）；§12.3-3；§1.3（禁临时/过渡状态）
- 四态判定（关键：原报两项已不成立）: **原报断言 1（头注 2.4.1 与 SCI/README 2.4.7 互斥）= 已修复** —— 当前 `rejection.h:27`、`rejection.cpp:100` 均写 2.4.7，`docs/science/REJECTION.md` 亦 2.4.7，全仓 grep "2\.4\.1" 于 `lib` 与 `docs` = **0 命中**（放宽 `rcr.{0,12}2\.4\.1` 亦 0）；**原报断言 2（"RCR oracle 实为 astropy.sigma_clipped_stats 冒名"）= 不成立** —— `lib/phase2/tools/rcr_oracle_compare.py:14` 直接从 `rcr` 包 import，`sigma_clipped_stats` 只出现在 `lib/photometric_calib/cpp/test/gate4_dr3sp_gaiaxpy/gate1_psf_final_test.py:253` 与 `gate2_psf_oracle.py:164,168`（另一方法学，非 RCR oracle）。故本条按残留结构面定稿，**原报的数字与结论一律不继承**。
- 问题说明: 该 SCI 的 RCR 行不给公式/推导位，而把语义权威写成"某外部包某版本"：①方法学内容绑定在第三方实现的版本号上，版本一换语义口径跟着换（宪章 §1.3、§16.1 版本唯一输入）；②该包在仓内登记面为零（`dependency-lock.json` 的 `test_only_oracles` 只有 astropy/numpy/pytest/astrometry.net，见 M8a-C-009），所谓"官方固定版本"只有注释里的包版本 + 上游 commit 号，无哈希无许可，外部审核无法复现；③同一版本串按上述口径散在 **13 处 / 11 文件**（SCI 2、ALG 2、索引 1、头注 1、实现 1、测试 1、oracle 脚本 3、模块 memory 1、装配脚本 1），全靠人工同步、零断言（另立 M8a-F-001）。
- 影响: RCR 方法在 L1 层不可脱离外部包版本被理解/复现；包升级或换实现时语义漂移无锚。
- 建议处置: ①`docs/science/REJECTION.md` §14 第 4 项改写为「RCR 语义 = 本 SCI §9a 所列迭代规则（推导位：`rejection.cpp` 的 RCR 分支 + `docs/algorithms/PHASE2_REJECTION.md` 伪码段）；外部包 rcr 仅作 ORACLE 参考，非语义权威」；②该包写入 lock 的 `test_only_oracles`（版本 + 上游 commit + 许可 + 哈希策略）；③版本串改单一来源（脚本与文档从 lock 读，禁字面量）。
- 置信度: 高（残留面逐处复算；两原报断言的失效证据亦为三级复核）
- 来源: L25-007（M8a 复验后缩窄并降级 P1→P2）
- related: M8a-C-009（同一包的登记面）、M8a-F-001（修复未固化）、L07（Rejection 科学域主落）、M8a-G-001（同族"外部权威面零登记"的 P0 后果）
