# M3 定稿 · findings · I_DOC_HYGIENE · P2（来源切片 L05 + L07）
> 合并代理 M3｜来源 L05 + L07｜逐条重读原文复核；处置表见 `../../_merge/M3.md`。

### M3-I-003 `lib/photometric_calib/docs/algorithm.md` 以「状态：已实现」呈现，且其两处核心事实被 ALG 判为失实
- 类别: I_DOC_HYGIENE ｜ 优先级: P2 ｜ 来源: L07-024
- 位置: `lib/photometric_calib/docs/algorithm.md::文档头`（复核时点 :4、:6）、`::§1.1 目标`（:14）、`::§3 网格`（:107）、`::§9 参数表`（:346）；对照 `docs/algorithms/PHOTOMETRIC_FIT.md::§13.2`（:143-144、:161-163）、`lib/photometric_calib/cpp/src/spectrum_integrator.cpp::F_syn 网格步长`（:246-247）
- 证据摘录（复核时点重读）:
  > > 文档版本: 1.0 / > 状态: 已实现（algorithm.md:4/:6）
  > 对单帧天文图像进行全局流量校准，消除空间缓变梯度（残留渐晕、月光、光害、大气消光、大气辉光）…（algorithm.md:14）
  > 波长步长 0.1nm …（algorithm.md:107 表格；:346 参数表同）
  > - F_syn 网格为**1.0nm**（spectrum_integrator.cpp:247-255），旧 lib/photometric_calib/docs/algorithm.md:107/:346 "0.1nm" 失实。（ALG-PHOT:143-144）
  > const double wl_step = 1.0;  // 1 nm 步长（spectrum_integrator.cpp:247）
  > - **DISP-PHOT-002**: 旧 README v2.0 与 docs/algorithm.md 大面积失实…README r1 重写修正，legacy docs 仅存档。（ALG-PHOT:161-163）
- 权威依据: 宪章 §12.3-10（活动文档不得以历史状态冒充当前；历史只准 `docs/archive/`）、§12.3-1
- 问题说明: ALG 已把该文档判为「大面积失实、仅存档」（DISP-PHOT-002）并在 :143-144 逐条点名 0.1nm，但文件仍在模块目录 `lib/photometric_calib/docs/` 下以 `状态: 已实现` 呈现，且其 §1.1 目标（消除空间缓变梯度）描述的是一张**平面/曲面增益校正**产品，与现行 SCI-PHOT/ALG-PHOT 的「全局标量 scale + QA 输出」范围不同（现行实现 `out_pixels[i] = pixels[i]*scale` 是单一标量乘算，见 pc_api.cpp:1069）。任何按目录路由读到该文档的人（AGENTS.md 第 4 条要求读模块文档）会把它当作现行规范。
- 影响: 测光模块存在两份互相否定的设计文本，其中失实的一份标着「已实现」。
- 建议处置: 移入 `docs/archive/` 或加 `ARCHIVED_NON_NORMATIVE` 头（同 `AstroCS_ENGINEERING_CONSTRAINTS.md` 做法），并把 :107/:346 的 0.1nm 与 §1.1 梯度目标改为指向 ALG-PHOT 的订正条目。
- 置信度: 高 ｜ related: M3-I-001、M3-C-012、M3-C-013

---

### M3-I-004 p1phot 测试套件存在两份同名异体副本，未注册的一份自称「共址于 lib/phase1/tests/p1phot」
- 类别: I_DOC_HYGIENE ｜ 优先级: P2 ｜ 来源: L07-025
- 位置: `lib/phase1/tests/p1phot/{p1phot_fixtures.hpp, p1phot_test_main.hpp, p1phot_oracle.hpp, p1phot_tests_core.cpp}`（该目录 **无** CMakeLists.txt，故不参与任何构建）；其 `p1phot_fixtures.hpp::头注`（复核时点 :12-13）；对照 `lib/photometric_calib/tests/p1phot/*`（14 个文件，含 CMakeLists.txt）、`tests/unit/CMakeLists.txt:::511`（`add_subdirectory(../../lib/photometric_calib/tests/p1phot …)`）、`lib/photometric_calib/tests/p1phot/p1phot_tests_core.cpp:::3-5`
- 证据摘录（复核时点重读）:
  > // P1-PHOT 测试夹具（**共址于 lib/phase1/tests/p1phot**，与 lib/photometric_calib/cpp/src 源码同树；…）（lib/phase1/tests/p1phot/p1phot_fixtures.hpp:12-13）
  > add_subdirectory(../../lib/photometric_calib/tests/p1phot ${CMAKE_CURRENT_BINARY_DIR}/p1phot)（tests/unit/CMakeLists.txt:511）
  > // 行数对比（复核时点实测）：孤立副本 265/134/323/417 行 vs 注册副本 252/462/176/411 行（同名四文件全部不同）
- 权威依据: 宪章 §13.2（测试与被验实现同仓共址）、§12.3-2（同一目标不得两套有效定义）、§12.3-10
- 问题说明: 同名四文件在两个目录各存一份且内容已分叉，其中 `lib/phase1/tests/p1phot` 一份没有构建接入（无 CMakeLists）却在自身头注里声称「共址于 lib/phase1/tests/p1phot」——即把**孤立副本**当作权威位置自证，读者据此会去改错的那一份。真正的注册套件在 `lib/photometric_calib/tests/p1phot`。此外 `docs/modules/photometric_calib.md:117` 的 p1phot 注册行号（:47）与复核时点实际（:49）漂移，属 R1 类。
- 影响: 测试面分叉（改一处不影响另一处，且未来可能有人把断言补到孤立副本上而 CI 永不执行）；文档路由误导。
- 建议处置: 删除或归档 `lib/phase1/tests/p1phot/`（先 diff 确认无独有断言，若有则并入注册套件），并在 AGENTS.md 的路由表把测光测试唯一位置标注为 `lib/photometric_calib/tests/p1phot`。与 F00-04（测光双实现/多套登记）同族，建议一并处置。
- 置信度: 高 ｜ related: M3-I-001、M3-I-003、M3-F-004、F00-04
