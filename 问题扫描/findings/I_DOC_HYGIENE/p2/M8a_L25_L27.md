# 定稿 I_DOC_HYGIENE / P2 — M8a（L25+L27）

> 合并代理 M8a｜来源 L25+L27｜纯只读复验（read/grep/glob），锚以 `path::符号` 优先。
> **规范缺口二分保留**（L27 方法，本文件不混淆）：G_GOV_GATE = 「要求存在但无门/假门」（见同域 p1/p2 的 M8a-G-00x）；本文件 = 「标准本身缺该要求 / 术语缺定义 / 叙述失真」。

---

## M8a-I-001 「每子库都要有 README」这一负责人直接要求在本仓无任何条文支撑，35/92 目录单元因此落在规范空白里

- 类别: I_DOC_HYGIENE（标准缺口，含建议新增条款）
- 定稿优先级: P2
- 位置（条文面）: `ASTROCS_PROJECT_CONSTITUTION.md §8.4`（复核 280-291）、`§12.2`（423-429）；`docs/standards/DOCUMENTATION_STANDARD.md`（全文 14 行，要素表 :5-12）；`docs/README-DOCS.md:8/:11`；`AGENTS.md::必读条目 4`；（缺口面）见下 35 单元清单
- 证据摘录:
  > 宪章 §8.4:282 「`lib/algorithms/` 中每个可独立调度的科学模块编译为独立 Windows DLL / Linux SO…每个算法模块必须有：…`README.md`、`module.yaml`、公开头文件、源码和可复用测试…」→ **义务主体只有"算法模块"**
  > 宪章 §12.2 只把「模块 README」列为 L2 层，未规定哪些目录必须有 L2 文档
  > DOCUMENTATION_STANDARD.md:8-10 「每份 module 文档：职责/非职责、public API、依赖/callers、data contract、ownership、threading、errors、config、performance、diagnostics、tests、source map。」—— 12 项要素中**没有**「构建/安装方式」「偏差登记（DISP）」「状态词」三项，也不规定文档落点（README 还是 docs/modules）
  > AGENTS.md 必读条目 4 只规定「读 README、module.yaml、公共头与共址测试」= **要求读，不要求写全**
  > 缺失面（L27 §0 穷举 + 本代理独立复算）：`cli/`（20 源、唯一用户入口全部命令面）、`include/astrocs`（22 公共头）、`contracts/`（AGENTS 称"合同 schema 唯一事实源"，22 份 schema）、`tests/` 21 目录、`cmake/`、`ci/`、`runtime/` 缺 5（artifact_store/io/monitoring/pipeline/registry）、`providers/cpu/{baseline,avx2,avx512}`、`modules/services/io`、`lib/{backend_host,core,io,phase1}`（`lib/core` 含约 5.5k 行 `module_adapters.cpp` 核心）—— 共 35/92
  - 独立复算（口径：glob `**/README.md` 剔除 规程 §0.4 免报区 + `.pytest_cache` 自动生成 + `docs/archive` + `engineering/control` + vendored cfitsio + 根 README）= **42** 份在范围，与 L27 报的 43 相差 1（是否计入根 README 自身）；**两种口径下现有 README 门实覆都是 5 份**（M8a-G-006）。
- 权威依据: 宪章 §8.4、§12.2；负责人口头要求（每子库 README：功能/用途/相关文档/接口代码）；规程 §2（纯表述问题不抬级，标准缺口如实定 P2）
- 问题说明: 这是"**标准本身缺该要求**"的一族（与 M8a-G-006/-007 的"有要求无门"相对，后者定 G_GOV_GATE P1）。宪章的 README 硬义务只覆盖"可独立调度的科学模块"，而该面 21/21 形式已满足 —— 于是负责人点名的"每子库 README"在条文层无处落脚，基建/CLI/tests/tools/cmake/ci 共约 35 个单元既无义务也无门，长期无人追责是**制度后果**而非执行懈怠。要素层同样缺三件（构建安装/偏差登记/状态词），所以即便 README 存在，也不能据标准判定其"要素齐备"。
- 影响: 负责人诉求无法验收（没有可判据）；`lib/core` 与 `cli/` 无 L2 入口文档，新人/Agent 只能靠 L3 生成物与口传。
- **建议新增规范条款**（供录入 `问题扫描/SUMMARY.md`「建议新增规范条款」清单，与 **C-07** 同批裁决）:
  > **建议条款 R-01（模块 L2 文档义务与最少要素）**：①凡 `CMakeLists.txt`/`cmake/install_layout.cmake` 中出现的**交付 target 所在目录**，以及 `runtime/`、`cli/`、`include/astrocs/`、`contracts/`、`cmake/`、`ci/`、`tools/quality/` 等承载治理职责的目录，必须有一份 `README.md`（L2），内容至少含：功能与定位、相关文档（可解析链接）、公共接口清单、构建与安装方式、测试锚、偏差登记（DISP 或"无"）、当前状态词（取 `docs/owner/RELEASE_STATUS.md` §0 阶梯）；②`DOCUMENTATION_STANDARD.md` 的 module 要素表补「构建安装 / 偏差登记 / 状态词」三项并规定落点为目录 README；③目录枚举为门的数据源（新增/删除目录未同步 README 即 FAIL），豁免须显式登记 owner + 理由；④异形文件名（如 `runtime/core/phase_lifecycle.README.md`）不合本条，须改名或被显式豁免。
- 置信度: 高（条文缺失为穷尽检索结论：`docs/standards/` 14 份标准全列，无一处要求"每子库 README"）
- 来源: L27-012
- related: M8a-G-006（该面唯一存在的门实覆 5/42）、M8a-G-008（模块 README 在分层标准里被挤出各层，是本条无落点的直接后果）、M8a-I-002

---

## M8a-I-002 门的全覆盖样本恰是最薄样本：5 份 README 缺负责人 8 要素中的 4 项且合同锚写法错误

- 类别: I_DOC_HYGIENE
- 定稿优先级: P2
- 位置: `lib/phase1/wcs/README.md`（10 行）、`lib/phase1/noise/README.md`（11）、`lib/phase1/photometry/README.md`（11）、`lib/phase1/stars/README.md`（15）、`lib/phase3_session/README.md`（15）
- 证据摘录:
  > wcs README 全文要点：「# phase1/wcs — WCS TAN (L2 模块 README) / 合同: `P1-004` / `SCI-WCS-001` (docs/contracts/INDEX.yaml) / Header: … / Source: … / Test: … / ## 职责 TAN 投影 pixel↔sky (ICRS deg)。」
  - 复算（口径：逐文件 read 全文 + 对 `docs/contracts/INDEX.yaml` 的 `- id:` 键集合 grep）：5 份均满足 `check_module_readmes.py` 的四条最小断言；但负责人 8 要素中 **②定位（Phase/调用方）、⑤构建 target、⑦偏差登记、⑧状态词** 4 项全缺（`astrocs_phase1_wcs/phot/noise/stars`、`astrocs_phase3_session` 五个真实 target 无一被提及）；`P1-003/P1-004/P1-005/P3-002..006` 是控制包任务号，在 INDEX.yaml **0 命中**，却以「合同: … (docs/contracts/INDEX.yaml)」句式写死。
- 权威依据: 宪章 §12.2、§8.4；`docs/standards/DOCUMENTATION_STANDARD.md:8-10`
- 问题说明: 该门"绿灯=合格"的错觉恰好建立在最薄的 5 份上：形式合规、内容空心、携带错误锚（机制见 M8a-G-006，ID 面见 M8a-E-001）。另记边界事实：`lib/phase1/{stars,wcs,photometry,noise}` 是桥接层，与 `lib/snr_estimator` 等生产模块并存"同一算法两套实现"的叙述（双实现互斥判定由 L13-013 主落），薄 README 完全不提，读者会误认 `lib/phase1/*` 即生产。
- 影响: 桥接层与生产层关系只能口传；后续任务据"有 README 且门绿"判定该子库已文档化。
- 建议处置: 补两行即可闭环 —— ①「现状构建 target + 迁移指向（哪个模块是生产真源）」；②「状态词」（BRIDGE/LEGACY 语义须先进 `docs/owner/RELEASE_STATUS.md` §0 词表，另见 M8a-C-008）；合同锚改指真实存在 ID 或改称"控制包任务号"。
- 置信度: 高
- 来源: L27-013
- related: M8a-G-006、M8a-I-001、L13-013（双实现主落）

---

## M8a-I-003 tools/README 与 tools/ 实体名实不符：唯一文档只述 astro_toolkit（git 批操作器），检查器资产无一索引

- 类别: I_DOC_HYGIENE
- 定稿优先级: P2
- 位置: `tools/README.md::全文 83 行`（复核 1-13、19-40、56-71、75-79、81-83）；对照 `tools/` 实构（复算见下）、`ci/checks.json`（70 项注册检查）、AGENTS.md 执行纪律
- 证据摘录:
  > README:1-3 「# AstroCS 工程工具集（astro_toolkit）/ Python 脚本 + JSON 配置驱动的批量操作工具，用于**减少子 Agent 频繁触发沙箱确认**。」
  > README:21-24/:59-64 step 类型表含 `git_status/git_add/git_commit/git_push` 四类；:75 「把 git add → commit → push → log 打包成一个 JSON，只需一次 RunCommand」；:81-83 「与 `vq-commit.ps1` 等价（都用 `-F` 读取 message 文件）…两者可并存」
  > README:26-32/:65 的 `run_orchestrator` step 以 `build/artifacts/orchestrator.exe` 为运行对象；:76 「`run_orchestrator` 自动将 `build/artifacts` 和 `C:/msys64/mingw64/bin` 加入 PATH」（原文为反斜杠 Windows 路径）
  - 复算（口径：glob `tools/**` = **218** 路径，剔除 `__pycache__` 33 后 **160**；`tools/quality/` = 97 文件；`tools/` 中文件名匹配 `(check|verify|audit)_*` 的脚本 = **63**（其中 quality 内 24）；子目录 README 仅 `tools/README.md` 与 `tools/realdata/README.md` 两份（quality/arch/doccheck/graph/monitoring/testkit/traceability/astrometry_oracle 均无）
- 权威依据: AGENTS.md 执行纪律（「SubAgent 不直接 commit」「修改后必须验证才能报告完成」）；`docs/standards/DOCUMENTATION_STANDARD.md`（目录文档）；宪章 §12.2；`DEPENDENCIES.md:83`（`msys2_mingw: FORBIDDEN`）
- 问题说明: 两处失守：①**可发现性** — CI 门禁的全部检查器（63 个 check/verify/audit 脚本 + `ci/checks.json` 70 项）没有任何入口索引，治理工具本身成为文档黑洞（本轴多条「有要求无门」结论难以被他人复核，同根在此）；②**边界声明缺失** — 唯一文档介绍的工具以「减少沙箱确认」为设计目的、一键 git add/commit/push，而 AGENTS.md 明定提交纪律与「SubAgent 不直接 commit」，README 未写任何适用边界（谁可触发、与验证前置的关系）。第三处名实冲突：README 教的 `run_orchestrator` 同时踩两个已裁定事实 — 目标二进制不在根交付图（M8a-C-001），其自动注入的 msys64/mingw64 路径又属 `DEPENDENCIES.md:83` 标 FORBIDDEN 的工具链族。
- 影响: 治理工具不可发现、可误用；文档与工具链政策、提交纪律三套口径并存。
- 建议处置: tools/README 增「检查器索引」节，由 `ci/checks.json` 与 `tools/**/(check|verify|audit)_*` 枚举生成（与门注册表同源以免二次漂移）；astro_toolkit 节补适用边界声明（仅前台 Agent 可用；SubAgent 禁用；必须验证后提交）；`run_orchestrator` step 与 mingw PATH 注入随遗留通道裁决一并处置（M8a-C-001 同批）。
- 置信度: 高（本条全部数字为 M8a 独立复算，不采用转述值；L27 原报「97 文件 / 130+ 检查器」口径已被本代理口径替换，见 _merge 复算台账）
- 来源: L27-014
- related: M8a-C-001（遗留通道）、M8a-G-004（门注册表是索引的天然数据源）、M8a-I-001

---

## M8a-I-004 healpix_core 上游出处 URL 格式损坏且 NOTICE 未保留上游版权原文（BSD-3 字面义务）

- 类别: I_DOC_HYGIENE
- 定稿优先级: P2
- 位置: `lib/common/healpix/healpix_core.cpp::来源头注`（复核 4-9）、`::neighbors 段注`（337-341、418）；`lib/common/healpix/THIRD_PARTY_NOTICE.md::全文 31 行`（复核 5-22、26-31）
- 证据摘录:
  > healpix_core.cpp:6 「// https:// github.com/astrometry/astrometry.net healpix.c 文件头许可)」— `https://` 之后含空格、且未指到文件路径/行，**非可定位链接**
  > THIRD_PARTY_NOTICE.md:9-10 「该文件为 **BSD 3-Clause** 许可 (文件头声明：Licensed under a 3-clause BSD style license - see LICENSE)」= 转述，全文**未粘贴**上游版权行原文
  > 正面样本对照：`lib/healpix_db/healpix_drizzle/nanoflann.hpp:2-9` 在源文件头**内联保留**上游 BSD 许可与署名（虽缺登记，见 M8a-G-005）；`lib/gaia_xpsd_client/README.md:17/:151-153` 有「上游来源 / 许可」两节
- 权威依据: BSD-3-Clause 条款 1（源码再分发须保留上游 copyright notice 原文）；宪章 §14.2（公共许可证须可寻址）、§12.3-9（无悬空引用）
- 问题说明: 归属登记本仓做了（31 行 NOTICE 含来源、许可名、逐条差异、外部验证依赖），但形式义务差两步：损坏的出处 URL + 未随 NOTICE 收录上游版权/许可正文。若按 `healpix_core.h:5` 的「独立实现」口径裁定不派生，则 NOTICE 与 .cpp 头注须与 .h 统一（该「独立实现 vs 迁移」出处冲突本体由 L15/L03 域主落，本条只落许可形式增量）。
- 影响: 归属可追溯性打折；外部审核按 BSD-3 字面检视时，「转述」是否满足保留义务存在解释风险。
- 建议处置: URL 修为可定位（含 tag/commit + 文件路径）；NOTICE 附上游 `LICENSE` 正文与该文件版权行原文；与 M8a-G-005 的 lock `vendored_copies` 登记同批落地。
- 置信度: 高
- 来源: L25-008
- related: M8a-G-005（登记面）、M8a-G-001（同族许可面）、M2b-G-04（NOTICE 与 :338/:418 移植声明互斥主落）、L15（出处表述冲突主落）

---

## M8a-I-005 L2 层把只存在于控制包解压区的「V7 编号标准」当现行权威：23 份 manifest 与多份 README 的条款原文仓内不可核

- 类别: I_DOC_HYGIENE（悬空规范引用族，L2 层实例；主条在他轴，本条不重复计）
- 定稿优先级: P2
- 位置（计数口径）: grep「字段遵循 11_MODULE_SOURCE_TEST_STANDARD」于 `lib` = **21 命中**（= 全部 21 份 lib manifest）；另 `modules/conformance/noop/module.yaml:3`、`modules/conformance/echo/module.yaml:3`、`noop/README.md:43`、`echo/README.md:62`；`providers/cpu/common/README.md`（15 号标准 §2 + 「C §C6」）、`packaging/README.md:37/:39`（03_TARGET §4 + `10_LINUX_CONTROL_NODE.md` §5）、`runtime/core/phase_lifecycle.README.md:13-16`（`AstroCS_ENGINEERING_CONSTRAINTS.md` A.3/A.4/A.6 + 03_TARGET §5）、`lib/orchestrator/cpp/include/spill_manager.h:4` 与同目录 `resource_monitor.h:4`、`admission_controller.h:4`（`engineering_authoritative/docs/04_RESOURCE_AWARE_ORCHESTRATOR_SPEC.md`）、`lib/core/src/plan_estimator.cpp:13/:494`（`14_RUNTIME_SCHEDULER_AND_TRACE_STANDARD.md`）
- 证据摘录:
  > module.yaml 通用行（21 份）「# 字段遵循 11_MODULE_SOURCE_TEST_STANDARD.md §4；source_symbols 为源码核对清单」
  > noop README:43 「模块元数据合同：`contracts/config/module_dll_contract.schema.json`（ARC-001 冻结）与 module.yaml 字段规范（`11_MODULE_SOURCE_TEST_STANDARD.md` §4）」
  > packaging README:37 「Windows 正式安装树（…，**03_TARGET §4**）由…」**；phase_lifecycle.README.md:13 「状态语义见…`AstroCS_ENGINEERING_CONSTRAINTS.md` A.3/A.4/A.6」**（该文件根位但状态 ARCHIVED_NON_NORMATIVE）
  - 存在性复算（口径：glob `**/<文件名>` 全仓）：`11_MODULE_SOURCE_TEST_STANDARD.md`、`12_DLL_ABI_AND_LOADER_STANDARD.md`、`15_CPU_PROVIDER_AND_RESOURCE_STANDARD.md`、`03_TARGET_PRODUCT_AND_ARCHITECTURE.md`、`10_LINUX_CONTROL_NODE.md` **只存在于** `工程控制/AstroCS_V7_MODULAR_REFOUNDATION_CONTROL_20260902_FINAL3/`（规程 §0.4 免报区，仅作旁证，不计入真源）；`docs/standards/` 实树 = **14 份命名制标准**（API / C_ABI / COMMENT / CONCURRENCY / ERROR_HANDLING / IO / LOGGING_DIAGNOSTICS / NUMERIC / CODE / RELEASE / TEST / DOCUMENTATION / STANDARDS_REGISTRY / BENCHMARK），**无任何编号制文件**
- 权威依据: 宪章 §1.1 第 8 层（历史文档、旧报告和旧控制包仅作线索）、§12.3-9；AGENTS.md（ENGINEERING_CONSTRAINTS = ARCHIVED_NON_NORMATIVE）
- 问题说明: 模块 manifest 的**字段值域本身**（`module_status`、`entrypoint`、`threading_model`、`source_symbols`）挂在活动文档树里不存在的规范上 — 想查「entrypoint 该填什么」，仓内无原文可读（只能进免报区翻控制包）。这是「编号制 V7 标准」与「命名制现行标准」双轨并存的引用面残留，与 M8a-I-006 互为因果（值域无仓内权威 → 同词两义无人能裁）。L14-002 已登记注释/部分 README 面（主条），本条**只补 L2 层未列实例**，不与其重复计数。
- 影响: 字段规范/加载语义/资源标准的条款原文不可仓内核验；按 AGENTS.md 路由读 manifest 的 Agent 无法判断其值域是否被遵守。
- 建议处置: 批量替换为 `docs/standards/*.md` + 宪章条款号；把 module.yaml 字段规范迁入仓内正式标准并订正全部 21+2 份 manifest；控制包解压区的编号文档在引用处一律标 ARCHIVED 或不引用。
- 置信度: 高（引用数与存在性均为 M8a 独立复算）
- 来源: L27-011（L2 层实例；主条 L14-002）
- related: **L14-002（主条，本条不重复登记注释/README 面）**、M8a-I-006（值域权威在仓外 → 术语缺口）、M8a-C-008（同文件的 15 号标准引用）

---

## M8a-I-006 「entrypoint」同词至少两义，仓内无权威定义 — 登记为术语缺口，不按猜测定档

- 类别: I_DOC_HYGIENE（术语缺口，交负责人/GOV 裁决）
- 定稿优先级: P2（裁决前不升档：它是 M8a-C-002 与 M8a-G-006 建议门的**前置条件**，本身不是独立错误）
- 位置（权威检索面，0 命中即为结论）: `docs/GLOSSARY.md`（全文 25 行，自称「唯一术语权威」，grep `entrypoint` **0 命中**）；`docs/standards/`（14 份标准 grep `entrypoint` 无定义性命中）；`ASTROCS_PROJECT_CONSTITUTION.md`（:274 §8.2 节点唯一实际 entrypoint、:288 §8.4 单一模块 entrypoint、:402 §11 trace 记录 entrypoint — **均用词而不定义 module.yaml 字段**）；`contracts/config/module_dll_contract.schema.json`（另见下）
- 证据摘录（两义并存的仓内实例）:
  > **义 A｜DLL 唯一导出入口符号**：`contracts/config/module_dll_contract.schema.json:24` `"entrypoint_abi": { "const": "astrocs_module_query_v1" }`（schema `required` 键之一）；`lib/calibration/module.yaml:21` `entrypoint: astrocs_module_query_v1`；宪章 §8.4:288「单一模块 entrypoint，不隐藏整阶段 Session」
  > **义 B｜host-registry descriptor 是否接线**：`docs/contracts/PUBLIC_API.md:345` 「`entrypoint=MISSING` — registry 无 descriptor；C ABI adapter 由…」（另 :434/:535/:861/:989 同式）；`lib/drizzle/module.yaml:21` 「`entrypoint=MISSING`：registry descriptor（`lib/core/src/module_adapters.cpp:508-525` p1_drizzle_descriptor）未接节点」，而同文件 :32-34 把它作为与 `dll_name`/`module_status` 并列的字段值
  > **同模块两义直接冲突**：`lib/calibration/README.md:163` 把**自家 yaml 字段抄反**（yaml:21 = `astrocs_module_query_v1`，README 写 MISSING 并加义 B 括注「registry 入口未接」）；`lib/snr_estimator/module.yaml` 一个文件内同时用两义（:41 `entrypoint: MISSING` + :26 注「registry 入口未接；descriptor 现状占位 ID」）
- 权威依据: 宪章 §12.1/§12.2（分层与术语一致）、§12.3-1；`docs/GLOSSARY.md` 自称唯一术语权威却无该词条
- 问题说明: 本代理按前台指令先检索权威定义，检索结论：**仓内没有任何文档定义 `module.yaml` 的 `entrypoint` 字段值域** — 唯一机器合同用的是另一个键名（`entrypoint_abi`，只锁符号名不锁该字段语义），字段规范本体（11 号标准 §4）不在活动文档树（M8a-I-005）。于是同一键在 21 份 manifest + 多份 README + `PUBLIC_API.md` 中按两义混用，后果是：即使 `astrocs_p1_drizzle.so` 已构建已安装（`cmake/install_layout.cmake:104-105`），`entrypoint: MISSING` 按义 B 为真、按义 A 为假，**任何自动比对都无法判真假**。本条不裁哪义正确。
- 影响: M8a-C-002（五源三态）与 M8a-G-006 建议门若在本条裁决前落地，必然对同一模块给出互斥结论；负责人「按 manifest 查交付状态」的诉求不可验收。
- **建议裁决项**（录入 SUMMARY 待裁决清单）: `entrypoint` 二选一 — ①唯一导出入口符号名（值域 = ABI 符号名，DLL 未建时另用 `NOT_BUILT`）；②registry descriptor 接线状态（值域 `WIRED`/`NOT_WIRED`）。第二义另起键名（建议 `registry_descriptor_wired`），并同步写入 `docs/GLOSSARY.md` 与迁入仓内的字段规范。
- 置信度: 高（两义实例与「无权威定义」均为全量检索结论）
- 来源: L27 §6 待复核第 1/3 条（本代理完成检索并闭合为术语缺口）+ L27-007 措辞前置
- related: M8a-C-002（本条是其门的前置）、M8a-I-005（值域权威在仓外）、M8a-G-006（无法判定的机制）

---

## M8a-I-007 toolchain.lock 已刷新，INVENTORY_REPORT 仍以现势口吻归因锁「九类工具全部缺失」— 修复未回写型失真

- 类别: I_DOC_HYGIENE（修复未固化：改了数据源，未回写引用它的活动文档）
- 定稿优先级: P2
- 位置: `ci/INVENTORY_REPORT.md::§1 盘点方法第 4 条`（复核 15）、`::头部`（1-8）；`ci/toolchain.lock.json`（复核 4-6、11-46、53/60、68/75、83、97、133-135）
- 证据摘录:
  > INVENTORY:15 「本机工具链（ADOPT-004 锁存）：python3 3.13.5、git 2.47.3、zstd 1.5.7 可用；**cmake / gcc / g++ / clang / clang++ / ninja / make / ccache / pytest 全部缺失（ci/toolchain.lock.json missing_tools）**」；INVENTORY:5 完成时间 2026-09-05T12:45Z、任务 V8-CI-001（验收含 `ci/validate_registry.py --strict` exit 0）
  > 当前锁逐键复核（grep `"present"`）：cmake:15 true、gcc:23 true、gxx:30 true、clang:37 true、clang++:44 true、ninja:53 true、make:60 true、python:68 true、git:75 true、pytest:97 true、**ccache:83 false**；:133-135 `"missing_tools": ["ccache"]`（1 项）；:4 `generated_utc: 2026-09-06T10:14:47Z`、:6 `git_head_sha: 52e71804…`（晚于报告一天）；另 :119 区段 `cmake_minimum_required_observed` 记 presets 3.31.0 与 CMakeLists 3.24 并存
- 权威依据: 宪章 §12.3-10（活动文档不得以历史状态冒充当前状态）、§12.3-1
- 问题说明: 原报（L25-009）说「报告与锁两把尺子、锁失真且多工具缺失」；本轮按当前树复验后**锁侧已被并发刷新**（`missing_tools` 收敛到仅 ccache），于是失真方向翻转：现在失真的是**报告**，而它把结论显式归因于锁（「…全部缺失（ci/toolchain.lock.json missing_tools）」），读者按该句去查锁会得到完全相反的事实 — 属「修复未固化」的文档面形态（数据源改了、引用未回写）。同时实跑的 `ci/verify_toolchain.py` 只比 policy ↔ lock（其 docstring 6-21），无门验证「引用锁的文档是否随锁刷新」。
- 影响: 控制节点能力被系统性低估（可能诱发重复安装，或因误判前置条件不满足而滥用 waiver）；锁作为实测快照的可信度被引用方拖累。
- 建议处置: ①刷新 INVENTORY:15 为当前锁值，并在头部加「时点快照（对应 generated_utc 日），不随锁更新」标注；②确立「外部命令/工具可用性」唯一口径 = `ci/toolchain.lock.json` + `ci/checks.json` 的 prerequisite 字段，报告类文档不再复述清单；③该面回归保护并入 M8a-G-004 建议的反向完整性门（文档→锁引用一致性），不单开薄条目；④无断言面另立 F_TEST_GAP（M8a-F-001）。
- 置信度: 高（两侧逐键复算）
- 来源: L25-009（本代理按当前树复验后改写为「修复未回写」形态；原「多工具缺失 / 09-05」结论在当前树已不成立，见 _merge 剔除与降级表）
- related: M8a-F-001（修复未固化的测试面）、M8a-G-004（无门）、F00-02（状态时效同型）
