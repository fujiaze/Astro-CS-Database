---

### M6b-I-003 文档内统计与清单陈述与实测不符（可复算的口径失真清单）

- 类别码: I_DOC_HYGIENE
- 优先级: **P2**
- 位置与逐项实测:
  1. `docs/ARCHITECTURE.md::§8 Traceability`（现 :129、:130）
     > - 科学 → 算法 → 工程映射见 `docs/TRACEABILITY.csv`(76 行，SCI-/ALG-/DATA-/ENG- 全 VERIFIED，`MUST` family `docs/TRACEABILITY_family.json` + `gen_v19_source_snapshot` 绑定实现符号/…)
     > - 模块级追溯见 `docs/modules/*.md`(13 份 L5) 与本文件 §5/附录。
     - 实测：`docs/TRACEABILITY.csv` = 68 行（表头 + **67 数据行**）；`docs/TRACEABILITY_family.json` = **0 文件**（`docs/TRACEABILITY_FAMILY.csv` 亦 0）；`docs/modules/*.md` = **23 份**（另有 `docs/modules/registry/` 26 份）；旧表 `requirement_type` **67/67 全 = science**（不存在 ALG-/DATA-/ENG- 类型行），`status` **67/67 全 = VERIFIED**（"全 VERIFIED"成立但为无差别批量值，见 M6b-C-001）。
  2. `docs/algorithms/anchors/ANCHOR_CONTRACT.md::§1 现行规模`（现 :18）
     > 现行规模：**36 文档 / 793 锚**（其中 784 为源码/文档目标锚，9 条为登记豁免）。
     - 实测：`doc_globs` 命中现存文档 **41 篇**；按同一 `ANCHOR_RE` 移植统计带文件名锚 **805 处**；登记豁免 **7 条**（非 9 条）；另有 **223 处**裸表格 `:NN` 锚不在该语法内（见 M6b-E-002）。
  3. `docs/audit/doc_classification.csv::（docs/history 行）`
     - 实测：指向 `docs/history/…` 的行 **15 行**，而 `docs/history/` 目录不存在（`glob docs/history/**` = 0）。
  4. `CHANGELOG.md::（traceability schema 条目）`（现 :40）
     > - `schemas/traceability_matrix.schema.json` + `docs/traceability/TRACEABILITY_LAYERS.csv`：
     - 实测根 `schemas/` 不存在，真实路径 `contracts/schemas/traceability_matrix.schema.json`（同错见 SPEC:20 与 `ci/checks.json:1121`，门侧归 M6b-E-005）。
  5. `memory.md::§5 开放问题 7-11`（现 :108-112）
     - 实测该节仍承载"控制包执行模式沉淀""会话交接（2026-09-09）""旧控制包作废归档"等**过程流水**，与该文件自定保留规则（:3-4「只保留：稳定目标、当前 SHA/版本、模块索引、开放问题」+ :70-76 归档声明）不完全一致；:85-86 另沿用已漂移的行锚。
  6. `docs/review/RELEASE_STATUS.md::§3`（现 :29）、`docs/TRACEABILITY.csv::status 列`：状态词与批量 VERIFIED 值（详见 M6b-C-001、M6b-I-002，此处只登记计数面）。
- 权威依据: 宪章 §12.3-1（清单一致）、§12.3-10、§12.2（L1-L3 文档须与事实一致）；`docs/standards/DOCUMENTATION_STANDARD.md::（数字须可核）`。
- 问题说明: 上述 5 项都是**一句话即可机检**的统计/清单陈述，却全部与树内实况不符；它们的共同点是"数字由人手抄自另一份文档或旧轮次快照"，而 `ci/checks.json` 内无任何"文档内统计值 vs 实测值"的门（`DOC-INDEX` 只校索引自洽、`P3-STATUS`/`UT-GLOSSARY`/`DOC-L0` 只做字符串集合比对，见 M6b-G-004/G-005）。
- 影响: 单条影响有限（不改变判定结果），但合起来使"按文档数字做规模/覆盖判断"系统性偏大或偏小；且 `docs/ARCHITECTURE.md` §8 是追溯面的读者入口，其数字错误会直接误导对追溯矩阵完备性的评估。定级 P2（说法不同但无合规后果者不外溢为 P1）。
- 建议处置: 把上述数字改为脚本生成（`tools/docs_machine_consistency.py` 已有同类能力但未接线，见 M6b-G-003）：新增 `doc_stats` 生成器，输出 67 行 / 23+26 份 / 41 篇 805 锚 / 7 豁免，并在门内比对文档中出现的统计字面量。
- 置信度: 高
- 来源: L18-004、L18-015、L18-023、L16-018（数字部分）
- related: M6b-E-005、M6b-G-003、M6b-C-001、M6b-I-001
