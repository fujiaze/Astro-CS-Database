# M6a 定稿 · I_DOC_HYGIENE × P2（来源切片 L13 + L14）

> 代理：M6a。只读取证；位置一律 path::符号。四态与移交见 问题扫描/_merge/M6a.md。

### M6a-I-004 遗留 cc_* 通道的同名分叉实现不带 legacy 声明（其文件头文案与生产版雷同、默认导出符号、含无条件 stderr 进度日志），且执行库存清单把该路径记为 production=yes，与根 CMake、README 三方互斥（来源 L13-020）

- 类别码: I_DOC_HYGIENE
- 优先级: P2
- 位置:
  - `lib/calibration/cpp/cosmetic_corrector.cpp::文件头`（复核时点 :1-7 无 legacy/禁接入/公式差异声明）与 `::修复循环`（:139-143 无条件 `std::fprintf(stderr, "[cosmetic] 开始修复: …")`）
  - `lib/calibration/cpp/cosmetic_corrector.h`（复核时点 :12-26 `CC_EXPORT` 默认可见性导出 `cc_*` 4 符号）
  - 生产版对照：`lib/calibration/src/cosmetic_corrector.cpp`（本域另有注释错文，见 M6a-D-011）
  - 构建面：根 `CMakeLists.txt`（复核时点 :376 区段只编 `lib/calibration/src/`；`cpp/cosmetic_corrector.cpp` 不在根源集）
  - 事实源：`docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv`（`lib/calibration/cpp/cosmetic_corrector.cpp` 行 production=yes）与 `lib/cosmetic/README.md::遗留通道`（:159-164 自述「未编译进 CMake 主构建…公式与 ac_* 通道不同」）
- 证据摘录:
  > （cpp/cosmetic_corrector.cpp:1-3）// cosmetic_corrector.cpp - 坏点修复模块 C++ 实现（OpenMP并行） // 功能: 中值滤波修复坏像素 + 从Dark/Bias检测热/冷像素
  > （README:159-164）遗留通道（计划迁移旧符号，P1-COS-IMPL 决定去留）：Makefile 编译 lib/calibration/cpp/cosmetic_corrector.cpp → cosmetic_corrector.dll（cc_* 4 导出…公式与 ac_* 通道不同，未编译进 CMake 主构建）
  > （PRODUCTION_EXECUTION_INVENTORY.csv 对应行）production = yes
- 权威依据: 宪章 §14.3（单一入口/禁双源冒充）、§14.1（原地演进的唯一权威实现面）、§12.2、§16.2（状态声明与实际一致）
- 问题说明: 两个可核事实：①同名文件的遗留分叉不带任何「legacy/未编译/公式不同」标记，其文件头与生产版文案雷同，还留着无条件 stderr 进度日志（在源码树里像活代码）；②`PRODUCTION_EXECUTION_INVENTORY.csv`（生产执行库存清单）把该路径标 `production=yes`，而同目录 README 与根 CMake 都说它不在主构建 —— 事实源之间三方不一致，迁移与门禁统计会被清单误导。
- 影响: 误 include/误拷语义风险（两版公式不同，`cc_*` 与 `ac_*` 结果不等价）；库存清单是 §17 门禁与迁移计划的输入，标错会高估生产面。
- 建议处置: ①遗留文件头首行加 `LEGACY / 未编译进 CMake 主构建 / 公式与 ac_* 通道不等价（指针 ALG-COS 遗留通道节）`；②按 P1-COS-IMPL 决定去留（删除或迁移），并同步 inventory 的 production 列；③inventory 生成侧加校验：production=yes 的路径必须在根源集内可解析。
- 取证口径: `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv` 按**列名 + 逐列定位**判定（表头 :1 = `category,symbol,location,classification,production_reachable,phase,thread_model,evidence,risk_note`；:79 行第 3 列 location = `lib/calibration/cpp/cosmetic_corrector.cpp`，第 4 列 classification = `production`、第 5 列 production_reachable = `yes`）；不以整行 read 文本作等值判据（防 >2000 字符单行截断伪影）。构建面反证用 grep 定位根源集（根 `CMakeLists.txt` 内 `lib/calibration/src/` 四 TU，`cpp/` 零命中）。
- 置信度: 高
- 来源: L13-020
- related: M6a-D-011（同族注释错文）、M6a-C-003（现状宣称三源对账）、M2a（calibration 域主定稿点）、M6b-G-001（无据 VERIFIED 总述归口，本条只登 inventory 一列失实实例）、M7（不可达门归口）

### M6a-I-005 横向新增：三件套与头注释把「唯一/内建/已接线/实测」类现状断言写成无锚散文，本次 38 条实核 20 条与事实源不符（本条为该族的量化定稿点，实例分列各条）
- 类别码: I_DOC_HYGIENE
- 优先级: P2（量化与措辞层；实例的实质后果已在 M6a-C-003/C-002/I-001/I-002 分条定稿，本条只落「宣称形态」的可机检面，避免与实例双计）
- 位置与统计: 本代理对 `lib/**` 与模块三件套中含「当前实现 / 已接线 / 唯一生产入口 / 唯一真实入口 / 唯一权威 / 实测 / 全 PASS / 内建 / 正式版」措辞的注释与 README 做 38 条定点对账，三源＝根 `CMakeLists.txt`、`lib/core/src/module_adapters.cpp` 节点表与 descriptor、`docs/standards/STANDARDS_REGISTRY.md` + `docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv`：相符 15 条、与事实源不符 20 条、无法在仓内判定 3 条（判定依赖 `run/**` 免报区证据）。不符 20 条中：11 条属「把目标合同态写成现状」（例 `module_adapters.cpp` 文件头委托总述、`astro_calibration.h` 的 FP64 结论句、`lib/phase1_session/README.md` 的 `status=complete`），6 条属「唯一类排他宣称被第二处在位实现打破」（例 `lib/cosmetic/README.md` 唯一生产调用点、`lib/snr_estimator/memory.md` 现状唯一生产实现、`lib/phase2/README.md:94` 唯一生产入口的行锚），3 条属「实测数字/路径已漂移」（`lib/phase2/README.md:10-11`、`lib/astro_image_io/tests/p1hips/CMakeLists.txt:37`、`tests/abi` 路径归属）。
- 证据摘录: 见 M6a-C-003 / M6a-I-001 / M6a-I-002 / M6a-D-004 / M6a-D-006 / M6a-D-008 各条的逐字引文（本条不复制，避免双计）
- 权威依据: 宪章 §16.2、§12.3-1/-8/-10、§17.2
- 问题说明: 形态结论三条：①「唯一/内建」类排他句是失实密度最高的句式（一句即可判真伪，却普遍无锚）；②「实测」被用作免锚凭证（写了「实测」二字即不再给 SHA/日期/符号，本域 7 个文件行数全部与实测宣称不符）；③不符条目集中在**跨层引用**（模块文档引用构建面/注册表面），而两侧各自演化、无对账门。
- 建议处置: ①立法式规范：ACTIVE 文档/注释中的排他与现状断言必须带 `path::符号` 或 CI/测试标识，否则降级为「目标态」并注明；②把「实测 N 行 / N/N PASS」类自述从文档模板中删除；③机器门：抽取这类句式的目标符号/路径做存在性与归属校验（与 M6a-I-001 建议处置②、M6b 的锚点合同合并实现，避免重复造门）。
- 判定口径: 「38 条」＝本域对含排他/现状措辞的注释与模块文档做的**抽样定点清单**（跨 lib/cli/runtime/providers）；本代理把其中下表 12 组写成可独立核对的三源反证并落到具体定稿编号。`相符 15 / 错位 20 / 无法判定 3` 为助手初判计数，**未逐条外化为定稿证据 ⇒ 只作线索，不得当全仓规模引用**；规模判词归 L28/前台，「行锚/无据 VERIFIED」的根因判词归 M6b-E-002 / M6b-G-001。
- 置信度: 中（抽样清单；不符项均已单条定稿）
- 来源: 本次横向对账新增（无对应叶子）
- related: M6a-C-003、M6a-I-001、M6a-I-002、M6b-E-002（锚点漂移根因定稿点）、M6b-G-001（无据 VERIFIED 定稿点）、M5b（文档-构建面对账门）
