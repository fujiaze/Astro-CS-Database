# M5a 定稿 · I_DOC_HYGIENE · p1 · 来源切片 L11
> 代理：M5a（第二层合并验证）。全部条目均在复核时点按当下树重读；位置一律 path::符号，行号仅为其时点参考。处置表/复算/剔除与降级/移交见 ../../../_merge/M5a.md。


### M5a-I-001 两份同名 ACR_EQUIVALENCE.md 给出互斥等价语义（逐位等价 vs 1e-6 容差）
- 类别: I_DOC_HYGIENE
- 建议优先级: P1
- 来源: L11-013
- 位置:
  - `docs/science/ACR_EQUIVALENCE.md`（:3 状态 FROZEN、:84-88 容差、:99-103 验证 Oracle）
  - `docs/algorithms/ACR_EQUIVALENCE.md`（:3 状态 DERIVED、:15 F2、:17 F4、:65 §9 容差来源）
- 证据摘录:
  > F2: legacy launcher: per-pixel rejection+integrate逐位等价, signal=Σw·x/Σw, support=max
  > F4: 容差: float32 max_abs ≤1e-6, float64 ≤1e-12; support/rejection exact
  - `docs/science/ACR_EQUIVALENCE.md:3`：「ID: SCI-ACR-EQUIV-001  状态: FROZEN (T109 冻结, 2026-08-23)」；`docs/algorithms/ACR_EQUIVALENCE.md:3`：「上游 SCI: SCI-ACR-EQUIV-001  状态: DERIVED (T209 冻结, 2026-08-23)」
- 权威依据: 宪章 §12.3-1/-3（文档↔算法↔实现一致、算法实现引用有效 SCI/ALG）、§1.1 权威分层（SCI > ALG）；SCI-ACR-EQUIV-001
- 问题说明: 同名文件分处 `docs/science/` 与 `docs/algorithms/`，对同一对象给出两种等价语义：SCI 全篇是「容差内等价（1e-6/1e-12 + 分块不变量）」，ALG 的冻结公式 F2 却断言 legacy launcher 与 CPU reference「**逐位等价**」，同一代码块内 F4 又回到 1e-6 容差（ALG 内部亦不自洽：F2 与 F4 互斥）。两文件除 ALG:3 的上游 ID 外无路径/章节级互引，无唯一事实源；审核方检索 `ACR_EQUIVALENCE` 会取到两份不同结论，等价门实现与测试可各自对齐不同文档（`tests/api/test_reject_integration_oracle.py` 即按「可解析/近似」路线写）。
- 影响: 休眠域的合同层文档可信度；一旦 ACR 激活，「bitwise 还是容差」取决于读者命中哪一份 → 会直接改变验收判据。属活动文档内部矛盾（宪章 §12.3-10 同类）。
- 建议处置: 明确 SCI 为容差权威，ALG 只写推导与实现映射；把 F2 改为「离散计数（support/rejection_count）exact，浮点在 SCI 容差内」；两文件互相加路径级链接，并在 `docs/DOCUMENT_INDEX.yaml` 标注唯一权威。
- 置信度: 高
- related: M5a-A-001、M5a-F-001、M5a-I-002
