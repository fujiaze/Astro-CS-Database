# M6a 定稿 · I_DOC_HYGIENE × P1（来源切片 L13 + L14；注释/文档中的「现状宣称」与事实源对账）

> 代理：M6a。只读取证；位置一律 path::符号，行号为复核时点参考值。四态与移交见 问题扫描/_merge/M6a.md。
> 移交边界：`docs/ARCHITECTURE.md`「本版实测」无 SHA 指针（F00-05）与 13 份 `docs/modules/*.md` 的行锚问题归 **M6b**；本文件只定稿 lib/** 模块三件套、docs/algorithms/PHASE3_*_IMPL 与合同字段语义面。

### M6a-I-001 合同与模块三件套把「实测 N 行」写成现状断言却整体滞后（7 个文件全部不符），phase3_proj 三件套的两条「现状」陈述更被代码直接推翻（来源 L14-001，本条为该族文档自述的 Phase2/3 侧定稿点）

- 类别码: I_DOC_HYGIENE
- 优先级: P1
- 位置与复算（本代理 read 的行数为复核时点值）:
  - `docs/… 无` → `lib/phase2/README.md`（复核时点 :10-11「coverage.cpp（239 行实测）与唯一权威签名头 coverage.h（59 行）」）— 实测 coverage.cpp **280**、coverage.h **65**
  - `docs/algorithms/PHASE3_RSMP_IMPL.md::§3 生产源图`（:51-63 自称「实测，2026-09-12」：p3_resample.h 58 行 / p3_resample.cpp 239 行 / p3_session.cpp 343 行）— 实测 **116 / 366 / 433**
  - `docs/algorithms/PHASE3_PROJ_IMPL.md::头部`（:11-12「p3_wcs.h（50 行…）+ p3_wcs.cpp（165 行），实测 2026-09-11」）— 实测 **63 / 230**
  - `lib/phase2/README.md::§2/§3 coverage 锚`（:34-42 与 :46-49 的 :59-140/:181-193/:204-214/:194-196/:218-228/:233-237 等）与 `::§2` :94「coverage.cpp:144 定义，唯一生产入口」— 实测 `p2_coverage_build` 在 :173，其余锚段与当前内容不对应
  - `lib/phase3_proj/README.md::§6 实测偏差登记`（复核时点 :93-97「PA 未接线: p3_session.cpp:160…」「projection 字段硬编码 "TAN"（p3_wcs.cpp:36）」）与 `lib/phase3_proj/memory.md::现状`（:100-102「projection 字段硬编码 "TAN"（:36/:89 (void)proj）；P3_WCS_UNSUPPORTED 枚举现无产生点（备而不用…）」）— 被现行代码推翻：`lib/phase3_session/p3_wcs.cpp::p3_wcs_validate_request`（复核时点 :59-82，非 TAN 于 :62-67 显式 `return P3_WCS_UNSUPPORTED;`、frame 于 :70-72 同，且 :37-40 的 STD-F1 注释自述「全文件唯一 +1 点」）与 `::p3_wcs_make`（:89-94 先校验后构造、:110 `out->projection = "TAN";   // 已在入口校验; descriptor 携带已校验投影(非硬编码路径)`）
- 证据摘录:
  > （lib/phase2/README.md:10-11）落位依据：coverage 生产源 lib/phase2/src/coverage.cpp（239 行实测）与唯一权威签名头 lib/phase2/include/astro/phase2/coverage.h（59 行）位于本目录
  > （PHASE3_RSMP_IMPL.md:51）## 3 生产源图（实测，2026-09-12）
  > （PHASE3_RSMP_IMPL.md:54-56）p3_resample.h 58 行 … p3_resample.cpp 239 行 … p3_session.cpp 343 行
  > （lib/phase3_proj/memory.md:100-102）projection 字段硬编码 "TAN"（:36/:89 (void)proj）；P3_WCS_UNSUPPORTED 枚举现无产生点（备而不用，SIN/ZEA/CAR/AIT 扩展 TODO）
  > （p3_wcs.cpp:110）out->projection = "TAN";   // 已在入口校验; descriptor 携带已校验投影(非硬编码路径)
- 权威依据: 宪章 §16.2（状态/自述与实际一致）、§12.3-5（追溯链与锚点一致）、§17.2（VERIFIED 须仓内可核）；`docs/standards/DOCUMENTATION_STANDARD.md`（合同文档须可定位到实现）
- 问题说明: 两层。①**「实测」自述整体失效**：7 个被测文件的实际行数全部大于文档值（+17% ~ +118%），说明这些数字是一次性快照而非可维护事实，且文档未给 SHA/commit 指针，读者无从判断陈旧度；据此锚检索的审查会读错代码段。②**两条现状陈述被反向推翻**（更严重）：phase3_proj 三件套仍写「硬编码 TAN」「P3_WCS_UNSUPPORTED 无产生点」，而代码已改为入口显式校验并在两处产生该状态码、注释还自述「非硬编码路径」⇒ 合同文档与代码相反，会引导后续任务重复整改已闭合项、或漏掉真实分支（DISP-P3PROJ-001 的「未接入生产」表述则仍成立，见 _merge §8）。
- 建议处置: ①文档删「实测 N 行」类自述，改「锚 = path::符号 + 短摘录」双要素（与 M6b 的锚点合同同方向）；②给机器门补一条断言：文档声明的锚所在行必须含所声明符号（DOC-LINE-ANCHORS 现仅验可解析/不越界）；③phase3_proj 三件套按 :59-82/:89-94/:110 现状重述，并把「未接入生产」与「已闭合」分列。
- 置信度: 高（7 项行数与两处代码状态本代理复算）
- 来源: L14-001
- related: M6b-E-002（行锚/引用锚漂移根因与规模判词的定稿点，本条为其 Phase2/3 三件套实例）、M6b-G-001（无据 VERIFIED 定稿点）、M6b（F00-05 docs/modules 面）、M5a-E-001（L11 侧实例）、M3b-E-01（L06 侧实例）、M6a-G-001（本域注释维门）

### M6a-I-002 部分修复残余：合同字段 result.coverage_ok 仍被写路径无条件置 1（真值只在 verify 路径计算），头注释/DATA §27.2 称其为「coverage 头/数据一致」的验证结论，ALG 把常量当规格写死，三处自测把常量当断言（来源 L14-008）

- 类别码: I_DOC_HYGIENE
- 优先级: P1（原报 P1；复算后：门禁后果已收窄——verify 节点上报的是真值，写路径的常量当前不外泄，故按「合同字段语义割裂 + 常量断言」定 P1）
- 位置:
  - `lib/phase3_session/p3_output.cpp::p3_output_write_atomic_ex 结果段`（复核时点 :360-379：:374 `result->coverage_ok = 1;`，随后 :376-379 调 `p3_output_verify_ex` 但只回传 `reopen_ok`，丢弃 `v.reopen_ok` 之外的真值）
  - `lib/phase3_session/p3_output.cpp::p3_output_verify_ex`（复核时点 :481-493 真算 covok、:537-538 `result->reopen_ok = (ok&&covok&&uncok&&wcsok); result->coverage_ok = covok;`）
  - `lib/phase3_session/p3_output.h::P3OutputResult`（:33 `int coverage_ok;  // 1=coverage 头/数据一致`）
  - `docs/contracts/DATA_SEMANTICS.md::§27.2`（复核时点 :1978「result.coverage_ok / reopen_ok | int 0/1 | coverage 头/数据一致；独立 reader 重开回环一致」）
  - `docs/algorithms/PHASE3_FITS_IMPL.md::§4 伪代码`（:142 写路径 `coverage_ok=1   # :284-289` vs :154 verify 路径 `coverage_ok=covok`）
  - 消费面（说明后果收窄）：`lib/phase3_session/p3_session.cpp::run 结果 JSON`（:400-411 只发 `coverage_stats`，不发 coverage_ok）、`lib/core/src/module_adapters.cpp::p3_op_writer`（:5120-5124 同上）、`::p3_op_verify`（:5198-5212 发 `vres.coverage_ok`＝真值）
  - 常量断言面：`tests/unit/p3_output_test.cpp`（:75 `CHECK(res.coverage_ok == 1);` 打在写路径）、`tests/unit/p3_assembly_test.cpp:121`、`tests/backend/test_p3_output.py:160`、`tests/backend/p3_output_probe_main.cpp:78`
- 证据摘录:
  > （p3_output.cpp:374-379）result->coverage_ok = 1; / // 独立重开读回验证 … / P3OutputStatus vst = p3_output_verify_ex(output_path, wcs, signal, coverage, variance, ivar, width, height, &v); / result->reopen_ok = (vst == P3_OUT_OK) ? v.reopen_ok : 0;
  > （p3_output.h:33）int coverage_ok;                // 1=coverage 头/数据一致
  > （PHASE3_FITS_IMPL.md:142）result.total_px = W*H; result.covered_px = #(coverage>0.5f); coverage_ok=1   # :284-289
  > （DATA_SEMANTICS.md:1978）| result.coverage_ok / reopen_ok | int 0/1 | coverage 头/数据一致；独立 reader 重开回环一致 |
- 权威依据: DATA-P3 §27.2（字段语义即合同）；宪章 §11（半成品禁发布、验证结论须有证据）、§16.2、§12.3-5
- 问题说明: 同一字段两条通道两套语义（写路径常量 / verify 路径真值），合同与头注释只描述真值语义，ALG 却把常量写进规格伪代码；结果是三个自测对写路径 `coverage_ok==1` 的断言恒真（无判别力，等价于无断言）。残余风险：任何新消费者按头注释取 write 结果的 coverage_ok 即得到无条件 PASS；`lib/phase3_fits/memory.md:57/:121` 仍把它当验收项列记。
- 建议处置: ①写路径改用 `v.coverage_ok` 回写（一次真值），或删除该字段并在 DATA/头注明「仅 verify 通道提供」；②ALG §4 与 §4 verify 段统一为 `coverage_ok=covok`；③三条自测改断言真值语义（构造一个 coverage 不一致的负例，令 covok=0），否则登记为无效断言。
- 置信度: 高
- 来源: L14-008（部分修复；修复面与残余的边界见 _merge §8）
- related: M6b-G-001（无据 VERIFIED 的根因定稿点，本条只登 coverage_ok 字段语义实例）、M6a-I-001、M4（L08/L09 域 verify 面）、M6a-G-001

### M6a-I-003 活动 README/模块状态面的「N/N PASS」与「全 PASS」宣称无仓内可核锚；本次复核另发现被指的测试脚本在 CI 面根本收集不到用例（来源 L14-014，含新证据）

- 类别码: I_DOC_HYGIENE
- 优先级: P1（原报 P2；新证据把该项从「快照数字陈旧」升级为「状态宣称无执行落点」，按敢升原则升 P1）
- 位置:
  - `runtime/module_loader/README.md::文件表/运行测试`（复核时点 :12「tests/abi/test_secure_loader.py | 全部正/负场景编排(36 checks)」、:39「python3 tests/abi/test_secure_loader.py   # 退出码 0 = 36/36 PASS」）
  - `tests/abi/test_secure_loader.py`（复核时点 :37/:49/:495-496 `CHECKS_TOTAL` 运行时自增、`FAILURES` 非空即 exit 1；全文件 grep `unittest\|TestCase\|def test_` **零命中**，仅 `if __name__ == "__main__"` 触发）
  - `ci/checks.json::UT-ABI`（复核时点 :1642-1658 命令 = `python3 -B -m unittest discover -s tests/abi -t tests/abi`）
  - `docs/architecture/abi/ABI_003_SECURE_LOADER.md::§6 测试清单`（:83「36 checks」同数）
  - 同类：`lib/hips/memory.md`（复核时点 :109-115/:185/:194/:199/:272-281 多条「五项自检 rc … 日志在案」「adapter 9 case 全 PASS」，证据指针为 `run/local/agent_p1_hips_doc/`）；`lib/phase3_proj/memory.md`（:105-106「五门验收: run/local/agent_p3_proj_doc/selfcheck.py ALL PASS」）
- 证据摘录:
  > （README.md:39）python3 tests/abi/test_secure_loader.py   # 退出码 0 = 36/36 PASS
  > （UT-ABI 命令）["python3","-B","-m","unittest","discover","-s","tests/abi","-t","tests/abi"]
  > （lib/hips/memory.md:109）五项自检 rc（run/local/agent_p1_hips_doc/ 日志在案，2026-09-07 实测）：
  > （lib/hips/memory.md:199）adapter 9 case 全 PASS（asan/主树双树实测; …）
- 权威依据: 宪章 §17（VERIFIED/PASS 须仓内可核证据）、§16.2（状态声明与实际一致）、§12.3-12；`AGENTS.md`「所有外部命令带 timeout 并保存日志」（日志落点须可指）
- 问题说明: `README.md` 的「36 checks」是与 `check()` 调用点数相符的快照（本代理复算确为 36 处），但仓内无基线/断言锁定该数：脚本按运行时自增计数，检查数增删不会让 README 变红。②更实质的：该脚本不是 unittest 用例（无 `TestCase`/`def test_`），而唯一挂 CI 的 ABI 面门（UT-ABI）用 `unittest discover` 收集 `tests/abi` ⇒ **该 36-check 编排在 CI 里根本不会被执行**，README 的「36/36 PASS」在仓内无任何运行落点（其「文件表把 tests/abi/… 列在本目录名下」另有路径口径错位）。③`lib/hips`、`lib/phase3_proj` 状态面的 PASS 指针全部落 `run/local/**`（免报区/gitignore），真源无对应产物 ⇒ 状态宣称不可复核。
- 影响: 「已通过」类结论失去证据链（§17 门禁的输入本身不可核）；README 与真实门行为的偏离不会被任何机器检查发现。
- 建议处置: ①把 36-check 编排改造成 unittest 用例（或新增 CI 项直接 `python3 tests/abi/test_secure_loader.py`），并把期望检查数写成脚本内断言；②模块状态面的 PASS 指针统一改指 `tests/…` 目标名或 `ci/checks.json` 条目，`run/local` 只作补充；③门：ACTIVE 文档中出现的 `N/N PASS`/`全 PASS` 必须同段含仓内可解析的测试/CI 标识。
- 置信度: 高（计数、脚本结构、CI 命令三项均实测；「曾否真跑过」不在仓内可判，见 _merge §10）
- 来源: L14-014（加强）
- related: M6a-I-001、M6b-G-001（无据 VERIFIED 根因定稿点，本条为 ABI/HiPS 两处实例）、M5b（L17 门失效/放行面主定稿域）、M6b-E-004（引用对象失效定稿点）
