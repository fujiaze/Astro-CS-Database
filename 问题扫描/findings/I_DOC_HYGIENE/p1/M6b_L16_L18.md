# M6b · I_DOC_HYGIENE 定稿（来源切片：L16 / L18）

> 合并代理 M6b。行号仅为辅助定位（并发修复期，锚以 `path::符号` 为准）。`reports/**`、`evidence/**`、`run/**` 为免报区，本文件只作指针存在性旁证。

---

### M6b-I-001 现行入口叙述失实：多份 ACTIVE 文档把 ARCH-002 已废止的 orchestrator.exe / astrocs-stage2 写成现状与生产入口，而"唯一入口"不变量的机器门只是两条字面量黑名单

- 类别码: I_DOC_HYGIENE
- 优先级: **P1**
- 位置:
  - `docs/TROUBLESHOOTING.md::标题 + Symptom 表`（现 :1、:7、:21、:23、:31、:33）
  - `docs/ARCHITECTURE.md::§2 数据流 + §5 模块表 + §6`（现 :16、:18、:51、:52）
  - `docs/API_REFERENCE.md::§12 phase2 / §13 orchestrator`（现 :112、:127）
  - `docs/modules/phase2.md::概述行`（现 :6）
  - `docs/diagnostics/TROUBLESHOOTING.md::定位链第 5 步`（现 :27）
  - `docs/architecture/ARCHITECTURE.md::§1 被取代的旧入口 + §7 不变量`（现 :10、:54）
  - `tests/arch/test_single_cli.py::test_01`（现 :11-15）
  - `docs/DOCUMENT_INDEX.yaml::（上述条目登记）`（现 :56-57、:66-71）
- 证据摘录:
  > （docs/TROUBLESHOOTING.md:1）# AstroCS 排障手册 (V19)
  > （:7）| orchestrator 启动报 DLL 加载失败 | Phase1 | loader.cpp 预加载顺序… | 检查 `toolchain.ps1 check` 与 `build2/lib/*.dll` 存在 |
  > （:21）| 单模块测试编译失败 | any | 未先构建依赖静态库 | `make -C lib/phase2 clean && make -C lib/phase2`… |
  > （:23/:31）… `lib\snr_estimator\cpp\test\noise_model_science_test.exe` … `lib\phase2\build\phase2_synthetic_gate.exe` |
  > （docs/ARCHITECTURE.md:16/:18）→ Phase1 HiPS(signal/support/snr/variance/ivar)  [orchestrator.exe <stage1.json>] / [astrocs-stage2 <stage2.json>]
  > （docs/ARCHITECTURE.md:52）| orchestrator | `lib/orchestrator/cpp` | `orchestrator.exe` | Phase1 编排(DllLoader 动态加载…
  > （docs/modules/phase2.md:6）rejection → integration → HiPS 写/验证。生产入口 astrocs-stage2。
  > （docs/diagnostics/TROUBLESHOOTING.md:27）5. 定位源码符号 → 测试（docs/TRACEABILITY.csv）。
  > （ARCH-002 docs/architecture/ARCHITECTURE.md:10）- **被取代的旧入口（迁移冻结）**：`orchestrator.exe`（Phase1 编排 exe）、`astrocs-stage2`（Phase2 CLI）…
  > （ARCH-002 :54 §7 不变量（机器可验））1. 唯一生产入口=astrocs；文档内"正式运行入口"表述唯一（测试断言）。
  > （tests/arch/test_single_cli.py:15）for banned in ("正式运行入口只有 orchestrator.exe", "astrocs-stage2（Phase2）入口"):
  - 实测：`docs/TROUBLESHOOTING.md` 全文 **0 处**出现 `astrocs` CLI 命令（本代理逐行读 40 行）；`docs/ARCHITECTURE.md`:16/:18 把已废止 exe 画进主数据流；`docs/architecture/PIPELINE.md:11/:22` 同样以 `orchestrator.exe`/`astrocs-stage2.exe` 为"入口"。
- 权威依据: 宪章 §3.1/§8.1（每平台唯一用户入口 `astrocs`）、§12.3-10（不得以历史状态冒充现状）、§17.2；ARCH-002 `docs/architecture/ARCHITECTURE.md::§1 + §7-1`；`docs/contracts/PUBLIC_API.md:54-55`（LEG-004/LEG-002 已声明二者退出生产）。
- 问题说明: ARCH-002 已把 `orchestrator.exe`/`astrocs-stage2` 归为"被取代旧入口"，PUBLIC_API 亦声明 LEG-002/LEG-004 退出生产，但顶层排障手册、顶层 ARCHITECTURE、API_REFERENCE、modules/phase2、diagnostics 手册仍以它们为**现状与生产入口**叙述，且 DOCUMENT_INDEX 把这些文档登记为 ACTIVE_*（ARCHITECTURE 为 ACTIVE_INFORMATIVE、TRACEABILITY.csv 为 ACTIVE_NORMATIVE）。ARCH-002 §7-1 要求的"入口表述唯一（测试断言）"由 `tests/arch/test_single_cli.py` 承担，而该测试只比对两条固定字面量是否出现——本域实测这些文档的表述全部换了写法（`[orchestrator.exe <stage1.json>]`、"生产入口 astrocs-stage2"），门因此恒绿。
- 影响: 按 ACTIVE 排障/架构文档执行会调用不再构建的目标（`build2/`、散装 exe），既得不到结果又可能误判"环境坏了"；"哪条命令是现行入口"与状态源问题（M6b-G-002）叠加，构成"权威不可判定"的同一根因族；机器门给出"入口唯一已断言"的假覆盖。
- 建议处置: ①`docs/TROUBLESHOOTING.md` 按 `astrocs` CLI 与现行构建面重写或移入 `docs/archive/`；②`docs/ARCHITECTURE.md` §2/§5/§6 与 `docs/API_REFERENCE.md` §12/§13、`docs/modules/phase2.md`、`docs/architecture/PIPELINE.md` 的入口叙述统一改 ARCH-002 §1 口径（旧入口显式标 LEGACY）；③把 `test_single_cli.py` 的判据从字面量黑名单改为白名单（"凡出现 exe/子命令入口名，必须等于 astrocs"），并覆盖 `docs/**.md` 全域；④`docs/diagnostics/TROUBLESHOOTING.md:27` 改指矩阵。
- 置信度: 高
- 来源: F00-02（排障手册部分）、L18-003、L18-005（diagnostics 侧）、L18-004（§5/§6 入口叙述部分）
- related: M6b-G-002、M6b-E-001、M6b-G-005、M5b-G-018、M5b-G-009

---

### M6b-I-002 冻结与基线页以现在时给出不具备定位性的验收结论，并复活 owner §0 已废止的 PASS 口径

- 类别码: I_DOC_HYGIENE
- 优先级: **P1**
- 位置:
  - `docs/validation/SCIENCE_FREEZE.md::文首状态机 + PERFORMANCE_BASELINE/FINALIZATION_SELF_REVIEW 行`（现 :3-:5、:40-:48）
  - `docs/performance/BASELINE.md::基准数据 + V14 首轮结果`（现 :12、:14-21）
  - `docs/browser/HIPS_BROWSER.md::§渲染架构（V14 冻结）+ §V15`（现 :3、:11）
  - `docs/review/RELEASE_STATUS.md::§3 标题`（现 :29）
  - `docs/owner/RELEASE_STATUS.md::§0 状态词阶梯 + §3/§4`（现 :18-27、:70、:82-84）
  - 对照（免报区只作指针存在性旁证）：`docs/validation/ACCEPTANCE_GATES.md`（全仓 0 文件）、`evidence/performance/*.json`（实测 **0 文件**）、`self_review/round6`（真实位置 `reports/self_review/round6_final_verification.md`）
  - 同族实例（本文件只登记，不重复定稿）：`memory.md::§5 开放问题 2.`（现 :85-86，沿用 `module_adapters.cpp`:4257/:4282/:4309 漂移锚）
- 证据摘录:
  > （SCIENCE_FREEZE.md:3-5）> 状态机：V17 Round0-6 clean-tree 终验已完成（self_review/round6），／> ACCEPTANCE_GATES.md G1-G10 全部 PASS、known P0/P1 = 0，／> `ASTROCS_FOUNDATION_FINAL_FREEZE = PASS`。
  > （SCIENCE_FREEZE.md:45-48）FINALIZATION_SELF_REVIEW = PASS（V17 Round0-6 见 self_review/，含 clean-tree 74/74 gate + 真实 16 帧 E2E …）／ ASTROCS_FOUNDATION_FINAL_FREEZE = PASS（V17 控制包 G1-G10 全部满足）
  > （docs/performance/BASELINE.md:12）完整数值见 `evidence/performance/*.json`（V14 交付）。
  > （docs/browser/HIPS_BROWSER.md:3）## 渲染架构（V14 冻结）
  > （docs/review/RELEASE_STATUS.md:29）## 3. 冻结面（PASS，当前提交静态可核）
  > （docs/owner/RELEASE_STATUS.md §0 摘要，现 :18-27）九级状态词取代旧 PASS 口径；:82-84「已实现未接入……不得声称 IMPLEMENTED/可用」
  - 实测：`docs/validation/ACCEPTANCE_GATES.md` 不存在（glob 0），`evidence/performance/` 下 **0 个 json**，`self_review/round6` 在仓库根的相对位置不存在（实际 `reports/self_review/round6_final_verification.md`）；`docs/DOCUMENT_INDEX.yaml:535` 仍把 SCIENCE_FREEZE 登记为活动文档（无 ARCHIVED 标记，实测该文件内 `grep ARCHIVED` = 0 命中），根 `VERSION` = `0.11.0-alpha.2` 而该页停留在 V17 世代叙述。
- 权威依据: 宪章 §12.3-10、§12.3-12（结论须可追溯到当前 SHA 的机器证据）、§13.2（验证层级）、§16.2/§16.3（状态与版本口径）、§17.10；`docs/owner/RELEASE_STATUS.md::§0`（状态词唯一口径，L0 自述）。
- 问题说明: 三条同型问题：①**结论无定位性**——以现在时宣告 "G1-G10 全部 PASS / known P0/P1 = 0"，但其引用文件全仓不存在、产物指针指向空目录、目录名与实际位置差一层 `reports/`，读者无法复核；②**世代错位**——页面停在 V14/V17，仓库已在 0.11.0-alpha.2 与 DOC-CONV-001 之后，且未标 ARCHIVED，仍在 ACTIVE 索引内；③**状态词回流**——`docs/review/RELEASE_STATUS.md:29` 用 owner §0 明令废止的 PASS 口径给"冻结面"打分，与 L0 权威 NOT_READY_FOR_RELEASE 并存（同族见 M6b-G-002）。
- 影响: 这些页面是被引用最多的"已完成"证据源（L18-002/L16-014 的下游），会直接支撑错误的发布判断；known P0/P1 = 0 的宣称与 `ci/known_failures.json` 在册项并存（实测该文件仍有 2 条 failures + never_waivable 清单），属 §17.10 口径冲突。
- 建议处置: ①`docs/validation/SCIENCE_FREEZE.md` 顶部加"历史轮次快照 + 指向当前 owner 状态页"头，或整体移入 `docs/archive/validation/`（同步 DOCUMENT_INDEX）；②把 `ACCEPTANCE_GATES.md`、`self_review/round6`、`evidence/performance/*` 三处指针改为可定位相对路径或显式标"产物未入库"；③`docs/performance/BASELINE.md`/`docs/browser/HIPS_BROWSER.md` 标注世代与"当前基线见 run/resource/… 或 NOT_VERIFIED"；④`docs/review/RELEASE_STATUS.md:29` 的 "PASS" 换为 owner §0 词表。
- 置信度: 高（不存在项均实测）
- 来源: L16-014、L18-022、L18-009、L18-023（memory 流水部分）
- related: M6b-G-002、M6b-G-003、M6b-E-002、M5b-I-001、M5b-G-017
