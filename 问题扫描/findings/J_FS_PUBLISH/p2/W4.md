# W4（第二轮）· P2 一条 + 前台自记

### W4-R2-03 `p1sess` 测试套件在 TMPDIR/TEMP/TMP 三缺时回退当前目录 ⇒ 产物散落仓库根，且被 `.gitignore:56` 的 `*.fts` 全局忽略 ⇒ **dirty 门永久失明**
- **现场实存（时点 a3a343a4，非推断）**：根目录 `astrocs_p1sess_{neg,perf,props,test}/` **4 个目录**（mtime 09-12 17:16，du 16K/168K/68K/96K），内含 `light1.fts`/`master_bias.fts` 等；`git status --porcelain` 对这 4 个路径输出 **0 行（rc=0）**；`git check-ignore -v astrocs_p1sess_neg/light1.fts` 命中 `.gitignore:56` 的 `*.fts`。
- **锚**：`lib/phase1_session/tests/p1sess/p1sess_tests_units.cpp:136-142` 的 `tmp_root` 三级回退（末级为当前目录）；`perf:57`/`negative:54`/`properties:102` 同型 ×4 文件 ⇒ 违 AGENTS 目录规范「禁止散落根目录」。
- **为什么比一般的根目录散落更糟**：`V9-N-15`/`V13-N-08` 的讨论前提是 status 看得见，而本条**连 dirty 视野都进不去** ⇒ 「不可见 = 不拦截」的实测闭环（簇 1）。
- **related** `M5b_L12_L17`（CLI 默认当前目录，同机理不同主体）、`V9-N-15`、`V13-N-08`、`C-15`

## 前台自记两条（结构性）
- **新增类别目录 `J_FS_PUBLISH`**（文件系统与原子发布面）：第一轮无此域，条目全塞在 `C_ALG_IMPL`/`G_GOV_GATE` ⇒ 第二轮按域重开分类。
- **抽取器 `ID_RE` 已扩 W 前缀**（否则 `W4-*` 全部静默不入账本）——这是我**的工具第四次不认我自己的输出**（前三次：`V\d` 漏 V10+、只数 `add_test`、BOM 读写不对称）。已加自检：每轮轴收工必须比对 findings 条目数与账本行数，不等即查正则。
