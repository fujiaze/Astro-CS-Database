# W4（第二轮 · 文件系统与原子发布）· P1 两条｜时点 HEAD a3a343a4｜新分类 J_FS_PUBLISH

### W4-R2-01 `aio_publish.cpp` 删除/校验遍历的 `snprintf` 拼接一律不查截断 ⇒ 长路径下按前缀路径操作，`rmrf` 面可整删同名兄弟目录
- **锚（同文件双纪律）**：`:166-174`（Win `publish_rmrf`，`child[1024]`，`snprintf` 后直接递归）、`:184-190`（POSIX 同型）、`:221-228`（`publish_fsync_tree` 同型）、`:127`（`publish_dir_is_nonempty` 的 `pat` 同型）；`PUBLISH_PATH_MAX=1024`（`:39`/`:54`）。**而 `stage_create:299` 与 `discard:328` 对顶层 `snprintf` 有 `n>=sizeof` 检查** ⇒ 同一文件两种纪律。
- **机理**：截断结果 = path + 分隔符 + 前缀名，可命中同父目录下真实兄弟（如 `Npix_123` 截为 `Npix_12`）→ `publish_stat_exists` 判存在 → **递归 unlink+rmdir 整删**；`fsync_tree` 截断则 fsync 错对象、计数错源。
- **可达性**：触界条件 `strlen(path)+1+name >= 1024`；Linux `PATH_MAX=4096` **合法可达**（引 `M9-G-4`【E4】实测 511~4095 Linux 合法），中文目录 3B/字更易触界。生产接线：`aio_publish` 被 `astrocs.p1.hips` 事务面 stage/discard/promote 接线（口径引 `M9-C-1` 的生产可达）。**实际深路径复现＝需运行期，未判。**
- **related** `M9-G-4`（`fits_core` 512 截断塌缩，不同文件不同机理）、`M9-C-1`（同文件 ANSI 面）、**簇 6 新边界不继承老边界纪律**

### W4-R2-02 同文件符号链接/并发删除面：`publish_rmrf` 以 `stat`（随链接）判目录 ⇒ 指向树外目录的 symlink 被递归删除；Win 分支不检 `REPARSE_POINT`，junction 同型
- **锚**：`publish_stat_exists:115-120` 用 `stat()`（POSIX 随符号链接解析）→ `:157-163` `is_dir` → `:181-196` `opendir` 递归 + `PUBLISH_UNLINK`/`PUBLISH_RMDIR`；Win `:164-179`/`:229` 仅判 `FILE_ATTRIBUTE_DIRECTORY`，**`REPARSE` 全文件零出现**。
- **对照（作者知道该怎么写）**：`publish_fsync_tree` POSIX 分支 `:251` **用 `lstat`**，且 `:264-265` 注释自证「非常规项…病态输入由 promote 前 STATE/IO 兜底」——**但该兜底不存在**：`promote:351-362` 只判 stage/target 目录态、无树内容校验，`rename` 整树带走。
- **放大面（两条真实危害路径）**：stage 名完全确定（`:297` parent + 点 + base + `ASTROCS_HIPS_STAGE_BASENAME`，**无 pid、无随机段**）；`stage_create:302-307`「RAII 自愈：同名残留先确定性删除」⇒ ①两进程并发写同一 `out_dir`：**后进入者直接 `rmrf` 先进入者在途 staging**（`:311-313` 的 `EEXIST` 防线在删除之后才可达，**形同虚设**）；②父目录若被预置同名 symlink ⇒ 自愈逻辑整删外部树（CWE-59）。
- **免重报核查（W4 自证）**：findings 全域检索 `publish_rmrf`、`PUBLISH_PATH_MAX`、`STAGE_BASENAME`、符号链接、reparse = **0 命中** ⇒ 本轮全新面。
- **related** `M9-G-4`/`M9-G-5`（同族确定性命名/同提交点）、§11「失败不得留下可被误认成品」的**对偶失守**（把别人在途物删掉）
