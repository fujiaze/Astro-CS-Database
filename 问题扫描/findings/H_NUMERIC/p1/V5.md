# V5 复核新发现（P1）· 本批剪枝引入的外部输入校验缺口

## V5-N-03 畸形 `magnitudeRange` 经裸 `atof` 进入新剪枝谓词 ⇒ 整文件 100% 静默漏星；该字段在剪枝引入前完全惰性
- **ID**：V5-N-03 ｜ **类别**：H_NUMERIC（与 M9-H-1/H-2 同族：gaia 客户端外部输入边界）｜ **优先级**：**P1**（§17.10 发布阻塞）
- **位置**：`lib/gaia_xpsd_client/src/gaia_client.c:::1201-1203`（`magnitude_low/high` 由裸 `atof` 解析，仅校验"XML 逗号解析成功"）→ 谓词 `:2054`（`has_magnitude_range && magnitude_low > mag_high + 0.25` 则**整文件跳过**）→ 叶过滤 `:1556-1559`
- **证据摘录**（V5 独立推导 + 我复算同向）：剪枝触发即整文件贡献 0；若声明被写坏（`"1e999"`→`inf`、或 `low>high` 颠倒、或 `nan`），谓词在**无任何校验**的情况下成立 ⇒ 该 shard 全部参考星消失，**不报错、不改退出码、不进日志**。
  关键对照：**同一批提交 `07eb229b` 刚为 `spectrumCount` 立了 `parse_bounded_int`（首字符/尾字符/上限/溢出四重校验 + 解析失败整文件拒）**——同仓同批、同类外部字段，一处立纪律一处裸 `atof`，**自相矛盾**。
- **问题说明**：这是 `bd4bc23b` **新造的失效面**：剪枝引入前 `magnitude_low/high` 完全惰性（解析了但不参与决策），引入后成为**整文件级开关**。⇒ "以前没事"不构成"现在没事"，且**以前不存在该风险**。
- **依据条款**：宪章 §12.3 机器一致性/外部输入边界校验、§17.6 发布禁止项（静默数据损失）、AGENTS.md「科学定义=算法=接口=代码=测试」五处同源（本条五处全缺，见 V5-N-04）。
- **影响（M8a 三准则齐）**：①**断言可为假** ✓ —— 加 `isfinite` + `low<=high` + 值域窗即推翻现谓词的成立条件；②**条文在位** ✓ —— §12.3 与同批 `parse_bounded_int` 先例；③**后果达交付面** ✓ —— 参考星缺失直接改变 plate-solve 与测光的交付数值（不是性能问题）。
- **建议处置（成本一行级，V5 原案）**：`load_xpsd_file` 对 `magnitude_low/high` 加 `isfinite` + `low<=high` + 值域窗（如 `[-10,40]`）校验，**失败则不置 `has_magnitude_range`**（宁可不剪，不可漏星）⇒ 一行关死漏星面。
- **取证口径**：V5 静态推导 + 我复核谓词与解析点；**运行期未验**（畸形声明的语料在库内不存在，属可构造输入）。｜ **状态**：OPEN
- **置信度**：高（谓词与解析点逐字；等价前提清单见 V5.md C4）｜ **related**：**M9-H-2**（同批正确先例）、**M9-H-1**、V5-N-04、V5-N-01、**A-39**（同属"外部输入与门禁口径"族）
