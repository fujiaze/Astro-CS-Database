# M9 定稿 · H_NUMERIC · P0（来源切片 L24 + L26）

> 合并代理: M9（域=健壮性与交付安全 L24 + 角度单位换算链 L26）· 复核时点: 全部证据按**当前树**重新 read/grep 定位（不信叶子行号）；位置一律 path::符号 +「复核时 N」。前台/叶子转述的数字一律重算，口径随条标注。

## M9-H-1 Gaia 模块 manifest 拼接：snprintf 返回「本应写入数」推进指针 + 转义函数无容量入参 → 512 字节栈缓冲越界写，且同一越界长度被回读进产品

- 类别: H_NUMERIC（主）；次挂 C_DOC_CODE_GAP、G_GOV_GATE
- 优先级: P0
- 来源: L24-001（前台代录，本代理独立复验成立，并补两项叶子未记的加重事实）
- 位置: lib/gaia_xpsd_client/src/module_entry.c::gaia_execute（head 缓冲与推进点）、::gaia_inspect（同型第二站点）、::json_append_escaped（无容量转义器）、::gaia_cfg（catalog_dir 尺寸源）
- 证据摘录（逐字，复核时点现文）:
  > （module_entry.c:384）`static void json_append_escaped(char** w, const char* s) {` — 签名只有写指针与源串，**无剩余容量入参**；体内 `*(*w)++ = '\\'; *(*w)++ = *s;` 逐字节自增，亦不写终止符。
  > （:738-741）`char head[512];` / `char* hw = head;` / `hw += snprintf(hw, (size_t)(head + sizeof(head) - hw),` —— 用 snprintf **返回值**（=本应写入的字符数）推进 hw。
  > （:766-768）`size_t schema_len = strlen(schema);` / `memcpy(hw, schema, schema_len);` / `hw += schema_len;` —— 无界 memcpy，长度由 schema 字面量决定（CONE 支 :751-753 实测 127 字节）。
  > （:770、:774）另两处 `hw += snprintf(hw, (size_t)(head + sizeof(head) - hw), …)`；（:776-777）`json_append_escaped(&hw, c.catalog_dir);` / `*hw++ = '"';`
  > （:808）`uint64_t total = (uint64_t)(hw - head);`；（:857）`memcpy(big, head, (size_t)(hw - head));`
  > （:930-940 第二站点）`char buf[512];` → `char* w = buf + strlen(buf);` → `json_append_escaped(&w, inst->cfg.catalog_dir);` → `snprintf(w, (size_t)(buf + sizeof(buf) - w), …)` —— w 越过 buf+512 后该表达式为**负值的 size_t 巨量**（x86-64 约 1.8e19），snprintf 以近无限 max 继续格式化。
  > （:211）`char catalog_dir[1024];`；（:285-286）`json_copy_str(config_json, ASTROCS_GAIA_CFG_KEY_CATALOG_DIR, c->catalog_dir, sizeof(c->catalog_dir))` —— 仅受缓冲上限约束，配置里 1023 字符的目录串合法通过。
  > （:816-817 作者自述不变量）`* 长度仅 (hw-head) 字节, 严禁作为 total 长度源直接拷贝。`
  > 同文件正对照（同函数族已设界）：（:101-105）`if (n >= out->cap) { …ACS_DIAG_ECODE_BUFFER_TOO_SMALL… return ACS_ERR_PARAM; }`；（:144）`uint64_t c = n < dst_cap - 1 ? n : dst_cap - 1;`
  > 跨模块正对照（同名 helper 的容量安全版）：lib/drizzle/src/module_entry.cpp:724-726 `char val[512]; … if (json_copy_str(manifest, kHeaderKeys[i], val, sizeof(val)))`
- 权威依据: 宪章 §14.4（配置/CLI 输入属信任边界，须 fail-fast 返回明确状态码）、§8.6、§11（禁吞错后继续产出"看似成功"的产品）；同文件 :24-27 自述的两阶段输出合同（尺寸查询→写入→不足即 BUFFER_TOO_SMALL）**只覆盖出参 strbuf，未覆盖这两个内部栈缓冲**。
- 问题说明: 三重缺陷叠加，本代理逐段复算（口径=按 :738-777 各段字面上界求和，非实测）：①转义器逐字节无界写入；②snprintf 返回截断前的应写长度，故 :741/:770/:774 三处一旦触边即令 hw 越出数组；③算术：`'{'` + 固定前缀（含 module_id/op/布尔/整数字段）+ schema 字面量（≤127）+ spec 三字段 ≈ 300–390 字节，head 余量 120–210；catalog_dir 容 1023 字符、全需转义时膨胀为 2046 → **越界写最多约 1900 字节**（与叶子同量级，独立复算一致）。另两点为叶子未记：④**越界长度会被回读**——:808 用 (hw−head) 计 total，:857 再以同长度 `memcpy(big, head, …)`，故产品 manifest 会把栈上 head[512] 之后的相邻字节原样拷进交付 JSON（信息外泄，且 total 与实际有效内容不一致）；⑤**触发点前移到两阶段协议第一次调用**——:830-841 的"仅报尺寸"分支同样执行 :738-777 的整段拼装，即调用方按合同先问尺寸时，溢出与回读已发生。
- 影响: astrocs_catalog_gaia 为随产品发布的官方模块（Phase1/2 取参考星与光谱的必经路径），execute 与 inspect 分别是每次查询与 host 诊断的必经调用。触发条件仅为 catalog_dir 超过约 120 字符——Windows 深路径、网络盘、含空格目录即命中（本项目正式平台为 Windows amd64，宪章 §3.1）。后果为栈破坏（随机崩溃或经构造目录串操控返回地址）+ 交付 JSON 含栈内容 + inspect 侧的巨量 snprintf 写。属发布阻断级。
- 建议处置: ①`json_append_escaped` 增加剩余容量入参并返回是否溢出（越界即整次输出失败）；②所有推进点对 snprintf 返回值取 `min(返回, 剩余)` 夹紧，触边即返回 ACS_ERR_PARAM/INTERNAL 并停止产品提交（只在边界校验一次，不在 CLI/AIO/算法层重复）；③head/buf 改为「先按各段字面上界精确求和 → 一次性分配」，与同文件出参两阶段合同同口径（:101-105 的写法即现成模板）；④负向用例：catalog_dir 长度 119/120/121/1023 + 全引号反斜杠 + 控制字符，断言不越界、状态码明确、**且尺寸查询阶段也不得越界**（ASan 版 ctest 注册——注意本域该注册面本身失守，见 M9-F-1）。
- 置信度: 高（本代理对 :384/:738/:741/:766-768/:770/:774-777/:808/:857/:930-940/:211/:285-286 逐字复验）
- related: L24-007（同一 catalog_dir/配置解析面，另一事实=活性挂死）、M9-F-1（本条修复的机器验证面正是零注册的孤儿 TU）、S1-001（同族堆版：lib/hips/src/module_entry.cpp::hips_execute_write_product，属他域已登）、M2a-C-9（同一 create/execute 生命周期，字段撒谎族）
- **【E3-实测 2026-09】** 运行期证据检索=**无 sanitizer 运行记录且全树无 stack-buffer-overflow 命中**（grep -rIn 排除影子树/源码注释后 0）。构建条件**已具备**：本函数所在 module_entry.c.o 在 build/asan 以 `-fsanitize=address,undefined -fno-sanitize-recover=all` 插桩（build.ninja FLAGS 行实测），nm 命中 gaia_execute/json_append_escaped 符号且 .so 链 libasan.so.8+libubsan.so.1；在册 gaia 用例（asan 树 ctest -N #81–#91）的 fixture catalog_dir 全为短路径，长目录+转义触发面从未执行。asan 树 gaia 面唯一失败记录系 **LSan 预存泄漏**非本条越界：run/local/agent_p1cal_int/gaia_fail.log（09-09）`SUMMARY: AddressSanitizer: 294912 byte(s) leaked in 3 allocation(s)` + build/asan/Testing/Temporary/LastTestsFailed.log（09-11 19:06）90:gaia_cat_adapter/91:gaia_integration，59fdeab3 提交自证「gaia asan LSan 预存泄漏 bisect 同败」。本条**维持静态推演**；台账 _cache/E3.md §1/§4、run/审计执行层/E3/appendix_commands.md。

## M9-H-2 XPSD 自报 spectrumCount 无任何上限即用作分配步长与每星 memcpy 长度 → 堆越界读 + 数百 MB 级 OOM 放大 + 污染下游测光输入

- 类别: H_NUMERIC（主）；次挂 G_GOV_GATE（§14.4 外部文件解析边界）、B_STD_MISMATCH（Gaia DR3 光谱 343 列固定面）
- 优先级: P0
- 来源: L24-002（本代理独立复验成立；文件行数在扫描窗口内漂移，涉引文已按当前树重取，全部命中）
- 位置: lib/gaia_xpsd_client/src/gaia_client.c::load_xpsd_file（自报值读入点）、::spec_collector_init、::spec_collector_push、::search_recursive_spectrum（源指针派生）、::gaia_client_cone_search_with_spectrum（合并/导出面）
- 证据摘录（逐字，复核时点现文）:
  > （:26-31）`#define XPSD_MAGIC "XPSD0100"` / `#define WL_COUNT 343` / `#define STAR_STRIDE_SP (40 + (WL_COUNT + (WL_COUNT & 1)))`（=384）/ `#define MAX_FILES 32` / `#define MAX_STARS_RESULT 200000`
  > （:1068）`else if (strncmp(p, "spectrumCount=", 14) == 0) { xf->spectrum_count = atoi(p + 14); }` —— 来自文件内 parameters 串，**无上限、无符号、无与 WL_COUNT 的对照**。
  > （:1205-1213）`spec_collector_init(SpectrumStarCollector *sc, int capacity, int spectrum_count)` → `sc->spectra = (uint8_t *)malloc((size_t)capacity * spectrum_count);` → `sc->spectrum_count = spectrum_count;`
  > （:1249-1250）扩容同样按自报值：`realloc(sc->spectra, (size_t)new_cap * sc->spectrum_count)`
  > （:1265-1266）`if (spectrum && sc->spectrum_count > 0)` / `memcpy(sc->spectra + (size_t)sc->count * sc->spectrum_count, spectrum, sc->spectrum_count);`
  > （:1547-1551）`memcpy(&flux_min, p + 32, 4);` / `memcpy(&flux_mul, p + 36, 4);` / `spectrum = p + 40;` / `spec_collector_push(sc, …, spectrum);`
  > 全文件仅「等于 0 才兜底」四处（:2050、:2090、:2241、:2432）`if (spec_count == 0) spec_count = WL_COUNT;`
  > 同文件正对照（作者知道这类字段要夹）：（:2130-2131）合并路径 `int copy_count = sc_arr[f].spectrum_count;` / `if (copy_count > global_spec_count) copy_count = global_spec_count;`；（:2274-2275）同型夹紧；（:1061 与 :1137 区间）tree_count/file_count 均有上界
- 权威依据: 宪章 §14.4（外部文件解析属真信任边界，明列「会造成数据破坏、安全问题或 ABI 未定义行为」的输入必须校验）、§5.3/§7.3（科学路径数值语义不得静默漂移）；D.catalog（Gaia DR3 光谱波段数固定 343）；DATA_SEMANTICS:458 `| spectrum_wl | double [343]（336..1020nm step 2nm） | nm | XPSD 固定网格`
- 问题说明: 源侧每颗星可用字节由记录布局固定：`spectrum = p + 40` 起、步长 STAR_STRIDE_SP=384 → **每条星记录在光谱起点后仅 344 字节可用**（其中有效通量 343）。而 :1266 的 memcpy 长度是文件自报 spectrum_count，两者无任何约束关系。本代理独立复算放大面（口径=以常量代入）：容量 4096（:2052 `spec_collector_init(&sc_arr[i], 4096, spec_count)`）× spectrumCount=100000 → 单文件 4.096e8 字节 ≈ **410MB**（与叶子同量级）；结果导出侧 :2106 再按 `total × global_spec_count` 分配，total 上界受 MAX_STARS_RESULT=200000 约束 → 200000×100000 ≈ 2e10 字节，即 **数十 GB 级 OOM 放大**。源侧则每星越界读 `spectrum_count − 343` 字节（越过 scratch 块缓冲末尾的堆内容）。唯二处置点是「==0 才回落 WL_COUNT」，对 >343 与负值（atoi 可为负 → :1207 `if (spectrum_count > 0)` 之外，:1265 亦只判 >0，故负值不 memcpy 但 :2106 的 malloc 尺寸转 size_t 后为巨值）不设防。
- 影响: XPSD 为外部拉取并持久化的产品（GaiaDR3/、GaiaDR3SP/），属不受信输入。①堆越界读（崩溃或把相邻堆内容当光谱字节）；②数百 MB 至数十 GB 分配（OOM 放大，触 §17.6 资源门禁）；③侥幸不崩时，越界字节经 base64 交给 lib/photometric_calib（pc_api 走 gaia_client_cone_search_with_spectrum）→ **污染测光定标所用光谱数值且不报错**（宪章 §11 禁止形态）。健康数据数值不变。
- 建议处置: ①对「文件自报计数」建立统一上限策略：`1 ≤ spectrum_count ≤ WL_COUNT`，越界即整文件拒绝并返回**显式损坏状态码**（边界之后信任 typed contract，不在查询路径重复校验）；②spectrum_start/step/bits 同口径校验；③:1266 的 memcpy 长度改为 `min(spectrum_count, 记录剩余可读字节)`，源不足返回错误——同文件 :2130/:2274 已有该写法可照抄；④负向用例注入 spectrumCount=0/1/343/344/65535/-1/2147483647 的 XPSD，断言拒绝与不越界（ASan 注册 TU；同样受 M9-F-1 影响）。
- 置信度: 高
- related: M2a-C-9（同一 load_xpsd_file 的 header_len/rootPosition/child 不设界——本条是该族第四个未校验字段，主落归属见 M2a §D 移交表「XPSD 解析面 4 条由 L24 主落」）、L24-005（同文件映射区寻址不设界，M9-H-3）、L24-003（本条修复的回归面零注册，M9-F-1）
- **【E3-实测 2026-09】** 恶意 spectrumCount 语料**从未在任何 sanitizer 下喂入**：gaia_sanitize_driver.c 确有两批 ASan/UBSan 跑批记录——P13-003 sanitize_wsl_v4_full.log（2026-08-09，阶段[4] 对真实 GaiaDR3SP：`files=20 sources=219165266 / GAIA_SANITIZE_OK matched=1 flux_probe=4.515788e-17`）与 V18 G7 ALL_SANITIZE_V5_PASS（memory_V18R2-V19_operational_log_2026-08-21.md:2114 转述）——但输入为合法真实文件（spectrumCount=343），**通过≠缺陷不存在**；在册 fixture 生成器 gaia_xpsd_fixture_gen.c:343 硬编码 343、负向面仅 truncate.xpsd，本条建议处置④的 6 语料（0/1/343/344/65535/-1）树内不存在。相邻命中不可移用：run/local/bughunt_p1_batchA/asan_before_gaia_p13.log（09-08）ASan 抓的是同函数 spec_collector_push 的 **double-free**（orig 副本 /tmp/b4p1 构建），非 memcpy 越界读。本条**维持静态推演**；详见 _cache/E3.md §1/§4。
