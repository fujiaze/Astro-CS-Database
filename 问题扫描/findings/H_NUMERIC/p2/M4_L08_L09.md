# 定稿 L08_L09 — Phase2 全链域（UPM/控制采样/覆盖 + 排异/积分/写出/会话）

> 合并代理 M4｜来源 L08+L09｜验证方式：逐条重读原文（当前树即时 read/grep；行锚以符号/关键语句重定位，标注「复核时点行号」，并发修复中可能漂移）
### M4-H-01 build_geo 的 obs.control_id ∉ nodes 时无裁决面：std::map operator[] 默认插 0，观测静默归并入 control 0
- 类别: H_NUMERIC（数值正确性风险：接口未定义行为面，无现行生产触发）
- 定稿优先级: P2（来源 L08-013 原议 P2，成立；置信度中维持）
- 位置: lib/phase2/src/upm.cpp::control_by_id/frame_index 预串行段与消费点（复核时点 :423-424,:530,:550,:554,:624,:636,:864；容器声明 :71-72）；docs/algorithms/PHASE2_UPM_IMPL.md:305-309（§10 rc 表）
- 证据摘录:
  > const std::size_t f = m->frame_index[obs[i].frame_id];
  > const std::size_t ck = m->control_by_id[obs[i].control_id];
- 权威依据: 宪章 §7.3（显式错误禁止静默）；ALG-P2-UPM-IMPL §10 rc 表（未定义该输入类）
- 问题说明: frame 集由 obs 自动注册（:340-346），control 集来自 nodes/cell 合成（build_geo 允许 obs 与 nodes 不完全一致）；传入 nodes 未覆盖的 obs.control_id 时 operator[] 默认插入 0 → 观测归并入 control 0（污染其归一化、gauge 分量与 C 行），无 rc 无告警。帧面因预注册不受影响；并发 worker 中 :530 的读取键已被 :422-427 预串行段补齐（先插后读，无竞态）——风险仅在集合不一致的未定义输入路径。产品 session/stage2 的 obs/nodes 同源一致，未触发。
- 影响: 属边界未定义而非现行生产错误；第三方编排传入不一致集合时产生难以察觉的场污染。
- 建议处置: build/build_geo 入参校验「obs.control_id ⊆ controls，否则 rc=1/2」并在 §10 登记。
- 置信度: 中
- 来源: L08-013
- related: M4-C-08（同接口面）

