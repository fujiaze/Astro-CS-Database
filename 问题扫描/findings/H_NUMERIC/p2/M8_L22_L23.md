# M8 定稿（L22）— H_NUMERIC / P2

> 合并代理 M8｜来源 _cache/L23.md + _cache/L22.md｜纯只读复核（read/grep/glob），锚点在当前树按符号重新定位；行号为「复核时点」参考。

### M8-H-001 `gaia_client.c` 的 GaiaTraceCtx 注释声称「只原子更新本查询上下文」，实为多线程共享同一栈对象上的裸 ++ / += （trace-on 诊断路径的数据竞争；进程级计数器形态已被修复但竞态换形未固化）

- 类别码: H_NUMERIC（并发正确性；默认路径不改变科学输出 ⇒ P2）
- 优先级: **P2**（**降级说明**：L22 原报 P1；本代理实测进程级 `trace_seq`/`active_trace_sinks` 已被移除、trace 改为 per-query 栈对象，且仅 `ASTROCS_GAIA_TRACE=1` 诊断路径可达，默认关闭 ⇒ 按当前树改判 P2，事实本体仍成立）
- 位置: `lib/gaia_xpsd_client/src/gaia_client.c::GaiaTraceCtx`（注释块 :42-45「…只**原子**更新本查询上下文…」；结构成员为裸 int/long long，无原子类型；写点 :1313/:1360/:1362、:1442/:1489/:1491、:1582/:1628/:1630）；并行入口 4 处 `#pragma omp parallel for schedule(dynamic) num_threads(gaia_omp_team_size())`（:1860、:2055、:2219、:2359）各以 `&trace`（栈对象声明于 :1815/:2033/:2342）传入被调函数
- 证据摘录:
  > （:42-45 注释）诊断上下文按查询分配…并行线程只原子更新本查询上下文，无进程级共享状态
  > （结构成员）long long nodes; long long bytes; …   ← 无 _Atomic / 无 atomic_* 包装
  > （写点 :1360-1362）trace->nodes++; trace->bytes += ...;   ← 裸自增/累加
  > （:1860 + :1873）#pragma omp parallel for schedule(dynamic) num_threads(gaia_omp_team_size()) … collect(..., &trace)
- 权威依据: 宪章 §12.3-4、§14.4 末段（并发/资源不变量须有机器面）、§5.3（文档-实现一致）、§10.4
- 问题说明: 同一 `GaiaTraceCtx` 栈对象被多线程以指针共享，写点为非原子自增 ⇒ 注释的「原子更新」为假，开启 trace 时是 UB（计数丢失/撕裂），且 `num_threads(gaia_omp_team_size())` 在租约>1 时确实多线程。四态判定为**部分修复**：L22 原始锚（进程级 `trace_seq`、`active_trace_sinks`）在全仓 grep **0 命中** ⇒ 旧形态（跨查询全局计数器）已被改掉；但竞态并未消失，只是从"全局"换成"同一查询的栈对象"，且修复没有任何回归测试固化（唯一候选 `lib/gaia_xpsd_client/test/test_gaia_race.c` 零注册：`lib/gaia_xpsd_client/CMakeLists.txt` 对 `add_test\|add_executable\|test_gaia_race` 全项 0 命中，仅 `UT-GAIA-ZLIB` 采 py）⇒ 归 修复未固化 + F_TEST_GAP 判定式（本条内）。
- 影响: 诊断开启时 trace 统计不可信（该路径正是用来定位真机问题的）；因默认关闭且不改科学输出，定 P2；后续若被接进性能剖析验收（§10.5）则升档。
- 建议处置: ①写点改 `__atomic_fetch_add(&trace->nodes, 1, __ATOMIC_RELAXED)`（或 thread-local 聚合后定序合并，与 THREAD_BUDGET_ARCH §4:33 的自设要求一致）；②注释与实现对齐（改「按线程本地聚合，owner 合并」）；③把 `test_gaia_race.c` 注册为 ctest 目标或在 checks.json 建 discover 项并配 trace-on 用例（同 M8-G-001 三门建议）；④若废弃则移 archive 并回写引用文档。
- 置信度: 高（注释/成员/写点/并行入口全部逐字；旧锚 0 命中为实测）
- 来源: L22-003（定稿；P1→P2 改判，锚点由 trace_seq 重锚至 GaiaTraceCtx）
- related: M8-C-001（同一 ARCH §4:33 要求）、M8-F-013（构建孤儿同族）、M8-G-001（方向②）
