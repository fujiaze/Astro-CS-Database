# H_NUMERIC — 数值与稳定性

覆盖范围：精度降级、溢出、NaN/Inf 传播未定义、静默夹紧或裁切、非确定性归约、不收敛与奇点处理错误。

本目录下按优先级分档：

- `p0/`：P0（发布阻断：科学结果错误或产品不合规）
- `p1/`：P1（一致性/可验证性破坏，须尽快修）
- `p2/`：P2（表述与可维护性）

判定标准与证据要求见 `../../10_PROTOCOL.md` §1、§2、§3。定稿文件由第二层合并代理写入，命名为 `<slice码>.md`。
