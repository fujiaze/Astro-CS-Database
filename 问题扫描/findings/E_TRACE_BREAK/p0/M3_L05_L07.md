# M3 定稿 · findings · E_TRACE_BREAK · P0（来源切片 L05 + L07）
> 合并代理 M3（第二层合并验证）｜来源 L05 + L07｜逐条重读原文复核。处置表见 `../../_merge/M3.md`。
> 本条为**根因合并条目**：L05-003（校准域）与 L07-015（测光/噪声/合同域）同为「权威文本引用的测试 ID、被引文档与被验测试不存在或挂错」，实例分列，独立事实未合并丢失。

### M3-E-001 FROZEN 合同与 ALG/registry 引用的测试 ID、被引规范文档与被验测试不存在或挂错，VERIFIED 声明失去可定位证据
- 类别: E_TRACE_BREAK ｜ 建议优先级: P0 ｜ 来源: L05-003（主）+ L07-015（同根实例）
- 位置与实例（全部在复核时点重读/glob 复算）:
  1. `docs/TRACEABILITY.csv::SCI-CAL-001 行`（复核时点 :20）——`test_ids=TEST-CAL-001`、`test_files=lib/calibration/tests/test_photometry_apply.cpp`、状态 `VERIFIED`；`docs/contracts/TEST_MATRIX.md:::32` 同锚。
  2. `lib/calibration/tests/test_photometry_apply.cpp::文件头与 main`（:2-3、:123-127）。
  3. `docs/science/CALIBRATION.md::§11/§13`（:107-109、:123）——`TST-CAL-001`/`TST-CAL-INV-*`/`TST-CAL-FAIL-*`；同族见 `docs/science/PHOTOMETRY.md::§13:122`、`docs/science/NOISE_MODEL.md::§13:130`、`docs/contracts/PHASE1_API_V1.md:25-26/:31/:39`。
  4. `docs/traceability/TRACEABILITY_SPEC.md:::78`——TEST ID 正则 `^TEST-[A-Z0-9]+(-[A-Z0-9]+)*$`。
  5. `docs/algorithms/CALIBRATION_ALGORITHMS.md::F2.4`（:159）与 `::§5`（:186）、`docs/algorithms/DRIZZLE_GEOMETRY.md:173`——引用 `02_FROZEN_STAGE1_HISS_SPEC`（全仓 glob `**/02_FROZEN*` 零命中）。
  6. `lib/snr_estimator/cpp/include/snr_estimator.h:::20/:227`、`lib/snr_estimator/cpp/src/noise_model.cpp:::17`、`docs/contracts/DATA_SEMANTICS.md:::401`——引用 `docs/science/SNR_SCIENCE_DERIVATION.md`、`SNR_REDESIGN_CONTRACT.md`（glob 零命中）。
  7. `docs/modules/photometric_calib.md:::27`、`docs/science/PHOTOMETRY.md:::3`、`docs/TRACEABILITY.csv:::22`——引用 `lib/photometric_calib/flux_calibrator/**`（目录不存在，glob 零命中）。
  8. `docs/standards/MEMORY_ARCHITECTURE.md:::83`、`docs/standards/DOCUMENT_VERSIONING.md:::103`、`docs/development/TOOLING.md:::62`、`docs/TRACEABILITY.md:::24/:50/:56`、`docs/README.md:::55`、`docs/modules/writer.md:::5`——引用 `SCIENCE_ACCEPTANCE_MATRIX.md`（glob 零命中）与 `contracts/schemas/test_ids.schema.json`（不存在）。
- 证据摘录（复核时点重读）:
  > SCI-CAL-001,science,校准科学门（bias/dark/flat/cosmetic）,…,ac_generate_master_bias; ac_generate_master_dark; ac_generate_master_flat; ac_calibrate_frame,…,TEST-CAL-001,lib/calibration/tests/test_photometry_apply.cpp,-,-,PRE_RELEASE_ENGINEERING_FOUNDATION,VERIFIED,V19R2 有效（TRACEABILITY.csv:20）
  > // test_photometry_apply.cpp — P1-004 apply_photometry() + FITS Writer photscal/photappl 元数据单元测试（test_photometry_apply.cpp:2-3）
  > - 测试: `TST-CAL-001` 常量场、`TST-CAL-INV-001` 幂等归一、`TST-CAL-FAIL-001` 参数校验（新增/映射见 `docs/TRACEABILITY.csv`）（SCI-CAL:123）
  > SCI 行 test_ids 匹配 `^SCI-[A-Z0-9]+(-[A-Z0-9]+)*`；ALG 行匹配 `^ALG-`；TEST 行匹配 `^TEST-[A-Z0-9]+(-[A-Z0-9]+)*$`（TRACEABILITY_SPEC.md:78）
  > 规范依据 `02_FROZEN_STAGE1_HISS_SPEC §2.3`（engineering 控制，非…）（ALG-CAL:159）
- 权威依据: 宪章 §17.2（追踪无断链是发布门禁）、§12.3-1（活动文档的事实性陈述必须可机器/人工定位，不得虚构证据指针）、§12.3-4（每个核心合同至少一个独立测试作为验证证据）、§11；`docs/traceability/TRACEABILITY_SPEC.md:78`
- 问题说明: ① **SCI-CAL 行锚到无关测试**：该行声明的验证对象是「校准科学门（bias/dark/flat/cosmetic）」，但 `test_files` 只给 `test_photometry_apply.cpp`，该文件自述只测 `apply_photometry` 与 FITS 元数据，与 bias/dark/flat/cosmetic 公式**零相关**；符号列亦缺 `ac_correct_frame`（cosmetic 域整体无锚）。即 `VERIFIED` 的唯一测试指针不覆盖被验内容。② **TST-* 是未定义 ID**：`TST-CAL-*/TST-PHOT-*/TST-NOISE-*` 在 `TEST_MATRIX.md`、`TRACEABILITY.csv` 与 schema 中都不存在，且 `TST-` 前缀直接违反 SPEC:78 的 `^TEST-` 文法（同族另见 `00_COORDINATION` 登记的双头矩阵问题 F00-01）。③ 校准域真正的验证载体（`lib/calibration/tests/p1cal/`、`tests/backend/test_calibration_oracle.py`、`tests/unit/master_flat_median_test.cpp`）**全部未登记入该行**，追溯链与构建事实双向断裂。④ 科学/接口文档引用的上游规范（`02_FROZEN_STAGE1_HISS_SPEC`、`SNR_SCIENCE_DERIVATION.md`、`SNR_REDESIGN_CONTRACT.md`、`SCIENCE_ACCEPTANCE_MATRIX.md`）与路径（`flux_calibrator/`、`test_ids.schema.json`）在仓库中不存在，共 8 处独立锚点。
- 影响: 无法按合同指针复核科学门；`VERIFIED` 状态对校准/测光/噪声三域都失去可定位证据；真实数据终验与发布评审依赖这些指针。宪章 §12.3-4「每个核心合同至少一个独立测试」在**登记层**不满足（实际测试存在但未登记）。
- 建议处置: (a) 重建 SCI-CAL-001 行：test_files 指向 p1cal + `test_calibration_oracle.py` + `master_flat_median_test.cpp`，符号列补 `ac_correct_frame`（cosmetic 另起一行），状态降 `PARTIAL` 直到映射闭合；(b) `TST-*` 与 SPEC 文法二选一（推荐把 SPEC 的正则登记进各合同尾注并统一改 `TEST-*`）；(c) 四条不存在的被引文档改指现行权威或按 `docs/archive/` 归档；`flux_calibrator` 路径全库清理（与 F00-04/M5 并档）。
- 置信度: 高
- related: F00-01（双头矩阵，主责 M6）、M3-F-001、M3-F-002、M3-F-004、M3-F-005、M3-C-007、M3-I-001、M3-A-001

