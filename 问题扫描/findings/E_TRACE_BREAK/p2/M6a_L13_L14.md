# M6a 定稿 · E_TRACE_BREAK × P2（来源切片 L13）

### M6a-E-002 权威星点检测实现把上游 C 源文件行锚当公式出处：sdet_api.cpp 内 39 处 `star_finder.c:NNN` + 1 处 `PSF.c:NNN`，两个文件在仓内均不存在（来源 L13-017 的锚点残余）

- 类别码: E_TRACE_BREAK
- 优先级: P2（M3b-D-01 已就同文件的断句残片/事故流水定稿；本条只登记「上游源文件锚不可解析」这一独立事实；因涉事公式本身另有 ALG 出处，未升 P1）
- 位置: `lib/star_detector/src/sdet_api.cpp`（复核时点命中样例 :195 `// 圆度, 1.0]（star_finder.c:105-108)`、:2283 `// mag, star_finder.c:638-655)`、:1572 区段、:2285/:2305 等；grep `star_finder\.c:` 本代理复算 = 39 处，全部落在该单文件；`PSF.c:` 1 处 :2305 区段）；对照 `lib/star_detector/src/sdet_detector.cpp:53`（描述已删代码的历史注释）
- 证据摘录:
  > （:195）// 圆度, 1.0] (star_finder.c:105-108)
  > （:2285）// mag = -2.5*log10(Σ_box(pixel - B)), B=PSF拟合背景 FIT(0) (PSF.c:724,653)
- 权威依据: 宪章 §12.3-9（无悬空引用）、§12.2（注释须给出可核的数学原因/出处）、§14.2（唯一权威实现面）；`docs/algorithms/STAR_DETECTION_ALGORITHMS.md`（现行公式权威位）
- 问题说明: `star_finder.c`/`PSF.c` 在本仓不存在（glob 复算零命中，含 testdata/third_party）；这些锚把「过滤阈值 0.5/0.2、dynrange、RMSE 系数、mag 定义」等判据的设计意图指向仓外不可定位的第三方源，读者既不能核标准也不能核版本（无 vendored 副本、无上游版本号声明）。
- 影响: 星点检测的阈值/公式设计出处不可追溯；一旦上游版本变化，注释不会失效也不会更新（无版本锚），形成静默错锚。
- 建议处置: 二选一——把上游对应版本副本 vendored 到 `third_party/` 并写版本号，或把锚改为 ALG 条款（`ALG-STARDET-001 §n`）+ 一句「源自 Siril/第三方 X.c，本仓不复制」；禁保留不可解析的行锚。
- 置信度: 高（计数与 glob 零命中本代理复算）
- 来源: L13-017（锚点残余）
- related: M3b-D-01（残句与事故流水定稿点）、M6a-E-001、M6a-D-010
