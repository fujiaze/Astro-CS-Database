# M8 定稿（L22）— E_TRACE_BREAK / P2

> 合并代理 M8｜来源 _cache/L23.md + _cache/L22.md｜纯只读复核（read/grep/glob），锚点在当前树按符号重新定位；行号为「复核时点」参考。



> 合并代理 M8｜来源 _cache/L23.md + _cache/L22.md｜纯只读复核（read/grep/glob），锚点在当前树按符号重新定位；行号为「复核时点」参考。

### M8-E-001 `lib/hips_p2/` 只有文档三件套（无源文件），追溯锚实指 `lib/phase2/tools/stage2.cpp` 的 `astrocs-stage2` 工具目标，而该目标 `EXCLUDE_FROM_ALL`（不进默认构建与任何 install）

- 类别码: E_TRACE_BREAK
- 优先级: **P2**
- 位置: `lib/hips_p2/`（glob 实测仅 `module.yaml`、`memory.md`、`README.md` 三文件）；`lib/hips_p2/README.md::构建`（:6-9 指 `lib/phase2/tools/stage2.cpp`（自称 1762 行实测）、:41 状态 CONTRACT_READY、:42 指 `lib/phase2/CMakeLists.txt:103 add_executable(astrocs-stage2 tools/stage2.cpp)`、:135「thread id 固定顺序定序归并 :1315-1317」）；构建真源 `lib/phase2/CMakeLists.txt`（:7-8 注释「astrocs-stage2 … 为按需工具 (EXCLUDE_FROM_ALL, 非正式 ALL target), 不进入默认构建与任何 install」、:113 add_executable、:120 `set_target_properties(astrocs-stage2 PROPERTIES EXCLUDE_FROM_ALL TRUE)`、:124 `add_dependencies(phase2_ivar_wiring astrocs-stage2)`）
- 证据摘录:
  > （lib/hips_p2/README.md:42）构建 | lib/phase2/CMakeLists.txt:103 `add_executable(astrocs-stage2 tools/stage2.cpp)`、:104-105 链接、… :110 EXCLUDE_FROM_ALL
  > （lib/phase2/CMakeLists.txt:113/:120）add_executable(astrocs-stage2 tools/stage2.cpp) / set_target_properties(astrocs-stage2 PROPERTIES EXCLUDE_FROM_ALL TRUE)   ← 复核时点行号 113/120，文档写 :103/:110（漂移）
  > （lib/hips_p2/README.md:135）…thread id 固定顺序定序归并 :1315-1317）
- 权威依据: 宪章 §17.10（追溯行必须指向可解析宿主）、§12.3-12、§16.2（状态声明与实际一致）
- 问题说明: ①追溯/登记把 `MOD-astrocs-phase2-hips-writer` 挂在 `lib/hips_p2` 名下，而该目录零源文件 ⇒ 按目录做 glob 的机器面必然零命中（同型：M5b-E-02 的 `lib/phase3` glob = 0）；②锚点行号已漂移（README 写 :103/:110，实测 :113/:120；README 自称 stage2.cpp「1762 行实测」而 README:46 又写 main :112-1762，属自证但无法机器核对）；③宿主是 `EXCLUDE_FROM_ALL TRUE` 的 compatibility 工具（:124 仅被 `phase2_ivar_wiring` 拉建）⇒ 模块文档的「构建」行给读者的印象（正式工具入口）与其在默认构建/install 中的实际地位不符（该定位在 :7-8 与 :111 注释里另有自述，文档未回写）。
- 影响: 按目录/锚点做覆盖审计的门会误判该模块无实现；模块状态与构建地位对外表述不一致。
- 建议处置: ①README/「构建」行改指真实宿主并显式标注「compatibility 工具（EXCLUDE_FROM_ALL），不入默认构建与 install」；②追溯行的宿主字段统一用 target 名（`astrocs-stage2`）而非目录路径，行号一律符号化；③若该模块确需转正，按 §12.3 走构建收编流程（另条评估）。
- 置信度: 高（目录零源文件 + CMake 两处逐字 + 文档自述对照）
- 来源: L22-010（定稿）
- related: M5b-E-02（`lib/phase3` glob 同型，主落 M5b）、M8-G-001（方向③）、M8-C-001