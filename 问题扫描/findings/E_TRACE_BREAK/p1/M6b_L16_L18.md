---

### M6b-E-002 行锚/引用锚系统性漂移的机器侧根因：锚门判据只有「可解析 + 界内 + 41 条符号绑定」，作用域仅 41 篇、语法仅带文件名锚，且 git 异常整段跳过仍出 PASS

- 类别码: E_TRACE_BREAK
- 优先级: **P1**
- 位置:
  - `docs/algorithms/anchors/check_doc_line_anchors.py::main`（现 :193-249：C2/C3 判据；:58-62 `git_ls` 无 timeout 且失败返回 None；:206/:234 在 `tracked_set is None` 时整段跳过）
  - `docs/algorithms/anchors/anchor_contract.json::doc_globs`（现 :5-8，仅 `docs/science/*.md` + `docs/algorithms/*.md`）、`::bindings`（41 条）、`::resolvers`（13 条）
  - `docs/algorithms/anchors/ANCHOR_CONTRACT.md::§1 现行规模`（现 :18）
  - `tools/traceability/check_traceability_matrix.py::_check_anchor / git_tracked`（现 :284-315、:102-105）
  - 漂移实例（本域）：`docs/owner/RELEASE_STATUS.md::§3 冻结与落地面清单（三 Phase 节点化行 / RT executor 行）`（现 :70、:71）、`::§4 未完成项（Phase3 四投影行）`（现 :94）；`docs/algorithms/CALIBRATION_ALGORITHMS.md::§0 生产源`（现 :20）；`docs/algorithms/PHASE2_REJECTION.md::（integrate.h:47）`；`docs/algorithms/PHASE2_INTEGRATION.md::（integrate.h:38）`
  - 域外实例（本条只作旁证，定稿在各自域）：`docs/architecture/MODULE_MAP.md`:4257/:4282/:4309、`docs/science/REJECTION.md:155`、`docs/algorithms/DRIZZLE_GEOMETRY.md`、`docs/algorithms/STAR_PSF_ALGORITHMS.md:125-130`
- 证据摘录:
  > （anchor_contract.json:5-8）"doc_globs": [ "docs/science/*.md", "docs/algorithms/*.md" ]
  > （ANCHOR_CONTRACT.md:18）现行规模：**36 文档 / 793 锚**（其中 784 为源码/文档目标锚，9 条为登记豁免）。
  > （checker :242）if start < 1 or end < start or end > n: → C3_range_in_bounds（界内即 PASS）
  > （checker :269-276）for a in anchors: … if sym in body: ok = True; break（**任一锚命中即整条绑定通过**）
  > （checker :193-194/:206/:234）tracked = git_ls(root); tracked_set = set(tracked) if tracked is not None else None → `tracked_set is None` 时 C1 跟踪性与 C2 锚目标跟踪性两项检查整段跳过
  > （checker :59）r = subprocess.run(["git", "ls-files"], cwd=root, capture_output=True, text=True)  —— 无 timeout
  - 实测复算（本代理自写正则移植 `ANCHOR_RE`）：锚面实为 **41 篇**（docs/science 15 + docs/algorithms 26，合同自述 36 已过期），带文件名锚 **805 处**；同面内另有 **223 处**表格裸单元格锚（形如 `| :192 |`，分布 11 篇，最多 `docs/algorithms/PHASE2_REJECTION.md` 61 处、`PHASE2_UPM_IMPL.md` 30 处、`PHASE2_INTEGRATION.md` 35 处），**不匹配 ANCHOR_RE，门对其零检测力**。
  - 实测漂移（L0，本域复算 4 处，非转述）：`docs/owner/RELEASE_STATUS.md:70` 引 `module_adapters.cpp:4257/:4282/:4309`，现 :4257=`}`、:4282=结构体成员注释、:4309=`}`，而三张节点表实测在 :5482（p1_nodes）/:5507（p2_nodes）/:5534（p3_nodes）——**漂移 1225 行以上**；:71 引 :3777-3793（P1 会话）实测该区间与 `p1_session_create` 无关（该符号声明在 :124、注册在 :905）；:94 引 `p3_wcs.cpp:36` 作"TAN-only"直证，实测 :36 为坐标推导注释（TAN-only 证据在 :61-64、:208-209）。
  - 实测漂移（ALG）：`docs/algorithms/CALIBRATION_ALGORITHMS.md:20` 引 `CMakeLists.txt:321-333` 作 `astrocs_calibration` 静态库构建清单，实测 :322-333 是 `astrocs_aio`，`astrocs_calibration` 在 :373-383。
- 权威依据: 宪章 §12.3-9、§12.3-12（结论可追溯到当前 SHA 的机器证据）、§13.4；`docs/algorithms/anchors/ANCHOR_CONTRACT.md::§2 锚是断言`（现 :23-24：「代码移动即锚失效，必须在同一提交内更新行号」）。
- 问题说明: 漂移之所以普遍且门抓不到，是三条结构性缺口叠加，不是个案疏漏：①**判据弱**——门只判"可解析 + 目标存在 + 行号在 1..目标行数内"，界内错锚 100% 放行（C3 的语义只是越界）；符号绑定只有 41 条且"任一锚命中即通过"，同一符号被多处锚共享时（`integrate.cpp` 一个文件 16 锚共享 :49）界内检查在 ±100 行漂移下几乎不可能触发；②**语法面外**——223 处表格裸 `:NNN` 锚不匹配 ANCHOR_RE；③**作用域外**——doc_globs 只有 science/algorithms 两目录，docs/owner、docs/architecture、docs/contracts、docs/modules、README/AGENTS/CHANGELOG 全部不在门内，而这些正是承载"当前提交实测绿"结论的层。外加 git 异常 fail-open：无 timeout、返回非 0 即跳过两项跟踪检查并照常输出 DOC_LINE_ANCHORS_PASS。
- 影响: L0/L3 文档中"当前提交内可核"的行锚成片失效（本域复算 4/4 抽点全错），核对者按锚打开会读到无关代码，据此做出的"实测绿"判断不可信；门持续输出 PASS 造成"锚点已被机器守护"的假象（同 §12.3-12 缺口，见 M6b-G-005）。
- 建议处置: ①锚一律迁移为 `path::symbol` 形态并把 41 条 binding 扩为「每条锚至少一个符号断言」；②doc_globs 扩到全部 ACTIVE_NORMATIVE 文档（`docs/DOCUMENT_INDEX.yaml` 可直接作为作用域输入）；③新增"裸 `:NNN` 单元格锚"采集（或禁止该形态）；④`git_ls` 加 timeout，git 失败输出 TOOLING_FAILURE 并 exit 3（与本仓"不允许伪 PASS"一致）；⑤C4 改为逐锚判定而非"任一命中即通过"。
- 置信度: 高（锚数/裸锚数/漂移四处均为本代理复算）
- 来源: L16-008（C4 任一锚通过 + C7 不校验 SCI 锚 + git 无 timeout）、L16-009（223 裸锚 / 33 处越界 / PRODUCTION-GRAPH 不读 trace）、L18-008（L0 锚 4/14 漂移 + docs/owner 不在门内）、L16-016（fail-open）
- related: L05-017、L07-016、L10（域外实例）、L12-017、L17-005、M5b（同类「门判据弱」条目；具体 ID 以前台合并为准）、M6b-G-004、M6b-E-006
- 说明: 按去重边界，"行锚系统性漂移"这一根因**只在本条定稿**；其它域只登实例并 related 指回本条。

---

### M6b-E-003 追溯 ID 无单一登记处：矩阵 152 个非占位 ID 中 85 个不在 contracts/INDEX.yaml（其中 VERIFIED 合同层 33 处 / 去重 28 个 ID），descriptor 侧另有 93 个 ID 中 72 个不在 INDEX

- 类别码: E_TRACE_BREAK
- 优先级: **P1**
- 位置:
  - `docs/traceability/TRACEABILITY_MATRIX.json::modules[*].{science_id,algorithm_id,data_id,api_id,arch_id,src_id,test_id,evidence_id}`
  - `docs/contracts/INDEX.yaml::entries`
  - `lib/core/src/module_adapters.cpp::descriptor 注册（d.module_id / d.sci_id / d.alg_id / d.data_id / d.api_id / d.test_id）`
  - `docs/traceability/TRACEABILITY_SPEC.md::§3 ID 命名与唯一性`（现 :83-:93）
  - `docs/modules/registry/astrocs.phase2.resample.md::§（合同 ID 行）`（现 :13-20）
  - 越界形态：`docs/traceability/TRACEABILITY_MATRIX.json::（MOD-astrocs-phase1-star-psf 行 science_doc）`（现 :246-247）
- 证据摘录（复算值）:
  - 矩阵 30 行中的非占位 ID 去重 = **152 个**；`docs/contracts/INDEX.yaml` 的 `- id:` 条目 = **100 个**；矩阵 ID 中不在 INDEX 者 = **85 个**（56%）。
  - 其中位于 `VERIFIED` 合同层（SCI/ALG/DATA/API/ARCH 且该行该层状态=VERIFIED）者 = **33 处**（按 层×行 计），去重后为 **28 个不同 ID**，含 `API-ABI-001`、`DATA-HIPS-001`、`DATA-P1-CAL`、`API-COS-001`、`DATA-P1-DRZ`、`DATA-P1-HIPS`、`SCI-P1-PSF-001`、`API-P2-001`（3 行共享）、`API-P3-001`（3 行共享）、`DATA-GAIA-001`、`API-GAIA-001`、`SCI-P1-STAR-001` 等。
  - descriptor 侧实测：22 个 descriptor 模块 / 93 个唯一 ID / **72 个不在 INDEX**。
  > （registry 页:13-20）contract: sci_id: SCI-P2-RES-001 # 占位 ID——descriptor 使用，docs/science 未定义 …… test_id: TEST-P2-RES-001 # 占位 ID；docs/validation/TEST_MATRIX.md 无 P2-RES 行，本模块唯一真实证据=rt009_node_trace_test.cpp:73-91（端口名）。
  > （矩阵 JSON:246-247）"science_id": "SCI-P1-PSF-001", / "science_doc": "docs/algorithms/STAR_PSF_ALGORITHMS.md::SCI-P1-PSF-001",
- 权威依据: 宪章 §12.3-1、§12.3-2（端口引用有效 DATA 合同）、§12.3-3（实现引用有效 SCI/ALG）、§14.2；`docs/contracts/INDEX.yaml::文件头自述（ID 全集唯一登记处）`；TRACEABILITY_SPEC §3/§5（SOURCE SYMBOL 与 ID 语义）。
- 问题说明: 同一"追溯 ID"存在**三套事实源**：INDEX.yaml（100 条自述为全集）、矩阵（152 个非占位 ID）、`lib/core` descriptor（93 个 ID，与矩阵又是一套）。矩阵里 85 个 ID 在"全集"登记处查不到，其中 28 个还被机器门判为 VERIFIED 合同层；descriptor 侧 72 个同样无处登记。更严重的是 `C7` 不校验 `science_doc`/`algorithm_doc` 的路径归属（见 M6b-G-001），于是 `SCI-P1-PSF-001` 的"科学权威页"被合法写成 `docs/algorithms/STAR_PSF_ALGORITHMS.md`（L1 内部把科学层降级为算法页子节），门无从发现。registry 页还把 `SCI-P2-RES-001` 一类 descriptor 占位 ID 写成 `contract:`，成为后续引用源——占位值获得文档身份。
- 影响: 任何人以 INDEX.yaml 为"合同全集"做影响面/覆盖判断都会漏掉 56% 的矩阵 ID；ID 反查断链；SCI 权威层可被 ALG 页冒名而不报错。
- 建议处置: ①指定 INDEX.yaml 为唯一登记处并补登矩阵全部真实 ID（或改由矩阵生成 INDEX 的 ID 面）；②descriptor 的 `sci_id/alg_id` 改引用矩阵既有 ID 或显式 `*-MISSING`，禁自造占位 ID；③在 `check_traceability_matrix.py` 增 C9：`VERIFIED 的 science_doc/algorithm_doc 必须落在 docs/science/ 与 docs/algorithms/ 且 token 命中`，并增"矩阵 ID ⊆ INDEX.yaml" 判据；④`API-*` 命名空间另立登记处（与 M6b-E-001④合并）。
- 置信度: 高（152/85/33/28/93/72 全部本代理复算）
- 来源: L16-012、L16-013、L18-024、L18-019（registry 占位 ID 部分）
- related: M6b-E-001、M6b-G-001、M2b（L18-019 API_REFERENCE 悬空）、M6b-E-004

---

### M6b-E-004 交叉引用系统性失效：八类全仓不存在的文档被真源 138 处当权威依据（实测 lib 36 文件 133 处 + docs 3 文件 5 处），另有 6 类路径前缀/文件在 ACTIVE 合同中悬空；宪章 §12.3-9 机器零实现

- 类别码: E_TRACE_BREAK
- 优先级: **P1**
- 位置（本代理复算的全部命中面）:
  - 不存在却被当依据：`02_FROZEN_STAGE1_HISS_SPEC(.md)`（全仓 0 文件）、`00_COMMON_CONTRACTS.md`（0）、`SNR_SCIENCE_DERIVATION.md`（0）、`SNR_REDESIGN_CONTRACT.md`（0）、`docs/stage1_fix/`（0）、`docs/architecture/cpu/ARCH_CONTRACTS.md`（0，ARCH 面另立 `docs/architecture/ARCH_CONTRACTS.md` 亦 0）、`docs/architecture/CPU_ADAPTIVE_V1.md`（0）、`include/astrocs/exit_codes.h`（0，真实 `cli/exit_codes.h` 存在）、`tools/check_cli_protocol.py`（0）、`docs/validation/ACCEPTANCE_GATES.md`（0）、`docs/history/`（0 目录）、`docs/TRACEABILITY_family.json`（0）、`docs/interfaces/data/DATA-001_ARTIFACT_CONTRACT.md`（0）
  - 存在但已归档、活动文档仍按在位引用：`docs/24_WCS_VALIDATION_V2_SPEC.md`、`docs/25_AUTHORITATIVE_MATCH_PAIR_CONTRACT.md`（真身在 `engineering/control/archive/2026-09-02_legacy_…`，实测 `docs/` 下无）
  - 引用点（真源）：`lib/astro_image_io/include/hiss_format.h`（16 处）、`lib/healpix_db/healpix_drizzle/drizzle_engine.cpp`（20 处）、`lib/astro_image_io/src/hiss_common.cpp`（9 处）、`lib/astro_image_io/src/{hiss_writer,hiss_reader}.cpp`（各 7 处）、`lib/astro_image_io/tests/*`（5 文件）、`lib/calibration/{include/astro_calibration.h,src/photometry_apply.{h,cpp},src/dark_optimizer.cpp}`、`lib/snr_estimator/cpp/{include/snr_estimator.h,src/snr_estimator.cpp}`、`lib/plate_solve/cpp/ipv/include/{ipv_api.h:208,ipv_solver.h:108}`、`docs/algorithms/{CALIBRATION_ALGORITHMS.md:159,186,392, DRIZZLE_GEOMETRY.md:173}`、`docs/contracts/DATA_SEMANTICS.md`、`lib/{phase3_proj,phase3_rsmp}/README.md` 与 `docs/modules/{phase3_proj,phase3_rsmp}.md`（ARCH-001 行）、`docs/algorithms/PHASE2_COVERAGE.md:243` + `lib/phase2/README.md:87`（CPU_ADAPTIVE_V1）、`docs/api/CLI_PROTOCOL_V1.md:25,47,51`、`docs/VERSIONING.md:16,32`、`CHANGELOG.md:40`、`VISUAL_CHECK_README.md:8,17`、`docs/standards/DOCUMENTATION_STANDARD.md:13`、`docs/audit/doc_classification.csv`（15 行 `docs/history/`）、`docs/validation/SCIENCE_FREEZE.md:4`
- 证据摘录:
  > （ipv_api.h:208）// 详见 docs/24_WCS_VALIDATION_V2_SPEC.md 与 docs/25_AUTHORITATIVE_MATCH_PAIR_CONTRACT.md
  > （snr_estimator.h:216-217）// 输出 out_snr 仍为 float32 (HISS SNR 子块格式已冻结为 float32, 见 02_FROZEN §17;
  > （photometry_apply.h:7,12）// 规范依据: 02_FROZEN_STAGE1_HISS_SPEC §7 … // 公共契约: 00_COMMON_CONTRACTS.md §1.1
  > （docs/modules/phase3_proj.md:30 与 lib/phase3_proj/README.md:38）| ARCH | ARCH-001 | docs/architecture/cpu/ARCH_CONTRACTS.md | VERIFIED |
  > （docs/algorithms/PHASE2_COVERAGE.md:243）- ARC-001（docs/architecture/CPU_ADAPTIVE_V1.md）: cpu_heavy 资源类、
  > （docs/api/CLI_PROTOCOL_V1.md:25 / :51）## 2 退出码(04 §2 全 11 条冻结,唯一源 `include/astrocs/exit_codes.h`) / 3. 退出码常量唯一源(include/astrocs/exit_codes.h, grep 无第二处数值表);
  > （docs/api/CLI_PROTOCOL_V1.md:47）## 6 机器化一致性检查器合同(04 §6,API-002 建立 `tools/check_cli_protocol.py`)
  > （docs/VERSIONING.md:16 / :32）schema 见 `schemas/version.schema.json`。/ - 扫描 `docs/ schemas/ tools/ launch/ tests/` 与根级 README/CHANGELOG/build.sh/toolchain.ps1
  > （VISUAL_CHECK_README.md:8）pwsh -File .\launch\start_browser.ps1
  > （CHANGELOG.md:40）- `schemas/traceability_matrix.schema.json` + `docs/traceability/TRACEABILITY_LAYERS.csv`：
  > （docs/traceability/TRACEABILITY_SPEC.md:20）- `schemas/traceability_matrix.schema.json`（JSON 合同）
  > （docs/validation/SCIENCE_FREEZE.md:4）> ACCEPTANCE_GATES.md G1-G10 全部 PASS、known P0/P1 = 0，
  - 实测计数（本代理）：`02_FROZEN`/`00_COMMON_CONTRACTS`/`24_WCS_*`/`25_AUTHORITATIVE_*`/`docs/stage1_fix`/`04_IMPLEMENTATION` 族在 `lib/**` **36 个文件 133 处**、在 `docs/**` **3 个文件 5 处**；`schemas/`、`launch/` 根前缀实测均 0 目录（真实位置 `contracts/schemas/`、`packaging/launch/`）。
- 权威依据: 宪章 §12.3-3（实现引用有效 SCI/ALG）、§12.3-9（无悬空引用）、§1.1 权威分层（历史/归档文档仅作线索）、§14.2（目录职责：schemas 与 launch 的现位置）；`HANDOVER.md` 的迁移记录（schemas→contracts/schemas、launch→packaging/launch）。
- 问题说明: 失效是**面**而非点：①被引用文档从未在仓库存在（02_FROZEN/00_COMMON_CONTRACTS/SNR_*），公共头与核心实现把科学/接口依据挂在它们上——即"依据不可定位"；②曾在控制包内、现已归档的编号文档（24/25）被写成 `docs/…` 活动路径；③目录迁移（schemas/launch）后活动合同与检查器扫描面未同步，`check_version_consistency.py` 的文档扫描面含两个不存在目录；④ARCH 层被 4 份 ACTIVE 文档标 VERIFIED 却指向不存在的 ARCH_CONTRACTS.md；⑤`tools/check_cli_protocol.py`、`ACCEPTANCE_GATES.md`、`docs/history/` 等属"宣称的门/文件不存在"。实测 **ci/checks.json 75 个检查项中无任何一项校验文档内引用路径的存在性**（`tools/quality/contracts/check_doc_symbols.py` 只解析反引号 token 且显式跳过无扩展名/复合名/占位符形态，`tools/doccheck/check_doc_index.py` 只校验索引本身与归档标记，均不覆盖 `.md` 之外的引用与反引号外的裸路径）。宪章 §12.3-9 在机器上为**零实现**。
- 影响: 交付物级——"依据文件不存在"使科学/接口结论无法复核；机器级——§12.3-9 假覆盖，任何删除/重命名都不会被门发现，本条与 M6b-E-002 共同构成交叉引用失效的两个方向（路径不存在 / 路径存在但内容已漂移）。
- 建议处置: ①实现 §12.3-9：以 `path::symbol` 提取全部 ACTIVE 文档与 `lib/**` 注释中的路径 token，做存在性 + Git 跟踪校验，接入 CI 并设 `waivable:false`；②一次性锚点订正提交：`02_FROZEN §N`→现行文档 ID、`docs/24|25_*`→归档包全路径或改写、`schemas/`→`contracts/schemas/`、`launch/`→`packaging/launch/`、`include/astrocs/exit_codes.h`→`cli/exit_codes.h`；③把 `tools/check_cli_protocol.py` 补实现并注册，或将 CLI_PROTOCOL §6 改为「未建立（缺口登记）」；④`docs/history/` 相关条目统一改 `docs/archive/history/`。
- 置信度: 高（存在性与计数均实测）
- 来源: L18-014（IO_001 清单，部分改判，见 _merge §3）、L18-001、L18-008（部分）、L18-012、L18-013、L18-021、L16-014、L13-002 与 L01-014 同主题（协调记录指定 M6 与 M1a 会签）
- related: M6b-E-002、M6b-G-002、M6b-G-005、M6b-I-002、M1a（L01-014）、M6a（L13-002）、M5b-G-015
