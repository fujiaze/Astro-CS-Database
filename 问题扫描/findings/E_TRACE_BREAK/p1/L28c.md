# L28c findings — E_TRACE_BREAK（tier2 头文件外围 · 注释溯源补轴）

- 代理码: L28c（第三轮补轴：L28b 明言未覆盖的 header 面）
- 判据: 复用 L28b D-TRACE-1 的 R-1..R-6（原文见 问题扫描/_cache/L28c.md §0.3），本域不另立判据
- 作用域: tier2 **交付堆**（81 文件 / 7,917 行）；非交付堆（120 文件）只统计不立条
- 本类 finding: 2（P1:2）

## L28c-E-001 runtime/pipeline/typed_dag_contract.h 以「编译期合同 + ABI 冒烟测试锁定」自我背书，但该头全仓零消费者、不在任何构建/测试源列表，被指派的锁不存在

- 类别: E_TRACE_BREAK
- 建议优先级: P1
- 位置: runtime/pipeline/typed_dag_contract.h:1、:61-62、:3-6、:43-44、:48、:52、:119-120
- 证据摘录:
  > :1 "// AstroCS Runtime — RT-001 typed DAG node 合同（冻结头，编译期合同）"
  > :61-62 "// RT-001 node 合同：id/module_id/operation/ports/config/resource class/phase。
  >          // 冻结字段顺序不得重排（ABI 冒烟测试锁定 layout 断言）。"
  > :48 "std::string data_schema_id;   // \"DATA-xxx\""
  > :52 "std::string shape_hint;       // \"[H,W]\" | \"[N,2]\" | \"[]\" ..."
- 权威依据: 宪章 §12.2（注释只解释非显然决定，禁审计流水）；§12.3-3「算法实现引用有效 SCI/ALG」；RQS-PROTOCOL-001 §1 E_TRACE 定义「悬空引用（指向不存在的文件/符号/章节）」「ID 未经定义即被引用」；L28b R-6b（锚必须有宿主）与 R-6c（注释被指派为权威必须回写 L1）；L28b R-3（占位形 DATA-xxx、空括号不計为溯源）
- 问题说明: 三处失效叠加在同一头：①**验证锁不存在**——全仓 grep 该头 basename `typed_dag_contract` **0 命中**（三级复核：整词 grep → 放宽 "TypedDag" → 定点 read 全文 122 行）。放宽后 16 命中全在 `runtime/pipeline/typed_dag.py`、`tests/{runtime,pipeline,monitoring}/*.py`、`tools/graph/render_run_graph.py`，即 typed DAG 的验证面**全部是 Python 侧**；注释所指的「ABI 冒烟测试」实体 = `tests/unit/rt001_abi_test.cpp`（tests/unit/CMakeLists.txt:42-45 注册为 `rt001_abi`），其 include 面实测只有 `astrocs/core/{context,module,runtime}.h`，断言对象是 ThreadBudget/RunContext/ModuleDescriptor 等，**不含本头任何类型**；②**编译期合同不成立**——本头不在任何 add_library/add_executable/install 源中（grep `runtime/pipeline`、`typed_dag_contract` 于 CMakeLists.txt = 0），根 CMakeLists 只编 runtime/io/fits_core.c(:146) 与 runtime/artifact_store/artifact_abi_v1.c(:133)，故「编译期」合同从未进入编译；③字段自身的溯源位是占位与空括号（:48 `"DATA-xxx"`、:52 `"[]"`），按 R-3 不计溯源，且 :43-44 声称「单位/坐标枚举与 astrocs::core::UnitId/CoordinateFrame 数值一致」而本头**没有任何数值枚举与之对应**（unit/coordinate 皆为 std::string），该一致性断言不可复算。
- 影响: 该头是 RT-001 typed DAG 的 C++ 侧合同视图，被 lib/core、CLI 与 scheduler 后续接线（:19「本任务为合同 + 骨架：执行由 RT-002..007 接线」）引用时，维护者会按注释相信「字段重排会被 CI 的 layout 断言拦下」而实际无此保护；真实后果是**下一次接线即可静默重排字段顺序**而不触发任何门。当前不影响已交付数值（该头未被编译）。
- 建议处置: ①把该头纳入构建与 ABI layout 断言面（与 rt001_abi_test 同一 target 族），或把 :62 的锁声明删除并改记「本头未参与编译，字段顺序变更不受机器保护」；②:48/:52 的占位与空括号改为真实 DATA ID 或写 MISSING；③:43-44 的「数值一致」断言给出可复算的映射表或删句。
- 置信度: 高
- related: L28b-D-005（同一「合同头以占位/示例值承载溯源字段」机制，不同载体，tier1→tier2 外推）；L28b-D-002（同一「注释被指派为权威而权威不存在/未登记」机制）；M6b-E-004（引用对象失效族，本条为「测试宿主不存在」新形态）；M6a §10-5（wiki/ARCHIVED 锚待裁，不涉及本条）

## L28c-E-002 spectrum_integrator.h 把合成流量 F_syn 的数值参考实现指向仓内不存在的 Python 路径，同头另一锚又指向模块内 docs/ 路径（该面亦不存在）

- 类别: E_TRACE_BREAK
- 建议优先级: P1
- 位置: lib/photometric_calib/cpp/src/spectrum_integrator.h:3、:7（并 :35-:36）
- 证据摘录:
  > :3 "// 契约锚: docs/algorithms/PHOTOMETRIC_FIT.md (ALG-PHOTOMETRIC-FIT-*) + docs/science/PHOTOMETRY.md (SCI-PHOT-001) + lib/photometric_calib/docs/algorithm.md §3 ; 数值: Akima 子样条(范围外 fill=0, 严格递增)+Simpson 1/3 复合(奇数区间末3用 3/8, 等间距) — NUMERIC/COMMENT 锚点, 实现见 spectrum_integrator.cpp akima_interpolate/simpson_integrate/compute_f_syn*。"
  > :7 "// 参考: lib/photometric_calib/spectrum_integrator/python/synthetic_photometry.py"
  > :35-36 "// spectrum_count: 光谱点数 (通常 343)" / "// spectrum_wl: 光谱波长数组 [336, 338, ..., 1020] nm (长度 = wl_count)"
- 权威依据: 宪章 §12.3-3、§12.2；RQS-PROTOCOL-001 §1 E_TRACE「悬空引用」；L28b R-2①（通配形须同窗口另给存在的路径方可）与 R-6b（锚必须有宿主）
- 问题说明: 头注释给出三个锚，实测两个宿主不存在：①`:7` 的整路径 —— glob `**/synthetic_photometry*` = **0 文件**（三级复核：路径整串 read 失败 → 放宽 basename grep 命中 9 处，全部是**其他文件转述同一失效路径**（lib/photometric_calib/memory.md、cpp/src/spectrum_integrator.{cpp,h}、cpp/test/test_spectrum_integrator_golden.py、docs/archive/history 运营日志），无一为该文件本身）；实际的数值对照件是 `lib/photometric_calib/cpp/test/test_spectrum_integrator_golden.py`（glob 复算存在），注释未指向它。②`:3` 的 `lib/photometric_calib/docs/algorithm.md` —— 该路径在 lib 下不存在（photometric_calib 的文档面是 README.md/module.yaml，glob 复算）。③`:3` 首锚 `ALG-PHOTOMETRIC-FIT-*` 为通配形，依 L28b R-3 须同窗口给存在的路径；本条窗口内 `docs/algorithms/PHOTOMETRIC_FIT.md` 与 `docs/science/PHOTOMETRY.md` 两个路径**实测存在**（glob 复算），故此锚按 R-3 的豁免分支判为**可解析**——记为 tier2 的 1 例「通配+路径」合法形态，供负责人校准判据。
- 影响: 该头是 Phase1/2 测光标定合成流量 F_syn 的入参合同（spectrum_integrator 被 pc_api.cpp 消费，实测生产消费者 6 处）。「参考实现」断链使数值口径（Akima+Simpson 1/3 与末 3 区间 3/8）无法按注释复现，后来者会去找不存在的 Python 件；不影响当前已交付数值（.cpp 内实现自洽，且 golden 测试在仓）。
- 建议处置: 把 :7 与 :3 的第二锚订正为真实宿主（cpp/test/test_spectrum_integrator_golden.py 与 docs/algorithms/PHOTOMETRIC_FIT.md 章节号），或在宿主确已被删除时改写为「参考实现已移除，数值口径以本文 §x 与 golden 测试为唯一源」。
- 置信度: 高
- related: M6b-E-004（失效引用面主定稿点；本条为其「Python 参考实现路径」新形态，不在其八族文件名清单内）；M6a-E-001（不可解析引用族）；M2b-C-02（同族：以「实测」口吻写死的规模/路径断言）
