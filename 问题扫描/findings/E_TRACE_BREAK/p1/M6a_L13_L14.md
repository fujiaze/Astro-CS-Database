# M6a 定稿 · E_TRACE_BREAK × P1（来源切片 L13 + L14；注释质量横向）

> 代理：M6a。只读取证；位置一律 path::符号。四态与移交见 问题扫描/_merge/M6a.md。
> 并档：L13-002 与 L14-002 中「引用仓内不存在的第一方文档」（SNR_SCIENCE_DERIVATION.md、SNR_REDESIGN_CONTRACT.md、02_FROZEN_STAGE1_HISS_SPEC、00_COMMON_CONTRACTS.md、docs/stage1_fix/、docs/24/25 spec、wiki Phase2_*）这一族，已由 **M6b-E-004** 全域定稿（其计数面 lib/** 36 文件 133 处 已含本域 snr_estimator.h/photometry_apply.h 实例）⇒ 本文件不重复登记，只定稿 M6b 未覆盖的两种新形态。

### M6a-E-001 ABI/加载器/provider/conformance 的注释与 README 把条款号写成控制包章节（「12 §6」「15 §2」「C §C6」「06 §2」），其目标文档按宪章 §1.1 不是现行权威；清理工具的正则只匹配完整 40 位哈希，对 8+8 截断形态永久失效（来源 L14-002 的新形态残余）

- 类别码: E_TRACE_BREAK
- 优先级: P1
- 位置:
  - `runtime/module_loader/README.md::语义节标题`（复核时点 :14「## 语义(12_DLL_ABI_AND_LOADER_STANDARD.md §6)」）
  - `runtime/module_loader/secure_loader.h::权威块`（复核时点 :7-8「规格 = 控制包 tasks/02_ABI_BUILD_CLI_TASKS.md ABI-003 节 + 12_DLL_ABI_AND_LOADER_STANDARD.md §6 + AstroCS_ENGINEERING_CONSTRAINTS.md §F3」、:21/:25/:158 同族）
  - `runtime/registry/module_registry.h::权威块`（复核时点 :8-12「… 12_DLL_ABI_AND_LOADER_STANDARD.md §5/§6 + 11_MODULE_SOURCE_TEST_STANDARD.md §4)…(12 §1: 模块只经批准的 C ABI 查询入口注册)」）与 `runtime/registry/module_registry.c`（:4）
  - `providers/cpu/common/README.md::合同表 标准行`（复核时点 :59「| 标准 | 15 §2 / C §C6 | 15_CPU_PROVIDER_AND_RESOURCE_STANDARD.md / AstroCS_ENGINEERING_CONSTRAINTS.md §C |」）与 `providers/cpu/avx2/src/avx2_provider.cpp`（:27/:370/:373）、`modules/conformance/echo/README.md`（:23/:38）
  - `lib/phase2/include/astro/phase2/coverage.h::语义注释`（复核时点 :5「// 语义（冻结， 34A532A2...B2EB308 + wiki Phase2_Architecture）：」）、`lib/phase2/tools/stage2.cpp`（:5 同一截断哈希 token 作为流程权威）、`lib/phase2/include/astro/phase2/block.h:5`/`sampler.h:5`/`upm.h:4`/`rejection.h:129`/`integrate.h:5`、`lib/phase2/src/upm.cpp:9`（同族残断 token）
  - 清理工具：`tools/quality/v19r4_strip_comments.py::SHA`（复核时点 :73 `SHA = re.compile(r"\b[0-9a-fA-F]{40}(?:\.\.\.)?\b")`）、`:9`（docstring 承诺「控制包 SHA（34A532A2...）：→ 删除」）、`:75`
  - 现行权威面：`docs/standards/`（实测 14 份，无 12_/15_/11_ 编号标准；对应现行为 `C_ABI_STANDARD.md`、`CONCURRENCY_STANDARD.md`、`docs/architecture/abi/ABI_003_SECURE_LOADER.md`）；`AstroCS.wiki/`（实测无 `.md` 页文件，`Phase2_Architecture`/`Phase2_Rejection` 页在仓内不可解析）
- 证据摘录:
  > （secure_loader.h:7-8）权威（ABI-003; 规格 = 控制包 tasks/02_ABI_BUILD_CLI_TASKS.md ABI-003 节 + 12_DLL_ABI_AND_LOADER_STANDARD.md §6 + AstroCS_ENGINEERING_CONSTRAINTS.md §F3）
  > （module_registry.h:11）static factory 不再是正式路由(12 §1: 模块只经批准的 C ABI 查询入口注册);
  > （providers/cpu/common/README.md:59）| 标准 | 15 §2 / C §C6 | 15_CPU_PROVIDER_AND_RESOURCE_STANDARD.md / AstroCS_ENGINEERING_CONSTRAINTS.md §C |
  > （coverage.h:5）// 语义（冻结， 34A532A2...B2EB308 + wiki Phase2_Architecture）：
  > （v19r4_strip_comments.py:73）SHA = re.compile(r"\b[0-9a-fA-F]{40}(?:\.\.\.)?\b")   ← 要求 40 位连续十六进制
- 权威依据: 宪章 §1.1（现行权威＝仓内 docs/science、docs/algorithms、docs/standards 真源；历史控制包/工程约束文档仅作线索）、§12.2（禁控制包引用入注释）、§12.3-9；`AGENTS.md`（`AstroCS_ENGINEERING_CONSTRAINTS.md` = ARCHIVED_NON_NORMATIVE，与宪章冲突一律以宪章为准）
- 问题说明: 两点均未被 M6b-E-004 覆盖。①**章节号形态**：把规范条款写成「12 §1/§5/§6」「11 §4」「15 §2」「C §C6」「06/TASK」「06 §2」，其展开名（12_DLL_ABI_AND_LOADER_STANDARD.md 等）实测只存在于 `工程控制/AstroCS_V7_..._FINAL3/`（免报区/控制包解压件），`docs/standards/` 无同名文件；且同批注释还把 ARCHIVED_NON_NORMATIVE 的 ENGINEERING_CONSTRAINTS 与现行标准并列为「权威」⇒ 读者在真源内无法核到条款原文，追溯链断在接口契约层。②**清理工具的规则缺口**：`v19r4_strip_comments.py` 的 docstring 承诺删除「控制包 SHA（34A532A2...）」，但其 SHA 正则要求 40 位连续十六进制，而树内形态是 `34A532A2...B2EB308`（8+8+省略号）⇒ 不匹配，任何一轮清理都碰不到它；`wiki Phase2_Architecture` 形态也不在其规则内，导致该 token 在 7 个活动头文件里持续存在并被当「冻结语义」依据。
- 影响: ABI/加载器/线程预算/provider 的规范落点不可在仓内核到 ⇒ §1.1 的分层与 §16.2 的可核性在接口面失效；清理工具对某类形态「声称清理而永不清理」，使该缺陷可长期免疫于卫生轮次。
- 建议处置: ①规范引用统一改 `docs/standards/*.md` + `docs/architecture/abi/ABI_00x` + SCI/ALG/DATA ID，控制包哈希/wiki 指向迁 `memory.md`；②给 strip 工具补规则（8+8 截断形态、`wiki <Page>` 形态、`NN §M` 简写章节号），并把该工具的清理面与 `COMMENT_STANDARD.md:44-45` 要求的 `reports/v19r2/file_audit_inventory.csv` 记录对账；③若 `AstroCS.wiki` 的页是现行依据，须落仓内真源，否则删除引用。
- 置信度: 高（glob/read 复算 `docs/standards*` 无同名标准、`工程控制` 有、wiki 无 `.md` 页；正则不匹配由表达式直接判定）
- 来源: L14-002（残余新形态；不存在文档族移交 M6b-E-004）
- related: M6b-E-004（同主题定稿点）、M6a-D-010、M6a-E-002、M5a-E-001（L11 侧锚点批量漂移）
