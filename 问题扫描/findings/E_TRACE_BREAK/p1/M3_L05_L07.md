# M3 定稿 · findings · E_TRACE_BREAK · P1（来源切片 L05 + L07）
> 合并代理 M3｜来源 L05 + L07｜逐条重读原文复核。本条为根因合并条目（L07-016 + L05-017），两处独立事实全部保留为实例。处置表见 `../../_merge/M3.md`。

### M3-E-002 SCI/ALG 的源码行锚、符号归属与文件行数批量失实（校准域 + 测光/噪声域实例）
- 类别: E_TRACE_BREAK ｜ 建议优先级: P1 ｜ 来源: L07-016（主）+ L05-017（同根实例）
- 位置（按符号定位；括号为复核时点参考行，源文件仍在被并发修改）:
  - `docs/science/PHOTOMETRY.md::§5 尾「与…一致」行`（:65）与 `§4:37`、`§8:82` —— 锚 `star_matcher.cpp:35-40`（实为 `percentileOf` 区）、`552-559`（实为 `sigma_residual` 计算块）、`:478`（实为 IRLS 初值注释）
  - `docs/algorithms/PHOTOMETRIC_FIT.md::F2 步骤表 r 行`（:95）—— 锚 `:396-403`，实际 `cleanAndScale` 的 r 计算在 :405-416
  - `docs/algorithms/NOISE_ESTIMATION.md::§13.1 锚表`（:132-140）—— `noise_model.cpp` 的 `fill_impl :371-429`（实际 371-418）、门面（实际 422-429）、`LS :340-370`（实际 `fit_plane_ls` 376-398）、`snr_phot_cal_quality … :67-79`（实际 noise_model.cpp:276-290）、`snr_psf_fit_quality … :296-334`（实际 295-328）
  - `docs/algorithms/NOISE_ESTIMATION.md:::121` —— 「noise_model.cpp（466 行…）；snr_estimator.h 431 行」；复核时点 `snr_estimator.h` 实测 **438** 行
  - `lib/snr_estimator/README.md:::7` —— 「`snr_estimator.h`（431 行）」同失实
  - `docs/science/CALIBRATION.md::§5:53` —— `calibrator.cpp:104-136,147-179`（复核时点 `calibrate` 为 106-138、`calibrate_d` 为 149-181）
  - `docs/science/CALIBRATION.md::§9a:93` —— 「FP32 饱和语义…（见 §9）」，而 §9（:80-87）通篇无 NaN/饱和条款（该条真实落点在 §4:37，指向错误）
  - `docs/algorithms/COSMETIC_ALGORITHMS.md::§13.1 锚表`（:35/:45-48/:63/:65-72）—— 8 邻接 `:74-86`（实际 84-96）、hot `:95-101`（实际 103-109）、cold `:111-117`（实际 119-127）、结构过滤 `锚 :70`（复核时点 :70 为 `compute_structure_size` 之后的空行/无关键行）
  - `docs/algorithms/CALIBRATION_ALGORITHMS.md::§1 构建行`（:20）—— 「根 `CMakeLists.txt:321-333`」；复核时点 `add_library(astrocs_calibration STATIC)` 在 :373-377，而 :321-333 是 `astrocs_aio`；同错误被复制到 `lib/calibration/CMakeLists.txt:12/:19`（「根 CMakeLists.txt:333 的 astrocs_calibration 目标」）与 `docs/algorithms/COSMETIC_ALGORITHMS.md:6`（`:373-380`，此处正确）→ 三处互斥锚点
- 证据摘录（复核时点重读，样本）:
  > | 有效残差 r_i=log10(F_instr/F_syn) | cleanAndScale 内 r 计算 | :396-403 |（ALG-PHOT:95）
  > - `noise_model.cpp`（466 行；…）与 `snr_estimator.h`（431 行）…（ALG-NOISE:121）
  > | 平面 LS `fit_plane_ls`（含归一化与部分选主元）| 算法内核 | :340-370；floor 回退 :402-405；fill 门面 :411-422 |（ALG-NOISE:136）
  > 与 `lib/calibration/src/calibrator.cpp:104-136,147-179` 及 `lib/calibration/src/master_generator.cpp:243-255` 一致。（SCI-CAL:53 —— 注：master_generator 锚在复核时点已被并发订正为 :243-255，calibrator 锚仍偏 2 行）
  > - **saturation**：FP32 饱和语义界定，不静默 clamp（§8 `t_light/t_dark` 行）。（SCI-CAL:93）
  > 根 `CMakeLists.txt:321-333` 的 `astrocs_calibration`（ALG-CAL:20）
- 权威依据: 宪章 §12.3-1/-4（锚点与证据指针必须可定位）、§11（证据可验证）；`docs/traceability/TRACEABILITY_SPEC.md` 的锚点约定
- 问题说明: 上述 12 组锚点中 10 组在复核时点不能命中其所称代码（漂移 1-30 行不等，另有 3 组指向完全无关的符号/空行）。`fit_plane_ls` 一例最典型：ALG 表把「LS :340-370 / floor 回退 :402-405 / fill 门面 :411-422」三锚并列，实际三段分别是 `fill_impl 的 det 分支 :393-398`、`:402-405`（此锚正确）、`fit_plane_ls :376-398` 与门面 `snr_noise_model_v1_fill :422-429` —— 即锚表把「门面」与「LS」两个符号的行区间互换。行数类断言（466/431 行）已随代码增长而失效，README 复述同一错误数字。
- 影响: 所有「与代码一致」类断言（SCI §5 尾、ALG §13.1）在人工与机器复核上都不可验证；评审者按锚点跳转看到的是无关代码，会得出与合同相反的结论（尤其 M3-C-001/M3-C-002 的两处锚，正好掩盖了「合同门在代码中不存在」）。按规程锚点漂移属 P1。
- 建议处置: 全面改为**符号级锚**（`path::symbol`），行号只作参考并在门中校验「锚所在行含该符号名」；把上述 12 组锚逐一订正；把 `tools/docs_machine_consistency.py` 的 `code_refs` 符号存在性检查扩为「符号所在行区间与文档锚区间求交」的弱校验，防止再次整批漂移。
- 置信度: 高（所有偏移量本轮按当前树逐一复算；行号可能继续漂移，故结论不依赖具体行号）
- related: M3-E-001、M3-C-001、M3-C-002、M3-A-003、M3-I-001
