# M3 定稿 · findings · E_TRACE_BREAK · P1（来源切片 L05 + L07）

> 合并代理 M3｜来源 L05 + L07｜逐条重读原文复核。
> **归口声明**：「行锚系统性失效」的根因与规模判词由 **M6b-E-002** 定稿；本条**只登记本域（校准 / 测光 / 噪声 / cosmetic）的锚点实例清单**，不含全仓规模结论，整改须与 M6b-E-002 并包。
> **取证口径**：每组锚均按「符号 → 实际起止行」重新 grep 定位后与文档锚区间比较；行号是复核时点值，源文件并发漂移中，故本条的立论单位是「锚所指符号是否在该区间」，不是绝对行号差。

---

### M3-E-002 本域 14 组源码锚中 13 组不命中其所称符号或区间（校准 / 测光 / 噪声 / cosmetic 域实例清单）
- 类别: E_TRACE_BREAK
- 建议优先级: P1（实例层；系统性判词归 M6b-E-002）
- 来源: L07-016（主）+ L05-017（合并登记，独立事实保留）
- 计数口径: 「组」= 文档中一条独立锚陈述（含多锚并列者算一组）；「不命中」= 该组内至少一个子锚所指符号不在文档声称的区间内。14 组中 13 组不命中，其中 2 组为部分不命中（各含一个仍正确的子锚）。
- 位置（实例清单，全部本轮重读）:
  1. `docs/science/PHOTOMETRY.md::§4:37` 的锚 `star_matcher.cpp:35-40` —— 该区间实为 `percentileOf`，不含任何饱和判据。**不命中**。
  2. `docs/science/PHOTOMETRY.md::§4:38` 的锚 `552-559` —— 该区间是 `sigma_residual` 计算块，不含 `>=3`/`>=2` 阈。**不命中**。
  3. `docs/science/PHOTOMETRY.md::§8:82` 的锚 `:478` —— 实为 IRLS 初值 `location=median(r)` 行，不含星数阈。**不命中**。
  4. `docs/algorithms/PHOTOMETRIC_FIT.md:::95` 的锚 `:396-403`（r 计算）—— 复核时点 r 计算在 `cleanAndScale` :405-416。漂移 2-13 行。**不命中**。
  5. `docs/algorithms/NOISE_ESTIMATION.md::§13.1:132-140` 的三锚并列 —— `fill_impl :371-429`（实际 371-418）、门面「`:411-422`」（实际 `snr_noise_model_v1_fill` :422-429）、LS「`:340-370`」（实际 `fit_plane_ls` :376-398）：**两个符号的区间被互换**；子锚「floor 回退 `:402-405`」**命中**。**部分不命中**。
  6. `docs/algorithms/NOISE_ESTIMATION.md:::140` 的「`snr_phot_cal_quality … :67-79`」—— 未标文件；按 `noise_model.cpp` 该符号实际 :276-290，按 `snr_estimator.h` :67-79 是 `PsfFitQualityRow` 区。**不可定位**。
  7. `docs/algorithms/NOISE_ESTIMATION.md:::121` 的行数断言 —— 「noise_model.cpp（466 行）；snr_estimator.h 431 行」；`read` 实测 **467 / 438** 行。**不命中**。
  8. `lib/snr_estimator/README.md:::7` —— 复述「431 行」同一错数。**不命中**。
  9. `docs/science/CALIBRATION.md::§5:53` —— `calibrator.cpp:104-136,147-179` 对 `calibrate`（实际 106-138）与 `calibrate_d`（实际 149-181）各偏 2 行；同句的 `master_generator.cpp:243-255` 子锚在并发中已被订正并**命中**步骤 3。**部分不命中**。
  10. `docs/science/CALIBRATION.md::§8:75` 的五个锚 `78-93,120,130,164,173` —— 实际 `normalize_flat` 80-95、floor 语句 122 / 132 / 165 / 175，各偏 1-2 行。**不命中**。子判据剔除：同表 :74 的 `calibrator.cpp:86` 精确命中 `if (!(med > 0.0f)) return;`（见 §4.3 第 5 项）。
  11. `docs/science/CALIBRATION.md::§9a:93` —— 「FP32 饱和语义…（见 §9）」，而 §9（:80-87）通篇无 NaN/饱和条款；该条真实落点是 §4:37。**内部条款指向失效**。
  12. `docs/science/CALIBRATION.md::§9a:94` 的锚 `cosmetic_corrector.cpp#158` —— 复核时点 :158 是 `// method=0 (median)…` 注释行，`bad_mask` 的 1=坏 赋值实际在 :130 / :151。**不命中**。
  13. `docs/algorithms/COSMETIC_ALGORITHMS.md::§13.1 锚表`（:35/:45-48/:63）—— 8 邻接「`:74-86`」→ 实际 84-96；hot「`:95-101`」→ 实际 103-109；cold「`:111-117`」→ 实际 119-127；结构过滤锚「`:70`」→ 复核时点为无关键行。**不命中（整体 +8~+10 漂移）**。
  14. `docs/algorithms/CALIBRATION_ALGORITHMS.md::§1:20` 的「根 `CMakeLists.txt:321-333` = astrocs_calibration」—— 该区间实为 `astrocs_aio`；目标实际在 :373-377。同错锚被复制到 `lib/calibration/CMakeLists.txt:12/:19`（写 `:333`），而 `docs/algorithms/COSMETIC_ALGORITHMS.md:6` 写 `:373-380` 为**正确** → 三处互斥。**不命中**。
- 补充（并发事实，仅登记不计入上表）: `lib/calibration/tests/test_photometry_apply.cpp:5` 在扫描期间新写入「根 CMakeLists.txt:329」，属第 15 组待发生失真，本轮不计。
- 权威依据: 宪章 §12.3-1/-4（锚点与证据指针必须可定位）、§11；`docs/traceability/TRACEABILITY_SPEC.md` 的锚点约定
- 问题说明: 上述 14 组的失效形态分三类，须区别处置：**(i) 纯行号漂移**（4、9、10、13、14 —— 源文件并发增长导致，符号仍在，改为符号锚即可根治）；**(ii) 指向无关符号**（1、2、3、5、6、12 —— 锚所指代码与被陈述的规则不是同一逻辑段，其中 1/2/3 恰是 M3-C-001、M3-C-002 两条 P0 的合同证据，掩盖了「合同门在代码中不存在」）；**(iii) 事实性数字/内部引用失效**（7、8 的行数，11 的条款指向）。**(ii) 类不是 R1 意义上的行号漂移**，不得以「行号会漂」豁免。
- 影响: 本域合同与 ALG 的「与源码一致」类断言（SCI-PHOT §5:65、SCI-CAL §5:53、ALG §13.1 锚表）在人工与机器复核上均不可验证；评审者按锚跳转会看到无关代码并得出与合同相反的结论。全仓规模、锚门的机器判据缺陷与根因判词见 M6b-E-002，本条不重复。
- 建议处置: (a) 本域 14 组锚逐一改指 `<path>::<符号>`（可由负责人决定与 M6b-E-002 的门改造并包执行）；(b) 第 (ii) 类的三处（SCI-PHOT:37/:38/:82）必须与 M3-C-001、M3-C-002 的裁决同时修订，不得只改行号；(c) 行数断言（7、8）改为「由机器注入门禁生成」。
- 置信度: 高（每组均本轮按符号重定位并给出复核时点区间）
- related: **M6b-E-002**（行锚系统性失效定稿点，本条为其本域实例）、**M6b-E-004**（被引对象失效定稿点）、M3-C-001、M3-C-002、M3-A-003、M3-I-001、M3-E-001
