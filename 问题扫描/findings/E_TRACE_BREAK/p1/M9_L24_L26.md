# M9 定稿 · E_TRACE_BREAK · P1（来源切片 L26）

> 合并代理: M9 · 复核时点: 全部证据按**当前树**重新定位。**表格比对口径**：对 STANDARDS_REGISTRY.md 的清单行按 `|` 分列解析后取第 3 列为状态值（不使用 read 的行文本作等值判据，避免单行 >2000 字符截断伪影）；偏差号存在性用整文件 grep 计数判定。

## M9-E-1 hips_pixel_scale 的单位违规已知（L15-002 定 P0）却未回写标准注册表：D.hips §6.3.1 行仍判 CONFORMANT，且该行偏差列挂着的是另一件事；同表相邻的 §4.2.1 可选键行遇占位值即降 PARTIAL——处理不对称

- 类别: E_TRACE_BREAK
- 优先级: P1
- 来源: L26-011（复验成立；锚点**保留**，两处清单行按列复核命中；本代理补**同表状态分布**作量化对照）
- 位置: docs/standards/STANDARDS_REGISTRY.md::D.hips 清单（§6.3.1 行、§4.2.1 可选键行、偏差汇总表、条款映射表）、docs/algorithms/HIPS_WRITER.md::§属性键段（角秒口径被文档化处）、docs/standards/checks/check_standards_registry.py::evaluate（机器门覆盖面）
- 证据摘录（逐字，复核时点现文）:
  > 主行（:98，按列解析 5 列）`| §6.3.1（客户端绘制所需的初始视场/像素尺度元数据） | 提供 `hips_pixel_scale`/`hips_initial_fov` 等客户端键 | **CONFORMANT** | lib/astro_image_io/src/hips/aio_hips_writer.cpp；docs/algorithms/HIPS_WRITER.md | DISP-HIPS-012（FIRSTPIX/LASTPIX 声明性头卡无消费方） |` —— 判据行把 `hips_pixel_scale` 写成"提供即符合"，状态 CONFORMANT，偏差列挂的是 FIRSTPIX/LASTPIX（另一件事）。
  > 对照行（:96，同法解析）`| §4.2.1（properties 可选/推荐键：hips_status/hips_estsize/hips_initial_fov） | 可选键存在时须自洽、非占位 | **PARTIAL** | … | DISP-HIPS-002（hips_estsize="1000000"、hips_initial_fov="60" 硬编码占位）；DISP-HIPS-003（hips_status 恒 "private master"） |` —— 同为"值不符合标准语义"，此处即降 PARTIAL 并挂偏差。
  > 同域相邻行（同法解析）：:95 §4.2.1 必需键集 = PARTIAL；:97 §4.4.1 = CONFORMANT（偏差列挂 DISP-HIPS-005）→ 本域内"状态与偏差列语义不绑定"的写法不止一处。
  > 量化对照（本代理按列计数）：全表条款行（以 `| §` 开头）**22 行**，状态分布 **CONFORMANT 15 / PARTIAL 6 / PROJECT_DEFINED 1**。
  > 单位违规的实体证据已在别处定档（不重报）：L15-002（产品以角秒写入 IVOA degrees 键，M2b 主落 P0）；本域回归面同构（M9-F-2）；且算法文档把该角秒口径当规范书写：HIPS_WRITER.md:186 `hips_pixel_scale = 3600·180/π·√(π/3)/nside arcsec` → **注册表判符合、实现供角秒、文档记角秒、标准要度**，四环彼此不一致而无一环报错。
  > 偏差号空缺（grep 计数口径）：`DISP-HIPS-013` 在 STANDARDS_REGISTRY.md 内命中 **0**；`DISP-HIPS-012` 命中 4（:89/:98/:117/:255）、`DISP-HIPS-002` 命中 4（:89/:96/:107/:245）→ 单位违规从未获得偏差编号，故汇总表面板上不可见。
  > 机器门覆盖（同仓事实，主落他域）：`check_standards_registry.py::evaluate` 的证据判定只要求 EVIDENCE 单元格里的路径存在（STD-REG-002 的 C3_evidence_path），不校验"状态值与偏差列是否覆盖该条款的已知违规" → 本条的账实不符不可能被机器门发现（**系统性判词主落 M8/L23-004，本条只作本站点证据、不重复定稿**）。
- 权威依据: 宪章 §12.3（登记与实现一致、机器一致性检查须能发现失配）、§13.1（符合性声明须可失败）；STD-REG-001 §1.4（CONFORMANT=提供且符合）、§3.1（登记纪律）。
- 标准原文口径（**三级复核后的诚实标注**）：本条**不依赖**外部原文即可成立（判据=同一注册表内两行处理不对称 + DISP-HIPS-013 命中 0，均为仓内事实）。至于 `hips_pixel_scale` 的单位应为度：由 **M2b-B-03 已实测取回** 的 IVOA HiPS 1.0 原文承担（`hips_pixel_scale … Unit : degrees`，URL https://www.ivoa.net/documents/HiPS/20170519/REC-HIPS-1.0-20170519.pdf §4.4.1），本代理自取失败（web_fetch 拒 application/pdf）→ 不重复取证、不另立依据。**注册表把该行挂在「§6.3.1」**这一条款号本代理未能独立核到对应内容（M2b 取回的原文里该键出现在 §4.4.1）→ 若属条款错挂，归 **L15-007（D.fits 条款锚错位）与 M7-G-001 第 2 类「引用不存在的标准依据」**同族，主落他域；本条只登记为"待核条款号"，不据此升档。
- 问题说明: 同一张表的同一"值不符合标准语义"问题类别，在 §4.2.1 行得到 PARTIAL+DISP 编号、在 §6.3.1 行得到 CONFORMANT+无关偏差编号 —— 处理不对称，且不对称的方向恰好偏向"看起来更符合"的一侧。后果：①负责人/外部审核按注册表判「HiPS 客户端元数据符合 IVOA」会被直接误导（该行是 D.hips 域里唯一为 `hips_pixel_scale` 作证的行）；②L15-002 修复之后注册表也没有翻转触发器（当前已是 CONFORMANT，修复无需动它）→ 偏差登记闭环失效；③`hips_initial_fov` 同键在 :96 已因占位值降 PARTIAL，说明该键族并非无人看管，属"看过但未改"。
- 影响: 标准符合性追溯链在 HiPS 客户端元数据这一格断开（治理面，不改数值）。注册表是 §12.3 机器门的输入，其失真会向下游（审核包、DISP 汇总、外部审阅）传播。
- 建议处置: ①§6.3.1 行状态改 **PARTIAL**，新开 `DISP-HIPS-013`（hips_pixel_scale 单位：实现供角秒、标准要度）并回挂 L15-002；②该行判据文字补单位条件（"提供且单位符合标准"），使 CONFORMANT 有可失败含义；③修复批次的验收清单显式加入"注册表行翻转 + DISP 闭环"一步（否则修完仍留假符合）；④把"状态值与偏差列必须覆盖该条款已知违规"补进 `check_standards_registry.py`（该项门机制的系统性定稿主落 M8/L23-004，本条只提交本站点）。
- 置信度: 高（按列解析复验 + 偏差号计数）
- related: L15-002（单位违规本体，M2b 主落）、M9-F-2（回归面同构，同一口径链）、M9-C-2（units/valid_range 列样板串，同类"登记面形同虚设"）、**M2b L15-006（D.fits §6 DATASUM/CHECKSUM 行同样判 CONFORMANT，:223，同型站点，主落 M2b）**、M8/L23-004（注册表机器门只判路径存在——机制半边主落，本条 related 指回，不写系统性判词）
