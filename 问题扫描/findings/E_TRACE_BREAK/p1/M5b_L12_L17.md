# M5b 定稿 · E_TRACE_BREAK · P1

> 全部锚点已按定稿时点工作树以「符号/关键片段」重定位；行号仅辅助。

### M5b-E-01 MODULE_MAP 与 L0 的 module_adapters 行锚全部错指；宪章 §F.1 不存在
- 类别: E_TRACE_BREAK · 建议优先级: P1
- 位置: `docs/architecture/MODULE_MAP.md::模块注册表行`（现 :33，引 :4257/:4282/:4309）；`docs/architecture/MODULE_MAP.md::Runtime 唯一 executor 行`（现 :32，引 :3777-3793）；真实锚 `lib/core/src/module_adapters.cpp::register_phase_modules / p1_nodes / p2_nodes / p3_nodes`（现 :5447 / :5471 / :5496 / :5523）；`docs/owner/RELEASE_STATUS.md::三 Phase 节点化`（现 :70）；`REVIEW.md::§5 表`（现 :82）；`docs/owner/SCIENCE_OVERVIEW.md::Phase1 节点化`（现 :79）；宪章章节清单（无字母节）
- 证据摘录:
  > | 模块注册表（三 Phase 节点） | `lib/core` | IMPLEMENTED | P1 八节点 / P2 七节点 / P3 五节点，各绑唯一真实 operation（宪章 §F.1） | `lib/core/src/module_adapters.cpp`:4257/:4282/:4309
  >   }                                    ← 现 :4257（文档所指处为 lambda 收尾花括号）
  >   const std::pair<ModuleDescriptor, P1NodeSpec> p1_nodes[] = {   ← 现 :5471（真实锚）
- 权威依据: 宪章 §12.3-9（删除/重命名后无悬空引用）、§12.3-12、§1.1；`docs/standards/DOCUMENTATION_STANDARD.md`（锚点可核）
- 问题说明: ①三处行锚（:4257/:4282/:4309）现落在 `inspect()` 内部与 config 解析 `catch` 块的收尾花括号上，P1/P2/P3 三张节点表实际位于现 :5471/:5496/:5523（`register_phase_modules` 在 :5447），偏差 1200+ 行——本会话内 `module_adapters.cpp` 又被并发提交追加（同一锚在 L12 归档时为 :5435/:5459/:5484/:5511，现为 :5447/:5471/:5496/:5523），说明该锚**持续漂移且无任何门会发现**。②:32 所引 `:3777-3793`（ThreadBudget 租约 / executor）同样落在无关行。③`§F.1` 被写成"宪章 §F.1"，但宪章只有 §1–§19 数字节（全文 grep `§F\.1` 命中 0），真实条文在 ARCHIVED 约束文件 :76，经 §18 supersession 现对应 §8.2/RT-001。④同一错误锚被 L0 三处复制（`RELEASE_STATUS.md:70`、`REVIEW.md:82`、`SCIENCE_OVERVIEW.md:79`），且 `RELEASE_STATUS.md:58-84` 声称该表每条"实测"级证据文件内可核。
- 影响: §12.3-9 与 §12.3-12 不成立；负责人按 L0 锚跳转落到无关代码，"IMPLEMENTED"结论不可复核。
- 建议处置: 锚改「path::符号」并纳入 `DOC-LINE-ANCHORS` 覆盖 `docs/architecture/**` 与 `docs/owner/**`；宪章条款号统一重映射。
- 置信度: 高 · related: M5b-E-05、M5b-G-09

### M5b-E-02 FROZEN 架构文档 PHASE3_MODULE_ARCH 以不存在的 lib/phase3 为模块根，四单元与已 VERIFIED 的五节点 IR 不对应
- 类别: E_TRACE_BREAK · 建议优先级: P1
- 位置: `docs/architecture/PHASE3_MODULE_ARCH.md::头部`（现 :1、:3 FROZEN）、`::§1`（现 :6-11）、`::§4 TileCache`（现 :30 区段）；`lib/phase3`（glob 0 命中，不存在）；`docs/traceability/TRACEABILITY_MATRIX.csv::MOD-astrocs-phase3-resample2`（现 :26，SRC 层 VERIFIED）；真实实现 `lib/phase3_rsmp/src/p3_resample.cpp::p3_sampler_set_max_tiles`；`lib/core/src/module_adapters.cpp::p3_nodes`（现 :5523-5528）
- 证据摘录:
  > # Phase3 模块架构 (HiPS reader → WCS → resampler → FITS writer)
  > > ID: ARCH-P3-001  状态: FROZEN (V5 ARCH-005, 2026-08-28)
  > ## 1 模块与数据结构(lib/phase3,四单元单向流)
  > [HiPSReader]  →  [TileCache]  →  [Resampler]  →  [FitsWriter]
- 权威依据: 宪章 §7.2（Phase3 模块流程）、§12.3-1/-2/-9、§8.4、§14.3-2
- 问题说明: 该 FROZEN 文档以 `lib/phase3` 为模块根路径，仓库不存在该目录（实际为 `lib/phase3_proj`/`phase3_rsmp`/`phase3_fits`/`phase3_session`）；其四单元划分不含生产 IR 的两个真实节点（`properties`、`wcs`）与 `verify` 节点（现 :5523-5528 五节点）；`TileCache` 不是独立单元，而是 `p3_sampler` 的 `max_tiles` 守卫（实现落在 lib/phase3_rsmp，README 以 `p3_sampler_set_max_tiles` 记述），且实现是 FIFO 而文档称 LRU——八层矩阵对 `resample2` 的 SRC/TEST 层已标 VERIFIED，与 FROZEN 架构互不引用。
- 影响: Phase3 产品结构（节点边界、缓存上界归属、取消粒度）权威定义不清；FROZEN 与 VERIFIED 并存使读者无法判断权威。
- 建议处置: 按 §1.2 订正路径与单元划分（与 `module_ports.registry.json`/IR 对齐），或标 `SUPERSEDED_BY` 指向现存四目录文档并在 DOCUMENT_INDEX 更新状态。
- 置信度: 高 · related: M5b-G-05、M5b-G-18

### M5b-E-03 CLI_PROTOCOL 声明的"退出码唯一源"与 JSONL schema 路径均不存在；检查器回退到第二路径，使悬空引用不可见
- 类别: E_TRACE_BREAK · 建议优先级: P1
- 位置: `docs/api/CLI_PROTOCOL_V1.md::§2 标题`（现 :25）、`::§6 规则 3`（现 :51）、`::§3`（现 :39）；`include/astrocs/exit_codes.h`（不存在，glob 0）；`schemas/jsonl_event_v1.schema.json`（不存在，glob `schemas/*.json` = 0；真实位置 `contracts/schemas/jsonl_event_v1.schema.json:63`）；`docs/contracts/API-001.md::§9`（现 :19，同指 `schemas/`）；`cli/exit_codes.h::（文件头自述）`（现 :1-2）；`tools/check_api_docs.py::check_exit_codes`（现 :153-158）；`lib/core/src/module_adapters.cpp::（validate 阶段校验 schema）`（现 :2771、:3311、:3592）
- 证据摘录:
  > ## 2 退出码(04 §2 全 11 条冻结,唯一源 `include/astrocs/exit_codes.h`)
  > 3. 退出码常量唯一源(include/astrocs/exit_codes.h, grep 无第二处数值表);
  > - schema: `schemas/jsonl_event_v1.schema.json`(API-002 建立,CLI-002 golden 用)。
  > // astrocs 退出码唯一源 (API-002 §2 / 04 §2) — CLI-002
  >             for cand in (os.path.join(self.repo, "include", "astrocs", "exit_codes.h"),
  >                          os.path.join(self.repo, "cli", "exit_codes.h"),
- 权威依据: 宪章 §12.3-9（删除/重命名后无悬空引用）、§8.1、§11
- 问题说明: ①两处"唯一源"声明指向不存在的 `include/astrocs/exit_codes.h`（真实文件 `cli/exit_codes.h`，其文件头又把权威写成 `API-002 §2 / 04 §2`，而 `docs/contracts/` 只有 `API-001.md`，无 API-002/003/005）⇒ 权威 ID 悬空。②`check_api_docs.py::check_exit_codes` 按 `include/astrocs → cli/ → include/astrocs/*.hpp` 三候选回退取第一个存在的文件 ⇒ 该门永远绿，文档声明的路径不存在不会被发现（检查器把文档错误吸收掉了）。③`schemas/jsonl_event_v1.schema.json` 与 `API-001.md:19` 的 `schemas/` 顶层路径不存在，真实路径为 `contracts/schemas/`，而 `module_adapters.cpp` 的 phase config 校验（现 :2771 等三处）也硬写 `schemas/phase3_config_v1.schema.json` 同一非真源前缀 ⇒ 按文档路径加载校验会失败，而 `check_api_docs.py::check_schema_files` 只 glob `docs/api/schemas` 与 `contracts/schemas`，不含 `schemas/` 声明面。
- 影响: 退出码/ schema 的权威指针不可解析（§12.3-9）；机器门因回退而失去发现能力。
- 建议处置: 文档与代码注释统一到 `cli/exit_codes.h` 与 `contracts/schemas/`（或把头文件搬到 `include/astrocs/` 作为唯一路径）；检查器取消候选列表，只认文档声明的那一条路径；补 API-002 合同文件或改指 API-001。
- 置信度: 高 · related: M5b-C-05、M5b-G-15

### M5b-E-04 acs_status/acs_head 在 legacy 与 abi 头族重复定义，其声称的机器检查文件不存在
- 类别: E_TRACE_BREAK · 建议优先级: P1
- 位置: `include/astrocs/abi/status_codes.h::（权威声明 + acs_head + acs_status + 值域）`（现 :9-10、:25-29、:151-166）；`include/astrocs/common_abi_v1.h::（同名 typedef）`（现 :20-23、:25-41、:55-67）；`tests/abi/run_abi_checks.sh`（不存在，全仓仅 :10 注释与 L12 档案引用）；`docs/standards/API_STANDARD.md::（禁重定义规则）`（现 :11）
- 证据摘录:
  > 冻结规则（权威 = 控制包 12_DLL_ABI_AND_LOADER_STANDARD.md §1/§4 + ARC-001 FORBID-004; 机器检查见 tests/abi/run_abi_checks.sh）:
  > 二者不得在同一 TU 混合 include（legacy 随模块迁移退役, ABI-002/006 处理收编）。
  > typedef enum acs_status {      ← common_abi_v1.h:55 与 abi/status_codes.h:153 各一次
- 权威依据: 宪章 §8.6、§12.3-6/-9；`docs/standards/API_STANDARD.md:11`（"禁重定义公共类型（机器检查：头文件包含扫描）"）
- 问题说明: 两个头族在 C 的单一全局命名空间中定义**同名类型** `acs_status`、`acs_head`、`acs_span_{u8,f32,f64}`，枚举集不同（abi 族多 `ACS_ERR_EXCEPTION=10`），且 `include/astrocs/abi/status_codes.h:23` 的注释自己承认"同值同义，**非重定义而是同名并行定义**"。唯一防线是一句"不得在同一 TU 混合 include"，而它指定的机器检查 `tests/abi/run_abi_checks.sh` 在全仓不存在（glob 0 命中）⇒ 悬空权威 + 悬空检查。另 `API_STANDARD.md:11` 声称"机器检查：头文件包含扫描"，`tools/check_abi_boundary.py` 的扫描集不含任何 `abi/` 头（M5b-G-13）⇒ 该规则同样无实现。附带的值域不可判定：`module_api_v1.h:48` 的 `phase` 值域写成 `1|2|3|catalog=0?`（带问号），三处定义互斥。
- 影响: 混 include 会造成同 TU 编译期重定义错误，只能靠人工纪律；追溯链中"有机器检查"的声明为假（§12.3-6/-9 与 §17-2）。
- 建议处置: 以 `abi/` 族为唯一类型源，legacy 头别名退役；补 `tests/abi/run_abi_checks.sh` 或把注释改指 `test_abi002_lifecycle.py` 并真加混合 include 负例；把 `ACS_ERR_*\|ACS_ANN_*\|ACS_DIAG_ECODE_*\|phase\|kind\|execution_class\|ABI 结构集` 纳入生成头单一提供。
- 置信度: 高 · related: M5b-G-13、M5b-C-04

### M5b-E-05 治理引用层把 ARCHIVED 文件的字母条款号写成"宪章"，且十项映射表与检查器双向不一致
- 类别: E_TRACE_BREAK · 建议优先级: P1
- 位置: `docs/owner/ARCHITECTURE_OVERVIEW.md::§1 表`（现 :18，"Windows 优先：宪章 §H"）；`REVIEW.md::8 发布口径`（现 :133）；`docs/owner/RELEASE_STATUS.md::§0/§1/§3/§6/§8`（现 :7、:13、:65、:94、:99、:109-111）；`docs/owner/SCIENCE_OVERVIEW.md::（§D 与 §K 双前缀）`（现 :57、:135）；`ASTROCS_PROJECT_CONSTITUTION.md::章节清单`（§1–§19 数字节，无字母节；`§H\|§F.1\|§K\|台账\|waiver\|REVIEW_PENDING\|NOT_STARTED` 全文 grep 命中 0）；`AGENTS.md::映射表`（现 :14、:16、:28）；`tools/check_agents_gov.py::REQUIRED`（现 :6-18）；`docs/standards/RELEASE_STANDARD.md`（现 :19）；`docs/traceability/TRACEABILITY_SPEC.md`（现 :104、:127、:150）；`docs/contracts/API_CONTRACTS.csv::（API-002/003/005 权威列）`；`docs/contracts/API-001.md::（文档 ID 自我声明）`
- 证据摘录:
  > | Windows 优先：交付形态、工具链锁、禁止本机裸构建（宪章 §H） | ACTIVE_NORMATIVE | …
  > 本 Agent 无权宣布发布（宪章 §1.2/§H）；
  > 治理：`ASTROCS_PROJECT_CONSTITUTION.md`（§D/§H）
  > 台账与 `waiver` 唯一语义源为 ARCHIVED 的 §D.7；当前仓库无任何 `TASK_LEDGER.csv`
- 权威依据: 宪章 §1.1、§12.3-1/-9；`AGENTS.md` 自述「条款号已逐条核对宪章实际内容，不得臆造」
- 问题说明: ①这些字母条款号（§H/§D/§F.1/§K）在宪章中命中数为 0，真实条文在 `AstroCS_ENGINEERING_CONSTRAINTS.md`（§D.1-§D.7、§K.2 等），而该文件按宪章 :660 与自身 :10 是 ARCHIVED_NON_NORMATIVE ⇒ "指向不存在的章节"的悬空引用，在宪章指定的负责人入口成规模存在。②同一治理事实因此有两个文字源且互不一致（`RELEASE_STANDARD.md:19` 用"宪章 §D.1/§F.1"描述 Fatduck 边界与每节点唯一 operation）。③`TRACEABILITY_SPEC.md:104/:127/:150` 三处把 `ARCHIVE_MANIFEST.mf` 归给"宪章 §K.2"；`API_CONTRACTS.csv` 与 `API-001.md` 的权威 ID 写 `API-002/003/005`，而 `docs/contracts/` 只有 `API-001.md`。④`AGENTS.md` 声明"本表 10 项与 REQUIRED 一一对应；维护规则：必须同提交订正"，但实际键集与语义不对应（门键 `main-only\|amd64\|节点\|cpu-only\|单入口\|资源门禁\|无硬编码\|alpha/发布\|状态机\|不停工`，表首列只对齐 4 项，余 6 项写成条款描述）而门不检查 ⇒ 表与实现双向不一致。
- 影响: §12.3-1/-9 不成立；负责人按 L0 路由会解析到已废止文本。
- 建议处置: 全仓字母条款号 → 宪章数字节映射表 + 悬空引用门（复用 `check_doc_refs\|check_dead_refs.py`，现未挂 CI）；AGENTS.md 首列改用门键；补建或改指缺失的 API-00x 合同。
- 置信度: 高 · related: M5b-G-08、M5b-G-09、M5b-E-01

---
