# M5a 定稿 · E_TRACE_BREAK · p1 · 来源切片 L11
> 代理：M5a（第二层合并验证）。全部条目均在复核时点按当下树重读；位置一律 path::符号，行号仅为其时点参考。处置表/复算/剔除与降级/移交见 ../../../_merge/M5a.md。


### M5a-E-001 线程/预算文档的 path:line 锚点批量漂移并断言「全部有效」，另有指向不存在路径的引用
- 类别: E_TRACE_BREAK
- 建议优先级: P1（原 L11-016 报 P2；规程 §2 将「锚点漂移或悬空引用」列为 P1 典型，且 FROZEN 文档主动断言锚点全部有效，故上调）
- 来源: L11-016
- 位置:
  - `docs/architecture/THREADING_MODEL.md::确定性锚点`（:20 upm.cpp:495、:21 CMakeLists.txt:18 与 af763de、:22 drizzle_engine.cpp:1662/1751/1834/1843）
  - `docs/architecture/THREAD_BUDGET_ARCH.md::§4`（:32 原样复读这批锚点并断言「旧 THREADING_MODEL.md §确定性锚点全部有效」）
  - `docs/architecture/EXECUTION_MODEL.md::§1`（:10 `drizzle_engine.cpp:1662 reduction`、:11 `sampler.cpp:604, CMakeLists.txt:18 OFF`）
  - `docs/architecture/execution_options_contract.md::约束`（:39-40 引 `工程控制/schemas/stage2.schema.json`）
  - `docs/owner/PIPELINE_OVERVIEW.md::§5`（:131 引 `module_adapters.cpp:3777-3793`）
  - 真实位置：`lib/healpix_db/healpix_drizzle/drizzle_engine.cpp`（reduction 实在 :1673-1674；:1662 是 `static std::atomic<std::uint64_t> s_target_cache_gen`；:1751 是 `if (fine) {`；parallel reduction 实在 :1853/:1862）；`lib/phase2/src/upm.cpp::Huber IRLS 注释块`（:495 为 `[B4-27 排异锚点 …]` 注释，权重语句经 `p2_upm_raw_weight` 于 :528/:545）；`lib/phase2/CMakeLists.txt::option(P2_ENABLE_OPENMP ...)`（:28）；`lib/core/src/module_adapters.cpp`（现 :3777-3793 为 ivar tile 读 fail-closed 段，行带 work unit 注记在 :4889/:5301）
- 证据摘录:
  > - Phase2 UPM 权重归一：`lib/phase2/src/upm.cpp:495` `compute_raw` — `raw_w = quality_factor * control_ivar` …
  >     // [B4-27 排异锚点 — REJECTION.md 迭代剔除/阈值契约, SCI-UPM 权重 SCI-UPM-WEIGHT-001 关联, 不改语义]
  - THREAD_BUDGET_ARCH.md:32：「浮点归约顺序冻结(旧 THREADING_MODEL.md §确定性锚点全部有效: upm.cpp:495/sampler 串行/drizzle_engine.cpp:1662,1751,1834,1843)」
  > schema 见 `工程控制/schemas/stage2.schema.json`（`properties.execution`）。
- 权威依据: 宪章 §12.3-8（文档并发模型与真实线程入口一致）、§12.3-9（删除/重命名后无悬空引用）、§12.3-12；规程 E_TRACE_BREAK
- 问题说明: 复算：THREAD_BUDGET_ARCH.md:32 断言「全部有效」的四个锚点中，`drizzle_engine.cpp:1662` 指向 atomic run-gen 计数器声明而非 reduction（真值 :1673-1674）、`:1751` 指向 `if (fine) {`、`:1834/:1843` 与真实的 :1853/:1862 不符、`sampler 串行` 与实现矛盾（另见 M5a-C-002）、`upm.cpp:495` 指向注释行而非所述语句。`lib/phase2/CMakeLists.txt` 的 option 实在 :28（文档写 :18）。`execution_options_contract.md:40` 引用的 `工程控制/schemas/stage2.schema.json` 在真源中不存在（glob 复算：`工程控制/schemas/` 为空目录，同名件只在 `run/**` 影子树与 `engineering/control/archive/**` 归档内）→ 活动架构文档指向已消失的控制包路径。`PIPELINE_OVERVIEW.md:131`（L0）给出的 `module_adapters.cpp:3777-3793` 现落在 ivar fail-closed 代码段，与「Phase3 resample 行带提交唯一池」的说明无关。并发期间 `module_adapters.cpp` 行号大幅漂移（前台记录 5023→5435 量级），故本条位置一律以符号/语句给出。
- 检查器作用域（本轮已判定，非"待复核"）: 真源存在锚点门 `docs/algorithms/anchors/check_doc_line_anchors.py`（CI 项 `DOC-LINE-ANCHORS`，`ci/checks.json:3292-3319`，三 profile、waivable=false），但其合同 `docs/algorithms/anchors/anchor_contract.json:5-8` 的 `doc_globs` 只有 `docs/science/*.md` 与 `docs/algorithms/*.md` —— **本条全部漂移锚位于 `docs/architecture/**` 与 `docs/owner/**`，落在锚点门的作用域之外**。故定性为「机器门未覆盖该文档层」，不是「检查器失效」；宪章 §12.3-9（无悬空引用）在 ARCH/L0 层无实现。
- 影响: §12.3-8/-9 的机械核对无法进行（锚点表面存在、实则指向语义无关代码）；FROZEN 文档关于「确定性归约顺序」的论证建立在不存在的锚点上，审核按图索骥即取错现场。
- 建议处置: 锚点改「文件 + 符号名/语句字面量」并由 anchor 检查器覆盖 `docs/architecture/**` 与 `docs/owner/**`（须先核实 `docs/algorithms/anchors/check_doc_line_anchors.py` 的扫描面，见 _merge 待复核）；删除或改指真源以替换 `工程控制/` 路径引用；订正 THREAD_BUDGET_ARCH.md:32 的「全部有效」断言。
- 置信度: 高
- related: M5a-C-002、M5a-D-002、F00-01（矩阵双头，M6 主落）、L12-017/L18-008（同类锚点漂移，前台「行锚系统性失效」主题）
