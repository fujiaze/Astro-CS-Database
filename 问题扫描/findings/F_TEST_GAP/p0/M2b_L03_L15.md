# M2b 定稿 — F_TEST_GAP（测试与独立证据缺口）P0

> 合并代理：M2b。来源切片 L03（19）+ L15（22）。证据锚一律「path::符号」。

---

### M2b-F-01 D.healpix 判 CONFORMANT 所依的「astropy-healpix 百万点独立 oracle / 往返 ≤1e-12 deg」在仓内不可复现：生成器与数据缺位、测试未注册 ctest、现存容差与极区口径差约 10 个数量级
（两代理独立收敛簇②：L03-006 与 L15-013 并档为一条根因，双方证据均保留）
- 类别: F_TEST_GAP
- 建议优先级: P0
- 位置: docs/standards/STANDARDS_REGISTRY.md::D.healpix COMPLIANCE 字段 + 清单行 1/2/3（§5.1/§5.2/§5.3 行）+ ::D.healpix 偏差表「（无）」；lib/common/healpix/healpix_core.h::文件头 Oracle 声明；lib/common/healpix/tests/test_healpix_oracle.cpp::main（argv 取 oracle.jsonl）、::往返比较段、::极点跳过段、::容差式；lib/common/healpix/tests/（目录清单）；tests/unit/CMakeLists.txt::healpix 相关 add_test 段；lib/common/healpix/tests/gen_spatial_fuzz.py
- 证据摘录:
  - 注册表（原文）:「- COMPLIANCE: CONFORMANT」、「DEVIATION: 无域内偏差（HEALPix 核心零偏差…）」、「| §5.3（ang2pix/pix2ang 往返） | 球面角 ↔ NESTED 索引往返在 FP64 下达机器精度 | CONFORMANT | lib/common/healpix/tests/test_healpix_oracle.cpp；lib/common/healpix/tests/snr_hips_spatial_oracle.py；docs/algorithms/HEALPIX_MAPPING.md | 无（往返 ≤1e-12 deg；astropy-healpix 百万点 oracle 对拍） |」
  - 头注释（原文）:「外部 Oracle: astropy-healpix（独立实现）1,000,000 全天随机点 + 极点/锚点（order 0..22）交叉验证, mismatch=0、父子不变量 1e6/1e6、tile 映射 262144/262144」
  - 测试依赖外部数据（原文）:「// 读取 healpix_fullsky_oracle.py 生成的 JSONL（order/ra/dec/ipix），逐行比较 ang2pix_nest」＋「if (argc < 2) { fprintf(stderr, "usage: %s <oracle.jsonl>\n", argv[0]); return 2; }」
  - **可复现性实测（本次）**：glob 「healpix_fullsky_oracle*」与「**/oracle.jsonl」**均 0 命中**（L03 在案亦为 0）；lib/common/healpix/tests/ 目录内唯一生成器是 gen_spatial_fuzz.py，且「--order」默认 **单阶 7**（非全天多阶），并标 NON_PRODUCTION_TOOL_ONLY
  - 现存测试的真实口径（原文）:「if (std::fabs(dec) >= 89.999999) continue;」（跳过极点）＋「if (d > 1.2 * hp_res_deg + 1e-9) { ++bad_roundtrip; }」（容差 = 1.2 倍像素角分辨率）＋「if (lines == 0 || …) FAIL」
  - 在册面（实测）:tests/unit/CMakeLists.txt 只注册 r9b_healpix_neighbors（healpix 相关唯一 ctest）；test_healpix_oracle 不在册；另有 lib/phase1/session/test_hips_tile_mapping.cpp 存在但**未进 tests/unit/CMakeLists.txt**
  - ALG 侧同样以该 Oracle 为锚（原文，HIPS_WRITER.md）:「astrometry.net healpix.c，astropy-healpix 百万点 oracle 已对拍」＋把 gate7_hips_validate.py / HIPS_VERIFY 列为 Oracle 生态
- 权威依据: 宪章 §13.1（科学正确性须由独立 Oracle 交叉验证；不得以自家实现互证）、§12.3-12（结论须可追溯到机器证据）、§17.11（禁无证据的合规声明）；STD-REG-001 §1.4（CONFORMANT 须有在位可执行证据）、§1.2；Górski 2005 §5.1/§5.2（被声称验证的映射本身）（URL: https://arxiv.org/html/astro-ph/0409513）
- 问题说明: 四项量化承诺（1,000,000 点、order 0..22、mismatch=0、≤1e-12 deg）在仓内既无生成脚本也无冻结样本，且无 ctest 注册 → 不可复现；即使补数据，现存实现的判定口径也只是「1.2 倍像素角分辨率」并**显式跳过极点 ±89.999999°**，与「≤1e-12 deg、含极点/锚点」相差约 10 个数量级且丢掉最易出错的区域。因它是**未注册的外部证据**（不在 ctest 里，等于永不执行），按前台裁决按 F_TEST_GAP 定档；其对注册表符合性结论的破坏面记入本条，注册表行本身的改判要求随 M2b-B-08（登记面失真）一并提出。
- 影响: D.healpix 的 COMPLIANCE: CONFORMANT 与「无域内偏差」失去支撑（HEALPix 是全仓 tile 映射与 Phase1/2 空间索引的唯一权威核心）；极区与 order 边界两个已知高风险面无任何证据；ALG-HIPS 与 SCI 层的「已对拍」措辞被下游当作既有事实引用。
- 建议处置: ① 把独立 Oracle 生成器与冻结样本（含 SHA256 + 生成环境版本）入库，并在 tests/unit/CMakeLists.txt 注册 ctest（样本缺失时显式 FAIL，不得 SKIP 为绿）；② 容差按声明改写（真实口径若只能到 1.2×像素，就把注册表与头注释改写为该口径并挂 DISP）；③ 补极点与 ±90° 专项（当前 continue 使极区零覆盖）；④ 把 test_hips_tile_mapping.cpp 一并注册；⑤ 在 Oracle 就位前把 D.healpix 的 §5.1/§5.2/§5.3 三行降为 PARTIAL 并登记偏差 ID。
- 置信度: 高（glob 0 命中 + 在册面实测）
- related: L02-001, L04-004, L07-006, L08-004（跨域「独立 Oracle 缺位」主题，按前台要求写入 related）；M2b-B-07、M2b-B-08、M2b-C-01（写侧一致性依赖此 Oracle）、L03-006、L15-013
- 来源: L03-006 + L15-013（并档）
