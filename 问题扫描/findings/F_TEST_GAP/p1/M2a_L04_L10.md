# M2a 定稿 · F_TEST_GAP · P1（来源切片 L04+L10）

> 合并代理: M2a · 复核时点: 全部证据按**当前树**重新定位（path::符号）。

## M2a-F-2 by_coords 的深树路径零测试覆盖：全部 fixture 只建单叶树，四叉树递归分支从未被执行

- 类别: F_TEST_GAP
- 优先级: P1
- 来源: L10-009
- 位置: tests/unit/gaia_xpsd_fixture_gen.c::xpsd_write_test_file / ::xpsd_write_spectrum_file（nodeCount 处）；tests/unit/gaia_xpsd_fixture_sp_gen.c；lib/gaia_xpsd_client/src/gaia_client.c::search_recursive / ::search_recursive_spectrum / ::search_recursive_photometry；docs/algorithms/GAIA_QUERY.md::§5 测试设计（深树形态）
- 证据摘录（逐字，复核时点现文）:
  > （gaia_xpsd_fixture_gen.c）nodeCount="1" —— 两个 writer（普通/光谱）各写一次，均为单叶
  > （gaia_client.c:1415 / :1555 / :1692）if (node->child_nw) search_recursive(xf, tree, node->child_nw, …) —— child_nw 恒 0 时三个入口的递归分支永不进入
  > （GAIA_QUERY.md §5 测试设计）Oracle：`oracle_cone_search` 线性全扫 `sources_size`…与树查询双向包含断言（`gaia_cat_test` D 模式）。多文件合并 / **深树** / 降级 / 零命中四形态按 §2.7 与合同负向面展开。
- 权威依据: 宪章 §13.1（边界/失败路径/极端参数）、§12.3-4；ALG-GAIA-001 §5 自述的「深树」形态
- 问题说明: 合同自述测试含「深树」形态，但注册面使用的两个合成 fixture 生成器只输出 `nodeCount="1"` 的单叶四叉树，故叶区间边界、多 depth 递归、child 索引推进、跨子树去重路径全部无执行覆盖；线性 Oracle 的双向包含断言因此只验证了「根叶自比较」。叠加 M2a-C-9（root/child 偏移不设边界）与 M2a-C-1（部分未命中零用例），深树与降级两形态实际不存在。
- 影响: 树检索核心分支回归不可见；fixture 或解析改动引入的漏查/重查/越界不会被现有门发现。
- 建议处置: 给 fixture 生成器加深度参数（至少 depth=2/3 与「叶数>1、跨子树边界命中」三形态），在注册面（gaia_cat_* ctest）补 D 类双向包含断言 + 深树用例。
- 置信度: 高
- related: M2a-C-9、M2a-C-1、M2a-A-3、M2a-F-1

## M2a-F-3 两处已落地修复均无在线回归保护：TSAN 竞态用例未注册、恒零天测列无任何断言

- 类别: F_TEST_GAP（前台指令第 2/5 项：「改过但没人守住」须独立成条，不得只在合并表划掉）
- 优先级: P1
- 来源: L10-007 的修复半边 + 本轮 freshness 复核
- 位置: 实例①（竞态）lib/gaia_xpsd_client/test/test_gaia_race.c；lib/gaia_xpsd_client/CMakeLists.txt（无该 target）；tests/unit/CMakeLists.txt（grep `gaia_race` → NO HIT）；ci/checks.json；对照 lib/gaia_xpsd_client/src/gaia_client.c::块缓存锁封装段。实例②（恒零天测列）lib/gaia_xpsd_client/src/gaia_client.c::cone 结果装配（calloc 显式置 0）与 ::GaiaSpectrumStar；lib/gaia_xpsd_client/src/module_entry.c::cone 行 schema；tests/unit/gaia_cat_test.c、::gaia_adapter_test.c、::gaia_integration_test.c
- 证据摘录（逐字，复核时点现文）:
  > （gaia_client.c:300-306）/* B4-P1-2: block_cache 锁封装。… match 类查询（gaia_client_query_spectrum_by_coords 等）并行轴=坐标, 多线程并发读写同一文件的 block_cache … = 无同步数据竞争 (UAF)。修复: block_cache 读写全部持 per-file 锁; 解压缓冲使用调用方线程局部 scratch, 不共享。 */
  > （lib/gaia_xpsd_client/test/test_gaia_race.c 在树内存在；grep `test_gaia_race` 于 lib/gaia_xpsd_client/CMakeLists.txt 与 tests/unit/CMakeLists.txt → **NO HIT**）
  > （ci/checks.json）无 gaia race/TSAN 检查项（grep `gaia` → NO HIT）
  > （实例②，复核时点检索）tests/** grep `parallax` → **0 命中**；而 gaia_client.c:1829/:1888 已把 parallax/pmra/pmdec/source_id 由「未初始化」改为 calloc 显式置 0，module_entry.c:751-753 仍把这四列写进 cone 行 schema 对外发布 ⇒ 该修复与恒零下发行为都没有任何在册断言
- 权威依据: 宪章 §13.1（并发/资源竞争须有测试）、§12.3-6/-10（测试须绑定当前 target；禁历史状态冒充当前）、§10.6
- 问题说明: **实例①**：锁修复本身在当下树内成立（per-file BcLock + 线程局部 scratch），但其唯一验证件 `test_gaia_race.c` 位于模块私有 `test/` 目录、不被任何 CMake target 编译、不进 ctest、不进 CI，且需要 TSAN 构建才有效——即该修复**在 CI 意义上无人守住**：后续任何人把某处 `bc` 访问挪到锁外，现有门都不会发现；同模块七项注册测试（gaia_cat_*）是单线程/单池口径，覆盖不到该竞争面。**实例②**：「未初始化字段显式置 0」的迁移已完成，但全测试面对 parallax/pmra/pmdec/source_id **零引用**，既没有断言它们恒零，也没有断言「不得当作天测值使用」——即该 UB 修复同样无人守住，且 M2a-C-12 的恒零列下发行为被固化为无测状态。
- 影响: 已闭缺陷的复发风险不可见；与 M2a-F-1（drizzle Oracle 未注册）同属「测试存在但不在线」的跨域模式。
- 建议处置: ① 给 `test_gaia_race.c` 建 target（TSAN profile）并注册进 `tests/unit/CMakeLists.txt` + `ci/ctest_baseline.json`；② 若 TSAN 门禁另有域级任务，则在 module.yaml/CI 登记其归属与生效条件，避免「有文件=有保护」的误读；③ 在 `gaia_cat_test` 在册面加一条断言：cone 行结果的 parallax/pmra/pmdec/source_id 恒为 0（或按 M2a-C-12 的处置改为不发布/带 invalid），把「占位」钉成合同行为而不是巧合。
- 置信度: 高
- related: M2a-C-8、M2a-F-1、L10-007

## M2a-F-4 球面 drizzle 的注册测试面不含任何 pixfrac<1 的正面数值用例，M2a-A-1 的偏差因子无人守住

- 类别: F_TEST_GAP（前台指令第 2/5 项：关键参数路径缺在线保护）
- 优先级: P1
- 来源: 由 L04-002 派生（本轮新增，叶子未单列）
- 位置: lib/healpix_db/healpix_drizzle/tests/p1drz/p1drz_tests_core.cpp（正例配置构造与负面矩阵两处 pixfrac 取值）；tests/p1drz/CMakeLists.txt::add_test（在线门清单）；对照 docs/science/DRIZZLE.md::§11 验证 Oracle
- 证据摘录（逐字，复核时点现文）:
  > （p1drz_tests_core.cpp 全篇）正例/不变量/Oracle 的配置构造一律 `make_cfg(NSIDE, 1.0, …)`；（:370）`for (double pf : {0.0, -0.5, 1.5})` —— 负面矩阵只测**被拒**的三值
  > （p1drz/CMakeLists.txt:48-72）六个 add_test（p1drz_units/properties/oracle/negative/performance/selfcheck）= 当前唯一在线 drizzle 门
  > （DRIZZLE.md:115-119区段）- **常数场流量守恒（逐像素 `max_abs==0`）**… / pixfrac 相关用例见 §11 声明面
- 权威依据: 宪章 §13.1（性质测试须覆盖参数合法域）、§13.2、§5.3；SCI-DRZ-001 §11
- 问题说明: 在线门里所有能跑通的正面对拍都在 pixfrac=1.0 上做，此时 `A_drop=A_pixel`、1/pixfrac² 因子恰好为 1 ⇒ M2a-A-1 的系统性偏亮在**任何被 CI 执行的断言下都是隐形的**；SCI §11 声明的 pixfrac 扫描用例面只存在于未注册文件里（M2a-F-1）。这不是测试写错，而是关键科学参数的合法开区间 (0,1) 零覆盖：缺陷即便按 M2a-A-1 修复，也没有对照能证明修对了。
- 影响: 面亮度归一语义无回归保护；修复验证缺前置。
- 建议处置: 在注册 `p1drz_oracle` 面内补 pixfrac∈{1.0,0.8,0.5} 三值的常数面亮度对拍（Owner 裁决后按正确归一写期望），并加「跨 pixfrac 结果一致性」性质断言。
- 置信度: 高
- related: M2a-A-1、M2a-F-1、M2a-C-6
