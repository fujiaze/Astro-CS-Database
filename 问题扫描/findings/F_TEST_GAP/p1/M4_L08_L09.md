# 定稿 L08_L09 — Phase2 全链域（UPM/控制采样/覆盖 + 排异/积分/写出/会话）

> 合并代理 M4｜来源 L08+L09｜验证方式：逐条重读原文（当前树即时 read/grep；行锚以符号/关键语句重定位，标注「复核时点行号」，并发修复中可能漂移）
### M4-F-03 产品 ctest「p2_upm_synthetic」不链接生产库且以 CHECK(true) 冒名不变量门、打印 PASS 结论
- 类别: F_TEST_GAP
- 定稿优先级: P1（来源 L08-006 原议 P1，成立）
- 位置: tests/unit/CMakeLists.txt::p2_upm_synthetic_test（复核时点 :668-671）；tests/unit/p2_upm_synthetic_test.cpp:1-12（include 面）、:125-129（复核时点）
- 证据摘录:
  > target_link_libraries(p2_upm_synthetic_test PRIVATE astrocs_contracts)
  > // 6) 禁 zero-support 生成值: 无重叠单帧区不虚构值 (harmonic continuation 只填覆盖)
  > CHECK(true);  // 语义: 合成框架无单覆盖污染 (C_f 只在有 overlap 的 cell 定义)
  > std::printf("P2-003 TESTS PASS (三块重叠 pairwise, gauge 闭合, 接缝下降, 源保留, 加性非乘性)\n");
- 权威依据: 宪章 §13.1/§13.2；协议 P1「断言空洞/名不副实」
- 问题说明: 产品构建内唯一以 UPM 命名的 ctest 只链 astrocs_contracts，全文 include 无 astro/phase2 头——C_A/C_B/接缝差全为测试文件自算中位数算术自演，从不经过 p2_upm_build/p2_upm_calibrate_block；末段以常量断言冒名「禁 zero-support 生成值」门并打印「TESTS PASS（…加性非乘性）」。TRACEABILITY.csv TEST-UPM-003/004 行登记的执行载体（p2_upm_synthetic_test.cpp）因此对生产代码零覆盖。
- 影响: CI 绿灯对 UPM 生产代码零覆盖贡献；SCI 不变量（加性非乘性/源不被拟合）在产品测试面无真实检验者。
- 建议处置: 改造为链接 astrocs_phase2 的真实回归（自算期望可保留作独立 oracle），或改名/降格为文档示例并从测试登记面移除。
- 置信度: 高
- 来源: L08-006
- related: M4-F-01、M4-F-07（同型空断言）、M4-A-02

### M4-F-04 tests/api UPM 确定性/恢复/接缝门永久静默跳过；驱动 calibrate_sum 恒打印 0.0，worker 等价从未被比较
- 类别: F_TEST_GAP
- 定稿优先级: P1（来源 L08-007 原议 P1，成立；接缝门 test_seam_metric_gate.py 同型并入本条证据面）
- 位置: tests/api/test_upm_parallel.py:14,:55,:69（复核时点）与 :49-50；tests/api/test_upm_recovery_oracle.py:17,:68,:79；tests/api/test_seam_metric_gate.py:22,:26,:37（M4 补充）；docs/algorithms/PHASE2_UPM_IMPL.md §12 T1-T4 锚（:319-321）
- 证据摘录:
  > OMP_LIB = os.path.join(REPO, "build", "linux-openmp-on", "libphase2.a")
  > @unittest.skipUnless(shutil.which("g++") and os.path.isfile(OMP_LIB), "需要 g++ + OpenMP phase2 lib")
  > cls.drv, OMP_LIB, os.path.join(AIO, "astro_image_io.dll"),
  > printf("…calibrate_sum=%.10f consistent=%d…\n", workers,(int)nret,forced_hash,0.0,consistent,first,peak);
- 权威依据: 宪章 §13.2（验证必须可执行）、§12.3-12；IMPL §12 T1-T4 把这两个文件列为现状测试锚
- 问题说明: 三个文件构建前置均要求 build/linux-openmp-on/libphase2.a——OpenMP 路线已移除（IMPL/SAMPLER 均述 std::thread 化），该制品在产品树不存在（build/ 亦为 gitignore 现场），skipUnless 使整类静默跳过（pytest 记 skip 即绿）；Linux g++ 命令行还直链 Windows 制品 astro_image_io.dll。test_upm_parallel 驱动内 :42-46 已计算 csum 但 :49-50 printf 传常量 0.0——跨 workers 的 calibrate 数值等价从未被比较；docstring 声称的 TSan 验证无执行面。IMPL §12 T1-T4「现状测试锚」因此是可达性为零的锚。
- 影响: worker 数无关性（D1）与恢复真值面两条 SCI 声明在 CI 无任何实际执行者；静默 skip 掩盖验证缺失。
- 建议处置: 改为对 astrocs_phase2 目标产物设门（产品构建必产），去掉 OpenMP 时代路径与 dll 直链；csum 打印改回计算值并补 1T vs NT hash+输出比对。
- 置信度: 高
- 来源: L08-007
- related: M4-F-01、M4-A-05（seam 门同型）、M4-F-02

### M4-F-05 UPMW-005「MC 硬门」实为 [0.98,2.0] 宽区间判据，与文件头自述断言相反，且不锚定 1.3883/1.4
- 类别: F_TEST_GAP
- 定稿优先级: P1（来源 L08-008 原议 P1，成立；文件头矛盾句为 M4 复核新增证据）
- 位置: lib/healpix_db/healpix_drizzle/tests/control_median_mc_test.cpp::判据段（复核时点 :217-219）与头注释 :16；docs/science/PHASE2_UPM.md:106,142；docs/algorithms/UPM_SOLVER.md:98
- 证据摘录:
  > if (!(k_corr >= 0.98 && k_corr <= 2.0)) {
  >     std::printf("[FAIL] k_corr 超出科学合理区间 [0.98, 2.0]\n");
  > //   - UPMW-005 断言 |k_corr_frozen − k_corr_empirical| 在容差内（非精确相等，MC 波动）
  （头注释 :16 声称断言的是「冻结值与实测值之差在容差内」；实际 :217-219 只判宽区间——自述与实现相反）
- 权威依据: SCI-UPM-001 §11 UPMW-005（k_corr≈1.3883 硬门）、§14:142；宪章 §6.3（冻结常数绑定验证）
- 问题说明: 任何落在 [0.98,2.0] 的重标定结果都「通过」——包括 M4-C-01 插值漂移后复算值（1.4136/2.9584 之一即在区间内）；既不比对 1.3883/1.4，也不落盘 pixfrac×scale 矩阵；且该测试本身未挂任何构建（M4-F-03 类 (b)）。k_corr 与标定表的绑定关系在测试面为空。
- 影响: ±2% 量级权重漂移类回归不可能被现有「门」捕获；「MC 硬门」名不副实。
- 建议处置: 登记为 F2/T8 类精确角点门（表值 exact；MC 复现值与 1.4 的偏差阈值由负责人冻结），接入产品构建。
- 置信度: 高
- 来源: L08-008
- related: M4-C-01、M4-F-01

### M4-F-06 backend「每节点执行证据」测试断言依赖 p2_session 日志串——当前生产 IR 链不发这些日志；5/7 节点无逐节点证据
- 类别: F_TEST_GAP
- 定稿优先级: P1（来源 L09-008 原议 P1，成立；CI 实际红绿需执行 → §6 待复核注）
- 位置: tests/backend/test_p2006_canonical_pipeline.py::test_01（复核时点 :84-96，断言句 :91-92；自述降级句 :88-90/:113）；tests/backend/test_p2007_joint_gate.py:73,:227-228；日志串唯一出处 lib/phase2_session/p2_session.cpp:116,:148,:177；生产路由 cli/commands.cpp::cmd_phase2_run（:1026-1027）与 cli/runtime_client.cpp:173-205；执行面 ci/checks.json::UT-BACKEND（:1345-1377，waivable:false，linux-main）
- 证据摘录:
  >         for frag in ("stage coverage ok", "stage sample ok"):
  >             self.assertIn(frag, self.res.stderr, ...)
  >             w = _session_budget_workers(res.stderr)；self.assertIsNotNone(w, "session 日志缺 budget workers 注入证据")
  > stderr 不含 “session run: budget workers”（p2_session 未产出预算证据）
  （p2_session.cpp:116/:148/:177；grep「stage coverage」于 lib/core 与 lib/phase2/src 零命中——节点链无此日志）
- 权威依据: 宪章 §12.3-7（Pipeline 静态图与实际 trace 一致）、§13.2-4；§6.2（七节点=不同算法符号的验收锚）
- 问题说明: M4 独立复证：astrocs phase2 run → cmd_phase2_run → run_pipeline（Runtime IR 子图，7 节点 bind 于 module_adapters 的 p2_op_*），全程不调 p2_session_run（:1027 的 ev.stage("phase2_session") 仅是事件标签）；被测二进制 build/astrocs 必含节点链。故 p2006:91-92/p2007:73,:227 所断言的三条 session 日志串在现路由下必不出现——静态证据指向「必红」；但 UT-BACKEND 为非豁免门且 ci/known_failures.json 无 p2006/p2007 挂账、ci/ci_repair_round.py:110 历史登记其失败归因于 resource gate——红/绿终态须执行定夺（§6 待复核，M5/L12 同域）。若其当前为绿，则必存在另一条使日志成立的路由（同样违 RT-008 注释）——两情形均构成追溯断裂。叠加测试自述：「Registry 注册 + final ok 承载」五节点（upm_fit/upm_apply/reject/integrate/write）无逐节点执行证据、observed_trace 无落盘载体。
- 影响: P2-006/007 VERIFIED 的证据链与真实执行链脱钩；§6.2 节点独立性验收失去测试锚；节点级取消/预算/资源联动验证虚化。
- 建议处置: 以节点 manifest（p2_op_* 已在 man 写 operation/entry 标记）或 observed_trace 落盘为断言源重写；补 IR trace 文件载体；同步 p2007。
- 置信度: 中高（静态完整；CI 终态待执行核实）
- 来源: L09-008
- related: M4-C-07、M4-G-02；M5（UT-BACKEND/CI 挂账域）、L12

### M4-F-07 p2_rejection_test/p2_output_semantics 含空断言与名不副实节（枚举值大小序/局部字面量非空/全零求和充当语义门）
- 类别: F_TEST_GAP
- 定稿优先级: P1（来源 L09-009 原议 P1，成立）
- 位置: tests/unit/p2_rejection_test.cpp::§4（复核时点 :60-72）、§5（:74-83）、§6（:85-90）；tests/unit/p2_output_semantics_test.cpp::§1（:19-32）、§5（:132-137）、打印句 :140
- 证据摘录:
  >   // 6) integration 语义: mean/weighted mean/variance/support + frame identity
  >     // 语义验证: reason 输出与 frame_id 解耦 (kernel 不知 frame_id)
  >     CHECK(true);
  >     for (const char* name : {“signal”, “support”, “ivar”, “variance”}) {
  >         CHECK(name[0] != ‘\0’);
- 权威依据: 宪章 §12.3-4、§13.1；PHASE2_REJECTION/PHASE2_INTEGRATION §13 把这类文件列为 TEST 证据
- 问题说明: §4 构造 20 样本 fixture 却从不送入 kernel（vals/rng 未使用），仅断言枚举常量数值大小序；§5 对全零数组求和判 0；§6/§5 为 CHECK(true) 占位；p2_output_semantics 标题宣称「writer 输出语义」但全文未链接 writer，§1 对本地字面量数组判非空字符。成功打印语（「P2-005/P2-006 TESTS PASS」）为追溯表引用面背书。真正有效的仅 §2/§3/4b/4c 的 reducer 面断言（4b/4c 恰好是 B2-A7 回归门，保留其效力）。
- 影响: 单元测试层「已验证」虚增；integration/frame identity/writer 名义覆盖不存在。
- 建议处置: 删除或坐实空节（产品目录断言 + kernel 注入门）；追溯表只登记真断言面。
- 置信度: 高
- 来源: L09-009
- related: M4-F-03、M4-F-08、M4-A-02（4b/4c 有效面的出处）

### M4-F-08 test_p2004「生产 Oracle」实为 compat 弃用接口 + 伪独立断言；compat frame_ids 映射存在错绑缺陷且无回归门
- 类别: F_TEST_GAP
- 定稿优先级: P1（来源 L09-010 原议 P1，成立；含修复无门事实）
- 位置: tests/backend/test_p2004_reject_integrate.py:3-8,:41-48,:157-172；lib/phase2/include/astro/phase2/rejection.h:299（COMPAT 注释）；lib/phase2/src/rejection.cpp::p2_reject_stack compat 体（复核时点 :1863，frame_ids 复制 :1888-1889）
- 证据摘录:
  > “““test_p2004_reject_integrate.py — P2-004 (G5) 排异/积分生产 Oracle。
  > C++ driver 调正式 kernel(p2_reject_stack / p2_reject_plan_resolve / p2_integrate_pixel)执行合成场景:
  > … cosmic ray / hot pixel / … frame identity / all-rejected; 拒绝计数可追溯。
  （docstring 声称面）；driver :41-48 实际走 P2SampleStackView+p2_reject_stack——rejection.h:299 自注「旧接口（COMPAT adapter，仅测试/旧调用；生产 Stage2 不再调用）」；sv.frame_ids = nullptr（:43）——「frame identity」无载体；「variance/ivar」在 p2_integrate_pixel 面根本无输出字段
  >     if (in->frame_ids != nullptr)
  >         for (std::uint32_t i = 0; i < m; ++i) fids[i] = in->frame_ids[i];
  （rejection.cpp:1888-1889——compact 下标 i 直取原始数组，未经 eligibility 掩码映射；一旦传入非空 frame_ids 且发生任何剔除即自首个被剔样本起错绑）
- 权威依据: 宪章 §13.1（验证真实实现）、§12.3-4；PHASE2_REJECTION/PHASE2_INTEGRATION 把 test_p2004 列为生产 Oracle 证据（TEST-P2-REJ/INT 设计面）
- 问题说明: ①名义「生产 Oracle」用 compat 接口，绕开生产 strided gather/source_indices/ex 面——生产路径缺陷（如 M4-C-06 权重不检查、M4-A-01 小栈回退）不可能被其暴露；②compat 的 frame_ids 映射为错绑缺陷（代码层，无现行调用方触发但属公共 API 面）；③Python 层 test_02..test_05（:157-172）只是复读 driver 退出码/"P2-004 DRIVER PASS" 字符串，无独立判定（docstring 声称逐项）。注：B2-A2/F-P2-002 系列对**生产 strided 路径**的 frame-slot/掩码修复（代码注释自述）没有对应的 strided 面回归测试——「改过但没人守住」形态，本条为其登记点。
- 影响: TEST-P2-REJ/INT 设计面引用该文件为「生产等价」证据失去效力；compat 错映射与 strided 修复均无暴露门。
- 建议处置: 迁 p2_reject_stack_ex+gather 生产签名；Python 侧解析结构化输出逐项断言；修 :1888-1889 或给 compat 补显式映射与回归门。
- 置信度: 高
- 来源: L09-010
- related: M4-C-06、M4-F-07、M4-A-01

