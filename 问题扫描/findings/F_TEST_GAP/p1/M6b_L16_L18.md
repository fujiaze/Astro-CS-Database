# M6b · F_TEST_GAP 定稿 P1（来源切片：L16 / L18 处置后果）

> 合并代理 M6b。

---

### M6b-F-001 本域全部"建议新增的判据"today 无回归防护：追溯试金石只覆盖结构类 6 个 fixture，锚门 11 个负例不含"界内错锚"，视图同构判据无 fixture，且 fail-open 门自身无任何测试

- 类别码: F_TEST_GAP
- 优先级: **P1**
- 位置:
  - `tests/traceability/fixtures/::全部`（实测 6 个：bad_id_format.json、chain_break.json、empty_cell.json、duplicate_module.json、missing_column.json、dangling_ref.json）
  - `tests/traceability/test_traceability_matrix.py::RELS/build_env/test_01..test_04`（现 :26-45、:50-84）
  - `tests/quality/test_doc_line_anchors.py::t01..t03 / f01..f11`（现 :97-:183）
  - `tools/traceability/check_traceability_matrix.py::_check_csv_parity`（现 :374-411）
  - `tools/quality/check_traceability.py`（无任何测试引用，实测 `grep "quality/check_traceability" tests` = 0 命中）
  - `tools/check_traceability.py::main`（其试金石 `tests/traceability/test_traceability.py:42-46` 只断 `returncode==0` 与 `TRACEABILITY_PASS claims=66` 字面量）
- 证据摘录:
  > （test_traceability_matrix.py:26-31）CHECKER = ... / FIXTURES = os.path.join(REPO, "tests", "traceability", "fixtures") / RELS = { "docs/traceability/TRACEABILITY_LAYERS.csv": ..., "schemas/traceability_matrix.schema.json": os.path.join(REPO, "contracts", "schemas", ...) }
  > （test_traceability.py:45-46）self.assertEqual(r.returncode, 0, ...) / self.assertIn("TRACEABILITY_PASS claims=66", r.stdout)
  > （fixtures/dangling_ref.json:28）"test_path": "modules/conformance/noop/tests/unit/noop_handshake_test.c",  ← 六个 fixture 的 `test_path` 一律是**真实测试文件**
  > （test_doc_line_anchors.py:124-152）def test_f01_target_file_deleted / f02_target_truncated_out_of_range / f03_symbol_moved_out_of_range / f05_anchor_to_missing_file / f06_anchor_out_of_bounds …
  - 实测缺口对照：①无任何 fixture 的 `test_path` 落在 `docs/` 且 `test_status=VERIFIED`（而真矩阵 18/22 正是该形态）→ "TEST 层锚指向文档"这条最重要的语义收紧一旦实现，**没有负例守护它**；②无 fixture 覆盖 `evidence_status=VERIFIED` 但证据不可定位（27/30 MISSING 的形态亦无 fixture）；③`_check_csv_parity` 在真树可判，但 6 个 fixture 全为 JSON，`build_env` 不复制矩阵 CSV → 视图不同构无独立回归样本（本域 L18-006 的误判正是这条无测试路径造成的）；④`test_doc_line_anchors.py` 的 11 个负例都是"文件删/界外/符号移出/豁免过期/歧义 basename"，**没有"行号在界内但指向错误代码行"**（即本域实测到的 owner:70 三锚形态）；⑤`tools/quality/check_traceability.py`（唯一被登记为非豁免、名字即 TRACEABILITY 的门）无任何测试引用；`tools/check_traceability.py` 的试金石只断"跑通 + 计数 66"，正好把它的 fail-open 与"判的是预发布快照"一起固化。
- 权威依据: 宪章 §12.3（机器一致性检查须真正生效）、§13.2 第 1/3 层（单元与合同测试须覆盖判据）、§14.4（最小充分校验 fail-fast）、§17.2。
- 问题说明: 本域提出的收紧（逐层取值域、TEST 锚必须是测试、EVIDENCE 须可定位、notes 自述未接线不得 VERIFIED、界内错锚检测、两表交比）全部落在"门判据"上；而门判据的变更 today 没有对应的负例集合，等于允许"改一次绿一次、下次同样失效"。同时现存的试金石对 fail-open 与快照口径是**反向锁定**的（断言 returncode 0 与固定计数），使这些缺陷更不可能被发现。
- 建议处置: ①补 4 个矩阵 fixture：`test_anchor_is_doc.json`（TEST VERIFIED + `test_path` 在 docs/）、`evidence_verified_unresolvable.json`、`unwired_row_all_verified.json`（notes 含 `entrypoint=MISSING`）、`view_csv_mismatch`（CSV 视图与 JSON 不同构）；②补锚门负例 `in_bounds_but_wrong`（界内指向错行）与 `bare_table_cell_anchor`（裸 `| :NN |`）；③为 `tools/quality/check_traceability.py` 加"有断链必非零"的负例，并把 `tools/check_traceability.py` 的试金石从"计数 66 字面量"改为"临时表注入断链 → 期望非零/期望错误码"；④`build_env` 的 schema 目标路径改 `contracts/schemas/`（与 `SCHEMA_REL` 一致）。
- 置信度: 高（fixture 清单与断言均实测）
- 来源: 本域定稿条目的处置后果（L16-001、L16-002、L16-003、L16-008、L16-011、L16-016、L18-006 剔除后的替代登记）
- related: M6b-G-001、M6b-G-004、M6b-E-002、M6b-E-005
