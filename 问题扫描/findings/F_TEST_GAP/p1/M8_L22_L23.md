# M8 定稿（L23 + L22）— F_TEST_GAP / P1

> 合并代理 M8｜来源 _cache/L23.md（14 条）+ _cache/L22.md（10 条）｜纯只读复核（read/grep/glob），锚点在当前树按符号重新定位。

### M8-F-005 60 个 C/C++ 测试源零注册；而唯一针对注册面的门方向相反（只审「已注册目标是否登记」），此类缺口对 CI 永久隐形

- 类别码: F_TEST_GAP
- 优先级: **P1**
- 位置: 全清单见 _cache/L23.md §1.3（drizzle 25 / astro_image_io 17 / gaia C 2 / modules/services/io selftest 2 / cpu baseline 3 / photometric_calib cpp 2 / p1phot 影子树 1 / 其余散点）；门 `tools/quality/check_ctest_registration.py::collect_real`、`::ADD_TEST_NAME_RE`、契约块 C1-C6/S1-S7（复核时点 :8-31）；注册面 `ci/checks.json::CTEST-REGISTRATION`；`tests/cpu/baseline/run_provider_oracle_checks.py` 文档块（:12-13）
- 证据摘录:
  > （check_ctest_registration.py 契约）C1 扫描活动 CTest 面全部 CMake 源，解析 add_test(NAME <target> ...) ... C3 未覆盖目标 → FAIL ... C5 基线漂移：ctest_baseline.json 中已不存在于 CMake 源的目标 → FAIL
  > （同文件 :10）出现非 NAME 形式 add_test 亦判违规
  > （run_provider_oracle_checks.py:12-13）provider query/self_test/export ABI 完整: 由 handshake/capability 测试 (C) 与 provider_so_load 冒烟覆盖 (见 run_cpu_baseline_checks.py)。   ← 被指向的文件全仓不存在
- 权威依据: 宪章 §13.1/§13.2（可复用验证面必须真实存在且执行）、§12.3-4、§14.4
- 问题说明: 该门是 CI-REG-002 的正例标杆（能失败、有 S1-S7 负例自检、C6 还防「登记但未真跑」），但其审计对象只能从 `add_test(NAME ...)` 出发 ⇒ 集合是「已注册目标」的子集判定，**没有任何规则枚举磁盘上的测试源** ⇒ 方向①（存在但从未注册）对它完全隐形。本轮独立抽验（口径=逐 basename 全仓 grep，排除 run/build/artifacts/evidence/reports/问题扫描；再看是否出现在 CMakeLists 非注释行 / ci/** / *.sh / *.ps1 / Makefile / tools/**/*.py）：29 个 `lib/healpix_db/healpix_drizzle/tests/*.cpp` 中 21 个三处全零、8 个仅命中 legacy 打包清单或追溯脚本（`tools/quality/build_v19r4_package.py:107`、`v19r3_traceability.py:75/119/218/227/318`、`tools/redteam_v19.py:100/192`，均为非阻断面，其中 v19r3 系脚本硬编码 WSL 路径且未挂 checks.json），`drizzle_nonfinite_test` 在 CMake 中仅出现于注释行 → **61/61 抽验通过，零注册结论成立**。最硬子事实：avx2/avx512 各有 runner（UT-CPU-AVX2/UT-CPU-AVX512 注册），唯一必然执行的 baseline 路径的三个 C 探针（handshake/capability/so_load）**无任何 runner、无任何 CMake/ci 引用**，而其文档把 ABI 冒烟推给不存在的 `run_cpu_baseline_checks.py`。另 `lib/phase1/tests/p1phot/p1phot_tests_core.cpp` 与已注册的 `lib/photometric_calib/tests/p1phot/` 同名并存（影子树）。
- 影响: 大量真实回归防护（drizzle 逆映射/科学完成度、HISS 写读、gaia 竞态与所有权、psf NaN 排序、snr 对账、IO selftest）从未执行；CI 绿的解释力被高估；baseline ISA 路径无 ABI 冒烟门。
- 建议处置: ①注册门反向化（= M8-G-001 三门建议 ②）：枚举 tests/**、lib/**/tests/**、modules/**/tests/** 下测试源，未出现在 add_test 且不在显式 LEGACY/ARCHIVE 白名单即 FAIL；②确认废弃者移 archive/删除（待负责人裁决）；③补 baseline runner 或修正文档指向（并 M8-A-001）。
- 置信度: 高（门契约逐行读 + 61/61 抽验复算；分母口径不可全量复算，见 _merge §2 注）
- 来源: L23-005（定稿；60 项清单与 4,512,403 字符分母为叶子口径，本代理只抽验，不背书该分母）
- related: M2a-F-1（drizzle oracle 未注册 P0，主落 M2a）、M4-F-01（MC/kcorr 未注册 P0，主落 M4）、L23-004→M8-F-004、L04-004、L15-013；元结论 M8-G-001 方向①

### M8-F-006 `lib/**` 与 `tests/realdata` 的 Python 测试不在任何阻断采集面；其中一批 test_* 以 return 布尔收尾（即使命令也永不失败）

- 类别码: F_TEST_GAP
- 优先级: **P1**
- 位置: `lib/photometric_calib/cpp/test/test_photometric_calib.py::test_sip_wcs`（复核时点 :269 `return t4_ok`）、`lib/photometric_calib/cpp/test/test_spectrum_integrator_golden.py`（:302 `return ok, provenance`、:320 `return False, []`、:345 `tolerance = 0.01`）、`lib/astro_image_io/tests/test_psf_fit_handler.py`、`lib/astro_image_io/tests/test_export_fits_fix.py`、`lib/orchestrator/tests/test_orchestrator_e2e.py`；采集面 `ci/checks.json::UT-*.command`（实测 discover -s 共 18 处：16 个 tests/* + ci/tests + lib/gaia_xpsd_client/tests）、`::DEEP-COV-PY`（:2109-2142）、`tools/quality/ci_coverage_runner.py::--tests`（:104-105 默认 "tests"）
- 证据摘录:
  > （ci_coverage_runner.py:104-105）ap.add_argument("--tests", default="tests", help="pytest 收集根（仓库相对，默认 tests）")
  > （test_photometric_calib.py:269）return t4_ok
  > （test_spectrum_integrator_golden.py:345）tolerance = 0.01  # 1.0% 相对误差容差
  > （checks.json 实测）18 个 discover -s 目录中无 lib/photometric_calib/cpp/test、无 lib/astro_image_io/tests、无 lib/orchestrator/tests、无 tests/realdata；全仓唯一注册的 lib 侧 discover 根是 UT-GAIA-ZLIB（:3105-3121 → lib/gaia_xpsd_client/tests）
- 权威依据: 宪章 §13.1（误差合同/独立 Oracle/负面路径必备且必须执行）、§13.2 层级 3-4、§12.3-4、§13.3（真实数据终验）
- 问题说明: 三重缺陷。①路径不被采：DEEP-COV-PY 的 pytest 根为 `tests`，`lib/**` 连 deep 都不采；18 个 discover 目录亦不含它们（**订正叶子口径**：并非"lib 面完全无注册"——lib/gaia_xpsd_client/tests 已注册 UT-GAIA-ZLIB，故本条范围限于上述 5 文件 + tests/realdata）。②形态不可失败：一批 `test_*` 以 `return` 布尔收尾而非 assert/raise；pytest 对被测函数返回非 None 只发 warning、不判失败 ⇒ 即使补上采集项也不会红。③口径脱钩：golden 容差写 1.0%/5%，仓内 docs/science 无出处；golden 还存在「exe 未找到 → return False」的静默降级。文件头自标 NON_PRODUCTION_TOOL_ONLY，但其内容（F_syn 一致性、SIP 校验、PSF 拟合）属科学合同面。
- 影响: 测光标定一致性、WCS/SIP 校验、PSF 拟合、drizzle 适配器等面无阻断保护；"补采集项"这一常规整改对本批文件无效（假绿会换个位置继续）。
- 建议处置: ①return 型改 assert/raise（或提供 `load_tests`）；②为 lib 模块测试建 discover 项（profile/waivable 明示），或把 DEEP-COV-PY 的 `--tests` 扩到多根；③门：discover 采集数>0 硬门（M8-G-001 ①）必须与「返回非 None 即 FAIL」的 pytest 配置（`-W error::pytest.PytestReturnNotNoneWarning`）配套，否则只治第①重。
- 置信度: 高（采集根实测 18 个；return 型逐文件核对）
- 来源: L23-006（定稿；范围与「13 个文件/12 个函数」计数按本代理实测收窄，见 _merge §4）
- related: L07-006、M8-F-004、M8-G-001

### M8-F-007 「X 或 true」短路恒真断言：资源门判定与 HiPS tile 结构合同被写成永真（可绕过现有 CHECK(true) 字面串探针）

- 类别码: F_TEST_GAP
- 优先级: **P1**
- 位置: `tests/unit/p1_resource_test.cpp::(4) 2 核 heavy gate`（复核时点 :78）、`tests/unit/p1_hips_writer_test.cpp::(3) verify: tile 文件存在`（复核时点 :89）；同文件整改自证 :80-85；机器门 `tools/quality/known_failures_baseline.py::(F-022 探针)`（:912-916）
- 证据摘录:
  > （p1_resource_test.cpp:78）CHECK(evaluate_gate(g) == astrocs::GateDiag::Ok || true);  // compute 判定由调用方注入均值
  > （p1_hips_writer_test.cpp:89）CHECK(tile.find("XTENSION= 'IMAGE   '") != std::string::npos || true);
  > （p1_resource_test.cpp:81-85 注释）旧断言 `>= 0 || == 0` 恒等于 `>= 0` … 断言自相矛盾。改为有界检查   ← 同一文件已识别并整改过同类恒真
  > （known_failures_baseline.py:913）p3_vacuous = "CHECK(true)" in p3_assembly_test   ← 探针只查这一文件这一字面串
- 权威依据: 宪章 §10.5/§18.2（资源门为硬门）、§12.3-4、§13.1（负面路径）；IO-002/DRIZZLE 写出面结构合同
- 问题说明: 两处 CHECK 条件尾部带逻辑或 true ⇒ 永真，宏内 `!(cond)` 分支不可能进入。p1_resource_test 已在注释中承认并修过同类恒真（RSS 斜率），但 `evaluate_gate` 判定仍残留 ⇒ 即「资源门是否真判定」从未被断言，只断言阈值公式；HiPS 侧「tile 是否为 IMAGE extension」永不被检查。全仓 `CHECK(.*|| true)` 正则实测恰命中这 2 行（口径=排除免报区后逐行），与叶子「39 处核查、2 处确证」一致。该形态还能绕过仓内唯一以恒真断言为对象的机器门（F-022 只查 p3_assembly_test.cpp 的 "CHECK(true)" 字面串）。
- 影响: §18.2 硬门的出口诊断与 HiPS 写出结构无断言保护；语义被改不会被发现。
- 建议处置: 删除尾部或-true 并补真实输入（compute 分支注入均值后断言 Ok，再加一条低利用率必 LowAvgCores 的负向用例）；把该形态纳入 sciencelint 与基线探针正则（M8-G-002 建议）。
- 置信度: 高（两处逐字 + 全仓正则复算）
- 来源: L23-007（定稿，仍成立）
- related: M4-F-03（CHECK(true) 冒名，M4 主落）、M8-F-002、M8-F-015（同类恒真面）、M8-G-002、L08-006、L09-009

### M8-F-008 十份同构 harness 的未知组名 fail-open：组名漂移即「0 检查仍打印 TESTS PASS 且 rc=0」（同仓已有正例写法但未回灌）

- 类别码: F_TEST_GAP（⑦变异视角）
- 优先级: **P1**
- 位置: `lib/calibration/tests/p1cal/p1cal_test_main.hpp::run_all_groups`（复核时点 :82-121）、`lib/healpix_db/healpix_drizzle/tests/p1drz/p1drz_test_main.hpp::run_all_groups`（:98-）、`lib/photometric_calib/tests/p1phot/p1phot_test_main.hpp::run_all_groups`（:143-）、另 p1cos :87 / p1noise :86 / p1wcs :186 / p1psf :130 / p1hips :155 / p1sess :136 / 影子树 lib/phase1/tests/p1phot :109；正例 `lib/star_detector/tests/p1star/p1star_test_main.hpp::run_all_groups`（:85-101）、`lib/astro_image_io/tests/p2hips/p2hips_test_main.hpp:64`、`tests/unit/aio_abi_test_main.hpp:111`；注册面 `lib/healpix_db/healpix_drizzle/tests/p1drz/CMakeLists.txt:22-29`
- 证据摘录:
  > （p1cal）int total_fail = 0; for (i..n) { if (group != "all" && group != groups[i].name) continue; ... }  /  if (total_fail == 0) { std::fprintf(stdout, "P1CAL TESTS PASS (group=%s)\n", group.c_str()); return 0; }
  > （p1star 正例 :96-99）if (want && !found) { std::printf("unknown group '%s'\n", want); return 2; }
  > （grep 实测）"unknown group" 全仓命中 3 个文件：p1star、p2hips、aio_abi_test_main.hpp（**订正 L23 的"仅 p1star"**）
- 权威依据: 宪章 §13.1（每模块必备六类测试且可复用）、§14.4（fail-fast 最小充分校验）、§12.3-4
- 问题说明: 10 份 p1* 套件共用同一模板 main：按 argv 组名过滤循环，组名不匹配任何条目则循环体一次都不执行，total_fail 保持 0 ⇒ 打印 TESTS PASS 并 return 0。仓内已有三处正确写法（found 守卫 + 未命中非零退出），证明可失败写法存在且成本极低，但未回灌 9 份。变异视角：把 CMake 里的 `p1drz_oracle` 改成 `p1drz_oracl`、或新增组却忘了在 TestGroup 表登记，都会让该 ctest 静默零检查且 CI 全绿（基线目标计数不变、日志还显示 PASS）。
- 影响: Phase1 六模块全部 units/properties/oracle/negative/performance/selfcheck 组存在被静默清零的路径，是整条 ctest 面的可信度缺陷。
- 置信度: 高（9 份同构 + 3 份正例逐字对照）
- 建议处置: ①把 found 守卫抽成公共头回灌所有 run_all_groups，未匹配组名一律非零退出；②CI 增一条「每个 p1* 套件的每组至少被执行一次」的日志聚合判定（现有 selfcheck 机制已具备同类能力）。
- 来源: L23-008（定稿；正例数由 1 订正为 3）
- related: 新登记（跨缓存 grep run_all_groups 未见同事实）；与 M8-G-001 方向②同族

### M8-F-009 资源门测试把 0.80 系数钉成期望值、用被测函数自造通过输入，且元测试把「注册表不得含 --gate-required」钉为期望（退不改假绿、真整改假红）

- 类别码: F_TEST_GAP（⑥错位 + 反向钉死）
- 优先级: **P1**
- 位置: `tests/unit/cpu_monitor_test.cpp::(4) gate 阈值`（复核时点 :72/:76/:79）、`tests/unit/p1_resource_test.cpp::(4)`（:77）、`tests/unit/p2_seam_gate_test.cpp::(2) 资源门`（:71）；被测 `cli/resource_gate.h::compute_cores_threshold`（:182-185）、`::kCpuP50MinPercent`/`::kCpuMeanMinPercent`（:94-95）；反向钉死面 `ci/tests/test_ci001_failclosed.py::(gate 旗标不变量)`（:128/:137/:139/:152）；采集面 `ci/checks.json::CI-BINDING-TESTS.command`（:2380-2409，`-p test_ci001b_*.py`）
- 证据摘录:
  > （cpu_monitor_test.cpp:76）CHECK(std::fabs(compute_cores_threshold(g) - 0.8) < 1e-9);
  > （p1_resource_test.cpp:77）CHECK(std::fabs(compute_cores_threshold(g) - 1.6) < 1e-9);   // 注释：阈值公式 0.80*min(2,2)=1.6 核
  > （resource_gate.h:95 vs :185）inline constexpr double kCpuMeanMinPercent = 85.0;  ...  return (m >= 1 ? 0.80 * static_cast<double>(m) : 0.0);
  > （p2_seam_gate_test.cpp:71）g.avg_equivalent_cores = compute_cores_threshold(g) * 1.2;  // 超阈值
  > （test_ci001_failclosed.py:128）"""裁决一致化：全部 requires_monitor 检查命令均不含 --gate-required。""" + :137/:152 self.assertNotIn("--gate-required", ...)
- 权威依据: 宪章 §10.5 + §18.2（负责人裁决：计算区间平均 CPU 利用率不低于已分配容量 85%、任何连续 10 秒低于 60% 即失败）、§13.1、§14.5（每个修复须有独立可验证面）
- 问题说明: 三个事实叠加，且第三点是本代理新证。①两个测试把 0.80 系数写成期望值（1.6、0.8），即把与宪章 §18.2 冲突的废止口径钉成回归基线 ⇒ 按 §18.2 整改时测试先变红（真整改假红）；②p2_seam_gate 用「被测实现算出的阈值 ×1.2」当通过输入，期望值来自被测对象，属自证式断言，且同文件对 evaluate_gate 判定值从不取反验证；③`--gate-required` 在 `tests/**` 与 `cli/**` 零命中（全仓命中面仅 ci/run.py、tools/monitoring、docs、以及 ci/tests 的元测试），其中 `test_ci001_failclosed.py` 用 assertNotIn 把「注册表零 --gate-required」钉成期望 ⇒ 门缺失也被固化为规格；同时该文件不在 CI 采集（CI-BINDING-TESTS 的 pattern 只命中 `test_ci001b_*.py`，2/21），故这条反向钉死连被执行都谈不上。**分工说明**：0.80 与 85% 的口径冲突本体由 M5a-G-001（P0）主落；B2-A18 的真实观测装配已有 mon001_gate 回归保护（M5a 会签接受）⇒ 本条**不**再主张「修复未固化」，只定稿测试面与元测试面的钉死事实。
- 影响: §18.2 资源门在「阈值口径」「判定值」「注册表旗标」三层都无有效回归保护，且退化方向被测试写成期望；整改者面对的是"改对了会红"。
- 建议处置: ①0.80 期望值改按 §18.2 的 85% / 60%·10s 口径书写（或与负责人确认口径后在 docs/science/INTEGRATION.md 登记，与 M5a-G-001 同批处理）；②p2_seam_gate 改为独立算出的绝对核数并对负例断言 LowAvgCores；③元测试的反向不变量改为「重计算面必须请求判定」（同 M5a 建议），并把 CI-BINDING-TESTS 的 pattern 恢复为全量 discover（M5b-F-01 主落）。
- 置信度: 高（三处测试 + 两处被测源码 + 元测试逐字；grep 计数自查）
- 来源: L23-009（定稿；子事实③「装配无回归」按 M5a 会签下修剔除）
- related: **M5a-G-001**（口径本体主落）、M5a-G-003、M5b-F-01（pattern 只命中 2/21 主落 M5b）、L17-004/F00-03、L11-004→M5a；「反向锁定」同族 M5b-G-0x

### M8-F-010 修复未固化：B2-A14 新增 PHOTDEGRADE / uncalibrated_adu_allowed 显式降级路径已实现并回写文档，但无任何回归用例钉住

- 类别码: F_TEST_GAP
- 优先级: **P1**
- 位置: `lib/healpix_db/healpix_drizzle/hp_drizzle_api.cpp`（复核时点 :935-938）、`lib/healpix_db/healpix_drizzle/drizzle_engine.cpp`（:1030-1033、:1950-1953）、`drizzle_engine.h::uncalibrated_adu_allowed`（:36）、`lib/core/src/module_adapters.cpp`（:2269/:2326/:2330/:2351 上游发射面）；文档 `docs/algorithms/DRIZZLE_GEOMETRY.md:178`、`docs/contracts/DATA_SEMANTICS.md:249`
- 证据摘录:
  > （hp_drizzle_api.cpp:937-938）const char* photdegrade_str = aio_frame_kv_get(frame, "header", "PHOTDEGRADE"); / config.uncalibrated_adu_allowed = (photdegrade_str && std::atoi(photdegrade_str) != 0);
  > （drizzle_engine.cpp:1030-1033）// B2-A14: 未显式声明 uncalibrated_adu_allowed 时保持原拒绝语义； ... !config.uncalibrated_adu_allowed) {
  > （grep 实测）PHOTDEGRADE / uncalibrated_adu_allowed 的 17 处命中全部落在 docs/2、lib/core/4、drizzle 源与头/11；`tests/**` 与 `lib/**/tests/**` 命中 **0**
- 权威依据: 宪章 §13.1（每模块负面路径与性质测试）、§12.3-4、§14.5（修复须独立可验证）；SCI-DRZ 对未测光 ADU 产物写出的语义约束
- 问题说明: 「未做测光缩放即拒绝写出」这条硬约束的豁免开关（显式声明 PHOTDEGRADE=1 时允许 PHOTAPPL=0/ADU 产物）是本轮修复新增的行为分支，两侧分支都无测试：既无「声明后放行且 provenance 正确记录降级」的正例，也无「声明为 0/拼写错误/空串仍拒绝」的反例。现有 tests 侧只有 PHOTAPPL 元数据用例（p1001_real_nodes_test），与该开关正交。上游 module_adapters 已在真实节点链发射该 KV ⇒ 该分支生产可达。
- 影响: 未测光 ADU 数据可能被静默当作已标定产物进入下游（科学正确性风险）；后续重构（把 atoi 换成别的解析、改默认值）无护栏。
- 建议处置: 在 p1drz_negative 与 p1001_real_nodes 各补一例（=1 → 写出成功 + provenance 标 degraded/ADU；缺键或 =0 或非法串 → 拒绝并给出 §8 合同错误串）；并把该开关登记进 DATA_SEMANTICS 的验证列。
- 置信度: 高（源码分支与 grep 空集双证）
- 来源: L23-010（定稿，仍成立）
- related: L04-005、L05-004（同形态先例）；「修复但无回归」判定纪律见 _merge §2

### M8-F-011 tests/realdata 的真实数据验证（36 用例）不在任何阻断 profile，仅可能出现在可豁免的 linux-deep 覆盖率面；且其形态本身不可失败

- 类别码: F_TEST_GAP
- 优先级: **P1**
- 位置: `tests/realdata/test_match_plan.py`、`tests/realdata/test_index_v12.py`（**重锚**：L23 原锚 `test_realdata_index_v12.py` 全仓不存在）、`tests/realdata/__init__.py`（该目录实测 3 文件）；采集面 `ci/checks.json::UT-*`（18 个 discover 目录无 realdata）、`::DEEP-COV-PY`（profiles=[linux-deep]、waivable=true）；引用面仅 `tools/realdata/README.md:48/52/53`、`REVIEW.md:92`
- 证据摘录:
  > （glob 实测）tests/realdata -> __init__.py, test_index_v12.py, test_match_plan.py
  > （grep 实测）两文件 `def test_` 合计 36（15 + 21），`class .*TestCase\|unittest` 命中 **0**
  > （checks.json 实测）discover -s 共 18 处，无 tests/realdata
- 权威依据: 宪章 §13.3（真实数据终验为必备层级）、§13.2 层级 6、§14.5（P0/P1 清零须有执行过的证据）
- 问题说明: 采集面只有两处可能：①任何 UT-* discover 目录（不在内）；②DEEP-COV-PY 的 pytest（根 tests 会采到），但该 profile 为 linux-deep 且 waivable=true（缺 pytest 即合同化 SKIP、整体可豁免）⇒ 真实数据匹配计划/索引 v12 的验证不在阻断面上。次级风险：其形态为裸 `def test_*`，若将来改用 unittest discover 会静默采集 0 用例（同 M8-F-001 机制）。
- 影响: §13.3 要求的真实数据终验缺少可阻断的机器门；「deep 跑过了」与「main 门绿了」是两件事，前者不阻断合并。
- 建议处置: ①改造为 TestCase 或提供 load_tests，并在 checks.json 增阻断 profile 的 realdata discover 项（waivable=false，prerequisite 注明所需数据）；②或明确登记为「终验层，仅 Fatduck/DeepSeek 节点手工执行」并在 TEST_MATRIX 标注执行人与频率。
- 置信度: 中高（采集面与形态确证；「deep 是否实际跑过」需 CI 运行证据 → _merge §7 R2 同类）
- 来源: L23-011（定稿 + 重锚）
- related: L08（synthetic_gate 静默跳过）、M8-F-001/006（同机制）、M6b-G-001

### M8-F-012 PAR-004「Drizzle 线程本地积累/安全归约」测试的被验对象是 provider 演示内核，"逐位一致"退化为 1/997 抽样和

- 类别码: F_TEST_GAP
- 优先级: **P1**
- 位置: `tests/backend/test_drizzle_parallel.py::TestDrizzleParallel`（**重锚**，L23 原锚未给类名；复核时点 :56 类声明、:48 抽样和、`test_02_bitwise_deterministic_across_budget` docstring 承诺「输出(chk 聚合/逐像素)必须逐位一致」）；被测对象 `lib/backend_host/baseline_kernels.h`（ACS_KOP_DRIZZLE_ACCUMULATE 演示内核，ARCH-004 §4 注释）；产品实现 `lib/healpix_db/healpix_drizzle/drizzle_engine.cpp::threadTiles`（:1648/:1681/:1770-1773，恰为跨 worker FP 合并）
- 证据摘录:
  > （:48）double chk=0; for(uint32_t i=0;i<N;i+=997) chk+=out[i];
  > （文件头/ docstring）drizzle accumulate 是逐像素元素独立(无跨 worker 归约)…同一输入不同 budget, 输出必须逐位一致
  > （baseline_kernels.h 注释）所有 op: 输出元素间独立(无跨线程归约)→固定序确定性随 worker 数不变(ARCH-004 §4)
- 权威依据: 宪章 §13.1、§12.3；规程 P1「验证对象≠交付对象」
- 问题说明: ①被测是 lib/backend_host 的 ABI 演示内核（per-i 固定下标序求和），产品 HISS 由 `drizzleTiled`（tile 分解 + thread-local + 跨线程合并）生成 ⇒ 标题承诺的「tile/task 分解、线程本地积累、安全归约」三者均未被执行；②"逐像素"未断言，只比较 1/997 采样点的串行和（抽样口径：N 步长 997）；③其对 ARCH-004 §4 的引用继承 M8-C-001 的错误期望（同文档另一处把「tile 合并」写成与线程数无关）。真实跨 budget 一致性证据其实在已注册的 p1drz_properties（`lib/healpix_db/healpix_drizzle/tests/p1drz/p1drz_tests_core.cpp:209-213`，1/2/4 线程按 <1e-12 容差），本文件是冗余且口吻强于证据的仿品。
- 影响: 「PAR-004 验收 PASS」不构成为产品 drizzle 的确定性证据。
- 建议处置: 改名 provider-baseline 内核测试并删「tile 分解/归约/逐像素」字样；或改驱 hp_drizzle_run 断言 HISS 回读（按 p1drz 容差语义）。
- 置信度: 高（逐字；抽样语句、内核枚举常量、产品实现三处对照）
- 来源: L22-004（定稿 + 类名重锚；"1057 个采样点"改述为 N/997 抽样，见 _merge §4）
- related: M8-C-001（ARCH §4 根因）、L12-001、M4-F-04（同族：被验对象≠交付对象）、F00-04

### M8-F-013 k_corr=1.4 的「项目自产 MC 证据」与 B4-22 原子修复的唯一回归守护均为构建孤儿；文档仍以 .exe 现状口吻延续

- 类别码: F_TEST_GAP
- 优先级: **P1**
- 位置: `docs/science/PHASE2_UPM.md`（复核时点 :52 `# k_corr = 1.4 保守冻结 (MC 实测 1.3883, control_median_mc_test, pixfrac=0.8, 2000次)`、:94「SparseEqualsDense 1e-12 等价门」）、`docs/algorithms/UPM_SOLVER.md` 同句、`lib/phase2/src/sampler.cpp:79/:85`（UPMW-005 引用）、`lib/phase2/include/astro/phase2/sampler.h:51`；证据 TU `lib/healpix_db/healpix_drizzle/tests/{control_median_mc_test.cpp, kcorr_matrix_test.cpp, concurrency_cache_test.cpp, variance_propagation_test.cpp}`；文档引用面 `docs/TROUBLESHOOTING.md`（variance_propagation_test.exe）、`docs/DEVELOPER_GUIDE.md:25`、`docs/algorithms/DRIZZLE_GEOMETRY.md`（drizzle_freeze_test.cpp 硬门）；被测修复 `drizzle_engine.cpp`（B4-22「原裸 static read-modify-write … 改 atomic fetch_add」）
- 证据摘录:
  > （control_median_mc_test.cpp:21）-o control_median_mc_test.exe control_median_mc_test.cpp   ← 全文件唯一"构建方式"=手敲命令行注释
  > （concurrency_cache_test.cpp 头注释）-o concurrency_cache_test.exe concurrency_cache_test.cpp
  > （grep 实测）四名在 CMakeLists 非注释行、ci/**、Makefile、*.sh、*.ps1 命中均为 0；仅有的非文档命中面是 legacy 脚本 tools/quality/v19r3_traceability.py:75/119/227/318 与 tools/redteam_v19.py:100
- 权威依据: 宪章 §13.1、§17.2、§12.3-4、§6.3（冻结常数须绑定验证）；AGENTS.md 验证纪律
- 问题说明: 三件新事实（drizzle/UPM oracle 未注册本体分别由 M4-F-01、M2a-F-1 主落，本条不重复）。①冻结科学常数 k_corr=1.4 的唯一"自产 MC 证据"在全部真源构建面零注册 ⇒ SCI 声称的证据既不可复算也不可回归；②B4-22「裸 static → atomic」并发修复的**唯一**回归守护 concurrency_cache_test.cpp 同为构建孤儿 ⇒ 典型"修复未固化"（代码改对但无 ctest、文档未回写，下一个改 cache 的人无护栏）；③TROUBLESHOOTING/DEVELOPER_GUIDE 仍以现状口吻给出手敲 .exe 路径，维持"验证面存在"的误导（文档卫生面与 M6a-I-003 同族）。
- 影响: 数值常数与并发修复的回归保护为 0；文档-构建-测试一致性断裂。
- 建议处置: 把仍被文档引用的 4 个 TU 纳入 p1drz/phase2 测试族注册（或显式废弃并回写文档，移 archive）；k_corr 出处保留可复算门（判据整改由 M4-F-05 主落）；concurrency_cache_test 与 trace-on 面（M8-H-001）一并补最小回归。
- 置信度: 高（四 TU grep 空集 + 三处文档现状口吻逐字）
- 来源: L22-005（定稿为增量事实，主体并档 M4/M2a）
- related: **M4-F-01**（未注册本体主落）、**M4-F-05**（MC 判据宽区间主落）、**M2a-F-1**（drizzle oracle 主落）、M8-H-001、M8-F-005、L04-004
- **【E2-实测 2026-09】** ①②证实并给出精确测量：`control_median_mc_test`/`kcorr_matrix_test`/`concurrency_cache_test` 三名的 CMakeLists 非注释命中 = 0、构建树注册文件（`*CTestTestfile*`/`*_tests.cmake`/`*_include.cmake`）命中 = 0、`ctest -N`（CI 树，288 项）命中 = 0、既有 build 树下无产物；`git ls-files lib/healpix_db/healpix_drizzle/tests/` = 42 ⇒ 三文件均 tracked-but-unbuilt（A 档：根本不在图里）。B4-22 修复与被测关系实测在位：`drizzle_engine.cpp:1661-1663 static std::atomic<std::uint64_t> s_target_cache_gen{0}; // B4-22 atomic run-gen … fetch_add(1) + 1`，其唯一守护 `concurrency_cache_test.cpp` 的「构建方式」仍是 `// -o concurrency_cache_test.exe concurrency_cache_test.cpp`（:12 逐字）。③的 `.exe` 现状口吻本轮未复验（文档面）。