# V15 · 「测文本不测行为」断言普查（分片 1-3：N-01..N-09，全 P1；HEAD `56b5e662`）

## ①类：对源文件文本 `find`/`contains`/正则命中即通过（`蕴含：无`）
| 条 | 位置 | 断言的是什么 | 为什么无效（一句话） |
|---|---|---|---|
| **V15-N-01** | `tests/unit/p2_ir_facade_test.cpp::main` `:29-38`/`:56-62`/`:68-69` | `src.find("stage(\""+node+"\"")!=npos` 数满 4 即 `CHECK(n_nodes==4)`；`CHECK(src.find("stages")!=npos)`、`find("kind")!=npos` | 源码出现字面量与"运行期 IR 真有 4 节点且按此序执行"无因果；`:68-69` **命中任意注释即绿**；节点名与委托名两份清单由测试手抄 ⇒ 被测源改名而语义不变即**假红**。守护的是 §8.2/§12.3-4 canonical IR 四节点与顺序 |
| **V15-N-02** | `tests/unit/p1_ir_facade_test.cpp::main` `:33-40`/`:47-49`/`:54-56` | 精确串不中即**退化为裸词命中**的放宽析取；把一整行 C++ 表达式 `cosmetic_flag(doc[\"cosmetic\"], \"enabled\", true)` 钉成契约 | 「缺省开」的运行期行为**零证据**，格式化即假红；`:54-56` 命中符号名只说明该串在文件里。守护 §3.2/§8.1/§14.4 节点集与"facade 不内联科学" |
| **V15-N-03** | `tests/backend/test_p3002_properties_order_unit.py` `:24-57`（**整文件 4 个用例**） | `assertIn("input_order", s)`、`re.search(r"const int max_order = \(input_order[^;]+;")`、`assertNotIn(\"Jy/beam\", s)`、`assertIn(\"bunit\", h)`、`assertIn(\"variance\", r)` | **整文件零执行**（不编译、不跑 `astrocs phase3`、不回读 properties/FITS）；`:40` 只保证字面量不在本文件 ⇒ 默认单位挪到 `p3_resample.cpp` 即**假绿**；`:44` 任何含 `bunit` 子串的注释都满足。守护 P3-002 四条合同（§7.3/§14.4） |
| **V15-N-04** | `tests/backend/test_p3003_parallel_resampler.py::test_02/test_03` `:100-116` | 剔注释后 `assertNotIn(\"hardware_concurrency\")` + `assertIn(\"budget.max_workers\"/...std::thread/rows_per_worker/P3Sampler w_samp\")` | 把 `budget.max_workers` 换成 `min(budget.max_workers, hardware_concurrency())` 时 `:107` **仍绿**；`:115` 钉死**变量名**。守护 §10.4/§17.6 与 P3-003 worker 独立 sampler。**同文件正对照**：`test_01_parallel_equals_serial` 是真行为断言（两次真跑 + FITS 逐字节比对，仅屏蔽 RUNID/CHECKSUM/DATASUM）⇒ 判"漏"不判"该文件全废" |
| **V15-N-07** | `tests/unit/io_adapter_test.cpp::test_io_adapter_no_scheduler_include` `:210-221` | `content.find(\"scheduler.h\")==npos` 等 5 条 | **fail-open 型负向锁**：①**文件读不到即全绿**（无 `is_open()` 前置，`ASTROCS_REPO` 未设时回退 `..`，非 ctest 直跑即命中此路）②头注释里合法写一句"本头不得依赖 phase1"即**假红**③真违规（经 `astrocs/core/xxx.h` 间接传递、或前向声明+别名）**完全测不到**。守护 §3.2/§8.1。有效改法：`check_abi_boundary.py` 已在解析 `#include`（其 `STL_INC :21`）⇒ 扩一条 include 闭包判据 |

## ②类：恒真断言（**数学上不可能失败**）
### V15-N-06（P1，建议负责人裁是否升 P0）`test_kernel_ids_authoritative` 的正则交替集与白名单**是同一个 12 项集合** ⇒ `assertIn` 恒真，而声称的权威文件从未被读
- 位置：`tests/runtime/test_rt005_plan_estimator.py:470-479`（白名单 `:432-438`），docstring 自称「实现中出现的 kernel id 必须都在 `backend_table.inc` 权威清单内」。
- **V15 解释器复算**（`scripts_v15_verify2.py`）：正则交替 12 项与 `_KNOWN_KERNELS` 12 项**集合完全相等（两集合相同? True）**，`m.group(1)` 取自交替式捕获组 ⇒ **`assertIn` 永真**；且 `.inc` **从未被本测试读取**（实测 `.inc` 内 `ACS_KERNEL_ENTRY("ALG-001","calibration-pixel-transform",…)` 恰 12 条、与白名单同集——**那是巧合一致，不是被测出的事实**）。⇒ **恒真结论纯静态成立，不依赖运行期**。
- 有效改法：解析 `.inc` 第二参得权威集 A、解析实现得 B，断 `B ⊆ A` 且 `A − B == ∅`（新增 kernel 未登记即红、清单改名即红）+ **注入式反向锁**（临时加一个未登记 id ⇒ 断言必红）。**related `FD-F-002`**（期望值与被匹配模式同处一个测试文件＝"生产者与守卫同处"的极限形态）、`M8-F-015`、`V14-N-03`（precision 漂移正从此门漏过）。

## ③类：钉诊断文案 / 名不副实
### V15-N-08（P1）`p1snr_linux_test.cpp::group_contract` C6：注释写「未参与行必须是 NaN」，断言只证「第 0 行是参与行且有限」——**两个命题不是同一件事**
- `:294-295`：`// C6: 未参与行必须是 NaN (不得静默回填 1.0 伪装 unknown)` 紧跟 `check(std::isnan(out.local_snr[0]) == false, "C6 used rows finite")`。全文件 `isnan` 命中点为 `:194`/`:238`（m5）、`:312`（snr_phot）、`:333`/`:345`（snr_f），**无一处针对未参与行的 `local_snr`** ⇒ **把未参与行回填 1.0，本锁仍绿**（正是 §11 禁吞错后产出、`FD-F-003` 族最想拦的行为）。
- **同时记正分（V15 通读结论，别过判）**：P8 其余四组（units/oracle/negative/determinism）**是真值断言**（`checkClose` rtol 1e-12 对独立 long-double 参考 + 冻结 NumPy 锚值），本条是该文件**唯一弱站**。
- 有效改法：contract 组造一个 `valid=false` 行断 `isnan(local_snr[k])` 为真；「参与行有限 / 未参与行 NaN」拆两条独立断言，**禁止一条 check 同时冒充两个命题**。

### V15-N-05（P1）`test_frozen_constants_pinned`：断的是**声明文本** ⇒ 实现里写死 `<<9` 或 `512*512*4` 绕过常数即全绿
- `tests/runtime/test_rt005_plan_estimator.py:463-468`：`assertIn(\"kHipsTileWidthPx = 512\", h)`、`assertIn(\"kHipsMaxOrder = 20\", h)`、`assertIn(\"kHipsTileF32Bytes\", h)`（**第三行是纯符号存在断言＝④类**）、`assertNotIn(\"hardware_concurrency\", src)`。头里写 `512u` 或折行即假红。有效改法：喂 1-tile 场景断估算字节数 == **独立算式** `4*4*512*512`（不从同一常数取）；`hardware_concurrency` 归 `THREAD-BUDGET` 门。

### V15-N-09（P1）MON-001/002 两测**钉 printf 呈现格式**而非值 ⇒ 改 `%.3f`、加 `%`、加单位即假红；`find(\"60\")`/`find(\"90\")` 对"阈值真的是 90"零判别
- `mon001_gate_test.cpp:183-190`：`m1.find(\"unsampled\")!=npos`、`m2.find(\"0.300000\")!=npos`、`m2.find(\"0.700000\")!=npos`、`m3.find(\"10.000000\")!=npos`；`mon002_gate_test.cpp:209-216`：`m1.find(\"60\") != npos && m1.find(\"90\") != npos`、`m3.find(\"32.0\") != npos`。⇒ **`\"1600\"`、`\"0.96\"` 里都含 `60`/`90`**；六位小数是 `printf(\"%f\")` 的呈现而非契约。
- **同段正例（V15 已给，直接照抄）**：`:177-182` 的 `CHECK(std::string(gate_diag_name(enum))==\"monitoring_missing\")` —— **枚举机器名是契约、呈现文案不是**。数值侧改法：拆出 `diag_kv()` 返回 `{阈值, 实测值}`，断 `kv.thr==90.0 && kv.obs==60.0`，文案只留「非空 + 含单位」。
- **与 `V12-N-08(b)` 合成一条完整因果**：`resource_gate.h:82` 的 `UtilizationP75Low` 实现实为"≥70% 样本 ≥0.85"（名值不符），而 `mon001_gate_test.cpp` 还在钉它的**文案** ⇒ 名字错、值错、测试钉文案，三层同时失守。另 `V4-N-02`（`resource_gate.h:170/:171/:395/:432` 文案滞留 `0.75/0.50`）是同一根因的实现侧。
- **related**：`M8-F-009`、`V4-N-16`、`V12-N-08`、`S-5`（反向钉死禁令）。
