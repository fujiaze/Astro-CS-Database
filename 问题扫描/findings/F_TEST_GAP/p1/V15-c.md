# V15 收工 · 注入式反向锁的真实数量 + 规模 + 一条运行期前置风险

## 一、回答"到底几把"：**按黄金口径（把缺陷改回去看门是否变红）≈ 30 把**（V15 三档并列，我全部保留其口径）
- **甲｜生产实现内注入**（缺陷做进生产码、env 触发、测试断必红）：**5 个生产文件 / 14 个具名注入名**——`aio_abi.cpp`（`n1_hash_value_flip`/`n2_verify_mismatch_shortcut`/`n3_file_size_skip`）、`aio_hips_writer.cpp`（`HIPS_DIAG: sentinel,skip_write`｜`HIPS_PROV: missing_key,value_drift`｜`HIPS_VERIFY: shortcut`）、`aio_publish.cpp`（`p1_stage_slow_write`/`p1_stage_create_fail`/`p1_discard_noop`/`p1_fsync_fail`/`p1_promote_fail`）、`core/src/module_adapters.cpp:5739`（`resample_drop_band`）、`phase3_session/p3_output.cpp:103-104`（`ASTROCS_HASH_FAIL_INJECT`，**唯一带 `#ifdef` 编译期剔除**）。⇒ **附一条事实**：14 名里 **13 名在生产源内常驻 `getenv` 分支、无编译期剔除**（本轴不立危害条，但我记为待议：生产二进制里留着可编程改变行为的开关，与 §11 原子发布/§14.4 的精神相悖，**建议随 `C-21` 一并裁**）。
- **乙｜两阶段 selfcheck 族（11 TU / 12 目标）——机制实测：不构成缺陷回归锁**（本轴最该被记住的否定结论）。`p1noise_test_main.hpp:53-67` 及 5 个同族 `main.hpp` 实测：命中的 CHECK 走 `++failures` 而 **`cond` 根本不参与求值** ⇒ 它锁的是「**测试报告通路能变红**」，**不是**「生产缺陷回归能变红」；且 CMake 里只有 **2 处** `ENVIRONMENT` 显式驱动注入（`ASTROCS_P3PROJ_FAULT=tan`、`ASTROCS_P1STAR_FAULT=` 置空），其余走内建默认名 ⇒ **25 个注入名里 CI 实际逐一驱动的为零**。⇒ **它解释了此前"非豁免真门"绿而事实红的机制来源**，也是 `A-38`（0 用例/0 输出算不算绿）的第五个实证。**建议升格为独立条款 C-21**：注入式反向锁必须①被 CI 逐一驱动②`cond` 真参与求值③生产侧注入名编译期剔除。
- **丙｜Python 篡改/构造缺陷变体 → 断言红**：宽口径 163 处 / 77 文件；其中「复制**真实在册工件**→篡改→跑**真门**→断言红」（甲档语义的 Python 对应物）**仅 16 处 / 9 文件**（`test_negative_guards` 7、`known_failures_baseline_ci` 2、`workflow_lock` 1、`ci001b_wf_step` 1、`adopt006` 1、`cpu_profile` 1、`iso_acr_gpu_isolation` 1、`phase123_pipeline` 1、`glossary` 1）。另 **~40 处属"合成人造 fixture"**（`thread_budget` 4、`doc_machine_check` 5、`traceability` 3、`ctest_registration` 8、`glossary` 4、`version_consistency` 1、`run_graph_render` 2、`provenance` 1、`hips_output_contract` 1 等）⇒ **只锁 checker 对人造输入的判别，不锁真实工件**。
- **⇒ 30 把（甲 14 名 + 丙严格 16 处）是本轮所有"门会不会红"判断的可信度上限**；`V9` 的全门清单必须以此扣减，否则会把 12 个 selfcheck 目标误当成 12 把锁。

## 二、规模（口径：扫描面 = `./tests/**` + `./ci/**` + `./tools/**` + `./lib/**` 中路径含 `/tests/` 或文件名含 `_test.`/`selftest` 的 `.py/.c/.cpp/.h/.hpp`，**排除 21 个前缀目录**（含 `./run/`、`./问题扫描/`、`./evidence/`、`./testdata/` 等），**共 717 文件**）
| 类 | 口径 | 规模 |
|---|---|---|
| ① | Python「读文件/取文本后子串或正则命中」 | **419 行** |
| ③ | 对 `stderr`/`stdout`/`msg`/`reason`/`detail`/`message` 做文案子串断言 | **204 行**；其中 `reason|message.find()!=npos` 精确族 **45 处 / 8 文件** |
| ④ | 正向存在/计数断言（`assertIn` 键、`isfile`、`len>=N`、`in ids`） | **277 行** |
| C++ | 含 `npos` 的行 **643**；`find(...)!=npos` 121 行；「读入文本后再 find」122 行 | 其中**读仓内源文件/配置正文当证据**的仅 **28 行 / 6 个文件**（`cpu_bench_test` 4、`cpu_provider_test` 2=`FD-F-001`、`io_adapter_test` 5、`p1_ir_facade_test` 6、`p2002_unc_rej_prov_test` 2、`p2_ir_facade_test` 9）⇒ **V15 的 9 条 P1 全在这 28 行内** |
| ② | 无独立计数口径（判据是"生产者与守卫同处"，须逐条读） | 命中者全部进 §2，**不估数** |

## 三、两处特别检查（**给 P5/P8 记正分，防我误判为全线失守**）
- **P5** `p1snr_science_test.cpp`（注册 `p1snr_science_{units,oracle,negative,production,determinism}`）：**四类弱断言 0 命中**，形态合格（`checkClose` 值断言 + 独立 long-double 参考 + `failures` 计退出码）⇒ **唯一遗留**是锚值权威 `run/perf-fix/P5-snr/harness/snr_oracle.py` 属 gitignored 现场**且现已不在盘上**（三级复核：`git ls-files|grep snr_oracle` 只命中 `engineering/control/archive/**` 3 个 txt）→ **挂 `V1-N-09`，不另立**。
- **P8** `p1snr_linux_test.cpp`（注册 `p1snr_linux_*`，`LABELS p1snr`）：**真值断言为主，唯一弱站 = `V15-N-08`**（C6 名不副实）。
- **backend 三文件**：命中 3 条（`N-04`/`N-12`/`N-13`）；**共同弱形态 = 整行日志文案钉死**（`p2007:295 assertIn("stage coverage ok", stderr)`、`p3006:118` 同型、`test_cpu_profile.py:181 assertIn("avail=2 backend=baseline workers=2 reason=no_valid_profile", out)`）⇒ 建议同批改"解析 `--output jsonl` 的 resource 事件字段"。

## 四、一条**运行期前置风险**（V15 报为"未单独立条"，我升为独立条目登记，因它会整片削弱 UT-BACKEND）
### V15-N-16（P1·需运行期定性）`UT-BACKEND` 的 command 只有 `unittest discover`、**无 build 依赖声明**，而三个 backend 测试都走 `EXE = REPO/build/astrocs` 真跑 ⇒ **干净 checkout 上"真行为断言"整体 error**
- 后果：backend 域里的**好断言**（`p3003 test_01` 真跑两遍 + FITS 逐字节比对、`p3006` 的资源门、`p2007:276-281` 的值断言）**全依赖那个可执行文件存在**；若 CI 顺序/缓存不凑巧，它们整体 error 或被 skip ⇒ 与 `V4-N-01`（该域静态推导疑红）**必须同一次运行期定性一起看**。
- 建议：给 `UT-BACKEND` 声明前置（同 CI 先跑一次 `build/astrocs` 的产出步骤，或在测试内 `skip` 时把"因缺 EXE 而 skip"计为失败——按 `A-38` 的裁定走）。**related** `M8-F-004`（在册≠执行）、`V4-N-01`、`A-38`、`FD-F-003`。

## 五、免重报核对（V15 自陈，我确认符合协议）
- 四类全部挂到既有总述（「机器门结构性不红 `M8-G-001`+`M6b-G-001`」/「修复即扩散·反向钉死 `M9.md §7`」/「行锚漂移 `M6b-E-002`」/「无据 VERIFIED `M6b-G-001`」），**未另立同义总述**；`FD-F-001/F-002`、`V4-N-16/05/01`、`M8-F-002/004/005/007/009/014/015`、`M2b-F-02`、`M5a-G-002/005`、`M5b-G-08`、`M6b-L18-003`、`V1-N-09`、`V2-N-06`、`V6-N-04` 一律挂 related 未重报。
- **专查「把旧错误口径当权威改回去」：未发现新站点**；相邻唯一事实是 `p2007:19/:231` 的 `0.80*N` vs 冻结 §18.2 的 85/60，属 `V4-N-01` 在册残留 ⇒ **挂 related 未重报**。
