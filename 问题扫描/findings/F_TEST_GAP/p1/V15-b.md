# V15 片5 · P1 两条

### V15-N-14（P1·恒真短路**新站**，在册普查口径外）`p3002_uncertainty_test.cpp:275` 读回助手 `return status == 0 || true;` ⇒ 助手**恒返回成功**，三处"写后读回"CHECK 永绿
- 该助手供 `:325`/`:351`/`:575` 三处 `CHECK(read_fits_hdus(fits,&hi))` 当**读回证据** ⇒ FITS 打开/逐 HDU 读失败时，后续对 `hi.hdus`/`hi.extname`/`hi.bunit` 的断言建立在未初始化/半初始化的 `hi` 上，失败以无关原因暴露或被 `:272` 的 `status=0` 复位掩盖。
- **全仓 `|| true` 逐行复核实测 4 处**（排除 `./run/` 影子树与 `./问题扫描/`）：`p1_resource_test.cpp:78`（=`M8-F-007`）、`p1_hips_writer_test.cpp:89`（=`M8-F-007`/`M2b-F-02`）、**本条 `p3002_uncertainty_test.cpp:275`（不在任一条在册条目内）**、`tools/check_warning_suppression.py:32`（shell 片段，合法）。⇒ **`M8-F-007` 的普查口径是正则 `CHECK(.*|| true)`，漏掉 `return` 语句形态** ⇒ 这是"门的正则小于缺陷空间"的又一实例。
- 改法：`return status == 0;` + 逐 HDU 显式计失败；配**注入式反向锁**——本文件已有的 `ASTROCS_P3002_FAULT=skip_hdu`（`:790-793` 装载）应使助手返回 false，而**当前注入只影响下游期望值、不影响助手返回值**。
- **related** `M8-F-007`（同型补站）、`M8-F-015`（恒真面无机器门覆盖）、`S-4③`（同类"判据口径不足"需一条覆盖 `return`/`CHECK`/`assert` 全形态的弱断言门）

### V15-N-15（P1·**反向钉死未达成状态**）`tests/arch/test_single_cli.py:31-32` 把"清单里 production exe 数 = 0"钉成期望 ⇒ **CLI-001 建成并正确登记的那一刻本测试必红（真整改假红）**
- 三事实合成：**(a) 文档措辞当架构门** `:13-14`/`:36-38` 对 `ARCHITECTURE.md` 的中文措辞做 `re.findall(唯一(?:生产)?(?:用户)?入口)` 与 `assertIn(\"in-process\")` ⇒ **同义改写即假红；真出现第二个入口而文档仍留一句"唯一入口"则全绿** ⇒ 门与交付对象无关（§3.1/§8.1/§3.2）。**(b) 反向钉死**：实测 `PRODUCTION_EXECUTION_INVENTORY.csv` 共 **291 行、`category=exe_target` 74 行、其中 `classification=production` 0 行、`symbol==astrocs` 0 行**，而 `ARCHITECTURE.md:9` **已声明 astrocs 为唯一生产 exe** ⇒ 二者互斥，`:32 assertEqual(len(prod), 0)` 钉的是**未登记**而非"只有一个入口"。**(c) 死条件**：`:20` 的 `self.assertNotIn(...) if os.path.exists(MODULE) else None` 是**三元表达式当语句用**且 `:19` 已无条件 `open(MODULE)` ⇒ 永不走 else，"看着像守卫、实际零作用"。
- 改法：①不变量改交付面结构断言——从根 `CMakeLists` 的 `install(TARGETS)`/`add_executable` 闭包（或 `tools/check_release_layout.py` 产物）断「RUNTIME 用户入口数 == 1 且名为 `astrocs`」；②`:32` 改 `assertEqual(len(prod),1)` + `assertIn(\"astrocs\", …)` 并**同步订正登记面**；③`:20` 改回普通断言 + `assertTrue(exists)`；④措辞类降级为 DOC-L0 文档门。
- **related** `M6b-L18-003`（在册"固定字面量⇒恒绿"已记，本条新增**反向钉死**与**死条件**两个不同事实，不并档）、`M9.md §7`（反向钉死第六站之后的**第七站**）、`M5b-G-08`（AGENTS-GOV 同型关键词全含门）、`V4-N-16`、`S-5`
