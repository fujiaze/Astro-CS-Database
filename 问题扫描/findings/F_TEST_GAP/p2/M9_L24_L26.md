# M9 定稿 · F_TEST_GAP · P2（来源切片 L26）

> 合并代理: M9 · 复核时点: 全部证据按**当前树**重新 read/grep 定位。

## M9-F-3 provenance 双键的 pixfrac 半有界、scale 半无界；在册断言把亚弧秒取值钉成合法基线，生产真实值域（0.1–6.3″）无任何代表值被校验

- 类别: F_TEST_GAP
- 优先级: P2
- 来源: L26-007（复验后**改述**：叶子的"仅判 scale ≥ 0"**不完全准确**——负值确被拒绝；真实缺口是「上界/域完全无声明 + 测试钉死无域基线」。另撤回一条叶子证据：其引"drizzle_science_matrix_test 断言 ASTROCS_DRIZZLE_SCALE_ARCSEC == "3.220769""在当前树**不可复现**，见 M9-A-2 的同一订正）
- 位置: lib/astro_image_io/src/hips/aio_hips_writer.cpp::aio_hips_set_drizzle_provenance；声明面 lib/astro_image_io/include/aio_hips.h:238-243；断言面 lib/astro_image_io/tests/p1hips/p1hips_tests_units.cpp、::p1hips_tests_negative.cpp、::p1hips_tests_properties.cpp、lib/healpix_db/healpix_drizzle/tests/drizzle_science_matrix_test.cpp
- 证据摘录（逐字，复核时点现文）:
  > 校验体（aio_hips_writer.cpp:1281-1282）`if (!(pixfrac > 0.0 && pixfrac <= 1.0)) return 2;` / `if (scale_arcsec < 0.0) return 2;` —— pixfrac 侧是**区间**判定，scale 侧只有**单边界**；且 `!(...)` 形态使 NaN 亦被拒（pixfrac）而 scale 的 NaN 因 `NaN < 0.0` 为假 → **NaN 被接受**（本代理按 IEEE 语义推演，未执行验证）。
  > 声明面（aio_hips.h:238-243）只述用途（`设置 Drizzle provenance（pixfrac / 像素角尺度）… Phase2 sampler 按帧读取以选择 control-ivar 的 k_corr 标定值`），**未写两参数定义域**；API_CONTRACTS 对该 API 的 units/valid_range 列是全表同值样板串（见 M9-C-2）。
  > 接受侧断言：p1hips_tests_units.cpp:183 `aio_hips_set_drizzle_provenance(ps, 0.7, 0.35)`；p1hips_tests_properties.cpp:328-329 `kv1.at("ASTROCS_DRIZZLE_SCALE_ARCSEC") == "0.3500"`、:335 `!kv2.count("ASTROCS_DRIZZLE_SCALE_ARCSEC")`。
  > 拒绝侧断言（存在但只测 pixfrac 与负 scale）：p1hips_tests_negative.cpp:237-240 `(1.5,1.0)→2`、`(0.0,1.0)→2`、`(0.5,-1.0)→2`、`(0.5,0.1)→0`。
  > 生产真实值域证据（本代理重取）：drizzle_science_matrix_test.cpp:227 `const double scales[] = {0.1, 1.0, 10.0, 60.0, 3600.0};`（:346 另用 6.3）；上游真值来源 hp_drizzle_api.cpp:1130-1133 `meta.fits_meta["src_pixel_scale_arcsec"] = std::to_string(std::fabs(img.wcs.cd[0]) * 3600.0);`，消费侧 astro_sphere_sink.cpp:68、sampler.cpp:130（读 `ASTROCS_DRIZZLE_SCALE_ARCSEC`）。
  > 写出侧（aio_hips_writer.cpp:975-978）`if (ps->drizzle_scale_arcsec > 0.0) snprintf(sc, sizeof(sc), "%.4f", …)` —— 与校验边界不一致：**0 被校验接受但不落键**（校验 `<0` 拒、写出 `>0` 才写）。
- 权威依据: 宪章 §13.1（核心合同须有正向与负向可失败测试）、§14.4（入参定义域）、§12.3（声明与实现一致）、§4.1。
- 问题说明: 三处不一致：①scale 侧无定义域上界，而消费侧 `kcorr_lookup` 的标定域是 [300,600]（M9-A-2）→ 校验通过的 0.1–6.3″ 全部在标定域外且被 clamp 静默饱和，"合法值"与"有意义值"两个集合几乎不相交；②NaN/极端大值可通过校验（无 `isfinite`、无上限），写出后成为 properties 中的畸形键值；③校验放行 0 而写出跳过 0 → `rc=0 成功` 与"键是否落地"不等价，测试族里两种情形被分别断言但无一条把二者串起来；④在册接受例（0.35/0.1）已成为基线，若按 M9-A-2 建议改为显式拒绝越域，这些断言会**假失败** → 二者必须同批修订（这是本条的真实危害：修复阻力）。
- 影响: provenance 通道的域约束仅存在于 pixfrac 一侧；scale 侧被"有校验"的表象掩盖，且测试面把无域取值固化为期望值。当前无生产数值错误（k_corr 面见 M9-A-2）。
- 建议处置: ①校验体按负责人对 M9-A-2 的裁决补 scale 上界与域（或"合法域=标定域外即拒绝"），并补 `std::isfinite` 判定；②声明面（aio_hips.h + API_CONTRACTS 的 valid_range 列）写实两参数域；③测试族补：域内代表值、恰在界上值、NaN、超界值各一，并把「rc 与键是否落地」写成同一断言；④与 M9-A-2 同批修订既有断言，避免修复被基线锁死（M9 §8 主题）。
- 置信度: 高（校验/断言/写出三面逐字复验；NaN 推演未执行，已如实标注）
- related: M9-A-2（同一域问题的标定侧，须同批处置）、M9-C-2（同族：入参域不声明）、M9-F-2（同文件同族：口径被测试钉住）、M9 §8、drizzle_science_matrix_test 证据订正见 M9-A-2
