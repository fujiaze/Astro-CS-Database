# V15 片4-5 · P2 四条（规模与分级由 V15 给出，我不扩判）

### V15-N-10（P2·③析取式 reason）CPU-005 路由回退原因只断"串里含 X 或 Y"，而 **`invalid` 几乎任何错误串都含**；全仓同族口径实测 **45 处**
- `tests/cpu/dispatch/cpu005_route_decision_test.cpp:199-200`/`:211-212` `CHECK(d2.fallback_reason.find(\"raised\")!=npos || find(\"verify\")!=npos)`；强例 `tests/unit/p1_noise_test.cpp:105` `CHECK(r0.value().reason.find(\"gain\")!=npos)`（四字母子串对"零 gain 被拒 vs gain 越界被拒"**不可区分**）。⇒ 实现在"因 hash 不符回退"时 `:211-212` 仍可绿。
- 分布（`scripts_v15_cpp.py` → `scripts_v15_cpp_text.json`）：`cpu008_worker_advisor_test` 15、`cpu005_route_decision_test` 10、`cpu007_profile_store_test` 5、`cpu004_routing_test` 5、`lib/backend_host/profile_store.cpp` 自测 5、`p1_noise_test` 2、`cpu001_negative_test` 2、`p1_wcs_phot_test` 1。
- **V15 的分级我照收**：`cpu008_worker_advisor_test` 那 15 处**同段已带值断言**（`CHECK(a.workers==2)`、`CHECK(r.limits.available_cpus==4)`）⇒ 属"冗余脆化"非"零判别"；**本条只判析取式与极短子串两类**，其余按 45 处规模登记不逐条立案。改法：reason 改诊断码枚举（仓内已有正例 `gate_diag_name()`）。**related** `M8-F-009`、`V15-N-09`、`S-5`。

### V15-N-11（P2·②自建自证第二实例）`test_isa_variants.py`：`.inc` 文本锁（同一不变量的 Python 侧第二实例）+ **测试自己 `csv.writer` 写出 `MEASUREMENTS.csv` 再断文件在** + 把历史数字 `\"+28.2%\"` 钉成文档契约
- `:53-58` `assertIn(\"#include \\"baseline_kernels_impl.inc\\"\", v)`（`FD-F-001` 的同一机制、**不同门**：那条在 ctest `cpu_provider`，本处在 `UT-BACKEND`）；`:79-88` 自写自证（且该工件是**测试产物而非被测交付物**）；`:90-96` 钉 `\"+28.2%\"` ⇒ 重新 benchmark 合法变化即红、测量变了而文档没改也"绿"。
- 改法：文本锁并入 `FD-F-001` 建议的符号/编译面门；工件审计改为跑 `astrocs benchmark cpu` 后断"schema 合法 + 数值与本次运行日志一致"，**删自写自证**；文档只断结构化字段（`decision: SHIP(avx2)` 行），数字改取 CSV 计算值。**related** `FD-F-001`、`M8-F-014`（`test_isa_avx.py:109 assertTrue(True)` 同域）、`L23-012`、`M9.md §7`。

### V15-N-12（P2·④正向在册冒充"已实现"）`test_p3006:133-139` docstring 自称验「状态 IMPLEMENTED」，实际**只查 task_id 列含 `P3-006`、完全不查状态列**
- 三级复核实测：`evidence/v6_1_rework/TASK_LEDGER.csv` 表头 `task_id,gate,title,depends_on,platform,change_class,heavy_compute,commit_required,scope,acceptance,status`；**`P3-006` 行的 `status` 值是 `PASS` 不是 `IMPLEMENTED`**，「status IMPLEMENTED」只出现在 `acceptance` 自由文本里（`scripts_v15_verify2.py` 输出）。⇒ 权威取自 `evidence/**`（一次性并发现场），台账改归档路径或 ID 命名空间即假红。
- 改法：`DictReader` 按列名断 `status` 值并把「IMPLEMENTED 对应哪个列值」写进合同；权威改受控状态源（`docs/TRACEABILITY.csv` 或台账门 T6 产物）；**或直接删本用例**（该事实由在册台账门覆盖）。**related** `V4-N-16`、`M6b-G-001`（无据 VERIFIED）。

### V15-N-13（P2·④枚举白名单 + 键存在 + 产物存在）`test_p2007:234-250`/`:281-284`：`verdict` 只断"属九元组"（**含 `cpu_p50_low` 本身**，故该条不判失败），证据字段只断键在（值 `-1.0` 未采样哨兵也过）
- `:281-284` 只断 `resource_samples.csv`/`worker_balance.csv` **文件存在**，不判行数/覆盖窗口。⇒ **同文件 `:276-281`（`n_samples>0`、`rss_peak_bytes<512MiB`、`workers_p50>=2`）是有效值断言，本条只登记弱半边**（V15 自限，我照收）。
- 改法：`verdict` 断"等值于 `ok`"；证据字段断 `>=0`（哨兵按 §10.5 应判失败或显式 skip 标记）；产物改断"行数==期望采样窗口 + 列头八字段齐"。**related** `V4-N-01`（本文件 docstring 的 `0.80*N` 与 §18.2 的 85/60 不同源，**已在册不重报**）、`M5a-G-002`。
