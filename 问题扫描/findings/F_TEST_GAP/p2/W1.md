# W1 片3 · P2｜F_TEST_GAP

### W1-N-07（P2）本区间被改的 `test_last_inlier_reset.cpp`（`lib/plate_solve/cpp/ipv/test/`，改用 `ipv_get_default_params` 填 ABI 头）**所在 TU 全仓 53 个 CMakeLists 零引用** ⇒ 修复动作零采集
- 三级复核：L1 对全部 CMakeLists 检索 `test_last_inlier` **0 命中**；L2 子代理全量构建面清单 **0 命中**；L3 该目录**无 `file(GLOB)`/目录式源收集** ⇒ 不在任何目标源列表内。
- **为什么仍值得记**：改动本身方向正确（补 ABI 头填法），但落在**永不编译面** ⇒ 与 `W1-N-03` 同族（机制⑪零采集必漂）：**同一区间 ipv 家族的修复，一站进了锁（`V2-N-01` 已 VERIFIED），另一站进了空气**。related `L22-005`、机制⑪、`W1-N-03`、`V2-N-01`
