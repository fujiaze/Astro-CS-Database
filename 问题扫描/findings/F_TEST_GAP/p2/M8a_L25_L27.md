# 定稿 F_TEST_GAP / P2 — M8a（L25+L27）

> 合并代理 M8a｜来源 L25-007 / L25-009（**并发修复后的"修复未固化"面**）｜依前台规则：修复本身不入 findings，修复缺回归断言另立本类。
> **0 命中判据三级复核声明（前台新令）**：本文件所有"0 命中/不存在"判据均经 ①整句 grep ②放宽字符集与大小写关键词 grep ③定点 read 三级复核；放宽级结果记于 `_merge/M8a.md §12`。未据任何"报告称未命中"撤证据。

---

## M8a-F-001 本轮两处并发修复（RCR 版本口径统一、toolchain.lock 刷新）均无回归断言，同类漂移必然复发

- 类别: F_TEST_GAP
- 定稿优先级: P2
- 位置（断言缺失面）: `ci/checks.json`（全文 grep "rcr" / "REJECTION" = **0 命中**；放宽 `(?i)rcr|rejection` 亦 0）；`tests/`（全目录 grep "2\.4\.7" = **0 命中**）；`ci/verify_toolchain.py::docstring`（6-21）；`lib/phase2/tools/rcr_oracle_compare.py::CLI 缺省与守卫`（3-5、14、17-20）；被同步的值面 = M8a-B-001 位置行所列 11 文件 13 处，另加引用锁的 `ci/INVENTORY_REPORT.md:15`
- 证据摘录:
  - 复算 1（口径：分域 grep "2\.4\.7"）：版本串共 **13 命中 / 11 文件**（docs 5、lib 7、tools 1），彼此靠人工同步；而注册表与测试域对该串 **0 断言**（`ci/checks.json` 唯一含 oracle 字样的 :1804 是 `run_provider_oracle_checks.py`，与 RCR 无关）。
  - 复算 2（口径：`ci/verify_toolchain.py` docstring 自述判据）：注册的 TOOLCHAIN-VERIFY 只做 `ci/toolchain.policy.json` ↔ `ci/toolchain.lock.json` 静态比对 + `missing_tools` 自洽 + 版本行格式，**不校验任何引用锁的活动文档** → 本次锁刷新后 `ci/INVENTORY_REPORT.md:15` 立即反向失真（后果见 M8a-I-007）。
  > rcr_oracle_compare.py:14 无守卫 import `rcr` + :17-20 CLI 缺省写死 `lib/phase2/build/rejection_cli.exe` —— oracle 在无该包或非 Windows 产物名时直接崩，无 skip 守卫、无版本断言（"官方 2.4.7"这一 SCI 锚点靠注释维持）
- 权威依据: 宪章 §12.3-1（清单/文档一致）、§12.3-10（禁历史状态冒充当前）、§12.3-12（结论可追溯到当前 SHA 的机器证据）、§13.1；协调记录「修复未固化」处置形态；前台指令「修复了但无回归测试的，另立 F_TEST_GAP 条目」
- 问题说明: 两处修复都是**手工把多个字面量改齐**：①改齐 13 处 RCR 版本串，但没有任何断言防止后续只改其中一处（本定稿时已见到第 11 个文件 `lib/phase2/memory.md:102` 也各写一份）；②刷新 `missing_tools` 后，锁与"引用锁的文档"之间仍无一致性判据，下一次刷新会重演同一失真。共同点是：修复动作已完成，但"同类问题不再发生"这一结论没有任何断言支撑 —— 依协调口径不得因"当前树已一致"而销项。
- 影响: 版本口径/工具链能力两面的回归风险为零门槛；B-001 与 I-007 的复发成本为 0。
- 建议处置: ①版本串单一来源：脚本与装配器从 `packaging/dependency-lock.json`（补 `test_only_oracles` 的 rcr 条目）读，禁字面量；②加一条 python 断言测试：SCI/ALG/头注/实现四处版本串 == lock 值，挂进 `ci/checks.json` 现有 DOC-* 项（不必新增 profile）；③引用锁的活动文档头部须记被引锁的 `generated_utc`，与当前锁不等则 FAIL（或与 M8a-I-007 的"报告不复述清单"方案二选一）；④oracle 脚本补 `importorskip` 守卫与跨平台产物名解析。
- 置信度: 高（八处→十三处的散写与零断言均为字面复算）
- 来源: L25-007（修复面 1）+ L25-009（修复面 2）
- related: M8a-B-001（版本串的语义权威面）、M8a-I-007（锁刷新后报告未回写的事实面）、M8a-G-004（同族缺门；机器门建议集中于该条以免重复）
