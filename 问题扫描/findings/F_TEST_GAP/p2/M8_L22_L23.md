# M8 定稿（L23）— F_TEST_GAP / P2

> 合并代理 M8｜来源 _cache/L23.md｜纯只读复核。P0/P1 见同目录 p0、p1 合并件。

### M8-F-014 ISA-002「AVX 永不优于已发布 AVX2」用例只断言 assertTrue(True)，且判据 csv 由测试自己写出（自证）

- 类别码: F_TEST_GAP
- 优先级: **P2**（不改变科学输出，但制造"门已判定"的错觉）
- 位置: `tests/backend/test_isa_avx.py::TestIsaAvx::test_05_avx_never_beats_shipped_avx2`（**重锚**：L23 原锚未给类名；复核时点 :109 `self.assertTrue(True)`；同文件 :78-88 自行写出判据工件）
- 证据摘录:
  > （:109）self.assertTrue(True)
  > （:78-88）（测试内把上一用例的测量结果写进 artifacts/prerelease_v5/ISA-002/MEASUREMENTS.csv，再由 :105 的注释声称"依据该 csv 判定 AVX 不优于 AVX2"）
- 权威依据: 宪章 §12.3-4（机器检查必须能失败）、§13.1（可复用验证面）；ISA 面合同 docs/architecture/ISA_VARIANTS.md
- 问题说明: 该用例名（"never beats"）承诺一条比较性判定，实际体为空断言；其判据工件（MEASUREMENTS.csv）由同一测试自己在前序用例中写出 ⇒ 即使把 :109 换成真比较，输入也是自产的，无法证伪。 UT-BACKEND 采集该文件（discover -s tests/backend）所以它确实在跑，但这条永不失败。
- 影响: ISA 退化门（AVX 变慢应被拒）在 CI 上不存在；文档若引用 ISA-002 的"已验证"，引用的是自证工件。
- 建议处置: ①:109 换成真断言（读同文件上一用例的度量字典，比较 ns/元素）；②判据工件改由 `astrocs benchmark cpu` 产出（§10.3），测试只读不写；③若短期无法实现，删除该用例并把 ISA-002 的"未判定"登记到 STANDARDS_REGISTRY。
- 置信度: 高（逐字复核）
- 来源: L23-012（定稿 + 重锚）
- related: M8-F-006（return 布尔同族）、M8-G-003（自证工件同族）

### M8-F-015 恒真断言面未被任何机器门覆盖：现存 5 处 `CHECK(true)`（含冒名不变量门）；`CHECK(0 == 0)` 写法已在当前树消失

- 类别码: F_TEST_GAP
- 优先级: **P2**
- 位置: `tests/unit/p2_upm_synthetic_test.cpp`（:125）、`tests/unit/p2_output_semantics_test.cpp`（:136）、`tests/unit/p2_rejection_test.cpp`（:89）、`tests/unit/p3_assembly_test.cpp`（:127）、`tests/unit/p3_interp_test.cpp`（:89）
- 证据摘录:
  > （p2_upm_synthetic_test.cpp:125）CHECK(true);  // 语义: 合成框架无单覆盖污染
  > （p2_output_semantics_test.cpp:136 / p2_rejection_test.cpp:89 / p3_assembly_test.cpp:127 / p3_interp_test.cpp:89）同型 CHECK(true)
  > （grep 实测）"CHECK(true)" 字面串 5 处（排除免报区、排除 || true 变体）；"CHECK(0 == 0)" 及其空格变体 **0 处**
- 权威依据: 宪章 §13.1（负面路径/不变量须有可失败断言）、§12.3-4
- 问题说明: ①**订正 L23 计数**：叶子报"6 处 CHECK(true) + CHECK(0 == 0)"，当前树实测 5 处，`CHECK(0 == 0)` 已不存在（列入 _merge §3「已修复」表）。②`CHECK(true)` 本身只是弱断言，真正的问题是它以注释冒名一条不变量（"无单覆盖污染""加性非乘性"），且所在文件对生产库零链接（M4-F-03 已定稿 p2_upm_synthetic 面）⇒ 这 5 处是"注册的 ctest 里对生产代码零覆盖的常量断言"。③机器门只覆盖其中 1 处：F-022 探针（见 M8-G-003）只读 p3_assembly_test.cpp。
- 影响: ctest 通过数被常量断言虚增；不变量声明与验证之间没有对应关系。
- 建议处置: 逐条替换为对生产库的真实断言或降级为文档示例（与 M4-F-03 处置合并执行）；把探针正则扩到全 tests/unit 面并覆盖 `|| true`、`if (true)`、`CHECK(1)` 变体。
- 置信度: 高（逐字 + 全仓正则）
- 来源: L23-014（部分：编号错挂已拆到 M8-G-002；CHECK(0==0) 子项判为已修复）
- related: **M4-F-03**（主落，同文件冒名门）、M8-F-007（同型恒真 || true）、M8-G-003
