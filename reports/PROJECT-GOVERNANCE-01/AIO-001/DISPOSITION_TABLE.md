# AIO-001 · 逐条处置表（32 条）

> 口径：问题扫描/REBASE.md 四态（OPEN / RESOLVED / VOID / UNVERIFIABLE）。**禁止采信账本 fix_state/verified_state**；每条均在本轮当前树复跑。
> 路径：清单原文为迁移前路径，已按 cmake/ARCH-001-migration-manifest.md 翻译（见 TRANSLATION_TABLE.md）；逐条日志 run/PROJECT-GOVERNANCE-01/AIO-001/logs/<ID>.log。
> 时点：HEAD=8f0a4c6b + 工作区未提交改动（本任务改动见下节）。

## 计数自证

| 口径 | 数量 |
|---|---|
| 清单条目（by_owner/AIO-001.txt） | **32** |
| 本表数据行 | **32** |
| ID 集合双向差集 | **空（一致）** |
| OPEN | **30** |
| RESOLVED | **2** |
| VOID | **0** |
| UNVERIFIABLE | **0** |
| 原优先级 P0（账本第 3 列） | **5** |
| 其中清单 P0-OPEN | **4**（M2b-A-01 / M2b-A-02 / M2b-C-01 / V11-N-01） |
| 其中 M8-F-002(P0) | **RESOLVED**（与清单 P0-OPEN=4 不冲突） |

## 逐条处置

| ID | 原类别 | 原优先级 | 旧判据 | 最新权威条款 | 当前证据（本轮真跑） | 结论 | 归属 | GAP关系 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| M2b-A-01 | A_SCI_DEF | P0 | 旧宪章 §14.4/§5.2/§6.2；DATA_SEMANTICS §2/§12.1 | docs/algorithms/HIPS_WRITER.md §1(1a)「叶级 nside=2^K≥512」；§9 负面矩阵；ASTROCS_DESIGN §9/附录 B（IVOA HiPS 1.0） | `sed -n '493p' lib/infrastructure/aio/src/hips/aio_hips_writer.cpp` → `        ps->leaf_order = ilog2_u64(nside);`；`grep -c 'nside & (nside - 1)'` 同文件 → `0`；同 grep `lib/algorithms/drizzle/hips/src/module_entry.cpp` → `1` | **OPEN** | AIO-001 | 无 | **已就地修复（改动2）**：nside 非 2 的幂 / >2^29 现被 `aio_hips_product_begin` 拒绝（含上界）；负例 `p1hips_negative` N1 新增 6 项，改前 6 check 失败 → 改后 0 |
| M2b-A-02 | A_SCI_DEF | P0 | 旧宪章 §12.2；DATA_SEMANTICS §2；IVOA HiPS 1.0 §4.2.1 | ASTROCS_DESIGN 附录 B（IVOA HiPS 1.0 为外部标准）；ENGINEERING_SPEC §12 | `sed -n '1069p' aio_hips_writer.cpp` → `            const uint32_t nside_k = 1u << (k + 9);`；`sed -n '146p' docs/algorithms/HIPS_WRITER.md` → `  约定。FITS cards ORDERING=NESTED + NSIDE=2^k；三处 scatter 同 (2a) 式`；`sed -n '245p' runtime/io/hips_core.c` → `  h->nside = 1ULL << ((uint64_t)order + 9u);` | **OPEN** | AIO-001 | 无 | 三源分歧逐字复现。**裁决项**：写侧/读侧 2^(k+9) 与 IVOA HiPS 1.0 的 leaf NSIDE=tileWidth·2^K 一致（tile_width=512=2^9），文档 HIPS_WRITER.md:146 的 NSIDE=2^k 才是偏差；该文件在 docs/algorithms/** 只读域且其锚正由 ARCH-001 同步 ⇒ 本任务不改，列入需权威裁决 |
| W2-N-09 | A_SCI_DEF | P1 | docs/standards/STANDARDS_REGISTRY.md:41/:50-66（非链上） | ASTROCS_DESIGN 附录 B（FITS WCS Paper I/II）+§11.1；ENGINEERING_SPEC §3 | `grep -n 'CD1_1", kw_float' lib/infrastructure/aio/src/aio_fits.cpp` → `410:    wcs.cd1_1 = kw_float("CD1_1", kw_float("CD001001"));`；`grep -rIl 'PC1_1' lib include cli` → 仅 `lib/infrastructure/aio/third_party/cfitsio/{wcssub.c,histo.c}`；`grep -c PC1_1 aio_fits.cpp` → `0` | **OPEN** | AIO-001 | 无 | 生产读入面仍无 PCi_j 支路（AIOWCSKeywords 无 PC 字段）；CDELT+PC 头仍丢旋转分量。属读侧 WCS 语义扩展（非本轮最小面）⇒ 登记后续 |
| V21-N-02 | C_ALG_IMPL | P1 | findings/C_ALG_IMPL/p1/V21.md（旧行号 732/759/786/813/846） | ENGINEERING_SPEC §9（统一状态码+结构化诊断传播）；ASTROCS_DESIGN §11.1/§12 | `grep -n HIO_ERR_FILE lib/infrastructure/aio/src/healpix/aio_healpix_io.cpp` → `732/759/786/813/846: if (ret != 0) return HIO_ERR_FILE;`（另有 328/329/377/… 共 20+ 处）；`sed -n '50,70p'` → `HIO_OK=0/PARAM=-1/FILE=-2/MAGIC=-3/ZSTD=-4/JSON=-5/MEM=-6/BOUNDS=-7/INTERNAL=-8`；`sed -n '240,250p' include/hiss_format.h` → `HISS_ERR_CHECKSUM=-4/UNSUPPORTED=-6/UNKNOWN_REQUIRED=-7/FORMAT_VIOLATION=-8` | **OPEN** | AIO-001 | 无 | 五处（及更多）HISS 仲裁点仍把上游返回码塌成 HIO_ERR_FILE(-2)，篡改与「打不开」不可区分；hiss_format.h 有 -4/-6/-7/-8 真实返回码可用。属错误码语义扩展，登记后续 |
| W3-R2-001 | C_ALG_IMPL | P1 | findings/C_ALG_IMPL/p1/W3.md；旧 lib/astro_image_io/src/aio_upm.cpp:86/101-111 | ASTROCS_DESIGN §9（临时文件+校验+fsync+原子 rename）；ENGINEERING_SPEC §9 | `grep -n rename lib/infrastructure/aio/src/aio_upm.cpp` → `101: if (std::rename(tmp.c_str(), path) != 0) {`、`105: std::rename(...)`；`grep -n remove` → `104: if (std::remove(path) == 0 && ...)`；`sed -n '95,115p'` → `// Windows rename 不覆盖已存在目标：删除旧正式模型后重试一次` | **OPEN** | AIO-001 | 无 | 「先删旧再 rename」兜底仍在（Windows 正常路径）；双失败时新旧同灭而注释称「失败可恢复」。正对照 lib/infrastructure/aio/io/src/io_adapter.cpp:124-127 已改用 std::filesystem::rename |
| W3-R2-009 | C_ALG_IMPL | P2 | 旧 lib/core/src/module_adapters.cpp:1183-1205、lib/astro_image_io/src/hiss_stream_writer.cpp:172-174；合同锚 00_COMMON_CONTRACTS.md §4.6 | docs/plugins/infrastructure/17_aio.md §4；ASTROCS_DESIGN §9 | `sed -n '1183,1210p' lib/infrastructure/scheduler/src/module_adapters.cpp` → `// 兜底（Windows 部分实现 rename 不覆盖已存在目标）: 先删目标再 rename。`；`grep -n '不能先删除旧文件再 rename' lib/infrastructure/aio/src/hiss_stream_writer.cpp` → `174:`；`find . -name 00_COMMON_CONTRACTS.md`（排除 run/build）→ **0 命中** | **OPEN** | AIO-001 | 无 | p1_atomic_publish 兜底仍 remove→rename；与 hiss_stream_writer.cpp:174 的冻结禁令仍两文一题互相矛盾；合同锚 00_COMMON_CONTRACTS.md 全仓不存在（悬空未消） |
| W3-R2-010 | C_ALG_IMPL | P2 | 无（原判据即现行权威；旧 lib/astro_image_io/src/aio_pipeline.cpp:78-95/877-878/911） | docs/plugins/infrastructure/17_aio.md §4/§8（Windows/Linux 双平台、长路径） | `grep -n MultiByteToWideChar lib/infrastructure/aio/src/aio_pipeline.cpp` → `81/84/86/89`；`grep -n MoveFileExA` → `911: if (!MoveFileExA(tmp_path.c_str(), path, MOVEFILE_REPLACE_EXISTING)) {`；`grep -n MoveFileExW lib/infrastructure/aio/src/hiss_stream_writer.cpp` → `185/189/645` | **OPEN** | AIO-001 | 无 | 同函数开文件走 UTF-8→UTF-16 widen，改名仍走 MoveFileExA（ANSI 代码页）⇒ 非 ASCII 路径下静默降级；真实 Windows 生效属运行期，需 Fatduck 复验 |
| M2b-C-01 | C_DOC_CODE_GAP | P0 | 旧宪章 §11/§14.4/§12.2/§5.2/§6.2；STANDARDS_REGISTRY §1.2；DATA_SEMANTICS §12.5 | docs/contracts/DATA_SEMANTICS.md §12.5:383；docs/algorithms/HIPS_WRITER.md §10 DISP-HIPS-004:313；ASTROCS_DESIGN §9 | `grep -c -e rename -e fsync -e staging lib/infrastructure/aio/src/hips/aio_hips_writer.cpp` → `0`；`grep -n std::remove 同文件` → `232: std::remove(path_n.c_str());`、`301:`、`1030:`；`grep -rn 原子发布 lib/infrastructure/scheduler/src/module_adapters.cpp` → `:28`、`:4602`「AIO-002 原子发布内建」 | **OPEN** | AIO-001 | 无 | HiPS writer 内原子原语仍 0 命中，remove+create 直写仍在；两处注释仍宣称 AIO-002 原子发布内建，与 §12.5 及 DISP-HIPS-004 直接冲突。**本任务只闭合 properties 一个写点（M9-G-6）**；目录级 staging+原子提交需 INT 层接线或 writer 内嵌事务 ⇒ 超出最小改动面，登记后续 |
| M2a-C-11 | C_DOC_CODE_GAP | P1 | 旧宪章 §12.3-1/-2、§16.2 | ENGINEERING_SPEC §8（模块 manifest/注册表/产品清单一致）；ASTROCS_DESIGN §12 | `python3 -c` 读 contracts/data/phase2_uncertainty_rejection_provenance_v1.json → `pending_aio_channels.writer_int32_tile.status='PENDING_AIO_DOMAIN'`、`properties_key_channel.status='PENDING_AIO_DOMAIN'`；`grep -n AIO_HIPS_PRODUCT_NREJ/NUSED lib/infrastructure/aio/include/aio_hips.h` → `46: = 32,`、`47: = 64,` | **OPEN** | AIO-001 | 无 | 两个 pending 通道均已实现（flags 位 32/64 已定义、writer 已写 ASTROCS_* 五键），但合同 JSON 无 resolution 回登 ⇒ 文档-代码不一致仍未闭合。合同 JSON 属 DATA-001 域，本任务只读 |
| M8a-C-003 | C_DOC_CODE_GAP | P1 | 旧宪章 §12.3-1/-10/§14.1/§10.4/§8.3；DEPENDENCIES.md:31/:83 | ASTROCS_DESIGN §9（aio 唯一读写边界）；ENGINEERING_SPEC §7（禁止散落与第二权威） | `grep -m1 -n -o '零外部依赖（不依赖 cfitsio / XML 库）' lib/infrastructure/aio/README.md` → `7:`；`grep -m1 -n 'add_library(astrocs_cfitsio' CMakeLists.txt` → `275:`；`python3 -c` 统计 cmake/cfitsio_sources.cmake → `vendored-cfitsio-sources 60` | **OPEN** | AIO-001 | 与 GAP-002 重复 | README 仍停留 2026-07-12 快照：:5/:7/:19/:180「零外部依赖（不依赖 cfitsio）」与同目录 vendored cfitsio 60 源编入 astrocs_cfitsio 直接矛盾；:28/:166/:204 仍写 OpenMP 16 线程。README 非本轮最小面 ⇒ 登记 |
| M9-C-1 | C_DOC_CODE_GAP | P1 | 旧宪章 §8.3/§3.1/§15.3/§12.3/§11；IO_001_FITS_STREAM_INTERFACE.md:45 | ASTROCS_DESIGN §9/§10.1/§10.2（平台相关源码分开写） | `grep -m1 -n MoveFileExA lib/algorithms/drizzle/hips/src/aio_publish.cpp` → `365:    if (!MoveFileExA(stage_path_utf8, out_dir_utf8, MOVEFILE_REPLACE_EXISTING))`；`grep -m1 -n '路径均为 UTF-8' lib/algorithms/drizzle/hips/include/astrocs/hips/publish.h` → `73:`；`grep -c MultiByteToWideChar` → `0` | **OPEN** | AIO-001 | 无 | 合同声明「路径均为 UTF-8」但 Windows 分支用 ANSI 代码页 API，全目录 UTF-8→UTF-16 适配层命中 0；aio_util.h::aio_fopen_utf8 未被复用。属 P1-HIPS 目标域，退出随该目录删除；Linux CI 不可见 ⇒ 需 Fatduck Windows 复验 |
| M2b-C-06 | C_DOC_CODE_GAP | P2 | 旧宪章 §12.2/§12.3-9；ALG-HIPS-001 §9 I8 与 (5d) 字段集 | docs/algorithms/HIPS_WRITER.md §9 I8「manifest 计数字段=实际文件数」:289-290；(5d) 清单:211-213 | `awk 'NR>=1480 && NR<=1520' aio_hips_writer.cpp` → `nrej_tiles`/`nused_tiles` 实参 `(size_t)(ps->flags & AIO_HIPS_PRODUCT_NREJ ? ps->leaf_ipix_list.size() : 0)`；`grep -c -e nrej_tiles -e nused_tiles docs/algorithms/HIPS_WRITER.md` → `0` | **OPEN** | AIO-001 | 无 | 两键仍为叶级 tile 总数（未按各自子产品实际落盘文件数计数），且 HIPS_WRITER.md 全文 0 命中两键、未登记于 (5d)。计数口径修正涉及 manifest 字段语义 ⇒ 登记裁决 |
| W5-N-06 | D_COMMENT | P1 | W5 判据；contracts/schemas/jsonl_event_v1.schema.json:53 形状 [string,null]+64 位 hex pattern | ENGINEERING_SPEC §8/§9（结构化日志走 JSONL，schema 见 contracts/schemas）；contracts/schemas 为唯一事实源 | `grep -c 'file_sha256(' cli/commands.cpp` → `19`；`grep -n sok cli/commands.cpp` → `:412/:416/:423/:427`（`bool sok=false` 后仅传参，从不读；旧锚 :122/:140/:468/:581 已漂移）；`grep -n '"sha256"' contracts/schemas/jsonl_event_v1.schema.json` → `53:    "sha256": { "type": ["string","null"], "pattern": "^[0-9a-f]{64}$" },` | **OPEN** | AIO-001 | 无 | 读失败返回空串 + *ok=false，空串被直写 sha256/ir_sha256/output_artifacts[].sha256 —— 既非 null 也不匹配 64 位 hex pattern；ok 出参未读。cli/** 属 CLI-001 域，本任务只读 ⇒ 登记 |
| M2a-E-4 | E_TRACE_BREAK | P1 | 旧宪章 §8.3/§8.5/§12.3-1/-6/§16.2 | ASTROCS_DESIGN §9；ENGINEERING_SPEC §8 | `sed -n '1,6p' modules/services/io/include/astrocs/io/hips_input_v1.h` → `* 角色: astrocs_io.dll 对外的 HiPS 输入读取合同 (IO-002 冻结)。`；`grep -n 'add_library(astrocs_io ' CMakeLists.txt` → `146:add_library(astrocs_io SHARED runtime/io/fits_core.c)`；`grep -c hips_core CMakeLists.txt` → `0` | **OPEN** | AIO-001 | 无 | 合同头仍把 HiPS 读侧归属 astrocs_io.dll，而该 target 只编 fits_core.c、hips_core 零构建登记、生产无调用点（该头仅被 modules/services/io/tests/hips_core_selftest.c 引用） |
| W4-R2-01 | J_FS_PUBLISH | P1 | 旧宪章 §11；旧控制包 C-16、M9-G-4 同族 | ASTROCS_DESIGN §9（更不得误删他人在途物）；ENGINEERING_SPEC §9 | `grep -n '>= sizeof' lib/algorithms/drizzle/hips/src/aio_publish.cpp` → `102/299/328`；`grep -n 'snprintf(child'` → `174/188/228/249`；`grep -n PUBLISH_PATH_MAX` → `39:#define PUBLISH_PATH_MAX 1024`、`54:` | **OPEN** | AIO-001 | 无 | 三处截断守卫只护顶层入口；rmrf/fsync_tree 递归面四处 child 拼接不判截断 ⇒ >1024 深路径可命中同名兄弟整删；publish_mkdir_parents 返回值全吞。属 P1-HIPS 目标域 |
| W4-R2-02 | J_FS_PUBLISH | P1 | 旧宪章 §11（提交点对偶失守）；M9-G-4/G-5 同族（CWE-59） | ASTROCS_DESIGN §9/§12（模块与产品边界可独立验证） | `sed -n '113,120p' aio_publish.cpp` → `115:static int publish_stat_exists(const char* p, int* is_dir) {`、`117:    if (stat(p, &st) != 0) return 0;`；`grep -c REPARSE` → `0`；`grep -n lstat` → 仅 `251:`；`sed -n '290,315p'` → stage 名由 parent+`.`+base+固定 ASTROCS_HIPS_STAGE_BASENAME 拼成，`:303` 确定性删除先于 `:311` EEXIST 防线 | **OPEN** | AIO-001 | 无 | 父目录预置同名 symlink 可被自愈 rmrf 整删；两进程并发写同 out_dir 时后者删前者在途 staging 的结构仍在（stage 无 pid/随机）。属 P1-HIPS 目标域 |
| W4-R2-08 | J_FS_PUBLISH | P2 | 旧宪章 §11 提交点缺口；M9-G-4/G-5、C-16 同族（CWE-377/CWE-59） | ASTROCS_DESIGN §9；ENGINEERING_SPEC §9 | `sed -n '1210,1225p' runtime/io/fits_core.c` → `1215:  if (!overwrite) {`、`1219:      set_err(err, cap, "target exists (overwrite=0)");`；`sed -n '1475,1485p'` → `1480:  if (rename(wr->tmp, wr->target) != 0) {`；`sed -n '1095,1105p'` → `snprintf(out, cap, "%s.tmp.%ld.%lu", target, (long)getpid(), seq);`；`grep -rn O_EXCL runtime/io/fits_core.c aio/io/src/io_adapter.cpp aio/src/aio_pipeline.cpp` → **0 命中** | **OPEN** | AIO-001 | 无 | 四站点结构全在：begin 只探测一次、end 无条件替换；tmp 名 pid+进程内 seq 全可预测、无独占创建；aio_pipeline.cpp:877 的 `path+".tmp"` 连 pid 都不带。orchestrator/cache 两站生产不可达仍是封顶理由；任一站接线生产写出即升 P0 |
| M8-F-002 | F_TEST_GAP | P0 | 旧宪章 §14.4 末段/§13.3（每条声明须有可失败测试）/§12.3-4 | ASTROCS_DESIGN §11.3（IMPLEMENTED=当前提交内实际执行通过）；ENGINEERING_SPEC §8（能红能绿） | `wc -l tests/unit/io_ownership_test.cpp` → `138`；`sed -n '78,140p'` → main 按 failures 返回 0/1（:131-137），含 `--inject-failure` 子进程回归锁 failure_sets_nonzero_exit（:80-83/:123-128）与真分配→释放 16 轮用例 | **RESOLVED** | AIO-001 | 无 | 不再成立之证据：文件由 32 行扩为 138 行，main 现按 failures 返回 0/1 并新增回归锁防止「恒 return 0」回归；本任务实测该手法有效（AIO-OWN-002 自检同机制：注入→rc≠0） |
| M9-F-3 | F_TEST_GAP | P2 | 旧宪章 §13.1/§14.4/§12.3/§4.1 | ENGINEERING_SPEC §5.1（边界/NaN/Inf/极端参数/错误输入须有测试）；§3（红线） | `grep -n 'pixfrac > 0.0' aio_hips_writer.cpp` → `1281:        if (!(pixfrac > 0.0 && pixfrac <= 1.0)) return 2;`；`grep -n 'scale_arcsec < 0.0'` → `1282:        if (scale_arcsec < 0.0) return 2;`；`grep -n 'drizzle_scale_arcsec > 0.0'` → `975:` | **OPEN** | AIO-001 | 无 | scale 侧仍无上界与 isfinite（NaN 因 NaN<0 为假被接受），校验放行 0 而写出 >0 才落键。**裁决项**：上界取值属科学域参数定义（HIPS_WRITER.md 冻结容差面），本任务不改 |
| V11-N-01 | G_GOV_GATE | P0 | 旧宪章 §8.6/§11/§5.3/§16.2 | ASTROCS_DESIGN §7.3（版本化 C ABI：结构体带 struct_size/abi_version，所有权明确）；§9 | `python3 -c`（C 头字段数）→ `C AioHipsSnrPoint field-count 6`；PY 镜像 → `_fields_` 仅 4 项（ra_deg/dec_deg/snr/source_id）；`grep -n snr.push_back aio_hips_writer.cpp` → `914:            ps->snr.push_back(pts[i]);`；`grep -n -e struct_size -e abi_version include/aio_hips.h` → **0 命中** | **OPEN** | AIO-001 | 无 | C 头 6 字段 vs Python 镜像 4 字段（末字段仍为已废弃 source_id）依旧；C 侧按 40 字节步长取元素而调用方只分配 32 字节/元素 ⇒ HiPS SNR 目录第 2/3 条仍错位；该结构仍无 struct_size/abi_version。**附加**：lib/infrastructure/aio/tests/hips_direct_smoke.py:43 仍硬编码旧 DLL 路径 lib\astro_image_io\astro_image_io.dll ⇒ 该测试当前不可加载（P0 面加重） |
| M1a-G-001 | G_GOV_GATE | P1 | 旧宪章 §4.3/§12.3-15；SCI-P3-001 §3/§7.3 | ASTROCS_DESIGN §9（manifest 至少记录单位/坐标 frame/像素采样语义/算法 ID/模块 build ID/实际 provider）；§2 | `grep -c RADESYS lib/phase3_session/p3_output.cpp` → `0`（退出码 1）；`grep -rn sampling_semantics lib cli docs contracts tests tools ci include modules providers runtime` → 仅 lib/algorithms/drizzle/healpix_drizzle/v6_drizzle_science.h:325 与一处 v6 单测；`grep -n frame lib/phase3_session/p3_session.cpp` → `98-102` 仅请求层 frame 校验 | **OPEN** | AIO-001 | 无 | phase3 产品/manifest/inspect 面仍缺 RADESYS/EQUINOX 与采样语义键。**文件域限制**：lib/phase3_session/** 是 ARCH-001 DEFERRED（待 INT-001 删除）⇒ 本任务不改，登记 |
| M2b-G-01 | G_GOV_GATE | P1 | 旧宪章 §5.2/§6.2；旧路径 lib/hips 与 lib/hips_p2 | ASTROCS_DESIGN §9（aio 唯一读写边界，禁各自复制 writer）；§7.1（HiPS/manifest/原子提交归 lib/infrastructure/aio） | `ls -d lib/infrastructure` → **存在**（原第 6 列证据「无法访问」已失效）；`grep -n add_library lib/algorithms/drizzle/hips/CMakeLists.txt` → `43:add_library(astrocs_p1_hips_writer SHARED`；`grep -n 'add_subdirectory(lib' CMakeLists.txt` → `208:add_subdirectory(lib/algorithms/drizzle/hips)`；`ls lib/algorithms/coverage/hips_p2` → 仅 memory.md/module.yaml/README.md；packaging/astrocs.product.json 含 astrocs_p1_hips_writer | **OPEN** | AIO-001 | 与 GAP-013 重复 | **原证据已失效**（lib/infrastructure 现存在），改以本树重取：第二阶段独立 HiPS writer（astrocs_p1_hips_writer）仍在算法目录、仍被根 CMakeLists 挂载、已列入产品清单 required unit；hips_p2 仍只有三份文档（entrypoint MISSING）。退出路径属 INT-001 架构边界 ⇒ 本任务只登记 |
| M9-G-4 | G_GOV_GATE | P1 | 旧宪章 §11/§14.4/§8.3（IO-001 唯一写出边界）/§12.3 | ASTROCS_DESIGN §9 | `grep -n 'ACS_FIO_PATH_MAX' runtime/io/fits_core.c` → `747/1077/1078`；`grep -rn 'define ACS_FIO_PATH_MAX' modules` → `modules/services/io/include/astrocs/io/fits_stream_v1.h:33:#define ACS_FIO_PATH_MAX 512`；`sed -n '1226,1232p'` → `snprintf(wr->target,...); fio_writer_make_tmp_name(...)` 均不检查截断 | **OPEN** | AIO-001 | 无 | strlen ≥ 511 时 tmp 截断为 target 的原地覆写路径仍成立；begin_v1 仍无长度校验。属 runtime/io/**（未迁移、非本任务文件域）⇒ 登记 |
| M9-G-5 | G_GOV_GATE | P1 | 旧宪章 §11（提交即持久）/§14.4/§8.3/§12.3 | ASTROCS_DESIGN §9（临时文件/目录+校验+fsync+原子 rename 提交） | `grep -c -e fsync -e _commit -e FlushFileBuffers runtime/io/fits_core.c` → `0`（退出码 1）；`grep -n 'rename(' 同文件` → 仅 `1480:`；`grep -n 'PUBLISH_FSYNC_FD' lib/algorithms/drizzle/hips/src/aio_publish.cpp` → `34:_commit`、`45:fsync`、`205:` | **OPEN** | AIO-001 | 无 | 提交链仍只有 fflush→close→rename，文件与父目录均无 durability barrier；同仓另一发布实现（aio_publish.cpp）已有该纪律而 IO-001 未继承。属 runtime/io/** ⇒ 登记 |
| M9-G-6 | G_GOV_GATE | P1 | 旧宪章 §11（禁吞错后继续产出）/§8.3/§14.4/§12.3 | ASTROCS_DESIGN §9；ENGINEERING_SPEC §9（统一状态码传播 + 输出临时文件+原子提交） | 改前：`grep -n write_properties aio_hips_writer.cpp` → `:335` 定义返回 `void`（fopen 失败即 return、fprintf/fclose 不查、直写正式路径），`:1023`/`:1242` 语句式调用。改后：`:351 bool write_properties(...)` → `aio_atomic::write_file_atomic`（同目录临时文件 → fflush → fsync → 原子 rename）；`:1057`/`:1276` 均 `if (!write_properties(...)) return false;` | **RESOLVED** | AIO-001 | 无 | **本任务修复（改动3）**：properties（IVOA+provenance 唯一文本载体）改为临时文件+fsync+原子 rename，失败经子产品错误码 −3..−8 传播并清理临时文件。改前红：负例 N10 报 `frc == -3 — ... got 0`（properties_exists=0，即产出缺 properties 的「完整」产品），`ctest -R p1hips_negative` rc=8；改后绿：rc=0，全 6 个 p1hips 组通过 |
| W5-N-13 | G_GOV_GATE | P2 | 旧宪章 §14.4/§17.6（无界内存增长列发布门禁禁止项） | ASTROCS_DESIGN §12；ENGINEERING_SPEC §10（队列必须有容量/背压/取消/超时/错误传播，禁止无界） | `sed -n '465,500p' lib/infrastructure/aio/src/ahpx/aio_ahpx_reader.cpp` → ZSTD/LZ4 两分支同为 `size_t estSize = blk->size * 8;` + `for (int attempt = 0; attempt < 8; attempt++) { ... estSize *= 4; }`，全败 `result.clear(); return result;`（静默空块） | **OPEN** | AIO-001 | 无 | 「无字节封顶 + 静默空块」静态可判部分复现（峰值 8×4^k 为算术推论）；**附加判据**：ahpx 已标记废弃且**不在构建图内**（根 CMakeLists 0 命中 ahpx；src/ahpx/DEPRECATED.md 自述「不用于生产路径」）⇒ 生产不可达；修死代码无生产收益，登记后续（若 INT-001 删除 ahpx 则本族转 VOID） |
| W5-N-15 | G_GOV_GATE | P2 | 旧宪章 §4.1（数据对象不得混同）；C-20 哨兵不得与合法值同值 | ENGINEERING_SPEC §3（数据对象按 UNIFIED_MODEL 区分，禁止一字段多义）；§9 | `sed -n '386,392p' lib/infrastructure/aio/src/aio_fits.cpp` → `auto kw_float = ...; try { return std::stod(v); } catch (...) { return def; }`；`sed -n '488,494p'` → `catch (...) { cal.ccd_temp = 0.0; }` | **OPEN** | AIO-001 | 无 | 「没有温度」与「温度解析失败」事后不可辨在现树逐字复现（缺失/解析失败/空值折叠为同一默认值，且 0.0 与物理合法温度同值）。哨兵显式化涉及字段语义 ⇒ 登记裁决 |
| M2b-H-01 | H_NUMERIC | P1 | 旧宪章 §12.3/§13.1 | docs/algorithms/HIPS_WRITER.md §9「sky fraction 绝对误差 <1e-9；MOC/properties 键值精确相等」 | `awk 'FNR==986||FNR==1501' aio_hips_writer.cpp` → `986:     kv.push_back({"moc_sky_fraction", std::to_string(moc_frac)});`、`1501:                     "  \"moc_sky_fraction\": %.8f,\n"`；`grep -n moc_sky_fraction` → 另见 `1224:`；`awk 'FNR==300' docs/algorithms/HIPS_WRITER.md` → `   绝对误差 <1e-9；MOC/properties 键值精确相等；checksum 字段由 CFITSIO` | **OPEN** | AIO-001 | 无 | properties 侧 std::to_string 固定 6 位小数 vs manifest 侧 %.8f ⇒ 同一产品同名键可有不同字面值。**裁决项**：统一到哪个字面精度会改变冻结容差面（<1e-9 与 %.8f 的 1e-8 量化本身不自洽），且既有断言把 6 位小数值钉成期望 ⇒ 本任务不改，列入需权威裁决 |
| M9-H-4 | H_NUMERIC | P1 | 旧宪章 §14.4/§8.6/§12.2-§12.3/§4.1 | docs/plugins/infrastructure/17_aio.md §7（哈希/格式不匹配→拒绝读取；I/O 错误→exit 7）；ENGINEERING_SPEC §1（公共 C ABI 版本化） | `sed -n '518,530p' lib/infrastructure/aio/src/hips/aio_hips_reader.cpp` → `522:                              int max)  {`、`526:        int n = (int)std::min((size_t)max, d->snr.size());`；`grep -c 'max < 0' 同文件` → `0`（退出码 1） | **OPEN** | AIO-001 | 无 | max 为 int 且全函数无负值判定 ⇒ (size_t)max 对负值取巨数后 min 取产品内点数，n 可远超调用方缓冲，一次写 ra/dec/snr/star_id/quality_flags/photometric_status 六个缓冲。本任务未修（避免与 V11-N-01 的 ABI 结构裁决在同一处反复改） |
| M9-H-5 | H_NUMERIC | P1 | 旧宪章 §14.4/§11/§8.3 | docs/plugins/infrastructure/17_aio.md §7/§8（格式不匹配→拒绝读取；哈希/校验失败拒绝读取） | `sed -n '565,580p' lib/infrastructure/aio/src/ahpx/aio_ahpx_reader.cpp` → `size_t floatCount = rawData.size() / sizeof(float); result.resize(floatCount); std::memcpy(result.data(), rawData.data(), rawData.size());`（readSnr）；`sed -n '625,640p'` → readWeight 同型；正对照 `sed -n '543,552p'` → readPixels 有 expectedSize 等值校验 | **OPEN** | AIO-001 | 无 | readSnr/readWeight 按 float 个数 resize 却按字节数 memcpy ⇒ 块长非 4 倍数时堆越界写 1~3 字节。**同一 ahpx 不可达性**（见 W5-N-13）：该文件不在构建图内、已废弃，无测试目标 ⇒ 修死代码无生产收益，登记后续（删除 ahpx 则本族转 VOID） |
| M9-H-6 | H_NUMERIC | P1 | 旧宪章 §14.4/§3.1/§8.3/§17.9 | ASTROCS_DESIGN §10.1（Windows/Linux 同一 C ABI）；ENGINEERING_SPEC §1（Windows 10+ amd64 下限） | `sed -n '345,355p' aio_ahpx_reader.cpp` → `            blk.offset = (uint64_t)extractNumber(objStr, offPos, nullptr);`；`sed -n '435,450p'` → `    if (std::fseek(m_fp, (long)offset, SEEK_SET) != 0) {`、`std::fread(dst, 1, size, m_fp)`；正对照 `grep -n '_fseeki64\|fseeko' lib/infrastructure/aio/src/hiss_reader.cpp` → `308/310/477/480/545/547` | **OPEN** | AIO-001 | 无 | JSON 浮点 double 强转 uint64 作块偏移/长度（越界转换 UB），(long)offset 在 LLP64 折成 32 位，全链无「offset+size ≤ 文件长度」对照；hiss_reader 有 _fseeki64/fseeko 跨平台正对照而 ahpx 无。同上：ahpx 不在构建图内 ⇒ 登记后续 |
| M2a-H-3 | H_NUMERIC | P2 | 旧宪章 §4.1/§5.3 | docs/plugins/algorithms_phase1/08_drizzle.md §4（signal 单位/源-目标像素面积/pixfrac/归一必须统一；常量面亮度与总积分通量 Oracle 同时成立） | `sed -n '568,576p' aio_hips_writer.cpp` → `                if (sup > 1.0) sup = 1.0;`；`sed -n '638,646p'` → `                flux = sig_n[i] * sup_n[i] * ps->A_cell;`；`grep -c support_clamped 同文件` → `0` | **OPEN** | AIO-001 | 无 | sup 钳到 1.0 后层级归约仍以 sig·sup·A_cell 反乘还原通量 ⇒ 覆盖 >1 的像素上层通量被系统性低估、闭合等式失效，且无 support_clamped 计数（不可观测）。**裁决项**：改归约公式或改 I2 不变量属科学语义 ⇒ 列入需权威裁决 |

**结论**：32/32 覆盖，0 VOID、0 UNVERIFIABLE；30 OPEN + 2 RESOLVED。

## 本轮就地修复的条目

| ID | 改动 | 改前红 | 改后绿 |
|---|---|---|---|
| M2b-A-01 | nside 必须为 2 的幂且 <=2^29（aio_hips_product_begin） | p1hips_negative 6 check 失败、rc=8 | 同用例 rc=0 |
| M9-G-6 | properties 原子落盘 + 失败传播 | 负例 N10 报 frc==-3 got 0（properties_exists=0）、rc=8 | 同用例 rc=0；全 6 组 p1hips 通过 |
| （工具）AIO-OWN-002 | tools/check_aio_ownership.py 新增算法目录禁产品 I/O | 注入 fits_create_file 后 rc=1 | 真树 rc=0；--self-test 正例 0 / 负例 >=1 |

