# AIO-001 · 自证摘要

| 字段 | 内容 |
|---|---|
| 任务ID | **AIO-001**（收敛唯一 AIO 与原子产品边界） |
| 执行者 | AstroCS 控制包执行 Agent（SubAgent，零 git 写） |
| 时点 | HEAD=`8f0a4c6b`；工作树含并行线未提交改动（本次取证均为当时工作树实况） |
| 层/依赖 | L2；DATA-001 / ARCH-001 |
| 清单条目 | **32**（`reports/PROJECT-GOVERNANCE-01/root-scan/by_owner/AIO-001.txt`），P0-OPEN **4** |

## 1. 交付物

| 交付物 | 路径 |
|---|---|
| 翻译表（含无法翻译项） | `reports/PROJECT-GOVERNANCE-01/AIO-001/TRANSLATION_TABLE.md`（31 行 + 无法翻译/需注意 5 条） |
| 逐条处置表（32 行 × 10 列） | `reports/PROJECT-GOVERNANCE-01/AIO-001/DISPOSITION_TABLE.md` |
| P0-OPEN 逐条结论 + 需裁决清单 | `reports/PROJECT-GOVERNANCE-01/AIO-001/P0_OPEN_CONCLUSIONS.md` |
| 改动清单 + 红绿证据 | `reports/PROJECT-GOVERNANCE-01/AIO-001/改动清单与红绿证据.md` |
| 命令日志（逐条 + 验收） | `run/PROJECT-GOVERNANCE-01/AIO-001/logs/` |
| 本摘要 | `run/PROJECT-GOVERNANCE-01/AIO-001/自证摘要.md` |

## 2. 逐条验收命令与退出码

| 命令 | 退出码 | 关键输出 |
|---|---|---|
| `timeout 60 python3 tools/check_aio_ownership.py` | **0** | `AIO-OWN-002_PASS` / `IO-002_PASS` |
| 注入算法目录 FITS 调用后同命令 | **1** | `AIO_OWNERSHIP_VIOLATION: 1 处` + 注入文件行号 |
| `timeout 60 python3 tools/check_aio_ownership.py --self-test` | **0** | `positive=0 [PASS]; negative=2 [PASS]` |
| `timeout 300 bash -c 'cd build && ctest -R p1hips_negative --output-on-failure'` | **0** | `Passed 0.53 sec` |
| `timeout 600 bash -c 'cd build && ctest -R p1hips_ --output-on-failure'` | **0** | `6/6 Passed` |
| `timeout 1200 bash -c 'cd build && ctest -R "aio\|hips\|io_\|io_adapter\|v6_aio"'` | **0** | `30/30 Passed` |
| `timeout 300 python3 -m unittest discover -s tests/io -t tests/io` | **0** | `Ran 43 tests … OK` |
| `timeout 3000 bash -c 'cd build && ninja'` | **0** | `103/103` |

## 3. 改动文件清单（工作区，未提交）

| 文件 | 类型 |
|---|---|
| `lib/infrastructure/aio/src/aio_atomic_file.h` | 新增（AIO 统一原子落盘原语） |
| `lib/infrastructure/aio/src/hips/aio_hips_writer.cpp` | 改（nside 校验 / properties 原子+传播 / fault_injected 上移） |
| `lib/infrastructure/aio/tests/p1hips/p1hips_tests_negative.cpp` | 改（负例 N1 扩充 + N10） |
| `lib/infrastructure/aio/tests/p1hips/p1hips_tests_properties.cpp` | 改（正例：无 tmp 残留） |
| `tools/check_aio_ownership.py` | 改（AIO-OWN-002 + `--self-test`） |

`git status --porcelain` 实况（限定文件域）：

```
 M lib/infrastructure/aio/src/hips/aio_hips_writer.cpp
 M lib/infrastructure/aio/tests/p1hips/p1hips_tests_negative.cpp
 M lib/infrastructure/aio/tests/p1hips/p1hips_tests_properties.cpp
 M tools/check_aio_ownership.py
?? lib/infrastructure/aio/src/aio_atomic_file.h
```

## 4. 结论计数

| 结论 | 数量 |
|---|---|
| OPEN | **30**（含 P0-OPEN 4 条全部 OPEN） |
| RESOLVED | **2**（M8-F-002 复核确认；M9-G-6 本轮修复） |
| VOID | **0** |
| UNVERIFIABLE | **0** |
| 合计 | **32** = 清单条目数 ✅（ID 集合双向差集为空） |

> 口径说明：清单 P0-OPEN=4；账本原优先级 P0 在本归属集内共 5 条（第 5 条 M8-F-002 本轮判 RESOLVED），两者不冲突。

## 5. 越界自查

| 检查项 | 结果 |
|---|---|
| `ci/**`、`.github/**` | **未改** |
| `cmake/**`、根 `CMakeLists.txt` | **未改** |
| `docs/algorithms/**`（含锚与 `anchor_contract.json`） | **未改**（P0 相关只登记，不改文档） |
| `docs/modules/MODULE_MAP.yaml` | **未改**。注：该文件在本会话期间被他域改动（mtime 19:28:05，晚于本任务开工，属 MOD-002 域）⇒ 按硬约束 5 不介入、只登记 |
| `tests/cli/**`、`tests/backend/**` 命令期望 | **未改** |
| 科学语义（公式/默认容差/SCI-ALG 冻结定义） | **未改**；涉科学/ABI 定义的 5 条（M2b-A-02 / V11-N-01 / M2b-H-01 / M2a-H-3 / M9-F-3）**停下上报** |
| 他线正在改的文件 | 未抢改。`lib/algorithms/drizzle/hips/**`、`lib/algorithms/drizzle/healpix_drizzle/**` 显示 `A`/`AM`（ARCH-001 rename 在途）⇒ **本任务未触碰** |
| git 写操作 | **零**（无 add/commit/push/branch/stash/reset/checkout/git mv） |
| skip / 放宽断言 / 删测试 | **无**。新增用例均先红后绿；`git diff` 无删除既有 CHECK |
| 运行产物落位 | 全部落 `run/PROJECT-GOVERNANCE-01/AIO-001/**`（gitignore）；正式报告落 `reports/PROJECT-GOVERNANCE-01/AIO-001/**` |

## 6. 未决项（需裁决 / 后续任务）

1. **M2b-A-02**（P0）：NSIDE 约定三源分歧 ⇒ 需裁决是否订正 `docs/algorithms/HIPS_WRITER.md:146`（只读权威，锚由 ARCH-001 同步）。
2. **V11-N-01**（P0）：`AioHipsSnrPoint` ABI 修法三选一 ⇒ 需裁决（改跨 DLL C ABI）。附加：`hips_direct_smoke.py:43` 旧 DLL 路径使该 smoke 当前不可加载。
3. **M2b-H-01**：`moc_sky_fraction` 字面精度与 `<1e-9` 容差面不自洽 ⇒ 需裁决口径。
4. **M2a-H-3**：support 钳制后层级通量归约公式 / I2 不变量口径 ⇒ 需裁决（科学语义）。
5. **M9-F-3**：`drizzle_scale_arcsec` 上界取值 ⇒ 需裁决（科学域参数定义）。
6. **M2b-C-01 剩余面 / M2b-G-01**：HiPS writer 目录级 staging+原子提交、`astrocs_p1_hips_writer` 退出路径 ⇒ 架构边界（INT-001 域）。
7. **W5-N-13 / M9-H-5 / M9-H-6**：`src/ahpx/**` 已废弃且**不在构建图内**（根 `CMakeLists.txt` 0 命中 `ahpx`；`src/ahpx/DEPRECATED.md` 自述「不用于生产路径」）⇒ 生产不可达亦无测试目标；按「不修死代码、不放大改动面」登记后续（若 INT-001 删除 ahpx，本族转 VOID）。
8. **M9-H-4**：`aio_hips_read_snr_catalog` 的 `max<0` 无守卫 ⇒ 与 V11-N-01 同属 ABI 读侧面，等裁决一并处理，避免同处反复改。

## 7. 一句话结论

AIO-001 的 32 条归属条目已逐条翻译路径并复跑判定（**30 OPEN / 2 RESOLVED / 0 VOID / 0 UNVERIFIABLE**）；本轮在文件域内就地闭合了 **nside 合法域（P0 · M2b-A-01）** 与 **properties 原子落盘及失败传播（M9-G-6）**，并把 `tools/check_aio_ownership.py` 从「只查 free()」提升为「算法目录禁产品 I/O 且能红能绿」；其余涉科学/ABI/架构边界的 5 条已按硬约束 3 **停下上报**，未自行选口径。
