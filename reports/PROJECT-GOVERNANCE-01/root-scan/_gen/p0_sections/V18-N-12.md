### V18-N-12（P0·**生产者与守卫同处的最纯形态**）`tests/backend/test_isa_avx.py`：自己写 CSV（decision 列还是硬编码字面量）→ 读回自己刚写的文件 → 循环体只有 `continue` → 末行 `assertTrue(True)`
- `test_03:78-88` 自写 `artifacts/prerelease_v5/ISA-002/MEASUREMENTS.csv`（`:87` 硬编码 `"NOT_SHIPPED(avx子集,avx2主导)"`）+ `assertTrue(isfile(out))`；`test_05:99-109` 读回**同一份自己刚写的文件**，唯一取值的循环体是 `continue`（`:107`），末行 `self.assertTrue(True)`（`:109`）。
- ⇒ **门名承诺的不变量是「avx 必被已 SHIP 的 avx2 严格主导」（§10.3 决策不变量），实断言 = 常量真**。全仓 `assertTrue(True)` 实测**仅 1 处**（`scripts_v18_taut.py`）⇒ **孤立单点，不是普遍形态**（此限定重要，防邻站以为遍地都是）。
- **与 `M8-F-014`（记 `check_isa_leak` 自检）、`V15-N-11`（同文件 `:90-96` 钉 `+28.2%`）均不同条**，挂 related。改法：删自写自读；不变量改由 `benchmark cpu` 真产出 + 三方比对（`avx` ⊆ `avx2` 的实测吞吐/误差）。
- **related** `M8-F-014`、`V15-N-11`、`V18-N-11`（其上所有注入锁连带扣减）、簇 1 机制⑤、`S-5`
