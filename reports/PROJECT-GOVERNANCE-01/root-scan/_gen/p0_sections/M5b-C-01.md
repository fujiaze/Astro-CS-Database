### M5b-C-01 API 合同表 422 行全部标 VERIFIED、396 行共用未定义占位 test_ids，门只比 4 列且自述"真实校验由 AST-API 承担"
- 类别: C_DOC_CODE_GAP · 建议优先级: P0
- 位置: `docs/contracts/API_CONTRACTS.csv::（表头 + 全部记录）`（现 :1-423；样本行 :5）；`tools/quality/contracts/check_api_contracts.py::main/required`（现 :48-52、:53-59 注释、:120-130 PASS 文案）；`docs/contracts/API-001.md::§6 检查器合同`（现 :60-67）；`docs/contracts/API-001.md::DOC-001 追溯`（现 :43-47）；`ci/checks.json::CON-API-CONTRACTS`（现 :721 起，三 profile、waivable=false）
- 证据摘录:
  > API-aio_hio_free,aio_hio_free,lib/astro_image_io/include/aio_healpix_io.h,C,AIO_EXPORT void aio_hio_free(void* ptr);,ADU/pixel/deg per header,per SCI valid_range,per header nullable,caller-alloc/free via aio_hio_free,call scope,immutable input,blocking,"thread-safe read, no global",reentrant if no global,sync,return code 0=OK,deterministic per SCI,fp32/fp64 per header,,SCI-SCOPE-001,TST-GEN-001,VERIFIED
  > # Required columns in API_CONTRACTS
  >     required = ["symbol","full_signature","header","linkage"]
- 权威依据: 宪章 §12.3-4（核心合同至少存在一个独立测试）、§13.1、§13.2、§13.4、§17-3/-4；`docs/contracts/API-001.md:61`（"每个 API-* 行 header/symbol 必须在 AST 中存在；missing_symbol=FAIL"）
- 问题说明: 计数复核（grep）：`^API-` 记录 422 行、含 VERIFIED 422 行、`TST-GEN-001` 396 行、`SCI-SCOPE-001` 396 行。①`TST-GEN-001` 在 `tests/` 与 `docs/traceability/` 的命中数为 0（只存在于本合同表自身与 `tools/quality/contracts/fixtures/` 的 fixture 副本）⇒ 396 行的 TEST 列指向一个未定义即被引用的 ID，§12.3-4 不成立；`status=VERIFIED` 却零测试工件。（对 L12-003 的修正：`SCI-SCOPE-001` 并非未定义——它在 `docs/science/SCIENCE_SCOPE.md:61` 有定义，但它是**全局范围声明**ID，把 396 个函数的科学上游一律挂到"范围/假设"文档等于逐函数 SCI 关联缺失。）②单位/有效域/nullable/所有权/生命周期/线程安全/重入/阻塞/错误模型/确定性/精度 11 列全部是同一批模板样板串，`valid_range` 与 `precision` 逐字节相同 ⇒ "科学量/容差/精度逐符号登记"不存在。③门只做"CSV 列名存在 + 非空"与 full_signature 文本比对；:53-59 注释与 PASS 文案把「参数个数一致、覆盖口径」归给 AST-API 与 DOC-005，而 AST-API 是自反恒真门（M5b-G-03）⇒ 两门互相指认，无一对 422 行做真实签名比对。④API-001 :43-47 称追溯以 `docs/TRACEABILITY.csv` 为权威，而八层矩阵 `MODULE` 层的 `module_anchor` 正指向本 CSV（矩阵列头 `module_anchor`），两表从未机器对账（`check_doc_symbols.py` 只校 `docs/TRACEABILITY.csv`）。
- 影响: 全部 C ABI 的"合同已验证"是占位文本；§17-3/-4 声称的满足度无证据基础；新增/修改导出函数不会暴露任何不一致。
- 建议处置: 删模板值，逐符号真实值或标 `MISSING:<原因>`；`TST-GEN-001` 换成可定位 TST-ID；本门自己解析头文件声明与 CSV 逐字段比对。
- 置信度: 高 · related: M5b-G-03、M5b-G-02、F00-01（M6）
