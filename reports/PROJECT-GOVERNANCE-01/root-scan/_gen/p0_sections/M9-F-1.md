## M9-F-1 本域八个恶意输入回归 TU 从不进构建：它们恰是全部 M9 边界缺陷的唯一复现件，却被 docs 当符合性证据引用

- 类别: F_TEST_GAP（主）；次挂 G_GOV_GATE（**根因判词主落 M8-F-004**；本条按 M8 定稿指令只定**实例面**，不另立独立根因）
- 优先级: P0
- 来源: L24-003（本代理复验成立；站点数由叶子的 7 补足为 8，另补 5 处「docs 当证据引用」逐字站点、8 个 TU 各自钉住的修复清单、sanitize_wsl_v4/v5 脚本分叉）
- 归属: **根因（注册门只做单向闭包 → 孤儿源对 CI 永久隐形）主落 M8-F-004**（M8 持 61 源逐 basename 抽验 = 61/61 零命中 + 判定器判据）；本条为该根因在**本域的实例面**，凡涉门机制一律 related 指回，不重复写根因判词
- 位置: 孤儿测试 TU（真源）lib/astro_image_io/tests/{test_p0_io_hardening.cpp, hips_robust_sanitize_driver.cpp, gaia_sanitize_driver.c, dataflow_fuzz.cpp, hiss_writer_smoke.cpp, pipeline_frame_contract_test.cpp}、modules/services/io/tests/{hips_core_selftest.c, fits_core_selftest.c}；正面对照 tests/unit/CMakeLists.txt::p1_io_hardening_test、::hips_publish_atomic_test；引用面 docs/DEVELOPER_GUIDE.md、docs/interfaces/io/IO_002_HIPS_INPUT_INTERFACE.md、docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv、docs/traceability/TRACEABILITY_MATRIX.{csv,json}
- 证据摘录（逐字，复核时点现文）:
  > 计数口径实证：对 8 个 TU 文件名（含 .cpp/.c 后缀）在全仓 `*.{txt,json,yml,yaml,cmake}` 做联合 grep，排除 run/build/out/Testing/artifacts 后按文件归属计数——**8 个孤儿 TU 名在 build/CI 声明文件中的命中数 = 0**；同一次查询里正面对照命中 `tests/unit/CMakeLists.txt` 11 次（p1_io_hardening 于 :761-762、hips_publish_atomic 于 :566-579）、`ci/ctest_baseline.json:90` 与 `ci/checks.json` 8 次（均只为已注册的 p1_io_hardening / hips_publish_atomic）。8 个孤儿名唯二的外部命中为 `reports/v19r7_quality/file_audit_before.json`（证据件，免报域）与 `docs/traceability/TRACEABILITY_MATRIX.json:54`（fits_core_selftest.c）。
  > test_p0_io_hardening.cpp:1-28（文件头自述）：T1..T6 恶意 fixture 清单 + 唯一"运行方式"是一行 ad-hoc `g++` 注释命令（即从不由机器执行）。
  > 引用面 1（docs/DEVELOPER_GUIDE.md:27）`astro_image_io:     pipeline_frame_contract_test / dataflow_fuzz` —— 作为测试入口清单呈现。
  > 引用面 2（docs/interfaces/io/IO_002_HIPS_INPUT_INTERFACE.md:46）`| C 层自检驱动 | `modules/services/io/tests/hips_core_selftest.c` |` —— IO-002 合同把该边界自测定为这个从不进构建的文件。
  > 引用面 3（docs/architecture/PRODUCTION_EXECUTION_INVENTORY.csv:270/:276）`io_writer,dataflow_fuzz.cpp,…,production,yes,Phase1/2/3,…` 与 `io_writer,pipeline_frame_contract_test.cpp,…,production,yes,…` —— 生产可达清单标 `production,yes`。
  > 引用面 4（docs/traceability/TRACEABILITY_MATRIX.csv:31 + .json:54）io 模块行以 `modules/services/io/include/astrocs/io/fits_stream_v1.h` 为锚、并把 `fits_core_selftest.c` 挂入 API/TEST 证据列（该模块的 SCI/ALG 列为 MISSING）。
  > 唯一非构建执行者：lib/astro_image_io/tests/sanitize_wsl_v4.sh / v5.sh 仅引用两个 sanitize driver（手工脚本，不在 ci/checks.json 任何 profile 内）。
  > **覆盖面精确表述（按 M8 反证 UT-GAIA-ZLIB 已在册，改为「部分在册」口径，本代理自证）**：gaia 客户端**并非无注册测试面**——①`ci/checks.json:3105` 起 `"id": "UT-GAIA-ZLIB"` 采 `lib/gaia_xpsd_client/tests/test_gaia_zlib_configure_contract.py`（该目录唯一文件，确为 lib 面 Python 在册样本）；②`tests/unit/CMakeLists.txt:787-915` 的 CAT-GAIA-TEST 族在册（gaia_cat_test 七模式 + gaia_cat_polar_ref + gaia_cat_oracle + gaia_cat_adapter + gaia_integration）；③同文件 :121-129 `gaia_unshuffle_test` 亦在册。本条的**精确主张**只是：上列 8 个**恶意输入/解析健壮性** TU（含 gaia_sanitize_driver.c）不在任何 add_executable/add_test/checks.json 中——即"在册面覆盖功能与配置契约，未覆盖健壮性负例"。
  > 实例面佐证（**根因判词归 M8-F-004**，此处只留本域可见性证据）：本代理核对 `tools/quality/check_ctest_registration.py` 的 C1..C6 契约后确认其 universe 只来自 `add_test(NAME …)`；门本身非豁免——`ci/checks.json` 的 `CTEST-REGISTRATION`（复核时 :2412-2442）挂 fast/linux-main/**windows-main** 三 profile、`"waivable": false`。
  > **8 个孤儿 TU 各自钉住的修复（逐字取自各 TU 文件头，本代理现读）**：
  > · test_p0_io_hardening.cpp:2-15 —— T1 XISF `geometry="999999999:999999999:1"`、T2 XISF xml_length 超 64MB 上限、T3 FITS `NAXIS1/NAXIS2=999999999`、T4 巨头 read 路径 `data_size` 巨量分配（上限 1GB）、T5 `CCD-TEMP='TBD'` 触发 `std::stod` throw→terminate、T6 合法文件不误伤回归；文件头逐条写「修复前」形态（calloc(4e18)→上层解引用崩溃、bad_alloc 跨 C 边界、terminate）。
  > · hips_robust_sanitize_driver.cpp:3-5 —— HiPS writer FP32/FP64 + VOTable metadata 写入完整性、corrupt TSV rows / corrupt properties / corrupt Moc.fits 输入、读回 catalog 与 tile 的异常路径（**正是 M9-F-2/M9-F-3/M9-G-6 所在文件的负向面**）。
  > · gaia_sanitize_driver.c:1-2 —— ASan/UBSan 驱动跑真实 XPSD 解析（DR3SP parser），入口即 `gaia_client_create_ex`（**正是 M9-G-1/M9-G-2/M9-H-2/M9-H-3 全族的复现件**）。
  > · dataflow_fuzz.cpp:3-8 —— 固定种子确定性 fuzz：Pipeline cache 10000 变异、HISS 文件 10000、JSON 配置 5000、snr_model RAW 5000（即 AIO 输入面的全部机器防线）。
  > · hiss_writer_smoke.cpp:1-2 —— HissWriter open→add_tile(FULL+BITMAP)→finalize + 文件头签名校验。
  > · pipeline_frame_contract_test.cpp:1-13 —— 帧合同（含 `aio_frame_copy_metadata` 异常屏障面）。注：本 TU 的 include 面极薄（无文件头自述），其归属由引用面（PRODUCTION_EXECUTION_INVENTORY.csv:276 标 `production,yes`、DEVELOPER_GUIDE.md:27）确立。
  > · hips_core_selftest.c:1-13 —— IO-002 §8 验收映射全套（properties 必填键、tile width 非 2 次幂、ipix 越界→ADDRESS、头卡冲突→TILE_INVALID、partial tree 绝不父 order 回退、MOC hint、f32/f64 平面读回），fixture 由 tests/io/make_hips_fixture.py 生成。
  > · fits_core_selftest.c:2-12（**本代理补的第 8 站点**）—— IO-001 §12 验收映射：BAD_HEADER / TRUNCATED / MISMATCH / **CHECKSUM** / NANINF / CANCELLED / **DISKFULL（只读目录写失败）** / 正常往返「原子写 → 读回」——即 M9-G-4、M9-G-5、M2b-L15-006 三条所要求的机器验证面，全部存在且全部从不进构建。
  > 唯一非构建执行者是两个手工脚本，且二者已分叉：sanitize_wsl_v4.sh:3-11（Phase1 Final Signoff **V4, G6**，覆盖 [1]HEALPix core vs 1e6 全天 oracle、[2]HiPS writer/reader FP32、[3]robustness corrupt TSV/properties/Moc、[4]DR3SP parser、[5]order-7 spatial fuzz vs astropy-healpix）与 sanitize_wsl_v5.sh:3-12（**V5, G7**，同五项 + 第 [6] 项「NESTED local<->xy<->FITS 标准 tile 映射 HIPS-IMG-001, 262144 全量」，[2] 改为「V5 标准 tile 排列」）—— 两脚本都要求外部 oracle JSONL 参数、`set -e`、且**不在 ci/checks.json 任何 profile 内**（计数口径：v4/v5 对 CI 声明文件的命中 0），即"有验证脚本"不等于"有机器门"。
  > 同目录已注册兄弟（证明是遗漏非风格）：tests/unit/CMakeLists.txt:761-769 `add_executable(p1_io_hardening_test ../../lib/astro_image_io/tests/test_p1_io_hardening.cpp)` + `add_test(NAME p1_io_hardening …)`；:566-580 `hips_publish_atomic_test`（源 p1_hips/publish_atomic_test.c）。
- 权威依据: 宪章 §13.1/§13.2（核心合同须有独立正向与负向测试且可失败）、§14.4 末段（对已观察到的故障建最小复现与机器测试）、§12.3-4（登记与事实一致）、§17.2（追溯不得断链）、§17.10；本仓 CI-REG-002 立规自述（新增测试目标须被 CI 显式登记）。
- 问题说明: 这 8 个 TU 不是普通未注册测试，而是**本域 12 条边界缺陷（M9-H-1/2、M9-G-1..5 等）的复现与防回退件**：AIO extern "C" 入口异常屏障、HISS/AHPX 恶意 fixture、XPSD 解析 ASan/UBSan 驱动、HiPS MOC 与 SNR TSV 上限、IO-001/IO-002 C 层自检全套。叶子记 7 个，本代理补第 8 个 `modules/services/io/tests/fits_core_selftest.c`（与 hips_core_selftest.c 同目录同性质，同样只被 TRACEABILITY_MATRIX 当证据）。加重事实：**5 处文档面把它们当"已在册/已生产可达"的证据**（DEVELOPER_GUIDE 测试清单、IO-002 合同的 C 层自检行、PRODUCTION_EXECUTION_INVENTORY 的 production=yes 两行、追溯矩阵 csv+json），即读者据文档判定"该边界有机器验证"，而实际从不存在。另 lib/orchestrator/cpp 整树确无 CMakeLists.txt（该目录只有 Makefile，且树内尚存 orchestrator.exe/nul 残留），其原子写出与 checkpoint 行为同样无保护。
- 影响: ①机器门可发现性本身失效：约 6000 行验证代码不产生任何信号，CI 全绿不可证伪；②本域所有边界修复（含正在推进的批次）可被无声回退——M9-H-1/M9-H-2 的建议处置里"注册 ASan 负例"这一步在门补齐反向闭包前**无法验收**；③文档符合性声明与事实相反（§12.3/§17.2 断链）；④宪章 §13 必备测试面在本域实为零覆盖，属发布阻断级而非质量改进项。
- 建议处置: ①反向闭包（C7 建议）由 **M8-F-004** 主落，本域配合：门须扫描 tests/、lib/**/tests/、modules/**/tests/ 下含 main() 或自述 test 的 TU，未出现在任何 add_executable/add_test/注册脚本即 FAIL，并补负例自检；②8 个 TU 逐个定归属：能进 ctest 的进 tests/unit/CMakeLists.txt 并登记 ci/checks.json（sanitize 类可单列非默认 profile 但**必须在册**），确认废弃的删除而非留在树里充当证据；③**同步订正 5 处引用面**：DEVELOPER_GUIDE:27、IO_002:46、PRODUCTION_EXECUTION_INVENTORY:270/:276（production 列）、TRACEABILITY_MATRIX 两处——未注册前不得再作为符合性证据引用；④lib/orchestrator/cpp 要么入构建，要么按 §14.1 明示非交付状态并停止在交付文档中引用其行为。
- 置信度: 高（命中计数与文件归属口径复核，不依赖行文本等值比较）
- related: **根因主落 M8-F-004**（L23-004/005 的 61 源逐 basename 抽验与「注册门方向相反」判词归 M8；本条只定稿本域 8 TU 身份、各自钉住的修复、sanitize_wsl_v4/v5 分叉与 5 处文档误引，不重复计口、不复写根因）；**M8 反证 UT-GAIA-ZLIB 已在册已采纳**（本条覆盖面表述已改为「部分在册 + 健壮性负例零注册」）；M9-H-1、M9-H-2、M9-G-3、M9-G-4、M9-G-5、M9-H-5、M9-H-6（这些条目的"补负例"处置全部依赖本条先落地）；F00-03（同类"门对不可见对象失灵"）、L22-005（修复未固化）、S3-017（orchestrator 整树不注册）
- **【E1-实测 2026-09】** 八 TU 五列实测（tracked / CMake add_executable|add_test|GLOB / 既有 build ctest -N（288 与 169 两条列表）/ ctest_baseline.json / docs 当证据）：8/8 tracked；8/8 CMake 命中 0；8/8 不在任何既有 ctest -N 列表（含 fits_core_selftest.c、hips_core_selftest.c——用例名维度 0 命中）；8/8 baseline 命中 0；docs 引用面 = M9 已列 5 站点全部复现（另 3 个 sanitize/smoke 类 TU 在 docs 真源零引用）⇒「从不进构建」由静态推演改判为**实测确证**，本条 **仍成立**；五列全表见 _cache/E1.md §2。
