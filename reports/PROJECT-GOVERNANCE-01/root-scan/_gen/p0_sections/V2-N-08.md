### V2-N-08 FAST 模式的裁决前提已被同批另一提交推翻 ⇒ `psf.max_stars` 静默决定交付的 SNR／极限星等，且无任何 parity 锁
- **ID** V2-N-08 ｜ **类别** G_GOV_GATE ｜ **优先级** **P0**（我裁定：V2 原判「P0/P1 需前台裁」）
- **位置**：截断源 `lib/core/src/module_adapters.cpp:1761-1765`（`psf.max_stars` 默认 **5000**）与 `:1772` `constexpr bool kPrecisePsfEnabled=false` + `:1785 if constexpr`（⇒ **生产永不进精确分支**）；新消费者 `module_adapters.cpp:2505` 起（SNR 节点读 `src_frame->value("psf_params")`）；语义 `lib/phase1/noise/README.md:50`「**SNR 目录成员 = `p1_sources.json` 的 `psf_params` ∩ sources**」。
- **证据链（这是关键）**：`23a7f665`（P2 PSF FAST）的正当性建立在「`psf_params` 在仓库内**零消费者**，截断只影响诊断」之上；而**同批 `35c85f53`（P8）恰好造出了这个消费者** ⇒ 前提被自己推翻。现况：`n_detected=145884` 被截到**最亮 ≤5000 颗**，**这 5000 颗直接决定 `p1_snr.json` 的 `median_snr`/`snr_phot`/`frame_depth_m5_mag`** ⇒ **SNR 与极限星等系统性偏乐观**（截最亮 ⇒ 残差与深度都偏好）。
- **为什么不是 P1**：①交付 JSON 里的科学量被一个**诊断性性能开关**静默决定；②**无 parity 锁**——P2 当初自证的「`p1_snr` 逐位不变」测的是**当时还没有该消费者**的实现，P8 的新锁用手工 fixture，两头都不覆盖这个耦合；③`psf_mode` 又恒标 `"fast"`（见 V2-N-09）⇒ **下游连"被截断过"都读不出来**。合起来正是宪章 §4.3「manifest 记实际执行路径」+ §17.6「禁静默降级」双违，且落在交付数值上。
- **建议处置（V2 原案，三选一或组合）**：①把 SNR 目录改回**全量成功星**（精确 PSF 重新有价值，FAST 只用于形态诊断）；②或在 `p1_snr.json` 显式记 `psf_mode`/`n_fit_input`/`n_sources` + **目录被截断标记**，并把该耦合写进 `DATA-P1-SNR` 与 `SCI-CW`；③无论哪种都补一条 parity 锁（同一输入在 `max_stars=0` 与 `5000` 下 `median_snr` 差异必须**可见且被断言**）。
- **取证口径** V2 静态逐字（三处锚 + `--stat` 时序对照）；**偏移幅度需真实帧实测，V2 与我均未跑** ｜ **置信度** 高（耦合存在性），影响量级未知 ｜ **related** **V2-N-09**（同一事的可观测面）、`V1-N-12`（同属交付 SNR 数值正确性）、`M2a-H-1`（同族反向：那条是请求被忽略，这条是截断被静默传播）、`A-40`
