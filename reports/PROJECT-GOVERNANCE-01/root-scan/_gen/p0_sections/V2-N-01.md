### V2-N-01 IpvParams 公共 C 结构体布局已改而仓内 ctypes 镜像未同步 ⇒ 72 字节越界写 + 字段全错位
- **ID** V2-N-01 ｜ **类别** G_GOV_GATE（内存安全，与 M9-H-1 同族）｜ **优先级** **P0** ｜ **状态** OPEN
- **位置**：`lib/plate_solve/tools/diag_gaia_psf_projection.py:61-80`（ctypes 镜像，`:73` 仍含已删的 `m_lim_step`）→ `:192` 调 `ipv_get_default_params` → C 侧 `lib/plate_solve/cpp/ipv/src/ipv_entry.cpp:333-335` 首行 `std::memset(params, 0, sizeof(IpvParams))`。
- **证据**：V2 按两份定义分别算布局 —— Python 镜像 **352 字节**、C 侧 **424 字节**（删 `m_lim_step`、增 10 个 `double`；`log_dir` 由 96 扩到 168）⇒ C 侧按 424 写、调用方只给 352 ⇒ **72 字节越界写 + 其后所有字段错位**。
- **为何本批新引入**：`IpvParams` 的字段变更在 `23a7f665`/`6d74046d` 一线（FAST 与割线迭代参数化），而镜像脚本未同步。更关键的是**同批在 `M3-C-002` 上以「公共头与 HEAD 逐字节相同」整批回退 ABI** ⇒ 纪律是清楚的，只是**没用于自己改的这处**。
- **加剧项**：`IpvParams` **无 `struct_size`/`abi_version` 字段**（宪章 §8.6 明文要求），故此类错位在结构体层面不可检；且该 diag 工具在 `ci/**`、`tools/quality`、`docs/**`、`tests/**` **零引用** ⇒ **任何机器门都看不见它**（与 C-02「注册校验器与被校验对象生成方式不匹配」同型）。
- **依据条款**：宪章 §8.6（跨语言结构须带版本/尺寸）、§11（不得越界、ABI 稳定）、§12.3。
- **影响**：越界写属内存安全类（同批 B1 刚按 P0 处理了两例），且方向是**被诊断工具触发**——诊断路径一旦有人跑即崩或静默读错字段（更糟：读到错位的 `double` 当参数用）。
- **建议处置**：①同步 ctypes 镜像（删 `m_lim_step`、补 10 个 `double`、`log_dir` 改 168）；②给 `IpvParams` 补 `struct_size`（或 `abi_version`）首字段，C 侧与 Python 侧各自断言，令其**结构上不可错配**；③把该 diag 脚本纳入 `ci/checks.json` 或 `tests/` 采集面（否则永远是暗面）。
- **取证口径** V2 双份定义逐字段求和（静态可判，无需运行）｜ **置信度** 高 ｜ **related** **M9-H-1/M9-H-2**（同族且同批双标）、V2-N-02、C-02、簇 9（修复即扩散）、A-38
