### M8-F-004 被 STANDARDS_REGISTRY / TEST_MATRIX / TRACEABILITY 当作符合性证据的测试源从不在任何执行面出现；注册表判定器只验「路径存在」且自身既未注册也未挂账

- 类别码: F_TEST_GAP（后果含 B_STD_MISMATCH / E_TRACE_BREAK）
- 优先级: **P0**（§17.10/§13.2 级证据链失效，且判定器不在册）
- 位置:
  - 判定器：`docs/standards/checks/check_standards_registry.py::evaluate`（复核时点 :180、:209 `key, std, ver, _clauses = row`、:243 `C3_clauses_form_*`、:249 `C3_evidence_path_*`）
  - 判定面：`docs/standards/STANDARDS_REGISTRY.md`（复核时点 :135 D.spherical `test_healpix_oracle.cpp`、:154/:159/:162 D.drizzle `candidate_oracle_test.cpp`）
  - 判定面：`docs/contracts/TEST_MATRIX.md::TEST-DRZ-CAND-001`（:37）、`::TEST-NOISE-001..015`（:36）
  - 判定面：`docs/traceability/TRACEABILITY_MATRIX.json::test_path`（:54 `modules/services/io/tests/fits_core_selftest.c`）
  - 判定面：`docs/science/NOISE_MODEL.md`（:59、:130 以 `noise_model_science_test.cpp` 为 TST-NOISE 载体）、`docs/TRACEABILITY.csv`（SCI-NOISE-001..015 行以之为验证）
  - 被测源真身：`lib/healpix_db/healpix_drizzle/tests/candidate_oracle_test.cpp`、`lib/common/healpix/tests/test_healpix_oracle.cpp`、`lib/snr_estimator/cpp/test/noise_model_science_test.cpp`（**订正 L23 锚：`tests/unit/noise_model_science_test.cpp` 不存在**）、`modules/services/io/tests/{fits_core_selftest.c,hips_core_selftest.c}`
- 证据摘录:
  > （check_standards_registry.py:249）results.append(check("C3_evidence_path_%s" % key, False, "证据路径不存在: %s" % ref))   ← 判据只有 path_exists
  > （check_standards_registry.py:209）key, std, ver, _clauses = row   ← 条款维数解析后被丢弃
  > （STANDARDS_REGISTRY.md:162）| §4（欠采样重建与候选枚举完备性） | ... | CONFORMANT | lib/healpix_db/healpix_drizzle/tests/candidate_oracle_test.cpp | ...
  > （TRACEABILITY_MATRIX.json:54）"test_path": "modules/services/io/tests/fits_core_selftest.c"
  > （grep 实测）`STD-REGISTRY` 全仓 **0 命中**；`check_standards_registry` 在 `ci/checks.json` **0 命中**；`ci/known_failures.json` 全文仅 2 条挂账（UT-CLI、p1_noise_adapter），无该项
- 权威依据: 宪章 §12.3-4、§13.2（登记的证据必须是实际执行过的验证）、§1.1（权威分层）、§17.10；AGENTS.md「修改后必须验证才能报告完成」
- 问题说明: 三个断点在同一条链上，本条定稿其中之三（③），①②见 related。③**证据路径只验存在、不验执行**：判定器对 EVIDENCE 单元格只跑 `path_exists`，故「CONFORMANT」可以完全建立在从未被编译过的 .cpp 上。一手复算（口径=逐 basename 全仓 grep，排除 run/build/artifacts/evidence/reports/问题扫描，再看是否出现在 CMake 非注释行 / `ci/**` / Makefile / `tools/**/*.py`）：`candidate_oracle_test` 唯一非文档命中是 legacy 追溯脚本 `tools/quality/v19r3_traceability.py:218`（该脚本硬编码 WSL 路径 `/mnt/f/Astro dev/...`，未挂 checks.json）；`control_median_mc_test`/`variance_propagation_test`/`kcorr_matrix_test`/`concurrency_cache_test` 同上；`test_healpix_oracle` 仅命中 `lib/common/healpix/Makefile`；`fits_core_selftest`/`hips_core_selftest` 命中面为 0 个执行者；`noise_model_science_test` 命中面为 docs + reports + 该 TU 自身。`lib/common/healpix/tests/test_healpix_oracle.cpp` 有源文件，但唯一"构建方式"是文件头手敲命令行注释。`test_spherical_overlap.cpp`、`drizzle_nonfinite_test.cpp` 在 CMake 中仅出现于**注释**（"与 drizzle_nonfinite_test 互补不重复"）→ 不属注册。另 ①判定器本身既未注册也未挂账，只被 6 处 docs 引用。
- 影响: 六域符合性判定与追溯矩阵的证据链失效（登记与事实不符）；若按 STANDARDS_REGISTRY 做 §17.10 清零判断，会把未验证当已验证；且该判定器不在册，连"漂移即红"都无从发生。
- 建议处置: ①给 `check_standards_registry.py` 增 C7 规则「EVIDENCE 路径必须出现在 `ci/ctest_baseline.json` / `ci/checks.json` / discover 采集根 三者之一」，且 C 规则的判定维度数不得少于条款行数（对齐 `_clauses`）；②把该判定器注册为 checks.json 项（或按负责人裁决显式挂账，不得留空）；③TRACEABILITY/TEST_MATRIX 证据列补注「执行途径 + 最近执行记录」；④确认废弃者移 archive/删除（待负责人裁决）。
- 置信度: 高（判定器逐行读 + 逐名 grep 复算；文档命中面全列）
- 来源: L23-004（定稿；主落 M8，与 M2a-G-1 / M9 会签见 _merge §8）
- related: **M2a-G-1**（判定器不在册＝符合性证据链断点①，主落 M2a）、**M7**（判定式维度少于条款面＝断点②，:186/:193 已交 M7）、M6b-G-001（无据 VERIFIED 根因）、M8-G-001（三向空集元结论，本条为其方向③）、L23-005、L22-005
- **【E1-实测 2026-09】** 「从不在任何执行面出现」判词落地（HEAD fa5581a0，命令=git ls-files / grep --include=CMakeLists.txt,*.cmake / ctest -N --test-dir build 与 build/asan / grep ci/ctest_baseline.json）：本条点名 11 TU（含 M9-F-1 八 TU + candidate_oracle_test.cpp、test_healpix_oracle.cpp、noise_model_science_test.cpp）全部 **tracked 且在盘**；basename 在真源 CMake/CMakeLists 命中 **0/11**（全仓 file(GLOB 命中 0，无整目录收录可能）；两份既有 ctest -N 列表（288 / 169 条）命中 **0/11**（列表存 run/审计执行层/E1/ctest_N_*.txt）；ctest_baseline.json 命中 **0/11**；docs 登记面仍把其中 ≥7 个当证据（TRACEABILITY_MATRIX .csv/.json、IO_002:46、DEVELOPER_GUIDE:27、PRODUCTION_EXECUTION_INVENTORY:270/:276、STANDARDS_REGISTRY、TEST_MATRIX、NOISE_MODEL）。docs 外唯一「执行者」面 = sanitize_wsl{,_v4,_v5}.sh、tools/redteam_v19.py、tools/quality/v19r3_{sanitizer,traceability}、lib/common/Makefile，其对 ci/checks.json、ci/steps、.github 命中 **全 0** ⇒ 本条 **仍成立**（四态由「静态推演」升「实测确证」）。附带订正：条目行 81 的 known_failures「仅 2 条挂账」当场复测=2（failures 数组 len=2：UT-CLI、p1_noise_adapter），为真。