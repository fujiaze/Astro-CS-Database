### M8-F-001 `tests/abi` 四个验收脚本被 UT-ABI 以「0 用例」方式采集，该门的加载/注册/echo 三面永不失败；文档仍以「36/36 PASS」为验收证据

- 类别码: F_TEST_GAP
- 优先级: **P0**（门禁形同虚设 + 已验证声明无执行证据）
- 位置:
  - `ci/checks.json::UT-ABI.command`（复核时点 :1642-1671，`waivable=false`、profiles 含 linux-main）
  - `tests/abi/test_secure_loader.py::main`（复核时点 :216 起为脚本式 `def main()`；全文件 `TestCase` 零命中）
  - `tests/abi/test_module_registry.py::main`（复核时点 :122）、`tests/abi/test_abi005_echo.py::main`（复核时点 :160）、`tests/abi/mod001_install_load_check.py::main`（复核时点 :186）
  - 反面对照（同目录内**可失败**的正规写法）：`tests/abi/test_abi002_lifecycle.py`（复核时点 :82/:161/:226/:269 四个 `unittest.TestCase` 类，被 discover 正常采集）
  - 文档面：`runtime/module_loader/README.md:39`「python3 tests/abi/test_secure_loader.py # 退出码 0 = 36/36 PASS」
- 证据摘录:
  > （UT-ABI 命令）"python3","-B","-m","unittest","discover","-s","tests/abi","-t","tests/abi"
  > （runtime/module_loader/README.md:39）python3 tests/abi/test_secure_loader.py   # 退出码 0 = 36/36 PASS
  > （tests/abi/test_abi005_echo.py:160 区段）def main(): ... / if __name__ == "__main__": sys.exit(main())   # 全文件无 TestCase 子类
- 权威依据: 宪章 §12.3-4（机器检查必须能失败）、§13.1（每模块必备独立 Oracle / 负面测试）、§18.4（插件签名与安全加载属发布门）
- 问题说明: `unittest discover` 只把 `TestCase` 子类计入采集集合（CPython TestLoader 语义；**判据为外部标准库语义，不作仓库锚**，本条以「同目录内唯一一个含 TestCase 的文件被采集、其余四个不含」的仓内可复算事实立证）。实测 `tests/abi` 5 个可执行测试文件中仅 `test_abi002_lifecycle.py` 含 TestCase ⇒ 安全加载（签名/白名单/卸载负面面，509 行）、模块注册表、ABI-005 echo、MOD-001 安装加载四个面的采集数为 0，discover 输出 `Ran 0 tests / OK`、退出码 0 ⇒ 门为绿。另实测 `ci/**`、`tools/**`、`ci/steps/*`、`.github/workflows/*` 内无任何命令直接执行这四个脚本（逐名 grep 零命中）。
- 影响: 安全加载与模块注册表两个发布门面在 CI 上从未执行却计入 PASS；`waivable=false` 的"非豁免"外观给了该门比实际更强的可信度；插件签名面回归不可能被 CI 捕获。
- 建议处置: ①四脚本改造为 `TestCase`（保留 `main()` 供人工直跑），或新增 `UT-ABI-LOADER` 检查项直接 `python3 tests/abi/test_secure_loader.py`；②门：给 `ci/validate_registry.py::R4` 增「discover 目录采集用例数 > 0」判定（见 M8-G-001 三门建议 ①）；③文档侧把「36/36」标注为「脚本直跑、非 CI 采集」。
- 置信度: 高（命令面 + 目录普查 + 采集语义三方一致；本轮逐文件重读）
- 来源: L23-001（定稿；文档口径由「四份文档」收窄为「1 处 36/36 字面 + 3 处 PASS 口径」，见 _merge §4）
- related: M6a-I-003（同事实的文档卫生面，M6a 主落）、M5b-G-06（mod001 不被 discover 收集）、L12-014；元结论 → M8-G-001
