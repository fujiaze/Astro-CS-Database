### V19-N-01（P0·**新机制：不是漏登记，是登记不了**）CI-REG-002 闭包面只认 `add_test(NAME …)` ⇒ **40 处 `gtest_discover_tests` 目标在两个登记面双零覆盖**，而 **C4 使唯一涉及它们的门"无法"补声明**
- 规模：`git grep -c gtest_discover_tests -- "*.cmake" "*CMakeLists.txt"` → `lib/acr/tests/unit` 27 + `lib/phase2` 7 + fault 4 + classic 1 + integration 1 = **40 处**；而 `tools/quality/check_ctest_registration.py` 内 `gtest_discover_tests` **0 命中**（`:64-66` 只有 `ADD_TEST_NAME_RE`/`ADD_TEST_ANY_RE`）。
- 基线侧：`ci/ctest_baseline.json` 的 `sources` 14 个 CMakeLists 中 **`has phase2 source? False`**，`targets` 160 条里**含 `phase2` 者 0 条**。登记侧：`gates with --target` = 41，而 **`--target` 不在自身 `ctest_targets` 里的只有 `[("CTEST-PHASE2-GATES","phase2_.*")]`** —— **41 道里唯一无声明者**。
- **互斥机制（这是本条的全部价值）**：`lib/phase2/CMakeLists.txt:100-118` 用 `TEST_PREFIX "phase2_<gate>."` **显式造可 grep 名域**，注释自证目的是「让 `CTEST-PHASE2-*` 注册项可用单一正则覆盖」；**但 C4「`ctest_targets` 模式匹配不到任何现存目标 → FAIL（陈旧注册）」里的"现存目标"集合不含 `phase2_*`** ⇒ **按 CI-REG-002 要求去补登记，会被闭包门自己判红** ⇒ **枚举面与执行面互斥**。
- 最小改法（一步修两侧、**不新增字段**）：`parse_targets` 增 `GTEST_DISCOVER_RE`，用 `TEST_PREFIX` 合成目标名并入 C2 的"现存目标"集合；随后给 `CTEST-PHASE2-GATES` 登记那 7 个 `phase2_*` 前缀；负例补 **S8**（`gtest_discover_tests` 目标未登记 → C3 FAIL）。
- **related** **`V9-N-09`**（缺 `--fail-if-no-tests` 是另一机制，本条不同）、`M8 §标杆11`（C6 正例）、`_cache/E2.md:232`（该处按"线索不立条"记，**本条升级为可施工并给出 C4 互斥新机制**）、`M9-F-1`、`G-D`
