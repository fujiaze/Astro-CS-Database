### M4-F-02 SCI 声称的 NumPy 独立 Huber-IRLS Oracle 真源不存在，「Python 参考 rtol 1e-9」为空头支票
- 类别: F_TEST_GAP
- 定稿优先级: P0（来源 L08-004 原议 P0，成立；按前台 D 项要求做全仓系统性 grep 后维持「缺位」判定）
- 位置: docs/science/PHASE2_UPM.md:108,147；检索面=tests/**（grep huber|IRLS|k_corr|numpy）、lib/phase2_upm/**、tools/**、scripts/**、lib/*.py（全仓）
- 证据摘录:
  > - **Python 参考**：NumPy 对同 `raw` 的 Huber IRLS + `control_ivar` 权重复算 `θ`（`rtol 1e-9`）。
  > - §11 Oracle 全过（含 `control_median_mc_test` MC 一致性、gauge 唯一性、harmonic continuation 边界）；
- 权威依据: 宪章 §13.1（每科学模块必备「不调用生产实现的标准实现/独立参考实现/解析推导」三选二）、§12.3-4；SCI-UPM-001 §11/§15；协议 §2 P0「已验证声明无测试/Oracle/证据支撑」
- 问题说明: 定稿前系统性检索证据（本轮独立重做）：tests/** 中 grep「huber|IRLS」仅命中 4 处（test_p2002:72/:78、test_p2003:178 的 huber_delta 配置字面量；p2001_real_nodes_test.cpp:169 注释），无一处 NumPy IRLS 复算；grep「kcorr|k_corr」于 tests/** 与 lib/phase2/tests 零命中；lib/phase2_upm/ 与 lib/phase2_samp/ 仅有 README/module.yaml/memory.md（自述 TEST 证据 MISSING，诚实）；tools/ 下 numpy 使用者（gen_p14_fixture.py、check_image_preview_semantics.py）与 UPM 无关；全仓无 p2_upm 的 Python 绑定。tests/api 两个名义「oracle」文件（test_upm_parallel.py、test_upm_recovery_oracle.py）内嵌的是**调用生产 p2_upm_build/p2_upm_calibrate_block 的 C++ 驱动**——即使运行也不满足 §13.1「不调用生产实现」，且实测永久静默跳过（另立 M4-F-04）。SCI 把 NumPy 参考列为 §11 验证支柱并以 §15「Oracle 全过」为验收条件——该支柱在真源无工件。
- 影响: UPM 核心解算器（M4-C-01/C-02 两条 P0 恰在其内）缺独立对照：产品实现自证 + 不挂构建的门（M4-F-01）= 「科学定义=算法=接口=代码=测试」验证链断裂。
- 建议处置: 落地 NumPy IRLS+control_ivar 独立参考至 tests/ 并接 CI（rtol 1e-9 按 SCI），或走 §1.2 把 SCI §11 该条显式改标 MISSING 并登记整改任务。
- 置信度: 高
- 来源: L08-004
- related: M4-F-01、M4-F-04；**L07-006（M3 域）**——前台按「SCI 声称的独立 Oracle 系统性缺位」归纳时以本条与 L07-006 为两实例

