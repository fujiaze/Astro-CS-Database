### M2b-C-01【部分修复】HiPS 生产写出仍为直写（无 staging/rename/fsync/COMPLETE），AIO 树内其它子系统已用原子原语而 HiPS writer 未采用；module_adapters 两处调用点注释仍称「AIO-002 原子发布原语内建」
- 类别: C_DOC_CODE_GAP
- 建议优先级: P0
- 位置: lib/astro_image_io/src/hips/aio_hips_writer.cpp::write_fits_image、::write_moc_fits、::make_dirs、::finalize_image_product、::aio_hips_finalize 的 manifest 段、::aio_hips_abort；lib/core/src/module_adapters.cpp::p1_op_writer（write_hips 节点）与 ::p2_op_write（write_mosaic 节点）头注释、::文件头节点地图注释；docs/contracts/DATA_SEMANTICS.md::DATA-P1-HIPS §12.5；docs/standards/STANDARDS_REGISTRY.md::D.hips 偏差表 DISP-HIPS-004；docs/algorithms/HIPS_WRITER.md::§10 DISP 表 DISP-HIPS-004 行
- 证据摘录（口径声明：本节所有「命中/0 命中」均为 ripgrep **整行**计数并逐条给出作用域，不使用 read 的显示文本作判据，以规避单行 >2000 字符截断）:
  - 生产写出即直写（原文，write_fits_image 起首）:
    > std::remove(path_n.c_str());
    > if (fits_create_file(&fptr, path_n.c_str(), &status)) {
  - write_moc_fits 同型（原文）:「std::remove(path_n.c_str());」「if (fits_create_file(&fptr, path_n.c_str(), &status)) { … "moc create " … }」
  - **限定作用域的 0 命中取证**：`lib/astro_image_io/src/hips/`（HiPS 写出路径，2 个文件）内 ripgrep「rename|fsync|staging|COMPLETE|aio_publish|\.tmp|atomic」= **0 命中**
  - **对照（本会话复算推翻了我此前的错误计数）**：同库其它子系统**已在用**原子写出原语——`lib/astro_image_io/src/hips/../aio_pipeline.cpp` 有「/* 原子保存: 先写临时文件, flush 后 rename (中断不留下可误认的正式文件) */」与「if (std::rename(tmp_path.c_str(), path) != 0) {」（rename 命中 :876/:912/:917/:918）；`hiss_stream_writer.cpp` 有「static int atomic_replace(const std::string& temp_path, const std::string& final_path)」与「// 1. 删除 .partial 文件 (避免残留半成品)」（atomic/partial 命中 17 处）。⇒ 事实不是「AIO 全树没有原子原语」，而是 **HiPS 写出路径没有采用**（与 DISP-HIPS-004 的 ALG 对照句一致）
  - 仍在的失实注释（原文，module_adapters 三处，逐字）:
    > //                write_variance_tile/finalize (AIO-002 原子发布内建;   ← 文件头节点地图 :24
    > //      aio_hips_product_begin/write_signal_support_tile/finalize（AIO-002
    > //      原子发布原语内建于 aio_hips 落盘路径）→ 标准化 HiPS             ← p1_op_writer :2419-2420
    > //      aio_hips_finalize — AIO-002 原子发布原语内建于 aio_hips 落盘路径; ← p2_op_write :3913
  - 合同侧禁止该口径（原文，DATA_SEMANTICS §12.5）:「当前 writer 不具备原子发布（无 staging/rename/fsync/COMPLETE；DISP-HIPS-004）… **不得以 writer 直写目录冒认 IO-003 原子语义**」
  - 注册表/ALG 登记（原文）:「| DISP-HIPS-004 | 高 | docs/algorithms/HIPS_WRITER.md；docs/interfaces/io/IO_003_ATOMIC_OUTPUT_PUBLISH.md | P1-HIPS-INT（C++ 写出无原子发布；原子语义由 IO-003 发布层承接） |」；ALG §10 同 ID 行（整行 612 字符，ripgrep 取回）:「C++ 写出无原子发布：FITS/MOC/metadata 先 remove 后 create 直写…原子语义由 IO-003 Python 发布层承接…**writer 层不冒认已原子**。对照：HISS 容器有 .partial/.tmppool+atomic_replace…但 writer 未采用」→ 整改选项「INT 层接线（writer 写 staging 由 IO-003 消费）或 writer 内嵌事务；tree hash 归属裁决」
  - 失败清理仍缺（原文，aio_hips_abort）:「delete ps; return 0;」——只释放句柄，不回收任何已落盘文件
- **本会话内已被并发修复、故从原判据中剔除的部分（记入 _merge 已修复表）**:
  1. `lib/astro_image_io/include/aio_hips.h` 曾有的「Atomic publish (DISP-HIPS-004 fix): … staging … promoted … COMPLETE」承诺块 → 现 ripgrep（作用域 lib/astro_image_io/include/，pattern「原子|publish|staging|promoted|COMPLETE|atomic|rename」）**仅 1 命中且是 nside 那行**，该虚假承诺已消失；
  2. `lib/healpix_db/healpix_drizzle/astro_sphere_sink.cpp` 写通道注释中的「AIO-002 原子发布原语内建」措辞 → 现该文件 grep「原子|AIO-002|IO-003|发布」= **0 命中**，已订正；
  3. `lib/hips_p2` 侧曾有的同名失实注释 → **该模块已无源码**（glob lib/hips_p2/** 仅 module.yaml/README.md/memory.md；lib/hips_p2/src/hips_p2_io.cpp 在非 run 区 0 命中），载体消失。
- 权威依据: 宪章 §11（临时工作区 + 原子提交，不得留半产品）、§14.4（不可恢复失败不得生成看似成功的产品）、§12.2（注释与实现一致）、§5.2/§6.2（HiPS 原子提交属 AIO 固有面）；DATA_SEMANTICS §12.5（同一事实的权威口径 + 明令禁冒认）；STANDARDS_REGISTRY §1.2（禁按实现反推标准）
- 问题说明: 收敛为一句：**项目已有原子发布能力，且已在同库的 HISS/缓存路径上用起来，唯独 HiPS 写出路径未接入**；因此 DISP-HIPS-004 长期以「能力缺口（非合同违规）」口径存在是失准的——缺口已由 aio_publish.cpp + hiss atomic_replace 补上，剩下的只是 HiPS 写出未采用。而 module_adapters 的三处注释仍称内建，等于把 IO-003 的原子语义记在 writer 账上，正是 §12.5 明令禁止的写法；同时失败清理仍只有 delete 句柄（无回滚/无暂存目录丢弃），宪章 §11 的半产品条件仍然成立（properties/Moc/metadata 先 remove 后 create 直写，多步 finalize 中途失败即混合新旧 tile）。
- 影响: 中断/失败可留下对外看似完整的半成品（manifest 未写但 properties/Moc/部分 tile 已落盘）；审计与检索按注释会把原子性责任派给 writer；同一 out_dir 重跑与旧运行残片混合的风险未闭。
- 建议处置: ① 三处注释改口为事实口径——「当前直写；原子发布语义在 IO-003 发布层与 lib/hips staging 路径，writer 未接入（DISP-HIPS-004）」；② 按 DISP-HIPS-004 已列的两个选项择一落地并同步 ALG/注册表：把 HiPS 写出接入既有 `aio_publish_stage_create_v1`/`aio_publish_promote_v1`（复用现成能力），或在 AIO 内为 HiPS 走与 HISS 同型的 .partial + atomic_replace；③ 接入前不得把该缺陷判为「能力缺口」——ALG 与注册表的表述应更新为「能力已在库内、HiPS 写出未接线」；④ abort/finalize 失败路径补暂存丢弃（接入 staging 后自然成立）；⑤ 若判定生产写必由发布层持有，则据 §12.5 把 writer 降级为只写暂存目录，并回归验证（与 M2b-G-01 的归属裁决同批处理）。
- 置信度: 高（全部证据为 ripgrep 整行计数 + 原文摘录；此前的错误计数已在本条内公开更正）
- related: M2b-A-01（同一 begin 的校验缺口）、M2b-G-01（写出面归属待裁决）、M2b-F-02（同函数族的测试无判别力）、DISP-HIPS-004、IO_003（合同侧归 M2a，本条只登记写出实现与注释事实）、L03-004
- 来源: L03-004
