### M8-F-002 `tests/unit/io_ownership_test.cpp` 在 ctest 下**不可能失败**：唯一 CHECK 的失败计数不参与退出码，`main` 无条件 `return 0`（覆盖 L23-002 与 L23 §1 的旧措辞）

- 类别码: F_TEST_GAP
- 优先级: **P0**（不可失败的门 + 被基线列为必过项 + 被标准登记与追溯表当作验证证据）
- 位置: `tests/unit/io_ownership_test.cpp::main`（全文件仅 32 行；`::failures` :9、`::CHECK` 宏定义 :10-16（失败仅 `++failures`）、唯一 CHECK 站点 :25 `CHECK(im == nullptr);`、PASS 打印 :30、`return 0;` :31）；注册 `tests/unit/CMakeLists.txt::io_ownership_test`（:136-:141，整组 `--output-on-failure`）；基线 `ci/ctest_baseline.json`（:77）；被当作验证证据：`docs/standards/STANDARDS_REGISTRY.md`（:118 IO-002 CONFORMANT）；`tools/check_aio_ownership.py::main`（:459 附近 `return 0` 恒零，`return 1` 仅在 :455）
- 证据摘录:
  > （:9）static int failures = 0;
  > （:14）++failures;
  > （:25）CHECK(im == nullptr);
  > （:31）return 0;
- 权威依据: 宪章 §14.4 末段「禁止只打印 PASS 不做机器比较」；§13.3（每条声明须有可失败测试）；§12.3-4（验证链不得以假绿充数）
- 问题说明: 三点叠加使该 TU **无论发生什么都返回 0**：①`failures` 累加后从未被读回（无 `return failures`、无 `exit(failures)`），:25 的 CHECK 即使失败也只向 stderr 打一行；②:20/:26 的空指针安全靠"不崩溃"隐式表达（崩溃虽会红，但泄漏/错序完全测不到）；③:30 仍打印「IO-002 TESTS PASS」字样。本项是 L23-002「唯一 CHECK」表述的**加强版**：真正的缺陷不是"断言少"，而是"断言结果不进退出码"，因此**门的能力为零**。
- 措辞覆盖（前台 09-xx 令，落实）: ①**覆盖 L23 §1 的「≥3 项 CHECK」**——实测全文 CHECK 站点仅 1 处（:25）；②**取代 L23-002 的「唯一 CHECK」表述**——以「唯一 CHECK 且其结果不参与退出码 ⇒ 不可失败」为定稿口径。
- 影响: `io_ownership` 在 `ctest_baseline.json`（:77）里属"应当通过"的基线成员，实际是把"任何情况都绿"当通过；IO-002 的 CONFORMANT 判定与 AIO-OWNERSHIP 静态门都从中取证（见 M8-F-004）。
- 建议处置: `return failures == 0 ? 0 : 1;`；按 :2 注释所指意图补齐断言面（deleter 全覆盖需真分配：经 `aio_write_image_data` 产出再释放，或经 `AIO_READ_REQ.flags` 构造带缓冲的读取，配 `aio_error(aio_read(...))` 的 AIO_ERR_* 判别）；`tools/check_aio_ownership.py` 的 main 在零违规时也应显式打印计数并非零化退出码逻辑（当前 :459 `return 0` 恒零）。
- 置信度: 高（全文件逐行读完，退出码路径唯一）
- 锚点澄清: 前台 09-xx 转来的「:64 `aio_write_bytes(..,NULL,0)==AIO_OK`、:77/:79 无条件 printf」**不属本文件**——真源与 `run/**` 影子树副本均 32 行（本轮实测 8 份副本行数一律 32，排除版本漂移解释）、`aio_write_bytes` 在 `tests/**` 全仓 **0 命中**、`AIO_OK` 只出现在 `include/astrocs/io/aio_abi_v1.h:47/:113/:142/:154`；已按 `path::符号` 纪律回到真源重取（见 _merge §5 追加行），本条不采该锚点。
- 来源: L23-002（P0 定档不变，判据加强并覆盖两处旧措辞）
- related: M8-F-007（同型恒真）、M8-G-002、M8-F-004（同源取证）、L08-006→M4-F-03（打印 PASS 冒名，同族不同文件）
