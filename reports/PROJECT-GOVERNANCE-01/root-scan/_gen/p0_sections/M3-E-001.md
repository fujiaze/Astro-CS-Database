### M3-E-001 校准 / 测光 / 噪声三域的追溯行与合同尾注存在行级失真：VERIFIED 行的测试锚不覆盖被验门，合同尾注用非法测试 ID
- 类别: E_TRACE_BREAK
- 建议优先级: P0（本域实例判级；「系统性失效」层级的定稿归 M6b）
- 来源: L05-003（主）+ L07-015（同域实例，合并登记）
- 取证口径: 计数 + 逐列 grep（列索引按 header 拆分：0=requirement_id，6=public_api，7=implementation_files，9=test_ids，10=test_files，14=status）
- 位置与实例:
  1. `docs/TRACEABILITY.csv::第 20 行（requirement_id=SCI-CAL-001）` —— 逐列读取结果：`title=校准科学门（bias/dark/flat/cosmetic）`、`public_api` 列 4 个 ac_*（**无 `ac_correct_frame`**）、`test_ids=TEST-CAL-001`、`test_files=lib/calibration/tests/test_photometry_apply.cpp`、`status=VERIFIED`。
  2. `lib/calibration/tests/test_photometry_apply.cpp::文件头两行`（复核时点 :2-3）—— 自述被测对象为 `apply_photometry()` 与 FITS Writer 的 `photscal/photappl` 元数据。
  3. 真实验证载体与该行无映射：`lib/calibration/tests/p1cal/`（ctest `p1cal`，注册于 `tests/unit/CMakeLists.txt:457`；亦在 `ci/ctest_baseline.json:8` 冻结来源清单内）、`tests/backend/test_calibration_oracle.py`、`tests/unit/master_flat_median_test.cpp`（`tests/unit/CMakeLists.txt:444-448`）—— 三者均**未**出现在第 20 行的第 9/10 列。
  4. `docs/contracts/TEST_MATRIX.md:::32` —— SCI-CAL-001 亦锚 `test_photometry_apply.cpp`（同一失真在两份矩阵并存，双头根因归 M6b-E-001）。
  5. 非法测试 ID（三域合同尾注）：`docs/science/CALIBRATION.md::§13:123`（`TST-CAL-001`/`TST-CAL-INV-001`/`TST-CAL-FAIL-001`）、`docs/science/PHOTOMETRY.md::§13:122`（`TST-PHOT-*`）、`docs/science/NOISE_MODEL.md::§13:130`（`TST-NOISE-*`），以及 §11:108/:109/:116 的 `TST-CAL-INV-*`/`TST-CAL-FAIL-*`/`TST-NOISE-INV-*`；文法源 `docs/traceability/TRACEABILITY_SPEC.md:::78`（TEST 行必须匹配 `^TEST-[A-Z0-9]+(-[A-Z0-9]+)*$`）。
  6. 本域被引对象不存在的**实例清单**（判词归 M6b-E-004，此处仅登记本域出现处与 glob 计数）：`02_FROZEN_STAGE1_HISS_SPEC`（被 `docs/algorithms/CALIBRATION_ALGORITHMS.md:159/:186/:392`、`docs/algorithms/DRIZZLE_GEOMETRY.md:173` 引用；glob `**/02_FROZEN*` = 0 命中）、`lib/photometric_calib/flux_calibrator/**`（被 `docs/modules/photometric_calib.md:27`、`docs/science/PHOTOMETRY.md:3`、`docs/TRACEABILITY.csv:22` 的 title 列引用；glob = 0 命中）、`docs/science/SNR_SCIENCE_DERIVATION.md` 与 `SNR_REDESIGN_CONTRACT.md`（被 `lib/snr_estimator/cpp/include/snr_estimator.h:20/:227`、`src/noise_model.cpp:17`、`docs/contracts/DATA_SEMANTICS.md:401` 引用；glob = 0 命中）。
- 证据摘录（逐字，本轮重读）:
  > requirement_id,requirement_type,title,authority_doc,algorithm_id,module,public_api,implementation_files,implementation_symbols,test_ids,test_files,diagnostic_ids,error_codes,release_gate,status,notes（TRACEABILITY.csv 表头，16 列）
  > SCI-CAL-001,science,校准科学门（bias/dark/flat/cosmetic）,…,TEST-CAL-001,lib/calibration/tests/test_photometry_apply.cpp,-,-,PRE_RELEASE_ENGINEERING_FOUNDATION,VERIFIED,V19R2 有效（第 20 行）
  > // test_photometry_apply.cpp — P1-004 apply_photometry() + FITS Writer photscal/photappl 元数据单元测试（该测试文件 :2）
  > - 测试: `TST-CAL-001` 常量场、`TST-CAL-INV-001` 幂等归一、`TST-CAL-FAIL-001` 参数校验（新增/映射见 `docs/TRACEABILITY.csv`）（SCI-CAL §13:123）
  > SCI 行 test_ids 匹配 `^SCI-[A-Z0-9]+(-[A-Z0-9]+)*`；…；TEST 行匹配 `^TEST-[A-Z0-9]+(-[A-Z0-9]+)*$`（TRACEABILITY_SPEC.md:78）
- 权威依据: 宪章 §17.2（追踪无断链属发布门禁）、§12.3-1（事实性陈述与证据指针必须可定位）、§12.3-4（核心合同至少一个独立测试）、§11
- 问题说明: 三域范围内可分别复核的行级事实：① 校准科学门的 VERIFIED 行，其第 10 列唯一测试指针指向一个自述只测 `apply_photometry` 与 FITS 元数据的文件，与被验的 bias/dark/flat/cosmetic 公式无覆盖关系；② 同一行的第 6 列公开 API 集缺 `ac_correct_frame`（cosmetic 域在该行整体无锚）；③ 真正覆盖这些门的三个测试载体（p1cal / 校准 Python oracle / master_flat_median）无一登记入该行或 TEST_MATRIX 对应行；④ 三域合同尾注的 `TST-*` 前缀不满足 SPEC:78 文法，且在 `TEST_MATRIX.md`/`TRACEABILITY.csv`/schema 中零定义（计数口径：`grep -c` 于 docs 树，`TST-CAL`/`TST-PHOT`/`TST-NOISE` 命中处仅为引用处自身）；⑤ 三域另有 3 类被引对象 glob 零命中（本域实例，列于位置 6）。
- 影响: 本域三份 FROZEN 科学合同的 VERIFIED 状态，在其自身登记的测试锚上不可复核；按 §12.3-4 做「合同→测试」遍历的机器门与人工评审都会在校准行拿到无关证据、在测光/噪声行拿到未定义 ID。发布门禁证据层被削弱（「无据 VERIFIED」的层级判定与全仓规模由 M6b-G-001 定稿，本条不重复计件）。
- 建议处置: 本域范围内做三件事：(a) 重建第 20 行（第 9/10 列改指 `TEST-CAL-*` 的真实载体：p1cal + `test_calibration_oracle.py` + `master_flat_median_test.cpp`；第 6 列补 `ac_correct_frame`，或把 cosmetic 另起一行）；(b) 三域尾注的 `TST-*` 统一改 `TEST-*` 并与 TEST_MATRIX 的既有 ID 对齐（校准域现成 ID 是 `TEST-CAL-001` 与 `TEST-CAL-DESIGN-001`）；(c) 三域被引对象改指现行权威或归档。跨域规模统计与根因判词由 M6b 三条定稿，整改应与之并包。
- 置信度: 高（列值由逐列 grep 复算；glob 计数已注明口径）
- related: **M6b-E-001**（追溯双头根因）、**M6b-E-004**（被引文档失效根因）、**M6b-G-001**（无据 VERIFIED 根因）、M3-F-001、M3-F-002、M3-F-004、M3-F-005、M3-C-006、M3-C-007、M3-I-001、M3-A-001、F00-01
