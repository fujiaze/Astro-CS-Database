ID|原类别|原优先级|旧判据(文档+节号/路径)|最新权威条款|当前证据(命令+输出)|结论|归属|GAP关系|备注
V10-N-01|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/healpix_db/healpix_drizzle/fits_reader.cpp，本会话实测已迁 lib/algorithms/drizzle/healpix_drizzle/）|ASTROCS_DESIGN §9（阶段间只经磁盘产品+manifest+哈希交换；aio 是唯一 FITS/HiPS/manifest 读写边界）＋ ENGINEERING_SPEC §9（失败不得留下可被误认成正式产品的半成品）／§5.1（边界、错误输入必备测试）|命令：sed -n "425,428p" lib/algorithms/drizzle/healpix_drizzle/fits_reader.cpp；输出：    if (got < data_size) { ／ fprintf(stderr, "[fits_reader] 警告: 数据读取不完整 (%zu/%zu 字节)\n", got, data_size); ／ n_pixels = got / (size_t)bytes_per_pixel;  // 按实际读取量处理 ／ }。命令：sed -n "514,515p" 同文件 ＋ sed -n "530p" 同文件；输出：    img.width    = width; ／     img.height   = height; ／     return true;。命令：sed -n "1896p" lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp；输出：            Scalar pixelValue = pixels[(size_t)y * (size_t)img.width + (size_t)x];|OPEN|P1-001|无|短读只缩 pixels 向量、宽高仍取头部 NAXIS ⇒ 输入面按 y*width+x 索引可越界且函数报成功；输出面另有 pixels.size() 校验（drizzle_engine.cpp:846/964/1622/2360），不覆盖输入面
V10-N-02|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/healpix_db/healpix_drizzle/{fits_reader,wcs_sip}）|ASTROCS_DESIGN 附录 B（FITS WCS Paper I/II；SIP 阶数有确定值域，标准偏离仍是偏离）＋ ENGINEERING_SPEC §5.1（边界、极端参数、错误输入必备测试）|命令：sed -n "225p" lib/algorithms/drizzle/healpix_drizzle/fits_reader.cpp ＋ grep -c order 同文件；输出：225:    int a_order = 0, b_order = 0, ap_order = 0, bp_order = 0; ／ 14（14 处 order 提及中无一条范围判定）。命令：sed -n "85,88p" lib/algorithms/drizzle/healpix_drizzle/wcs_sip.cpp；输出：    for (int i = 0; i <= order; i++) { ／        T pdy = T(1); ／        for (int j = 0; j <= order - i; j++) { ／            result += T(coeffs[i * 6 + j]) * pdx * pdy;。命令：grep -n "double a\[36\]" lib/algorithms/drizzle/healpix_drizzle/fits_reader.h；输出：12:    double a[36] = {0};   // 前向 A|OPEN|P1-001|无|读侧不夹阶数即以外部件数索引 36 槽固定系数池；正对照＝同族 KV 边已夹（hp_drizzle_api.cpp:461-465 判定 [0,5] 并 return -10），同一字段两种口径
V10-N-03|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/gaia_xpsd_client/src/gaia_client.c，已迁 lib/infrastructure/gaia_xpsd_client/src/）|ASTROCS_DESIGN §3.5（存在 error 时强制阻断运行）＋ ENGINEERING_SPEC §9（错误通过统一状态码+结构化诊断传播）＋ docs/plugins/infrastructure/22_gaia_xpsd_client.md（外部星表读面）|命令：sed -n "1269p" lib/infrastructure/gaia_xpsd_client/src/gaia_client.c；输出：        xf->item_size = item_sz[0] ? atoi(item_sz) : 0;。命令：sed -n "1284p" 同文件；输出：                    if (!parse_bounded_int(p + 14, WL_COUNT, &xf->spectrum_count)) {|OPEN|P1-001|无|同一函数相邻两处：一边用带界解析器，一边对 itemSize 用裸 atoi；item_size 是解交织步长，负值或垃圾值无错误分支即进入后续解码
V10-N-04|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/astro_image_io/src/aio_fits.cpp，已迁 lib/infrastructure/aio/src/）|ASTROCS_DESIGN 附录 B（FITS Standard 实数字段允许 D 指数记法）＋ §9（aio 是唯一 FITS 读写边界，禁止各自复制 reader/writer）＋ ENGINEERING_SPEC §3（禁止一个字段承载多个含义）|命令：sed -n "161,162p" lib/infrastructure/aio/src/aio_fits.cpp；输出：            else if (key == "BSCALE") { try { hdr.bscale = std::stod(kw.value); } catch (...) {} } ／            else if (key == "BZERO") { try { hdr.bzero = std::stod(kw.value); } catch (...) {} }。命令：grep -c isfinite lib/infrastructure/aio/src/aio_fits.cpp；输出：1（无 D→E 归一或有限性门）。命令：sed -n "64,66p" lib/algorithms/drizzle/healpix_drizzle/fits_reader.cpp；输出：static double parseDouble(const std::string& s) { ／     std::string t = trimStr(s); ／     for (auto& c : t) {|OPEN|AIO-001|与 GAP-013 重复|BSCALE/BZERO 解析失败被 catch(...) 吞成缺省 1.0/0.0（交付像素量级静默错）；另一读面 parseDouble 显式逐字符归一 D→E ⇒ 同一合法头两读面结果不同
V10-N-05|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/astro_image_io/src/hips/aio_hips_reader.cpp，已迁 lib/infrastructure/aio/src/hips/）|ASTROCS_DESIGN §9（交付产品完整性；哈希/校验通过才算产品）＋ docs/plugins/infrastructure/17_aio.md §7（错误与边界：不得静默）＋ ENGINEERING_SPEC §5.1（错误输入必备测试）|命令：grep -rn bad_snr_rows lib cli include；输出：lib/infrastructure/aio/src/hips/aio_hips_reader.cpp:98:    size_t bad_snr_rows = 0;          // 损坏 TSV 行计数 (G6 robustness) ／ lib/infrastructure/aio/src/hips/aio_hips_reader.cpp:339:                        ++d->bad_snr_rows;（全仓仅声明＋自增，零消费者）。命令：sed -n "332,333p" 同文件；输出：                    if (std::sscanf(line.c_str(), "%lld %lf %lf %lf %u %u", ／                                    &sid, &ra, &dec, &snr, &qf, &ps) == 6) {。命令：grep -c isfinite 同文件；输出：0|OPEN|AIO-001|无|坏行被计数但无人回读、函数仍返回成功 ⇒ 交付 SNR 星表少行不可观测；%lf 可解析 nan/inf 且无有限性门
V10-N-06|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/healpix_db/healpix_drizzle/fits_reader.cpp 的 :315-321／:333-342／:419-422）|ASTROCS_DESIGN §8（无界内存增长禁项）＋ §12（发布候选清单含「无界内存增长」）＋ ENGINEERING_SPEC §5.1（极端参数必备测试）|命令：grep -c -e 65535 -e MAX_DIM lib/algorithms/drizzle/healpix_drizzle/fits_reader.cpp；输出：0。命令：sed -n "315p" 同文件；输出：    if (naxis1 <= 0 ∥∥ naxis2 <= 0) {（唯一尺寸判定，仅正数）（引文内的 ∥ 代表原文的两句号逻辑或，避免与记录分隔符冲突）。命令：sed -n "419,420p" 同文件；输出：    size_t n_pixels = (size_t)width * (size_t)height * (size_t)naxis3; ／     size_t data_size = n_pixels * (size_t)bytes_per_pixel;（其后即 std::vector<uint8_t> raw(data_size)）|OPEN|P1-001|无|仅判正数、无上限即把头部尺寸直乘成分配量；正对照＝同族另两面已夹（aio_fits.cpp:554 注释「cfitsio 路径对齐手写路径 parse_fits_header 的 65535 上限口径」；aio_xisf.cpp:33 亦有上限）
V20-N-01|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/drizzle/src/module_entry.cpp，已迁 lib/algorithms/drizzle/src/）|ASTROCS_DESIGN §3.3（输入合同字段有确定语义）＋ §9（manifest 记录生效科学配置；禁止把计划值伪装成实际值）＋ ENGINEERING_SPEC §8（字段与消费者不得悬空）|命令：grep -c -e rev_scale -e rev_ra0 -e rev_dec0 -e rev_proj lib/algorithms/drizzle/src/module_entry.cpp；输出：7。命令：grep -n rev_scale 同文件；输出：313:    double   rev_scale; ／ 456:        c->rev_scale = sc;（声明＋唯一写入，无第三处读取；另三键与 have_scale/have_ra0/have_dec0 同形）。命令：sed -n "1049p" 同文件；输出：    double* crval = json_get_f64_array(manifest, "crval", &n_arr);（op=reverse 的反向几何实取输入 manifest 的 crval/crpix/cd）|OPEN|P1-001|无|四配置键解析后在整仓零读取 ⇒ 用户改这些配置不改产物，属「配置字段无消费者」判据族
V21-N-01|C_ALG_IMPL|P1|无（原判据即现行权威；站点 providers/cpu/* 与 include 未迁移）|ENGINEERING_SPEC §9（统一状态码，跨平台同失败同码）＋ ASTROCS_DESIGN §6.3 退出码表（5＝backend ABI/签名/CPU 特征或加载失败，须可区分）＋ docs/api/COMMON_ABI_V1.md ＋ §7.3|命令：grep -n "rc != ACS_CAP_OK) return ACS_ERR_UNSUPPORTED" providers/cpu/baseline/src/baseline_provider.cpp providers/cpu/avx2/src/avx2_provider.cpp providers/cpu/avx512/src/avx512_provider.cpp；输出：baseline:63 ／ avx2:76 ／ avx512:103。命令：sed -n "110,113p" providers/cpu/common/include/astrocs/cpu/capability_v1.h；输出：    ACS_CAP_OK = 0, ／     ACS_CAP_ERR_PARAM = 1, ／     ACS_CAP_ERR_ABI_MISMATCH = 2, ／     ACS_CAP_ERR_UNSUPPORTED = 5。命令：grep -n "grc != ACS_OK) return ACS_ERR_UNSUPPORTED" providers/cpu/*/src/*.cpp；输出：avx2:398 ／ avx512:398 ／ baseline:614|OPEN|CPU-001|无|能力 ABI 的 4 值域在 provider 边被并成「OK／非 OK→ACS_ERR_UNSUPPORTED」，参数错与 ABI 失配在宿主侧不可区分；正对照＝同文件 host_abi 边仍保留 ACS_ERR_ABI_MISMATCH（avx2_provider.cpp:122／:287）
V21-N-02|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/astro_image_io/src/healpix/aio_healpix_io.cpp，已迁 lib/infrastructure/aio/src/healpix/）|ENGINEERING_SPEC §9（统一状态码＋结构化诊断传播；不跨 C ABI 抛异常）＋ ASTROCS_DESIGN §6.3（7＝I/O 失败，须与其它因可区分）＋ docs/plugins/infrastructure/17_aio.md §7|命令：grep -c "if (ret != 0) return HIO_ERR_FILE" lib/infrastructure/aio/src/healpix/aio_healpix_io.cpp；输出：5。命令：grep -n "HIO_ERR_" lib/infrastructure/aio/src/healpix/aio_healpix_io.cpp；输出：55-64 HIO_ERR_* 定义域为 -1..-8（含 PARAM/MAGIC/ZSTD/JSON/MEM/BOUNDS 等语义位）。命令：grep -n "HISS_ERR_" lib/infrastructure/aio/include/astrocs/aio/hiss_format.h；输出：245-249 HISS_ERR_CHECKSUM=-4 ／ HISS_ERR_UNSUPPORTED=-6 ／ HISS_ERR_UNKNOWN_REQUIRED=-7 ／ HISS_ERR_FORMAT_VIOLATION=-8|OPEN|AIO-001|无|HISS 私有错误域（校验和损坏、格式违规、缺必填键）在跨边界处全塌成单一 HIO_ERR_FILE，调用方无法区分「文件坏」与「数据坏」
V21-N-05|C_ALG_IMPL|P1|无（原判据即现行权威；站点 tools/check_warning_suppression.py 未迁移）|docs/ci/01_CHECKS.md §1（每项检查必须能绿能红）与 §2 表 CHK-WARN（P0）＋ docs/ci/03_GATES.md §1（P0 红灯无豁免）／§4 ＋ CONTROL_PACK_SPEC.md §7.3（不得用 waiver 掩盖红灯）|命令：grep -c returncode tools/check_warning_suppression.py；输出：0。命令：sed -n "29,33p" 同文件；输出：BUILD_COUNT_SCRIPT = ( ／     cd "$1" ∥∥ exit 9 拼接串 ／     cmake --build "$2" --target astrocs 2>&1 ／     grep -v WSL 后接 grep -c "warning:" ∥∥ true ／ )。命令：sed -n "104,105p" 同文件；输出：    warn = measure_build(build_dir) ／     if warn not in ("", "0"):（判定只看子进程 stdout 文本）|OPEN|CI-001|无|残余缺陷：仍不读 cmake 退出码，且子命令退出码被 ∥∥ true 拉平 ⇒ 构建失败但无 warning 行时 warn 为空/0 → 判绿。订正：原判据「无构建树即 QA-001_SKIP + rc=0」一腿已按 GAP-027 改 fail-closed（现 :98-103 打印 QA-001_FAIL 并 return 1），不再作依据。门仍注册在 CHK-WARN 下（ci/checks.json:239）
V21-N-06|C_ALG_IMPL|P1|无（原判据即现行权威；站点 tools/quality/contracts/check_full_integration.py 未迁移）|docs/ci/01_CHECKS.md §1（豁免显式登记 ci/exemptions.json，只减不增，需负责人批准）与 §4（能绿能红）＋ docs/ci/03_GATES.md §1／§4 ＋ CONTROL_PACK_SPEC.md §7.3|命令：grep -c returncode tools/quality/contracts/check_full_integration.py；输出：0。命令：grep -n "except:" 同文件；输出：34:        except: pass。命令：git grep -ln waivers.json -- tools ci scripts runtime tests packaging；输出：tools/quality/contracts/check_full_integration.py ／ tools/quality/contracts/fixtures/check_full_integration/invalid_waivers.json（全仓无生产者）。命令：python3 统计 ci/exemptions.json 的 exemptions 条目数；输出：0（该文件为现行豁免面，由 validate_registry.py:437 R14 强制存在）|OPEN|CI-001|无|防掩盖门自身既不看子检查退出码（异常还 except: pass），又盯一份全仓无生产者的 waivers.json；现行权威豁免面是 ci/exemptions.json（当前 0 条），两面脱钩 ⇒ 该门对真实豁免面失明
V21-N-07|C_ALG_IMPL|P1|旧判据含旧世代发布义务（旧宪章 §16.3 版本纪律；旧控制包路径 工程控制/AstroCS_17_最终发布门禁 已删）|发布包义务的最新替代＝ASTROCS_DESIGN §12（发布候选至少满足发布包白名单/哈希/版本/provenance 通过）＋ docs/ci/01_CHECKS.md §2 表 CHK-PACKAGE（P0）；旧世代控制包义务按 §0 与负责人裁决作废（登记于 docs/ci/01_CHECKS.md §2.2 RETIRE-001）|命令：grep -c returncode scripts/package_audit.py；输出：0。命令：grep -c returncode tools/quality/build_v19r2_package.py tools/quality/build_v19r3_package.py tools/quality/build_v19r4_package.py；输出：三份均为 0。命令：grep -c returncode tools/assemble_audit.py；输出：1（该腿已按 RETIRE-001 改：main 打印 ASSEMBLE_AUDIT_RETIRED 并 exit 2，原实现保留为 legacy_main）。命令：python3 统计 ci/checks.json 中 package_audit／build_v19r2／build_v19r3／build_v19r4／assemble_audit 出现次数；输出：0 0 0 0 0|OPEN|PKG-001|无|rc 不判定在三份脚本上仍可复跑（git() 失败→空串→仍打 PACKAGE_OK，打包链无一步校验），但原三腿之一的 assemble_audit 已显式退役、且四脚本在 147 条注册 step 面内引用数为 0 ⇒ 降级为遗留工具处置：要么按 RETIRE-001 口径登记退役，要么补 rc 判定
V21-N-12|C_ALG_IMPL|P1|旧判据＝旧宪章 §18.4（只加载签名清单官方模块；ASTROCS_PROJECT_CONSTITUTION.md 不在现行权威链）|哈希/来源义务的最新替代＝ASTROCS_DESIGN §12（发布候选至少满足发布包白名单/哈希/版本/provenance 通过）＋ §9（阶段间只经磁盘产品+manifest+哈希交换）＋ docs/ci/01_CHECKS.md §2 CHK-PACKAGE／CHK-ABI（均 P0）＋ §10.1（交付形态含 manifest）|命令：python3 统计 packaging/astrocs.product.json 的 units[].sha256 取值集合；输出：units: 10 ／ sha256 set: {None}。命令：grep -c "null" packaging/astrocs.product.json.in；输出：10（十处均为 units[].sha256 占位）。命令：sed -n "734p" runtime/registry/module_registry.c ＋ sed -n "780p" 同文件；输出：734:        if (e->sha_registered[0] && e->sha_actual[0] && strcmp(e->sha_registered, e->sha_actual) != 0) { ／ 780:（同一调用实参列）e->sha_registered, "", (int)loaded, err, err_size);。命令：grep -n sha256 packaging/verify_install_tree.py cmake/install_layout.cmake；输出：verify_install_tree.py:14（唯一命中＝文档串「sha256 为 null (SKELETON)」）；install_layout.cmake 无命中（装配期无填充）|OPEN|PKG-001|无|清单哈希恒 null ⇒ 注册表比较分支永不成立、build_id 边被硬传空串短路；tests/abi/mod001_install_load_check.py:266 更把 null 钉成必须。订正：loader 侧非空分支现已存在（runtime/module_loader/secure_loader.c:499 判 expected_sha256.size > 0），原「读端无非空分支」一腿不再成立，残余为清单/装配/校验/测试四面
V21-N-13|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/core/src/module_adapters.cpp，已迁 lib/infrastructure/scheduler/src/module_adapters.cpp）|ENGINEERING_SPEC §8（合同/清单变更必须有追踪；字段与消费者不得悬空）＋ ASTROCS_DESIGN §9（manifest 记录生效配置；禁止把计划值伪装成实际值）|命令：python3 逐键统计 14 个 manifest 键在 tracked 代码面（含 docs/tests/tools/ci 扩展名 .cpp/.h/.py/.c）的字面量命中与写出数；输出：band_executed 1/1/0 ／ max_forward_cross_deg 4/4/0 ／ mosaic_root 1/1/0 ／ nside_clamped 2/2/0 ／ photometry_provenance 2/2/0 ／ photometry_provenance_artifact 1/1/0 ／ product_sha256 3/3/0 ／ props_artifact 1/1/0 ／ resampled_artifact 1/1/0 ／ snr_schema 2/2/0 ／ verified_artifact 1/1/0 ／ wcs_artifact 2/2/0 ／ wcs_plan_artifact 1/1/0 ／ writer_artifact 1/1/0（三列＝总命中／manifest 写出／读取，第三列十四键全为 0）。命令：grep -c 'man' 无关；补测：sed -n '2843,2846p' lib/infrastructure/scheduler/src/module_adapters.cpp；输出：  (*man)["n_frames"] = frames.size(); ／  (*man)["snr_schema"] = "DATA-P1-SNR/2";（键名只出现一次＝零回读）|OPEN|MOD-001|无|14 键零回读；nside_clamped（:3217/:3242，语义＝nside 被钳位、drizzle_engine.h 自述「决策不自由」）无人据此告警，snr_schema（:2844 单键 DATA-P1-SNR/2）与产物内 schema+schema_version 两键（:2838-2840）两种表意并存且无人比对。限定：max_forward_cross_deg 的数值本身在判定处有绝对门，本条只报 manifest 副本无人读；product_sha256 的第三处命中是从上游 writer 清单再打包，不构成对本节点 manifest 键的回读
V21-N-15|C_ALG_IMPL|P1|旧判据＝旧宪章 §10.5（资源记录与自动门禁）＋ 旧门禁文档 07 §1（无 stage 标注且 wall>5s 记 P1）|最新替代＝ASTROCS_DESIGN §8（每个 heavy 运行自动记录线程数/并行效率/峰值内存；超预算自动记 violation）＋ §12（持续低利用率属发布禁止项）＋ docs/plugins/infrastructure/19_runtime.md §7（门禁失败→exit 10）／§8（利用率门禁测试能红能绿）＋ docs/ci/01_CHECKS.md §2 CHK-RESOURCE（P0）|命令：grep -n -e "g.has_stage_annotation = true" -e "g.monitor_present = true" cli/commands.cpp；输出：806:    g.has_stage_annotation = true; ／ 846:        g.monitor_present = true;   // ProcessMonitor 采样线程已实际运行并落盘三产物。命令：grep -n -e has_stage_annotation -e monitor_present cli/resource_gate.h；输出：139:    bool has_stage_annotation = false; ／ 187:    bool monitor_present = false;     // 监控是否实际运行(heavy run 必须为 true, 否则 FAIL) ／ 294:    if (!g.has_stage_annotation && g.wall_seconds > 5.0)（返回 UnannotatedPriority） ／ 347（同型第二处） ／ 370:    return g.monitor_present && mon001_sampled(g.util_samples_measured) &&。命令：grep -n is_stage_priority cli/commands.cpp；输出：596:[[maybe_unused]] static bool is_stage_priority(const char* annotation, double wall_seconds)（生产零调用者）|OPEN|OBS-001|无|判据输入位被无条件写 true ⇒ UnannotatedPriority 与「监控缺失即 FAIL」两条分支在现产永不可达，stage 类别映射助手退化为 maybe_unused。行号已随 CLI-001 重定位（原判 912／946，现 806／846）
V21-N-16|C_ALG_IMPL|P1|旧判据＝旧宪章 §10.5＋§17.6（资源分类与利用率禁项）＋ 旧门禁文档 07 §3（按类别差异化阈值）|最新替代＝ASTROCS_DESIGN §8（一个进程只有一个资源调度器与线程预算源；自动记录实际并行宽度）＋ §12（持续低利用率属发布禁止项）＋ docs/plugins/infrastructure/21_observability.md §4（trace 是实际观测）＋ docs/ci/01_CHECKS.md CHK-RESOURCE（P0）|命令：grep -c "g.kind = astrocs::ResKind::Compute" cli/commands.cpp；输出：1（:795，为 CLI 面唯一 kind 写点）。命令：grep -n "ResKind::" cli/resource_gate.h；输出：128:    ResKind kind = ResKind::Unknown; ／ 262:    if (g.kind != ResKind::Compute) return 0.0; ／ 287:    if (g.kind == ResKind::Mixed) { ／ 331:    if (g.kind == ResKind::Memory) { ／ 338:    if (g.kind == ResKind::Io) { ／ 345:    if (g.kind == ResKind::Unknown) {|OPEN|OBS-001|无|四分类差异化阈值分支在现产全部不可达，gate 对一切工作按单一 Compute 评分（混合 IO/内存型工作被按纯计算阈值判）；:262 又使任何非 Compute 的利用率恒为 0（将来若真写非 Compute 即恒红）⇒ 两向都无正确语义
V21-N-17|C_ALG_IMPL|P1|旧判据＝旧宪章 §17.6（无界内存增长属发布禁止项）＋ 旧门禁文档 07 §4（内存背压必须生效）|最新替代＝ASTROCS_DESIGN §8（自动记录内存峰值与增长斜率，超预算自动记 violation 并阻断后续 heavy 调度）＋ §12（发布候选禁无界内存增长）＋ ENGINEERING_SPEC §10 ＋ docs/ci/01_CHECKS.md §2 CHK-RESOURCE（P0）|命令：sed -n "96p" lib/infrastructure/scheduler/src/runtime.cpp；输出：    scheduler_ = std::make_unique<Scheduler>(budget_, budget_);（第三参缺省）＋ sed -n "121p" 同文件；输出：      spec.estimated_memory_bytes = 0;。命令：sed -n "45,47p" include/astrocs/core/scheduler.h；输出：    // memory_limit_bytes: 0 = 不限制内存回压 ／   Scheduler(uint32_t available_cpu, uint32_t budget, ／             uint64_t memory_limit_bytes = 0);。命令：sed -n "172p" lib/infrastructure/scheduler/src/scheduler.cpp；输出：        if (memory_limit_bytes_ > 0 && active.load() > 0) {|OPEN|RT-001|无|全仓唯一生产构造点不传内存上限、节点内存估算恒写 0 ⇒ 内存回压分支永不进入，「超预算阻断后续 heavy 节点」在 CLI/生产路径不存在。站点路径已随 ARCH-001 由 runtime/runtime.cpp 迁至 lib/infrastructure/scheduler/src/runtime.cpp
V7-N-04|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/healpix_db/hp_drizzle_api.cpp，现实测在 lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.cpp）|ASTROCS_DESIGN 附录 B（FITS WCS Paper I：CD 矩阵为任意 2×2，非仅对角）＋ §5.3（几何与排异不变量）＋ §11.1（重采样验证 C_out=R·C_in·Rᵀ）＋ §3.5（error 必须阻断）|命令：sed -n "428p;431p;433p" lib/algorithms/drizzle/healpix_drizzle/hp_drizzle_api.cpp；输出：    bool has_cd = (cd11 != 0.0 ∥∥ cd22 != 0.0); ／     if (has_cd) { ／     } else if (has_cdelt && crval1 != 0.0 && crval2 != 0.0) {。命令：sed -n "404p" 同文件；输出：    double cd11   = aio_frame_kv_get_double(frame, "header", "CD1_1", 0.0);。命令：grep -n isfinite 同文件；输出：320 ／ 955（两处仅针对 photscal，不覆盖 CD/CDELT/SIP 读入面）|OPEN|P1-001|无|只检 CD1_1/CD2_2 两角元 ⇒ 反对角或纯旋转 CD（对角为 0、非对角有值）被判为「无 CD」并静默改用 CDELT·CROTA2 重建，交付天区几何可整体反转或转置且退出码仍为 0
V7-N-05|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/core/module_adapters.cpp，已迁 lib/infrastructure/scheduler/src/module_adapters.cpp）|ASTROCS_DESIGN §9（manifest 至少记录生效科学配置；禁止把计划值伪装成实际值）＋ §11.3（未达成必须显式报告，不得用占位/空输出冒充）＋ ENGINEERING_SPEC §3|命令：sed -n "2664,2666p" lib/infrastructure/scheduler/src/module_adapters.cpp；输出：  if (doc.contains("snr") && doc["snr"].is_object()) { ／     const Json& sc = doc["snr"]; ／     sci_cfg.gain_e_per_adu = sc.value("gain_e_per_adu", 0.0);（其后至 :2674 共九键同形，缺键一律落 0.0）。命令：sed -n "2838,2840p" 同文件；输出：  Json snr_out = Json{{"schema", "DATA-P1-SNR"}, ／                      {"schema_version", "2"}, ／                      {"frames", frames}};。命令：sed -n "2843,2846p" 同文件；输出：  (*man)["n_frames"] = frames.size(); ／  (*man)["snr_schema"] = "DATA-P1-SNR/2"; ／  (*man)["snr_artifact"] = out_path; ／  (*man)["artifacts"] = Json::array({out_path});（无生效配置回显）|OPEN|P1-002|无|snr 节点若被写成非 object 则整块被 if 静默忽略（不报错、不降级），缺键落 0.0 与显式 0.0 在产物面不可区分；p1_snr.json 不回显生效 SNR 科学配置（帧内仅 zp/定义串可反推一项）
V7-N-06|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/core/module_adapters.cpp:1392 起，已迁同库新路径）|ENGINEERING_SPEC §3（数据对象按 UNIFIED_MODEL 区分，禁止一个字段承载多个含义）＋ ASTROCS_DESIGN §9（manifest 记录生效值）＋ §3.5（暗场超容差须走 optimize/error 判定）|命令：sed -n "1391p" lib/infrastructure/scheduler/src/module_adapters.cpp；输出：  const bool dark_opt = doc.value("dark_optimization", false);。命令：sed -n "1494,1495p" 同文件；输出：                             {"dark_scale", dark_opt ? static_cast<double>(actual_k) ／                                                     : static_cast<double>(k_fixed)}});。命令：grep -c -e k_source -e "dark_optimization" 同文件；输出：1（仅 :1391 读配置，无任何写出/回显）|OPEN|P1-001|无|同一个 per_frame.dark_scale 承载「固定 K」与「实测优化 K」两种含义，且清单既不写开关也不写 K 来源 ⇒ 交付值 1.0 无法区分「应用了 K=1.0」与「暗优化实际未执行」
V7-N-07|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/core/module_adapters.cpp:2891，已迁同库新路径并现为 :2966）|ASTROCS_DESIGN §3.3（输入合同字段有确定语义）／§3.5（error 强制阻断，不得静默取默认）＋ ENGINEERING_SPEC §3（禁止一个字段承载多个含义）＋ docs/plugins/infrastructure/16_config_defaults.md|命令：sed -n "2966p" lib/infrastructure/scheduler/src/module_adapters.cpp；输出：  const int nested = p1_int(dj, "nested", 1);。命令：sed -n "1139p;1143p" 同文件；输出：int p1_int(const Json& c, const char* key, int dflt) { ／   if (it->is_number_integer()) {（字符串／浮点分支之后最终 return dflt，类型不符不报错）。命令：grep -n is_number_integer 同文件；输出：1867:    if (!v.is_number_integer()) { *ok = false; *err = std::string(k) + " must be integer"; return 0; }（同文件对其它字段有显式类型门）|OPEN|P1-001|无|布尔意图字段用整型助手解析 ⇒ 字符串 "0"／"false" 落缺省 1＝NESTED，交付索引语义被反转且退出码仍为 0；同文件别处已建类型门，属同族纪律不一致
W1-N-01|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/plate_solve/cpp/ipv/src/ipv_select.cpp，已迁 lib/algorithms/platesolve/cpp/ipv/src/）|ASTROCS_DESIGN §5.3（收敛与极限星等不变量不得因实现细节翻转）＋ §9（外部星表读取的唯一边界与错误面）＋ ENGINEERING_SPEC §3／§5.1 ＋ docs/algorithms/IPV_PIPELINE.md|命令：grep -n -e "StarCollector *sc_arr" -e "collector_init(&sc_arr" -e "&sc_arr\[f\]" lib/infrastructure/gaia_xpsd_client/src/gaia_client.c；输出：2099:    StarCollector *sc_arr = (StarCollector *)calloc(nfiles, sizeof(StarCollector)); ／ 2101:    for (int i = 0; i < nfiles; i++) collector_init(&sc_arr[i], 4096); ／ 2124:                                  cos_dec_q, sin_dec_q, cos_radius, &sc_arr[f], scratch, &trace);。命令：sed -n "487,488p" lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp；输出：    const bool capped = (n_ret > 0 && cap_unit > 0.0 && ／                         n_ret >= cap_unit);。命令：grep -n m_lim_gaia_cap_per_file lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h；输出：260:    double m_lim_gaia_cap_per_file = 200000.0;|OPEN|P1-001|无|原判据的机制描述被现树否证：collector 为每文件一份（sc_arr[f]），新注释的「每文件顺序截断」前提成立。但判据本身仍不成立——n_ret 是跨文件求和（经 gaia_cone_search 取聚合 count），拿总量与每文件上限同型比较 ⇒ 多文件各未触顶时仍判 capped 并 break（converged=false，丢更紧收敛解）；且交付公开头 ipv_types.h:258-260 仍写「返回数为其整数倍」旧口径，与 :487 实现互斥 ⇒ 注释-实现-测试三者不一致仍在
W1-N-04|C_ALG_IMPL|P1|无（原判据即现行权威；站点＝旧路径 lib/snr_estimator/src/module_entry.cpp，现落位 lib/algorithms/noise_snr/src/module_entry.cpp）|ENGINEERING_SPEC §5.1（边界、NaN/Inf、极端参数、错误输入必备测试）＋ ASTROCS_DESIGN §3.3／§3.5（配置字段确定语义＋error 阻断）＋ docs/plugins/algorithms_phase1/09_noise.md|命令：sed -n "447p;449p;463p;465p" lib/algorithms/noise_snr/src/module_entry.cpp；输出：        c->cfg.hreadn    = (int)d; ／         c->cfg.dark    = (int)d; ／         c->cfg.full_well    = (int)d; ／         c->cfg.gain    = (int)d;（四处均无 isfinite/范围夹取）。命令：grep -n "0x7fffffff" 同文件；输出：422（同文件对 h/w 已做上限防护＝正对照）。命令：git status --porcelain lib/algorithms/noise_snr/src ＋ grep -n "add_subdirectory(lib/algorithms/noise_snr)" CMakeLists.txt；输出：?? lib/algorithms/noise_snr/src/ ／ 210:# F-CI-002-01(CI-002 owner 裁决 2026-09-11): add_subdirectory(lib/algorithms/noise_snr)（注释态，未入图）|OPEN|P1-002|无|静态 UB 面成立（int 溢出与 nan/inf→int；该文件 JSON 取值面允许 nan/inf 字面量）；该 TU 仍未跟踪、根 CMake 按 CI-002 负责人裁决未收编 ⇒ 现产不可达，维持原判「静态判 UB、实跑触发需入库接线后验证」，不抬 P0
W3-R2-001|C_ALG_IMPL|P1|旧文档 docs/standards/IO_AND_ATOMICITY.md:3（docs/standards/** 不在现行权威链）|原子提交义务的最新替代＝ASTROCS_DESIGN §9（输出临时文件＋校验＋fsync＋原子 rename 提交；失败/取消不得留下可被误认为正式产品的半成品）＋ ENGINEERING_SPEC §9（输出临时文件+原子提交）＋ docs/plugins/infrastructure/17_aio.md §7|命令：sed -n "101,105p" lib/infrastructure/aio/src/aio_upm.cpp；输出：    if (std::rename(tmp.c_str(), path) != 0) { ／         // Windows rename 不覆盖已存在目标：删除旧正式模型后重试一次 ／         // （temp 仍完整，失败可恢复；ENG-IO-001 单文件原子语义保持） ／         if (std::remove(path) == 0 && ／             std::rename(tmp.c_str(), path) == 0) {（其 else 分支再 std::remove(tmp)）。命令：grep -c MOVEFILE_REPLACE_EXISTING lib/infrastructure/aio/src/hiss_stream_writer.cpp；输出：2（正对照：同库 HISS 写者用单步带 REPLACE_EXISTING 的移动，实现 :185）|OPEN|AIO-001|无|先删旧正式文件再改名：第二次 rename 亦失败时新旧两份同失，是数据净损失面，且注释以「失败可恢复」把它写成合规；同库同族写者用单步 REPLACE_EXISTING 为正对照。真机 Windows 双失败概率未测（本轮无该节点），本条只判代码路径与注释语义
W3-R2-002|C_ALG_IMPL|P1|旧文档 docs/modules/registry/PHASE3_RESAMPLE.md:87 与 THREAD_BUDGET_ARCH.md:27（docs/modules/registry/** 不在现行权威链）＋ 旧控制包 P15-06 取消验收|取消义务的最新替代＝ASTROCS_DESIGN §6.3（取消（Ctrl-C）：协作取消 → 停止调度新单元 → 等待运行中单元完成 → exit 9）＋ §7.1（scheduler/pipeline 唯一归属）＋ docs/plugins/infrastructure/19_runtime.md §6|命令：git grep -n astrocs_host_state_set_cancel；输出：lib/infrastructure/benchmark/backend_host/host_services.cpp:128: void astrocs_host_state_set_cancel(void* state, int v) {（全仓仅此定义，零调用点）。命令：sed -n "923,924p" lib/algorithms/calibration/src/module_entry.cpp；输出：    if (inst->host && inst->host->cancel && inst->host->cancel->is_cancelled && ／        inst->host->cancel->is_cancelled(inst->host->cancel->user_data)) {。命令：grep -n "ctx && ctx->cancelled()" lib/infrastructure/scheduler/src/module_adapters.cpp；输出：5611:      if (ctx && ctx->cancelled()) { cancelled.store(true); break; }|OPEN|RT-001|无|原判据第二腿（resample band worker 零取消检查）已被现树 RunContext 取消安全点补上，不再作依据；仍成立的是 host 取消通道整体断线：set_cancel 零调用 ⇒ host->cancel->is_cancelled 恒 false，calibration（:923）／cosmetic（:741）／gaia（module_entry.c:651 另设 gaia_set_cancel_checkpoint 第二套通道）三处模块级检查永不为真 ⇒ §6.3 的协作取消对经 host 服务面运行的模块不可达
W3-R2-003|C_ALG_IMPL|P1|旧门禁文档 03 §4（峰值租约＝该节点实际并行宽度）|最新替代＝ASTROCS_DESIGN §9（plan 是预期，trace 是实际观测，禁止把计划值伪装成实际值）＋ §8（自动记录实际并行宽度）＋ docs/plugins/infrastructure/21_observability.md §4（运行图是实际观测）／§8（事件与 run 产物一致性）＋ §11.3|命令：sed -n "99,102p" include/astrocs/core/context.h；输出：struct GrantedWorkerObservation { ／   std::atomic<uint32_t> peak_active{0};     // max 瞬时授予租约数 ／   std::atomic<uint32_t> peak_lease{0};      // max 单次租约 size ／   std::atomic<uint64_t> acquired_total{0};  // 成功 acquire 次数（三字段无任何复位/epoch 面）。命令：grep -n peak_lease lib/infrastructure/scheduler/src/runtime.cpp；输出：188:                ? obs_post.peak_lease.load(std::memory_order_relaxed)（其后 :218 tr.workers = node_granted; ／ :243 ev_end.workers = static_cast<uint32_t>(tr.workers);）|OPEN|OBS-001|无|单例峰值单调不减、无 run/节点边界复位；runtime 已用 acquired_total 差分判「本节点是否新租约」，却把全局历史峰值当本节点 workers 写出 ⇒ 后一节点继承前序更宽节点的峰值，trace 里的实际并行宽度不是该节点观测值
W3-R2-005|C_ALG_IMPL|P1|旧文档 docs/modules/registry/THREAD_BUDGET_ARCH.md:8（Σactive ≤ 预算 不变量）与旧版 19_runtime 租约条款＋旧控制包 P15-06 租约验收|预算唯一源义务的最新替代＝ASTROCS_DESIGN §8（一个进程只有一个资源调度器与线程预算源；§5.2/§8 heavy 无硬编码线程）＋ ENGINEERING_SPEC §10（并发度/线程池属架构不变量）＋ docs/plugins/infrastructure/19_runtime.md §4／§6|命令：sed -n "429p" providers/cpu/baseline/src/baseline_provider.cpp；输出：                (void)host->executor->acquire(host->executor->user_data, 1);（返回值被丢弃）。命令：sed -n "455,457p" 同文件；输出：    if (host != nullptr && host->executor != nullptr && ／        host->executor->release != nullptr && workers > 0) ／        host->executor->release(host->executor->user_data, workers);（守卫不含「是否曾 acquire」）。命令：sed -n "83p" lib/infrastructure/benchmark/backend_host/host_services.cpp；输出：    static_cast<HostState*>(ud)->active_workers.fetch_sub(static_cast<long>(n));（无下限钳制；acquire 侧 :77 才用 min 夹取）|OPEN|CPU-001|无|cap≤1 或 N≤1 分支整段跳过 acquire 却仍执行 release ⇒ 每次净 -1；acquire 失败仍继续 fork 并在末尾 release ⇒ 同样净 -1；Σactive 可为负后 :418 的「预算−已用」门恒真 ⇒ 预算不变量永久失效。avx2（:250-255／:276-277）与 avx512（:249-254／:275-276）同形。provider execute 面在 cli/lib 无直调（可达面为 tests/cpu oracle），故维持 P1 不抬 P0
W3-R2-006|C_ALG_IMPL|P1|旧文档 docs/standards/PUBLIC_API.md:687（采样线程所有权）＋ 旧控制包 P15-06 监控验收|监控采样线程安全的最新判据＝ASTROCS_DESIGN §8（每个 heavy 运行自动记录，记录本身须为真实观测）＋ §9（trace 是实际观测）＋ docs/plugins/infrastructure/21_observability.md §7（事件/日志不得静默失真）＋ ENGINEERING_SPEC §5.1（边界测试）|命令：grep -c -e mutex -e lock_guard -e unique_lock cli/monitor.h；输出：0（唯一 atomic 命中是 :9 的 include，用于 stop 标志而非样本容器）。命令：grep -n -e "samples_.push_back" -e "for (const auto& sm : samples_)" cli/monitor.h；输出：370:        samples_.push_back(cur); ／ 422:        for (const auto& sm : samples_) { ／ 428:            for (const auto& sm : samples_) {。命令：sed -n "747p;751p;752,753p" cli/commands.cpp；输出：747:    const auto s0 = mon.summary(); ／ 751:        sampling.store(false); ／ 752:        if (sampler.joinable()) { ／ 753:            sampler.join();|OPEN|OBS-001|无|读侧在停采样与 join 之前调用 summary()，与仍在 push_back 的采样线程并发读写同一 std::vector ⇒ UB（可表现为迭代器/缓冲区撕裂而非常量陈旧值），后果是利用率/线程数证据失真甚至崩溃；TSan 实跑缺（本轮无 sanitizer 构建），静态可判。行号已随 CLI-001 重定位（原判 843-853／858-859，现 745-753）
W6-N-01|C_ALG_IMPL|P1|无（原判据即现行权威；站点 ci/ctest_baseline.json 与 tools/quality/check_ctest_registration.py 未迁移）|docs/ci/01_CHECKS.md §1（每项检查必须能绿能红；登记即须可执行）与 §2 表 CHK-TEST／§3（P0 红灯不可豁免）＋ ENGINEERING_SPEC §8（禁止悬空测试路径；新门同提交登记测试）＋ ASTROCS_DESIGN §11.1（验证体系）|命令：python3 统计 ci/ctest_baseline.json；输出：baseline targets: 160。命令：python3 检查六个在册名是否仍在基线；输出：healpix_math True ／ stf_engine True ／ browser_backend True ／ hips_browser_backend True ／ geometry_truth True ／ module:astrocs.conformance.echo True。命令：grep -c -e browser_qt -e "conformance/echo" CMakeLists.txt；输出：0（相关子树均不在根构建图）|OPEN|CI-001|无|门的两条判定仍只看「注册名是否出现在活动 CMake 源文本」与「是否在基线名单」，无「已注册进真实构建目录/可被执行」维度 ⇒ 不在构建图的 add_test 名永挂基线且永不运行。注册表重排后该门现为 CHK-MODULE-MANIFEST 下的 step CTEST-REGISTRATION（command 仍指向同一脚本）
W6-N-02|C_ALG_IMPL|P1|无（原判据即现行权威；站点 ci/checks.json 与 ci/validate_registry.py；本会话注册表已被 CI-001 重构）|docs/ci/01_CHECKS.md §1（每项检查必须能绿能红）与 §5（运行方式 python3 ci/run_checks.py）＋ docs/ci/03_GATES.md §1（P0/P1/P2 分级；P0 无豁免）＋ ENGINEERING_SPEC §8（检查器覆盖与登记）|命令：python3 展平 ci/checks.json 统计 prerequisite_tools；输出：top gates: 38 ／ steps total: 147 ／ declaring prereq: 27 ／ declared 值集＝{cmake 13, clang 3, python3 9, python3:yaml 1, python3:astropy 2, python3:numpy 6, llvm-profdata 1, llvm-cov 1, pytest 1, dumpbin 1}。命令：python3 对 g++/gcc/nm/objdump/tar/bash/taskset/ctest 计「声明数／以该 token 为 argv 首元素的 step 数／命令串含该 token 的 step 数」；输出：g++ 0/0/0 ／ gcc 0/0/52 ／ nm 0/0/0 ／ objdump 0/0/0 ／ tar 0/0/46 ／ bash 0/0/0 ／ taskset 0/0/1 ／ ctest 0/0/51。命令：grep -n "# R12" ci/validate_registry.py；输出：291:        # R12（CI-001）：聚合项 steps 结构 + 派发入口（非 prereq→waiver 绑定规则）|OPEN|CI-001|无|核心仍成立：实际依赖 ctest（51 步）/tar（46 步）/gcc（52 处）的门零宿主工具声明，姊妹门 UT-BACKEND／UT-IO／UT-CLI 的 prerequisite_tools 仍为空数组 ⇒ 缺工具时只按 run.py 对 argv[0] 的探测处理或被子脚本 except 吞成假绿，既不可诊断也无法按 CI-REG-002 归因跳过。口径已重算（原 18/135 → 现 27/147）；nm/objdump/bash/g++ 已不再是任何 step 的 argv 首元素；「R12 未实现」须改述为「R12 名额已被 steps 派发规则占用，prereq→waiver 绑定仍无实现」
W6-N-03|C_ALG_IMPL|P1|无（原判据即现行权威；站点 cmake/install_layout.cmake、packaging/install-tree.contract.json、packaging/verify_install_tree.py）|ASTROCS_DESIGN §10.1（交付形态＝唯一 exe＋各 dll＋schemas＋product manifest）／§12（发布包白名单）＋ docs/ci/01_CHECKS.md §2 CHK-PACKAGE（P0 打包/白名单/哈希/版本/provenance）＋ ENGINEERING_SPEC §8（清单唯一事实源且必须机器校验）|命令：sed -n "115,116p" cmake/install_layout.cmake；输出：  install(DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/packaging/schemas/ ／    DESTINATION ${ASTROCS_SCHEMA_DIR} FILES_MATCHING PATTERN "*.json")。命令：ls packaging/schemas；输出：astrocs-product.schema.json ／ dependency-lock.schema.json ／ hips_product.schema.json ／ preset-contract.json（共 4 份）。命令：python3 统计 packaging/install-tree.contract.json；输出：units: 16 ／ SCHEMA 类型 units: 2。命令：python3 检索注册表内含 verify_install_tree 的门；输出：[]|OPEN|PKG-001|无|安装规则按目录通配、合同按逐文件白名单，两面不同源（4 份 vs 2 单元）⇒ 白名单不闭合；唯一闭集校验器 verify_install_tree.py 在现行 38 门+147 step 全文内零引用，其文档串自承只查「合同声明的文件存在」。实际交付树是否真多装需一次 cmake --install 实测（本轮未跑），本条只判口径与注册缺失
W6-N-04|C_ALG_IMPL|P1|旧判据含旧扫描轴 ARCH-001/V11 的 struct_size 普查口径（问题扫描自设统计，非权威条款）|struct_size 义务的最新替代＝ASTROCS_DESIGN §7.3（版本化 C ABI：跨 DLL 不传 STL/异常/RTTI；结构体带 struct_size/abi_version）＋ ENGINEERING_SPEC §4 第 3 条（公开头文件带 struct_size/abi_version）＋ docs/api/COMMON_ABI_V1.md:8（所有 struct 首两字段 struct_size, abi_version）|命令：sed -n "403p" ASTROCS_DESIGN.md；输出：- 版本化 C ABI：跨 DLL 不传 STL/异常/RTTI/编译器私有类型；结构体带 `struct_size`/`abi_version`；所有权与释放方明确。命令：grep -c struct_size lib/infrastructure/aio/include/astrocs/aio_hips.h lib/infrastructure/gaia_xpsd_client/src/gaia_client.h lib/algorithms/star_detect/include/astrocs/p1/star_detector.h lib/algorithms/dynamic_psf/include/astrocs/p1/dynamic_psf.h；输出：四份均为 0（且 gaia_client.h 仍位于 src/ 而非 include/）。命令：grep -c struct_size lib/algorithms/platesolve/cpp/ipv/include/ipv_api.h；输出：3（同族已修面存在＝正对照）|OPEN|CI-001|无|判据在现行链有三处明文，原点名四站点仍无 struct_size。口径说明：本会话以 git ls-files 下 include/＋lib/ 头文件重普查「匿名 typedef struct」面为 23 文件／其中 2 文件含 struct_size，与原 124 处／20 文件口径不同（原按声明点计数并含已迁路径），差异不改判定；「五份镜像脚本在门里引用数=0」一腿属问题扫描自用工具（_verify/_v11_*.py），非活动采集面，故改按「现行 147 step 内无任何 ABI struct 尺寸/镜像一致性采集门」立据
W6-N-05|C_ALG_IMPL|P1|旧判据含旧控制包 05/06/07 门禁文档（旧世代编号）与旧宪章 §12.3-6（链外文本）|导出面义务的最新替代＝ASTROCS_DESIGN §7.3（模块与 DLL/SO 边界；版本化 C ABI）＋ §12（发布包白名单）＋ docs/contracts/ARC-001.md（现行合同文档）＋ docs/ci/01_CHECKS.md §2 CHK-ABI／CHK-PACKAGE（均 P0）与 §4（能绿能红）＋ docs/ci/03_GATES.md §4|命令：git grep -ln module_dll_contract；输出：contracts/config/module_dll_contract.schema.json ／ docs/modules/MODULE_MAP.yaml ／ docs/owner/ARCHITECTURE_OVERVIEW.md ／ docs/review/ARCHITECTURE_OVERVIEW.md ／ modules/conformance/noop/README.md（无任何 .py／CMakeLists／ci 消费者）。命令：sed -n "622p" tools/quality/ci_windows_driver.py；输出：                ok = res["exit_code"] == 0 and bool(exports)。命令：grep -c "WINDOWS_EXPORT_ALL_SYMBOLS ON" CMakeLists.txt；输出：3。命令：python3 统计注册表内含 nm 的门；输出：0|OPEN|PKG-001|无|ARC-001 导出面合同只有 schema、无实例也无机器消费者（MODULE_MAP.yaml 仅文本提及）；Windows 符号门判据为「非空即过」，不校验白名单；Linux 侧零采集面；根 CMake 三处仍开 WINDOWS_EXPORT_ALL_SYMBOLS ON 使导出集由链接器全开决定。真机导出集是否越界需 Windows x64 构建目录 dumpbin（本轮无该节点），不另判
V10-N-07|C_ALG_IMPL|P2|无（原判据即现行权威；站点＝旧路径 lib/hips／lib/drizzle／lib/gaia_xpsd_client 的 src/module_entry.*，已迁 lib/algorithms/drizzle/{hips/,}src/ 与 lib/infrastructure/gaia_xpsd_client/src/）|ASTROCS_DESIGN §9（aio 是唯一 manifest/FITS/HiPS 读写边界，禁止各自复制 reader/writer）＋ §8（无界内存增长禁项）＋ ENGINEERING_SPEC §5.1（边界、NaN/Inf、极端参数必备测试）|命令：grep -n "malloc((size_t)" lib/algorithms/drizzle/hips/src/module_entry.cpp lib/algorithms/drizzle/src/module_entry.cpp lib/infrastructure/gaia_xpsd_client/src/module_entry.c；输出：hips:218:    uint64_t* arr = (uint64_t*)malloc((size_t)(count > 0 ? count : 1) * sizeof(uint64_t)); ／ drizzle:206 与 gaia:197 同形（double 版）／三处 count 均来自外部 JSON 数组长度，无上限。命令：sed -n "462p" lib/algorithms/drizzle/hips/src/module_entry.cpp；输出：    if (!r->parent_ipix ∥∥ an < 0 ∥∥ (uint64_t)an != n_tiles) {（长度一致性校验位于 :218 分配之后）|OPEN|AIO-001|与 GAP-013 重复|三处自写 JSON 解析器都在按外部 count 分配之后才校验一致性；数值面：hips 全文 isfinite 计数 0（double→uint64 直转即 UB），gaia 数组仅比 nr/nd 长度不校验元素有限性（同文件标量侧有 4 处 isfinite），drizzle 侧有 10 处但不覆盖数组元素。调用点新增的 65535/512 上限是另两档字段的夹取，不补足本项
V10-N-08|C_ALG_IMPL|P2|无（原判据即现行权威；站点＝旧路径 lib/healpix_db/healpix_browser_qt 与 lib/orchestrator，已迁 lib/infrastructure/hips_browser/healpix_browser_qt 与 lib/infrastructure/pipeline/orchestrator）|ASTROCS_DESIGN §9（aio 是唯一 HiPS 读写边界）＋ §10.1（Alpha 不含 GUI；HiPS Browser 不进产品清单）＋ docs/plugins/infrastructure/23_hips_browser.md §1／§4（读侧复用 aio，不复制 reader）／§7（产品不可读/损坏 → 明确报错，不渲染伪图像）＋ 附录 B（IVOA HiPS 1.0：order 值域）|命令：sed -n "157p;162p;168p;200p" lib/infrastructure/hips_browser/healpix_browser_qt/core/hips_browser_backend.cpp；输出：                order_ = std::atoi(v.c_str()); ／         leaf_order_ = order_ + kTileShift; ／             order_ = std::atoi(props.c_str() + p + 11); ／     const uint32_t nside = uint32_t(1) << (uint32_t)leaf_order_;（该文件零范围判定）。命令：sed -n "3982p;3985p" lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp；输出：            if (p != std::string::npos) hips_order = std::atoi(s.c_str() + p + 11); ／     const uint32_t nside_leaf = 1u << (uint32_t)(hips_order + 9);。命令：grep -c -e browser_qt -e "pipeline/orchestrator" CMakeLists.txt；输出：0（两子树均不在根构建图）|OPEN|INT-001|无|同一 hips_order 字段在 aio 读面与 hp_drizzle_api 已夹（后者 :461-465 越界 return -10），此两面 atoi 结果直进移位 ⇒ 属「同一字段多口径」族；两子树均不在编译面，按原判「补注册即红」，不判现产可达。浏览器面另违反 23_hips_browser.md §4（读侧应复用 aio，此处自带 properties/FITS 解析）。订正：orchestrator 的 A_ORDER 腿现已带 SNR_SIP_MAX_ORDER 界（:4453／:4456），残余为出界整段静默跳过 SIP
