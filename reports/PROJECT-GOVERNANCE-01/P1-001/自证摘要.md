# P1-001 自证摘要 —— phase1(normalize) 侧问题清单整改（归属 112 条 / P0-OPEN 16 条）

- 任务：P1-001（工程控制/PROJECT-GOVERNANCE-01/tasks/P1-001.md），本轮承担「归属 112 条问题清单整改」
- 执行时刻工作树：开工 `HEAD=main=origin/main=8f0a4c6bf24ffd7346a8c2c3fd8dc0e4407ee6d0`；执行中前台并行提交推进到 `fa454b67144766abfc346606bacdb483cabae386`（见 §6 归属说明）
- 输入：`reports/PROJECT-GOVERNANCE-01/root-scan/by_owner/P1-001.txt`（112 条，P0-OPEN 16 条单列）、`问题扫描/REBASE_TABLE.md`（10 列判定）、`问题扫描/REBASE.md`（四态口径）、`reports/PROJECT-GOVERNANCE-01/root-scan/CONTINUATION.md`（陷阱 20 条）
- 硬约束遵守：零 git 写（不 add/commit/push/branch/stash/reset/checkout/git mv）；未改 `ci/**`、`.github/**`、`docs/algorithms/**` 锚与 `anchor_contract.json`、`cmake/**`、`tests/cli/**`、`tests/backend/**`；未改任何科学公式/默认容差/SCI·ALG 冻结定义；未用 skip/放宽断言/删测试制造绿灯。
- **本轮禁止采信账本 `fix_state/verified_state`**：112 条一律重新取证。

---

## 1. 翻译表（旧路径 → 新路径）

> ARCH-001 已把 35 个旧 `lib/` 目录迁到目标结构，清单第 6 列证据命令里的旧路径全部失效。
> 本轮先建映射（来源 `cmake/ARCH-001-migration-manifest.md` §1），逐条把证据命令翻译后再复跑。
> 完整表（含每条命令里实际出现的全部 76 个路径）见 `run/PROJECT-GOVERNANCE-01/P1-001/翻译表.md`；下列为骨架。

| 旧路径 | 新路径 | 状态 |
|---|---|---|
| `lib/calibration` | `lib/algorithms/calibration` | DONE |
| `lib/cosmetic` | `lib/algorithms/cosmetic` | DONE |
| `lib/star_detector`（含旧漏写形态 `lib/star_detection`） | `lib/algorithms/star_detection` | DONE |
| `lib/dynamic_psf` | `lib/algorithms/psf` | DONE |
| `lib/plate_solve` | `lib/algorithms/platesolve` | DONE |
| `lib/photometric_calib` | `lib/algorithms/photometry` | DONE |
| `lib/snr_estimator` | `lib/algorithms/noise_snr` | DONE |
| `lib/drizzle` | `lib/algorithms/drizzle` | DONE |
| `lib/healpix_db/healpix_drizzle` | `lib/algorithms/drizzle/healpix_drizzle` | DONE |
| `lib/hips` | `lib/algorithms/drizzle/hips` | DONE |
| `lib/healpix_db`（余部） | `lib/infrastructure/aio/healpix_db` | DONE |
| `lib/astro_image_io` | `lib/infrastructure/aio` | DONE |
| `lib/orchestrator` | `lib/infrastructure/pipeline/orchestrator` | DONE |
| `lib/core` | `lib/infrastructure/scheduler` | DONE |
| `lib/gaia_xpsd_client` | `lib/infrastructure/gaia_xpsd_client` | DONE |
| `lib/phase2_proj` / `lib/phase3_proj` | `lib/algorithms/projection` | DONE |
| `lib/phase1/{noise,photometry,stars,wcs,v6,tests}` | `lib/algorithms/{noise_snr,photometry,star_detection,platesolve}/wrapper_phase1`、`lib/algorithms/integration/v6_phase1` | DONE |
| `lib/{phase1,phase2,phase3}_session` | （不迁移，待 INT-001 删除） | DEFERRED |
| `runtime/io/*.c`、`docs/**`、`tests/**`、`cli/**` | 原位（不在迁移范围） | 无需翻译 |

**无法翻译 / 不可复跑的条目（及原因）**：

| 条目 | 出现在 | 原因 | 处置 |
|---|---|---|---|
| `lib/star_detection/sdet_api.cpp` | CONTINUATION §9.2 陷阱 16 | 该路径从未存在；真实位置 `lib/algorithms/star_detection/src/sdet_api.cpp` | 按 basename 重定位后复跑 |
| `docs/algorithms/IPV_PIPELINE.md` | V12-N-06 | 当前树**不存在**（同仓仅 `lib/algorithms/platesolve/IPV_PIPELINE.md`，且其中 0 处偏差登记） | 该悬空引用即缺陷本身 → 已整改注释 |
| `python3 读 testdata/NGC55_T3_flying_dutchman 真帧 CD 复算` | W2-N-08 第 2 条 | 自然语言伪命令，不可执行 | 该子项不可复跑（本轮按第 1 条证据判定） |
| `git grep … 与 git grep …` | V20-N-01 | 中文「与」连接两命令，非法 shell | 拆两条分别复跑（均 rc=0） |
| `sed -n … 同文件` / `… 与 … 与 … 同文件` | V2-N-09 / V2-N-10 | 占位符不可解析 | 按上下文还原为首命令同一文件后复跑 |
| `run/perf-fix/**`、`run/release-rescue/**` | W1-N-08 / V5-N-04 | `run/` 为 gitignore 运行产物，非入库权威 | 判据侧记「出处未入库」（V5-N-04 维持 OPEN；W1-N-08 见 §5） |

---

## 2. 复检方法（可复跑）

```bash
cd "/workspace/Astro CS Database"
# 1) 抽取归属 112 条 + REBASE_TABLE 10 列
timeout 120 python3 run/PROJECT-GOVERNANCE-01/P1-001/extract2.py
# 2) 路径翻译（ARCH-001 映射，长前缀优先）+ 语义拆命令 + 逐条复跑（timeout 60，日志落 run/…/logs/<ID>.log）
timeout 1800 python3 run/PROJECT-GOVERNANCE-01/P1-001/run_recheck.py
# 3) 生成翻译表与 112 行处置表
timeout 120 python3 run/PROJECT-GOVERNANCE-01/P1-001/gen_tables.py
# 4) 已整改条目「改前/改后」对照复跑
timeout 600 python3 run/PROJECT-GOVERNANCE-01/P1-001/after_check.py
```

- 复跑规模：**112 条 → 218 条子命令**（含拆分后的命令），逐条 rc 与逐字输出见 `run/PROJECT-GOVERNANCE-01/P1-001/logs/<ID>.log`，机器汇总 `recheck_results.json`。
- 相对 REBASE_TABLE 第 6 列：**113 条子命令输出逐字一致**（即旧判定的事实在当前树仍成立），差异全部由本轮整改或前台并行提交解释（见 §6）。
- 未采信账本状态：8 条账本 `RESOLVED` 全部独立复跑并**维持 RESOLVED**（M7-A-204、V12-N-15、M1a-C-002、M2a-C-2、V2-N-09、V2-N-10、M2a-H-1、M3b-H-01）；无「账本 RESOLVED 而证据仍红」的反例。

---

## 3. 112 行处置表（计数与清单一致）

完整表：`run/PROJECT-GOVERNANCE-01/P1-001/处置表.md`（同时复制到 `reports/PROJECT-GOVERNANCE-01/P1-001/处置表.md`）。
行数 **112**，ID 集合与 `by_owner/P1-001.txt` 的 112 个 ID **双向差集为空**。

| 四态 | 条数 | 其中 P0 |
|---|---|---|
| OPEN | **104** | 16 |
| RESOLVED | **8** | 3 |
| VOID | 0 | 0 |
| UNVERIFIABLE | 0 | 0 |
| 合计 | **112** | 19 |

| 处置类别 | 条数 |
|---|---|
| FIXED-COMMENT（本域注释整改已完成） | 14 |
| FIXED-COMMENT+FOLLOWUP（部分整改 + 残余后续） | 3 |
| FIXED-COMMENT+NEED-RULING（部分整改 + 残余需裁决） | 2 |
| FIXED-REF（悬空引用整改） | 1 |
| **小计（本轮实际整改）** | **20** |
| NEED-RULING（需权威裁决，含科学定义/冻结文档） | 49 |
| OUT-DOMAIN（他任务文件域，已指明归属） | 26 |
| OUT-DOMAIN+FOLLOWUP | 1 |
| FOLLOWUP（本域后续小任务，需独立设计+正负例） | 8 |
| CONFIRMED（原判 RESOLVED 复检维持） | 8 |
| 合计 | 112 |

> 四态分布与 REBASE_TABLE 的机械分布**完全一致**（104 OPEN / 8 RESOLVED），说明本轮复跑未引入口径漂移。

---

## 4. P0-OPEN 16 条逐条结论

| # | ID | 复检 | 结论 | 处置 |
|---|---|---|---|---|
| 1 | `M1a-A-001` | OPEN | SCI `ASTROMETRY.md:48` 仍写 `(u,v)=CD·(xp−CRPIX)+SIP_A/B(u,v)`，与实现 `ipv_wcs.cpp:918`「像素域解 u+A(u)=UV」在量纲/自变量上互斥 | **需裁决**：改 SCI 公式或改实现均属科学定义变更（ENGINEERING_SPEC §3）；`docs/science` 只读 → 阻塞 |
| 2 | `M1a-A-002` | OPEN | `PLATESOLVE.md:186` 精度门 `rms≤0.5″` 与 `plate_solve/memory.md:55/63` 实测（RMS 0.151px vs 独立 median 0.897px，5.9×，VERDICT PASS）并存，量测域未定义 | **需裁决**：门定义域（角秒 vs 像素 vs 全帧外部闭环）→ 阻塞 |
| 3 | `M2a-A-1` | OPEN | `DRIZZLE.md:40/61/81` 三种面亮度陈述（`w=a/A_drop`、`S_p=B0`、常数场 `S_p=C`）不可同真 | **需裁决**：SCI 冻结文本只读 → 阻塞 |
| 4 | `M3b-A-01` | OPEN | `sdet_api.cpp:375` 生产拟合母函数仍为椭圆高斯 `sdet_gaussian_f`（`sdet_moffat4_fit` 转调同一 LM），而 `STAR_DETECTION.md:41` 冻结「Moffat4 GSL trust-region LM」，`PSF.md §10` 明列此为不可接受变化 | **需裁决**：换母函数=改科学公式 → 阻塞 |
| 5 | `M3b-A-03` | OPEN | `orchestrator.cpp:2470/2506` 把 PSF 残差 MAD 原样写入 `flux_uncertainty` 列（缺 √N_eff 与沿 A/sx/sy 传播） | **需裁决 + 越界**：列语义属 DATA 合同；文件域属 INT-001 → 阻塞 |
| 6 | `M2b-B-01` | OPEN | `aio_hips_writer.cpp:137` / `aio_hips_reader.cpp:41` / `runtime/io/hips_core.c:558` 三处 Dir/Npix 与 IVOA REC-HIPS-1.0 §4.1 算式相反 | **越界**：文件域属 AIO-001（本轮未改） |
| 7 | `M2b-B-02` | OPEN | `write_moc_fits` 缺强制键 `ORDERING=NUNIQ`/`COORDSYS=C`（`NUNIQ` 全文件 0 命中） | **越界**：文件域属 AIO-001 |
| 8 | `M2b-B-03` | OPEN | `aio_hips_writer.cpp:943` `hips_pixel_scale` 写出含 3600 因子（角秒），标准同名键单位为度 | **越界**：文件域属 AIO-001 |
| 9 | `M2b-B-09` | OPEN | `runtime/io/fits_core.c:1155` 无条件写 CHECKSUM 全零串（注释自称 updated），verify 豁免设在计算值侧 `:1651` | **越界**：文件域属 runtime/AIO-001 |
| 10 | `M9-B-1` | OPEN（残余） | `ipv_wcs.cpp:361` 注释把像素域系数称「(SIP 标准)」；同一函数 :274 除 3600 而 A/B 不除 | **已修复（注释）+ 需裁决（残余）**：注释已改为如实区分「像素域约定」与「SIP 标准 deg/px^(i+j)」；写盘仍以标准键 `A_i_j`/`B_i_j` 承载像素域系数、`module_adapters.cpp:1933` 写出面未变 → 键语义属科学/合同裁决 |
| 11 | `M1a-C-001` | OPEN | `ASTROMETRY.md:39/125` 仍 `NB_GRID=7`（2 处），`ipv_wcs.cpp:411` 实为 41（另一路 81），且 :409 自注 1e-4px 数学不可达 | **需裁决**：SCI 冻结值只读 → 阻塞 |
| 12 | `M1a-C-003` | OPEN | 自检环以 0-based 下标 `x∈[0,W)` 喂 1-based 契约的 `WcsTan`（`module_adapters.cpp:2149/2384` vs `:1920`），独立参考解同施 `dx=x−crpix1` 故两门零判别力 | **越界 + 需裁决**：文件域属 scheduler/INT-001；pixel_origin 桥接属 SCI-WCS 合同 |
| 13 | `M3-E-001` | OPEN | `docs/TRACEABILITY.csv:20` TEST-CAL-001 仍 `VERIFIED`，而该行 test_path 指向测光比例应用测试，`ac_correct_frame` 不在 public_api（实测 `False`） | **越界**：文件域属追踪/文档任务 |
| 14 | `M3b-F-01` | OPEN | `gate2_psf_oracle` 零 CI 登记（`ci/checks.json` 命中 0），`evidence/gate2_result.json` 生产门 `centroid_p95_le_0.3px_production=false`（blocker 自述 PSF-001 未闭合） | **越界 + 后续**：`ci/**` 禁改；生产门转绿需 PSF-001 收口 |
| 15 | `M3b-F-02` | OPEN | `PSF.md:99` 承诺的 scipy/NumPy `curve_fit` 独立 Oracle 在 `tests/`+`lib/` **0 命中** | **需裁决**：补 Oracle 需新依赖与验收口径；SCI 承诺只读 |
| 16 | `M3b-F-03` | OPEN（部分） | fixture 三段幅值使低谱段实际 SNR=6.7–9.2（注释原写「SNR≈20」，为假），SCI 冻结的召回域 SNR∈[10,32.5) **零样本** | **已修复（注释）+ 后续**：已如实标注三段实际 SNR 并标出低于冻结域；补 SNR≥10 样本需改测试数据（可能转红，需独立任务）；`tests/unit/p1_stars_test.cpp` 条件守卫属他域 |

**P0-OPEN 汇总**：修复/部分修复 **2** 条（M9-B-1、M3b-F-03）；需权威裁决 **7** 条（M1a-A-001/A-002、M2a-A-1、M3b-A-01/A-03(兼越界)、M1a-C-001、M3b-F-02）；越界转交 **7** 条（M2b-B-01/02/03/09 → AIO-001、M1a-C-003 → INT-001、M3-E-001 → 追踪域、M3b-F-01 → PSF-001+CI 域）。
**本轮未对任何 P0-OPEN 条目擅自改动科学语义或越界改动**。

---

## 5. 改动清单与「改前红 → 改后绿」证据

### 5.1 改动清单（16 文件，**全部为注释/引用整改，零代码语义改动**）

| # | 文件 | 对应条目 |
|---|---|---|
| 1 | `lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp` | L28b-D-004、M2a-D-1、M9-D-1 |
| 2 | `lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.h` | M2a-D-2 |
| 3 | `lib/algorithms/drizzle/healpix_drizzle/spherical_overlap.cpp` | L28d-D-002 |
| 4 | `lib/algorithms/drizzle/src/module_entry.cpp` | M2a-D-2、V7-N-10 |
| 5 | `lib/algorithms/drizzle/include/astrocs/drizzle/types.h` | M6a-C-004 |
| 6 | `lib/algorithms/calibration/src/master_generator.cpp` | M3-D-004 |
| 7 | `lib/algorithms/calibration/src/cosmetic_corrector.cpp` | M3-D-005、M6a-D-011 |
| 8 | `lib/algorithms/platesolve/cpp/ipv/src/ipv_wcs.cpp` | M9-B-1 |
| 9 | `lib/algorithms/platesolve/cpp/ipv/include/ipv_types.h` | M1a-D-002、V12-N-06 |
| 10 | `lib/algorithms/platesolve/cpp/ipv/src/ipv_robust_refine.cpp` | L28d-D-003 |
| 11 | `lib/algorithms/platesolve/cpp/ipv/src/ipv_select.cpp` | V12-N-06、M1a-C-006 |
| 12 | `lib/algorithms/star_detection/src/sdet_api.cpp` | M3b-D-01 |
| 13 | `lib/algorithms/star_detection/src/sdet_angle_guard.h` | V8-N-08 |
| 14 | `lib/algorithms/star_detection/tests/p1star/p1star_fixtures.hpp` | M3b-F-03 |
| 15 | `lib/algorithms/photometry/cpp/src/pc_api.cpp` | M3-D-006 |
| 16 | `lib/algorithms/photometry/tests/p1phot/p1phot_fixgates.cpp` | V6-N-03 |

- 机械校验：未提交部分逐行核对，**新增/删除行 100% 为注释行**（命令：`git diff HEAD -- <5 files> | grep -E '^[+-]' | grep -vE '^[+-]\s*(//|/\*|\*)'` → 空输出）。
- 未改任何 `.cpp/.h` 的可执行语句、未改测试断言、未改 CI、未改 cmake、未改 `docs/**`。

### 5.2 「改前红 → 改后绿」证据（20 条，逐条 before/after 逐字对照）

完整对照（命令 / before rc / after rc / before 输出 / after 输出 / CHANGED 标志）：
`run/PROJECT-GOVERNANCE-01/P1-001/logs/20_after_fix_recheck.log`。

代表样例：

| 条目 | 证据命令（翻译后） | 改前（红） | 改后（绿） |
|---|---|---|---|
| L28b-D-004 | `grep -n "ADAPTIVE_RATIO_THRESH" lib/algorithms/drizzle/healpix_drizzle/drizzle_engine.cpp` | `370: … 触发细分) []` 空溯源括号 | 同一行无 `[]`；rc 由 0→0，缺陷文本消失 |
| L28d-D-002 | `sed -n '550,578p' …/spherical_overlap.cpp` | `// hp_epsilon = hp_res_rad * 1e-12 … ~256 段`（与实现 1e-6 互斥） | `// 细分阈值取相对值 hp_epsilon = hp_res_rad * 1e-6 …`；两代口径不再并存 |
| L28d-D-003 | `sed -n '613,629p' …/ipv_robust_refine.cpp` | `// 1%-3% → 指数过渡 (1e-4 → ~22)`（真值 1e6，差 4.5 个数量级） | `// abs(rel_change) > cd_freeze_threshold → cd_damp_freeze (冻结)` |
| M1a-D-002 | `grep -n "角秒" …/ipv_types.h` | `26:// 星点 (角秒坐标…)`、`28:// X坐标(角秒)` | `26:// 星点 (… U=像素, W=角秒)`、`28:// X坐标 (U 向量: 像素 / W 向量: 角秒)` |
| M2a-D-1 | `grep -n "211034.6" …/drizzle_engine.cpp` | 4 处 `≈ 211034.6 / nside arcsec`（实算 211076.285） | 4 处 `≈ 211076.3`（与 `sqrt(π/3)·(180/π)·3600` 一致） |
| M2a-D-2 | `sed -n '62,70p' …/drizzle_engine.h` + `sed -n '551p' …/module_entry.cpp` | 注释称 `release 只保留 3 字段(20B→12B)`；plan 按 `8B*6` | 注释改为实际 4 字段；plan 改为 `8B*4`（32B/叶） |
| M3-D-004 | `sed -n '171,175p' …/master_generator.cpp` | `// B13-R13-7: …` + 不存在的节锚 `§flat_norm` | 任务流水号已删；锚改为 `docs/science/CALIBRATION.md §5 连续定义` |
| M3-D-005/M6a-D-011 | `sed -n '14p;60p;158,159p' …/cosmetic_corrector.cpp` | 「背景 label 0 的 size 设为 max_size」（全文件无此赋值）、「用8邻居双线性插值」 | 改为如实的 `labels[i]>0` 跳过机制；插值改为「名义 bilinear，实为 4 正交方向 1/dist（DISP-COS-003）」 |
| M3b-D-01 | `grep -n "修复: RMSE 系数\|FWHM 上限完全" …/sdet_api.cpp` | 命中 2 处流水/残句 | 命中 0；改为事实陈述 |
| M3b-F-03 | `grep -n "amp = " …/p1star_fixtures.hpp` | `// SNR≈20 谱段`（实际 6.7–9.2） | `// amp 40–55 → SNR 6.7–9.2`，并在文件头标注低谱段低于 SCI 冻结召回域 |
| M9-B-1 | `awk 'NR==361' …/ipv_wcs.cpp` | `// 单位: … = 1/像素^(i+j-1) (SIP 标准)` | 补两行如实区分像素域约定与 SIP 标准 `deg/像素^(i+j)` |
| M9-D-1 | `sed -n '484,487p' …/drizzle_engine.cpp` | 「gnomonic … 量级为弧度」 | 「tan(θ)，无量纲；仅 θ≪1 时数值等于 θ(rad)，相对偏差 θ²/3」 |
| V6-N-03 | `grep -n "编译期即证明" …/p1phot_fixgates.cpp` | 命中 1 处不可核声明 | 命中 0；改为只声明本 TU 实际断言的位值一致性 |
| V7-N-10 | `sed -n '355p' …/drizzle/src/module_entry.cpp` | `/* API-DRZ-001 默认 1.0 */`（该节实无此条款） | `/* 缺键默认 1.0 (契约只约束 (0,1]) */` |
| V8-N-08 | `sed -n '33,39p' …/sdet_angle_guard.h` | `(-90, 90]`（两端实为不动点） | `[-90, 90]` |
| V12-N-06 | `grep -rn "docs/algorithms/IPV_PIPELINE.md" lib/` | 6 处引用不存在的路径当作「偏差已登记」 | **命中 0**；改为如实标注「尚未在 docs/algorithms 登记」 |
| M1a-C-006 | `grep -n 'flux(A) 降序' …/ipv_select.cpp` | 头注与策略注写 `flux(A) 降序`（实现为 mag(box) 升序） | 代码侧注释已订正一致（残余文档面 `PLATESOLVE.md:115` 只读） |

### 5.3 回归证据（构建 + 测试）

| 命令 | rc | 关键输出 | 日志 |
|---|---|---|---|
| `timeout 2400 ninja -C build -k 0`（整改前基线） | **0** | 40/40 目标，0 FAILED | `run/PROJECT-GOVERNANCE-01/P1-001/logs/10_build.log` |
| `timeout 2400 ninja -C build -k 0`（整改后） | **0** | 111/111 目标，0 FAILED | `…/logs/11_build_after.log` |
| `timeout 2400 ctest --test-dir build -R 'normalize\|calibration\|cosmetic\|star\|psf\|plate\|photometry\|noise\|drizzle' --output-on-failure` | **0** | `100% tests passed, 0 tests failed out of 46`（含 p1cal / p1phot / p1star / p1wcs / drizzle / noise 全套） | `…/logs/12_ctest_after.log` |

---

## 6. 越界自查

- `git status --porcelain=v1` 过滤 `run/`、`reports/PROJECT-GOVERNANCE-01/P1-001/` 后，工作区里另有**其他并行线**在改的 `lib/algorithms/coverage/**`、`fits_output/**`、`resample/**`、`noise_snr/tests/**` 等条目（非本任务）；**属于本任务的入库改动仅 §5.1 的 16 个文件**，全部落在 P1-001 允许域（`lib/algorithms/{calibration,cosmetic,star_detection,psf,platesolve,photometry,noise_snr,drizzle}/**` 与模块测试）。核验命令：
  `git status --porcelain=v1 | grep -E "lib/algorithms/(drizzle|calibration|platesolve|star_detection|photometry)"`（本任务改动集）与 §5.1 清单逐项对齐。
- 未触碰：`ci/**`、`.github/**`、`cmake/**`、`docs/**`（含 `docs/algorithms/**` 锚与 `anchor_contract.json`）、`contracts/**`、`runtime/**`、`lib/infrastructure/**`、`tests/cli/**`、`tests/backend/**`、`tests/unit/**`。
- ARC-001 冲突规避：开工核对 `lib/algorithms` 最近改动时间（源文件最新 17:19），`docs/algorithms` 正在被 ARCH-001 改写（18:51）→ 本任务对 `docs/**` **零写入**，未抢改。
- ⚠️ **提交归属提示（需前台知悉）**：执行窗口内前台并行线于 `2026-09-16 19:03:05` 提交 `fa454b67`（提交消息为 CI-001，与 P1-001 无关），**顺带把本任务当时的 9 个文件纳入该 commit**：`drizzle/healpix_drizzle/drizzle_engine.cpp`、`drizzle_engine.h`、`spherical_overlap.cpp`、`drizzle/src/module_entry.cpp`、`calibration/src/master_generator.cpp`、`calibration/src/cosmetic_corrector.cpp`、`platesolve/cpp/ipv/src/ipv_wcs.cpp`、`platesolve/cpp/ipv/src/ipv_robust_refine.cpp`、`platesolve/cpp/ipv/include/ipv_types.h`。
  19:03 之后完成、**目前仍在工作区未提交**的 9 个文件：`platesolve/cpp/ipv/include/ipv_types.h`（V12-N-06 二次编辑）、`platesolve/cpp/ipv/src/ipv_select.cpp`、`drizzle/include/astrocs/drizzle/types.h`、`drizzle/src/module_entry.cpp`（pixfrac 二次编辑）、`star_detection/tests/p1star/p1star_fixtures.hpp`、`star_detection/src/sdet_api.cpp`、`star_detection/src/sdet_angle_guard.h`、`photometry/cpp/src/pc_api.cpp`、`photometry/tests/p1phot/p1phot_fixgates.cpp`。
  由于该 commit 消息未提及 P1-001，**provenance 会失真** —— 本摘要即为该批改动的归因凭证（提交前请前台决定是否补记）。

---

## 7. 未决项与需权威裁决清单

### 7.1 需权威裁决（阻塞，本轮不可自行改）
科学定义/冻结文档类共 **49** 条（明细见处置表 `NEED-RULING`），其中 P0 7 条（§4）。共性：
1. **SCI/ALG 文本与实现互斥**（M1a-A-001/A-002、M2a-A-1、M7-A-101/116/118/130/134/135、M1a-C-001/C-005、V2-N-03、M9-C-3）：`docs/science/**`、`docs/algorithms/**` 只读，改哪侧属科学变更流程（ENGINEERING_SPEC §3）。
2. **科学门/容差/阈值缺依据或自相矛盾**（M9-A-1、M7-A-128、M7-A-131、M7-A-202、M7-H-102 同族、M3b-H-02、V2-N-05、M3-A-007、M2a-A-1）。
3. **默认值/精度语义多源或不唯一**（V12-N-07、V12-N-13、V12-N-05、M9-C-3、M3b-H-03、M3-C-012、W2-N-12、V3-N-02）。
4. **数据对象/单位一字段多义**（M3b-A-03、M3b-A-07、W2-N-08、M9-B-1 残余、M3b-C-03、M7-A-207）。
5. **测试/Oracle 承诺无法兑现**（M3b-F-02、M3b-F-01、M2a-F 同族、W2-N-15、M7-A-206）。

### 7.2 本域后续小任务（8 条 FOLLOWUP，建议独立成卡）
`V10-N-01`（短读 fail-open）、`V10-N-02`（SIP order 读侧无上界）、`V10-N-06`（NAXIS 无上限）、`V21-N-11`（n_contrib 只写不读）、`W5-N-14`（GAIN 解析静默吞错）、`M7-H-102`（NaN 哨兵穿过拒绝门）、`M2a-H-4`（variance dtype 收窄）、`M6a-E-002`（上游 star_finder.c 悬空引用）。
共性：均为**错误传播/边界防护**类，改动会改变运行期行为，须配独立正例+负例与错误码口径，不宜混入本次注释批次。

### 7.3 需前置条件（非本轮可解）
- `M3b-F-03` 补 SNR≥10 样本会改变 `p1star` 召回门现状（可能转红）→ 需与 PSF/星点门同批处理。
- `M3b-F-01` 生产门 `centroid_p95_le_0.3px_production=false` + `gate2_psf_oracle` 零 CI 登记 → 需 PSF-001 收口并由 CI 域登记。

---

## 8. 交付物索引

| 路径 | 内容 |
|---|---|
| `run/PROJECT-GOVERNANCE-01/P1-001/自证摘要.md` | 本文件 |
| `run/PROJECT-GOVERNANCE-01/P1-001/翻译表.md` | 旧→新路径翻译表（含无法翻译项） |
| `run/PROJECT-GOVERNANCE-01/P1-001/处置表.md` | 112 行处置表（ID/类别/优先级/复检 rc/四态/处置/明细） |
| `run/PROJECT-GOVERNANCE-01/P1-001/recheck_results.json` | 112 条 × 218 子命令的 rc 与逐字输出（机器可复跑） |
| `run/PROJECT-GOVERNANCE-01/P1-001/logs/<ID>.log` | 每条证据命令的原始日志（112 份） |
| `run/PROJECT-GOVERNANCE-01/P1-001/logs/10_build.log`、`11_build_after.log`、`12_ctest_after.log` | 构建/测试证据 |
| `run/PROJECT-GOVERNANCE-01/P1-001/logs/20_after_fix_recheck.log` | 20 条整改的改前/改后逐字对照 |
| `run/PROJECT-GOVERNANCE-01/P1-001/{extract2,translate,run_recheck,gen_tables,after_check}.py` | 全流程可复跑脚本 |
| `reports/PROJECT-GOVERNANCE-01/P1-001/` | 翻译表 / 处置表 / 自证摘要 的存档副本 |
