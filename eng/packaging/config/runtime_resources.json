{
  "schema": "astrocs.runtime-resources/v1",
  "config_class": "program_global",
  "title": "运行时资源预算（调度准入；**不是**资源门）",
  "authority": "内存预算 = 当前可用内存 × memory_budget_percent/100，交给调度器作为准入输入（在途节点估算峰值之和超预算时就绪节点排队等待）；内存不设资源门（ASTROCS_DESIGN.md §3.5），故本预算不产生退出码、不阻断运行。",
  "authority_refs": [
    "ASTROCS_DESIGN.md §8.3（静态预算：估算是调度决策的输入；编排策略「内存占用永不越界」——越界时按「可中断排队 / 可丢弃重跑」处置，不是拒绝任务）",
    "ASTROCS_DESIGN.md §9（一个进程一个资源调度器与线程预算源；内存与 CPU 同源）",
    "docs/contracts/SCHEDULER_CONTRACT.md §3（内存上限（峰值工作集）由配置/资源门决定，禁止硬编码）",
    "ASTROCS_DESIGN.md §3.5 + ENGINEERING_SPEC.md §12 + eng/contracts/resource_gate_v1.json#disk_gate.no_gate（资源门只管磁盘；内存 / CPU / 线程**不设门** ⇒ 本预算是调度准入输入，不产生退出码、不阻断运行）"
  ],
  "readers": [
    "CMakeLists.txt（configure_file → runtime_resources_generated.h；实现侧零字面量）",
    "lib/infrastructure/scheduler/src/memory_budget.cpp（唯一消费点）"
  ],
  "memory_budget_percent": 95,
  "memory_budget_percent_constraint": "整数 1..100。含义：内存预算上限 = **当前可用内存 × percent / 100**。默认 95（避免卡死，不是省内存）。运行期可经机器绑定 profile（--cpu-profile 指向的 astrocs.cpu-profile/v2 的 host.memory_budget_percent）覆盖；profile 未声明或取值非法时用本文件的默认值。",
  "memory_budget_percent_source": "默认值 95 冻结于本文件；运行期可经机器绑定 profile 覆盖（见 memory_budget_percent_constraint）",
  "memory_pressure_enabled": 1,
  "memory_pressure_enabled_constraint": "0/1。0 = 关闭内存压力治理（与未启用逐字节等价：恒允许派发、零台账、零计数、零行为变化）；1 = 启用（默认）。关闭面是**受控对照实验**用的开关，不是「内存紧张时把它关掉」的逃生门。",
  "memory_pressure_high_percent": 90,
  "memory_pressure_low_percent": 75,
  "memory_pressure_high_percent_derivation": "高水位 = 预算的 90%。推导：压力 = 进程树 RSS / 预算，而「停止派发」只在**已启动的那一帧仍有落脚空间**时才安全 ⇒ 需 100 − high ≥ 100 × (单帧边际 RSS) / 预算。单帧边际 RSS 取实测内存模型 RSS(F) = 0.143 GB + F × 1.6724 GB 的边际项 1.6724 GB（run/P1-CONCURRENCY-CALIB-01，F = 在飞帧数，残差 ≤ ±2.5%）。预算 B = 0.95 × 可用内存 A，本机 A ∈ [17.6, 23.3] GB ⇒ B ∈ [16.7, 22.1] GB ⇒ 100 × 1.6724 / B ∈ [7.6, 10.0] ⇒ high ≤ [90.0, 92.4]。取区间下沿 90，使最坏情形（B = 16.7 GB）也留足一帧余量，且为整数。",
  "memory_pressure_low_percent_derivation": "低水位（恢复派发）= 预算的 75%。推导：滞回带 [75, 90) 必须容纳「一次丢弃释放的量」并留裕度。一次丢弃释放约 100 × 1.6724 / B ∈ [7.6, 10.0] 个百分点 ⇒ 压力从 90 落到 [80.0, 82.4]；取 low = 75 使一次丢弃**不足以免除**高压力，须待在飞帧再自然完成若干才恢复 ⇒ 恢复侧比触发侧更保守，避免在阈值附近反复「停派发/恢复」。",
  "memory_pressure_derivation_scope": "上述两个水位的适用域（超出必须重推，与 kP1FrameBytesPerPixel 同一条重标定纪律）：P1 帧几何 4500×3600 / 4096²、FP64、drizzle auto nside、kScratchPoolCap = 2、无 swap。帧几何、精度模式、nside 或 scratch 池上限变更 ⇒ 边际 1.6724 GB 失效 ⇒ high/low 必须重测重推。",
  "memory_pressure_high_dwell_samples": 2,
  "memory_pressure_low_dwell_samples": 2,
  "memory_pressure_high_dwell_samples_constraint": "≥ 1。连续 N 次采样压力 ≥ high_percent 才置高压力态（去抖：单次尖峰不触发）。",
  "memory_pressure_low_dwell_samples_constraint": "≥ 1。连续 N 次采样压力 ≤ low_percent 才解除高压力态。",
  "memory_pressure_evict_after_samples": 3,
  "memory_pressure_evict_after_samples_constraint": "≥ 1。高压力态**已连续持续** N 次采样后才允许丢弃进度最低的在飞帧（ASTROCS_DESIGN.md §8.3:615「可丢弃重跑」的触发门槛）：先停派发（§8.3:614「可中断排队」），仍不缓解才丢弃（§8.3:615）。取 3 ⇒ 在 1000 ms 采样间隔下 = 约 3 s 的持续高压，足以排除采样尖峰。",
  "memory_pressure_sample_interval_ms": 1000,
  "memory_pressure_sample_interval_ms_constraint": "≥ 0，毫秒。压力采样节流间隔（0 = 每次都采样；生产不可为 0，否则派发热点会以每次派发一次的频率读 /proc）。取值依据：单次采样 = 读 /proc/self/status + 一次 /proc 枚举，实测开销在毫秒量级，1 s 间隔下开销占比 < 0.1%，而 1 s 远小于单帧处理时长（秒级）⇒ 不会漏掉一次帧派发窗口。",
  "memory_pressure_ledger": "run 级 JSONL 台账（每行一个事件对象），由 run 的 output_dir 派生路径；事件含 pressure_high / pressure_low / evict_lowest_progress / rss_unavailable / governance_disabled / governance_unavailable / dispatch_escape_no_inflight。落盘经 aio 唯一追加写原语。台账只增不改，不参与任何判据（ASTROCS_DESIGN.md §3.5：内存不设门）。",
  "available_memory_probe": "lib/infrastructure/aio/src/aio_sysinfo.cpp#aio_system_available_memory_bytes（aio 是文件级唯一 I/O 边界，ASTROCS_DESIGN.md §10）。Linux 口径 = /proc/meminfo 的 MemAvailable（含可回收 page cache，**不是** MemFree）∩ cgroup 内存余量（v2 memory.max - memory.current；v1 memory.limit_in_bytes - memory.usage_in_bytes）；Windows 口径 = GlobalMemoryStatusEx.ullAvailPhys。返回 0 = 不可判定（fail-closed：不启用回压，如实登记，不冒充有预算）。",
  "enforcement": {
    "in_process": "调度准入（Scheduler 内存回压：在途节点估算峰值之和 > 预算 ⇒ 就绪节点排队等待，不拒绝、不改退出码）",
    "not_a_gate": true,
    "cli_exit_code": null
  },
  "x-astrocs-writer": "用户（可调）；默认值 95 冻结于本文件",
  "x-astrocs-not-writable-by": [
    "benchmark"
  ]
}
