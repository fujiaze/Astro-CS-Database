# run/ 轮次保留清单（eng/tools/run_gc.py 读取；与本文件同目录，路径单源）
RELEASE-*
AUTONOMOUS-*
RUN-PROVENANCE-*
CONTRACT-*
STARDET-*
DS-*
WEIGHTMODE-*
NOISE-*
PASSBAND-*
PHOT-*
VAR-*
HIPS-*
P1-*
#
# 每行一个 fnmatch glob，匹配 run/ 的**直接子项名**；# 开头为注释。
# 登记规则：现行轮次的验收证据在此登记；未登记的轮次可被 run_gc.py 回收。
# 新增轮次时：把 <轮次ID> 加进来（或用 eng/tools/round_start.sh <轮次ID> 自动保留）。
# 本文件缺失或解析为空 ⇒ run_gc.py 以 ANCHOR_STALE + exit 2 拒绝回收（fail-closed）。
RELEASE-04
RELEASE-05
DOC-*
SCI-*
CLEAN-*
FIX-*
PERF-*
BLD-*
E2E-*
VIS-*
ROOT-*
.gitkeep

# ── 轮次 ID 前缀陷阱（登记规则补充）──────────────────────────────────────────
# 本清单按 **fnmatch 前缀族** 匹配，因此"名字里含族名"**不会**命中：
#   M42-E2E-01 不匹配 E2E-*（它以 M42- 开头）⇒ 会被当成未命中轮次整体回收。
# 新增轮次时不要想当然，登记前先跑 dry-run（eng/tools/run_gc.py，不加 --apply）
# 确认目标轮次打印的是 KEEP 而不是 WOULD-DELETE。
M42-*
DRZ-*
AUDIT-*
CHAIN-*
VARIANCE-*
SPARSE-*
EXPORT-*
WCS-*
PATHDOMAIN-*
GATE-*
PHOTOCURVE-*
