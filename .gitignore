# 第三方源码与数据库（参考/适配用，不入仓库）
siril-1.4.3/
gaia/

# BASS DR3 单帧归档索引（2026-09-21 ROOT-CONSOLIDATION：由根目录 `BASS DR3/` 移入 `testdata/BASS_DR3/`，
# 目录名去掉空格）。依据 ENGINEERING_SPEC §7「外部只读数据集……一旦被代码消费或需入库，移入 testdata/」：
# 元数据/索引/工具/日期表**入库**（GitHub 可见），只有 FITS 与下载/再生成产物不入库。
# 具体规则见下方「testdata 段」（testdata/BASS_DR3/）。

# IDE 配置
.trae/
.vscode/
.idea/

# 日志与编译产物
logs/
run/*
!run/.gitkeep
output/
build/
Release/
Debug/
*.o
*.obj
*.dll
*.exe
*.lib
*.a
*.so
*.pdb
*.ipdb
*.iobj
*.exp
*.ilk

# 归档目录（GOV-002 起归档文件须受 Git 跟踪）
archive/
!docs/archive/

# 测试数据与图像 (testdata 目录内容默认忽略；仅索引与具名登记的元数据入仓库)
testdata/*
!testdata/index.json
!testdata/README.md
# BASS DR3 元数据面入库（2026-09-21 ROOT-CONSOLIDATION）：先放行目录，再逐类排除 FITS/下载产物。
# gitignore 语义：父目录被排除时无法再放行子文件，故 `!testdata/BASS_DR3/` 必须在 `testdata/*` 之后。
!testdata/BASS_DR3/
!testdata/BASS_DR3/**
# 仅 FITS（fpack .fits.fz / 标准 .fits）与下载、可再生成产物不入库（全局 *.fits 之外的兜底）
testdata/BASS_DR3/**/*.fits
testdata/BASS_DR3/**/*.fits.gz
testdata/BASS_DR3/**/*.fits.fz
testdata/BASS_DR3/**/*.fz
testdata/BASS_DR3/index.csv
testdata/BASS_DR3/coords.csv
testdata/BASS_DR3/data/
testdata/BASS_DR3/downloads/
testdata/BASS_DR3/logs/
test_data/
test_output/
*.png
*.jpg
*.jpeg
*.bmp
*.tif
*.tiff
*.fits
*.fit
*.fts
*.xisf
*.xpsd
*.hiss
*.hcsd
*.npy
*.npz

# 临时文件
*.tmp
*.bak
*.swp
*~

# Python 缓存与构建
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg
# dist/ 目录默认排除, 但发布包文本文件 (VERSION/SHA256SUMS/README/verify.bat/config) 例外
# 二进制文件 (*.exe/*.dll) 不提交, 只通过 SHA256SUMS.txt 记录 hash
dist/*
!dist/AstroCS-CLI-v1/
dist/AstroCS-CLI-v1/*
!dist/AstroCS-CLI-v1/VERSION.txt
!dist/AstroCS-CLI-v1/SHA256SUMS.txt
!dist/AstroCS-CLI-v1/README.txt
!dist/AstroCS-CLI-v1/verify.bat
!dist/AstroCS-CLI-v1/config/
!dist/AstroCS-CLI-v1/config/**
.pytest_cache/
.ninja_log
meson-private/

# 导出包与上下文压缩包
AstroCS_Database_Context.zip
*_Context.zip
repo.bundle

# 编译输出日志
compile_out.txt
compile_out*.txt
link_err.txt

# 自治 Agent 临时解压目录与压缩包
.astrocs_agent_bootstrap/
AstroCS_Autonomous_Agent_Pack*.zip
AstroCS_Next_Phase_Development_Pack*.zip
AstroCS_CLI_Core_Development_Pack*.zip
_new_pack_v*/

# v1.1 审计包
audit/*.rar

# 审核包（放在项目根目录，本地保留，不 commit）
*_review_bundle.zip

# 根目录整洁约束 (2026-09-09 整理): 运行产物/测试残留/资料克隆一律不入库
astrocs_run_*.json
/Testing/
/out/
/AstroCS.wiki/
resource_timeseries.csv
resource_summary.json
worker_balance.csv
# FIX-E2E B1-A8: MON-002 allocation report 亦属运行产物(此前漏登记 → CI dirty);
# 运行产物应只落 output_dir, 此处仅作根目录整洁兜底(与 resource_* 同策略)。
alloc_report.json
alloc_samples.csv
/graph/

/dist/audit/

# 根目录整洁兜底 (2026-09-16 ROOT-002 / CHK-ROOT-CLEAN): 本地保留但不入库
# 保留理由与清理条件见 工程控制/PROJECT-GOVERNANCE-01/ROOT_LEDGER.md §9；
# 机器门见 eng/tools/quality/check_root_cleanliness.py + eng/ci/root_manifest.json。
# 注意：机器门不使用 .gitignore 判定，物理根目录出现未登记条目仍判红。
/p*-files.patch
/run_context.json
/astrocs_p1sess_*/
/CS/
/Database/
/worktrees/

# 安全（ROOT-006 / CHK-SECRET-HYGIENE）: 凭据/接入类文档不入库——精确条目，不加宽通配
# 注意：忽略规则对**已 tracked** 文件无效；本条目只是「误加回」护栏，不解决存量。
# 存量处置 = 审计包白名单移除 + 排除保证（eng/tools/pack_audit_package.py）；
# 是否 git rm --cached 见 reports/PROJECT-GOVERNANCE-01/security/EXPOSURE_NOTE.md（负责人裁决）。
FATDUCK_ACCESS.md

# 运行/CI 产物（ROOT-007 裁决：artifacts/ 不归档不保留；CI 产物不入库）
/artifacts/ci/
/artifacts/**/*.zip

# 论文工作区：仅本地留痕，不上传（负责人 2026-09-19 令）
run/RELEASE-02/paper/

# 外部只读数据集（RELEASE-02：HST WFC3/UVIS M16 三帧 drz，公开 HLSP）
# 2026-09-21 ROOT-CONSOLIDATION：由根目录 `HST_M16/` 移入 `testdata/HST_M16/`（ENGINEERING_SPEC §7）。
# 770 MB FITS 仍不入库（全局 `*.fits` + 本条目兜底）；来源/PHOTFLAM/下载方式见 testdata/README.md。
testdata/HST_M16/

# 本机路径覆盖（用户手动配置绝对路径，不入库）
eng/packaging/config/local.paths.json
