# 接缝门 1e-2 的确定性下限 1.0050% —— 推导与实验闭合

**性质**：支撑推导（[推导] 腿），供 REPORT_paper.md §2.3 与 REPORT_experiment.md 引用。
判据与适用域的权威出处：`docs/science/PHASE2_UPM.md` §9a/§17；实验腿：
`code/audit_rework/route1/c3_seam_gate.py`（固化读数 `results/audit_rework/route1/c3_seam_gate.json`）。

## 1. 推导

接缝门的判定统计量 `rel_step` 取**线性差分比**口径（正本定义 `docs/science/PHASE2_UPM.md` §9a：`rel_step(e) = median(img[+d] − img[−d]) / bg(e)`；§17 同）：

```
rel_step = Δ / (L + Δ/2)      （Δ = 左右 patch 背景电平台阶，L = 电平）
```

门 `max|rel_step| ≤ 1e-2` 对应物理电平台阶的**确定性下限**（§17.3 线性硬界）：

```
rel_step = Δ/(L+Δ/2) = 1e-2  ⇒  Δ/L = gate/(1 − gate/2) = 0.0100503…（= 1.0050%）
```

即：无论统计涨落如何，只要线性台阶 **Δ/L ≤ 1.0050% 判绿是确定性的**（非概率行为）。
这 0.5 个千分点的超出量（1.0050% vs 名义 1%）来自 rel_step 分母的 (L+Δ/2) 口径，
不是判据的松弛。

<!-- 订正: 检查-行文逻辑 R2（同 检查-跨文档冲突 黄10）——原作「rel_step 取对数口径的跨 patch
背景电平比（PHASE2_UPM §17）：rel_step = ln(B_R/B_L)，下限 |Δ|/L ≤ e^{1e-2}−1 = 0.0100503」，
把对数口径挂在正本 §17 名下；正本 §9a/§17 均为线性差分比，下限推导为 §17.3 的
gate/(1−gate/2) 形式。数值巧合说明：e^{1e-2}−1 = 0.01005017 与 g/(1−g/2) = 0.01005025
在报告精度 1.0050% 内一致，故下限数值结论不受影响，仅口径归属更正。旧对照：
rel_step = ln(B_R / B_L)；|Δ|/L = |e^{rel_step} − 1| ≤ e^{1e-2} − 1 = 0.0100503… -->

## 2. 实验闭合 [实验:route1/c3_seam_gate.py，seed=20260319]

| 检验 | 读数 | 与推导的关系 |
|---|---|---|
| 闭式下限 | 0.0100503 | 与 gate/(1−gate/2) 逐位一致 |
| Δ/L = 1.0050% | 恰在门上（0.01000005 判红） | 确定性边界落位；该读数 0.01000005 本身是**线性口径** rel_step = Δ/(L+Δ/2) 的复现（若真为对数口径 ln(1+0.010050)=0.0099995<0.01 应判绿）——数值地板 1.0050% 因 e^g−1 与 g/(1−g/2) 在 g=1e-2 数值巧合一致，结论不受影响，口径归属更正（见 §1 订正注） |
| Δ/L = 1.0049% | 判绿 | 边界下侧确定判绿 |
| H0 散布 | σ_meas = 1.151e-3 vs 判据量散布公式 1.187e-3（比值 0.970）；门处虚警 0/4000 | 统计行为不越过确定性下限 |
| 多重性检出 @Δ/L=1.05% | n_s=1: 0.646/0.652；n_s=2: 0.875/0.870；n_s=4: 0.984/0.983（解析 1−∏Φ / MC） | 检出曲线在地板之上 |
| 平滑过渡 | w=8/16/32 px ⇒ 观察台阶 ×0.50/0.25/0.125（=2d/w）；2% 名义台阶判绿 | **结构性漏检面**：门量的是 patch 级 rel_step，光滑过渡在 patch 尺度上被抹平 |
| 反号梯度相消 | 注入 1.005% + 反号梯度（2d·g=Δ）⇒ rel_step 读 0.06% 判绿；无梯度对照 1.09% 判红 | **结构性漏检面**（01 D-57：漏检面抬升 ≈1.73%） |
| 负例 | 无台阶：median rel_step = 0，4000 次 MC 全绿 | 判据非恒真（真值无效应⇒度量归零） |

## 3. 适用域声明

门是「**确定性地板 + 统计检出**」的复合判据：Δ/L = 1.0050% 只在梯度项为零时必红。
两个结构性漏检面（平滑过渡 w/2d 抬升、反号梯度代数相消）必须随门一并陈述
（PHASE2_UPM §17.3 适用域声明成立）。另按台账 A-P5-05 限定：
「f<1/2 中位数结构性失明」仅在**无噪极限**成立；M42 级噪声下 f=0.3、Δ/L=5%
读数已达 1.03e-2（到门）。

## 4. rel_step_max=0.1 已除名（按台账 A-P5-10 / 补实验 C2）

判据面上**仅存**的模型级台阶红位是本门 max|rel_step| ≤ 1e-2 与 Huber δ=1.345。
原 05_正向规格 判据01 的 rel_step_max=0.1（相邻 control 平滑性诊断阈值）经补实验
`code/audit_rework/supp_507_relstep/eb_relstep_calibration.py` 证明：非 IRLS 步长上限
（仓库无此参数）、不可标定（B0=5 假红 45–60%、B0=1000 永不红）、对局部缺陷结构性失明
（单节点缺陷 ≤4/112 邻对，×10 注入配方实测不红）、灵敏度比本门松约 20×——
从判据面删除；如需保留降级为无阈值分布报告量（逐对 rel_step 落盘，不判红）。

## 5. 精度约定

- 接缝门以**左/右 patch** 的 rel_step 计；方差定义必须写明——逐侧方差对阶跃失明成立，
  合并窗口方差以 Δ²/4 泄漏（VR_pool = 1.25/2.01/5.03 @ Δ/σ = 1/2/4，A-P5-04）。
- 检出率区间口径挂 Clopper & Pearson 1934（DOI 10.1093/biomet/26.4.404）[文献]。
- 本推导的 fixture 未含双线性亚像素方差因子与足迹多边形几何（正本 §17.4 实测
  max|rel_step| 相对差 4.5e-3 属该二阶效应）。
