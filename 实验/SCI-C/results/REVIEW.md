# SCI-C 独立对抗审稿记录

审稿对象：`实验/SCI-C/`（报告 `README.md`、代码 `code/*.py|cpp|sh`、结果 `results/*.json`、图 `results/figs/*.png`）
对照：任务 `/tmp/astrocs_pkg/extracted/AstroCS工程包_RELEASE-04/tasks/SCI-403.md`（只读）、上游 `实验/SCI-B/results/*.json`
审稿方式：全文精读 + 逐门复核 JSON `gates.rows` + 用当前代码**只写 /tmp** 重跑 C1（A4/A10）、C3（B7）关键路径 + 解析复算预测子/统计量 + Crossref 核验文献元数据 + git 只读检查
审稿人立场：对抗性。凡我攻不动的结论，在"已核实为站得住"一节明确写出。

---

## 审稿轮次 1

### 致命问题（必须修）

**1. README §4.1 的 A10 表格与归档结果 `results/c1_additive.json` 完全不符，且归档数据不支持"随基外 RMS 线性/单调增长"的表述。**
- 位置：`实验/SCI-C/README.md:131-134`（表格）与 `README.md:124-125,17-18,240-241`（结论）；数据源 `实验/SCI-C/results/c1_additive.json` → `out_of_basis_sweep`。
- 问题：A10 是本单元自称的"主要发现/可证伪边界"（§0.2、§5.1"失效（C1 A10）"），但报告里那张 6 点表**不是**归档结果里的数。
- 证据（逐点对照，我原样打印 JSON）：

  | 波长 [px] | 1600 | 800 | 400 | 200 | 100 | 50 |
  |---|---|---|---|---|---|---|
  | README 基外 RMS | 0.04 | 0.12 | 0.40 | 1.30 | 4.24 | 8.20 |
  | **JSON oob_rms** | **0.3125** | **1.3474** | **3.0702** | **5.4911** | **6.5145** | **6.6434** |
  | README 残余接缝 max | 0.90 | 1.07 | 1.24 | 1.96 | 3.30 | 4.56 |
  | **JSON seam_max** | **0.8917** | **1.8364** | **1.5246** | **4.6750** | **7.1595** | **4.5228** |

  只有首末两点近似（0.90/0.89、4.56/4.52）；其余 4 点全部不符，且**归档序列非单调**：800 px 的接缝（1.836）> 400 px（1.525），100 px（7.160）> 50 px（4.523），而 400 px 的基外 RMS 是 800 px 的 2.3 倍。`seam_med` 更乱（0.330/0.624/0.244/2.431/1.169/2.808）。
- 我做的复算：用当前 `code/sci_c_common.py + c1_additive.py` 的原始 seed 重跑整段扫描（只写 /tmp），得到 **与 JSON 逐位相同**的 6 组 (oob_rms, seam_max) 和相同的 slope=0.797588 / pearson=0.896402 / ratio=5.0721。即：**JSON 可复现，README 的表格不可复现**；摘要统计（slope 0.798、Pearson 0.896、×5.07）抄的是 JSON，正文表格抄的是更早一版（很可能是预测子 σ 更小的一版：README 的 RMS 序列到 50 px 仍在增长，而 σ=64 px 高通在 λ≪64 px 时应当饱和，JSON 的 6.51→6.64 正是饱和形态）。
- 影响：A10 门（`c1_additive.py:321-324`，Pearson>0.85）虽然 PASS，但 README 用一张不存在的表去论证"线性增长"，属于**证据与结论脱节**。另外 JSON 序列下 Spearman 仅 0.771（门里算了这个值却没设阈值），与"单调增长"（`c1_additive.py:19` 的原始判据文字）直接冲突。
- 建议：删掉/重算该表，直接以 JSON 六点作图；把结论改写为"存在整体正趋势（Pearson 0.896，斜率 0.798±0.197，95% CI [0.25,1.35]，R²=0.804），但单点关系非单调、单实现无误差棒，不足以称线性标度律"；若要称标度律，每个波长至少 5–10 次独立实现并给误差棒。

**2. C2 的 M6 判据被替换：文档判据实测不合格，实现改用更弱判据后 PASS，且 README 未披露。**
- 位置：文档判据 `code/c2_multiplicative.py:22`（"M6 残余接缝随乘性残余幅度线性增长（Spearman>0.8, slope>0）"）；实现 `code/c2_multiplicative.py:410-413`（"max|Δseam| < 50% 基线"）；结果 `results/c2_multiplicative.json`。
- 证据：JSON 里 `multiplicative_prediction.spearman = 0.09999999999999999`（p≈0.87），即**文档判据会 FAIL**；seam_max 序列 [18.052, 19.431, 16.358, 15.835, 23.140] 对 hf_amp [0,0.002,0.005,0.01,0.02] 完全非单调。实现的 M6 用同一批数据算 max|Δseam|=5.088 < 0.5×18.052=9.026 ⇒ PASS。README §4.2/§9 只报后者的数字，只字未提 Spearman=0.1。
- 附加问题：该扫描每个 hf_amp 用**不同 seed**（`"c2_sweep_%g" % hf`，第 374 行），而 seam 度量在 none 臂的 6 个边界上跨度为 −47.7…+41.4 e-（同文件 JSON `seam_arms.none.steps`）。因此 dseam 由**实现间散布**主导，即便不换判据也无法把 5.09 e- 归因给 24 px 高频分量。
- 建议：恢复文档判据并把 FAIL 如实登记；若要主张"贡献有界"，改用同一 world 只变 hf_amp（同 seed 同噪声）或多次实现取均值±误差；两者都做不到就写"未能归因"。

**3. C2 的 M5（PSD 空间频率定位）存在单位错位，门是"假通过"。**
- 位置：`code/c2_multiplicative.py:344-369`；结果 `results/c2_multiplicative.json.hf_component`。
- 问题：`radial_psd` 作用在**分块后**的 128×128 数组（BIN=4，每个样本=4 原始 px），其 k 轴单位是"每 128 个分块样本"；注入周期 24 px = 6 个分块样本 ⇒ 预期峰应为 **k≈21.33**。代码却写 `kexp = nb/24.0 = 5.333`（把分块数组当成原始像素），再在 `P[1:40]` 里找峰得 k=4。
- 证据（我重跑 M5 段，无需探针）：分块数组 (128,128)；`nb/(24/BIN)=21.33`；代码 `kexp=5.333`；argmax(k∈[1,39])=4；PSD(k=4)=3.000，PSD(k=21.3)=2.273，k∈[18,25] 的最大值=2.273，全谱最大在 k=82(3.098)。JSON 自己写了 `psd_period_peak_px = 128.0`——**峰对应 128 px，而不是注入的 24 px**。同时 `resid_hf_rms=0.0297` 是注入分量 RMS（`injected_rms=0.00612`）的 4.8 倍，说明分块残差并非由该分量主导。
- 影响：README §4.2/§0.3"被分块 PSD 定位在预期空间频率（k=4 vs 预期 5.33，±2 bin）"不成立；任务步骤 2 要求的"量化空间频率"未真正完成。
- 建议：修 `kexp = nb/(period/BIN)`，把搜索窗放到正确频段（k≈21），并给出扣除白噪声本底后的对比度；若峰仍不在 21.3，如实登记"未能定位"。

**4. C4 的"退化判据"N1 是恒真反证；README 由它推出的"排除自身参考面必要"不成立。**
- 位置：`code/sci_c_common.py:413-431`（`degenerate_steps`：`c = frames[nm] - sky_truth[nm]`）、`code/c4_seam_criterion.py:51-53,99-100`；README §0.4（`README.md:23-25`）、§4.4、§5.2（`README.md:250`）。
- 问题：退化臂减掉的是**逐帧天光真值**，而注入接缝同时被加进了 `w["sky"]["B"]`（`c4_seam_criterion.py:38-39`）⇒ 注入项被代数精确抵消，响应必然为 0。N1 的"0.09σ"只是噪声（`deg` null std=0.601）。这不是"判据没有鉴别力"的实证，而是"把真值减掉以后没有信号"的同义反复。
- 更关键的是推论：§5.2 用 N1/N2 判定"**排除自身的参考面** 必要"，但 N1 的对造与"leave-one-out 参考面"无关（对照臂是"减去整张真值天光"，不是"用自身参考面"）。任务 SCI-403 §4 确实要求构造"背景皆零 ⇒ 差值天然为零"的反证，所以构造本身合规；**不合规的是把它当作"保留 B_ref / 排除自身参考面"必要性的证据**。真正的"保留公共面"理由（光度零点）由 C3 B3/B4 支持（299.17 vs 0.711 e-），与 N1/N2 是两回事。
- 建议：N1 明确标注"恒真反证（constructive tautology）"，不计入"非退化判据"的正面证据；§5.2 的"排除自身参考面"改引 C3 的产品中位数或另设真正的 leave-one-out 对照臂。

**5. C4 的检测器是单边的，"5σ 检测限 6.98 e-"只对一个符号成立，README 未声明。**
- 位置：`code/c4_seam_criterion.py:81-91,110-125`；README §4.4、§0.4。
- 问题：阈值取 `thr = mu + 5σ`，而 null 均值 mu=−5.012 e- 本身就是一个**真实存在的未校正接缝**（与 C1 A4 未校正 x=256 的 −4.64 e- 同源）。判据 `nondeg(w) > thr` 只对"正方向"的接缝灵敏。
- 证据（我实测，每点 6 次实现）：amp=+50 → 17.61（翻红）；amp=−50 → **−28.47（不翻红）**；amp=−100 → **−51.12（不翻红）**。即任意大的反向接缝都检不出。另：null 不是"无接缝世界"（mu=−5.01，8σ 偏离 0），"样本外假阳性 0/60"实际含义是"不把既有的 5 e- 未校正接缝判红"，与 README §4.4"真值无效应时假阳性为 0"的表述不是一回事（真正无效应的是 N5 的公共梯度臂）。
- 建议：明确写出"检测限为单边（对与既有偏置同号的反向接缝无任何功效）"；或改用双边阈值（|metric−mu|>5σ）并重报 FP/检出率/检测限；同时说明检测器标定在**未校正**产品上，而生产使用场景是校正后产品。

**6. 系统性判据放松（把文档判据换成更松的实现判据），其中至少 5 处未在 README 披露。**
- 位置与证据（文档判据 → 实现判据 → 实测值 → 若按文档判据的结论）：

  | 门 | 代码文档判据（原判据） | 实现判据 | 实测 | 按原判据 |
  |---|---|---|---|---|
  | C1 A10 | `c1_additive.py:19` Pearson > 0.9（且"单调增长"） | `c1_additive.py:321-324` Pearson > 0.85 | 0.8964，Spearman 0.771 | **FAIL** |
  | C1 A2 | `c1_additive.py:11` 未校正斜率 ∈ [0.8,1.2] | `:218-220` 实测/预言比值 ∈ [0.85,1.15] 且预言斜率>0.3 | slope_uncorr=**0.7681**，比值 0.9747 | **FAIL**（0.768<0.8） |
  | C1 A7 | `c1_additive.py:16` α=1 不收敛 / α=0.5 收敛 | 未实现，改成 A7b"生产容差不收敛" | α=1 收敛（4 轮） | **FAIL**（README §5.2 已披露） |
  | C1 A8 | `c1_additive.py:17` 异源权重使子集不变性退化 ≥10× | `:268-270` 异源与同源偏差 |Δ|<20% | 0.023619 vs 0.023174（退化 1.9%） | **FAIL**（README §6.1 已披露） |
  | C2 M2 | `c2_multiplicative.py:18` max|g−1| < 5e-3 | `:306-310` < 0.15 | 0.0376 | **FAIL**（README 未提 30× 放松） |
  | C2 M3 | `c2_multiplicative.py:19` 相对误差 < 2% | `:311-314` < 5% | 0.0295 | **FAIL**（未披露） |
  | C2 M4 | `c2_multiplicative.py:20` 未做 Phase1 臂 > 5× | `:330-334` > 3.0 | 4.334 | **FAIL**（未披露） |
  | C2 M6 | `c2_multiplicative.py:22` Spearman>0.8 | `:410-413` 有界判据 | 0.10 | **FAIL**（未披露，见致命问题 2） |
  | C4 N4 | `c4_seam_criterion.py:15` A* ∈ [1,5] e- | `:124-125` ∈ [0.5,8] | 6.975 | **FAIL**（未披露） |
  | C4 N3 | `c4_seam_criterion.py:13` A=3×A* 时检出率≥95% | `:122-123` 用 A=10 e-（=1.43×A*） | 0.975 | 3×A*≈20.9 e-，由 JSON 响应外推约 1.0，结果可能仍 PASS（次要） |
  | C5 W1 | `c5_weights.py:10` 相对**次优**臂 ≥10% | `:155-159` 只要求对 SNR² ≥20%，对 uniform 无要求 | vs uniform 仅 8.45% | **FAIL**（README §6.2 已披露） |
- 判断：C1 A7/A8、C5 W1 的放松有**实质披露**（§5.2/§6.1/§6.2 说明要素不可检验/不可分辨），属"如实降级"，可接受；但 **A10、M2、M3、M4、M6、N4 六处只改了阈值/判据、没有在报告里说明原判据及其不合格事实**，且这些门都计入"x/x 全 PASS"。这是本单元最系统性的方法论缺陷。
- 建议：在每份 JSON 的 `gates.rows` 里加 `doc_criterion` 字段记录原始判据，README 增加一张"判据变更/放松登记表"，写明"原判据—实测—为何改—改后是否 PASS"。

---

### 重要问题（应修）

**7. C7 R1 的"加性偏移占比"是不可辨识量，报告把它当作实测结果。**
- 位置：`code/c7_realdata.py:104-114,157-165`（`offset_frac = intercept / level_j`）；README §0.7、§4.7、§6.6。
- 证据：6 对的 `bin_range` 只有 7–11 ADU 的杠杆臂，而截距为 +97.7/+166.2/−129.9/+137.4/−168.0/−325.9 ADU；`corr(slope, intercept) = −0.9982`；每对都满足 `a·level_j + c ≈ level_i`（偏差 ≤0.42 ADU），即回归线**锚在操作点**，截距纯属向 x=0 外推。良态量是 `level_ratio`：中位 0.9805，范围 0.9386–1.0503（±5%）。
- 影响：§6.6 只提示了斜率的不确定度，未提示加性偏移不可测；"加性偏移占比中位 −0.0139"（±0.25 量级的散布）不应作为结论数字。README §4.7 的"斜率 0.995"也因此被高估为"乘性失配 ≤±30%"，而良态读数其实是 ±5%。
- 建议：改报 `level_ratio`（含散布），删掉或标注 offset_frac 为"外推量，无物理含义"。

**8. 报告数字与被报告自己规定"必须使用"的度量不一致（step vs excess）。**
- 位置：README §2.3（`README.md:91`："HST/真实数据上必须看 `excess`"）vs §4.1 A4/A10、§4.3 B7、§4.7 R3/R5 报的都是 `step`。
- 证据（我重跑，只写 /tmp；三次均逐位复现归档 JSON 的 step 值）：
  - C1 A4：step 4.8023 → 0.3830（12.5×）；**excess 5.1082 → 0.3550（14.4×）**
  - C3 B7：step 5.1883 → 0.3862（13.4×）；**excess 5.4905 → 0.4969（11.0×）**
  - C7 R3：step 20.8858 → 0.4599（45.4×）；**excess 21.8358 → 0.5828（37.5×）**（可由归档 JSON 直接读出，无需重跑）
- 判断：**结论不倒**（三个世界在 excess 下仍分别压缩 14.4×/11.0×/37.5×），但报告正文的 45×、13.4× 用的是自己判定"会误判真结构"的度量，属自相矛盾；且 C1 的归档 JSON 连 `excess` 字段都没有（见问题 9）。
- 建议：统一改报 excess，或双栏并列。

**9. C1/C6 的归档 JSON 是旧版公共库产出，归档链不自洽。**
- 证据：`results/c1_additive.json` mtime 14:36:47、`c6_sparse_dense.json` 14:36:40，而 `code/sci_c_common.py` mtime 14:39:49（更晚）；c1 JSON 的 `seam_native[*]` 行**没有 excess 键**（只有 x/step/rel/nvalid/level），而当前 `seam_steps` 必产出 excess ⇒ c1/c6 结果产自加 excess 之前的版本。
- 我重跑 C1（A4+A10 全段）与 C3（B7 全段）得到与归档 JSON **逐位相同**的数值与相同 `model_hash`（C1 `7aae7212…`、C3 `607f93da…`），说明当前代码对这些路径没有改变结论；但"归档结果 = 当前代码产出"这一命题不成立。
- 建议：用最终代码重跑 c1/c6 并覆盖 JSON（顺带补上 excess 字段）。

**10. C5 的 W5"stalled"臂其实没有 stall。**
- 位置：`code/c5_weights.py:220-234`。
- 证据：`production_convergence.iters = [4, 1, 300]`——第三臂（宣称 stalled）跑满 300 次迭代，是 max_iterations 情形，不是"目标函数停滞而步长仍大"的真 stall。因此"生产 out_converged 无法区分 max_iter 与 stalled"这条**实验证据不成立**（代码事实仍成立：`lib/algorithms/coverage/src/upm.cpp:1527-1536` 的注释明确只有 0/1，全仓无 converged=2/3 分支，FIX-2 作为代码事实可站住）。
- 建议：W5 改成"代码审查 + 构造真 stall（例如固定点数值停滞）"两类证据分开写；或直接降级为 meta/代码事实。

**11. C5 的"伪影漏入"命名与量级需要重新解释。**
- 位置：`code/c5_weights.py:130-135`；README §0.5/§4.5。
- 分析：污染伪影是 `25·(x≥256)`，而污染帧 D 的覆盖只有 gx6-7（x≥384）⇒ 伪影在 D 的**全部采样点内是常数**，可被 D 自己的 δ_k（frame_gradient_order=1 的平面）完全吸收。所以被度量的不是"伪影漏进干净帧"，而是"污染帧的 5× 读出噪声（以及 SNR² 权重被 +25 抬高后对污染帧的过加权）对公共面的扰动"。实测量级 0.0245/0.0651/0.3203 e- 只有伪影的 0.1%–1.3%。
- 公平性判断：三臂用同一批样本、同一真值、同一 seed 派生、同一差分构造，**度量本身对三臂是公平的**；且用 JSON 的 mean/std/N 反推，control_ivar 对 uniform 的分离约 5.5σ、对 SNR² 约 6.2σ（leak std 0.0182/0.0272/0.2279，N=20），排名统计上站得住。但"伪影漏入"这个名字把二阶噪声效应说成了一阶偏差，且 W2 没有任何阈值/误差棒（只比较中位大小）。
- 建议：改名"污染帧对公共面的扰动"，补误差棒与显著性；同时说明伪影在覆盖区内可被自身 δ_k 吸收这一前提。

**12. 证据分级在机器可读结果里与报告不一致。**
- 位置：README §2.4（`README.md:97-98`）声明 B2（代数恒等式）与 W4 为 `meta`；但 `results/c3_public_plane.json` 的 `gates.rows` 里 **B2 的 `level` 是 `data`**（`c3_public_plane.py:115` 调用 `g.add` 时没传 level，`sci_c_common.py:472` 默认 `level="data"`）。同类：A6/A8b/A9 在 README §6.1 被承认为"精确拟合域内平凡/不可检验"，但机器证据仍是 `data`。
- 建议：给这些门补 `level="meta"` 或 `"boundary"`，并在 `gates.summary` 里分别计数（现在 `n_pass=11/11` 把恒真门、缺陷登记门、边界门与能力门混在一起）。

**13. 两个恒真门被当作验收门计数。**
- C7 R1（`c7_realdata.py:170-174`）：`ok = np.all(isfinite(slopes)) and np.all(isfinite(offs))`——任何能跑出有限数的数据都 PASS，与"失配被量化（含不确定度）"的文档判据无关。
- C7 R4（`c7_realdata.py:276-277`）：`ok = Path(fig).exists()`，而图是同一函数在上一行刚 `savefig` 的 ⇒ 恒真。
- 同类（恒真/构造性对照）：C1 A5 与 C3 B4（"全减臂中位≈0"，减去真值后必然≈0）、C3 B2（b_k−δ_k 与帧无关，是生产定义式）、C3 B6（gauge 只差常数是 gauge 的定义；且 `span` 只算帧内 ptp，任何"逐帧常数"都能过）、C4 N1、C6 E4（逐点求值的子集一致性近乎构造性）。这些门本身有"负例登记"价值，但计入"x/x PASS"会误导。
- 建议：`gates.summary` 增加 `n_informative`（排除恒真/缺陷登记/边界门），README 的"11/11 PASS"改为分类计数。

**14. §8.1 文献表 4/9 行的卷页与本单位自己的 evidence_lit.json（及 Crossref）不符。**
- 位置：`README.md:312-320`。
- 证据（Crossref 实时核验）：
  - `README.md:313` 写 "Gruen+2014, MNRAS 442, 1507"，但 DOI 10.1086/675080 = **PASP 126, 158–169**（"Implementation of Robust Image Artifact Removal in SWarp through Clipped Mean Stacking"）；evidence_lit.json 的 cite 字段写的是 PASP 126, 158（正确）。README 的卷页与 DOI 张冠李戴（MNRAS 442,1507 是另一篇 Gruen+2014 弱透镜论文）。
  - `README.md:314` "Wild & Hewett 2005, MNRAS 358, 1089" → Crossref 10.1111/j.1365-2966.2005.08844.x = **358, 1083–1099**。
  - `README.md:315` "Soto+2016, MNRAS 458, 3110" → Crossref 10.1093/mnras/stw474 = **458, 3210–3220**。
  - `README.md:320` "Morganson+2018, PASP 130, 074503" → Crossref 10.1088/1538-3873/aab4ef = **130, 074501**。
- 影响：DOI 链接都对、底层 evidence_lit.json 也对，但 README 表格的元数据错了 4 处，与 §8"11/11 已核验"的呈现不一致。
- 建议：直接以 evidence_lit.json 的 cite 字段生成 §8.1 表格。

**15. 复现链证据不足（详见"复现性检查结果"）。** run_all.sh 不构建 `build/*.a`；归档日志全部带 run_all.sh 不会产生的 `exit=0` 行；JSON mtime 顺序与循环顺序不符；`build/libastrocs_phase2.a` 在结果生成窗口中间（14:54:36）被重建。建议：把生产库构建纳入脚本（或显式前置检查），并在报告里给出"一次 run_all.sh 全绿"的日志。

---

### 次要问题 / 表述

- **C1 A2 的斜率**：未校正臂实测斜率 0.7681 低于无噪预言 0.8235（比值 0.9747）。现判据（对预言比值）比原判据（绝对窗 [0.8,1.2]）更科学，但 7% 的系统偏低没有任何解释（是 order-2 基线拟合吸收了一部分阶跃，还是权重/子集效应？）。建议补一句机理说明。
- **C1 A10 无误差棒**：每波长仅 1 次实现，而 C4 null 给出单边界度量 std≈0.61 e-、6 边界取 max 的散布≈0.74 e-，与 800→400 px 的差值（0.31 e-）同量级 ⇒ 6 点差异大多不显著。斜率 0.798±0.197（t=4.04，R²=0.804）。
- **C2 的 M4 绝对残差未突出**：吸收臂残差 6.31 e- 是 C1 纯加性世界 0.383 e- 的 16 倍，oracle Phase1 臂也有 6.20 e-（`seam_arms.truth`）⇒ 说明该世界残余接缝来自 Phase1 的 m̂ 拟合误差（`resid_std`≈0.35%–0.52% in log10）而不是加性链路。README 只报 4.33× 比值，未给"6.31 e- 是否可接受"的绝对判据。任务原文要求"残余归零"，实测并未归零。
- **C2 M2/M3 的 oracle 底噪未报**：flat-sky 世界 oracle 臂 g 残差 0.0217（2.2%），说明 MA 求解器本身有 ~2% 底噪；M3 的 2.95% 仅比底噪高 1.4×。建议同时报 oracle 臂。
- **C2 M1b** 与 M1 是同一数字的两面（0.5599），作为独立"对照门"计入 7/7 略冗余。
- **C1 A7b / C5 W5 是"缺陷登记门"**：PASS 表示生产代码有缺陷（`converged=0`），与能力门同列在"11/11 PASS"里容易被误读。
- **FIX-1 的建议已存在变更草案**：`lib/infrastructure/scheduler/src/module_adapters.cpp:5936-5947` 的注释明确写着"上一版实现就地改为 tolerance=1e-3 + tolerance_relative=1（未走变更流程）⇒ 现回退到冻结值"，并指向 `工程控制/RELEASE-02/change-claims/CONFORM-FIX-B-001-tolerance-relative.md`（草案，待裁决）。SCI-C §10 的 FIX-1 建议应引用该草案而不是另起一条。
- **FIX-1/FIX-2 的行号已漂移**：README §10 写 `module_adapters.cpp:5941-5942`，当前该处是注释；实际赋值在 `:5948-5949`（该文件工作树里被别的任务改了 735 行，mtime 14:27:41）。引用生产代码行号时应带 commit/内容锚点。
- **κ 的普遍性**：C1 κ=3.36e6、C3 κ=3.00e6、C7 κ=3.16e7，三者都接近 `kappa_max=1e8`（`lib/algorithms/coverage/src/sky_plane.cpp:409` 的默认值，实验未覆写）；README §10 只登记了真实数据的 FIX-3。建议合并为一条"天光面正规方程条件数普遍偏高"。
- **未报告的量**：C3 `chi2_red=1.524`（合成世界，误差模型低估 ~50%）未进报告；C5 报了 `chi2_red=0.771` 但没有任何门。
- **make_figures.py:93** 用 `np.random.default_rng(0).normal(mean, std, 4000)` 画 null 直方图（不是 120 个真实实现），虽标注 "gauss fit"，仍属合成数据入图。
- **make_figures.py:165** `fig_c1(load("c1_additive.json"), load("c3_public_plane.json"))` 第二参数 `d3` 未使用。
- **c4_seam_criterion.py:92-93** 的 `sep` 是死代码（`np.mean(x and [x])` 这种写法也易误读）。
- **C6 E3** 只有 20% 余量（11,688 vs 14,568 kB）、单次测量、无重复，且两臂工作量不同（full 臂还要跑 dense-vs-sparse 比对循环）。作为"内存随分块而非像素增长"的证据偏弱，建议改测"物化 dense 与否的峰值增量"并重复 3 次。
- **C7 §4.7 R4 行没有数字**（其他行都有），且 §9 验收表把 R4 列为"完成"；实际 R4 只是"图存在"。图我已目检：左（未校正）面板在 x=384 处确有可见的条带台阶，右（校正）面板无可见条带——与度量一致，且 README §6.7 已声明"不作科学证据"，这一点合规。

---

### 已核实为**站得住**的部分

以下结论我尝试攻破但失败，明确记为可站住：

1. **C1 A4"多退少补压接缝"**：我用当前代码重跑，step 4.8023→0.3830（12.54×）与归档 JSON 逐位一致；换成本报告自己规定的 excess 度量仍为 5.1082→0.3550（14.39×）。结论稳健，且**不依赖**被质疑的 A10 预测子。
2. **C1 A3"校正臂阶跃不随注入增长"**：注入幅度 0→40 e-（基内 offset+plane）时，未校正臂 4.197→34.93 e-、无噪预言 4.841→37.73 e-，而 raw−δ_k 臂 0.589→0.966 e-（斜率 0.0087）。双向都有标度 ⇒ 该门非恒真、非退化。这是本单元最干净的一条能力门。
3. **C1 A4/A10 的预测子实现正确且可复现**：我独立按解析天光场（`out_of_basis_rms` 的 1D 化）复算 6 个 RMS，得 0.3125/1.3474/3.0702/5.4911/6.5145/6.6434，与 JSON 完全相同；重跑扫描得到相同的 slope/pearson/ratio。**预测子的算法没有实现错误**（有问题的是它与 seam 度量共用 ~64 px 高通、以及 README 的表格）。
4. **C1 A6/A8b/A9 的数值本身**：0.02362（<0.05）、0.023619 vs 0.023174、0.003719（<0.01）与 JSON 一致；只是这些量的"平凡性"已在 §6.1 承认（分级问题见重要问题 12/13）。
5. **C4 的注入几何确实落在覆盖子集突变上**：x=256 处覆盖子集从 {B} 变到 {B,C}（`COVER_GX`），实测响应斜率 0.449 ≈ w_B/(w_B+w_C) ≈ 0.5，与几何预言一致。
6. **C4 的阈值标定确实是样本外**：前 60 个 null 定 (mu,σ)，后 60 个独立实现测假阳性（0/60）；null 与注入臂用不同 seed 前缀，互不重叠。
7. **C4 N5/N6**：强平滑公共梯度臂假阳性 0/30；零假设偏度 0.074、Shapiro p=0.858。这两条我复算/复核无误。
8. **C4 的 36.4σ 分离度数值**：|17.323−(−5.011)|/0.6128 = 36.44，与 JSON 一致（其解释问题见致命问题 4/5，但算术无误）。
9. **C5 的权重排名在统计上显著**：用 JSON 的 mean/std/N=20 反推，leak 差 control_ivar−uniform = 0.0401 ± 0.0073（≈5.5σ）、control_ivar−snr2 = 0.3157 ± 0.0512（≈6.2σ）。README §6.2"RMS 上 8.5% 优势小于 MC 误差"的诚实边界与 JSON 一致（std 0.698）。
10. **C6 的数值等价**：dense cache vs sparse calibrate_block 在 1,048,576 点上 max|Δ|=3.11e-15（≤1e-12）；体积 14,001 B vs 8,389,129 B（0.167%）；子集求值逐位一致（0.0）。E1/E2/E4 我复核 JSON 无误，E1 是两条生产代码路径的真实对拍。
11. **C7 的真实数据链路可复现**：R2 的 rc=0 / n_nodes=49 / rank=49 / chi2_red=1.0037 / κ=3.156e7 与 JSON 一致；4 帧的 WCS 预估位移与 FFT 相位相关位移（[0,0]/[0,0]/[8,6]/[10,8]）与 `data` 字段一致；接缝在 excess 下仍为 37.5×。
12. **C3 的"保留公共面"**：产品中位 299.168 vs B_ref 297.331（比值 1.006）、全减臂 0.711、负值占比 0.0、gauge 两模式接缝完全相同（`seam_ref` 与 `seam_sum` 逐位相同，我核对了 JSON）。B3/B5/B6 的数值站得住（B2/B6 的判据平凡性见重要问题 13）。
13. **确定性与无副作用**：`SEED_BASE=20260923` + SHA-256 派生；grep 全代码无 `time/datetime/environ/getpid/uuid/random.seed`；无网络调用（无 requests/urllib/http/curl/wget）；无 git 写；探针只读链接 `build/*.a`，所有写操作只落 `run/SCI-403/` 与 `results/`。探针 C++ 里没有硬编码绝对路径（路径全部来自 scenario JSON）。
14. **FIX-2 作为代码事实成立**：`lib/algorithms/coverage/src/upm.cpp:1527-1536` 注释即"0 = 迭代耗尽；1 = 达 tolerance"，全仓无 converged=2/3 分支；`module_adapters.cpp` 侧 `upm_converged` 也只有 0/1。（实验证据不足见重要问题 10。）
15. **FIX-1 的现象可复现**：C1 A7b 的 `converged=0, iterations=300`（tolerance=1e-6, tolerance_relative=0）与 JSON 一致；生产注释也确认该绝对容差在 |M|~3e15 时低于 ULP。

---

### 复现性检查结果

1. **脚本本身干净**：`run_all.sh` 用 `BASH_SOURCE` 推 ROOT、无绝对硬编码路径、无网络、无 git 写、`set -euo pipefail`、每步 `timeout 3600`；`build_probes.sh` 用 `$ROOT` 相对定位、`flock` 串行化。
2. **干净检出不能一键跑通**：`build_probes.sh:23-30` 直接链接 `$ROOT/build/libastrocs_phase2.a` 等 6 个静态库，脚本**不构建**它们（也不检查存在性）。在没有预先 `cmake/ninja` 的检出上，`run_all.sh` 会在第一步编译失败。README §7 未写"先构建生产库"这一前置。**建议：脚本内加 `test -f build/libastrocs_phase2.a || ninja -C build`（或显式报错提示）。**
3. **归档日志不是 run_all.sh 产出**：`run/SCI-403/logs/*.log` 全部以 `exit=0` 结尾（8/8，含 build 日志），而 `run_all.sh:22-27` 只做重定向 + tail，不会写 `exit=`。说明这些日志来自作者自己的逐步包装命令。
4. **JSON mtime 顺序 ≠ 循环顺序**：循环是 c1→c2→c3→c4→c5→c6→c7，实际 mtime 为 c1 14:36:47、c6 14:36:40、c7 14:49:00、c4 14:52:02、c3 14:56:29、c5 14:56:43、c2 14:57:06（`run_all.sh` 自身 mtime 14:42:56）。**归档结果不是一次 run_all.sh 的产物**，§9"一键复跑（固定 seed）完成"缺少证据。
5. **结果跨了两个构建**：`build/libastrocs_phase2.a` mtime 14:54:36，而结果 JSON 落在 14:36–14:57 两端。也就是说 c1/c6/c7/c4 用的是重建前的库，c3/c5/c2 用的是重建后的库。**缓解证据**：我用当前（重建后）库重跑 C1 与 C3，数值与 `model_hash` 与归档逐位一致 ⇒ 至少 sky-plane/UPM 这些路径没变；但 c2 的 MA 路径未重跑（**未验证**），且"同一二进制产出全部结果"这一命题不成立。
6. **我实际做的重跑**（全部只写 `/tmp/scic_chk`，用软链指向 `run/SCI-403/{sky_probe,upm_probe}`，未改动仓库内任何文件）：
   - C1 A4（world seed `c1`，1 次 sky_probe）：step 4.8023/0.3830/0.8495 与 JSON 逐位一致；excess 5.1082/0.3550/1.0014。
   - C1 A10 全扫描（6 次 world + 6 次 sky_probe）：6 组 (oob_rms, seam_max) 与 JSON 逐位一致；slope/pearson/spearman/ratio 全同。
   - C3 B7（1 次 sky_probe）：model_hash `607f93da…`、chi2_red 1.5240、κ 3.003e6 与 JSON 一致；step 5.1883/0.3862，excess 5.4905/0.4969。
   - C2 M5 段（无探针）：复现出 kexp=5.333、kpk=4、period 128 px，并算出正确预期 k≈21.33。
   - C4 符号测试（无探针，每点 6 次实现）：+50→17.61（翻红）、−50→−28.47（不翻红）、−100→−51.12（不翻红）。
7. **结论**：单脚本（c1..c7 各自）在**已有 build/ 与探针**的前提下可复现且确定性良好（seed 派生无环境依赖）；"一键复跑"与"归档 = 最终代码产出"两条**未验证/不成立**。

---

### 生产代码改动检查结果

1. **本单元自身没有写生产代码的路径**：`grep` 全部 SCI-C 代码，写操作只落在 `S.RUN=run/SCI-403`、`S.RESULTS=实验/SCI-C/results`、`S.FIGS=.../figs`；`build_probes.sh` 只 `g++ ... -o run/SCI-403/<probe>`；探针 C++ 只 `p2_*` 只读调用，无写生产文件的代码。这一点与 README"零生产代码改动"一致。
2. **但 git status 无法支持"只动 实验/SCI-C/** 与 docs"**：工作树共有 **357** 个已跟踪文件被改（docs 275、lib 46、tests 20、ci 4、artifacts 4、tools 2、config 2、contracts 1、README.md、工程控制 1），另有 `?? 实验/SCI-C/`（整目录未跟踪）。也就是说 `实验/SCI-C/**` 本身是新增未跟踪目录，而**仓库里另有大量与 SCI-C 无关的未提交改动**（projection / aio / cli / drizzle / noise_snr / star_detection 等子系统，mtime 13:11–15:01）。
3. **与 SCI-C 引用直接相关的两个生产文件在 SCI-C 时间窗内被改过**（不能排除是并行任务）：
   - `lib/infrastructure/scheduler/src/module_adapters.cpp`：mtime **14:27:41**，diff 735 行，内容含 "CONFORM-FIX-B-001（合规回退）… 上一版实现就地改为 tolerance=1e-3 + tolerance_relative=1（未走变更流程）⇒ 现回退到冻结值"——即 SCI-C 的 FIX-1 建议已被另一条线作为变更草案处理，且该文件正在被改。**README §10 引用的 `:5941-5942` 现在是注释文本，实际赋值在 `:5948-5949`。**
   - `lib/algorithms/coverage/src/sampler.cpp`：mtime **14:58:14**（c2 结果写出后 1 分钟），diff 5 行；`build/libastrocs_phase2.a` 于 14:54:36 重建。
4. **可判定的结论**：**"SCI-C 未修改生产代码"这一命题，证据方向一致（代码层面无写入路径、探针只读），但在当前脏工作树上无法用 git 证明**；且"生产库在实验窗口内被重建"使得"所有结果来自同一冻结二进制"不成立。建议：把 SCI-C 的实测锚定到一个明确的 `build/libastrocs_phase2.a` 哈希（或 commit SHA）并写进报告，而不是只写"只读链接"。

---

审稿结论: 核心结论（可表示的帧间天光差下加性 UPM 能把覆盖接缝压到 0.4 e- 量级、Phase1 归一是乘性差的前提、稀疏求值与稠密等价）方向可站住且关键数字可复现，但 A10 的证据表与归档 JSON 完全不符、M5 频率定位单位错、M6/N4/M2/M3/M4/A10 六处判据被放松而未披露、C4 的"非退化"反证是恒真构造且检测器单边——这些必须在下一轮修正后才可作为验收证据。
