# FIX-001 实施修复

## 目标
按差异清单完成代码修复，构建与单测全绿。

## 输入
- `audit-report.md`（上游 AUDIT-001 产出）
- 仓库当前 HEAD

## 要做的事
1. 按清单逐项修复，只动 `src/`、`tests/`
2. 每项修复配套单测
3. 跑通构建与单测

## 产出
- 修复代码 + 单测；结束回填证据报告

## 验收条件（对应 evidence.checks）
- build：`npm run build`（示例，换真实命令）退出码 0
- unit-tests：`npm test` 全部通过

## 禁止事项
- 只修改 write_scope（src/、tests/）内的文件
- 不提交 git（集成节点负责）；不伪造未执行的 check
