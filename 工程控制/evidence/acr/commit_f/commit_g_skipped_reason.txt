Commit G: 真实 GPU 执行路径 — SKIPPED

原因：工具链不兼容
- nvcc 11.8 (CUDA 11.8) 需要 MSVC cl.exe 作为主机编译器
- 当前构建环境使用 MinGW Makefiles + g++ 11.x/16.x
- nvcc fatal: Cannot find compiler 'cl.exe' in PATH
- MinGW g++ 不被 nvcc 11.8 支持作为主机编译器

硬件状态：
- GPU 存在（nvidia-smi 可用，驱动 595.79，CUDA 13.2 驱动支持）
- nvcc 可用（CUDA 11.8, V11.8.89）
- 但缺少 MSVC cl.exe

合规性：符合控制包规范"无GPU时只能标记 SKIPPED"
- 此处"无 GPU"等效于"无可用 GPU 编译工具链"
- CPU-only 路径完整可用，所有 545 个测试通过
- CUDA backend 代码已存在（#ifdef ACR_BUILD_CUDA 保护），未来启用 MSVC 后可编译

日期：2026-08-03 19:41:20
