# 任务：MOD-002 迁移后引用刷新（ARCH-001 停稳后执行）

状态：NOT_STARTED
层：L2　依赖：**ARCH-001 迁移停稳**（必须等它收口，不得并发）
文件域互斥组：S4-B（模块图/合同文档的引用面）

## 背景（前台实测 2026-09-16）

ARCH-001 正在把 `lib/**` 的 31 个旧目录迁到 `lib/algorithms/**` 与 `lib/infrastructure/**`（含 `git mv` 与就地路径改写）。这会让**已完成任务的交付物里写死的旧路径引用**指向不存在的路径：

- `docs/modules/MODULE_MAP.yaml`（MOD-001 交付，含 `legacy_paths` 迁移映射）；
- `docs/contracts/**`（DATA-001/CFG-001 交付，含路径与锚引用）；
- `tools/quality/**` 与 `tests/**` 中写死的 lib 路径常量；
- `ci/**` 注册项里写死的 `-s/-t` 目录（由 CI-001 收敛窗口一并订正，**不归本任务**）。

同时 `tools/quality/check_module_map.py` 的**悬空判定**会随迁移改变结论——必须重跑并重新给出「真实悬空=0」的证据。

## 目标

ARCH 迁移停稳后，一次性刷新所有**非 ci/** 的旧路径引用**，并证明悬空为 0。

## 允许改

- `docs/modules/MODULE_MAP.yaml`（`legacy_paths` 映射按迁移实际结果补全/订正；**保留旧路径作为历史键**，不删）；
- `docs/contracts/**` 的路径引用（只改引用，不改合同语义）；
- `tools/quality/**`、`tests/**` 中写死的 lib 旧路径常量（机械替换）；
- 新建 `reports/PROJECT-GOVERNANCE-01/mod/MOD-002_REFRESH_LEDGER.md`：逐条「旧路径 → 新路径 → 引用点 → 处置」。

## 禁止改

- 产品代码（`lib/**`、`cli/**`）—— 属 ARCH-001/INT-001；
- `ci/**`（CI-001 窗口）；
- `docs/science/**`、`docs/algorithms/**`（冻结只读；锚点同步归 ARCH-001）；
- 合同语义/公式/结论。

## 步骤

1. 用 `git grep -n -E 'lib/(phase1[^/]*|phase2[^/]*|phase3[^/]*|common|backend_host|orchestrator|healpix_db|calibration|astro_image_io)/'` 实测引用清单；
2. 逐条判定：**文档/测试常量引用** → 改；**MODULE_MAP 的历史映射键** → 保留并核对目标路径正确；**已删模块** → 标注为历史；
3. 重跑并留证：`python3 tools/quality/check_module_map.py`（真实仓库判决 + 悬空计数）、`python3 tools/quality/check_root_cleanliness.py`、`python3 tools/doccheck/check_doc_index.py --strict`；
4. 复跑 `tests/quality` 与 `tests/contracts`，确认无新增红。

## 验收门（前台独立复跑）

- [ ] 引用清单逐条有结论（计数一致：实测 = 改 + 保留 + 历史）；
- [ ] `check_module_map.py` 对真实仓库的判决与悬空计数**如实给出**（允许仍为 NOT_IMPLEMENTED，但**悬空=0** 且证据完整）；
- [ ] `tests/quality`、`tests/contracts` 无新增红；
- [ ] MOD-002_REFRESH_LEDGER 覆盖全部改动；
- [ ] 越界自查（未动 `lib/**`、`cli/**`、`ci/**`）。

## 交付物

1. 引用刷新改动；2. REFRESH_LEDGER；3. 复跑证据；4. 自证摘要。