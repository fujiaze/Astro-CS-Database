# 任务：CLI-001 切换唯一用户命令树

状态：`NOT_STARTED`
层：L3　依赖：CFG-001, AIO-001, RT-001　文件域互斥组：S6-A

## 目标

唯一可执行入口只提供 `normalize / mosaic / export / help / --version / doctor / benchmark`；旧 `phase1/phase2/phase3` 用户命令与别名全部消失且返回 rc=2。

## 基线状态（编制时实测）

- 命令实现在根 `cli/`（`parser.cpp`、`commands.cpp`、`main.cpp`、`runtime_client.cpp` 等），派发旧 phase1/phase2/phase3。
- 旧检查器 `tools/check_cli_command_layer.py` 实测 **rc=0**（输出 `CLI-001_PASS: 15 cmd_* uniform, 27 unique paths, kRules dispatch, stable exits`）——它把旧命令层当合格判据，是本任务必须改造或替换的检查。
- ASTROCS_DESIGN §6.2 明确：phase1|2|3 仅为内部指代，代码与命令名使用 normalize/mosaic/export。

## 权威依据
- ASTROCS_DESIGN.md §6.1（三命令平级、薄入口职责）、§6.2（唯一命令树）、§1.2（禁止隐式串接三阶段）
- docs/plugins/infrastructure/18_cli.md
- ASTROCS_DESIGN.md §7.1（lib/infrastructure/cli/{normalize,mosaic,export}）

## 改动范围（文件域）
### 允许改
- `lib/infrastructure/cli/**`（新命令实现）
- 根 `cli/**` 的迁移与删除
- `tests/cli/**`
- 改造 `tools/check_cli_command_layer.py`（或由 CI-001 以 `CHK-*` 取代）

### 禁止改
- 算法实现
- AIO/调度内部语义
- 保留 phase1/2/3 用户别名或兼容开关
- 三个命令之间隐式串接

## 步骤
1. 在 `lib/infrastructure/cli/{normalize,mosaic,export}` 落位子命令入口，引用对应算法模块。
2. 顶层命令树只注册 §6.2 的条目：`normalize|mosaic|export` × (`--json`/`--template`/`--help`)、help、--version、doctor、benchmark。
3. 移除旧 phase1/phase2/phase3 命令与任何别名、隐藏开关、环境变量兼容路径。
4. 断言三命令进程与恢复相互独立：分别启动、分别恢复、互不依赖同进程状态。
5. 更新/替换旧命令层检查器：它以 15 个 `cmd_*` 为合格判据，必须改为对 §6.2 命令树的断言。

## 验收门（前台独立复跑）
- [ ] `<cli> help` 输出只含 §6.2 命令（给出完整输出并与 §6.2 逐行对照）
- [ ] `<cli> phase1 run`、`phase2 run`、`phase3 run`、`phase1` 等旧命令 rc=2（逐条给命令与退出码）
- [ ] 三命令各自路由到独立 DAG（给出运行记录或事件流证据）
- [ ] 命令注册处不存在旧命令：`grep -rn "phase1\|phase2\|phase3" cli/ lib/infrastructure/cli/` 只允许内部指代注释命中

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/CLI-001/logs
<cli> help > run/PROJECT-GOVERNANCE-01/CLI-001/logs/help.txt 2>&1; echo "rc=$?"
for c in phase1 phase2 phase3; do <cli> $c run; echo "$c rc=$?"; done
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
