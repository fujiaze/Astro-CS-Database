# 任务：P3-001 治理八投影 registry 与独立 Oracle

状态：`NOT_STARTED`
层：L4　依赖：DATA-001, RT-001　文件域互斥组：S6-P3

## 目标

投影 registry 内置 TAN/SIN/CAR/AIT/STG/MOL/CEA/ZEA 八个投影，每个声明适用域/奇点/wrap/手性/WCS 规则，并有独立往返 Oracle 与边界负例。

## 基线状态（编制时实测）

- 现实现：`lib/phase3_proj`；`docs/plugins/algorithms_phase3/14_projection.md`、`docs/algorithms/PHASE3_PROJ_IMPL.md`、`docs/algorithms/v6/phase3/ALG-P3-001_KERNEL_REGISTRY.md` 并存。
- ASTROCS_DESIGN §5.3 冻结八投影集合。

## 权威依据
- ASTROCS_DESIGN.md §5.3（投影算法：内置多种）、§5.4（硬约束）
- docs/plugins/algorithms_phase3/14_projection.md
- docs/algorithms/PHASE3_PROJ_IMPL.md、docs/algorithms/v6/phase3/（只读）

## 改动范围（文件域）
### 允许改
- `lib/algorithms/projection/**`
- `tests/export/**` 投影用例
- registry 定义与文档索引

### 禁止改
- 把投影公式散落在 CLI switch 里
- 修改既有投影的科学定义/常数
- 用生产实现当 Oracle

## 步骤
1. 把八投影收敛为 registry：每个条目含 code、适用域、奇点、wrap 行为、手性、WCS 关键字映射。
2. 为每种投影建立独立 Oracle（解析往返或高精度参考），不调用生产实现。
3. 边界负例：非法域、奇点邻域、超范围坐标必须 fail-closed 且给可诊断错误。
4. 验证 registry 与 CLI/export 的接线只通过 code 引用，不复制公式。

## 验收门（前台独立复跑）
- [ ] 8/8 投影注册且 code 唯一（给出 registry 导出清单）
- [ ] 每投影往返误差在冻结合同内（给出逐投影数值表）
- [ ] 非法域/奇点负例 fail-closed（逐条给输入与错误码）
- [ ] 投影相关 ctest rc=0（给出命令）

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/P3-001/logs
ctest --test-dir build -R 'proj' --output-on-failure 2>&1 | tail -30; echo "rc=$?"
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
