# 任务：DOC-001 收敛活动文档的术语、状态与引用

状态：`NOT_STARTED`
层：L0　依赖：BASE-001　文件域互斥组：S1-B

## 目标

让所有**活动**文档只说新文档集的语言：命令叫 `normalize/mosaic/export`，状态只用 ASTROCS_DESIGN §11.3 的词表，不把已删除的旧控制包或旧权威当成现状。

## 基线状态（编制时实测）

- `python3 tools/doccheck/check_doc_index.py --strict` → **rc=1**，`verdict=DOC_INDEX_FAIL`；`docs/DOCUMENT_INDEX.yaml`(56703B) 仍索引旧文档体系。
- `docs` 下并存多套口径：`docs/API_REFERENCE.md`、`docs/ARCHITECTURE.md` 与 `docs/api/**`、`docs/architecture/**`；`docs/owner/**` 与 `docs/review/**` 内容重复；`docs/contracts/v6/**`、`docs/science/v6/**`、`docs/algorithms/v6/**` 为上一轮治理产物。
- 活动文档中仍存在旧命令术语（把 phase1/phase2/phase3 当**用户命令**）与旧控制包路径引用（GAP-019）。
- `python3 tools/check_l0_docs.py` → rc=0（`DOC-002_PASS: REVIEW.md + 5 L0 docs`）：该检查绑定旧活动文档集合，本身属 GAP-001/GAP-016 范围。

## 权威依据
- ASTROCS_DESIGN.md §0（权威链与「只写要怎么做」的边界）、§1.2（三命令定位）、§6.2（命令树唯一）、§11.3（状态阶梯唯一口径）、§12（版本信息）
- ENGINEERING_SPEC.md §2（注释/文档不得堆积历史版本号与任务编号）、§7（目录落位）、§8（活动文档无陈旧版本号/历史状态冒充）
- CONTROL_PACK_SPEC.md §3（控制包路径引用）

## 改动范围（文件域）
### 允许改
- `README.md`、`REVIEW.md`、`HANDOVER.md`、`memory.md`、`CHANGELOG.md`、`DEPENDENCIES.md`
- `docs/**`（**不含** `docs/science/**` 的公式与 `docs/algorithms/**` 的推导；这两处只允许改索引/链接/状态标注）
- `docs/DOCUMENT_INDEX.yaml`、`docs/README-DOCS.md`、`docs/RELEASE_STATUS.md`、`docs/GLOSSARY.md`

### 禁止改
- `docs/science/**`、`docs/algorithms/**` 的公式、阈值、推导与冻结清单
- 源码、测试、合同 schema、`ci/**`、`tools/**`（检查器归 GOV-001/CI-001）
- `ASTROCS_DESIGN.md`、`AGENTS.md`、`ENGINEERING_SPEC.md`、`CONTROL_PACK_SPEC.md`、`docs/ci/**`、`docs/plugins/**`（新文档集本体）

## 步骤
1. 建立「活动文档 vs 历史资料」的判定线：给出白名单（活动）与黑名单（历史）；历史项统一加 `ARCHIVED_NON_NORMATIVE` 抬头，并从 `docs/DOCUMENT_INDEX.yaml` 的活动索引移出（可保留归档段）。
2. 术语收敛：把描述**用户命令**的 phase1/phase2/phase3 run 全部改为 `normalize/mosaic/export`；保留「内部阶段指代」用法时须显式注明（ASTROCS_DESIGN §6.2 末条）。
3. 状态收敛：所有模块/阶段状态改用 §11.3 词表（`CONTRACT_READY / IMPLEMENTED / INSTALLED / VERIFIED` 及负向 `NOT_IMPLEMENTED / NOT_VERIFIED / DEFERRED / DORMANT / FAIL`），删除 PASS/已完成/已发布之类自称。
4. 陈旧引用清理：删除已不存在的路径引用（旧 `工程控制/*`、`engineering/control/archive/**`、旧根治理文件），链接不可达的改为新文档集对应章节。
5. 处理重复口径：`docs/API_REFERENCE.md`↔`docs/api/**`、`docs/ARCHITECTURE.md`↔`docs/architecture/**`、`docs/owner/**`↔`docs/review/**` 每组只保留一个活动版本，其余归档并登记。
6. 修 `docs/DOCUMENT_INDEX.yaml` 使 `check_doc_index.py --strict` 可绿（若该检查器本身语义过旧，登记给 CI-001，不在此处放宽检查）。

## 验收门（前台独立复跑）
- [ ] 活动文档中不存在指向已删除控制包路径的引用（命中仅允许在带 `ARCHIVED_NON_NORMATIVE` 的归档处）
- [ ] 活动文档中不存在把旧宪章/旧工程约束当权威的表述（可复用 GOV-001 的 grep 门）
- [ ] 活动文档状态词全部落在 §11.3 词表内（给出检索命令与逐条处置）
- [ ] `python3 tools/doccheck/check_doc_index.py --strict; echo rc=$?` → rc=0（若判为检查器过旧，须有 CI-001 的迁移登记）
- [ ] `git diff --stat` 未改 `docs/science/**` 与 `docs/algorithms/**` 的内容行（只允许索引/抬头/链接改动，逐文件在自证中说明）

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/DOC-001/logs
python3 tools/doccheck/check_doc_index.py --strict; echo "rc=$?"
python3 tools/check_l0_docs.py; echo "rc=$?"
git diff --stat -- docs README.md REVIEW.md HANDOVER.md memory.md CHANGELOG.md
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
