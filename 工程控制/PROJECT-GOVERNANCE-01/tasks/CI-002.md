# 任务：CI-002 补齐 run_checks.py 三项活守卫并切换工作流调用点

状态：NOT_STARTED
层：L2　依赖：**QA-001**（守卫语义必须先由 QA 线定义/固化）、CI-001
文件域互斥组：S5-C（ci/run_checks.py + .github/workflows/**）

## 背景（前台裁决 2026-09-16）

CI-001 已交付 `ci/run_checks.py`（新规范入口，`docs/ci/01_CHECKS.md §5` 与 `AGENTS.md:36` 均为规范依据），但**尚未切换工作流调用点**：

- `.github/workflows/ci-linux.yml:141` → `python3 ci/run.py --profile …`；
- `.github/workflows/ci-windows.yml:110` → `python ci/run.py --profile windows-main`；
- 两文件头部注释仍写「只允许三类调用：`ci/run.py`…」。

执行者给出的切换阻塞理由（**前台认可**）：`ci/run_checks.py` 尚缺 `ci/run.py` 的三项活守卫，此刻切换等于**静默放宽 CI**：
1. `known_failures` 基线的应用（豁免清单如何生效）；
2. `mutating` 检查在脏工作区的 `FAIL(dirty)` 判定；
3. `requires_monitor` 的 `frozen_gate` 证据要求。

## 目标

在 `run_checks.py` 中补齐上述三项守卫（语义须与 `run.py` **逐条等价**，不得放宽），然后**切换两处工作流调用点**并把文件头注释改为新入口，使「规范 = 实现 = 工作流」三者一致。

## 允许改

- `ci/run_checks.py`（补齐守卫；不得降低任何现有判定强度）；
- `.github/workflows/ci-linux.yml`、`.github/workflows/ci-windows.yml`（调用点 + 头部注释）；
- `ci/run.py`：如需，改为**薄兼容 shim**（转调 run_checks.py，保留旧参数）——**不得直接删除**（工作流与历史脚本仍可能引用）；
- `docs/ci/01_CHECKS.md`：仅在「实现与 §5 不一致」时补注（§5 是规范，**不得改写其语义**）。

## 禁止改

- 检查器本体（`tools/**`）的判定语义；
- `ci/checks.json` 的注册项 ID（收敛已由 CI-001 完成；如确需新增项须先向前台申请）；
- 产品代码与 `docs/science|algorithms|plugins/**`。

## 验收门（前台独立复跑）

- [ ] 三项守卫**逐条等价性证据**：对同一工作区，`run.py` 与 `run_checks.py` 的判定结果逐检查项对照（差异必须为空或逐条解释）；
- [ ] 切换后**真跑一次**工作流等价命令（`python3 ci/run_checks.py --profile linux-main --all` 等）并给 rc；
- [ ] 负例：三项守卫各自「能够变红」（自造 dirty / known-failure / frozen-gate 未给证据三种输入）；
- [ ] `grep -rn 'ci/run.py' .github/workflows/` 的结果与最终口径一致（要么 0 处，要么仅 shim 说明）；
- [ ] `docs/ci/01_CHECKS.md §5` 的三条命令**全部可跑通**（含多值 `--check CHK-FMT CHK-UNIT`）。

## 交付物

1. run_checks.py 守卫补齐；2. 工作流调用点与注释；3. 等价性与负例证据；4. 自证摘要。