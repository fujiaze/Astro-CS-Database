# 任务：TEST-CLI-SYNC-2 残余同步（后端 CLI 类 10 文件 + 陈旧 skip 载体 20 条）

状态：NOT_STARTED
层：L2　依赖：**CLI-002**（export 缺陷修复，GAP-034）、**ARCH-001 停稳**
文件域互斥组：S3-B（tests/backend 的 CLI 类 + 陈旧 skip 载体）

## 背景（TEST-CLI-SYNC 交付后的残余，前台复核确认）

TEST-CLI-SYNC 已把 `tests/cli` 从 Ran 77/F2/E11/S38 收敛到 Ran 125/F9/E1/S21，红集合逐条归因；
以下残余未闭合（**均在其报告与自证摘要中登记，不得静默丢失**）：

### ① 后端 CLI 类 10 文件未同步

`p1004`、`p2001`、`p2002`、`p2003`、`p2006`、`p2007`、`p3003`、`p3004`、`p3005`、`p3006`。
阻塞原因（三条，须逐条实证后解除）：
1. **V1 嵌套 config → 扁平会话形态**的改写（旧 `phaseN` 命令配置结构）；
2. `run/` 与临时目录 fixture 的重建；
3. **export 缺陷（CLI-002）**：export 链路在修复前无合法配置可运行。

### ② 陈旧 skip 载体 20 条

`iso_acr` 6 条、`monitor_events` 6 条、`phase123` 8 条。
- `iso_acr` 前 4 条**不依赖 export**，可先同步；
- `monitor_events` 的 `--resource-detail` 旧旗标已随旧命令树删除 ⇒ 必须**确认新载体**（新命令树/新观察面）或**登记能力删除**（写明依据，不许静默 skip）；
- `phase123` 8 条待 export 缺陷修复后解除。

## 目标

把上述 30 条全部收敛为「已同步并绿」或「显式退役/能力登记」，**新增 skip 必须为 0**。

## 允许改

- `tests/backend/**` 的 CLI 类测试、`tests/cli/**` 的陈旧 skip 载体；
- 必要的测试夹具（放 `tests/**` 内，不落仓库根）。

## 禁止改

- 产品代码（`lib/**`、`cli/**`）—— export 缺陷归 CLI-002；
- `ci/**`、`.github/**`（CI-001/CI-002 域）；`docs/**`（DOC-001 域）；
- 科学/算法文档；
- **不得**用 skip/xfail 变绿；不得删除测试意图而不登记能力去向。

## 验收门（前台独立复跑）

- [ ] 10 个后端文件 + 20 条 skip 载体**逐条处置表**（30 行，计数一致）；
- [ ] `tests/backend` 的 CLI 类失败数按「归本任务的」清零（ARCH 迁移类红另行归因，不计入）；
- [ ] **新增 skip = 0**（与 TEST-CLI-SYNC 留下的 21 条基线逐条对照）；
- [ ] 每条能力删除都有登记（去向 + 依据条款）；
- [ ] 越界自查。

## 交付物

1. 测试同步/退役改动；2. 30 行处置表；3. 复跑证据（rc + 失败集合 + skip 对照）；4. 自证摘要。