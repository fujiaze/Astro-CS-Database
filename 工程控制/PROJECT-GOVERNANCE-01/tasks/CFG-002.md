# 任务：CFG-002 配置登记类遗留闭合（CFG-001 收口时只登记未动作的 5 项）

状态：NOT_STARTED
层：L2　依赖：CFG-001（已完成）、ARCH-001（迁移停稳）
文件域互斥组：S6-A（config/** 与配置类合同/文档）

## 背景

CFG-001 交付了 `config/defaults.json`（44 字段，4 项 pending_authority）、`config/filters.json`（45 条逐字转录 + provenance 未验证 + 零点列完全不出现，机器门 zero_keys=0）、三份 phase_config schema 与模板、`run_manifest` schema、`cpu_profile` 迁移为 oneOf[v1 legacy, v2 现行]。
收口时以下 5 项**只登记未动作**，需本任务闭合：

| # | 遗留 | 说明 |
|---|---|---|
| 6.3 | plugin 级默认（`weight_mode` 等）未进 `defaults.json` | 需按插件文档逐项登记或明确「无默认」 |
| 6.4 | per-module knobs 未进 `phase_config` | 模块级旋钮的归属与默认值 |
| 6.5 | 滤镜名匹配语义（设计示例 `"bader r"` vs 库键 `"Baader R"`） | 大小写/空格/别名的规范化规则未定义 |
| 6.6 | `cpu_profile.host.os_abi` 值域未冻结 | 需要枚举与校验 |
| 6.7 | `docs/DOCUMENT_INDEX.yaml` 与 `tests/test_index.csv` 的登记归属 | 两份索引的权威关系与同步方向 |

## 权威依据

- `ASTROCS_DESIGN.md §3.3`（配置分离：默认值 / 滤镜库 / 阶段配置）；
- `docs/plugins/infrastructure/18_cli.md` 与相关插件文档（只读引用）；
- `docs/contracts/CONFIG_CONTRACT.md`（CFG-001 交付的配置合同）；
- `ENGINEERING_SPEC.md §8`（登记项必须能红能绿）。

## 允许改

- `config/**`、`contracts/schemas/phase_config_*.schema.json`、`docs/contracts/CONFIG_CONTRACT.md`、`docs/DOCUMENT_INDEX.yaml`；
- 相应测试（`tests/config/**`）与机器门。

## 禁止改

- 科学默认值（SCI/ALG 冻结定义）——**只能登记与校验，不得改数值**；
- `lib/**`、`cli/**`、`docs/science|algorithms/**`。

## 验收门

- [ ] 5 项逐条结论（闭合 / 明确「无默认」/ 登记为设计缺口），**不允许含糊带过**；
- [ ] 6.5 的匹配语义给出**可执行规则 + 正反例**（能红能绿）；
- [ ] 6.6 值域冻结为枚举 + 校验器 + 负例；
- [ ] 6.7 明确两份索引的权威关系（单一事实源或明确同步方向）并落地校验；
- [ ] `tests/config` 全绿、无新增 skip。

## 交付物

1. 配置与合同改动；2. 机器门/校验器；3. 逐项结论表；4. 自证摘要。