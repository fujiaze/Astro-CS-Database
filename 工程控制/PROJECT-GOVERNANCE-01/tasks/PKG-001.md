# 任务：PKG-001 治理双平台安装树、清单与候选包

状态：`NOT_STARTED`
层：L7　依赖：INT-001, CI-001　文件域互斥组：S9

## 目标

Linux/Windows 安装树含唯一 CLI + 基建 + 算法模块 + CPU provider；产品 manifest 白名单/hash/provenance 与依赖锁一致；Alpha 前产物只有 SHA+哈希。

## 基线状态（编制时实测）

- `packaging/`、`cmake/` 安装规则、`tools/make_linux_release.py`、`make_windows_release.py`、`ci/validate_candidate.py`、`ci/select_candidate.py` 均绑定旧形态。
- **可执行名硬冲突（GAP-018）**：§10.1 要求 `ACSD Cli.exe` / `acsd_cli`，实际根 `CMakeLists.txt` 的 `add_executable(astrocs …)` 与 `cmake/install_layout.cmake` 的安装名为 `astrocs` / `astrocs_p1_*.so`；在 `packaging/`、`cmake/`、`docs/`、`ci/` 内检索 `acsd` **零命中**。
- `packaging/astrocs.product.json` 的 `product_version=0.11.0-alpha.1`（≠ `VERSION` 的 `0.11.0-alpha.2`）、`source_commit=9f6b72b5…`（≠ 当前基线）、`PLATFORM-RUNTIME`/`PLATFORM-IO` 仍为 `SKELETON`。
- `VERSION` 内容 `0.11.0-alpha.2`，`ci/checks.json` 的 `VERSION-CONSISTENCY` 仍以该值硬绑定。

## 权威依据
- ASTROCS_DESIGN.md §10.1（平台与形态）、§10.2（双平台构建）、§12（Alpha 前无版本信息）
- docs/ci/04_ARTIFACTS.md
- ENGINEERING_SPEC.md §7（Alpha 前代码与产物不含版本信息）

## 改动范围（文件域）
### 允许改
- `packaging/**`
- `cmake/` 安装规则
- `tests/packaging/**`

### 禁止改
- 负责人发布决定
- Alpha 前向程序/产物注入版本号
- 打包 ACR/GUI/旧 Session 重复件

## 步骤
1. 生成双平台安装树：唯一 CLI + infrastructure 模块 + algorithms 模块 + CPU provider + `config/**`。
2. 产品 manifest：白名单、逐文件 hash、provenance（源码 SHA/工具链/依赖锁）与 `ci/*.lock.json` 一致。
3. 普通构建与发布候选隔离存放；Alpha 前只用 commit SHA + 哈希标识。
4. 验证安装目录 cpu_profile 读写位置（UNIFIED_MODEL §3）。

## 验收门（前台独立复跑）
- [ ] 候选包白名单与 hash 校验 rc=0（给出校验命令与输出）
- [ ] 包内无 ACR/CUDA/hips_browser/旧 Session 重复件（给出包内容清单与断言）
- [ ] 干净环境安装后 doctor 与模块加载通过（给出环境与结果）
- [ ] 包内无版本号（Alpha 前）：给出 --version 行为与文件名/头抽查结果

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/PKG-001/logs
python3 ci/validate_candidate.py 2>&1 | tail -20; echo "rc=$?"
tar -tzf <linux-pkg> | head -40 | tee run/PROJECT-GOVERNANCE-01/PKG-001/logs/pkg-list.txt
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
