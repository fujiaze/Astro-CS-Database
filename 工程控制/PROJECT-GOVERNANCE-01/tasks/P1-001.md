# 任务：P1-001 治理 normalize 模块与固定 DAG

状态：`NOT_STARTED`
层：L4　依赖：DATA-001, AIO-001, RT-001　文件域互斥组：S6-P1

## 目标

normalize 的固定 DAG（ingest→calibration→cosmetic→background/noise→star_detection→psf→platesolve→photometry→noise_snr→drizzle→verify→publish）逐节点绑定唯一模块 entrypoint，禁止整阶段 Session facade。

## 基线状态（编制时实测）

- 现实现：`lib/phase1`、`lib/phase1_session` 加 `lib/calibration`、`lib/cosmetic`、`lib/star_detector`、`lib/dynamic_psf`、`lib/plate_solve`、`lib/photometric_calib`、`lib/snr_estimator`、`lib/drizzle`；命名与 23 插件文档的目标模块名不一致（见 MOD-001 映射表）。
- `lib/snr_estimator/**`、`tests/unit/p1_noise/**` 在编制时仍为未跟踪新增。
- `docs/plugins/algorithms_phase1/01..08` 已给出每模块 8 节规范（职责/权威/合同/公式/配置/ABI/错误/测试）。

## 权威依据
- ASTROCS_DESIGN.md §3.1（使命）、§3.2（节点流程）、§3.3（输入合同）、§3.6（硬约束）
- docs/plugins/algorithms_phase1/01_calibration.md … 08_drizzle.md（各 8 节）
- docs/science/ 与 docs/algorithms/ 相关篇章（只读，不改）

## 改动范围（文件域）
### 允许改
- `lib/algorithms/{calibration,cosmetic,star_detection,psf,platesolve,photometry,noise_snr,drizzle}/**`
- `tests/normalize/**`
- 各模块 `README.md`/`module.yaml`（与 MOD-001 映射表一致）

### 禁止改
- `docs/science/**`、`docs/algorithms/**`（只读）
- mosaic/export 模块
- 以整 Session 代替节点实现
- 改变科学公式与默认容差

## 步骤
1. 按 §3.2 建立 DAG 节点清单与边，每节点一行：节点名 → 输入对象 → 输出对象 → 唯一模块 entrypoint（引用 DATA-001 schema）。
2. 逐模块补 ENGINEERING_SPEC §4 的 7 项（README/module.yaml/公开头/实现/target/共址测试/端口合同引用）。
3. 去掉 Session facade：任何节点不得调用 `phase1_session` 的整阶段入口。
4. 为每节点建立独立 Oracle 与错误输入测试（合成数据生成器不得调用生产实现）。

## 验收门（前台独立复跑）
- [ ] DAG 节点与边与 §3.2 逐项一致（给出对照表；差异必须为 0 或有负责人批准的登记）
- [ ] 每算法模块可独立构建/加载/测试（给出逐模块 target 与测试命令）
- [ ] 合成链测试不调用完整旧 Phase1 Session（给出符号引用断言）
- [ ] normalize 相关 ctest 全过（给出命令与 rc=0）

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/P1-001/logs
ctest --test-dir build -R 'normalize|calibration|cosmetic|star|psf|plate|photometry|noise|drizzle' --output-on-failure 2>&1 | tail -30; echo "rc=$?"
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
