# 任务：P2-001 治理 mosaic 固定 DAG 与产品合同

状态：`NOT_STARTED`
层：L4　依赖：DATA-001, AIO-001, RT-001　文件域互斥组：S6-P2

## 目标

mosaic 固定科学流程（admit→coverage→sampling→upm fit/apply→rejection→integration→verify→publish）每节点唯一模块，coverage/validity/rejection/provenance 端口严格区分。

## 基线状态（编制时实测）

- 现实现：`lib/phase2`、`lib/phase2_session`、`lib/phase2_int`、`lib/phase2_rej`、`lib/phase2_samp`、`lib/phase2_upm`、`lib/hips_p2`、`lib/healpix_db`。
- `docs/plugins/algorithms_phase2/09_coverage.md` … `13_integration.md` 已定义 5 个模块的 8 节规范。
- UNIFIED_MODEL §2 明确 coverage / validity / rejection / support 各自语义且**都不是权重**。

## 权威依据
- ASTROCS_DESIGN.md §4.1（使命）、§4.2（固定科学流程）、§4.4（硬约束）
- docs/plugins/algorithms_phase2/09_coverage.md … 13_integration.md
- docs/design/UNIFIED_MODEL.md §2（coverage/validity/rejection/support）

## 改动范围（文件域）
### 允许改
- `lib/algorithms/{coverage,sampling,upm,rejection,integration}/**`
- `tests/mosaic/**`
- 各模块 `README.md`/`module.yaml`

### 禁止改
- normalize/export 模块
- 修改 SCI/ALG
- 把 coverage/support 当权重

## 步骤
1. 建立 §4.2 流程的节点清单与端口类型（引用 DATA-001 schema），每节点唯一 entrypoint。
2. 实现 admit 的 fail-closed：输入 HiPS 不满足合同即拒绝并给可诊断原因。
3. 分开 coverage（几何有效域）、validity（坏点/缺失）、rejection（污染推断）、provenance（来源链）四类端口，禁止互相冒充。
4. 为每节点建模块级测试与合成链测试。

## 验收门（前台独立复跑）
- [ ] DAG 顺序与端口类型与 §4.2 一致（对照表，差异 0）
- [ ] 不兼容 HiPS 在 admit 处 fail-closed（给出构造的坏输入与退出码/错误码）
- [ ] mosaic 相关 ctest rc=0（给出命令）
- [ ] 算法模块独立可加载测试通过（逐模块给命令）

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/P2-001/logs
ctest --test-dir build -R 'phase2|mosaic|coverage|sampling|upm|rejection|integration' --output-on-failure 2>&1 | tail -30; echo "rc=$?"
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
