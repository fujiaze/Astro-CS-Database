# 任务：GOV-001 收敛最高权威、根目录与废止治理入口

状态：`NOT_STARTED`
层：L0　依赖：BASE-001　文件域互斥组：S1-A

## 目标

让 `ASTROCS_DESIGN.md` 成为**唯一**最高权威入口，清掉与它并列的旧权威/旧状态入口，并把仍然把旧治理文本当权威对象的检查器改成新文档集的门——同时保证「旧权威重新出现会红灯」。

## 基线状态（编制时实测）

- 根目录存在 `ASTROCS_PROJECT_CONSTITUTION.md`(39903B)、`AstroCS_ENGINEERING_CONSTRAINTS.md`(10061B)、`REVIEW.md`(20936B)、`HANDOVER.md`(9216B)、`VERSION`；均不在 ENGINEERING_SPEC §7 的根固定条目表内（GAP-001）。
- `python3 tools/check_agents_gov.py` → **rc=1**，输出 `GOV_CHECK_FAIL missing=[main-only, amd64, 节点, cpu-only, 单入口, 资源门禁, 无硬编码, alpha/发布, 状态机, 不停工]`：检查器把旧 AGENTS.md 的 10 组字符串当硬编码期望。
- `python3 tools/doccheck/check_engineering_constraints.py` → **rc=1**，`verdict=CONSTRAINTS_FAIL`：仍以旧 `AstroCS_ENGINEERING_CONSTRAINTS.md` 为权威对象。
- `ci/checks.json` 中 `AGENTS-GOV` 的 `changed_paths` 仍为 `["AGENTS.md","memory.md","docs/governance/**"]`，`ENG-CONSTRAINTS` 为 `["AstroCS_ENGINEERING_CONSTRAINTS.md","AGENTS.md","docs/governance/**"]`。

## 权威依据
- ASTROCS_DESIGN.md §0（权威链：本文 > AGENTS > ENGINEERING_SPEC > CONTROL_PACK_SPEC > docs/ci > 插件文档；冲突以本文为准）
- ASTROCS_DESIGN.md §12（Alpha 前程序与代码不含版本信息；VERSION/CHANGELOG 仅内部助记）
- ENGINEERING_SPEC.md §7（仓库根固定条目；新根条目须先登记并经负责人确认）
- ENGINEERING_SPEC.md §8（`ci/checks.json` 唯一注册表；检查器覆盖「活动文档无陈旧版本号/历史状态冒充」）

## 改动范围（文件域）
### 允许改
- 根目录旧治理/旧状态入口的处置：`ASTROCS_PROJECT_CONSTITUTION.md`、`AstroCS_ENGINEERING_CONSTRAINTS.md`、`REVIEW.md`、`HANDOVER.md`、`VERSION`（删除，或移入 `docs/archive/**` 并标 `ARCHIVED_NON_NORMATIVE`；处置方式与理由写进自证摘要）
- `tools/check_agents_gov.py`、`tools/doccheck/check_engineering_constraints.py`（改造为新文档集门，或删除并由 CI-001 以 `CHK-*` 取代）
- `ci/checks.json` 中**仅本任务相关**条目：`AGENTS-GOV`、`ENG-CONSTRAINTS`、`VERSION-CONSISTENCY`、`VERSION-NAMESPACES`、`DOC-L0`、`GLOSSARY-DOCS`
- `README.md` 中「权威入口」小节（与 DOC-001 的边界：本任务只改权威声明与根条目表，术语/状态清单留给 DOC-001）

### 禁止改
- `lib/**`、`cli/**`、`runtime/**`、`tests/**` 中的实现
- `docs/science/**`、`docs/algorithms/**` 的公式与推导
- `ASTROCS_DESIGN.md`、`ENGINEERING_SPEC.md`、`CONTROL_PACK_SPEC.md`、`AGENTS.md` 本身（它们是权威，不是被治理对象）
- 不得为了让检查通过而放宽检查语义（ENGINEERING_SPEC §8：豁免只减不增，需负责人批准）

## 步骤
1. 处置旧权威入口：逐个判定「删除」或「归档 + `ARCHIVED_NON_NORMATIVE` 标注」，并确认 `ASTROCS_DESIGN.md §0` 权威链是仓库中唯一声明最高权威的位置。
2. 修正 `README.md` 的权威入口表：只指向 `ASTROCS_DESIGN.md`、`AGENTS.md`、`ENGINEERING_SPEC.md`、`CONTROL_PACK_SPEC.md`、`docs/ci/`、`docs/plugins/`、`docs/design/UNIFIED_MODEL.md`。
3. 改造旧检查器：`check_agents_gov.py` 的期望不得再是对某版本文本的字符串快照，改为「从 `AGENTS.md` 抽取硬禁令条款并断言其存在/非空/无旧权威词」的语义检查；`check_engineering_constraints.py` 改为以 `ENGINEERING_SPEC.md` 为权威对象，或整体删除并登记到 CI-001 的 `CHK-*` 迁移清单。
4. 处理版本绑定：`VERSION-CONSISTENCY` 现以 `--expected 0.11.0-alpha.2` 硬绑定旧版本号（实测 rc=0 属「旧值自洽」掩盖）；按 §12 改为 Alpha 前「程序和产物不含版本信息」的反向检查（出现版本号即红）。
5. 根目录条目对齐：列出当前顶层条目与 ENGINEERING_SPEC §7 的差集，对「文档未列但存在」的条目逐个给出处置（删除/归档/登记待负责人确认）；**不在本任务内擅自新增根条目**。
6. 建负例：在旧权威文本或旧版本号重新出现时，对应检查必须 rc≠0。

## 验收门（前台独立复跑）
- [ ] 活动面（`README.md`、`AGENTS.md`、`docs/*.md`、`ci/`、`tools/`、`tests/`）中旧宪章/旧工程约束不再作为上位权威或必读入口出现；命中仅允许在带 `ARCHIVED_NON_NORMATIVE` 的归档处
- [ ] `python3 tools/check_agents_gov.py; echo rc=$?` → rc=0，且注入旧权威词后 rc≠0
- [ ] `python3 tools/doccheck/check_engineering_constraints.py; echo rc=$?` → rc=0（或被删除且 `ci/checks.json` 已同步移除该 ID）
- [ ] `git diff --stat` 不含 `lib/**/src`、`cli/**`、`tests/**` 实现文件
- [ ] 根目录顶层条目差集已写入自证摘要，未新增未经确认的根条目

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/GOV-001/logs
python3 tools/check_agents_gov.py; echo "rc=$?" | tee -a run/PROJECT-GOVERNANCE-01/GOV-001/logs/gates.txt
python3 tools/doccheck/check_engineering_constraints.py; echo "rc=$?" | tee -a run/PROJECT-GOVERNANCE-01/GOV-001/logs/gates.txt
ls -A . | sort > run/PROJECT-GOVERNANCE-01/GOV-001/logs/root-entries.txt
git diff --stat | tee run/PROJECT-GOVERNANCE-01/GOV-001/logs/diffstat.txt
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
