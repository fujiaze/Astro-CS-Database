# 任务：QA-001 接通双平台 CI 与完整测试矩阵

状态：`NOT_STARTED`
层：L7　依赖：INT-001, CI-001　文件域互斥组：S9

## 目标

GitHub Actions 按 docs/ci/02_PIPELINE.md 接通 Linux/Windows 构建、静态/文档/合同、单元/模块、合成科学、sanitizer/coverage、gates、候选打包；workflow 与注册表零孤儿。

## 基线状态（编制时实测）

- `.github/` 已存在（含 workflows 绑定与 `ci/workflow_binding.json`、`ci/validate_workflow_binding.py`、`ci/verify_actions_lock.py`）。
- `ci/checks.json` 的 145 项按 profiles（fast/linux-main/windows-main）分组。
- docs/ci/02_PIPELINE.md 定义 job 链：build-linux → build-windows → static+doc+contract → unit+module → synthetic-science → gates-report → package-candidates → artifacts。

## 权威依据
- docs/ci/02_PIPELINE.md（流水线与 job 依赖）、03_GATES.md（门禁）、04_ARTIFACTS.md（产物留存）
- docs/ci/CI_SPEC.md §2（push 到 main 全量跑）、§7（失败策略）
- ASTROCS_DESIGN.md §11.2（验证层级）

## 改动范围（文件域）
### 允许改
- `.github/workflows/**`
- `ci/**` 的流水线绑定与校验器
- `tests/**` 测试注册（不改测试语义）

### 禁止改
- 降低 P0/P1 门
- 跳过 Windows
- 把真实数据测试冒充合成测试
- 用 continue-on-error 吞失败

## 步骤
1. 按 02_PIPELINE.md 接通各 job 与依赖顺序，每 job 有超时与日志留存。
2. workflow ↔ checks registry 双向绑定检查（零孤儿：注册表项都有 job，job 步骤都有注册项）。
3. 失败传播：任一 P0/P1 红即阻塞候选打包。
4. 验证 artifacts 留存内容符合 04_ARTIFACTS.md（Alpha 前不带版本号，仅 SHA+哈希清单）。

## 验收门（前台独立复跑）
- [ ] workflow 与 checks registry 零孤儿（给出双向差集为空）
- [ ] Linux 与 Windows 在同一 SHA 全绿（给出 run 链接或 CI 结果摘要）
- [ ] 故意注入失败检查会阻塞候选打包（给出实验证据）
- [ ] artifacts 留存内容符合 04_ARTIFACTS.md（给出留存清单）

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/QA-001/logs
python3 ci/validate_workflow_binding.py 2>&1 | tail -20; echo "rc=$?"
python3 ci/verify_actions_lock.py 2>&1 | tail -10; echo "rc=$?"
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
