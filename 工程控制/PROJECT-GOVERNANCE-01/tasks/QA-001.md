# 任务：QA-001 修掉最后一条测试红 core_pipeline（GAP-036）

状态：NOT_STARTED　层：L1　依赖：无（可立即开工）

## 现象
全量 ctest 426 项中**唯一失败**：`core_pipeline`（#5）。它是全项目最后一条红，修掉即达成「ctest 全绿」这一阶段门。

## 已知线索
- 根因方向：该测试依赖的夹具被 GOV-002（提交 `b7b2dea7`）归档 ⇒ 运行时找不到输入而失败（登记为 GAP-036）；
- 相关历史：`runtime/**` 已由 ROOT-008 迁入 `lib/infrastructure/{aio,scheduler,observability,pipeline}/**`，若该测试引用了旧路径（`lib/core/src`、`runtime/**`、`lib/phase2/src` 之类），需按新址改绑；
- 另一处同类先例：`tests/runtime/test_rt00{3..7}.py` 曾指向 `lib/core/src`（RT-001 发现）——先确认 `core_pipeline` 是否同一失效型。

## 要做的
1. **复现并定位**：`ctest --test-dir build -R core_pipeline --output-on-failure`，给完整失败输出；找到它实际执行什么（CTest 定义在哪个 CMakeLists、驱动脚本/二进制是什么），定位**确切根因**（缺夹具 / 路径旧 / 判据过时 / 真缺陷）；
2. **按根因修**（三选一，给依据）：
   - 缺夹具 ⇒ 恢复夹具（从 git 历史取回被归档的文件，或在测试内生成等价合成夹具），**不得**删测试或放空断言；
   - 路径旧 ⇒ 改绑到 ROOT-008 后的现行位置；
   - 判据过时 ⇒ 说明为什么过时，按现行权威重写判据并给负例；
3. **红→绿**：给修前（rc=1 + 失败输出）与修后（rc=0 + 通过输出）**两次真实输出**；
4. **回归**：`ctest --test-dir build` 全量复跑，报告计数（目标：**426 项 / 426 通过**）；
5. 若该测试实质已无价值（例如测的是已退役能力），**不得擅自删除**：给出「能力去向 + 为什么可退役」的证据，交前台裁决。

## 纪律
零 git 写；不得改 `问题扫描/**`、`reports/**`；不得改科学语义；不得用 `--repeat`、跳过、放宽断言制造绿灯；命令带 timeout，日志落 `run/PROJECT-GOVERNANCE-01/QA-001/logs/`。

## 交付
根因说明（含证据）→ 修复改动 → 红绿两次输出 → 全量 ctest 计数 → 自证摘要 `run/PROJECT-GOVERNANCE-01/QA-001/自证摘要.md`，发给前台。