# 任务：OBS-001 治理观测事件、资源门与运行图

状态：`NOT_STARTED`
层：L5　依赖：RT-001　文件域互斥组：S6-D

## 目标

统一 progress/resource/artifact/backend/final 事件与运行图，资源监控覆盖 CPU/内存/IO/队列/worker 均衡，并实现利用率门（>10s heavy 平均 ≥85%；连续 10s <60% 或单活跃线程判失败）。

## 基线状态（编制时实测）

- 现实现：`lib/orchestrator`、`runtime/`、`cli/monitor.h`、`cli/resource_gate.h`、`cli/resource_recorder.h`、`cli/memory_growth.h`、`ci/resource_monitor.py`。
- 根目录被运行产物污染（GAP-021，实测）：工作区根顶层 126 个条目（tracked 46 / untracked 80），含 `astrocs_run_*.json` **62 个**、`astrocs_p1sess_{neg,perf,props,test}`、`alloc_report.json`、`alloc_samples.csv`、`resource_samples.csv`、`resource_summary.json`、`worker_balance.csv`、`run_context.json`、`p8..p15a-files.patch`；另有 `build`、`out`、`logs`、`Testing`、`.pytest_cache`、`worktrees` 等未登记目录。`engineering/` 虽在 §7 白名单内但**为空目录**。
- `docs/plugins/infrastructure/21_observability.md` 定义事件与运行图职责。

## 权威依据
- ASTROCS_DESIGN.md §8（资源与利用率门）
- docs/plugins/infrastructure/21_observability.md
- ENGINEERING_SPEC.md §9（结构化日志走 JSONL）、§10（利用率门禁）、§7（运行产物不得散落根目录）

## 改动范围（文件域）
### 允许改
- `lib/infrastructure/observability/**`
- `tests/monitoring/**`
- `contracts/schemas/*event*.json`、`*resource*.json`
- 清理本任务范围内的根目录散落运行产物（须登记清单）

### 禁止改
- 静默吞掉资源失败
- 运行产物写仓库根
- 降低门禁阈值

## 步骤
1. 统一事件类型与 run graph：progress/resource/artifact/backend/final，含 schema_version/run_id/时间戳/必需字段。
2. 记录进程/线程 CPU、RSS/PSS 与增长、I/O、队列深度、work units、worker 均衡。
3. 实现利用率门：>10s 的 heavy 区间平均利用率 ≥85%；连续 10s <60% 或单活跃线程 → 失败（退出码 10）。
4. 负例：低利用率、内存无界增长、事件乱序/重复/缺字段。
5. 把根目录散落运行产物按 §7 归位或登记处置（不得删除他人预存文件，见 BASE-001 边界）。

## 验收门（前台独立复跑）
- [ ] 资源报告 schema 校验 rc=0 且字段完整（给出报告路径与字段清单）
- [ ] 门禁阈值边界测试通过（85%/60%/10s 边界逐条给用例与结果）
- [ ] 低利用率与内存无界负例必败（给出注入方式与退出码 10）
- [ ] 运行产物只落 output_dir/run：给出本次运行前后的根目录条目对照

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/OBS-001/logs
python3 tools/monitoring/check_log_contract.py --selfcheck; echo "rc=$?"
ls -A . | sort > run/PROJECT-GOVERNANCE-01/OBS-001/logs/root-before.txt
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
