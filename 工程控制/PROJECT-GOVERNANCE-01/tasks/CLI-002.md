# 任务：CLI-002 export 预检/会话不一致缺陷 + tests/cli 红收口（GAP-034）

状态：NOT_STARTED　层：L1　依赖：ROOT-008（已完成：cli/** 已迁 `lib/infrastructure/cli/**`）

## 现象（此前实测）
`tests/cli` 整体 `Ran 125, F9 E1 S21`——其中 **8 条红**指向 `export` 的预检/会话不一致（登记为 GAP-034 / CLI-002），另 1 条红与 21 条跳过需逐条定性。

## 要做的
1. **先复现并分类**：`python3 -m pytest tests/cli -x -q`（或该目录实际的 runner；先读其 README/CMake 注册方式），把 9 红 + 21 跳过**逐条定性**：
   - 真缺陷（export 预检与运行期会话不一致：预检通过但运行期拒绝，或反之）；
   - 判据过时（测试仍按迁移前命令树 `phase2 run` / 旧旗标调用）；
   - 能力已退役（测试该删或该改测新能力）。
2. **修 export 缺陷**（若确为真缺陷）：定位预检与运行期**同一判据的两个实现**为何分叉，收敛为**单一判据来源**（同一函数/同一契约字段），给红→绿两次真实输出；
3. **过时判据改绑**：按现行命令树与 `docs/api/CLI_PROTOCOL_V1.md` 改绑；**不得**用放宽断言或删测试收口；
4. **21 条跳过**：逐条给「为什么跳过」（平台限定 / 缺依赖 / 待实现），并把「在 Linux 上本应可跑却被跳过」的挑出来改成真跑（参考：`phase2_sampler_parallel.OneTvsTwoTDeterminism` 在 Linux 恒 SKIPPED 已被 SCI-FIX-WEIGHT 修掉，可照其做法）；
5. **门**：`tests/cli` 全绿（或每条例外有明确依据与登记）+ 该目录的 CI 注册项通过。

## 纪律
零 git 写；不得改 `ci/**`（需改动只登记转 W4-A3）；不得改产品科学语义；不得动 `lib/algorithms/**` 科学实现（若缺陷根因在那里，停在报告并交前台派研究线）；命令带 timeout，日志落 `run/PROJECT-GOVERNANCE-01/CLI-002/logs/`。

## 交付
逐条定性表（红/跳过 → 类别 → 处置 → 依据）+ 修复改动 + 红绿证据 + 门结果 + 自证摘要 `run/PROJECT-GOVERNANCE-01/CLI-002/自证摘要.md`，发给前台。