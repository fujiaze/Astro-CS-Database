# 任务：CI-003 检查器现代化与门7 极性证据债

状态：NOT_STARTED
层：L2　依赖：CI-001（已完成）、DOC-001（已完成）、ROOT-007（已完成）
文件域互斥组：S5-D（`tools/doccheck/**` 的绑定面 + `ci/tests/**` 与极性证据产物）

## 背景（前台裁决 2026-09-16：CI-001 本轮范围闭合 = PASS，未证清单转本任务）

### ① 门7 极性证据债（CI-001 第二轮自报 31 门：5 双极实证 + 7 inconclusive + 19 not_proven）

`ENGINEERING_SPEC §8` 要求每项检查**能红能绿**。现状是 26 门未取得可复核的极性证据，逐门原因与归属已由 CI-001 登记在 `run/PROJECT-GOVERNANCE-01/CI-001/evidence/polarity_evidence.json`（含 `polarity_probe.py`）。

⇒ 本任务对 26 门**逐门**做且只做两件事之一：
1. **补齐极性证据**：给「正例（正常输入 → rc=0）」+「负例（注入缺陷 → rc≠0）」的**可复跑脚本**与逐字输出；
2. **显式登记不可双极**：写明「为什么在该环境无法构造负例」「归属哪个任务/哪类条件」「未来如何在 CI 上验证」——**不允许**含糊带过或标 PARTIAL 了事。

### ② 陈旧检查器绑定（检查器硬编码指向已删除路径）

| 检查器 | 症状 | 出处 |
|---|---|---|
| `tools/doccheck/check_doc_index.py` | 12 项里唯一 FAIL = `control_archive_dir_readme`：硬编码绑定 ROOT-007 已删除的 `engineering/control/archive/.../README_ARCHIVED.md` ⇒ **恒红** | DOC-001 门4 |
| `tools/check_l0_docs.py` | 绑定已删除的 `REVIEW.md` 与旧 `docs/review/**` ⇒ 恒红/空转 | DOC-001 附注（GAP-001/016） |

⇒ 迁移为新文档集的门（活动文档集合 + 现行权威链），**并保证「旧权威重新出现会红灯」**；不得为了变绿删断言或放宽。

### ③ GAP-027 的 P2 余项

12 处 fail-closed 已由 CI-001 修（含前台定位的 `_repo()` 静默回落真凶）；**P2 的 12 条行号清单**（CI-001 已在审计产物中登记）在本任务逐条判定：修 / 登记后续 / 判为非缺陷（给理由）。

## 允许改

- `tools/doccheck/**`（检查器绑定与判据迁移）；
- `tools/check_l0_docs.py`；
- `ci/tests/**`、`ci/polarity_evidence.json`、`ci/polarity_probe.py`（极性证据载体）；
- 如有必要的新增**机器门**测试（放 `tests/quality/**` 或 `ci/tests/**`）。

## 禁止改

- `ci/checks.json` 的**注册项 ID/命令**（收敛已完成；如确需改须先向前台申请）；
- 产品代码 `lib/**`、`cli/**`；`docs/**`（DOC-001 已收口；如必须改文档，只登记不改）；
- 科学/算法文档。

## 硬约束

- 不得用 skip/放宽断言/删检查制造绿灯；每处改动必须给**能红能绿**的双向证据；
- 新增豁免 = 0（`ci/exemptions.json` 保持空）；
- 零 git 写；日志落 `run/PROJECT-GOVERNANCE-01/CI-003/logs/`。

## 验收门（前台独立复跑）

- [ ] 26 门逐门结论（补证 / 显式登记），**计数一致**；
- [ ] 两个陈旧检查器：迁移后正例 rc=0 + 负例（旧权威回归/缺文件）rc≠0；
- [ ] P2 的 12 条逐条判定；
- [ ] `ci/exemptions.json` 仍为空、`waivable` 无放宽；
- [ ] 越界自查。

## 交付物

1. 检查器迁移与极性证据；2. 26 门 + 12 条处置表；3. 复跑证据；4. 自证摘要。