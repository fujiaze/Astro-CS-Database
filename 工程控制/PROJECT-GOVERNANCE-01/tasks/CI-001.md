# 任务：CI-001 重建新规范机器检查入口与治理门

状态：`NOT_STARTED`
层：L6　依赖：GOV-001, DOC-001, MOD-001　文件域互斥组：S5-C

## 目标

提供 `python3 ci/run_checks.py --all/--check/--json-out`，注册表 ID 收敛到 `CHK-*` 语义并移除旧权威绑定，每项 P0/P1 门都能绿能红。

## 基线状态（编制时实测）

- 文档要求 `ci/run_checks.py`（docs/ci/01_CHECKS.md §5）；实际入口为 `ci/run.py`，**无** `run_checks.py`。
- `ci/checks.json` 共 145 项，ID 为旧语义；`AGENTS-GOV`、`ENG-CONSTRAINTS` 在编制时实测 **rc=1**；`VERSION-CONSISTENCY` 以 `--expected 0.11.0-alpha.2` 硬绑定旧版本号。
- docs/ci/01_CHECKS.md §2 列出 27 个 `CHK-*` 检查项与 P0/P1/P2 分级。

## 负责人裁决（2026-09-16）

- 本波**不删除、不改语义**旧治理检查器（`tools/check_agents_gov.py`、`tools/doccheck/check_engineering_constraints.py`）与对应注册项：在迁移映射里登记为 `RETIRE-PENDING-GOV-001`，实际退休归 W2 的 GOV-001。
- 理由：避免与 GOV-001 的文件域重叠；注册表语义迁移（145 → `CHK-*`）可在不删旧项的前提下完成。
- 本任务必须处理 `python3 ci/validate_registry.py --registry ci/checks.json --strict` 的基线 rc=1（R10：linux-main 末位类目）与 `ci/exemptions.json` 缺失（docs/ci/01_CHECKS.md §1 要求）。

## 权威依据
- docs/ci/01_CHECKS.md §1-§5（注册表原则、检查项清单、门禁分级、新增流程、运行方式）
- docs/ci/CI_SPEC.md §3-§4（检查项总表、门禁 P0/P1 必须 0）
- ENGINEERING_SPEC.md §8（ci/checks.json 唯一注册表；每项有正例与负例；豁免只减不增）

## 改动范围（文件域）
### 允许改
- `ci/**`
- `tools/**` 检查器（含删除旧治理检查器）
- `tests/quality/**`

### 禁止改
- 用 waiver 掩盖红灯
- 修改产品行为
- 新增无负例的门
- 删除检查项而不登记迁移

## 步骤
1. 实现 `ci/run_checks.py`，支持 `--all`、`--check <ID...>`、`--json-out <path>`，保持确定性执行与每项超时。
2. 把注册表 ID/分级收敛到 `CHK-*`（docs/ci/01_CHECKS.md §2），并给出旧 ID → 新 ID 的完整迁移映射（145 项逐项归并或删除并说明）。
3. 实现检查器覆盖：模块 manifest/注册表/target/产品清单一致、端口引用有效 DATA 合同、算法引用有效 SCI/ALG、核心合同有独立测试、AGENTS.md 硬禁令存在、删除/重命名无悬空引用、活动文档无陈旧版本号/历史状态冒充、Git diff 映射到受影响合同与最小测试集。
4. 每项 P0/P1 门提供能绿能红的确定性自测（正例 rc=0、负例 rc≠0）。
5. 清理旧权威绑定：不得再以 `AstroCS_ENGINEERING_CONSTRAINTS.md` 或某版 AGENTS.md 文本快照为判据。

## 验收门（前台独立复跑）
- [ ] `python3 ci/run_checks.py --all --json-out ci_result.json` → 可执行并产出机器 JSON（给出 schema 与首屏汇总）
- [ ] 注册表所有入口文件存在、超时明确、无孤儿 ID（给出断言命令）
- [ ] 每个 P0/P1 门都有负例且负例 rc≠0（给出逐门证据）
- [ ] 旧 ID → 新 ID 迁移映射覆盖 145/145，无静默丢弃

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/CI-001/logs
python3 ci/run_checks.py --all --json-out run/PROJECT-GOVERNANCE-01/CI-001/logs/ci_result.json; echo "rc=$?"
python3 -c "import json;d=json.load(open('run/PROJECT-GOVERNANCE-01/CI-001/logs/ci_result.json'));print(len(d.get('checks',[])))"
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
