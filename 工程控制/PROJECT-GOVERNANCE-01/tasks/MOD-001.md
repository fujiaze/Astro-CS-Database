# 任务：MOD-001 建立 23 插件模块的一致性映射门

状态：`NOT_STARTED`
层：L1　依赖：GOV-001, DATA-001　文件域互斥组：S3-B

## 目标

把 docs/plugins/ 23 篇规范与目标目录/README/module.yaml/公开头/单 entrypoint/target/产品清单/测试建立一一机器映射，并让缺项必红。

## 基线状态（编制时实测）

- `docs/plugins/00_INDEX.md` 已列出 23 个模块名：calibration cosmetic star_detection psf platesolve photometry noise_snr drizzle / coverage sampling upm rejection integration / projection resample fits_output / aio cli runtime benchmark observability gaia_xpsd_client hips_browser。
- 现有实现命名与之不符：`lib/star_detector`、`lib/plate_solve`、`lib/photometric_calib`、`lib/snr_estimator`、`lib/dynamic_psf`、`lib/hips_p2`、`lib/phase2_*`、`lib/phase3_*`、`lib/orchestrator`、`lib/io`、`lib/core`、`lib/common`。
- 现有 `modules/*/module.yaml`、`docs/modules/registry/*.md` 与 `tools/check_module_readmes.py`（实测 rc=0，只查 5 个模块 README 的链接）均绑定旧命名。

## 负责人裁决（2026-09-16）

- **接受**「本任务自身门全绿 + 不新增 tests/quality 失败」的验收口径；前提是执行前先把基线失败集合冻结成文件（`run/PROJECT-GOVERNANCE-01/BOUNDARY-w1/quality-baseline.txt`），且执行后逐字比对。
- 两条既存失败的真实归属：`test_01_real_repo_passes` → DOC-001 的文档债务；`test_t13b_check_gate_is_last_linux_main_entry` → CI-001 的注册表接线。**MOD-001 不得为过门去改它们**。
- `tests/quality` 中依赖 `设计大纲/` 的锚点用例（ROOT-001 会删除该目录）属过渡态，两侧不得互相等待；删除后若该用例由红转绿，按「失败集合减少」如实记录。

## 权威依据
- docs/plugins/00_INDEX.md §2（模块总表 23 篇）、§3（每篇 8 节模板）、§5（维护规则）
- ENGINEERING_SPEC.md §4（每模块必备 7 项）、§8（manifest/注册表/target/产品清单一致）
- ASTROCS_DESIGN.md §7.3（模块与 DLL/SO 边界）

## 改动范围（文件域）
### 允许改
- `tools/**` 新增/改造模块一致性检查器
- `ci/checks.json` 中本任务对应登记项
- `tests/quality/**`
- `docs/modules/**` 生成的映射索引（不改模块规范本身）

### 禁止改
- 模块源码迁移（归 ARCH-001/INT-001）
- 中央 CMake 产品装配（归 INT-001）
- 科学语义与 `docs/plugins/**` 内容

## 步骤
1. 写出期望映射表：23 个模块 ID → 目标目录 `lib/algorithms/<id>` 或 `lib/infrastructure/<id>` → README → module.yaml（ID/版本/ABI/端口/schema 链接/entrypoint）→ 公开头 → target → 产品清单条目 → 共址测试；产物落 `docs/modules/MODULE_MAP.yaml`（人机可读）。
2. 实现检查器（建议 `tools/quality/check_module_map.py`）：对 23 个模块逐一判定 CONTRACT_READY / IMPLEMENTED / INSTALLED 并输出机器 JSON；状态词只用 ASTROCS_DESIGN §11.3 词表。
3. 检查器必须能识别 facade/no-op：导出符号存在但无可执行路径、或 entrypoint 直接转发到整阶段 Session，判为 NOT_IMPLEMENTED。
4. 注册到 `ci/checks.json`（ID 形如 `CHK-MODULE-MANIFEST`，P0），并为每类失败提供正例与负例。

## 验收门（前台独立复跑）
- [ ] 23/23 模块在映射表中都有唯一一行（计数断言 23 == 23）
- [ ] 检查器正例 rc=0；五类负例（缺 manifest / 重复 entrypoint / 无 target / 无测试 / 悬空合同引用）各自 rc≠0
- [ ] `python3 -m unittest discover -s tests/quality -t tests/quality` 的失败集合与 `run/PROJECT-GOVERNANCE-01/BOUNDARY-w1/quality-baseline.txt` **逐字一致**（不新增、不删除、不跳过、不注释、不放宽）
- [ ] 两条既存失败 `test_01_real_repo_passes`（docs/science/v6/observation/OBSERVATION_MODEL_REVIEW.md 的 ADU/e⁻ 单位歧义）与 `test_t13b_check_gate_is_last_linux_main_entry`（ci/checks.json linux-main 末位，CI-001 域）已登记为**既存偏差转交**（写明根因与归属任务），不计入 MOD-001 失败
- [ ] `git diff --stat` 不含 `docs/science/**`、`ci/checks.json`
- [ ] 检查器输出 JSON 里没有把 facade/no-op 判为 IMPLEMENTED 的条目（人工抽查 + 至少 1 个负例证明）

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/MOD-001/logs
python3 tools/quality/check_module_map.py --json-out run/PROJECT-GOVERNANCE-01/MOD-001/logs/map.json; echo "rc=$?"
python3 -m unittest discover -s tests/quality -t tests/quality 2>&1 | tail -20; echo "rc=$?"
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
