# 任务：INT-001 汇总目标构建图、注册表与产品装配

状态：`NOT_STARTED`
层：L6　依赖：CLI-002, CLI-003, P1-002, P2-002, P3-002, CPU-001, OBS-001　文件域互斥组：S8

## 目标

根 CMake 只装配目标 `lib/algorithms` 与 `lib/infrastructure` targets；消除重复旧 target/Session/facade 编译路径；模块注册表/module.yaml/target/产品清单/安装文件五方一致。

## 基线状态（编制时实测）

- 根 `CMakeLists.txt` 43614B（9月16日），同时包含大量旧命名 STATIC 库与 Session 库。
- `packaging/astrocs.product.json`、`cmake/` presets、`modules/*/module.yaml`、`docs/modules/registry/*.md` 均绑定旧命名。
- ACR 必须 DORMANT 且不进生产；hips_browser 不进产品 manifest（ASTROCS_DESIGN §1.3、§7.1）。

## 权威依据
- ASTROCS_DESIGN.md §7.1-§7.3（唯一结构、DLL/SO 边界、唯一 entrypoint）、§10（双平台发行与安装）
- ENGINEERING_SPEC.md §4（每模块 7 项）、§7（唯一根 CMake，不引入第二构建入口）
- ASTROCS_DESIGN.md §1.3（ACR/GUI 非目标）

## 改动范围（文件域）
### 允许改
- `CMakeLists.txt`、`cmake/**`
- 中央 module registry
- `packaging/astrocs.product.json` 的模块装配部分
- 删除已迁移旧路径（配合 ARCH-001 的迁移清单）

### 禁止改
- 修改科学实现
- 引入第二套构建入口
- 把 ACR/hips_browser 装入生产 manifest
- 保留重复实现

## 步骤
1. 根 CMake 只装配目标 algorithms/infrastructure targets；删除旧 STATIC/Session/facade 编译路径（以 ARCH-001 迁移清单为准）。
2. 统一五方：module registry ↔ module.yaml ↔ CMake target ↔ 产品清单 ↔ 实际安装文件。
3. 验证每生产 DAG 节点映射唯一 DLL/SO + 唯一 entrypoint。
4. 验证 ACR 与 hips_browser 不在产品 manifest。
5. 全量 Release 构建 + 安装树加载测试。

## 验收门（前台独立复跑）
- [ ] `cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release && ninja -C build` → rc=0（给出日志与耗时）
- [ ] 安装树模块可逐个加载且无旧重复实现（给出逐模块加载结果）
- [ ] INSTALLED 集合 == 产品清单集合 == 安装文件集合（给出三方对照）
- [ ] ACR/hips_browser 不在 manifest（给出 grep 断言）

## 证据命令
```bash
mkdir -p run/PROJECT-GOVERNANCE-01/INT-001/logs
cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release 2>&1 | tail -5; echo "rc=$?"
ninja -C build 2>&1 | tail -20; echo "rc=$?"
```

## 执行规则（每个任务都适用）

- 开工前按 `AGENTS.md §1` 读完本任务「权威依据」列出的全部条款；未读不开工。
- 只改本文件「改动范围」声明的文件域；**不顺手改无关代码**；不改 `docs/science/**` 公式与 `docs/algorithms/**` 推导。
- 科学公式、权重/variance/ivar/SNR 定义、排异规则、归约顺序、默认容差**一律只读**（ENGINEERING_SPEC §3）。
- 工作区在本控制包编制前已有大量预存修改与未跟踪文件（见 BASE-001）：**不得** reset/stash/clean/checkout，**不得**把它们混进本任务的改动。
- SubAgent 零 git 写权限：不 commit、不 push、不建分支；改动留在工作区并交自证材料，由前台按任务原子提交。
- 所有外部命令带 `timeout`，日志落 `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/`（`run/` 已 gitignore，不入库）。
- 报告「完成」必须附：命令、退出码、关键输出片段、产物路径。禁止用「环境问题/工具问题」掩盖失败。
- 发现文档冲突、科学歧义或无法证明的状态：登记 `BLOCKED`（控制包内）或 `UNRESOLVED`（GAP_AUDIT 内），不得自行选口径。

## 交付物

1. 限「改动范围」内的代码/合同/配置改动；
2. `run/PROJECT-GOVERNANCE-01/<任务ID>/logs/` 下的命令日志与机器输出；
3. 自证摘要（字段：任务ID / 改动文件清单 / 逐条验收命令与退出码 / 未决项）。
