# SCIENCE_CORRECTNESS —— 科学正确性优先（负责人指令 2026-09-16）

## 一句话

**科学文档必须正确。** 有独立证据证明它错了，就改它——不需要为"它是冻结的"而保留错误，也不需要等负责人逐条批准。

依据：负责人原话「**我不要遵循旧制，我要求科学文档必须是正确的**」，与 `AGENTS.md §8`（科学歧义才问负责人）一致：
**证据能定的事，执行行自己定；证据不能定的，才上呈。**

## 判定规则（四条，取代旧的"一律不得修改"）

| 情形 | 处置 | 谁决定 |
|---|---|---|
| 文档与**外部标准/文献**不符（如 FITS WCS Paper II、IVOA 规范） | **改文档**（+ 改实现，若实现也随之错） | 执行行，凭标准原文 + 实验 |
| 文档与**可复现实验**不符（如蒙特卡洛证明某默认值统计上不成立） | **改文档**（或改实现，取决于哪边被证明错） | 执行行，凭实验（脚本 + 输出 + 样本量） |
| 文档**自相矛盾**（同文两处互斥） | 判定哪一处与标准/实验一致，改另一处 | 执行行，凭证据 |
| 两份权威文档互斥**且证据无法判定** | 上呈负责人 | 负责人 |

## 变更 claim（每次订正必填，落在本文件 §登记表）

订正科学文档时必须在同一提交里登记一条 claim：**改了什么 / 依据什么证据（可复跑）/ 影响面（哪些模块、合同、registry、测试）/ 版本递增**。

## 登记表

| # | 日期 | 文档 | 改什么 | 证据（可复跑） | 影响面 | 状态 |
|---|---|---|---|---|---|---|
| SC-010 | 2026-09-17 | `docs/science/ASTROMETRY.md`（新增 §11a + §15 门项 + §5a 路径锚同步）、`docs/algorithms/GATES_AND_TOLERANCES.md`（§2 增 `SNR_det` 行、§3 重写 G-P1-WCS-CLOSURE 并新增 G-P1-WCS-CLOSURE-REPRO、新增 §4a 台账重锚、§5 变更记录）、`docs/algorithms/PLATESOLVE.md`（§11.4 F1 与回归锚标历史值）、`lib/algorithms/platesolve/memory.md`（重锚登记）、`tools/astrometry/closure_metric.py`（新：口径唯一实现 + 门）、`tests/unit/p1wcs/CMakeLists.txt`（新增 ctest `p1wcs_closure_metric_gate`） | **天测精度外部闭环口径冻结（E2E-FIX-001 批次 1）**：① **旧口径不可复现**——legacy `0.897 px` 的匹配半径/星选/统计量均未冻结，仅把半径 1″→5″ median 即漂 13.5%（T3 0.5410″→0.6140″）⇒ 冻结 v1：样本 = `p1_sources.json` 中 `x,y` 有限 ∧ `snr>20`（**新增 §2 定义行 `SNR_det := (peak − background)/noise_sigma`**，与 `SNR_peak := A_fit/σ_bg` 不同源、禁止互换）、超 20000 按 flux 降序截断并记录 `sample_capped`；星表 = 本仓 Gaia DR3 XPSD 视场单锥 `G<18`；**匹配半径唯一 1.0″**；统计量 median 且**必须同报** `n_matched`/`match_rate`/`p95`/`max`；solved(CD+SIP) 与 frame_header **分别报告**；`s0 = 3600·sqrt\|det(CD)\|` 同报。② **历史值保留并标注不再作为门**：`0.1431″`（memory.md:32）只在 T2/T3 档场复现（0.1472/0.1584″），T4 实测 0.2803–0.3588″（2.0–2.5×）；`0.897 px`/`0.772 px`（memory.md:55-56）单值不可复现。③ **新增可复现门 `G-P1-WCS-CLOSURE-REPRO`**（发布门=Y，容差 **0 px**，判据含「记录声明的 n_matched/median/p95/max/match_rate 必须能由该记录自带残差向量 + 声明半径重新导出」⇒ 半径改大而不记录必红）。④ **G-P1-WCS-CLOSURE 阈值仍 UNJUSTIFIED（发布门=N）**：实测值取自 UNIT-001（XISF 母版单位）未修复的输入，修复后须整体复跑才可定阈。⑤ 顺带机械同步：`docs/science/ASTROMETRY.md` §5a 的 `lib/phase3_session/p3_wcs.cpp` → `lib/algorithms/projection/p3_wcs.cpp`（ROOT-008 迁移后锚失效 ⇒ `tools/science_contract_lint.py` 由 FAIL 转 PASS；DISPATCH 附录 G 锚存活义务，只改路径不改语义）。 | **可复跑**：`python3 tools/astrometry/closure_metric.py selftest`（合成场两口径各两跑必绿 + 7 类注入必红 + 2 类缺输入 fail-closed 必红，rc=0）；`ctest -R p1wcs_closure_metric_gate`；真实数据记录 `run/PROJECT-GOVERNANCE-01/E2E-FIX-001/out/rec_*`（T3/T2/T4 × solved/header；**T4 用 `det2/n1` 与 `det3/n1` 两次独立全链路运行 ⇒ 记录逐位相同**，median 0.107658 px、n_matched 6265）；门矩阵 `run/PROJECT-GOVERNANCE-01/E2E-FIX-001/logs/gate_check_matrix.log`（4 正例 PASS；E2E 旧 closure 记录 6 条 fail-closed 判红；6 类注入 + 口径混用全部判红）；样本口径必要性 `run/PROJECT-GOVERNANCE-01/E2E-FIX-001/probe_sample_def.py`（**LDN43 旧样本 1497 个 1″ 匹配中 813 个（54%）来自 `snr≤20` 检出星，median 0.4486″ 差于合格子集 0.4059″** ⇒ 旧口径错配主导）；星表准备 `prep_catalog.py`（独立 XPSD python 解码器，只读） | `docs/science/ASTROMETRY.md`（§11a 新增；§1–§15 公式/不变量/容差零改动，不构成 FROZEN 条款变更）、`docs/algorithms/GATES_AND_TOLERANCES.md`（17 门 = 原 16 + 1；G-P1-WCS-F1 的 `0.1431″` 标定改标历史值 ⇒ 无有效标定，发布门仍 N）、`docs/algorithms/PLATESOLVE.md` §11.4、`lib/algorithms/platesolve/memory.md`、`tools/astrometry/closure_metric.py`（新）、`tests/unit/p1wcs/CMakeLists.txt`、ctest 基线 435→441（其中本任务 1 项，其余为并行线）。**未改任何生产 C++ 代码、未改任何阈值语义、未放宽断言、未新增豁免**。影响面：P1-WCS-TEST 回归锚基线、G-P1-WCS-CLOSURE 判读口径、后续按分档定阈；CI 注册转 **CI-003**（本任务零 `ci/**` 写权限） | 待执行 |
| SC-007 | 2026-09-17 | `docs/science/CALIBRATION.md`、`docs/algorithms/CALIBRATION_ALGORITHMS.md`、`docs/algorithms/v6/phase1/ALG_P1_001_PHASE1_ALGORITHM_SPEC.md`、`docs/contracts/DATA_SEMANTICS.md`、`lib/algorithms/calibration/src/calibrator.cpp`、`lib/algorithms/calibration/src/v6_calibration_covariance.cpp`、`lib/algorithms/calibration/tests/p1cal/**`、`tests/backend/test_calibration_oracle.py`、`lib/infrastructure/scheduler/src/module_adapters.cpp`、`lib/phase1_session/p1_session.cpp`、`lib/phase1_session/tests/p1sess/{p1sess_oracle.hpp,p1sess_tests_units.cpp}`（**期望重锚：旧式 42.1519 → 新式 39.816**，p1sess_units U2）、`tests/backend/test_calibration_oracle.py`（oracle_cal 标准式 mirror + 遗留路径修复）、`tests/unit/{p1_calibration_test,calibration_adapter_test}.cpp`、`tests/unit/v6_p1_cal/**`、`tests/unit/p1001_real_nodes_test.cpp`（链路夹具补 EXPTIME：light=dark=60s ⇒ K=1，10 处写入）、`tests/cli/{test_phase1_inprocess,test_cli004_process_protocol,test_phase123_pipeline}.py`（含 bias 的 dark 夹具显式声明 `dark_optimization: true`，使 CLI-002 Oracle +40 成立） | **BIAS-001（master_bias 零影响——标定链科学缺陷）**：① **SCI-CAL-001 §5 唯一式订正**——旧文本把默认分支定义为 `(raw−dark)/flat`（"Dark 已含 Bias"），`bias` 参数在该分支完全不被读取、`K=t_light/t_dark` 只在 `dark_opt=1` 生效；订正为**标准式** `cal=(raw−bias−K·dark)/max(flat,0.1)`（**master_dark 约定 = 已减 bias 的暗电流母版**）+ **兼容式** `dark_opt=1`：`cal=(raw−bias−K·(dark−bias))/max(flat,0.1)`（master_dark 含 bias，显式分离）；**K 在两分支都施加**。② §2 符号表增 `dark_total` 兼容形态、§6 增"单位一致/平场已归一"假设、§7 增 **bias 参与不变量**（dark==NULL 时提供 vs 不提供 bias 的差恒为 `bias/max(flat,0.1)`）与 **K 参与不变量**、§8 增 4 行退化语义、§9a 重写 bias/dark 条、§11 增 **bias 参与门 + K 参与门 + ignore-bias 变异注入**、§14 增外部标准第 4/5/6 条（ccdproc/LSST 逐字 + IRAF 未能逐字核验的如实登记）。③ ALG-CAL-001 §2/§3.3/§7/§9.1 同步 + 新增 **DISP-CAL-012** 缺陷登记（旧实现证据）。④ ALG-P1-001 §2.1/§2.2/§2.3 同步（标准式 Jacobian `J=[1/f,−1/f,−α/f,−y/f]`；兼容式保留 `−(1−α)/f` 折叠）。⑤ DATA_SEMANTICS §9.1 拆 `master_bias`/`master_dark` 两行并写明约定、K 行与 actual_k 行同步。 | **外部标准（逐字核验 2026-09-17）**：astropy ccdproc "Reduction toolbox"——"master dark image … **that has been bias-subtracted** so that it can be scaled by exposure time"；顺序 trim/overscan → `subtract_bias` → `subtract_dark(scale=True)` → `flat_correct`（<https://ccdproc.readthedocs.io/en/latest/reduction_toolbox.html>）。LSST `lsst.ip.isr`：`biasCorrection` = `maskedImage -= bias`；`darkCorrection` Notes 逐字 "maskedImage -= dark * expScaling / darkScaling"；`flatCorrection` 最后除且 flat 标度取自数据；`IsrTask.run` 顺序自述 doBias→…→doDark→…→doFlat（<https://raw.githubusercontent.com/lsst/ip_isr/main/python/lsst/ip/isr/isrFunctions.py>、<https://pipelines.lsst.io/modules/lsst.ip.isr/index.html>）。IRAF `ccdproc`/`zerocombine`：iraf.net 对本节点 403（Cloudflare），**未逐字取原文**，仅按 ccdproc 明示的 IRAF 等价关系引用，不作冻结判据。**实测（真实 T2 NGC1727 Red 600s，固化二进制 63da68618aa73203，独立复跑 8 构型）**：默认分支下"提供 vs 缺失 master_bias"产物 **逐位相同（16,777,216 px 全等，max\|Δ\|=0）**；**只提供 bias（无 dark/flat）时产物与完全不标定逐位相同**（bias 零影响）；缺 bias 的 dark+flat 产物 100% 像素不同（max 300192.1、mean 5863.80）；`dark_opt=1` 有/无 bias 并非逐位相同（2,141,721 px 差、max\|Δ\|=0.03125）⇒ K=1 是**代数**相消而非 FP32 逐位相消。独立 oracle 认定默认分支实现逐位等于 `(L−D)/max(F,0.1)`（max\|Δ\|=0）。**同时证伪 E2E-D04 的补测**：`E2E-001/configs/d4_darkopt_{bias,nobias}.json` 逐字仅差 `output_dir`、两份**都含 master_bias**（sha256 9318a334…/c54104a9…）⇒ 该"逐位相同"是空测。 | 所有用默认分支（`dark_optimization` 缺省）的标定产物；含 bias 的暗场母版必须改用 `dark_optimization=true` 显式声明（否则多减一次 bias）；`calibrated_*`/`cleaned_*`/HiPS signal 的标定面数值、`p1_*.json` manifest 的 `actual_k`/per_frame.dark_scale 语义；受影响测试/工件清单与重锚数字见 `run/PROJECT-GOVERNANCE-01/BIAS-001/自证摘要.md` §影响面。相邻缺陷（**不在本卡**）：**UNIT-001**（XISF 母版 [0,1] vs 亮场 ADU 量级不一致、master_flat 未归一 median 0.2064 无机器门）另卡在办。 | 批次A（文档+门）+ 批次B（实现+红→绿）完成：自有 build 目录 cmake rc=0 / ninja 0 FAILED / ctest 442/442 绿 / 本域锚点 0 违规；独立门 PASS（M1–M4 全判红） |
| SC-008 | 2026-09-17 | `docs/science/PHASE3_HIPS_TO_FITS.md` §4、`docs/algorithms/HIPS_WRITER.md`、`docs/contracts/DATA_SEMANTICS.md` §12.3、`docs/interfaces/io/IO_002_HIPS_INPUT_INTERFACE.md` §3.1/§4/§5；实现 `lib/infrastructure/aio/src/hips/{aio_hips_writer,aio_hips_reader}.cpp`、`lib/infrastructure/aio/io/hips_core.c` | **LEDGER-P1 Batch-1（HiPS 产品面 IVOA 符合性）**：① `hips_frame` 写出值由非标准 `equatorial` 改为 IVOA 标准值 `icrs`（值域 {icrs,galactic,ecliptic}），读侧保留旧值**只读别名**、galactic/ecliptic 显式拒绝；② `hips_pixel_scale` 单位由**角秒**改为**度**（IVOA 同名键单位），值 ÷3600；③ tile 目录布局由 `Dir=ipix/10000, Npix=ipix%10000` 改为 §4.1 标准式 `Dir=(ipix/10000)*10000, Npix=ipix`（ipix<10000 路径不变），读侧标准优先 + 旧布局只读回退。 | IVOA REC-HIPS-1.0 **§4.1**（Tile N in order K at `NorderK/DirD/NpixN`，D=(N/10000)*10000，示例 10302@order6→Dir10000/Npix10302.fits）、**§4.4.1**（`hips_frame` 值域 icrs/galactic/ecliptic；`hips_pixel_scale`/s_pixel_scale 单位 degrees）。可复跑：`bash run/PROJECT-GOVERNANCE-01/LEDGER-P1/dual_evidence.sh`（改前 HEAD vs 改后工作树）；产物级：`ctest -R "p1_hips_writer|p1hips"`（7/7）。 | 所有新产 HiPS `properties`（frame 值、像素尺度值、≥10000 的 tile 路径）；消费侧仅 `lib/algorithms/coverage/src/sampler.cpp:360` 以**字符串**参与 provenance 哈希，无数值解释，无算术后效；旧产品经只读回退仍可读；相关测试 `p1hips_tests_units/oracle/negative`、`tests/unit/p1_hips_writer_test.cpp`、`tests/unit/p1001_real_nodes_test.cpp`。 | 落地（Batch-1，四门见 `run/PROJECT-GOVERNANCE-01/LEDGER-P1/自证摘要.md`） |
| SC-009 | 2026-09-17 | `docs/contracts/DATA_SEMANTICS.md` §15.2（star_measurements 列语义）、`lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp` 列头 | **LEDGER-P1 Batch-1（M3b-A-03）**：`star_measurements` 列 [4] 名义语义由 `flux_uncertainty (PSF mad proxy)` 改为 `residual_mad`（PSF 10–90% 截尾平均绝对残差，ADU/pixel，**诊断量**），并写明「PSF 无参数协方差/不确定度输出」。**零数值改动**：`row[4] = prow[7]` 与 [10]=mad 兼容别名均不变。 | docs/algorithms/STAR_PSF_ALGORITHMS.md §11.3 **DISP-PSF-005**（无参数协方差/不确定性输出，禁止宣称已实现）+ docs/science/PSF.md **§9a**（q_psf=A/residual_scale 是拟合质量代理，不是 SNR/光度不确定度）。 | 合同文本与实现列名；**不改变任何数值**；下游若按列名消费需同步（本仓无按 `flux_uncertainty` 取值的消费点，`grep` 仅注释/合同命中）。 | 落地（Batch-1） |
| SC-001 | 2026-09-16 | `docs/science/PHASE3_HIPS_TO_FITS*.md`、`docs/algorithms/PHASE3_PROJ_IMPL*.md`、`docs/algorithms/PHASE3_RESAMPLE*.md`、`docs/science/UNCERTAINTY_AND_COVARIANCE.md`、`docs/contracts/{DATA_SEMANTICS,v6/data/08_phase3}.md`、`docs/plugins/algorithms_phase3/14_projection.md`、`contracts/schemas/projection_registry.schema.json`（新建）、`lib/algorithms/projection/**`、`tests/unit/v6_p3_proj/**` | **实际改动（SCI-FIX-PROJ，BASE=6de369f7）**：① CAR/AIT 按 Paper II §2.2 三 Euler 角把 **CRVAL2（含 LONPOLE 默认 0/180）纳入映射**（v6 registry v2→**v3**：`tilt_params`/`native_to_sky_general`/`sky_to_native_general`）；② AIT 域界 **`A<2` → `A≤1`**（A=xp²/4+yp²；A=1 即 φ=±180° 边界合法）；③ CAR **native 极行 θ=±90° fail-closed**（pix2world 含 =90 拒、world2pix native 极点 φ 不唯一拒、make 四角守卫、plan margin）；④ 投影集合权威改为 `ASTROCS_DESIGN §5.3` **八投影**（TAN/SIN/CAR/AIT/STG/MOL/CEA/ZEA；`registry_frozen_set`/`registry_is_frozen_code` 导出，表内 code 必须属于集合），删除 ALG §15 / SCI / UNC / DATA §30 / module.yaml / README 中对**已废止宪章**（ASTROCS-CONSTITUTION-001 §7.3/§18.1/§7.1/§18.3）的引用，改为在役权威（DESIGN §5.3 + Paper I/II + 实质 SCI/DATA 条款）；⑤ legacy v1（`p3_projection.cpp`，`kP3ProjectionRegistryVersion=1`）**RETIRED**（`kP3ProjectionRegistryRetired=true`，四项偏差 D1..D4 冻结对照、禁止新消费方引用），ALG §15.9 建立 v1 偏差表；⑥ 门与测试：`p3_proj_wcs_oracle.py` 增 **6 条 dec0≠0 用例** + **CRPIX↔CRVAL 定义性不变量** + AIT 椭圆域判据 + CAR 解析纬度带限定 `|CRVAL2|≤1e-9`；`v6_p3_proj_test.cpp` 增 `test_crval2_rotation`/`test_domain_limits`（CAR θ=90 必拒、AIT |X|=229.125° 折叠环带必拒）与 v3/冻结集合断言；`p3_proj_legacy_deviation.py` 改为 **LEGACY_DEVIATION_TABLE**（四项偏差必须仍复现、「只减不增」，修好 v1 即转红）；⑦ P3-002 八条：M1a-A-007（`CD⁻¹`→**`CD`**、`|center.Dec|≥5°`→**≤85°**，实现本正确只改文档）、M1a-A-008（coverage = 足迹内**存在 tile 像素**，与 §8「tile 内 NaN」互斥解除 + 真值表）、M1a-A-009（ivar==0 = 像素级零权重 ⇒ u 无效/NaN 传播态；删除 DATA §30.4-3「含 ivar==0 导出」硬错误括注，显式禁止 1/0→Inf）、M7-A-114（UNC/DATA 主式改 `var_out=[R C_in Rᵀ]_ii`，`Σc_k²u_k` 降为 C_in 对角特例 + 低估界 0.75ρ：ρ=0.19 ⇒ 方差低估 36.3%）、M7-A-117（**账本改法本身是错的**：正确一般式 `leaf_order=order_sel+log2(W)`，`+2·log2(W)` 是 `>>` **索引位移**；SCI §4 tile 宽收窄 W=512；已订正 FIX_LEDGER 备注，**不按账本处方改码**）、M7-A-129（①「跳象限未重归一」**误报撤案**——实现逐象限补齐 + 缺角 C=0 fail-closed；②「Σw=1 精确 / 常数场 max_abs=0」→「**±k·ULP + 分 dtype 相对容差**」；已订正 FIX_LEDGER 备注）、M7-A-209（`s_out_rad = s_out·π/180` 量纲声明）、M7-F-201（常数场/权重和删除逐位断言）；⑧ 悬空引用：新建 `contracts/schemas/projection_registry.schema.json`（修好 `docs/modules/MODULE_MAP.yaml:445` 与 `14_projection.md` 的悬空 schema 链接）。 | `reports/PROJECT-GOVERNANCE-01/research/R-1_投影WCS数学正确性.md`；`run/PROJECT-GOVERNANCE-01/R-1/**`（astropy 7.0.1 逐点对拍 22 组 max 6.854e-13°）；**本任务新增** `run/PROJECT-GOVERNANCE-01/SCI-FIX-PROJ/logs/`：**10** 新 Oracle vs 改前实现（6 条 dec0≠0 用例必红，ORACLE FAIL rc=1）、**11** 新单测 vs 改前实现（18 FAIL rc=1：`pix2world(CRPIX)≠CRVAL`、CAR θ=90 被放行、AIT X=229.125° 被放行）、**14/15** 改后同两门全绿（ORACLE PASS / V6 PROJECTION PASS）、**13** `ctest -R 'proj|p3_proj'` 10/10 绿、**16** 偏差表门对 v6（正确实现）转红（门有区分力）、**24** 锚点门（本任务改动的 6 个文档 24/24 锚 OK；其余 7 项失败属并行任务域，见自证摘要「未做项」）、**27/28** 本域目标构建 0 错 + proj ctest 10/10。 | `lib/algorithms/projection/**`（v6 v3 公式 + registry 版本/冻结集合 + v1 RETIRED 标记 + module.yaml/README 口径）、`tests/unit/v6_p3_proj/**`（Oracle/单测/偏差表门）、`contracts/schemas/projection_registry.schema.json`（新增）、`docs/algorithms/{PHASE3_PROJ_IMPL,PHASE3_RESAMPLE}.md`（**V6 ALG-008**）、`docs/science/{PHASE3_HIPS_TO_FITS,UNCERTAINTY_AND_COVARIANCE}.md`（**SCI V6 SCI-008**）、`docs/contracts/{DATA_SEMANTICS,v6/data/08_phase3}.md`、`docs/plugins/algorithms_phase3/14_projection.md`、`问题扫描/账本/FIX_LEDGER.{jsonl,csv}`（M7-A-117/M7-A-129 备注订正）⑨（**追加授权收尾 2026-09-16**，负责人明确扩权）DATA_SEMANTICS.md 剩余 6 处已废止宪章引用换锚为在役权威：`:248` PRECISION 缺省 FP64 → `ALG docs/algorithms/DRIZZLE_GEOMETRY.md` RESCUE-FD-02 条 + 实现/回归锚；`:640` worker 预算观测 → `ENGINEERING_SPEC §10` + `context.cpp:127`/`commands.cpp:668,788`；`:2054` VARIANCE/IVAR 必写 → 本文 §30.4 + `UNCERTAINTY_AND_COVARIANCE.md` Phase3 节；`:2129` input_mode 目标态上位 → 同上；`:2258` 未实现投影 fail-fast → `ENGINEERING_SPEC §9`；`:2577` PENDING_OWNER_SIGNOFF 状态语义 → `docs/contracts/v6/W6_SCHEMA_INTEGRATION.md:101` + 冻结 JSON 词表。**六处语义均不变**（纯引用换锚，未动相邻段落，追加收尾日志 run/PROJECT-GOVERNANCE-01/SCI-FIX-PROJ/logs/40）。 | 已执行（工作区，待前台提交） |
| SC-005 | 2026-09-17 | `docs/science/PHASE2_UPM.md`、`docs/algorithms/UPM_SOLVER.md`、`docs/algorithms/PHASE2_UPM_IMPL.md`、`docs/science/CONTROL_WEIGHT_SNR.md`、`docs/algorithms/PHASE2_INTEGRATION.md`、`docs/science/INTEGRATION.md`、`docs/science/REJECTION.md`、`docs/science/UNCERTAINTY_AND_COVARIANCE.md`、`docs/algorithms/PHASE2_SAMPLER.md`、`docs/GLOSSARY.md` | ① **weight_mode 分支语义**：`CONTROL_WEIGHT_SNR §4` 的 `weight_mode=2` → **0**（legacy/非生产），并写明 mode 2 = 逐样本 ivar、无 fallback；`PHASE2_INTEGRATION §5` 的 `ivar_valid?ivar:support` 改标「仅 legacy_allow_weight_fallback=true 的显式降级路径」（零代码）。② **w_UPM 两级命名**：`w_UPM`（绝对式 ADU⁻²，科学定义）与 `w_cell`（份额式，无量纲，Σ_cell=control_reliability，求解器实际消费）分别命名并冻结三条性质（单元内权比一致/公共精度因子消去/丢弃跨 control 精度）+ §10 增「未同步换算 λs/λ0 不得改用绝对权」；`geometric_reliability` → `control_reliability`（**登记真实缺陷：实现只是配置常量 1.0，不是单 control 覆盖度**，未实现）。③ **k_corr 定义域 1 ≤ k_corr + scale 维退役**：`p2_upm_control_variance`/`p2_upm_ma_build` 现显式拒 k<1；`kcorr_lookup` 域外不再 clamp（生产真帧 0.9586″/px ∈ 域外）而显式回退冻结默认 1.4 并打标；`kcorr.domain_clamp` 门改 `domain_fallback_to_frozen_default_not_clamp`。④ **排异 N≤4 容错登记**：`n=4 ∧ 全拒 ⇒ UNDERDETERMINED 全接受`（域写死 n≥5 仍 ALL_REJECTED）+ 根因登记（percentile `scale=|median|` 在近零天光塌缩：n=6/8 显式 percentile 全拒 73.9%/70.1%）；补 n=3/4/5/6 边界门。⑤ **support reducer**：`SCI INTEGRATION:58` `max_{valid,W>0}` → `max_{accepted}`；`PHASE2_INTEGRATION` 的 DISP-P2INT-001/002 过期登记清零（实现 B2-A7 已在位）。⑥ **M4-F-01 改判**：两个 MC 测试是 tracked-but-unbuilt，SCI 现在时「已过」改 MISSING；新增三合一证据门（存在∧构建注册∧CI 采集 + 文档点名双向一致）。⑦ **哨兵**：`p2_upm_evaluate_c` null model 0.0 → NaN。⑧ **收敛可观测**：新增只读 `p2_upm_convergence`（不改冻结结构体）。⑨ **确定性口径**：跨 worker 数门 1e-6 → **1e-12**，并冻结三档（同配置重复位精确+hash exact / 跨 worker 数 1e-12 / 跨后端不允许）；补 Linux 合成 HiPS 的 sampler 并行位精确门 | `reports/PROJECT-GOVERNANCE-01/research/R-2_phase2权重与UPM语义.md`；`run/PROJECT-GOVERNANCE-01/R-2/logs/probe_r2.log`（[UPMW]/[KCORR]/[CVAR]/[SENT]/[REJX]/[MC]/[INVARI]）；本次新增 `run/PROJECT-GOVERNANCE-01/SCI-FIX-WEIGHT/logs/probe_1t2t.log`（1T/2T：同 control 各帧 ivar 不等时 model_hash 不同、ΔC_max=2.22e-15 ≈ 1 ulp @10 ADU ⇒ 「位精确」不成立，容差冻结 1e-12） | `lib/algorithms/coverage/src/upm.cpp`（k_corr≥1、NaN 哨兵、convergence）、`sampler.cpp`（scale 维退役）、`include/astro/phase2/upm.h`（新访问器）；测试 `synthetic_gate.cpp`、`kcorr_lookup_test.cpp`、`sampler_parallel_consistency_test.cpp`、`tests/unit/v6_p2_upm/`、`tests/quality/test_sci_evidence_registration.py`（新门，ctest `sci_evidence_registration_quality`）；registry/门：`anchor_contract.json`（删 1 条陈旧豁免 + 锚重映射）、`ci/checks.json` 新门登记转 **CI-003** | 待执行 |
| SC-009 | 2026-09-17 | `docs/science/NOISE_MODEL.md`、`docs/algorithms/NOISE_ESTIMATION.md`、`config/defaults.json`（noise.*）、`lib/algorithms/noise_snr/cpp/include/snr_estimator.h`、`lib/algorithms/noise_snr/cpp/src/noise_model.cpp`、`lib/algorithms/noise_snr/src/module_entry.cpp`、`lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp`（噪声调用点）、`lib/algorithms/noise_snr/tests/p1noise/**` | **掩膜语义整改（MASK-002，依据 MASK-001 重推导 + EXP-A/B/C）**：① `rmax=10·6=60 px` 由「操作默认半径」改判为**硬上界**，实际半径按 §5a 逐星导出 `r_i = clip(r_local(F_i, FWHM_i, k·σ_bg), max(1.5 px, 0.75·FWHM_i), 60 px)`（Moffat β=2.5 保守翼指数；Gaussian 极限同式），`k=0.1`；② 删除「掩膜半径与亮度解耦」这一**被证伪**的不变量（实测固定 r=10 px 在 F 1e3→1e6 时偏差 0.02%→2.29%），改为「半径对 `F_i`、`FWHM_i` 单调不减 + 同输入逐位可复现」；③ 新增天空预算不变量 `n_qualified ≥ 8 且 N_sky ≥ 9216`（由 `SE(σ̂)/σ ≈ 1.144/√N_sky ≤ 1.5%` 导出）与显式降级标 `MASK_LEGACY/MASK_DEGRADED`；`nq==0` 全帧兜底阈由 `min_samples/2 = 32` 像素收紧为 `max(min_samples, 9216)`；④ C ABI 版本化（`struct_size@0`/`abi_version@4`，`-9 = SNR_ABI_MISMATCH` fail-closed）并新增逐星 `star_flux`/`star_fwhm` 入参 + `mask_degraded/mask_radius_p50/mask_frac` 诊断；⑤ `config/defaults.json` 新增 5 键 `noise.mask_k_sigma=0.1`、`noise.mask_r_min_px=1.5`、`noise.mask_fwhm_floor_scale=0.75`、`noise.mask_budget_min_patches=8`、`noise.mask_budget_min_sky=9216`，并解除 `source_mask_radius_px`/`mask_radius_scale` 对 ALG 实现行的**循环引用**（改锚 SCI §5a） | MASK-001 证据 `reports/PROJECT-GOVERNANCE-01/research/MASK-001_掩膜语义重新推导.md`（EXP-C 54 帧：worst \|σ 偏差\| P0 6.93% / P1 2.63% / P2 8.44% / P3 4.77% / **P4 1.11%**；零权重像素 14.8%→0；权重场效率损失 13.28%→0.013%）；复跑件 `run/PROJECT-GOVERNANCE-01/MASK-002/`（`probe_mask002` 密度/兜底/4K 模式、`probe_old` 改前对照、`logs/00..09`）；门：`ninja -C build -k 0` 我的目标 0 FAILED、`ctest -R 'p1noise\|p1snr'` **25/25**（新增 `p1noise_mask`＝含星帧源污染 oracle + 半径单调门，替换旧 `o2_mask_channel_parity`；新增 `p1noise_abi_layout`＝布局锁定 + 突变自检）、`p1noise_numpy_oracle` 108/108（NumPy 独立复算 §5a 半径/掩膜，rtol 1e-9） | `noise_snr` 模块实现与测试、orchestrator 噪声块调用点与 KV、`config/defaults.json`（noise 组 8→13 字段）、`docs/contracts/CONFIG_CONTRACT.md` 噪声组待同步、CI 检查面（`ci/checks.json` 门注册）待 W4-A3 落；未改任何容差/旧断言，未删测试 | 已落地（docs+config+impl+门同批；批次1=文档/配置、批次2=实现+门） |
| SC-002 | 2026-09-16 | `docs/science/NOISE_MODEL.md`、`docs/algorithms/NOISE_ESTIMATION.md`、`docs/contracts/{DATA_SEMANTICS,PUBLIC_API}.md`、`lib/algorithms/noise_snr/README.md` | **文档侧**：① §4 `min_samples` 默认值 5 → **64**；② §2:16 ivar 单位 `pixel⁻²·ADU⁻²` → `ADU⁻²`；③ §5:51 兜底式记号拆为两条显式式（合格 patch 支 `max(vmed,floor)` / 全帧退化支 `max(sig²,floor)`）；④ ALG §13.2 删除「不改 SCI，以代码为准」的权威倒置表述，改「以 SCI 为准」；⑤ §4/§5 增平面几何启用条件（点云相对条件数 `λlo/λhi≥1/16`，κ≤4）；⑥ §9a 补 `sigma_cal_rel`（逐星散度 ≠ 零点不确定度；零点标准误 `1.253·σ/√N_eff`）；⑦ §14 增默认值导出依据；⑧ ALG §13.3 增 DISP-NOISE-010、§13.3a 增四处实现整改登记；⑨ 全部受影响锚点同步（noise_model.cpp 行号表 + snr_estimator.h/README/合同）；⑭ **MAD→σ 常数唯一写法**：全域统一到 `1.482602218505602`，11 位简写 `1.4826022185` 只保留在标注"约等于"语境并给出实测差（绝对 5.602e-12 / 相对 3.779e-12），订正 `NOISE_MODEL.md:94` 与 `NOISE_ESTIMATION.md:96` 的**假算术断言**「差 <1e-12」（实测大 5.6 倍）；⑮ **V12-N-03**：`NOISE_MODEL.md:139`「与 SCI-PHOT 的 `0.6745` 同源」改为实测口径（`Φ⁻¹(3/4)=0.6744897501960817`；`0.6745` 相对差 +1.5196e-05、`1/0.6745` 相对差 −1.5196e-05，不得与本层冻结值互换）；⑯ **`config/defaults.json` 权威改指**（负责人批准）：6 条 `noise.*` 的 `source_ref` 由 ALG:134 改指 SCI 实际陈述行（:46/:48/:37/:39/:21），`source_mask_radius_px`(10)/`mask_radius_scale`(6) 因 SCI 只冻结 rmax 公式不给数值而保留 ALG:134 并在 note 写明；⑰ **CMake 迁移锚再同步**：ALG §13.1/§13.5 与模块 README/memory 共 11 处 `CMakeLists.txt` 行号随 ROOT-008 二次迁移更新（`:495-498→:521-524`、`:556→:607`、`:435-438→:521-524`、`tests/unit/CMakeLists.txt:312-316→:614-618`、`:210→:236`），消除 `NOISE-CMAKE` BINDING_VIOLATION。**实现侧（同 claim，非架构改动）**：⑩ noise_model.cpp 相对条件数判据 + build 阶段回退 `has_spatial_field`；⑪ wrapper 注释补 `/gain²`、单测 fixture 增益方向回 SCI 约定 + 容差收到 5%、`photometer.cpp:90` 常数改冻结值；⑫ `orchestrator.cpp` 删 `\|\| psf_status==3.0`（3=ITERATION_LIMIT 失败码不再拿满权）；⑬ 删除 `snr_science.cpp` 零引用死常数 `kLn10`（模块单一定义点） | `reports/PROJECT-GOVERNANCE-01/research/R-5_噪声SNR与统计口径.md`；`run/PROJECT-GOVERNANCE-01/R-5/exp1..exp9`（N=5 ⇒ 单 patch 偏差 −19.2%、SCI 自设 5% oracle 通过率 0.6%；N=64 ⇒ −1.25%、92.8%）；**本任务新增** `run/PROJECT-GOVERNANCE-01/SCI-FIX-NOISE/logs/`：01 几何判据数值表（**推翻** R-5 §5.4 建议式 `det/(sxx·syy)`——实测 B=0.794 vs 满格 1.0 无分离度，改用点云条件数 A=0/B=0.0133/C=0.200/满格 1.0）、10-13 新测试红→绿、15 Oracle 注入红、17/19 单测红→绿、21/22 四处整改探针红→绿、30/31 锚点 JSON；**收尾轮**：60/68 `science_contract_lint` rc=0、62/70 `ctest` 21/21 rc=0、63/66/67 锚点 JSON（842 锚 / 38 文档，域内 0 失败）、65 常数复算、69 Oracle 92/92 | `lib/algorithms/noise_snr/**`（默认配置/测试/Oracle）、`lib/algorithms/photometry/wrapper_phase1/photometer.cpp`、`lib/infrastructure/pipeline/orchestrator/cpp/src/orchestrator.cpp`、`tests/unit/p1_noise_test.cpp`、`docs/contracts/*`、SCI §11 oracle、`config/defaults.json`（8 条 `noise.*` 的 `source_ref` 已按权威链改指 `docs/science/**`） | 已执行（第一批 15 文件由前台提交 `99fc384d`；收尾批：常数统一 + config `source_ref` + CMake 锚再同步，留工作区待前台提交） |

| SC-004 | 2026-09-16 | `docs/science/STAR_DETECTION.md`、`docs/algorithms/STAR_DETECTION_ALGORITHMS.md`、`docs/science/PSF.md`、`docs/science/ASTROMETRY.md`、`docs/algorithms/PLATESOLVE.md`、`docs/contracts/DATA_SEMANTICS.md`、`docs/standards/STANDARDS_REGISTRY.md`、`docs/algorithms/GATES_AND_TOLERANCES.md`（新，ALG-GATES-001）、`docs/DOCUMENT_INDEX.yaml`、`lib/algorithms/star_detection/README.md`+`module.yaml` | **SCI-FIX-PSF 质心坐标契约修正（9 项）**：① D-16 重新定性——0.5px = ALG-STARDET-001 §11.4 **F4 FP32/u16 量化通道容差**（实测 u16 量化 median 0.0018 / p95 0.0036 / max 0.0056 px，余量 ~90×，**可达未超标**），0.897px 系 plate-solve **外部闭环台账值**（不同门/不同域/不同工具代，不可比）⇒ 整改对象改为**端到端坐标契约**：同星 sdet 报 truth、dpsf 报 truth−0.5，编排写端再 −0.5 ⇒ PSF 支路 median −0.5000 px、fallback +0.0001 px、**分离恰 0.5000 px 卡在 ipv 去重阈值严格 <0.5 之外 ⇒ 同星双份进入 ipv**；修桥=PSF 支路写端不再 −0.5（dpsf 输出即统一契约 index-is-center），并新增链接 sdet+dpsf 的绝对位置门 **G-P1-CENTROID-1**（ctest `p1psf_centroid_gate`/`_neg`）；② M3b-A-01——SCI-PSF-001 §10 禁则对象是 dpsf，被 STAR_DETECTION §2 越界扩张到检测域 ⇒ 冻结**双模型**（检测侧=椭圆高斯 / PSF 侧=Moffat4）+ 换算声明（同 sx 下 FWHM 差 **1.9140×**；高斯拟合 Moffat4 真星质心无偏 0.0047 px，但 FWHM 报值/真值=1.086、解析流量比=0.902）+ 函数改名 `sdet_moffat4_fit→sdet_gauss_fit` + DISP-STAR-007；③ F1 只补量测域（**门值 0.5″ 不动**：内点 trans 域 + 合成线性场 + n_pairs≥12；0.897px 归独立证据面 G-P1-WCS-CLOSURE）；④ M3b-F-02——scipy `curve_fit` 独立 Oracle 落地 + TRACEABILITY_MATRIX `test_path` 由文档锚改为可执行目标 + `VERIFIED⇒evidence≠MISSING` 机器门；⑤ M1a-A-001——ASTROMETRY.md §5 前向式改 SIP 标准形 `(ξ,η)=CD·[(xp−CRPIX)+SIP_A/B(xp−CRPIX)]`（原式把 px 量 SIP 加到 deg 量 CD 上，等效误差 3.7e3–4.4e5 px；**实现零改动，以实现为准订正 SCI**）；⑥ M1a-C-001——**判据本身错**（7×7 自证门：自网格 1.8e-12 px vs 离网格 3.10 px）⇒ 取消 `NB_GRID=7` 冻结地位、往返不变量改**独立密集域** ≤1e-4 px（原 1e-6 px/7×7 口径废止）、登记 DISP-WCS-008、归档 owner 裁决 WCS-003 提回活动登记面；⑦ F-2——新增**机器可校验冻结门表** ALG-GATES-001（16 门，列=门ID/判据式/量测域/统计量/SNR定义/阈值/阈值来源/证据ID/发布门 + 规则 R1 表外阈值禁止、R2 证据必需、R3 量测域独立、R4 统计量显式、R5 SNR 统一）+ 补 **SNR 定义** `SNR_peak:=A_fit/sigma_bg`（原全文无定义；实测同一合成场 SNR_peak=20 检出 0 星、=50 检出 36/40）+ 机器门 `tools/check_gates_and_tolerances.py`；⑧ DATA_SEMANTICS §17.2 的 fallback「−0.5 转统一契约」过期句订正为**直送**（与实码互斥） | `reports/PROJECT-GOVERNANCE-01/research/R-3_PSF质心科学门与容差.md` + `run/PROJECT-GOVERNANCE-01/R-3/**`（探针直链本树 `libastrocs_p1_sdet.a`+`libastrocs_p1_dpsf.a`：sdet−truthFITS median 0.0035 px、dpsf−truthFITS 恒 −0.5000 px（三种初值同解）、PSF 支路 −0.5000 / fallback +0.0001 / 分离 0.5000 px；u16 量化 p95 0.0036 px；SIP 网格对照 7/41/81）＋ `run/PROJECT-GOVERNANCE-01/SCI-FIX-PSF/logs/**`（**门红→绿**：`p1psf_centroid_gate` 14/14 PASS、双支路分离 −0.0000/+0.0001 px；`p1psf_centroid_gate_neg` 必红检出 −0.5000 px；`tools/check_gates_and_tolerances.py --self-test` 7/7 注入必红） | `lib/infrastructure/pipeline/orchestrator/**`（坐标桥 + 新 `star_coord_contract.h`）、`lib/algorithms/star_detection/**`（改名）、`lib/algorithms/psf/tests/p1psf/**`（新门）、`docs/traceability/TRACEABILITY_MATRIX.{json,csv}`、`docs/standards/STANDARDS_REGISTRY.md`、`问题扫描/账本/FIX_LEDGER.{jsonl,csv}`（7 行处置列）、`lib/algorithms/platesolve/cpp/ipv/src/*.cpp`（仅注释）；**产品面**：PSF 支路像素坐标 **+0.5 px**（同星不再双份进 ipv）⇒ 依产品面影响评估走版本递增（PSF 走有效星 +0.5 px，fallback 星不变） | 待执行 |

| SC-006 | 2026-09-16 | `docs/algorithms/HIPS_WRITER.md`、`docs/contracts/DATA_SEMANTICS.md`（§20.3）、`lib/infrastructure/aio/include/aio_hips.h`、`lib/infrastructure/aio/src/hips/aio_hips_writer.cpp`、`lib/algorithms/drizzle/healpix_drizzle/astro_sphere_sink.cpp`、`lib/infrastructure/aio/tests/p1hips/*`、`lib/infrastructure/aio/tests/abi/*`（新）、`lib/infrastructure/aio/tools/aio_abi_mirror.py`（新） | **SCI-FIX-AIO 五项（BASE=工作树）**：① **勘误**：`HIPS_WRITER.md:146` `NSIDE=2^k` → `NSIDE=2^(k+9)`（实现三处 hierarchy 头卡恒写 `nside_k=2^(k+9)`；IVOA HiPS 1.0 §4.2.1 表 5 `hips_order` 与 FITS `NSIDE` 的定义关系；写/读侧零改动）；② **ABI 原位版本化**：`AstroSphereTileView`/`AioHipsSnrPoint`/`AioHipsDiagTileView`/`AioHipsTile` 首部加 `struct_size`@0/`abi_version`@4 + 公共入口逐元素 fail-closed（rc=−9，点名 ABI）+ C 探针 + 唯一 Python 镜像（删除 5 处内联 `_fields_` 镜像分叉）+ 逐字段布局锁 `aio_abi_layout_lock` 与变异自检（改字段顺序即红）；③ **`moc_sky_fraction` 精度**：properties 与 manifest 共用 `fmt_sky_fraction=%.17g`（`std::to_string` 6dp 在 1/12 点偏 3.333e-7 = §9 容差 1e-9 的 333 倍；两面字面量分叉），**容差 1e-9 不动**；把恒绿门（`p1hips_tests_oracle.cpp` 取 0.25）改为 1/12 非平凡点 round-trip + 双面逐字符一致；④ **层级归约改未钳制真实覆盖面积**（`flux_n += sig·a`、`area_n += a`；`support≤1` 只在发布时钳一次）：旧式把钳后 support 当归约权重 ⇒ 异质覆盖（c>1）下父级通量假损失 **37.032% → 0**；新增可观测钳制计数 `astrocs_support_clamped_pixels`/`astrocs_coverage_gt1_pixels`（properties+manifest）与 DATA_SEMANTICS §20.3「覆盖>1 时 F=signal×support×A_cell 不可复原」编码限声明；修同源 oracle（`p1hips_tests_properties.cpp:95` 改用 fixture 输入未钳制面积）；⑤ **`drizzle_scale_arcsec` 合法域**：`!isfinite→2`、`!(x>0)→2`、`>824.5167388361774″→2`（=2×叶 nside≥512 的最粗像素 412.258369″，SCI-DRZ-001 1–2× 过采样）、每次拒绝 `set_error`、删除「>0 才写」静默省略分支；生产调用方改为帧尺度未知时不调用 setter（provenance 全或无）。 | `reports/PROJECT-GOVERNANCE-01/research/R-7_AIO_HIPS_ABI与精度.md`（IVOA PDF 抽文 + 12 真实 tile + 真实库 C 探针 + NumPy 复算）；本次新增 `run/PROJECT-GOVERNANCE-01/SCI-FIX-AIO/logs/`：`pre_fix_new_gates.log`（改前红：oracle 4 处、properties 6 处（含 8.02→5.05）、negative 11 处）、`post_fix_new_gates.log`（改后绿）、`abi_lock_header_mutation.log`（改字段顺序 → 锁 rc=1，2 处 offset 不符）、`abi_before_after_probe.log`（改前 rc=0 且 TSV 第 2/3 条错位 → 改后入口 −9）；ctest `aio_abi_layout_lock`/`_selfcheck`、`p1hips_units/properties/oracle/negative/selfcheck` 全绿 | 影响面：aio HiPS writer C ABI（4 结构二进制布局变更 = ABI 版本 1，旧调用方在入口被拒而非静默错位）、Phase2 消费侧 moc_sky_fraction 字面量、drizzle sink provenance 调用条件、p1hips 测试面 + 新 ctest 两目标；未改 tools/**（`check_abi_boundary.py` 漏扫 aio_hips.h、`check_module_map.py:355` any() 恒绿两条转 CI-003 登记） | 已落地（v1） |

| SC-003 | 2026-09-16 | `docs/plugins/infrastructure/21_observability.md`、`ASTROCS_DESIGN.md` §8/§9、`ENGINEERING_SPEC.md` §10、`contracts/resource_gate_v1.json`、`tools/monitoring/run_monitored.py`、`tools/quality/resource_monitor.py`、`lib/infrastructure/cli/**` | ① 新增 §8「重计算负载资源门（G-RES-01）」作判据唯一语义权威（判定域/已分配容量分母取义/enforce 与 record_and_justify 划分/exit 10 条件）；② 数值唯一源 `contracts/resource_gate_v1.json`（C++ 经 CMake 生成头共读、两个 Python 实现运行期共读），实现侧不再有字面量阈值；③ **修实缺陷**：`--gate-required` 曾把机器有效核当已分配容量 ⇒ 分母改 `granted_workers`（哨兵 0 回落 `min(selected,available)`；两者皆哨兵时利用率类判据不成立并记 `allocated_capacity_undeclared`，禁止以机器核冒充）；④ 五处判据对齐（①取 p50 不取峰值、补 p50≥90% 与逐样本 85%·70%、C++ `wall<5s` 与判定域改显式 `NotApplicable` 不再静默 Ok、单位 MiB·s⁻¹、方向 `>=`）；⑤ 硬失败收窄为 ①②③（85% 均值降为 record_and_justify）；⑥ 删死代码 `--resource-detail`/`curve_points`/`build_curve()`；⑦ 工件名统一下划线 `resource_timeseries.csv`；⑧ DESIGN §9 五件从未产出的工件如实标 NOT_IMPLEMENTED | `run/PROJECT-GOVERNANCE-01/GATE-FIX-RES/logs/E7_red_green.log`（**同一命令**改前 rc=10 → 改后 rc=0；E7a 单活跃线程改前改后都 rc=10 为负对照）、`T1_frozen_gate.log`（19/19 OK）、`T2_judge_test.log`（5/5 OK）、`B9_ninja.log`（0 FAILED）、`T6_ctest.log`（11/11）、`A1_anchors.log`（rc=0）；判据来源 `reports/PROJECT-GOVERNANCE-01/research/R-4_资源门与观测权威落点.md` + `run/PROJECT-GOVERNANCE-01/R-4/**`（E0–E9） | 新增 `contracts/resource_gate_v1.json`；改 `docs/plugins/infrastructure/21_observability.md`、`ASTROCS_DESIGN.md`、`ENGINEERING_SPEC.md`、`tools/monitoring/run_monitored.py`、`tools/quality/resource_monitor.py`、`lib/infrastructure/cli/{resource_gate,memory_report,resource_events,resource_recorder,commands}`、`tests/monitoring/test_frozen_gate.py`、`tests/unit/{mon001_gate,mon002_gate}_test.cpp`、`tests/unit/CMakeLists.txt`、`CMakeLists.txt`；`ci/**` 同步（root_manifest 工件名、checks.json dirty_ignore、`RESOURCE-GATE-REAL` 步骤）登记转 **CI-003** | 待执行 |

| SC-010 | 2026-09-17 | `docs/science/CALIBRATION.md`（§3/§6/§8/§11/§14/§15）、`docs/algorithms/CALIBRATION_ALGORITHMS.md`（§2 标度声明表 + §10 **DISP-CAL-013**）、`docs/contracts/DATA_SEMANTICS.md`（§9.1/§9.1a + 表行）、`config/defaults.json`（新字段 `calibration.master_flat_median_range`，50→51 字段） | **UNIT-001（真实数据母版的单位/归一化不一致——标定正确性；与 SC-007/BIAS-001 同文件域、不同缺陷）**。① **冻结文件解释规则**：FITS 物理值 = `BSCALE·样本+BZERO`（真实亮场 `BITPIX=16 BZERO=32768` ⇒ [0,65535] ADU）；XISF `Image` 的 `sampleFormat="Float32"`+`bounds="0:1"` 声明的是**可表示域（黑/白点，渲染域）**，XISF 1.0 规定浮点实型**必须**带 `bounds`、无默认域，`FITSKeyword` 仅为 FITS 元数据兼容层、**不承担像素单位语义**；**到 ADU 的换算因子不由文件给出**（PCL `NormalizeSamples`：浮点目标映到 [0,1]、整数目标映到 [0,2ⁿ−1]，UInt16 `MaxSampleValue=65535`）⇒ **必须由调用方显式声明**（`master_units`/`master_scale`），禁止按文件名/后缀/目录名推断（`phase_product_exchange_matrix` R-NO-NAME-BINDING / X-NO-UNITS）。② **消费边界四条机器规则 U1–U4**（单位/标度一致；flat 已归一；dark bias 约定显式声明；声明自洽），违反即 `ErrorDomain::DATA`（CLI rc=2）+ 结构化诊断点名文件，**禁止静默按错误标度计算**。③ 平场判定带 `calibration.master_flat_median_range=[0.5,2.0]` 冻结为**状态判定带**（已归一 vs 未归一，非科学容差）；带外须显式 `master_flat_normalize="median"`（= SCI §5 `flat_norm`，对已归一平场幂等）才允许消费。 | `run/PROJECT-GOVERNANCE-01/UNIT-001/repro/repro_units.py` + `logs/02_repro_units.log`（自写 XISF/FITS 读取器，不导入 AstroCS）：T2 母版 `sampleFormat=Float32 bounds="0:1"`——`masterBias` median 0.015288（×65535 = **1001.87 ADU**）、`masterDark600` 0.015391（**1008.63 ADU**）、`masterFlatRed` 0.206381（**13525.15 ADU**，**未归一**）；亮场 T2 NGC1727 600s Red median **1481 ADU**、T4 panel1 180s Red median **1457 ADU**；`repro_dark_bias.py` + `logs/03_repro_dark_bias.log`：T2 600s/1200s 零曝光外推截距 **999.96 ADU** ≈ bias 1001.87、T4 180/300/600s 斜率 0.5700 ADU/s 截距 **955.05 ADU** ≈ bias 916.16 ⇒ 两套真实暗场母版**均含 bias**；外部原文见 SCI §14 第 6–8 条（XISF 1.0 §Image + XSD；PCL `XISFReader.cpp`；FITS 3.0 §4.2.1）；可执行门 `tools/quality/check_master_unit_guard.py --self-test`（3 负例 + 1 正例，真实二进制端到端，红→绿对照落 `run/PROJECT-GOVERNANCE-01/UNIT-001/logs/`） | 受影响产品：一切消费 XISF 母版的真实链路产物（`calibrated_*`/`cleaned_*`、Phase1 HiPS signal 及下游 mosaic/export）——E2E-001 `out/det3/*`、BIAS-001 `out/A..H/*` 标度全错（T2 600s：现行 ≈7175 ADU vs 正确 ≈472 ADU ≈ **15.2×**；T4 180s：≈3614 vs ≈359）；受影响台账数字：由真实标定帧派生的通量/SNR/测光量级；受影响代码/合同/工件：`lib/infrastructure/cli/parser.cpp` 键白名单、`lib/infrastructure/scheduler/include/astrocs/core/master_unit_guard.h`（新）、`module_adapters.cpp p1_op_calibrate`、新 ctest 单测、`config/defaults.json`；**未做**：结构化 phase_config 的声明字段未扩 schema（避免与 CFG-001 冻结键集冲突）、`ci/checks.json` 注册（本卡禁改 `ci/**`，待前台） | 文档+门+实现同批（UNIT-001）；CI 注册待前台 |
| SC-011 | 2026-09-17 | `docs/contracts/DATA_SEMANTICS.md`（§4a:45-50 裁决记录、§12.1 `var_num_sum` invalid 列、§12.2 variance/ivar invalid 列、§12.4 invalid 汇总、§20.1 `AIO_HIPS_RD_IVAR` 读侧行、§30.4-1 variance 分支）、`docs/science/NOISE_MODEL.md`（§7 空 support / Floor 夹逼两条不变量） | **F-UNC-001 裁决落地（Phase1 逐帧 variance/ivar 无覆盖/无效像素的 0 与 NaN 唯一收敛；裁决书 工程控制/PROJECT-GOVERNANCE-01/F-UNC-001-无覆盖方差语义裁决.md）**：① 写侧**不可用（无覆盖或无方差信息）⇒ `variance=0 ∧ ivar=0`**，**NaN/负只表示产品损坏**（写侧非有限/负输入 ⇒ 新增 rc=−6 硬失败，禁 clamp/禁静默跳过；读侧 ⇒ hard fail）；② tile 级不变：全无信息 ⇒ rc=−5 不落盘（0 不得构成整幅伪方差）；③ 读侧顺序固定：**先资格门（support>0 ∧ finite signal）后权重面**；`ivar==0` = 合法零权重、`variance==0` = 无信息（Phase3 映射 NaN 传播态，禁读作零不确定度）、禁止 `1/0→Inf`；④ signal/support 语义不变，无覆盖像素三产品互补（signal=NaN、support=0、variance=0/ivar=0）；⑤ 噪声模型：消费**校准后**帧且须与 drizzle 节点同一数组（新缺陷 NOISE-DRZ-ARRAY-01）；无效样本置不可用、禁外推；`variance_floor` 单位 ADU²、默认 1e-12（冻结）、clamp 只作用于可用值、生效 floor 须随产品登记。**不改任何公式**；不改 §30.1/§30.2/§30.4 的 Phase2/3 输出 NaN 同态冻结部分。 | **可复跑**：裁决全文与逐字条款对照 `工程控制/PROJECT-GOVERNANCE-01/F-UNC-001-无覆盖方差语义裁决.md` §1/§2/§3；锚点门 `python3 docs/algorithms/anchors/check_doc_line_anchors.py`（本批严格**行数中性**：DATA_SEMANTICS.md 2745→2745 行、NOISE_MODEL.md 182→182 行 ⇒ 零行漂移、零锚同步）；文本落地证据 `git diff docs/contracts/DATA_SEMANTICS.md docs/science/NOISE_MODEL.md`；本批为**文本批**，代码改动（writer NaN→0/0 + rc=−6 + sink 不跳过 + P2 审计计数 + §7 全部正负臂含 `ASTROCS_IVAR_FAULT=nan_for_uncovered`）随 IVAR-001-FUP 批次实施（前置：ORCH-001 接线 + 本裁决文本落地） | 合同面：DATA-P1-HIPS 无效语义、DATA-UNC-001 读侧零权重、DATA-P2/P3 读侧资格门；产品面：Phase1 variance/ivar 无效填充 NaN→0（该产品在默认链路尚未产出）；代码面：aio_hips_writer.cpp（NaN→0/0、rc=−6）、astro_sphere_sink.cpp（−6 不跳过）、module_adapters.cpp（P2 审计计数，FUP 批）；测试面：合成夹具断言（tests/backend/phase2_fixture_main.cpp:74 注释）随 FUP 同步；无 SCI/ALG 公式改动 | 文本已落地（本批）；代码随 IVAR-001-FUP |

### 判据变更登记（DET-001）：可复现性语义与双哈希口径

| # | 日期 | 文档 | 改什么 | 证据（可复跑） | 影响面 | 状态 |
|---|---|---|---|---|---|---|
| SC-008 | 2026-09-17 | docs/contracts/DATA_SEMANTICS.md（p1_stack.json 行）、docs/contracts/DATA_ARTIFACTS.md（DATA-P1-PHOTPROV-001 行）、docs/api/CLI_PROTOCOL_V1.md（artifact 词表）、工程控制/PROJECT-GOVERNANCE-01/ACCEPTANCE_FINAL.md（D2/E4）、tools/canonical_product_hash.py（新）、lib/infrastructure/scheduler/src/{canonical_hash.cpp,module_adapters.cpp}、include/astrocs/core/canonical_hash.h（新）、lib/infrastructure/cli/{runtime_client.cpp,commands.cpp}、lib/infrastructure/pipeline/module_ports.registry.json、CMakeLists.txt、tests/cli/test_phase1_inprocess.py | **DET-001：可复现性语义 + 规范产品哈希（判定口径变更，非科学公式变更）**。① **D5 根因与修法**：p1_op_drizzle 对 out_dir/p1_phot.json 做**存在性判定**，而该文件由 p1_op_photometry 产出；IR 里 drz 只声明消费 artifact:cal / artifact:p1_wcs，phot 只声明输出 artifact:p1_flux ⇒ 二者同为 cal/psf 下游**并发执行**，drz 可在 phot 落盘前读到「不存在」。实测 14 次：4 次 drz 早于 phot 启动 0.270–0.332 s 并全部把 photometry_provenance 记成 absent（同时把 PHOTAPPL=0/PHOTDEGRADE=1 的静默 ADU 降级写进 drizzle 帧头 = **非确定性科学降级**）。修法 = 与既有 F-8（p1_wcs）同款：phot 增 typed 输出端口 photprov（DATA-P1-PHOTPROV-001）、drz 增同名输入端口，IR 绑定 artifact:p1_phot 依赖边，由调度器保证先后（module_adapters.cpp:806/854、runtime_client.cpp:163/180、module_ports.registry.json:81/112）。② **p1_stack.json 去墙钟遥测**：elapsed_sec 从**产品**移入节点 manifest（module_adapters.cpp:3286）——manifest 登记产物必须逐字节可复现。③ **双哈希口径（判据问题）**：文件级 sha256 / integrity_sha256 保留作**完整性**校验；新增**规范产品哈希** canonical_sha256（spec astrocs.canonical-product-hash/v1）= 数据单元原始字节 sha256 + 规范化科学元数据（FITS 卡用原始 80 字节卡文本、按 keyword 排序），**排除清单**（FITS 卡 CHECKSUM/DATASUM/DATE/RUNID；JSON 键 run_id/elapsed_sec/*_utc/hips_*date/sha256/product_sha256/integrity_sha256）逐条带外部依据（FITS 4.0 §4.4.2.7 + 附录 K、cfitsio §5.8.1、reproducible-builds.org、JWST/drizzlepac/romancal 官方忽略表），并与部署内既有裁决 tools/quality/compare_products.py（P26 T3，负责人裁决 4）的墙钟清单**一致**；DATE-OBS 等科学卡明确**不排除**。④ **产品侧落地**：run manifest 每个 artifact 增 canonical_sha256 / canonical_hash_spec / canonical_format / integrity_sha256；p3_writer.json / p3_verify.json 的 sha256 / product_sha256 **替换**为命名区分的 canonical_sha256 + integrity_sha256，且 verify 侧**独立重算**并给出 canonical_match（不再透传自报值）。 | run/PROJECT-GOVERNANCE-01/DET-001/自证摘要.md + run/PROJECT-GOVERNANCE-01/DET-001/logs/**：① **D5 修复前** logs/d5_pre/summary.json（14 次 → 14 种 p1_stack.json sha256、provenance 10 present/4 absent、absent 4 次全部 drz 早启）+ logs/d5_pre/01..14_run.{cmd,out,err,rc}；**修复后** logs/d5_post/summary.json（**16 次 → 1 种** sha256 5f0dc0e5…、provenance 16/16 p1_phot.json、phot 恒定先于 drz）。② **E4/D2 判据入口** logs/e4/{compare_normalize,compare_mosaic,compare_export}.json（A/B 两次独立运行：37 个登记产物 canonical_mismatch=0，11 个文件级不同（normalize 2 / mosaic 6 / export 3），差异 100% 落在排除清单内）+ logs/e4/verdicts.json（E4_CANONICAL_VERDICT=PASS）。③ **D6 逐字节证据** logs/d6_fits_diff.txt（同配置两次 output_phase3.fits 仅 34 B 不同 = 6 张卡：主头 RUNID 与两 HDU CHECKSUM（值+注释时刻）、两 HDU DATASUM **仅注释**时刻；DATASUM 值 256291000/939524195 完全相同；两 HDU verify_checksum/verify_datasum 均 =1）。④ **可执行负例面** python3 tools/canonical_product_hash.py --self-test → CANONICAL_HASH_SELFTEST_PASS: checks=11（改 1 像素必变、改 CRVAL1 必变、改 hips_pixel_scale 必变、改科学 JSON 值必变；仅改 RUNID/DATE/时间戳/派生摘要**不变**；篡改 DATASUM 卡 → 硬错 exit 3）。⑤ **跨语言同构**：C++（manifest 字段）与 Python（工具）在 10/10 normalize 产物 + export 7 产物上 canonical_sha256 **逐字相同**（drift=0）。⑥ 门：ninja 0 FAILED；ctest 通过数 429→429（总数 429→432，3 个 p1hips_* 失败属并行 aio/hips 线的在飞改动，见自证摘要「未做项/环境」）；docs/algorithms/anchors/check_doc_line_anchors.py --root . → PASS: 38 docs, 848 anchors, EXEMPT:8, OK:840；tests/cli/test_phase1_inprocess.py -k "det001 or frozen_registry" → 2 passed。 | **判据面**：ACCEPTANCE_FINAL D2/E4 由「文件级 sha256 一致」改为「登记产物 canonical_sha256 一致」（旧判据对 HiPS hips_creation_date、FITS RUNID/CHECKSUM 原理上不可达，已给实测降级与计数）；**产品面**：run manifest artifact 行新增 4 键、p3_writer.json/p3_verify.json 键改名（sha256/product_sha256 → canonical_sha256+integrity_sha256，全仓无旧键消费者）、p1_stack.json 去掉 elapsed_sec（遥测移入节点 manifest）、IR 新增 phot→drz 依赖边（phase1 八节点链端口集变化，module_ports.registry.json 同步）；**测试面**：新增 tests/cli/test_phase1_inprocess.py::test_10_det001_… 回归门（依赖边 + provenance 不翻转 + 3 次逐字节一致）。**科学公式/默认容差零改动** | 已执行（工作区，待前台提交） |


## 与旧做法的差别（一句话对照）

- 旧：文档冻结 ⇒ 发现文档错也只能"登记、上呈、等批准"，甚至写出"以代码为准"来绕过；
- 新：**文档错就改文档**（凭标准或实验），改了登记 claim；只有"证据判不了"的才上呈。
