# NEXT_PHASE_PLAN —— 负责人指令下的收尾顺序（2026-09-16）

> 负责人原话：「**先把搬迁、整改什么的跑完，然后按顺序逐个做冒烟测试和端到端测试，debug。此前应该确认台账上的清单全部清理干净，把 bug 什么的修完。**」

## 阶段划分（严格按序，前阶段未完成不进入下一阶段）

### 阶段 0　搬迁与整改收口（进行中）
- W4 四线：`W4-A3` 门禁收口（28 执行单元）· `W4-A1` 像素中心口径 · `W4-A6` 常数残留 · `W4-A9` INT-001 架构收口；
- 判据：`cmake` rc=0 · `ninja` **0 FAILED** · `ctest` **≥ 426/425**（唯一既有红 `core_pipeline` 属 QA-001）· 锚点门 rc=0 · 注册表双向一致 0/0 · 零新增豁免。

### 阶段 1　台账清零核查（本轮新派 `SCAN-CLEAR`）
- 把 `问题扫描/REBASE_TABLE.md`（785 行）逐条对当前树复核，判定：**已修 / 仍存在 / 判据本身错 / 不适用**；
- 输出「未清零清单 + 逐条修复方案」，并把**判据本身错**的条目单独成册（R-1/R-3/R-5/R-6 已各判掉若干条）；
- 判据：**未清零项 = 0**，或全部转入已派单任务且每项有明确归属与验收门。

### 阶段 2　冒烟测试（逐命令、逐个，不批量）
- 按 `normalize → mosaic → export` 顺序，**每条命令单独跑**：`astrocs <cmd> --help` → 最小合法输入 → 失败路径（缺参数/坏配置/缺数据）；
- 每步记录：命令、输入、rc、产物（路径+哈希）、日志落 `run/PROJECT-GOVERNANCE-01/SMOKE/<cmd>/`；
- 判据：三命令的最小路径全部 rc=0 且产物可复现（同输入两次哈希一致）。

### 阶段 3　端到端测试（真实数据）
- 用 `testdata/` + `GaiaDR3`/`GaiaDR3SP`/`BASS DR3` 跑完整链路，覆盖：单帧标准化 → 马赛克 → 投影导出 → 与外部标准（astropy）对拍；
- **真实数据台账重锚**（`OPEN_ITEMS A2`）：`Galaxy_Center rms_arcsec`、`0.897px` 等值为**修复前**产物，须重测并回填；
- 判据：链路 rc=0、产物与外部标准一致（容差按冻结门表）、台账数字全部来自当前版本。

### 阶段 4　Debug
- 阶段 2/3 暴露的每个缺陷：先定位根因（可复跑探针）→ 修 → 红→绿双证据 → 前台提交；
- 不得以「环境问题」结案（AGENTS §5）。

## 版本号口径（负责人 2026-09-16 明确）
- **首个对外发布版本之前不设版本语义**；文档写的内部版本号「只是个标签」；
- ⇒ **不做版本递增动作**，不为版本号安排任何工作；`0.11.0-alpha.*` 一类字符串不进入决策；
- 首个对外发布版本的统一编号由负责人在发布时决定（发布权专属负责人）。
