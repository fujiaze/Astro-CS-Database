# DEL-102 成品帧重新交付（待负责人检查）

## 目标

VIS-102 与 PERF-102 双 PASS 后，重新生成并交付两个 R 通道平面 FITS。只交付负责人检查，不推送发布；发布决定只属负责人。

## 交付物

| 文件 | 数据 | 内容 |
|---|---|---|
| `run/RELEASE-02/deliverables/DEL-102/M42_R_plane.fits` | T2+T3 Red 300s ×49 | signal + COVERAGE 两 HDU，CHECKSUM/DATASUM，覆盖外 NaN |
| `run/RELEASE-02/deliverables/DEL-102/GalaxyCenter_R_plane.fits` | T4 Red 180s ×32 | 同上 |

## 交付前校验（前台独立复跑）

- 结构：HDU、BITPIX、WCS（RA---TAN/DEC--TAN）、CHECKSUM；
- 数值：零值 0.0000%、finite 统计、variance_available=true、逆方差路径真实生效；
- 单位：BUNIT 与数值量级一致（RELEASE-01 的"标 ADU 实为面元归一级别"问题已按 FIX-B 结论消除）；
- 视觉：随附拉伸整幅 PNG + 4×4 分块 + 裁剪放大（VIS-102 证据副本）；
- sha256、复现命令、配置 JSON 一并归档。

## 已知限制清单（相对 RELEASE-01 的变化）

RELEASE-01 的四条限制中：
1. 接缝——应由 FIX-A/VIS-102 消除；
2. 等权降级——应由 FIX-B/C 消除（默认逆方差/point_information）；
3. 单位口径——应由 FIX-B 消除；
4. 仅 R 通道——**保留**（负责人本版只要 R）。

交付说明中逐条标注 CLOSED 或保留原因；任何新发现的限制如实列出，不隐藏。

## 交付后

- 写 DEL-102 交付报告与 ACCEPTANCE 总表；
- 通知负责人检查；**FIN-001（README、版本号 0.0.1alpha、发布收尾）不在本控制包**，待负责人检查成品帧并明确认可后另开控制包执行。
