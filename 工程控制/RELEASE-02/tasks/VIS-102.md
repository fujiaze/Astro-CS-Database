# VIS-102 视觉验收（必须通过）

## 目标

ACCEPTANCE_SPEC L4 §5.2 全部通过，重点是 RELEASE-01 失败的"无接缝/背景均匀"。

## 方法（沿用 RELEASE-01 工具链）

```bash
python3 tools/quality/plane_stretch.py --input <p3>_vis/output_phase3.fits \
  --out <g>_full.png --thumbnail <g>_thumb.png --thumbnail-size 2048 \
  --stretch asinh --percentiles 1,99 --sidecar <g>_stretch.json
python3 tools/quality/plane_chunks.py --input <g>_full.png --outdir chunks_<g> \
  --grid 4 --tile 1024 --label --thumbnail 256
# 可疑区域 1:1 裁剪放大 + 行中位数剖面
python3 artifacts/RELEASE-02/VIS-102/profile_and_crop.py
```

## 检查清单（两组都过）

| 检查项 | 通过标准 |
|---|---|
| 无黑洞 | 覆盖区内零值 0.0000%；覆盖外为 NaN（非死区） |
| 无亮斑 | 无宇宙线残留/伪亮点/饱和溢出；亮星衍射星芒正常 |
| **无接缝** | **无水平/垂直接缝、无重影、无重复星点；面板交界行中位数跳变 ≤ 天区内部典型行差的 2 倍**（RELEASE-01 为 17.2–22.4× / 9.8–12.4×）；裁剪放大无台阶 |
| 星点质量 | 星点圆锐无拖尾 |
| **背景均匀** | **面板间无背景台阶、无整片偏暗/偏亮；南侧偏暗问题消除** |
| 全局观感 | M42/M43/暗带、银道/暗星云/星场密度真实可信 |

## 执行要求

- agent 必须自己先做完整视觉检查：整幅 + 4×4 分块逐块看 + 行中位数剖面；分辨率不足处裁剪放大再读，不得只看缩略图就下结论；
- 发现接缝/亮斑/异常，回到 FIX-A/C 定位修复后重跑 E2E-102 与本任务，直到全绿；
- 卫星/流星轨迹等原始数据现象如实标注，不算管线伪影；
- 证据：整幅 PNG、缩略图、分块各 16 张、裁剪放大、剖面数据，全部归档 artifacts/RELEASE-02/VIS-102/。

## 验收门

上表全部通过，前台独立复跑拉伸/切块/剖面并独立目检后写 PASS。
