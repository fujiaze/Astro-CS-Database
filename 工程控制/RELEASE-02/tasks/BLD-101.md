# BLD-101 全量构建与机器门

## 目标

全部修复合并后，独立复跑构建与所有机器门，达到"机器门全绿"。

## 内容

1. `cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release` rc=0、`ninja` rc=0，0 warning；
2. 全量 `ctest --test-dir build`（RELEASE-01 基线 447）0 失败，新增用例全部注册、归属正确；
3. 逐模块独立构建冒烟表更新：每个模块有独立 target 或在 manifest 中如实登记合并构建关系（闭合 P0-01：现仅 5 模块有独立 .so、20 模块 NOT_IMPLEMENTED 登记）；NOT_IMPLEMENTED 项逐条核实是"已实现未登记"还是"确未实现"，前者补登记、后者列入实际状态；
4. 机器门逐项复跑转绿：CHK-MODULE-MANIFEST、CHK-REGISTRY-DOC-SYNC、ENG-CONSTRAINTS、CHK-EXIT-CONSISTENCY、CHK-E2E-REPRO、CHK-RESOURCE（合成面）、CHK-SCI-REF、CHK-DANGLING、tests/quality/test_root_cleanliness；
5. 12 个历史跳过用例复核：6 个合理跳过（无 AVX512F/CUDA/ACR dormant/合成替代）保留并注明，6 个 fixture-gated 随 FIX-B 解锁；
6. verify_toolchain fail-closed 保持（RELEASE-01 已修，回归）。

## 验收门

- 构建 0 警告 0 错误；ctest 全绿；
- 上述机器门全部 PASS，无 waiver、无 owner-sanctioned 临时红（P0-01 要么闭合要么 manifest 如实反映真实状态后该门转绿）；
- 逐模块冒烟证据表（.so 路径、独立构建 rc、用例 总/过/跳/败）更新归档。
