# 任务：FIN-001 收尾与发布准备

## 目标

负责人认可 DEL-001 成品帧后，对项目收尾：更新 README 等文档、设置 CLI `--version` 为 `0.0.1alpha`、就绪发布包（白名单/哈希/版本/provenance），准备发布预览版。

## 权威依据

- 负责人指示：认可后对项目收尾，更新 readme 文档等，将版本号改成 0.0.1 alpha 准备发布
- 最高设计 §12（版本与发布权：验证全部通过时 `--version` 输出 `0.0.1alpha`，发布候选条件清单）
- ACCEPTANCE_SPEC §6（发布门：L1–L4 全过、机器门 P0 全绿、版本纪律）

## 改动范围（文件域，互斥）

- 允许改：README、安装/使用说明、发布说明；CLI `--version` 输出（写 `0.0.1alpha`）；发布包组装脚本/清单
- 禁止改：科学公式、默认容差、已验收实现（仅允许发布相关最小改动）

## 步骤

1. 核对发布候选条件（最高设计 §12）：合同冻结无冲突、追踪无断链、模块可独立加载/验证/卸载、ACR 生产不可达、heavy 无硬编码线程/单线程长计算/持续低利用率/无界内存增长、双平台 CI 通过、四层验收全过（L4 含两组目检）、P0/P1=0——逐项核验并留证；
2. 更新 README：项目定位、三命令用法（normalize/mosaic/export + `help`、`--json`、`-y`/`-force`）、安装与配置（config/）、验收状态（L1–L4 结果）、已知限制（release notes）；
3. 在 CLI `--version` 写入 `0.0.1alpha`（此前代码与产物无版本信息；此为本项目第一次出现版本号）；
4. 组装发布包：白名单文件清单、哈希校验、版本标记、provenance（输入/配置/软件来源/manifest）就绪；双平台（Windows 单入口 exe + Linux CLI）发布说明同步；
5. 写 SUMMARY.md 阶段汇总：目标、差距闭合、各模块状态、验收结论、遗留项（P2/UNRESOLVED）、发布建议；
6. 提交：前台统一按序提交（文档、版本、发布包脚本分离 commit），push 后核对三 SHA 一致；
7. 汇报负责人：发布候选就绪，等待负责人最终发布决定。

## 验收门（可机器复跑）

- [ ] 最高设计 §12 发布候选条件逐项核验留证，P0/P1=0；
- [ ] README/安装说明更新且与实际 CLI 行为一致（help 输出、--version 输出 `0.0.1alpha`）；
- [ ] `--version` 输出 `0.0.1alpha`；此前产物中无版本信息残留；
- [ ] 发布包白名单/哈希/版本/provenance 检查通过（机器门）；
- [ ] 双平台 CI 通过（或 Windows 侧按当前进度明确登记 BLOCKED 原因）；
- [ ] 三 SHA 一致（HEAD=main=origin/main）；
- [ ] SUMMARY.md 完成并交付负责人。

## 边界

- 发布决定只属负责人（最高设计 §12），agent 至多声明 `READY_FOR_OWNER_REVIEW`；
- Windows 发行版若未跑通（负责人此前计划 Linux 先跑通、Windows 后做），发布说明中如实声明平台状态，不虚报。
