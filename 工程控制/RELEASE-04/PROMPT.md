# PROMPT — RELEASE-04 新会话启动提示词

把本控制包整个目录上传给执行 agent 后，在新会话发送以下提示词：

---

执行控制包 RELEASE-04。

仓库：https://github.com/fujiaze/Astro-CS-Database ，先 `git pull` 到最新 main。
控制包已上传，入口是控制包根目录的 `00_README.md`，任务总览与依赖图在 `TASK_LIST.md`，差距清单在 `GAP_AUDIT.md`，逐任务书在 `tasks/`。

要求：

1. 先读 `AGENTS.md` 的开工纪律：从 `ASTROCS_DESIGN.md` 沿索引逐层下钻读完相关文档再动手；
2. 第一步执行 DOC-401：用包内 `docs/` 的五份根文档原样替换仓库同名文件并单独提交；
3. 按 `TASK_LIST.md` 依赖图派 SubAgent 并行干活，SubAgent 零 git 写权限，前台独立验证、统一提交；
4. **最高优先级是 SCI-401/402/403 三个创新点实验单元**（测光星等坐标系、跨帧绝对 SNR、加性天光无接缝）：每个结论必须有核验过的论文（DOI/arXiv）、开源代码（项目+版本+文件:行）、仓内实测三重佐证，用 HST 物理仿真、纯代数合成（含非退化负例）、testdata 真实数据三类数据互证，报告八要素齐全并经独立子代理审稿。我提出的方法不一定对、科学文档也不一定对：发现前提可疑就派子代理查一手文献和开源实现、做实验裁决，按"发现问题→研究→订正文档→改代码→补 Oracle/负例→独立验证→统一提交"闭环推进，不要把技术问题原样抛给我；
5. 工程治理按 CLEAN-401/402/403 做彻底清理：死代码直接删除或按规范注释（是什么/为什么/现状/删除条件/依据），陈旧控制包与历史工件收口清理，仓库只留最新生产代码与自解释文档集；
6. 全部机器门转绿后做 E2E 与 L4：M42 与银心两组数据（仅 R 通道）全流程，产出两张平面 FITS、拉伸切块 PNG，自行逐块视觉验收（无黑洞、无亮斑、无接缝，疑问区域裁剪放大再读），并做分段计时找热点优化；
7. 只有以下情况停下来问我：发布与成品帧最终判定、版本号、顶层合同结构性变更、三个创新点被实验证伪或存在互斥方向、许可证存疑、删除 testdata/用户数据；
8. 两组成品帧与全部证据就绪后声明 READY_FOR_OWNER_REVIEW，等我目检认可后再执行 FIN-401（README、版本号 0.0.1alpha、控制包收口自清理）。

每完成一批任务在 `ACCEPTANCE.md` 登记状态与证据路径，收口时写 `SUMMARY.md`。
