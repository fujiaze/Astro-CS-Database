# 任务：FIX-402 Phase3 语义守卫接线与写端口单位

## 目标
export 输入语义守卫进入生产构建：只接受面亮度语义输入，方差/逆方差显式消费传播，其余语义显式拒绝；Phase2 写端口单位从 ADU 改为 SURFACE_BRIGHTNESS；产品 BUNIT 与像素语义 provenance 齐全。

## 权威依据
- ASTROCS_DESIGN.md §5.6、§6.3；§3.1 数据对象
- GAP_AUDIT G3-2、G3-4

## 改动范围
- 允许改：`module_adapters.cpp`（约 1040-1057 行写端口）、phase3/p3_v6_export 接线（v6 处置与 CLEAN-401 协同：守卫逻辑进生产 phase3，旧壳随 v6 删除）、provenance/BUNIT 写出、测试
- 禁止改：投影科学公式（FIX-406 处理 SIN 容差）、docs

## 步骤
1. Phase2 signal 写出端口单位改 SURFACE_BRIGHTNESS，HiPS/FITS 头 BUNIT 与 manifest 像素语义字段同步；
2. 语义守卫进生产构建：输入按 provenance 单位分派——面亮度放行、flux/ADU 等拒绝并给明确错误码（exit 3/4 按语义）、variance/ivar 显式消费并按 C_out=R C_in Rᵀ 传播；
3. 输出模式 surface_brightness/point_source_flux/visualization 显式声明，visualization 标注不可测量；
4. 测试：合法面亮度输入通过；ADU/flux 输入被拒；缺 BUNIT/缺 provenance 被拒；方差传播数值对拍。

## 验收门
- [ ] 非面亮度输入 100% 被拒（负例矩阵全红得正确）；
- [ ] grep 生产写端口无 UnitId::ADU 残留于 Phase2；
- [ ] BUNIT/像素语义 provenance 在 Phase2/3 产品齐全；
- [ ] ctest 新增用例全绿。

## 禁止
- 不做"自动猜测单位"的宽松解析。
