# 任务：DOC-401 替换五份根文档

## 目标
用控制包内 `docs/` 的最新版根文档替换仓库同名文件，使后续全部任务以新版权威文档为准。

## 权威依据
- CONTROL_PACK_SPEC.md §2（控制包可自带 docs/，执行首步替换）
- 包内 `docs/ASTROCS_DESIGN.md`（最高权威新版）

## 改动范围（文件域，互斥）
- 允许改（整体覆盖）：仓库根 `AGENTS.md`、`ASTROCS_DESIGN.md`、`ENGINEERING_SPEC.md`、`ACCEPTANCE_SPEC.md`、`CONTROL_PACK_SPEC.md`
- 禁止改：其他任何文件、代码、docs/ 下级文档（属 DOC-402/403/404）

## 步骤
1. `git pull` 到最新 main，确认工作区干净（预存修改先登记，不自动处理）；
2. 用包内 `docs/` 五份文件原样覆盖仓库根同名文件（逐字节一致，不做二次编辑）；
3. 单独提交一次，提交信息说明"根文档替换为 RELEASE-04 新版（最高设计重写：三创新点/实验单元/预检最终语义/治理清理）"；
4. push 后核对三 SHA 一致。

## 验收门（可机器复跑）
- [ ] `diff <(cat docs/ASTROCS_DESIGN.md) <(git show HEAD:ASTROCS_DESIGN.md)>` 等五组 diff 为空；
- [ ] 根文档中无日期、无任务编号（EXP-/RELEASE-/§9.x）、无"旧版/作废/留痕"等历史叙事（机器扫描脚本，允许的角色表述"负责人批准/终审"除外）；
- [ ] 文档类机器门通过（悬空链接检查此时可能因下级文档尚未对齐而红——记录并转入 DOC-403，不作为本任务阻断）。

## 禁止
- 不"顺手"修改包内文档文字；发现问题登记到 GAP 清单上呈，由前台统一处理；
- 不动代码。
