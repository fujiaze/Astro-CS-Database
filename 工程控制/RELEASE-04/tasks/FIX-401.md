# 任务：FIX-401 HiPS tile 原子发布与 Phase2 暂存区

## 目标
所有 HiPS tile 走"私有临时区 → 校验 → fsync → 哈希 → 原子 rename → 完成清单"；Phase2 不直写输出目录，先写暂存区，完成后原子发布。

## 权威依据
- ASTROCS_DESIGN.md §10（I/O 与原子产品）
- GAP_AUDIT G3-1

## 改动范围
- 允许改：`lib/infrastructure/aio/**`（HiPS writer、manifest、完成清单）、phase2 输出路径接线、相关测试
- 禁止改：科学算法、docs（文档缺口登记给 DOC 任务）

## 步骤
1. HiPS tile 写入改为每 tile 临时文件 + fsync + 哈希校验 + 原子 rename，全部 tile 完成后写完成清单（manifest），无清单则消费者不认；
2. Phase2 在运行私有暂存区生成全部产品，成功对象校验通过后统一原子发布到 output_dir；
3. 失败/取消路径清理临时产物，正式目录不出现半成品 tile；
4. 补测试：正常发布、写入中途 kill（SIGTERM）、磁盘满模拟、消费者对无清单目录的拒绝；
5. 与 CLEAN-403 协同（所有新 I/O 经 aio）。

## 验收门
- [ ] 中途 kill 后 output_dir 无半成品 tile、无完成清单，重跑不消费残留；
- [ ] 磁盘满时 exit 10 且无半成品；
- [ ] 新增负例测试在 CI 全绿；
- [ ] 合成全链 Phase1/Phase2 产品哈希与 manifest 完整。

## 禁止
- 不用"写后忽略"代替完成清单协议。
