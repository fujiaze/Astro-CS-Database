# 任务：FIX-404 登记与映射闭合

## 目标
MODULE_MAP 声明全部指向真实路径；代码注释无悬空文档/文件引用；variance 进入 DATA_SEMANTICS 标准块表；命名块白名单收口。

## 权威依据
- ASTROCS_DESIGN.md §0.2、§8.4；ENGINEERING_SPEC.md §10
- GAP_AUDIT G1-5、G3-8

## 改动范围
- 允许改：`MODULE_MAP.yaml`（164 条声明）、lib/ 注释（40 条悬空引用）、`DATA_SEMANTICS` 标准块表、`aio_pipeline.h`（约 304 行"自定义块名也允许"）、映射检查器
- 禁止改：模块实现本身、docs 正文（索引归 DOC-403）

## 步骤
1. MODULE_MAP 53 条不存在路径逐条核实：文件已移动的更新路径、已删除的移除声明、缺失实现的登记缺口转对应 FIX/CLEAN；
2. lib/ 注释 40 条引用（真 28/假 12）逐条修正或删除；
3. variance（及 ivar）按 UNIFIED_MODEL 正本补入 DATA_SEMANTICS 标准块表，端口引用随之闭合；
4. `aio_pipeline.h` 收紧为只接受标准块表中的命名块；确需扩展走显式注册而非任意自定义名；
5. 映射检查器增强：声明↔文件↔CMake target↔产品清单四方一致，fail-closed + 红绿自测；与 DOC-403 的 doc-index 门共用扫描数据。

## 验收门
- [ ] MODULE_MAP 假路径 = 0（机器出表）；
- [ ] 悬空注释引用 = 0；
- [ ] variance 在标准块表且端口测试通过；任意自定义块名被拒（负例）；
- [ ] `ci --all` 全绿。

## 禁止
- 不删除"声明存在但实现缺失"的条目来粉饰——这类转缺口登记。
