# 任务：FIX-403 HiPS hierarchy 累加改双精度

## 目标
消除 `aio_hips_writer.cpp` hierarchy 累加用 f32 导致的深层级偏差（dk=9 实测 2.5e-3/3.95e-4），层级累加在 f64 进行，落盘按声明位深量化。

## 权威依据
- ASTROCS_DESIGN.md §3.3（默认科学计算双精度）、§10
- GAP_AUDIT G3-3

## 改动范围
- 允许改：`aio_hips_writer.cpp`（约 495-499、776-800 行 hierarchy 累加路径）、对应单测
- 禁止改：HiPS 产品 schema、其他 aio 路径

## 步骤
1. hierarchy 父层累加（通量/面积/计数）全部在 f64 累加器进行，写出时按产品声明位深量化；
2. 补 Oracle：独立 f64 参考实现生成多层级 tile，对比 dk=1..10 偏差；
3. 评估内存增量并记录（累加器仅层级汇总，应远小于稠密面代价）；
4. 负例：强制 f32 累加路径时测试必须判红（保护测试）。

## 验收门
- [ ] dk=9 偏差进入合同容差（给出修复前后对照表）；
- [ ] 全层级通量守恒（父子层积分通量一致，容差冻结）；
- [ ] ctest 全绿，1/N worker 一致。

## 禁止
- 不以"换容差"代替精度修复。
