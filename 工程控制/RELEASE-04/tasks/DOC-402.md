# 任务：DOC-402 下级文档对齐新最高设计

## 目标
让 docs/ 下全部下级文档与新版最高设计口径一致、写法合规：只写现行设计，无历史叙事，已定案科学口径全文统一。

## 权威依据
- ASTROCS_DESIGN.md §0.3（文档写法）、§2（三创新点）、§5（排异五档/control_ivar/面亮度/NaN）、§4.5（预检）
- ENGINEERING_SPEC.md §8（文档集自解释）

## 改动范围（文件域，互斥）
- 允许改：`docs/plugins/**`、`docs/science/**`、`docs/algorithms/**`、`docs/design/**`、`docs/architecture/**`、`docs/interfaces/**`、`docs/standards/**`、`docs/modules/**`、`docs/development/**`、`docs/validation/**`、`docs/GLOSSARY.md`、`docs/ARCHITECTURE.md` 等下级文档
- 禁止改：五份根文档（DOC-401 已定）、`docs/research/**`（DOC-404 与 SCI 任务域）、`实验/**`、代码

## 步骤
1. 全量扫描下级文档中的历史叙事：日期戳、任务/实验编号（EXP-2xx、§9.4x、RELEASE-0x、change-claim）、"旧版/曾/作废/留痕/退役窗口"等，逐条改写为现行口径的肯定陈述（结论保留，过程删除）；
2. 逐项核对已定案口径并统一：
   - 排异最终五档（N 1–3 none / 4–5 percentile / 6–15 winsorized / ≥16 linear fit；N=几何覆盖帧数；min/max 不用于生产）；
   - 天光采样权重 control_ivar（w_ki 公式），SNR² 不作变化背景电平的逆方差代理；
   - Phase2 信号量纲=面亮度，写端口 SURFACE_BRIGHTNESS；
   - NaN 样本级掩膜 + 重归一 + 覆盖级 NaN + 强制计数；
   - 噪声模型 A 为唯一生产模型（模型 B 的内容从正式文档删除，结论只保留"A 为生产模型"）；
   - 全程只有 SNR、无权重模式；snr_path 三键 dense/sparse_reconstruct（默认）/frame_reconstruct；
   - 预检三档 correct/warn/error 与确认语义（§4.5）；
3. 三创新点在相关科学文档中的表述与最高设计 §2 一致（测光星等坐标系、绝对 SNR、加性天光），细节指针指向 SCI 实验单元与研究包；
4. 公式/数值只在 science/algorithms 出现，plugins 文档引用而不复制；
5. 每类文档一个提交，提交信息注明对齐的条款。

## 验收门（可机器复跑）
- [ ] 机器扫描：docs/ 下无日期、无任务编号、无历史叙事词（白名单方式给出允许项）；
- [ ] 关键词口径检查：五档表/control_ivar/SURFACE_BRIGHTNESS/样本级掩膜/噪声模型 A 在相关文档各出现且表述一致；
- [ ] 无"权重模式/weight_mode"作为现行概念出现；
- [ ] `ci --all` 文档门全绿（索引门与 DOC-403 协同）；
- [ ] 抽查 5 篇文档，公式与最高设计/science 正本无矛盾。

## 禁止
- 不改科学公式本身（公式订正只能由 SCI 任务凭实验证据发起）；
- 不删除仍被代码引用且未退役的接口说明；
- 不新建平行文档（能并入现有文档的并入）。
