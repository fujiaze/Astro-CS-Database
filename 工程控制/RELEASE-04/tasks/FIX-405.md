# 任务：FIX-405 配置/产物/事件/导出小项收口

## 目标
收口 RELEASE-03 遗留的若干小项，使产品合同与机器判据一致。

## 权威依据
- ASTROCS_DESIGN.md §3.1（无权重模式）、§7.2（事件流）、§10
- GAP_AUDIT G3-5、G3-7、G3-9..G3-13

## 改动范围（逐项独立提交）
- 允许改：
  1. drizzle 非有限样本处理（暴露 `n_rejected_nonfinite` 计数，消除静默 continue）；
  2. `variance_floor` 钳制改 fail-closed（触发即显式状态/错误，不静默放行）；
  3. DISP-P2SMP-002 `rejected_insufficient_retained` 双计数修复；
  4. 事件流 10 类开放 kind 全部登记 schema（progress/resource/artifact/backend/final 等）；
  5. export FITS 严格字节判据：RUNID/CHECKSUM 等易变头从字节判据掩码或固定化；`astrocs verify` 纳入命令树（doctor 或 verify 子命令，按最高设计 §7.1 命令树落位）；
  6. `p2_final.json` 移除 `weight_mode:2`（全程无权重模式）；
  7. `compare_bitwise.py` 掩码 `sampler_config.cpu_workers`；
- 禁止改：科学公式、docs（由 DOC 任务同步措辞）

## 步骤
1. 七小项逐项修复 + 逐项测试 + 逐项提交；
2. 每项给红/绿用例（非有限样本计数、floor 触发、双计数、未知 kind 拒绝、字节判据稳定、weight_mode grep 为零、workers 掩码生效）。

## 验收门
- [ ] 七项各有自动化测试且全绿；
- [ ] 全仓 grep `weight_mode` 生产产物为零；
- [ ] 事件 kind 注册表与文档一致，未登记 kind 被拒；
- [ ] 同输入两次 export 严格字节比较通过；
- [ ] `ci --all` 全绿。

## 禁止
- 不把多个小项混成一个提交。
