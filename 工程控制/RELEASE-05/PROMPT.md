# PROMPT — RELEASE-05 新会话启动提示词

把本控制包整个目录上传给执行 agent 后，在新会话发送以下提示词：

---

执行控制包 RELEASE-05。

仓库：https://github.com/fujiaze/Astro-CS-Database ，先 `git pull` 到最新 main。
入口 `00_README.md`，任务总览与双线依赖图在 `TASK_LIST.md`，差距清单 `GAP_AUDIT.md`，任务书 `tasks/`。

总体顺序：

1. 先读 `AGENTS.md`：从 `ASTROCS_DESIGN.md` 沿索引逐层下钻读完再动手。
2. DOC-501：用包内 `docs/` 原样替换仓库五份根文档与 `docs/owner/PROJECT_SPEC.md`，单独提交。**最高设计 §8 已重写**：三命令三独立进程三调度器、阶段间只走 HiPS、阶段内命名块内存管线与块生命周期、normalize 异步工作流/mosaic 窗口并行/export 子块流式、性能探针驱动优化。
3. 阶段 1 科学闭环（DOC-502 + SCI-501/502/506）：把 RELEASE-04 三个实验单元的定案结论订正进科学/插件文档（含 σ_sky 不双计读噪的正确口径、c_est 单位、UPM 参考面表示能力边界、测光 n≥100 适用域、converged 0/1/2/3）；修复 SNR 双计（σ_F 高估 12.8%~36.6%）、UPM 三个缺陷（绝对容差永不收敛、状态缺档、正规方程近奇异）、p1001 红；每项配 Monte Carlo/Oracle 与负例。SCI-503/504/505 产出三篇**论文形式的中文精读报告**（摘要/引言/方法/实验设计/结果精选图表/讨论与诚实边界/结论/参考文献），给我看的，不堆原始数据。
4. CONTRACT-501 先冻结双线文件域与接口合同（块生命周期、三调度器接口、性能门判据、监控字段语义、docs/contracts 与 eng/contracts 双向对应），然后**双线并行互不打扰**：
   - A 线（lib/）：ARCH-501 命名块机制 → ARCH-502/503/504 三阶段调度器 → ARCH-505 全部生产节点改写为块读写、合成全链数值等价后退役 Session 与未采纳的 orchestrator → CLEAN-501 死代码与注册表缺口；
   - B 线（eng/、docs/contracts、docs/ci）：GATE-501 门禁审计（L2 假门改真判红、恒定 50% 指标、requires_monitor 语义、陈旧输出路径、fail-closed 普查，每门配正负例）、GATE-502 测试审计（不存在措辞的断言、失效反例、空断言普查、缺口测试补齐）；
   - B 线发现代码缺陷只登记派单给 A 线，不越域改 lib/；A 线不改门禁与断言口径；共享文档在阶段 1 冻结，争议前台裁决。
5. 性能优化（PERF-501）排在功能与数值正确之后：用预埋探针实测数据定位阶段串行段与约 3.9GB/帧读放大，优化窗口大小/预取/帧并发，L2 门必须是真判红且达标；1/N worker 逐位一致不破坏。
6. ACCEPT-501 独立验收（执行者不参与实现）：逐项核对**科学-文档-合同-代码-测试**五一致性，问题回派闭环、自迭代；无法裁决的写入审核包 `OPEN_QUESTIONS.md`，不要硬改。
7. 然后 BLD → E2E（含 correct/warn/error × yes/-y/-force 预检矩阵）→ VIS-501：M42 与银心两组数据（仅 R 通道）全流程，两张平面 FITS + 拉伸切块 PNG，自行逐块目检（无黑洞、亮斑、接缝，疑问区裁剪放大），接缝度量与 SCI-C 互证，分段计时热点优化，交我目检。
8. REPORT-501 汇总：三篇论文报告 + 五一致性审核报告 + OPEN_QUESTIONS + 性能/视觉证据索引。
9. 只有以下情况停下来问我：发布与成品帧判定、版本号、顶层合同结构性变更、创新点被证伪或互斥方向、许可证、删 testdata/用户数据、双线争议前台无法裁决。我认可两组成品帧后再做收尾（README、0.0.1alpha、控制包收口）。

状态登记 `ACCEPTANCE.md`，收口写 `SUMMARY.md`。
