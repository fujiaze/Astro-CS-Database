# 任务：GATE-502 测试体系审计与补充（B 线）

## 目标
测试必须有判别力且与现行合同一致：清除断言不存在措辞、失效反例、空断言，补齐能力缺口测试。

## 权威依据
最高设计 §12；ENGINEERING_SPEC §10；GAP_AUDIT G2-4..G2-7。

## 改动范围
- 允许改：`eng/tests/**`、`docs/api/**`（仅协议措辞同步）、`eng/packaging/config/**` 的测试夹具
- 禁止改：lib/、eng/ci 注册表（GATE-501 域）；科学口径变更需引 DOC-502 条款

## 步骤
1. linux-main profile 15 条内容面红逐条处置：产品缺陷→派单 A 线（登记回执编号）、测试过时→按现行合同修、夹具问题→修夹具；
2. api 测试 5 条断言 CLI_PROTOCOL_V1 中不存在措辞：与 CLI 协议合同核对，改为真实存在的字段/行为断言；
3. CFG002-04 依赖已删的 bader r 反例：改用现行 filters.json 中真实存在的失配反例（型号错/路径缺），保证 error 阻断语义被测试；
4. 空断言/恒真普查：扫描无断言、assert(true)、断言常量的测试（RELEASE-04 已发现 io_adapter 一例），逐个补判别力断言；建立 CI 静态检查防复发；
5. 66 路径缺口/72 能力缺口（与 CLEAN-501 共享清单）：每个"待实现"项补对应测试（先红后绿或标记 DEFERRED 不进全绿集）；"作废"项删测试与注册表；
6. 预检语义矩阵测试补齐：correct/warn 均需 yes、-y/-yes 跳过、error 阻断且 -y 不可越、-force 跳过整个检查（最高设计 §6.2）；
7. 测试分层复核：单元/合同/集成/科学 Oracle 边界清晰，科学测试固定 seed 可复现。

## 验收门
- [ ] linux-main 15 红全部闭环（绿或派单回执）；全量 ctest 零红；
- [ ] 空断言普查表清零并有防复发检查；
- [ ] 预检矩阵测试覆盖全部组合（含 -force 越权保护）；
- [ ] 每个新增/修改测试有红绿双向证据（先验证它能红）。

## 禁止
- 不删除失败测试求绿；不写无断言的占位测试。
