# 任务：DOC-502 科学定案结论订正入文档

## 目标
把 RELEASE-04 三个实验单元的定案结论与本轮裁决写入科学/插件/研究文档，使文档先于代码正确；为双线冻结共享面。

## 权威依据
最高设计 §2、§4.2、§5.4、§8；三实验单元 results/ 与 REVIEW.md。

## 改动范围
- 允许改：`docs/science/**`、`docs/plugins/**`、`docs/algorithms/**`、`docs/research/**`、`docs/design/UNIFIED_MODEL.md`
- 禁止改：五根文档与 PROJECT_SPEC（DOC-501 已定）、lib/ 代码、eng/（SCI 修复任务域）

## 步骤（逐条成文，每条给实验证据指针）
1. **噪声口径冻结**（SCI-B D1）：写清 σ_F 噪声项组成中 σ_sky 的唯一口径——上游填"天光+暗流散粒"时不再加 (RN/g)²；填"经验总 rms（含读噪）"时不重复加；给出决策树与默认路径。修复代码是 SCI-501，本任务只改文档；
2. **c_est 单位订正**（SCI-B D2）：1.44/√N 是相对标准误（dex 为 1.166/√N），订正 NOISE_MODEL 与实验文档中被当 dex 阈值的表述；
3. **UPM 表示能力边界**（SCI-C）：参考面须能表示帧间天光差空间尺度；不可表示且沿向相干、尺度 ≲2 节点间距的分量使残余接缝 ≈0.8×RMS；写清节点间距/平滑参数/粗糙度正则化的选择依据与 provenance 条件数记录；
4. **UPM 收敛语义**：容差按观测尺度归一（相对容差）；converged 状态枚举 **0=max_iter / 1=converged / 2=stalled / 3=invalid** 的定义（以仓内权威 `docs/plugins/algorithms_phase2/11_upm.md` §4.6 为准）；
5. **测光星数语义**：星数**不构成拒绝条件**（不设适用域门槛）；星少到 SCI-PHOT-001 §4 求解前提不成立时走 NO_DATA 拟合失败路径；N5 现象作为判据的低样本量能力边界记录；
6. **权重退役**：contracts/科学文档中 weight_modes、forbidden.weight_source_tokens 等历史段落按"全程只有 SNR"重写（与 CLEAN-501 联动删代码侧）；
7. 三口径适用域图谱（地面视宁度 sparse 优、HST 帧级优、稠密 64× 超存储门）写入 SNR 科学文档；
8. 全部改动过 doc-index 与 doc-symbols 门，无历史叙事。

## 验收门
- [ ] 上述 7 条各有文档落点与实验证据指针（出对照表）；
- [ ] doc-index --strict 全绿；无日期/编号流水账；
- [ ] 独立子代理审稿：文档表述与实验 results/JSON 数值一致。

## 禁止
- 不改公式去迁就代码；代码与文档冲突转 SCI 任务按证据修代码。
