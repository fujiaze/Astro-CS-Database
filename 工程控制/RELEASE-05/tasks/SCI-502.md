# 任务：SCI-502 UPM 三个生产缺陷修复

## 目标
修复 SCI-C 登记的三个生产缺陷，使 UPM 在真实尺度收敛、状态可诊断、近奇异有处理。

## 权威依据
最高设计 §5.4；实验/additive-sky-seamless/README.md §10（FIX-1/2/3）；DOC-502 收敛语义。

## 改动范围
- 允许改：phase2 UPM 实现（`lib/algorithms/upm/**`、`lib/infrastructure/scheduler/src/module_adapters.cpp` 约 5948-5949 行及 out_converged）、对应测试
- 禁止改：SCI-C 实验脚本的判据（实验是证据，不动）、门禁、文档（DOC-502 域）

## 步骤
1. **FIX-1 容差**：绝对 tolerance=1e-6 改按观测尺度归一的相对容差（或尺度归一化），验证 ~300 e⁻ 尺度下有限步收敛；保留绝对容差作为近零尺度保护；
2. **FIX-2 状态机**：out_converged 输出 0/1/2/3（收敛/未收敛/达 max_iter/stalled），补 stalled 检测（SCI-C 诚实边界指出原 stalled 臂跑满 300 轮，先实现可证伪的停滞判据：残差改善量低于数值地板连续 N 次）；
3. **FIX-3 近奇异**：天光面正规方程条件数 κ 可观测（真实 M42 样本 κ=3.16e7）；加粗糙度正则化（roughness_penalty）或节点数自适应，κ 超阈值时走自适应并写 provenance；
4. 复跑 SCI-C 全部判据（c1..c7）：纯加性 12.5× 接缝压缩、保留背景、非退化判据 36.4σ、稀疏-稠密等价 3.1e-15 不回退；
5. 新增负例：不收敛输入→状态 2/3 且产品显式标记；近奇异输入→自适应触发可观测。

## 验收门
- [ ] M42 真实样本有限迭代 converged=1（给迭代数与残差表）；
- [ ] 0/1/2/3 四态各有测试；stalled 判据红/绿双向；
- [ ] κ 处理有测试与 provenance 字段；
- [ ] SCI-C 判据复跑全绿（结果 JSON 与归档一致或差异可解释）。

## 禁止
- 不通过放宽接缝判据换取收敛。
