# 任务：SCI-501 SNR 噪声项双计修复（FIX-407）

## 目标
消除 σ_sky 与读出噪声的重复计入，使帧级 SNR 在 Monte Carlo 真值下成立；修复前 σ_F 高估 +12.8%（基线）~+36.6%（最坏）。

## 权威依据
最高设计 §2.2、§4.4；实验/absolute-snr/results/REVIEW.md（D1）、DOC-502 冻结的噪声口径。

## 改动范围
- 允许改：`lib/algorithms/noise_snr/**`、`lib/infrastructure/scheduler/src/module_adapters.cpp` 约 4258 行附近（σ_sky 组合）、对应科学测试（按双线登记）
- 禁止改：门禁注册表、其他模块、文档（DOC-502 域）

## 步骤
1. 按 DOC-502 冻结的决策树实现：σ_sky 输入语义显式声明（散粒 only / 经验总 rms），两条路径各加测试锁口径；默认 gain=0 的 PSF 行路径验证不受影响；
2. Monte Carlo 复算 SCI-B b1/b2 扫描：26+29 扫描点全部 ≤3σ；天光抬升 SNR 单调趋零斜率 −1/2 保持；
3. 构造双计负例：旧组合方式必须被新测试判红（保护测试）；
4. 生产 C++ 与 Python 镜像对拍恢复 2.24e-14 量级；
5. ctest -R 'p1snr|p1noise' 全绿；更新实验 results 中受影响数字并记录修复前后对照。

## 验收门
- [ ] 双计臂负例判红、正确口径臂全绿；
- [ ] σ_F 高估消除（修复后偏差在 MC 3σ 内），给对照表；
- [ ] 1/N worker 一致；ctest 全绿；
- [ ] SCI-B 报告（SCI-504）使用修复后结果。

## 禁止
- 不用放宽容差掩盖；不改 SNR 定义式本身（定义已被实验证实）。
