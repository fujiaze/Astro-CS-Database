# 任务：ARCH-501 命名块机制与块生命周期（A 线）

## 目标
实现最高设计 §8.2 的阶段内内存管线：PipelineFrame 承载命名块，模块读块/写新块/显式消费旧块，调度器按生命周期即时回收内存。

## 权威依据
最高设计 §8.1/§8.2；ENGINEERING_SPEC §4.1；CONTRACT-501 的块生命周期合同。

## 改动范围
- 允许改：`lib/infrastructure/pipeline/**`（现有命名块/帧机制可复用、不足则扩展）、其共址测试
- 禁止改：科学算法模块、eng/、三阶段 Session（ARCH-505 域）

## 步骤
1. 盘点现有 pipeline/ 与 orchestrator 中可复用的块/帧原语（34 文件 18427 行未接入生产，甄别可复用部分，避免重写）；
2. 实现 block 元数据（合同字段）、引用计数/消费者集合、创建-消费-销毁状态机；块被全部声明消费者用完即销毁、内存归还；
3. PipelineFrame 支持 provenance 头部 KV 随块流转、可缺块的显式降级声明；
4. DAG 校验器：每个被消费块有且仅有生产者；长/短生命周期声明与实际一致；非法引用（消费不存在的块、重复生产）在构建期报错；
5. 单测：生灭顺序、内存即时归还（RSS 观测）、异常/取消路径无泄漏、退化面不挂帧的负例。

## 验收门
- [ ] 生命周期单测覆盖合同状态机全分支；
- [ ] 内存归还可观测（构造 raw→calibrated→photo 链，raw/calibrated 销毁点 RSS 下降或池回收计数）；
- [ ] DAG 校验器对三类非法图各有红绿测试；
- [ ] 不引入跨阶段状态、不引入全局单例帧。

## 禁止
- 不做科学计算；不改三阶段生产装配（ARCH-505 做）。
