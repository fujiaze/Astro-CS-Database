# 任务：DOC-202 详细层其余 🔴 订正（36 条）

## 目标

把四份提取报告中**与最高设计相反**的 🔴 条目逐条处置，消除「最高设计与详细层两边并存」的状态。

## 权威依据

- ASTROCS_DESIGN.md（已补齐的 1,159 行版本）
- run/RELEASE-02/design-merge/DESIGN-DRAFT.md §3.2（36 条）+ §2（26 条已落地条文订正的对应详细层侧）
- ARCH-01 / MOD-01 / IFC-01 / CTR-01 四份报告 🔴 清单

## 改动范围（文件域，互斥）

允许改：`docs/architecture/**`、`docs/interfaces/**`、`docs/standards/**`、`docs/modules/**`
禁止改：`docs/contracts/**`（DOC-203）、`docs/science/**`、`docs/algorithms/**`、`lib/**`

## 步骤

1. 逐条处置 🔴，每条**必须**二选一并写明：**(a) 改详细层**（最高设计对）或 **(b) 上呈**（科学问题 ⇒ 挂 EXP-* 待定）；**禁止**保留两边并存；
2. 高优先级已知项：① 原子性（HiPS tile 非原子 vs 最高设计「所有产品原子」）⇒ 改详细层为「如实登记本期例外」（最高设计 §9 已同步）；② 投影集（设计冻结 8 种 vs 实际仅 TAN）⇒ 改详细层为「未实现必须显式报不支持」；③ 模块命名（`scheduler`/`pipeline` vs `runtime`）⇒ 统一为 `scheduler`+`pipeline`；④ 星表客户端（网络 vs 离线零网络）⇒ 改「离线、零网络」；⑤ UPM apply 口径（`raw−δ_k` vs 全量 `raw−C_f(p)`）⇒ 统一为「只扣 δ_k，保留公共天光面」；⑥ Phase3 接受域（拒 variance 输入 vs 最高设计「任意兼容 HiPS 可直接进」）⇒ 改「接受任一合同兼容 HiPS」；⑦ NaN 契约（禁传播 vs 已闭环为传播）⇒ **挂 EXP-202 待定**；⑧ Windows 工具链（MinGW64 vs MSVC）⇒ 改 MSVC；⑨ `API-001` 第二套命令树含被禁的 `run` ⇒ **作废该命令面表**；⑩ `ARCH-001` 自称「V6 唯一权威」⇒ 删该声明；⑪ 合同层版本号 ⇒ 改「日期或变更编号」；⑫ 退出码表路径 ⇒ 订正为 `lib/infrastructure/cli/exit_codes.h`；⑬ 事件 JSONL 双身份 ⇒ 统一为一套；⑭ `resource_timeseries.csv` 双合同 ⇒ **挂 DOC-203 Q4**；⑮ `run/` vs 块级 `output_dir` ⇒ 统一为「产品只落 `output_dir`，`run/` 只放临时产物与日志」；⑯ Phase2 链入 ACR ⇒ 改「生产构建不得链入 ACR/CUDA」；⑰ worker 默认 `hardware_concurrency` ⇒ 删默认值，改由 benchmark profile 决定；
3. 每条改动在提交消息与 `ACCEPTANCE.md` 中登记「原状 / 改为 / 依据」；
4. 改完自查：四报告 🔴 清单逐条打勾，无遗漏。

## 验收门（可机器复跑）

- [ ] `grep -rn 'MinGW64' docs/standards/` 只剩「须订正」留痕
- [ ] `grep -rn 'runtime' docs/modules/MODULE_MAP.yaml | grep -v scheduler` 语义检查通过（无第二名字指同一职责）
- [ ] `grep -rn '唯一权威' docs/ | grep -v 'ASTROCS_DESIGN'` **输出为空**
- [ ] `python3 ci/run_checks.py` 相关项 rc=0
- [ ] `tests/config` 58 passed（锚点同步）

## 禁止

- 不得改 `docs/science/**`、`docs/algorithms/**`（只读权威；科学问题挂 EXP-*）
- 不得在 main 外开分支
- 不得挂 waiver
- **不得两边并存**——每条 🔴 必须有明确归宿
