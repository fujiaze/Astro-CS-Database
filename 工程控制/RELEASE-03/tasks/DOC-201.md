# 任务：DOC-201 详细层「权重模式」整套作废（§9.73 A44）

## 目标

让详细文档层不再出现「权重模式 / 权重档位 / mode0·mode1·mode2」这一整套**已被负责人判定不存在**的概念，使详细层与最高设计 §2.1 一致。

## 权威依据

- ASTROCS_DESIGN.md §2.1（总纲：全程只有 SNR，不存在「权重模式」）
- GAP_AUDIT.md §9.73 裁决 A44（负责人逐字：「这是一个概念吗？而且哪里有权重……全程都是 SNR 才对……直接叫 weight 不行吗」）
- run/RELEASE-02/design-merge/DESIGN-DRAFT.md §3.1（22 处逐处清单 W01–W22 + B 组 schema + C 组其它文档）
- run/RELEASE-02/design-merge/CTR-01.md 🔴1（约 20 处实测位置）

## 改动范围（文件域，互斥）

允许改：`docs/contracts/**`、`docs/modules/**`、`docs/architecture/**`、`docs/science/v6/**`、`README.md`
禁止改：`lib/**`（代码侧由 FIX-202/FIX-207 承担）、`docs/science/**`（非 v6）、`docs/algorithms/**`、`contracts/**`、`config/**`

## 步骤

1. 按 DESIGN-DRAFT §3.1 的 W01–W22 逐处处置：**删键 / 改写 / 退役**，每处保留一行「**已按 §9.73 A44 作废**」留痕（含日期与依据）；
2. §3.1 B 组（v6 schema / 数据字典 / 冻结件）**随 v6 合同层去留**一并处置——**先等 DOC-203 的 Q2 裁决**，本任务只做 A 组 + C 组；
3. C 组：`docs/architecture/ARCH-001.md:63-64`、`README.md:14,45`、`docs/algorithms/PHASE2_MOSAIC_WRITE.md:140`、`docs/plugins/algorithms_phase1/07_noise_snr.md:81`、`docs/modules/registry/astrocs.phase2.write.md:40,45-47,87-90`、`docs/modules/phase2.md:50-56` 等；
4. 统一替换口径：凡「权重模式」处改为「**权重（阶段二按像素集合现场算的派生量）**」；凡 `weight_mode` 键名处**删除**并注明去向；
5. 改完自查：`grep -rn 'weight_mode\|权重模式\|权重档位' docs/ README.md` 结果只剩「已作废」留痕行。

## 验收门（可机器复跑）

- [ ] `grep -rn 'weight_mode' docs/ README.md | grep -v '已按 §9.73 A44 作废'` **输出为空**（rc=1）
- [ ] `grep -rn '权重模式' docs/ README.md | grep -v '不存在' | grep -v '已作废'` **输出为空**
- [ ] `python3 ci/run_checks.py` 相关项 rc=0（含 `CHK-REGISTRY-DOC-SYNC`）
- [ ] `python3 tests/config/check_cfg002_registry.py --self-test` → PASS（文档行号变动须同步 registry 锚点）
- [ ] 新增负例：故意写回一处 `weight_mode` ⇒ 检查器**判红**（能红能绿）

## 禁止

- 不得修改科学公式/默认容差（ENGINEERING_SPEC §3）
- 不得改 `docs/science/**`（非 v6）与 `docs/algorithms/**`（只读权威）
- 不得在 main 外开分支
- 不得删检查器或挂 waiver
- 改文档行号后**必须**同步 `config/config_registry.json` 与 `config/defaults.json` 的 `source_ref` 锚点（否则 CFG002 判红）
