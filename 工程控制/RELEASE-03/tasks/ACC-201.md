# 任务：ACC-201 验收（§7.1 三层）

## 目标

对全部任务做前台独立验收，逐任务写入 `ACCEPTANCE.md` 的 PASS/FAIL/BLOCKED 结论。

## 权威依据

- CONTROL_PACK_SPEC.md §6.3（任务状态：PASS 仅由前台验收后写入）、§7.1（验收三层）、§7.2（ACCEPTANCE.md 格式）、§7.3（禁止事项）
- CONTROL_PACK_SPEC.md §9（阶段汇总与归档）

## 改动范围（文件域，互斥）

允许改：`工程控制/RELEASE-03/ACCEPTANCE.md`、`SUMMARY.md`
禁止改：`lib/**`、`docs/**`、`contracts/**`、`config/**`

## 步骤

1. 对每个任务：**机器门**（CI/单测/合同检查器 rc=0）→ **证据核验**（前台**独立复跑**关键命令，不复用 SubAgent 自述）→ **文档-代码一致性**（改动与任务声明文件域一致，未越界）；
2. 按 §7.2 表格格式写 `ACCEPTANCE.md`：`| 任务 | 状态 | 机器门结果 | 证据路径 | 前台结论 |`；
3. 全部 PASS 后写 `SUMMARY.md`（§9）：目标、差距清单闭合情况、每模块状态、遗留项；
4. **真实数据终验先于完成声明**（§9）。

## 验收门（可机器复跑）

- [ ] [ ] 每个任务在 `ACCEPTANCE.md` 有归宿（PASS/FAIL/BLOCKED，**不得留空**）
- [ ] [ ] 每个 PASS **有独立复跑证据**（非 SubAgent 自述）
- [ ] [ ] `SUMMARY.md` 含差距清单闭合情况（对照 `GAP_AUDIT.md`）
- [ ] [ ] 遗留项如实登记，**未过终验不写「完成」**

## 禁止

- **不得用 waiver 掩盖红灯**
- 不得以「工具问题/环境问题」掩盖失败而不给复现
- **不得把「能编译」当「验收过」**
- 不得在 main 外开分支
