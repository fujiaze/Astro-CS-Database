# 任务：FIX-202 HiPS 格式内部权重枚举作废

## 目标

让 HiPS 格式内部不再有「权重模式」枚举，与最高设计 §2.1 一致：HiPS 只存**帧级 SNR + 稀疏相对 SNR 比值**。

## 权威依据

- ASTROCS_DESIGN.md §2.1（全程只有 SNR，不存在「权重模式」）、§3.4（稀疏 SNR 层必须在交换合同里有位置）
- GAP_AUDIT.md §9.73 裁决 A44
- DESIGN-DRAFT.md「新发现」：`lib/infrastructure/aio/include/aio_ahpx_format.h:18,42`、`src/ahpx/aio_ahpx_writer.h:28,70`、`reader.h:55`、`reader.cpp:587`

## 改动范围（文件域，互斥）

允许改：`lib/infrastructure/aio/include/aio_ahpx_format.h`、`lib/infrastructure/aio/src/ahpx/**`
禁止改：`docs/**`、`contracts/**`、`config/**`、其它模块

## 步骤

1. **先补 Oracle/负例**：构造含/不含权重枚举的 HiPS 文件，写「读侧必须显式拒绝旧权重枚举」的负例；
2. 从 `aio_ahpx_format.h` 移除权重模式枚举与相关字段（`weight_mode` 及 `:18,:42`）；
3. `aio_ahpx_writer.h:28,70` / `reader.h:55` / `reader.cpp:587` 同步；读侧遇旧文件含该字段 ⇒ **显式拒绝**（不得静默忽略）；
4. 确认 HiPS 只承载**帧级 SNR + 稀疏相对 SNR 比值**；权重一律由阶段二现场算；
5. 格式变更按 §12「版本号不作为生效条件」用**变更编号/日期**登记。

## 验收门（可机器复跑）

- [ ] `grep -rn 'weight_mode' lib/infrastructure/aio/` **输出为空**
- [ ] 负例：含旧权重枚举的 HiPS ⇒ 读侧**显式拒绝**（rc≠0 且给出原因）
- [ ] 正例：正常 HiPS 读写往返一致
- [ ] `ninja -C build` rc=0；`ctest` 全绿
- [ ] `python3 ci/run_checks.py` rc=0

## 禁止

- 不得改科学公式/默认容差
- 不得在 main 外开分支
- 不得挂 waiver
- **破坏性格式变更须在 README/变更 claim 中写明兼容策略**
