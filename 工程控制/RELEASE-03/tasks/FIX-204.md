# 任务：FIX-204 排异按 N 自动选择（§9.71 裁决 3）

## 目标

把排异从「全局单算法 / 四档表」改为**逐像素按输入集数量 N 自动选择**，与一手实测 WBPP 表一致。

## 权威依据

- ASTROCS_DESIGN.md §4.5（逐像素排异：按该像素输入集数量 N 自动选择）
- GAP_AUDIT.md §9.71 裁决 3（负责人逐字：「这个也问过我了……按 WBPP 脚本表为准」）
- 一手实测 `run/RELEASE-02/FIX-REJ/wbpp/BatchPreprocessing/BPP-FrameGroup.js:1304-1312`（N<6 percentile / 6≤N≤15 winsorized / N>15 linear fit）、`:1229-1293`（合法性窗口）、`BPP-engine.js:2695-2719`（清单无 minmax）、`:1239-1240`（明文拒绝 min/max）

## 改动范围（文件域，互斥）

允许改：`lib/algorithms/**rejection**`、`lib/infrastructure/scheduler/src/module_adapters.cpp`、`tests/**rejection**`
禁止改：`docs/**`（DOC-203）、`contracts/**`、`config/**`（FIX-207）

## 步骤

1. **先补合成数据 Oracle 正例/负例**：注入高 SNR 卫星线/宇宙线（必被剔除）；无污染（**不得**误剔真实信号）；
2. 实现逐像素路由：N = 该像素的**几何可贡献帧数**（一次解析；**禁止**用整组帧数或掩膜后存活数代替）；
3. 映射表按 WBPP 实测：`N<6` percentile / `6≤N≤15`（或 BIAS/DARK 类帧）winsorized / `N>15` linear fit；
4. **禁止 min/max**（含 `NoRejection`、已废弃 CCD clip）；
5. 显式指定的合法性窗口按 `:1229-1293` 只**告警不硬阻断**；**禁止**静默改算法、**禁止**静默降级；
6. 实际方法/参数/N 写入排异 provenance；
7. 原四档表（`n≤3` / 4–7 / 8–15 / ≥16）**作废**（最高设计 §4.5 已同步）。

## 验收门（可机器复跑）

- [ ] 合成 Oracle：注入污染 ⇒ 被剔除（正例）；无污染 ⇒ 不误剔（负例）——**能红能绿**
- [ ] **1 worker 与 N worker 结果逐位一致**
- [ ] `N` 在档界（6、16）行为确定且可复现
- [ ] `grep -rn 'minmax\|MinMax' lib/algorithms/**rejection**` 无生产路径
- [ ] `ninja -C build` rc=0；`ctest` 全绿

## 禁止

- 不得改科学公式/默认容差（除非走变更 claim）
- 不得在 main 外开分支
- 不得挂 waiver
- **不得静默改算法或降级**
