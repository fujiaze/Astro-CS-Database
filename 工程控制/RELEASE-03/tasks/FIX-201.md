# 任务：FIX-201 aio 文件级唯一 I/O 边界

## 目标

让全仓文件读写**只经 aio**，与最高设计 §9「aio 是文件级唯一 I/O 边界」一致。

## 权威依据

- ASTROCS_DESIGN.md §9（aio 文件级唯一 I/O 边界 + I/O 点穷举 + 机器判据）
- GAP_AUDIT.md §9.73 裁决 U5（负责人逐字：「全部走 aio……没有其他需要读写的地方了」）
- CTR-01.md 🔴3（块↔文件 API 清单）
- ARCH-01.md（`aio_publish.cpp` / `fits_output` 的 `fopen`）

## 改动范围（文件域，互斥）

允许改：`lib/infrastructure/aio/**`、`lib/algorithms/drizzle/healpix_drizzle/**`、`lib/algorithms/**/fits_output/**`、`ci/`（新增机器判据）
禁止改：`docs/**`（DOC-201/202/203 承担）、`contracts/**`、`config/**`

## 步骤

1. **先补 Oracle/负例**：写机器判据脚本（扫全仓 `fopen` / `open(` / `ofstream` / `ifstream` / `std::filesystem` 写操作，除 aio 内**应为 0**），并证明**能红**（故意加一处 ⇒ 判红）；
2. 整改 `lib/algorithms/drizzle/healpix_drizzle/src/aio_publish.cpp`：文件写操作改经 aio；
3. 整改 `fits_output` 的 `fopen`：改经 aio；
4. 块↔文件 API（`aio_frame_export_block_fits` / `_block_xml` / `_all_xml` / `aio_frame_save_cache` / `load_cache` / `aio_pipeline_export_xml`）⇒ **删除或明确降级为非生产/诊断并登记**，确保无生产调用点；
5. 登记 I/O 点穷举表（阶段一读原始 FITS + 校准帧 → 写 HiPS；阶段二读 HiPS → 写天球 HiPS；阶段三读天球 HiPS → 写 FITS）与**机器判据**入 `ci/checks.json`。

## 验收门（可机器复跑）

- [ ] 机器判据脚本 rc=0（除 aio 外文件写操作为 0）
- [ ] 负例注入（故意加一处非 aio 写）⇒ **判红**（能红能绿）
- [ ] `ninja -C build` rc=0；`ctest --test-dir build` 全绿
- [ ] `python3 ci/run_checks.py` rc=0
- [ ] 块↔文件 API 无生产调用点（`grep` 证据）

## 禁止

- 不得改科学公式/默认容差
- 不得删检查器或挂 waiver
- 不得在 main 外开分支
- **不得用「兼容保留」为名留下生产路径的越界 I/O**
