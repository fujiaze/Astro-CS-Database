# 任务：EXP-201 实验：天光采样点权重 SNR² vs control_ivar

## 目标

用实验判定天光面拟合的**控制点权重**应取 `SNR²` 还是 `control_ivar`，消除同一科学链上两个相反定义。

## 权威依据

- ASTROCS_DESIGN.md §4.4（四概念分离升为正式条款：控制权重与产品方差严格分离）
- GAP_AUDIT.md §9.72（负责人逐字：「科学方面的就写待定，由实验证明」）
- MOD-01.md 🔴P11（`ASTROCS_DESIGN.md:459` + `10_sampling.md:38,45` 的 `w∝SNR²` vs `phase2_samp.md:56-59` 的「科学权重一律 control_ivar，SNR 仅 veto/回退」）
- run/RELEASE-02/design-merge/DESIGN-DRAFT.md §4.1-S1

## 改动范围（文件域，互斥）

允许改：`run/RELEASE-02/实验/**`（**不入库**，gitignore）
禁止改：`lib/**`、`docs/**`、`contracts/**`、`config/**`（**实验阶段只做实验，不改生产**）

## 步骤

1. **三种数据面**（负责人明确要求）：① **纯合成数据**；② **基于哈勃真实信号 + 科学生产噪声梯度的合成数据**（用 `run/RELEASE-02/实验/E05-真实数据验证` 的 M16 模板）；③ **`testdata` 真实数据**；
2. 两种权重各跑一遍同一链（天光面拟合 → 施加 → 马赛克），**其余变量全部固定**；
3. **判据必须非退化**（能红能绿）：预注册判据（如天光面恢复精度、马赛克残差 MAD、接缝跳变），**先写判据再跑**，跑完不得改；
4. 给出**误差预算**与置信区间（bootstrap）；
5. **≥5 轮独立复核**（最高设计 §11.1.1）——换实现、换数据切片、换判据顺序各至少一轮；
6. **结论与预期相反时改结论，不改数据**；实验代码 + 实验报告落 `run/RELEASE-02/实验/E0x-*/`。

## 验收门（可机器复跑）

- [ ] [ ] 三种数据面**都有**结果（缺一不算完成）
- [ ] [ ] 判据**事前冻结**（有时间戳证据），跑后未改
- [ ] [ ] 判据**非退化**：能证明「若换成明显错误的权重，判据会红」
- [ ] [ ] ≥5 轮独立复核记录在案
- [ ] [ ] 结论有**误差预算**支撑，非拍脑袋
- [ ] [ ] 实验报告含：假设 / 方法 / 数据面 / 判据 / 结果 / 局限 / 结论
- [ ] [ ] 结论落到 `GAP_AUDIT` 或变更 claim，并据此订正对应文档（后续 FIX 任务）

## 禁止

- **不得预设结论**（实验说了算）
- 不得改生产代码
- 不得改 `docs/science/**`、`docs/algorithms/**`（结论先落实验报告，订正另走变更 claim）
- **实验材料不入库**（`run/*` gitignore）
- 不得改数据去迁就预期
