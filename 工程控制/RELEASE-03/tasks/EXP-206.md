# 任务：EXP-206 实验：噪声模型两套取哪套

## 目标

判定 SCI-NOISE-001 §5/§5a 的两套噪声模型实现取哪套，收口 §9.66 B / §9.67 定案 3。

## 权威依据

- ASTROCS_DESIGN.md §2.5（epsilon 必须说明物理/数值来源，禁止裸常数）
- GAP_AUDIT.md §9.67 定案 3（双实现登记，**未闭合**）
- `lib/algorithms/noise_snr/cpp/src/noise_model.cpp`（A，797 行，`snr_noise_model_v1`/`_fill`/`_free`）

## 改动范围（文件域，互斥）

允许改：`run/RELEASE-02/实验/**`（**不入库**）
禁止改：`lib/**`、`docs/**`、`contracts/**`、`config/**`

## 步骤

1. 核清两套实现各自的口径、适用域、退化行为（含 `variance_bg_global=0 ⇒ fill` 全零平面这一已知退化）；
2. 同一输入上对拍两套：逐像素方差值、退化触发条件、性能；
3. 判据事前冻结：与解析复算的方差一致性（已知增益/读出噪声/天光下可解析）、退化是否 fail-closed；
4. 三种数据面；≥5 轮独立复核；
5. 定案后收敛为一套生产实现 + 一个 Oracle（最高设计 §3.6「单一生产实现通则」）。

## 验收门（可机器复跑）

- [ ] [ ] 两套的口径/适用域/退化行为**有实测证据**
- [ ] [ ] 与解析复算的方差一致性达容差（容差来源写明）
- [ ] [ ] 退化行为是否 fail-closed 有正/负例
- [ ] [ ] ≥5 轮独立复核
- [ ] [ ] 结论明确（取 A / 取 B / 合并）并说明理由
- [ ] [ ] 收敛方案符合「单一生产实现 + 一个 Oracle」

## 禁止

- 不得预设结论
- 不得改生产代码
- **实验材料不入库**
- **不得留两套生产实现**
