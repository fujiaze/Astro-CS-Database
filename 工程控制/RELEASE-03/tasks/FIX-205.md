# 任务：FIX-205 投影未实现显式报不支持

## 目标

让未实现的投影**显式报「不支持」**，不得声称支持；与最高设计 §5.3 一致。

## 权威依据

- ASTROCS_DESIGN.md §5.3（冻结清单 8 种；未实现的必须显式报「不支持」，当前登记仅 TAN 已实现）
- MOD-01.md 🔴P17（`phase3_proj.md:40-42,71` + `registry/astrocs.phase3.wcs.md:135-136` 说只有 TAN；代码 `p3_projection.cpp:1` 实现 4）
- ARCH-01.md 🔴14

## 改动范围（文件域，互斥）

允许改：`lib/algorithms/coverage/**`（含 `p3_projection.cpp`）、`tests/**projection**`
禁止改：`docs/**`（DOC-202）、`contracts/**`、`config/**`

## 步骤

1. 先核清**实际实现了几种**（文档说 1、代码说 4 ⇒ 以**可运行验证**为准，逐一跑通/跑不通）；
2. 未实现的投影：请求时**显式报「不支持」+ 列出已支持清单**，**不得**静默回落 TAN、**不得**声称支持；
3. 已实现的：补齐适用域声明（含 TAN 的 `|dec| ≤ 85°`、FOV ≤ 20°、手性 `det(CD) < 0`、CRPIX 用 FITS 1-based 像素中心、往返误差 < 1e-6 px），违反 ⇒ 拒绝；
4. 投影注册表与实际实现**必须一致**（注册表是唯一权威）；
5. 补负例：请求未实现投影 ⇒ rc≠0 且给出原因。

## 验收门（可机器复跑）

- [ ] 负例：请求未实现投影 ⇒ **rc≠0 + 明确原因**（不得静默回落）
- [ ] 正例：已实现投影往返误差 < 1e-6 px
- [ ] 注册表声明集 == 实际可运行集（机器判据）
- [ ] `ninja -C build` rc=0；`ctest` 全绿

## 禁止

- 不得改科学公式/默认容差
- 不得在 main 外开分支
- 不得挂 waiver
- **不得声称支持未实现的投影**
