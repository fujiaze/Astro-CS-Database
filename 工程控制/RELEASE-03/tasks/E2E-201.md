# 任务：E2E-201 端到端重跑

## 目标

在 BLD-201 全绿后，端到端跑通三命令，验证裁决落地后的实际行为。

## 权威依据

- CONTROL_PACK_SPEC.md §6.2（执行流程）、§7.1（验收三层）
- ASTROCS_DESIGN.md §3/§4/§5（三命令合同）、§9（I/O 与原子产品）

## 改动范围（文件域，互斥）

允许改：`run/RELEASE-03/**`（不入库）
禁止改：`lib/**`、`docs/**`、`contracts/**`、`config/**`

## 步骤

1. 三命令各跑一次真实输入（`testdata`），rc=0；
2. **多块形态**跑一次（`blocks[]` 至少 2 块）⇒ 逐块派发、每块独立 manifest、块间不互相中断、**CWD 残留 0**；
3. **1 worker 与 N worker 结果逐位一致**；
4. 事件流**默认输出**（不带旗标）并校验 schema 唯一；
5. 产物结构校验：manifest / 完成清单 / 目录树哈希可重算；
6. 原子性验证：中途 kill ⇒ 正式目录无半成品。

## 验收门（可机器复跑）

- [ ] [ ] 三命令 rc=0
- [ ] [ ] 多块形态：逐块 manifest + CWD 残留 0
- [ ] [ ] 1/N worker **逐位一致**
- [ ] [ ] 事件流默认输出且 schema 校验通过
- [ ] [ ] 完成清单 + 目录树哈希**可重算一致**
- [ ] [ ] 中途 kill ⇒ 无半成品
- [ ] [ ] 日志与证据存 `run/RELEASE-03/`

## 禁止

- 不得改生产代码
- 不得以「环境问题」掩盖失败
- 不得在 main 外开分支
