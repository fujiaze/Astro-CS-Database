# 任务：EXP-204 实验：排异 N ≤ 3 是否保留「不排异」

## 目标

判定输入集极小（N ≤ 3）时是否保留「不排异」行为，与 WBPP 表对齐。

## 权威依据

- ASTROCS_DESIGN.md §4.5（排异按 N 自动选择；映射表以 WBPP 脚本为准）
- DESIGN-DRAFT.md §4.1-S3（WBPP 表无 `N≤3` 档；原四档表有 `n≤3` 不排异）
- 一手实测 `BPP-FrameGroup.js:1304-1312`

## 改动范围（文件域，互斥）

允许改：`run/RELEASE-02/实验/**`（**不入库**）
禁止改：`lib/**`、`docs/**`、`contracts/**`、`config/**`

## 步骤

1. 构造 N = 2、3 的输入集（含与不含污染两种）；
2. 两支：**不排异直接集成** vs **强制 percentile 排异**；
3. 判据事前冻结：① 污染是否被抑制；② 真实信号是否被误剔；③ 与 N≥6 行为是否连续；
4. 三种数据面；≥5 轮独立复核；
5. 定案后据此确定 `N<6` 档的下界是否含 `N≤3`。

## 验收门（可机器复跑）

- [ ] [ ] 两支在 N=2/3 上有结果（含污染/无污染）
- [ ] [ ] 判据事前冻结且非退化
- [ ] [ ] ≥5 轮独立复核
- [ ] [ ] 结论明确（保留 / 不保留 / 加显式警告）
- [ ] [ ] 与 WBPP 表的关系写清（对齐 / 有意偏离及理由）

## 禁止

- 不得预设结论
- 不得改生产代码
- **实验材料不入库**
