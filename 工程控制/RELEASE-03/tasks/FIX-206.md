# 任务：FIX-206 原子性缺口闭合

## 目标

让 HiPS tile 与阶段二输出走原子发布，使最高设计 §9 的「本期例外」可移除。

## 权威依据

- ASTROCS_DESIGN.md §9（所有产品含 HiPS tile：临时区 → 校验 → 哈希 → 原子改名；**本期例外如实登记**：HiPS tile 非原子、阶段二直写无暂存区）
- MOD-01.md 🔴P23（`registry/astrocs.phase1.hips-writer.md:80-81`、`phase2.write.md:51,132`）
- ARCH-01.md 🔴7

## 改动范围（文件域，互斥）

允许改：`lib/infrastructure/aio/**`（HiPS writer）、`lib/algorithms/coverage/**`（阶段二写出）
禁止改：`docs/**`、`contracts/**`、`config/**`

## 步骤

1. HiPS tile 写出改为**临时文件 → 校验 → fsync → 原子 rename**；失败/取消**清理临时产物**，正式产品目录只出现完整产品；
2. 阶段二马赛克改为**经暂存区**（不再直写输出目录）；
3. 补负例：写中途失败/取消 ⇒ 正式目录**不出现半成品**，临时产物被清理；
4. 两处缺口闭合后，**申请**从最高设计 §9 移除「本期例外」段（**由前台在验收时判定是否达成**）。

## 验收门（可机器复跑）

- [ ] 负例：写中途 kill ⇒ 正式目录无半成品、临时产物已清理
- [ ] 正例：正常写出的产品校验通过（哈希一致）
- [ ] `ninja -C build` rc=0；`ctest` 全绿
- [ ] `python3 ci/run_checks.py` rc=0

## 禁止

- 不得改科学公式/默认容差
- 不得在 main 外开分支
- 不得挂 waiver
- **未真正闭合前不得移除最高设计的例外登记**
