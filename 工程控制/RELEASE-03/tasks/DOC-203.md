# 任务：DOC-203 同批同步订正（12 条）+ 4 前置裁决

## 目标

先定 4 条前置裁决，再完成与之连动的 12 条同步订正，使详细层内部自洽。

## 权威依据

- run/RELEASE-02/design-merge/DESIGN-DRAFT.md §3.3（12 条）+ §4.2（Q1–Q14）
- CTR-01.md §6（判不了项）
- MOD-01.md（P10/P11/P17/P20/P21/P23）

## 改动范围（文件域，互斥）

允许改：`docs/contracts/**`、`docs/development/**`、`docs/design/**`、`docs/api/**`
禁止改：`docs/architecture/**`、`docs/interfaces/**`、`docs/standards/**`、`docs/modules/**`（DOC-202）、`docs/science/**`、`docs/algorithms/**`、`lib/**`

## 步骤

1. **先定 4 条前置**（前台裁决，记入 `GAP_AUDIT` 或变更 claim）：**Q2** v6 合同层（16 文档 + 10 份生产 schema）去留；**Q4** `resource_timeseries.csv` 唯一列合同（`LOG-002:44-48` vs `resource_recorder.h:260-266` 的 20 列）；**Q6** 事件流唯一 schema（`STRUCTURED_LOGGING_CONTRACT.md:38-59` vs 最高设计 §6.3）；**Q8** 块词表唯一登记处（aio 12 块 vs orchestrator 6 块，交集 4）；
2. 按 4 条前置的结论完成 12 条同步订正；
3. `snr_path` / `precision` 两键**不在 CLI 白名单**（实测 `parser.cpp` 0 命中）⇒ 与 FIX-203 对齐后订正合同文本；
4. `DATA_SEMANTICS.md` 的 drizzle 文件通道「weight / snr 面」按 A44 订正（与 DOC-201 协调，**不重复改**）；
5. `INDEX.yaml` 全表 `version 1.0.0` + 退役窗口 `0.11.0-alpha.2→0.12.0` ⇒ 按最高设计 §12「版本号不作为生效或退役条件」改日期或变更编号。

## 验收门（可机器复跑）

- [ ] 4 条前置裁决**有明确文字结论**（含依据），登记在案
- [ ] `grep -rn 'resource_timeseries' docs/ | wc -l` 对应的列合同**只有一处**
- [ ] `grep -rn 'STRUCTURED_LOGGING\|run_event' docs/` 事件 schema **只有一处**
- [ ] 块词表**只有一处**权威登记，另一处为引用
- [ ] `python3 ci/run_checks.py` rc=0；`tests/config` 58 passed

## 禁止

- 不得改 `docs/science/**`、`docs/algorithms/**`
- 不得在 main 外开分支
- 不得挂 waiver
- **结构性破坏变更须先上呈负责人**
