# 任务：FIX-207 三命令同构 schema/模板统一（§9.71 裁决 2）

## 目标

让三命令的配置 schema 与模板同构（块结构），旧形态保留但不再是 CLI 运行形态。

## 权威依据

- ASTROCS_DESIGN.md §3.3（三命令通用输入合同：顶层 blocks[] 或平铺单块简写，互斥）
- GAP_AUDIT.md §9.71 裁决 2（负责人逐字：「HiPS 类似阶段一……可以依然用块状结构，输出名称，运行参数+输入的一组帧构成一个大括号块」）+ 裁决 3（旧合同**不退役**）
- CTR-01.md 🔴2（`{phase_name, config, inputs[]}` 与 CLI 白名单不一致；`snr_path`/`precision` 不在白名单）
- run/RELEASE-02/merge/MULTIBLOCK.md（CLI 侧已就绪）

## 改动范围（文件域，互斥）

允许改：`contracts/schemas/phase_config_normalize.schema.json`、`phase_config_mosaic.schema.json`、`phase_config_export.schema.json`、`config/templates/**`
禁止改：`lib/**`（CLI 侧已完成）、`docs/**`（DOC-203）

## 步骤

1. 三份 schema **保留** `{phase_name, config, inputs[]}` 分支（§9.71 裁决 3：不退役），**新增** `blocks[]` 分支 + 平铺简写分支（**互斥**）；
2. 块内键集 = 该会话 `config_fields()` 派生 + `{name, output_dir}`；**按 A44 排除** `weight_mode` / `legacy_allow_weight_fallback` / `algorithm_weight_mode`；
3. 模板与帮助由**同一份键表**生成——**禁止**第二份键名清单；
4. `inputs[].product` / `inputs[].filter` / `phase_name` 在新分支中**不出现**（滤镜身份由 HiPS `properties.obs_filter` 承载）；
5. 旧形态**显式拒绝 + 迁移提示**（不得当普通未知键糊弄）。

## 验收门（可机器复跑）

- [ ] 三命令 `--json <blocks>` 预检 rc=0；`--json <旧形态>` ⇒ **rc≠0 + 迁移提示**
- [ ] 块内 `weight_mode` ⇒ rc=3（A44）
- [ ] `<cmd> --template` 输出能被自身 `--json` 接受（**往返一致**，三命令）
- [ ] 平铺单块简写与 `blocks[]` **同时出现** ⇒ 报错（不静默取一）
- [ ] `tests/config` 58 passed；`python3 ci/run_checks.py` rc=0

## 禁止

- 不得改科学公式/默认容差
- 不得在 main 外开分支
- 不得挂 waiver
- **不得退役旧合同分支**（§9.71 裁决 3）
