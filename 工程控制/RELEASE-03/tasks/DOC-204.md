# 任务：DOC-204 权威链与状态字段清理

## 目标

落实最高设计 §0.1/§0.2：只有一份权威链；登记表/映射表不得写状态字段；清除失实声明。

## 权威依据

- ASTROCS_DESIGN.md §0.1（只有一份权威链）、§0.2（详细文档层的地位：登记表与映射表不得写状态字段）
- ASTROCS_DESIGN.md §11（状态必须现场计算）
- MOD-01.md（`benchmark/PENDING.md` 等三处「本目录为空」失实声明）
- IFC-01.md 🟡Y1–Y4（义务悬空）

## 改动范围（文件域，互斥）

允许改：`docs/**`（**除** `docs/science/**`、`docs/algorithms/**`）、`README.md`
禁止改：`lib/**`、`contracts/**`、`config/**`

## 步骤

1. 删所有下级文档的「唯一权威 / 权威顺序」自我声明（`ARCH-001.md:5` 等），改为「**依 `ASTROCS_DESIGN.md` §X**」引用式；
2. 登记表 / 映射表**移除状态字段**（`MODULE_MAP.yaml`、`DOCUMENT_INDEX.yaml`、`STANDARDS_REGISTRY.md`、`PRODUCTION_EXECUTION_INVENTORY.csv` 等）——状态由检查器现场计算；
3. 清除失实声明：三处 `PENDING.md` 称「本目录为空」而实际有 20/64/38 文件 ⇒ 改为**如实登记**；`API-001.md:60` 称 `API_CONTRACTS.csv`「423 行」实测 382 行 ⇒ 订正；
4. 处置 IFC-01 🟡：`CODE_STANDARD.md:3` 引用的 `V19R2 MASTER_CONTROL_SPEC` **全仓零命中** ⇒ 改引用可解析来源或标注「来源已不可考」；`RELEASE_STANDARD.md:3` 要求 `CHANGELOG.md` 而该文件不存在 ⇒ 二选一（建文件或删要求）；`DOCUMENTATION_STANDARD.md:13` 要求 `docs/history/` 实际是 `docs/archive/history/` ⇒ 订正路径；
5. `STANDARDS_REGISTRY.md:341-368` 把「9 场景负向注入 + 空转守卫」写成强制义务但检查器未登记进 `ci/checks.json` ⇒ 补登记或降级为「计划」。 

## 验收门（可机器复跑）

- [ ] `grep -rn '唯一权威' docs/ README.md | grep -v 'ASTROCS_DESIGN'` **输出为空**
- [ ] 登记表/映射表中**无状态字段**（`grep -rn 'status:\|状态：' docs/modules/MODULE_MAP.yaml docs/DOCUMENT_INDEX.yaml` 检查）
- [ ] 三处 `PENDING.md` 的「本目录为空」表述**已改为如实登记**
- [ ] `API_CONTRACTS.csv` 行数声明与实际一致
- [ ] `python3 ci/run_checks.py` rc=0

## 禁止

- 不得改 `docs/science/**`、`docs/algorithms/**`
- 不得删除检查器或挂 waiver
- 不得在 main 外开分支
- **不得用「本目录为空」掩盖已存在的实现**
