# 硬阻塞协议

允许阻塞的情况仅包括不可恢复源码、不可替代数据、私有凭据、环境确实缺少必要 GPU/硬件且无软件替代、或需求根本冲突。

阻塞前必须检查仓库、Git 历史/分支/tag/远端、同级目录、TestData 说明文档、所有 Master 文件、构建产物和旧证据。生成 `BLOCKED_REPORT.md`，写明缺失对象、已查位置、命令与超时、最小用户动作和恢复命令。

“任务很大”“全量测试耗时”“浏览器重构复杂”“需要更多思考”不是阻塞。
