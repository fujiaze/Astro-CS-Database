# WCS-003｜AP/BP 布局扩展与消费方迭代反演（负责人裁决 1，选择 B）

## 背景（owner 裁决 2026-09-09）
WCS-002 已将 AP/BP 反变换从 ~42px 整改至 7.6438px，但 F2 冻结 1e-4px 在冻结 fixture（边缘畸变 ~117px）下数学不可达。负责人裁决选择 B：**AP/BP 布局扩展 + 消费方迭代式反演**，不接受缩小 fixture 畸变量级（方案 A），暂无依据修改冻结门（方案 C）。当前 `<50px` 仅为防退化观察线，**不得作为验收线**；7.6438px 不具科学可用性。

## 目标
1. AP/BP 表达能力与布局扩展（网格规模、拟合阶上限、布局容量按畸变场需要升级）。
2. 消费方迭代式反演语义（迭代重线性化/不动点迭代至收敛，替代一步语义）。
3. 独立 Oracle 与第三方 WCS 交叉验证（Astropy 或等价独立实现，只用于测试面比对，不引入生产依赖）。

## 依赖
`WCS-002`（commit 04556761）。BASE 取执行时最新 main，不得回写或重写既有提交历史。

## 写入白名单
- `lib/plate_solve/cpp/ipv/src/ipv_wcs.cpp`（及同模块私有头，如确需）
- `tests/unit/p1wcs/`
- fixture 生成/加载文件（畸变档位数据）

## 硬性要求
1. **保留当前高畸变 fixture 原样**，新增低、中、高三档畸变数据（不替换原 fixture）。
2. 验收覆盖（全部满足才可申请 PASS）：
   - 中心 90% 区域；
   - 图像边界与角点；
   - 不少于 1000 个确定性随机采样点（固定种子）；
   - 奇点附近明确拒绝或收敛失败（确定性，零 UB）；
   - 1 worker/N worker 一致性；
   - Astropy 或等价独立 WCS 交叉验证。
3. **最终恢复冻结门：AP/BP/迭代反演误差 <1e-4 px；合同规定的完整 roundtrip 门保持不变。**
4. 在恢复冻结门之前：WCS-002 保持「整改执行完成、科学 finding OPEN」；G-SCI 不得开放（G-SCI 门已加 WCS-003 依赖）；Phase1 不得标记科学验收通过。
5. 科学公式与容差定义不得改——本任务是让实现能力达到冻结口径，不是改口径。
6. 独立提交；不与 P2/P3/P1 节点化混合。

## 纪律
- 不顺手修复域外问题；发现后登记 finding。
- 不 reset/stash/clean/rebase；不创建 branch/worktree/clone。
- SubAgent 不 git add/commit/push，不修改 TASK_LEDGER。
- 所有外部命令带 timeout 存日志；未运行不得写 PASS。

## 验收
- write_scope 零越界；预存 dirty 不被覆盖；生产源仅限白名单。
- 上述 6 类验收覆盖全绿 + 第三方交叉验证通过 + 冻结门恢复。
- 前台复跑后才可 PASS；一个任务一个原子 commit 并 push main。

## 返回证据
schema 要求的 scope/acceptance/provenance + artifacts（数值证据表：各档 fixture 各区域误差分布）；如有外部阻断给出可复现条件，不得伪造 PASS。
