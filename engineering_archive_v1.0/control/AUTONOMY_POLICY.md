# 自治执行策略

## 默认策略

- 用户不参与逐任务调度；
- 单主任务串行执行；
- 只读调查可并行；
- 代码修改任务不可并行；
- 每个任务必须独立复核；
- 复核 PASS 后自动推进；
- 不自动 push；
- 不覆盖用户已有 dirty 修改；
- 真实硬阻塞才请求用户动作。

## 自治不等于放宽验收

自治模式只减少人工调度，不减少测试、证据、契约、Gate 或复核。Agent 不得用“用户未回复”作为停止理由，也不得因为用户不参与而自行改变项目目标。
