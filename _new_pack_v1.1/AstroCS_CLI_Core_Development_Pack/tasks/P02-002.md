# P02-002 共享 detections 候选路径与 star_det v1

- 阶段：P02
- 依赖：P02-001
- Gate：G2

## 目标

实现实验性的 `ipv_solve_from_detections_v1` 和 `star_det v1`，但不切换正式 CLI。

## 执行步骤

1. 严格遵守 `docs/05_STAR_DETECT_PSF_DEDUP_SPEC.md`。
2. 新增功能先写 contract/unit tests。
3. 保持 PlateSolve 检测后的选星、匹配、RANSAC、refine 和 WCS/SIP 算法不变。
4. 候选路径仅由测试开关启用，生产默认仍走原路径。

## 必需交付物

- 代码/文档差异与原子提交建议；
- 自动化测试和真实数据证据；
- 兼容性、回滚和残留风险；
- `evidence/P02-002/` 下四份标准报告。

## 验收

- 依赖任务均已通过；
- 本任务目标有可复现证据；
- 相关回归全部运行；
- 独立复核以 `VERDICT: PASS` 结束；
- 更新任务注册表、当前任务和项目状态。

## 通用执行规则

- 开始前记录 commit、工作树和入口条件。
- 先复现旧行为，再做最小修改。
- 外部进程、数据库和可能阻塞操作设置明确超时。
- 不覆盖用户未提交修改，不强推或重写历史。
- 所有接口/格式变更同步更新契约和测试。
- 产出 TASK_REPORT、TEST_REPORT、EVIDENCE_INDEX、REVIEW_REPORT。
