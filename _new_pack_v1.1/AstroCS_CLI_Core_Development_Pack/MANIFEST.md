# Manifest

本包面向 AstroCS CLI Real-Data Core v1，包含：

- 19 份架构、数据、协议、测试和发布 Spec；
- PlateSolve 全量 TestData 条件迁移与保守回退规范；
- PipelineFrame/CLI/错误码契约；
- 单次星点检测、内部 detection export 与 float32 PSF 的 ABI 参考草案；
- 31 个有依赖关系的执行任务；
- G0–G8 验收清单；
- Agent 执行、复核、阻塞和恢复协议；
- 状态、需求、风险和决策注册表；
- 标准证据模板；
- 100 字以内启动提示词。

入口：`AUTONOMOUS_ENTRY.md`。
