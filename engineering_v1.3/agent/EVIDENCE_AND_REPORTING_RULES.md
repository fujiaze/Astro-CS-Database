# 证据与报告规则

- 原始日志只增不改；摘要必须可追溯到具体文件和行。
- 所有数据集列出相对路径、SHA-256、设备 ID、滤镜、面板、曝光和校准映射。
- 性能报告记录 CPU、GPU、RAM、磁盘、分辨率、驱动、构建类型和冷/热缓存状态。
- 图像对比使用相同 RA/Dec、FOV、STF、窗口尺寸和色彩映射。
- 科学指标输出 CSV/JSON；截图仅作辅助。
- 任何 fallback、跳过和退化均必须出现在 CLI JSONL、报告和 provenance 中。
