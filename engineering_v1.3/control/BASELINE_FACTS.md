# 已确认基线事实

1. 当前 `main` 已包含 PlateSolve 内部单次检测 + callback 共享导出，710 帧 A/B 无成功率回归。
2. 当前代表帧 Gaia DR3SP 读取与合成积分可产生大量有效 `F_syn`；测光失败发生在空间匹配/拟合之前或期间。
3. WCS Y 轴与 SIP 奇次项符号为重点嫌疑，但尚需标准星对闭环证明。
4. TestData 只有 T1–T4 四套设备；用户确认全部主校准帧配齐。
5. 银心有 panel1/panel2/panel3，Red 共 32 帧，是正式马赛克数据集。
6. v1.1 的 Stage2 证据未验证真实三片非零梯度；真实 HISS 曾 `has_snr=false`。
7. 浏览器 HCSD 打开会全量读一次；按叶读取重复开文件/解头；渲染线程同步加载；CPU 每顶点查值并重传 VBO。
